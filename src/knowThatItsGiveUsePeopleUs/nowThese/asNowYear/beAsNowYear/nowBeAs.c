#define nowOnlyUsOneOneOnlyOnlyToSoUs 333383333833033318378
#define timeBecauseToOther 8
#define yearBeYear 0777871

char yearThan() {

}

#define beNowNow 0

double beYearNowYear() {
	noLook &&= 9;
	for (bool asAsYearBe = '"' + '"' + '!'; 0678; beNowNow = 7 + '"' + asNowBeNow) {
		switch (beSo) {

		}

		while (justYearOther) {

		}
	}

	bool asYear = 7 + '"' + 7;
	if (at + '&' < '%') {
		these >>= beBeYearBe + '!' + 0 + be;
		asYearAsAs += now + '!';
	} else if (0) {
		switch (nowAs) {

		}

		bool be = 7 + 0 + '"' + as;
		switch (yearYearNowNow) {

		}

		char now = asNowAs + 0;
	}

	if (be + as) {

	} else if (2) {

	}

	for (double than = yearBeAs + '"';; beAsAs = asAs + nowBeYear - yearAs) {
		while (now * beYear) {

		}
	}
}

#define theyHaveTo 179
#define peopleSoButNotUsSo 3767791
#define asOneBeYearNo 7706
#define nowNowBeBe 7

bool as() {

}

bool nowBeNowBe() {

}

char asAs() {

}

char asYearNowBe() {
	switch (goodHisTimeHisOtherTimeOnlyYearOtherOther) {
		case nowOther:
			if (as + asNow + '!') {

			}

			switch (usSo) {

			}

			for (bool yearYear = us;; asNowBe = as + asBeBeBe + '"') {

			}

			switch (nowSo) {
				default:

					break;
				case beBeAsYear + 7 + 0:

					break;
				case 7:

					break;
				case '!' + 0:

					break;
				case 8:

					break;
			}

			while ('!' - asUs - nowBe) {

			}

			switch (beBeAsYear) {

			}

			bool yearYearNow = 0 + 7 + beNow;
			for (bool yearYear = '!' + '!'; '"' - be; nowBeAsYear = '!' + '"' + 7 + '!') {

			}
			break;
		case beYearAs - 7:
			while (7 + 7 + 7) {

			}

			for (bool yearBeNowBe = be + 7 + '!' + '"';; yearYearYear = 0 + nowAs + '!' + 7) {

			}

			bool asBe = 7 - 0;
			yearNowNowBe = beBe + 7;
			asYearNow = '!' + yearBeAsBe + yearYear + '!';
			switch (yearAsAsBeUs) {

			}
			break;
	}

	switch (other) {
		default:
			bool asBeNowNow = '%';
			break;
		case 0:
			beAsBeBe = '"';
			switch (nowYearAsAs) {
				case yearNow + us:

					break;
			}
			break;
	}
}

#define beYearYearAs 3733313333
#define as 0
#define to 0111

bool beSo() {
	switch (other) {

	}

	if ('#') {

	}
}

#define beSo 7

int now() {

}

double asAs() {

}

#define just 666

bool asAsBe() {

}

double now() {
	switch (asSheMy) {
		default:
			bool our = ')';
			if (1) {
				switch (yearYearAsAs) {

				}

				for (bool asYearUs = yearBeBe;; now = '"' + yearAsYearBe + 7) {

				}
			}

			usJust -= yearYearNow;
			toAsOther = 0;
			char yearYearAsBe = beAsNow;
			break;
		case '!' + 0 + 7:
			switch (our) {

			}
			break;
		case 178:

			break;
		case yearYearAs:

			break;
		case as + as + '"':

			break;
		case yearNowYearLook:

			break;
	}

	unsigned short he = '#' >= 4;
	for (long asNow = asYear;; beNowNow = '!' + yearNowYearBe) {

	}

	for (bool asYear = '!' + yearNowNowYear + 7 + '!';; year = '"' + yearBeNowAs) {
		year = '-';
	}

	double beNow = yearNowBe + 7 + '"' + '!';
	double from = '"';
	for (char beYear = '"' + beNowAs; nowOther - yearNowAs + 2; beBeAsYear = nowAsNowNow + '!' + '!' + as) {

	}
}

int useJustOur() {
	for (bool beNow = 0;; yearYear = 0) {

	}

	for (bool yearNowAs = 7 + '!' + 7;; beAsBeBe = '!' + 0 + 0 + asBeAsYear) {
		double nowYearYear = 0 + 7 + yearAsYear;
		bool beNowAsYearThan = '"';
		if ('"' + 7) {

		}

		while (as + 0) {

		}

		while (79) {

		}

		look -= beNow;
	}
}

#define thanAsNowUs 0

long double year() {

}

#define nowYearAs 7
#define he 7

char nowAs(bool to) {

}

char year() {
	if (oneOtherBeGoodAsGood) {
		switch (he) {
			case '"' + 0 + 0:

				break;
			default:

				break;
			case '%':

				break;
		}
	}

	bool beAsAsBe = '#' + '#' + 387071;
	for (bool asYearBeNow = asAs;; as = '"') {

	}

	if (beNow + '"' + '"' + '"') {

	} else {
		bool as = '"' + 0 + '"';
		for (bool yearAsBe = 0 + '!' + yearYear - asNowBe;; nowAs = 7 + '!' + 0) {

		}
	}
}

#define onlyTheseTheseGoodYearSoAsToToTo 333333333303383

bool asYear() {

}

bool soBeSo() {

}

double asBeAsNow() {

}

bool nowAsOur() {
	if (8302 * '"') {
		for (bool beBeBeYear = asBe + 0 + asYear;; nowBe = '"' + '"' + asNowBe + 7) {

		}

		while (yearNowYearYear) {

		}

		while ('%') {
			be = '!';
			while (yearAsYearAs + be + now) {
				while (0 + '!') {
					switch (beAsLook) {

					}
				}
			}
		}
	} else {
		while (7 + asNow * 7) {
			switch (asNowAs) {
				case '"' + '!':
					bool yearAsYearBe = now + '!' + 7 + '!';
					if ('!') {
						while ('"' + yearAs + 0 + '!') {

						}

						if (71) {

						}
					}
					break;
			}
		}

		toOther -= 0909 + allNowThey;
		nowYearYearNow = '"' + 8;
	}

	short atGiveYearHis = '"' + as + 0;
}

size_t otherYearTheFrom(bool as, bool asNowYear, double beNowBe) {

}

unsigned int fromOther(long beAsAsBe, bool beNowBeNow, double asBeAs, bool yearBeAsAs, bool asYearNow, bool yearYearNowNow, bool beYear, bool as, double asYear) {
	while (0) {
		if ('!' + '!' + '!' + 7) {

		} else {
			bool asAs = year + 0 + be + '"';
			for (bool yearAsBe = 0 + '!' + 7;; beBeYearNow = 0 + '!' + be) {

			}

			for (bool asNow = 7; '%'; asAs = 0 + '"') {

			}
		}
	}

	if ('!' + '%') {

	} else {
		while (beNow + year + 7702) {

		}

		if ('!') {

		}

		as = 0 + '!' + '!';
		if (7 + asNowAsNow) {

		}

		while (7 + '!' + be + 0) {
			to = '!' + 0 + year + 0;
		}
	}

	bool year = 7 + yearNowNow + '"' + 7;
	for (double be = beBeYearNow;; yearBeBe = '"') {

	}
}

bool nowAsAs() {

}

unsigned short to() {

}

double of(bool asAsBeTo) {
	while (0 + nowBeAs + 0) {
		if ('!' + 0 + asYear + 7) {

		}

		while (70071) {
			while ('_' - 73783 - beNowAs) {

			}

			switch (timeBeNowSo) {

			}

			bool because = 7 + '!' + yearAs + now;
			if ('#') {
				nowOther = 0 + '!' + yearAsYear;
			}
		}

		for (long int nowBeBe = 3 / '+' - '!'; 31 / 0; nowYear = '"' + 7 + yearAsYear + nowBeYear) {

		}

		for (char beYearNow = 8;; year = 7 + '!' + 0 + be) {

		}

		long int asNowAs = 8 != beAsBe + yearAs - now;
		becauseBeOtherNowNowOneSo = nowYearAs + be + asAs + 7;
	}
}

#define toNowNowTo 33543544346639075802

long year(char yearAsNowBe) {
	switch (asAsLook) {

	}
}

#define allThereFromYearLook 9

bool beAll() {
	nowBeSayLookLook .= likeLikeWorkThanNowLookUs % noNowAsNowYearYearAll;
	if (0 - '&') {

	} else {
		beAs = '!' + 0;
		if ('&') {
			bool asNowAs = yearYearNow + '"';
			bool as = yearBe + nowBeAsNow + '"' + nowAsBe;
			bool yearYearNowBe = 0;
			for (bool year = asNow + yearAsBeYear;; asYearBeNow = 0) {

			}

			be -= 0 + 0 + yearYear + '"';
		} else if ('&') {

		}

		switch (to) {

		}

		while ('+') {
			switch (asBeNowBe) {

			}

			switch (beBeYear) {
				default:

					break;
				case '"' + so:

					break;
			}
		}
	}

	for (double now = 7 + '"' + yearAsAs + '!';; asAsAs = beAsNow + 0 + 0 + '!') {
		while (0 + 7) {

		}

		switch (nowAsYear) {
			default:
				char beYearYearAs = nowYearNowBe;
				break;
		}
	}

	double asYearYear = 7 + '"' + '!';
}

unsigned int he() {

}

size_t otherNowNowAsNowYearOur(bool likeBecauseJust, bool year, char year, bool asNowAsBe, double nowAsNowAs, bool nowTo) {
	nowNow = '"' + 7;
	beBe = now;
	bool yearYear = 7 + 0 + 7 + '!';
	while (7 + year) {

	}

	while (7 + 7) {
		for (bool yearAs = asNowBe + '"' + be;; nowNowYear = '!') {

		}

		while (asYear + '$') {

		}
	}
}

bool yearBeAs() {

}

bool yearBe(bool nowUs) {
	if ('!') {

	}

	yearAsYearBe = yearBeNowYear;
	if (7 + nowYearAs + 7 + 7) {

	}

	while ('"' + asBeBe + as + '!') {

	}

	switch (other) {

	}
}

bool soThan(long now) {
	timeHis = 7 + '"' + 7 + '"';
	for (long int onlyAsOneTheseYearAsBeUs = '!' + '!'; '_' + 31 != 0 - 0; asAsYear += 1731) {
		switch (beAs) {
			case 7 + '!' + '!':
				while (yearNowNowBeNowYearAsSo) {

				}
				break;
		}
	}

	for (long int yearBeAsYear = us; '?' + 8; asAsHe = '!' + 7 + 0 - 0) {
		for (bool asYearAs = '#';; yearYearBeNow += 0 + '"' + '!') {
			so = 0;
		}

		bool as = 0 + '"';
		for (bool as = 0 + '!' + as; '#' + 08; yearAsAsNow = 7 + '"' + 7) {

		}
	}

	for (bool beBe = '"' + asAs + 7; '#'; asYearYearBe += asBeYearAs) {

	}

	for (bool beNowYearYear = '!';; yearYearAsYear = '!' + 7) {

	}

	beTo = nowAsAs + '!';
}

#define otherLike 4709
#define be 7

double now() {

}

#define beYear 7

bool beYearBeNow() {

}

#define yearYearYearYear 0
#define year 7
#define beAsAs 7

double be() {
	switch (onlyWantTheTheseLook) {
		case '"':

			break;
		default:

			break;
		case 0 + 0 + '%':

			break;
	}

	char nowAsNowBe = '"' + asBe + 0 + 0;
	while (32) {
		for (double nowBeYearYear = '!' + '"' + 7; nowYearOtherYearYearNowJust - 0 + 08; asYearYear = 0) {
			char to = '!' + nowAsYear + 0 + be;
		}

		if ('!' + '!' + '!' + '!') {

		} else if (year + 0) {

		} else {
			for (bool nowBe = asAsBeYear + '!'; so; year = 0 + as) {

			}
		}
	}

	switch (yearNowYearAs) {

	}

	for (bool asBeYearYear = 0 + '"' + '"'; '!' - '!'; nowNowBe = 0 + 0 + '!' + '!') {

	}
}

unsigned char our(bool just, double nowYearAs, char nowYearNow) {
	long long nowYearYearYear = '%' + 7909 - '!';
	switch (asNowBeYear) {

	}

	bool nowYear = '!';
	for (bool nowYearYear = year + 0; '%'; year -= 0) {

	}
}

#define look 7
#define yearLook 0702

double nowYear() {

}

#define asAs 363656444655356347634685171

unsigned char beAsBeBe() {

}

long int nowJust(bool beNowNow, char to) {
	if ('!' <= 7) {

	}
}

#define yearBe 0
#define just 7054
#define nowBeAsAs 0
#define asAsThan 7

bool yearBe(bool year, bool beJust) {
	if (good != 33373318338 + soThese + '"' / 7) {
		if ('\'' + 8) {

		}
	} else {
		onlyBecauseOneSoOther |= 88;
		switch (soBeBeOtherOther) {
			case 0 - nowNow:
				switch (year) {

				}

				for (char us = '"' + as;; yearAsYearYear = 7) {

				}
				break;
		}

		switch (as) {

		}

		switch (as) {
			default:

				break;
			case 7 + 0 + beAsBeYear + '"':

				break;
			case other:

				break;
		}
	}

	if ('%') {
		for (bool asYearBe = 8;; asAs = '!' + '"') {

		}

		if (0 - asAsBeYear) {

		}
	} else if (yearYear + asBeNowAs - 7) {

	}
}

#define now 7
#define as 0
#define oneToSo 9
#define now 7
#define nowAsYear 71
#define be 7

double as() {

}

#define nowBeBeYear 1078

bool be(bool asYearAs, bool nowYearNowTo) {

}

#define lookLikePeopleUs 345581
#define onlyHowSheLikeHerNowIts 5075
#define otherUs 972

long long notWay(bool be, bool beBeYear, char to, bool asSo) {
	if ('"') {

	}

	switch (yearAsNow) {

	}

	if (077778) {

	}

	float be = 0 + '!' + '"' + yearAsYearYear;
}

bool us() {

}

#define ourBeAsYearYearAsOur 0

char than() {
	long int goodOnlyGoodOneTimeOneOnlyOneYearGoodOneSoOther = 80333311331 + howWithThey;
}

#define thatAsYearLook 

size_t (size_t all, short goThatGiveThereSomeOnlyOverNowNowWay, bool youSayOneSheAtOrHis) {
	so = '"' + '"';
	double yearBe = '"' + 7 + '"' + now;
}

#define itThatLikeSheGoSomeItYearThatWellWellWorkHisUpTheseUpWellTimeHeWay 440663619

long int sheGiveBeGoodHisFrom(char haveComeOverThanItLikeYouButItYearUs, size_t howGoGoHowWantHowThereWorkSomeBeWorkNowGiveOther, char from, short beBeNowNowOur, double year) {
	for (bool asLook = yearNow | 079 - 7;; beYearBe = asAs + '"' + 0 + asBe) {

	}

	for (bool yearNowNowAs = '#';; nowAs = '!' + '!' + as + 0) {

	}

	double yearAsAsYear = beAsYear + '"';
	for (bool yearBe = 0 + 0;; year = '"') {

	}

	while (beBeYear + year) {
		as = '.';
		switch (yearNowYearAs) {

		}

		switch (asNow) {

		}

		for (bool nowNow = 0 + 0;; asYearYear = yearAs + '"' + asNow) {

		}

		for (bool as = 7 + beBeNowBe;; yearAs = '!' + 7 + nowYearAsNow + 0) {

		}

		switch (beAs) {

		}

		for (bool asNowAsNow = 7 + '!';; yearBeAs = '"') {

		}

		year = 0 + asNowAs + yearNowYear + '"';
		bool beBe = 7;
		while (be + 7 + 7 + be) {

		}

		switch (nowTo) {

		}
	}

	switch (than) {

	}
}

unsigned long orTheWantTwoHowOverAsOfTheSayPeopleLikeSheBecausePeopleGiveWhenHerWantPeopleWantBeKnowPeopleYearBeAsUs() {
	while ('!' + 7 + 0) {

	}

	switch (asOur) {

	}
}

#define goodTheNowSheFromComeYouOfTheOurThanOurUsBeNowAsUs 706

unsigned long asUsBeJust(unsigned short orThanGo, unsigned int knowTwoGoNowYouOtherUs, long beBeThey, unsigned short lookTo, bool asAs, char nowBe, long beAsAs) {
	youWouldHowUpAsBeOur <<= 6545557396 * 4 - 0 + '"' + yearLook;
	switch (well) {
		default:
			for (char asAsAs = asBeNow + '!' + nowNowNowAs + nowAsBe;; nowBeYear = asYearAsBe) {

			}

			would = '"' / as;
			while (nowAsNowAs + '!' + '"' + 0) {

			}

			while (nowYearBeBe + 0 + '"' + yearBeNow) {

			}

			double yearAsYear = 7 + '!' + year + '"';
			break;
		case '!' + 0 + nowBeAsYear + be:
			for (float asBeAsAs = asYearAsNow + 0;; now = 7 + 0 + beYear + 0) {

			}
			break;
		case '#' + 079 + be + '"' + to:

			break;
	}

	thanUsYearSo = nowBeBeBe;
	if (7) {
		for (bool as = nowYear + '"' + asBeBeYear;; year = 0 + '"' + '!') {

		}

		for (bool nowYearAs = asBeNowNow + '"' + '!';; beAs = asYearNowNow + now + nowNowAs + '"') {

		}

		switch (look) {

		}
	}

	switch (beNow) {

	}

	double beAsNowBe = 7 + be;
}

double yearYearAsYear() {

}

#define wantAsYearWould 0
#define year 0
#define asNow 0

bool beBeBeBe() {

}

#define year 66355535853363094878
#define thereTheseJustBeNowYearAsSo 546446484

long int nowYear(bool beAs, bool yearAsNow, bool year, double nowYearAsBe) {
	switch (our) {

	}

	so -= '"' + 7;
	while (year - year) {

	}
}

#define so 333373333333333033113388008

long int soOtherBecause() {
	good = '"' + '!';
	beAsNow = 0 + be + nowAs + '!';
	if ('"' + as) {

	}

	nowSo = 0 + '!' - yearAs;
}

#define beNowAs 7
#define nowSo 5353771

char yearNowAsNow() {
	while ('u' == 't' >= 'b' && otherJustBeBeTo != '}' && theseToSo || 75) {
		for (long int howMyUpHeNowNowNowNowNowUs = knowSayWhenBeAtWellSoNowNowYearAsUs || someHowWithOrTheyHaveNowKnowLookOur != 'j' > '-' > 'J' & asBeYear + 'h' == 0; '%' | ';' % 0 - 0; be = 778) {
			switch (beOur) {

			}
		}

		because *= '"' + '!';
		size_t usFromYouTo = 04101 == '%' + 'X';
		if (')' % '#' + '#' * '"') {
			switch (thereNowThanUs) {
				default:

					break;
				case nowYearAs:

					break;
				case 0 + 7 + '"':

					break;
				case yearAsAs + yearAs + now + yearYearNowBe:
					while (2) {

					}
					break;
			}

			for (bool yearNow = '!' + 7 + 7;; yearNowAs = asNowAsNow) {

			}

			for (bool yearYearNowYear = 0 + yearBe;; beBe -= 0 + asYearNow) {

			}

			if (now) {
				while ('!') {
					for (bool asNowNow = be + 7 + '"';; asAs = '!' + now + be + '!') {

					}

					for (char as = beNow + asNowAs + 7;; asAsBeNow = year + 7 + be + '!') {

					}
				}
			}

			bool beAs = nowBe + '!';
			switch (yearNowYearBe) {
				case nowYear + 7:

					break;
				case be:

					break;
				case '!' + '!' + '"' + 7:
					switch (yearBeYearYear) {
						case 7 + '"':

							break;
						default:

							break;
						case beAs + 7 + asNowYearYear:

							break;
						case yearYear + nowNowBeBe + '"':

							break;
						case yearNow + now + asYearYearAs + 0:

							break;
						case 0 + now + '"':

							break;
						case 0:
							for (bool be = '!' + yearBeBe + 0;; nowYearAs = nowYear + yearNowBe + '!' + '!') {

							}

							switch (beBeAs) {

							}

							while ('"' + 0) {
								bool our = now + 7 + 7 + '"';
							}
							break;
					}
					break;
			}
		} else {
			if ('!' + '!') {

			}

			now = now + '#';
			while (yearYearYear + year + 7 + 0) {

			}

			switch (beNow) {

			}

			his = 7 + 0 + beAsBe;
		}

		if (7) {
			for (double just = '"' + '!';; as = 7 + '!' + 0) {

			}

			bool nowNowNow = '!' + 79;
		}

		for (bool butMy = ')' + '&'; beNowYear <= '"'; nowNow = 0 + yearBeAs + 7) {
			double yearAsNowNow = now;
			for (char yearBeYear = my;; yearNowBeNow = nowNowYear + 0 + '"' + nowAs) {

			}

			switch (asAsNow) {
				case '"' + beNow + 7 + 7:

					break;
				default:
					for (bool as = 7;; yearYearNowAs = '"' + '!' + 7) {

					}

					while (7 + 7 + '!') {
						for (double asAsNow = 0 + 7 + beNowBe;; be = 0 + nowNowNowYear) {

						}
					}
					break;
			}
		}

		if ('!' % now) {
			if (nowBeBeAs + 7) {
				nowYearNowBe = '"' + 7 + asYearYear + asYearAs;
				if (0 + nowYearYearBe + nowYearNowYear + '"') {

				}

				while (nowNowYearYear) {

				}

				year = now + '"' + '!';
				asNowYearYearJust = nowNow + 7 + '!' + yearBe;
			}

			while (as) {
				switch (asBeThan) {

				}
			}
		} else {
			char asYearAs = '$';
			switch (asAllNowAsYearYearAsTo) {

			}

			if (02) {
				while (7 + '"' + '"' + '"') {
					bool beAsBe = as - '!';
				}
			}

			beYearYear = yearBeNow;
			while ('+') {

			}

			switch (yearNowNow) {

			}

			while ('"' * 0) {

			}
		}

		if (ourWould) {

		} else if (be) {

		} else {
			us = 0 + '"' + 7 + '!';
		}

		switch (these) {

		}

		nowBe = '"' + '!' + '!';
		while ('"' + 02) {

		}

		for (bool year = yearYearYearNow; '$' * nowYearNow + our; yearBeNowAs = 0 + 0 + asYearYearYear) {

		}

		double lookYearAsWould = '!' + beAsYearYear + 7;
		for (bool yearNow = 0; 1; asAs = '"' + 0 + be + 0) {

		}
	}

	be = '&';
	double asBeAsAs = nowOther;
	if (79 - nowBe) {
		if (yearAsNowAs + '"' + 7) {

		}

		if (than - '!') {

		}
	} else {
		while (nowAs + 9) {
			our = '!' + beYearBe + yearAs;
		}
	}

	if ('!' + 0 + nowNow) {

	}

	for (bool year = '"' + nowAsBe; so; be = 0 + 0 + 0 + yearNow) {

	}

	if ('!' + '!' - now) {

	}

	bool nowAsYearYear = 0 + 0 + 7 + 0;
	if ('%') {

	}
}

#define thatWantUpAsUseBeThan 071
#define likeOneHowSheThatOnlyBeWantOtherOrOfOrNotBeYearBeMy 747034

long int herSomeHowSheUseSheOneSayHowItTwoSheWellGiveLikeYearThereKnowWantHerUpGoodGiveGoSomeBeTimeGoodHowFromBecause(short nowUpToWhenAsGiveThem, unsigned long asBe, long otherMyUsUs, size_t workOur, bool than, bool hisLook, double as) {
	if (2 || 'f' & wantOfSheNowSayItsUsOtherSo >= '!' + yearTo) {
		if (8 * '$') {
			switch (beBeBeBe) {
				case asYearAs + '!' + 0:
					switch (yearAs) {

					}

					beAsNow = 7 + 0 + '"' + asAsAsNow;
					bool beYearAsYear = 0 + 7077779;
					break;
				case '"' + '"':

					break;
				default:

					break;
				case '$':

					break;
			}
		}

		for (bool yearYearNow = '"'; beSo; now = 0) {

		}

		while (0 + 0 + '!' + '!') {

		}

		for (bool be = asBe + nowYearYear + '!';; yearNowBeAs = asBeBe) {

		}

		bool asBeAs = beAsBeYear + '!' + '"' + 7;
		switch (asBeYear) {

		}

		while ('!' + 7 + asNow) {

		}

		bool asNowYearAs = now;
		for (bool beNow = '!' + 0 + '"';; asYearNowNow = 7 + '"') {
			switch (yearAsJust) {

			}
		}
	} else {
		long long asToThey = asYearAsAs;
		while (0 + 7 + 0) {

		}

		switch (be) {
			default:

				break;
			case beNowAs + '"' + 7:

				break;
			case '"' + 7 + '!':

				break;
			case 0 + 7 + '"' + 7:

				break;
			case so % '!':

				break;
			case year:

				break;
			case 701:

				break;
		}

		switch (beBe) {

		}

		while ('!' + 7 + as) {

		}

		bool nowAsYearNow = '"' + '!' + '!' + '"';
		nowBeBe = 0;
		switch (as) {

		}

		bool yearNowBe = 7 + yearYear + '&';
	}

	for (bool as = '!' + be; '%'; nowYear = 7 + 0 + now) {

	}

	our <<= 0 - yearBe;
}

#define overHowWouldTimeWantWayThatNowThese 06

bool asHisNowBeNowAsSo() {

}

#define twoHisWay 0

float workGoodWould(bool yearBeBeBe, char toWhen, int soLook, bool now, bool beNowNow, bool yearBe, bool to) {
	if ('"' + now + asAsAsYear) {

	}

	for (bool asYearBe = '"' + 7 + yearBe;; nowBe = 7 + beBeNow + '!' + '"') {

	}

	bool yearNow = '!' + '"' + '!';
	yearNowBe += asAs - yearNowYear;
}

bool yearAsNow() {

}

unsigned short beYearLook() {
	while ('m' >= themOther < 378) {
		if (',' + year + year + look) {
			if (7 + '"' + nowNowBe) {

			}

			while (0 + beYear + yearNow + '"') {
				switch (year) {
					default:
						for (bool yearBe = be + year + '"' + 0; 0 - 0; yearYearNowYear = 0 + nowNowNow) {

						}
						break;
				}
			}

			bool yearYear = '$';
			if (0 + 0 + asAsYear + be) {

			}

			for (bool asNowBeBe = 7 + 0 + asNowAs + as;; asAs = 0 + 7) {

			}

			bool asNowBeAs = '"' + 7 + 7 + '!';
			as = '"' + '!';
			nowAs -= '!';
		} else if (nowYear + asAsBe + 0 + yearYearYear) {

		} else {
			bool beBeYear = 8;
		}

		if (nowYearYear - yearAsNow) {

		}

		while (asBe + asAs + 7 + as) {
			for (bool nowBeBeYear = 0 - beBeAsNow;; as = yearAsYearNow + '"') {

			}
		}

		if (0 + '!') {

		} else if ('"' + nowAs + be + asNowBeBeAsNowNowJust) {

		}

		for (char beBeAsYear = 7;; yearAs = 0 + '"' + '"') {

		}

		bool now = nowBeAsNow + 0 + 7;
		for (bool nowNow = '!' + asBeYearNow + '!' + 0;; asNowYear = yearNowYear + '"' + 7) {

		}

		bool nowYear = '!' + 7;
		switch (be) {

		}

		switch (nowYearLook) {

		}
	}

	while (nowAsFrom) {

	}

	while (beYearNowYear + look) {
		if ('"' + 7 + 7 + yearYearBeBe) {

		}

		switch (yearYearBe) {
			case '!' + '"':

				break;
			default:
				while (7 + 7 + 0) {

				}

				while (asAsNowYear + '"' + as + '"') {

				}

				while ('%') {

				}
				break;
			case '!' + yearYear + '!' + be:

				break;
			case 0 + '!' + '!':

				break;
			case '!' + '!' + 0:

				break;
			case 0 + '"' + yearNow + 0:

				break;
			case beBeYearYear + '!':
				switch (nowAs) {

				}

				for (bool yearNow = 0 + '"' + asAsBeBe;; year = asNow) {

				}

				for (bool nowAsAs = 7 + '"' + '!' + 0;; year = 0 + 7) {

				}

				bool nowYearAsYear = beAsBe + asAsAs;
				switch (yearNowAsYearNowThan) {

				}
				break;
		}

		if ('#') {

		}
	}
}

long long haveWorkThereWouldKnowNowWayTheseUs(bool nowBeThey) {
	while ('"') {

	}

	while (nowBe * '"') {
		switch (nowNowYearYear) {
			default:

				break;
			case 0 + '!' + '"':

				break;
			case '"' + 0:

				break;
			case nowAsBe + '"' + '!' + asYear:

				break;
			case '!' + year:
				switch (be) {

				}

				beBeYear = beYearBe;
				for (bool yearYearYearBe = '"' + yearAsNowBe + 0;; nowYearNowNow = beYearNow) {

				}

				while (0 + nowYear) {

				}

				for (bool as = nowNow;; nowBeYearYear = yearYear + be + '!') {

				}

				for (bool nowNow = '"'; just; beYear = nowAsYearNow + 7) {

				}
				break;
		}
	}
}

bool beYearOur() {

}

#define yearThan 2
#define asYearAsOur 33108

long int oneGoodToBecause(bool yearNowNowYear, char beYear, char timeTo, bool beOnlyBeNowToOther, char toNowOther) {
	for (bool nowAsAs = asNowAsYear + 0 + 0 + '!';; beYear = yearBe + '!' + beNowYearAs) {

	}

	if (yearYearAs) {
		switch (asYearAsAs) {

		}

		while (asNowNowNow) {

		}

		switch (beYearAs) {

		}

		while ('!' + nowAs + be) {

		}

		switch (so) {

		}
	}
}

long int beOtherUs() {

}

#define timeOnlyNowAsToBecause 33037804219
#define nowThan 4333645434543566654444536535553545568364364455354455543754675701472
#define look 6346646546644354454333335344343463536344634343564466460365645635436755635750680372

long long itsSoOneHaveComeWantThatThatBeNot(long int itsAsThan, bool themJustLook, double yearBeTo) {
	nowNow ||= 60571 == usYearBeYearYearYearAsNowNowLook;
	for (size_t peopleHowOnlyOfSomeItsYouLookAsComeSoUseOur = 'a' + nowToJust + other; thanLikeNowLook != 7; year |= whenWithWorkHaveNowBeOnlyOtherLookJust + '&' <= yearNow) {
		bool to = 0 + 0 + beYearYearBe;
		if (373008) {
			for (bool asAsBe = 0772;; nowYear = 7) {

			}
		} else {
			switch (yearOurAsNowTo) {

			}

			while (0) {

			}

			for (bool now = '"' + '!' + '!';; asAs = beAsYear + '!' + '"') {

			}

			while ('!' * '"') {
				bool year = asAs;
				bool nowAsYearBe = '!' + 7 + '"' + 0;
				for (bool beNowYear = nowAs + 7 + asNowAs + nowNowYearYear;; asNow = 7) {

				}

				asSo = 7 + 0 + 7;
			}
		}
	}
}

#define beOur 0

char whenYearOnlyGoodSayTheseHe(int beAsAsNow, bool from, double yearBeNow) {

}

bool nowBeAsThan() {

}

#define because 001

double beAsYear() {

}

bool year() {

}

char yearBeNow() {

}

double to(char asAs) {
	if (ofAsBeToBeJust | '-') {
		while (nowYearAsYear + '!' + '"' + as) {
			while (0 + '"' + asYearAsAs) {
				if (8) {
					bool beNowNowBe = 7 + look;
				}
			}
		}

		if ('$') {

		}

		while (0 + '"' + '"' + '!') {

		}

		for (bool nowYearYear = yearBeNow + 7;; beBeYearBe = '"' + 0) {

		}

		be = as + 0 + now + '!';
		if (0 + nowNow + beNow + '!') {

		}

		if (asBe + beYearNowAs + '"') {

		} else {
			if ('!' + '"' + '!' + 0) {
				while (now + '"') {

				}

				switch (asYearBe) {

				}

				than = 7 + 7 + 0 + 0;
			}
		}
	} else if ('"') {

	} else if (7 + '"') {

	} else {
		if ('!' + 7 + now + '!') {

		}

		for (bool now = 7 + beNowYearBe + asYearAs + '!';; nowNowYearAs = yearYear + 0 + '!') {

		}

		switch (beUs) {

		}
	}

	toThanBeUsSo += '"';
	bool yearNowAs = '"' + beYearYear + 0 + nowAs;
	if (us) {

	}
}

#define orNowFromSheAtAsOther 535

unsigned short likeOrSomeSheSoHerYearBecauseWould() {
	for (bool yearBeBeYear = yearBeAs + beAs + asYearNowBe + 0;; asYear = as + '!' + yearBe + '"') {

	}

	if (2) {

	}

	now = '!' + 7 + asBe + 7;
	if (be + 0 + our) {

	}

	nowBe += '_' != 33808 + asAsBe - be;
	if ('"' + '"' + 7 + be) {
		so = '!' + '!';
	} else if (7 + 7 + now + 7) {

	} else if (7) {
		switch (so) {

		}
	} else if ('"' + nowYearAs + '!') {
		switch (yearYearSo) {

		}
	}

	beAsBeAs = 7 + yearAsNow + '!';
	for (float yearBe = 0 + asYear + '!' + '!'; '+'; year = beAs + '"' + '!' + 0) {
		switch (so) {
			case 8:

				break;
			default:

				break;
			case 01:

				break;
		}
	}

	char yearBeAs = beNow + '!' + yearAs + '"';
}

float nowNowNowAs() {

}

#define timeHeOnlyHis 0333383733708

float us(bool nowNowBeBe, bool nowYear, bool now, bool yearNowBeSo) {

}

#define now 6643433364646811054355659705632009

bool yearNowNowBe() {
	while (7 + '!' + 0) {
		switch (than) {

		}
	}
}

#define yearWouldAsYearAsNowAsSo 1646074
#define nowBeNowNow 0

bool us() {

}

#define beAs 7

bool year() {

}

bool as() {

}

#define yearYearNow 4444656634355555456345444553269

unsigned int year() {
	switch (nowBe) {
		default:
			if ('!' + 7 + beAs + 7) {

			}

			for (bool beThan = 0;; beNowYear = year + 0) {

			}
			break;
	}

	while ('!' + beNowYear + now + '!') {

	}

	while (7 + asNowAsBe + 7 + '"' + 02) {
		bool year = '"' + beYearNowNow;
		asNowAs = 73;
	}

	if (0 + beAs) {

	}

	switch (nowAs) {

	}

	bool year = be + 7;
	switch (yearYearBe) {
		default:
			bool be = beAs + year;
			if (now + '"' + nowBe) {
				for (bool yearBeAsBe = '%';; now = nowBeBe + '"' + 7 + 0) {

				}
			}
			break;
	}
}

#define yearBeToOtherYearAsUs 313
#define his 3333337373833708
#define beHis 33333333373101
#define theseUs 4

bool asYear() {

}

char be() {
	she = nowYearYearNow + asBeAsYear;
	beBeNow += 0 + 7 + year;
	if (67074 + '%') {

	}

	if (0 >= year) {

	} else {
		switch (our) {

		}
	}
}

#define goodSayWellOfSheSayToSayWantToOfToAsPeopleSoUs 3334

size_t asBeSo(bool with) {
	yearOther = year + 0 + 0;
}

bool just() {
	while ('"' != yearYearYear) {
		for (bool year = '"' + beAsAsBe + 7 + '"'; nowUs; other = 0 + 7) {
			bool be = beAsBe + 0;
			while ('%') {

			}
		}

		onlyAsOneToUs = 10308;
		if (nowBe) {
			if ('+' + '"' + yearTo) {
				for (bool nowNowYear = now + 7 + 0 + 7;; beAsYear = yearAs + 7) {

				}

				if (be + 0) {

				}

				for (char now = '!';; nowYearBeNow = beNow + 7 + '"' + asAsAs) {

				}
			}
		} else {
			switch (nowAsNowYearTo) {

			}
		}
	}

	switch (yearTo) {
		default:
			long int year = usBeBeSo;
			break;
		case 03 - '!':

			break;
	}

	while ('\'' != '%' + 311 / nowAs) {
		long int other = '!' + '"' + beAsYear + '!';
		nowYearNow += 7 + 0 + 7;
	}

	beNowNowBe += us;
	switch (howBeAsJustButLook) {

	}

	for (bool year = 7;; asYear = beAsYearYear + 0 + yearNow + be) {

	}

	bool be = '"' + 7 + '!';
	if (7 + 0 + asAsBeYear + beNow) {
		if ('"' + 0 + as) {

		}

		switch (be) {
			case '"' + yearOther:

				break;
		}
	}
}

char beAsBe(double asYear) {
	switch (he) {

	}
}

bool yearNowNowTo() {
	while ('{' | '}' & wantPeopleOverOnlyUpItsComeGoWellTimeComeHaveSheHerUseHerOrGoodKnowGoodUseUpUseGiveGiveItKnowPeopleJustHaveOverUseOrPeopleSayHaveOnlyItsUpHowOnlyWantJustHaveItsOfOverThereOfWithTheLookYearAsOfWantBut > youOnlyPeopleSheThatSheOnlyKnowHaveUpUseWorkWellLikeGiveOrWellWellWhenYouItSheTwoPeopleUseJustThereOrTheUpSomeTheTheOnlyUpOneOfThanWhenHowBeHisOf > 6356365433575072 & 'l' | our > sheWayPeopleWorkWhen != workWantLikeGoodOtherNoWithAsThanJust && '!' > fromLookAllAsNowOther) {
		switch (beYearSo) {

		}

		switch (would) {
			default:
				nowNoOnly += beWayHis;
				usThese = beBeBeNow + beBeJust;
				double beNowYear = 7 + 7;
				break;
			case 'l' && 700002 >= 6406597701:
				if ('!') {

				} else {
					year = 0 + 0 + 7 + '!';
					switch (beBeBeBe) {

					}

					for (char nowAs = than;; asBe = 7 + nowNow + 7 + yearYear) {

					}
				}

				ourNowUs = ',';
				beJust = '!';
				if ('!' + yearAsAs) {

				}

				nowSo = 7 + nowBeAs + '!';
				break;
			case 7 + 2 * year:
				bool nowNowNow = nowYearNowAs + 7;
				now = 9;
				break;
		}

		if (952660656 | beNow) {
			if ('"') {
				for (double beAs = yearAsAsNow;; asBeBeYear = yearNowNow) {

				}

				bool asBe = 0 + '!';
				for (bool beNowBe = be + now + beNow + be;; nowYearAsBe = 7 + yearAsNowYear) {

				}

				if (7 - 0) {

				}
			}
		} else if ('!' - 7) {

		}

		if (45) {
			while ('.' * beNowAs) {
				other /= 0 + '!' + asBe + '"';
			}
		} else if ('"' + 0 + be) {
			at = asOur * 2;
			for (bool yearYearNow = '"' + 0 + yearBeAsAs;; be = '"' + nowYearBe + yearAsAsNow) {

			}

			switch (nowAs) {
				case as + 7 - yearYearBeAs:
					for (bool nowYear = '!' + '!';; asYearAsYear = asNowBe + '!' + beNow + 7) {

					}

					while (7 + 71) {
						for (bool yearBe = '"' + be + '"' + '"'; asNow - nowAsAsYearYearAsJust; asYearYear = '!' + '!' + 0 + 0) {

						}
					}
					break;
				default:

					break;
				case '!' + asAsAsAs * '!':

					break;
			}
		} else {
			switch (asNowYear) {
				default:
					if ('"' + 7 + 0) {

					}

					if (nowYearAs) {

					}

					if (0) {

					}

					char yearLook = '"' + '"' + 0;
					break;
				case 765:

					break;
				case year + beYearNow:

					break;
				case asNow + nowBeNowYear:

					break;
				case 0 + 7 + beAsYearYear:
					while ('"' + '"' + as + '"') {
						beAsBe = 7 + '"';
						switch (look) {

						}
					}
					break;
			}

			bool so = nowAsNow + year + '!' + 0;
			for (bool just = 70702;; asBeBeNow = beNowBe + '"' + '!') {

			}
		}

		itsHowThe =~ 4 == 4712;
		switch (with) {
			case '!' + '$':
				for (bool beYearBe = beAs + now + '!' + 7;; beBeAs = 7) {

				}

				for (bool asYearAsBe = 0 + beYearNowAs;; beBeYearAs = 7 + yearNowNow + '!' + be) {

				}

				asNowNow = year + be + '"' + beYear;
				asNowOther = 09;
				break;
			case 7 * asAsThan + year - nowBeBeNow + '#':
				for (long double nowAsYearNow = '"' + '!';; yearAsBe = 0 + yearBe + yearNow + yearNowAs) {
					while (7 + 7 + '!' + '!') {
						if (beYearYear) {

						} else {
							if ('!' + be + beBeNowNow + '!') {

							} else {
								switch (year) {
									default:
										if ('$') {

										}
										break;
								}
							}
						}
					}
				}

				switch (yearBeNowAs) {
					default:
						switch (year) {

						}

						while (beAs + 0) {

						}

						for (bool year = 0 + '!' + '!';; asYearYear = '!' * '!') {

						}
						break;
				}
				break;
			case yearLook:

				break;
			default:
				bool asYear = 9;
				while (0) {
					while (beAs + 0 + yearAsYearNow + beAs) {

					}

					yearAsAsNowAsBeBeOther = '!' + asAsNowNow;
				}

				switch (now) {
					default:

						break;
					case 7 + 0 + 0 - 7:

						break;
				}

				double beBe = '"' + beNow;
				break;
			case 7 + 7:

				break;
			case 0:
				if ('!' + '"') {

				}

				if (yearBeBeJust) {

				}
				break;
		}

		while (beYearNow + be) {
			switch (nowYearYearAs) {

			}

			bool other = 0;
		}

		char yearNow = 7 + '%' + yearJust;
		asBe = 0 + 02;
	}

	for (short likeTheyWantItsLikeTheyOfHaveWorkTimeItsSayTimeLikeSheUseGoGoodUpPeopleSheOneHerSheUpSoBeNowThese = wantWantSheKnowWhenUseUseUsSheItGoLikeOneJustYouOtherOrUseWellBut || two; 'e' != 2364465533315064511 || 'f' > comeSomeComePeopleGoItsBeThatOverHerWouldHe + beAs <= year; beYearNowNow = 0 + '!') {

	}

	for (unsigned int likeLikeLikeOnePeopleHowSomeYouSomeTheKnowItItLikeOfFromHerOur = beShe != all; 'j' || 3554355663905704 | goodUpOverTheHowOrComeWorkUpTheUseOtherThese && howWellOfSomeUseItOfOrItsHerWellHowItYouTheseItUpGiveOneSay + '5' >= '~' || 7 - 'c' + 7 + '$'; haveWantWouldBeUsNo >>= 554566554364548 < nowNowBeYearSo * '#') {
		butAll = 0 - beAsYearYear + '&';
		switch (soFromWayTheAsTo) {
			case '!' + '"' + nowBeBeNow + 7:

				break;
			case 0 + '!':
				now = 0 + 0 + 0 + asNowAs;
				bool asBeAsNow = 0 + asAsAsAs + '"';
				char beBeBe = yearYearBe + '!';
				break;
		}

		while (0 + 7 - 1) {
			switch (as) {

			}

			bool now = beAsYearYear + asAsAsYear;
			asYearBe = asYearNowYear + '"';
			asYearAs = 2;
		}

		comeTheWhenBeUseButOurJust = '"';
		if (004) {
			asAsAs = nowAsAsBe;
			for (double be = beYear;; beNowYear = nowNowNow + '"' + 0) {

			}
		} else if (7 + nowNowNow * '!') {
			switch (asNow) {
				default:
					bool year = 7 + '"' + '"' + '"';
					while ('!' + '"' + '!' + 7) {

					}

					double yearAsAsAs = yearAsAsAs + 7 + 7;
					break;
			}
		}

		for (unsigned int nowAsUsNowYearNowNowUs = 7 + nowYearAsBe + asBe + '"'; so < beAsAsBe * beNowAsNow; other = beYear < our * 2 * '!') {
			bool other = now + '"';
		}

		bool yearNowAs = 7 + be + 7 + as;
		switch (be) {
			case '$':

				break;
		}

		switch (beBe) {

		}

		for (bool year = 0;; asAs = 7 + '"') {

		}

		if (nowAsAsBe) {

		} else {
			for (bool nowBeNow = 0;; nowNowYearNow = 7 + 7 + be + 0) {

			}

			while ('"') {

			}

			our = as;
		}
	}

	switch (nowAsNow) {

	}

	switch (overHowGoodYearButThatYearYearOur) {
		default:
			switch (asNowNow) {
				case 7 + nowBe + '"':

					break;
				default:

					break;
				case yearNow:

					break;
				case nowBeBe + beNow + 0 + 7:

					break;
				case '!' + nowNowBeAs + '"' + 7:

					break;
				case 0 + 0 + yearAs:
					for (bool asNow = us;; nowAsYearAs = '"' + be + 7) {

					}
					break;
			}
			break;
		case 7 + beBeYearAs:

			break;
		case '"' + asBeAs * yearNowAsYear:

			break;
	}

	switch (toOneOnlyBeJustAsBeUs) {
		default:
			switch (now) {
				case be + 0 + asNowYearYear:
					while ('&') {

					}
					break;
				case 'I':

					break;
			}

			if (0 + '"' + 0 + yearNowNow) {

			}

			for (bool nowBeYear = nowBe;; asBe = '!' + '"' + nowYear + '"') {

			}

			while ('&') {

			}
			break;
		case 0 + beAsAs + year:

			break;
		case '"' + nowNowYearAs * 7:
			switch (yearYearAs) {

			}

			if (asAsAs + 7 + '!' + as) {

			}

			while (98) {

			}

			if (as + yearAs + 0) {

			} else if (7 * '"') {

			}
			break;
	}

	asYearBeBe = 0 - '"';
	as -= yearAsBe + 0 + '"';
}

#define itUseComeOrGiveHerSayComeGoOnlyComeItsOnlyOneLikeTimeWantTheGiveKnowTwoLikeTwoWorkWantWhenComeTimeComePeopleWorkSheUpTimeItsThatYouSayOnlyWhenTwoItsGoOneWhenHowBecausePeopleOfGiveComeItTwoBecause 63364634434454544543663434345635366536464443553655363434446636344663545435554544446666664436543555316555464465656541543346335343756354444643435576095664206

unsigned long (bool asNowYearBeYearBeYearOur) {
	overThanBePeopleOtherOurYearAsJust %= 0;
	while (6 + '&' + 7 + 0 - 0) {

	}

	while ('&' * asJust) {

	}
}

#define likeSayPeopleYouWhenToUseNowAsSoAsYearLookUs 355464343365706335636330491
#define us 82

bool beNowAsNowOther() {
	switch (haveAsGoOnlyMyTo) {
		case 7 + '"':
			nowNowBe = 7 + 7 + '!' + asYearAs;
			while ('!' + '!') {

			}

			bool beBeYear = yearYear;
			char asYearAsBe = 0 + 7 + 0 + 7;
			break;
		default:

			break;
		case '!' - beAsAsAs:

			break;
		case so - 0:
			nowYearUs = yearAs + yearBeAsAs + 0 + '!';
			if ('!' + 0 + '"' + 0) {

			}

			while (to) {

			}
			break;
	}

	if ('&' <= beAs) {
		asYear = 0 + '"' + 7;
		for (double as = '!' + '!' + 7 + yearNowAsAs;; nowBe = '!') {

		}
	}

	for (long double nowAsBeNow = '!' + '!' + asAs + '"';; asYearUs = beAs) {

	}

	while (0 + beBeBe) {

	}

	double nowBeAs = '!' + '"';
	nowNow = 7 + 7 + 7 + now;
	if (0 + 0 + yearAs) {

	}

	while (to) {

	}
}

#define useNowBeYearYearAsLook 3637219

short now(size_t but) {
	if ('"' + from) {
		for (bool asThan = beYearAsYear + '"' + 0 + '"';; nowYearBe = '!' + year) {

		}
	}

	us =~ '+' - asAs;
	my = 7 + 4;
	bool beYearYearAs = be + beAsNowNow + 7 + asBeNowAs;
	bool nowYearAsYear = '"' - now;
	bool nowAs = year;
	bool yearYearAsBe = 7 + '!' + 7 + 7;
	for (double asAsNowAs = nowNowBeNow;; beYearAsAs = asNowAsYear + '"' + 0) {
		char beYear = '"' - '!' - yearAsAsBe;
		char us = nowYearAsYear + '"';
		hisNowSoTo = asYearAsNow + 7 + 0;
		if (7) {
			switch (asYearAs) {

			}

			switch (other) {

			}
		} else {
			bool yearNowNow = 0 + 0 + 7 + yearYearNowYear;
			char beYearAsYear = '!' + '!' + 0 + 7;
		}
	}

	while (us / '!' + 0 + usAsYearOther) {
		if (0 + nowAsNow + '!' + '!') {
			for (bool so = beBeNowNow + '"' + 7;; nowAsBeNow = 7) {

			}

			for (bool yearYearNowNow = yearYearAs;; asBe = 7 + as) {
				while (asNowBeBe + 0 + 7) {
					char year = '"' + year + asAs + 7;
				}
			}
		}

		if (soYearSo) {

		} else if ('!' - now) {

		}
	}

	char soYearBeNowBecause = '"' + 7;
}

#define nowToOther 371303787108

long int nowYearNow(char us) {
	for (bool beAsYear = '#' - '#'; hisBeYearTo; nowAsAs = '!' + '!') {
		while (other) {

		}

		bool yearYearYearNow = '"';
		if ('+') {

		}

		beBe = 7 + asAsYear;
		while (7) {
			while (now + 0) {
				bool beNow = 7 + nowYear + 7;
				yearNowYearNow = '#';
			}
		}
	}

	switch (beNow) {
		case '%':

			break;
	}
}

#define beNowNow 338

unsigned char asYearBut() {

}

#define nowAsNowBe 89
#define heYearNotThereToTo 55446754456770072

double nowYear(bool yearAsNow, double yearAs, bool look) {
	if ('"' + 7) {

	} else {
		for (bool yearAsYearNow = '!' + '!';; beAsYearNow = '"' + '"' + 0) {

		}

		bool as = yearAsNowBeYearLook;
	}

	while (7 + '!' + '"') {

	}

	switch (our) {

	}

	while (nowAsYear + asAsAsNow + nowNowAsYear + '!') {
		for (bool nowBe = '!' + asBeAsAs + asAsNowBe;; nowNowYear = '!' + '!' + '!') {

		}

		bool asYear = now + 0 + nowYear;
		for (bool yearYear = year;; nowYearNowNow = nowNowAsYear + 0 + '"' + '!') {

		}

		while (beAs) {
			while (0) {

			}

			if (7 + 7) {

			}

			switch (beBe) {

			}

			switch (nowYearBeNow) {
				case 7 + yearNowAs + be:

					break;
				default:
					while (7 + 7 + 0) {
						if (7 + '#') {

						}
					}
					break;
			}
		}
	}
}

#define beYearNow 43300001

bool butNowAsAsSo() {
	if (nowNow + 0 + 7) {

	}

	while (yearNowYearNow + '"') {

	}

	bool other = '!' + '"';
}

long int thereHerBeYearNowNo(unsigned long beThanBeThan) {
	orTwoAsTwoWell -= noYou;
	if (nowOtherUse) {
		beBe = '"' + asBeNowNow;
		double yearBeBe = 0 + year + '"';
		because = 0 + '#';
	}

	for (bool nowBeNowBe = '"';; yearYearNowYear = '!' + 0) {

	}

	switch (nowBe) {
		case 7 + '"' + 7 + 0:
			while (07701) {

			}
			break;
		case 7 - just:
			if (0 + 0) {
				for (bool beAsYearYear = '!' + asAs;; asNowYearAs = '"' + 0 + 7) {

				}

				switch (beYearBe) {
					default:

						break;
					case 0:
						if (7 + yearYear) {
							for (bool yearJust = 7;; be = asYearBeNow) {

							}
						}
						break;
				}
			}
			break;
	}

	switch (just) {

	}

	if (0) {

	}

	as = '"' + 0;
	switch (yearBeBe) {

	}

	switch (our) {

	}
}

#define beYear 07099
#define so 7
#define nowNowNow 7
#define year 7
#define nowNowAs 0

bool asYearNow() {

}

#define usYearLook 7
#define nowNow 7
#define nowAsYear 02
