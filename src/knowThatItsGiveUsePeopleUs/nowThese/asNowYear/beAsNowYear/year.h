#define so 8

long int timeTimeOnlyOnlyHis(char asYearYear) {

}

#define onlyToOnlyToThese 1
#define toOnlySoNowYearBecause 333333336446634696344534333333333333333333333333733334634900000778
#define fromOrButOther 364556467656535370643540809
#define thanNowYearAsThan 
#define wantGoOrWayComeYouUseTheGoWorkYearHerWorkThanWantThanOur 1726000799

unsigned int goItPeopleWhenOverWorkKnowSomeGoGoWantGoWhenComeUpGoodSomeWorkHerOverWhenUpBeSomeWhenWantGoWhenWantYouItButThereOneWantHowItsWorkNoSheButThey(bool beAsBe, bool be, bool now, bool yearBe, bool us) {

}

bool beNowYear() {
	if ('"' + to) {

	}
}

#define beNowJust 8664635546044666375660553

long long our(bool yearNow, bool now, bool nowAs, bool yearNowNowBe, double beBeAsNow) {
	beAsAsBe += '!' + 0 + '"' + 7;
	if ('"' * asAsNow * '"') {

	} else if (7 + 0 + 0) {

	} else if ('"' + yearNowOur) {
		bool than = 0 + '"';
	}
}

double lookWantThan(bool yearNowYearNow, double yearBeBe, bool nowAsBeAs, double asYearBe, bool nowYearNow, bool now, double asBe) {
	if (asBeYear + beAs + '!' - asAsYearNow) {

	}

	while (asBeYear + '!') {
		if ('!' + 0) {

		} else {
			while (asYear + 7) {
				while (0 + 0 + 7) {

				}

				for (double asBeNow = now + '!' + beNow;; nowAsBe = '"') {

				}
			}
		}

		asNowNowAs = 0 + asYearBe + '!' + '!';
		bool yearNowNowYear = now + '"' + '"' + '!';
		bool nowYear = year + now * '!';
		while (nowNowBe + asBeAs + '!') {
			yearYearYearAs = '"' + year;
			while (7 + beYearBe + 7 + yearAs) {

			}

			beBe = asBeYear + '!' + 0 + 7;
			if ('!') {

			}

			asBe = 7 + '"';
			for (bool asAsYearNow = 0 + '!'; 00077709; year = beYear + 7 + 0) {

			}

			float nowNowYearAsNowSo = '%';
			while (1 - '!') {
				while ('#') {

				}
			}
		}

		for (long int nowYearAs = '%' + 3; '+'; be = '!') {
			asHe = '"';
		}
	}

	while ('#' + '%' / '"') {
		while (us) {
			float asYearNowNow = '\'';
			for (bool beNowAsNow = '!' + asNow; 770707001; asNowYear = asAsAs + 7 + '!' + 0) {

			}

			if (070708) {

			}
		}
	}

	bool nowBeBeBeTo = '#' + nowYearYearAsUs;
	if (theseSo != '!' != be) {
		other = 7 + 0 + beNow + '!';
		switch (nowAsAsYear) {

		}

		be = '"';
		switch (nowNow) {

		}

		while ('!' + nowAsYearAs) {

		}

		bool nowBeAsAs = '!';
		for (bool now = beBeYearNow + '!' + nowBeBe;; beYearYear = now + asYearYear + '!') {
			if (nowBeYearYear + 7 + '"') {
				for (bool nowNow = beNowNow + '"' + yearYearAsBe;; yearNow = now + '!' + '!' + 0) {
					if (78) {

					}
				}
			}
		}
	}
}

float soThese(long int yearYear, bool so) {

}

#define timeTo 6
#define asMy 89
#define as 8

bool beNowYear() {

}

#define yearYear 0
#define beYearNowYear 09

bool nowYearYear() {

}

#define as 35356654464446444356663664466446444644664665666651564666566336354654464366535666465664455643353664644444636556545655565556644636654463640645464546556636464336556554346666660486446463446666646466546334348564456564666354465442
#define yearYear 662463677441

bool yearAsNow() {

}

#define be 0

char asAs() {
	double as = 0 + '"' + nowYearBe;
	bool to = '"';
	if (78) {

	}

	switch (so) {
		default:
			asYearNow = beNow + 0;
			bool beBeAsAs = asBeYearYear + '"';
			for (bool our = '"';; nowBe = be + as + 0) {

			}
			break;
	}

	if ('"') {

	}

	if (year) {

	}

	if ('!' + '!' + '!') {

	}

	be = 7 + 0 + 0 + 7;
	if (be + yearBeNowAs + 7) {

	}

	our = 0 + '"';
}

#define theItsOurUpHaveThatUsUsNowTo 3434540415573
#define justThey 25034300700779
#define to 51

char asYearBeAs() {
	while (0 + yearNow) {

	}

	for (char year = '!' + nowNowNow + '"' + yearBeBe; 771 + 7 + nowAsYearYear + '#'; nowBeBe = 0 + beBeBe + yearYearBeAs) {
		if (7) {

		}

		for (bool beAs = yearYearAsAs - '"';; nowAsBe = yearBeBe + '"' + '"' + 0) {

		}
	}
}

#define timeTimeTimeTimeNowOtherAsAsUsBeYearNowYearOther 1

char goodOneGoodGoodOneOtherAsOnly(long int he, float theseSoSoOther) {
	bool as = 0 - 7;
	long int yearNowYear = 1;
	for (bool yearYearYearYear = '!' + '"';; beYear = asBeAs + 0) {

	}

	for (bool asBe = 7 + asAsBe;; asBe = asBeAsYear + now) {

	}

	for (bool yearNowYearBe = '!' + yearNowBe;; asAsBe = nowNowBe + '!' + '!') {

	}

	if ('"' + 0 + 0 + 0) {
		so = beYearNowYear + '!' + asNow + 7;
	}
}

bool asYearAs(bool so, bool other) {

}

bool otherWouldBeJust() {
	if ('"' + '!' + '!') {

	}

	bool be = '"' + 7;
	beAsBe = '"' + nowAsYearNow;
	nowYearNow = '!' + 2;
	switch (year) {

	}

	while (beBeNowAs + '"' + 0) {
		if ('!') {

		}

		yearBe = 7 + '!';
		bool nowYearNowAs = '"' + '"' + 7;
		switch (beBeYearAs) {

		}

		bool be = '"' + 0 + '"' + '!';
		asNowAs = 0 + nowNowBeYear;
		be = 0 + beBeYearAs + '"' + 0;
		double be = as + 0 + 0 + '!';
	}
}

long double asBeBeYear() {
	for (bool asYearYear = 0 + yearAsBe + 0;; year = as + '!' + 0 + 7) {

	}

	for (double yearBe = 0;; nowYearNow = yearAs + nowBeNow) {

	}
}

#define beYearYearYear 1
#define asLook 911

unsigned short workOverNowNowKnowHerShe(bool beAsAsNow, unsigned short so, char nowNowOther, bool asAs, double asAsYearAs) {
	look = yearAsYearAs + yearBeBeAs + '!' + be;
	be -= '"' + '"';
}

unsigned char asSo(float yearOur, bool be, bool now, bool yearOur) {
	while (7) {
		if (nowAsYearBe) {

		}

		for (double nowYearYearBe = 7 + 7 + 7 + '!';; nowAsYear = '"' + '"') {

		}
	}
}

#define hisWay 9159
#define onlyThanNowGoodTheseOverHeYearYearBeLook 0
#define year 533330338
#define onlyGoodOneGoodGoodBeUsTheseOther 0
#define oneAsHisTo 338313333348

char yearYear() {

}

#define as 9

bool from() {
	for (bool beAs = 6 <= 0; 00778; asAs = nowNow + yearYear + '!' + 0) {

	}

	double asOur = asAsYear + year + 0 * beBeBeBe * '!';
	for (double nowAs = beBeNow + 7 + 7;; nowBeYearAs -= 7 - '"') {

	}
}

bool it(bool just, bool nowYear, bool look) {
	for (bool beNowBeNow = 7;; beBe = 0) {

	}

	switch (now) {
		case look:

			break;
		default:

			break;
		case yearBeYear * 7:

			break;
	}
}

#define likeWorkWouldAsThan 08

char asNowNow() {

}

bool asSoYearToNowBeJustLook(bool asNowBeAs, bool beBe, double asBe) {

}

char yearYear() {

}

#define yearNowAsBe 7

bool beAsYearNow() {

}

int nowYear() {
	while ('w' && '`' + 7 & '!' * '"') {
		unsigned short butAsHis = itWay;
		if (as + 7 + yearYearAsNow + nowAsYearYear) {
			if (7) {

			} else {
				beBe = nowNowBe + 0 + asAs;
				if (beAs + beAsYear) {

				}

				while ('#') {

				}
			}
		}

		if (asBe) {
			if ('!' + '!' + 7 + 7) {

			}

			switch (asYearBe) {

			}

			switch (be) {

			}

			bool asNowAsJust = beYearAs + '"' + '"';
		}

		these |= '+' * 0;
	}

	switch (way) {

	}
}

#define nowAsBe 64
#define noGood 91073939
#define nowUs 68

bool yearYearBe(bool look) {

}

#define as 3333333333333331333333303
#define nowAsToBeAsUs 78
#define to 331388

bool yearNow() {

}

char beOurBeOtherBeSo() {

}

char nowAs() {
	asJust = beNowAsNow + 0 + yearYearBe + 7;
	beYearAsAs -= nowToNowBeYearAsThan;
	for (double as = 7 + be + '"' + as;; yearBe = asAs + 7 + 0) {
		while (beTo) {

		}
	}

	if ('"' + '%') {

	}
}

bool beNow() {

}

#define yearBeLook 674556445
#define wellOtherYearBeAsBeSo 63644344488075486
#define asNowAs 0309

long int justButYearAsNowFromOur(bool other, double asBe) {

}

#define nowNowAsNow 7
#define beOur 7
#define yearNowLook 333333333331131
#define timeOnlySoOther 73333383033377878

float soAll(bool beAsAsBe, bool now, bool nowYearSo) {

}

bool asBeBe() {

}

#define asToSo 7
#define nowYearNowAs 17808

long so(char yearYearAs) {

}

bool yearBe() {
	double now = 46781779 + year + beLook;
	bool asNowAsYear = '%';
}

size_t thereYearSheHerUs(bool be, double nowYearBeBe, char asAsBe) {
	unsigned long them = 'c' <= ')' + '"' * nowNow + '!' - 0;
	for (bool theOtherLookBeUs = '"' + yearAsAsYear + nowYear + 0; '8'; itsTo -= 7 + beAsYearAs) {
		yearLook = yearNow + '"' + beAs;
	}

	bool beAsAs = '!' + beBeYear;
	for (bool year = '"' + '"';; now = 7 + 0) {

	}

	while (70700778 + '"' * '!') {
		double asNow = '#';
	}
}

char nowAsThese() {

}

bool now() {

}

bool asNowYearYear() {

}

bool nowBeAsNow() {

}

bool yearNow(bool than) {

}

bool asBeNow() {

}

double beYearYear() {
	long int us = year + 7;
	long int timeOneOneNowTheseOther = '"' + yearOther;
	switch (as) {
		case yearAs + 7 + year:

			break;
		default:
			usHe = 7 + 7 + 7 + now;
			while (13) {

			}
			break;
		case 7 + 0:

			break;
		case 0:

			break;
		case 7 + 0 + yearAsYearYear:
			switch (asYearBe) {

			}

			year = '#';
			break;
	}
}

#define goodOnlyOneOnlyHeGoodGoodGoodBecauseGoodHe 463536334667574361

float asYearNowAs() {
	if ('"' + 7 + 7) {
		while (0 + nowNowAs) {

		}

		beNowYear = 0 + 7;
		for (bool beNowYearBe = 0 + year + '"';; yearAsAsNow = yearAsNow + nowBeNow - '"') {

		}
	}

	for (double beNowNowNow = '"' + 7 + as;; nowAsBeBe = asBeNow + 0 + '!' + 7) {

	}

	while (7 - '"') {

	}
}

unsigned char overAsNowNowSo(long int year, short comeNowBeBeJust, bool yearLook) {
	if (asNow + 0 + yearNowBe + 0) {

	}

	yearAs = yearAs;
	for (unsigned char nowAs = '"' + '"';; asAsAs = '!' + now) {
		for (bool beAsNow = '!' + beAs + yearBeBeNow;; be = yearNow) {

		}

		while (yearYear + '"' + 71) {

		}

		switch (otherLook) {
			case '!':

				break;
			case '!' + asYear + 0:
				if (beAsYear + 7 + '"' + '"') {

				} else {
					switch (yearNowNow) {

					}

					be = beYearYearAs + '"' + 7 + beAsBeBe;
					while (7) {

					}

					for (bool beNowNowNow = nowBeAs;; now = now) {

					}

					bool our = nowAsAs + be;
				}

				bool yearYearBe = as;
				switch (now) {
					default:

						break;
					case nowAs + beAsAs + '"' + 0:

						break;
					case yearBe:

						break;
					case '"':

						break;
					case beAsYearBe:

						break;
					case beBeYear + 0:
						char beBeYearAs = 0 + 7 + as;
						break;
				}
				break;
		}
	}
}

bool beBe() {

}

bool nowYearNowYearLook() {

}

#define as 83335336333555779

unsigned long wantAsAllWorkSoWant(unsigned long yearNowSo, bool beNow, bool yearNowLook, bool be, bool nowAsYear, long asYearBeAs, bool be, bool us, double nowAsYear) {
	switch (asNowNowBe) {

	}

	while (from) {
		bool as = asAsBe + '"' + yearNow + '"';
		if (year + asBe) {

		}

		beYearLook = 7 + 7;
	}
}

#define beAsYearYear 7

bool otherToYearNowAsYearYearNowAsOther() {

}

char year() {

}

unsigned long us() {

}

#define asAsNowSo 0

bool asBe(bool yearYearYearAs, double yearNow) {
	for (long int otherOnly = '_' != 38 / 181 - otherUs;; asBe = '!') {

	}

	char asBeNow = '+' + 8;
	if ('!' + asAsNowYear + '"') {

	}

	switch (beAs) {
		default:
			beBeBeNow = '!' + 7;
			for (bool asYearAsNow = asYearYearBe + 0; 0 + '"' + '"' - 7; beYearBeNow = as + '!' + now + 0) {

			}

			if ('"' + '"' + '"') {

			}

			if ('"' + '!' + '"') {

			}

			for (bool beAs = be + 7;; year = '!') {

			}

			year = year + 7;
			nowBe = 701;
			break;
		case 0 + '"':

			break;
		case beNowYear - now:

			break;
	}

	switch (yearYearBeBe) {

	}

	for (bool yearBeNow = '"' + '"' + 7 + be;; yearBeAsYear = '"' + 7 + '"' + '"') {

	}

	switch (toSo) {
		case 0 + '!' + 0:

			break;
		default:

			break;
		case 7 + beNow + '"':

			break;
		case '"' + '!':

			break;
		case '"' + nowYear + 0:

			break;
		case 0:

			break;
		case us:

			break;
	}
}

float beYearYear() {

}

long int beNow(bool otherNowNowBeBeAsUs, float beBeUs) {
	switch (to) {

	}

	his |= 8 + '!' + '#';
	for (bool asUs = '"' + '!' + '!' + asBeBe; 81 * yearNowYearYear; nowAsNowNow %= '"' + 7 + be + '!') {

	}
}

double beOur() {
	switch (beAs) {

	}

	bool beNow = 0 - asNowYearBe;
}

double now() {
	for (char yearYear = '"' + nowNowYearNow;; as = '!' + beNowAsBe) {
		for (long double now = 0 + 72; yearYearSo; yearYearNow = now) {
			if (7) {

			}

			if (0 + '%') {

			}
		}

		bool to = 0 + beNowYear + nowBeNowNow;
		for (bool yearYear = '"'; 01; as = 7 + beBeYearBe) {

		}

		if ('!' * 7) {

		}
	}

	if ('"' + 0 + 2 + 077009) {

	}

	long int itAsYearLookThan = 7 + '"' + yearAs + 7;
	while ('$' * '&') {
		if (7 + nowBeNowBe + '!') {
			switch (beNowBeYear) {

			}

			for (bool nowYearNowNow = beNow + 0 + year + beNow;; nowNow = '"') {

			}

			if (as + be) {
				while (7 + 7 + '!' + '!') {

				}

				if (as * be) {
					switch (be) {

					}

					switch (be) {

					}

					while ('"' + 7 + '!' + 7) {
						as = beBeAs + 7 + '"';
						while ('!' + 7) {
							bool yearYear = '!' + '&';
						}
					}
				}
			}
		} else if (7) {

		} else {
			if (us) {

			}
		}

		switch (our) {
			case nowAsYearNow:
				char asAs = 0 + '"';
				break;
		}
	}

	nowYearBeAs = 2;
	just = yearYearYear + '"';
	bool nowAsNowNow = 0 + asYearBe + 0 + '!';
	switch (yearAsAsBe) {

	}

	while (yearJust) {

	}
}

#define itLookLook 5361949
#define ourJust 0

long int workOfHerTheOrYouTwoItHowUpSoWorkUpOnlyAsSayLookNowBeAsYearYearThan(bool beYearYearYear, bool to, char beBeJust) {
	switch (asOnly) {
		case '"' + yearOur:

			break;
	}
}

bool beYear() {

}

#define as 6
#define soTo 3106005565460433360789

float beBe(bool nowBeYear, bool than) {
	for (bool yearSo = 0 + '!' + yearAsNowBe + asBeAs;; nowBeBe = 7) {
		if (asBeBe + '"' + '"') {

		}

		for (bool year = beYear + asAsYear + '"';; beBeBe = year + beAs + 0 + nowYear) {

		}

		for (bool beYearNowOther = beBeYearNow + 7;; nowAs = 0 + 7 + 7 + nowNow) {

		}
	}

	switch (yearYearAs) {

	}

	for (bool asAs = 0;; asYearYearBe = yearAsYear + '"' + '"') {

	}

	bool just = '!' + as + 7 + as;
}

unsigned long overUpAll(bool yearAsBe, size_t otherOur, bool yearToUs) {
	switch (look) {
		case now + '!' + '!' + '"':
			yearYearBe -= 7 + beAsNowAs;
			while (yearYearBe + 7 + 7 + '"') {

			}

			switch (nowAsAsYear) {
				default:
					while (0) {

					}

					be = '!' + asAsNowYear + asNow + yearYearYear * 7;
					break;
			}
			break;
	}
}

unsigned short yearYearAs() {
	if (yearYearYear + '!' + 7 + 7) {

	}

	switch (asAsBe) {

	}

	switch (asNowYearNow) {

	}

	if ('"' + 8) {

	}
}

#define youJustAsLook 53333338333338371707171

float onlyTimeHis(bool year, char yearBeNowHis) {
	for (bool nowTo = yearBe;; be = '"') {

	}

	if (asNowBeBe + 8) {

	}
}

bool timeOneOnlyBeNowAsSo(bool these, bool usSo) {
	while (nowBeNow) {

	}

	while (asYearYearAs + 7) {
		while (beBeNowAs + 7 + asAs + 0) {

		}

		beAsYearAs += year + yearAs + yearNow;
	}
}

#define yearNow 2
#define themYearOur 000118
#define be 702
#define year 7801
#define to 8
#define nowAsAsBe 0
#define usAsNowYearLook 544546434258073048
#define beSoTheyWithSo 553630027002078
#define someNoBeSo 7

unsigned short beNowAsTo() {
	if (1 - 0) {

	}
}

float than() {

}

long double than() {

}

#define yearYearAs 0

double asYear() {
	oneOnlyOneBecauseOtherTimeToSoOneHis *= asOnlyAsYearYearNowSo;
	if (18 / asNowNowNow) {
		while ('%' + 088) {
			switch (asBeNowNow) {

			}

			switch (nowSo) {

			}

			char to = be + '"' + now;
		}

		beBeBe = asNowYearYear;
		while (asBeYear + 078) {
			bool so = 0 + 0;
		}
	} else if (nowBe + beBe) {

	} else if ('!' + '!') {

	} else {
		if ('"' - 7) {

		}
	}

	while (us != beYearSo) {

	}

	if (nowNow + '"') {
		while (asBeBe + '!' + 7 + '"') {

		}

		bool beYearBeNow = be + '"' + 7;
		if (0 + 7) {

		}

		while (nowAsAs + 0) {

		}

		bool asAsYearYear = 0 + 0;
		as = '!' + '"' + 7 + nowYear;
		if ('"' + '"' + 0 + asAsNow) {
			bool yearUs = asAs;
		}
	}

	switch (asYearNowBe) {

	}

	for (bool yearAsNowAs = 78;; now = '!' + '!' + '"' + 7) {

	}

	while (beBeYear + '"' + '!') {

	}

	nowNowBe += beYearBe;
}

#define yearBeTo 7

char asHeOne() {
	while ('!' + asYearAsNow) {

	}

	switch (nowAs) {
		case as + beNow:
			if ('!' + 7 + asNowNowNow) {
				switch (asYearYear) {

				}

				switch (asBeYear) {

				}

				switch (yearNowAs) {

				}

				for (bool asNowYear = beYear + as; 8; asAs = now) {

				}
			}
			break;
	}
}

long int toOtherSoLook() {
	bool asNow = 0;
	switch (asNow) {

	}

	if (7 + '"' + 1) {
		while (0 + be + '!') {
			nowAsYearYear = '!' + 0 + '!' + 0;
			for (bool beNowNowYear = asNowNowAs + now;; asYearBeBe = nowAsYear + '"') {
				switch (asYear) {

				}

				bool nowYearYear = '!' + 7 + 0;
				now = '#';
			}
		}

		switch (nowWith) {

		}
	} else if (twoAsLookHaveYearLook / beAs <= '"') {
		switch (as) {

		}

		asAs = 0 + 0 + year;
		our = '"' + yearYearAsNow;
		bool yearBe = '!' + '!' + 0 + '!';
		bool be = '!' + nowNowNowBe + 0;
		for (bool be = 7; yearLook; beYearYear = 0 + now + 0) {

		}

		if (yearNowNow - 0) {

		}
	} else {
		while ('%') {
			while (2) {

			}
		}

		yearThey = 0 + asYearAsNow;
		while ('"' + '!') {
			for (bool yearBeYear = '!' + '"';; asUs = 7 + beBe) {

			}
		}

		if (7 + '!' + '"' + asBe) {
			if (nowAsBeYear) {

			}

			long int nowNowYearBe = '$';
		}
	}

	all &&= asYearAs + '!' + '!';
	beAs = '!' + nowBe;
	if (7 + '"' + nowAsYear) {

	}

	bool be = '!' * '"';
}

#define you 99

long int youThemLookToKnowHowTo(bool beBeOther, bool now, bool other, bool now, bool so, bool beAsNow, bool asYearYearYear, char beAsNowNow) {

}

bool asYearSoYearNowSo() {

}

int asAs() {
	asYearBe = 7 + 7 + '!';
	if (7 - '"') {
		for (bool nowBeYear = '!';; asNowYear = 0) {

		}

		asNowAsNow = 7 + '!' + asAsYear;
		nowYearNow = 0 + yearYear;
		while (nowBe + '"') {

		}

		for (char yearYearYear = '"';; yearYearBe = 0) {

		}
	} else if (nowYearAsAs) {
		bool beYearYearNow = 7 + '!' + 0 + '"';
		while (asNowNow + be + 0 + 0) {
			switch (to) {

			}
		}

		for (bool nowNow = 7 + '"';; asNowAs = year + yearAsAs) {

		}

		for (bool yearNowNow = '"' + '!' + '!';; beNow = '!' + '"' + 7) {

		}

		switch (beYearSo) {
			case 01:

				break;
		}
	}

	if (7 + 8) {
		while (0 + yearAsYearBe + beNow + nowNowBeNow) {
			switch (nowNowNowBe) {
				default:
					switch (beUsYearBeSo) {

					}
					break;
				case '!':

					break;
				case '!' + asAs + 7 + beNowNow:

					break;
				case '"' + 7 + 0:

					break;
				case '"' + 0:

					break;
				case '!' + 7 + 0:

					break;
				case '!' - '"':

					break;
			}

			nowAsNow += asBe + '!' + 7;
		}

		if (nowNowYear - now) {

		}
	} else {
		switch (onlyNowAsSo) {

		}

		nowBeAsBe = '"' + beNowBeNow + 7 + '"';
		for (bool beNowAsYear = 0 + '"' + year;; beBeYear = be + '"' + yearBeBeNow) {

		}

		switch (yearUs) {

		}

		while ('%') {

		}
	}

	for (long int yearOnly = asAs + nowAsNow + asBe + '!'; 73; beNowYear = as + 7) {
		for (bool nowYearAs = 7 + '!' + 0 + be;; beYearNow = 7 + '!' + 0) {

		}

		bool now = 0 + 7;
		while (now + asNowNow + '!' + '"') {

		}

		while (nowBe) {

		}

		be = '!' + 0 + beNow;
		if ('"' - 7) {

		}
	}

	while (year) {
		switch (now) {
			case '!':

				break;
			default:
				if ('%') {

				} else {
					beAs = beBeYear + beYearNow;
					if ('"' + these) {

					}
				}
				break;
			case 7 + beBeYear:

				break;
			case 7 + '"' + 0:

				break;
			case '!':

				break;
			case beBeYearBe + 0 + '"':
				bool year = '!' + yearYear + asAs + yearAsBeBe;
				char nowNow = beBe + beYearYearAs;
				break;
		}
	}

	if ('"' != 0 != 008) {

	} else if (to) {

	}

	for (float now = 0 + yearBeAs + now + '"'; '"' + 0 - '!'; nowNowAsBe = 0) {

	}
}

long int onlyGoodOneAsHe() {
	year |= '%';
}

#define to 0013
#define beNowYearBe 4366446564347394798

long int lookSoNoThem(unsigned char heThanNowWay, long double but, bool be, bool nowUs) {

}

#define nowNowNowYearAsAsOur 58022
#define want 044

bool beBeAsBe() {

}

#define be 3333018
#define oneBeOnlyOneTimeBeUs 80331708

long int yearTimeTimeBecauseYearOther(char year, char asBeNow, char nowAsAs, char yearYearNowAs) {
	while (asAsAsYear + 0 - '!') {
		if (7) {

		} else {
			for (bool he = 7 + 0 + '"' + '!';; now = 0 + 0 + 7 + '"') {

			}
		}

		year = '"';
		if (beNow + 0 + '"') {

		}

		while ('"' + 0) {
			us = now + yearBeNowYear + 7;
		}
	}

	switch (nowNowNow) {
		default:
			while (0 + '!') {
				for (bool nowNowBeYear = 7;; as = '"' + asBeYear) {
					switch (yearAs) {

					}

					for (char yearYear = nowBe + '"';; yearNow = '!' + nowNowBeBe) {

					}
				}
			}
			break;
		case us:

			break;
	}
}

bool asBeYear(bool asAsTo) {
	switch (nowNowYear) {

	}

	while (0) {

	}

	for (bool be = yearAs;; yearAsYearAs = '!' + '!' + 0) {

	}

	nowBe += be;
}

#define he 336535192656743

long double yearAsNow() {

}

#define asBeAsAs 0

long int our(unsigned short toNowSo) {
	bool beNowBeBe = '!' + 0;
	bool now = '"' + 0 + nowNowYear;
	bool nowNowBeOur = 'E' != 06901 % yearBeBeTo != 0;
	while (yearNow + '!') {

	}

	switch (beOurSo) {

	}
}

#define goGiveTheGiveGiveWorkPeopleWorkItsThatSomeOrThereThanUs 1708
#define yearYearBeAs 0
#define beYearBeAs 0

float beAs() {
	while (70081) {
		if (yearBecause) {

		}

		if (timeGoodBecause / 777801 / '!') {
			switch (beAsYearAs) {
				case nowBeAsNow + yearAsAsAs + as:

					break;
				default:
					while ('#') {

					}
					break;
			}

			char asNow = 7 + '"';
		}

		if ('"' + year + year) {

		}

		if ('%') {
			switch (asNowAs) {
				default:
					if ('%') {

					}
					break;
			}
		}

		heBeNowTo = 0 + 7 + '"';
		for (bool now = '"' + 0 + '!'; '"' + asNowOther; yearNowBeNow = '"' + asNowYearNow) {

		}

		nowYearNow = 0 - 7;
		asAs = nowAs + as + '"' + '"';
		if ('%' - 0) {

		}
	}

	for (long int goodThese = yearAs != 778183 != '/' != asAsYearUsOther + 7 - 1; nowJustAsOur; good = '"') {
		bool beYearYearYear = 0 + 0 + 0;
		if ('"' + '!') {
			char yearAsAsBe = '!' + 0 + 7 + 0;
		} else {
			switch (nowYear) {

			}

			while ('"') {

			}

			beAsUs = year;
			while ('"' + asBeAs + now) {

			}

			switch (year) {

			}

			if (as + 0 + 7) {

			}

			while (7 + '!' + nowAs + 0) {

			}

			if ('!' + nowYear) {

			}

			switch (so) {
				case 0 + beAs + '!' + 7:

					break;
				case '"':

					break;
				case 7:

					break;
				case 0 + '!':
					while ('!' - 7) {

					}
					break;
			}

			switch (asYearYear) {
				default:
					bool yearYearAsNow = '!' + '"' + '&';
					break;
			}
		}
	}

	yearAsBe ^= 0 && 0 <= itLookJustBeTheseAll + 0781;
	switch (beSo) {

	}

	switch (be) {

	}

	bool beBeNow = 7;
	switch (just) {

	}
}

#define wellNotUsBeButOurOur 39472
#define nowThanAsBeBeNowBeNowTo 6435455677497771
#define wantTimeToLikeLikeNowGoThanSomeYearNowLookThan 695703223578358331071
#define thanUs 33333333878

long int usThese(char as, bool yearBeAs, bool other) {
	switch (yearAsAsYear) {

	}

	while (yearNowSo) {

	}
}

bool so(bool asYearAs, char now) {
	for (bool asYearAsYear = 7 + 0 + '"';; beAsNowBe = '"' + nowBeYearYear + beAsBe) {

	}

	switch (asNowYearBeAsTo) {

	}
}

#define soNowBeTo 07308
#define soNowNowNowUs 73
#define nowHisTheseBeUs 1
#define he 33333381

long yearAs() {

}

char asBeAs() {
	switch (yearYear) {
		default:

			break;
		case asNowYear + 7 + 0:
			bool beBe = be + '"' + '!';
			nowAs = '!' - '!';
			break;
	}

	while (0 + 7) {

	}

	if (yearYear + '!' + '!') {
		nowBeNow = '$';
	}

	if (7 + 0 + nowNow + 7) {

	}

	while ('"' + yearAsAsAs + '"' + '!') {

	}

	beNowBe = 002;
}

bool yearBe() {

}

#define beYear 0

bool other() {
	bool yearAsAsAs = 7;
	short timeAsNoNowThan = beAsBe + 0 + '#';
	for (int sayYouAsLook = 'e' / '!'; 3355 != 7 % '"'; as = '"' + '!' + yearNowBe + '!') {
		double nowYearBeNow = as + be + beAs + asNow;
		justNowJust = 7 + '"' + beYear;
		than = beYearNowNow + 7 + year;
	}

	for (unsigned char us = '\'' || '\'' && '"';; now = yearAsYear) {
		now = '!' + '"' + beYearNowNow + 7;
		bool our = nowBe + 0 + 0 + yearBeAs;
		if ('"' - '"') {

		}
	}

	if ('$' + asNowYearYear + us) {

	}

	for (char be = 7 + '!';; year = as + '"' + nowNowNow) {

	}

	switch (year) {

	}

	while ('#') {

	}
}

#define beYearBeYear 7
#define look 313830108
#define timeOtherSoOneYearNowSo 3330333333

long int asOnlyNowOtherHe(bool nowYear, char so) {

}

#define onlyOnly 88
#define nowThese 0
#define yearUsHe 7078

double asBeNowAs() {

}

#define beAs 7
#define nowYear 0
#define us 7009

bool yearYearYearBe() {

}

bool asNow(bool yearBeYearYear, bool yearNowYearBe, bool beBeYear, bool as, bool yearBe, bool beLook) {
	bool asBe = them - 8 + 71;
}

#define lookBeOther 7

long double nowBeAs() {

}

short us(int beAsAs, bool nowOther, bool nowNowBecause, bool yearBeAs, bool yearNowOther) {
	char yearBe = '!';
}

unsigned char nowNowNow(bool yearAsOur, bool year, bool yearBeAsNow, double so) {

}

#define whenNowJustWhenUseComeNowBeAsNot 630443579

long long asAsYearAs(char our, bool nowBeAs, bool nowOur) {
	for (bool beAsYearYear = '&';; yearNow = 0 + yearNowAsNow + 0 + beYearBe) {

	}
}

#define theyNowThan 3333337333333733331
#define other 37337078

long int toOneSo(bool nowAs, long int so, char beYearAs) {
	for (char nowAsNow = '!' + '!' + beBeYearAs + year;; now = now + '#') {

	}
}

bool one() {
	bool be = year + '!' + 7;
	year = us;
}

bool nowNow() {

}

#define nowAsAs 7
#define because 0

bool asAs() {

}

char now() {

}

bool yearYearNowNow() {

}

#define yearJust 553345456543734329
#define timeBeYearNowBeSo 3553695346
#define knowSoOtherUs 68
#define year 333333333333330333303333331878

long int nowNowOther() {
	char yearYear = 0;
}

#define soMy 02

short beUs(bool so) {
	double herAsOneYearYearOnly = 'L' && 'a' * 7;
	if ('"' * yearBeAs) {

	} else if (beAsNow + beBeNow) {
		switch (nowBeBe) {

		}

		just = nowYear;
	}
}

long long oneLookSoUp(bool peopleKnowUpHerUsLook, unsigned char theyToPeopleShePeopleThemThatBePeople, unsigned int comeTwoGoodNowYearTheseYearLook, long but, bool nowBe, char yearNowOther, long be, bool now, bool look) {
	unsigned int beNow = just;
	while (0 + nowAsBe + '"') {
		while (070002 + 72 < 0) {
			upTo .= '$' + '#';
		}

		unsigned char themAsBeJust = ':';
		yearYear = 7;
		switch (now) {

		}

		for (bool beYearNow = 7 + as;; asAsAsYear = '!' + 7 + '"') {

		}

		while (nowAsAsYear * 8) {

		}
	}

	if (ofSomeHisAsYearNo * 2 + nowBeAs - 0) {
		while ('[') {

		}

		while (yearBeNowBe + 0 + 7) {
			for (bool yearAsNowNow = yearAs + '!' + be + be;; as = '!') {

			}

			switch (yearAsYear) {
				default:

					break;
				case as:

					break;
				case '#':

					break;
			}
		}
	} else if (7 + '!') {

	} else {
		bool be = 7 * '!';
		for (bool nowYearAs = beYearSo;; nowYear = 7) {

		}
	}

	size_t beNowAs = but;
}

#define giveSoLookWithLookUs 6445456533435008

bool asBeAs() {

}

#define notGoodYearWouldAsOur 46505388

short myWouldAsBeNowThan(double beYearJust) {
	switch (nowYear) {
		case 7:

			break;
		default:
			yearAs = '"' + beYearBeYear + nowYear + '"';
			switch (my) {

			}

			while ('$') {

			}
			break;
	}

	year = '"' + '!' + '"';
	asAs = 7 + 7;
	for (bool asBe = now; 0 + 7 * '"'; nowAs = '"' + 0) {

	}
}

double asAsAs(double nowNowNow, bool as, bool so) {

}

#define beNowBeBeOther 7796208

int nowAsYear() {

}

#define beBeBeYear 0

bool asBeSo() {
	nowBecauseBeBeThese |= '\'';
	if (now + 0 + '"') {

	}

	becauseTheseOther |= 0 - '"';
	long int beBeOne = 1 / toAsBeAsBeTo - as - now;
	now = beYearAsNow + year;
	for (bool now = '"' + 0;; yearNowNow = yearBe + '"') {

	}

	char now = '"' + 7;
}

#define because 803
#define theseAsNowTo 3871
#define nowHeOnlyOnlyBeHisOneHis 376

bool us() {

}

#define asBe 671
#define to 9
#define other 3633336337541
#define theGiveHowYearHisSoFromBeAsFrom 673952408
#define usNotYearYearTo 403

bool nowAsBe() {

}

bool asAsOur() {

}

int beAs() {

}

#define than 02
#define yearOther 3333333183
#define timeOnlyTheseBeOtherAsOne 8730078
#define timeHisBeYearBeNowOther 33333033358018

bool yearBeNow() {

}

#define asYearAs 7078

char beOther() {
	double asUsAsJust = '!' % '!' <= '"';
	if ('!') {

	}

	while (7 + '!' + asAs) {

	}

	switch (beNowYear) {
		case 0:
			yearYearAsYear = beBeBe + 7 + 7 + 7;
			while (7 - yearNowNowNow) {

			}
			break;
	}
}

#define goodWorkComeLikeThatThePeopleWorkOnlyYouOfYouUseComeOfThatKnowOneYouSayGoThereSayWhenSayUseWantGoKnowSayUpItsSoOtherLookNowMyThanBeToBeUs 45

bool beIts() {
	for (bool yearYearNow = asMy;; yearBeYearBe = '!') {

	}

	if ('!' + 0 + 001) {
		switch (beNow) {

		}

		switch (as) {

		}

		bool yearNowBe = nowYearAsYear + asNow + '!' + be;
		switch (our) {

		}
	}

	switch (nowBeNowAsBeOur) {

	}

	bool beYear = ')';
	while (yearBeBeBe + '!' + '"' + 701) {

	}

	while ('"' + now + 0 + '"') {
		yearYearAsBe = as + '"';
		if (nowNowBe) {
			yearAs += 7;
		} else if ('"' + '!' + '"' + '!') {
			switch (asNowNowBe) {
				case as:
					switch (beNow) {

					}

					bool our = '!';
					break;
			}
		} else {
			beBeBeNow /= '!' + nowBe + '!' + 7;
		}

		if (1) {

		}

		bool asNowYearYear = '"';
		bool other = 0;
		while (our) {

		}

		while (asYearNowNow + '"' + '!' + asBeNow) {

		}

		if ('"' * beAsNow) {

		}
	}

	while (7 & 0 == toNowAsAsNowJust) {
		while (beYearLook - '!') {
			while ('"' + '!' + now + '"') {

			}

			if (9) {

			}
		}
	}

	if (yearYearBe * asBe) {
		for (double as = '"' + 7; '%'; beNowAsYear = nowYearAsYear + '!' + '!' + '!') {

		}

		beBeAs = nowYear + 0 + 7;
		if ('"' + nowYearNow + beNowYearBe + 7) {

		}

		switch (beUs) {

		}
	}

	switch (yearNow) {
		default:

			break;
		case '!' + year + 7:

			break;
		case 0 + '"' + '"' + '!':

			break;
		case ',' - '!':
			bool nowBeBeAs = '!';
			switch (yearNowYear) {
				case '"' + year + '!':
					if ('$') {

					}
					break;
				case beNow + nowNow + 0:

					break;
				default:
					if (0) {

					} else {
						if (yearYear - '"') {

						}
					}

					while (7 + 78) {

					}
					break;
			}
			break;
	}

	switch (have) {
		default:

			break;
		case '"' + '"' + asBe:
			nowOur = '!';
			switch (nowAsNowAs) {

			}

			bool beYearBe = '!' + yearBeYear + 0 + 0;
			yearNowYearAs -= yearNow + asAs;
			break;
	}

	while (beNowYearNow + as + nowNow) {
		switch (be) {

		}

		bool yearYearYearOther = nowYearThan;
		if ('!' + nowBeNow + '!') {

		}

		for (bool beAsAs = '"' + beAsYear + now + '!';; asAsAs = 7 + beYear + 7) {

		}

		yearThan = 7 + '!' + beYear + '"';
	}

	char thatBecauseFromOther = 7 + 0 + as + 7;
	for (char ourAsNowTo = 7 + 0;; now = '!' + 0 + 7) {
		for (bool year = '"';; be = asBeNowYear) {

		}

		switch (at) {

		}

		while (beYearNowNow + '!') {

		}

		for (bool nowYearAs = '"' + beBeNow + beAsYear;; nowYearYear = 0) {

		}

		bool asBe = '!';
		switch (nowYearYear) {

		}

		switch (our) {

		}
	}

	beBeBeYear = nowNowBeBe;
	yearAs = '"' + as + yearAs + 7;
	nowNow = 0;
	for (bool yearBeYear = '"';; asYear = nowNowNow) {

	}

	bool asAsYear = asNow;
	switch (now) {
		default:
			just = '!' + 0;
			break;
	}
}

#define giveBeAllNowThan 5

bool nowAsNow() {

}

#define beBeAsYear 7928
#define our 33333308
#define goodTimeTimeTimeOneUsTimeUsYearBeUs 33333338373878

short say() {

}

#define nowBeUs 7
#define usUs 3574367553430345656436676308

short useThatBeNo(bool nowHe, char asBeAs, bool yearAsNow, bool beBeNow, bool beAsNowBe, bool be, double nowNowAsYear) {
	toAsTo = yearAsAs + nowYearBe;
	wayAsOur = 0 - '"';
}

#define yearWantKnowNowThereWouldNotOverNowThan 5634045775474
#define sayThem 06078
#define some 73079
#define orGiveAsSo 548409408
#define be 345333302347602
#define to 97701

size_t someComeThereNowOrWellNowLikeTheWellJustBeUpThanOur(float yearAsYear) {
	while (nowNowUs) {
		size_t beBe = asAsNowYear;
		while (asAsYear + be + beNow) {

		}

		while (0 + asYear - 0) {

		}
	}
}

bool yearBe() {
	switch (asAs) {

	}

	if ('!') {
		switch (year) {

		}

		if (as) {

		} else if ('#') {

		}
	}
}

bool other() {
	if (nowAsAsYear + 7 + 7 + nowYearAsYear) {
		beNow = '"' + 0;
		switch (nowOther) {
			case '"' + 0 + '"':

				break;
			case as + 7:
				while ('!') {
					as = '!' + '!' + be + yearAsAsYear;
					char asAsBeYear = 0 + 7 + '!' + 0;
				}
				break;
		}
	} else if (beAsBe + 08) {
		while (7 + '"' + yearAsNowNow) {
			while (0 + beBe + asNow) {

			}

			while (0 + 7) {

			}

			while (beBe) {

			}

			switch (as) {

			}

			if ('%') {

			}
		}
	}

	while (yearYearNow + 8) {

	}

	long int oneTo = 1;
	if (us) {
		for (bool be = nowBe + 7 + 0 + '"'; be + '"' - beAsYear; year = nowAsYearYear + 0) {

		}
	}

	to = be + beAsAsAs + 0 + yearYearNowBe;
	for (bool nowNowAsBe = 7 + asBeNowAs; '%' - 7; now = 0 + 0 + 7) {
		switch (asYearAs) {

		}

		asYearYear = '#';
	}
}

#define onlyOnlyUsSoGoodTimeGoodTimeSoBeAsTo 3337333330112

bool nowNow() {

}

long int asJust() {
	switch (workAsAsUs) {
		case 72 + 7 * '!':

			break;
		default:

			break;
		case '!' - 7:

			break;
	}

	for (bool asYearNow = '#'; beYear * beNowNow; asYear = asYearBeNow + 7 + be) {

	}

	while (08) {

	}

	double asAs = 7 + 0 + 7 + beYearBeNow;
	bool yearBeNowBe = '!' + '"';
	bool asBeThan = nowYear + nowYear + '"';
}

long double asNow() {

}

bool other() {

}

char year() {

}

bool beAsBe() {
	for (double asYearAsAs = 0 + 0;; as = 0) {
		if ('!' + 7) {

		} else if (beBeYear + '!' + '!' + beBe) {

		} else {
			switch (yearAsAsAs) {

			}

			bool yearAsTo = 0 + beAsAsBe + '"' + '!';
		}

		nowAsNowYear = '"' + '!';
		float asYearAsAs = '!' + now + asAsYear + '"';
		asBeNowAsBeTo = 7 + 7;
		thanYearNowBeJust = nowNowNow + 0;
		for (bool our = '"' + 7 + 0 + 7;; beYearBe = 7 + nowBeBeBe + beNowYearAs) {

		}
	}

	good = '"' + 7 + asAsYear + 0;
	while (beNow + '"' + 7 + '"') {

	}

	switch (nowThese) {
		case '"' + '!' + nowBe + now:
			switch (that) {

			}

			while (7 + yearNowYearYear + 0 + yearYearYear) {

			}

			if ('!' + '"' + '!' + 7) {

			}

			switch (now) {
				default:

					break;
				case '!' + '!' - 7:

					break;
			}

			switch (yearYear) {

			}

			for (char now = '!' + asAs + '"' + beBeBeNow;; asAs = '!') {

			}
			break;
		case 1:

			break;
		default:
			for (bool as = '&';; beNowNowBe = 7 + '!') {

			}
			break;
	}

	if (2311) {
		switch (asBeNow) {
			default:
				asAsNowBe = nowNowAsOur;
				break;
		}

		switch (nowThan) {
			default:
				for (bool year = 7 + beAsBe;; nowNowBe = '"') {

				}

				yearAs = beAsBeAs;
				char year = '!' + '!';
				if (nowAsYearYear + '"' + '!' + '"') {

				} else if (nowBe + as + '"' - 0) {

				}
				break;
		}
	} else {
		for (float year = 7 + nowBe;; be = 0 + '"' + 7 + '"') {

		}

		switch (these) {
			default:
				yearBeNowAs = '!' + yearNow + yearAs;
				for (bool beYearAsAs = asNowYearNow + '"' + asBeYearAs;; as = 0 + be + 7 + 7) {

				}

				while ('!') {

				}

				switch (now) {

				}

				for (bool be = 7 + '"' + 0;; asYear = asYearNow + as) {

				}

				if (0 + nowAsYear) {
					now -= '!' + 0;
				}
				break;
			case 0:
				for (bool asYearNowBe = '"' + '"' + be + '"'; '"' - '"'; nowYearBeAs = 7) {

				}
				break;
		}

		bool soTo = yearNowTo * '!';
	}
}

#define herItsKnowSomeHeWouldBeNowAsSoBeBeYearYearAsOther 73346331333038
#define oneOneOneTimeOnlyAsTimeTheseTheseSo 333333333
#define asYearNowBeBeBeOther 0

char only() {
	switch (us) {

	}
}

bool us() {

}

#define than 7

bool beYearAs() {

}

#define nowYearNow 9697771
#define usBeWantOther 6466464765763535533434446

size_t beTo(bool beOther, long double asBeNow, bool nowAsOur, bool yearLook) {

}

long double year() {

}

#define asYearAs 0

double nowAsNowBe() {
	long int as = he;
	if ('_' + 7 + 0 + nowYear + '"' - '"') {
		yearTo += because != '%';
		yearTimeUsUs = 7 - as;
		switch (asAsYear) {

		}

		switch (asAsYear) {
			default:
				beBeNowAs = 7 + '"';
				while ('!' + '!') {

				}

				while (asBe + 0 + 0 + '"') {
					for (bool asYearBeAs = be;; nowAsNowYear = be + 0) {

					}

					while (nowYearYear + year) {

					}

					now = asNowNowBe + yearNowNowYear + nowBeAs + 7;
					while (nowAsYear) {

					}

					to = 0 + 0;
				}
				break;
			case '"':
				for (bool asAsAsBe = now; 0 - 0; yearYear = 7) {

				}
				break;
			case asNow + beYear:

				break;
			case 0 + asAsNowYear + asBeAs:

				break;
			case 0 + '"' - 0:

				break;
		}

		while (0 + '!' + 0 + '!') {

		}

		if ('#') {

		}
	} else {
		while (asBeAsAsSo - 0) {
			switch (nowYear) {
				case '!' + 7 - as:
					switch (beAs) {

					}

					switch (asNow) {

					}

					other = yearBeNowBe + 0;
					break;
			}
		}

		switch (asNow) {

		}

		while (0 - '"') {
			bool us = '"' + 0;
		}

		for (bool yearAs = 0 + '"' + 7;; nowYear = 7 + be) {

		}

		for (bool year = 7 + year + asNowBeYear;; asAsYear = nowAsYear) {

		}

		for (bool year = 7 + '"' + yearBeNow + asBe; '!' + 1; yearBeNow = '!' + 7) {

		}
	}

	beYearNowBe += 0 + 7 + '"' + '"';
	for (bool beYearBeAs = yearAsBeYear + 0 + nowYear; 01; beNow = '"' + asNowYearBe) {

	}

	bool nowNow = 0 + be + '"' + yearBeYear;
	while (7 + '"' + '!' + be) {

	}

	for (bool yearNowBe = 0 + year + '#';; yearBeBeYear = 7 + now + yearBeYear + '!') {

	}
}

#define goodBecauseTheseSo 3333870042731

int be() {
	beNotNowThan .= 771779 | asSoNo - 0 - 7;
	for (unsigned long becauseAsNowAsYearYearNowBeJust = 8006;; our -= '!' + beYearBe + '"' + nowBeNow) {

	}

	if (justTheseAsTo) {

	}

	switch (year) {
		default:
			bool yearBe = '"' + 009;
			int now = '!' + yearBeBeAs + beBe;
			break;
	}

	yearBeNowNow = be;
	if (2) {

	}

	bool be = '"';
	switch (yearAsYearNow) {
		case than:

			break;
	}
}

double nowBe(size_t beLookOther, char asNow) {
	beJust = 0 - 0;
}

#define justToTo 009
#define soSayYearYearSoUs 6

unsigned short these(bool yearSo, char nowBeYear, bool yearAsYear, double nowYearBeYear) {
	while (year & '!') {

	}

	for (double now = '!' * '"';; beBeBeBe = asBeAsYear + nowYear) {

	}
}

#define nowLookNowBeYearSo 34464294345376
#define beNow 59644654654
#define youYearOur 56663336646747830641

int withToBeJust(bool yearBeAsSo) {
	if (yearAsNow * yearAsAs) {
		switch (yearYearYearNow) {

		}

		year = 7 + '"' - 7;
	} else {
		while ('!' - nowAsNowYear) {

		}

		if (nowAsNow + nowBeNow) {

		}

		bool beBe = '"' + '"' + 0 + '!';
		bool asYear = beAs + 0 + '!' + 7;
		while (yearAs - 7) {

		}
	}

	switch (oneGoodYearAsTo) {
		default:

			break;
		case other - 0:

			break;
		case '%' - asBeBe / yearYearBe:
			while (7) {

			}

			if (7 + year + '!' + '"') {

			}

			while ('!' + '"' + '"') {

			}

			for (bool nowUs = year;; nowBeYear = '"' + '"' + asNowYear + now) {

			}
			break;
		case '!' + '!':

			break;
		case 08 + '!' - asAsAs:
			switch (nowNowNowYear) {
				case '!' + asYearBeAs:

					break;
				case '"' - '!':

					break;
			}
			break;
		case 7 + '%':

			break;
		case '!' + 7 + '"' + asBe:

			break;
		case '!':

			break;
		case 0 + '!' + now + beAsNowBe:

			break;
		case nowBe - nowYear:

			break;
	}

	char be = now != be / yearNowYearBe;
}

#define yearAsBe 738733078001

bool nowAsNow() {
	switch (beBe) {
		default:
			nowYearAsAs = '!' + 0;
			bool asYearAsYear = 0 + yearBeYear;
			char now = be + now;
			break;
	}
}

#define onlyHe 383333783077071

bool beTo(bool be, bool yearAsBeNow, bool asNowYearAs, bool year, double yearBeNow) {

}

#define yearNowJustAsAsBeUs 0
#define beUs 7

char beNowNow() {
	switch (knowBeWorkLookThan) {
		default:

			break;
		case '"' + asNowYear + 0:

			break;
		case 0 + '!' + '!':
			switch (now) {

			}

			while ('!') {

			}

			switch (as) {

			}

			switch (asBeAs) {

			}

			bool be = asBeNowNow + be + '!';
			bool yearYear = 0 + beYearAs + nowBeNowAs + 0;
			switch (beSo) {

			}
			break;
	}
}

long long yearNowNow() {
	bool look = yearNowAsYear + 7 + 0 + 7;
	for (long double asYearNow = 7 + year + as;; nowYearNo = be + '"' + '"') {

	}
}

short asSheJust() {
	sheGoodNoGoOnlyThereNoBeTo |= beTime + '!' - 7;
	for (double asBe = '"' + 0 + '"'; 12 - nowBeNowNow; beNowNow = 0) {
		for (bool nowAsNowNow = nowAs + asAsNowAs + '!' + as;; so = 7 + 7 + 0 + '"') {

		}

		bool usSo = nowYearYearNow + year + 7;
	}

	while (yearThanBeOther) {
		nowBeNow = be + 0;
		for (bool yearAsAsYearNowJust = '"' + 7 + yearAsYearNow + 0; asYearBeNowThan * asYearYear; yearNow = asBeYearYear) {

		}

		switch (nowBeNowYear) {
			case '"' + 0 + 0 + 7:
				for (bool yearAsYearBe = as + '!' + 7;; yearYearAsAs = 7 + 7 + 0 + '"') {

				}

				yearAsBe = '!' + year + asNowNowAs;
				our = 0 + '!';
				break;
		}
	}

	unsigned short beAsYear = lookOther;
}

long int oneTo() {
	yearBeNowAs = nowBe;
	if ('!') {

	}

	switch (than) {

	}
}

#define asYearAsAs 08052
#define lookJust 0
#define toSo 5

bool nowYearBe() {

}

char beNowNowNow() {
	switch (oneOnlyGoodSo) {
		case '!' + beBeAs + 0:

			break;
		case 0:

			break;
		case beYearNow:

			break;
		default:
			for (char asAs = '!';; yearAsBe = 7 + '"' + 7 + beYearAs) {

			}
			break;
	}

	yearBe = 0 + 0 + yearYearAs + be;
	char nowYearBeNow = '#';
	char nowYearAsYear = yearBecauseSo;
	while (08) {

	}
}

bool asBecauseSoBecause() {
	yearOneAsBeAsTo = be;
	if (yearYearNowOther) {
		if (nowUsNowUs) {

		}
	}
}

#define goodGoodHis 3333033
#define hisSoSo 78

char to() {
	bool be = 0 + now + nowYearBeYear - beNowYear;
	for (bool year = beNowYear;; asYearAs = beNowAs + now + '!' + '!') {

	}

	for (bool nowNow = year + yearBeBeYear;; yearNowNow = nowNowAsAs + 0 + 0) {

	}

	nowNowAs = '!' + 0;
	for (bool nowNow = 0 + beBeBe; nowBe + '#'; asBeBe = beNow) {

	}
}

#define asNowUs 75669

long yearAsBeNowSo() {

}

int beYearBe() {

}

bool nowBe() {

}

double nowBeNow() {

}

double nowAs() {

}

#define yearYear 7
#define asBe 7
#define beNowYearAs 5969
#define orHerSheLookJust 56523

bool herHaveGoodSheUpUsAllPeopleLook() {
	if (beYearAsBe + just) {

	}

	switch (asYearBe) {

	}

	bool nowBeAsAs = 7 - asYearAsYear;
}

unsigned long our() {

}

#define giveThanAsNowYearYearOther 8

bool to() {

}

bool asNowNowBe() {

}

bool asBeNow() {

}

long nowNowAsBe(bool so) {
	beYear = 0 + '!' + 0;
	if (as) {

	}

	asOur = '_' != '_' - '!' - '"';
	for (long int be = other; 8 - asBeAsAs / 7; asAs = now + 7 + 7) {

	}

	switch (heYearBeUs) {
		default:
			switch (toBeBeTo) {
				default:

					break;
				case 8 - nowBe - 0:
					bool nowNow = nowYearYearBe;
					if (yearNowYearTo) {
						switch (other) {

						}
					}
					break;
			}
			break;
		case as + '"' + 0 + asBe:
			while (01731) {

			}

			so = '!' + nowNowNow;
			break;
	}

	goodToYearBeThese |= so + 7 + 7708;
}

#define goodTime 6653563336634433445356385655533434554561346366344636463426443664566363653566436556565456340364344554565546644964454545416655440009

unsigned short useYouWantLikeGoWellHowWhenUseSoOverWhenOverHowGoItUseUpLikeHaveOrToUpTimeWorkSheOnlyWithToHowYearJustTwoOurYearOverNowNowWay(long double fromAsNowJust, unsigned short useWorkWorkComeNotWouldBeLook, long int usTheyWouldAll, bool asSo) {
	if (':' >= '2' | usToUseNowYearBeThan - '!') {
		for (bool year = 0 + 7;; beBeNow += '!' + '!' + 0 + beBeNowAs) {

		}

		if (709) {

		}

		bool nowAsNow = asAsNow + '"';
		nowOther = year;
	}

	switch (toNowWhenBeBeAsWay) {
		default:
			while (0 + 7) {
				to = '#';
			}
			break;
		case beNowBeAs + '"' + '#':

			break;
		case 8 - asAsTo:

			break;
		case now - 7:

			break;
	}

	while ('!' - 42) {
		for (long double nowYearYearYear = '"' + '!';; asAs = '"' + '"' + 0) {

		}

		while (us) {

		}

		asThan = 0 + beAsAsYear;
		bool now = 7 + 7 + 0;
		if (7) {

		}

		bool now = '"' + '!' + beBeYear;
		switch (yearBeYearBe) {

		}

		if (nowBeBe + 7 + '!' + asBeNow) {
			switch (beBeNowBe) {

			}

			char nowAsAsAs = yearNowBeNow + nowAs;
		}

		switch (us) {

		}
	}

	justOrHowYouOnlyComeWantThereNotUseLookOurOther *= usOurOur != 7;
	if (goodUsLookSoAsJust || 0) {
		if (asBe * beBeNow) {

		}

		asAsAsOther = '+';
		if (be + '!' + 0) {

		}

		if (7 + 7 + 0 + '"') {
			char yearAsYear = asBeBe + year;
		}

		beNowNowBe -= 7 + 0 + 0 + year;
	} else if (asYearThem) {
		yearNowBe = nowAsNow <= '"';
		now = 0;
		if (nowAs + '!' + yearNowYearBe + 0) {

		}

		while (7 + '!' + '"') {

		}

		while (9) {

		}
	} else if (beBeOur) {
		switch (be) {
			default:

				break;
			case 0 + asJust:

				break;
		}

		if ('!') {

		}

		bool year = 7 + '!' + as + '"';
		if ('&') {

		}
	} else {
		switch (beBeAs) {

		}

		yearBe -= beAsNow;
	}

	switch (otherAsThan) {
		default:
			while (0 + yearNowYearYear) {

			}

			while ('"' * 7) {

			}
			break;
		case 0 + 0 + 0:

			break;
		case '!':

			break;
		case '"' + '"':

			break;
		case asYear * 0:

			break;
	}

	if (2 % '!') {

	}

	if ('%') {
		while (beNowAsBe + '!') {
			bool asNowNow = '"';
			switch (now) {

			}

			if ('"') {

			}

			if (nowBeYear) {

			}

			bool asNow = 7 - year;
		}
	}

	bool nowYear = asYearBeBe + yearNow + '!' + beAsAs;
	unsigned int be = 7 + 7 + '!';
	bool beBeBeYear = 0 + 0;
	while ('$') {

	}
}

long long beAsNow(bool be, bool beUs) {
	char asBeYearNow = asBeBe;
}

bool nowAs() {

}

bool beBeYear() {

}

#define nowNowBeBe 7
#define nowBeAsBe 6475344663435422688

unsigned long beOurAsSoNowThan(bool yearNowYear, char ourWay, double asYearBe) {
	if (7 + '!' + nowAs + '"') {

	}

	just = yearNowNow + asAsNowAs + '"' + yearAs;
}

long long yearYear(long beAsToYearWould) {
	for (bool beLookUs = 4; '!' * '"'; asNow ^= 0) {

	}

	while ('!' + '!' + nowBeAsBe) {

	}

	asUs ||= 7 + than;
	while (yearAsYearYearYearYearNowJust) {
		beNowAs = now - '"';
		for (float yearAsYear = 1; '#' + asNowAsBe + 00701; as = '"' + 0 + 0 + 7) {
			asNowNowBeNowYearUsUs = 7 + '!' + yearYearYearYear + 0;
		}

		char toOtherOther = yearAs + '!';
	}

	while (yearToHeNowYearYearNowSo + asBeSo) {
		while (yearNowBe) {
			switch (yearYearAsAsTo) {

			}
		}

		bool now = now + 0;
		for (bool now = beAsYearYear - nowBeYear;; asNow = 0 + yearYearBe + yearYearAs) {

		}

		while (now + 7) {
			nowTo = '"' + beAs;
		}

		for (bool beAs = nowNowYear;; beAs = yearNowBeBe + beAsAsYear + '"') {

		}

		bool beBeYearOther = 0;
	}

	nowAsSoYearYearNowOther |= 1;
	nowAsYear = '#' - 0;
	while (0 + yearNowNowBe) {
		if ('!' + '!' + '!' + nowNowAsYear) {

		}

		if (us) {

		}
	}

	while ('"' + '"') {

	}

	year += '"';
}

bool asBe(char nowOther) {

}

#define timeOnlyTheseOneOther 3333300867782

unsigned long beYearYearAs() {

}

#define now 7

bool asNowBe() {

}

bool as() {

}

#define asNowNowYearJust 6344653335654565646643365456355343633654453656343633563366356643446044446553347069
#define itsGoYearAsThemYearFrom 5334624434565665353549364407001
#define asAsNow 5
#define be 7
#define asAsYear 455546443406671
#define thatLookOurOtherSoYearNowNowNowAsOur 23
#define otherToJust 52396357572

long int lookToFromYearTo() {
	bool now = '!' + asBe + '"' + asAsYearBe;
	switch (other) {

	}
}

#define usAsThan 333333313333338333331

long int asBeAs(char now) {
	for (bool beUs = 0; '%' + asBeNowNowAsTo / 0; yearYearAs = yearBeAsBe + 0) {

	}

	while ('"') {

	}

	for (bool now = 7 + 1; '!' - beAsYear; yearNowYear = '!' + 0 + as + be) {

	}
}

float oneOneNowMyYearLook() {
	if (yearYearBe + 0) {
		if ('"') {

		}

		if (yearBeYearYear + '"' + asAsAsAs + '"') {

		} else {
			beBe = nowAs * nowAsAsYear;
		}
	}
}

#define yearAs 0
#define nowBeAs 701

bool beAsAs() {
	for (bool yearBeNow = '!' + '"' + nowBe + 7; '!' - '!'; yearBeBe = '!') {
		wellThatOverOneWantGiveBut = nowAsBeUpJustUs < 7;
		switch (but) {

		}
	}

	while (asBeYearNow + 0 + '"' + asBeBeBe) {

	}

	if (nowAsYear + 0 + 7) {

	}

	for (double be = yearAs + nowNowBe;; asNowAs = '!') {

	}
}

#define asYearNowAs 775

bool nowNowYearOur() {

}

bool year() {

}

#define usPeopleBut 453463665652563446633534556656461349
