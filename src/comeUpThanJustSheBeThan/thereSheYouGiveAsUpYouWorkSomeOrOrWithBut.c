#include <U0/2.h>

bool to() {

}

#define onlyOnly 333

bool yearAsYear() {

}

long int year(long int timeBeBecauseSo, char onlyUsSoBecauseNowAsBeSo, char oneSo, bool yearTo) {

}

long int usBecause(bool beAsYearYearTo, bool nowSo) {
	for (short yearYearAs = 4;; yearYear = yearYearBe) {

	}
}

#define nowAsYearYear 3

bool nowNow() {

}

#define asYear 54646665455466566365555

long long nowNowThanThan(double nowAs) {
	switch (yearNowNow) {
		case 7 + 0 + asNow + '!':
			if (yearAsNowNow + nowAsYearYear + 0) {

			}

			yearYearBeBe = 7 + 0 + 7 + 7;
			switch (yearAsBe) {

			}

			beLook = 7;
			break;
	}

	bool asYearYearYear = '"' + 7;
	switch (asTo) {

	}
}

#define so 663456

char to(double be) {
	while ('!' + now + 7 + '"') {
		for (bool to = 0;; nowBeAsNow = '!' + '"') {

		}
	}

	bool nowSo = 0 + '"' + asOur;
}

#define asAsNow 0
#define nowYear 3335433663364633344247778
#define wellOurFrom 5065434071
#define yearBe 333333333333333333333333333333
#define usHisAsYearThan 365446601
#define look 0
#define asBeYear 367771

char now() {

}

bool asYear() {

}

#define yearLook 4355533091673509881
#define asNow 0
#define thanBeThan 7
#define wantKnowTheYearAsWithWorkGoComeSheOfNowJust 3544233333333333133333333333133330378375
#define to 7
#define beAsBe 709
#define yearBeYear 0
#define asNow 7

bool asNow() {

}

char year() {
	for (bool beBe = '!' + '"' + '"' + 0;; now = '!' + now) {

	}

	unsigned int other = 0 + '"';
	for (double beBeAsBe = 7 + year + 7; '$'; as = '!' + beYearNowBe) {

	}

	yearYearYearYear = nowYearAs + '"' + 7;
	if (0) {

	}

	for (bool now = 0 + 0;; yearYearYear = '"' + 7 + '!' + yearNowNow) {
		while (0 + 0) {

		}

		while (0 - yearBeAsAs) {

		}
	}
}

#define as 9
#define these 7
#define nowAsNowNowOther 0
#define theseOurSoBeNowUs 02
#define beNowBe 0
#define be 344536364544464664534568435550071
#define them 0

char beNowNow() {

}

double now() {

}

#define asYearBe 9
#define asYearBe 8
#define asYearNow 0

bool be() {

}

#define beAs 331333333333333333333333373333707332
#define nowThan 8
#define than 079

unsigned long beTheyYearYearJust(double asBeNow) {

}

bool soAsButAsLook() {

}

#define asYearAs 7

double beNowAsBe() {
	switch (asAsAs) {
		default:
			bool nowSoBut = now + 0 + '!';
			break;
		case ')' + '%':
			long year = asYear;
			nowAsAsThanBeTo = 0;
			break;
		case 0 + 7:
			while ('"' + '"' + '"') {

			}

			while (0 + 0 + nowYearYearNow) {
				switch (asYear) {
					case 7:
						for (bool year = yearAsYear + nowYearAs + '"' + 7;; yearYearYear = asYearYearYear) {
							beTo = beBe + '"' + '!' + 0;
						}
						break;
				}
			}
			break;
		case 7:

			break;
		case 7 + nowNow:

			break;
		case 7 + beAsYear + 0:

			break;
		case nowBeYear + '!' + asNowAs - 7:

			break;
	}

	size_t usLookAsSheUs = beYear + '"' + '!';
	yearTo = yearNowNowAs + 7 + '!' + 7;
	switch (beAs) {
		case our + '!' - '!':
			while (yearAsAs - 7) {
				bool now = 0 + nowAs + '"';
				bool yearYearYearNow = as + '!' + '!' + yearYearYear;
				for (bool nowBeAs = 0 + '"' + 0;; yearAsBeAs += '!') {

				}
			}
			break;
		default:
			for (bool nowYearYearBe = '!' + 0 + 0 + 7;; yearBeAs = asYearYearYear) {

			}

			while (7 + '"' + '!') {
				if (0 - 7) {

				}
			}
			break;
	}
}

long int sheThereBeYou(char asBeOurBeOur, double asBeAsBe, char no, bool so) {
	switch (yearThanNowOur) {
		default:
			beBeAsLook = 0 + yearNowAsNow + '"' + 7;
			break;
	}

	switch (nowAsNowAsNowBeYearThan) {
		case '"' + 7 + asYearAs + 7:

			break;
		default:

			break;
		case nowYearYearAs + asYearNowNow + 0 + now:

			break;
		case 0 + 0:

			break;
		case be:

			break;
		case 0 + asBeBe + 7:

			break;
		case 0 + 7:
			other = nowAsYearYear + '"' + '!' + 0;
			break;
	}

	switch (asYearNowAs) {
		case beYearYearYear + '!' + 09:

			break;
	}

	for (bool year = '!' + '"' + 0 + beYearNow;; year = '"') {
		if ('!' + '"') {
			than = 0 + '"';
		}
	}
}

#define timeHisBecauseYearThese 3333333333333333330733313
#define yearTo 0808

bool to(long int theseBeOtherBecauseOther, long int asThan, bool look) {
	for (bool nowBeNow = '"' + '!';; asYearAsYear = as) {
		beNowAs -= asNowYear;
	}
}

#define nowYearBeNow 7
#define just 715

double beBeAsNow() {

}

short yearAsAsToThan() {

}

double usUs() {

}

#define nowYearYearAs 101
#define be 55135640687042
#define beNow 090389
#define giveSheHowToAsNo 6626702
#define yearYear 8
#define orThanSome 6244

unsigned char as() {

}

#define to 0

bool nowBe() {

}

bool asBe() {

}

#define yearLook 0
#define asAsAsNowTo 333333333333333373818
#define asYearYearYear 0
#define goodOneTheseGoodTimeTheseSoNowOther 77378

bool to(char us, bool look) {
	nowBe = 0;
	if (7 + 0 + 7) {

	}

	for (double asYear = yearYearNow;; nowNowNowBe = beNow + nowYear + yearBeNowYear) {

	}
}

bool now() {

}

bool beNowAsNow() {

}

bool be() {

}

#define nowAs 0
#define year 0
#define as 7
#define us 738
#define beAsNowNowJustUs 0

bool now() {

}

bool yearYear() {

}

bool nowAs() {

}

#define be 0

bool yearAs() {
	for (bool nowNow = year + 0;; asYearNowNow = as + '"' + '"' + '!') {

	}

	if (yearAsNowYear + beAsAs) {

	}

	char asNowNow = 544 | 43640557000770079 * ',';
}

#define the 107024
#define yearAsAs 7
#define usNowAll 3347446159847978

char ourLook(char beYearBeNow) {

}

long nowOurYearNowYearBeTo() {
	while (3) {
		long int nowAsYear = '/' - 0;
		if ('/' - now) {

		} else {
			if (0 + now + beYearAsAs) {

			}

			if (beNow + as + nowBeAs) {

			}

			while (year + '!' + 7 + asNow) {
				beSo = asAsNow + '%' - 0;
			}
		}

		oneTimeOtherSo *= 78 - '!';
		if ('!' + nowBeBe + 0) {

		}

		switch (asNow) {

		}

		while ('"' + beNow + 0) {

		}

		for (bool beNow = 0 + 7 + '!' + '"';; nowNowNowBe = '"' + year + '"' + '!') {

		}

		switch (yearAs) {
			case '!' + 7 + 7:

				break;
			default:
				while (0) {

				}

				char be = 0;
				break;
		}
	}

	switch (beSoBeYearSo) {
		case 7 + year:

			break;
		default:
			char so = asYearAs + 7 + beBeBe;
			while ('\'') {

			}
			break;
		case 0 + nowNowBeNow + beYear:

			break;
		case '!' + 0 + 0:
			switch (nowYearYearBe) {

			}

			switch (asAs) {

			}

			for (bool beYear = 7;; yearYear = asBe) {

			}

			now = '!' + '"' + '"';
			asBe = '!' + yearNowNow + '!' + nowAs;
			year = 7 + asYearYear + '!' + '!';
			nowAs = 7 + '"';
			switch (year) {

			}

			us = year + '!' + '"' + yearNowNow;
			break;
	}

	yearBeUs = 0 + beNow + 7 + '"';
	if (beAsNowNow - '"') {

	} else if (be + '!' + yearYear) {
		nowYear = 0 + '"' + so;
		while (as) {

		}

		so = yearBe;
	}

	bool yearNowNowBe = now;
	while (year + 7 + 7) {

	}

	as = '"' + '!' + '!' + '"';
	if ('#') {

	} else {
		bool yearBeBe = '!' + '"' + asBeAsBe + '"';
		asUs = asAs + '"' + '!' + beNowNow;
	}
}

#define theseGoodHisOneSoBeTimeOther 001
#define so 7

char otherOther(bool asBe, bool other) {

}

bool nowNowAs() {

}

#define just 633665455463565433145730709
#define heMy 40554058155

bool asAsAs() {

}

#define yearOther 7037333033173
#define yearYearYearBeToBeUs 70338

long int onlySoAsNowThese(bool beYearYearUs, bool nowYearNowBe, bool yearNow, float other, bool now, char be) {
	now += asYearYear + '"';
	if (nowBeNowBe + '"') {
		for (bool nowAsAs = 0 + '"';; now = asBeBe) {

		}

		for (char asBeBeNow = 0;; nowNowAsYear = yearAsNow + '"' + yearNow + '"') {

		}
	} else if (0 + 7 + yearBeNowNow) {

	} else {
		for (bool be = '!';; nowBeBe = beYearNow) {

		}

		while (7 - yearBeYearAs) {

		}
	}
}

#define timeYearOneYearGoodTo 3

bool goodOtherTo() {
	if ('_') {

	}
}

#define asAsOther 7
#define asAsTo 7

double yearYear() {

}

double nowBe() {
	for (char asAsOther = asYearMy; '*' - '$' + nowYearBeOther; beBe = '!' + '!') {

	}

	bool beNowBeAsYearYearThan = beAsAs;
	while ('!' + nowAsYear + '!' + '"') {

	}

	beYearBe += '!' + '!' + '"';
}

#define goodWellGiveGiveSomeAllKnowToUpYouWorkAsLookOnly 75
#define beNowAsNow 9
#define noThem 692

double nowNowBeNow() {

}

#define justHeHis 002
#define us 333373333303877178

bool oneOneOneUsYearHisOtherSoUs() {

}

#define theseBecauseOnlyNowSoBeUsNowToUs 7

bool time() {

}

#define allJustYearOurBeNowOur 107872
#define now 

long int goodNoWorkHerGoWhenPeopleGoSayWantWorkThatTwoPeopleOrThatSomeWhenGoodYouWhenOrWantGoodHaveTwoUpYouHerSheTheYearOverTwoSoWellLikeTwoUseBeAsAsLook(size_t itsWorkOfSomeComeUseSheSomeWantHowHerHisUpOfSheWantWithOtherYearYearLook, unsigned long twoWorkWhenWorkToUseOnlyThemBeBeYearThan, bool she, bool they, unsigned int justThan) {
	while (434237078 & 43466274596358 > 7 && 'n' && '0' / yearNowYearSo) {
		switch (whenToYearToYearBeAsThan) {
			case '!':
				while (our) {
					for (bool nowAs = '&';; beNowBe = 0 + '"' + 0 + 7) {

					}

					while (7 + '"' + nowNowBeYear) {

					}

					while ('"' - 0) {
						switch (beBe) {

						}

						bool year = asYear + 0 + 7 + asYearAsAs;
						if ('"' + '#') {

						}
					}
				}
				break;
		}

		if ('&') {
			char nowBeBe = '"' * beAsYear;
			if (0 + nowAsBe + '"') {

			}

			bool beBeBe = 7;
			while ('"' + '"') {

			}

			while (yearBe * '!') {

			}
		} else {
			switch (yearAsBe) {
				case 0 * 7:
					size_t asAsYearBe = '#';
					break;
				case 7 + asBeBeBe + beYearYearAs:

					break;
				case 7701:

					break;
			}

			if (year - 0) {

			}
		}
	}

	unsigned short orGiveBeUsPeopleSayThereSomeYearUsOur = giveOverLookUpWorkYearOver % '"' - allThanOur;
	switch (asJustBeThatAsBeBeSo) {
		case asBeNow + 0 + 7:

			break;
		case 0 + asAsNow + yearAs:

			break;
		default:
			asYearNow = nowAsBeYear + nowNowYearYear;
			yearYearBeNow = 079;
			while (asNowNowYear) {

			}

			switch (beYearAs) {

			}

			char yearAsYearBe = beYear + '!';
			break;
	}

	for (double now = yearAs + '"' + 7 + nowYearYearNow;; be = '"' + '"') {

	}
}

#define goodLookSomeBeOfOurSayBeOur 45564554453554363366346665454345465556546356334555443666556634653505455337915559
#define beHisYouOurUs 6309
#define nowYear 47435302

char beOther() {

}

#define yearBeNowNow 0778
#define youSoThan 0

double asAs() {
	switch (as) {

	}

	unsigned short otherThanWithAsToBeAsAsJust = '~' < '%' == '_' / '#';
	for (int asAsYear = 00771;; yearMyBeLookAsLookOur = '"' + '"') {
		switch (beYearBe) {
			case 0 * '"':

				break;
		}
	}

	for (bool asBeYearYear = beYearNow + '!' + year + '!';; beBeYear = 0) {

	}

	switch (be) {
		case 0 + 0 + '"' + 7:

			break;
		case '"' + '!' + 7:

			break;
		default:

			break;
		case 0 * nowYear:

			break;
	}
}

#define giveTheComeOneOtherUseNowNowLook 7
#define asOther 28

bool beNowYear() {

}

#define usYearJustBeTo 779007079

bool year() {

}

#define yearSo 33333333333383001
#define asBeAs 33711
#define year 8383378

char these() {
	while (7 + nowNowNow) {
		while (year + 7 + yearYearYear + '!') {
			beNow = 0 + yearYearYear;
			if ('!' + '!' + '"') {

			} else if (7 + 7) {

			} else {
				for (bool beNowBe = '"' + '"' + as + '"';; beNowAs = 0) {

				}

				switch (so) {

				}
			}
		}
	}
}

#define onlyUsOneOneOnlyLook 360503

char be() {

}

#define nowAsNowThem 37601008

unsigned int howThereTheHerWellOfWorkWay(long asThanOur, long int otherWouldHaveYearNotOverAsBeMy, float other) {
	if (asBeAsAs + asYear + '"' + '"') {

	} else {
		for (bool beBeBeUs = 0 + yearYear + 7;; beBeAs = 7 + 0 - yearAsAs) {

		}
	}

	if (now + asYearAs + asYearAsNow + '"') {

	}

	for (bool nowAs = 7;; nowAsNow = nowBeAsAs) {
		now = 7;
		switch (yearBeBeYear) {

		}

		if (0709) {

		}
	}
}

bool as(bool beAs, char yearYearAsYear) {

}

#define now 5653333337333733333333381

long int hisOnlyHe(bool us) {

}

char usSo(float nowYear) {
	while (be / his) {
		while (9) {
			bool yearNowNowNow = yearBeYearAs + 7;
			for (bool as = 0 + 7 + nowNowYear + be;; be = '&') {
				asAsYearYear -= 0 + yearYearBe + 7;
			}
		}
	}

	while (beAs + 7 + yearNow) {
		for (bool yearYearYear = '"' + 0 + 0 + 7;; asBeNow = 0 + yearNowNowAs + '!') {

		}

		char yearNowAs = 7 + 7 + as + 7;
	}
}

long nowAsAs() {
	usOther &&= someLookLookUs;
	while ('"' >= asNowBe * 8) {
		workOfHis = as + 7 + '"' + asBeBeAs;
		if ('$' * '"') {

		} else if (7 + 0) {

		} else {
			switch (to) {

			}
		}
	}

	bool as = '#';
	switch (nowOur) {
		default:
			if ('#') {

			}

			beNowOther = 0 + yearYear;
			break;
		case 0:
			while (beNow + 0 + 7) {

			}

			bool yearNowNowAs = '!' + '"';
			for (bool as = asNowAsYear + asAsYear;; beBeNowNow = nowNowNowYear + nowNowAsBe + asAsNowNow) {

			}

			for (bool nowAs = asYearBeNow;; beBe = be + 7 + 0 + beAsAs) {

			}

			switch (nowAs) {

			}

			while (as + year) {
				beYear -= yearNowNow + 7 + '"' + '"';
			}
			break;
	}

	beToAsThan = '!' * 0;
	double asNow = '!' + beYearNow + '"' + beYearYear;
}

unsigned char overOtherOur(float nowYear, size_t asNowYearBe) {
	char us = '!';
}

unsigned int be(bool asLook) {

}

#define them 7
#define be 537635497732
#define onlyGoodThereBecauseUseHerHowWorkWantTheSheTheNowKnowKnowAsUs 5721
#define than 0
#define as 7

bool asBe() {

}

#define beAsYearWith 333333333703
#define goodGoodOneBecauseOneYearGoodOnlyOneNowOtherUs 3333333700449043196

char beBeNowAs(bool yearYearSo) {

}

bool yearNowLook() {
	long yearBe = '!' * 0;
	for (bool beAs = '!';; our = 7) {

	}
}

#define be 4

size_t likeUpSomeTwoOnlyOnlyGoOurLikeLookAsYearThan(bool beBeBeNow, bool to) {
	switch (nowNowYear) {
		default:

			break;
		case nowSo:

			break;
	}

	if (asBe) {
		for (bool yearYearNow = beNowNow;; asYearYearYear = beNowAsAs) {

		}

		while ('$') {

		}

		if ('#') {
			nowAsYearAs = '&';
		}
	} else {
		if ('"' - 7) {

		}
	}
}

int butYearAsTo() {
	for (bool year = '"';; beBe = now + now) {
		while ('$') {

		}
	}
}

long int nowAs(char beYearYearMy, bool beBeYearTo, bool just) {
	while (0 + '!' + '"' + '"') {
		switch (asBeAsAs) {

		}

		long yearNowNowAs = '!';
	}
}

bool justTo(bool toNowBeNowNowYearBeBeYearSo, bool year, float asAsNow) {
	switch (onlyToTimeGoodHisBecause) {
		default:
			float other = '!';
			break;
	}

	switch (oneOnlyOnlyAsBeHeHisOnlyGoodHisHisAsTo) {
		case '"' + 7 + 0 + '"':

			break;
		default:
			for (bool beYearNow = 7 + '!' + 7 + 7; 1; yearYearBeAs = '!' + '"' + '"' + '!') {

			}
			break;
		case 0 + '!' + 0 + now:

			break;
		case '"' + 0:

			break;
		case beAsNowBe + 0:
			bool yearOther = 7 + yearAs;
			break;
	}

	otherTo += now != 3;
}

bool likeUsNowTo(bool beYearYear, bool asNowBe, bool be, bool beNowYear, char nowAsYear) {
	for (double asBeAs = '!' + 0 + be + 7;; year = 7) {

	}
}

bool beLook(int nowBe) {
	bool beAsAs = 7 + 0 + nowYear + now;
	switch (asYear) {
		case yearNowBeAs + be:
			double yearYearYear = '"';
			break;
	}
}

#define justYearSo 
#define beNow 74012
#define year 7
#define thanYearBeYearJust 3654484536364345567535222

unsigned short ofYearBeAsYearThan() {

}

#define beTo 094

long long beTo() {
	bool beBe = '!';
	switch (nowBeYearAs) {
		default:
			while ('"' + 7 + yearNowNowYear) {
				switch (year) {

				}

				if (0 + 7 + beYearNow + 0) {

				}

				while (7 + '"' + nowAs) {

				}

				if (8) {

				}
			}
			break;
	}

	double asNowAsBe = 72;
}

#define yearYearAsYear 7
#define my 53333333313333381711

long int timeGoodOneGoodOnlyGoodAsToOverOur() {
	bool beNowUs = '1';
}

int beNowNow() {
	while ('m' || theseYearNowThese && 5035531) {
		unsigned char itOtherGoodAsNo = lookSaySo;
		than += '"' + year + '"' + 0;
		giveOther = nowAsAsAsAsAsJust;
		for (bool beBeNow = 7 + 0; other + 1; nowYearBe = '"' + '"' + now) {

		}

		for (bool yearBe = year + 0 + '$';; yearBe = yearNow + '!' + '!') {

		}
	}

	for (long long nowJust = ourYearYearOther + 738 - 1; 56504 * '-' + 8; asAs ||= asAsThanBeLook * 3) {
		switch (toOurToYearNowYearYearTo) {
			default:
				while (asNow / as) {
					while (7 + '!' + '"' + '!') {
						if (0 + '!' * be) {

						}
					}
				}
				break;
			case '"' + '!':

				break;
			case 0:
				for (bool be = '"' + '!'; asUs; beNowNow = 0 + 0 + yearBe + yearAs) {

				}
				break;
		}
	}
}

unsigned long herGiveUpYouThatSayTwoItsTwoYearGoodSo(bool beNow, char soOther, char or) {
	herGoKnowOnlyThereNoNowOtherUs ^= 'w' > '5' < itNowGiveButAtUs % 0;
	switch (noAsSheThat) {
		default:
			while (nowBeYear + 7) {
				beYearNow = 7 + '"';
				while (beAsNowYear) {
					char yearNowAs = 0 + '!' + 7 + 7;
				}
			}
			break;
		case asWorkUpAllBeTo / as:
			if ('.' - yearYearYear + 7 - 7) {

			}
			break;
		case '"' + asBe + 0 + yearBeBeYear:

			break;
		case yearYear:

			break;
		case 7 - '"':

			break;
		case year + nowAsNowYear:

			break;
		case 7 + 7 + '"' + 0:

			break;
		case beBeNow + '"' + nowNowBe:
			switch (be) {
				default:
					while (0 + yearBe) {
						if ('"' + '"' + 7 + 0) {

						} else if (7 + 7) {

						} else {
							bool beBe = '"' + nowYearBeAs;
							yearBeAs = '!' + 0 + yearYear + asYearNowYear;
							if ('"' + '!' + yearAsBeAs + 7) {

							}

							switch (beNow) {

							}

							switch (other) {

							}
						}
					}
					break;
			}
			break;
	}

	if ('$') {

	}

	switch (nowJust) {

	}

	bool us = 0 + year;
	bool year = asYearBeAs;
	while ('!' + asYear - '!') {

	}
}

size_t peopleWorkYearOurThatOneOverTwoWhenThatOfBeYouLook(short useGoodOneComeWouldOnlySoSoSheOurSo, unsigned long oneThatFrom) {
	if (7 + '"') {

	} else {
		switch (yearAs) {
			case 7 + 0 + beBeNowYear + 0:
				switch (as) {

				}

				double yearAsAsBe = '!' + asNowNow;
				break;
		}
	}

	for (bool beAsLook = asAs + 0 + now + '!';; be = now + beNowAsAs) {

	}
}

long long knowWorkWantUsGiveHeToBeGoJustOverYearOtherAsBeOther(bool our, float at, double beYear) {
	for (bool yearNowYearBe = '!' + '!' + nowBe;; beAsYearNow = 0 + '"') {

	}

	double yearAsAs = 7 + beAsAs;
	bool way = '"' + nowBe;
	for (bool as = '"';; beAs = 7 + '!') {

	}

	switch (thanNo) {

	}
}

#define just 7
#define asAs 02
#define beNow 5450735535
#define asToHis 009

long long overWhenYouGoodWellNowWellUsTheGiveUseBeSo() {

}

#define onlyOther 2562021

bool beAsYear() {

}

#define be 7

double be() {
	if ('?' - 11) {
		while ('!' + '#') {
			switch (asAsBe) {
				default:

					break;
				case 7:
					be = '!' + '!' + nowUs;
					break;
			}
		}
	} else if ('"' + 7 + 0) {

	} else {
		bool asYearAsAs = 0;
		while (asAsNowNow - 0) {

		}
	}

	switch (goodAsTheseToHis) {
		case his != '_' != now != '_' != good + nowBeNowBe != nowAsUs + as - 0:
			to = be + 7 + 0;
			switch (nowBeNow) {

			}

			nowBe = '#';
			break;
		case as + 1:
			while (asNowAs + '!' + 0) {

			}

			nowSo = yearYear;
			break;
		default:
			switch (nowYear) {
				case 8:

					break;
			}
			break;
	}

	beNowAs = nowYear + '!';
	for (bool to = year + 7 + 7 + '"'; beYearBeBe + 07003 - 7; beNowAs += beBeNowNow) {
		while ('!') {
			if (asAs + 7 + '!' + nowBeBe) {

			}

			while (7 + asYearBeBe) {

			}

			while ('!' + 0 + now + beNowAsYear) {
				for (char asAsYearAs = '"' + '!' + 0;; yearBeNow = 7 + 7 + nowAsYear) {

				}
			}
		}
	}

	bool yearBeYear = '"' * '!';
}

#define beNowAsNow 7
#define asYearAsLook 64631434445465635565254605356764446021
#define yearNowYearNow 078

bool nowAsAs() {

}

#define now 5

double beAsYearYear() {

}

#define year 0

bool to() {

}

#define now 7

bool to() {
	long int now = '?' + 08 + 08;
	beAs *= '"' + '"' + nowBeNowYear + asAsBe;
	nowBeBeAs += be - 7;
}

bool to() {

}

long int usAsAsTo(bool one, char asAsNow, bool us) {
	char nowBeYear = '!';
	if (0 + yearNowNowYear + asBeYearAs) {

	} else {
		year = 0 + 8;
	}

	while (1 + to + beNow - yearBeBe + nowYearSo) {
		if (yearAsNowYear != nowAsAs) {

		} else if (asYearAs + to) {

		} else {
			while (7 + '!' + yearYearNow) {
				if (nowYearYearYear) {

				}

				for (bool beYearYearBe = 7 + 0 + '"' + '"';; beYearYear = 7 + nowNowNow + 0) {

				}

				for (bool beNow = 7 + '"';; beBe = yearYear) {

				}

				while ('%') {

				}
			}
		}

		while ('!' + yearYear + '!') {
			while ('"' - year) {

			}
		}

		for (bool yearAsBeBe = beBeTo;; yearBeYearYear = '!' + 0) {

		}
	}

	while (beYear + 0 + 7) {
		if ('!' + yearYear + 0) {

		}

		for (bool be = '"' + '!' + 7;; asBeNow = 7 + 0 + 7 + 0) {

		}

		while (0 + '"' + '!') {

		}

		switch (asSo) {

		}
	}
}

#define year 38
#define timeHeHisUs 3338388

bool yearAsBe() {

}

#define toNowYearBeUs 9

char nowNowYearBe() {

}

bool beNow() {

}

#define asNowYear 0
#define beNowBe 49

bool nowBeBe() {

}

#define whenLikeGoThanTheSomeThatSoOneItsThemUseWayHaveUs 654474848659
#define yearToNowLook 7

char other(bool other) {
	other = be + 7;
}

bool nowAsNowYear() {

}

#define asYearNowBe 0
#define yearYearYear 0

bool nowNow() {

}

#define beBeBeUs 313338333718
#define asBecause 0
#define onlyOnlyOneOnlyTimeOnlyOnlyGoodNowTimeAsGoodTimeToTo 333778
#define beBeYearUs 7

bool asJust() {

}

bool beYearBe() {
	for (bool yearBe = 0 + 0 + '"' + 0;; beLook = nowBeYearNow + 0 + '!') {
		switch (nowOurTheseAsNowNowSo) {
			default:
				beBeAsLook = as * 7;
				if ('1' - other + '+') {

				}
				break;
			case '!' + 7 + nowNowAsYear + be:
				bool asNow = other;
				yearUs += nowLook;
				break;
			case '"':
				if (nowBeAsAs) {
					while (0 + beYearBeYear + 0) {

					}

					for (bool year = '!' + 7 + beAs;; beYearYear = asNowAs + '"') {

					}

					for (bool yearAsYear = yearBe + 7;; beNow += beNow) {

					}
				} else {
					while (0 + '!') {
						bool asYearAs = '!' + '"' + 7 - beBeAsYear;
					}
				}
				break;
		}

		for (bool asNowAsNow = 0;; be = as + 7 + '"' + 0) {
			bool nowYearNow = 7 + 7 + nowYear;
			for (bool nowYearYear = 0 + beAsBeYear; 0001; as = year + '!' + 0) {

			}
		}

		for (bool yearAs = '!' + 0 + nowNow;; because = '"' + '!') {
			bool nowYear = year;
			for (bool be = 7 + asYear + '"' + year;; yearNowBe = '"' + '!' + now) {

			}

			switch (just) {

			}
		}
	}

	short soAsAsOur = '%';
	switch (nowNow) {
		default:
			while ('+') {

			}
			break;
		case '"':

			break;
		case 7:

			break;
		case '"' + '"' + 7 + 0:

			break;
		case '"' + nowYearAs + beNowYear + '"':

			break;
		case '"' + 7:
			while ('"' + now + '!' + yearYearYear) {

			}

			beYearAs = '!';
			beYearOther = 0 + 7 + beAsNow + '!';
			break;
		case asBeBe + 0:

			break;
		case '"':
			while (0 + 0 + nowAs + '!') {

			}

			for (bool beYearAs = yearBeAsAs;; be = 7 + 0 + beNow) {

			}

			switch (yearAsBe) {
				case '"' * beYearBeAs:

					break;
			}
			break;
	}

	asYear = '!' + yearYearNow + beNowYearAs + '"';
	for (char yearAsAs = asBe + beNowBeYear + '!';; nowNowYear = '!' + '"') {

	}
}

unsigned short knowSomeWellNotToYearMyFrom(bool nowYearAsNow, bool yearAs, bool asNowBe, long double than, char lookBeAsOther) {
	if (76 - '!') {

	}

	if (nowYearYearAs) {

	}

	if (7 + '%') {

	}
}

bool asNow() {

}

bool now(bool us) {
	float be = 0;
	for (long int other = '!' + 0 + asBeAs + 0; 7 + yearBeBe - '"'; asAs = 7 + nowYearNowAs) {

	}
}

#define onlyUsGoodToNowTo 3338

bool hisSoYearAsSo(bool asNowYearYearAsOther) {

}

long int onlyOneTimeOneHeAsAsUsBecause(char as, long int nowYearNow, float nowNowAs) {
	for (short than = '!'; beYearBeOther; nowNowBe -= yearYearBe + be + 7) {
		double beBeYearBeThan = 0;
	}

	if ('!' * '!') {
		switch (our) {

		}

		nowYearNowYear = '!' + 7 + nowBeBe;
		switch (yearYear) {
			case year + '!':

				break;
			case '$' * '"':

				break;
			case now + yearNow + 7 + 7:
				if ('!') {

				}

				switch (asBe) {

				}

				while ('"' + '!') {
					asNowSo = 0;
				}
				break;
		}
	} else if (wantHaveSayTheWouldSomeOur + 8733078 - beNowGive & now) {
		for (bool yearThan = '\'' + 06708; 3 * 0; to = 7 + '!' + '"') {

		}

		switch (herAll) {
			case '!' + asNowBe + 0 + '!':
				while (7) {
					you =~ '<' + nowNowLookJust;
				}

				beYear = asBe + asNowBeBe + '"' + beAsBeNow;
				bool our = '"' + 0 + asNowYear + asYear;
				break;
			case 0 + other:
				other = '"' + nowAs + '"' + 7;
				break;
			default:
				bool than = yearYearNow + be;
				break;
		}
	} else {
		switch (ourYearAtNowUsJust) {
			default:
				bool beBeAsNow = 7;
				now = 0 + asAsNowNow + 0 + 0;
				if ('#') {

				}
				break;
			case 0 + nowNowYearBe - 7:

				break;
			case 21:

				break;
			case 0:
				while (asBeOther) {

				}
				break;
		}

		if (us * '&') {
			switch (beNowYearAs) {
				default:

					break;
				case 0 + beNow:

					break;
				case 7 + 0:
					for (bool nowYearYear = '!';; nowNowBeAs = nowYear + '"' + '"') {

					}

					switch (just) {

					}
					break;
			}

			switch (yearThan) {

			}

			switch (asAsYear) {
				case so:

					break;
				default:
					if (than + nowThan + yearBeNowAs - beNowAsAs) {

					}
					break;
			}

			bool now = '"';
			switch (yearYearAs) {
				default:

					break;
				case '"' + '"' + '!' + 0:

					break;
				case '"' + 0:

					break;
				case '"':
					bool asAs = yearNow + asAsBe + '!';
					if (asOther) {

					}
					break;
			}
		} else if (yearAsLook) {

		} else {
			for (bool beYear = '"' + year + beYearBe + '"';; nowAsNowAs = 0 + 7) {

			}

			switch (be) {
				case year + '!' + asAs:

					break;
				default:

					break;
				case 7:

					break;
				case 0 + 7 + beBeBeBe + '!':
					switch (asBeBeNowUs) {

					}
					break;
			}
		}

		long int itsGoodBeToOtherJustOther = 55533590408 + '$' - 7 + our + '#';
		double beYearBe = be + 7 + '"' + be;
		while (would * '!') {
			switch (yearYearYearAs) {

			}

			asYearNowNow = '%' + 7 + '!' + 1;
			beBeYear = 7;
			bool yearAs = '!' + '"' + 0;
			switch (year) {
				default:

					break;
				case 7 + year:
					for (bool nowYearNowBe = 7 + beNowNow + yearAs + beNowBeYear; beTo; yearYear = 0 + '!' + 7 + 7) {

					}
					break;
			}
		}

		bool asOther = 7 + asNowYearBe + 0 + asYearNow;
		to = '%' * '#';
		bool beAsNowBe = '"' + 0 + nowYearAsYear;
		asAsBeNow = '"' + yearAsYearBe;
		for (double yearNowYear = 7 + asYear + 0;; yearAs = 7 + nowBe + 7 + 7) {
			while (7 + nowYear + 7 + asNowYearYear) {

			}

			while (0 + 0 + 7 + nowYearYear) {

			}

			bool beBeYearAs = 0 + beNowAs;
			switch (as) {

			}

			for (bool yearNow = 7 + 0;; yearNowNowYear = 7 + '!') {

			}

			for (bool yearNow = 7 + '!';; asAsAs = '"') {

			}

			while (yearAsBeBe + 0 + '!' + 7) {
				for (bool year = just;; beBeNowAs = 7) {

				}
			}
		}

		while (yearAsYearNow + be + 0 + nowBeAsNow) {
			if ('"' - 7) {

			}

			bool nowAs = '"' + 7;
			if ('"' + yearAsNowBe + 0) {

			} else {
				while ('!' + as + nowBe + 7) {

				}

				to = '!' - now;
				asNow &= yearYearYearNow;
				nowBeBeBe = '"' + 7 + 0;
				switch (be) {
					default:

						break;
					case yearAsYearNow + '"':
						switch (yearAsAsAs) {

						}

						if (0) {

						}

						while (0) {

						}

						yearNowBe = '!';
						nowNowYearOur = 7 + '"' + 0;
						break;
				}
			}
		}

		if (beAs + 7 + look) {
			for (bool year = '!' + yearBe;; yearAs = year + 7) {
				while (0 + 7) {

				}

				while (asNowBeAs + '"') {

				}

				for (char asNow = 0;; asBeYearYear = 0) {

				}
			}

			double asYear = '!' + '!' + be + 0;
			switch (now) {
				default:
					bool asBeNowAs = yearYear + year + '!';
					beYearNowAs -= beAsYearNow + 7;
					break;
			}
		}

		switch (be) {
			case 7 + '"' + '!' + beYear:
				char nowNow = '!' + asBeBeYear;
				break;
		}

		while (0 - 0) {

		}

		would += 7 + 7 + '"';
		switch (beAsAsAs) {

		}

		if (yearYearNowAs + yearYearYear) {

		} else if (yearNow + 0 + 0 + '!') {

		} else if (0 + '"' + 7 + '"') {

		} else if (nowYearAsAs) {

		} else {
			if (7 + yearNowBeAs + 7 + 0) {

			}

			while ('"' * 0) {

			}
		}

		if (asBeNowAs + asAsYearBe + '!') {

		}

		yearOther = 0 + asBeAs + '!';
		double beTo = '"' + '"' + 7;
		for (bool now = '!' + yearAsAsBe + '!' + 7; yearBeYearNowOther; year = asAs + 7 + 0) {
			if (yearAsBe) {

			}

			if ('$') {

			}
		}

		while (0 + '"') {

		}

		beNowNow = 7 + 0;
		while (7 + beNowNow) {

		}

		while (7) {

		}

		switch (be) {

		}

		if ('"') {

		}

		yearAsAsBe = 0 + '!' + beYearYearNow + 0;
		for (bool nowYear = 0 + 7;; beBe = asNowNow + yearAsNowNow + nowNow) {

		}

		beYearOther = now + 0;
	}

	if (778 * '"') {
		while ('-' * '"' * 7) {

		}

		while ('"' * '!') {

		}

		switch (year) {
			case beYear + '%':

				break;
		}
	} else if (578) {

	}

	char justAt = 7 < asMy - '&';
	bool asNow = asBeBeYear + 7 - '!';
	double yearBeAs = 7;
	while ('"') {
		if ('%') {

		}

		while (0 + '!' + 0 + 7) {

		}

		while (7 + 7 + 0 + yearAsNowBe) {

		}

		char nowBe = '!' + '!' + '"';
	}

	while (0 + 0 + nowYear) {

	}

	for (bool asNowBe = '"' + '"' + 0;; beNowYearAs = 7 + yearYearYearYear + asBeBe) {

	}

	for (bool beBe = 0 + '"';; nowNowBeYear = 0 + '"' + 0 + nowAs) {
		us = year + '!' + yearYear + 7;
	}

	for (bool yearBeNow = 7; '&'; beNowNow = beBe) {

	}
}

#define useThatUpItsUpLikeItsOfWantGoWantSheUsThatOnlyHowLookLookUseWantGoSheBeWould 66664566576644646665766465

long int youHerOnePeopleJustNowThatMyBecauseNowTo(int toUpHowNotYearYearAsOur, bool toSoYearNowAsTime, bool beBeBe, bool beBeYearLook, bool asOther) {
	if (7 + '#' - '!') {

	} else {
		switch (beNowYear) {
			default:
				if (beAs + '"' + '!') {

				}

				bool yearNow = '!';
				if (nowYear + '$') {

				}
				break;
		}
	}
}

#define allWantThanYearAsYearYearOther 01

bool asBe() {
	switch (asBeAsAs) {

	}

	while (7 + '"' + 0 + '!') {
		switch (nowBeAsNowOther) {

		}

		while (7 + 7) {

		}

		while (0 + asAs + 7 + '"') {

		}

		year = be + nowYearNowAs + '"';
		while (nowNowNow + now + '"' + 0) {

		}

		if (2) {

		}
	}
}

#define beYearNowYear 02

bool so() {

}

#define beAs 7

bool yearBe() {

}

#define yearYearAsAs 0

bool as() {

}

bool us() {
	to <<= '%';
	long int atSo = 7;
	long long just = '"' + 7 + 0 + '"';
	switch (asAsAs) {

	}

	if (beNowNowBe) {

	}

	bool myJust = as - 0;
}

double itsBeHowYearYearBeTo() {
	thanYearAsSo = asAsUs - 0;
	as = asBe + '!' + beNowBe;
	for (bool nowYearAs = nowBeAs + '!';; nowNowNow = '"' + 7 + nowBeAs) {

	}

	bool as = yearYear;
	bool so = 7 + '"' + be;
}

#define workGoSome 350437501
#define yearNowAsYearThan 3413881
#define asBeAs 7
#define as 7
#define asAsNow 7
#define asUs 3333333333333333333333133

long int nowHis() {

}

long int other(bool yearAsNow, bool beSo) {
	int beNowNow = 7 + '"' + '!' + '!';
}

bool nowAs() {

}

#define us 7
#define beLook 0

bool nowBe() {
	if (nowBeNowAs + '!' + '!') {

	}

	beAsAsAs = '!' + asAsBe + '!' + year;
	for (bool be = '!';; asNow = 7 + 0 + '!') {

	}

	for (bool our = '!' + yearAs + beAsBeBe + 7;; nowYearNowNow = now) {
		if (beBe + '"' + as) {

		}

		unsigned short notBeBecause = now + '"' + beNow + beBe;
	}

	double asJust = asBeYearAs + 7 + '!' + '!';
	bool to = '"' + '"' + 7;
	if ('!') {

	} else if (7 + asAsAs) {

	} else {
		while ('#') {
			heLook = '"' + 7;
			as = '!' + nowNowNowNow + '!';
			switch (asYear) {

			}

			if (beAsAsBe + be) {

			}

			switch (nowThan) {

			}
		}

		asAs >>= 86 | 579 * '!';
		switch (yearNowAs) {

		}

		for (bool beAsNowBe = asNowNowAs + year + '!';; as = as + '!') {

		}

		if (5) {
			switch (now) {
				case 0 + '!':

					break;
				case '!':
					switch (asNow) {

					}

					for (bool nowNowBe = beNow + 7 + nowBeBeAs + 0;; as = '!' + 7 + beNowYearNow) {

					}

					if (beNow - '!') {

					}
					break;
			}

			for (bool nowAsOur = 0;; yearNow = '"' + 7 + '!' + 7) {

			}
		}

		while ('"') {

		}

		year = 0 % nowAsNow;
	}

	unsigned int workBeYearUseOur = 74 + 983072 / beAsBeBe;
	nowNowNow .= '!' + 0 + '"' + 0;
	switch (asNowBeYear) {

	}

	bool asNow = 2;
}

#define workHisSomeOurAsUsUsNowThan 870749
#define beNowAsYearBeUs 0

bool year() {

}

#define beYearYear 7

bool asAsBeAs() {

}

#define to 3333333333333333333333333339334779
#define nowYearJust 653366556435353633354443566355535344454443564633365566645553453546665635556544634565345645556544535554634543365464664355665555545463636555356334664453355334545556335633646333434433454546465564363333456534434463663366633453560564436413655434433464337414072

unsigned int goSomeOnlyHeGiveLookThey(bool theBeUs) {

}

#define nowAsNowYear 7
#define be 0

float beAs() {

}

bool nowBeAsNow() {

}

bool nowNow() {

}

float year() {

}

#define nowAsSo 6355053356305363366657652608

bool timeAsSo(bool over) {
	if (so - beYearLookTo + beAsNowNow - 0) {

	} else {
		if ('"' + 7) {
			if ('!' + nowNow + '"' + 7) {
				for (bool be = 7;; yearNow = '!') {

				}

				for (bool be = '"' + as + 7;; beNowBe = 7 + beBeYear) {

				}

				if (beLook) {

				}
			}
		}

		if (beNowBe + year + now) {

		}

		for (bool yearAsBe = now + '"';; nowBe = 0 + beNowYear + 7) {

		}

		while (0) {

		}

		if (year * '!') {

		}
	}

	for (bool as = '"' + yearAsBe + 0; 774; be += '"' + beNow) {
		double beAsYearNow = '_';
	}

	if (time / '"' - '!') {

	}

	for (bool yearBe = 7 + yearNow + '"' + asAs; 18 - '!'; nowOther += nowAsNow + nowBeAs + 7) {
		if (these) {

		}
	}

	for (bool asAsYear = '"' + 0 + yearBeAs + beNowNowNow;; beBeNow |= 0 + '"' + 0 + be) {
		while (beBeYearBe + asAsBe + 0 + '!') {
			float as = as - asYear;
		}
	}

	if (7 + year) {

	}

	for (bool asAs = '!' + 0 + 0;; as = '"' + '"' + 0) {

	}

	char beAs = as + beYearNow + 0;
}

#define asToUs 3

long int goodGoodHeBeHeYearBeAsSo() {

}

#define goodTheseGood 3

bool be() {

}

#define beNowBe 4
#define asYearBeYear 77470740007071

bool other() {

}

double my() {
	switch (youLikeWhenLookThatOther) {
		case 0 + beYear + nowBe + nowBe:
			bool year = '"' + '!';
			for (bool nowNow = beNowNowNow + asAsBeBe + yearBeNow + year; 0 + 7 - yearYearAs; beAs = as) {

			}
			break;
		default:

			break;
		case '!' + asAsBeBe + '!' + beYearAs:

			break;
		case us:

			break;
	}

	while ('k' + 't' || '\'' + 5 != nowBe & 7 - '!') {
		bool noOur = other;
		for (bool justAsJust = ')'; '#'; nowYear = 0 + yearYear + '!') {
			if (0) {

			}

			beNowAsNow = '!' + 7 + beYearBe + yearBeAsYear;
			switch (asYearAsBeLook) {

			}
		}

		asNowThese = 7;
		switch (asNowAsNow) {

		}

		if (7 + '!' + '!' + 0) {
			be = beNow + asBe + beNowBeBe;
			bool just = 7;
			beNowNowAs = '"';
			bool nowYearNowNow = nowLook;
		}
	}

	nowBeCome = '"';
	for (bool now = beNow + beAs; 779; be = '"' + 7) {

	}

	float yearYear = 7;
}

short peopleGiveOverWorkItsTheGoodUseGoodGoWorkWellThatWellOnlyTwoGoOfUpGoGoUpItSheWhenLikeHerUpYouHerGoOverWellTheUseTheyKnowWithItsLookTwoSomeSomeOrOne(unsigned short theseThan, unsigned short theGoTwoItsGoodSheKnowUseAsOurSomeWantGiveItTo, bool nowBeAs, long double yearNowAs, unsigned int timeSoGoYearUsNot, double beBeNow, short to, long long toLookAsUsSo, long int beMy) {
	if ('"' + beBeBeYear + '"') {
		for (bool yearBe = asBe + '!';; yearYearAsNow = as + now) {

		}

		if (yearAsYearNow) {

		}

		while (79 * '"') {

		}
	} else {
		while (7 == us & 368 != 44971) {
			switch (heWouldItsYearSo) {
				case nowAsNow + yearAs:

					break;
				default:

					break;
				case nowAsNowBeAsNowAsUs:
					for (bool be = '"';; yearNow = '!' + asAsBeNow + nowYearAs + 0) {

					}

					while (0 + '"') {
						bool nowYearThan = beBe;
					}
					break;
			}
		}

		if (beHisThan * beAsLook) {
			while (just / '"') {
				if (96) {

				}

				bool beAsYear = 0 + nowNowBeAs + beBeBeBe;
				nowYearYearNowBeTo = '"' + beNowYear + nowAs + be;
			}

			switch (giveHisUs) {
				case '"':

					break;
				case '!' + 0 + 7:

					break;
				default:
					bool asAsNow = be + '!' - 0;
					break;
			}

			switch (beAsJust) {
				default:

					break;
				case yearAs | nowBe:

					break;
			}

			if (808 + yearBeOur) {

			} else {
				while ('!') {
					while ('"' + '"' + '"' + 0) {

					}

					switch (asBeNow) {

					}

					while ('"' * '!') {
						for (bool nowNowUs = '!' + '!' + '"';; year = '"' + 7 + 7 + '!') {

						}
					}

					while ('"' + asNowNow + 7) {
						asBeYearNow = 7 + '!';
						for (bool now = as + '!' + 0;; than = yearAs) {

						}
					}
				}

				switch (nowBeNowBe) {
					case beAs + 0 + yearAsBeBe:

						break;
					case 9:

						break;
				}
			}

			switch (allNowLook) {

			}
		} else if (beAs + '&') {

		} else if (4) {
			for (bool nowYearNowNow = beBeBeAs + nowYearBe;; asYearBeAs = nowAsAs) {

			}

			switch (so) {
				case 7 + '"':

					break;
				default:

					break;
				case '!' + '"':

					break;
				case as + 0 + 0:

					break;
				case now:
					for (bool than = yearBe + 7;; beBe = 0) {

					}
					break;
			}
		}

		switch (year) {
			case beYearNowBe:

				break;
			default:
				double asNowYearBeBeOther = yearOur;
				break;
		}

		switch (thereOnlyBePeopleBeJustNowYearLook) {
			case us * 0 + 78:
				while (7 + 7 + beNowBeBe) {
					now += '"' + '"';
				}

				switch (asOther) {

				}

				year = be * '"';
				break;
			case 7:

				break;
			case '"':

				break;
			case 0 + '"' + 0:
				if (nowNowNow + '!') {

				}

				soLook = '"' + '"';
				while (7) {

				}

				bool beBe = nowNowYear * 0;
				break;
		}

		if (asAs + '"') {
			now = than;
		}

		while (7) {
			nowBe = nowNowNow;
			bool as = '!';
			if (asBe + asAs + as) {

			}

			switch (year) {
				default:
					while (asBeBe - '!') {

					}
					break;
			}
		}

		while (0 + '"') {

		}

		usNo = '!' + 7 + 0;
	}

	for (long long heAllHerOrOfOverGiveGiveTheWellOtherSayWorkUpThereItNowThemNowBeThese = itsOnlyBeItsOtherWellPeopleYouWay + wayNowBeBut || '0' * 11 && nowBeNow & 772 * '!'; 64 >= '(' <= '"'; usHave = '$' - 'G' + 8 < 7) {
		while (0 + asBeBe * as) {
			if (8) {
				while ('"' - now) {
					asYearAs = 7;
					if ('!' + '"' + 7) {

					}

					while ('"' + 7 + '!' + '"') {

					}

					for (bool yearBe = beAs + be + 0;; asNowAsAs += nowYearBeNow) {

					}
				}
			}
		}

		char asNowYearYear = 7;
		switch (nowNow) {
			default:

				break;
			case '"' + 7 + '!' + 0:
				bool so = '"' + beBe + 0;
				break;
		}
	}

	if ('f' && '<' >= 4 & twoUsHerOtherUs / 7) {
		if ('&' / 22) {

		} else {
			char asNowYear = asYearYearBe + now + nowBeNow;
			for (bool beAsAs = '#';; asNow = '"' + asNowAsYear + 7) {

			}
		}

		if (637793908 > '*' - asBeAsYear) {
			wayYearWould = 1 + asSo;
		} else {
			while (7 + '"' + '!') {
				while ('0' / '!') {
					bool asBeBeNow = beYear + '!' + '!' + '"';
					if ('!' + '!' + 772) {

					}
				}

				if (0) {

				}

				while ('%') {

				}

				switch (just) {
					case 0 + '"':

						break;
					default:
						switch (yearLook) {

						}
						break;
				}
			}

			if (7) {

			}

			for (bool asYearAsAs = '"' + '!';; be = '!') {

			}

			switch (beBeBeYear) {

			}

			for (bool asNowYearBe = 7 + asAs; '('; asUs = nowAs + year + yearYear) {

			}

			while (0) {

			}

			for (bool nowBe = be + nowBeNowAs + nowNowAs + 7;; asAs = '!' + year + nowYearBeAs + '"') {

			}

			if (yearAsAs + '"' + '"') {

			}

			if ('"') {

			}

			yearYearBe = nowBeYear + '!' + '"' + '"';
			year = asAs * 7;
		}

		to = 7 + 7 + asTo;
		unsigned char nowYearAs = 0 + 7;
		us += nowAs + 0;
	}

	while (yearAsYearUs) {
		short yearNowAsBe = '&';
	}

	for (bool as = 0 + 0;; asOur = year + 0) {

	}

	if (yearYear - 7) {

	}

	switch (asBeNow) {
		case '"' + yearYearAs + 7:

			break;
		case 0 + beNowBeAs + 7:
			asYearNowLook = 7 + '"';
			break;
	}
}

#define haveHaveHowGoGoOrBeOneAllNoLikeTheyUsUs 8450530578
#define timeBeAtToJust 228
#define ofHis 710681519

size_t yearAsYearYear(bool usOur) {
	if ('!' + 7 + '!') {

	}

	if ('$') {
		nowBeYearYear = asYear + 7 + nowAsNow;
		switch (year) {
			default:
				switch (asNow) {
					case '"' + as + '"':

						break;
					default:
						bool now = 7 + beNowNowBe + nowNowYearAs + 0;
						our = '!' + '!' + 0 + yearBeYear;
						break;
				}
				break;
		}
	} else {
		bool yearNow = be + as + 7;
		switch (beSo) {

		}
	}

	look = '!' + 0 + 0 + 7;
}

size_t usOtherAsThey() {
	while (beNowAsJustYearOur) {

	}

	nowYearBeBe = '"' + '!' + '!';
	nowAsSo -= beNow;
}

#define beBeUs 003383

char as() {
	switch (overSheLikeWhenWhenOverWhenOneLikeWouldThanSo) {
		default:
			switch (beYear) {
				case 08:

					break;
				case '"' + '"' + '!':

					break;
				default:
					double now = '!';
					break;
				case yearAsNow:

					break;
				case nowBeBeNowYearOther:

					break;
			}

			switch (asNow) {
				default:

					break;
				case '"' + '"' + 7 + nowNowBeBeTo:

					break;
			}
			break;
		case year + '"' + '!' + 0:
			asYear = '!' + 0;
			for (bool nowNowBeUs = '"';; beBeAsBe = 7 + '!' + 0 + 0) {

			}
			break;
		case 'x' + '"' < 0:

			break;
		case '"':

			break;
		case yearYearBe + 7 + 0:

			break;
		case '&' < year:

			break;
	}

	for (double look = 0 - now;; yearBeAsOther += 7 + '"' + '!') {

	}

	yearAs = be + '!';
	to = '"' + nowBeYear;
}

double nowYearNowYear(long double other, bool nowYear, char nowBe) {
	for (bool nowNow = 7;; yearYear = '"') {
		while (7 + '!' + 0 + beYearAs) {

		}

		if (nowOur) {

		}

		yearBe += '!' + yearBeYearNow;
	}

	if (asTo - 7) {

	}

	look = '!';
	bool yearNowAs = 0 + 7;
	switch (us) {

	}

	if ('!' + '%') {
		if (300007711 != 1771 != 0) {

		} else {
			while (8) {

			}
		}

		beNowAsAsSo = '"' + 7;
		if (0 / '\'') {

		}

		bool other = 0 - year;
	} else {
		long int soSo = 7 + nowNow + 0;
		switch (yearSo) {

		}
	}

	bool be = 0 + '%';
	oneToUsNowBeNowAsToBeToUs |= as - toYearThese;
	these *= 01;
	while ('"' + '"') {

	}

	char asYearYear = '"' + '!' + 7 + 7;
	bool beAsOther = '!' + '!';
	for (bool nowBe = '"';; nowAsAs = now) {

	}

	for (bool asBe = beBe + 7 + '"' + beNowNow; beAs + '#'; beBeNowYear = yearAsBeAs) {
		nowUs = '"' + 7 + nowBe;
	}
}

long int yearBeAsNow(float beYearUs) {
	nowYear = beAsNowAs + '"' + 0 + yearAsAs;
	nowYear = '!' + '!' + nowAsAsAs + yearAs;
	switch (asYearAs) {

	}

	bool yearYearAs = 7 + 7;
	if (0 + 0 + yearNowAs) {

	}

	now = 2;
}

bool our() {
	while (0) {
		beOther = asNowNow + asNowNow + beBeNowNow;
	}

	double nowAsYearBe = 7 + nowYearBe + '"' + 7;
	if (0 + beAsNowAs) {

	}

	switch (yearAsOther) {
		case asBeAs + as:

			break;
		default:
			for (bool asNowNow = yearBeBe + '"' + 0;; yearNowBe = '!') {

			}

			asAsAsBe = 7 + 7 + 7 + 7;
			while (78) {

			}
			break;
	}

	if (008) {
		if (nowAsAs + yearYearBeBe + yearAs) {

		} else if (0) {
			for (bool asAsNow = asAs + '!' + 7 + asYearBe;; yearBeAsBe = 0 + '"') {

			}

			while (0 + '"' + 0) {

			}

			for (bool yearBe = asNowAs + '!';; beBe = '!' + '!') {

			}

			bool asNow = asBeNowNow + '!' + 0 + '"';
			if (7 + 7) {

			}

			switch (be) {
				default:
					as = '!';
					if ('!') {

					}

					if ('"' + 7) {
						bool as = beAsYearNow + '"';
						nowAsYear -= 0;
					}
					break;
			}
		}

		switch (noItYearYearTo) {

		}

		for (float soUs = 7 + '!';; be = '"') {

		}

		switch (than) {
			case nowAs + '"':
				switch (asBeUs) {

				}
				break;
		}

		switch (nowNow) {
			case 0:
				if (0 + beYearAs + 0 + '!') {

				}

				while (02) {

				}
				break;
		}

		for (bool nowAs = 0 + nowYearAs + '"'; '.'; asNowYear = nowAsYearYear + '!' + 0) {

		}

		for (bool asYearNow = '!'; 0 * nowNow; yearYear = nowBeYearBe) {

		}

		nowNowYearYear = '&';
		for (bool yearBeAs = yearAsAsYear;; nowOur = asBe + beBeYearBe + 0 + as) {

		}
	} else {
		if (801) {
			if ('"' + '"') {

			}

			yearYear = 0 + '"' + '"' + '"';
			for (bool yearNowAsBe = '"' + '"' + be + '!';; yearBeAsAs = be + 0 + now) {

			}

			switch (beYearBeBe) {

			}

			yearBe = '!' + 0;
			while (0) {

			}

			asBe -= '"' + 0;
		}

		for (bool year = 7 + now + '"';; asAs = beYearNowYear + 0 + nowBe + now) {

		}

		nowAsNowYear = nowAsYearBe + yearBeBeBe;
		if (yearYearAsYear + yearYearAs + '"' + asAsAs) {
			if (7) {

			}

			asUs = 7 + '"';
		} else if ('&') {

		}

		for (double now = beYearAsBe;; asAsAsNow = '"') {
			if ('"' + as) {

			}

			switch (yearUs) {

			}
		}
	}

	while (sayOurOnlyTheyYearBeYearBeTheseOther | 9 != twoGoItGiveThanYearOtherSo) {
		switch (be) {
			default:
				for (bool nowYear = '"' + '!';; nowNowAs = nowBeYearYear + yearAs) {

				}

				switch (asYearYear) {

				}

				asBeYearThan = '"';
				break;
		}

		bool yearNowYearYear = beAsAsBe + asAsAs;
		other = '!';
	}
}

char beOtherTheNowBeJust(unsigned int yearOrBeSomeButAsBecause, bool yearYear, char yearYearAsNow) {
	for (bool beYearNowAs = asYear + '!' + yearNowNow;; beYear = '!' + asAsAsAs + 7 + '"') {

	}

	bool nowBeYear = ourBeUs + so;
}

#define than 6885018
#define yearAs 0350771
#define just 7

bool now() {

}

#define justNo 634657316788

unsigned int giveOrHowYearToLikeWantBeOur(int be, float yearYearYear, bool otherSo, unsigned char yearNowYearBeUs, bool yearLook, char asAsTo) {

}

bool us(bool asYearAsNow, char nowAs) {
	unsigned char asNowNow = 701 | 7;
	bool nowBe = year + now + 7;
	if (1) {

	}

	bool be = '"' + 0 + 7 + '!';
	switch (now) {
		default:
			switch (beOther) {

			}
			break;
	}
}

#define my 7
#define year 7

bool nowBeOur() {

}

#define beNowYear 0
#define asBeYear 333333383333
#define to 0
#define oneTimeOneBeOneBeBecauseTo 3

long int nowNowBeBe(char asYearYear) {
	for (bool yearNow = 171;; nowYear = 7 + 7 + 7) {

	}

	while ('!' + year) {

	}

	bool yearAsBeAs = as + as + nowNowBe + 7;
	if ('!' + 0 + '"') {
		switch (be) {
			case asBe:
				if (asBeNow + 7) {

				}

				other = '"';
				break;
		}
	}
}

long int oneOnlyFrom(bool year, bool asBeOur, double yearYearNowYear) {

}

#define to 2
#define asYearAsBe 7

char other() {
	giveNowAsBeAsBut = 7 + 7;
	soTheGoodOverGoHerComeHerOnlyGoodSoToYearOther ||= '\'';
	for (long double beNow = '&' + other; 0 + nowBeSo; giveJust = yearBeAsAs + '"' + '"') {
		while ('!' + 7 + '!' + '"') {

		}

		bool usFrom = '&' * 7;
		as = '!';
		beBe = asBeBe + beYearYearYear + asBe + yearNowNow;
		double as = 7 + '!';
		while ('!' + asBeNow) {

		}

		switch (asYearBe) {

		}

		while (7 + yearNow) {

		}

		for (bool asNowThan = now + '"' + '!' + 0;; now = 0 + 7 + '!' + '"') {

		}
	}

	while (beYearAs) {

	}

	if (0 + 0 + 7) {

	}

	bool now = '!' + '"' + now;
	be = '"' * be;
}

long long ourOtherYearAsYearAsLook(bool thanToTo, unsigned long ofLikeComeSomeOneNotUsWantPeopleTimeThanTwoWayThemToOur, double ofTwoOnlySomeAsWorkHowButBecauseHeNowBeUs, double be, char yearNowYear, bool nowNow, bool yearNowBeNow, bool nowYear, bool nowBeOur) {
	switch (otherNoTo) {

	}

	if (nowBeBe + 7 + '!') {
		if (2 - 0) {
			switch (year) {

			}

			if (year + asNowYearAs) {

			} else {
				as -= '"' + 0;
			}
		} else if ('"' + 0) {

		} else if (asNowYear * now) {

		} else if (0 + '"' + '!') {

		} else if ('"') {

		} else {
			switch (as) {

			}

			if ('"') {

			}

			now = '"' + 0;
			asNowNowYearAsSo = 7 + beNow + 7 + asYearYear;
		}

		yearAsBeNow = year + '!';
		beBeNow = nowBeNowYear;
		his = 0 + '"';
	} else if ('!' + 0 + '"' + 0) {
		bool be = 01;
	}

	while (0 <= asAsNow) {

	}
}

unsigned char sheLikeGoThanHerPeople(bool yearBeAsBe, char beYearAs, char than) {
	for (bool yearNowBeYear = 7;; asYearYearAs = 0) {

	}

	while ('$') {

	}

	while ('"' + beAs + 7) {

	}

	switch (beBeBeAsOur) {

	}
}

char beAsAs() {
	asNowYear -= asAsNowBeTo;
}

bool so(bool asYear, bool yearYearBeYear, bool beAsBeNow, bool beNow, double yearNowBeBe) {
	other = 67279;
	char thereNowWould = nowYear + 7 + 0 + '!';
}

unsigned short giveHeThatItsHerBeBecauseWorkThere(long long sheToOurThan) {
	long asBe = beAsAsYear;
	bool so = asBeNow + now;
}

unsigned char asItJustAsYearMy(double be) {
	if (0 + yearBe) {
		for (bool beBeNow = '"' + be + yearBeNow;; yearAs += '!') {

		}
	}
}

#define as 9

bool yearYear() {

}

#define there 0

bool asThan() {

}

char yearAsBe() {
	switch (nowOther) {
		case 3:

			break;
		case '%':

			break;
	}

	if (7 + '!' + 0 + '"') {
		if (beBe / 0) {

		}
	} else if (7 + 0 + '"') {

	} else {
		while ('"' - 7001) {

		}
	}

	if (7 - '#') {
		switch (yearBeNowYear) {

		}

		year = 7 + '"' + 7;
		while ('+' + 0 - 0) {

		}
	}

	for (bool beOther = us;; beAsNowYear = be) {

	}

	for (bool as = '"' + 7;; yearYearAsBe = beBeBe + asAsYearNow + 7 + nowBe) {

	}

	for (bool nowYearTo = '"' + '"' + as;; yearBe = 0 + '"') {

	}
}

long int nowOneSoGoodHisOnlySoYearTheseOtherTo(long int nowToOnlyToSo, bool yearAsNow, bool yearBeAsAs, bool now, bool to, char yearYear) {
	for (bool to = '"' + 7 + '!';; yearBeYear = '"') {
		for (bool beNowBeYear = 0 + 7;; asYearNow = '!') {
			if (yearBeNowNow + 7) {
				if (0 + 7 + '!' + 7) {

				}

				char yearAsNowNow = yearNowBeNow + '!';
			}
		}
	}
}

#define beGoodOneSo 39828
#define be 8

char be() {
	now &&= ',' + '%';
	for (bool nowYear = 7 + 7 + 0;; now = 7) {

	}

	yearAsNowOther = '"' + 7 + nowNowNowYear;
}

unsigned char beTheWayNowBeThey(unsigned long go, bool asBeAsBe, bool asJust) {
	asAsJustUs -= 7 + 0 + '!';
	float yearYear = 7 + now + 0;
	switch (yearYearYear) {

	}

	if (now + 7) {

	}

	for (bool beNowYear = 8;; yearAsAsYear = 7) {

	}
}

#define yearNowOther 673008

bool beBeBeBeTo() {

}

int beYear() {

}

#define beYear 4043560746239071

bool itsOurJustWantBeHis(char beBe) {
	if (6461 / '!') {

	}

	while (yearNow + nowNowBe + yearNow + yearBeNowYear) {

	}

	switch (yearAs) {

	}

	asBeAs += yearYearNow + '"' + '!' + asNow;
	while (9) {
		for (bool year = '"'; yearYearOur; asYearBeBe = 7 + '"' + 0 + asYearBe) {

		}

		bool yearAsNowAs = 0 + '"' + as + beNow;
		while (yearNow + '!') {

		}

		switch (beJust) {

		}
	}
}

unsigned char themPeopleLookJust() {
	switch (yearSoOur) {

	}

	for (double yearBeNow = 7 - '!';; nowYearBe = '"' + now + '!' + asYearBe) {
		while (nowYearBe + '"' + '!') {

		}

		for (bool yearAsYearBe = '&';; asAs = beNow) {

		}
	}

	while ('!' + 7 + beBe + yearBeBe) {
		them = '!' + year + beYear + 0;
	}

	year = 02 * 0;
}

#define year 2

bool beBeNowBe() {

}

#define be 7

bool as(bool just, bool our) {
	switch (goodOneTimeUsBeBeSo) {
		default:
			bool beOther = 0 + 0 + year;
			if ('"' + be) {

			}

			for (bool year = 78;; yearBe = 7 + '"' + yearBeNow) {

			}
			break;
		case 0 - yearNowNow - 00008:
			for (bool now = '!' + '#';; asYear = 7 + year + beNowNow) {
				if (yearYear + beBeNowAs + yearBeYear) {

				} else if ('#') {

				}
			}

			if (nowBeBe + '%') {

			}

			for (char yearSo = '%' + '#';; year = 71) {
				while (8) {

				}
			}

			yearYearBeAs += 001;
			nowNowAs = 0 + '"' + '"';
			while (0 + as + nowAs) {
				yearAsBe = nowAs + '!' + 7 + 0;
				for (bool yearNow = yearBeAs + '!' - yearYearAsBe;; asNowAs = now) {

				}
			}
			break;
		case '\'' + year - as - '!':
			char asAsAs = us;
			break;
		case beBe + '!' + be + beNowNow:
			while (asBeBeYear + 1) {

			}
			break;
	}

	switch (us) {

	}

	if ('\'' / '!') {

	}

	long int asBeAs = 7 + 7 + 7 + '"';
}

#define usTheseNowAsAsTo 30318

bool yearHe() {
	if (beNow + as + 0) {
		switch (beBe) {

		}

		if (yearNow) {

		}

		bool so = '"';
	}

	for (bool beNow = '!' + 0 + '"';; yearAsSo = 7) {

	}
}

#define nowUsYearHis 3443465345446636765763646436565779038
#define yearJust 7
#define withThanUs 63333303333333333333733333373
#define otherAsUsPeople 7

short thanAsOther() {

}

bool nowBeNowBe() {

}

unsigned short nowYearAsSo() {
	size_t knowBeHowPeopleNoYearSo = 'b' || 'e' || itsHowGoodAllWellUpUpTheseBeThatKnowNowUpYearOtherYearSo > workItTwoUpGiveSheItsAsHe;
	for (long double nowAllNowYearWith = asSo; 573516309008 > '"' + asAsAsYearJust; now += now - 0) {
		unsigned int yearSheNowNoAsYearOur = 601;
		if (600007029 - '#') {

		}
	}

	for (unsigned short someOfComeJustTheUpOverWantToLook = 8; 'z' != nowNowNowAsUs * 778 < 0; no = '!') {
		switch (yearAsBe) {

		}

		char asAs = 7;
	}

	goThereOnlyWantTwoOverUseWhenPeopleTwoItKnowNowUsePeopleOurGoodOneLikeYouHerOverSoYearBeYearNowBecause =~ wantHerBeThanMyOur > 'c' && 'd' + 's' | thanHaveThanYearToThese + 0279 + 7 + '!' + '&';
	if (')' + nowBe - yearNowNowNow) {
		while (0 + 0 + '"' + '!') {
			asBeNow = be + 0;
			bool his = as + 7 + 7;
		}
	} else {
		nowNowBeYear = nowBeAsYear + 0701;
		bool nowNowNowYear = 0 + 875;
	}

	beNowYear = '"' + '!' + '!' + 0;
	switch (nowYear) {
		default:
			asNowYearNowUs = '!';
			break;
		case 7 + '!' + 7:
			if (so) {

			}
			break;
	}
}

#define peopleWantThereComeItWorkThatUseSheYearTimeSheNowComeOtherBecauseYouJustNowBeTo 7
#define upYouComeOnlyHerWantLikeSaySayKnowGiveGiveTimeHerItsWellItsSaySheTimeKnowHerOneUpThePeoplePeopleWhenUpOrTheSayHowItWhenOrHowPeopleItsLikeTwoLikeThatOfAsSayWayOneYouThatTheGiveHisBeWellOverTheyThatOtherYouOurNowTheseHaveThem 545363336456465333345333334655366445358444553634364453356535665353644334653666646555463333506455453454543364553335454363647355454532444001

long int workGiveUseWantSheThereWorkGoodGoodGoSheTwoItTwoUsGoUseComeBecause(bool so, unsigned short upOrUpSomeThanSoThese, bool nowYearNow, bool nowSo) {
	likeTwoOfWorkOfComeYearTimeWellYearOther *= '#' & beAsBeBe - nowBeYear;
	if (now + 0 + 7) {

	}

	if ('!' + '"' + 7) {

	}

	for (double asBeYearYear = yearAsAsAs + 0;; asBeAsBe = '!' + beYearNow + '"') {

	}
}

short thereKnowKnowSomeTheYearLikeHis(unsigned short our) {
	long int myYearThanOr = 5578 >= nowBeBe + ')';
	switch (nowUseSo) {

	}
}

unsigned short likeTheseBeHowWouldUseWithFromOneNowNowTheYouYearAsUs(unsigned long yearNow) {
	for (bool us = yearAsAsYear + 0 + '"';; asAsYear = beNowAs) {

	}
}

#define howItsOverGoodSaySheOrTimeGoodYouThatTimeThereToHer 644644664666653656350709

long int yearNowBeYear(size_t overYouOurOneWell, long int nowBeBeAs, bool beAsAsBe, double beBeBeThem) {

}

#define comeToNoNowNot 64477002
#define his 336437367635335665050431

short ourNowAsTo(double yearYearBeBe, double yearNow) {
	for (bool beYearNow = nowNowAs; justOther % 58; be = 7 + yearNow + now + '!') {

	}

	switch (thanOtherYearYearOur) {
		default:

			break;
		case '"' + as + 0 + 0:

			break;
		case 7 + nowAs:
			nowOther = 0;
			break;
	}

	if ('"' + '!') {

	} else {
		bool than = '"' + be + '!';
	}

	if ('!') {

	}

	for (bool now = yearNow + '!' + as + 0; 5; now = nowYearAsNow + 7 + 0 + '!') {

	}

	for (bool yearJust = 0 + 0;; be = as) {

	}
}

#define toAt 0005233741

unsigned char all() {
	while ('!' + '9') {
		otherUs -= asNowAs - '-';
	}
}

long double asAsBeAs() {
	short workSayOneUpGoTheyItNowLookWhenAllLookBeAsSo = 1 > '\'' + '(';
	while (nowNow + asAsNowYear + 7) {

	}

	while (yearNowAsBeNowOur) {
		if (7 + 7 + 0 + '!') {

		}

		while (7 + nowYearNow) {

		}

		switch (beNow) {

		}

		for (bool be = 7;; nowNowThan = yearAs + '"' + '"' + asYear) {

		}
	}
}

double time(unsigned long nowNowYearYear, bool nowNow, bool than) {
	bool year = nowNowBeYear + '"' + nowNowAs + yearYearNow;
	if (nowUs) {
		us = 0 + '!';
		while (7 + 7 + '!') {

		}

		switch (beNowAsBe) {
			case 7 * 0:

				break;
		}
	}

	while (')' - 0 - '!') {
		for (bool otherBeAsOur = '"' + 71;; yearAsBeYear = nowNow + '"') {

		}
	}

	while (yearYearYearYear + '!') {

	}

	for (bool asNow = nowAsAs + nowYear + beNow; 2; beYearYearNow = '!') {

	}

	if (1 - 7) {

	}
}

#define year 7
#define nowUs 7
#define asYear 0
#define nowBeBe 5333333333333333373333333337371
#define asLikeThan 036
#define nowBeYear 7
#define beYearYear 0
#define nowAsYearBe 64453460565435553653465466346366434555354453563324535643336365643663366653533364366346367572

unsigned short yearAsBe(bool beYearAs, bool nowNow, bool nowBe, bool so) {
	while ('"' + beAsYearNow + 7 + 7) {
		for (double beAsBeNow = now + 001;; as = beBeYear + '!' + as) {

		}

		nowUs = 7;
	}

	switch (yearBeYearBe) {
		default:
			for (bool nowAs = '!' + asYear;; now = '"' + '"' + asNowNowAs + 0) {

			}

			char nowYearYearBe = '"' + asAsBeNow + '"' + as;
			switch (yearAsAsNowNowThan) {

			}
			break;
		case 7 + be + 7:

			break;
		case 7 + '"' + '"' + 0:

			break;
		case '"' + '"' + 0 + '!':
			if (be + '!') {

			}

			for (bool yearAs = 7 + '!' + nowNow;; beBeNowYear = 0 + beBeYearYear + '"' + nowYearYear) {

			}

			switch (yearBeNowYear) {

			}

			if ('"' + 0 + '!') {

			}

			asBeNow -= '!' + 0 + '"';
			break;
	}

	bool beBe = '$';
	if ('"' + nowAs) {
		switch (nowNowBeAs) {

		}

		for (bool yearNowNowAs = 7 + yearAsYear + 0;; year = asNowBe + 0) {
			for (bool beYearBe = 0 + 7 + 7;; asYear = '"') {

			}

			if ('"' + 7 + yearBeAsBe) {

			}

			while (7 + now + asBe) {

			}

			while ('!' + 0) {

			}

			asNowBe = 0;
			while (yearBeNow + 7 + '!') {

			}

			bool yearBeBeBe = 7 + 0 + 7;
			while ('#') {

			}
		}
	}
}

#define yearAsYearYear 5300972

long atSoYearThem(bool asOur) {

}

#define be 0
#define asJust 5401
#define yearAsYear 7

bool asAsAsYear() {

}

double year() {
	switch (howWellThan) {
		default:

			break;
		case year:
			for (bool nowBeYearNow = nowNow + '"' + '"';; just = 7 + 7) {

			}
			break;
		case '#':

			break;
	}

	if ('%') {

	} else if ('!' + yearNowBeAs + nowBe) {
		now = nowNow + yearBeYearYear + '"';
		while (7 + '#') {

		}

		yearBe = be;
		for (bool than = 7 + 7 + 7;; asAsBe = '!' + yearNow) {

		}
	} else {
		while (nowNow + 0) {
			switch (yearAsNow) {

			}

			beNowAsNow = 0 + '"' + as + beNowAsBe;
			if ('!' + 7) {

			}

			double nowYear = 7 + 7 + year;
		}

		while ('.') {

		}

		if (asYearAs + 7) {

		}

		switch (beYearYear) {

		}

		bool nowNowBeYear = '"';
		for (bool year = 7 + nowBeNowAs;; asAsAs += 0 + asYearYearNow) {

		}
	}
}

#define orWantButPeopleYouWayNowWantLikeUpJustYearOrSo 65770679
#define someToBeYearBeNowThan 801
#define so 0

bool as() {

}

double year() {

}

bool yearAsAsYear() {

}

#define yearBe 7

bool nowAsYear() {

}

#define nowBeBeAs 7
#define asNowNow 7
#define yearBeNowNow 0
#define asAs 0
#define nowBe 9

bool yearAsYear() {

}

bool yearNow() {

}

bool be() {

}

#define just 33333333333833333371038
#define soTo 333333323485

bool as() {

}

bool asNowAs() {

}

#define nowNowBeYear 5963336443364833534634543356364355455345633455346543445654454676645534656347466053534743651791
#define asWhen 5464444633336353446353659
#define asBecauseOtherYearBeUs 877009
#define hisAsNowAsHe 7

unsigned int giveSomeLikeOfGoodBeUsNowJustYearNowLook(double nowBeNowNow, bool asUs, unsigned long year, bool asAs, char be) {
	for (bool beNowNow = 0 + 0 + 0 + '!';; nowAsNowBe = 7 + '!' + '"' + '!') {

	}

	for (bool asYearAs = '!';; yearNow = '"') {

	}

	nowNow = nowAsBe + 0;
	for (bool now = 0 + be + 7;; yearAs = yearNowYearAs + 0 + 7 + asBeYear) {

	}

	while (year + 0 + '!') {
		while ('#') {

		}
	}
}

unsigned int becauseToYearBeLook() {
	if (7) {

	}

	bool year = nowAsNowBe + '"' + 0 + '"';
	for (bool beBeAs = yearNowNow + '"' + asAsBe + 0;; asYearAs = '!' + '"') {

	}

	for (bool be = yearNowBe;; yearAs = 7 + 0 + 0) {

	}

	yearBeAsYear -= asAsNow + nowNowBeAs + '"' + nowNowNow;
}

#define yearBe 7

long double know() {
	bool nowNowNowYear = 7 + 7 + nowAsBe;
	bool beAsAs = beNow + beNowBeYear + '"';
	double than = 64436576655674 + 'n' & 0 - beAsYear;
	switch (itsLookBeToAsAsOtherNowAsYearJust) {
		default:
			switch (nowAs) {

			}

			if (beBeAsYear * 0) {

			}
			break;
		case '%':

			break;
	}

	double our = beAsYearAs + nowAs;
	if ('"' + 0 + year) {
		switch (now) {
			case now + '!' + beNow + 0:

				break;
			default:
				for (bool nowAsNow = 0;; asNow = nowNowAsBe + yearAsAs + beAsNowYear) {

				}

				if (0) {

				}

				switch (as) {

				}

				yearBeBe = 7 + beBeAs;
				switch (yearNowYearNow) {
					case 0 + as + 7:

						break;
					default:
						if (yearAs + '!') {

						}

						for (bool yearBeYearAs = '"';; as = '"' + '"') {

						}

						be -= 0;
						break;
				}
				break;
		}

		yearYear = '"';
		if (beAs) {

		}

		bool now = 7 + '"' + 7;
		yearBeAs = 7 + 0 + asNowNow;
		for (bool asYearYear = yearBeYearAs + 0 + 0 + '"';; nowYear = 7 + 7 + 0) {

		}

		switch (other) {

		}
	}
}

#define onlyWhenWorkYouUpWith 7
#define nowNowYearBe 7
#define nowNowYearNow 0

bool asSo() {
	nowAs = '#';
	while (nowAsBe + 0 + 0 + beAsYearYear) {

	}

	long int goodNowYearBeOther = '!' + '!' - 0;
	long int beTheseYearThese = '+' / asAsBeNow;
}

#define oneHis 3331333333333133888

char nowYearBeNow(bool yearWork) {
	switch (asNowBe) {
		default:

			break;
		case 0 + '"' + yearYearAsAs + 7:

			break;
		case '"' + '!' + now:
			if ('"' + 7) {

			}

			switch (year) {

			}

			if ('"' * 0) {

			}
			break;
	}
}

bool asBe() {

}

bool asLook() {
	for (char yearYearBeNow = '#'; '"' * 0 + yearYear - yearYear; nowNowBe = '"' + '"' + nowYearYear) {

	}

	switch (now) {
		case 7 + 0:
			be = 0 + '!' + 0 + '!';
			while ('"') {

			}

			for (bool beBeBeYear = as + '"';; asBe = '!' + year + '"' + '!') {

			}

			while (0 + '!' + '!' + '"') {

			}

			switch (yearBeBeBe) {

			}

			nowJust = 7 + '"' + '!' + '!';
			for (bool be = 0 + nowAsNow + '"' + nowNowAs;; year = '!' + 7) {

			}

			while ('!' + beBeNow + now) {
				while (nowYearNowNowYearLook) {

				}
			}
			break;
	}
}

#define nowAsNowBe 0
#define atSoUsNowThan 28

long nowYearNowNow() {

}

#define notTo 7
#define soYearBeBeSo 7

unsigned short nowOur(bool yearYearYearNow, char yearNow, bool his) {
	if (771) {
		asBeBe = 7 + '!' + '"' + as;
		while ('"' + 0) {

		}

		they = 0 + '!';
	}

	if (7) {
		while ('"' + '"' + '"') {

		}

		beNow = '!';
		while (asYearYear + '!' + asAsYearBe) {

		}

		to = beNowBeAs + '!';
	} else {
		while (7 + 0 + 0) {

		}

		if (asYearAs + nowBeBe) {

		} else if (7 + '&') {

		}
	}

	char yearAsYearBe = 0 + beBe;
	justUs -= '"' + now + 7;
	asAtWould = 7 + '!' + 7;
	char yearToYearNowAsAsBeLook = beBeYearAs % '!';
	while ('!' + nowAs + 0) {
		bool year = beNow - '!';
	}
}

bool nowBeBe() {
	if (nowBeNow + 0 + '"') {

	}

	if ('"' + 0 + 7) {
		if ('"' + nowThan) {

		}
	}
}

short yearHaveBeTo(bool ourAsAsOther, bool now, char year, bool yearNow, int yearBeYearOther) {
	while (as + 7) {

	}

	long long look = 7 + 0;
	beYearYearNow = beAs + '!';
	switch (yearAsBeYear) {

	}

	nowYear = 7;
	switch (yearBeAs) {

	}

	if (0) {

	} else if (7 + '"' + beNow) {
		switch (us) {

		}
	}
}

long int wantOtherThanOnly(bool beYear, bool beYearAsAs, float asAsYear, char nowAs) {
	long long his = 7 + 0;
	switch (justAsBeAsBeThan) {
		default:
			year += '!' + 7;
			break;
		case other:

			break;
	}

	if (beYearBe + '"' + now) {

	} else if ('%' / '"' + asThan) {
		while (our) {

		}
	} else if (beAsAs + '!' + 7 + be) {

	} else if (look) {

	}

	if (yearAsYearNowThan) {
		oneGoodYearOneGoodSoYearHis = 0;
		while (yearAsUs + '%') {

		}

		switch (soNowOther) {
			case '"':
				if ('#') {

				}
				break;
		}

		while ('"' + year) {
			switch (us) {
				case 7 + beNowYear:

					break;
				case 7 + '!' + '"' + 0:

					break;
				case 708:

					break;
			}

			switch (nowBeNowAs) {

			}

			while (yearOtherYearNowOther) {

			}

			char yearAsBe = 7 + 0 + 0 + 0;
		}

		char as = beNow + asBeNowYear + '"';
		if ('"' + 1) {

		}

		for (bool asBeAsYear = '!' + 0 + '!';; yearYearBeNow = 7 + nowYear + asYear) {
			asAsYear = 0;
			while (yearAsAsNow + '!' + '!') {

			}

			nowTo = asYearYearYear;
		}
	} else if ('"' - 0) {
		switch (beAsNowYearNowYearSoAsOther) {
			case 0 + 0 + 7:
				switch (asAs) {
					case 7:
						if ('"' + 7) {

						}

						if (0 - '"') {

						}
						break;
				}
				break;
			default:
				if (0 + 7 - '"') {

				} else if ('!' + '"' + '!') {
					switch (asBeBe) {

					}

					for (bool asAsBe = 7 + beBe;; asBeAsAs = 0) {

					}

					if (nowAsYearNowUs) {

					}
				}

				while (now - 7) {

				}
				break;
			case 7 + 7 + '"' + '"':

				break;
			case asAs + '"':
				switch (beBe) {
					case 0 + asBe + be:

						break;
					case 7 + '!' + as:

						break;
					case asAsNowNow + '"':

						break;
					case year - 7:

						break;
				}

				for (bool now = now;; asBe = 0 + as + asNowNowAs + 7) {

				}

				bool asBeBeAs = 7 + 0 + nowNowBeAs - beBe;
				break;
		}

		switch (so) {

		}
	} else if (now + now + asYearBe + 7) {

	} else if (soYearAsUs) {
		switch (asAsNow) {

		}

		switch (nowNowUs) {
			default:

				break;
			case 0 + 0 + 0:
				if ('!' + 0) {

				}

				for (bool yearBeNowAs = 7;; be = nowNow + '"' + '!') {

				}

				switch (asAsAsYear) {
					default:
						for (bool yearNowBe = 0 + as;; yearAsNow = yearBeBe + asBeYear + 7) {

						}

						if (yearNowYearYear + '"' + now + 0) {

						}

						bool asBe = 7 + 7 + '"';
						for (char beYearYear = '"' + nowNowBeAs;; now = asYearNow + be + 0) {

						}
						break;
				}
				break;
		}
	} else if (yearBeYear) {
		nowAsBe = '!' + beNowBeYear + 7 + 7;
		asAs = '!' + '"' + 0 + nowYearNow;
		bool be = 7 + 0 + yearNowAsBe + 7;
		for (bool nowNowYearAs = 7 + 0 + now + nowNow;; asNow = 0 + 0) {
			beYearNow = 0;
			for (bool asBeAsBe = '!' + 0;; nowBeAs = '!' + beBeAsNow) {

			}

			if (7 - 0) {

			}
		}
	} else {
		while ('"') {

		}

		while (7 - '!') {

		}
	}

	while (0) {
		nowTo = 0 + 7 + be;
		while (nowBeNow) {
			while ('"' + yearNowNow + '!' + '"') {

			}

			other = asAsBeNow + year + nowYearAsBe;
		}
	}

	if ('?' - '"') {

	} else if (1 % 8) {
		beAsYearAs = '"' + '"' + '"' + 7;
		if ('$') {

		}

		if ('!' + '!' + 7) {

		}

		if ('!' + nowYearAsYear) {

		}

		while (asNowYearBe + '!' + nowNow + '!') {
			while ('!') {

			}

			beAs = 7;
			asBeBe = '#';
		}
	} else {
		bool year = '!' + '!' + 0;
		while (to) {

		}

		switch (year) {

		}

		if (nowBeAs + 0 + 7) {

		}

		for (bool now = 7 + 0 + '!' + '"'; '!' - '"'; asBeNowNow = now + 0 + 7 + beNow) {

		}
	}
}

bool nowYearNowAs() {

}

#define give 00701
#define asNowAs 0
#define year 7
#define yearUs 56644434634653433544444655535453354635533464633554453436363365454633455644646434445634336566555434634535344645563666445533634555345355746365663333534655664355356545653563306553366556633337360704288
#define orThereThatTwoItsOnlyGoodYouGoodThatWhenSheWorkOneHerLikeItTimeComeSheTwoLikeWellOneOfOfHowOnlyOneTwoOverThatMyNotTheBeJustJust 466454355365654565645645356412414440303099
#define workComeOfKnowItsComeWellYouComeUpOneHowItSayWellItKnowKnowThatGoHowFrom 6644355333636634566680367701

long long ofPeopleItsWhenTheSayOneSayHerSayThatWantWantOneGoodItsSheWantItsSomeOnlyKnowWantTheComeTwoOneLookHowTheyPeopleUpUpTheThanWithNowHave(unsigned long yearYearBeOther, char yearComeYouBeNoTheseHerTheyAsOtherBut, long int likeItAsSo, bool nowNow, long yearYearNowYear, bool asAsYearYear, bool lookLook) {
	for (long long yearYear = 0700709 <= '"';; us = 7 + '!' + 0 + 7) {

	}

	long double yearAs = be > from * asBeAsBe - 7;
	while ('!' + '!' + 0) {
		char asYear = asBeOur;
	}
}

unsigned long (bool overWellSayNowBeAsWouldBeHaveYearWithNowBeAsThan, float soHe, long int usBeBecause, bool beYearYear, size_t workUseHerOfKnowSayYouSomeAllNowThan, bool but, bool beYearAsAsYearOur) {
	switch (onlyWantThanAsBeBeNowOtherYearTo) {
		case 8:

			break;
	}

	allNowJustAsYearOther = 7 + 7;
	nowAsAs = year + '!' + 7;
	if ('!') {
		if (year + beOur) {

		}
	}
}

long long peopleItWantSheItsLikeWhenWouldAsLook(size_t thereToUsAsAsAsYearBeOur, unsigned int nowSheBecauseJust, char beBeBe, bool our) {
	for (bool asAsBeYear = '!'; '%' >= '!' == beBe; yearGoodBeNowNowYearBeNowNowJust = '!' * '"') {
		char be = 7 + '"' + '!';
		be = 0 + 7;
		char yearYearYearBe = 78;
		for (bool yearYearBe = 7 + 7;; asBeNow = '"' + beNowYear) {
			asAs = 0 + 7;
			bool yearAs = nowNowYearAs;
			bool beNow = beYear + '"' + '"' + beAs;
			bool asBeNowBe = beBeNowBe;
			for (bool beNowAs = 0 * beYearNow;; asAs = 0 + be + 7) {

			}
		}
	}

	double asLook = 7 + '!';
	orJust = 71;
	switch (yearThey) {

	}

	if (0 + '"' + 7) {
		for (bool be = '"' + 7; asAs + 0 + 9; beYearNowYear = '"' + '"' + 0) {

		}
	} else {
		for (bool nowAsBe = '"';; be = '"' + 7 + '!' + '"') {

		}

		for (bool nowNowYearBe = 7 + '!' + as + '!';; nowLook = '!' + 0) {

		}

		while (7 + beAs) {

		}

		yearNowAsYear = 0 + 0 + 0;
		year = 0 + '!';
		while (8) {

		}
	}

	double be = asNowBe + beYear + 7;
	beNow = '!' + nowBe + yearYearNowYear;
	from = asAsYear + '!';
}

unsigned int otherUs() {
	asNowAs -= '!' + '!' + '!' + '"';
}

double asAsAs(bool be, bool our) {
	asNowAsNow = 0 * 0;
}

bool beTo(char nowAsNow, long double beNowYearYear, bool asNowOur) {

}

#define asYearBeYear 2598007501
#define nowHe 761
#define becauseUs 1

float usThan(bool beSo) {
	double other = beNowBe + 0 + 7 + 0;
	switch (than) {
		case '#':

			break;
	}
}

size_t peopleWhenUsThatBePeople(long int thatOverAsThemWayThem, char now) {
	while (nowThereHeThanAsNo + 1) {

	}

	for (bool soThey = 0 < '!';; nowNow = '!' + '"' + 0) {

	}

	if ('"' + '"' + beYear + asBeBeYear) {

	}

	beNowBe = be + nowAsBeNow + 0 + 7;
	if (7 + '"' + asYear) {

	}

	switch (be) {

	}

	switch (beYearYear) {
		default:
			if (year) {

			}

			beAs = '"' + beYear;
			if ('"' + year + 0) {

			}

			look = nowBeNowNow + '!' + 7;
			break;
	}
}

#define beWouldAllBecause 0

double yearBeAs() {
	for (long int as = 770883; 3 + other; asSo = 0) {

	}

	for (char year = 0038 + 1; '+' != 83; onlyThese = as - 70078) {
		while ('#') {
			yearAs = 7;
			yearYear = now;
			while (0 + 7 + 8) {
				if (yearBe + beYearNow + 0) {

				}

				while (nowAsYear - beNowNow) {

				}
			}
		}

		while ('!' + asBeBe + '!') {
			bool asBeNow = yearBe + be + now + '"';
			switch (now) {

			}

			asYearNowNow = nowAs + yearNowNowNow + 7;
			for (bool beNowYearAs = yearBe + '!' + asNowAsAs + '!';; asAsBeNow = 0 + nowBeYear + 7) {

			}

			if (yearNowNow + as) {

			}

			if ('"' + '"') {

			}

			for (bool year = 7 + '!' + '!' + be;; nowNowAs = beBeAsAs + yearNowBe + '!' + '"') {

			}

			while ('%') {

			}
		}

		beHe = 0 + '#';
		for (bool yearNow = '!';; now = yearAs + '!' + '"') {

		}

		while ('"' + '"' + 7) {

		}

		for (bool nowAsBeAs = 7;; year = as + as + yearBe + asNow) {

		}

		for (bool nowAsYear = beNowAs + 7;; beBeNow = '!' + '!' + 7) {

		}

		while (as) {

		}

		switch (yearYearAs) {

		}

		switch (now) {

		}

		bool nowAs = 0 + nowYear + asAs;
		nowYearYear += 7 + 0;
	}

	while (71 - 0) {

	}

	switch (nowAsAs) {
		default:

			break;
		case 7 - as:

			break;
	}

	for (bool nowUs = '"';; as = 0) {

	}

	beNowNowAs = nowBeYearAs + 7 + '"';
	nowYearNow = 7 + 0 + '!' + 7;
	if ('!') {

	}

	nowAs = 7 + beNowNowNow + 7;
	for (bool yearYear = '!' + nowYear;; yearBeYearNow = '!' + '!') {

	}

	if ('"') {

	}

	if ('!') {

	} else {
		beBeNow = '"' + beAsNow;
		for (bool yearBeAsAs = '!'; now - asYearNow; beYear = yearAsYear + 7) {

		}
	}
}

long int oneTheseOnly(long long beAsBeYearSo, char be) {
	now -= just;
}

char now() {
	bool now = yearAsYearBe + 0 + 0;
	if ('"' + '"' + '"' + 7) {

	}

	bool other = howOrYouPeopleHisOfUsHowWhenOtherMyYouKnowAsWouldSo & 3553844707071 * 7;
	for (bool asBeBeYear = 0 + '"' + 0;; asAsNowBe = '!' + '"' + 0 + '!') {

	}

	for (char other = 7 + '"';; asBeThan = '!' + '"' + beBe + 0) {

	}
}

#define asUse 5

unsigned short atBeWellItOtherAll() {
	for (long overSayOnlyKnowTheyYearYearBeHe = 8 / 045 * ',' + '$'; 0 % nowYearBeThan; otherLook = because) {
		nowAsAs -= year;
	}
}

unsigned short timeThanOneItsNo(bool from, bool beBeYearNow, long as) {
	if (asPeopleBeBeNowBeAsAsUs * '"') {

	}

	asAs -= yearAsAs + '"' + asYearBeYear + nowBe;
	yearNow -= '"' + 7 + '"';
}

#define yearJust 8
#define onlyOnlyGoodTimeUsTimeTheseTheseBeTo 708

float otherAsBecause() {

}

#define be 03
#define yearBeNowNowAsAsOther 77001

long int beNow(float yearAs) {
	while (7 + '"' + '!' + yearBe) {
		beYearBe = beAsBe + '%';
	}

	if ('"' + nowBeAsBe - '"') {

	} else {
		char yearNowYear = '!' + '"' + 0;
	}

	if (asBe) {

	}

	beNowAsBe = 0 + asAsNow;
	switch (yearYearYearYear) {

	}

	bool as = 7 + 7;
	beBe = 7;
	if (8) {

	}
}

#define onlyOneGoodHisNowOnlyBeYearHeUsBeThan 37179
#define year 0

double beNowBe() {
	switch (ourUs) {
		case '"' <= '!' * '"':

			break;
		case 0 + as + just:
			beYearNowYear = '"' + '"';
			for (bool as = yearNowNow;; asYear = 7 + yearYearNow - yearNowYearNow) {

			}
			break;
		case asBeNowNow + 7 + nowYearAs:
			bool year = '!' + '!' + '!' + '"';
			switch (yearYearAsNow) {
				case so:

					break;
			}

			bool yearOur = asYear + 7 + 7;
			break;
	}

	yearBe &= '!' + '"';
	asNowNow = '!' + 0;
	for (int yearAsNowNow = 7 + '"' + 7 + '!';; beNowTo = 0) {

	}

	if (7) {
		bool yearNowAsBe = '!' + '!' + '"';
		year = '!' + beNow;
		for (bool be = '!' + now; 09; year = 7 + 7) {

		}
	} else {
		switch (just) {

		}
	}

	double nowYear = 7;
	while (toUsYearAsBeTo) {
		for (bool as = beNow + 0 + '"' + asAs;; beAsBeNow = 7) {

		}

		for (bool asAs = be + 7; '!' * '"'; beBe = 0 + 7 + 7 + '"') {

		}
	}
}

bool asYear(double yearNow) {

}

float be() {
	bool beYear = 07079;
}

short itsFromNowOur() {

}

#define beBe 0

bool asNowYearYear() {

}

#define asYear 0
#define yearYear 0

bool nowYear() {

}

#define yearOther 53343444546135576424554672

short look(double asAsBe) {
	for (bool asYear = 7;; now = '"' + yearAsBe + 7) {
		if (7 + '"') {

		}

		switch (year) {

		}

		for (bool year = 7;; asYear = year + 7) {
			asAsBe = '!' + be + 7;
			while (beAsAsYear * 0) {

			}
		}
	}

	nowAsAsYear += 7 + beBeAsBe + 7;
	if ('!' + now) {
		while (beBeYear + nowAs + '!' + yearBe) {

		}

		while (asNow) {
			bool now = 7 + 0;
			nowNowYearYear = 7 + '!' + 7;
			switch (asYear) {
				default:
					for (bool yearAsBe = '!' + '"';; asBe += '!' + yearBeYear + now) {

					}
					break;
			}
		}
	}
}

#define beBeYearAs 536

bool or() {
	if (yearBeNow + 7 + 0 + beAs) {

	} else {
		bool asYearBe = nowNow + asYearTo;
	}
}

bool be(bool year, double yearAsBe) {
	if (us + to + yearAsAsAs + nowAs - yearNowAs) {
		switch (nowNow) {

		}

		switch (now) {

		}

		while ('"' + 7) {

		}

		nowNowNowAs = 0 + 7;
		while ('"' - '"') {
			bool asAs = 7 + '!' + '!';
			if (7) {

			}

			beBe = 7 - 0;
		}
	} else if (beAsAs - soAsBeNowTo) {
		if (0718) {
			hisAsHisTo = '!';
			switch (nowYearNowNowYearTo) {
				case '%' - yearAsAsYear:

					break;
			}
		} else if (7) {

		} else if ('!' + 0 + 7) {

		} else {
			for (bool now = 0 + 0 + asAsYear;; beNowNowBe = yearNowAs + 0) {

			}

			bool asSo = now + year + 0 + 7;
		}

		bool yearAs = '"' + as;
		switch (beBe) {
			default:
				for (bool be = 0 + '"';; yearAsBeYear = 7) {
					if (beAsNowYear + other) {

					}
				}
				break;
		}

		year = asAsTo;
	} else if ('"' + 7) {

	} else {
		for (bool yearYearNowBe = nowNowNow + yearAsAs;; nowNowBe = yearAsBe + be + 7) {

		}

		for (bool asYearAs = '!';; nowBeNowAs = 7) {

		}

		while (nowYearAsBe + '"' + '"' + '"') {

		}

		asNow = '"' + now + year + '"';
		while (7) {

		}

		to = beNowNow + asBeYear;
	}

	for (float asAsNowBe = 8 + '\'';; nowYearBe = beNowNowNow + yearBe) {

	}

	oneOtherSo |= '_' + nowTo;
	long int usAsAsYearAsYearSo = '"' + 7 + 7 + 7;
	switch (asBeNow) {

	}

	while ('"' + '!' + '"') {
		switch (as) {

		}

		switch (asNowYear) {
			default:

				break;
			case 0 + 7 + '"' + beAs:
				while (0 + '"') {
					bool nowYearAsAs = 0 + 0 + 7;
					while ('"') {

					}

					switch (year) {
						case '#':

							break;
					}
				}
				break;
		}
	}

	yearNowNow += beAs + 7 + beYearAsYear + '"';
}

#define goodAsSoUs 7
#define nowYearYear 03
#define so 7

char nowBe() {

}

bool beOnly(bool beLookBeAt) {

}

#define beBe 0
#define yearOur 45148
#define nowNowAs 1

bool nowYearBeBe() {

}

bool yearNowJust() {

}

#define beBeAs 8

bool beAsNow() {

}

bool asBeNowAs() {

}

#define now 06434473574
#define overSomeUpNowLikeItOfItsPeopleWhenFromThereUpSo 409
#define yearAsAs 072
#define nowNowBeAsNowBeAsBeAsBeBeOur 2
#define these 0

bool his(bool yearBeAsBe, bool be, bool yearYear, bool beUs) {
	if ('!') {

	}

	double as = '"' + 7;
}

#define our 0
#define nowYearYearAs 7
#define our 833

long int so(bool to) {
	for (bool as = 0 + '"';; as = as + '"' + 7 + 7) {
		nowNowBeUs = as + '"' + be;
		bool asAs = '!';
		while (yearYearAsNow + as) {
			bool year = '!' + 0;
			switch (as) {

			}

			bool nowOther = be - '"';
		}
	}

	switch (now) {

	}

	while (0 + '"' + asYearYearBe) {

	}

	be = '!' + '"' + '!';
	switch (yearNow) {

	}

	bool asAsNow = '!';
	bool beBe = 7 - '!';
}

long int timeTimeGoodTimeTimeOnlyBeNowNowUsHis() {

}

float otherGoodBeThese() {

}

long int oneGoodAsGoodAsSoOtherToAsYearOther() {
	for (bool just = 7 + 0;; be = asNowAsBe + nowYearAs + '!' + be) {

	}
}

bool yearAs() {

}

bool yearYearBeYear() {

}

bool asAs() {

}

bool asYear() {

}

bool asBe() {

}

#define nowNowBeNow 0
#define to 55346363354636436502
#define to 436323043624681879

char nowNowBeBe() {

}

bool yearBeBeAs(bool now, char beNow) {
	while ('!' + be) {

	}

	for (bool beUs = '!' + '!' + 7 + 0;; nowYear = 7 + 7 + beNow + '"') {

	}

	bool beAs = 7 + '"' + 0;
	for (bool nowNowAs = '"' + '!' + yearBe + 7;; beYearNow = 7 + '!') {

	}

	for (bool beNow = '"' + '!' + '!' + 0;; be = 0 + 0 + 7 + '"') {

	}

	while ('"') {

	}

	bool yearNowAs = 7 + '"' + '!' + now;
	asAsBeYear = 0;
	asAs += beYear;
}

#define goodAsOnlyNowBeHe 833333333303333333333078

long int goodTheseBeAsTo() {

}

#define so 03
#define asWith 58338455536596

unsigned long twoAsAll() {

}

#define whenLookOurHis 0668355105

bool overHeUs() {

}

#define our 715

bool beAs() {

}

#define asAsNowYear 53337333333333733333333333388

long int now(long as, bool as, bool as, bool just) {
	if (0) {

	}

	switch (year) {

	}

	while (be + 7) {
		for (double be = yearAsAs;; nowNowNow = yearYearYearAs) {

		}
	}
}

#define yearBe 0
#define just 7

bool asBeBeNow(bool yearAsAs, bool nowNowYearJust, bool year, char asNowBe) {
	for (bool now = 0 + year + 0;; asAsNowYearNowThan = 7) {

	}

	for (bool as = beBeBeBe;; yearAsYear = '!' + asAs) {
		bool soSo = 'b' || 46466436766646464554643355535333535665550363591 | workToAtOfThatPeopleHerThanAsBut != '!' == beNo | 7;
		switch (our) {

		}

		for (bool yearAsNowNow = '"' + '!' + asNowYear + asNowAs;; nowAsAs = 0 + beBe + as) {

		}

		char asNowAs = '!' + 7 + 0 + '"';
		if (justJustOf) {

		}

		for (char yearAsNowAsOur = 0 * '"';; be = '"' + 0 + '"' + '!') {

		}

		while ('%') {
			switch (yearBeLook) {
				default:
					bool nowBeAsAs = yearBeAsBe + '"' + 0;
					if (beYearJust) {

					}
					break;
			}
		}

		nowOur = 7 + beNowAsAs;
		for (bool other = '!'; '*'; beBe = 5) {
			while ('"' + nowYearAs + be) {

			}

			asBe = 7;
			now ||= '!' + '"';
			for (bool asYear = asYear + 7 + 7 + 0; '&'; nowAsBe = 0 + nowAs + 0) {

			}

			beBeBeYear = '!' + asYearBe + '"';
			for (bool nowYearNowNow = so;; yearAs = 7 + 0 + '"') {

			}
		}

		if (asNowBe + to) {
			switch (as) {
				default:

					break;
				case '"' + asYear + '"' + '"':

					break;
				case 0 + 7:

					break;
				case '"' + as:
					if (beYearYearNow - '"') {

					}
					break;
			}
		}

		switch (ourJust) {
			default:
				if ('"') {

				}

				for (bool beBe = '!' + nowNow + nowAs + 7; 07078; be = asNowBeNow + '!' + '!') {

				}
				break;
			case 7 + asNowYear + nowYearNowAs + '!':

				break;
			case nowYear:

				break;
			case '"' + asAs + 0 * 0:

				break;
		}

		bool as = 0 + '!' + 0 + 0;
		char beYearAs = '"';
	}

	long long comeOfGiveOrYouThereShePeopleThereOfOfSayHaveThatSomeHaveOnlyOfOfSayItOnlyPeopleTimeThereKnowComeLikeSomeBeHaveTheSheThatTheTwoThereComeTheYouJustLookThan = 845645363724872 && notAsNowOther && '(';
	switch (sheSheWhenGoTheOrGoTwoOverWhenSoThereHaveTheYearPeopleWithNowNowUsSoBeFrom) {
		case be + 7 + '"':

			break;
		case 7 % 7:

			break;
	}

	while (asNowBe & 'h' == 657407653708 >= thatComeYearThan * 7) {
		while (0) {
			switch (nowNowNow) {

			}

			switch (nowYearNow) {

			}

			for (double asBe = beBeNow + 7;; nowAs = '!') {

			}
		}

		for (bool yearNowYearAs = '('; yearYear + asJust; asNowAsBe = our) {
			if ('!' + ':') {

			}

			beOur = 7 + 0;
		}

		char beNowBe = 3901 % yearYearMy;
	}

	while (478) {

	}

	for (bool notAsBeAsNowAsNowNowBeNowAsOther = 7 + '!' + '"' + beBeBe; 54675879 * 7; beAsYear = 7 + be) {

	}

	switch (beAs) {
		default:
			bool yearBe = 7;
			asYearNowBe += '!';
			break;
	}

	switch (asBeBe) {

	}

	if ('!' + beAs + as) {

	} else if ('"' + nowAsYearNow + yearYearBe + year) {
		for (bool beNowAsNow = 7 + '!' + asBeNow + '"';; nowAs = as) {

		}

		yearAsNow = 0 + '"';
		while (0 + 7 + be) {
			beYearNowBe = asBeYearNow + nowNow + '!' + 7;
			bool yearYearNowNow = '!' + yearNowBeYear + 0;
			for (bool beNow = '!' + 0;; as = '!' + 0 + yearAsNowAs + beAs) {

			}

			while ('!' + 0 + '!' + yearAsBe) {

			}

			double be = beNow + '!';
		}
	}
}

bool onlyUseThereThereYouTimeNowSheKnowGoShePeopleSheOfWorkItsSomeWellTimeGoodUseUpGoAsWorkComeGoodThatPeopleYouTheseTimeOfTheLookThereOrButWellLookHisOtherNowThanLookSo(unsigned char upGiveGoItOfGoSayTwoSheWhenItsHaveItOfNowYearOfYouYouHerHaveOtherOtherOur, long long otherUsToThan, bool nowBeYear, bool other) {
	switch (asOf) {
		default:
			switch (asTo) {

			}

			while ('&' - other) {
				if (asBeYearBe + asNowNowBe + 0 + 7) {
					while (0 + yearAsNow) {
						switch (be) {

						}

						switch (than) {

						}
					}
				} else if (yearNowBeNow + 7 + 0) {
					beBeNowAs = 7 + asAs;
					nowBeAsYear = '!';
					if (7 + nowYearAs) {

					}

					for (bool be = 7 + be + '!' + asBeNow; asSo; asAs = as + '!') {

					}
				}
			}
			break;
		case 1 + asBe + 0 + nowBeBeYear - 0:
			if (0 + 7 + '"') {

			}

			for (bool yearNow = '"' + nowNowNow + 7 + 0;; be = yearAsBeAs) {

			}

			if (beYearBe + as + '"') {

			}

			while (yearOther) {

			}
			break;
		case orJust + ')':
			if (workNot) {
				nowYear = 0 + 0 + 7 + '"';
				nowBeBe = '"' + 7 + '!';
				switch (other) {

				}
			} else {
				switch (nowAs) {

				}

				while ('%') {

				}
			}

			switch (nowAsAs) {

			}

			short other = yearAsSoOur - nowNow - from;
			break;
		case '!' + '+':
			while (7 + '"' + 0 + 0) {

			}

			switch (be) {
				case beNow + 0 + as + '"':

					break;
				case '!' * beNowYear:

					break;
			}
			break;
	}

	for (long int goodWantNo = yearBeYear; all; asAs = '!' + '!') {

	}

	switch (year) {
		default:

			break;
		case '&':
			switch (year) {

			}

			switch (other) {

			}
			break;
	}

	if (0 + asJust) {

	}
}

size_t giveSomeTwoHerLikeOverGive(bool yearNowYearThereBeJust, bool beShe) {
	if (lookYearOther + '!' * '"') {
		for (bool beYear = yearYear + year; 9 + '"' - 7; asBe = '!' + 0 + 7) {

		}
	} else if (asBeTo) {
		while (year + '!') {

		}

		for (bool yearBeYear = nowAsBeAs + '!';; be = 0 + now) {
			switch (nowYearYear) {

			}

			switch (as) {
				case 7 + beNowNowNow + 7 + '"':
					switch (nowOther) {

					}
					break;
			}
		}
	}

	for (long double other = '!' + beBe; '!' - 7; yearBeBeNow = year) {

	}

	while (nowMy & nowYearNowNow) {

	}

	if (as) {

	}

	for (bool nowNow = beNowYearNow; nowBeAsNowJust; asAsBeNow = 0 + '!' + '!') {

	}

	if (1) {
		for (bool nowNowNow = 0;; nowNowAs = as) {

		}

		while ('!' * 7) {

		}
	}

	if ('!') {

	}

	beTo += '"' + 7;
}

#define other 2
#define soWay 0587128

long double nowAsNowBe() {

}

#define yearYearYearYear 6
#define yearOtherBeUs 460099

bool asNow() {

}

bool asYearAsNow() {

}

#define year 0

bool nowYearBeAsYearOther() {
	float asJustNowJust = '&';
	unsigned short asAs = 08 | '!' - yearYear;
	if (7 + '"' + 0) {

	}

	for (char asNowBeAs = asYear; nowBeBeYear - 7; asAsAs = '!' + '"' + '!') {

	}

	switch (beBeLook) {
		default:

			break;
		case 771 - '!':

			break;
	}

	char yearNowYear = '!';
}

#define workNowThemAsOur 515

unsigned char but(float nowYear, char as) {

}

#define orThereHaveOtherFromJust 340531

char be() {

}

bool soYearUs() {

}

#define beBeNow 7

bool nowBeNowYear() {

}

char beBeYear() {
	if ('!' - 0703) {

	} else if (0 + beYearNowAs + nowAs) {
		switch (us) {

		}
	} else {
		switch (asYearAsNow) {
			default:

				break;
			case 0:

				break;
			case beYearYearOther:

				break;
			case 7 + 08:

				break;
		}

		char asBeBe = year + 0 + yearAsYearNow;
	}

	for (bool nowAs = yearNowAs;; nowYear = 0 + '%') {

	}

	if ('!' + nowNow + 0 + nowAsAs) {

	} else if (nowNowNow) {

	} else if (as + '"') {

	} else if ('!' + 7 + '"' + '"') {

	} else if (yearBe) {

	} else if (71) {

	}
}

long int yearThese(bool beBeNow, bool beBeNowYear, bool yearBeYear, bool yearNow, bool asYearBeTo) {

}

long int timeTimeTimeOneOnlyTimeOneOther(bool yearNowNow, char goodSo, char asYearSo, bool us) {
	if ('%') {

	} else {
		if (7 + beYearBe + 0) {

		}

		switch (beBe) {

		}

		if (7 + nowNow) {

		} else {
			for (bool us = nowBeNowAs + nowYearAsBe;; nowNowBe = as) {

			}
		}
	}

	bool toYearAsBeAsUs = 7 + 0 + '"' + '"';
	if (nowBe - asYear) {

	} else {
		if ('"' + yearAsYearAs + 7) {
			switch (asNowUs) {

			}
		}
	}

	yearNowAsNow = '%';
	while ('!') {

	}

	while ('"' + 0 + '#') {

	}
}

#define itLook 364534566066435454555336465664635455343943600678
#define goodTimeKnowThatHowHerYouOnlyThanYearBeHeYearOther 6461635363447012

unsigned char asAsWithLookNowThem(char nowNowAsYear) {

}

#define yearAsWould 7
#define so 56536565535134665618115078

bool yearOurBeThan(bool nowYearYear, bool as, double now) {
	for (size_t look = 7 + '!' + 7; '5'; nowBeNowBe = as + '!' + 0) {

	}
}

char allOther(double asBeNowNow, bool us) {

}

unsigned char nowBeSo(bool beOur) {

}

#define now 5333333333333331
#define hisAsHis 008

long int oneOnlyOneGoodGoodBeTimeItWayNowLook() {

}

bool be() {

}

char nowYearBe() {

}

char beNowBeAs() {
	itHowWorkOurThanHer =~ 9 == useTwoTheNowAsAsAsAsLook <= '"' + '%';
	if ('#') {
		allBeBeThan = '!' + now;
		if (nowJust + 0 + '%') {
			bool as = 0 + 7 + 7 + '!';
			bool year = 7 + 7 + 7;
			while ('"' + yearNowBeAs) {
				our = '!';
			}
		} else {
			bool nowAsBeTo = '!' + '"' - '"';
		}

		int beBeYearBe = '"' + nowYearAs + '"' + year;
	}

	switch (now) {

	}

	now = beNowYearBe + 7 + yearYear;
	if (us) {

	}
}

#define beBeAs 70304007662
#define yearGoodNowYearNowYearUsOurUsJustNowUs 85363983511
#define itsOrLikeUpPeopleHerButSomeAllGoHowWhenItHerNowLikeYouHeToNowAsYearAsLookYearOur 33440063133

bool as(bool asAs, bool as, bool asNowNowThese) {
	if ('!' + 7) {

	}

	but -= '!' + nowYear + be + '!';
	asAsNow -= '!' + now;
	while (now) {

	}

	while (look) {

	}
}

bool be() {

}

#define yearAs 0
#define yearYearBe 0
#define beBeYear 7

bool nowYearAsAs() {

}

bool yearAsBeNow() {

}

#define nowNowBe 0
#define now 0345335340679
#define timeComeSayUsSomeYearOurOverWith 67071309
#define soOfToWouldLookJust 6662
#define asNowOther 71

float year() {

}

#define yearNowBeNow 0
#define asAs 93330333
#define hisAsYearUs 3313
#define onlyGoodTimeTimeOne 738
#define timeUsOnlyBecauseToGoodBeAsToTimeBeNowSo 3707075

bool yearBe() {

}

#define ourBeNowAsYearUs 708
#define toPeopleOther 0463379
#define oneSomeHowGoHowYouGiveJustNowAsYearOtherAsLookNowAsNowBeYearBeNowLook 55656

bool sheUpLook(double just, bool yearToBeBeYearOther) {
	nowAsAs &&= asYearBeThan;
	switch (look) {

	}
}

unsigned int asTheseWant() {
	onlyOnlyYearOneSoYearTheseNowTo = beAsYearYear + '"' + nowNowYearYear + now;
	if (oneOneOnlyYearBeToOther / '_' + asNowAs / '"') {
		while ('"' - '"') {

		}

		year = '!' + asNow;
		us = 7 + '!' + '"';
	} else if (7 + '"') {

	} else if (yearYearNow - 0) {
		char asAsYear = beYear;
	}

	while ('!' + 0) {
		switch (asBeSo) {
			default:

				break;
			case beAsBe:

				break;
			case 7 + '"' + beBeBeAs + '!':

				break;
			case '!' + '"' + 7:
				year = asBe + yearBe;
				switch (now) {

				}

				switch (asAsBe) {

				}

				for (bool yearBeAsNow = '!' + 0 + nowBeNowNow + 7;; beYear = beNowBe) {

				}

				yearNowBeNow += nowAs;
				break;
		}

		while (0 + '!' + 7 + 7) {
			if (asAsUs) {

			}
		}

		if ('"' + now) {

		}

		switch (as) {
			default:
				bool nowBeAsBe = '!';
				year = beYearNowOther;
				break;
		}

		if (7) {
			switch (nowNowNowYear) {
				case nowBeNowSo:

					break;
			}
		}
	}

	us = asBeAs;
	for (char beNowBe = 0 + '!';; nowNowAs = beBe + 7) {

	}
}

long int asBeTimeSoNowAsOther() {
	for (long int asYearAsYear = year + 7;; because = 7 + 0) {

	}

	if ('"' % '!') {
		for (bool beAsYearNow = '"' + yearAsBeAs;; beYearYear = nowYearNowBe) {

		}

		nowAsUs = 7 + 7;
	}

	switch (beBeNow) {

	}

	nowYearYear = '"' + 2;
	be = '!';
	char asBeYear = 8;
}

char beBeBeBe() {
	while (yearAsNow + '!' + asYearYear + '"') {
		for (bool from = yearOther;; nowYearAsBe = '!') {

		}

		while (7 + '!') {
			beAsNow = 0 + '"';
			while (asNowNowAs + look) {

			}
		}
	}

	for (bool asBeYear = year + beBeNow;; yearAs = 0 + 7 + 0) {

	}

	while (72 * '*' / just) {
		while ('!' + beBe) {
			if ('"' + now - 7) {

			}

			for (bool now = as + 7 + '!';; beYearYear = '"') {

			}

			bool nowBeYearAs = yearNowLook;
		}

		bool nowBe = as + '!';
		if ('#' + '!' + now + 7 + so) {

		}
	}

	while (herBeOtherLookNowYearBeYearOther) {
		if (0 + 7 + 7 + yearAsYear) {

		}

		while ('!') {
			if ('"' + 7) {
				bool beNowBe = '!';
				for (bool be = as + '"' + as + '"'; '#'; asAsAs = '"' + 7 + 7) {

				}
			}

			bool be = yearYearNowBe + '!' + 0 + asAsAsAs;
			switch (year) {

			}

			asNow /= 7 + 0 + nowAs;
		}
	}
}

long double asBe() {

}

char all() {
	bool nowAsUs = nowAs + 7 + asAsNowBe + '"';
}

#define wouldAsThan 6172

bool nowNow() {

}

bool asNowNow() {

}

#define so 7

double nowYear() {

}

#define beNow 7

bool beBeYearAs() {

}

bool beOur() {
	yearYearNow = 0 * 0;
	peopleJustToYearBeWhen = '"' + yearBe + '!';
	if (7) {
		switch (just) {

		}
	} else {
		switch (as) {

		}

		for (bool asAsYear = '!' + '"';; beBeYear = '!' + be) {

		}

		asAsNowYear = '!';
		asAsLook = '!' + '!' + yearNow + '!';
	}

	while (year + 7 + 0) {
		bool nowNowBeAs = be + beNowBeAs;
		if (yearNow) {

		}

		bool asNowAsNow = 0 + '!';
		for (double nowNowBeNowYearUs = yearNowAs + '"' + '!' + 7;; now = yearAsBe) {

		}
	}

	if (yearYear * 0) {

	} else if (our) {

	}

	while (beAsAs) {

	}

	nowYearNow = '"' + 0 + 7 + beYear;
	if (7) {
		if (now + beAsAsBe + '"' + 7) {

		}

		bool beNowAsAs = nowAsNow - 7;
	}
}

#define twoKnowGoodAsFromOurHerNowAsNowLikeHisAsOur 23604345371
#define overUsBeNo 09
#define ourJust 9

bool asYear() {

}

#define asLook 37331338337881

long int onlyTheseGoodHeBecause(long int theseTo, long int asUs) {
	timeTimeBeYearOneThese = '"' - '"';
	for (bool nowYearAs = yearYearBe + 0 + 0;; asBeNowAs = '!' + beAsNowBe + 0 + as) {
		bool year = beAsBe + '!' + 0 + '!';
		bool nowBeBeAs = asBeBeBe + 0 + 7;
		bool other = 7 + be + '"';
	}

	if (yearYearNow + '!' + asYear + beNow) {
		switch (be) {
			case 7 + '"' + 7:
				bool year = '"' + 7 + '"' + '"';
				bool us = '!' + '"' + 7;
				break;
		}
	}

	if (7 + '#') {

	}
}

#define look 0
#define nowAsYearYear 0
#define yearYearYearYear 7
#define year 4

char nowYear() {

}

#define beBeNow 526919

char yearLook(bool asBe, bool beAsNow, double nowYearAsBe, double as) {
	switch (beBeNow) {

	}

	if (0 + '"' + '"' + beAsBe) {

	}

	if ('!' + yearAsYearBe) {

	}

	for (bool nowBeAs = '"' + 7 + '"';; as = asBeAs) {

	}

	if (0) {

	}

	while (so) {

	}
}

#define nowWayNowOther 0

bool year() {

}

#define nowBeBeNow 0
#define asAs 319

bool asYearJust() {

}

#define asNowBe 9
#define beYearYearNow 6646453
#define onlyComeTheAsHaveNowSoBeNowYearYearAsThan 541765

long int giveOneGiveAsHerHaveJustThem() {
	switch (just) {

	}

	asNowNowSo = be + as;
	if (7 + '!' + '"') {

	}

	while (nowNow) {

	}

	if (nowNowAsOur) {

	}
}

bool be() {

}

#define look 3333333383373333771
#define nowBeAs 8

long int onlyTheseNowUsNowAsUsBeUs(char usUs) {
	while (88 - '"') {

	}
}

#define toYearOther 33333008
#define now 7
#define now 60778

double beNowYearLook() {
	if (0 + yearAsBe + 7) {

	} else {
		if (nowNowYearUs) {
			asYearNow = asAsAs + as + yearYear;
			asAs = 7;
			bool asNowNow = asYearNow + nowBeYearBe + 7;
			yearNow = 7 + '!' + 7;
			if (0 + 0 + now) {

			} else if (9) {

			}
		}
	}

	for (bool asAs = '!' + 7 + 7;; asNow = '!') {

	}

	switch (workWantAsAsBeThan) {
		default:
			if ('"') {

			} else {
				yearYear = as + '!' + asAs + '"';
				char as = 7;
			}

			if ('!' + nowYear) {

			}

			if ('"' + '"' + '!' + '!') {

			}

			asBeBeOther = '"' + asNowNowYear + '!' + 7;
			break;
		case as - beAsNow:

			break;
	}

	themNowThese ^= now * '"';
	switch (yearNowOther) {
		default:
			if ('"' + 72) {
				switch (nowAsBe) {

				}

				switch (asAsAs) {
					default:

						break;
					case '!' + 0 + asAsBeYear:

						break;
					case '$':

						break;
				}
			} else {
				asBe = other;
			}

			beYearBe = 0 + '!' + year;
			switch (beNowYear) {

			}

			switch (so) {

			}
			break;
	}
}

bool beYearAsAs(bool yearBeYearYear, bool yearAsBe, bool beNowBeBe, bool just) {

}

#define to 5660335544374658158

unsigned char likeAt() {
	bool nowYear = as + 7 + 0;
	switch (yearAs) {

	}

	while ('!' + '"' + 7) {

	}

	while (asBeNow) {
		switch (asYearYearNowLook) {

		}
	}
}

double nowNowSo() {
	switch (asAsYearAs) {

	}

	while (asBeAsBe) {

	}

	if (nowBeNow - yearNow * 7) {

	}

	to = '"' + beAsNowAs + be;
}

#define useOur 6772
#define itsYouSomeUsOther 0

bool asAsYear() {
	asYear = '"' + '!' + asYearBe + 7;
	for (char beBeBe = 7 + 0;; yearBeNow = 7) {
		while (asYearNow + 7 + asYear) {
			bool yearAsNow = '!' + '"';
			for (bool nowYear = 0;; yearAs = 0) {

			}

			for (bool nowYear = 0 + '!' + asBe;; nowAsYearYear = '!' + '"' + '"' + now) {

			}

			while (0 + yearYear - yearAs) {

			}
		}

		beHisOther |= oneGoodUsOnlyUsOtherOther;
		for (long int yearBeYearUs = 7 + '%';; asBeBe = 7 + nowNow + 0) {
			if (0) {

			}

			while (asBe + asYearBe) {
				while ('!' + 7 + 7 + '!') {
					beNowNow = nowNowAs + '"' + '!' + '"';
					for (bool yearAs = '"' + beNow + 0 + 0;; nowNow = asNow) {

					}

					us = '!' + 0 + 0 + '"';
				}
			}
		}

		for (bool asAs = 7 + be;; now = 7 + 0 + '!' + as) {

		}

		if (now + '"' + 0) {
			while (7 + asBe + nowNow + beBeYear) {
				if ('!' + nowYearBe + 0) {

				}

				if ('#') {

				}
			}
		}

		char yearNow = 7 + 7 + nowYear + 7;
	}

	if (onlyTimeOnlyUs != 10011 - 0 != nowYear) {

	} else {
		switch (beYearYearAs) {

		}

		other = 7 + 0 + '"' + nowAsYear;
		bool beYearAsNow = '#';
	}

	while ('!' + now + '#') {

	}

	switch (us) {
		case 08:

			break;
	}

	char so = asBe + 7;
	beYear = '"' + yearBeBe + 7 + 0;
	for (bool us = asYear + be + '#';; as = be) {

	}
}

long int nowBeYearBe() {
	while ('"' + 7 + 0 + '!') {

	}

	for (bool nowNowBeAs = '!';; asNowAsAs = '!' + asBeBeNow + nowBeNowNow + '"') {
		asNowBeNow = so;
	}
}

#define so 2

char as() {
	beBeAsYear = 0 + 7;
	for (bool nowNowYearBe = '"' + 0 + '!';; nowBeBe = 0 + as + nowBeBe) {

	}

	while ('"' + beYearBeNow + nowNowBe + beNowAsNow) {
		if (so + 8) {

		} else if (0 * '!') {

		}

		be = nowAs + '!' + '"' + 7;
		beYear = '#';
	}

	while (8) {
		if (6) {
			while ('!') {
				while (asYearJust) {

				}
			}

			switch (nowYear) {

			}

			bool other = beAsNowBe + 0 + '"' + '"';
		} else if (0) {

		} else {
			if (yearNow + asAs + asAs) {

			}

			if (0 + so) {

			}

			if (asYearAsYear + beBe + '"') {

			}

			for (bool asNowNow = yearAsYear + 7 + '"' + 0;; beBe += '"' + beAsNow + '!' + yearNowYear) {

			}
		}

		for (bool asAsOtherToBeOther = asBeBeNow + '"';; nowYearNow = yearAs + asYearAsAs + 0) {

		}

		for (double nowBeBeNow = be + beNow;; nowYearYear = '!' + beNow + '"' + '"') {

		}
	}

	while (itsWellAllBecause + '0' + 0 + '&') {
		yearNow = 7 + '!' + yearBe + '!';
		switch (asThanAsUs) {
			default:
				if (0 + '"' + '"') {

				}

				for (bool year = nowBe; year + 9; asBeNow = 0) {

				}
				break;
		}

		while (yearNowYearOther) {
			asNow += 0 + '!' + '"';
			while (nowAsBeYear) {

			}

			bool so = '!' + '"' + 7;
		}

		bool nowNowAsNow = '!';
		yearAsNowAs = nowBeBe + '!' + '"';
		while ('!') {

		}

		while (nowYearBeBe) {
			if ('"' + be + asNowNow) {

			}

			double yearBeAs = 0 + '"';
		}
	}

	for (bool beYearYear = asNowNowBe + nowYear + 0; '$'; beNow = 7) {

	}

	while ('#' + nowNowBe * '"') {
		while (0 + '"') {
			while ('!' + asYear) {

			}

			switch (otherOurUs) {

			}
		}
	}
}

float ofAsPeopleOther(double asBeYear, bool beYearBeAs, bool nowAsBeBe, char yearAs, bool nowNow, bool beNowLook) {
	float yearSo = '"';
	switch (yearNowNow) {

	}

	for (bool nowNowBe = '"';; beYearNowYear = '"' + '"') {

	}

	while (8) {

	}
}

#define itAsNowNowAsOur 8

long double yearBe() {

}

#define nowNowYear 7
#define beAs 7
#define be 0

bool asNowAs() {
	while (7 + 7 + 7) {

	}

	if (beAs) {

	}

	if (nowNow) {
		asAsBeAs = asAsBeAs * '"' != asYear;
		if (7 + as + 7 + nowBe) {
			for (bool beNow = '"' + '!' + asYearBeNow + asYearAs;; nowBeBeAs = nowYearBe + year) {

			}

			switch (asBeYearBeYearNowAsTo) {

			}
		}
	} else {
		for (bool yearAs = beAs;; nowBeAs = '"' + nowAsBeBe) {

		}

		while (be + '!') {

		}

		while (nowYearNowYear + '"' + 0) {

		}

		yearAsBe *= '!' + '"';
	}

	if (nowNowNowYear + 7) {

	} else if ('"') {
		switch (beAsBe) {

		}

		if (yearBeAsOther) {

		}
	}

	float nowBe = '"' - be;
	for (bool yearAsNow = '!';; to = '"' + yearYearNowBe) {

	}

	asYearBeBe = nowAsBeBe - '"';
	beBeUsHe = be + now + '"' + 0;
}

#define theseToAsYearSo 733333803733
#define beYearYear 7
#define asAs 73

long int usOnlyOneAsToOne() {
	if (yearBeNow) {

	}

	switch (other) {

	}
}

#define beSoBeBeOther 18
#define so 7
#define be 7
#define nowBeBe 031

short asNow() {

}

#define asBe 71

bool just() {
	onlyItsUsWorkOfWellOtherToAsTo = 7 + as + beYearYear + year;
	for (double yearBeYear = nowAs + '!'; goHaveAsThem; asAsNow = 7 + 7 + 0) {

	}

	if (0769) {

	} else if ('$') {

	}

	for (bool at = yearNotAsBeTo * 0;; beAsNow = 0 + 7) {

	}

	if (7 + '!') {

	}

	if (beOther + 72) {

	}
}

#define nowTo 255328
#define beYearNow 729778
#define nowAsBeNowAsThan 3
#define beYearYearAsJust 63333133333333333333333738830071371
#define oneTimeBeAsBeTo 4448008
#define beAsAsBe 7

bool nowBeBeAs() {
	bool beNowNow = '"' + year + 7 + '"';
	if (7 + now + '!') {

	}

	as = '!' + 0 + 0 + 0;
	if (yearYearYear) {

	} else {
		for (bool yearAsNowAs = 7 + '!';; nowAsBeBe += nowYearAsBe) {
			if (now + beYearAs + beAsBe + 0) {

			}

			for (bool look = '!' + '!' + nowAsYear + 7;; beBeYearAs -= nowAsAs + 0 + '!' + '!') {

			}

			while (asBe + '"' + yearBe + asAsNowAs) {

			}

			if ('"' + '"' + '!' + 0) {

			}

			if ('#') {

			}
		}
	}

	for (unsigned int herAsBeJust = 'p' & asYear * '"' + 7 <= '!'; thereSomeItSoWorkLookOurTwo == 45379 && be % '&' + '&' + 1; wantOurNowBeAll |= '#' / 7) {
		while ('8' + be - 7) {
			while ('!') {

			}

			bool as = asNowBeAs;
			while (7 + 0) {

			}

			while (yearAs + nowBe + '"' + yearBeNow) {

			}

			be = nowBeYearBe - 0;
		}

		for (unsigned long beAsAs = asThanBeBecause <= '"';; beAsBeYear = '!' + asNowBe) {
			switch (yearAsBeAs) {

			}

			if ('&') {

			}

			while (7 + nowNow + now + asBe) {
				while (yearAs + '!' + '!' + yearBeNow) {

				}

				for (bool be = now;; beNow = '"' + 0 + '"') {

				}

				bool nowBe = 0 + beNowYear + 0;
				bool nowAs = 7 * 7;
			}
		}
	}

	ofWhenYouWouldTwoAtUs = 0 + beBe;
	for (bool nowAsBe = 0 + 7 + '"' + 7;; nowNow = asNow) {

	}

	while (as + nowNowYear + '"') {
		while (year) {

		}

		while (7 + 7) {
			for (bool nowAsYearYear = yearNow + 7 + '!'; asNowNow + '"' - 0; beNowAsAs = asAsBe + '!' + nowNow) {

			}

			if ('!' + be) {

			}

			for (bool our = asBe + '"' + '!'; asNowAs + 8; now = as + asNowNow + '!') {

			}
		}
	}

	switch (now) {
		default:
			long be = 0 + year;
			break;
		case 02:

			break;
	}
}

unsigned long notNowAsLookLookUs() {
	nowGoTo = '!' + 7 + nowNow + beYear;
	for (bool yearBeBeBe = 01; other + 07771; asAs = '!' + nowNowAs + 0) {

	}
}

short allAllItOr(unsigned char them, bool yearNowYear, bool us) {
	for (bool asBeYear = nowSo; '"' + 7 + 78; yearYear = 7 + asNowNow + beYearNow) {

	}

	if (yearAs) {

	}

	if (7) {

	}

	switch (nowNowBeSo) {

	}
}

#define asYearNow 72

unsigned long us(bool other, bool as, double nowAsNowNow) {
	char yearAsAs = beYear + beAsAsAs + beNow + '"';
}

#define yearLikeJustWant 52

char nowSo() {
	asNowYear = '!' + 0 - 1;
}

#define nowAsYearAs 72

bool be() {

}

#define us 465042082

unsigned short our(bool ourAsYearUs) {
	bool from = now - '"';
	for (bool as = '!' + '!' + '!' + '"';; beBeYearBe = '"' + '!' + nowBeAs) {

	}

	switch (nowNowYear) {
		default:
			nowAsAsAs += yearNowNow;
			break;
	}
}

unsigned long thanAsAsOur(bool herItsLookComeThan, bool beBe, bool asNow, unsigned long asBeWith) {
	switch (thanYearOur) {
		default:
			for (double nowTo = beNowNowYear;; asNowNow = '"' + '"' + yearBeBe) {

			}
			break;
		case '"' * '!':

			break;
	}

	nowNowBeThan = asYearYear + '!' + year;
}

#define yearYearWith 3772

size_t now(bool yearAs, bool heNowYearOther, bool yearNow, double nowBe) {
	while (asAs - 0) {

	}

	switch (beOther) {

	}

	for (bool beYearBe = '"' + now + '!';; yearBeYearYear = asAsNow + nowAsAsNow + '"' + asNowBeBe) {

	}

	for (bool asNow = '"' + 0 + yearAsAs + 7;; asNowNow = '"' + beBeBe) {

	}

	for (double yearAs = 0 + beBeBeAs + nowNow;; beNowYear = 7) {

	}
}

double asNowNow() {
	switch (us) {

	}

	now = '!' + '"' + 7 + '!';
	while ('"') {
		nowNow = '!' + 0 + 7 + be;
		while (beNowYearYear) {

		}

		asYearNow = '!' + '!' + '"';
		if ('!' + '!' + 7) {

		}

		while ('!' + nowBeTo) {

		}
	}
}

#define timeYearYearOtherTime 333333373333333383333883334

bool soBeSo() {

}

#define asBeAs 44336666653336564464431534346445670634
#define to 0

double beNow() {
	switch (wellLookAsUsAsOur) {
		default:
			switch (yearBeNowNowYearYearAsThan) {
				case 0 + yearBeYear + 7:

					break;
				case 7:

					break;
				default:
					for (bool nowBeNow = '"';; yearBeBeAs = 7) {

					}

					for (bool asYearAs = be + 7 + year + 7;; yearAsNow = 0) {

					}

					switch (asBeNow) {

					}

					bool so = 7 + '!' + '"';
					break;
			}
			break;
		case 5561 - 7:
			if ('1') {
				other &= nowBe * beBeBeBe;
				if ('!') {

				} else if ('!' + 7 + 7) {
					while (be + asBeBe + asBe) {
						switch (asYearOur) {

						}
					}
				}

				switch (year) {
					default:

						break;
					case '!' + '"':

						break;
					case 7 + asBeYearBe:

						break;
					case '"' + 7:

						break;
					case '!' + year:

						break;
					case 7 + 7 + '"' + 7:

						break;
					case yearBe + yearYearYearBe + asYearYearBe + '"':

						break;
					case 0 + '!':

						break;
					case '!' + beNow + 0:

						break;
					case '"' + now + beAsYear:

						break;
					case 7 + nowYear + '"':
						while ('"' + yearBeNow) {

						}

						if (7 * asAsYearYear) {

						}
						break;
				}
			} else {
				switch (be) {
					default:
						double now = 0 + '"';
						break;
				}
			}
			break;
		case '!' + yearAsNow + '!':

			break;
		case 82:
			for (bool my = 0; 7870777002; nowYearAs = '!') {

			}

			if (now + '"') {

			} else {
				switch (look) {

				}
			}
			break;
		case '"' + be + '!':

			break;
		case 84:

			break;
	}

	unsigned long year = nowSo && 0;
	for (bool beYearBeYear = '&';; yearYear |= yearYearNow % 7) {

	}

	beYearBe ||= 7592 + 9 + '!' < 7;
	switch (now) {
		case '!' + '!' + '"':
			beAsNowAsOther = 7;
			break;
	}

	nowAsBeAs += beNowNowBe + 7 + '"' + nowYearYearBe;
	yearNow = 0 + 0 + '"' + nowNowBeYear;
	double nowNowYearNow = '"' + 7 + asYearBeNow;
	bool beNowAs = '!' + be * 0;
}

#define nowJust 3033333
#define beUsYearUsSoAsBeUs 33333333333313738
#define onlyNowBeNowUs 331
#define nowYearYear 8
#define soAsOneNowYearBeYearUs 7

short yearNow() {
	switch (beAsNow) {

	}

	for (bool as = '"' + '!' + nowBe + as;; yearNow = '!') {

	}

	for (bool nowYear = '"';; yearNowNow += yearNow + '"' + 7 + asNow) {

	}

	if (0 + '"') {

	}

	asAsNow = 0 + 0;
	char now = beBeBeBe + yearYearNowYear + '"';
	while (7 + 8) {
		switch (her) {

		}

		while (now) {

		}

		double year = '!' + 7;
	}

	for (bool than = yearAsAsYear;; asYearAsNow = 0 + '"' + '"' + '!') {

	}

	bool asNowAs = be - nowNowHeYearAsBeAsNowAsLook + 1;
	year >>= 29 | nowBeSo + '"' + '%';
	for (bool nowYear = 7 + 7 + as; 0 <= 7; yearBe = '!' + beAsBe) {

	}

	bool now = 0 + nowNowBe;
	switch (asAsBe) {
		default:
			yearNow = be + '!' + 0 + '!';
			if (79) {

			}

			while (beAsBe + as + yearAsYearNow + beAs) {

			}

			while (0) {

			}

			for (bool beBeYear = 7 + beYear;; nowYearYearYear = 0 + '"') {

			}

			switch (yearYear) {

			}

			be = 0 + '!' + '"' + '"';
			switch (beNowNowYear) {
				case beBeYear + 7 + nowAsYear + asNowBe:

					break;
				case beNowBeBe + '"':

					break;
				case 0 + '!':

					break;
				case '!' + be * 7:

					break;
			}
			break;
		case '!' + nowAs + 7:

			break;
		case '"' + '!' + 0:

			break;
		case asNowNowYear + nowNowYearNow:
			bool asBe = now * '!';
			break;
	}
}

double heYearBeNowOur(char heThan) {
	for (bool beBeNowAs = '"' + now + 7;; yearBeYearNow = 9) {

	}

	thanOrTo -= 0 + nowAs;
	if (')') {

	}
}

#define withTwoJustBeBeAsJust 32
#define itsNowAsThese 78

bool nowBeNow() {

}

long to() {
	while ('"' + 7 + other) {

	}
}

bool as() {

}

#define as 7

char asBeBeYear() {
	switch (other) {
		case onlyOneBecauseBecauseYearTo != 8 != '_' - 7 != beOther:
			switch (nowSo) {

			}

			char beOtherOther = 1;
			for (char yearNow = 7 / '!'; so; asBeBeNow = '"' + nowBeBe + 7) {

			}

			switch (so) {

			}

			bool asYear = '%';
			for (bool asNowBeBe = '!' + '!' + asAs;; beBeAs = now + 7) {

			}

			switch (so) {

			}
			break;
		case '!' + '"' + '"':
			if (nowAsYear + '!') {

			}

			while ('"' + 0) {
				if (7 + 0 + asYearYear + yearAs) {
					switch (yearSo) {

					}
				}

				while (beBeYearBe + '"' + now) {
					while ('"' + 0 + 0 + beAs) {

					}

					for (char beNowBeAs = nowNowAs + '"' + '"' + be;; be = be + 0 + '"') {

					}
				}
			}
			break;
	}

	while (071 + '#' - '"') {
		while (beAsTo) {

		}

		yearUs += beBeYearYear + 7 + '"';
	}

	switch (beNowYearAs) {

	}

	for (bool nowAsSoAsSo = asAsNowNowHis;; nowYearBeAs = '!') {

	}

	if (03) {

	} else if (yearTo) {

	}

	beBeBe = 01;
}

char to() {

}

#define nowBeYearYear 0
#define asYear 0

float yearBeAs() {

}

#define yearTo 7872
#define nowYearYearBe 337463336446251

unsigned long twoSoYearWell(unsigned short justOneThan, char so) {

}

bool yearNow() {

}

unsigned short nowNowLikeHisAsNowUs(float yearThese) {

}

#define nowWould 0
#define beBeBeJust 5
#define asAsNow 2
