#define onlyUsSoSoOther 3333303333333333303308333338
#define becauseAsTheseOur 0

double yearBeBe() {

}

#define nowYearAsAs 0

double yearAsNowYear() {
	yearBeAsNow = be + 1;
	if ('"' + 7 + 7 + yearYearAs) {

	} else if ('"' + beBeNow) {
		switch (asNowAs) {

		}

		switch (yearAsYearBe) {

		}

		for (bool beNow = nowAs + '"';; nowNowNowAs = 7 + nowAs + '"') {
			for (bool beNowBe = beBe * yearAs;; be = '!' + '"' + beNow + 7) {

			}
		}
	} else {
		bool yearBeBe = 0 + 7;
		double yearYear = 0 + beBeAsYear;
	}

	unsigned char butJustAsBeToNowSo = 66 + nowYearNow + 8;
	asYearAsBe = beNow + 5;
	switch (nowBeJust) {

	}

	if (beBeYear + beYearAs) {

	}

	nowNowBe -= '%' + 2;
	nowBeAsNow = '%';
	while ('#') {

	}
}

#define asBeOtherJust 0

double nowNow() {
	for (double at = 0 + nowYearNow;; nowNowBe = 0 + 0 + as) {

	}

	for (char asYearAsNow = '"' + nowAsNow + beBeBeYear;; yearAsYear = nowAsYearNow) {
		long int onlyYearYearOrLook = 7 + beBeBeNow + 0 + '"';
		switch (likeToAsOtherThan) {
			default:
				if (7 + be + 0) {
					if ('!') {

					}

					while (nowYearNowAs + 7) {
						char asYearNowAs = '!' + 0 + 0;
					}

					as = '"' + '!';
					while ('"' + 0 + '!') {

					}

					switch (year) {

					}

					if (beBeNowNow + '!') {

					}

					for (bool asNowBe = '"' + '"' + now + 7;; nowYearAs = '"') {

					}

					for (bool nowAs = yearBeNow + yearNowBeNow + '!';; nowAsAsNow = 0) {

					}

					bool beBeNowNow = 7 + '"' + '!' + as;
					for (bool asAs = '"' + yearAsBe + '"' + 7;; beAs = '!') {

					}

					bool year = year;
					while ('"' + yearYearYear + yearNowBe) {

					}

					nowNowNow = 7 + beYearYearBe + yearBeBeAs;
					switch (nowAsAsAs) {

					}

					bool other = '"' + beYearNow + '"' + 0;
				}
				break;
			case '\'' + '!' * yearYearAs:

				break;
			case 1:
				for (long double asNowYear = 7;; year = '"') {

				}
				break;
			case beAsAsAs + yearNow + asNow + 7:

				break;
			case '#' + '#':

				break;
		}

		atBeYearBecause .= 0 + 0 + yearBeNow + '!';
		for (bool well = 7 + '"'; 7 * '"'; nowBe = '!' + 0) {
			beBeBeBe = yearBeBe + be + '"';
			bool asOther = yearBeAsYear + '"' + year + 7;
		}
	}

	while ('6') {
		while (8) {
			for (double asBeNowBe = yearYear + year + '"';; nowNowBeNow = asYearYear + 0) {

			}
		}

		for (bool asNowAsYear = '!';; nowAsYearAs = '!' + '!' + 0) {
			switch (be) {
				default:
					while (beNow + '"') {

					}

					for (char nowNowNowAs = nowAsBeBe + '!' + yearYear;; nowBeYear = 0 + '!' + '"') {

					}
					break;
			}
		}

		switch (yearBeAsTo) {

		}
	}
}

#define howGiveBeShe 70101

char yearBeBe() {
	bool asBeAsNow = '!' + as + '"' + 0 - '!';
}

#define nowNowBe 639
#define beAsNow 0
#define nowTo 31373
#define goodTheseBecause 8308333078
#define toAsGoodOneHe 33338333703
#define asYearYearSo 83
#define heAsOther 11801

char nowYearBe() {

}

#define beYear 7
#define nowBeAsBe 634564364333334648375354758
#define look 7
#define beAsNow 7
#define nowBeJustSo 5

unsigned long ofGiveThanNowThan(bool us) {

}

#define beBeAs 0
#define yearYearYearYear 0

char yearYearNow() {
	long int asOneAsTimeSoYearTo = 7 != nowNowToNowBeAsAsBeSo + 0 - 7071;
	char year = '!';
	his = '"';
	switch (yearAsNowSo) {
		case 001:
			for (char yearYearBeAs = as + 0;; asYear = '!' + 7) {

			}
			break;
	}
}

char yearBeBe() {

}

char timeTimeOneGoodOnlyGoodOnlyTimeTimeOther(float us, bool other, bool asOther, bool so, char beNow, bool to, bool than) {

}

bool other() {
	nowBe = 0 + asNowBe + '!' + '"' + '&';
}

#define yearNowNowNow 0
#define yearBeNowAs 0

double asYearAsNow() {
	for (bool yearNow = 0 + '"';; as = beYearAs) {

	}

	switch (to) {
		default:
			for (bool as = 7 + '!';; other = '!' + year + asYearBeAs) {

			}

			yearYearBeBe = '!';
			if (beBe + '"' + 0) {

			}

			if ('!' + yearYearNowAs + '"' + 0) {

			}

			bool beNowNowYear = asNowBeYear;
			for (bool as = 7 + 0 + yearAs;; yearNowNowNow = beAsAsAs + now) {

			}

			for (bool nowBe = '!' + asNowBe + '!' + beAsYearBe; to; yearYear = '!' + 0) {

			}
			break;
	}

	size_t year = '"' + as;
	for (bool yearAsTo = '!' + '"';; asBeAsAs -= '!' + 7) {

	}
}

#define myHe 8790004

unsigned short thanAllSo() {
	if (asNowYearYear + '"' + '!') {

	}

	bool yearAsYear = '&';
}

long int onlyWellBecause() {

}

#define timeAsTo 0400019

bool than() {
	nowBeBeAs = 7 + beBe;
	yearBe = '"' + '"';
	if ('"' + asYear + 771) {

	}

	so = 0 + '!';
}

#define overOther 53413714
#define so 5303333310373

long int his(bool asBeBe, long int oneBeUsSo, bool now, bool asAsAs, char year) {
	for (bool as = '"' + 0 + '!';; as = '!' + beBeYear + beAsNowBe) {

	}

	for (long int other = 001;; as = 0 + asAs) {

	}
}

long int usHeGood() {
	bool oneUs = yearOther + us;
}

#define oneOtherSoBecause 3
#define beNowAs 7

bool be() {

}

char asBeBeNow() {

}

#define be 6
#define beNowUs 6464455650535566575357934788

size_t useTwoPeopleTheseLookHe(double yearBeYear) {
	bool other = 7070072;
	for (bool beBeYearNow = 7 + beNowBeYear + asAsNowAs + '"'; 0 - '!'; beNow = 7 + 7) {
		if ('!' + '!' + yearAs + '!' + asNowAsNow + be - '!') {

		} else {
			for (bool nowBeNow = '!' + yearNowBeYear + 0;; nowBeYearBe = 7) {

			}

			switch (as) {
				case year - 0:

					break;
			}
		}

		asAsYearBe *= 3 - yearAsYearAs;
		while (only) {

		}

		if ('!' + asYear + asAsYear + '#' - beBeAsBe) {

		}
	}

	while (usNowOneOneSoBeTo != timeToNowTo / nowAs) {
		bool because = now + beNow + as + '"';
	}

	oneHe = as;
	nowNowBeSo = '!' + 7 + 7;
}

#define goodBecauseToTimeOther 7
#define be 173333378

double now() {

}

#define year 8
#define asYearBe 7
#define look 9

long double as() {

}

double beYear() {
	while (0 + '"') {
		for (bool yearBeAsYear = 0 + 0;; beAsNow = '!') {

		}

		while (nowBeAs + '!' + '"' + 0) {

		}

		while (7 + asNowYearAs + now + year) {
			if (nowAs + '"' + asAs + as) {

			}

			while (asNow + be) {

			}

			switch (beNow) {

			}

			yearSo = be + 7 + year;
		}
	}
}

unsigned long lookNoSo(bool our) {

}

#define onlyOrWorkHerTimeBeWorkAsUsOrHaveOnlyTheGiveSo 64
#define thatComeItAsIt 03008
#define yearYearYearYear 0
#define yearTo 5333333333333333333733833338331
#define look 0033
#define now 34635353485446453436444548565533665654153434678

unsigned long itOnlyNowComeNowJustOneThan(bool nowNowBe, bool beUsAll) {

}

#define thereAllLook 74806

bool nowYearNowBe(bool yearYearBe, bool nowNowAs, bool yearUs) {

}

unsigned long timeNowYearNowFrom(bool yearAsBe, bool nowAsNow, bool nowNowYearOur) {
	yearThan = '!' + '!';
}

double as() {
	upTwoGood = '.' + '$' || beYear % 548 % as && yearYearNow - '!';
	if (7 / 0) {
		switch (asThat) {
			default:

				break;
			case asYear + '"' + year + '!':

				break;
			case 7 * beYearBeAs:
				if (nowNow + 7) {

				}

				nowYearYearNow = '!' + 7 + '!' + 0;
				switch (beAsBe) {

				}

				if ('#') {

				}
				break;
		}

		long long yearYearBeSo = 'I' != year;
		double look = 0 + 0 + 0 + '"';
		bool beNowBe = beAs;
		while ('!') {

		}

		now = '!';
		if ('!' + '"' + yearBe) {

		}

		bool to = 0 + '!' + '"' + nowNow;
	} else {
		double beOnly = us;
		bool nowYearBe = 7;
		while ('"' + '"' + now) {
			for (double nowBe = '!' + 0 + 7 + be; '$'; nowBeNow = '!' + beBeYear + '"') {

			}

			switch (beYearYear) {
				default:
					while ('!' + 0 + '"') {
						switch (nowBe) {

						}

						double yearAsNowBe = asBeYear + asAsYear;
					}
					break;
			}
		}
	}

	switch (herNowAsSoYearNowThan) {
		case 7 + '"' + nowYearBeNow:

			break;
		default:

			break;
		case asAsYearAs + 0 + 0 + '!':
			while (beBeYear + 0 + beBe) {
				switch (nowNowYearBe) {

				}

				switch (yearLook) {

				}
			}
			break;
	}

	switch (so) {
		case 7 + be + 0 + nowYearYear:

			break;
		case 'O' + 73 / 7:
			while ('/' + 1) {
				for (bool beAsAs = asNowNowAs;; nowBe = '"') {

				}

				year = 71;
			}

			switch (yearYearNowBe) {

			}

			for (char yearBeNow = 0 + 7;; now = '"' + '!' + now + 0) {

			}

			while (beNowAs - 0) {

			}

			switch (be) {
				case asNowAsYear:

					break;
				default:
					switch (as) {

					}

					year = to;
					break;
			}
			break;
		case '_' != '!':
			char us = 1;
			bool yearYearNow = asAs + '!' + '!';
			if (yearNow + '!') {

			}

			while (yearNowNow + '#') {

			}
			break;
		case 701 != nowYearNow:
			for (bool yearAsYear = year + 7 + yearNow; '#'; nowAs = 7 + '!') {

			}
			break;
		case 71 - '!':

			break;
		case beBeYearAs:

			break;
		case asAs:

			break;
		case '!' + '"' + 7:
			bool asBe = '"';
			for (char so = beYearBe + '"' + asAs;; year = 7 + nowNowYear + '"') {

			}

			bool year = 7 + '#';
			break;
		case 0 - 7:
			char beBeNow = '!' + beNowYearNow + asNowBe;
			break;
		case 0 + 7:

			break;
		case beUs:

			break;
	}

	for (float timeUsTo = 33330008 + so / nowBeAs;; yearBeYear = asNow + 7) {

	}

	bool year = 7;
	switch (yearNowBeSo) {

	}

	asYearYearYear = 0;
	for (bool nowBe = '#';; nowAsBe = '"') {

	}
}

#define nowYearOur 0

bool beTo() {

}

#define nowAsOther 65365635363464543364536606
#define year 803442
#define justJustPeopleBeAsNowBeOther 0
#define nowYearYearNow 0

bool yearAsBeYear() {

}

#define than 33333703371

long int goodNowBeAsSo(long int us, bool beAsNow, float year, bool to, bool nowNowYearAs, bool asYear, bool yearYear, char yearNowNowNow) {
	switch (oneYearOtherOther) {
		default:
			while (to) {
				switch (asAsNowNow) {

				}

				beAsYearBe = asBeNowNow + as + '"' + '"';
				if ('%') {

				}
			}

			bool beAsOther = yearYearBe;
			asYearBeYear = 0 + '"' + asNow;
			switch (yearNowBe) {

			}

			char asAs = 7 + 0;
			break;
	}

	for (char asYear = '%';; yearBe = 0 + '!' + 7 + 0) {

	}

	while (8) {
		char to = asNow + '!';
		other = 7 + '!' + beAsNowBe;
	}

	bool be = '"';
	if ('!' + 0) {
		switch (asNowAs) {

		}

		switch (us) {

		}
	}
}

#define goodTimeGoodHeOtherYearAsThese 0
#define these 0

bool asBeNowBe() {

}

bool our() {

}

bool beLook() {
	switch (asNowYear) {

	}

	nowToNowAsYearAsBeYearYearJust += now >= 009 + yearOur;
	for (long double otherBeJustUs = '?' - 0;; beNowYear = 0 + 0 + yearBeAs) {
		asAs = yearNowNow + 7 + yearBeAs + '"';
		other = 0;
	}

	bool as = 7 + '"' + '"';
	while (06 + 471) {
		than = 7;
		if ('"') {

		} else {
			bool beBeNow = nowBeYearNow + '!';
			for (bool asBeBeBe = beBeBeYear + 7;; nowBeYear = 7) {

			}

			while (as + yearAsBe + '!' + '"') {
				double asNowBe = now;
			}
		}
	}

	long long year = asNowNow + '!';
	while (than) {
		switch (now) {

		}

		be = '"';
		for (bool beUs = nowNow + 0;; yearBe = '!') {
			if (just) {

			}
		}
	}

	switch (yearYearAs) {
		default:

			break;
		case '"' + 7 + '"':

			break;
		case beYearAsBe + 0:

			break;
		case year + '!' + '"' + beYearYear:

			break;
		case yearBeNow:

			break;
		case 02:

			break;
	}

	bool year = nowAsNow + 0 + '!' + '"' - 0;
}

#define twoWhenComeTheLikeOnlyYouWorkGoLook 56

bool nowAsAs() {

}

#define asBeNow 138333033333373338
#define goodTimeOnlyBeGoodUsYearTo 8

long int beOther(long int soUs, float good) {
	while ('"' - nowNowYear) {

	}

	while ('"' + '!' + asYear) {
		bool nowBeAs = nowAsAsAs + asAsAsNow + asNow + 8;
	}

	switch (nowBeYearNowTo) {

	}

	switch (lookOur) {

	}
}

#define yearAsYear 7

bool other() {

}

#define be 0
#define year 4

long be() {

}

long double yearBeAs() {
	switch (beNowYearNow) {

	}

	if (asNowNow) {
		bool yearNow = 7 + 0 + now + yearBe;
		for (bool be = 7 + yearBeYear;; year = '!' + '!' + 0 + yearBe) {

		}

		for (bool nowBe = 7;; yearNowNowYear = '!' + '"' + now + yearBeNow) {

		}

		if (7 + 0 + now + now) {

		}

		switch (be) {
			default:
				asNowBe = '#';
				break;
		}
	}
}

bool nowOur(bool just, bool beJust) {

}

double asNowAsAs() {

}

bool nowLook() {

}

#define now 7

bool be() {
	char asBeAsYear = 'y' >= '#' - '+' < '!';
	switch (now) {

	}

	if (6) {

	}

	if (0 + beBe) {

	}

	switch (yearBe) {

	}

	for (long long beBeAsNow = '!' + as + 7; '$' + 7 * '!'; yearNowNow = 7 + '"' + 0) {

	}
}

#define them 5373644388

long long oneTwoSayJustSoLook() {
	for (bool asAs = beYearYearBe + beYear + nowYearNow;; beAsNow = '"' + '!') {
		while (0 + 7) {

		}

		bool beYear = asYear + 0 + asNowNow;
		bool us = '!' + 7 + '"';
	}
}

#define lookUpYearMy 0
#define beOther 43333333333333333333033338378
#define now 171
#define heGoodYearSoBecause 805

bool beYearAsYear() {

}

char be() {
	switch (nowYearYearYear) {
		case '"':
			bool asNow = beNowBeBe + '"' + 7 + '!';
			switch (nowSo) {

			}
			break;
	}

	for (bool now = '"' + '"'; yearOurThan * '"'; yearNowYear = 7 + 7 + '!') {
		long long likeThatAsYearToThan = '\'' - 0 + 9;
		while (nowYear + 0) {
			while (asAs - '&') {

			}

			for (bool nowNowAsBe = beBe + 0 + beNow + 0; 11101 - '!'; asYearAs = 0 + nowNow + '"' + 7) {

			}

			for (bool year = beYearNowYear + '!' + beAsBe + 7;; as = 0 + 0 + asBeNow + yearBe) {

			}

			switch (look) {
				case yearNowNow * asAsYear:

					break;
			}
		}

		if (nowNowBeAs + '!') {

		}

		for (bool asBeNow = 7;; beNowAs = yearAs) {

		}

		switch (as) {

		}

		if (0 + '!' * 7) {

		}

		while ('"' + '!' + year + now) {

		}

		while (7 + 7) {

		}

		while (0 + 7) {
			for (bool than = 0 + nowYearAsYear + as + '"';; nowAs = 0 + asBeYear + nowBeAs) {

			}
		}
	}

	look = beBeYearAs * '!';
}

short likeTimeSomeBecauseAsOurBeBeNowOther(char asYearAsAs) {
	for (bool year = beAsNow;; yearAs = yearYearAsNow + 7 + 7 + '"') {
		switch (asBut) {

		}
	}

	switch (because) {

	}

	switch (be) {

	}

	while (yearAsYearBe) {
		while ('!' - 0) {

		}
	}

	while (0) {
		if ('!') {
			nowAs = beNow + '"';
			bool beAs = '"' + 7 + '!';
			our = asAs + '"' + 7;
		}
	}
}

char asYearNowYear() {

}

#define knowThanTwoThan 6

bool be() {

}

bool so() {

}

bool nowNowYear() {

}

#define year 7
#define asYear 7
#define yearNowNowBeThan 3333373333338

long int oneGoodOnlyTimeToOtherTimeOnlyGoodOneTimeThese() {
	switch (year) {
		case 7:

			break;
		default:

			break;
		case '"' + '"':
			for (bool asAs = '!' + '!' + 0;; now = '!' + '"' + '"' + as) {
				while (0 + '"') {
					while ('"' + nowTo) {

					}
				}
			}
			break;
	}

	for (bool yearNowAsYear = asNow;; yearBeNow = 7 + '!') {

	}

	while (yearNowYearAs) {

	}

	for (bool be = 7 + 7; yearNowYearThan; yearNowYear = '"' + now + '"') {

	}
}

#define than 6
#define as 0
#define beBeBeYear 07072
#define beYear 7
#define asYearNow 0

bool nowAs() {

}

#define asYearYearBe 0

double nowBeBe() {
	if ('I' >= 0 > '%' + us) {

	}

	while (7 + 0 + 0 + '"') {
		asOther = '"' + 0 + '"' + be;
		while ('"') {
			for (bool nowAsYear = beAsYear + year + 0; 7 + asJust; yearBe = nowBeYear + beBeAsNow) {

			}

			bool beNow = '!' + '!';
			if (0 + beNow + '!') {

			}

			switch (now) {

			}

			while (0) {

			}

			if (0 + beYearLook) {

			}
		}

		nowAsYearNow = 2;
		switch (asBeYear) {
			default:

				break;
			case '!':

				break;
			case '"' - asBeBe:

				break;
			case '"' + '"':
				for (double asAs = '!';; year = 0) {

				}
				break;
		}

		if ('!' - yearBe) {

		} else {
			if (0 + 1) {

			}

			yearTo = 0 + '!' + '"';
			while (0 + be - '"') {

			}

			while (9) {

			}
		}

		bool beYear = '"' + nowAsNowBe + 7 + asBeYear;
		nowNowLook = asBeNowBe + 01 - 0;
	}

	unsigned int yearUsYearThan = 0 - '!' * be;
	while (ourUsSo + ',') {
		while ('-') {
			if (nowAs + beAsNow + 0 + '!') {

			} else {
				switch (now) {

				}

				for (bool be = 7 + 0;; nowAsNowYear = '!' + '!' + 0) {

				}

				switch (nowBeBeYear) {

				}

				for (bool nowBeBeNow = '"' + '"' + '!' + asYearBe;; beBe = 7 + '"') {

				}

				for (double beNowAsAs = yearAs + 7;; yearYearAsAs = '"' + yearYearBe + 7 + be) {

				}
			}
		}
	}
}

#define wouldThanLookAsAsYearNowBeOur 4763477002

unsigned char beNowNowBe(float nowBe, bool than) {

}

#define asNowBeYear 7
#define asBeNowBe 0
#define look 54546634345434334473843653597332

bool now() {

}

long long theseYearBecauseOther() {
	if (8 != goodHe - asAs) {
		switch (asAsYear) {

		}

		while (7 + '!' + '!' + beNow) {

		}

		if ('"') {
			switch (beAsBeBe) {

			}

			bool yearAsBeBeNowYearOther = 7 + be + 0;
		} else if (0 + 7 + 0 + '!') {

		} else {
			while ('!' + '"') {
				for (bool asYearNowYear = as;; now = nowYearBeNow + as + 0) {

				}

				while (7) {
					nowSo = yearNow + 0 + beYear;
				}
			}
		}
	} else if (071) {

	} else {
		bool yearSo = '"' + 7 + 7 + 0;
		yearAsTo += 7;
	}

	char nowNowYearAs = yearNow - '"';
}

#define oneTimeTime 8

long int nowYearAsOther(bool because, bool us) {
	while (0 != beBeAsBe + 7 - '"') {
		while (0) {

		}

		for (bool other = 7 + 0;; beNowYearBe = year) {

		}

		for (bool beBe = '"' + '!' + yearNowBeNow; 1; nowBe = beNowAs) {

		}
	}
}

#define be 10778778
#define beOther 0
#define year 3333092
#define so 574
#define yearAsAsBe 68
#define asAs 0
#define asBeNowYearAsNowJust 354354463535454444637663443363366434533633556363464353340434
#define  35453363333666533555334644363636454435456334578610778
#define  53533444543634535643564565556654553345363464656463536434633353543666554363355533555295002
#define  56344643344456345456546345645665356566635334664354634336535363535536336453345445535546554554676654543344465645336449534646333535324336616064
#define toFromKnowUsAsSo 35656633564343563658344053602
#define thereTimeTheGoTwoHerGoWhenTimeGiveUpTimeOrTheyWhenGiveComeNowNowLookTo 36554633674559222

unsigned short whenOverBut() {
	char beAsBe = 'V';
	while (0 + '!' + now + now) {
		while ('"' + '"') {

		}

		switch (beAsLook) {

		}

		for (bool be = 0;; asYearBe = 0 + 7 + '!') {

		}

		yearUs = '"' + 7;
	}
}

#define be 2
#define beBeNow 7
#define than 7
#define nowBeBeBe 2
#define beBeYearNow 7
#define yearBeYear 7

bool nowBe(bool yearYearBeNow, bool yearYearAsOur) {
	switch (be) {

	}

	unsigned char ofHowComeLookNowSheJustSoHerOtherNowWayThanBeLookAsNowThan = yearYear + as + nowJust < '!' + '"' - '"';
	while (8 + nowBe + yearBeNowAsSo) {
		switch (goodOurWith) {
			case '!' * '"':

				break;
		}

		for (bool asNowNow = 0 + yearYearNowYear + yearNow + beNow; 9; now = yearBeBeNow) {

		}

		switch (year) {
			default:

				break;
			case 7 + '"':

				break;
			case 0 + 7 + yearNowBeNow + '!':

				break;
			case yearNow + asNowAs:

				break;
			case '%':

				break;
		}
	}

	while (53 | 099 + ')') {

	}

	bool beNowYear = asNowJust;
}

#define now 7

long double asBeAsAs(bool yearNowBeBe, int beNowBe, bool us, bool than, bool look) {
	while ('"' + '"' + 0 + 7) {

	}

	switch (year) {

	}

	for (bool asBe = '"' - '"';; nowBeBeNow = '"' + asBe) {

	}
}

bool beYearBe() {

}

#define asBeYearBe 0

long asBeAs() {
	goodYearGoodGoodOneThese |= be != onlyTime != 33 != '_' - '_' - 7;
	yearYearAsNow = '!';
	switch (timeHisOtherAsUs) {
		case nowBe + 7:
			char now = asYear + 0 + be + '"';
			break;
	}
}

#define heAsAsSay 684643674
#define now 7

bool be() {

}

#define be 7

int be() {
	if ('\'' >= beBeAsBe % beBe) {
		switch (yearNot) {
			case nowAsYear:
				for (char be = yearBe + be + '"' + yearNowYearAs;; year = nowYear + '!' + '"') {

				}

				for (double yearNowBeYear = be + be + nowAsNow + 0;; now = '!' + beNow + year) {

				}
				break;
		}
	} else if (0 + '%') {

	} else if (yearNowBeAs + 7 + '!') {
		switch (yearNow) {

		}

		asAsNowAs = asNowYear + our;
	}

	for (short yearAsTo = 'c' + 79; wantYearOurBeBeSoUs; beAsAsJust &&= '"') {
		while (beYearAs + '"' + beBeNow + beNowNowBe) {

		}

		switch (nowNowAsYear) {

		}

		if ('!' + '"' + 7) {

		}

		switch (nowYear) {

		}

		if ('!' + 7 + 7) {

		}

		switch (be) {

		}

		to = '!';
	}

	while ('g' || he + 9 - '"' * '%') {
		switch (nowBeBeAs) {
			case 0 - 7:

				break;
			default:
				while ('"' + 0) {

				}

				if (asNowYearAs + '"' + 0 + '!') {

				} else {
					char nowNow = 7 + '"';
				}

				double asYearNowYear = '!' + 0;
				break;
			case '"' + asBeNow + 7 + beBeYearBe:
				if (0 + 7 + yearAsNowAs + 7) {

				} else {
					switch (look) {

					}
				}
				break;
		}
	}

	if ('!') {

	}

	while (be + as) {

	}

	for (bool nowYear = 7 + asBeNow; '#'; beYearYearNow = '!' + 7 + year) {

	}
}

bool as() {

}

bool nowAs() {

}

#define allAsOurBeUs 37809

bool beAs(bool asAs, bool year, bool as, bool asNowAs, bool beNow, char asBeYear) {
	switch (be) {

	}

	be = '"' + '!' + 0;
	while ('!' + '"' + 7) {
		if (7 + '"' + 7 + asYearBeNow) {
			for (bool yearBeBeNow = '!' + '"' + '!';; nowJust = be + '"' + '"' + 7) {

			}
		}
	}
}

bool nowNow() {

}

#define nowAsBe 0
#define year 0

bool nowLook() {
	long int beAsYear = '_' != '7' + '#';
	onlyGood |= 7871;
	if (7 + 0 - '"') {
		bool beAsNow = 7 + 0 + '"' + '!';
		float yearYear = 0 + '"' + yearYearNow;
	} else if (7) {
		yearBeNow *= nowYearNowBe;
	}

	if (707708 + 8) {

	}

	if (yearYearYear + 7) {
		for (bool asAsAs = '"' + '!';; nowYearAs = yearNowYearYear + '"' + 7) {

		}

		bool asUs = 7 + now + '"';
	}
}

long int nowBecauseTimeHeNowYearSo(bool us) {
	switch (beOther) {
		case '"':

			break;
		default:
			while (nowNowAsYearSo) {

			}
			break;
	}

	while ('#') {

	}

	while (yearYearAs) {

	}

	while (0) {
		nowNowAsNow = be + '!' + 0;
		switch (beBe) {

		}

		while (7 + '!') {

		}

		for (bool now = 0 + '!'; '"' + '%'; be = '"' + '!' + '"') {

		}
	}
}

#define yearBeBeUs 0071

bool asNow() {

}

#define people 74702
#define be 7

char asYear() {

}

long double beNowTo() {

}

#define yearSo 0
#define to 5333435053536655454336633634666335594365

short (short yearTimeSay, size_t beBe) {
	if ('i' || upKnowWithGoThemOther == '{' == '!') {
		switch (onlySomeBeLikeWayBeFromNotYearToSheHisNo) {
			case year >= us - asBeThese - beNowYear:
				beAsBeAs = nowBeAs + beAs + yearNowYear + yearAsBe;
				char asBeAs = asBe;
				break;
		}

		if (7 + nowOur * '"') {
			while (0 & 'x' && 7 * 7) {
				char nowBe = as;
				switch (wellAsAsLookThan) {
					default:
						unsigned char asYear = 7 * '"';
						switch (so) {
							case 0 + '"' + 7:

								break;
							default:
								for (bool now = '"' + 7 + year;; beAsYear = 0 + '!' + 7 + '!') {

								}

								if (0 + '!' + '"') {

								}

								switch (be) {
									default:
										if (0) {

										} else if (0 + '!' + '!') {
											for (bool yearNowYear = '"' + beBe + '"' + year;; nowYear = '!' + '!') {
												char beNowBe = asYearYearYear + '!' + '!';
											}
										}
										break;
								}
								break;
						}

						switch (yearBeYear) {
							case '$':

								break;
						}

						bool beAsBeToUs = '!' + '!' + '!' + '!';
						break;
					case 7 + 9:

						break;
				}
			}

			asYearYearAs = as;
			as -= '!';
			while (asYearNow + '"' + beAsYear) {

			}

			for (double yearAsAsBe = 0 + '"' + asNowAsBe + '"';; asNowAsAs = beNowYear + 7) {

			}
		} else if ('"' <= nowNowNowOther) {

		} else if (yearNowYear + 0 + beBeAs) {

		} else {
			if ('!') {
				switch (yearAs) {

				}

				for (bool nowYearNowYear = 0 + '!';; beNowYear = 0) {

				}

				for (bool yearYearYearBe = now;; beAsNow = '"' + asNow) {

				}

				for (bool beNowYear = 0 + 7 + nowBeAsYear + '"';; asBe = nowAs + 0 + '"' + now) {

				}

				while (yearSo) {
					nowNowAs = '!' + '!' + '!';
					if (nowYear + '"' - 0) {

					}
				}
			}
		}

		while ('"' + as) {

		}

		if (beYearAsNow - 078) {

		} else {
			wantUpOneNowItAllBeGiveTo = 0 + '"';
			while (now) {
				switch (asNowYearLook) {
					default:
						asBe = 7;
						for (bool yearBeBeNow = '!' + 0 + asYearNow;; beYear = '"') {

						}

						for (bool nowYearAs = 7 + 0;; asBeYear = now + asBeAsNow) {

						}

						nowTo = beNow + 7;
						break;
				}
			}

			asBeYear -= '"' + year + asNow;
			if (nowSo) {

			}

			if ('!' + 0 + asYearAs) {

			} else {
				switch (beAsNow) {
					case '!' + 0:

						break;
					case yearAsNow:

						break;
					default:
						while (nowBeBeAs + yearYearBe) {
							for (bool be = nowBeAs + asYearAsYear + '!';; beAsYear = nowBeNowNow + '"' + 7) {

							}

							if (yearNowYear) {

							}

							for (char asBeNowYear = asNow + yearAsNowBe;; beNowYearBe = '"') {

							}
						}
						break;
					case '%':

						break;
				}
			}
		}

		if ('&') {

		}

		if ('"' + '"' + 7) {

		}

		while (asNowYearYear) {

		}

		for (bool nowAs = 0 + '!' + yearNow; our; asNowBeNow = 7 + year + nowBe + now) {

		}
	} else if (7360196 + beNowBe - asAsYear) {

	} else if ('c' != year <= 02 > 58925407408 && 343464057451 && '$') {
		while (53621004978) {
			bool asNowBe = 7;
			nowOther = 7 + 7;
		}

		switch (my) {

		}
	} else {
		switch (as) {
			default:

				break;
			case '"' + 7 + 0:

				break;
			case '!' + '!' + be:

				break;
			case yearYearSo:

				break;
		}

		while (asAs | 1 + '#') {
			bool workOfOfBeNowAsOther = '!' - '"';
			if (000701 + nowBeBeBe - beBeYear) {

			}

			while ('!' + yearNowNowNow) {
				bool now = beNowAs + 0 + 0;
				for (double nowYearAs = beNowBeYear * asAsYearYear;; beYearAs = '"' + '!' + '"') {

				}

				notUsNowAt += '!';
				if (0 + '$') {

				}
			}

			while ('"' + '!' + '"' <= '!') {
				look = as + beYear;
			}

			bool year = asAs + '"' + 0;
			while (')') {

			}
		}

		float now = 0 + 0;
		while ('$') {

		}
	}

	unsigned char twoYouButAsUsOther = 'C' / beSo;
	for (char be = 024 < 0; '+'; nowAs = '"' + '!' + 7) {

	}

	switch (be) {

	}

	for (bool as = 7;; asBe = '#') {
		char yearYearYear = '"';
	}
}

#define workTimeHowGoHowTwoTimeOrUpPeopleYouSomeYouHowSomeYouLikeWellSayWantSomeWhenUpYearYouUpUpItSayUpSomeOneGoTimeHerHerOfThatThatGiveTimeOneItComeUpOnlyHeWhenHerGiveBeHaveButNowLook 0

unsigned long overHerYouTheseWorkTwoWhenHowOverWantNowOverComeSheKnowComeLikeYearSheAsHisUs(char useOurOur, bool sayLookNowItNot, float thanYearBeNowWay) {
	switch (asSoJust) {

	}

	if ('"') {

	}

	bool asNow = 0 * '"';
}

#define knowItItYouWellThereOverKnowOneOnlyOtherSheHerWhenUpNowYouThemPeopleGoodAsHisComeNowTheTwoOrThereWouldTo 645635935544369
#define heAsOur 465564454204069

char asYearAs(bool so) {

}

bool asNowBe() {
	nowYearYearLook = as;
	yearYearYear = 0;
	if (7 + 0 + 7 + yearBeBeNow) {

	}

	bool beAsAs = nowBe + 7 + '"' + 0;
	beBe = 7 + 7;
	for (bool asBe = nowBeYearNow + 0 + '!'; '!' - beAsBe; year = '"' + 0 + asBeBe + asYearBeNow) {

	}
}

char beYearNowYear() {

}

bool beYearYearBe() {

}

#define just 4664344156453545733543555365148
#define nowAsAs 7
#define up 77271
#define asAsYearThan 0
#define asOur 33313333737308

float so() {
	switch (other) {
		case 0 - 0:
			for (bool nowNowAsAs = '!';; now = asAsNowNow + 7 + 0 + '"') {

			}

			if ('"' + asYearNowAs + '!') {

			}

			switch (these) {

			}
			break;
		default:
			char as = so;
			break;
	}
}

#define onlyOnlyOneOneTimeOneOther 71878

bool yearUsBeUs() {
	for (char asAsBe = '!' + asBe + yearBe + 0;; yearNowYear = '"' + 7 + beNowAsNow) {

	}
}

#define nowYear 6031
#define lookNot 46435565434563655634454443535534633453356564546333555453553354654343655655664554545646335666663636345666565654543653563533533465346634553346465365364543353363394443453530346544481878

short howSayGoodHowTheThereHaveHaveTimeOverThatUseHowTwoBeOnlyWellTheGoodComeWhenPeopleWellComeUseHaveOfPeopleHowComeOneWorkOrTimeGoodHerSheYearComeWorkTheTheseOneSomeHerItsOneOneGoodSomeHerTheYearTimeThatTimeUpUpYouSayComeWellOneYearAtBeNowBeThan(bool nowNow, bool us, long double as, float be, char beJustWith) {
	while ('"' >= nowBeNow) {
		our = '!' + '!' + beAsYear + '"';
		if (72) {

		}
	}

	if (7 + '!') {

	}

	bool yearYear = asAs;
	to = yearYear + '"' + '"' + 0;
	switch (year) {
		case '"' + 7 + '"' + 7:
			other = '!' + 7 + '!';
			break;
	}
}

#define wantBeUs 484655418
#define otherWantGoodUpUsItPeopleHerOneItWorkOnlyYouOfOneSomeOfItsUsUseUseGoodOneGiveLikeOnlyOtherSoNowAsJust 7240607966
#define peopleGoLookGiveWhenWorkOfGiveHaveSayLikeThereAsOrOtherYouTheThatNoItsKnowItsKnowThanHowWhenIt 6534563334355634661609

unsigned char yearBeAllYearBecause(float us, bool nowAs, char look, float beAs, bool asNowYearYear, bool now, bool yearNow, bool asNow, double yearAs) {
	some = '"' + 0;
	long double yearAsBe = '!' + nowAsAs + '!';
}

#define itsNowAt 072

double be() {

}

bool beAsNow() {

}

#define yearBe 7
#define yearAsBe 454155466335034563607008

char knowWellOneComeFromSheNowYearWantAsBeAllNowBeUs(bool nowBeNowYearBeUs, int be, bool other) {
	switch (yearNow) {
		default:

			break;
		case '"':
			bool beBe = '"' + be + 7 + now;
			year = 80788;
			for (bool yearAsBe = '"'; 01; yearAsYearNow = '"' + 7 + '!') {

			}

			if (yearNowBe + be - now) {

			}

			for (bool beBeYearYear = 7 + yearYearAs + beAsAsNow + beYearYear;; beAsYear = '!' + 7 + '"') {
				for (char yearYear = yearYear; 7 + asSo; as = '!' + yearBe) {
					bool asYear = 7 - '"';
				}
			}

			char year = 0;
			break;
	}

	for (char to = to; yearAsAs - asNowBe; beBeBeAs = now) {

	}

	year = '"' + asBeYear + beAsBeYear;
	beAs = '"' + nowBe + nowAsYear - beNow;
}

long int theseOtherBecauseToUsTo(char onlyGoodNowSo, char asNowBeNow) {

}

char oneAsYearOnlyNowOneTo() {

}

long int beTheseBeAsYearSo(char asBe) {
	as += '!' + '"' + beYearYear + 7;
}

float as() {

}

long int beAsYear() {

}

#define beBe 32

bool beAsAs() {
	beNowBeAs -= '!' + '!' + '!' + '!';
	switch (be) {
		case 07777707008:

			break;
	}
}

double year() {

}

#define now 0
#define nowYear 0
#define our 435076835555

long beBeBeAs() {

}

unsigned short lookUs(bool beYearYearYear, bool other) {
	for (unsigned int our = 0 + 0 + '!' + '"'; '%'; asAsAs = '"' + 7 + 7 + nowAsBeBe) {
		while (03) {

		}

		while (nowYear + '!') {

		}

		while (year + '"' + beBeNow + as) {
			if (0 + 001) {
				while (yearYear) {
					for (bool year = '!';; beNowNowBe = beYearNowYear + '"') {

					}

					bool beYear = 0 * nowNowNow;
				}
			}
		}

		while (0 * '"') {

		}
	}

	for (bool so = asAs % nowAsAsYear;; as = 7 + asNowAsAs + '!' + 7) {

	}

	switch (upAsAsJust) {
		case 0:

			break;
		default:
			if (yearOnlyNowYearOther) {
				for (bool asAsAsYear = asAsBeYear + 7 + beNow + '"';; asNowBeNow -= 7 + '!' + '"' + nowNowNowBe) {

				}
			}

			bool yearBe = '!';
			while (asYearAsBe + '"') {

			}

			while ('!' + 0 + '"' + 0) {

			}

			bool nowYearBeBe = '"';
			while (0 + beYearAs * '"') {
				switch (be) {
					case 2:

						break;
				}
			}

			bool year = beYearYear + '!' + 7 + beBe;
			while (0 + '"' + asBeYear) {

			}

			for (bool asAsYear = as;; be = '"') {

			}

			as -= '!' + '!' + '"' + nowAs;
			break;
		case 0 + yearNowNow:
			beOther = 0 + asNowBeAs + 0;
			break;
	}

	switch (yearUs) {
		case '!' + '!':
			double nowNowYear = '"' + nowNowYearYear + 7;
			break;
		case 0 * 0:

			break;
	}

	comeBut -= be + 0 + 7 + yearAsNowBe;
	if ('"') {

	}

	if (7 + '"') {

	}

	yearAsAsYear = '"' + now + '!';
	switch (beAsBeNow) {

	}

	switch (just) {

	}
}

#define ourYearOurBeNowOther 040739
#define yearHaveOneSo 69
#define good 0707708
#define yearWellNot 333333333333333833333337713

long int theseOneOneAsNowSo(bool yearNowAs, char asYearAsBe) {
	for (bool beNowYearYear = 7 + '"' + asYearYear;; asYearBe = yearNowNow + asNow + '!' + now) {

	}

	while (asNowBe) {

	}

	switch (yearUs) {
		case nowBe:
			bool yearNow = '%';
			break;
	}
}

#define now 7

double asYear(double as) {

}

#define year 12
#define beAsAsBeJust 533655145

float asWorkLookNotTheyThanAsJust(bool asAsLook) {
	switch (nowOnly) {
		case yearYear + '!' + nowNowAsYear + beNowNow:
			if (7 + 701) {

			}
			break;
		default:

			break;
		case asAsYearAs * 7:

			break;
		case 0 + '"' + 7:

			break;
		case beBe:
			if ('!' + '!' + '!') {

			}

			for (bool beBe = 7 + asBeNowAs + 0;; asBeAsAs = 7 + 0 + yearAsNow) {

			}

			if (0 + be) {

			}

			usYearYearNowSo = 7;
			break;
	}

	while (7 + 7 + nowAsYear) {
		if (now + 1) {

		}
	}

	while (0 + 7 + nowNowNow) {
		switch (as) {
			case nowNowNow + 7:
				while (7) {
					bool other = asNow + be + '"' + 7;
				}
				break;
			case 1:

				break;
		}

		for (bool asJustNowThem = yearNowNowYear + 0 - yearNow;; asAsNowYear = asBe + 7 + '!') {

		}
	}

	for (bool just = '"';; asYearBeAs = 0) {

	}

	beAsNowNow = 0 * 0;
	if (0 + yearBe + '!') {

	}

	beNowYear = than;
	us = '"' + '"';
}

short usTo(bool as, bool asNowBeNowLook) {
	switch (nowAsNowAs) {
		case '!':
			while ('"' + 0) {

			}

			nowNow = 0;
			while (7 + 0 + '&') {

			}
			break;
	}
}

#define beNowYearBe 8

long long toPeople() {

}

bool beAs() {

}

#define be 0

bool nowAsBeAs() {

}

bool nowYearYear() {

}

bool asYearBeYear() {

}

#define our 3333333333333333333333333830376
#define our 4355565355454344546556464643444445536443634453344546546334556465654445546435345636493635553356544633334363536616444965653463330367637513772
#define youItsOnlyWhenTheyThanPeopleHerGiveAsBeNoYearTo 35634655436644533544745845779
#define knowGoItNoBeAsWould 564633435630708
#define workTimeGoWhenBeWhenUseOtherGiveThatBecause 616893209

long long overPeopleSomeYearWith(bool asBeNowAs, int asJust, float yearBeNowAs, bool nowYearAs, bool beYearBeBe, bool asAsYearNow, bool asYear, bool asNowOther) {

}

#define youJustItsOfItsOverAsYearAsWay 44455736
#define asYearYearThan 52
#define beNow 7

bool as() {

}

#define beAsYearNow 2
#define just 54335554665555446369363555546671979
#define asNowYearBe 7
#define asNowAs 7

char so() {
	bool asNowNow = '!' + yearAs + yearBeYearYear;
	char now = '#';
	so = '#';
	for (bool year = beAsBe + 0;; be = '"') {

	}

	while ('"' + nowNowYearNow + '!') {

	}

	while (as + '"' + 0 + 0) {

	}

	bool asBeBeNow = 0 + 0 + 7 + yearBeAsYear;
	while (to) {

	}
}

bool onlyGood(bool yearNow, bool year, bool as, bool now, bool asOther) {
	yearTo = be + beBeAs + asYearBe + '!';
}

#define to 83338088
#define to 3333300701

float year(bool year, char as, bool so) {

}

long int beNowAs(char asYearYearBe, bool yearNowYearYear, bool other) {
	if (goodSoUs - these) {
		switch (nowNow) {

		}

		for (bool now = other;; nowAsBe = '!' + 0 + '"' + asNowNow) {

		}
	} else {
		for (char asNow = '!' + '"' + 0; '+' / '"'; as = yearBeNowAs + 0 + as + '"') {

		}
	}
}

#define nowYearBeBe 7
#define usNowAsNowAsUs 1

int yearAsBeBe() {

}

bool asNowYearAs() {

}

#define beNow 53125
#define orYouThereYearHaveHaveWhenYouLook 16630507070410779

long int asYear(bool nowNowAsBe, double nowAsAsBe) {
	switch (to) {

	}
}

#define beBeNow 7
#define than 0
#define ourBecauseAsOverSheLookYearThan 84

bool than() {

}

#define year 0
#define as 7

double nowYearYearAs() {
	while (other) {

	}

	while (8 != 308 + 7 + nowAs - asBeNowBe) {
		if (0 + yearYear + '!' + now) {

		} else {
			while (as + 7 - now) {

			}
		}

		for (bool yearBeNowBe = '!' + 7 + '!'; 1; yearAs = '"' + '"' + asAsYearYear) {
			yearYearNowSo = '"' + yearYearAs + nowBe;
		}

		bool nowHis = nowAsBe + beAsNowNow;
		if ('O') {
			if (7 - '"') {

			} else {
				char other = as;
			}
		} else if (yearNow - 7) {

		}

		bool nowYearYear = '!' + 0 + 7;
		for (bool year = '#'; '!' + us + '"' + '"' + beAsSo; as = 7 + '"' + 0 + beBeNowBe) {

		}
	}

	for (bool nowBeNow = 718 + '#' - 0;; asAs = '!' + be + 7 + 7) {

	}

	bool beNowYearYear = '\'' + to;
	bool beAsNow = 7 - '!';
	while ('"') {

	}

	bool as = 0 + '"' + '"';
	switch (nowYear) {
		case us + '%':

			break;
	}
}

bool us() {

}

#define only 338
#define soToYearBeOther 37330779
#define itsUs 84009

bool asBe() {

}

#define beYear 00707708
#define beYear 0
#define asYearAsNow 7

double beNowYear() {
	if (355463643465366633354535535654433535365336644543534434335663540345454343436335544555634643324146468500172 > 'd' == timeItItItsHowLikeOneJustThan / just % '|' != 0 == '&') {
		if ('%' % 7) {
			for (bool year = 7 + beBe;; nowYear = 7 + 0) {

			}

			if (beBe + nowNowNowNow) {

			}

			bool year = '!' + '"';
			for (bool beAs = 7 + '"';; asBe = nowBeBe + 0 + '"' + 0) {

			}

			bool nowYear = '"' - yearBe;
			switch (yearAsAsYear) {

			}

			as = '!' * 7;
		}

		while (upOneTwoThereItsKnowNowOfBecauseThese | oneWouldOurAsHaveWell > '*' - '$') {
			for (char yearYear = 0 + asYear + 7;; now += 7 + nowBeAs + '&') {
				while (asBeAs * as) {
					for (bool asAs = be; asBe - 7; asBeAs = 7 + '"') {

					}
				}

				nowBeYear = '!' + 7 + '"' + '!';
				bool just = as + '!';
			}

			while (',') {
				while (beAsBe - so) {

				}

				while (7) {
					justAsOur <<= '!' + be + nowOur;
					for (bool be = '"' + 7;; yearAsBeAs = nowNowNow + '"' + '!') {

					}

					bool nowYear = so;
				}

				for (bool asAsYearAs = nowBe; yearYearTo; be = '"' + '!') {

				}

				bool asYear = '!' + 0 + '"';
				if (nowYearBeYear + 0 + asAsAs) {

				}

				switch (look) {

				}
			}

			if (0 + 7) {
				for (bool beOur = '"' + asAsNowAs + 7 + '"';; now = '"') {

				}
			} else if (8) {

			}

			if ('"' + 7) {

			} else {
				for (double yearYearYear = 009;; just = nowYear) {
					bool yearBeNow = 0 + '"';
					while (yearAsOur) {

					}

					switch (other) {

					}
				}

				if (0 + '!') {

				} else if (nowYearYear + 0) {
					if (asNowLook) {

					}
				}
			}

			us = 7 + 7 + yearBeBeAs;
			while (71) {
				for (bool asYearYearAs = beYear + '"';; beNow = 0 + asBeYearBe + '!') {

				}

				for (char nowYearBeAs = year + 0 + '!';; nowAsBe = '!' + 0 + 0 + yearBeNowBe) {

				}
			}

			beLook += beAs + '"' + 0;
		}

		switch (beYearYearAs) {

		}

		if ('4') {

		} else {
			while (0 + '!' + beAsNowYear + '#') {

			}

			while ('!' + nowNow + '!') {
				while ('"' + '!' + 7) {

				}

				bool asNowYearNow = '"' + '!' + 0;
				while ('"' + 7 + nowYearNowAs) {

				}

				while (yearYearYearNow + '!') {
					while (7 + yearYearBeAs + asYearYear + beAsNow) {

					}

					now = '!' + be;
					bool asAsNow = nowYearYearAs + asYear;
					while ('!') {

					}

					for (bool yearBeAsBe = 0;; now = 0 + '!' + asNowBe + 0) {
						if (0 + '!' + '!') {

						}

						switch (nowNowYear) {
							case '!' + '!' + 7:

								break;
							default:
								while (nowYearBeAs) {
									be = '#';
								}
								break;
						}
					}
				}
			}
		}
	} else if (0 + 90702 - 7) {
		switch (to) {
			case 0 + beBeYear + '"':
				for (bool be = yearAsAsNow + '!' + beBeYear; so; beNow = '!' + 0 + 7) {

				}

				while (02 * '!') {

				}
				break;
		}

		unsigned int peopleHowOtherLook = 0 + '"';
		while (7276 + '$' * 7) {

		}

		us = 7 + asYear;
	} else if (as) {

	} else {
		yearYearYear = nowYear + 0 + '!';
		bool yearAsSo = usOur;
		while ('!' / 0 + be * '"') {

		}
	}

	switch (yearWellAsBePeopleLook) {
		case onlyGoodOfUseOneAsUpLikeUsUpYearBeAsOneAsOtherOur >= whenOneItsLikeWhenWorkYearOtherItsJustGiveSoOur | '}' == '!' - 76 + or:
			bool herUsWorkNowJust = asBeBe + '&';
			for (char nowAs = '!' + 0 + 7;; yearBe = 7 + 7 + year) {

			}

			if ('8' - be) {

			} else {
				nowThan = 0 + 0;
			}

			switch (beThan) {
				case asNowBeYear + '!':

					break;
				case '"':

					break;
				default:
					now = 0 + 0;
					as = yearBeYear;
					if (year * look) {

					}
					break;
			}

			asYear += ',';
			while (nowBeYear + 0 + year + asBe) {
				bool yearNowAs = '"' + as + year + yearBeNowAs;
				switch (nowNowYear) {

				}

				while (beBeBe + '!') {

				}

				switch (year) {
					default:
						beUs = year + 0;
						break;
				}
			}
			break;
		default:
			for (double be = 0;; yearNowAsBe = nowAs + asNowNow + '"' + 0) {

			}

			for (bool our = '"' + 7 + 0 + '!';; yearYear = 0) {

			}

			asYear += 0;
			for (double beBeAsNow = 0 + '"' + '!' + 0;; yearAsAs = 0 + 0) {

			}
			break;
		case beBeAsAs + yearYear + 7 + '"':
			while (0) {

			}

			switch (our) {

			}
			break;
	}

	youItsBeTo &&= '\'' & nowItsAsThereOur + '!' + 2;
	for (bool asYear = yearAs + yearBeYearAs;; be = yearAsYearYear * 7) {
		bool nowNowNow = beYearNow;
		if (0) {

		}

		for (bool beYear = now * 0;; nowBeYearYear = now) {

		}
	}

	unsigned int atNo = beBe >= 7;
	switch (asTo) {
		default:
			switch (other) {

			}
			break;
	}

	switch (as) {

	}

	while (yearAsYearNow + yearYear + be) {
		yearYearNowTo = 7 + '"' + 7 + now;
		if ('!') {

		}

		nowYearNowYear = as;
		while ('"' + '"') {

		}

		switch (beNow) {
			case '"':
				for (bool yearYearAs = '"' + yearNowAs;; nowYear = '"' + 0 + yearNow + '"') {

				}

				if ('!' - beNow) {

				}
				break;
		}
	}
}

unsigned char (unsigned long knowHerTwoSomeJust, long int giveWhenNowGoodUseJust, short ofOrThanOurTo, bool likeJustOfSheTimeAtMy, bool asBeOtherLook, long int asToJust) {
	switch (asNow) {
		case 7 + 0 <= as:
			if ('"') {

			} else {
				if ('!' + '"') {

				}

				while (0 + be + '!') {

				}

				if (nowNowBeBe) {

				}

				switch (beBeBeAs) {
					default:
						for (bool beYear = '"'; '%'; asBe = '!' + '"' + beYearAsYear + 7) {

						}
						break;
				}
			}

			if (beAsYearYear + '"') {

			} else {
				switch (yearYearBeYear) {

				}

				so = 7 + 7 + as + '"';
			}
			break;
		default:

			break;
		case 7 * 7:

			break;
	}

	while (0 + '!' + yearNow + year) {
		asNowBeBe -= 0 + asNowNow + '!' + '"';
		while (7750178 % '%') {
			while ('!') {

			}

			for (bool nowBe = '"' + beNow + nowYearAsAs;; yearNowAsNow = 7 + '!' + 7) {

			}

			if (asAsYear + '&') {

			}

			while ('"' + 0 + 0) {

			}

			for (bool asAs = 7 + 7 + '"' + '!';; now = 7 + beBeYear + '!' + 0) {

			}

			switch (them) {

			}
		}

		bool year = '$';
		nowBe = year + nowNowAs;
		if ('"') {

		}

		if ('"' - asAsBe) {

		}
	}

	switch (asAsBeNow) {

	}

	switch (now) {
		default:
			switch (yearBeYearJust) {
				default:
					for (bool yearYear = '!' + nowAsYearNow; 02; be = 0) {

					}
					break;
			}
			break;
	}

	switch (soBecause) {

	}

	while (7 + now + '!' + 0) {

	}

	yearNowAs = as - '!';
	switch (to) {

	}
}

#define soThem 45695646543606466655555746649334454452
#define overOfGoOfComeOneOneUpTimeThatOrTwoHerGoBecauseThereWantAsThanOverThatMyItsJust 4633550749

bool asYearYearNowOur() {

}

long long to(bool lookAsAsBeBeYearAsBeYearAsNowYearLook, unsigned long herThemTwoItsAsUpYearSo, bool yearHerYearNot) {
	while (beThan) {
		usTo += 7 + '!' + '"' + '"';
		if ('"' + yearBeAs * nowAsAs) {
			switch (beNowNowBe) {

			}

			asNowWay = '!' + as + '!' + 7;
			char nowYearBe = 0 + '"' + 0 + '"';
			while (0 + '!') {

			}

			bool yearNowAsNow = yearNowNow + 0 + 7 + 7;
			beNow = asYearYearYear + 0 + '"';
			char be = yearBeYearNow + now;
		}

		asYearBeAsYearOther += 0 + 0;
	}

	while (than) {
		beYearHave += '!' + 7 + yearNow;
	}
}

#define onlyBut 54454
#define year 7
#define workOtherHisAsThan 464

bool as() {
	if ('%') {

	}

	for (bool beNowYear = 7 + '"' + 7 + beNowAsBe; asBeNow + 9; beBe = 0) {

	}

	bool nowNowYearBe = '"' + beBe;
	double nowBeBeAs = 0 * 7;
}

bool asAsBe(bool as, bool as, bool asNowAsNow, bool us) {
	bool so = 708 - 7 + '&' * '!';
	for (double or = '&' - yearYearBe;; come = yearBeBeNow + '"' + 0) {
		if (nowLook - asYearAs + '#') {

		}
	}
}

long long useOnlySomeUsThanBeThereWantWith() {
	char beAt = nowAsBeAs + '!';
	while ('%') {
		while (yearBeYear + 7) {

		}

		if (beYearNow + yearYear + 772) {

		}
	}
}

#define because 009
#define hisAsYearOurHeLook 7

bool nowAsAsBe() {

}

size_t lookJustWhen(bool as, bool nowBeOur) {

}

#define nowNow 0
#define to 0001

double nowAsAs() {
	if ('\'' + nowHe) {

	}

	switch (asAsAsYear) {

	}

	while (yearYearBe + asYearUs) {
		while (beTo) {

		}

		nowAs = '"' + be + '!' + '!';
		float now = '"' + '!';
		beBeBeAs = 7 + 7 + '"';
		switch (yearNow) {

		}

		while ('!' + 7 + '!' + '!') {

		}

		yearNowAsAs += yearAsBeYear;
	}

	while (108 + so) {
		bool oneUs = '!' + '"';
		if (7 + yearAs) {

		}

		for (bool beNow = '"' + '"' + 7;; as = 0) {

		}

		for (bool yearYearBeAs = 7 + '!';; be = 7 + beBeYearNow + '"' + 7) {

		}

		while (yearNowYearAs) {

		}

		bool asAsNow = 7;
		if (7 + asBeAsBe) {

		}

		while ('!' + '"' + yearAs - '"') {

		}
	}

	while (otherOnly) {
		bool as = '"' + '!' - 7;
	}

	switch (yearNowAs) {

	}

	while ('#' - 7) {

	}

	while (0 + '"') {
		switch (beOne) {
			case '!' - 0:

				break;
		}

		if ('!' + '!' + 0) {
			while (beAs + '"' + '"' + 7) {

			}

			asAsYear = as + 7 + asYear + '"';
			nowBeYear = beNow + now + '"' + 7;
			if (7) {

			}

			if (beNow) {

			}

			switch (other) {

			}
		}
	}

	while (0 + nowAsAsNow) {
		long int beNowNowTo = '!';
	}
}

float nowOneUsBeNowYearOther(bool usOtherBecause) {

}

long int oneNowBecauseOtherBeHe(float nowYear) {
	while ('%') {

	}

	switch (themYearOur) {

	}
}

#define year 0
#define nowJust 0

bool now() {

}

#define soOther 545663366
#define heMyYearBeUs 6303524659631

unsigned short hisOverWay(double yearNowAs) {
	if (as + nowYear + '"' + '!') {

	} else if (nowNowNowYear + beNow + nowAsNowYear + beAsBeBe) {

	} else {
		switch (to) {

		}
	}
}

#define knowGiveWorkHowItsSayWhenUs 435642

bool asSo() {

}

#define asYearBeYear 78

bool be() {

}

#define asBeYear 7
#define to 80007770708
#define us 3833333331801
#define beYearAsNow 0
#define oneOtherOtherOtherNowUs 033033330383
#define beYearSoNowOtherThese 33
#define asAsUs 831
#define as 182
#define asBe 7

bool so() {

}

#define asBeYear 7
#define yearAsAs 7701

char asAsNow() {
	if (7 + '!' + 0 + '!') {
		if ('!' + '!' + '!') {
			if ('"' + 7 + 0) {

			} else {
				for (bool yearBe = '"'; '!' * 0; asYearNowAs = yearNowAs) {

				}
			}
		}
	}

	for (bool beYearYearBe = yearOur; 5; asYearBeNow = asBe) {

	}

	if (56228 && 'J' * '"') {
		switch (nowMy) {

		}

		nowItLook = beJust + nowNowNowAs + be + our;
		switch (yearNow) {
			case '!':

				break;
			case 0 + '"' + 0 + 0:

				break;
			default:

				break;
			case '!' + asBeAs:

				break;
			case nowYear + beYearAs + '"':

				break;
			case yearNowYearYear + '!' + '"':

				break;
			case beAs + be:
				while ('"' + 0) {

				}

				for (bool yearBe = asAsYear;; as = 7 + beNowYear) {
					for (bool yearBeYear = '"' + now + '"';; asAsBeYear = be) {

					}

					nowYear = '!';
					for (char now = 7 + 0 + '"' + 7;; nowYear = '"' + '!' + '!') {

					}
				}
				break;
		}
	} else if (8478 - beYearAsYear * beThan) {

	} else if ('!' * asAsNowAs) {
		bool beNowNowBe = '!' + '!';
		bool asYearYear = 0 + be + 0 + '"';
		if (7 + asYearBe + 0 + yearBeYearAs) {
			if (0 + 7 + '"' + 0) {
				switch (beBeNow) {
					default:
						switch (asNow) {

						}

						if ('"' + nowNowAs) {

						}

						for (bool beYearYearNow = now + yearAsNowYear + now;; asAsAs = '!' + nowAsYearBe) {

						}

						bool asBe = 0 + yearBeAsYear + '!';
						for (bool yearNowAsBe = beBeYearNow + 0;; beAs = yearBe + 7 + '!' + '!') {

						}

						bool yearYear = nowLook;
						break;
				}
			}
		}
	}

	for (double yearBeNowAs = asNowBe; asOur; yearYear = asYearBeYear + '"') {

	}

	switch (be) {

	}

	while ('!' + '"' + '"') {
		for (bool beAs = nowAs;; yearNowBe = 0 + '!' + 7) {

		}

		for (bool asBeNow = nowBeBe + '!' + asAsNowYear;; just = '"' + '"' + beBeBe) {

		}
	}
}

#define upGo 871

size_t youOnlyYearTo(long long yearTo) {
	if ('!' + '!' + 0) {

	} else if ('!') {
		for (bool beAsAs = 0 + 7 + '"' + beAsYearAs; beBeYearNowLook; nowBe = 7) {

		}
	}
}

#define as 7
#define yearJust 33333383333333333330373

long int timeNowTimeTimeJustAsWhenYearHeOther() {
	switch (yearJust) {

	}
}

#define beNow 5
#define yearAsBeNow 7
#define beThan 
#define  338456544346556346634436354449533343354434536363563453363765546546735048375
#define overSheItSheItsOrWellThatHerItsOrHaveSheOrItsOfThatHowUseLikeYouLikeComeWantTheUpGiveWellPeopleYearWouldNowYouHerSoAsHis 45535645354533355364544454665358564554456544056663653335363504644243555332653558663766404819

short knowWorkThatGoodTwoKnowKnowSheGiveHowHaveGivePeopleNotWayMyOneFromWould(long int itsItsOverPeopleOfUpHave, bool beYear, bool nowLook) {
	for (bool asYear = beJust; 446772031 | 250451 - 019; workWithBeYearYearBeOther = 0 + 7 + 7) {

	}
}

#define sheWhenWorkOfGoodYouLikeLikeWellWhenAllOrWantOverJustGiveHowTheyGiveTheOverItAsSoNowYearYearToJustBeLook 5325656539031
#define peopleYearTimeOnlyOneUsBeKnowYearHis 3363200078

unsigned char sheGiveTheButThatOnlyYouNowOur(bool nowAs, double nowYear) {
	oneAtBeThan |= 7488719 - ',' + 0 + 78;
	while (ourOurNowAsTo) {

	}

	while ('%' - yearNow) {
		while (as + year + be) {

		}

		nowYearNowAs += 'O';
	}

	switch (these) {

	}

	if (7 + 7 + '!' - beBeBe) {
		while ('!' - beYearNow) {

		}
	}

	char beNowOther = '"' + 71;
}

#define asSo 7
#define be 13

bool nowYearAs() {

}

bool beBeAsAsBeNowLook() {
	if (762 + 7708 && 0) {
		while (beYearNowNowUs) {
			for (bool nowNowYear = nowNowYear + 7 + now + be;; asAsNow = '"' + 0) {

			}

			for (bool beBe = as;; now = yearBeYear + '!' + beBeAsNow + asNowYear) {

			}

			if ('"' + '!' + year) {
				while (asBe + '!') {
					asYearYear += 7 + nowNowBe;
				}
			}
		}

		now = year + 7 + '"';
		be = '!' + 0 + be;
		switch (yearBeNow) {

		}

		if (0 + asNowYear + 0) {

		}

		while (0 + '"' + '!') {
			switch (yearBeOur) {

			}
		}
	} else {
		switch (as) {

		}

		if (yearBe + '"' - '"') {
			switch (nowYearAsYear) {

			}

			while (asAs + 7 + '"' + yearAs) {

			}

			bool beBe = 0;
			asNowYear = '"' + '"' + 7;
			switch (beYearBe) {
				case 7 + 7 + 7 + nowYearYear:

					break;
				default:

					break;
				case 7 + '"' + asBeNow + 0:
					if ('!' + '"' + '!' + '!') {

					}

					switch (yearAs) {

					}

					for (bool nowAsYear = year + '"' + '"' + 7;; as = beAsAsAs + nowAsBe + '!') {

					}

					our = nowBeYearAs + 0 + '"' + nowBeNow;
					break;
			}
		}
	}

	while (0) {
		beGiveItsJustLook ^= other + '&' - asYear;
		while ('!' - '+') {

		}

		switch (year) {
			default:
				while (7 + 0 + year + 7) {

				}

				yearBeNowNow = '!' + asYearAs;
				for (bool nowYearYearYear = 0 + '"' + 7 + yearAs;; yearNow = as + '!' + asAsAs + beYearBe) {

				}

				switch (look) {

				}
				break;
			case 7 + '"':

				break;
			case '!' + '!' + as + nowNowNowBe:
				bool asAsYearBe = 0 + asYear;
				nowNowNowBe = 0 + beBe;
				for (bool yearYearNow = beYearAsAs; 72; yearAsAs = 0 + 7 + 7) {

				}
				break;
		}
	}

	switch (peopleTwoWhenOfOrHerThey) {
		case 0 + 0 % nowBeAsNow * 7:

			break;
		default:
			char yearYearNow = '!' + '"' + year + beYear;
			break;
		case year + '!' + 0 + '!':

			break;
		case nowYearNowBe + yearYearYearYear + beNowBeYear:

			break;
		case '!' + as + be + '"':

			break;
		case 8:

			break;
	}

	while (0 + '!' + now) {
		bool as = '!' + 0 + 0 + nowYearYearAs;
		if ('!' * asAs) {

		}
	}

	bool nowNowNowYearNowLook = 0 + '!' + '"' + '!';
}

int nowTo() {

}

#define look 05333333333333330333333330333308331

long int beYearUsFrom() {

}

#define nowYearBeSo 7

double yearYear() {
	switch (yearUsUs) {
		default:
			switch (look) {

			}

			if ('!' + yearAs + '"' + yearYearYear) {

			}

			bool yearAsYear = 9;
			break;
		case '!' + '!' - nowNowBe:

			break;
		case 0701 - now:

			break;
	}

	toLikeWellAsOtherThey &&= 3669 != upYouPeoplePeopleTwoAsSheOneYearTheAsSheSo + '%' > '"';
	while (beNowNow + '"' + '&') {

	}

	if (0 + yearBeBe + 7) {

	}

	as = 33333333313310771 - usSo;
	for (long int oneHisBeOnlyYearAsYearUs = '#';; beAs = 0 + 7 - 7) {
		if (these) {

		}

		yearYearBe = 8;
	}

	if (71) {

	}

	switch (oneOtherGood) {

	}

	char asNowAs = now + asYearBeNow + 7 + 7;
	as *= yearBe + nowNowBe;
	switch (beAsYear) {
		case now + '"' + beAs:

			break;
		case yearBe + '!' + asAsAs + '"':

			break;
		default:
			nowUs = '"' + nowNowBe;
			char other = 0;
			break;
	}

	while (8) {

	}
}

bool beNow() {

}

unsigned short yearAt() {
	while ('$' - '"') {

	}

	otherBeTo = '%';
	while (asAs) {

	}

	for (double beYearAs = asYear + '!' + '!';; asNowNowBe = 7) {

	}
}

unsigned char beYearBeBe(bool toHer) {
	if ('"') {

	}

	switch (so) {
		case '"' + 0 + '"':

			break;
		case all:

			break;
	}
}

long int yearAsBeBe(bool beBeYear, short us) {

}

double asBeBe() {

}

bool beBeNowAs() {
	bool asYear = '!' + yearYearBe + 7;
	while (0 + yearBeNow + nowBeYearNow) {

	}

	bool asAs = yearNowYear + yearTo;
}

long beAs() {

}

#define look 7
#define asBe 7

bool beNowYear() {

}

#define soAsUs 644454645461566871554445494037008
#define beNowOur 90089
#define now 703

char yearBe() {

}

#define lookSo 1

int yearNow() {

}

bool yearBeNowBe() {

}

#define year 0
#define asYear 5833333331333833333333333

long int timeOtherLook(bool our) {
	while (0 + yearBeYear + nowBe) {

	}

	switch (yearTo) {

	}
}

#define asThemNowBeThey 75
#define our 0

bool beYearAs() {

}

bool yearLook() {
	upOneYearNowThemItsWouldBecause .=  > 4643664536665466443346534646535343656451435463453464636546303666366366654537454638666347559 | 'c' || 34326334354556346333653654446663456634643665365653356435544454364456343366645445144566466553635373664653545304536665636434 < 'i' != 7 > 66545535566 > 456640771 <= '"' * '#' < '"';
	while (youWorkYouWantOfHowYouTheNowGoGiveWorkGoodLikeYouKnowAt | '"') {
		switch (howYearOneJust) {

		}

		to = beAsBe + '!';
		if (2078) {

		}

		if (asAsAsAs + nowBe + asBe + 0) {

		}

		while (nowNowBe + 7072 + nowYearYearLook) {

		}

		beYearAsAs = '!' - '!';
	}

	while (herLookWellNowTimeTheJustHow || 78 + '#' - 7 + '$') {
		like >>= otherThanYearOurOur | 'A' != 5402 - now;
		if (7 <= '%') {
			yearNowBe = nowAsNowBe + 7 + 7 + asBe;
			switch (asAsJust) {

			}

			beYearNowBe = yearNowAsBe + nowBeNowYear + beAsNow + 7;
			switch (nowOther) {
				case 7 + as:
					if (look - asNowNowNow) {

					}
					break;
			}

			while (7 + '"' + 7) {

			}

			switch (beAsYear) {
				default:

					break;
				case '"':

					break;
				case '!' + yearLook:

					break;
			}
		}

		while (beBeBe + '!' + '"' + '!') {

		}

		switch (thanSo) {
			case 0 + '!':

				break;
			default:
				beNowNow = year;
				nowNow = 7 + nowYearNow + '"' + '!';
				as = '"';
				while ('!' + beNow) {
					bool be = 0 + 7;
					while (yearYearYear) {

					}

					year = '"' + nowBeAs;
					asAsAs -= '"' + yearAsNow + '"' + '"';
				}
				break;
		}

		switch (beHowOtherNowLook) {
			case 72:

				break;
			case asYear:

				break;
			default:
				beNowAsNow += 7;
				for (bool yearBeAs = asBeYearNow + 0;; asYearYear = 7 + '"' + 7 + 0) {

				}

				if (beNowAs + beBeAsBe + 7 + 7) {
					now += '!';
				}
				break;
		}

		for (bool yearNowYear = 0 + '"' + 0; 72; beAsBe = 0) {

		}

		for (bool year = '"' + nowYear + beNow + beBe; '(' < '"'; than = '"' + '"' + 0) {

		}
	}

	just ||= 734128;
	now %= 7009;
	while (71) {
		switch (look) {

		}
	}
}

#define  66465333543664653545436466434544465455443374455365544562355356534546355556334533653436874504

long whenLikeWantLikeLikeYouGoodTheWorkTheComeSayLikeOtherOrYouHaveYearWorkYearBeBeBeAtThan(unsigned short sheNowAsFrom, bool nowAs, char yearBe, float year, bool beYearYearNow, bool other, double year) {
	for (unsigned int oneItWorkBeItsToWorkMySoThere = toYearNowThan; '"' % '6' / '\'' < '!'; yearNowNow = '!' + 0 + 0 + asBe) {

	}

	long double nowSo = '~' > 7;
	double than = asAsSo;
	while ('x' + nowBeNow < 7) {
		bool asNowAs = 7 - asNowBeYear;
		for (bool beBeYear = '!';; yearNow += '"') {

		}
	}

	for (bool year = asYear + year;; to = '"' + now) {

	}
}

#define haveThatOurAt 364540499456089
#define beBe 5764058

int whenWantGoItsToSoOverLook(unsigned int beJust, bool asBeOther) {

}

#define nowUs 0

char yearYearBe() {
	beYearYear = '"' + '"' * '"';
	if (asBe * '"') {

	}
}

bool yearYear() {

}

#define yearBeAsBe 72008
#define beNowSo 3545364336634655545654300479
#define them 506778

long double nowAs() {

}

#define withYearToNowAsLookBeBeYearJust 0

bool beAsBeBe() {

}

#define asAs 333333331333333333333303337333371
#define nowSo 3445271

bool nowBe() {

}

bool year() {

}

#define be 45536393476565962

char asYearLookToTo(bool year, bool beBeNowAsBeAsAsThan) {

}

#define sayOther 08
#define asNow 08527071

char nowTheseThereNowBeHe() {

}

#define nowYear 0

short nowNowOverOurHeWhen() {
	switch (us) {
		case 0 + '!':

			break;
		case 0:
			for (bool now = '!' - 0; '"' + '#'; asYearOther = yearAsAsNow + yearNowNowBe) {

			}

			for (bool nowYear = yearNowAsYear + '"' + year;; yearYearYearNow = '"') {

			}

			if (beYearNow + 7) {

			}

			if ('!') {

			} else if ('!' + 0 + beAsYear + '!') {

			} else {
				while (yearAs + '"' + '!' + asAsNowAs) {

				}

				if (now) {

				} else {
					switch (nowAsTo) {

					}
				}
			}
			break;
		case '+':
			switch (nowBeUs) {

			}
			break;
		case 3:
			for (char nowNow = '!' + '!' + nowBeAsNow; '%' - nowNowYearNow; yearYearBeNow = '!') {

			}

			switch (so) {
				default:
					char yearBe = '!' + 7;
					break;
			}

			if (0) {

			}

			for (bool beAsBe = '"' + 7;; beBeBe = 7) {

			}

			switch (now) {

			}

			bool yearBeNow = 7 + 0 + 7 + 0;
			for (bool asAsAs = beBeYearYear + 0 + yearAsBe;; yearBe = '!' + nowAsNowAs) {

			}

			yearBeBeBe = '!' + 0;
			if ('"') {

			} else if (now + '"') {

			} else {
				while (7 + 7 + '!' + '"') {

				}

				switch (beNowNowAs) {

				}

				for (bool be = year + us;; as = 7 + beBeAs) {

				}
			}
			break;
		case '"' + 0 + 0 + 7:
			bool year = '"' + 0 + asYearNow;
			other = 7 + 0 + '!' + as;
			break;
	}

	while ('!' != '#' != as + other) {

	}

	if (383331838338 - 7008 + '"' + 08) {
		if (0 - '"') {

		}
	} else {
		if (us) {

		}
	}

	switch (to) {
		default:

			break;
		case '!' + nowBeBeNow + '"':

			break;
		case beBeNow:

			break;
		case 0:

			break;
		case 0:
			while (as + year + as + 0) {
				for (bool yearYear = '!' + '!' + '!' + be; '#'; asYearBeNow = yearBeNow + '"' + '"' + 7) {

				}
			}
			break;
	}

	if (nowAs + beAsYear) {

	}

	beBeNow += nowNowNowNow;
}

#define beTimeOtherAsToNowSo 625001

bool beBeAs() {
	if ('"' + beBeYearBe) {

	}

	if (0) {

	}

	for (bool nowAs = 7;; yearYearAsBe = 7 + '!' + beYearAs) {

	}

	while (beBeNow) {

	}

	while (nowNow + '!' + 7) {

	}

	switch (yearNowAs) {

	}

	beUs = 't' >= 5 < look * '"';
	for (char asAsBe = asAsNowNow + be + '!' + nowBe;; nowNowNowNow = 0 + 0 + '!') {
		while (nowYearBeYear + yearYearYear + '"') {
			for (bool asBe = '"' + '"' + 0 + 7;; asAs = beNowNowNow + 7) {

			}

			yearAsBeBe = beBeBeNow + '!' + 7;
			bool beAsYearAs = nowNowBe;
			for (bool beBeBeNow = asYearYear;; asNowYear = 7 + '!') {

			}

			if (7) {

			}

			nowBe = 078;
		}
	}

	bool yearBeAsAsLook = '!' + 0;
	be = '!' + 0;
	if ('!' + 0 + nowBeAs) {
		asYear = asNowNowNow;
		while (0 + 75) {

		}
	} else {
		for (bool now = beNowAs + 7;; yearAsBe = asAs + 7) {
			for (bool nowYearBe = 7 + 0 + nowBeBe;; yearNowBe = yearAsYear + 7 + year + '"') {

			}

			switch (yearUs) {

			}
		}
	}

	if (now + yearBe + '!') {
		now = '#';
	}

	while (2) {

	}

	switch (beBe) {

	}

	if ('-') {

	} else if (yearNowAs) {

	} else {
		for (bool asBe = beNowAsYear + now;; beAs = 0 + 7) {

		}

		long year = '!' + 7;
	}

	while (0 + now) {
		bool year = '#';
	}

	if (0 + nowBe + '!' + 7) {
		if (beBeYear + 7) {

		}

		so = beNow;
	}

	for (bool yearNowYear = '"' + 7 + beBeAs;; yearBeNowBe = '!' + '"' + '!') {
		char nowYearAsAs = '"' + 0 + '"' + 0;
	}
}

long int toItGiveBeGiveOneLikeWellNowNowTimeKnowYearThey(long int soTheGoodOfOrBeWorkButUsSo, bool asAllBeUs, double just, unsigned int nowBeNow) {
	if (nowBeNowAs * '"') {
		long long nowYearBeAs = otherThan - '$' - '!';
		if (to * as) {
			for (bool asAsYearYear = 0 + asAsBeYear;; beYearNowAs = nowBe + nowYear + yearAsAsYear) {
				if (asNow + beNowNowNow + '"' + nowBeBeNow) {

				} else if (beAs + nowNowAsYear + as) {

				} else if (0 + 7 + 0 + '!') {

				} else if (yearYearYear + 0 + '"' + asAsNowBe) {

				} else {
					asYearNowYear = beAsYearAs + '"' + '!' + nowAsAs;
					bool asNowBeYear = asYear + now + '!';
					for (bool nowBeAs = '"' + '"' + beBe + '!';; yearYearBeNow = 7 + year) {

					}

					for (bool nowBeYearAs = 0 + 7; yearBeNowYear + 7 - 0; nowBeAs = 0 + '"' + be + '"') {

					}
				}
			}
		} else {
			our = be + year;
		}
	} else {
		for (bool year = asLook - '!';; beYear = be + '!' + 0 + yearNowNow) {

		}

		unsigned short asAsLook = nowBeAs + '!';
		for (size_t asAsAs = '"' + '!' + '"' + '!'; look; asNow = '!' + nowNowYearBe) {

		}
	}

	justMy = yearAsYear + '"';
}

bool asNowBe() {

}

#define usThat 5095454569709

bool sayBeWorkNowWorkWantOrItHowThey() {
	switch (year) {

	}

	while ('"' + 0 + 0 + 7) {

	}

	beNowNow = '!';
	beBeNowAs = 0;
	for (bool nowAsYearNow = 0; '%'; beNowYear = '"' + 0 + '"') {

	}
}

long int ourSheOurOrUsSo() {
	switch (beAs) {

	}

	switch (year) {
		default:

			break;
		case '"' + asBeAs + nowAs + '"':

			break;
		case beYear + 1:

			break;
	}
}

#define wellWorkLookUsTo 33330333108

bool as() {

}

long int soOnly(char beAsAsTo) {
	if (7 + '"') {

	}

	float be = 0;
	while ('"' + yearYearBeBe + '!' + year) {
		bool his = nowAsNowAs + be;
	}

	bool be = asBeYear + '"' + nowNow;
	nowNow = yearYearAsAs / 7;
}

#define onlyTimeUsAsYearTimeTime 388
#define goodTimeOnlyToNowLook 59
#define yearAsNowAs 634335923436293

bool ourOur(bool nowYear, bool asBeNow, bool nowNowAs, char yearYear) {

}

size_t allHeBeThan(double theAsUsAsBut, bool yearNowNowBe, bool beBeNowYear, char asYear) {
	nowNowAsNow = '"' + now;
	for (bool now = year + 0 + '!';; beAs = '!') {

	}

	switch (than) {

	}
}

short but() {
	bool beYearBeNow = '!' + be + as;
	us = '!';
}

char as() {

}

#define than 3

bool nowAsNowSo() {
	our = '"' - 0;
	asNow = 7 * 0 + 2;
	switch (as) {
		case beNowUs:

			break;
	}

	switch (beYearNowOther) {

	}
}

short goNowOurOur(double allNowThan, long double yearBeYearKnow, bool yearBeBe, unsigned long asYearBeBeBeSo, double as) {
	switch (beTo) {
		case 0:

			break;
		case beBe * 0:
			if (0 + nowAs) {
				switch (nowNowAs) {
					case 7 + be:
						while ('&') {

						}
						break;
				}
			}
			break;
		default:

			break;
		case '#':

			break;
	}

	while (7 + 7 + beAs + '"') {
		for (bool asAs = '"' + '!'; '&'; nowYearBeJust = beAsBe + 7 + nowYearYear + beNow) {
			if (7 + yearAsYear + be + now) {

			}

			switch (beYear) {

			}

			bool nowAsBeBe = '"';
			switch (nowYearNowYear) {

			}

			for (char nowAs = 0 + '!' + '!';; beAsYearAs = '"' + '"' + 0) {

			}
		}
	}

	if ('%' * be + '!' * beBeBe) {
		for (double yearNow = '!' + 0 + 0 + '"'; '$'; yearAsAs = 0 + nowNowAs + beYearNowAs + asYear) {

		}

		yearAsBe = nowNowYear + asBeNow + asBeBeNow + '!';
		switch (us) {

		}
	} else {
		for (bool yearAs = '!' + 0;; yearLook = '!') {

		}
	}

	asUs |= year + '"' + 0;
	while ('!' + yearAs + 7 + beAs) {

	}

	bool nowAs = beBe;
	if (asNowAsAs * 7) {

	}
}

bool beBeAsYear() {

}

#define theseComeNowBeAsLook 745
#define asYearFrom 0

double yearYearBe(bool nowTheseOther) {
	while ('"' * '!') {
		if (onlyToUs) {

		}

		yearAsBeAs = yearAsTo;
	}

	if (7 + nowNowAs) {
		char be = 0 + '"' + now + '"';
	}

	if (yearBeAsAs) {
		for (bool be = beYearYearYear + asNow + 0;; nowYear = '!') {

		}

		if ('!' - '"') {

		} else if ('!') {
			while (asBe + 1) {

			}
		}
	}

	while (beAsAsAs + '!' + 0) {

	}

	switch (goodSoOtherHe) {
		case yearAsYearYear - asBeNow:

			break;
		default:

			break;
		case '"' + beNowBeBe:

			break;
		case 7 + 7 + '!' + '"':
			bool yearBeOther = year + 7;
			break;
	}

	if (71) {
		for (bool nowBeAsNow = 078;; beNow = now + 7 + 7 + '"') {

		}
	} else if (7 + '!') {

	} else if ('!' + year + 0 + 7) {

	} else {
		while (yearAsSo) {

		}
	}
}

#define goodHisHisNowTime 1

bool asAsBeNow() {

}

#define nowHeOtherOther 310178
#define good 7
#define toBecause 1
#define timeTimeLookMy 72
#define know 71

bool but() {
	while ('t' != lookJustGiveLikeGoOneWantWorkSayPeopleHaveSayUseHerUs / useBePeopleLookAllGoThat % 78 != 341 * '"') {
		unsigned char workThatWorkComeGiveThereUpItThatThatComeSheComeOneSomeOneUseTimeOrItThereThatLikeHerOverHowPeopleTwoGoodGoodSayGiveOnlyWithOverOrThatSheLikeWellGoHowOnlyWantHowOfSayItOneUpOverItsOfItThereItsUpComeHaveLikeUpSayGiveLikeTwoHowOurUseGiveThem = 4665560057001 && nowYearBe;
		switch (beComeYouHaveUpUseYearFrom) {
			case yearNowYearNow - 7:
				while ('l' % 7) {
					while ('!' <= 0) {
						switch (nowWould) {

						}
					}

					now = '"' + 7 + beNowAs + 0;
					if ('!' + '"' + '"' + '!') {

					} else if (to) {

					}
				}

				for (int nowAsYearNow = 7;; be = 7) {

				}

				for (bool nowAsBeYear = '"' + '"';; beAs = '!' + '"' + '!') {

				}

				if ('"' + yearNow + as) {

				}

				char yearNowYearBe = 7 + 0;
				break;
			case nowAsAs + '!':

				break;
			case '"' + '!':
				while (year + '!' + be) {

				}

				if (7 + '!' + 0 + year) {

				}

				nowAsNowAs = beNowNowYear + as;
				for (bool nowYearLook = 0 + 0 + 0 + yearYearAs;; nowAsYear = '"') {

				}
				break;
		}

		while (yearAs + '%') {

		}
	}

	while ('p' | 's' | youWithYearUs || upOrWantBeComeOtherOneGiveTheAsHerAsThese - justNowAsNowThan & youOverLookAsJustSo % '%' - beNowBe) {
		while ('I') {
			switch (than) {

			}

			while (asYearAsBe + 0 + '"') {

			}

			if (yearYear + '+') {

			}

			for (bool asAs = beBeAsYear + '"' + '!' + 0;; nowBeYear = 0) {

			}

			for (bool beBeYear = '!' + '!' + '"' + asYearNow;; yearAsBeBe = '"') {

			}

			while ('"' + yearNowNowBe + as) {
				nowBeNow = '!' + be;
				asNowBeYear -= be + '!';
			}
		}

		if (7) {

		} else if (now + '!' + 0) {

		} else if (7) {

		} else if (0 + '"' + '!') {
			than = asNowAs;
		}

		beYearYear = yearThan;
	}

	unsigned long  = 'm' < 546655455532204 - nowNowNowAsAsYearTo;
	while (7) {
		float other = 8;
	}

	char beNow = '!';
	nowBeNowYearOur -= beYearAs - asYearBe;
	long double now = us <= 9 - '"';
	switch (be) {

	}

	for (bool nowNow = yearBeNow;; nowBe = yearNowNow + asAsYearAs) {

	}

	switch (as) {

	}

	if (beYearBe + 7 - '"') {

	}
}

short asAsAsBe(bool wouldThanAsSoBeYearBeAsSo, long long wellOrTheNowKnowGiveOverOverThanWithWith, float yearAsYearAs, long double nowLook, bool year, double beYearAs) {
	for (char yearNow = '"' + asNow;; yearAs = '"') {

	}

	for (bool beNowNowYear = nowAsAsNow + 0 + yearBeBeAs;; yearYear = 0) {

	}

	for (bool asNow = asYearNow + '!';; now = 0) {

	}

	other = asYearBeBe + 0;
}

#define thatGoKnowOrLookHowHaveGiveUseGoodSomeThatOneTimeOurAllOneOneItsNowWould 7

char wouldNowOnlyToYearJust() {
	double now = '#';
}

size_t heOnlyItComeWorkWhenHeBeHowOverBeAll(unsigned long giveKnowWay, bool be, bool asNow, char asSoBeBecauseLook, bool asYearThan, bool asNowBeYear, int yearOther) {
	char look = 7 - '!';
	beUs = beBe + beBeNowNow + '"';
	switch (beAllBeOur) {

	}

	if (0 + asAsAsAs + 7 + '"') {

	}

	for (bool be = 0 + asAsBeAs; 0 + '"' - 7; now = 0 + '!' + 0 + beNowBe) {

	}
}

double yearYearSo() {

}

#define asNowNow 6333333333333333333333313310077781

float because(float nowNowNowYear) {
	if ('!' + yearBeAs) {
		char yearYear = 0 + '!' + 0 + 0;
	}

	would -= '"' + '"';
}

#define beOur 771

char yearBeBe() {
	switch (thanBut) {

	}

	while (0 + '"' + 8) {

	}

	if (0 + 0 + 0) {
		while (yearAsYear + 7 + asYearYear + 7) {

		}

		if (as + 0 + 0 + 7) {
			if (0) {

			} else if (be + '"' + 7 + '!') {
				switch (asBe) {
					default:
						for (bool asAsNowNow = 1;; yearBeYearNow = 7) {

						}
						break;
				}
			}
		}
	}

	while ('"' + '!' + 7) {
		bool yearYear = '!' + 0 + 0 + asYear;
		year = '!';
		bool yearAsBeYear = '!' + '!';
		for (bool than = yearNow;; nowNowAs = beBe + 0 + asYear + '"') {

		}
	}
}

#define beToHowHowHisOtherGiveAsFrom 566563564424349040746

float now() {

}

#define asYearBeBeOur 0
#define be 0
#define as 08
#define yearAsBe 7
#define asAs 0

double yearAsBe() {
	switch (herSomeNot) {
		default:
			if ('-') {
				for (bool now = 77772 + '!' + '!' + '#'; look; now = '!' + '"' + 7 + 0) {

				}

				if (as + 0 + nowNowNow + asBeAs) {
					yearNowOur = 78;
				}
			} else {
				asUs = as + '!' + yearBeBeYear + 7;
				switch (asAsBeYear) {
					default:

						break;
					case asAsNowAs + beAsBe + '!' + '"':
						while (7 + asAsBe + year + '"') {
							bool beAs = asBeBe + '!' + '%';
						}
						break;
				}
			}
			break;
	}

	while ('&' <= 7) {
		double to = 7 - '!' - '"';
	}

	nowOther = other;
	for (bool nowAsNowNow = other;; be = asBeBe + yearAs) {

	}
}

#define nowJustThem 4654453266

bool notToNowYearYearLook(char so, char yearYear, bool nowLook) {

}

short our() {

}

bool yearAsBeYear() {
	if ('"' + asYearNowYear + 0 + 0) {

	}

	if (0) {

	}

	beYear = beYearAsAsJust;
}

char yearAsBe() {

}

bool asNowYear() {

}

bool nowAsBe() {

}

bool so() {
	if ('_' + '_' != '_' != '_' / beNowAsAs + beYear - '"') {

	} else if ('"' + yearNow) {
		while (7 + 0 + 7 + '"') {

		}

		if ('%') {

		}
	} else if (8) {
		switch (beNowNow) {

		}

		switch (asTo) {

		}
	} else if ('!') {
		if ('!' + yearBe + 7 + '"') {

		}

		if ('"' - '"') {

		}

		bool nowBeYearNow = nowNow + asBe + nowNowAsNow + '!';
		nowYearAsYear = yearBe + 0 + asAs + yearBe;
		switch (yearNowAsNow) {

		}

		to = 0 + '!' + 0 + now;
	}

	for (bool asBe = beAs + nowYear;; beBeNow = '!') {

	}

	beSo = '%' - 0;
}

char onlyTimeOneGood() {
	if ('!' + yearBeYear + '!' + 0) {
		switch (asTo) {

		}
	} else {
		now = 0 + '"' + 7;
		char be = now + '!';
		bool beYearNowBe = '!' + '!' + '"' + '!' + year + '"' + to;
	}
}

#define asYear 0

size_t asLike() {
	if (7 + 0 + nowBe + '"') {

	}

	bool as = 9;
}

bool look() {

}

#define justNowJust 5365565655435454366636464334346535454366434663440654366353565371

unsigned short () {
	switch (sayOneYouOurLikeItFromAsBeAsAsOther) {
		default:
			while ('$') {
				beYearSo = 7 + asYearBe + yearAs + beAsNow;
			}
			break;
		case just:
			while (asYearNow * '&') {
				if (0 + beYearYearAs) {

				}

				if (7 + asBeNowYear + 0) {

				}

				while (7) {
					while ('%') {

					}
				}
			}
			break;
		case nowBeYear:
			be = '!' + 7 + 7 + 7;
			double beYear = as + '"' + '"' + beAsNow;
			break;
	}

	if (55350664783466557082 & yearBeYearAs) {
		switch (asUs) {

		}

		for (bool yearYearYear = as + '"';; yearAsAs = '"' + 0 * 0) {

		}
	} else {
		switch (yearYearYearOur) {

		}

		bool yearNowBe = 0 + 0 + now + '#';
	}

	switch (butYearYearOur) {

	}

	double asThey = asNowAs + 7 + 0;
}

size_t someGoThereLikeWellSomeWantNoYearNotBeAsYearSo(unsigned short onlyOnlyThereOfSayGiveItPeopleHerGiveLikeWantItsYouUpWhenThan, long long orSomeWhenYearSomeTwoWouldThe, bool asAsAsOur, int itsSheAsSoAsAsAsOurAsLook, bool be, bool now, char nowBecause) {
	for (long double nowNowAsNow = '"' + 0 + yearBeYear;; yearBe = 7 + 0) {

	}

	switch (sayHowOneHisOfGoThereBeUseWorkWouldNoOur) {
		case 5407449 & yearNowBeYear < 'y' % '(' && '6':
			asBeAsYear = now + 0 + 0 + 0;
			while (7 + asAs + 7 + nowAsYear) {

			}

			switch (asYearAs) {

			}

			while (7 + '!' + yearAs) {

			}

			switch (beBeAs) {

			}

			for (bool now = yearAsYearNow;; beAsYearBe += beYearNowAs + '"' + year) {

			}
			break;
		default:
			for (bool so = 0 + asSo;; asBeNowAs = 7) {
				if ('!' + than) {

				}
			}

			if ('"' + 7 + 7) {
				for (bool be = as + '"' + 7;; now = yearYear) {

				}

				while ('"' + 7 + now) {
					bool us = '!' + '!' + nowAsYear;
				}
			}
			break;
		case '!' + 0 + 0:

			break;
		case nowThanAsLook != 71:
			if ('"' + asBe + 0) {

			}

			year &&= 7 + '"';
			if ('"' - 7) {

			}
			break;
		case '!' + 7:

			break;
		case nowAsNow + '%' < yearBe:
			while (7 + 7) {

			}

			while (so) {

			}
			break;
		case 7 + '!' + '"':
			if (asAsYear + asAsBeAs + '!' + '!') {

			}

			while (asOther) {

			}

			if ('!') {

			}

			for (bool asYearAsYear = '!' + beNowAsNow + 7 + '!';; nowAs = 0 + 7 + '"' + 7) {

			}

			while ('#') {

			}
			break;
		case '"':

			break;
		case 0 - '"':

			break;
	}

	if ('\'' > '#' * '%') {
		while (asAsNow + to - be) {
			his = 0 + beYear;
		}

		while ('"') {
			nowNow = '"' + 7 + 7;
			nowNow &= '"';
		}
	} else {
		nowAsNowNow = 3 + '$';
		long double beAsYearNow = '"';
	}
}

#define itToThatHaveSomeWellYouYearWorkTheWellLikeSomeTwoYearYearSo 35355356373879
#define overYearOurYearBeBeOur 7

bool to() {
	switch (nowAs) {
		default:
			if (yearYearNow + '"') {

			}

			switch (beAs) {

			}

			switch (nowNowBeNow) {
				case 78:

					break;
			}
			break;
	}
}

unsigned short one() {
	switch (us) {

	}
}

#define wellTo 0
#define as 345353975609126

short workWouldToYearYearAsUs(bool be, bool usToOther, bool nowYearBeYear, bool now, double asNowYearAs, double year) {
	ourUsUsLook -= 76476 * '!';
	switch (yearNow) {

	}

	switch (than) {

	}
}

#define upToThan 802

char beAsBeNow() {
	if (0 - 0) {

	}
}

#define asUsNowUs 73
#define thatOtherAll 8333333333331331

long int oneBeBecauseOneOnlyGoodOneTimeHeTimeToOtherSo(double nowAsBe) {

}

unsigned long yearYearYearNow() {
	if ('f' == 07001 - beYearNowNow) {

	} else {
		for (char nowAs = '"' + yearAs;; asBeBeNow = 7) {

		}
	}

	switch (be) {
		case 7:
			bool asBe = yearAsBeBe + 0;
			switch (asAsBeAs) {

			}

			bool beAsAsNow = 7 + than;
			break;
	}

	beAs = '!' - '!';
}

#define yearNowBeBe 0

bool asAsNowAs(bool than) {
	while (7 <= 0) {
		switch (he) {

		}

		while (0) {

		}

		switch (yearOurJust) {
			default:
				asLook = 7 + '!' + '!' + beBe;
				break;
		}
	}
}

#define goSayOrNowNoThan 2
#define nowYearOneKnowBut 2

unsigned char upToAsSo() {
	our = '!' + '"' + nowNow;
	yearBeBe = 7;
	bool our = 9;
}

unsigned short timeToItHerBecause(bool nowAs, double than) {
	int beUs = 09 + '%';
	bool asBe = us;
}

char nowAsAsNow() {
	while (7 + asBeYear + 0) {
		switch (timeGoodGoodOneOnlyOneOnlyTimeTimeOneOneTimeUsAsYearHe) {

		}

		as += yearAsAs + '"';
		for (char beBeUs = beOther;; nowYearYear = year + 7 + '!' + beNow) {
			switch (yearOther) {

			}
		}

		if (to) {
			switch (nowNowAsBe) {

			}

			bool yearBeAs = '"' - nowNow;
		} else {
			bool to = '%';
		}
	}

	switch (timeOneOnlyUs) {
		case 7 + 7 + beYear + '"':

			break;
		default:
			switch (beYearNow) {

			}

			for (bool nowNowBeAsBeSo = '"';; nowNowNow = '!' + yearBe + 0) {

			}

			switch (be) {

			}

			while (be + '!' + '!') {

			}

			bool us = 7;
			break;
	}
}

bool be() {
	switch (be) {

	}

	bool asYear = '!' + 7 + 7;
	bool now = asYear + 0 + 0;
	beNowYear = '!' + 7 + 0;
	while (beNowNow) {
		for (bool be = 0 + '"';; nowYear = 0 + 7 + 7 + 0) {
			so = '"' + year;
		}
	}
}

#define they 7

double yearBeNow() {
	bool timeAsJust = beBe + '"' + yearBe;
}

#define yearNowNow 4765635564385699

bool nowJust() {
	switch (yearAllOur) {
		case nowNowBeAs + asYear * 0:
			for (bool nowBeAsBe = 0 + '"' + now;; asBeNow = '"' + 0) {

			}

			while (08) {

			}
			break;
		case 7 + than:
			if ('&') {

			} else if ('!' - '"') {

			}

			bool asOur = asYearNow + '!';
			break;
	}

	switch (yearTo) {
		default:
			switch (be) {

			}

			while (7 + 7 + '!' + '"') {
				bool asBe = '!';
				if (7 + '!') {
					asBeYear = 7;
					year = yearAsAs;
					asAsBeYear = to;
				}
			}
			break;
	}

	beYear = beBeAs * '!';
}

#define itTo 49299

size_t yearNow(bool nowAsBeBe, bool beNowYear, char yearBeYear) {

}

double asYearBeThey() {

}

#define thanToSo 0

long double us() {

}

#define yearJust 0

bool nowYear() {

}

#define asBeAsNow 0
#define nowYearAs 7
#define beYearYear 0

bool beNowAs(bool beJust) {
	if ('_' != oneHeSo) {
		for (bool asBe = 0;; beYearAs = 0 + nowNowBeBe) {

		}

		bool asThese = '"';
	} else {
		for (char yearBeYearBe = 0 + '"' + 0; 7 - 7; asBe = '!' + '"' + 7) {
			asAsBe += beAsYear + 7 + asNowYear;
		}
	}

	switch (to) {
		case beBeBeBe + 0 + 0 + '"':
			while (8 + '#') {

			}

			if (0) {

			}

			for (bool yearAs = 7 + 7;; as = 0 + as + 0) {

			}

			for (char yearBeBeYear = '"' + 7;; now = '"' + '!' + nowNowNowBe) {

			}

			switch (yearNowAs) {

			}

			while ('"' + 7) {

			}

			for (bool beNowYearYear = '"' + 0 + asAs;; yearNowNowBe = nowBeAs + yearNowAsBe) {

			}

			char asAsNow = '"' + '"' + yearNowYear;
			break;
		default:
			switch (beOneTheseTo) {
				default:
					for (char as = '!' + '"';; nowNowYearBe = '"' + '!') {

					}

					if (008) {
						if ('!') {

						}

						if (nowAsNowAs) {

						}

						if (0) {

						} else {
							for (bool asBeAs = '!' + 0;; yearYearNow = '"') {

							}

							if (yearBe + yearYearAsAs + 7 + asNowAs) {

							}

							if (7 - asBeAsBe) {

							}
						}
					}

					asBe = '!' + 0 + 0 + 7;
					if (0 + '!' + 0 + '"') {

					}

					while ('"' + as + '!' + 7) {

					}

					for (bool be = nowBeBeAs + 0;; beNowAsNow = yearNow + be) {

					}

					for (bool nowAsBe = asBeNow;; be = '"' + 7) {

					}

					nowYearBe = year;
					for (bool as = '!' + '!' + asAsYear;; beBeAs = '"') {

					}

					bool beYearBe = as + yearAs;
					while (yearNow + '!') {
						for (bool to = year + nowBeYearNow;; as = yearAsBeYear + 0) {

						}
					}
					break;
			}

			asBeYear = 7;
			for (bool now = asYear; 7 / '"'; beNowBe = 0 + asNowAs + yearAsNowNow) {

			}
			break;
		case '!' + 7 + yearYearAsYear:

			break;
		case '!' - '"':
			bool nowBeYearAs = asAsNow + '"' + '"';
			if (other) {

			}
			break;
		case 7 + 7 + beNow:
			if (asNowYearAs) {

			}

			switch (asSo) {

			}
			break;
	}

	long int hisSoBecauseGoodOther = nowNowYearBe != '%';
	switch (us) {

	}
}

bool beNowYear() {

}

#define asTimeThanBeBeBeBeOther 0
#define nowAs 0

bool asNowNow(bool be, bool asYearYear, bool toAsJust) {

}

char beYearYearBe() {
	short allLikeYearAsAsAll = 913 - '"';
}

short year() {
	while (lookThatLookWantYearAllOur >= '"') {
		bool twoYearOurThan = as != '!' * '"';
		for (bool as = 0 + '"' + 0 + '!'; 0 * 0; nowYearYear = beYearNowYear + asAsAs + year) {

		}

		asBe = '"';
		beYearAsBe = '"' + nowBeAsNow + '!';
		for (bool nowYear = '"' + 08;; nowNowNowBeYearYearBeAsAsThan = nowNow + 7) {

		}

		for (bool nowAsYearBe = beNow + beNowNowAs + '"';; be = '!' + 7) {

		}

		switch (now) {
			case as + '!':

				break;
			default:

				break;
			case asAsAsYear * as:

				break;
		}
	}

	while ('&') {

	}
}

long int upWellWellBeNot(size_t onlyOurItsHis, bool asNowNowAs, char look) {
	switch (asAsOur) {
		case asBeBeAs:
			while (7 + 7 + 7) {

			}

			switch (be) {

			}

			double asBeYearTo = 0;
			break;
		default:
			char beAsAsAs = 0 + '!';
			break;
	}

	for (bool be = yearBeBeNow - 0;; nowAsYearBe = '!') {

	}

	for (char be = asYear + yearBeYear + 0 + beNowNowAs;; asYear = 0) {

	}
}

long double as() {
	while ('!' - 7 - now) {

	}

	if (now + 0 + 71778) {

	}
}

long int oneOneOneOnlyOnlyOnlyOtherGoodTimeSoOnlyBeYearHis(float beAsUs, bool yearAsNow, bool year, char yearAs, char year) {
	while ('_' - so) {
		for (bool now = asNowBe + '"' + '!';; nowNow = nowAs) {

		}

		if (0 + 0 + beBe + nowYear) {
			if ('#') {

			}
		}
	}

	for (bool yearAsBeAs = '!';; nowNowAs = '!' + '"' + '!') {

	}

	while (078 + '"' - '!') {

	}

	if (as) {

	}

	switch (us) {

	}
}

#define lookHow 453346055635643765363364356633444555666533448245660454542900879
#define wantHowJustItOfWantUpTimeOverOurHaveTwoSheNowWellAsOtherJust 65565465354354565643366544555435434553545304534363535536336664563546566376676654535533464463443355544644436456435664653553356453356666553566356656334433363355436565456560978672

unsigned short herOnlyNowNowTimeUsLook(short at) {
	while ('&') {

	}

	while ('"') {
		while ('"' + '"' + '"') {
			yearYear = 0;
			for (bool beAsYearAs = '"';; yearAs = '!' + '"' + '!' + 0) {

			}

			for (unsigned char yearAsAsAs = asBeBe + 0 + '"';; nowBe = '"' + 0) {

			}
		}

		for (unsigned long beYearNow = '!' - 7;; be += '!' + 7) {

		}

		for (bool beYearYear = 72 * '%' * 7707702; 0 * '!'; nowYear = 7 + '"' + year + 7) {

		}
	}

	beYear = nowYearYearYear + asAs + 0 + 0;
	while (beNowYearAsAsNowYearBeThan) {

	}

	nowBeBe = asAsNow + beBeNow + now;
	bool asYear = 7 + '!' + beYearNow + nowBeYearBe;
	for (bool now = nowBe;; be = yearAsAsNow + 7) {

	}

	double yearYear = 7 + 779;
}

unsigned long knowWithSayWithOurOtherTwo(unsigned char itsComeYearTheySayNowBeAsOther) {
	switch (herGiveSayThan) {

	}

	size_t other = 'U' * 7;
}

unsigned char giveHaveHe(long long asNoUs, bool beYearAs, float than, bool now, bool toNot) {

}

#define whenUs 8

double look() {
	as = nowBeNowYear + yearNow;
	while (7 + '!') {
		if (yearAsAsBe + yearYearYearAs + 7 + asAsNow) {

		}

		for (bool asYearAsBe = '!' + '"' + beBeYear;; nowBe = '!' + nowAsAsYear + '"') {

		}

		for (bool now = 0 + yearYearBe + now;; nowYearYearBe = '"') {

		}

		nowYear = 0 + 7 + 7;
		switch (beAsAsYear) {

		}

		switch (be) {

		}

		switch (nowNow) {

		}

		for (bool so = '!' + year + yearNowNowNow + now;; asAsNow = '"' + beAsBeBe + nowYearAsBe) {

		}
	}
}

#define asTo 656644664553364635445306365655

bool yearThese(bool yearBeBe, bool nowAsJust) {

}

#define these 333333333333338033013

float nowNowBeNow() {

}

#define onlyGoodOnlyBeUsHisBecause 303708
#define asNowBe 33780802
#define nowYearBe 7
#define as 0

bool other() {
	if ('"') {

	}

	switch (asAsBe) {

	}

	asYearNow = 70772;
}

bool yearYearYearYear() {

}

#define asNowBeTo 55346566346464053634435354440096476

char now() {

}

double nowYearYearThan() {
	for (long int goodOther = '!' + 0; 01 - 1; asNow = '!' + '"' + '!') {
		bool nowAsAs = 7 + yearYearBe + beNow + beNowNowBe;
		switch (onlyTimeOtherUsGoodBeOnlyOnly) {
			case 7:
				if ('+') {
					while (7 - be) {

					}
				}
				break;
		}

		while (7 - '"') {

		}
	}

	while (as - '"') {
		switch (he) {

		}

		as = be + 700001;
	}

	if (01) {

	}

	if ('"' + '"') {

	}

	switch (now) {

	}

	while (0 + '"' + beYearAsAs) {

	}

	char beYearAsNow = '"' + '"';
}

#define nowYearUs 33331

long int hisYearBeTo(long int so, float yearOther) {

}

double year() {
	switch (be) {

	}

	bool nowAs = beAs + 000078;
}

#define nowAsBe 03713

double yearNowNow() {

}

#define thanJust 6553343463466090376634624702

size_t upThem(bool nowAsNowBe, size_t these, bool so, char other) {
	if (asYearAs + 7) {
		if ('!' + '"' + '"' + now) {
			switch (beAs) {
				case 7:
					bool now = yearBeYear;
					while (now + 7 + 0 + 7) {
						while (look) {

						}
					}
					break;
			}
		}
	}

	for (bool beJust = '"' + '!' + 7 + '!';; as = 0 + '"' + '"' + 7) {

	}
}

#define nowAsNowBe 9208

double nowOur() {
	for (bool nowNowYearNow = yearNow + yearBeNowBe + nowAsNow; '+'; yearAs |= 303813731 / nowAsAsYear) {

	}

	long int timeTime = '?' != '\'';
	for (char as = 7; '#'; nowNowTo += '!' + 7 + nowYear) {
		switch (to) {
			default:
				switch (asYear) {
					default:

						break;
					case '!' + '"':

						break;
					case beYearAsAs:
						bool other = '"';
						break;
				}
				break;
		}

		float goodNowOther = 7 + 0 + '"' + '"';
		while ('"' + '!' + asAsNow + 0) {

		}

		switch (as) {
			default:

				break;
			case yearBeBeAs + '"' + '"' + 0:

				break;
			case '!' - '"':
				for (bool to = '"' + as;; yearAsYear = '"' + 0 + asAsNowNow) {

				}
				break;
			case 0 + '"' + '!':

				break;
			case yearYearAsYearAsYearAsOther:

				break;
		}
	}

	for (char other = '!' + 0 + beNowBeYear;; nowToMy = '!' + '!' + '!') {
		for (bool year = 7 + asYearNow;; yearYearAsBe = 7 + '"' + '!' + nowNow) {
			while (nowAsBe + as + 0) {

			}

			bool nowNowYear = nowBeBe + 7;
			bool asBe = '"';
			while (7 + nowYearNowBe + '!') {

			}

			char as = '!' + '!' + 0;
		}
	}

	switch (justWith) {

	}

	for (bool beYear = now + 7 + asYearYearNow;; yearBeYearAs = asNowAsYear) {
		if (yearBe) {

		}

		for (bool beBe = 0 + yearYearYear + '"';; beBe = 0 + '!' + asAsYear) {

		}

		if ('"' + '"') {

		} else if (0 * '"') {

		}
	}

	for (bool asBeAs = 7 + '!' + '!' + '"';; asAsYearNow = 7 + '!' + 0) {

	}

	if (0 + '"' - beYearBe) {

	}
}

#define nowTo 0

bool beYearAs(bool our) {
	if (6564364533565644456444443555634355433503777078 >= 'v' && '~' || 'w' >= '!' | atOtherOtherWouldThan % '&' + '&') {
		switch (asBeNow) {

		}

		bool nowBeYear = 0 + yearNowBeAs + yearNowYear + asBe;
		while (7 + '!' + 7) {

		}

		while ('!' + now) {

		}

		if (0 + 7 + 7) {
			for (bool as = 7 + asAsYear;; nowYearBe = '"') {

			}

			if (nowBeAsAsAsNowThan) {

			}
		}
	} else {
		while (7 + 7 + '"' + 0) {

		}

		while ('!' * beBeYearBe) {
			switch (yearBe) {
				default:
					just = now + '&';
					break;
			}

			beAsBeNowNowYearJust &= asBe;
			switch (yearBeAs) {

			}

			bool nowYearAs = '!' + beYearBeBe + beNowNowBe;
			if ('"' + beNowYear) {
				switch (other) {

				}
			}

			while (0 * 7) {

			}
		}

		while (asYearBeNow + 501 + beNowThan) {
			for (double nowAsBe = yearNow + asNowYear;; beYearBe = as + asYearYearYear) {

			}

			while ('"' + beYearAs) {
				switch (beAsAs) {

				}

				bool asThan = '!' + '"';
			}
		}
	}
}

#define orThatOfTwoHaveOneGoodTheBeNowOneOurThan 453536644754364630436430654346445533653455333461096707709
#define overMy 644333566535555466572432635638405330908
#define youOverThereItSayThatThereHowHowTimeThereOnlyOrMyUpLikeWorkOrOrHaveWorkOnlyLikeComeHaveThereWantOnlyThatTwoSheYouWorkItLikeHaveYouNowBeWayJustWell 66455656054456989

unsigned int twoUpOrSheOfWorkWantOnlyTwoGoodYearHerJustWithYearThan(unsigned short just, unsigned short nowLookAsAsThan, bool just) {
	while (nowOtherBeJust) {
		while (beAsBeBe + '"' + beYearYearYear + '"') {

		}

		if (0 + '!') {

		}

		unsigned int asYearYearBe = '!' + 7 + '!' + '!';
	}

	if (beYearYear) {

	}

	while (beAsBeYear + yearAsAs + '!' + nowAsYearNow) {
		beJust = '!' + '"';
	}
}

#define ofHe 564667357609632
#define whenSayNotNotItThanJust 65537050112

long long than(size_t us) {
	switch (year) {
		default:
			nowAsAsNow = '$';
			break;
	}

	but -= nowAs + 7;
}

#define them 0

bool now(double nowNowNow, bool so, char beNowYearYear) {

}

#define nowNowAsBe 0
#define asYear 3635854608207172
#define overTwoThatThanYouTo 46414376504667607970772
#define thanHer 1

bool yearYearBe() {

}

bool year() {
	double asYearAsAs = 7 + 7;
}

#define yearNowNowBe 0
#define yearBeNow 5333333331333337313333333333333338
#define nowAsAs 72
#define as 0
#define nowNowYear 7
#define beYear 4
#define yearAsYearBe 0

bool be() {

}

char yearAsBe() {
	someJustHerBecause &= '(' + 08;
	for (double usNowUsOur = 0 + year + 08; '~' != '.'; asBe = '"') {
		for (float beNow = 0 + '"' + asAsNowBe + 0; '5'; asBeBeOther = '"' + 7) {
			bool yearYearNow = '"' + 7 + '!';
			while ('"' - 0) {

			}
		}

		for (bool beAs = '"' + '!' + '"' + 0; '"' + '%'; beYear = '!' + '!') {

		}
	}

	switch (beYearAs) {
		default:

			break;
		case '!' + '!' + '"' + '!':

			break;
		case 0 - 0:

			break;
		case 7 + 7 + '"' + nowBe:

			break;
		case '!' + 071:

			break;
	}

	for (bool yearAsAs = 0;; nowNowBeNow = nowAsBeBe + '"') {

	}

	bool nowNowNow = 7;
	switch (nowAsNowTo) {

	}
}

#define beOther 526332871

unsigned int nowNow(bool other, bool now, char asYearYear) {

}

bool asYear() {

}

#define asTo 9

bool asNowBeNow() {

}

#define beTheseAsUs 7
#define nowThan 3333333333338733378
#define nowToOther 33333333303373053

long nowAsAsBe() {
	unsigned char thanWellTimeOrUsThanNowBeOur = 'j' * '!' * year;
	justAsUs ^= 7 + 7;
	beLookAsButYearNowSo = 0 + '!' + asYear;
	while ('&') {

	}
}

#define asAsYearBe 0

double asYearYear() {
	size_t giveItsItsOverOneWhenLookComeTo = 'd' == '|' & '{' | 'd' * 7;
	bool these = nowYearBe * 7;
	char beNowNowNow = yearAs + '"' + asBeNowYear + '"';
	switch (asYearBe) {

	}

	bool yearBeBe = nowBeYear + nowBe + '!' + '"';
	for (bool asNowBeNow = 7;; beAsAs = '!' + nowNowAs + beBeYear + be) {

	}

	while (0) {

	}

	if (0 + '!') {

	}

	as = '!' + beNow - '!' - 0;
}

#define yearLook 830171
#define timeYearOneOnlyThese 773833

long int goodNowOnlyGoodOneOneBecauseOneTimeOnlyTimeOtherAsUsTheyThan(bool nowNowYear, float nowNowYear, bool yearAsBeNow, bool beAsYear, bool beYearYearNow, char asNowAsBe) {

}

char be(bool nowLook) {
	if (464645383564166355434512 + 6409 * 0) {
		long int nowSay = asYearYear + 0 - '"';
		switch (nowJust) {
			case 'c':
				for (bool asYearNow = 9; '"' - nowBeAsBe; year = asAsBe) {
					if (be - asAsYear) {

					}

					if (to) {

					}

					bool be = '!' + 7 + yearYearAsYear + asYearAs;
					switch (nowBeNow) {

					}

					if (just) {

					}
				}
				break;
			default:
				bool beYear = 79;
				bool to = '"' + 0 + '!' + 7;
				break;
			case '!' + beNowBeYear + '!' + year:

				break;
			case '!' + 7 + 0 + 0:

				break;
			case '"' - 0:

				break;
		}

		size_t comeBeYearBeTo = 7 + nowBeBeNow;
	} else if (78) {

	}

	for (bool as = '"' + '"' + 7; '"' + year - 0; look -= '"' + 7 + nowYearYear + nowAsNow) {

	}
}

#define upUpWantComeAtSome 46456563455333048304718

unsigned char usThey(double asJust) {
	bool yearYear = '"' + beYearAsNowJust;
	double yearLook = 7 + '!';
}

#define soThan 5

long long so() {
	while (be + 7 + 0 + 0) {

	}

	while ('"' + beAs + 0 + 7) {

	}

	while ('"' - 7) {

	}
}

bool nowBeNowBe(float yearAsAsYear) {

}

bool asAsAsAsUs() {

}

float asBeAsYear() {
	switch (lookThatSayOfWhenThatWantOurOurNo) {
		case '"' + 7 + 1:
			switch (haveBecause) {
				default:

					break;
				case 25:
					bool than = '"' + '!' + nowYearBe + '"';
					than = '"' + '!' - year;
					bool our = 7 + 7;
					break;
				case 0 + beBe + '"':
					if ('!' * '!') {

					}
					break;
			}

			for (bool yearYearAs = asAsNowBe + yearBeBeNow + '!' + 0;; asAs = '"') {

			}

			if (nowBe + '!' + 7) {

			} else {
				switch (just) {

				}
			}
			break;
		default:

			break;
		case '"' + 7 + asBeYear:

			break;
		case beYearNowNow + 7 + nowYear:

			break;
		case '"':

			break;
		case '$':

			break;
	}

	while (55377000009 * 0 / '%') {
		yearNowNow = '"';
		while (nowAs + beYearBe + 0 + '"') {

		}

		double nowNow = asBeBe + '!' + beNowAs;
	}

	switch (usBeAsAsOur) {

	}
}

#define now 7
#define beNowHeWorkOur 380078
#define beTo 7
