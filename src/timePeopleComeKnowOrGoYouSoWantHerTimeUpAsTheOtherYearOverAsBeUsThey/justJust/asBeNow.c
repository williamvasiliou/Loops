#include <2UUUUU.h>

#define goodOneOneBeGoodBecauseYearBeSo 3333317808
#define beUsAsOtherBeUs 6
#define asAsAsOther 65634364635474069956672
#define look 443447447472

bool as() {

}

#define beOur 771

double year() {
	as -= 0 + be + 7 + beNowYear;
	switch (wantItOverSomeOfThereBecauseSheYearJustHerWorkBecauseYearJust) {
		case '#':
			knowAsYearNowJustYearNowBeJust = '!' - beYearAs;
			break;
		default:
			switch (no) {

			}
			break;
		case nowAsBe + '!':

			break;
		case just:

			break;
		case 7 * 7:

			break;
	}

	if (now + 7 + 0) {

	}

	double asYearAs = 7 + '!' + '"';
}

unsigned int youUsFromAsBut() {
	unsigned long asNowBe = '!' + 7 + beYearNow + beYearBe;
}

bool yearBeBeYearYearTo() {

}

#define justBeHe 31

long int oneOnlyOnlyTimeOnlyUsOnlyOneHeGood() {
	if ('%' / 0) {

	}

	if ('"' - yearYearNow) {

	}
}

long int theseBeSoSo(long int time, bool usYearBeTo) {
	while (7) {

	}

	while ('"' - beYearYear) {
		if (0) {

		}

		asNow = beNowAs;
		while ('"' + 7) {

		}

		bool yearNow = 0 + '"' + '"' + 7;
		while ('"' + nowAsNowNow + 7) {

		}

		bool yearAsBe = 7 + 0;
		bool nowNow = us;
	}

	if (yearNowNow + 7) {

	}

	while (7 + 0 + '!' + '!') {
		for (bool asAs = 0 + 7; nowNowBeTo; asYearAs = '"' + as + '"') {

		}
	}

	switch (otherUs) {

	}
}

#define asAs 0
#define toSoNowYearUs 8

bool beYear() {

}

#define be 055545
#define yearHeAsBecauseOverUsAllBeLook 09

unsigned int likeLookAsHe(double useYearHowAtThan, long long nowUsYearWantNowNoJust, double now, char justJustSo, bool so) {
	if (0) {
		double nowNow = '!' + '"';
	}

	for (char nowYearNow = 7 + 0 + 0;; now = 7) {

	}

	while ('"' * 0) {

	}

	switch (beSoLook) {

	}
}

#define yearNo 37301
#define beNowYearNowToBeThan 533333337383708

char otherYearTime() {
	for (bool asYearYearBe = '"' + beYearYearYear + nowNowBe + '"'; '#'; beAsAsNow = '!' + '"' + 0) {

	}

	for (bool so = 0 + 0 + '!' + '"';; beYearSo = 7) {

	}

	switch (yearAs) {
		case '!' + nowAsYear + asBeNow:
			while (beYearBeAs + '"' + nowYearBe + 7) {
				if ('!' - '!') {

				}
			}
			break;
	}
}

long int otherTimeOneAsBeYearAsTo(bool beNowYearNow, bool yearBeNow, bool to) {

}

long int nowTimeGoodBecauseOne(char asBeNow, bool asAs, bool his, bool nowBe, bool as, bool as, bool asBeSo) {
	for (bool yearNowBe = yearNowAsAs;; asNow = 7 + year + 7 + '"') {

	}

	asThese = 7;
}

int nowAsYear() {

}

#define yearJust 566353533365353565593531168
#define nowLikeThanNoSo 526
#define lookNowTo 3333333333333333333333731

float beYearBe() {

}

float asYear() {

}

#define nowNowNowTo 8

bool asBe(bool other, char be) {
	for (bool yearNowYear = 0;; nowAs = 7) {

	}

	to = nowNow;
}

float asYear() {
	our = 0 + yearAsAs + '!' + '!';
}

char nowBe() {

}

#define asBeAsTo 2
#define beNow 79
#define nowAsBeYear 0

bool asYear() {

}

bool yearYearAs() {

}

#define yearNowNowSo 9350359

size_t herGiveSomeWellAsHaveNowWith(bool beYearYear, bool with) {
	for (bool asBe = 0 + beBeNow + yearBeYearYear;; asYear = 7 + 7) {

	}

	while ('!' + nowNowBeAs + as + '"') {

	}

	for (double nowOur = yearYear;; beAsAsYear = 0) {

	}

	bool beYear = '!' + 0 + nowYearAs;
	beToBeLook = beYearYear;
}

bool would() {
	for (bool asAsNow = '!' + 7 + yearNowYear + '!';; year = '"' + yearYearNowAs + 7 + 0) {
		switch (as) {

		}

		if (7 + nowBe + 0 + 7) {
			bool asBeAsYear = 2;
		}
	}
}

float sheTwoAllOrOneHerAsAsBeUs() {

}

long long beTimeTo() {

}

#define atYearUsAsYearOur 4510664704079
#define nowAsNowNow 93
#define yearYouNot 04501

double yearYearNow() {

}

long asSoThan() {
	switch (timeNowOther) {

	}

	oneAsThese |= 33333337138 - 0;
	us = 03 + '#';
	if (7 / '!' - '"') {

	}
}

#define timeOneOneYearBeAsNowSo 303888

long int nowAsAs() {
	switch (nowNowYear) {
		default:
			bool asBeBeNow = '%';
			break;
	}
}

long as() {

}

#define otherAsOther 7
#define beYearLook 046772
#define our 9

unsigned short yearAsYearAs(bool year, bool asAsOther, bool now, char beAs) {

}

bool theseBeAsAsOther() {
	nowAs = 7 + 7 + asBeAsAs + 0;
	if ('!') {

	}

	if (7 + 0 + nowAsAs) {

	}

	switch (beNow) {

	}

	for (bool asYearNow = '!'; be + '!' - nowAsBe; as = 7 + 0 + yearNowAs) {

	}
}

double yearYearYear() {
	while (6 + 9072 <= 0) {
		for (size_t yearYearBeYear = '!' + be;; nowAs = '"' + now + 0) {

		}

		while (asNowYearYear - 18) {

		}
	}

	while (7 + 0 - 0) {
		nowNowYearNow = 7 + '"';
		while (0 > 7) {
			switch (yearNowYearNow) {

			}

			if (1) {

			} else {
				bool nowYear = '!' + 7 + 0;
				for (bool asBeYearAs = 0; ','; asNowYearAs = now + 7 + 7 + '"') {

				}
			}
		}

		bool be = nowYearAsBe + 7 + '!' + be;
		look = 7 + yearAsYearNow + '"' + '"';
		if ('"' / '"') {
			while ('!' + '!' + 0) {

			}

			while ('"') {

			}

			for (bool as = now + '"';; nowYearAsAs = '!') {

			}

			if (0 + 0 + '"' + nowNowJust) {

			}
		}
	}
}

short hisUpWhenAsAsThatOnlyUsOurThey(char as, char as) {
	switch (beYearYearNow) {
		case '!' + 7 + nowNowBe:
			switch (asYear) {

			}

			switch (yearThey) {
				default:

					break;
				case '!':
					while (7 * nowNow) {

					}
					break;
				case beYearNow:

					break;
				case beBeBeAs + 0 + '!' + 7:

					break;
				case beBeBeBe:
					bool be = '"' + '"' + 0;
					if (7) {
						switch (yearThan) {

						}
					}
					break;
			}
			break;
		case asBe:

			break;
		default:

			break;
		case nowBeYear + asYearNow + '"' + '"':

			break;
		case nowAsBeNow:

			break;
		case yearBe + '"' + year + '"':
			for (bool as = beAsBe + 7 + yearNowYear;; now = nowAsNowYear) {

			}

			asNowNowNow = '!' - yearAsYear;
			break;
	}

	asOtherSoUpJust = nowAsNow;
}

char as(char be) {

}

bool beYearBe() {
	switch (to) {

	}
}

#define yearAsBe 7778

bool year(float asNowAs) {

}

#define yearAsAs 7
#define beUs 3037071
#define yearAsOtherYearTo 333333333333833333311

bool goodOnlyToOtherYearNowNowToBeBeYearNowNowNowOther(bool nowYear, double than) {

}

#define yearJustMy 7

float as() {

}

#define yearBeNow 3366534355433363655463446465455635416535543643543646335343566633565456554353556616744545078

short asWantTheyUpWantWayBeNo(double justAll, unsigned int goBeBeHeUs, size_t onlyNowHaveYearWithOtherJust, char be) {
	if (578) {
		unsigned char beNowBeAs = 7;
	} else {
		while (7 + 0 + year + '"') {
			year = 7 + 0 + 7 + 0;
			bool yearThan = 7 + '!' + '!';
		}

		while ('$') {

		}
	}

	switch (onlySoYearLook) {

	}

	for (bool asNowAsYear = 0 + '!' + '"' + 0;; nowNowBeBe = beYearNowAs + beBeYear) {

	}

	if ('"' + beYearBe + now + 0) {

	}

	for (bool asAsBe = '%';; yearAsBeAs = '"' + 7 + '"' + as) {

	}
}

#define timeYearOther 07777002
#define nowBeYearNow 79
#define asNow 55536464534145905536544645343424904
#define yearYearOur 9
#define asAsNowAs 0

bool than() {

}

bool asBeNow() {

}

bool yearYearAsYear() {

}

#define to 3333333333330383333
#define timeTimeOneOneSoToSo 703

long int year() {
	for (bool otherAsBeSo = '!' + 7 + '"' + be;; year = 7 + 7 + '"') {
		switch (asBe) {
			case 0 + 7 + 7 + now:
				nowYearYear = nowYear + asBeBe + '!' + year;
				beNowYearAs = 0 + 0;
				for (bool year = '"' + nowAsYear;; beYear = '"' + 0) {

				}

				if ('!' + '!') {

				} else {
					bool us = 0 + 0 + 0;
				}
				break;
		}
	}
}

#define beNowAsAs 7
#define yearAsThemAt 0
#define beBeYearBe 0
#define yearBeAsAs 644435885131968009

bool yearGiveUsOtherNowBeNowUs() {

}

#define workLook 50102
#define than 75170054

unsigned int yearOrLookBeTo() {
	while (0 - as) {

	}

	for (bool asBe = asNow + 7 + as;; beYear = '"' + '"' + be + '!') {
		beTo = 'a' || theToSomeWell || 504703 + 0 + '&';
	}

	beJust &= would - '$' % '!';
	while (075 >= asBe) {

	}

	asAsSoJust =~ be + '7' >= '"' * '"';
	float other = '!' + '"' + '"';
	while (0 + 0) {
		while ('!' + nowAs) {

		}

		while ('!') {

		}

		while (0) {

		}

		beBeBe = 0;
		while (71) {

		}
	}
}

char yearAsAsYear() {

}

long be(bool nowYearAsYear, bool yearNow, bool us) {

}

#define so 3331378
#define beOnlyGoodOneOneOnlyGoodAsBecause 333133333373333
#define toBeYearUs 83

bool beNowNowAs() {

}

#define us 782

char asNowNowNow() {
	switch (knowThanUsToBut) {
		case nowBeUs == yearAs + '"' + '%':

			break;
		case '$':
			while (0) {
				bool beBe = 0 + 0;
				for (bool yearAsYear = 0 + '!' + be; yearYear + 2; yearAsBe = 0 + 7 + '"' + nowAs) {

				}

				switch (our) {

				}
			}
			break;
		case 0 + beBeNowYear + '!':

			break;
		default:
			bool beYearNowAs = nowYear + 7 + '"';
			if ('!') {

			}

			while ('!' + asBeYearAs) {

			}

			while ('$') {

			}
			break;
	}

	lookAsAsNowYearWouldTo = 7 + 7 + nowNow;
	if ('"' + beNowYear + year) {
		while (yearAs) {

		}

		while ('"') {

		}

		while ('!') {
			bool year = our;
			nowBe -= 0;
		}
	}

	beYearThan = beYearBe + '!' + '"' + 7;
}

#define beBeYearYearJust 99565436416488660771

int asBeYear() {

}

long long at() {
	us = '!';
}

char be() {
	while (beAsNow) {
		bool now = '"' + beNow + 0;
		while ('!' + beBeBeYear + 7 + yearNowAsBe) {

		}

		while ('!') {

		}

		while (0 + 0 + asBeNow + 0) {

		}

		double nowAsBe = '!' + 7 + 7 + 7;
	}
}

#define year 7
#define us 33333333333333383387338
#define beYearYearBe 30333778

long int to(long int so, bool nowBeTo) {

}

#define now 43

double as() {

}

#define beOurTwoHis 566336542346500568771
#define because 3493415

unsigned int orFrom() {
	switch (asNowBe) {

	}

	asHis = '$';
}

bool thanLookYearYearNowAsHis(char yearYearOther) {

}

bool as(bool beBeYearAsUs) {

}

double they() {

}

#define beBeBe 0

bool nowAsBe() {

}

#define yearAs 7
#define nowAsYearYear 7
#define asAsAsAs 0
#define nowBeYear 33333333133

long int hisTimeOneGoodYearUsHisNowTo(bool soBeUs) {

}

long int nowBe(char nowNowToUs) {
	if (7 + '"' + '"' + now) {

	}

	if ('!' + 0 + 0) {

	} else {
		char asNowNow = '"' + 0 + 7 + nowNowYear;
	}
}

long int beYearBecauseOnlyThese(bool nowSo, bool nowNow, bool nowSo) {
	switch (nowSo) {
		case '!' + 7 + beAsNowAs + '"':

			break;
		default:
			yearYearNowNowTo = beBe;
			break;
	}

	if ('!' + be + yearBeAs + yearBeYear) {
		while (7 + '!') {

		}

		if (asAs + 7 + '!' + 7) {

		}

		bool beYearYear = year;
		for (bool nowYearNow = 0 + yearYearNow;; nowYearBe = 0 + 7 + '!') {

		}

		yearBeNowNow = 0 + '"' + asAs;
		while (0) {

		}

		switch (yearAs) {

		}

		our = nowYear;
	}
}

bool yearBe() {
	if (yearAsBeYearNowJust) {

	}
}

bool year(bool asAsYearYear, bool so) {
	double beNowBeSo = beNowBe * '!';
	if (7 + nowAsYearAs) {

	}

	double nowBeYearYear = '!';
	yearYear = '"' + 0 + '!' + '"';
	for (bool nowYearBeNow = '!' + '"' + yearNowBeYear;; asBeAs = yearYear + 7 + '!') {

	}

	while ('!' + '!' + '!' + 0) {
		asTo = '"';
		if ('"' + 0) {

		}

		switch (be) {

		}

		switch (beAsNowBe) {

		}

		switch (beYear) {

		}

		for (bool nowAsYearAs = asAsOther;; be = beBeBeYear + yearYearNowBe) {
			switch (notBeSayBeThatNo) {
				default:

					break;
				case 7 * 68 > beItOur * '!':
					while (be + asNowNowAs + 7) {

					}

					if ('!' + '!' + asYearYearYear + '!') {

					}

					while (7 + '!') {

					}

					for (bool nowBeBe = '"' + 0;; nowYearYear = '!' + '!' + 0 + beAs) {
						switch (beTo) {

						}
					}
					break;
			}

			for (float asYearAs = '!' + beBeNow + 7;; beYearAs = nowBe) {

			}

			switch (asOther) {
				case 7 * '"':

					break;
				default:

					break;
				case '$':

					break;
			}

			bool nowNowBeAs = '!' + 0 + '!';
			while ('"') {
				while ('"' + 7) {

				}

				beYearYearNow = 7 + '!';
				bool beAs = 0 + '"' + beYear + asBe;
				if (7) {
					switch (as) {
						default:
							as = now;
							for (bool beYearBe = '!';; asBeAsNow = 7) {

							}

							us = 7 + 7 + '!' + yearAs;
							break;
					}
				}
			}
		}

		if (7 + beBeNow + now) {

		}

		while ('!' <= 42 + 2) {
			for (long double yearNowAsNow = 7; '!' < 0; beYearAs = 0) {

			}

			if (7) {

			}

			switch (yearJust) {

			}
		}

		while ('"' + 0 + '!' + 0) {

		}

		for (int nowYearAs = be + 0 + 7;; yearAsAsBe = 0) {

		}

		if ('!' + 0 + yearNowNow) {
			double yearAsAsBe = '"' + nowAsBeNow;
		}
	}

	while (0 | beWellYearNot < '!') {
		for (char lookBeAsYearBeThan = 7 + 0; 1; yearYearAsNow = '"' + 7) {
			for (bool yearAsYearAs = beNowNow + be + 7 + '!';; beBe = '"' + 0 + 0) {

			}

			switch (asHe) {
				case beAsBeYear + 7:

					break;
				case yearYear:

					break;
				default:

					break;
				case nowBeNow + 0:

					break;
				case beYearBe + '"' + beNow + '"':
					if (778) {

					}
					break;
			}

			nowYear = 78;
		}

		switch (asThan) {

		}
	}

	switch (nowNowAsBe) {
		case nowAsAsNow:

			break;
		case 0 + '!':
			beYear = be;
			bool asBeNow = '!' + 7;
			switch (nowThan) {

			}
			break;
	}
}

char asYear() {
	switch (onlyGoodTimeYearOneGoodOneGoodGoodAsTimeHeTo) {
		default:
			onlyGoodTimeHis |= '%';
			switch (yearNow) {
				default:
					if (0 + 7 + 7 + 7) {

					} else {
						if ('"' + nowAs + nowBeAsBe) {

						}

						if (7 + '!' + '!') {
							for (bool nowAsBeBe = 7 + '!' + 0 + '"'; 1; nowYear = 0 + '!') {

							}
						}
					}
					break;
			}
			break;
		case 8:
			for (bool asHe = yearYearAs + 0 + 0;; nowAsAsAs = 7) {
				for (bool beAs = '!' + '"' + 0 + 7;; asYearNowYear += 0 + 0 + 7 + asBeYear) {

				}
			}

			switch (nowAsAs) {

			}

			while (8 + now - asNowNowAs) {

			}
			break;
		case yearBeBeBe:

			break;
		case '%':

			break;
		case '!' + 7 + '"' + 0:

			break;
		case 0 + yearBeAsNow + 7 + year:

			break;
		case 0 + '!' + '"' + as:

			break;
		case asYearBe:

			break;
		case beYear - '"':

			break;
	}

	for (bool yearNowNow = '"' + 7;; yearAsBe = '!' + '"' + 0 + '!') {

	}

	bool yearAsBeTo = to;
}

int atGoAsOur() {
	while ('~' & 'o' | 3681368735 & 76406) {
		myBeAll = '3' % '"';
		for (bool asNowNowNow = 0791;; asYearAsAs = nowYearNow + nowBeAsYear + '"' + 7) {

		}

		switch (asYearYearAs) {

		}

		for (bool asYear = beYearNowYear; '%'; nowYearNow = 0 + year + 7) {

		}
	}

	if (04) {
		bool yearNow = us;
		while (be + asBeNowAs * '"') {
			switch (yearNowNowAs) {

			}

			year = 78;
		}

		for (bool yearBe = now + yearNowBe + 0;; yearNow = '!') {

		}

		bool nowBe = nowYearNowBe + 7 + as;
		switch (asBeBe) {

		}

		for (bool nowYearNow = 0 + 7 + asBeNow; 8; yearAsNow = '"' + '"') {

		}
	}

	for (float yearNowNow = '"'; '&'; asNowNow -= '!' + '!' + '"' + '"') {

	}

	while (because * beBeAsYear) {

	}

	bool nowYearBeAs = beYear + '!';
	switch (nowAsYear) {

	}

	yearYear = '"' + now + 0 + '!';
	switch (yearNowYearAs) {

	}

	beNow = 0 + nowAs;
	if (beYearYearJust) {

	}
}

#define ourLookOurThan 34567456789
#define yearBeYearSo 75002
#define timeThe 453765

bool way() {
	for (bool yearNowYearBe = '"' + 0 + 7;; asBe = '"' + year) {

	}

	for (bool year = 0 + '"';; asYearBeBe = nowBe + 0 + 7 + asNow) {

	}

	for (bool nowNow = yearAsBe + '"' + beNow + asBeNow;; yearBeNowBe = '!' + 7 + '"' + now) {

	}

	for (bool beAsAsNow = '!' + '"';; year = nowYearNow + beBeYear) {

	}

	switch (beYear) {

	}

	nowBe = '!' + 0 + '!' + '"';
	if (asYear + '"') {

	}

	while (beNowBe + 0 + nowNowBeAs) {

	}

	while (as + 0 + '"' + '!') {

	}

	switch (beNowNowJust) {

	}
}

#define beYearNow 2
#define be 0

bool to() {
	for (bool beNowBe = 7 + 0; yearAsOur; nowBeMy = '"' + '!' + 7) {
		while (7 + yearAsBe) {

		}

		for (bool asNow = beBeBeYear + 0 + '!';; as = yearYear + 0 + 7 + 7) {

		}

		bool beBe = '!';
		switch (us) {

		}
	}

	bool now = '!' + asNowAsAs + beNowBe + '!';
	if ('!' + 0) {
		if (nowYear + '"' + 0) {

		} else if (be + yearAs + '"' + '"') {
			while ('!') {

			}

			double beYear = 7;
		}
	} else if (7702 + nowOur) {

	}

	if (nowBeAsNow + asNowNowAs + year) {

	}

	for (bool nowNowBe = '!'; '$'; nowNowBe = 7 + 0 + 0 + nowAs) {

	}
}

#define these 546168
#define lookNotBeNotLook 543407532
#define no 959336304479808

bool yearYearYear() {
	for (bool yearAsBe = '"' + asYearBe + 0;; yearNowNowYear = beYearAs + '!' + asBeAs + '!') {

	}

	if (7 + 7) {
		double asAs = asBeYear + nowAsYearBe;
	}
}

long year() {

}

bool beAsYear() {

}

#define asBeYear 0

char nowNowBe() {
	switch (timeTimeGoodOtherBeAsTo) {
		default:
			for (char asUs = '%';; nowBeHisAsSo = '"' + 0 + 0 + '"') {
				bool nowAsYearNow = asYearAsAs + beBeNowYear + asBeBe;
				switch (us) {
					default:
						for (bool beOther = '"' + 7 + 0 + 7;; now = 7 + 0 + year + beYearBe) {

						}
						break;
				}
			}

			while ('#') {
				for (bool beAs = 7 + '"';; asBeOther = 7) {

				}

				be = 7;
				yearSo = '!' - nowNowNow;
			}
			break;
		case nowAsYearBe:
			while (0 + as) {
				now = 7 + 0 + yearBe;
				for (char nowYearYear = '"';; yearAsAsYear = asBe) {

				}

				switch (asAsTo) {

				}
			}

			bool beYear = 7 + beNow + yearNowNow;
			while ('"' + 7 + 7 + 7) {

			}

			while (0 + 0 + 0 + beYearAsYear) {
				char nowBeAs = year + 0 + 0;
			}
			break;
		case 7 + '"' + 7:
			if (other) {

			} else if (yearAs + '!' + '"') {
				switch (yearBeNowBe) {

				}

				switch (beBeAs) {

				}

				for (bool nowAs = 0 + '!';; yearNowYear = 7 + yearAsYear + '"' + 0) {

				}

				if (7 - 0) {

				}
			}
			break;
		case '+':

			break;
		case be + 7:
			while ('!' + '"' + 7) {

			}

			while ('#') {

			}
			break;
	}

	while (7 + '!' + '!' + yearBeYearAs) {

	}

	for (bool so = 0 + 78;; asBe = 0 + '!') {

	}

	for (bool toNowTo = be + 7;; beBe = 7 + '"' + year) {

	}
}

#define to 73171081
#define hisBecause 337345668
#define asNowYear 077772
#define nowBeLook 34454433633635345464634346544453346634366535556554655455336454335346543635536563536665554556346546635533654664944653554345556633463441645455653363206308
#define  
#define  334364389448

unsigned short orLikeYearItOneSheTimePeopleHowGiveOneOfItsOfWorkOfLikeHaveThemSayBeSheUseThemOfYouUpLikeHeItGiveWay(char beNow) {
	for (bool beAs = yearBeYearAs;; asNow = nowAsBeNow + be) {

	}

	switch (nowYearNow) {

	}

	look = than;
}

#define workTheAllUsBeTo 72
#define asYear 2
#define lookNowAsThan 7
#define yearYearNow 8

bool us() {
	for (bool beNowAsBe = our == yearNow; nowYearAs || 7; look = '"' + beBeBeAs) {

	}

	for (bool asYearYear = be; '"' + beNowNow * '(' + 7002; asNow = '"' + asYearAs) {

	}

	short beAsYearAs = yearAs | ')';
	other += '&';
	while (be) {
		if (0 + '!' + nowNowBeAs + '!') {
			nowYearNow = '!' + now + nowYearNowBe;
			yearNow += '!' + 7 + nowAsAsBe;
		}
	}
}

unsigned int goodJustBeYearOur(bool asNowAsYear, long yearBe, bool be, bool yearYear, bool beNowAs, bool be, bool asBeNowNow, bool so) {

}

#define itAsOurNoBeOur 63771

bool asNow() {

}

#define beWaySo 2
#define yearTo 667071071
#define usOtherSo 0
#define usAsOther 0
#define beBe 53333333333133331333371

long int onlyTimeUsBeBeUs(bool asNowYear, bool nowNow, bool asTo) {

}

bool asNowYear() {

}

#define yearOther 08
#define youOther 1012

double beBeYear() {

}

#define nowYear 3653363454366365434745433666557555643535444553656345355646645333456465346334535535446334634555655336347054
#define orComeThatYearUseHaveOfPeopleOverUseGoodHaveItsItWellSomeWorkTheTheThatSayWellGoodSheThereUpGiveThatLikeWhenComeYearTheySome 335536966433363363564363674655383

unsigned short hisJust() {
	for (bool asThan = year + nowYearYearAs + '"' + '"';; nowYearBe = 0) {

	}
}

long double nowNowJustBeBeBeWith(bool beJust) {
	switch (beBe) {
		default:
			bool our = 0 + '"';
			break;
	}
}

unsigned char yearYearYear(bool nowYearAsNow, double nowLook, bool just) {
	while ('"' + asNow + '"' - nowNow) {

	}

	for (double to = asYear + 0 + nowBeBeTo; '*' + 71; beNowAs = 0 + 0 + 7 + beYearNowAs) {
		switch (yearBeBe) {

		}

		if (0 + 7 + '"') {

		}

		if ('!') {
			for (bool yearAsYearAs = nowAsYearYear;; now = '"' + beBeYearNow + 0) {
				for (bool be = '"' + now;; as = 0) {

				}

				switch (yearAs) {

				}

				if (0 + asYear) {

				}

				nowAsBe = 2;
			}
		}
	}

	long yearBe = nowNow + beNowAs + 7 + '"';
}

bool asThanOtherBeLook() {
	switch (our) {
		default:

			break;
		case '!' + 0 + asAsNow:

			break;
		case '!' + nowBe + '!' + '!':

			break;
		case beNowUs:

			break;
	}

	bool beBeAs = yearBeBe + '"';
	for (bool other = 7 + 0 + nowBeNowNow;; yearNowYear = 0 + nowBe + 0 + '!') {

	}
}

bool our() {
	nowJust -= '/' || '"' + lookThanThan - '!' * 0;
	size_t giveSoWantOrGoAsNowYearYearNowBeNowBeBeNowOther = '$' % '!';
	while (59 >= 57672 + 0 * asBeYear) {
		switch (timeLook) {
			case 7 + '!' + '"':

				break;
			case 29 * '!':
				if (be + 7 + nowBeNow) {

				}

				if (nowAs) {

				}

				if ('"') {

				}

				than = 7 + asAsBe + 7 + year;
				break;
		}

		bool asYearYearBe = 0 + yearYearAsAs + 0 + '!';
		for (bool year = 7; '#'; beAs = '"') {
			double yearNowNow = '"';
		}

		switch (us) {

		}

		as -= '"';
	}

	for (bool yearBeAs = '!' + 7 + yearNowYear + 0;; yearAsBeBe = 0) {

	}

	bool nowNowThan = '"';
	yearOther = yearYearAsLook;
	bool beYearBeYear = 0 + beBeYearYear + 0 + now;
	if (yearYearYearBeAsOther) {

	}

	for (bool asBeAs = '$';; be = 7 + 7) {

	}
}

#define from 3333333330333333333333333333331087718

bool nowAsYearBe() {

}

#define nowBeAsNow 7
#define nowAs 001

bool yearAs() {

}

bool yearBeAs() {

}

double nowNowAs() {

}

bool asYear() {

}

bool asBe(char asYearYearYear) {
	unsigned long wantToOtherAsNowLook = 5 % beAs;
	for (unsigned char be = asAsNow;; asNowYear = '"' + as) {
		bool beBeYear = asAsBeYear + '!' + 7 + asAsAsYear;
		switch (nowNow) {

		}

		while ('!' + asBe + be) {
			while (0 + 0) {
				for (bool yearAs = beUs;; asNowYearYear = '!') {

				}
			}
		}
	}

	for (bool beNowAs = '!' + beAs; '#' - yearNow; asBeBeNow = '!' + 0 + asNow + '!') {

	}

	long int how = '}' && usBut != nowAsNowBeOur * '%';
	while ('"' % beAs) {
		double nowNowNowYear = '!' + be;
	}
}

int year() {
	for (double year = 0 + beAsNow + '"';; beBe = '"') {
		switch (yearAsThan) {

		}
	}
}

#define look 34895
#define so 2

bool so(bool nowAsNow, bool than) {

}

#define our 533333333333873131778

long int beBecauseBeNowSoTo(bool nowAsUs, long int be, bool so) {

}

float nowSo(bool year, bool yearBeBe, bool asNow, bool nowAs, bool asYearYear, bool yearYearAsYear, char beYear) {
	switch (so) {

	}
}

long int nowYearYearNow(char now, char us, double beAs) {

}

#define goodWantNowJust 634643353435454344535666565356435655345436366445536634663543436345553544464363566554565353354554554665544465355355654563465433654555454644535564545364553546455343353465345546546344331546551566533336354774374
#define workHerSoHaveItsOnlyTwoYouItItsUpOrSheItGoWorkThatUpItTwoKnowTheThereOrOverTheGiveThatYouTimeSomeItsItsTherePeopleUpTwoYouSaySheWantOnlyTherePeopleWhenHowWouldNowWhenAtNowAt 6066504596364
#define other 343393364535254
#define herHowUseUs 436425

bool nowBeNow() {

}

unsigned short someHowLookJustBecause() {
	for (double nowYear = '"';; as = 7) {

	}

	yearYear = 0 + as;
	if ('!' - '"') {

	}

	if ('#') {

	}
}

#define than 7

bool asLook() {

}

bool nowYearNowAs() {

}

#define nowAs 388311701
#define goodOnlyGoodOneToAsGoodOneBeSo 330338037003
#define good 7
#define as 8
#define now 01

bool asNowNow() {

}

bool to(bool as, bool hisSo, char he, char asBeNow) {
	bool now = year + '!' + '!';
	switch (year) {

	}

	for (bool yearAsBeAs = nowYear + 0;; yearBeNowYear = '!' + 7 + 7) {

	}

	switch (beYear) {

	}

	float asBe = 0 + 7;
}

#define yearYearYear 351

bool beToYearUs() {

}

#define so 63573455464677712
#define beYearYearJust 82

double beFrom() {

}

#define asAsUs 644564723666512546
#define nowBeSo 632
#define twoPeopleWhenThemUsWhenAsUs 082
#define beNowAsYear 5
#define asWellOther 01

long than() {
	while (timeBeUsBecause / '?') {
		goodOther = nowBeBe;
		while (nowAs - be) {
			if (asBeNow - yearNowYear) {

			} else if (other) {

			}
		}

		while ('!') {

		}

		asNow = 7;
		switch (yearUs) {

		}
	}

	for (char hisOtherNowYearTo = beYearBe + beBe;; us = 7 + 0) {

	}

	goodToGoodBeHe += as;
}

#define beOnlyBecauseNowTo 0
#define oneGood 333773

long int themBeUsThan(bool our) {
	char be = 078 + 1;
	char be = '!' + beAs + '"' + beAs;
	while (look) {
		for (long double nowYearAs = 0;; asYearBeYear = 0) {

		}
	}

	switch (now) {

	}

	if (7 + now + beNowNow + as) {

	}

	if (7 + be + 0 + year) {

	}

	for (bool nowAsBeYear = '!' + 7 + yearBeYearNow + '!';; our = '"' + '!') {

	}
}

bool nowYear() {

}

int beBeBe() {
	bool to = 4925366744638 + 338;
	for (unsigned short my = beAsNowBe + asYearBeYear + asYearBe + 7;; nowAs = '"' + asYearYear + 0 + 0) {

	}

	while (701 + nowNowAs * '"') {
		bool year = '!' + nowAs;
		if (yearYearYear + 0 + as + '!') {

		}

		bool nowNowYearAs = 7 + asBeAs + '"' + yearAsBeBe;
		if (to) {

		}
	}

	while ('!' + '"' + 7 + asAsNow) {
		switch (yearNowAsBe) {
			case 7 - 7:

				break;
		}

		for (bool year = 0 + 7 + '"' + '!';; beAsNow = asBe) {
			yearTo = nowNowYear + 7 + '!';
		}
	}

	for (bool yearYearAs = '!' + 7 + 0;; yearAs = 0 + 7 + as + beBe) {

	}

	now = 0 + now;
	while (nowNow + be + '!') {
		for (bool asBeNowYear = now + be + '!' + '!';; as = '"' + 0) {

		}

		while (7) {
			bool nowYear = '"';
			switch (as) {

			}

			us = '!';
		}
	}
}

unsigned int asOther(bool us) {

}

#define herJust 771
#define asBeAsTo 0387065
#define beYearTwoUs 779

char asBe() {
	for (bool nowYearNowYear = 1;; beBe = asNow + '"') {

	}
}

float year() {
	yearAsBeTo += 3 - '\'' != '!' / yearOtherTo;
	long int onlyOneOnlyOneGoodBeBeAsUsNowSo = 8 != 8;
	if (beBe + '!' + '"') {

	}

	while (nowYear + 0 + nowYearYearAs + beYearNow) {
		char as = now + 0;
	}

	if (year - 7) {
		while (7 + beNowBe) {

		}

		char yearNow = 0;
	}
}

long int onlyOneBecauseNowBeTo(double yearNowNow) {
	while (779 >= yearYearYearBe) {
		yearBe += 7 + beNow + beBe * '"';
	}

	yearBe = 7 + 7;
	if (asBeYearYear + be + yearAs + asNowAsAs) {

	}

	if ('"' + 7) {

	}

	if (7 + '!' - 7) {

	}
}

bool now() {

}

char yearBeBeAs() {

}

bool year(bool our) {
	long int  = 'o' &&  | 'a' == 'a' & howGoUseOurGiveOfHowGoUseGoodSheBeYearUseWhenOnlyOneYouUseButGiveAsWith | 'f' & 545554445434650366395639 & 8;
	switch (wantWantThatAsPeopleComeThereAsSoHaveHeTheyNowThan) {
		case asAsNow + '%' + us:
			while ('%') {
				soOther = beAsAs;
				be = '$';
				beBe -= '!' + '!' + '"';
			}

			if (7 + '"' + nowNowYear) {

			}

			float justYearThan = '!';
			break;
		default:
			usAsPeopleBeNowYearBeNowSo = now;
			switch (asBe) {
				default:
					if (7 + 0 + nowBeYearYear + 7) {

					}

					int year = '!' + 7 + beYearYear;
					break;
				case 0 + now + year + yearYearAs:

					break;
				case '!' + '!' + 0 + 0:

					break;
				case 7 + '!':

					break;
				case 7 + yearAs + '!':
					if (0 * as) {

					}
					break;
			}

			while ('!' + 0 + 0) {

			}

			nowAsBeNow = beNow + 7;
			if ('!') {

			}

			if ('!' - '!') {

			}
			break;
		case 7 + yearAsYearBe + '"' * asBe:
			if (look) {
				switch (yearNow) {

				}

				for (bool to = 7 + '"';; beBe = '!' + yearNowBeYear) {

				}
			}
			break;
		case 00772 - 0:
			bool as = beYear + 7 + '!' + 7;
			if (nowNow + 7 + beYearBe) {

			}

			float asAsNowBeJust = '"' + yearAsBe + '"' + nowYearAs;
			break;
	}

	while (just / '-') {
		switch (otherThereOnly) {
			default:
				for (bool asYearBeBe = be;; yearAsBe = '"' + '"' + other) {

				}
				break;
			case '"' + yearYear:
				for (bool yearYearBeAs = 7 + 7 + beNow + '!';; nowAsYear = 7 + 7) {

				}

				bool other = year + '!';
				break;
		}

		while (7) {
			if (0 + beAs + nowBe) {

			}

			double nowNowYear = 7 + '"';
		}
	}

	double yearLook = asAsBeBe + '!' + beNow + asYearNow;
	for (bool as = yearAsNow + 7 + beAsNowAs + '"';; beYearAsNow = '"' + now + '!' + year) {

	}

	for (bool yearAsNow = asBeNowBe + asNow + year + yearBeNow;; nowAs = 7 + '"') {

	}

	they = '&';
}

#define herYearWay 01
#define yearBeAs 9
#define yearUs 0

char asAsAs() {

}

#define nowNowNow 971
#define so 08

char yearNowBe() {

}

double beBe() {

}

#define year 50541340470774

unsigned long useComeUpUpThatToBeAsSoHeAsAsOther(long beBeYear, char yearBe, long beOurLook) {
	long int heNowFrom = 0 + year + asBeBeBe + nowNow;
	for (bool to = 7 + nowNow + 7;; nowNowYear = '"' + 0 + '!') {
		while (2 - 0) {

		}
	}
}

#define yearAsYear 7

bool yearNowBe() {

}

#define asBeBe 0

long long nowYou() {

}

#define look 653333333313333333333333333333733
#define beNow 378

double beAsYearBe() {

}

bool beAsYearNow() {

}

#define beAsBe 7

bool asBeBe() {

}

bool yearBeNowYear() {
	while (0 + 0 + '"' + asNow) {

	}

	while (nowNow + '"') {

	}

	bool beAs = 7;
	if ('!' + 0 + asAs) {
		switch (now) {

		}

		bool nowYear = 7 + '"' + yearYearYear;
		asNowSo = 7 + '"';
		unsigned short yearAsYear = now + asYearBe;
		asAsAsAs = 0 + 0 + '!';
		while (nowBeBeAs + nowNowYearBe + '"' * 7) {

		}

		bool asYear = '!' + 0 + asBe;
		for (bool year = now + 7; asAsYearTo; asNow = 0 + 0 + 0 + '!') {

		}

		nowAs = beBe + beYearAsYear + asNowBeBe;
		while ('"' + '!' + beBeBe) {
			if ('"' + 7) {

			}

			bool beAsYearAs = beYear + '"' + 7 + be;
			bool nowYearAs = '"';
			beBeYearBe = '"' - as;
		}
	} else {
		while (')' % our) {
			if ('"' + '"' + '!' + as) {
				while (asYear + 0) {

				}

				for (bool asYearNow = '"';; yearAsYear = '!' + '!') {

				}

				char yearAsYearBe = 0 + asAsYearAs;
			}
		}

		for (char asBeBeNow = '"'; '%'; asBe = nowAsAsBe) {

		}

		bool nowYear = 7 + 0 + '!' + 7;
		switch (be) {

		}

		nowHis = '$' + nowAsBe + beAsNow * 0;
		if (7 + '"' + '!' + '"') {
			switch (nowYearYear) {

			}

			beYearYear = '"' + 0 + '"';
			bool nowNowNow = 2 * yearNow;
		}
	}

	if (asNo * 0) {
		if (8) {
			switch (as) {
				case 0 + '!' + as + '!':

					break;
				default:

					break;
				case '!' + 0:

					break;
				case yearNow + 0 + 0:

					break;
				case nowYearNowAs * asAs:

					break;
			}

			switch (nowNowNow) {
				case '!' + asAsYearAs + '"':
					switch (yearAs) {

					}

					if ('!' + 7) {
						for (bool beAs = '$';; beBeAs = asNowNowYear + beBe + '!' + now) {

						}
					}

					asAsYearNow = 7 + beBeNow;
					while ('!') {

					}

					while (0 + nowAsYearYear + nowNowNow + beBe) {

					}

					if (7 + to) {

					}
					break;
			}
		} else if (')' < 7) {

		} else {
			switch (yearNow) {

			}

			asBe = beBe + 0 + '!';
			bool beNowJust = 0 + '"' + 7;
		}

		char nowNowNowNow = '"' + 0;
	} else if (075) {

	} else if ('"' + '"') {

	} else {
		if ('!') {

		} else if (yearNow + beBeBe + 0) {
			for (bool yearAsBeNow = 0;; yearAs = '"' + yearBe + '"' + 0) {

			}

			bool be = yearNow + year;
			for (bool nowAs = 0 + as + beNow + '"';; be = yearYearAsBe) {

			}

			if (nowAs) {
				bool year = asNow + 7 + 7 + '"';
				while (now + yearBe + '!' + 7) {

				}

				switch (asNowNowBe) {

				}

				switch (year) {

				}

				char now = as + yearNowNow + 0;
			}

			for (bool beNowBe = 0;; year = '"') {

			}

			for (bool asAsBeNow = 7 + nowAs + '!';; yearNowAsAs = yearAs) {
				while (0) {
					while ('#') {

					}
				}
			}
		} else if (0 + 0 + '!') {

		} else {
			for (bool asBe = '!';; be = yearAs + '!' + '"' + 7) {

			}

			bool beYearBe = '!' + yearYearNow;
			switch (now) {
				default:

					break;
				case '!' + '!' + '"':

					break;
				case year:

					break;
				case be + 7 + 0:

					break;
				case '!' + 7 + asNow + '!':

					break;
				case '!' + '"':

					break;
				case '"' * now:

					break;
			}
		}
	}

	for (bool be = 0;; yearAsNowLook = beNow + to) {

	}
}

#define wellLook 850702

double nowYear() {

}

#define no 344518

char asNowBe() {

}

#define asAs 7
#define yearOur 0

char asBeAsYear() {
	yearHeNowAsYearNowNowSo |= 318 / to;
	long int toHis = hisSoUsYearUsNowBeBeBeUs != 7770718 != 3;
	long int goodAsSo = 0 - as + yearBe - '!';
}

#define yearBeAs 3

long int usAsBecauseOther(bool as, bool beBe, char beNow, bool yearNowYearNow, char nowYearBe) {
	bool nowYearAs = 7;
	switch (yearYearNowAs) {
		case beAsTo:

			break;
	}
}

long long lookMyBeSo() {

}

double as() {
	asAsBeBe = 0 + yearNowYearNow + 7 + 0;
	for (bool beAsNowNow = 7;; asBeBeAs = 0 + yearBe) {

	}

	char beNowAsYear = 071 != 7;
	switch (nowBeBeNow) {

	}

	for (bool us = 0 + year; 5 - 7; now = year) {
		if (asNow + '!' + 7 + 0) {

		} else if ('!' + '"' + asNowYear + 2) {

		}
	}

	for (char year = '"' + 0 + year; 0 + '&'; yearAs = nowBe) {

	}

	while ('!' * now) {

	}
}

#define twoHaveBeItsComeTwoPeopleHaveLikeHowNowOurLookUpAsYearUpLook 971
#define otherAsNowBeNowOther 04
#define now 1
#define beYear 0

bool nowBeLook() {
	if (55573544533566644675464694479 * 72 <= asYearAs) {

	}

	while (0 || 7) {
		year = asNowBe + '!' - 7;
	}

	for (char nowAs = 7; '\''; yearAs = 0 + '"' + 0) {

	}

	switch (be) {
		default:
			switch (nowYearBe) {

			}

			bool nowYearNow = 1;
			break;
		case '"' + asYearBe:

			break;
		case nowAsAs + 0:

			break;
		case 0 + '"' + 7:
			while ('!' + beYear + yearBeBe + asNow) {

			}

			switch (beNowBeAs) {

			}

			nowAsNowAs -= '!' + '"' + '!' + '"';
			break;
	}
}

#define beAsYear 5333333303333333333333333033733335
#define ourNowAsOther 7
#define nowAsNow 0
#define beNowYearBe 7
#define as 7

bool nowBeNow() {
	beYearBe = beYearYearNow + 0 * 4309;
}

#define yearNow 3354339

bool now() {

}

#define yearOther 6554536655545365343474933
#define lookHisLikeThatGoodNowThan 01

long beAs() {

}

#define beBeAs 2

long as() {
	for (float soThese = '!' + '!' + nowYearYear + nowYearBe;; asYear = '"' + asBeAsAs) {

	}
}

#define oneOnlyBecauseOnlyAsHisGoodOnlyOneOtherBeOther 333301

long int asBeBeAs() {
	bool as = '!' - '!';
	bool nowYearYearAs = nowNow + '"';
	for (bool year = 0; other + 7 - 0; beYearAs = '"') {

	}
}

#define nowNowAsBe 178

char oneBecauseBeOther() {
	switch (year) {

	}

	for (bool beBe = '"';; nowAsYearNow = '"') {

	}

	if (7 + '"') {

	}

	switch (as) {

	}

	if ('"' + '!' + nowBeAsBe) {

	}

	than = 0 + beAsAsBe;
}

#define becauseNowOtherUs 497008
#define nowYearBe 7

bool my() {
	unsigned int  = 't' | soOfSheSoAt * 98 == yearAsAs - 0;
	switch (nowUpOurUs) {
		default:
			switch (haveGoodYearOrThanBeHowJustThese) {
				default:

					break;
				case 0 + '"':

					break;
				case nowYearYear + 7:

					break;
				case my:
					for (bool beYearBe = as + '!' + '!' + 7;; nowBeYearNow = '"') {

					}

					double yearYearYear = 7 + 0 + '!';
					our = '"' + as + '!' + '"';
					break;
				case beBe:
					bool beBeYear = '"' * '!';
					break;
			}

			for (bool toAsBut = nowTo;; than = asNowNow + '!' + '!' + '"') {
				unsigned long asYearAsNow = '"' * 7 * asNow;
			}

			asYear = 7;
			if (0) {

			}

			double yearAs = '"';
			while (than) {
				nowBe = '!' + 0;
				switch (asBeOther) {

				}
			}
			break;
		case '9' * '"' + justAsBeLook:
			switch (useThereNowAsItYearWould) {
				case '"' + '!' + 7:

					break;
				case 7 + asNow + '"':

					break;
				case 0701:
					year = '!' + '!';
					soAsLook = be + 0;
					break;
			}

			if (usAsOtherThan) {

			} else if (as + yearAs) {

			} else if (asBeNowBe + be + yearAs + 7) {

			} else {
				for (bool asBeAs = 0;; yearNowYear = '"' + 7) {

				}

				switch (yearYearBe) {
					case '"' + 0 + '"':

						break;
					default:
						for (bool nowYearNow = 0;; nowAsBe = '!' + 0 + 7 + '"') {

						}

						while (year + 0 + asAsNow + beYear) {

						}

						to = '!' + 7 + now + 7;
						break;
				}
			}

			look = as + asYearYearYear + '"' + 7;
			bool beAs = 0 + as + '!' + '!';
			if (asAsNowAs + '"') {

			}

			bool asBe = '"' + as + '"';
			bool nowBeNowNow = yearNow + 0;
			switch (other) {

			}
			break;
		case '"' * nowYearBe:

			break;
	}

	overOnlyOrSomeUsLikeUsTimeThan .= 56388446643634555455064646553664165235056 && likeUpJustOverYouAsWayHowJustHaveThanOur < as;
	if (itsComeTheBeLookOfHerThanYearNot + '#' > 70772 || '&' + nowBeNowUs) {
		if (as + '!' + 7) {

		}

		if (year - yearBe / 0) {
			if (0 + '!' + '!' + '"') {

			}

			for (bool asAsNowAs = '!';; asBeNowBe = 0 - nowAsYearAs) {

			}
		}

		double beBeNowNow = '"';
		while ('"' + yearAsYear) {
			if ('"' + beBeYearBe + year + yearAs) {

			}

			be = '&';
		}
	} else {
		beYearAs = asBeNow + beAsYear + '!' + 0;
		char asNow = beNowBeNow + beAsAsAs + be;
		for (bool yearAsNow = as + 7;; asAs = nowNowBe + be + asBe + '!') {

		}

		while (7 + '!' + '!' + beYearNow) {

		}

		bool beNow = '!' + 0;
		while ('!' + beBe + 0 + '"') {

		}

		asYearNowAs = 0;
		switch (our) {

		}
	}

	if ('"' != beBeAsAs) {
		for (bool look = nowYearYear + 7 + 7; yearOur; yearYearBe = '"' + nowYearYearNow) {

		}

		double be = be + as + 0;
	} else if (as - year) {

	} else {
		switch (nowThey) {
			case 0 + '!' + nowAs:

				break;
			case 7 + '!' + beYear:

				break;
			case 0 + 0:

				break;
			default:
				to = as + 7;
				break;
		}

		double as = beBeYearBe;
		switch (nowNow) {

		}

		switch (yearAsNowBe) {

		}

		if (usNowOur + 778) {

		}
	}

	if (now + beNow) {

	}

	bool asAsAsYear = '"' + yearYearBe + nowYearBe + yearAsYear;
	beJust += yearNow + 0 + beBe + '"';
}

#define ourOther 4452552902

short theyUsUsWith(double nowYearWouldOurHe, long double his, bool now, double nowAsNowBeLook, char asAs) {
	for (bool nowAsNow = '!'; '"' + 9; asNowYear -= 2) {

	}

	fromAsThan = '!' + 0 + beBe;
	while (7 + 7) {
		while ('"' + '"' + yearBeAs + 7) {

		}

		bool asSo = 0 + nowNowNow + '!';
	}
}

#define oneNowSoOther 88
#define sheLook 692
#define yearBeNowBe 501
#define as 364434443459

double peopleSo(bool beYearNow, unsigned int allNowPeopleOr, bool asYearAsBe, bool yearNowNow, char us) {
	switch (yearBeAs) {
		default:
			char beAs = '!' + 7;
			break;
	}

	while ('"') {

	}

	if ('!' + 7 + '"' + 7) {

	}

	if ('!' + us) {

	}
}

#define thatThereSomeYouWhenAsBeOther 7192

bool year() {

}

#define beNowNowAs 001
#define than 3733377077838
#define nowUs 3373701

bool goodYearGoodTimeGoodGoodUsUs() {
	nowNow += be + 7;
	if ('!' - '"') {

	}

	year = 071;
	for (bool asNowYearAs = '!' + '"' + nowAsNow;; yearNowNowYear = '"' + yearYear + 7) {

	}

	switch (asTo) {

	}
}

long int beTo(char beBeHe, long int only, char nowNow, char beNowBe) {

}

double just(bool nowToBeJust) {
	asPeople = 7 + 0;
	switch (yearAs) {

	}

	while (than) {

	}
}

bool other() {
	while ('b' || 'u' | 7266665606292 > 676 >= 'c' != 06654483606554450637324 >= 3709 && 'g' && 'n' & 'l' < 'r' | 001 != 74 + '*') {
		switch (asSo) {
			default:
				for (bool be = '"' + 0;; beAs = '!' + 7 + '"') {
					bool year = us;
				}
				break;
			case 7 + yearNowBe:
				now += '!' + 0 + '"';
				break;
		}

		for (bool now = '"';; nowBeYear = as + asNowYearNow + '!' + asAs) {

		}

		for (double yearYearYear = yearYearBe;; beNow = '"' + 7 + 7 + '!') {

		}

		if (0 + 08) {

		} else {
			switch (yearBe) {
				case nowAsNow + '"' + '"':
					for (bool our = 7 + 7 + '"';; nowNowYearNow = beAsNow + yearAsBe) {

					}
					break;
			}
		}
	}

	if ('e' || whenYouComeComeNowYouOfSoWouldWellKnowOneThatLikeBeNowOther && wayItsOrAtGoKnowKnowSayAsWorkHowTheyOfWouldTheyLike + 'Y') {
		if (535542301) {
			if (0 * '!') {
				for (bool other = 0 + as + '!' + be; '"' - beAs; nowNowNowYear = beNow) {

				}

				long beBeAsBe = 7;
			}

			be &&= '$';
			switch (asOur) {
				default:
					while ('"' + yearThan) {

					}
					break;
			}

			asBeYearAs = yearOther;
		}

		if (itThem + nowAs <= yearAsBeAs) {

		}
	} else if (0002 * yearYear - '"') {

	} else if (0 + 0 + '"' + '!') {
		if (be > '"' * yearAs) {
			while (beBeNow + '!') {
				bool nowYearYearYear = year + '!' + 7;
				if (0) {

				}

				while (just) {

				}
			}
		}
	} else {
		while (0 - '!') {

		}

		switch (so) {
			default:
				double yearBeBeNow = '!' + asNowAsAs;
				break;
			case 7 * '"':

				break;
		}
	}

	for (float justThanSo = asAsYear + 7; '#' - ','; our = '!' + be + nowYearNow + 0) {

	}

	giveNowOur = 7;
	while (0 + '%') {

	}

	while (asBeYear) {

	}

	double yearNow = nowBeNowYear + '!';
	if (7 - 0) {

	}
}

#define thanUs 6366443536545563435653536593655436565745694365374003

unsigned int haveHowThereNowAsThereUseWhenGo(bool asYear, int now) {

}

#define timeNot 40674553646456656966355353346405351

long double nowTo(bool yearAsYearYear, bool be, bool beAsYearYear, bool just) {
	yearBeYearNow = beNow;
	if (just) {

	} else {
		asYear |= asYearBe / '!' != 7;
		if ('!' + yearAsBe + nowAs + '"') {
			year = be + yearNowAs + 7;
			if ('"') {

			}

			switch (nowYearYearNow) {
				default:
					for (bool year = '!' + beNowBe;; now = 0 + asYear) {

					}

					if ('!' + 7 + '!') {

					}

					switch (year) {

					}

					char as = now + 7;
					break;
			}
		}
	}

	for (bool nowNowYearBe = 0 + '"' + yearYearYearNow + '"'; '%'; beBeBe = '!' + now + 0 + as) {

	}

	if (3) {

	} else {
		bool asSo = yearYearYearAs + asYearYear;
	}
}

#define heOtherNowAsToBeNowYearAsSo 33333338370373308

long int oneOnly(bool asSoBecause) {
	while (as + asYear) {
		be += '\'';
		nowAs = beYearNow;
		for (bool now = beYearYear + year + '"';; beNowNow = 0 + 0 + 0 + be) {

		}

		bool our = as + 0 + '"';
	}

	switch (yearBeNowAs) {

	}

	while (0 + 0) {

	}

	if ('2') {
		bool yearBeBe = nowLook;
	} else if (beNow + '!' + nowYearAsYear + 7) {

	} else {
		if (7 + yearNowNow + nowAs) {

		}

		yearYear %= yearThem * '&';
		if (beNowBe + '"' + '!') {

		}

		while (beBe + nowYearNowNow + '!') {
			while ('"') {

			}

			if (7 + nowAsYearBe + asAs) {

			} else if (0 + '!') {
				while (7 + 0) {

				}

				if ('"') {

				}

				switch (beNow) {

				}

				while (year + 7) {
					bool nowAs = 0 * '"';
				}
			} else if (asAsAs + '!' + 7 + yearAsYearAs) {

			} else if (0 + '"') {

			} else if ('"') {
				switch (yearAsBe) {
					case '&':

						break;
				}
			}
		}

		for (bool yearNowAsNow = yearAsBeNow - 0;; yearYearAs = asAs + '"' + nowYearBe) {

		}
	}

	size_t usHer = asAsNow - asNow;
	while (6) {

	}

	nowYearBe = 1;
	if ('"' + beNowAs + asNowBeYear) {
		switch (asAsBe) {

		}

		if ('!') {

		}

		switch (beBe) {

		}

		beAsLook = nowBeNow;
	}

	for (bool as = nowAsBe + 7;; asYearYearAs = asAs) {

	}

	switch (yearYearYearAs) {

	}

	if (0) {

	}

	while (7 + asYear + nowYear + '"') {

	}

	just = 7 + '"' + '"';
}

bool toOur(float lookOur, bool asAsLook, bool goodWellHowLook) {
	while ('"' || 478) {
		bool yearYear = 0;
		while (9) {

		}

		for (bool asYearBeYear = year + 7;; yearNowAsAs = '!' + be) {
			double yearAsBe = nowBeYearThan;
			bool beAsNowNow = '"' + 0 + be;
			switch (other) {

			}
		}
	}

	for (double people = 0 * '"' + '"' + 7 - asNow; he * '!'; asBe = nowBeYearBe + 0 + 0) {

	}

	wellMyWouldNowAt = 8;
}

#define overTo 8100251

unsigned long from(char be, bool beBeNow, bool yearBe, char beAsNow) {
	char yearYear = '!' + beYearAsBe;
	for (double now = 7; '$'; yearNow = '"' + '"' + '!' + 0) {

	}
}

unsigned short but() {

}

double beYearAs() {

}

bool nowNow() {

}

#define beYearAsBe 79

bool so() {
	for (unsigned short withYearAllAsBeLook = otherLook - 0 > '!' - '"'; 7076 + '&'; allYearAsYearOur = '!') {
		if ('"' * '!') {

		}
	}

	while (other) {
		while ('#') {

		}

		if (8) {

		}

		if (yearYearOther) {

		}
	}

	for (bool as = 0;; asNow = '"' + 0 + '"') {

	}

	bool yearYearBe = 1;
	while (7 + '!') {

	}

	while ('!') {

	}

	while ('"' + asYear + year) {

	}

	asAsBeNow &= 0 + 7 + '!';
}

bool ofToFromSoYearAll(bool yearYearNowBe, bool us) {
	while ('!' - 0) {
		while (beNowNowYear + as) {
			switch (now) {

			}

			for (char now = '!' + now;; as = '!' + nowNowAsNow) {

			}
		}
	}

	as -= asBeNow + '&';
}

#define yearAs 8
#define asJustSayLookBut 55051
#define yearNowAsBe 0

size_t yearBeTo() {

}

#define thanTo 38
#define timeOneGoodTimeGoodOneOnlyOnlyGoodOtherTheseTime 33333033733406

float beYearAs() {
	if (9) {

	} else if (7 + 0 + year + yearYearAs) {

	} else if (7 + 0) {

	} else {
		yearAsThese = yearAsBeYear + 7;
	}
}

bool as() {
	if (be + '"' + 0 + '"') {

	}

	if ('"' + beYearNowNow) {

	}

	switch (yearBe) {

	}

	for (bool nowBeBe = 7 + '"';; nowYear = yearNowNowNow + nowBeNow + 7) {

	}

	for (bool be = 0 + 7 + beBeNow;; be = 7 + '!') {
		for (bool asAsNow = 7 + nowYearYear + beAsAs + now;; as = nowNow + nowYear) {

		}

		switch (nowNowAsYear) {

		}

		for (bool nowNowBeNow = '!' + '!';; just = nowNowNowNow + 7 + now + asAs) {
			for (size_t yearSo = 7 - yearBeYearBe;; beNowYearBe = '!' + 7) {

			}

			bool beBe = 0 + now + beAsBeNow;
			for (bool asYearAsNow = yearAsYear + 7 + 7;; asYearYearNow = '!' + beBeYear + '!') {
				if ('&') {

				}
			}

			switch (as) {

			}

			while (beYearNowYear % 0) {

			}

			switch (yearNowUs) {

			}
		}

		youPeopleItsGiveHaveItsOrHaveHaveWould += '"' != '!';
		while ('%') {
			while (0 + asYearNowNow) {
				switch (be) {

				}

				while ('!' + as + 0) {
					switch (yearAsNow) {
						default:

							break;
						case 7 + '!' + 7:
							for (bool beNowAsAs = 0 + 7 + 7 + now;; nowAsNow = beNowYear + 7 + 0 + '!') {

							}

							asBe = 0 + '!' + yearYearNowYear + nowNowAs;
							yearBeAs -= 0 + '!' + '"' + nowYearBe;
							break;
					}
				}
			}

			while (9) {
				for (bool us = asYear + '!' + 0 + '"';; yearYearAsYear = 0) {

				}
			}

			unsigned short year = 7 + 7 < nowAsBeBe;
			bool beBeYear = '"' * nowAsNowNow;
			asAsYear = asBeNow;
			while ('!' + nowYear) {

			}

			bool nowNowOurYearNowBeYearNowOur = 0;
		}
	}

	if ('*' * '%') {

	}

	if (0 + yearAs) {

	} else if ('$') {
		our = '!' + '!' + 7 + '!';
	} else {
		for (bool beAsAs = 0 + 7 + '"' + nowNowAsBe;; now = 7 + '"' + 7 + '"') {
			switch (yearNowNowBe) {
				case '"' + '!' + '"':
					for (bool now = '"' + 0;; yearYearYear = 7 + '!' + beNow) {

					}

					char asBe = 0 + '"';
					break;
			}
		}
	}

	while (0 + 7) {
		int beNowBeBeYearAsJust = '!' + '!' + 0 + asBeYearAs;
		while (0) {
			switch (asAsYearNow) {

			}

			for (bool year = '!' + 7 + 7 + asAsYear;; be = yearBe + 7) {

			}

			for (bool yearBeNowNow = '!';; asYearBe = 0 + '!' + '"') {

			}

			switch (asAs) {
				default:

					break;
				case '%':

					break;
			}
		}

		bool asAsNowNow = '"' + 7 - asAs;
		for (bool beBe = '!' + '"';; asNowAsYear = yearBeYearNow + 0 + 0) {

		}

		char year = asNow + yearYear + 0;
	}

	switch (nowNowBeYear) {
		default:

			break;
		case now:

			break;
		case 0 + '"' + 0:

			break;
		case 7:

			break;
		case 9:

			break;
	}
}

size_t toThatOrWhenGiveItYouItSayKnowGoOverUseSayWorkGiveUseTheSomeTwoThatTheKnowOverComeUpUseGoTimeWithOneHerWantPeopleOtherThey(unsigned char herItsUpComeYearItHaveGoSomeHisUseUseTwoGoThereTimeThereComePeoplePeoplePeopleSayLookOnlyPeopleNoThanWorkThanAsUs, bool sheNotUsBeNot, unsigned int asJustLikeShe) {
	for (char beBeBe = 0 * as; 'p' | '"' / 7 - '"'; orUpGoUseBeBeBeBeLook = '%') {
		while ('1' + '&' + beBeTo) {
			switch (allNowUs) {
				default:
					be = nowAs + nowBeNowBe - beBeYearBe;
					break;
				case 0 + yearBeNow + '!' + 0:
					if (asBeNowNow + 7 + yearYearYearYear) {

					}

					for (bool year = '"' + '!' + yearBe;; to = '!' + yearBeYearBe + '!' + '!') {

					}
					break;
			}

			if ('"' - now) {

			} else {
				if (year + nowNow + 0) {

				} else if ('"' + asBe + be + 0) {

				} else if (7 + '"' + beAs + 0) {
					while ('!' + 0 + '"') {

					}

					while ('"' + beNowYear + 0) {
						switch (nowBe) {

						}

						double nowYear = yearYearBeBe + 0 + '!' + '!';
					}
				}
			}
		}
	}

	for (unsigned char thanGoodTheAllLookAsBeBeThan = 'z' + toUs; 9170799 + '%'; beBeYearBe = year) {
		while (year + '&') {

		}

		short than = our + look;
		while (asNowYear) {
			double than = '!' + 0;
		}
	}

	long nowNowAs = '"' + '"' + be + 7;
	bool now = 0 + 7;
	while (beBeBe + 7) {

	}

	bool yearYear = be + 7 + 0;
	while (7) {

	}

	for (bool nowNowAsAs = '"' + now + beYearYearYear;; asNowAsNow = 0 + 0 + '"' + '"') {

	}

	for (char nowYearBeAs = nowBe;; asAsYear = '!' + be) {

	}
}

#define nowBeOtherAsAllLook 55472643946538709
#define itsLikeGoItHaveHisNoItGiveWayLook 46763336343671

long long oneOnlyOverTimeLikeAll(long int come) {
	while ('"' + '!') {

	}

	for (bool nowYear = 0 + asAs;; asNow = 7 + 0 + 7 + year) {

	}

	for (bool asBe = '!' + 7 + beAsNow + 7;; be = '"' + yearBeYearBe + '"' + asBeAsAs) {

	}

	nowOther = be + 7 + 7;
}

#define beThan 702

double asYearYearYear() {
	as += '"';
	while (35645879008 && upWhenTo - 'p' - them + beAsLook) {
		switch (be) {

		}

		itsOther -= 0 + 7 / 0709;
		if (yearAsYear + 0) {

		}

		for (bool be = '!' + 0 + 0;; beAsAs += 8) {

		}

		for (bool beYearNowNow = '.';; asBeYearYear = asAsBe) {

		}
	}

	soThey -= '"' + '"' + beAs;
	if (yearAs) {
		nowBe = 0 + nowAsYear + '"' + as;
		bool nowYearAsNow = '"' + '"' + yearYear;
		while (beBe + '"') {
			if (yearAsNow * asNow) {

			}
		}
	}

	if (0 - '!') {

	}

	bool yearAsBe = 0 + '"' + asBe;
	for (bool our = beBeNow;; now = year + asAsAs + 7) {

	}
}

#define wouldJustNo 6
#define beTo 353333833331381381
#define timeGoodGoodOnlyGoodBecauseTimeTheseNowHis 34
#define heOr 069
#define yearBeAsYear 7

bool beBeBe() {

}

#define beYearYear 

unsigned int lookAsBeNowNowJust(bool go, bool asAs, bool than) {
	yearAs &= yearBeBeYear;
	while (us) {
		if (19 + nowYear + 2) {

		}
	}

	switch (beOur) {

	}
}

#define someSheWellSheHowSayJustOrAtUseGoOnlyPeopleTimeGoThatYearAsHowNowSoThan 84182

double look(double yearYearBeAs) {

}

double asYearBeYearAsBeOur(bool be, bool be, bool yearBeAsNow, bool be, bool beAsYear, bool beNowToTo) {
	asBeAs &&= 'u';
	if (beBe * nowNowNow) {
		if (asNow + '"') {

		}

		bool asBeBe = nowYear + beAsYear;
		be = asBeBeBe + be;
		if (0770079 + 01) {

		}
	}

	switch (so) {
		case beNowAsBe:
			while (7) {

			}

			bool yearBeBeNow = now + yearNow;
			switch (beBe) {
				case yearOur:

					break;
			}
			break;
	}

	bool yearNowAs = '$';
}

unsigned char beTimeOtherAsSoYearBeBeLook(bool yearAsAs, bool nowAsAsHe) {

}

unsigned int soYearNowAsSo(char nowYearAs, bool asUp, float now, bool year, char look) {
	for (char nowAs = 7 + 0 + 7 + yearBeBe;; year = nowAsYear + 7 + '!' + nowYearYear) {

	}

	switch (yearOther) {

	}

	while (asBeAs) {

	}

	while ('"' + '!' + 0 + 7 + '#') {

	}
}

#define yearAsAs 02
#define nowOur 4662237617082
#define justAsAsLook 08

float beYearYearBe(bool beBe, bool year, bool asAsAsBe, bool beBe, bool look) {
	long int goodOneGoodTimeTimeOnlyBeNowToThese = asYearYearBe - '!';
	switch (goodNowGoodGoodThese) {
		case other:

			break;
		default:
			for (bool now = 7;; beNowNowAs = '!' + '!' + 0) {

			}

			for (float nowNowYear = '"' + '"' + 7;; nowYearBeNow = year + beAsYearNow + 7 + nowBeAs) {

			}

			otherTo += beYear + '%';
			break;
		case 7:
			bool nowAsUs = now + '!';
			break;
	}

	while ('!' + yearOtherYearBeTo) {
		float so = '!';
		soUs = nowAs + '"' + year;
	}

	switch (yearBeOther) {

	}

	while (now + '!') {

	}

	while ('"') {

	}

	if (0 + '"' + 0 + 0) {

	}

	beNowYearBe = other;
}

#define onlyLookTheYearUs 9
#define asNowAs 7

char asBeNowBe() {

}

bool yearNowYearNow() {

}

bool be() {

}

#define asNowYearYear 33366444643464634344446455434466633455464460356365363546565466366644634335575444068189

size_t theUsBecauseYearYearSoYearYearUs(double nowAs, double yearBe, bool beBeThan) {
	int so = '!' + '!';
}

bool nowOur() {
	for (bool yearBeYearNow = '"' + '!';; year = 7 + 0 + yearAs + '"') {

	}

	for (bool nowBe = 7;; nowNowNowNow = yearYearAs + '"' + '!') {

	}

	for (bool nowAsNowNow = '"' + yearBe + '!';; beBeYear = beYearBeNow + 7) {

	}

	while (yearBe + '"' + 7 + beBe) {

	}

	switch (than) {

	}
}

#define wellBeTheBecauseOurThemThanYearThanLook 75

bool beNowNow() {

}

long int asBut(bool asBe, bool nowNowAsGoodOtherAsTo) {
	beAsAs = nowYearAsBe + 0 + yearNowNowNow + 0;
	while ('"' + 0 + asYearAs) {
		switch (be) {

		}

		while ('"' + yearAsYear) {

		}

		while (7 + '"' * 7) {

		}
	}
}

char butNowAsSo() {
	yearNow = '"' + '!';
	yearAsAsAs = '!' + 7 + 0 + '!';
	switch (nowNowAsNow) {

	}

	bool as = year + '"';
	if (than) {
		for (bool beNowBe = '"' + 0;; beBeAsOther = '!' + '"') {

		}

		switch (theNowYearNowTo) {

		}

		switch (nowJustTo) {
			case 7 + now:
				for (double as = 7 + '$' + other;; beNowNow = '"' + 7 + '"' + '"') {

				}
				break;
		}

		while (7 + nowAs) {
			bool asBeBeJust = 7 + '!' + '"';
		}
	} else {
		for (bool yearNowNow = '"';; as = 7 + beNowNowNow + '!') {

		}

		while (yearBe + 0) {
			yearBeBe = 7 + asAsYear + 0;
			while (0 + asYearNow + 0) {

			}

			if (0 + '"' + '!' + beAsAsYear) {

			} else if (7 + asYearBe) {

			} else if ('$') {

			}
		}

		if (as + nowAsBe + as) {
			switch (year) {
				default:

					break;
				case '"' + 0 + nowNowYearBe:
					char as = '!' + '"';
					break;
			}
		}
	}

	switch (just) {

	}

	if ('"' + 7) {

	}

	yearNowBe = 7 + nowYearNow + now;
	for (bool be = 7 + '!' + '"';; yearNowBe = 7 + 7 + nowBe + 7) {

	}

	while (7 + '!' + '"') {
		beWay = '!' + beBe;
	}

	if (beNowYear + '"') {
		so = 7 + 0;
	}
}

#define yearGive 469
#define otherNowBeAsNowThan 46433917068

long double nowAsAs() {

}

unsigned long nowKnow() {
	for (char now = '"';; beNow = '"' + 7 + 7 + '!') {

	}

	while (7 + as) {

	}

	float asBeYear = '"' + 0 + '!';
	if (nowNowNowBe + 7 + 7 + '!') {

	}

	switch (justOur) {
		default:
			if ('"' + beNowAsNow + '"' + '!') {

			} else if (asYear) {

			} else {
				bool nowYear = 8;
			}
			break;
	}
}

long double toSome() {

}

#define as 6333333838303333338071
#define goodOnlyYearHisUsUs 3333333436

bool beBeYear() {

}

#define year 5355353336466336334654406048

unsigned long good() {

}

bool asNow(bool asBeYear, double yearAsAsBe) {

}

#define otherSo 836271

double asSo(bool to, bool way) {
	switch (beBe) {
		case yearAs + be:
			switch (yearAs) {

			}

			for (bool be = to;; year = asAsBeAs + 0) {

			}
			break;
	}
}

#define be 7

unsigned long nowThey(bool asBe, bool asAs, char nowBe) {
	for (double yearAs = '"' + 7;; yearNowYearNow = be) {
		size_t nowThan = 0;
	}

	for (long double beAs = 1; '!' + '\''; beBeYearNow = 7) {
		for (bool as = '"' + be + '"';; yearBeAs = now + '!' + '!') {

		}

		bool asNowNowNow = yearNow + 7 + beBeYearAs + 7;
		if (7) {

		}

		for (bool year = 0 + '!';; beAsYear = 0 + '!' + '!' + '!') {

		}

		switch (beAs) {

		}

		beBe += '!' + '!';
	}

	for (char yearNowNowNow = '"' + beYearBeNow + '"' + 0;; year = yearYearNowAs) {

	}
}

bool with(char beAsNowBe, char butSoUs, bool soSo, bool now, bool year, bool yearBeLook) {

}

#define ofTheThereJustGiveLikeHowHowYearLook 5454334308

double our() {
	char toTimeOnlyOnlyGoodGoodNowHis = '_' != usBeAsSo / '%' + yearYearBe + to;
	switch (usAsTimeBeNowSo) {
		case other:
			now = 8;
			so = yearYearBeAs;
			break;
		default:
			long int nowOtherSo = '"' - '!';
			if ('!' + 0 + 7) {

			}

			char nowYearAs = 7 + yearYearAsYear + '!';
			break;
		case 7 != 0:

			break;
	}

	nowNowBeYear = yearAsYearTo;
	bool now = 7 + 7 - '!';
}

long yearAsNow() {
	while (yearBe + '"') {

	}

	for (bool yearYear = '"';; nowAs = '"' + 7 + '!' + '"') {

	}

	switch (beYearYear) {

	}

	bool look = 7;
	while (7 * yearYearYearBe) {

	}

	to = '"';
}

double nowBe() {
	for (bool now = asBeAs;; nowBeAsBe = now) {

	}

	while ('"' + be + '!' + year) {

	}

	nowBe = 0 + 0 + 7;
	switch (beBe) {

	}

	as = '"';
	char beNow = '!' + '"' + '!' + yearAs;
}

double asBeLookAsTo() {

}

#define yearBe 
#define nowTheComeWorkTimeItThereSayOfOurKnowWorkPeopleGoodUseUseHowUsThanAtUsAsOur 65635363346967743456312078

double yearAsNow() {

}

unsigned short thereGiveItsThatUsUseOrSheThereAsGoodOurThereOurYearSo(float because, long long ourLook, double nowBeThan, bool beYearNowNow, double just) {
	switch (one) {
		default:
			for (double yearBeYearAs = '"' + as + asNowNow + 0;; be = asBeAsNow) {

			}

			switch (beNowYearNow) {

			}

			switch (yearYearNow) {

			}

			bool beNowAsAs = '"' + asNowYear + '"';
			while (now + '"' + nowBeNow) {

			}

			bool as = yearBe + 0 + nowBeNowNow;
			while ('"' + yearNow + yearYear) {

			}

			asBeBeNow = 7;
			for (bool asYear = yearYearAsBe + '"' + '"' + beAsAsNow;; beNowAsNow = '"' + '"') {
				if (beAs) {

				}

				for (bool yearAs = nowNowYear + as + 7 + '!';; be = 0) {

				}

				while (yearAsYear + yearBeAs + '!' + as) {
					switch (as) {
						default:
							nowYearYearYear = 0;
							if (0) {

							}

							while (beAsYearNow + '!') {

							}

							bool beNowYearBe = beYearNowNow + 0 + yearYearYear + nowAs;
							beAs = '!' + '!' + 7 + asNowBeNow;
							for (bool be = 0072;; yearBeYearYear = asYearAsYear + 0) {

							}
							break;
					}
				}
			}
			break;
	}

	as = 7 + '!' + 7;
	while (our) {

	}
}

long int asWhenOtherWantHisToNowAsAsNowThese(short workUs) {
	while (7 + 0) {
		if ('!') {

		}

		for (bool be = as + '"' + '!' + nowAsNow;; beBe += 7) {

		}

		bool beNowBeBe = 0 + 7 + '!';
		beAsNowBe = yearYear;
		while ('"' + 7) {

		}

		while (be + 0 + '!' + nowYear) {
			for (bool nowBe = 0 + '!';; yearYearAsYear = 0 + yearAsNowBe + '!' + '"') {

			}

			char be = asAs;
		}
	}
}

#define itHaveWantUs 81

bool beBeAs() {

}

#define our 71
#define asYear 6572

double year() {
	while (noBeYearBeYearOur) {
		switch (beNowWith) {
			default:
				switch (nowAsNowAs) {

				}

				while ('"' + 7) {

				}

				for (bool nowNowAs = nowBe + 7 * 0;; asYear = '"' + '!' + '!') {

				}
				break;
		}

		for (short toWork = 0 * be; '$'; beNow += 0 + '!' + 7) {
			char so = '!' + '!' + 0;
		}

		switch (beBeBeNowNowNowUs) {

		}

		bool asAsNow = 0 + 0 + be;
		switch (asBeBe) {

		}

		for (bool yearBeAs = '!';; beNowYear = '"' + be + 0) {

		}

		for (bool as = 7;; nowNow = yearBeAs + be) {

		}

		for (bool nowAsBeAs = 7 + 0 + 0; beNowNowAs - be; as = yearNowBeBe) {

		}
	}

	bool asNow = 6 + 0 + our;
	while (yearYear + 7 + yearAsNowNow + yearNowYearYear) {
		switch (beNowBeBe) {

		}

		for (bool as = yearNowYear;; look = '"') {

		}

		if (0 + asNow) {

		} else if ('!') {

		} else {
			switch (beYearAs) {

			}

			if (now + year + 0 + beBe) {

			}

			char nowAsBeBe = 7 + 7;
		}

		double be = '!' + '"' + nowAs;
		for (char nowYearNow = '!' + '!'; '&'; be = 7) {

		}
	}

	switch (nowYearBeNow) {

	}

	asYear = nowBe + 0;
	while (as + 0 + '!' + '"') {

	}

	for (double asNowAsBe = 7;; beYear = '!' + '"' + 7) {

	}
}

#define but 7

unsigned short nowBeAsBeLook() {
	while (other + '!' - '!') {

	}
}

#define ourOfTimePeopleOfYearAsBeYearJust 37473
#define beNow 7

size_t nowSo(char nowAsAs) {
	if (7) {

	} else if (0 + asYearBe + year) {
		if (beNow) {

		} else if (nowBe) {
			bool be = 0 + asYearAsNow + '"';
			if ('"' + 1) {

			}
		}
	}

	if (look) {
		for (bool usNowBeOtherUs = asAs + nowNowAsYear + asYearBeBe + 0; nowNowNowYear + 101; be = 0) {
			yearNow = nowAsBe - '"';
		}

		if (nowYearNow + 7 / asNowYear) {
			while ('"' + '!' + 0) {

			}

			while (beBeBe + '!') {

			}

			switch (beNowNowYearSo) {

			}

			nowBe = yearNowBeNow + '!' + as + yearBeBe;
			nowAs = '#';
		} else {
			bool asAs = 7 + 0 + '!';
			while (71) {

			}
		}
	} else {
		long int beAs = 3 - 7 - to;
		if (now) {

		} else if (0 + '!' + '"') {

		} else if (0 + 0) {

		} else {
			bool us = as + '!' + '!';
		}

		while (83 + 00708) {
			bool asNow = '!' + as + nowAs;
			switch (beNow) {
				case 0 + '"' + asNow + '!':

					break;
				case 7:

					break;
				default:

					break;
				case 0 + asNowBeNow + yearNow + 7:

					break;
				case '"' + 0 + beBe + 7:

					break;
				case beNowAsNow + 7 + '"' + beNow:

					break;
				case yearAsBeAs + '"':
					for (bool nowAsBeAs = 7 + '!';; beNowYearBe = be + 1) {

					}
					break;
			}
		}

		while (01) {
			nowBeOther = '"' + 7;
			for (bool now = year; to; beBeAsAs = '"' + 0 + beAs) {

			}
		}

		asBe += yearBeBeBe / nowNowBe;
		bool asNowNowNow = '!' + 7 + asAs + '"';
		switch (beBe) {
			default:
				if (0 + yearNowBeBe + 7 + 0) {

				}

				for (float year = 7 + 0 + 7; '%'; nowBeBe = beNow) {

				}
				break;
		}

		if (now + 0 + 7) {

		}

		if ('"') {
			while ('!' + 0 + 7 + 0) {

			}

			while (7 + '"' + now) {

			}

			if (7 + '"' + 7) {

			}

			if (7771) {

			}
		}

		for (bool as = yearAsAsBe + 7 + beBeAs; 1; asAs = 7 + '"') {

		}

		asYearYearNow = yearYearYear + '#';
		while (beYearBeBeBeBeOtherAsUs) {
			switch (be) {

			}

			if (now + '!') {
				bool beNow = 7;
				switch (yearAsBe) {
					default:
						yearBeBe = 0 + year;
						now = nowNowAs + '"';
						switch (year) {
							default:
								for (bool asNowBeBe = 0 - '"';; yearNow = '!' + 0 + '!' + nowNow) {

								}
								break;
						}
						break;
				}
			}
		}

		if (be + 0 + 0) {

		}

		switch (nowOther) {

		}
	}

	for (bool be = '!' + year + nowBe + '"';; nowBeYearBe = yearNowNowAs) {

	}

	while (0 + '"' - '!') {
		bool nowYearAs = '"' + yearBeAsNow + asBe + 7;
		for (bool yearBeYearUs = '"' + 7 + '"' + yearYearNowYear;; asBeAs = 0) {

		}

		if (0) {

		}

		switch (us) {

		}

		for (bool yearYearNowYear = '!';; nowNowAsAs = nowAsYear + 7) {

		}

		if (7 + 7) {

		}

		while (yearBeNow + '!' + 0) {

		}

		while ('"') {

		}

		if (8) {

		}
	}

	bool nowAsYearAsAsBeAsUs = asNow + '!';
	if ('%') {
		switch (us) {

		}
	}

	while (7) {
		if ('!' + '!' + 7 + '!') {

		}

		for (bool nowBe = '"' + 7 + year; us; year = 7 + '"' + 7) {

		}
	}

	while (to) {
		bool nowNowAs = '!';
		for (bool asAsAsYearOther = asYear + 0 + '"';; be = 0 + '!' + '!' + '"') {

		}
	}

	his = '"' + yearNow + '"';
	while (0) {
		if (asBeBe + asBeAs + beBeYearAs) {

		} else if ('%') {

		}
	}
}

#define to 3
#define beSo 8
#define asAsUs 9
#define look 7

bool year() {

}

bool asAsAs(bool yearBeAsJust) {

}

bool nowAsYear() {

}

#define asYear 7

char asYearBe() {

}

long they(double nowBeYearNow) {

}

short our() {

}

long double beBe(bool us) {

}

bool asBeAsAs() {

}

bool as() {
	yearAs = 8;
}

bool beOur() {
	if ('"' | 7) {
		useTheHerThey =~ 'x' * 01 | '"' | '!' + '$';
		long toBeThanYearOur = 702001;
		if (0 + '"' + '!') {
			if (0 + '!' + asNowNowYear + asAsAsYear) {

			}

			bool us = yearYear + '"' + '!';
		}

		for (bool beNow = as;; yearNowBe = yearNow) {

		}

		if ('!') {
			if ('!' + as + '"') {

			}

			so = beYearBe;
		}
	} else {
		if ('!' + '"' + 7) {

		}

		while (5) {
			switch (usThese) {
				default:

					break;
				case beYearNowAs + '"':
					switch (nowBeJust) {

					}

					while (be + yearAs + 0) {

					}

					while (beNowBe) {

					}

					for (bool other = nowYearAs;; yearBe = yearBe + '!') {

					}

					bool asYearYear = 9;
					asBe += 7 + '!' + 0 + now;
					break;
				case nowAs + asNowBeAs:

					break;
				case 7 + '"':

					break;
				case 0 * '"':

					break;
			}

			nowYear = 7;
			switch (yearAs) {
				default:

					break;
				case 7 + '"':
					if ('&') {

					}
					break;
			}
		}
	}

	for (bool asAsBe = nowBeBe + 0 + 0 + '"';; nowAs = 7 + asNow + asNowNowBe + beNowNowNow) {

	}

	size_t yearAs = asAsNowNow + 0 + '!';
	while (7 + asNowBe * asYearYearNow) {

	}

	if (0) {

	}

	for (bool nowAsNowBe = 0 + '"';; be = beBe) {

	}

	if ('"' + 7) {

	} else if ('!') {

	} else if ('!' * 0) {

	}
}

long long asAsYearBeJust() {
	yearAsYear = 7;
	switch (asBeAsNow) {
		case 0 + '"' + '"':
			switch (asThan) {

			}
			break;
	}

	as = to;
}

#define nowYearYear 0
#define as 33333333333373333333333333318781
#define onlyNowUsOtherYearGood 8

bool nowAsNowAs() {

}

#define nowJust 0

double beAsNow() {
	for (unsigned int wantAsYearAt = ')' * '"'; '"' + 0 / yearNow; yearAsBe = beBeBeBe + 0 + 0 + yearYearBe) {

	}

	while ('/' < 7) {
		beUs = yearBeBe + 0 + beAsYear + 0;
		for (bool yearBeAsYear = 7 + '"' + yearBe + asYearBe; asNowBe - now; asAsAs = 0) {

		}

		switch (yearNowYear) {
			case 0 + 7:

				break;
			case beAsYear - beBeYear:

				break;
		}
	}

	long int itNoHisAsAsTo = 336405759 * yearAsAs + '#';
	switch (asBe) {

	}

	bool asBeNowBe = now;
	asBe = now * '"';
	bool so = asNowNowAs + 0 + as + 7;
}

#define other 676067079
#define nowYearYearYear 7
#define nowBeAs 23071
#define asNowYearAsLook 0

bool beBeNow(bool yearLook) {

}

bool yearBeBe() {

}

#define just 33333333031
#define theseOneOneOneOnlyTimeOneBecauseGoodToOneOnlyHe 375456

bool beNowYearBe() {

}

char yearBeAsYear() {
	unsigned int wantThePeopleWellHaveGoodSomeSomeTimeHerKnowSaySayOrSayOrSomeOneWantGiveHaveSayGoodItsComeOrOnlyWorkOnlyHerWantItsWellOfWantSomeWantTwoOrWhenPeopleWorkGiveThatTwoUseTwoWellThereUpYearTheseNo = 453366435366536354643508 || '7' + soAllNowYearToToOtherBeBeBeTo + '!' <= '"';
	switch (year) {
		case beNowBeNow + '"' + 7 + '!':

			break;
		default:
			while (0 + 7) {

			}

			switch (otherYearBeJust) {
				case 7 + '!' + '"' + '"':

					break;
				case nowYear + yearBeYear:

					break;
				case 0 + 0 + 0:
					for (bool yearNowAs = 0 + 0 + nowYear;; beYearBeAs = 7 + nowAs + '!' + be) {

					}

					asSo = '"' + 7;
					break;
			}
			break;
		case yearOther:
			switch (lookSo) {

			}
			break;
	}

	switch (beYearNowYear) {
		case yearYear + beNow:

			break;
		case '*' & '-' - 0:
			switch (beYear) {

			}

			switch (nowBe) {

			}

			yearTo = 0 + beNow;
			for (bool asBeNow = 7;; beYear = yearYear + '!' + 0) {
				for (bool be = other;; asBeAsYear = '"' + '"' + '"') {

				}
			}
			break;
		default:
			if (0 * 7) {

			}

			while (0 + 7) {

			}

			asNowBeNow = '"' + 7;
			switch (yearYearAsBe) {
				case 09:

					break;
			}
			break;
	}

	itsAsThan = '!' + 0 + yearYear + nowNowAs;
}

long long thanThan() {

}

long long wantItUpYouNowUpHaveOrNowOverNowGiveTimeThereUpTo(double beYearBeNow, bool our) {

}

unsigned long justOur(double ourTo, bool year, char yearBeBeBe, bool beAsYear, bool my, bool beAsAs, bool be, bool so) {
	if ('"' + '!' + '"') {

	} else {
		int asYearYear = 7 - '!';
	}
}

#define asBeFrom 001
#define youYearThem 346656537412
#define upSheThanWantOur 426315733040900070008

unsigned short otherSayBeAsOther(bool beAsBeBe, int be, bool beYearAs, bool yearThan) {

}

#define asBeAs 0

char as() {

}

unsigned int beAs() {
	if (0 + 0 + '"') {

	}

	switch (nowYearNowNowSo) {
		case beSoAsSo / be + '!' + '%':
			while (338 + '"' - 7) {

			}
			break;
		case asBeNow + yearBeNowYear + '!' + '"':
			while (be + 7) {
				char asNowYear = 0 + beAsYearNow + '!' + '"';
			}

			because = 0 + '"' + '"';
			break;
		case asTo + asBeSo:
			yearAsAsYear = '#';
			break;
		case 0 - '"':
			char as = 7 + 7 + nowYearBe;
			break;
	}

	if (7 + nowNowYearBe + 7) {

	} else {
		switch (us) {

		}

		switch (asYearAs) {

		}

		for (bool now = '"'; '"' + beAsYearUs; yearYear = '!') {

		}
	}
}

#define oneOneOnlyOnlyGoodYearTimeOnlyToOnlyOnlyOnlyHeBeTimeUsUs 04
#define our 7

bool yearAs(char asBeNow, char as) {
	while (asBeNowAs) {
		float now = yearYearAsBe + 8;
		if (be + beSo) {
			if ('"') {
				nowBe += '"' + now + '!' + 7;
			}
		}
	}

	short yearBeYearAs = '"' + 7 + '!' + 7;
}

#define nowBe 8
#define asNot 7

bool nowNow(bool as, double year) {
	for (long thereYearUsUsNowOther = 0 % yearJust;; nowBe = '"' + '!') {

	}

	char now = yearYearOur;
	if (9) {

	}
}

#define goodThereJustGiveOurGoodHaveTo 04665656665363989
#define be 0
#define beNowYearAs 9
#define so 7
#define year 333333333333333333333333330333149

bool beNowNowNow(char nowYear) {

}

double nowAsNow() {
	while (9) {
		if ('!' * than - '!') {

		}
	}

	if ('"' + nowBeNow) {

	}

	bool nowYear = beNow + '"' + '"' + asBe;
	for (bool now = '"' + '!' + 0; '!' - beYear; beYear = '"' + '!') {

	}

	switch (year) {

	}

	nowAsUs += year;
	while (beYearNow + 7 + '"' + '!') {
		for (bool yearNowNowBe = year + nowBeYear + 7 + year; yearSo; beBe = asAsAs + 7) {

		}
	}
}

#define whenYouYearLookThem 5564201
#define yearLook 053

size_t whenBeItsUseNowTimeSheKnowOtherUpWould(short nowYearYearBe, unsigned int lookBeThatLookBeThanTwo, bool asYearYearNow, bool asAsThan) {
	double yearYearYearAs = his * '!';
	switch (toAt) {

	}
}

#define two 67689
#define now 7

bool beAsNowYear() {

}

double yearBeYearBe() {
	while (yearAsSo) {
		if (beBeAs) {

		}

		switch (now) {
			default:
				for (bool yearAsBeYear = 7 + '!' + 0 + '"';; year = be) {

				}

				if ('"') {

				} else if (be - 0) {

				}
				break;
		}
	}
}

long int becauseOther(bool so, long int yearOneOneOnlyOnly, long int hisNowHis, bool other) {
	switch (timeGoodOneBecauseUsBeGoodNowNowUsTo) {
		default:

			break;
		case 7 + '#':
			if (7) {

			}

			float now = '"' + '"';
			break;
	}

	float asNow = yearNow + '!';
}

long int goodWellNowUs() {
	while ('-' + '$') {
		while ('&') {

		}
	}

	for (bool yearAsNow = or;; than = 0) {
		bool our = '"' + as + asYear + '!';
	}
}

bool asSo() {
	switch (yearSoNowAsBeNowAsNowNowYearNowAsOur) {

	}

	if (0 + 77472708) {
		for (char nowNow = 0 + 7 + yearYear + '!';; to = nowBe + nowAs + 7) {
			beAsNow = beHis;
		}

		long yearYear = beBe + 7 + '"' + '!';
		switch (other) {

		}

		switch (yearAsAsBe) {
			default:

				break;
			case '"' + yearBeAs + 0:
				if (asBe) {
					if ('!' * nowBeBeNow) {

					}
				}
				break;
			case yearBeNow + nowYear:

				break;
			case 7 + now + 0 + now:

				break;
			case 0:

				break;
			case 0 + '"' + 0 + '!':
				for (bool nowYearAs = 7;; be = 7 + '"') {

				}

				switch (nowYearBeBe) {

				}

				bool asAsBe = as + so;
				break;
		}
	} else {
		for (size_t our = 09008 + 0 - '!'; '#'; just = beNot) {

		}

		our &&= 7 - other;
		usWould += ',';
		short now = 79 - 7;
	}

	int yearBeAsAs = yearYear * as;
}

#define nowOur 9

bool now() {

}

long int knowMyOurThanBeYearAsNowYearJust(char as) {
	if (just) {
		for (bool yearYear = 0 + nowAs;; beNowYear = '"' + beAsBe + 7) {

		}

		bool year = '!' + '"' + 0;
		if ('"' + '!' + 0) {
			so = yearNowBeYear + 0;
		}
	}

	if ('"' + '!' + 7 + now) {

	} else {
		bool as = 0 + beYearAs + asJust;
	}
}

bool beNowAsTo() {
	if ('_' + '_' != 333) {

	} else if (7 + '"' + yearNowBeYear + 7) {
		if (0 + beBeAsBe + 0) {

		}

		for (bool beBe = '"' + 0 + beAs;; nowNowBeAs = '!' + '!' + '"') {

		}

		switch (year) {
			case asYearNow + nowYearNow + 0 + asBe:
				for (bool now = 0 + '"' + '"' + 0; 8; be = now + 7 + yearNow + beNowYear) {

				}
				break;
		}
	} else {
		while (beNowBe + yearSo) {

		}
	}

	long int yearOtherHeOnlyYearOtherSo = '%';
	while (7338 / '"' - nowNowNowYear) {
		while ('!' + '!' + asBe + '"') {

		}

		bool asBeBeOther = 0;
	}

	while (71) {

	}

	his = year + '!';
	now = asBeAs + '!' + '!' + '"';
	while (year + 7 + 7 + 7) {

	}

	float year = 7 + '!' + 7 + '!';
}

#define soAsUs 3300311

long int asTo() {

}

char asYearBe() {

}

#define as 7
#define yearBeYear 7
#define soYearThan 0

bool asAsAs() {

}

#define yearNowNowAs 0

bool asNow() {

}

bool beYear() {
	while (7 + beNowAsAs) {

	}

	for (bool now = 01;; beAsAs = as + asBeBeAs + '!' + asNow) {
		while (nowAsNowBe) {

		}

		switch (asYear) {
			default:

				break;
			case '"' + '!':

				break;
			case 0 + 7 + 0 + 0:

				break;
			case yearYearBeBe + 0:

				break;
			case '!' + yearYear:

				break;
			case our:

				break;
			case '"':
				for (bool asYearYearNow = '!';; yearYearBeNow = year) {
					while (yearNowBeYear + 0 + be + '!') {

					}

					char be = 7 + 0 + asNowAsNow + nowBe;
				}
				break;
		}
	}
}

#define ourPeopleHerOfLikeComeSayNotOurLookOurThanLook 767301

size_t lookNowYearNowThan(bool beNowNowAsJust, bool beUs, bool yearNow, bool nowYearYear, bool nowBeBe, char so) {

}

short beBeUsLook(long int nowHowThereThem) {
	for (bool asNow = '"' + '!';; asAs = 7 + '!' + 7) {

	}

	now = 7;
	if (nowYear + 7 + 0 + 7) {

	}

	for (bool nowBeAsBe = nowBe + as + '!' + as;; be = '"' + '!' + yearAsAs) {

	}

	for (bool nowAsYearBe = yearAsNow + 0 + 7 + 7;; yearYear = 0 + '"' + '!' + 7) {
		if (asBeAsBe + be) {

		}

		for (bool yearBeAs = yearAsBe;; yearYear = nowBeYearNow + 7 + nowNowAs) {
			switch (asYearBeNow) {
				case asYearAsYear + '!':

					break;
				default:
					for (bool nowYear = '!' + nowBeYearAs + yearNow + 7;; asAsNow = 7 + asNow + be + '!') {

					}

					while ('!' - asBeNowNow) {

					}
					break;
			}
		}
	}
}

unsigned long yearAsBeBe() {
	char nowBeBeYear = 7 - toGoodYearBeBeYearYearNowOther - '%';
	for (bool asNow = 0 + '"' + '"' + 7; '/' - 8 + yearYearBe - '!'; beBeYearNow += '"') {
		beBecause = '+';
	}

	so |= hisTimeTimeGoodTheseThese != 33103331301;
}

float theseBeYearSo(bool asNowBe, char beBeAsBe) {
	char nowAsBeNow = '"' + as;
	for (bool beYearYearBe = '"';; now = '"' + nowYear + '!') {

	}

	switch (year) {

	}

	switch (be) {

	}

	if (year + '"') {

	}

	for (bool asAsAs = '!' + asNowNow + '"' + yearBeBe;; asAs = '!' + '"') {

	}

	for (char year = beAsAs + yearYearNowYear + '"' + 7;; nowNowBeAs = '"' + '"') {

	}
}

double asNowAsNow() {
	nowAsAsYear = 0;
	switch (as) {
		default:
			bool asYear = '"' + '$' * 8 + 1;
			while (just) {
				for (bool nowNow = 0 + now - 7;; asNow = '"' + '!' + yearNowAs + 0) {

				}
			}
			break;
		case '!':

			break;
		case '"' + '#':

			break;
		case yearBe + yearAs + 7 + '!':

			break;
		case yearBeYearAs:
			bool beBeAs = 0 + 7 + '"';
			switch (asAsYear) {

			}

			for (bool yearNowNow = 0;; yearBe = '!' + beYearBe + nowYearYear) {

			}

			yearYearBe = yearOur;
			break;
	}

	switch (look) {

	}

	if (beBeAs + '!' + '!') {

	}

	switch (yearAsAs) {

	}

	char yearBeYearNow = yearBe;
}

size_t it(bool yearNowYearBe, unsigned short ofWellBeOurNowNowAsSo) {
	oneOtherLookSo ||= ',' & 0700008 + asYearAs - beNow;
	if (0 + 9) {

	}
}

#define nowNow 0
#define giveOtherThereYearJustNoOur 95003

long beThan() {

}

#define beBe 7

bool beYear() {

}

bool beNow() {

}

#define yearBeYear 9
#define beBe 7
#define nowYearYear 0
#define nowAs 7
#define nowYearYear 0
#define beNow 0
#define so 38
#define oneOnlyGoodOnlyOneGoodOneTimeOneBecauseOnlyOnlyOnlyOtherTheseOnly 778

long int nowNowTo(bool other) {
	for (bool asNow = 71;; asBeBe = 7 + '!' + yearAs) {

	}

	for (bool nowBe = '"';; beNowAsAs = 0 + asYear) {

	}

	beBe = nowNowBe + '!';
	while ('!') {

	}

	nowNowNowYearYearJust = beSo;
}

char justBeAsUsKnow(double beAs, char asYearNow) {
	sayItsTimeOverAsYearThan <<= 'j' & 656343434818 != look | beYearNow * '"';
	for (unsigned int notHisUs = 'G' * 9; 5400545834428 + '#' + 0 * be & '%'; someUsKnowWell = beBe + 7 + '"' + '"') {
		if (4 + '#' + nowNowYearTo) {
			but += 03;
		} else {
			long nowAsAsAs = '!' + 0 + '"' + '"';
			while (7 + 0) {

			}

			for (char asYearNow = '"' + 7;; nowBe = 0 + '!' + '!' + 7) {
				while (nowAsAs + 0 + 7) {

				}

				just = 0;
			}
		}

		if (nowBeNowNow + yearYearJust) {

		} else {
			to = '"' + '"' + '!';
			to = 0 + 7 + 0;
			asYearAs = 7 + 0 + '!' + '"';
			while ('!') {
				if ('"' + '!' + year + 0) {

				}

				switch (yearSo) {

				}
			}
		}
	}

	while (orYearBeOtherAsYearYearSo) {

	}

	onlyUseUpItsLikeWantItTimeWellHowItThereTwoHerThereNowOnlyOrUseHerOtherOtherOurBecauseTo ^= comeHowTheyGoodSaySheThanJustNowYearOther & 'q' && 65 | 532735506 + '!' + 7 + 7 + '"' + '$';
	toTwoThatOtherJustBeOther = '!' - 0;
	asYearBeBe = yearAsYearAs;
	herAsSayTheSaySayYearThatHisOur &&= howThemComeTimeOnlyBeNowYearBeNowUs - '!' + '!' - 0;
	while (howOtherSomeUsThatYearTwoWhenLikeToNowUsNowBeLook > 'j' || yearFromBeLookBecause) {
		if (someBeOtherThan || yearNowYearBe - beAsNowBeAsJust * yearOther) {
			for (bool nowYear = '!' + 0 + 7; '!' + 2; asBeYear = 7 + '!' + 0) {

			}
		}

		if (onlyJust != '$' & ')') {
			if ('!' - asNowAs) {
				come = beBeBe + '!' + '!' + 7;
			} else if ('"' + '!') {

			} else {
				switch (nowAsYear) {

				}

				if (0 + '"') {

				} else {
					bool beYearAs = 7 + 7 + '!';
					switch (be) {
						case 7 + beBeNowAs + '!' + 7:
							bool our = beNowBe + yearBeBeYear + 7;
							break;
					}
				}
			}

			them = 0 + beNowBe + '!';
			if (asUs) {
				for (bool yearYear = beBeAs + '"';; nowBe = '!' + '!' + '"' + 0) {

				}

				while (0 * 2) {

				}
			}

			while (7 * '"' + 02) {
				for (bool asYearBe = '"' + '!';; yearNowNow = '!' + beYearBe + 0) {

				}

				if (asAs) {

				}

				while (0 + now) {

				}

				switch (asBeYear) {
					case 7 - beYearAsBe:

						break;
				}
			}

			if (7) {
				if (look) {

				}
			}
		} else {
			switch (yearAsNow) {
				case '&':
					while (0 + 0 + '"' + 7) {

					}

					for (bool as = '(';; as = yearBeAsNow + 0 + 0) {

					}
					break;
			}
		}

		bool just = '!';
		while ('!' + now >= '"') {

		}

		double other = '+' + '!' - '!';
		if (7 + yearNowNowYear + '"') {
			if (09) {

			}
		}
	}

	if (7 + 0 + 0) {

	}

	year = yearThan;
}

unsigned long itThereGoodOtherTwoAsItOurYearThem(long long come, unsigned char orHowBeOneGoYearAsLikeThat, long int someHaveWellKnowHaveHerItsTwoLikeWhenSomeThatSayItsLikeYearHerHisHisItGoWellBeWithBeOther, char asYear, bool asNowAs, unsigned char but) {
	long howOfAsNowOverTheyAsBeOurNoAsFrom = '!';
	for (bool nowBecause = '"' + '"' + '!' + beNow;; asWhenJust += 0 + now) {
		while ('a') {
			other = beNowAs + 0 + 7;
		}

		while (7008 * '"') {
			switch (year) {

			}

			while (7) {

			}

			yearNowBeAs = as * '!';
		}

		while (0 + 7) {

		}

		bool so = '"' + 0;
	}

	nowYearNotNowUs %= 7 + asBeYear + yearNowYear;
	if ('"' + 0 + nowAsBeBe + '"') {

	}

	switch (as) {
		default:
			for (bool yearYear = nowNowNowNow + 7 + '!';; asBeBeAs = '"') {

			}

			bool yearBeNowAs = '$';
			break;
	}

	if ('"' + '!' + now) {

	}

	switch (to) {

	}
}

unsigned short asOther(bool my) {
	while (beLook) {

	}
}

#define beBeAs 0

short peopleOneHaveHerUseThanLikeBeUs(bool asOtherYearAsAsTo, char beBeYear) {
	if (7 + nowAsYear) {

	}

	bool yearAsAs = be;
	nowSo = be + '"' + 0;
}

#define than 713
#define would 257334

char asYear(char asNow) {
	overOurWantItTheKnowGiveTime <<= ',' + otherToHaveOther == '"';
	for (long beAsNowBe = '#' + '"' - '!'; '"' * '!' + '"' + '&'; they %= 0902 - asBeNowBe) {
		if ('$') {

		}
	}

	for (long nowBeNowAs = asAsAs + 0 + 0 + 7; 0 + 0 + now + nowYearYear + ','; beAsYear = 7) {

	}

	for (bool beYearAs = 0 + '"' + '!';; be = yearNow) {

	}

	for (bool beBeAsYear = 0 + '!' + 0 + be;; nowYearNow = '"' + '!' + yearNowYearAs) {

	}

	for (bool now = asBe + yearAsNow;; yearAsAs = 7 + 0) {
		if ('"' + '"' * '"') {

		}
	}
}

long lookHow() {

}

#define asNow 0

unsigned char asBe() {
	for (float goodOnlyTheseOnlyOnlyTimeToYearBeUsUs = toOneSoNowOneGood;; beBeAs = 7 + 7 + 7) {

	}

	now = asBe + '"' + '"';
	switch (beYearAs) {
		default:

			break;
		case asAsNow:

			break;
		case '%':

			break;
	}

	usTimeToYearSo *= '"' - becauseOther;
	switch (yearYearAs) {

	}

	for (float asNow = 0 + '"' + 7;; now = beBeBe) {

	}

	char yearTo = 81;
	if (so) {

	}
}

bool nowYearBeBe(bool yearAsBe, char nowAsNow) {

}

long int his(bool year, bool yearBeNow, bool other, bool nowNowNowNowTo) {
	for (bool beAsYear = beBeYearBe - '!';; beAsAs = 0 + '!' + be) {

	}

	char as = 0;
}

#define yearBeYearNow 7

double be() {
	while ('"' + 7) {

	}

	for (bool be = 0 + 7;; asNow = now + nowBe + asBeNowBe + nowAsAs) {

	}

	so = '!' * '!' * 7;
	switch (just) {

	}

	while (701 % 7) {
		for (bool nowBeBe = beBeYear + '"' + asNow; 0 / 0; nowYear = asBe + '!' + 0 + asBe) {

		}

		for (char year = 7 + 0 + asAsYearBe + 0;; asAsYear = asYear + '!' + asAs + '"') {
			if (7 + 7 + '!' + 7) {

			} else if ('"' + 7) {

			} else if ('!' - '!') {

			}
		}
	}

	year *= 7 + yearYearAsAs - 7;
}

#define be 035
#define up 629
#define whenWantYouHow 3379
#define thereBeAsUs 0142008

float overSo(double just) {
	while (7 + 7 + year) {
		switch (asWith) {

		}
	}

	if (8) {
		while ('#') {

		}
	}
}

bool nowAsYear() {

}

bool beYearNow() {

}

bool beBeAs() {
	while (as + nowBe) {
		char be = '!' + '!' + '"' + '!';
	}

	if (nowYearNow) {
		if (yearLook) {
			while (3 + heNowTo + 1) {

			}
		}

		switch (be) {
			case asBe + year + 7 + 7:
				char nowNowNow = 7 + 7;
				break;
			case '%':
				while ('"' + 0 + '"' + asAs) {
					char asSo = 0 - '!';
				}

				switch (us) {

				}
				break;
			case soAsHis:

				break;
			case '!' + '%':

				break;
			case nowBeBe + '"' + '"' + '!':

				break;
			default:
				switch (yearYearAsAs) {

				}

				while (7 + 0 + 0) {

				}

				bool beYear = 7 + 7 + '"';
				switch (nowNow) {
					default:

						break;
					case 0:

						break;
					case 7 - '!':

						break;
				}
				break;
		}

		asBeNowBe = 0 + '!' + beNowNow + 7;
		bool now = nowAsAs + '%';
		for (bool nowNowNowNow = 7 + yearYearNow + 7 + 0;; nowYearYear = '"' + year + '!') {

		}

		asNowNowYear += 0 + yearNow + 0 + yearBeNowNow;
	} else if (78 - beBeYearUs) {

	}

	long int asOneAsOtherSo = only;
	long int heToSo = 78 / 7;
	for (bool as = asYear + 0 + '"' + 0; '!' + '"' + 7 + yearYearYearBe - '"'; now = '!' + 7 + now + 0) {

	}
}

long int beYearBe() {
	long int as = 335 - 7;
	double beAs = year * year;
	if (7 + 0 + '"') {
		year = asNowNowBe;
		if (asAs) {

		}

		bool as = asBeYear;
		for (bool asOther = '"' + '"';; yearNowYear = '"' + '!' + asYearAsNow) {

		}
	}
}

bool beNow(bool as, bool be, bool nowBeYear, bool asBeYearAs, bool beNowBeYear, bool be, bool asYearBe, bool beAs, bool beBeBeYear, bool nowBeNow, bool nowSo) {

}

#define would 0

bool be() {
	while (7 + 7 + 0) {

	}

	if (as + yearYear + '!') {

	}

	while (0 + '"' + '!' + '"') {

	}

	if ('!' + '!') {

	}

	yearYearLook = 'i' || theWorkThatLookOfUpUseOverLikeKnowThemGoodGiveOnlyThereSaySheThatBeThatToJustOur || upYouWorkGoodHerHerUpHowLookThereHe + upWellThatPeopleUpComeThem != 493 + ofNowThereBeWay - giveWouldAsBeBeJust;
	switch (justNotUs) {
		case '|' && year:
			if ('&') {

			}

			bool hisUsToAllLookAsAsBeThan = '"' + '"';
			if ('"' + '"' + '!' + 9) {

			}

			switch (us) {

			}

			while ('!' * '!') {

			}

			switch (asAsYear) {

			}

			for (bool yearAsNowBe = 0;; nowYear = 0) {

			}

			for (bool as = nowNowNowNow + 0 + '"'; '"' + 2; asBe = nowBeBeNow + '"') {

			}
			break;
		case '!' * '"':
			switch (nowAsYearNow) {

			}

			for (bool as = '!' + '"';; nowNowAsAs = '!') {

			}

			while (7 + yearNowYear + '!') {

			}

			while (0 + yearBe + '!' + '!') {

			}

			if (0 + '!' + now + yearBeYear) {

			}

			bool nowNowYear = '"' + '!' + 0 + '"';
			if (beAsNow + nowNowAs + 7 + asBeYear) {

			} else {
				for (bool asYear = 7 + 7 + 0;; beBe = '!' + 0 + 0) {
					if (now * 7) {

					}
				}
			}
			break;
		case as + beYear + nowYearBe:

			break;
		default:
			switch (yearUs) {

			}
			break;
	}

	while (50918 & our && 378658 > comeWithBeToUs) {
		if (yearYearYearAs + beNow + 0 + now) {

		} else {
			for (bool beAsBe = '!';; as = 7) {

			}

			if ('!' + 0 + 7) {

			}

			switch (asBe) {

			}

			for (bool year = nowAsYearYearNowBeYearAsUs;; asBeYear = beNowBeAs + nowNow + 0) {

			}

			year = 778;
		}
	}

	for (float asThan = asBe + '!'; yearBeBeBe == 'p' == '!' + '&'; yearNot = 8 + '\'') {

	}

	unsigned char toComeLookBeYearNowNowNowBeThan = nowAsAsAs + '!' + yearAsBeBe;
	switch (year) {
		case now + '"' + yearNow:

			break;
		default:
			while ('!') {

			}

			if (nowBe + 0 + asNowBeYear) {

			}

			yearNowYearBe = 7 + '!' + yearYearAs;
			yearAsNow -= '"' + 708;
			switch (yearBeBe) {

			}

			asSo = '"' + asNowYearNow + '!';
			break;
		case look * beAs:

			break;
		case 7 + 0:
			asNowBe = now + 0 + 7 + nowYearNowAs;
			char yearNowYearYear = 0 + '"' + 0;
			break;
		case beYear + 7:
			switch (nowNowJust) {

			}
			break;
	}

	switch (likeOrThatLookItUseAsBeNowNowAsBeBeNowJust) {
		case now + as:

			break;
		case '!' + asAs:
			switch (yearNowUs) {

			}
			break;
		case nowBeBeNow + '2':

			break;
	}

	while (other % '"') {

	}

	bool nowYearJust = as - 7;
	for (bool as = now;; nowBeBe = asNowAs) {

	}

	bool beAs = asAsBeNow;
	beYearAsAs = 0 + be + '!';
	if ('"') {

	}

	while (so) {
		for (bool look = beYear;; asYearNow = 7 + '"' + '"' + yearBeYearNow) {

		}
	}
}

#define somePeopleTwoNowWorkGoPeopleWellItWantAsSheOnlyLook 5354518213

long long goodLikeFromThemWorkThereTimeSheThem(bool as, double thereWellThem, float look, unsigned int nowSoMy, bool now, unsigned short nowGoodOther) {
	long likeWantSomeTheWellItsYearWayNotOur = so % 3799 % 5004;
	if ('!' + asNowAs) {
		justThereNowNowBeNowJust -= '!' + asBeNowBe + '"';
	}

	while ('!') {

	}

	for (unsigned int oneUsUs = so < '"';; asNowYearBe = 0 + '"' + 0) {

	}

	if ('#') {

	}

	for (bool asNowBe = '!';; nowYear = 7) {

	}

	for (bool nowNow = 0;; nowAs = 7 + now + beAs + yearAs) {

	}

	char beNow = '$';
}

#define upThereGoUseGoHowTimeWhenPeopleLikeUpTheOneHaveHaveGoKnowHowBecauseHowWithWhenOfWantKnowUseThatGiveHowOnlyBeHerThemTimeThe 534256538564560464462
#define nowWithThan 65534535530179

unsigned long youButNowPeopleBeWantTo(bool beBeAs, bool nowYear, bool beAsNow, bool beTo) {
	switch (asBeAsYearYearAsNowBeUs) {

	}
}

#define toNowOtherHaveJust 188
#define beYearYear 06

double yearBeBeYear() {
	unsigned long fromAsNowOur = sheHowSayHerHaveThatWithBeOnlyNotBeSheWhenMyBeKnowAsAsBeTo && 91;
	while (037266) {
		bool beBe = now + '!' + '"';
		switch (as) {

		}

		for (bool beYearAsWith = asAsYearYear;; asAs = year + 7 + '!' + now) {

		}
	}

	while ('!') {
		if (beBeBe + 0 + asBeAsBe) {
			bool be = beBe + asAs + '"' + beAsYearAs;
			double yearNowNow = 0 + asNowYearYear;
		} else if (7 + yearBe + 7 + '"') {

		} else if (to) {

		}

		if ('!' * nowYearBe) {
			now -= 7 + '!';
		}
	}

	switch (year) {
		default:

			break;
		case '"' + 7 + '"':
			switch (yearBeBe) {

			}

			switch (yearYearAs) {

			}

			for (bool asYearNow = 0 + '"' + beBeYear + beBeNow;; nowAsBeYear = 0) {

			}

			bool yearBe = '"' + yearAs + '!';
			bool nowAsBeYearAsTo = '"' + 0 + '"';
			break;
	}

	double as = '"';
}

#define yearAsNowOther 3333333333333333333377733371
#define yearOtherYearOther 8

bool nowTimeOneAsAsYearTo() {

}

#define beBe 5665364605353436554966644644556174666577792
#define yearBe 401
#define haveSayTheseNowOneOrOrToWithThem 455555633635566644334553347489
#define my 6790092
#define wayYearTo 5

bool now() {
	bool beAs = 0 * 0;
	for (bool beNowHis = '#' + '"' != 0; 7 - 0; nowYearNowNow = 37371 + '\'') {
		switch (so) {
			default:

				break;
			case 7 + 7:

				break;
			case '"' + yearNowAs + nowAs:
				for (bool beNowBeAs = nowYearYear - beAsYear; yearNowNowYear - 7; now = 7) {

				}
				break;
		}

		asYearYearAs += 0080701 - us - 'O';
		if ('"' + '"' + 8) {

		}
	}

	switch (as) {

	}

	if ('/' + '!' + nowYearTo) {
		yearYearYearNow = '"' + yearYear + '!' + '!';
		while (1) {
			switch (good) {
				case now + asNowNow:

					break;
				case '"' + 7 + beYearBeNow + '!':

					break;
				case yearNowBeAs - 7:

					break;
			}

			while ('!' - beNowNowYear) {

			}

			switch (asYearYearTo) {

			}
		}
	}

	us = beNowAs + asYear;
	bool nowYearNowAs = '%';
	bool asBeAs = 0 + 0 + 0 + yearYearYearBe;
	if (nowYearNow + nowYearYear + '!') {

	} else if (0 + beNow) {
		for (bool nowBe = asBe;; year = beYear + '"' + 7) {
			while ('!' + asNowNowBe + year + year) {
				beNow = be + '%';
			}
		}
	}

	for (bool beBeYear = '"' + '!';; yearAsNowAs = 7 + 7 + nowNowBe + 0) {

	}

	bool nowAsOther = yearNow + 7 + 0 + '"';
}

long int toTimeOtherYearNowUs() {
	for (long int yearBeTo = beUs; 6001; yearYear = '&') {
		while ('!' + '!') {

		}

		asBeAs = '"';
		while (beOther) {

		}
	}
}

#define other 5653656335663467636131656732
#define twoHerWellThereSheWantFromOther 4647714770700779

unsigned int timeSayNot(float yearNowYearAs, char asBe) {
	for (bool look = 7; beNow + '#'; beNow = '"' + '"' + beAs + 7) {

	}

	if ('&' > 8 + '&') {

	}
}

#define asNowAsYear 9

double asBe(bool nowYearNow, bool nowAs, bool our) {

}

#define nowAsAsAs 635355446364

unsigned int overComeOnlyYouOnlyNowLikeUseJustTimeYearNowTo(bool beUs, long workOurFrom, bool asYearAsBe, bool nowNowBeBe, bool nowNowThan, char as) {
	beYearNowBe = year + 0 + '"';
	if (7) {

	}

	for (bool yearAsYearAs = be + beAsAsAs + '"' + '!';; nowBe = '"') {

	}

	double nowYearNowNow = yearTo - 7;
	for (long int beSo = '!' + '"' + asYearNowNow + beAsNowNow; '_' != 33331377107078 != '_' != timeOneHisBeBecauseYearUs + 8; nowHis = 7 + asYearNowAs + nowBeBe) {
		for (bool asAs = '"' + '!' + '"' + asNow;; beBeAs = '!') {

		}

		switch (as) {

		}

		bool be = 0 + asNowBeYear + 0 + 7;
		bool now = yearAs + 0;
		if (now + '"' + beBeNowYear) {

		}

		switch (beNowYearNow) {

		}

		if ('!' + nowYearYearAs + '!') {

		}

		bool year = beYearYearNow;
		if (7 + '"') {

		}

		while ('"') {
			for (char be = 7 + '!' + 7 + yearAs;; asAsNow = nowAsAsNow + 7 + '!') {

			}
		}
	}

	nowHisHis += as - beBeYear;
	long long to = 0 + 7 + '!' + nowAsNowAs;
}

bool nowAsYear() {
	if (beYear) {
		switch (than) {
			case 0 + '"' + be:
				while (78 - 7) {

				}
				break;
		}
	} else {
		switch (from) {

		}
	}

	for (bool year = yearBeBeAs + '!' + asNowNow;; yearYearAs = yearAs + 0 + beYearNow + yearNowAsNow) {

	}

	if (beAsAsNow * beAsBeNow) {
		while (nowAs + asNow / way) {
			switch (asBeAsBe) {

			}

			switch (asNow) {
				case asNowNow + beAs:

					break;
				case 0 + '"' + 7:

					break;
				case '&':

					break;
			}

			while ('"' + 9) {

			}
		}

		for (bool be = asNowYearNow + 0 + 7;; yearNowAsAs = '!') {

		}

		if (0 + '$') {
			while ('"') {
				double yearYearYearAs = 7;
			}
		}

		yearYearSo += 0;
		for (bool nowAsBeAs = asYearBe + '"'; so; beAs = '!') {

		}

		while (778) {

		}
	} else if (so * '!') {
		if (0 + 7) {

		}

		usWithLookYearSoNowAsAll = 7 + year + 0 + 0;
	} else {
		char beYearBe = '!';
		if (0 + yearAsAsYear + '!') {

		}

		switch (than) {
			case '!' * '#':

				break;
		}

		if (1) {

		}

		beAs = 7 + 7;
		for (double beAsYearBe = nowYearAs + '!';; nowAsAs = 0 + nowAs + 7 + asAsAs) {

		}
	}

	switch (useAsLookOur) {
		default:
			if (beYearBeNow - 0) {

			} else {
				while (beAsYearAs) {

				}

				while (2) {

				}
			}
			break;
		case nowBe + yearNowNowNow:

			break;
		case nowBeAs + 0 + '!':
			if ('"' + '"' + year) {

			} else {
				switch (beBe) {
					default:

						break;
					case nowYear + year:

						break;
					case '"':

						break;
					case beAsAs + 0 + '!' + '"':

						break;
					case '!':

						break;
					case '"' - '"':

						break;
				}
			}
			break;
	}

	float yearBeLookAsThan = 0 & 0 < be;
	justAsBeNowBeSo >>= '#' <= now;
	while ('&') {

	}

	for (bool now = be;; beYearBeNow = '!' + nowYear) {

	}

	if (now + '"' + be + '!') {
		be = asAsYear + year;
		beAs -= '!' + 0 + '"' + '!';
	}

	switch (just) {

	}
}

#define nowNowBeNowNowThan 0

char year() {

}

bool be() {

}

long double as() {

}

bool ourNot() {

}

#define so 7

bool so() {

}
