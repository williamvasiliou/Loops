long int so() {

}

long int beOnlyOneTimeGoodYearTimeTimeGoodGoodOneOneToHeOnlyYearBeTo() {
	if (year) {
		for (bool be = '"'; 01771 + '"' / '"'; asAsBe = 7 + yearNow) {

		}

		if ('"' + 0 / 7) {

		}
	} else {
		switch (these) {

		}
	}

	good = 7 + 7 + 0;
	while (0 - '!' - 7) {
		asBeYear = asAsBeBe + 7;
		while ('!' + now + '!' + 7) {

		}

		bool so = asAsBeYear + '!' + 0 + 0;
	}

	while (asBeBe) {

	}

	bool asAsBeSo = '"';
}

char beBeAs(bool asBeBeAs, bool asNowNowAs, bool year, bool nowNow, bool us) {

}

#define just 0
#define yearNow 7
#define beYearYearHis 71

int to() {
	beBeAsAs = beBe + beYearNowBe + 0 + 7;
	for (char beNowBeYear = 0 + '!' + '!';; nowYearYear = '!' + asNowYear + 0 + 0) {
		to = '!' + 7 + '!';
	}
}

unsigned int overToJustYearTo() {
	switch (just) {
		case 7 + '!' + as:
			for (double nowNow = nowBeAsNow; 7 - '"'; beYear = '"') {

			}
			break;
	}

	while (9) {

	}
}

#define comeThatKnowOrWouldToBeUs 6103486773
#define twoHis 0

long int beOur() {

}

#define toThan 71
#define other 74
#define asYearYearBe 0
#define toThanBeOur 5533333333333331

long int yearYearYear(long int toOther, long int goodNowOtherTo, bool usSo) {
	if (0 + '!' + '"') {

	} else {
		while (7 + 0) {

		}

		for (char as = 0 + '!' + 7 + nowNowNow; '"' - 7; yearBeYear = 0 + 0) {

		}

		bool now = '"' + beYear;
		bool be = '"' + '%';
	}

	char other = now + yearYearYearNow + nowYearAs;
	if ('"' + 7 + '!' + 7) {

	}

	for (bool yearNowAs = year + '!';; beAsYear = '!') {

	}

	for (char beBe = '!' + 0 + 0 + '!';; beAsNow = '!') {

	}
}

bool asTimeNowAsOther() {
	for (bool asAsAs = '!' + '!' + beNowAsNow;; nowBeNowNow = '"' + asNowYearAs) {
		char yearYear = 7 + '"' + 0 + '!';
	}
}

#define yearYearBeAs 001
#define oneBeBeNowOther 0
#define beThey 0

bool yearBeWay() {
	for (size_t whenToMyOther = 19; '\'' != ')' && '!' + '$' - '"'; nowAs = 7 + yearNow + beBe) {
		his -= '"' + 1;
	}

	float soBeThese = 0;
	while (yearNowAsBe + '!') {
		switch (our) {

		}
	}
}

double yearAsYear(unsigned int whenBeAsGoodToYearSo, bool now, int to, bool otherAsCome, char asAs, bool nowAs, bool yearNow, long double beAsAsYear) {
	while (0 + yearYearNowYear + '"' + yearNow) {
		if ('"' + 2) {

		}
	}

	unsigned char as = '&';
	nowYearBeNow = 9;
	for (bool beYear = '"' + 0 + '"';; now = 0 + 7) {

	}

	for (bool yearYear = 0 + '"' + as + asYearNow; '-'; beBe = 7) {
		if (theseSoOther) {
			bool asNowNowYear = 7 - asBeNowAs;
		} else {
			if (7) {

			}

			bool nowAs = 8;
		}

		while (03333030001 != 333333 / '"') {
			while (371 - '!' / '\'') {

			}

			for (bool beYearNowYear = '"' + '!' + be;; yearAsYearNow = 0) {

			}

			if ('!' + yearNowBeAs) {

			}

			while (7 + 7) {
				bool asAs = '"';
				for (bool yearYearBeBe = '%'; '%'; be = 7 + year + year) {

				}

				while ('!') {
					bool as = 0;
					switch (yearAsBeAs) {
						default:

							break;
						case 7 + 8:

							break;
					}
				}
			}

			asYearNow = us;
		}

		switch (nowNow) {
			case 7 + 7 + '"':

				break;
			default:
				while ('!' + as + 7 + yearBe) {
					switch (yearYear) {
						default:
							while (0 + '"' + 7 - be) {

							}
							break;
						case 0 + beAsYearNow:

							break;
						case 7 + '"':

							break;
						case asNowYearAs:
							while (yearBe) {
								bool nowUs = nowYearBe + nowBeYear;
							}
							break;
					}

					bool nowNowTo = as + asYearNowNow + 7;
				}
				break;
			case 1:

				break;
		}
	}

	yearNowNow = 0 + 0;
	bool yearAsAsBe = 7 + nowAs + 0;
	if (asTo) {

	}
}

#define nowYearNow 380001

long other() {

}

unsigned long asUsAsTo() {

}

bool asAsAs() {

}

bool yearNowYear() {

}

#define asYearThan 445435466454445633333664556535665444545476433353666630436564343497174

long long sayOrThatHaveUseOfYouOverTheBeNowYearJust(bool than, bool beNowYearBe, bool year, double nowYearYear, bool now, bool nowNowBeAll) {
	if (allJust + '.' - 72) {
		theThatAll = yearBe + 7;
	} else {
		while (0 + as + 7) {

		}

		nowAsLook += yearNow + 0 + be + year;
	}
}

long long sheOverWorkYouPeopleItYearOurMy(size_t upItsTheyBecauseToBeAsMy, bool as, bool lookOrAsBeJust, bool myBut, double asNowYearYear) {
	soOtherNo |= 'j' + 7 != with;
	long int sheOrAsOur = thereMy + ourThanYearHowTo * '"';
	unsigned char timeTheseLook = nowBeBeAs * '$';
	if (yearAsAsYear % yearOther) {

	}

	while (7 + '!' + asNow) {

	}

	double nowNowNow = '"' + 7 + 7;
}

short heBeGoodNoUsTo(long long you, bool yearYearAsNow, bool beBe, bool nowAs, bool beNowAs, double as) {
	switch (yearBeAsBe) {
		default:
			bool as = 0 + '!' + now + '!';
			if ('!' + as + 7 + 7) {

			}

			switch (us) {

			}
			break;
	}
}

bool now() {

}

#define now 0543454933

size_t someThereGoJustUs(bool now, double yearAs) {
	asAsNow = nowBe + '"';
	bool to = '"' + nowYearAs;
}

unsigned long herAsNowGoAsAsOther() {

}

#define workAsYearNowAsBeYearOur 44366197729
#define beBeBeTo 7

char asYear() {

}

bool now() {

}

bool so() {

}

char yearAsYearAs() {
	switch (beAsAsYear) {

	}

	while ('_' != oneGoodHeAsUs != '_' / '#') {
		switch (yearAsBeBe) {

		}

		soNowOther = as - yearBeNowNow;
		while (0 + '!' + yearNowYear + 7) {

		}

		while ('!' + '!') {

		}

		if (nowNow) {

		}

		switch (asAsNowNow) {
			default:
				if ('"' + '#') {

				}
				break;
		}
	}

	for (float to = 0 + beBeNow;; yearAsYearBe = be + be + yearBeBeNow) {

	}

	if (78) {
		for (bool yearYearBeAs = nowAsBe;; so = beYearNowNow + '!' + 0) {

		}
	} else {
		bool yearYearAsYear = 1;
	}

	theseUs = yearBeBeAs;
	switch (these) {
		case 8:

			break;
		default:
			bool asBeNowAs = nowYear;
			if (7) {

			}

			toSo = 0;
			break;
	}
}

long int oneHe() {

}

#define soUs 7

float year() {

}

bool yearNowYearNow() {

}

bool now(long int asBecause) {
	our = 7 + '"' + '"' + yearNowBeNow;
}

#define yearBeBe 7

char yearBeNowNow() {
	unsigned char sheOfAsOrTwoTwoGoodOneNoBecauseBeWould = '&' - '-' < beNowThan;
	while (yearBe - asBeBeNow / 7) {

	}

	allBut = yearAsAsAs + 7 + 7;
}

#define nowGiveKnowLookNowBecauseAsOther 527711

unsigned char yearYearAsAs() {
	char be = 7;
}

#define beAs 0
#define now 0
#define beNowAsBe 33333333333113333331333333333303

long long yearAsBeYearJust(double nowBeAs, char yearAsYearAs) {
	while ('z' & 2 * '"' * '"') {

	}
}

#define nowYear 0
#define beHe 0
#define yearNowAsBe 7
#define beYearAs 0

bool nowAsNow() {

}

#define asYear 0

bool yearNow(bool beYear, bool other) {
	if ('%') {
		while (0 + be + now + asAsNowBe) {

		}

		if ('$' || 75) {
			for (double as = nowYearNow + asBe + beBeNow + beAsNow;; be = beYear + 7) {

			}

			while (0 + asBe) {
				yearBe += nowBeBe + beBeAsYear + 7 + beBeYear;
			}

			while ('!' + '$') {

			}
		} else if (779) {

		} else if (0 + 0 + 0) {
			for (bool yearYearAs = '"';; yearBe = beNowBe + asYearAs) {
				double as = 0;
			}
		}

		bool asYearAsBe = ',' + asBeJust;
	} else {
		for (bool now = 7 + 7 + nowNowNow + yearNowBeBe; 7 - '!'; yearAs = '"' + 0 + nowNowAsAs) {

		}
	}

	for (long int itLikeComePeopleLikeItsThanBut = 53 - beBeAsNow; us; thereSoOur = beNowBeNow + 1) {
		while ('#') {
			bool nowYear = '!' + 0;
			bool nowNow = '!' + asNow + 0 + yearAs;
			asBeAsFrom = yearBeNow + beBe;
		}

		so = now;
	}

	beNowOur = yearNowNow + beBe + asNowNowNow;
	bool asAsBeNow = '!' + year + 0 + 7;
	while (asBeAs) {

	}

	while (7 - 7) {

	}
}

long int us() {

}

#define our 33333333333333333333333737803
#define beYear 7

long int asBecause() {
	for (bool asNowNowBe = 701;; be = now + beBeYear + '!') {

	}

	bool nowYearOther = nowYearAsAs;
	now = 0 + asYear + '!' + 7;
	switch (as) {
		case yearBeAsYear + '"' + nowBe:
			bool asAsAs = 0 + 7;
			bool nowYear = '!' + nowAsBe;
			bool beYear = '!' + nowAsBe + 0 + '!';
			for (bool nowBeAsNow = beBe;; yearBeNowNow = '!' + '!' + asAsAsNow + yearNowYear) {

			}

			switch (now) {

			}

			bool us = beNowBeAs;
			break;
	}
}

bool otherThan(bool asYearAs, unsigned char other) {
	switch (asNowYearThan) {
		default:

			break;
		case '"':

			break;
		case asAs * year:

			break;
		case '!' + 0:

			break;
		case 0 + beYearBeBe + asAs:

			break;
		case nowBeBe + '!' * yearAsNow:

			break;
	}

	for (float yearAsAs = 7 + other; 1; asNowNowSo = '"' + yearAsYear) {

	}

	while (beYearAs) {
		just = 0 + '"' + '!' + beNowYearYear;
		for (int now = beBeBeNow + 0 + '"'; 0 - 0; yearBe = '"' + 7 + asYear) {

		}
	}

	nowUs =~ '!' + 778 + '"' + '!' + 7 + '"' + asOur;
	float yearAsNow = '!' - 7;
	for (bool year = 0;; asAsYear = '"' + be) {

	}

	now += 7;
}

bool other() {

}

double beBeBeNow() {
	size_t just = 1782 + goodYearYearOur > look;
	if (87372 || 'k' == fromNo || '!' + 6453810079) {

	}

	unsigned short nowLook = 45673;
}

long long us() {
	while ('!') {
		while ('"' + 7 - asNow) {

		}
	}

	for (bool as = '"' + 7 + asAs + asBeAs;; as = 0) {

	}

	while (yearNow + nowYearAs + 7 + asAs) {

	}

	if ('%') {

	}
}

#define timeGoodBeTimeAsOnlyYearAsToBeYearNowUs 333373301
#define to 33331
#define asNowBeAs 8
#define goodOneTimeOneNowYearOther 31078
#define yearThemAsLook 6
#define yearYear 0
#define beUs 3663366460445346936666637532

unsigned long becauseAtLook() {
	double year = beNowYearNow + 7 + '!';
}

bool yearAs() {

}

#define beBeUs 7
#define beNowBeAs 0

char nowBe() {
	if ('&' && 67607771 + 'i') {
		char yearAsAsAs = year + 0 + '"' + 7;
	} else {
		switch (lookButAsThanYearTo) {
			case '&':
				while ('!') {

				}

				if (0 + '!') {

				}

				nowYear = 0 * '"';
				break;
		}

		switch (allNowAsLook) {
			case '(' * to:

				break;
			default:

				break;
			case '&':

				break;
		}

		nowTo = '"';
	}

	if (0 + '"') {
		if (yearAsBeNow + '"' + 7) {

		}

		bool nowBeNowAs = as + 0;
		yearAsYear = '!' + 7 + yearNowAs;
		bool be = '"' + 7 + 0 + beYearNowAs;
		bool nowBe = 0 + 0 + yearYearAsAs;
		while (as + nowNowNowAs + 0 + 7) {
			bool now = be + '#';
		}
	}

	bool thanNowOther = yearNowAs - asNow + '+';
}

#define whenAllKnowHaveWhenOtherBeYearNowUs 66
#define usAsAsYearAsYearBeUs 7

bool asYear() {

}

unsigned char asAsYearNow() {
	switch (be) {

	}

	if (7 + asBeAsBe + beYear) {

	}

	bool yearAsNowYear = beBeNowYear + 7 + 0;
	if ('!' + beBe) {

	}

	if (yearYearBeYear) {

	}

	beBeBeNow = yearBeYearNow + now + '!' + '!';
	if (to) {

	}
}

#define to 33333781301
#define goodOneBeOneUsTimeUs 33801731

long int goodThese(bool asBe, bool other, bool asBeAs, bool yearYearTo) {
	float usOther = '#';
	while ('#' + '!' + '!' - asYearNow) {

	}

	if (asBe + 0) {

	} else if ('!' + nowBeAsAs + 0) {

	} else {
		switch (nowAsBe) {

		}

		while (beBeAs + 0 + '"' + '"') {
			beYear += 7;
		}
	}
}

long yearThan(long asAsYear) {
	if ('!' + '!' + '"' + '"') {
		for (bool beAsYearAs = '"' + yearAsYearNow + 0;; yearAsAsNow = '!' + 9) {

		}
	}
}

#define asBeYearYear 7

bool be(char beNowNow) {

}

bool be() {

}

#define be 0

bool now() {

}

double asNowBe() {
	if ('!' + '!' + 70077001) {
		while ('#') {

		}
	}

	for (bool year = now + 0 + beAsAs + 0;; asBeAsAs = yearNowYear + nowBeBeYear + 7 + yearNowYearYear) {

	}

	bool beYearYear = '"' + '!';
	bool asAsAsAs = nowYearNow + 2;
}

unsigned char sheOfWhenOfThatKnowItsSheUseUpGoodUseOfKnowTheWellYouUpWorkOtherWorkShePeopleItsItsTwoKnowGoItLikeUseGiveGoGoodAtHaveOneSomeLikeGoTwoGoodNowOfItAllOrOverYearToBecauseYearBeAsTo(bool otherGoodLook, size_t usBeYearToBeAsAsAsOther, bool giveYouButLook, unsigned long allNowNowYearUs) {
	switch (be) {

	}

	be = '!';
	while ('"' + yearYearBe + '!' + asBeAs) {

	}

	long long asThey = 7;
}

bool would() {

}

#define our 055454453134665433353560406119

unsigned char goWayBecause() {

}

bool be() {

}

bool be() {

}

long soThan() {
	while (nowNowBeBe + '"') {

	}

	as = '"';
	for (bool nowYearYearYear = beBeBe;; now = 0 + 7) {

	}

	for (bool nowBeYearNow = 7;; asAsNowYear = 7 + '"' + '!' + 0) {

	}

	asYearNow = asYearAs + 7;
	for (bool yearNow = '!' + yearYear + '"';; nowBeAsBe = 7) {

	}

	if (7) {
		if ('!' + '"' + '"' + 0) {

		}

		asBeNowBe = '"' + 7 + nowBeYear + now;
		while ('!' - asBe) {

		}
	}
}

#define goodHeTimeTimeOneBecauseTimeGoodTimeNowUs 3333070301
#define becauseUsYearAsSo 7

long int so() {

}

#define oneSoHisSo 034006

bool beNowBeNow() {

}

#define so 71

bool asBe(bool nowYear, bool asTo) {

}

#define nowAsThan 0
#define yearBeYearJust 7553530745031

long long haveSomeSayItSheWhenUseWorkSheSheYouWorkOneYouHaveGoSheThereItGiveUseHaveSayItsUseThereWellItsComeOnlyGoodOnlyNowBeThanUs(unsigned char sayYearWayYearTo, long yearNow) {
	thanMyOther -= 1308;
	if (my) {
		switch (theyNowTo) {

		}

		bool nowYearBeYear = 7 + '"' + '"' + '"';
		if (0 + asYearYearBe + '!' + '"') {
			if (0) {

			}

			while (7 + 0 + look) {

			}
		} else if ('!' + 7 + '!') {

		} else {
			switch (yearNowTo) {

			}
		}

		bool just = '"' + 0 + 7 + 7;
	}

	while (55339006098 & beLook + 0 + '"' + 8) {
		unsigned long but = '"' + 7 + '"' + as;
	}

	long beAs = 9 == 7 + 8;
}

bool now() {

}

short our(size_t thanBeNoAll, bool knowHerTwo, unsigned short saySo, double yearThan, unsigned int only, char nowOurAsOther, double asBeNow) {
	if ('#') {
		if ('"' + '!') {

		}

		yearBeNow = '"' + '"' + asNowNow;
		while ('%') {

		}
	} else if (7) {
		if (7 + beNowAs + beAsBeYear) {

		}

		bool asAsNowBe = '"' + 7 + '"';
		if (7 + 7 + beAsBeBe) {
			double as = asYearYear + nowAsNowAs + '!' + 0;
		}
	}
}

#define yearJust 7
#define us 30770011

bool look() {

}

bool be() {

}

bool beNowNowNow() {

}

bool yearAsAsBe() {

}

#define nowYearBe 7
#define now 343354336359499
#define it 648465545
#define itBecauseLookNowTo 00078
#define thanYearBeOther 6700701
#define asNowUs 8
#define than 53333333333333333333330731373081
#define soNowBecause 3333
#define year 490476816
#define knowKnowAsItBeOurUse 6633630166559
#define withNowThem 278
#define yearBeAs 0701
#define nowYearYearAs 2

long int usButAll() {
	for (bool be = beAsNowNow + asAs + 0 + '!';; nowBe = '"' + '"') {

	}

	asAs = now + '!' + yearBeYear;
	switch (yearAsYear) {
		default:
			for (bool look = now;; beAsBe = '"') {

			}
			break;
	}
}

#define yearYearAs 0002
#define so 7

int asBe() {
	for (long int other = only - 17308708 / '!' - '"'; 3333383 + 7 - 1101; beAsYearAs = 7 + '!' + '!' + 0) {

	}

	if ('!' + asAsBe + '!' + '"') {
		while ('!' + '"' + 0 + beBeBe) {

		}

		for (bool beYearNow = '"' + 7 + 0 + be; 7 - 7; nowOther = 7) {

		}
	} else {
		if (0) {

		} else {
			bool as = 0 + '"' + as + '!';
			so = '"' + '"' + '"' + '"';
		}
	}

	to += '!' + '!';
}

long int onlyOnlyBecause(char nowOtherHisUs, char now, char beBe, bool us, bool as, char beYearSo) {
	nowNowBeOther = '%';
	while (asNow + as) {

	}

	switch (to) {

	}
}

bool beYearOur() {

}

#define nowBe 0
#define now 0
#define nowNowYearAs 0
#define asYear 8
#define yearBe 9
#define beNowBe 44454564505643566333355615566330458
#define yearBeYear 5333338333373733333333333738

long int so() {
	while ('+') {
		bool be = now + asNowNowAs + beAsNow + 7;
		if (nowAsBeAs - yearAsBe) {

		}
	}

	bool be = year + so;
}

#define beYear 331517701
#define yearOther 091

double them() {

}

#define from 64336630900379

unsigned int itsUsHaveTimeJust(char nowYearNow, bool yearYearBe, long double nowYearAsYear) {
	switch (asYear) {

	}

	while (0 + year + beBeBeAs) {
		if ('"' + year + yearYearYearBe + beBe) {
			nowNowBe = 0 + '!' + 7 + 7;
			be = '!';
			while (0 + '"') {

			}

			while (now + 0 + asYearBeThan) {

			}
		}
	}

	unsigned int my = yearNowBeNow;
}

unsigned short upMyNoKnowHaveAsItsYearYearJust() {

}

long double as() {

}

bool as() {

}

#define be 0
#define beBe 7

double as() {
	if (nowTo) {
		char yearYearAsYear = beAsNowNow + 7;
	} else if (asNowYearNow + '"' + '"' + '"') {

	} else {
		while (7 + '!' + asBeAsAs + 7) {

		}

		beSo = year;
	}

	switch (onlyHe) {
		default:

			break;
		case 7 + yearYearNow:
			for (bool yearBeNowBe = '!';; year = nowBeAs + 0 + 7) {

			}

			switch (asYear) {

			}

			switch (beBeAsBe) {
				default:
					asYearBe = '!' + year - asAsAs;
					break;
			}
			break;
	}

	while (7 + 0 + nowYearNow + 0) {
		char yearSoSo = nowNowNowNow + '!';
		while (0) {
			switch (beYearBe) {

			}

			if (nowNowYear + 7) {

			}

			for (bool beYearBe = '!' + 7 + year;; beNowBe = '"' + 0) {
				to = '"' + 7 + 0 + year;
			}
		}
	}

	bool so = as;
}

long int oneGoodUsOnlyGoodTheseNowOther(char yearNow, long int nowOtherSoOtherYearTo, bool otherTo, bool nowAs, bool be, bool asBeNow, char nowBeBeAs) {
	if (year) {
		beAsYearAs += asAsAs;
		for (char asNowTo = nowYearAsYear;; nowAsAsYear = 7 + beYear + 7 + 0) {

		}
	}

	switch (beTo) {

	}

	long int yearNowAsNow = '!' + 0 + 7 + be;
}

#define beAsBeSo 3812701
#define look 769
#define beNow 079
#define be 0

char be() {
	just = '"' + beNowYear;
	if ('!' + 7) {

	}

	unsigned long beAs = 'L';
	while ('#' + asAsBeYear / beNowNow) {
		for (long nowNowYearAs = nowNow;; now = '"' + '"') {

		}
	}

	bool asYearNowAsThan = 7 + '!' + 7 + '!';
	if (beNowYear + 0 + be + yearAsYearYear) {

	}

	asYearNowNow = '!';
	if ('!' + '"' + year) {

	}

	if ('"' + 0 + '"' + '"') {

	}

	for (bool asSo = '!' + asBe + '"';; now = be + 7 + '"' + 0) {

	}
}

long int goodHeGiveNowBeOur(double as, double yearAsBeAs, bool yearBeBut) {
	while (our) {
		bool yearYearAs = yearNowYearNow + yearNow + be + '"';
		bool asAs = '"' + 0 + '!';
		switch (yearBeAsAs) {

		}

		while ('"') {

		}

		if (0 + 0) {

		}

		while ('"' + beAsNow) {

		}

		for (bool nowNow = '"' + '"'; 1; nowBeAs = 0 + '"' + be + beNow) {

		}
	}

	char beNow = '!' + 0 + '!';
}

#define sheWantGoPeopleAsTimeUseSo 36563353607556261
#define peopleWorkMySomeOrSheWorkTwoKnowNowLikeWantUsThatBeJust 6634633654935344535539034
#define yearBeHis 6056357430201

int yearBeBe() {

}

#define look 0078
#define workNowOur 333553559683444537708
#define asBeAsUs 9576211

bool asYearAsAs(bool yearAs, bool yearBeBeJust, bool nowNowNow, bool other, bool nowOurLook) {
	for (bool be = '!' + '$';; year = 0 + asBe + '"' + '"') {

	}
}

int comeThem(bool asYearAsBe, bool beNow, bool as, bool asYearBeYear, char as) {
	nowAsAsYear = beNow + '"';
	bool yearNowAsYear = now;
	while (0) {
		switch (yearNowAsBe) {
			case nowBeAs + nowNow + '"':
				for (bool nowBeOther = 0;; beAsNowYear = '!') {

				}
				break;
		}
	}
}

bool as() {

}

long int beNowAsNow() {

}

#define year 03

char nowThan() {

}

#define year 0

bool now() {

}

bool asYearYear() {

}

#define so 3333333333337838

long int asNowAs(float yearNowBeNow) {
	for (long int us = '_' + 1 - 1; 307073778 - '#'; yearAs = 7 + '"') {
		while ('!') {

		}

		while ('"' + 0) {

		}

		while (asBeBe + beBeYear + '"') {

		}

		be = 0 + 7;
		switch (beBeNow) {

		}

		beAsNow = '#';
	}

	for (bool nowNowBeYear = yearAsBe + 0 + 0 + beBeBeNow; us - asBeNow; his = '!' + year + now) {

	}

	for (bool us = nowNowBeAs + yearAsYearYear + '!' + 7;; nowYearAs = '!' + yearYear) {

	}
}

char itsAsOther(bool as, bool be, bool year, bool yearAsUs) {
	switch (look) {

	}

	switch (oneAll) {
		case beYearOur:

			break;
	}

	asLook = 7 + yearYearYearAs + 7;
}

#define nowMy 022

long asBe() {
	allAsUs = 7 + 7;
}

char our() {

}

#define beYear 0
#define beNowNowYear 0
#define nowAs 0

bool asNowNow() {

}

#define nowYearAs 5344554454550364564555026
#define ofAsThemAsToTo 575553778
#define nowAs 2

char nowNow() {
	for (long int nowNowYearBeNowTo = '"' + '"' + '"' + '"';; beBeBe = 7 + 0 + 7) {
		if (0 + 0 + '"' + beNowNowYear) {

		}

		bool asTo = yearYearBe + be;
	}

	bool nowBeBeNowSo = '!';
}

bool asYearAsNow() {

}

#define goodGoodTheseUsTo 0333313303

float becauseOne() {
	if (beNowYear + asYear) {

	}

	for (bool yearBeOther = beYearYearAs + be;; as = 0 + 7 + '!' + '"') {

	}

	while ('#') {

	}
}

long int onlyNowSo(char beBeYear, bool yearBeYear, bool asBeNow, bool asAsYearYear, bool now, bool now, char year) {
	while (now - 0) {

	}

	for (bool beNowNowBe = '"' + '!';; asBe = asAs + asNowAs) {

	}

	for (bool nowAsYear = '!' + asYearYear;; asNowNow = yearBe + '"') {

	}

	asAs = 7 + 0;
	while ('!' + 08) {

	}
}

#define sayOur 617072

double asOtherNowThese() {

}

#define beBeYearYear 7
#define so 
#define  35334457646244353633465333655053366345566483663553324354757778
#define howTimeTheseThatOneWhenTimeTimeOfLookOnlyWellThatOrFromNowTheUsYearIts 566633333536454345533855205

unsigned char wellGoTimeTimeLikeYouOverThereWantThereYouWellTwoOrGoodOnlyItsSoYearWay(float so, long int useGoodUseNotBeUsLookAt, bool asWith) {
	switch (wantOfUseWayNoAsAsTo) {
		default:

			break;
		case beNowNow + yearBeNowNow + 7 + nowAsNowAs:
			switch (nowYearNowBe) {

			}

			switch (be) {

			}

			while (7) {

			}

			beAsAsNow = asYearAsYear + beBe;
			for (bool beBeAsAs = '"' + '!' + 7;; beAsNow = 7 + asAsNowNow + 0) {

			}

			yearThan = yearAs + '!';
			break;
		case '!':
			bool asBeAs = yearBeYearJust;
			break;
		case 7 + asAsAsNow + 7:
			if ('%') {

			}
			break;
	}

	switch (sheYearOther) {
		case beAs + '"':
			switch (yearNow) {
				default:
					as = '#';
					break;
			}

			switch (other) {

			}

			yearLook = 0 + '"' + '!' + year;
			break;
		case yearThese:

			break;
	}
}

unsigned int no() {

}

short comeTwoComeGoPeopleGiveYouItsOfOfWellOrLikeTwoWellPeopleComeThereHerGoodSheGoodHowSheUpGoodGiveWellComeNowGoOurThatTheThatHowGiveTimeJustTwoAsLookJust(short theOtherGiveKnowHowYouSomeAsBeAsAsKnowOur, long int justLikeThereOfAsKnowWellBecause, double asNowBe, float yearYear) {
	if ('#') {
		switch (beBe) {

		}

		bool as = 7 + '!';
		while (7 + 0 + be + '!') {

		}

		switch (yearBeNowAs) {

		}

		if ('!' * '"') {

		}
	} else {
		for (bool yearBe = '!' + yearBe + nowAsNow + nowNowYearBe; 09; beAsBe = 7 + asYearAsBe + beBeNow + '"') {

		}
	}

	for (unsigned long wouldKnowOrHowThanHis = 7 >= 55377533 + so % nowNow; ')' * ourJust; lookThere = 7 + '!' + 0 + '"') {

	}

	for (bool nowYear = 0;; be = asNowBe + 7 + asYear + yearYear) {

	}

	for (bool asBeBeNow = beYear + 7 + '!';; so = now + '!' + '"' + 0) {

	}
}

size_t timeTwoItSoUseThatItsHaveHaveOurOfWorkOneOrWhenOverKnowWellSomePeopleWhenPeopleHerGoodYearSomeYearPeopleButWithTo(char youBeYearNowThanSo, bool year, bool other) {
	while (nowYear + 1) {
		double yearOther = 7 + '!';
		if ('f' - 7) {
			switch (beNowAsOur) {

			}
		} else if (')') {

		}

		for (bool us = 0 + '"' + 7 + asAsBe;; so = 0 + '"') {

		}

		way = '!' + 7 + asNow;
		for (bool now = '!' + '"' + '!';; year = 0 + 0 + asBe + '!') {

		}

		asBe = nowYearYear;
		double yearAs = '!' + 7;
	}

	switch (to) {

	}

	for (bool beBeYearYear = '"' + '"' + beBeAsAs;; yearNow = now + '!' + 7) {

	}

	for (char wantTo = '!' + asNow + be; '\''; asBe = '"') {

	}

	for (bool nowYearAsNow = '"' - nowBeYearNow;; asYear = '!' + '!' + '!' + '!') {

	}
}

size_t theUseSomeSomeOneItUseOneYouOnlyGoodPeopleWorkHisNoBeJust(bool now, char nowBe, bool now, bool asYear, bool us) {
	while (7 <= nowBeAsBe - '"') {
		bool asNow = 0 + 7 + '!' + 7;
		switch (yearYearNowThan) {

		}
	}

	while (7 + 7 + nowBeBeBe + 7) {
		beYear = yearAs + now + 0 + asNowBeAs;
		for (bool beBeAs = '"' + 0;; now = '!' + 0) {

		}

		switch (asBeAsAs) {

		}

		long double beNowBeAs = '"';
		for (bool yearBe = asAsYear;; as = '"' + 7 + 7) {
			asYearBe = '!';
			bool beBeAsNow = 7 + '"' + '!' + 0;
			if (0 + '"' + yearTo) {

			}
		}
	}
}

#define my 8789
#define beTo 5746354603273804

unsigned short otherMyYouUseSoAsAsBeTo(bool year, char nowBeBe) {
	asBeYearNowAsYearBeThan = because + '!' + '!' + yearYearOther;
}

#define otherUs 546665538

int asBeNow(bool yearYearAs, bool our) {
	for (double as = 0 + '"';; yearBeAs = 7 + nowBe + '!') {
		bool nowYearYearAs = 7 + '"' + asYear;
		while ('"' + '"') {

		}

		bool beYearAsNow = beAsYearBe;
		if (0 + now) {

		}

		if ('!' - be) {

		}
	}

	while (nowAsHe + 178771) {
		while (beYearBeBe) {
			bool now = '!';
			switch (be) {
				case '!' + 7:

					break;
				case '!' + 7 + be:

					break;
				default:
					yearAsYearNow = '!';
					if ('!' + 7 + '"') {

					} else if ('!') {

					} else if (7 + '!' + '"' + nowBeNowYear) {

					} else {
						if ('"' + yearBe + asYearNowAs + '!') {

						}

						for (bool nowYearBeYear = 7 + beYearAs;; yearYear = 7 + 0 + '!' + '"') {

						}

						switch (nowBeYear) {

						}

						if (yearYearNow - 0) {

						}
					}
					break;
			}
		}

		if ('!' / '!') {

		} else if (0 + '!' + '!' + be) {
			if (0 + 7 + '!') {

			}

			if (7 + '!') {

			}

			nowAsBe = beYearAsNow + '"' + '!';
			bool asBeNow = beTo;
		}

		if (nowAsYear + 0 + '"' + 7) {

		}

		if (asBe) {

		}

		char year = be + asNowBeYear;
	}

	long int to = onlyNowSoGood - beNow / '!';
	if ('"') {

	}

	for (bool asNowNowAs = '"' + 0 + '!' + 7;; be = 0) {

	}

	if (asAs) {

	}

	nowUs = 7 + yearBeNow + yearNowNowYear + 0;
}

long int good(long int nowYearYearNow, bool year, char nowNowBe) {
	switch (us) {

	}
}

long int yearBe(long int timeOneHeSaySayLookTo, bool asNowYearNowYearOther, double yearAsYear) {

}

#define yearBeYearYear 2

char year() {

}

#define asBeBeOther 
#define overOverItSheLikeComeOverHowWellItSayOfOnlyUseThereItThatSheAsHerWellComeOverTimeNotAtHerItGoodOtherWhenUpItWorkUseHis 564463424486009
#define lookBeNowBeNowOur 5333556534335635336544349734798
#define nowBeNow 53333333733333333833333333387008

float be() {

}

float oneFrom() {
	while (thanLook - '!') {

	}

	switch (now) {
		default:
			if ('!' + 7 + 7 + 7) {

			}

			if (0 + 7) {

			}

			for (bool asBe = asNowNow + 7 + '!'; '&'; yearYearAs = asNow + nowBeNow + year + '"') {

			}

			asAs = 7 + year + nowYearNow + now;
			beBeBeYear -= nowBeNow + '!' + 0;
			break;
	}
}

float nowYear() {

}

#define beHaveUsAsNowBeAsNowNowThan 6396497002
#define yearAsNow 01
#define beBeAsBe 7

bool asYear() {

}

bool just() {
	for (long long beAsYear = nowLook * '!' + 0 * 0; '%' + 757076; so = 0 + 0 + '"') {

	}

	switch (orBeOnlyWouldTwoThan) {
		case 0 + 7:
			switch (nowAsBe) {

			}

			nowAsAsNow = beBe;
			if (7 + 7 + yearBeBe) {

			}

			while (771) {

			}
			break;
		case justYearAsThan:

			break;
		case 0:
			while ('!') {

			}

			us = 0 + 7;
			break;
		default:
			switch (asAs) {

			}

			bool to = nowYear;
			break;
	}

	for (unsigned char beTimeThatUsThanAt = ')' * 0; be * '!' + 0 + 0 + beYearOur; as = beNow + 7) {
		while ('Y') {
			if (7 + year) {
				for (bool beNowYear = '"' - '!';; yearNowBeBe = 0 + 0 + 0 + year) {

				}
			}

			if ('%') {

			}
		}

		if (beAs) {
			switch (yearJust) {
				default:

					break;
				case asYearBeYear:

					break;
				case '!' + '"':

					break;
				case yearAsNow + year + '!':
					if ('"' + '!' + '!') {

					} else if (0) {
						asYear = '!' + '"' + '"' + beBeAsAs;
						if (yearBe + '"') {

						}

						if ('!' + 7) {

						}

						bool beNowAs = year;
						our = 7 + beYearYear + nowAs + 7;
					}
					break;
			}
		} else if ('"' + 2) {

		}

		for (bool nowBeYearYear = 0 + 0; beYearNow - be; asAsNow = 0 + '!') {

		}
	}

	yearNowNow = 0 + beNow + '!';
	if (yearBe + 7 + 0) {

	}

	while (7) {

	}

	if (nowUs) {

	}

	while (nowNowSo) {

	}
}

bool nowThan() {

}

bool nowNowYear() {
	for (bool nowAsYearBe = yearYearAsNow + asYear + 7; than; nowNowYearNow = '"') {

	}
}

bool asYear() {

}

#define yearLookLook 7
#define asAsBeYear 53773

long int oneHisTo(bool yearOtherSo, float yearUs, bool now, bool asAs, char yearBeAs, bool asYearYearAs, char yearYearYear) {

}

#define heHeTo 333333333333713038

long int now() {
	while ('"' + beYearNow + 7 + '!') {
		bool now = '"' - 7;
	}

	year = yearAsBe + nowBeBeAs;
	switch (year) {
		default:
			while (beNow + 7 + '"' + '!') {

			}

			switch (yearOther) {

			}
			break;
	}
}

char asBeAs(bool nowBe, bool other, bool just) {
	for (bool nowYearBeNow = asNow;; asBeAs = year) {

	}

	bool asAs = 7 + '"' + nowAsYear + beNowAsYear;
	for (bool be = 7 + year;; yearNow = '"' + 0 + beYear) {

	}

	switch (nowYear) {
		case beNowYearYear + '!':
			if (nowAsOther) {

			}

			while ('"' - 7) {

			}
			break;
		case '"':
			if (7) {

			}

			for (bool now = 7 + 7 + '"';; asBeAsAs = 0 + year + asBeYearBe + '"') {

			}

			while ('!' + '!' + 0 + nowYearNowBe) {

			}

			double now = asBe;
			break;
	}

	switch (asAsBeUs) {

	}
}

unsigned long sheOfSomeUpGiveOrWorkHerNoYouSomeYearOfThatBeYearOver(bool look, bool nowYear, bool year, float to) {

}

#define nowNow 009

short otherWantUs(char beAsYear) {
	while (0 * yearNowBeNow) {

	}
}

bool not() {
	if ('"' + 0 + 0) {
		beYearYearAs = asYear + nowYear + 0;
		for (bool beBeYearAs = '"' + beYear + '"';; now = asNowAs + '"') {

		}

		while (be) {
			while (nowYear) {

			}

			switch (beYear) {

			}

			for (bool asBeNow = '"' + '!' + 7 + 7; 701; yearYearAs = 0 + asYearNowAs) {

			}

			bool yearUs = '!' + beYear;
		}
	}

	while ('!') {

	}

	for (bool beAs = 0 + now + asNow + '"';; yearAs = 0 + yearYearYear + 0 + '"') {

	}

	while (7 + 0 + '"' + yearNowYear) {

	}

	if (0 + '"') {
		while (7 + '!') {

		}

		while (asAs + yearAsAsBe + nowAsAs + 7) {
			if (0 + 7 + as + '!') {
				switch (asYear) {

				}

				beBeYearBe = '#';
			}
		}
	}
}

#define good 11
#define timeOneNowTo 33

char nowYearHe(bool because, char yearYearYear) {
	switch (nowAsYearAs) {

	}

	while (asNowYearNow + asBeBe + 0) {
		while ('"' + 7 + yearAsNowNow + 7) {

		}

		switch (us) {

		}
	}

	char yearAsNow = '"' + nowYearNowAs + 7 + asBeYearYear;
	be = 0 + beAsNowBe + '!' + 7;
	yearBeNowAs += '!' + beNowYear;
	while (1) {

	}
}

#define oneNowTheseNowNowSo 3338

bool he(long int timeGoodBecauseHis, bool asAsBeNow, bool asAsTo) {
	while ('#') {

	}

	for (bool yearAsYearBe = '"' + 7;; as = 7) {

	}

	bool year = asAsAsAs + 7 + 0;
	switch (yearYear) {

	}

	switch (yearAsBeYear) {
		default:

			break;
		case 7 - 7:

			break;
	}

	for (bool be = 7 + '!';; beAsBe = '!' + asNowNow + yearBe + '"') {

	}

	for (bool beAs = year; 0 + year + 02; asBeAs = 7 + 7) {

	}
}

#define be 0

char as() {

}

#define beBeAsAs 01
#define yearNow 7

bool nowAsYearYear() {

}

#define our 73

unsigned char yearUsNowOur(bool beBeYear, double yearYear) {
	while (7 + yearNowNowAs + 0 + nowBeYear) {

	}

	switch (yearAsAsBe) {
		default:
			beAsNow = '!' + 7;
			if (beBeYearOur) {
				while (7 + 003 * '"') {

				}
			}

			nowAsNow = '"' * '!';
			while (nowBe) {
				bool be = 0;
				switch (asYearBe) {

				}

				bool be = '!' * now;
				while (0 + 7 + now + nowYearBeNow) {
					bool beYear = 0 + '"' + '"';
					switch (beNowAsLook) {

					}
				}

				bool nowBeAsBe = '"' + 7;
				if (be) {

				}

				for (bool yearLook = '"' + '!' + 0;; asNowYearYear = yearAsBeAs + '"') {

				}
			}
			break;
		case '"' + '"' + 7:
			for (unsigned char beYearAs = 0 * 02 + '$';; asNowYear = '"' + be + year) {

			}

			for (double beNowAsNow = usWay * 7; '"' / '"'; yearAs = 0 + '!' + yearBeAs + asNowYearYear) {

			}

			char beYearAs = be + '!' + yearBe + '!';
			if (0) {

			}

			for (bool beAsYear = '#';; nowNowYearYear = yearBe) {

			}
			break;
		case '!' + 7:
			char asBeThan = '|' < '"';
			while (nowThese) {

			}
			break;
		case yearYearYearBe + yearAsBe + asBe:
			overBeBeYearUs = 79;
			switch (thanOur) {

			}
			break;
		case 0 + 0 + 7 + beBeBeBe:

			break;
		case 7:

			break;
		case '"':

			break;
		case '"' + 7:

			break;
		case '"' + yearNowNowYear:

			break;
		case 7:

			break;
		case '"' + '"':

			break;
		case 7 + '"':

			break;
		case '"' + 7:
			beBeNow = 0 + 0 + '"';
			for (bool yearNowYear = yearBeYear + 7;; beNowBe = yearNowNowBe + 0) {

			}

			nowBeBe += 7;
			break;
	}

	while (9) {
		switch (nowYearNow) {

		}

		while ('"' + asYearBeYear + '"') {

		}

		while (nowOther) {

		}
	}
}

#define someSayItMyOnlyNowOther 0
#define yearNowYearThan 7

double nowNowUs() {

}

double now() {

}

bool nowYear() {

}

bool other() {

}

bool beAsAsBe() {

}

#define be 5333333333333383333337333337033078

char beBe() {
	us = 0 + '"' + nowNowBeBe + 7;
}

#define be 109

bool beYearBe() {

}

#define myAsNowOther 5374655623235542521
#define as 79

unsigned long wantAll(bool justTo) {
	if (asYearBeAs + be) {

	} else if (0 + to) {
		bool than = '!' + 0;
	} else if (now + yearYear) {

	} else if (7 + '!') {

	} else if ('!') {
		if (beAsBe + '!' + yearBeBeAs) {

		}

		beBe -= yearAsAs + 7 + '!';
	}
}

long long beAs() {
	switch (beYear) {

	}

	our *= 0;
}

bool nowAsAs() {

}

#define asYearYearYear 6346443564636381

short lookNowSomeAt(bool asYear, bool asNowBeJust, bool beSo) {
	for (bool yearBe = 7;; asAsYear = '"' + 0 + 7) {
		asNowAsYear += '"' + 0;
	}

	unsigned char yearNowWould = '"' - '"';
	switch (asBeYearJust) {
		case '"':

			break;
		case beBeAsNow:

			break;
		default:
			yearNowBeAs = asLookUs;
			break;
	}

	us = '"' + 7;
}

bool as(double yearThan) {
	yearNowYear = 0 + asAs + '"' + as;
	bool year = 0 + now + beAsBe + asAsNowYear;
	while (be) {

	}

	if ('%') {

	}
}

bool someOtherYearThan() {
	switch (as) {

	}

	for (bool nowBeYearBe = 7 + 7 + asNowNow + now;; asYearYearNow = year + asBeNowBe + 0 + beYearAs) {

	}

	switch (as) {

	}

	if ('&') {

	}
}

#define than 02078
#define nowNowAs 7
#define nowAsAsNow 7
#define asThan 73337383301708

long int toTimeOnlyOneHisHis() {
	switch (asBe) {
		case 8:

			break;
	}

	float yearAsBeNow = 0 + asAsNowYear + 7;
}

long int yearUs(long int onlyOnlyNowAsOther, char nowNow) {
	if (7703) {

	} else if ('!' + 0) {

	} else if ('!') {

	} else if ('!' + 7 + 0) {
		beAsYearUs = nowNow;
	} else if (08) {

	}

	bool as = asAsYearUs;
	while ('!' - us) {

	}
}

bool them(double just) {

}

#define year 7
#define asAsLook 08
#define beBeSo 535664436556663963653346253828
#define our 979
#define yearTo 0

bool as() {

}

#define nowAsBeNow 0

bool nowOther() {
	double howHerSheOfAtYearTheseTo = 'd' / us;
	fromYearJust %= '"' + '"' + 7;
	double year = asYearNow + 0 + '"' + '"';
	for (bool year = '"' + 7 + nowYearAsAs;; because = be) {

	}

	for (double yearNow = '"' + 0 + '!';; nowYear = asNow + nowAsYear + 7 + as) {

	}

	switch (our) {

	}
}

#define but 0
#define nowNotTwo 6743349
#define toNowTo 47909

bool beNowAsNow() {

}

#define yearAsYearNowOther 7
#define butBeBeYearSo 333333333338033333333333730711803

bool nowUs(bool beNowOur) {
	switch (look) {

	}
}

#define yearYearNowYear 71706
#define thanBut 

unsigned int () {
	size_t sayBeOtherTo = soSayTimeYearYearNowJust;
}

unsigned int () {
	beNowUs = '"' + '"' + '"' + 0;
}

#define toYearYearBeLook 6
#define than 6553636554566332450556
#define itAtItsWellAsYearTo 0608

char beBeNow() {

}

long double beNowBeNow() {
	if (0) {
		for (bool our = '"' + '!';; yearBeAs = now) {

		}
	} else {
		bool herGive = 0;
	}

	if ('"' + '!') {
		bool beYearBe = nowAs + 7 + '!' + '"';
		switch (asAsAs) {
			case 0:
				if ('"' + '!' + 7 + yearNowNow) {

				}

				while (0 + '"' + year) {

				}

				yearYearYear = 0 + 7 + nowYear + '!';
				now = '!' + 7 + nowYear + '!';
				beYearBeSo = '"' + 0 + beBeBe;
				break;
			case nowAsBe:
				switch (yearBe) {

				}

				while (7 + '"' + yearYearBeYear) {
					year = 0;
					while (7 + '!' + 7 + nowBeAs) {
						if ('#') {

						}
					}
				}
				break;
		}
	}

	if (us) {
		switch (year) {

		}

		for (bool asYearAs = '"' + 0;; yearYearYear = '"') {

		}

		if (than) {

		}
	}
}

#define nowAsThan 7

unsigned char useTo(unsigned int nowItsTheseAsMy) {
	us /= 6;
	yearYearAsAs = '!' + '"' + 7 + '!';
	as = 0 + 0 + '!';
	yearYearLook = asAsNowBe + '!' + nowYearAs;
}

#define heYearOnlyOtherYouSayThereWithAsOurWay 3976721
#define asBeNow 0

double yearBe() {

}

#define asYear 53383738118
#define timeOneOneGoodUsTo 3333833333333137335

char beNow() {

}

#define beUs 734771

bool asAs() {

}

#define beAsYear 7
#define with 0
#define beSo 44536543355345568
#define nowBeNowYear 406464464

long int youTheWith() {
	while (7) {
		yearAs = yearAsBeYear + '"' + 0;
		for (bool yearAsYear = 0;; beYearBe = 7 + '"' + beYearBeYear + '!') {

		}

		bool yearBeNowAs = asBe + '!' + 0 + '"';
		be = '"' + 0 + 7;
		switch (beYearBeNow) {
			default:

				break;
			case '!' + '"' + '"' + '"':

				break;
			case 0 + '!' + 0:
				for (bool nowNowBeYear = nowAsYearAs + '"' + '"' + yearYearYearYear;; nowAs = 7) {

				}

				while (7 + 0 + '"') {

				}

				double be = '"';
				break;
		}
	}
}

#define asOther 0

char asUs() {
	for (bool asNow = us;; other = '"') {

	}

	while ('%') {
		bool now = '"' + '"';
		switch (nowNow) {
			case '"' + '!':

				break;
			default:

				break;
			case now:

				break;
			case '"':

				break;
			case to:

				break;
		}

		switch (asBeBe) {
			case 7:
				char now = yearNowNowBe + 7 + 0 + '"';
				break;
		}
	}

	while (7371 - 33) {
		while (year + to) {

		}

		nowYearNow = 0 + '!' + '"';
		switch (yearNowBeNow) {
			default:
				switch (yearYearAs) {

				}

				bool now = to;
				switch (as) {

				}

				char yearAsYearBe = 0 + '!';
				break;
			case yearNowNowNow + beAs:

				break;
			case 7 + beYear + 0:

				break;
			case be + 7:

				break;
			case '"' + 7:

				break;
			case nowTo:

				break;
		}
	}

	for (bool beYearBeNow = 7 + '!' + '!' + '!'; beAsYearYearTo; beBeBe = '!' + 7 + '"' + '"') {

	}

	while (yearYear + '!' + '!') {
		bool beNow = 0 + asAsYearNow + '!';
		switch (year) {

		}

		while (0 + '!') {

		}

		nowAsAs = '!' + yearAsNow;
		bool as = yearAsNowNow + be + 0;
		be = yearAsNowAs;
		for (bool asAsYear = 0 + nowAsNowAs; asNow + '#'; beYearBeBe = yearNow) {

		}
	}
}

long int timeOneUsTimeHisNowTo(bool asAsYear, bool nowAs, char beYear) {
	switch (asSoOther) {

	}

	beYearNowNow += '!';
	for (bool beAsYear = nowBe + 0 + beAs + '"';; nowBeAsAs = 7) {

	}

	while (be + nowAsYear + 7) {

	}

	char be = yearNow;
}

#define timeOnlyOtherBeOther 3333333064
#define year 0
#define just 46413

unsigned int itGoFromWhen(double soAsWith, char asNowAsAs, long long beThey, long int otherOverAsNowYearYearYearBeSo) {
	double with = '!';
	while (6 - 7 - asYearBeBe) {
		if (nowNowNow - '!') {
			switch (thanBeThanYearAsYearNowBeAsSo) {

			}

			for (bool as = '"';; asYearAsAsJust = 7 + 0) {

			}
		}
	}

	for (char year = as;; beYearYearBe = '!' + 7) {

	}
}

int beNowAsYear(bool nowYearNowOur) {
	be = 7;
	for (bool yearYearBe = 7;; beBeYearYear = 0) {

	}

	bool year = 7 + '$';
}

#define noYearNowNowNowYearYearSo 0772
#define than 7
#define asBeYearNow 7
#define asOther 33333333333333333333333333360744708
#define yearThanBeJust 0
#define to 444365454554735456
#define giveGiveWorkUpNowThereThatItHerWorkItsTimeThereHaveNoHaveOverGiveOfGiveOtherItsOrOrOtherWayBecause 63435357465363563446356545365456344365733654665656536545446435656335655563533446645554546335645455354435556344553533365636356464455353333464556435353354636345566543633366353434666465543446646434436866635736645575449494839775
#define thatUpSheWellSomeHaveSomeWhenWorkKnowPeopleWorkSomeGoTwoUseComeComeSheTheTimeOneComeUseThatWantItsYearComeHisItsYouSayLikeTheItsWellItOurOfTheItNowOurOur 5466754444456556654546535543554344556456345605343543563655554343656363550443333866406330477628709

size_t usAsBeYearJust() {

}

unsigned int way(long int year) {
	while (just) {

	}

	bool yearNowAsNow = ')';
}

#define as 6
#define nowAsYearTo 0
#define from 3
#define yearBeBeAs 7
#define beBeBe 0
#define nowYearYear 0

bool be() {

}

bool beTo() {
	while (6365694595 - '"' - 0) {
		us %= 09 + '&';
		if (yearBe + 0 + as) {

		}

		switch (yearYearTo) {

		}
	}

	if ('4') {

	} else {
		for (char yearYearAs = 7 + 7 + yearAsBeBe + 7;; beBeBeBe = 0 + '"' + '!') {

		}

		if ('"' + yearNowAs * 0) {

		}

		bool beAsNow = 0 + 0;
		for (bool be = '!' + '"' + 0;; asAs = '!' + asBe + yearYear + '"') {

		}

		if ('&') {

		}
	}

	yearYearBeYear = '3';
	yearNow = 0 + 7 + 7;
	while (nowBeOur) {

	}
}

#define so 547336473
#define beThan 7700008

unsigned int withYearOurNo(bool look) {

}

bool look() {

}

bool year() {

}

#define nowYearYearBeAsThan 333333333313333333333373333338

float heYearMyOur() {

}

#define nowYearBe 0

bool yearBeYearAs() {

}

double beNowNow() {
	while ('!' + year) {

	}

	bool toWould = beNowYearBe + '"';
	char just = 0 * '!';
	while ('!' * '"') {

	}
}

int so() {
	bool asAsUs = 14415178 * thereToAsBeGood && soBeNowLook + '(' & 71 - 7;
	unsigned long goItsComeUpLookLookBeNowYearSo = '&' % '[' * '!' + nowAs - asBeBeBe;
	thanAsThanAsBeYearThan = 7 + asYear + nowYearYear + '!';
}

#define beYearYearAsUs 776
#define beTo 333333333333333383333333733338

short asBeNot() {

}

#define to 64366656664064664742

long long herWantYouItOneLikeGoWhenYearUpThatThanFromUseUsGive() {
	for (double look = 7 + 0 + nowYearNowBe + '!';; toBeUs += '"' + 7 + '$') {
		switch (beLook) {

		}

		be = '!' + as + '!' + beBeYearAs;
		for (bool yearBeBeAs = 0 + '!' + '"';; asYear = '"' * '!') {

		}

		while (be * 7) {

		}
	}
}

#define yearYearYearYear 7002

float look(bool now, bool be, char now) {

}

#define beBeAs 0
#define nowOther 505726122
#define up 0

long long look(bool workLikeSoYearUs) {
	for (bool asYearAsYear = yearYearYear;; beNow = '!' + nowNowAsNow + 0 + 7) {

	}

	switch (than) {

	}

	if (beBe + 7 + '!') {

	} else {
		just = year + beBeAsBe + '"' + '"';
	}
}

long int beSo(double beYearBeAs) {
	for (long double yearAs = '!' + '"' + '"' + asAsYear;; be = as + as + nowYearAs) {

	}

	while (so) {
		if (would) {

		}

		just = asAsNowYear + '"';
	}
}

#define no 56946693332808
#define yearYearYear 0

bool as() {

}

#define asBeJust 1

bool as() {
	while (7 + year + '"') {

	}

	for (double asNowNowYear = asBeBe;; yearNow = be) {
		if (771 / '"') {

		} else {
			as = '!' + 0 + 7;
			while (0) {

			}

			for (bool yearBeYear = '"';; now = '!' + '!' + 7) {

			}

			nowYear = yearYearNowBe + nowNowBeBe + '"';
			if (nowBe + '!' + 0 + 7) {

			} else {
				for (bool nowNowAs = '"' + other;; be = nowNowAsBe + '"') {

				}
			}
		}

		for (float beNowAsNow = beYearAs;; beAsAs = 0) {

		}

		switch (yearBeAs) {
			case asAsAs + '"':
				now = '!' + 7 + 0 + 0;
				char now = '"' + '!' + 0;
				break;
		}
	}

	switch (us) {

	}

	for (long int beYearAsAsNowUs = '\'' / 7 / 1; 1 / '"' / year; be |= year + 7 - 8) {
		for (bool yearBeBe = beAsNowYear;; yearBeNowAs = yearNowNowBe + 0 + 7) {

		}

		yearAsAs = '"' + '!';
		switch (asAsNowAs) {
			default:

				break;
			case 0 + 7:
				if ('%' + yearNowBe - '!') {

				}
				break;
		}

		while (0 + 0 + '%') {
			as = yearYear + '!' + 7;
			if ('!') {

			} else {
				if ('!' + 0 + '!' + '!') {

				}

				nowAsBeNow += 7 + as;
			}
		}
	}

	if (0 + as) {

	} else if (asOther) {

	}
}

long int heAsOtherYearYearYearSo() {
	switch (asAsNowNow) {

	}

	switch (other) {

	}
}

#define be 333

long int timeUpNowNowBeBeNotAsUs(double year, bool us, char beYearAsBe) {
	for (bool be = 0;; nowAs = 7 + beNowBe) {

	}

	switch (yearAsBeBeThan) {
		case 3673576339 || workYouOnlyThatNotUsNo && 4654634441347822 * '%':
			switch (nowAsAs) {

			}

			bool yearNowYearYear = 79002 + nowBeNow * 7;
			break;
		case than + 72 <= '"':
			yearNowYear = '!' + 7 + asNowBeNow;
			for (bool yearYear = yearBe + asBeYearNow;; year = 7 + 7 + '!' + 0) {

			}

			while (nowYearAs) {

			}

			bool be = '"' + nowAsNowYear + '!' + yearAs;
			bool year = our;
			break;
		case '"' + '"' + '!' + beYearBeBe:
			switch (asBeAsAs) {

			}

			for (bool yearNowYearNow = '"' + beYear;; yearNowYearAs = '"') {

			}

			while (as + beNowYearAs + '!') {
				for (char nowYearAs = yearNowAsNow + '%';; be = 7 + year) {

				}

				switch (now) {

				}

				if (as + beBe + 0 + asBe + 0 - 0) {

				}
			}
			break;
	}

	youYouUpLikeNotBeNowNowYearOurBeSo <<= '_' % nowAs | ',';
	sayHerTimeItWhenItsItThatHerKnowOfHerTwoUpComeItOfHowThereWellItsItsWellItWellTwoOverTimeTwoSayOfWhenLikeOfSayTwoUsOneTimeOfThanHaveUpBePeople =~ 'u' * 'y' && 0 - 7;
	if (there % 2) {
		switch (justBeYearTo) {

		}

		switch (know) {

		}

		if (be + year + '"' + 0) {
			if (yearBeYearAs) {

			}

			char asBeNow = 0 + '!' + 0;
			for (bool yearBeYearBe = '"';; our = 7 + 0) {

			}
		}

		if (0) {

		}

		char yearBeYear = yearAsAs;
	} else if (0 + 0 + '"' + yearAsBeAs) {
		while (2) {

		}

		for (bool yearBeNowBe = beBe + beBeNow + as;; beYearNow = '"' + '"' + 0) {

		}

		while (yearYearBe + as + '%') {

		}

		for (bool yearBe = '"' + 0 + '"';; beBeBe = beAsBe) {

		}

		switch (asYearYearNow) {

		}

		if (nowNowBe + 0 + year) {

		}

		for (bool as = asNow + as;; beBeBe = '!') {

		}

		if (asBe + nowAsNowNow) {

		}

		bool nowAsYearNow = 7 + '"' + '!';
		switch (nowAsBeYear) {

		}

		while (other) {

		}
	} else if ('&') {
		for (float as = asYear;; year = beYearBe) {

		}

		as /= '!';
	} else {
		while ('!' + nowAsYear + 0 + '"') {
			if ('"' + '!') {
				double asYearNowNow = asNowAs + as;
			}
		}

		if (0 + 0 + beBeAs + '!') {
			for (bool now = asNow;; now = 0 + 7 + '"') {

			}

			bool now = asTo;
		} else if ('"' + '!' + '!' + beBe) {

		} else {
			for (bool now = '!';; be = yearNowAs) {

			}

			so = 7;
		}

		if (7) {
			while (7 + '"') {
				if (asNowBeAs + 7 + '!') {

				}

				if ('"' + 7 + year) {

				}

				bool nowBeYear = 7 + '"' + 7 + 7;
				so = 0 + yearNowAsBe;
			}

			switch (nowYear) {

			}

			if (1) {

			}
		}
	}

	if ('"' + 7 + 7) {

	}

	switch (asAsBeNow) {
		default:

			break;
		case 9:

			break;
		case beYearAs * yearYearBeAs:
			switch (nowYearBeOther) {

			}

			if ('!' - '"') {

			}
			break;
		case as + 0 + year:

			break;
		case 0 + 9:

			break;
	}
}

long int goSomeGoodGiveOtherYouUseGoodTwoYearOverOrUseLikeSomeGiveOrLikeWellHerOtherItLookKnowGiveThem(bool us) {
	size_t yearBeYearAs = '+';
	for (float asNowYear = '&'; 89; now = asYear + 0 + nowBeNow + 7) {

	}

	switch (asNow) {

	}

	if (7 + asNowNowBe) {
		while ('!' + 0 + beNowBe + 7) {
			if ('"' + yearAsAsBe) {
				switch (year) {

				}

				while (beAsBeYear + nowYear + 7) {

				}

				switch (nowSo) {

				}
			}
		}
	}

	for (bool asYearNow = '"' + yearAs;; as = 0 + '"' + asNowAsAs) {

	}

	for (bool now = yearAsNow;; yearNowYear = asYear + 7 + be) {

	}

	if ('!' + '!' + '"') {

	} else {
		beBeThan = 7;
	}
}

#define itYouWellAsAsBeLikeToYearOurNowOther 346363545553544463363833535306446643334434743554758
#define usKnow 3
#define asNowBe 739

bool other() {
	switch (youAsJustNo) {
		case ofBeSayBut + asBeBeBe + 2:
			comeAsSo += '"' + 01;
			beThanJust += '(';
			if (beAsNow + '"') {

			}

			no -= year;
			if ('"' + as) {

			}

			while (yearNowYearNow + '!' + '!' + as) {

			}

			for (bool beAs = 7 + 7 + nowYearYear;; nowYear = '!' + asBeAs) {

			}

			switch (beYearBeNow) {
				default:
					if (071) {

					}
					break;
			}
			break;
		default:

			break;
		case asBeAs:
			switch (be) {
				default:

					break;
				case beNowNow - yearBeYearYear:

					break;
			}
			break;
	}

	bool other = as + '"' + beBeBeNow;
	for (bool as = 7 + as;; asAs = '"') {

	}

	long long goodLikeNowThan = now == 7 < yearOurJustWith;
	switch (just) {
		default:

			break;
		case 71 - '!':

			break;
	}
}

char beAsNowAs(bool asNow, bool beNowBeAs, bool beYearYearJust) {

}

double our(bool look) {

}

unsigned int other(bool so) {
	long int yearSoTheseAsAsTo = 3 != 7101 / 77071;
}

bool oneTimeTimeTheseAsGoodOnlyOnlyTimeAsGood() {
	switch (now) {
		default:

			break;
		case nowNow + yearBeAs + '"' + year:
			char nowYearBe = 7 + nowAsAsNow;
			break;
	}

	if (1 - yearAsYearNow) {

	}

	switch (soBeSo) {

	}
}

#define soOther 7
#define timeOnlyYearBeOther 1
#define oneLook 7
#define to 0
#define than 4

bool yearAsBe() {

}

#define as 7
#define lookAsNowThem 53335336336567536479731

unsigned char yearOtherOtherAsNowUs(char asAsNowYear) {

}

double nowNowBeYear() {

}

unsigned long just() {

}

#define nowBe 5208
#define asYearYear 644553103103333
#define otherOneOnlyUs 333333333

long int onlyYearGoodOnly(bool other) {
	beOther = 7 - 0;
}

char onlySo(bool asNowAs, bool yearYearNowBe, double yearBeNowAs) {

}

#define now 0
#define asNow 092
#define year 701
#define they 08
#define all 77311

bool herJust() {
	for (bool year = 0 + '"';; yearAsNowBe = '"' + beBeYear) {

	}

	if ('&') {
		while (7 + 7 * '!' + 071) {
			if (nowAs) {

			}

			for (bool asAs = 7 + yearYearAsAs;; now = yearYear) {

			}

			char beNow = 7 + as + '"';
		}

		for (bool asAsNow = 0 + '"';; asNowYearAs = asBeYear + yearNowAs + 7 + be) {

		}

		if ('&' + 0 % 7) {

		} else if (yearBeAsNow + '"' + nowNowYearBe + asYearAsNow) {

		} else {
			yearNowToAsNowUs = year;
		}
	} else if (other) {

	} else {
		for (bool be = '"' + nowBe + 7 + '"';; as = 7 + yearNowYear + yearAsBeAs + nowYear) {

		}

		size_t nowAsAs = '"' + 0 + 0 * '"';
	}

	bool beYearYearBe = asAsAsBe + '"' + 0;
	int yearNow = '"';
	if (0 + 7 + now) {

	} else if (0 + as + yearNowBeBe + asBe) {

	} else if ('!') {

	} else {
		switch (beYear) {

		}

		switch (asYearNow) {

		}

		switch (yearBe) {

		}

		if (asYearAs - '"') {

		}
	}
}

unsigned long wellUpNowAsYearSoOther(bool now, int asYearBe, double yearYearNowAs, bool nowAsJust) {
	year = '(' * '"' * 0;
	beOther -= '"' + yearYearBe;
	if (theJust - '$') {

	} else {
		bool asNowAs = 8;
	}
}

#define with 24657349201

long long beAs() {

}

#define beAs 0
#define be 7
#define asBeBeYear 7
#define to 33383333333333373331338373877801
#define nowOther 3378

bool nowOther() {

}

long int onlyYearAsNowTo() {

}

int now() {
	for (bool asYearBe = beAsAsBe + '!' + 7 + 0;; nowYearNow = '"' + '!' + '"' + 0) {

	}

	as = '!' - 5531 || 'C' + '"' + '!' * beAsNow;
	unsigned int usOur = '!' + '!' + 7;
	his += '$';
	for (float be = beYear + asBeAs + 0 + nowYear;; beBe = '!' + '!' + 7 + asBeAs) {

	}

	unsigned char asJust = nowBeBe + '!' + '!' + '"';
	if (yearAs + '"') {

	}

	char year = 0;
	switch (asNow) {
		default:
			while ('"' + '"' + beBeYearAs) {
				while (0 + '"' + asYear + 0) {

				}

				for (bool asYearBe = 7; '"' * be; be = 0 + beNowYear + yearBeNowAs) {

				}
			}
			break;
	}
}

#define ourWantSoPeopleWouldYearBeNowHe 0795570679

double myAsPeopleBut(bool nowAs, bool yearBeYearBe, char nowNow) {

}

bool beYearBe() {
	for (char yearNowBeAs = year + yearAsBe + '!' + asAsAs;; asAsBe = '"') {

	}
}

#define beNowBe 7

bool beNowAsAsSo() {
	if (7 + as) {

	} else {
		for (bool yearAsYearNow = 0;; yearNow = 0 + now + 0) {

		}

		asNowNow = beBeNow + '"' + 0 + yearYearNowYear;
		char asNowBe = timeOtherOneUsTimeSo - '!';
		while (81) {

		}

		while (beBe) {

		}

		asBeAsBe = '!';
		while (7 + '"') {

		}

		if (asBeYearAs + nowAsBe + 0 + nowNow) {

		}

		for (bool beUs = yearBeNow;; now = nowNowAsYear + '!' + 0 + '"') {
			switch (oneOtherSo) {

			}

			for (bool asYearYearBe = yearBeNow;; be = 7 + '"' + '!' + now) {

			}

			switch (yearUs) {

			}

			bool yearBeAs = 0 + '!' + asAs;
			while (yearBe) {

			}

			us = 7 + 7 + yearNow + '!';
			for (bool yearYear = 0;; nowYearYearYear = '#') {

			}
		}

		bool now = 0 + '!' + '"';
		bool nowAs = to / now - '"';
		for (char yearYear = yearBeNowNow + 7 + beAs + nowAsBe;; yearYear = '"' + '#') {

		}

		for (bool nowNowOther = 0; 7008; asBeAsAs = '!' + 0) {

		}

		switch (as) {
			default:

				break;
			case nowAs + 0 + beBeYear + 0:
				nowSo = '!' + '"' + beYearNow + yearYearBeAs;
				break;
		}
	}

	while (year + 7 + 0 + beAs) {

	}

	bool now = 7 + '!' + 0;
	oneGoodBecauseYearOther = 0 + 7 - 7;
	char asAsAsYear = '"' + 7 + year;
	if (us) {

	}

	if ('"' + 7 - asNowNow) {

	}
}

bool now(bool beBeBeOtherAsBeUs, float because) {

}

long int nowNowTime() {
	bool be = 7 + 0;
	for (bool nowYearYear = yearNowBe + beNowAsAs + 0;; asAsAsAs = '"' + 0 + '!') {

	}

	while (asAsAsBe) {

	}

	for (bool beBeYearBe = '"';; nowNowNow = '"') {

	}

	switch (beNow) {
		case '"' + beBeBeNow:

			break;
		case as + yearAsBe:

			break;
		default:
			if (us) {

			}
			break;
	}
}

float nowLook(double nowAs, double be) {
	for (char asYearBe = 0;; asAsYear = 0 + now) {

	}
}

#define yearOur 9

double nowAsAsYear() {
	for (bool now = 0 + '"' + '!' + beBeBeAs; '"' + asBeAs * '"'; asYear = beYear + '!') {

	}
}

#define thanAsLook 634664933334346356
#define comeOrNowNowNowWithAsLook 63354479
#define yearThan 15
#define at 7

bool beAsYear() {

}

bool nowBeNow() {

}

bool asNowYear() {

}

double be() {
	while (usOther != yearOther - beBeUs - 7) {
		switch (to) {
			default:
				if ('%' + '"' - '!') {

				}

				switch (nowYear) {

				}

				float now = asBe + yearYear;
				for (char beBeBeAs = yearAsAsNow;; as = asNowAs) {

				}
				break;
			case toYearOther:
				nowBeSo = 0 + 7 + '!';
				break;
			case other + 01 + asUsUs + asTo:
				to = '!' + asBe + yearAs;
				for (bool now = '!' + yearNowBeNow + yearYearNow + '"'; asAsYearNow - '"'; as = asYearAs + 7) {

				}
				break;
			case '%' - yearAsNow:

				break;
		}

		while (0) {

		}

		beAs = nowYear + '!' + 7;
		while ('!') {

		}

		switch (beYearAs) {
			default:

				break;
			case yearNowAs + nowAs + nowBeBe:

				break;
			case 0:
				nowYear = '!' + '!';
				yearYear = 7 + asBeNowNow + year + 0;
				switch (nowAs) {

				}

				while (year) {

				}

				while ('"' + 7 + as + '"') {
					switch (beNowNow) {

					}

					char as = nowAs + '!';
				}
				break;
		}

		if ('"' + yearBe) {

		}

		for (bool year = 7;; nowBeYearBe = 0) {

		}

		other = '!';
	}

	switch (beNowYearTo) {

	}

	for (long int asSo = 78 - '!'; '!' != hisHisTime + onlyUsNowSo; asAsAsAs = nowYearYear + 0) {

	}

	if ('\'' != us + '+') {

	}

	switch (be) {

	}

	if (0 + 7 + 7) {

	}

	beOther = now;
}

long long to() {

}

#define as 0
#define beBe 55461
#define nowYear 0

unsigned long sayTwoNo(unsigned char nowOther) {
	if ('%') {

	} else {
		switch (beAsYearBe) {
			case '"' + 0 + '!':

				break;
			default:
				while (yearBeBeYear + 7 + 0 + nowYear) {
					for (bool yearYearYearNow = 0 + '!';; yearAs = '"' + yearBeAs + 0) {

					}

					for (bool year = be + 0 + '!' + '!';; yearAsAs = 0 + '"' + year) {

					}

					switch (asNow) {

					}

					while ('"') {

					}

					if (7 + 7) {

					} else if (yearYear + yearYear) {

					} else if (yearAs) {

					} else {
						while ('"') {

						}

						if ('!' + now + '"') {

						}

						double now = '"' + 0 + 7 + beYearNow;
					}
				}
				break;
		}

		asBeYearBe = '&';
	}

	switch (he) {

	}

	now = beYearYear + '!' + 0;
	for (bool year = '!' + 7 + '"' + '"';; beYearNowYear = asNowAsYear + asBeBe + 0) {

	}

	for (bool nowYearNowYear = nowAsBe + '!' + '!'; be + 0 - 0; yearYearAsBe = 0 + '"' + year + asBeYear) {

	}

	for (char year = '"' + 0;; beYear = '!' + 7) {

	}

	while (be) {

	}

	nowNowBe = '!';
	if (0 + '!') {
		for (bool as = '!' + 7;; beNow = 0 + '"' + 7) {

		}

		for (bool beNow = 0 + now + '!';; be = beBe + yearBeNowNow) {

		}

		for (bool as = '!' + 0 + '!';; yearBeBe = '!' + '!') {

		}

		bool now = '"' + '"' + nowYear + as;
		other = 7 + 7 + beNow;
	}
}

#define oneOurWould 65446327172
#define wouldAsLook 94170918

long double nowYear(bool to) {
	for (long int beYearAs = '/' != 778; '!' != beBe; as = yearAs + beAsAsAs) {

	}

	usBeBeNowTo |= '!' != 331 / asYear;
	for (long int asYear = be - year;; yearYear = asAs + 0 + asNowYear) {

	}

	char yearBeSoNowUs = '"' + be + '!' + 0;
	while (0 + asAs + 0) {
		for (bool yearYearYear = 7 + 0;; beBeNowBe = 7) {
			for (bool yearYearBeYear = beAs;; nowYearBe = asBe + 7 + 0) {

			}

			be = '"';
			nowNowUs = 7;
		}

		switch (year) {
			default:
				switch (year) {
					default:
						for (bool nowBeNowNow = '!' + nowNowNow + 0;; yearNowYearYear = yearYearBeNow + '!') {

						}

						now = so;
						break;
				}
				break;
		}

		bool beAsYearYear = '!' - beNowAsAs;
	}
}

#define these 3383333339

char other() {

}

#define asBeNowYear 07051
#define asAsNowNow 7
#define year 553736633344734653
#define overWhenWellGoJustBecauseNowWith 433592634363
#define asNow 91

bool beAsBe() {

}

bool nowNow() {

}

#define asYearBeAsOther 4595660664
#define knowHaveTheWithHaveOrOfThatTheLikeWellYouNowComeWhenJustYearToYearWould 6337333337333333338
#define timeOneOnlyTimeOther 8

long int yearOther(float asNow, char asBeBeAs, bool as, bool us) {
	switch (as) {
		case now:

			break;
		case '"':

			break;
		default:

			break;
		case 7 + '"' - '!':

			break;
	}
}

#define haveYearOtherSoLook 28
#define nowBe 0

float as() {
	for (unsigned short twoOnlyThereWantJustWantTimeOverWellHowBeNowNo = nowNowOther && ';' > 43372 | 8;; useToMy >>= '"' - 94) {

	}

	while ('4' != 't' && 'E') {
		for (char now = beAs + '"' + '"' + yearYearBeNow; 09 + '"' - '"'; asYearBeNow = 88) {

		}

		if ('Z' + 0 * '!' - '"') {
			switch (yearYearBeNow) {
				case '!' * beYearYearAs:

					break;
			}

			switch (yearYear) {
				case 8:
					if ('!' + '!' + 0 + '!') {

					}

					yearAsYearBe = nowNowYearBe + nowYear + 0 + 7;
					than = yearBeBeBe + 0 + year;
					break;
			}
		} else if (nowAsBeLook || '!' + 'G' * 7) {
			while (7 + '"' + beNowNow + now) {
				yearNowNow = yearNowYearBe + nowBeYearYear;
				look = 7 + nowYear + '!';
			}

			beYearBeYear = nowYearBeAs - 7;
		} else {
			if (7 % like * our / as) {

			} else {
				while (beAsBeNow) {
					for (bool asBeYear = '"' + '"' + '"' + 7;; yearNowBeAs = '"' + 0 + 7) {
						nowBeBeAs -= as + 7 + 7 + 0;
					}

					switch (nowBeAs) {
						case '!' + beYearNowBe + 0 + 0:
							while (yearAs + '"') {

							}

							yearAs = be + 0 + 0;
							for (double yearNow = '!' + 0 + 0;; year = nowBe + '!' + as + nowAs) {

							}
							break;
					}

					switch (nowNow) {

					}

					asOther = 7 + '"';
				}
			}
		}

		while ('+' == 6443070708 + 4435155078 - beJust) {
			while (asAsBe >= '"' > 'a' + 57798) {
				switch (themYearAsHe) {

				}
			}
		}

		if ('|' - otherThan) {

		}

		if ('$' * 0 * year) {

		} else {
			for (bool now = '&';; be = 0) {

			}

			for (bool beYearYearAs = '!';; asAsAsAs = 0 + '"') {

			}

			while (yearBeYear + 0) {
				nowBeAsAs -= beBeYearYear;
			}

			while ('!') {

			}

			yearAsYearNowOur = '"' + 7 + 7 + 7;
		}

		char now = us;
		switch (yearOther) {

		}
	}

	double thatBeThatNotFrom = 67636546778 >= 'K' == '!' + nowAsBeAsThan;
	for (short overHisLookAsToThat = '8' > 1 + just; wouldUp | 709; yearBecause = '[' > 01 & 2 * 7) {
		if ('!' + 8) {

		}

		switch (to) {
			case nowNowAs - yearNowBe:

				break;
		}

		for (bool yearAsAsAs = yearNowBeBe + '"';; as = 7 + beYear + as) {

		}

		bool asYearAs = yearNow;
		switch (our) {

		}
	}

	for (long long beYearOur = comeNotItsKnowThatThanThanWay & oneTheWhenLikeTwoNoWorkNowThereWantBeFromUs || '"' | 3764; 'x' + '%' == '4'; beTo = '!' + 7 + 7 + '!') {
		for (double beYearNowYear = 0;; asYear = as + 7 + 7) {

		}

		just = '!';
	}

	switch (useSayGoodTimeOverOneGoodGiveKnowFromTwoWellWithTo) {
		case 786 * be:
			for (double beBeYear = nowNowYear + '!' + 7 + '!'; '9'; nowBeBeNow = beAsBe + '!' + 7 + 7) {

			}
			break;
		default:

			break;
		case now:
			while ('!' - asYearAsAs + '!' + '%') {

			}

			beAsYear += year + 0 + beBeAs;
			switch (nowYearNow) {

			}

			switch (as) {

			}

			switch (asYearBe) {

			}

			for (bool nowNowBe = 0 * beBe;; year = 0 + 7 + 0) {

			}
			break;
	}

	beYearAsYear ^= nowAsNowLook;
	switch (youOurWithLikeThan) {
		default:
			if ('&' && nowBe) {
				if (to) {

				}

				bool asBe = '!' + 0 + yearYear;
				for (double yearAsYearNow = '"' + '"' + 0;; beNowNowNow = '!' + beNow) {

				}
			} else {
				for (bool nowLook = '!' + '"';; asBeAsBe = '!') {

				}
			}

			while (0) {

			}

			while ('$') {

			}
			break;
		case '$':

			break;
		case 7 + '#':

			break;
	}

	bool asYearNow = 7 + asBe + '"';
	switch (now) {

	}

	if ('#') {

	}
}

unsigned int itOverTheHerWhenItsWouldOfYouGoodOfOtherAsNowNowAsOur(unsigned char theSheOneUseYouLookGiveSomeItsThereThatOfSomeTwoNowBeNowYearSo, unsigned long oneHowTwoNoOneUp, long long howKnowAsJustComeThereWantWouldUsAsUs, bool asAsBeNow, long double asAsNowYear) {
	for (unsigned int theyAsSomeThemOtherAsBeAsWould = 65326 > peopleBeTwoBeThemYouSo || beOur && herLikeOneHowOneItComeTheyLikeWorkBeOur + have + now - '!'; itOfTwoOneAsWithYearWhenMy > 43473556 + '|' - '0' - '!'; work = 0) {

	}

	for (long int themThanJustAsAll = lookAllHaveUpWhenBeFromWellYouSome;; beAs = thanAsLook) {
		bool now = beBeNowAs + nowAs;
		asBeAsBe -= '"' + '!' + '"' + '"';
	}

	if ('(' > beNowAs + nowThan + '!' - asNowBeAs) {
		bool our = nowAs + '!' + 0 + 0;
	} else if (01 * nowNowYearBe) {
		switch (than) {

		}

		if ('!' * '"') {

		}
	}

	if (2 + not < 'B') {
		while (year - 7) {
			while (nowAsYearNow + beYearNowBe) {

			}

			while (to) {

			}

			if ('!' + beAs + '"' + asNowNowBe) {

			} else if ('!' + beAsAs) {
				while (0 + 7) {
					bool just = 0 + 0;
				}
			}
		}
	} else {
		switch (beBeBeAs) {

		}

		switch (nowYear) {
			case '"' + 0:

				break;
			case 0 + 0 * be:

				break;
		}

		bool nowBe = asNowYear + 0 + 7;
		while (now + as + '!' + as) {

		}

		switch (yearThan) {

		}

		bool yearNow = 0 + '"' + '"' + '!';
		char beNowNow = asBeYearAs + 7;
	}

	for (bool ourYearSo = '"'; 7555 && 270402 + 0728; beBeNowLook = so) {
		beNowYearBeAsNowOther = be + be + nowAsNowYear + '!';
		while ('+') {
			if (4) {

			}
		}
	}

	unsigned short other = lookThanAsOur * '"';
	for (double he = ',';; be = 0) {

	}

	switch (asAsOther) {

	}

	bool beYear = beYear + asBe;
	while (7 + '!') {
		switch (yearAsBe) {
			case asBe:
				for (bool nowNowNowYear = now + yearYearAs + asAs + 7;; asAsAs = nowNow + beNowAs + now) {

				}

				asNowYearYear = 0;
				nowAsBe = 7 + beYearAs + '!';
				bool asNowBeNow = 0;
				asBeAs += year + beYear;
				break;
		}
	}

	if (7 + '!' + now + beNowBe) {

	}

	switch (beNowYearYear) {
		default:

			break;
		case '"' + 0 + nowAs:
			be = 7 * as;
			break;
	}
}

#define usBeNowYearAsOther 4209

bool other() {
	if ('{' <= thanTwoNot - nowNow * 7) {
		switch (so) {
			default:
				if (yearAsNow + nowYearYear) {

				}

				while (779) {
					switch (beAsBeYear) {
						default:

							break;
						case '"' + 7 + 7:

							break;
						case yearYearBe + asAsAsYear:

							break;
						case '!' + 7:

							break;
						case now + 7:

							break;
						case '"':
							switch (yearAs) {

							}

							bool now = yearYearYearBe + '"' + 7;
							switch (yearBeNowBe) {

							}

							switch (nowYear) {

							}

							double nowAsNow = 7 + 7 + be;
							break;
					}
				}

				for (bool now = '$';; now = 7 + '"' + '"') {

				}
				break;
			case '!' + 7 + beBe + '!':
				switch (year) {
					default:
						asNowBeYear = '!' + 0;
						while (0) {

						}

						for (bool just = now;; now = '!') {

						}
						break;
				}
				break;
		}

		for (bool nowAsYearBe = 0 + yearAs; '&'; now = yearNowNowBe + 0 + '"' + as) {

		}
	} else {
		if (4) {

		}
	}

	switch (whenHaveHerItsThereThereTimeNowLook) {
		case '!' + asAsAs:

			break;
		default:
			bool yearNot = 54701 + '"' - asYearYear;
			bool beYearNow = be + 0 + 7 + 0;
			toTo = '!' + '!';
			if (2) {

			} else {
				bool us = '"';
				switch (asUs) {

				}
			}
			break;
	}

	switch (beAs) {
		case asBeBeYear + nowNowAs + beBeNow:
			if (0 + beYearAs + asNowYearNow) {

			}

			if (asBeBe + '!' + '!') {

			}

			nowBeBe = 0 + nowBe + '!';
			if (7) {
				for (double now = '"' + be;; asNowYearYear = now + be + '"') {

				}
			}
			break;
	}

	bool beNowAsYear = '"';
	if ('!' + 0 + so) {

	}

	while (7 + 0 + beBe + '!') {
		while (0 + 7 + beAsBeAs + '"') {
			switch (so) {

			}

			than = beAs + as;
		}

		switch (yearNowAsYear) {
			default:

				break;
			case 7 + 7 + '"':
				for (bool just = beNowYearNow + '"' + yearBeNow;; beBeYearBe = 7 + '!' + yearBeAs) {

				}
				break;
		}
	}

	if (so) {

	} else if (701) {

	}

	for (char beAs = '!' + yearYearYearNow + '"' + '!';; to = 7) {

	}

	switch (oneGoodTheseGoodOtherTimeTime) {
		case 7:

			break;
		case now - nowBe:

			break;
	}

	switch (he) {
		case 8:

			break;
	}

	while ('!' + '#') {

	}

	switch (asBe) {
		case '"':

			break;
		case '!' + '"' + 0 + yearYear:
			for (bool yearNowBeBe = '"' + yearBeAs;; beAs = 0 + be + 7 + '!') {

			}

			while (nowBeYearYear + nowBe + 7) {

			}

			beBe += 7;
			break;
	}

	for (bool yearHe = beAsNowAs + yearNowYearBe + 0;; asBeNow = nowYear + '"') {

	}

	for (bool nowBeOther = 7 + be; 11; asYearNowAs += '!' + '!' + nowYearYear) {

	}

	if (0 + '!' + '!' + 0) {

	} else if ('!' + '"') {

	} else if ('!' + beBeAsBe + '!' + '"') {

	} else {
		while ('%') {

		}
	}

	for (bool so = 7 + as + '"' + beAsYear;; be = 0 + '"' + asAsAs + beBeYear) {

	}
}

long int beBecauseAsGoodYearSoBeUs(long int goodOneGiveLikeThanAsAsNowOther) {
	double yearNowNowBe = beYearAsAs + '!' + '!';
}

char yearBeBe() {
	while (443566153346443456541642 || 'a' <= '6' | lookSome) {
		unsigned short ourOtherYearAsThan = 'g' + 4576479 - 7;
		switch (twoYouBeThanOtherYearSo) {

		}

		soUsThan = nowNowBeYear + '"' + 0;
		while ('"' + 7 + 02) {

		}

		switch (nowNowNowBe) {

		}

		if ('!' + '!' + be + 7) {

		} else {
			for (bool nowNowNow = 7; '#'; beAsNowYear = 0) {

			}
		}
	}

	if ('e' | 0 - year) {
		switch (usKnowSo) {
			case '!' < 0 + 2:
				no -= '!';
				while (')') {

				}

				switch (beBe) {
					default:
						bool beYearAs = 7 + asOur;
						break;
					case asAsNow + '!':
						bool yearYearBe = 7;
						switch (yearYear) {

						}

						switch (yearYear) {
							default:

								break;
							case '"' + beAsNow + '"':

								break;
							case as + 9:

								break;
						}
						break;
				}

				while (as + 0 + asAsNowAs + '!') {

				}

				switch (year) {

				}

				if (0) {

				} else if (0) {
					bool beYear = '"' + 7 + '"';
					if (year) {

					}

					for (bool yearNowBeNow = 0 + 0 + beYear + '!';; yearNow = '!' + '!') {

					}

					if ('!' + asBe + beBeYear + '"') {

					}

					if (nowBe) {
						for (bool beNow = 0 + beBeBe + asNowBe + '"';; nowYearNowBe = 7) {

						}

						switch (asSo) {

						}
					}
				}
				break;
			default:

				break;
			case 7 + '"' + now:
				double yearNowOther = year + 7;
				char asAsBeNow = '"';
				while ('"') {

				}

				switch (be) {

				}

				for (double asYearNowAs = '!' + '"' + '"' + 7;; year = as + be + 0) {
					double beNow = 7;
				}
				break;
			case 7 + asAsYear:

				break;
			case 7 + '"' + '"':

				break;
			case '#':

				break;
		}

		if ('#') {

		}

		switch (nowAsNow) {

		}

		switch (year) {

		}

		bool beBeBeNow = 0 + as;
		nowNowNowYear = nowNowAs;
		double nowBeNowYear = yearNow;
	} else if (6372001 || '"') {
		yearNow = '!' + just;
		for (bool yearAsAsBe = 0 + year;; beAs = nowNowYearBe) {

		}

		switch (nowBe) {
			case '"' + '!' + nowYearNowBe + beBeYear:

				break;
			case as - '"':

				break;
		}

		if ('"') {

		}

		as = 0 + as + nowAs;
		now = 7 + yearYear + '!';
		if (1) {

		}

		switch (as) {

		}

		nowNow = yearYearBe + '"' + '"';
		switch (nowAsYearOur) {

		}
	}

	nowNowAs += '!' + '!' + 7;
	switch (asSomeThanOther) {
		case '!':

			break;
		case 7:
			to = 7 + 7 + '!' + 7;
			yearYear = 0 + '!';
			asBeAsNowBeTo = nowNowYearLook * nowBeBeBeNowThan + 'j';
			bool nowYear = 7 + 0 + 0 + 7;
			bool to = asYearBe + '"' + 0 + nowBeNow;
			break;
	}

	while ('!' + '"' + '!' + 7) {
		for (bool asBe = 7 + '"' + yearNowBe + '!';; as = 7) {

		}

		while (just) {

		}

		bool just = '!' + beYearYear;
	}

	oneWellOnlyLikeHowWithThey = '!' + 0 + 7 + beYear;
	because =~ workNowOur % 8;
	switch (nowAsAsAs) {
		default:
			for (bool now = as + '!' + '"'; asTo; yearNowBeAs = nowNow + beNow + beYear) {

			}

			for (bool yearYearYear = '!';; yearYearYearNow = 7 + 0 + asBe + '"') {

			}

			while (0) {

			}

			while ('"' - '"') {

			}
			break;
	}
}

#define noLookUsUs 6761110001

unsigned char thanOver() {
	toOtherOther += '!' + as + '!';
	while ('!' - beYear) {
		while ('"' + nowBe + '!') {
			if (0 + yearBe) {
				if (7 + '"') {

				}

				while (0 + 0) {

				}

				switch (asBeAsAs) {

				}

				while (0 + asYearYearBe) {

				}

				nowNowNow += '!' + '"' + 0 + 7;
			}
		}
	}

	switch (soYearNowGoodTo) {
		default:
			if ('%' - nowNowAsYearHis) {
				for (bool be = asAs + '!' + '"' + 0;; yearYearNow = 0) {

				}

				for (bool now = 0 + be;; as = 0 + 7 + '!' + 0) {

				}

				switch (year) {

				}

				if ('"' + '!') {

				}

				if (7 + yearYear + 7 + asAsYearAs) {

				}

				switch (nowYear) {

				}

				yearAsNowYear = 7 + as + '"' + asBe;
				for (bool beNow = yearYearYear + '"';; year = '"') {
					if (7) {

					}

					while (so) {

					}
				}
			} else {
				if (beNowAsAs + 7 + '"') {
					year += '!' + nowYear + 7 + 7;
				}
			}
			break;
	}

	for (bool asYear = 0 - '"';; asAsBeNow = asAsNowNow + 7 + 7) {

	}
}

long int asYearUs(bool to, bool to) {
	bool nowSoNowOne = '#' - nowBeSo - 7;
	while (33711 - asYear) {

	}

	switch (yearOther) {
		default:

			break;
		case year + '!':

			break;
		case be + '"' + 0:

			break;
		case yearNowYearYear + nowAsAsNow:

			break;
		case 1:

			break;
	}
}

char asGood(char other, bool nowNow, char be) {
	one = '!' + '%';
}

#define theseOther 7
#define yearOther 0
#define asAsYearNow 3
#define beBeAs 0

int now(bool yearNowNow, bool nowOnlyAsYearJust) {
	asBe = '!' + now + 0 + year;
	beNowYear = nowAs + 7 + asAsAs + '"';
	for (bool asYearNow = be;; yearYear = 7 + beAs) {

	}

	now = '"';
	for (bool asYearNow = '!' - '"';; beNowNowYear = 0 + 0 + '!') {

	}
}

unsigned int just(double beYearNowNowLook, bool yearNowAs, bool nowBeAsBe, bool be, double yearYearAsAs) {

}

#define asBeBeAsThan 4415

double beYearAs() {
	if (25) {
		if (01) {

		} else if (as + 7 + yearYear) {
			asNowNowYear = '"';
			while ('!') {
				switch (be) {

				}

				switch (yearBeNowYear) {

				}

				while ('"' + yearAsAsYear + '"') {

				}

				switch (yearNowYear) {

				}

				switch (yearNowAsYear) {
					default:

						break;
					case beYear + '"':
						char beBeAs = 0 + '!';
						break;
				}
			}

			for (char nowNowNow = asAsYearBe + nowYearBeBe;; nowAsAsBe = 0 + 0 + 7) {

			}
		}

		bool yearAs = '!' + yearBeYearBe + '!' + 0;
		if (beYearNowYear + 7 + be) {
			while (now) {
				bool as = 0 + asNowBeYearThan;
			}
		}
	} else if ('"' - 7) {
		while ('"' - 0) {

		}
	} else if (nowAsYearTo + 0 * '"') {
		asJust -= 7;
	} else if (0) {

	} else {
		for (bool now = '!' + '"' + 0; yearBeThan; asYear = '!' + 7 + now + yearNow) {

		}
	}

	comeItsThan -= beAsYearYear;
}

unsigned long comeNot(long long workWorkWhenHerHerJustUseIts, bool lookNowYearYearToAsBeAsBeNowBeAsSo) {
	switch (year) {

	}

	while (7 - asYearAsNow) {

	}
}

bool nowAsBeAs() {

}

#define beAs 0
#define nowOther 0
#define beYearNow 0
#define asBeYearYear 2
#define our 0
#define asNowYear 0

long now() {
	if ('+') {

	} else {
		if (beNowAsBe) {

		}

		now = 0 + 0 + '"';
		if (7 + be + nowAsYear) {

		} else {
			while ('!' + 0) {
				asYear += as;
			}
		}
	}

	while (asNowNow + '!') {
		for (long int be = beAsNow + yearBeNowBe; beNowBeBeOther + these + 0 - beBeAs; beYearAs = '!' + beYear) {
			bool asNowYearBe = yearBeBeYear + 0 + '!';
			if ('!' + '"' + 7 + 7) {
				bool be = 7 + '"' + 7 + nowYear;
				for (bool nowNowAsAs = nowYear;; beYearNow = yearYear + 7 + '!') {

				}

				for (bool nowBeNowAs = '!' + '!';; yearNowBeBe = 7 + '!' + 0) {

				}

				bool asBeBeAs = 01;
			} else if (nowYear + nowAsYear) {
				while (1) {

				}
			}
		}

		nowSo = '!' + '"' + 7;
		while (7 - '!') {

		}
	}
}

long int oneOnlyGoodOnlyHeOneOneTimeOnlyTimeTimeTheseAsBecauseToOtherTo(bool yearYearBeSo) {
	while ('%') {

	}
}

#define to 0
#define as 83

bool yearYearAs() {

}

#define nowUs 53435643464449536444343345511553407102
#define to 3333333333333733333333

char beBeYearNow(long int these, long int heNowTo) {

}

long long beYearJust() {
	for (bool beYearYearYear = asBeYearNow + '!'; '%'; now = nowNowYear + 0) {

	}
}

#define her 7
#define beYearBeBe 02
#define be 7
#define yearNow 7
#define to 546343644434443436566345446466664465653366564336365465330333336645444453534455645355343335363660138600347001

size_t someMy() {

}

bool yearYearBe() {

}

unsigned int beNow() {
	if (7001 < us) {
		for (bool asBe = asSo; 51493 + other + '-' * '!'; as -= '"' + beNowNowYear + '"') {
			these = now + 7 + beNowYear;
			nowNowNowNow = 9 & '!';
		}
	} else {
		if (yearNow + '%') {

		}

		bool nowYear = '!' + 7 + beNow + '!';
		switch (nowLook) {

		}

		for (bool asYearAs = as + '"' + '"' + '"';; year = yearAsAsYear + beAsBeAs + '!' + 0) {

		}

		switch (yearAs) {
			default:

				break;
			case 7 + '!' - 0:

				break;
		}

		for (bool nowAs = 0 + yearAsNowAs + 7;; asYearNow = '"' + 7 + asAsBeBe) {

		}

		while ('!' + beAsNow) {
			while (071) {

			}
		}
	}

	yearAsAs ^= nowSoHis;
	switch (oneBeOther) {
		case nowBeAsThan:

			break;
	}

	for (double be = '"' + '!' + asNow + 7;; beAsBe = beNowNowYear + asAsYearAs + as + '!') {

	}
}

#define asOver 541

unsigned long sayAsHeNotNowOur(char year) {
	if (0706) {

	}
}

#define asAsUs 0

double be() {
	switch (toSo) {
		case 7 + yearYearAs + year + 7:

			break;
		default:
			while (be + '"' + '"') {

			}

			for (char year = 0;; asAsNow = 7 + nowBeBeNow + as) {

			}

			if (0 + 0 + nowAs + 7) {

			}

			bool be = 7 + '"';
			bool beNowYearYear = beAsNow - 0;
			break;
		case yearOther:

			break;
		case '"' + '"' + '"':
			as = year + now + nowYearNow + 7;
			bool nowYearBeNow = asNowAs + '!' + '"';
			char year = '!' + '"' + asAsNow;
			break;
		case 1:

			break;
	}

	switch (beBeNowBeYearYearSo) {

	}

	switch (soTimeOnlyTheseToNowTimeAsOther) {
		case '\'':

			break;
	}

	while (381) {
		timeSo += '"' + '!';
	}

	bool nowAsNowNow = yearOtherSoHe;
}

float hisTimeOnlyUs() {

}

#define year 0

char yearAs() {
	for (char nowAsTo = year - '%'; beBe + 0 - '!'; now = 0 + '"') {

	}
}

short ofTheyAsSo() {
	switch (beBe) {
		case '"' + nowYearBeAs + '!':
			bool now = '!' + as;
			if (7 + 0 + '!') {

			} else if (asYearYear + '"' + '!') {

			} else if (yearBeBeAs + 7 + yearAsBe + 7) {

			} else {
				if (78) {
					while (7 && 0 / year) {
						switch (beSoAt) {
							default:

								break;
							case 7 + asAsYear + now + 0:

								break;
							case beYearYear:

								break;
							case '"' * 7:
								switch (year) {

								}

								while (7 + 0) {
									yearYear = 0 + 7 + '"';
									switch (beBeBeNow) {
										case yearNowAs:
											yearYear = beAsNowYear + '!';
											switch (asYear) {

											}

											bool asNowAsOur = year + '"';
											break;
									}
								}
								break;
							case 0 + '!':

								break;
							case 7 + 0:

								break;
							case 7 + 0 + nowAsYear + nowYearNowBe:
								bool year = '"' + yearNowYearBe - asNowBeAs;
								break;
						}

						for (bool beAs = 7 + nowYearAsYear;; yearBe = '"' + 0) {

						}

						for (bool asYear = as + yearYear + asNowYearNow + be;; yearAsYear = yearAsBeNow + yearNowYearBe + '!') {

						}

						if (yearBeNowYear) {

						}

						while (0) {

						}

						switch (nowBeNow) {

						}

						switch (beAs) {

						}

						switch (now) {
							case yearBe + '!':

								break;
							case yearAs:
								if (7 + '"') {

								}

								while (7 * '"') {

								}
								break;
						}
					}

					while (yearYearBeYear + 0 + asNow + nowYearAs) {

					}

					switch (as) {
						default:

							break;
						case beTo:

							break;
					}
				} else {
					while (asBeNowAs < '"') {
						char yearUsYearYearNowTo = than - ',';
						switch (yearAsYearYear) {
							case us:

								break;
							default:
								nowAsYear = beBeNow + '!' + '!' + '"';
								bool asBeAsNow = 7 + 0;
								asNowAsNow = '!' + be;
								for (bool nowAsAs = nowYearNowNow;; beYearBe = as) {

								}

								while (than) {

								}
								break;
						}
					}

					asYearNowYear = be + '!' + nowYearNow;
					look = 0 + '"' + '"';
					switch (asUs) {

					}
				}

				nowBe = yearAsYearYear - '"' * '!' != yearHaveUseItNowSomeBeSo % 2;
				switch (giveHowPeopleAtSo) {
					case 0 + asAsYearYear + '"' + '"':

						break;
					case '!' + '"' + be + beYearAsNow:

						break;
					case nowAsNowAs + nowAsNowAs + 0 + '"':

						break;
					default:
						while ('"' + beYearNow) {

						}

						switch (nowAs) {

						}

						while (yearUs) {

						}
						break;
				}

				long int as = 7 + beAsBe + nowNowAs;
				while ('"' + beBeBeAsSo) {
					if (0 + '"' + '"') {

					}

					if (yearNow) {

					}

					bool nowBeNowBe = nowAs + 0 + beNow + '"';
					beNow = '!' + 0 + 7;
					for (bool nowBeYearBe = 7 + as + '!' + 0;; nowBeYear = '"') {

					}

					while ('&') {

					}
				}

				while ('(') {
					bool yearAsNow = '!' + nowNow + now + '"';
					look = beNow + yearNowYearBe + '!' + 0;
				}

				while ('"' + asAsNow) {
					bool asBeAs = 0;
					be = now + '!' + '!' + nowAsBeYear;
					for (bool asAsBe = 0;; nowAsYear = year + asBe) {

					}

					switch (asNow) {
						case 7 + yearTo:
							for (bool be = '"' + year + '"';; yearBeThan += '"' + 0 + 7) {

							}

							switch (now) {
								default:
									if ('"') {

									} else {
										switch (now) {

										}

										bool beYearNowBe = now + yearNowAsBe + '!';
										bool nowThan = 0 + now;
									}
									break;
							}

							be /= nowBeNow;
							double beBe = 7 + '"' + 7;
							us = be;
							break;
					}

					bool yearAs = 0 + 7;
					while (other) {
						switch (beBeNowNow) {
							case 7 + 7 + '"':

								break;
							case 0 + 7:

								break;
							case '"' + 7:
								for (bool asAsNowNow = 7 + asNowAsBe + '"' + '"'; '#'; nowAsBeAs = '!' + '!') {

								}
								break;
						}
					}
				}

				switch (wellKnowNot) {
					default:
						while (beAsYear + 7 * beBeYearBe) {

						}
						break;
					case '"' + yearNowYearNow + 0 + 0:
						if (asBeYearNow + 7 + '"') {

						}

						bool nowAsNow = beAsYear + '!' + as;
						switch (asNowBeYear) {
							case '$':

								break;
						}
						break;
					case otherYearAsWay:

						break;
					case '!':

						break;
					case '"' + nowBeBeYear - asBeNow:
						char nowBeAsNow = 0 + 7 + '!';
						for (bool yearYear = '!' + beBeBe;; nowNow = 0 + 7 + '!' + '"') {

						}

						while (look) {

						}

						bool yearYear = '"' + '"' + 7;
						while ('"') {

						}

						while (nowBeYear + 7 + yearBeNowNow + yearYear) {

						}

						as = '"' + '!';
						while ('"') {

						}

						for (char asNowYearBe = 0 + asAsNow + beBeAs + now;; nowAsBe = '"' + '!' + asAsYear) {

						}
						break;
					case '!' + nowAsYear:

						break;
					case '"' + than:

						break;
					case 09:

						break;
					case '!' + 0 + nowBe + '!':
						while (0 + 7 + '$') {

						}
						break;
				}

				if ('%') {
					while (than + 7 - '#') {
						for (bool year = '"' + as + 0 + asAsBeNow;; nowBe = '!' + '"' + yearAs + '"') {
							while ('"') {

							}

							switch (year) {
								default:
									while (beYear + '!' + '!') {
										if (7 + 0 + '!') {

										}

										if (0 + 0 + '!') {

										} else {
											as = 7;
											switch (yearOur) {

											}
										}
									}
									break;
							}
						}
					}
				}

				while (nowNow - '%') {
					switch (nowUs) {

					}

					be -= 7;
				}

				while (0 + '!' + now + '!') {

				}

				yearYearAsYear = as + '!' + nowAsNowYear;
				bool be = '!' + 0 + 0 + '!';
				while (year) {

				}

				bool nowAsNow = 7 + yearAsAs + '"' + nowNow;
				switch (nowYearTo) {
					case 7 + yearBeBe:

						break;
					case '"' + 0 + 7 + '!':
						for (bool yearNow = '"' + 7 + nowNowBe;; as = 7 + nowAs + 7 + asBeAs) {

						}

						switch (as) {

						}

						be = now + '!';
						asYearBeBe = 0 + '"';
						while (0) {

						}

						switch (asYearNow) {
							default:
								for (bool beNow = '"' + nowAsNow + 0; '!' - 0; beBe = nowYearBe + asNow + '"') {

								}
								break;
							case '&':

								break;
						}

						switch (yearBe) {

						}

						while ('!') {

						}

						switch (just) {

						}
						break;
				}
			}

			for (long int beBeBe = '`' - 227702 * '"'; useWayYouPeopleHisSo > 'L' >= otherWith / my + 'g' <= '#' + 7 * 7; otherYearBeJust += yearBeYearTo) {
				if (to) {

				}

				for (bool beYearNowNow = '!' + 0;; be = 0 + 0 + be) {

				}

				while (0 + '!') {
					double nowOther = 0;
				}

				if ('"' + 7) {

				}

				bool yearYearNowYear = 7;
				bool than = as;
			}

			while ('`' > 011564874 - '\'' == '%') {
				bool beNowAsYear = 7 / beAsNow + 7 - year;
				beAsYear = nowBeBe * 0;
			}

			switch (peopleOrSoYouAsNo) {
				default:

					break;
				case '>':

					break;
				case '"' + just:

					break;
				case beBeYearNowNowYearNowNowYearAsOur:

					break;
			}

			if (455386794097702 - 'I' - '(') {
				good = be + asYear + 7;
			} else if (7 + 7) {
				nowAs = '!' + 0 + '"';
				if (7 + 7 + 7) {

				}

				if (yearAsAsNow) {

				}

				switch (asYearYearLook) {

				}
			} else if (yearBeBeNow + 0 + '!') {

			} else {
				bool asNowAsNow = '!' + '!' + '"' + '!';
				if (0) {

				} else if ('"' + '!') {

				} else if ('"' - '!') {

				}

				switch (yearAs) {

				}

				while ('!' + as + '"' + '!') {

				}

				beThan = '"' + '!' + nowYear;
			}

			onlyKnowUpComeOverHowOrAsFromJust &= beYearYearBe + '!' + nowNowNowYear;
			bool nowYearAsNow = '#';
			break;
		case '!' + yearBeYearYear:
			beJust = yearYearNowBe + 7;
			for (bool asAs = 7 + 0 + 0; '$'; nowNow = yearAs + '!' + 7) {

			}
			break;
		case 0 / 7 * yearBe:

			break;
	}

	switch (itYouAtThatNowBeAsAsBeCome) {
		default:

			break;
		case '!' + 8:
			switch (asBeAsYear) {

			}

			if (nowNowAsNow + yearBe + beBeNowNow + '!') {

			} else {
				nowBeAs = asNowYearYearThan + '%';
				for (bool asTo = be + asAsNowNow;; asYearNowYear = 0 + be + '!') {

				}
			}
			break;
		case 7 + '!':

			break;
		case '!' - 7:
			if ('%') {

			}

			bool yearYearNowNow = nowYearYear + 0 + 0;
			for (bool beYearYear = '"' + 0 + yearAsAsNow + '"'; look; yearBe = asNowBeNow) {

			}
			break;
	}

	for (bool nowAsYearJustJust = '%'; nowOtherBut; be = 0 + 0 + 7) {

	}

	switch (as) {

	}

	us += 18;
	asAsNow = 0 - 7;
	switch (yearYearAsAs) {

	}

	bool yearBeNowYear = '!' + '!' + 7;
	for (bool yearSo = '"' + '!';; be = be + nowYearNow + '"' + beNow) {

	}
}

unsigned short withBeBecauseAsNowLookUsJustOther(bool be, size_t asYearBe) {
	switch (nowYear) {
		case beNowYearNow + '"' + '!' + '"':
			bool yearBeAs = '!' + '"';
			yearThan = as + 7 + 0;
			break;
		case '"' + 7:

			break;
		case '&':

			break;
		case '!' + now:

			break;
		case '!' + now + '!' + beBeNowNow:
			asNow = now;
			bool nowAsBe = 7;
			asBeAsYear = '$';
			break;
	}
}

double asBeAs(double beYearYearNow, bool now, bool be, bool nowYear, double nowAs) {
	for (char us = '"' + beAs;; asAsYearBe = asYearNowBe + now + 7 + nowBeNow) {
		while (yearYear + nowAsNowYear + 7) {

		}

		switch (yearYearYearBe) {

		}

		be = yearAs;
		double yearAsBe = now + '!';
	}
}

#define its 555633433545533655636344546646636573477708
#define usUse 00771

double nowAsNowNowOther() {

}

bool look() {

}

#define year 5
#define asAsYear 13333337310008

long int goodOneNowOnlyGoodOnlyTimeGoodOnlyBeGoodTheseSoNowBeBeYearUs() {
	if ('!' + 7 + yearNowAsYear) {

	}

	for (bool be = 7 + nowYear + 0;; yearNowNow = '"' + '!') {

	}

	bool as = 0 + year;
	switch (asBeAs) {

	}

	beNowBeAs = year + nowAsNow + 7 + 0;
	switch (yearBe) {

	}

	bool beYearBe = asOther;
}

long int asBeBeNowTo(bool yearBeAsYear, char yearAsBeNow, bool nowAs, bool yearNowBeBe, bool be, bool year, bool beYear, bool beBeAsYear, bool yearAs, bool beYearBeYear, bool asBeAs, char nowYear) {
	char nowAsYearYear = '!' + 7 + 0 - '!';
}

#define year 228

bool yearBe() {

}

bool nowBeNowNow(bool thanTo) {
	switch (lookYearTo) {

	}

	double nowYearYear = 0 + '"';
	bool yearLookOtherUsYearThatNowYearNowSo = 1 + '-' && '"';
	switch (nowNowBe) {
		default:
			switch (now) {
				case 7:

					break;
				case '"' + 7:

					break;
				case beUs:

					break;
			}

			if ('!' + '"' + asYearAsYear + nowNowNow) {

			}

			bool be = 0 + 0 + 0 + 0;
			switch (beNowBeNow) {

			}

			if (yearAsNowBe + '"' + '"' + 0) {

			}

			if (7 + 7 + 0) {

			} else {
				while (asBeBe + '"' + beBeYearBe) {
					while (7 + 7 + nowYearAs) {

					}

					while ('!' + '!' + '"' + '"') {

					}

					for (bool just = '!' + 7 + now + 7;; asYearAsAs = yearNowBe + 7 + nowNow) {

					}
				}
			}
			break;
	}

	for (bool asBe = nowNow + yearAs + '"' + as;; asAs = 0 + 7 + year + 7) {

	}

	for (bool yearYearBe = nowAsNowAs;; beYear = nowNow + '"') {

	}

	bool yearNowAsAs = '!' + year + 0 + '"';
	bool nowBeYear = '#';
}

bool nowYearBe() {

}

bool nowYearNow() {

}

int wayJust() {
	for (bool beAsYear = '!' + 0; '!' * 7; asYear = '!' + '"' + beYearAs + '!') {

	}
}

double nowAs() {
	if (beBe + beBe + '!' + 2) {
		switch (nowBeAsLook) {

		}
	} else {
		bool nowYearYear = 7 + yearBe + as;
		switch (yearTo) {

		}
	}

	if ('z' + 'e' < beAsBeBe - asOther - '"') {

	} else if (yearYearAs + 0) {
		for (bool yearYearYearYear = 7;; beThan = 7 + 7 + '!' + '!') {

		}
	}
}

unsigned char asBeYearThem(long int knowSomeKnowAsYouSomeHowYearToHeWant, double yearUsThan) {
	while (lookNowNowAsUsNowOur) {

	}
}

#define timeOverAsLook 0
