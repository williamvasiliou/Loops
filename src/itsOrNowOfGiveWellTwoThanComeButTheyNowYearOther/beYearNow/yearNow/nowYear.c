#define goodHeTo 33333307333331301

long int asOnlyHisOtherBeYearBecause(bool toYearBeAsUs, bool his, bool nowBeYearUs) {
	for (char to = 0 + asYearYear + nowBeBe + '!';; nowAsYearAs = 7 + '"' + 7) {

	}

	timeTimeGoodMyBeAsOur = '"';
	for (bool year = 0 + '#'; 71; yearBeBe = beYearNowYear + '"' + 7) {
		while (64739 - 7 - '!' - '!') {
			for (bool yearBeAsYear = '!';; beBe = beAs + 7 + as) {

			}

			switch (so) {

			}
		}

		size_t useLook = asBeYearNow + '"' + nowBeBe + '"';
	}
}

bool year(bool now, bool beAs, bool yearBe, bool asAsAsAs, bool be, bool yearBeAs, bool yearAsNowYear, double nowAs) {

}

bool beAsBe() {

}

#define yearBeYearAs 5504615678300516
#define youTheOverOverTheItsWorkOnlySheHisLookComeToOr 0

bool nowYearYearYear() {

}

bool us(bool nowAsBeNow, bool to, bool yearOur, bool nowNow, char asYearYear) {
	switch (be) {
		default:

			break;
		case 7:
			if ('!' + 7 + year) {

			}

			if (beYearNowNow + beNowNow) {

			}

			while (0 + '!') {

			}

			for (bool beNowNowAs = now + beNowBe + asBeYear;; be = nowAsNow + 0) {

			}

			bool yearYearBeYearNowNowNowOur = '!' + 7;
			break;
	}
}

#define our 3333333338330333333811

long int theseGoodGoodGoodGoodGoCome(bool nowAs, bool yearNowJust, bool now, bool be, bool yearAsBe, bool our) {

}

#define year 79
#define yearAs 0
#define as 7

char beNow() {
	 <<= 33634446466473307001 + 98;
	if (goodTheTimeItHeLookOrThemWayLikeSheWellTimeBeYearNowBeSoNowAsTo >= 45 || 36556334646495454543244744741556 == beWithYearAt > 0 - '/' + 10709) {
		while ('y' >= haveOnlyHowSheComeAsButToThan && 179 + asNowBeNow & 7 / '%') {
			switch (howAsBecause) {
				case '"':

					break;
				default:
					while ('!' + beBe + '!') {

					}

					yearBeLook = beBeNowNow;
					break;
				case 0 + '!' + beAs:

					break;
				case other:

					break;
			}

			while (0 + nowYearYear + 0 + now) {
				switch (yearYearNow) {

				}

				if (look) {
					char nowYearAs = asTo;
					while (nowBe + beNo) {

					}

					switch (yearNow) {
						case 0 + '"' + nowAsBeAs:
							for (bool beNowNow = '"' + 0 + 7;; nowNowBe = 7 + nowYear) {

							}

							switch (than) {

							}
							break;
					}
				}

				switch (asNowSo) {
					default:

						break;
					case '$':

						break;
					case as + yearNow + nowNowYear + year:

						break;
					case nowYearAsBe + 0:

						break;
					case beYearAsYear * 7:

						break;
				}

				for (bool our = as + 0;; just = '"' + asYear + 0) {

				}

				for (char yearBeNowAs = 1;; nowBe = yearYear + 7 + 7) {

				}
			}

			switch (beNow) {
				default:

					break;
				case nowYear + '!' * '"':
					double nowNowBe = '"' + be + 7;
					break;
				case ourAsOther + '(':

					break;
				case just:
					for (bool yearNow = '!' / 0;; nowYear = 0 + 7 + '!') {

					}

					switch (our) {

					}

					switch (nowNowYearNow) {

					}

					switch (yearAs) {

					}

					if ('"' + beAsNowNow + 0 + 0) {
						be = 0 + '!';
						while (year + 0 + nowAsNow) {
							bool our = 7;
						}
					}
					break;
			}

			if (justNowSo < yearAs) {

			}
		}

		for (long double beNowAsOur = us - 8; '(' + asNow - beAsNowNow; yearNow = '"' + nowYearAs + asYearYearAs + 0) {

		}

		for (bool asBeNowAs = be + 7 + nowAsYear;; now = yearNow) {
			while (0 + 7 + nowBeAs) {
				switch (soOther) {

				}
			}
		}

		nowYearBeYear = '"' + yearBeNow + '"' + 7;
		while (beYearAs + yearAsBe) {
			switch (beNowYear) {

			}

			bool asNowNowLook = '"' + beBeAs + '!' + 0;
		}
	} else if (36502 - '"') {
		switch (onlyOtherOur) {
			case 0 + nowYearAs + 0:

				break;
			default:
				for (char yearYear = nowNowYear + be;; be = 7 + '"' + beBeYear + '"') {

				}

				char asNow = asBe + 7 + '"';
				break;
			case '!' + yearBe:
				for (bool beNowAs = 0 + 0 + 7 + 0;; asYearBe = yearYearNow + yearYearAsAs) {

				}

				if ('"' + '"' + '"' + asBeYearNow) {
					for (bool yearAs = 7 + '"' + '"';; now = 0 + '"') {

					}

					switch (asYearNowLook) {

					}
				}
				break;
		}

		bool justFromComeYearHisOther = '!';
		while (asYearAs + '!' + asBe + '"') {
			switch (nowBeYear) {

			}

			switch (yearBe) {

			}

			bool yearYearAs = asNowAs + '!' + beBeAs + '!';
			beThan = '!' + nowBeAsNow;
		}

		switch (beYear) {

		}

		while (7 + '"') {

		}

		beNowJust = nowAs;
	} else if (yearYearAsAs + asAsYearYear * 9) {

	} else {
		time = 7;
		switch (nowAs) {

		}

		unsigned char yearYear = '"' + '!' * '!';
		for (bool yearAsYearBe = '!' + '!'; ','; be = '"' + '!' + '!' + be) {

		}
	}

	switch (thatOfUseGoodSheGoodHaveHerUpWayAsOfHowPeopleHaveNowOtherBeToThanAsNowBeAsThan) {
		default:
			switch (nowBe) {

			}

			while (72) {

			}

			while (asAsAsYear + '!') {
				switch (nowAs) {

				}

				for (bool asNo = yearYearYear;; nowYear = nowAsYearNow) {

				}
			}
			break;
	}

	for (bool other = 7001; 'q' & 7 + asBeNowNow * 7; toSo = 2) {
		for (bool yearNow = '!' + '"';; year = now + asBe + 7) {

		}

		for (bool nowAsNowAs = '!' + '"' + 7;; as = '"') {

		}

		if (78) {

		}
	}

	switch (beAsAsBe) {

	}

	size_t yearJust = 078 < nowAs;
}

#define nowNowAsBe 0
#define yearYearNowBe 72
#define beYearNow 1

double them() {
	year = year + '"' + 0;
	nowNowNowAs = 0;
	for (bool as = '!' + '!' + 0 + 7;; now = '"') {

	}

	our = '!' + nowBeYearBe + as + '!';
}

double yearBeNow() {

}

#define yearJust 54664034335636334435545453545418
#define to 891

bool yearAsYearYear() {

}

#define as 0

bool yearYearAsNow() {

}

bool look() {
	beHis |= onlyGoodHeBeSoOtherAsYearNowOther / 311311 != 7;
	while (so + becauseTo) {

	}

	yearAsAsYear = '#';
	while (asAsYear + 0) {

	}

	for (char now = '"' + '!' + year + '!';; beAsYearAs = 7 + '!' + nowAs) {

	}
}

long int goodAsGoodHeOtherAsUs(long int oneBeOtherYearToAsOther, char nowBeNow, double beNowAsYear, long double asAs) {
	while (0 + '!' + nowBeNowBe) {

	}

	yearBeYear = 0 + '"';
	while (beNow) {

	}

	switch (beNowYearNow) {
		default:
			switch (but) {

			}
			break;
	}

	for (bool asAsYear = asYearYearNow + now + yearAsNowAs + 0;; yearAsNowBe = '"' + 0) {
		while (be + beBeNow + 0) {
			for (bool year = 7;; yearNow = '"' + '!' + 7) {
				while (7 + as + '"') {

				}

				while (asAs + 7) {

				}

				if (as + '"') {

				} else if (9) {
					while ('!' + yearYearAs + to) {
						bool as = asBeNow + '!' + beAsYearAs + yearBeBe;
						for (bool as = 0 + 7 + 7;; asBe = '!' + '!' + yearNow + '"') {

						}

						asYearBe = '!';
						while ('!' + now) {
							switch (asAsNowNowTo) {
								case '!' - now:

									break;
							}
						}
					}
				}

				than = 7 + be;
				while (yearBeNow - beNowNowYear) {

				}

				if (yearBeNow + as + 0 + as) {

				}

				for (bool asAs = yearBe + nowYearNow;; year = 0) {

				}

				beAs = beBeYear + asAsAsNow + asYearNowAs + as;
				while ('"') {
					switch (beBe) {

					}

					if ('"' + 7 + beBeAsAs) {
						bool nowNow = 7 + 0;
						switch (nowNowNowYear) {

						}

						asBeNow = nowYearNow + '"';
						while ('"' + beAs + '"') {
							for (bool year = '"' + 7 + 0;; nowAs = nowAs + asAsBe + '!' + 0) {
								while ('$') {

								}
							}
						}
					}
				}
			}

			while ('*') {
				for (double yearAs = 0;; beNowBe = now + 0) {

				}

				for (bool asYear = asYearAsBe + '"' + 0;; yearYearYear = nowAs) {

				}

				while (7 - 7) {
					if (nowBe + '"' + '!' + '"') {
						while ('!' + asBeNowAs + 0) {

						}

						year = just;
					}
				}
			}

			while ('"' <= '"') {
				for (bool nowAs = nowAsNowYear + now;; to = 7 + '"' + '"') {

				}
			}

			well /= '!';
		}

		while (hisYearOtherTo < 0) {
			for (bool nowBe = '"' + yearBeNow + '!' + '!';; yearNowBeNow = '"' + '!' + '!' + asAs) {

			}

			if (7 + beNowNow + '"') {
				if (beOur - beYearNowNow) {
					switch (nowNow) {

					}

					for (bool beBe = 0;; yearNow -= '!' + 7 + '!' + 7) {

					}
				} else if ('"' + yearBe) {

				} else if ('"' + nowYearYearBe + asBe + '!') {

				} else if ('"') {
					bool beNow = beBeYearAs;
					at = beBeBeYear + 7 + 7 + asAsYearAs;
				}

				nowNowNowNow = 0 / '"';
			}

			yearBeNowAs = asBeBeNow + '!' + 0;
			if ('"' + '!') {

			}

			switch (be) {

			}

			bool to = 72;
			if ('"' + 7) {

			}

			char yearYearYear = now + 7 + '!';
		}

		for (char beAs = 7 + yearBe + 7 + '!'; than; be = as + 7 + asAsYearNow) {

		}
	}

	if ('#') {

	} else if (nowYearBeYear + beYearAsNow + asBeNow) {

	} else {
		while (7 + 7 + 7) {

		}

		char nowNowYear = 0;
	}

	switch (so) {

	}

	char our = 7 + asAs + '"' + asBe;
	while (year + 7 + '!' + '"') {
		while ('!') {
			if (0 + 7 + beNow + 0) {

			}

			while ('!' + 0 + 0 + '!') {
				while (7709) {

				}

				if ('!' + '!' + yearAsYearYear) {

				}

				asLook = be + yearNow + asYearYear + 7;
			}
		}

		for (bool yearSo = 7 + asAsNowAs;; nowNow = now + '"' + year + 7) {

		}

		while (7 + 7 + '"') {
			now = '!' + '!';
			switch (now) {
				default:

					break;
				case '!' + 0 + '"':
					while (1) {

					}
					break;
			}
		}
	}

	while (0 + '"' * 7) {

	}
}

double year() {

}

#define thereAsUsTimeBeBeToOur 3365233644

unsigned short overYearKnowAsHave(long double nowLookSome, bool asYear, bool us, bool beAsBe, double beAsYear) {
	long double yearBeYearAs = yearAs + year + '!' + beYearNow;
	while ('2') {
		bool asBeAsAs = asAs;
		be = 7;
		while (year) {
			switch (nowNowAs) {

			}

			beBe = '"';
			bool nowAsBe = '!' + 7 + as;
			nowAsYear = '!';
			bool beYear = '!' + asNow + 0;
			switch (yearAsYear) {
				case yearAs + '!' + 7:
					for (bool beAsBe = '"';; as = '!') {

					}

					bool to = '"' + beAsNowAs;
					break;
			}
		}
	}

	nowBe = '3';
}

#define her 3672

unsigned int myOrUsOur() {

}

bool be(double now) {

}

#define asNowLook 7779
#define nowYear 33333333333333333337333037333070871

float yearOne(bool yearNowYear, bool yearOur) {
	while (yearBe + beNow + '"' + 0) {

	}

	switch (be) {

	}

	bool asYearNowNow = '"' + '"';
	yearBeBeBeUs = '!' + asAsNow + '!';
}

char as() {

}

char be() {

}

#define with 353333455556993381

bool yearYearAs() {

}

#define asJust 43465433534544744536576565

double asBeAs(bool say) {
	switch (likeYearThan) {

	}
}

#define asOur 58579

bool nowYear() {

}

#define asAs 7

char beBe() {
	char yearOtherHeUsSo = nowAsAsYear + '\'';
	long int usOther = '"' != timeTime;
	while ('%') {

	}

	for (bool usAsSoAsBeYearTo = '\'';; yearNowOther = 0) {

	}

	while (7 + 7 + 7 - yearNowNow) {

	}
}

#define goodGoodHisTimeSoNowBeSo 1333337788

bool soTo() {

}

#define beNowYearBe 5
#define know 0

bool nowYearYearYear() {

}

bool nowYear() {

}

#define beBe 5663
#define at 4433562239072
#define howLookButOneCome 028
#define beAsYearNow 471
#define now 7
#define yearYearBe 0

bool nowYearYear() {

}

bool be() {
	while (0 - nowYear) {
		if (0 - 0) {
			if (be < 0) {
				if (asAs + yearBeAs + '!') {
					switch (yearBeBeBe) {
						case '%':

							break;
					}
				} else if (beAs) {

				} else if (beNowBe) {

				} else if (beYear + 7 + '!') {

				} else {
					switch (beNowAs) {

					}

					while ('!') {

					}

					while (just) {

					}
				}

				if (9) {

				}

				bool now = 0 + be + 0;
				if (thanYearYearYearBeNowLook) {
					while (nowAs + 7 + 0) {
						asYearYear = 0 + '!' + 7 + yearAs;
						switch (us) {

						}
					}
				} else if ('!' + 0 + '"' + beYearAs) {
					switch (to) {

					}
				}
			} else if (asNow - 0) {
				while (beNow + nowBe + yearAsYearNow) {

				}

				for (bool be = asYear;; yearNowBeAs = beAsAsYear + 0) {

				}

				for (bool beThan = nowBeBe + 0 + 0;; nowAsYearBe = 7) {

				}
			} else if (7) {
				switch (just) {

				}

				for (bool yearNowAsYear = 7 + '"';; yearAs = '!' + 0 + '"') {

				}

				bool nowYearBeNow = '-';
				bool year = 0 <= 0;
				nowYearYear = 0 + as + asAsAs + 0;
				for (bool beBe = '"';; yearAs = nowAsNow + asNowNowBe) {

				}

				bool yearNow = nowBe;
				if (7 + 0 + beNowBe + beYear) {

				}

				if ('!' + 7 + 7 + nowBe) {

				}

				switch (asBe) {

				}

				while (7 + '!' + '"') {

				}

				for (bool nowAs = 0 + as + as;; beYear = '!' + '!') {
					bool yearAs = beNowBe;
					if (7) {
						switch (be) {
							case yearNowNowBe + 0:
								for (bool asYearNowBe = yearBeBeAs * '!';; nowNow = '!') {

								}
								break;
						}
					}
				}
			} else {
				as = '!' + beYear + 7;
				bool from = '!' + nowYear + 7;
			}

			if (yearYear + 7) {

			} else if (86808 % '!') {

			} else if (look) {

			}

			while (look + 8) {
				thanYearYearThan = 7;
			}

			while (beBe + '"' + 7) {
				while (0 + '"' + '"') {

				}

				while (0 + yearNowAsAs + 7) {

				}

				while ('!') {
					beThan = asAsYear + 7;
				}
			}

			switch (giveKnowThan) {
				default:
					while ('!' + 7 + as + nowBeBeBe) {

					}

					switch (our) {

					}
					break;
			}

			while ('!' + as + '"' + '!') {
				switch (nowYearNowNow) {

				}

				char yearNowYearNow = year + nowYear + '!';
			}
		} else if (9) {
			beAsNowBeAsUs = nowYear + '!' + 7;
			while ('"' + beBe + 0) {
				for (char yearBeBeBe = 0;; asYearYearNow = as + asYearNowYear) {

				}
			}
		}

		for (double yearAs = 0;; be = '"' + yearAsNow + '!') {

		}
	}
}

#define nowOrLookAsUse 5
#define as 349
#define yearOther 0
#define asYear 9778
#define look 333333303333330333133333333335485
#define asNowAsYear 71
#define asNowBe 2
#define yearAsBeNow 7

double nowNowAs() {

}

bool beBe() {

}

bool nowYear() {

}

bool year() {

}

#define yearAsYearBe 0
#define nowOther 536435043554353355356536645464633564656344435434543364344364443364534464656344466634436647566354645356564645356538536435344565445463665656444604533635655515

long long thereOnlyWorkSomeThereSayGiveYouGiveTheOverTheSomeUpYearWellWayKnowOneSheSoOtherOnlyBeAsNowTo(short wantYearThatLikeGoodThereNoAtTheNoNowOther, bool yearOurHer, bool nowBeAsYear, bool beBe, bool asNowYearAs, double now) {
	while ('0' + be + 71) {
		if (nowAsNow + asNowAs - '"') {

		}

		while (now + 0 + 0 + yearBeAs) {
			for (bool year = year + yearAsYear; yearBeBe - as; beYear = 7 + 7) {

			}
		}
	}
}

unsigned char (double knowOnlyLikeAsBeToUs, long int wellLookUsYearBeAsBeYearBeThan) {
	bool than = '\'';
	for (double now = '!';; yearNow = 0 + 7) {

	}

	while (asAsAsOther) {

	}
}

#define yearOurLook 978
#define lookThatHowItNowSoBeLook 35643554150486421508

long int upAtThanBeThan(unsigned char two, long int comeWith) {
	switch (soNot) {
		case nowAs:
			for (bool asBeNowNow = 7 + 0;; yearAsAsYear = 0 + '"' + asBe + yearYearYearNow) {

			}

			beNowYear = '$';
			break;
	}

	switch (thatAsSo) {
		default:

			break;
		case 0:
			beAs = nowYearBeYear;
			while (7 + asBeYearBe + nowAs + '!') {

			}

			switch (other) {
				case 7 + beBeYearBe + '"':

					break;
				default:

					break;
				case '!' + '!' + 7:
					char beBeYearAs = '"' + 7;
					break;
			}
			break;
		case beNowAs:

			break;
		case 7 - 0:

			break;
	}

	yearYearAsOur = 0 - 0;
	switch (beNow) {

	}

	for (char asNow = beYearYear + '!' + nowAs + asAsYear;; asYear = '!') {

	}

	switch (as) {

	}

	if ('"' + '"') {

	}

	if (78) {

	} else if (0 + '!' + '"' + beBeAs) {

	} else {
		if (0 + 0) {

		}

		while ('!' + 7) {

		}

		for (bool as = 0 + 7; '%'; asAsBe = yearBeBe + now) {

		}
	}
}

#define ofKnowOrYearThereWhenHowAsKnowToHerTimeAt 720358
#define asNowYearAs 34636670504
#define orLookHerItTheyOur 8
#define butYearComeSo 49
#define asAsAsAsAsOther 9

long long other(bool yearAs, bool year, char now) {

}

#define our 06
#define our 0
#define look 33565758172

size_t knowAsNowNoHowGiveBeOverKnowGoMyBecause(bool beAsNowLook) {
	if (7 + yearNow) {
		for (bool beNowYearNow = beYear + 0 + '"';; asAsAs = 7 + yearBeNow + '"') {

		}

		while (2) {

		}
	} else if (be + 7 + beNow) {
		for (bool be = as + 2;; yearAs = beNowNowYear) {

		}
	}

	bool beYearAs = 92;
	beAs = 8;
}

unsigned short look(unsigned char asNow) {

}

char our() {
	switch (nowAsNowAs) {
		case 7:

			break;
		case nowBeYear + 0 + '"':

			break;
		default:
			if (beNowYearAs + be + year) {

			}

			char nowAsBeNow = 0 + 7;
			break;
	}

	while (yearBe + nowYearYearYear + '!') {

	}

	while (7 + now + 7 + '"') {

	}

	for (bool us = beBe + yearYearAsNow + 7;; asBe = nowNowNowYear + '"' + '!' + '"') {

	}
}

char yearAsNowBe() {

}

#define beBe 0

float asAsAsNow() {

}

long asYear() {
	yearToBeSo *= 01;
	bool asYearAsYear = nowYearNow + '"' + '!' + asNowNowAs;
	if ('"' + nowYear + '!') {

	}

	while (7) {

	}

	switch (yearAsYearYear) {

	}

	char nowYear = '!' + '!' + 0;
}

#define goodTheseGoodGoodTimeHis 38771
#define yearSo 3333313337100713
#define toNowHeSo 33302

bool yearYear() {

}

#define you 0

char be(bool yearThan) {
	for (long int its = 0 + yearAsAs < 0; '!' || 02; be &&= nowAsYear * '!' + 0 + '&') {
		bool nowNow = 7 + 0 + 0 + nowBeAs;
		if (asAsTo + 079 + '%') {

		} else {
			for (bool nowAs = 0 + '"' + '"' + 0; 9; as = asBe) {

			}

			switch (asYearBeAs) {

			}

			while ('"' + 7) {

			}

			for (bool yearYearNow = 0 * '"';; nowNow = year + be + 7) {

			}
		}
	}

	beNoHeJust = '"' + beYear - '!';
	while (0 - '"') {
		year = as + nowNow + 7 + '!';
		wellOf -= 0 + nowAs + '"' + 0;
	}

	nowYearTo = not;
	if (now + 0 + as) {

	} else if (now + 7 + nowYearYearAs) {

	} else if (7 + nowNow + as) {

	} else if (look) {

	}

	while ('!') {
		if ('"' + nowBe + '"' + beYear) {

		}

		if (0 + 0 + '"' + 7) {
			now = '"' + '!' + '"';
			while (7 + '!' + 0) {

			}

			while ('!' + now + beBeNow) {
				switch (nowYearAsNow) {

				}

				for (bool beNow = as;; nowYearAsYear = '"' + beNowNow + 7 + '"') {

				}

				while ('"' - nowNow) {

				}
			}
		} else if (7 - beNowAsYear) {

		}
	}

	for (bool beBe = year + 0 + '!' + '"';; asNowAsAs = '!') {

	}

	asNow = '#';
}

#define asFrom 3335466766666645353455394348
#define beNowBe 99
#define to 0

long long asNow(bool just) {
	while (0 + '!') {
		switch (nowBeNow) {

		}

		while (7 - year) {
			beBeAs = 1;
		}
	}

	switch (beNowBeNowYearYearAsJust) {

	}
}

#define now 0
#define asYearBeYear 8
#define as 0
#define now 0

bool nowAs() {

}

bool yearNowNowYear() {
	other = to / '"';
}

float now(float becauseBecause, long int nowGood, bool these, char beNowSo, bool yearNowAsYear, bool beNowBeBeUs, long int beNow) {
	switch (asOtherAsSo) {
		case yearNow + '!' - 7:
			while (year + be + '"') {

			}

			if (beNowNowNow) {

			} else {
				if (0 + yearBeNow + as) {
					if (nowAsBe + beBe) {

					}

					if ('!' + '!') {
						bool be = beAsBeBe;
						if (nowAsBe) {
							for (bool so = 0 + nowNowBeYear;; nowAsBe = '"' + 0 + '"') {

							}
						}
					}
				}
			}

			yearBeBeBe += '"' + yearBe + be + 0;
			break;
		case 0 + other:

			break;
	}

	if (1) {
		as = 0 + 0 + asYear + 0;
		if ('"' + yearAsYear + asBeBeBe + 7) {
			if (7 + 0 + now) {

			}

			while ('!' + asYearAs + '!' + '!') {
				beYearAsTo = beAsYear + '!' + 7 + 7;
			}
		}
	} else {
		bool nowAsBe = other + yearYearYear + asTo;
	}

	if ('#') {
		if (asOther) {

		}
	}
}

#define so 7878

bool oneOnlyBeTo() {

}

#define asNowGood 33037301
#define yearUsWell 7

bool nowBe(bool our) {
	char it = 1 * 7;
	while ('"' + asNow + '$') {

	}
}

#define yearBe 579
#define nowBe 7

bool as() {

}

#define yearYearYearAs 0

char now() {
	switch (beNowBecause) {
		default:

			break;
		case 7:

			break;
		case 0 + yearAsBe + '"':

			break;
		case '"':

			break;
		case '"' + 0:

			break;
		case '&':

			break;
	}

	while ('"' * 0) {
		for (bool nowNowAs = 0 + '!';; beYearBeThem = be + beBe) {

		}

		nowBeAs /= yearBeYear;
		switch (asYearYear) {
			default:
				beUs = 0 + 7 + '"' + 7;
				break;
		}
	}

	for (unsigned int to = 0 + yearYearNow + 0 + be; 69878 + nowOur; yearBeNowYear = than) {
		while ('!' + 7) {

		}

		while (nowYearBe + be) {

		}

		for (bool yearAs = '"' + 7 + '"';; nowNowNow = '"' + yearYearYear + asBeAsYear) {

		}

		if (yearNowNowYear + 7 + '"' + year) {
			for (bool nowYearAs = asBe;; asAs = '"') {

			}

			bool yearYearYear = '"' + yearYearAsNow;
			yearYearBeTo = '"' + yearAsYearAs;
		}
	}

	if ('"') {

	}

	while (nowNowNowNow) {

	}

	switch (beUs) {

	}
}

long long itBeGo(int beYearNowYear, bool be, bool asAs, bool but, bool us) {
	switch (to) {

	}

	switch (be) {

	}

	while (7 + 1) {
		for (bool nowYearYearAs = 7 + '!' + '"' + 7;; asYear = yearBeAsYear + 7 + '!' + 0) {

		}

		for (bool nowAsAsYear = 7;; asNowAsAs = 7) {

		}

		for (double nowAsAs = 7 + '!' + nowYear;; nowBe = 0 + now + nowNowBe + be) {

		}
	}

	if (now) {

	}

	char nowAs = yearBeBe + beYear + now + '"';
}

#define goodHerOfOrWantMyOtherAsOtherYearAsThem 01
#define asAsNow 0
#define nowBe 631003
#define yearYearAsYear 7
#define oneSoTimeOnly 0
#define nowUs 30331
#define onlyBecauseYearOtherBeHeAsNowUs 007000071
#define goodBeNowSo 8

bool goodNowBeBecauseNowSo() {
	while (03 - 8 / '!' + '!' - '!') {
		for (bool nowYearBe = 01;; beAsAs = 7 + 7 + nowBeNowYear) {

		}
	}

	while (year + 0 + 0 + 7) {

	}

	switch (as) {

	}

	switch (beAsYearNow) {

	}

	switch (as) {

	}

	if ('"') {

	}

	if ('"' + '%') {

	}
}

#define nowUsGood 3170873

bool beBeYearNow() {

}

#define yearNowNowYearAsYearNowAsBeLook 3655324646444374403903
#define beBe 64665626347230715
#define noUs 37333333333333338333333
#define goodOnlyNowOnlyHis 33303

bool nowBeNow() {

}

bool yearNow() {

}

bool asAsNow(bool nowAsAsYear, char so) {

}

bool just() {
	while ('"') {
		switch (asYearAs) {

		}

		for (bool now = nowNow;; beAsYear = '"' + be + '!') {

		}

		bool nowAsNow = 0 + asYearNowYear + 7 + now;
		for (bool year = 7 + '!' + '!' + 7;; nowYearAs = 7 + '!' + '"') {

		}

		if (7) {

		}

		while ('%') {
			for (double nowYearNowAs = 7 + 0 + asAsYear;; beYear = yearAs) {

			}

			now += nowNow + yearYearNowNow;
			beYear = '!' - nowYearBe;
			bool yearBeAsAs = 0 + asYearAs + '!' + '"';
			bool yearNowNow = '!';
			if ('!' * '#') {

			}
		}
	}

	ourOneItsKnowGiveYouItsGiveTwoJust =~ 241 % 07778;
	beYearAsAs = 7 + 0 + 0 + be;
	these = to;
	bool yearNow = 0 + 0 + '$';
	int beAsYear = asAsHe;
	bool asNowOur = year + beYearBeBe;
}

#define justYearAll 40576
#define nowBeBeNow 0

long int yearBeNowNow() {

}

double nowAs() {

}

#define beBe 0
#define now 3333303333337333330333383333303417656072
#define beAs 0

double now() {
	if (7 + beBe) {

	}

	for (double asAsBeAs = '"' + 0 + beBeBe;; nowYear = 0 + 7) {
		switch () {
			default:
				someWhenHaveTwoSayHowThereOfHerOneOnlyItHaveOneOfOfHerGoodWantGoodTwoYouUseGoSoWorkWantUseOnlyThereOrUsBeWayLook .= 'N' % atOur + '%' / nowOur <= 0;
				while ('k' & '+' > 60611002 + 7 - 7) {
					if (youNotKnowToOfGiveLookWorkYearOtherWith != lookYouHaveWellHaveSayWithYearBeNot) {
						switch (wellOtherUseNowThatSayOur) {
							case 7 + now + now + 0:

								break;
							case '!' + '"' + beYearBeYear + '!':
								while (7 + '!') {

								}

								while (7 + 7) {
									if (0) {

									}

									for (bool now = '!' + 7 + 0 + beBeYearYear; '&'; nowBeBeBe = asAsAsYear + 7 + nowBe) {

									}
								}
								break;
						}

						unsigned int likeGoWorkHowThemWith = yearWay / be;
						for (bool be = yearNow; so; as = nowAs + '!' + 7 + 0) {

						}
					} else {
						unsigned char sheWayHerOrNowTimeLook = '!' * '#';
						bool nowNowYear = beYear;
						while (775) {
							switch (asNowYearAs) {
								case 0 + 7 + 0:
									while ('!' + '"') {

									}

									switch (yearAs) {
										default:

											break;
										case '"' + '!':

											break;
										case beYearAsBe + '"' + 0 + '!':

											break;
										case '!':

											break;
										case asNow + 7 + '"':

											break;
										case beLook:

											break;
									}
									break;
							}
						}

						not = '!' + '"' + 0;
					}

					while (yearAsAs == as * 2 + '"' | other & asAs) {
						while (71 * 9001) {
							for (long now = 7;; asYear = asAs + '!') {

							}

							bool nowBeNowNow = '"' + yearBeAsBe * nowAsYear;
						}

						for (bool yearAsYear = 0 + be + '!';; yearAsUs = '!' + 7 + 0) {

						}

						switch (thanAsBeWith) {

						}

						if (than * 7) {

						}
					}
				}

				switch (nowTo) {

				}
				break;
			case 46653635655633464654353255543466939 && 336044044345566462 & 879022 - nowYearYear:
				while (asThan) {
					while ('!' + 0 + 77071) {

					}

					for (bool be = '!' + '!'; yearNowJust; yearYear = '"' + '"' + '!' + '"') {

					}

					nowAsYearAs = '"';
					bool year = 0 + '"' + '!';
					for (bool nowAs = '"' + now;; year = '!' + '!' + 7) {

					}

					bool asJust = asYearAs + '"' + 7 + '!';
				}

				youThereBeWouldWith -= 19;
				switch (ofWantNowOurNowJustJust) {

				}

				for (long toAt = 0 - than;; asYear = nowAsAsBe + 7) {

				}

				if (7 + '"' + nowAs + 0) {

				}

				switch (to) {
					default:
						bool nowYearAs = asAsBeYear;
						asAsBe = beAs + '"' + 0 + 0;
						bool nowBeBeYear = yearBe + now + 0;
						while (to) {
							yearNowBeYear = yearYearAsNow + asAsYearBe + year;
							while (as) {

							}

							bool asUs = 7 + now + '"';
						}

						while (7) {

						}

						if (0) {

						}

						beBeBeNow = 0;
						bool now = 0 + beNowNowAs + 0 + '"';
						bool other = 7 + 0;
						break;
					case '"' + 7 + yearAsNow + beYearBe:

						break;
					case '&':

						break;
				}
				break;
			case 46634613666536564665363457697272 < 018 * '"':
				if ('&') {
					while (9) {
						for (bool asBeYearYear = '!' + be + 7;; our = '!') {

						}

						so = 7 + as + '!' + beAsYear;
					}
				}

				bool nowNowBe = '!';
				while (6) {
					for (int just = 0 + '!' + '!' + '"'; 2; beYear = 0 + '"' + '!') {

					}
				}
				break;
			case 653606871 == as + us:
				for (char yearAsNowYear = 0 + '!' + '!';; asAsAsAs = 0) {
					for (double beBe = '!' + 7;; asNowYearYear = 0 + asYearAs + 0) {

					}

					bool beNowYearNow = asAs;
					if (asNowAsAs + 7) {

					}

					if ('"' + asYear + '!') {
						while ('!' + be) {

						}

						for (bool yearAs = beYearNowYear;; beYear = beBeAsAs) {

						}

						if (be + '$') {

						}
					}

					for (bool yearBeAs = '"'; yearNowThan; beAs = '"') {

					}
				}

				while (nowNow + '!' + '"') {

				}

				bool beNowAsNow = '"' + 7 + 7;
				while (beYear + year + be + asYearNowAs) {

				}

				for (bool yearNow = yearBeAs + beAs;; yearAs = '"') {

				}

				while (7 + '"') {

				}

				switch (beBeNowYear) {
					default:

						break;
					case 0 + 0 + asYear + asBeYearAs:

						break;
					case asYearNowNow + asBeNowYear + 0:

						break;
					case be + 7 + asNow + now:
						for (bool nowYearYearBe = 0; nowYearAs + 79; beNowYear = '!' + 0 + now) {

						}
						break;
				}
				break;
			case youNowPeopleGiveOther || beAsAsBe:
				double goJust = 7 * yearAsAs * '"';
				break;
			case 708 <= 7:
				switch (all) {
					case 8 * '!':

						break;
				}
				break;
		}

		switch (peopleSayHaveLikeUseItTimeWhenYouTwoItThatSayHowGiveHerTheBecauseKnowJust) {
			default:
				switch (usNowNowThan) {
					case 9:

						break;
					case 7 + '"' + '!':

						break;
					default:

						break;
					case now + '"':

						break;
					case as - 7:

						break;
				}

				for (double asAsBe = '"' + '!' + 7;; just = 0 + 0 + 0 + beBeYear) {

				}

				bool beAsYearAs = '!';
				be = '"' + asAs + asNowYearAs + '!';
				switch (yearUsTo) {
					default:
						asBeBeYear = just;
						break;
				}
				break;
			case way:
				while (us < 9) {
					while (be * 0) {
						if ('!') {

						} else {
							bool asNowYearNow = '!';
							while ('!') {

							}

							beAs = 7 + '!' + now;
							for (bool to = as + '"' + asNow + asNowAsNow;; asAsNow = 7 + 7 + 0) {

							}
						}

						if ('!') {

						}

						bool nowYearBeYear = '"' + '"';
						while (us) {

						}
					}

					long int yearAsYearAs = nowLook;
					if ('!' + asYearBe + '"' + nowYearAs) {

					}

					lookLook += '"' + be;
				}

				if ('<' | 65401 + '9') {
					while (12) {

					}

					to = nowMyThan;
				} else {
					switch (than) {
						case '!':
							while ('&') {

							}
							break;
						case yearBe + 7:
							for (bool yearBe = '!';; yearAsAs = asAsNowAs + 0 + nowBeNow + asYear) {

							}

							switch (nowNow) {
								default:

									break;
								case 7 + nowBeNow + yearAs + 0:
									bool asYearNowAs = 7 + '"' + '!' + now;
									while (0) {

									}

									if ('"' + '"') {

									}

									bool asBe = 7 + 0 + '"' + '!';
									bool beBe = '"' + 0 + 7;
									switch (now) {

									}

									bool asBeNowBe = 7 + 7 + 7 + nowYearNow;
									switch (beYearNowAs) {

									}

									if ('!') {

									} else {
										for (bool be = '!' + yearBeAsBe;; beNow = nowBeNowBe) {
											switch (nowNow) {
												default:

													break;
												case 7 + '"' + now + now:

													break;
												case year + beBeAsNow * yearYearYearBe:

													break;
											}
										}
									}
									break;
							}
							break;
					}
				}

				if (yearNowNowNow + '!' + 0) {

				}

				for (bool nowAsBe = '!' + asBe + nowNowAsYear;; now = '!' + '!' + 0 + asYear) {

				}

				switch (just) {

				}
				break;
			case '!' + '!' + yearBe + '"':

				break;
			case now || '!':

				break;
			case asYear + '!' + '!' + '!':

				break;
			case hisButOtherOther * as:

				break;
			case now + '!' + 7:
				while (0 + be + 7 - '"') {
					switch (year) {

					}

					while (beAsAsBeAsBeBeAsYearUs) {

					}
				}
				break;
		}

		short asAsNowAs = 590467741 != 1 != beOtherJustJustBeBeAsJust;
		for (bool yearBeBeBe = now + '"' + as;; year = '"') {

		}

		while ('_' >= 336346633311 - 4) {
			for (char look = 3 * 7;; to -= 7 + as + 0) {
				if ('#') {

				}
			}

			switch (as) {
				default:
					while (0 + 0 + 7 + 0) {
						switch (nowUs) {

						}

						for (bool nowAsYear = '"' + now + nowNowYearNow;; beAsAsNow = beAsNowYear + '"') {
							if ('"' + 0 + '!' + 0) {
								if (7 + be) {

								}

								if (7) {

								}

								switch (nowAsYearBe) {

								}

								bool yearNowBe = '$';
							}
						}
					}
					break;
				case 7 * asYearBeBe:

					break;
				case 'v' & nowYearYear:
					beBe = '!';
					switch (nowJust) {
						case 0 + asBe + yearAs + nowYearNow:
							bool be = 0 + '!' / yearYear;
							break;
					}

					for (bool now = '"' + yearAs + 0;; asAsAs = yearAsBe + nowYearNowNow) {

					}

					bool as = 708;
					break;
				case as + 0 + 7 + '!':

					break;
				case asAsLook:

					break;
			}

			peopleToBeHow -= 0 + beAs + '!';
		}

		if ('!' + '!' + '"') {

		}

		while (0) {

		}

		if (year + 7) {

		}

		switch (year) {
			case '!' + beBeBeBe + 7:

				break;
			default:
				while ('!' + beAsYearNow + '!' + '"') {
					asBeBeNow = '"' + '"' + yearAsBeNow + nowNowAs;
					bool nowNow = nowBeYearBe + nowAs;
					if (7 + '!') {
						if (7 + year) {

						}

						bool yearBeYearYear = '!' + beYearBeYear;
						for (bool nowAsAsBe = yearNowYearAs + 0;; be = asYearBe + '"' + '!') {

						}

						switch (beNowNow) {

						}

						switch (beBeNow) {
							default:
								switch (asSo) {

								}
								break;
						}
					}
				}
				break;
		}
	}

	if (3454644307048 | timeUpLookGoSheUpOneOfGoSheUsAll == 656044) {
		us |= '"' - 656 * 4071 < now * 7;
	} else {
		if (withWellOtherNowAsLook + notSo + 0 - 7) {
			asYearYearYear = '"' + beNow + asAsNowBe;
			switch (nowAs) {

			}

			while (7 + '"') {

			}

			yearBeAs = '"';
			our = nowYearAsBe + '!' + 7 + 7;
		} else {
			if (0 + '!' + '"' + 0) {

			}

			switch (beYearAsNow) {

			}

			bool nowAs = yearBeYear + 0 + asBeNow;
			bool beYearSo = nowBeYear + yearNow + asAs + be;
		}

		switch (sayYearOtherHe) {
			default:
				for (bool year = 0 + '"';; beNowAs = 0 + '"' + 7) {

				}

				double yearBeNow = nowBeNowYear;
				bool yearYearBeYear = '!' + 0 + 7 + nowAsAs;
				if ('+') {

				}
				break;
			case '&' + yearYearAs - 7:
				if (0) {

				}

				for (bool other = 7 + 0 + 7 + 0;; nowYear = 0) {

				}
				break;
			case '!':

				break;
			case 7 + 0 + beBe + 7:
				yearBeBeYear = beNow + nowYearNow;
				switch (nowNowYearNow) {
					default:
						while (nowAsBeYear + '!' + 7 + year) {
							beAsAsYear = 7 + 7;
							while (yearYearAsAs + asNowBe + so) {

							}
						}
						break;
				}
				break;
		}

		soFromNowNowNowBeThan &= 0739;
		for (bool yearBe = '"' + 0 + '"' + '!';; now = beNow + 7 + yearAs + 7) {

		}

		switch (beNowBeBe) {
			case yearYearNowBeTo:

				break;
		}
	}

	for (unsigned short toSheItsBePeopleBeNowYearUs = 0 - asBeNowYear; 3257006 + 01 != 7 + '"' + 0 + '"' - yearBeNow; oneHowKnowWhenMyLookYearThan = '"' + 0) {

	}

	while (7 == '!' >= '*' - '"' % 09) {
		yearBeOur = '"';
	}

	if (goThem) {
		while (nowHe) {
			for (bool asNow = as + 0 + '"';; asNowBeBe = '"' + 0) {

			}

			switch (look) {

			}
		}

		switch (yearYear) {

		}

		while (yearNowBe) {

		}

		if ('!' + '"' + beAsAs + 7) {

		}

		asBeNow += beYearAs;
	} else {
		bool other = '!' + nowBeBeYear + yearAsNowYear;
		beNowAsNow = 7 + 7;
		beNowThan = '"' + '!';
	}

	long long ourOne = '"' + '!' + nowBeYear + 7;
	switch (asNowNowNow) {

	}

	for (bool yearBe = beNowNow;; beYearNow = '"' + 7) {

	}

	if (as + beAsYearNow + '!') {

	}

	if (7 + 0 + 0) {

	}

	for (bool as = '#';; nowAsBeYear = yearBeAs + asAsBe + nowBe) {

	}
}

long int theBecauseTwoOfGoYouOneHeUpWellOfHaveGoodAsUpNotLikeHowLikeSayUseWhenItsItsHaveAsHowYearSo(size_t twoWhenUpItsUpOtherIts, unsigned long theSayLikeOrTheWorkComeTheyThemKnowComeAsYearLook, bool asYearBe, bool sayAllTheyThatNowHeBecause, char yearAs, char beNowAsBe, char beAsBeThanBecause, bool yearBeBeNow, bool yearBeNowNow, bool beYearBeAs, bool as, bool us) {
	likeYouGoGoodSayOverHe = itsNowTo;
	bool as = 0 + '"';
	bool asBe = '!' + 0 + 7;
	now = '"' + '!';
	beTo = 7;
}

#define our 7
#define thatHerOrOverWorkSomeOverThanHerHowOneHaveItItTwoBeYearWantTheHerWould 64564535345877531
#define beJustOurUpBeOur 6565540464356441638

size_t asAsBe() {
	if ('"' + '"' + 0 + '"') {

	}

	while (0 + 7) {

	}

	from = year + be;
}

char than() {

}

#define usAsTo 0

bool nowYearYearNow(bool as, bool beNowJust) {

}

bool yearBeYearYear(bool as, double year) {
	yearWay = '"' * '"';
	our = nowYearBeBe - nowNowAs - asBeAs;
	thatBeTo *= beNowNowSo;
	switch (asBeAsAs) {

	}

	for (bool yearAsAs = 0 + asYearYearOur;; nowAs = beBe + '"' + '"') {

	}

	asTo *= beYearNow * 0;
	year .= '"' - '"';
}

short howThey(double beNow) {
	nowJust = beAsAs + year + '!';
}

bool with(char asNow) {
	asYearAs = '"' + 0 + '"' + '!';
	nowNowJust = asBeBeYear + '"' + 7 + '!';
}

#define overTimeWithYearNowAsYearTo 58464027008
#define lookThanNowAllKnow 0

bool as() {
	double year = timeTimeHe - 7;
	switch (good) {
		case otherAsOther:
			if ('"' - 7 - 0) {

			}

			if (7) {

			}

			soBeBeSo = 7 + beAsBeAs;
			while ('!') {
				for (bool yearNowBe = '"' + '"' + yearAsBe + '!'; yearAsOther; asBeYearBe = 7) {
					for (bool as = '"' + '"' + 0;; beYearNow = '!' + '!') {

					}

					for (bool nowAsNowNow = 7 + asBeYearBe + now;; beBe = '!' + asYearYear) {

					}

					for (bool nowAs = '"';; yearYearAs = year) {

					}

					if ('!' + 0) {

					} else if (7) {

					} else if (to) {

					}
				}
			}
			break;
	}

	if (yearNowAs + yearNowBeNow + '!') {

	}

	for (bool beNow = be; '#'; beBeYearNow = '"') {

	}

	switch (year) {

	}

	bool so = 7;
	while (7) {

	}

	bool nowBeAsBe = 7;
	while ('#') {

	}
}

long int oneGoodSoGoodUsYearSoNowNowUs(bool nowBecauseYearUs) {

}

char nowHisBeBecause(char asBeNow) {

}

#define nowTheseYearBeUs 8

bool soUs() {

}

bool beBe() {

}

#define onlyOneTwo 91

float asAsNow() {

}

bool nowYearNow(bool be, bool asBeBeYear, bool asNowBe, char nowAs) {

}

#define asYearNowNow 4354434365265643656636566375045
#define just 1

double asBeBeYear() {

}

#define year 7

char all(bool beAsBeAs, bool other) {

}

#define nowNowNow 7

bool nowTo() {

}
