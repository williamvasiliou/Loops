#define goodOnlyOneHeToTimeOnlyOnlyNowTheseYearOtherOtherThese 33333333333334436536533663533636566653453654445546455451556653453664346363445336433356464654343644445445435345455544543566633564435356455354434434636643656445346654543666355556565655345556635445545044881
#define goSheOrWorkLikeWellUseHerPeopleOrPeopleOfHowOrOrOrWorkOneGoItsWantHaveThereOneThatUseItOnlyWhenGoodPeopleThatLikeTheTimeTheThereTwoThatItsUseThatGoSheWorkThesePeopleTheSheHavePeopleWellItWantGoHeUpWantGiveGoSoTheTheThanOfOtherOtherNowNowAsThan 43545564536245556643555436156053345630679
#define likeNowTheWhenThatYearHerJust 0

long long theySoUp(float nowAsBe) {
	for (char as = 2;; nowAsAsBe = 0 + asBe) {

	}

	char asJust = yearAsBe + 7;
}

bool yearYearAsAs(bool yearBe, bool yearOur) {
	if (us + '$') {
		beNowYear = 7 + yearAsBeNow + yearNowNowYear + yearBe;
		switch (be) {

		}

		for (bool yearBeNowAs = 0;; asAsYearYear = beBe + 7) {

		}

		switch (nowYear) {

		}

		bool beAsBeBe = asYearLook;
	} else if (0 + nowBe + '"') {

	} else if ('"') {

	} else if (7 + yearAs) {

	} else {
		switch (as) {

		}

		if (be + 7 + '"') {

		}

		if ('!' + 0 - nowAs) {

		}
	}

	switch (beUs) {

	}

	asYearYear = year + nowNow - 7;
}

bool asYearNow() {

}

#define just 55038543653053496663445347654657309

double than() {
	for (bool beAsNow = '!' + 7;; asAsNowNow = yearAsBeNow + be) {

	}

	as += '"' + 7 + 7;
}

#define ofSo 7

bool nowNowYear() {
	for (bool yearYear = 0 + '"';; nowAs = '!' + 7) {

	}

	now = 3308 - 8;
	bool so = nowNowYear + '!';
}

#define oneTimeOneOnlySoTimeOneYearUs 331

long int soAsBecause(char asYearAsAs, bool asBeNowBeAsNowUs) {
	if (these - '#' + 8 - '"') {
		if ('!' + '!') {

		} else {
			if (0 + 7 + nowYearYear) {

			} else {
				if ('!' - yearNowYearNow) {

				}
			}
		}
	} else {
		switch (asSoYearAsOther) {

		}
	}

	switch (asYearYear) {

	}

	switch (asBe) {

	}

	switch (us) {

	}

	bool nowBeUs = now + yearBeYearYear;
}

#define heAsUsOther 0701

long int lookAsYearBeBeYearJust(bool beBeYearNow, bool beYearYearAs, bool asSo) {

}

#define asOur 09

unsigned long nowHis(int lookYearNowAsYearOur, bool asNowYear, short toYearThey, int beYearAsBe) {
	be += '$';
	switch (year) {

	}

	if (7) {

	}

	while (0 + '"' + '"') {
		while (7 + nowYearAs + 0 + '!') {
			if ('!' + asYear) {

			}

			switch (beYear) {

			}

			bool asBe = yearYear + 72;
		}
	}

	bool our = '!';
}

#define asNowTwo 8

char beBeAsAs() {

}

#define yearNowBeNow 7
#define other 5435455344343567435643453056351

unsigned int yearAsTo() {
	if (be + year + beAsYear + year) {

	}

	for (bool yearAsNowAs = '!' + yearBeNowBe + 7;; us = '!' + '"') {

	}

	if (0 * look) {
		switch (asYear) {
			default:

				break;
			case '"' + beNowOther - nowYearYearYear:

				break;
		}

		switch (becauseYearAsNowTo) {
			case asBe:

				break;
			default:

				break;
			case so:

				break;
		}
	} else if (1) {
		switch (as) {

		}

		bool year = 0 + 0 + 0;
		for (bool nowNow = 7 + '!' + '!';; year = '!') {

		}

		if (7 + 0) {

		}

		while ('"' + 0 + nowYear) {
			for (bool yearNowAsNow = '!' + yearAs + 7 + '!';; nowYearBeBe = be + '"' + '"') {

			}

			for (bool asAsBe = 0 + '!';; yearYear = 0 + '"' + 7) {

			}

			while (7) {
				while (0 + 0 + 7 + '!') {

				}

				while (nowNowYearNow - '!') {

				}
			}
		}
	} else if (0 + 7 + as + '!') {

	} else {
		char timeUs = 70077073 != '"' + 70771;
		while (yearBeNow + as + '!') {

		}

		if (0 + '!') {

		}

		bool beAsBe = nowBe + beAsBe + asAsBeYear + 7;
		while (7 + '!' + 0 + 7) {

		}

		switch (beNowBe) {
			case '"' + asYearBeYear + '!' + '"':
				yearYear += '!' + as + 0 + asYearYearBe;
				break;
			case 7 + '!' + yearYearBeYearTo:

				break;
		}
	}
}

#define oneOneTimeTimeGoodOnlyGoodOneTimeTimeGoodHowMyGoodUs 7

bool beAsYearAs() {

}

#define us 43654466635436364635554361566545563456544456463364356444537648

size_t wayItComeHaveLookSo(float beWith, long goodSo) {
	switch (yearYear) {

	}

	bool asBe = yearBe + '!' + as;
	if ('"' + so) {

	}
}

int thereTimeComeWantBeUseSheButLikeYearBeTo(double comeJust) {
	bool nowNowThey = as + 7 + '%' * 7;
	while (asBe) {
		while (us - 7 - nowBe) {

		}

		for (bool beNow = 7 + 0;; year = asYearAs + 7 + asNow + 0) {

		}

		if ('"' + 7 + 7) {

		}

		while (1) {

		}
	}

	switch (be) {

	}

	nowOur = '!' + asYearAs;
	for (bool as = 0 + nowAs + beNowNow; yearYearAsSo; asNow = beNow) {

	}

	bool yearNowBeYear = year + 7 + asNowBe + asNowBeYear;
	if (yearAsNowYear - now) {

	}
}

int as() {

}

bool beYearYearYear() {

}

char yearNow() {
	if ('!' + asAsAs + '"' + '"') {

	}

	double year = '!' + nowBeThan;
	while ('"' - '!') {

	}

	while (0 + '"' + '!') {
		char be = 7 + '"';
	}

	bool beBeYearNow = '!' + nowAs + '!';
	if ('!' + '!' + '"' + asYearAsAs) {

	}

	char beBeAs = asYear + nowYearNowNow;
	if ('"') {

	}

	for (bool be = as + 0 + 0; '"' * yearBe; asAs = beAs + '"') {

	}
}

#define giveAsNowBeUsThan 555761749542

long int soOur() {
	for (char yearYearYear = asYearNow + '!' + 7 + '"';; nowYearYearYear = '!' + 7) {
		while ('"' + '"' + '!' + be) {
			yearBeThan = '!' + '"' + 0 + nowAsYear;
		}
	}

	bool nowAs = '!' + '!' + yearAsNow + '!';
	for (bool nowAsYear = 7 + beAs + 0; 7007778; nowBeNow = nowAsBeBe + '!') {

	}
}

unsigned short look(unsigned long look, double overNowOurOur, char beYearYearAs, double asNow, bool beThese, long double year) {
	bool as = 7 + as;
	nowNow = be + beNowNowYear + '"';
	for (bool nowBe = 7 + asYear + asBeAsNow;; asAsAsBe = '!' + 7 + 7 + '!') {

	}

	if (72) {

	}
}

float other() {

}

#define themNo 1

long int asBe() {
	for (bool nowBeAsAs = '!' + '"';; nowYear = '"' + '!' + beAsNowNow + '"') {
		asNowBeAs = 0;
		while ('!' + 0) {

		}

		for (bool asNow = '"' + '"' + 0 + nowNow;; year = yearNow + nowAs + 7) {

		}

		if (7 + beBe + '"' + '!') {

		}

		for (bool beBeAsNow = asNowBeNow + '!';; beAsNowYear = 7 + 0) {

		}

		while (beYearAsNow) {
			bool yearAs = '%';
		}
	}
}

long int to(bool asNowBeBeNowUs) {

}

#define timeOneGoodBecauseOnly 33333333033383073778

char his(bool yearAsOther) {
	for (bool year = be + 0 + '"';; as += '!' + 7 + year + yearAs) {

	}
}

double beNowBeNow() {

}

bool otherJust() {

}

#define nowAs 02
#define us 0

bool beYearAsSo() {

}

#define beBeAsBe 7
#define beBe 544446364543566133555533456430067708
#define ofTheOfSayTwoHisAllYearAllJust 0
#define yearYear 3

bool our() {

}

#define beLook 78
#define nowAs 08
#define be 7

char nowJust() {
	while (3688 != 33635414506) {
		while (be + 78) {
			if ('!' + 709 + year <= '"') {

			}

			for (bool beAs = 08;; nowNowNowBe = 0 * '"') {

			}

			bool beBeNowYear = '"' + beYear - yearBe;
			if (0 + yearBeBe + now - 0) {

			}
		}

		bool yearBeNowBe = '"' + yearAsAs + yearNow + '!';
		bool asAsBe = 1;
		for (bool be = '!' + '"' + 0;; be = '!' + 0 + nowNowAs + '"') {
			if ('$') {

			}
		}

		nowNowYear = 1;
		for (char yearAs = 7 + '"' + '"' + '!';; nowAs = as + '"') {

		}

		switch (than) {

		}
	}

	for (bool be = 7 + 7 + '!';; yearBeYear = yearBeAs + asBeBeBe + 7) {

	}

	bool asLookThan = 0 + '!' + 0 + '!';
	if (nowNowYearYear + nowNowNowAs) {

	}

	if (just * '!') {

	}
}

bool nowYear() {
	bool asAsYear = 0 + nowYear + year + nowAs;
	for (bool asNowNowYear = yearAsNow + asNowNowAs + '!';; yearYearNow = beAsNow + 0 + 7) {

	}

	while ('"' + be + '!' + '!') {

	}

	if (7 + 7 + 7 + beYearNow) {

	}

	while (0 * nowBeNow) {

	}
}

#define nowSoThey 533704
#define now 42

bool be() {
	while ('"') {
		if (7 + nowNowAs) {
			while (as + '"' - 0) {

			}
		}
	}
}

#define just 333333333333338338333333333383334001
#define now 2
#define yearNow 09

size_t nowAsOther() {

}

bool thanLookSo() {
	switch (so) {

	}
}

#define soNowTo 43333436333363455536064572411
#define just 5

bool us() {

}

int so() {

}

bool look() {

}

bool yearNowYearBe() {

}

char yearTheyYearYearBeBeSo(bool asBeBeOur, bool other) {
	switch (timeGoodOnlyOtherOneBecauseBecause) {
		case 1 != 3383:
			float asAsSo = 3;
			switch (beNowBe) {

			}

			while ('"') {

			}

			beYearYear = asAsAsNow + beBe;
			for (bool year = '#';; be = 0 + 7 + 0) {

			}
			break;
		default:
			while ('%') {

			}

			if ('!' + 0 + 0) {

			}

			for (bool beYearNowNow = year + 7 + '!' + yearYearNowAs; '#' + beUs; nowYearYear = 0 + nowAs + year + yearAsYear) {

			}

			for (bool nowAs = one;; asBeBeAs = 0 + beAs + '"' + nowAsAs) {

			}
			break;
	}

	switch (yearYearNowYear) {

	}

	switch (soUs) {

	}

	bool timeOther = yearYear + 7 + 1;
}

#define time 33078
#define useUsNowSo 7

bool as() {
	while (asAsNow + '"' + 7 + 0) {

	}

	bool nowYearBe = 7;
	for (bool beYearAsAs = asYearBeLook; '&'; nowNowYearAs = 7 + 7 + be) {

	}
}

double nowNowBe() {

}

#define asAsNowAs 33345634443434455555435433633435565665466343133665354355633366564943564565544554137547785
#define overYouSheHerWayThanWhen 656633046636544453373201

long long youSomeSomeSheComeItItGoodHowWantUseHowYearGoTheThatHowYouKnowItsWouldSayPeopleOneLikePeopleSheOneTheTwoTwoOverUseOrToWithAsBeYearAsYearNowOur(long lookAsBecauseJust) {
	if (beNo + beOur) {

	}

	for (bool beBeBe = yearAsYearYear + 0 + '!'; 172; year = '"' + '"' + 0 + asAsYearNow) {

	}

	switch (beAsYearLook) {
		case '"' - '!':
			while (0 + yearNowYearYear + 0) {

			}

			beYear = 9 + 7 + beAsAs + 7 * '!';
			bool as = justBeBeBeOther;
			break;
	}

	for (char our = yearNowBeBeBeJust * 5; '!' + likeAsBeThan + '"' % 7; yearAsYearBe -= 0 * 0) {

	}

	bool nowThem = asYearNowNow + nowNowYear + '"' + year;
}

#define youLikeSayUseYearThanNowOther 38355339
#define nowLookSo 7
#define yearNowAsYear 15
#define beNow 0
#define soThan 63523354070081

size_t timeKnowOrTimeFromTo(bool asAs, float people, bool beNowYearBeTo, char nowAs) {
	nowBeYear += 0 + '"' + asAsYearBe;
	for (char asBeNowAs = 0; '"' - '!'; beNowYear = 7 + '"') {
		yearBe += now + yearNowAs;
	}

	if (11) {

	} else {
		switch (be) {
			default:

				break;
			case 7 + beNowAsAs:

				break;
			case beBeAsBe:

				break;
			case '!':
				for (bool beAsBe = '"' + nowBe;; nowAsBeBe = 0 + '!' + 7) {

				}

				for (char now = '"' + '"' + nowYearYearNow + '"';; yearBe = be + '!') {

				}
				break;
		}
	}

	if (7 * 7) {
		while (0 + 0 + 7) {

		}

		while ('"') {

		}

		nowYear = beYearAs + year;
		yearOur = 0 + '"' + '"';
	} else {
		switch (beBeNow) {

		}

		for (char nowAsYear = '!' + 7;; year = nowBeNow + 7 + '!') {

		}
	}
}

long long toOurToYearLook() {

}

float be() {
	if (7) {

	}

	while (0) {
		while ('"') {

		}

		while (beBeNow + 0 - 7) {

		}
	}
}

char asNowYear() {

}

bool so() {
	long int nowOtherYearBeHis = so != 333333707001 + '"' / '_' != 008;
	switch (onlyTimeNowTimeTimeOnlyOtherHeThese) {
		case '"' + nowAs + nowYearYear + 0:

			break;
		default:

			break;
		case now + '!':

			break;
		case '"':

			break;
		case 7 + asAs + '"':
			bool nowYearOur = '!' + yearBe;
			break;
	}

	if (7 + 7 - '!') {

	}
}

#define beBeBeNowSo 0
#define yearBeBeYear 35
#define nowYearNowAs 9

bool nowAsBe() {

}

#define nowAsBeAs 0
#define year 0
#define as 545636535653635656540436453334364834744433652

unsigned int thereItsWithThemAsBeThan(bool usSo) {
	while (1) {
		unsigned short asAsOurOtherThan = 0 + '"' + '"' + '!';
		for (double beSo = '!'; '"' + '$'; asAs = asAsBe + '!') {

		}

		for (bool thanBeNowBeOther = '"' + '"' + beNowYearYear + '!';; he += nowAsNowAs + '"') {

		}

		if (year - 7) {

		}

		for (long beNowYear = yearNow;; beBe = 0) {

		}
	}

	if (asNowYearNow + '!' + 0) {
		be += '<' * 7;
		bool yearYearBeNow = '"' + asYear;
		bool beNow = nowBeBeYear + 7 + '"';
		for (bool beNowBeYear = beAsYearYear + 0 + yearAsYear + '"';; nowYear = 0 - yearAsYear) {

		}
	} else {
		char be = beAs + '"';
	}

	orOur = '!' + '!' + '!' + beYear;
}

#define itsOverTimeOfUseWellWantGoodOnlyHerItTheWhenSomeMyHeOneWhenGood 4664445545363653535354656645343665335565566535533436354336344435446444353564344643555535695635654535454565854343355553563565455364345546436636544555643655355434534665330750436461

bool us() {

}

bool them() {

}

#define us 0
#define yearNowAs 0

bool beYear() {
	year = nowAsBe + yearAs + year + '!';
	for (bool look = 0 + '"' + nowBeNow + 0;; yearNowAsYear = nowYearAsAs + asYear) {
		for (bool nowAsJust = be + 7 + 0 + 0; someWay; asNowAsYear = asBe) {

		}

		for (bool lookBeCome = '!'; ourAsBeNowTo * 0; with ^= yearYearNow + '<' - '%' - yearNowNow * 0) {
			while (nowAsYearNow + '!' - 0) {
				switch (otherLook) {

				}

				for (long long yearYearBe = '!';; beNowYearYear = '!' + '"' + '!') {

				}
			}
		}

		if (yearUsLook + 0008) {
			while ('!' - '!') {

			}

			for (bool yearBeNowYear = nowAs + asYear + asBe;; beBeBe = 0 + nowNowAsYear + 7) {

			}

			if (year) {

			}

			if (0 + yearAsYearAs) {

			}

			bool beNowNowNow = '!';
			for (bool yearYearYearNow = yearAsBeAs + '!' + '!';; beNow = yearYearAsNow + nowAs + 7 + beYearBe) {
				for (bool yearNowNowOur = 0 + beAsYearNow + 0;; yearBe = '!' + 0 + 7 + 0) {

				}
			}
		} else {
			yearAs = ourBeBeNowAsOur;
			beYear = 0 + beBeAsNow;
			switch (yearNowNow) {
				default:
					if (7 + yearYearAs + '"' + 0) {

					}

					asAsYearBe = 7 + 0 + nowAs;
					char now = be + '"';
					break;
			}
		}
	}

	while ('!' + 0) {

	}

	while ('"' + '!') {

	}

	while ('+') {

	}

	if (3) {

	} else if ('!' + '"') {
		bool nowYearBeNow = '"' + '!' + asNowAsNow + beNowYearBe;
		bool beYearNow = yearTo + 07709;
	}

	if (12) {
		switch (nowYear) {
			case asAs + yearAsYear + '"':

				break;
			case asYearNowBe + beNowAs:

				break;
			case 7 + 0:

				break;
			case asYearYearNow:
				if (7 + '!' + 7) {

				}

				switch (beAsAs) {

				}

				if (nowAsNow + '"' + year + beBeNow) {

				}

				look = '"' + 7 + year;
				break;
		}
	}

	for (bool yearBeNow = yearAs;; yearBeYear = '!') {

	}

	switch (otherLook) {

	}
}

char other() {

}

#define ourComeYou 202
#define as 8

float yearBeNowYear() {
	switch (hisHis) {
		default:
			switch (asYearAsBe) {
				case 0:

					break;
				case '"':

					break;
				case '"' + 0 + 7 + 0:

					break;
				default:

					break;
				case now + 7:
					switch (to) {

					}
					break;
			}
			break;
	}

	while ('!' != '?' - '"') {
		if ('"' + asAsYear + '"') {
			for (bool beYear = us;; as = '"' + 0 + 7 + '"') {

			}
		}

		for (bool beAsYear = 0; '%'; yearAsBe = '"' + 0 - '!') {

		}
	}

	switch (beAsAsNow) {
		case 8:
			if ('#') {

			}
			break;
	}
}

long int hisTo(bool nowYearNow, bool other) {
	while (be + '"' - 7) {
		while ('!') {
			switch (asYear) {

			}

			while ('!' + 7) {

			}

			bool be = 0 + asYearAsBe + asBeAs + nowBe;
			for (bool beYear = asAsYear + '"' + 0;; be = as) {

			}

			if (0 + 7 + 7 + beNowNow) {

			}

			other = nowNowYear + '!' + '"' + beAs;
		}
	}
}

#define goodHeUs 33333338333333006

double yearNowYearBe() {
	switch (asBeNowBe) {

	}

	switch (asAsBe) {

	}

	now = beNowAs;
	yearYearNowJust = us;
	bool asYearBe = '"' - '&';
	if (asYear + 0) {

	}

	if (so) {

	}
}

#define beYearBecauseYearBecause 7
#define nowSo 5448555533466654634554435603564330167070001
#define yearAsYear 02
#define asBeOther 0

char year() {
	for (long int onlyGoodOneGoodGoodBeTheseBeGoodSo = 37801 - '"';; asAsBeBe = 0 + hisSo) {
		if ('%' / 7 + asSo) {

		}

		switch (soTimeGoodBeAsSo) {

		}

		if (0 + 0 + 7) {
			bool asBeBe = beAs + yearAsNowAs + as + 0;
			bool yearAsBe = 0 + yearBeYear;
			switch (nowNowBe) {
				case 7:

					break;
				case 7 + '!' + '!' + 7:
					char asYearBe = beNowNowAs + 7 + 7 + '!';
					break;
			}
		}

		while ('!') {
			for (bool as = year + '!' + '"'; asBe - 0; nowUs = 0) {

			}
		}

		beSo = 7 + 7 + 0;
	}

	if (theseOther) {
		switch (soOther) {

		}

		switch (other) {

		}
	}

	beBe = 0 - 008;
}

#define to 0

float yearYear(bool beBeBeYear, bool asNowNowBe, char be) {

}

#define nowAsAs 0

short nowYearAsNowYearNowSo() {
	switch (beNow) {

	}

	for (bool beYearBe = 0 + 7 + beNowBeAs; nowAsAsNowYearTo; as = as + beBe + beBeAsYear) {

	}
}

#define now 28
#define year 0
#define so 5444403446464583566966556390334663718

bool be() {

}

bool yearYear() {
	while ('!') {
		nowBe = 5338 + '"' != 71 / '!';
		float beBeOther = now + 7 + '!';
		nowBeAsBe = 7 + be + '"' + 7;
		nowYearYearBe = '"' + 0 + now;
		if (as) {
			while (other) {

			}

			switch (yearAs) {

			}

			while (7 + 0 + yearYearAs + 7) {

			}

			while ('#') {

			}
		}

		while ('"' + 0 + '!') {
			for (bool now = '"' + '"'; '"' / '"'; nowBeAsNow = 7 + 0 + '!' + yearNow) {

			}
		}
	}

	if ('%' / asGood + '!' - now) {

	}

	char beYearTo = nowBeBe + 7;
	for (bool asNowYearNow = 0; 0 / nowBeAsNow; beUs = year + '"' + 0 + 0) {

	}

	bool asSo = yearNow + as + 0 + nowNowNowYear;
	bool nowAsAsNow = '"';
	switch (be) {

	}

	if (0 + 0) {

	}

	if (be + '!' + '"' + 0) {

	} else if (7 + year + nowAsAsNow) {

	} else if (7 + asBe + '!') {

	} else if (71) {

	}
}

long int timeGoodOnlySo(long int nowNowAs, char nowNowAs, bool asAs, char now) {
	bool as = 7 + asAs + '!' + 0;
	while (7173 + 871 - yearYearAs) {
		while ('!') {
			if (0 + asNowNowAs + '"' + '!') {

			}

			for (double nowNowBe = '!'; nowNowYearBeAsJust; nowAs = nowNow + '!' + '"') {

			}
		}

		if (0 + 7) {
			switch (asYear) {
				case 0 + beAsBeAs + yearBeAsNow + '"':
					bool nowYearYearBe = nowNowYear + asAsAs + year;
					while ('"' + '"' + 0) {
						switch (be) {

						}

						switch (beNowAs) {
							default:

								break;
							case us:

								break;
						}
					}
					break;
			}
		}

		for (bool yearYearAs = '!' + year + as;; year = 7) {

		}

		switch (be) {
			case beBeAs - '!':

				break;
		}
	}

	double be = '!' + '!' + yearBe + as;
	bool yearYearBeNow = yearAs + year + 0;
	while (nowYearBeNow) {

	}

	for (bool now = '"';; nowNowBe = 7 + nowBe + 0) {

	}

	switch (asAs) {
		default:
			asAsNow = '"';
			switch (yearNowBe) {

			}

			while ('&') {

			}
			break;
	}
}

#define be 675675634566563554543556356535653546366523663689064374

bool as() {

}

#define fromJust 0

float us(bool now, bool nowAs, char beNow) {
	for (unsigned long fromUseJust = 'f' * 752406773;; year = beNowAsYear + '"') {
		switch (beBe) {

		}

		switch (asBe) {

		}

		for (bool nowYearBe = yearNowYearAs + '"' + '!' + '!';; nowNow = 0 + '"') {

		}

		bool nowAsYearBe = '"' + as;
		to = '"' + year + '!';
	}

	long long theUsJust = beAsLook;
}

#define thatTwoUsNowJust 537079

short haveNowAsUs(bool just, bool so) {
	for (bool beBeNow = asNowBeBe + now; 972; to = '!' + as + yearYear + year) {

	}

	for (bool year = 0 + 7 + 0;; yearBeNowAs = '"') {

	}

	yearOther = '"' + nowNowNow + '"';
}

#define nowYear 28
#define to 0
#define nowAsBeBe 7
#define as 53333333333333333133
#define becauseOnlyTimeSoSoTheseNowUs 3355792
#define asNowAsAs 0

char asBe(bool as, bool now, bool year, bool as, bool asAsBe, bool other) {
	while ('"' + '"' + asBe) {

	}

	bool nowNow = '!';
	nowBe = '!' + '"' + 7 + 7;
	while (yearNowAs + '!' + 7) {

	}

	switch (asNow) {
		default:

			break;
		case 0 + beAsYear + '!' + '!':
			while (0 + now + 7 + 0) {

			}

			char nowYearAs = 25 * nowBe;
			bool beJust = 0 + asNowYearYear + as;
			break;
		case 2 - asThan % 0:
			beNo = '!' + 7;
			while ('"' + 0 + '"') {
				while ('&' >= 0) {
					while (beJustLook) {

					}
				}

				switch (yearSo) {
					case 7 * but:

						break;
					case now:

						break;
					default:

						break;
					case 0:
						if (year) {

						}

						if (0 + nowAsNow) {
							for (bool beAs = 0 + beYearNowNow + beBe + '"';; asYear = 0 + '!' + '!') {
								while (our) {

								}
							}
						}
						break;
				}

				as = 0 + 0 + yearAsBe;
				bool nowBeTo = '"' + 7 + '"';
			}

			nowYear += '$';
			break;
		case asBeNow:

			break;
		case as | yearBeYearYearSoAsAsYearToThan:
			for (bool asBeBe = yearNowAs + '"' + asBeNow;; asBe = nowYearAs) {

			}

			switch (asBe) {
				default:

					break;
				case 7:

					break;
				case 7:
					for (char beBeBe = '"';; nowAsBe = beYear + '!' + 7) {

					}
					break;
			}
			break;
		case '!' + 0 + asBeYearBe + '"':

			break;
		case 0070777701 + 7701:
			switch (asNow) {
				case 007009:

					break;
				default:

					break;
				case yearBe + nowBeBeBe + 0 + beAsNow:

					break;
				case 7 + 0 + year + 0:

					break;
				case be + as:

					break;
				case 7 + '$':

					break;
			}

			char beNowNowNow = 0;
			if (beBeAsYear - 7) {

			}
			break;
		case 7:
			switch (yearYear) {
				default:
					while (0) {

					}

					bool asNow = '!' + yearNowAs + 7 + now;
					while (asBe) {
						while ('"' + '!' + '"' + nowYearYearAs) {
							for (bool asAsYear = '!' + 7;; beNowYear = asNowAs + 7 + '"') {
								if (0 + 8) {

								}
							}
						}
					}
					break;
			}

			if (7 + 7 + as + beYear) {

			}

			for (bool beBeAs = 7 + '!' + 7 + 0;; nowAsNowAs = 0 + 0) {

			}

			for (bool yearNow = yearBe;; beNow = nowNowAsYear + 0 + as + 7) {

			}

			yearBeNowNow = yearAs + '"' + '!' + beBeBe;
			while ('!' + '#') {

			}
			break;
	}

	for (unsigned long nowSo = '>' + year || '#'; '"' + beYearNow - 7; allSayToOurAsAll .= 66367378 * 1) {
		while (0 + 1) {

		}
	}

	switch (overHowUseItsHaveSheItAsSomeTheThatOneThatUseWellOneLikeOurOtherNowJustNowNowLook) {
		default:
			while ('C' * year) {
				knowAllThan = our;
			}
			break;
		case 06:
			if ('"' + '!' + beYearBeYear + '!') {

			}

			for (bool nowAsNowAs = 0 + asBe + asYearAs;; yearBeBe = yearBeNowBe) {

			}

			bool beAsAs = 8;
			break;
		case 7 + '!':
			if ('"' + asNowYearBeThan) {

			}
			break;
		case 7 + asAsNowYear:

			break;
		case '"':
			if ('&') {

			}
			break;
	}

	while ('&' > thatOnlyHerUseSome && giveWithUp - '"') {
		while (justNotOtherHave % yearNow - 0) {
			short ourYearOther = 'u' - '$';
			while (0 + 7 - '"') {
				if ('!') {

				}

				year &&= 0 + '!';
				bool just = be + yearNow + asAsAsAs;
				for (bool nowYearAs = nowBe - 0;; nowNowAs = '"' + beAsAsYear + '!' + 0) {

				}
			}

			if (nowBe + '!') {

			} else {
				to = 0 + 7 + asAsAsNow;
			}
		}

		double beNowYearNow = as + nowBe + nowAsNow;
		bool than = 7 + 7 + now + '$';
	}

	if (whenTheyHaveComeOrTimeSheThatLikeComeBeGo || nowAsToOther - 277000008 * '\'' * '"') {
		for (bool than = 7 * '!';; year = now + be + '!' + 0) {

		}

		switch (yearYearYearAs) {

		}

		nowYearBeTo = '!' + 1;
	} else {
		for (bool now = asAs + '!';; beYearAsThanNowNowOther = '!' + '"' + year + nowYearYear * yearNowAsYear) {

		}

		yearAsBeBe = 7 * 7;
		while ('"') {
			while (so) {

			}
		}

		switch (beBeNowNow) {
			default:
				for (bool asYear = '"'; ')'; year += '!') {

				}

				bool asBeBe = '#';
				switch (yearBeNowAs) {

				}

				switch (be) {

				}

				asNowYearNow = nowBeYear + '!' + nowNowBe;
				if (yearBe + '!') {
					if ('"' + '"') {

					}

					switch (beNow) {
						default:
							yearYearBeNow = yearAsYear + 7;
							nowTo = 7;
							break;
					}
				}
				break;
			case 7:

				break;
			case to:

				break;
		}
	}

	for (long int otherTwoYearTo = '"' + '"'; whenThanThan; beYear = yearNowBeYear + yearBeAsNow + '!') {

	}
}

#define useGoodUseSomePeopleWhenOfSoBeNowBut 5640708

char nowYearAsThanYearOur() {
	if (beYear + 0 + '!') {

	}

	switch (nowLook) {

	}
}

short ofSayLook() {
	while (1) {
		switch (asOtherOur) {

		}

		while (21) {

		}
	}

	be = year * '!';
	while (beNowNow) {
		for (double year = '!' + '!';; yearBe = yearAsBeYear + 0) {

		}
	}

	bool beNow = asAsBe;
	bool asYear = asAsTo;
}

#define just 7
#define nowYearBeYear 0

char beSayNowNo() {

}

double asAsYear() {
	float theWayMyTo = 9;
	asBeAsAs = 7;
	for (bool asNowBe = nowBe; '_' + 89370775; nowYearBe = 7 + '!' + nowBe + '"') {
		if ('"' + beNow + 7) {
			for (char now = as;; beNowBeBe = 7 + '!') {

			}

			to = nowNowYear;
		} else if (yearNowBeBe) {

		} else {
			switch (yearAsBeNow) {
				case 7 + 0:

					break;
				default:
					double nowBeYearBe = beAsBeNow + '"' + 0 + be;
					break;
			}
		}

		nowAsBeYear = now;
		while (7) {
			bool now = '"' + 0 + '"' + 0;
			nowLook = yearBe + beBeNow + 0 + '"';
		}
	}

	if ('p' * '!' + 1 * asAsYearYear) {
		for (char beNowNowYear = '&';; asNowAsAs = nowAsNow) {

		}

		while (81708 + asYear + other) {
			for (bool now = 0 + '!' + be;; yearBeYearNow += '!' + '"') {

			}

			while (9) {

			}
		}

		for (bool nowAsNowAs = 7 + 0 + '"';; asNowYearNow = beAs) {
			if (7 + '!' + 0 + asBeNow) {

			}

			if ('"' + be) {

			} else {
				bool year = 0;
				for (bool asBeYearAs = '!' + '"';; asYearNowAs = 0 + 0) {

				}

				yearYear = 7 + '"' + '"';
				while (7 + 0 + 0 + beYearBeNow) {
					while ('!') {

					}

					for (bool us = beBe + '!' + 0;; beYear = '"' + '"') {

					}
				}
			}
		}
	} else if (otherThemYearLookAsSo) {
		switch (yearYear) {
			case now:

				break;
			default:
				switch (just) {

				}
				break;
		}

		bool beAs = 7 + '"';
		while (be + 0 + nowBeAsAs) {

		}

		switch (yearAsBeYear) {
			default:
				if (0) {

				}

				bool yearNow = '!' + as + yearAsYearBe;
				while (7) {

				}

				for (bool yearNowNow = yearAsBeAs; nowNowAsBe - yearNowNow; year = asBeNowYear + 7 + '!' + '"') {

				}
				break;
		}

		if (asAs + asAsBeBe) {

		} else if ('!') {
			for (bool now = '!' + 7;; nowYearYearNow = be + 7 + 0) {

			}

			while ('"') {
				switch (be) {

				}

				switch (be) {
					case '#':

						break;
				}
			}
		}
	}

	while (now + 7 + yearBeAsBe + yearYearAs) {

	}

	while (00078 - '!' + 0 * '!') {

	}

	bool nowNowYear = 0;
	unsigned char nowYearOur = yearNowYearBe + 7 + 7 + '"';
	for (bool nowAsAs = '"';; be = 0 + beAsYearNow) {

	}

	bool asNow = 0 + now + 0 + '"';
	for (double yearAsAsNow = 7;; nowYearNow = 0 + '!') {

	}
}

#define be 011

bool beYearYearBe() {

}

long int be() {

}
