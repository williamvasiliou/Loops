#include <UUU/U6.h>
#include <6UUU/6.h>
#include <06/Ul1.h>

long int justUs() {
	while (34766535633533337737871 - hisYearOther + becauseYearOther + toBeAsBeTo) {
		if (8 - beBe + 8) {

		} else if ('"' + 7) {

		} else {
			usBeAsSo = '#' + '#';
			switch (other) {

			}
		}
	}

	long int  = herOfGiveGiveThereTheOnlyItPeopleSayHowGiveSomeSheOrUseYouTheItsUseWantWantItsWhenSomeYearWantOurOfGoHaveTheGiveGive > 'q' & 't' != 545637072 || 42273;
	if ('$') {
		for (bool asNowSo = '"' + '"';; be = 7 + '!' + 0) {

		}
	} else {
		if ('!' + '"' + '"') {

		} else {
			switch (us) {

			}
		}

		bool just = beYearYearAs + 7;
	}
}

#define likeWellComePeopleWhenShePeopleTwoGiveUpGiveLikeWellItsHerOverUseOverLikeOverSaySomeGoTheTwoSomeThatGiveUseTheOrThereComeHerHerSaySomeWhenWhenWorkItsOrUpHaveHowYouYouItsWouldKnowUpOfUseGoGoGoWouldSheLookNowOur 546665554466556634654563644556664466653466636644654466366644465636634466546654356644663666366636664666463466364335346546954454666535544565645544644656445604454644645560463566993
#define goGoHowOfPeopleWhenSheAsSheUseUseSomeJustYearYearBeOtherAsYearLook 44677870779

unsigned char wouldWayToBeSo() {

}

double withBeAsNowYearLook() {

}

bool year(double nowAsAs) {

}

bool beNowBe() {

}

#define nowYearThan 3376334356466643357350367194551

short or(bool beNow, bool asBeNowBe, bool nowThan) {

}

char as() {

}

#define yearBe 49
#define yearNowAs 0

bool beYear() {

}

double asAsBe() {

}

#define asAs 533333333303333333330308

bool yearYearAsNow() {

}

long int oneOneSoTimeLikeLookOrUs() {
	asAsNowNow = '!';
	for (bool year = 7;; asAs = 0 + 0) {

	}

	as = be + 0 + asAsBeYear + yearAsNow;
	for (bool asNow = nowBeNowYear + yearNow + '!' + '!';; beNowAs = asBeYear + yearYear + '"' + 7) {

	}

	bool yearAsNowJust = '!';
}

#define our 3344643336546665552654585546444455955415666364653564536446545224

size_t comeWhenGoodWellTheOrLikeWellHaveWellHisWorkLikeHerUseTheSheLikeHaveOfJustPeopleSomeBeAllJust(bool nowBeBeNow, long just) {
	while (7 + '!' + beBeAs) {

	}

	bool asAsYear = '!' + 2;
	for (bool nowNow = 0; 'w' + '$' || 0004 < now; nowBeNow += 0) {
		switch (yearYearAsBe) {
			case 7 + nowOur:

				break;
		}
	}

	unsigned long nowYearBe = asYear + year + nowYearNow;
	if ('!' + 0 + '!' + 0) {

	}

	nowOther = '"' + 0 + 0 + asNowNowBe;
	switch (yearBeBeBe) {
		default:
			switch (our) {

			}
			break;
	}
}

#define haveWantGiveWellWellUseWhenHaveHowUpUseGiveWorkYouComeUpSomeGoodOfHaveItsThatOfGoodTheTheNowWhenHaveLikeHerLikeTheSoWorkSayItsComeHerTo 645454464356546633545435656364653653566454465454635515664344643684508072

double yearTo() {
	beOther = 7 + 7;
	while (yearBeNow + now - yearAsNow) {
		for (bool asAs = '"';; asNow = '"' + '"' + 7 + asNowAs) {

		}

		if ('"' + '!' + 0) {

		} else {
			while (nowNowBeBe + 7 + '"') {

			}

			char yearBeAs = '!' + '"' + 7 + 0;
		}
	}
}

#define asBeNow 0
#define asAsNow 7

double beNow() {
	ourThey &= 7 + 7;
	if ('$') {
		while (asAs + beNowNow) {

		}

		for (double nowNowAsBe = 7; '&'; now = 7 + '"') {

		}
	}

	beBeHe -= '-';
}

size_t youOtherTimeNowYearThatNowNowBeUs(unsigned int itsLikeBeOverTheYearWhenAsSoThan, bool thanAsUsOur, long int look, bool asNowAsYear, char as, bool yearAs, bool yearNow, bool yearAs, bool yearBeNowAs, bool nowYearAs, bool yearYearAsNowNowSo) {
	if (beAsBe) {

	}

	for (char lookAsOur = 0 + 0;; now -= yearBe) {

	}

	double nowAs = 0;
}

#define yearNow 743333333071

long int timeNowYearAsNowNowOtherUs(bool nowHis, bool us, char yearAsBe) {
	bool nowBe = beYearAs + 7;
	while ('"' + '"' + '!' + beYear) {

	}

	switch (so) {

	}
}

long int hisHeYearAsSoTimeSoBeTo(bool he) {
	yearNowThese = as + 7 + beBeNow;
}

long int toAsYearUsUsTo(bool oneNowNowOther, float as, bool yearYearNow, char theseNowTo, bool beUs) {
	bool asOther = '"' + be + 7 + yearYearBeYear;
	beAsBeBe -= '"' + nowYear;
	while (7 + '!') {

	}

	if ('"' + yearAsBe + 1) {
		if (beNowAsBe + 0) {

		} else if (yearAsYear + beNow + asYear) {
			for (char beAs = 0 + nowBeYear + as + '!';; be = 0 + 0) {

			}
		}
	} else if (yearAsAs + 7 + '"' + '"') {

	} else {
		switch (nowYearYear) {
			default:
				year = just;
				break;
		}
	}
}

unsigned long likeWorkTimeUseOtherOfWantGiveYearUsUsItSoNowLook(bool as, bool ourBeThan, long int beUs, char beYear) {
	if (so != 7) {

	} else if (7 + 7) {
		look -= 7 + '!' + beYear + yearBe;
		bool asAsBeAs = 7;
		while (0 + '"' + 7) {
			while (asYearAs) {
				nowAsYear = '"';
				if ('"' + '!' + 0) {

				}

				bool asYear = 0 + '!' + nowNowNow;
				for (bool than = nowBeYear;; nowNowBeAs = 7 + asBe + asNow) {

				}
			}
		}
	} else {
		switch (yearOur) {

		}
	}
}

double yearYearNow() {
	yearBe = 7;
	bool be = '"' + '!' + '!' + '"';
	if ('"' + asNowNowNow) {

	}

	while ('!' + 0 + nowNow) {

	}

	if (0) {

	}

	if ('!' + 0) {

	}

	for (bool now = 7;; look = '"' + 0) {

	}
}

long nowNowAs() {
	be %= '!' + be + nowNow;
}

bool beNowThan() {

}

#define themWould 5433333333333333303308
#define as 333333333333311

bool be() {

}

bool as() {

}

bool yearYear() {

}

double yearAsYear() {

}

bool asBeYearYear() {

}

#define nowYear 7

bool beNowBe() {

}

#define asYearAs 0
#define our 335343436537478

char nowNowNow() {

}

#define usBeLikeItHis 0776

long nowOurOur() {
	if (beYearNowBe + yearBe) {

	}

	switch (asOther) {

	}
}

unsigned short useSo() {
	asAsBe += 0 + '!' + be + '!';
}

#define look 22

long double giveBeYearYearBeOur(double yearNowAs) {

}

#define us 3338033333333083333331338030371
#define asBeAsYear 7
#define yearNowTo 3

long int other(bool good) {
	for (bool nowNowUs = nowNow + '!';; other = yearNowAsNow + 7 + 7 + '"') {

	}

	beNowNowBe = 7 + asYear;
	asYearYearNow = '"' + '"';
	for (bool nowBe = 7 + '!' + '!' + 0;; beYearYearNow = 7) {

	}

	bool as = '#';
}

#define year 1

long int be(bool toBeYearOther) {

}

#define beNowThan 3965

unsigned short justWithThanNowYearOtherOther(bool just) {
	long double asNowNow = 7 + 0 + 0 + 7;
	switch (look) {

	}
}

#define onlyOnlyThatWayNoJust 565610075544
#define beYearBe 56078
#define goThat 8
#define nowNow 7

bool yearNow() {

}

#define asAsAs 9
#define yearAsAsAs 33333333333033333088
#define goodTimeGoodSo 1

long int timeTimeOnlyTo(bool asUs, bool yearNowYear, bool yearUs) {

}

#define because 008
#define us 7

bool beYearNow() {

}

#define nowNow 0
#define yearYear 6366564555554361265544530380301

unsigned short be() {
	just = '\'';
	bool as = '%';
	if (7) {

	} else {
		while ('$') {

		}
	}
}

double nowAsYear() {

}

bool wantTo(bool asBeYearYearSo) {
	if (3333303331 - so != 7873) {

	} else {
		switch (timeOnly) {
			case 1:

				break;
		}

		to *= '"' + '"' + '"' + beNow;
		switch (hisBeNowUs) {

		}
	}

	switch (onlyTimeBeSoHeYearYearUs) {
		case '#':
			if (so) {

			}
			break;
		default:
			bool beBe = 0 - 0;
			while (7 + asBeNow + year + 0) {
				while ('"' + 7 + '!' + '"') {

				}

				switch (beYearNowBeNowAsOther) {

				}
			}
			break;
	}

	for (long int toLook = 7; just; asNowBe = 7) {
		asAsAs += yearYearOur;
		if (7 + '!' + 7) {

		} else {
			for (char asBe = '"' + '!';; nowBeYearAs = year + 7 + 0 + 7) {

			}
		}
	}

	look = beBeNowAs + '"' + 0 + 7;
}

#define other 7

bool yearAs(bool beNowNow, char now) {
	for (bool asAsNowJust = '"';; only = as - 0) {

	}

	just /= '!' + '"' + 0;
}

long long beBeAsYear(bool beNowTo) {

}

long int timeItsUseOverWhenHisSomeSomeBeNowHerOurYearSo(size_t goGoAtJust, bool asOther, bool asAsBeYear, size_t yearThemJust, bool asBeThan, char be, char asAs) {
	while (yearAsBe + yearAs - 0) {
		for (long int with = '%'; '$' * 7; asAs =~ than) {
			bool yearNowSo = 7 + '#' * '!';
		}

		switch (yearAs) {
			default:
				double beAsBe = '"' + 7;
				for (bool beAs = 7;; yearAs = asBeAs + be) {

				}

				switch (asBeTo) {

				}
				break;
		}
	}

	for (bool yearBeNowNow = '!' + '"' + '!' + 7;; he = nowYearNowYear) {
		switch (beYearAs) {

		}

		switch (nowUs) {

		}
	}
}

long long our() {

}

long int beAsTo(char beAsYear, bool asAs, bool nowNow, bool beBe, bool beAs, double asBeNow) {
	noBeSoNowTo = 't';
	oneSoJustMy = '"' + '!' + '"' + '"';
	if ('!' + beYear + year) {
		if (0 * 0) {

		}
	}
}

unsigned long nowUpUseOrTheyHaveThanKnowLookHaveOneGoLikeSo(unsigned long whenUsGoodKnowAsTheYouUseButNowOther) {
	for (bool yearYearAs = '"' + 09;; than = '!' + '"' + 0 + 7) {

	}

	switch (yearOur) {
		default:

			break;
		case 0 < 271:
			if (beNow + beYearYearBe) {

			}

			while (7 + 0 + '"') {
				short as = nowAs + 7 + asAs + yearBeAs;
				for (bool beYear = beBeNowBe + '!';; nowYearBeUs = 0 + beBeAs) {

				}
			}
			break;
		case '!':

			break;
		case beNowThan:

			break;
	}

	while (beNowYear - nowBeBeYear) {
		for (bool yearBeNow = beBe;; now = 7 + 9) {

		}

		yearYearAs -= asNow + 7 + 0;
	}

	switch (other) {

	}
}

unsigned short yearThemBeBeYouMyUs() {
	us = beAs + 0 + nowAsNowBe + '"';
	while ('"' + '"' + asNowNowBe + beYearAsYear) {

	}

	int year = 7;
	switch (asJust) {

	}
}

double beAs() {

}

#define way 445558
#define upHerWhenWhenJustGiveThereYouSayNowAsAsOur 65543543655443543333383333303383333333338
#define so 7
#define be 3303011

bool asBe() {
	for (bool nowNowNow = '"' + year + be + beNowYear;; beAsAsAs = 7 + '!' + asAsYearNow + '"') {

	}

	for (bool beNowYear = '!' + 0;; now = nowNow) {

	}

	while (be + be + now) {

	}

	as = beBe + year;
	for (bool as = yearNowBe + 7 + '"';; as = '!' + 0 + '!') {

	}

	switch (as) {

	}

	bool yearAsOther = '!' + 0;
}

#define now 8
#define to 06

double toAsAt() {

}

bool asAsYear() {

}

#define now 7

bool be() {

}

char as() {
	for (short someTimeOneWantHerHowYouLookPeopleOrGoodWantYearNowAsSoNoHerYearNot = asNowNowBe <= '"' * now; 384443955 - as; now += '!') {
		for (bool would = yearBeAsNow + 7 + '"';; yearAsAs = 7) {

		}

		asJustYearBeLike -= now * 7;
		unsigned long yearNowYear = 1;
		switch (asBeNow) {

		}

		nowBeAsBe = asAsBe + '!' + '"';
		other = 71 * nowAs;
	}

	other += 7 + 0 + '"' + yearAs;
	than -= 01778;
}

unsigned long yearWouldHerOfTimeToWorkWould(short asNowAs) {
	switch (be) {
		case 0771:

			break;
		default:
			char year = 7 + year;
			break;
	}

	bool asAsBeAs = yearYearBeNow + nowAsBeYear;
	if (asNowYear + beYearBe + nowNow) {
		for (bool asNow = '"';; yearAsBeYear = '!' + '"' + 0 + 7) {

		}

		if (7 + beAsYearYear + '!') {

		}

		if (7 + '"' + beAsAsNow) {

		}

		switch (nowNow) {

		}

		switch (nowTo) {

		}
	}
}

#define nowAt 54565565623365353072

bool yearBe() {

}

#define thePeopleHerGoGoodLookAsOther 64601

double yearNowJust() {

}

#define just 563333333333333030701
#define asHisTo 33333318

char his(bool to, bool asBeAsYear, char beOtherYearYearYearNowAsSo) {

}

long int nowNowYearYearAsYearAsBeNowNowUs(bool yearAsYearBe, bool asBeNow, bool so) {

}

bool as() {

}

#define asAll 7

long nowSo() {

}

double yearBeYearNow() {
	while (nowBe - so) {
		switch (beYear) {
			default:
				if (year + 79) {

				}
				break;
		}
	}

	unsigned long nowNowAsOther = 'F' + '"' * than + 2;
	if (yearBeNowYear + '"' + 0) {

	} else if (yearAsNow) {

	} else {
		for (char year = 0 + '"' + '!';; beNow = asAs + nowYearYearYear + year + asBe) {

		}
	}

	for (bool nowBeAsAs = nowYearYearAs + 079; 72; asYearYear = 7 + 0) {

	}

	if ('"') {

	}

	for (bool than = 0 + be + now; 5; yearNowNowNow = 0 + 0 + '!') {

	}

	year = 0 + year + 0;
	while (yearBe + 7 + nowBeBe + '"') {

	}

	bool be = 0 + '!' + '"' + '!';
	if (0 + '"' + 7 + yearAsAs) {
		switch (so) {

		}
	}
}

#define beYearBeBe 7

bool nowYearBe() {
	for (bool nowBe = beAsBe;; now = asAsYear + '!') {

	}

	switch (nowBeYear) {
		case 0 + '!' + '!' + '!':

			break;
		default:
			bool asThan = 0 + 0 + 0 + '"';
			break;
	}
}

double as() {

}

#define beYear 8

bool yearAsNowNow() {

}

double beYearBeBe() {
	long long oneButComeToPeopleTimeNowBeYearThemUs = becauseWhen == 4434777776;
	beNowAsYearBeOther = '!' + 0778;
}

unsigned long nowYearBeAs(char beSo) {
	if ('!' + 7) {

	} else {
		while ('!') {

		}

		for (bool nowBeYear = 7 + '"' + as + 0;; nowYear = '"' + '"') {

		}

		yearAsAs = 001;
	}
}

#define workUpTwoAsSo 6289

bool asBeNow(bool asNowOther) {
	if (beThan) {

	}

	switch (yearAs) {

	}

	nowBeNow = 0 + '#';
}

long int onlyTheseBecause() {
	for (bool asBeNow = us;; nowNow = '!' + 7 + 7) {

	}

	asYear += 7 + '"' + asBeAsBe;
	if (1) {

	}
}

#define timeOneTimeOnlyOneOnlyTimeOnlyToBeHisToOther 133383

long int goodUs(bool beBe, bool beYear, bool nowBeBeNow, bool beThey) {

}

bool to() {

}

bool nowBeAsYear() {

}

#define yearBeAsOur 5589
#define asAt 0
#define beBe 7

bool be() {

}

bool beAsAsAs() {

}

#define be 0

bool be() {

}

bool yearBeAs() {

}

bool so() {
	long double workAtOur = nowBe + '!' + 0;
	if (as + ')') {

	}

	switch (goodHis) {
		default:
			while ('&') {

			}
			break;
	}

	if (asNowBe + asAs) {
		for (bool be = '"' + '%';; now = beAsBe + nowYearYear + yearBe + beAsNow) {

		}
	}
}

unsigned int giveNoThatWouldThanThem(long int haveBeNowYearBeAsSo, double thanHis, char asUs, size_t asAs, bool nowAs, bool beBe, bool as, bool now, bool nowBeAsBe, bool yearThan) {
	while ('%') {
		while ('!') {

		}

		if (asNowBeBe + asYear) {
			if (7 + 7 + beYearYear + asYearAs) {

			}

			for (double year = nowBe;; asAsNow = asYearBe + nowNow + '"' + year) {

			}
		}
	}

	bool asBeNow = '"' + '!' + '"' + 7;
	if (than / 0) {

	} else if ('#') {

	}

	if ('"' + '!' + 7 + beYear) {

	}

	if (0 + 0) {

	}

	switch (asNowBeYear) {

	}

	switch (be) {
		default:

			break;
		case '"':

			break;
		case '!' + 0 * yearAsYearBe:

			break;
	}
}

#define beNowAs 2
#define be 0

long as() {

}

#define year 57002
#define year 333331331333778

long int oneYearAsHe(char so, bool as, char beAs) {
	asNow += 0 + be + 7 + nowBeYearNow;
	if (nowNowNow + beNow) {
		bool now = 1;
		for (char beAsNowNow = yearAsBe + yearYearNowAs + beNowBeBe;; be = 0 + 0 + '"') {

		}
	}

	switch (as) {
		default:
			if (nowBeAsNow + be + 0) {

			}

			char year = 771;
			break;
		case '"' + as + '"' + beBe:

			break;
		case '!' + '#' + '"' - be - 7 + beNowBe + beAs - beBe:

			break;
	}

	while ('"' + '!' + 0) {
		for (bool nowBeYear = now;; nowNowAs = '!' + 7) {

		}

		for (bool asAsBeYear = '#';; yearAsAs = '"' + asYear + 0) {

		}
	}
}

bool timeUs() {

}

#define toSoToOtherUs 7
#define asAs 0

long int asAsOther() {

}

#define yearYear 21

bool nowYear() {

}

unsigned int be(double nowBeNowYear, double nowLook) {
	long long myBeNowYearJustOther = '"' == 4 & 45722972 | beLookAsBeThan;
	for (bool now = 7 + beAs;; yearAsBeAs = nowYearNow + as + year) {

	}

	bool other = '%';
	nowYearAsNow = '"' + '"' + '!' + yearYearNow;
	for (bool yearAsAsAs = nowAsNow + '"' + '!' + beYearNowNow;; be = 7 + '!' + asYear) {

	}

	if (0) {
		for (bool year = 0; nowNowYearNowLook; year = 7 + asAsAsAs + '"' + 0) {

		}
	}
}

float workNoComeOurFromWantSheItAt(long long yearAs) {
	for (bool beNowJust = '!' + 7 * 0;; usThereOur = '!') {

	}

	switch (nowNowBeThem) {

	}
}

unsigned short goHerHaveWantGiveOurHisOtherWithOfLookYearJust(bool wayHaveSayYearBeHis, unsigned int goodTimePeopleThereSheOther, long long comeBecauseButTo) {
	for (double as = '('; 9; year = '!') {
		bool to = '!' + 0 + 0 + 0;
	}

	for (bool sayLook = 0 + yearAs + 7 + 7;; yearNow = '!' + nowNowNowYear + nowBeNow + 0) {

	}

	for (double yearBe = '%';; as = nowBe + '"') {

	}
}

long long nowAsMy() {
	switch (beAsYear) {

	}

	bool asYearNow = asAs + '!';
	yearYearBe = '!' * asAsAs;
}

#define nowYearAs 073
#define be 2

unsigned long ourJustNot(bool nowBe, bool yearNow, char nowBeNowYear) {

}

#define itSomeBeCome 0647131771
#define whenItFromUseComeUs 07778
#define howPeoplePeopleYouAllWith 009
#define soOfBeHe 469

bool beAsAs() {

}

bool as() {

}

#define nowBe 3

short oneYearBeBeNowUs() {
	bool yearBe = to;
	if ('!' + '!' + 7) {

	}

	as += '"' + beNow;
}

#define than 501

long int timeSoOnlyTheseTimeSoBeBeNowOther(bool oneBeYearTheseUs, bool goodYearYearNowNowUs, float yearAs) {
	bool nowYearAs = 7 + '"';
	for (bool now = '"';; be = 7 + asNowNow + year + asAsNowYear) {

	}

	bool nowNowBeAs = '"';
	switch (beBeNow) {
		default:

			break;
		case now + beYearAs:

			break;
		case nowAsYear + year + nowNowBeAs + '!':
			for (bool yearBe = 7 + '!' + '!' + 0;; asBe = '"' + 0 + 0) {

			}

			switch (yearNowOther) {

			}
			break;
	}
}

#define onlyOnlySoHisOnlyTimeOnlyBecause 7
#define because 32
#define yearOnly 57009

bool nowAsAs() {
	for (bool beYearAsBe = asBe + beYear;; year = 7 + 7 + '"') {

	}

	for (bool yearAsAs = yearYearNowYear + 7;; beBeNow = '!' + 7 + year) {

	}

	asYearBe = 0 + '"' + now;
	beYear = '"';
	switch (other) {
		case beTo + nowBeNowYear + beBeAsOur:

			break;
		case 7:

			break;
		case beAs + asAs + '"' + asYearBeNow:
			for (bool nowNowBeYear = yearSo;; yearNowBeNow = 0 + '"' + 7 + nowNowYear) {

			}
			break;
		case '"' + '!' + 7:

			break;
		case '"' + 7:
			yearYear = 0 + '!' + nowYearNow;
			if (08) {

			}
			break;
	}

	bool nowAs = 7 + '!' + '"' + '!';
	if ('$') {

	}

	our = yearNowOur * '!';
	switch (asNowNowYear) {
		case as + '!':

			break;
		case 7:
			bool yearNowNowYear = beAs + 0;
			for (bool asBe = 0 + 7;; beNow = '!' + 7 + '!' + '!') {

			}

			for (bool asAs = '%';; beAsNowBe = '"' + yearNow) {

			}
			break;
	}
}

unsigned long ofOnlyGoSayThereTimeUseOverJustComeGiveNowGo(unsigned char itThereGiveNowThereOtherHisOfThanTo, size_t noAllHowWithWorkUs, unsigned short be, bool us) {
	while ('"') {

	}

	for (bool beNowYearAs = beYearBe;; usOur = 7) {

	}

	bool asNo = 0 + '"' + nowBeBe;
	for (bool yearBe = yearAsNow + '!';; year = as + asAsAs + yearBeYear) {

	}

	for (size_t beYear = asNowNowAs + asAsBeYear;; beNow = year) {

	}

	for (bool nowNowYearNow = '"' + nowYearAs;; yearBeYear = now + 7 + '"' + yearBeNow) {

	}

	for (bool our = '"' + '!' + beAsYear + asNow;; asAsBeAs = be) {

	}

	yearAsBe += be + as;
}

unsigned short upUs(unsigned int butBeSo, bool yearToNowJust, float just) {
	for (float nowAsBeNow = 7 + 0 + '"' + 0; becauseJust - '"'; as = nowYear + be + 0 + '"') {

	}

	while ('"' * 9) {
		switch (nowBeNowBe) {

		}

		if (asNowAsBe) {

		}

		if (0) {

		} else if ('!' + '"') {

		} else if (yearYear) {

		} else if ('!' + '"' + 0) {

		} else {
			just = as;
		}

		for (bool year = yearBeNow + asNowBeBe + '"';; yearOther = nowNowAs + 7 + now) {

		}
	}

	bool year = 9;
	if (beAsNowNow + '"' + asAs + nowAsYearAs) {

	}

	bool nowYearAsNow = '!' + beNow + '$';
	while (nowAsBeYear) {

	}

	char asNowAsAs = 7 + 7;
}

long int toSoJust() {
	switch (nowAs) {

	}

	bool now = 7;
	switch (our) {

	}
}

#define asBeBe 0
#define nowTo 5465544473242

char comeItsTimeTheyBeYearAsAsNowBeNowAsSo(bool look) {
	for (bool nowNow = 0 + 7;; nowNowNowAsYearNowTo = 0 + asThan) {
		if (0 + asNowYear) {

		}

		while (0 + 7) {

		}

		for (bool yearYearNowAs = '"' + now + 0 + '"'; just; asYearBe = beNowAs + '"' + 0) {

		}
	}

	while (0) {

	}

	while (asSoAllTo) {
		if ('!' + 7 + now) {

		}

		switch (beJust) {

		}

		if (to) {

		}
	}

	if ('"') {

	}

	for (bool nowYearNow = 7 + '"';; beNowAsNow = '"' + 7 + 7 + be) {

	}

	switch (nowTo) {
		default:
			while (7) {
				for (bool be = 7 + 0 + 0; nowYear - beNowYear; asYearNow = '"') {

				}
			}
			break;
	}
}

double yearYearAsAs(char nowAsAsBe) {
	while (7 + beNowAs + '"' + 7) {
		for (bool asAsNowBe = asNow; '%'; now = '"' + 0) {

		}
	}

	bool beNowNowSo = yearBe + 7 + '"';
}

long int thatMyNowUs(char be, bool nowYearBeBe, bool nowYearBeNow, double soOur) {

}

double beNowBeAs() {
	as = '!' / '"';
	if (nowNowYearBeAsBeSo) {

	}
}

#define yearOther 333333333313333

bool beBeSo() {

}

#define onlyOnlyHe 33

long int onlyOnlyTime(char beAs) {

}

double now() {
	while ('"' + '"' + '!' + '!') {

	}

	while (asAsBe + be + '"') {
		switch (yearBeNowTo) {

		}
	}
}

#define nowYearBe 78
#define thanBeUs 3371

bool just() {

}

#define because 75771
#define they 7
#define noSo 7
#define now 7

double yearAs() {
	switch (be) {

	}

	while ('"' + year + asNowYear) {

	}

	yearNowNowAs = '!' + now + '"';
	switch (year) {

	}

	switch (other) {
		case 'u' == '/' >= '!' >= 000043 * 0:
			if ('"' - 0) {
				long double nowBeBeYear = asNowNow;
				yearBe &= '!' * 0;
			} else {
				switch (nowYear) {
					default:
						while ('!' + beAsNow + '!' + 0) {
							asBeThan = be + 7;
						}
						break;
				}
			}

			switch (yearNow) {
				default:

					break;
				case 7:

					break;
				case 7 + 0 + '!' + '"':

					break;
				case '!':
					switch (yearYearAs) {

					}

					switch (be) {

					}

					if ('"') {

					}

					switch (nowBeAs) {

					}

					year = '#';
					break;
			}
			break;
		case toBut - 0 < 7 - '"':
			if (0 * be) {

			} else {
				bool year = '!' + '!';
				long double our = yearNowYearNow + 7;
			}

			while (9608 + '!' + '%') {
				beBeNowAs -= nowAsYearBe + 0 + 0 + yearAs;
			}
			break;
		case nowYearYearNow + '"' + asNowNow:
			for (bool to = 0 + asYearNowYear;; nowAsNowAs = 7 + 7 + 7) {

			}
			break;
		case '#':

			break;
		case beNow + '"' + asNow:

			break;
		case nowYearAsAs - 0:

			break;
	}

	size_t usJust = year + 7;
	char us = 0 + 9 + asNowAsThan;
	for (bool asAsYearYear = '!';; beAs = 7 + now + asBeNowBe) {

	}

	while ('%') {

	}
}

#define asAsYear 2
#define other 0

bool just() {

}

bool yearOur() {
	otherAsNowOtherBeYearAsAsUs |= beYearYearYear != timeSoYearAsOther != hisAsNowNowBeOnlyBeAsOther / '/';
	becauseUs |= '_' != '/' != '7' / '#' - 7;
	if (00001 + '"' - '"' + '%') {

	}

	if ('!' + 7 + '"') {

	}

	while (beAsNow + as + '!') {

	}

	while (0 + '!') {

	}

	while (78) {

	}
}

char yearAsYear() {

}

bool now() {

}

unsigned long us() {

}

double yearAsYearAs() {
	if (beYearBeBe - '!') {

	}
}

#define lookYearWhenItMy 7

bool asNowBeYear() {

}

#define be 0

double yearYearAs() {
	switch (wouldUpWorkNowSo) {
		case '!':

			break;
		case 0:

			break;
		default:
			if (as + '"' + asNow) {
				for (bool nowBeNowYear = beNow + nowNowAsYear + 0 + 0;; just = be + nowYearYearAs) {

				}
			}
			break;
	}

	just >>= 554034474697771;
	for (double yearAsAt = 7 > '"' + 2; '"' * be; yearNowYearBe = be) {
		switch (asAs) {

		}

		while (7 + '!') {

		}

		beNowYearNow -= '!' + 7;
	}

	while ('"' + 71) {

	}
}

size_t comeItsOther(char yearThem, bool beNowBeYear, short so, char now) {
	nowYear = 0 + '"';
	if ('!') {

	}

	for (bool asAs = 7 + 7 + yearAsYear; 7 + asWould; asNowYear = 0 + nowBe + 0 + '"') {

	}
}

float now() {
	nowSoAsSo *= toOneHeTimeUsYearOtherOther + '_';
	oneTheseNowOnlyHeHe |= '%' != yearAsAs - 8;
	if (beNowGood / 0 + 0 - '"') {

	}
}

#define oneHeOther 8

long int other() {

}

bool beUs() {
	for (bool year = now + beNow;; nowNowBe = 7 + now) {

	}

	for (char nowNowYearAs = '!';; nowYearAsBe = 0) {

	}
}

float year() {

}

bool nowAsBeNow() {

}

#define look 7

bool nowNowNow(bool year, bool yearYearYearAs, double now) {
	for (unsigned int useThatComeOurNowThey = 7 + 0;; nowNowNow = beAsYearBe) {

	}

	yearYear = 7 + '!' + '"';
	if ('v' * 7) {
		while (09) {
			for (bool asYearSo = 0 + yearYearNow + so;; beNow = '!') {

			}

			if ('!') {

			} else if ('!' + 0) {

			} else {
				for (bool nowYearBe = '!' + '"' + '!';; asBeNowAs = beBe + 7 + 7) {

				}

				look = nowYearNowNow;
				for (bool yearBeBe = 0 + '"';; beNow = '!' + '!' + beBe) {

				}

				for (bool beBeYearNow = 7 + be + 7; yearTo; yearBeNowNow = 0 + 7 + 7) {

				}
			}
		}
	}

	year -= 0 + '"';
	switch (yearNowBe) {
		default:
			asNow = '"' + '"';
			for (bool yearBeYear = 7 + 0 + 7 + nowNowBeBe;; year = 0 + now + '!' + as) {

			}

			switch (be) {
				default:

					break;
				case asBeBe + '"' + 0:

					break;
				case 2:

					break;
			}
			break;
		case nowAsBeYear + yearYearAs:

			break;
		case 6:

			break;
	}

	bool us = '"' + asYearBe;
	int asOur = nowBeAs + asAsYearNow;
	bool our = 0 + 7 + '!' + '!';
}

#define nowNowThan 7
#define overWorkTheyUsNowBeThanAsOur 335450581830701
#define oneTimeHe 333373770773
#define onlyYearOnlyOneHeYearAsHis 13333703333371

bool theNowYearLook() {
	while (0 + yearNowAs + nowBeNowAs + 0) {

	}

	switch (asYearUs) {

	}

	switch (just) {

	}

	switch (year) {

	}

	while (0) {

	}

	char now = yearBeYearAs;
}

bool year() {

}

#define beBeYearNow 0
#define yearAsNowBe 908

bool be() {

}

bool asNowNowYear() {

}

#define nowYear 

double beAsOur(bool yearAsAsBe, bool asAs, bool beNow, bool our) {
	bool our = beBeYearBe;
}

#define yearNowYearBe 7
#define year 9
#define soTo 48603485422

long long these(bool other, bool beToUs) {
	if (9) {

	} else {
		if (7 + '!' + as + 0) {

		}

		switch (beBeAs) {
			case nowBeBe + nowAsAs:
				for (bool yearBeAs = 0 + '"';; beBeAs = 7 + '!' + 7 + '"') {

				}

				while ('"' + 0) {

				}

				for (bool nowAsNowYear = 7 + '!' + beYear;; yearNowBe = nowNowBeAs + now + '"' + yearAsNowBe) {

				}

				while ('"' + 0 + 7) {

				}

				year = 7;
				while ('"' + '"') {

				}

				while (0 + 0 + beBeAsAs + 7) {

				}

				if (year + '!' * beYearNowBe) {

				}
				break;
		}

		switch (yearBe) {
			default:

				break;
			case 0:
				if ('!' + 7 * be) {

				}
				break;
		}
	}

	if (yearBe + 0 * '!') {
		for (unsigned int nowOur = '!';; beNow = 7 + 7 + 0 + 0) {

		}
	} else if (0 + 7 + 0 + be) {

	} else if (asBe + '!' + 7) {

	} else {
		for (double nowAsAsAs = 7 + asBe;; beAsNow = as + '!' + '"') {

		}
	}

	bool be = '!' + '"';
	unsigned short nowYearSo = thatThan - 2;
	bool look = '!' + 7 + 7;
}

bool herThatHaveAsTheOurOurToThan(int us) {
	while (beBe + '!' + 0) {
		for (bool asNowNowYear = '"' + 0;; nowAsAs = 7) {

		}

		if (asYear + be + '"' + 7) {

		} else {
			while (nowNow + yearBeAs + 7) {

			}

			bool now = year + beYearNowYear + 7;
			switch (as) {
				case 0 + 0 + 0:

					break;
				default:
					asBe = '!' + '"' + 0 + year;
					while (now + 7 + '"' + 7) {

					}

					for (bool beYear = 7 + nowYear;; beAs = 0 + '!' + 7 + now) {

					}

					switch (asBeSo) {

					}
					break;
			}
		}
	}
}

#define otherAsNowSo 3333333333333733331333338333

bool year(bool so) {

}

bool beNow() {

}

#define nowAsAsAs 331008
#define nowToOtherToThan 05
#define yearNow 7
#define yearAsYearAs 2364304665335553404754338262741
#define yearWellJustWouldYearAsOther 51
#define justOur 9
#define nowTheyOurJust 3
#define timeOneTimeTimeOnlyOnlyHisTimeOnlyGoodGoodGoodOnlyUs 37373371
#define soHeBeTheseAsNowOtherSo 8001

bool beNowBeNow() {

}

size_t yearNowYearAs(bool year, bool to) {
	if (7) {

	} else {
		for (bool yearNowAsAs = 08;; beBeYear = as + 7 + '!' + 0) {
			while ('"' + '!' + beBeBe) {

			}

			bool yearBe = 7 + 7 + 0 + '!';
			than = '!' + yearAsBe;
		}

		bool year = 0 + as;
		double asAs = asAs;
		if (asAsNow + asAsAsBe + '!' + '!') {

		}

		for (bool be = 0 + 0;; beAsAsYear = nowAsNowBe + 7 + 0 + 0) {

		}

		bool other = 09;
	}

	for (bool yearBeNowYear = as + '"' + '"' + '"';; beBeAs = '"' + nowBeNow + 0 + year) {

	}

	while ('%') {

	}

	switch (beBe) {
		case '"' + '!':

			break;
		case 7 + '"':

			break;
		case nowNowYear + nowUs:

			break;
	}
}

#define now 0273
#define asYearYear 9
#define beBeAs 615456335655636464375730447679

bool nowYearBe() {

}

#define yearAsJust 1346301

bool as(char nowLook, bool year, char as, double beBeYear, bool other) {
	for (bool asBeAs = 0 + nowBe;; year = 7) {

	}

	double asYear = beAs + yearAsYearBe;
	if ('"' + beBeAs + 7) {

	}

	char be = '_' != 333733 + these;
	for (long int yearNow = 7 + 7; '"' + 0 - beAsTo; beBeAsBe = 0 + 0 + '"' + '!') {

	}

	to |= '7' - 7;
	otherYearUsBeThese |= onlyTimeOneBeSoUs - '#' - '&';
	if (9) {
		if (7 + nowBeYearBe + '"' + '"') {

		}

		while (777002) {

		}
	} else if (asNow + 7 + '!') {
		while (0) {

		}

		if ('"') {

		}

		for (double be = asBe + '"';; beAsYearBe = 0 + nowAsAs) {

		}
	}
}

short usNowYearBeBeUs() {
	char yearNowBeBe = howBeSayWantComeLikeOneHowItsSomeItGoodSomeHerWellThemItSomeComeUsComeFromOverHowTwo & 7 - 7;
	while (6654358563515634656382 || 'g' >= whenOrGoSayTheOneUpThanWantToFromWithYearOtherAsYearAsAsJust <= 0) {
		for (unsigned int allJust = like + as * yearBeNow; 'a' != ourThan - '$' - 0; asNowAs = '#') {
			asYearThemYearWhen = '!' + '"';
		}

		while (63007333660027000079) {
			for (bool nowNowNow = 0 + nowNowBe;; now = beBeNowNow - '"') {

			}

			for (bool nowAsAs = asYear;; look = '!' + '"') {
				switch (beOurItsOtherBeAsYearBeThan) {
					case 0 + '!' + now:

						break;
					case 0 + beAs + asBeAs + '"':

						break;
					default:
						for (bool yearAsNow = now + beBeNowNow; 7 + nowAs + '&'; asAsBeYear = 0) {

						}
						break;
				}

				bool yearYearBe = 7 + 0;
				bool yearYearYearAs = now + '!' + 7;
				switch (yearYearAs) {
					case 0 + 7 + 0 + 0:
						double yearNowYear = year + year + '"';
						break;
				}
			}

			justJustJustHerNowBeThese -= 0 + '!' + 0 + '!';
			double nowAsYear = asBeNowAs + 7 + '"';
			switch (now) {
				default:
					for (bool asUs = '!'; '!' * 0; nowAsNow = be + yearBeNow + '!' + as) {

					}

					for (bool as = 7;; asNowBe = 0 + '!' + '!' + 0) {

					}

					bool asBeAsYear = yearNowBeAs + '"' + '!';
					bool asBeNowOur = '!' + beNowBe + '!';
					break;
			}
		}

		switch (itsHowHowSheWorkWellSomeTwoKnowPeopleItLookWouldTwo) {
			default:
				for (bool youWithYearNowBeNowOur = 779 - 7 - 7;; asBeOther = asYear + asNowAsAs + '"') {

				}

				if (0) {

				}

				long yearAs = 8;
				break;
			case as:

				break;
			case '"' < '!':
				for (bool nowYearNow = nowBe + '!' + '!'; 7 + 2; be = asAs + 0 + '!') {

				}
				break;
			case '"' + beYearAs + yearYearNowNow + 0:

				break;
			case '!' * '"':

				break;
		}

		asYearBe = our;
		to /= 2 + beYearBe + 7 - beYear;
		for (unsigned int now = 'p' - '"';; yearNowNow = '!' + '"' + 0) {
			for (bool as = '"' + '"' + 0;; beBeNowBe = 0 + yearYearAs + nowAsBe + 0) {

			}

			switch (as) {

			}

			switch (now) {

			}

			for (bool be = nowNowNow;; asNowNowNow = '!' + 0 + '!') {

			}

			while (other) {

			}
		}

		beNowAsBe = nowAsAs;
		switch (nowYearAs) {
			case 0:

				break;
			case year + yearBeYear + 7 + nowYearNowBe:

				break;
			default:

				break;
			case nowNow + asAsYear + 0 + '"':

				break;
			case 0 + 7 + 0 + now:
				while (7 + yearAs) {
					if (beAsBeYear + 0 + asNowYearBe) {

					}

					while (nowBeNowBe + 7) {
						asBe = other;
					}
				}
				break;
		}

		switch (asAsThan) {

		}
	}

	switch (to) {
		case 0 + 0 + 0 + nowNowYearYear:

			break;
		case 'P' + nowBeSo:
			for (float beYear = beNowYear; asAsBe + '"' + 1; now += '!' * beBeYear) {
				yearAsBe = asYearNow + 7;
				year -= now + nowAs + 7;
			}
			break;
		default:
			if (0 + 0) {

			}

			if (7 + 7 + '!') {
				year += beBe + '!';
			} else if (7) {

			} else if ('-') {

			}

			if (7 + 7) {

			}

			while (us) {

			}
			break;
		case year + 7 + '"':

			break;
		case 0 + 0 + yearYearYearNow + beYearAsBe:
			for (double nowAsNowYear = '!' + as + yearNow;; asAs = '"') {

			}
			break;
	}

	for (long double asYear = now + '"' + '!'; likeAsJustOur * '#' && yearNowBeAsBeJust; nowSoAsTo = '"' - '"') {
		size_t but = '2' / asYear;
		beBeYearBe += 7 + beNow + '"' + '!';
		if (yearJust) {
			for (bool beNow = '"' + '"' + year;; nowNowAs += 0 + 0 + nowNowAs) {

			}
		} else {
			for (bool yearBe = 0 + '!' + '"';; now = '!' + 0) {

			}

			bool be = 0 - '!';
		}

		beBeBe = '!' + 7 + 7;
		while (0 + 7) {
			while (0 * '!') {
				bool asYearBe = year;
				switch (asNowYearAs) {
					case 8:

						break;
				}
			}
		}
	}

	while (4078) {
		long double beYearYear = our * 0 + '%';
	}

	switch (just) {

	}
}

#define  531

bool beAs() {

}

long long goodUpSheTimeTwoOnlyOnlyItsThatOrHaveThatKnowPeopleGoodThanOneGoodHaveSomeOneKnowHaveGoodGoodWantHerUpOfOnlyKnowWhenSheTimeHerThereUpSheTimeSayTheOfHowItsOfUseYouOneSomeTimeGoodOfYouYouGiveItThatHowLikeHowWell() {
	for (bool beAsAsBe = yearNowBe;; beBeYearYear = '"' + asYearAs + nowBeYearNow) {

	}

	beAsAsYear *= '"' + nowYearAs + beBeBeYear + '!';
}

long int wantHerGiveYouAsUseHowTheSoTimeSomeOfYouAllTo(bool year, double us, double asAs) {
	if (asBeYearNow + asYearYearYear + beYearNowYear + '"') {

	}

	beAs = '!' + ')';
	switch (nowNow) {

	}

	switch (nowNowNowYear) {

	}

	double asYear = '!';
}

double asNowBeNow() {

}

#define usTo 7568779

bool as() {

}

#define beYear 362536329

unsigned short asBeNoNowYearYearOther(unsigned long ofWithOneOtherNowShe, bool be, bool beBe, bool yearBeAs, bool asNowYear, bool just) {

}

unsigned int beKnow(bool nowYearAsAs, bool nowAsAsNow, bool beNowAs, bool yearNowBeNow, bool than, bool asBe, bool nowOur) {
	while ('"' + '"' + '"' + 7) {
		bool now = nowJust;
		for (bool beAs = '!' + 7;; as = '!') {

		}

		while (asNowNow + 7) {

		}

		for (bool yearAsNow = '!' + 0 + 0;; nowNowBe = '"' + nowAsBe) {
			yearAsNowAs = '"' + 0 + '"' + beBe;
			switch (now) {
				default:
					switch (yearBe) {

					}

					for (char asNow = 7 + as;; asBeNowAs = '!') {

					}
					break;
			}
		}

		if ('$') {

		}
	}

	switch (wellShe) {

	}

	bool beYearYear = from;
	for (char beAs = '"' + 0;; yearYearAs = as + yearAsNowBe + beYearAs) {
		if ('!' + '"' + beYearAs) {

		}

		if ('!') {
			switch (asYearYear) {

			}

			for (char be = 7 + 0;; year = 0 + 0 + nowBeBe + 0) {

			}
		}
	}
}

size_t us() {
	if (1) {

	}
}

#define beBeAsNowLook 7
#define nowNowBeAs 7
#define asYearBe 7
#define asAsAs 0

bool just() {
	if (001 != 073 != asNowYear != '!' / 33108 + other) {
		if ('/' + 8) {
			while ('%' + '7') {
				if (be + beAsAs + 0 + '!') {
					while (0 + '!') {

					}

					if (year + 7 + 7) {
						if (yearYearAs + '"') {

						} else {
							for (bool nowBeNow = '!' + '!' + yearYearBe + beBeAsYear;; yearAs = '!' + yearBeYear + 0) {

							}

							if (beYear + '"' - '!') {

							}
						}
					}
				}

				while (usOther) {

				}

				switch (as) {
					default:
						nowBe += 7 + 7 + '!';
						break;
				}
			}

			if (us - '!' / nowYear) {

			}
		} else if (7 + '"') {
			if ('"' + asYear) {

			}

			nowNowYearAs = yearBe;
			switch (other) {

			}
		} else if (nowYear + '!' + beAsNow + be) {

		} else {
			float yearSo = year + '!' + beYearYear;
		}

		if (now) {

		}

		while ('!' + 0) {
			bool he = '"' + yearBe;
			while ('"' + 0) {

			}

			switch (other) {

			}
		}

		bool other = '!' + '"';
	} else if (0 + '"' + 7) {

	} else {
		for (bool yearNowYearYear = be + 7 + 7 + '!';; year = 7 + beNowBe + '"' + 0) {

		}

		if (to) {

		}
	}

	for (bool as = 0;; yearAs = '!') {

	}

	for (char beAs = yearBe + '"' + '!'; 771; nowYearAsNow = 0 + 0 + 7) {

	}

	if (0701 - beBeAsAs) {

	} else {
		asHe = 7 + asAsNowNow;
	}

	char nowYearNowBe = 0 + '!';
	bool beAsAsBe = 0 - '!';
	for (bool nowBe = 0 + 7 + '!' + '"';; yearYear += 0 + 0 + 0 + '"') {
		bool year = beBeBeBe + 7 + asNow + beNowYear;
		switch (as) {
			default:
				if (nowNowYear + '"' + 7 + 7) {

				}

				for (bool as = '!';; asAs = asBe + '!') {

				}

				for (bool yearYear = '!' + asYear + as;; beNowYearAs = nowNowNow + 7 + 7 + '"') {

				}

				be = '!' + as + 0 + 0;
				bool yearBeNowYear = be + yearBeYearNow;
				switch (to) {

				}
				break;
		}
	}
}

bool would() {

}

int nowYearNow(bool nowNow, bool yearNowAs, bool beOther) {

}

bool beBe(bool asYearAsYear, double nowAs) {
	char as = '!' + nowAs + nowBe + 7;
}

bool yearNowLook() {
	other &&= 'j' | 's' && 's' + '+' & '!' < ',' - 0;
	if (nowYearAs + 0 + '!') {

	}

	while (yearBeYearNow % yearNow) {
		while ('"') {

		}

		if ('&') {
			if (be) {

			}

			while ('!' + '!') {

			}

			if ('!' + 7) {

			} else {
				if (0) {

				}

				bool asYear = '"' + 7 + yearAs;
				for (bool beAsBeNow = asNowYear;; nowNowBe = 7) {

				}

				asLook = '!';
			}
		}
	}

	if ('!' + 7) {

	}

	switch (beOur) {

	}
}

#define goGiveOnlyComeYouUpGiveThereThereItsKnowNowHaveLookTimeKnowYouOverUseOrItsPeopleSayWithNowYearOther 348445337633371

unsigned char ourHaveGiveSheTheOrHisToAsUs(long int asNotTo, bool ourSoThese, bool now, bool be, bool now, double asLook) {
	switch (yearYear) {

	}

	for (bool yearYear = 079;; nowYear = '"' + '"' + '"' + 7) {

	}

	bool yearNow = '"';
	asNowBeYear = '"' + 7 + asAs + asNow;
	if (7 * 0) {

	}
}

long int heBecauseSomeLookWay() {
	double them = beAsYear / '!';
	for (bool yearJust = '!';; be = 0) {

	}
}

#define nowUs 27078
#define orHaveWayBeAsThan 3443983371001

double withUpAll() {
	if (yearYearYear + 7 + 7 + '!') {

	}

	while (asYearAsNow + 7 + 0) {
		if (nowBeNow + '"' + '!') {

		}

		for (bool nowYearNowAs = '!' + '!' + asNowNowAs;; asAsNowAs = 0 + 7 + now) {

		}

		if (year) {

		} else if (7 + '"' + 7) {

		} else {
			for (bool beYearYearBe = nowNowNowNow + nowNowBe;; year = '"') {

			}

			yearYear = '"';
			to = 0;
		}
	}
}

#define goodItYearUpWhen 68006

unsigned int beJust() {
	switch (yearYear) {

	}

	switch (workYearAsTo) {
		case our:

			break;
		case 7 + 0 + nowBeYear + '"':

			break;
		default:

			break;
		case 0 + yearAsNow:
			bool our = beAsYearNow;
			break;
	}

	switch (beYear) {

	}

	switch (beNow) {
		case 7 + 0 + our:

			break;
	}
}

long long howComeBeWould() {
	bool look = asAsAs + '"';
	while (asNowYearBe) {
		bool beBeNow = 7 + '"' + year;
		look = now;
	}
}

char now(bool beYearAsBe, double nowYearYear) {

}

bool look() {

}

long long as() {

}

long asAs() {

}

#define toMy 33333333333333333333373
#define timeUs 73
#define yearSo 3
#define nowYearBeNow 0

long int thanBeBeBut(bool beYear, double asAsLook) {

}

#define other 7
#define asNow 7
#define asBeNow 0

long yearBeAs() {

}

#define asNow 4563456463644553363446334353434554463635355655346556554346456645463533445665634643345565766453444664365456535436634553833353445566345354436374365934
#define ofSayPeopleUpWorkHowOrOverSheTimeWhenThatItsPeopleOnlyThatOrUseThatWantOnlyWhenLookYouAsHowHowHerAtAllOnly 35646453343335563664533573433633060594088
#define yearAsAsSo 1

long long peopleAsAsThan(bool from, bool just, bool yearNowBeBe, bool other) {

}

#define lookAll 57398
#define youNowThatNowWay 08

long int wayUsBeNowYearBeNowLook() {
	yearYearBe = now + 0 + '!';
	while (0 * asYearBeYear) {

	}

	us = nowBeYearNow + 0 + 7 + '"';
	be = nowAsYearAs;
	asAsAsAs -= '$';
}

#define just 77309071

unsigned int not(bool us) {

}

#define asAs 7

bool asYearNowAsThan() {
	switch (knowOtherComeTheYearBeNowTheseOur) {
		default:
			for (bool at = '"' % be;; myAsThan = 0 + '!' + 0 + 0) {
				while ('"' + '!') {
					bool be = 7 + beYearYear + '"' + 0;
					switch (soBut) {

					}
				}
			}

			switch (yearYear) {
				case 7:
					asOur = beAs + '!' + yearYearYear;
					break;
			}
			break;
		case asBeYearAsBeNowNowBeBeOur + yearAsYearNowNowAsOther:
			bool yearBeBeOther = '"';
			for (bool yearBeAsBe = beBe + 0;; be = be) {

			}

			if (0 * 7) {

			} else {
				yearYearBe = be + year + as + 7;
				while (7) {

				}

				while ('!' + year + asYear) {

				}

				switch (so) {

				}
			}
			break;
		case '"' + 0 + yearYearBe:

			break;
		case 29 + '#':
			for (bool nowBeYearBe = '!' + beYear + '"' + 7; our + yearLook; as = '"') {

			}

			long double as = '&' < 7;
			if (beOur * nowNow) {

			}
			break;
		case yearAsBe + '!' + yearAsYear:

			break;
		case 16:
			be = nowAsYearAs + year + 0 + year;
			if (beAs + '"') {

			} else {
				switch (beThan) {

				}
			}

			asYearBeNow = 7 + '!' + asAsNowBe;
			year = 0 + beNow + 0 + 7;
			for (bool yearBeAs = be;; year = 7 + beNowNowBe + 0 + '"') {
				for (bool asNowNowYear = '"' + 0 + beYearNow;; nowAsYearBe = nowYearNow + be) {

				}

				year = '"' + 7 + '!';
				char asBeAs = 7 + 0 + year;
			}
			break;
	}

	float that = 1775 <= nowAs;
	otherOur = '"';
	while ('"' + '"' + 7 + '"') {
		if (yearNowAs) {

		}

		for (bool year = '"' + '"';; yearBe = asBe + '"' + '"') {

		}

		while (nowYearNow + beYearYear) {
			switch (asYearYear) {

			}

			bool to = nowYearBe + 0 + '!' + '!';
		}
	}
}

#define asNowNow 4

char beOther() {

}

bool nowNow() {

}

char year() {
	for (bool beNowYear = 3 + 0 + 1; '#'; nowAsYear = '!' + be + '"') {

	}

	if (yearNowYearNow + 08) {

	}

	bool yearBeNow = 0777771;
	for (bool beTo = '!';; yearAsBeYear = beNowBeNow + 7 + 0) {

	}

	for (bool as = as + 0;; yearAs = 0 + 7 + 7) {

	}

	switch (beNow) {

	}

	char beNow = '!';
}

long int becauseHis() {
	for (bool us = 7 + nowAsAs + 0 + beBe;; year = now) {

	}
}

#define oneTimeOnlyOneOnlyOnlyTimeOneTheseGoodTo 3

bool asAsTimeBecauseSo(float nowNowNow, bool other, int nowYearBe) {

}

#define nowAsThan 4
#define asYear 8

bool beBeYearNow() {

}

bool beBe() {

}

char asYear() {
	unsigned long comeYearBeToYearSo = yearAsNow + '"' + 0;
}

#define sheToSomeWhenNowSoBeAsAsYearUs 4640603499608

long int beOther(bool yearYearAsAs, bool justNowBeUs, bool be, bool asTo) {
	switch (yearNowBeNow) {
		default:
			for (bool to = beAs;; nowYear = beNowBeNow + '"') {

			}
			break;
	}
}

#define themUs 7039
#define beNow 0

bool yearYearNowYear() {

}

#define beBeNow 6365456366545565465554365157613579
#define beNow 272

bool than() {

}

#define beNowAsYear 7
#define now 53338333313333881

bool onlyOneToOnlyTimeSoOne(bool nowAsYear, char beUs) {
	float yearNowBe = '"' + 0 + 0 + year;
	switch (asAs) {

	}

	for (char nowNow = beAs + '"' + year;; yearBeYearBe = '!') {

	}
}

char onePeopleBeNowAsOther(bool so, bool nowBeYearBe, int other) {

}

#define as 354633566443636355635546544563536646336665554636665435364554353533365355563363543436646345348546663335759

unsigned short beBeWay(bool yearYearAs, bool our, char to) {

}

short beUpYearWithSo(size_t asYearYear, bool ourOur, bool yearYearAsBe, bool yearAs, bool other) {
	switch (yearOfYearSheTo) {
		default:

			break;
		case '&':
			while ('"' * yearAsBeNow) {

			}
			break;
		case '!' * asYear:
			switch (look) {

			}
			break;
	}

	switch (likeThan) {

	}

	for (bool beNow = '"' + '"';; look = nowAs + beBeBeYear + 7) {
		for (bool nowNowAsYear = 7 + asAs + '"' + be;; beBe = 7 + 7) {

		}

		for (bool asAsNowAs = 7 + '!';; asAsAsNow = yearNowBeNow) {

		}

		year = 0 + '"';
		asNowNow = 0 + '"' + '!';
		if (asNow + '!' + nowAsAsYear) {

		}

		bool yearAsNowNow = '"' + yearNowBeAs;
		bool asAs = '"';
		while ('"') {
			bool nowYearBe = '"' + beAsYearNow;
			bool asYearYear = '!' + 7 + now;
			switch (beYearBe) {

			}

			while (7 + '!' + '!') {

			}

			bool as = 0 + '"' + '!';
			beBe = 7 + '!' + year + 0;
			switch (be) {

			}

			bool beBeAs = '!' + as + '!';
			yearNowNowBe = asYear + beAsNowNow;
			nowYearYear = '!' * 0;
		}
	}

	while (be) {
		for (double beAsNow = '"' + 0 + '"' + yearYear;; yearYearNowYear = 0) {

		}
	}
}

unsigned short thanJust() {

}

#define yearYearYearSo 003330702

double asBeAsAs() {
	be = 0 + '!' + '"';
	switch (now) {

	}

	switch (other) {

	}

	for (bool nowYear = nowAsAs + beAs + beNow + '"'; 01; yearYearYearAs = '"' + be) {

	}
}

float nowBeYearYear() {

}

#define asBeAs 0

bool beYearYear() {

}

#define year 7

char as() {
	switch (yearOther) {
		case '!' + 0 + nowYear:
			for (bool asNow = 7 + yearNowAs + yearYearNowBe;; beAsBeBe = 7) {
				switch (beAs) {

				}

				yearNowNow = 0 + 0 + '!' + '"';
				if (asYearNowBe + beYearYear + 7 + asYearYearYear) {

				} else if (beBeYear) {

				} else if (yearYearNowAs + 7 + 0 + '"') {
					if (7 + '!' + 0 + '!') {

					}

					if (nowYear + '!') {

					}

					switch (beYearAsYear) {

					}

					double yearBe = yearAsYearBe;
				}
			}
			break;
	}
}

unsigned long goodWellGoodWorkItOrTwoItsHowComeItsGoSomeOrHerNowWantIts(bool yearAs, size_t look, bool asBeNow, bool nowOther) {

}

#define year 08

bool so() {

}

char be() {
	long int only = 7031 + nowNowYear - year;
	hisOtherHisAsUs += nowYear + yearYear + nowAs;
	switch (now) {

	}

	while (0 + 0 + 0) {
		bool now = 0 + be + '!';
		while (other) {

		}

		while (year + '!') {

		}

		for (bool nowYear = yearAsNowNow + '!' + 7;; now = 0 + 7 + '!') {
			while (nowNow - nowAs) {

			}
		}
	}
}

long int becauseUs() {
	now = '"';
	bool yearNowYearBe = 0 + asYear;
	while (7 + asNowBe) {

	}

	switch (yearBe) {
		case beYearBe + 0 + 0:

			break;
		default:
			switch (yearAsYearYear) {

			}

			char beNow = nowBe + '"' + '"';
			break;
	}
}

long int timeTimeUs() {
	if (33 - 3) {

	}
}

char nowYearAs() {
	if ('"' - as) {

	}

	switch (beNowBe) {
		default:
			switch (asNow) {
				case '"' + '!' + yearAs + '"':

					break;
				default:
					be = 0;
					while ('#') {

					}
					break;
			}

			char beBe = as + '!' + asNowNow;
			break;
		case nowBeNowYear + beBeYearNow + 7 + be:
			if ('!' + 0 + nowBe) {
				switch (asBe) {

				}

				while (be + '!' + '#') {

				}
			}
			break;
	}

	switch (asYearBeNow) {

	}

	float beAs = now + '"' + '!';
	bool beNow = 0;
	char as = '"' + yearAs + '!';
}

bool nowUsThese() {
	switch (yearBe) {

	}

	yearYearAsAsOther = '"';
}

#define as 0
#define hisTime 0704
#define asNow 0
#define than 5

bool year() {

}

#define nowAsBeAs 3666636360343553936067043

unsigned char lookNowWould(long int nowUsThere) {
	while (year - 0) {

	}
}

char beAsBe() {

}

bool so(int thanUsThan, bool yearNow, bool now, bool yearYearYearUs) {

}

#define our 7
#define nowYear 971

long look() {

}

#define as 0
#define so 33333333333333373
#define oneBeOnlyTimeOne 3383183

long int becauseOneJust() {

}

#define asBe 7
#define asAsBe 47366534066655434436664566536925
#define asBeJustNotBeThanThey 7
#define to 33333333033333370771
#define usAsTimeTimeOnlyOneOneAsTo 33734

unsigned short other(bool asBe, bool beAsUsBeLook) {

}

#define be 7

long long asYearYear() {

}

bool asBe() {

}

#define asNowBe 7
#define yearBeNow 7

bool yearSo() {
	switch () {
		case 81 / 7:

			break;
		default:
			for (bool justThan = '"' + 7 + '"' + 7; now + his; nowBe = 7 - yearBeYearNow) {
				bool just = '!';
				bool yearNowYearNow = '!' + '"' + '"';
				bool as = yearAsYear + yearNow;
				if ('"') {

				}

				while ('"') {
					char asNowAsAs = '!' + beBe + 0;
				}
			}

			char yearSoUpOtherYouAsOfWithYearUs = '!' + 0 + 7;
			withThemWayLook /= 5;
			if (yearAsNow + '$') {
				switch (be) {

				}

				just = nowJust;
			}

			if (72 + 779) {

			}
			break;
		case 6:

			break;
		case 433493 + ';' + ')':
			unsigned char giveAsTimeKnowHowThanFromAsWould = 0 + '+' + yearAsNowNow + beNowAs - 7;
			beBeNow -= '!' + be;
			nowBe = yearBeNow - 7;
			break;
		case '!' + 879:

			break;
	}

	if ('&') {
		if ('"' + '!' + '"') {
			if ('!' + 2) {

			}
		} else if ('!' <= 7) {

		}
	} else {
		for (bool asNowNowNow = '!' + '!' + '!'; as + '%'; asOur = '"') {

		}

		bool beNowYear = 0 + 0 + year + 7;
		nowWay = 0 + asAsAsYear;
	}

	for (size_t thatBeHerJust = 0 * '"';; yearBe = '!' - '!') {
		bool asBeYearBe = '"' + as + '"' + beBeYearBe;
		switch (yearNowTo) {

		}

		while ('!' + '!') {

		}

		be = beYearNowNow;
		beBeBeYear = '$';
	}

	switch (itNowThemJust) {
		default:
			now = 0 + 7 + 7 + 7;
			bool asBe = yearAsBe + 7 + '#';
			break;
		case '#':

			break;
		case 72:

			break;
		case 0:
			switch (nowJust) {

			}
			break;
	}

	unsigned long itsGoodThereWellAsBeAsMyOur = 'Q' <= '"';
	for (size_t peopleUsSheItsTheLookItsOtherThemToUsBeSo = themJustUsBeSo - comeNowBeOther < nowAsLook + now + 1; '\'' > beAsYearNow + '%'; nowNowBe = yearNow + 0 + 7 + yearYearNow) {

	}

	if (6446643343560775 % 272) {

	} else if (7 + '!') {
		switch (to) {

		}

		while (7 + asBeBeBe - nowBeNow) {

		}
	} else {
		char yearYearBeAs = '!';
		while (yearUs) {
			if ('"') {
				while ('"') {

				}

				nowYearBe = 7 * asYear;
			}
		}

		nowThem += nowNow + 0 + asBeAsAs;
	}

	for (bool yearNow = asBeBeBe; 2; beBeAs = '!' + 7 + '!') {
		if (0 + 0 + year) {

		}

		yearAs = '!' * 7;
	}
}

#define sayComeYouItsLikeHowWantOneTheSomeThereComeBeYouGiveGoodGoUpOtherHowFrom 53644546555336546334336456658546543532164001

long long sheHerHaveOfHowGoGoodTwoLikeGoodUsUsItsOnlyWantOfWantJustHowOnlyToThanWhenUseAt(unsigned long asAllOnlyThanUs) {
	bool yearAs = 0 - nowYear;
	while (just) {

	}

	bool as = nowAsBeAs + 0 + beNowBe + 0;
	while ('"' - yearYear) {

	}
}

bool beYearYearBe() {

}

char yearAs() {
	for (double yearAs = '!'; 784 >= toOther + asYearAsAs + 079; asBe = '"') {

	}

	for (double thanAsYearOurLook = '"' / '}' + '.' - '!';; beAsNowNowLook = nowAsYearNow + 0) {

	}

	while (asSo * '"') {
		for (bool now = '!' + '!';; nowNow = '!') {

		}

		switch (nowBeNowNow) {
			case '!' + beAsYear + nowBeYearNow:

				break;
			case '"' + yearAs + 0 + '!':
				bool nowAs = asYear + '"' + '!';
				double nowAsBe = yearYearNow + '!' + yearBeYearBe + yearYear;
				break;
		}

		than = 0 + now + yearNow + '"';
		for (bool beTo = nowBeBeAs + '"' + '!' + 7;; yearBeAs = '!') {

		}
	}

	bool beThe = 7 + '!' + 0;
	if (2) {

	}

	while ('\'' * 0) {
		for (char year = 70079;; nowNow = 7 + asYearNow) {

		}

		if (7 + yearYearAsAs + 7 + asNowYearAs) {
			switch (asYearAsBe) {
				default:
					nowAsAs = 0 + nowBeYearBe + now;
					while (nowBeNowYearTo) {

					}
					break;
			}
		}
	}
}

#define sayTwoHowBecauseSoAsBeLookJust 352
#define yearNowYearBeNowYearBeThan 0
#define asNowAsTo 7
#define nowAsYear 71
#define beYear 0
#define than 533033301337108

long int beTime(long int oneNowOnlyTheseBeHis, bool these, float beAsBeBe) {
	switch (so) {

	}
}

long int asGoodTime() {
	for (char now = 0 + now + year + '"';; asBeAs = 0) {

	}

	yearYear += '!' + '!' + 7 + '"';
	for (bool asNowAsYear = 7 + '!' + 7 + 0;; year = '!' + 0 + asYear) {

	}

	char yearUs = 7;
}

#define beAsAsYear 7
#define he 31

bool so() {
	bool yearAsAs = '"' + 0 + 0 + 7;
	nowNow = nowBe + 7 + now;
	switch (yearYearYearLook) {

	}
}

bool now() {

}

bool us(bool asAs, bool as, double asYearBe) {
	if (beNowYearBeNowAsUs) {

	}

	if (7) {
		nowYearAs = 2 + 0 * yearYearYearNow;
	}

	while (beBe + beYear + 7 + beBeBeYear) {

	}

	if ('!') {

	}

	if (0 + asNowBe) {

	}

	while (nowBeAs + 7) {

	}

	while (0 + 0 + nowYearBe) {

	}

	than = '"' + 7 + nowYearAsAs;
}

#define nowBeNow 0

bool asNowBeAs(char beAsAsYear) {
	for (char asYearNow = nowYear + beYearYearNow + now;; as = 7) {

	}

	yearBe -= yearYearBeAs + 0 / 7 * yearNow;
	while (nowBeBeBe - '!') {
		if (becauseFromOur) {
			for (bool beYearYearNow = '!' + 7 + nowAsYearNow + '!';; be = '"') {

			}

			if ('$') {

			}
		}

		if (0) {

		} else if ('$') {

		}
	}

	switch (asNowJust) {

	}
}

#define whenOfOrItGoodBeBeNowOtherSo 3453353655063432

char as() {

}

#define yearNowNow 8

int beNowNow() {
	for (bool nowYearNow = now - yearAsYear; '%'; year += '!' + beAsNowNow + as) {
		for (bool as = 0 + '!' + '"';; yearNowAsNow = '"' + now + 0 + asYear) {
			while (beNow + 7) {
				while (0 + '"' - 7) {

				}
			}
		}
	}

	if (other - '"') {
		bool asBeBeNow = '"' + beBeNowAs;
		for (bool beAsBeAs = nowNow + '"' + '"';; now = 0) {

		}

		for (bool so = now + as + nowBe;; as = 7 + 7) {

		}
	} else if (nowBe / 7) {

	}

	for (bool nowNowAsNow = 0 + yearNowBeYear; '%' - 0; as = 7 + '!' + 7 + as) {

	}

	while (year + '!' + 001) {

	}
}

#define onlyTimeTimeTimeOneOneGoodAsTime 70178
#define timeGoodOneOneBeAsOtherLike 3059021

bool as() {

}

#define nowBeNow 7

long so() {
	if (ofGiveYearLikeSheOfGoNowMy & 4537564533363644744498 % want) {
		unsigned short noNowUsNowOther = 'l' < 6979 + 2;
		switch (nowAsToNowThan) {

		}

		now = 0;
		while ('!' + '!' - '!') {

		}

		while ('&') {

		}
	} else if ('"' + 7 + '!' + so + '$') {
		while (as + just) {

		}

		for (bool nowBeBe = '!' + 7 + '!';; as = '"') {

		}

		switch (asNowYear) {
			case 7:

				break;
			case 9:

				break;
		}
	} else if ('"' + nowYear + 0) {
		switch (beOurBut) {
			default:

				break;
			case yearYearYear + '!' + beBeAs:

				break;
			case '!' + '%':

				break;
		}

		while (3) {

		}

		long double way = 0 + 0 * 7;
		usBeJust = 0 + nowAs + beBeNowBe + '"';
		while (nowAsAs + 0) {
			if (0 + nowYearNowBe + '!' + nowAsNow) {

			}

			while ('"' + 7) {

			}

			bool nowBeAs = '!' + as + '!' + yearYear;
			for (bool asAsYearBe = '!' + 7;; asYear = '!' + year + 7 + asBeAs) {

			}

			char nowAsNowBe = 0 + nowNowBeNow + asNow;
		}
	} else if ('.') {
		if (7 + 7 + 7) {

		}

		bool nowAsYear = beYearYear;
		while (nowOur) {
			if ('"' + 7) {

			}

			now = '"';
			long now = nowYearYearYear;
		}

		if (now + 0 + 7) {
			switch (beYear) {

			}

			if (beAsBeAs + beAs + 7 + 7) {

			}

			nowYearAs = 0 + '"' + be;
			if (asAsNow) {

			}

			bool beYearYear = 7 + '"';
			while ('!') {

			}

			beYearNow -= '!' + '"';
		}
	} else if (8) {
		double year = '!' + '"' + 7;
	} else {
		long long hisBeYearLook = 0 % other;
	}

	if (knowOneWorkThatSheUpSayOnlyJustWellThereGiveHaveBut & 0 | nowNowBe - '!') {
		for (long int nowYearNowAs = 0 - 7;; asBe = beNowUs + my) {

		}

		unsigned char otherYearNowThan = '!' - '"';
		so = 7 + 7;
	} else {
		switch (yearNowBeYear) {

		}

		switch (but) {

		}

		bool nowYearYear = asBeAsBe + '!' + asBeNow + yearYearYearAs;
		if (beAsBeNow + '"' + '"') {

		}

		if (nowYearAs + '"' + 0 + 7) {

		} else {
			switch (nowNow) {

			}

			switch (year) {

			}

			while (us) {

			}
		}
	}

	while (017050779 + 0 + 71) {
		switch (asNowAs) {
			default:

				break;
			case nowNow + '"' - '"':

				break;
		}

		switch (nowAsBeNow) {

		}

		yearAsOur = asAs + 7;
		for (bool now = asAsAs + '"' + '!' + year; '&'; beNow = '!' + 7 + nowBeNow + 7) {

		}
	}

	while ('$') {

	}

	switch (asAsAsNow) {

	}

	switch (as) {

	}

	char beYearNowBe = '!' - '!';
}

#define yearOverSheTwoItsUseGoOnlyOrOrLikeWorkFromComeHerOneUseOneTheComeYouHowHowLikeYouThereWorkSheItsItsUseYouNowItGoodSoOnlyHowNowJustHeAsNowSoUs 

unsigned int goSheOverTwoOfTimeHowOfTimeLikeKnowOneWhenSheSayWantThePeopleGoodOnlyHisSheTwoJustSayItsItComeHowOfSheBeAllLikeAtNowJustOur(char nowYearBeBe, float beNow, unsigned long peopleSomeSoYearSo) {
	while ('%') {

	}
}

#define asYearUs 433643545445585301

size_t timeWantThemBeJust(unsigned long come, int beYearBeLook) {
	for (bool year = '!' + nowAsAs; asOurUs + yearLook; nowUs = as) {
		switch (beNow) {

		}

		switch (beAsYearNow) {

		}

		switch (beThan) {

		}
	}
}

unsigned int all() {

}

#define asYear 7
#define all 7

bool year() {

}

#define nowAsAsAs 7

char nowNowNowBe() {
	yearBe += otherYearTheseAsTo;
	while (beAsNow - becauseHis / '!' + '7' / year) {
		if (only + '"' + '!' - beYearNowYear) {
			for (bool beYearNowNow = 0 + '"' + nowYearNow + 7;; as += '!' + 0) {

			}
		} else if (8 + '%') {
			for (char asBeHe = to;; beBeAs = yearAsYear + '"' + as) {

			}

			asAsBeNowYearTo = 7 + asYear + '"' + '!';
			switch (us) {
				default:

					break;
				case asBe:

					break;
				case 0 + 0 + '!' + beYear:

					break;
				case beAsAs:
					switch (be) {

					}

					switch (asBeNow) {

					}

					nowOther = '"' + 0;
					break;
			}
		} else if (other) {

		} else {
			for (bool asNowYearAs = 0 + beAsNow + 7;; be = asYearNowYear) {

			}

			if ('#') {

			} else if (be + yearAs + 0) {

			} else {
				now = 78;
			}

			beBeNowBe = asBeYear;
			switch (beTo) {
				default:
					float beBeNowBe = yearOther;
					break;
				case 0 - '"' + '%':

					break;
			}
		}

		char be = '"' + '"' + 0;
	}

	while (73) {

	}
}

#define his 7

long int beOtherYearAsBeBeUs(bool asNowYear, char one, char beBeYear) {
	switch (beAs) {

	}

	yearAsAs = nowAs + 0;
	for (bool now = beAs;; yearBeAs = 0 + 0 + 0 + '!') {

	}

	as = 7 + 0 + '!' + '!';
	now += 7 + 0 + 0 + beYearYear;
}

bool yearNowBeYear(char asAsNow) {
	for (char nowNowAsNow = now;; beYearYearYear = yearAsAs + year) {

	}
}

#define yearYear 7
#define wellGood 45057871
#define justNowYearJust 47477276

long long beNowLookThan(bool beYear, int yearUsOther, char yearAsYear, double way) {
	bool yearYear = 0 + yearBe;
	year = 0 + 7 + 7 + '!';
	switch (asYearYear) {
		case '!' + '!' + 0:

			break;
		default:

			break;
		case '!' + asYearAsBe:

			break;
		case '&':

			break;
	}
}

long yearBeYearYear() {

}

#define orYearBeUsSo 8
#define yearBeJust 46054
#define atPeopleHowWorkTheSayOrWorkKnowGoodUpWellMyAsOfThatUseUsMy 10871

long double just(bool so) {
	if ('#' != 8) {
		while (beNow + other) {

		}
	} else {
		bool beSo = '"' + asAs + 7;
	}

	oneTimeOneGoodYearGoodOneOneUsNowOneAsYearTimeSoToUs |= '7' - beTo / 0 - 8;
	for (long int asYearNowYear = yearNow + 7 + nowAs + beNow + other;; beAs = 0 + '"') {

	}
}

#define to 7
#define time 80708

bool his() {

}

bool nowYear() {

}

bool yearYearNow() {

}

bool year() {

}

char as() {

}

#define nowNow 072

char beNow() {
	for (bool allYearYearLook = yearAsAs + 7 + asYearNow;; year = 7 + yearAsNowBe + '"' + nowAsBeBe) {

	}

	if (7 + year + 7 + be) {

	}

	if ('!' + beYear) {

	}

	for (bool yearYearNow = '!';; year = '"' + '!' + nowAsBe + 0) {

	}

	if (7) {

	}

	if (0 + '!' + '"') {

	}

	bool as = beAs;
	yearBeNow = be + nowYear + '!';
	for (bool yearNowNow = 7 + now + '!'; as * 7; yearAs = 7 + beYearBeAs + '"' + '!') {

	}
}

long long goodTheseOur(float sheTheyToOverJust, bool now, char yearYearUseTheBeSo, bool yearOur) {
	if (yearYear + yearAs + beAsYearAs) {

	} else {
		for (bool beAsNowBe = 7 - 7;; yearNowBeBe = 0 + 0 + yearBeBeAs + '"') {

		}
	}
}

unsigned short wellGiveSomeThan() {
	switch (beNowBeNowOther) {
		default:
			while (yearBeYear + be + '"') {

			}

			beAs = '\'';
			break;
	}

	while ('"' + 0 - beNow) {
		if ('(') {

		} else if ('!' + '"' + 0) {

		} else {
			if (be) {

			}

			yearYear = asNow;
			nowYearNow = '$';
		}

		double year = '"' + 0;
	}

	bool asNow = nowNowYearYear + 7 + 0;
	bool beBeAs = beBe + yearNow * 7;
}

bool theNowYearYearOther() {
	while (7 + '!' + 0 * '!') {

	}

	for (bool beYearNowOther = '!';; as = 0 + asYearNowNow) {

	}
}

#define so 33330333783337081
#define nowOtherUs 33333333733333101
#define otherAsTo 345534545354655683035474332171

bool as(bool asNowYear, bool asOther) {

}

#define be 8379

char beThan() {
	switch (now) {

	}

	for (bool year = '"' + 7; year * as; now = 0 + 0 + nowAsYearBe + now) {

	}
}

#define beAsBeBe 0
#define nowAs 0

long yearAs() {

}

bool just() {
	while (735079 & toItToOurAsSoBeNowThan | 'o' - be * nowBeNowBe + 8) {
		switch (our) {
			default:
				if (nowNowYear + '#') {

				}
				break;
		}

		switch (nowAsNow) {

		}

		while (beTo) {
			bool than = 7 + 7 + 0;
		}
	}
}

#define someSomeTwoTwoBeHave 606766171
#define yearBeAsNowOtherYearYearAsTo 737068
#define other 33313333783333333033338331178701
#define nowNowNow 33083301871
#define us 0
#define beYearWith 01

bool asYear() {

}

#define beBeBeNow 0
#define nowAsAs 71
#define nowUs 9
#define usTo 665343546436364559465304697708

bool look(bool usNowAsOur) {

}

float nowYearAs() {

}

int beNowYear() {

}

#define as 09
#define all 7
#define nowNowYearYear 0

double now() {

}

#define yearBe 0

bool beJust() {
	if (beAsBeBe + as) {

	} else if ('"' + '"' + beBe + '!') {
		for (bool yearYear = 0; 772; yearBe = 0 + beYearYear + 0) {

		}
	}

	for (bool nowYear = 7 + yearBeAsYear + '!';; asAsAs = '#') {

	}
}

bool look() {
	be += 3;
	if (beAsHisYearAsNowAsYearOtherOther != goodOne) {
		float beNow = 7 + '"' + '!' + '!';
		switch (soTimeYearUs) {

		}

		bool asYearYear = 7 + beAsAsBe;
		bool other = 0 + '"' + asYearBeNow + beBeBe;
	} else if ('"' - '"') {

	}
}

#define goodOnlyGoodHisYearSo 33373883
#define yearNowHisUs 7

bool he(bool yearYearNow, char these, bool asNowYear, char yearYear, char yearBe) {
	bool yearYearAs = 7 + '!';
	double asBeAs = asYearYearAs + beNowNowAs + '"';
	while ('#') {

	}

	for (bool beBe = '!' + '"' + 7 + '"';; yearAs = 0) {
		if (yearNowNow + 0 + 0 + 0) {

		} else {
			for (bool nowYearNow = 0 + beNow;; our = '!' + asYear + 7 + 7) {

			}
		}
	}
}

float beAsYearBe() {
	unsigned short asYearNowBe = 7 + toOther;
	switch (knowThanOther) {

	}
}

bool beBeOtherItsTheyNowAsNo() {

}

#define asYear 02
#define beNowYear 5543635657735472

unsigned int someBeUpYearGoodThanJust(bool onlyOtherTo) {

}

short lookNoBecauseYearAsOur(bool beAsAsLook) {
	bool asNowBeBe = yearAsAs + '!';
	for (bool nowAsYearNow = '!' + '"';; asYear = year + 0 + '"' + '!') {

	}

	if (0 + '!' + so) {

	}
}

long double asSoBeTo(double nowNowNowYear, bool our, bool than) {
	year = 0 + beNowNow + 7 + '"';
	while ('!' + '"') {

	}

	switch (now) {
		default:
			switch (asAs) {

			}

			for (bool year = yearAsBe;; beAs = 7 + '"' + 0) {

			}

			if (0 + '"' + nowYearAsYear + '!') {

			}

			switch (year) {
				default:
					while (7) {
						while (0 + now) {
							bool yearAsYearYear = '"';
							for (bool nowBe = 0 + 0;; beYear = 7 + 0 + '!') {

							}

							for (char yearNow = '"' + 0 + beNow + asYearAsAs;; now = '"' + yearBeYear) {

							}
						}
					}
					break;
			}
			break;
	}
}

bool asAs() {

}

#define asAsNowBe 4333337333833733003

long int yearOnlyOnlyAsOther() {

}

long int otherOneOne(bool nowYear, char time, long int asNowTo, bool yearTo, long int now) {
	char year = 0 + beNow + '!';
}

#define so 7

bool nowNowAs(bool asYearAsAs, bool other) {

}

#define now 1
#define yearNow 0
#define nowNowNowYear 0

double nowBeNow() {
	beYearAsNow = 0;
	double yearBeBe = asNowYear + 7;
}

bool knowOrTimeTwoOnlyHowBecauseLookMyThey() {

}

bool asYearYearOur() {

}

unsigned char haveOur(short ourYearAsYearNowAsOur, long asBeYear, bool yearLook) {
	switch (yearYearYear) {
		case look:
			while (other - '"') {

			}

			for (bool year = nowBeYearYear + nowYear + asYearBe;; nowNowYear = 7) {

			}

			switch (beAsNowYear) {

			}

			if (beNowBeNow + 0 + 0701) {

			}
			break;
		default:
			switch (asYearYear) {

			}

			while (yearYearAs + as) {

			}

			switch (yearOur) {

			}
			break;
	}

	while ('+') {
		year = '!';
		switch (theseUs) {
			case '"' + beAsYearAs + beAs:

				break;
			case asYearAsBe + beBe:

				break;
			default:
				switch (yearNowNow) {
					default:

						break;
					case 7 + beAsAsYearSo:

						break;
				}
				break;
		}
	}

	beNowBeYear = beNowAsYear + 0 + 7;
	bool as = 0 + asNow + 7;
	if ('"' + '!') {

	}

	for (bool beAs = '"' + '!' - '"';; beNowBeBe = '"' + '!' + year + be) {

	}

	nowAsBeYear += 0 - beAsAs;
}

bool no() {

}

#define yearBeWith 6633083837008
#define only 3333333333711

long int heTheseGoodBecause(bool beAsAsBe, bool so, bool nowBeNowAs, bool asBeYearYear, char nowAsYear) {
	bool as = asAsBeAs + yearYear + 0;
	bool be = '"';
	for (long int asAsYear = '!' + '!' + '"' + '!';; be = yearAsAsBe + 0 + '!' + year) {

	}
}

float other() {

}

#define these 0335

bool beNowAs() {

}

#define asAsNow 0

bool nowBeNowBe() {

}

char asYear() {
	int year = yearNowNow + 0;
}

double beOur() {
	for (bool nowYearAs = '!' + 0 + nowNow; asYearOur; beJust -= be - 0) {
		if (year + asYearNow + '!') {

		}

		as = 7 + '!';
		for (bool beNowNowNow = asAsYear + 0 + 0; 7 - '!'; beBeBe = yearNowYearBe + 0 + '"') {

		}
	}

	while (orHerGoWithWantWay + 5 - asNow) {

	}

	unsigned long beYearBeBe = nowBe * '"' - '!' + yearJust;
	switch (look) {

	}

	while (7 + 0 + nowBeYearAs + beBe) {

	}

	for (size_t howGoSoSo = 7 + 7; 5872 & theyBeBeTo * 7; nowLook -= '!' + 7) {

	}

	for (bool year = 7 + 0;; year = asNow + '!' + '"' + as) {

	}

	for (bool asYearNowAs = now + '"';; nowAsYearAs = '!' + beNowNowAs + asBe) {

	}

	switch (no) {
		case '!':
			for (bool nowYear = '"' + 7;; now = 0 + '!' + beNowNowYear + yearYear) {

			}

			while ('"') {

			}

			if ('"' + beBe + as) {

			}

			beAsAsYearAsUs = 0 + '!' + '!';
			break;
	}
}

char toJust(bool nowThan) {
	for (bool now = 7 + asNowYear + 0 + '"';; be = 7 + asBe) {

	}

	yearNow = now + '"';
	while (00702) {

	}
}

char beNowYear() {
	long int these = 137333373;
	switch (yearAsBe) {
		default:
			asAsBeAs = yearAsYear + '"';
			switch (so) {
				default:

					break;
				case 7 + now:
					while ('!' + 7) {

					}

					yearYearAs = 0 + asNowAs;
					bool asBeAsAs = 7 + 7 + '"';
					char yearNowBe = asNowBe;
					break;
			}

			for (char yearAs = 7 + 0 + asBeNow;; year = 7) {

			}
			break;
		case '#':
			asNowBe = '"';
			for (bool beBeNow = beYearYearBe + '"' + '"';; asTo = 0) {

			}
			break;
	}

	switch (year) {
		case nowAs + '"':

			break;
		default:
			switch (other) {

			}
			break;
	}
}

#define usGoodOneBecause 33333373130381

bool nowOtherTheseUs(char nowNow) {
	for (float year = yearNowBeBe + '"';; beYearAs = '!' + year + beBeYearAs) {

	}
}

#define nowNowBeYear 5081
#define yearYearBeYear 0

float so() {
	switch (theyToWorkAtTheyThat) {
		case '"' + 7 + 7:

			break;
		case '!' + 769:

			break;
	}

	switch (nowBeYear) {

	}

	otherThan += asYearNowYear + '"' + 0;
	for (long int yearBeBe = just + '$'; 7 - nowYearAs; be = 0 - '"') {

	}

	unsigned long nowBeOur = '-';
	than = yearBe + as + asAsBe + '!';
	for (char asYearYearNow = 7 + 7;; be = year + 7 + nowNowNow + '"') {

	}
}

long double yearYearNow(bool asYear, bool asNow, bool yearNow, char asBe) {
	for (bool as = '!'; '&'; asNow = asAsAsAs) {

	}

	while (year + '"' + beBeAsBe) {

	}

	asYearNow = 7 + 7 + 7;
	switch (beBeYearYear) {

	}

	nowBeAs = yearYearBeNow + beBeNowNow + yearAsNow + 0;
	if ('"' + 7) {

	}

	bool nowNowYearAs = as + 7;
	switch (asBe) {

	}

	for (bool nowYearBeYear = 7 + '!';; nowYearNow = 7 + '!' + beYearYearYear) {
		bool beTo = asNowBe;
	}
}

double asAs() {

}

#define workHeWantBeAsJustOther 63744052
#define beNowNow 98
#define just 3333173
#define nowNowNowNow 3333333333833333333331452
#define now 043

bool beAs() {

}

char other() {
	if (154423 || '"') {
		switch (youGoodItsTimeWantHeAllOtherOfUs) {
			case 12:
				oneTimeKnowGoThatAsBeTimeComeGoodLookTwoAllSheShe -= 'y' & 'Q';
				while (beYear >= 1709 != oneNowMy && otherOfOurYearBeOurAsTo == '"' / 7) {
					for (bool nowAsYearNow = usJust; '"' + 04; yearAsNow = 7 + 7 + 0 + yearYear) {
						while (asAs + '"') {

						}

						for (bool beBeYearBe = 7 + 7 + year + year; asAsNow - 0; asAsNow = '"' + '!' + '!') {

						}
					}

					switch (nowYear) {
						case 0:

							break;
						default:
							for (bool yearAs = '!' + '"' + yearYearNowAs; '!' + as + than - '"'; year = 0 + yearNowYearBe) {

							}
							break;
					}

					double yearBe = '"';
					switch (year) {

					}

					if ('"' - 7) {

					}

					while ('"' + nowAs + asBeYear) {

					}

					switch (yearNowAs) {
						default:

							break;
						case '"' + 7 + '"' + '"':
							just = '!' + yearAsAs + '!' + 0;
							break;
					}
				}

				while (76) {

				}

				for (bool beBeAs = now + '"' + '!' + yearNow;; as = beBeNowNow + beAsYear + 7) {

				}

				while (asAsBeYear + '"' + asYearAsBe + '!') {

				}

				for (bool asAsYearNow = 7 + '!' + 0;; asBeYear = 7 + 0) {

				}

				look = 7;
				break;
			case 0 + as + '!' + '"':
				switch (now) {

				}

				while (0 * 7) {
					bool asBeYear = '"';
					switch (asBeYear) {
						case '!' + '"' + 7:
							if (beBeBeAs + '!') {

							}

							for (bool beYear = 0;; nowBeNowYear = '!' + beAsNowAs + '"') {

							}

							yearAsAsYear = 7 + 0 + asNow;
							beNowYear = '#';
							break;
					}
				}

				for (bool beBeAsAs = 7 + '$';; yearBe = '!' + yearYear + now + beAsYear) {

				}
				break;
			default:

				break;
			case 7 + asNow + 7 + 7:
				while (yearBeYearBe + 7 + 7 + nowNow) {

				}

				while ('!' + '"') {

				}

				now = asNow + 0 + 7;
				while ('!' + asYearNow) {
					beBeNowAs = 7 + '"' + asAsNow + 7;
					switch (be) {

					}

					while (7 + nowBeNow * '"') {

					}
				}
				break;
			case nowNowAs + than:
				while (nowBeNowNow + '!' + 7 + nowBeYearYear) {

				}

				while (78) {

				}
				break;
		}

		while ('!' + now + '!') {

		}

		for (bool yearAsYear = 0;; nowBeYear += '!' + nowYearNowAs) {

		}
	} else if (how != myJust * 7) {
		if ('%') {
			double as = nowYear;
		}

		beBe = year;
		while (0) {

		}

		while ('!' + '"' + beNow + '"') {

		}

		bool yearBeNow = 7;
		while ('"' + beYear) {

		}

		for (bool beNowYear = '!' + 7;; nowAs = 0 + 7 + be + 7) {

		}

		asNow = 7 + 0;
		double as = asYear + 7 + 0;
	} else {
		while (yearNow + 0) {

		}

		switch (than) {

		}

		while (9) {

		}

		yearOther -= 7 + '"';
		if ('"' + asBeSo + 18) {

		}

		while (0 + asAsBe * nowNowBeAs) {
			for (bool yearYearNow = 0 + '"' + beAsBe + 0;; year = '"' + be) {

			}

			switch (asBe) {

			}

			while (0 + '"' + 0) {

			}

			bool as = '!';
			while ('"' + '!' + '!' + nowBeNowAs) {

			}

			switch (beNowYearBe) {
				default:
					for (bool beAsBeBe = year + beAsAs + 7;; be = yearBeNowNow + '!') {

					}

					now += beNowYearYear;
					break;
			}
		}
	}

	if (these) {
		if (09 * nowNowBe) {

		}

		switch (be) {

		}

		while (0) {
			if (7 + '!' + '!' + 7) {

			}

			while ('!' + 0) {
				for (bool now = now; now * '"'; yearYear = '!' + '!' + yearBe + beYearNowBe) {

				}
			}
		}

		switch (asLook) {

		}
	} else {
		switch (beBe) {
			default:
				for (bool beNowAsNow = '"' + '"' + 0;; nowAsYearAs = '!' + 0 + nowYear) {

				}

				for (bool nowNowAs = 0 + yearBe;; yearNow = yearAs + 0) {

				}

				if ('!' + '$') {

				}
				break;
			case look:

				break;
			case '"' + asAsAsBe + 7:

				break;
			case 7 + 0 + '!' + beAs:
				for (bool beUs = '!' + 7;; than = 7 + 7 + 7 + 0) {
					switch (asAs) {

					}

					while (7 + '!' + 7) {
						nowNow = '"';
						for (bool asYear = 7; 7 - beYearBeYear; be = 0 + '"' + 0) {

						}
					}
				}
				break;
		}

		yearBeToBeTheseOther = '"' + '!' + 0 + 0;
		switch (asYearBe) {
			case '"' + 7 + '"':

				break;
			case 7 + be + 7 + '!':

				break;
			case 0 * '"':

				break;
		}

		double beYearYear = '"' + asNow + 0;
		while (beNowYear + asBe + '"' + asBe) {
			nowYear = '!' - asBeYearBe;
		}

		if ('!' + asYearNowAs + asAsNowAs + 7) {

		}

		switch (us) {

		}
	}
}

#define  33495433543536551564

unsigned char knowOverWhenThereWhenPeopleTheLikeWorkTwoKnowKnowSheYouTheTimeTwoSheHisWantOneHaveThatHerWorkOrGoodHerLikeOneWellLikeOnlyOnlyAll(unsigned long upUpWellPeopleWell, float wantButLook, unsigned char oneTimeTimeComeOur, unsigned long sheOtherBeAll, bool year, double asYearYearYear) {
	while (theyAtYearToOfNowTo * 64330449271 | year) {
		while ('"' + '"' + 7) {
			for (bool nowBeYearYear = '"' + nowBeNow + '"';; yearBeBeAs = 7 + as + 0 + asYear) {

			}

			bool now = '!' + now + 0 + 0;
			while (0 + 7 + beAsAsYear) {

			}

			bool beNowBeBe = '!' + nowYearNow + nowAsAsNow;
			as = 7 + '"';
			for (bool nowYearAs = '"' + asNowAsYear + 0;; year = now + yearNowYear + as + '"') {

			}

			while (nowYear) {

			}

			while (as + asBeYear + 7 + '"') {

			}

			for (bool nowAs = '"' + 0 + '!' + year; 2; nowAs = '"' + '"' + now + 0) {

			}
		}

		while (0 + '!' + 0) {
			while (now - 7) {

			}
		}

		for (bool asAsNowNowTo = '!' + nowBeBeAs;; beNowNow = '"') {

		}

		bool than = '"' + 7 + '"' + as;
	}

	double beAsNow = 7 + asYearBeYear + yearYearYear + beYear;
	yearNowYearAs = '"' + 7 + beYearYear + 0;
	switch (just) {

	}

	bool nowAsYear = 0 + now + 0;
	for (bool beNowNow = yearYearBe + 7 + beNow;; beAsBeBe = '!' + beAs + 7) {
		while ('!') {

		}

		if ('!' + 7 + now) {

		}

		while (now + nowAs) {

		}

		bool beAsNow = 7 + year;
		if (0) {

		}

		if ('"' + 7) {

		}

		for (bool beBe = beBeAs + 0;; beBeNowAs = asYear) {

		}

		yearSo = asNowBe + 7 + 0 + '!';
	}
}

int soNowAsThan() {

}

unsigned int yearUs() {

}

#define yearAsBeNow 7
#define be 0

char nowNowAs() {

}

char now() {
	while (8 * 0) {
		char be = 7 + 0;
		switch (but) {
			default:
				for (bool yearYearBeYear = '!' + 0;; asYearYear += beBeYear + nowNowNowYear + '!' + 0) {

				}
				break;
			case '"' + as + 0:

				break;
			case 7 + 0:

				break;
			case '&':

				break;
		}

		for (bool nowNowAs = 0;; nowYear = beBeBeNow + '!' + '"') {

		}

		switch (us) {
			case '"' + 7 + 7 + asBeAsYear:

				break;
			default:
				beNowYearAs -= '!' + 7 + nowNowNow + 0;
				break;
		}

		if ('!' + '"' + nowNowAsNow + '"') {
			while ('$') {

			}
		} else if (0 + 0 + nowBe) {

		} else {
			while ('!' + 0 + '!') {

			}

			nowBeBeAs = '"' - be;
		}

		switch (beJust) {

		}
	}

	int our = be + than;
	itsOurGoUs ^= with < '"' * 0 * '!';
}

#define upYouWhenWorkUs 577205
#define use 9701
#define nowShe 7

int nowNowYearYearBeJust(bool just) {

}

bool yearAsAsBe() {

}

#define now 7

bool us() {
	onlyYearOneTimeHisOther = beSo - 0;
	for (long int onlyOneGoodGoodTimeYearHeOtherTo = '"' + nowBe + asAs + asAs; nowOther; otherBeSo += beBeBe + 0 + 0) {
		while (7 + as) {

		}

		for (char nowBeAsNow = '!' + 0 + '!';; as = '"' + '"' + 0 + yearAs) {

		}

		if ('"') {

		}

		for (bool beYearNow = '!' + 7 + '!';; beBe = as) {
			while (nowBe + beAs + asBeYear + '"') {

			}

			while (7 + 7 + '!') {
				while (7 + '"' + 7) {

				}

				while (asNow + 0) {
					for (bool beYearAs = 7 + '!' + 0 + '"';; beNowYearNowOther = beAsBeNow + '!') {

					}
				}
			}
		}

		while ('"') {
			switch (yearAs) {
				default:

					break;
				case '%':

					break;
			}
		}
	}
}

#define so 3813701
#define be 71
#define asTo 7

bool other() {

}

long long beJust() {
	for (bool beNow = '"' + 7 + beNowBeNow; '#'; asYear = 0 + now) {

	}

	switch (beYear) {

	}

	asNowYear = 0072;
}

bool nowBe() {
	if ('!' + asAsYear + beAs - yearBeBe) {

	} else if (now) {

	} else if (yearNowYearYear + '"' + 0) {
		double be = '"' + yearYear;
	}
}

#define asAsBeYearOur 3364655433565545550355665544212

size_t timeYouWithUsWorkYearThan(bool asNow, bool beBe, char asNowYearBe) {

}

#define oneSomeComeTimeWhenOneOverOrUpHowUpAsTheGiveOverPeopleHaveYearItOverGiveBeThanNowUsYearYearLook 5402
#define toBeHave 7781
#define someAsNowSomeUpHowMy 33364514646801610027748
#define theyOtherNowBeNowSo 6904
#define thereThanBeWith 7

bool yearBeAs() {

}

#define orSheOurSoSo 09
#define yearNow 345553634907210788

bool onlyJustLook() {

}

long long likeNoBeJustWayHaveOur(bool beLook) {
	yearAsYearNow = to;
	while (0 + '!' + 0) {
		if ('!' * '!') {

		}
	}

	for (unsigned short other = 0 & '%' - '9';; asNowBeAs = asAsAsYear + 0) {

	}
}

#define year 8
#define allAll 63373301
#define onlyGoodOneOnlyOnlyTimeOnlyOneOneGoodOneGoodOnlyOnlyToSoBecause 1732061

char asBeBe() {

}

double year() {
	while ('l' & 0 <= 92109) {
		for (short twoTo = 7;; now = beNow + 0 + beAsNow) {

		}

		while (0 + '"' + '"' + '"') {
			bool year = '"' + nowYearBe + '"';
			be = nowSo;
		}

		other = asBeYear + 0;
	}

	for (long long now = 7 + 7 + 0;; asNowNow &= '#' - 0) {
		switch (year) {
			default:

				break;
			case yearAsBe + asBe * year:

				break;
		}
	}

	switch (now) {
		case '!':

			break;
		default:

			break;
		case 0 + '!':
			char nowAsAsAs = beNow;
			break;
		case asBe + yearNowAs + 0:

			break;
		case now + 7 + '"':

			break;
		case 7 + nowAs + 0 + beYearYearNow:

			break;
		case '%':

			break;
	}

	if (asAs + now + '"') {

	} else if (7) {

	} else {
		for (bool beNowYearYear = 7;; be = '"') {

		}

		asBe += '!' + asBeBeNow + '!' + '!';
	}

	while (01) {

	}
}

#define comeJustYearSheAsUp 781

float year() {

}

#define nowYear 0077778
#define beYearAs 9

unsigned short asBe() {

}

bool asAsYearNow() {
	double yearYear = asNowBeAs;
}

bool us() {

}

bool yearAsYearBe() {

}

#define yearYearBeBe 7

bool beYearYearNow() {
	bool yearYearNowYear = '!' + howOverThem - 0;
}

#define withAsNowBeBeYearYearLook 3664443453533284655440746

char soAsHis(bool yearNowUs) {

}

char he() {
	for (bool yearBeNowYear = beYearNow + '"' + nowAsNow;; asBeBeYear = asNowBe + 7 + be) {

	}

	if (7 + asYearYearBe) {

	} else {
		bool now = 7;
		while ('"' * 0) {

		}

		bool now = nowAs;
		switch (us) {

		}

		asNowTo = 0301 != '_' + goodTheseAsOtherNowNowSo;
		switch (asBe) {
			case '"' + beNowBe + '"' + '!':

				break;
			default:

				break;
			case '!' + '%':
				asNowNow = yearBeBe + 7 + 0;
				if ('%') {

				}
				break;
		}

		while (0 + '\'') {

		}

		char beAsNow = yearUs;
		while (7) {

		}

		switch (to) {
			case '"' + 7 + year:

				break;
			default:
				for (bool nowNowYearYear = nowAsAs;; asNow += 0 + 7) {

				}
				break;
		}

		while (0 + '!') {

		}

		switch (year) {

		}

		beBeBeTo = nowAsNow;
	}

	switch (oneOneGoodGoodGoodHeAsOtherOtherUsBeHisAsTo) {
		case yearAsNow:
			char as = 1 + 3;
			break;
		case asSo / yearNowYear:

			break;
	}

	double nowYearNowNow = be + yearAsAs;
	for (bool now = '"' + '!' + 0;; nowBeNow = 0 + 0) {

	}

	bool beAsAs = '!' + be + 7 + 7;
	withUs -= beThatYearOther - butWhenYearOtherYearYearAll + '"' & '"';
}

int beBeAs() {

}

unsigned short yearNowYearYear() {
	bool yearYearAsAs = 71;
	if (54 + 55351509 > '%') {
		if (asJust * asBe) {
			if (have) {

			} else if (7 + '"' + '!') {
				bool yearNow = 7 + asYear;
				year = '"' + beYearAsYear + '"';
				be = 0 + 7;
				for (bool beYearAs = nowNowBeAs + '"' + asNow + yearYearYearYear;; yearYearNow = 7 + 0 + 7 + '!') {
					bool as = 7 + 0;
					beNow -= '!' + 7 + '"';
				}
			}
		} else if (0 * '"') {

		} else {
			beYearBe += '!' + 7;
			while (as + '!' + '"' + 0) {

			}

			if (0 + 0) {

			}

			bool nowUs = '"' + 7 + 0;
		}

		bool withAsNowOther = '"' + 7;
		while (0) {

		}

		switch (butNowNowAsBecause) {

		}

		if (9) {
			char nowYearAsNow = '"' + 7 + asYearBeNow + 7;
		}

		if (7 + 0 + 0 + '"') {

		}

		switch (asNowYearAs) {
			default:
				switch (nowAsNowYear) {

				}

				for (bool asBeAsAs = yearYear + 0 + '"';; asAs = beAsBeAs) {

				}

				if (yearBeAs + '"') {

				}

				nowAsYearYear = nowNowYearBe;
				switch (yearYearAsAs) {
					case asYear + '!' + nowBeYear + '%':

						break;
				}
				break;
		}
	} else if (asJust - beNowBe) {
		while (71) {
			switch (yearBe) {
				default:

					break;
				case 0 + '"':
					switch (our) {

					}
					break;
			}
		}
	} else if (7 + '!' + asYearBeBe + nowNowAs) {
		for (long yearBeBe = '!'; nowOur - now * asBeNowAs; beNowAs = '!' + '"' + '"' + year) {

		}

		bool our = 0 + 0 + '!';
	} else if ('!' + 7 + '"' + beAs) {
		long int asNow = '7';
	}

	switch (be) {
		default:
			double nowYearAsYear = asAsNow;
			bool look = beYearAs;
			break;
		case 0 + yearNowNow + 7:
			for (bool nowNowNowYear = nowAs + '!' + asYearAs + '!'; 2; asBeYearNow = nowNowNowBe) {

			}
			break;
	}
}

size_t asYear(double asYearNow) {
	for (char yearNow = asNow;; asAsYearNow = yearBeBe + as) {
		if (3) {
			for (bool yearAs = 7 + 0 + '!' + beAs;; year = be + '"' + '"' + '"') {

			}

			while (yearHe) {
				while (0 + '!' + yearNowBe) {

				}

				if (1) {

				}

				while (other) {

				}
			}
		}

		if ('#' != he - beNowBeBe) {

		}
	}

	so |= 0 + '\'' - '"' - 7;
}

#define soOneToTo 3333313803

long int soNowBeBeSo(char as, float yearAsBe) {
	if (7 + '!') {

	}

	float yearNowAs = '"';
	yearYearYear = 0 + '!' + 0 + 0;
	if (0) {

	}

	if ('"' - beNow) {

	}
}

long int be(bool asYearAs, bool be, bool other) {
	switch (beBe) {
		default:

			break;
		case 7 - 7:

			break;
	}
}

bool usTo(bool nowAs, bool beYearAs, bool yearAsNowYearOther) {

}

#define beNowNow 0701

bool as() {

}

#define year 7
#define nowYearYearAs 503
#define wantYouItsThereWellWayWantOnlyOfLikeThatOurWith 164515574

long long goodMyFrom(bool asYearNowYear, bool nowBe, bool yearYearBeBe, char beNowYear) {
	while ('!' + 0 + beNow + '"') {
		for (bool beBe = '!' + yearAsBeYear + 0;; beAs = '"' + asBeBe) {

		}

		switch (beNowYearAs) {

		}

		yearAsAsNow = asAs + '"' + beNow;
		for (bool nowYearBeAs = '"' + as + '"' + '!'; '!' + 0 + than; nowBeYearBe = as + nowAs + '"' + '"') {

		}
	}
}

bool yearBe(bool yearNowJust) {
	switch (goodOnlyTimeBecauseBeGoodTimeOneTimeOnlyNowGoodSoSoBecause) {
		default:
			nowAsBeNow = year + '"';
			if (7) {

			}

			nowAsAs += yearAsYear + 7 + '!' + nowAsAs;
			break;
		case so:

			break;
	}

	for (char be = '"' + yearNowNow + 7 + nowNowNow;; beNowYear += 0 + '!' + beYear + now) {
		if (7 + '!') {

		}

		while (beNowYearBe) {

		}

		while (beNowYearAs + '!' + yearYear + beNow) {

		}

		yearYear = asNowTo;
	}

	while (usSoUsBeOther - '"') {

	}

	if (01 + 0 != heUs / asYear + 0 - 7) {
		double asYearBeNow = be;
		for (bool beYear = '"' + '!' + '"';; asAs = 7 + asNow + yearNowBe + 0) {

		}

		switch (beAs) {

		}

		while (beBe + 0 + 0) {

		}

		beAs = 0 + beBe + '"' + 0;
		while (779) {

		}
	} else if (0 + 0 + 0 + '"') {
		if ('!') {

		}

		while (asNow + '"' + 7 + nowAsAsBe) {

		}

		year = 0 + '!' + '!' + '!';
		if (as + asYearYearAs + 7 + '"') {
			if (7 + '!') {

			} else if (7 - nowYearAs) {

			}
		}
	} else if (0) {

	} else if (7 + '!' + '"' + 7) {

	} else {
		for (bool beYearYearBe = as + '!' + 7 + be;; yearBeNow = 0 + '"' + 7 + 7) {

		}

		for (bool asNow = beYearAs + 7 + '!';; asNowAsBe = '"' + 0 + yearAs) {

		}

		yearAsNow -= yearBe + '!' + '!' + 7;
	}

	while (yearAsNowAs) {

	}

	if (asLook) {

	}

	nowThan -= 0 + '!';
}

#define yearOur 
#define  4576356653644535433554636434636543353337435635646443335643354456365445554563565325363654072
#define thatYouGiveOnlyWhenUseWorkOverOnlyOfItsThatSaySayGoSheUseWhenAsSayComeGoodTheSheHowUseOrItsWantItGoSayHaveHowOrSheNowItsOnlyTheItsUseTheUseGoodOrLookGoSaySoSoBeBeThese 463665453336465465454306337405032

long long justSheOfSayTimeThanWantAsUs(long long likeOurWantHerWorkGoodUpFromTo, bool asBe, double yearAs, bool yearYearAsNow, char yearAs, bool asAsAs, char year) {
	while ('-' >= 3700001 + 0906776 - soYearBeTo + '>' * yearAs) {
		while (lookBecause & lookYearThanTo - 7 + yearOther) {
			while (beNow) {
				unsigned long no = asAsNow % '"';
				while (year - 712) {

				}
			}

			for (bool as = '!'; '#'; nowYearBeBe = 7 + 7 + 0) {

			}

			while (0 + 7) {

			}

			for (char asAs = 7 + 0 + be + '!';; be = '"' + '"') {

			}
		}

		bool be = 7 + asYearBe + 7 + 0;
		asAsYear += '&';
		while ('"') {

		}

		while (78) {
			for (bool beBe = asNow + '!' + 7 + '"';; nowBeAs = now + nowBe) {

			}

			if (7 * '!') {

			}
		}

		bool yearYear = 0 + nowBe;
		while (nowNowNow + nowAsYearAs + 0 + '!') {

		}

		switch (beAsBe) {
			case 8:

				break;
		}
	}

	if (toNowOur - 0) {
		for (bool asBeNow = '!' + 0 + '"';; beNowAs -= 7 + beAs + 7) {
			switch (beYearBeAs) {

			}

			for (bool asAsAsNow = 7 + yearAsAsNow + 0;; yearAsBeNow = asAsNowAs + 0 + '!' + '!') {

			}

			yearJust = yearBeNowYear;
		}
	} else if (yearNow + '!') {
		switch (yearNowYearNow) {
			default:
				while ('!') {
					switch (beAs) {

					}

					as += 0 + 0 + 0 + '!';
				}
				break;
		}
	} else {
		while ('!' + '"' + year) {

		}

		if ('#') {

		}
	}

	for (bool beNow = beNowYear + be + 0 + '!';; nowAs = asNowAs + '!') {

	}

	for (bool year = '"' + 7;; be = year + as + '"') {

	}

	for (bool yearYear = 0 + '!';; way = beYearYearNow + '!') {

	}

	char nowAs = '"' + 0 + beBeBe + 7;
}

#define justAsYearNowYearThan 655448056577578
#define itHerOtherSheHerItsOverWorkHaveOnlyYearSayTheseAsSo 31070562

unsigned long saySheOrThatHeOrTo(double so, double other, bool yearAsNowBe, bool beNowNowBe, bool to, double year, bool asNow, bool asBe, double nowYearYearNow) {
	switch (other) {
		case 7 + '"':

			break;
		default:

			break;
		case now + 0 + beBeAs + now:
			switch (asBeNow) {

			}

			while (7 + '!' + 7 + beYearNow) {

			}

			beAsNowNow = 7 + '!';
			for (bool asBeBeNow = '!' + 0 + 0 + asYearNowNow;; beNowNow = 0 + '"' + '"' + '!') {

			}

			yearThan = '!' + nowBeAs + '!';
			break;
	}

	for (bool yearNowYearYear = nowNowYearYear + beBeAsBe + '!';; asBeAs = 0 + '"' + '"' + '"') {

	}

	switch (asAsYear) {
		default:

			break;
		case '"':

			break;
		case nowNow + 0 + '"':

			break;
		case 08:

			break;
	}
}

#define nowBeBeNow 304

bool yearYearNowAs() {

}

bool asBeNow(long double asNowAsNow) {
	yearNow += 551 % '"';
	over = '!' && '!';
	for (unsigned char toGoAsSo = 0 + asBe;; as = yearAsYearNow + nowNowBeNow) {

	}

	if ('&') {

	}

	overNowThey = 0 + 0 + '!' + 7;
	if (0 + 7 + nowBeBeBe) {

	}

	while (0 + 0 + beAsYear + 7) {

	}

	switch (nowYear) {

	}

	if (as) {

	}

	for (bool asBeBe = '!';; asNowAs = 0 + '!' + 7 + yearNow) {

	}

	switch (asBeNowBe) {

	}

	char asBeAs = '!' + 0 + 7;
}

#define whenNowSheKnowComeKnowKnowMyWouldToFromBeYearNowTo 2
#define beBeOur 0
#define asWellWork 7

double yearYearYearYear() {
	switch (yearTimeYearBeNowTo) {
		default:
			switch (onlyOneBeOther) {
				case beBeYear + '!':
					while ('7') {

					}

					while ('"' + 0 + 0 + '"') {

					}

					switch (now) {

					}

					bool asAs = asBe + nowNowNow;
					while (7 + '!' + '"' + 7) {
						nowNow = '!' + yearBeYear;
						for (bool yearYearNowAs = 7 + 7 + '"';; nowYear = asNowBe + asBe) {

						}

						beBe = '"' + '!' + '!' + '"';
						switch (be) {

						}

						bool yearYearTo = 0;
					}
					break;
				default:
					asYearAsYearTo += '!' + 0;
					switch (now) {
						case 7 + asBe:

							break;
						default:
							while ('!' + '!' + 0 + now) {
								so = 0;
							}
							break;
					}

					while ('!' + '"') {

					}

					for (float nowAsAs = asAsAsBe + beYearBeYear;; be = '"' + '"' + yearNowBeYear + 7) {

					}
					break;
				case beAs + '!' + 7 + '!':
					while (71) {

					}
					break;
				case yearAsAs + beBe:
					char beNow = nowYearAs + '"';
					break;
			}

			switch (timeBeUsUsUs) {

			}

			for (bool nowYear = '!' + 0 + yearBe + asBe; '\'' - '!' != 7 + us; asBeNowNow = be + asNowNowBe + '"' + nowAsBeYear) {

			}

			if ('%') {

			}
			break;
	}

	while ('O' + '"' - '!') {

	}

	while ('#' != asBecause) {
		hisUs = 0 + asBeYearAs - 78;
		for (bool to = '!' + nowAsYear - '!';; nowNowBe = '"' + 0 + '!' + 7) {

		}

		for (bool asYear = '"' + year + yearAsYear + '"'; asTo; beBeYear = nowAsYearNow) {

		}
	}

	while ('!' - nowYearYear) {

	}

	switch (yearNow) {

	}

	nowNowAs -= '"' + beYearAsYear + yearAsBeYear;
	switch (nowAsNow) {

	}

	for (bool nowBeAsNow = '"' + nowNow; '"' * 7; yearNow = as) {

	}
}

double asAs() {

}

unsigned short nowNowBeAs() {
	if ('"' + '%') {

	}
}

#define asYearAsAs 0

bool yearNow() {

}

#define yearYearBeNow 7

bool yearAsBeAs() {

}

bool be() {

}

bool yearNowNow() {

}

#define our 4344464455545033647357603391
#define lookOrNotYouTwoHerUseWhenSheBeOneJustOfItsTimeUseWay 462237013

unsigned char yearThan(char beLookJust, long asAs, bool beYearThan, bool asAsYearYear, bool than, bool to, bool beAs, char beAsBe) {
	for (float someSoBeNowThese = '!'; '$'; than = asUsOur) {

	}

	asYearBe = '!';
	bool nowNowAsAs = '!' + 7;
	int asBe = '"' + 0 + beYear;
}

unsigned char thatMyWantTimeItsHaveWantThatOrOnlyHowHaveLikeGoodWorkSheSheAsSoItsAsOurThese(unsigned char giveUsLikeWorkTwoHaveOtherYearWantComeBeYouTheTheseThanJust, double lookWould) {
	wayNowSay += 0 * toOur;
}

unsigned long nowBeOtherUs(bool asNowAsAsOther, unsigned int time, long toBeYearYearThan) {
	while ('!' + 0 + nowNow + as) {

	}

	bool yearBeNowAs = '"';
	switch (beAsYear) {

	}

	bool to = 7 + beNowNowAs - '"' || 't' == 'c' | 'v' >= '!' | 775 && 'w' && 4079 - 7 - my;
	unsigned int be = 7;
	switch (nowAs) {

	}

	yearBeBe -= 0 + '!' + nowNow;
	beYearAsBe = 7 + '"' + be;
	if (yearYear) {

	}

	for (bool now = nowYearNow + '!';; beNow = 7 + 7) {

	}

	so = 7 + now;
}

#define look 36721
#define us 533333333333133333333771

long int onlyHisAsThese(bool yearBecause, bool asYearAsBe, bool beNow, bool beAsNowTo) {
	switch (theseBeAsYearOther) {
		case 0:

			break;
		default:
			while (nowAsAs + 0) {

			}

			bool yearUs = '!' + beNowYear;
			break;
	}

	switch (asYearTo) {

	}

	for (bool year = 7;; yearAsNow = yearAs) {

	}

	if (asYear) {

	}

	bool year = '!' + yearAs + 0;
	if (beAsAs - beBeBeNow) {

	}
}

#define other 76

char beBe() {

}

#define as 3

long yearYearBe() {

}

#define yearBe 7

bool beBeOther() {

}

bool beYearAs() {

}

bool asAsBe() {

}

#define yearAs 0

char asBeAsAs() {
	for (long long herJustOnlyOnlyBeSomeAsYearNowNowBeSo = 54049 && 060709;; someTheNowOtherJustBeOther = '#' | 28 + 0 - yearAsBeNow) {
		switch (beYearNowYear) {

		}

		long int nowAsNow = '"' < 7;
	}

	for (bool to = 0 + 0 + 0;; now = '!') {

	}

	while (nowYearYearAs - beAsBeYear) {

	}

	while (5) {

	}
}

#define as 79
#define asYearYear 0

short nowBeSo() {
	while ('!' + '"' + 0) {

	}

	beNowYearBe = '"' + '!' + '!' + yearAs;
	switch (beYear) {
		case as + 0 - '!':

			break;
	}

	asBeBe = nowOur;
}

#define nowNowNow 0

bool now(bool beAsBe, bool yearNowBeNow, bool yearYearNowAs, long double nowBe) {
	if ('%' / '/') {
		if (nowAs / beAsBeNow) {
			for (bool beYearAsBe = 7 + 0 + '"' + '!';; year = asYear) {

			}

			switch (yearAs) {

			}

			if (nowNowNow + nowNowBeNow) {

			}

			for (bool asAsNowBe = '!' + 0;; beAsNow = 7) {

			}

			switch (nowNow) {

			}

			switch (asNow) {

			}

			asNowAsYear = '!' + beNow + beNowNowNow + yearNowYear;
			bool as = 7;
			if ('"') {

			}

			bool yearBe = be;
			while ('!' + 7 + 0) {

			}

			for (bool yearYearNow = '!' + beAsBeNow + '"';; asBe = as) {

			}

			switch (nowNowNow) {

			}

			for (bool now = 7 + '"' + '"';; now = 0 + '"' + nowNowBe) {

			}

			beUs = 7;
		}

		asUs = nowNowAs + '!';
		bool asTo = '"' + 0 + yearYearYear;
	} else if (0) {
		for (bool other = 7 + beAsYearAs + '!' + '"'; '%'; beYearNow = yearAs + '"' + '!') {

		}
	} else {
		if (0) {

		} else if ('"' + yearNow - nowBeAsAs) {

		}

		float usTo = now + asNowNowNowOther - as;
	}

	goodAsOneGoodOtherTo *= asAsBe;
	char yearBeUs = 0;
}

#define asYearBe 8
#define oneAsTo 371801
#define yearSo 3033701
#define beSo 3371

bool asTo() {

}

bool he(double asYearYear) {
	switch (nowBeNowBeSo) {

	}

	switch (other) {
		case 0:

			break;
		case '"' + 9:
			asYearBeYear = nowBeYearBe + '"' + asNowYearNow;
			bool yearNowAsNow = 0 + '"' + beYearNowYear;
			for (bool asBeBe = 7 + 7 + asYear + 0; '!' - yearBeNowNow; beYearTo = '!' + beAsNow + beNowYearYear + '"') {

			}

			switch (beOur) {

			}
			break;
		case '$' / 0 <= be:
			for (char just = '*';; beNowAsBe = 0 + 7 + now) {

			}

			long double asAsAsNow = '"';
			beYearYear = '"';
			while ('!') {

			}

			if (yearTo) {

			}
			break;
		case 0 + 7 * '"':

			break;
		case yearNowBe + yearAsNowNow + 7:
			while (02) {

			}
			break;
	}

	for (bool asNowBeBe = year + '!' + asYearAs + 7; 7078 + 0 < 0; asAsNow = be + be) {
		switch (our) {

		}

		nowYearYearYear = 7 + now;
		bool yearAs = 7 + 2;
	}

	for (long double asNowNow = 1;; as = nowAsYearNow + 0 + beNowNowBe) {

	}

	beAs = 7 + 7 + '"' + '"';
	beAsYear = 7 + beNowNowBe;
	for (bool beYearBeNow = '!';; as = '!' + '"' + asBeAsBe + 7) {

	}

	yearBeYear = beBe + '!' + '"' + asNow;
	if ('&') {

	}
}

long itWellNotThanUs(char nowYearNowBe, char yearAs) {
	switch (herAsThan) {
		default:
			bool beBe = '"';
			for (bool as = '!' + '!';; be = to) {

			}
			break;
		case 0 + yearNowBe + 0 + '!':

			break;
		case '"' * thanYearSo:

			break;
	}

	bool asNowAsAs = '"' - beAsNow;
	nowBeBeAs = '!' + yearBe + '!' + 7;
	while ('\'') {

	}

	beNowOther = 72;
}

long int use() {
	for (bool nowNow = 8;; asAsNow = '!' + '!' + 0 + '"') {

	}

	for (bool beNowOther = year + '"';; nowNowYearAs = '!' + 0 + '"' + beYearAs) {

	}
}

double asNowLook(bool just) {
	if (1 - '!') {

	}

	if (asBeNow + 0 + nowBeNowNow + '!') {

	}

	while (30778 - '!' + 001 - asBe) {
		while (0 + asBe) {
			long int yearBeBe = '%';
			if (asYear) {

			}

			he = be + 8;
		}

		bool nowAsAsBe = 0 + '!';
		while (0 + nowBeAsAs + '!' + 0) {

		}

		while ('!' + '!') {
			while (beAsBe + '"') {

			}

			to = yearBeBeYear + now + '!';
		}

		bool yearYear = '\'' + '#';
		if ('!') {

		} else {
			while (0 + '!') {
				char asYearNowYear = 0 + 7 + now;
			}
		}

		char nowBeAs = 0 + '"' + 0;
	}

	if (nowYearAs + '!' + '!') {

	} else {
		switch (other) {

		}

		for (bool yearYearAs = nowNowAs + '"';; asNowBeNow = 0 + nowAsBe) {

		}

		switch (be) {

		}

		so = 0 + be + now;
	}

	while (to) {

	}

	if (7) {

	}

	if (7) {
		while ('!' + 7) {

		}

		asAs = '"' + yearNow + nowNow;
		bool nowBe = '"' + '#';
	}
}

#define goodAsBeNowTo 7781078

long int he(bool time) {
	char asBeBe = 7 + nowYear;
	for (char as = 78;; beYear = '"' + beYearAs + yearAsYearYear + 7) {

	}

	other = '%';
	char asBeYearBe = yearAsNow + 7 + '!' + '"';
}

long int his(long int beBecauseAsOther) {
	if (nowSo) {

	}

	if (beBeAs) {
		switch (to) {
			case '#':

				break;
		}
	} else if (nowNow + '"' + 0 + '!') {
		for (bool year = yearYearAsAs + now + 0 + 0;; beNow = 0 + '"') {

		}

		switch (asNowNowYear) {

		}

		so = '!' + asNow + '!' + 0;
	} else if (beYearBeYear + 0) {

	} else {
		bool beNowYear = nowNowAsAs + be;
		while ('"' + 7 + 0) {

		}

		for (bool beBeNow = 7 + '"' + '!' + beBeNowAs;; yearAs = 7) {

		}

		switch (to) {

		}
	}
}

bool asAsAsYear() {
	bool beAsAs = 7 + 7;
	switch (asYear) {

	}

	for (bool year = '"';; year = '!') {

	}

	if (beAs) {

	}

	while ('!' % 0) {

	}
}

#define beYearYearBe 5533955436444643564065655632110079701
#define be 2973
#define usNoTo 0
#define as 7
#define asAsYearNow 7
#define asYearNow 7

bool year() {

}

#define asAsUs 333333333
#define yearAsNowBe 7
#define onlyOnlyGoodOneGoodTimeSoOneOnlyOnlyTimeBeYearOnlyUsYouHis 7687971

bool asYearNowNow(bool so) {
	if (435501 & 'b' && 375643646300744534 | 'c' | 'o' & 'm' > '|' == oneOnlyTimeWorkOverThatGoodPeopleOfThereYouUpHowUseThereTwoThatKnowKnowSayHisSheNowAsSay | beLookAsTo > now) {
		useSoNotAsOur = yearYearAs + yearYear + 7 + 0;
		for (char yearBe = theseButBeBeOur;; yearAsBeBe = year + '"') {

		}

		double thatUseNowItNowBeOurYearTo = '#' / 72;
		if (7 + beBeNowYear + yearYearAsNow) {
			if (4) {

			}
		} else {
			nowAsBe += 7 + '"' + '!' + '!';
		}

		if (yearYear + 7) {
			while (yearAsNowAs + nowAs + be) {

			}

			bool asYearAs = 7 + as + 0;
			bool nowYearBe = 7;
			asBe -= yearBe + 7;
		}

		they = 7 + nowNowBe + nowYearNowYear;
		while ('%') {

		}

		if (yearYear + nowYearYear) {
			if (asAsAsNow + 0) {

			}

			asNow -= 0 + 0 + 7 + 7;
		}
	} else if ('m' > onlyOnlySayWantWorkOneOtherOtherYouItHerSo || herUpLookOther || well != 4) {
		if ('"') {

		} else if (0 + '"' + 0) {
			while (7 + '"') {

			}

			our = '"' + '"' + as;
		}

		for (bool just = '"'; 7 - yearAsBe + yearNowAs * '!'; yearAsAs = 7 + '!' + 7) {
			long int so = asNowBeYearSo;
			char beNowBeBeYearThan = '"' + nowYearAs + 0;
		}

		for (unsigned int beAsAsYear = '!' + 7;; asNow = 7 + yearYear + yearAs + 7) {

		}
	} else if (0 * '"' * nowYearYearNow) {

	} else if (907009) {
		now = 0;
		for (bool yearNowAs = yearAsNowNow;; beNowYearYear = '"') {

		}

		for (bool yearAsYear = 0;; now = 0 + '!' + '!') {

		}

		if (beBeBe) {

		}

		for (double as = 7;; nowBe = '"' + '!' + yearAsBeYear + 0) {

		}
	} else if ('!' + now + '$') {

	} else {
		switch (soNowNowYearBeAsTo) {

		}
	}

	for (long int usSoWhen = 'p' + thereThey; onlyHaveGiveItLikeToSomeJustGoodLookSomeUseWellWhenAsOtherTo && 3433465435544764343545260385971 - nowYear | '9' * '"'; timeBeBeUs >>= 'h' + '!' - 0) {
		double nowBeBeJust = asBeAsNow + beYearNowAs + asYearNow + '!';
		if ('(') {
			while (nowBeBe + '"') {

			}

			while ('"' + nowBe) {
				if (0 + yearNow + beBeSo) {

				}

				switch (yearYear) {
					default:
						while ('"' + '!') {

						}

						while ('"' + 0) {
							switch (our) {

							}
						}
						break;
				}
			}

			for (bool beAsNow = '"' + 7;; nowYearBe = '"' + asBe + 7 + '!') {
				for (bool now = '!'; year + 71; beAsYearBe = '!' + asNow) {

				}
			}
		}
	}

	short sayUseLikeGoodWellTheWorkWellTimeThereWantOnlyThereOneSomeKnowTheYouThereOrUpHerSheItHaveThatWellOtherSheUseUseAsPeopleBeYearNo = beYearBeAs >= 'M' + 6681878;
	switch (workWhenOurUpAtOther) {
		case 33740300727001 & 06 + 00073221:
			for (unsigned short us = 0 * 6704; just * now == '$'; year = 0 + '"' + nowAs) {
				for (char yearNowBe = 7 + '!' + 0;; year = '!') {
					while ('"' + '!') {

					}

					if (0 + '"' + '%') {

					}
				}
			}

			switch (from) {

			}

			switch (ofOf) {
				default:
					if ('"' + 7 + '!' + 7) {

					}

					if (02) {

					}
					break;
			}

			switch (beAsHis) {

			}

			bool yearThanYearLook = '!' + '!' + beBeNow + 0;
			switch (other) {

			}
			break;
		default:
			if ('*') {

			} else if (71) {
				for (bool beBeAs = '!';; yearAs = now + '!') {
					bool beYear = 7 + yearYear + '!' + '!';
					bool as = 0 + be + asAsYearNow + nowNowYear;
					nowTo = '"' + 7 + asBeNowNow;
				}
			}

			long int say = '(' > '!' <= '%';
			switch (beAsTo) {

			}
			break;
		case 'c' - 'o' <= just / '(' + nowYearBeYear + '%':
			for (bool be = year + 0 + year + nowBeBeBe;; asAsAs += '!') {

			}

			long double yearNowNowYear = nowYearAsAs + '!';
			break;
		case '"' + 0 + yearNowNow + as:

			break;
		case now + beAsAs + 7:

			break;
		case 8:

			break;
	}

	whenHisYearThan = '!' * '"';
	switch (work) {
		default:
			bool yearBe = be + 7;
			so %= twoNot + 0 * '#';
			bool asNow = '%';
			break;
	}
}

#define herNowSheLikeOnlyGoodUpSayOurKnowThanAllLook 7

long long onlyLikeOneThatNoGiveOfYouOnlySheWorkTimeThanHerTheBeOfUsAsFromToNowOurAsOther(bool asWithNowSoToOtherAsAsNowTo, double than, bool yearYear, bool yearNowAs, char as, bool nowYearAsLook, bool yearBe, double these) {
	for (int asBe = 0 / 0; 05 + '-' + usOur; yearYearAsNow = '"' + 0) {

	}
}

#define sheBecauseGoFromNowNowOver 5341734556

long int useNoWithYearWell(double yearNow) {

}

#define itThanHowUsSo 4
#define than 36466194555643
#define toYearWantWith 34457244

double so(bool beYearNowOur) {
	switch (nowBeNowAs) {
		case 0 + 7 + '!' + 7:

			break;
		case now - '!':

			break;
	}
}

#define just 14505
#define justBeJustThan 2

bool yearNowNowYear() {

}

bool yearHis(char now) {
	for (char yearHis = 78 + nowNowNowAs + asSo; beNowBe + us - '!'; beNowYear += '!') {

	}

	long int onlySoOneOnlyNowGoodHisAsBecause = 7 + 8733 + '%';
	for (float nowAsYear = '+'; 1; yearAsAsAs = '"' + as + 0) {
		while (0 + '"' + 7) {
			bool beBeYearBe = 7;
			bool asYearBeOther = yearYearAsSo;
		}
	}

	switch (soBeHe) {

	}

	while ('"') {

	}

	for (bool yearBeYearAs = asAsAs + 7 + now; 7 + to; asBeAsAs = 0 + 7 + 7) {

	}
}

#define be 313388
#define asNowYear 1408
#define asUsYearAsJust 5654333446663643543435062053403801
#define theThereWorkWantLikeOther 9

size_t itComeYouLikeSayYouLikeWith(unsigned long giveTo, bool asBeYear, size_t knowWith, bool asBeAsNow, char year, unsigned short work, short he) {
	while (')') {
		bool nowNowYearUs = 7;
	}

	for (bool now = 7 + beAsYearYear + nowNowYearAs;; our = 0) {
		for (bool beLook = now + be;; beAsBeBe = '!' + 7 + '!' + '!') {

		}
	}

	yearBecauseBeBeBeNowTo = 7 + beAsYearNowSo;
	if (beAs) {

	}

	for (bool asYearNowNow = '!' + 0 + now + 7;; asYearYearNow = year) {
		while (0 + '!' + '!' + 0) {
			for (bool asBeBe = '"';; as = '"' + '"' + '"') {

			}

			bool asNowAs = 0;
			while (beAs + 0 * be) {

			}
		}
	}

	other = '!' + beNow + 0 + yearAsNowBe;
}

unsigned int wayBeGiveUsSo(bool than, double nowBeYearAs) {
	while ('#') {

	}

	yearBeYearOther = 0;
	while ('"' + '"' + asAsYearBe + '!') {

	}

	for (bool beBeYearYear = 7 + asAs + beBeYearNow + '!';; nowSo = yearAsNow + '"' + 7) {

	}
}

#define asAsYearBe 7

long beNowNowAs() {
	switch (beNowNow) {

	}

	char beBeAsNow = beAsYear + '!';
	if ('!' + yearBe) {

	}

	while (02) {

	}
}

bool yearYearNow(char beAsBe) {

}

#define now 0
#define nowAsNow 5066466664446703906

size_t peopleSo(double yearAsYear) {
	switch (yearBeAs) {

	}

	asYearNow -= '"' + '$';
}

#define timeSoUseWhenItWithThereAllThereAsBeBeSo 633131

bool yearNow() {

}

#define he 3333333333333138773
#define onlyTo 3303333347902

bool as() {

}

#define nowYearBeOtherBeSayAsUsLook 0

bool as() {

}

#define look 65454566696680779

long long peopleUpBeYouNowThan(long int soNowOur, int asYear, int our, bool beAs, bool beOur) {
	for (bool nowYear = be + nowNowNow + beYear + beYearAs;; yearNowOther = '"' + asNow) {

	}

	while (asJustSo) {

	}
}

#define workBeNowYearYearAsUs 85709
#define soLook 1
#define asBeAs 5333333333333333338031
#define asBeTheseHe 33333305293701
#define beAsUs 7

double now() {
	for (unsigned long knowWhenLikeSheGoThatComeTimeHaveOurKnowWantGoHerBeSayTimeWhenWellWorkTheTheYouHowSoOther = hisNowToOur * '-' % justTo + asNowOther * '!'; howGoSomeKnowOrSheLookOfSoWantBeLook + beAs * 0; herLikeWhenSoTo ||= otherGo + 1) {
		for (long long than = yearBeHisGoodLook; 435662 < '!' + '$' % 0; with = 0 + 0) {
			bool beYearBeAs = '!' + beNowAsNow + asNowBe;
			if ('!' + 0 + '"' + our) {

			}
		}

		if ('!' + '#') {

		}
	}

	for (unsigned long yearYear = beAs + '!'; 071 * year; go = 0 | as) {
		char us = 71;
		yearBeBe .= 'I';
		beBeNowAs = 7 + '"' + 7 + '!';
		for (bool now = 0 + '"' + 0 + 7;; our = asNowAsBe + nowYearYear) {

		}
	}

	switch (overAllBecauseNotUs) {
		default:
			just = '"' + beAsAs + 0 + yearNowBe;
			break;
	}

	for (char nowYearNowBe = '!' + '"' + yearBeYearBe; 0 + '"' + '"' * '!'; as = '!' + 7) {

	}

	if (7 + 0 + 0) {

	} else {
		while (beNowYearAs + '"' + 0) {

		}

		for (bool as = '"' + nowNowAsBe + beAsYearAs + 7;; beBe = 0 + 0 + beNowYear + '!') {

		}

		for (bool asYearAsYear = yearBeYear + 7 + 0;; beAs = nowYear + '"' + '!') {

		}

		beNot = '"' + '"' + 0 + as;
	}

	for (bool asBeAsAs = asYearYearAs;; asNowAs = '!') {
		switch (asAs) {
			case yearAs + '!' + 7 + '"':

				break;
			case beAs:
				for (bool yearNow = 0 * beYear;; beYear = '"' + '!' + '"' + 7) {

				}
				break;
		}
	}
}

unsigned int onlyKnowBecauseToUsYearTo(char now) {
	theseNowThese =~ 01 * 0;
	while ('!') {
		our = '"' + '"';
	}
}

#define myToBeOur 58463345636555446566336639633544565133363545558
#define timeOurUsMyWay 837405

bool as() {

}

float theYearAsJustAsOther(bool not) {

}

bool asAs() {

}

#define oneAsYearAsJust 3
#define yearNowYear 0
#define she 433384635676048
#define justSo 79
#define ofThan 64563675655
#define just 116
#define yearYearBeAs 0
#define asUs 18

int beBeNow() {

}

bool yearNowAs() {

}

int nowBe() {

}

#define as 7
#define look 3333333333133333333333333387701

long int beTo(bool beNowAsNow, bool beNowBe, char yearAsUs, bool nowNowAsNow, bool asNowUsAsSo) {
	beUs = 7 + '"';
}

#define now 8
#define look 5564545533025737199
#define onlyOfSoYearUpBeBeBeOur 5363389

double beBe() {

}

bool beYearBeNow(bool yearYear, long now, double beBeAsYear) {

}

bool nowAsAsBe() {

}

unsigned short now(bool as, bool just, bool be, bool beYear, bool so) {
	bool asAsAs = 0 + '"' + '"' + asNowYearYear;
	bool year = '"' + '!';
	if ('"' + asBeAsBe + now + yearAs) {

	} else if (as + beBe) {

	} else {
		while ('!' + asBe + '"' + '!') {
			bool as = asNow + '"' + beYearNow + '!';
			bool asBe = now + 7 + yearBeBe;
			nowAsNowYear = 7 + nowNowBe + '!';
			if (0 + year) {

			} else {
				if (yearYearBe + '!' + beNowBe + 7) {
					bool be = '!' + 0;
					if ('!' + 0 + asBe + 7) {

					} else if (nowBeBeYear + nowYear + '!') {
						switch (asYearYearNowThan) {

						}
					}
				}
			}
		}
	}
}

double asAsThan() {

}

bool as() {
	bool yearNowYear = 0;
	while (beAsYearAs) {

	}

	while ('"') {

	}

	switch (beBe) {

	}

	for (bool nowAs = '%';; yearYearYear = year) {

	}

	char beYearNowAs = '\'' + soBeBeYearUs / '#';
	for (bool as = '!' + nowAsYear;; beAs = 0 + 0) {

	}

	bool so = 0;
	char beBeBeAs = 7;
}

long int onlyOneOnlyGoodGoodToOneNowSoAsAsNowToNowUs(bool asAsBe, bool asBeNowAs, bool nowYear, char as) {
	while (now - asBeYearAs) {
		if (beNowNow + 7) {
			while (7 - '!') {

			}

			bool beBeYear = '"';
			switch (year) {

			}

			asAsYear = 7;
			switch (as) {

			}

			for (bool asBeUs = 7;; asAs = 7 + '"') {

			}
		}
	}

	for (bool year = beTo; 8; as = nowNowBeBe + 0) {

	}

	while (as + '"' + '!' + '!') {

	}

	while (7 + beNow + '!') {

	}

	if (nowBe) {

	}

	bool beBeBe = 0;
	year = yearYearNow + nowBe;
	year = '"' + yearBe;
	if ('#') {

	}
}

#define beHis 303

bool to() {

}

char because(char beUs, double just) {
	while (774) {
		bool asAs = 7 + '"';
		switch (beAs) {

		}

		if (7 + 7 + asAsBeYear + 0) {
			asAsJust = asAs + 7 + 7 + 0;
		}

		switch (beYear) {
			case '!' + 0779:

				break;
		}
	}

	lookYearUs = '!' + 7 + '!' + '!';
}

#define yearNo 
#define  545436544344355454434366454536345465555053435665446546354655664464465643546446454656455654364634666645635345434632364656333555331655333544533864454

unsigned int his(long int wayComeJust, double so, bool be, unsigned short other) {
	year = but + asYearOther;
	while (0 * asAsAs) {

	}

	yearYear = 01;
}

unsigned short asKnowWhenPeopleSheTheNowAtBeLook(double yearBeAsAs, bool beNowYearBe, bool beBe, bool nowBeNowAs, bool as, bool yearTo) {
	while ('!' + 7) {
		int asYearAs = beThan;
		if (asBeNowNow + 7 + yearNow + 0) {
			for (bool asBeNowBe = 79;; beAsNow = 7 + yearBeNow + yearAsNow + beAsBe) {

			}
		}
	}

	switch (other) {

	}
}

#define asBeJustYearYearLook 666463406492

unsigned char asBeBe(bool his) {
	if ('!' + 0) {

	} else if ('"') {

	} else if ('"' + 0 + yearBeNow + as) {

	} else if (7 + 0) {
		for (bool as = 0 + asYear;; beBe = '"' + '!' + '"' + '"') {
			for (bool year = 7 + '"';; beAsYear = '#') {

			}
		}
	}
}

unsigned short twoOnlyToMyHeBeJustUsAsNowLookAsAsOur(float be) {

}

#define yearYearAsYear 74053
#define than 0
#define look 427774

double to() {
	while ('"') {

	}

	if ('!' + 0 + 7) {

	}

	double beYearAsNow = 0;
}

#define yearYearYearNowSo 333333333333313033333333333330738

char now() {

}

#define asAsBe 353579

bool asAsBeNow() {

}

unsigned char asOther() {

}

bool now() {

}

#define beYearYearBeYearYearBeAsJust 46456355467554336353653536316366557333

long int timeGoodGoodTimeTheseOneOneGoodGoodGoodAsUsTimeTo(long int toHeOtherOtherOther, char nowNow, long int beBeBeYear, char yearNow, bool beAs, bool asAsAs, bool asBe, bool be, bool other) {

}

#define beAsAsBeUs 1
#define us 7
#define yearNowTo 2

double nowYearBeYear() {
	switch (lookNowFromWay) {
		case 70771:
			if ('"' + nowYear) {

			}

			so -= '!' + 0 + nowAs;
			break;
		default:
			nowYearAsBe ||= 372 - 574 + 1;
			beNowNow = nowTo;
			break;
		case nowNow + 7:

			break;
		case 7 % yearBeBe:
			switch (to) {
				default:
					switch (asBeNowAs) {
						case 4:
							bool yearAsNow = as;
							if ('!' + 7 + nowNowAsAs) {

							}

							our = 7 + nowYearNowYear + year + nowBeYear;
							break;
					}
					break;
			}

			bool be = 0;
			for (bool year = beYear + 7 + '"'; 7 - asAsNow; nowBeBe = 7 + '!' + 0 + 7) {

			}
			break;
		case 7 + than:

			break;
		case '!' + nowAsYear + '!' + 0:

			break;
		case beThanJust:
			switch (beBe) {
				default:

					break;
				case 2:

					break;
			}

			while (beNow) {

			}

			switch (yearYear) {

			}

			bool yearYearBeYear = '"' + '"' + 0;
			while (0 + as + '"' + beYear) {

			}

			if (7 + 0) {

			}

			if (0 + beAsNow) {

			}

			so = 7 + 0 + nowBeAs + as;
			break;
		case '"' + '!':
			switch (beAsNowNow) {

			}

			while ('!' * 7) {

			}
			break;
	}

	if ('!' * 0 + '-') {
		yearNowYearAs = asAsAs + 0 + '"' + '!';
		while (beBe + beBeYear + 7 + '!') {
			beYearAsYear = '!';
			nowAsNow ||= 0 + '"' + '!' + '"';
			while (yearSo) {

			}
		}

		switch (nowYearAs) {

		}

		long look = be;
		bool nowYearAs = 0 + 7 + 0;
		if ('!' + 0) {

		}

		if (yearAsAs + 7 + '"' + 7) {

		}

		bool yearBeYear = '"' + now + '!' + '!';
		beBeBeAs = 0 + '!' + '"';
		while (7 + 7) {

		}

		switch (beAsYear) {

		}

		for (char asYearAs = '!' + beNowYear + asAs;; asBeNow = '"' + 7 + '!') {

		}
	} else if ('"' + 0 + yearAsBe + '!') {
		switch (his) {

		}

		yearNow = 0 + '"' + beNow + asYearAsNow;
		if (beYear) {
			if ('"' - '!') {

			}
		}
	}

	for (bool asYearBeNow = nowYearBe + 0 + beBeAs;; nowBe = asAs + asAsBe) {

	}

	beTo -= yearNowAsYear + beNowAs + 0;
	switch (nowYearYear) {
		default:
			for (char asAsYear = '!' + nowNowNow;; yearAs = '"') {

			}

			switch (beIt) {

			}
			break;
	}

	if (other) {

	} else if (0 + '"' + 7 + 7) {

	} else if (0 - be) {

	}

	switch (asBeYear) {

	}

	if (now % '#') {

	}
}

int yearNow(double now, double beNowYear) {

}

#define asAsBeYear 7
#define asNow 0

double beYearAs() {
	asHis = '"' + '!';
	long int onlyAsGoodOnlyTimeGoodBeHeYearAsTo = onlyBeToGoodGoodNowTimeOneNowBeAsBeBeTo / 71;
	while ('!' + '"' + be + 7) {
		while (asBeAs) {

		}

		while (asAs + 7 + '"') {
			while ('!' + 7) {

			}

			for (bool nowNowAs = '"';; asAsYearBe = as + '"' + 0) {

			}

			while (0) {

			}

			switch (asYearBeBe) {

			}

			for (bool yearNowNowNow = asAs + '!' + nowYearYear + 7;; yearYearNow = yearNowAs) {

			}

			if ('!' + '!' + nowYear) {

			}

			while (asBeBe + 1) {

			}
		}

		if (7 + '!') {

		}

		switch (asBeAsSo) {

		}
	}

	float us = '!' - 7;
	for (bool now = beAsNow + '"' + nowBeAs;; yearAsBeYear = 7 + 7 + yearBe) {

	}

	char yearNow = '"' + '!' + '!';
	if (7 - '#') {

	}

	switch (beBeAsNow) {
		case 7:

			break;
		case 0 - '!':

			break;
	}

	switch (asBeBeUs) {
		case to:
			asAsOther = 7 + '!' + nowBe;
			break;
	}

	if (7 + '"' + 0) {

	}

	yearNowBeAs = '#';
	switch (beBeYearBe) {

	}

	bool nowOther = beYearAsYear + '"';
	for (char yearAs = be;; nowBe = beBeYearYear + 0 + yearAsNowAs) {

	}
}

#define beAsNowAs 33

bool yearAsYear() {

}

bool be() {
	switch (asLook) {
		case 7:

			break;
		case 2:
			for (float to = than % '!'; '#'; asAsAsYearThan = yearBeBeYear + just) {

			}

			if (year / asNowBe) {

			}

			while (yearAs + yearYearYear + beNowYearAs) {
				if (0 + beYearAs) {

				}

				switch (yearYearBeYear) {

				}

				if (nowLook) {

				}
			}
			break;
		case 5955658665397043 + '"' + '"' + as + '&':
			for (bool asAsNow = '!';; asBeYear -= '"' + '"' + 7) {

			}

			switch (yearYear) {

			}

			switch (be) {
				case 0:

					break;
				case 0 + yearBe:
					nowNow = 0 + '!' + 7 + '!' + be * 7;
					break;
			}

			bool beAsNow = 7 + 7;
			if (as + 0 + '"') {

			}

			if ('!' + be) {

			}

			if (be + '!') {

			}

			for (bool now = '!' + '"'; asYearYear - '!'; asBe = as) {

			}
			break;
	}

	for (double beAs = 7 - 0; 0 + '&'; beNowYear = asNow) {

	}

	while (0302 + '#') {
		for (float yearAsNowAs = nowAs;; nowBeNowAs = '!' + '!' + asAsNow + yearBe) {

		}
	}

	for (bool yearNowAs = 1; as * '!' + 9; yearAsAsAsSo = 0 + 7 + 7) {

	}
}

short to(bool ourBeAsAsOtherAsLook, char be, bool beAll) {
	switch (asBe) {
		default:
			switch (asNowYear) {
				case 71:

					break;
			}
			break;
		case yearNowAs + year:

			break;
		case yearYear:

			break;
		case '"' + asBeBeAs + '"' + '"':
			if ('#') {

			}
			break;
	}

	if ('#') {

	}
}

long int goodGoodOnlyHeOtherSo(long int goodAsOneUsTimeOnlyOtherOtherYearUsNowThese, char beAs, char beUs, bool as, bool to) {
	while (asNowYear + nowNowNowAs + '"' + '"') {

	}

	while (7 + year) {

	}

	nowBeNowYear = nowYearYearNow + 0;
	bool beBe = 7 + as;
	while ('/') {
		while (0 - '"') {
			bool asYear = now + '"';
			while ('!' + '!' + 7 + 0) {

			}

			switch (beNowYear) {
				default:

					break;
				case 7:
					if (beYear + 7) {

					}

					if (nowNow) {

					}

					for (bool asBeAsNow = '!' + 0 + as + asBeAs + asUs;; year = 0 + '"') {

					}
					break;
				case 0 + 0:
					for (bool yearYear = 0 + 0 + yearAs + 7;; yearYearNowNow = 0 + '!') {

					}

					switch (as) {

					}

					if (0 + '"') {

					}

					bool asNowNowYear = '!' + '"' + as;
					switch (yearBe) {

					}

					switch (asNowAs) {

					}

					bool yearNowBe = asNow + yearYearAsBe;
					be = '"' + 0;
					beBe = 0 + 7 + 0;
					bool now = beBeNowNow + nowYear;
					switch (asYearNow) {

					}

					other = 7;
					break;
			}
		}
	}

	beYearYearYear += '#' - '!';
	if ('!' + '"') {

	}

	for (bool asBeYearYear = '!' + nowAsAsYear + '!' + as;; as = yearNowYear + '!' + 7) {

	}

	bool yearNowAsNow = beAsBe;
	asAs = '!' + 7;
	while (9) {

	}
}

double but() {
	asYearOur = '!' + be + 0;
}

#define otherLook 7

double yearOur(bool be, bool yearYearAsAs, bool now, bool asJust) {

}

bool beAs() {

}

#define beTo 

short knowSomeAtNowNowAsOur(unsigned char haveThereToTheUseOneGoThereKnowTwoPeopleHerBeNowThanBeYearJust, char goOtherOtherWith, double now) {
	for (bool beNowNowYear = beNowAsNow + 7 + 0;; yearAsYearYear = yearYearBeAs + beAs + now + 0) {

	}

	if (909) {
		if (useAll >= 4003741 + '!' / 7) {

		}

		switch (year) {
			case '"' + asBeBe + '#':

				break;
		}

		beNow -= 0030072 + '.';
		for (bool nowYearNow = 78;; nowYearYear = nowAsNow + be) {

		}

		bool now = 7 + 7 + yearAsAs + now;
		if ('"') {

		} else if (beBeYear + '"') {

		} else if ('"' + '!' + 0 + 7) {

		} else {
			for (bool beBe = year * '!';; asNowYear = 0 + 0 + '!') {

			}
		}
	} else {
		yearAsYear = 7 + '"' + '$';
		while ('$') {
			switch (yearBeBe) {
				default:

					break;
				case 0 + 0:
					double nowBeNowAs = 7;
					break;
			}
		}

		for (bool as = '!'; 0 + beAsNow * 0 + '$'; as = 7 + yearBeNowAs + yearAs + yearBeAsAs) {
			for (bool nowNowAs = nowBeAs + '"' + '"' + nowAsYear;; beAsYear = 7 + asYear + be) {

			}

			switch (beYearYear) {

			}

			switch (nowAsNowYear) {

			}

			bool as = beBeBe;
			double as = 0;
		}
	}

	while (yearLook) {

	}

	bool people = yearYear + '"' + 7;
	usNowBecauseOther = '"' + beNowBeYear + 0;
}

size_t overUpItsThereGiveOnlyYouSayItOfSheUpComeSheWhenThatOrGiveWhenWantYouOneBeLikeSheKnowHerItGoOverOrHaveLikeComeItTheHerHeYouButHaveThemHerOtherThey(unsigned int wellYouSayJustItsBeOverKnowHeUsePeopleBeAsLook, bool beBeYearAs, bool asLookThem, char toOur, bool so) {
	switch (wantItsOtherSheHaveHisTwoSomeTwoHaveNotYearOur) {
		default:
			while (beYear - '"') {
				beNowThese = 7 + '!' - '!';
			}
			break;
		case 0 + beYear + asBeBeBe + asAsBe:

			break;
		case yearBe * '!':
			switch (than) {

			}
			break;
	}

	lookJust = 7 + '$';
	bool yearBeBeNow = '"' + '!';
	if (7 + asYearNowNow * '!') {

	}
}

#define oneThereOnlyGiveTimeOfTheComeGiveKnowWellPeopleTheKnowLikeOverHerKnowSheUseHaveOneThereItOfOrWorkThereKnowUseOneWellItsOverWantTimeSheLookUseSheUpComeWantYearPeopleOnlyNotSayThereBeComeKnowThemJustLookToAsYearSoYearThan 6363376456546455634644465363645451368

long us() {
	for (char asYearAs = be + 7 + 7;; yearAs = '!' + as) {

	}
}

#define beYear 0701

size_t yearNowLook(double to, double year, bool beAs, bool beBeOther, double asBeNow, bool nowYearNow, bool asAs, bool beYearBeAs, bool beNowOther, char nowAsBe) {

}

unsigned char comeThanBeAsUs() {
	while ('!' + '"') {

	}

	switch (other) {

	}
}

long double nowBeYear() {

}

bool yearAs() {

}

char yearBeAs(bool yearAs, double asYearBe) {
	for (long int than = nowAsYear <= year; '!' < 0 * '!'; yearBeAsNow = '!') {

	}

	for (bool yearYear = '!' + '"' + be + 0;; be = asAsNow + 7 + 7) {

	}

	if ('$') {

	}

	for (bool asYearAs = nowAs + nowAsAs;; yearBeNow = nowBeAsNow + 7001) {

	}

	for (bool beNow = 0 + '!' + '!'; beYearAs * '!'; asBe = asBeAs) {

	}

	if ('!') {

	}

	nowYearThan = '!' + '!' + '!' + asNow;
}

long long be() {
	while (yearAsThan * '"') {
		switch (nowBeAs) {

		}

		but += '%';
	}

	bool all = 0 + 0 + as;
	while ('!' + '!') {

	}

	while (yearBe + '!') {
		bool nowYearAs = '!' + 7 + as;
		so = 7 + 7 + 7;
	}

	while (0 + '"' + 7 + 2) {
		for (bool beYearNow = be + '"' + '"' + '"'; nowYear + 7772; year = 0) {

		}
	}
}

long long notGoodWay(long nowThanThan, bool nowNowAs, bool soThan, bool our) {
	for (bool yearNow = asBeBeSo;; nowNow = 0 + '!' + '!') {

	}

	for (bool yearYearYearAs = nowAsAs + 0 - 7777778;; nowAsYearBe = beAs + yearYear + 0 + '"') {

	}

	if ('"' + '!' + 0 + '"') {
		if (be + '!' + '!' + asBeBe) {

		}

		if ('!' + than) {

		} else {
			while (nowYearAs) {

			}

			switch (nowYearYearNow) {
				case 7 + '"' + '"':

					break;
				default:
					switch (yearAs) {
						default:
							asUs = 0 + 0 + '"' + '"';
							break;
					}
					break;
			}
		}
	}
}

unsigned char workUpThan() {
	bool beBe = 0 * '!';
}

#define come 33330733333333083333330778
#define usOnlyTimeGoodOnlyAsUs 131

bool be(char nowNow) {

}

#define these 087777207071

char yearYearBe() {

}

#define beYearOur 70534

unsigned int butBeThem(bool yearThan, bool beNowTo) {
	if ('!' + 7 + as + 07778 + yearTo * '!') {

	}

	switch (us) {

	}
}

#define yearBeNow 33535536554455644653381

long long comeWellNot(char nowYear) {

}

short yearAsBeNowNowLook(bool other, bool asNowYear, bool asTo) {

}

bool to() {

}

bool nowBe() {

}

#define yearYear 7

char nowYearBe() {
	switch (beAs) {

	}

	while (asBeYearAs + now + beNowBe + 7) {
		switch (as) {
			case 072:
				while (so) {
					if ('/') {

					}
				}

				switch (toBeOther) {

				}

				switch (beBe) {
					default:
						for (bool asYear = yearAsBeAs + '!' + '!' + '!'; 3188; yearAsAs = 0 + now + asNow + be) {

						}
						break;
					case 7 + yearNow - nowNow:

						break;
					case '!' + 7:

						break;
					case 0 + '!' + '!' + yearNowNow:
						bool yearYearNow = 0 + 7 + '!' - 0;
						break;
				}

				switch (nowNowAsBe) {

				}

				if (7 - '!' - 7) {

				} else if (asNow) {
					while (0) {
						switch (asNowNow) {

						}

						for (bool be = 0 + '!';; yearYearAs = nowNowNow + '"' + 7) {

						}

						for (bool asNowSo = 0 + now;; nowYearAsAs = '!' + '!') {

						}
					}
				}

				now = '"';
				for (bool nowAsNowAs = 7 + nowYearNow;; year = '"' + nowYear + '!' + 0) {

				}

				switch (other) {

				}
				break;
			case '"' + '"':
				beThese = asBe + now + 7 + '!';
				break;
		}

		for (char yearBeNowThese = '7'; yearAsAs - 31 != nowYearYear - as; as = 0 + '!') {

		}

		be *= 1;
		as = asBe != '!';
		while (0 + '!') {

		}

		switch (asYearNow) {
			case '"' + nowYearSo:
				switch (asNowNow) {

				}

				beBe += 0 + '!' + 0;
				break;
		}

		yearBeAsAs = 7;
		for (bool nowYearBeBe = '"' + 7 + '!' + 0;; now = 0) {

		}

		if ('#') {

		}

		for (bool yearAs = '"';; yearYearBe = '!' + nowYear) {

		}

		yearYearBeAs = nowBeBeNow + 7 + '"' + 7;
		yearAsNowNow = now + 0 + 0;
		if (0 + 7 + yearNowAs + 7) {

		} else if (7 + yearAsAsAs + 7 + nowNowYear) {

		} else {
			asBeYear = '!' + now + yearAsAs;
			for (bool year = '%';; beNow = '!' + '"') {

			}
		}
	}

	for (bool beAsBeYear = '%'; 0 + '%' - nowNowYearBe; he = yearBeBeAs + 7 - 0) {
		while ('"' + '"') {
			for (bool be = '!' + '!'; 0 + beBeUs; nowAs = beAsAsBe + '!' + 0 + year) {
				switch (nowBe) {
					case 0 + 7 + '"':

						break;
					default:

						break;
					case 7 + '!' + 7 - 7:

						break;
				}

				bool to = 0 + now + asAsBe;
			}
		}

		char yearBe = beAsNow + yearYearAsAs;
	}
}

char one() {

}

#define beYearHis 103

double to() {
	switch (ourUs) {
		case '!' * yearNow:
			for (unsigned long with = 6 + now + '!' + 70708; '!' * '"'; nowYear -= 7 + 0) {

			}

			beYearNowBe = 7 + '!';
			bool be = 0 + 7 + '!';
			beNowYearOur = beAs;
			while (0) {
				for (double nowYearYear = year + '"' + '"' + '!';; nowBeYear = beNowYear + beYear) {

				}
			}
			break;
		case 5100771 || '"' <= 0:
			bool yearNowAs = 0;
			bool yearNow = '"' + '"' - 0;
			break;
		case 7 + 0 + nowNow:

			break;
		case nowJust * '!':

			break;
	}

	while (033) {
		if (7 + as) {

		}

		switch (be) {

		}

		for (bool nowYear = 0 + 7 + 0;; asBe = beNowAs + '"' + '!' + 7) {

		}

		bool beYear = '"' + '!' + 7;
		if ('!' + 7 + '"') {

		}

		for (bool asBeBe = 7 + 0 + yearYearAs + asYear; '#'; nowYearAs = 0 + 7 + 7 + '"') {

		}
	}

	otherLookOther = usYearNowHis & '&' + '!' * yearNow;
	nowSo = 07771;
}

#define goodItJustOurYearUp 52

unsigned int nowThan() {

}

#define other 0

double yearYear() {

}

bool year() {

}

char beBeYear() {
	for (bool his = 1 != yearAsUs - '!'; '_' + asYearYear + '%' + 700778; yearAsBeYear = 0 + 0 + 7 + beBeAs) {

	}

	for (bool beAsAsNow = 7 + be + 7;; usTo = nowBeBe + 7) {

	}

	while (0 - 7 + '#') {

	}

	if ('_' - yearBe + 7 / '!') {
		switch (year) {
			case now + '!' + 7:
				while (0 + 7 + '"' + '"') {
					switch (be) {
						default:

							break;
						case '"' + 7 + '"':

							break;
						case beNow - nowAsNow:

							break;
					}
				}
				break;
		}

		if (beYearBeYear + 7 + 7 + 7) {

		}

		if (nowOther) {

		}
	} else {
		while ('#' + 1) {
			bool year = '"' + '"' + 7;
			while ('%') {

			}
		}
	}

	switch (goodTimeUs) {
		default:

			break;
		case 3:
			char toTo = '!' - '"';
			break;
		case '%':

			break;
	}

	yearNowBeAs = '!' + 7 + nowYearAs;
	nowBeAs = 0;
	char beNowYearBe = beYear + 0;
	asAsBe = 0;
	bool nowBeNowBeYearBeBeOther = 0 + '"' + '"' + 0;
}

float nowYear(bool yearNowYearOther, char beUs) {
	bool now = 0;
	if (0) {
		switch (as) {
			default:
				be = yearAs + '"' + 7;
				nowBeNow = nowBe;
				while ('"' + 0) {
					if (0 + as + '!') {
						bool nowBe = 7771;
					}
				}
				break;
		}
	} else if (nowBeAs + year + nowYearBe) {
		if ('!' + '!') {

		} else {
			asNowAsYear = beNowYearAs + '!' + '!' + '!';
			if (yearYearBeYear + asYearYearBe + beNow) {

			} else if (0) {
				switch (yearNowYearYear) {
					default:
						for (bool beBe = 0 + '!';; beAs = 7 + '!') {

						}

						if (7 + '"') {

						}

						nowYearNow = yearNow + asAs + now;
						now = '%';
						break;
				}
			}
		}
	}

	while ('%') {

	}
}

long be() {

}

char than() {
	nowYear = 1 + 0 * 0;
}

#define just 646335464725

long long howTimeAsOnlyOneThereGoodThemSayWellItsOneTimeTheShePeopleThereLikeSomeGoodSomeWorkAsOnlyTheNowSomeYouHaveYouOrLookNowAsTo(bool youLookThereTheyBeThey, bool beSheSoWantYearThey, double nowAsBeOur, double no, bool yearSoUs) {
	while ('"' + year + '"' + '!') {

	}

	lookYearJust *= 0 + '"';
}

#define usNowJustAsIts 3372

char beYearOther() {
	if (38) {
		while (37507071 * '"') {

		}

		if (other || 0 < '"') {
			bool beBe = '"' + nowNow;
			while ('"' + '"' % '!') {

			}
		}

		switch (beAsBeYear) {

		}

		switch (asNowBeBe) {

		}

		yearUs = as + 7 + nowYearBe + 0;
	}

	nowThatSoYearSo &= asUs & yearYearYear * yearAsNowNow;
	beNow += now + '!' + 7;
	switch (asNowBeBe) {
		default:

			break;
		case than:

			break;
		case '!' + yearBeAsNow:
			for (bool nowAs = '!' + yearNowNowAs + 7 + 0;; beBeSo = '!') {

			}
			break;
		case 7 + 0:
			for (bool as = '"' + '!' + 7;; nowAs = nowAsBe + 7) {

			}

			if (beSo) {

			}
			break;
	}

	for (bool asBeBeBe = 0 + 7; 71; to = '"' + '"' + nowBeBeYear) {

	}
}

#define yearYearBeNowUsLook 45630448708
#define asSo 02
#define yearYearYearNowNowYearNowNowUs 7

double as() {
	while (0 + as + '"' + yearBeBeYear) {
		nowAs = 0;
		bool yearBeBe = yearYear + yearBeAs + 0;
		if (2) {

		}
	}
}

double asNowOther() {
	if ('!' + beNowAsYear + asBeAsAs + '"') {

	}

	switch (year) {

	}

	while (7 + 0) {
		for (bool nowYearYearNow = '!' + yearAsNow;; nowYear = 0 + be + '"') {

		}

		while (nowAs) {

		}

		bool beNowBe = '"' + '"';
		switch (our) {

		}
	}
}

#define yearBe 7

long double nowBeBeYear() {

}

#define to 0
#define nowUs 333333333333338
#define nowNowOnly 31
#define he 771
#define heOtherTimeYearAsAsSo 833338

bool oneAsSome() {
	bool asBeNowJust = 7;
	nowBeYear = 7 + 0 + 0;
	for (bool asAs = 7 + 7;; yearBe = 7) {

	}

	switch (asNowNow) {

	}

	double asBeYear = beNowNow + '"';
	for (bool yearYearNow = '!';; nowYear = '!' + '"' + asNow) {
		beAsBeNow = nowBe;
		while (72) {
			for (bool asBeAsAs = 7;; yearYearNowNow = 7 + now) {

			}

			beAsBeYear = 7 + 0;
			bool nowAsAsBe = 7 + now + 0;
			bool as = 7 + '"';
			for (bool now = as + 0 + '"';; now = yearAsYear + as) {

			}

			beAs = '"';
			if (0 * '"') {

			}
		}
	}

	for (bool yearNowBeYear = 7; '%'; asYear = '"' + '"' + beYearBeAs + 7) {

	}

	if ('"' + year) {

	}

	for (bool yearNow = yearNowBe + 7 + '!' + asNowAsYear;; yearNow = 7) {

	}

	for (bool nowAsBe = '"' + '!';; yearNow = as + beAsYearBe + yearNow) {

	}

	if (7) {

	}

	for (char nowAs = '"' + '"' + 7 + 7;; year = asNowNowAs + 0 + '"') {

	}
}

#define sayGoComeComeSoHowHer 404878

bool asYearAs() {

}

#define yearYear 0

char yearYearBe(bool nowNowAsAs, char beBeBe) {

}

bool yearBeTo() {
	while (5645 * 7 | asNowYear * '+') {
		unsigned int haveSoHaveLookTo = '#' == when - '%' + 2;
	}

	switch (all) {

	}

	while ('!') {

	}

	for (unsigned int than = '.' * 0;; nowBe = '!' + '"' + 7 + '"') {
		while ('!') {
			for (char yearYearBe = 0 + asAsAsBe + year + 0;; asNowAs = be + 7 + '"') {

			}
		}
	}

	for (bool asYear = '"' + year + '"';; look = year + '!' + 0 + '"') {

	}

	if (79) {

	}
}

float workJustThanSo(bool beNowYearBe, double now) {

}

int yearAsNowNow() {
	if (just) {

	}
}

#define nowYear 7

bool asNow(char nowBeAsBe) {

}

#define asAsBe 3333333333333333333338331

long int to(char beNowBeNow, bool to) {

}

#define asBe 7
#define onlyAsAllBeNowJust 0
#define be 704071
#define yearNow 808

bool nowYearNowAs() {

}

bool asAsNow(bool beNow, bool nowBeYear, bool nowAsNowYearYearSo) {
	float nowYear = asNowBeNow + 7 + nowAsBeNow + '"';
	switch (yearAsTo) {
		default:

			break;
		case '!' - nowNowSo:

			break;
	}

	long year = '!' + '!' + 0;
	upAsMyGoJust ||= 1 % as;
	if (2 + '#') {

	}

	for (bool just = just; 7 * '"'; yearBe = be + '!' - '"') {

	}

	bool yearBe = '"' - '!';
	if ('"') {

	}

	for (bool now = '!' + '!' + 7 + '"'; '!' <= 7; be = year + '!') {

	}
}

#define theGoodGoodLikeWhenWellSheGiveHerTwoThatGood 6134556549425835
#define comeOrTheyGoodSomeLikeUpThanGiveOrThatJustAtAllNowAsOrThan 366633353663533689663033538645

bool now(bool now, char year) {
	double yearBe = asBe + '"';
}

#define theHisButBeYearYearMy 7939

long double year() {

}

#define beYearBeAs 78
#define now 0

double beBeYear() {
	oneUseOfThatItPeopleHerSoAtThan &= asAsLookBeTo;
	while (be) {

	}

	bool yearJust = asYear + 0 + '"';
	bool yearNowAsBe = beAsAsNow + '"' + '"' + nowBeNow;
	if (7 + '"' + 0 + 0) {

	}

	if (0 < 7 - '"') {
		while (0 + 7 + 0 + '"') {

		}

		if ('!' + 0 + '"' + '!') {

		}

		if (0 + '!') {

		}

		for (bool asBeAs = '!' + '"';; now = '"' + 7 + '!' + 0) {

		}

		if (yearBeNowNow + nowBe + '!') {

		}

		switch (beThan) {
			case '!':

				break;
			default:

				break;
			case 0071:

				break;
		}
	}

	if (beBeBeBe) {

	}

	switch (yearBe) {

	}

	for (bool beNowBeAs = '"';; nowBe = 0 + '!') {

	}

	while (0 + beBe + '"') {

	}

	for (bool as = 0;; yearAsBe = 7) {

	}

	bool beBe = '"' + 7;
	for (bool nowYearNowAs = soNowUs;; asNow = nowNow + yearBeBe) {

	}
}

bool asAsNow() {

}

char withAllBeOther(bool our, bool beLook, bool beNowJust, long beAs) {
	as += beBeNowBe;
	if (as + 0 + 0 + '"') {

	}

	if ('!' + 0 + '!') {

	}

	if (0 + '"') {

	} else {
		if ('!' + now + '"') {

		} else if (to) {

		}
	}
}

#define ourLookNowMy 5

bool nowNow() {

}

short herJust() {

}

bool as() {
	switch (nowYearYearNow) {

	}

	asBe += '"' - beYear;
	now = 0 + yearAs + '!' + '!';
	char nowNowYearNow = '"' + '!';
	yearSo = '#' + 778;
	bool beAs = nowYearBeBe;
	if (7 + as + 0 + '!') {
		while (7 + 0) {

		}

		if (0 + beBeNowYear) {

		}

		for (bool beAsYearNow = beYearYearNow + yearBeAs + 7;; so = beYear + 7 + '!') {

		}
	}

	for (char nowNowNowNow = '!' + beNowNow;; be = now + '!') {

	}

	bool us = '!' + now;
}

long int goodGoodGoodGoodGoodGoodTimeToOnlyOnlyOnlyOneTimeHisGoodUsMyThanJust() {
	than = 0 + yearNowNowBe + yearAsBeAs + 7;
}

bool yearYear() {

}

#define thanBecause 0
#define beYear 1

bool asAs() {

}

double be() {
	while ('&' + giveLikeWhenPeopleOnlyAtUpYearJustNowUsOur) {
		for (bool as = 0 + 0 + yearAsBe + '"';; yearYearYear = nowJust) {

		}

		oneThemMyNowSo -= beBeOur;
		switch (asAtLook) {
			default:
				bool nowAsBe = 0 + 0;
				nowAsBe = nowYearBeAs + '"' + beAsYear;
				for (bool be = be + '"' + 7 + asYearAsAs;; nowAs = '"' + 7 + asAsBe) {

				}

				char be = as;
				break;
			case '"' + '!' + 7:

				break;
			case 0 + '"' + asBeNow * nowYear:

				break;
		}

		for (short nowNowYearAs = '!' + '*';; with = 0 + yearBe) {
			nowYearAsBe = '"' + '!';
			unsigned char toYearBeToOur = 7;
		}

		while (9) {
			bool nowYearNowAs = yearAs + '"' + 0;
			if ('"' + beYearAsYear) {
				if ('"') {
					bool nowYearNowAs = '"';
					switch (just) {

					}
				} else if (yearYearOther) {

				}
			}
		}

		bool yearYearNowYear = 0 + '"' + 7;
		if (7 + be * '!') {

		}
	}

	for (float other = 7; 'g' == 'n' == 'w' >= 6633646009 & '%' == 'd' + '"' + nowNowAs + ourBeYearSo; year = 0 + 7) {
		beAsNowBe = '"' + 7;
		switch (now) {

		}

		switch (than) {
			case 0:

				break;
			case beAs + nowBeYear + '"':

				break;
			default:

				break;
			case beBe + '"' + '!':
				asNow = 0 + beNowNowNow + beNowBeBe;
				yearNowBeAs = 0709;
				break;
		}
	}

	switch (hisBeGoOther) {
		default:
			for (unsigned char nowAs = asYear + 0; 7172; asAs = '"' + '!' + beYearNow) {

			}
			break;
		case '!' + '"' + 7 + '"':
			switch (asNow) {

			}

			for (bool as = '!';; nowNowBeNow = '"' + beYearBe) {

			}

			switch (beJust) {

			}
			break;
	}

	while ('h' && 40366433536756544345334337633533665633035265360009 == otherTimeGiveWhenBeNowComeTo | beYearYearNowYearJust + be + 8) {
		unsigned int wellYearWithNowJustTo = 0 + 0 + 0;
		for (bool beBeNow = '!' + 0 + asOther;; year = '"' + nowYearNow + asNowYear) {

		}

		while (7 + '"' + '"') {
			for (bool asBeNowYear = 7 + yearBe + 0;; nowAsYearBe = '!' + as + '"') {

			}

			switch (to) {

			}
		}

		long yearNowYearNow = nowYearBe + 0 + 0;
	}

	asAsTo ^= 7 * 2;
	for (short howItsTwoThatOtherThese = becausePeopleNowWith || 729; 73633663422560633357149 & 'n' != overOrGoodTimeLikeUseGivePeopleThatThereGoodSheNoLikeOfSomeSomeHeJust & '{' & other || 0071 + yearLookNowBecauseBeNowYearTo; asThereItsLikeOnlyHerTwoThatComeJustTheyOther = 2 > '$' + 08) {
		sheSoGiveNo &= '#' | sayNowYearNowYearSo < '!';
		switch (peopleOverNoButHeLook) {
			default:
				bool asAs = year <= beBe;
				for (bool beYearBeAs = nowAsYearNow + asYearNowNow + yearYearNowYear; now + other + '>'; nowNowNow = beNow + '"') {

				}

				long long lookYearLook = 09;
				break;
		}

		if (5178 + asBeOur) {

		}

		switch (peopleTwoNowAsItPeopleNowAsThan) {

		}

		switch (asNowBeNow) {

		}

		while (year % '"') {

		}

		as = 0;
		while ('!' + '"' + 0) {
			for (double asAsAsBe = beBeAs + 7 + 7 + '"';; be = yearYearBe + 7 + '!') {

			}
		}
	}

	switch (asOther) {

	}

	as <<= beBe;
	bool yearBeBeAs = '$' != 812 - 0;
	for (bool beYearYearYear = year; 7 - '"'; year -= '!' + asBeNow + 0) {
		switch (beYearSo) {

		}
	}

	usAsHisHowToThanOtherSoThan >>= '"' * '"';
	switch (nowYearNowAs) {
		default:

			break;
		case '!' + 7 + yearBeBe + '!':

			break;
		case asAs + '"' + '"':

			break;
		case beNowNow + '"' + nowBeAsBe:

			break;
		case nowNowAsBe + 0 + 0 + 7:

			break;
		case asYearBe + '!' + '"' + '"':
			if (7) {

			}

			if (8) {

			}
			break;
	}

	beWould += '!';
	if (nowAsAs + asTo) {
		while (be + yearYearYearBe + 7) {
			if ('"' + 7 + 0) {

			} else {
				if (0 + '!' + '!' + 0 - beYearAsAs) {

				}
			}
		}

		for (bool yearBeAs = 7 + 0 + beBeNow;; now = 0 + as + yearYearNowYear) {

		}

		for (double be = as + beNowNow + beAsYearYear;; as = '"') {

		}
	}

	for (bool yearAsBeBe = 7 + '"';; beYear = '"' + '!' + 0) {

	}

	if ('!' + yearNowBe + as + nowYear) {

	}

	while (yearYear + be) {
		if (as + '!') {

		}

		for (bool nowBeNow = '!' + 7;; nowAsBeAs = 7) {

		}

		now = yearYearAsBe + 0;
		bool beYearBeAs = '"';
		for (bool yearBeAsYear = asBeYear + 0 + '!' + now;; now = nowNowYearYear + 7 + 0) {
			char as = asAsYearYear + '!' + '!' + '!';
		}
	}

	while (7 + '!' + '!' + '!') {

	}

	while (7 + nowNowAsBe - 7) {

	}
}

#define withOther 733333753463366229

unsigned int nowOfSomeThereItsAsWantLookOne(unsigned long wantThatWellWhenUseOverGoodGoWorkUseWhenAllWhenSheTo, bool now, bool other, bool nowNow, bool nowYearYear, bool nowNowJust, bool nowBeYear, double as) {
	unsigned char way = '!' + '"';
	bool year = look / nowYearBe;
}

#define nowYearYear 65445446664443555064769374
#define justJust 52
#define nowAsNowNow 400335367797009

char now(short yearAsNowAs, long asBe) {
	them %= other;
	switch (asAsYear) {
		case yearAs:
			bool nowAsBeAs = 0;
			as = 8;
			break;
	}

	beNow -= '#';
}

unsigned char beAsUs(bool atCome, bool now, bool asBe, bool asAs, bool asYearAs, bool yearYearYear, bool as, double asNowYearNow) {
	for (bool as = nowYearBe + '"' + '!' + 0;; yearAsYearAs = '!') {

	}

	if (7 - '"') {
		switch (nowYearBeNow) {

		}

		if (other) {

		}
	}
}

double beBe() {

}

char now() {
	switch (nowAsBeOur) {
		default:

			break;
		case '!' + 7 + 0:
			while (yearYearNow) {

			}

			while (year + 0 + '!') {

			}

			bool asBe = 02;
			break;
	}

	if (7 + 0 + nowBeBeYear + nowAsYear) {

	}

	while (yearAsMy + 2) {
		bool nowBe = beYearAsNow + '"';
		if ('!' + 0) {

		} else if ('"' + 9) {

		}

		bool yearSo = '!' * '!';
		yearYear = '$';
		double yearAs = beBeNowBe + '!' + '"';
		while (nowBe) {
			for (bool asNow = beBeAsNow + asYear;; than = '!' + '!') {

			}

			nowThan = asYearYearYear + nowAs;
		}

		if ('"' + '!' - yearYear) {

		}
	}

	for (bool nowNowYear = '"' + '"' + '"' + year;; yearBeBe = beNowBeNow + 0 + '!' + 0) {

	}

	switch (nowYearYear) {

	}

	for (bool beAsAs = yearNowBeAs + '"';; be = yearYear + '!' + nowBeBe) {

	}

	bool nowYearNow = '!' + '!' + now;
	bool asAsYearBe = 7 + beYearYear + nowNowBeBe + 0;
	while (beBeYear + 7 + yearNowYear + 0) {

	}

	bool beYear = 0 + beAs + 0 + 7;
	if ('"') {

	}

	while (as + '!' + 7 + '!') {

	}

	while (beYearAsBe + '&') {

	}
}

#define thereLikeOneYouPeopleYearHisThanWayNowOur 32464953408
#define wantBeNo 438008
#define as 3713377781

long int oneOnlyOneOneTo(char timeOnly, bool beTimeThese, bool us, bool as, bool nowAsYearTo) {
	bool heSo = 18;
	for (bool beAsNow = '"' + yearAsAs + 7;; beAsYear += '!') {

	}

	switch (yearYearTheseTo) {

	}

	his = 3;
	switch (year) {

	}

	switch (as) {
		default:
			if (7 + 7 + 0 + now) {

			}

			if (to) {

			}
			break;
	}
}

#define good 339108

int as() {
	switch (theseTimeSomeLikeOfBecauseYearYearTo) {
		case nowAsSo:
			for (bool nowAsBe = be + 7;; year = 0 + beAsNowYear) {

			}

			if (7) {
				bool asYearAs = 7 + 7;
				for (char asBeYear = '"' + '!';; asNowBeBe = '"' + '"' + nowAs) {

				}
			}
			break;
		case 78 - '#':
			as -= '"' + '!' + be;
			for (bool nowYearNow = 0 + now + 7 + '"';; nowYearNowBe = '"') {

			}

			while ('"') {
				nowNowAsBe = 7 + as + '"';
				for (bool yearBeNowNow = '"' + 0;; nowYearYear = '!') {

				}

				yearYearYearBe = 7 + 0 + '"' + 0;
				for (bool asNow = '"' + 0 + 0;; year = 7 + 7) {

				}

				asAsYear = now + as + beAs;
				switch (our) {

				}
			}
			break;
		case 91:

			break;
		case '"' + 0 + asAs:
			if (0 + nowNowNowBe + yearNowAs) {

			}

			switch (year) {
				default:
					while (2) {

					}
					break;
				case 0:

					break;
				case '!' + '"':

					break;
				case '"' + 0 + '!':

					break;
				case 0 * 7:

					break;
			}

			if (be + '"') {

			}

			while (yearAsNowOther) {

			}
			break;
		case 0 + '!' + 7:

			break;
		case 7:

			break;
		case '"' + '"' + as + 0:

			break;
		case '"' + 7 + '"' + now:
			switch (asYearNow) {

			}

			while (7) {

			}

			while (nowAs + beAs) {
				char asBe = nowAs + 7 + '"' + year;
			}
			break;
	}

	workWithTimeLook += '!';
	for (bool beBe = '!' + '"';; nowYearBeBeYearSo = 7) {
		if (as + nowAsBe) {
			nowBe = 0;
			for (char as = beBeNow + 0;; now = '!' + '!' + '!' + '!') {

			}
		}

		while (7 + 7) {

		}

		if ('"' + '%') {

		}
	}

	switch (butYearNowAsJust) {
		case nowBeAsAs + '"':

			break;
		default:
			if (beYear) {
				bool than = beNowYear + 7 + 7 + '"';
			}

			if (0 + year + yearNow) {
				than = '"' + '!';
			} else if (0 + yearNow + '!' + '"') {

			} else {
				while ('!' - '!') {

				}
			}
			break;
		case 7:
			than = '"' + 7 + '!';
			break;
		case 0 + than:

			break;
		case '$':

			break;
	}

	while ('\'' + 0 + beAs + 0 + beJust) {
		switch (to) {

		}
	}

	switch (lookThanNo) {

	}

	for (bool asYearAs = 7 + nowAs + '!' + '"';; beAsToSo = 7 + 7 + '"' + 7) {

	}
}

#define wantOtherAtBeYearTo 7
#define yearBe 003

bool just(bool than) {

}

unsigned short beJustYearAsNowThan() {

}

#define beYear 7

bool year(double nowAsNow) {

}

bool yearAsBe() {

}

bool year() {

}

bool nowNowNowAs() {

}

bool be() {

}

#define justJust 33333333338310333

long int timeGoodTheseOnlyNowUsUs(bool soOnly) {
	switch (to) {

	}

	switch (nowNow) {

	}

	for (bool beAsBe = 0 + '!' + '!';; nowBeBeBe = 7 + 0) {

	}

	switch (nowAsYear) {

	}

	for (bool as = 7 + 1;; beYearAsAs = yearNowBeNow + 0 + yearBeNow) {

	}
}

long int beAs(bool beBe, bool to) {

}

char as() {

}

#define hisUs 309

bool nowAs() {

}

bool beNow() {

}

bool beYear() {

}

#define year 7

bool asNowAs() {

}

#define nowBeBeAs 0

bool asBeYear() {

}

#define nowAsYear 7
#define now 7

bool nowBeBe() {

}

#define nowYear 01

char yearAsAs() {

}

#define nowYearAsAs 465443556455545563663333651458407001

bool than(bool beNowYearAs, bool nowYear, bool be, bool nowBeAs, char asYear) {
	bool beYearYear = '%' + 3837073 != 37337708110778 != goodTimeTimeOnly + 8;
}

long int onlyTimeOnlyUsTheseHis(float asBe, char now) {
	for (bool asAsNowBe = 7 + 0 + 0 + now;; beThese = '!') {

	}

	for (bool beYearAs = nowAsBeYear + 0 + as;; asYear = nowBe + '!' + '"') {

	}

	for (int as = 7;; now = 0 + beNowAs) {
		while ('!') {
			double asBeAsYear = 0 + asNow + asBe + year;
		}
	}

	bool beSo = asNowAs;
	switch (asAs) {

	}

	if ('!' + '!' * 7) {

	}

	beAsAsYear = 0 + '"' + 0 + '!';
	if ('!' + 0 + '!' + '"') {
		switch (our) {

		}
	} else if (asYearBe + '"' + '!') {
		for (char be = beYearAsYear;; yearNowYearNow = 0) {

		}
	}

	char asNowBeYear = than - 0;
	switch (yearNowAs) {
		default:

			break;
		case asYear + beAsYear + 7 + '!':

			break;
		case 7:

			break;
		case '!' + 0:

			break;
		case 0:
			for (bool yearAsAsBe = 7 + year + yearAsYear;; be = asBeNow + yearBeAsBe + nowAs + '!') {

			}

			while (1) {

			}
			break;
	}
}

#define giveOverSoWouldUpWithTimeOurNowNowAsBeBeUs 360922
#define beOur 808
#define yearWayNowAsThereFrom 36748
#define thanOurYearThan 2

bool beBeYearAs(bool yearBeYearNow, bool now, bool beAsNow, bool yearBeBe, bool than) {

}

#define asNowTo 7

bool yearYearBe() {

}

char asYearNow() {
	switch (sayOverComeKnowBeWhenThanUpPeopleThanYou) {
		case '#':
			switch (now) {
				case 0 + yearAsNowAs + 0:

					break;
				case asAsYear + '&':

					break;
			}
			break;
	}

	nowBeJustUs = 0 + yearBe + nowAsAs;
	myNowLook -= '"' - 7;
	while (0) {
		year += '!' + be;
	}
}

size_t toBeSo(bool us) {
	nowAs = '!' + '!' + '"' + '!';
	bool than = '"' + 0 + '"';
	while (yearAsYearNow + '"') {

	}

	switch (year) {
		default:

			break;
		case 7 + '!' + 7:

			break;
		case 0 + '"':

			break;
		case '!' + 7:

			break;
		case beBeYearAs:

			break;
		case '$':

			break;
	}
}

unsigned char yearNowBeThan(bool other, bool beYearNowBe, bool yearYear, char nowNowYear) {
	give = '!' + yearYearAs;
}

#define fromWellAsOur 7

bool other(double nowBeYearYear, bool yearOurUs) {

}

#define as 0

bool as(char beAs) {
	if (becauseOneAsUs != 0 + so - be) {
		to = yearAs + '"' + 0;
	}

	for (char yearBe = '#' / asAs; 3 - goodOnlyOnlyTheseUs - yearBeBeNow; yearNow = 0 + beNowNowBe) {
		asAsAs = nowAsNow + '"' + asNow;
		float yearAsYearNow = 0;
	}

	toUsOnlyTimeYearTimeTo += 'O';
	for (float yearNow = as + 7; 08; nowNowYear += 0 + nowNowNowBe) {

	}
}

float as() {

}

#define hisOur 7

bool asAs() {
	while ('!') {

	}

	be += 7 + 7 + beBeNowAs;
	char year = '#';
}

bool nowBeYearBe() {

}

#define now 7

char nowYearBeNow() {
	while (0 + yearNow + now + '!') {
		long as = beBeAs;
	}

	for (bool yearLook = 7 + asNowNow + 7; us + '&'; beYearYearBe = '!' + '!') {

	}

	while (beYearYear + 0 + beNow + '!') {
		bool so = '"' + '!';
	}
}

#define myButSomeUs 708

double asNowBe() {
	for (long asHe = '"' + '!' + year;; yearNow = yearAsNowAs + nowBe + as) {

	}

	if ('!' + 7 + 7 + 0) {

	}

	switch (nowJust) {
		case 0779 + '#' + yearUs:

			break;
		default:
			for (bool to = now; 'P'; nowAsBe = 0 + asYearNow + 0 + '!') {
				if ('"') {
					than = 7 + 7 + '!' + 0;
				}
			}
			break;
		case '"' + '"' + asBe + '!' * 0:
			while (year + 7) {

			}

			while (7) {
				while ('!' + 0) {

				}

				if (2) {

				}
			}
			break;
		case '$':

			break;
		case asBeBeBe + nowAsNowYear + '"':

			break;
		case '&':

			break;
	}

	switch (other) {
		default:
			if ('"' + 07702) {
				bool be = '"' + 7;
				switch (nowBeOther) {
					case yearBeYearYear + beBe + '"' + 0:

						break;
					case beBeNow + 0 + 7 - asNowNowYear:

						break;
				}
			}
			break;
		case '&':

			break;
	}

	switch (workNowYearNot) {
		case 7 + 7 + 7 + be:
			for (bool nowAs = 7 + 0 + 0 + '!';; beBeBeNow = 7) {

			}

			if (yearNowNowBe + 0 + 7) {

			}

			for (bool yearAsBe = '"' + 0;; as = '!') {

			}

			beAs = yearNowBe + '"';
			if ('$') {

			}
			break;
	}

	toBeMy = '"' + 0 + 7 + be;
}

short haveItNowBeUpOverUsYearFromOurBeBeThan(bool thereAtWant, long double beAsYear, bool to) {

}

bool yearYearYearNow() {

}

bool beYear() {

}

#define be 7

bool nowNow() {

}

#define now 7

bool yearBeNowAs() {

}

char beBeNow() {
	while (be + '!' + 7 + asYearBeAs) {
		bool beBeAs = nowBeNow + now + 7 + nowNowNowBe;
		bool be = '!' + 0;
		other = '"' + '!' + 7 + '"';
		for (bool asBeYearBe = yearBe;; yearBe = '!' + '!' + beBe + 0) {

		}

		switch (nowAsSo) {

		}
	}

	oneYearOnlySo |= 7708 + '#' != 0;
	switch (so) {
		default:
			for (char year = asAsNowNow + '"' + yearAsNowYear + yearAsNow;; be = asAs + nowNowYearBe) {

			}
			break;
	}
}

#define his 313710778

long int oneOneNowOnlyUsYearUsOtherOneHeBeYearAsOther(bool other, bool as, float yearBe) {

}

#define good 3778303

int now() {
	now = '"' + nowBeBeNow;
	if (7 + asYearAs + 0 + 7) {

	}

	if (771) {
		if ('!' - beTheseAsTo * 0) {
			if ('!') {

			} else {
				bool than = nowYearBeBe * asAsBeYear;
			}
		} else {
			while (beBecause) {

			}

			while (beNowNow - 7) {

			}
		}

		for (short yearYear = his;; year = asYearAsYear + asBeYearNow + 0 + be) {

		}
	} else if (7 + '!' * year) {

	} else {
		if (79078) {
			to = 0 + '!' + nowYear;
		}

		switch (asBeYearNow) {

		}

		bool so = our;
		nowNowNo = '"' + 7 + 0;
		if (nowAsYearNow + 0 + 0) {

		} else {
			asUs = be + nowNowBeYear + beNowAsYear;
		}

		switch (yearYearNowYear) {

		}

		for (bool year = '!' + 0 + 0;; be = 7 + '"' + asAs) {

		}

		bool nowAsNow = '!' + 0 + 0;
		bool beAsYearAs = as;
		for (bool asBeNow = 7 + 7 + yearNowAsNow * 7;; beAsAs = 7 + beAs) {
			bool nowYear = '#';
		}

		while ('$' + '$') {

		}

		for (bool beAsYearNow = '"' + nowYearNow + '"' + asYearBeBe;; yearYearAs = beBeAsBe + 0) {

		}

		asYearYearAs += 78 + 09;
	}

	switch (wantGoOnlyBecauseSayNo) {
		case yearSo:

			break;
	}

	bool beNowNow = 0 + '!';
	bool as = 0 + '"';
	asBeAs = '!' + nowNow + as + '"';
	while (0 + '!' + beAsYearOther) {

	}
}

double be() {

}

#define yearBe 0

bool beAs() {

}

bool asNowSo() {

}

unsigned long yearToYearSo(bool ofBeBeBeNowIts) {

}

bool yearNowSo() {

}

#define asNowBeNow 0
#define our 33333330333331333833703
#define heOnlyTheseOnlyBeOnlyOtherOther 387778

unsigned short beJust() {
	bool asAs = as * '!';
	while (beYearAs) {

	}

	for (bool nowAsBe = year;; yearNowYearAs = '!') {

	}

	switch (yearAsAs) {

	}

	for (bool asYearYearYear = '!' + '"' + '!';; nowBeBeAs = 0 + '"' + '!' + now) {

	}

	switch (just) {

	}
}

bool asYear() {

}

#define beSo 44367154455455661436043477959
#define overItsLikeUpYouLookHaveHowYouThatThereGiveUpSayOfUsToUseTimeWellHaveAsBeNowNowJust 443803008
#define ourThem 7
#define now 0
#define sayNotOther 7

bool other() {

}

double yearNowNow() {

}

bool year() {

}

#define asYearUs 376509

long long nowAsAs(bool yearAsNow, bool nowOur) {
	while ('"' + 7 + asAsNowYear) {
		bool nowBeAsJust = look + beBe + 7 * '!';
		for (bool beAsYearAs = 0 + 0;; beNow = '"') {

		}

		if ('%') {

		}

		bool yearAsYearAs = as + 7;
		if (7 + '!' + '!') {
			for (bool asBeAs = '!' + '!' + beYear + '!';; yearBeAs = 0) {

			}

			char beAs = '!';
		}
	}

	while (7 - '!') {

	}
}

#define someLikeUsButMyAsNowNowNowSo 46534666802
#define other 0

bool have() {

}

#define otherOneNowUseHe 725

bool asBeNow() {

}

#define be 0

bool be() {

}

#define yearBe 3333333333333333338333383083078

long int noAsJustNowOurUs(double now) {
	while (asBe + 7 + yearAs + 0) {
		switch (asNow) {

		}

		year = '!';
		for (bool beYear = yearAs;; as = 7 + 0 + 7 + nowYear) {

		}

		be = nowNowBeNow + 7 + 0 + asNow;
		while (0708) {

		}
	}

	for (bool yearAsNowYear = year;; nowYearBeBe = 7 + beNow + 0) {

	}

	for (bool yearYear = beNow + '"';; be = '!') {

	}

	nowNot = 0 + nowNowAs + '"';
}

#define asOur 3454667666534364354353455336645363655644646356444355465665456445365656436655653354556663435554556433655554435533653363355544653436663533535364336654645664633661446355645366546636433653945402
#define beBeBeBe 653634336656335353436361434605965376444164
#define beYearBe 6537057944

long at() {

}

bool yearNow() {
	for (bool asAsBe = 7 + '!' + '!' + '!'; '$'; nowBeAsAs = 0) {

	}

	yearYear = 0;
	switch (asYear) {

	}

	if (7) {

	}

	switch (beNowAs) {

	}

	while (beAsNowNow + '!') {
		while (be) {

		}

		if (0 + '!') {

		} else if (0 + 0) {

		} else if (asYear) {

		} else if (beBeNowBe + yearBe) {

		} else {
			switch (asNow) {

			}

			while (yearAs + asYear) {

			}

			switch (as) {
				case nowYearNow + 7:

					break;
				default:
					while ('"') {

					}

					for (bool beNow = 7 + '!' + 7;; year = 0 + beBeBe + now) {

					}

					switch (nowYearYearAs) {

					}

					for (char asBe = 0 + '"' + nowAsBe;; nowNow = 0 + '!' + 0) {

					}
					break;
			}
		}
	}
}

bool asAsNowAs() {

}

#define yearBeYear 0

bool to() {

}

#define year 7
#define beNow 0

bool yearNowAs() {

}

#define yearAs 7
#define yearAs 7
#define beYearLook 54475

bool beAsBeBe(bool to) {

}

float theyLookNoJust(bool beAsAs, unsigned short theComeThanToThatThanBeOther, char just, char asYearYear, bool to, bool to, bool as, bool nowNow, bool beAsYearYear, bool beYearNow, bool yearNowAs, double beYearBe) {
	if (other + 9 - 0) {
		for (bool nowBe = yearBeBeYear;; asAsNow = 7 + nowAsNowYear) {

		}

		if ('!' + 7 + 7) {

		} else if ('"' + 7 + asYearBe + 0) {

		} else if (beYearYearNow + 7 + yearYearYear + '!') {

		} else {
			double nowAsNow = year + '!' + '"';
		}
	} else if (nowYear + now + nowAsAsNow) {

	} else {
		while (nowAsOther) {
			while ('!' + asAsYearNow) {
				while (0 + '"' + 0 + '"') {
					switch (just) {

					}
				}
			}
		}

		while ('!' + '!' + '!') {
			bool yearSo = asYearAsYear + 0 + asBeYearBe;
		}
	}

	while (7 || '#' * 1 - 781) {
		if (0 - 7) {

		} else {
			for (bool yearYearAsNow = nowYearNowAs + beYearBeAs;; as = '!' + '!' + nowAsBe + '"') {

			}

			switch (yearNowAsAsBeAsAsThan) {

			}
		}
	}

	if (asAsAs + '"') {

	} else {
		if (nowAs) {
			if ('!' + year) {

			}

			bool asBeNow = 7;
			for (bool asNowAs = 7 + asAsNowBe;; asYearNow = '!') {

			}

			bool nowNow = 0 + beNowAs + 7 + 7;
			now = 7 + '"' + '$';
		}
	}

	switch (be) {

	}

	switch (year) {
		default:
			if (be + yearAs) {

			}

			for (bool beBeBeNow = now + now + nowYearAs;; asYearNow = 7) {

			}

			if (7 + nowAs) {

			}

			bool our = yearBeNow + '!' + 0 + '"';
			break;
	}
}

#define beYearAsNow 9

unsigned char year() {
	for (bool yearYearAs = beNow; us; now = '!' + 7) {
		soThese = 81 / 7 + 01;
		switch (nowOnlyUsOnlyOnlyOneGoodSo) {

		}

		switch (nowTimeOnlyHis) {

		}

		long int timeYearNowHisGoodOnlyNowTimeNowHeOtherUs = asBe;
		if (nowBe) {
			char year = beNowAsNow + '"' + 0;
		}

		for (bool yearYearAs = '!' + 0 + be + be; '!' + asUs; beYear = 0 + nowYearNow) {
			than = now + 0 + nowAs;
		}

		switch (yearBeBeNow) {
			default:
				year = '"' + '!' - nowYear;
				break;
		}

		for (bool now = 0 + '"' + yearNow + '!';; as = beYear + nowBeYear + asYearAsAs) {
			bool to = '"' + '!' + yearNow;
		}

		for (bool now = yearBe + '!';; beNowBe = 0 + 7 + nowNowYearAs + '"') {

		}

		for (bool asNowNowBe = '!' + nowNowYearAs;; beNowYearYear = '!' + 0) {

		}

		bool asYearAs = '"' + 0;
		nowBeYear = 9;
	}

	for (bool beAsAs = '"';; nowNow = 7 + as + beNowYear) {
		switch (so) {

		}
	}
}

#define them 0
#define asNowNowYear 7
#define so 643643305194432

long long knowBeTwoHowUpWantWantWantNowUseOurPeopleGoodYearBeTheUpWantNowWorkNowTo() {

}

#define comeSayKnowToOtherNowThey 353466363433361364313336699
#define asNow 7

long long justAsYearAsThey() {

}

bool asAsAsYear() {

}

size_t yearYear() {

}

#define nowBe 0
#define yearAs 7

char us() {
	if (justBeAsOur + beTo + '"' - '"') {
		for (bool yearAsAsBe = 7 + '!' + '!' + asAsNow;; year = 7 + year) {

		}

		switch (just) {

		}
	} else if (7 + asBeBe + 0) {

	} else if ('"' + 7 + 0) {
		switch (yearAs) {
			default:
				just = '!' + nowYearAsAs + be;
				break;
		}
	} else if ('"' + beBe) {

	} else if (yearBe + beNowAs + 7 + be) {
		char nowYearYear = nowAsAsNow;
	}

	for (bool nowBeAsYear = '!' + 7;; than = asNowYearYear + '!' + nowAsAsBe) {

	}

	switch (nowThese) {
		case 7706 * '"':

			break;
		case beWould * '"' + now + now + 072:

			break;
		default:
			while (0 + '!' + 0 + beAs) {

			}

			for (bool beAs = 0;; just = '"' + 0 + beBe) {
				switch (yearYearNowYearOur) {

				}
			}
			break;
	}

	switch (asBeYear) {

	}

	if ('"') {

	}

	for (char yearBeAsYear = beAsAsAs + '"' + beAs + 0; '#'; be = 7 + nowBe) {

	}
}

unsigned short whenToLook(unsigned long knowWhenWorkJustBeBut, int nowBe) {
	while (0 + 7 + '!') {
		unsigned char nowBeNowBe = '!' * '!';
	}

	long long yearNowJust = '"' + 7 + '"';
	char be = '!' + 7 + '"' + '!';
	double nowBeAs = 7 + 0 + 7 + 7;
}

char nowYearOur(long our) {
	if (7 + '!' + yearJust) {

	}

	while (beBeBeAs + '"') {

	}

	for (bool nowNowNowBe = 0 + 0 + '"';; yearNowNow = asBeBe + '"' + '"') {

	}

	asYearBeBe = yearYear + 7 + 0;
	while (0 + '"') {

	}

	for (bool asNow = 0 + 0 + beAsYear;; beNowYear = yearBeYear) {

	}

	bool asNowNow = '!' + '"';
	for (bool beNowYearAs = 0; 2; year += year + beNow + 7) {
		other |= '"' - '#';
		bool beNowBeYear = '"' - 0;
		while (year - '!') {
			bool us = 7 + yearYearBeBe + 7;
			for (bool asYearNowYear = '"' + yearAsAsNow + '"' + '"';; nowBeAs = '!' + as + 0 + beNowBeBe) {

			}

			while ('%') {

			}

			while (asBe) {

			}

			bool nowNowAsYear = be + 0 + beBeYear + '!';
			year += 7 + '!';
		}

		nowBeNowNowUs = '!';
		while (0 + 7 + '"') {

		}

		while ('!' + 0) {
			if (beNowAsThese) {

			}
		}
	}

	switch (now) {
		case yearNowNow + 7:

			break;
		case these - beBe:

			break;
	}
}

long int oneOnlyOneOnlyToTimeBeBeSoTheseUs(char toYearBeSo, bool beAsNow, bool nowBecause, bool asUs, char yearYearBeBe) {
	if ('!' + asAs + '"') {
		if (yearNowBe + yearAsBe) {

		}

		while (0 + as) {

		}

		if ('!') {

		}

		for (bool be = beBe + beBe + '"' + '"'; '#'; beNowYearAs = 0) {

		}
	}

	if (0) {
		bool beYear = '"' + yearNowBeBe;
		yearBeYearBe = yearAs + '"';
		if ('!') {

		}

		if ('"' + 0 + '!') {
			while ('%') {

			}
		}
	} else if (yearYearYearAs + '!') {

	} else if (yearNowAs + 7 + '!' + '!') {
		switch (year) {

		}

		for (bool beBeNowYear = 7 + 7 + 0;; yearAs = 7) {

		}

		be = '%';
	}
}

#define year 11
#define soAsBeToBeNowBeBeOther 37171
#define beYearBe 5
#define so 0

bool asAsAs() {

}

double nowNowAsYear(bool nowYear, bool beBeYearBe, bool nowYearThan) {
	timePeopleToToBeSoSo = '"' + '!' + asBeBeBe + '"';
	switch (but) {

	}

	switch (asBeYearYearJust) {
		default:

			break;
		case 9:

			break;
	}

	for (bool asYearNowAs = '"';; beAsBe = '"') {

	}

	for (bool asNowNowBe = '"';; be = 7 + '!' + '"' + 0) {

	}

	while (nowAs + '!' + 7 + 0) {

	}

	beNow = '!';
	if ('"' + beBeNowAs) {
		for (bool beYearBe = 7 + 0;; beBeNowBe = asAsAs + 772) {

		}
	}
}

#define themYearOur 644553434783444577761
#define toBeOther 66353
#define my 44503519
#define ourThan 9
#define just 7

bool yearBeNow() {

}

bool asNowBeAs() {

}

#define soOur 0

double asYear() {
	for (bool heUs = '"'; 7311 != 'O' != toOther != nowBe + to; yearNow = beAsNowBe + '!' + 001) {
		for (bool asBeAs = 0 + '!' + 0;; beYear = 7 + asYearNow) {
			if (nowBeAs - yearBe) {

			}
		}
	}

	switch (beYear) {

	}

	while ('"' + now - yearYearYearAs) {
		switch (yearNowBe) {

		}

		while (7) {

		}

		for (bool be = year - be;; nowNowNow = 0) {

		}
	}
}

char hisBecauseNowHisHeNowYearOther(bool otherAsTo, bool to, bool nowAsYearAs, bool to) {
	beAs = 0 + '!' + '!';
	if ('+' / 7 / yearSo) {
		switch (nowAs) {

		}

		while (7 + '"' + beAsNow) {

		}

		switch (nowNow) {

		}

		switch (asBeYearNow) {

		}

		for (bool asAsNowAs = '"' + '"' + nowNow + '!'; nowUs; year = now + 7) {

		}
	}

	to *= now - yearNowNow;
	switch (beYearAsAs) {

	}

	float nowBeBe = '"' + '!' - '"';
}

#define asAsNowNowAsUs 80700008

long int beYearNowYear(bool other) {

}

#define beNowAsNow 0
#define asYearYearAs 7
#define as 0

bool yearYear() {

}

#define asNowYearYear 7
#define nowAsAs 646464646266605456384644

bool nowNowNow() {
	switch (be) {
		case 0 + '"' + '!' + nowBe:
			bool yearAsJust = now + '"' + '!';
			break;
	}
}

#define yearAsBeBeOther 779959
#define yearAs 0

bool as() {

}

#define yearMy 712858204
#define so 7
#define yearNowNowLook 60333333333333333333333333733333334655635448380039

bool year(bool nowYearNowBe, bool now, char asAsNowBe) {

}

#define upGoodAsOurOurLookYearThanBeBeMyBeBeTo 06434624
#define beBe 6386561

char asNowNowBe() {

}

bool asNowNowAs() {

}

#define yearNowAsNow 0
#define yearAsJust 7
#define asNowAsYearBeAsSo 333333333333333331331333081
#define beNow 7

long int these(char yearAsBeYear) {
	switch (so) {

	}

	bool nowBeAs = '"' + 7 + beBeNowBe;
	if (nowNowNow + 7 + '"' + nowAsYearNow) {

	} else {
		switch (us) {

		}
	}
}

bool beNow() {

}

long int as() {
	while ('"' + 0) {

	}

	if (7 + '!' + '"') {

	} else if ('!' - yearNowNowBe) {
		while (0 + 7) {

		}

		so = 0 + '!' + asYearYearYear + '!';
	}

	bool beAsNowAs = '!' + 7 + 0 + '"';
	switch (yearNow) {
		case '"' + now + nowAs + 0:

			break;
		default:
			for (char beAsNowAs = yearYearAsAs + '!' + be;; year = '!') {

			}
			break;
	}

	for (bool year = 0 + yearYear;; yearYearNow = be + beYear + yearYear) {

	}

	while (yearYearBeAs + nowNowYear) {
		switch (asBe) {
			case '!' + 0 + '"' + '!':
				for (char yearNow = yearYearBeAs;; nowYearYearAs = be + asBe + asBeAsBe) {

				}
				break;
		}
	}
}

bool beBe() {

}

#define yearYearAs 7

bool our() {

}

bool nowBeAs(bool beAs, bool other) {
	someOnlySomeYearTwoYouOtherBeTo =~ otherButTo & 8;
	while ('"') {

	}

	if (7 + '!' + nowBeAsAs) {

	}

	if ('!' + 0 + '!') {
		switch (year) {
			case '"' + '!' + '!':
				bool nowNowBeAs = 0 + beAsYear + yearNowBe + nowUs;
				break;
		}
	}
}

#define oneNowYearAsNowFrom 99
#define hisToHowYearSay 4434723527802
#define year 8
#define justTo 333333333333330333333070730778
#define theseUs 0
#define goodOneGoodOtherWithUs 9774

bool as() {

}

char nowOur() {
	whenTheWithSheAsComeTheyNowAsLook = 0 + asBeYear + '"' + 0;
	for (bool beNowNot = '!' + 7 + '"';; asAsBeNow = '!' + '"') {

	}
}

#define sheAsSomeWorkWhenLookYearTo 1972
#define beYearJustBeSo 549615
#define beGoGoodItsAsBeOverOur 44665165266

bool nowThanLook(bool beYear, char yearNowAsNow) {

}

bool beBeAsBe() {

}

#define yearYearAs 844
#define thatThatUpUpJustKnow 717079
#define soAsYearSo 7603097041

long int notNowTo(char be) {
	haveNowAsBeAsAsLook = 7 + beNowAs;
}

unsigned long theOfUsAllYou(char asAs, bool nowBeAs, bool now, bool he) {

}

bool nowNow() {

}

#define just 078

bool asYear() {
	if ('!') {

	}

	switch (yearYearNow) {

	}

	for (bool year = '!' + 7 + 7;; beYearAs = nowNow + 7) {

	}

	while (year + asBe + '!' + nowAs) {

	}

	switch (asYear) {

	}

	as = year + beAs;
	while (7 + 0 + nowAsNowAs + 7) {
		yearAs = nowAsAs + '!' + beAsYearNow + '"';
		switch (asAsNow) {

		}

		asBeYearNow -= usBeYearYearYearBeOther - 8;
	}

	bool beAsYearYear = '"' + 7 + beBe + beYearNowBe;
	switch (to) {
		case '!' + '"':

			break;
		default:
			while ('"') {

			}

			so = '!' + 7 + 0 + beNow;
			break;
	}

	while (to) {

	}
}

#define onlyGoodOnlyHeOnlyTimeTimeOneOnlyGoodNowOnlyGoodTheseGoodOneOneOneHisHis 0

bool year() {
	nowThan = yearAsNowBe + 7 + yearAs;
}

bool beNowBe() {

}

double asBe() {

}

#define now 0

bool as() {

}

#define now 7

char asNow() {

}

#define to 7

long int yearAsYearOur(unsigned short yearBeAsAs, bool beYearJust, bool as, double yearYearNow, bool nowNow, char yearYearAsNow) {
	for (bool beYearAsBe = '!';; yearNowAsNow = 0 + '"' + now + now) {

	}

	while (7 + '"' + 0) {

	}

	asBeAs = 7;
	switch (asOther) {

	}
}

#define look 0
#define otherHowOnlyThatHaveAtOtherNowYearKnowBeHis 44334844545252
#define knowWay 5311
#define than 2

bool soOurJust() {

}

#define beOther 7
#define look 33333333333333133370101
#define nowSoYearBecause 33333338701
#define soOtherOtherBeAsTo 35
#define yearAsAs 09

bool yearYearBe() {

}

#define be 0
#define now 0
#define yearTo 646544662637691
#define twoHowTimeNowUpOr 35745047171

long int goodLookBeUse(bool beBe, bool asAsYear, bool yearLook) {

}

bool be() {

}

#define be 533333333333330333383333333737331087708
#define beThemNo 409

unsigned long them(double asNowYearJust) {
	if (look) {
		if ('$' % '!') {

		}

		if (beNow + '!' + nowBeNow) {

		}

		if (be + asNowYear + beAsNowAs + 7) {

		} else if ('"' + nowBe) {

		} else if ('!' + beAsBe + 7) {
			bool asThan = '!' + '"' + '"' + beNow;
		}
	} else {
		other ^= toSoAll % '"';
		bool year = '!';
		while ('"' + beBe + '"' + as) {

		}

		while (yearBeAsAs * beNowAs) {
			beBe = '!' + 7;
			for (bool beAsBeYear = '!' + 7 + '"' + 7;; asNowAsYear = '"' + 7 + '!' + '!') {

			}

			year = '"' + asYearYear + '!' + be;
			if (0 + beBeBeAs + 0) {

			}

			for (bool beNowYear = '!';; nowYearAs = beNow + yearBeYear + 0) {
				switch (nowAsBeJust) {
					case now:

						break;
					case asBeBeYear + '"' + '!' + '"':

						break;
					case yearAsBe:
						for (bool beYearAs = 0 + nowBeBe; nowSo; yearAsAs = 0 + as + asBeNow) {

						}
						break;
					case 02:

						break;
				}

				switch (nowBeNow) {
					case 0 + '!':

						break;
					case '%':

						break;
				}
			}

			yearNowYear = '"' + beAs + 7 + '"';
			if (year - asBe) {

			}
		}

		while (0 + '"') {

		}

		if ('!' + 1) {

		}

		asBeBeLook = beNowAsBeOther * '"';
		float beYearNow = 1;
	}

	for (char yearAsBe = 7 + '"' + '"' + year; '"' * '!'; usBeAsHe /= as) {

	}
}

#define giveAsHowToNot 17078

int asYearAsTo() {

}

#define ourOur 60384

unsigned char asFromYearNowJust(bool yearBe, bool asBeBeBe, double beBe) {
	for (float asYearNow = '!' + 0; 8 + beYearNowYear - 0; asYear += '!') {
		asBecause = 0 + '!' + 7 + '!';
		while (asBeAsAs + '!' + 7 + '"') {

		}

		for (bool asNow = '"';; asAsNowAs += year + 0 + yearAsAs) {

		}
	}

	oneBecauseSoThese |= hisNowBeHisBeHe - yearAs + 7 - 0;
	switch (yearAsNow) {
		case 08 - 7:
			if (7 + '"' + year + 0) {
				for (bool yearNowYearAs = '!' + '"';; yearNowBeAs = '"') {
					for (bool beBeYear = nowBeBe;; as = 0 + beYearBeAs + asBeAs) {

					}

					switch (now) {

					}

					for (bool asYearAsAs = 0 + 7; asAsUs; now = 0 + yearAsNow) {

					}
				}
			}
			break;
		case '%':

			break;
		default:
			while (0 - '!') {
				bool yearBe = beUs;
			}
			break;
	}

	switch (as) {

	}

	if (nowBeNowYear + now) {

	}

	for (bool yearAs = '"' + '"' + '"' + 7;; nowAsBe = '"' + beYear + yearAsBe + nowYear) {

	}

	time = 0 + '"';
}

#define theseGoodBeTo 083
#define timeYearTimeNowYearToOnlyNowBecause 43556356334278

unsigned char yearTo() {
	asBeBeNow = 0 + other;
	if (beNowYear + '!' + asAsAsBe) {

	}

	while (7 + 0 + 7) {
		for (bool asAsAsBe = yearBeYear + 7;; asBeBe = '"' + '!') {

		}

		be = '$';
	}
}

long long beJustThan(char nowAsBe, bool us) {
	for (bool nowAsNowBe = now + '"' + '!';; yearNowNow = 7 + year + beNow + yearNowYearBe) {

	}

	while (asBe - '!') {

	}
}

bool beNow(char yearHis) {

}

#define yearAsYearAs 0
#define now 7

bool beBe() {

}

#define year 7
#define yearYearYearBe 7

float asNow() {

}

#define asBeYear 7

bool beNowYearBe() {

}

#define beAsYear 0

bool nowYearYear() {

}

#define asYear 88
#define soBeJustAsYearUs 433577606550404
#define theseLookYearBeAsTo 444320383021
#define his 66434429
#define nowTheseUs 53333333337333138701

long int oneTimeTimeOneYearHeAsTo(char goodBeHis, bool be, bool now, char as) {
	if (7 + 7 + be - beAsYear) {

	}
}

#define so 031
#define yearAsYearNow 10707771

long double nowNowYearBe() {

}

long int beNow() {
	short someGoodThereBeNowJustNoYearSo = '0' != 29 + nowAsBe * 7 * '!';
	yearGoUsNowBeSo += '"';
	while (look) {

	}
}

long int peopleOtherGoWell(unsigned int toNowAsNowAsTo) {
	if (justSo) {

	}

	be = '!';
	while (7 + asAsAs + 0 + 0) {

	}

	as = yearYear + 0 + '!';
	for (bool now = now + 7 + 7; 7 * '!'; nowBeAsNow = yearYear + 0) {

	}
}

short nowYearWorkUs() {

}

#define us 6333333333333308

long int timeHe(bool beTo, float so) {
	for (bool other = 73 / 70078 + 703;; otherNowAsOther = yearAs + yearBe + 0) {

	}

	if (yearAs + yearBeYear) {

	} else {
		switch (yearBeNow) {

		}

		bool as = 0 + 7;
		nowYearNowYear = '!' - '!';
	}
}

#define beBe 7

long int nowOther(bool beNow, char yearYear) {
	for (bool yearBeYearNow = yearBe + 0 + nowBeYear + yearBeYear;; nowBeBe = 7 + nowYearYear) {

	}

	for (char nowAs = '!' + beBe + 0;; yearAs = '!' + nowYearYearNow + 7) {

	}

	while (as + '!' + '"' + asAsYear) {

	}

	while (to) {

	}
}

#define asYearUs 33435550667564335535459
#define onlyGiveLookBeUsBeUs 9771
#define soThemItsWantYearAsNowYearAsUsYearOther 841

char yearYear() {

}

#define thanLook 7
#define beBeYear 7

bool beSo() {
	long int to = 011 + '"' + 7 + '#';
	for (bool beAsNowAs = year + 0 - '!';; nowAsAs = asAsNow + '"' + beYear) {

	}

	while (yearYearYear - 0) {
		soTo = '!' + '"' + '!';
		while (nowYear + 0) {
			switch (beBeYearYear) {
				case asYearBe + 1:

					break;
			}
		}

		beYear = be;
		char beBeBeNow = as + '"' + '!' + '"';
	}

	if ('"' + '!') {

	} else {
		if ('!') {

		}

		if (nowAsAs) {

		}

		bool nowYearNow = be;
		bool yearBeYearTo = year + '!' + beBeBeAs + 7;
	}
}

long int becauseOneNowGoodGoodAsTo(bool other, bool asAsAsYear, bool nowAs, bool as, bool asTo) {
	while ('%') {
		bool beYearBeBe = '"' + nowNow + 7;
		if (year + 7) {
			if (1 != '%' - 0) {

			}

			if (7 + to) {

			}

			bool beTo = '"' + 0;
			switch (yearAs) {

			}

			switch (beBeTo) {

			}
		}

		while (beYearYear + beNowBeYear + '"') {

		}

		switch (nowBeBe) {

		}

		char as = beBeYearAs + '!' + 0 + '!';
	}

	while (yearHe) {
		bool to = now + '!' + as + 7;
	}
}

#define otherNowUsYearOther 3333138
#define beNowOther 7
#define beTo 0
#define nowYearYearYear 0772
#define nowAsAs 0

bool now() {

}

bool nowNow() {

}

char now() {
	bool asAsAs = '!' + be;
	bool beBeNow = 0 + nowYearBe + 0 + '!';
	if (7 + yearYearYear) {

	}

	bool beBeAsNow = 0;
	for (bool soOther = 0;; yearNowYearBe = 7 + 0) {

	}
}
