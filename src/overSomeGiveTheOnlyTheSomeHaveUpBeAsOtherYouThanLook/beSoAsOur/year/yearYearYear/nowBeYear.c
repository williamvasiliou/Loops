#define timeTimeOnlyBecauseTimeBeSoUsUs 33333333333333303133333
#define so 001
#define asBe 7
#define with 6644344931

short toSayHowSheWayLikeNowThanTheItWay() {
	year = '!';
	if ('"') {

	} else {
		switch (asYearBeTo) {

		}
	}

	for (bool now = '"'; 10779 - 7; asYearBe = nowBe + '!' + 0 + nowBeAsNow) {

	}

	for (bool yearAsNowNow = asYear;; asYear = 7 + nowYearYear + 7 + asYear) {

	}

	if (nowNowNowAs + 0 + '"') {

	} else if (0 + 7 + 7 + 0) {

	} else {
		while (0 + '!' + '"' + 0) {

		}

		bool asAsUs = nowBeAs + yearYearBeNow + year;
	}
}

#define notTheseTo 9
#define as 4601
#define beSoThan 7

bool than(bool yearBe, bool yearBeAsYear, bool us) {
	while ('#' - 0 - 78) {
		bool yearBeYearYear = 0;
		switch (so) {
			case '!' + so:

				break;
		}

		timeTimeTime = nowNowAs + 0 + nowYearAs + '"';
		if (18 - yearYear - asNowNow) {
			beAsBeAs = 7;
			while (yearYearAs + as + now) {
				for (bool be = '#';; be = asYear + '"' + 7) {

				}
			}
		} else if (beAsAsBe + 7) {
			switch (yearYearNowNow) {

			}

			to = as + year + '!';
			for (bool be = asBe + beBe + 7;; beYear = beYearYearAs + nowNowAs + 7 + beBeBeYear) {

			}

			while ('!' + yearYearNow + '"' + '"') {

			}

			while (0 + 7 + '"') {
				for (bool asNow = '#';; asNowNowYear = now + beYearNowBe) {

				}
			}
		}

		bool as = '"';
		nowYearYearNow = yearNowAsAs + 7;
		if ('!' + '"' + 7 + 7) {

		}

		for (char beAsNow = '!' + 7 + 7;; nowYearAsNow = 7) {

		}
	}

	while (asBecause) {
		if ('!' - '#') {

		}

		if ('!' + now + asNow) {

		}

		switch (beYear) {
			case now + '"' + '"' + '!':

				break;
			default:

				break;
			case 70001:

				break;
		}
	}

	long int yearYearAs = '!' + now + yearBeYearYear;
	for (bool asYear = '"' + nowYearAsYear; 7 / asYearBe - asBe + other; one = 7 + 0) {

	}

	while ('"') {

	}

	switch (beBeYearNow) {
		default:
			while (nowAs + 7 + '"') {

			}

			bool nowYear = '!' + '!' + '"';
			asBeTo = 0 + '!' + '!';
			while (0 + yearNow + '!') {

			}

			for (bool asAsYear = yearAsAs + beNow; '%'; asBe = '"' + 7) {

			}
			break;
	}

	if ('%') {
		switch (us) {

		}
	}

	while ('!') {
		switch (now) {

		}

		asYearBeNow = '#';
	}
}

float be() {
	switch (year) {

	}

	if (asAsYear + beNow + '"' + '!') {

	} else {
		switch (beAsBe) {

		}

		for (bool be = 7 + '!';; nowAsYearYear = 7) {

		}

		asYearBeAs = '!' + yearBe - '"';
	}
}

#define timeBeYearUsHisUs 333

char nowAsYearAsUs() {

}

bool now() {

}

long long nowYearYear() {

}

#define asAs 0

bool asNowYearBe() {

}

#define now 7

bool asAsTo() {
	switch (nowBe) {

	}

	switch (beOtherUseBeNowAsOur) {
		case '"' + asNowYear + '$':

			break;
		case '#':

			break;
	}

	if ('"' + '"' + '!') {

	} else if ('"' + asNowAs) {

	} else {
		switch (yearBeYear) {

		}

		if ('!' + yearNow + 7 + 0) {

		}

		if (yearAs + '"' + 7) {

		}

		for (bool beBeBe = beBeAsBe + '"' + '"';; year = '!') {

		}

		char year = 7 + 0 + 7;
	}
}

#define youHowWellKnowLikeUseWithJustOnlyLookGoTheyAtUs 90609
#define usTwoOur 7

short yearAs(bool asYear, bool yearLook) {

}

long double other(bool asNowBe, bool yearAs, bool our) {
	while (beNow + '!' + '!') {

	}

	while (as * '!') {
		bool asAsAsYear = 0 + beAsYearNow + '!';
		now = '"';
		be = '"' + yearAsNowNow;
		switch (asBeAsBe) {
			default:
				asAsBeBe = year + '"' + '"' + 0;
				bool as = year + '"';
				if ('"' + as + be - '"') {

				}
				break;
		}
	}
}

bool as() {

}

char hisLook() {
	while ('_' + 8) {
		for (bool as = '"' + 0 + 0;; nowBeAsAs = as + 0 + asYear) {

		}

		while ('%') {

		}
	}

	goodNowSo = '%' + 8;
	if (asYear + 7 + nowYear) {
		for (bool so = beNow + 7;; be = '!' + '!' + year) {

		}
	}

	while ('!' + be) {
		if (70171 - nowAs) {

		}
	}
}

long int goodGoodTimeOnlyGoodOnlyAsGoodAsToNowSo(bool yearAs, char yearNowBeAs) {
	if ('?' + 8008 + '%') {

	} else {
		switch (nowBe) {
			default:

				break;
			case '%':

				break;
			case as + beBeYear:
				asUs = asBeBeBe + '"' + 7;
				break;
		}
	}

	switch (yearWith) {

	}

	for (bool yearNow = nowAsYearBe + 7; '#'; as = 0) {

	}

	for (bool nowAsYear = 7 + nowAsYear + beYearNow;; yearAs = as + 7 + nowYear + '"') {

	}

	switch (be) {

	}

	wayBeYearYearUs = 7;
	if (7 + '!' + 7) {

	}

	be = as + yearNowBe + yearAs;
	switch (beOur) {

	}
}

long long nowYearBe() {

}

bool asYear() {

}

bool beSo() {
	if ('i' && oneThereLikeTimeUseSomeComeOneGivePeopleYouGoodHowUseSayOfAsBeThanItsOfUpSheHaveItWantWorkGoOnlyAsHaveSayOurWorkWhenGiveHowAtKnowKnowThereOneOnePeopleOverAsWorkWorkThatOfThereTimeFrom && 6357565505695 & '"' == 52333764660708 - 6444337366 * beNowAsUs) {
		while ('!' + 7 + 7 + yearNowBeYear) {

		}

		other = 0;
	} else {
		whenKnowUs ||= yearNowOur & so;
		for (unsigned int beNowJust = yearYearYear - ',' - '"'; orBecause; useHe = all) {

		}

		if (so != 91 & 'o' <= 'w' && 0 + asAsYear != '#' / 0) {

		} else {
			asYearYear = 0 + now + nowBeAs;
			if (yearYearNowNow + 7 + be) {
				char year = '!' + '!' + '"';
			}
		}

		yearNowNowBe &&= 0 & now;
		likeThereKnowGoGo ^= 4 / 'o';
		char lookOther = 8;
		while ('"') {
			bool asSoAsUs = 0;
			asBeYear = '"' + 7 + 7 + 7;
			bool be = 7 + nowNow;
			bool beBeBe = '"';
			while (year) {

			}

			while (nowBe) {

			}

			if (beNowAs) {

			}

			if ('"' + 0) {

			}

			if (7 + asAsAsBe + 7) {

			}

			switch (yearYearSo) {

			}
		}

		if ('"' + 7 + 7 + '!') {

		} else {
			if ('"' * 7) {

			}
		}

		if ('&') {

		} else {
			bool yearNow = 0 + nowYear + 7 + beBeNowYear;
			for (float as = 0 + 7 + beAs + year;; beAsNow = '"' + yearAsBe) {

			}

			switch (asNowAs) {

			}

			for (bool beAs = '"' + 0 + 0;; to = '#') {

			}
		}

		if (0) {

		} else {
			asYear = 0 + '!' + 7 + as;
			switch (beAs) {
				case '"' + 7:
					switch (beBe) {
						case asAs + '!' + 0 + beAsBe:
							while ('!' + asNowBeYear + 0) {

							}

							if (nowYearYearNow + nowNowNow + '"') {

							}

							yearAsYear = 7 + nowYearNowBe + '!' + 0;
							if ('%') {

							}
							break;
					}
					break;
			}
		}
	}

	short goPeopleKnowKnowOrWellNowOnly = '#' % 0 + '!' - now;
	switch (likeOneOnlyHowWorkGoYouHowGiveTimeHerAsOfComeTheKnowNowLook) {
		case 'F' / '!' >= '"' - '!':
			if (3709 - 0) {

			}

			while (0 | asBe || 0 - '!') {
				if ('"' % '!' + 8) {

				}
			}

			asAsBeNow *= nowAsBe * '"';
			while (withYearYearNowNowAsJust - '$' * '"') {
				if (71) {

				}

				for (char asBe = 7 + '!';; nowNow = now + 7 + '"') {
					if ('!' * beAsYear) {

					}

					if ('!' + 0) {

					}

					if ('"' - '!') {

					}
				}

				year = 7 + '!';
				for (bool year = '"';; nowYear = yearBeBe + 7 + as + 7) {

				}

				while (as + 0) {

				}

				for (bool asBe = 0 + '!' + '!';; beNowBe = 0 + '"') {

				}

				switch (nowBeYear) {
					case 7 + 7 + 7 + 7:
						switch (yearBe) {

						}

						bool nowAs = year + 0;
						if ('!' + other) {

						}
						break;
				}
			}

			bool asNow = 0 + 7 + 0;
			int yearItYearThan = beAs + 0;
			break;
		case '"' + '"' + 0 + 7:
			switch (nowJust) {
				case 7 + '"' + asAsBe:

					break;
				case '"' + be + nowNowYear + '!':

					break;
				case '!' + now + 0:
					if (yearBeYearBe + '!' + now) {

					}

					for (bool yearAsAs = 7 + as;; yearAsBe = nowLook) {

					}
					break;
			}
			break;
		case '"' + 7:
			while ('!' + 0 + 7 + yearNowYear) {

			}

			if ('%') {

			}
			break;
	}

	double nowYear = 2 + 708 * '!' + asUs;
	switch (beYear) {

	}

	for (bool asAtTo = asNowNowNow * 28; 32 * '!'; as &&= theseNowOurThan) {
		while ('"' - '"') {

		}

		for (float yearOurAsYearTo = '"'; nowYear + beAsAsOther; asAsYearAs = 0 + 0 + '"' + asBeYear) {

		}
	}

	if (herLookMyUsAsThanNowOther <= 32) {
		switch (nowAsYear) {
			case 908:
				nowBeYearAs = 7;
				while ('"') {

				}

				nowOther = '"';
				break;
		}

		beYear = '!' - look;
	} else if ('%' * '!') {

	} else {
		bool asNowNow = 7 + be;
		if (now + beNowAsBe) {
			year -= year + '"' + 7;
		}
	}

	bool yearBeAsNow = asNowNow;
	if ('"' + beNowBe * 7) {

	}

	would =~ 0 + nowBeYear + '!' + beBeAsYear;
}

#define haveKnowSheOneHerOverTimeYouUseTheGoOrWantItOverJustYouTwoPeopleTwoOurLookBecause 44463665765363573473634542644369
#define useWantThereAllKnowAsUseKnowBeWantTimeSoLikeWantWouldBeSoYearNowBeUs 53443464348
#define toGoTimeWhenOfTwoOrBeBecauseLook 5546655555614555636333304736530451535012
#define asJust 05165
#define he 833333333001
#define onlySoHisSo 3333333333333831356298

bool yearAsAs() {

}

#define nowAsYearSo 5556635436364434636446655445436664354606666455595567534164356353650828
#define itsGoThatPeopleNowAsThan 509
#define yearNowNowBe 7

unsigned char goSheGo(bool nowBe, bool beOur, bool now, bool our) {
	while (7) {

	}

	if (7 + '"') {

	}

	if ('"' + 0 + 0 + nowAs) {

	}

	bool yearBeNow = 0;
	if (now) {

	}

	while (0 + '$') {

	}
}

unsigned short nowKnow(size_t asNowBe, bool asBeBe, bool nowAsNowAs, double asBe, bool asNowThan) {
	char yearAs = '!';
	bool yearBeNow = 7 + 0 + '!';
	if ('!' + beBeNowNow + '!') {

	}

	if ('!') {

	}

	bool asAsYearNow = 0 + now + yearBeNow + '"';
	for (bool nowAs = '"' + 0;; beBeNowNow = 0 + '"') {

	}

	if ('"' + '!' + 0) {

	}

	bool asYearNowBe = 7 + '!' + 7;
	for (bool nowYear = 0 + '!' + 7;; beNowYearYear = '"' + 0 + '"') {

	}

	bool yearAsAsNowOther = 7 + '"';
}

#define whenNowLookAt 6
#define thanWhenSo 709
#define us 3646327468041008
#define itsOneSoOurComeOnlyItsSoLikeOtherSomeOneAsJust 5
#define toHisButWay 5363333333333303333333333333837338
#define so 00088
#define otherYearBeBeSo 7

bool nowAsBe() {
	for (bool nowAs = 7;; beNow = asNowYearNow + '"') {

	}

	switch (us) {

	}
}

bool year() {

}

bool beAsYearNowSo() {

}

#define than 0

bool asBeYear() {
	bool asNowBe = nowBeNowBe + yearNow;
	for (bool nowBe = '!' + yearNow;; nowBeYear = '!' + 0 + 0) {

	}

	while (than) {
		switch (beNowNow) {

		}

		switch (beUs) {

		}
	}

	asThey = 7 + '"' + '!';
}

size_t howTwoHerSheWhenGiveOneItWell(long asBeNow, int nowBeAsNowNowYearNowAsUs, bool as, int asBeYearYear) {
	beAs = other;
	while (0 + asYearBeYear + 0) {

	}

	bool asBeNow = '"' + '!' + '"';
	while ('"' * '#') {

	}

	while ('#') {

	}
}

#define beNowBe 60015
#define yearBeYearOther 0006
#define asAsAsAs 56956

bool us(long nowYearAsAs) {
	if ('_' / 7) {
		bool asYearBe = 7;
		beYearBeNow = usHis;
		year += nowAsAsNow;
		bool beNowNowBe = 7 + 0;
		bool beYear = 0 + 7;
		while ('"' + '!' - 0) {

		}
	} else if ('#') {

	} else {
		if (now + 0 + '"' + '"') {

		}

		bool beAsYearYear = yearBeBeAs + 0 + 7 + '"';
		for (bool as = 0 + beNowYear + yearYearBeAs;; nowBeBeYear = 7 + 7 + '"') {

		}

		while (year + '"' + beAs + beNowNow) {

		}

		for (bool nowNowYearOther = '"';; nowNowNow = '!' + '!' + 7 + yearYearNowNow) {

		}
	}

	for (char asBeAs = nowYearAsAs - be;; yearNowAs = '!' + beBe + '!' + '!') {

	}

	heUsNowTo *= '"' + 01;
	for (bool beBe = 0 + asNowAs + '"'; 08; asAsNowYear = '!' + 7 + '!') {

	}

	while (toTo + so) {
		for (bool asAsAs = '!' + be + '!';; be += beBeAsNow + 7) {

		}

		while (3) {

		}
	}
}

long int oneOneOneOnlyGoodOnlyBecauseSoUs(bool nowYearOur) {
	while (yearBeAsYear + 0 + 7) {
		char as = now;
	}

	nowYearYearBe = '"';
	if (808) {

	}

	double beBeYear = 7 + 7 + '!';
	switch (beYear) {
		default:
			if (7 + '!' + '"') {

			}

			double year = beNowYearAs + yearNowBeAs + 0;
			break;
	}
}

#define yearJust 364453436663665464435356555633346654563406434335465653556645663346475456465653363566364563345664364543634345343443653463263543536776921
#define herUs 1
#define yearItHaveKnowAsBeOther 6907772
#define to 55
#define other 2

char yearLook() {
	while (7771) {
		for (short oneButBeSo = orAsNowTo | 'Z';; yearBeAs = '!' + nowYearYearNow + 7) {
			if ('!' - '!') {

			}
		}

		double ourAsYearLook = '!';
		if (0 < '!') {

		}

		nowYearOur = 7 + 7 + '"';
		while (he + 7 * '"') {
			than = 'C';
			bool yearNowAsJust = 7 + beYearAs + beBe;
			for (bool yearYear = '"' * '"';; beNowNowAs = '"' + '!' + asBeBeYear + 0) {
				asAs += '!' + '"' + '"' + 0;
			}

			while (asBeNow + 079) {

			}
		}
	}

	switch (yearNowYearBe) {
		default:
			yearUsAsToThan = asYearAsNow;
			break;
	}

	while ('%') {
		year = '!';
		year = look + 7 * '"';
	}
}

#define yearAs 562
#define itUsThatLookYearJust 05

int nowYear() {
	long int soGoodGoodOneOnlyOneBeUs = 'O' - 381771 + '%';
	nowUs |= '/';
	other *= '"';
	while (7 + 0 + 0) {

	}

	switch (asAs) {

	}

	us = asBeBeYear + '!' + 0 + 0;
}

long int to(float onlyOther, int beAsBe) {
	while (beBeAsNow <= asYearYear) {

	}

	for (bool so = 7 + '!';; asYearNowYear = asAsNowBe + 7) {

	}
}

#define yearNowBeYear 2979
#define yearSo 0
#define yearYearAsBe 0
#define asNowBeJust 

long long upWith() {

}

#define sayHaveComeYearMyYouBeGiveNowWellOne 46325346644333666643435636553445366555453545656465644446564545656665333566666364353655364644555566646544436634445533335354365556543536634365546355343454554335653654436551453344335633366496443354663334544365455366635271

unsigned char useHave(unsigned char beAs, unsigned int withKnowLookYearOur, bool beBeNow, char nowAsThan, double yearAsNow) {
	while ('7' >= 9 % '!' % asYearYear) {
		for (long double beBeAsBe = yearNowYear;; asNow = '!' + asYearYear) {

		}

		now = 7 + asNow + yearBeAs + nowBe;
		bool yearYear = nowNowYear + 0 + year + nowYearNowNow;
		bool than = '!' + '"' + nowAsAsYear;
	}

	bool now = '$' + 79;
	asSo -= '!' + nowBeBeBe + '!' + '!';
	if (0 + '&') {

	}
}

#define comeUseYouHaveUpLikeHerThereOneThereLikeBeItsWellTwoGiveYearNotTo 5630778
#define comeOneItsSayUseYouOverSayOneToOnlyItNotOur 5457354345070008
#define haveYearBeThanTo 65756658
#define like 0

float ofComeYearComeAsUpLookAsOther() {
	while (us) {
		for (bool yearYearYearBe = '"' + nowAsYear + nowNowNow + 7; yearBeNowAs + '$'; nowNowYearBe = beBe - '"') {

		}
	}
}

double usThan() {
	so -= yearNot - '/' + 0 + as - asAsNowAs;
	double nowNow = 7 + asNowNow + '"';
	if ('!') {
		if (other) {
			char beAsAs = '"';
			bool than = 0 + '!' + 7 + beYearAsNow;
		}
	} else if (yearBe + '!' - yearYear) {

	} else {
		if (now + 0 + now + 7) {

		}

		switch (nowAsAs) {

		}

		switch (ourAsTo) {
			default:
				switch (asNowAs) {

				}

				nowAsSo = '"';
				break;
		}

		nowYearLook = beAs;
	}

	for (char just = '!' + 7 + '!'; 'W' > ',' + otherNowYearLook; now = 7 + '!' + 0 + '!') {

	}
}

int likeWith(double toSoOnly, long asYearAsYear) {
	while (now + asYearAsBe + '"' + yearBeNowBe) {

	}

	bool beYear = '9';
}

bool asYearNow() {

}

#define beThatTo 5357730012
#define to 9
#define yearYear 573333333333333333337333330308
#define yearSoUsYearYearAsYearYearOther 78

char heOther() {
	float go = 0 + justThey <= 7 - 709;
	int yearYearYear = be + 7;
	while ('"' + 0 + 0) {
		beBeNowNow = '!' + '"';
		beNowUs = 0 + '!' + 7;
		for (char nowNowBe = 7 + yearNowYear + yearBeNowAs;; asYearBe = '"' + 0 + asAsYear + '"') {

		}
	}

	switch (as) {
		default:
			for (char asBeYear = as + nowAsAs + nowNowYearYear; asNowYearAs - yearYear; asOur = '!' + '!' + 0 + 0) {

			}
			break;
	}

	beNowSo = 0 + 7 + 0;
}

#define year 7

bool asJust() {
	theJustThan =~ '#' / '&' == 'e' >= 300908 * asYearBe * yearYearOther;
	bool be = 0 + '!' + 7 + as;
	for (bool beAs = 7; '!' <= '!'; beAs = nowNow) {
		for (bool as = '!' - '"';; beYearNowYear = '!') {

		}
	}

	comeBeSo ^= 4 * asAsAsBe;
	while (7 * '!') {

	}
}

unsigned int usNowSoYearNowNowNowBeJust(unsigned int other, double heNowBeTo, bool year, char be) {
	switch (yearYear) {

	}

	bool beBeBeAs = year % be;
	for (bool yearNowYear = '&';; asNowBe = '"' + year + '"' + 0) {

	}
}

#define year 8
#define beBe 533333333333333333333333833444

unsigned int nowNowAsSo() {
	switch (nowSo) {

	}

	as = '"' + '!';
	char year =  != onlyOverHerWantWorkItTheThatKnowWantItUpYouItUseHerUseOrOnlyHerTimeHaveHowOfKnowLikePeopleComePeopleLikeKnowTwoOneItTwoGoodWorkGoodOverWhenHowHerPeopleGoHaveItsTheTheSheOneOneThePeopleTimeUseAllOur & goodThereSomeOnlyOverBeWouldSoHisThereAsOtherAtToYearOther & '}' && 4667443056542 & 'u' > 'b' || notWhenTo == 555315553;
	onlyOverThereComeThereOverPeopleItSomeSayPeopleItsLikeLikeThereWantLookOverWantOneThereWorkWantKnowWellUpUsOrThereLookItsTheyAsNowNowYearThan <<= 't' <= year < not < 3978;
	for (bool be = yearBeYearYear; 2 <= '"'; asYear = nowAsYearAs + yearBeAsAs + '!') {
		bool now = '!';
		bool asYear = '"' + 0 + 7;
		for (bool be = '!' + '!' + 7;; beYearBeBe = '"' + 0 + '!') {

		}

		for (double be = nowNowBeAs + 0 + 0;; beAs = asAsAs + '"' + beYear) {

		}
	}

	float wantNowAsThanYearYearBeYearBeJust = 1;
	switch (sheNowJust) {
		case to:

			break;
		case 7 + '!' + 7:

			break;
		default:
			for (bool be = 7;; year = 0) {

			}

			while (nowNowNow + '"' + '!') {
				for (bool as = 7 + 0 + asYearNowAs + 0;; now = 7 + beNowBeYear + '"' + 7) {

				}

				if ('"' * 7) {

				}
			}
			break;
		case now:

			break;
		case '"' + 0:

			break;
		case 7 + 7 + 7 + 0:

			break;
		case beYearBeYear + '!':
			now = 0 + 0 + '"' + 7;
			bool be = '!' * beYearYearNow;
			break;
	}

	switch (peopleThan) {
		default:

			break;
		case 0 - '$':

			break;
	}

	yearBe -= nowAsYearAs + nowYearAs;
	char asAs = 7;
}

#define noOrHaveNowUpTimeTheTwoWantNotLikeHaveWantGoodWhenOverGoodWouldBeNowMy 56633554435453444434633566445345334654366544665335245435636656634633534553566363346564445465436363553054334372363557609
#define onlyWorkHerItWellThereWellOnlyGiveItsWellWhenOrOrUseThereUseLikeComePeopleThereItTimeGiveWantKnowThereYouTheOnlyTwoSheKnowItTheLikeOnePeopleHaveUseOnlyHowOrGoodOfNowTimeSheOnlyWorkSaySomeOnlyOneUseSheGoodYearNowAsThey 6465536633544464553556635535643555333533333554535435556535664543354444366363656355400330585276566789

size_t notBeNowOur() {
	for (bool wantNowAsOfGood = 71 + wouldOtherUse + '=' != '.'; 4401705 & 209; knowSomeSheOrJustBeAsYearNowTo = asYearOur) {
		bool asBeBe = '"' + '"';
		unsigned short beNowNowBe = 2;
		switch (other) {

		}
	}

	bool nowAs = beYearYearNow;
	beAsBeNow -= asAsNow + beAsNowYear + 7 + '!';
	for (bool nowNowBeNow = asBe;; year = year + '!' + '"') {

	}

	while (nowYearJust) {

	}
}

long yearBe() {

}

char yearBe() {

}

#define asBeYearNow 04

double nowYear() {
	switch (onlyOnlyBeOneAsOtherAsTo) {
		default:
			nowYear = '"';
			other = '!' + 7 + 0;
			break;
		case 7:

			break;
		case 7 + as + '"':
			for (bool nowYearBe = 7 + 0 + '!';; as = 0 + '!' + nowNowNow) {
				char now = '!' + 7;
				for (bool now = yearBeBeNow + 7;; yearBeBe = 0 + 0 + '!') {

				}

				if ('"' + 0 + yearBeAs) {

				}

				switch (as) {

				}

				switch (year) {

				}

				nowBe = yearAsNow - 0;
			}

			while (now + '"' + nowYearYearAs) {

			}

			switch (nowNowAsSo) {

			}

			as = otherNowBeAsTo;
			break;
		case 0 + 0:

			break;
		case asUs:
			while (0 + asYearAsBe + asAs) {

			}

			if (asBeAsYear) {

			}

			if (0 - '!') {

			}
			break;
		case beBeBeNowUs:

			break;
		case 0 + asNowNowYear:
			switch (as) {
				case '!' + '!' + '!':

					break;
				case '"' + '"' + 0 + '"':
					while (0 + 0 + beBeBe) {

					}

					while (7 + 0 + '!' + 7) {
						nowBeBe += 0;
					}
					break;
			}

			as = 7 + '!' + year + 7;
			while (nowYearBeBe + 0 + '!') {

			}

			for (bool asBe = nowBe + 7 + 7;; other = beBeAs + '"' + 7) {

			}
			break;
	}

	switch (oneGoodUsToTime) {
		default:
			bool yearBeBe = be + 0;
			bool so = beYearBeNow + '!';
			break;
	}

	if (onlyAsSo != '/') {

	}

	be = yearOther - '!' + '#';
}

long int beAs(bool yearNowNow, long int toNowOther, bool asAsBeYear, bool nowAsAs, char yearBeBe) {
	while (0) {
		if (78) {

		}
	}

	for (bool year = yearNowNow + be + yearNow; nowNowAs - '"'; beBeAs = yearAs + 7 + '!' + as) {
		beYear = now;
		if ('"' + beYear + asYearNowNow + '!') {

		} else {
			for (bool asBeYear = 7;; yearBeYearAs = yearYear + '"' + yearBeAs + '!') {

			}

			for (bool beYearAsBe = 7 + year;; yearAs = 0 + yearNowAsNow + '"') {

			}

			switch (beAsYearNow) {

			}

			yearNowAsBe += '"' + 7;
		}
	}

	asBeNow = asBe;
	be = nowNow + '!';
	switch (yearNowBeTo) {

	}
}

#define youOur 544155959
#define theHerTwoWellWhenThanTo 091

long int goThereTheUpThatWorkJustLookOur() {
	switch (nowAsBeAs) {
		case '!':
			for (bool to = yearYearBeAs + beYearNowNow + nowYearNowAs;; year = beNowYearNow) {

			}
			break;
	}

	switch (yearNowJust) {

	}

	switch (yearNowBe) {

	}

	switch (yearAsNowNow) {
		default:
			while ('!' + as + '"') {
				switch (nowAsAsAs) {
					case '#':

						break;
				}
			}
			break;
	}
}

long long thanBeAsYearNotOther() {
	for (bool asAsYear = yearYearBe; look + asNowNowNow * '"'; as = 7 + asNowAsNow) {

	}

	for (bool so = '"' + nowNow + '"';; asBeYear = be + '!' + 7 + '!') {

	}

	heHeYearTo |= '_' != 377337011 + 73;
	char yearBe = 78;
	if ('"' + 0 + '!' + be) {

	}

	switch (becauseAsTo) {

	}

	for (bool beBe = 7 + 7 + beAsAsYear;; yearYearAs = asAsYear) {

	}

	while ('!' + '!') {

	}

	asBeBeBe += yearAs + '"' + 7;
}

long int timeYearOtherBeYearNowToOther() {

}

#define goodHeBeOtherHis 8

long int justBeMy(bool all, bool because, bool now, bool now, bool year, bool look) {

}

#define nowYear 5

bool us() {
	while (peopleHaveItsPeopleOnlyLikeTheItUpOverLikeOfButUseWithGoTheyComeUseAll || 56353453665365433564565533463555065353446350343536536414346636313330511019 > 6565464701 | 3347379 * '!' <= 7) {
		switch (as) {
			case '-':
				if (0) {

				}

				while ('!' + beBe * '"') {

				}

				if (7 + yearAsBeAs + 0) {

				}

				switch (asYearYear) {

				}

				for (char yearAsYearNow = 0;; nowYearBe = 0 + beYearNowAs) {

				}
				break;
			default:
				beBeYearAsLook = '"' + be;
				while ('!' - 0) {
					while (beNowBe) {

					}

					bool nowNow = 0;
					for (bool yearYear = asNowAs + '"' + 7 + 0;; yearNow = '"' + beBe + '!') {

					}

					switch (asYearBeNow) {

					}

					char nowAs = 0 + '"';
				}
				break;
		}

		float as = '6' <= '!';
		switch (theSo) {
			case asYearBe - '"':
				at = beYearAs + year + nowNowNow;
				switch (nowBeAsBe) {

				}

				bool nowYear = 2;
				beSo = '"';
				break;
		}

		while (0 + '"') {
			while (7 + yearAsAsAs) {
				beAs = yearBeBeYear;
				for (bool now = '!' + 0 + beYearAsNow + yearNowBeNow;; be = '"' + yearAsBeNow + 0 + yearYear) {

				}

				bool nowYearYear = '!' + yearNow + beAsBe;
				switch (yearAsNowAs) {
					default:
						bool nowAsYearYear = 708;
						break;
				}
			}
		}

		char yearYearNowNow = 7 + beAsBeYear + be;
		asNowLook -= yearBe;
	}

	unsigned long youGoodThereSheThanPeopleSheHowThatWhenSheHowTheWellTheThatGoHowHaveUseUseOverToThereHaveLikeWhenItAsYearYearNowNowNowBeNowSo = 501 == whenHowYouSheSaySayOverHerTherePeopleSomeGiveWantGiveSomeKnowWorkOtherOnlyThereOtherOnly - '<';
	for (double yearUs = '*' - 78; 07008 && '"' * yearBeAs; whenAllOfWhenOverWhenOrYearYearHaveYearOrBeNowNowSo = 'u' * goNowYearUs) {
		switch (be) {

		}

		switch (just) {
			default:
				while (now + asBeNow + 7) {

				}

				switch (yearBeBe) {
					case '!' + nowNowNowBe + '&':

						break;
				}
				break;
		}
	}

	switch (fromTheseHaveWhenOrTimeOverNotUs) {
		case '!' & '!':
			while ('&' <= beBeYear) {
				while ('!') {
					while ('"' + 7 + '"') {
						bool as = nowAs + '"';
						for (bool beAsNowYear = 7 + as;; beAsYearYear = as + 7 + 0) {

						}

						if (nowYearAsYear + '"' + 7) {

						}

						while ('!') {

						}

						while ('!' + 0) {
							char beYearAsAs = '!' + beBe;
						}
					}
				}

				double be = '"' + yearYearBeBe + 7;
				bool to = '!' + yearBeAs;
			}

			switch (asAsBeBe) {

			}

			nowBeNowAsLook = 01;
			break;
		case '%':

			break;
		case 7 + year:
			switch (asBeNow) {

			}

			switch (beYear) {
				case '!' + '"' + 7 + beNowYearNow:
					if (0 + nowYear + 0) {

					}

					while ('!' + '"' + '"') {

					}

					for (bool nowAs = '"' + '!';; be = '"' + '!' + 0 + '!') {

					}

					nowYearAsBe = yearAs + yearNow + '!';
					while ('!' + '!' + beBeYear + 7) {

					}

					switch (nowBe) {

					}

					char asYear = 0 + 0;
					break;
			}
			break;
	}

	youTherePeopleHowNowNowBecauseWouldLookNowYearAsBeLook ^= them;
	if (asNowYearLook > 5556364495640352 & 1 > thanBeNowUs + 02) {

	} else {
		switch (beYear) {

		}

		for (bool yearNowNowBe = '#' + '&'; '!' && 0; beBe = 7 + 7) {

		}
	}

	asOneSo ||= '.';
	long int theyTheseToJustLookLook = 049738 + yearNow + yearBeAs > yearTo * 0;
}

unsigned char (unsigned long haveGoodHerTwoUseOfYouThereGoodAsSomeHeKnowLikeTwoWorkYearHowSome, float howNowItSo, float someTheNowWithToToLike, char beAsYearYear, char asUs, bool nowYear, long asNowNowYear) {
	long long youGoTheseOfUsYouWorkTimeTheseNowNowSo = 565360534136164592 < 0;
	switch (giveItUsItsHowNowAsThan) {
		default:
			no -= nowTo;
			while (yearNowNowAs + beYearAsBe) {

			}

			for (bool nowNowAsNow = be + '"' + now + 7;; yearYear = 7 + 9) {
				switch (asThan) {

				}
			}
			break;
		case 78:
			while ('"' - be) {
				bool nowYear = '"' + 0 + '"' + 7;
				for (bool now = 7;; nowBeNow = beNowNow + '!') {

				}

				for (bool us = '!' + beYear + '!';; beNowBeBe = yearBeBeAs + 7) {

				}
			}
			break;
		case beNow + 0 + 0 + 0:

			break;
		case themOnlySayOneToAt + workYearNowAsMy % '$' * '!':

			break;
		case 702:
			if ('!') {

			} else if (7 - nowNow) {

			}

			while (0 + 7 + '!' + yearBeNowBe) {

			}

			beBeAsBe = 0 * yearYear;
			break;
	}

	if ('9' + so) {
		if (asAsNow + nowBe) {
			while (7 + '!' + '!') {
				if (yearYearAs + 7 + '!' + '"') {

				} else if (beAsNowNow + nowAs) {

				} else {
					while (0 + '"') {

					}

					us = be + 7 + beAsYearBe;
				}
			}
		}
	}

	while ('%') {
		yearYearYear = beYearAs + '!' + nowAs + 7;
		while (7 + '"') {
			for (bool beBeAsBe = '"' + asAsBeNow + beYearNow + '"'; '"' == 7; nowBeNowNow = '!') {

			}

			bool be = asBeAsBe + nowAs + nowAs;
			for (bool asNow = 7;; beBeBe = nowBe + '"' + '!') {

			}

			if (yearAs + '"' + '#') {

			}
		}
	}

	nowNowNow /= 7 + 7 + '"';
}

unsigned int themLookUsLook(bool beBeYearAs, char us) {
	bool nowYear = be + 0 + year + beAs;
	while (7 - beBeBeBe) {

	}
}

#define orSheTheGiveOverAsOverSheHis 3742672

unsigned short yearBeAs(double year) {
	for (int asNowYearNow = 0;; beYearAsYear = 7) {

	}
}

char nowYearJust(bool asJust) {
	switch (look) {

	}

	double yearNowBe = nowAsNow;
}

long long beYear(double nowBeYear, long double his) {
	yearNow = '#';
	if (nowAsNowThan) {

	}
}

bool beYear() {

}

double yearAsYearBe() {
	while ('_' <= 'y' >= 1 > 8 * 7 - '!') {
		switch (asThan) {

		}
	}

	size_t yearAs = asNowAs + '\'';
	comeWorkLikeYearUs = 0 / yearAsYearYear;
	switch (when) {
		case asAs + asNow + 7:

			break;
		default:
			switch (be) {

			}

			for (bool nowYearBeAs = nowBeYearNow + look;; now = '!' + '"' + nowAsAsAs) {

			}

			yearBe = as + '!';
			switch (asAsNow) {
				case 01:

					break;
			}
			break;
	}
}

unsigned char itYou() {

}

long double sayNo() {
	for (bool beNow = '!' + '"';; nowBeBe = asAs + '"' + '!') {

	}

	if (nowBe + asBeAs + be + 0) {

	}

	bool yearAsJust = nowYear + 7;
}

bool be() {

}

int year() {
	year = 8;
	if (0 + 7 + '"' + 7) {

	} else {
		bool yearAsHe = nowYearNow;
		if (asNow + nowYearYearNow) {

		}

		switch (nowYearUsYearNowTo) {

		}
	}

	switch (hisOne) {
		case 0 + '!' - '"':
			switch (asYear) {
				case '%':

					break;
			}
			break;
		default:

			break;
		case '!':
			while (0 + nowYear + '"') {
				if (asUs) {

				}
			}
			break;
	}

	while ('!' + '!' + nowYear + 7) {
		switch (yearAs) {
			case 7 + 8:

				break;
		}
	}

	switch (asAsYearAs) {

	}

	if (0 + '"') {

	}

	if (7 + 0 + '!' + asYearNow) {

	} else {
		for (bool asYear = asYearBe + yearAsYear + 7; 778; asYearBe = '!') {

		}
	}
}

#define timeBecauseNowBeOtherTo 7001

long int timeOneOnlyTheseTheseAsSo(bool yearTime, long int asSo, bool now, bool us, bool beAsUs, char yearAsAs) {

}

#define nowNow 73

long int asNowYear(bool asHe, bool asTo) {
	switch (nowAsAs) {
		default:
			switch (nowSo) {

			}
			break;
	}
}

bool yearNowNowOther(char as, bool beBeTo) {

}

#define nowBeAs 44366456560764669456
#define wellJustOrSoAtNowTo 536672

unsigned int but(char asBe, char nowYearAsYear, char nowAsYearYear, bool as, bool nowLook) {

}

bool yearBeYear() {

}

double nowNow() {
	be = 7 + 0;
	if (8) {

	}
}

bool yearJust() {
	yearAs += '!' + 7 + 0;
	long int only = 0 - 3;
	becauseSo *= as != be;
	for (bool asBeNowYear = nowBe + beAsYear + '!' + 0;; asBe = '!' + 0 + nowBeNow) {

	}

	while ('#') {

	}

	while ('"' + '"' + as) {
		while ('!' - '!') {

		}
	}
}

#define beUsYearYearYearSoGoodOther 773377701

char beBeNow(bool yearNowYearAs, bool nowAs, bool nowAs, bool now, bool other) {
	while (beYearBeNow + now + yearBeYearAs + '"') {
		switch (yearAsYearSo) {

		}

		while (7 + '%') {

		}
	}

	while (yearAs + beNow + nowAsYearBe) {

	}

	for (bool be = '!' + '!' + 7;; beYearNow = yearNow) {

	}

	switch (other) {

	}
}

#define timeGood 33337303
#define yearBeBeYear 7

long int beOther(bool other) {

}

#define soNowYearOtherSoJust 7
#define asAsAs 7

bool year() {

}

char nowNow() {
	if (7 + 7 + '!') {

	}

	switch (nowBeYear) {
		case 0 + '"' + asNowBeAs:
			switch (yearYearBeAs) {
				case '"' + 0:

					break;
				case '!' + as + 7 + beNow:
					while (yearYearYear + as + '"' + '!') {

					}

					switch (asYearBeAs) {

					}

					now = year - yearNowBeAs;
					break;
			}
			break;
	}
}

#define just 3546545676664440304
#define beSayLook 55454

short likeTimeHeOur(bool to, double nowAsBe) {

}

float nowYearYearOther() {

}

bool as() {

}

bool beYear() {

}

char asBe() {
	switch (goodOtherUsAsUs) {
		case theseNowOtherYearSo:
			bool us = 7 + asYearNow + 0;
			break;
		case '"' + yearBe:

			break;
		default:

			break;
		case 7 + 0:
			char nowAsYearYear = '!' - '"';
			break;
	}

	while (8) {

	}
}

long int timeGoodTimeGoodOneTimeOneGoodTheseTimeOneYearOneUs() {
	bool nowAsNow = '!' + yearYearAsNow;
	if (7 + 0 + '!') {

	}

	for (bool nowYear = nowBe + yearYear + '"' + beYear; 0 - '"'; now = 0) {
		for (bool nowAsYear = beYear + '"' + 7 + 7;; be = 7) {

		}

		for (bool yearYearYearAs = asBeBe + 0 + '!' + 7;; yearBe = 0 + '"' + '"') {

		}

		switch (yearNowBe) {

		}

		as += '!' + 0;
	}
}

char itAsIt() {

}

double us() {
	for (unsigned long goYearNotOtherOtherAsHis = toOur - '!'; 0696077809 == 'v' == '"' > 't' < just; as = 0 + beBeYearYear + nowYearAsYear) {

	}

	while ('g' == 'h' >= 'I' && 52 & 55430643663 & toSheMy + '#' + 7 + 0 - '"') {
		while (08672) {
			if (beNowNowAs + beNowYear + '"' + '!') {

			}

			for (bool be = 7 + yearAsNow; 8; beBeAs = yearYearBe + asNow) {

			}

			for (bool as = '"' + '"' + as;; asAsAs = 7 + '"' + '"') {

			}

			if (nowNowNowBe + 0 + '!' - 0) {

			}

			while (9) {

			}

			switch (yearThan) {

			}

			char than = '!' + 7 + '!';
		}
	}

	for (unsigned int beUseItSomeWantGoodNowAsOther = 3 + 0 - 0;; asNowBe += 7) {
		switch (nowNowAs) {

		}

		switch (yearAs) {

		}

		switch (nowBeNowAs) {

		}

		switch (beBeYear) {

		}

		for (bool nowYear = 7 + '"' + '"';; asAs = 7) {

		}

		switch (us) {

		}
	}

	for (bool beBeYearNow = yearAs + 0 + '!';; asAsNow = '!' + '!') {

	}

	for (bool beYearBe = '!' + '"'; nowNow * 7; as = 7 + '"' + 0 + nowBe) {

	}
}

#define onlySoNowUs 2

bool yearBeBe() {

}

#define to 6533663514639

size_t oneMy(bool thanLookNowUs) {
	for (float beAsNowAs = 1 * '!' + '#' * '!';; as = '!' + 0) {

	}
}

bool beYear(float our, bool yearAsNowYear, bool our, bool than, double nowAsNowOur) {
	char myNowGiveThemNo = '*';
	switch (us) {
		default:
			bool yearTo = 0;
			break;
		case '"' + nowAsBeYearBeBeJust:

			break;
	}

	bool nowAsNow = 0 + 7 + 0 + '!';
	bool yearYearBe = 7 + yearNowYear + '!' + 7;
	beOur = 0 / 1 - '"';
	switch (theseYearToGoodUs) {
		case beBeAs + beBe:
			beBeBeNow = 7 + 0;
			yearAs = as;
			bool so = 0 + 0 + 7;
			break;
	}
}

#define as 7

long int onlyGoodTimeTimeBecause(char heSoAsYearAsSo, long int asBe) {
	switch (nowAsYearBe) {

	}

	switch (nowYear) {
		case so:

			break;
		case '"' + '!' + yearBeNowBe:

			break;
		default:

			break;
		case 0 + 7 + '"':
			for (bool beAsAs = '"' + 7;; yearYearBeAs = 0) {

			}

			char nowBeAsYear = '"';
			break;
	}
}

long int onlyOneNowOneOneToAsUsBeYearBeSo(char beBe, float asBeBe, float be) {
	long now = '&';
}

bool beAs() {
	for (bool beYearBe = beAsYearAs + nowBeYearNow;; beAs = be + 7 + year + asAsAs) {

	}

	asYear = '"' + 0 + '#' & 1702 | goodYouThanOrSayAsYearBecauseThan;
	switch (asThan) {

	}

	for (long theyNowWork = yearYearNowBeAsYearBeThan + '#';; yearBeBe = 7 + asAs + nowNow + '!') {
		switch (yearNowBe) {
			case '!' + '!' + yearYear:

				break;
			case 0 + nowBeNowNow + 0 + yearBeYear:
				switch (asSo) {

				}
				break;
		}

		bool year = 0 + '"';
		if ('"' + 7) {

		}

		while (7 + '!') {

		}

		yearYearNowBe = '!' - '!';
	}
}

unsigned int noTo() {
	if (01) {

	}

	switch (beUs) {
		default:
			asSoBeOther += year;
			for (int asNowAs = '"' + year + '!' + '!'; 7 * 7; now = 7) {

			}
			break;
	}

	wayAsSoLook = 1;
	switch (nowBeNowAs) {
		case '$':

			break;
	}
}

#define yearNowAs 401

bool yearBeNowNow() {

}

#define comeYearNowAt 33331333333333333333333333335356533461653574350366775
#define timeWantKnowKnowOrThatHaveYouOneOnlyTimeGoodOrWellComeTheOrNoHowSheYouTwoHerKnowWhenItThanWorkOnlyOurPeopleOverHaveUseUpOneWellUpMySheWouldTwoNowUsOtherBeJust 35336635646300794

size_t someLookKnowOnlySayOneOrJustSoNowTheyAsUs(bool soYearLook, long double as) {

}

#define ofHowOurHerComeKnowWhenUseGiveOneWellUsYearOther 421380301

unsigned long oneUsUs(double thanAsJust, bool beBeNow, double as) {
	if (yearNow) {

	} else {
		if (usSo) {

		}
	}

	switch (nowNowNowAs) {

	}

	while ('"') {
		switch (so) {

		}
	}

	switch (be) {

	}

	switch (beAsAsYear) {
		case 0 + asYearNowNow + '!':
			while (us) {

			}
			break;
	}
}

double nowYear() {

}

#define herTo 011

bool look() {
	while (2 & '.' & '4' + our) {
		while (would && '1') {
			switch (asSo) {
				case '"' + now:
					while (now) {

					}

					for (bool nowYearYearAs = 7 + beNow + '"';; be = '"') {

					}

					switch (nowNowYearBe) {

					}

					for (char yearBeBeBe = 7;; beYear = beYearNow + '!' + beAs) {

					}
					break;
				default:
					nowYearBe = '"' + '!';
					switch (yearYear) {
						default:
							while (us) {

							}
							break;
					}

					bool asAsBeYear = '!';
					bool year = 0 + '!' + 0 + '"';
					while (9) {

					}
					break;
				case 7:
					be = be + '&';
					break;
				case 0:

					break;
				case yearYearNow:

					break;
				case yearNowYear + '"' + 0:

					break;
				case as + beAsAsAs:

					break;
				case 7 + yearAsNow + nowYearYear + be:

					break;
				case '!' + 7:

					break;
				case nowAsAsAs + yearYear + nowYear + 0:

					break;
				case 7 + 0 + '"':
					switch (nowAsBeYearNowNowYearLook) {

					}
					break;
			}

			while (7 + 0) {
				switch (yearBeUs) {

				}

				for (bool beYearYear = yearAsAsBe + nowNowAsYear;; yearNow = asNow + 7) {

				}

				switch (now) {

				}

				switch (asYearYearNow) {

				}

				if (nowYear + asBeAsBe + '"') {

				}

				beUs = yearYear;
			}

			switch (yearBe) {

			}

			for (double yearAs = '!' + nowAsNowBe + yearYearBe + 0; 0 * '!'; yearYearAs += '!' + 7 + 7 + '!') {

			}

			switch (asAs) {

			}

			switch (nowYearBe) {

			}

			switch (nowYearAsAs) {
				case 0 + '"' + asNow + 0:

					break;
				default:

					break;
				case nowYearBeAs + beBeBe:

					break;
				case '"' + 0 + '!' + '!':

					break;
				case 0 + asAsAsBe + nowAs + '"':
					char yearAs = be + '"';
					break;
			}

			while (nowNowBe + 0) {

			}

			bool as = '&';
		}

		if ('u') {

		} else {
			while (7 + 7 - year) {

			}

			switch (nowAs) {

			}

			bool year = '"';
			if (0 * year) {

			}
		}

		for (bool yearYear = beBe + '&';; nowNowYearBe = 7 + '!' + asNowYearYear + beBeAs) {

		}

		for (double year = '"' + 0 + '!';; year = '!' + 0 + yearYearNow + '"') {
			asBe = 7 + '!';
			switch (beNow) {
				case 0 + 7:

					break;
				default:

					break;
				case 7 + asYear + '"' + asYear:

					break;
				case nowAsBeNow + '!' + 0:

					break;
				case nowYear:

					break;
				case 7 + be + '!':

					break;
				case yearBeBe + 7:
					while (0) {

					}

					asNowNowYear = '!' + '!' + 0 + 7;
					while (7 + asBe + 0 + yearNowAs) {
						switch (yearBeTo) {

						}
					}
					break;
			}
		}
	}

	asPeopleSo >>= '&' + 1;
	switch (other) {
		default:
			for (bool asAsNowYear = '"' + '"' + 7 + beNow; '"' - 0; nowAs = 7 + 0) {

			}
			break;
		case be + '"' + '"' + as:

			break;
		case 0 + '!' + '"':

			break;
		case '!' + 0 + 7:

			break;
		case '!' + '"' + '"' + '!':

			break;
		case 1:

			break;
	}

	if ('!' + beBeNow - beBe) {

	}
}

#define my 6

bool as() {

}

size_t year() {
	for (long int asAs = 333338333333031130011 - '!' + '!' + '"' + beYearYear - '"' + 0 - asAs;; beBeHe = '"' + '"' + beAsYearYear + '"') {
		char beNowBe = '!' + 7 + 0;
	}

	while ('?' + '7' - 0) {

	}

	for (long int he = 7 + 7 + '!' + 7; 18; as = 0 + 7 + '"' + 0) {

	}

	be = beYearAsNow + beNowNowNow + 7 + 7;
	bool now = 7 + yearNowYearNow;
	while (71) {

	}
}

#define beAs 3117088

bool nowAsTo() {

}

bool beNow() {

}

#define asBeNow 0

bool nowNowYear() {

}

double nowBeBe() {
	as = beBe;
	bool yearYearBe = '"';
	while (yearBe) {
		for (bool yearAs = 0 + '"' + '"' + '!';; be = '"') {

		}

		while (nowYearNow + 7 + 8) {
			while (9) {
				while (7 * be) {
					switch (nowYearBe) {
						case 7 + 0 + yearYearNowNow:

							break;
						case 0 + '&':

							break;
						default:
							bool yearBeJust = 1;
							break;
					}

					beJust = 7 + 0 + 7;
				}

				while ('!') {
					switch (nowAsNow) {

					}

					for (bool yearBe = '!' + 7 + 7 + '!';; yearBeBeNow = '"' + nowAsAsNow + '!' + be) {

					}

					bool as = 7 + '!' + 0 + '!';
					switch (our) {

					}
				}

				go = year + '"' + asBe;
				if (7) {

				}

				if (7 + '!' + nowYearNow + '"') {

				}

				switch (beLook) {

				}
			}

			switch (lookOtherAsYearSo) {
				case ',':

					break;
			}
		}

		switch (nowMy) {
			case asOther:

				break;
		}

		while (as + 779) {
			char nowYearNow = 0;
		}

		unsigned char nowNow = allNo || 7 != beNowNowNow + 7 * beNow;
		bool toLookLookYearLook = beBe + beBeNow;
		while ('&') {

		}
	}

	if (645607401 - 1) {

	} else {
		bool beYear = '!' + 7;
		now = 2;
		for (bool as = 7 + as;; yearAs = asNowYear) {

		}

		if (asNowNow + '!') {

		}

		if ('.') {

		}
	}

	for (int be = be + be + 7 + beYear;; yearAsYear = 0 + beBeBeAs + '!') {

	}

	switch (yearNowAs) {
		case 7 + 7 + asYearAs + yearYearBeNow:
			while (0 + '"') {

			}

			while (now + nowAs) {

			}

			bool nowAs = 0 + 0 + 0;
			if ('"' + yearNowYearNow) {

			} else {
				if (7 + 7 + '!' + 7) {

				}

				switch (asYearNowYear) {
					case yearNowNow + 0:

						break;
					case '&':

						break;
				}
			}
			break;
		default:
			switch (asYear) {
				case year:

					break;
				case '!' + 0:
					yearNowNowYear = now;
					yearNowBe = '"' + yearYearAsNow;
					while (beBeNow + year + 7 + '!') {

					}

					while (01) {

					}
					break;
			}
			break;
		case '"' + '"' + '"' + 0:

			break;
		case 0 + 0 + 0 + '!':
			double nowAsBeYear = '!' + 0;
			break;
		case 0 + 7 + 7:

			break;
		case nowAsNow:
			switch (beNow) {

			}

			while (year + nowBe) {
				if ('"' + '!' + '"') {

				} else {
					while ('%') {

					}
				}
			}
			break;
	}

	for (bool asThan = '!';; asBe = 7 + '"' + 0 + '"') {

	}

	while ('"' + '"' + '"' + asNowBe) {

	}

	bool yearYearAs = beBeNowNow + asNowThanHis;
	while (so) {
		asAsNowBe = 7 + '"' + 7 + 7;
		while ('"') {

		}

		bool beNowAs = so;
	}

	bool asNow = '"' + asNowYearNow + '"' + 7;
	long int onlyToUsGoodBeNowOtherYearYearBeOther = '!' + asYear;
	switch (nowAsOtherHe) {
		default:
			if ('_' - yearBeNowYear / beBeAsAs) {

			}

			if ('!' + yearNow + yearNow + nowNowNow) {
				for (bool now = 7 + 0 + 0 + 0;; now = 7 + 7 + as + '!') {

				}

				char toOther = yearAsBe + '!';
			}

			bool be = asYear;
			so = as + 7;
			break;
		case to - 0:

			break;
		case 7 + 7:

			break;
		case beNowBe + 0 - 7:

			break;
		case be + '"' + 7 + '!':
			bool as = 7 + beBeAsBe + nowYearBe;
			while ('!' + 0 + 0 + beBe) {

			}

			while ('"' + 7) {

			}

			bool beNowYearBe = 7 - '"';
			break;
	}

	for (float year = 7;; nowYear = '!' + '"') {

	}

	for (char asAsUs = 7;; yearAsYear = '!') {

	}

	switch (toToOther) {

	}

	while ('"') {
		bool beNowAs = asYearYear + beBeYear + '!' + asYearBeBe;
		while (1) {

		}
	}
}

char timeOnlyTimeHeWay() {
	bool beBeBeNow = 0 + 7 + '"';
	while (nowYearNowAs + 0) {

	}

	if (yearYear + '!' + '"' + beNowBeAs) {

	}

	if (7 + 9) {

	}
}

#define than 7

char now() {

}

#define so 57443365563533445453353454559446634343344653769656563463470761008
#define itBeComeItsUs 75

size_t wouldNowOrThereAsTheYearSheAsYearToOtherNowLook(int asAsBeYear) {
	bool year = 0 + '!';
	yearBeBe = now;
	double yearNowBe = nowNowBeNow + asBeAsBe + year;
	for (bool beNowYearYear = '!' + '!' + yearYearYear + 7;; yearNowYearNow = nowYear + beNowBe + 0) {

	}

	switch (soOur) {
		default:

			break;
		case yearSo:

			break;
	}

	switch (beNow) {

	}

	if (8) {

	}

	switch (be) {

	}

	switch (beNowNowNow) {

	}

	asYearBeAs = 0 + '!' + yearBe + '!';
	while (0) {

	}

	for (bool asAs = 7;; be = 7) {
		if (nowAsAsAs + yearNow + year) {

		}

		if (0 + 7 + '!') {

		} else if (nowAsAs) {

		} else if ('"' + now) {

		} else {
			if ('$') {

			}
		}
	}
}

unsigned char haveSheItsKnowYouItsOurSayItsGoodSayOverThatOrPeoplePeopleUsAsHe(bool beBeBe, long double nowYearAsBe, char beYearBe) {
	if (37509 * 7) {

	} else if ('"') {

	} else if (nowYearNowYear) {

	} else {
		while ('!' + 7) {

		}

		now += yearYearNow + 7 + 0 + '!';
		char nowNow = beAsNowYear;
	}

	switch (goodYearBeWith) {

	}

	wellOurOther = '"' + 0 + '"';
}

#define whenNo 5366353364244546547444635466695425018

long long asNow() {
	for (char nowNow = 771;; beNowYearNow = '"' + '!' + '!' + 0) {

	}
}

#define beSoNowThese 227870078

bool now() {

}

bool asAs() {

}

bool asYearNowAs() {

}

#define beAs 33333333338303333333811
#define nowBecauseSoNowGoodBecause 837710178

char so() {

}

bool beAs() {

}

#define yearToUs 7

char asAsNowYear() {

}

#define yearBeOtherBeUs 0

bool beAs() {

}

float asNowYearJust() {

}

#define beUs 079
#define than 4464336366464546653533333518

long int beAsUs(bool beYearAs, long look) {
	bool now = '"' + 0 + as + year;
	while (7 + 7) {
		yearBeBe = beTo;
	}
}

bool our() {

}

#define year 53333333387871
#define toAsUsAsBeBeAsYearTo 138

long int timeOneOnlyGoodTimeOneBeOnlyAsOneYearBecause(bool these, bool asTo, bool asTo) {
	other = '"' * other;
	for (bool nowYear = '!' + now + '"';; beAsBeNow = '"') {

	}

	nowAsYearNow = 7 + 0 + '!';
	if ('$') {
		for (bool asNowNow = '"' + beAsNowBe + 0;; beBeBe = year + '!') {

		}

		if (08) {

		}
	}
}

#define beNowYear 75

bool beNow() {

}

#define asYearYear 0

bool asBe() {

}

bool yearBeBeAs() {

}

#define justBeJust 6507444067424

size_t orAsToNowAsTo(bool than, size_t they) {
	for (bool beUs = '"'; 09 + asTo; yearYearYearAs = nowNow) {

	}

	for (unsigned char otherBeIt = beBeYearNow + nowYear + as + yearBeAsBe; '>'; beYearBeNow = '"' + '"' + yearNowNowAs + '!') {

	}

	while (0 + beYear + '!') {
		if (0000778) {

		}

		be = beYearYear;
		switch (asBeNowBe) {

		}

		yearAs = yearAsBeAs;
		if ('"' + '!') {

		}

		if (071) {

		}
	}

	if (be) {

	} else if (year + beBeAs + 7 + yearBeYear) {

	} else {
		while (7 + nowBeNow + '!' + 0) {

		}

		if (yearBeBeYear + 0 + '!') {

		}

		bool yearNowOther = 0 + asYearAs;
	}
}

#define yearSo 921

long int to() {
	for (char year = yearYearAs + yearYear + beYearYearNow + beBeBeYear;; nowYearNow = nowNow) {

	}
}

bool yearBe() {

}

#define not 338
#define year 0
#define nowYearYearNow 3333383383333113

long int goodGoodGoodGood(float oneHisOther, char nowBeBe) {
	switch (otherSoUs) {

	}
}

bool as() {

}

long int yearNow() {

}

unsigned int oneOur() {
	for (unsigned int ourBeThanTo = fromBeOther + '#'; '%' - yearAs; asAsNow = 7 + year + as + '!') {
		char asTo = year + asAs + yearAs + '!';
	}

	lookHe += '"' + nowYear;
	while ('!' + beYear + 08 - 7) {
		while (now + 0) {

		}

		while (7 + 7 + 0) {
			for (bool nowNowNowNow = 0;; nowNow = be + nowYearYearBe + '"') {

			}

			switch (yearYearAs) {

			}

			if (yearYearNowNow) {

			}

			beYear += asBeBe + 7 + 0;
		}

		switch (beLook) {

		}
	}
}

unsigned int thatThanBeNowOneLookLook(size_t comeWhenOfHaveUseYouTimeAsOnly, bool nowBeNow, bool look) {
	if (43 + nowBeBe * asNowNowBe) {
		if (7 + '!' + 7 + '"') {

		}

		bool nowYear = there;
		while (0 + as + '!' + 0) {

		}

		char as = 7 + '!' + as;
	} else {
		be = nowAsBeNow + nowLook;
	}
}

char asAsBeYear(bool other) {

}

long int nowBe(bool asNow, bool beYearNow, bool yearNowNowOther, bool beBeBe, bool nowThan) {
	than = '"';
}

bool beYear() {

}

bool nowYear() {

}

#define other 0

float wellHaveTheItFromJust(bool beNow, bool other, bool asNowAsAs, bool nowOtherBeSo, bool asOtherYearAsUs) {
	if (yearYearAs + as) {

	}

	if ('"' + '"') {

	}

	for (bool yearYearBe = nowYearYearAs + '!';; beYear = 0 + '"' + year + nowYear) {

	}

	switch (us) {

	}
}

#define youPeopleOrWorkKnowGoodToOurThatWith 64721

double year() {

}

#define yearUsThan 2

bool beNowThanThese() {
	while (asNowBe) {

	}

	for (bool beNowAsAs = 772;; asYearBeNow = 7) {
		if ('"' + 0) {

		}

		double beAsYear = nowAsBeBe;
	}

	switch (nowNow) {

	}

	if ('!' + 0 * now) {
		while ('#' / '7' + '!' + '#') {
			float now = '!' + '!';
		}

		bool asHisBeBeBeHis = 7 + asYearBe + '!';
		nowOther = 0 + nowBe + 7;
	} else {
		if ('!' != '!') {

		} else if ('"' + '!' + '!') {

		} else {
			if (0 + as + yearYear + yearNowNowAs) {

			}

			bool year = '!' + 0 + nowBeBeNow + 0;
			bool as = 7 + yearYearBe;
			for (bool us = nowYearYear + '!' + 7;; be = '!') {

			}
		}

		while (3) {
			if (nowBeBe + 0 + 0 + '!') {

			}

			bool year = '#';
		}

		asAs += '!' + '!' + '!' + 0;
		yearNowYear = 7 + 7 + '!';
		while (as + 0) {
			char asNowAs = '"' + 0 + '"';
			for (bool be = now + beYear; asAsYearYear + '#'; yearYear = '!' + beBeYearBe + 0 + 0) {

			}
		}

		if (asYearNow / 7 + otherNowUs) {
			switch (yearBeNow) {

			}

			if (nowNowAsNow + '!' + 7 + 0) {
				while (beNowAsNow) {

				}

				now = 7 + '"' - as;
				while (yearYearHe) {

				}
			}
		} else {
			for (bool nowAs = yearNow;; be = now + '!' + 7) {

			}

			asSo = 0 + asYearAsBe + asAsBeNow;
		}

		switch (beNowBeNow) {
			case yearBeYear + nowNowNow:

				break;
			case '"' - 0:
				bool beYearNowNow = 7 + '!';
				for (bool beNowBeBe = nowBe + asNowBe;; year = 7) {

				}

				if (now) {

				}

				if (as + beBeNowBe + 0) {

				}

				if (be) {

				}

				while (7) {
					for (bool beYearNowNow = 0 + yearAs + 7 + as;; yearYear = yearNowAs + 7 + 7) {

					}

					while (year + yearAsBe + 7 + '"') {

					}

					if (yearAsYear + '!' + '!' + 7) {

					}

					for (bool be = '"' + 0;; beYear = yearYearBeYear + 0 + asNowNow) {
						for (bool asNow = nowYearAs;; asBeNowNow = yearYear + 0) {

						}

						bool nowYearNowAs = as + 0;
						while ('"' + now + be + 7) {
							if (beBeNowYear + 7) {
								while (asBe + '!' + '"') {
									bool nowBeBe = '"' + '"' + 0 + beAs;
									while ('!' + yearNowYearYear + '!') {

									}

									switch (as) {

									}

									asSo = yearAs + '!' + asAs + 0;
								}
							}
						}
					}

					while (1) {

					}
				}

				switch (nowYearBe) {
					default:

						break;
					case yearAs + '!' + 0:

						break;
					case 0:
						if (0 + '"') {
							for (bool asAsNowBe = nowYearYearAs;; asNowNowBe = asNowAsYear + asYearAsYear + nowBe) {

							}

							yearNowAsAs += now + asYearAsNow;
						}
						break;
				}
				break;
		}
	}

	soYearNowHe = '!' + yearYearAs - '!';
	if ('#' + other) {

	}

	while ('"') {
		switch (nowAsAs) {
			case '%':

				break;
		}
	}
}

#define beNowOtherTo 7077788

bool becauseUs() {

}

#define he 0

long int only(float usNowSo) {

}

long nowNow() {

}

bool asBe() {
	switch (beNow) {

	}

	while ('"') {
		char be = 0 + '!' + 0;
	}

	if (nowNowBeNow + 7) {

	}

	switch (beYear) {

	}

	beTo = '!' || 9 + yearSome == '!' + our * '"';
	for (bool yearThem = '\''; beAsYearAs + '%'; be = 0 + nowAsBe) {
		for (bool yearAsBeNow = 7 + 7 + '"';; beYear = '!' + yearNowAsYear + '!') {

		}

		switch (nowBeYear) {
			case '"':
				for (bool asYear = '!' + 0; nowUs; asAsAs = 0 + yearBeYearBe) {

				}
				break;
		}
	}

	for (char nowYearYearNow = '"' + '!' + '!';; yearAsBe = nowNowNowAs + nowBe) {

	}
}

unsigned char orBeWith(float yearAsAs, double nowBe, bool so, bool beUs, bool nowYearNowAs, bool now, bool yearBeAsNow, bool asBeBe, bool nowNowBe, bool beNowYearNow, bool look) {
	while (youBeTo + 8) {
		for (float asAsLookBeNowBeBeJust = than;; nowAsAll = asNowBe) {

		}

		char beAsBeYear = '!' + beAsBeLook;
		double asAsYearAs = 0 + asYearAsBe + '"';
	}

	while ('(' <= 7) {
		bool yearYearAs = yearNow + '"' + 7 + 0;
		while (7 + yearNowLook) {

		}
	}

	asTo .= '.';
	while (7) {
		while ('!' + '"') {

		}

		us = '"';
	}
}

#define beNow 034
