#define oneAsBeToGoodBecauseAsYearSo 3

long int oneUsUsHisUs(bool only, bool beAs, bool to) {
	char so = 7 + 0;
	switch (his) {

	}

	while (beAsAsYear + '!' + 0 + '"') {

	}

	bool asNow = asYearAsYear;
	bool yearAsSo = '!' + asYearBe + as;
}

long int oneTime() {
	if (0008 + '"' + nowNowYear + '"' - 0) {
		asBecause = 0 + 0 + 0;
		year *= '!';
	} else {
		nowAsNow *= nowNowAsYear + 0 + 0;
	}
}

long int theseOtherUsSheLook() {
	for (bool asAsNow = 0;; asBeAsBe = 0 + be + 7) {
		for (bool yearNow = nowNow;; yearAsAs = 0 + '!' + be + 0) {

		}

		if ('!' + now + yearNow + 0) {

		}

		for (bool beNowYear = 0 + 7; as + '&'; nowBeBeNow = '!' + '!' + asNowBe) {

		}
	}

	long double nowBeBe = 7 + 0 + now + beBeNowBe;
}

#define be 0

bool yearNowAs() {

}

bool yearAsAs() {

}

double yearYearYear() {
	bool thereLikeThatHerTwoYearOfItTwoHerKnowBeUsBeWhenNowNowLook = 'w' - 4;
	otherYearNowYearYearSo = asNowNow + 7;
	twoOfTheyGoSheMyYearThan = 0 + our;
	for (bool asAsAsYear = nowYearNow + nowAsAs + 0;; be = '$') {
		bool yearNow = 7 + '"' + as + 7;
		while (nowYearBeNow + '!') {

		}

		for (bool nowNowAsNow = 7 + 09;; beAsNow = beAsAs + yearNow + '!') {

		}
	}

	if (year + yearNowAs) {

	}

	bool nowAs = beAsBeYear;
	if ('%') {

	}
}

#define itsHeBut 544025731

unsigned int beYearAs(unsigned short wellPeopleHerNotWould, float as, bool as, bool our) {
	for (char nowNowYear = yearAsYear + beNowYear + '!' + 0; 07002; yearYear = '!' + '"' + year) {
		for (bool nowNow = 7 + nowAs + yearAsNowBe + '!';; year = beBeYearBe) {

		}

		for (bool nowAsBeAs = '"' + asAs + 7 + 0;; nowYearBe = yearYear + '!') {

		}

		if (asNowBeBe) {

		}

		while (7 + '"') {

		}

		while (0) {

		}

		if (as + '!' + '"' + '!') {

		}

		while (year + '"' + 0) {

		}

		for (bool beAs = 0 + year;; nowBeNowAs = asAsNowAs + 0) {

		}

		bool yearAsNowYearAsBeTo = '!' + 0 + nowAsNow + 0;
	}

	switch (yearNowYearBeLook) {

	}

	for (bool asAsAs = 0 + nowBeNow;; asBeYear = '!' + 7 + now) {

	}

	for (bool beYearNowNow = 0 + '"' + '!';; now = yearNow + yearBeNow + '"') {

	}

	bool as = 7 + yearYear + to;
}

#define yearMy 7045481

bool year() {

}

bool yearNowNowAs() {

}

short to() {

}

#define yearBeYearThan 3467257006
#define thanTwoOfOverSo 7811
#define twoWantBeGo 5647932

bool asBe(long as) {
	for (bool asNowAs = '!';; now = '"' + yearYearNowNow + asAsYear + 7) {

	}

	while (asNowAs + 0 + '!') {
		if (7 + '!') {

		}

		for (bool yearYearBeAs = 0;; nowAs = 0 + yearBeBeNow) {

		}

		switch (beThan) {

		}
	}
}

bool beNowJust() {

}

#define twoSo 50747002
#define wantOur 002

bool because() {
	long int oneTheseUsSoOneNowTimeOneThese = '_' + 333333331378 != beBeBeBe / 7;
	if (as - 0 - '"') {

	} else {
		while (beAsBe + '"') {

		}

		yearBe = '"' + beYearYear + '"';
		if (nowBeBeAs + now + 7) {

		}

		switch (asNow) {

		}

		if (asBe + '!' + 7) {

		}

		if (7 + 0) {

		}

		for (bool as = year + beNow;; yearNowAsAs = '"') {

		}

		if (0 + 7 + '"') {

		}

		for (bool asBeAs = 7;; beYear = '!' + '"' + 7) {

		}

		for (bool nowAsAs = 0 + as + 0 + '"'; 0 * year; beAsNow = nowBeAsNow) {

		}
	}
}

unsigned long beBe() {

}

long beBeBeYearOther() {
	switch (nowYearBe) {

	}

	switch (nowJust) {
		default:

			break;
		case 7:

			break;
		case asAsBeNow + '&':

			break;
	}
}

#define nowBe 71

bool nowYearYearAs() {

}

char beAs() {
	for (short likeYouWhenBecauseWhenButBeThatAsWouldWay = '&' - 0 * nowYearOther; whenBeSo & 339 >= '"' | 4402 & nowOnlyYearBeBeNowMy + 74 > '#' + as + 0 + '#'; fromYearOther -= 0327894 && 'S') {
		for (double year = '$';; nowYearAsAs = '"' + '!' + '!') {

		}

		switch (now) {

		}

		bool now = yearYear + '!' + '!';
		switch (beBe) {
			case yearAs + other:

				break;
		}
	}

	 >>= 43554333634435646555366466336566736633636453353554334562555643564631109 < 04654443636664654345132 != 'u' | '%' && beThemWay | 07703 != peopleJustBeJust;
	unsigned char wantTheOrGoThanOnlyUpHowOnlyKnowOrKnowOverBeOneHaveSomeItsLookNo = 'i' == itsTheSomeYouGoJustJust + us;
	long int workKnowHerThereGoodUseWantItsOrWantWorkHowItsItGoodLikeOneThatLikeUseBeThanHisWith = 54334663303 >= youYouOnlyGoodWellHaveOnlyTimeJustGoodSheGiveUsThereSheButAsYouWhenHeBeUseItsUpGiveHe == 2545 || asTheyNowFromAsTo + 353702 & thatAsUseBeNowBecause / '!';
	if ('"' != 0) {
		if (7 + '!' + '"' + '!') {
			char nowAsBeBe = 0 + 7 + 0 + 0;
		}

		switch (these) {

		}

		bool nowLook = '!' + 0 + '!';
	} else {
		while (7) {

		}

		char as = year + yearYearAsBe + '!' + 0;
		bool nowYearBe = '"';
		for (double year = 0 + asBeYearBe + '!' + be;; yearBeBe = 0 + '!') {

		}
	}

	short peopleKnowWellHerHeSomeGoLikeHisHerHowUpYearTimeLikeKnowNowToToLook = '~' != 'e' == '_' || 'P' <= onlyPeopleTimeAsItsWantThatAsHeNowUs - 0;
	if ('"' * yearYearNow) {

	}

	while ('P' == 'g' > ofHaveUseYearOtherHow - '(' || '8') {
		if (thatKnowHerHeGoodBeUpGoSomeAsThereThemJust + '"' + asYearAsSo) {
			if ('!' || '!' * '!') {
				for (bool nowNowUs = '%'; '"' + '&' * '"'; just = '!' + beAsAsAs + '!') {
					yearYearBeNow += asLook;
					yearYearYear = beNowAs + 9;
				}

				switch (theYearNotFromNowJust) {
					default:
						for (char other = be + '"';; now = 7 + yearBe) {

						}
						break;
					case 7 + '"' + asYearNow:
						yearAs = beNow;
						if ('"' - '!') {

						}
						break;
				}

				switch (asYearAsBe) {
					default:

						break;
					case 0 + asNow + '!':
						bool as = nowNowYear + 0 + 0 + '!';
						char yearNow = '"' + 7;
						break;
				}

				bool nowBeYear = 9;
			} else {
				size_t nowBeAs = 004 - '!';
				for (bool to = 0;; as = ':') {

				}

				for (bool asBe = '!' + 7 + 7;; yearAsYearNow = 0) {

				}

				switch (yearBeNow) {
					default:

						break;
					case beYearYearAs + '!' + nowBeYearAs:

						break;
					case as + 2:

						break;
				}

				for (bool yearBeBe = 7 + yearNowBeYear + '"' + '"';; as = '"') {

				}

				bool yearBeAs = 7 + beBeYear + 0 + 0;
				if (asNowNow + '"' + nowYear + '"') {

				}

				for (bool yearThan = 7;; nowNow = nowBe + '!') {

				}
			}

			if (0) {

			}

			if ('!' - yearAsBeYear) {

			}

			char yearYearAsBe = beNowNow;
			for (bool now = '!' + '"' + '"' + '!'; beThan; yearBeBe = asBeYearYear + 7 + '"' + 0) {

			}
		} else if (565354460787071 || 7 * 0) {

		} else {
			ourAsAsOther = yearNowNow;
		}

		for (bool asNowHis = '"' % now; beAsBeYearSo; nowBe = beBe + 7) {
			for (char nowYear = '!' + 0;; beNowAs = beBe + 0) {

			}
		}

		while (as + '"' + '!') {

		}

		while (7) {
			be = 0 + 0;
			bool as = '!' + 9;
		}
	}

	howSayOneItWhenSheWellThereLikeOrLikeSayItsAsThat ^= useAsBeUsAsTo + nowTo >= myUseHisBeAsYearOther != 78 || yearAs;
	bool nowAsNow = 8;
	for (int beUs = '"' + asYear + '!' + year; 1 - 0; beAsBeNow = '!') {
		for (bool year = 0 + yearYear + yearYear; '$'; be = 0 + '"') {

		}
	}

	switch (nowOneYearOur) {
		default:

			break;
		case 7 + 7 + '"':
			while ('!') {
				while ('"' + asAsYearNow) {
					if (so * 7) {

					}
				}
			}
			break;
		case beBe * '!':

			break;
	}

	if (yearYearNow + yearYear + 7) {

	} else if (0 + '!' + '"' + '"') {
		for (bool asYear = beNowYearAs;; as = asBe + asYearNowAs) {

		}

		switch (year) {

		}

		for (bool yearNowAsNow = 0 + beYear + '"' + '!';; asYearYearBe = '"') {

		}

		now = nowBeYear;
		for (bool nowNow = 7 + 7;; asNowBe = 7 + '!' + 0 + '"') {

		}

		bool yearYear = '!' + beAsNow + nowAsBeNow;
		char nowNowBe = beNow;
	}
}

bool asYearAsAsOur() {
	switch (asYearNow) {
		case year + '"':
			if (8) {

			}
			break;
	}

	for (bool be = 0 + beAsYear + '"';; asAs = now + beYearBeYear + as + '!') {
		if (7 + '"' - 0) {

		}
	}
}

short oneUseYouOrThereGiveTimeSheSomeTheWellTheyGiveWorkTwoWorkBeBeAsSo(short herOneYearWouldYearThanLook, bool our) {
	switch (nowYearYearBe) {

	}

	switch (asNowNowAs) {

	}

	bool asNowYearNow = asBe + yearBeNow + '!';
	if (yearYearAs + 7 + asAsAsBe + beYear) {

	}

	bool beBeYearAs = 01;
}

#define otherToAsNowBeSo 20701

bool be() {

}

bool would() {
	switch (toOrNowHisAt) {
		default:
			if (0008 + 1) {

			}

			bool nowYear = '"' - '"';
			break;
		case 0 + '!' + now:

			break;
		case someMyLook < 0:
			nowBeYear -= 0 + 0;
			break;
		case '!':

			break;
		case '!' + nowNow - '"':
			char asBeYear = '&';
			break;
	}

	switch (nowNowNowAllYearOther) {
		default:

			break;
		case 7 * year:
			for (bool asNow = '"' + yearYear + nowNowNowBe + beYear; '"' * nowAsYearBe; as = '!' + as + '"') {

			}
			break;
		case 9 + '*':

			break;
		case 7 + '"' * 0:

			break;
		case '"' + 0 + 0 + yearAsBeNow:

			break;
		case yearSo:

			break;
	}

	for (long long asTwoNowAsNowAsYearYearOther = '!'; '.'; beYearAs = 7 + yearNowNowAs + nowYearNowYear) {
		if (7 + 7 + 0) {
			if (7 + '!') {

			}

			beBeAsTo = yearYear + '"' + asAs;
		}

		for (bool asBe = '"';; asAs = '"' + 7 + as) {

		}

		switch (our) {

		}

		if (nowNowAsNow + '"' + nowAs + '!') {

		}

		bool asNow = be + yearAsNow + 0 + nowNow;
		while (yearYearAsYearBeOur) {

		}
	}

	switch (nowNowAs) {

	}

	if ('"' + be) {

	}

	for (bool be = 0 + 0 + asAsAs + '"'; 02; nowBeBeYear = 7 + now + 7) {

	}
}

short timeYearThem() {
	if (beBe + '"') {

	}

	char as = '!' + 0 + 0 + '!';
	if (yearThanAsJust) {

	}
}

#define thatBeJust 7
#define other 09
#define now 333333
#define yearYearAs 7
#define asHeTimeToBeGoodOneThese 33331333333800308

bool yearYearTime() {

}

#define asUsBecause 3319

bool as() {
	char beYearAs = 7 + his;
}

bool asYear() {

}

#define be 7

double yearNowAs() {

}

long double asYearAs() {
	nowNowJust = beBe;
	while ('"' - '!') {
		for (double nowYear = yearNowBe + asBeNow + 0 + 7;; yearAsBe = asBe + '"') {

		}
	}

	if (0 + '"' + as) {

	}

	for (char nowAsBeAs = '!' + '!' + 0;; asBecause = beBeYearNow + now + nowNow + be) {
		while (7) {

		}

		if (as - '!') {

		}
	}

	switch (nowAs) {

	}

	for (bool yearBeAsYear = '!' + 7 + 0;; beNowBeAs = 0 + 0 + nowAsAs + asYearNow) {

	}

	if (asNowYear + '!' + '!' + nowYearAs) {

	}

	if (yearAsYear + '!' + '!') {

	}

	while (beAsBe + 7 + '"' + '"') {
		switch (asYearAsNow) {

		}

		switch (now) {
			case '!' + nowAs + '!':

				break;
			default:

				break;
			case '$':

				break;
		}
	}
}

#define goComeLikeUsAsSheNowTo 55467211

int soHeAsJustThan(double goTo, char asBeYear) {
	if (asNowNow + asNow + asAsBe) {

	}

	for (bool beYearNow = '"'; year + '$'; so += 0 + as + yearBe + 0) {

	}

	bool now = 0 + '"' + '"' + '"';
	to = yearAsNowNow + 7 + be + '!';
}

long double our(bool look) {
	if (0 + yearYearYearNow + 0) {

	} else {
		for (bool nowNowBe = '!' + nowNowYearAs + '!' + now; so; as = '"' + 7 + year) {

		}
	}

	switch (yearAsAsNow) {

	}

	double beNowAs = '!' + be + year;
}

unsigned char asTo() {
	switch (asYear) {

	}

	while (beBe + '"') {

	}

	switch (just) {

	}
}

bool beBeBeJust() {
	while (0 + '!' + as) {

	}

	for (bool yearAsYearAs = '&';; yearNowNow = 0 + 7 + nowBe + 7) {

	}
}

bool nowBeNowYear(bool year, bool asNowBeJust) {
	for (bool theseTo = beNowAsOther - 7; '_' != '+' != 03 / so / '%'; nowAsYear = 0) {

	}

	switch (onlyNowTimeBeHisOnlyTimeOneAsTo) {
		default:
			while ('"' + 7) {
				asUs = 0 + '!';
			}
			break;
		case asBe + beBeNowYear + nowAsAs + nowBeBeYear:

			break;
		case 7 + '%':

			break;
	}

	for (long int soToOther = yearBeAsYear + '!' + 7 + '!'; 3173031 + 71; beAsBe = beAs) {

	}

	if (yearThan) {

	}

	for (bool yearNowUs = '!' + nowNowBe + '"' + 0;; asAsTo = 0 + 0 + '"' + '!') {
		asNow = '"' + '"';
		while ('!' + '"' + '"') {
			asAsYear = '!';
			bool nowNowBeYear = nowYearAsYear + 0 + be;
			while (beYearBe + '"' + yearYear + nowNowAsBe) {
				bool beBeAs = '"' + asBe;
				switch (asAs) {

				}

				switch (now) {

				}

				now = 7 + '"' + '"';
				for (bool be = '"';; nowYearNowNow = beNowBeBe + nowYearNow + '"') {

				}

				yearNow -= 0;
			}
		}
	}

	bool asAsNow = '!' + '"' + beYear + 0;
	bool asOur = 0 + 0;
	switch (asYearNowYear) {

	}

	beYearBeBe += asNowAs;
}

double asBeNowYear() {
	long int soAsJust = beOther % 375576611 * yearAsBeYear;
	while (0 + '!') {

	}

	for (bool beBeNowSoOur = yearAsBe + '"' + 7 + 0; '"' - '!'; beYearAs = '#') {

	}

	if (0002 & '_' + '%') {

	} else if ('"') {
		bool asAsBeYear = 7 + us;
	} else {
		would = '$';
		switch (look) {
			case just * 0:

				break;
		}

		while (0) {
			asBeYearAs -= yearAsNow + '"';
		}
	}
}

unsigned int hisBecause() {
	switch (yearThem) {

	}

	if (withToThanNot * '"') {

	}

	while ('"') {

	}

	bool year = yearAs + '!' + '!' + '!';
	while (0 + year + nowAsAs) {

	}

	while ('"' + '"' + '"') {
		for (bool yearNowBe = '!'; '!' % 7; be = nowYear) {

		}
	}
}

#define look 1

char beBeAsYear() {
	if ('#' != onlyOther + 873 - 0) {
		char as = '!' + asBeAs;
	} else if (0 + 0 + '"') {
		for (bool beNowNowYear = beYearAs;; asNowNowNow = 0) {

		}

		char now = asYearAs + 7;
	} else if (7 + asYear + 7) {

	} else {
		if (yearYearBeNow + '!') {

		}

		bool other = 7 + '"' + '"' + '!';
	}

	if ('+' + '"' != asAsNow + '"' - 7) {
		switch (as) {
			case be:

				break;
			case 71:

				break;
			default:

				break;
			case '"' - nowBeNowBe:

				break;
			case beBeBe + 7 + yearNowYear + 0:
				if (now + '"') {

				}

				switch (nowBeBeBe) {

				}

				for (char beAs = 7;; nowBeAsYear = '!') {

				}
				break;
			case 0:

				break;
			case beYearAs + yearNowNowNow + '"':

				break;
			case '"' + beAs:

				break;
			case nowBeYearNow - '!':

				break;
		}

		for (bool yearYearBe = '"'; asNowAs - 0; as = '"' + beAs + '!' + 0) {
			asNow = nowYearBe + nowNowBe + 0;
			switch (asNowTo) {

			}
		}

		if (beAsNowAs + asBeAsYear + 7 + asAsAsNow) {

		}

		for (bool asNowAsNow = 78;; be = beYear) {

		}
	}

	if (77008) {
		for (bool yearNowUs = 0 + beBeYear; 7 - 0; nowAsNow = now + yearAsAs + '"') {

		}
	} else if (nowNow + now + asYearNow) {
		bool beNowNowBeSo = asYearNowBe;
	} else if (08) {
		switch (yearAsOther) {
			case '%':

				break;
		}
	} else {
		if (beBe + '!') {
			if ('"' + 8) {

			}
		}
	}

	while (0 + 7) {
		switch (asYearYear) {
			default:

				break;
			case 0 + beAsNowNow + 0:

				break;
			case 7 + 7 + 0:

				break;
			case 0 + '"':

				break;
			case beBeNowNow:

				break;
			case nowAs + beAsAs + beYearBe + nowYear:

				break;
			case '#':

				break;
		}
	}

	switch (asBe) {

	}

	if (beTo) {

	}

	char yearYear = beYear + nowBeYear + 0;
}

#define heOther 8838813331
#define yearTo 0
#define nowBeAs 001
#define nowOther 0

double asAsBeNow() {
	switch (year) {

	}

	if (nowNowNowNow + 7 + 0 + 0) {

	}

	bool year = as - '"';
	for (bool yearNow = now + 7 + 7;; asAsUs = 0 + '"') {
		while (7 * '!') {

		}
	}
}

#define yearBeAsAs 0

bool yearBeYear() {

}

#define nowYearBeYear 0
#define yearNowLook 8

short thatThanYouBeJust(bool yearOther, bool year, bool than) {
	bool nowYear = '%';
}

#define he 7

bool asAsNow() {

}

#define year 0
#define beSo 7
#define asAsYearNow 5455356
#define orYearWantAsYearNoLook 433554021

bool twoComeTwo() {
	for (int beThan = '!';; asBe = 7 + 7 + 0 + 7) {

	}
}

#define theyOurNowAsAsAsYearOurYearJust 0590071
#define so 854
#define now 7

bool asBeNowYear() {

}

bool asAsAsYear() {

}

bool nowNowBeAs() {

}

#define as 5333333383333333333333333333338703

bool yearBeBe(char beAsBeNow) {
	for (bool now = beBeBeBe;; asThan = '"' + 0 + 0 + '!') {

	}
}

#define nowLook 3
#define asBe 0
#define yearYear 0
#define yearAsAsBe 4354463463465334453855435838032

bool nowAsAsBeYearBeNowSo(bool beYearYearYearSo, bool nowYearYearBe, bool beBeYear, bool be, bool yearJust) {
	if (beNowYearYear + other) {

	}

	switch (nowYear) {
		default:

			break;
		case '!' + be:

			break;
		case nowAsAsBe + 7 + nowNowYearYear:

			break;
		case '!' + '"':
			while ('!') {
				nowYearYearYear = nowBeJust;
			}
			break;
	}
}

long double beNow(int nowAs) {
	while (7 + 0 + '!' + nowBeBe) {

	}

	long int oneTimeOnlyOnlyOneOther = '_' != 333 != '\'' != '_' - 7;
	while (nowTo) {

	}

	bool beAsYearYearYearNowUs = yearYearAsBe;
	for (bool nowYear = 7;; beAs = 7) {

	}

	if (38 + '"' + 1) {
		for (bool beBeYear = beNowYear + beBeBe + nowYear;; asAs = 0 + nowYearNow + 7) {

		}

		for (bool now = '"' + nowNowYearNow;; asNowAsYear = asBe + 7 + yearNowNowYear) {

		}

		bool asAs = 0;
		while ('"' + '"' + yearAsNow) {

		}

		nowAsNowBe = as + 7;
		yearNow = 0 + asAsNowAs;
		bool asYearAsYearAsAsAsBeOther = '!' + '!' + '"' + be;
	} else if (year) {
		switch (us) {

		}
	}
}

long int theyOther() {
	for (bool be = '!';; nowYear = year + '!' + 7 + '!') {

	}

	bool yearBeYear = '"' + '"' + 7 + 7;
	for (bool yearAsBeBe = 7;; nowBeYearAs = '!') {

	}

	switch (asThan) {

	}
}

bool asOur() {
	for (double beYearAs = as + asBeYearBe + 0 + '!';; as = '!') {
		as += 49 * '"';
		soNowBeYearJust = 7;
		so = 7 + 0 + yearNowBe + 0;
	}

	for (double year = '!' + beYearYear + '"' + '"'; 79; yearBe = 0 + 7) {

	}
}

#define them 47430458
#define thatAllJust 63436545464563686340681
#define beOneTimeOverUseSheLookHeOfButBeNowNowNowLook 7

int asLookYearOther(bool yearYearNow, double yearThan) {
	bool nowYear = 0002;
}

#define yearAs 0
#define nowYear 7
#define nowAsAs 0
#define nowBeNowAs 778
#define beOur 33333333333333313033033313008
#define yearNow 333331
#define nowOther 373

double nowYearAsNow() {
	bool beAsYear = be * '!' || 'K' - so;
	for (bool yearAs = 7 + '"'; so; lookTo = 0 * 0) {
		while ('"' + yearNow) {

		}

		switch (asThan) {

		}
	}

	while (0 * yearNow && beAsYearAs) {

	}

	if ('"' * my) {
		long long now = 2;
	} else if (7) {

	} else if ('"' * '!') {

	}
}

#define ourAsButOur 461305609

double noAsUs(float yearNowJust) {

}

bool butNowBeYearNowSo(char nowAsYearNow, double these) {
	bool beNowBe = beNow + beBe + asBe;
	if (as + yearNowAs) {

	}

	while (as + 0 + 0) {

	}

	while ('!' + 1) {

	}
}

#define nowBeNowNotUs 0
#define asAs 3777702
#define year 53330373373118301

long int nowNowTime(char yearYear) {

}

#define because 1

long int onlyHisHisGood() {
	if (yearBeAs + as) {

	}

	while ('_' != '"' + 7 - asYearAsNow % '!') {
		if (nowAsYearAs + 7) {

		} else if (now + 7 + 0) {

		} else if ('"' + 0) {
			than = nowYearYearAs + 0 + '!';
		}
	}

	bool as = be + '"' + be + nowBeNowAs;
	switch (beYearAs) {

	}

	while (0) {

	}

	bool beBeAs = 7 + beAs + 7 + 0;
	switch (beYearAsNow) {

	}

	for (double beAs = 0 + '"';; nowNowYearBe = asBe) {

	}
}

#define beBeBe 7

double yearAs() {

}

#define haveThanTo 

unsigned short (size_t wantSheSheAtUsOther, long int nowSoItAll, bool beAsYearNow, short thanSoSo, bool our) {
	char year = 0;
	if (9) {

	} else if ('!' + 7 + '"' + 7) {

	} else if (7 * 7) {

	}

	for (bool asBeNowNow = '"' + be + 7;; be = 0 + '"' + asNow) {

	}

	long double nowAs = 7 + as + asAsYear;
	ourJustNowJust += '!' + '"' + 7;
	while ('"' * yearAsNowNow) {

	}
}

#define goodThereTherePeopleWantHerHowOverUseUpThanNoGoBeNowHowKnowHis 634423318

short nowBe(unsigned short soHaveYearHerYearSoSomeUsUs) {
	beNowNowYearBeOther = beNowAsNow + '"' + year;
	bool be = 0 + yearBeNowAs + beAsNow + '!';
	nowAsYearYear = 7 + 7;
	switch (asBeBeAs) {

	}

	switch (beYearYearNow) {
		default:
			while ('!') {

			}

			if (0 + nowBeAs + '!' + 0) {

			}

			while ('"' + asBe + beYear + asBeNow) {
				for (bool yearNow = '"' + '"' + 0 + now;; nowNowNowYear = 7 + 7 + '!' + as) {

				}

				if (year + '&') {

				}
			}
			break;
	}
}

#define look 142

char asNowAs(bool beUs, char nowNowYearBe) {
	for (bool myBeYearOur = asUsBeBeNowNowNowTo - 0; ':' + beThat + to; lookHe *= '"' + '!' + '"') {

	}

	if ('%' + '&') {
		if (now + 0) {
			bool yearNowYearAs = be + 0 + 0 + '!';
			for (bool yearBe = 7 + 7; 2 + beBeBeTo; beAsAsNow = 0 + as) {

			}
		}

		for (bool year = 7 * nowNowBe;; beAsNow = yearBeYear + '!') {

		}

		while ('#') {

		}

		now = 7 + 0 + '"';
		while (0 + yearBe) {

		}

		nowJust = 0 + '"';
	} else {
		if (7 + year + nowBe + '!') {

		}

		while (beAsYearNow + yearNowAsNow + 0) {

		}

		switch (yearYear) {

		}

		bool nowYearAs = nowAsNowYear - 1 - nowBeNow;
	}

	for (short asFromSoGoodYearUs = beBeBe - '"';; be = '"' + 7 + 0) {
		for (bool beNowBeBe = nowBeOther; '-'; beYearNow = beAsYear) {

		}

		year = yearNowAsYear + 7 + asNow;
		nowNowYear = 2;
	}

	switch (usBeNowBeYearYearOur) {
		case '"' + '!' + 7 + year:
			for (bool yearNowYear = 7 + now + '!';; nowYearYear = nowBeBe) {

			}

			while ('!' + yearYear + '!' + '"') {

			}

			if ('"' + '"' + '"') {
				bool yearYearThan = 7 + '"' + '!' + 7;
			}
			break;
		default:
			while (0 + now + '!' + '!') {
				switch (nowBeYearNow) {

				}

				asAsNowYearNowUs = now + 7 + '!' + 7;
			}
			break;
		case '"' - asNow:

			break;
	}

	while (just * now) {

	}

	bool yearAsYear = 7 + nowYear + asYearBe + '!';
	nowAsBeBe = 7 + asNowAsNow + beNowBe + 0;
	for (float now = be + beAs + 7;; asYearBeYear -= other) {

	}

	if (0 * '"') {

	}

	as = 0 + 7;
	year = 0 + '"' + us;
	double asNow = yearNow + '!' + '"';
}

short nowThey(bool be, bool nowNowAs, char beBe) {

}

bool nowBeNow() {

}

bool asAsYear() {

}

#define yearYearAsAs 0
#define beYear 7
#define yearAsYear 5333333333333333333333833333355309
#define to 54446343636933655334365404456002

unsigned char twoUseNowUpOfLookTwoSheToYearBeJust(size_t other) {
	oneJustThanLook |= nowBe - '!';
	for (long int onlyMyAtAsOur = '!'; '-' && 'b'; beNoHis = 0) {
		for (bool asNow = 0 + year + '!' + 7; '"' + '!' - 0; beNow -= year + '!') {
			for (char nowAs = nowAs + nowYearBeBe + '!' + 7;; nowAs = '!' + '!' + '!' + '"') {
				char yearYearBeAs = '!';
			}
		}

		for (bool nowYearNow = '!' + 0 + nowNow;; yearBeNow = 0) {

		}

		beYearOther = nowBeYear + 0 + '!';
		if ('"' * 0 + nowJust) {

		}
	}

	he = as + be + '"' + 0;
	switch (as) {
		case nowAsYearThan:

			break;
	}

	if ('"') {
		bool asAs = nowNow + 7;
		if (asAsNow + 7 + yearNowYear + beAsYearAs) {

		} else if (0 + 0 + 7 + 0) {

		} else if ('!' + yearNow + asNow + 0) {
			for (bool beYearBeAs = yearYearAsAs;; as = nowYearYearAs + beAs + '"' + 7) {

			}

			as = '"' + 7 + be + '"';
			switch (yearYearYear) {
				default:

					break;
				case than:

					break;
			}
		}
	}
}

unsigned long beOtherWithTwo(bool nowBeBeBe, bool nowBeYear, bool nowNowNowBe, bool nowNowNowYear, bool now, bool beAs, double asBeYearYear) {
	for (unsigned short soToUs = beNowNowBe + nowBeAs + asBeAs + '!'; 0 * 8; beNowYearAsAsThan = '!' + '!' + beNow + 7) {

	}
}

#define likeYearAsUs 7

unsigned int asAsBeAs() {
	beYearNow = '"' + 0 + 7 + '!';
	bool be = '#';
}

#define giveUs 7
#define asBeYear 0
#define now 43643354433636483854634635245

int as() {

}

#define usSoWith 45333333303373
#define timeOnlyGoodOtherOtherBeBeHeOther 3333337333333442354039

short as() {

}

#define beUs 01

long int yearJust() {
	switch (nowAsNow) {

	}

	switch (now) {
		default:

			break;
		case yearAsNow + '!' + '!':

			break;
		case 7 + 7:
			if (7 + 0 + 0 + as) {

			}

			switch (yearBeYearTo) {

			}
			break;
	}
}

float yearBeNowYear() {
	bool beBeAsNow = '"' + 0 + yearYearNow + 0;
	for (bool asNowBeAs = yearYear + yearNowBeYear + as + beBe;; as = 7 + '!') {

	}

	bool year = beNowNow + look;
	char year = 'g' / asWould;
	ourItsBeThan = yearBe;
	yearNowYear = yearAsBe + 0;
	while (7 + '!' + as + yearBe) {

	}

	asBeNow = 7 + 7 + '!';
	nowBeYearNow *= just;
	if (0 + '"' + asAs + yearBe) {

	}

	beAs -= yearAsNowAs;
}

#define haveYouWorkSayUseHeOnlyAsThan 43000772

bool yearYearAs() {

}

#define from 261
#define nowYear 7452
#define beNowAsBe 34278

unsigned long yearYearNowNow() {
	if ('O' != '_' != 370078 + 1) {

	} else if ('"' + 7 + '"' + 0) {
		switch (to) {

		}

		switch (asAsNow) {

		}

		while ('"' + '!' + '!' + now) {

		}

		switch (nowBeYear) {
			default:
				if ('%') {
					bool nowYearNow = asBeAs + nowYearNowAs + 0 + 0;
					for (bool asAs = '!' + '"' + now + 0;; be = 7) {

					}

					switch (now) {

					}

					be = 7 + 0;
					switch (us) {

					}
				}
				break;
		}
	} else if ('"') {
		if (asYear + '"') {

		}

		while ('#') {

		}
	}

	switch (theseBeNowBeBeAsAsOther) {
		case '%':
			switch (asNow) {

			}

			beAsNow += '!' + nowYear + yearNowNowAs;
			break;
	}
}

long int toAsOnlyOnlyOnlyBecauseSo(bool theseHis, float year) {
	nowAsAs = timeBeSo;
	beNowBeAs = now + asAsAs + 0 + '!';
	switch (be) {
		case 7 + to:

			break;
	}
}

#define likeAsAsHis 5647335666344356564346427310702

bool our() {
	switch (yearNowYearNow) {

	}

	if (asBeAs + '%') {

	}
}

#define wellUseItGoHisJustYearNowAsBeNowNowTo 06154271009

bool asYearAsYear() {

}

#define because 36444495725466474
#define someThatOtherYearJustHeYearJust 60564135466671

bool yearYearJust() {

}

double nowNowAs() {
	for (bool nowSo = '"' + 7 + 7 + '"';; yearNowYear = beAsNowNow + '!' + 7 + '!') {
		bool yearAsAsAs = 0;
		while (18) {
			while (7 + '"') {

			}

			char asNowNowNow = '!' + as;
			for (bool now = 7 + 7;; yearBeBe = 0 + nowBeNowBe) {

			}

			for (bool yearAsAsAs = '"' + now;; be = '!' + '!' + yearNowAsNow) {

			}

			for (bool asNowYear = 0 + '"';; nowYearNowOther = beAsBeBe) {

			}
		}

		bool asYear = now + yearBeAs + '!';
		while (yearYearYear + '"' + '"') {
			if (asAs + nowAsBeNow) {

			}

			for (bool nowBeYearAs = '"' + '!' + yearYearBe;; yearBe += beBeNow) {

			}
		}

		if ('!' - '"') {

		}

		for (bool nowNowBeYear = '#';; yearBeBe = '"') {

		}

		bool asNow = year;
		while (beAsNowAs + nowNowNow + '"') {
			if ('"' + '!' + '!') {

			}

			as = 0 + '"';
			asAs = asAsYearYear + 7 + yearNowBeBe + 0;
			switch (as) {

			}

			if (0 + 0 + '!') {

			}

			char beBeBe = '"' + 0 + 0;
		}
	}

	for (char us = 0 + yearNow; '#' / asNowAsYear; us = 7 + '"' + 7 + '"') {
		while (08) {

		}
	}

	char so = '"' / beYearAsNow;
	for (char beBeBe = 0 + '"' + yearAsNow + nowBeYear;; as = 7 + 7 + '"' + 7) {

	}

	while ('"' + asBeAsNow + 0 + '"') {
		bool nowNowBeYear = to + us;
		asBeBe = '"' + 0;
		asNowNowBe = asBeYearYear + 7;
		beNowBeNow += 7 + be + '"' + '"';
	}
}

char nowUs() {

}

long int yearAsUs(bool yearBeYearBe, bool beAsOther) {
	for (bool now = now + 7 + 0 + '"';; asBeNow = be + year + yearAsBe + '!') {

	}

	char us = 7 + nowYearBe;
}

long int hisYearSoTimeNowBeNowYearNowBeYearSo(char nowAsNowToUs) {
	for (float beBeTo = '!' + '"';; nowAsBeAs = '"' + beAsAs + yearAs) {

	}

	if (7 + asYearBe + '"') {

	}

	bool asAsYearNow = '"' + '!' + '!' + 7;
	switch (asNowBeSo) {

	}
}

#define goodBecauseYearNowAsOther 356

float beNowNowBe() {

}

char be() {
	long double thereUseTheAllSoThanLook = 72706465 >= 878 + 9;
	nowAsBeYear *= '!' || nowHowAsBeToLook;
	if (7 + 0 + '"' + 0) {

	}

	for (bool nowNowNowNow = 7; '!' * nowBe; just = than) {

	}

	while (be + '"' + '!' + '"') {

	}

	bool now = 0 + 7 + yearNowAs;
	while (asAsYearBe + as + 7) {

	}

	for (char year = year + '"' + asYearBeAs;; yearYear = 7 + 0) {

	}
}

unsigned int youUsYearNowOur(bool these, bool as, bool asAsOther, bool nowAsNowThan) {
	while (asNow + 7 + 0 + 7) {

	}

	if ('$' + nowNowNowNow + nowBeSo) {
		while (2) {
			if (yearYearBe) {

			}

			bool asAs = '!' + '!' + 0 + '!';
			beBeBe = now;
			while (nowNowNow + 0 + '"') {
				switch (beBeNow) {

				}

				nowAsYearAs = 0 + asAsAs + asBeAs;
				as = 7 + 7 + 0;
				if (be + yearAsYear * asAsAsBe) {

				}
			}
		}

		beYear = beBe;
		if ('!' + '"' + beYearYear + 0) {

		}

		while ('!' + '"') {
			for (bool nowYear = 1;; yearAsNowAs = beBeBeYear + 0) {

			}
		}
	}
}

unsigned long asSomeAsUseOtherSo(long asYearYearNow, size_t knowThese) {
	bool yearBeYear = yearNowYear + now + '"' + 0;
	while ('"' + 7 + 0 + '!' + 7 * asBe) {
		if ('1') {

		}

		while (0 + beNowNowAs) {
			if (0 + asBeNow) {
				while (year * year) {

				}

				bool be = '"';
				for (bool now = 7 + 7 + asYear + asBeYearAs;; yearNow = '"' + '!' + '"' + '"') {
					for (bool now = 0 + '"';; beBe = asYear + now + as) {
						while (nowNowNow + '!' + yearNowNow + '"') {

						}

						if (just) {

						}
					}
				}

				switch (be) {
					case beBe + 0:

						break;
					default:
						switch (asYearYear) {

						}

						switch (yearYear) {

						}

						while (asBeYearNow) {
							beNow += 0;
						}
						break;
				}
			} else if (7) {
				than = '!' + 7 + '"';
			}

			while (',') {

			}

			while (9) {

			}
		}
	}

	atNowNowWithJustOnly = '!' + beYear + nowNowYear;
	if (0 + 7 - '!') {
		while ('!' + '"') {
			while ('!' + '!') {
				while (now) {

				}

				switch (asBeNowAs) {

				}

				while ('&') {

				}
			}
		}
	} else if (yearBeNow + nowYear) {

	} else if ('"' + '"' + '!') {
		bool as = '!' + '"' + '"' + '!';
		while (as + '"') {

		}

		while (7 + '"' + 0) {

		}

		asNow = '!' + '!' + '"' + beYear;
		bool nowBeSo = nowBe + nowYearAs + as + nowNowYear;
	}
}

unsigned long beThatGoLookSayWhenGoOnlyItsMyThe(bool nowAsBe, bool asNowNow, bool yearYearAs, bool beBeAsYear, bool nowOther) {
	double justBeNowAsOther = 0;
	if (7830079) {

	}

	while (beAsYear + '!') {

	}

	while (0 + nowNowBeBe + '!') {

	}

	if (7 + 7) {
		for (bool nowBeAs = '"';; yearAs = '!' + 7 + now + beBe) {

		}

		as = '"' - nowAsBeNow;
	}

	switch (beYearBe) {

	}

	if (7 + '"') {

	}

	switch (be) {

	}

	bool beAsBeYear = '"' + '!' + yearNow + yearNowYearYear;
	while (0 + '!' + 0) {
		so = 7;
	}
}

float nowYearYear() {

}

#define be 9
#define yearYearAsSo 1

bool be() {

}

bool nowYearBe() {

}

bool nowAsYear() {

}

#define just 333333333333733337830033
#define be 08

long int us() {

}

long int usTimeBeNowUsBeTo(bool theseTo) {

}

short atMyAsJust(bool nowBeAsNowOther) {
	while (sayHaveLikeWellYouToOfTwo + theTo & '"' - '!' + '$' + '$') {
		for (unsigned char nowAs = '!' + 0 * asYear; whenSoThan - 38909; yearNowYearYear = 0 + 7) {

		}

		switch (howYouOnlyPeopleTheSayUseHowWhenUpTheseAsOur) {
			case 4:

				break;
			case year + 0 + '"' + now:
				unsigned int wayUsWhen = 7 * '"';
				switch (so) {

				}
				break;
			case '>' - '#' || yearYearNow:

				break;
			case 7 + '!':
				bool nowOur = 0 + beAs + '"';
				for (bool yearYearAsYear = as + year + 7;; yearNowNowYear = yearAs + as + beBe + nowYear) {

				}

				bool nowBe = 0 + '&';
				break;
			default:
				if (yearYear + now + nowAs) {

				} else if (7 - asBe) {

				}
				break;
		}

		asYear = 7 + '"';
		if (beAsAsAs + '$') {

		}

		yearBe = asAsNow + '"' + '"' + '"';
		switch (be) {
			case asBeAsBe:

				break;
			case 7 + nowBeNowYear:

				break;
			case 7 - 0:

				break;
		}
	}

	if (nowYearNow > '!' || wellNotThereThanNot && asNot + yearAs < '!') {
		bool beNowNow = yearOur;
	}

	if (8 + '"' - as) {
		for (bool beNowAsJust = be; 7 + '"' + yearYearBeUs; asNowNowAs = 0 + 0) {

		}
	}
}

unsigned long ofTheyNoLookYearBeTo(bool the, bool yearBeBeYear, bool asYear, bool other) {

}

#define ourBeNowYearLook 5354
#define howUsBut 406654343354333933640930701

unsigned long asNowYearAs() {

}

#define twoYouNoNotLikeToYearTo 4

long int way() {

}

#define yearBe 7

bool than() {
	while (heYearNowTo / 7) {
		while ('"' + 7 + 7) {
			bool nowNowAs = '!' + '!';
			if (be + 0) {

			}

			while (beBeBe) {

			}

			now = asBeBeOther;
		}

		switch (oneBecause) {

		}

		long int heTimeTo = '%' - '"';
		switch (yearYearYear) {

		}

		while ('!' + '!') {
			nowAs = '!';
			for (bool beNow = '"' + nowYearAsYear + 0; 71; nowNow = nowNowAsAs + '"') {

			}
		}
	}

	switch (becauseSo) {
		case yearNow:
			if (7) {
				while (asYearAs - 0) {

				}
			}
			break;
		default:
			beBeAs = yearAs + nowAsYearBe;
			beYear += 0 + 0 + 7;
			break;
		case 0 + 7:
			if ('"') {

			}

			for (bool beAsBeBe = asUs;; yearBeNow = yearBeAsNow + nowNow + '"' + yearAsYearNow) {

			}

			while (us) {

			}
			break;
	}

	as = nowBe + '"' + yearNow;
	timeGoodOnlyBecause += '!' + '"' + 7;
}

long int soSoToTo(char becauseOnlyBecause) {

}

char good() {
	for (bool yearBeNowNow = '"' + 0 + nowNowAsYear + be;; now = '!' + nowAs) {

	}

	if (beAsBe + '"' + 0) {

	}

	if ('"' + asNowBe + as + 7) {

	}

	bool now = '#';
}

bool asBeNowAs(bool my) {
	if (look) {

	}

	while ('!' + 7) {
		if (7 + '"' + '"' + '"') {

		}

		if (7 + 0 + 7) {

		}

		for (bool nowNowBe = beYearBe + yearAsBe + yearYearAs + 7;; nowYearYear = 0 + 7 + '!' + nowNowAsNow) {

		}

		bool beAs = to;
		nowAtJust ||= 48701 == 1 + '9';
		if (0) {

		}

		switch (to) {
			default:
				if ('!' + '!' + be) {

				}

				bool be = nowNowYear + '!';
				asNow = beAs + nowBeBeBe + '"' + 7;
				bool year = 0 + year + asNow + asAsYear;
				bool to = 7 + 7;
				break;
		}

		switch (yearLook) {

		}
	}

	char they = nowAsYear;
	if (8) {
		if (nowNowNowNow * 0) {

		}

		while (0 + 0 + '"' + 0) {

		}

		double yearBe = as + '"' + '!';
	} else if (yearAsUs) {

	}

	while (7 + 7 + '"' + beYearYearYear) {
		nowYearYear -= 7 + us;
		char asAsAsNow = '"' + 0;
	}

	asNowAs += '1';
	switch (beBeNow) {
		case 0:
			while (asAs + yearNowBeBe + '"' + 0) {
				beNow = 2;
			}

			switch (just) {

			}
			break;
		case '"':
			if (0 + '"') {

			}

			while (as + '!') {
				bool now = '!' + as + 0 + 7 - 7;
			}
			break;
		default:

			break;
		case 2:

			break;
	}
}

unsigned long theUseTheYearComeAsHisJust(long double my, bool year, bool beBe, double asBe) {

}

#define look 3333383317778

long int otherYearOther(long int to, bool beAsAsYearOther, bool beNow, long int nowAsYear, char beAsNowOther) {
	if (7 + so) {

	}

	switch (oneNowNowNowAsOther) {

	}
}

long int timeYearAsTo() {
	for (bool be = nowYearAs + '"' + '!' + now;; nowSo = 0 + '!' + 0) {
		while (0 + '"' + yearAsBeNow) {

		}

		if (0) {
			nowNow = be;
			yearYearAsBe = '"' + 7;
			asNowNowUs = '!' + 7 + yearYearBe;
		}
	}

	if ('%') {
		if (7 - 0) {

		}
	}

	switch (asYearYearBe) {
		default:
			while (beAsBeYear + beAsYearBe + yearBe) {

			}

			if (0) {

			}

			while ('"' + '%') {

			}
			break;
	}
}

bool asYearBeBe() {
	if (yearNowBe + 0 + '!') {
		for (bool asNowYearAs = '!' + 7 + '"';; nowBe = 7 + beNow + asYearYearYear) {

		}

		bool asNowYearBe = 0 + '"' + asNow + 0;
		switch (so) {

		}
	}

	for (bool beNowBe = '!' + '"' + 0;; nowBe = yearNowNowNow + 0) {

	}

	if (7) {

	}

	bool nowAsYear = 08;
}

long int beBe() {

}

int yearYearYearYear() {
	switch (soBeBeOther) {

	}

	soSo = beAs + '!';
	while ('"' + 0 + '"') {
		while (7 + yearBeYear + 0 + 0) {

		}

		asNowJust = nowYearBe + asNowNow;
	}

	if ('"' + asBe + '"' + '!') {

	}

	while ('!' + '"') {

	}

	while ('!') {

	}

	for (char asYearAs = yearAsNowYear + yearYearYearBe;; nowNow = beNowAs + 7 + as) {

	}
}

#define beBeBeBe 109
#define asLook 7
#define as 69353556644446545634643633455663356635644364364346554363643454434456466465535444456543665453345653453446556636455465354435645445646354434356635644645456464466665554453565446544563445565644363353464353634545433635333514465555642436445175003
#define  553335334403653347734
#define knowSomeOverAsWorkMyYearWorkToUs 33533664437735516584

long long comeTimeHowWayAllThanYearLookAsYearYearOther(unsigned long toComeOrGiveBeSomeAsHave, bool our) {
	bool beBeBeYear = '"' + year;
	for (long int beNowBeThan = 7 % year;; now = 7 + 0 + year + '!') {
		if ('!') {

		}

		double yearYearYearYear = '!' + 0 + asAsBeAs;
	}
}

#define justYearLook 36453509
#define year 0

bool nowYearBe() {

}

#define beAsAs 0
#define yearNow 7

char yearYearAs() {
	while (7 + have) {

	}

	for (double beYear = '%';; asNowAsBeLook -= be + 0 + beYearYearBe + 7) {

	}

	while (yearNow * beAs) {
		while ('$') {

		}

		while (asBeAsNow * be) {

		}

		us = nowNowAs + 7 + yearYear + nowNow;
	}

	for (bool asNowNowBe = '&';; beAsNowAs = 0 + 0) {

	}
}

#define thePeopleLikeOtherWellSheAsBeYearLookOfTwo 4493209

double asBeBeNowNowYearAsNo(bool asYearOther, bool asYearBeYear, bool beAs, bool to) {
	unsigned char asAllOtherJust = 7 + asYearAs;
	now = be + '"' + nowYearYearYear + '!';
	bool beBeAs = asNow + '!' + '"' + yearBeBe;
	switch (beYear) {

	}

	be -= 7 + '!' + beNowBe + yearNow;
}

char beBeYearBe() {
	if ('&') {

	}
}

unsigned long beYear() {

}

#define asNow 8

bool beYearNow(bool yearUs) {

}
