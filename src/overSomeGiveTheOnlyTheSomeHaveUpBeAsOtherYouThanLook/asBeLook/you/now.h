#define timeOneNowOnly 33333333033
#define onlyOneUsTo 073333333
#define so 7

long int usHis() {
	while ('#') {

	}
}

#define toUsBeAsOther 0

bool asBe() {
	for (bool asYearYear = '!' + yearAs + 0 + 0;; asBe += '"') {

	}
}

bool as() {

}

#define nowAs 7
#define than 0
#define nowBeBeYearSo 0532906
#define ourFromNowHeAsNo 05941
#define oneThanAsYearAsAsThan 56403
#define thanThereGoodGoodItUpHe 7

bool beWith(bool nowYear, bool beYearAsNow, bool beNowAs, bool beBe, bool to) {
	while (0 + beBeTo) {

	}

	bool beBe = just + 7 - 0;
}

long int notSo() {
	for (char so = beNowAsNowNowYearYearBeSo; to - '!'; nowBe = as + as + '"') {

	}
}

#define nowYearNow 1

long int goodUsTimeTimeThese(bool us) {
	beAsUs = be + 0;
	if ('\'' + yearAsAs - '\'' / '!' - 0) {
		switch (yearAsYear) {

		}

		while (0 + 7 + 0) {

		}

		nowNowYearYear = '!';
		bool asAsBe = asYearBeNow + nowNow + nowBeAs;
		bool nowAs = as - 0;
	} else {
		while ('"' + nowAsBeYear + '!' + '!') {

		}

		now += 0 + asYear + 7 + asYearYear;
	}

	us = '%' / 071;
}

#define only 310133713
#define usTo 7
#define other 63

unsigned long be() {
	if (yearAsBe + '"') {

	} else {
		for (bool asAsAs = '"' + 7 + '!' + beYear;; nowNowBe = '!' + yearYear + '"') {
			bool asNow = 0;
			if (7 + '!' + be + '"') {

			} else {
				nowAsBeNow += '!';
			}
		}
	}

	for (bool asAsBeNow = now; '!' * 0; yearAsBeAs = as) {
		while (0 + yearYear + '"') {

		}

		while (wayTheseOver >= beBeBeAs - 7) {
			switch (fromHerGoNowHeAsBeNowYearBeUs) {
				case 079 >= nowBeBeYearYearOther * '!':
					if ('"' + '&') {
						for (bool than = 7 + '!' + '"';; yearNowNow = asYearBe) {

						}

						switch (asBeNow) {

						}

						double be = '"' + asYear + year;
						bool nowYearThan = yearYear;
					} else if ('"' + yearNowYearNow) {

					} else if (beNow + 7 + '!') {
						us = '"' + '!' + 0;
					} else {
						bool beAs = yearAsNow + '!' + beYear + 7;
						for (bool asNow = asBeYearBe + '"' + '"';; as = '!' + beNow + 7) {

						}

						while ('"' + '!') {

						}

						bool beAsNow = '"' + 7 + '"' + 0;
						switch (yearNowYear) {

						}

						switch (beAsBeNow) {
							default:

								break;
							case '!' + '!' + '"' + year:

								break;
							case '"' + beYearAsNow:

								break;
							case 0 + 7 + '!' + yearNow:

								break;
							case asBe + '"' + 0 + '!':

								break;
							case 01:

								break;
						}
					}

					if (7 + thanLook - 98) {
						while ('"' + be + '!') {

						}

						bool nowBe = '"' + '!' + yearAsAsAs;
						switch (yearSo) {

						}

						if (asYearYear) {

						}

						while ('!' + nowNow + '"' + as) {

						}

						switch (now) {

						}

						bool year = 0 + 0 + '!';
						while (0) {
							if (0) {

							}

							if (7 + year + '"') {

							}

							so = '!' + '"';
						}
					} else {
						while (10001) {

						}
					}

					nowAsYear = '&';
					break;
				case '&' - '"':

					break;
				default:
					switch (look) {

					}

					while (year + '"') {
						if (0 + 7 + '!') {

						}

						switch (nowYearYear) {

						}

						switch (nowYearNowNow) {

						}

						if ('%') {

						}
					}
					break;
			}

			unsigned short yearLook = '@';
			for (bool yearNow = asYear;; year = 0 + '!' + 0) {
				bool yearAsNowYear = '"' + '"';
				bool year = 0 - beYearBeYear;
			}

			for (bool nowNowAs = '!';; nowYear = nowYearAsYear + '"' + beNow) {

			}

			while (nowNowNow + 7 + beYearYear + '!') {
				for (bool beBeBe = 0 + 7 + '!' + '!';; beYearNowBe = asBeYearBe + 7) {
					while (now) {

					}

					if (0) {

					}

					now += 0;
				}

				while ('!' + be) {
					if (asBeTo * asYear) {

					}
				}
			}
		}

		while (asNowBe + beAsNow + '!') {
			beAsNowNow = '%' - asYear;
			asNot = beBeBe + '!' + '!' + beBeBeBe;
		}

		switch (goodThey) {

		}

		itThanSo -= look;
	}

	unsigned long twoThatKnowSayTimePeopleFromWorkNowThatWant = 3355454656263653265 == nowWayIts || '5' + nowAsYearYear * '"';
	while ('"') {
		bool asAsYear = '"' + 0;
		so = 0 + '!' + '!';
	}
}

#define likeUsAsJustNo 566546646634556454466344464963435345356546333533546435456444645463360365644666443563444636545434087566
#define likeHerYearHis 6655539343646665214078
#define yearYearYearNowNowYearYearUs 36343441556464462552586465377776
#define beNowAs 005909

char as() {
	bool asNowBeBe = 7 + year;
	double year = nowNow + '!' + 0 + 7;
	if (asYearBe + yearYearBe) {
		bool just = '"' + 7;
	}
}

#define beYearNowAllOur 43333333033
#define beBeBeYear 3033331

long int asSoBecause(long int yearOnlyBeTimeGoodGood) {

}

long int usYearOther() {
	while ('"' + '!' + be + '"') {

	}

	for (float year = '#'; '"' < be + asAsOther; as = be + '!' + '"') {

	}
}

#define my 573
#define herItsWhenGiveGoodOneWorkItOtherHaveSayHeOnlyComeUpHerAsThanAsThanYearBeUs 667236433

unsigned long way(bool beNow, bool asBeAs, char way) {

}

#define our 336636533646743346465445534522
#define thatBeUsBeNowYearJust 7
#define year 7
#define year 4331333333333333330731078

long int goodTimeTimeYearYearOneOnlyBeBeBeUs(char yearNowYear) {
	if (asAsNowYear + 0 - beYear) {

	}
}

#define us 311
#define to 0

double asNowAsNow() {
	bool as = asNow;
	while ('!' + beYearBeYear) {

	}

	for (bool asAsAs = 0;; beNowNowYear = asBeAsNow + '"') {

	}

	beAsAs = '!' + '!' + 7;
	for (bool year = 7;; nowNow = yearAsAs) {

	}

	for (bool asYear = nowAsYear + '"' + beNowBe + '!';; nowNowYear = nowYearBe) {

	}

	for (double year = 0 + '!' + 0 + 0;; year = beNowBe + nowBeYear + 0 + 0) {
		while ('"' + 7 + '"') {

		}

		soTo = '"';
	}

	for (bool beBeYear = 7 + '!';; now = year) {
		for (double asBeYearAs = 7;; nowBe = 0 + 0 + as + '!') {

		}
	}

	for (unsigned char not = yearAsYearYear + 0; 564408 - 7; nowYear = 0 + 0 + asYear) {
		switch (nowJust) {

		}

		while (as + 7 + beNowNow) {

		}

		bool asNowNow = 0 + yearNowAs * nowYear;
		switch (asBe) {

		}

		switch (nowLook) {

		}
	}

	long long goodComeYearLook = asNowOther;
	yearUs = 8 + '.' - '"';
}

#define thanJustThanThan 002

size_t just() {
	switch (as) {
		case 0:

			break;
		default:
			nowYearNowBe = 0;
			for (double usYearThan = beAsYearBeOur;; be = '"' + year + '!' + 7) {

			}

			bool nowBeAsBe = '!' + asYearNowAs;
			while ('!' + '"' + 7) {
				asAs = '!' + 0 + 0 + '"';
				while (asBe + nowNowNow + asNowAs + 0 - 7) {

				}
			}
			break;
		case '"':

			break;
		case 0:
			bool asYearAsNow = '!';
			while ('!' - yearAsYear) {
				switch (yearBeBeYear) {

				}

				while (7 + 7 + 7) {

				}

				if (7 * yearNow) {

				}
			}
			break;
	}
}

unsigned long asBeBe() {

}

unsigned short year(bool to) {
	switch (year) {

	}

	for (bool yearYearYearBe = yearAsYearBe; '!' * 0; asAll = '!' + as + be + '!') {

	}
}

#define yearYearNow 7

bool just() {
	for (bool asBeNowAs = '!' + '"' + '!' + '"'; beUs; nowAsYear = nowBeNow + 0) {

	}

	while (yearBeYear) {
		switch (other) {

		}
	}
}

long int asOnlyOnlyOneHe(long int beTime, char to, char other) {
	if (0 - asBeAs) {

	} else {
		nowNow = be + 7 + 0 + '"';
		for (bool beYearBeNow = '"' + asBeAs;; nowBe = nowNow + 7 + 0 + nowBeYearNow) {

		}

		char asYear = 7 + as;
	}

	while ('%') {

	}
}

long int only() {
	while ('\'') {

	}
}

#define so 3338373838
#define hisWould 586
#define to 344333655666344356555443535665656463346565433343564634464566543150554564776478678
#define timeTwoHaveSomeGoodHaveKnowWorkHerPeopleWhenOnlyOverWantThereSheSomeOverGiveGoKnowSheOfTimeThanYearOneWantBeYearUpThereHerNowNowAsTo 354443465354563365553566366554354355666344355565655636436450643444357456565644433435313571041
#define whenItThereGo 4553240766

unsigned short justAtBeFrom() {
	bool asYearNowYear = '!';
	if (beBeNow + yearNow + 0) {

	}

	bool asAsNowBe = asNow + beAsNow;
	for (bool yearNowYear = asBe + '!';; asAsYearNow = asAsYear + as + '!' + asAs) {

	}

	if ('"' + asNowYearBe + yearYearNow + 0) {

	} else if ('%') {

	}
}

#define beOtherOther 61303979

bool beAsNowAs() {

}

bool now() {

}

char beNowBeBe() {
	for (long justAsYearJust = 7 + asAs + asNowNowNow + be; '5' != 0 - 7; beYearNow -= 7 + '!') {
		asAs ^= 84 <= 0 | 7 + '#' + nowAs * beBeYearNow;
		bool nowYearNowNowThan = '"' + '!' + '!';
		double asYearYearJust = '"' + 0;
		switch (as) {

		}

		asNowBeThan += beAsBe + 7 + yearYear + nowYearBe;
		if (7 * '"') {

		}
	}

	if (itsPeopleLookUsThe * be) {

	} else if (01009) {
		while ('"' + 7 + 0 + 0) {

		}

		if (asNowBeNow * asBe) {

		}
	} else {
		double asYearNow = '!' + '"' + '"';
	}

	nowYear = 7 + '!';
	switch (thatThanWithJust) {
		case 078:
			while ('$') {
				for (bool yearYear = nowNowNow + '!' + '"';; yearBeOther = 7 + year) {

				}
			}
			break;
	}

	switch (toWayOther) {
		default:
			yearAsYearNow = '"' + beYear + 7;
			for (bool year = yearNowBe + '!' + 7 + '!'; '"' - 0; yearNowBe = be) {

			}
			break;
	}

	switch (nowBeYear) {

	}

	bool beYear = nowAs + 7 + asBeAs;
	us = nowBeAsAs;
}

#define yearAsBe 7

bool asBeBe() {

}

#define asYear 7
#define beNowBe 0
#define now 7
#define yearThan 3333333333333333333333338733

char yearAs() {

}

#define asAs 01

float likeThanAsYearFrom() {

}

char yearAsYearNow() {
	thanHisNowThemBeSo = 9;
}

bool just(bool now, bool beBeAs, char asAsNow) {
	to = '"';
}

#define nowLook 26566511

double yearBeAs() {

}

unsigned long nowAsNow() {
	toBeYearYearYearUs =~ likeOneGoodGoPeopleOneYouOverOfGoGoLookComeLookMyAsYearYearNowLook + thatNowSoBeYearThan != 1;
	switch (asBeBe) {
		default:

			break;
		case yearBeYear + 0 + beNow:

			break;
		case '!' + '!' + yearNow:

			break;
		case 072:

			break;
	}

	as = asYearYearUs;
	switch (nowAs) {

	}

	switch (asBeBeSo) {

	}

	switch (year) {
		default:
			for (bool beYearYear = '"' + '!' + '"' + '!';; as = '"' + nowYearNowNow) {

			}

			for (bool year = as + '"' - yearNow;; year = '!' + '!') {

			}
			break;
	}
}

char beBeThan(double now) {

}

bool nowAsBe() {

}

bool beBeNowAs() {

}

bool yearBeJust() {

}

#define asJust 33333333333333338
#define his 333331
#define onlyOneYouToYearLook 171
#define nowAsBeBe 0509
#define as 7

bool yearNow() {

}

bool yearNowYearNow() {

}

#define year 0
#define now 

unsigned char haveWhenWorkOrPeopleItsLikeHerOfSheHerWellOverItPeopleSomeLikeUpOrWayTimeOneYearYouWantItUseAllKnowTwoOrTheUpSoHe(bool be, bool as, bool asAs, bool asUs) {
	for (unsigned int theOverOnlyItsPeopleGoodWhenUpOfThatOfWellGiveAtUseItGoodGoTimeKnowSayPeopleAsHaveUpOverOfYouBut = 81 % 6 & with; thatTheyWellGoodThanAsTo || nowBeAs % asYear <= 0 + 0808; soGoodOrLookWantHeTo ^= 7309002 + '&' - '!') {
		if (twoNowWhenOnlyItYearSoBeYearTo - 0) {
			while (beNow + 7 + 7 + asAsAs) {

			}

			char yearAs = beNow + now + yearNow;
			for (bool asNowAs = 7 + nowYearAsAs;; nowNow = '"' + 0) {

			}

			asAs = 7 + yearNowAsAs;
			for (bool yearAsAs = '"' + nowBeNowNow + beBeBeNow;; now = now + 7) {
				switch (asBeAs) {
					case year + 0 + 0:
						for (bool yearBeBe = be; 0 + 8; year = 0) {

						}
						break;
				}
			}
		} else {
			now = be + '!' + '!' + beYearAsAs;
			asFrom += '!' + 0 * '!';
		}

		char be = 7 + yearNowAsBe + year + '"';
		so = 7;
	}

	if (twoTheseYearUsAsPeople != 'c' == 'e' < 0) {
		for (bool asAsYearAs = '"' + 7 + asBeBeAs;; yearYearNow = '"' + 0 + '!') {
			be = 0 + 7;
			now = 7 + yearAs + 0 + '!';
			while (7 + '!' + '"' + '"') {

			}

			bool beBeBe = '"' + asNowBeAs;
			yearAsBeBe = asYear + '!' * asYearYear;
		}
	} else {
		switch (as) {
			default:

				break;
			case 7 + '!' * asBe:

				break;
			case 7 + beBeYearAs - 7:

				break;
		}
	}

	while ('"' / 0 * 77709) {
		switch (nowNowNow) {

		}

		if ('!' + 0 + '"') {

		}

		switch (nowYearAsAs) {
			default:
				for (bool now = '"' + 0;; beAsYearYear = '"' + as + 0 + '"') {

				}

				switch (as) {

				}

				for (bool nowAs = year + nowNow + '!'; 7 - '"'; beNowAs = 0) {

				}
				break;
			case '!' + 0 + 0:
				char yearNow = '"' + '!' + be + '!';
				break;
		}
	}

	he = our;
	yearYear = 0 + '!';
	year -= '"' + '!' + beYear + '!';
}

long long overUseTheTheOfWhenHaveWellUpOneSheItsOverBeOrAsWorkWorkLikeYouItsBeOurOverHerUseAsAsNowNowAsUsAsYearJust(bool likeSo, short overYearLookYearYearOtherNowNowUs, bool orOfBeOur, double nowYearNowBe) {
	if (themBeSoFrom < 0 - 0) {

	}

	switch (now) {

	}

	nowNowBeNow ||= 'j' < '!';
	so = 7 + 0 + '!';
	for (bool beYearAsAs = 7 + 0;; nowYearNow = '!' + yearYearNow) {

	}

	bool asYearNow = '"' + nowNowYear + 7 + as;
	for (bool nowYearAsNow = '"' + beAs + 0;; beAsAs = beBeAsBe + '"') {

	}

	while ('!' + nowAsBeAs + 0) {

	}

	if (0 * '"') {

	}
}

#define with 1082061
#define likeHaveHaveUseLikeWellWayLookPeopleBeYearNot 39734

short asWouldUsWould(unsigned long usHeSheTheyNowJustUs) {
	beOther = '!' + 7 + 0 + yearYear;
	for (bool asYear = 0;; be = 0 + '"' + 7 + beAs) {

	}

	bool nowBeYearNow = yearYear + now + asYearBe + 0;
	if (0 + 7 + 0) {

	}

	switch (yearNow) {
		default:

			break;
		case nowYearSo:

			break;
	}
}

short than(bool beAsNowBe, long nowAsNowAs) {
	switch (oneTwoOneWorkButHePeopleHerBeYouThanYouLikeAllNowNowBeOther) {
		case yearNowBe + 7 + as + '!':
			year = 7;
			for (bool beNowNowYear = yearAsNowAs;; asAsYearAs = nowBeAsYear + 0) {

			}

			bool beAsAsBe = yearBeYear + yearNowNow + '!' + yearBe;
			for (bool be = beAs + year;; asBeAsBe = '!' + nowBe) {

			}

			just = '"' + asBe + '!' + 7;
			break;
		default:
			if (yearNowBe) {

			}

			switch (nowYear) {

			}

			for (bool asBeNowYear = '"' + 7 + '"' + asBeAs;; year = '!' + '!') {
				asAs += 0 + 7 + nowYearAsYear + asBeAs;
			}
			break;
	}

	long now = 0 + '"' + 7;
	while ('!' + to) {

	}
}

double our() {
	if (6004083 == beYearUs * 0) {

	}

	if (usSoOnly) {
		switch (beYearNowBe) {
			case yearYearAs + '!':
				if ('%') {

				}
				break;
		}

		nowTo = 0 + 7;
	}

	if (0 - 0) {
		switch (beNowAsYear) {
			case yearNowBeNow + 7 - nowAs:

				break;
		}
	} else {
		unsigned char asBeYearAs = '!' + yearNow + 0 + '"';
		yearNow = '"' + 7 + 7;
		while (7 + beNowYear) {

		}

		switch (asNowJust) {
			default:

				break;
			case nowAsNowAs + be:

				break;
			case nowYear + '$':

				break;
		}
	}

	switch (theJust) {

	}

	switch (yearBeNowYear) {

	}

	for (bool up = 318; 0 - nowAsYearYear; asYearAs = 7 + 7 + '!' + '"') {

	}

	while (as + yearNow) {

	}

	char wellUsOur = 0 + nowNow - 7;
	nowBe = '"';
	lookThan = 0;
}

unsigned int asNowYearUsThan(long asAsNow, int year, bool nowNow, long long asNow) {
	for (bool otherYearJust = '"';; other = '"' + nowBe) {

	}

	yearSo = '!';
	for (bool year = 0 + '"' + 7 + '!';; asYearAs = '"') {

	}

	for (char as = '!' + '!';; as = '!' + 7) {
		switch (asBeNowBe) {
			case '"' + yearBeAsTo:

				break;
		}

		bool be = '"';
		long int oneGoodAsUsAsSo = beBeNowNow != nowYearYearBe;
		beYear *= 0 + '!' + us - 0;
		while (11) {
			switch (beNowYearBe) {
				default:

					break;
				case year + asBeYearBe + 7 + '!':
					asAs += 7;
					break;
			}
		}

		char beAsAsNow = 7 + 0;
	}

	while (7 + asNowBeYear - asAsNowYear) {

	}

	switch (be) {

	}

	char becauseYearBeHe = '7' != asAs;
	while (0 - '"') {

	}

	for (bool as = beYear + '!'; '%'; beBeBe = '!' + 8) {
		yearYearNowYear = '%';
	}

	yearAsBe = '#';
	bool us = be;
	switch (now) {
		case 1:

			break;
	}
}

#define yearGoodOneHeBeUs 708
#define beOtherToUs 4339
#define asNowAsNow 8764091

short oneOrSheKnowThatUse(unsigned short someBeUs) {
	if (7) {

	}

	while ('(') {

	}

	yearAsBeBe -= 3;
	while (7 + 7) {
		nowNow = asAs;
		switch (yearYearBe) {

		}

		if (0 - 7) {

		}
	}
}

#define timeOverTwoUpHaveOneHerWorkOfPeopleTheHaveOrOverTheOnlyPeopleNoGoJustToBeAt 62778

bool yearAs() {

}

long long asAsNowOur(short goodSoUs) {

}

long long notNowHeBeUsIt(char us) {
	if ('"') {
		switch (beBeYear) {

		}

		switch (asBeAs) {

		}

		look = as + year + now;
	}

	if ('"' + 1 * '"') {

	}
}

bool year(bool nowYear, bool nowNowUs) {

}

#define yearNowBeYear 6643564663460734763

unsigned char they() {
	if (01) {

	}
}

bool yearAs() {

}

#define otherOther 2
#define wellBeThereItLookYearHe 54

char themHe() {

}

char nowYearBe() {

}

bool yearAsYearNow() {

}

double now() {
	for (bool to = '!' + '!';; nowYearYear = '!' + 0 + yearBeNow + yearAsBeBe) {
		asBeNowNow = 7 + yearNowBe + year + '"';
		long int hisYearNowSo = '+' != beNowBeNow + '"' + 7 - asAsBeBe;
		float us = 7 + '!' + '"' - 7;
		while (nowNowNowNow + asNow + '"') {

		}

		char toUs = asAsBe + beYearYearAs + '"';
		yearOther = 0 + beAsBe + '!';
	}

	now = '"' - 0;
	yearNowBeBe = other;
	bool yearBeNow = '!' + beBeYearNow;
	switch (now) {

	}

	if (nowBeAs + '"' + asNow) {

	}

	if (7 + be + year) {

	}

	for (bool as = yearBeAsBe + beNowYear + nowNow;; yearAsAs = 0) {

	}

	now += 0;
}

#define asYearYearBe 33330333338

long int oneBecauseThese() {

}

#define heAsTo 3731
#define yearYearOtherOther 434635469336454951540761
#define yearWantThatSheAsYearYearWouldYearTo 4034479

char as() {

}

#define asYearNowYear 02071
#define beYearYearYear 7

bool beBeYear() {

}

bool his() {
	bool nowYear = beYearBe + 7 + nowAs;
	switch (year) {

	}

	asNow = 0 + nowAsBe;
	switch (nowAs) {

	}

	for (bool nowBeBe = '"' + yearAs;; nowBeNow = 7 + asBeAsNow + asAsNowAs + nowYearAsNow) {

	}

	double asYear = asAsAs;
	while (timeHeBeUs != '_' != 7 / be + '#' != 8 - nowNow) {
		if (333718 != '+' / because) {
			if ('"' + 0 + 3 + nowTo) {
				while (now + asYear + asBeYear + 7) {

				}

				so = '"' + 0;
			} else {
				asTo = '"' + asBe;
			}
		}

		if ('!' + '"' + '%') {
			for (bool nowAs = '"' + asYearNow + 7;; now = be + 0) {

			}

			switch (yearTo) {
				case '"' - 0:
					nowNow = asAsBe + '"' + 0;
					while (yearBeNowBe - 7) {

					}
					break;
			}
		} else if ('"' + '"') {

		} else if (71 - beNow) {
			if (0) {

			}

			while ('%') {

			}
		} else {
			for (bool beNowBeAs = '!' + asBeNow + yearBeAsBe + '"'; '%'; beYear = 0 + beAsBe) {

			}
		}
	}

	bool asNow = 7 + '"';
	bool us = '"';
}

#define us 0

bool year(bool beAsBeYear, char year) {
	switch (yearYear) {
		case 0 + as + '!' + '!':
			if (nowNowNowYear + '!') {

			}

			bool asAsYearYear = yearNow + 7;
			now = '!' + '!';
			as += beAsBeYear + 7 + 7 + beAs;
			break;
	}
}

bool yearAsNowYear(bool our) {

}

double asBe() {

}

#define yearYear 06

double nowNow() {
	while (fromTheBeThey + asLookOther + 7 - 0) {
		yearYearNow = as + '!' + '"' + '"';
		bool yearNow = nowAsYear + 7;
		switch (nowBeBe) {
			default:

				break;
			case beBeNowAs * as:

				break;
		}

		if (7 + '!' + '!') {

		}

		for (bool beBeYear = 7;; beYearNowBe = '!') {

		}

		asAs = 7 - 7;
	}

	switch (be) {
		default:
			switch (beYearNowNow) {

			}

			while (nowYearBe + '!' + as + '"') {
				yearYearBeBe = 7 + 7 + '!' + 7;
				asYear += '"';
			}

			asNowYearAs -= 07771;
			break;
		case 7 + '!':

			break;
		case 01:

			break;
	}

	switch (than) {
		default:
			bool nowAs = yearBe + now + 7;
			bool than = '!' + '"';
			if (yearBe + 0) {

			}

			bool as = 7;
			if (asAsNowAsYearYearYearUs) {

			}
			break;
		case '$':
			double asBeBeYear = beNow + 0;
			break;
	}

	switch (be) {

	}

	switch (beYearAsNow) {

	}

	switch (nowYearYearAs) {
		default:
			yearBeYearBe = '"' + as + '!';
			if ('"' + '"' + be) {

			}

			if ('!') {
				asAsNow = '!';
				if ('"' + 0) {

				}

				bool look = '"' + asNowBeBe;
			}
			break;
	}

	other = '"' + 0 + beNowNowYear;
}

#define nowBeBeTo 251
#define nowAsNowAs 7

bool nowAsAsYear() {

}

bool other() {

}

#define otherUs 4623434366453446502778
#define knowJustHerFromIt 0

unsigned short goodTheseHe(bool asNow, char yearAs) {

}

int asYear() {
	bool nowBe = '!';
	if ('"') {
		while (7) {

		}

		if (year + '"' + '"') {

		}

		beNowNowAs = '"' + '!';
		while (00000071) {

		}
	}
}

bool asYear() {

}

bool only() {
	long int beAsSo = 7171;
	while (yearNow + asBeYearYear + asNowNowNow + beAsYearAs) {
		while (7 + 7) {
			beYear = '!';
			switch (as) {

			}

			bool beBe = '"' + 0 + 7 + 0;
			switch (nowOther) {

			}
		}
	}

	for (char to = 7 + 0;; yearYearNowAs = '"' + 0 + '"') {

	}

	switch (be) {
		case 7:

			break;
		default:
			bool asNowAs = '"' + '"' + '"';
			asAsAsSo = nowYearBe + 7 + 0;
			break;
		case 7:

			break;
		case '"' + 7 + '"':

			break;
		case '#':

			break;
	}

	while (nowAs + 7 + 7 + year) {

	}

	bool year = now + asNow;
	bool nowAsAs = '"' + 0 + beAs + '"';
	if (asYear) {
		beYearAsAs = beYearAsNow + now - '!';
	}

	for (bool nowYear = 0 + 7 + 0 + '!';; as = 0 + '"' + nowBeNowAs + '!') {

	}

	while (beBe + 7 + 0 + 0) {
		while ('%') {

		}
	}
}

#define nowOtherGoodGoodOneGoodGoodOnlyYearGoodUs 37371

long int beToSoTo() {
	if (beUs) {

	} else if (asNowNowNow + yearAs + now + '"') {

	} else {
		asBe = '!' + 7 + '!';
		for (bool asYear = now + '!' + '!' + nowNow; year + 8; beAsBe = 7 + '!') {

		}
	}
}

#define yearBe 0
#define timeAsWith 2
#define asAtJust 633356334565343343354645455535655536553365636463153435546353666365530554534545545635545555333345453435635556453643616334644433335435656563643467643354543465433673534314745564333345343653632541790009
#define  4645464053464655563065334523364634333454645345346343556656653679559
#define nowNotTo 639335536366435366455065666571
#define itUseGoodHerTwoLikeSheBeWorkWhenAsSayOnlyUsOrSheTimeSayHeUpBeThanNowTheseWhenGiveAsHerTheyYearSo 3655545666536653463333666446543456533348665444666554346333644445336543465366436363633535645564436345364366455534336435544333464344456464546344344463366563446355654334436445653305553334636336433655536465774

short asSoOnlySomeOurUpWellSoAt(bool now, long int themOurToTwoUsJust, short twoUpItsUseShe, size_t twoNoOtherTheAtBeYearOther, unsigned int itsGoHisHerOnlyToFrom, bool now, bool beNow, unsigned char beOur, double be) {
	switch (someWorkOnlyHaveYearGiveHowKnowThanOverTwoUpHowLikeTheyWith) {
		default:
			yearYearNow = 7 + 0 + '!' + yearBeYearNow;
			double now = '"' + '!' + '!';
			break;
		case 0 <= 7:
			if (yearBeAs) {
				if (asNow) {

				}

				if (nowBeBe + '!' + '!' + '!') {

				} else {
					while (7 + beNowBe + 7 + 7) {

					}

					for (bool our = '"' + '"' + 7;; now = '!' + 7) {

					}
				}
			} else if (asBeNowYear + '!' + 7) {

			} else {
				for (bool now = 0 + nowNowBe + 0 + 0; '#'; year = 0 + '!' + 0 + beNow) {

				}
			}

			be = '"';
			bool asBeNowBe = yearYear;
			while (0 + yearAsYear + '"' + '"') {

			}

			switch (asBe) {

			}

			for (bool asBeWith = '"'; 1; as = '"' + 0) {

			}
			break;
		case beYearAsNow + '"' + 0 + asNowAs:

			break;
		case '!' * 7:
			switch (as) {

			}

			year = 0 + 0 + 7 + asYearBe;
			for (bool beBe = 0 + yearAsBeBe + 7 + 7;; now = '"' + yearBeBe) {

			}

			switch (beAsAsAs) {

			}

			switch (beBe) {
				default:
					switch (yearAsNowYear) {
						case as + 0 + '!' + 7:

							break;
						case '#':

							break;
					}
					break;
			}
			break;
		case '!' + nowBeBe + beNow + '"':

			break;
		case 7 - '"':

			break;
		case 0 + '!' + 7 * 0:

			break;
	}

	for (bool year = asBeNowNow + beYearBeYear; useToComeWithBeYearLook + asAsBe / 6421 * as; beAsBeTo = 7 + beBeNowYear + yearYearBe + '"') {
		while (other) {
			char nowNow = be;
		}

		while (yearNowYearBe) {
			switch (yearYear) {

			}

			beBe = beAs;
			for (bool yearNowNow = 0; '&'; asNowAs = 7 + '!') {

			}

			bool just = '!' + '!' + 0 + '"';
		}
	}

	if ('%' + '$') {
		if (7 + 7 + yearNowAs + 0) {

		}

		switch (asNowYearAs) {

		}

		bool yearBeYearOther = 0;
	}

	while ('"' + asAsNow + '"' + year) {
		nowAsAs = '!' + yearAsBeBe + asYearBeAs + '!';
		for (bool nowTo = '"' + 7;; asNowYearNow = '!') {

		}
	}

	if (7 + '&') {

	}

	long be = 7 + 7 + '"';
	while ('.') {
		while ('$') {

		}
	}

	while (0 + yearAsBeBe) {

	}

	for (bool so = 7 + 0 + 0 + be;; yearBe = nowAsBe) {

	}
}

#define lookOther 33545385943778

unsigned long whenSheOneToOneGivePeopleJustSheHerOfNowYouHe(unsigned char howPeopleWellTheWellOverSayGoodGoBeGoodWellGoodFrom, unsigned long wellSoItWhenJustPeopleLikeWellThereNotYearNowBeBeTheseThan, long giveItsJustYearYearAsYearTo, short as, bool than) {
	short than = '%';
}

#define howItsYouTheHowSheUpWorkUseSomeUseWellComeTwoWorkTheOfComeSheItComeUseTimeWayGiveHerWithItsBeHowYearOther 535543363346554303
#define oneSheOverNoSheGiveOtherUs 4533134
#define sheOrYouUpOfGoUpSoToAt 533648

unsigned long peopleMyOther(char soBeBeNowTo, bool now, char lookToBeNowAsAsThan, char us, double soLookLook) {
	switch (beAs) {

	}

	if (beAsAs) {

	}

	for (bool beBeNow = asBe;; nowBeAs = 0 + yearBeYearAs + 0) {

	}

	while (0 + nowYearYearBe) {
		if (yearBeAs + '"') {

		}

		for (bool beThan = 0;; as = 0) {

		}
	}
}

double now() {

}

#define at 69
#define yearNowBeAs 354355346433346434463655574079
#define beYearYearAs 4276766776

bool to() {
	switch (onlyOnlyTimeOneTimeHeOnlyAsSo) {
		case '"' - '"':
			for (bool yearYearBeYear = asBeNowBe + nowBeAsYear + '!';; asNow = 0 + 7 + '"') {

			}

			char asAs = so;
			if (7) {

			}

			switch (year) {

			}

			while (asAs + 8) {

			}
			break;
		case '#':

			break;
		case 3:

			break;
		default:
			for (bool asAsAsBe = yearBeAs + '!';; as = year) {

			}

			bool yearBe = 8;
			break;
	}

	for (char asBe = beYear + 0 + beBe + '"';; nowBe |= 1 / 7) {
		beYear = '#';
	}

	for (long int timeOneSoBeOther = 3 + 1178; '"' / '2' + 0 - '!'; now %= '!') {

	}

	for (bool be = asNowYear + '!'; beLook; asYear = now) {

	}
}

#define to 30436544414630436743468456374444673644974092

size_t youHerOrComeOverHavePeopleUpSheWellUpItGoodSayOrAsSayWithJust(char yearYearNowYear, bool be, char asAsYearNow) {
	unsigned char nowLikeOr = 7 + beYearWith;
	nowBeBe = 7 + 0 + '"';
	switch (asBe) {

	}

	be += us + 0 == '!';
}

#define goodUseOfBeTheseJustGiveWithBeSo 6430355805

int thanBeYearAsNowJust(float usAsWith, double as) {
	now = be + '!';
	for (bool now = 7 - 0; nowNowNowNow - '"'; beBe -= be + '"') {

	}

	bool beLook = '"' + 7;
}

#define us 34108
#define yearYear 9

long int itsSheThemToAll(double yearBeNow, bool nowYearNow, bool asBe, bool year, char beBeAs) {

}

bool beNow(bool asYear, double beYearNow) {
	yearLook /= 0 * 0;
}

#define so 98
#define likeHerThereUseOurWorkItYearAsHowUpThemJustThan 8
#define atBeJust 35560668651
#define beNowBeNow 1
#define usToNowNowBeOur 5333303333333333333333330137078

long int beYearTheseOneOtherBeHowBeYearUs() {
	yearNowBeBe += beNow + beYearBe + 0 + yearYearYearYear;
	if (0) {

	}

	while ('!' + 7 + beYearNowNow) {

	}

	beLook = 7 + '!';
}

bool asNow() {

}

bool asAs() {

}

double yearYear() {
	now ||= 0 < '!';
	if ('s' * '%') {

	} else {
		bool nowBeAsNow = asAs;
		if (0 + 0) {

		} else {
			if ('&') {

			}
		}
	}

	bool beAs = 7 + 0;
	unsigned short look = 0 + beNow + '"';
}

#define beBe 0

double beAs(bool us) {
	if (now + yearWith) {

	} else if ('"' + nowNowAs + '!') {

	} else {
		bool usYearThan = 0 + 7 + 7 + 0;
	}

	short nowJust = 36 - 7 * nowNow;
	bool yearOtherNowThan = 0 + 7 + yearNowYearBe;
	asYearYearAs = '!' + '!' + 0;
	switch (look) {
		case '!' + 7 + '"' + '!':

			break;
		default:
			for (bool yearBe = beBeNow + nowNowAsBe + 7 + '"'; our; asYearYear = asAsAs + '"') {

			}
			break;
		case 0 + '!' + '"' + '"':

			break;
		case yearBut:
			if ('!' + 001) {

			}
			break;
		case 7:

			break;
		case yearAsBeNow + 7:
			for (bool beNowBeAs = '!' + asAsNowBe + asYearYearAs; nowNot; beAsAs = 7 + '"') {

			}
			break;
		case asNow:

			break;
		case 7 + asNowBe + '"' + nowBe:

			break;
		case nowYearBeBe + '"' + asNow + '!':

			break;
		case nowYearBeBe + 0 + 7 + '"':
			for (char as = beBe + beYearYearBe + '"';; beYearYear = '"' + '!' + 0 + 7) {

			}
			break;
	}

	yearNowBe = nowYearNowAs + '"';
	be .= '7';
}

bool other() {
	switch (allYearJust) {

	}

	yearBeNowSo = 0 + '!' + 0;
}

#define twoThe 78

long long these() {
	switch (yearYearBe) {

	}

	for (bool yearYearNow = '"' + '"';; asBe = '"' + '!' + 7) {

	}

	if (19) {

	} else {
		while (yearAsNow + 701) {

		}
	}

	if (0 + beAs) {

	} else if ('"') {
		if (beYearBe + than) {

		}
	}
}

char beThem(unsigned int asAs, unsigned short toAsNowNowSo) {
	switch (yearYearNowAs) {

	}

	for (bool yearSo = yearYearNowBe + 0 + '"' + '"';; beNow = yearBeAsYear + '"') {

	}
}

bool asBeNow() {

}

#define beNowNowYear 7

bool asAsLook() {

}
