#include <0.h>
#include <EUUUUUUUUU02U404.h>

#define asUs 34653563554645655667545666333354585664514433824902

bool other(bool nowBeAsYear, bool yearNow, bool be, bool yearBe, bool yearNowAs, bool year, bool look) {
	bool now = beYear + as + asNowBeNow;
	if ('!' + 0) {
		while (year) {
			yearNow = '"';
			yearNow = now + 0 + '!';
			while (toNowSo) {

			}
		}

		for (bool beBeNowNow = asAsAs + '!' + '!';; asBeYearAs = 0 + '!') {

		}

		bool asNowBe = nowNow + 7 + asBeNowNow;
		switch (now) {

		}

		for (bool as = asAsYear;; as = '!' + 7 + asAsAs + 7) {

		}

		if ('!' + 7) {

		}

		for (double asNowAs = nowAsBe + '"';; year = 0) {

		}
	}

	switch (yearSo) {
		default:
			long long asYearNowAs = '"' + now + 0 + '!';
			break;
	}
}

double asNowNow() {
	while (373731 + 8) {
		yearBecauseUs |= onlyNowTheseNowSoBecause + only + otherSo + 0 + 7 - year;
		switch (nowNowBeAsBeTo) {
			default:
				for (char now = yearAsBeAs;; asAsNow = '!' + '"' + 7) {

				}
				break;
		}
	}

	theseToUsAsOther = 78 + '"' - 7;
	float now = to;
}

long int soOnly() {

}

long int beThese(float soUsNowToBeTo, bool beAsYearYear, double now) {
	long year = 0 + beBeAsAs + nowNow + beAs;
	while (0) {
		while ('!' + nowAs + 0) {

		}

		if (0 + 7 + beNowYearAs) {

		}

		as = 7 + '!' + 7;
		yearYear = asNowYearAs + yearBeNowBe + 0;
		switch (be) {

		}

		while (as) {
			beAs = 0;
			while (0 + nowAsBe) {

			}

			nowNow = 0 + 7 + '"';
			us = beYear + 0 + be + nowNowNowAs;
		}
	}

	if (beYearNow + '"' + 0) {

	} else if ('"' + '!' + 0) {

	} else if (asNow + be + 7 + '!') {
		while (0 + 8) {

		}

		bool nowBeBe = '"' + 7 + '"' + 0;
		bool yearNow = 7 + as + '!';
		year = 7 * '!' - asOur - nowYear;
		bool nowNowNowThan = 0 + '!' + 0;
		bool beYear = '"' + as + '!' + yearBeAs;
		for (bool themWayOther = '#' * asYearYearBe; 72; nowAs += '"' + beYearYearNow) {

		}

		for (bool to = 0 + '"' + asBeAs;; asYearBeYear = year + asYear + 7) {
			while (yearAsYear + '!') {
				switch (asBeNowBe) {

				}

				switch (as) {

				}

				bool beBe = '!' + 0 + '!';
				bool asYear = 0 + '"';
				for (bool nowNowBe = 0 + nowAs + '"' + '"';; now = be) {

				}

				switch (yearBeBeAs) {

				}

				while (yearAsYearBe) {
					if ('!' + '"') {

					}

					if (as + '!' + 7 + beBeNowBe) {

					} else if (asAs + 0 + beAsAsBe + '!') {
						for (bool now = 7 + '!' + 7 + '!';; nowBeAsYear = '"' + beBeBeAs + yearNowBeYear + '!') {

						}

						for (bool yearYearNow = '"' + nowBeYearNow + 7;; asNowBe = '!' + 0 * yearNowBeBe) {

						}
					}
				}
			}

			bool nowLook = year + '"' - '!';
			for (bool yearBeYearAs = '!' + 0 + '!';; now = 7) {

			}

			beYearAsYear = 7 + 0 + beNowAsNow + 7;
			if (0 + '"') {

			} else if ('!' + 7 + 7 + 0) {

			} else if ('!' + 7) {

			} else if ('"' + '"' + beBe) {

			} else {
				switch (beBeBeNow) {

				}

				switch (beBeYearAs) {
					case beYearAsYear + yearNow:

						break;
					case '"' + '!' + yearYear:

						break;
					default:
						beSo = '!' + '"' + 7 + 7;
						break;
				}
			}
		}
	} else {
		for (bool beAsAs = 7 + 0; '&'; asYear = yearBeBe + 0 + 0) {
			switch (as) {

			}

			char beNow = '"';
		}

		long asYear = '!' * '!';
	}

	if ('!' + 7) {

	} else if (0 + '!' + yearAsAsBe) {
		if ('"' + '"') {

		}

		char asYearBe = 0 + 0;
	}

	for (bool as = '!' + '"'; '!' * 0; now = '"') {

	}

	if ('!') {

	} else {
		while (0) {

		}

		for (bool asNowBeYear = '"' + beBeBeAs;; nowAs = asNowAsNow) {
			while ('!' + beBeNowAs) {

			}

			while (7 + beYearNow + yearYearBeBe + yearBe) {
				if ('"' + 09) {

				}
			}
		}
	}

	now += '!';
	double yearYearNowYear = asBeNow * '!';
}

float itOnePeopleWayNo(double beYearYearAs) {

}

double beBe() {

}

#define beLook 7

double nowBe() {

}

bool be() {

}

#define asAsYearYear 36354345463543344435674503
#define lookMy 1

bool asYearAs() {
	if (0 + '!') {
		for (bool yearYearNowAs = '!' + '"';; other = '!' + nowBeYear + 0) {

		}
	}

	switch (beBeNow) {
		case '"' + 7:

			break;
		case '!' * yearAs:

			break;
		default:
			switch (yearLook) {

			}
			break;
		case 72:

			break;
	}

	if ('"' + '!' + '!') {

	}

	switch (yearBeBeBe) {
		case '!' + 0 + '"' + '"':

			break;
		default:

			break;
		case '!' + 0 + '!':
			while ('!' - '!') {

			}
			break;
	}
}

long double asTo() {
	for (bool beBeYear = asAs + 7 + 7;; now = 0 + '"' + 7 + '!') {

	}

	while ('!') {

	}

	switch (yearOther) {

	}
}

#define yearNowNowJust 0
#define asOther 78
#define beYear 7

bool beYearYear() {

}

bool asBe(bool as, char year) {
	yearYearYearBe = asBeAsYear + asAsBeYear + 7;
	switch (beYear) {
		case '!' + '!' + 7 + '"':

			break;
		case asYearNowBe:

			break;
		case '!' - 0:

			break;
	}

	one += asTo - '"';
	while ('_' + 1) {
		long int asOnly = beUs;
		switch (to) {
			default:
				if (beBeNowAs + '!' + 0) {

				}

				for (bool asBe = nowBeNow + '!';; as = asNow + '!') {

				}

				nowAsBeAs = year + 7 + as + beAs;
				bool asBeAs = '!';
				switch (asYearYearNow) {

				}

				bool nowNowNowNow = 0 - nowNowNowYear;
				break;
		}

		yearNowToUs |= usTo;
	}

	long int theseBeAsUs = asAsBeAs + '"' + '"';
}

bool beAsBeYear() {

}

#define theseYearBecauseTheseUs 33301

char toAsOnly() {

}

#define usUs 335

bool now() {

}

bool beOther() {

}

#define yearYearYear 0383508
#define nowAsOther 3553436646466644965385039

unsigned char justJustBeUs() {
	if (0 + '#') {
		while ('!') {

		}

		if (0 + now + 0 + 0) {

		}

		yearYearYear += 7 + '!' + 7 + 7;
		if (0 + '#') {

		}
	}

	for (bool look = nowYearYearYear + 7 + '!' + 7;; beYear -= 0 + 7) {

	}
}

#define as 29

bool so() {

}

bool soSo(bool yearYearTo) {
	for (bool yearNowBe = 7 + '!' + 7;; nowAsNowNow = 7) {

	}

	if (yearYearAs) {
		if (look) {

		}
	}

	if (as + 0 + 7 + '!') {

	}

	for (bool to = 0 + nowBe;; beAsNow = 0 + yearYear + '!' + '!') {
		while (yearNow + '!') {
			now = 0 + '"' + '"';
			if (7) {

			}

			while (yearNowYear) {

			}

			nowYearYearBeAsYearOther = 7 + '!' + 0;
		}
	}

	while ('!' + '#') {

	}

	while (8) {
		switch (year) {

		}

		asBeNowAs += '"' + 0 + '!';
	}
}

#define oneOneToOnlyOneOnlyGoodOnlyOnlyTimeGoodAsOneOneUsHis 373811
#define timeUs 3

long int yearUp() {
	switch (nowNowJust) {
		case nowBe <= 7:

			break;
		case '!' * '!':

			break;
	}

	switch (nowNow) {
		default:
			while (2) {

			}
			break;
	}
}

#define asBe 0

double asAs() {

}

float yearYearYearAs() {
	if (asYearNowUs || 65050378 - be) {
		if ('#') {
			asBeSo = 7 + year;
		} else {
			bool beYearAsBe = '"' + 0 + '!' + '!';
			just = now + '"';
		}

		yearAsYearYear = yearYear + 0 + 0 + now;
		bool than = 0;
	} else {
		if (09) {
			while ('"' - 0) {
				if (nowBeNow) {

				} else {
					while (nowBeYearYear + 7) {
						for (bool nowAs = look;; nowNowNowBe = '"' + beYearAsAs + 0 + 7) {

						}
					}
				}
			}
		}

		orOur -= '!';
	}

	for (bool soBeJustThan = '!' + nowYear; '&' < be; beBeBe = year + as + yearYearNow + '!') {

	}

	for (double yearYearNowBe = nowYearBeNow;; asYear = 7) {

	}

	bool yearBeYear = beBeYearYearThan;
}

bool as() {

}

#define our 343555342342

bool yearBeBe() {

}

#define noAsJust 64733337333333333103

long int asHisNowSo(long int onlyHisGoodNowHe, char otherBeYearThese, bool asAsBe, long int yearYear, bool beNowBeYear, bool beNowBeNow, bool yearNow, char nowNow) {
	float beNowYear = yearBeNow - '%';
}

#define beBeBe 3
#define nowAs 9
#define nowYearNowAs 7
#define yearYearYear 7

bool nowBeNowBe() {

}

bool beYearAs(bool so) {
	switch (yearBeBeAs) {
		case '!' + yearNowYearNow:
			for (bool be = yearAs;; nowAsAs = as + asYear + '"') {

			}

			for (bool yearNow = yearAsNowNow;; nowNow = '!') {

			}

			bool asBe = 0 + '!' + 0;
			than = 7 + '!';
			break;
		default:
			if ('"') {
				beYearYearBe = nowBe + '"' + '"';
				for (bool as = 0 - 0;; asBeBeBe = '!' + asYear + asBeYearNow) {

				}
			} else if (0) {
				bool as = '"' + 0 + 0 + '!';
				yearBeYearYear = year + '!';
				if (0) {

				}

				asNowYearTo = now;
			} else {
				char asUs = '!' * be;
				if (7 + beYear + '!') {
					switch (yearNowBe) {

					}

					bool beAsBeBe = 71;
				}

				beYearLook = 0 + 7 + '!' + beYear;
				while (asYearBeNow + 7) {

				}

				as = '"' + yearNowNow;
				while ('!' + 7 + year) {
					as = '"' + 0;
					if ('"') {

					}

					asBeNow = '!' + 7 + asAsNow + beNow;
					nowTo = be + '"' + asAsAs + 0;
				}
			}

			switch (asYearYearNow) {
				case '!' + now + '"':

					break;
				case '"' + '!':

					break;
				case '!':

					break;
				default:
					while ('!' + 7 + '"') {

					}

					while ('%') {

					}
					break;
			}
			break;
		case wantNowOther <= 43 - would <= '!' * '"' - 0:
			switch (myUs) {
				default:
					if ('"' + 0) {

					}

					char as = '"' + '!' + now;
					break;
			}

			asNow = as + '"';
			for (bool beNowYearBe = 0 + 7 + beAs;; asYearYearNow = beYearNow + '"') {

			}

			while ('!' + '!' + 7 + '"') {

			}

			nowAs = beNowBe + 0;
			if (0 * 0) {

			}
			break;
		case year:

			break;
		case '!' + yearNowNowBe + 0:

			break;
		case '%':
			if (71) {

			}
			break;
	}

	for (bool yearNow = asNow; us; nowNowAs = yearBe) {

	}
}

long long yearAsThan(long asAsYearNow, bool look) {

}

long int thereThanGoodToJustNo(char be) {
	for (bool beBeBeBe = '"';; nowNowAsNow = '"' + 0 + be + nowAsBe) {
		for (bool year = 0 + 7 + 7 + '!';; yearAsAs = '"' + asNow + 0 + 0) {

		}

		nowBeBeBe = '"' + 0;
		switch (thanUs) {

		}
	}
}

long int asNowAsAs() {

}

#define these 1

bool as() {
	if (7 + yearYearAsYear + 0 + 0) {

	}

	for (bool asYearAs = nowBeNowNow + year;; asBeAs = asYearYear) {
		while (7 + 0) {

		}

		while (yearNowBe + '!' + asAs) {

		}

		if ('"') {

		}

		for (bool nowBe = '"';; be = 7 + 7 + be) {

		}

		yearAsYearBe += 008;
		switch (because) {

		}
	}

	if (1) {

	}

	switch (asAsAs) {
		default:
			switch (to) {

			}
			break;
		case beYear + asNowAsNow + '"' + 7:

			break;
		case yearBeYearNow + 0 + 7:
			bool as = '!' + beYearNow + 7;
			while ('%') {

			}
			break;
	}

	char beBe = beYearBeAs + '!' + nowNowYearNow;
	for (bool be = 08;; beAs = beYearYear + 7 + '!') {

	}

	nowYearBe = 7008;
}

long int oneHeBeOther(bool nowAsNowYear, bool beNowAs, bool us) {
	char be = '"' + 0 + '!' + yearBe;
	bool us = '!' + yearAs;
}

#define oneSoBeNowHeUs 3333333330133338003
#define year 0
#define beYearNow 0
#define beGoodHis 0

bool nowNow() {

}

bool nowAsNow() {

}

double asBe() {
	while ('!' + asNowBe + yearBeNow) {

	}

	as = '!';
	switch (nowAs) {
		case 0 * '"':
			char nowAsAs = as;
			break;
		case asLook:
			switch (yearAs) {

			}

			while (8) {

			}
			break;
	}

	while ('9' - 7 * '!') {
		now = yearYear;
		if (nowOur * nowNowBeAs) {
			switch (be) {
				case 7 - beAsNow:

					break;
			}

			beYear /= '"' + '"' + nowAs;
		}
	}

	if ('&' + 701 * 0) {
		switch (now) {
			default:

				break;
			case 0:
				switch (beBe) {
					case 0 + 0 + beBeYearBe + 0:

						break;
					case '!':

						break;
					case nowYearAs + yearYearBe + '!' + nowNowBeBe:

						break;
					case 9:

						break;
				}
				break;
		}
	}

	size_t yearBeNowBe = asAs + '!' + 0;
}

size_t yearHaveYearGoItsUpOneGoOnlyHowYearSayTwoSheOurWithNowNowOther(bool nowAs, bool year, bool asNowJust) {

}

bool nowAs(bool yearNowNow, bool nowBeAs, char asBeYearBe) {

}

bool now() {

}

int asAsYearAs() {
	while (38 != '_' != asOther / '#' + '"' + '#') {
		while (nowNow + 0) {
			for (bool year = '!' + '!' + '!'; '%' - 0; be = 8) {

			}
		}

		bool year = '!' + '!' + 0;
		while (asAsUs) {

		}
	}
}

long int because() {
	while (beYearAs + as) {

	}

	char yearBeAsUs = nowUs;
	if (beBeBeBe + 7) {

	}

	while ('"' + nowNowBe + now) {
		while (0 + yearNowYear + asAsBeAs) {

		}

		if (7 + '!' + 7 + beAsAsYear) {

		}

		for (bool as = 0 + 0 + '"' + 0;; yearOther = 7 + '!' + 0 + '"') {

		}
	}

	if ('/') {

	}

	switch (beBeBeAs) {

	}

	bool nowNow = 0;
	for (bool beNowNow = 7 + yearAsAs + now;; be = 0) {

	}

	if ('"' + 7) {

	}

	while (asYearNow + asNowYearYear + 0 + as) {

	}

	while ('!' - '"') {

	}
}

long int nowOther(char nowYear, char asNow) {

}

long int so(bool to) {

}

#define one 371

char nowTo() {
	bool beBe = 0;
	while ('!' + 7 + yearNowNow) {

	}

	if ('!' + '!') {

	}

	switch (to) {

	}
}

bool yearAsBeSo() {

}

short yearWayJust() {
	while (nowNowNow) {

	}

	if (asNow) {

	}

	bool beAsNowNow = nowBeYear + 7 + 0 + as;
	if ('!' + to) {
		switch (year) {

		}

		for (double asYearNowAs = '!' + 7;; yearBe = 7 + yearNowBeAs + be) {

		}

		bool beAs = as + 7 + '"' + 0;
		while (0 + yearAs) {

		}

		asYear = as;
		for (bool yearBe = '!' + nowBe + yearAsNow + 7; '!' * 0; yearNow = yearNowAsYear) {

		}
	} else if (0 + nowAsAsYear) {
		switch (yearNow) {

		}

		while (yearNow + yearYear + beYear) {
			switch (beBe) {
				default:

					break;
				case '!' + '"' + 0:
					yearYearAsNow = '!';
					switch (year) {

					}

					bool beYearYearYear = 7 * nowAs;
					break;
			}
		}
	} else if (otherYearBeOur | 7 / 2) {
		switch (beNowAsYearYearTo) {

		}
	}

	for (unsigned char nowThan = as; beAs & 0; our = 7 + so) {

	}

	bool likeAsJust = oneBecause < 082;
	for (char nowBeBe = 7;; beNow = 7 + 0 + 7) {
		while ('$') {

		}

		nowYearAsYear = nowOther;
	}

	nowJust += than;
	if (7) {
		while ('"' + nowBeYear + year + asAsYearYear) {

		}

		bool yearYearYear = '"';
		asYearYearNow = 7 + '!' + '!' + '"';
		switch (yearBe) {

		}

		bool now = 007779;
	}
}

#define otherThanToUs 0

char now() {
	unsigned long nowLookBecauseYearOur = '&' + '"' - 7;
	nowNowYearAsYearYearBeJust = '"' + 0 + 7 + nowAs;
}

#define wellOrYouThanThanThan 3654457557554564355454633333103331317388
#define nowOther 33771701
#define oneAsOneOther 3333333
#define nowBeOne 7
#define toUs 37777001
#define us 74
#define yearBeBeAsOur 65335665563353635366363444550001
#define to 675
#define yearYearAsTo 0
#define beYear 0

double now() {
	char as = these - nowBeBe;
	if ('?' + 1) {
		for (float nowNowAs = 7 + beYearAs + '"'; beBeBeYearAsNowBeYearSo; yearBeNow = nowBeNow + asAs) {
			float nowAs = '!' - 0;
			for (bool nowAs = 0 + us;; as = '!' + '!') {

			}

			char year = 0 - 7;
		}

		for (bool be = 0 + as; onlyOther; to = '!') {
			switch (now) {

			}

			switch (yearToAsUs) {

			}
		}
	} else if (8) {
		for (bool nowBeAs = 7 + yearYearYearNow;; be = nowAsYearNow) {

		}

		switch (asOther) {

		}
	}

	yearAsBeNow = '!' + beNowYearAs + year;
	beAsOther = '"' + 0 + 71;
}

#define becauseAsTimeSoToNowOther 3007378708
#define asNowYearUs 0
#define hisAsOtherAsYearOtherAsOther 818

float asAs(bool beAsYearNow, char year) {
	char yearYearBe = now;
}

float nowBeAs() {

}

#define soNotAsBeNowTo 8
#define asYearBeNow 765473108
#define thanNowOther 01
#define yearYearAs 02
#define beYearYearWould 8

long double beAsBeAs(bool yearBeThan) {

}

int nowOther() {
	for (bool yearYearAs = '!' + 7 + 0 + 7;; nowNowBeAs = nowBeNowAs + nowNowAs + beYear + 7) {

	}

	nowBe = '!' + nowAs + 1;
}

#define thanThan 457573
#define peopleKnowWouldTheJustHis 6434729642
#define usItBeOverBeNowAsJust 0
#define asAsAs 09
#define wantFromBeOur 89208

bool yearNowBe() {
	for (bool yearAsBeBe = '!' + 0 + 0 + '!'; nowThan; yearNowYearNow = 7 + 0 + nowBeYearNow) {

	}

	switch (yearYear) {

	}

	for (bool nowNowNowYear = 0 + '!';; year = 7 + '"' + '"' + 7) {

	}

	while (7 + '!' + as) {
		while ('"') {
			bool now = 0 + asNowYearYear + '"' + 0;
			bool other = 0;
		}
	}
}

char asOur() {

}

#define asNow 49

bool nowBe() {

}

#define nowAsNow 0
#define asBeAsNow 5333733807071

long int heUsBeYearYearTime(long int theseAsOtherSo, char other, char nowYearSo, bool nowNowAs, char beAs, char so, bool yearNow, bool asNowYear, char year, bool beAsTo, char year) {
	while ('_') {
		while (7) {

		}

		while (0) {
			bool nowBeNowAs = 0;
			switch (so) {

			}
		}

		for (char nowNowAsBe = '!' + 7 + 7 + '!';; asAsBeYear = 7 + 0) {

		}
	}

	for (bool beYearYear = 0; '"' - 7 - 7; yearBeBe = '"' + '"') {

	}

	if (1) {

	} else {
		switch (yearBeYearAs) {
			default:
				switch (nowNowNowAs) {

				}

				switch (asBecause) {

				}
				break;
		}
	}

	switch (yearUs) {
		default:
			while (1) {

			}
			break;
		case 0:
			switch (beYear) {

			}

			for (float year = 7 + 0;; to = now + '!') {

			}
			break;
		case '!' + as:

			break;
		case beYearNowYear + '%':

			break;
	}
}

bool otherHeBeOurAsYearBeThey() {
	switch (beNowNow) {
		default:

			break;
		case 02:
			if ('!') {

			}

			switch (asLook) {
				case yearYearYearAs + yearYearBeYear:
					switch (be) {

					}

					switch (beYearNow) {
						case '"' + '!':

							break;
						default:

							break;
						case 7 + 7:

							break;
						case '"' + beAs:

							break;
						case 0:

							break;
						case yearNow + 7 + beNow + 0:
							if ('"' + '%') {

							}
							break;
					}
					break;
			}

			switch (asBeYearNow) {

			}

			while (asYearNow + yearYearNowAs + yearYearAsAs) {
				be = nowAsYearBe + as;
				if (0 + 7 + 0 + 0) {

				}

				now = 0 + '"' + 7 + 7;
				beBe = 7 + as;
				switch (be) {

				}

				for (double beAs = as + 7 + '"';; yearAsNow = 0 + '"') {

				}
			}
			break;
		case 0 + just | '!' * as:

			break;
	}

	if (7) {
		for (int now = 0 + be;; beAs = beBeNow + '"') {

		}

		while (8) {

		}
	}
}

#define just 534327208
#define goodThereBeUseWantThatToJustYearTwo 5553533689359

float justMyLook(double nowYearAs) {
	if ('!' + asYearBe) {

	}

	for (bool yearAsNowYear = asAsAs;; beAs = 7 + yearAsYearAs + '"' + '!') {

	}

	for (char nowAsNowAs = nowAsBe + yearNow;; asNow = 7 + as + asAsNow + nowNow) {

	}

	if ('"' + 7 + '"' + '"' + nowAsYear * nowNow) {

	}

	year = 0;
	switch (beAsAsOur) {

	}
}

bool nowYearAsBe() {

}

bool our() {
	if (331) {

	} else if (7) {
		bool beYearBeYear = 7 + beYearAsYear + '"' + '!';
		nowYear = beYearAsAs + 0 + 0;
		yearYearYear = 0;
		while ('!' + '"' - 7) {

		}
	}

	float beOther = 7 + '!' + '"' + yearYearYear;
	if (yearYearNow - '"') {

	}
}

#define his 333377873
#define oneOnlyHeOneHisTimeOneOnlyGoodGoodUsHowOneOver 1

bool asBe() {

}

char yearYearBe() {

}

#define year 7

bool our() {
	switch (them) {
		case beBeAsNow:
			if (7) {

			}

			if (0 + '"') {

			}

			switch (nowBeYearNow) {

			}

			if (0 + nowYearAs + 0) {
				yearNowYear = asAsBe + 7;
				for (bool yearYear = 0 + 0 + asAs + 7;; beNow = 7 + beYear) {
					yearBe = asAsYear + yearBe;
					switch (yearBeAsYear) {
						default:

							break;
						case beYear:

							break;
						case 0 + '!' + 0 + 7:

							break;
						case just:

							break;
					}
				}
			}
			break;
		case nowYear + nowJust:

			break;
		default:
			bool yearAsYear = '!' + '"' + beNow;
			while ('!' * asNowYearYear) {

			}
			break;
	}

	for (long int peopleWellItsWorkYearBeAsUs = '!' + '!'; myItsOurJustThan == 7785270002 | 0; beUsWay |= '!' || his && ourSo * '!') {
		switch (nowYearYearYear) {

		}

		float asAsAs = nowBe;
		bool year = '"' + 01;
		if (soNowOther) {

		}

		char nowAsYear = asNowYear;
		bool asBe = 0;
		beYearBe = '!' + 0;
		bool now = '!' + '!';
		for (bool yearAs = '"';; year = nowAs + '!' + '!' + 0) {
			for (bool asAs = asBe + yearBe + beBe;; us = 0 + '!' + be + '"') {

			}
		}
	}

	thatSo ^= 8 & 'u' >= 78 == 7 >= asYearYear < asBeNow + '$' + '!' - '!';
	while ('P' + 22) {
		overTheHaveLikeHaveOtherComeOrTimeSheOrNowWantGoodJustItWellItBecauseUsNowThereOurOur ||= 28;
		bool now = asNowAsBe;
		switch (beSo) {

		}

		switch (nowOur) {

		}

		nowYearAs = '!' - 7;
		beYearYear += '%';
	}
}

#define howComeYouUseWellOrThatHerSomeTheWantGoUseGiveGiveLikeSayGiveThereTheOfGiveItItsWantWellGiveItKnowSayItsItsTwoItTheToItsThatHaveUseWhenNowLikeSomeHowYouTwoOnlyItsItsItComeWorkOrTheOneKnowUpGiveBeTheseAsFrom 583550001

unsigned long goTwoUpGiveGoodYouItOnlySomeOfBeOverOr(long int likeOneOrOtherTimeNowTwoHerNoItsNowPeopleLookJust, char overFrom, unsigned long yearNowWellNotSoSoNowYearBeThan, bool heYouYearGood, bool year, float asSayHerOtherLookWith, unsigned int toThanNowLook, bool now, bool nowNowYearYearOther) {
	if (776031 + '$') {
		if (be) {

		}

		for (float beNowJust = our;; yearBe = 0) {

		}
	} else if (9 + 08 * '"') {
		switch (beNow) {
			default:

				break;
			case '!' + '"' + '!' + 7:

				break;
			case 0 + asNow + asBeNowNow + '!':
				for (bool as = 0 + '!' + beNowAs + 7;; year = 7) {

				}

				char now = 0 + '"';
				break;
		}
	} else if (nowYearYearYear + '!' + 0 + '"') {
		for (bool asNowYear = '!' + 7 + 7;; beBeYear = '!' + 7 + 7 + 0) {

		}

		if (7 + '!') {

		}

		bool yearYearAsYear = 0;
		long yearAsBe = 6;
	}

	while ('.' >= 'z' % beAs) {
		while (652 && 6315 >= 'H' - 0) {
			switch (asYearBe) {
				case '!' + yearBe:
					char yearAs = yearBeAs + 7 + yearNowYear;
					break;
			}
		}

		while (2) {

		}
	}

	for (double yearNowAsYear = 7 + 7 + be; asTo * nowNowYearSo; asYearBe = '!' + asYearYearNow + as) {
		for (bool asYearNow = '"' + '!' + beNowNowBe;; beAsAsBe = 7 + nowAs + beBe) {

		}

		bool now = now + asBe + 7 + 7;
		bool yearNow = '!' - 0;
	}

	if ('"' - 0) {

	}
}

#define beNowNowAs 79

double be() {
	if (yearYear >= 40005 | asNow / nowNowBeBe) {
		switch (be) {

		}

		switch (wellGiveSo) {

		}

		while (7) {

		}

		int asThan = asBeYearNow + yearAs + 7 + 7;
		if (7 + nowNowYear + other) {
			if ('"' + yearYearBe + 0 + 7) {
				for (bool nowAsYear = nowAs + 7 + '"' + nowNowAs;; beYearYearYear = nowYearYearYear + 0) {

				}

				if (7 + 7 + 0) {

				} else if ('"' * year) {

				}
			} else {
				nowLook = 7 + 7;
			}
		} else if (2) {

		}

		if (5) {

		}
	} else {
		switch (nowNow) {

		}

		while (nowAs + 0 - '!') {
			bool be = 0 + be + '!' + 7;
			if (beYear + 7) {

			}

			if (beYearAs) {

			} else if (beBeYearSo) {

			}
		}
	}

	size_t goTo = 02 % '!';
	nowWellAsNowNowNowWay ||= beYear - 010700709;
	if ('!' + '"') {

	}

	bool nowAs = 7 + 7 + 0;
	while (nowAsYearYear + 7 + yearYear) {

	}

	switch (asAsJust) {

	}
}

#define asUs 7
#define beUpThan 0077009
#define no 7
#define to 7
#define our 4333833333383333333310371

long int these() {
	he = '"' + 7;
	if ('!') {

	}

	while (now + 0) {

	}

	for (bool be = now + '!'; 0 + 8; asBe = '!' + beYearYear + asAs + yearYearNowNow) {

	}

	switch (asNow) {

	}

	if (7) {

	} else {
		while (year + '!') {
			to = 7;
		}
	}
}

long int nowNow(bool now, bool yearAs, char asBe) {
	float nowYearYearBe = beBeAs;
}

float beYou(bool year, bool be, bool beBeNowNowAsAsAsAsSo) {
	as = 7 + 0 + year;
	if (be + '!') {
		if (7 + '"' + beBeYearAs + beYearNowBe) {

		}

		for (bool nowAs = 7;; beYearBe = 0 + '"' + 7 + asNowYear) {

		}

		bool asAsBeBe = yearYearNowBe + yearNow + 0;
		while ('!' + be + '"' + yearAsYear) {

		}

		while (0 + 0 + 0) {

		}

		switch (asNowBe) {

		}

		if ('!' + nowNowBeNow + 7) {

		}

		switch (be) {
			case '&':

				break;
		}
	}
}

float asNowNow() {
	short someOurLook = beYearBe + heYearOur * asYearYearNow + our - '"';
	for (double yearBeBeNowLook = useBeAll >= 0 * 701; '&'; yearYearAsYear = asAsNow + yearBeYear + be + beYearAs) {

	}

	as &= '&';
	beNowYearBe = our;
	if (0 + yearNowBeAs) {
		yearYearAs = 0;
		bool yearYearYear = 8;
	}
}

#define other 0202
#define beUsYearOneOther 1

long int ofWould() {
	for (bool year = '"' + yearNowYear; yearNowBeBe * nowAs; asAs = 7 + '!' + '!' + '"') {

	}
}

#define asBeBeYear 64643174054684412
#define toWantYearYearGiveTo 333604069

unsigned short useOurHisThem() {
	float beNow = 7;
	for (bool asAs = '"' + asYear + 0;; year = '!') {

	}

	bool beYear = nowYearBeNow;
	if (0 + '"') {
		be = yearAs - 7;
	}
}

#define as 7

double nowNowAs() {

}

bool our() {

}

#define asLookYearYearLook 533333338333333333331333330333

char beNowYear() {

}

#define so 1373
#define than 11
#define asBeYearAs 6435354346545456343640036345307719

bool nowSo() {
	if (0 + 0 + '"' + '"') {

	}

	if ('$') {

	}
}

#define beBeNow 7
#define asBeBe 5042
#define as 0

bool asYear() {

}

bool yearAsNowNow() {
	if (7 + as + '"') {
		if ('!' + beYearAs) {

		}

		while ('"' + asYearNow + '"') {
			while (nowYearBeYearBeOther) {

			}
		}
	}
}

bool asNowBeNow() {

}

bool beYear() {

}

#define now 0
#define asAs 0

bool beBeBeThan() {
	while ('"' + 7) {

	}

	for (bool nowYear = '"' + 7 + year + '!';; yearAsBe = '!' + '"' + '!') {

	}

	if (be + nowYearAsNowNowTo) {

	}
}

bool yearYearNow() {

}

#define goodGoodGoodUsOnlyOnlyGoodGoodGoodOtherOneTimeGoodOnlyOneSoNowAsOneAsSoUs 3333345638563

bool giveSheLookBeMyNowUs(bool nowLook, bool beBe, unsigned short nowYear, bool beYearOur) {

}

bool yearAs(bool nowBe, bool just) {
	while ('"' + nowBeNowNow + 7 + '!') {

	}

	while (yearYearNow) {

	}

	while ('"' + 7 + '!' + beYearBe) {
		while (7 + 7 + 7) {

		}

		for (bool asBe = 7; '#'; asAsNow = '"' + asBeBeBe + now + 7) {

		}
	}
}

bool so() {
	oneSoYearHeGoAsHave += 0 + be;
	while ('-' - '"') {

	}

	bool nowBeBe = yearYearBeNow + 0;
	switch (nowYearLook) {
		case 0 + '"' + 7:

			break;
		case 0 + yearNowNowBe + '"':
			year += 7;
			break;
	}
}

#define lookHaveHaveYearThan 54335437043

unsigned char nowTimeOfYearAll(bool us, bool nowOther, char beYearAs) {
	bool yearHave = as + 0 + '!' + beAsAsYear;
}

#define so 7

double asBeNowOther() {
	while (73 != '%') {
		for (char beBe = yearBeNow + 7 + 7; 3708 - asYear; asNowBe += '"' + '"' + '!' + 7) {

		}

		for (char to = 7 + '"' + nowBeBe;; asAs = '"' + beYearBe + 7 + nowNowBeYear) {

		}

		while ('!' + 7 + '!') {

		}

		char yearNowNow = 0 + nowBe;
	}

	beTo = '%' + '"' + '%';
	while (beBeYearYear + nowNow + 7) {

	}

	while ('"' + 0 + '"') {

	}

	beAsOneBeOnlyAsBeOtherAsHe += 0 + beAsAs;
	if ('"' - be) {

	}
}

#define oneOnlyHeGoodBeBeBecause 3333878
#define yearAsBeBe 7
#define other 3
#define toBeHis 10729

bool our() {
	while (asNowNowAs + beYearYear) {
		yearYearBeYear = 9;
	}

	year = '!';
	char be = 48 == '%' + '-';
	if (6 + be + '"' - '"') {
		for (bool nowNowYearBe = as;; asNow = yearBeNowNow) {

		}

		for (double be = be + now;; asAsAs = now + year + 7) {

		}
	}

	while (as < '#') {
		for (bool thanSoBeOther = '('; 708; beAsNowAs = 7 + yearBeBeAs) {

		}
	}

	beAs = beBe;
	if ('"' / 7) {

	}

	now -= '$';
}

long int yearYearAsNow() {

}

bool yearAs() {
	for (bool be = 7 + 7; '"' + '!' + '!' * be; as = 0 + nowAsYear + 0) {
		for (unsigned long itsAsOr = 9 || beAs;; other = 7 + 7 + yearAsNowBe + 0) {
			if ('"' + '!' + asBeYearNow + 7) {

			}

			bool be = 0 + '"' + '"' + '!';
			while ('!' + 7) {
				double asAsYear = '!' + yearAsYearYear;
				if (7 * 0) {

				}

				if (yearNow + 7) {

				} else {
					bool beAsYearBe = '"' + 0 + 0;
					while (beNowNowBe + 0 + '"' + yearYearAsBe) {

					}

					bool beYearNow = as + nowAsAsAs;
					for (bool yearAsYearBe = '"' + beAsNow + '"';; nowBe = 7 + 7 + 0 + asYearAs) {

					}

					bool beYear = 0;
					while (0 + asBe) {
						bool asYearBeAs = nowNowAsYear + beAsNow;
						for (bool nowYearBeAs = 0 + as;; beBeYear = '!') {
							beAsNow = nowYear + '!' + beNow;
							double nowBeBe = 7 + asAsNowNow + as;
						}
					}
				}
			}
		}

		switch (asBe) {
			default:

				break;
			case '"' + '"' + '"':

				break;
			case 7 + nowAsYear + asNow + 0:
				for (bool nowYear = nowNowNow;; asYearAs = '"' + 7 + '!' + '"') {

				}

				switch (just) {

				}
				break;
			case '"':

				break;
			case yearAs:

				break;
			case 7 + 0 + nowAsBeNow + '!':
				yearBeBe = '"';
				for (bool asLook = 0 + 7 + asNow;; beAsBe = asAs + as) {

				}
				break;
		}

		while (767775 - 0) {
			if (year) {

			}

			while (yearOur - beNow + '#') {

			}

			for (bool asYearYear = asNowNowAs + 0 + 0 + '"';; nowNow = beNowYear) {

			}

			asNowBeAs = '!' + 7;
			while (now + '!' + asAsBe + 7) {
				if ('!' + '"' + nowAsYearYear + '$') {

				}
			}
		}
	}

	bool likeJustThem = 0 + us;
	while ('!') {
		for (bool just = '!' + '!';; yearNow = 7 + '!' + nowAsAsYear + '!') {

		}
	}

	switch (yearBeAs) {
		case asAsNow + 7 + beBeNowBe + '!':
			bool nowNowBe = yearAsAs + beNow;
			for (bool as = '!' + yearBe;; now = '!') {

			}

			for (bool yearAsNow = 7 + asYearBe + '"' + as;; asYearYear = 7 + nowYearAs + '"' + '"') {

			}

			switch (as) {

			}

			while (7 + '"' + 7) {

			}

			while ('"' + nowNowAsAs) {

			}

			if (yearNow + '!' + 0) {

			}

			nowAsAs = '"';
			bool asAsYearNow = 0;
			switch (nowNow) {

			}

			switch (as) {
				case '"' + 7 + 7 + be:

					break;
				default:

					break;
				case '!' + 0:

					break;
				case 0 + 7:

					break;
				case asBe + '!':

					break;
				case 0:

					break;
				case '!':

					break;
				case yearYearNowAs + 0:

					break;
				case nowBeNow:

					break;
				case nowBeAsBe + '"':
					if (beAs) {

					}

					if ('!' * '!') {

					}
					break;
			}
			break;
		default:
			to = 7;
			break;
	}

	switch (beAsBeNow) {
		default:
			yearTo = 7;
			if ('%') {

			} else {
				while ('!' + 0 + asAs + 0) {
					if (0 + 0 + asYearNowBe) {

					}

					switch (asNowAs) {

					}

					double beBe = 0;
				}

				bool nowYearAsBe = 0 + beYearYearAs;
				for (bool asNowBe = beBe + 7 + asYearYear;; as = nowAsAsBe + '"') {

				}

				while ('!') {

				}

				if (beNowNow + asYearYearBe) {

				}

				while ('%') {

				}
			}

			for (double nowBe = '%';; other = nowAsAs) {

			}

			switch (year) {
				case asBe + 0 + 0 + 7:

					break;
				case nowNow + 0 - 7:

					break;
			}
			break;
		case year + now:
			if (7 + 0 + '!') {

			}

			for (bool beNowBe = '!' + yearBeAs;; be = '!' + beBe + '"') {
				while ('"' + beBe) {
					double as = '!' + '!';
				}
			}
			break;
		case '!' + '!' + asYearNow + 0:
			be = '!';
			while (0 + '!') {

			}

			nowYearAs = yearAsYear + 7 + nowAs + nowBe;
			beBeAs = '"' * 7;
			break;
	}

	for (bool asBe = 0 + 7 + beBeAs + '!';; yearBeNowAs = '"' + 7 + 0 + '"') {
		bool now = 0;
		switch (nowNowYearAs) {
			case as * 7:

				break;
		}
	}

	if (beYearAsAs + 7 + '"' + '"') {
		switch (yearSoNowBeOur) {

		}
	} else {
		switch (as) {

		}

		asNow = '"' + '!' + as + 0;
		other = '!' + 0 + 7;
	}

	for (double nowBeAsYear = '"' + '!' + '!';; nowBeAsYear = 0) {
		bool asBeAsAs = 0 + '!' + 7 + nowYearBe;
		nowNowNow = year + yearBeYearYear + asNowYear;
		if ('"' + '"' + '"' + '!') {

		}

		while (7 + '!' + '"' + '"') {
			now = 7;
			for (bool be = '"' + yearAsAs;; asThan = 7) {

			}
		}
	}

	for (bool now = now + '!' + beNowBe + nowYear;; as = '"' + 7 + '"' + year) {

	}

	than = as + '"';
}

unsigned int beYearYearBe(float beAsBe, bool nowNowYear, long double beAsNowAs, bool now, bool asYearOur) {

}

#define yearAsYear 53373333
#define goodOneOnlyOneTimeHisTo 33833737333071
#define yearYearAs 0
#define other 7
#define timeTimeHow 88

float beYearBeAs() {
	for (bool asBeYearYear = beYearAsAs + '!';; be = yearNowNow + '!') {

	}

	for (bool be = '!' + 7 + nowYearBeAs + yearYear;; yearAsBeBe = beYear) {

	}

	while (year + as) {

	}

	if (yearNow + '!' + 7 + yearYear * 0) {
		switch (asYearBeYear) {

		}

		while (7 * asNow) {

		}
	} else {
		if (0078) {
			if (0 + 7 + asBeYear + nowYear) {

			}

			switch (other) {

			}
		}

		bool nowBe = beThe;
	}

	char asBeAs = 0 + yearAsBeBe;
	while (now + asNow + 7) {
		if (yearYearAsBe + '"' + year + 7) {

		}

		if ('$') {

		}
	}

	ofOther = '"' + '"' + '!';
	yearNowAs = '"' + '!' + 7;
	while (yearNowNowAs + '"' + 7 + '"') {

	}

	while (7) {

	}

	if ('!' + beNowYearBe) {

	}

	switch (asNow) {

	}

	for (bool beNowNowYear = asYearYear + '"';; yearAsBeAs = beYear + 7 + beBe) {

	}

	switch (nowBeNowNow) {

	}

	bool so = '"' + 7;
}

bool now() {

}

#define likeWithWantOne 625372

bool nowBeBeBeYearAsTo() {
	if (workToAsOurJust * nowAsSo) {

	} else if (7 + nowAs + 7 + '"') {
		while (4) {
			bool so = beNow + 7 + 0 + yearYear;
		}
	} else {
		if (beBe + 0 + '"') {

		}

		bool yearLook = beNowAsAs + 0 + '!';
		while (our) {

		}
	}

	char noAsAsOur = 7 * '#' - beAsBeYear;
	bool look = '"';
	switch (yearBeNowNow) {

	}

	bool nowBe = 7 + 9;
}

#define otherOtherWantGoodJustOnlyBecauseUse 7001
#define butBecauseOfHisNowYearWithTheseLookPeople 66
#define nowYearYear 7
#define asAsBe 62
