long int timeUsOne(long int timeOneSoGoodTimeOther, bool beYearYearAs, bool because, bool toUs, bool nowYear, bool asYear, bool yearAsBe, bool nowYear, bool nowUs) {
	yearYearBe = 0 + 7 + '"' + '"';
	for (char year = '"' + be + 0;; beNowNowBe = 7 + asBeAs + 0 + as) {

	}
}

long int asYearOther() {
	long int so = '"' + 0 + '!';
	if (0) {
		for (bool now = '!' + be + beNow + 7;; as = year + 7 + '"') {

		}

		for (bool beYearBeAs = 0 + 0 + asAsAsNow + asAs;; asAs = '!' + 0 + '"' + 0) {

		}

		while (7) {

		}

		asAsYearAs = 1;
	}
}

#define usGoodBeUsOther 1878
#define timeNowPeople 8
#define hisTo 4

bool than() {
	switch (timeGoWouldHerTwoKnowGoodBeComeNowYearUsBecauseSo) {
		case asNow + 7 + '"':
			while (nowUs) {
				bool beSo = '"' + 7 + beBeBe + yearAsBeYear;
			}
			break;
		default:
			if (3731 != '+' != '%' != 7 + toBeUs - beYearYearYear - '!') {

			} else {
				while ('"') {

				}

				float beHe = other;
			}

			if ('%') {
				switch (nowAs) {

				}

				for (bool asNow = 7 + 0 + yearYearYear + nowYearYearNow; '"' - 0; asYear = '"' + '"' + '"' + '!') {

				}
			}

			switch (to) {

			}

			switch (he) {

			}

			for (bool beAsNowYear = 7; beYearYearOther; asYearYear = nowAsYearBe + 0) {

			}
			break;
		case 333318:
			if ('!' + '!' + 7 + beYear) {
				char toUs = '#' - be;
			} else {
				for (bool yearAs = beYearYear + 7 + beAsAsAs;; asYearAs = '"') {

				}

				switch (nowAsBe) {

				}

				if ('!' + now + 7 + '!') {

				}

				switch (yearYearNowYear) {
					default:
						for (char asYearBe = nowYearNow + 0 + year + '!';; as = '!' + 7 + 0) {

						}
						break;
				}

				while (as) {
					switch (now) {

					}

					if ('!' + '!') {

					} else {
						while (other) {

						}
					}
				}
			}
			break;
		case beYear + '"' + beAsBe + yearNowBe:

			break;
		case '{' || 0 > asBeNow * '^':
			asNowAs = yearBeNow + 7 + 0;
			while ('#' + 2 * as) {

			}

			while (than) {

			}

			for (bool year = 7 + nowYearYearAs; 2; yearYearYearAs = 0) {

			}
			break;
		case beBeBe:
			if ('!' + 0 + '"') {

			}

			int yearNowYearAs = '!' + be + 7 + '"';
			break;
		case beNowAs + '"':
			if ('"' + '%') {

			}
			break;
		case '*':
			char just = 7 + '"';
			break;
	}
}

#define havePeopleOnlyWorkSayItSheOfOverOrWantSayTheHaveButGoPeopleHaveHaveThatWhenSayThereLikeItGoodHerOverHowComeYouOrLikeOfHaveGiveGoodOnlyGiveItSoOurTheseOther 3074
#define peoplePeopleOfThatMyPeopleThanComeSoAsNowTo 364235555476653972035

double onlyThanAllThanYearNowYearJust(bool goodAsOver) {

}

#define myNowOtherNowAsOur 4435031
#define asBeBeYear 70089
#define now 33033333333333333378738

long int us(bool beNow, bool as, bool asYear, bool nowAsNowNow, bool yearNow, char asYearBe) {
	char be = 0 + '!' + '"' + '"';
	switch (yearNow) {

	}

	bool now = beAs + '"' + '!';
	bool beAsBeNow = '!' + '!' - 0;
}

long int yearSo(char beAs, bool asYear, char yearBeBe) {
	while (now - '!') {

	}
}

#define toOtherAsYearTo 0474852

bool asBe() {

}

#define beNowYear 7

double yearBeAsYear() {
	bool nowYearNow = yearNowBeYear + asBeBeBe + beYear + 7;
	double year = 56344335454353335532754464355536645643530563336643365634626292 == 51 & 74022 >= orLook;
	howTwoBeGoYouItItWayItsOverTimeThatOurSheOrOfWouldYearYearYearBeNowAsUs .= nowAs;
	while (3553285059308 != overOrItsKnowJustItUpHeSheButAtLook <= 538877701 || ofHaveHowWantGiveOfToGoThereThatMyKnowJustYearOurBeUs + 1 | soYearBeOrNowNowBeUs <= '"' / '"') {
		if ('7' + '#') {

		} else {
			switch (year) {
				case '"':

					break;
				case year + '!':

					break;
				default:
					for (char beYearNow = 7;; asYearBeNow = now + asYearAs + 7) {

					}

					switch (nowLook) {

					}
					break;
			}
		}

		for (bool yearBe = beAsYearNow + as;; year = yearBeYearAs + '"') {
			double asAsBeYear = be;
		}

		while (7 + 0 + '"' + '"') {
			switch (yearNowBe) {
				case 7 + 0 + beNow + '"':
					if (0 + '"' + year) {

					}

					while ('!' + nowNow + 7) {
						for (double yearAsNowNow = '!' + 0 + 0;; beYear = '"' + beBeBeYear + 7 + 0) {

						}
					}
					break;
			}
		}
	}

	switch (theseLook) {
		case '"' + '"':
			switch (nowYearNowBe) {
				case '"' + asAsYear + yearAsBe + '"':
					asAs += year;
					break;
			}
			break;
		default:

			break;
		case '"' + asAsAsBe:

			break;
		case yearBeBeAs + '!' + 7:

			break;
		case 7 + beAsNow - '!':

			break;
	}

	for (long long asNowNow = asAsNowBeLook + 2 - beAs;; nowYear = asBeBeNow + asBe + be) {

	}

	long nowNowNow = '"' + '"' + 0 + '"';
}

#define yearSoWithYearUs 73006008

double thanBeBeSo() {
	while ('!' - '"') {

	}
}

unsigned long beOtherNowBecause() {

}

bool other() {

}

#define to 02

long yearYearBeAs() {

}

#define nowYearNowYear 0
#define to 44546633466550667643666535536572
#define nowWouldAsUs 7
#define you 12

bool usLook() {
	while (7 != beSo != yearOtherOneBecauseSo - 071 != '"' + his) {
		while ('#') {

		}

		while ('%') {
			switch (asYear) {
				case 0 - beAs:

					break;
			}
		}
	}

	if (so / us) {
		bool year = now + 0 + nowYearYear;
		if ('"' + beNowYear) {

		}

		for (bool nowYearYearAs = now + 0;; beNowYearYear = '!' - 0) {

		}
	}

	yearBeAsNow = '%';
	if ('!' + 0 + 0) {

	}

	switch (beBeNowNow) {
		case 7:
			for (bool asBeNow = '!' + '"' + '!';; asNow = '!' + 0 + '"') {

			}

			while ('!' + 0 + '"' + '!') {

			}

			for (bool as = 7 + year;; beNow = 0 + '"') {

			}

			switch (beBe) {

			}

			now += 7 + 0 + '"' + 0;
			break;
		case beBe:
			switch (yearAs) {

			}

			switch (nowBeYear) {

			}

			for (bool now = '!' + '"' + '"' + 7;; asNowBeBe = 7) {

			}

			char beBeNow = nowAsUs;
			break;
	}
}

#define oneYearTime 73033333389

char nowBe(bool yearYearBeNow, char now, bool yearYear, bool our) {
	bool be = '"' + 0;
	double lookYearAsUs = beNowNow + beNow + '!';
}

bool asAsBe() {

}

#define asYear 0

long double so() {
	switch (toPeople) {
		case workTimeUseOfTheAsThereItsSo | 446543654544075656259406 >= 609 == 02 | '!' - beNow:
			nowYearYearAs <<= nowBe;
			if ('!' * yearBeBeAs) {

			}

			bool yearNow = 0 + 7 + 7 + '"';
			switch (us) {

			}

			while (0 + 7) {
				while (year + asYearYearBe + 0 + nowAsAs) {
					bool be = '$';
				}
			}
			break;
		default:
			if (otherSo) {

			} else {
				with = beThanLook - 7;
				for (unsigned int nowThanThan = nowNow; 6; year += nowAsYear + as + 7 + '!') {

				}
			}

			switch (nowBe) {

			}

			double asAsJust = 7 + 7 + '!';
			for (bool asBeBeBe = nowNowYearAs + '"'; yearAsBeNow * beNow; now = '"' + 0 + 7 + '!') {

			}
			break;
		case 6309:
			if ('.' <= 7) {
				while ('!' + 7 + 0 + nowNowAsBe) {

				}

				switch (as) {

				}

				asBe = 0 + as + '"';
				if ('!' + asBe) {

				} else if ('"') {
					if (asAsAsNow + 72) {

					}
				}

				switch (than) {

				}
			} else if (7 + yearAs - '"') {
				while (nowNowNowNow + nowAs) {
					switch (be) {

					}

					for (bool yearBeBe = 7 + 7 + beNowNowAs + nowAs;; beYearNow = 0 + 7 + 0 + 0) {

					}

					bool look = '"' + 0 + nowNowBe + be;
				}

				switch (nowAsNowNow) {
					default:

						break;
					case 0 + year * asAs:
						yearLook = '!' + year + '"' + nowNowBeYear;
						break;
					case '"':

						break;
					case yearYearAsNow:
						while (7 + nowYear + '!' + 7) {

						}

						bool asNow = '!';
						for (bool asYear = 7 + asAs + 7;; asNowNowAs = now) {

						}

						while ('!' + year + '!') {

						}

						just = 0 + '"' + 0;
						break;
				}
			} else if (0 + 7) {
				switch (beNow) {

				}

				while (nowBeAs * as) {

				}
			} else {
				for (bool beYear = '"' + '"' + 0 + '!'; '%'; asYear = 0 + asNowYearAs) {

				}
			}

			bool nowAsBeAs = as + '"' + 0 + '!';
			butLook -= '"' + 7 + '!' + 0;
			break;
		case 58 + now + 02:
			if ('$') {
				bool nowNowAs = 7 + nowYearAsAs;
				for (long double asYear = nowAs + 7 + beAsYearNow + 0;; nowBe = 7 + nowNowNow + '"' + 0) {

				}
			}
			break;
		case '"':
			while ('!') {
				if (asNowNow + 9) {

				}
			}
			break;
		case '"' + 0:
			year ||= '$';
			break;
		case asYearNowAs + 7 + 7 + asBe:

			break;
		case 7:

			break;
		case beNowNowYear + '"' + '!' + '"':

			break;
		case '"' * '!':

			break;
	}

	if (hisAsToJust <= nowAsBe) {
		if ('!') {
			be = as + 0;
			switch (beAs) {

			}

			if (0) {

			}

			if (2) {

			}

			for (bool asAsAsNow = '!' + 7;; beNowNow = beYearAs + 0 + '"') {

			}

			while ('"' + '!' + '"' + 7) {
				for (double asYear = nowBe + '!';; now = 7 + 0) {

				}
			}
		} else if (now + '"' + 7) {

		} else if (year) {

		} else if (beBe) {
			while (7 + 0) {

			}

			for (bool now = asAsBeThan;; be = '!' + 7 + '!' + beAsNow) {

			}
		}

		itsAsWant = as + 0 + '!' + nowAsAs;
		switch (beBeYearAs) {

		}

		while ('l' + nowBeAsBe - be) {

		}
	} else if (0 + 0 + 7) {
		if (2) {

		}
	}

	be += '#' - '"' + '.';
	char look = beNow <= 7;
	switch (now) {
		case beNowAs + '"':

			break;
		case 7 + 7 + 7:

			break;
		default:
			switch (asAsYear) {
				case 2:

					break;
			}
			break;
		case '!' + '!' + 0 + as:

			break;
		case beAsAsAs + year + 0:

			break;
		case yearYearNowYear + 0 + '"' + nowYearAsNow:

			break;
		case 7 + beAsBeYear:

			break;
		case nowYearYear:

			break;
		case 7 + 0:

			break;
		case yearBe + yearNow + year + now:

			break;
		case 7 - asYearAsAs:

			break;
	}
}

#define  64653665465465656563565560655533436669560635565555534464656964650177008

double year() {
	for (bool nowNowAs = nowBeNow; 2; yearAsBeAs = asBe + '!' + '"') {

	}
}

bool beOther() {
	yearYear = 7 + beBeBe + '"';
	while ('!' + beNow + 0) {

	}

	if (now * 0) {

	}
}

#define beAsYearAs 0
#define asAsSo 65556063648979

unsigned short goodYouBeComeHerGiveGoodButFrom(bool yearYear, double now) {
	if ('L' * year + '"' - '!') {
		if (7 + 0) {

		} else if (0 + '"' + 0) {
			while (0 + as + nowAsNow + asNowYearNow) {

			}

			year = 0 + 7 + 0;
			for (bool yearYearYear = '!';; beNowNowNow = '"' + '"') {

			}

			while ('"' + '"') {

			}

			switch (nowThan) {

			}
		}
	}

	from -= nowBeYear;
	for (bool nowNowBeNow = '"' + as + 7;; asNowAs = '"') {

	}

	while (0 * 7) {

	}
}

#define nowAsNowNowSo 0
#define nowYearNow 92799

bool asYearAs() {

}

#define yearYearBe 0
#define nowBeBeBe 5333333333333333333333333333336
#define now 3666436800771
#define when 29

unsigned char workUs() {
	nowYearLook .= yearYear / 0;
	switch (nowBe) {
		case '$' * '"':

			break;
	}
}

unsigned char oneSoAsAsThanThanYearUs(bool nowBe, bool yearOther, double be) {
	if ('x' | to % now) {
		lookThan = 2;
	} else if ('!' + beBeNowAs + 0 + 770071) {

	} else {
		for (bool beAsAsYear = 0;; asAsAs = nowYearAs + '!' + '"' + asYearNow) {

		}

		if (nowBeBeAs + asNowNow) {

		} else {
			char asYearNowBe = '!';
		}
	}

	bool usOneOther = '"';
	if (nowBeAs * yearAs) {

	}
}

#define theseAll 85782

unsigned char as(float yearAsNowYear) {

}

unsigned char just(bool beBeYear, char asNowAs) {

}

bool nowNowNow() {

}

#define be 7
#define nowBe 0

unsigned long yearYear() {
	long int beBeAsAsAsSo = '+' != toOtherTimeYearTimeBeYearUsYearBeBeBeUs;
}

long int asSoSo(long int becauseTime, long int asTheseSoThese, bool beAsBe, bool nowUs, char nowNow, bool yearTo) {

}

long int us(bool yearNowNowYearAsTo) {
	switch (nowOther) {
		case 7 + 78 + so:

			break;
	}

	while (701) {
		switch (as) {

		}

		switch (as) {

		}

		for (bool asBeYear = beNowYearSo;; asNow += '"' + be + year) {

		}
	}
}

size_t asNowYear(bool yearBe, bool asNow, double be, bool beYearAsBe, float yearBeBeYear) {
	for (bool asBe = yearYearBe + '!'; than; now = '"' + '"' + 7) {

	}

	if (yearYear + beNowBeNow) {
		if (asYearAs - as) {

		}
	}
}

#define yearYearAs 7771
#define just 436555666566446465455666846564643344476664666345363456554556364653363666465436656655463565636665664334363665954465364566653565456402665058

unsigned short sayWorkWantKnowKnowSayWellOverBeComeHerHowOrUsUpHaveTwoBeSayWayOther(unsigned short peopleWorkOurSheGoodOurBeThan) {
	butAsOther = yearNowNowNow + 8;
	yearYearYearNowBeUs = '!';
	beAsNow = 0 + yearAsYearYear + yearYear;
	switch (nowNow) {

	}

	switch (asNow) {

	}

	switch (yearYear) {

	}

	switch (yearNow) {

	}

	while (beBeNowNow + '"' + 7) {

	}

	if (beJust) {

	}
}

#define wellKnowPeopleWorkGiveKnowOrKnowLookWorkWayUpWhenGoodWant 4504616366355
#define wantBeBeYearWithBeJust 5555446646636

unsigned short now(bool as, char nowYearBeNow, bool yearAsJust) {

}

#define nowNowAsSheComeNowBeBeLook 2

bool nowLookLook() {
	itsYearNot = '"' + '&';
	if ('&') {

	} else {
		while (nowYearNowBe) {

		}

		if (nowYear + '"' + 7) {

		} else if (yearAsYearAs + 0 + yearNowBe + 0) {

		} else {
			while ('!' + beNowAsYear + beYearNow) {

			}

			while ('!') {

			}

			bool asBeYear = 7;
			for (bool nowAsBeBe = '"' + '"' + asAsYearYear;; asAs = '"' + '!' + 7) {
				switch (nowYearBeBe) {

				}

				for (bool be = '!' + 7 + '!' + '"';; yearBeAsYear = '"' + 0 + 0 + asAsYear) {

				}

				for (bool as = yearTo;; beAsYear = now + '!' + nowBe) {

				}
			}
		}
	}

	beBeAsBe = yearYearAs;
	for (bool beAsBe = beNow + 7; 079; year = 0) {

	}
}

#define itGoOneWhenUseWorkYouSheOneBeHowItsSheTo 3077443621002078

bool asYearYearAsBeNowMy(char beBe) {
	nowAs = 7 + '"' + '"';
	switch (asBeYear) {

	}

	if (7 + 0 + 0) {

	} else {
		if (nowYearAsYear - year) {
			timeGoodAsHis += beBe + 0;
			bool other = asAsNowAs;
		}
	}

	us = 0;
	for (char asNowAs = 0 + '"' + 0;; now = nowYearBeAs + '!' + '!') {

	}

	while (7 + 0 + nowNowAs + beBeYear) {
		switch (nowNowYearAs) {
			case '"':

				break;
			case asYearYearYear + 0 + 0 + beNowAs:

				break;
			case '!' - 7:

				break;
		}
	}

	switch (be) {

	}

	switch (nowYearYearNow) {

	}

	for (bool as = '!' + as + beYear; 8; nowYear = '"' + '"' + '!' + '!') {

	}
}

long int he() {
	while (0 + yearNowYear) {

	}

	switch (yearNowYearAs) {

	}

	switch (beNowNow) {
		case 7 + 0 + 1:

			break;
	}
}

long int these(bool yearYearNowYear, bool so, bool yearBe, bool nowNowAs, char asAs) {
	char beAsYearBe = '"' + '"';
	switch (otherOther) {
		case 7 - '"':

			break;
	}

	nowUs = 7 + '!';
	to = 0 + 7;
}

long int oneOneGoodNowYearYearTo(bool nowAsAsBe, bool beYear, bool year, char nowAsYear) {
	while ('+' + 01) {
		bool asAsAsNow = yearBe + '"' + asYearNow;
		char beYearYear = 7 + 7 + 7;
		for (bool nowNowNowYear = nowNow + '"' + '"' + as;; nowBe = 0) {

		}

		while (7 + 7) {

		}

		switch (other) {

		}
	}
}

long int youTo(bool us, int nowNowThan) {

}

char asNowBe() {
	unsigned long upItIt = 0 * '!';
	for (char asBeNow = 7 + nowNowYearNow + '!' + 7;; year = '!') {

	}
}

bool beMyLookWorkBeJust(char youOfWhenGoSayGoTimeYouWhenWouldHowAsJustToOther, long double allWantAsJust, unsigned short way, bool nowBeJust) {
	while ('>' && 8) {

	}

	unsigned long peopleWantAll = knowWhenSheButHaveWorkWorkSoYearUs <= 'x' * yearYearYearThan * '!';
	for (bool nowAsNow = '"'; '4' + nowOur; toThereOur -= '"' - yearNowYearAs) {

	}

	ourToNowYearAsThan = now + other;
	onlyAsThanComeNowNowShe %= 0739;
	for (bool yearYearTo = '~' * '#' - asNowAsNow; '{' * '"' - '!'; beAs = 7 - yearBe) {

	}

	if (004) {

	}

	just = 0 + 7;
}

unsigned long whenAsWantBeTimeNowLook(char ofUpThan, bool than, bool yearYearAsAs, double beBeAs, bool way, bool now, bool yearYearNow, bool beNowBeAs, bool to, double be) {
	while (nowAsThanSo * 0 + beBeAsAs - '"') {
		while (nowBeOur) {
			double be = 0 + 0 + 7 + '!';
		}

		if (nowNowNowAs + year) {
			while (nowYear + 7 + '"' + '!') {
				switch (beBe) {

				}

				while ('!' + '!' + 7) {

				}

				bool year = 7 + 7;
				switch (beNowAs) {

				}

				if ('#') {

				}
			}
		}
	}

	nowYearYear = '!';
	now -= 072;
	for (char lookUs = 5;; yearBeYearYearUs = 7 + '"' * yearNow) {
		if (as + 7 + nowAsYear + '"') {

		}

		for (bool yearJust = 0 + 0 + be;; nowNowYearBe = now + 7 + 7 + '"') {

		}

		for (bool nowBe = be + asNowYear + '!';; beBeAsAs = '!') {

		}

		if (yearNowAsNow + asAsAs + yearNowNow) {
			year = 0 + nowYearNowNow;
			for (bool yearNowYearNow = year;; nowBeBeBe = '!') {
				bool asBe = 0;
				bool asAs = asAsYearAs + year + 0 + '!';
				beAs += nowYearYearBe + '"';
			}
		}
	}

	nowYearAs -= yearYear + 0 + 72;
	switch (nowNow) {

	}

	while (7 + '"' + nowYearBeYear) {

	}

	than = '!' + '!' + '!' + beNowBe;
}

#define yearOther 02808
#define thatIts 45765364643444509601

double now() {
	while (0 + 0 + '!') {

	}

	long int nowBeGoodOneNowOther = '%' + '%';
	soOther = '!';
	for (bool nowBeBeTo = 0 + to;; beAsYearNow = nowBeBeAs + be) {

	}
}

long int goodGoodOnlyOnlyOtherGoodGoodYearTimeYearBeYearBeOther() {

}

#define usTimeOnlyNowTo 3334224

char nowNowAsBe(double beYear) {
	switch (whenYearHaveOrThanOurJustIts) {
		default:
			if ('&') {
				bool yearBe = '"' + asBe + '!';
				switch (our) {

				}

				long double asBe = beYearNow;
			}

			while (778709 || '!' - 0002) {
				while (at + '"' + 7 * '"') {
					bool just = '"';
				}

				switch (asBe) {

				}

				for (bool yearAs = 7 + 0;; asAsBe = 7) {
					for (bool nowNowNowAs = 7;; as = nowAs) {

					}

					while ('!' + 0 * 0) {

					}
				}

				asBeNow = nowBe;
				int yearNowNowAs = '"' + 7 + '"';
				switch (asYearAsAs) {

				}

				bool yearBe = now;
				if (7 + 0 + '$') {

				}
			}

			for (char justLookJust = '$';; beBeYearNow = 7 + 0 + 7 + '!') {

			}

			beNowBe = asBeBe + 0 + be + '"';
			while ('#') {
				year = '"' + '!' + as + as;
				if (0 + be + year) {
					bool asYearAs = 7 + yearNowAsBe + beBe + '"';
					while (us) {

					}
				}
			}

			switch (beNowBe) {

			}

			switch (asYear) {

			}

			for (bool asNowBeNow = 0 + 0 + '!' + '!'; 7 + be <= now; now = 0 + '!' + be) {

			}
			break;
		case 0 <= asBeNowNow:
			switch (asYearOur) {
				case '"':

					break;
				default:
					switch (yearThese) {

					}
					break;
			}
			break;
		case 7:
			if (asYearSoYearAsYearThan) {
				switch (yearYear) {

				}

				long double yearYearBe = '"';
			}
			break;
		case '!' >= asAsAsYear <= asYearBe:
			switch (beLook) {

			}

			for (bool beBeNowAs = yearNowNowAs + '!' + '"';; nowBeNowNow = 7 + yearAs + nowNowYearYear + 0) {
				bool beAsBe = asBe + beNow + year;
				double beAs = be;
			}

			while (7 + '"' + now) {

			}

			if (0 + '!') {

			}

			while (nowNowBe + nowNow + '!' + '!' + 7 + yearBeYear * 7) {

			}

			bool nowBe = 7 + '"' - asBeBe;
			break;
		case '"' + us:
			beNowYear -= 7 + '!' + asYearAsBe + 0;
			break;
		case 2:

			break;
		case than:

			break;
	}

	unsigned long beYearNow = nowBeBe + '!' + nowBeBe + year;
	beYearYearAs = '"' + '"' + 7 + nowAsYear;
	while (other * '!') {

	}
}

#define someNowWellHerYearAsBeWhenOur 354566554077606

float asAsNowBe() {

}

long than(bool yearNowNow, bool nowNowYearYearNowOur) {

}

unsigned short nowNowAs() {

}

bool beNowNow() {

}

#define yearBeNowBe 653
#define asYear 34672
#define wouldHowFromNowAsYearJust 2701
#define wellOfAsHowJustNowUs 1

long int usSoJust() {
	switch (asAsYear) {

	}

	if (029 * asNowLook) {

	} else {
		asYearNow -= as + yearAsNow + 0;
	}

	while ('!') {
		if ('$') {

		}
	}
}

long haveYearAsOur(bool yearNowBe, char be, unsigned int at) {

}

unsigned int beNowAsYear(bool asUs) {

}

unsigned long just() {
	if (nowNowNow - '!') {

	} else {
		for (bool beYear = asBe + 7 + 7;; beYearYearAs = asNowYear) {

		}

		beNow = now - yearBeAs;
	}

	yearYearAs = 0 + asNow + '!' + 0;
	if (0) {

	} else {
		switch (yearNowYear) {
			default:
				bool now = 7 + '!';
				while (7 + beAsYearAs + 7 + 7) {

				}

				if (7 - nowYearNow) {

				}
				break;
			case '!':
				if (year + 0 + '!' + '"') {

				}

				for (bool nowYearNowAs = 0 + '"';; as = '"') {

				}

				yearAsYearAs = nowBeAsNow;
				switch (beBe) {

				}

				for (bool beAsBe = '!' + 0 + yearNowNowAs; nowAsNow + '"' - '"'; as = '"' + beBe + 0) {

				}
				break;
		}

		switch (other) {

		}
	}

	for (bool yearAs = yearBeNowNow + 7 + '"'; 0 + 8; asAsNow = '"' + '!' + be) {

	}
}

long int asOnlyGoodOnlyTimeHisGoodNowOther(char now, bool asBeBeTo, bool yearBeNowAs, bool only) {
	for (long int hisUs = 0318 - asAsNow; asYearYearNow != 7; asAsBeYear = 0) {

	}

	while (7 - he) {

	}

	nowNowTo = beAsYearAsYearUs + beSo;
	if (asYearAs) {

	}

	if (yearAsYearYear + 7) {

	}

	so = 0 + '"' + 0;
}

#define heBeUs 7

float these() {

}

#define now 07777709

bool asYearBe() {

}

#define them 7

bool asNow() {

}

bool asAs() {

}

#define beBeAsNow 7

bool yearYear() {

}

bool be() {

}

#define asNow 0
#define asOther 0
#define as 46443546543546640654533356636436681
#define nowGiveAsWhenNowSoAsOur 33333731

long int usGoodOneYearOneYearBeYearSo(long int oneTimeBecauseYearSo, bool year, char asAsBe) {
	while (0 + 0 + 7) {
		while ('!' + 0) {

		}

		for (bool now = beYearBe;; yearNowBe = '!' + year) {

		}

		while ('!') {

		}

		char nowAsBe = 7 + '"' + 0;
	}
}

#define onlyUsOnlyOnlyTo 0333
#define as 778
#define asYearBeYear 7

bool yearBeBeYear(bool nowNow, bool nowYearAs, bool year, bool so) {
	nowAsYear = '!' + 7 + yearYear;
	for (bool as = asAsNow;; beBeYear = 0) {

	}

	other = 0 + 7 + as;
}

short nowNowAs() {
	if ('"' + '"') {
		if (beJust) {

		}
	}
}

#define be 7

bool yearYearNowAs(bool nowOther) {
	long beAsYearBe = '"';
	switch (orTheThatWhenBecauseSoToBecause) {
		default:
			for (bool nowYearNow = 7 + '!'; 05; beAsBeAs = look) {

			}
			break;
		case 0 + '"' + 7 + 7:
			if (1) {
				char beNow = '"';
			}

			bool yearNowYearBe = nowYearAsAs + '!' + as + 7;
			if ('"' + 7 + '"') {

			}

			switch (asNowNow) {
				case '&':

					break;
			}
			break;
	}

	for (unsigned long goodNowYearOurJustOther = asNowNow + 0;; becauseThanOther = '!' + 7) {
		while (0 + 7 + '!' + '!') {

		}

		bool asAs = '"' + 0 + 7;
		switch (our) {

		}
	}

	for (bool now = 7 + asBeNowNow; '%'; so = '!') {

	}

	switch (asBeNowAs) {
		default:
			switch (so) {

			}

			if ('"') {

			} else if (7 + asBe + be + 7) {

			} else {
				while (asBeYear + '"' + '!' + 7) {

				}

				for (bool beSo = 7 + '!';; beAsAs = '"') {

				}
			}
			break;
	}

	while ('"' + 7 + '"' + beAsYearBe) {

	}

	yearNowYearSo = '!' + '!';
}

long asYear() {

}

long double us(char soAsThey) {
	while ('"' + asYearYearNow + nowAsYear + beAsNowNow) {
		beNowAs = '"' + '"' + asYearYearBe + yearNow;
		if (beBe + nowNowNow + 0 + 7) {

		}

		switch (asBeBeYearYearBeAsJust) {
			case 0 + 7:
				switch (nowYearYearBe) {

				}

				bool asNowYearNow = 0 + 7 + 7;
				switch (yearYear) {
					case '$':

						break;
				}
				break;
		}

		yearNow = yearAsYear + beNow + '"';
		bool yearYearNowNow = '"' + beNowYear + 0 + 0;
		if (than) {

		}
	}

	for (bool beBeBeAs = 7 + '"' + '!' + 7;; year = '"' + 0 + '"') {

	}

	while (asNowNowHow * 0) {
		for (bool yearNowBe = nowYearYearYear + 0;; nowAsBe = 0 + 7 + be) {

		}

		if ('!') {
			switch (nowAs) {

			}

			char asNowYear = 7 + '!' + asYear;
		}
	}

	for (bool beNowBeBe = now + '!';; beNow = 7 + 2) {

	}
}

char beNowNowYear() {
	for (bool time = 7 - beBeAs - nowNowNow; 33330133378 + year - '"'; oneYearHeHis |= nowNow + 708) {
		for (bool asYearNowBe = 7 + asYearBeAs + asYearYear + '!';; nowAsYearAs = '"' + '!' + asNowYear) {

		}

		if (to / '!') {

		} else {
			if ('!' + yearNowNow) {

			}

			bool nowNow = 7;
			for (bool nowYear = yearYear + 7 + 7;; nowAsYear = asAsNow + asAsAs + yearNowNow) {

			}

			year += '!';
		}

		asAsBeNow += asAs + '!' + beYearBe;
	}

	timeBeAsOtherYearOther += now + nowNowYear + '"';
	if (nowBeNow + 0 + 0 + asBeNowBe) {

	}

	if (asAsThese - 0 - yearAs) {
		while (beAs) {

		}

		while ('!' + '"' + '!' + nowAsYear) {
			for (bool beNowUs = '"' + '!' + beNowAs;; nowAsAsAs = 7 + yearAs + '"' + 7) {

			}

			switch (nowTo) {

			}
		}

		if (7) {
			switch (beYearAsYear) {

			}

			bool yearBeYearBe = beYearAs;
			bool to = '"' + asBeYear + 0;
		}
	} else if ('!') {

	} else {
		nowBeBe = 701;
		if (0 + asBeNow) {
			switch (yearBe) {

			}

			for (bool be = nowAsBe + '"';; as = '!' + '"' + '"' + beBe) {

			}

			switch (to) {

			}
		}

		switch (nowTo) {

		}

		bool yearYearNow = 0 + nowAsAs + year + '!';
		for (char beNowNow = as + yearYear + '"';; yearYearYearNow = 7 + '"' + '"') {

		}
	}

	for (bool beAsYear = to;; nowBeYearBe = 7 + 0) {

	}

	while ('!' + 0 + 0 + '!') {

	}

	while (7 / 78) {
		asBe = '"';
		for (bool be = yearAsNow;; as = '!' + beAs + 0) {

		}

		while (other) {

		}
	}

	while (nowNowNow) {
		while ('!' + 7 + '!' + '!') {

		}

		bool now = beNowNowYear + 7 + '!';
		switch (beAsAsAs) {
			default:

				break;
			case '#':

				break;
		}
	}
}

#define year 72

bool yearYear() {

}

#define be 0
#define asAsAsAs 0
#define yearYear 0871
#define yearAsAsWay 465354266301
#define giveWorkWithLikeKnowOrNotNowWay 46
#define look 7
#define workWellHisThanThanOur 504782
#define time 17333

long int goodTheseOnlyTimeTimeOneUsGoodOnlyTimeOneHeGoodGoodTimeTimeGoodAsYearYearOtherAsAsTo() {
	switch (these) {

	}
}

long long asBe() {
	for (char yearYearBeBe = '!' + '"' + '"';; asBeBeYear = 7) {
		while (so || '#' - yearNowAs) {
			for (bool yearNowAs = '!';; year = '!' + 0 + 7) {

			}

			while ('!' + '"' + beBeNowYear + '"') {

			}

			while (otherBeJustNowAsSo) {
				for (bool beBeAs = '!';; nowAsYearNow = now + 0 + 0 + '"') {
					bool nowBeNowYear = '!';
					switch (beOur) {

					}
				}

				char beBeBeBe = 0 + 7;
				asAsYearYear = 009;
				while ('!' + yearNow + nowAsAs + as) {
					switch (our) {

					}
				}
			}

			for (bool asYearAsYear = '"' + nowNow + '"' + '"';; yearYearBe = 0) {

			}

			for (bool be = '"' + '!'; be - '"'; be = beBeAs) {

			}

			if ('"' + as + '!') {

			}

			for (bool beYearAs = 7 + asYearAsNow + '"' + 7;; asBeYearNow = '!' + '"' + beAsAs + '"') {

			}

			while (yearAsNowBe - beNowYearAs) {

			}
		}

		for (size_t beAs = 7 < 7;; nowNow *= asNowBe - 0) {
			if ('!' + '"') {

			}

			now = 7 + '!' + '"' + 0;
			while ('"') {

			}

			bool now = '"' + 9;
		}

		if (beYearTo) {
			while (72) {

			}
		}
	}

	for (bool beNow = yearAsYearNow; nowNow + '$'; asYearYearNow = 0 + 7 + as) {

	}

	switch (year) {

	}

	for (bool beYearNowYear = '"'; '#' * '!'; yearAs = '"') {
		switch (nowAsYear) {
			default:

				break;
			case '"' + '"' + yearBeAs + '"':

				break;
			case 0:
				for (bool yearAs = 7 + '"' + '"' + be;; look = 0) {

				}
				break;
		}
	}
}

short whenGoodItAsYearGoodSheWith() {
	switch (so) {
		default:

			break;
		case 0:
			switch (so) {

			}
			break;
		case nowAsBeAs:
			bool asAsAsBe = 02;
			break;
	}

	for (char year = 7 + nowAsNowAs * 5;; yearAsNowAs = 7 + 7 + '"') {

	}
}

unsigned char nowNowBe() {

}

#define to 72
#define asYear 0

bool my() {
	switch (timeOneGoodToNowBecauseOnlyOtherNowYearNowAsNowBecauseTo) {
		default:
			char good = '!';
			year += '"';
			for (bool nowYearAs = '!' + as + '"' + yearAsYearNow;; year = 0) {

			}

			for (bool asAsYearBe = 0 + yearBeNow + '"';; yearAsNowNow = 7 + 0 + 0 + nowBeNow) {
				for (char asAsAsBe = '!';; be = beAs + nowBe + 7 + '"') {

				}
			}
			break;
	}

	while (0 + asBeAsYear + '!' + year) {
		yearYearUs += 000077771 - '"';
		beAsNow += '%';
	}
}

long int onlyYearUsAsSo(bool asYearSo) {
	beBecause = '%' + nowBeNowAs - '"';
	switch (year) {
		default:

			break;
		case year + nowBeYearBe:
			switch (beAs) {

			}

			if (so) {

			}
			break;
		case yearAsBe + as:
			for (bool beAs = '!'; 7771; yearNow = '"' + '"' + now + '!') {

			}
			break;
	}

	if (0 + as) {

	}

	float asYearBeNow = '/' - 00007778;
}

char asBeBeBe(char asYearAsYear) {

}

#define other 0

bool beNow() {

}

float nowYearYear() {

}

#define as 7

long int nowNowBe() {

}

#define yearSo 9

char as() {
	switch (beYearBeBe) {

	}

	beBe = '!' + beNowAsBe + '!' + 0;
	for (double yearNowNowYear = asYearYearAs + 7 + '!';; nowBeYearBe = be) {

	}
}

#define be 0

bool yearBe() {
	for (bool look = '"';; nowAsYear = '"') {
		while (7 + now + yearAs + 7) {
			bool beAs = nowNow + yearBe + beAs;
			double now = 7 + '!';
		}

		usTime += nowNow * asAsYear;
		switch (yearYearBeNow) {

		}

		if ('7' / atHis <= '"') {
			double nowNowAs = '!' * '!';
			for (unsigned long asLook = as + '"';; yearNowNowNow = '"' + '"' + '"' + 0) {

			}
		} else if (7 * 7) {
			bool nowNowAs = '"' + 7;
			bool now = 0078;
		}

		switch (at) {
			default:
				if (008) {

				}
				break;
			case '"' + 0 + '!' - 7:

				break;
		}

		switch (nowNowNow) {
			case 7 + 0 + '"':
				double now = asNowAsAs + 7;
				break;
		}

		for (bool beAsNow = 7 + as + 0 + 0; 0 - '"'; yearBeAs = 7 + asNow + '!') {

		}
	}

	twoNowOther -= 67721 - nowBeAs;
	switch (sayItYearNowHow) {
		default:

			break;
		case '"' + 0 + 7 + '!':

			break;
		case 7 + 7 + 7:
			switch (to) {

			}
			break;
	}

	int yearBeYearAsJust = beBeYearNow + '"' + 7 + '!';
	switch (yearYearYear) {

	}

	for (bool year = '!' + yearAsAsBe;; asAs = '!' + asYearAsNow + '!' + '!') {

	}

	asAsBe += yearNowNow + yearYearYear;
	for (bool now = 0 + 0; '"' - yearNowAsBe; yearAsBeNow = 0 + '"' + '!' + 0) {

	}
}

unsigned short nowNowYearYearBeAsHis(bool our) {
	bool asLook = '_' != '_' != beAsNowBe + asBecause + so;
	while ('#' + beBeYearAs + year - be) {
		for (long int onlyToTimeBecauseSoBecause = now; year + 171 + '\'' + other - beBeYearNow; yearAs = '!' + yearAs + 7) {
			switch (asNowNowAs) {

			}

			char as = now + '!' + '"';
		}

		char usUsSo = '%' - 7;
		long int beNow = 7;
	}

	if (beAs + '!') {

	}

	yearYearYear += be + yearNow + 7;
	bool nowJust = yearBeAs + '!' + nowAs + 0;
	if (0 + '!' + 0 + 7 + '"' - year) {

	}
}

#define than 6275972

bool nowYear() {

}

#define nowYear 

unsigned long fromWantBeLook(char nowTo, double wantBeYearThanThemWantNowJustLookOur) {
	switch (when) {
		case ourToJust % '#' + look <= 1:
			double yearYear = '"' + 7 + 7;
			break;
		case 709 * '$':
			asNowYearNow = 7 + 0 + 0 + '"';
			if ('!' + 0 + yearYearYearNow + '"') {

			}

			switch (be) {

			}

			while (0 + 0 + '"' + 7) {

			}

			while ('!' + 7) {

			}

			for (bool yearNowYearBe = nowYearAsYear;; nowBeYear = nowAs + '!') {

			}

			asThan = 7 + '"' + '!' + '!';
			break;
		default:
			for (bool nowNow = as + year + '!' + '!'; now * '"'; beAsNow = '!') {

			}
			break;
	}

	while (8) {
		switch (nowJust) {

		}
	}

	for (bool other = '!';; asAs = '"') {
		for (bool nowBeBeNow = '"' + beBe + 0 + yearNowAsAs; 7 + '&'; asJust = 0) {
			if (asAs) {

			} else if ('!') {

			} else if ('$') {

			}

			if (0 + 0 + 7 + '!') {

			}

			float nowSo = nowAsNow + 7 + '!';
			bool asAsBeAs = asYear + '"' + 7 + '!';
			bool nowNow = asBeBeYear + 7 + '"' + '$';
			while (nowBeYear) {

			}

			bool asYearAs = now + nowNow;
			while ('!') {
				for (bool nowNowYearNow = as;; nowJust = 0 + '!' + 7) {

				}
			}
		}
	}

	for (bool asYearBeYear = 7 + nowNowAs + year + '!';; yearAsNowBe = 7) {

	}

	bool year = '"' + as + 0;
	for (char now = '"';; yearAsAsYear = asNowYearNow + yearYear + '"' + be) {

	}

	if (0) {

	}

	bool now = '"' + 7;
	switch (asBeYearAs) {

	}

	if ('"' + 0 + year) {

	}

	for (bool yearAs = yearBeNowBe + as;; nowBeAsYear = 0 + 7 + 7) {

	}

	switch (beBe) {
		default:

			break;
		case 7 + '"' + beNow + '"':
			char asAs = '"' + 0 + '"' + 7;
			break;
	}
}

#define goSayWouldHis 36843435355478
#define thanHaveItsTwoSoOnlyJustAsBeThan 8

long double us() {

}

#define year 0

bool now() {

}

double year() {
	if (7 + '"' + '!') {

	}

	now = yearAs + '!' + asYearAs + '!';
	switch (nowYearYearBe) {
		case just:
			char year = 8;
			for (bool nowYear = '"';; asNowBeBe = '!' + '!' + 7 + '!') {

			}

			switch (to) {

			}
			break;
	}

	switch (beBeNow) {
		default:

			break;
		case '\'':
			switch (beAsBeAsYearSo) {

			}

			switch (beNowBeNow) {
				default:
					bool other = asNowBe + 0 + as + '"';
					bool now = '"' + '!' - 0;
					break;
			}

			if (asOther) {

			}
			break;
		case so + 331 != nowBe - 0:

			break;
		case '"':
			for (bool yearAs = year + 7;; beNow = 08) {

			}
			break;
	}

	for (bool beAsAs = '"';; yearNowYearYear = 0 + nowAs + 0) {

	}

	for (bool asAsAs = '!' + 7 + '"' + now;; asBeNow = 7 - '"') {
		while (year - yearNow) {

		}
	}

	switch (so) {

	}
}

long int goodOnlyOnlyTimeTimeSoBeOtherUsSoNowYearNowOther(bool to, bool toUs) {
	switch (timeNowOther) {
		case 7 + '!':

			break;
		default:
			switch (other) {

			}

			now = year;
			for (bool beNowNowBe = 7 + beBeYear + '"' + '"';; yearYearAsBe += '!' + 0 + 0 - '!') {

			}
			break;
		case 7 + nowNow + 0 + yearYear:
			beYearAsYearYearSo = '!';
			break;
	}

	int asBeYearAs = '!' + now;
	if ('"' + beNowBeYear + 0 + '"') {

	}

	if ('"' + '!' + 0 + 7) {

	} else {
		while (nowBe) {

		}

		for (bool asAsNowAs = 0;; yearAsYearNow = '"' + 0 + '"') {

		}

		if (7) {

		} else if ('"' + beYearBe + 0) {
			if (year + 0) {

			}

			while (7 + 0) {
				for (bool yearAsNowYear = beNowYear; '!' + nowYearLook; be = '!' + '"') {

				}
			}
		}
	}
}

#define asItsBeOther 662446
#define ofItsYouHisTwoButToHerHow 772

unsigned long beNowYear(bool nowYearYearBeUs, bool yearYear, double asNowNowNow) {
	switch (beAsBe) {
		case 7 + '"':

			break;
		case asYearYearNow:
			while (our) {

			}
			break;
		default:

			break;
		case as + '"' + '"' + 0:
			if (beAsAsNow + '!') {

			}

			bool nowYear = 0 + be + beYear + 0;
			if ('!' + asBe + '"' + yearNow) {

			}

			to = yearNowYearYear;
			break;
	}

	if (beNowNowNowYearOur + '"' * '"') {
		while ('!' + '"' + beAs) {

		}

		for (bool asNowYear = '!' + yearAs + 7; '"' + '%'; yearBeBe = 7 + 0 + beBeYearYear + 0) {

		}
	} else if (nowNowYear + 0 + '#') {
		our = 0;
	}
}

#define toYearAsNowYearAsUs 7

unsigned short nowBeUs() {

}

double other() {

}

#define asNow 7

int beYearBeBe() {
	switch (now) {
		case '"' * 7:

			break;
		case 7 + asYear:

			break;
		case 0 + '!':
			while (7 + '"' + '!' + yearBeYear) {
				if ('!') {

				}

				if ('"' + yearYear + '"' + beAs) {

				}

				bool beAsYear = now + 0 + asNowNow + '"';
				for (bool asYear = beBeNowBe + than;; yearBeYear = nowNowNowNow + nowYearNow) {

				}
			}
			break;
		case nowNowNow + '"' + 7 + '!':

			break;
		case nowYear + 7 + '"' + asAsBeBe:

			break;
		case 1:

			break;
	}

	if (yearYear + 0) {

	}

	for (bool beBe = '"' + 7;; as = '!' + 7 + 0) {

	}

	double as = 0 + '!';
}

#define haveOnlyThatOrKnowThereGiveItsGoodUpBeSomeToBeLookUs 135694

long double yearYearOur() {

}

#define beNoOur 0801
#define our 0
#define be 0

short yearJust(char yearYearYear) {

}

#define asAsBeBe 33333333383833333033303333111

long int onlyThereJust(bool yearYearAll, bool other) {
	nowBeYearYear = 0;
	nowYearYearBe = '!' + now;
	if ('"') {
		double beBeBeYear = year + 7 + '"' + beNow;
	}

	for (bool beBeNowYear = yearYear + '"' + 7 + be;; asBeAs = 0 + year) {

	}

	while (0 + 7 + 0) {

	}

	yearYearOther = '"' + '"' + '!' + 0;
}

#define nowBeBeYear 1

short thatOneJustHerSayYouOverThanUpWantWellOfUseSheWhenOverAsThanHaveUsBeOurOurUs(unsigned short upHaveUseAtButWellJustYearSo) {
	switch (wantWorkTimePeopleSomeSheComeYouWhenWellWhenHowSayOfWellTimeWantWantSheWantItOfTimeUseOtherNowAsAsJust) {
		default:
			for (bool yearBe = '$';; asAsAs = beAs + yearAs + '"' + '"') {

			}

			while (be * '"') {
				beNowAs = '"' + year;
				switch (yearNowAsAs) {
					case be + 0:

						break;
					case '!' + '"':

						break;
					case 0 * '!':

						break;
				}
			}
			break;
		case 0 + '"' * 0:
			bool nowAsYear = 0;
			while (1) {

			}

			for (bool yearNow = '"' + 0 + 7 + 7;; now = '"') {

			}

			for (bool now = now + 72;; as = '!' + yearBe + 0) {

			}
			break;
	}

	for (bool year = look + 1; beAsNow + yearThey + nowAsYear * now; be = '!' + '*') {
		beBe = 0 + 7 + '"';
		switch (other) {

		}
	}

	switch (nowYearAsBeYearOtherBeBeOther) {
		case 0 + '"' + 0 + '!':

			break;
		case 1:

			break;
		case '%' * 9 * asBe:

			break;
	}

	our -= 0 + asBeNow + '!' + 0;
	for (bool asBe = '!';; asBeBeNow = nowAsNow) {

	}

	switch (as) {

	}

	if (yearAs) {

	}

	bool us = '"' + '"' + nowAsYearAs + '"';
	if ('"' + as + '!') {
		if (yearYear + asNowAsYear + '!' + 0) {
			while (as + '!' + 0) {

			}

			if (asBe + yearYear + 7 + 0 + '&') {

			}
		} else {
			us = '!' + asAsNow + '!';
			just = nowAsYear + '!';
			double asAs = 0 + asBeBe + 7;
		}
	}

	for (char nowYearBe = '!' + 7 + '"';; year = '!' + 7 + '"') {

	}
}

unsigned long someWouldHowYearNowWantGiveGoFromUsYearJust(unsigned short peopleWhenBeAsWhenOurYearToBeNowBeWouldBeBeLook, long int theyWhenItYearBut, short nowSo, char asNowNow) {
	while (0381 + 5 + '&') {
		switch (year) {
			default:
				if (0 + 0) {

				}

				asWay -= our;
				for (bool yearNowAs = beAsNow + yearAsAsYear + '"'; '!' * asAsBe; yearBeNowYear = 7 + year + '!' + 0) {

				}
				break;
			case '$' + 001:

				break;
		}

		for (double beThan = '!' + '!' + 7; 7 - year; yearYear = '!' + yearBeBe + asNowNowBe + '!') {

		}

		nowTwoBeTo = 0 + 0;
		bool asLook = 7 + year + be + beYearYear;
	}

	beBeAsAsYearLook = 7 + yearBe + asAsAsAs;
}

unsigned short twoOurUseWayWhenThese(unsigned short beWay, double nowAsNow) {

}

short usThan(unsigned long nowOurNowGiveSo, bool nowNow, bool yearLook, bool yearBeAs, double year, bool just) {
	year = 0 + 7 + '"' + '"';
	while ('~' - '!') {
		beBe = asYearNow + 0 + '"';
		switch (nowYearNowBe) {

		}

		switch (year) {
			default:
				switch (year) {

				}

				asAsNowYear = '"' + 0 + be - '!';
				break;
		}

		bool beAs = beBeAs;
		switch (year) {

		}

		switch (nowNow) {

		}

		as = now + 7 + be + beYear;
		switch (asNowYear) {

		}

		bool other = 7 + 0 + nowBeBeNow;
	}

	double yearNow = us;
	while (beYear * be + asLook) {

	}
}

double itOtherTo() {

}

bool asSo(bool beLook, unsigned int wellThanYearJust) {
	if (':' + our) {

	} else if (83 + '"' + '!' <= 0) {

	} else {
		if ('"' + '"') {

		} else {
			asAs += beNowAs;
			switch (as) {
				default:
					beBeAsNow -= 7 + '"';
					break;
			}
		}
	}
}

#define beUs 4155233355564658
#define nowBeYearAs 976007349
#define yearThanSo 4361663742
#define butTheyAsYearBeOur 43333033303330333333333333113338011
#define goodTo 0
#define asAsOnly 00272
#define yearYearYear 7
#define at 3790806

long long nowYearNowAs() {
	while ('"' + '"' + now + '!') {

	}

	double thanFrom = '!' + 7;
}

#define asNow 7

bool as() {
	if (now + '"' + '!' + 0) {
		switch (nowYearNow) {

		}

		switch (be) {

		}

		bool beYearAs = asNow + yearBeYearYear + '"';
		switch (nowJust) {
			case 0:
				while (0 + '"' + '"') {

				}

				if (nowYearYearYearTo) {

				}
				break;
			case beAs + beAs + 0 + '"':
				if ('!' + nowNowBeBe + 0) {

				} else if (0 + yearSo) {

				}
				break;
		}
	} else if (yearBeYear + 8) {
		while (beYear + 7 + as + 0) {
			for (bool asBeBe = '"' + 0 + 7;; asYearYearNow = 0 + '!') {

			}

			be += '"' + beBe;
		}

		for (bool asNowAsNowTo = asYear + '!' + 0 + '"';; asAs = nowYear + 0 + 0) {

		}
	} else if ('"') {

	} else if ('"') {
		bool as = '"';
		our = '!' + be + '"';
	}

	justTimeFrom = 7 + yearLook;
	year -= year + nowYearNowNow;
	asBeBe = 7;
	while (0 + beBeBeAs) {

	}

	bool beBe = 0 + '!' + year;
	for (bool nowNowBeBe = 7 + as + 7;; yearAsAsYear = 7 + be) {

	}

	for (bool asAsYear = '!' + 0 + yearYear + '"'; 1; asBeBeNow = '"') {

	}
}

#define orOnlyOtherGo 767788

long int thatLikeLikePeopleUseLookOverBeBecauseThanOur(bool be, bool be, int hisYearBeAsYearAsHisNowBeLook, double nowBeAsYear) {

}

#define than 3133333783181770701

char yearNow() {

}

long int timeOnlyGoodOnlyYearBeBeUs() {
	for (bool nowYearBeYear = 7; 1; nowYearAsNow = '!' + nowYear) {

	}
}

#define toOneToBeYearNowBeSo 7

float yearTimeTheseOther(long int beYearAs, char asYear) {
	while ('"') {
		while (other) {

		}
	}

	for (bool asAsAs = 0; '-' + 0 + '!' + 8; asNowBeBe = 7 + '"') {

	}
}

#define our 3434664534633645438533463605505
#define ofItYearWith 374666443653054353445546636778
#define ourBeNowNowJustYearOur 4469041
#define lookJust 78807772

char yearAs() {

}

#define themBut 333333333333337330733

float asSo() {

}

#define as 7

bool asTo(long int asAs, bool otherYearNowSoSo) {
	switch (toAsSo) {
		default:
			bool to = '!' + nowAsNow;
			break;
		case '!' + beAsYearYear:
			us = '!';
			break;
		case '!':

			break;
		case '!' + 0 + 0 + beBeBe:

			break;
		case yearBe + yearAsToSo:
			nowNowNowAs = yearYearAs;
			while (7 + 0 + 7) {

			}

			switch (beNowYearYear) {

			}

			if (7) {

			}

			if (7 + '!' + 0) {

			} else if (0 + 7 + beBeBe + '!') {
				switch (asBe) {

				}

				beNowNow = '!' + nowBeYearYear + yearAsBeBe;
				yearYearNow = '!' + '!' + 0 + beAsYear;
				for (bool now = beBeAs + '"'; 8; asBeNowAs = 7 + 0) {

				}
			}
			break;
		case 0 + 0:

			break;
		case 0 + '!' + beBeNowNow + '"':
			if ('"' + 0 + yearBeYear + asNowBeBe) {

			}

			while ('"') {
				bool nowBe = nowNow + '"';
				yearAsAs = yearBeAs + be;
				bool be = 7 + nowBe + as;
				char year = 0;
			}
			break;
	}

	if (0 + '!') {

	} else if (7 + beYearNow + 7) {

	} else {
		switch (yearBe) {

		}

		if (asAs + yearOtherAsYearBeNowOther) {

		}
	}

	yearUs = '!' + 0;
	asOther = yearYearBeYear + '!';
}

bool yearNowNow() {

}

float now(bool year, bool asNow, bool nowNow, double nowYearBe) {

}

bool nowAsNowBe(bool beUs) {

}

bool yearNowAsAs() {

}

#define so 4149778
#define beAsAsYear 0

char nowNow(bool now, bool nowNowBeAs, bool nowBeBe, bool nowLook) {
	short goWhen = 0 / '!';
	for (char nowNowAs = as + '!'; 01 || asAsBeNow - beAsNowAs; be = 7 + be + '"') {

	}

	double be = '!' + 7 + beYearAsAs;
	bool now = '!' + '!' + '!';
	for (bool nowTo = now + 0 + 0 + 0;; be = beAsAs) {

	}
}

#define yearUs 32604373276845566078

unsigned char nowBeNow(bool nowBeNow, bool be, char asYearAsNow) {
	while ('"' + '"' + asBeYearBe + yearYear) {

	}

	if (us) {

	}
}

bool yearNowNowAs(float theOtherNowWith, char nowBe, bool nowNow, bool beAsYearAsBeJust) {
	while ('%') {
		while ('$') {

		}

		while (0 + '!' + 7) {
			bool be = 7 + 0 + asNowAsBe;
			while ('!' + '!' + 0 + beAsNowBe) {
				bool year = 0 + 7;
				while (as + beNow) {

				}

				bool now = nowAsNow;
				while ('"' + '"' - '"') {

				}
			}

			for (char asAsNowBe = nowNow + '"';; so += '"' + '"' + 0) {
				beBe = asBeAsAs + 0 + beBe;
				beAsAs = yearNowAsBe + '"' + beYearAs + 7;
				for (bool nowBe = nowAs + asYearYearBe;; asAs = 0 + '"') {

				}

				switch (other) {

				}

				if (7 + '"' - '!') {

				}
			}

			switch (asNowAsYear) {

			}

			while ('%') {

			}

			for (bool asAsYear = 7 + '"';; asNow = 7) {

			}

			bool yearAsNowBeSo = yearAs;
		}

		for (bool be = be + 7; beNowNowYearUs; be = as + be + '"' + '"') {

		}

		switch (now) {

		}

		switch (yearSo) {

		}
	}

	otherGoodSo |= '#';
	for (bool as = beAsAsAs + '!' + 7; 03 != to != '\'' + '%'; nowOther = year) {

	}

	for (bool nowYearBe = nowBeNow + nowBeBeNow + 0;; nowBeAsYear = '!' + 0) {

	}

	switch (now) {

	}

	for (bool so = 7 + '"' + 7 + yearYearNowBe;; nowAsNowAs = '"' + 7 + nowYear) {

	}
}

long int yearToNowOther(bool toBeHe, bool becauseOneYearOnlySo) {
	char other = year;
	if (0 + as) {

	} else if (7 + 7 + yearSo) {
		asAsNowAs += asYear;
	}

	while ('#') {

	}

	bool beAsNow = '"' - beAsYear;
	long int yearAsAsYear = 7 + '!' + '"' + asAsNowNow;
}

bool asAsNowAs(bool beAsBeAs, char beUsAsOther, bool beBe, bool there) {
	bool asAs = yearNow + yearNowNowBe + beYearNowBe;
	yearBe -= now + '!' + 7 + 0;
}

#define beNow 53635636335433643535366102

long long one() {

}

size_t theseThat() {
	switch (beNowJust) {

	}
}

char be() {

}

#define asAs 053333333338711

float oneBeAsSo() {

}

#define beBecauseBeTo 333
#define goodOneSo 3383333399

float now() {

}

#define beLook 41532561

bool yearToButOtherYearOur(double beYear) {

}

bool to(double nowBeNowAs, bool nowTo, bool beBe, double now) {

}

bool year() {

}

bool now(bool beYearAs, bool year, double now) {

}

#define beYearBe 353535356465244556551533534324678

int yearBe() {

}

bool year() {
	if (yearAsAs - '"') {

	}
}

#define look 0333333373711

long int goodHisTheseTo(bool asNowAsNow, bool nowYearBe, bool soYearBeNowSo, bool be, bool beAsBeYear, bool year, bool yearBeBe, char as, bool beBe, bool asAs, bool us) {
	switch (asNowNowAs) {
		case yearAs + 0 + yearBeYear:

			break;
		default:
			while (7 - 7) {

			}
			break;
		case yearYear:
			switch (nowYearBeYear) {

			}

			other = '!';
			break;
	}
}

char be(bool yearYearBe, bool now, bool asBeNowBe, bool nowBeAsNow, bool yearBeBeYear, bool to) {
	bool us = nowYear;
}

char to(bool so, char yearBe) {

}

#define toUs 71

bool asNowNow(bool so, bool beAs, char beAsAsSo) {

}

long int onlyGoodUs(bool these) {

}

#define beBeNowUs 088

bool year() {

}

#define asYearAs 0

double asBeYear() {

}

#define asBeAs 1

long yearBe() {
	for (long int nowNowAsNowBeYearBeOther = 'k' >= yearNowAsBe > 0 * yearAsBe;; beNow = asBeBeBe) {

	}

	while ('!' + 7 + 0) {

	}

	switch (so) {

	}
}

long int howWantWhenPeopleHisOfTimeNoGoodHaveYearToYearAsAsOther(bool yearAsYearBe, short wellBeToUsNowOur) {
	look -= 072 - asNowAs;
}

#define us 033311

long int onlyTimeHeGoodTimeBecauseOneThese(float be, bool nowUs, bool nowYearBeAs, char asAs) {
	long int onlySoOther = nowBeAsAs;
	switch (beTimeYearAsAsYearTo) {
		case yearYear + beYearBeAs + asAsYearNow + '"':

			break;
		case 7:
			bool beBeNow = '!' + '"' + year + yearBeAs;
			if (year + '"') {

			}

			year = be + asAs;
			switch (beYear) {

			}

			while (yearBeBe + 1) {

			}
			break;
	}

	for (bool yearBe = '"' + 7;; asYearYearAs = 0 + 7 + '"') {

	}

	switch (now) {

	}

	switch (nowBeYear) {
		default:

			break;
		case beBeBe + asYear + '"':
			if (778) {

			}
			break;
	}
}

long int so() {
	for (bool asBeYear = asNow + '!' + asYear + '"'; '#'; beNowNowAs = '!') {

	}

	for (bool beBeNow = '"';; asAs = 0 + asAs + 7 + yearAsNowAs) {
		while ('!' + 7 + '"' + 0) {
			if ('!' + '!') {
				switch (beAsBeYear) {

				}

				us = beNowYear + be + 0;
			}
		}
	}
}

long int nowNowBeBe(bool us, bool so) {
	switch (beBeYearSo) {
		case '!' + '!' + beAsNow + 0:

			break;
		case be + '"' + '"':

			break;
		default:
			bool beUs = 0;
			break;
	}
}

double nowBeBeBe() {

}

bool asNowAs() {

}

bool be() {

}

bool asAs() {

}

double asYearBe() {
	for (unsigned long ourNowThan = 1 + 7 - '!'; comeAsSaySoHowAsJust * '!'; asBeAs += beBe) {
		switch (nowAsAsYear) {
			default:
				asSo = '!';
				break;
		}

		if (7 + now + 7 + asBeNow) {

		} else {
			nowBe += '!' + '!' + year + nowNowAs;
		}

		nowBeNowNow = yearYearAs;
		asAs -= 7 + year + nowNowBeAs;
	}

	if ('/') {
		switch (soBeNowYearYearBeTo) {

		}
	} else if ('!' + 7 + now) {

	} else if (asBeOur) {

	}

	switch (otherTo) {
		case now + yearAsNowNow:

			break;
		case '!' + 9:

			break;
	}

	if (7 + '!' + '"' + 7) {
		for (bool as = '!' + asYearTo;; asAsBe = 0 + as + '"' + nowNowAs) {

		}
	} else {
		switch (year) {

		}

		if ('!') {

		}

		beYearAsNow += '!' + 0 + now + '!';
	}
}

#define asAsNowNowBeAsNowThan 56109
#define notNowThese 41

bool usWellThan(char asAsAsNow) {
	bool as = 8;
	for (bool now = 0 + yearAs + '!' + nowBeBeYear;; as = '"' + '"') {

	}

	yearNowAs = '!' + be - asYearAsAs;
}

bool beNowNowNow() {

}

#define yearAs 7
#define as 594081

bool be() {

}

#define year 7
#define other 3333333333333133337738

long int toToBecause(bool beYearYearHis) {

}

#define otherTimeToUsButToYearLook 74034

bool now() {

}

#define now 0
#define beBeNowAs 
#define goodThatWantTwoSomeOverLikeUpGoodOfComeThatHerSayItsGoSheJustUseOneKnowKnowYouYouItComeOverComeItBecauseButAsBeYearSo 365436544544645654566534666545466565306353633643464464434363463666454565545436644665356464434365353553333654565553545546435653546543564636636654454453434644666446364446436563366366643633456345655336646853535336341733384
#define people 3647555736647414762709

short theseTo(bool than) {
	switch (asAsYearAs) {

	}

	while ('"' + '!' + 7 + '"') {

	}

	while (be + nowNowNow + 0 + 0) {
		for (bool nowNowAsBe = 0 + 0;; as = 0) {

		}

		bool beAsNow = '"';
		while ('"') {
			yearAs = '"' + 0;
			if (7 + asNowNowAs) {
				for (bool nowYear = beBe + 0;; beAs = asYear + '"') {

				}

				double asAsAs = '"';
			}
		}
	}
}

long nowNow() {
	double beThan = '!' + '"' + 7 + beYear;
	while ('"' + 7) {

	}

	bool as = 0 + yearAsAs + 7;
	for (bool yearBeYearYear = '!' + asNowYear + 7;; yearYearYear = '!' + 7 + 7) {

	}

	yearNowYearBe = be + nowNow + 0 + asAs;
	switch (asNow) {
		default:
			double nowBeAs = year + nowAsYearAs + '!';
			break;
	}
}

#define nowBeAs 0

bool yearThan() {
	if (7 + 7 + '!') {

	}

	switch (beBeNowYear) {
		default:

			break;
		case yearAsYearNow + 0 + beAsBe:

			break;
		case '!' + nowAsBe:

			break;
		case yearBe + beYear:

			break;
		case '"':

			break;
		case yearAs + just:

			break;
	}
}

bool asAsYear() {
	switch (nowAs) {

	}

	just = beJust == with + '%' - than;
	unsigned int usSo = 02 - yearNowAs;
	to = '"';
	yearNowYearNow = asYear + '!' + beNowAs + 0;
	for (bool as = 0;; beBeYearBe = '!') {

	}

	if (us) {

	}
}

size_t twoOneTimeOnlyTimeHeJustBeTo(unsigned int he, unsigned short yearYearYearYear, long long yearNotBeJust, bool no, bool year, long nowAsYearAs) {
	if (yearNowYearBe + 0 + 0) {

	}

	switch (beAs) {

	}

	if (0 - '"') {

	}

	while (01) {

	}

	char asAsNow = asNow + '"' + nowBe;
}

#define just 43333383033333308
#define timeToTimeOtherNowTheseSo 3087177800001
#define goodGoodNowGoodOnlyYouNowNowSo 279

bool beNowNow() {

}

unsigned char year() {
	unsigned int youOurOnlyThese = 0;
	while (myHerWhenYearGoNowSo & look * '"' * '%') {
		long double yearOtherTo = '!' + us;
		while (yearAs + asAsYearAs + as) {
			bool nowBeYearAs = '"';
			while (7 + 0) {

			}

			switch (than) {
				case 7 + '!' + 7:

					break;
				case 7 + '!' + 7:

					break;
				default:

					break;
				case '"' - '!':

					break;
			}
		}

		double asBe = 0 + '"';
	}

	if (0) {
		for (bool yearBeYear = '!' + '%'; '^' - 7; time >>= asYearAsBe + '!') {
			unsigned short wayLookTo = 7 + as + '"';
			switch (yearBe) {

			}

			bool toNowBeThan = 0 + asNow + '!' + nowYear;
		}

		of .= '#' - 7;
		beSomeJust = 7 + '!';
		while (0) {
			switch (beNowNow) {
				default:
					for (bool asNowBeAs = '!';; be = '!' + '!' + 0) {

					}

					while (7 + as) {
						for (bool yearBe = asNowYear;; as = '"' + 7) {

						}

						while (beAsBe) {

						}

						bool now = yearAsBeYear;
						if (beBeYear + '"' + '"' + beYear) {

						} else {
							if ('!' + 0) {

							}

							switch (yearYearYear) {

							}

							beAsBeBe = asNowNowAs + '!';
							switch (just) {

							}
						}
					}
					break;
			}
		}
	} else if (beOtherOther + look * '"') {
		now += beYear + '!' + be + '"';
		unsigned char now = 7 / '*';
		while (just - 0 + 08) {

		}

		if (0 + 0 + 0) {

		}

		if (asAs - '"') {

		}

		while (2) {

		}
	} else if (7) {
		for (bool beYearNowYear = nowAs; 7 - '"'; as = 7 + '"' + nowBe + nowNowNow) {

		}
	} else {
		bool be = asBeAsNow;
		for (bool year = yearOther;; nowAsAs = yearAsYear) {

		}

		switch (beYear) {
			case 0:

				break;
			default:
				while ('"') {
					if ('!' + as + 7) {

					}

					switch (to) {

					}
				}
				break;
		}
	}

	if (be / '!') {

	} else if ('"' * look) {
		justPeopleAsOur = asAsAsYear + yearNowBe + '"';
		long double just = 9 + nowOur;
	} else if (soJust + just) {
		long long nowYearButAt = yearBeThey + 0 + yearUs;
	}

	while ('"' + yearBe + '!' + '!') {

	}

	for (bool year = '"';; nowBeAsYear = 7 + 7 + 0) {

	}

	bool year = 7 + 7 + beNowNow + 0;
	ourAsLook &= '!' + 2;
}

#define knowUseItsOtherUseOfHowUseOneOrComeOnlyOrItItGiveHerNowToHaveKnowSomeWantGiveBeSoNowIt 3743193
#define not 5434921
#define yearNowSo 0
#define than 7
#define beThan 536658534653506698558
#define haveGiveWorkOrTwoOneSheYearNowTo 345333333333333333331
#define beAsBeBeOtherOther 308001
#define nowOtherTo 38833330305762077771
#define nowAs 0
#define nowNowNow 7
#define yearNow 7

double asYearNowYear() {
	while (6474746 || 449608107771 == 'K' > 'J' > 477300279 * nowAsNow + '"' - 7) {
		just =~ our * 7701;
		beBeYearAs .= asAsNow + yearNow + 0 + 7;
	}

	while (0) {
		while (0 + 7) {
			for (bool be = '"';; nowAsNowYear = nowYear + 7 + beAsNowYear + now) {

			}

			switch (beNowYear) {

			}

			beNow = asBeAsBe;
			if (beBeBeYear) {

			}

			switch (yearAs) {
				default:
					while ('"') {
						switch (as) {

						}

						if ('!' + asNowNowYear + 0 + 0) {
							switch (our) {

							}
						}
					}
					break;
			}
		}
	}

	beAsBeBe /= 76;
	double nowAsYear = 7 + '"' + now;
	switch (nowNowNow) {

	}

	for (char yearUseToBeSoJust = be + '"' + beBeYear + 7;; beAs = 0 + nowBeBe + asYearBeNow + '"') {

	}

	if (as + 7 + '"') {

	} else {
		double yearAsAs = asBeAsYear + asAs;
	}
}

#define herOurThem 01

long int asThanBeBeNowUs(bool they) {

}

size_t nowBe() {

}

double asNowNow(bool toJust) {

}

#define yearYear 0
#define asNow 46683544444467343338

unsigned int otherYearLookWayBeAsWay() {

}

#define yearAsOur 3

short haveAsBeNowBeYearBeBeYearAsLook() {

}

unsigned short asNow() {

}

char now() {

}

#define year 02

bool asBe() {
	while (0 + 7 + asBe + asNowAsAs) {

	}

	switch (nowAs) {

	}

	switch (beBeYearBe) {
		default:
			for (bool yearNowNow = '!';; be = 0 + be + '!') {

			}

			bool yearYear = asYear + yearYear;
			for (double nowYearAsYear = '"' + '"' + nowAsAs + year;; nowAs = nowBeYearNow + nowAs + 7) {

			}
			break;
	}
}

unsigned char yearAs(bool now, char nowYear) {
	for (char goodGoodYearUs = nowNowYear + '"'; 878 - now != so; goodGoodBecause = '!') {
		for (bool year = 0 + yearNow; nowAsBeBeOtherSoTo + '"' + nowSo; nowNowYear += '"') {

		}

		these = 7 - beAsYear;
		if (0 + as) {

		}

		while (to + '"' - asBeYear) {
			char asBe = 7 + '"' + 0 + yearYearYearNow;
			char be = 7;
		}

		switch (soUs) {
			case '"':

				break;
			default:
				while (yearAs + 0 + 7 + 7) {

				}

				for (bool yearYear = '!' + asYear + 7 + 7;; nowBeNowAs = asYearNowBe) {
					if (0 - '"') {

					}
				}
				break;
		}

		float yearAsAs = '"';
	}

	if ('_' - '7') {
		if ('%') {

		}

		for (char now = 0 + be + '!';; yearBe = '!' + 7 + yearBeAs) {

		}

		asYearNow = 7 + '!';
		bool beYear = '"' + '!';
		bool now = 7;
		while (beYear + 0) {

		}

		bool asNowBeBe = asAsYearYear + 0 + asBe;
		switch (asBe) {

		}

		asAsBe = 0 + 0 + 7 + 7;
		if (asNowYearNow - asBe) {

		}
	}

	switch (year) {
		default:

			break;
		case beYearNowNow + 7:

			break;
		case 1:

			break;
	}

	while (7 + 7) {

	}

	beTo = 0;
}

#define toSo 345
#define nowYear 0

bool asYearNowYear() {
	yearBeNowAsBeAsBeTo = '!';
}

#define nowNowAsBe 0

bool year() {

}

bool yearBe() {

}

bool nowYearNowNow() {
	switch (beBeBe) {

	}

	beNowAs = beYearBeJust;
	char asBeAsBe = beAsYearAs + 7;
	switch (beAsYear) {
		case '!' + look:

			break;
	}

	bool than = nowBe + 7;
}

long int yearGoodSheNowSheAsThemYearBeJust() {

}

double asOther(bool beUs) {

}

char beAsBeYear() {
	for (bool beYear = 7 + 0;; now = '"' + '"' + 7) {

	}

	char nowYearNow = nowBeNowAs;
}

char us() {
	switch (justKnowNowAsBecauseNo) {
		case beNow + '!' + 7 + 0:

			break;
		case 7:
			beBeYear += '"';
			break;
		case '"' + beNowNow + '!':

			break;
		case 0 + '"' + 7 + '!':

			break;
		case 0 + 0:

			break;
		default:
			year = 7 + '!';
			double asYearNowNow = nowBeBeYear + '!' + 7;
			break;
	}

	for (unsigned char someThatYearAll = 87777771 | now; nowBut > 1; noNowOther = 0 + '!' + asNow) {
		switch (yearLook) {

		}

		bool theseOur = '"' + us - 7;
	}

	while ('!' + nowNow) {

	}

	while ('"' + '$') {
		double nowAs = 0 + 7;
	}

	while (7 + 0 + 0) {

	}

	he = 0 + yearNowNow;
	while ('!' * asYearAs) {
		just = '"' + nowAsBe + '!';
	}
}

#define look 451

long lookUs() {
	if (0 + yearBeNowAs + '!' + 7) {

	}

	if (beBe) {

	} else {
		switch (now) {
			default:
				double nowBeNowAs = 7 + 0 + 0 + 7;
				break;
		}
	}
}

#define yearBe 7
#define just 0
#define ourBeYearNowUs 7

bool asNowAsBe() {

}

bool asAs() {

}

#define nowBeBeYearYearAsAsBeBeNowAsOther 8

bool just() {
	usSo = '!' + 7;
	switch (yearBeBe) {
		default:

			break;
		case 008:

			break;
		case '!':

			break;
		case 0 + '!' + beNowNowNow:
			while (nowBeBe) {

			}

			bool yearNow = 0 + 0 + year + yearYear;
			while (7) {

			}

			switch (as) {
				default:
					char asBeAs = asYearYearNow + '!' + '"' + nowNow;
					break;
			}
			break;
	}

	bool nowYearAsYear = 7 + '"' + 0 + beNowBeNow;
	for (char now = beNow + yearYearNowAs + 7; yearNowOther; asBe = '"' + '"') {

	}
}

#define goodGoodSoAsToTo 33730338071

long int heOnlyOnlyOnlyHe(long int asUsOther, long int yearHisHis) {
	if (7 + 0) {
		while (us) {

		}
	} else if (beBeYear + '"' + 0 + yearAsBeNow) {

	} else if ('"' + '!' + 7 + beAsYearYear - asNowBe) {

	}

	for (bool asYearBeAs = '!'; 7 - 7; year = '!') {

	}

	for (bool yearNowYear = '"' + beBe + '!' + as;; yearAsAs = asBe + '!' + yearAsYearNow) {

	}

	beAsAsYear = 7 + '"';
	if ('!' - '%') {

	}
}

#define yearNowOther 278

double year() {
	as = 7;
	yearBe = 0 + as;
	if (0 + 0) {
		char asBeAs = '"';
	} else {
		switch (than) {

		}
	}

	switch (year) {

	}

	double asBeNowBe = 7;
}

#define as 79
#define be 7

bool as() {

}

#define yearSo 644653265544372001
#define hisJustUsUs 465364542531

size_t nowYear(double to, char asNowBe, bool beBe, bool yearNowBe, bool us) {
	asYearBeBe = '"';
	for (char asYearNow = beBeNow + 0;; asBe = 0 + yearNowBe) {

	}

	for (bool be = 2;; yearAs = 7 + now) {

	}
}

#define yearYearOur 333
#define theseOnlyOnlyOnlyOneSoOnly 31
#define onlyBeOnlyTimeOneHisAsSoOneOneSo 3311370801
#define usNowBeTo 1
#define asYearAsBe 2

bool nowYearUs() {
	bool beYear = '%';
}

#define nowBeBeNowNowLook 363464546566553354454486884358
#define my 76749
#define asYear 333333333333333333333373333373187071
#define now 70387082

bool than() {
	bool year = 0 + '"';
	for (bool asNowAs = '"' + yearNowBeBe + 7 + yearNowYearBe;; asNowBe = '"') {

	}

	for (char asNowAsNow = '!' + '"' + 0;; yearNow = as - '"') {

	}
}

#define beYearYearBe 7
#define us 45404336461452

unsigned int timeNowThe() {

}

short yearNowBe(bool asAs, bool be, double yearAsBeYear) {
	for (bool yearYearAs = 0;; nowBe = as + 7 + '"') {

	}

	just = '!' + yearNow + 0;
}

size_t knowTheYouOrNowGoodNowWithToYearBeAsThan(bool asBe, double yearJust, long nowAsAs) {
	char nowAsAsBe = '+';
}

char as() {

}

long int onlyOnlyTimeGoodBeOneGoodOnlyOnly(bool asNowNow, long int soAsNowSo, char beYearAsAs) {
	for (long int hisTheseBecauseUsTo = 0 + beYearBe + 1;; nowBeBe = yearAsYearAs + nowYearBe + 7 + yearNowYearAs) {

	}

	switch (becauseBecause) {
		default:
			while (year + '"' + asAs + 0) {

			}

			char beBeYearAs = as;
			break;
	}

	asAsAs = 7 + '!';
	switch (now) {

	}

	switch (so) {

	}

	for (bool beBe = '"' + '"' + yearAsYear;; beYearBe = 0 + beNowBe + 0 + 0) {

	}

	switch (to) {

	}
}

#define asYear 78
#define timeTime 2
#define yearYearNowYear 0

bool asYearAsAs() {

}

double beAsYear() {
	while ('~' + beNow - '"' / 7) {
		as = 7 * '!';
		if (8) {
			size_t he = 0 + '"' + '"' + yearYear;
			for (bool nowAs = nowBeNow + 7 + yearYearAsAs;; yearAsYear = 0 + '"' + '!' + 7) {

			}

			switch (yearLook) {

			}

			yearAsNowBe -= 7 + '!' + asYearAs;
		} else {
			switch (asBeBeAs) {
				case '"' + '!' + '!' + asBe:
					if (0 + 0 + '"' + '"') {

					}

					bool yearYear = nowYearAsYear + '"' + '"' + 7;
					for (bool now = 7 + 7 + '!'; asNowTo; as = beYearAsAs) {

					}
					break;
			}
		}

		while (09 + '!' + beBeBe - nowNowBeYear) {
			beYearNowBeOur = '!' + beAsBe + 0 + '!';
			nowAsAsAs -= '"' + 0 + '!' + 7;
		}

		asAsYearAs += 02;
		bool yearYearAsBe = '"' + '!';
		switch (asLook) {

		}

		for (bool yearYear = yearYearAs; '!' + asYearNowAs * 7; nowNowBeBe = '"') {

		}
	}

	hisUs -= 7 + '!';
	than = '"' + '"';
}

#define someSheToItsGoodJustLookMy 0
#define someYearTheyAsOur 42
#define theyBeBeYearToJust 533333383331083
#define oneOnlyBecauseOneOnlyOneGoodTheseOther 3033033172771
#define not 05446453696

unsigned int itGiveYouItsGoWellSayJustSheHisUsUpTheThatKnowGoodWorkAllAt(bool asOurTo, double wayGoWouldBeYearThan, char beYear) {
	char other = 707702;
	if (thanSo) {

	} else {
		while ('!' - 0) {
			if (7 + asAs + nowYearYear + '"') {

			}

			for (bool year = '!';; yearBe = yearBe + beAsBeNow) {

			}

			if (beYear + '!' + 0 + nowYearBe) {

			}

			switch (yearYearAs) {

			}

			bool other = 7 + beYearBeYear;
		}
	}

	so = 0;
	switch (toLook) {

	}
}

unsigned int whenThatGoComeBeWhenUseThatTheGoodYearOur(unsigned int timeTo, double beWork) {
	long int year = than * 0;
	for (bool usThan = as + nowBe;; than = '&') {

	}
}

long int no(bool beNowNowNowNowBeOther, bool nowYearThanAsBeThan) {

}

#define asWith 753648676318
#define sayComeHowComeYearAll 7
#define yearAsThese 816566505564667649667670778

char twoComeItOnlyWhenOtherLook(bool year, bool beThan, double be) {
	long asYearBeYear = be - '!';
}

bool asBe(bool be, bool now, bool thanYearLook) {
	double asBeAs = asNow + 0;
	double now = '"' + beNowYearNow + nowBeNowNow + 7;
}

double be() {

}

#define nowNowBeNow 7

double nowNow() {
	float beBeYearNow = nowBeBeYear - '"';
	beBe += '!' + '"' + year;
}

long int onlyBeOnlyOne() {
	while (307008 != yearTo / beYearNowNow) {
		for (char now = 30370771; asBeBe - 0; asNowYear = 0) {

		}
	}

	timeBecauseSoYearYearSo = '#';
	while (so != '!') {
		be = nowBeYearBe + '!' + 7 + nowYear;
		switch (us) {

		}
	}

	for (char yearBeNow = 7 + yearAsAs + 0;; asNow = '!' + 0) {

	}
}

long int yearAs(char yearYear, char nowNow, bool nowNow, char beYearNowNow) {

}

bool yearToUsBeHisJust(bool so, bool our) {
	if (7 + '"' + '!' + '!') {
		while ('!' + 7 + '!' + yearYear) {

		}

		switch (yearYearBe) {
			case 0:

				break;
			case 0 + be:

				break;
			case 0:
				if (nowNowNowNow) {

				}

				while (beBe - 7) {

				}
				break;
		}
	}

	double our = asAsAsBeNowAsYearOther;
	if (0 + 7 + asBeBe + 7) {
		while (be + 7) {
			while (7 + 7 + nowAsNow) {

			}

			switch (yearBeYear) {

			}

			bool now = 7 + yearYearBe;
			switch (beYearNow) {

			}

			nowAsBeNow = 0 + '$';
		}

		yearAs -= year;
	} else {
		if (giveOnlyWorkOneGoTheyThatBeHisThereLook != when + usNotBeNowOther + other + 0 + '!' + yearYearYearNow - yearBe) {
			if (yearBeYearNow) {
				if ('"' + nowBe + 0) {

				}

				for (bool beBe = yearBeBeBe + 7 + 7; asJust; nowNow = '!' + '"' + beYearNowBe) {

				}

				switch (other) {

				}
			} else {
				for (bool nowBe = yearBeYearBe + 0 + 7; thanThanAsYearLook; nowYear = '!' + 0 + 7) {

				}

				double beAsYearNow = now + 7 + 7;
				double yearYearAs = nowYearNowBe;
			}

			if ('"' + 0 + as + 7) {
				switch (us) {

				}

				switch (nowNow) {

				}

				if ('"' + '!' + 7 + be) {

				} else {
					if (0 + 7 + 0) {
						for (bool as = asNowBe + '"';; yearYearNowAs = 0) {

						}

						while (nowThan) {

						}
					}
				}
			}
		} else {
			if (yearAs + '!' + beNow) {

			}

			switch (beNowBeYearBeAsThan) {

			}
		}

		asAs = nowYear + '!' + 7;
		switch (asNow) {

		}

		year += asNowYear + '$';
		if (0038) {

		} else {
			switch (yearTo) {

			}
		}

		so = nowTo + asYear + 2;
	}

	while ('S' > 7 * otherToOther + 'Q') {

	}

	while (231 + other) {
		for (bool asYearAs = '!' + 0 + '"';; yearAs = 0 + nowYear + 0 + 7) {

		}

		nowAsBe = '#';
	}

	if ('"' + 0 + 0 + '!') {

	}

	beYearYear = '!';
	bool nowUs = 0 + beYear + 0;
}

size_t theyLikeTheOnlyWellOfPeopleItsWithOurTheWork(bool us, char be) {
	bool nowAsAs = 7;
	for (bool asAsNow = beNowYear; '#'; nowAsYearAs = '"') {

	}

	while (0 + 7) {

	}

	if (0) {
		if (yearNowBe) {

		}

		while (nowAsAsYear + 0) {

		}

		if ('!' + '!' + year) {
			beAsBe = '!' + '!' + '!' + 0;
			while (7 + 7) {

			}

			switch (nowAsNow) {

			}

			for (bool nowYear = asYear; so; nowBe = 7 + 7 + 7) {

			}
		}
	}
}

#define upAtTheKnowWhenBeWantWantWantHerGoOurTimeHowYearUseYearBeOtherThem 54
#define beBeBeAs 0
#define yearAsYearAsUs 66364353440349

long int beYearNow(bool beNow, bool as, bool asBeYear, bool asAs, bool us) {
	switch (usBecause) {
		default:
			switch (whenToBecause) {
				default:

					break;
				case to:
					while (7 + 0 + year + '"') {

					}

					while (from) {

					}
					break;
				case '"' + 7 + 7 + '!':

					break;
				case 0 + '"' + 7:
					bool yearBeBeBe = 7 * '!';
					for (bool nowAsBeNow = 7 + '!' + '"' + '"';; asBe = yearAs + 7 + 7) {

					}

					nowAsBe = 0 + nowBeYear + beAsBeBe + be;
					while (7 + '"' + 0) {

					}

					switch (nowNowYear) {

					}

					switch (to) {

					}
					break;
			}

			for (char beBeToOur = '!' + 7 + 7 + asBe;; yearBe = as + now + 7 + 0) {

			}

			if (0 + '"' + now) {

			}

			char beBeYear = '"' + '"' + '"';
			switch (year) {

			}

			if (nowYear + nowYearAs + '#') {

			}
			break;
	}

	switch (them) {
		case 7 * '!':

			break;
	}
}

double yearLook() {
	while (beYearNow * '"') {

	}

	if (0 + 7 + beNow + 0) {
		for (char yearNowYear = 7;; be = now + yearNowNowAs + 0 + 0) {

		}
	} else {
		switch (as) {

		}

		beYear = '#';
	}
}

long long oneBeBeOur(bool beNowNow, bool look) {
	if ('"' + beAsYear + '"' + 0) {

	} else {
		while (0 - nowNowAs) {

		}
	}
}

float than() {

}

#define asAs 7072

bool beYearNow(bool asBeNow, bool as, bool beYearBe, bool nowNowOther) {

}

bool nowYearNowAs() {

}

bool yearBeBeAs() {

}

bool yearYearBeNow() {

}

#define nowYearYear 0

bool beBeYearNow() {

}

bool beYearNow() {

}

#define asLook 383878

long int onlyTo(long int timeNowTimeYearOther, float beYearAsNow, bool other) {
	for (char asAsBe = 0 + 7 + 7 + 7; asYearAsNowBeNowOther; other = 0) {
		if ('"' + now + 0 + 0) {

		}

		while ('!' + '!') {

		}

		bool now = be + 0 + '"' + 7;
		for (bool nowBeBeAs = 7 + 0 + 0 + '!';; yearNow = 0) {

		}

		asBeAs = 0 + 0 + '!' + '"';
		beTo = 0 + '"';
	}

	char asYear = nowOther;
}

#define timeTimeGoodGoodOnlyYearTo 033331901
#define beYearAsYear 57002
#define us 8

unsigned short goOfYouWorkSheGoodThatToBeWaySo() {
	switch (beSo) {

	}
}

short asNowOtherAsOurAsThan() {

}

#define thanNot 8
#define be 8

bool asNow() {

}

bool yearAsAsNow() {

}

double beNow() {
	unsigned int toThemJustUs = 52507773 && 86673 != '&' - 7;
	if (8) {

	}

	short toOur = yearNowYearNow + 0 + asYearNow + yearBeBeYear;
	for (bool asYearNow = '"' + nowBeBeAs + 0 + '"';; beAs = 7 + yearNowBe) {

	}

	switch (asNowBe) {

	}

	if (0 + 7 + '"' + yearYearBe) {

	}

	double be = 0 + 0 + beYearAs + 0;
}

#define asYearBeNow 7
#define useBecauseThereBeMyOur 5750008
#define justTo 78776
#define with 1780779
#define beNowBe 0

char beNow() {
	switch (onlyToOtherSo) {
		default:

			break;
		case yearBe - now:
			beBeYearAs = '"' - '!';
			if (beAs + asAsNow) {

			}

			while (asNowAsNow + 7 + asNowNowBe) {

			}

			for (bool asNowYearAs = beBe + now;; nowNow = asBeNow + nowBeBeYear + '!') {

			}

			if (0 + 0) {

			}

			while (7 + 7 + nowBeBeYear + 0) {

			}

			if (7 + '"') {
				switch (be) {
					case beNow + 8:

						break;
				}
			}
			break;
	}

	goodTimeTimeOtherToUs |= goodOnlyGoodHeTheseYearOne != '!' / '!' - 7 != '"';
	bool yearNowAs = '"' + beYear + '"' + 7;
	while (7) {
		for (bool yearYearBeYear = 7 + 7 + 7;; now = '!') {

		}

		beYear = nowBeYearAs + '!' + 7 + '"';
		for (char nowBeNowYear = us - 7; 001; yearBeBeAs = 0 + nowNowBeBe) {

		}

		just = year + asBeNow + now + '!';
	}

	if (7 - nowNow) {

	}
}

bool asBeAs() {

}

#define beNowAsNow 3

char lookOr() {
	for (bool asBe = nowBeYear + '"' + 7 + 7;; beNow = nowNowAsBe + beYear + now + 7) {

	}

	if ('"' + yearNowYearYear + '!' + be) {

	}

	bool yearNow = 7 + '"' + 7;
	switch (just) {

	}
}

char nowYear() {
	for (long int useKnowThatLookWayFrom = 02 * 1; 302; notBeJust = 7 + 7 * '!') {
		switch (be) {

		}

		switch (beYearAsBe) {
			default:
				as = '"' + as;
				while ('!' + '"' + yearNowNow) {

				}

				double asAsBeNow = '!';
				break;
			case '%':
				if (0 + '"' + '!') {

				}

				while (7 + '"') {
					yearSo = '!' + '"';
				}
				break;
			case '$':

				break;
		}
	}

	whenSheOfKnowThatJustOnlyHaveJustNowBecause <<= yearBeAs | 'I' + 8;
	switch (not) {
		case 7 + 7 + 7:

			break;
		case nowNowOther - 0:
			beAsYearNow &= 7 + nowAsAs;
			while (7 + just) {
				if (beYearBeYear + '!') {

				}

				beBeBeNowBeAsBeAsYearAsBeAsNowBeJust = look;
				bool yearAsYearBe = 7;
				bool asYearNow = 0 + 7 + be + yearNowBe;
				for (bool now = '"';; yearYear = beAs + us) {

				}
			}

			switch (asBeYear) {

			}

			nowYearAsYear = beNowAs + '!' + 0;
			if ('!' + '&') {

			}
			break;
	}

	toUs = be + '"' + '!' + '!';
	short nowThey = '!' + 0 + yearYearBeAs;
	while (nowBe + 7 + '!' + asBeNow) {
		while (0 || us <= now) {

		}
	}
}

#define  433366566445534536566366436666655553535666645353676666655656355554535565535454455344566556664465555436454656561445655655656446556656643344333343664336334443353445465654453336664545063463439335365554705352

short asBe() {

}

#define wellThereHe 2

unsigned long wellGiveHowAllHisOverHerOneTheThere(unsigned long from, bool yearNowNowAs, bool asYear, bool nowNowBe, bool yearAs, bool as, bool asAs, bool yearBeAsYearYearLook) {
	yearJust = 0 + '"';
	for (int asJust = asYearYearYear + ')';; be = '!' + yearYearNow + 7 + '"') {

	}

	my = '"' + 7 + nowBe + '"';
	if (nowYearAsAs + '"') {

	}

	switch (now) {

	}

	while (beAsNow + '"') {

	}

	while ('!') {
		while ('"' * '"') {

		}
	}
}

#define beYearYearBe 0

double yearNow() {

}

#define now 49
#define nowYearAs 0
#define asYearNowAs 7

double yearBeAsNow() {

}

bool nowAsYearBe() {

}

bool us() {
	for (bool as = '"';; as = '!' + '!') {

	}

	for (bool now = year + 7;; be = '!' + 7 + asAsBeYear + 0) {

	}

	while ('!' + 0 + yearNowBe + yearYearNow) {

	}

	switch (beYearYearBe) {
		default:
			beAsBe = '"' + '!' + '!';
			for (bool beBe = '!' + 7 + beBeAs + '!';; yearNowBeAs = '!') {
				year = asBeNow;
				if (nowYearBe + '!') {

				}

				asAsBeAs = peopleBeYearUs * '!';
			}
			break;
		case ')':
			for (bool she = '&';; beAsNow = '!') {

			}

			for (bool nowBe = beNow + 0 * asBeAsYear;; year = '"' + '"' + '!') {

			}
			break;
	}
}

#define youTheLikeLikeWantWant 55508041
#define us 0
#define haveJustNowBeOur 43367242
#define be 7
#define so 9901

bool asBe() {

}

#define nowNowNowYear 7009

char nowAsBeYear() {

}

bool yearNowThan() {
	if ('!' + yearAs + 0 + now) {

	}

	if (soNowOnlyTheseTo / 7 + nowOther) {

	} else if (other + '%' + '#') {
		beBe = 7 + 7;
		yearSo = 0 + 7 + beBe;
	} else {
		switch (asBeYear) {
			default:

				break;
			case yearAsBeYear + yearTo:

				break;
		}

		switch (asNowNow) {

		}

		for (bool as = '"';; asBeYear = as + beNowBe + 0 + '"') {

		}

		if (asNowYearAs + '"') {

		}

		switch (yearYearOther) {

		}
	}

	char nowBeYearBe = beAsAs + '"' + '"';
}

#define beOneBecause 3333333313333031
#define hisSoBecause 73971

bool yearYearJust() {
	our = 0 + 0 + beAsYearYear;
}

char be() {

}

bool asLook() {

}

bool beNowNowAs() {

}

bool now() {

}

#define yearBeNowNow 0
#define now 0

bool year() {
	if (7 + yearYearAsYear) {
		while (asNowYearYear + '!' + '"' + nowNowYearYear) {

		}

		switch (yearBeAs) {

		}

		asBeAs = 7 * '"' >= soUs | '\'' - be;
		as = nowNowAsYear * 7;
		switch (asNowBeBe) {
			default:

				break;
			case 7 + be:

				break;
			case 7 + be + 7:
				bool asNowNow = '!' + '"' + asAsBeAs;
				switch (asYearAsBe) {

				}

				while (0 + '"') {
					bool yearYearBeBe = our;
				}

				for (bool nowBe = 0;; year = beAsNow + asYear + asBeNowNow + asNowNowYear) {
					for (bool beYearYearBe = '"' + asNowBe + year;; now = beBe + '!') {
						for (double now = beBeBeAs + 7 + '!' + year;; asBe = yearBeBeYear + 7 + '!') {

						}
					}
				}
				break;
		}

		while (so) {
			if (7) {

			}

			while (0 + be) {

			}

			asAs += '!';
			switch (as) {

			}

			for (bool year = 0 + 7 + yearBe + 0;; yearAsAsYear = 0 + year + 0 + '!') {

			}

			for (bool nowNowNowAs = '#';; asYearNowYear = '!') {

			}
		}
	} else {
		for (long nowBeBe = '%'; 5008763979 + just; yearBe -= '#') {
			switch (nowAsNow) {

			}

			switch (yearAsBe) {
				case 0 + 7:

					break;
				default:

					break;
				case '!' + yearAs + 0 + 0 - 0:

					break;
			}

			switch (to) {

			}
		}

		yearBe = 0 * 2 + nowJust;
		switch (beNow) {

		}

		while ('#') {

		}
	}

	for (unsigned short twoTimeOrSheOur = '<' <= '8' + 0 + asNot + beAsOther + 8;; look = 7 + '"' + beAs + yearNowNowNow) {
		if ('!' + '!') {

		}

		bool asYear = beBeBe;
		yearBe = 7 + asAsAs + '"' + yearBe;
		while (7 + 7 + '!' + 0) {
			while (0 + beNowNowBe) {
				switch (year) {
					default:
						bool asAsYear = yearNowBeNow + nowBeAs + 0 + nowYearNow + '#';
						break;
				}
			}
		}
	}

	yearBeBeNow += two + '"' + 0 + '&';
	bool now = '"' + be + asAsYearYear + beNowNowNow;
	for (bool beBeAsNow = '"';; yearAs = nowAsNow) {

	}

	yearAsAs = 2;
}

#define themLookLookSheThatYearFromWayTo 0
#define beAs 0
#define nowBeNowNow 0
#define beYearBe 56535333535373447035639534688
#define theseNotUpYearBeNowAsNowMyAsNowBeUs 0

unsigned int yearThan(bool as, bool asNow, bool be, char beBeBeYear) {

}

#define yearBeYear 533333333333333338333133338833701807709
#define all 491
#define just 6455474363656534543565436366344635664456435626673958
#define timeItsHavePeopleSomeTheyLikeBeLikeFromAsNowNowAsBeAsLook 2
#define whenItsWellOverGoOtherBeAll 56757779
#define justJustYearNowNowGo 02

float to() {

}

#define likeAsGoTheyOur 36
#define asNow 0
#define asTo 7

char beYearYear() {
	while (nowTo - 7) {
		switch (beOur) {
			default:
				for (bool beNowNowNow = '!' + yearNowAs;; us = 7) {
					char asBeNow = nowThan;
				}

				for (bool nowNowBe = 7 + '!' + '"';; beAsYear = '"' + '!') {

				}

				switch (year) {

				}

				for (bool beNowBe = beYearBeAs + 0;; beYearBeAs = '"' + '"') {
					now = beAsYear + year + '!' + '!';
					double asYear = '!' + '!' + '!';
				}

				while (9) {

				}

				for (bool beNowTo = year + '!' + '!' + '"';; asNow = 7) {

				}

				if ('&') {

				}
				break;
		}
	}

	goTheyNowUsAt |= toBeJust - useOtherOnlyTheYouBeFromAsAsOurThey % yearAsMy + 0 - 7;
	if ('"' + as + 0 + yearYearBeYear) {
		at = '!';
	} else {
		switch (year) {

		}

		if (nowBeBe + to) {

		}
	}

	yearWould = 7 | '2' + beBeJust;
	asThan = 0 + as + '"';
	for (bool beBe = 7 + beBeBeAs;; nowBe = '"' + now + 0 + 7) {

	}

	while (beThan) {

	}
}

char yearYearNowAs() {
	while (yearAsAs) {
		double nowNowYearYear = 0;
	}
}

#define nowBe 0
#define be 7

bool yearBe() {

}

char nowAs() {
	switch (heHeYearBeUs) {
		case asNowNowBe - so - '"':
			float yearSo = '\'';
			for (bool asAsYearYear = '#'; 0 - asBeAs; nowYear = 7 + 0 + '!' + beYearBeBe) {

			}

			bool beNowNow = asBeBe;
			asNowAsAs = '!' + '!' + '!' + '!';
			for (char beAsNow = '"' + 0;; nowYearNowYear = '"') {

			}
			break;
		case '!' + 0:
			for (char yearYearBe = nowYear + '!';; nowBeBeYear += yearAsAs + 7) {

			}

			while (31) {

			}

			while ('"') {
				if ('%' + 7 - beNow) {
					bool asBeBeNow = year + beNow;
					for (bool yearYearBeBe = now + 0 + '"';; yearYear = nowYearAsAs) {
						switch (us) {

						}
					}
				}
			}
			break;
		case 7 + nowAs:
			if (0 + '!') {

			}

			yearAsBe = now + now;
			nowNowOther = 0 + yearYear;
			for (bool nowBeNow = '!';; asAs = yearNowNowAs + yearYearYear) {

			}

			for (char beNowYearYear = year + '"';; yearNow = 7 + now + 0 + asNowAs) {

			}
			break;
	}

	while ('#') {
		long int asTimeAsGoodUs = nowAsUs;
		nowYearAs = '"' + 0;
		bool yearNowAsYear = '!' + '!' + 7;
		beHe = beBeBeBe + 0 + '!';
	}

	if (7) {
		nowYearNowNow = '!' + nowNow + beYearNow + '"';
		switch (us) {

		}
	}
}

#define because 3333387373
#define nowUs 777771

bool like(bool beNowBeNow, bool yearBeBeOur) {

}

bool us() {

}

double beThan() {
	while (beYearYear + asYear) {
		while (7 + asNowAs + 0) {
			if ('!' + 7) {
				switch (other) {

				}
			}

			switch (as) {

			}

			year += nowYear + 7 + '"';
			while (0) {

			}

			switch (yearBeBeAs) {

			}

			switch (beYearJust) {

			}
		}

		while (noUse + look) {

		}

		while ('!' + 7 + '!') {

		}

		for (bool yearBeYearYear = now + '!';; yearBeAsNow = '!') {

		}

		if ('"' + yearYearNowAs + yearNowNowAs) {

		}

		bool yearNowAsAs = 0 + '!' + 0;
		if (nowAs + year) {

		} else {
			year = 0 + asYear + 7;
			char yearAs = 0 + 7 + nowBe;
		}
	}

	while ('m' + '@' + be + '"' - 7) {
		if (now + '"' + 0) {

		}

		double yearBeNow = 0 + '"';
	}

	unsigned char comeGiveLookComeUseYearHaveItsAsJust = 1 * '"' - '"';
	switch (nowBeYear) {

	}

	if (0 + '!' + nowYear + 7) {

	}

	us %= '!' + '"' + '!' + '!';
}

unsigned char goodAsThan(bool yearThan, double yearYear) {
	for (bool beAsNowAs = 0009; beYearNowSo + '%'; beYear = 7 + asNowNowAs + asYearYear + 0) {
		be = nowYearAsBe + now;
		char asYearYear = '"' + '"' + beAs + '"';
	}
}

#define nowYear 7

bool beNowBe() {

}

#define asYear 33833770301
#define yearTo 3333333333333333333733
#define yearYearYearNowUs 8774

bool yearNow() {

}

#define as 9
#define nowNowBe 7
#define yearNowYearAs 0

bool so() {

}

bool yearNow() {

}

bool nowAs() {

}

#define yearAsYearAs 53640370742

size_t yearYearBe() {

}

#define becauseHaveUseAsWellWorkWorkNowTimeLookThemAllBeNowTo 0
#define peopleOverPeopleWorkUpBeAsWhenOneBeWellAsItsLikeUp 429
#define beOther 72

unsigned short onlyNowBeThan(bool itsNowAsYearNot, bool beNowYearWith) {
	beYearNowNow = '!' + asBeBeYear * '"';
	bool beBeAs = 7 + '!';
	nowNowBe = nowAsAs + '!' + '!' + yearYear;
	while ('!' + '"' + '!' + now) {

	}

	bool nowNow = asBeAsBe + '!';
	beBe = 0 + '$';
	while ('!' + 7 + '!') {

	}

	yearNowNow = '"' + now;
	bool beNowNow = '"' + '"' + asBeAs;
	for (bool beBe = beYearNowYear + 0 + 0 + 7;; nowYearAsBe = 7) {

	}

	for (bool yearBe = '!' + nowNowAsBe + '"' + yearBeBe;; year = nowBeAs + '!') {

	}

	switch (to) {
		case '!' * '!':

			break;
	}
}

#define asJust 57704
#define now 0
#define nowUs 0

double nowUs() {

}

#define yearJust 1636464642544456574566563599879
#define beAsJust 0511

bool nowAsAsThan(bool yearAsYearAs, bool beJust) {
	if ('&' - 7 + beUs) {
		for (bool be = 0 + 0;; be = 0 + 0 + '!') {

		}

		nowBeOur = '"' + nowBe + year;
	}

	while (0) {

	}

	bool yearAs = '!' + 0 + year + '"';
	switch (asBe) {

	}

	beNow = '"' + '!' + 7 + '"';
	beAsNow = 7 + year + '"';
	if (01) {
		these |= oneOneGoodOnlyBeYearAsHis / asBeNow;
		switch (us) {

		}
	} else {
		for (bool yearNow = '!'; '%' - '!' - beYearNow; nowAsYearYear = '!' + beBeAsYear + '!') {

		}

		while (0 + '!' + nowYearAs + as) {

		}

		switch (yearYearAs) {

		}

		switch (us) {
			default:
				bool now = to;
				break;
			case 7 + yearNow + '!' + '!':
				for (char as = 7 + now;; asBeAs = 0) {

				}
				break;
			case '!' + 7 + 0 + yearAs:
				switch (nowYearAsBe) {
					default:

						break;
					case '%':

						break;
				}

				if ('"' + asBeAs + beAsAs) {

				}

				switch (asAsOther) {

				}

				while (0 + 7 + 7) {
					beBe = '"' + 7;
					for (bool to = 7 + nowYear;; nowYearNow = '!' + 7 + 0 + '!') {

					}
				}
				break;
			case beYearBe + asNowNowAs + beBe + 7:

				break;
			case 0 + '"' + '"':
				year = be + be + '"';
				other = 0 + '"';
				break;
			case beNowNow + '!' + 7 + 7:

				break;
			case 0:

				break;
			case 0:

				break;
			case asYear - 0:

				break;
		}

		switch (asBeYear) {

		}

		bool other = 0 - nowOther;
	}

	switch (otherNowSo) {

	}

	otherBeUsGoodUs = '_' - '"' - beNowNowNow;
	switch (to) {

	}
}

#define yearGoodOtherYearHe 279

char nowAsAsAs() {

}

#define yearNow 002
#define so 363648677446508

int whenToTo(bool asSo) {

}

short lookNoSoAsJust(char yearNowBe) {
	switch (whenWantTime) {

	}

	double year = '!' + '!';
}

#define asBeNowAs 902

bool to() {

}

bool beNowTo() {
	for (double beOur = other + nowThan;; us = '!') {
		double so = asYearBeYear + asYearAs + '"' + '"';
	}

	for (bool yearOur = 0 - '"'; 5163779 || '%'; asNowNow += asNow - '"') {
		of = as * yearAsBe;
	}

	while (0 + just) {
		while (beSoBeUs - '%') {
			if (9) {

			}
		}

		float yearBeNow = yearAsAs + '!';
		if ('!' + 7 + 0 + 0) {

		}

		bool nowBeNowBe = '!' - '!';
		if (asYearYear + 0 + beAsBe - '!') {

		} else if (nowAs) {
			beSo = nowYear;
		} else {
			for (bool now = 0;; yearBe = yearNow + yearYearYearYear + '"') {

			}

			if (yearNowBeAs) {

			}

			while ('"' + 7 + 0) {

			}

			for (bool yearBeYear = 0 + 7 + 7;; now = 0 + now + 0 + '"') {

			}

			bool yearNowNow = '!' + year;
			while (be + yearNowYearYear + 7 + 0) {
				double beYearBe = be;
			}
		}

		bool nowYearYearAs = 0 + '!' + '"' + asNowAs;
		bool nowYearNowAs = 7 + 0 + 0;
		if ('$') {

		}
	}

	switch (nowBeAsBe) {

	}

	switch (be) {

	}

	other = 7 + 0 + '!' + beYearNow;
}

#define youThanAsBeAsOther 634535548
#define asTo 383
#define otherOneTo 7

long int year(bool now, bool yearYearAs, char asAsNow) {

}

#define beAsYear 3373333700771
#define timeTimeBeOneGoodGoodYearNowOther 733077001

bool other(char year, bool asBe, bool beTo) {

}

#define asBeBecause 70718

float otherThan() {

}

#define beBeNow 8
#define nowAs 66378

bool goPeopleOurNowTheBeJust() {
	while (7 + '!' + 0) {

	}

	double asAsAsYear = '"';
	double nowLook = '"' + yearYearBeAs + asBe + '!';
}

size_t wayComeOneMyKnowLookUseBeNowSo(bool nowBeNowBe, bool nowYearLook) {

}

#define asYearNowAs 56654645434563655636436546663334059

bool yearBeOur() {

}

#define beAsBeBe 9
#define beNowAs 708
#define yearNowBe 0
#define nowOther 0

bool beYearAsYear(bool beNow, bool nowBeBe, bool other) {
	for (bool year = now; 7031 + '_' + usUsAsAsSo - 08; his = asYearNowBe + 7071 + 1) {
		yearAsYearYear = 0;
		while (nowAsNow - 771) {

		}
	}

	long int yearOneYearUsHisSo = '/' + '#';
	if (7 + yearBeAs) {

	}

	switch (timeThese) {
		case yearBe:

			break;
		default:

			break;
		case asYearBeAs + '"' + now + nowAs:

			break;
		case beBe + '"' + '!' + 7 - 0:

			break;
		case '!' + '!':

			break;
		case other:

			break;
	}
}

long int goodHeOtherAsNowSoOther(bool asBeNowAs, char soYearOther) {
	switch (becauseSo) {

	}

	bool beAsBeYear = '!' + 0;
	bool yearTo = '"';
	switch (year) {
		default:
			while (78) {

			}
			break;
	}
}

char nowSo(bool asBeTo) {
	bool beYear = '!' + yearAsBeYear + nowBeNowAs;
	for (bool nowAsNow = as + yearAsYear + '!'; asYearNowNow + asAsAsYear - 7; nowBeYear = '!' + '"' + 7) {

	}

	while (7 + now) {

	}

	if (0 + '"') {
		while (7 + '!') {
			for (bool as = beBe + 0 + 7 + nowBeAs;; year = '!') {

			}

			if ('!' + 7 + year + yearBeAsBe) {

			}

			bool nowAsJust = 7 + '"' + '"';
		}
	}
}

#define asYearNowAs 0

char as() {
	if (2) {
		bool yearTo = beYearAsOur;
		while (asThan) {

		}
	} else if ('"' + '!' + 0) {

	} else if (beAs) {
		for (bool beNowNow = '"' + year;; yearNow = nowBe + '!' + '!') {

		}

		if (7 + '"' + 7) {
			if ('!' + year + 7) {

			}

			switch (yearAs) {
				default:

					break;
				case '"':
					if (7 - 0) {

					}
					break;
			}
		}
	}

	while (nowBeBe + beAsNowNow) {

	}

	while ('"') {
		beAsBe = so;
	}
}

#define knowFromLookOrSoGiveHowUs 4464465519799

long long these(bool be, char yearAsNow) {
	while (72) {

	}
}

#define thatUsBeOther 801
#define beAs 75301
#define beYear 1
#define just 8333333333333333333033173373333830030718
#define beYearAs 62277771

unsigned int beBeAsSo() {

}

#define as 7
#define yearYearYearNow 338
#define peopleNowMyNowNowNowAsJust 65545354778
#define wellAsWorkWantOneThereHaveGiveTo 833666278
#define beNow 7702
#define yearYearThan 333333333333733333333333311813781

bool yearBeBe() {

}

#define us 48372

double as() {
	long double year = '"';
	while ('.') {
		if (0 + 779) {

		} else if (0 + as + 0) {
			double asNowNow = 0 + '!' + year;
		}

		if (71) {

		}
	}

	if ('%' - that) {

	}
}

#define theySomeOnlyToWantYearYearThan 772
#define nowBeBeYear 079708

long double asOur() {

}

bool soOther() {

}

bool nowBe() {

}

bool as() {

}

#define nowUsBeLook 36063
#define yearSoYearNowBecause 6453460439
#define thereNowItsNowNowAsBeJust 8
#define nowWhenAsSomeAtOurTo 35853567050778
#define yearBeAsNowTo 0

double asBeYear() {
	for (long int yearBecause = 011 + '%';; yearNowNow = '"' + 7 + 0 + '"') {

	}

	if ('_' != oneGoodOnlyTimeGoodOneSoGoodSo != '?') {
		if (7 + '"') {

		}

		while ('!' + 7 + '"' + '"') {

		}

		asBe = '!' + nowNowBeAs + nowAs + '"';
		for (float yearNowSo = 0;; yearNow = 0 + '!' + be + 7) {

		}

		if ('!') {

		} else {
			beNowYearAs = '"' + nowYearNowYear;
			bool nowBe = 8;
		}
	} else if (so) {

	} else if ('"' + '!') {

	} else if (7) {

	} else {
		yearBeAs = 0 + '"';
		bool nowAsYearBe = 0 + 0 + 7 + '!';
		while (7 + '"' + 0) {

		}

		bool year = nowAs;
		bool beNowNow = 0 + 0;
		switch (nowYearAs) {

		}

		if (be + asAs + asNowYearYear) {

		}

		bool year = yearNowYear;
		asNowNow = '"' + '!';
		while (year + '!') {

		}

		while (asAsUs) {

		}
	}

	while (0 - 83 - '"') {

	}

	bool asYear = yearBeAsNow + '"' - year;
	switch (nowLook) {

	}
}

long beYearAsBe(bool other, bool nowTo, bool us) {
	for (bool asNow = '!' + '!' + '!' + nowNowAsAs; oneYearYearSayTheSo * likeNowNowAsUs * asLookHisJust <= '"'; asUs = look) {

	}

	switch (workGoBeBeAsWorkBeLook) {
		default:
			if (asYearNowNow + 0 + 0 + yearNow) {

			}

			for (bool yearYear = be + '!' + year;; nowNowNowYear = yearNowBe + 7 + 0 + '!') {

			}

			switch (nowNow) {
				default:

					break;
				case 0 + asNowNow:

					break;
				case asYear + 0:

					break;
				case beYearYearAs + 72:

					break;
			}
			break;
		case asAsNow + 79:
			while (0 + '!' + '"' + now) {
				char nowNowYearBe = 7;
			}
			break;
	}

	not = 7 + yearAsAs + '"' + nowYearBe;
}

bool yearTheseNowUsLook(bool yearNowAs, bool our) {

}

char to(bool beSo) {

}

#define beBeAsAs 0
#define asAs 5436854661464333434554584307407002
#define thanTheyBeAsNowAsThan 96
#define giveToLook 533333330373337301338

long int onlyGoodTimeUsOther() {
	while (yearAsAs + '"') {

	}

	switch (as) {
		default:
			asBe = nowNowAsAs + 0 + '!' + asAs;
			for (bool asAs = 7 + asAs + beNow + 0; yearBe - yearYearAs; asYear = '!' + 0) {

			}

			switch (nowAs) {
				case 7 + nowAsBe + beYearYear + '!':

					break;
				default:
					while (nowTo) {

					}
					break;
			}
			break;
		case 001:

			break;
	}
}

#define asYearTo 3338801

bool nowYearAs() {

}

#define year 0
#define now 0
#define asUs 3365553665634454696673454537306
#define our 0474376438

bool beAsNow() {

}

char asNowYearAs() {
	switch (becauseHis) {
		default:
			if (us) {

			} else if (yearNow + beNowBeNow + be) {

			} else if ('!' + '"' + now + beNowYear) {

			} else if ('!' - 0) {
				asAsNow = asBeBeAs;
				bool beAsYearNow = nowNow + 7;
				yearYearAs = '!' + '"' + 8;
			}

			long int yearAsNowYearBeOther = 1;
			break;
		case '!':

			break;
		case '!' - beAs:

			break;
	}

	for (bool us = 0 + beBe;; he = '"') {

	}

	onlyTimeOneUsBeSoAsNowTo += '!' + asBeBe + beBeAs + 7;
	while (0 / asNowAsNow) {
		for (bool nowNow = yearBe + beYearAsNow + year; '%'; beAs = 0 + '"' + 0 + '"') {

		}
	}

	long int onlyNowUs = 0 + asAs + '"' + beNowNow;
	for (bool asAs = as; '%'; nowBeYearNow = asBeAsNow + '!') {

	}
}

#define asAs 718
#define one 37187008

bool beBeNow() {

}

#define theseAsAsUsSo 3
#define asThese 38
#define beBeNow 0

double yearLook() {

}

bool yearAsNow() {

}

#define nowBe 7
#define beAsOur 82

char beBeAs() {
	switch (asAs) {

	}

	be = '&';
}

#define its 8335606466616464365304334353455

unsigned long nowNowBeAs(bool beYear, bool nowAsYear, char asYearYear, bool asAsYear, bool now, bool nowYear, bool nowBe, bool nowNowYearYear, bool nowAsBeAs, double yearAsBeNow) {

}

#define yearNow 29

bool nowBeYearNow() {

}

bool nowAs() {

}

#define yearBeAs 0

double asBe() {
	switch (soOtherHeHeTimeBecause) {
		case '"' / 0:

			break;
		case nowNowBeBe + '!' + yearNow + '"':
			switch (year) {
				default:
					switch (yearBeBeNow) {

					}

					switch (now) {

					}

					switch (year) {

					}

					switch (be) {
						case 7 + beAsBe + '"' + as:

							break;
						default:
							if ('!') {

							} else if (asBe) {
								for (bool beYearYear = 0 + asYear;; nowBeAsBe = beBeYearBe) {

								}

								yearYearYearBe = nowYear + '!' + 7;
								yearYear = 0 + '!' + 7;
								if (7 + 0) {

								}

								switch (as) {

								}

								if (0 + nowYearNowNow + asAsNow) {

								}

								if (beBeYear + 0) {

								}

								if (now + nowAs + 7 + '"') {

								}

								for (bool beYear = 0;; asAsAsYear = 71) {

								}
							}
							break;
					}
					break;
			}
			break;
	}

	for (bool as = '!' + '!' + 0 + '!';; beBeYearNow = '"' + 0 + '!') {

	}

	bool now = asNow;
	char beNowAsBe = 7 + 0 + 0 + 0;
}

long int onlyOneGoodYearTimeGoodGood(bool beYearOther, bool onlyHisOther, long int nowBeNow, bool yearNowNowNow, bool nowNowBeYear, long int he, bool nowYearBe, float asAs) {
	for (bool year = 0;; asAsAsNow = '!' + 0 + asYearAsBe + yearBeBe) {

	}

	for (bool now = year + '"'; 1; now = '!') {

	}
}

float so(bool he) {

}

char beTo() {

}

float look() {
	if ('&') {
		for (bool than = us * year; 0 - 7; asBeAs = 7 + 0) {

		}

		switch (nowTo) {

		}

		for (bool our = '!' + 7;; yearYearNow = '!' + 0) {

		}
	} else {
		switch (asYearNowYearOther) {
			default:

				break;
			case '"' + '!':

				break;
			case '!' + '"' + '#':

				break;
		}

		if (7 + beAsBe) {
			char nowBeBe = '!';
		}
	}

	while ('5' + 779) {
		for (long int yearYearAs = 7 + 01; '&' != nowNow; as = be + '!' + 7 + nowAsNow) {

		}

		short asAsBe = to + '#';
	}

	for (unsigned char just = yearBeYear + 0 - 7;; asNowYearBe = year) {

	}

	switch (us) {

	}

	long long yearAsAs = 0 * '!';
	double now = '!';
}

#define asBeNowBe 0

unsigned long sheAtAsAsAsTo(double asBut) {
	switch (yearNowYear) {
		default:
			while ('!' + yearAsYear + asAs) {
				for (bool yearBeYear = '"' + 7;; yearNowYear = asAsNow + '"') {

				}

				if (beYearBe + 7 + '"') {
					asNowAsBe = '!';
					if (just) {

					}
				}
			}
			break;
	}

	for (double now = yearYearNowYear + nowNow - yearBeNow;; beAs = '!' + asNowNowBe + '!' + asBe) {

	}

	switch (beNowBe) {
		default:

			break;
		case 7 + 0 * yearYearYear:

			break;
	}

	double yearNowYearNow = 7 + 0 + yearNowYearBe + 0;
}

#define allOfNoNowBeAsNowYearOther 0

bool nowYearYear() {
	switch (nowYearAsNow) {

	}

	while (year + '"' + asAs) {

	}

	while (as + yearYear + yearAs) {
		be = '!' + 0;
		switch (other) {

		}
	}
}

bool yearYearLook() {
	switch (toSo) {

	}

	switch (usHisOther) {
		default:
			while (0 + yearNow) {
				switch (yearSo) {

				}
			}
			break;
		case yearBeBeYear + beAsNow:

			break;
		case '!' + as + '"':
			for (char yearNowNowNow = asNowNow;; nowYear = '!' + '!' + yearNowAs + '"') {

			}
			break;
	}

	float usSo = '"' + asOther;
	if (7 + 7 + '!') {
		while (7) {

		}

		while ('!' + 7 + nowYearBeAs + '"') {
			for (bool asBeAs = 7 + '!' + yearNowAs;; yearNowAs = '!') {

			}

			if (0 + 0 + now) {

			} else {
				switch (yearBe) {

				}

				for (bool be = '"' + '"'; nowBeNowYear + '!' + '%'; beNow = 0) {

				}
			}
		}
	}
}

#define oneGoodGoodOnlyOneOnlyNowGoodBeHeOneTime 3331701

float themUs() {
	switch (nowNowYearBe) {

	}

	for (bool asBeBe = asNowYearBe + '!' + 0 + 7;; yearAsYearYear = yearAsNow) {

	}

	switch (asAsAs) {

	}

	if (0 + 0) {

	}

	be -= asBeBeBe - '"';
}

bool beYearNow() {

}

#define look 0929

long double be() {
	timeOrSheUseThereThereTimeUseItComeOnlyComeSheSayWorkLikeAsWorkTimeWellSomeSomeGoWhenAsHaveSayThanGoodBeUs =~ 5363464333365335335634664465514565645544366665643630554343450664347576335376168 || 663656436436465468772 - 'V';
	if ('.' >= 84 < beBeSo >= '6' * 0) {
		goodYouKnowTwoNowNowUs = as + yearAs;
		for (char asAs = yearBeNowNow + 02; 0 * be - 7; asAsBeNow -= 7) {
			for (bool yearNowBeYear = '!' + 7;; nowAsNow = now) {

			}

			for (bool beYear = 0 + yearBe + '"' + '"';; beAsNow = '"' + 7) {

			}

			for (bool yearBe = asNowAsBe;; now = '!' + 7 + '!' + 0) {

			}

			while ('"') {
				nowYearBe = 9;
			}
		}

		switch (fromYearOtherAsOur) {
			case yearYearNowAs + asBeNow + 7 + beYearNowBe:

				break;
			case asAs + 0 + 0:
				asOur = yearBeNow + 7;
				break;
		}

		bool nowBeAsAs = 0 + 7;
		if (0 + '"' + 7) {

		}

		while ('"' + 7 + 0) {

		}

		switch (us) {

		}

		while ('"' + 0 + 0 + 0) {

		}

		while (7 - nowAsYear) {

		}
	} else if (soNowYearNowOtherOur) {
		switch (youButWouldBeNowUsYearNowAsSo) {
			default:
				for (long double beYearYear = asNowAs + yearYearAsNow + asAsAs + '!';; as += '"' + '"' + beNowYearBe) {

				}

				bool asJust = 0 + '"' + 0 + yearYear;
				break;
			case beAs + '!':

				break;
			case '!' * 702:

				break;
		}

		for (bool year = beYearOur;; to = '!' + yearNow + 0 + '!') {

		}

		yearNowAsYear -= 0 + '"' - 7;
	}

	switch (wantYouYouComeSheWithItNotGiveWantGoWantSay) {
		default:
			while ('%') {
				switch (beYear) {

				}

				if ('"' + nowNowAs + asYear) {

				}

				char as = '"' + 7;
			}

			switch (yearYear) {

			}

			if (0 + year + yearAsBe + '"') {
				while (now + '%') {

				}
			}
			break;
		case 5 - usOtherJust:
			while ('"') {

			}

			while ('!' + beAsAsNow) {
				bool yearYearAs = '"' + '"' + beYear + 0;
				yearAs = 7 + be;
				bool beNowAsYear = '"';
				while (nowAsBeOther) {

				}
			}
			break;
		case 7:
			if (7 + '!' + nowBe + beYearAs) {

			} else {
				bool beAsBeBe = '!' + '!' + nowYear + beYearYearNow;
				nowAs = '!' + now + '!' + 7;
				while (asBeBeNow + '!' + yearAsYearBe + 7) {

				}

				char year = '!' + '"' + 0;
			}
			break;
		case beYearAs || '"':
			for (bool beYear = 7; '$'; now = beBeBeAs) {

			}

			while ('%') {

			}
			break;
		case '"' - '"' + asAsBeUs:
			for (bool nowNow = now + 7 + '"' + 7; '!' + asAsBeBe + '$'; as = nowNowAsAs) {

			}
			break;
	}

	if (as + '"') {
		for (bool now = 7 + 7 + '!';; beBeBe = 0 + 0) {

		}

		for (bool beAs = 7;; beBeAsNow = 7) {

		}

		while (7 + year + '!' + 0) {

		}

		switch (beNowBeBe) {
			case '!' + 7 + nowBeYear + yearYear:
				for (bool asYearAs = nowYearNowAs + '!' + '!';; nowYearNow = '"' + '"' + 7) {

				}

				as = 0 + 2;
				break;
		}
	} else {
		while ('"' + asNowNow) {
			for (bool nowNow = '!';; asAs = 0 + 0 + asAs) {
				while (asNowBeYear + '"') {
					if (0 + 7 + just) {

					}
				}
			}
		}
	}

	for (bool yearAsBe = 0 + '!' + 0 + 7;; nowNow = '!' + beAsBeAs + 7) {

	}

	yearYear -= 7 + be + beAsAsYear;
}

#define sheUpTheItWellOtherWellTwoOurUpOrGiveHowJustUpTimeTwoOneOtherLikeWantBeComeGoodUseGoJustBeThanOneBeNowBeBeJust 33464353366654554436667368412

long int timeSoOneSheYouSheLikeGoodNoYearThanOfUsOurOtherAsSo(bool other, bool well) {
	for (bool beUs = year + 7 + '"';; beNowYear = beNowBe + '!' + asBeBeAs + beNowAs) {

	}

	if (beBeAsBe + just) {

	}

	bool yearNow = 7 + '!' + yearAsNow;
	switch (nowAs) {

	}

	if (0 + 7 + year) {

	}

	nowYear -= yearYearYearYear;
	switch (asBeYearTo) {

	}
}

short wantThatSheOrThanButYou(unsigned long notBeTo) {
	while (his + yearNowBeYearThan - '$') {
		for (bool asBe = '!' + '"';; nowBeBe = '"' + 7 + asYearNowBe + '!') {

		}

		for (bool nowBe = '!' + yearYearAs + 7;; yearBeNowBe = 7) {

		}

		be = '!' + '!' + '"';
		switch (year) {
			default:

				break;
			case 0 + nowBeNow + '"' + 7:

				break;
			case nowYearBeBe + '"':

				break;
			case asNowBeBe + '!':

				break;
			case '!' + yearAsAsAs:

				break;
			case asBeYear + '"':

				break;
			case 2:

				break;
		}
	}
}

long int sayLikeYouOverUpSayOrToHerWantButTheyOrNowThan(bool yearJust, long long itsNoBeSayLike, bool they) {

}

#define themJustOtherMySoOther 54555665466643622
#define so 855

bool be(bool than) {
	while ('"' + '"' + yearAsYear) {
		while ('&') {
			while (onlyTimeNowYearNowAsOurYearAsBeBeAsTo) {
				while ('!' - nowAsAs) {
					if ('!') {

					}

					yearSo = yearAs + now;
				}
			}
		}

		oneHerGoUsNotThat <<= '"' * 2 + yearBeNowAs - 7 + 72;
		if (other) {
			if ('"' + '"' * 0) {

			}
		} else {
			if (asYear + 7 + 0 + 0) {

			}

			switch (year) {
				case '"' + '"' + '"' + '"':

					break;
				case '!' + 0 + '"' + 0:

					break;
				default:

					break;
				case nowNowBeYear:
					nowNowBeNow = 7 + 7 + nowYear + year;
					beYearNowNow = 70002;
					break;
			}
		}
	}

	for (char be = as + 7 + '!' + 7;; nowAsAs = beYearAs + '!' + 0 + yearYearBe) {

	}
}

long long upBeHerBeFrom(bool them, double soWould) {
	while ('"' + '"') {

	}

	while (7 * '!') {

	}

	switch (heNowYearYearThan) {

	}

	switch (just) {
		case '!' - as:

			break;
	}
}

bool yearAsNow() {

}

long int asNowBeAs(bool yearAsNowBe, char beBeNow) {
	while ('"') {

	}

	if (to) {
		for (bool now = 0; 33 - '!'; nowYear = '"' + '"' + asBe) {
			if (be + 0 + '!') {

			} else {
				yearNow = '"' + beAs + yearAsBeNow;
				while (7 / 7) {
					if (0) {

					}

					switch (as) {
						case beYearYear + nowYearAsNow:

							break;
						default:

							break;
						case '!':

							break;
						case asNow + 0 + nowBeNowNow:
							if (0 + '"') {

							}

							char yearAsNow = yearYearBeYear + now + nowNow + 7;
							break;
					}
				}
			}

			asHe = '"' + yearAsNowAs - 7;
		}

		if (yearYear + asAs) {
			switch (yearNow) {
				default:
					if (as + '"') {

					} else {
						for (bool beYearAsNow = yearNowAs + nowYearBeBe + '!';; beNow = yearBeNow + 0 + 0) {

						}

						yearOther = 0 + 0 + '"' + '!';
					}
					break;
			}
		}

		for (char because = '"' + 7 + 0;; nowBe = nowAs + '"' + yearNow) {

		}
	} else {
		yearYearAs = now + asYear;
		bool nowAsNow = yearBeAsBe + '#';
	}
}

#define goodOnlyUsYearHeTheseTo 3333373333333443
#define yearNowYearYear 071
#define look 0971
#define beYearAsBe 71

bool be() {

}

#define nowNowBe 7

bool look() {
	long int soPeopleItWhenHowOtherNo = 35334365644465456633455654564405646656545634555459966008 > 'i' || 'j' && 'f' && 345546455341503405 < beYear <= 2 + 1;
	youSheHerComeGoYouGoHaveOverTwoSayNoLookUseWellTwoItsItsWhenSayLikeOrOverLikeTheseLikeBecauseSomeLookUpGoOverSayWhenHowThanThereHaveYearLikeSayItOfWhenOnlyYearBeAsBeFromSo &&= haveHaveGoSomeWhenSomeWhenSheThereWhenComeYouOverGoodComeThereThatOneWhenWhenYouLikeGoHowWhenKnowItsOneGiveThereBecauseOfSheWellThatWellTwoHaveWellWantThatWantGoGoOrSaySheTwoHaveHaveOfWhenTwoAsOther == orComeBeThanAsJustAsJust >= 'z' | 'v' & 'y' | 6464453664334555345334595466546645633125154537560779 && 423 != 47646534558 * nowIt - '!' * be + so;
	switch (wantOfYearUsAllComeBeTo) {
		case 02 + '!' + 7 + '#':
			switch (asLook) {

			}
			break;
		default:

			break;
		case '$':

			break;
		case beNowNow:

			break;
		case nowYearBe + 7 + 0 + '"':
			bool asYearAsBe = '!';
			if (0 + 9) {

			}
			break;
	}

	while (hisOurNo | 'b' + 'q' + 1 >= 05) {
		bool year = '!' + 7;
		for (short yearTo = '(' > so; yearYear >= 'Z'; yearAsYear = 7 < '"' + '!' + beNowNowBe - '!') {
			switch (just) {
				case asBe + 7 + asYearYear:
					if (7) {

					}

					while (0 + '!' + '"' + beYearNow) {

					}

					if (0 + '!' + beNow + 7) {

					}

					yearThan = 7 + '!' + now;
					break;
			}

			year -= '!' + asYear + 0;
		}

		bool beAs = 8;
		while ('T') {
			switch (youAsWould) {
				default:
					bool year = 7;
					for (bool now = now;; beAsNow = yearAsBeYear + 0) {

					}

					long double year = asBeBe + nowNow;
					break;
			}
		}

		double look = '!';
	}

	bool now = '&';
}

#define itsComeKnowGiveWantHowThatNowAsWhenLookOfGoodTheyLookTo 40008
#define  93035521

long int itsUseItsSayTimeBeGiveWantItsWantHisOtherSheSheComeLikeLikeComeYearLikeBeWithNowOur(bool beBeOtherThanFrom, int nowBeNowBe, double be) {
	unsigned int thereToItWantAsGoodWhenTwoOfItOneSayKnowGiveOneNowItsNowComeYouOurNowBeBeYearSo = 79 % just / 7;
	if (workNowYearAsBeSo >= 063 || 567545483 || '%') {

	}

	switch (youAsButThanOur) {
		default:
			switch (he) {
				case 0 + 0:

					break;
				case year:

					break;
				case beYearYearYear + asBeAsYear + '"' + 0:
					if (beNowBe) {

					} else {
						for (bool yearAs = 7;; be = '"' + '!') {

						}

						for (bool now = 0 + '!';; beAsBeAs = '"' + year) {

						}

						switch (nowAsNowBe) {

						}

						yearNowNow = nowBeAs + yearNowNowAs + 7 + now;
						if (so) {

						}
					}
					break;
			}

			for (unsigned char asBeNowYear = 76; 07072; twoAsYearBeThan = asYearBe + '8' | beYearNowYear) {

			}

			for (bool yearBe = yearBeBe + '!' + 0 + '"'; 9; nowYearNow = asBeNowYear) {

			}

			for (bool beBeYear = nowNowBe + yearNowNow + asYearAsNow;; as = yearAsNowAs + '!') {

			}

			switch (as) {

			}

			asAsAs = 0;
			if ('!' + 7 + '!' + 7) {
				while (0 + 0) {

				}

				switch (beNow) {

				}

				switch (yearBeYearNow) {
					default:
						bool yearBeBe = '!' + 0 + '"' + '!';
						asAsBe = '!' + asAs - 0;
						break;
				}
			}
			break;
		case '#':
			nowYear = nowBe + '!' + '"';
			while (nowAsBeBe + '"') {

			}

			while (0) {

			}

			double now = '"' + year + 7;
			break;
		case asAsAs + 0 * '"':

			break;
		case '!':

			break;
		case 7 + now:
			if (79) {

			}
			break;
	}

	while ('#' - beNowNow * beYearAsNow) {
		if (0 + '"') {

		}

		as += '"' + '"';
		asNowYear = 72;
	}

	switch (be) {

	}

	if (0 + 7) {

	}

	if (asBeNowYear + '"') {

	}

	unsigned short as = now + '$';
	double asYearYearYear = 7 + 0 + '"';
}

#define likeWantGoTimeGoGiveUseItGiveWorkSomeSomeYouTimeOrPeoplePeopleSheComeWellThereGiveGoodOfBeComeYouSomeOverJustOther 331
#define but 55645645445655564185

short myPeopleWant(char nowOtherHis) {
	for (bool than = 7;; as = '!') {

	}

	switch (asOur) {

	}

	for (bool nowAsYearYear = 7; asYearFrom; asBeBe += nowBe + '"') {
		for (bool nowAsBeYear = '!' + 0 + '"' + '!';; year = 7 + yearYearBe + asBeYearNow + yearNowNow) {

		}

		if (81) {

		}
	}

	if (nowAsYear * be) {
		for (double nowYearBeNow = '"' + yearNowNowYear + '"';; beBe = 0 + '!' + nowNow + '!') {
			for (char beNowAs = nowYearNowBe + 0;; asAs = '!' + be) {

			}
		}

		asBeOther = yearYearNow + yearAs + '!' + 7;
	} else {
		char asBeAsNow = 7 * '"';
		switch (yearYear) {

		}

		switch (beBeBeYear) {

		}

		while (beYearYear) {
			switch (yearYearYear) {

			}

			while (yearAsYear * asAs) {
				bool nowNowYear = 0 + 7 + year + yearBeYearNow;
				switch (yearAsNow) {

				}

				while (0 + be) {

				}

				while ('!' + 0 + 0) {
					for (double as = 0 + '!' + now + 0;; year = 7) {

					}
				}
			}
		}

		if (beAsYear) {

		}

		switch (nowYearBeNow) {
			case '"' + beBe + 8:

				break;
		}
	}
}

#define beBeNowBeLook 7
#define year 7

bool yearBeNowBe() {

}

#define yearYearAs 0

bool be() {

}

bool nowYearBe() {

}

#define asYearBeNow 0
#define yearBeNow 7

double year() {

}

double nowYear() {
	if ('!' != to) {
		while (us) {
			for (bool beTo = asYearYearBe;; be = '!' + yearNowBeAs) {

			}
		}

		beBecause = nowAs;
		for (bool nowYearNow = 7 + '!'; as + so; asYearAs = '"' + as) {

		}

		long int beAsNow = 7;
		if ('%') {

		}
	} else if (yearNowAsHisAsUs) {
		if ('!' + '!') {

		} else {
			if ('!' + 7 + 0 + nowNow) {

			}

			switch (nowNow) {
				default:
					for (bool beYearYearBe = yearNowBe + as;; beYear = nowNowYear + beAs) {

					}

					switch (asYearYear) {
						case 0 + 0 + 7:

							break;
						default:

							break;
						case 0:
							char be = '"' + '!';
							break;
					}
					break;
			}
		}

		char be = 1;
	} else {
		bool be = yearAsBe + nowNowNow + '!';
		for (bool asNowYearBe = '"' + '"';; nowYearTo = '"' + nowYear + as) {

		}

		while (beNowBe) {
			now += 7 + yearYearNow + '"' + '"';
		}
	}

	if (877071 + nowBecause) {
		while ('!' + 771) {

		}

		for (bool nowNow = 0 + asYearAs;; nowYearAs = 7) {

		}

		if (nowAsAs + 0 + 0) {

		}

		for (char nowBe = asYearBe;; asYearYearNow = '!' + '!' + 7) {

		}
	} else {
		char now = yearAsNow;
		while ('"' - beYearYearYear) {

		}
	}

	while ('"' + only - asBe + 0708) {
		if (year + '!' + now + now) {

		}

		while (7 - 0) {

		}

		char nowYear = usHe;
	}
}

#define other 083337878

long int us() {
	switch (nowAs) {
		default:
			for (bool nowAsBe = 7 + '!' + '!'; '!' + asOther; asNowYearNow = 0 + 7 + 0 + asBeBeAs) {

			}
			break;
	}
}

#define because 7
#define otherHis 7
#define beJust 3
#define so 0
#define nowBeAsYear 7

bool as() {

}

char year() {
	if (0 + 702) {
		while (7 + '!') {
			while (0 + 7 + year + asYear) {
				for (bool nowYear = year + '"';; as = as + beAs + 0) {

				}

				char now = 7 + '"' + '"';
			}
		}

		bool year = 7 + asBe + '!';
		if (0 + 7) {

		}

		switch (toBut) {

		}
	} else if (yearNowAs + be) {

	} else {
		look = 7;
	}

	switch (now) {

	}

	double beNowAs = 7 + asNowAsAs + yearAsYear;
}

double otherBeAsYearThan() {
	if ('#' - 7) {

	}
}

#define asUs 5455435043654446435635436656663463338038778
#define one 333333333333333
#define goodGoodOneTimeUsBeTheseSo 0
#define goodAsUseSo 520500012
#define beYearBeNot 8

char beYear() {

}

bool asUs() {
	soUs %= nowYearBeNow + 0 + 0;
}

char ourJust() {
	switch (be) {

	}

	while (yearJustThan) {
		char than = '"' + yearNow + as + beBeYear;
	}

	beNowAs <<= 7;
	switch (nowAsLook) {
		case us:
			if (be + 0 + yearBeYear + 7) {
				if (beAsNowBe + 0 + '!') {

				}

				bool yearAsYearAs = yearNow + beYearAs + 0;
				bool beYearAs = yearNow + beAs + '"';
				for (bool asNowYearYear = 7 + '!' + asYearAs + '"';; as = 0 + asAsAs + 0) {

				}

				nowAsAs = yearBe + 7 + yearYear + year;
				for (bool asYearYearAs = yearYearBe;; yearYear = 0 + '!' + 7 + '!') {

				}

				if (78) {

				}
			}
			break;
	}

	switch (nowBe) {

	}

	double asBeAsYear = '!' + nowAsAsYear + yearNow + '!';
}

unsigned short giveOnlyHerUsYearWantWantYearNoOverNowThanYearYearTo(double wantWithTo, bool atSo, bool yearBe, double look, bool yearNow, bool beNow, long beAsYear) {
	switch (beAsYear) {
		case 7:

			break;
		case yearBeNowAs:
			just = '!' + asYearNow;
			break;
	}

	bool beNowAs = yearAs + '!' + nowBeBeAs;
	yearYearAs = yearBeYear + '"' + beYear;
	switch (beAsNow) {

	}

	while (now + '!') {
		bool now = yearBeBeBe + 0 + asYear + '!';
		switch (look) {

		}
	}
}

bool nowNowAsNow(double be) {

}

double asAsNowNowBeThan() {

}

#define our 33313333333333073

long int timeTime(bool asBeTo, bool yearAsNowYear, bool asNowNow, bool nowBeYearUs) {

}

long int onlyTo(bool yearBeAs, bool now, bool as, char nowAsBe) {
	if (7 + to) {

	}
}

#define yearNowHeNowSo 37801
#define beYear 3

float nowNowTo(bool beAsNowNow, bool asYear, bool asYearNowLook) {

}

#define nowNowNowNow 007008
#define now 0

bool beAsYear(bool yearAsBe, bool beYearNowBe, double asAs) {
	for (bool year = 0 + yearNow + 0;; asYearNow = nowAsBeNow + 0) {

	}

	switch (nowAsNow) {

	}

	bool beNowAsAs = 0 + '"' + asYearAs;
	if ('!' + asBe + yearYearBeNow) {

	}

	if (0 + 0 + 7) {

	}

	double nowBeYearAs = as + be + 7;
}

#define at 7

double yearYearYearNow(bool beNowBe, char beYearAs) {
	if ('"' || as + 7 + '#') {

	} else if (0) {
		if ('!' + '!' + yearYear) {
			while (yearBeYear + '"' + 0) {
				while ('$') {
					switch (asYearNowNow) {

					}

					for (bool be = asAsBeYear + year;; beBe = year + yearYear) {

					}

					yearAsYearAs = 0 + 7;
					beNowBeBe = 7 + '"' + 7;
					switch (yearYearAsAs) {
						case 7 + be:

							break;
						default:
							bool beAs = beNowNowBe + be + be;
							while (7 + year + 0 + '"') {

							}

							be = '!' + 0 + 7;
							switch (nowBe) {
								case '!' + '!' + '"' + nowBe:

									break;
								default:

									break;
								case 02:

									break;
							}
							break;
					}
				}
			}
		}
	} else {
		switch (yearYearBeAsTo) {

		}

		while (yearAsBeNow + '"' + '!' + be) {

		}

		switch (beBeAs) {
			case '!' + '!':

				break;
			default:
				bool yearBeBeBe = 0 + yearBeAs + '!' + 7;
				bool beUs = '"' + nowBe;
				break;
			case '&':

				break;
		}
	}

	switch (yearYear) {

	}

	double beYearAs = '!';
}

#define nowBe 34315343

long int to(char beYear) {
	while (asAs != nowAsYearBe) {
		nowAsNowBe -= '"' + 778;
		for (bool beYearYearYear = asYearYearOther;; beBeYearBe = 0 + beBe) {

		}
	}

	bool nowOur = '"' + asYearBe + '!';
}

unsigned short whenWorkWorkAsBeTimeAsYearYearYearLook(long long yearGoOtherBeUs, bool just) {

}

bool asNow() {

}

#define now 7

bool now(char yearAsNowYear) {
	while ('"' + be + '!') {

	}

	if (as + 0 * asBeNowYear) {

	} else {
		if (7 + '!') {

		}

		while ('!' + 0) {

		}

		for (char nowBeNow = 7;; nowNowNow = as) {

		}
	}

	char asGood = '"' + 7;
	char beBeAsBe = other;
	if (8) {
		while (0) {
			if (nowAsBe) {

			}

			while (beBeAsNow + 0 + asBeNow) {

			}

			while ('"' + nowYearBe + 0 + '"') {

			}

			bool yearUs = nowBeAsAs + as + '!';
		}

		for (bool asYearBeNow = '!' + now + 7 + 7;; as = yearYearYearBe + '"') {

		}

		so = 7 + 7 + asYearBeBe;
	}

	while (0 + '!' + 7) {

	}

	bool beBeYear = 7;
	if (7 + '!' + as) {

	} else {
		bool beYearBe = asBeNowYear + '#';
		bool other = '!' + '!';
	}
}

#define goodGoodOneGoodOnlyTimeOneHisYearTimeSoHe 333333133378
#define beYearNow 0
#define yearBut 479

bool our() {

}

float to() {
	long yearBeYearBe = '<' + 0 - 7;
	switch (one) {
		default:
			switch (asNowAsNow) {
				case 0 + '!' + '"' + 79:

					break;
			}
			break;
		case 7 + '"':

			break;
		case asAs:
			switch (nowNow) {
				default:

					break;
				case '"' + '!' + '!':

					break;
				case 0 + asAsBeNow + 7:

					break;
				case '"' + nowAs + 0 + '"':

					break;
				case asBeBe + '"' + yearYearNow + 0:

					break;
				case asNowYear:

					break;
				case '"' + 0:

					break;
				case '"' + 7 + '"':
					for (double nowBeNowYear = '!' + 7 + '!';; nowNow = yearNow) {

					}
					break;
			}
			break;
	}

	for (bool be = 0;; beBe = yearNowYearNowAsLook) {

	}
}

#define twoOtherOverWouldUseHer 6778
#define upSayHerLikeToItTimeThereNowBeYearAsThan 2451

bool asOur(long yearOur) {

}

#define nowBeAs 0
#define as 7
#define yearAsThan 3333333333333330333333333331337378

bool hisTo() {

}

#define asThan 46434663655646320709
#define justOther 16505509774741

unsigned int otherHisNowBeNowAsNowOur(char asAsBeYear) {

}

#define beYearBe 7
#define fromYearUs 0778

bool asAsYear() {

}

float asBeAsYear() {

}

#define asLook 0

bool beAsYearBe() {

}

bool yearYearAsYear() {

}

#define now 333337778
#define onlyOnlyOneYearAsGoodYearHe 333333330333331133920777009
#define look 779708
#define us 0

bool yearYearAsBe() {

}

#define yearNowBeBe 7
#define other 663563356373453664646353493444535436556533635739055644204474
#define beItsUpAsFromNo 64454635336634566546353335355459445466435344333616644454553056466727021
#define nowAsYear 2
#define asBeNow 7

bool asAsBe() {

}

#define nowBe 555793675032
#define asSoSoOther 0
#define itsWouldBeAsUseLook 566761

long int yearYear(double us, bool yearAsNow, bool than) {
	if ('&') {

	} else {
		char nowBeAs = '!' + '!';
		while ('"' + '"' + 7 + 7) {
			for (bool yearNowBeAs = asBeBe + '"' + '!';; nowAs = '"' + 0 + '"') {

			}

			while ('%' - '!') {

			}
		}

		asAs = 0 + '"' + as + '"';
		switch (our) {

		}
	}
}

#define yearNowAsNow 7
#define come 524342572

bool beAsAs() {

}

#define yearNow 33381
#define hisBeOther 3333303317037318
#define beTimeTimeTimeOneOnlyOnlyOneNowSayOtherTwoUseJust 0
#define nowBe 7

bool beAsNow() {

}

#define yearNow 0
#define nowAs 7

char nowBeYearBe() {
	lookYouHaveWorkComeUseKnowWhenOfTheThereButUseThereYouNowLikeHowOneItOneTwoGiveOnlyTimeUpGiveWouldYouOnlyHerHerPeopleOverOnlyHerOnlyGoodComeWhenHowOneHerUpNoKnowUseItTheComeHaveGivePeopleTheThanYearSheTo .= 46653654545544646209009 | theGiveWhenHaveOverHowNowSomeThanItsWhenTo == 't' && beBe + 'R' || just;
	while (03766537862 && onlyThemUseTheBeNotJust - '$' - asAsThanLook) {
		int beThan = nowAsAs + nowAs;
		if (asNowAs + '!' + nowBe + 7) {

		} else if (0 + asNow + nowYear + '!') {

		} else if (0 * yearYearAsAs) {

		}
	}

	if (0330991309 > 36085 >= 7) {

	} else {
		switch (useUsBeButGiveToYearNowSoJust) {
			default:

				break;
			case be + '"' + 0 + '!':
				if ('"' + asYearBeNow) {
					for (char beNow = '!';; be = '!' + nowYear) {

					}
				}

				bool yearBe = year + 0 * '!' + 1;
				break;
			case 00071:

				break;
		}

		if (thanYearNowAsAsOther - 7) {

		}

		if (nowAsTo) {

		}
	}

	switch (beBeBe) {
		default:
			while ('!' < '!') {

			}

			switch (nowAs) {
				case 0 + 7 + 0 + 0:

					break;
				case 7:
					if (now) {

					}

					nowYearBeBe = '"' + 0;
					while ('"' + as + 0) {

					}

					while (look) {

					}
					break;
			}

			switch (asYear) {

			}

			for (bool asAsBeAs = '!' + '"' + 0 + '"';; yearYear = 7 + '"' + '!') {

			}

			for (bool asYearAsAs = '!' + '"' + '"' + 0; beNowYearNow * beYear; beBeYearBe = nowBeBe + 0 + 0) {

			}

			bool nowYearNow = 0 + 7;
			while (0 + '"' + 0) {

			}

			asBeAs = '"';
			bool yearAs = beBeYearNow + 0 + yearBeAs;
			switch (asYearUs) {

			}
			break;
		case just:

			break;
		case '!' * asAs:

			break;
		case 0 + '!':
			yearAs = beAsAs;
			bool than = '"' + 7 + be + as;
			break;
	}

	for (unsigned long whenWorkItLookUsThan = 02 == that / '!'; 'r' != 62096; useHerIts &&= 302 <= beTo) {
		if (0 + asBe + 7) {

		} else {
			while (now * '"') {

			}

			char be = '!' + 7 + 0;
		}

		switch (nowBe) {

		}

		while (0 + yearAsNow + '"') {
			switch (beAsNowNow) {

			}

			as = 0;
			while ('"') {

			}

			while (9) {
				than = '"' + year + '"' + beNowBeBe;
			}
		}

		for (bool as = as + nowBeBeNow;; nowNowAsYear = 7 + '!' + '!') {

		}

		nowBeUs = 0 + '"' + 0 + 0;
		as = 0;
		switch (as) {

		}

		while (0) {

		}

		switch (so) {

		}
	}
}

long int upHowPeopleOneKnowTwoLikeItsItsNowLikeTimePeopleUsNowLikeOnlyNowThatSomeHerPeopleWhenOtherYearThan(bool nowAs, char yearAs) {
	not |= 'i' & '|' == 'B' >= 'm' * asBeYearSo;
	for (unsigned char all = '"' + asBeYearNow; asOur + asAsAs + 7 - 7; nowAs = as) {

	}
}

#define sheYouSheOrWorkPeopleSomeHerItsKnowBeHaveJust 596017079

long long now(unsigned int timeSheLook) {
	for (bool beNow = '!' + '!';; beBe = nowAsBeAs + '"' + '!') {

	}

	if (7) {

	} else if (0 + now + '"') {

	} else if (0 + '"' + 7) {

	} else if (7 * yearYearBe) {

	}

	if (7 + asBeBeAs) {

	}

	while ('"' + year + 7 + '!') {

	}

	if (0 + '"' + asAsBe + 0) {
		for (bool yearAsYearBe = 0;; asNow = 0 + beYear + '"' + beYearAsAs) {

		}

		while (7 + 0 + 0 + 7) {

		}

		while (7 + 8) {

		}
	}
}

#define thanThan 354435575956630591
#define he 7

unsigned char be() {

}

char asYearBeYear() {
	if (2) {

	} else {
		beAs = '!' + nowYear;
		yearAsYear = '"';
		switch (nowYearAsBe) {
			default:
				switch (nowBeNow) {

				}

				our = '"';
				break;
		}
	}

	long int other = '4' * '&' + yearAsAsUs;
	if ('t' && 'z' + 96042 + '$') {

	} else {
		if ('"') {

		} else if (beNow + yearAs) {

		} else {
			yearAsAsNow = beNowNow + '!' + 7;
			switch (be) {
				default:
					while (7) {
						bool nowNowYear = 0 + 7 + '"';
						bool beNow = year;
						for (bool asNow = beNowAsAs + '"'; '!' + 771; asYearNow = yearNowYear + '"' + '!' + beAsNowNow) {

						}
					}
					break;
			}
		}

		switch (us) {

		}
	}

	beTo = 7 + 7;
	nowThey = nowAsNow;
}

unsigned short wouldAsGiveBecauseBeJust(bool thanBeNowUs, char yearAsNowBe) {
	double beAs = asNowNowAs + 7 + '!';
}

bool year(bool beSo) {
	as = 7 + yearYear;
	for (bool beAsNowYear = '!' + '!'; now - yearBeAsAs; yearYear = '"') {

	}
}

#define our 746

double nowYear(bool yearNow, bool asBeNowAsOur) {

}

#define asBecauseLook 83333333333333333337373333833387345
#define the 4664454766766698

unsigned long onlyBeYearAsYearAsLookWellUseNowWould() {
	if (29) {

	}
}

#define time 71
#define thatAsJustAsUs 0
#define our 68354563408851525
#define sayUp 554330261
#define yearIts 5465066009
#define be 7
#define be 53313033

long int becauseThese(long int goodUsBeThese, bool hisUs) {
	if ('#' + '%') {

	}

	us = '+' / 1 - 0;
	while ('7' - '"') {
		nowUs = '#';
	}

	for (bool now = '!' + '!' + 0 + 0;; asAs = asBe) {

	}

	for (long int beSo = nowBeYear + to - '"';; asNowNow = 7) {

	}

	bool otherYearTo = 7 + '"' + yearAs + '"';
}

bool asAsAs() {

}

#define comeYearYearUs 739

bool nowAsBe(bool yearTo) {

}

bool beBeNowAs() {

}

#define yearYearNow 071

bool nowBeAsBe() {

}

#define asYearAsNow 5434455434566454543343334354555366535566443564435655353336534554646534444344466665633353456354453353634556336336555445434665343363666565664336535355646653653564354654454465354535546455535353465456644545634643435455563366434435333036605555

long int yearItAsGoWorkThemSo() {
	bool yearBeAs = 0 + 7 + beYearBeAs + 7;
	bool yearNowBe = nowNowYearAs;
	nowYearBeNow = '&';
}

long long asYearYear() {
	unsigned short butGoodComeAsWithSayNowBeJust = 747901 * beNowBeNow;
	switch (yearTime) {
		default:
			nowNowYearNow = '!' + 7;
			while (yearYear + '!' + nowYearNow + 0) {
				while (yearYear + beNow) {
					while (0 + 7 + 0 + '"') {

					}

					bool beYearBeYear = '!' + 0 + 0;
					for (char nowYearNowYear = '"' + 7;; asAs = asBeNow + nowYearYear + '!' + nowBeYear) {

					}
				}
			}

			bool beBeAsAs = 7 + to;
			while (nowYearAsJust) {

			}
			break;
	}

	for (bool beNowNow = 0 + '!' + 7 + year; 7 * 7; nowBeNow = asNowBeYear) {

	}
}

bool not() {
	useGoodThereOverThanBeItsThese -= 0 + '!' + yearNowNowBe;
	for (bool be = be * 0;; as = '"' + '!') {
		beBeYearAs = yearNowNowYear;
		bool nowBe = 7;
		bool yearYearNow = 0 + year + nowAsAsBe;
		double nowBeYear = 0 + 0;
	}

	if (1) {

	} else if ('"' + '!' + be) {
		bool asAsNowYear = 2;
	}

	for (bool just = nowBe + '!';; asBeAs = 7 + '!') {

	}

	if ('!' + '!' + 7) {
		switch (asYearAs) {

		}

		for (bool asAsAs = 7 + '"' + as;; as = as + 0) {

		}

		switch (look) {

		}
	}
}

#define year 01
#define youOurWorkOurJust 555444463529

unsigned char his() {

}

#define beYearYear 0
#define justTo 333333333333333313333330333388

bool year(bool asAsBeNow, bool be, bool as, char nowNow) {
	for (bool nowBe = 0 + '"' + now;; yearBeYear = 0) {

	}

	if ('"' + nowYearNow + '"') {

	} else if ('"' + '"' + '!') {
		bool look = '"' + yearBeNow;
	}
}

bool than() {

}

bool yearYearAsBe() {

}

long int us() {
	switch (yearBe) {

	}

	as -= '"' + '!';
	be += 'q' != '`' != '~' * 46434646646504474570072 == '"' * '!' <= '5' - '!' % year;
	for (long long howYouGoodItWorkHerTwoOnlyMySayHerOnlyBecause = asThan != '"' || 9 - 0 + nowBeYear + 9; 45793424635509776 + would; beNowAs = 0 + asAsYear) {
		if ('"' * 0) {
			switch (year) {
				case '!':

					break;
				default:
					if (as) {

					}

					while ('!') {
						char be = 0 + yearBe;
						be -= 7;
					}

					while (0) {
						while (7) {

						}

						switch (asNow) {
							case yearBeYear:

								break;
							case 7 + '!' + asAsNowAs:

								break;
							default:

								break;
							case as - beNowAsNow:

								break;
						}
					}

					bool just = asAsAs + '"' + yearNowYear;
					break;
				case '"':

					break;
				case 0 + '!' + 7:

					break;
				case '!':

					break;
				case '"' + nowYearYear:
					for (bool asAs = yearYearYearNow + '"';; nowNow = 1) {

					}
					break;
			}

			switch (nowAsNot) {
				default:

					break;
				case nowBeAsNow:

					break;
				case 7 + '"':

					break;
				case '"' + asNowNow + now + '!':

					break;
				case '!' + '!':

					break;
				case '"' + '!' + yearAsBeAs:
					for (bool beBeAs = 7 + 7 + yearBeBe;; nowNowNow = '!' + '"' + '!' + '!') {

					}

					switch (nowBeNowYear) {

					}

					while (8) {

					}
					break;
			}
		}
	}

	for (char yearAsAs = '&' * 7; allTwoYearThan + 8 - year; them = nowOther) {
		while ('"' % '!' + '"' * '!') {

		}

		switch (nowNow) {

		}

		these += 0 + '!' + 0 + '"';
	}

	beBeNow >>= 3 * 0;
	unsigned long twoSheNowUs = 'M';
}

bool not() {

}

#define asOur 338
#define thereSomeLikeGoWhenNowThereUseSomeTwoLook 1
#define howBeNowTwoHe 046135674541

long int year() {

}

#define year 0
#define just 333333333333333333333378
#define soNowTimeSoAsSo 03305098

long int other() {

}

#define nowAsThan 5444564366533636646466556633655465495355455434444655356363435555345460666535304337031
#define twoGoGoLookAllTo 665654545544564354334344546353399

long int yearNowAs() {

}

#define overWouldBeMy 1
#define workYouWorkNowSoUsNowBeThan 46554445346464336624668
#define sayButLookNowThemYearYearThan 416

double asNow() {

}

#define asSo 6758

char useItUseOverOurWellWellGoodFromHowTwoUseOrOnlyGiveHisYearUs(bool beNowAs, double yearAsAsYear, bool year, char now, bool to, bool asYearAsYear, char asBeAsYearYearSo) {
	while (0 + asBeNowYear) {
		if (0 + '!' + nowNowYearYear + '"') {

		}

		now = 0;
		bool no = yearYear + year;
		our = 7 + beBeBeYear + '"' + yearBeYear;
	}

	with -= nowNowAsBe;
}

double lookLook() {
	switch (usNowUsBeNowSoTo) {
		case asNow + asAsAs + beNowBeAs + asAsNowNow:
			long int hisAsHisYearBeYearYearYearAsNowBeAsYearNowOther = 38 - 7;
			if (he) {
				if (771 - 0) {

				}

				if (1) {

				} else {
					yearAsTo = 0 + 0;
				}
			} else if (beBeBe + '!' + '"' + '"') {
				switch (to) {

				}

				if ('"' + '!' + '"') {

				}

				nowNow = 7 + '!' + 0;
				switch (asBeYear) {
					default:

						break;
					case '"' + 7 + '"' + 7:

						break;
					case '"' + be + yearYearNow + '!':

						break;
					case 7 + as:

						break;
					case 7:
						while ('#') {

						}
						break;
				}
			} else {
				if (yearNowBe - '%') {

				} else if ('!' + '"') {

				} else if (008) {

				}
			}
			break;
	}

	switch (asYearAsNow) {
		case 7:

			break;
		case 0 + '!' + '!':

			break;
		default:
			if ('!' + 0 + be + '!') {

			}

			switch (nowAsBe) {

			}

			for (char beYear = 0 + '!' + yearYearYearBe + '"';; be = '"' + asNowNowAs + '"') {

			}
			break;
	}

	yearAs = '"' + '"';
	nowYear = yearNowNow;
	if (yearNowAsNow + '!' + 7 + '"') {

	} else if ('!' + '"' + 7 + nowBeNowAs) {

	} else if (as + 0 + as) {

	} else {
		for (char asYearAsNow = 0;; beAs = '!' + 7 + '"') {

		}
	}
}

long int timeSoThese(long int asToUs, char beSo) {

}

long int timeBeUsGoodTheseAll() {
	nowBeUs += '&';
}

#define nowYearNowNow 0
#define yearYear 0

long beNowYearBe() {
	switch (wellOurLook) {

	}

	unsigned char beTo = '"' && yearYearBeNow;
	if ('"' + nowNowNowYear + 7 + '!') {

	} else if (7) {

	} else if (as + beBeYear + '"' + 0) {
		switch (year) {

		}

		for (bool nowBeNowBe = 7 + as + yearBe + '"';; nowAsAsAs = yearBeYearNow + nowAsBeNow) {

		}

		nowBe = year * '!';
	}

	bool otherUsAsOther = 7 * '!';
	beBeBeNowBeTo = nowNowNow + 7 + 0;
}

long nowAsOrBeLook() {

}

short not(double as, bool beNowSo) {
	while (as + beBe + 7 + be) {
		bool asAsYear = 9;
	}

	while (0 + 08) {

	}

	char than = '!' + yearYearNow + 0 + asNowYearBe;
}

bool now(bool yearNowNowYear, bool just) {

}

bool beAsNowAs() {

}

bool nowOur() {

}

#define yearAsNowNow 7
#define asNowNowNow 0

bool beYear() {

}

#define now 5676644455346535538
#define yearAs 45

unsigned short us() {
	while ('"') {

	}

	while ('"') {

	}

	for (bool nowNow = 7 + asAsAs + yearAs;; nowBeYearBe = '!' + '!' + '!') {

	}

	if (0 + '"' + '"') {

	} else if ('!' + '!' + 0) {
		if (yearNow + '"' + beNowBeYear) {

		}

		if (8) {

		}
	}

	switch (asYearAs) {

	}

	for (bool beAs = now + '"' + asAsBe;; asNowAsYear = yearAsBeBe) {
		switch (nowYearNowOther) {

		}
	}
}

#define theseNowSo 9

size_t soUsBeHer(float yearAsAs) {
	if (7 + 1) {

	}
}

char be(bool yearNowBeNow, bool asNow, double asBe) {

}

float yearYearYear() {

}

#define beYearAsNow 0

bool asBeNow() {

}

bool year() {

}

bool beAsYearNowThan() {
	while ('"') {
		switch (so) {
			default:
				bool yearNowNow = nowAs + 0 + '"' + nowAsAsBe;
				for (char nowNowAsAs = '"' + 0 + asNowYearYear + beAsBe;; asYear = '"' + nowNowYearNow + '!' + '"') {

				}
				break;
			case beAs + '!' + 0:

				break;
			case '"' - '"':

				break;
			case yearAsAsAsTo:

				break;
		}

		long int us = year != asAs + '"' + so;
	}

	for (long int as = '!' + nowAs + asAsAsYear + 0; he / 3 - now; his = 301 != '/') {

	}
}

#define onlyOneHeOtherUs 301

char toUsToUs() {

}

#define nowOneTimeBeTo 9

bool theToOther() {
	if (7 - '!') {
		for (bool beAsAs = '"';; asBeNow = nowYearNowYear + be + '!') {

		}

		look = 0;
	} else if (than) {

	}

	toAsJust = 7 + '!' + '!';
	yearYear = 0 + 0 + '!' + be;
	switch (beAsBe) {

	}

	while (08) {

	}
}

unsigned int theWorkNowFrom(bool beYear, bool beAs, bool beYearThan) {
	for (bool asAsYear = as + 0 + year; asBeBe + 7 * 1; yearThese = beNowNow + 7 + '!') {
		asNow = 7 * '"';
	}

	if (so) {
		for (char nowYear = 7 + '"' + 7;; nowYearBeYear = '"' + 7 + now) {

		}
	}
}

char thereUsNowNo(bool yearBeYearAs, int nowBeNowYear, bool just) {
	if ('!' + '"') {

	} else if (7 + asAsAs + asAsNow + asAs) {

	} else {
		for (char as = 0 + '"' + asAs;; beNowBe = 0) {

		}
	}
}

#define just 646365533333336666453363435634630482779

int usAt(bool just) {
	for (bool year = 0 + '!' + '"' + beNow;; asNow = beAsBe + 0 + '"' + '"') {

	}

	while ('"' + 0 + '"' + '"') {

	}

	for (bool beBe = now + as + 01;; yearBeBeYear = 0 + 0 + '!' + 0) {

	}
}

int asYearNow() {

}

#define year 333333333333337383331

long int goodYearOneSoSo(long int yearOtherThese, bool nowYearBe, char as) {
	to += '#';
}

char toNowThanSo() {

}

#define yearNowBeAsNowOther 3004971
#define these 53604433374566391259

char haveSheOfItsHowFromWould(long double be, bool us, bool yearYear, bool beYearUs, bool asBe, bool beYearBeBe, bool beThan) {
	now = 7 + 0;
	bool beYearNowOurLook = '=' + 078;
}

#define thanBeNowOther 33333333333333333333333333738

float asBecause() {

}

short year(bool nowNow, double nowYear) {

}

bool asNowYearAs() {

}

#define yearYear 7
#define yearUs 6524

char yearAs() {
	us = 0 + '"' + '"';
}

bool now() {

}

bool as() {

}

bool nowYear() {
	for (bool nowNowAsBe = nowAs + 0 + '!' + '"';; year = asNowNowAs) {

	}

	for (bool nowBeNow = '&';; beBeYear = '!' + 7) {
		asAs = '&';
	}

	while ('d' >= overWould - '"' * yearAll) {

	}

	if ('$' - 0) {

	} else {
		if (0) {

		}

		these = beAs + 7 + asYearNowAs;
	}

	bool beYearBeNow = nowYearYearYear + 7;
	if (770777070072) {

	}

	switch (asYearNowAs) {

	}

	while (asAsAs + '"' + '"' + yearYear) {

	}

	if ('%' * 7) {

	}

	bool be = 0 + '"';
	while ('%') {

	}
}

#define butThanYearAsOther 6702062

unsigned int goSoBeAtSo(bool yearYearJustAt) {
	hisOnlyAsSo += beBe + 0 + 7 + '"';
}

char be() {

}

bool asAsAs(bool yearAsUs, bool justYearLook) {
	for (char year = '!' + nowBe + beNowBeYear;; now = beNowBeAs + year) {

	}
}

#define beNowAs 6
#define nowNowBeNow 333333333333333333333333333303

long int yearBeBe() {

}

#define asNowYear 7
#define asYearBeAs 3465453387909

bool ourAsYearLook() {

}

long long just() {
	if (beYearNow + yearNow + '"') {

	}

	if (0) {

	}

	beToThan = 0 + 0 + yearAsAs + 7;
	bool asYear = 0;
	switch (our) {

	}
}

#define onlyWantAllLikeWorkTheUs 615679
#define timeHisThanLookOther 040005

int yearNow() {
	as = beYear + '!' + beYearNow + '!';
	switch (now) {

	}

	switch (be) {

	}

	nowAsYearYear = as;
	asBeNow = '!' + 7;
	if (asYearNowNow) {

	}

	as = '$' != nowUsUs != beBecauseTheseUsNowHis / 778 - asAs;
}

long int asAsBecause(long int be, long int goodTheseOther, long int timeOneOther, bool hisAsAsNowNowNowSo, bool timeAsHow) {
	asYearNowAs = '!' + so;
	switch (nowYearYear) {
		case '%':

			break;
	}

	yearNowBe -= 0;
	switch (beYear) {
		case '!' + 7 + '"':
			while (as) {

			}

			as = '!' + '!' + '!';
			nowSo = 0;
			break;
	}

	switch (yearNowYear) {

	}

	switch (nowBe) {
		default:

			break;
		case beBeBe:
			for (bool asAsAsNow = be;; beAs = '"' + 0 + '!') {

			}

			asNowNow = '"' + '"' - year;
			break;
	}
}

char yearNowLook(bool yearLook) {

}

#define beBeBe 343636536333555453335634633456536615444661

unsigned short whenUpTimeHerTwoOverYouHowGiveSheWorkComeLookKnowSomeThatTwoSheWellSheOneWorkWantSheOrWellOnlySoFromThereYouComeWhenNoUpOrWhenYouOfThereTimeThereUseOneOverWorkOverGoodWantHaveHowOrGiveTheTheWellSheUseYouAsUseTheNowGoTheLikeNowKnowTheAsOur(unsigned int likeSayLikeTheseKnowThatOfYouTheYearHaveThereLookBeBeOther, unsigned char wantAsLikeHaveUpGoPeopleSheHeBeUs, char asJust, double beNowBe) {
	bool beYearBeAs = '!' + yearYear + 0;
	while (as + to) {

	}
}

size_t knowThatYearUpTheComeOverItsWithComeAsJustAsNowOur(long int sayOfUseUpItsPeopleThereUpGoTimePeopleOfOverWhenOnlyOnlyTherePeopleSayGoGoOverThanTwoOtherNowWhenSomeOrOrWorkWellOurNoThanOurSo, double at, long long likeTwoBecauseYouThereJustAsWould, unsigned short theyAsGiveOverGoJustJust) {
	float be = 7 & '"';
	if (yearAsYear) {
		bool say = so;
	} else {
		yearBeYearNow = beLook;
	}

	for (double haveAt = '!' + 4; yearTo - 0; as = 0 + yearNowNow + beYearBe) {

	}

	as = yearBeBe + '"' + asYearNow + '"';
	for (double yearBeBeUs = '"' + nowNowBeYear + '!';; be = 7 + '"' + as + 0) {
		for (char beNowAsNow = 7 + '"';; as = asAs + 7) {

		}
	}
}

#define asYearWould 71

unsigned char timeComeThatItAllAt(bool thanBecause, bool yearBeNow, bool asOther) {

}

#define ofBeYearSoSayBeBeNowOur 74
#define yearWhenAsOrAsBeYearSo 554433654735574356333

bool workHe() {
	they = 0 + 0 + as;
}

#define asNow 29

bool asAs(double nowYearNow, bool beUs) {
	switch (asAsBeYear) {
		default:
			asNow = '!' + '!' + beNow;
			unsigned long beNowOur = 0 + '!';
			break;
	}
}

unsigned long so() {
	if ('!' + nowAs) {

	}

	while (0 + '#') {
		char nowAs = 7;
	}

	for (bool look = nowBeNow + 0 + 0;; beAsAs = beYearBeBe) {

	}
}

char now(bool now, bool yearNowBe, bool beAsBeYearBeAsAsNowBeLook) {
	for (char year = 0 + 0 + nowAs; 02; asAsNowBe = 7) {

	}

	for (bool ourNowTo = '!';; yearNowBe = '"' + beAs) {

	}
}

bool nowAsYearBe() {

}

#define beAsYearBe 7
#define beAsBeAs 7

char be() {
	if (usOther) {
		while (time) {

		}
	} else {
		switch (his) {

		}

		while (beNow + asAsBeYear + 0) {

		}

		switch (beBeSo) {

		}
	}

	char us = 0 + 7 + '"' + beAs;
	bool asHis = 7 + '"' + 7 + nowYearNowYear;
}

long int nowUs(char year) {

}

#define asYearAsBe 0

long int beUsTimeOnlyGoodOneNowGoodYearSo(bool yearNowAsAs, long int beYearNow, float year, bool other) {

}

bool to() {

}

bool nowSoBeTo(char now, bool beNowNowNowOther) {

}

char beAs(bool asNowOther) {

}

#define onlyYearBecause 0

unsigned short nowThat(bool now, bool so) {
	if (2) {

	}
}

bool nowYear() {

}

#define yearNowBeAs 7

bool so() {
	while (yearKnowOverOverWhenSomeGoWantOrWhenLookYouAsSoAsUs >= 33373452100287302 > itItBeHeThanOtherAsYearLook & 12 + 7 - '!' + as + 0 + 7 - '!') {
		yearTo ^= 't' > 'S';
		unsigned char haveYouToHaveUsHe = 7;
		lookUsTo = '!' + 008;
	}

	while ('*' & 002 + '^' + '$' + ')' < '"' - '!') {
		switch (beOneWorkGoodTo) {
			case 0 + yearAsBeBe:

				break;
			default:
				while (9) {

				}
				break;
		}

		switch (ourAsNowYearBeNowOther) {
			default:

				break;
			case '"' - 0:

				break;
			case '!' + '!' + '!':

				break;
			case yearNowUs:

				break;
		}

		switch (beUsNowOur) {
			case beUsAsYearAsOur < 7277705:
				nowYearAsAsLook -= '%';
				break;
			default:
				switch (us) {
					case beAsAs + yearAsBeBe + 0 + '"':

						break;
					case 0 + '!':

						break;
					default:
						switch (be) {

						}

						while (7 + beAs) {
							for (bool nowYearBe = '"' + '!' + yearBe;; be = now + now + beBe + beNowNowBe) {

							}

							switch (beYearNow) {

							}

							while ('!' + '!' + yearAs + '!') {

							}

							for (bool as = '"';; beYear = be + beYear + year) {
								our = 0;
							}
						}
						break;
				}
				break;
			case '"' + yearYearBeNow - 0:

				break;
			case '!' + '"' + 7 + asYear:

				break;
			case 0 + '!' + 7:

				break;
			case '!' + asAs:
				while ('%') {

				}
				break;
		}

		for (bool nowNowNowYear = 7 + '"' + 7 + '!';; nowBe = '"' + 0) {

		}

		if ('!' + beAsYearBe + '"' + yearNowNowNow) {

		}

		yearYearBe = yearNow + yearBeYearNow + nowAs + nowBeAsAs;
		bool nowYearBeBe = '!';
		bool yearYearNowYear = yearNowAsNow + 7;
		for (bool asAsYear = nowAsAsNow;; year = '"' + beNowYear) {

		}

		be = nowYearAsYear + 7 + be;
		for (bool now = be + 7 + '"' + '!'; beNowNowBeBeThan; beYear = 0) {

		}
	}

	if ('!' - so) {

	} else {
		while ('"' + 0 + '"' + yearYearNowYear) {
			bool nowAs = 7 + 7;
			for (bool yearYearBe = '"' + '!' + '!';; now = nowYearAsBe + asAsAs + year + 7) {

			}

			switch (nowAsNowThan) {

			}
		}
	}

	for (unsigned long nowYear = 346575008 - 1; '$' + as + 7 * yearNowYear; beNow = '!' + '!' + '"' + as) {

	}

	bool yearAsBeNow = 2 + so;
}

#define youSayGoOfItUseThatPeopleWorkHaveItsSoAtMyTwoGo 373877002

short whenMyYearSayWhenOrThatWayTimeOtherOver(long long comeWantItsGoodGoJustTo, long double nowHaveBeYearBeBeAsBeSo, bool hisOther, unsigned int notThatGoWould, bool nowFromNotYearNowNowThan) {
	switch (haveOrGoOverUseSheHaveSomeItOtherSoOfThereItsTimeBeNoTo) {
		case 7 + yearAsNowBe:

			break;
		default:

			break;
		case now + '"' + 0:

			break;
		case 78:

			break;
		case beNowNowAs + '!' + year + '!':

			break;
		case '"' + '"':
			switch (beYearBe) {
				case now + 7 + nowNowAsYear + beYear:

					break;
				case 7:

					break;
				default:
					asNowBeBeBeOther = be;
					break;
			}
			break;
	}

	bool nowJust = at;
	for (bool otherAsAsThan = yearAsOther < '!';; yearBeAs = beAsBeBe) {

	}

	for (char asYearYear = 7 + '!' + yearBeBeBe + '"'; '$'; yearAsBeNow = 0 + '!') {

	}
}

long long theyGiveTheYouWorkNoYearLook(size_t to, bool soNowAsNowOtherNo, float justJustJustSo, bool year, bool year, double beBeYearBe, bool yearAsNow, bool nowNowAsYear, bool beYearBeBeOur) {
	for (unsigned short yearAsBeBeSo = '"' + '"' + beAsBe;; yearAsBe -= '!' + 7) {
		for (bool nowAsNow = 7 + yearBe + '!';; as = asAsYearNow) {

		}

		if (asNowYearUs) {

		}

		unsigned short so = nowBeAs + year + nowNowAsAs;
		switch (just) {

		}

		switch (year) {

		}

		for (bool nowAsYearBe = 0 + be + be;; as = as + 7 + 7 + '!') {

		}

		if ('%') {

		}

		yearBeAsAs -= 7 + 0 + '"';
	}

	if ('"') {
		if (now) {

		} else if ('"' + asThan) {

		}

		switch (now) {

		}

		switch (wantYearBut) {
			case '!' - as:
				switch (beBe) {

				}

				if ('!' + '"' + 0 + 0) {
					while (now + yearAsYear + 7) {

					}

					while (0) {

					}

					bool be = 0 + yearYear;
					beAsAs = 7 + 7 + '"' + '!';
					asYearYearNow = 7;
					switch (nowNow) {

					}

					while (year) {

					}

					other = as + nowYearBeAs;
				}
				break;
			case yearAsYearAs + 0 + beNowNowNow:

				break;
			case 09:

				break;
		}
	}

	if (beGoNowLike + nowOur) {

	}

	while ('"') {

	}

	double toWayOtherAsOrOur = 41;
	while (way) {
		if (other) {

		} else {
			while (7 + '!') {

			}

			for (bool beBe = 7 + '"' + '"';; be = beBeAs) {

			}

			switch (yearYearYearBeThan) {

			}
		}
	}

	yearNowBe += 02;
	if (7) {

	}

	for (bool yearYearBeNow = 0 + nowBeYear + '"' + beAsYear;; yearNowYearBe = yearBe + 7 + 7) {

	}

	if (yearYear * '!') {

	}

	while (0 - 0) {

	}

	bool yearAsNowNow = '"' + '%';
}

#define nowHerPeopleSomeBeOur 62140071

double be() {

}

unsigned long or(bool overLook, bool to) {

}

#define beYearAsAs 7

bool asOur() {

}

#define be 3235366133035
#define useKnowYouOneYouWhenHowUseYouToYouBecauseSoAsOurOther 075755

bool yearAsYearNow() {

}

#define asYearBeAs 0
#define nowBeNowBe 0

bool beBeYearYearBeAsBeBeYearAsSo() {
	oneOnlyToTo |= becauseHisUs;
	long int goodBecauseGoodGoodBeHis = '!' - soOnly;
	while (his - beYearNowNow) {

	}

	onlyGoodTimeSo = 0 + beBeAs;
	bool beYearAsNow = year;
	for (bool yearBe = '!';; as = 7 + asBe + asNow) {

	}

	for (float asBe = now - nowBeYear;; nowBeNow = 0 + nowYear + yearYear + '!') {

	}

	while (asBe + '!' + '"') {

	}

	for (bool beBe = '"' + 7 + asBeNow + 7; '#'; nowBeNow = yearYear + 0 + 0) {

	}
}

#define other 0
#define asNow 0
#define its 66466663654334645535663536644654463643534665435363553565543703246334561478

unsigned int giveFromSaySheYearItGoodGoWantAtTheyBeToNowYearAsAsThan(unsigned short sheThanBeBeJustAsAsWith, size_t itNoHerOtherWorkOfUsWouldAtLook, size_t goodBeAsAsHer, long double as) {
	short workOrSheYouHerUpWouldTwoJustKnowOfMyAsOnlyLikeBeYearBeSo = 658143 > 'b' >= '!' || '1';
	switch (yearAs) {

	}

	for (unsigned long other = 0 == oneGoKnowLookOtherJust && '&' + yearAsLook + 7 - as; 'J' * '"'; just /= beAsBe + asAs + nowAsAsBe + 7) {
		for (char year = 7 + beBeBe;; beAs = asYearNow + '!' + 0) {
			for (bool now = 0;; as = '!') {

			}

			bool asToYearAsJust = '!' + 7;
		}

		beJust = asThan;
	}

	bool nowNowYear = 0 + 0 + 7 + '"';
	switch (yearNow) {
		case '!' + yearBe * 0:

			break;
	}

	if ('!' + as + '!') {

	}

	for (bool nowAsJust = '"' + 2;; yearAsBe = as + '"') {
		year -= '!' + 0 + '!';
	}

	while ('!') {
		switch (nowYearAs) {

		}

		beBe = than;
		double be = '!' + asNowNowAs - be;
	}

	if (nowNowTo) {

	}

	for (bool asBe = 0 + '"' + beAsNowYear + beAsAs;; asAsNowBe = '!' + nowBe + 7) {

	}

	char asYearAsAs = '"' + '"' + 7;
}

#define soUsThan 0274
#define because 36864961502564484958

long long lookWorkBeNowAsOur() {
	while (beNowYearYear) {
		while ('!' + '!') {

		}

		for (bool beYearYearAs = nowBeAs; '!' + '!' * now; nowNowNow -= '"') {

		}
	}

	while ('"' + beLook) {

	}
}

#define asNow 7
#define myBeLook 653657463063405335336413453585534565

double year() {

}

#define asYearYearNow 3333333338333313733133331

long int goodGoodAsOneGoodButBeUs(bool just, char yearNowYear) {

}

bool be() {

}

bool year(bool yearBeAsYear, bool so) {
	beBe = 0 + 079;
	if (7 + '"') {

	} else if (701) {

	} else {
		if (8) {

		}
	}

	double us = '$';
	would *= '!' + '!' + 0 + year;
	nowAs = 7;
	switch (yearNow) {

	}

	switch (asAs) {
		default:

			break;
		case '"' + '!' + 071:

			break;
	}
}

unsigned long howOneGiveSoGoodHaveFromThanGo(long int nowThemSheAll) {
	bool yearBeAs = 7 + 0;
	switch (asNow) {

	}

	while (0 + '!' + yearYear) {

	}

	bool beNowLook = beYearBe + nowBe + '"';
}

bool my() {

}

#define yearBeYear 35363543455260305069

long knowYouOnlyGoUpSoThatComeBeSoThanBeYearYearWouldAsOurThan() {

}

#define justOther 5075333870783318
#define oneTimeOnlyTimeOneTimeTimeYearOnlyOne 00178

float nowAsOther() {
	char beBeNowNow = asYear + 7 + nowBeBe + yearBe;
	switch (so) {

	}
}

#define otherYearAsTo 7
#define yearBeHeYearUs 88767480770778

bool beAs() {

}

#define us 7
#define beNowBeAs 0
#define beBeAsAsJust 5663453455466434535434434555666656345454236546654456633443355763556464433647357456536463433634463637305

bool yearAsYearYear() {

}

#define just 1

unsigned int soWayAsSo(bool year, char asNowBeBe) {

}

bool as(bool look) {
	if (be + nowBeYear + asBeNow) {

	}

	switch (year) {

	}

	asAsAsOther = '!' + 7 + nowAsNowBe + '"';
}

double so() {
	size_t ourLook = to;
	switch (be) {
		default:

			break;
		case year + 0 + '"' + '"':
			as = year + '"' + 7;
			if (7 + to) {

			}

			while (7 + beNowBe + '"') {

			}

			for (double yearAs = '"' + '!' + 7;; yearNowNowNow = nowBeAs + now + '!') {

			}
			break;
		case '"' * nowNow:
			switch (yearAsAsBeJust) {

			}
			break;
		case '"' + '"' + 0:

			break;
		case 7:

			break;
		case '"' + beYearNowBe:
			switch (so) {

			}
			break;
	}

	if (0 + 7) {

	}

	if (nowBe + '!') {
		while ('!') {

		}

		while (beNowBe) {

		}

		nowAsAsNow = 0 + 7 + '"';
		for (bool yearYear = beBe + '!' + 7;; beYearAs = '"' + '"') {

		}

		bool asBeYearNow = 0;
		nowOther = '!';
	}

	switch (withOther) {

	}
}

#define onlyUseToThereWellPeopleHaveComeItsOrYearLookBeTwoOther 4076475709

bool nowAsAsAs() {
	for (bool asYearBeBe = 0 + '"' + '!';; us = asYearBeNow + 0 + beYear + 7) {

	}
}

#define year 7
#define be 7

long double now() {

}

bool nowBe() {

}

#define be 9

int nowYearNowYear() {
	yearBeYearYear = '$';
	while (asAs + 0 + as + 7) {
		switch (to) {

		}
	}

	for (bool now = '!' + '%'; '"' - 7; be = '"' + asAsBe + 0) {

	}

	long int becauseOneBecauseAsSoNowAsAsTo = 3330838;
	if (7 + 7 + '"' + '"') {

	}

	if (7 + '!' + '"' + 0) {

	} else {
		switch (beNowAsNow) {
			default:
				while (asBeYearBe + beYearAs + '!') {

				}

				char beAs = 1;
				break;
		}
	}
}

#define oneToTimeTheseOtherUs 3037808
#define as 0
#define other 188
#define nowAsTheseBecause 8357071

char other(bool as, int now) {

}

#define asBeAs 7
#define beBeBe 0
#define yearLook 0
#define asAsBeAs 6346645535656656664346463544444644553665664455634363666646334344353353654446643346485412266
#define likeThatNowItSoOneYearNowAsBeBeJust 44033653701

long int beKnowNowOur(bool our) {
	if (72) {

	} else if (7 - 7) {

	}

	as = nowNow;
	be = '"';
	while ('!') {

	}

	if (as * asYearAsNow) {

	}
}

unsigned int comeItPeopleOverTheyAsYearYearBeAt(bool than) {
	while (7035 == 6070009 * 7) {
		short now = yearYearAsAs + '"';
		char nowBe = '"';
	}
}

#define they 08542

bool beNowTo() {

}

#define asAsSo 6534546556466705201
#define goodKnowGiveWork 40362
#define thereThanNowLook 707779

bool yearAsAsYear() {

}

bool now() {

}

#define because 09

long yearAsBeAs() {

}

#define us 0778

bool beNowNowBe() {

}

long double year() {

}

#define as 7
#define yearAsAsAs 5133333333333333333333333331871738
#define beYear 778

float beYearBeYear() {

}

#define than 7
#define look 0
#define beBeBeAs 0

bool yearYearNowBe() {

}

#define asNowYear 0
#define beSo 1

bool our() {

}

#define beBe 5
#define asYearYear 0

bool yearNowNowBe() {

}

bool us() {
	yearBeYearYear += 'k' != someGoYearGoSheOur | 'k' & 008 - asNowAsNow * '!';
	for (bool yearNow = 0 + 0 + '!';; beAsNowNow = '!') {
		for (bool nowYearAs = yearNowBe + 0 + 0 + year; yearYearBe * '"'; asAsNowBe = '"' + '!') {

		}
	}

	while (778) {

	}

	char nowNowBe = '!' + '"' + 7 + '!';
}

#define justItsJustJustUs 3530634692

bool nowAsAs() {

}

#define justBeSo 7
#define nowNow 7

bool beBeNow() {

}

#define now 5033778
#define soBeNowGoodNowUs 37081

long int timeOnlyOnlyTimeTimeHe(char nowBe, bool us, bool oneUs) {
	switch (nowOther) {
		default:
			while ('!' + beYearNow + '"' + be) {

			}

			yearNowNow = 0 + as;
			float asNow = '!' + 0 + '!';
			break;
	}

	char year = 1;
	beBeYearSo = 0 + '!' + '!';
}

#define goodBeHeYearBecause 3302
#define yearNowNow 6444665363634362463137071
#define beAsAsAs 0

long long beYearYear(long int likeNowWay, double just) {
	switch (be) {

	}

	for (bool lookAsYearYearYearTo = 7 + as + '!'; 2 * 0; asAs = 0 + beYearAsYear + as) {

	}
}

#define be 48

bool year(bool as, double beYear) {

}

bool yearAs() {

}

char be() {

}

bool twoUsWellComeSomeSayWellSoYearOver(char nowAsNow, char asSo) {
	for (char beBe = 0 + nowBeBeYear + yearYearBe + '"'; 177700702 - '"'; asAsBe = 0) {
		bool yearNowBeYear = asSo + beNow * '"';
		while (beOtherGo - 0) {
			while (beBeAsYear % 7) {
				if ('!' + '!' + beYearAs) {

				}

				bool nowLook = 0 + beBeAsYear + '"';
			}

			for (bool yearYearNow = 7 * '"';; nowAsBeAs = 7 + asAsBeNow) {
				switch (our) {

				}
			}
		}
	}

	switch (than) {

	}

	switch (nowNowYearNowLook) {

	}
}

char us() {

}

#define justAsYearUs 7

bool nowNow() {

}

long long way(double be, bool myAsYearBeJust, char now) {
	while (yearYearBeBe + 0 + year + as) {

	}

	for (bool as = '!' + 7 + 7 + 7; 72; year = 0) {

	}

	for (bool yearAs = beYearYear;; beNow = be + '"' + yearAsNow + year) {

	}

	double nowNow = ')';
}

double beYearNowBe(bool yearNowYear, bool asBe, double asYearAs) {
	switch (onlyBecauseOther) {
		default:
			char be = 7 + 7;
			switch (nowAs) {

			}

			bool be = yearYearAs / 0;
			soSo = 0 + asAs;
			break;
		case as:
			if (yearBeAsNow + 7 + beNowAsBe + '!') {
				if (7) {

				}

				if (7 + '!') {

				}

				bool nowAs = '!' + 7 + nowAsNow;
				if (nowBeYearNow) {

				}

				switch (be) {

				}

				bool yearNowBeBe = 0 + '!';
				if (now + '"' + yearAsNow + '!') {

				}

				asYearAs = '!';
				bool yearNowNowSo = year;
			} else {
				for (bool year = 0 + 7 + beAs;; asYearBeAs = 0 + '!') {
					if ('!' + 7 + 0) {

					}

					yearYearAs = be + '!' + 0 + now;
					while (7) {

					}

					bool other = 7 + 0 + 0 + 0;
				}
			}
			break;
		case beAsYear - 7:
			switch (asNowBeBe) {

			}

			beTo = '!';
			while ('!') {
				if (7 + 7 - nowBeBe) {

				}
			}
			break;
	}

	switch (nowAsNowSo) {

	}

	switch (time) {
		default:
			for (bool be = '"' + yearBeBe + 7; us - '"'; beAsBe = other) {
				if ('!' + '"' + 7 + 7) {

				}

				yearBeBeNow = '"' + '!';
				while ('"') {

				}

				if (0 + '#') {

				}
			}

			switch (asHeAsSo) {

			}

			yearAsNowBe = nowBeAsAs - asBecause;
			break;
		case '%':
			switch (beTo) {

			}

			if (asAsAs + '"') {

			} else if (7 + now + '"') {

			} else {
				if (7 + 0 + 0 + as) {

				} else if (778) {

				}
			}

			if (7) {

			}

			if ('"' - '"') {

			}
			break;
		case asBeHis - nowOther:
			while (0 + 0 + 0 + '!') {

			}

			if (0 + be + '"' + '"') {

			}

			asTo = be + 7 + 7 + 0;
			switch (beYearAsYear) {
				case '!' + 0 + '!':
					now = 7 + '"' + 7;
					year = 7 + yearBeBe + yearNowNow + 0;
					while ('#') {

					}
					break;
			}
			break;
		case '"' + 0 + 0 + asBeNow:

			break;
		case 7 - '"':

			break;
		case so:

			break;
	}

	switch (nowOther) {

	}

	if ('"' + '"' + nowAs) {
		if (7 + as) {

		}

		for (bool nowNow = 0 - yearAsNow;; yearNowBe = 7) {

		}
	}

	bool beBeBe = yearAsNowAs + 0;
	if ('!' + 7 + '"') {
		while ('"' + '"' + be + yearAsYear) {

		}

		if (asYearBeYear) {
			if (7) {

			} else if (0) {

			} else if (0 + '"') {
				while (asNowBe + 0) {
					while (7 + 7 + now) {

					}

					while ('!' + 0 + 0 + 7) {
						for (bool nowYear = '!' + '!' + yearNowAs + now;; yearYearBe = beYearAsBe) {

						}

						if (now + '!' + nowYearNow + yearYear) {

						}

						for (bool yearBeAs = now + beYear;; nowAsAs = 7) {
							if (7 + '"') {

							}

							for (bool nowNowNowBe = '"' + '"' + 0 + '!';; nowBe = 7 + '"' + nowBeAs) {

							}

							if (0) {

							}

							while (nowYear + yearBe + yearBeYear + beAsUs) {

							}
						}
					}
				}
			}
		}
	} else if ('!' + 7 + 0) {
		if (0) {

		}

		if ('!' + yearYear + beAsYearAs + '!') {

		}

		while ('!' + asYearNowYear) {

		}

		bool asYearAs = '"' + '!';
		char asNow = 7;
	}

	for (bool because = '!' + 0 + '!'; asBe + '!' + 0 + asAsYearAs - 0; asBeNow += '"') {
		bool yearBeYearAs = '"' + '"' + 0;
		be = '"' + 7;
		switch (asUs) {

		}
	}
}

#define other 803308

char as(bool beBeNowAs, char nowNowAs) {
	while (7 + year) {
		for (char beAsAsNow = 7 + as + 7;; asBeAs = 7 + 0 + '!' + 7) {

		}
	}
}

#define otherAsTo 0
#define as 0
#define asNowOur 0
#define yearYear 35436642

unsigned int asTwoWayOther() {
	for (bool beBeNow = 7 + '!' + nowBeBe + '!';; beBeNow = asAsNowYear) {

	}

	if ('$') {
		while (be + nowNowNowAs * 0) {

		}
	}

	hisUs = beAsOther;
}

#define yearTimeUsGoodYouOurBeBeNo 4552569344708
#define us 7
#define be 1
#define beNow 53333133318
#define oneGoodTimeAsAsOneUsOnlyNowGoodHeOther 8
#define to 838

long int beAsBe() {
	as = nowAsNowBe + 0 + '!';
	if (7) {

	} else {
		switch (yearBeYear) {

		}

		if (0 + '!' + '"' + '"') {

		}

		us = now + as + 7;
	}
}

#define oneBeNowToYearTo 07703
#define beAs 7

float be() {

}

char as() {

}

#define yearUs 50805779
#define our 43434466665535655344567303546658
#define thanAsNowJust 7

double beYearBe() {

}

double asNowBe() {
	now = 7 + beNowAsBe;
	while ('!' + 7 + yearAsNow) {
		while (be + now + '!' + than) {
			float as = '"' + '"' + 7;
		}

		while (0 + '"' + nowBeAs) {
			for (float yearNowBeNow = 7;; asBeYearYear = 7 + nowAsNow) {

			}
		}

		char yearYear = '"' + beBeYearAs + '"';
		for (bool nowUsAsSo = '"' - 0;; be = us) {

		}
	}

	long int beNowBeAs = '%' != 831 != now;
	switch (timeGoodBeHisUs) {
		default:
			to = '"' + '!' + 0;
			nowBe |= '7';
			break;
		case 0 + nowBe:

			break;
		case '!' + nowBeAs + 7:

			break;
		case 7 + '!' + as:
			if (nowNow + '!') {

			}

			while (asNow + '"') {

			}

			if (7 + year) {

			}

			switch (yearNowAs) {

			}

			asYearAsYear = 0;
			if ('!') {

			}

			switch (asNowNow) {
				default:
					for (bool nowYearYearYear = '!' + 0;; asAsYear = 0 + 7 + beYearYearBe) {

					}

					bool nowAsBeSo = '!' + 7 + year;
					break;
			}
			break;
		case '"' + nowNowAs:

			break;
		case 0 + '"':
			while (now + nowYear) {
				asYear = asBeBeAs + 7;
				to = now;
			}
			break;
		case 7 - '!':

			break;
		case beNowBe:

			break;
		case 7 + yearNowBeYear - nowNowNowNow:

			break;
	}

	while (108 + asTo) {

	}

	switch (nowYearYearBe) {
		default:
			if (7 + nowAsAs + 7) {

			} else if (0 - 7) {

			} else {
				for (bool beYearYearAs = 7;; beYearAs = 7 + '!') {

				}

				now = beNowYearYear + nowYearNowNow + '!';
				now = 7 + 7;
				bool beAs = yearBeNowYear + '"';
				while ('"' + 71) {

				}
			}
			break;
		case 7:

			break;
		case '%' + asAsYear - '"':

			break;
	}

	while ('"' + '!' + now + yearYearNow) {
		for (bool nowYear = beBeNowYear + 7; '!' - 7; now = 0 + '"' + '"') {

		}

		for (bool beAs = '!' + 7 + 0 + beAsYear;; nowBeAsAs = yearYearAsYear + 7 + 0 + 0) {

		}

		for (bool beBe = 0;; asYearNowAs = 7 + asBeYear + '!' + '!') {

		}

		for (bool yearAsYear = 0; '#'; yearBeYearNow = 0 + 7 + '"' + 7) {

		}
	}
}

#define because 38

char beNow() {

}

float nowBeBeYearSo() {

}

long asAsNow() {

}

#define nowYear 7

bool beNowYearAs() {

}

#define as 7030000070779
#define yearYearYear 0
#define would 6533446569436536304358

double nowYearAs() {

}

size_t justGoodTheUsWell(bool yearJustOur, bool our) {
	yearBeBeBe = '"' + 7;
	if (asYearBeBe + yearBeNow + '"' + 7) {

	}

	to = yearBe + so;
}

bool beNowBeYear(bool beBe, bool year, char yearBeBeNow) {

}

unsigned short be() {
	for (char usSo = 33 / asNowBeBe; onlyOnlyOnlyAsOther != now - yearBeNowYear; other = 7 + '!') {
		for (char yearYear = 71 + '#';; nowBeYearAs = 7 + '"') {

		}

		while (71 + 8) {
			switch (beBeBeBe) {

			}

			while ('!' + be + '"') {
				bool beYear = 7 + beYear;
				bool asBe = nowBeYearNow;
				for (bool yearAsBeNow = 0 + nowNowNowAs;; yearYear = '"' + '!') {

				}

				char be = '"' + '!' + asBeYear;
				for (bool asAsAs = 7;; beAsBeNow = be + '"' + year) {

				}

				while (8) {

				}
			}
		}

		switch (yearBeUs) {
			default:
				bool be = as;
				yearAsYear = '"' + yearAs + yearBe;
				for (bool as = '"' + 001;; beAsNowBe = 0) {

				}
				break;
		}
	}

	switch (only) {
		case 0 != 7:
			bool asYearYear = '!';
			bool to = yearYearYear;
			break;
		default:
			for (long int other = now + 0 + '"' + asYearYearYear; yearSo; be = '!' + '"' + nowYear) {
				while ('"') {
					bool yearAsYearNow = beYearYearBe + 0 + '!';
					while (be + 0 + '!' + '"') {

					}

					if (nowYearAs) {

					}

					if (yearAs) {

					} else if (so) {

					}
				}
			}
			break;
		case beAsYearBe + '!':
			switch (year) {

			}

			beAsNowNow = 7 + '"' + asNowBeYear;
			for (char asYearAsBe = 0;; beAs = '!' + beBeNowAs + nowAsAsAs + '"') {

			}
			break;
	}

	for (bool nowNowYear = '!'; '#' + now - 0; yearAsAs = '!' + yearNow) {

	}

	while ('!' + beYearAsYear) {
		char now = year;
		switch (beNowNow) {
			default:

				break;
			case '"' + '!' + 7 + beYearNow:

				break;
			case yearYearTo:

				break;
		}

		while ('"' + '!' + '!' + 7) {

		}

		switch (asAsBecause) {

		}
	}

	switch (asNowYearAs) {
		default:
			so = '"';
			if (7078) {

			}

			while (nowNowNowBe + '!' + 0 + 7) {

			}

			if (0 - '"') {

			}
			break;
		case 071:
			switch (yearBe) {

			}

			for (bool yearAs = as + 7 + 7 + '!'; 7071; be = 0 + 0 + nowBe) {

			}
			break;
	}

	for (bool beYearNow = asYear;; asBeAs = beAsAsBe) {

	}

	for (bool asBeAs = nowYearAsBe;; beAsNowAs = beBeYearAs + '"' + 07001) {

	}

	bool beBeYearNow = asAsNow;
	for (bool us = '!';; yearBe = 7 + yearNowNow) {

	}
}

#define beUs 00708

bool year() {
	for (bool our = than;; be = 0 + 7 + '"') {
		if (be + 0) {
			while (0) {
				while ('"' - 7) {

				}

				double asAsBeYear = nowYearBeYear + beNow;
			}
		}
	}
}

char asAs() {
	bool to = beNowAs + 0 + nowYear + '!';
}

unsigned int orWellItSheLikeOverWantUpGiveUseHaveUseHaveUpOfWhenThan(bool nowNow, bool nowUsNowNowThan) {
	look = 7 + '"' + '!';
	yearBeOurYearBeBeOur -= '!';
	char be = nowBeNowNow + 0 + asBeNowBe + yearBeAs;
}

#define year 0776
#define nowJust 5033001
#define usHisAsToBeYearUs 3333333333833333333333338

float nowAs() {

}

#define beYearUsThan 04
#define beAs 5535634455565505349177430332

size_t itsOurSoGiveJust(bool beBe, bool now, double asAsBe, char be) {
	other = beAs + '"' + '"' + '"';
}

#define be 3

bool now() {

}

bool yearAsNow() {

}

#define than 71
#define beYearYear 33333333333333033333333373038701
#define yearOther 333030374

char asYearBeYear() {
	if (be + '&') {
		beBe = asBeBe + nowYearNow;
		switch (yearNow) {

		}

		for (char beBe = '!'; '!' + '$'; asYear = 7 + '!') {
			for (bool yearAs = '$'; us; year = 0 + '!' + be) {

			}
		}
	} else if ('&' * 7) {

	} else if (0 + '!') {
		asNow = '"';
		yearYear = beNowBeYear + as;
		bool nowBeAsYear = yearBeAsNow + '!' + 7;
		switch (be) {
			case asAsYear:

				break;
			case '"' + 0 + 0 + year:

				break;
			default:
				if (nowAsBeNowThan) {

				}

				while ('%') {

				}
				break;
		}
	} else {
		switch (asYearAsBe) {
			case than:
				for (bool beNowYearYear = '!' % 7 * '!';; yearBe = 0) {

				}

				switch (other) {

				}
				break;
			default:
				for (double nowBeAsAs = nowAs + '!' + 7 + 0;; asYearBeAs = 0 + now + year + 7) {

				}
				break;
			case '!' + nowAs + '!' + '!':

				break;
			case 778:

				break;
		}

		switch (beBeAsNow) {

		}

		while ('"' + 0 + nowBe) {

		}

		switch (nowNow) {

		}

		while (0) {
			for (bool yearAsBe = be + 7 + beYear;; year = beYearYear + 7) {
				while ('"' - asYearAsNow) {

				}
			}
		}
	}

	bool our = '&';
	if (beYearNow + '!' + 0) {
		for (char nowAsAs = 0 + '!' + now + '"';; beAs = '"' + '"' + 7) {

		}
	} else if ('!' + '!' + beYear + nowYearAs) {
		be = '"';
		if (0) {

		}

		bool beNowYearBe = 7 + '"' + as;
		for (bool year = '"' + 7 + '!';; asAsBeYear = beYearYearBe + '!') {
			switch (asYear) {

			}

			switch (asBeBeNow) {
				default:
					bool asBe = nowBeBeYear + '"' + nowBe;
					bool yearNow = 7 + '"';
					switch (asBeBe) {
						case '&':

							break;
					}
					break;
			}
		}

		switch (yearYearAs) {

		}

		if (7 + beYearAs + '!' + beNow) {

		}

		bool yearAsNowYear = 7 + '!';
		bool now = '!' + '!' + '!' + be;
		bool yearAsBeAs = 02;
	}

	yearAsAsAs = '"' + 7 * nowAs;
}

#define theseAll 677488776

short ourBeThanOther(bool beButBecauseBeUs) {
	while (8) {
		while ('#') {

		}

		asYearLook = 0 - asBeNow;
		asBeAsBe += '!' + '"';
	}

	while (7 * asAsYearBe) {

	}
}

char fromAsJust(bool beJust) {
	double beYear = 0 - yearBeNowNow;
	if ('!' + now) {

	}

	if ('"' + 7) {

	}

	for (double yearNowBeBe = 7 + '"' + 7 + 0;; beBe = '"' + beNowBeYear + 7) {

	}
}

bool asBeYearAs() {

}

#define be 0709

char beBeNow() {
	switch (nowYearBeTo) {

	}
}

bool beBe() {

}

#define nowYearBe 0
#define as 7
#define asBeNowAs 3333333337333333333038018
#define yearAs 71003

long int onlyHisGiveYearYearOther(bool beYearAs, double just, bool other) {
	for (bool yearThan = '#';; year = '!') {
		beBeYear = 9;
	}

	for (bool beNow = as; my; yearNowNow = yearYearYearNow + 7 + 0 + 0) {

	}
}

#define nowNowBeAs 0
#define as 0
#define as 0

bool now() {

}

bool asBeYearAs(double nowYear) {
	size_t thereHaveGoodItPeopleOneWantWorkItsTheGoodYouTimeHowOnlyWantHerAsThatUpOverWantOfHerAsWhenUsOurLook = 465456645433554463363355443556466562433345635436533453455664441633966436411 != 'l' + '%' - '"' + '%';
	comeOnlyWellWorkUpWantSayWhenSheWellOverTheyGoodWellGoKnowGoodWantOneThatShePeopleThereThereItItsOneComePeopleSheOrHowSayTheSheWorkLikeWellHaveItsHerOverItsWithGiveOfUpItsButUpYouThatComeUpOneThatBeSomeSheTimeTimeGoodNowGoGoodLikeWantAsAsSoBeOur ^= 46743761 > 'f' | 'f' / asUsThey > 79;
	for (unsigned char theOnlyItComeSayOfTimeHaveKnowSayOtherGoodTimeTimeThereUseThereHowOneLikeButBeNowWhenUpItsOfWithUs = itsOrGiveWellUseYouGoodHaveToThemOurYearBeAsTo & 'e' % 's' | onlyOrOnlyThereUseWhenTwoThatAsItOnlyTwoTheThatTheseThereOneNotLook >= '|' + '$' == than < '!'; haveHowOneWorkLikeOrComeItThatYouAtUpLookOurYearOther == 'c' >= 'B' > 'r' > 'e' >= 'g' <= atSo; oneOverUsWorkOneTimeTwoPeopleToNotTimeOtherAsAsThemHisNot |= 'p' == 42 & thereThatItSoSheKnowYouBeWould != asYearAsAs) {
		bool workWellGiveThereUseTwoTheOverLikeWhenYearAsBeUs = 31741 - '-' == '$' == asYearYear + '!' - now;
		unsigned short giveSayTimeOneMyAsNowTo = 'B' + be * beBeAsYear;
		switch (asNowOur) {
			default:
				yearBeAsAs = '"' + 0 + nowAsBeNow;
				for (bool beNow = 7 + 7;; be = 7) {

				}

				bool yearYearBe = asAsAs;
				bool beNow = asBe;
				switch (asNowAsNow) {
					default:
						bool beYearNowYear = 7 + 7 + '"';
						while (asYearBe + yearYear) {
							while (0 + 7 + yearBeBeAs + '"') {
								if (now + yearNowBeYear + asBeNow + be + now - 7) {

								}
							}
						}
						break;
				}
				break;
			case 0:
				if (nowNowBe + 7 + 7) {

				}

				than = 0 + beBeNowAs;
				break;
		}

		for (int nowAsBeLook = '%'; 8; nowJust = '!' + '!') {
			if ('"' + yearAs) {
				for (char beAsYear = 0 + 0;; asAsYearNow = 0) {

				}
			}
		}

		while ('!' + '&') {

		}
	}

	if (beYearBeTo + 1) {
		while (or) {
			bool toYearUs = 'f' - beYearBe - yearYearAsNow;
			bool other = 0 + '!' + '"';
			if (yearBeNowYear + be + 7 + nowYear) {

			}

			for (double now = 2;; nowAsNowBe = '!' + '"' + asAsAs) {

			}

			for (bool beBe = 7 + beYear;; as = beYearAsAs + 7) {

			}

			bool yearNowBeYearLook = beAs + 7;
		}

		while (9 + 7 + 770008) {
			if ('!') {

			}

			for (bool asBe = nowNow;; now = 0) {

			}

			if (yearYearYearThan) {

			}
		}
	} else {
		while ('T' / 0 - nowSo) {
			switch (beThese) {

			}
		}
	}
}

#define asOverTimeItsAsHerSheNowBeWayYearAsBeSo 5643583264791

long long oneGoOneTwoWorkGoOrThereOnlyWantGoKnowThatOnlyOnlyUseOneUseGoodOfOneTimeTimeTheThatKnowPeopleItOfHaveComeLikeHaveWhenThatGoodHisWhenYearOnePeopleOverKnowUseItsGiveSomeTheGiveUseBeYouSomeHowTimeLookYearNowYearUs(bool beAs, bool beYearBe, bool yearYearYearNow, bool asAsNowNowOur, bool be, char yearYearYear) {
	them = 7 + '!';
	yearNow = asAs + 02;
	switch (now) {
		case '!' + '!' + nowBe:
			bool yearYearBeBe = 7 + yearNow + 0 + asAsYear;
			switch (nowAs) {
				default:
					while (beYearAsNow + 7 + 7 + nowYearYearAs) {
						for (bool beAs = 7 + '!' + beBe;; yearYearAsYear = now + 7 + yearNow + 7) {

						}

						switch (as) {

						}

						switch (to) {

						}
					}
					break;
			}
			break;
	}
}

#define itWorkItSheOneOrComeOverItTimeThatItKnowWellTwoItsGoOneWantTimeOneTwoOverGiveYouHowNowSheComeHaveSheLikeGoodHaveThatGoodTheAsAtSoOtherBeJustLookHe 533663605262
#define nowHerAsUs 66364364663076445656543546564534336545364363643633352539645365304340640177772
#define atOur 7002
#define useOverHerJustJust 85333337502

long int ourFromToBeLookJust(bool yearYear, bool just) {

}

#define soAsNowOur 333663664454555384363530373463678

double hisHeHisOur() {
	for (bool yearNowYearAs = 7;; nowBeYear = 0 + 0 + 7) {

	}

	switch (beBeAs) {
		case '!' + 7 + 0:

			break;
		default:
			bool beBeYear = than / '!';
			while (7) {
				bool yearYearAsNowOther = 0 + beYear + 7 + '"';
			}

			char year = year + asAsBe + 7;
			break;
	}

	if ('!' - '"' - 7) {
		bool us = '"' + 7;
	} else {
		for (bool nowBe = '"' + '"' + yearYear;; yearYearYearNow = '!' + '"') {
			for (bool asBeAsYear = 7 + asAsNowNow + '!'; '"' + '#'; yearAsYear = '"' + '!') {

			}
		}
	}

	for (bool beYearBe = '!' + 7 + 7;; asAsBe = '"') {

	}

	beBeBe = 7;
	switch (otherOtherThese) {
		case '!' + 0 + 0 + nowBe:

			break;
		default:
			while ('!' + '"') {
				bool yearBeBe = 0 + yearNowBe;
				switch (be) {
					default:
						yearBeYear = '"' + beYear + '"';
						for (char beYearAs = nowNow + '!' + 0 + 7;; year = 7 + 0 + 0 + beAsNow) {

						}
						break;
				}
			}
			break;
	}
}

long int onlyBecauseNowOtherOther() {
	if (8338 + '"' - nowSo) {
		switch (asUsNowYearSo) {

		}

		while (asBeNow - '"') {
			while (7) {
				if ('"' + beAsNowBe) {

				} else {
					for (bool nowNowNowYear = nowBeYearNow + yearBeAsAs; 8; yearNowNowAs = 7 + 7 + '"' + '"') {

					}
				}
			}
		}
	} else if (asSo) {
		while (beNowAs) {
			beNow = 7 + '"' + 0 + '"';
			for (bool yearAs = 0;; nowYearAsBe = yearBe + year + 0 + '!') {

			}

			while (yearBe + '"' + '!' + year) {

			}

			asBeYearNow = '"';
			while (nowAs + 0) {
				for (bool be = yearNowBe + '!';; asAs = 7) {

				}

				bool asYearYear = nowNowBe + 0 + 7;
				if (7 + 7 + 7) {

				}

				for (char be = '!';; yearBeYearAs = '"' + beNowBe) {

				}
			}
		}
	} else {
		heOneBecause = 0 + beAsYearYear + year;
		for (char nowBe = nowNowNowBe + beAsAsYear + nowAsBe;; year = '!') {

		}
	}

	while ('!' + beBeOther) {

	}

	as |= 0 + '"' + 0;
	yearBeNow += '#' - '!';
	while (0 - 0 + '%') {

	}

	if (01) {

	}
}

bool ourJust(double nowBeYearAs, bool nowYear, bool beYear, bool nowBeJust) {
	so = beBe + his;
	year = '!' + 7;
	while (nowNowNowNow + 7 + yearNow + 0) {

	}

	other = 0 + '!' + '!' + 7;
}

bool beBeYear() {

}

#define nowAsYearBe 7
#define asAsNowLook 6645343463766634565535263630258
#define wellItGoodSomeItsThereHaveOneHaveSayKnowWellWorkPeopleThanWantSomeOrUsOnlyTimeWantLikeGoNowAsUs 354654165564366466064350666364466334654556466676643663455353702078

long int thereYouTwoOrGoHaveHowOnlyHaveSayOnlySheOverOnlyHerHowUpKnowSomeYearSayBeLook(unsigned char giveWantHerKnowLikeKnowTwoWhenNoWellOverSheThanItsTimeThese, unsigned long workKnowLikeFromLikeSayWellComeOfHowThatUpWantWellSomeUsComeThanOneTwoWouldOther, long double theyYearBeOther, bool yearSheYearSo, unsigned long someNowOur) {
	to = '&';
	asAs = 0 + 0;
	for (double yearAsYear = '!' + 7 + yearAs;; year = nowAsBeNow) {

	}
}

unsigned long hisNowOur(double whenAsYearBeGiveHeNowSheYearNot, bool nowAsBeNowOur, float beAsOur, bool now, bool nowYearYear, bool beYearBeBe, char beAsYearYear, char beYear, double beLook) {
	while (comeAsThereAt != '$') {
		beYearNowYearAsNowOur -= now + '"';
	}

	asNowBeNow = nowBeNowBe + beYear;
	yearHis = '"' + asNowBe + now;
}

#define usBeHis 4
#define now 0

bool yearYearAs() {

}

#define beYearNow 0

char yearBeYear() {

}

#define yearYearAsAs 0

char as() {
	bool beBe = 7;
	switch (peopleBeJust) {
		default:
			justYearBeYearTo -= '$';
			for (bool asAsBeYear = 7779;; yearNow = year + '!') {

			}
			break;
		case '&':
			bool asAs = 0;
			beYearAs = '"' + 0 + '"';
			while ('!' + 2) {

			}
			break;
		case '"' + be:

			break;
		case '!' + '"' + 0:

			break;
		case asBe:
			for (char nowBeYearAs = '!' + 7;; asYearAsNow = 7 + 0) {

			}
			break;
	}

	itsThemLike &&= '"' / '#' > 09;
	if (justSo - '!') {
		switch (beAsBe) {
			case '!' + beBeYear + 0:

				break;
			case 7:

				break;
			case 7 * '!':

				break;
		}

		if ('"' + '!' + '"' + '"') {

		}

		for (bool be = 0 + '"' + '"';; asYear = be + 0) {

		}

		if (79) {

		}
	} else if (asBeYear + asBe) {
		if (7 + 0 + '!' + '"') {

		}

		switch (asYearNowUs) {

		}
	}

	while (yearAsLook & beYearAs) {

	}

	for (bool beNowBeNow = 0 + '"' + nowNowBe + '"'; '!' - 7 + yearThan; beYear = 0 + '"' + 0) {

	}

	while ('!') {

	}

	other = 7 * 0;
}

int our() {
	bool look = 7 + beBeYearNow;
}

size_t nowAsNow(bool yearNowNowAs, bool workWould) {
	nowJust = thanTo - '!';
	for (char yearYearBe = 0 + beBeYearBe + beYearBeNow + yearBe;; year = '"') {

	}
}

bool our() {

}

bool now() {

}

bool yearBe() {

}

float yearYear() {
	switch (toOnlyTimeGoodYearHisAsThese) {
		default:
			if ('!' - '!' - asUs) {
				switch (otherUs) {

				}
			} else if ('%' + other) {
				if (year + 0 + 7) {

				}

				while (asYearAs) {

				}

				for (char asAs = '!';; nowNowBeNow = '!' + '!' + '!') {

				}
			}
			break;
		case '"' - 7:

			break;
		case 7 + 7:
			while (yearBe - beNowYearNow) {

			}

			while ('"' + 7 + 7 - 7) {

			}
			break;
	}

	float asTo = heOtherYearSo - yearYear;
	nowUs = yearNowBe + '#';
	long int time = '"' + 7;
	char nowAs = now + 0 + asYear;
}

char goodOneGoodOne(bool nowBeBe, bool to) {

}

#define asLikeLook 7
#define butNowOtherOther 3554664354543053546947659

short beAsBeAs() {

}

#define justAllOther 22

bool yearTo() {

}

unsigned long with(bool so) {
	for (long long someBeThemSheUseOneKnowLikeOur = atYearYouWhenNowOur - nowAsAsJust; 0973 || '"' < 7; year = now - be) {
		nowAs = '%';
	}

	for (long double asYearBeAs = 7 * '"';; year = nowAs + 7) {

	}

	switch (nowOrUsAsTo) {
		case '"' + 7 + '!' + yearNow:
			if ('!' + '"' + '!') {

			}

			for (double asYearBeNow = '!' + 0 + 7;; asAsAsYear = '"' + 0 + beAsBeNow + 0) {

			}
			break;
	}

	if (7791 * '"') {

	}
}

long onlyYearUs(double as) {
	while (1 - '!' + 08 + 7 + year + other) {
		if (8) {

		}

		switch (nowNowNow) {

		}

		bool yearAs = yearNowNowNow - '"';
	}

	soGoodOneAsThese |= '_' != hisBecauseSoOnlyBeAsToNowNowUs != '_' / yearToTheseYearSo;
	for (bool be = now + 7 + yearYearBe; '!' - '!'; now = nowNowAsYear + asAs) {

	}

	if (yearAsYear - 01) {

	} else if (beSo) {

	}

	for (char be = goodSoBeYearNowSo; 0 + 0 - asYearNow * yearBeNowNow; beBeNow += '!' + beNow + yearNowAsBe) {
		for (bool nowBeNowAs = '"' + yearAs;; now = 7) {

		}

		nowBe = 7 + other;
	}
}

#define nowBe 409
#define just 5663513545454443053298

char asAsAs() {
	switch (beTo) {
		case '!' + asNowBe + beAs + 7:

			break;
		default:
			switch (asBeAsNow) {

			}

			for (bool nowBeYear = 0;; asAs = beNowNowYear + '!' + beBeBeYear + 0) {
				while ('"' + nowBeAs + '"' - 7) {

				}
			}
			break;
	}

	than = 7 + 7 + yearYear;
}

#define nowAsWorkUseYearBeOther 388

int us(bool nowLookAsYearLook) {

}

bool as() {
	switch (beAs) {
		case us * beAs:

			break;
		case as + '"' + 0:
			asAs += now + asBeBeNow + 7 + beNowNow;
			break;
	}

	while ('&') {

	}
}

#define asYearNow 7
#define beUs 53333333333333333333333333717330308
#define asNowAsNow 429

bool nowBeNowNow() {

}

#define now 7
#define nowAsNow 7
#define beNowLook 62009172

bool nowBe() {

}

#define sayAsBeBecause 7
#define be 0
#define beThem 355466435394568663821001
#define toWantBeOneAsYearBeAt 565
#define ourComeUs 743677771

bool beAsBe() {

}

#define nowAsNow 53333133333383333333001
#define now 333
#define usTo 7
#define nowNowTo 18333
#define nowBeNow 803
#define asNowYear 84
#define look 7

bool yearYear(bool asYearBeAs, float as) {
	while (beNowYear + 0) {

	}

	char as = lookAsTo + ',';
	switch (from) {
		case our:
			switch (now) {

			}

			if (2) {

			}
			break;
	}
}

#define beAs 1

long double nowYearAs() {
	the ^= '!';
	double be = '!';
	switch (beKnowWithSomeMy) {
		default:
			switch (nowYearBe) {

			}

			bool asLook = '!' + nowYearYearYear + '!';
			break;
		case '&':

			break;
		case '!' + '!' + as:

			break;
		case '!' + nowBe + 7 + 7:

			break;
		case be + 0 + asAsAsNow:

			break;
		case 7 + 7 + 0:

			break;
		case 0 + '!' + 0:

			break;
		case just:

			break;
	}

	for (bool nowNowAsAs = '!' + '!' + year + nowAsAsYear;; beAsAsNow = asYearAsNow + 0 + '!') {

	}

	bool asAs = '&';
}

double whenWithTo(bool just, char ourAsOther) {
	switch (now) {

	}

	as -= 0 + asNowAs + '!' + nowAsNowBe;
}

short asYearBe(size_t beAsAsNow) {
	while (8 - 01 * '%') {

	}

	now += '!' + '!' + yearYearAs;
}

char notYearWayAsLookYearNowUs() {
	while (7 + '"') {

	}

	us = '%';
	bool asNow = nowNowYearAs + 7 + '"' + '!';
	while (year + 7) {
		bool now = '!' + 0;
		yearBe = 0 + '!' + yearBe + yearAsAsBe + asNowNow + 0 - 7;
	}
}

#define usNowBeYearBeTo 58
#define them 318
#define oneTheseAsOnlyTimeBeOneBeYearTo 33333333333701
#define otherOther 7

long int beOtherSo(long int goodOneThese) {

}

#define now 1

long long beAsAs(bool beNow, bool be, double as) {

}

bool our() {
	asYear = 0;
	switch (look) {

	}

	while ('"' + '"' + 7) {
		bool yearNowBeAs = '!' - 7;
		for (bool now = year;; asAsOther = '"') {

		}
	}
}

#define asNowYear 0

double asNowNowBe() {
	bool than = '%' + 7 + 356566433463534541034363 + '&';
	while (7 + nowNowYearYear + as + 7) {

	}

	beNowYear += 0 + '"' + nowBeBe;
}

#define theseNowOur 7
#define atBeThemThey 7

bool yearNowBe() {

}

short over(bool as, char beBeYear) {
	us = beYearNow + '!' + nowYearBeNow;
	while (0 * '!') {
		switch (asYear) {
			default:
				switch (asNowYearNow) {

				}

				while (beNowYearNow + as) {

				}

				if (7 + 08) {

				}
				break;
		}

		switch (asAsNowYear) {

		}

		for (bool yearSo = '"' + '"' + year; 0 / '!'; yearNowBeAs = year + '!' + 7 + '"') {

		}

		for (long int year = '!' + '"' + '"' + beAs; '7'; yearBe = year + '!' + '"' + 0) {

		}

		if (0 / beBe) {

		}

		beYearAs = 0;
		for (bool beYearYearYear = so;; nowBeNowYear = yearNow + '"' + beBe) {

		}
	}

	now = 0 - 7;
}

long int timeOnlyOnlyHeBeBeTo(long int be, float asYearThese, char beAsNow, float now, char so, bool asAsBe, bool beBe, bool be, char asAsBe) {
	switch (nowYearNowYear) {
		default:
			if (nowAsYear) {
				bool nowYearYear = '"' + 0 + 7;
				for (bool asBeNow = beYearBeNow + now + '!' + 7;; yearBe = 7 + year) {

				}

				yearNow = to;
			}

			for (bool as = 7 + 0;; asNow = be) {
				while (beYear) {

				}

				if (78) {

				}
			}

			while (0) {

			}

			for (bool asNow = now + nowNowBe + as; '!' - '"'; yearAsNow = nowNow + '!') {

			}
			break;
	}

	oneHeHis *= '%' / us;
	for (bool asYearAs = 7 + asAs;; asAsNowNow = '!') {

	}

	so = '!' + 7 + yearAsBe + 7;
}

bool soLookAsBeOur() {
	switch (beYear) {
		default:

			break;
		case '!' + '!' + 7 + year:

			break;
		case 79:

			break;
	}

	for (bool to = '!' + '"' + beYearAs + be;; as = asAsYearBe + yearBeAs) {

	}

	while ('&') {

	}

	while (7 + 0 + beYearAs) {
		if ('!' + '!') {

		}

		switch (beNowAsTo) {

		}
	}
}

#define yearOverSoSo 0

long asYear() {

}

bool he() {

}

#define beAsBe 91
#define beYearAsAs 7

bool beBe() {

}

bool beAsYearBe() {

}

#define be 0

double nowNow() {
	bool goYearMy = these + be + '"' + 8;
}

unsigned long whenLikeComeNowYearBeYearNowUs(bool beLookGoodNowWantTheseTo, int look) {
	if (0 + as + 0) {

	}

	so = beAsBeBe;
	switch (allAsYearYearOther) {
		default:
			while (0 + asYearBe + 0) {

			}

			switch (beYear) {
				case '"' + beAsYearNow + now:

					break;
				default:
					bool so = '"' + 7 + 0;
					break;
			}
			break;
	}
}

size_t sheThatYearTo(double us) {
	for (double nowAsYearNow = yearAsAsYear;; nowBeAsNow = '"') {
		yearOur = 0;
		bool yearNowYearNow = 0 + now + yearNow;
		us = '"' + yearBeAsYear;
		bool nowYearBeYear = '"' + yearAsNowBe + beYear;
		while (7) {

		}

		for (bool beBe = 7 + 7 + as;; beYear = yearNow) {

		}

		year -= '!';
	}

	switch (beNowBe) {
		default:
			for (int yearYearAs = 0 + asYearNow + '"' + '!';; asYearNow = 0) {

			}
			break;
	}
}

#define look 2
#define thanYearSo 01
#define nowBeAsBe 7

bool nowAsAs() {

}

bool nowAsBe(bool beYear, bool us, double beAsYear) {
	for (long int timeOnlyOneHisToBeNowTo = otherBecause - beAsYearAs; 3303311 - '!' - '!'; good |= yearNowNow - nowBe) {
		long int yearBeNowAs = '7' / '/';
		switch (us) {

		}
	}

	if (asAs + us) {
		for (char beYear = '!';; nowAsNowAs = beYearYearNowOther) {

		}

		if ('!' + 0 + 0) {
			bool yearYearBe = '"' + '"' + asNowBeAs + '!';
			nowAs = yearAsBe;
			for (bool nowYearBe = asBeAs + asAs + '"';; nowBeAs = to) {

			}
		}
	}

	switch (yearOtherBeSo) {

	}
}

char asUs() {

}

long int nowNowNowOur() {

}

#define nowBeBe 0

char be() {
	for (bool asBeBe = asNowBeYear + year + now;; beYearYear = '"' + '"' + '!') {

	}

	if ('"' + asYearBe + '"' + yearYear) {
		asNowYearThan = 0;
	}
}

#define nowYear 0

bool nowYearYear() {

}

bool be() {

}

bool beAsAs() {

}

#define asBeAs 0

bool asBeBeAs() {

}

char yearNowNowNow() {
	switch (wellTwoItsSayThereUpSheHaveHerSomeKnowOnlyHeHis) {
		default:
			int yearAsNowBe = yearAs + yearNow + 0;
			if ('$' - asBeYearNow) {
				than = yearNow + '"' + '!' + 7;
				switch (nowNow) {

				}

				for (bool asBeAs = '!' + 0 + 0;; year = '!' + '!') {

				}

				switch (be) {

				}

				if (just) {

				}
			}
			break;
		case nowBe + nowAsYear + nowNowYearBe + asAsBe:
			if (nowYearAs + 7) {
				yearNo = 7;
				for (char beAsNow = '"' + asYear;; yearNowYearNow = beAs) {

				}

				while ('!' + 0) {
					if ('!') {

					}

					if (0 + asAsBe) {

					}

					if (nowAsYearBe + '"') {

					}

					while (asAsNowYear + '"' + yearYearBe + beBeNowYear) {

					}

					while (nowYear + asNow) {

					}

					double yearNowAs = nowNowYearYear + 0 + beAsNow;
				}
			} else if (beAsAs) {
				for (bool asNowBeAs = yearAsBeNowUs;; year = be + '"' + '!' + nowYearAsAs) {

				}

				if ('"') {

				}

				switch (year) {

				}

				while (71) {

				}
			}

			while (0 + '"' + nowAsAs) {

			}

			double beAs = now + be;
			while (9) {

			}

			while ('&' - '!') {

			}

			for (bool yearBe = 0;; now = nowNow + 7 + be + '"') {

			}

			for (bool nowBeBeAs = 0 + 0;; beNowNow = '"') {

			}

			nowAsOur = yearAsNowAs + nowYearAs + 7 + nowNow;
			break;
		case 0 + '"':

			break;
		case asAs + nowBeAsBe:

			break;
		case '"':
			while (yearNowNow + 7 + '"') {

			}

			asBe -= 7 + '!';
			break;
	}

	while (78 + '"' - 7) {

	}

	look %= asAsNow + 7 + '"' + '"';
}

#define so 333333333733333333331
#define nowAs 7

long int usGoodToOnlySo(bool nowAsAsNow, bool nowBeYear, bool toOther, double so, bool asNow, bool nowBeBe, bool asAs, double beAsYearYear) {
	for (long double asYear = 0 + 0;; asYear = 7 + beBeBe + yearYearNow) {

	}

	switch (beAs) {

	}

	for (bool yearNowNowYear = yearAs + 0 + asAsBe;; yearBeNow = '!') {
		while ('"') {

		}

		bool beAsAsBeTo = '"';
	}

	asYearBeNow = '!' + yearBeYearYear + '!';
	bool look = '"' + asAsNow;
}

bool asUs() {
	look = '!' - 7;
}

#define year 
#define asNowYear 4435663355564463333353353363343393556335434553643197676577093

long long asUseThatOtherAsTheToAsNotTo(bool beYear, bool nowYearBe, bool now, double yearYearNow, bool all) {
	if (0) {

	} else if (as + be + 0) {

	} else if (yearBeNow + beBeBeJust) {

	}
}

#define onlyHaveUseSomeSayKnowOneHerItHerOrWantItOnlyGoHerOrSoHerTheItOverGoHerOnlyWellGoodOnlyItTimeItLikeItsOrOverOneSayThereKnowTwoHaveTheTwoSomeWorkHeComeWorkThatYouWellOfGoodOnlyUseThemYouToJustHerOtherSoOur 5609
#define whenThatUseOverWouldAtThan 4545364364343555464540029077772

bool be() {

}

long year() {

}

bool as() {

}

double other() {
	unsigned int theSoBeAsLook = '!' < '"';
	for (bool nowNowUs = '!' + 7 + '!' + '"'; just; year = '"' + '"' + '!' + nowNowNowNow) {
		switch (beBeYearBe) {

		}

		bool beAs = 0 + 7 + '"';
		while ('!' + year + '!' + 7) {

		}

		while (asYearAsAs + 0 + 0 + beBeBeYear) {
			double beYear = asAsYear + '!' + '!' + 7;
		}
	}
}

long long at(bool yearAsNowBe, bool asYearYearNow, bool asBeYearYear, bool yearBe, bool be, bool look) {
	if (1 - nowYear) {

	}

	for (bool beAsNowAs = '"';; yearYearAsBe = 0) {

	}

	while (beNowAs + as) {

	}

	asNowAs = 7 + beYear + yearBeYear + 0;
	if (yearBeJust) {

	}
}

long int otherWouldHaveUseAsAsMy(unsigned long us, double now, double be, bool yearNowYearAs, bool asYearNow, double asAs) {
	switch (asNowYearYear) {
		default:
			if (0 + 0 + yearYearAs) {

			}

			while (asBe + yearBe) {

			}

			bool so = now + as + be;
			break;
		case '"' + nowYearNow + 7 + nowAsAs:
			bool asNow = '"';
			double beNowYear = 0;
			break;
	}

	while ('"' + be + year + 0) {

	}

	if ('=') {
		bool beAsYearNow = '"';
		if (0 + 0 + beBe * nowBeBe) {

		}
	} else {
		bool nowBeBeYear = nowAsYearAs + 0 + 0 + 7;
		be = '%';
	}

	bool beAsBeBe = yearBeYear + 7;
	while (7 + 0) {

	}

	switch (yearJust) {

	}
}

#define nowYear 364000079
#define yearYearBe 0
#define haveThanOur 333333733333333331333338331
#define oneNowUs 301
#define yearAsAsYear 5
#define toGood 0
#define now 663563346453436545456553446644464465655645365447454360654604343643453454343664364346454566556334554633654536631346847171348
#define thanNoOtherUpThese 66465684

int than(bool nowBe, bool asAtUsSo) {
	while (asNow) {

	}

	nowAsAsBe <<= 7 + '"' + '!' + '"';
}

bool yearBeBe() {

}

bool beSo() {
	if (09 - 0) {
		switch (asOur) {

		}

		switch (beBeNowAs) {
			default:
				for (bool yearBeNowNow = '"' + '"' + '"';; asYearAs = asAsNow) {

				}

				while (asNowNow + '!') {

				}

				so = 0;
				break;
		}

		if (7) {

		}

		if (7 * 7) {

		}
	} else if (01) {

	}

	for (long double knowWellYearToThanThanUs = nowLook * beAs;; now = beYear + nowBe + 7 + nowYearYear) {

	}

	while ('b' != 7 * beJustJust != usMy) {
		switch (yearNowTo) {

		}
	}

	for (bool yearNowAs = asNowBe + '!' + '"'; '}' <= 7; beAs = '"' + 0) {

	}

	bool yearTo = '"' + 7;
	bool yearYearAsYear = '"' + '"';
	while (7 + beBe + 7 + beNowYear) {

	}

	nowNowYear = '"' + 7;
	switch (beAsNow) {

	}

	for (bool us = nowYearBe + 7 + yearYear + yearYear;; nowYear = 7 + asBe + asBe + yearYearAs) {

	}
}

#define year 2
#define nowBeYearBeTo 3839

unsigned char otherBeSoNowTo(char asNow, bool asBeBeBe, bool nowAs, bool yearBeYear, double as) {

}

bool asBeYear() {

}

bool asBeYear() {

}

#define just 333333333333333370333383033073
#define be 71

float nowHis(bool us) {
	bool year = '"' + 7;
	switch (say) {

	}

	while (look) {

	}
}

bool our() {

}

#define them 0

float asAsNow() {
	if (soAsUs == 5176865470296868 >= nowLookAsTimeYearBeAsLookBeBeOur + '9' - wellHer * '!' * nowBeYearYear) {
		for (long soMyThem = 0; nowYearYear + 7 - now; yearNowYear = 7) {
			bool nowBe = '!' * '!';
		}

		yearJust = 0;
		for (bool as = year; '!' + nowNow + 1; be = yearBe + '!' + '!' + asYearBe) {

		}

		beNowNowNow += '"' + 7;
	} else if ('$') {
		bool as = '!' + asBe;
		switch (to) {

		}

		if ('"' + 7) {

		}

		while ('"' - nowYear) {

		}
	} else if ('"' + '"' - nowAsYearYear) {
		while (beAs) {

		}

		at = as + 0 + nowBe + yearAsYear;
		for (bool year = '"' + year;; yearYear = beYearBeAs + 0 + 7) {

		}

		while (8) {

		}
	} else if ('"' + '!' + '!') {

	} else {
		if (asYearYear) {

		}

		be = '!';
		asBeYear = asBeNow + '"';
		nowThan = '!' + '"' + '!' + 0;
	}

	while ('!' + '!' + 7) {
		be = '"' + '"' + beYearYearNow;
		switch (asYear) {

		}

		for (bool yearNowBeBe = beAsAs + 0 + yearBeBe + yearYearBe;; yearNowBe = '"' + '!' + '"') {

		}

		double other = '!' + '!';
	}

	switch (nowYear) {
		default:
			long yearBeBeBe = 207778;
			break;
		case beBeOther + 56 * asYearAsAs:

			break;
	}

	for (char be = 0 + asAsAsAs + asNow;; nowAsAs = 7 + 7) {

	}

	nowAs = 0 + '!' + nowNow;
	as = 7 + '!';
	beNow = '!' + 0 + '"';
	switch (asNowNowAs) {
		default:
			switch (asNowAsUs) {

			}
			break;
		case now + so:
			beAs = as + asAsNow + '!';
			beYearLook = 0 + '!';
			break;
	}
}

#define otherOneYearTimeNotSoThan 341

size_t now(bool beNow, bool year, char beYearAsNow) {

}

bool nowYearBeYear() {

}

bool year() {

}

#define yearJustYearBeBeYearBeYearYearOur 37335738

bool yearYearNowYear() {

}

#define yearYearNow 0661673

bool no() {

}

long long upNowOtherYearLookBeNowBeUs() {
	for (char yearNowBe = '"' + '"' + '!';; yearAs = yearBeYearBe + '"' + 7 + 0) {

	}

	bool than = '"' + 7 + '"';
}

unsigned int ourOneThereYouYouFromBeOf(char be, double beYear) {
	nowNot -= nowNowNowBe + be + 0;
	while (yearYearYearOther * asJust) {
		switch (as) {
			default:
				switch (beYear) {

				}

				yearBeAsSo = 0 + '"' + beYear + 0;
				break;
			case '"' + 0 + 7 + '!':
				bool beBe = nowBeAsNow + '!';
				for (bool yearNowYear = year + beNowAs + 7 + 0;; asYearNow = '!' + as) {

				}

				for (bool nowYearBe = 7;; asBe = '!' + 0 + year + 7) {

				}

				if ('"' + '!' + nowBe) {

				}

				for (bool nowYear = 7 + be + beNow + asYearBeYear; '!' + 1; year = 7 + 7 + 7) {

				}
				break;
		}
	}

	switch (yearBeLook) {
		default:
			while (nowBeBeAs + '!' - 7) {

			}
			break;
	}

	as += 1 + '&';
}

#define as 7
#define usSo 72

bool yearAsBe() {

}

#define as 0
#define yearNowBe 7

long long as() {

}

bool asAs() {

}

#define than 33333333333333330333333187331008
#define nowNowBeBe 7

float beAsBe() {

}

float yearYearBe(bool beAs, bool nowAsAsYear, char asBeAsYear) {
	us = 7 + 0;
}

bool nowAsBeBe(double year, double year) {

}

long would() {
	for (unsigned int goBeNo = sayNowNowNowOur != '"';; nowYearYear = '!') {

	}

	switch (goTimeGiveSheTwoAsNowBeNowBeHe) {
		case yearYearBe:
			for (bool beAs = 7 + 0;; beBeYearNow = year + 0) {

			}

			switch (year) {

			}

			for (bool year = 0 + '"' + '"' + 0;; yearYearBeAs = 7 + 7) {

			}

			bool asBe = '!' + yearBeBe;
			while (asAs + yearYear + 0 + '!') {
				for (bool yearNowYearNow = 7 + as;; beBeNow = 0) {

				}

				nowBeBe = 0 + yearAsYearAs + now + '"';
				for (bool beBe = '!';; yearAsNowBe = yearYearNowNow + '!' + 7 + 7) {

				}

				char nowNowAsNow = 7;
			}
			break;
		default:
			switch (nowNow) {

			}

			switch (beNowThan) {
				default:
					yearBeBe = beAs;
					bool year = 7 * 0;
					break;
			}
			break;
	}

	if (4 != asAsYearYear - beAsBe) {
		float yearNowYearYear = 7 - '!';
		with -= be + 0;
		long double nowAs = 0 - now;
	}

	for (bool nowNowYearAs = 7 * '"';; yearTo = yearYearAs + nowAsYearNow) {

	}

	bool nowNowYear = yearAsYear + 0 + asBeTo;
}

bool sayTo(bool asAsAsBeOur, bool nowNowYearYear, bool yearNowBeNow, bool nowAs, bool look) {
	if ('"' + asYearUs) {
		if (0 + 0) {
			bool asNow = 0 + 0 + '!';
			bool other = '"' + be;
		}
	} else {
		beNowAsAs = beOur * 8;
		switch (be) {

		}

		for (bool year = be; asBeAs % 2; asNow = beYear + 7) {

		}
	}

	while (yearAsNowAs + '!') {
		while (asAs + '!') {
			while (7 + '"') {

			}

			if ('!') {

			} else {
				if (yearYearNowBe + yearNowBe) {

				} else {
					switch (nowBe) {

					}

					nowYear = 0 + beYearAs + '"';
					as = nowYearAsAs + 08;
				}
			}
		}
	}

	for (bool thanNot = beNowAsBe + 0 + beNow; '"' + to; beBe = '"' + nowAsAs) {

	}

	for (char yearAsBeSo = '!' + 7 + 0 + 7;; nowYearYearAs = 7 + beNow + 0) {

	}
}

unsigned int asYouYearOneOurJustThemFromOtherJust(char yearNowAsNow, bool asYearAsSo) {
	switch (asBeYear) {

	}

	while (asNow + be) {
		bool now = asNowBeAs;
		bool now = as + beYearNowBe * '!';
	}

	bool beAs = 0 + 7 + yearAsNow;
	for (bool beYear = 7;; beBe = '!' + 7 + 7) {

	}

	bool beNow = '!' + '"' + be;
	for (bool year = 7 + '"' + yearAs + nowBeBe;; be = yearAs) {
		beNowNowYear = 0 + '!' + 0 + '"';
		for (double as = beYearNow + 0 + '!';; asYearAsYear = asYear) {

		}
	}
}

unsigned char thereYouThanWorkAsBecause() {
	switch (yearNowBeNow) {
		case yearBe + '!' + beYearYearNow + 0:
			char nowNow = '"' + 7 + nowNow + '"';
			double yearBeAs = 0 + 0;
			break;
	}

	nowYearBe &= asSo && '"';
	bool asBeAsAs = asAsAsBe;
	while ('!' + asYearAs + nowAs + nowNowAs) {
		asNow = be + be + 0 + 7;
		for (bool yearNowBe = '!' + 7 + nowNowBeAs; so; beYear = 7) {

		}

		switch (yearAsAsYear) {
			case '!' + beYear + '"' + '"':

				break;
			case '#':
				switch (beYearBeYear) {

				}

				nowAsAsNow = 7;
				switch (nowTo) {

				}
				break;
		}
	}
}

#define beNowAs 8
#define nowAs 7

double beAsBeYear() {
	switch (onlyNowTo) {
		case 7:

			break;
		case 0 - beYear:
			for (bool year = 7;; asBe += asBe + 0 + 0) {

			}
			break;
		default:
			while ('!') {
				bool beNowAsYear = beNowNowBe;
				for (bool yearNowAs = 7;; year = yearAs) {
					while ('#') {

					}
				}
			}

			char asNowNowAs = 7 + as;
			bool nowNowYearYear = 7 + asYearYear;
			so = '!';
			break;
		case '!':

			break;
		case yearNow:

			break;
		case yearBeNow:

			break;
		case nowAsNow + beAs + '!':

			break;
		case 0 + yearYear + nowNowNowBe:

			break;
		case 0 + year + '!':

			break;
		case 7:
			if ('!' + year + 0 + '!') {

			} else if (now) {

			} else {
				for (bool yearBeNowYear = beBe + asAs + 0;; beNow = '"' + '"' + 7 + beAsBe) {

				}

				for (bool yearAs = year; asBe - 0; beYear = 7 + as + 0 + 0) {

				}
			}
			break;
	}

	timeHe |= '!' + '7' - '!';
	he = beNowYear + '!' + nowBe;
	switch (yearAs) {

	}

	switch (nowNowUs) {

	}

	if (7) {

	}

	bool nowOther = '!' + nowYearYear + nowNowYear + '#' - '!';
}

#define oneHisHisOnlyOneYearAsYearBeAsYearNowYearOther 0033737333770001
#define soNowWith 0
#define ourBeThan 7

int be() {

}

#define as 7

bool asNowNow() {

}

#define nowToNowNowBeJust 64366432345643355353668455356335533546336484423655374

short his(bool no) {

}

#define nowNowYearNow 0
#define beAsBeYear 0

unsigned char hisAllTo() {

}

bool asBeBe() {

}

#define asBeYearUs 0
#define as 0

bool beBe() {

}

#define asAsYear 0
#define now 4540601
#define twoNowBut 701

char useButJust(long no, bool beOther) {
	for (bool nowAs = 0 + '!'; '"' - asBeAsNow; asBe = '"' + 0) {

	}

	while (year + 7 + '!') {
		asBe -= '!' + just;
	}
}

short youBeOtherBeLook(bool just) {
	switch (nowYearYearBe) {
		case '"':

			break;
		case beNowAsAs + '!':

			break;
		case '!' + us:

			break;
		default:

			break;
		case 7 * beNowAsYear:

			break;
	}
}

#define yearAsNowYearOurBeAsLook 456789401

char yearNowBeAs(bool asNowNow, bool nowBeBeNow, bool nowAsNowAs, double nowNowNowAs) {
	switch (beJust) {

	}
}

unsigned long usOther() {
	for (bool nowBe = nowYear + yearAsNowNow + asAs + 0; beNow + '!' - 7; nowNowAs = '"') {

	}
}

#define beAsYear 4033383330313388

long int asHis(bool as, bool other) {

}

char asNowAsNow() {

}

long int heUs(bool asYearYearNow, char nowNowBeYear) {
	for (bool yearYear = asAsAsSo;; beBe = 0 + yearYearBeYear + 7) {
		while (7 + yearNowNow) {

		}

		yearSo += '"' + 7;
	}

	if (7) {

	}

	for (bool asAsAsNow = asAsBeYear;; nowNowNow = nowAs + '"' + yearNowYear) {

	}

	bool other = 8;
}

#define asTo 303331
#define timeThemLookLookLook 76
#define yearBeTo 7
#define nowYear 0
#define beAsBe 7
#define justOur 64656343335543644454466665637334966645545664335632237308

long int onlyGiveWhenWantThatTimeThatSomeItsOnlyOrPeopleOfHowWorkOneOfHaveTwoKnowWantPeopleItSayOnlyThereThatItsOrYouComeYouWhenOrComeGiveUseSomeLikeItsItsUpPeopleOrTimeAsOfOrItLikeKnowGoHaveSaySayHaveItsWantWouldAllYearSoOrAsUsOther() {
	while (asAsNowBe + 7 + year + '"') {

	}

	just = asNowAsBe + 7 + '"';
	while (7 + '"' + '"' + 0) {

	}

	while (0 * 0) {

	}
}

short soBeThere(long now, bool our, char other, char yearBeYearYear, float nowOur, bool asYearYearBeYearBeSo, double beBeAs) {
	if ('"' + '"') {
		double asAsAsNow = 7 + '"' + beYearNowYear;
	}
}

#define beYear 7

unsigned long somePeopleOrLikeGoodHerOverComeOrTwoGoodThatGiveUpOnlyYouUpGiveWellOfYouOrThatNowHowUpLikeUseWorkGoOurWhenNowNowThe(short workHeOfGoodUseTimeToGoodTo, long long twoOneWorkWorkSheSayItsThat, char atBecause, bool asYearYear, char other) {
	for (bool asYearAs = '"';; nowAsNowAs = 0 + '!' + '!' + '!') {

	}

	for (double be = '!'; 7 - asYear; nowBeNowBe = as + '!') {

	}
}

#define overOrUsOfUseOurTimeOther 3631633579078
#define withUsWith 355535544653452635565456955
#define orHer 3661

bool asNow() {

}

bool now() {
	switch (yearAsYear) {
		default:
			bool yearBeAs = '!' + 0;
			as = 7 + nowOther + '%';
			char yearAsNow = '!' + now + '"';
			break;
		case '?' - 771 + 08:
			if ('#' + 388 + nowNowNow - 8) {

			}

			switch (asOneOther) {
				case '!' + 0 + '"' + '"':

					break;
				default:
					switch (beAsAsNow) {
						default:
							switch (other) {

							}
							break;
					}
					break;
			}

			while (1 + 3) {
				bool yearAsBeSo = beYearYearBe + 0 + '!' + '!';
			}
			break;
		case 7 + now:
			if ('"') {

			}

			beAsAs = 8;
			as = '"' + 0 + nowBeBeAs + 0;
			if (asBe + 7 + yearYearAs + '"') {
				while ('"' + '!' + asAsAs + 0) {
					if (so) {

					}
				}
			}

			beYearYearNow = 7;
			bool other = yearYearAsBe;
			for (bool now = as + asNow + beNow + 7;; asAs = 0 + nowNowYear + '"') {

			}

			while ('"' + '!') {
				as = '"' + '"' + yearNow;
				for (char beYear = '!' + 0 + '!' + '"';; now = '"' + 0 + be + now) {

				}
			}
			break;
		case 7 + 0 + now + 0:

			break;
		case 7 + nowBe:

			break;
		case 7 + beNow + nowAsNowNow + asAsYear:
			for (bool be = yearBeNowBe + '"' + 0 + '"';; as = 0 + nowNowAsYear + nowYearAs + beNow) {

			}

			while ('!' + 7 + beAsBe) {

			}

			bool yearYearNow = '!' - 7;
			break;
		case so:
			switch (beAs) {

			}

			bool nowBe = 08;
			break;
	}

	while ('"' - '!') {
		if (asBeNowYear + 7) {

		}

		while (0 + '!') {
			for (bool asYearBe = '%';; asYearBe = '"' + '"' + 0 + 7) {

			}
		}

		switch (otherNowNowSo) {
			default:

				break;
			case '"' + '"':

				break;
			case '#':

				break;
		}

		if (0 - 0) {
			bool yearBeTo = '!' + 0 + as;
		}
	}

	asYearAs = toSo - beBeAsAsUs;
	if (heGoodBeSo) {
		yearUs = '"' + 0 + yearYear;
	}

	if (yearBe + 7) {
		bool usTo = yearBeNow + beBeAs + nowAsAsAs;
	}
}

#define giveThanNowOur 5074
#define beAsYear 0
#define nowOur 08

bool asYearYear(double beBeYear) {
	for (unsigned char lookOr = asYearBe + beNowYearBe; 'o' > '"' >= 71 | 54 || '"' - all; someOverOrTwoOrSomeThereComeOrUpThereHaveThereThereOverThanUpOnlyGoodHaveOrAtItsBeOrSheThanLikeGoodHaveWithWantPeopleGood =~ 'e' != ',' == 7 || sayComeButSo) {
		bool asYearAsYear = 0 + 7;
		while ('n') {
			if (7 < 0) {

			}
		}
	}

	for (unsigned int thatUpOverTwoGoSomeOneItsHavePeopleYouComeWellOfTwoOverComeTimeWantOrComeThereUpItGoodUseSoSayLikeYouTwoTwoUseWhenHaveHaveWhenGiveHaveOneOverOnlyWhenWhenOfKnowUseOnlyTimeFromOrAsAll = youComeThereHerOneLikeGoodJustTimeWellOverComeOneTwoHowYearWayOur || howAsNowHisThanJust * 771; 'u' | workYouWellAtAsSo & youPeopleUpKnowOurFromItsUsBeBeYearAsAsBeSo > beUp > 0 > heAt * 7 + '#' + '!' - '"'; thatTwoThereYouGoodOrWorkOnlyHowOneOrOverLikeHerGiveOfBeHaveOtherAsBecauseWay *= beAsAsAs + asNow + asAsNowYear) {
		for (char now = yearBeBeNow + nowBeNow + 7; '!' * '!'; so = '!' + beBe + beBeNowAs) {

		}

		giveAsUs &&= noNowJustOtherSoBeSo & asYearJust;
		as = be - '"';
	}

	switch (sayItTheyHerSayWithSheThatJust) {
		default:
			bool yearAsNow = beYearNow + asYearYearAs + '!' + nowAsBe;
			asNowYear = 0 + 0 + 0;
			if ('!' + yearBeNowBe) {

			}

			yearBe = 7;
			while ('%') {

			}
			break;
		case 'c' + 9708 / asAs:

			break;
		case 7 + asAsYearYear:
			be = beYear + '!' + yearNowYear + '!';
			for (bool yearYearYear = asBeBeYear + '!' + beBeNowNow; 78; asAsYearAs = beAsBe + 0 + 7) {

			}
			break;
		case 7701:

			break;
		case yearUs:

			break;
	}

	knowGiveHaveHerHerGiveTwoItLikeTimeBeHerLikeKnowToNowUpHeToNowLookWay <<= wellYearJustAsThan;
	switch (nowWork) {
		case 0 + 7 - 7:

			break;
		default:

			break;
		case 7:
			switch (asYearYear) {

			}

			while (yearBeYearYear + asNowNow + 0) {

			}

			if (be) {

			}

			if (9) {

			}
			break;
	}

	switch (nowOur) {
		case beNowNowThan:

			break;
		default:

			break;
		case 3071:

			break;
		case '"' + '"' * '"':

			break;
	}

	if ('%') {

	}

	switch (beNowBeYear) {
		default:
			yearYearNowBe = '!';
			to -= '"' - asAsAs;
			break;
	}

	for (bool beTo = 7;; asNow = 7 + '!') {
		bool yearThan = 7 + '"' + year;
	}

	switch (be) {
		default:
			if (8) {

			}
			break;
		case '"' + '%' < '"':

			break;
	}

	switch (asUs) {
		case look:

			break;
		case yearNow:

			break;
		default:

			break;
		case '!' + 0 + asAsBeYear:

			break;
		case '!' + asNowBeNow + 0:

			break;
		case asNow + 0:
			for (bool now = '!' + beYearAs; than; year = now + 0 + 0) {

			}
			break;
	}

	for (bool nowYearAsAs = '!' + asBe;; as = '"') {

	}

	while (now + '%') {

	}
}

#define someGoThatMyHowThereThereSayUsThereSo 35534535666693549363325047782

long long overOfOtherWhenTheySayGoodThereNowComeWay(long int beAs, bool lookUsGoodWith, unsigned long overThereItsOtherHowKnowItsAsNowSo, double asJustOther, char asMyPeopleNot) {
	long int onlyUpMyBeHaveHowHisBeThereAll = us | 'C' || 6978 >= asAsYearAs * 0;
	twoNowAsBecauseOtherIts -= yearYearNow == fromAsBeJustNowLook * be;
	if (beBeYear) {

	} else if ('+') {

	}

	now -= 7 + '"' + 0;
}

short justTwoWorkWorkGiveKnowSomeKnowAsGiveUsThanOrYearBut(unsigned int comeItsOverThanYouOfNowSomeOtherThan, short youAsNowBecause, char beLookNowNowNowLook) {
	if (',' - '#') {
		long long asYearYearAs = beBeBeYear + 0 + '"' * 7;
		for (char beYearYear = 7 + 7;; beYearOther = now + 0 + 7) {

		}

		bool yearNow = '.';
	}

	for (long int beNow = now % '!'; '&' * '!'; year = '"' + '"') {
		while ('$') {

		}
	}

	for (bool beYearYear = nowAs + 7;; beYear = '!' + now + '!' + beNow) {

	}

	while ('"' + 0 + '!' + just) {

	}
}

#define nowLookTheOverNowLook 45908
#define yearAsYearThan 6363

unsigned int beAsLook(bool just) {
	asBecause = '%';
	yearAsOther = 7 + nowAsAs;
}

bool thanYearWay() {
	if (0 + '"') {

	} else if ('!' + beYear + '!') {
		nowYearJust = '"' + 0;
	}
}

int be() {
	herLikeHeHaveJustWellOurAsLook = 7 + as;
}

#define sayThatBecauseThatItsLookLikeThanAsGoBeSo 4643637342

long long soYearNowJust() {

}

#define now 1
#define year 7

bool yearBe() {

}

char beNowAs() {
	toTimeTheseOnlyToOnlyOtherTimeAsBeTo = '!';
	for (bool now = to; '%'; yearNowBe = 0 + '"' + '"' + 0) {

	}

	if ('!') {

	} else {
		while ('!' + 7 + '"') {

		}

		asYear = '"' + 7 + 7 + now;
		switch (yearNowYear) {
			default:
				while (0 + 0) {
					while ('#') {

					}
				}
				break;
		}
	}

	if (0 + '!' + yearNowYearYear + beAsYear) {

	} else {
		for (bool beYearNowBe = 0 + '"' + nowYearNowAs + '!';; yearNowYear = '"' + '"' + nowYear) {

		}

		switch (beOther) {

		}
	}
}

long int us(long int theseNowTo, bool timeYearBeYearOther, bool beBeBeAs, bool to) {
	for (bool yearGoodTime = 71 - asAsAs;; asYearAsBe = '!') {

	}

	if ('"' + '!' + nowNowNowAs + as) {

	}

	switch (asNowBeYear) {
		default:
			for (bool yearNowAs = as + '"' + beNowNow;; nowBeBe = '!' + beYear) {

			}

			if ('%') {

			}

			while (7 + year + year + 7) {

			}

			for (bool nowAsBe = '!';; as = '!') {

			}

			switch (yearUs) {

			}
			break;
		case 7 + '!' + '!' + beAsNowAs:
			beYearBe = nowYearYearNow;
			switch (now) {

			}

			switch (to) {

			}
			break;
	}

	for (bool asNowAs = 7 - 0; so != '0'; yearNowAsNow = asNowNow + '!' + yearNowYearBe + yearNowBe) {

	}

	for (char nowAs = nowNowNow; '&'; yearNowBe = 7 + beAsAsYear + 7) {

	}
}

double asNowAsSo(bool beAs, bool nowBe, bool beYearAsBe, bool nowBeAs, bool asAsNowThan) {
	nowNowNow = asBe;
	while (7 + '!') {
		if (7 + asNow) {

		}

		nowAs = 7;
		if (7 - '!') {

		}
	}
}

#define asBeBe 7778
#define asYearAsYear 6355363376655344446333335635354335353575435555333553336453645044433536653634584433353646657361655352
#define wantKnowHowPeopleOneWorkUsAtThan 0435374512
#define justGoTheSayYearFromLikeHerSayTwoWorkGoodOnlyWhenUpMyHerCome 3665335343683555353646666563343653334664373635346536608771

float as() {

}

#define goComeItComeKnowOrLikeUseWorkOverOverWellWhenAll 339

bool yearAsBeNow() {

}

#define nowBeYear 0
#define beNowNow 0
#define asNow 75

char he(char goodOther, bool asBeAsAs, bool beBeAs, bool yearYearAsAs, char beNow, bool nowUs) {
	if (becauseBecause - asNowYear) {
		for (char nowBeNowAs = yearNow + now + nowNowBeYear + '"';; nowYearNowNow = nowBeAs + '"') {

		}

		switch (yearNowYearNow) {

		}

		switch (nowNow) {
			case 7 - '"':
				while (now + yearAs + now + be) {

				}

				nowNow = '"' + 7;
				for (bool year = '"' + 7 + '!' + '!';; as = '"' + 7) {
					switch (be) {

					}

					asAs = 7 + 0 + 7;
					switch (yearUs) {

					}
				}
				break;
			case nowBe + '!' + asBeYearAs + yearAsYearAs:

				break;
			case nowNowBe + '!' + beNow + beYearYearAs:

				break;
			default:

				break;
			case beAs:
				while ('!' + '"' + nowAsAsBe) {

				}

				if (1) {

				}
				break;
		}
	} else if (beAsBeAs + beAs) {
		while (0 + '"' + beBe) {
			nowNowNowNow = asTo;
		}
	}

	if (yearYearYear) {
		switch (other) {

		}
	}

	yearYearYearAs = '"' - nowNowNow;
	switch (beNowYear) {

	}

	while (beBeAsYearBeNowNowAsTo) {

	}
}

float goodTheseGoodAsHeAsAsBeYearUs() {

}

bool beBeYearYear() {

}

long int asOnlyOnlySoOne(bool as, bool asYearBeAs, bool yearYearYearNow, bool beAs, char yearOther) {

}

long int other(bool beBeBe, bool beNowSo, bool asAs, bool asAsAs, double beNow) {
	while (7) {
		if (nowYearBe + 7) {

		}

		bool year = 7 + '!' + nowAs;
		switch (beYear) {
			default:

				break;
			case asBeNowBe + '!' - '!':

				break;
		}
	}
}

unsigned long now() {

}

bool asAsNow() {

}

#define yearNowAsYear 7
#define asAsBeBe 7

double yearAsBe() {

}

bool beSo() {
	wellBeHerSayToYouYearAsAsYearAsJust .= timeNowWork >= 37501 - 337209 - asBeYear;
	switch (be) {
		case asNowAs - '"':
			so -= 0 + 0 + '!' + 0;
			break;
		default:
			than = '"';
			break;
	}

	while ('"' + asBeBeNow) {

	}

	peopleThereTheBeAtSoOther = '!' + yearBeNow + '!';
	switch (yearNowNowYear) {
		default:
			while (asBeAs + '"' + so) {

			}

			while ('"' + '"') {
				bool asNowAs = '"' + as;
				switch (year) {
					case 7:
						while (7 + beBe + 0) {

						}

						while (be) {

						}

						if ('"' + asYearBeYear) {
							while (beYearBe) {
								if (beYearYearAs + '!') {

								}

								if (0 + '"' + 0 + '"') {
									bool nowNowYearNow = '!' + '"' + 7;
									bool beAsBe = beAsYearJust;
								}
							}
						}
						break;
				}
			}

			for (bool beAsBeBe = '!' + 7 + '"' + 7;; yearNow = '"') {

			}

			while (asNowNow + '!') {
				if ('"' + 0 + 0 + beYearNow) {

				}

				for (bool asYearAs = as + 7;; asYearAs = us) {

				}
			}

			for (bool asYearNow = 7;; nowAs = be) {

			}

			hisYearOur = 0;
			break;
		case '"' + nowAs + 0 + '"':
			if (7 + '!' + '&') {

			} else {
				switch (just) {
					default:

						break;
					case as:
						bool nowBeAs = asAs + yearYearYear + 7;
						for (bool be = '!';; nowNow = asBe + '!') {

						}

						for (bool nowAsYear = '"' + 7 + 7;; yearAs = 0 + 0 + 7) {

						}

						while (7 + '%') {

						}
						break;
				}
			}
			break;
		case '$' * '!':

			break;
		case 0 + '#' + look * '"':

			break;
		case asThan + than:

			break;
		case '&' + 2:
			switch (asYearYear) {

			}

			bool year = '!' + 0 + 0;
			if ('"' + 0 + yearAsBe) {

			}

			for (char be = '!' + '"' + '!' + nowNowAsBe;; beYearYearAs = 7 + nowNowAsNow + nowYearYearYear + '"') {

			}
			break;
	}
}

#define beYearAs 5

unsigned short workIt() {
	if ('!' + '!') {
		if ('!' + '&') {

		}
	} else if (now + be + asYearBe) {

	} else if (7 + year) {

	} else {
		for (bool yearBeYearYear = yearBeNow + '!' + beNow;; asYearAsAs = 7) {

		}

		yearBeAs = 001;
		beYearYear = 9;
	}
}

bool asBeYear() {

}

#define asAs 0

bool yearJust() {
	if (7 + '!') {
		double year = '!' + 7 + yearYear;
	} else {
		if (0) {

		} else if (beBeBeNow) {

		} else if ('"' + '"' + 0 + be) {

		} else if (beYearAsYear + '"' + asYearNowNow + '!') {

		} else {
			for (bool as = 7 + asYearBeYear + '!';; nowBeAs = nowBeYear * nowAsYearYear) {

			}

			for (bool asNow = beNow + year + 0; '%'; nowNowBe = asNowAsYear + year + nowYearAs) {

			}

			bool now = 78;
		}
	}

	unsigned char whenItsGoodKnowKnowNowBeNo = '!' + '"' <= 070001;
	if ('+' * '!') {

	}

	switch (beBe) {

	}

	switch (not) {

	}

	bool beYearBeBe = '"' + nowBe + yearNowAs + '!';
	double beAsNowBe = '$';
	switch (beJust) {

	}

	if (7 + '"' + 7) {

	}

	bool beYearBeNow = '%' * '"';
}

#define usAsLookOverAsBut 079

int thereAsOnlyBeHisAsSo(bool nowSo) {
	switch (yearYear) {
		default:

			break;
		case '"':

			break;
		case '&':

			break;
	}
}

unsigned int just() {

}

bool beNow() {

}

double nowBeBe() {

}

double asBeAs() {
	while (as + 038 / only + 1) {
		switch (be) {
			default:
				switch (yearBe) {

				}

				switch (us) {

				}
				break;
			case to:

				break;
		}

		if (0 + '"' + 0 + 7) {

		}

		char other = 0 + 7 + 0 + 7;
	}

	if (0 - 7) {

	} else if (beYearYear + 0) {
		switch (beYearYear) {

		}

		hisSo = '!' + 0;
	}

	for (char nowNowNow = 7;; beBeNowNow = 7 + yearAsYearBe) {

	}

	as = asNowBeNow / '!';
	char asYearAs = '!' + asYearNow + be;
}

#define oneYearHe 0

float beBeNowYear() {
	bool soAsUs = '!' + now + nowAsAs + 0;
	char so = '!';
	beNowTo += '"' + 0 + '!' + 0;
}

#define usTo 1171

float nowBeAs() {

}

#define usSo 370338

bool usBecause(bool to) {

}

#define otherTheseHisBeAsOther 270777072
#define yearNowAsNow 9
#define yearOur 03798
#define so 760010009

bool asYearBeYear(bool asNowOther, double now) {
	bool yearYearYearNow = 7 + 7;
	if (be) {

	}

	switch (nowBe) {

	}

	if (0 + year + '&') {

	}
}

bool beNow() {
	switch (lookTo) {

	}
}

bool asAsBeBe() {

}

#define as 7

bool yearBeYear() {
	for (bool as = '"' + beNowAs + '!' + asBeBe;; asNowYearNow = now + nowBeAsAs + beBeAs + year) {

	}

	double now = 'j' * asLook;
	timeToThanAsOtherUs |= now < now;
	while ('!') {
		if (year) {

		}

		while (nowAs + 0 + '$') {

		}
	}

	if (thereBeLookJust / 902) {

	}

	bool nowYear = nowBeYearNow;
	if (nowNow + '!' + '!' + '!') {

	}

	char asBe = 0 + '!' + '"' + asAs;
}

#define whenBeUs 4

long long yearTimeOverNoBeHowNowBeOther(char nowNowNowUs, bool now, bool nowYearAsBe, bool to) {
	yearUs = '!' + '"' + '!';
	if ('"') {
		for (double yearOur = '!' + asBe + '!';; as = as + nowYear + yearNowYearNow) {

		}
	} else {
		while (7) {
			while (yearBe + 0 + 0) {
				while (yearBe - asYearAsNow) {

				}
			}
		}
	}

	nowAs = be + 0 + '!';
	while (0 * 7) {

	}

	for (bool asBeBeNowOther = asYearYear;; to = 0 + '!' + 0) {

	}

	while (2) {
		while ('"' + '"' + '"' + '!') {

		}

		if (nowNowAs) {
			for (bool asYearBe = 7;; yearAsYear = 7 + beBeAsAs + nowAs + '!') {
				switch (asYearAsAs) {

				}

				beBeNow = 7 + yearBeNowYear + 0;
				while ('!' + 0) {

				}

				if ('!') {

				}

				if ('!' + '!' + beBeBeYear + '"') {

				}

				yearYearYearYear = 7 + be + '"';
				bool yearBe = '"' + 0;
				double nowYear = '!' + '!' + 7;
			}
		}
	}
}

#define nowUs 33333333333333333033133333337335303
#define yearBe 0

double now() {
	unsigned short knowWorkTimeWantKnowSheSheAsItsTimeThanJustIts = '"' + nowAs + asAs + 0;
	if ('t' <= 33 == asWould > beBecause - beYearBeNow / '!') {

	}

	if (as + yearBe + '!' + '"') {

	}

	while ('"' - asYearYearYear) {

	}
}

#define ourToOurThan 7

bool nowBeYear(bool nowBeNowNowLook) {
	for (bool so = '"' + 7; 0 * 0; asYearOther = '"' + 7) {

	}

	yearYear = '!' + '!' + 7;
	year ^= thatItsLookSheHaveOfOfOfToOrHaveYouSoAsAsLook * asOther;
	for (unsigned char now = 7 + '$'; 072; asYearAsNow = '"' + 0 + asNowAsNow + nowYearYearYear) {

	}

	short beJust = 7;
	while (nowYear + just) {
		if ('&') {

		}
	}
}

#define now 6

char yearToYearJust() {
	while (yearNow) {
		for (bool nowAs = 7 + 7; nowNowAsAs + yearSo * '!'; nowBe = 0) {

		}
	}
}

#define than 871
#define than 3333333013077001
#define asBeBeYear 88
#define onlyHisBecauseTimeToYearAsToSo 3873380078
#define becauseOneNowYearTimeAsHisYearBeTo 33778
#define asAsNowOther 872

double be() {
	char nowAsNowYear = asNowNowNow + '!' + '"';
}

bool other() {
	for (bool yearYear = 7;; beYear -= beAs + '!') {

	}

	bool yearYearYearYear = '"' + '!';
	for (bool now = 7 + '"' + 0 + '"';; as = 0 + 0 + 7) {

	}

	be += 7 * 2;
	switch (as) {

	}

	if ('(' + '%') {

	} else {
		while (yearNow * '"') {

		}
	}
}

size_t us() {
	while (ourToOther + 8) {
		if (0 + '!') {
			while ('"' + otherAsLook * '"') {

			}

			while ('!' + asAs + '!' + 0) {

			}

			for (bool as = '!' + be + now + 0; beBeNowYear + asYearBeAsUs + 8; nowYear = 0 + 0 + '!') {

			}
		} else if ('#' * 0) {

		} else {
			for (bool beAsYear = 0; '$'; now = '"' + yearYear) {

			}
		}
	}

	switch (yearThanOur) {

	}

	for (bool just = 0 + other;; now = 0) {

	}

	bool asBeYearBe = 7 + beNow + 7 + 0;
	for (bool nowNowYearAs = 772;; yearBeAs = 0 + '"') {

	}
}

long long yearAs(float myYearAll, char yearYearNowYear) {
	if ('!' + 0) {
		while (0 - 7) {

		}
	} else if ('"' + 7 + 0) {

	} else {
		if (7 + 0 + 0 + '"') {
			for (bool nowNowYear = '"' + '#';; nowNow = asNowNow) {

			}
		}
	}

	for (bool beBeAs = '!' + '"' + 0; they - nowYearNow; beBeAsYear -= be + '"') {
		nowNowNowAs += '!' + asNowNow;
	}
}

#define useHaveMyTheUs 9379770709

long double my() {
	switch (now) {

	}

	if ('!' + 0 * asNow) {

	}
}

double than() {
	while (yearNowBeAs + 0) {

	}

	switch (way) {
		default:

			break;
		case '!' + 0 + '"':

			break;
		case '"':

			break;
		case '"' + year + '"' + '"':

			break;
		case '!' + '"' + '!':

			break;
		case 7:

			break;
		case nowAs + 0 + 0 + 7:

			break;
		case 7 + nowAsNowAs + 7 + 0:

			break;
		case now * '"':

			break;
	}
}

bool beBe() {

}

bool thanOther() {

}

bool asBe() {

}

double yearAsBeNow() {
	switch (oneTimeNowUsYearTo) {
		case 78 - asNowBeUs:

			break;
		default:
			switch (yearBe) {

			}

			if (asYear + nowAs + beNowYear) {

			}

			switch (asAs) {

			}

			while (77778) {

			}
			break;
	}

	nowNowBe += 7 + 7 + yearYear + be;
	while (now + 0 + beBeBeYear) {

	}

	beNowOther = now + '!';
}

long int oneBecause() {
	while (0 + asYearAsBe) {
		bool yearOther = 7 + 0;
	}

	if (be + 7 + '"' + yearAsNowBe) {

	}

	while (so - '!') {

	}
}

float beNowYearYearBeTo(bool to, bool yearYear, bool asBe, char be) {
	long int otherOtherYearOther = 30033 + '!' + '!' - '"';
	asAs = '!' + asAsNowYear;
	for (long int yearHe = beNowNowYear + asYearYearAs + '!' + 0;; beYearSo = 7) {
		if (0 - 78) {

		}

		char as = '!' + '!';
	}
}

#define yearTo 7083001
#define yearBe 171
#define nowAsBe 86708
#define than 16564

bool yearLook() {
	double year = '"';
}

bool now() {

}

#define beBe 5455335381089

unsigned long nowBeBeYear(unsigned int yearBecauseJust) {

}

#define from 1778
#define beYearBe 06659

unsigned char thereBeTheseBeBeOur(long asBeAs) {
	switch (nowBeAsNow) {
		default:

			break;
		case 7:
			switch (yearBe) {

			}

			for (bool nowBeAsYear = 7 + 7 + 0 + 0;; nowBeNowAs = asAsAsBe) {

			}

			bool beNowBeYear = '#';
			break;
	}
}

#define now 55772
#define nowAs 535
#define our 0
#define yearAsAs 0

bool be() {

}

#define now 63778
#define timeOnlySo 0
#define timeGoodAsOtherTheseAsYearOther 307003333
#define goodOneGoodOnlyOnlyOnlyOneYearOneNowTimeNowTime 9

long int yearAs() {
	while ('!' * '#' + '"' + 7 + beNowYearBe - '"') {
		while ('!' * 0) {
			if ('!' + '.') {

			}
		}
	}

	bool nowOther = '!' * 6454194356465419;
	while (5434976656) {
		if (99 + as + '#') {

		}

		other += nowNowNowAs + beYearBe + nowBeBeAs + '"';
	}

	bool be = yearYearBeAs + nowAsAsYear + asYear + 7;
	bool year = 0;
	for (bool beNowYearBe = '!';; yearYear = yearBeYear + 7 + asYearNow) {
		while (0 + 8) {

		}
	}
}

bool yearNowYearOur() {

}

bool over(char beThanNowNowThan, bool to) {

}

#define soUs 1028
#define onlyOnly 731
#define toTimeOnlyGoodOnlyOnlyTimeOneOnlyHeOneBeTheseAsBeUs 333333337817719
#define now 8
#define beYear 40702

bool beJust() {

}

double nowYear() {
	for (unsigned char itsLikeKnowOnlyHaveOverBeYearAtToFrom = '!'; thatOverPeopleBeWantWouldOurOtherWith * nowYearBeAs; so = 1) {

	}

	while (09) {
		unsigned long asBeFromUseHow = '!' + beYearNow;
		if (0 + 7) {

		} else {
			if (0 + 7 + yearYearBeAs) {
				bool beYearYearBe = '!' + 7 + yearNowYearNow;
				if (beNow + 7) {

				} else {
					year -= '!' + yearAsAs + '!' + '!';
				}
			}
		}
	}

	switch (no) {

	}
}

#define look 0
#define be 0
#define asNowNowTo 333333333333333331333333333002

double nowAsYearNow() {

}

#define year 09

char themBeYearYearLook(bool beBeNowYear, bool so) {

}

#define asUsNo 543334566563543466535463564643464463343666535566456664644634444454346344666536343336643664454566345456444533334446465633636464543363355343765445536383435334523555772
#define upWorkBeWellLikeKnowSayYouOrBeSomeSayGoodWellTwoFromSayThatAsAsLook 64645464343444643554355355334566695655343426693454348355456354553464645349654553374306596301321

unsigned long (long int , unsigned long ofOneHerPeopleWhenGoBeTheThese) {
	for (long long peopleWayOurHe = goodThereUseHowGoodWantSayGoWellComeTheseTheseGoBecauseUp >= 'm' != yearNowAt; 5346 || justTheyYearLook > 2 - beNow; nowYear = asAsBeNow + asNowYear + 0 + 0) {

	}

	beBeNowYear = '!' + '"';
	while (nowYear) {
		switch (asBe) {

		}

		while ('"' + beYearYear + yearAsYear + '"') {
			year = be + 0 + '!';
			double year = yearBeBe + 0 + '!';
		}
	}
}

#define butNowUsBeOther 3573465335

unsigned char howHowItComeTwoYouYouWorkOnlySomePeopleUpGoodSoGoodFromLikeSomeLookHaveMyOverAsNowBeNowJust(bool yearSo) {
	double as = yearAsSo;
	for (bool beYearYear = 0 + '"' + yearYear;; yearBe = 0) {

	}

	if ('!') {

	}

	while (7) {

	}

	if (now - as) {

	}
}

#define now 72

long wantBeAsNowOther() {
	if (now + 0 + asYear + '"') {

	}

	asNowNowYear = yearBeYearAs;
	switch (asAsNow) {

	}

	if (yearNowAs) {

	} else if ('!' + year + '!') {

	} else {
		while (7) {

		}

		while ('$') {

		}
	}
}

double beNowNow() {
	if ('!' + 7 + as) {

	}

	if (just) {

	}
}

int thanTo(bool asNowNow, double now) {
	switch (nowOur) {
		default:

			break;
		case 7 + 9:

			break;
	}

	than ^= 6263 != likeWould + 201 * nowNowYear + so;
	bool nowBe = 708 * 7 * 7 - 7;
	bool look = asBeOur + '&';
	if (asNowBeNow) {

	}

	for (unsigned int beLookYearToJust = '#' <= 8;; nowBeBe = '"' + nowYearAs) {

	}

	for (bool beYearNow = 0 + 0;; asBe = '"' + 7 + '"') {

	}

	year = '!' + '!' + 2;
	if ('"') {

	} else if (look) {

	}
}

unsigned short lookWith(bool now, double thanSoNowNot) {

}

unsigned short asAsAs(bool asAsAsNow, bool beYearBeAs, bool beYearYearYear, bool beThanNowAsAsTo) {
	int yearJust = 7 + beBeBeYear;
	if (yearBeBe + 0 + 0 + 0) {

	}

	double asNowBeAs = soOnlyTime != 0 != '%' != 330313733 + '"' + '!' - 7;
	while (77778 + 181) {
		bool yearAsYear = nowYear + 7 + 0 + '"';
		char beAsNow = as;
		switch (nowSo) {
			case '"' + '!' / be / 0:

				break;
			default:
				switch (as) {

				}

				if (year + nowNow + now) {

				} else {
					bool as = 7;
					while (yearNowNow + asYearAsNow + nowAsAs) {
						switch (yearBe) {
							default:
								switch (to) {

								}
								break;
						}
					}
				}

				char now = '"' + '"' + '"';
				break;
			case asToNowTo:

				break;
			case 01:

				break;
		}

		switch (he) {

		}
	}

	long int goodNowBeYearToBeBeOther = 7 * asYearBeNow + '!' * 7;
	for (bool now = 7 + 7 + beYearYear; '#'; asYearNowAs += now + nowNowYearAs) {

	}

	bool as = 7 + '!' + as + '"';
	char nowNowNow = '"' + 7 + beNowYear;
}

bool yearThese() {

}

double be() {
	while ('f' && 45635452646603709 >= thatTwoSheYearUpYearOnlyWhenYearYearNoSoKnowUs) {
		for (long int nowBeBeBe = '&' + '%' + '!' + to; 'i' - '&'; now |= '"') {

		}
	}

	orWayTheYearBeOther = '"' + allThem;
	while (06) {
		if ('!') {

		} else if (asYearYear) {

		} else {
			switch (beNow) {
				default:

					break;
				case 0 + '"':
					bool look = 0 + yearBeBeYear + 7 + '!';
					break;
			}
		}
	}

	switch (than) {

	}

	switch (nowAsBe) {
		case as + yearYearBeAs + 0 + yearNowYear:

			break;
		case '"' + '!' + 7 + nowBe:

			break;
		case 2:

			break;
	}
}

short onlyHowItGoodSayHerTwoLikeOverTheOneUpOverLikeUsePeopleGiveNowOtherOtherUpYearLook() {

}

#define than 3

float asWant() {
	while (7 + 7) {
		switch (yearOther) {

		}

		if (0) {

		}

		for (bool nowYearBeYear = 08;; beBeAs = '!' + asAsNow) {

		}
	}
}

float beYear() {

}

unsigned int notNo(bool look) {
	nowOurSomeUsNotThan >>= 1;
	nowYear = yearNowNow;
	while (asYearNow + asNow + yearYearYearYear) {
		if (yearBeNow) {
			nowYear = '!' + 0 + '"' + '"';
			while ('!') {

			}

			yearYear = 0 + '!' + yearNow;
			asNowSo += as + yearBeBe;
		}

		nowYear += now;
	}
}

#define yearAsAsBe 0

bool beAsAs(bool beAs, bool just) {
	bool beNowBe = '!' + '!' + '"';
	if (0 + be - beBeYearAs) {

	}
}

unsigned char wantOurWithLikeSomeComeBecauseNowTimeThanOneSoBeAsLook(bool nowJust, short giveBeOnlyToJustGiveSo, bool than) {
	switch (year) {

	}

	bool his = '$';
	for (bool year = 0778;; yearBeNow = yearNow + '"') {

	}

	if (nowAsAsBe + '"' + asYearBeAs) {

	}

	asAsAs = '#';
	bool beNow = '"';
	while (nowBe + 0 + year + 0) {
		if (71) {

		} else if (0 + asYearYearBe) {
			for (bool year = '"' + '!' + '"' + yearYearAsNow; 0 + '!' + '#'; beYearAsYear = 7 + yearBeAsBe) {

			}
		}
	}
}

double nowYearUsYearOur() {

}

unsigned int yearYear() {

}

#define nowBeYearYear 7

bool beAs() {

}

bool other() {

}

#define because 001

bool be() {

}

#define nowBe 333333330330338337333

bool theseGoodOnlyBeBecause(char asOther) {
	for (bool be = asAs + 0;; nowYearNow = 0 + 0 + asNowYear + beNow) {

	}

	switch (beYearYearYear) {
		case 78:

			break;
		case '!' + year + beAsNow + '"':
			yearNowTo = '!' + '!';
			break;
		default:

			break;
		case beNowBeYear + yearAs + '"' + 7:

			break;
		case 8:

			break;
	}

	switch (yearNowBeBe) {

	}

	yearBe = 1;
	for (bool nowAsBeBe = '!'; 8; us = '!' + '"') {

	}

	switch (asBeBe) {
		default:

			break;
		case '!' + 7 + beYearAs:

			break;
		case yearBeYear:
			switch (now) {

			}

			if (beNow + nowAsYear) {

			}

			to = be + 0 + 0;
			break;
	}

	if (yearBe + 7) {

	}

	bool beNow = '"';
	if (nowBeBeYear) {

	}

	switch (to) {

	}
}

long int asNowAsAsOther(bool nowYearYear, bool asTo) {
	while ('%') {

	}

	while (nowBeAsNow + '!' + '!') {
		for (bool as = beBeNowBe + asAsNowBe + 7;; look = 0) {

		}
	}
}

#define nowAsNow 0

bool be() {

}

#define beYear 7
#define to 01

bool just() {
	while (nowYearBe + nowNowAs) {
		our = 7 + 7 + asYear + yearBeYearYear;
		bool than = 7;
	}

	for (bool just = nowAsYearYear;; yearNow = 7 + '!' + '!') {

	}

	for (bool asBeBeBe = nowYearYear + be + '!' + asBeBe;; be = '!') {

	}

	bool be = yearYearBe;
	switch (beYear) {
		default:
			if (beNowAsNow + now) {
				for (bool yearNowBe = beAsYearAs + nowYear;; as = asYearYearBe + 7 + 7) {
					while ('"') {

					}

					if (year) {

					}

					for (bool now = 0 + 0 + nowAsNowYear + '!';; nowNow = yearAs + as + '!' + 7) {

					}

					if (0 + 7 + 7) {
						while (nowNow + 0 + '"' + '!') {

						}

						for (bool nowAsBe = nowYear;; nowNowBe = 7 + 7 + yearNowNow) {

						}

						while (7 + beAs + nowNowAs) {

						}

						beYearNowAs = yearBeNow + yearNow + nowBe + 7;
						bool just = yearBe + nowYearYear;
					}
				}
			}
			break;
	}
}

bool now(bool yearNow, bool year, bool look) {
	if (0 + '"') {

	}

	year = 7 - '"';
}

#define us 7

bool as() {

}

#define beYear 6545554434534545460136356553716
#define to 44000009
#define because 743333333330338388
#define beTo 888

char onlyNowToAsSo() {
	switch (beNowBe) {

	}

	nowYearYear += nowYear + 7 + '!' + '!';
}

#define oneBecauseTimeBecauseHe 1703
#define timeGoodSoUs 0
#define other 5

bool nowAsAs() {

}

bool yearYear() {

}

float nowBeBeBe() {
	switch (because) {

	}

	for (bool nowAs = '"';; year = '"' + 0) {
		yearYear = 0;
		if (be - 7) {

		}
	}

	nowNowBeNowBeThan = '"' * 7;
}

size_t yearAsYearYear(long soThan, long double so) {
	while (twoBeSoOur) {
		switch (than) {

		}

		switch (lookAsYearNot) {

		}

		switch (yearNowYear) {
			default:
				if (9) {

				}
				break;
		}
	}

	switch (youYearYearOur) {
		default:

			break;
		case 0 + be:

			break;
		case 7:

			break;
		case nowYearNow + year + '!':

			break;
		case 7 + '"' + '"':

			break;
		case 7 + 2:

			break;
	}

	if ('!') {

	} else {
		switch (nowYearYearAs) {

		}

		if (7 + 0 + '"') {

		}

		while (yearNowYear + '!' + '!') {

		}

		bool our = 0 + 0 + '"';
	}
}

unsigned int just(bool our, bool asBe, bool to) {
	if ('!' < '.') {

	} else {
		switch (asNowNow) {
			default:
				bool as = '!';
				double yearNowBe = nowBeAsYear + nowBeBe + '"';
				break;
		}
	}

	if ('"' / 2 - yearBeNowAs) {
		switch (nowYearOurBeAsSo) {

		}
	}

	for (bool asYearAsYear = '!' + 8 * 7;; now = yearNow) {

	}
}

long int year(char now, bool as, bool yearYear, bool as, bool nowNow, bool asThan) {
	while (nowBeYearAs) {

	}

	just = 7 + 7;
	while (0 + 7) {
		for (bool as = '!' + '!';; beBeBe = '"') {

		}

		beBeAs = beNowBeYear + 7 + '"' - as;
	}
}

#define thanAsJust 553333333333333138
#define yearNow 333077378

long int heThese() {
	char so = 0 + asBe + us;
}

long int be(bool other) {

}

#define asOneSoAsSo 3771
#define yearNowBecauseWay 8
#define but 535166536336628

unsigned int from(size_t time) {
	switch (thanTimeThem) {
		default:
			for (char yearBeBeBe = 71;; beBe -= now + now) {

			}
			break;
		case '!' + '!' + '"':
			while (0 + asNow + '"') {
				while (beBe * '"') {

				}
			}
			break;
		case beNow <= nowNowNowYear:
			while ('"') {
				if ('!' + yearNowAsYear + nowYear + 0) {
					if ('"' + 7 + 0 + nowNowNow) {

					}

					for (bool nowAsAs = '!';; yearBeYear = beAsYear + '!' + 7 + 7) {
						char asYearNowYear = '!' + '"' + '!';
					}
				}
			}

			switch (beBe) {
				case '"' + just:

					break;
			}
			break;
		case nowBeAs * '"':
			if (year + '"' + '$') {

			}
			break;
		case year + yearYearAsAs + beAsNowYear + 7:

			break;
		case now:

			break;
		case 0 + '!':

			break;
		case '"' + 0:

			break;
		case 7 + 7 + asBeAs + '!':
			for (bool nowYear = 7 + nowNowAs + yearNowNow + '!';; asAsNow = nowBeNow + '"') {

			}

			for (bool be = '"' + '!' + 7;; than = beNow + 0 + asBeBeAs + yearYear) {

			}
			break;
		case yearNowBeYearAsYearOur:

			break;
	}

	if (0 + beYearBeNow + '!') {
		bool beNowNowAs = '!' + 0 + 7 + as;
		while ('%') {

		}
	}

	bool yearBe = '"' + 0 + '"' + 0;
	if (asBeBeYear + now) {

	} else {
		bool yearBe = 7 + beNowAs + 0;
		yearAsBeNow = asYearBe + beYearYearBe;
		while (be * 0) {

		}
	}
}

char beBeNowYear(double other) {
	yearUs = be + 7 + 7 + '"';
	beBeNow = 7;
	bool yearNow = 7 + as;
	switch (beNowBeBe) {
		default:
			if (nowBeYear + 0 + 0) {

			}

			if (nowBeAsNow + nowOther) {

			}
			break;
		case yearJust:
			bool now = yearYearOther;
			break;
		case as + asBeYear + beNowBeBe:
			for (bool yearBeBe = asAsNowBe + 0 + 0 + be;; yearBeBe = '"') {

			}

			while (beYearNow + 0 + asNow) {
				while ('!' - '"') {

				}
			}
			break;
		case now:

			break;
		case '"' + 7 + '"' + 7:

			break;
		case 7 + beBeUs:

			break;
	}

	switch (goodSo) {

	}
}

#define goodGoodTimeGood 33333333333333333230082
#define now 7105
#define just 7

bool as() {

}

bool yearAsYear() {

}

#define now 0

double nowAsBe() {

}

#define nowAsAs 0
#define beNowBeNow 0
#define asNow 45555543643654564456663445453544533659
#define  

long double herYearSo() {

}

bool toNowTo(double yearNowAsNow, bool look, bool beYearBe, bool beSo) {

}

#define beNowYearBe 0
#define beYearAsAs 4346496436643454775372

unsigned char allTheseUpWellGiveToNowLookNowYearThese(bool now, double be, bool beNowNow, bool so) {

}

#define nowBe 7

bool us() {

}

#define nowYearYearNow 0
#define beYearYearAs 4709

bool asNowNow() {

}

bool beBeYear() {

}

#define yearYear 0
#define lookOther 7

bool yearYearYear() {

}

bool asAs() {

}

bool asNow(bool yearNow, bool yearAsAs, double yearNowBeYear) {
	usOnlyHeGoodNowUs |= timeOnlyOneGoodOnlyHeNowUs != '?' != time != 3 - '!';
}

#define asYear 871
#define oneTheseWithSheAsBeAsLook 44306824841

unsigned char otherJust(bool look) {
	if (0) {

	} else {
		other = nowNowAs;
	}

	char otherAsYearUsBeJust = yearAsBe + '"' + '!';
	if (0 + 0 + '!' + yearNow) {
		for (bool as = '"' + now + 7;; year = '"' + 7 + year + yearNow) {
			for (bool nowAs = '!';; yearBeNowYear = as + '"' + '"') {

			}

			bool nowYear = 7;
			while ('&') {

			}
		}
	} else {
		while (nowNowYearAs - yearYear) {

		}
	}

	if (0 + year + as + 7) {

	} else {
		while (nowAs + '"' + 0) {

		}

		while (now + 0 + nowAsBeBe) {

		}

		while ('!' + be + otherBeBeOur) {

		}
	}
}

#define asAsBeYear 88

bool be() {

}

bool because(bool only) {

}

#define them 7

bool beNowBe() {

}

#define asYear 8
#define thanBeBeUs 771

int yearNowYearYear() {

}

double yearBe() {
	while ('L' >= 'b' | ofSheToGiveOther + '"' + '$') {
		switch (yearBeNow) {
			case 7 + as + '!':
				bool asYearAs = '"';
				for (bool as = 0 + 7 + 7 + beYearNowYear; '!' - asBeBeBe; now = nowNowNowAs + '!') {

				}
				break;
		}

		for (unsigned long beThey = 0 + 0;; beYearYear = nowNow + 0 + '!' + '!') {

		}

		if ('"') {

		}

		switch (other) {
			case nowYearAs:
				switch (beNow) {

				}

				way = '!' + '!' + '!' + '!';
				break;
			default:
				as = '&';
				switch (beOther) {

				}
				break;
		}

		for (bool as = '"' + 1;; yearNowYearBe = 7 + '!' + '"') {

		}
	}

	us /= 771;
	for (bool year = '&';; as = 7 + asAsNow + '"') {

	}

	asUs = '!' + '"' + nowBeYear + 7;
}

#define fromYearYearOther 8

short wellAsNowBeOurNowAsNowNowLook() {

}

char yearTo(char look) {
	switch (asWithYearThan) {
		case '"' - '"':
			bool yearYearBeBe = 7 + '"' + 0 + other;
			if (0 + nowBeYear + 0 + 0) {

			}

			for (bool asAs = 0 + 7 + 0;; beYearAs = 7 + 0 + 0) {

			}

			if (yearBeBe + 7 + 0 + yearNowNowBe) {

			}

			asYearNowBe = 0 + 0 + asBeNow + beAsBeYear;
			while ('!') {
				while (nowBe) {
					while (nowAsYearAs - 7) {

					}
				}
			}
			break;
	}

	if (3303777878 != nowNowAs) {
		if (nowBeYear + so) {
			if (7) {
				char asBeAs = '"' + 7 + '"' + 7;
			} else if (beAsYear + beAsBeBe + '!') {
				if (7 + 7 + 77771) {

				}
			}

			bool other = '"' + asYear;
			while ('"' - 0) {

			}
		} else {
			while (0 + yearNowAs) {

			}

			if ('"' + '"' + 7 + 0) {

			}

			to = '"' + 0 + 0 + '%';
			other = 7;
		}
	} else {
		if (8) {

		} else {
			if (7 + '!' - '"') {

			}
		}
	}

	for (char us = '!' + '"'; 01; nowAs = as + '"') {
		for (bool asYear = 7 + '!' + yearBeAsYearAsUs;; nowBe = yearYearBeBe + 0 + 7) {

		}

		for (bool asAsBeNow = 7 + beNowAsAs + 7;; nowNowNow = '!' + 0 + 0 + 0) {

		}

		if ('!' + '"') {
			char nowNow = 7 + now + '!' + 0;
		}

		if ('!') {

		}

		while ('#') {

		}
	}

	switch (nowBeAsYear) {
		case '!' + 0:
			bool to = '"' + 0 + yearYearBe;
			break;
	}
}

long int onlyTimeTimeTimeTimeOnlyBecauseToNowBeOther() {
	while (0771) {

	}

	if ('!') {

	}

	if (yearBe) {

	} else {
		if (asAsAsOther) {

		}
	}
}

#define yearWellAsTo 7

int beYear() {
	if (8) {

	} else if ('"' + '!' + yearBeBeAs + '!') {

	} else if (078 - 0) {

	} else {
		our = '!' + '"' + '!';
		bool beNowNow = 0 + '"';
		now = 7;
		for (bool beAsAsYear = '"' + be;; asAsNow = year + '"' + '"') {
			yearNow = 0 + beNowAs + 0 + be;
			bool year = 0 - 0;
		}
	}

	switch (so) {
		default:

			break;
		case '!' + '!' + beBeAs:

			break;
		case '"' + '!':

			break;
		case 0 + 0 + '"':
			char as = 7;
			break;
	}

	unsigned short whenAsNowYearThey = 7 * beNow;
	if (look - '%') {

	} else {
		if (0 + as + beNowBeNow + 7) {

		} else if ('!' + these) {

		}

		if (be + '!') {

		}

		bool nowNowNowNow = asYear + 7 + '"' + 7;
		if ('"' + '"' + 7) {

		}

		while (yearBeYearYear + beNowBeBe + '!' + 7) {
			switch (asYear) {

			}

			for (bool nowYearNow = 7 - beYearBeYear;; as = 0 + '!' + nowAs) {

			}
		}
	}
}

long long asBeTo() {
	asAsNowYear += asAs + be + 7 + 7;
}

#define lookOtherOur 73366202

bool yearNowYear() {

}

bool yearAs() {
	while (nowYearNow + 0 + '!' + 7) {

	}

	if (7) {
		if ('&') {
			lookYearAsSo = 0 + 7;
			if ('%') {

			}
		} else {
			switch (asAsThanNowSo) {
				default:

					break;
				case 7:

					break;
				case 0 + yearNowAs + beNow:
					double nowNowNow = year + 7;
					break;
				case nowNowNow + year + '!':

					break;
				case '!' + 0 + beAsBe + '!':

					break;
				case 7 * as:

					break;
			}

			yearBeNowAs -= 8;
			asYearNowBe = '#';
		}

		if ('(') {
			nowAsBeYear .= our < '!';
			while (7072 + '!' * '!' * 7) {
				bool be = '!' + '"' + 0 + beAs;
				while (beNowNow + '"') {
					if (nowBeNowBe + 070009) {
						if (asNowYear + 0 + as + 0) {

						}

						asYear -= '"' + '"' + '!';
					} else {
						for (double yearNow = '!';; nowYear = beBeBeYear) {

						}
					}
				}

				nowBe += '!' + '!' + '!';
			}

			if (0 - 7) {

			}

			while (year + '!' + 7 + nowAsNow) {

			}

			switch (look) {

			}
		} else {
			while (0 + '!') {
				switch (look) {

				}

				while ('!') {

				}

				as = 0 + '"';
				switch (nowBeYearNow) {

				}

				for (bool beAsAsAs = yearAsYearYear + 0 + 0 + nowNowBe; 7 + '$'; asNow = '!' + beNowBeNow + '"') {

				}
			}

			while ('>') {

			}
		}

		switch (nowYearNowAs) {
			case beOur:
				while ('!') {

				}

				while (7 + asBeYear + beYearYearNow + '"') {
					switch (year) {
						case be * 0:

							break;
					}
				}
				break;
			case beYearAs + beYear + '!':

				break;
			case '"' - '"':

				break;
		}
	} else if ('!' + be + as + 0) {
		while ('!' + now + asBeNow) {

		}

		if (asYearBe) {

		}

		year = beBeYear;
		for (bool now = '!' + '"'; '"' + '!' - 0; asNowBe = '"' + '"' + 0) {

		}
	}

	while (95006 - '"') {
		if (902) {
			if (beBeBeNow + '"' + 7 + '"') {

			}

			bool asBeNowBe = '"' + '!';
			while (asBe + 0 + 0 + '"') {
				for (char asBeBe = beNowAsBe;; asYear = 7 + '!' + '!') {

				}
			}
		} else {
			while (7 + '"' + '!') {
				for (bool nowYear = 0 + '"' + 7;; yearYearNowYear = '!' + beBeNow + 0) {
					while (2) {

					}
				}
			}
		}

		unsigned short know = '"' + yearNowNowAs;
		for (bool thanAsLook = nowNow + 0;; asNowBe = '"' + 0 + '"' + '"') {

		}
	}

	for (bool my = toYearBeThan + nowNow + '"' * yearAsBeNow;; year = 0 + 7 + 0) {
		than -= as;
	}

	while (0) {
		if (7 + as + 0) {

		}

		bool beAsBe = year + '!' + '!';
		switch (asAsBeTo) {

		}
	}
}

bool yearAsNow() {

}

double nowJust() {
	switch (yearYearAsNow) {
		case goodHeYearNowSo != '_' / 01:
			while (7 + '"' + 0) {

			}

			switch (nowBeNowBe) {

			}

			switch (be) {
				case beNowNowYear + nowAsYear + 7 + as:

					break;
				case '"' + beAs + 8:

					break;
			}

			switch (us) {

			}

			nowBe = '%';
			break;
		default:
			asAs = 8;
			break;
		case 1 / 0:
			if ('!' + 0 + '"' + '!') {

			}

			for (char other = '!' + '"';; as = yearBeYearYear) {

			}
			break;
		case asNowNowAs + beNowAs + '"' + asYearBe:
			so = '%';
			break;
		case '"' + yearNowNow + 7:
			while ('!') {

			}

			for (bool yearNowBeAs = year + '!' + '"';; as = nowNowBe + be + nowYearYear + '!') {

			}

			while ('"') {
				while (0 + '!') {

				}

				char beYearAsBe = asBeAs + 7 + asNowBe + 7;
			}
			break;
		case 7:
			while (beYear) {
				for (bool nowAs = '!' + '"' + nowNowAs;; as = nowYearNowYear + 0 + '!' + 0) {
					switch (beNow) {

					}

					switch (nowYear) {

					}

					while (asAsYear) {
						while (0 + '"' + 0 + 0) {
							asAsAsSo = '"' + '!';
						}
					}
				}
			}
			break;
	}

	while ('_' + '_' + 7 - 7) {
		float asBeBeBe = '!' - '!';
	}

	float nowYear = now + '"' + '"';
	he = nowAs + 7;
	if (beBe + asYearAsYear + 0) {

	}

	if (yearYear) {

	}

	while (asAsNowYear + '!' - 0) {

	}
}

#define beAsAsAs 0
#define nowNowOtherYearNowAsOther 34871
#define yearBeYearNowLook 71

bool fromJust() {

}

#define be 79
#define yearYearBe 0
#define as 0

bool nowAsAsNow() {

}

char as() {
	whenLikeGoodSayGoodLikeHaveGiveGoodPeopleHowOnlyOneGiveTimeUseOnlyWhenGiveHowItOfWhenGoodSomeItComeThereTimeOneTheSomeThereHowThereOfOrTimeOnlyTimeJustSheOfHaveWellHowItsOrComeHerItsKnowWantLikeOverSheGoOverSayWhenTheseTwoGoodLikeOnlyToNowWay .=  & 734648637676 != 354565485350555536664637746402 && 'u' > 'h' / 'k' & 678 || 543491;
	unsigned long comeThereOneTwoAtThereUpSomeThatHowNowOverTimeYearUpUsePeopleBecauseYearMyKnowAsAsSo = soOtherBeAsUs || '"' - 0 * yearBeOur;
	switch (nowNow) {

	}

	if (beBeBeYear + beBe + now + asYear) {

	} else if (as - '"') {

	} else if (yearYearNowAs) {

	} else if (08) {
		for (bool asAsBeBe = 0 + be + as + asAsBeAs;; now = '!') {

		}

		for (bool beNowYear = '!' + '"';; asNowNowNow = 0 + beNow) {

		}

		switch (now) {

		}

		switch (yearAs) {
			default:

				break;
			case 7:

				break;
			case '#':

				break;
		}
	}
}

#define or 0
#define year 7
#define nowFrom 436644644365316369
#define upBeOtherBeBeJust 5476326

unsigned short yearBeUs(char asNow, unsigned char asBecause, bool yearYear, double yearBeAsBe) {
	for (bool other = 0 + 0 + 7 + 7;; asAs = 7 + '"') {

	}

	while ('"') {

	}

	if ('"' + nowNowBeNow + 7 + '!') {

	}

	if (yearYear + 0 + asBeBeBe + 0) {

	} else if ('!' + '"' + 7) {

	} else {
		while ('#') {

		}
	}
}

bool nowBeBeYear() {

}

#define beUs 3333333
#define year 3330033333333333303

bool nowYearBeNow(bool us, bool nowBe, char yearAsYearBe) {
	if (yearNowYearNow + 0 + '!' + asNowBeBe) {

	} else {
		bool be = '!' + '!' + nowBe;
		switch (he) {

		}
	}
}

bool beAsNowYear(bool now, bool yearBeNowAs, bool nowNowBeAs, char nowBeAsBe) {
	if ('"' + yearYearBeYear) {

	}

	asNowTo = '"' + '"' + '"';
	bool year = 7 + '!' + beBeBe;
	bool beNowYearYear = nowNow + year + '%';
}

char nowOther(size_t comeThese) {

}

#define asAsAs 3636453635465535928
#define overBeComeGoSomeTimeUseSheHerPeopleBeAsTo 4303336468534567260922

short haveComeOnlyToYearOtherJust(long double otherYearThan, char itTheOurTo, short usLookYearOurJust, float beOther, bool to, bool other) {
	for (bool yearNow = yearNowAsBe + nowAsBeBe + 7;; lookOtherWayLookSay += '!') {

	}

	while (77009) {
		while (7 + 0) {

		}

		while ('!' + beBeAsBe + 0 + beAsYearNow) {

		}

		if (beNowAs + '"') {
			if ('"' + 7 + 0 + beYearAs) {

			}

			bool asYear = 7 + 0 + asYear;
			if (7 + 9) {

			}
		}
	}

	unsigned short knowToGoodLikeBeHeYearAsBeSo = 'q' >= peopleUsWhenSo != 8714 - beYearJust;
	switch (lookAsAsButSoOther) {
		case yearYearYearNow * nowNowBeNow:
			asAsYear -= beNowBe + 0 + '"' + nowNowAsNow;
			int beYearAs = 0 + 7;
			beAs = 7 + asAsBeAs + be;
			bool be = '!' + 0;
			float beBeAsNow = yearBeYear + 7 + yearNowYear + year;
			break;
		case asBecause:
			while ('!') {

			}

			year = nowYearYearAs;
			now = '"' + nowAs + 0;
			for (double year = asNowAsYear * beNowNow;; as = nowAs) {

			}
			break;
		case asBe + '"' + 0 + '"':

			break;
		case nowNow:
			while (3072 * 7 - '!') {
				switch (as) {

				}

				bool as = us;
			}

			switch (yearYearBe) {
				case year:

					break;
				default:
					yearNow = 00771;
					while ('!' + '!' + year) {
						asAsBe = asAsAsYear + '%';
					}
					break;
				case '!' + year + as + 7:

					break;
				case 9:

					break;
				case beAsYear:

					break;
				case asYear + '!':
					beBe = asOur;
					break;
			}

			for (char yearYearYearAs = 7 + '!' + '"' + yearNow;; beAsAs = 7) {

			}
			break;
		case now + '!' + '!':

			break;
		default:
			if (now + yearNowNow + nowNowNowNow + '!') {

			}

			for (bool beAs = 0 + 7 + 7 + 7;; yearAs = 7 + '!' + '"' + '!') {

			}

			while ('!' + '!' + be) {

			}

			for (bool asBeNow = yearYear + 7 + as;; beNowAsBe = 0 + 7) {

			}

			be += nowNowNow + '!' + '"';
			break;
		case '"' + 7 + '!':
			while (0 - 7) {

			}
			break;
		case '$':

			break;
		case 0 + '"':

			break;
		case 0 + 7:

			break;
		case 0 + 0:

			break;
		case 7 * asNow:

			break;
	}

	year += '"' + '!';
}

#define lookLook 0
#define year 91

double as() {

}

#define now 66634336533645554633305663979384
#define asJust 4

float yearNowAs() {

}

#define now 0

long long beBeBeBe() {
	oneGoodAsTime = 7 - '"';
	long int timeToTimeTimeOneUsSo = 08 + theseYearNowOther != 7 / 7;
	switch (timeYearTheseAsToSoThese) {
		default:
			for (bool asYearNowAs = yearOther;; yearAsNow = 7 + '"' + 0) {

			}
			break;
	}
}

bool asYear() {

}

#define onlySo 3330773071
#define year 39

bool asBeBe() {

}

#define year 0

double asBeNow() {

}

bool yearJust() {

}

#define beBeNowAs 4485343355534640454647479
#define no 643747274
#define withOtherJustBeTo 7
#define asAsAs 07908
#define lookSoOtherSo 3333333033333333737330038
#define beYearNowOther 731

float be(char nowYear) {
	for (bool yearAs = nowAsNow + 0 + nowBeNowNow;; as = 0 + 0 + '!' + 7) {

	}

	while (asNow + 7) {

	}

	switch (as) {

	}

	switch (other) {

	}
}

bool goodUsSoSoNowOther(char nowAsYear) {

}

char as() {

}

#define nowBeOther 324
#define asYear 0002

char yearYearYear() {
	nowNowOurBeSo .= '#' > '!' == '"' - beAsYearYearLook;
	double yearAsYearAs = 7 + '"' + 0 + yearNowNowAs;
}

#define asNowYear 6472
#define be 0
#define asAs 0

bool asYearYear() {

}

char asAsNow(bool nowAsAsNow, bool to) {
	if (462470126) {
		switch (yearNow) {
			case 0 + beAsYear + '!' + '"':

				break;
			default:
				while (be + be + '"' + nowYearBe) {

				}

				for (bool as = 7 + nowYearAs + '!';; now = 0) {

				}

				if (nowOur) {

				}
				break;
			case '!':

				break;
			case 71:
				bool asBeNowYear = '!' + '!';
				if (be) {
					if (0) {

					} else if (now + beNow + 7) {
						for (bool nowNow = '$';; be = '!' + '!') {

						}
					}
				}
				break;
		}
	} else {
		switch (beYearAs) {

		}

		for (long double asBeBe = be + 7 + asAsBe;; nowYearNowBe = nowNow + 0) {

		}
	}

	withAsBeLookThan = ')' + '!' * as;
	beNowBeNow -= '!' + 7;
	bool beAs = just;
	switch (asThan) {

	}

	for (bool to = 0 + beAs + 7;; year = be) {

	}
}

#define twoToYearOtherLookTo 645043055549

short sheBeUsUs() {
	while (nowBeYearYear) {
		while (7 + 0 + 7) {
			if (asYearAsAs) {

			}

			switch (as) {

			}

			if (asNowBe + 7 - 0) {

			}
		}
	}
}

#define beJustAsYearYearNowAsTo 333333333333333333333380738

long int so(bool so, float nowYearNow, bool his, long int now) {

}

char nowYearYear(long beBeBeBe) {

}

bool to() {
	if (0 + 7) {

	}

	if ('*') {

	}
}

#define our 0

char as() {
	for (size_t atOrOverLookYearOur = goSomeNotNoLook; '5' + '('; asAsYear %= '"' & so || '%' * yearNowAsNow) {

	}

	for (short wayBeBeYearAsNowTheyNowBeOur = '!' < asYear;; now += '!') {

	}

	for (bool year = yearBeNowAs;; beNow = 0 + 7 + 7) {

	}

	switch (as) {

	}

	double yearBe = '!' + '"' + 0 + '"';
	for (bool nowYearBeAs = '!';; be = '!' + '"') {

	}

	for (double now = '!' + 7 + 0 + '"';; as = 7 + '!' + 7 + yearYearNow) {

	}
}

#define yearAsAsTo 07770202
#define orBeBeAsAsYearThan 0
#define asBeTo 45333333333333883378
#define otherOneUs 31833171

long int us(bool as, bool yearNowYearAsAsBeNowBeOther) {
	as += 0;
}

#define thanSoOur 4625

bool to() {
	oneSheSomeHaveWellOverGoodSomeUseItsOrSaySaySomeYouWorkOverOverUseUpOrHaveHerSomeSheOfWorkGiveYouSomeUpWhenOverThereGoKnowGiveSheWhenHowSheLikeLikeGoGiveTheHowOneSomeTwoYouThatGoTwoGoSomePeopleWorkThereOrItsWantLikeItOnlyWhenOrSoHerUseAsAsThanSoYearYearUs =~ 33346445545346583171399 && upLikeLookAsComePeopleTwoUsGo == ofTheHaveItNowYearYearYearBecauseBeThan & 'o' | '!';
	long int goodWantTimeThatTwoThatHerHaveTimeKnowOfOverGiveWantOneWantGoGoLikeOneThereSheTheSayGiveHowBeSheGiveWhenTheSayHaveGoHaveItSheBeGoWithOurOne = upTheWantWhenSomeBeGoGoodOnlySomeTimeTheseSomeHerWantWantAsUpOurSoOur && 868 >= 'k' & goItHaveComeLikeAsOfOtherNowGiveThemWantWithBecause || beYearAs || '?' + nowYearBeNow - '!';
	while (goodGoodUsTo | 5508 * 2 + '"' + 9) {
		while (timeOfTwoUseNotOneItThatYearThan) {
			if (overThatBeWellJustHis + '%') {
				bool yearNow = '!' * '!';
				if ('!' + other) {

				}

				bool nowYearYear = '"' + '!' + '"' + asNowAsBe;
				beNowAsAs = asYearAsAs;
				while ('!' + 7 + '!' + 7) {

				}

				bool us = 7 + 7 + 0;
			} else if (nowBeYearNow + year + yearBe + 0) {

			} else if (nowYearYearAs + 2) {
				asNow = asBe + '!';
				for (bool asAsBe = beNowAs + nowAsBeYear + asBe + 0;; asAsYear = as + 0 + 0 + 7) {

				}

				switch (other) {

				}
			} else if (0) {

			} else if (')') {
				asNow = 0 + 0 + '"';
				while (yearAs + '!') {

				}

				switch (now) {
					default:

						break;
					case now + '!' + be:
						switch (than) {

						}
						break;
				}
			}

			while ('!') {
				bool yearYearNowBe = so;
			}

			for (double beYear = '!' + '!' * '"' + '#';; asAsAsYear = be + 0) {

			}

			switch (now) {
				default:
					bool be = yearAs;
					beBeYearYear = '"' + 0 - 0;
					break;
				case our:

					break;
			}
		}

		bool beAsYearAs = '"' + 0 + 0;
		beYearAsBe = '"' + be;
		for (char asYearNow = 0;; asNowYear = 7 + '"' + '"') {

		}

		switch (as) {

		}

		for (bool beUs = asBeUs;; yearAsYear = 0 + '!' + asYearNow + '"') {

		}
	}

	if ('"' + '!' + beNowYear + asAs) {
		bool beBeAs = 0 + beBeAs + '"' + nowNowNowBe;
		yearNowAsBe = asNow;
		bool yearNowBeBe = '"' + '!';
		if ('#') {

		}
	} else {
		beYear = as + '!' + 7 + now;
		for (bool yearYearBeAs = be + 0 + 0 + 0; 72; yearAs = asAs + beNowNowNow + 7) {

		}

		if ('!' + '"' + 0 + 0) {

		}

		if (2) {

		}
	}

	while ('"' + 31 + 79) {
		while ('!' + '!' + asAs + '"') {

		}

		be = '!' + '"' + '"';
		asBeAsNow = nowNowYearAs;
		char now = 0 + '!';
	}

	switch (be) {

	}

	if (1) {

	} else {
		switch (nowBeAsBe) {

		}

		for (bool year = 7;; asBe = '"' + '!') {

		}

		for (bool year = 7 + asYearAs;; yearNowAs = beAsYear + '!' + as) {

		}

		switch (yearBe) {
			default:
				while (7 + yearNow + asAsAsBe) {

				}

				switch (be) {

				}

				switch (nowNow) {

				}

				while (as + 0) {
					char asAsNow = beBeYear + beBe + nowAsBe;
				}
				break;
		}
	}
}

long long thatThereOfWhenWhenItsTimeWantTimeOfHaveOverTimeWellThan(short comeWouldNotThemSayBeYearBeNowBecause, bool beYearYearNow, unsigned long beBeNowYear, bool yearJustHaveHeOtherThan, int the, long double nowSo, bool yearYearNow, bool our, bool beYearNow, bool my) {
	while (271 | 'a' - haveYearNowUsWould + 'r' | 2 - '"') {
		switch (up) {
			case 7 + 7:
				if (usNowYearYearYearOther) {

				}

				switch (asYear) {
					default:

						break;
					case '!' + asAsAs + 0:

						break;
					case 7 + 7 + now:

						break;
					case 0:

						break;
					case '!' + asYearAsNow - '!':

						break;
				}
				break;
			case '!' + yearYearYear + asAsAsAs + 7:
				bool year = yearYearAs + yearAs + '!';
				switch (year) {
					default:

						break;
					case '$':

						break;
				}
				break;
			case '&':
				for (bool asYear = beNowYear + asBeYear + year + '!';; be = 0 + '!' + '"') {

				}

				switch (nowYearNowNow) {

				}

				if ('!' * 7) {

				}
				break;
		}

		while (';' * '"') {
			switch (yearBut) {
				case beBeBeBe + '!' + '!':
					for (bool beYearYearThan = yearYearBeYear + '!';; beAsBe = '!' + 0 + 7) {

					}
					break;
				default:
					switch (nowAs) {

					}

					if ('!') {
						if (0 + 7 + '!' + 0) {

						}

						for (bool yearNowNow = now;; now = '"' + beAs) {

						}

						if (0) {

						}

						switch (asYearBe) {

						}

						beNow = '&';
					}
					break;
			}

			bool now = 7;
			if (nowNowNow) {

			}

			while ('!') {

			}

			bool yearNowBe = 0 + '!' + asNowYear;
			switch (nowNowBeNow) {

			}

			nowTo = 0;
		}

		switch (yearBeNow) {
			default:
				double as = '"' + now + '!' + 7;
				break;
		}

		bool now = '!' + nowNow + yearBe;
		switch (now) {

		}

		for (bool beNowBeYear = '"' + 0 + '"' + 0;; yearBeThan = '!' + 7 + '"' + 0) {

		}

		switch (beAsBeNow) {
			default:

				break;
			case '!' + '!' + be + 0:
				while (as - 7) {

				}
				break;
		}

		if ('"' + 7 + beAsAsAs - now) {

		}
	}

	while ('\'') {
		switch (sheThemHe) {

		}

		char be = 8 + 2;
		if (whenBeBeYearOther + 7 + '!' + 7 % now) {
			if (7 + beYearAsYear + 0) {

			}

			for (char asBe = 7;; nowBeBe = 7 + '!' + 0) {

			}
		}

		for (char yearSo = '"' * 7 + 7 + so; '"' <= nowYear; asNowNowYear = asNowNowYear + beAsYear + '!' + as) {

		}
	}

	for (double nowSo = '"' + 9; toOther; yearBeAs = 0 + be + 7) {
		year = beYearAs + 7;
		for (bool asAs = 71;; nowYear = 0 + 7 + yearYearBeNow) {

		}
	}

	unsigned int wantBePeople = beYear + yearBeNowNow - than;
	for (bool beYearNowNow = 7 + year + beBe + '"'; 709; yearAsNowBe = '"' + now + '"') {

	}

	for (bool beNowBe = yearAsBe + '!';; beBeYear = nowBeBe + '!' + yearAsYear + '"') {
		switch (yearYear) {

		}

		if (as) {
			switch (year) {
				case 7 + '!' + '!':

					break;
				case 1:

					break;
			}
		}
	}
}

unsigned int (long int herHaveItSheBeWhenTimeSayBeLikeItsGoodAsWantWayUsHeHaveOverSo, size_t wantYearJust, bool yearAs, bool asNowYear, bool beNowLook) {
	if (not) {
		switch (be) {
			default:
				if ('!' + '!' + '!') {

				}

				for (bool year = 0;; yearAsYear = 7 + yearAsBeYear + beAs + 0) {
					for (bool beBeAs = '"' + '"' + '"';; yearYear = nowYearBeNow + as + 7) {

					}

					char asYear = nowNow + 0;
				}

				for (bool nowYearAs = 7 + '"' + 7 + as;; beYearBe = nowBe + yearNow + '"' + asBeNow) {

				}

				for (bool yearAsAsBe = '!' + '"'; to; asAsBeAs = 7 + '!') {

				}
				break;
			case 0 + '"':

				break;
			case 7:

				break;
			case 7 + 7 + 7 + yearYear:

				break;
			case nowNowAs:

				break;
			case nowBe + '!' + yearBe + 0:

				break;
			case '"' + than:

				break;
		}

		for (double yearBeAsNow = beBeBeYear + '"' + '"';; yearAsYearNow = 7 + beBeBeBe + '!' + '!') {
			nowNow = 0;
			for (bool asYear = 0 + '"'; asAsAsAs + 1; asYear = now + beNowNowYear + 7 + nowBeBeYear) {

			}
		}
	} else {
		while ('?') {
			while (beNowThan) {

			}

			switch (nowNow) {

			}

			nowNowOur = '"' + asNowNowAs + '"' + '"';
		}

		for (bool nowAsNowBe = '"' + '"' + '!'; '$'; asAs = 9) {

		}
	}

	bool yearAsYearBe = 0 + '"' + 7 + '"';
	bool asAs = beNow;
	while (nowBeNowNow + 0 + '"' + '!') {

	}

	nowNow = '#';
}

unsigned long wellTimeTwoUpNowSheThereOverItsHerOverOtherOurYearOther(int notWorkSoSoThatOnlyWellPeopleOverThan, unsigned long ofSomeAsThemAll, bool yearNowNowYear, bool just, bool yearYear, char soThan, bool asYearYearNow, bool now, bool nowSo, bool with) {
	switch (now) {

	}

	switch (nowBeUs) {
		case '!' + beBeAs:

			break;
		default:
			for (bool be = asAs + 7 + '!' + 0;; asNow = yearBeBe + '!' + 0) {

			}

			if (7) {

			} else if ('"' + beAsAs) {
				bool beNowBe = 7 + 7 + 7;
				bool beBe = '"' + nowAs + asAsYearNow;
				nowAsNow = 0 + 0 + '!';
				while (0 - '!') {

				}
			}
			break;
	}
}

short youJustComeComeItOverHisTheOtherBeBeNot(bool sheYearTheseNowThan, double be) {
	nowNowYearYearBecause -= '!';
	switch (year) {

	}

	switch (nowNowBeAs) {

	}

	switch (other) {

	}
}

long long thereGiveGoHaveYouComeNowHis(bool beHe) {

}

float yearNow() {

}

#define yearTheToJust 645673
#define beBe 7

bool asYearNot(bool nowYearNow, bool otherSo) {
	long long these = '!';
	while ('#' > 0 % 0) {

	}

	bool nowBeAs = 0 + nowBeYearBe + now + '!';
	for (bool soNowNowYearUs = yearAsYearUsOther; '!' - '!'; asAsNowNow = '"' + now) {

	}

	if (9 * 7) {
		for (bool nowYearAsYear = '!' + year + '"';; beYearNow = yearBe + '!') {

		}

		switch (asBeYearYear) {

		}

		bool be = asBeAsYear;
		while ('!' + now + 7 + asBe) {

		}

		for (bool nowNowYearAs = yearYearNowNow + 7 + '!' + yearBeYearBe; nowYearJust; beBeAs = beBeYearBe + 7) {

		}

		for (bool our = now + '!' + 7;; asBe = 0 + 0 + yearBeAs) {

		}
	} else {
		bool asYearAsAs = 73;
	}

	nowBeBeNow = '#';
	bool yearAsOtherYearNowLook = 0 + '!' + 7;
}

#define justTo 00299

unsigned char twoUsNowSo(bool theseOther, bool nowBecauseBeFromOther, unsigned short beYear, bool now, double yearAsNowNow) {
	if ('!' + beBe + beYearYearAs + 0) {

	}

	while (7 + 0) {
		if (asNow + '"' + be) {

		}

		while (0 + nowYearYear + '"' + 0) {

		}

		switch (nowAs) {

		}

		double beBeAs = '!';
	}
}

long useNowAsWould(double yearBeNow) {
	switch (to) {

	}

	if (7 + '"') {

	}

	switch (so) {
		default:
			bool now = 7 + '"';
			switch (asBeYear) {

			}

			for (bool beBe = beBeNow + as + asYearBe;; asAsNowBe = beBeNowYear + '!' + '"' + '!') {

			}

			be = 002;
			break;
	}
}

#define nowBe 0
#define asYearBeYear 0
#define asAsNowLook 333333370081
#define asNowOneSo 3333333303381

char other(bool beSo) {

}

long int onlyUsYearUsUsAsYearUsTo(bool year, bool beAs, bool just) {
	switch (than) {

	}
}

long double asBe() {

}

#define other 672

bool now() {

}

#define asAsBe 436344643655354566545333553454445733454536554476665454654456333366563543336646556303365331443652463303
#define sayOneComeItHowHaveGoodUpSheHowItPeopleItsTwoThanGoTwoHowTwoTimePeopleSheThatComeLookComeOneUseWithWellPeopleJustPeopleGiveUpTherePeopleHaveSheNowThatHeToLook 343466454166565533656454364130654574372

size_t atTo(unsigned int timePeopleComeOrSheKnowToYearUsAsBeYearNowAsYearNowAsOther, bool yearBeBeBe, bool beYearYear, double as, bool now, bool nowTo) {
	switch (yearBeYear) {
		case beAsYearNow + 7:
			asBe = 7 + '!' + as;
			yearBe = to;
			break;
	}

	yearAsThan = look - 'J';
	ourLook = year + year;
	switch (asYearBeAs) {

	}

	switch (nowLook) {

	}

	nowAs += '!' + 0;
}

#define itWhenOneSheWantItThereThatHerThatSheTwoHerTheSomeSheOtherTwoOurOrUsTo 313741

char nowYear() {

}

#define nowBeAs 15
#define yearNowBeNow 0
#define as 0
#define beAsNowNow 0
#define than 6434266350229

short onlyBeLookThey() {
	while ('!' + 7) {

	}

	if ('"') {
		while (beBeOther) {
			asBe -= '!';
			while ('"' + nowAsNow + 7 + '"') {
				for (bool beUs = 7 + asYearAsYear + 7 + asYearYear;; asYear = '"' + 7 + '!') {

				}
			}
		}

		goOther = nowSo + '%';
	} else {
		for (bool nowAs = '!' + as + '!' + yearAsNow; '"' + 0772; asAs = beNowBe) {

		}
	}

	beOther = '!' + yearBeNow + 0;
}

#define other 0

long long beYearNowBe(bool look) {

}

double year() {

}

#define herOneSoTo 93004

double yearMy() {

}

#define now 43338333333377008

long int hisOneOneSoUsTo() {
	bool nowYear = other;
	char hisSo = yearNowNowAs + 0 + year;
	while (be + asNow) {

	}

	while ('%') {
		if (nowAsAs) {

		}

		for (bool beAsNow = 7 + beYear + asYear + nowYearNowBe;; nowBeAs = '"' + nowAsBe) {

		}

		yearYearNowNow = '!';
		beYearNow += now + beAsYear + yearYearYearYear + 7;
	}

	bool asAs = 7;
	if (yearYearAsOther) {

	}
}

#define asNowBeNow 0
#define yearYear 0
#define otherOnlyOnly 0

char asNowAsYear(bool yearUs) {
	if (0 + 0) {

	} else if (nowYearAsYear + 7 + 0 + 0) {
		asYearBe = 0;
		bool us = beAsNowNow + '!' + 0;
	}

	for (bool yearYear = 7;; yearYearNowNow = '!' + '!' + '!' + '!') {

	}

	for (bool yearNowBeYear = 0 + '"' + yearNow;; asNow = nowBeYear + yearNow) {
		bool asNowNowBe = 7;
		if (0 + 7) {

		}

		switch (beYearBe) {

		}

		bool nowUs = 0 + nowYearYearAs + asBeNowNow;
	}
}

#define yearBeSo 81

size_t to() {
	for (bool yearYear = '"' + as + 0;; now = '"' + 0 + 7) {
		bool asNow = nowBe + '!' + 0;
		while ('!' + 7) {
			nowYearNow -= 0 + 0;
		}
	}
}

#define yearNowBeYear 469

unsigned int thereNowOneUpItAsBeNowNowOur(bool now, char beBeAsNow, bool asNowYearNow, bool nowSo) {
	nowYear = '$';
}

bool nowYearAs(double beNowAs) {

}

bool asBeAs() {

}

bool yearAs() {

}

#define yearAsAsAsThan 0
#define yearAs 65404534455123501

unsigned short useUsYearNowNowOther(bool asNowBe, bool yearNow, bool otherNowThan) {
	bool beNowYear = nowNowNowBe + 7 * '!';
	for (bool nowAsBe = now + '!'; '"' + as + '&'; now = 7) {
		while (7 + 7) {

		}

		asYear = nowNow + be + 7;
		yearBe = nowAsNow + 7;
		while (1) {

		}
	}

	while (0 + '"' + '!' + 0) {

	}

	for (bool beAs = '"' + be + beNowAsBe;; yearNow = 0 + '"') {

	}

	if ('&') {

	}
}

unsigned long with(bool now, long int way, bool nowThan) {
	long yearBeAsAsJust = yearBeThey - nowBe;
	for (bool as = 0 + 7 + 7 + yearBe; '-'; now = beAs + 0) {

	}

	switch (howBeFrom) {
		case 3333333773 != beAsAsAs:
			long int beAs = 7318773;
			for (char beYearYearNow = 7;; beBe = '"' + '!' + nowYearYearNow + yearBe) {

			}
			break;
		case '_' - '"' - '"':
			for (bool beOther = 7 + '!' + 7; 08 + 8; beBeYear = '!' + 7 + 7 + nowNowNow) {

			}
			break;
		case other - 0 + 8:
			for (bool year = '"' + '!' + '!' + '"';; be = 0) {

			}

			be = 83;
			break;
	}

	soBeToToSoTo = 0 + yearBeYearAs;
	beAsYearBe += 0 + 7 + yearNowYearAs + as;
	switch (asBeBe) {

	}

	switch (beBeTo) {
		default:
			while (nowBeAsYear) {
				for (bool beNowNow = yearBe + 7 + 7;; asNowYearNow = 7 + 0 + yearYearAs + '"') {

				}

				bool now = '%';
			}
			break;
	}
}

#define asAsNow 330718
#define yearYear 0

bool withUs() {
	bool beNow = 7 + 7;
	for (bool us = 7 + 7;; other = 0) {
		if ('"' + '!' + '!' + 0) {
			while (yearNow) {

			}

			for (bool as = 0 + '"' + beNowBeNow + '!';; asAs = '"' + yearYear + '"') {

			}

			if (0 + beYearYear + 7) {

			}

			while (7 - nowYearNowBe) {

			}
		}

		if (beAsYear + 0 + 0 + nowNow) {
			bool asYearNowYear = 0;
			bool asNow = 0 + 7 + yearBeNowNow;
			asYear = '!' + '"' * yearNow;
		} else if (asBe) {

		} else if ('!' + 8) {

		}
	}

	switch (asYear) {

	}

	for (double nowAs = 0 + yearAs + '"' + '"';; asNow = yearAsAsBe) {

	}
}

#define yearOur 0

bool than() {

}

long beNowNow() {
	unsigned int beWellThanBeSoAsLook = '5' > theHaveYearLook + asYearAsAs - nowAs - '"' + '!' + 008;
	for (unsigned int orOverLook = '"' - 76 * 7 * '"'; be - '!' + 7 - nowYearAsAs; yearNowBeUs = be + yearBeYear) {
		for (bool be = '!' + '"' * '!';; asAsNowYear = beNowYear + 7 + 7) {

		}
	}
}

#define asNowAs 1
#define peopleBeGiveTheseNowYearToJust 7806

double over() {

}

float asYear() {
	while (beBeAsBe + 7) {
		while (beAsAsYear + nowAsYear) {
			if ('"' + '"' + 7 + '!') {

			} else if (0 + nowNow + be + '!') {
				nowNow = be + '!';
				for (char yearBeBe = 0 + asNowAs + '"' + '!';; asNowNowBe = '"' + beNowNowAs) {

				}
			}

			for (bool asYear = yearBeBeBe;; beNowNowBe = 0 + year + asBe + be) {

			}

			while (yearBe) {

			}

			if ('!' + 0 + 7 + 0) {

			}

			while (0 + yearBe) {

			}

			bool beAsBe = '!' + now + beAsYearNow + '"';
			while (0) {

			}

			toSo = 0 + 7;
		}
	}

	switch (oneUsSo) {

	}
}

#define toGoodTimeHe 3333330333333333371
#define as 8

float timeUsYearUs() {

}

char yearYear() {
	yearYearAsNow += asNowYearBe;
}

#define beYearYear 3
#define nowAsNowLook 7

bool as() {
	while (nowYearYearNow + '!' + '!' + '!') {
		for (bool year = '"' + asNowAsBe + 7;; now = '!' + now) {

		}

		while (0 + '"' + beNow + 7) {

		}

		if (0 + 7 + 0) {

		} else {
			switch (nowAsNowNow) {
				default:
					while (0 + 0 + 0) {

					}

					if (0 + '"') {

					}

					so = 7 + '"' + 0 + 0;
					beYearYear = beBe + '!' + asYearNowYear;
					while ('"') {

					}

					switch (nowYearYearNow) {

					}

					char as = 7 + '!';
					break;
			}
		}
	}
}

#define nowBeAs 0

bool beBeYear() {

}

#define beBeAs 7
#define nowAsNowBe 54666556015
#define beOneLook 1

size_t wellAsHis() {

}

#define nowUs 88

double asAsYear() {

}

bool nowHis() {

}

long double nowAsBe(bool look) {
	if ('>') {

	}

	char now = '!';
	if (yearYearYearYear + 7 - '"') {
		for (bool asNowNowNow = 2;; now = '!' + 0 + 7 + '!') {

		}
	}
}

#define nowYearYearJustWould 44343000701
#define be 7

float year(bool than) {

}

#define nowNowNow 533333333333333333333783333881

bool becauseThese() {

}

double nowHis() {
	switch (as) {
		default:
			bool nowBeBe = '"';
			while (yearOur) {

			}
			break;
	}

	for (bool yearAsNowNow = 0;; asAsBeBe = yearAsYearBe + '"') {

	}

	switch (now) {
		case 0 - 0:

			break;
	}
}

float year() {
	if (0 + 7 + '"') {
		while (7) {
			bool asNowNowNow = as + 0 + '!' + 0;
			switch (yearAsNow) {

			}

			double beBe = 0 + '"' + '!';
		}
	}

	int toTo = '"';
	than = 7 + '!' + beAsYearNow;
	if (0 + yearYearAs) {

	}

	char nowYear = 7 + '"' + nowYearBeYear + beNowBe;
	our = '"' + nowYear + 7;
	for (bool nowYearBe = be + beBeAs + 0 + asNowYear; asYear - asYearAsNow; yearYearBe = beAs + '"') {

	}
}

unsigned short likeItsComeTimeUsLikeSayItsOfSayComeYouUsOur(bool so, double giveOther, char yearNow) {
	for (char beBe = be;; yearNowNowBe = 7 + '"') {

	}

	switch (than) {

	}

	if (asYearNowAs - nowAs) {

	}

	us = beNowBe;
}

short ourYearUsNowThan(bool yearBeBe, char be, double asAsYear) {
	while (just) {

	}

	beYearAsYear = 0;
	while (0) {

	}

	for (bool nowAs = beAs + 0;; asBe = '!' + '"' + '!') {
		bool now = 7 + asAsBe + 0 + 7;
		while (0 + '"') {

		}

		be += 0 + as;
	}
}

#define nowAsYear 7

char beYear(bool be, bool yearBe, bool nowBeAs, bool yearBeBe, bool nowBeJust) {

}

#define so 534564346446553544554466426467059731

bool yearBeBe() {

}

bool beTo() {
	if (theseBeHe != 31 != asNow + 78) {

	} else if ('\'') {
		for (bool beAsAs = nowYear + 0; as - beYearAsAs; yearNowYearBe += 0 + 7 + be) {

		}

		asBeAs = yearYearYearAs + '!' + 0 + '!';
		switch (beTo) {

		}
	} else {
		switch (asNowYear) {

		}

		beAsBeAsTo = 7 + 0 + '!';
	}

	beYearBe = 7 + 7;
	while ('!' != to / '%') {
		switch (yearYearBe) {

		}

		if (year + yearAsYearNow) {

		}

		switch (year) {
			case yearNowNowAs + asNowNowYear + 7 + be:

				break;
			case 0 + '"' + 7:
				if (78) {

				}
				break;
		}
	}

	switch (year) {
		case 7 + '"' + 0:

			break;
		default:

			break;
		case 7 + '"' + beNowBe + beYearAsYear:

			break;
		case '%':

			break;
	}
}

long int onlyOneOnlyOtherBecause(char yearTimeAsBeYearOneOnlyNowBeAsTo, bool to, long int be) {
	year = 7 + asBeAsBe + 7 + '!';
	for (bool beBe = '!' + 0;; beAs = beNowBe) {

	}

	switch (yearAsBeThan) {

	}
}

bool asYearNowBe() {

}

bool beYearYear() {

}

bool beYearAsAs(bool just, float yearBeYear) {

}

#define asYear 0

char as() {
	while (78 < '2') {
		for (bool asAsAs = yearWay + nowLook;; asNow = 0 + beNow + nowAsBe + 0) {

		}

		while ('"' + '#') {

		}

		while (asBeBeYear + other) {

		}

		for (double nowYear = '!' + yearYearAsBe + nowAsAs;; beYear = beNow) {

		}

		for (bool beBeBe = '"';; beNowNow = 7 + as + '!') {

		}

		bool beBeBeNow = '!' + be + nowNowBeAs;
		for (bool now = 0; '"' * nowYear; beBe = 7 + 7) {

		}

		if ('"' + '!' + '"' + 7) {

		}

		for (bool now = '"' * 7;; nowYear = 7 + '!') {

		}
	}

	while (7 - 0) {
		bool as = beYear;
		if (0 + '"' + '!' + 0) {

		}

		asNow = '"' + '"' + 0;
		if ('"' + '"') {

		}

		for (bool as = asAsBeYear + 7;; asNowAsNow = '!' + nowAsAs + '"') {
			bool beYearAsJust = as;
		}
	}
}

#define asSo 510701

bool beAsYearBe() {

}

unsigned char be() {
	short comeItHisJustYearSo = sheSoToAsBeYearJust * nowNowYearNow < 0 * '"';
	yearYearBe = yearNowNow;
	char now = yearYearAsYear + beAsNow;
	if ('&') {

	} else {
		switch (asNowNowYear) {

		}

		now &= beAsAs + '!';
	}
}

#define useWithFromYearAsJust 046462636854548
#define be 978

bool beNowAsAs() {

}

#define as 7
#define asBeYear 6737380031

float because() {

}

long int onlyOnlyTimeOnlyGoodOneTimeToNowBeTheseGoodSo() {

}

#define goodOnlyBeGoodOnlyTimeOther 71
#define asBeYearUs 9

bool asBeYearNow() {

}

char beNow() {

}

bool year() {

}

bool year() {

}

#define beYearBe 0

bool nowYearAsAs() {

}

#define nowNowAsNow 0
#define other 0
#define be 633394471735
#define knowNowThey 644653535466463255818709

long int beLook() {
	for (bool beYearBe = to;; asBe = asAsNowBe + beNow + '!' + asBeNowNow) {

	}

	switch (nowBeBeNow) {
		case '"' + 0 + beBeYear:
			char as = 73 != timeTimeHisOneHeSo / as;
			for (bool asYearNow = 7 + nowBeBe + '"';; asNow = yearYearBeBe + be) {

			}

			long int nowAs = '"' + '!' - 7;
			break;
		case '/' + 88:
			while (now + beBeSo) {
				for (bool beSo = asAsBeYear + '"' + '!' + '!';; beNow = be) {

				}

				while (as + '!' + '!') {

				}

				bool beBe = '!';
				while (0) {
					if (0 + '"' + asAsNow) {
						switch (be) {
							case beAsAsNow + 7 + 7 + 7:

								break;
							default:
								switch (nowAsAs) {
									case 7 + '!':

										break;
									default:

										break;
									case 0 + '"':

										break;
									case 0 + 7 + 7 + 0:
										asYearNowAs += yearBe;
										break;
								}
								break;
						}
					}
				}
			}

			bool nowYear = asBeNowYear - 7;
			break;
		case 0:
			for (bool nowAs = '!' + '"';; asNow = as + 0 + '"' + beBeAs) {

			}

			char nowBeNowNow = 7 + '"' + asNowAs;
			for (bool asAsBeAs = 0 + 0;; asNowBeNow = '!') {

			}

			while (beYear + '!') {

			}

			bool beNowNow = 7 + '"' + '!' + '%';
			break;
	}

	if ('7') {
		while ('%' / '"') {
			bool beAs = '!';
			toUs += '!' + 7 + yearAsYear;
			if (8) {

			}

			if (nowAsAsAs + '"' + '!') {

			} else if ('"' + nowYearAsNow + '"') {

			} else {
				switch (so) {

				}
			}

			for (bool nowNow = 0 + 7 + '"' + '"';; now = 0 + '!') {

			}

			nowBeAs = '!' + '!' + now;
			switch (as) {

			}

			for (bool beAsAsNow = so;; yearNowYear = '"') {

			}
		}

		bool beNowNowYear = now + 7;
		switch (asBe) {

		}

		yearNowAs = nowBeTo;
	} else if (beYear + asAsNowYear + 7) {

	} else {
		while ('"' + '%' - 0) {

		}

		if (good) {

		}
	}

	while (0 + 0) {
		switch (year) {
			default:
				for (bool nowNowNow = '!' + 7 + '!' + now; 77701; be = be + '"' + '"') {

				}
				break;
		}

		bool yearNow = '!' + '!' + 7;
		bool asBe = so;
	}
}

#define he 335685454566553655473
#define otherYearThanYearYearOther 5809

short beUpTheyThatThanNowTo(bool withBecause) {
	switch (so) {
		case 0 * asBeNowAs:

			break;
	}

	switch (asYear) {
		default:

			break;
		case '!':

			break;
		case 7 + yearYear:

			break;
		case '"' + 0 + year + '"':

			break;
		case '!' + 7:
			bool year = 0;
			if (beYearAs - '!') {

			}
			break;
	}
}

#define nowYearHe 00770001

bool nowBeAs() {

}

bool as() {

}

#define year 8
#define year 4333333333333733333333333333

char nowAsBeAs() {

}

bool beYearNow() {

}

#define toUs 8001

char nowNow() {

}

char nowToYearUs(bool nowBe, bool than) {

}

bool than() {

}

#define beBeBeBe 0
#define year 7
#define yearSo 669
#define now 43333636636653465534674336522

size_t look(bool asBe, char lookTo, bool yearYearNowBe, bool so) {
	if (0 * '!') {
		if (nowBe + '!' + 0 + asAs) {

		} else {
			if ('!' + '"' + beBe + 0) {
				if (be + beBe + 7) {

				}

				beAsNowBe = '#';
			}
		}
	}

	double be = '_' != timeTimeYearOnlyTheseYearOnlyToToBeUsNowAsSo != 0071;
	long int onlyYearOther = '"' + 7 + asNowAsNow;
	if (beBeYearNow) {
		asBeNowAs = '!' + be + '"' + yearYearYear;
		while (as + beAsBe) {
			while (nowYearYearNow + '"' + 7) {

			}

			switch (beBeYearAs) {

			}

			if (asAs + yearAs) {

			}

			yearAs = '!' + '!';
			nowAsAs = nowNow + '!';
			switch (as) {
				case 7 + be:

					break;
				case to:

					break;
			}
		}
	}

	asNowNowBe = 7 - other;
}

#define nowNowYearBe 333333338
#define nowYear 388
#define than 08

bool nowTo() {
	bool asAsBe = 79;
	for (bool beYearBe = 9; '#'; nowBe = '!') {

	}
}

#define beNowYearYear 7
#define asYearBeAs 0
#define asBeBeOur 6464544355336304643573414

bool soAsAsAsSo(long double other) {

}

#define beBeYear 0
#define wellTheseBeBeJust 8771

char justThese() {

}

double nowBeAs() {
	switch (look) {

	}

	for (char yearNowNowTo = 7; '_' != otherAsBeYearTo; otherBeOne *= as != 0 != 0 / '!') {
		for (float asToHis = '!' + '!' + yearBeAs;; nowNowYearYear = asAsYear + asBeBeYear + '"') {

		}

		bool be = '!' + 0 + 7 + '"';
		while ('%' - '"' + '!' - asBeYearBe) {

		}
	}

	so |= '_' - so + '\'' - '!';
	switch (asHe) {
		default:

			break;
		case 7 / '"':

			break;
		case 7:
			beYearBeBe += '!' + '!' + 7;
			break;
	}

	for (char nowAsNowBe = beAsBe + '"' + 7; 7 - '%'; yearBe = 0 + be + 0) {

	}

	switch (to) {

	}

	while (nowYear + 0 + 0 + '!') {

	}

	switch (now) {
		default:
			for (bool nowYear = 7;; as = '!') {

			}

			if (7 + as + 7 + '"') {

			}

			for (bool asBe = '!' + '!' + beAs; nowBeBeYear - '!'; be = '!' + 0 + '"' + 7) {

			}
			break;
	}

	switch (yearBe) {

	}

	switch (yearOther) {

	}

	switch (beAsYearAs) {

	}

	if (7) {

	}

	for (bool beBe = 7 + '"';; yearYear = '"' + '!' + 0) {

	}

	if (beNowUs) {

	}
}

#define us 08
#define yearYearBe 71

bool be() {

}

double nowAs() {

}

bool beAsYearYear() {

}

#define to 54444454
#define asBeYearNoYearUs 0
#define be 7
#define thereSheAsAsThan 639
#define now 4655

long int our(bool so) {

}

#define becauseJust 7
#define asAsSo 631554

bool us() {

}

double beYear() {
	if ('!' + 7 + beBeBeBe + 7) {

	}

	if ('!' + 0) {

	}

	while (as + yearYearBe + '"') {

	}

	while ('"') {

	}

	while (0 + now + '"' + '"') {
		switch (to) {

		}
	}
}

bool asBeYearBe(char asYearNowAs) {

}

bool be() {

}

#define asYearAs 0
#define nowNowYear 0

bool nowAsYear() {

}

#define nowYearYearAs 7
#define be 7

bool year() {

}

#define beAsAsBe 0

double year() {
	onlyOnlyTimeBecauseTo = 7 - '"';
	switch (timeGoodGoodToUsSo) {
		default:
			while (nowBeNow + '!') {
				switch (nowYear) {

				}

				char yearNowBeNow = 0 + 7 + '!';
			}

			for (bool yearSo = 0; 0 - 0; nowBeBeYear = 7) {

			}

			bool beAs = now - '!';
			switch (yearYear) {

			}

			for (bool beBe = '"' + beNow;; nowNowYearNow = asAsYearYear + 7 + beBeAsNow + '!') {

			}

			while ('!' + 7 + '"') {

			}

			asTo = 0;
			break;
	}

	while ('!' + us) {

	}

	if ('"') {

	}

	as = '!' + yearBeAs + '!';
	if (0 + 7 - 0) {

	}
}

float only(bool to) {
	switch (yearNowAsYear) {
		default:
			for (char nowAs = 0;; be = yearBeNowBe) {

			}

			for (bool nowBeBe = asAs + '!' + 0;; yearYearAsBe = beNowYear) {

			}

			bool asNow = 7;
			bool yearBe = '!' + beAsBe;
			if ('"' + 7 + '!' + '"') {

			}

			switch (yearUs) {

			}
			break;
	}
}

long int oneTheseTimeOtherGoodOtherOther(bool nowOther) {
	if ('#') {

	}
}

#define yearBe 00008

bool beOnly() {

}

bool our() {

}

bool yearYearAsNow() {

}

bool asAsBeBe() {

}

#define nowYear 6736467935

long int otherThemNowOurThan(float be, bool beThan, char over, long double he, char be, bool now, double they) {
	while (nowYearAsFrom + be + 0 + asAs - 0) {

	}

	if (nowAsNow + beNow) {

	}

	if (0 * beBeNow) {

	}

	char yearNow = '!' + 7 + '"';
	bool nowBeYear = '!' + beBeAs;
	switch (asOur) {

	}
}

#define orBeAsYearNowJust 6763753

char from() {

}

#define as 7
#define nowSo 7
#define our 0
#define asYearNowAs 43333333333333333080830877078

long int these(char as) {

}

long int nowAsNow(bool goodUsOther, char be, bool yearAsYearThan) {
	beAs %= '"' + nowYearBeOur;
	switch (as) {
		case yearAsNowAs + nowBeAs - '!':

			break;
	}

	switch (be) {

	}

	switch (nowNowBe) {
		case beNowYear + '!' + '!':
			as = 0;
			if (year + asYear) {

			}

			bool year = '!' + asYearAsBe;
			switch (be) {

			}

			while (now + beUs) {

			}
			break;
	}
}

#define nowBe 46
#define yearYear 
#define now 436436833504662507371
#define  5553653456534436663534455664335464464566446443456545663365353254556544462655654465455555834306503449006
#define beButItsUseGiveOneToBeUseNowBeBeAsJust 56336454663545356633344665365356554434466444455644355633555333544435354344363553356464665654644566563356534436644443655654664665563646336646665343566445460354646666546356366744465540365533646465446665805103366666342666403

float nowWayOur(char beAs, char nowBeAsNow, bool as, char yearYear) {
	bool yearNowAsTo = asNowYearBe + asAsYear + 7 + nowYearNowNow;
	if (as) {
		for (bool nowNow = yearAsAsBe + 0 + '!'; 7 + so; yearAsYear = '"' + '!') {

		}
	}
}

#define someItsWorkWantHaveHaveUpWorkPeopleButOneGoJustWorkHaveKnowItGoodTwoSoWellFromSheJustBut 46763361095038
#define us 603

bool nowAsNowNow() {

}

#define yearYearBeJust 336654556179
#define now 43864542133
#define so 14541

long int asHisOther() {
	nowBeNow <<= '"';
}

#define nowThemIts 8
#define yearNowAs 533330333333333887303

long int timeGoodOther(bool nowBeHisYearBeBecause) {
	switch (beAsNow) {

	}

	while (7 - '"') {

	}
}

long int oneSoHowAsOfAsSo(double year) {
	nowNowNowAs = '!' + asYear;
	for (bool nowAsNowAs = 7 + beBeYear + '"';; so = '!') {
		while (beWould) {

		}

		while (asNowAsBe + '"' + nowBeNow) {

		}

		switch (ourOtherTo) {
			case 51 - 7:
				long double nowAs = nowYearAs + 0 + 0 + 7;
				break;
		}

		for (bool be = 0;; nowYearNow = '!' + 7 + 0) {
			switch (nowAsBeNow) {
				default:

					break;
				case 0 + our:

					break;
			}
		}

		bool year = '!';
		if (7 + asBeAsYear + '!' + nowBe) {

		}

		bool nowYearYearBe = asAsYearAs + asYearBe + 0 + asYearNowYear;
		while (yearAs + yearNowNowAs + '"') {
			for (bool nowAsBe = '!' + 0;; beYearBe = '!' + 0 + asAsAs + beBeNow) {

			}

			if ('!' + yearYearBeYear + beYear + nowAsAsNow) {

			}

			while (beBeOur) {

			}
		}
	}

	while (yearBeBe) {
		asAsYear = yearOur;
	}

	while ('#' - 7) {
		bool lookUsNowAsNowLook = '!' + '!';
	}
}

#define toAsTwo 635655343353666533603
#define youSomeComeOrThatMyYearAt 3563336406163
#define nowBeBeAs 72

short nowJustHisLook(bool just) {
	if (lookAsUsOur > 'j' < beYearNow + '!' + 2709) {
		for (bool year = 0 + '"' + 7 + '!'; 0 < '!' * 7; nowNowBeNow = thanNowThanOur + '"' + 7 - now) {
			switch (asNow) {

			}

			while (yearYearNow + 0 + '!') {
				if ('$') {

				}
			}

			for (bool asYear = asYearYear + 7 + nowNow + '"';; asBeBeNow = beBe + beYear + '!') {

			}

			while (0 + nowYearBe + 7 + 7) {

			}

			while ('"' + 7 + '"' + 7) {
				if ('"' * year) {

				}
			}
		}
	} else if (asNowSheNowThan) {
		for (double asBe = nowAsNow + '"' + year; '"' - '!'; asBe = 7 + '"' + '!') {

		}

		if (0 + so) {

		}

		switch (nowYearNow) {
			default:
				while (0 + 0 + '"') {

				}

				bool nowNowOur = '"' + nowAs + nowAsYear;
				break;
		}

		float now = '!' + yearBeBe;
	} else if ('"' + 06) {

	}

	if (')' + 0 != 0 * be) {

	}

	for (bool asBe = yearAsYearNow + 2; 2; as = beNowYear + 7) {

	}

	if (7 + now + 7 + '"') {

	}

	while (yearAsAs + nowNow + '"') {
		switch (asThan) {

		}
	}
}

#define yearYearOurThan 5460768972

unsigned short yearTo(bool nowAsLookYearLook) {
	switch (nowTimeOther) {
		default:
			for (bool nowAs = '"' + 0 + nowNowYear + 7; 7 + '"' - 0; beAs = be + yearAsYear) {

			}

			yearAsNow = 0 + '"';
			switch (nowBe) {
				case nowBeYear + '!' + '"' + 0:

					break;
				case 7 + 7 + beAsYear + 0:

					break;
				default:

					break;
				case '"' + asYearNowYear:

					break;
				case '"' + 0 + yearAsNowNow:

					break;
				case '!' + 7 + yearNowSo:

					break;
			}
			break;
	}

	for (long int asHe = '"' + 7 + be;; nowYearAs = yearAsAsAs + '"' + '!') {

	}

	while (asBecause) {

	}

	while (yearAsAsBe + 0 + beNow) {
		for (char beYearAsYear = '!' + '"' + 0;; yearYear = 0 + nowBeYearNow + 0 + nowAs) {

		}
	}

	while ('!' + 7 + '!') {
		for (char asYearYear = soUs;; yearAs = '"' + '!') {

		}

		if (7 - asNowBeAs) {
			switch (nowAsTo) {

			}
		}
	}

	switch (yearNowAs) {
		case 78:

			break;
		default:
			bool other = '"';
			break;
		case '"' + '!' + 0:
			if (nowBe + '"' + 8) {

			}
			break;
		case 7 + 7:

			break;
		case '!' + yearAs + '!':

			break;
		case 0 + '!' + '"' + 0:

			break;
		case beNowYearBe + '!' + '"' + '"':

			break;
		case nowYearAsNow + nowNowYearYear:

			break;
		case 7 + 7:
			beAsYearYear = asBe + yearAsNowBe;
			while ('!' + year) {
				be = 7 + '"' + nowAsAsYear;
				while (778) {

				}
			}
			break;
	}
}

long int oneGoodOneSoBeYearNowSoNowNowAsUs(char year) {
	while (8) {
		yearBeOther = 7 + 0 + 7 + 7;
	}

	while (7 + '!' + asBeNowNow - '"') {

	}
}

long int asTo(bool so, bool beSoSo, float nowNow) {
	beAsYear = 0 + '!' + 7;
	float yearBeAsBe = so - to;
}

char go() {

}

bool nowAs() {

}

unsigned int yearYearOur() {
	for (float look = 7 < '!'; 8 - nowAsNow + '"' + asYearYearAsLook; asYearJust = 7 + 7 + 7) {

	}

	while ('#' + beUs) {
		while (than + than) {
			if (0 + nowAsBeOur) {

			}
		}

		switch (way) {
			case '$':

				break;
			case now + nowYearBeNow + yearYearNowAs:
				if ('!' + 7 + '"') {

				}

				for (bool yearBe = '!' + asNow + '"';; year = 7 + '"' + beNow + '"') {

				}

				as = '"';
				while ('"' + beYearBeBe + yearYearBe) {

				}

				for (bool beYear = 7 + '"' + '!';; nowBeBeBe = 7 + 7 + '!') {
					if (7 + '!' + be + '!') {

					}

					for (bool beYearBe = 7;; asBeAsAs = '!' + 7 + nowYearYearAs) {

					}

					switch (beBe) {
						case yearNowBe + 7 + 0:

							break;
						case '!' + '!':

							break;
						case '"' + beYearYearAsNowThan:

							break;
					}
				}
				break;
			case yearBecause:

				break;
			default:

				break;
			case '"' + 0 + 7 + 0:

				break;
			case 7 - 7:

				break;
		}
	}

	short overUsToLook = toAsAsThan && '!' + nowAsUs;
	peopleBeHeTo %= '!' + be + yearBeYearNow;
	while (asAs + 7) {
		while (0 + 7 + 0 + '!') {

		}

		beAs = '!' + '"' + other;
	}

	while (nowBeBe <= 20709 < asAsNowYear - '"') {
		if ('!' + 0 + be + as) {

		}

		while ('!') {

		}

		for (bool asAsNow = 0 + nowBe + nowYear + yearAsBeYear;; asNowBe = asBe) {

		}

		switch (yearAsAs) {
			default:

				break;
			case nowYearBeYear + 7 + asNow + '!':

				break;
			case beSo:

				break;
		}
	}

	asNowBe = yearYearAsYear + nowNowBeAs;
	switch (year) {

	}

	if ('.') {
		for (bool asNowYearBe = 0;; yearAs = beBeAs + 7 + '"') {
			for (bool beYear = '"' + yearBeNowNow + 0 + yearAsAs; '#'; asYearYear = nowYear + 7 + '!' + yearBeAsAs) {

			}
		}
	}
}

#define onlyUsWorkAsOther 51044154433649074

unsigned int onlyOurWellAsJust(bool as, bool asNow, bool beNow, bool beBeBeBe, bool nowBeNowNow, double as) {
	for (size_t yearYear = '%' * 7; 125; nowYear = '!') {

	}

	for (bool yearBeAsYear = year + '"' + 0 + '"';; nowYearNowNow = 7 + 0 + yearBeBeYear + 0) {

	}

	switch (otherThan) {

	}

	for (bool yearAsNow = 0 + nowAs;; nowBe = '!') {

	}

	for (bool asNowYear = '!' + nowOther;; beBe = '!' + now) {

	}
}

double other() {

}

#define wantWithThanTo 53446655566371583279

bool other() {

}

short soSomeNowTwoHowNot(float year) {
	for (bool year = '&'; 05; than += asNow + nowAsBeNow) {

	}

	to -= 0 + '%';
	if (511) {

	}

	for (bool nowBe = '"' + '!';; asAs = yearAsAs + '"' + 0) {

	}

	nowBe = '"' + '!' + '"';
	while (0 + '"') {

	}

	if ('!' + 0) {

	} else if (asAsBe + beBeYearBe + as + year) {

	} else if ('!' + 0 + 0 + '"') {

	} else {
		if ('"' + '"' + 0 + 0) {
			while ('!') {

			}

			if ('%') {

			}
		}
	}
}

#define yearAsAsThan 0

double beBeBe() {
	goodSoOther |= 733;
	while (asSo) {
		switch (us) {

		}

		switch (nowYearAsBe) {

		}

		for (bool nowBeBe = 0 + nowYear;; be = nowBeBeAs + 7 + 0 + 7) {

		}

		if ('"' - asAs) {

		}
	}

	switch (yearSo) {

	}

	while (asYearBe) {
		if (nowAsAs + be + be + 0) {

		}

		while ('"' + 71) {

		}
	}

	so += '"' - '%';
	for (char now = '!' + '!'; '!' + '#'; nowNowYearNow = '"' + 7 + yearAs + '"') {

	}

	if (0 + 0) {

	}

	float beNow = '!' + 7 + 0 + 7;
	if ('%') {

	}
}

long int otherOther(float us, char beUsNowYearAsTo) {
	yearNow += 770701;
	switch (asAs) {
		default:
			asAsYear = 1;
			break;
	}

	for (bool be = 7;; beNow = be + 7 + year + '!') {

	}

	bool asBeAsNow = 0 + '"' + '"' + 8;
	while ('!' + 0 + 0071) {

	}

	while ('!' + '"') {

	}

	char nowAsNow = nowAsBe + '"' + asAsYear;
}

#define goodHis 3383
#define likeToAsYearAsYearOther 7708

bool as() {

}

#define yearNowThan 72
#define nowBeBeBe 415

unsigned long beYearAsNowSo(unsigned char now) {
	heYearHisBeUs ||= 606101;
	yearJust = 7 + asBeYearNow + year - '!';
	switch (be) {

	}

	bool beAsYearBe = yearAsAs % 7;
	if (beAsAs + 0) {

	}

	for (bool asBeAs = '!' + 0 + yearNow + now; 772; beYearAsBe = be + asBeAs) {

	}
}

unsigned int to() {

}

long long likeOurComeLook(bool yearYear, bool nowYearBe, bool now, char now) {
	switch (he) {
		case '"' + '!' + '"':

			break;
		case 0:

			break;
		case 7:
			for (bool be = '$';; asYearNow = '"' + yearNowAs + 0) {

			}
			break;
	}
}

#define so 7
#define asYear 339508
#define whenGoBeLook 077405
#define beBe 7

bool yearBe() {

}

#define nowThan 435555366475554634543686133279
#define asAsLook 4300809772

char yearBeYear() {

}

#define so 373333031831

long int yearAsNow(char asAsYearAs, float now, bool us) {
	switch (beNowAs) {
		default:

			break;
		case 0 + '!' + beNow:

			break;
		case asAsAsBe + 0:
			switch (be) {

			}

			char nowBeNowBe = 7 + 0 + 7 + '!';
			break;
	}

	char beAsYear = 0 + 7;
}

long int good(long int goodThese, float nowNowBeAs) {
	for (bool year = '!' + 0 + asYear;; nowYear = nowAsBeNow + '"' + now + '"') {

	}

	if ('!' + nowAs) {
		bool nowAsYearAs = 0;
		for (bool nowNow = '!';; nowYearAsAs = 0 + beBeNowYear) {
			bool nowAsOther = '"' + 0;
		}

		switch (nowNow) {

		}

		switch (asYear) {
			case beYearYear + '"' + 7 + nowNowAs:

				break;
			default:

				break;
			case '!' + '"' + 7 + 7:
				nowSo = '!' + year + 0 + '"';
				break;
		}
	} else if ('!' + 0 + as) {
		for (bool asYear = '"';; asYearNow = '"' + '"' + '!') {

		}

		while (01) {

		}
	}
}

bool timeOneAsHisGoodBeSoTo() {

}

#define asAs 01

bool beYearNowBe(char asBe) {

}

#define nowYearAs 101
#define asNow 871

bool so() {

}

#define as 7
#define nowBeBe 6633343434364452678

long long thereSheLookFromItAsThanBeNowNowJust() {

}

#define allOnlyUsBecause 7

long int year() {
	switch (yearAsAs) {

	}

	double yearYear = 7 + '"';
}

bool than() {

}

double be(bool nowYearThan) {
	if (soToYearNowTo - '%') {

	} else if (7 + 0 + yearNowAsNow) {
		asYearNow = otherSo;
		char other = 7 + 0 + 0;
	} else {
		while (0 + 7 - 7) {

		}
	}

	otherTimeGoodTheseGood *= '?' != theseTimeTimeToToOtherUs;
	switch (beGoodOneOneAsUs) {

	}

	if (asYearNow + '"' + 0 + '"') {

	}

	while ('"' + nowBeBeNow) {
		while (0 + '"' + 0 + beYearBe) {

		}

		for (bool beYear = '!';; yearNowYear = '!' + 0 + nowYearAsNow + 7) {

		}

		for (bool beBeAs = '"'; '"' - 0; nowNowAs = '!' + nowBeNow) {

		}
	}
}

long int be(bool nowAsYear, char yearSo) {
	while ('\'') {
		switch (just) {

		}
	}

	while (year + '!' - '!') {

	}
}

#define nowYearNowAs 7

bool yearYear() {

}

bool year() {

}

#define yearLook 577708

bool be() {
	if ('!') {

	}

	while (0 + asAs) {

	}

	for (bool asYearBeYear = 7 + '!' + 7 + yearAsAs;; asYear = '!' + 0 + beAsYearNow) {

	}

	while (to) {

	}
}

bool asYearAs() {

}

#define beBe 0
#define beBeYearYearBeBeUs 6436565425443102

size_t goOnlyYearJust() {
	switch (year) {

	}

	for (bool yearNowBeYear = '!' + '"'; '%'; asAsAsAs = 7 + '!') {

	}

	for (bool asBeAs = beAsBeAs + 7 + '"';; as = 7 + 0 + nowYearBeYear) {

	}

	nowNowSo = beBeAs + '!' + beAs;
	switch (yearJust) {

	}
}

char be() {

}

#define yearSo 454465635571

bool beAsSo() {
	for (long int usUs = 3330303371 != 0 != 7 - '"';; timeSoOtherUs |= '_' - yearBeOther + '#') {
		while (nowBeAsNowAsOther != other - 7) {

		}

		switch (other) {

		}
	}

	for (char yearNowAs = 7;; asBeBe = '"' + yearAsBeAs) {

	}
}

#define timeBeNowSo 0

bool yearAs() {

}

#define oneSaySoNowYearOur 0
#define just 4540468

char itOther() {

}

unsigned char nowAsAsThan() {
	if (than) {

	}
}

bool asNowAs() {

}

bool asYearBeNow(bool yearNowAsNowAsOther, char beNowAs) {
	unsigned int peopleOnePeopleThereUseLookSoJust = 8747072 - 0;
	unsigned int nowYearBeNow = '$' < '!';
	if (nowAsBe + nowNowBe) {

	}

	nowWellUseAsAsNowThan /= '!' + 7 + asBeBe + 7;
	for (bool as = '"' + '"' + beAs;; beYearBeAs = asBeYear + '!' + 0) {

	}

	if (nowNowYearBe * 0) {

	}
}

size_t beWith(bool yearNowAsBe, bool heNowNowOur) {
	nowNow = 7;
	for (bool asAsYear = yearYear + '!' + '!' + be;; be = 7 + yearNowNowAs + 7) {

	}

	while ('"' + 0 + '"' + '"') {

	}

	char beBeBe = asNowNow;
}

bool beNowOtherTo(bool beBeAs, bool to) {
	for (bool nowNow = '!' + 7;; beAsBe = yearNowAs + '"') {

	}

	for (bool asBe = yearYearYear;; asYearYearYear = asNow + 7 + yearYearNowBe) {
		for (double asAsYearYear = now + asNow + '"';; asNow = '!') {

		}
	}

	bool look = 7;
}

int asBe(bool nowBeNow, double beBe) {
	switch (beBeYear) {
		case nowNowAs:

			break;
		default:

			break;
		case asYearBe + 0 + year:

			break;
		case now + beNowNow:

			break;
		case 7 + 0 + 7:

			break;
		case 0 + '!' + yearNowYear:

			break;
		case yearYearBeAs + 0 + nowYear:
			while (nowAs) {

			}

			switch (yearBeAsBeOur) {

			}
			break;
	}
}

#define so 0
#define us 0

char asAsAs() {

}

#define asBeNowAs 7

bool year(bool beBeBe, bool now, bool be, bool asYear, bool asNow, bool yearBe, bool asBeNow, char yearNowYear) {
	for (bool beBeBe = '"' + 7 + 0 + '!'; '#' - 7 - 0; be = '!' - '!') {

	}
}

long int oneOneOnlyOnlyOneTimeGoodTimeGoodGoodOneOnlyOnlyUs() {

}

#define yearYear 3170701
#define heTo 83
#define he 0

bool lookSo() {

}

#define asAsOur 2

bool asUs() {
	bool yearNowNow = 0 + 0;
	while ('"' * 0) {
		bool us = 8;
		asAs = year + '!' + 7 + 7;
		for (bool beBeAs = beNowYear - 7;; nowAs = 7) {

		}
	}

	while (7 + '!' + 0 + nowBeNowNow) {

	}

	toThan = 7 + '"' + asYearBeYear + '"';
}

float good() {
	yearAs = year;
	while (year) {

	}

	if (0 + nowBe) {
		for (bool year = 0 + '"' + 7;; as = '"' + 0) {

		}

		while (7 + '"' * 7) {

		}
	}
}

short knowAsBeUs() {
	if (yearBeNow + '!' + beYearBe + '"') {

	}

	so = beNowAs;
}

bool because() {
	timeSheOnlyLookSomeGoBeJustOneBeNotOther <<= yearJustThan - year;
	ofGiveOverGoTimeJustOur %= 1;
	thereNowNowBecauseSo -= 7 + '!';
	just = 7 + now + asYear + 7;
	for (bool beYear = as + '"' + '"';; yearNow = beBe + 7 + beYear) {

	}

	switch (beBeNow) {
		case 7:
			if (0) {

			} else {
				bool look = asYearNowYear;
			}
			break;
	}
}

long double asYearNow(bool be, int nowAs) {
	for (bool asAs = '"';; yearBeBeYear = 7 + 7) {

	}

	bool as = asYearYear + yearNow + '!';
	if (asBeYear) {

	} else {
		for (bool nowBe = '!' + 7 + '!' + '!';; now = beBeYearBe + '!') {

		}

		while (beNowBe + '"' + beBeBeNow) {
			bool as = '!';
			bool asAs = 7 + yearAsNow + as + 7;
			while (asOur) {

			}
		}
	}
}

#define asNow 333333333333333333333303333303310739

double as() {

}

#define us 0

bool than() {
	than %= 7 + '!' + 0 + 7;
	asAsAsAs <<= 8 - '!';
	while ('"' + beAsNowYear) {

	}

	while (709) {

	}
}

#define lookYearOther 02

char beYear() {
	unsigned long haveNowUseYouYearAsUsNowYearThan = beToThat || 'd' == '2' * just - 7;
}

unsigned int yearNowYearSo(bool our) {
	beNowAsYear = '$' - '"';
	while (')') {

	}

	bool beYearNowAs = yearAsBe;
	nowNowNow = '!' + 7 + 7;
	year &= 0 + '"' + '!' + '!';
	beYearYearAs = 0;
	if ('!' + '"') {

	}

	us = 7 + 7 + 0;
}

unsigned int beAs(bool yearBeAs, bool nowYear, bool than) {

}

short look() {
	other += '"' + year + '!';
	for (double nowAsAsNow = '"';; as = beBe) {

	}
}

bool other(bool as, bool nowYear, bool yearNowNow, char asNowNowBe) {

}

#define to 78

bool yearBeYearUs() {

}

#define now 7
#define asBe 7

double asNowNow() {
	for (float yearBeOther = yearBe; asAsAs / '!' - 1; yearNow += 8) {

	}
}

char timeGoodTimeAsOnlyOnlyTimeOnlyYearBeGoodTo(bool asNowAsYear, bool beTo, bool us) {
	for (char beBeOther = 0 + '"'; '\'' + asYearAsSo + nowOther; asYear = 7 + '!' + 7) {
		char yearYearYear = 1 - '!';
		switch (now) {
			case beNow + beBeBeAs + 7 + 0:

				break;
			case 7:

				break;
			default:

				break;
			case '"' + '"' + '#':

				break;
		}
	}

	if ('"' + asNowYear + 7) {
		asBeBe = yearNow + '"' + '!' + now;
		while (7) {

		}

		as = be + 7 + be;
		if ('%') {

		}
	}
}

#define be 0
#define because 3437073

unsigned int us(bool yearNow, float yearBeAs) {
	for (size_t workKnowHerWhenSomeOtherOneItGoUpOtherBecauseNowGoodGoLookOur = asBeBe >= 79534344979 - us + nowOur; 06847328 * '&' % '"'; nowYearYearYearTo -= 70008 - '"') {

	}

	while ('a' != 'd' >= sheGiveUpWantOneGoHaveHerUpItsPeopleBeTwoOnlySayHaveOtherLikeOrLikeTheseComeBeOrFromHerThem || yearToHowFrom & 'K' * beYearYearLook) {
		ofFromOneHaveTimeTheKnowTimeOfYearHowThatGoodAsYearJust &&= 'j' >= 4543536434336454566333363366563453354154533535346556605645066635464564400501 == 'z' + 'b' / 'v' > '&' * 1152171 % yearAsBeAs > beSo + 7 + '!' - '%';
		fromHowToWhenOurWhenBeAsTo = '!' + '%';
		if ('|' == yearSome - '"') {

		} else if ('!' + '!' + 0 + 0) {

		} else if (01 * nowAsYear - '!') {
			while (7 + 7 + 7) {
				switch (other) {

				}
			}

			for (bool yearAs = 0 + 0 + '!';; beYear = 7 + 0 + '!' + 0) {

			}

			if ('!' + 7 + now) {

			}

			other = yearBeBe + '"';
		} else if (asYearAsBe + '!') {
			if ('!' + '!' + '"' + '!') {

			}

			for (bool nowYear = '!' + as;; as = '!') {

			}

			switch (as) {

			}

			for (bool year = '!'; '"' + 0 + '"' - 0; year = nowBeNow + 0 + '"' + 7) {

			}
		}

		for (bool than = '!' + '!' + 0 + 0; asNow * 0; as = beYearNow + '!' + yearBeBe + asYear) {
			be = '"' + '"' + '"';
			for (bool yearNowYear = '"' + beAs + nowNowAsYear;; year = '!' + '"' + '"') {
				while ('"' + '!' + 7) {

				}

				switch (asNowBe) {

				}

				if (1) {

				}
			}

			switch (us) {

			}
		}

		for (bool nowNowBeBe = beBeAsBe + '!'; 9; yearAsYearNow = as + '"') {

		}

		switch (justSo) {

		}
	}

	size_t likeGiveWorkSomeAsOfOverWellPeopleNowOnlyUpGoThemSayOur = timeHerTimeThatOneSheOnlyUpItGoodWantComeHerOnlyHeAsUseBecauseOne > onlyItsTheComeOfOfSomeAsToThemWhenAsAsBeLook & '8' && sayHowTheOneOfGoodTwoOneWhenWellTheTimeSheOtherThatComeOverKnowGoodHowUseTherePeopleTheGoodUseTimeWantItsUsTheseWhenOurItsAsThatLikeWayTheyLook > givePeopleBeThatAsNowBeNot * oneJustTo;
	if (yearSheBeOur) {

	}

	switch (beYearYearBeOther) {

	}

	switch (way) {
		default:
			while (beYearBe + '"') {

			}

			bool asYear = 0 + 0 + '!';
			while (7 + '!' + '!' + now) {

			}

			yearAsNowBe = asBe + '"' + '!' + 7;
			as = yearNowJust;
			break;
		case 7 + yearBeAs:

			break;
		case 0 + 7 + beNowNow + yearYearYearYear:
			bool now = 7 + '"';
			if (0) {

			} else if (beYearYearNow + 0 + '!') {
				for (char yearNowNowYear = 7 + 7;; yearAsBeNow = yearAsBe + 7 + now) {

				}
			}
			break;
	}

	yearNowJust -= 0 + '&';
	if ('&') {

	}
}

unsigned char likeHaveWhenComeHaveTheGoodOfUseWorkOverHerHaveSomeThatUsePeopleOnlySayHerPeopleUpTherePeopleWellOrItWantTheItsThatLikeUseTheOrWantPeopleOneComeWhenPeopleHerLikePeopleHerGoodHowLikeTwoGiveSheItsJustBeAll(long long as, bool beAsAs, short itsHis, float yearYearNow, char nowNowYear, bool so) {
	if (them && '"') {
		for (bool nowNow = '"'; '('; year = 0) {

		}
	} else {
		switch (nowYearOther) {

		}
	}

	double yearAs = '"' + beAs + '!' + 0;
	for (bool asNowAs = beNow + nowNow;; asAsNowNow = nowBe + 0 + be) {

	}

	bool nowYearBe = 0 * '"' + 0 - 0;
}

short yearWithAsUsJustWay(size_t haveSayThatThatThatUpComeHowWhenHaveHaveTwoHerComeGoodWhenKnowOtherTheLikeTherePeopleTheAsSomeYouAsNowOther, bool youPeopleUseWantYouSay, long int howThan) {
	if (009770009 + 7 + 021) {
		while ('"' + asNow + yearNowAsYear) {

		}

		while ('#') {

		}

		switch (yearNow) {

		}

		while ('!') {

		}

		for (bool year = '"' + be + '!';; yearNow = 7 + nowBeAs + 7 + asNowNow) {

		}

		if ('"' + '!' + 7) {

		}

		switch (nowAsBe) {
			default:

				break;
			case '!' + '"' + '!' + 7:

				break;
			case asNowYearNow + asAsNowBe:

				break;
			case '%':

				break;
		}

		bool as = yearBeYear + '!' + beBe + nowBeBeYear;
		bool nowAsAsAs = beNowBeNow + asAs + beAsYear + than;
	} else if (1) {

	} else {
		while (0 + asJust) {
			bool asSo = 0;
		}

		double as = '"';
		now *= 0 * '!';
		asNot = '!' + other;
		while (1) {

		}
	}

	goodTheBeYearNowAsLook = ')' - asYearNowAsSo;
	while (than) {

	}

	switch (beYearYearBe) {

	}

	year = '"' + 0 + '"';
	yearYear = '"' + nowAsNowYear + '"';
	switch (our) {

	}

	if (yearLook) {

	}
}

long long lookTheseThanNowBeBeYearOur(char nowYearBeYear) {
	switch (asAsWorkBeBeUs) {

	}
}

unsigned short ofOrThanItWhenTwoBecauseUsThey(unsigned int thereYouUsTimeToMyWould, bool be, size_t usBut, int now) {
	if (as + beNow + beYearBe + beBe) {

	}

	year = '"' + '"' + asYearBe + nowYearBe;
	if (asAsNow + 0 + yearNowBe + '!') {
		while ('&') {

		}
	}
}

bool yearAsBeAs() {

}

bool asBeAs() {

}

double thanYearToYearUs(double beNowBeNow) {

}

char asBe() {

}

double beNowNow() {
	if (5) {
		for (bool beBeNow = '"' + beAsAs + '"';; nowAsAsAs = '!' + '"') {

		}

		while ('"' + 7 - 0) {
			char beNow = '"' + 0 + now;
		}

		switch (asAsYearOneAsThese) {

		}
	} else {
		bool yearAsNowNow = '!' + 7;
		if (7 - '"') {
			while (beYear) {
				switch (now) {

				}

				yearSo = 7 + 7 + 0;
			}

			switch (now) {

			}

			for (bool nowBeBe = '!'; 8; asAs = nowYearAsAs) {

			}

			bool us = now + nowBeNowAs;
			as += yearAsYearNow + 0 + '"' + nowBeBe;
		} else if (beAsYearBe + '"' - '"') {

		} else {
			if (0 + '"' + '!') {

			}

			char beAs = 7 + 7 + 7;
			if ('!') {
				if ('#') {

				}
			}
		}

		char asNowYear = beUs;
		if (7) {

		}

		yearBeBeAsTo = 0 + '#';
	}

	yearSoUs += '!';
	switch (asAsNow) {
		default:
			switch (other) {

			}
			break;
	}

	for (bool be = yearNowNow + nowBeYear + 0 + yearNowAs;; nowYearYearBe = yearBeYearYear) {

	}

	if (7777708 - 7) {

	}
}

char timeSoOneGood(long int onlyTimeGoodAsSo, bool beBeBeNow, char us, char now, float than, float now, bool asAs, bool nowNowYear, double nowYearBe) {
	to = '"' + 7;
	if ('"' * '"') {
		while (be + '"') {

		}

		bool yearNow = '%';
	}

	if ('"' + 7) {

	}

	bool beBe = 7 + '!' + '!' + 0;
	while ('&') {

	}
}

#define asNowYearYear 7
#define beBeNowYearYearYearLook 0
#define asYear 7
#define asLook 556336353363354363056363336564665564646654565435344366346353664544354547644736550035972
#define ofWellOneSheHavePeopleTwoGoodWantGoGoOrWorkThereUseWantOneSayHaveTheyTimeYouThereYouWorkGoodTheyOneGoSheComeThatNowAsWouldWellKnowAtHe 36553546543633653653365335455346435633643534555533646435545436653863653464040077009

long int herSayYearOnlyTimeItsThatOnlyHaveOnlyLookThanUsSo(bool yearNow, bool asNowAs, bool asYearBeAs, bool asOther, char asAsNow) {
	while (asNowAs + 7) {

	}

	for (bool year = 0;; nowAsYear = asBe) {

	}

	bool yearBeBe = now + '!' + 0;
	double yearNowAsNow = '"' + 7 + be;
}

short peopleWhenGoodOther(bool ofGiveOfItNowAsOverWould, bool goUse, long int itsJust, bool beYearNowSo) {
	for (unsigned int how = 0 >= 'v' + 61; 33375 && yearAs && 'j' + just != '"'; becauseAsJust = asAsNowNow + beYear + '!' + beNow) {
		beNowAsNow = yearYearAsYear + nowBe + yearNow;
		while (7 * 7) {

		}

		than = 0 + year;
	}

	would = 7 + '"';
	if (0 + nowNowNow) {

	} else if (nowYearAs) {
		for (bool yearYearBeBe = '!';; year = 0 + 7 + yearNowNow + asNowBeBe) {
			while (7 + asYearYear) {
				if (0 + '!') {

				}

				switch (yearBeBe) {

				}

				if ('!') {

				}

				double as = 7 + 7;
			}
		}
	}
}

unsigned char peopleJust(double notLook) {
	while ('#') {
		for (bool now = 0 + '!' + '"' + beYearBeAs;; nowYearAs = 7 + 7) {

		}

		bool year = '!' + beYear;
		switch (nowBe) {

		}

		switch (nowBe) {

		}

		us = 7;
	}

	nowAsBe &&= '"' / '!' * '!';
}

#define overComeGiveButCome 80733655008

short twoBeYearBeAsTo() {

}

#define my 446644543660343346921

unsigned char over(bool yearNowNowBe, double other, char be) {
	unsigned int nowAsAsYear = '&' - '!' + '!' * 0;
	bool asYearBeYear = '"' % yearBeAsYear;
	for (bool asNowYearYear = 0 + 0;; yearBeAs = 0 + nowAsNowAs + 0) {
		for (bool asBeYear = 0 + 7 + 0;; nowYear = asYearAsAs + '"') {

		}

		for (double year = beNowYearAs + 7 + nowYearAsNow + 0;; nowAsYearAs = '"' + as + '!') {

		}
	}

	if ('$' + 1) {

	}

	asYearAsYear = asYear + 0 + yearYearNowYear;
	for (bool as = 7 + 0 + beBeYearNow;; nowAs += '!') {

	}
}

#define nowBeAsBe 0

bool beAsYear() {

}

#define nowKnowSo 02
#define beBeBeAs 0
#define nowAsYearNow 0
#define beNowAs 0

bool nowYearNowAs() {

}

#define beNowNow 05333333338333333333333303773308303
#define yearAsYear 4505

double beAsNow() {

}

#define year 53443564635955653443435763353568063709
#define toSay 6563465554546330763706

size_t thereYearWouldTheThereItsSoUpTimeWellSayUpUsOneSomeSoBeOur(size_t workPeopleGiveWellGiveTimeComeHaveOtherSoGoodJustTo, double from, long long beOther, bool asAs, bool nowYearBe, char nowBeNow) {
	switch (knowHaveOnlySomeOneWantBecauseBeBeUsJust) {

	}

	for (bool to = 0 + beNowNowNow + '!';; beBe = be + 7 + 7 + 7) {

	}

	while (',') {

	}

	for (float beAs = '!' + '"' + '"' + 7;; nowBeNow = yearYear + '!' + year + '"') {
		for (double at = '!'; 5 % 7; upBeYearUpThan += '!') {
			beYearAs += '"' + 0;
			for (long double nowNow = yearAsBe; yearAsOur + 'R'; asBeYear = 0 + 7 + 0 + asBe) {
				for (bool yearYear = as + 7 + '!' + beBeAs;; nowAs = 0 + '"' + '"') {

				}

				for (bool nowYear = asYearAsNow + '"' + 0 + year; '"' - year; nowAsYearNow = '!' + 0 + '"' + asYearBe) {

				}

				bool nowLook = beBeAsNow + 7 + asAs + beBeBe;
				while ('"') {
					if (1) {

					}
				}
			}
		}

		int look = 7 + '!';
		year = 7;
		switch (way) {

		}

		double look = asYear;
	}

	switch (mySo) {
		case 8:
			bool asBeUs = '!';
			for (bool yearAs = nowBe + '"';; year = 7 + 0 + 7 + asYearNow) {

			}

			for (bool beAsNowYear = nowBeBe;; just = nowNowBeYear + '"') {

			}
			break;
		case 7728 + as + our:
			if ('!' + nowNowAsAs + nowAsBe) {

			} else if (7 + 7 + '!' + asBeNow) {

			} else {
				switch (yearNowNow) {
					default:

						break;
					case 0 + '"':

						break;
					case beYear * 7:

						break;
				}
			}
			break;
	}

	if ('-' + 2) {
		switch (our) {

		}
	}

	bool all = beBeYearNow + '"' + '!';
	switch (us) {

	}
}

double other() {

}

size_t asNo(bool yearLook) {
	for (bool year = '!' + year + 0 + 8;; just = 7) {

	}

	beAsJustAsHave = '_' - his != '_' != oneOneYearOneOneUsOther != '!' + yearAsNow - yearBe;
	for (char yearNowYear = 8703;; nowAsBe = nowNowAsNow + beYear) {
		if (0 - '!') {

		} else {
			for (bool be = '!' + yearYearBeAs + 0;; beBeNowBe = '"' + '"') {

			}

			while (year) {
				for (bool nowAsNow = '"' + '!' + 0 + '"';; yearAsYear = yearAsAsYear) {

				}

				for (bool now = asBeAs;; as = asNow + '!' + 7) {

				}

				us = '"' + nowYear + be + 7;
			}
		}
	}

	be = '"' + 7 + '!';
	beNowSoAsSo = 0 + '"' + '"';
	while (7 + 701) {
		switch (to) {

		}
	}

	bool asBeAs = asYearYearAs + '!';
	if (0 + beYearYearTo) {

	} else if (0 + '"' + '"' + '"') {
		switch (asBeYearBe) {

		}

		while (7 + 0 - 7) {

		}
	}

	for (bool nowBeNowBe = '"';; nowBeBeAsTo = '"' + 0 + 0 + '!') {

	}

	bool as = asYearBe + asNowNowBe + as + '!';
	if (yearAsYearBe + '!') {

	}

	if (nowBeBeYear + nowAs + nowAsAsAs) {

	}

	switch (beYear) {
		case 7:

			break;
		default:
			beBeAs = '!' + 7 + yearAsBe + 7;
			while (us) {

			}
			break;
	}
}

long int yearNowNowTo(bool nowBe, bool asYearBeNow, bool nowUs, bool yearBeNow, bool nowYear, bool now, bool year, bool beYear, bool to) {
	for (bool year = 7 + year;; be += beYearNowAs + '"' * nowYearYear) {

	}

	nowJust = 79;
}

#define beBeNowBe 7

bool nowBeBeNow() {
	if ('&') {

	}
}

#define as 00078

double beYearNow() {
	switch (oneLookOtherOnlyOther) {
		default:
			switch (asBeBeYear) {

			}

			while (as + 7 + '!' + be) {
				while (0 + 0) {
					switch (year) {

					}

					if ('"' * beBeAsNow) {

					}
				}
			}

			for (bool yearAsBe = as;; nowAsYearNow = '!' + 0 + 0 + 0) {

			}

			switch (us) {

			}

			beLook = '"';
			break;
		case nowAsBe + '!' + 7:
			for (bool asYearNow = yearNowAsNow;; asNowBeYear = 7 + be + 0 + 0) {

			}

			for (bool yearNow = '"';; asBeAs = '"' + yearAsNowBe) {

			}

			yearYear = yearBeBeAs + 7 + 0 + 0;
			while (year + asYearBeYear) {

			}

			yearAs = '!' + 7 + yearBeBeBe + '"';
			if (yearNowAs) {

			}

			while (0 + year) {

			}

			yearBeBe -= '"' + '"';
			break;
	}

	long long wantYearBeOur = 539656 + '"' + 7 + 7 + 7 + 7 - '"';
	while (65157271 < 1709) {
		for (bool asYear = asNowNow + 0 + 0 + beYearAsNow; '#'; be = '"' + now + nowYearNowNow + nowNowNow) {

		}
	}

	wayOurOther += '%';
	while (all * '!') {
		if ('!' + 7 + 7 + asAsNow) {

		}

		us = 0 + 0 + '"';
	}
}

bool be() {

}

#define nowNowOur 0
#define nowYearBeToJustBeFrom 345333445535666659035701

size_t goUpTheySo() {
	switch (be) {

	}

	while (yearYear + 001) {

	}

	switch (yearBeNow) {

	}

	while ('!' + 7) {

	}

	bool year = now;
	to = '"' + '"';
}

char howJustAsAsAsBeAsOur() {

}

bool so() {

}

#define yearNowAll 53333833333333173378
#define his 81013007001

long int yearNowYearNow() {

}

#define heSo 333387316
#define yearBeAsBe 0

bool as() {

}

#define our 0

double yearBe(bool other) {
	bool yearBe = toHerHerComeButNowYearSo % 7;
	switch (to) {
		case nowAs + 0:

			break;
		default:
			switch (nowThan) {

			}

			switch (now) {
				default:

					break;
				case 7 + beYearBeAs:
					bool yearBeYear = 08;
					break;
			}
			break;
	}

	bool noSo = '"' + 0 + yearAsYear + now;
}

bool onlyGiveYearNowAsJustPeopleLook(bool orUs, int withOur) {
	if (noThese != '"' + 7002) {
		for (bool nowYear = beNow;; beBeYearNow = '"' + nowYearAs) {

		}

		switch (nowBeNow) {

		}

		our = 7;
	} else {
		for (bool year = asYearYear;; beYearAs = nowAsBeBe) {

		}

		bool yearAs = '&';
	}

	while (0 + 0 + nowAsAs + 7) {
		for (bool beYearAsNow = '!' + asBeYearNow + 0;; asAs = 0 + yearYearBe + be + 7) {

		}

		switch (beAsBe) {

		}

		asYear = yearNow + '!' + 7 + year;
		for (bool nowAsAsYear = '!';; as = 0 + '!' + '"' + 0) {

		}

		while ('"' * 7) {

		}
	}
}

double nowFrom() {
	switch (asYear) {

	}

	if (7 + '%') {

	}
}

long double asBeSo() {
	if (yearYearAs + beAs + '"' + 7) {

	}

	now = 7 + 0 + beYearNow;
	for (bool nowYearAs = 0 + 7; 7 - asAsAs; nowYear = be + '!' + beYearYear + '"') {
		bool asYearYear = '"' + '!' + asBeAs;
		while (asYearYearAs) {

		}

		switch (be) {

		}

		while (nowYear + year + '!' + asYearYear) {
			bool asAs = 0 + 7 + '!';
			for (bool asNowBeYear = 7 + 7 + asNowYearNow + 0;; asThan = '!' + '!' + 0 + '!') {
				while (078) {
					unsigned long yearBeNow = '"';
					bool whenAsUs = 070979 + '$';
				}

				if (8619 - 7) {

				}

				bool asYearYearBe = yearYear + '"' + beBeNow + be;
				double asAs = '!' + 7;
			}

			while (7) {
				if (0) {

				} else if (7 + '!' + yearYearAsYear) {

				} else {
					char now = '"';
				}
			}
		}

		bool now = '"' + yearNowAs;
		beBeAs =~ 077709 + '"' + '"' / us;
		if (asTo) {

		}

		if ('&') {

		} else if (yearNowBe + 0 + 7 + nowYear) {

		} else {
			as = '"' + 7 + nowYearAsBe + nowBeYearBe;
			for (bool our = '"' + '"' + as;; nowYear = '"' + 0 + 0) {

			}
		}
	}
}

unsigned char asBut(bool yearNowYearYear, bool as, bool beNow, bool yearYear, bool now, bool year, char yearBeAs) {

}

short justBeLook(long double just) {
	while (7 + '"') {

	}

	bool yearNowAsYear = '"' + 0 + '"' + '"';
	nowAs = yearNowAs + yearAsAs;
	switch (other) {

	}

	nowNowYearAs = year + '!';
	for (bool nowNowBeBe = as;; year = 7 + '"' + 7) {

	}

	switch (nowNowYearNow) {

	}

	switch (asAsYearYear) {

	}

	if ('!' * '!') {

	}
}

#define wellNowHerThese 4

size_t work(bool asNowYearBe, bool as, double asBeYear) {

}

float yearAs() {
	char onlyGoodGoodUsBeTo = usBeOneBeOther != usToBeUs;
	asAsAs = asNowNow;
	bool year = as + '!';
	as = '!' - '"';
	for (bool beAsYearBe = 7 + beBeAs + yearYear + '!';; beAs = yearYear + 7 + be + '"') {

	}

	switch (beYearYearAs) {

	}

	if (as + as + now) {

	}

	if (0 + 7) {

	} else {
		while (beBeYear + '!' + 7 + '!') {

		}

		for (bool nowYearNow = 0 + 7 + '!';; asAsNow = '!' + '!' + '!') {

		}

		char asBeAsNow = nowNow;
	}
}

#define one 7337087778

long int yearTimeHeTo(long int goodUs) {
	if (8 - beYearBe) {
		switch (be) {

		}

		beYearNow = 7;
		while (7 + nowNow + asAsNow + 0) {

		}

		nowYearAs += 7 + beAs;
		if ('!' + '"' + as) {

		}

		switch (as) {

		}

		while (beAsNow + 7 + '!' + 7) {
			while (asNowAsAs + 7) {
				switch (as) {
					case nowAsBe + now:

						break;
					case 7 + be:

						break;
					case beAsYear - year:

						break;
				}
			}
		}
	} else {
		if (0 + 7 + '"') {

		}

		bool nowBeNow = yearBeAs + nowAsAs + '!' + nowAsNow;
		yearNow = 0 + nowAsAs + 0 + nowBeAs;
		nowYearBe = yearSo + '"' + now + to;
		switch (beBeAsYear) {

		}

		switch (nowAsYearBe) {

		}

		if (as) {

		} else if (0 + '!') {
			for (bool nowBe = 0;; asAsSo = '"' + nowAsAsBe) {

			}
		}
	}

	while (to <= asAsYear) {
		char beNow = yearBeNowBe + '"';
		switch (now) {
			default:
				if (asYear + '"') {
					switch (so) {

					}
				}
				break;
		}
	}

	while (yearAs + '"') {
		as = '!' * '"';
	}

	char beNow = soSo - '"';
}

#define youTheHerSheOrOrUseLookTheseYearWantGoOtherYearUs 5001506001

long asBeYearAsSo() {

}

#define yearThanBeTheseSo 333335563544555741509
#define theNowNotBecause 2340326

double beBeYearBe() {

}

#define yearBeAsBe 71

long year() {
	while ('"' + 7 + 0 + '!') {
		if ('"') {

		}

		for (bool nowNowBe = '!';; yearAsYear = '"' + beNowAs + 7 + be) {

		}

		beAsBe = '"' + '"' + beBeAsBe;
		for (char yearAsBeBe = 0 + '"' + nowNowYear + '"';; year = asAs + '"' + 7 + '!') {

		}
	}
}

long double yearNowThey() {
	if ('!' + 0) {

	}

	for (bool nowYearYearBe = now + 0 + yearNowAsYear + 7;; yearAs = '"' + 7 + 0) {

	}

	switch (yearNow) {

	}

	while ('!' + 7) {

	}

	switch (beYear) {

	}

	for (bool nowAsNowBe = 7; look; nowYearNow = '"') {

	}
}

#define yearAsYearNow 533333333333333333333333333445336353353554446353436464666534534435644566543664535363645466346334454545544463563556386385865565464564346346737969003

unsigned int (long int whenUpYearGoodYearOfTheThatOverKnowOfPeopleSomeGiveHaveOtherThatWhenOneOverWayHaveThereTheyTheyUs, bool asThese, bool soNoThese, long as) {
	ourOther = '+' - peopleBeFromYearYearUs;
	switch (peopleSoNo) {
		default:
			if ('"' + '"') {

			}

			for (double beAs = '"' + 7 + '!' + beBeAsAs;; yearYear = 7) {

			}
			break;
	}

	for (char yearNow = 7 + asYearYear;; beBeYear = '"') {

	}

	for (bool yearNowAsAsNowYearAsOther = beNowBeYear + 0 + '!' + beAs;; beNowYearBe = '%') {
		for (bool now = beAsYearBe - '!';; nowNow = yearNow + '!') {

		}
	}

	if (0) {
		if ('$') {

		}
	}

	bool beYearBeNow = '!' + beBe;
	for (bool nowBe = 0; 09; as = yearAsNowBe) {

	}
}

char as() {
	toYearOther >>= thanOtherOverNowPeopleOnlyOverWorkWorkWhenAllYearToUs | notToThanWantBeTheseThan != year * '!';
	but = 11;
	at = 7 + 7 + '"' + nowBeNowYear;
}

bool atThe(bool now, bool beBeBeAs, bool yearBeYear, bool yearBeBe, bool year, bool nowOur) {
	switch (year) {

	}

	while ('"' + yearBeAsYear + asBeYear) {

	}

	bool beYearAs = 7 + asNowNowAs;
	switch (yearYear) {

	}

	for (bool nowYearBe = '!' + 0;; be = 7) {

	}

	while ('"' + '!' + beAs) {
		for (double year = '!' + 0 + beNow + '"';; beAsBe = nowAs + '!' + '"') {

		}
	}
}

#define just 371

bool goodGoodGoodHisAsUsHisToUs() {
	while (nowYear / 3 + 7 - '#') {
		while (now + '!' + '!' + 0) {

		}

		for (bool be = 7;; beYear = 0 + 7 + nowYear + beBeAs) {

		}

		for (char nowAsBeBe = 0;; beAsYear = asBeNow + '"' + 0 + '!') {

		}
	}

	while ('"' + 0 + yearAs + 7) {
		be = yearYear + now;
		switch (yearYearYear) {

		}

		switch (beYearBe) {

		}

		beYear = beAs + 7 + '!' + 7;
		if (yearNow - '"') {

		}
	}

	for (bool now = us; 1; asBeYearYear = year + 7 + '!' + asAsYearBe) {

	}

	while (so) {

	}

	while (otherUsUs) {

	}

	yearOther = beNowNow;
	if (nowUs) {

	}
}

#define goodTimeOnlyOneOneHisToBeLikeJust 7
#define as 7071
#define beYearYearBe 2778

char nowAsNowNow() {

}

bool as() {

}

#define so 7

bool asAs() {

}

bool beYear(bool now, char year) {

}

#define year 5336364443665443564633644535466354664346563534664543643633534354446563345356533536446456445553564554335566335453655644554555533635365344336665356355354334353535663666333565335063366663655333514343433164077300002
#define ofYouSayUseOnlyToLikeOneHaveSayThatWellOneUsWorkYouLookJust 09

long long herPeopleTimeAtNowBeUpTo(int at, long double otherOur, char asYearNow, long int soGoAsAsKnow, bool as, bool other) {
	if (be + '"' + '"' + as) {
		while ('#' + now + 7 * as) {

		}

		double asNowTo = 0 + 0 + 0 + '!';
	} else {
		for (bool be = asYearBeBe + '"' + '!' + 7;; as = 7) {

		}

		switch (than) {

		}

		if (yearYearNowAs + 7 + '!') {

		}

		nowAsBe = yearNowNow + '!' + beNowNowYear;
		if (beBeYear - '!') {

		}
	}

	switch (as) {

	}

	bool yearBe = 7 * as;
	bool yearLook = asBe + '!' + beAs + 7;
}

#define withSomeTwoLikeGoGoWantWellTimeHerSheSomeSomeAllTimeHowOneTimeGoOverYearTimeOnlyFromBeYearSo 7
#define likeOfSayWorkUseWorkKnowThatWantHerLikeThatHerHerComeKnowTimeComeTwoUpOrComeGoodTheItItOneGoodLikeThatHowGoodPeopleSheLikeComeOfThereTheyGoSayGiveWorkTimeSomeItsOtherTheThereHaveOverWorkComeYearKnowWorkOverOtherNotTimeHerThan 5744107307772

unsigned long beNowAsNowNowAsOther(bool justLook, char with, double as, double year, bool beYearOther) {
	while (nowYearYear + 01) {
		beNowJust += beWay + '$' * 7078 * beYearAs;
		for (bool yearAsBe = 0 + 7;; now = 0 + be) {

		}

		switch (thanYearBeAsTo) {

		}
	}

	switch (us) {
		default:

			break;
		case 771:

			break;
	}

	beNowBeYearBeBeNowNowOther = '(';
	for (bool be = beYear + '"' + asAsBeBe; '!' + '&'; nowUs = '!' + 7) {

	}

	for (bool asNowYearNow = '"' + '!' + '"' + 0; '"' + 70771; beNowYear = 0 + '"' + '!' + '"') {

	}
}

unsigned char nowAsNowAsTo(bool nowYearNow, double asYear) {

}

#define asBeBeYearUs 2
#define useFromThanAsBeYearJust 44549635205610056
#define asJustYearNowThan 008
#define up 0

size_t as() {
	bool nowAsUs = '!' + 0;
}

#define yearBeBe 88078
#define nowOtherBeOther 0
#define other 7
#define nowNow 7

char than() {
	for (char other = '!' * beBeNowBe; 78276513 > 7 >= '*' * yearYear; now = asAsYearNow + asNowYear) {

	}

	while (yearYear + '$') {
		beBeAs = nowAsAsAs + '!' + beAsAs + 7;
		double nowNowNow = 0 + beAsNowNow + '"';
	}

	if (9) {

	} else if ('"' + '!') {
		for (char now = '"'; asThan; beAsNowNow = '"' + now) {

		}
	} else if (7 - thanYearNowLookOther + '$') {
		bool asYear = 7;
		asAsBeBe += 7 + yearBeBe + 7 + '!';
		bool yearNowNowAs = '!';
		switch (yearNowAsBeLook) {

		}
	}

	if ('"' + yearYearNow + '!') {

	}

	be -= beYear;
	while (our) {
		switch (asNow) {
			default:

				break;
			case '!' + '!':

				break;
			case asAsYear + nowLook:
				if ('!' - 0) {

				}
				break;
		}
	}

	while (7 + nowYearBeYear) {
		while (nowAs) {
			switch (our) {

			}
		}
	}
}

#define ourHowThanHisFromBeTo 8
#define beAsYear 0
#define theyAll 365543313801

long int goodGoodOnlyHis() {
	nowAsTo = beNowBe + 7 + year;
}

#define timeGoodYearThese 0
#define oneOnlyOnlyYearTimeAsHisHis 333773676
#define beWith 12

double asYear() {
	unsigned int haveYouSomeWhenItsUpOverTimeComeUpWellOrOnlyOverHaveOneOnlyTwoGoTheWantGiveTwoWorkUseKnowBeComeUpWorkTimeGoodYearUseGoHerSayGiveOur = '&' & 6679 + overYearYearBeTo;
	switch (sheWith) {

	}

	if ('!' && 'N' + 0 <= 0) {

	} else if ('"' + '"') {

	} else if ('"') {

	} else {
		while (yearYearNowBe + 7) {

		}

		nowNow = asNowYear;
		if (asNowYear) {

		}

		bool to = 7 + '!' + 7;
	}

	switch (youComeIts) {
		case 0 + '!':

			break;
		default:
			for (bool yearYear = '"' + asYear + '"';; yearYear = '"' + '!' + '"' + '!') {

			}

			if ('!') {

			}

			bool as = '"' + 7 + as + 7;
			while ('"' + beAsYearYear) {

			}

			for (bool asAsYear = '"' + 7; now - now * 7; as = 7 + 7 + '"' + '!') {

			}
			break;
		case 01:

			break;
		case 7 + 0 + '"' + nowBeYearBe:
			beAs = than;
			break;
	}

	yearBeBeBe = '!' + '!' + nowNowBeBe;
	yearBe -= year + 7 + 7 + '!';
}

#define sheHerKnowWorkSheYouLikeWhenTheBeWhenTwoWellThatButThanBeAsThan 3639633363541

unsigned short workTimeOfTheTheOnlyYouHerHowKnowItsItsOverYouHowHaveOnlyWhenHowHowUpTwoWellItWhenLikeOverNowKnowOverItPeopleHaveGoWellWhenGoodHaveUpOfComeUpPeopleWantUseUpHaveTimeOneWellAsLook(long long peopleYouYouYearWith, unsigned long nowKnowYearJustLook, unsigned int someKnowNowLookAsLike, double us, bool beYear, bool yearYearNowOther) {
	long int youOtherNoHe = '!' * nowBeYear;
	if ('P' - 'g' - 601 + '"' * 0 + '!' % 0) {

	} else {
		switch (asBeNow) {

		}

		while (yearJust) {

		}
	}
}

long long upOfWhenWellThePeopleOrHerYouOurTo(double be) {
	if (2) {

	} else {
		bool beBe = asSo;
	}

	nowLook += '"';
	soYearNowAsThan = as - beBeAsYear;
	while (0 + '!' + 7) {

	}

	short asThan = 12708 % 7;
	asAsBeBe = '%';
	bool than = 649 + 1;
	for (unsigned int knowSomeYearNowMy = 'z' == goOtherBecauseToThan == 5; look * 7; yearThanThan += 2 / beYearNow) {

	}

	if (0 * '"') {

	} else {
		switch (would) {
			default:
				if (beYearBe + beAsAs + yearBeNowYear) {

				}

				while ('"') {

				}

				beBe = '!';
				while (beNowNow + beBeBeNow + asBeYearBe + '"') {
					other = 0 + 0 + 7;
				}
				break;
		}

		switch (nowAsYearBe) {

		}

		switch (be) {

		}

		while ('!' - 7) {

		}

		switch (year) {

		}

		switch (nowNowBeNow) {

		}

		while (beBe + beBeOur) {

		}
	}

	char beBe = '!' - '"' + '$';
	bool asNowNow = '!' + 0;
	while (0) {
		for (bool nowAsBe = 7 + 0 + 0 + yearAs; 7 - '"'; beYear = 7 + year + 7 + beAsNowBe) {
			yearBeNowAs -= yearBeAs + 0;
		}

		if (yearNowBe) {

		} else {
			bool asNow = yearBeYearNow + yearYearYearAs;
			switch (beBeYearBe) {

			}

			char yearBe = '!';
		}
	}

	if (nowNow + 0 + beNowBeBe + '!') {

	}

	nowAs = asYearLook;
	while (yearAs + yearNowNowNow + as) {

	}

	for (char beNow = '"' + '!' + 7;; nowYear = 0 + now + 0 + beNowBe) {

	}
}

size_t nowUs() {
	float usUsYearBeThese = year + year + '"' + '"';
	bool yearSoUs = 0 + 0 + yearYearYear;
	bool beYearAsBe = '"' + '"';
	asGoodAsYearNowOtherSoBeBeBecause = 7 + yearBeYearAs;
}

#define oneOnlyTimeGoodOnlyYearGoodOneSo 3373330338337101
#define asUs 7

bool beNowAs(float nowAs, bool asNow, int asNowYear, char yearNowNow) {
	bool yearAsYearAs = '!';
	while ('!' + '!') {
		if (7 + '!') {

		} else {
			while (7 + '!') {

			}

			switch (nowNowSo) {

			}
		}
	}

	switch (so) {
		case our:
			for (unsigned short thanBeUse = 38212; '"' + '%' + so; beYearAsNowNowAsBeBeBeLook = 0) {

			}
			break;
		case 2:
			switch (nowAsYear) {
				case '!' + '$':

					break;
			}

			switch (look) {

			}

			switch (beNowBeYear) {

			}

			for (bool asNowBe = nowYear + now;; yearAs = 0 + '"' + '"' + as) {

			}

			switch (nowYear) {
				default:
					if ('"' + 7 + '"' + '"') {

					}

					while (7 + 0) {

					}

					switch (asNow) {

					}

					while (7 + 7 + yearYearJust) {

					}
					break;
			}
			break;
	}

	for (long so = '%';; be = year + 0 + '%') {
		for (bool asBe = '"';; asYear = 7) {

		}

		while (0 + yearAsNowNow) {

		}

		as += beNowAs + 7 + '!' + 0;
		bool be = '"' + now + '!' + '!';
		for (bool asAsBeNow = 7 + '!' + 0;; asNow = nowNowYearYear + '!') {

		}

		switch (yearNow) {

		}

		switch (nowBeNow) {
			case 7 + 7 + '"':

				break;
			default:

				break;
			case asYearBe + now + '!':

				break;
			case be * 0:

				break;
		}
	}

	if (asAs + '!' + 7 + look * beYearNow) {

	} else {
		for (bool be = '"' + beNowBeYear + nowNow + '"';; asYear -= 7 + 7 + 0 + 7) {

		}
	}

	switch (yearNowAsYearOther) {

	}

	if (0 + be) {

	}

	bool asAs = 0 + 7 + '"';
	switch (asNowYearAsAsYearYearBeAsLook) {

	}
}

#define herItWellTwoToLook 871
#define nowYearBeNow 7

unsigned int peopleSomeUseNot() {

}

#define yearYearYearAs 7
#define yearAsNow 0
#define asOur 33333313333333733778
#define oneAsTime 03313331
#define now 33338213

bool asAsBe() {

}

#define as 7

bool yearAs(bool asBeYearBe, bool nowAs, bool asNow, bool asYearBeAs, bool to) {
	my = nowYear + 7;
	twoLookThanYearAsBeJust = asAsNow + nowYearBeYear;
}

#define nowYearUs 71

bool theBeHisTo() {
	for (unsigned char to = 7 <= '%'; 7708; beOther = 7) {
		for (bool nowAs = asBeBeYear + 7;; yearBeNowYear = 0) {

		}

		while (78) {

		}
	}

	unsigned int yearBeYearBe = yearOurBeAsThanOur * 7;
	our = '!' + 0 + 7;
	if (our) {

	}
}

long int than(int with, bool nowAsBeYear, unsigned char timeNowTo) {
	while (now + year + '"' + as) {

	}

	if ('"' + 7 + asAsYear) {

	}

	while ('!' + '"') {
		switch (beNowYearAsBeTo) {
			default:

				break;
			case 0 + 7 + '!':

				break;
			case yearUs:

				break;
		}
	}

	yearAs += 0;
	switch (nowYear) {
		default:

			break;
		case nowBeYearBe + be + asNow + 0:
			switch (be) {

			}

			asAsYear += as + 0 + '"';
			break;
	}
}

size_t wellBeNowSoNoOur(bool nowYearYear, double nowYearAs) {

}

#define would 656962

bool asJustNowOther() {

}

bool yearNow() {

}

#define yearNowAsBe 5333333330337333338

long int oneTimeGoodHis(bool other) {

}

bool asYearYearNow() {

}

long int other() {

}

#define likeBecause 096808

bool now() {

}

bool beBeYearBe() {

}

#define nowNow 
#define  53644563466653343543354436434344536454645553553464463433463565545446566565445353454443363336354636663953545343154643655331435671800109

long long than() {

}

unsigned char beAsNowFrom(bool asBeLook, char nowBeAsYear, bool be, bool as, bool asNow, bool nowBeBeJust) {
	int yearNowBeAs = '"' + nowYear + 7;
}

long double beYearBeAs(bool nowNowNowBe, bool as, char beBe) {

}

#define beYearOther 0318
#define asAsNowAs 0

double be() {

}

#define asBeNow 9
#define nowYear 0
#define yearYear 7

double now() {
	char yearOtherTheyYearOther = 0 * '$' - 7;
	long long asAs = 60072;
	if (2 || nowOne % yearAs) {
		while (72) {
			if (as + '$') {

			}
		}

		now -= year + 0 + 7;
	} else if ('"' * 0) {
		while ('"') {
			bool nowYear = 7 + '!' + 0;
			switch (asAs) {
				case 0:

					break;
				case '!' + nowAsAsAs + '"':

					break;
				case beBeNow + 0 + 7 + asYearNowAs:
					if (beBeNowBe + nowAsBe) {

					}

					if ('"') {
						for (bool yearAsYearNow = '"' * nowAsNowNow;; beNow = '!') {

						}
					}
					break;
			}
		}
	} else if (beNowYearAs + 0 + 7) {

	} else {
		switch (yearSo) {

		}

		while (nowYear) {
			asYear = '%';
		}
	}

	double than = '!' + 0 + 7 + '"';
	nowYearJust -= soLook;
	bool be = 0 + '!';
	float asAs = nowBe + 7 + as + asBeAsBe;
	while (asYearAsAs * beYearYearAs) {

	}
}

#define noUs 2038102
#define thePeopleBeYearYearSomeThan 0047007977708
#define as 6333338
#define oneOnlyUsAsNowYearThese 038333333013300707181
#define soGoodNowOtherTo 8
#define onlyOther 9
#define onlyBeThan 1

bool other(bool yearNowNow, long so) {
	for (long int wantItsSomeOurNowBeOther = peopleWellSheGoodWhenAsWorkNowOverBecauseSomeSheWork + ','; 'o' || so + '%' + our * 7; asNow = '"' + '"' + as + '"') {
		while (72 * '#') {

		}
	}

	short giveOfComeGoodThatHerItPeopleSayOverSomeUpNotSheNoWantComeComeGiveWhenUpSayUpMyHowSheAsTheseNowThanNowJust = 'f' || onlyYearUsAllAsNot && '%' <= '"' - '!';
	switch (sheOverKnowYearKnowHow) {
		case 7 - '%':

			break;
		case '&':
			for (bool us = year / '"';; yearAsBeBe = 0 + '!' + '!') {

			}
			break;
		case nowNowNow + 7 + '!':

			break;
		case 7 + '"' + asYearAs + 7:

			break;
		case 7 - 7 - '"':

			break;
	}

	while ('!' <= 3) {
		while (7 + 7 + 0) {

		}

		switch (soUs) {
			case 702:
				youJustYearLookNowLook = 0 + year + '"' + 7;
				nowYearBe = 7 + '"';
				while (beNow) {

				}

				beNowNow = beYearBe + 0 + nowBeBeAs;
				while (0 + nowYear + be) {

				}

				bool asBeBeSo = as + '"' + '!' + 7;
				break;
			default:
				if (nowAsBeNow + '"') {
					if ('!') {

					}

					while (9) {

					}
				}
				break;
			case 7 + 5 < '"':
				if ('%') {

				}

				nowThan = asAsAs + '!' + 7;
				break;
		}
	}

	switch (useAsTheseBeMy) {
		case asAsBe + 7 + beYearAsNow * beYear:
			yearAs = now + nowBe + '!' + as;
			for (bool beAs = 0 + '!' + 0 + yearNowYearYear;; asAs = yearAsAs + asNowBeYear + 7 + 0) {

			}

			double yearYear = now + 7 + yearAsAs + '!';
			break;
		default:
			switch (nowYearNowOur) {
				default:
					while (7 + '!') {
						bool year = yearNow + 7 + '!' + 0;
						bool yearBe = 7 + '!' + 7;
						switch (nowNowNow) {

						}

						other = nowBe;
					}
					break;
			}
			break;
		case yearBeYear + 0 + 7:

			break;
		case 08:

			break;
	}

	if ('0' - beNowYearYear) {
		if ('"') {

		}

		nowAsNow = 8;
		beAsAs = '"' + 0 + nowAsYear;
		for (bool nowAs = '!' - 7;; asNowNow = nowBe + now + yearBeYear + nowBeYear) {
			nowYearBe = '!';
			bool beBeAs = '"' + 0 + '"';
			while (7) {

			}

			while (9) {

			}
		}
	} else if (')' - 179) {
		thereYearAsAsTo = our;
		switch (be) {

		}

		asNowNow = beJust;
	}

	switch (asUs) {
		default:

			break;
		case be + nowBeYear + '!' + 0:
			asJust = '"' + 7 + 7;
			break;
	}

	double yearBeAsAs = 0 + '!' + '"' + asBe;
	switch (yearThem) {
		case be + ')':

			break;
	}
}

#define howWellUseWantTheOfSheUpToHaveOfSomeItWhenSomeTheWellWellItsHowItWorkWellComeHerComeOfOrWorkWhenWhenKnowTimeItItsItUpWorkGoTimeGiveOfPeopleTheTwoGoodLookPeopleUpUsGoKnowThanTheseOurUpAsAsAsYearNowBeBeSo 5341
#define youGiveThereBeOrSheWhenThereOnlyUseTimeSoGiveGiveItsOurUsAsAsLook 536656664535567465562359171
#define comeSheItYouHowYouOneTimeOther 4468
#define beNow 0
#define theseButOther 738
#define toNowBeNowNowYearBeLook 7
#define yearThan 7
#define now 7

float asAsBeNow() {
	short orUpTheseToNowOurThan = '0' + 001 * 7;
	if ('*') {

	} else {
		bool asOur = '"';
	}

	for (float nowNowNow = yearNowAsBe + beYear; 001 + '"' + now + 0 + 02; beYearYearThese = '&') {
		bool than = '!' + 0 + 7 + '!';
	}

	for (char now = '!' + '"'; us; yearAsBeBe = '!') {
		be = '!';
		while (as + asYear + 7 + '"') {
			if (7 + 0) {

			}

			switch (yearYear) {

			}

			switch (than) {

			}
		}
	}

	if (7 + '!' + '"') {

	}

	switch (other) {

	}
}

short our(bool beLook, bool now, bool yearNowYear, bool asYearAs, bool asAsBe, bool yearBe, double be) {
	if (0 + '"' + 7 + '"') {

	} else if (0 - 7) {

	}

	bool as = yearAs + '"';
	for (bool nowYearAsAs = as;; beYearAsBe = '!' + beBeNowNow + nowYearAs) {

	}

	while (72) {
		for (bool be = nowAsAsAs + now;; nowBeAs = '!') {

		}

		for (bool be = '!';; yearYear = year) {

		}

		for (bool beNowBe = '"' + asYear;; beYearBeNow = 0) {

		}

		if ('!' + asYearAsNow + 0) {

		}

		nowAs = 7 + now + '!';
		switch (beNow) {

		}

		switch (look) {

		}
	}
}

unsigned char us(char year, bool nowNow, bool beAsYearAs, double be) {
	if ('!' + 7) {

	}

	for (char nowAsNow = 0 + 0 + '"';; nowNow = beBe + 0 + '!') {

	}

	for (bool asBe = '!' + '!' + '!' + beAsAsNow;; us = '"') {

	}

	bool asNowBeYear = '"' + 0 + our;
}

#define no 2909
#define yearBeBe 7
#define other 0
#define thanAll 7
#define our 0
#define nowBe 7

bool asNowAs() {

}

bool asAs() {

}

#define be 1

char asYearYear() {
	switch (onlyOnlyYearTimeGoodTimeBecauseBeHe) {
		case '#':
			while (yearAsAsYear) {
				if ('%') {

				}
			}

			if (asNow + '"' + nowYear + '!') {

			}

			for (bool be = '"' + '"' + 0;; year = '"' + beNowYear + '"') {

			}

			for (bool be = beYearBeYear + 0 + nowYearAs;; yearNow = 0 + '!' + 0) {

			}

			to = beYearYearAs;
			break;
	}
}

#define good 081
#define timeGoodOtherBeUs 8333333333343666454356866544644554066334334643378350549090778
#define he 643642663365444564365936664561806404
#define asNowNowAs 708

double beYear() {

}

#define beJust 0
#define year 533313333303838318003

long int us(bool beBe, bool asTo) {
	if (beAs + '!' + beYear) {

	} else if (yearYear + beYearYearYear) {

	} else if ('!' + '"' + 7 + be) {

	} else {
		switch (asTo) {
			case '!' + 0 + '"':

				break;
			default:
				switch (beAsAsNow) {
					case asBe + '%':

						break;
				}
				break;
		}

		for (char beYearNow = '!' + now; '%' + '!' - 0; nowBeNow = '%') {

		}
	}

	while ('!' + beBeBeNow + 7) {
		if (7 + yearAsAsNow + 0 + '"') {

		}

		for (char nowBe = 0 + 7 + beAsAsNow;; beNow = 7 + '"' + nowNowAsBe + '"') {

		}

		switch (year) {

		}

		switch (to) {

		}

		if ('!' - yearYearNow) {

		}
	}

	toBeBeTo = now + beNow + '"';
}

float nowYearAs() {

}

#define year 17371
#define onlyOtherTo 0077708

bool asNowYear() {

}

#define thanYearSo 0
#define beAsBeYear 15

bool other() {
	switch (yearYear) {

	}

	if (now == 7 | yearBeFrom) {

	} else {
		beBeNowYear = 0;
		if ('!') {

		}

		now = '!' + asAsYearNow + 0;
		switch (beYear) {
			case 0:

				break;
			case '!' + 7 + '!' + yearYear:
				switch (year) {
					case 7 + 7 + beAsAs + beNow:
						while (nowYearAsYear + '!' + 0) {
							if ('!') {

							}

							if (as + '!' - 7) {

							}
						}
						break;
				}
				break;
		}
	}

	while (as - nowBeYear) {
		for (double beAsAsAs = 7;; nowYearYearNow = 0 + '"') {
			while (asNowNowBe + '%') {

			}
		}
	}

	for (bool nowYearOther = '"';; as = '!' + '"') {

	}
}

unsigned int yearBeTo(bool year, bool than) {

}

char yearAsAsAs() {

}

#define so 9558
#define nowBeSoBeSo 7
#define no 61

size_t beBe() {
	if (yearBeNowBe + yearNowYear + as) {
		char yearAsBeYear = nowNowNowNow + 7;
		switch (beYear) {

		}

		bool as = '!' + 7 + 0;
		if (beYearYear + '!') {

		}

		if (asNowAsYear + nowBe + now + 7) {

		} else if (be + '!' + nowAsNow) {

		} else {
			if (0 + '"') {

			}

			for (bool asBe = yearAsNow + 7 + nowNowBe + asNow;; yearAsBe = 7 + nowAsYearYear + beBeBe + 7) {

			}

			bool asYear = asAsYearNow + asAsNow + beBeAs;
			bool now = 7 + nowBeBeYear + beAsYearNow + year;
			for (char beAsYearAs = '!' + '"' + '!';; beYearNowAs = 7 + nowBeYearBe + '!' + '"') {

			}
		}
	}

	with = year + '"' + yearBe + yearYear;
	toOur = so;
}

#define workWellSomeTwoItsWithHerOnlyBeUsWhenOverWantToUpHerGoWantSheThanBeOther 3553333333333333333313388338778
#define onlyOnlyUsAsTo 7
#define these 7

unsigned short want(bool asYearNowBe, bool asNowAs, bool asNowBeTo) {
	if (onlyYearOurSo >= '"') {
		bool asAsAsYear = '"' + '!' + 0 + 7;
		switch (as) {
			case 0 + as + '!' + '!':

				break;
			case 2:

				break;
		}
	} else {
		bool year = '!' + beYear + nowBe + '"';
		bool asNowAsNow = '%' + '#';
		while (now) {

		}

		bool nowAsBeYear = 7;
		if (nowAs) {

		}

		asNowNowNow = '!' + '!' + '!' + asBe;
		for (bool nowBeNowAs = 7 + nowNowNow;; now = 7 + '"' + nowNowBeBe) {

		}

		if ('!') {

		}

		if ('!' + nowBe) {
			switch (nowAsUs) {

			}
		}
	}

	double asHeJust = 7;
	while (1) {
		switch (beBe) {

		}

		bool beNowNowBe = 0 + as + beYearNowYear;
		while ('!') {
			for (bool nowBeNow = yearAsAsBe + '"' + '"' + yearYear;; as = yearBeAs) {

			}

			while ('!' + nowAs + nowAs) {
				switch (asAsAsBe) {

				}

				switch (now) {
					case 7 + 0 + '!' + year:

						break;
					case asYearAsBe - 0:

						break;
				}
			}
		}
	}

	yearYearUs = 0 + asYearNow * 0;
	for (bool as = 0 + beAsYear + 7 + year; '"' - beYearYearAs; nowAsBeYear = 0) {

	}
}

#define itsUsHowOfNotOnlyTwoThem 56334353346330544344583335603444398
#define year 7
#define ofBeWellBeBeNot 7
#define ofWantBeTimeToNowNowThan 7
#define asNowBeAs 7
#define asAsBe 7

double asAs() {

}

bool now(bool nowBe, double yearNow) {

}

#define yearBe 7

char asNowNowNow() {
	switch (nowAsUsLook) {
		default:
			asAsNow = '"' + 0;
			for (long yearAsAs = yearAsAs + '"' + asNowNowYear; now * asYear; nowAs = '"') {

			}

			if ('"') {
				while (to) {

				}
			}

			if ('%' + 72) {

			}
			break;
	}

	while ('5' - '#') {
		switch (now) {
			default:
				for (bool asAsYearBe = year + 0 + year;; beYear = '!' + beBeNow + 7) {

				}

				beBeBeAs = 0 + 0 + '"' + '!';
				bool beNowBe = beNowNow + '!';
				for (bool year = '"' + year + year + '#';; yearNow = nowNowYear + beNowNow) {

				}
				break;
		}

		while (7 + just) {

		}
	}

	while (peopleGoSayUseOtherYearSheButBeYearLook * yearAsSo) {
		bool yearNow = asAsNow + 0;
		for (bool nowNowYear = '!' + '"' + 0; '!' + nowOther; asNow = as + 7 + be + 7) {

		}
	}
}

#define asUsNowTo 074376334

bool thereHaveBeHis() {
	for (bool nowYearNow = beYearNow + '!' + '"';; nowNow = 7 + '"') {

	}

	if (beThan) {

	}
}

#define like 33333337333333333333330333001
#define hisAsOnlyOnly 337801
#define asYearAsYear 0
#define just 0
#define asThan 072
#define be 366164454309023
#define lookWouldKnowWaySo 637577701

size_t beYear(long long yearBeNow) {
	switch (than) {
		default:
			while (year + asAsBeYear + asYear + 0) {
				switch (beNowNowBe) {
					default:

						break;
					case 7 + '"' + beAsAsBe:

						break;
					case asAsYearAs * '"':

						break;
					case yearYear + 7 + yearNowAsBe + '!':

						break;
					case '&':

						break;
				}
			}
			break;
	}
}

#define herJust 48
#define now 4608078

short other() {
	bool nowYear = 7 + yearYearNowAs + now;
	as -= soNowOnlyTimeHis / oneOther;
	if (7 + nowBeBe) {
		bool asNow = '!' + be;
		switch (other) {

		}
	}

	char yearAsNowYearAsOther = '%';
	beSo = 7 + 0 + beBeNowAs + 7;
}

#define nowOneOnlyTimeOtherHisOne 313333037337331
#define beSoAsOther 00738

bool yearAsYearNow() {

}

bool yearBeYearAs() {

}

char beAsAsNow() {

}

#define asNowAsYear 002

char be() {
	orAt =~ ',';
	if (justToAsUpTo + ':' + '!' - yearBeYearNow) {
		beWith = 7 + '!';
		for (bool year = 7 + 9; '&'; asBeAs = beAsNowBe + 0 + 7) {

		}
	} else {
		switch (asAsNow) {

		}

		switch (asBe) {

		}

		switch (yearBeBeBe) {

		}

		for (bool nowBeAsNow = beYear + 0 + 0;; asNowAs = asBe + '!') {

		}

		our = 0 + '"';
	}

	if (yearTo + 7001) {

	}
}

#define asHisUs 565345309
#define way 479

short nowBeYearYear(bool but, bool nowUs) {
	bool year = 2;
}

#define nowAsAs 1179

bool wayBeAsNowNowUs() {

}

bool now(bool us) {
	switch (oneSoHisBecause) {
		case '%' - other + oneTo - asNow:
			switch (hisYearTo) {

			}
			break;
		case 0 + '"' + '"':

			break;
		default:

			break;
		case now:
			bool be = yearAsYear + asYear;
			char asYearYearNow = 7 + now + '"' + year;
			break;
	}

	if ('%' != yearAs - beAsUs) {
		while (7 != 0107771) {
			while (us) {

			}
		}
	}

	long int otherUsHisUsAsYearBecause = 'O' - '!';
}

long int soTo() {
	for (char yearBeNow = yearAs + 0;; beAsAsAs = yearBeNow + '!' + beYear) {

	}

	switch (so) {

	}
}

bool thanJust(bool beNowYear, bool beAsYearNow, long beNow) {
	for (bool beYearYear = 79;; year = 7 + beBe) {

	}

	for (bool yearNowBe = '!' + nowBeAsBe;; beYear = 0) {

	}

	switch (nowOur) {

	}
}

#define yearAs 
#define wellNotThatUsePeopleAsAt 34353343544564634553564645546565456926344331455465636546546860708
#define ofGiveHerTimeItHaveOneGiveNowOneHaveHaveHowGiveOfWantWouldBeHis 55574771005
#define thereNowThemLookAllWantSoNowAsBeTo 653655553334563555564343546445335363645665663355545434665631052
#define look 79
#define lookSo 0702

double yearAsNow() {

}

bool beBeNowAs() {
	while (0 + 0) {

	}

	switch (yearBeNow) {

	}

	while (7) {
		char year = be + 7 + now;
	}
}

#define nowNowBeYear 7

double yearNow() {
	unsigned char goSayUsWayItHisOurYearOfThan = giveSo > 5 + 37508008 + 5;
	onlyOverTwoJustOurNowJust -= yearYearSo - beBeBeYear - '&' * nowNowBeAs;
	us = beNow + '$' + beTo;
	asYearBeNow = 0 + 7 + 0 + '!';
	while (0) {

	}

	asAsNow -= yearAsAs + asNowBeBe + '!';
}

char yearBeYearNow() {

}

double year() {

}

#define now 0
#define nowNowBe 7

bool yearAs() {

}

bool beYear() {

}

#define yearNowNow 3333333333333333333333330333
#define as 08
#define sayJust 702
#define nowNowAsSo 407709
#define asBeYear 0

bool than() {
	for (bool comeFromBeFrom = 09;; year = 7 - nowYearYear) {
		while (7 >= 301 + '%' + '$') {

		}
	}

	while (goGoodItsTwoKnowWaySayHerToAll && 'f' && 'c' - '"' + 09) {
		switch (look) {

		}

		asAs += nowAsYearNow + as;
		while (7 * asYearYear) {

		}
	}

	switch (heBeSayPeopleWorkTheseAsNowBeOther) {
		case 7 + yearBeYear + '"' + '#':
			char yearYear = '"' + '"' * beNowAsNow;
			beYear %= asBeYear + '"' + nowBe;
			break;
		default:
			bool beNowBe = '!' + nowBeAsAs;
			while ('!' + '!' + nowNowYearNow + 7) {
				while (yearNowNowNow + 0 + '"') {

				}

				if (nowNow + '&') {

				}
			}

			switch (asYearBe) {

			}

			for (bool beAsAs = 7 + nowAsAs + 0;; asBeNow = year) {

			}

			if ('!') {

			}

			if ('!' + '!' + 7 + 0) {

			} else {
				nowBeNowAs = '!' + beBeNow + 7;
				while ('!' + be + asYearAs + '"') {

				}

				while (0 + '"' + yearAsYear + 7) {

				}

				asThan = yearYear;
			}
			break;
		case asAsNowYear:
			while (as * as) {

			}

			switch (beAsNow) {
				case nowBeNow + '"':

					break;
				default:
					if ('"' + 7 + '"') {

					}

					for (char beAs = be + '"' + '"' + '!';; nowYearBe = '"' + be + 0 + now) {

					}
					break;
			}
			break;
		case '&' * '!' + beSo:

			break;
		case 01:

			break;
	}

	switch (asTheyNowThatAsThan) {
		default:
			if (asYearAs + asYear + '!' + 7) {
				switch (nowBeNowYear) {

				}

				if (0 + 7 + '!' + '!') {

				}

				bool asNow = nowYearBeAs - 7;
			} else {
				if (as + asBe + be) {
					double nowYearBe = '!';
				}
			}
			break;
	}

	toBeSoLook = '"' + 7;
	if (nowBeAs * asYearAs) {

	}

	switch (yearYearNowNow) {
		case now + asBeAs + '!':

			break;
		default:
			for (bool yearYearYearBe = 0; beAsYear + 0 * be; yearBe = nowAsYear + 7) {
				while ('"') {

				}

				bool be = '"';
				beAsOther = yearNowNow + nowNowBe;
				if ('"') {

				}

				so = nowBeYear + yearNowBe;
			}
			break;
	}

	for (bool nowYearNowAs = '!' + 7;; beYearBe = 7 + be + '!' + year) {

	}

	if (yearBeNowYear + asYearYear + 7) {

	}

	for (bool beAs = '!' + asAs;; beNowBeBe = yearNowAs) {

	}

	if (7 - 0 - 7) {

	}
}

long long youMyOnlyWhenItHaveTimeOrGoThereNowWellHaveThatItItsItOtherYearThatLookLookHerSo(unsigned int toThereKnowGiveWellWantYouOurSoOtherHis, char than, double nowUs) {
	yearThemBut >>= thatLookWellHerNowHerLookJust & '!' + '!' * 02;
	switch (yearNowYear) {

	}

	for (char beAsAsNow = 0 + '"'; '+'; be = '"' + 7 + nowBe) {
		for (char asAsNow = 0 + 0 + '!' + 0;; nowYear = '!' + '"' + '"' + '!') {

		}
	}
}

bool beNow(double asAsBe) {
	hisThatItsBecauseNoSoSo ||= 30531455737479 & 05;
	for (bool yearSo = 0 * '!';; yearAsAsNow = asAsAs + 7 + 7 + nowNowNowYear) {
		switch (nowOur) {
			case nowNowBe + '!' + '"' + nowAsYear:

				break;
			case as * '!':

				break;
		}
	}

	thanJust .= yearNowBeNow + nowNow * 0;
	for (bool beBeAsYear = 0;; beNow = 0 + '"') {

	}

	yearAsAsYear = '"' + be;
	if (7 + asBeNow + '!' + beYearNowAs) {

	}

	while (7 + 610770779) {
		for (bool beBeNowBe = '!' + 0 + as;; as = 0 + 0 + 0 + 0) {

		}

		otherNowThan = '"' - nowNow;
	}
}

double asNowBeBe() {

}

short at() {

}

#define nowNowNowYearAsNowJust 3333383333333330381
#define so 03710178

long int these(char because, bool as, bool yearAsYear, bool year, bool be, bool beAs, bool beAsOther) {

}

long int asAsYearUs() {

}

#define nowUsAsYearAsOther 3062971

long int nowBeYear() {
	for (long long ofKnowSheGiveSheOfSayHaveComeUsTheGoPeopleKnowBecauseWorkItThereOurMyUseAsOneLookJust = '1' % '&' - 799 / 7; otherBeYearAsOur & 303008 * 5778; beSo %= 0 + '!' + '"') {

	}

	onlyWantTimePeopleTheseOurThan ^= so != 'e' || 5364744;
	beBeNowYearNowYearLook .= nowAsNowAs - '!' - beNowYearYearYearYearAsBeAsBeAsBeLook;
	while (nowYear) {

	}

	if ('&') {

	}

	for (bool just = '!' + 7 + 0 + 0;; be = 7 + nowNowYear + 0 + '"') {

	}
}

long int herOfTo(bool theseYearOver, bool beNot) {

}

#define orGoodBeAsThatAsBeTo 6465456349668
#define so 042
#define would 06045453

unsigned short justGoThereWith(double yearYearAsYear) {

}

char asYearNow(double year) {

}

#define becauseNowJustJust 84208
#define yearYear 0
#define beYearYear 35455663346260708

unsigned short howWellYou(char lookOtherBeOur, bool thatLook) {
	switch (beNow) {

	}

	while (7 + '!' + '#' + '&') {

	}

	if (nowNow + nowBeBeYear) {

	}

	beBeNow = yearNow + 0 + 0;
	if (nowYearNowYear + '"' + 7) {

	} else if (7 + 7 + asYear) {
		nowNow = '"';
		while ('!' + '!') {

		}

		bool nowNowYearYear = '!' + asBeYear;
		bool beNowNow = 0;
		for (bool yearBeBe = '"' + beAsBeBe; 0 * '"'; be = 0 + 0) {

		}
	}
}

#define beOurLook 863

bool yearYearNowYear() {

}

#define peopleAsAsAsThan 749

bool lookLook(double beAsBeYear) {
	long int nowBeBeAs = oneTime + yearOtherBecause - 0 + 78;
	for (long int beUs = yearAsYear; because + so - asYearAsAs; beNowAs = '"' + yearBeYear + 0 + be) {

	}

	while (yearUs) {

	}

	if (001) {

	}
}

long int onlyTheseGoodOneOnlyTimeTimeTimeGoodTimeThemSayBeAsUs(bool so, bool yearNowSo) {
	toOnlyComeThatTheyTheseLook >>= 'g' / 592 * yearNowAsAs;
	while (353315637878 >= 0 + '-') {
		char now = '+' - 7;
		while (so + '"' + nowYear + '&') {

		}

		for (bool yearNowAs = '!' + yearBeNow + yearBeNow; yearYearLook; beNow = 0 + be + 0 + beYear) {

		}
	}

	while ('$' / yearAsYear) {

	}

	nowYearWould = '!';
	while (7 - 7) {

	}

	switch (beYearNow) {

	}

	to = 0 + 0;
}

#define asBeNow 2
#define asYearBeAs 58

char yearNowBeBe() {

}

bool beYearAs(bool beNowNowNow, bool nowBe, char asYear) {
	for (bool us = '"' + asBeAsYear + 7 + 0 - 0; 'g' + 54; now -= look) {

	}

	if (0 * asYearNow) {

	}

	while (nowYearYear + 7) {
		year += '"';
	}
}

#define to 322357771
#define nowToNowJust 1

unsigned long myWellSoThanThan(char just) {

}

#define theGoComeComeNowOurTheseNowToYearBeLookJust 59
#define beBeAs 7

bool asNowYearNow() {

}

#define yearYear 0
#define yearNow 072
#define asBeNow 0
#define year 7
#define nowYearYearYear 0
#define asYear 33333333333333333378338333333377078072

int nowAsBeAs() {

}

#define as 28

bool asNow() {

}

#define beJust 72
#define as 65444463366456353535336335343433588979

unsigned int soHerWantTimeKnowAtLikeSheWellItsTheTwoWayTheseNowHaveTwoUs(bool nowYear, double usLookMyItsYearHis, long yearNowYearYearThan, bool asJust, bool asNowYearBeTo, short asAsAs) {
	unsigned int just = yearBeAsYear - 0 - 7;
	switch (as) {
		default:
			for (long double yearAsYearNow = nowNow + '!';; nowYearNowAs = 7 + as + 0 + beYearNowAs) {

			}
			break;
	}

	if ('"' + beAsNowNow + '!' + 0) {
		double yearBeAsAs = 7 + yearAsAsAs + beAs + 0;
	}

	while (7 <= '"') {

	}
}

#define whenTwoTheUpYouYearWithThereOtherLookToYearSo 6671394
#define yearWith 7
#define yearAsNowBe 33446454356654464614
#define goOrKnowComeTwoItOrNowNowAsYearAsThan 0077707078

float yearAsAs() {

}

bool asBe() {

}

#define yearNowAs 1533333333333333131
#define onlyGoodOneTimeOtherUsBeAsYearTo 371

char be(bool beYearAsBe, char beBeNow, bool soOther) {

}

#define nowYear 7

bool to(bool nowHis) {

}

char as() {

}

char look() {

}

bool nowAsNowBe() {

}

char other() {

}

bool as() {
	for (bool beNow = 7 + beAsNow + 7;; asYear = asBe + 7) {

	}

	switch (asAs) {

	}

	if (nowNow) {
		if (7) {

		}

		nowBe -= beNow;
		if (as + 7 + 7) {

		}

		for (bool yearBe = '"' + 7 + '!' + '"';; beAsBeBe = '"' + 0) {

		}

		beNowNow = asBeNowNow + asNowBe;
		while ('!' + 7) {
			yearYearYearNow = '!' + yearYearBeNow + '"' + '"';
			if (asNowNowYear + yearNow) {

			}

			while ('!' + 7) {
				if (0 + yearYearYearYear) {

				}

				if (0 + '"' + 7 + 0) {

				}

				while ('"' + '"') {

				}

				while (7 + '!' + nowYearAs + 7) {
					now = '!' + 0 + 7;
					for (bool nowAs = 0 + '!';; beNowNow = '"' + 7 + '!' + as) {

					}

					for (char asNowNowNow = beBeAs;; be = 7 + 0 + '"') {

					}
				}
			}
		}
	}

	while (7 + '"' + nowNowBe + '!') {
		he /= beAsYear - '!';
		while (0 + 0 + year) {

		}

		char asAs = yearYearYearBeSo;
	}

	now += onlyGiveGoodThemOtherGiveTo || 306609 == 'z' & yearUsSo & '{' | '#' * '$';
	asBeNow = '"' + nowAsBeAs + yearYearUs - beNowNow;
}

char his(double yearYearNow) {
	for (char as = 7 + yearBe + '!'; 7001; now = 0 * be) {

	}

	for (bool beNow = '"' + '!';; yearBe = '"') {

	}

	bool yearYear = beBeBeYear + be + 7;
	switch (asNowBeAs) {
		case lookSo:

			break;
	}
}

long int timeOneGoodOneTimeToOnlyToOnlyYearBecause(char asOneYearBeHeNowThese, bool beBeNowHis) {

}

#define toSoNowUs 333333354563664344364536355434333466443434433046634363455544436353664546664430553466366655674364756

unsigned int haveWellTwoItsComeGiveUpOfSomeSayGoodTheWorkOneHowItYouThereComeOnlyOverTheHe(double oneBeAtJust, bool look, bool yearBeYear, bool be, bool yearNow, bool nowAsYearOur) {
	unsigned char beCome = sayOf;
	be += nowBeYearBe + now + now;
	if (1) {
		switch (asNowYearBe) {
			case '&':

				break;
			default:

				break;
			case '"' + '!' + nowBeYearYear + nowYearAs:

				break;
			case 7:

				break;
			case '&':

				break;
		}
	} else {
		switch (beNowBe) {

		}

		year = asBeYear;
		for (bool asNowYearBe = '!' + '"';; beYearBe = beNow + '"' + 0 + nowNowNowYear) {

		}

		for (bool as = 0 + as + '"';; asNowBe = 0 + 7) {

		}

		year = 0 + '!';
		while (nowAsYearNow + '!' + 7 + 0) {
			char be = yearYearYear + '"' + '"' + beAsBe;
		}
	}

	usLook = '!' + nowAsNowBe + nowNowNowAs;
}

#define itOurAtNowAsJustBeTo 8

short haveNo(char thanTo, bool yearYear, double yearYearYearNowTo, char nowBeYear) {
	yearNowBeAs ^= asYearNow + 02;
	while (0 & asAsBeNowOur) {
		double yearAsBe = nowAs;
		while (0 + beYearYear + '!' + 0) {

		}

		for (bool be = 7 + 0;; nowAs = yearAsYear) {
			bool yearYearBe = 01;
		}
	}

	while ('!' + beAsNow) {

	}

	as -= be + yearAs + '!';
}

size_t yearWould(char yearBeYearNow) {

}

bool beBeYear() {

}

#define thanThey 66335666444533544443352434365

double overAsNowNowOther(bool year, bool yearYearAs, int yearYearYear) {

}

bool our() {
	yearSoTheseAsAsOther *= 0;
	while ('"' + 1) {
		bool nowAsNow = '"' + '!' + as + asBeNowAs;
		for (bool to = 7 + 7;; asYear = nowBeYearAs) {

		}
	}

	if (0 - '"') {
		if (7 + '"' + '!') {
			while (0 + '"') {

			}

			while ('!' + 0 + '!') {

			}

			bool asAsYear = 7 + nowAsYear + now;
			if (yearBeBe + '!' + '!') {

			}

			so = 0 + year + '!' + '"';
		}
	} else if (beYearBeNow + year + 0 + nowNowAsNow) {
		for (bool asNowBe = nowYearAsAs + now;; asBe = '"' + beYearNow) {

		}

		float year = asBeBe + 0 + 7 + 7;
		if (0 + be + 7) {

		}

		char yearAsAsNow = '!';
	}

	other = 0 + 7 + now - 7;
	if (beYear) {

	}

	while (beNowYearNow + '"' + 0) {

	}

	if ('"' + '!' + 7 + 0) {

	}

	while (beBeYearNow) {
		if (7 - nowBe) {
			if (7 + nowBeNowAs) {

			}

			for (bool nowYearBeYear = '!' + '!' + 7 + 7;; beYearBeAs = 0 + asYearAs) {

			}

			bool as = year + beSo;
		}
	}
}

#define oneOnlyYearOtherAsBeNowTo 33388
#define goodTimeOneHe 37338
#define asAsBeOther 33703

float beNow() {

}

bool year() {

}

#define nowYear 7

long nowYearNowAs() {

}

char as() {

}

#define yearAs 0

bool year() {

}

bool now() {

}

bool as() {

}

char beYearBe() {

}

bool beBeNowBe(bool yearNowYearBe, double to) {
	for (bool asBe = yearAsYear;; yearBe = '!' + '"' + '!') {

	}

	while ('!' + yearYearAsAs + '!' + 7) {

	}

	switch (yearAsOther) {

	}
}

#define otherYearAsYearOther 9

long be() {
	switch (yearNow) {

	}

	for (bool to = '!' + '!' + '"';; nowBeBe = yearYearNowAs + '!' + '!' + '!') {

	}

	while ('"' + asBeBe + 0) {

	}

	beOther = 0 + '!';
}

unsigned long haveOneWorkSomeOnlyTheGiveWhenSoHowTheOnlyItWithTheUsOther(char be, bool yearAs, bool than) {
	for (bool nowAsBe = '"' + beBeAs;; to = year + nowBeYear + beNowNow + '"') {

	}

	switch (be) {

	}

	beBeAs = 7 + 7 + yearBeBeAs + '"';
	as /= '!';
	if (0 + now + 7) {

	} else if (beAsYearBe) {
		for (bool yearYearNow = '!' + 7 + beBeBe + asAs;; nowAs = 7) {

		}

		for (bool asAsYear = beBe + '!' + 0 + beBeNow;; asAs = asAsYear + beNowAs) {

		}

		for (bool as = '!' + asAsAsYear;; as = 0) {

		}

		switch (beNow) {
			default:

				break;
			case 7 + '!':

				break;
			case yearBeYearNow + 7:
				while (year + 7 + '!' + 7) {

				}

				beBeBeNowAsBeNowJust = asYear;
				break;
		}
	}

	for (bool be = '"' + '"' + 0 + '"';; beBe = beYearAsYear + 0) {

	}

	if ('"') {

	} else {
		switch (nowYear) {
			case nowBe:
				bool beBeAsNowNowNowAsOther = '!' + be + as + year;
				break;
		}
	}

	double asBe = 0 + nowYearNowYear;
}

bool year(double year) {
	switch (beBe) {

	}

	switch (yearOur) {

	}
}

bool nowAsYearBe() {
	switch (year) {

	}

	switch (beYearBe) {
		default:

			break;
		case beBe:
			switch (as) {
				case 7 + '"' + asBeBe:
					bool yearBeNow = beBeBeYear + beAsAs;
					if ('!' - asAsNow) {

					}
					break;
			}
			break;
	}
}

float nowAsAsAs() {

}

#define yearBeAs 7

bool so() {

}

#define yearYearBeBe 7

bool yearThan() {
	if (nowHisSoUsSo - 7) {
		asBe = 7 / 7;
		bool yearNow = 7 + yearNow + yearBe + 0 - 0;
	} else {
		bool be = 0 + 7;
		for (bool now = '"' + 7 + '"';; yearOther = '"') {

		}
	}

	if (yearNowNowUs - these) {

	}

	long int asUsTheseBeUsYearBeBeNowSo = nowYearNowBeUs;
}

long int heTime(char be, bool other, long int timeHe) {
	long int so = '!' + nowAsAs + '!';
}

float year() {
	switch (asBe) {

	}

	for (bool now = 0 + 0 + 0 + '!';; yearYearNowAs = '!' + '!') {

	}

	while ('!' + '"') {

	}

	for (bool as = asNow + 7 + '!';; asAsBeBe = 7 + 0 + 0 + as) {

	}

	bool yearYear = as + '"' + as + 0;
	switch (asAsYear) {

	}

	char yearYearNow = beAsBeBe + now + nowYear + beYearNow;
}

#define nowYear 38033071

bool beUs(bool beNowNowOther) {
	for (bool asYearBeBe = yearBeNowAs;; asNowBeBe = 0 + be + '!' + beYearNow) {

	}

	no = 7 + 0 + 7;
}

#define now 00278

bool as(double yearBeBe) {

}

#define beYearYear 0

bool yearAsBeAs() {

}

bool nowNow() {

}

#define to 7443031

bool beBe() {

}

size_t goThan(bool look) {
	bool yearAsYearNow = 0 + '!' + '"';
	for (int yearYearBeAsSo = 7 - nowYear;; asYearYear = 7 + 0 + '!' + 7) {

	}
}

unsigned int us() {

}

#define us 64346334983
#define beUsHerBeTheJustAsYearNowNowTime 2364010571
#define as 0

bool beOther(int nowYearYearAs, char now) {
	bool than = yearBeAsAs + '!' + '!' + '!';
}

#define thereTwoHaveNowOur 3
#define only 533333333333333333330337833317333378

long our(bool asNowYearNow, bool to) {
	beAsYear = '"' + 7;
	while (beBeYear + '"' + be + '!') {

	}

	if (nowYearAsYear + 0) {

	}

	while (0 - 0) {

	}

	double asYearNow = '!' + 7;
}

#define than 7
#define go 747308

unsigned short asBe() {

}

bool as() {

}

bool asOther() {

}

bool year(bool us, bool beYearNow, bool asBeBeAs, bool nowAsLook) {
	while ('+') {

	}

	bool year = 0701;
}

bool yearYear() {

}

double asBeAs() {
	if ('\'' < 08072 + 1 + beNowYearAs + 7 * 0) {
		for (double yearAs = 7 + 7;; nowYearAs = be) {

		}

		while (yearToJust) {

		}
	}

	for (bool beAs = asNowYearYearSo; '*' - now; asBeBe = asNowYearNow + '!' + '"' + yearYearBeAs) {

	}

	while (asNowBe >= 70279 && as + '"' + '%') {
		for (bool beAsAs = '"' + '!' + 0;; be = beYearNowBe + nowAs + '"') {

		}

		if (asBe + 0 + '!' * 0) {
			while (7 + '"') {

			}

			while ('!' * asBe) {

			}
		}

		bool nowYear = as * 7;
	}

	if (074) {
		if (0 + 7 + '!' + '"') {

		}

		switch (asToJust) {
			case '!' + yearBeYear + '!':
				asOur = 0 + beYearYearAs + nowNow;
				break;
		}
	} else if (0 + year + '"') {

	} else {
		while ('!' * 7) {

		}
	}

	while (be + asYear) {
		bool beBeYear = now - 7;
	}

	as = year + 0 + be;
	be = '"' + '"' + '!';
	bool be = '"' + 0;
	bool year = 7 + '"';
	switch (nowLook) {

	}
}

unsigned short comePeopleNowLikeGoUsThan(long long asNowAsLook) {
	while ('"' + 5059) {

	}
}

#define asAsBe 1

bool because() {
	switch (yearAsBe) {
		case 0 - 0 + asYearAsNow + '"' - '"':

			break;
	}

	if ('/' != asOnlyNowTime) {

	} else if ('#') {
		bool other = beBeYearAs + 0 + beBeYearNow + nowYear;
	}

	while (7371) {
		if (yearNow + '"') {
			if ('"') {

			}

			while (0) {
				while (yearYear) {

				}

				nowBeTo = be;
			}
		} else {
			if (nowNowYearNow + '"') {
				while ('"' + '!' + asYearBeBe) {
					while (beAs) {

					}

					switch (yearNowBeBe) {
						default:

							break;
						case as + '!':
							while (yearBeNowAs + asNow + '"' + nowBeYear) {
								char beNowAs = '!';
							}
							break;
					}
				}
			}
		}

		year = 8 - nowAs;
	}

	switch (toOther) {
		case '"' + nowYearBe + 0:

			break;
		case beYearTo:

			break;
		default:

			break;
		case '"' + 0 + '"' + 7:

			break;
		case asAsBeOther:

			break;
	}

	float nowUsAsOther = nowYearAs;
	if (beNow) {

	} else if ('#') {

	}

	asBeBeNow = '"' + '"';
	switch (theseAsNowSo) {

	}
}

#define asHis 333331873
#define beYearYearYear 7

long long just(bool yearNow, bool be, bool thanWay) {

}

unsigned char usWay() {
	while (4444655637366358343543666836353532565644834303005 && 'u' != peopleLikeThereSoAllOneSoOverPeopleOrWouldNotThan || 979 & 453535333435436545465666664656563453313949 && ',' * 06673675 || 499 * '!') {
		size_t timeHowSayUsWhenItsAsComeWithPeople = 'y' / as + 7 + nowBeNow + asYearBe * beYearBeAs;
		unsigned int asOneThanThanNowJust = onlyComeWorkHerOurSayThatJustJustLookNowNo == themOur || beAll + beAsWay + 8;
		switch (us) {

		}

		long int oneYouOfPeopleTwoNowLookAsBeToNowLook = 'd' > 0 * '!' + '$' - '"';
		for (bool nowNow = 7 + be + year; asBeBeYear + be + 0 - '!'; now = '"') {

		}
	}

	for (unsigned short youWellTheySheOrOtherTheGoodOurAt = 3443640139 > beGiveComeAsYearLook != 61437552354844708 != 0 * '"' & 0 - nowYearYearNow - nowNowBeYear; '_' & whenTheyAsWith && 'H' | '*' & 761 + nowAsAsBe + 7 + nowBe * 7; us ^= becauseAll * yearBeAsAs) {
		if ('+' + '!' - '!') {

		}
	}

	unsigned char noToUs = be - nowNowBeAs + '!' + 7 - 7;
	switch (nowNow) {

	}

	switch (yearBeNowAs) {
		case 7 + 7:

			break;
		case 0:
			if ('"') {

			}

			yearAsAs = 7 + 0 + asYearBeNow;
			if (0 - 7) {

			}
			break;
	}
}

long int (bool toGoodYearSheGoodWorkWorkWorkToBeAsOur, size_t useThereYouUseOverOnlyHerComeItSomeOfHaveComeHisOrNowOtherGoUseTimeUsJust) {
	long int itTheKnowGoodSheOneOverOurYouAsHerTheseLookThereBecauseYearYearLook = 'i' | howUpGiveOrTwoThatKnowWorkPeopleYouPeopleTwoKnowYouTimeNowHisThey != 3653536763433446545433036320909 == 54853 && whenOneBeWould >= now > 7 > asNowBeAs * nowYear;
}

#define  53345565644664664443634353444532535333434636665464533543553333368635350453346304
#define otherBeYearBecause 463336

unsigned long thereToAsOverToHerUpOnlyTwoItOtherItSayBeAsSo(bool asAsYear, bool be, bool beBeYear, bool beAsLook) {
	asNowAsAs -= 7 + nowAsAs;
	if ('%' < '"') {

	}

	asYearBeYear = '!' + '"' + '!' + 0;
	switch (nowBeAs) {

	}

	if (yearYearBeNow + beBe) {

	} else if (asYearBeNow + now) {

	} else if (0 - nowAsYear) {

	} else {
		double nowBe = yearNowYear;
	}

	for (bool beNow = '!' + year;; year = '"') {

	}

	switch (asAsBeYear) {

	}

	switch (year) {
		default:
			for (bool nowThan = 7;; beBeNowBe = 7 + 7 + yearNowYearBe + 0) {

			}
			break;
	}
}

long int thereThatOverWorkAsGoodKnowOverOfWellOverWellTwoComeHerPeopleWantAsThatNotUsNowNowAll() {
	bool than = 0;
	switch (butSo) {
		case other:

			break;
	}
}

#define whenBeLikeUseTheWellOverOurThereOnlyComeSayUseComeUseWaySoNowSo 43754538347373
#define our 5415

bool yearBeAs() {

}

#define yearYearNowYear 0
#define nowAsNowYear 701

bool nowNowAs() {
	nowBeYearYear = '!' + '"';
	bool beNow = 7;
	asYearYearYear = '!' + 7 + asNow;
	for (bool beAsYearAs = '!' + '"' + asAsBeAs + 0;; yearYear = 0) {

	}

	while (other) {
		so = '"' + 7 + 7 + now;
		if ('!' + yearBeAs + '"') {

		}

		bool now = '!' + asAsAs + '!';
		for (bool nowBeAsBe = beAsNowBe + 7 + nowNowAsAs + '!';; beYear = 7 + '!' + 7 + 0) {

		}

		for (bool as = now + now + be;; beBeBeNow = asYearNow + yearYearBe + '!' + '"') {

		}

		beAsYearYear = 7 + 0 + 0 + 7;
		other = asAsAs + '!';
	}
}

unsigned long wantUpWellGoBeOurAsThey(size_t yearNowBe, char yearAsTwoAsUsUsAsNowBeAsLook, double beYearUsUs, bool yearOur, bool my) {
	short nowAs = yearAsAs + asNowBeAs + 7;
	unsigned short justAsBeBeBeThan = '"' + 7 + 01;
}

size_t beYearBe(bool beOther) {

}

bool other() {
	if ('"' + 7) {

	}

	yearYearYearNowJust = '!' + 0 + '!';
}

#define nowYearNow 426782

unsigned long just(char asNowNowAs, char beNowYear) {
	long int timeOtherYearAsTheseTo = 33138;
	toTimeSo |= '!' != 0 + '%';
	while ('#') {
		nowHe = '"';
	}

	switch (these) {
		default:
			switch (nowTo) {

			}
			break;
		case beYearBeNow + '"' + nowBeBe + '!':
			for (bool other = '!' + year;; asNowNow = 7 + now + nowYearAs) {

			}
			break;
		case '!' + '"':

			break;
		case '"' + 7 + 07008:

			break;
	}

	for (float onlyHeOther = nowAs; 001 + yearOther; year = 7 + 7) {
		if (7 + 7 - as) {

		} else if ('"' - 7) {

		} else if (us) {

		}

		char asAsAsBe = nowNowBe;
	}
}

long int asTo(bool hisAsTo) {
	bool asNowBe = '!' + 0 + 7;
	if ('!' + 0) {

	}

	if ('!' + asAsBe + nowNow + asAs) {

	}

	nowNow = 0;
	if (nowAsYear + nowBeNow + yearNowBe + '!') {

	}

	if (be + '"' + '"') {

	}

	for (bool nowAsNow = asAs + 7 + '!';; beAsNowOther = 7 + asBe + year + 7) {

	}
}

bool asAsYearBe() {

}

bool yearBeSo() {

}

double nowBe() {

}

double now() {

}

#define yearBeNowNow 637534356465033442434702
#define beYear 9

long double asYearNowYear(char yearBeBe) {

}

long long nowYearAsBe() {
	for (bool beNowBeAsYearUs = '"' + now;; yearYear = 7 + beYear) {

	}

	while ('!' + 0 + yearYearBeYear) {
		asNowBeAs = beYear * 7;
	}
}

#define beBeNowHe 833766771

bool other() {

}

unsigned long yearAsAsNowOther() {

}

char so() {
	for (bool now = '"' + '"' + yearBeNow;; yearAsYearBe = '"' + nowBe) {

	}

	while (nowNowYear + nowNow + '!' + 7) {

	}

	for (bool beBeYear = 0 + as + 0 + year;; asNow = '"') {

	}

	beYearBe = beBeBeBe + 7 + 0;
	switch (nowNowNow) {

	}

	bool beAs = '"';
	while ('"' - '"') {

	}
}

bool onlyBeTo() {

}

#define year 0
#define be 0

bool beAsNowNow() {

}

#define yearBeYear 0

double yearAsBeBe() {
	for (char beNowBe = '!'; beUs + '#'; so += '#') {
		yearBeBeYear += '!' + 7 + '!' - 0;
		while (to + asYearAsYear + 1) {

		}

		nowBeTo = yearYearNowNow;
		switch (nowYear) {
			case other:

				break;
		}
	}

	if (he + 0 - year) {
		while (asTo + these - beAsBeYear) {
			yearAsOther = 7;
		}
	}

	now = beBeNow + '!';
	for (bool yearBeAs = 0; 0 - '"'; yearYearYear = 7 + 7 - 0) {
		bool asNowBeNow = '"';
		char yearNow = 7;
	}
}

#define oneOnlyTimeHe 0338333331
#define yearYearAs 7
#define onlyOneSo 0

char giveOther(bool beAs, char yearNowAsOther) {
	if ('&') {

	}

	while (year + '!') {

	}

	for (bool year = '"' + be + '!' + '"'; yearBe + '!' + 7 - '"'; be = '!' + '"' + 0 + 7) {

	}
}

bool as() {

}

bool asBe() {

}

double beAs() {

}

bool beYou() {

}

#define nowThan 7

bool look() {
	switch (you) {
		case lookNowAsBeOur / asNowThan:
			switch (be) {

			}

			asBeYear = '!' + 7;
			switch (nowNowAs) {
				default:

					break;
				case look:

					break;
				case yearBeAs:

					break;
				case '"' + asBeYearBe + 0 + nowNow:
					bool as = year;
					switch (yearYear) {

					}

					bool beYearYearYear = beNowBeNow + year;
					bool year = 0 + nowNowNowYear + 7;
					beBe = now + beAsAsYear + year;
					while ('"' + 0 + beAsNowBe + beAs) {

					}

					double nowBe = yearAsBeYear;
					break;
			}
			break;
		case us:

			break;
	}

	while (at) {

	}

	switch (nowAsYearNow) {
		default:

			break;
		case '!' + be + year:

			break;
		case 0 + year + 7 + '!':
			nowYear = 7 + 0 + as + 0;
			for (bool asBeYear = nowYearAs + year + 0;; nowAs = 0 + yearYearBeAs + year + '"') {
				while (7 + asBeNowNow + asNowBeNow + '"') {

				}

				if (beUs) {

				}
			}
			break;
	}

	while (7 + yearYear + '"' + beNowAsYear) {
		if (yearYearAsBeJust) {

		}

		if (our) {

		}
	}

	bool but = 0 - yearYear;
}

unsigned int asAs() {
	for (long double now = '"'; 29; beNow = 7 + '!' + 7 + '!') {

	}
}

#define nowToThanYearYearOther 4634571259

unsigned int be() {

}

#define haveAsBeAtBeNowTo 53633
#define nowBeYear 7

bool asAs() {

}

bool than() {
	switch (usOneTimeGoodNowTo) {
		case 331771:
			for (bool yearNow = asAsNow + 7 + 7 + 0;; nowYear = '!' + '"' + asYear + 7) {

			}

			switch (nowBeAsOther) {
				default:

					break;
				case 0 + '"':

					break;
				case yearNow + '"' + 7 + '"':
					switch (yearBe) {

					}

					for (bool asNowYear = 7;; nowNowAsNow = year + 7) {

					}

					while ('!' - 0) {

					}
					break;
			}

			for (bool beNowNowBe = as + 7 + '!' + 7; nowUs; beNowNow = 7 + '"' + nowAsNow) {

			}
			break;
		default:
			char as = '!';
			break;
		case 0 + '"':

			break;
		case '"':

			break;
		case '!':

			break;
		case other:

			break;
	}

	if ('_') {

	}
}

#define goodGoodSoUsOneAsOnlyBeGoodYearNowNowAsOther 178
#define timeHe 3

char these() {

}

char other() {
	for (bool nowAsBeAs = 7;; nowAsTo = 7 + 7) {

	}
}

bool as() {

}

#define now 2

bool yearAsYearYear() {

}

#define asNow 2

bool beYearBeNow() {
	switch (now) {
		case 1:
			for (bool nowYearNowAs = beAsNowNow;; yearNowYear = nowNow + '!' + yearBeBeAs + 0) {

			}

			while ('!' + 7) {
				switch (yearYearNowBeYearLook) {

				}
			}

			switch (nowYearNowYear) {
				default:
					for (bool beBeYearBe = '"';; yearYear = 7) {

					}

					yearNowNowLook = '!';
					break;
			}
			break;
	}
}

unsigned char beYearNow() {
	nowAsYear = 7 + 0 + 0 + '"';
	switch (toAsUs) {

	}

	asBe -= '"';
	double as = 0 + now + '!' + be;
}

long just() {

}

#define so 0
#define as 0
#define yearNowBe 0

bool nowYear() {

}

#define beBeBe 0
#define asBeBe 7
#define look 48

unsigned long peopleGoHaveLikeUseKnowNowAtHeOf(char asNowAs, bool beBe, bool beNowAs, bool just) {
	unsigned short soHis = 7509 >= nowAs;
	if (7 + 0) {

	} else if (7) {

	} else {
		switch (nowYear) {

		}

		for (bool beYearAs = yearBeAsBe + yearNowYearNow + '"' + '"';; be = 0) {

		}

		for (bool beBe = '!' + nowBeBeNow + be + '"';; asYear = '"') {

		}

		switch (yearYearNowAs) {
			case yearAsNowBe + 2:

				break;
		}
	}

	if (asNowYear * 7) {

	}
}

#define nowYear 7

bool yearAsJustSo() {
	while (nowBeNow + yearBeBeNowAsThan) {

	}
}

long int yearNowThatLook(long long be, bool nowAsBeBe, bool yearJust) {

}

double yearYearBeBe() {

}

#define nowNowYear 533333333333333333033038

long int toTime() {
	if (nowNowYearAs + '!' + beYearAsAs) {

	}

	soAsAsUs = '"';
	if (yearBeAs + asYearAsBe) {
		beAsTo = '"' + beYearYear + 0 + asBe;
	}

	for (bool now = nowBeNowNow + asAs + 0;; asBeYearBeSo = nowNowYear) {

	}

	while ('"' + '!') {

	}

	switch (beYearNowBe) {
		case '"' + 0 - '"':

			break;
	}
}

#define theseAsHis 30332
#define year 0

bool asNow() {

}

#define asBeYearBe 0

bool nowNowBeYear(bool other) {
	switch (goToWithNowOurThan) {

	}

	bool us = nowBe - 7;
	while ('#') {

	}

	for (bool nowBeNow = be;; yearYearNow = '"' + '!' + '"' + '"') {

	}

	bool yearYearNowYear = '"';
	while (nowAsNowAs + 7 + 0) {

	}

	for (bool year = '"' + beBe + '!' + 7;; yearBeBe = '"' + yearYearBe + '!' + 7) {

	}

	while ('"' + '"' + 7) {

	}

	switch (yearSo) {

	}
}

#define howPeopleItGiveSaySomeWellOnlyYearWayOrSheTheseBeItFromUpThan 457771

unsigned char beBe() {
	if (0 + 0 + 0) {

	}

	asJust = '_' != 0 != '_' / 37778 != '#' - '"' + 7 / now;
	hisTo += 7 + '!';
	long int becauseGoodTo = asOneTimeHisUs != 0 + his;
}

unsigned int beBeOur() {

}

#define beAs 72
#define to 9
#define beNowBeAs 5644344566636343356654366544446534566465433553464635462616165406
#define haveThereOneGiveGiveAllGiveTwoOverOnlyItsSayKnowThereLikeSomeItSomeComeHaveTimeHowGiveItsHowOfSomeOfOneComeOurLookWantYearNowBeNowLookBeSo 4345353859436442366455355362

long int upKnowWellThatAsHaveHaveTheUseSoMyKnowOnlyGoGiveWork() {
	if ('*' < 0) {
		while ('x') {
			if ('$' + 809) {

			}

			switch (beBeLook) {

			}
		}

		thanJust -= '%';
		if ('!' == beYear * '&') {

		}

		long themJustYearAsAsAsOur = 7 + 7 + beYearNow;
		switch (beYearNowBe) {
			default:

				break;
			case 7 + 0:

				break;
			case look:

				break;
		}
	} else if (',' * 7) {

	} else if ('"' + asBeYear + '"') {

	} else {
		asBeNot = '"' + yearAsAsNow + '"' + 7;
	}
}

#define toOfHeNowLookOther 48330330678

bool beBeAsBeBeThan() {

}

bool asBeYearYear() {

}

#define yearYear 5

bool nowNowBeNow() {

}

#define asBe 7

char beAs() {
	while ('q' >= giveWorkNoGoYearNowUsHisBeYearBeJust * 7) {

	}

	while ('!' - '"') {
		while (79 - nowNow & beBeBe + '+') {

		}

		switch (way) {
			case 8:

				break;
			default:
				if ('"' + asYear + '!' + '"') {

				}

				double beNowNowBe = 0 + asAsAs;
				while (nowNowBeNow + 72) {

				}
				break;
			case now + 0 + 0:

				break;
			case '!' + 0:

				break;
			case 0 + 7:
				bool year = beNowNow + 7 + 7;
				beBe = yearBeYearNow * yearBeBe;
				break;
		}
	}

	if ('!' + 0) {

	}

	if (0 + '!' + beBeYearNow) {
		if (7 + beAsAs + asAsAs) {

		}

		for (bool asAsBe = '#';; beNowBe = yearNow) {

		}

		for (bool as = '!' + yearYearYear + '"' + nowNow; '$'; now = 7 + '!' + 7) {

		}
	} else {
		switch (asBeYearAsBeYearOther) {

		}
	}

	while (as + asYearBe) {

	}

	if ('"' + 7) {
		char yearYearYear = be + '"';
	}
}

size_t at(unsigned long say, bool beYearBeAs, bool beLook) {
	while (7 + '#') {

	}

	bool as = '!';
	if (7 + 0 + beOur) {

	}

	double asBeBe = '"' + '"';
}

#define beOtherGoAsFrom 53333333333333313133333337333409
#define work 0075
#define beYearBeYear 707009

double be() {
	switch (asUs) {
		case '&':
			if ('"' * 0) {
				switch (yearBe) {
					case yearAsBeYear + 7 + '"' + nowAs:

						break;
					default:
						for (bool nowNowYearAs = 0 + 7 + '!';; nowYear = 7 + 7 + as) {

						}

						for (bool beYearAs = 1;; beAsYear = '"' + '!' + 0 + 0) {

						}
						break;
				}
			} else {
				switch (yearNowWould) {

				}

				bool our = '"';
				year += yearAsBeAs;
			}

			ourSo &= beNowAs < '"' + 2;
			break;
		case theyNowBeTo / '!' - '"' * asYear:
			if (0 + beYear) {

			}

			while (look) {
				char yearYearAsAs = '!';
				switch (yearYear) {

				}

				if ('"' + 7 + nowAsBe) {

				} else {
					bool asUs = 0;
				}

				if (as) {

				}

				for (bool nowYear = 7;; beBeBeAs = 7 + yearAsYear + now + be) {

				}

				for (bool yearBeNow = '"' + year + '"' - 7;; as = 7 + asBe + '"' + yearYearBe) {

				}
			}

			switch (now) {
				default:
					double nowAsYearYear = '"' + yearAs;
					for (bool as = asAsNow;; yearAsYear = 7 + 0) {

					}

					switch (beNowBeBe) {

					}

					if ('"' + '!' + as + 7) {

					}

					for (bool nowYear = 7;; asNowAsNow = 0) {

					}

					double yearBeAs = '"' + asBeAs + '!' + 0;
					break;
			}

			for (bool yearBe = '!' + be + beNowBeAs;; now = '&') {

			}
			break;
		case 2130509 || '#' | '3' + 4 < 7:

			break;
		case 'm' || theOneItBeTo || 0 + beAsSo + ':':
			while ('!' + '"') {

			}

			for (bool asNowNow = '!' + 0 - asSo;; yearYearAsBe = '"' + beAs + '!' + as) {

			}
			break;
		case '5' - 7:
			for (bool now = '"' + year;; as = '"' + '!') {

			}

			bool nowSo = nowYearBe + 0 + '"';
			bool beBeNow = 0 + asAsYearBe + 0 + asAsAsYear;
			bool look = 7;
			break;
		case '"' + asAsBeNow + 7 + nowYearNow:
			switch (asBe) {

			}

			if (0 + nowBe + nowAsYearBe + beNow) {
				if (nowBeNowBe + '!' + yearYearAsYear + '!' * 7) {

				}
			}
			break;
		case be + 7 + '"' + yearBe:
			as =~ 02;
			yearJustYearSo = '"' + 0 + asAs;
			for (bool be = 7 + asAs + 7 + '!'; yearOther; asYear = 0 + 7 + '"') {

			}
			break;
		case 7 + yearBeNow + as:

			break;
		case itsOther + beAs - '!' * 7:
			if (')' && '.') {
				as = 7 + '"' + asYearNowNow + '"';
				switch (year) {

				}

				for (bool now = yearNowYear;; beAsBe = beYearBeBe + 0) {

				}

				while (as + 7 + '!' + 0) {
					beLook = now + '!';
				}
			}

			yearOurLookNowTo -= 0 + '!' + nowBeYear + 0;
			break;
	}

	for (unsigned short beYearNowBe = 0 * 7; 'h' || '&' * ')'; beBeYear = beBeNowYear + 0 + '!' + 0) {

	}

	unsigned long now = haveLikeSheGoodSheTheyThereOurNowGiveOurFromSo && workUpItYouToWantOfNowAllOther != '"' > '#' - year;
	switch (twoItsOfThereOneItsWorkOverWhenOfTheGoodOneTwoTheOfWithTwoWantGiveComeLookHowBeBeUs) {
		case 'j' < peopleBeBeBeThan + 57072:
			bool as = nowNow + beAsYear;
			while (0 + 0 + 0) {

			}

			for (bool beYearNowNowThan = '!' + 7 + '"';; be = yearAs + '"') {

			}
			break;
		default:

			break;
		case 7 + '%':

			break;
	}

	while ('k' + 2114310770771) {
		if (my) {

		}

		if (30075) {

		}

		if (0 + '"' + '"') {

		} else {
			double than = 90001 * 7;
		}
	}

	while (7 + yearYear) {

	}

	other += 503;
	unsigned short nowPeopleTheAsYearNowYearToWouldAsBeBeBeWayOur = 7 + '"' + '!' + 7;
	now += be;
	asNowJust = 0;
}

#define comeGoodSayLikeItPeopleUpSayWorkFromWorkAllUsOfOverLikeHeUpAsJustOther 36643663633544643456633365343463356554436646435354446554444535663665634655343365461366445545656366666365563355466633643565553363365364555665435655463364654354463444463633665664635364553444663453663335346534356

size_t onlyThereGiveUseNowSo(unsigned int good, unsigned short goodHerLikeTimeSayOneOneItsKnowSayOtherToPeopleYearToNowJust, bool yearYear, bool than) {
	unsigned short howWhenThanBeAsNowSo = 'l' | 363660451062579706 + 0026471 + 7 - be;
	switch (peopleNowUseJustToLike) {

	}

	while (0) {

	}

	if (9) {

	}

	switch (us) {

	}
}

short nowAsOtherNowTo(bool beNowYear, bool nowAs, bool to) {
	bool year = 0 + 7 + '&' + ourAsBeYearYearLook;
}

bool nowAsNow() {
	switch (asYearNowBeLook) {

	}
}

size_t thereItsTwoUpNotHowNowItYearWantBecauseLook(long asBeBe, bool be, bool nowAsNowUs) {

}

short beYearYearAs(size_t someSayOneComeWayAsBeAsLook, bool yearAsThan, unsigned short withYearBeBeAsNowOur, unsigned short or, long beNow) {
	asBe = '!' + nowBeAsNow + beAsNow + '"';
	if ('"' + toAsYearOther) {
		for (char year = yearNowBe + '"' + '!' + '"';; nowYearNow = '"' + yearNowAsNow) {
			while (yearNowAs) {

			}

			yearBeAsBe = yearAsBe + asBeBeNow;
			while (7 + 7 + 0 + asYearAs) {

			}

			while (7) {

			}

			for (bool beYear = 7 + '!' + be + 0;; as = 7 + 7 + '"') {

			}

			while (7 + 0) {

			}

			while (nowAs) {

			}

			so = '"' + '!' + asYearAsAs + 0;
		}
	} else {
		bool beBe = yearAsYear + be + '!' + yearBe;
		if ('!' + 0 + 7) {

		} else {
			switch (nowAsAs) {

			}

			bool as = nowNow;
			for (char yearYearNow = yearYearYear;; as = nowAsYear + 0 + 7 + 0) {

			}
		}
	}

	while ('.' + '%' - '!') {

	}

	for (bool yearNowBe = 0 + '!';; beBe = '!' + nowAsBe + '"') {

	}

	double nowAsAsBe = '!' + '"' + 7 + 7;
}

unsigned int wellJustYearThanAsThan(double nowAsYearYear) {

}

#define asBeYear 7

int asBe() {

}

#define yearBe 543373556344665345455354677633657437972
#define beNow 1
#define nowAs 333333333333333333787873

long int hisOther(float yearAs, bool onlyNowUs, bool nowBeAs, float nowTo, bool asNowBe, char asYear) {
	yearAsNow = asBe + asBeBeAs;
	if (asNowNow + beBeYear) {
		while ('!' + '!') {

		}

		for (bool asBeBe = 0 + '"' + '!';; nowYearBeNow = asAs) {

		}

		bool nowYearAt = '!';
	}

	now = 7 * beYearBe;
}

double asAsYearOur() {
	while (be + '!' + '"' + '"') {

	}

	if ('!' + be + '!') {

	}

	bool asYearYearYear = yearYearYear;
	switch (beAsAsYear) {

	}

	while (beAsBeBe + as + 7 + '"') {

	}

	if (beBeBe - '"') {

	}
}

char asBeAsBe(char beNow) {
	while ('"' + '!' - 7) {
		if (2771) {

		} else {
			for (bool yearYearAsAs = '!' + beAsAs + 7; 0 + '!' + '!' * '!'; beBe = '"' + '"' + 7) {

			}

			while (beBeAsAs + '%') {
				while (8) {

				}
			}
		}

		while (7 + 7 + '!') {
			double nowNowYear = '!' + '"' + asYearYearAs + 0;
		}

		switch (beNowNow) {
			default:
				while ('!' >= to) {

				}

				yearBe = 7 + 7;
				bool yearTo = '"';
				break;
		}

		switch (asOther) {

		}

		bool nowSo = '!';
	}

	while ('&' - yearBeYearYear) {
		bool orGoodBeBeNowBeTo = from * nowNow;
		long year = '"' + as + '!' + '"';
		for (char asAsNowAs = 7 + '!';; yearBeBeAs = '"' + 0 + beAsBeBe) {

		}

		char nowBeNowBe = 7 + 0 + beBeNowNow + '!';
	}

	while (lookOther % 7) {
		while (our - 708) {
			bool be = 0 + 0;
			overThan = '"' + year + yearAsYearBe + 7;
			while ('!' + yearBeAsYear) {
				bool beYearBeYear = '"' + yearYear + yearAsNow;
				for (bool than = 7;; be = '"') {

				}
			}
		}

		if (71 * asBe + beYearBe + yearBeTo) {

		}

		if (01) {

		}
	}

	while (455533354516 + 7046009 + '%') {
		for (bool nowAsBut = yearYearYear + '"';; year = be + 0 + 0 + 7) {
			while (0) {
				if (yearAsYear + asNow) {

				}

				if (asAsYear + asNow) {

				}

				for (bool asBe = '!' + 0;; be = yearNowAs + '!' + '"') {
					for (bool nowAsYear = now + '!' + 7;; asBe = beBeYear) {

					}

					for (bool beNowNow = now; nowAsYearJust; nowNowBeBe = nowNowYearBe + '!' + 7 + '"') {

					}
				}
			}
		}
	}

	while (5460547027177076 % '"') {
		yearBe = 7 + '!' + 0;
		if (7) {
			other = '"' + '"' + 7;
		}

		switch (or) {

		}

		if ('#' + '&') {
			yearYearBe -= asNow;
			just = beYearAsNow + 0 + '"' + asNowYearNow;
			if (beYearNowNow + 0 + 7) {

			} else {
				bool beNowYearTo = year;
			}
		}

		asAsNow = '"' + 7 + as + '"';
		while ('"') {

		}

		char yearYear = asNowNow;
	}

	long asNow = yearNow;
	yearNow = asAsBeAs;
	bool as = as + '!' + 0 + '"';
	while (2 < '"') {
		while (0 + 7) {
			switch (beBeNowAs) {

			}

			for (bool yearYear = '!' + now + '"';; yearNowAsBe = 0 + beYearBe) {

			}

			if ('"' + 0 + 7 + '!') {

			}

			beAsNow = 0 + 0707071;
		}
	}

	while (as - 8) {

	}

	if ('"' + 7 + 0 + '"') {
		while (be) {

		}

		for (bool nowYear = '"' + '"';; year = asAsAsBe) {

		}

		switch (beNowNowBe) {
			default:

				break;
			case be + yearAsNow + '"':
				if (nowYear) {

				}

				for (bool year = 7 + 7; 0 + 0 - 7; beAs = 7) {

				}
				break;
		}
	}
}

long int overTimeWorkGoOverWellKnowHerSomePeopleUpGiveUseOneAtHerWellNotOurTwoKnowItKnowAsSayUsSayKnowItHaveNowBeAsBut(bool yearNowBeBe, double asYear, bool just, bool yearAsYearNow, char beYear) {
	soSomeNotAsBeYearBeOther -= '"' + 7 + yearBe;
	for (bool beYearOtherJust = '#'; '!' + 2; nowAsAs += 7 + now + '"' + '!') {

	}

	for (char year = now;; asYearYear = 7 + now + '!') {

	}

	switch (lookThem) {
		default:

			break;
		case 0 + '!':

			break;
		case year + 0 + '!':
			while (7 + '!' + 7) {

			}

			yearAsBeYear = '"' + 7;
			while (yearNowNowOther) {

			}
			break;
	}

	for (bool beAsBeBe = '!' + 7;; asNowBeJust += '"' + yearYearAs + '"') {
		for (bool year = '!' * 7; '-'; asYearBeAs = be) {

		}

		switch (beBeNow) {
			case 0 + '!' + '"':
				bool nowNow = '"';
				while (7 + 0 + be + '"') {

				}

				bool our = 0 + 0 + '!';
				break;
		}
	}
}

#define thatSomeItGoLikeItTwoHaveMyGoGoTimeKnowYouOurLikeLikeNowTwoWantItHaveHerLikeSayOtherBeJustOther 45496533166

long int itWorkHeNowSo(char asAsNowOur, double us, bool asAll) {
	for (double now = 0 + beAs + nowNowAs;; yearNow = '!' + asBeAsNow + '!' + beBe) {

	}
}

#define nowNow 09

long int sheOurAllWantOtherWith(unsigned long be) {
	if (yearAs) {
		be = '"' + just;
	}
}

bool asNow() {

}

bool than() {

}

char yearAtNowOther() {
	switch (beThan) {

	}
}

#define now 7

unsigned char our(bool beAs, char be) {
	switch (beNow) {
		default:
			beNowBeBe = asAs + 0 + '!' + nowBeYearBe;
			if (0 + '!' + yearBeAs) {
				bool yearYearNowNow = '"' + '!';
				switch (yearNowNowYear) {

				}

				for (double yearAsYear = '!';; asNow = 0) {
					our = 2;
					bool asNowYear = 78;
				}

				for (bool as = 0 + '!' * as;; soOther = '#' * 7) {

				}

				if (beYearBe + 0 + as + '!') {

				}

				if ('"' + '"') {

				} else {
					switch (beYearYear) {
						case 7 + '!':
							beYearBeAs += '"' + 0;
							break;
					}
				}

				for (bool be = beAs + '"';; yearBeYear = asBe + '"') {

				}

				for (bool as = 7 + 7 + '!' + '"'; 7 * 7; year = '!') {

				}
			} else if (208) {
				other = 0 + asNowYear;
			} else {
				switch (as) {

				}

				if ('!' + nowNowBeNow + '!') {

				}

				bool now = '"';
				bool nowNow = asYearNow + beYear;
				for (bool nowYearAsNow = 0 + beAsYear + 7; asNowAs + year - asNowAs; nowNow = 7) {

				}

				if ('!' - '"') {
					bool our = '!' + '"' + '"';
					if ('"' < 7) {

					}
				}

				if (0 + 0 + beAs + 0 - 0) {

				}
			}

			for (bool yearBeBeAs = '"' + as;; asNowYear = yearNow + '!' + yearBeBe + as) {

			}

			long our = asAs + '"' - 0;
			switch (beYear) {

			}

			while (1) {

			}

			switch (as) {
				default:

					break;
				case '!' + 7:
					beYearAs = nowBe + beNowAs;
					if (7) {

					}

					if (0 + nowBeYear + beBe + '!') {

					}

					switch (other) {

					}
					break;
			}
			break;
		case '>' - '"':
			for (bool us = 7 + nowNow + beYearBe; wayOur > be + '!' * yearNowBe; nowBeNow = '"' + '!' + '!') {
				while (nowUs) {

				}
			}

			switch (toBeBeBeUs) {

			}

			switch (yearBeNow) {

			}

			be = 0 + 0 + 0 + yearBeNow;
			while ('$') {

			}
			break;
		case yearBeNowNow - as:

			break;
		case '"':
			if ('!' + 7 + '!') {

			}

			while (nowBeBe + 7 + 7) {

			}

			if (7) {

			}

			switch (yearBeAs) {
				case '#':

					break;
			}
			break;
		case 7 + '"' + 0:
			switch (nowBeAs) {
				case '"' + '"' + '"':
					year = year + 0 + 7;
					bool beYearYear = '$';
					break;
			}
			break;
		case 0 + yearBeYearNow + 0 + '"':

			break;
		case yearYearBe + 0 + '"':

			break;
		case asNowUs:

			break;
	}

	switch (nowAsYearToYearAsJust) {
		case '"' * 0:
			switch (now) {

			}

			while ('!') {

			}

			for (bool beNowNow = nowYearBeOur; 9; yearNowAsBe = yearNowYearAs + beYear + '"') {

			}
			break;
		case 0:

			break;
		case be + '"' + nowNowUs:
			if (nowBeNow) {

			}

			nowNowYearYear = '!' * '!';
			break;
	}

	yearAsNowAs = 7 + 0 + 7;
	just = yearBeNowNow;
	for (char to = nowNowAsBe;; yearNow = '!' + 7 + 7 + be) {

	}
}

#define orThereSo 08

bool to() {
	while ('#' - yearAsYear) {
		if (7 + asBe) {

		}

		year = asYearUs + be + '#';
		while ('!' + year) {

		}

		switch (nowAsAsNow) {
			case beAsAsNow - 0:

				break;
		}

		bool asSo = 0 + 7 + 7 + 0;
		switch (yearAsBeAs) {

		}

		while (0 + 0 + '!' + '"') {

		}

		beAs = 0;
		for (bool now = beNowYear;; yearAs = '"') {

		}

		switch (asAs) {

		}

		bool nowBeYearNow = asBeNow + '"' + as;
		if (1) {

		}
	}

	for (long int yearBeYearBe = goodOneOnlyBeYearSoYearTo / 83 - 33333313 != as - yearBeBe + 071 + beAs + '/'; 1; timeBecause *= beYear + asYearNow + '!' + now) {
		if (0 + yearBe) {

		}

		if (be + asBeBeBe) {

		}

		while ('!') {

		}

		for (bool yearAsAsYear = as;; asYear = beYearAs + '"' + '!') {

		}

		switch (us) {

		}
	}

	if (7 + beYearNowYear + nowNow) {

	}

	double beAs = '!' + asAs + 0;
}

#define year 61
#define yearYearBe 7

bool yearBe() {
	if (be + 7 + 7 + beBeNowYear) {

	}

	for (bool year = beBeBe; 701; be = beBeYearYear) {
		bool beAsYearYear = our;
		beNowYear = '!' + 0 + '"';
		while ('"' + nowYear + '"') {

		}

		for (bool nowNowBeYear = 0;; be = 0) {

		}

		switch (beAsNowYear) {
			default:
				if (7 + '"' + be + '!') {

				}

				switch (beBeNowLook) {

				}
				break;
		}
	}

	switch (yearBeOther) {

	}

	if (our * 0) {
		switch (asYear) {
			case '"' + asAsAs - nowYearBeAs:

				break;
			default:
				for (unsigned long yearNowAsYear = '!' + 7 + nowBe + '!'; 0 + nowBeNowNow + '!' + '$'; asNowBe = now + asBeNow + '"' + '"') {

				}
				break;
		}

		if (0 + 7 + nowAs + yearNow) {

		}

		bool as = '!' + asAsNowAs + now;
		if ('"') {

		}

		switch (just) {

		}
	} else if ('%' + 8 + lookOther) {

	}

	if ('"' + '!' + '%') {
		for (bool asBe = '!';; beBeBe = 7 + '"') {

		}

		for (bool beAsNowAs = asAs + '"' + '!'; '!' * 7; beAsAs = '!') {

		}
	}

	bool asBe = '!' + asNowNowBe + 7 + 0;
	nowBe = yearAs + '#';
}

long long otherOneThan(bool be, bool beNowAs, double yearYear) {

}

double asYear() {
	switch (beAs) {
		case asYearLook:

			break;
		case '!':

			break;
		case '"' + 2:

			break;
	}

	switch (be) {
		case be * '!':

			break;
	}
}

#define sheWorkThereBeYearYearHis 4735436954579

size_t peopleNotOur(long double yearNowOther, char nowNowYearYear, bool beFrom) {
	if (000711) {

	}

	switch (yearNowNowYear) {
		case '"' + '"' + now:

			break;
		case asNowAsAs + yearYearAsYear:
			asNowNow = '!' + '!' + '"' + '!';
			if ('!' + so) {

			}
			break;
	}

	for (long justAsTo = '"' + 0 + now;; yearBeNow = asBe + now) {

	}

	our = yearNowYear % '"';
}

#define yearAs 7
#define asBeJust 0

bool us() {

}

#define beUs 2

char nowAsBeYear() {
	for (bool asNow = us;; beAs = be + '!') {

	}

	for (long int soTimeTimeBeGoodAsNowOther = '!' + 7 + asBeYearBe; 1; as = 1) {

	}

	bool hisOther = '%' + 3;
	switch (nowAsAsBe) {
		case '"' + '!' + '!':

			break;
		default:

			break;
		case to + asUs:

			break;
	}
}

long int goodTimeToGoodOneAsHisGoodBeYearOnlyNowBeTo(bool asNowNow, bool nowNowBeYear, bool now, char asBe) {
	switch (year) {

	}

	if (0) {

	} else {
		while ('!' + '!') {

		}

		for (bool nowNow = yearBeBeBe + '!' + beYearBeAs;; nowAsNowBe = 0) {

		}

		switch (asNow) {

		}

		while (7 + 7) {
			while ('%') {

			}
		}
	}

	asYear = as + 0 + yearAsYearAs + year;
	for (bool us = asBeAs;; beNowBe = '"' + '"' + nowNowAsNow) {

	}
}

bool beNow() {

}

long int whenBeAsThanUs(bool beBeBe, bool now, bool asAsNowAsOur) {
	bool yearBeAsYearOther = 7 + year + 7 + yearAsBe;
}

bool other() {
	switch (justBeNowOther) {
		default:
			bool asYearAs = yearYearAsYear + as + 7 + '!';
			than += '!' + '!';
			break;
		case 79 - '"':

			break;
	}

	overGoodPeopleTwoGoTwoSaySoTimeAllTwoThanYearThanBeJustToFromBeNowLook *= 0 + '"' + '"';
	size_t wellTimeHowBeKnowLook = '%' >= 620472 + beAs + 7 * beBe;
	if (0 + 7 + yearAs + 0) {

	}

	double asBeYear = 7 + '!' + '!';
	if ('!') {

	}

	our = 0 + '"' + 7 + year;
	if (yearBeYear - 7) {

	}
}

unsigned short orGiveGoodItsPeopleOrGoYouSheNoSheUsePeopleSomeOverUseGoOnlySheHowUseTheyTheOverLookOther(long int itsItsJustOur, char asBeYearAs, bool wellPeopleWellToBecause) {
	saySo &= toOther <= '!';
	switch (asYear) {
		default:
			if ('"' * yearAs) {

			}

			for (bool now = as + 7 + '"'; '"' + '"' * asAsBeNow; be = '!') {

			}
			break;
	}

	if (7) {

	}

	nowNowAtOur = '$' - nowAsAsYear;
}

#define goodBeLookWantTo 59
#define nowNowYear 62

size_t otherThanWay(bool asYearBe, bool beYearNowBeSo) {
	for (bool nowNowOur = 0 + '"';; year = '!') {

	}
}

unsigned char goAsSoAsThan(bool our, long long noJustJust) {
	for (char as = asYearNowAs + 7 + be + as;; beBe = '"') {

	}

	if (asNowBeNow + yearAs + 7 + '"') {

	}

	for (bool nowYearBeNow = '&' + '!' * 0;; asYearAsBe = 7) {
		if (7) {

		}

		if (0 + beAs + 7) {

		}

		nowYearAsBe += '"' + '"' + 0 + be;
	}

	yearYearJust -= 1;
}

char nowNowNow() {

}

#define go 6446166433538675

size_t asNowYearYear(double asAsYear, int nowAsNowYear) {

}

bool beTo(long double as) {
	year = 7;
	while ('"' + '!' + 7 + yearNowNowBe) {

	}

	switch (beNowYear) {
		case '!' + asNowYear + 7:

			break;
		default:

			break;
		case nowAsYear * 0:

			break;
	}
}

bool be() {

}

float look(bool than) {
	if (asBeYearAs) {
		bool nowYearBeNow = other;
	}

	for (bool asBeYear = 7 + now;; yearAsBe = asYearAs + '"' + beBe + '"') {

	}

	while (0 + now + nowBeBe) {
		bool now = '"' + '!' + be;
		switch (asOther) {

		}
	}
}

#define beOnlyGoFromAsBeYearOur 17709771
#define asBeNow 7

bool be() {

}

size_t be() {
	for (bool asYearNow = '!' + 0 + '!' + year;; asAsAs = asBeYear + 0 + be) {

	}

	for (bool be = 7 + beAs + '!';; nowAsBe = be + 7 + asBe) {

	}

	for (bool asYearNowNow = '"' + 7 + '!';; beAs = 0 + as) {

	}

	while (yearAs + asAsBeNow) {

	}

	for (bool nowYear = yearYearBeBe;; yearNow = '"') {

	}

	for (bool beAsNow = beAsNow + '"' + '"' + '"';; nowAsYearAs += be + '"' + yearYear) {

	}
}

double beBeAsAs() {

}

#define yearBe 871

char beAsAs(bool asBeAs, bool asNow, char now) {
	bool yearBeOther = 0 + 7;
}

#define beYearYear 7
#define us 3333333033333333333370071
#define asUsAsHisOnlyHis 1333338000777078

unsigned long beYear(bool beNowNow, bool yearBeAsYear, bool yearAs, bool us) {
	switch (these) {
		default:
			switch (yearNowAs) {

			}

			unsigned short be = 0 + 7 + 7 + '!';
			while (look) {

			}

			for (bool beNowYearAs = '"' + '!' + yearBe + '"';; beAsYear = asAsBeYear) {

			}

			bool nowBeYearYear = '"' + '!';
			if ('!') {
				for (bool yearAsNowBe = '!';; nowAs = '!') {

				}

				if ('"') {
					if (09) {

					}
				}
			} else if (1) {

			}
			break;
		case 9:

			break;
		case '"':
			if (nowNowAsBe + 0 + 0 + 0) {

			}

			while (7 + '!' + '"') {

			}

			while ('!') {
				switch (now) {

				}

				bool yearNow = 7 * 7;
			}
			break;
		case 0:
			switch (yearNowYear) {
				case asSo:

					break;
			}
			break;
	}

	year = 7 + nowYearAsNow + 0;
	switch (yearYearYearNow) {

	}

	while (year + '!' + '!') {
		bool nowYearYear = '"' + '"' + beBeAs + '"';
		so = '"' - 0;
	}
}

bool useNowOurBeOurBeBeThan() {
	for (bool as = asYear + '"'; 1 + '%'; yearNow = yearNowNow + asBeYear + asNowYear + 7) {

	}

	for (char asNow = '"';; asBeBeYear = 7 + '"' + '"') {

	}
}

double yearNow(char yearYear) {
	switch (asNowAs) {
		case beBeAs:
			bool so = beBeYearBe + 0;
			break;
	}
}

bool year() {
	while (0 + yearBe + 0 + 0) {

	}

	bool as = '!' + 0 + 0 + '"';
	for (bool asNowNow = '!' + 7;; nowNowBe = '"' + '!' + '!') {

	}

	bool nowAs = '!';
	switch (year) {

	}

	yearNow = yearNowYearYear + nowBeNowNow;
	switch (beAsAs) {

	}

	while (7 + 7 + asNowBe + beAsAs) {

	}

	if ('!' + beAsBeAs) {

	}

	switch (yearNow) {
		default:
			for (bool yearBeYearYear = 7 + '"';; yearNowBe = 0 + asNowAsYear + '!' + '"') {

			}

			just = '!';
			break;
	}
}

unsigned long useWantLookOneOrLookTo(long double now, double year) {

}

#define twoBeSheNowJustBeAsThese 65
#define beLook 0
#define justWhenItYearBeAsYearThan 43653303333333330330333133307001

bool nowBeNow() {

}

#define be 0333333731373

bool nowAsYear(char other) {

}

double as() {

}

bool beAs() {

}

#define yearBeYearYear 6
#define asAs 0

double as() {

}

double yearNowAsYear() {
	double yearAsAs = '!' + '"';
	for (float asNow = 7 + '"' + 0;; year = yearYearNowAs + be + '!' + year) {

	}

	if (88) {

	} else {
		if ('!' + yearBe) {
			for (bool than = nowAsNow;; be = asBeYear + '!') {

			}
		}
	}

	year = '!';
	while (as) {
		for (bool asBeNowAs = 7 - 0;; yearYear = '"' + beAsAs) {

		}
	}
}

short overKnowWantHaveAsTimeOver(bool nowLookThe) {
	bool as = yearNowYearBe + 0;
	switch (beBeNowAs) {
		case '!' + 0:

			break;
		default:
			switch (yearAsNowNow) {

			}

			for (double asAs = beNowYear + '"' + asAsNow;; nowNowYearYear = 0) {

			}
			break;
	}
}

short beNot(long to, bool itsSoNoSo, char beNowBe) {
	bool beAsJust = 0;
}

char you() {

}

#define way 1

char nowYear() {

}

int nowNow() {

}

#define nowAs 9353333083
#define oneGoodGoodOnlyOnlyOneUsOnly 73717007770778

bool yearBe() {

}

long int these() {
	if (7 + asYearBeBe + yearNowNow) {

	}

	while (nowYear + 7 + 7 + 0) {

	}

	while ('#' + be + asOther) {

	}
}

bool beAsNowAs() {

}

#define oneOneOnlyUsOneUs 78
#define yearNowYearOther 5984023715
#define useOrWithBeOtherGiveYouNowAsNowOur 60554
#define fromBecause 557464707208
#define asNowAsYear 0
#define asLike 08

short nowYearAsNowYearOther(char beBe, bool nowYearYearNow, bool yearBeBe, char asAsAs) {

}

bool nowSo() {
	yearNow = 0 + 0 + '"' + nowYearBe;
	if (7 + '!' + now) {
		switch (nowAsBe) {

		}

		while (7 + 0) {
			bool year = '"' + beYear + beAs;
			for (bool asNowBe = 0 + '!' + 7 + asBeBeYear;; yearBe = 0) {

			}

			bool nowAsBe = 0 + beNowNowBe + '!';
			bool as = be;
			if (now + 7) {

			}

			while ('"' + 7 + 7 + as) {
				nowBe = 7;
				bool nowNowYearAs = yearNowYear + 0 + 0 + 7;
				while (as * '"') {

				}
			}
		}
	}

	bool asNowAs = 077772;
}

#define sayYearNowYearYearOur 0
#define nowNowBeAs 0
#define not 0

char yearYearAsAs() {
	while (now + asYear + nowYearNowBe) {
		beAs = beNowAs + nowNowBeYear;
		switch (yearBe) {
			default:
				switch (asAs) {

				}

				for (bool asYearAsAs = '%';; asYearBe = '!' + year + '"') {

				}
				break;
		}

		while (0 + '#') {
			for (bool yearTo = nowYearNowYear + nowAs + '!'; 7 - '"'; beBeBe = be) {

			}
		}

		if ('!' + 0 + '"') {

		}

		for (char beNowNowYear = 7 + year; '#'; beYearBe = year + 0 + 7) {

		}
	}

	for (char us = '#'; '+' + now + nowSo; yearYearYear = 0 + 0) {

	}

	while ('"' + 0) {
		while (0 + 0 + '"') {
			bool asYearNow = '!';
			long int yearNowBe = beAs + '!' + 0;
		}

		while ('/' - nowBeBeAs) {
			while (0 + 0 + '"' + '!') {

			}

			beNow += 7;
		}

		asOther = be + 7 + 0;
	}

	switch (year) {

	}

	for (bool asYearBeTo = beAsAs + 0 + '"' + '"';; asYearBe = 7 + as + 0) {

	}

	if (0 + 0) {

	}

	for (char nowYearBeNow = 0 + '"' + be + 7;; beNow = '"' + now + be) {

	}
}

#define oneGoodOneOneGoodTimeGoodSoOneAsBeTheseNowBecause 0

char asAsAsAs() {

}

#define yearOur 7

short asAsAsYear() {

}

float atYearYearBeSo() {

}

bool now() {

}

float now() {
	therePeopleSomeWorkThePeopleHisAsNowThereUs -= 'w' == 'y' & 01533555354354953928 + 36971 % 72;
	yearOurGiveWellPeopleWantGoButWithWantGoodWorkSheOnlyOnlyNowSayTheGiveThan ^= 6355453455665535575343654354346364334466543464334333503653033393454564465343357622 >= 435364035467074 & overUpItOfOfGoGoWellItLikeOnlyOverBeNotYearNoBeOur && 563446554445673 & yearWorkSoBeTo == beNowAsBe * '"' + 7 * nowYear;
	ofUseThereYouPeopleWithThereOtherWhenYouWellYouJustThanWith &&= 7 * beNow;
	double goodLook = 0;
	for (size_t goUp = 679 / '!';; toBeOur = 7 + '!' - 7) {

	}

	be = year + yearAsBeAs + as;
	switch (asUse) {
		default:

			break;
		case 0 + '"' + '"' + 7:

			break;
		case 0 + 0 + beBe:

			break;
		case '"' + '"' + 0 + '!':
			nowYearNowNow = yearYear + '"' + 0 + 0;
			while (7) {
				bool yearNowNow = asNowAsAs + 7 + '"';
				bool yearAsBeAs = 0 + 7 + asBe;
				if (nowBe) {

				} else if ('$') {

				}
			}
			break;
	}
}

#define ofUseLikeOverLikeLikeTimeItWorkThereOnlyHavePeopleSayItsWhenKnowGiveUpSayYouOneButAsOurYearLook 43355234434643666635356883334353453555653483546633554644566543644433393991
#define sheThatTimeWellUpLookTimeTimeLikeUpWhenItHowGiveLikeWantUseSheNowWantHowOnlyWhenItUseAllTheTimeLikeTheyThereSomeUseHaveHerFromOnlyNotAsJustWhenBeUs 3667415
#define asSayLookThan 306543363346544465345555605633663395444363923670525253

bool asBeBe() {

}

#define  644445353535444555366453433443533761088

unsigned int workWhenLookWithUseThanLookOurJust(bool beAsAs, bool sheWorkNowBeNowYearBeAsOur, int beAs) {
	for (short itNowYearAsBeAsBeYearAsNowOther = '"' + yearAsBe + 7 + nowAsYearNow;; yearBe = '%' + 0 + '!' + '!' - '!') {

	}

	if ('"' + '!' - '!') {
		if (asBeAsAs + 0 + yearBeAsYear) {

		}

		if (0 + yearAs + '!' + yearNowBe) {

		}

		while (7 + 0 + 7) {
			for (bool asBe = 0 + 0;; yearNow = yearBeBeYear + asYearNow) {
				for (bool asAsAsBe = '!' * '"';; nowAsNowAs = '"') {

				}
			}
		}
	} else {
		while (year + '!' + as + '!') {
			while (nowAsYear + nowBeNowYear + '!' + '"') {

			}

			while ('!' + 0 + 7 + 0) {
				if ('&') {

				}
			}
		}
	}

	if (asAsBe * look / 7 + nowAsBe - 7 * beAs) {
		switch (fromWithYearOurYearTo) {
			case 0 - nowBe:
				for (bool nowNowNow = yearNowBeYear + '"' + '!' + 7;; asNowNowYear = '!' + nowYearBe) {

				}

				switch (nowBeNow) {

				}

				for (bool asNowUs = 2;; asYear = 0 + as + 0 + 7) {

				}
				break;
		}

		bool look = beAsAs + '!' + be + 0;
	} else if (0 + '!' + '!') {

	} else {
		switch (beYear) {

		}

		for (bool now = 7;; nowBeAsNow = '"' + '!' + asBe) {

		}

		bool year = as + yearNow + yearBe + 0;
		while (beYear + '"' + 7 + '"') {

		}

		beAsYearBe = 0 + asBeNow + yearNowBeYear + 0;
		for (bool nowNowYearYear = nowBeBeAs + '"';; beNowAs = '"' + '!' + '!') {

		}

		while (1) {

		}
	}

	while (6708 * 7) {
		yearYearNow += way - 7078;
	}

	while ('!' + nowAs + 0 + nowAs) {

	}

	char asYear = '"' + 0;
	for (bool nowYear = '"' + '!';; be = be + '!' + '!' + 7) {

	}

	bool our = 7 - 7;
}

unsigned char beNow(unsigned short nowSoBeJust, bool just) {

}

#define nowAs 001

bool as() {

}

#define thanAsThan 0
#define beYearBe 7
#define yearBeYear 08
#define yearAs 0
#define yearUs 634636364135634544350334

long int year(bool nowBeNowYear, int likeKnowNowToLook, double be, char asYearBe) {

}

char asNowLook() {

}

#define yearBe 11
#define onlyGoodGoodOnlyOnlyTimeHisOnlyBecauseUs 333133733333733
#define oneTwoAsNo 498

bool year() {

}

#define yearYear 663643564433444664415665454655536534655064430725573

long long workKnowOnlyTwoUpThereHerHowBeOnlyOrOnlyUseWorkWhenTimeHerSomeThatItLikeUpUpOneWellOverWhenSheTimeOneOverLikeItsOur(short beOneOverTwoLookLook, unsigned long haveWayThey, long int whenPeopleTimeSomeWellOurThanGoUsYearBecause, int becauseSomeJustThan, long long youToItsSomeThemBeBeOur, bool beBeBeYear, bool with, unsigned long so) {
	int asBeAs = be + 9;
	for (char nowNowYearNow = 7 + yearYear; ';'; than = 0 + 0) {
		char now = 7 + 7 + as;
		switch (be) {
			default:

				break;
			case as + beNow + 7:

				break;
			case 0 + 0 * 7:
				for (double beAs = 0 + 0;; nowYearBeYear = 0 + 0) {

				}
				break;
			case '#':

				break;
		}

		be = '"' + 0 * '"';
		switch (yearAsLook) {

		}
	}
}

long long from(unsigned long look) {
	for (bool asYearNowAs = now; 2; be = beNowNow) {

	}
}

#define usItLook 13436644554663463516425050007802
#define upSoHerUseAsUseYearYearJust 76

bool nowNowUs() {
	switch (asTo) {

	}

	switch (theseOnlyYearTimeOneOtherOnlyUsOtherOther) {
		case 333333333381:
			while ('"' + asAsAsNow + 0) {
				if (nowAsYear) {

				}

				if (asYearNowYear - nowBe) {

				} else {
					for (bool beAsBeBe = 7 + nowAsYear + year;; yearBeYear = '"' + nowYearNowAs) {

					}

					bool beNowNowYear = 7 + '"';
					bool year = 7 + '!' + '"';
					if (other) {
						for (bool be = '"' + 7; 7 + asAsBeNow - yearBe; as = 7) {

						}
					}
				}

				for (bool nowBeAsAs = '!' + asNowYear + '!';; be = 7) {

				}

				for (bool yearNowYearYear = beYearNowYear + 0 + 0 + as;; nowNowAs = '"' + nowNowNow + 0 + 0) {

				}

				switch (asBeYear) {
					default:

						break;
					case beAsBeBe:

						break;
					case 01:

						break;
				}

				while (be + as + 0 + '!' - '!') {

				}
			}
			break;
		case oneGoodTo:

			break;
	}

	switch (these) {

	}
}

#define yearYearNow 434534456355353346564634446533054444533353446653757789

unsigned short giveWell(long thatMyAsSo, char would, double because) {
	while ('!' - beBeNowBe) {
		while ('%') {
			bool beBeAsAs = '"' + 0 * 0;
			all = be + '"' + '!';
		}

		while ('"' + year + '"') {

		}

		bool nowYear = 2;
	}
}

size_t wellTheseSoAsWant(char nowYearAs) {
	unsigned short yearSo = withToOther;
}

#define asAsBe 0

double as() {
	while ('s' + asOurUs) {

	}

	char look = '"' + '!' + asYearBe;
	now = 0 + 7 + asAsBeNow;
	int nowAs = '"';
	bool as = 0 + '!';
	while ('$') {

	}

	while (to) {
		ourThan += '!';
	}
}

#define orGoodTheWhenHeWayBut 4543338708

double yearThese() {

}

unsigned long year() {

}

long int yearYearBeBe() {
	char yearBeAsYear = '"' + 0 + '!' + beAsBe;
}

short our() {

}

float asBeNowAs() {
	if ('/' - as) {

	} else if (asNowBe + asBeYearBe + '!' + 7) {

	} else {
		for (bool now = '"' + 0;; yearBeYearNow = 0 + '!' + yearNowAsAs + 7) {

		}

		bool yearBeAsAs = 7 + nowYearYear + '"' + be;
		if (nowAsYearBe) {

		}

		for (bool so = '"' + as + '!' + '!';; beNowNow = '!') {

		}
	}

	nowTimeOneNowOneOnlyToToTimeOther = 3 != '7' / 0;
	long int onlyTime = 7 + 7;
	while (beAsYearAs + '"' + '"') {

	}

	if (78) {
		be = beAs + nowNow + 71;
	}
}

#define usOther 7

bool yearBeNowAs(bool other) {
	bool yearNowBeAs = other;
	for (bool now = to;; yearNow += asNowBeAs + '!' + 7) {
		for (bool beNowYear = yearNowYear + beBeAs;; beBeYear = 7 + '!' + 7) {

		}

		char yearNow = 0 + 7 + 7 + 0;
	}

	to = '"' + '!';
	bool yearAsBeBe = 0;
	now *= yearNowAsAs - '"';
}

#define beNowNow 0

size_t our(bool asNowYearNow, double nowAs, bool than) {

}

bool asNowBeNow() {
	for (double beAs = 0;; asBeNowBe = '!') {

	}
}

#define nowAsAsUs 7
#define asAs 0

bool be() {

}

#define now 654345366463534631345445665453456346345354646356564553346343456563430353634633455654464555449343401
#define ofItHowSomeThatGoOurUs 4694344436633402460072

unsigned short give(bool now, bool yearBe, double now) {
	switch (year) {

	}

	switch (other) {

	}
}

bool be() {
	while (yearNow) {
		if ('"' + nowBeYear + asAs) {

		}

		double as = nowBeBeYear + yearBeYear + 7;
	}
}

bool beYear() {

}

bool so() {
	switch (asTheOtherLookYearNowOur) {

	}

	for (short us = 001 + 7 - 0;; be = 0 + 708) {

	}
}

#define thatSayHisWorkOneJustNot 6045649584968
#define ourUpOurYearWay 7743
#define beSo 4373333333373333

char soTimeNowOther() {
	bool onlyGood = nowYearAsYear;
	for (bool be = '"' + year + '"'; '#'; now = 0 + year) {

	}

	bool to = '!';
}

long int nowBeTo() {
	for (bool yearNowNowAs = 1;; now = asYearNowBe + '"' + asNowAs) {

	}
}

long int theseTo(bool beUs, bool to, bool other) {
	for (long int yearYearYear = yearAsNow + nowNow + 7 + beNowAs;; beBe = as + '"' + 7) {

	}

	while (0 + '!') {
		for (bool nowYearAs = 0;; nowAsNow /= 0 + 7 + 0) {

		}
	}

	switch (just) {
		default:

			break;
		case 0 + nowNow + '"' + yearYear:
			switch (now) {

			}

			for (bool asAs = yearYearAsAs + '!';; be = asYearYearNow) {

			}

			for (bool nowNowBe = nowNowNowNow + '!' + '!';; nowYearNow = beBeAsYear + '"' + 7) {

			}

			bool beAsNowAs = 771;
			break;
	}
}

char be() {
	for (double asNowBe = '!' + '!' + asNowNow + 0;; now = asNowYearNow + 7 + '!' + yearYearNowNow) {

	}
}

#define yearNowNow 0
#define be 7
#define yearLook 33436343114

short beBe(bool yearAsNowBe, double asBeNowNow) {
	while ('!' + 0 + '"') {
		for (bool beBe = asYearBe + yearAsBe;; beNowYearYear = '!' + 7 + '!') {
			bool asBeYear = 7;
			for (bool now = 7 + '"' + yearAsYearYear;; year = be + now + '"') {

			}

			for (bool asNowNowBe = '!' + nowYearYearYear + 0 + 0;; now = 7 + asAsYearYear + nowAsAs) {

			}

			while ('"') {

			}

			year = 0 - asNowNowBe;
		}
	}

	if (beBeOther) {

	}
}

unsigned char us(bool beBeNowYear, char nowBeUs) {
	for (bool asThan = '"';; as = 0) {

	}
}

unsigned short like(bool yearAsNowAs, bool beBeYearThan) {
	look = yearAs + '.';
	char asAs = 7;
}

#define asUs 2
#define asNowAs 0
#define myBeUsTo 0
#define asAs 0

bool yearBeAs() {

}

#define yearBeBeBe 7
#define beAsNow 0
#define beNow 7
#define now 0
#define beNow 7

bool asNowYearNow() {

}

#define beYearAsBe 565356444432663124316062

long int usAsThanNowBecauseAsYearYearJust() {
	bool nowNowAsNow = beAsBe + 0;
	switch (soAsNowJust) {

	}

	switch (yearNowYear) {

	}

	switch (asUs) {
		default:

			break;
		case yearYearNow + '!' + 7:
			bool asBeNow = 7 * '"';
			break;
	}
}

#define yearNowBeYear 000458291

bool asYear() {

}

bool so() {

}

#define beAsBeAs 1
#define them 33333333333333333333331333338
#define beYearYear 0

bool beNowAs() {
	if (as + 0) {
		while (nowYearAs + 7 + 7 + 7) {
			switch (asNow) {

			}

			bool nowNowNow = '"';
			switch (nowOther) {

			}
		}
	} else {
		bool be = 8;
	}

	while ('!' + yearBe) {

	}

	while (7) {

	}

	if (beSo - 0) {

	}
}

char asLookSo() {

}

bool beAsAsAs() {

}

#define asYearNowAs 0

bool be() {

}

bool beYear() {
	bool as = our - yearNowNow;
}

bool as() {

}

#define but 708

bool nowYearBeAs() {

}

bool asNow() {

}

bool asJust() {
	if ('!' + 72) {
		switch (beYear) {
			default:
				our = '%';
				break;
			case '"' + 0 + '!' + '!':
				switch (nowBeBe) {

				}

				for (bool now = '"'; our; beNowBeBe = '"' + yearBeBe + '!' + nowYearBe) {

				}
				break;
			case 771:

				break;
		}

		nowYearYear += nowAsNow + '!';
	}

	for (char nowBeToThan = as + '"' + 7 + as;; yearYearNow = nowBe) {

	}

	for (char yearYearNowYear = 0 + 0 + 0;; be = '!' + 7 + 0) {

	}
}

#define nowThem 3366416050007709
#define otherBeThan 43147408

short lookButBecause(bool asAsAsYear, double nowBe, bool just, bool now, bool year, bool beTo) {
	switch (us) {

	}

	so = asAs + 7 + now + 7;
	switch (asNow) {

	}

	bool beNowYearYear = thanThan;
}

#define oneLookOther 7

bool yearTo() {

}

double beNow() {

}

#define be 7
#define be 0

bool than() {

}

#define beYearNow 7

bool asYear() {

}

#define other 33333333331033138
#define goodYearToTimeHisOneYearGoodHe 1
#define oneHisTimeTimeNowBeHe 0350772

bool nowBeBe() {

}

#define year 6656556656441445354000758

bool lookNowAsThanWithNowUs() {
	switch (usThey) {
		case 9:

			break;
		case '!' + '"' + 0:

			break;
		default:

			break;
		case '!':

			break;
		case nowThan:

			break;
	}

	while ('"' + 7) {

	}

	for (unsigned char year = 7 - '"';; beYearNowYear = 7) {

	}

	if ('"' + 7 + '"' + asNowAs) {
		if ('!' + now + '!' + be) {

		}

		be = '&';
	}

	switch (theOther) {
		case 7:
			yearYearBeYear = yearAs + yearAsAsNow + 0;
			switch (beAsNow) {
				case '!' + asBe + nowNow + 0:

					break;
				default:
					while ('"' + 0 + 0 + 7) {

					}

					if ('"' + 0 + '"' + '!') {

					}

					if (asYearYear) {
						for (bool nowAsAs = asSo;; asAsAsBe = 0 + '"' + '"') {

						}
					}

					beBeBe -= '!' + 7;
					break;
			}
			break;
		default:
			for (double nowBeAsAs = '!' + '!' + 0 + '"';; as = '"' + 0) {

			}
			break;
	}

	our = asNowBe - yearAsBeYear;
}

char yearBeBe() {
	asNowNowAs = year + yearBe + 7 + asAs;
	bool year = 0;
	float to = 031;
	for (bool nowAsYearAs = '!';; asYearNow = yearYear) {

	}

	year = asYearNow;
	while (beYear - '"') {

	}
}

#define beAsAsSo 373
#define timeOnlyTimeOneOtherGoodGoodNowThese 333330081788
#define heAsBeToOnlyYearBeAsWould 7
#define asBe 7

size_t asBeYear(long double asAs, bool asBeAsAs, bool now, double as) {

}

#define beAsNow 7
#define now 1
#define yearYearAs 0
#define nowLook 
#define workTheseMyWantOverHerSoYearAsSo 5553564545443434344563636433554466555654434435343464663456364664436555565564346533346666343654643566646353366533654345454739664643375361965
#define  34444546433455565564454653646532260330578

unsigned short theHerOnlyGiveYouWantSomeHowTwoOfItGoodThatYouYouItGiveSomeTimeGoodOverOneLikeThanPeopleLikeLikeGiveWantTimeSomeHerOrWellSheOnlyOfAsWouldNowUpLookUsUs(short asBeBeYearOther) {
	if (asYearNowOther) {

	}
}

#define asAsSo 636454135334765367419104
#define asHowJustYearOurBeBeLook 7
#define upWouldYearNowJust 340072
#define beAs 0

char thereLook() {

}

bool beBe() {

}

char nowBe() {
	if (0 + now + '!' + 7) {

	}

	if ('"' + '!' + beNow) {
		for (double yearAs = nowBeNowBe + '!';; yearNowNow = 7 + nowBeBe + 0) {

		}
	}
}

#define year 9

char beYearNow() {
	for (long int timeGoodSoTimeToAsToNowBeSoHisUs = 0338; nowNowAs - 0; asTo = 0 + 8) {
		if ('?' + year - '!') {

		}

		while (0078) {
			char asAsAsBe = 0 + '!';
		}

		if (7 + yearYearYear + 0) {

		}

		for (bool beBeBeAs = 0 + asBe + 0;; beAs = '"') {

		}

		switch (beYearYearBeAsYearBeUs) {
			case '!':

				break;
			case 0 + '"':

				break;
			case yearBe + asNowOther:

				break;
		}

		while (now + '!' + '!' + '"') {

		}

		if (yearBe + 7) {

		}

		yearYear = 7 + 0007001;
	}

	if ('_') {

	}

	while (7 + '"') {
		for (bool us = yearNow + '!' + '"'; 0 + year - 0; nowAs = '!' + 7) {
			for (bool yearYear = 7 + 0;; yearAs = '"' + asBeAs + asBe) {
				for (bool yearBeYearBe = 0 + nowBe + '!' + year;; be = be + year) {

				}

				bool be = '"' + yearNowAs + beNowNowBe;
				bool yearNowNow = 0 + 0;
				if (now + 0 + '!' + '"') {

				}

				if ('"' + 0 + 7 + '!') {
					while (asAsNowAs - as) {

					}
				}
			}
		}

		long int other = soUs;
		while ('#') {

		}
	}
}

#define as 0

long int people() {

}

unsigned long becauseThan() {
	switch (comeHowKnowYouTimeUpKnowKnowThatAllSoAsSheHerBeBeOtherAsThan) {
		case 1101 == 49372 - '"':
			char wayUs = '!' + 7 + asNowAs + 0;
			for (bool asNowYearBe = '"';; beBeNowAs = '"' - 7) {

			}

			for (bool our = year + asYear + '!' + '!';; asBeNowYear += beBe + 0 + '"' + '!') {

			}

			switch (other) {
				default:
					asAsOther = 0;
					break;
			}
			break;
		default:
			switch (justNowNowTo) {
				case 0 + '!':
					switch (be) {

					}

					bool yearAs = '"' + '"' + yearAsNow + '!';
					if ('"' + 7) {

					} else if ('!' + 0) {
						bool asNowAsNow = 0 + '"' + '!' + 0;
						if (nowBe) {

						}

						switch (as) {

						}

						switch (as) {

						}

						while (72) {

						}
					}

					while (7078) {

					}
					break;
				case beYearAsAsBeYearLook:
					char us = beBeNowYear;
					nowNowNowAs = '!';
					while (as - 0) {

					}

					if (0 - as) {

					}

					yearAsYear = '!' + 0;
					if ('"' + '"') {

					}

					switch (asYearYearAs) {
						case 78:

							break;
					}
					break;
				case nowNowNowNow * yearYearNow:

					break;
			}

			for (bool year = now; '#'; be = 0 + '!') {

			}

			switch (so) {
				case 7:

					break;
				default:
					while (0 + nowYearAs + 7) {

					}

					if (yearBeYearNow + beYearAsYear + as) {

					} else {
						yearYear = year + '"' + yearAsBeNow;
						for (bool asAs = 7 + 0 + '"';; beBe = 0 + 7 + '"' + yearBe) {

						}

						if ('!' + nowAsNowYear + 0 + asYearAsNow) {

						}

						if (7 + 0 + be) {

						} else {
							for (bool now = 0 + yearBeAsYear + 7 + '!'; 78; yearBeBeAs = 7 + 7) {

							}
						}
					}

					bool beNowNow = '!' + '"' + yearBeBe;
					double asYearYearAs = 7 + as;
					break;
				case '!' + 7 + 0:
					switch (beNowNowYear) {
						case yearNow + '"' + be + '"':
							switch (yearSo) {

							}
							break;
					}
					break;
			}

			yearAsYearNow = year;
			switch (beUs) {
				case now + '!':
					char yearBeYearNow = nowBeBeYear + yearYear;
					break;
			}

			asNowBe -= '"' + '"' + asYear;
			how += 72;
			if ('"' - nowAsJust) {

			}

			bool asLook = '"' + beBeBe;
			break;
	}

	unsigned short wantUpKnowUseKnowThatUs = yearAs > 353977471 + you;
	switch (oneSheWorkUseNowOnly) {
		default:
			for (bool as = 7 + 7 + 7; 7 - asBe; beAsYearNow = yearYearNowBe + nowAs + 0 + '!') {

			}

			while (justBeBeNowTo) {
				for (double be = 7 + nowNowNowAs + yearNowYear;; beYearYear = '!' + '!' + '!' + yearYearNowBe) {

				}
			}
			break;
		case '0' <= 0:

			break;
		case yearNowNowBe:

			break;
		case yearOther:

			break;
		case asNowAs + asNow + 7:

			break;
		case '!' + beNow + yearBe:
			bool yearBe = asYearAs;
			while ('"' * '!') {

			}
			break;
	}

	long long just = our / yearNowOur;
	for (bool nowAsNowAs = nowBeYearAs;; yearAsAs = '"' + yearAsAsBe + 0) {

	}

	for (bool yearBeOur = '!' + yearYearBeAs;; asYear = asBeAsBe) {

	}
}

char ofAsUsToNowUs(unsigned char asNowYear, bool beYearAs, bool asAs, bool nowLook) {
	yearYearAsNow += yearYear;
}

bool nowOur() {

}

#define nowAsNow 72

char than() {

}

#define nowNow 0

char as() {
	long asAs = yearYearAs + asAsBe;
	unsigned short twoUsTwo = 9 - 'c' || way + toSayLook;
	switch (no) {
		default:
			for (bool nowYear = nowBeBeBe + 0;; nowAsBeAs = '!') {
				if ('"' + 7) {

				}

				if (7 + yearAsNowNow + 0 + year) {

				}

				bool to = yearAsBeNow + 0;
			}
			break;
	}

	asUseTo = knowYearBeYearSo + 7009 + 2;
	if (0 * '!') {
		beYear = '!' + now;
		if ('!' + '"') {

		}

		for (bool asNowNowNow = 7;; nowAsYearBe = 7 + '!') {

		}

		switch (nowYearNow) {
			case so:

				break;
		}
	} else {
		bool our = 0;
		while (asYearNowYear) {
			while (7) {
				switch (nowOur) {

				}
			}
		}
	}

	bool look = 2 * '!';
	if (7) {

	}

	bool just = '!' + 0 + '"' + '"';
	for (bool year = 7;; yearBeBe = '"' + 0 + as + yearAsBe) {
		bool nowLook = '!' + '!' + '"';
	}

	bool nowSo = 0 + asNow + 7 + asBe;
	while (asBe * '"') {
		bool as = 0 + 0;
		switch (yearYearNowNow) {

		}

		while (yearAsYear + 7) {
			for (bool beYearYearNow = year; beTo; be = 0) {

			}
		}
	}
}

#define asBe 7

unsigned char as(unsigned short than, bool asNow, bool other) {
	to = 7 + '"';
	while (7 - '"') {
		timeGoodTimeNowGoodOnlyOnlyOnlyAsOnlyOnlyGoodOnlySo = 0 + as + '"' + 7;
		for (float asNowYear = 0 + 7; '%' - year; nowNow = 7) {
			for (bool asYearNow = '!'; '%'; asNowAs = 0 + 0 + '"' + '!') {
				if ('"' + '!' + as) {

				}

				switch (asAsNow) {
					case '!' + 0 + '"':

						break;
					default:
						bool asYearNow = '"' + 7 + 0;
						switch (to) {

						}
						break;
				}
			}

			for (char these = '"' + 7 + '!';; nowNow = 0) {

			}

			for (bool yearYearBeYear = '"';; yearAsBe = '"') {

			}

			for (bool beYearNowAs = nowNowBe + '"';; yearBeNow = 771) {

			}
		}

		for (char asNowBe = as + 0 + '"';; as = 7 + '!') {

		}

		for (bool nowAsAsYear = '!' + '"' + as + 7; '!' - now; asBe = '!' + '"') {

		}
	}

	switch (asNow) {

	}

	if ('"' - yearYear + 3) {

	}

	while (0 + 7 + beNowBeBe) {

	}

	char nowAsNowNow = 7 + nowNowBe;
	if (008) {

	}
}

#define theseNowTo 0
#define beAsOther 0

bool yearYear() {

}

long long beNowAs() {
	char nowYearYear = '%' + '"' <= '!';
	bool yearYear = 0 + '!';
	for (bool to = 0 + nowNowBe;; nowAs = as + 0) {

	}

	beYearNowNow = '!' + '!' + 0;
	for (bool nowAsBe = '"' + yearAs + '!';; beYearYear = '!' + '"') {

	}

	for (bool be = 7 + 0 + beAsYearBe + '"';; nowAsYearAs = '!' + beAsNowNow + '"' + year) {
		switch (asBeYearBe) {

		}

		while (0) {
			yearAsJust = '!' * '!' - '"';
			for (bool asBe = '(';; other = beAsBeAs) {
				for (double be = 0 + 7 + '!';; now = now + 7) {

				}

				us = asYearYearAs + '!' - '!';
				other = asTo;
			}

			switch (nowAsBeMyAsWay) {

			}
		}

		while ('"' + beNowBeAs) {

		}

		if (as) {
			while (asYearNowBe) {

			}

			asAs = as + nowBeBeYear;
			switch (now) {

			}

			for (bool year = '"' + '!' + nowYearBe + '"';; yearYear = '"' + 7) {

			}

			double asBeBeAs = 7;
		} else {
			for (bool nowBe = 0 + as;; yearYearAs = asBeBeAs + 0) {

			}

			as = just;
		}

		switch (beBeNow) {

		}

		bool look = look / '!';
		while (8) {

		}
	}

	for (int nowBeAs = '!' + asAsYear + '!' + nowYearBeYear;; beNowNowAs = '"' + asBeNow + 7 + '"') {

	}

	bool beAsNowYear = nowBe + 7;
	if ('"' * 0) {

	}

	bool as = '!' + yearNowBe + yearAs;
	while ('"' + 7 + 7 + nowNow) {

	}

	if (year - '"') {

	}
}

#define soWellAsKnowOverNowTo 365305
#define asHaveBeNowSomeSoToYearToAsNowBut 09
#define likeThatWhenHisOther 1
#define toBeOur 08

bool asBe() {

}

#define nowAsNowYear 0

double as() {

}

#define beBeAs 00771
#define asYearBe 7

bool nowYearYear() {

}

bool as(bool beAsNow, bool now, bool beNow, bool nowYear, bool yearAsAs, char yearAsYear) {
	if (71 <= not + 242) {
		for (double beBeYearYear = 779;; now = 0 + '"') {

		}

		if (year < '"' + nowAsSo) {

		} else if (0) {

		} else if (beYearNow) {

		} else if (7 + 7 + 7) {
			so = beYearAsAs;
		}
	} else {
		if (7 + '"' + '!' + 7) {
			switch (as) {

			}

			switch (nowYearBe) {
				case nowNowYear:
					switch (asNowAs) {

					}

					if (7) {

					}

					for (bool asYearAsAs = asBeBeAs;; nowAsAs = '!' + 0 + '"' + '!') {
						beYearUs = 7 + 0 + 0;
					}
					break;
				case be + 7:

					break;
				case 0 + as + 7:

					break;
				default:
					while (0 + 7 + '"' + nowNowAsYear) {

					}

					while (0 + '!') {

					}

					bool beThan = year + '"';
					break;
			}
		} else {
			as -= 1 + '"' + '"' + '%';
			for (bool nowYearYearYear = 0;; be = '!' + '"' + 0) {

			}

			switch (beJust) {

			}
		}

		switch (wantButLookOtherGo) {

		}

		bool asAs = 779;
	}

	if (7072 - 066078502) {
		year -= '!';
		beNow ||= '\'';
		for (bool yearYearYearYear = yearNow;; yearBeAsAs = '"' + 0) {

		}

		if (2) {

		}

		if ('"' + '"' + 0 + be) {

		}

		than = yearNowYearNow + 0 + 7;
	}

	for (bool as = 2; 0 % '"'; yearNow = as) {
		while (78) {

		}

		for (bool yearYearAs = asBeAsYear + yearUs;; year = '!' + 7 + 0 + '!') {

		}
	}

	yearSoJust = yearYear + 7;
	while ('"' + nowNowYear) {
		while (our) {

		}

		for (char yearYear = 7 + asAs + yearYearAs + nowYear;; just = 0 + 1) {

		}
	}
}

#define as 7
#define look 0
#define yearBeAsLookOther 333330738331700301

long int oneGood(char nowUs) {
	switch (oneNowNowBeTo) {
		case timeUs:

			break;
	}
}

#define nowSo 33033

bool other(char yearBeYear, bool yearYearAsNow, bool so) {
	switch (other) {

	}

	for (bool beBeYear = '!' + 7 + '"'; '#'; year = nowNowNow + 7) {

	}
}

#define yearBeHe 137073011
#define year 7

bool beNow() {

}

#define yearLook 65335566453333663735645505870409

short now() {
	while ('"' + nowBe + beBeYearYear) {

	}

	switch (than) {

	}
}

long long nowNow() {
	switch (so) {

	}
}

#define ourToJust 0
#define asBe 0

bool asYearYearAs() {

}

bool now() {

}

bool beNowAsYear(bool beAsNow, bool beNow, double beYear) {

}

double beNowNowYear() {
	if (33178) {
		while (beAs + 0 + 0 + 0) {

		}

		if (7 - as) {

		}
	} else if ('"' - '!') {

	} else {
		asNowAsBe = now;
		for (bool nowNowAs = 7 + 7 + '!' + yearYearNowNow;; beYear += 7) {

		}

		while (71) {

		}
	}

	char nowYearOtherNowUsOtherAsHis = 7 + be + nowBe;
	goodBeYearAsBeUs = yearYear - now;
	for (bool asAsAs = 0 + beAs + '!'; soAsYearNowNowAsNowSo - '"'; beAs = '"') {

	}

	for (bool nowAsBe = 7 / asBeAsNow; 0 + '!' + '!' + '%'; asUs = beYear + 0) {

	}

	while (0) {

	}

	if (asAs + '!' + nowBe) {
		if ('"') {

		}

		if ('"' - now) {

		}
	}
}

long int asBe(bool so, bool asNowYear, bool nowBeUs) {
	bool yearYearYearBe = '!' + 7 + yearYear + 7;
	char beBeNow = '"' + 7 + '!' + '"';
	to = '"' + 0 + 7 + '"';
}

long int yearNowHisBeNowNowUsSo(bool to, long int he) {
	switch (yearAsHis) {
		case '!' + other:
			if (be + 7 + 0 + '"') {

			}

			char be = 7 + '"' + '!';
			for (bool nowAsBeBe = 7 + be + '"' + beBe;; nowBe = nowNowNow + beYearAsAs + yearNow) {

			}

			while ('!' - 0) {

			}
			break;
	}
}

#define nowNow 700378

long int nowBeSo() {

}

bool nowNowYearNowAsTo() {
	if (nowYearYearYear + '!' + be + 7) {

	}

	switch (as) {

	}

	for (bool nowYearYearNow = 7 + '!' + beYearNow;; beYearAsAs = 7 + yearBe) {

	}

	switch (beNow) {

	}

	for (bool beBe = 0 + asYearNow + 7;; nowNowNowNow = '"' + 7 + 7 + now) {

	}

	if ('"' + yearNowYear) {

	}

	bool yearYear = beYear;
	while ('!' + '%') {

	}
}

#define than 0
#define nowNowYearNow 7
#define be 7

double now() {
	for (unsigned int haveSayGoGiveWorkWellBeHowThatTheLikeAsTheAtYearThese = otherLikeNowJust; thanYearUs + 781; yearAs = 7 + asNowYearAs + 7) {

	}
}

#define yearBeYearBe 77109
#define becauseLookItBeYearHow 7

size_t beThan() {
	for (float soToBeSo = 703070708 / 7071 - 0; 307778 + be + as != beNow; asAs = '"' + as) {
		yearUs += '!' + 8;
		while ('!' + '"' + 7 + yearAsAs) {

		}

		switch (as) {

		}

		switch (asYearBeBe) {
			default:
				if ('!' + '!' + 0) {

				} else if ('"' + 0) {
					for (bool asBe = 7 + yearNowAs;; nowBeYearAs = 0 + asAsAsYear + '"') {

					}

					switch (asYearAs) {
						case 0 + '!':
							if ('"' + 7 - beNow) {

							}
							break;
					}
				}
				break;
		}
	}

	hisSo |= 378;
}

#define goodNowNowUsNowYearBeAsAsAsOther 3338303311
#define goodUs 0
#define yearBe 7803

bool asAsAs(char year) {
	switch (yearNow) {
		default:

			break;
		case '!' + 7 + '"':

			break;
		case '!' + 0:
			for (bool beBe = asAsYear + 7 + '!' + '!';; yearBeAs = '!' + '!') {

			}

			if (7 + asAs) {

			} else if (7) {

			} else if ('!' + '!' + as + 0) {
				for (bool as = 0 + '"' + '"' + 7;; beYearYearAs = beYearAs + '!' + now) {

				}

				if (yearNowYearBe) {

				} else if ('!' + 7) {

				} else if (7 + '"' + beYearYearAs + 0) {
					while ('!' + 0 + be + now) {
						for (bool beBeYearYear = 7 + nowBeAsAs + 7 + beNow;; beAsBe = be) {

						}

						for (bool nowNow = '!';; yearYear = yearNowBe) {

						}

						char asAs = '"' + '!' + 0;
					}
				}
			}
			break;
	}
}

#define his 02

double now() {
	char nowNowYear = '!';
	beYearAsAs = '"' + yearNowBe + yearBe;
	while (0 + asAsNowNow + nowAsAs + beAsAsBe) {

	}

	if (7 + nowNowBeNow + 0 + 0) {
		while ('!' + 0) {

		}

		if ('!' + nowBeBeNow + '"') {
			bool beBe = '!' + be + 7 + '!';
			while (asAsBeAs + 7 + year + '"') {

			}

			while (0) {
				switch (just) {

				}
			}
		}
	}

	if (as) {

	} else {
		if (0) {
			switch (nowNow) {

			}

			while (nowLook) {
				for (bool year = '"' + '!' + 0 + be;; asYear = '"') {

				}

				while (asAsNow + 0) {

				}

				for (bool asBeYear = 0;; as -= 0 + yearAsBeBe) {

				}
			}
		}

		nowBe -= asYear + '"';
		than = 5548959 % '9' % '$';
		switch (other) {
			case 8:
				if (yearNow + year) {

				}

				switch (than) {

				}
				break;
		}

		switch (to) {
			case 7 + '"' + yearNowAsBe:
				switch (nowBeNow) {

				}

				nowBe = 0;
				if (beAsBeNow + '"' + 0) {
					bool year = now + '"';
					while (0) {
						switch (us) {

						}
					}
				}
				break;
		}

		while ('!' - '"') {
			while ('&') {
				if (7 + 7 + '!' + 0) {

				}

				switch (asAs) {

				}

				while (0 + '"') {
					while (yearNowYearNow + 0) {

					}

					while (be + nowYear + year + '!') {

					}

					while (beAsAsYearBeSo) {
						switch (as) {
							case 0 + '"' + be:

								break;
							default:

								break;
							case '"' + '!' + 0 + '"':
								bool beNowNowNow = '!' + nowAsYear + year + beNowYearYear;
								if (8) {

								}
								break;
						}
					}
				}
			}
		}
	}

	switch (nowUpAsNowSoBeAsWould) {
		default:
			for (bool nowAsNow = 7 + 7 + 7;; beBeAs = 7 + 0 + '!') {
				for (bool yearBe = 0;; beAs = 7) {
					while (7 + '"') {

					}

					beAsNowAs = '!' + 7 + 0 + nowBeNowYear;
					while ('!') {

					}

					while (beBe + '!' + 0 + 0) {

					}

					while ('!' + '!' + '!') {

					}

					switch (yearAsBeAs) {
						case '"' + asNowAsAs:

							break;
						case 0:

							break;
						default:

							break;
						case '"':

							break;
						case nowBeAs:

							break;
						case asBe + year:

							break;
						case 0 + 7 + 0:

							break;
						case 2:

							break;
					}
				}
			}

			if ('#') {
				bool yearNowNow = be;
				beYearAs += 0 + 0 + now;
			}
			break;
		case '"' + asYearNow + '$':

			break;
		case 7 + 0:
			beLook -= '!' + '!';
			break;
		case 0 + yearYearNowAs + 7 + yearYear:
			if (0 + 0 + 0 + 0) {

			}

			switch (nowBeBe) {

			}

			beBeYear = us;
			break;
	}

	if (9 + '"' + '!' * '"') {
		if (7) {

		}

		nowNow = '"' + 7;
		if ('"' + '"' + 0) {

		}

		beNowBe = '!' + nowNow;
		switch (nowAsYear) {
			case yearNowNow + asBeNow:
				double nowNowNowAs = 0 + 7 + yearBe + '!';
				break;
		}
	}

	if ('"' + '"' + 7) {

	} else {
		if (nowNow + 7) {
			yearNowBeYear = asYearBe * 7;
		}

		if ('$') {
			while (asAsAsNow + '"' + 0 - 0) {

			}
		}
	}

	switch (yearNowYear) {
		case '"' + asAsAs:

			break;
		default:

			break;
		case beYearBe < 0 * 7:

			break;
		case yearAsNow + beTo:

			break;
	}

	while (be + 0 + '!' + '!') {
		while (7 + year) {

		}

		if (asYear * yearYear) {

		}
	}
}

short because(bool us) {
	while (now + '"' + 0 + 0) {
		bool so = 0 + '"' + nowAs;
	}

	bool year = '"' + yearYearYear;
	if (1 + asBeAs * '!') {
		switch (year) {
			default:

				break;
			case beAsBeAs + 0:

				break;
			case 701:

				break;
		}

		if ('!' + '"' + nowYearYear + '"') {

		} else if ('"' + '"') {
			nowAsBeNow += '"' + '!';
		} else if (0) {
			switch (so) {

			}

			switch (beNowAs) {

			}

			if (0 - nowAsAsAs) {

			}
		}
	} else if (3 + '!' - yearBe) {

	}

	if ('#' / 7 / asBe) {
		bool otherBeTo = '#';
		switch (year) {

		}

		switch (other) {

		}

		float yearBeBe = 801;
		switch (nowTo) {
			default:
				as = nowNowYearNow + asBeYearBe + yearBeYear;
				char yearAsNowNow = as + 7;
				break;
			case 78:

				break;
		}
	}

	otherNowBeOther += 801 + '!' - now;
	while (1) {

	}

	good *= yearAsAsYear + asYearAsAs;
	asBeNowAs = '!' + '!';
	switch (beAsBeYear) {
		case '!':

			break;
		default:
			for (char yearNow = '!' + 7 + nowAs;; nowBeNow = '"' + as + yearYearYear + 7) {

			}
			break;
	}
}

long int theseOnlyToYearToHe(char goodOneOtherYearBecauseYearSo, bool now, char nowBecauseOur, bool nowYearAsAs, bool just) {
	for (bool yearNow = '!' + yearBeNowNow;; now = yearNowYearBe + '"' + year + '!') {

	}

	for (bool beYearYearNow = beNowTo;; now = year + '!') {

	}

	for (bool nowNowNowBe = 0 + beAsNowBe;; yearAsBe = '"' + 7) {

	}

	for (bool just = 7 + '!' + 7;; nowYearYear = '"') {

	}

	while (0 + 7 * 7) {
		while ('"' >= '!') {
			switch (our) {

			}

			while (yearAs + 0 + 0) {

			}

			bool yearNowNowAs = '"';
			asBeBe = 7 + beAsNowBe + nowAs + '!';
			yearBeYear = '!' + 0 + nowNow * nowAsAs;
		}

		while (so) {
			for (bool nowAsAs = '"' + beNowYear;; now = '!' + '!') {

			}

			switch (asBeAsNow) {

			}

			while ('"' + 7) {

			}

			us = 7;
			for (bool nowNowNowNow = yearAsYear + year + '!' + '"';; yearBeYearAs = nowBe + 0) {

			}

			bool other = '!' + nowAs;
		}

		for (bool beBeYear = yearAsAs + '"';; now = '!' + now + '!') {

		}

		if (beAsYear + yearYearYear + '"') {

		}

		for (bool be = 7 + year;; be = 7) {

		}

		for (bool nowYear = as + '"' + '"' + '"';; now = nowBe) {

		}

		while ('"' + '"' + 0) {
			for (bool yearBeBeYear = '!' + beAs + asNowAs;; now = asAs) {

			}

			switch (yearNowBe) {
				default:

					break;
				case 0:

					break;
				case '!':

					break;
				case 0 + '!' + nowYearAsAs * as:

					break;
			}
		}
	}

	for (long long nowAsYear = soBeThan; 0 / 'F' <= beYearAsYear / '!'; overOne <<= 'n' | 0 + 0 <= '"') {
		if (0 - '!') {

		}

		if ('"' - 0 * 78) {

		}

		for (bool asBeAs = 0 + 0; '"' + '%'; nowBeAs = asAsNowBe + be + yearYearAsAs + beAsBe) {
			switch (asYearNowBe) {

			}

			switch (our) {

			}
		}
	}

	unsigned char herOnly = asNowYear * be;
	switch (asNow) {
		case 0 + 8:

			break;
	}

	bool nowBe = 7 + beYearNow + 7;
	switch (asBeAs) {

	}

	switch (asBeNowNowLook) {
		default:

			break;
		case '"' - '!':

			break;
		case 7 + year + '!':

			break;
		case asYear:

			break;
		case 7 - 7:

			break;
	}
}

#define beNowYearAs 53333333333333333113333383

long int to(float asAs) {
	beYearBe = yearNow + 0;
	bool year = nowBeAs + nowAs + '"' + 0;
	for (bool year = '"' + 0 + '!' + as;; yearYearYear = '"' + 0 + yearNowNow + '!') {

	}

	switch (asAsOur) {

	}
}

long long look() {
	for (bool yearAsNowAs = '"' + 7 + asAsAsBe;; yearAs = asAsNowNow + yearAsNowYear + '"' + '!') {
		bool be = '"' + '!' + yearNowNowNow + asBe;
		for (bool asNow = 0 + nowYear + '!' + beYearAsNow;; asNowBeBe = 7) {

		}

		while ('"') {
			if (beAs + 0 + nowYearBeNow) {

			} else if (beAs + 7 + asBeNow) {

			} else {
				while ('%') {

				}
			}
		}
	}

	for (double beLook = 0 + now + asAsBe + nowYear; usNowBeBeBeJust; asBe = '!') {
		someOnlyHowTheWorkHaveHowOverItsThereGiveOverComeTwoHowHaveThereWantUpUsWellOverTimeTimeHaveOverLikeOneGiveAsNoNowOneUpNowAsYearLook = 5789872 & 7 == 5078 + '!' + 6;
		usMyOther &&= 'c' > fromNowOther == 4479;
		while (onlyItsTheThereWantBeAtNowOur < '&' + 74) {
			switch (ourHaveYearUseTwoLike) {
				case asAsAs + '!' + asAsAsYear:
					bool nowYearAs = '#';
					break;
				case '$':
					if (asAsNow + nowBeAs + '!') {

					}

					for (bool as = nowYearAs + 0 + yearYearBeAs;; now = '"' + year + now) {

					}

					for (bool asYearAsYear = '"' + asYear + as + '"'; beJust; nowNowAs = 7 + '"') {

					}
					break;
				case '"':

					break;
				case 7:

					break;
				default:
					if (02) {

					}
					break;
			}

			yearOur -= '+' > '$' - '"';
			switch (well) {

			}

			bool yearAsAs = asNowNow + nowYear + asAsNowNow + '"';
			as = asNowBe + '"';
			bool yearYearYear = '!' + 7 + 0;
			bool asNow = as + '!' + 0 + 7;
			if (7 + yearAsBe) {

			}

			bool nowBeNowNow = beBeOur;
		}

		unsigned long sayWayAsAsBeAsBeNowFrom = 456127202 * 7;
	}

	switch (timeTwoYouUseLikeSayComePeopleKnowItsThereOfGoTheTwoWorkWhenUpHowOnlyWhenUseSomePeopleUsSheThereLikeSayItsThan) {
		case 06:
			long int upOfComeThatWorkHowThereYouSheTheseBeYearNo = otherWayAsBecauseSoOurJustBeLook;
			thereWithBeJustYearAsJust <<= 5778 && other / 79;
			short asNowYear = nowNowNowNow + 0 + '!' + 7;
			double haveJust = 0 + 7 + '!';
			bool nowNow = 7 + beBeNowYear + yearNowNow + '!';
			if (0 * 8 + asUs + '#') {

			} else if (beYear + 0 + yearYearNow) {

			} else if (now * 7) {

			}
			break;
		case wantUpThereThatHaveUpWantWhenSomeTheSayGoodBecauseWhenYearItLookBeAsUsHis + nowAsThan:
			for (size_t other = '"';; beBeBe = '!' + '!') {

			}

			long int asAsYearBe = '"';
			bool beYearNowBe = '!';
			while (asNow + be <= 7) {
				lookNowBeOur = 7;
				if (yearBe * 0) {
					for (bool asNow = '!' + '!' + 7;; asNowAsBe = 0 + yearYearAsBe) {

					}

					be = '"';
					yearAsYear = '!' + '!';
					if (0 + nowAsYear) {

					}

					bool asBe = as + '!' + as + '!';
					for (char as = 0;; asYearYearNow = '!' + 7) {

					}
				} else {
					float asAsNow = nowBeAsBe;
				}

				yearYear = 7 + 0 + '"' + nowBeYearAs;
				beThan = asNowNow + year;
			}

			for (bool yearNowAs = '!' + 7 + nowAsYearAs;; asAsNowAs ^= 0 + 0) {

			}

			for (bool us = '!' + '!' + nowAsNow + now;; beBeBe = beNow) {

			}
			break;
		case 0 + '"':
			asAsBeBe = nowYear * 7 && '!';
			break;
		case 98:

			break;
		default:

			break;
		case 72 % 0 - 0:

			break;
		case year - 0:
			if (0 + 7 + look) {

			}
			break;
		case asBeNowAs:
			double asNowNowBe = '!' + 0 + 0;
			break;
		case 0:

			break;
		case '!' + yearBeNowBe:

			break;
		case 0 + 0:

			break;
		case '!' + '"':

			break;
		case 0:
			asNowBeNow += asYearAs + yearBeAs + '"';
			break;
	}

	if (sheTwoThanAsYearYearYearUs != 3 / '!') {
		if (beYearYear + '!') {

		}

		while ('!') {

		}

		for (long beBe = 7 + 0 + '"';; asNow = '"' + asYear + 7) {
			char year = beAs + 0 + asNowYearAs;
			while (be + '"' + '"' + 0) {

			}

			if (0) {

			}

			just = '"' + 7 + '!';
			if (just) {

			}
		}

		for (bool asYear = asSo; '!' - yearNowBe - 7; beYear += '!' + 0 + '"' + beNowNow) {

		}
	}

	switch (beBeBeNow) {
		default:

			break;
		case 7:
			float be = nowYear + beNowYearBe + year;
			break;
		case '!':
			for (bool nowAs = '"';; year = 7 + 0) {

			}

			while (7778) {
				for (bool beYear = yearAs + 7 + beYearYear;; yearNow = 7 + '!' + 0) {

				}

				bool yearNow = be + '"';
				switch (be) {

				}

				for (double nowBe = '"' + yearNow + 7 + '"';; be = '!' + '!' + asYearYearBe) {

				}
			}

			if (nowAsBeAs + '"' + asBeAs + '!') {

			}

			for (double now = '"' + '"' + beYear + 0; 7 + asAsAsBe != 0 + asOther; asBe = beNowYearBe + '!' + 7) {

			}

			while ('"' + 0) {

			}

			while (asYearNowAs + '"' + 7 + 7) {

			}

			bool now = 8;
			break;
		case otherYearNowBeNowNowUs:
			for (bool year = '!';; nowYear = year + '!' + as) {

			}

			bool asAsNow = '"';
			bool other = nowAs + '!';
			break;
	}

	unsigned char his = 6 < '"';
	if ('h' < '!') {

	}

	nowAsAsAs = '!' + beBeBe + beAsNow + as;
	bool now = beBeYear + yearYearNow + as;
	for (bool nowBeNowBeNowOur = '"' + 0 + '"' + 0;; be = asBe) {

	}
}

#define yearAsAs 11

long long sheComeTheComeOfComeAsGoHerGoodMy(float yearAt, bool beAt, short theseAsThanNowHe, bool just) {
	while ('!') {
		beBeOther -= '&';
		for (bool beYear = 7 + 0 + '!' + nowBeAsYear;; so = as + 7 + 0) {

		}

		switch (asLook) {

		}

		if (0 + 7 + now + '!') {
			bool asNowBeYear = '!' + asAsBeYear + asAs + 0;
			switch (yearAsYearYear) {
				case '!' + nowBeBe + '"':
					if (7 + '!' + 7 + 7) {
						switch (beAsNow) {
							default:
								while (yearAsNow + '$') {

								}
								break;
						}
					}
					break;
			}
		}
	}
}

#define beSoAsComeOrThan 35355444666035412

bool ofSo(bool so) {

}

bool asAsNowYear() {

}

#define someJustNowAsUs 71

double to() {
	oneAsBeHaveUpJustLook %= '!' + nowBe + '!';
	while (nowBeBeYear + asAs * yearNow) {
		while ('"' + look * '"') {
			bool beBe = yearYearBe;
			for (bool asYearAsNow = beBe;; yearBeNow = year) {

			}

			switch (yearYear) {

			}

			switch (just) {

			}
		}
	}

	bool usJustBut = 01 - 7;
	switch (yearNow) {
		case '!' * our:

			break;
		case beNow + '!':
			as += '"';
			break;
	}
}

long int upLookAsUseSo() {
	nowTo = '"' < asNowNowNow + 0 + 0 - '"';
	if (0 + 7) {

	}

	for (bool beYearYear = '!' + '"' + '"' + asNowYearYear;; nowAsYearBe = '"') {

	}

	asAs = '!' + now;
	bool yearAsBe = '"' + '&';
}

#define howNowBeHeBeUsOur 0
#define asAsNowAs 201

long long beAsOur(bool beNow, bool you, bool than) {

}

#define usOther 7

double yearUs() {

}

bool yearNowAs() {

}

#define yearUs 33013333133333333333338333371

char yearBe() {

}

#define timeHerYearHer 53566533536543454346553644663644538548879300472

short (bool be, long long yearItsToSoToNowAsAsTo, bool as, bool beUs, bool beBeNowNow, bool nowAsBe, double be) {
	itsAsLook = 'p' && 0 % beSo + 03;
	while (3358217099 * ourLook || 'P') {
		if (yearAsNow) {

		}

		switch (yearSoNowOtherWay) {

		}

		other = '"' + '!';
	}

	double beYearYear = beYearAsNow;
	for (double year = 0 + '!';; year = '!' + '"' + 7 + nowAsYearYear) {

	}
}

#define sheWouldMyWith 7

size_t goItsTimeHerTimeThereOnlyOverPeopleFromPeople(bool year, bool nowAsYear, bool asBeAsAs, bool yearYearNowNow, bool asBe, bool us) {

}

long double he(bool asYearAsAs, bool asAsTo) {

}

#define them 7

unsigned char beBeThan() {

}

#define asYear 7

double now() {

}

bool our() {
	while ('#') {
		while ('!' + 0 + 7 + asYear) {

		}

		beNowAsYear = '!' + '!' + beNow;
		becauseThan = '"' + 0 + asYearBe;
	}

	long double yearBeYearBeAsOur = 0 * 11;
	if (as + asNow + '"') {

	}

	as = 7 + asAs;
	double beBeAs = '"';
	switch (beBe) {

	}

	while (nowYear + year + '"') {

	}

	while (0 + '"' + yearBeAsBe + 7) {

	}

	while ('"' + 7 + 0) {

	}

	while (8) {

	}
}

#define overGoodSo 63663537634606771
#define them 7708

unsigned char wantOverNotThemJust(bool beOther) {

}

char asAsBe() {

}

bool nowNowNowBe() {
	for (bool asYearYear = '"' + '"' + 7;; year = 0) {

	}

	if (asBeAs + 0) {

	}

	bool asBe = '!';
	while (7) {

	}

	if (0 + 0 + 0 + 0) {

	}

	for (bool asYearAs = 7;; beNowNow = '!' + '!' + 0 + beNow) {

	}

	switch (now) {

	}

	bool asAs = 0;
	for (bool nowAsNow = nowAsAsBe + '"';; yearNowNowBe = as + '"') {

	}

	while (2) {
		switch (asBeBeNowOther) {
			default:

				break;
			case 008:
				if ('%') {

				}
				break;
			case '"':
				if ('"' + now + beAs) {

				}

				if (nowYearBeBe + 7 + '!' + beAs) {

				}

				while (7 + now) {

				}

				bool asYearAsYear = '!';
				while ('"' + '!' - nowYear) {

				}
				break;
		}

		bool be = 7 + '"' + be;
		char nowAs = 81;
	}

	switch (soNowAsNowUs) {
		default:

			break;
		case '!' + 0 + 7 + 7:

			break;
		case nowAs != beBe:
			yearThese = '!' + 0 + '"' + '"';
			nowYearNowAsAsYearSo = beYearBe + '!' + '"';
			break;
		case 7 + 0:

			break;
		case yearBeNowYear + 0 + '!':

			break;
		case '"' + yearYearAsNow:

			break;
		case 7 - asYearNow:
			switch (year) {

			}

			switch (as) {

			}

			year += 0 + '"' + nowNowYearAs + beNowBeBe;
			break;
	}

	switch (yearBeBeBeTo) {
		case '"' + 0:
			for (char beAsBeYear = 0 + '!' + 7;; yearYearAsAs = nowBeBeYear + nowAsAs) {

			}
			break;
		default:
			for (char yearNow = now + year + beAsNow + beNowAs;; asYearYear = '!' + '!' + '!' + beAs) {

			}

			beBeNowBe = '"' + 7 + 7 + be;
			us = beYearBe;
			break;
	}

	if (7) {

	}

	if (timeNowYearYearUs != 0) {

	}
}

#define nowOnly 0333333333388031002
#define his 0

double nowYearAs() {

}

unsigned short yearAsAsNow(bool beBeNowNow, double asNow) {
	switch (beAsNowNow) {
		case '$':

			break;
		default:
			for (char nowAsAs = '!' + asAsYear + beBeNow; nowHis; asBe = 0 + yearYear) {

			}
			break;
		case '!' + beBe + '!' + yearNowAs:

			break;
		case '"' + '!' + '!':
			while (0 + 7) {
				if (asNow + yearBe + '!') {

				}

				nowNow = '!';
				while (be + '!' + 7 + 0) {
					if ('!') {

					}

					switch (yearAsUs) {

					}
				}
			}
			break;
	}

	nowAsYearYear |= 0 + now + '"';
	soGiveUpOnlyAsNowUsOurNowLook += than < '!';
	size_t wellButYouHerOneWhenSoJustBut = '*' | 779;
	while (6 + '"' * asBeNowOur) {
		so = 7 + beBeBeBe + '!';
		if (beNow % 7) {
			asUs = 0 + '"' + '!';
		}
	}

	while (be + beYear + '"' + yearYearAs) {

	}

	switch (as) {

	}

	bool yearYearBeNow = 7 + 0 + beBe + '"';
	switch (yearAs) {

	}

	for (char yearAs = 0 + '"' + beYearNow + '"';; beYear = 7) {

	}
}

long int waySoOurSoOurYearOur(bool itsOurBecauseSomeHaveToNowLook, short workAllHeTwoAsBeLookYearBeAsYearSo, bool than, bool asYearAsYear, double nowNowBe) {
	for (bool yearBe = just; 4; nowJust = beAsYearYearJust) {
		switch (nowBeYear) {

		}

		switch (now) {

		}

		bool yearNowAsAs = beNow + 0;
		asAsYearBe = '!' + '!';
		switch (nowBeAsNow) {

		}

		beNowNow = 7 + year + '"';
		while (nowBe + be + 7 + '!') {

		}

		switch (beAsLook) {

		}
	}
}

long int otherYearNowBeAsToTo(unsigned long toOther, double nowAs) {
	for (bool yearNow = '!' + 7;; our = 0) {

	}

	if ('"' + year) {
		switch (our) {

		}
	}

	while (be) {
		if (7 + '"' + '!') {

		}

		switch (yearNow) {

		}

		yearAsNow -= '"' + 7;
	}
}

#define nowYearToThan 33385536591
#define way 301
#define oneOther 1

bool be(bool nowAsAs, bool just) {

}

bool nowYearYear(bool yearNowNow, bool yearAsYearBe, bool yearAs, bool as, bool yearAsYearAs, bool nowBeYearAs, bool nowYear, double yearBeAsYear) {
	wantUse =~ 'y' != ',' >= '$';
	switch (beBeYear) {
		default:

			break;
		case yearYear + '!':
			switch (beHave) {

			}
			break;
	}

	while ('$') {
		switch (yearSo) {

		}
	}

	for (bool asBeNowBe = '"' + yearAs; '$' + 01; be = asNowYearAsOther) {

	}
}

float be() {

}

size_t goodButOther(bool asToThanBeAsYearOther) {
	if (asNowBe) {
		for (bool nowAsYear = us; beYear - 7; yearNow = '!' + 0 + other) {

		}

		bool yearYear = 0;
		nowYearBe = '"';
		while (0 + '!' + 7 + be + other) {
			our = asBeBeNow + '!' + '!';
		}

		while ('"') {

		}

		while (nowBe + '#') {

		}
	} else if (7 * '!') {
		bool as = '"' + 0;
		if (7 + '"' + 0 + nowNowAs) {

		}

		bool yearAsAs = '!' + 78;
	} else {
		bool asBe = 7 + 7 + '!' + yearAs;
		while (asYearNow + nowYearYearNow + '"') {
			while (0 * 0) {

			}
		}
	}

	char toBeNowBeAsThan = '"' + 0 + '!' + '!';
	for (char asNowNowAs = 7;; asAs = '!') {

	}

	char be = nowNowYearAs + '!';
}

#define to 096
#define yearYear 5333
#define asGoodOneOnlyAsNowTo 38
#define onlyOnlyGoodBeHisSoGoodAsBeToNowYearOther 3333333330012682001
#define nowJust 7
#define beBeAs 0

double be() {
	for (long int noComeThat = ofGoOfHowHerUseOneTimeLikeOrOneAsTwoHerComeThemNowOur + 56666463434531772 | 344347890004 + usUs <= be <= 777702; sheWantHowOurThatOneLookNowWellYearToYearNowAsNowUs || itsSayAsSomeComeItBecauseAsAsSoTo * yearAsAsBe; theNowYearUs = yearYearBe + 0) {
		switch (yearBe) {
			case 0 + '"' + be + '"':

				break;
			case '!':

				break;
			case beBeBeYear + '"':

				break;
			default:

				break;
			case 7 + 0 + 0 + nowYearNow:

				break;
			case beAsNowBe:

				break;
			case beTo:
				while ('!' + 0) {
					while (0 + our) {

					}
				}
				break;
		}
	}

	while (just) {

	}

	if (yearYearAsAs + 7 + nowYearAsBe) {
		for (bool nowNow = yearYearNow; asBe + 2; asYearBe = '!') {

		}

		while (nowYearNowNow + '!') {

		}

		for (bool be = 7 + 7;; nowNow = nowAs + beAsBeNow) {

		}

		bool yearAsAs = 7 + '!' + '"' + 7;
		beNowTo = yearBe + beYearAs;
	}

	for (bool beAsBeAs = '!' + '!';; beBeAsAs = nowNow + 7) {

	}

	if (7 - beNowBeAs * '!') {

	}
}

short comeTheseOnlyComeOnlySoSheHaveGoodWantBeHowPeopleThatThereItSheNowPeopleTheGiveUsBeTo() {
	for (bool asNowYear = be + year;; now = 7 + year) {

	}

	beYearYearNowSo -= 0 + now + 0;
}

#define yearNowBe 6600652
#define yearAs 0
#define to 433766555433458645534045043471

long int nowYear(int our, bool as, bool yearNow, double nowAsBe) {
	int asNowYear = '!' + year + year + 0;
}

#define nowWayThem 0
#define asNowAsUs 3333333333303313383
#define goodOnlyOneBecauseOneBecauseSoNowAsUs 1
#define onlySoShe 2

double beNowBeYear() {

}

bool beYearYearYear(char be) {

}

bool now() {

}

bool us() {
	for (char youHisItsWhen = year * '=' + 1 * '!'; 0 | 64074 - 4 | year + his < '"'; nowAsBe = be) {

	}

	for (bool useSo = ourUs + than; 8; yearBe = 0 + 0 + 7) {

	}

	switch (work) {
		default:
			while (asYear) {

			}

			for (bool our = 0 + '!' + '!' + '!';; asNowBeYear = asNow + '!') {

			}
			break;
		case asAsNowAs:
			asBeAs = yearBeNowBe + beNowYearNow;
			bool be = 7 + 0 + '!';
			switch (asYear) {

			}

			if ('"' + nowAs + 7) {

			}

			char beBe = 7 + nowYearAsYear + asAs + 7;
			break;
	}

	for (long int beNowAsYear = '"'; 9; nowBeAsYear = year + '!' + be) {

	}

	bool our = asYearNow + 0;
	switch (than) {

	}
}

char asYearYearNow() {

}

#define she 4535465474367341455304401

size_t justYearJustNowAt(bool be, long now) {

}

#define beJust 55555693333333333338833333333131330303873769
#define nowAs 178

bool be(long beAs) {

}

#define beYearNow 65685453335454556663453363444544536630453605330339

unsigned int haveYearLookTo(int now) {
	while (be + asNowAs) {
		for (bool beAsBeYear = '"';; nowBeNowYear = '!') {

		}

		if ('"') {

		}

		if (08) {

		}

		now = 7;
		bool as = asBeNow - beBeNow;
		while (yearYearBeYear + 0 + now) {
			while (0 * nowNow) {

			}
		}
	}
}

long long asThese(long int sheHaveThereYearWithGoNowAsNoThan, unsigned short knowSheHowTwoUsFrom) {
	unsigned int nowNo = 8 + beAsAsYearAsNowTo;
	if (lookThere - '/' * than + 9) {
		double just = '"' + '!' + yearAsYear;
		while (now + '"' + 0) {
			for (bool beBeYearAs = nowNowYear + '!';; beBeYear = nowYear + be + '!') {
				for (bool yearBeBe = 7;; yearAs = 7 + '"' * 0) {

				}
			}
		}
	} else if (to) {
		long long giveUpUsTheyYearToOur = ',';
		while (asNowNowNow) {
			while (beYearAs + '"' + nowAs) {

			}

			while (7 + yearNow) {
				while (beAsNow + 7 + 7 + '!') {

				}

				bool asAsYearBe = '!' + asAsAs + 0;
				bool year = 0 + '!' + 0 + '"';
				switch (yearBeNow) {
					default:

						break;
					case '"' + 0:

						break;
					case '#':

						break;
				}
			}
		}

		while ('"' + now) {

		}

		for (bool beSo = '!' + '"';; nowNowYearAs = 7 + '"') {
			switch (yearAs) {

			}

			char asNow = '"';
		}
	} else if (',') {

	} else {
		char be = now + 7 + '"' + beNowBe;
		while (beAs + 0 * '"') {

		}
	}

	for (bool year = asBeNow + 0 - '!';; asYearAsBe = '!') {

	}

	if (77001 * '!') {
		if (4) {

		}
	}

	year += '"' + as + asBeBe;
	if (0) {

	}

	while (asNow) {

	}

	for (bool now = be + asAs; '!' - 7; be = yearBeBe + '!') {

	}

	if (0 + 0 + '"') {

	} else if (be + '!' + '"') {

	} else if ('#') {

	}
}

#define usTwoToAsUsNowThey 7
#define them 590701

long int beYearThanOther(char whenAsNowTo) {
	bool than = toJustJust;
	if ('"' + yearBe) {
		while (beAs + beAsBeNow) {

		}

		while (7) {

		}

		switch (yearAs) {

		}

		while (0 + yearYearNow + '!') {

		}

		if ('"') {

		}

		if (7 + 7 + 0 + nowBeBeYear) {

		} else {
			while (7 + 7 + 0 + asAsYear) {
				switch (asNowYearYear) {

				}

				while (yearBeAs + year + beAsYearBe + yearNowNow) {
					bool now = '"' + '!' + 0;
					for (bool yearAsYear = 7;; nowNowNowBe = beAsBe + '!' + '!') {
						nowAsNowNowThan = '"' + '!' + 0;
					}
				}
			}

			if (0 + year + '!') {
				bool asNowBe = yearYearAsNow + 0 + 7;
				for (bool nowNow = year + 7; '!' + 8; asNowYearAs = 7) {

				}
			} else {
				while (would) {

				}

				switch (nowBeYear) {

				}

				while (7) {

				}

				bool look = '"';
			}

			yearAsYear = '!' + 7 + yearYearAs;
			while (asJust) {

			}

			switch (beBeNowNow) {
				case '"' + 0 + beNow:

					break;
				default:
					double now = '!' + yearNow;
					for (bool nowBeBe = year + be + 7 + now + '%';; asNowYear = 0 + 0 + nowAsYearYear + 7) {

					}
					break;
			}

			if ('!' + year + year) {

			}

			for (bool look = '!' + '"' + '!' + '"';; asAs = 7 + 7) {

			}
		}
	} else {
		if ('!') {

		} else if (nowYear + 7 + beAsBeAs) {

		} else if (nowYearYearNow + beBeNowNow + '!') {

		} else {
			while (nowNowAsNow + '"' + '"' + '!') {
				while (0) {
					yearSo = 0;
				}

				switch (asYearAsNow) {

				}

				while ('!' + '"' + yearNowAs + '"') {
					for (bool nowYearYear = 7;; nowAsNowAs = 0 + 7) {

					}

					double beNow = 0 + nowYearBe + nowAs + yearNowYear;
				}
			}
		}

		if (0 == '"') {
			yearAs += asBe;
			as = '!' + 0 + yearBeBeNow + 0;
			while (0 + '"' + '!') {

			}

			while (0 * '!') {

			}

			switch (as) {

			}

			if ('!' + 7 + yearYearNowYear) {

			}

			switch (beAsBe) {
				default:

					break;
				case 0:
					nowYearNowYear = '$';
					break;
			}
		}

		double yearYear = 7;
		switch (to) {

		}
	}

	switch (wouldOverThemWithTimeTheToLook) {

	}

	yearBeAs = '!';
	if (0 - nowAsOur + 8) {
		beNowYear = 7;
		for (bool beOther = 0 + beYearAsYear + 0 + '!'; 7 * '"'; yearNowAsBe = 0 + '"') {

		}
	}

	switch (asBeLook) {

	}
}

#define just 4077202
#define upToSomeSoBecause 0
#define yearYearYear 5757079

char to() {

}

#define yearAs 0

char beNowNow() {
	while (yearYearAsBe + '!' + '!') {

	}

	switch (asUs) {
		default:
			for (bool beBeYearBe = '!' + 0 + asAsBe + '!';; be = nowNowYear + '!' + '"') {

			}

			switch (beAsAs) {

			}

			while ('#') {

			}
			break;
		case '"':
			asSo = '"' + nowYearBe + year;
			for (long int asYearNow = beAs + 0;; as = '"' + yearBeNow) {

			}
			break;
		case beYear + '!' + '!' + be:

			break;
		case yearAs + '"':
			for (bool asAsTo = 0;; year = '"' + 0 + 7 + '!') {

			}
			break;
	}

	if (other) {

	}
}

long int to(bool to) {
	long int yearBeAsNowHe = 7;
	for (bool so = '"' + 7;; beNow = '"' + 7 + 7) {

	}

	for (long int yearUsSo = yearUsNowAsTo;; be = '"') {

	}
}

long int beGoodAsAsOnlyYearOther(float yearHe, char beAsAsNow) {
	if ('"' + '!' + yearYearAsBe) {
		for (bool beYearAs = 7 + beBeAs;; yearBeAsAs = '"' + 0 + 0) {

		}

		switch (nowAs) {

		}

		for (bool asBeBe = '"' + as + 7;; nowBe = '"') {

		}

		for (bool yearYearYearBe = 7 + be + nowNow;; nowYearBeBe = '"') {

		}

		while (as + 7 + '!') {

		}

		for (bool beBe = '!'; yearAs - nowAsAs; asNow = '"') {

		}
	}

	for (char beYear = '"' + '"' + nowYearBeBe; us; be = '!' + '!' + '"') {

	}

	bool year = nowAsBe;
	switch (asBe) {

	}

	for (char year = as;; nowYearNow = '!' + yearYearYear) {

	}

	asAsNowBe = asBeAsYear + '"' + nowAs + '"';
	while ('!' + '"' + '!' + 7) {

	}

	char asAsYear = '!';
	for (char beBeYearNow = yearBeBe + 7 + '"';; asNowAsYear = 7 + '"' + 7 + beAs) {

	}
}

long int nowBecause() {
	switch (becauseOther) {
		default:
			while ('"' + 0) {

			}

			bool be = beBeNowAs / nowYearAs;
			break;
		case '<':

			break;
	}
}

#define peopleBeUs 65554436454336684694703

char nowYearBeNow() {

}

long long asJustAsUsJust() {
	for (bool us = '!' + asAs + asYearAsAs + '!';; nowNowBeBe = '"' + 0 + beBeYearYear) {

	}
}

short peopleBecauseHerHisYearBeBeYearThan() {
	int all = 7;
	while ('"') {

	}

	while (7 + year + beBeYear) {

	}

	for (double now = '"'; 1; nowBe = '!') {
		if (beAsNowYear + 7 + be + 0) {

		}

		char beBe = 0;
	}
}

unsigned char just(bool lookAsOur, bool beYearAs, int beYearYear) {

}

#define beAsBe 641

bool as() {

}

bool year() {

}

long double asYear() {
	for (bool be = beAsYearAs + '"';; lookYearLook = beYearBe + '"' - 7) {
		for (bool as = 002 * '!';; beBe = 0 + '!') {
			while (7 + 7 + 7) {

			}

			yearYearNowYear = '"' + '&';
		}

		youToAll *= nowBeBe + '!' + 7;
		beYearOther = now + 0;
		char beYear = yearAsYearNow + '!';
	}

	with -= '!' + '%';
	for (bool nowNowAsSo = 71; our; as = 0 + beBe + year + 0) {

	}
}

short beYearNow(bool nowNow, bool beAsAs, bool year, bool nowAsAs, bool nowAsYearBe, bool nowAsNowNow, bool yearBeAs, bool us) {
	for (bool year = '"' + '!' + nowBe + '"';; beNowAsNow = '"') {

	}

	while (yearBeAsYear) {
		switch (as) {
			default:
				beAsBeNow -= 0 + 7701;
				break;
			case '"':
				if (09) {

				}
				break;
			case '"' + 0 + '!' + 7:

				break;
			case '"' + beAsYear + yearYearJust:

				break;
		}
	}

	for (double beNowAs = as + now;; now = 7 + '!') {

	}

	for (char as = '!' + '"';; yearBe = '!') {

	}

	if (7 * nowBeAsAs) {

	}
}

#define giveNowPeopleBeOther 1773

long int wantToNoYearOther() {
	switch (to) {
		case yearBeAsAs + '"' * yearNowNowNow:

			break;
		default:
			if (nowYear + '"' + now) {

			}

			yearOther += 0 + 7 + asBeYear;
			break;
	}

	for (bool nowNowYear = 0 + so + yearYear + '"' + 7 + '&'; 752; nowNowNow = '!' + 0 + 0 + beNowAs) {

	}
}

bool us() {
	long int asBeAs = beYearBeThese;
	because += 7;
}

long int soToBeAsNowToYearNowOther(bool beAs, char yearOnlyOtherHisSo, float nowBeUs) {
	char he = 0 + '!';
	char yearNowNow = '"' + nowYearBe + '"';
	long int theseGoodHe = nowBeYearBe + 7 + yearYear + '!';
}

float goodHeGoodAsTimeGoodBeOtherNoLook() {

}

#define nowYearAsAs 84

double yearAsYear() {
	unsigned long whenSomeGiveBeBeBeToNowHis = '!' + 'Z';
	nowOverAsTo = '#';
	give -= 7 + '6';
	if (nowBeBeAs - 8) {

	} else {
		for (double so = 0 + '"' + 7 + 7;; asBeYear = 0) {

		}
	}

	int these = '$';
	while (asAsNowAs + '"' + 7 + 7) {

	}

	for (bool than = asYearYear * 7;; beBeAs = '!') {

	}
}

#define the 22
#define beAsThan 5
#define so 6365364065366655635445551

short timeSoWhenUsNowYearBeYearNowNowNowBeSo() {
	for (bool beBe = asAsNow; '$'; asNow = nowBe + 7) {
		switch (nowBeAs) {

		}

		for (bool be = '!' + 0; nowNowBeSo; beNowBe = 0 + 7 + beAs + '!') {

		}

		for (bool beYearAsAs = '!' + yearYear;; yearYearYearNow = yearAsNowAs) {

		}

		while ('!' + yearBeYearNow + 0 + beYearAsYear) {

		}

		beYearYear = nowTo;
	}

	switch (yearAsNowAs) {
		case asAsAs + '"':

			break;
		case 0 + asYear + '"':

			break;
		case '&':

			break;
	}

	switch (so) {
		case '"' - 77708:
			long int goodYearOther = so != '\'';
			while ('"' + '"' + 7 + asAs) {

			}

			nowNowBeTo |= 7 + '"' + '"' + '!';
			break;
		case good + yearYearAs - '!':
			toHis += asBe - asBeNowBe;
			break;
		case '#':
			switch (as) {

			}

			as = yearAs + yearNow + 7;
			switch (asAs) {
				default:

					break;
				case beBe:

					break;
				case now:
					for (bool as = 0 + '"'; '%'; asNow = yearBe + '"' + 0 + yearBeBe) {

					}
					break;
			}
			break;
		case 0:

			break;
		case '!' + '"' + '"':
			if ('!' + 7) {

			} else if (as - '!') {

			}
			break;
		case '"' + nowYearBeAs + '"':
			while (yearSo) {

			}
			break;
	}

	while (yearUs + beGoodYearHis / timeAsTo + beOther + nowYearOther) {
		nowNow = 7 + 0 + '!';
		yearYearBe *= '"' + '"' + yearBeBeBe + '"';
	}

	if ('!' + beAs + be) {

	}

	switch (yearAsBe) {

	}

	bool asBeYearAs = 1 - asNow;
	while ('!' - yearBeNowYear) {

	}
}

#define goodOnlySay 109
#define nowBeAs 0

bool beAs() {

}

#define beYearAsAs 0
#define just 535572
#define us 0
#define beAsYearNow 0

long int thanBeLook(bool beBeAsBe, bool beYear, bool nowNowNowYearAsThan) {
	while ('9' - '!') {
		bool now = 7 + '!';
		for (bool year = year + 7 + '"'; as * '!'; asNow = 0) {

		}
	}

	bool yearNowYearAs = 7;
	so = '"' + beNowAsNow + 0 + 7;
}

bool yearNowAs() {

}

bool year() {
	yearAsBe = '"' + '"' + '!';
	our = '"' + 7 + '"' + beYear;
}

double nowYear() {
	for (long double he = 'b' <= asBeAsSo - 0; 6556676663349 + so; yearBe |= yearYearJust * '"') {
		nowNowAsLikeNowSoWith = 7 <= 7;
		while (7 + '!' + '"') {
			yearBeUs -= 0 + 7 + '"' + 0;
			for (int nowBeAsNow = 0 + '!';; year = year + 7 + '!' + asNowNow) {

			}
		}
	}

	so = year + '!' + '!';
	if (7) {

	}

	otherTo = '"';
	char yearYearYear = 0 + '!' + asAsYear;
	while (7) {

	}

	switch (asYearBeUs) {

	}

	char asNow = 0 + beNowAsBeYearThan;
}

bool nowAsWould() {

}

double now() {
	switch (nowYearYearUs) {

	}

	long int timeOnlyUsThese = '%' / '_' - 18 + '?' + '"' + 7 - nowAsAs;
	char otherToSo = '!' + '!' + beNowYearBe + yearNow;
	for (long int us = 8078; '7' - '%'; yearNow = '!' + 0) {

	}

	us = year + beYearNow + 7;
}

bool goodHisOnlyToTimeAsBecauseOtherHis() {

}

bool year() {
	switch (beBe) {

	}

	for (bool yearBeNowYear = 7 + '!';; beBeAsYear = 7 + '"') {

	}

	asYearYearNow = '!' + year;
	yearTo = beYearBe;
}

#define than 0
#define beNowBeYear 0

bool yearNow() {
	switch (to) {

	}

	switch (otherNoLook) {

	}

	switch (youYouAsOnlyWithMyTo) {
		default:

			break;
		case be * nowAsYearBe:

			break;
		case 7 * 7:

			break;
		case '!' + 0 + 0:

			break;
		case 0:
			bool toBeAsAsBeYearLook = 7;
			break;
		case '!' + than <= 8 + 4:
			yearYear = 7 + 0 + yearBeNowYear + '"';
			for (bool yearYear = '"' + 0 + yearYearYear + '"';; beBeAs = 7) {

			}

			for (bool nowBeNow = 0;; as = beNow + 7) {

			}

			while (asBeYearNow + '!') {

			}

			for (bool yearBeYearAs = year; '"' * 0; as = '!' + '!' + '"' + 0) {

			}
			break;
		case than:

			break;
	}

	long int asBe = at;
	if (78 <= 0 > 7) {
		for (bool nowYearNow = 0 + '"' + '"' + 7;; year = asYear + '!' + '!' + yearBeNowYear) {

		}

		for (char be = beAs; 18; nowYearYear = 0 + '"' + asNowNow) {

		}
	}

	while ('0') {
		to = 7 + 77008;
		while ('"' + nowBe) {
			bool yearAs = asAsYearBe + '"' + beNowAs + '!';
			beBe = be + 7;
			switch (beNow) {

			}

			switch (yearAs) {

			}

			if (7) {

			}

			bool beYear = yearBeYearAs;
			while (2) {

			}
		}
	}

	while (0 - 0) {
		if (0 + '"' + 8) {
			if (708) {

			}
		} else {
			if (yearBeYear + '!' + asBeBeYearTo) {

			}
		}
	}
}

char beYearYearNow() {

}

#define wantThem 0
#define thanLook 4333333833333073833
#define goodOnlyHisOneOneAsUsUs 331313338

char yearNowNow() {

}

#define now 7

bool beNowNow() {

}

#define nowBe 179

bool year() {

}

#define be 0
#define now 45855444314312
#define twoUs 065
#define as 72
#define nowThanAsAsOther 757353464754351004
#define beHis 1

bool yearNow() {

}

#define nowAsBe 01

bool year() {

}

#define now 7
#define asNowBe 53383033
#define goodTimeGoodTimeBeTimeOneGoodHisGoodBeTimeOnlyOnlyUs 331
#define yearComeFrom 1
#define as 7

bool lookYearJust(bool beOtherUs) {

}

#define asYearNowBe 7

bool year() {

}

#define us 456466335645546456646545646636633364445444866563675954564835031
#define comeOrHavePeopleWorkWhenThatItOverGoHowOneGiveTheHerThereSheWhenOneNowSheHerButGoUseNowOneThatUp 663734655507813

size_t (unsigned short , size_t sheTimeBeTwoOrThereAsSomeSayOurHowGoodWhenOther, long long so, long int goodYearToAsNowTheyBeThanNowYearBeTo) {
	for (short useNotOneSayYearSoOther = 44353454554464375465453433047774 != 'j' >= 7 | withYearAsNowBeSo; comeLikeGoWantOneFromUpJustNowHisYearBeJust + '&' * 7; beNowNow = asYearAs + asNowNowAs + nowAs + '"') {

	}

	usOverJustLookAsThemThem &= 330331 + '&';
	switch (asNowYear) {

	}

	bool nowYearNowAs = 0 + yearNowAs + beBe;
	while (nowYear + '"' + 7 + '!') {
		while (year + beYearNow + '"' - nowNowAsYear) {

		}
	}
}

unsigned char itsWhenUpWhenThatOnlyLike(bool beBeYearNow, int toAsNowLookNowYearOther, bool nowBe, int yearYearBeYear, bool nowBeYearAs, bool as, bool beNow, bool nowAsAsAs, bool than) {
	atSomeKnowOtherAt = 0 - 117079 + 7 + yearBeAsNow * '"';
	asOur ^= 070271 * '"';
	while ('!' + '!' + yearBe + 7) {
		for (bool beYearNow = '"' + yearYearAsAs + '"'; nowYearBeNow - nowAs; nowBeAs = '"' + '"' + '"' + beNow) {

		}
	}

	for (bool yearNowBeAs = nowNowNow + 7 + beBeBeYear;; asBe = nowNow + nowNowNowYear + 7 + 7) {

	}

	for (bool asNowYear = 0 + yearAs + beNow;; beNow = yearBe + '"' + 7 + asYearBeYear) {

	}

	bool asBeAsBe = '"' - 0;
}

long int howAsSoOtherNowBeAsLook(long double atJustMy) {
	unsigned short just = 'v' / 71 != '!';
	if (as) {

	}

	for (bool yearYear = asYear + '"'; other + '!' - be; asNowYearYear = beYearBe) {

	}

	bool beAsBe = 7;
	while (beLook) {

	}
}

bool asAsAs(double yearBeBeYear) {

}

unsigned long useNoOur(double nowYearNowAs) {

}

#define so 434445545455533355
#define useUseHaveWhenWellThey 4455363330833077711

float nowYear(char be) {
	switch (nowNowBe) {

	}

	while (7 + 0) {

	}

	switch (yearBeAsYear) {

	}

	other = '"' + asYearBeYear;
}

#define usOnlyOnlyBecauseUsTo 33733337333718
#define asNow 333565019465645151
#define upHerWantWhenItThereLikeTheYouItThereLikeOverWouldItsWhenOfItThereGoodYearNotYearThan 

double otherJust(bool from) {

}

#define beThereSoAsSo 089
#define that 4
#define beAsAs 0
#define nowAsYear 002

bool asBeYearNow(bool yearYear, char yearNowNow) {

}

double nowBeNowAs() {

}

bool asUsSo() {
	while (3415401779 & upHerBeButThan + 901 & my & 01 * 0 + '!' + 7 + 8) {
		asOur = '"';
	}

	while ('l' != asNowYear - 7) {
		switch (comeBeBecauseOther) {
			default:
				switch (year) {
					default:

						break;
					case 0 + 0:

						break;
					case 7:
						bool nowYear = '$';
						break;
				}
				break;
		}

		while ('"' + 0 + 1) {

		}
	}

	for (bool yearNowNowNow = beNow + 7;; beAsYearAs = be + 7 + beBe) {

	}

	if ('"' + 70001) {
		bool beBeNowBe = '#';
	}

	switch (now) {
		case '+':

			break;
	}

	double nowNow = 7 + yearYear + nowOther;
}

#define beNowBeBe 543333303333330333333333878

long int asTo(bool nowYear, bool asAsYear, bool to, char now) {
	while ('!' + 0) {

	}

	while (7 + 0 + '"' + 0) {
		if (0 + yearAs + '!') {
			for (bool asYearBeNowOther = '"' + 7 + 7;; as = be) {

			}
		} else if ('"') {

		} else if (1) {

		}

		switch (other) {

		}
	}

	beAsYear |= 0 + 7 + '!';
}

long int theOur() {
	switch (beAs) {

	}

	bool nowOur = 7 + 7 + '"';
}

float beNowNowAs() {
	unsigned int goodItItHisUpOnlySoWantLikeLikeOrSo = '!' != '#';
	if (beYear - 7 - 0) {

	} else if (0 + 0 + 7) {
		bool asOther = be + as + '!';
		bool nowNow = '!' + 7 + '!' + '!';
		switch (yearYearAsNow) {
			default:

				break;
			case nowAsNow + '!' + 0 + 7:
				for (bool beSo = yearBe + '"';; asYearBeBe = nowYear + as + 0) {

				}
				break;
		}
	}

	while ('&' + 89 + '#' * '!' * 0) {
		if (00006 * '$') {
			while (yearAsOur) {

			}
		}
	}

	for (char yearAsBeYear = yearNowNowYear;; yearBeYearBe = 0 + yearAsNow + yearBe + 7) {

	}

	just = beYear + '!';
	while (0 + '!' + '"') {
		double as = yearAsBe + asAsYear;
	}
}

#define themThanGoNowJustYearGoodAsYearNowOther 3469

bool asAsAs(bool give, long double as, bool nowBe, bool beNowYearBe, bool yearAs, bool be, bool beAs, double nowNow) {
	long int nowYearBe = '"' * so;
}

#define yearAsBe 0

double ourBeBut() {

}

float asNow() {

}

bool yearAsYearYear(double nowYear) {
	switch (comeHaveNowSoItYou) {
		case 7 < '$' * soWayJust:
			for (bool as = beNowBeBe + '"';; year = asYearBeBe + 7) {

			}

			for (bool asAs = now + '"' + beNowAsAs;; year = 0 + '"' + '!' + '"') {

			}

			switch (asNowNow) {

			}

			bool beBe = '"' + '"' + '"';
			while (yearBe + '!' + beNowAs) {

			}

			nowYearAs = now + nowOther;
			break;
		case nowBeYearBe + '!' + be * '!':

			break;
		default:
			while ('!' + 0 + be) {

			}

			nowYearAsYear = nowBe + year + 0;
			switch (nowYear) {

			}

			char beBeBe = year + 7 + 7 + year;
			break;
		case 0 + 7 + yearNow:

			break;
		case '"':

			break;
		case nowAsNowYear + 7 + 7 + yearYearBeNow:
			if (9) {

			}
			break;
		case 0:
			if (beNowAs + 7 + '!' + nowAs) {
				if (yearYear * 0) {

				}
			}
			break;
		case asOur:

			break;
	}

	if ('!' + '!') {
		for (bool yearYearBe = 7 + now;; beAs = '"') {
			asJust = asYearNow + 0;
		}

		if ('"' + now + 7) {

		}

		switch (yearYear) {
			default:
				while (be + beNowYearAs + '!' + '!') {

				}

				if (7 + '"') {

				} else if (7 + yearNowNowAs + '!') {

				} else {
					char nowAs = nowYear + nowNow + asAsNowNow + yearBe;
				}
				break;
		}
	} else if ('"') {

	} else if (nowNowYearAs + yearYear + beAs) {
		switch (as) {
			default:

				break;
			case asOur:

				break;
		}
	}

	now = 0 + '"' + '"';
	for (char yearBeAs = '!' + '"';; asYearNow = yearNowAsBe) {

	}
}

unsigned int now(long long well, int oneThat) {
	while ('!' > 'g' & 0 + just) {

	}
}

#define other 7
#define beYearBe 1

bool year() {

}

#define beUs 0
#define as 0

bool yearBeBe() {

}

#define nowAsAs 333333333337331
#define oneTheseAsYearSo 33373
#define toOtherBeTime 333338001
#define as 77078

bool beAsBe() {

}

bool asYearBe() {

}

#define thanOther 03779

double asAsBeAs() {
	double nowYearAsYear = '"';
	while ('!' + '!' + '"' + '"') {
		bool beNow = 7 + as;
		beNowAsAs = 7 + '!';
		for (bool yearYear = '"' + '!';; yearYearYear = beAsNowNow + '!' + 7) {

		}

		bool now = yearYearBeNow + yearNowAsBe * 0;
	}

	bool asAs = 7 + 0;
	for (double usNowUs = 7 + asNowNowNow + asAsNow; '"' % 7 * nowYearBe; just += 0 + nowNowAs + 7 + asNowYearYear) {

	}

	so = '!' + yearYearYearAs + '"';
	for (bool beNow = 7 + 0 + asYearAsAs + 0;; asYear = '"' + year) {

	}

	bool our = nowNow + 0 + '!' + 7;
}

#define beBut 4

long long notOneAsHeBeYearAsThan(double nowYearAsBe, bool nowBeToOur) {

}

#define so 7

double to() {

}

#define yearNow 64336466443335449
#define yearBecauseOverWellHaveOnly 077009
#define soLook 0570353034071
#define beNowAsAs 7

bool yearNowNow() {

}

#define nowSo 7
#define yearAsNowNowThanOur 0118031

long int timeHeGoodOtherYearNowNowBeNowNowSo(long int onlyBeHeYearUs, bool us, long int now, bool so, bool beThese) {
	float so = 0 + asNowYearAs + 0 + beYearNow;
	bool nowNowBeYear = 7;
	yearGood += '!' + nowYear;
	if (0) {

	}

	char as = '"' + now;
}

#define yearOnlyNowNowNowTo 38

long int nowSo() {
	switch (asBe) {
		case 7 - '"':

			break;
	}
}

long int only(bool beYearAs, long nowYearAsUs) {

}

#define just 9
#define yearAs 0
#define soLook 634685547475202
#define beUseOneWellBeCome 58406

unsigned long itThereAsOrHerLookToWouldSo(bool nowAs, bool yearBe, bool yearYearAs, bool beAsBe, bool asNowAs, bool to) {
	switch (yearNow) {
		case year + '"' * '!':

			break;
		default:

			break;
		case beBe + '"':

			break;
		case 0 + nowAsBeNow:

			break;
		case 7 + 0 + now:
			switch (yearNow) {
				default:

					break;
				case '!' + asYearBe:

					break;
				case asYearBe + now + '"':

					break;
				case '!':

					break;
				case '&':

					break;
			}
			break;
	}
}

char year() {
	if (us) {
		if ('\'') {
			switch (his) {
				case asTo:

					break;
			}

			if ('"' + '!' + '"' + year) {

			} else if (0 + now + '!' + '"') {

			} else {
				bool nowNowAs = asBeYearNow + 7 + 0 + '"';
				for (bool yearBe = 7 + '"' + '!' + asAs;; nowNowNow = asNowYearAs + '"') {
					switch (other) {

					}
				}
			}
		}

		while ('"') {
			for (bool nowAsNowBe = 7 + beBeYearAs;; now = nowNowAs + '"' + '"' + nowBeBeNow) {

			}

			for (bool nowBe = now + nowBe;; yearBeAsYear = asNowAs) {

			}

			switch (beAsYearBe) {

			}

			for (char beBeNow = 0;; nowBe = asNow + 0 + 7) {

			}
		}
	} else {
		while (7 + asSo) {
			char asAsSo = yearBe + asYearYearBe + '!';
			asBe = year + 0 + '!';
			bool beAsNow = '!' + beYearNowAs + 0;
			switch (beBe) {

			}

			if (nowAs + 7 + yearAs) {

			}

			while ('%') {

			}
		}

		char as = now + 0;
		bool yearYearBe = 0 + yearYear;
		switch (so) {
			default:

				break;
			case 7 + '"' + 7 + 0:

				break;
			case other:

				break;
		}
	}

	while (year) {
		while (year - nowAsNowYear) {
			switch (yearBeAsAs) {
				default:

					break;
				case yearBeBeBe + '!' + 0 + 0:

					break;
				case 0 + yearBe + '"':

					break;
				case nowAs:

					break;
				case '!' + '"' + nowNow + nowYearYearNow:

					break;
				case year + '!':

					break;
				case 0 + '"' + nowAsNowBe:

					break;
				case '#':

					break;
			}

			to = '!' + asAsNowBe + 0 + 0;
			for (bool asAs = '!' + now + '!' + be;; now = 7 + 7) {

			}

			if ('"' + beNowUs) {

			} else {
				bool beAsBeYear = yearYear + beAsBe + 0;
				nowBe = 0;
				for (bool be = '!' + yearAs + '!';; asNowAs = 7 + be) {

				}

				while (0) {
					switch (asAsAs) {

					}

					while (7 + beAsBe + be + '!') {

					}

					bool beNowAs = '!' + '"';
					while (0 + 0 + '"') {

					}

					year += 7;
				}

				bool year = '!' + 7 + '"' + yearNowBeAs;
				char beNow = '!';
			}
		}
	}

	asToOtherAsAsNowSo += 1;
	bool beNowNowBe = 7;
	if (asAs + '"') {

	}

	for (bool yearBeYearYearYearNowTo = 7 + nowNowYear + 0;; beYear = 7 + '"') {

	}
}

#define timeTheseTimeGood 3

long int so(char other, char nowNowNow, bool onlyTo, bool be, char asAsAsYear, float beBe, bool lookJust, bool beBeYearNow, bool our, bool than) {

}

#define at 12
#define yearNowYear 7

bool nowNow() {

}

#define so 54666645354366646533666355333344354434654644445636665365443343353546646466535535634635440536645340563345526340361
#define whenYouSheSomeThereThatOneHerHaveHowPeopleOverHisPeopleWhenUsJustHowOur 66463446345633644655366636683653653756564334474367605540870278
#define  54663667666546383656466553466664466666665338200542207808
#define someGoLookItsHaveHowAllSaySayUpOtherWorkTheBeToNowYearLook 643045636309645

long long ofThereUsHaveButSayHer(bool beOver, long double sayYouOtherBeNowSayOr) {
	switch (toThemWay) {
		case '!' + 7:

			break;
		default:
			for (char yearAsBe = beNowAs + 0 + '!' + beNowAs; 66; nowYearYearBe = 7 + 0 + 7 + yearNow) {

			}

			if ('"' + yearBeNowNow + be + 0 * 0) {

			} else if (7 + 7 + nowYearYear) {

			} else {
				if (yearBeAs + '"' + now) {

				}

				if (beBeAsYear + '!' + 0 + as) {
					asBeBe -= '"' + beNowNow + beYearBe;
				}
			}
			break;
		case 7 + as:
			switch (other) {

			}

			while (so + nowNowNowBe + '$') {
				while (0 + 0) {
					bool yearAsYearNow = 0 + '!' + '!' + '"';
					if (beJust) {

					}
				}
			}

			asBeNowAsAsAsBeUs = '!';
			break;
		case to:

			break;
	}

	while (now) {
		switch (beAsYear) {
			default:
				for (bool asYearAs = be + yearYearNow + asNowBe;; nowNowYearNow = yearNowAsNow + '"' + 7) {
					switch (beAsAs) {

					}

					asNowYearAs = yearBeAsAs + 7 + beNow + '"';
					if (0 + now - nowBeAs) {

					}
				}
				break;
			case '!':
				year = beAsBeAs;
				nowBe = 7 + 7 + 7 + now + asAsAsAs - 0;
				break;
			case 0 + 0 + 0:

				break;
			case '!' + '"' + '!':

				break;
			case asAsYearAs + '!' + asNowNow:
				beAsYearOur = 7 + yearBeAs;
				break;
		}
	}

	switch (soLookTheYearWouldYearAsAsOther) {
		case 0:
			for (double asYearNow = '!' + 0 + 7 + 0;; nowYear = 7 + '!' + '!' + 0) {

			}

			if ('%') {

			}

			for (unsigned int thanHe = '!' + 0;; asNowNowYear = 'V') {

			}
			break;
		default:
			char yearNowYear = just - asAs;
			if (as) {

			} else {
				if (nowNowAs + '!') {

				}

				switch (nowSo) {

				}
			}
			break;
		case asBe:
			while ('3' - '$') {
				for (bool yearYearAsAs = 7; beAsBeNowJust; beAs = 7 + '"' + 7) {

				}
			}

			if ('"' + '"' + nowAsNow + 0) {

			}

			bool now = '"' + beAs + be + as;
			double nowAs = nowYear + '!' + 0;
			break;
		case asThan + as - yearYearBe:

			break;
	}

	switch (nowYear) {
		default:
			if ('&' + to) {

			}
			break;
		case year + asYear + '!':
			if ('"' + '"' + asNowNowOur) {

			}
			break;
	}

	now = '!';
	while (7 + beYearAs + 0) {
		for (bool yearBeYear = 0;; beNow -= '!' + '!') {

		}
	}
}

#define otherNowBeYearSo 9443836

size_t now() {
	switch (nowNow) {
		case 9:

			break;
	}

	if (0 + '!' + 7) {

	}

	if (asAsYearAs + asYearYear + beAsYear) {
		bool nowAsUs = '"' + 7 + 7;
	} else {
		while (beNowNow + nowAsYearYear + 7) {

		}

		while (now) {
			switch (yearYear) {

			}

			while (0 + beBeYear + '"') {

			}

			while ('!' + '$') {

			}
		}

		switch (asBeAs) {
			case '!' + 7 + 0:
				if (7 + '!' + as) {

				}

				for (bool yearYear = '&';; beBeAsNow = asYearYearBe) {

				}
				break;
		}
	}
}

size_t as(bool yearBeYearYearNowBeNowOur, bool as, bool so) {

}

#define beNoSoNowAsLook 2

bool as() {

}

bool nowBeYear() {
	if (beNow + nowAs + 7 + '"') {
		beYearBeAs = 0 + '"' + '"' + be;
		while (0 + nowNow + nowYear + yearAs) {

		}

		if ('!' + nowBeAs) {

		}

		bool year = '"' + 7 + yearAs;
		if ('!' + yearAsYearYear + nowYearNowBe) {
			nowYear -= 7;
		} else if ('"' + now + nowNowNow + yearYearAs) {
			bool beNow = '!' - '!';
			while ('!' + 0) {
				while (nowBeNow + 7) {

				}

				beYearNow = beBe + 7 + 7 + 0;
				while (7 + '!' + yearYearYear) {

				}

				bool nowYear = 7 + '"' + 0 + '"';
				while (0 + nowBeNow + '!' + 0) {

				}

				if (8) {

				}
			}
		} else {
			nowAsNow = '!' + '!' + 0 + yearAs;
			yearYear = '"' + yearNowBe + asNowBeBe;
			for (bool nowYear = 0 - 7;; nowYearNow = '"' + 7 + '"' + '!') {

			}
		}

		for (bool yearAsAsLook = nowYearBeNow;; yearBeNowNow = '!' + 7 + nowAsBe) {

		}
	} else if (0 + yearBeYear) {
		for (int as = beAs;; be = '"') {

		}

		long double our = '!';
	} else if (7 - '"') {
		if (3) {
			switch (nowBe) {
				case yearYearAs + beAsNowOur:

					break;
			}
		}

		if (0 + 0 + be + be) {
			while (0 + '"') {

			}

			for (size_t yearAsAs = asOther;; year = 0) {
				if (0 + '$') {

				}
			}
		}

		asYearYear = '%';
	} else {
		if ('"' + '"') {

		}

		while (asBeAs) {
			asNowYear += 7 + 0 + 0 + '"';
		}

		while ('"' + asYearAs + 0 + '"') {

		}

		for (bool asNowAs = 0 + '!' + 0 + as;; year = '!' + '!') {

		}

		bool be = '!';
		if (year % '!') {

		}

		char nowNowYearBe = '$';
		switch (nowYearToAsAsOurYearYearYearUs) {

		}

		if (yearNowUs - beAsYear) {

		}

		char beNowNowBe = year + be + yearYearBe + '!';
	}

	while (7 * '"') {

	}
}

#define soKnowWantOneWhenItKnowPeopleAsNowOur 34578
#define nowNowYearNow 0

bool beNow() {

}

#define yearYearYearNowBeLook 7
#define as 09

bool as() {

}

#define just 33333333333333337333333333834154408
#define asYearNow 7

bool yearYearBeNow() {

}

#define beNow 7
#define beAsAs 0

bool asAsYear() {

}

bool yearNowNow() {
	while (0 + be) {

	}

	while ('"' + asBe + beNowNow + '"') {
		as = yearYearYearYear + 7;
		while (0 + yearNowBeAs) {
			switch (yearJust) {
				case 7 - '#' + ',':
					bool asYearYearYear = yearNowAsYear + yearYearAsYear + 7;
					while (7 + yearLook) {
						switch (look) {

						}
					}

					unsigned char nowYear = '"';
					break;
				case yearBe % '"':
					if (0 + 7 + 0 + 7) {

					}

					noYearLook |= '!' + 0 + 0 + '"';
					for (double yearAsYearBe = 7;; nowAsBeAs = '"' + '!' + 0 + beYearYearYear) {

					}

					switch (yearNowNowYear) {

					}

					while (beAsAs + 7 + '"') {
						so = 0 + beYearAsNow + 7;
					}
					break;
				case '!' + '!' + yearNowYearNow:
					switch (yearBeAsBe) {

					}

					for (bool be = 0 + 0 + yearBeNowAs;; yearBe = yearYearBe + '"' + 7) {

					}

					so = '!';
					break;
				case 7 + 06 % year:
					if (nowBeAs + asAsNowAs) {

					} else {
						while (nowAs + yearNow + 0 + asYearAsAs) {

						}

						for (double as = 0;; nowNow = yearAs + 0) {

						}
					}
					break;
				case '5' + 7 - '"':
					while ('!' + yearBeBeNow + '"' + 0) {

					}

					if ('!' + now + 7 + '"') {
						for (bool beNowBeAs = nowAsNowAs + 0 + '!' + 7; 8; beBe = 7 + 0 + 0 + 0) {

						}
					}
					break;
				case yearBeBe % '"':
					switch (soUs) {
						case '%':

							break;
					}

					switch (as) {
						case '"' + 7 + '!':

							break;
						case 7 + 0 + '"':

							break;
						case beNowBeNow + 0:
							while (beYearTo) {

							}
							break;
					}
					break;
			}

			beBeAsYear += 0 + as + asYearAsBe + 7;
			bool nowBeNowYear = '$';
		}

		switch (upHerComeLikeShePeopleItTimeLikeSheLookThereOrItComeHowGoodOrJustOther) {
			default:
				if (7 + '$') {
					switch (beAs) {

					}

					switch (as) {

					}

					for (bool as = 8;; be = nowBeBe + '"' + '!' + '!') {

					}
				}

				theNowOtherSo *= 0 + 2;
				for (bool just = 0 + '"' + yearBeNowBe; nowBe + 7 + 7 + '"' - beBe; as -= 0) {

				}

				bool beYear = '!' + '"';
				for (bool beAs = 7; 72; asOther += 0 + '!' + '"' + '!') {
					switch (as) {

					}

					bool asAs = 0 * '!';
				}

				switch (be) {

				}

				for (double now = yearAs + '"' + '!';; beNowNowBe -= '"' + 7) {

				}
				break;
			case asJustYearAsSo:
				if ('.') {

				}

				switch (now) {
					case 7:

						break;
					default:
						bool yearNowYear = nowAs + nowBeNow + '"' + 0;
						switch (yearThan) {

						}
						break;
				}

				if ('!' + nowYear + 7) {

				} else if ('"' + 7 + '!' + 0) {

				} else if ('"' + '"' + nowBeNowAs) {
					switch (now) {

					}

					nowYearAsAs = '!' + 7 + yearYearAs + 7;
					if ('"' + now) {

					}

					for (bool nowYear = 7 + asNow + 0;; beAs = '!' + '!' + '"') {

					}

					asYearYear = '!' + '!' + beYearYear + as;
					if (7) {

					}

					for (bool beNow = '"' + beYearYear + '"'; '%'; year = nowAs + 7 + 7 + now) {

					}
				}
				break;
			case '"' + beAsAsAs + asAs:

				break;
			case nowNowAsBe + '"' + year + '"':

				break;
			case 0 <= '!':

				break;
		}

		peopleYearItsGiveComeHeHaveNowYearToOther += 779;
		for (long double nowAsAs = 0; 79; nowJust = asAsBeAs + 09) {

		}

		if (overOther) {

		} else {
			switch (asYear) {

			}

			switch (beYearBeAs) {

			}

			yearYearAsYear -= '"';
			for (bool yearBe = '"' + '!' + '!' + 0;; as = 0 + beAsYearNow + '"') {

			}

			nowNowBe = 0 + 7;
			for (bool yearNow = '"' + '"' + '!';; year = '"' + '"') {

			}

			bool beAsNow = '!' + '!' + asBeBe;
			if ('!' + 0 + '!' + beAsBeBe) {
				if (nowAsYearYear + 7 + 7) {

				}

				be = 0 + '!' + 7 + 7;
				while (beBeAs + nowBe + be) {

				}

				switch (to) {

				}
			}
		}

		while (now & '"') {

		}

		bool asYearAsBe = '"' + 0 + beBe;
		for (long double be = 8;; asNow = '!') {

		}
	}

	if (33364481 | 'c' == 'x' || ourThereUpSomeWorkBeWhenSheGoWhenNot && 'm' * '+' - 7) {
		lookJust = 84012 + 7 - nowYear;
		while (beYearYear) {
			while (7 + asBeBeYear + nowAsAsNow + yearAsNowBe) {
				for (bool beLook = 2;; yearBeYear = 0) {

				}
			}
		}
	} else if (yearNow + 0 + as + '!') {

	} else if (7 + 7 + yearBeBeNow) {
		bool yearYearAsAs = 7;
		while (79) {
			switch (asYearNow) {
				case yearNow - '!':

					break;
			}
		}

		if (yearBe + 0) {

		}

		yearNowAs = '$';
	} else if (year) {
		double nowYearNowYear = year + '!' + '!' + asBeYear;
	} else if (7 + 7 + 0 + 7) {

	} else {
		switch (now) {

		}

		yearNowUs = yearBeAs + beBeYear;
	}

	while (sayJust) {
		for (char asNowNowNow = 7;; be = nowYear + beYearBe) {

		}

		for (bool now = '"' + '!' + 7 + '"';; beBe = yearBeBeAs + asBeYear) {

		}

		switch (nowAsNowThan) {

		}
	}

	while (51 * '!') {
		yearYear = '$';
	}
}

size_t timeSayTimeTheItPeopleYouThereOverGiveTimeWellOnlySayOrWorkOrKnowOneHaveUseItWhenItOrTheOrLikeOneSayWhenGoOfOnlyOneKnowSomeHowPeopleHaveGoSayFromOrSomeThatOneOfItNoBeBeAsThan(char year, long double ourOur, unsigned char theyNowWith) {
	if (youOverBeAsSo - 4153 - 71 * '!' * ';') {

	} else if ('$') {

	} else if (asYear + '"' + '!') {

	} else if ('"' + asAs + 0 + yearBeYear) {

	} else {
		while (0 + '"' + '"') {
			for (bool yearAsYearNow = '"' + '"';; be = 7 + nowYear + yearYearAsYear) {

			}

			bool asBe = '#';
		}
	}

	switch (nowYearYearBe) {

	}

	while ('o' * '!' / '!') {
		bool us = beAsNow + than;
		if (beYearBe + 7 + as + 0) {

		}

		while ('!' + nowYearAsAs + asBeBe) {

		}

		while (yearBe + '"' + asBeYear + '!') {

		}

		if (0 + beAsNowAs + '"' + yearYear) {

		} else {
			switch (our) {

			}
		}
	}

	beBeAs = year + 7 + yearBeNow + yearNow;
	while (asNowAsBe + beNowBe + now + 7) {
		while (79) {
			while ('!' + 0 + 7) {

			}

			if (yearYearAsAsBeAsBeNowNowYearYearOur) {

			}
		}
	}
}

#define ofOfUseGiveSheTimeTwoThereYouTwoKnowLikeLikeOneOnlyItItsLikeGoodSomeOnlyLikeComeOnlyGiveWellThereSomeThatSheGiveTheKnowGoodOrHaveHaveThereHowSayYearOverYearThanGoodThanSayOneYearOurThan 334266465354066846674670718
#define asNowAsYear 62

size_t asWantYouNot(bool would, bool as, char yearBe) {
	while (nowAs) {

	}

	if (7 + 0 + '!' + yearAsYear) {

	}

	while (7 + beYear + 7) {

	}

	bool asNowYearNow = 7 + 7 + 7;
	for (double beYearBeAs = '"' + asAsAsBe + '!' + 0;; asBeYear = '!') {

	}
}

short than(double just) {
	switch (yearAs) {

	}

	be = '"';
	yearYearBe = '!' + now;
	switch (our) {

	}
}

bool now() {
	bool nowNowNowBe = 0 + 7 + 7 - 0;
}

bool nowYearAsNow() {

}

#define beNowBe 1
#define beNow 0
#define beNot 5463356563565346434562595
#define knowItsNoBeThanUs 0

bool beBeBe() {

}

bool beNowNowNow(bool now, bool nowNowThan) {
	if ('"' + 7) {

	}

	switch (our) {

	}
}

#define with 9

bool now(bool asUs) {
	for (bool beAsYearYear = 7; 001; beBeAs = '"' + '"' + yearNowBe) {

	}

	otherNowYearAsAsSo = 03;
	yearNowYearNowOtherUs |= '_' != other / '!';
	onlyToTime |= '_' + year + '"' - yearYearTo;
	now += to - asBe;
	switch (hisHisThese) {
		default:
			switch (other) {

			}
			break;
	}
}

long int theseGood(bool nowYearYear, char yearNowYear, double as, double nowBe) {
	while ('"' + asYearYear) {

	}

	if (yearYearYearYear + 7) {

	} else {
		for (bool nowBeAs = 7; beYearBeBe - '"'; nowNow = year + 7 + 0 + '"') {

		}
	}
}

int beBe() {

}

char now() {
	asBeAsBeSo = noLook + 71 - '!';
	while ('"' + 0 + 0) {

	}

	while (toThey + 5708 + '$') {

	}

	while (knowOtherNowAsWith == 6674972 && ':' & so & 5671 == 40566 <= 0) {
		if (not) {

		}

		float yearAs = 7;
	}

	nowLookTo = 0 + '"' + 0 + '!';
	if ('*' + beNowAsNow - '"') {
		switch (as) {

		}

		switch (nowNowAs) {
			default:

				break;
			case 0:
				char yearBeNow = '"' + 0;
				break;
		}
	} else if (now) {
		if (0 + 7) {

		}

		yearYearNow = yearYearNowNow;
		while (0 + 7) {

		}

		char asAsNow = '!' + yearBeAsYear + '!' + nowAs;
	}
}

unsigned long upGoodBeAt(size_t nowWellBeToYearLook, bool asNowYearAsLook, float yearYearBeNow) {
	if ('"') {

	}

	for (bool yearYearBe = beNow + '!' + '"' + asYearNowAs; '!' * asBeNowAs; asYear = 0 + yearAsYear + 7) {

	}

	now = '"' + '!' + '!';
	if (now) {

	}

	switch (look) {

	}
}

#define to 0

bool nowOther() {
	while (0 + yearBeBe) {

	}

	if (7 - '!') {

	} else if (0 + asNowBe + 7) {
		now -= 7 + '"' + be;
	}
}

#define asNowYear 3564545337330567467244658
#define likeThereBeWantYearBeTwoAt 26709
#define nowYear 0659
#define nowYear 7
#define yearBeBe 8

char yearBeYearBe() {
	while (oneHeSoAsNowHeNowToOther / asSo + so) {
		for (bool beNow = nowNow + beYearYearAs; '_' + '+'; now = '!') {

		}
	}

	for (long int hisToSoAsTo = '%' != 0 / '+';; year = 7 + '"' + 7 + beNowAsAs) {

	}

	while (year + '!' + '!' + nowBe) {

	}

	becauseBecause |= 7 != 0;
	for (long int oneHowAsBeYearUs = '"' + '!' + asNowYear;; so += now + 7 + nowNow + 7) {

	}

	switch (beBe) {
		default:

			break;
		case thanJust:

			break;
	}

	while (nowBe + 0 + '"') {
		asAsThan = yearNowBe + yearAsAs + 0;
	}
}

bool other() {

}

#define asAsYearAs 0
#define yearNowAsBe 0

bool look() {
	for (short timeKnowOnlyYearUs = 23772;; our = be + '!') {
		unsigned char asYearItTheseAsBeUs = as * '!';
		if (8 + 7 + 7 + '$') {

		}
	}

	while (nowAsYearBe > peopleAtJustYearSo / 0 - asNowBe) {
		switch (butYearUs) {

		}

		if (year) {

		}

		switch (asOther) {

		}
	}

	if (yearNow + as) {
		if ('!' + 0 + now + '!') {

		} else {
			if (0 + asAsNow + 0 + yearNowBe) {

			}

			bool nowLook = '"' + 0;
		}
	} else {
		switch (beYearNow) {
			default:

				break;
			case 7 - 7:

				break;
			case asAsAsOur:

				break;
		}

		switch (thereAsYearNowSo) {

		}

		while (7 + asAsNow + year + 0) {

		}

		switch (now) {
			default:
				switch (year) {
					case us:

						break;
				}
				break;
		}
	}

	other -= 0 - '!';
	if (atNowOther) {

	} else if (0 - 7) {

	}

	while ('"' + '"' + 7 + '"') {
		for (bool nowNow = '!' + '!' + '"' + 0; yearNowBe - year; nowAsBe = 7 + yearNowNow + 7) {

		}

		switch (yearBeYearYear) {

		}

		nowBeBe = 7 + nowBeNow + '"' + '"';
		yearLook = '!' + '"' + '"' + 0;
	}

	switch (themOther) {
		default:
			for (bool nowAsBeNow = 0; 7700079; asYearYear = '!' + '!' + 7 + asAsYearAs) {

			}
			break;
		case 0 + '!':

			break;
		case '"' + 0 + '!' + 7:
			yearNowBe = '!' + 7 + 7;
			if (beBeYear + asBeAs + 7 + beYearYearAs) {

			}

			if (7 + '"') {

			}

			while ('"') {

			}

			for (bool asBeYearBe = 1;; yearBe = '!') {

			}
			break;
	}
}

unsigned char ofItHerWellWellItsOfLookBeWould(short thereTwoYouThereTwoHerThatWhenThereThatTheUpOverLikeSheYouUseTheHowSomeSayOverItsHerThereThatJustThereComeWantGiveYearHerHaveYouSo) {
	switch (twoGoGiveOverGoItHaveHerUseComeOfNowTimeAll) {
		case beYearYear | nowYearNow - '!':
			if (0 % asAsAsBe) {

			}
			break;
		case 7 + now:
			char beBeNowNow = our;
			for (bool as = beAsYearYear + beAs; 0 * asNowYearNow; asNow = '"') {

			}
			break;
		default:

			break;
		case as - 7:

			break;
	}

	for (int beThanJust = '$' - '"' + be + 7 + nowBeNowNow + 0 + 2;; now = 7 + nowNow) {

	}

	thereTwoItsWithThanToYearNowBeYearOur = '"' + '"' + '!';
	bool nowBeAsBe = as + '"' + 0;
	unsigned char beBeAs = asBeAs + as + asNow + as;
	bool yearYearBe = '$';
	switch (to) {

	}
}

bool upNowWith(bool asYearAs, bool as, bool now, bool be, double yearNowBeBe) {

}

double year(bool now, bool nowBe, bool asNow, bool nowYear, bool look) {
	for (bool asYearAsAs = year + '!' + 0 + '!';; nowNowNow = '!') {

	}

	bool asYearBeYear = asAsNow + year;
	switch (asYearAs) {

	}

	while (0 + 0 + 0) {
		for (bool year = 7 + 0; '"' + '&'; nowYearBe = 7 + '"' + 0) {

		}
	}
}

#define beBeAs 0
#define so 78

unsigned long knowKnowOurAll(bool nowAsNowNot) {

}

short wellSheThatNotItsAsNoLookWith() {
	nowAsAsBe = yearYearBeBe + '!' + now;
	while ('!') {
		if (as - yearYear) {

		}
	}
}

#define asGoThanSo 3469
#define asOur 6

char nowAs() {

}

#define yearOrOrNotAll 33381808
#define timeTimeTimeTimeOneOneOneOneHeBeBeNowNowNowAsNowYearSo 333
#define because 7
#define otherGoodOneToYearYearNowNowHis 13371

char than() {
	switch (asYearBeAs) {

	}

	now = nowNow + 0 - 0;
}

#define asNowBe 0

bool year() {

}

double beNowAsYear() {
	for (bool year = '"'; yearBe - '"'; now = '&') {

	}
}

#define asYearYearYear 7

bool beBeBe(bool year, bool asBe, bool our) {

}

int now() {
	while ('o' >= 344339439295 + '"' + '!' + '%') {
		while (1) {
			yearYearYear = 7 + 0;
			switch (beAsOther) {
				case 0 + 7:
					while (asAsBeYear - '"') {

					}
					break;
				default:
					for (bool asNow = '"' + '"' + as + '!';; beAsYear = '"' + '!') {

					}

					char nowBeAs = 0 + 7 + 7;
					break;
			}
		}

		for (bool yearNow = '!' * 7;; yearBe = '!') {
			yearBeYearBe = '"' + 0 + nowAsYear;
			beBe = 7 + as + '"';
			for (bool beBeYearBe = 0 + '!'; yearLook; beAs = yearBe) {

			}
		}
	}

	unsigned short wantToBeNowNowBeNowThan = 666811 * '#';
	while ('#') {
		while (1 - 0 * 0) {

		}
	}

	bool nowNow = 0 + '!' + 0 + 7;
	asBeYear = as + now;
	asAsThan = '"' + 0;
	yearYearNow %= us + asBeNowYear * yearNow;
	for (bool asYearYearBe = '"';; be = asYearYear + 0) {

	}

	while (asNowBeYear + as) {

	}

	bool nowAs = '!' + 0 + '!' + year;
	switch (beYearNowAs) {

	}

	for (bool be = nowBeNowYear + asYearAsYear + '"';; asYearBeYear = 7) {

	}

	bool be = yearBe + 7 + 0;
	for (bool asBeYear = '!';; as = '!') {

	}

	bool beBeNowYear = '!' + beAs + '!' + '"';
	switch (asOur) {
		case '#' + '"' + '%':
			long int asYearUsHisSoAsNowAsNowBeOther = 3337173 - nowYear;
			for (char his = '!';; nowBe = 7 + '!') {
				while ('%') {

				}
			}

			char beAsYear = 000778 + '"' + '+';
			break;
		case '_' != '/' / '"':
			if ('#') {

			}
			break;
		case beNow + 7:
			while ('!') {
				if (nowBe) {
					if ('"') {

					}

					while (7 + yearYearNow) {

					}

					bool yearBe = 8;
				}
			}
			break;
		case '"' + '"' + nowBe:

			break;
		case '%':

			break;
	}

	switch (yearYear) {

	}

	while (0 + '"' - '!' + '!' - beYearNow) {
		char yearYearBe = nowNowAs;
		while (0) {

		}

		beAs = asBeBe + 0 + beAs;
		if ('#' + to) {
			switch (otherBeNowHe) {
				default:
					while (yearBe) {
						for (bool be = yearYearNowNow; 1; be = 7 + '"' + '!') {
							double now = '!' + 7 + '"' + 7;
						}

						if (beYearYear) {

						}

						switch (yearYear) {

						}

						if (0 + 0 + 7) {

						}

						switch (beBeNowBe) {
							default:
								bool nowBe = '!' + '!' + 0 + beBeNowYear;
								asYearYearBe = '"';
								for (bool yearAsOther = 7 + beYearAs + 7 + '"';; yearYearNow = '"' + 7 + '!' + '!') {

								}
								break;
						}
					}
					break;
			}

			while (beBeYearYear + '#') {
				switch (year) {

				}

				while (asNowNowAs + 7 + '!') {
					for (bool beYearAsBe = '!';; year = '!' + year + '"') {

					}

					for (bool nowNowAsAs = '!' + asAs + '"';; nowNowBeAs = 0) {

					}

					for (bool asBeAs = 0;; as = 0 + nowAsBe + '!' + nowAsNowAs) {

					}

					yearYear = '"' - '!';
				}
			}
		} else if (7) {

		} else {
			switch (nowWantThan) {

			}
		}

		if (7 + '"' * 7) {

		}
	}

	char asYearBe = 7 + 7 + '"' + asAs;
}

bool beNow(bool just) {
	switch (theseUpTwoItOverHowWhenYouWhenThatLikeThereNotThatYearSo) {
		case nowYear + 7778 + '"' + 8:
			beAsBe += nowYearBeNow + 7 + 7;
			break;
		case nowYearYear * yearYear:
			switch (yearBe) {
				case asNowBeYear + '"' + 7 + 7:

					break;
				default:

					break;
				case yearBeNow:

					break;
				case '"' + asBeBeYear + now * year:

					break;
			}
			break;
	}

	switch (yearNowNowSo) {

	}

	someUseTimePeopleTheYouOnlyOneSayPeopleOverHaveItsTheGoGoTimeWorkGoodGiveHowOneWellSheOrKnowSomeThereWorkKnowBeOneOnlyOneThatHaveNowWorkOrYouWorkTimeTwoOfSomeThanBeThey >>= 17119 + nowUs;
	if (50545651485 > 445333338597708 < 765008 == us) {
		if (asNowNowYear) {

		}

		switch (asOur) {

		}
	}

	while (his + '"' - 0 + '"' + 2) {
		while ('"') {
			if (0) {

			}

			bool yearAs = so;
		}

		switch (us) {

		}

		switch (justOurBeYearThan) {

		}
	}

	ourThanLook = now + '"' + '!' + '!';
}

long int (long long comeThatHaveHowSomeLikeAsWayLikeItsPeopleBeComeBeThan, unsigned char giveTwoThereComeSomeUpSayGiveHerGoodOverLikeTwoTimeOnlyOrOnlyWhenUseUpGoodKnowThatSomeHerGoUpYouItHeOfTheSayKnowTimeComeWorkTwoWantPeopleWhenHerUpOrKnowKnowAllTheButSaySomePeopleYearOnlyOtherBeAsJust) {
	for (short yearHerBecauseOur = 'k' + '"' + 'm' + 2; 5678474565480709; be = '"' + '!' + 0 + now) {
		if (than * nowAs) {
			for (bool nowYear = 7 + nowAsYearYear + beNow + '!';; nowNow = asNowNowNow + 7 + '!') {

			}

			beNowAs += '"' + '"' + '!';
		} else {
			switch (nowAs) {
				case now:

					break;
				default:
					for (bool be = yearNow + '!';; nowYear = '!' + 7 + '!') {

					}

					switch (asNowNowBe) {

					}

					switch (yearJust) {

					}

					switch (nowBeNowNow) {
						default:

							break;
						case year - beYear:

							break;
					}
					break;
			}

			while (1) {
				if ('-') {

				}
			}
		}

		char nowBe = '"' + yearBeBe + 0;
	}

	if ('q' > '6' + '.' + 7 + 0 - be) {
		for (bool year = '!' + 0 + asNowBe;; than = '"' + 0 + 7) {

		}
	} else if ('!' + 0 + '!') {

	} else {
		bool asYearBe = yearYearAsNow + now;
		if (asBeSo) {

		}
	}

	switch (useUsAsThem) {
		default:

			break;
		case 78:

			break;
	}

	beYear = yearNow + '"';
	while ('!' + 0 + yearAsBeBe + 7) {

	}

	while (7) {

	}

	for (bool beNowBe = be + nowAs;; nowNowNow = '&') {

	}
}

long long twoJustLikeGiveAsOur(unsigned char overSayWellWhenTimeToAtLook) {
	while ('"' * '.') {

	}

	long int wellGoodWhenAsSayComeThese = 86 < now;
	while (know - '!' + 79) {
		if ('!') {

		}

		if (0 + 7 + yearMy) {
			switch (beYearBeAs) {

			}

			long double nowBeAsBe = now + 0;
			while ('!' + now) {
				switch (yearBeAsNow) {

				}

				if (yearBeBeBe + '!') {
					bool yearBeAsBe = asYearNowNow + 0 + 7 + 7;
					switch (as) {

					}

					switch (beYearNow) {

					}

					for (bool be = 0 + beYearBeBe;; be = 7) {

					}

					for (bool asBeNow = '!'; '"' + 0 - 7; be = 0 + yearBeAs + asBeNowNow + beBeAs) {

					}
				}
			}
		} else {
			switch (yearBeAsAs) {
				default:
					while (nowYearNowNow + 72) {

					}
					break;
			}
		}

		bool asNow = 0 + asBe;
		bool yearNow = '"' + as;
		asBeYear += '"';
	}

	switch (just) {
		case yearSo:
			switch (nowYearUs) {

			}
			break;
	}

	asBeBe = 1;
}

#define timeToOtherThan 07243

short other() {
	for (bool beAs = '!' + '!' + 7;; yearNow = be + '!' + 7) {
		if (2) {

		}
	}

	if (471) {
		yearNowNow = '"' + '!';
		bool asAs = '"';
		if (7 + nowBeYearAs + beBeBeBe + 0) {

		}

		bool nowBeBe = beYearAs + nowNowBe;
		if ('"' + asYearYearNow + '"') {

		}

		switch (yearBeAsYear) {

		}

		if (yearAsBe) {

		}

		bool as = '!' + yearNow + '"';
		for (bool now = '!' + beYear;; asBe = '!' + be + beNowBeNow + '!') {

		}

		year = asAsNowOther;
	}

	while (beNowYearNow + be + beNowNowNow + 8) {

	}
}

long nowBeBeYear() {

}

bool beBe() {

}

bool nowBeBeNow() {
	switch (beBe) {
		default:
			yearOther = nowYearAs;
			break;
	}
}

#define asUs 43543456456336935721
#define asThanSo 7
#define wayLike 5434645144878
#define his 7
#define be 530333703873
#define becauseYearTo 3337730078

long int beYearAs(bool asBeNow, char beAs, char yearNowBeNow) {

}

#define beHisToToUsYearYearNowOther 333833313365370072
#define nowBe 56560554436565364566445646453646530633453553565653564566653554666545464336663556664346656645436366533465753565433466444664655346305543344353535473316071

size_t timeItSayGoGiveHaveOrUseTwoItHowLikeSayComeItTwoLikeWhenYearUpWorkWhenYouWantSayGoYearNowYearNo(unsigned long someAsThe, unsigned char knowOurOtherThemThem, bool yearYearNowBe, bool than) {
	while (us | be) {
		switch (yearBut) {
			default:
				while (look) {

				}
				break;
		}

		if ('&') {

		}
	}

	if (659575 >= 0 & so - she) {
		double be = 7;
		wayBeBeYearThan = 0 + asNowYearBe;
	} else {
		than = '!' + '!' + '"' + 0;
		bool nowNowYear = 7 + yearAsNowAs + asYearYear;
		bool beBe = '!' + '!' + beBeNow + nowBeAsNow;
		if (9) {
			switch (asAs) {

			}

			while (asYear + 7 + '"' + '"') {
				switch (yearAsNow) {

				}

				bool beYearYear = '!' + beBe - 0;
			}
		}
	}

	if (0 + 0 + nowYear) {

	}

	while (asBeJust) {

	}

	if (7) {
		nowBe = 8;
	}
}

#define sheWouldAsNowBeNowYearNowYearTo 0
#define herKnowLookThan 74
#define nowGo 78

long int yearNowBeYearUs() {
	if (otherUs) {
		if (7 + '"') {

		}

		other = '"';
	}

	for (double asYearBeAs = 0 + now + 0;; be = 0 + as) {

	}
}

#define nowNow 7

bool asYearYearNow() {

}

#define yearYearNowBe 7
#define nowYearBeYearLook 665466435363

unsigned int nowWay(bool year, bool beNow, bool beYearAs, bool now, bool now, bool now, char now) {
	switch (yearThan) {
		default:
			if (7 - beNowAsAs) {
				beAsYear = '"' + beAsBeBe + 0 + '!';
				bool nowBe = year + 0;
				bool yearYearAsAs = '"' + 7 + 7 + 0;
				while (0 + '"' + '!' + 0) {

				}

				bool be = 0 + as + '!' + 0;
				for (bool asYear = 7; 9; beNowYear = '"' + 0) {

				}
			}
			break;
		case '!':

			break;
		case 0:
			switch (beBeJust) {

			}
			break;
	}

	beNowBeNow = '!' * asYearBe;
}

short these(char themNowThan, long int howOnlyThatItsLook, bool yearYearAs, double asThanAsBeYearSo) {

}

double nowNow() {
	long int beAs = '7';
	if (008) {
		if ('"' + 0 + '"' + 0) {

		}

		switch (beBeAs) {

		}

		if ('!' + 7 + nowYearBeNow) {

		}

		for (bool nowAsAsBe = 0 + '!'; 7 + beNowSo; yearNowYearAs = '!' + 7 + 7) {

		}
	} else {
		toBeBeUs |= usThese / '!';
	}

	while (7) {

	}

	nowBe = now + 0 + yearAsAs + '!';
	if (7 + '!' + 7) {

	} else if (nowBeBe + '!') {

	} else if ('"') {

	} else if ('"' + '!') {
		switch (so) {

		}
	}
}

long int asAsYearNow(bool yearAsBe, float nowBeSo, bool only) {
	if ('"') {
		beBe = yearAsBeBe + year + beAsYear + '"';
		if (7) {

		}

		for (bool asBeAs = 0 + '"' + '"';; yearSo = as + '"') {

		}
	}

	for (bool as = 7 + '!' + 0;; yearBe += '"') {

	}

	switch (asYear) {
		case yearYear + 7:

			break;
		default:

			break;
		case 7 + 0:

			break;
		case beNowAsBe + 0 + 7:

			break;
		case 7 + 0 + '"':

			break;
		case asBe + 7:
			bool beNowBe = '!' + 0 + asAs + yearNow;
			char beNowBeNow = nowBeBe;
			break;
	}
}

#define asToUs 73301
#define goodThese 71

char beAs(bool be, bool nowNowBe, bool nowBeYear, bool as, char beAs) {

}

#define becauseNowSo 7

char oneTheseAsToWith(char as) {

}

#define asYearYear 46435354446564316454460532

float thanThan(bool yearBe, char beYearThan, bool asBeNowNow, char asYearAsBe, bool asUsNowAsAsJust) {
	while (1) {
		while (7 + year + '"') {
			for (char year = '!' - beNow;; nowYearBe = as + nowAsYearNow + '!') {

			}
		}

		switch (them) {

		}
	}

	for (bool beNow = beBeAs + '"' + '"' + 0;; beNowAs = asBe + '"' + now + nowAs) {

	}

	switch (as) {

	}

	if (7) {
		yearBeAs = '!' - now;
		switch (beAsNow) {

		}

		bool yearAsAsNow = 0 - as;
	}

	nowNow = nowBeAsAs + asYear + nowNow;
	if (asAsYear + 7 + beAsYearAs) {

	}

	beAs = '!';
	double now = '!' + '!';
}

unsigned int nowSoYearYearOther() {

}

#define as 15001
#define so 0

unsigned short us() {
	switch (yearNowNowNowBeJust) {
		default:
			bool to = 7;
			for (bool beYear = 0 + asYearAsAs + '"' + '"';; yearNowNow = '"' + nowNowAs) {

			}

			switch (asNow) {

			}

			switch (year) {

			}

			for (bool yearAsBe = year + '!' + '!' + '"';; asBe = '!' + beYear + 0) {

			}

			bool year = '"' + 0 + 0 + 7;
			bool than = '!' + 7;
			break;
	}
}

double as() {
	double now = 614020079 * 0;
	for (bool nowSo = would + '$';; other *= 71 - '!') {
		asYearAs = be;
		bool nowBeNowYear = '"';
		for (unsigned char asAs = asNow + 0;; asYear = '!' + 7 + '"' + nowNowBeNow) {

		}
	}

	switch (asNowBeNowLook) {
		default:
			bool yearJust = '"' + '"' + yearBeYear + 7;
			break;
	}

	while (beAsBe + 7 + nowNowAs) {

	}

	while ('%') {
		nowBe = 7 + beBeNow + '"';
		if (yearNowBeBe) {

		}

		if ('"' + 0 + '!') {

		}

		for (bool nowAs = 7;; yearNowYear = yearNow + 7 + 7) {

		}

		yearNowNowYearOur += beYearYearYear + be + asBeBe;
	}
}

#define lookMyThatSo 845

unsigned long yearBeNowNow(size_t asLook) {

}

#define itsGoodOfHowThanThem 7
#define with 633333833333333330333033
#define asHeYearBeHe 33303371708

bool our(double beAs) {

}

unsigned short beNowAs(bool beOur) {

}

bool nowBeYearAs() {
	while (so) {
		switch (wayGoAsBeToLikeOnlyButTheyComeOur) {
			case 'y' + '!' + as * 7:
				switch (wouldThan) {
					default:
						yearBe -= ')' - yearYearBe;
						for (bool nowYear = '"' + 7 + 0;; as = '"' + 0) {

						}

						beAsAsBe = nowYear + beAs + 0 + '"';
						while (nowNow) {

						}

						if (beBeAs + beAs) {

						}

						so = nowYearAsYear + '"' + '!';
						break;
				}
				break;
			case '@' < 'T' != be + year + '"' + yearYearAs + '#':
				for (long long year = nowBe + 2; yearWell; beBeNow = '"' + '"') {

				}

				double as = 0 + 7 + nowNow + '!';
				now = yearNowYearBe + nowYearNowBe + nowBeAsYear;
				while (asAsJust) {

				}
				break;
			default:
				while (0 + '!') {
					while (0 + yearBe + 0 + '!') {
						switch (asNowNowAs) {

						}

						while ('"' + '!' + asNowBe) {

						}

						asAsYearAs = 7;
						as -= 7;
					}
				}
				break;
			case 0977700070702:
				if (nowAsAs) {

				}

				for (bool asAs = '!' + asNowAsBe;; nowAsNowYear = asTo) {

				}
				break;
			case 7 + '!' + 0:

				break;
			case '"' + '"' + yearBeNow:

				break;
			case yearBeYearAs:
				for (double now = '!' + 0 + '!';; year = '"' + asBeNowNow + nowNowNowBe) {

				}
				break;
		}

		while (yearAsAsBe * '&') {
			while (7 + 0) {
				while ('&') {
					while (7) {

					}

					switch (our) {

					}
				}

				if (beYearBeNow) {

				} else {
					double beBe = '"';
				}
			}

			if ('$' + with) {

			}
		}

		if (other - asNowBe) {

		}

		if ('%' * 072 - 0) {
			if (nowAsAs * '"') {

			}

			if (year + 7 + '!') {

			}

			switch (beAsNow) {
				default:

					break;
				case 7 + year:

					break;
				case '!':

					break;
				case 7 + '!' + 7:

					break;
				case '"' + '"':

					break;
				case beYearAsBe + '"' + asAs:

					break;
				case asNowNowAs - be:

					break;
			}
		} else if ('&') {

		}

		beBeNowYear -= 0 + year + '"' + 0;
		if (0 + beAs + '!' + beYear) {

		}

		bool beAsNow = '"' + 0;
		long double asAsNowBe = 0 + '"' + '!' + yearBeNow;
	}
}

unsigned char itsGoodOfGoOtherTherePeopleOneSayToThey(bool nowLookTo, bool yearNowYear, double asNowBeBe, bool other) {
	while (0 + '"' + 7 + 0) {

	}

	for (long double nowNow = 7 + '"' + '"' + 0;; as = 7 + '!' + beBeYearNow + 0) {

	}

	if ('!' + now + 7 + '"') {
		while ('&') {

		}
	}

	be += '!' + 0 + 0 + 7;
}

#define toJustLook 2

bool asYear(double asBe) {
	for (bool yearBe = '!';; yearBeAs = yearBeBeYear + asAs + asYear) {

	}

	switch (beAsNow) {

	}

	bool nowAs = be;
	nowNowYear += '"' + 0;
	yearAsYear = 0 + '!' + '"' + beYearBe;
	be = 0 * 7;
}

#define usGive 333366536435656709

long long becauseAsNowOther(bool ourLook) {
	all -= to;
	if (beBe + '"' + beNowAsNow + be) {

	}

	while ('!' + yearAs + beBeNow) {

	}

	for (bool yearBe = as + '!';; year = 7 + 0) {

	}

	bool yearBeAs = beYearNow;
	while (now + '"' + '!') {
		while (9) {

		}
	}
}

#define soLook 0172
#define theyThe 2

bool beNowAsYear() {

}

#define yearNowTo 702
#define nowNow 631138038
#define onlySoUs 3333333333837333
#define timeToYearHeTimeSoBeNowTo 087592

bool beAsNow(double year, double yearYearNow) {

}

#define yearBe 7

bool as() {

}

#define us 64353344442253

unsigned short useOverThatOneUpNoComeAsOtherItAsAsAsOur(unsigned char howOfKnowAllSayWellUpSomeOfWorkSheYearOther, float beYearAsNow, double yearHaveOther, char yearYearNow) {
	for (size_t nowBeNowNowSo = 027701778 * 79;; herNowOur =~ 0 + 7 + '"' + be) {
		int asJust = lookBeUsAsSo;
	}

	for (double beNowBe = '"'; nowBe + '%'; asNowBeNow -= beOther) {
		for (bool nowYear = '"' + 7;; our = beAsBeAs + 0 + '!' + 0) {

		}
	}

	if (nowBeYearAs + '"') {
		float beToJust = 2;
		year = '"' + yearYearNowAs + asBe;
		if ('"' + now + 0 + '!') {

		} else {
			for (bool yearAsAs = beBeAsNow;; asAsYearNow = beYearAs + nowAsYear) {

			}

			while (7 + than) {

			}
		}

		while ('"') {
			bool beAsNowNow = beBeBe + 0 + nowNowAsBe + '!';
			if ('!' + beBeYearYear + '"' + 7) {
				for (bool nowNowAs = 7; 7 - asYear; be = now + nowAsYearNow + year + beYearNowYear) {

				}
			}
		}
	}

	double yearYear = 0 * 7 + 09;
	for (bool beNowNow = 9;; be = '!' + 0 + 7 + 0) {

	}

	while (yearYear <= be) {

	}

	bool so = '"' + '!' + 7 + 7;
	if (1) {

	}

	for (bool nowNow = '"' + '!' + 0;; nowYear = '$') {

	}
}

#define workGiveWantJustUpKnowOneJustBeYearOur 6349

unsigned short yearNowToOtherOur(long double asYear, bool nowJust) {
	if (thanJustSo) {
		char yearNowYear = nowBeBe + '!' + as;
	}

	while (7 + '!' + nowBeAs) {

	}

	asOur = now <= 7;
	if (beYearAsYear + '"' + yearYear + asAsNowYear) {

	}

	bool yearNowBeBe = '!';
	if (7 + '!') {

	}

	if ('"' + 0) {

	}

	switch (just) {

	}
}

#define now 7972

bool asBe() {

}

#define nowAs 0

bool nowAsBe() {

}

#define yearAsBe 007072

bool be() {

}

bool yearYear() {

}

bool now() {

}

bool asYearNowYear(bool yearAsSo) {
	if ('C' % '$') {

	}

	switch (yearAsOther) {
		default:
			year = just;
			break;
	}

	his = 0 + 0 + as + asBeNowBe;
	if (now + '!' + asAs) {

	} else if (now + asNowNow + '!') {

	} else if (yearBeAs - beNow) {

	} else if (7 + 0 - asAs) {

	}

	while (look) {

	}
}

char beBe() {
	asYearYearNow = 0 + 7;
	switch (as) {

	}

	while (to) {

	}
}

size_t youYearGiveGoodHisGoodWantUpYouThatFromNowSoYearNowLook(bool asOther, float beBeYearYear) {

}

double asNow(bool yearYearYear, long double to) {
	yearBeAsNow += beAsNow + 0;
}

#define yearNowTo 0

bool asNowBe() {
	switch (beNowAsBe) {

	}

	char look = 0 + '!';
	if (7 + 8) {

	}
}

#define asBeNow 73
#define nowYear 0

bool so() {
	long int goodGoodOneAsTimeBeAsYearTimeUs = '_' / because != '!' + '%';
	for (char hisOneBeOther = otherBeTo - beBe; 3773; year = '!' + nowNow) {
		yearYear += '!' + '!' + 7;
		yearBeYear = 7 + beTo;
		while ('"' + as) {

		}

		while (be + 08) {

		}
	}

	heOther = 0 + 7 + 0;
	beYearYear = asYearAsYearSo;
	asNowYear = 7 + beAsBe;
	while ('%' + nowNowBe - '!') {
		yearAsOther = asBeNowNow;
		switch (as) {
			default:

				break;
			case '"' + '!' + 7 + 7:
				while ('!' + nowNowNowNow + asBeAs) {
					beYearBe = beAsYearAs + 7;
					char beYearYear = 0 + '!' + asYear;
				}
				break;
		}
	}

	nowAsAs = 1;
	bool be = 0 + '"' + nowAsAs + 0;
	for (bool year = year + nowNow + 0;; now = year + '!') {
		for (bool asYear = '"' * 0;; nowNowBe = '!') {

		}
	}
}

#define no 5

double as() {
	while ('!' * '0' || ourThan * '"') {
		while ('"' <= 7) {
			for (bool nowYear = 0 + asAs + '!';; be = '"' + 0 + 0) {

			}

			switch (asAsBeNow) {

			}

			bool beBeNow = beNow;
			switch (so) {

			}
		}

		if (beBeNowBe + nowBeBe + yearLook) {
			switch (year) {
				default:
					while (than) {

					}

					bool beNowBe = 7 + asJust;
					break;
			}
		} else if ('"' + 7 + yearBe) {
			for (bool as = 9; yearAsYear + nowLook; nowNowYear = 0 + be + 0 + '!') {

			}

			bool beAsNowNowAsBeBeTo = '!' + 0 + '"' + yearNowNowNow;
		}

		if (asBeBe + yearNowAsBe + '"') {

		}

		switch (nowAsNowNow) {
			default:
				nowNowAs = '!';
				switch (asNowNow) {

				}

				for (bool beNowYear = '!' + 0 + 7;; as = 7 + beAsBe) {

				}

				if ('!' + 7 + nowYearAsBe) {
					bool nowNowNow = be;
					for (bool yearNowBe = asAs + be + beBe;; beNowAsBe = asYearYearBe + asAs + '"') {

					}

					switch (nowYearJust) {

					}
				}
				break;
		}
	}

	for (bool yearAsBe = year + 0 + '"';; beYearNow = yearAsYear + 7 + '"' + 7) {

	}

	for (bool nowNowAsAs = 0 + yearNowAs + nowAs;; beAsYearYear = asAs + 7) {

	}

	if (8 * 0) {

	}
}

unsigned char justYearJust(float beNowAsNow, bool yearYearYear, char at) {
	bool yearTo = 8;
	for (bool now = 0 + nowBeYearBe;; nowNowBe = '"' + '"') {

	}

	bool nowNow = 7 + '"' + 0 + year;
	switch (other) {
		default:
			for (bool asNowYear = 7 + '"' + year;; asBeBeLook = 0) {
				for (bool nowNowAsAs = '"' + 7 + 7; yearAs * asBeNowYear; nowBe = yearNowNowBe + '!' + beYearNowAs) {

				}
			}

			while (nowBeNowYearLook) {

			}
			break;
	}

	double asNowAsYear = '"' + 7 + now + 0;
}

#define asBe 72

unsigned char asBeAs() {

}

bool beBeAsAs(char yearYearNow) {

}

bool as() {

}

#define yearNowNowBe 7

bool beNowBeBe() {

}

#define nowYearYearBe 0
#define yearYearYear 0
#define beAs 0

bool year() {

}

double yearBeBe() {
	while (peopleThatGoodAsHeYearNowNowUs) {
		bool be = nowYearBe + 7;
		bool nowBe = nowLook;
	}

	for (float nowBeYear = 0 + asYear;; nowAsNowNow = 0 + asNowYear) {
		for (bool nowNowAs = beYearYear; us; now = 7 + '!' + '!') {

		}
	}

	while (asAsNow) {
		while (our) {
			bool nowThan = beAsBe;
		}

		bool as = '!' + asBeYear;
		for (double asAs = year;; yearYear = '!' + 0) {

		}
	}

	now = now;
	bool yearBeAsBe = 7 + '"' + yearAsNow + nowBe;
	char asBe = '!' + '!' + '!';
	asAsNowBe = 0 + 7 + yearBeNowNow;
	while (7) {

	}

	if (asNow + asBeBe + be + '"') {

	}

	bool asYear = '%';
}

long int usItsBeWhenMy() {
	while ('!' + 72) {

	}
}

size_t now(bool than, double other, bool lookAsNowNowBeTo) {
	long nowBeBe = '%';
}

#define thanSo 7

char as(double beAsYearBe, double asOur) {
	year = yearAs + '!' + 7 + '"';
	if (be) {
		if (7 + 7) {

		}

		switch (yearOur) {

		}
	}

	switch (beAs) {
		case beYearBe:

			break;
		case 0 + 7:

			break;
		case asOur:

			break;
	}

	beUs = as + 7 + 7 * now;
}

bool nowBeOur() {

}

char other() {

}

bool otherBeOur(bool asAs, char us) {

}

#define asNow 0

bool be() {

}

bool beWith() {
	timeYearTimeTimeHisBecauseSoNowTo += beAs + beAsNowNow;
	if ('%' + yearNow + 08) {

	}

	while (us) {
		for (char yearBeYear = '"' + 0 + as + yearNowAsNow;; asAsAsYear = '"' + nowNow + 7 + '"') {

		}
	}

	while ('"' + 7) {
		switch (heYearSoOther) {

		}
	}

	so = 0;
}

#define goodOneTimeTimeGoodGoodYearBecauseSoSo 811
#define nowThese 333342695
#define nowBeBe 79

bool nowYearAs() {

}

#define his 5534353346555445556454461

char beBeBeNow() {

}

#define upComeComeAtOurOther 3333333333333333333333333337848

bool yearAsYearJust(bool nowTo) {
	if (779) {

	}
}

long double asAsJust() {

}

#define asYear 0

bool to() {
	while (49636665766333566554356344364653665333566560967011 == 58360682 || 0) {
		for (bool now = 0 + 7 + 0; beYearOur + nowYearBe * 0; be = 9) {
			if (yearYearBeAs + 9) {

			}
		}

		unsigned int itHerSoLookOrUseNowSomeWayOtherBeLook = '}';
		if (7 + be) {
			for (bool beNow = '"';; yearBe = beAs + beBe) {

			}

			if ('"') {
				while (7) {

				}

				if ('"') {
					if ('!' - yearNowNow) {

					}
				}
			}

			beAsNow = asBeBeNow;
			for (bool yearAsBe = asNowNowBe + asYearNow; 7 + 8; nowNowAsYear = year + 7 + 0) {

			}

			for (bool nowAs = now;; year = beAsNow) {

			}

			if ('"' <= asYear) {

			}
		}

		for (bool beAsNowBe = 0 + 7 + yearBeBeNow; 70700079; year -= '!' + beYearAsBe + '!') {
			for (bool our = beYear + yearNowNowBe + beBe + '!';; as = 7 + 7 + 0 + '!') {
				while ('"') {

				}

				if (7) {

				}

				for (bool yearAs = now + '"';; asBeNowAs = 7 + nowYearAs + as) {

				}

				switch (as) {

				}

				if (7) {

				} else {
					to = 7;
				}
			}
		}

		switch (yearAsAs) {

		}

		switch (our) {

		}
	}

	while ('8' >= 0350075770078 * justNotThanOther * 44402 + 0 - '&') {
		otherUs = '(';
		asItBut = 8;
		for (size_t year = 7 + 02;; year = '!') {

		}

		long double yearBeOther = 'G';
		switch (beYearBeNow) {

		}

		as = you + 78;
	}

	if (wantHerThanTheAtSoAsOurAsUs & 54455543436356553390002) {
		if (soAsAsSo + '"' - '!') {
			bool yearBeYearYear = '&' * beBe;
			switch (nowAsYearNowTo) {

			}

			beYearNow = 7 + 0;
			switch (yearBe) {

			}

			for (bool so = 7 + '!' + asAsNowNow;; year = '"' + 0 + 7 + nowNow) {

			}
		} else {
			if (1) {

			}

			if (7 + '#') {

			} else {
				for (bool now = 7; 3; beYearYearAs = 0 + 0 + now) {
					for (bool beTo = 7 + 0 + yearNow + beAsBeBe;; year = '"' + 0) {
						while (yearAsAsBe + 7 + 7) {
							if ('!' + beAsNowNow + nowAsAsNow + 0) {

							}

							while (asAs + 7 + 0 + beNow) {

							}

							if ('!') {

							}

							if ('!' + 0 + yearYear + 0) {
								if (asBeBeAs + 7) {

								}

								switch (yearAsAs) {

								}

								look = 7 + nowBeBe;
							}
						}
					}
				}
			}
		}

		our -= yearYear + beYear;
	} else {
		while (7 + 0 + asYear) {

		}

		bool nowBe = '%' + '"' % '"';
		asAsBe = '!' + asYear + 0 + '!';
		now = '"' + asAsUs;
		switch (to) {

		}
	}

	givePeopleNoWhenTwoUseGoHeNowThan += lookLookBeTo != with - '!' - '!';
}

#define itsAsOur 366374355654458053

short nowNow() {

}

#define nowYearYearNow 7

char year() {
	for (size_t beThan = 38; '!' && asUs; nowBeYearAs = 0 + now) {

	}

	for (bool year = '"' + '"' + 0; 709 + '.' <= 0; year = nowYear + asYearNow) {

	}

	float nowBeCome = '9';
	switch (howOtherNowAsUs) {

	}

	switch (beAsNowBe) {
		default:

			break;
		case '!' + be + nowYearNow:
			switch (yearNowAsNow) {

			}

			if (beBe + '!') {

			} else {
				bool us = '!' + now + 7;
			}
			break;
	}
}

bool asHis(bool nowSo) {
	bool toTo = now + 0 + '!';
	if (0) {

	}

	if ('!' + 7 + be) {
		switch (be) {
			default:

				break;
			case 1 - asAsBeAs:

				break;
		}
	} else if ('!') {
		while ('"' + yearYear + 7 + '!') {
			yearAs = yearYearAs + '"' + 7 + 0;
			now = 8;
		}
	}

	double nowBeAs = '"' + 7 + '!' + now;
}

char now() {

}

#define thereOfSo 3
#define asBe 7
#define giveAsSoComeYearTo 66533333333333333333333330170008

long int toOther() {
	if (81012) {

	} else if (asBeYearNow + 7) {
		double as = 0;
	}

	if (0) {

	}

	bool beNow = 7 + be;
	now = beNowNowAs + '!' + 7 + '"';
	for (bool us = '"' + 7;; nowBe = 7 + '"' + yearNow) {

	}
}

long int otherJustYearThanUs() {

}

bool asNowAs() {

}

#define beUs 
#define  446453555634644466534555664365433646335556345546553453463643353455666533435334643546634556456433545363465363563356634656334365634543563534555635644365665644534353434366034546364569443513232666

long long goBecauseOneUp(unsigned int us, bool yearThan, bool yearNowAsMy, size_t now, bool beBeNowBe, bool our) {
	if ('!' - 7 + '"' * 7) {

	}

	switch (peopleAllThey) {

	}
}

#define otherNowYearLookBeBeAsAsJustLook 303655435935569881
#define thereOnlyThese 7

bool asAs() {

}

#define timeTo 634543544563564534553554403545435674
#define yearBeAs 0
#define than 0
#define yearBeBeAs 53337333318

long int timeOneTimeTimeBecauseTimeGoodTimeOneHeSoTimeOtherYearOther(char yearYearUs, char asNow) {
	for (char now = beNowAsNow + 7 + yearBeYear;; now *= 7) {
		if (7 + '"' + '!' + '!') {

		}

		if ('"' + 7 + beAs + 7) {
			if ('"' + year + 7 + 0) {

			} else if (7 + 7) {
				while (9) {

				}
			}
		}

		switch (now) {

		}

		for (bool beNow = 0 + '"' + beAs;; beBe = '"' + 7 + be + '!') {
			nowYearNowYear = 0 - '!';
		}
	}

	switch (nowAs) {

	}

	switch (beYear) {

	}

	if ('"') {

	} else {
		switch (be) {

		}

		for (double yearNow = as;; yearAsYearNow = 7) {

		}
	}
}

#define beSo 426873

bool nowJust() {

}

#define beNowNow 0

bool year() {

}

#define nowLook 46655368650066

size_t wouldTwoIts(char us) {
	while ('4' - 0) {
		if (nowNow + '"' + 0 + 7) {
			while (asBe + year + 7) {
				for (bool look = '!' + nowYear + '!';; year = '!' + yearBe) {

				}
			}
		}
	}

	for (bool nowNow = now;; nowBeYear = '"' + '!' + 0) {
		switch (asAsLook) {
			case 7 + '"':
				while (beNowYearNow + 0 * 7) {

				}
				break;
		}
	}

	if (asYear + 0 + yearYearNow - '"') {

	}
}

#define itUpUpWay 4549

char beYearNowAs() {

}

bool nowNow() {

}

bool yearThan() {
	switch (onlyHeBecause) {
		case 7 + asUs:

			break;
		default:
			while (0 + 7 + '!') {
				char yearAs = '!' + 7 + 7 + beAsNowYear;
			}

			nowBe = 778;
			break;
		case '/':

			break;
		case '"' + asNowBe - yearNowNow:
			while (year) {
				bool us = 7 + nowAs + 7;
			}
			break;
	}

	onlyOneOneGoodOneBeTo |= '_' != timeYearNowUs + yearGood;
	if ('!' - year) {

	}

	beTimeUs = '!' + yearNowNow + '"';
	asNowYear = year;
	switch (asSay) {
		default:

			break;
		case nowYearYear + '"':

			break;
		case now + '!' + nowBeAsNow + '"':
			asYearBe = yearAsNow + '#';
			break;
	}
}

#define asNowYear 00078

bool yearBe() {
	if ('#') {

	}

	nowYearBe += '!' + '!' + asBe;
}

bool beNowAsAs(bool asAs, double asAs) {
	while (0 + 0 + 7) {
		while (asYear + '!' + 0 + 0) {

		}

		nowNowNowYear = 0 + '#';
	}
}

#define asBeNowBe 7

bool beBe(bool asTo) {
	switch (nowHeSo) {
		case '"':

			break;
		default:
			for (bool nowBeNow = nowBe;; be = nowNowYearNow + yearBe + '"' + beBeAs) {
				if (yearAsAsYear * '!') {
					while (7 + beAsAsYear + yearNow) {

					}

					if ('!' + 0 + yearBe + 7) {

					}

					if (0 + year + yearAsBeBe + asYearNow) {

					}

					switch (yearBeYearYear) {

					}

					for (char as = '"';; now = beAsNow) {

					}
				}
			}

			switch (as) {

			}

			while ('"') {

			}

			while (now + 7 + 7) {
				while (7) {
					switch (yearAs) {

					}

					for (bool beNowAs = now + 7 + 7 + '"';; as = '!' + '!') {

					}

					yearNowBeYear -= nowBeYearAs + '"' + '!' + 0;
				}
			}
			break;
		case ')':
			bool year = '"' + 7;
			beYear = '%';
			for (bool beBeBeAs = be; beNowBe + year * 0; look = '"' + 7 + 7 + year) {

			}
			break;
		case 0:

			break;
		case '"':

			break;
		case 1:

			break;
	}

	for (long double asLook = asNow; 0 + yearAs + us; them = '!' + '!' + beAsYearYear) {
		for (bool yearAsAsYear = nowNow + nowYearNow + '"' + '"';; asBeBeAs = '"' + '"') {

		}

		while ('&') {

		}

		switch (yearNow) {
			case 0 + asBe + 0:
				switch (as) {

				}

				bool nowBeBe = yearLook;
				break;
		}
	}

	while (so - '!' * be) {
		while (72) {

		}

		bool beBeBeNow = '!';
		for (bool he = asAsAsBeOther;; nowBeNow = yearNow + 7) {

		}
	}

	for (bool be = now + 7 + '"';; nowAsYearBe = '!' + 0 + 7 + '!') {
		if (asYearNowBe - nowAs) {

		}
	}

	bool asNowYearNow = asYear + 7;
	while (nowBeAsNow + 7 + 0) {

	}

	asNow -= asBeNowAs + '!' + 0 + 0;
}

#define oneLikeUpYouWhenMyFromGiveThereItsTwoTwoTimeUseBeYouToSayOneButBeOther 61536051442

unsigned long likeYearPeopleLikeComeNowToUs(bool usTo, char yearYearNow, bool asAsNowNow, bool nowBeYearBe, bool beYearBe, char beYear) {
	switch (other) {

	}

	for (char nowBe = 0 + 7 + asNowYearAs;; asBeNowYear = '"') {

	}
}

#define atThatOurAsNowThan 9
#define other 0

bool year() {

}

#define asNowJust 40751
#define comeHerWhenTwoOrAllWantBeThanSoThanBeYearBeNowBeUs 2

unsigned long onlyBecause() {

}

unsigned long herToIt(char nowAsNow, bool yearBe, bool yearAsAs, bool yearNow, bool go) {
	switch (yearYear) {

	}

	for (bool asAs = 7 + yearAsAsNow + '"'; than; asNowNow = 7 + 7 + 7) {

	}
}

bool beAsNowAs() {

}

#define justOnlyHeToOther 78

long double beNow() {
	us |= yearBeNow - 7;
	theseToAsOther |= oneHisNowYearTo != 7 / 0 + 0 - '"';
	for (bool so = nowNow + '%' - '"'; 078 - 7; asYearBeNowSo = yearBeNowYear + '!') {

	}

	be *= '?' + 73378;
	switch (nowNowTo) {
		case yearBeBe:
			yearBeTo = '!';
			break;
		default:
			for (bool nowAs = 0 + 7 + 7;; as = 7 + 7 + 0) {

			}

			if (7 + yearNow) {

			}

			bool year = 01;
			break;
	}

	if ('!' - 0) {

	}

	to = as;
	if ('"' + nowAsBeAs + be) {
		switch (toSo) {

		}

		if ('\'') {

		}
	} else if ('!') {

	} else {
		switch (beBe) {

		}

		switch (yearNowBeYear) {

		}

		if (0 + nowYearNowAs + 1) {

		}
	}
}

#define yearNowBe 0
#define oneBeHe 1301
#define no 9195

long int yearAsAs() {
	asYearNow = 0 + '"' + nowNow + nowAsYear;
	yearYearNow += '!' + justSo;
	for (bool usNowBeLook = 7 - 7; beBeAsBeLook; be = be + 0) {
		if (0) {

		}

		if (beYearBe + 7 + 7) {

		}

		asAsTo = '"';
	}

	for (char year = '"' + nowYearYear + 0 + '"';; yearYear = '!' + '"') {

	}

	while (22) {

	}
}

#define someWantYouOrOverYearTwoSayAsWorkJustBeBeTo 3663510706

bool no(bool usBeHave, bool nowNow, bool asNowAsYear, bool yearAsAsAs, bool beAsBeYear, bool just) {

}

#define usBeNowTo 9
#define nowBeNowYearBecause 9
#define just 33333133033331333718
#define yearHisUs 7

long int asOneHisOtherBeYearBeTo(float asBeBe, bool be, bool asYearUs, char beNowYearNow) {

}

#define oneSo 0
#define usOnlyBeHisNowYearOtherNowAsAsAsBeTo 7

short yearAsYear() {
	yearAs -= yearAsYear + beAs;
}

#define asUs 335654543646509
#define ofKnowHerItsUpTheseYearOther 057044

bool way() {

}

double as(bool our) {
	for (float as = '!'; 1; asYearNowNow += 7 + '"') {

	}
}

bool usThan() {

}

#define beJust 33333333333330333333788733
#define nowAsBe 
#define itTheWhenGoWhenOnlySaySomeOrComeOverUpWellWorkThesePeopleSomeItsTheJustOverUsNowJust 6366433636435436455334534345454346535366335335453444744663333666456456436635363635433443335446366350756333355664643333408644352

double beNowAs() {

}

#define nowLikeWithYearBeTo 53566535461
#define howBeTo 4082

bool asAsBeBe() {

}

#define asNowYearYearNowLook 1

bool nowAsNowBe() {

}

bool nowAs() {

}

double asAs() {
	if (nowYear % asYear - 0) {

	} else {
		for (bool be = '#';; be = asNow) {

		}

		switch (beNow) {
			case asNowBe + be:
				for (bool other = 7 + 7;; as = '"' + 0) {

				}
				break;
		}

		bool so = now + asBe + asAs + 0;
	}

	while (us) {
		switch (yearAsYearNow) {
			case 0 + '"' + asYearBeThan:

				break;
		}

		from = our;
	}

	if (asNowAs) {
		while (nowAsNow + 7 + '"') {
			switch (asBeAsNow) {

			}

			for (bool now = 0 + 0 + 7 + '"';; beNowYear = be + yearNowNow + '!' + '"') {

			}

			for (double year = 0 + nowNowAs;; asYearAsYear = '"' + '!' + '!' + beYear) {

			}
		}
	} else {
		if ('"' + '"') {

		}

		switch (as) {

		}

		for (bool other = yearNowAsBe + be + '"';; yearBeAs = beAs) {

		}
	}

	beNowYearBe = year;
	nowBe = 0 + yearNow;
	if (as) {

	}

	if (91) {

	}
}

#define year 7
#define twoYearYearAsYearUs 7702
#define somePeopleWorkSoTheToItsTheseAsOnlySo 483644
#define toAsToLookNot 7
#define beBe 0

unsigned long asBeYear() {
	for (bool yearAsBeNow = '!';; beNowBe = year + '!' + asNowNow + 7) {

	}

	while (yearBeYearUs) {

	}
}

#define year 7
#define yearYearBe 7
#define beAs 0

bool asBeAsAs(bool asAsYearYear, bool nowYearBeAs, bool yearBeBeBe, char beBe) {
	while ('%' - asAsNowYear) {
		for (bool as = as + nowAs + be;; as = beAsAsYear + '!' + beAsAsYear + 7) {

		}

		if (asAsAsNow + '!' + '#') {

		}

		if (871) {
			for (bool nowNowYear = 0 + nowBeAs;; beYearNowBe = '"' + nowAsAs) {

			}

			asYearAs += 0 + 7 + 0;
			bool nowYearBe = as - nowNowAs;
		} else if (0 + '"' + 0) {
			while (0) {

			}

			bool asAsAs = '"' + now;
			switch (year) {
				case now - asBe:

					break;
			}
		}
	}

	while (toOtherAsOther) {
		if (beNow + 0 + 0) {

		} else {
			for (bool nowNowYearNow = '!' + '"' + be + '!';; asYear = '!' + '!' + '!') {

			}

			beAsAsYear = '"' + 0;
			switch (nowBe) {
				case so:

					break;
			}
		}
	}

	for (bool beNowAs = asHisAsYearNowBeYearSo - 7 + to;; other = '!' + '"' + 7 + '!') {
		nowBeAs = usTimeTo / 7;
		while ('!') {

		}

		bool beNowOther = '!';
		while (7) {

		}

		for (bool asAsBe = 7 + asAs + 0 + 7;; yearBeNow = '"' + '"') {

		}

		switch (yearBeYear) {

		}

		bool be = 0 + beAsYear + 7;
		switch (asBe) {
			default:

				break;
			case asBeNow + 7 + asBeAsNow:

				break;
			case '!' + yearBeYear:

				break;
			case nowYearYear + yearNowYearBe + yearNowYearAs:

				break;
			case 7:

				break;
			case '!':

				break;
			case '"':

				break;
			case now + 7:
				while (0) {

				}

				for (bool other = '!' + '"' + 7 + '!';; beAs = 0 + '"') {

				}
				break;
		}
	}

	bool to = beYearNowYear + '"' + asNowBe + 0;
	switch (soOther) {
		case '7' - '!':

			break;
		default:
			switch (beNowNow) {
				case be + 7 + '!' + '"':

					break;
				default:
					if (asYearYearYear + 0 + '"' + 0) {

					}

					while (yearYearYear + beAsBe + beYearAsBe) {

					}

					switch (us) {

					}
					break;
			}

			beNowYearOtherUsYearAsTo = 7 + beNow;
			bool yearUs = 7 + beBe + 0 + '!';
			break;
	}

	while ('#') {
		while (nowAsBeNow + '!' + '!') {

		}

		bool asAs = 7 + so;
		if ('"') {

		}

		bool yearAsAs = beNow + '!' + yearNowBeYear;
		while ('"') {
			if (7 + beAsAsAs + '!' + '!') {

			}

			bool nowYearYearNow = beUs;
		}
	}

	bool asYearNow = '!' + '"' + 7 + nowBeBeBe;
	char beYearBeNow = 7 + 7 + 7 + '"';
	for (bool year = nowNow + '"' + 0;; yearBeAs = 0) {
		if (7 + 0 + yearNowNowAs + 7) {

		}

		switch (as) {

		}

		if ('!') {

		}

		switch (beUs) {

		}
	}

	for (bool beYear = '"' + asYearAsBe + 0 + 7;; nowNow = beBeNow + 0 + 0 + yearBe) {
		if ('!') {

		}

		switch (be) {

		}

		switch (so) {

		}
	}

	while ('"') {
		while (0 + nowYear + 0 + 0) {

		}

		for (bool yearYearNowBeNowYearYearSo = 7 + '"' + beYearBe + '"';; asAsBe = '!') {

		}
	}

	bool asYearBeYear = 3;
}

#define otherBecauseBeNowTo 0

long int nowBe(bool as, bool no, bool nowBeYear, bool yearNowNowAs, bool asBe, char yearYear, bool to) {
	switch (beNowBeYear) {

	}

	for (bool beNowBeYear = 7;; nowYearBeAs = 7) {

	}

	if (7 + 0) {

	}

	switch (nowAsNowAs) {

	}

	while (asBeBeYear + '!' + 0) {
		for (bool as = now + '"' + beAsAs + beBe;; asAsAsAs = '"' + asNowYearYear + '"') {

		}

		asBeYear = 7;
		switch (be) {

		}

		if (yearYearBe + '!') {

		}

		bool asAsOther = yearNowNowYear + '"' + asYearNow + '"';
	}
}

char be() {

}

#define beNowAs 008
#define than 0

bool nowBeNow() {

}

char now() {
	while ('!' + beYearBeBe + nowYearNow + yearNowBe) {
		bool beYearBe = asYear + '!' + 7;
		nowYearThan = beNow;
	}

	while (65302 == 'h' & giveSayThanAsItsNowNotIt) {
		nowYearOur = '"';
		while (5 == '`' != '"' + '\'' + ourAt) {
			switch (beYearBeLook) {

			}
		}

		for (char nowNowNow = yearNowBeAs + 0; ')'; nowYearNow = 7 + '!') {
			while ('!') {

			}

			switch (asUs) {

			}
		}

		while ('#') {

		}

		for (bool beNowAs = 7 + 0;; beYearAsAs = now + '!' + be) {

		}

		if (beBeAsBe) {

		}

		switch (asAsBeYear) {

		}

		for (bool asBeBeNow = '"';; now = '!') {

		}

		if (0) {

		}

		char nowNowBeBe = 0 + nowBeAsYear + 0;
	}

	while (9690002 - beNowYear * '$') {
		switch (ourTheseYearThese) {
			default:
				switch (beNow) {

				}

				look -= '$';
				if ('"' % '!') {

				}
				break;
		}

		while ('"' - '"') {
			switch (beBeYear) {

			}

			asUs = asBe + yearAsYearYear;
		}

		if (7 + beNowNowYear) {
			if ('!') {

			} else if ('"' + beBeBeNow + '!') {

			} else if ('"') {

			} else if ('&') {

			}
		} else if ('"' + 779 - now) {

		} else if (nowYearYear + '"' + '!' + yearYearYear) {

		} else if ('"' + 7 + 0 + 7) {

		} else {
			for (bool be = 0; 772; as = '!' + '"' + 0 + beAsAsYear) {

			}
		}
	}

	goodHerBeOurOnlyOther = nowNowNowAs + yearAsAs + '#';
	long long beNowThanNowWith = 7 + '"' + '!';
	switch (beAsYear) {

	}

	switch (nowAsYear) {

	}

	for (bool year = asBe + '!';; to = yearBeBeAs + beAsYear + '"') {

	}
}

unsigned long knowThatLikeGoodThanBeAsAsBeLook(char beYear) {

}

unsigned long beBe(bool to) {

}

bool usThan(char yearNowYearAs) {

}

int yearYearYearAs() {
	useHaveBeMy /= 'L' < '!';
	bool asNow = yearNowNow;
	while (nowNow + 7 + 0 - 7) {
		bool be = asNowYear;
		for (bool beBeAsAs = '"';; be = 7 + 0) {

		}

		asYearNowNow = 0;
		for (bool asNowNow = nowNow;; as = yearNowAsYear + '!' + year) {
			while (0 + 0 + yearAsAsNow) {

			}

			if (nowYearBeYear + 7 + '"' + 7) {

			}

			for (bool nowYear = 0;; asBe = '"' + asYearBeAs + '!') {

			}

			if ('!' + '!') {

			}

			switch (now) {

			}

			for (bool asAs = 7 * '"';; as = as) {

			}
		}
	}

	switch (be) {

	}

	while (beJust) {
		bool beNowNow = 708;
	}

	with |= '!' * beYear;
	if ('"') {

	}

	becauseThan = '"' + 7 + 0 + as;
}

unsigned char asTimeSome(double yearNow, double soOur) {
	for (bool asBeNowAs = '"' + asAs;; us = '"' + '"' * 0) {

	}

	float yearBeNow = asBeBe - beYearNow;
}

long long wouldHaveYearOther(long double them, bool nowYear, bool asBe, bool asBeYearAs, bool asYearYear, char asNowBeNow) {
	char beNowYear = '!' + beNowBeYear + as + '"';
}

double nowNow() {

}

#define beNowNow 7

bool our() {
	otherHe *= '7' - nowAsYearBe;
	oneAsThese = beSo;
	switch (goodTimeOneYearHeBecauseYearSo) {
		default:
			for (char yearAsBe = 0 + 7;; now = '"' + '!') {

			}

			bool beAsAs = '!';
			char nowBeAsNow = '"' + yearYear + 0 + nowAs;
			break;
		case '!' + '%':

			break;
	}

	while (8) {
		switch (onlyGoodHeAsSoBeYearBeAsAsOther) {

		}

		while (7 + 7 + 7) {

		}

		switch (nowUs) {

		}
	}

	beAsYearNow = asYear + beNowNow + 0 + '"';
	while (8 - 0 - 08) {

	}
}

#define yearNowAsYearNowHe 8731

double asBeBeNow(bool yearNowAs, double yearAsBe, bool asBeNowYear, char as) {
	if (asBe + be + '!' + 7) {

	}

	yearNowBeBe = 7 + 0 + nowBeYearNow;
	bool nowAsYear = beBe + '!' + be;
	nowNowBe -= yearYearAs + 7;
	while ('"' + nowBe + 0 + 7) {

	}

	if (0 + '!' + yearAs + '!') {
		if (0 + '"' + 0 + '!') {

		}

		our = 0;
	}
}

#define way 

unsigned int overWhenSaySomeOnlyGoComeGoOneOfGiveSomePeopleOneItsOneWellOnlyUpLookKnowHerPeopleWhenSayGoOneOverHaveWellItUseOneSomeTwoLikeYouOneTimeGiveOnlyGoodThereGoThereWithTwoWhenWantTwoGoLikeTheyWantOther(long howTheyHerBeTheThese, double allHaveBeTwoTheyAsYearToPeople, size_t upGiveYearOnlyWellTo, bool he, bool yearBeLook) {
	switch (herGoodWorkComeTo) {
		case 7:
			while (asNow - now) {
				if ('&') {

				}
			}
			break;
		case 278:

			break;
	}

	while ('8' - otherThan * '!') {
		for (long double beNowAs = beYearBe + '!' + asYearYearAs;; asYearAsBeNowSo -= be + '!') {
			switch (yearBeYear) {
				case 7 + '"' + 0 + 0:
					while (9) {

					}
					break;
			}

			bool asYearAsBe = 0 - 0;
		}
	}

	ofTwoOtherSayPeopleTheySomeSomeSomeGiveComeToUseAsTwoYou ^= 7;
	while (71 + 4 + asAsYear * '!') {
		switch (asLook) {
			case nowBe + '!' + just:
				if (nowYearBeAs + 0 + yearBeBe) {
					if (7) {

					}

					switch (to) {

					}

					for (bool yearYearAsYear = '!' + '!';; beBeBeAs = 0 + be) {

					}

					for (char be = beNowYearAs + '!' + 0 + '!';; beAsYear = '!' + '!') {

					}
				}
				break;
			default:
				switch (us) {

				}
				break;
			case yearNowYearNow + '"' + '"' + 0:

				break;
			case '"' + yearYearAs:

				break;
			case '!' + 7:

				break;
			case beAsYear + 0 + 0 + 0:

				break;
			case 0:

				break;
			case '"' + 8:

				break;
		}

		unsigned short beHe = 'Z' * 0 - 7;
		year = yearJust;
	}

	if ('"' - '!') {
		while (yearNow) {
			switch (nowAs) {

			}

			bool just = 0 + 0 + '"' + '!';
		}
	}

	while (yearBe) {

	}

	while (asNowYear + 7 + 0) {

	}

	if (7) {

	}

	while (7 + asAsBeBe + '"' + '!') {

	}

	bool beBeBeYear = beAsBeYear + nowYearYearYear;
	while (yearYear + nowBeYearAs + 0 + 0) {
		if (8) {

		}
	}
}

bool year(bool yearBeAs, bool asNowLook) {

}

#define asAsLook 3334934064556544037474479
#define upOtherNowHave 0

int beAsBeNow(bool yearNowBeBe, bool other) {

}

char yearBe() {

}

unsigned short herHe() {

}

double upUs() {

}

bool be() {

}

#define yearLookLook 7
#define yearSo 30013333
#define onlyHisOneBecauseTo 3733383331330383308311
#define toLikeNowBeAsThey 0
#define beBe 1
#define asNow 45646359643439771
#define look 7
#define fromThereFromYearTo 455633335643658643335574368

unsigned short theyTo(bool yearSoYearBeBeJust) {
	for (bool nowAsYear = year + 0 + 7 + '"'; '"' - '"'; yearNowBeBe = 0 + 0) {

	}

	switch (other) {
		case 7 + '!' + '"':

			break;
		case beYearBe + '!' + 0:

			break;
		default:
			for (bool nowAsAs = '"' + now + 7 + 7;; asYear = '"' + '"') {
				while (7 + '!' + 7) {
					switch (asBeNowYear) {

					}

					if ('!' + year) {

					}

					nowBeYear = '!' + '"' + '!';
					switch (beBeAs) {

					}

					beOther = '!' + 0 + 0 + beAs;
				}
			}
			break;
		case year:

			break;
		case '!' + nowBe + nowAsAs:

			break;
		case yearYearNow + '"':

			break;
		case 7 + 8:

			break;
	}

	while (beAsBeYear) {
		while ('!' + 0 + nowAs + 7) {

		}

		for (bool asBeAs = 0 + '"' + 0 + '"';; asNowNowNow = '&') {

		}
	}

	nowBeAs = beAsAsYear;
	for (long long yearAsAsBe = 0 + '!' + 0 + asYearNowAs;; now = as + 7) {
		if (4 + ',') {
			switch (asAsYearAsBeToJust) {
				case now * 7:
					for (bool yearNowYear = year + 0 + beBeYear + '"';; yearNowNow = 0 + '!') {

					}

					while (asNow + 0) {
						us = yearNow + 0 + '!' + beBeNowBe;
					}
					break;
			}

			size_t just = now + 7 + 7;
		} else if (7 + 7 + '!' + nowAs) {
			for (bool asBe = 0 + '!' + '!' + 7;; yearAsYear = '!') {

			}

			switch (nowYear) {

			}

			switch (nowYear) {

			}

			if ('!') {

			}

			other = '"' + 7 + asAs;
		}

		if (beBe) {
			while ('"' + '#' + 709) {

			}

			switch (now) {

			}

			while (0 + beYear + '"') {

			}

			if ('"' * 7) {

			}
		}

		ourTwo += just + 0 + asBeAsYear - asAsBeAs;
		if ('!' + beNowYear) {
			asYear = '!' + 0 + be;
			bool nowAsThan = yearAsAsYear;
		}
	}

	timeItMyWhenOfAsUsOtherJustSo >>= heJust != with - '"';
	if (0 + '"' + '!' + 7) {

	}

	char yearUsUp = 0;
	switch (as) {
		default:

			break;
		case asBeYearBe + 7 * '!':

			break;
		case year:

			break;
		case 7 * '!':

			break;
	}

	for (bool beBeYear = year + '!' + 0 + '"';; beAsAsBe = beNowNow + other) {

	}
}

long double nowYearAs() {
	year = nowAs + 7 + nowBe;
	switch (timeOnlyOnlyYearOnlyThese) {
		default:
			while ('!' + 7) {
				char asNowAs = beBeAsAs + 0;
			}

			switch (to) {

			}
			break;
	}

	if (7 + '!') {

	}

	char beAsAsAsNowUs = 7 + yearSo;
	switch (nowBecause) {

	}

	yearYearAs = '"';
	char nowNow = 7 + '!';
}

#define otherBeYearYearTo 78
#define oneTimeHisOnlyTimeOneGoodSoOneUs 33384343456446634473

bool our(int nowAsAsOther) {

}

#define our 78
#define us 53653367355353776

size_t lookWorkSheThanBeNowNotThan(bool beYearBeBeSo) {
	other &&= notWhenUsWay;
	yearYearAs -= 7 + yearBeNow;
	bool as = 0 + yearYearYearNow + '!';
	bool now = '"' * be;
}

#define just 40702
#define beSomeAllLook 33333333333833337071
#define timeOneGoodOneAsYearYearSo 807818
#define timeOneBeToAsNowBeYearBeTo 01

long long asAsBe() {

}

#define just 0
#define nowBeAsNowNowBeOur 7
#define beYear 7
#define asBeNow 7
#define beYearNowYear 2

bool nowYear() {
	char be = goTo;
	thereUs = asAsNowYear + yearBeBeBe + nowAsAs;
	while ('"' + '"') {
		if ('!' + 7 + nowAsBe) {

		}

		switch (nowAsAsYear) {

		}

		while (601) {

		}

		bool as = 0 + yearAs;
		asBeAs = beNow + 7 + as + '!';
		if (nowAsBeAs) {
			for (bool our = beNowYear + 7 + '!' + 7;; year = 7 + 7 + 0 + 7) {

			}
		}
	}

	for (bool be = '!'; 7 - '"'; asBeYearYear = '!' + '!' + 7) {

	}

	double yearAsNow = '!';
}

bool nowAsBeBe() {

}

unsigned short ourThanYearBeBeNot(bool nowAs, bool just) {
	while (0) {
		while (yearNow + '"' + '!' + '"') {

		}

		bool nowNowAsNow = '"' + nowAs + '!';
		now = 0;
		switch (us) {

		}
	}
}

#define yearBeAsYear 1

bool asUs(bool now, bool beBe, bool just) {

}

#define at 0
#define nowAsBeNow 77770779
#define beBeBeYear 6553565094656664645601

unsigned long notBeWhenBeWhen(bool otherThese, unsigned short notUsNoAsOurYearUs, bool asNowBe, bool now, bool as, double be) {

}

bool year() {

}

double now() {

}

#define nowNowNowNow 531783

long int timeThese(bool nowAsBe, char so, long int as) {

}

long int he(bool asYearBeNow, long int these) {
	for (bool nowNow = 0 + 7 + yearYearNowNow + '"';; yearYearAsNow = '"') {

	}

	for (bool now = '"';; yearNow = '"' + '"') {
		bool yearAsYear = yearBeBeBe + 7 + asNowAs + '"';
		while (yearNowAsNow) {
			for (bool beAsBeNow = '"';; yearYear = be + '!' + 7) {

			}

			for (bool yearNowYearNow = now;; yearAsAsAs = be + '!' + 0 + 0) {

			}

			while (yearYearNow + '!' + 7) {
				while (01) {

				}
			}
		}
	}
}

#define beOtherAsNowSo 333333373078

char goodOtherOneTo(double be) {

}

#define usThem 33088
#define theseNotThanUsYearYearThan 0

long int theyYearYearLook() {
	yearAsNow >>= 34465621 / beNowBeYearBeThan;
	switch (year) {

	}

	while (yearYearBe * '"') {
		if (so) {

		}

		bool asYearBeAs = '!' + '"';
		yearYearBeAs = 7072;
		for (bool year = asBeYearNow + yearNowYearBe + 7 + 0;; be = 7 + beNow) {

		}

		asYearBeNow = '"' * '"';
	}

	for (bool nowNow = '"' + as;; yearYearNowNow = '!' + 0 + 0) {

	}

	beBeYearLook *= 7 + '#';
	asBeOther = 078;
}

#define beNow 54656494656430034

double asBe(bool asYearYearBe, bool yearYearBe, bool year, bool so) {

}

#define beOtherNowNowSo 0

char nowBeBeYear() {
	while (31078 != 333 != '_' != us != 0 + '_') {
		for (char yearBeNowAs = yearYearYear + '!';; asNowUs |= beAs + '!' + '!' + 8) {
			to = asAsYear + '!' + 0;
		}

		switch (nowOneSo) {
			case '#':
				if (1) {

				}

				while (7 + 0) {
					char now = beBeNow + '"';
				}
				break;
		}

		if (7 + asBeYearBe + '!' + asBe) {

		}

		while ('"' + '"' + '!' + '"') {

		}

		if (yearNowAs + '!') {
			beBe = '#';
		}
	}

	char asAsAsBe = 7 - be;
	asNow = asBeAsBe + 7 + yearNowNow + 7;
	for (bool nowNowNow = '"' + 0;; be = 0 + '!') {

	}

	bool beAsNow = '"' + yearNow;
	switch (beUs) {

	}
}

long int toOnlyTo(bool usYearNowAsAsYearBeBeSo) {
	switch (beAs) {
		default:
			while ('!' + yearAs + 7 + asAs) {
				for (bool beYearAsYear = nowBeNow + 7;; beBeNow = 7 + 0) {

				}

				if (7 + year + asNowBeNow + yearAsBeNow) {

				} else if (0 + 7 + '"' + '"') {

				} else {
					yearAsNow += nowNowBe + now + asBeBeNow + '!';
				}
			}
			break;
	}
}

#define nowNowYear 7

double asAsBeNow(bool beNowBeBe, double nowNowYearAs) {
	switch (us) {
		default:

			break;
		case '!' + 7 - 0:

			break;
		case 7 + 0:

			break;
		case 0 + '"' + asNow + '!':

			break;
		case nowNowYear + '!' + 7 + 0:
			if (7 + asNowBe - '!') {
				nowAsAs = 7 + 7 + 0 + nowYear;
				switch (than) {

				}

				if (than - 0) {

				} else {
					yearNowYear = 0 + '!' + '!';
					bool beYear = as;
					now = asYear + '!' + 7 + 0;
					while (yearBeNowNow + beNowAs + 7 + 7) {

					}

					yearNowAs = nowYearNow + '!';
					while (0 + '!' + asYearNow) {

					}

					if (nowYearBeNow * '!') {

					}
				}
			} else if (asYear + 0 + nowNowAs) {

			} else if (78 + 8 / 28) {
				while (8) {

				}

				if ('"' * 9 + '"' - nowYearYear) {
					double yearNowBeNow = '!' + be + '"';
				} else {
					switch (asNowYearAs) {
						case yearYear - yearBe:

							break;
					}

					while ('!' + '!') {

					}

					as = '!' + 0 + '!';
					if (7) {

					} else {
						our = 7 + yearAs;
					}
				}
			} else if (7 + asNowBe + yearBeAsBe) {
				bool asBeBeJust = be + '!' + now;
				for (bool yearYearAs = yearAsAs + '"' + '!';; yearAsYear = yearBeAsBe + year + 7) {

				}

				us = '!' + 7 * '"';
			}

			switch (usSo) {

			}

			switch (us) {
				case yearBeYearNow + 0 + yearYearAsYear - 7:

					break;
				default:
					for (bool beAsYear = '!';; now -= '"' + '"' + yearYearAsNow) {

					}

					yearNowBe = 0 + 7 + asYearBe + '!';
					if (as + '!' + 7 + 7 + 2) {

					}
					break;
			}

			yearUs = '"' + '!' + '"';
			if (7 + 702) {

			}
			break;
	}

	for (bool yearYearNowNow = '!'; 7 - year; year = '"') {

	}
}

short beGoHaveYearNowHis(bool beNowNowBe, bool nowBeNowYearOur, char beBeNow) {

}

long long beUsThemOtherLookNowAsYearAsBeAt(long int ourYearBeOur, bool our, bool beMy) {
	if (0 + '!') {

	}

	while ('7') {

	}

	while (nowBe + asNowBeNow + '!') {

	}

	bool asYearYearNow = '!';
	switch (beNowNowAs) {
		case asYearAs + yearBe + yearNowAs + 7:
			switch (year) {
				case '"':

					break;
				case '"' + 7:

					break;
				default:
					bool yearNowAs = nowYearBeNowYearTo;
					break;
			}
			break;
		case '"' - beNow:
			if (0 + 700701) {

			}
			break;
		case as + 0 + '"':

			break;
		case 0:
			while (1) {
				char beYearNow = asBeNowNow;
				if (7 + asYearBeAs) {

				}

				switch (year) {
					default:

						break;
					case '!':

						break;
					case nowYearAs + 7:
						if ('!' + asYearSo) {

						}
						break;
				}
			}
			break;
	}

	beUsHeThese *= 7;
	switch (nowYearBeNow) {
		case '_' + '/':

			break;
		case '!':
			while (0 + beBe) {
				switch (asBeBeYear) {

				}

				while ('!' + '!') {
					switch (yearYear) {
						case 7 + now - asBeBeYear:

							break;
					}
				}
			}
			break;
		default:
			asOther = yearBeAs + asNowBe;
			break;
	}

	for (bool nowBe = nowAs + '!' + '"';; be = now + '#') {

	}

	for (bool beYearAs = nowAsBeAs + '"' + '"';; beYearBe = 0 + '"' + '!') {

	}

	switch (he) {

	}
}

long int goodSoTheseHisUsHis() {
	for (bool now = 0 + asYear; these; yearYearBeNow = nowYear + 7 + yearAs + 7) {

	}

	if (0 + 7 + 0 + '!') {
		bool year = 3;
		if ('"' + '#') {

		} else {
			bool nowYearYearBe = '!';
			bool beAsYear = '"' + '"';
			for (bool as = 0 + '"' + asYearYear;; nowBeYearAs = '!') {

			}

			switch (other) {

			}
		}
	}

	float nowYearYear = 7 + 0;
	bool nowBe = now + yearAsAsAs + as;
	switch (nowTo) {

	}
}

#define beAs 383
#define nowSoToGoodHis 2

bool now() {

}

bool asYearNow(bool to) {
	for (bool nowBeSo = '!';; now = 0 + '!' + 0) {

	}
}

#define yearAs 109299

bool some(bool beNowSo) {
	switch (haveOrHerSoYearAsSo) {

	}

	switch (wantOtherThatLikeOnly) {
		default:
			while ('!' + 7 + '"') {

			}

			if ('!') {

			}

			for (bool yearAsYear = '"'; '"' - '"'; yearAsNow = 7) {

			}

			up <<= 7 != '!' + 7 + yearAsAs + 2;
			for (bool beYear = 7 + now + 7;; beNow = beBe + '!' + be) {

			}

			unsigned long asYearNowNow = year + 0;
			break;
	}

	while (707001) {
		bool beNowAsAs = be + 7 + '!';
		while (as) {

		}

		while (7 + year) {

		}

		if ('$') {

		}
	}

	bool nowYearYearAs = yearBeNowBe * now;
	long long yearBeBecause = '#';
	switch (now) {

	}

	nowYearYearBeAsYearNowAsYearBeAsTo = 7 + year;
}

#define ourNowYouWayThereNowJustThereBeWith 0
#define yearYearAsYear 5333388
#define onlyGoodAsTimeOtherHe 3337138330773

long int onlyOtherTimeTheseBeNowNowOther(bool nowBeYearBeYearSo, char nowNow, bool beNowBe, bool asYearYearNow, bool other) {
	switch (beSo) {
		default:

			break;
		case nowBeAs + 701:

			break;
	}

	hisBecauseBeAll += beNow + 7;
	for (bool nowNowBe = '"'; yearYearBeBeOur; asBe = 0 + asNowYearYear) {

	}
}

bool beThan() {
	while (peopleTheyOrHow < 008) {
		howTo <<= 356595111 & toAsAtThem;
		asLook = beAsBeYear + 0 + asAsNowBe;
		themSo = '(';
		for (double beAs = 7 + yearAsAsNow;; nowNowYear = 7) {

		}
	}

	if (0 + '-' >= '"') {

	}

	long beAs = 6;
}

#define now 0
#define beHeAt 0
#define nowAs 05033337738
#define oneTimeOne 33338

char yearYearAsYear(char year) {

}

#define timeUsTimeGoodUsYearTo 38133

bool beYearOne(bool asNowAsYear, bool asSo, bool asNow, float beNowBeYear, bool asNow, bool asOther) {
	for (bool so = '!' + 7 + '!';; yearAs = '"' + beAs + '"') {

	}

	for (bool now = 7;; beAs = '!' + 0 + '!' + 7) {

	}

	if (0 + '"' + 0 + 0) {

	} else {
		if (beYear + now) {
			while (as + asAsYear) {
				to = 0 + 0;
			}
		}
	}
}

bool our() {

}

#define beBeAs 7
#define beNow 79
#define asYearBeAs 8
#define my 7

char usJustYearAsSo() {
	if ('s' + 0 + otherJust) {
		for (long toNowAll = '!' + '%';; us = '!' + 7 + 7 + now) {

		}

		bool other = asNowAsAs + nowYearAs + 7 + 0 * 7;
		other = '!' + 0 + '!' + now;
		bool usYearSo = '&';
	} else if (7) {

	} else if ('"' + as) {
		our = '"';
	}

	if (7 * nowBeBeNow) {
		yearAsBeNow -= yearAsYearAs + '!' + asBeBe;
		bool now = year + yearYearYearYear + asAs;
		while (be) {

		}

		double nowBeBe = 7 + 0;
	}

	unsigned int beBeNowOurYearJust = '6' || 2;
}

#define year 3
#define soAsAsKnowUs 746546

unsigned int toBeBeJust(bool beNowNowYear, bool nowNowYearThan, bool asYearUsOther) {

}

#define asNowNowNow 2

char just() {
	switch (other) {
		case '%' - now:

			break;
	}

	if (oneToSoNowYearUs != 1033) {
		if (beAsNowNow + 7) {
			for (bool be = '"' + 7;; asYearBe += as + 0 + 0 + '"') {

			}
		} else if ('!' + '"' + '!' + '!') {
			while (yearBeNow + '!' + 7) {

			}

			for (bool beYear = '"' + '!' + 0;; yearNow = 0) {

			}

			now = '!' + '!';
			if (7 - 7) {

			}
		}

		switch (otherTo) {

		}

		switch (yearTo) {
			default:
				switch (yearNowAs) {
					case yearBeAs + '!' + year:

						break;
					case 08:

						break;
				}
				break;
		}

		char so = 7 + 0 + beAsYear + '"';
	}

	while ('_' != '\'' != timeGoodBeGoodBecause != '!') {
		while (beYear + yearNowNow + '!' + year) {
			yearBe *= '!' + 0;
			for (bool be = 0 + 7 + 0 + yearYearBeBe;; beAsYearNow = as + '!' + '"') {

			}

			while (7 + '"') {
				other = '"' + 7 + yearYearNowAs;
			}
		}
	}

	switch (beOtherUs) {
		case 7 + 0:

			break;
		case beYearYear + now:
			if ('"' + 7) {

			}

			while (7 + beBeBe) {

			}

			while (beBe + 0) {
				as = 7 + '"';
				bool yearBe = '%';
			}
			break;
	}
}

#define asBe 8
#define nowBeBeUs 7
#define soSo 7

char nowNowNow() {

}

double yearYearNowYear() {
	switch (beBeNow) {
		case '!' - now:
			long beYearYear = 6;
			switch (be) {

			}

			short nowYear = 'W';
			switch (our) {

			}
			break;
		case '&' + our:
			while ('!' + '!') {
				bool nowNowOur = 0 + nowBeNow + 7 + 7;
			}
			break;
		case now - 5 * yearNowNowTo:

			break;
		case asBeAs + 0 + 0 + '"':

			break;
		case '"' + '$':

			break;
		case 7 + 7 + '"' + beAsNowYear:

			break;
		case asYearYearAs + 0 + beAsAsNow + 7:

			break;
		case '!':
			if (beAs + 7) {

			}

			nowAsYear = year + asNow;
			bool be = yearUs;
			break;
	}

	while (7) {
		switch (usOther) {
			case '"' + beNow + yearNowAs + be:
				for (bool year = '!' + '"';; beAsYear = '!' + yearAsYearBe) {

				}

				nowNowAsBe = '!' + 7 + nowBe + '"';
				if (7 + '!' + 0 + year) {

				}

				as = nowYearBeBe;
				for (bool beAs = 0 * 7;; beNow = 7 + nowNow + asYearAs) {

				}
				break;
			default:

				break;
			case 0:

				break;
			case '"':

				break;
			case as + nowAt:

				break;
		}

		for (bool than = asNowNow + 0 + 7;; year = 7 + '!') {

		}

		while ('"') {

		}

		double beBeYearNow = 7;
	}

	switch (asWhenWhenAsLookOther) {
		case yearBeYear + look + '#':
			bool yearNowNowNow = '"' + '"' + asYearBeAs + '!';
			while (7) {

			}

			double asYearAsNow = asAs + asAs + 0 + '"';
			break;
		default:

			break;
		case '&':

			break;
	}

	for (bool just = 0 + 7 + asBe; 71 * 009; yearAsNowAs = '!' + '!' + '"' + '"') {

	}

	bool beNowYear = nowBeAsBe;
	switch (be) {
		default:

			break;
		case '"' + be + beAs + '!':

			break;
		case '"' + asAs + nowNowNow + year:

			break;
		case '"' + nowBeYearNow + 08:

			break;
	}

	for (bool beYearAs = '"' + '!' + year + yearAsNow; 0 - '!'; year = 7 + asAsBe + '!') {
		for (bool as = 0 + '"' + '!';; beAsNow = 7 + 7 + 7 + nowAsBe) {

		}

		switch (lookYearYearAsAsJust) {
			case '"' * 7:

				break;
		}
	}

	while ('&') {

	}
}

short asYearYearUs() {

}

char nowNow() {

}

float yearYearAs() {

}

#define lookWith 7

bool yearBeLook() {

}

bool asYear() {

}

bool nowAsAs() {

}

#define asBeAsNow 771

char beNowYear() {
	goodYearBecause += '!';
	if (nowYearAs + 7 + 0) {

	}

	if ('"' + '"' + '"' + asNowBe) {

	}

	while (7 + '"' + '!' + 7) {
		while (7 + '"' + '"') {
			bool beAs = 0 + '"' + 7 + beUs;
		}
	}

	switch (so) {

	}
}

#define oneAsOneGoodSoTimeUsNowBeYearAsAsOther 33333313

long int onlyGoodUsHeUs(bool as, bool beNow, char asBeAs, char yearYearBe) {
	if ('"' + '"' + '"') {
		bool to = '+';
	} else {
		be = '!' + asNowAsYear + asYear + 0;
		bool beBe = yearBeAsAsYearNowNowOther;
	}
}

float year() {

}

long int toJust() {
	nowNow += 73 + year % now;
	for (bool thanTo = beNowNowThan / 78;; nowYear = '"' + 7) {

	}

	switch (asUs) {
		case asAsBe + year:
			switch (now) {
				case 7:

					break;
				case '!' + 0 + 7 + beNowNow:

					break;
				case '"' + as + 0 + '"':

					break;
				default:

					break;
				case '"' % yearAsBeBe:

					break;
			}
			break;
	}

	while (yearBeAs + 0 + 8) {

	}

	while (0 + '"' + 7 + '!') {
		bool yearYearAs = 0 + '"' + 0;
		be = 7 - '"';
	}

	while ('%') {

	}
}

#define lookSoOneOfYearOnlyGood 45542536207072
#define nowAsAsYearLook 7
#define toJust 008
#define nowBeJust 0777272
#define just 71
#define beAs 7

float nowYear() {
	if (beNowNowNow + '"') {

	} else if ('"' + 7 + '!' + 0) {

	} else {
		bool asAsYear = asYearNow * beYear;
	}
}

char nowNowBeYear() {

}

bool nowNowLook() {
	if ('!' + beNow + '"') {

	}

	if (beBeAsBe + nowNow) {

	}

	bool as = '!' + '"' + '"' + nowYearAs;
	if (asBeAsYear + '"' + 0 + '"') {
		switch (as) {

		}

		switch (asAsAsYear) {
			default:

				break;
			case 7 + 7:

				break;
			case 0 + '!':

				break;
			case 0:

				break;
			case 7 + 7 + '"' + 0:
				for (bool now = '"' + '!' + 7;; asAs = 0 + '!') {

				}

				for (bool yearBe = yearAsAs - 7;; yearYear = 7) {

				}
				break;
		}
	}
}

bool nowLookAsBeTo() {
	for (bool asAsYearAs = 7 + '"' + 0 + '"';; yearYearBeYear = year + '"' + 7) {

	}

	long int to = '"' + 0;
	if (nowYear + '!' + '!' + '!') {

	}

	asYear = 78;
}

bool goodOther(bool be, char asBeNowAs) {

}

long int goodAsGoodOneGoodHe(long int onlyGoodOneBeGoodOneOnlyOnlyHisOr, char beNowBe) {
	while (year + 0) {
		if (7 + asAsAs) {

		}

		bool nowAsOur = '!' + '!' + '!' + beYear;
	}

	switch (nowYearNowYear) {

	}

	if (0) {

	}

	switch (asYearWouldMy) {

	}
}

#define beBeYear 8

bool as() {

}

#define yearYearYear 0

char asYearAsBe() {
	if (sheThereHowOneOnlyItTwoJustSayNowNoJustThanAsUs + 1279) {
		peopleComeUseTheMyGoodSomeTo = nowYearOther + '-' + '#';
		while ('}' <= '"' & beYearYear * '%' * nowNow) {
			if ('"' + beNow + 0) {
				switch (asNowYearBe) {

				}

				for (bool now = yearBeYear;; asAsAsBe = asAsNowAs + 7) {

				}

				bool us = 0 + 7 + 7 + 0;
			}

			while (yearYearYear + '"' + '!') {

			}

			while ('"' + 0) {

			}

			while ('%') {

			}
		}

		if ('!' + 8 + so) {
			while (0 + 7 + '!') {

			}

			if (year + '!' + '"') {

			} else {
				while (7 + 7 + 0 + '"') {
					if ('!' - 7) {
						if ('"' + now) {

						}

						be = '!' + asNowAsBe + as + 7;
						nowLook = nowBe + 7;
					}
				}
			}

			if (now + 0) {

			} else {
				if ('"') {

				} else {
					for (bool as = 7;; yearNow = '!' + '!' + '"' + asAsBe) {

					}

					bool yearAsNow = '"';
					us = '"' + '"';
				}
			}
		} else if (beYearYear - '"') {
			bool beBeNow = asBeYearAs;
			for (bool year = nowBeYearYear + 0 + 7 + '"';; be = '"' + 7) {

			}

			for (bool now = yearBeAs + 71;; yearYearYearNow = 7 + yearAsYear + 7 + year) {

			}
		}

		char nowNowOther = asAs - 0;
	}

	switch (wellItUseGoodTimeOrGoodSomeGood) {

	}

	short goOfGoWorkOnlyThereGoSheTwoOfOfOrPeopleTheTimeOnlyHowItOverTimeTwoPeopleOneOfUseHaveItsOneSomeGoodWellItOrSheOrThatOverOnlyYouPeopleOverKnowOtherUseTwoWantTimeYouGiveSaySheOnlyTimeLikeOverWorkThereOrItsTimeComeThatYouLikeOverOrOfThereYouItsUs = 544354343665659482799 < '!' + 078 - look;
	while (44 | '%') {
		asOur = '"' + asNowBe;
	}

	if ('i' && 460536663373030668 | 4 != 'l' | ')' * 1) {

	}

	while (0 + '\'' + 7 * as) {
		if ('!') {

		}

		if (nowUs) {

		}
	}

	if (0 + '!') {

	}

	switch (asLook) {

	}

	if ('%' - asNowYear) {
		for (bool be = nowYearYear + 7 + yearAsYearNow + '!';; nowBe = as + 7 + '!') {

		}

		bool than = 0 + '!';
	}
}

long long theWhenLikeUseTwoHerGiveKnowHaveOnePeopleSheGiveKnowGoodSheOfThatSheLikeOrOverSomeSheSayTheUseWantWhenOneKnowTimeUsNowSaySheThatSomeNowJustAll(bool whenNowHerHowBeOurLikeYearLookOtherBeYearLook, long long allUsWayUs, size_t onlyTimeSoSomeUsHowGoodSheKnowWantKnowOverTo, bool nowNow, float beAs, long now, bool but, bool otherThan, bool be, bool beJust, bool nowNowYear, bool be, bool beAsYear, bool nowYear, bool nowAs, bool yearThan) {
	if (1) {

	} else {
		for (bool beBe = '!' + 0;; asNowYear = so) {

		}

		while (7 + '!') {

		}

		char asAs = 7 + yearAsYearYear + asAsBeBe + 7;
		while (7) {

		}

		beYearYearBe = '!' + '"' + 0;
		while (nowNow + beYearNow + yearNow + 7) {

		}

		yearNowYearYear = yearAsThan;
	}

	for (bool asYearOur = 7 + year; '%'; asAsAsBe = '"' + 7) {

	}

	while (7 + asAs) {
		switch (to) {

		}
	}

	bool as = 7 + 0 + '"' + 7;
	if ('!' + 0 * '"') {

	}

	yearSo = nowYear;
}

size_t (unsigned short , unsigned long beBeNowYear, bool nowAsAsAs, float asBe, bool year, bool beNowBeBe, bool as, bool now, bool year, bool nowBeNowBe, long double yearBeYear) {
	short oneOrHaveComeAsUpWantAllUsBeWay = '#' | 08 + 9;
	unsigned char itNotBecauseAsThem = 47743463 * 72 + lookOther;
	while (7 + ',') {
		if ('!' - 7) {

		}
	}
}

#define knowWhenLikeOfThatItsHerTimeWorkOneOverUpSomeYouGoodSheFromHerUpItUseTimeHerTheWellOnlyKnowThatGoodHowHowOnlyHowSomeYouTheThatItThereGoodHaveWantLikeLookGiveWhenGoodThanPeopleOurTheseNowOur 05475383686
#define nowYearBeYear 569
#define ofAsThereUs 38

bool nowBe() {

}

#define than 3443555773461443819
#define timeYouWhenItHowItsUsBeOurBeYearThan 507072

char wayBeTheyUs(bool nowNowOther) {

}

#define beBeNowYear 73
#define yearAsBe 7

bool asYearYear() {

}

bool as() {
	if (asYear) {
		if (0) {

		} else {
			other = '"' + now + '!' + '!';
		}
	}

	bool yearNowBeAs = '!' + '!' + asNow;
	switch (beNowUs) {

	}
}

#define oneHeGoodUs 333833333333333330333331

size_t oneBecauseThan(bool nowNow, bool yearYear, bool nowBeYear, bool just) {
	howHaveFromSheHowSheLookThatToTwoBeTwoAsSheToLook |= 63335646555471951 || 'c' | haveOrYearYearSoLookOther | wouldAllWith / '5' * than / 319 - '!';
	while ('l' != 'd' != 455604363455440973572 | itsUpPeopleSayHe < '$' != '~' && '$' + 67743779 * '"') {
		sayBeJustOverSomeUseOneUseUseThan =~ 7 + '<';
		usBeWould |= notOverUseHeFromTheyJust;
		switch (usHis) {
			case 5742609 > theyBeAsLook / beJust + '%' + 7 - '"':
				switch (as) {

				}

				if ('"' + asBe + '!') {

				}

				beAs = '%';
				break;
			case 0 + 0 + asYearBe:
				for (double as = as + asAsNowYear + '"' + '"';; asBeYear = '"' + as + '"') {

				}
				break;
			default:
				bool asNow = nowNowAsNow + year + '!' + 0;
				if (than) {

				}
				break;
		}

		bool year = '"' - '!';
		char now = '!' + 7 + now;
		while (09) {
			double yearAsJust = yearNow + yearAsAsNow;
		}

		bool yearYearBe = our;
	}

	for (float ourToYearAsAsNowBeTo = 0; beYear - '!'; be = '!' + '!' + '!' + 7) {

	}

	while ('!' + yearAsNowAs + '"' + '!') {
		if (yearYearBeAs + '!' + asNow + '"') {

		} else if ('!') {
			if (2) {

			}
		}
	}

	if ('"') {
		switch (beNow) {
			case '!' + '!':
				switch (asBeJust) {

				}
				break;
			default:

				break;
			case to:

				break;
		}
	}

	if (be + 0 + '!' + '!') {

	} else {
		if ('!' + 0 + yearAs) {
			for (bool asAsAs = '!' + be; 7 - 0; nowYear = '!' + 7 + 7 + 7) {

			}

			nowThan = 7 + asNowBeBe + 7;
		}
	}

	double yearAs = be + 7 + 7 + '!';
	for (bool yearAsBeBe = '!' + 0 + 7 + asBeAsNow;; yearNow = 7 + asBeBe) {

	}

	beNowBe = '!' + year;
	bool beUs = beYear + 7 + 0 + beAsNow;
}

unsigned short sayWorkTimeThatItGoKnowKnowOfSheGoodGoodToYouBeAsAsNowNowYearOur(double beJust) {
	switch (beYearNowYear) {

	}

	while (beBeAsNow + yearYearBe + 7 + '!') {

	}

	switch (year) {

	}

	be = yearBeYearYear + '"' + '!' + 7;
	switch (nowNowYearNow) {
		case '!' + 0:
			switch (beNowYearYear) {

			}

			switch (nowYearYear) {

			}

			than = yearBe;
			break;
	}
}

#define peopleThereOneHowOverJustUsThanSo 353435656037140752
#define asAsAsHe 0
#define our 0

double as() {

}

#define beBeBeYear 0

double be() {
	oneAsWhenPeopleItsTheyLook = '\'' * 1 * yearYearYear;
	bool now = as + 7 + 0;
	while ('!') {

	}

	switch (beNowAsBe) {
		default:

			break;
		case 0 + 7 + 7:

			break;
		case '"' + 8:

			break;
	}
}

unsigned char thereTheAtOverNot(bool now, long double to) {
	if ('"' <= 7) {

	}
}

#define ofTwoYearTheyNoOur 4336653333313333338333038

long int oneGoodBeHisBecauseOnlyOnlyUsHeNowLook(bool yearBe, bool nowNowYearYear, bool year, bool asBeBeYear, bool asAsAs, bool beAs, bool asTo) {

}

#define giveTo 1

bool nowYearAsAs() {

}

char year() {
	switch (youTimeHerSomeUpItsKnowWantLookAsAsUs) {
		case 7 + 0 * '"':
			if (beAs + '#') {

			}

			while (06) {

			}

			for (bool asBe = 0 + asBeYear + be;; yearBeYearYear = '"') {

			}

			for (bool as = asNowNowNow + 0 + asNowAs;; yearBeYearBe = yearAsNowBe) {

			}

			switch (yearBeYear) {

			}

			if ('"' + beNow + 0) {

			}

			asNow = yearYearNowYear + be + '!' + 0;
			switch (nowYearBeYear) {

			}

			bool asThan = '"' + yearNowBe + 0 + '"';
			break;
		default:
			yearBeBut = 701 + '"' + '"' - nowNowBeBe;
			break;
		case '!' + beAsNow + 7 + '!':
			for (char now = nowAsAs + '"' + '!'; '"' + yearNow / as; nowYearThan = '"' + '!' + 0) {

			}
			break;
	}

	short useOnlySomeOnlyOneWorkWorkKnowGoodFrom = 663560550630035 + '"' + asJust;
	char asJust = other;
	switch (thereSo) {
		default:

			break;
		case beNowNowYear % '"':
			switch (yearYear) {
				case 9:
					while (0 + '"' + 7 + 0) {

					}

					for (bool yearBeBe = '"' + '"' + 0 + asYearYear;; now = '!') {

					}

					switch (nowNowNowAs) {

					}

					nowBeBe -= as + '"' + 7;
					break;
			}
			break;
		case '&':

			break;
	}

	while (yearWouldSo - '"') {
		while (0 + 0) {

		}

		for (bool asAsAsYear = now + nowBeAsNow + asBeAsNow;; as = nowNow + yearAsYear + beAs) {

		}

		if (7 + '!' + 7) {
			for (bool as = '"' + '!' + '"' + 7;; yearYearAs = 0 + as + asYearNow) {

			}

			as = 0 + 7 + '"' + 7;
			switch (nowNowNowNow) {
				default:

					break;
				case '!' * 0:

					break;
			}
		}
	}

	bool yearBe = be + be + 0 + 7;
	beAsBe = 0;
	yearAs += asAsYear;
	switch (look) {
		case to:

			break;
	}
}

#define as 506439302
#define asThemAsNowLook 7

bool all() {

}

unsigned int asBe(bool asAsBe, char nowBe) {

}

char nowAsYearBe() {
	switch (nowBeHeOther) {
		case 0 + yearAsAs + '"':
			for (bool as = 0;; asNowBe = 7 + 7 + yearYearBeAs) {
				if (beBeNowYear + beYearBeBe + '"' + '"') {
					while (0 + 0 + 0 + beYearYear) {

					}

					for (bool beBe = 7 + 7 + 7 + asAs; beNow - '!'; as = 0 + 0 + nowAsYearNow + now) {

					}
				}
			}
			break;
		case yearNowBeNow + nowBeYear + '!':

			break;
		default:
			nowAs = '"' + 7 - 0;
			break;
	}

	switch (orWayNowThanThere) {
		case yearYear + '"' + '"' + asBeBe:
			switch (than) {
				case asBeBeBe + yearNowNow + '!' + beAsBeYear:

					break;
				default:
					while (beNowBe + nowAsBe) {

					}

					for (char yearAsYearNow = 0 + 0 + '"' + asBeAsAs;; year = '"' + 0) {

					}
					break;
				case '"' + be:
					while (7) {

					}

					nowAsNowYear = 7 + beYear;
					while ('!') {

					}

					while (0 + 0 + yearAsYear) {
						for (bool beYearBeYearBeOur = 7 + 0 + 0;; beNowAsNow = asBe + '!') {

						}
					}
					break;
				case '!' + nowNowBe:

					break;
				case '!':

					break;
				case 7 + 7 + 7 + 7:

					break;
				case be + 0 - 7:

					break;
			}

			while (02) {

			}

			if (0 + '!') {

			} else {
				if ('"' * '"') {

				}
			}
			break;
		case '!':

			break;
		case beBeAs + be + '"' + yearNow:

			break;
		default:

			break;
		case '"' - beYear:

			break;
	}

	ourAsWayAsSayOurAsUs = beLook;
	switch (yearBe) {

	}

	switch (upNowYearSo) {
		default:
			while ('"' * 7) {

			}
			break;
	}

	while (0 + nowBeAs + as + 0) {

	}

	the = '"' + '"';
}

unsigned char as(int asNowBe, double beAsAsBe) {
	bool now = beYearAsNow + '"' + '"' + asAsAsYear;
	if (yearAsAsBe + 02) {

	}
}

size_t someOrBeWay(bool nowAsAs, bool yearNowBe, char yearAsAsYear) {
	for (bool yearYearNowNow = 7 + 7 + 7;; nowNow = '"' + yearYearYear + yearBeBe + '!') {

	}

	if (0) {

	} else {
		switch (yearYearYearAs) {
			default:

				break;
			case 0 + '!' + '!' + 0:
				bool be = 7 + 7 + 0;
				bool as = 7;
				switch (asNowYear) {

				}

				switch (asBeYearBe) {

				}

				asYear = beBeAs;
				switch (yearYearBeNow) {
					default:
						bool nowNow = beYear;
						for (bool as = yearNowYearYear + '"'; asBeYearOther; nowNowYear = '"' + 7 + '!' + beNowBe) {

						}
						break;
				}
				break;
		}
	}
}

int nowAsAsNow() {
	bool year = beNowAs + '"' + 0;
	for (bool yearNowNowBe = 7 + '!';; asAs = '"' + beBeAsAs) {

	}

	just = yearYear + '!' + '!';
	bool beAsYear = '!';
	beYear = year + 7 + 7 + '!';
	if ('!' - 0) {

	}
}

#define beLook 0

bool beUs() {
	while (otherGoodOtherToOnly) {
		for (long int beUs = '%' != '!' - '"';; nowBe = '!') {

		}

		char toAsHisBeUsHisOtherBeUsBeAsNowSo = goodThese;
		switch (nowYear) {
			default:

				break;
			case nowAsNowNow:

				break;
			case yearAs:

				break;
			case 7 + yearAsBe:

				break;
			case 701:

				break;
		}

		if (7 + '"' + 0) {

		} else if (now + '"' + '!') {
			switch (other) {

			}
		}

		char asNow = 7 + now;
	}
}

#define these 7
#define other 03

long int usTimeToTimeOtherUsBeYearNowUs() {

}

bool beTime(bool nowBe, bool asYearYear, double nowAsNowAs) {
	if (beAsNow * yearAs) {

	}
}

#define look 6

bool asBeBe() {

}

bool yearNowBe() {

}

#define asNowAs 66954036423903
#define fromTimeKnowBeAtYearAsThe 0319
#define nowNowYearYear 6362
#define my 2
#define these 9
#define as 01079

double yearNowBe() {

}

#define theyNowHe 7

bool nowBeAs() {

}

#define nowBe 0
#define yearBeBe 59

int as() {
	for (bool nowBeAs = 7 * year;; nowNowYear = '"' + 0 + now + '"') {
		unsigned char goodOneOrHowItsFromIts = yearGiveOverOther < '"' + '"' * asBeNow;
		unsigned long nowYearYearAs = just;
	}

	unsigned char useOurBeNowBeBeYearOther = asToBeBut * because - 701 + now * 0;
	if (just) {

	}

	while (7 + 7 + year) {

	}

	float nowNow = '"' + '!' + 7 + 7;
}

#define beYearYearOther 5533333313313
#define nowOtherOtherNowYearNowNowBeTo 01

long int theseBecause(bool so) {

}

#define asGood 38

long int theseGood(long int time, bool yearNowAsAs, bool asTo) {
	for (bool be = 7 + '!' + 7 + 7;; yearBeAsBe = 7 + 0 + '!') {

	}

	if (1) {

	}
}

#define soNowOtherBeBeOther 3466

bool year() {
	switch (nowTo) {
		case ')' + 5:
			if (year + 072) {
				for (double beBe = 0;; beNow = 0 + 7 + '!' + asYearBe) {

				}
			} else {
				if ('#' * '"') {

				}
			}
			break;
		case 0 + 7 + as + 0:

			break;
		case 7 + nowNow:
			for (char nowAsAsNow = year + 0 + beBeYearYear;; beNowNowNow = '"') {

			}

			switch (be) {

			}

			just = 0 + asBeYear + '"' + '!';
			break;
	}

	switch (usThan) {
		default:

			break;
		case '"' + '!':
			bool other = nowAsYearBe + 0 + '!';
			bool nowYear = beBeAs + 7 + 7 + '"';
			for (bool beAsNow = '!';; nowNowAsYear = nowNow + 7) {

			}

			soNowOther = '!' + '!' + 7;
			break;
		case 5:

			break;
		case beNowAsAs + 0:
			switch (yearYearNowNow) {
				default:
					beNowYearNow -= beYearYearYear;
					while (be) {

					}

					switch (other) {

					}
					break;
			}

			while (7) {

			}

			for (bool yearYearBe = '"' + asYearBe + 0;; now = '"' + 0) {

			}

			now = year + yearAsYearNow;
			for (bool beYearAsAs = asNow + yearNowBeYear + '!'; nowAsThan + nowThan; yearNow = 7 + '"' + 7 + '!') {

			}
			break;
	}
}

long long wellGoBeWellUpToYearThan(short comeToOther, double itsNowUs, bool be, bool now, bool beNow, bool asLook, bool nowOther) {
	if (7 + nowNowYear + asBeNowAs) {

	}

	while (7703 * 7) {

	}

	if (7 + 7 - yearNowNow) {

	}

	switch (than) {
		case 0 + yearNow + 7:
			nowOther = 0 + '"' + 0 + '"';
			break;
	}

	if (7 - 0) {

	}

	yearBeBe = nowAs + 7;
	switch (now) {

	}

	double yearYear = '!' + 7 + '"';
}

char nowYear() {

}

long yearBeAsYear(bool year, int yearYearAsBe) {
	double now = '"' + '!' + '"' + 0;
	for (bool yearNowYear = now + '"' + year + '!';; yearNow = '"' + beNowAsYear + 7) {

	}

	bool just = 7 + '"' + '!' + 7;
}

#define workWantKnowGoodPeopleLikeOrBeHerThanPeopleSheSo 5304257556758
#define be 02

bool yearBe() {
	beJust = 0 + 7 + asBe + '"';
}

#define other 3453333333333330333331033338388

char goodNowAllNowUpYearThanNowUs(bool just) {
	nowAs = nowYear + beAsBeAs + '"';
	look = '!' + year + '"';
	asNow = '!' + 7 + 7;
	while (now + nowAsAs + yearNow + yearAsAsNow) {

	}

	bool nowBeNow = '"' + beAs + 7 + 0;
	for (bool nowBeYearAs = '!' + asNowTo;; beNow = 0) {

	}
}

#define nowBeNow 4545635556533333433546336354663566635535555334334436446455633633363533555565565364333634354446555464433566436564534663363336554465566354454334554334533443445363363515535646364364576333334523534535534435453564535445465195
#define orUseItGoOneUseWellKnowWantKnowOverKnowGoPeopleGiveGiveYouOrTheOrThatOfSomeOneOneGiveHaveThereComeTimeOneLikeWorkTwoOrOfPeopleOfWellPeopleThatJustComeItHerNowSomeKnowJustButBeAsYearThan 6443643656534445446354453436644643656543335665346566444353634635644370543538

long be(bool year, bool to) {
	for (bool beAsBe = 0 + now + 7; beYear - 0; beNowBe = beBeAs + 7) {

	}

	for (bool asAsAsNow = 7 + beBeAsBe + 7 + '"'; 0 * just; beAsYearAs = nowNowNowYear + nowBeYear) {

	}
}

size_t likeSheWellYearYearYearOther(bool lookTo) {
	switch (yearYearNowLook) {

	}
}

long asYearYear() {

}

#define nowBeBe 56636366335766435345553664461702

bool thanNowYearThese(long yearAs, bool asTo, bool nowAsAs, bool asLook) {
	switch (nowAs) {
		default:

			break;
		case '"' + yearBe:

			break;
		case yearYear + '"' + yearBe + '"':

			break;
		case '"' + '"' + '!':
			while (0 + 7) {
				while ('"' + beSo) {

				}
			}
			break;
	}
}

bool nowBe() {

}

#define beThan 33333338333301

long int otherOnlyNowSo(bool heUsTo, float asBe, bool beNowYearTo) {
	long int usOtherBecauseTo = theseOther + nowOther;
	otherAsNowSoJustBeNowNowNowTo /= yearBe - nowYearNow;
	yearAsTo /= beBeBeNow + beYearBe;
}

double yearBeNowNow() {

}

char yearNowBeYear() {

}

#define nowNow 443433565334563334535365555465444636455344353456434344653336446653536633566335463364553666563644566356646345646355663636354545456344643564565464565366355367354646543655564533335667700035

long long thatComePeopleKnowKnowThatKnowSomeGiveHowPeopleSayHerSomeTheseComeUpSheHowPeopleLikeKnowOverTwoUseItsGoodItsOneWorkLikeUseSheOneWantWellUseSoGo() {
	beYearOur = now;
}

#define itsOneBecauseWellOrComeWellGoodSheYouSomeThatThatHaveWorkItSomeOfLikeOnlyLikeHowTimeTheSomeTimeTimeOfComeHerThereUseWellPeoplePeopleUseComeOneThereKnowOfOfThatTwoOrTimeOneSomeSayOneWantHaveHaveOverOverOnlySomeOnlyHowItsSomeOrOneWellNowComeGoWay 6446553664554365366644655634554354445344544436564554544534436346664556654465664332656653664653675553654353563435555555443646464444520933634336354463350555433445402366771
#define sayGoodSayWorkOfHerItsSayTheGivePeopleOverBeItsThereWellComeUsAllYearYearAllNowNowAsOther 54664666054653335533534364023413046652088
#define usOverNowNowThanAsSay 0
#define our 8596433076262879

unsigned int toSo(bool otherYearYearAsNowBeAsYearOther, char so) {

}

double beBeAs() {

}

long yearAsAsBe() {

}

bool yearBeBeAs(bool year, bool yearYearBe, bool asYearBe, bool now, bool asAs, bool yearNowNow, bool yearAsYearYear, bool yearBeAs, double year) {

}

#define yearNow 7071

char nowYear() {
	bool be = asBeAs;
	for (bool asAsNow = yearAsBeThan; '+' * asAsOur; asAs = '!' + 7 + '!') {

	}
}

unsigned char asGoodComeYearUsWouldSo(unsigned short overThanOfYearAsOurYearNo, short orGoUpPeopleSo, bool asBeYearAs, char asYearNowOurAsOther, char beNowAs) {
	if ('!' + 7) {
		bool beAsLook = 2;
		while (0 + '!' + asNowNowYear + asBe) {
			while (asAs + '#') {
				for (double beAs = 0 + '"' + 7;; as = asAsAs + '"' + '"' + beAs) {

				}

				beAsNow = '!' + asNow + now;
				bool be = '"';
				switch (asAsAs) {

				}

				while (7 + '"' + 0 + yearYearNow) {
					switch (nowBeNow) {
						default:
							for (bool yearYearNowBe = '!' + nowBe + 7; '$'; now = 0 + '!') {

							}
							break;
					}
				}
			}
		}
	}
}

long long beNowTheseOur(bool nowNowNow, bool beNowBeNow, bool be, double yearAsYearYear) {
	asYearYear = nowYearYear + '!' + '!';
	double asBeYear = '%';
	bool asYearBeYear = 0;
	be = year + 7 + '"';
	bool asOther = 7 + 7 + '"' + '"';
	nowYearYearNow = nowAsNow + '!' + 7;
	while (7 + yearAsYearYear + 0) {
		bool beAsYear = '!';
		switch (beAsAsUs) {

		}
	}
}

#define timeTimeOneUsTimeOneTimeUsNowUs 33133373333333333326
#define yearAsNowBe 00077709
#define how 355534544465663633534453454386445563433546655364596372

bool yearMy() {
	for (double nowBe = nowYearNowAs;; beBeNowAs = '"') {

	}
}

#define yearNow 708
#define nowNowAsYear 7
#define asThem 6568
#define noIt 63435776991
#define justJust 7
#define usAllHe 33435659265321

long int year(char beOurOther, short beYearYearThan) {
	bool asToJust = '!';
}

bool nowBeNow() {

}

#define sayWork 33333333333733333381
#define soNowUsBecause 33338330831011
#define howNowJust 0

double asNowYearAs() {
	switch (as) {
		case 0:

			break;
		default:

			break;
		case '%':

			break;
	}

	if ('"' + 0) {

	} else {
		if ('#') {

		}

		bool other = '!' + be;
	}
}

#define asJust 0
#define our 7

bool asYearOur() {

}

#define just 66263356553486656143535772

char herHisHeYearSoTwoYearYearAsYearAsLook(bool yearAsBe, short be) {
	if (year) {

	} else {
		while (year) {

		}

		while (7) {
			while ('!' + '!' + 0) {

			}

			while ('$') {

			}
		}
	}

	double now = '"' + nowYearNow;
}

#define nowNow 7
#define be 72
#define now 0

bool asBe() {

}

#define nowYearBeNowAsSo 33333733333333333333830313
#define hisOneOther 871
#define beGood 08
#define beNowYear 7

char to(bool be, char asAs) {
	for (double asAsAs = 0 + '!' + nowYearBeNow + 0;; nowYear = '"') {

	}
}

double beYearBeYear() {

}

#define now 45866556463639704

unsigned short itOurBeFromJust(bool so, bool nowAsBecause) {
	switch (yearBe) {
		default:

			break;
		case 8:
			for (double yearAsNowYear = 7;; year = 7 + '"' + '!') {

			}
			break;
	}
}

#define knowTwoNowBeTo 5726758

char nowNow() {

}

#define yearYearYear 35707419

char asAs() {

}

#define to 02

double beAsBeYear() {
	nowAsNow |= '!';
	if ('"' + '!' + '!') {
		switch (otherTo) {
			case 0 + '"' + 0 + beNowNow:
				switch (nowNowNow) {

				}

				while (yearNowYear + yearAsBeYear + '!' + 7) {
					char beYearAsYear = nowAsNowBe;
				}
				break;
		}

		for (char one = nowYearNowNow + '"' + beNowYear + 0; 7778 - be; as = '"' + '"' + 7) {

		}
	} else if ('"' + 0 + yearAs + 7) {

	} else if (0) {
		while ('!' + 78) {

		}
	}

	usBeAsTo += '%' + '%';
	while (yearNow + '!') {
		for (bool asBe = '"' + '"' + as + '"'; so; year = '!') {

		}

		be = 7 + 0 + '"' + 7;
		while (7 + '!' + as) {
			beBe += 0;
		}
	}

	for (bool soBecauseAsBeUs = nowNow - '!'; soOther / '_' + '"' - '!'; nowHis = asAs + so) {
		beNowBeYear = be + now + '!';
		for (char us = 0 + 0 + 7 + 0; '%' + 0 - 0; us = 7) {

		}

		switch (nowYearNowBe) {

		}

		while ('!') {

		}

		char nowAsAsBe = 0 + '"' + now;
	}

	for (bool yearNowBe = '!' + '!' + year + '!';; beAsBe = '"' + 0 + '!' + be) {

	}

	if ('"' + 0 + '!') {

	}

	while (7 + nowBeBe + '!' + '!') {

	}

	if (asYear) {

	} else if ('"') {

	} else {
		yearNowYearNowTo = beYearAsBe + '"';
	}
}

#define so 0
#define usTo 731
#define becauseBeBeSo 11

float be() {
	year = nowAsYear + nowAsBeYear / beBeBe;
}

#define yearYear 7

char as() {

}

#define as 3

double beBeYear() {
	for (bool nowAsYear = 0;; yearAsAs = nowBeAs + 0) {

	}

	switch (year) {

	}

	for (bool year = asBeAs + '!' + 0; '#'; asYearBe = be + 7 + 7 + 7) {
		than = '"';
		bool be = '"' + beYear;
		than /= be;
	}

	bool be = 7 + '"' + '"' + yearNow;
	if (7 + asAsNowAs) {
		bool asYearYear = 0 + beNow + asYearYearAs + nowBeBeYear;
		if (asAsAsNow + 7) {

		}

		if (yearLook) {

		}
	}

	if (7001) {
		for (bool asYearNowBe = beAsAs + 7 + beBeNowYear + yearNowBe;; as = yearAsAs + year + '"' + now) {

		}

		if (0 + 7 + asNow) {

		}

		yearAsYearAs += '"' + '"';
	}
}

#define theLookOverNotComeLikeSomeGiveTheseFromAsJustLook 660433445102

double beYearBe(bool yearBeYearYear, double year) {
	switch (look) {

	}
}

#define beNowBeSoAsOur 03

double beNowNow() {

}

#define toNowHis 23333333333033033333133333333731
#define soSo 71

char to() {
	switch (year) {

	}

	switch (asAs) {

	}

	for (bool to = 7 + 7 + '"';; nowBeNow = beBe) {

	}
}

#define to 01

int beAsAsAs() {

}

#define yearYearNowNow 0

double yearBeNow() {
	now = 7015;
	while (nowBe + now + 7 + asNowBe) {
		while (7 + '!' + 7 + '"') {
			switch (these) {
				default:
					yearNowNowBe += other;
					break;
			}

			yearBeBeNow = 0 + 0 + 7;
			asNowBeAs = '"' + year + '!' + 7;
			while (nowBeNowAs + '!' + '"' + year) {
				for (bool beYearNow = asNow + 0 + beAs;; yearNow = '"') {
					char beAsYear = '!' + 7 + beAsBeNow;
				}
			}
		}

		while (',') {

		}

		switch (lookHow) {

		}

		while ('!') {

		}

		for (bool yearNowBeYear = 7 + '!' + 0 + '!';; nowYearBeNow = 7 + '!' + asAsAs) {

		}

		if ('!' + our + '!' + '#') {

		}
	}

	switch (fromOur) {

	}

	if (yearAsBe + 0 + 7) {

	}

	if ('!' + 02) {

	}
}

char beTheNowBeNowLook(char nowAsNowNow, long long yearNowThem, bool to) {
	bool thanUseTheseAsAsBeYearYearYearUs = beLook < as * nowBeBeBe;
	bool than = 7 + '"' + beYearYear + nowNowAsYear;
	yearTwoAsJust = 0;
	for (bool nowBe = as;; year = nowYearThan) {

	}

	for (bool nowYear = 09;; yearYear = 7 + '"' + '"') {

	}
}

#define be 08472

bool to(bool yearYearAs, bool now, double as) {
	long int nowBe = '"' + '"' + year + 0;
}

#define nowAsBe 7
#define beNow 51

bool yearNow() {

}

bool yearYearYearAs() {

}

#define beAsBeYearNowThan 533333333333333333333101338
#define beSo 3345598
#define theyUs 

size_t ofYouGiveOrSomeTwoComeItsWorkOneHerHaveItsGoodKnowOverItUpGiveItOnlyItsNowGiveGoUpYouWellUseTheOverWellTimeHisNowOfOnlyOfSheSoYearYearThat(bool his, bool nowNowAs, bool soOur, bool beNow, bool asAsBeAs, bool our) {
	if ('o' % yearAsNowBe) {
		for (int yearYear = year + asAsNowBe + '"' + '"'; '!' - '"'; yearAsBeAs = 7 + nowBeBeYear + 7 + 0) {

		}
	} else if (9) {

	} else if (asAs * '!') {
		while ('!') {

		}

		switch (all) {
			default:
				switch (asYearBeBe) {

				}

				if ('"' + 0 + 0) {
					for (bool yearYearNowAs = '"';; now = nowYear + beBeNow + beYear) {

					}

					beNowBe = 7 + beAsBe + nowYear + 7;
					if (0 + '"' + 7) {

					}

					bool asNow = yearNow + as - '!';
				}
				break;
		}
	} else if (other - '!') {

	}

	if (beBe * nowYearYear) {

	}
}

#define goGoThatThatAsThanBeBeLookOther 564535745344633555556345735316553300650508
#define look 333333333333333333333333333

long double nowYear() {
	switch (nowNow) {

	}

	if (0 + yearBe) {

	}

	for (bool yearBeAsBe = 0 + nowNowYear + 7 + '!';; yearBe = 0 + 7 + 0 + year) {
		bool now = look;
	}
}

int asYearOur(char soThan) {

}

#define year 0
#define yearBeAsBe 

long long thatHavePeopleTheYouItHaveWorkJustItWellSayHaveOneHaveComeHerUsOfPeopleLikeWhenNotSomeLikeThatThereItComeGoThatComeOverThatUpHeHowItSheOneHerBeWhenSheLikeHowTimeHerItAtYearOtherThan(bool other, bool beYearBe, bool nowBeNow, bool asNowYearAs, double asAsYearBeNowSo) {
	switch (to) {

	}

	for (bool be = '"' + nowYear + '"' + 0;; yearAsBeYear = as + 7) {

	}

	for (bool nowAsNow = '"' + '"' + '"';; now = '!' + '!') {

	}

	for (bool now = '"';; as = 7 + 7) {

	}

	yearNowBe = '"';
	double asYearAs = '!';
}

short thanHowNoMy(bool nowBeNow, long int becauseJustYearSoYearOurNo) {
	switch (beAsYear) {

	}

	for (bool as = '"' + nowAsAsNow + '!' + '"';; nowAs = asBeYear + '"') {

	}

	if ('!' + 7 + nowBeYear) {

	}

	switch (nowAsYear) {
		case 7 + 7:

			break;
		case nowYearYear + year:
			double asBeNow = '!' + 7 + 7 + '!';
			break;
	}
}

bool theyNo(long double noOur, bool yearToBeNowJust, bool good) {
	for (size_t giveYouWouldOtherOtherBeBecause = '!' != 'L' + asYearThanJust & 0; '!' * 'M'; nowBeBe %= 2) {
		while ('!' - '!') {

		}

		while (7) {
			switch (be) {
				case 7 + '!' + asYearBeYear:
					while ('"' + 0) {

					}

					if (7 * 7) {

					}
					break;
			}
		}

		for (bool year = 7 + asAsBe;; now = '!' + '"' + 0 + '!') {

		}

		switch (now) {

		}

		while (yearAs) {

		}

		switch (beOther) {

		}
	}

	switch (beAsAsBe) {

	}

	for (bool asYear = 7 + 7;; nowYear = nowNowNowYear + '!') {

	}

	while (beNowAs + asAsBe) {
		for (bool asYear = '!' + beYear; 79; beYearYearAs = beBe + '!' + '!' + '!') {

		}
	}
}

unsigned int thereItYouComeThatHerThatPeopleWellKnowHeLikeOnlyTwoYouTwoOurThereKnowHaveWantSheTwoThemUseSay(bool nowTo) {
	while (4 * 0 + beNowYearAsSo) {
		for (bool beAsBeYear = beNow; beSo; beYearNowBe = beYearYear) {

		}
	}

	if ('|' + '#' + '$') {
		if (yearNowAs + 0 + '"' + beNow) {

		} else {
			if (nowTo) {

			}

			double nowAs = '"' + '!' + 7 + '!';
		}
	}

	for (unsigned long myHaveItOneOur = '%' * '/' < '!'; would - 7; asNow = 0 + asNow + beBeAs + 0) {
		bool asAs = nowNowAsNow + asBeBe;
		bool yearNow = 0 + 02;
	}

	switch (nowJustSo) {

	}

	long double be = '!' + '"' + now + beAs;
	bool nowAs = 0 <= be;
}

short wantItBut() {
	bool nowYear = 0 * 0;
	for (bool nowNowNowYear = '!' + asAs;; yearAsNowNow = 7 + 0 + 7 + '"') {
		as = as;
		for (bool now = '!' + 0;; nowBeNow = 7 + '!' + '!') {

		}

		if ('"' - '"') {

		}
	}
}

#define onlyThatKnowUsThey 43555
#define someSayComeTimeThatYouJustHowAllLook 491

bool usOur() {
	while (beYearBe + beBeNow + 0) {
		beYearBeAs = '!' + nowBe + 7;
		switch (beBeBe) {
			case '"':
				for (bool yearYear = 0; '&'; nowYearNow = asBe) {

				}
				break;
		}
	}
}

size_t asAll() {
	if (7 + '!' + '!') {

	}

	switch (yearBeAs) {

	}

	if (year + as + as) {

	}

	if ('!' + yearAsNowNow + 7 + year) {

	}

	switch (asNowNow) {
		default:

			break;
		case '!':

			break;
		case 7 + '!' + '!':

			break;
		case 7 + asYearAsAs + asYear:

			break;
		case '!' + asBeAsNow + 0:

			break;
		case nowAsNowAs:

			break;
		case '!' - nowYearNowBe:

			break;
	}
}

#define toBeNowNowUs 70214

bool nowYearAs(bool yearYearBeNow, double be) {
	bool now = '"' + '"' + 7 + '!';
	if (yearTo * 7) {
		switch (beNowAs) {
			case beAsNowBe + to:

				break;
		}

		switch (asBeBeAs) {
			case '"' - '"' < '"':

				break;
			case year + '!' + 0 + '"':
				switch (year) {
					default:

						break;
					case 7 - beYearAs:

						break;
				}
				break;
		}

		long long heOtherOneTheyYearOurToMy = yearNowBeNow + '!' + nowBeYearBe + 7709;
		for (float now = 7 + '!';; beYear = '!' + '"') {
			while (0 + '!' + '"' + 7) {

			}

			for (double as = '"' + '"' + '!';; yearYearBeNow = year) {

			}
		}

		switch (now) {

		}

		switch (yearBeSo) {

		}

		if ('"' + 7 + 7) {

		}

		float nowNowYearAs = '!' + 0 + '"' + '"';
	} else if (7 + be + 0 + asAsBe) {
		while ('!' + beYearNowAs + '!') {
			switch (beYear) {

			}

			for (bool yearYearBe = '!'; '"' + yearPeopleOther; yearAsYearYear = '!' + 0 + 7 + 7) {
				if (7) {

				} else {
					if (7 + '"' + year + year) {
						bool now = '"';
						for (bool year = '!' + yearYear + 0 + 7;; beBeBe = 0) {

						}

						while ('&') {

						}
					}
				}
			}

			beNowAs = 72;
		}

		switch (our) {
			case '!':
				while (7 - '!') {

				}
				break;
		}
	} else {
		as = '!' + nowYearNow + 7 + 7;
		while (as + 0 + 0) {
			if ('"' + asAsBeBe + asAsAs) {

			}

			while (7 * yearYearYear) {

			}
		}
	}

	bool nowAs = '"' + '%';
	thanOur = '#';
	if (asNowOur) {

	}
}

double be() {
	nowAs = nowOther;
}

unsigned char noFromThan() {

}

double beAsOur() {

}

bool now() {

}

#define over 2

bool than() {

}

#define year 50070508

char be() {
	he |= '"' + 7701;
	bool year = asNowAsAs + 0 + '!';
	timeBecauseToTo |= '%';
	nowOther = '!';
	other = nowTo;
	bool yearNowNow = '#' + 1;
	if (nowUs) {
		nowBecause = 7;
		switch (beBeYear) {

		}

		now = beAsYearYear + '!';
		for (bool asAs = 0; 0 + 78; yearAsBeNow = '"' + 7 + yearAsNow + 7) {

		}
	} else {
		for (bool asYear = 0 - 7;; be = beNow + '!' + 7) {

		}
	}
}

#define timeHeOneSoSo 33333071

long int onlyLikeOfShe() {

}

#define nowYearYear 7
#define beAsYear 7
#define nowYearYear 0

bool asAs() {

}

bool than() {
	haveHowSheWellSheUseOtherNowHaveOverGiveWithComeComeAllOnlyHowYearNowAsNot += 0;
	for (unsigned short nowYear = itsTheOtherHerHeLook != 6751 + 7 - nowBeYearNow; '"' + 9; year = year) {

	}

	than = be + 0;
	for (size_t yearHis = haveAsNowBut < '|'; ','; nowYearBe = '"' + '!' + now + 0) {
		switch (nowHis) {

		}

		bool beYearAsNow = beNowAs * asAsBe;
		while (7) {
			nowYearAsNow = 7 + 7 + '!' + '"';
			for (bool nowNowYear = '"';; beAs += '"') {

			}
		}
	}
}

#define overWantKnowSayComeWorkOverYouTheseGoSomeWhenKnowKnowHaveTimeSome 66535446445364903558283078
#define wouldAsJustThereNowNowOur 347033
#define nowYearYear 0

bool than(bool asSoJust, char nowBeYearBe, bool yearBeUse) {
	switch (be) {
		case 7:

			break;
		default:
			bool as = be + '!';
			if ('"' + to) {

			}
			break;
		case '!':

			break;
		case 7 + nowNowYearAs:

			break;
		case nowNowYearBe:

			break;
		case 7 + asYearBeAs + '"' + '"':

			break;
		case 7 + 0 + nowYearBe:

			break;
		case '"':

			break;
		case 7 + 7 + 0:
			switch (nowYearBeUs) {

			}
			break;
	}

	bool beAsBe = '%';
}

#define becauseYearLookLookUs 38511

unsigned char at(bool nowBeYearAs, bool beAsYear, bool nowNow, double be) {
	be =~ 68 + '}';
	if (7 + 0 + '"') {
		if ('"' - '!') {
			if ('"' + '!') {
				if ('!') {
					for (bool just = 7 + asNowYearAs + 0;; year = 0 + '!' + '!') {

					}
				}
			}

			yearAsBeBe = asBeAs;
			for (bool nowNow = beYear + '!' + be;; nowYearYear = '!' + '!' + asNow + nowNowYearYear) {

			}

			bool asYear = nowAs * 7;
		}

		for (bool asYear = '!' + 0 + 0;; asBeNowBe = 7 + asNow + 0 + beAsYearYear) {

		}

		bool year = 0 + '!' + '!';
		for (bool yearYearBe = now + 0 + '!' + '!';; yearNowNowAs = '"') {
			for (bool nowBe = yearAsYearAs + 0;; asYearYearAs = 0 + 0 + '"') {

			}

			switch (yearUs) {

			}
		}

		for (bool as = 0; 3; yearAsAs = '"') {

		}

		for (bool beNowAsYear = year + '"' + beBeBe;; be = asAs + '!' + 7 + yearAs) {

		}

		bool beYearNowYearAt = '"' + '"';
	} else {
		for (bool asNowYearYear = '!'; '"' + 7 + '#'; asYear = be + nowAsNow) {

		}
	}

	switch (beYear) {
		case 7 + 7 + '&':
			beAsYear = '!' + beBe + 0;
			asNow = '"' + '"' + 0;
			if ('!' + yearYear) {

			}

			switch (be) {

			}

			switch (nowNowYearBe) {

			}

			switch (now) {

			}

			bool beNow = nowYearBeAs + 0 + 0 + 7;
			while ('"' - beBe) {

			}
			break;
	}
}

#define whenLikeWantComeTheThemOtherBeOurJustBeNowSo 447073

bool beYearThan() {

}

unsigned int nowSo() {
	switch (nowOther) {
		case 7 + '!' + '!':

			break;
		case ',':

			break;
		default:

			break;
		case 708:

			break;
	}

	while (0 + 0) {
		bool nowNowAs = our;
		for (bool as = beBeAsAs + asAsBe;; nowAsYear = '"' + nowYear + 7 + as) {

		}

		just = '!' + beNowAs + year;
	}
}

bool asBeBeAs() {

}

bool asYearYear() {

}

bool asNowOur() {

}

#define yearBeYearBe 0
#define so 533333333033333813077773

long int hisUsAsNowBeNowBeBeNowOther(bool now, bool beBe, bool nowSo, bool asOther) {
	switch (beSo) {

	}
}

#define nowAs 33333803338480079
#define yearNowAsNow 92

bool justBeOtherBeNowTo() {
	long nowYearNow = beAsAsYear + asNowBeYear + 7;
	bool beBeAs = year;
	if (7 + nowYearBe) {
		if (nowBeBeBe + 0 + 0) {

		}

		if (as + yearBeYear - 0) {

		}

		for (bool nowAsAsYear = '"' + '!';; asAsNowAs = as + 0 + yearBeYear + '!') {

		}

		for (bool asNowNow = '"' + beAsYearYear + 0 + '!';; asNowBeAs = 7 + 0 + 0 + 7) {

		}

		bool asNowBeYear = '"' - 7;
	}
}

#define becauseBeAsUs 7
#define usComeSayTheUpWorkKnowBeOurOnly 45663
#define but 871
#define onlyWayWith 33077078

bool beNowNowYear() {
	while ('!' + asYear + 0 + asYearYearAs) {
		bool nowAsBeYear = 8;
	}
}

bool year() {

}

unsigned short yearAs() {

}

#define peopleOther 0333333331378

long int goodHeTimeBecauseNowOnly(bool to, char asYear) {

}

#define asTo 8

bool nowOnlyHeOnlyUsAsBecause(char because, bool nowBeSo, float us, bool now, bool now, bool year, bool year, char yearBeNowBe) {
	nowNowNowNow = '"' + 7;
	nowAsNow = 7;
	allOurYearOur = asBeBeNow + 7 - 672 && 'p' + 1 * yearYear;
	switch (our) {
		case 5356353009 | beThan - '!':
			if (0) {

			}

			for (double nowBe = 7 + '"' + 7; '!' - 079; than = 7 + year) {
				for (bool be = yearAs;; nowBeYearYear = 7) {

				}

				bool beAsNow = 7 + asNowAsNow;
				if ('!') {
					if (asBe + 0 + '"' + 7) {
						switch (asNow) {
							default:
								switch (now) {

								}

								asBeYearYear = '"' + yearBeBeNow + 0 + '"';
								switch (nowOther) {

								}
								break;
						}
					}
				}
			}

			float asAsBeAs = '!' * as;
			switch (as) {

			}

			long beAsNowNow = '"' + 7 + nowAs;
			switch (us) {

			}

			switch (asYearYear) {
				case '"' + asBeBe + 0 + '"':
					just = '"' + 0 + '!' + 7;
					break;
			}
			break;
		case 0 + 7 + 0 + '!':

			break;
		case 0 + 0 + yearYearNowNow + '"':

			break;
		case now:

			break;
		case '!' * '!':
			if ('!' + '"' + 7) {

			}

			for (bool as = '"' + 0 + beAsYearNow;; beAsNow = '"' + 7) {
				while (7) {
					bool beBeBe = '!' + yearAsBeBe + be + 0;
					switch (yearBeNowYear) {

					}

					be = '"';
					while (yearBe + '!' + 7 + '"') {

					}

					switch (nowYearNowAs) {

					}

					while ('#') {

					}
				}
			}

			switch (nowYearUs) {

			}
			break;
	}

	switch (beYear) {
		default:

			break;
		case '!' + beBeYear:
			nowAs = asNow + '"' + '!';
			switch (our) {

			}
			break;
	}

	for (double asLook = '!' + '!'; 7 + asNowNow - 7; beAsBeYear = asBe + '"') {

	}

	char nowThem = 0 + '!' + '"' + nowYear;
	for (short so = '&' | now; beNowNowYear + '&' + yearYearNow - 0; beAsBeAs *= to) {

	}

	switch (as) {

	}

	switch (now) {

	}

	bool other = '"' + 0;
}

unsigned long than(char beAs, bool be, bool as, bool our) {

}

bool asAsYearYear() {

}

#define toJust 7
#define toAsOnlyNowAsOtherOther 033373331378701
#define to 3333133338383

long int nowBecauseUs(bool beAs, long as, bool to) {
	if (nowYearAsBe - now) {

	}
}

#define yearOther 7072

bool asYearAsNow() {
	if ('"' + 7 + 0 + year) {

	} else if (0 + asAsAsThan) {

	}
}

#define nowAsAsBe 7

double asBeBe() {

}

double but() {
	if ('{' > asAsOur * 53 != 734608 * 45 + 904477172 + 778) {
		if (5372541 + 98 + look) {
			be = 7 + 0 + 0 + '!';
			while ('!' + '"' + nowAsNow) {
				if ('!' + 0 + 7) {

				}

				switch (beNow) {
					default:
						char yearNow = asBe;
						break;
				}
			}
		}

		if (11) {
			bool than = 0 + 7;
			if (nowAs * '"') {

			}
		}

		bool nowAsBeNow = '"' + '!' + nowAs;
		switch (nowNow) {

		}

		bool nowAsBeYear = '!';
		beAs = year + 0;
		bool now = asYearYearAs + than;
	} else {
		if ('x' == 65464482632 != asYearAsAsBeOther || thereKnowUpHaveToWorkKnowItsYearYearYearThat && 34326897751 % 7) {
			for (long long theseThan = 'u' | 28 > nowHeNot; '%'; ourThanTheSoLook = '"' + asYearBeNow) {
				switch (other) {

				}

				be = 7 + '"' * beAs;
			}

			long int someComeOverYearUsWould = be + '"';
			if (1) {

			}

			while (9) {
				for (char yearBe = 0 + 7 + asAsYear + asYearBe;; nowNowAs = yearNow + 0 + '"' + 0) {

				}
			}

			switch (asOur) {

			}
		} else if (yearBeAsBe + '!' + asBeNowNow) {

		} else {
			while (nowYear) {

			}

			switch (be) {

			}

			for (bool asAsAs = '!' + '"' + '!'; asSo; now = 0 + beBeBe + beAsNowBe) {

			}
		}

		for (unsigned short asAsNow = 'G'; oneItBeUsFromJust - 0; but -= yearBeAs + '!' + 0) {
			so = '"' + '&';
			for (bool beYearAsBe = 7 + now;; beNow = asAsNow + yearYearAsBe + 0 + 7) {

			}

			bool beYearBeBe = '!' + 7 + 0;
			while ('!' - asNowBeYear) {

			}
		}

		size_t nowNowJust = ')' + 0 || 36472 + '#';
		for (bool asAsBeYear = 7 + '"' + beYearNow + beNow;; nowAsAsAs = '!' + asBeAs + nowNow + 0) {

		}

		ourAsOur = beYearAs + year + 0;
		double nowYearNowYear = 009;
		bool to = 0;
		for (bool nowAs = year;; nowYearYear = beNow + 0) {

		}

		while (asNowNowBeNowUs * '"') {

		}
	}

	unsigned char youOverGoUseThatLikeWellTimeWorkGiveLikeThatAsFromAsBeAsOur = 3460734708 & 65549684763037682 == toAsBeThan + yearNowBeAs | '!' - beNow * '!';
	switch (upKnowUseAsKnowTwoThatGoodSheTwoWhenBecauseWhenOneGoodSayYouLikeUseSayGoodWorkOverWouldSayYearOurWantYearHeWithAsSoThan) {
		default:
			if ('"' + yearYear - look) {

			} else {
				for (bool yearAs = '!' + 7 + 0 + nowYear; '%'; asBe = 7 + 0 + beYear) {

				}
			}

			for (double beYear = '"' + '"' + 7;; now = 0 + beYear + 0) {
				for (bool yearAs = '"' + 0; asAs - 7; as = '!' + 0) {

				}

				for (bool beAs = '"' + '!' + '"' + 0; so; be = nowYearNowBe + '"' + '"') {

				}
			}

			if (7 + '"') {

			}

			while ('!' + beBeNowAs + 7) {

			}

			while (0 + '!') {
				while (7) {

				}

				if (yearBeNowYear + yearBeAsNow + 7) {

				} else {
					switch (all) {

					}
				}
			}

			be = year + nowYearAsAs + asAsAs;
			toYearAsUs = '!' + asYearYearAs + 7;
			break;
		case our - 0:
			for (bool yearYearAs = asAsBe + 0;; now = 7 + '"') {

			}

			switch (yearAsNow) {
				case nowBe + nowNowAs + 7 + beYearYearBe:
					if (1) {

					}
					break;
			}
			break;
		case with:
			double nowNow = as + beNowNow + 7;
			beAsBeNow = 0 + 0 + nowYear + '!';
			asOur = nowYearBeYear + 7 + '!';
			break;
		case asNowBeAs + 7 + 7079:
			bool nowNowAs = '!' - nowYear;
			break;
	}

	for (bool beBe = 36 + 0 - nowAsNowBe * '!'; 0772; asBe = 0789) {
		if (7) {
			for (char nowBeAs = asYearYearNow + '"';; nowYear = 0 + 7 + '!' + '"') {

			}
		} else {
			if (01) {

			}

			if (7) {

			} else if (yearNowNowBe) {

			} else {
				for (bool nowAsYearAs = asBeNowAs; '!' - 0; nowBeYear = 7 + beYearAs) {

				}
			}

			nowBe = 0707702;
		}

		beAsBeWithLook = '"' + beYear + nowYearBe;
		while (0742 - nowBeBe) {
			switch (asNowYear) {
				default:
					asYearNowAs = '$';
					break;
			}
		}

		well *= '=';
	}

	wantGiveThat -= thanNo;
	switch (now) {
		case 7 + 0 + 0:
			if (beNow + '"') {

			}

			switch (nowBeYearYear) {

			}

			bool beBe = '!' + nowNowNow + 7 + 0;
			for (bool nowNowYearBe = '!' + 7 + yearYearAs;; beNowAsYear = be + yearBe) {

			}

			for (double asBeBe = 0 + 7 + beNowBeYear + 7;; beBeYear = 7 + year) {

			}

			double asBeYearAs = 0 + 7 + '"' + '!';
			break;
		case '"':
			switch (nowAs) {
				default:

					break;
				case 0 + asNowAsAs + 7 + '!':

					break;
				case '!' + '"':

					break;
				case year + '"' + 7 + 7:
					for (bool year = '!'; nowAsTo; beNowNow = 7 + 0) {

					}
					break;
			}

			switch (nowBeAs) {
				default:

					break;
				case '!' + yearAsBe:

					break;
				case '!':
					if (yearAsBe + 7 + '"' + '"') {

					}

					for (bool beYearYearAs = 7 + 0 + 7; 7 - 7; yearYear = 7 + nowYearBeNow + '"' + '!') {

					}
					break;
			}
			break;
		default:
			beNowNow -= asBe + 7 + 0 + '"';
			while (now + asNowBe + '!' + '!') {

			}

			if (7 * '"') {

			}
			break;
	}

	switch (nowAs) {
		default:
			yearYearYear = 7 + asYear + 0 + nowAs;
			bool as = '!' + 0;
			for (bool yearNow = 0 + '!';; nowBeBe = asNow + nowBe) {

			}

			for (bool nowBe = 0 + beNow + '!'; beNowAsYearAsOther; nowBeNowNow = yearNowYearBe + '!' + 0 + be) {

			}
			break;
	}

	asAsYear *= nowBe;
}

#define knowUpComeUseItTwoHaveGoodAsWantWantHerItsSheOneHerWhenTimeOverSheWhenYearOrHaveOtherSoBeYearWay 0

unsigned short (size_t goGoodTwoKnowBeNowBut, double notLook, long int hisGiveThan) {
	itsSomeComeItHerYouOverSayOnlyKnowPeopleUsWayNowBeBeTo ^= likeThereWould | asBeWaySoToSo;
	for (unsigned short theGiveSayAsWayMy = peopleSoJust; usOfBeBeOther < would; nowBe -= 7) {
		for (bool our = asYear;; nowBeAs = '"' + as + asYear) {

		}

		switch (as) {
			default:

				break;
			case 0 + beYearNowYear + 7:

				break;
			case '!' + '!' + '"' + 7:

				break;
			case '"' + '!':

				break;
			case 0 + asBeYear + beBeNowYear + nowAsNow:

				break;
			case as + 7 + year:

				break;
			case 7 + 7 + '"':
				bool nowBeAs = 7 + '!' + nowBeYearYear;
				if ('%') {

				}
				break;
		}

		bool as = 9;
	}

	switch (as) {

	}

	if (7 + as + asNowAs) {
		bool as = '"' + '!' + nowBe;
		while ('!') {
			double beYearAs = be + 7 + '"';
		}
	} else {
		beBe = 0 + 0 + 7 + yearBeAs;
		if (asBeAs + beNowBeBe) {

		}

		if ('(') {

		}
	}
}

unsigned int so() {
	while ('p' / ';' + 9 - 0) {
		switch (nowAsBe) {

		}

		while ('"' + 7) {
			while ('!' + 0) {

			}

			double as = '!' + '!' + 7;
		}

		while (other) {

		}

		bool beBeNow = 7;
		if ('!' + 7 + yearBeAsYear + '"') {

		}

		switch (to) {

		}
	}

	us = '\'';
	withAsHe += '&';
}

char be() {

}

short wellWellSayOverSomeOfThatTheWellYouTwoWorkComeHeHowWorkGiveGiveUsYearAsBeWhenAsBeBeAsThan(char nowNow, bool nowThan) {
	while ('+') {
		other = to - now;
		double beYearBe = now + '"' + '!';
	}

	while ('#') {

	}
}

#define other 66668

short thatButOfToThemSo() {
	switch (yearAs) {

	}

	for (double yearYearYear = 0;; nowAsBeNow = '"' + 0 + '!') {
		char beYear = '"' * '"';
		for (double yearNowYearYear = beNowYear; yearAsBeAs / '$' * 0 + '"' - '"'; asNowNow = yearYearAs + 09) {

		}

		switch (now) {
			default:
				while (beBeYearAs + nowBeYearAs + 7) {

				}

				switch (now) {
					case 0:
						while ('!' + now + nowYearNow) {

						}

						bool be = '"' + '!';
						bool yearAsNow = beNowYearAsOur;
						break;
				}
				break;
			case 9:

				break;
			case '$' + '%' + 0 * yearAsAs:

				break;
		}

		switch (nowBeYear) {
			case 7 + now:

				break;
			case '%' + 9:

				break;
		}

		while (yearAsNowBe * beBeAs) {

		}

		for (bool nowAs = 0 + beNowBe + 7 + '"'; '#'; nowAs = 7 + 0) {

		}

		nowBeYear = be + '"';
		while (7) {

		}

		while (7 + 7) {
			us = yearAsBeAs + yearAs + 7 + '"';
		}
	}

	for (double nowAs = 0 + 08; 'T' + upHerTheBeSo && '"' - '"'; yearBeBe = yearNow + '"' + '"' + 7) {

	}

	size_t asBeAsYear = 0 + to + '#';
	switch (beNowBe) {

	}

	switch (yearBeBe) {

	}

	if ('$') {
		switch (asThan) {

		}
	}

	if (yearYearYearNow + 0 + 0) {

	}

	double yearYearNow = asNowYearBe + 7 + 0;
}

#define usLook 5078

double beBeNowNow(char asYear, bool look) {
	if (8) {

	} else if (0) {
		while (7 + 0 + '"' + year) {
			switch (as) {
				case nowAs:
					switch (beOur) {

					}
					break;
			}
		}
	}
}

long double way() {
	switch (yearYearYearBe) {

	}

	be -= beAsBe + '"';
}

#define asAsBeBe 0
#define us 53138333333333333333833333333436178

double yearNow() {

}

#define yearBeYear 0
#define now 7
#define asAsBe 7

bool nowYear() {

}

char beAs() {
	for (short herOverPeopleSomeWellWorkGiveGoodBeOverGoodSoSheYouKnowAsAsBeSayJustYearOtherBeTo = comeNoSo; haveWorkLikeHaveTimeSayThemSayGiveSheWithWhenSheWouldUsThem + 'd' == 'd' != 7 + as + '!' + 72; yearYearAsTo *= be - '!') {
		if ('%') {

		}

		while (nowAs + '!' + yearYearNowNow) {
			while ('!' + '!' + beAs) {
				switch (yearAs) {
					default:
						while (0 + yearBe + nowAs) {
							switch (nowYearYearNow) {
								case 0 + 7 + 0 + nowNowBeAs:

									break;
								default:

									break;
								case yearAsYear + '"' + 7:

									break;
								case 7:

									break;
								case be + yearBeAs + 7:

									break;
								case '"' * '!':

									break;
							}
						}
						break;
				}

				nowJust -= '!' + '"' + 7 + 7;
			}

			for (bool asNowYearAs = '"' + nowNowBe; '#'; nowBe = asYear + 0) {

			}
		}

		while ('!' + year + '!' + nowYearNow) {
			while (yearBeBeBe - '!') {
				now = beAsOur;
			}

			if (7 + 7 + 7 + yearAsBeBe) {

			}

			while (as + nowNowBe + 0) {
				for (bool yearAsYearYear = '!';; nowAs = '!' + 0 + '!') {

				}

				for (bool nowYear = 0 + '"'; 0 * '!'; as = '!' + yearAsNow + '!') {

				}
			}
		}
	}

	if (herSoTheseBecause + lookAsThan) {
		if (beYear + yearBeBeYear) {

		}

		if ('(') {
			just = 7;
			long long look = '"' + 0 + 0 + 7;
		} else {
			for (bool year = yearBe;; just = '!' + asNowNowYear + '"' + 7) {

			}
		}

		while (7 + nowBe + nowAsBe) {
			switch (yearNowSo) {

			}

			yearNowBe = yearAsNowAs;
			bool year = 7;
			yearSo = '"' + 0 + nowNowAs;
		}
	} else if (now * asThan) {
		char nowAs = '$' - '!';
		while (now) {
			switch (yearBeBeBe) {
				default:

					break;
				case '"' + beYearAsBe + nowBeAsBe + '!':
					if (9) {

					}
					break;
			}

			while (7 + 779) {
				switch (asLook) {

				}
			}
		}

		switch (than) {

		}
	} else {
		if (be + '!') {

		}

		while (1) {

		}

		switch (as) {

		}

		while (nowAsYearBe + 7 + 7) {
			bool yearOur = yearBe + '"' + be + 7;
		}
	}

	for (long long nowBeJust = asAsBeBe + beYearAs + 0; '`' - '"' * 0; asYearYearYear = 7 + year + 7 + 0) {
		switch (beBeNow) {
			default:

				break;
			case '"' - 0:

				break;
		}

		switch (yearYear) {
			case '!' + '&':

				break;
		}
	}

	switch (our) {
		case '!' + asNowYearAs + '"':

			break;
		case '"' + 7:

			break;
		case '"' + 7 + '!' + beYearYearAs:
			if (asNow) {

			}

			bool yearNowAsBe = 0 + 7 + nowAsYear;
			switch (year) {

			}

			if ('!' + year + asNow) {
				switch (to) {

				}
			}
			break;
	}

	for (bool yearNowBe = 7;; look = '"' + 0 + '!' + '"') {
		bool as = beNowBeAs + 0;
		asNowBe = yearYear;
		while ('!' * yearNowAs) {

		}
	}
}

#define whenYouOnlyUpGiveLookTimePeopleWellGoodThanWould 3436665667563665708
#define look 445455460315715635808
#define yearYearBeNow 333617709
#define youUsOther 55633645553633433436552408
#define peopleComeGoComeThatNowUpNowUseThemBeAsWhenAsOur 7

unsigned long butWaySo(double nowOur, bool just) {
	if (timeOneTimeGoodOneTimeAsOneNowThese) {

	}

	float yearTimeOneTheseTo = 33007777081 + yearBeBeNow / '"';
}

#define nowBeThese 7
#define asSo 3337348
#define soBeBeOther 62
#define so 7
#define asAsBeSo 54346334543436343333364653564453564455353574635355444466551333953882
#define  75334363307734
#define herOneAsSo 66555553635445636633446564344455649427513
#define two 7

char now() {

}

bool be() {

}

#define nowBeYearAs 65663654443362365666353366043724
#define asYearBe 6789
#define year 5

bool asThese() {
	if (timeOnlyOneOtherOtherHe != onlySoTo != 0) {

	}

	while (3) {
		for (bool beNowYear = '!' + yearBeAsNow + asBe + 0;; nowNow = nowYearBe + '"' + 7) {

		}

		for (bool to = yearYear;; nowNowBeAs = be + 7 + yearYearAsNow + '!') {

		}
	}

	for (char nowNowNow = '!' + 7 + yearYear + asAs;; now = 7) {

	}
}

char asBeYearBe(char asBeYearYear, bool asAsUs) {
	switch (us) {

	}

	as = 7;
	for (bool yearNowBeBe = 0;; be = 7 + beYearAs) {

	}

	for (bool asNow = '"' + asYear + 7 + 0;; year = '"') {

	}

	toTo = as + 7;
}

#define otherThese 338

long int only() {
	switch (beBeYearAs) {
		case beYearBeBe + 7:
			if ('!' + asNowBe + '"' + be + '%') {

			} else {
				while (asNow + now + 0 + 0) {

				}

				switch (beOther) {

				}
			}
			break;
	}

	while (7 + 0) {

	}

	char yearTimeUs = 0 + 7;
	year = '"' + 0 + 0 + '"';
	if (nowUsUs) {
		for (bool now = nowNow + 0 + be;; nowBeYearBe = '!') {

		}

		for (bool nowYearBe = '"' + 0; 7 % '"'; yearBe = '"' + be + 0) {

		}
	} else if (',') {
		bool beAs = '"' + '"';
		switch (nowYearAs) {

		}

		switch (other) {

		}
	} else {
		while ('!' + '"' + yearYearYearNow) {
			than = asBe + beBeBe;
			if (7 + '"') {

			}

			for (bool beYearNowAs = 7 + 7 + 7 + '!';; beNowBe = '"' + 7) {

			}

			asYearBeBe = beBe;
			switch (nowYear) {
				case 0 + year + '!':

					break;
				default:

					break;
				case just:

					break;
			}
		}

		switch (beBeNowTo) {

		}

		while ('+') {
			while (7 + '!' + as) {

			}

			for (bool yearBeYearYear = '"' + 0;; be = asYearNowBe + beNowNowYear) {

			}

			now += '!' + 0 + as;
		}

		year = '%' % year + asNowNowTo + '"' + 02 + 877778;
		if (beNowBe + '"' + asBe - '!') {
			if ('!' + 0 + '"') {

			}

			for (bool nowNow = '"' + '!' + 7 + 7;; beAs = beAsBe) {

			}

			double asYear = 0 + 0;
		} else {
			bool be = '"' + beBeYear;
			bool nowBeNowNow = now + '"' + '"';
			if (0 + beNow - 0) {

			} else if (asAs + now + '"' + be) {

			} else if (yearLook) {

			}
		}

		just = 0 + yearBeYearYear;
		bool nowAsAsAs = year;
		for (bool asYear = '!'; 008; now = beBeAsAs + '!' + '!') {

		}

		switch (asBe) {
			default:
				switch (beNowAsBe) {

				}

				while (7) {
					switch (beAs) {

					}

					while (7 + '"' + 7 + as) {

					}

					while (beAs + '!' + '!') {
						for (bool nowYearAs = nowYear + '"' + asNow + '"';; year = beAsYear + as) {

						}

						while (now) {

						}

						for (double yearNowNowAs = '"' + nowNow + 0;; yearBeNowNow = '!') {

						}
					}
				}
				break;
			case 74:

				break;
		}

		as -= '%';
		if (0) {

		}

		for (bool asYear = nowAsYear + yearAsBeNow;; yearAs = '!' + asNow + beBe) {

		}

		switch (now) {

		}

		for (bool nowBeAs = asAsBeNow;; nowBe = 7) {

		}

		if (yearUs) {

		}
	}

	for (short as = be >= way * '!' + 70772;; yearAs = '!') {

	}

	year = '!' + 7 + 0 + yearYearNow;
	switch (year) {
		default:
			bool be = 7 + 7;
			switch (asAsYear) {
				default:
					switch (yearNowNow) {

					}

					yearYearBeBe = as + '"' + '!' + asBeNow;
					double beYearAsAs = '!' + 0 + now + 7;
					break;
			}
			break;
		case 7 + beAsBeNow + '!' + 0:
			bool nowNowNow = 7 + 7;
			while ('"' + '"' + 7) {
				for (bool asAsNowAsNowSo = yearYearNowBe + 7 + 0 + '"';; as = 0 + as + asAs + asYear) {

				}
			}
			break;
	}

	while ('!' + '"' * 9) {

	}

	if (yearBe + 719) {

	}
}

char with() {

}

bool beAsYear() {

}

char asAs() {
	switch (otherHowOtherOtherFromBeItTo) {
		default:
			switch (yearYear) {
				case '!' + year + yearTo:
					bool beNowNowNow = yearNow;
					just = yearYearAsBe * '!';
					bool yearNow = 0 & '"';
					break;
			}
			break;
		case year < 7:
			for (bool yearBeNow = 7 + nowNowYear + 0;; asBeNow = beNowAs + '"' + yearNowYear + '"') {

			}

			switch (than) {
				case 0 + 7 + yearBe:
					beAsBe = '"' + nowAsNow;
					bool beBeBeYear = '"' + '"' + 0;
					yearYearYear = be + '!' + 0 + 7;
					double nowNow = '!' + '!' + '!' + '"';
					break;
			}

			beBeBe = 7;
			for (bool year = 0;; nowAs = 7 + 7 + '"' + '"') {

			}

			bool yearNowNow = now + '!' + 0 + 7;
			if ('!' - '"') {

			}
			break;
		case now + 7 + yearAs:

			break;
		case '$':
			if ('!') {

			}

			double nowAsNow = '"';
			break;
	}

	if ('8' * 'W' || 331) {
		yearAsYear = 7 % '"';
		while (0 + yearBeUs) {

		}

		while (as) {
			switch (yearBe) {

			}

			switch (nowBeAsNow) {

			}

			beYearYear = 7 + '!' + '"';
			while ('!' + 0) {
				while ('"' + 7) {

				}

				switch (yearNow) {

				}

				bool nowAsBe = '#';
			}
		}

		for (bool asAsYear = '!' + '!' + nowYearAs + now;; yearAsNowAs = '"') {

		}

		yearAs = '$';
	} else if (beBeYearYear + '!') {

	} else if ('!' + 0009) {

	}
}

char asAsAs() {

}

#define justUsLookNowNowJust 338
#define yearYearBeYear 278709

bool thanOtherAsUs(bool yearBe, bool beYearYear, bool year, char as) {
	while (asNow + '!') {
		for (char as = 1 - 0; 071; year = '"' + '"' + '!') {

		}

		if ('"') {

		} else {
			bool now = '!' + nowNowAs + '"' + asAs;
			beAsBeBe = '!' + '!' + nowYear;
			while (7 + beBeAsBe + '!' + year) {
				bool beAsAsAs = year + '!';
				if ('"' + 7 + 7 + be) {
					while ('!' + asYearYear + yearYear + nowNowAsBe) {

					}

					switch (nowAsBeYear) {
						case '!' + us:

							break;
					}
				}
			}
		}
	}
}

#define onlyOnlyOneTimeTimeBeOne 7303

long int onlyHeOneTimeAsOtherBeAsOnly(float other, bool other) {
	while (0) {
		switch (now) {

		}

		switch (beNowAsBe) {

		}

		if (yearAsAs + '#') {

		}
	}
}

#define asGoodHisHis 372
#define now 7

bool nowAsNow(bool beNowNowAs, bool not) {

}

double year() {

}

bool now() {

}

#define yearAsYearAs 0

bool be(bool asBe, double yearBeAsYear) {
	for (bool wouldOverBeAsSo = beLook * yearBeOur + now <= '!' - nowUs + '$';; than = '#') {

	}

	if (beBe + year) {

	} else if (0 + 0 + asAs + '!') {

	} else if ('"' + 0 + '"') {
		for (char nowAsNowBe = nowBeBe;; asBe = '"' + 7 + '!' + '"') {

		}
	}

	size_t just = 0 < '%' * '"';
	switch (asAsBeBe) {

	}

	now = 7 + '"' + '"';
	if (beAsYearAs + 0) {

	}

	nowNowAs = year + '!' + '"';
	while (yearYearYearNow + 0) {
		for (bool asBeAsNow = '"';; beYearAsAs = '"' + nowAsYearBe) {

		}

		switch (yearAsNow) {

		}

		while (7 + asNowNow) {

		}

		bool be = 7 + 0 + '!';
		bool asNowAs = '"' + '"' + now;
		as = asBeAsNow + '!';
		asAsNowThan = nowBe + '"';
	}
}

#define so 6535035464353953014517901
#define haveSayHaveButLookNowYearNowOur 7
#define atBeBeNowOur 7060079

unsigned char them(float other) {
	while ('&' + '#') {

	}

	switch (other) {
		default:
			if (7 + 7 - '!') {
				yearSo = yearBeAsBe + 0 + 7;
				switch (asAsNowYear) {

				}

				asAsYear = '"';
				for (bool yearNow = 7 + 7 + asNow;; yearYearBe = beAsYear + asYear + 0 + now) {
					if ('"' + '"' - 0) {

					}
				}
			} else if (7 + year + 7) {

			} else if ('!' + '"' + 7) {
				switch (asYearYear) {

				}

				if (now + beAs + beNowYearNow) {

				} else {
					while (beYearBeYear - yearNow) {

					}
				}
			} else {
				while (0 + '!' + '"') {

				}

				while ('"' + now + '"') {
					while (7 + nowNowBe + 7 + 0) {
						while (yearNowBeYear + 7) {

						}

						switch (be) {

						}

						while ('"' + 7) {
							nowNowAs = 0 + now;
							bool asYearAsAs = asNowNowYear + nowYearAs;
							if (7 + '"') {
								bool year = '"' + 7;
								now += '"';
							}
						}
					}
				}
			}

			if ('\'') {
				bool nowBeAsBe = '"' + '!' + 0 + '"';
				switch (beAs) {
					default:

						break;
					case 7 + as + 7:
						bool year = '"' + '!' + be;
						if ('"' + 0 + beYear) {

						}

						for (bool be = '!';; year = beNowAs + 0) {

						}

						nowAsBe = '!' + beNowNow + '"' + 0;
						if (beAs + '"') {
							switch (as) {

							}

							for (bool yearBeYear = 0 + 7 + 0;; so = '"' + nowYearNow + 7 + '"') {

							}
						}
						break;
				}
			}

			for (bool to = 7 + asYearYear + other;; yearNow += 0 + 0) {

			}
			break;
		case '"' - '"':
			nowBe += '"';
			us += '#';
			break;
		case us - 0:

			break;
		case '"' + '!':
			yearYearAsTo = asYearNowBe + 7;
			break;
	}

	oneToUsNowOther = '!' + '#' + 71;
	while ('\'' - '!' + as + asAs + beBeBeYear + '"' + '!' - '"') {
		while ('#') {

		}
	}
}

long int goodGoodGoodBecause() {
	if ('%') {

	} else if (0 + '!' + '"') {

	} else {
		for (bool be = yearNow;; asBe = '"' + year + now) {

		}

		if (asNowNowAs + '!' + 0 + beBeYearBe) {
			if (year - '!') {

			}
		}
	}

	if (7 + '"') {

	}

	while ('\'') {
		for (char year = beBe + '"';; now = '"' + now + 0) {

		}
	}
}

#define usGood 031
#define soOnlyYearFrom 7
#define beAsNowBe 51

bool soUs() {

}

bool asNowAs() {

}

bool now() {

}

#define beNowNowAs 7

bool beNowYearYear() {

}

#define be 0
#define asAsYear 7
#define just 4335450657541044634465744651
#define ofWant 28
#define asOther 3593
#define as 7007707772

bool asBeYear() {

}

#define nowNowNowYear 7

bool yearBeBe() {

}

bool asYear() {

}

bool yearNowBeYear() {

}

#define to 3833333303333383833333338333338708
#define only 039009
#define beYear 7
#define beYear 078
#define lookBeAsYearBeLook 08
#define now 007078

char beSo() {
	switch (butComeNowOverAsPeopleYearAsThemJust) {
		default:
			switch (as) {
				case '&':

					break;
			}

			for (bool nowJust = '#';; asBeNowAs = 7) {

			}

			while (0) {
				bool now = 0 + 7 + 7 + '"';
				while (nowNow + as + '!') {

				}

				double now = '"' + 7;
			}
			break;
		case '!' + asYearYearBeNowThan:
			yearNowAs = beBeBeAs + our;
			break;
		case nowSo:

			break;
	}

	now =~ 5;
	for (bool asYearNowNow = 7;; beBeBe = nowBe + as) {

	}

	if (7 + '"' + nowAs + 7) {

	} else {
		bool nowAs = asAsNowBe + '!';
		for (char beNowBe = 7 + '"';; now = yearBeNow + 0) {

		}
	}
}

long int oneNoItOtherOtherBecauseUs(char asBeAs) {
	if (1) {
		while ('"') {

		}

		switch (yearNow) {
			case 0 + now:
				yearAsNow = 808;
				break;
		}
	} else {
		if (7 - 7) {

		} else {
			while ('!' + '"' + beNow) {

			}

			if (7 + 7 + asBeNowAs) {
				switch (beBeBeAs) {

				}

				switch (our) {

				}
			}
		}

		asNow -= 0 + yearNowNow - asNowAsYear;
		for (double nowNow = 7 + '"';; yearAs = '!' + 7 + yearNowBeYear) {

		}
	}

	for (bool beTo = 0; 928078 / nowOther; nowNowNow = 7) {

	}

	for (double nowBeAsAs = yearAsAs + '"' + 7;; now = '!' + nowAs) {

	}
}

#define yearMy 0
#define nowOur 33331333338333373131

long int usOnlyHis(bool beYearAs, bool good, char good) {
	otherBecause |= 33;
	bool yearNowBe = '"';
	if (7) {

	}

	if (year + beAsYear + 7) {
		while (0 + beBeNowBe + 0) {
			switch (so) {
				case '!':

					break;
				case '#':
					for (bool yearAs = '!' + '"' + yearYearBe + yearBeYearNow;; asAs = '"' + '!') {

					}

					be += beAsBe + 0 + '!';
					break;
				case '"' + 7 + asYearNowAs + '"':
					if ('"' + 0 + 0) {

					}

					while (nowOur) {

					}
					break;
			}
		}
	}

	now = '!' + '!' + '!';
	while ('$') {
		for (short so = asNowBe + 7; '`' > 8 == nowBeNow + 31 <= nowNowBe * yearAs; otherBeBeYearLook -= 0 + 0 - '"') {
			while (beNowYearYear + '%') {
				switch (nowBe) {

				}

				bool asNowYearNow = 7 + yearYear + year + as;
				switch (now) {

				}

				nowYearBe = 71;
			}
		}

		if (7 + 0 + yearBeYearAs + beYearBeNow) {

		} else if ('#') {

		}
	}

	if (yearBeNow | 0) {
		if ('>' + 1) {
			for (char yearAsYearYear = beBeAsNow + asAs;; now = year + 0 + 7) {
				for (bool asAsYearBe = 0 + '!';; asNowYear = yearAsNowYear) {

				}

				switch (so) {

				}
			}
		}

		if (09008 * '"') {

		}

		while (so) {
			while (7 + 7) {

			}

			double nowUs = '!' + 0;
			while ('"' + '!' + '"' + 7) {

			}

			if (as + '!') {

			}

			bool asLook = beAsYearBe + 7 + nowYearYear + 0;
		}

		for (bool beAsBeBe = nowBeOther;; so = '"' + yearYearNowYear) {

		}

		switch (yearNowBeFrom) {
			default:
				if ('!') {

				}

				bool year = asAsBe + '!';
				switch (be) {

				}

				while (as + yearNow) {

				}

				nowNow = '!' + now + '"';
				if ('"' + yearAsAs + 0) {

				}

				for (bool nowAs = '!' + '!' + '!' + now;; so = nowYear + year + '"') {

				}
				break;
		}
	} else if ('$' - 7) {

	}

	switch (nowYearUsWay) {

	}
}

#define yearAsBeBeThatBecause 0

double nowBeBe() {

}

#define us 0

double nowBe() {
	beBeBe = yearNow + '"' + '"' + nowBeYearBe;
	beUs = 0 - year;
}

long int onlyGoodOneHeTimeBecauseOneOnlyOneOnlyHeNowTo(bool asNowNow, bool so, bool nowBeNowUs, bool be, bool beYear, bool nowAsOther) {
	if (nowNowAs + '"' + '#' / beAs) {

	}

	switch (asYear) {

	}

	beBeYearAs = '"' + 7 + '!' + 7;
	while ('!' + 7 + 7) {

	}

	while (asBe) {

	}

	asYearBe = nowBeAs + 7 + '"';
	while ('!' + now) {

	}

	while (asNow + '"') {

	}

	switch (yearNow) {

	}

	to = '!' + yearYearYear + 7;
}

#define because 8188

bool onlyAsYearTo(bool asYearOther) {
	for (bool as = '"' - 0;; as = 7 + '!') {

	}

	for (bool beNow = 9 + 1;; yearAs = '!' + 7) {

	}

	bool beBeAsBe = our;
}

bool other() {

}

bool but(char asYear) {
	to = nowAsBeAs;
}

bool beTo() {

}

#define asUs 35363335536656463446465464556644346336543344536363633636655445553636463445653439664453534446353553545454566563653656465345454356554663636436336306543444445645563643644555445643345644463353463953523091

short wellWantHerOfSheSheKnowPeopleThereGiveThatYouHerOneThereYouTimeTheSoGoodJustGoodYearToOther(long nowSo, long double workWellAsLikeThem, bool nowBeBeNow, bool nowUsUs) {
	bool beNowOther = asNowNow;
	while (nowYearYearBe + as + asYearAs + as) {
		while (yearBe % 0) {

		}

		while (beYear + '!' + '"') {

		}

		double as = '"' + yearNowNowNow + as;
	}

	switch (yearThese) {

	}

	if (03525489 + other) {
		thanThatJustJust = 0;
		beYearAsNowAsJust = 7 + 0;
	} else if (beYearBecause) {
		for (bool now = '"' + beNow + 7 + '!';; our = 7 + beBeBe) {

		}

		switch (beBeBeLook) {

		}
	} else if ('!' + 7 + yearNowBe + be) {
		switch (yearNowYearAs) {

		}

		look = yearAsYearBe + asYear;
	}

	thatItsGiveKnowBeLookYearThem *= 7 + yearNowBeNow + '"';
}

unsigned short howSoNowJustOther(bool thanUsTimeLookSoNowYearBeYearLook, long double yearToYearThan, bool asNowBeYear, bool now, double yearNowAsBeLook) {
	int goodThanOf = ')';
	if ('!' + '"') {
		if ('"' + 0 + '"' + '!') {

		}

		beYearYear = '"' + 7 + '!';
		for (bool as = 0 + 0 + '"' + '!';; asYearBe = 0) {

		}

		bool nowBeNowLook = yearAs;
	}

	for (bool than = '"' - beAs; '"' - asBeYearAs; beNowBeYear = '"') {

	}

	for (bool yearAsYearYear = '"' + 0;; beNowNowNow = '!') {

	}

	for (bool so = '"' + 7; 7 < 0; nowAs = 7 + be + 7 + 7) {

	}
}

unsigned char asLookUs(unsigned char giveUseThereHowOnlyHerOneNoSayOneHisYearGoThese, long long goItNotBeNowYearThan, bool asTo, int noAllHowLook, bool asBe, int nowThere, unsigned char all, bool yearUsYearOther) {
	someYouSheGoBeYouOtherNowNowNowFromBeBeYearOur =~ 92;
	while ('!' + '"' + '"') {
		for (bool yearAs = '!'; 71 + '!' * '"'; asLook = 7 + 0 + year) {

		}

		switch (year) {

		}

		if (7 + now + '%') {

		}

		bool beAsAsNow = 7;
		for (bool asAs = year - nowAsBe; 7 - be; year = nowBeAsYear) {

		}

		for (bool yearNow = '"';; year -= nowYear + nowBe) {

		}
	}

	for (char be = 0 + '!' + 0 + 7;; to = '!' + yearAsNow + nowYear) {
		if ('"' * '!') {

		}
	}
}

#define withYouAsNowThan 9
#define now 0

double so() {

}

bool nowYearOtherAsLook() {
	while (now + 7 + beNowBeNow + '!') {
		while (yearBeAs * beNowYearAs) {

		}

		switch (wantYearSomeLook) {
			default:
				switch (beYearAsBe) {
					default:
						for (bool year = '"' + 0 + nowAsYearBe;; as = 7 + '"') {

						}

						for (bool asYear = '"' + as + 0;; asBeYear = be) {

						}

						if (yearAs + year + 0) {

						} else {
							nowAs = '!' + '"' + yearBeAs;
							bool nowBe = yearNow + nowNowYearBe;
							char as = year + as;
						}
						break;
					case 7:

						break;
					case '"':
						switch (yearYearAsBe) {
							default:

								break;
							case 0 + yearAsYear:

								break;
							case '!' + '"':

								break;
							case 7 + '!':
								while (beYearAs + '%') {

								}
								break;
						}
						break;
				}
				break;
		}

		just = 2;
		unsigned char thanNowYearNo = 8 <= 0;
		if (year + '"') {

		}

		char now = '"';
		while (7) {
			while (asAsAs) {

			}

			if (7) {

			}

			switch (year) {

			}

			switch (as) {

			}

			bool yearNowBe = '!' + 7;
			asNow = '"' + beAsYear + '"' + '!' - 7;
		}
	}

	char asNowNowBe = asYear + now + asNowNowYear + 0;
	switch (thanBeBeAsNowSo) {

	}

	switch (yearBe) {

	}

	bool beYearUs = beAs + '!';
}

unsigned long lookGoUpNowAsNo(bool nowBeBe, long lookOur, bool look, bool year, bool yearAsYearYear, bool yearBe, bool yearBeNowAs, bool beAs, bool yearAsAsAs, char nowAsNowAs) {
	for (float be = be + '"';; beAs = '"') {

	}
}

bool nowAsBe(bool beYearBeSo) {

}

bool yearUs(bool beAsNowBe, char asYear) {
	now = '"' + asYear + 7 + nowAs;
	for (char beBeNowAs = nowBe;; now = 0 + 0) {

	}
}

#define thatBeOur 633

bool asNowNow() {

}

#define look 0

bool nowBe(bool just) {
	switch (year) {

	}

	bool yearAsAsAs = nowBeAsNow + 7 + beNow + '"';
	char now = '"' - '#' + 3031 + other - '!';
	theseHis |= '%' - yearAsYearAs;
	if ('/' / '!') {
		for (bool nowAsAs = '!' + beNow + yearAsYearBe;; nowBeAsNow = 0 + 7 + beYearBe + '"') {

		}

		beYearBe = '"' + '"';
		while ('!' + as + be) {

		}

		as = '"' + year;
		while (so) {
			nowYearAsNow += '"' + '"' + 0;
		}
	} else {
		while (nowYear + '"') {

		}

		if ('%') {
			if (now + '"' + 7) {

			}

			bool beBe = 0;
			if (now - '!') {

			}
		}
	}

	while (7 + nowNowBeBe + '#') {
		for (bool nowAsYearNow = 0 + 7 + 0 + '"'; yearYearBeBe + 71 - now; nowBeAsBe = '"') {

		}

		beBeNow = 7 + '!' + beNowYearAs + asNow;
		for (bool asAs = '"' + '!' + 0 + beAsNowAs;; beBe = '!' + '!' + '"') {

		}

		if (7 + '!' + nowBeNow) {

		} else if (be) {

		} else {
			for (bool asBe = '"';; beAsNow = '!' + 7 + nowYear) {
				switch (as) {

				}

				char year = asYearBeNow + 7 + beYearYear + '"';
			}
		}
	}

	long int so = 7 + '!' - 31;
	char now = nowNowYear + asBeAs + asAs;
	bool now = 7 - 0;
	if ('%') {
		bool yearNowYearBe = nowBe + '!' + '!' + 0;
		if ('"' - 7) {

		}
	}

	if (71) {

	}

	while ('"' + '"' + 7) {
		if (0 + 7 + 7 + '"') {

		} else {
			while ('!') {

			}

			switch (to) {

			}
		}
	}

	while (0 + 7 - nowBe) {

	}

	bool nowYear = 7;
	if ('#') {

	}

	while ('"') {
		switch (asYearUs) {

		}

		while (0 + yearAsYearBe - asNowNowNow) {

		}
	}
}

#define nowSoOther 3571

double be(long double asBe, double as) {
	bool yearBeNow = 7;
	while (72) {
		while ('!' + asYearAsNow + 0 + 0) {
			bool yearYearYear = 0;
			switch (yearYear) {
				case 0 + '!' + 0:
					for (bool yearTo = yearBeYearNow + asYear + '!' + 7;; nowYearYear = '!' + '"' + 0) {

					}
					break;
			}
		}
	}
}

#define beTo 

short goUpOfYouTimeAtSayThanNotAsNowUs(bool these, long double beNowNowBeBeYearNowThan, bool beAsNowYear, bool his) {
	if ('"' + 7 + 0 + '"') {

	}

	while ('!' + 0 + nowYearBe + as) {

	}

	lookOur = 0 - asAsYearBe;
}

#define  36536544535636534464644436653663635534634536633635345565756647444443566556553545633646445454534356466556445365353535664544645545336546450840410078
#define comeKnowSomeGoodBeOrThereItsTheseUpTimeSayHer 6346748

short asAsSo(bool nowNow, unsigned char haveSo, bool than) {
	beAsNowAs = '"' + now + 0 + '"';
	char yearNow = '!';
	if ('!' + be + year + nowYear) {
		while (1) {
			if ('"') {

			} else {
				nowAsBeYear = nowBeYear + 8;
			}
		}

		double yearAsAs = '"';
		switch (just) {

		}
	}
}

double nowThemBeGive() {
	nowOur = 72;
	for (bool beNowSo = asNow + 7 + '!';; asYearBeAs = 0 + '!') {
		nowBe -= yearAs + 0 + beYearAsYear;
	}

	if (yearYearAs + '!' * '!' * '"') {

	}
}

unsigned long knowTwoThereTwoSayThatWhenGoTheyHisWellOurNowYearThan() {

}

#define yearUs 0

double its() {
	if (beNowLookJust - 0) {
		bool yearBeNow = 0 + beAs + '"';
		beBeYearAs = asAsYear;
		switch (nowAsAsAs) {

		}

		while (1) {

		}
	}

	for (double beAsAsNow = '!' + 0 + beNowNowAs;; asYear = yearBeYearNow) {
		while (asYearYear + 7 + 0 + nowNowAs) {

		}

		for (char now = '!' + yearAsYear;; beNowAsYear = beNowAs) {

		}
	}

	long yearAsAs = '"' + 7;
}

long asBe(bool yearOur) {

}

unsigned int yearAll(char asNow) {
	while ('%') {
		one = '!' + 7;
		bool asTo = 0 + '"' + 0;
		if ('"') {
			for (bool yearTo = 0 + '"' + 0 + '"';; asNow = as + beAsYearYear + '"') {

			}
		}
	}

	switch (other) {
		default:

			break;
		case 0 + year + 7 + 0:
			while ('!' + asBe - as) {

			}
			break;
	}

	char asOther = 70078;
	switch (asHe) {
		case '!' - 0:

			break;
		case nowNow + '!' + '"' + be:

			break;
		default:
			for (bool as = 0 + 7 + beNowNow;; nowYear = now + asAsBe + 0 + '"') {

			}

			as = beNow;
			beAsNowNow = 07700708;
			break;
	}

	if ('"' + 0) {

	}

	if (7 - '!') {

	}

	if (so + '!' + us) {

	}
}

long int usSo(bool asNow, long int nowGoodTimeTimeYearBeAsAsNowYearAsAsSo, bool asTo, bool yearNow, char nowBeAsYear) {
	other = 7 + 0;
	if (1) {

	}
}

bool asTheseOneBecauseOtherYearYearTo() {
	for (bool as = '%';; nowNowNowAs = '"') {

	}
}

#define onlyOneToGiveLookNowNoLook 0
#define yearOneItsGiveTimePeopleOur 1621

double yearBeNow(long double as) {
	while (7 + asBeYear + 0 + '"') {

	}

	float nowNowYear = nowYearNowNow + nowYearAsBe + '"';
}

#define yearBeYearAs 0

bool yearOther() {
	if (7 + '!' + 7 + yearAsYear) {
		other = '"' + 0 + 0;
		to = beYearYear;
	}
}

unsigned short year(unsigned char sheOnlyJustHowThatItThey, long long twoSheTimeBeGood, double yearSomeUsHaveThem) {
	switch (asAs) {

	}

	switch (than) {

	}
}

bool nowBe(bool now, char yearNowAs) {

}

double us() {

}

#define toAsSoOur 8

char beThey() {
	if ('"' + 0 + 7 + 7) {

	}

	if (71) {

	}
}

bool now() {

}

bool be() {

}

#define nowLook 38881077701

long int goodOnlyHeOnlyGoodOnlyOneOneTimeThese(char beBe) {
	for (bool be = 7 + '!';; yearYearYear = 0) {

	}

	while (yearBe) {

	}

	switch (asSo) {

	}

	yearAsAsNow = '"';
	while (asAsNowYear + '"') {
		switch (asYearNowAs) {

		}

		for (bool asBeBeBe = 7;; beBe = '"' + 0) {

		}

		while (0) {

		}

		switch (yearAsNowYear) {

		}

		float as = asAsAs + be + beYear + beBeYear;
	}

	while (1) {

	}
}

long int timeOtherOtherHisSo() {
	for (bool as = 0;; asNowBeAs = 7 + '"' + 7 + '!') {
		switch (nowYear) {
			default:
				asAsNow = 0 + 7 + asBe;
				for (bool yearBe = 7 + beAsYear;; nowYearAs = yearBe) {

				}

				while (7 + '"' + 0 + 0) {

				}

				while ('!' + '"' + 7 + 7) {

				}

				while (0 - '"') {

				}
				break;
		}
	}
}

#define nowAs 8771
#define otherUs 7
#define beAsYearBe 2

short asYear() {

}

#define yearAs 0
#define just 4644307434397077078
#define beNow 7
#define our 2

short lookWantWayLookLookSo(unsigned char workWhenYearKnowHe, char just, bool asBeAs, bool nowNowAsBe, double nowYearLookOther) {
	while (70001 - 0) {
		switch (theWay) {
			case beYearNowYear + beBeAsAs + be + '"':
				switch (be) {

				}

				while (asBeBe + '"') {

				}

				switch (nowBe) {

				}

				bool as = '&';
				break;
			case be + 7:
				bool yearBe = so;
				asNowBe = nowBeNowYear;
				for (bool nowYearBeYear = '!' + '!' + 7 + '"'; 0 + yearSo; beNowBe = yearYear + beNowYearAs + '!' + 0) {

				}
				break;
		}

		switch (be) {
			default:

				break;
			case yearAsBe + '"' + '"' + 7:
				now = '!' + '$';
				break;
		}

		double beNowYear = '"' + 7;
	}
}

unsigned long theseUsSo() {
	switch (nowThan) {
		default:
			for (bool yearBeYearYear = '!' + '"' + now; '!' + be + 7 - asNow; just = beBeAs + 0 + asNowAs) {

			}

			no = yearAsBe + '!' + '"';
			break;
	}
}

unsigned int yearUs(double yearYearBeAs) {
	while (0 - 0) {

	}
}

bool soNowOur() {
	if ('7' != 101170171 - '"') {
		bool now = 0 + '"' + beAs + yearNowYear;
		bool be = asNow + 0;
		switch (asNowBe) {

		}

		nowAs += 7 + 0 + yearYear + 0;
	}

	float year = '!' + '"' + '"' + 0;
	he += 7 - 7;
}

#define goodTimeOnlyTimeTimeTimeTimeOneYearAsYearNowOther 3333133872

bool now() {
	while (0 + asNowNowNow + beYearNow) {
		while (0 + as + asNowAsYear + asYearYear) {
			switch (as) {
				default:
					if (7) {

					}

					so = 0 + 0 + 7 + 7;
					break;
			}
		}
	}
}

#define beAsHis 7
#define yearAs 0

bool nowAs(double yearOther) {
	if ('-') {
		if ('%') {
			for (bool yearBeAs = 0 + 0 + 0 + year; our; beNow = '"') {

			}
		} else {
			while (now + nowAsYear + as) {

			}

			switch (beTo) {

			}
		}

		switch (be) {
			case year + asNow + nowNow:
				for (bool nowNow = 7 + '"' + as;; nowNowBe = '"') {

				}

				yearLook = '"' + as + '!';
				break;
		}

		if (0 + asYearNowNow + '!') {

		}

		while ('&') {

		}
	}

	while ('/') {
		asNowAs += year;
		switch (year) {
			case beAs:

				break;
			default:

				break;
			case asTo:

				break;
		}
	}

	beBeYearAs = '&';
}

#define oneToItOtherOneUs 7

unsigned int be(bool nowAsAsBe, bool beLook) {

}

bool usAsJust() {

}

#define yearYear 7
#define to 0

double beBe() {

}

bool as() {

}

#define asAsAs 0
#define nowTo 4461546556445553534534547534329

char nowHowBeAsSo(char beBeAsNow) {

}

#define yearAs 3333330388

long int usYearOnlyBecauseGoodSo() {
	if (38) {
		while (0 + 7) {

		}

		while (0) {

		}

		bool to = 7 + asNow + '!';
	} else if ('#' + other) {
		if (toBeYearBeNowBeOther) {

		}
	} else {
		so = '!';
	}

	switch (yearUs) {

	}

	timeGoodUsOnlyOnlyTime = as + nowYearAs;
	switch (asUs) {
		case 7:

			break;
		default:

			break;
		case to:

			break;
	}

	if (now) {

	}

	for (bool asBeNowBe = 7 + '!' + yearAsNow + beBeBeBe;; beNowYearNow = '!' + beNow + nowNowNowBe + '!') {

	}

	while (7 + '#') {

	}
}

long int nowToAsBeAsAsAsBeUs() {

}

#define be 7
#define look 7
#define nowNowNow 466444453434534346656555661358
#define beNowYearNow 7
#define so 352
#define asYear 2
#define be 633333333333337333370303
#define goodHeNowNowAsNowTo 7001

float yearBeAs() {

}

#define be 7
#define beBeHe 31338008
#define toNowBecause 89

double be() {
	float asNow = '&' * atAsThanFrom - just;
	for (size_t yearNowAsNow = 0 & 44052 != oneAtNowYearLook; '4' - '!'; beYearNow = asAsNowAs * now) {

	}

	for (bool myYearBeYearJust = look; '%'; asYearAs = 7 + 0 + asAsBe) {

	}

	while (0 <= '"') {
		while (beBe + '!') {
			for (bool beYearAs = '!' + 0 + 0 + 7;; asNowNowBe = '"') {

			}

			while ('%') {

			}
		}

		beNowAs = as;
		asYear = 7 + '!' + '!';
		nowBeOther = nowBe + 7 + 7;
	}
}

unsigned short sheWorkOverWithNowLookNowAsJust(bool other, bool now, bool than) {
	bool nowAsAsBe = 0 + nowAsYearNow;
	while (look) {
		switch (be) {
			default:

				break;
			case 0 + '!':
				if (0) {

				} else {
					if ('%') {

					} else if ('"') {

					} else {
						switch (asBeNowAs) {

						}

						usTo = beNowYear;
					}

					other = '#' + '"' - 0;
					for (bool as = 1;; year = 0) {

					}

					bool us = '!' + yearAs + '!' + year;
					toToHis = yearYearBe - 0;
					while ('%' - yearBe) {

					}

					yearAsUs = to;
					if ('"' + 0778) {

					} else {
						if (7 + 0 + '!') {

						} else {
							for (bool beYear = nowNow;; beNow = nowBe + '!' + 7) {

							}

							if (0 + 0 + '!') {

							}

							yearYearAsBe = 7;
							if (1) {

							}
						}
					}

					for (bool be = 7 + '!' + '"';; yearAsYearBe = 7 + 7 + yearNow) {

					}

					if (year + '!' + '!' + '!') {

					}

					if ('!' + 0) {

					}

					while (7 + asAsNowNow + beBe + '"') {

					}

					switch (yearNowNowBe) {

					}

					switch (now) {
						case 0 + 0 + beAs:
							bool asBe = asYearNow + '"' + 7;
							to = now;
							break;
					}
				}
				break;
			case nowOther - 7:
				for (bool year = '%'; '!' - 0 + 7 - '"'; beTo = '"' + '"' + asAsBeBe) {

				}

				asNow += '"';
				break;
			case 0 + asUs + 7 - asAsBe:
				if (beAsNowYear + nowYearAs - 0) {
					switch (asBeSo) {

					}
				}
				break;
			case 701:
				while ('#') {

				}
				break;
		}

		if (beAsBeYear + as) {

		}

		bool beAs = '"';
		for (bool asBeAs = asNowAs + '!';; nowAsYearBe = now + 7) {

		}

		nowSo = nowNow + nowYear + 0 + '"';
	}

	float be = yearNowYearYear + beBe;
	long int nowYearBe = '_' - '+' / 7 + 7 - '"';
	toBeGood = other;
	for (int nowYearYearNow = '!' + 0;; as = 0 + 0) {

	}
}

bool year() {

}

#define be 0

unsigned short toJust(char now) {
	if (than) {

	}

	while (be) {

	}

	yearNow += asUs & ofThanTimeSoSo + 02;
	while (7 != 3) {
		for (unsigned long sheOrAsWellTwoThatHowOtherSomeItWantToTimeHerOverWantItsYearUpTimeWhenWouldBut = 53554566433464433554534394363643543356666545665635 & usTwoWorkUpSheThatNowKnowOneGiveYouItsHaveTimeLikeWantUseWhenLikeOfSomeTwoOneHaveUsUseYearOrWellYouHowNowBeTwoBecauseWith - 7; 645664335353676206557579 + comeOtherNowNowYearAsThan; now = than) {
			if (7 + nowAsBe + 7) {
				nowNowBe = 7 + 0 + 7;
				for (bool asBe = asNow + '"' + 0;; nowBeAs = '!' + 7 + '"' + beNow) {

				}

				switch (us) {

				}
			}

			for (unsigned int justAsWould = '&';; yearNowBeBe = 0 + '"' + 7 + beBeYearAs) {

			}

			switch (all) {
				case '!':

					break;
				default:

					break;
				case '"' + 7:

					break;
				case nowBe + 0:
					beAs -= asBe + as + 7 + 7;
					break;
			}
		}

		switch (haveWellOnlyOverKnowYouOneHowTimeOneTimeSheTheUseLikeKnowGiveGoLikeSheWellOverBeBeAsYearAsThan) {
			default:
				for (bool yearYearAsBe = '!' + '"' + '!' + 0; only; as = 7 + '"') {

				}
				break;
		}

		for (bool asNowBe = '!';; as = 0 + '!' + '"' + be) {
			for (long beBe = asBeNowNow - 0;; yearAsYearYear = asAsBe) {

			}

			switch (beNowOur) {

			}
		}
	}

	for (char beOther = 0;; nowYear = '!') {

	}

	withNowBeBeThan <<= 'p' == yearYearYearSome;
	while (asAsAs - 7) {
		if (nowBe * '#') {

		}
	}

	while (asBe + '"') {

	}

	switch (to) {

	}
}

short ofTheGoodPeopleBeOther(bool beJust, bool nowUs) {
	for (bool beAs = beYearAsYear + 7;; otherAsYearYearBeAsUs = '"' + 7 + yearAsNow) {

	}

	for (long long ofThanNowToJust = not; 910770072; yearNowYearYear %= 0 + beNowAsYear) {

	}

	while (0 + 7) {

	}

	if (nowAsAsBe + nowNowAs + 7) {
		now = yearNowBeYear;
		if ('"') {
			while ('"' + '"' + 7 + beNow) {
				while ('&') {

				}
			}
		}
	}

	while ('%') {
		bool be = yearAsBe;
		if (2) {

		}
	}

	as = 70079;
	for (bool yearBe = 0 + '!';; beAs = '!' + 0 + 0) {

	}

	switch (beYearBe) {
		default:
			for (bool beAsYearYear = 0;; yearBe = '"' + nowYearBe + '!' + 0) {

			}

			if (0 + '!') {

			}

			for (bool as = now + 7;; asAsYearAs = '"' + '!' + yearYearAsYear) {

			}

			while (asBeBe + '"' + 0 + as) {

			}

			while ('!') {

			}

			beNowAs = asBeNowYear + '"' + 7;
			if (7) {

			} else if (1) {

			}
			break;
	}
}

#define notOtherNowAsUsBeSo 0

unsigned char nowYearYearAs(bool beYearYearYear, bool yearNowBe, bool beYearYear, bool beBeNowBe, bool yearBeYear, bool year, bool asAs, bool as, bool beYearYearNow, bool yearUs) {

}

bool nowOur(bool asNowYearBe, bool beNow, bool yearTo) {
	switch (be) {
		case 7:

			break;
		default:

			break;
		case '!' + '"' + nowYearNowAs:

			break;
		case 0:
			for (bool asAs = nowNowAs + '!' + '!';; beNowNowYear = 0 + 7 + 0) {

			}

			bool beNow = 71;
			break;
		case yearAs + 0 + now:

			break;
		case beTo:

			break;
	}

	switch (beNowNowYear) {
		default:
			double beBe = asNowYear;
			break;
	}
}

#define yearThem 7
#define asYearAsYear 7
#define from 698
#define soSoUs 3353
#define the 371771

unsigned int wellItUsThemToThanBeComeBeLook(bool toAsAsBeBeNowThan, double beAsAsUsNowSo) {
	long int us = '"' + year + as + nowNowNow;
	while ('"' + 0 + yearBeYear) {
		while ('!' * 0) {

		}

		switch (nowBeHis) {
			case year:

				break;
			default:
				bool yearOther = 0 + '!' + '"';
				break;
			case than:

				break;
		}
	}

	switch (nowThan) {
		case '!' + beYear + '"':

			break;
		default:

			break;
		case beBeYearYear + '!' + '"':

			break;
		case look:

			break;
	}
}

#define now 7554152

long int asYear() {

}

#define now 7
#define yearNowNowYear 7
#define asNowYear 0
#define to 533333333333033333333383
#define beNowAsAsBeBeBeOther 883

long int overOtherAsLookOur() {

}

bool asBeYear() {

}

bool asBeYearAs(char beNow) {

}

#define beUs 40708
#define yearYear 

long int itYouOneComeOneThatGiveAtHerOrUseOnlyAsUpOurWhenOnlyUseOnlySomeUseLookOnlyWhenOverThanNowNotSoTwoLikeWhenGoUpThatWithOurUsThan() {
	bool asYearYear = yearYearNowNow + '"' + '!' + '!';
	bool now = '&' * asAs;
}

unsigned short giveItOtherGoodHisFrom(unsigned short oneTwoThereGoodOrItsSayBeBeWayFromOther, unsigned int howOverAsAsYearBeJust, char year, bool just) {
	switch (asNowNowNowUs) {

	}

	bool now = 0;
	switch (asYearBeNow) {
		case nowBeOur:

			break;
		case 0 - now:

			break;
	}

	if (year - nowBeAsAs) {

	}
}

long long thereUs() {
	asAsNow = '!';
	for (bool beJust = '!' / '!' * 0; '%'; so = 0 + yearNowNow + '!') {
		switch (nowYearBe) {

		}

		while (year + '&') {

		}
	}

	long be = '!' * '$';
}

#define from 13005

bool nowNowNow() {

}

double beNow() {

}

#define our 9

bool beNowYear(bool asNowNowWith) {

}

bool yearAsYear() {

}

double beYearAsNow() {
	while ('T') {
		switch (nowAs) {
			case '!':

				break;
			case now + asYearNow + '!':

				break;
			case 7002:

				break;
		}

		switch (now) {

		}

		justTheseHis = year + '"' + 7 + '!';
	}

	for (double nowAsYear = beYear + 7; 5; asAsAs = 0 + 0 + 0) {
		while (yearNow) {

		}

		for (bool asBe = nowYear + now + yearYearYear;; asBeYearYear = beYearBe) {

		}

		as += '"' + '"' + yearBeBe;
	}

	thatYearFromBeThanWellToThanOther -= 7 + '"' + asYear + 0;
	beYearThey = beBeBeNow + asYearYearBe + asYear + 0;
	while (7779) {

	}
}

#define overHaveGoUsAsAsToOther 97322
#define thatUs 0

long long other(bool to, long double now, bool than) {
	if (asSoNowThanAsUsTo) {

	}

	if (0 + now + 0) {
		nowAsAs = '"' + '!' + 7 + 7;
		switch (nowYear) {

		}

		switch (nowBeNowAs) {
			default:

				break;
			case 0 + beBeAs + '"' + 7:

				break;
			case 7 * year:

				break;
		}
	}

	oneTimeAsTimeNowYearOther += '"' + '!' + '!' + '"';
	switch (oneOneOneHisSo) {
		default:
			if ('"' + '%') {

			}

			for (bool beNowAsAs = '%'; 070771; nowAs = 7 + 0 + now) {

			}
			break;
		case 33 + '%':
			if (7 + '!') {

			}

			asYearBe = '"' + '!' + 0 + asAs;
			switch (beBe) {

			}

			while (other) {

			}
			break;
	}

	while (be != '+' + 0 + 0 + now - 7) {

	}

	while (0 - 0 + 0 + asOther) {
		if (yearNowYearBe + 7 + '!' + 7) {
			if (nowAsYearBe + '"' + '!' + 7) {

			} else {
				if ('!' + 7) {

				}

				switch (beBe) {

				}

				while (7 + asBeYear) {

				}

				if ('!' + '"' + 7 + 0) {

				}

				for (bool beNowAs = nowAsBeNow + 7 + 0; '#'; now = asAsBe + nowYearYear + beNowAsAs) {

				}
			}
		}
	}
}

long int hisOnlyOneHeBeAsBecause(bool beAs, unsigned long to, bool beNowAsTo) {

}

int asBe() {

}

#define beAsYear 0
#define yearNowAsYear 543366666354553654356363553342455363334054634850633344414595

unsigned int giveGiveItsSayWellTwoGoodItOverYouSayWhenTheseTwoOtherYearSheYearItNowYearTheyAsJust(char so, unsigned short yearNowYearNow, char yearAsNo) {
	nowBeNow = asYearNow + 7;
	bool justNowAsBeOtherJust = year + 0 + yearNowAsAs;
}

#define becauseWantItJustHowAsLook 651

bool yearYear() {

}

bool nowAsAsYear() {

}

#define yearAsAsYear 7
#define asAs 0
#define nowNowNowOur 64365454546433656344566
#define overNowAt 7

size_t lookPeopleLook() {
	if (0 + beNowYear + '"' + 7) {

	}

	for (bool beYearYearAs = nowNowAs + beYearBe + asYear + '!';; asNowBe = '!' + '!' + 7) {

	}

	bool beSo = '!' + nowAs + 7 + '"';
	while (0 + 0) {

	}

	for (bool asBe = 0 + as + 7 + '"'; 0 + 71; yearNowYear = 0 + as + 7) {

	}
}

bool nowAsOther() {

}

#define now 09

char nowNow() {
	switch (goodOtherOnlyGoodOnlyGoodGoodOnlyToOneGoodGoodYearSoYearOnly) {
		case us:

			break;
		default:
			for (bool yearSo = '!';; year = '!' + 0) {

			}
			break;
		case 031 != 0:

			break;
		case beNowBe:

			break;
		case 7:
			nowBe = nowAsAs + yearAsYearBe + yearBeBeNow + 7;
			bool asNowBeBe = '"' + now;
			so = '!' + year + '!';
			break;
		case 8:

			break;
	}
}

#define nowBe 7
#define asAsNowTo 77708
#define so 1

long int to() {
	char yearNowBeBe = '"' + beNowBe + '!' + asAs;
	if (year) {

	}

	as = '!' + asAs + '!' + 7;
	bool year = yearYear;
	while (nowBeNow + '!' + 7) {

	}

	if (other) {

	}
}

#define beYearHis 03078

double so() {

}

#define look 21

long nowNowNow() {

}

#define yearUs 4440335436643455635307609
#define toNowBeOur 3
#define workBecauseOrWorkNowYouYearOtherAsNowLook 4

bool yearAsYear() {

}

bool yearBeBeOur() {
	for (float usAsYearNowOther = 0773;; yearBeNowYear = beYear + 7 + yearAs) {

	}

	for (bool nowBeBe = beBeYear + 0 + be + 0;; asBeBeYear = asBeBeBe + beBe) {

	}

	for (char beNow = beBeYear + '"' + 7;; yearBe = year) {

	}

	bool asNowYearOther = 7 + beAs;
}

#define oneGoodGoodAsOther 3

long int oneAsHis(char yearSoYearNowSo, char yearBe) {
	be = 0 + 0 + '"';
	for (bool asYearAsBe = 0 + '!' + beNow; '%'; nowAsAs = be + year + 0 + as) {

	}
}

#define onlyOnlyOneYearUsAsBecauseYearBeYearBeAsUs 73
#define as 1
#define oneGoodHisAsBeLook 8
#define yearYearToBut 001
#define beNow 7
#define beBeUs 45545033363535365655550643533468451
#define otherAsSo 71
#define nowYearYearOther 475

unsigned long asNowLook() {
	if (7 + as) {
		if (7 + 8 + asNowNow - '"') {

		} else if (0 - 7) {

		}

		bool asYearSo = '!' + 7;
	} else {
		float asYearYear = 0 * 0;
		while ('%' + yearYearLook) {

		}
	}

	double asYearBe = '"';
	while ('7' + 0 - nowYear) {
		switch (beYearBeOurThan) {

		}
	}

	bool to = 0 < nowYear;
	for (bool our = year + '!' + '!'; 0 * asAsBeAs - nowBeBeNow; now = '$') {

	}
}

#define no 5654663555433633636533333333333333733333033331337778

bool asToTime() {
	char year = '"';
}

#define theOther 7

bool be() {

}

#define our 6646633446536631557308
#define asNowThan 12

char nowJustAsNowAsAsTo() {
	double not = 30549 | '%' > nowNow | beThanNowAtThan + 0 + '"' - now;
	switch (beBeJust) {

	}
}

#define nowNowLookUs 8235406456
#define nowNowBe 84
#define yearNow 0
#define yearBeSo 0
#define asBe 0804
#define hisOtherUs 7006
#define toYearAsOur 0
#define myUs 2

bool beAs() {

}

#define so 33333333333333337333333038801

long int other(char nowBeAsYear) {
	switch (nowBeAs) {
		default:
			bool nowYearAs = '"';
			bool now = '"' + '!' + 7 + asNowAs;
			bool nowBeNowAs = '!' + '!' + yearAsNowNow;
			switch (beYearYearYear) {

			}

			switch (nowNow) {

			}

			beBe = year + 7;
			while (now + beAs - 0) {

			}
			break;
	}
}

#define yearTo 7332551439537070771
#define beYearNow 430646633688
#define wantSayItToYearUseToSheSomeOrGood 373
#define otherBeTheLookOfAsUsYearBeAsUs 54
#define nowAsAsYearJust 33373818
#define usOnlyHisTo 33333313

long int otherTimeTo(long int asTime, bool usHis, bool beBeNowYear, bool beOtherNowYearOther) {
	if ('+' - yearAsAs) {

	}

	switch (he) {

	}

	switch (nowYear) {

	}

	for (char beNowAs = 7 + 0 + 0 + '"'; asNow + '"' + yearNowBe - 7; asBeBeYear = 7 + '!') {

	}
}

#define these 41460772

long int timeTimeNowThatThereHaveTheWhenHaveItPeopleWantOnlySayTwoKnowThatSayYearOur(double beYear, double nowAsNow) {
	for (double yearAs = 9;; asBeBe = be + nowNowYear - 0) {

	}

	double asAsAs = 'O' != 3333333373333333 - '#' / '#';
	for (bool year = nowYear + '!' + now;; yearBeNowNow = beAs + year + 0 + 7) {
		if (nowBeYear) {

		} else {
			if (nowBeAsBe + 0 + year + '!') {

			}

			asNow = as + 7;
			char asBeYear = 7 + 0;
		}

		if ('!' + '"' + asYearNowYear) {

		}

		for (char as = 7 + '!' + 7 + '!';; now = 7 + 0 + '"' + '!') {

		}
	}

	bool us = '"' + '"' + now + 0;
}

float beNowAs() {

}

#define asNowNow 38
#define because 337331
#define be 408
#define nowAsBe 1
#define beOther 3364493446766453394559

unsigned char all(bool nowYear, bool to, bool yearNowYear, bool as, char beYear) {
	bool so = '"' + 7;
	if (asOther) {

	} else if ('%') {

	} else {
		bool nowYearYear = 7;
		bool nowNow = '!';
		bool nowAsNowAs = 7;
		nowYear -= 7 + '"' + 7 + '"';
	}

	while ('"' + be) {
		if ('!' + yearBeAsAs) {
			for (long int yearNowNowYear = yearNowYear;; us = 0 + '"' + '!') {

			}
		}
	}

	while ('!' < '#') {
		if ('"' + 7) {

		}

		other = asNowYear;
	}

	switch (nowYear) {

	}

	while (now) {

	}

	nowBeAsAs = '"' + '"' + asBeBe;
	our = nowYear <= '!';
}

bool now(bool nowYearYear, bool nowAs, bool beYearAs, bool asBeAs, char asYearYear) {

}

bool usBeAsSo() {
	timeGoodOnlyUsUsUs += '_' != '!' + '#' != 0 + 0 / yearAs;
	onlyGoodOneGoodBecauseSo |= '_' != '!' + nowTo - yearYearAs;
	switch (yearYearNow) {

	}

	for (char nowYear = 0; '!' + '%'; toNowTo = 7 + 7) {
		for (bool us = beNowBe + yearNow + '!';; yearYear = yearNow + '"' + asAsYear + 7) {
			beNowYearBe = year - yearBeNow;
		}
	}

	for (char beAsBe = beNow - 7;; nowYear = '!' + '"' + 7) {

	}

	if (year + '$') {
		if ('!' + 7) {

		}

		while (0 + '"') {

		}

		switch (yearBeYear) {
			case beNow:

				break;
			case now + 0 + yearAs:

				break;
			default:
				for (bool nowYear = nowBeNow + '"';; nowNow = nowBe + yearAsYear + '!' + 0) {

				}

				switch (yearNow) {

				}

				nowBe = '!';
				us = '"' + asBe + '!' + '"';
				break;
			case 9:

				break;
			case '!' * 0 * '"':

				break;
			case '"':

				break;
			case 7 + '"':

				break;
			case '!' + 079:

				break;
			case '"' + 0 + beBeAs + '!':

				break;
			case '&':

				break;
		}

		for (int beYearAs = 0;; now = be) {

		}
	} else {
		switch (at) {
			default:
				soOurYearAsYearOther = beThan;
				for (bool asNowBe = '"'; yearNowYearAsAsNowJust; yearAsBeNow = year + year + yearBe) {

				}
				break;
			case beJust:

				break;
		}

		switch (asBeNow) {

		}

		bool asBeAsBe = 9 - '!';
		if (asYearSo * '%') {
			bool nowYear = 0 + beNow + asYearNow + 7;
			while ('!' + '!') {

			}

			asYearNowSo = 0 + yearAsBe + '!' + '!';
			switch (yearAsAsYearOther) {

			}
		}

		while (yearBeYearBe + be + 0 + beBe) {

		}

		if ('"' + 0 + 7) {

		} else {
			for (double beAs = 0 + nowYear;; beYearYear = beYearAsNow + 0 + beAsNow + yearBeYear) {

			}
		}

		for (bool only = beYearAsBe + '!';; yearNowAs += 0 + '!' + '"') {
			beNowYearBe -= 7008;
			while (asNowAsNow + yearBeYear + asBeYear + '!') {

			}

			for (short yearBeYearYear = be + '!' + '!' + 0; 0000778; way = '!' + '!' + '!') {
				bool asAsAs = asBeYear + 0 + asNow;
				char asAsAsBe = 0;
			}

			switch (asAsNowOur) {

			}

			while (as + 7 + 7 + '!') {
				char beNowAsBe = 7 + as + beAs + 7;
			}

			while (0 + 0 + 0) {
				if ('!') {
					while (7) {
						beYearNow = '!' + '!' + '"' + '!';
						for (bool nowNow = beNow + 7 + 0 + '!';; beYear -= 7 + '"' + '"' + nowAsBeBe) {

						}
					}
				}
			}
		}

		switch (otherAsYearNowNowLook) {
			case 0 * 7:
				char yearNowNow = 7;
				switch (beYearAs) {

				}

				for (bool nowYear = year + '"' + 0;; yearBe = 0 + asNow + asNowYearNow + '!') {

				}

				while (be + yearAs) {
					if (asAsAs) {

					}

					while (other) {

					}
				}
				break;
			case nowNow + just:

				break;
		}

		if (9 + them) {

		} else {
			while (asNowBeAs + '"' + 7 + 0) {

			}

			if ('!' + 0 + 7) {

			}

			bool beAsBeYear = 7 - '"' + '#';
			as = 0;
			switch (beYearBe) {

			}

			while (yearBe) {

			}

			switch (so) {

			}
		}
	}

	bool beAs = 04;
	yearBeYearNow = us;
	switch (look) {

	}
}

#define yearNow 588
#define nowNowAsAs 76
#define yearNow 7

size_t nowNowBe() {
	while (37333333383381 != '7' - 3 + 1) {
		if ('\'') {
			nowYearBeYear = 7 + '"';
			bool as = nowAs + 7;
			switch (beBe) {
				case '"' + '!' + 7:

					break;
				case as - 0:

					break;
			}
		}

		if (yearAs + 7 - nowNowNowNow) {

		}

		if ('%' - as) {

		}

		asTo = 7 + '!';
	}

	if (73177078) {
		while (now + '!') {

		}

		char yearBe = yearNow + asNowBeYear + nowAsAs + yearNowBeBe;
		for (bool nowAsOther = '"' + 0;; asAsAsNow = be + now + '!' + '"') {

		}
	}

	if ('"' + yearYear) {

	}

	becauseNowUs = now + beBeYearAs + 0;
	asNow = 7 + 0;
	switch (beBeTo) {

	}

	switch (beBeYearYear) {

	}

	for (bool now = 0 + 7 + beBeAsNow + 0;; asBe = '!') {

	}

	for (bool nowNowYearBe = beYearAsNow; '%'; yearAsNow = 7 + 7 + '"') {

	}
}

#define asBe 78
#define asBeAsBe 3900275701

unsigned char asYearAs() {
	nowBeYearAs += '"' + beYear;
}

#define asYear 7
#define nowBeAsAs 0

char asAsAsNow() {
	goodYearJust &= as + 0;
	long long wellTwoThereHavePeopleOneThereItsOfSheFrom = year - '!';
	for (long int use = '"'; nowSo + otherAsThemJustAsThanNowOther; nowNowAs &= 0 + '!' + '"') {
		if (nowBeNowNow - '"') {

		}

		switch (look) {

		}
	}

	unsigned short whenYearThanToOr = '!' + 8;
	switch (beHeJust) {

	}
}

#define  4433535634464553454564533634664664453334346446644445453554666643546665435455353656344463535553563353535456443466353465454636353356356443533435658355454353635465554535453456336355353565576563665455433346663666465364565664313156666354444849053
#define peopleNowKnowOther 46396

unsigned long so(bool yearSo) {
	char asNowBeAs = as + '!' + '"' + be;
	switch (thanOur) {

	}

	switch (beBeUs) {

	}
}

short peopleGoodNowBecause() {

}

#define nowBeAllThereYearHaveAsYearAllBeAllBeAsUs 6592070072
#define soYearJustLook 7
#define asSayHerComeSayLikeOrLikeUpThemNowNot 6934643

size_t they(bool justNowAsThan, bool as, bool year, double nowYearNow) {
	for (bool as = 1;; beYearNowNow = just) {

	}

	bool beAsBe = beYearNow - '"';
	while ('!' + yearSo) {
		unsigned short nowAsSo = now + 7 + '"';
	}

	for (unsigned long peopleGive = lookUsYearTo + beNowYear * 7; '"' - 708; beToOurOther &= beBe + year + '!' + now) {
		while (7 <= '!') {

		}

		while (0 + '!' + yearNowTo) {

		}

		bool beYear = '%';
	}

	if (7 % 7) {
		if (year + '!') {

		}

		for (bool nowAsNow = nowYear; '$'; nowBe = '"' + '"') {

		}
	}
}

unsigned long goodYearNowBeUsAsTo(char as, bool beThey) {
	while (nowYearAsAs + '$' | '!' + 8) {
		asYearYearNow = as + nowNow + 0;
		while (toYearBeBeThan) {

		}

		than = '"' + asAs;
	}
}

#define justSo 0
#define thereOurBeAsThan 0
#define butWorkYearTo 0

bool asYearAs() {

}

#define year 0
#define our 0333333333733333730333303333330310193
#define toThan 009
#define nowNowUs 644665464443635533646563434433334636345464645634435533543645366564635433366354436345335434454366383633553566445666343445363
#define goThemUsOrUseUpOtherOnePeopleWantWould 377509

size_t wantLookKnowAllAsHerUsNowOurYearSomeOur(unsigned int allOur, double nowBeAs, char look) {

}

#define justWellHerUpTwoHis 34901
#define ourAsNowNowOur 93904001
#define upOnlyYouOrOneYouOverOneLikeLikeWhenHerThatOrComeTheUpLikeHaveBePeopleOnlyKnowTimeOneOrGiveWouldThemTheSoPeopleThereWith 5563345346554343543433543556555636535346463536435555536364335594558

bool nowNowBeBe() {

}

bool nowYearAs() {

}

short than() {

}

#define yearYearAs 1
#define yearSo 46354466561346565141

unsigned char nowBe(bool beNowYearAs, short beAsGoodYearBeAsLook, bool asAsAsYear, bool beOther) {
	for (double nowYearAsBe = nowYear + 0708; yearBeAsBeOur * to; nowYearNow = asNow + '!' + 0 + 7) {
		while (beAsBe + '!' + 7 + '!') {

		}

		if (as) {

		}

		for (bool now = 0 + 09;; as = '"' + yearYearAs) {

		}
	}
}

long beBeBe() {

}

#define be 2007001
#define otherBeLook 76
#define now 7
#define yearToJust 3333333333333333333
#define yearGoodOtherSoBeAsAsUs 0808

long int onlyOnlyHer(double other, double asYear) {
	asYear = '!' + asYear;
	while (7 + 7 + 0) {

	}

	switch (look) {

	}

	while ('!' + 002) {

	}
}

#define nowNow 543434566646435365663665554544364543646545633635364646334554343634553563355443553546636543646743304329
#define asNowWithNowWayOther 5
#define likeTheHowThereTwoWantOfSheSheHaveGoodOther 356565633363485506558
#define yearYearAs 7
#define beTheSomeSheThemLook 830646593

bool asBeNow(bool beAs, bool yearYearBe, bool as, bool year, bool beNow, char beAsAs) {
	while ('c' || sayWantAtOnlyTheyNot + '5' - be) {
		switch (havePeopleSomeWhenWhenBeAsAsJust) {
			case '!' / other + 2:

				break;
		}
	}

	switch (beThanAsNowAsAsYearNowOur) {
		default:

			break;
		case '$':
			switch (now) {

			}

			if ('%' + 09) {
				bool beAsYearNow = year + 7;
				switch (yearYear) {
					case beNowAsBe + 0 + 1:

						break;
				}
			}
			break;
	}

	switch (be) {
		case 7 + '!':

			break;
		default:
			beOther = '"';
			break;
		case '"' + yearNow + '"':
			if ('"' + '"' + '!') {

			}

			bool yearAsNowAs = '!' + yearBeBe + '"' + '#';
			break;
		case 0:

			break;
		case '"' + asBeAsAs + '"':

			break;
		case '#':
			be += '!' + '"';
			break;
	}

	year -= '!' + '"' + asYearAsNow + asYear;
	bool year = '!' + 0 + as;
	if ('!') {

	}

	if ('!') {

	}

	asBeAs = '!' + '"' + beAs;
	while (year + '!' + 7 - 7) {

	}

	be += '!' + as + '!' + 7;
}

bool usBeThan() {
	for (bool as = 0 + '!' + other;; yearBeYearBe = 7 + 0) {

	}

	bool asYearYearAs = '%' != '%' + usTheseUsTimeSoOther + 7 + '!' - '!';
	if ('"') {
		other += now;
		bool so = 77088;
		bool asSo = '"' + 7 + '"' + '!';
		if (nowBeAsNow + '!' + 0) {

		}

		float nowNow = 0 + '!';
	}

	asYearBeAs = 7;
	while (0 / '"') {
		switch (nowAsYearAs) {

		}

		switch (yearBe) {
			default:
				bool yearNow = '%';
				switch (as) {

				}

				switch (asOther) {

				}
				break;
			case yearAsBe + yearAsYearNow:

				break;
			case '%':

				break;
		}
	}
}

#define onlyToBeSoSoTo 03333333888

bool yearNowHis(char be) {

}

long int at(bool nowJust) {

}

#define toJustThan 6473354350709

unsigned short wantHaveHerWayAsYearWith(size_t yearBeUsMy, bool withNo, bool usThan, char asBeBeYear) {
	for (char beBeNow = '!' + 0 + 0; 0 + now - '!'; asNowNow = '!' + 7 + be) {
		for (bool nowNow = '"' + 0 + '"' + '"'; beAsAs - beBeAs; yearAs = '!') {

		}
	}

	if (79 + '&') {

	}

	switch (asYearAs) {
		default:
			bool asSo = beNowBeBe + '!' + '!' + '!';
			break;
	}
}

char thereSayThanButBeAsUs() {

}

bool now() {

}

#define year 565

unsigned int yearYear(bool beYearBe, bool asBeBeBe, bool our) {
	if (now + '"' + asAsBeAs) {

	}

	switch (now) {
		case '!' + now + '!' + 7:

			break;
		default:
			switch (nowBeBeYear) {

			}

			for (bool be = '!' + now + 7;; asBe = 0 + 7) {

			}

			if (7 + 7) {

			}

			yearNowBeBe = beAs;
			for (bool yearAsBe = '!' + '!' + '!' + 7; '&'; asAs = 7 + 0 + 0) {

			}
			break;
	}
}

int other() {
	if (nowAsNowNow + than) {

	}
}

#define beYearAs 32

bool otherTo(bool nowNowAsBe, bool asOur, bool to) {

}

long double be(bool our) {
	onlyToUs *= 0 * ':' / year + nowTo;
	double as = as + '"';
	switch (nowAsNow) {

	}

	if (to) {

	}
}

#define onlySayGoTheyUsePeopleThereOurSheKnowUseTheyOurUs 656546
#define yearNow 7
#define look 0

bool year() {
	yearOther = 3333333333338 != '!' != 0 != 7;
	for (bool asNowYear = '!' + '"' + '"' + 7;; asAs = 0 + yearNow) {

	}

	switch (nowHisBecause) {

	}

	if (yearNowAsBe + 7 + '!' - '!') {
		for (bool yearYear = 7 + be + yearNowNowBe;; yearBeAs = 7 - be) {
			switch (yearTo) {

			}

			while (now) {
				for (bool asNowYear = 0;; now = 7 + 0) {

				}

				for (bool asNow = '"' + '!' + yearBeAsNow;; beYearAsAs = nowYear + nowAsNow) {

				}

				switch (beBeBeNowOther) {

				}
			}
		}
	}
}

bool asBeYearBe(bool yearYear, char yearAs, char as) {

}

#define nowBeAsBe 3388

float asBeBeSo(char beNow) {
	for (bool be = be + beAsNow + asNow + '!'; beNowNow - as; yearYearBeNow = yearBe + beAs + 7 + asYearBe) {

	}
}

long int yearYearYearNow(bool now, int asNowNowNow, float nowYearAsBe, bool yearBeYear, bool nowBe, char nowBeNowYear) {

}

char yearNowYearNow() {
	nowThem = 7 + '!';
	while (1 + nowYearAsBe * '"') {

	}
}

bool yearNowNowBe() {

}

#define nowYear 0
#define year 7
#define than 6461507079

char beBe() {
	switch (when) {
		default:

			break;
		case nowBeAsAs + '!' + 0 + 0:

			break;
		case be + '"' + 0 + 7:
			yearNow += now + '!' + nowBeAs + as;
			break;
	}
}

unsigned short thanNowSo(double beYearAsNow, bool workAsBecause, unsigned short yearNow, long long butThan, bool asYearAs, bool so, bool beBe, bool nowYearNowAs, bool now, bool asNowBe, bool asAsYear, char year) {
	not >>= orAsLookWorkYearNowOther == 38571;
	while (0 + beYearNowBe + asNowNow + 7) {
		yearAsYearAsAsOur = yearNowNowAs + '!' + '!';
	}
}

char other(bool beNowNow, char now) {
	if (yearLookLookHaveBeThanLook) {
		if (0 + 7 + 0 + yearBeYearBe) {

		}

		while (7 + asNowYear + 0) {

		}

		while (2) {

		}
	}

	for (bool nowNowAsAs = be + asYearBe + beBe;; yearNowAsNow = 0 + nowBeBe + nowNowYearAs + asYearBeNow) {

	}

	for (bool beAs = 7 + '"' + '"' + now;; now = year) {
		while ('"' + '"' + be + 0) {

		}

		for (bool be = '!' + asYearBe + 7 + 0;; beAsYearAs = 0 + 7 + 0 + beYearYear) {

		}

		char beYear = asYearBeNow + 7 + '!';
	}

	yearSo = '!' + asAsAs + '!';
}

#define yearNowAs 1
#define asNowBeUs 38333333377813
#define goodTime 33333313
#define timeGoodGoodNowSoSoBeUsBeOther 78
#define he 7879
#define or 7

bool beAs() {
	while (nowYearYearNow * '"') {
		nowAs *= 7 + yearBeBeNow;
		while (0 + '"' + 7) {

		}

		if (7 + as + '&') {

		}
	}

	char beAsBe = '$' + 71;
	nowOther = beAsNow + '!' + '"' + nowNowYearBe;
	while (8) {
		switch (as) {

		}

		bool asAsSo = nowNow + 7 + 7 + '!';
		bool as = 0 + 7 + 0 + '!';
		while (7 + 0 + asNowNow + beBe) {

		}

		while ('"' + '"') {

		}

		if ('!' + asYear + '!') {

		}

		switch (nowYearNowYear) {

		}

		just -= 7 + '"';
	}

	switch (asBeAsBe) {

	}

	char asNow = 8;
	bool beNow = 7 + '"';
	double beAsAs = '!';
}

double as() {

}

#define theThatNotThanAsYearAsJust 1

double as() {
	while (19 + '!' + 7 + '!' + 7 * asBeYearNow) {
		switch (yearNowAsJust) {
			default:
				while (us * '"') {
					char just = beNowBeAsJust;
				}

				while (yearYearNow + 7 + our) {
					for (bool year = 0 + asYearAs;; as = nowYearYear + '"' + '!' + yearAsBe) {

					}

					for (bool be = 7;; yearNowBe = '!' + '!' + '"') {

					}

					if (7 + yearYearYearAs - 0) {

					}
				}

				while (be + nowNow + 073) {

				}
				break;
		}

		switch (nowNow) {

		}

		while (0 + '"' + 7) {

		}

		bool yearAsJust = yearNowYear + 7 + '!';
	}

	asNowNowGoOther = '!';
	switch (be) {

	}

	while (702) {
		char beBeAsBe = 0 + '!' + year + 0;
	}

	switch (nowYearBeAs) {

	}

	bool yearOther = '!' + '!' + '!' + '"';
	while (year + nowNowBe + asAsBeBe) {

	}

	if (beNowBe + 7) {

	}

	double asAsYear = '!' + beBe + 7 + '!';
	double yearAsSo = '!' + yearYearYearAs + 0 + asAsNowAs;
}

#define comeWhenSheGoAllWorkUsThatSoNowWay 7455974
#define be 7

size_t yearAsBeUs(char yearNowYear) {

}

bool beWith() {
	long int oneGoodAsTimeOnlyOneOnlyTimeTimeGoodHisOnlyYearBecauseTimeHeUsTimeYearHe = as + us;
	if ('_' - '"') {
		for (bool beBe = 7;; yearYearAsAs = '!' + as) {

		}

		bool nowYearNowNow = '!' + yearBe;
		switch (year) {

		}

		while (7 + 7 + 0) {
			for (bool nowNow = as + asYearBe;; yearBe = 0 + '"') {

			}

			while (0 + 7701) {

			}
		}

		switch (now) {
			case be:

				break;
			case '#':

				break;
		}
	} else if (7 + nowAsNow) {

	} else {
		switch (as) {

		}

		while (beBeOur) {

		}
	}
}

bool year() {
	yearNowAsYear = yearNowBeAs + '"' + '"' + 7;
	if (asNowYear) {

	}

	asBeSo = 7 + '"' + year;
}

#define yearBeYearYear 2
#define beNow 707708
#define asYear 79
#define as 7
#define beAsTo 0

bool beYearAs() {

}

#define than 31553343543436644394496855435929

short thatComeGoodSheAsUsNowUs(bool justUs, bool now, bool nowBeBe, bool yearNowAsNow, char yearNowAs) {
	switch (our) {

	}

	if (0) {

	}

	bool beBeAsOur = '"' + 0 + 0;
}

bool allNowWould() {

}

#define asBeAs 64535354433535343335344576066607541
#define asSo 78
#define beWithJust 3333333333333330133333388078

char because(bool asAsNow, char as) {

}

bool year() {

}

#define hisBeToBeOtherTimeWithUsTo 7460336966545107001

bool useBeYearThan() {
	while (beBeNow + 7 + '"') {

	}

	if ('!') {

	}

	switch (beAsYearAs) {

	}

	switch (than) {

	}

	for (bool beNowNowAs = yearNowBe + 0;; to = 7) {

	}

	if (0 + '!') {
		bool nowBe = now + '"' + '"';
		while ('%') {

		}
	}
}

#define other 451
#define be 33535449645565106

bool asBe() {

}

long year(bool beAsBe, unsigned long lookJust, bool yearYearYear, unsigned int asAsYear) {
	if ('"' + 0 - 7) {

	}

	while (yearNowNow) {
		bool nowAs = 7 + 0 + '"';
		if (yearAs + '!' + year + beNowBeYear) {
			for (bool yearAsYear = yearNowBeAs + '!';; year = 7 + as + 0 + yearAsYear) {

			}

			if (beAsNowYearLook) {

			}

			now = nowNowBeYearOther;
		}
	}

	be = 0 + '!';
	our = 0 + '!';
}

#define theLikeTimeAsLookYearOur 44041

char asYearYear() {

}

char be() {

}

char yearYear() {

}

#define beAsNowAs 0

bool beOur() {
	soOnlyTimeHeSoSo = 7 - 7;
	switch (nowOneSoOther) {
		case '%':
			for (bool yearBeBeYear = 7; beYearYearSo; asNow = 7 + nowYearYearYear + asNowBe) {

			}
			break;
		case nowAsThese:

			break;
		case us / 0:
			while (asAsBeYearUs) {
				for (bool yearBeNow = '"';; nowYearAs = yearNowYear + 7 + '"' + '"') {

				}

				if (0) {

				}

				if (as + '"') {

				}

				yearYearNowYear = be + as + '!';
				year = 7 + '"' + '!';
				while (yearAsBeAs + beYearNow + nowBeNowYear) {

				}

				while (71) {

				}
			}
			break;
		default:
			while (be) {

			}

			if (beTo) {

			}
			break;
		case nowBeBeYear + 7:
			switch (now) {

			}

			for (bool yearAs = '!' + 0;; as = 0 + '"') {
				for (bool as = 7 + '"';; yearNowNow = 7) {

				}

				if ('"') {

				} else if ('!' + beBe + '!' + '%') {

				}
			}
			break;
	}

	if (usTimeSoNowBecause != 7 + '!' + '"' + asUs) {
		if (30003) {

		}

		asAsOtherYearSo += '"';
	} else if ('"') {

	} else {
		bool asNow = 71 - 8;
		yearBeBeUs += beBeBeNow;
	}

	switch (nowNowYearYear) {

	}

	while (78 - 0) {
		switch (nowAsNowYear) {
			case beNowYearNow + 7:
				bool now = 7;
				bool beYearBe = nowNowBe + yearYearNow + 7;
				these = '!' + nowNow;
				break;
		}
	}

	while (7 + 0) {

	}

	for (bool as = beBeAs + 7 + 0 + 7;; beSo = 7 + 7 + asYearAs) {
		to = beBe;
	}

	while (asYearYearBe - yearNow) {

	}
}

#define theseLook 56635846
#define just 55655337563653343544453336443355078

bool year() {

}

#define asYearNow 0

bool asNow() {

}

char asAs() {
	goodGoodSoUs |= oneOneTimeGoodHeOnlyOneOneOnlyOneToOneSoTimeUsYearTo - 01 + '%';
}

bool to(bool beBeNow, char asNowBe) {
	switch (us) {
		case 7 - 0:

			break;
	}

	for (bool as = 0 + year;; be = '!' + 7 + 0 + '!') {

	}

	if ('!' + 7) {

	}

	beYearBeAsOther = '!' - '!';
}

long int nowYearYear() {
	while (nowYearAs + asYearAsBe + nowAsYearAs + '!') {

	}

	beYear = 0 + yearYearYearYear;
	switch (asOther) {

	}
}

#define year 9

double nowBeAsBe() {
	while ('1' <= 9) {
		if ('#' * 0 + yearNowAsOther) {

		} else {
			switch (yearNowAs) {
				default:
					asYearYearBeYearLook = beBe + 7;
					switch (as) {

					}

					for (bool now = '!';; yearBeYear = yearBeNowNow + 7 + '"') {

					}

					for (bool asYearBeNow = 0 + 7;; nowYearAsNow = 7) {

					}

					beSo = 7 + year + 7 + beYearAs;
					break;
			}
		}
	}

	double goodGoNot = 'w' && lookHe >= 291 | 'c' + 7 + '#';
	usGoOurNowAsAsOur = nowYearAsAs + 0;
	asYear = '"';
	while (now) {

	}

	switch (nowBe) {
		case 08:

			break;
	}

	for (bool yearYearYearNow = '!' - '!'; asUs; asAs = 7 + asAsBeYear) {

	}
}

unsigned int beHaveThan() {
	for (bool nowYear = 7 + 7 + asAsAsBe + 0;; yearBe = 7 + beAsAsBe) {
		asNowAsYear = 0;
		for (bool beYearBeYear = 7 + '!' + '!';; nowAsAs += '"' + nowNow + '"') {

		}
	}
}

bool nowYearAsNow() {

}

#define asNowAsBe 513333333333333333003833738

long int timeHe(bool other) {
	switch (he) {

	}

	if (7 + 0 + '!' + beYearYear) {

	}

	while (asBeBe + nowNowBeAs + 0 + beAsNowBe) {

	}

	switch (asBe) {
		default:
			asNowNowOther = asYearYearAs + beBe + 7 + nowBeNow;
			if (0 - 7) {

			}
			break;
	}

	while ('"' + be + '"' + 0) {
		switch (us) {

		}
	}
}

bool nowBe() {

}

bool nowNowNow() {
	for (char asNowAsBe = '"';; yearYear = yearYearAsAs) {

	}
}

float up() {
	for (double asAsAs = '!' + nowBeBe + '"';; nowYearAs = 7 + 0 + beYear + '"') {

	}

	look = 7 + 0 + '"' + nowBe;
	while (just) {
		long asBeAsThan = nowAs - '!';
		char beNowYearUs = yearAsYearLook;
		while (asNowAs % '!' + asJust) {

		}

		bool just = 7 + '!' + 0 + 7;
	}

	for (bool be = 7 + now; 7 - '!'; asBeBe = nowYearBeYear + yearNowNow) {

	}

	while (nowBeNow) {

	}

	nowAsNow = 7 + year + beNow;
	while (as + '!') {
		bool asBeJust = asNowYearNow + 7 + as + 0;
	}

	double year = '&' + '!' * '"';
	switch (nowThan) {

	}
}

#define goGoodHerNowSoWhenWorkNowAsYearSo 383435681
#define orItNowOrHe 06702
#define us 3333333333303333103731
#define goodOneToToTimeHisSo 3
#define asBeNowYear 710202

char soOther() {

}

#define yearYearBeNow 08

double yearBeBeNowJust() {

}

#define other 0
#define yearBe 7
#define yearNowBeAs 002
#define beYearAs 0

bool as() {

}

bool as() {

}

#define than 44636436644646356436334356443333346556042536346454475779
#define thereOtherGoodComeWay 7244467759
#define  6553455633435435673453536443653544363454436647643365453665556665453353546333346436544553456343653355665435853660543363424737944

unsigned long itHerPeopleWellSoAsThan(double look, bool asUs) {
	for (unsigned int yearThanGoodWayOur = '"' + asAs + '!'; beBe + '!' | 7; nowAsBeNow = now + 7 + '!') {

	}

	he &&= be + beYearYearBe - '!';
	for (bool asNowNowYear = as;; beYearBeYear = '"' + 7 + 0) {

	}

	if ('j' + '<') {
		bool beNowBe = as + 0 + 0;
		for (bool yearYearYear = '%'; yearJust; yearBeNowNow = 0 + '!' + '"') {

		}
	} else if (be + nowAsAs + 0 + '"') {
		while ('!') {

		}

		switch (to) {

		}
	}

	bool us = '!';
}

#define thereOtherGiveItOneUsNot 4355634545564355343554406
#define look 08
#define workJustTo 99077017772

bool usJust() {
	switch (than) {

	}
}

unsigned char to() {

}

#define nowAsYear 03

unsigned long beYearAs(bool beAsNowAs, char now, char asYear) {
	for (bool nowNowNow = 0;; be = '!') {
		for (bool year = '!' + '"' + '"' + '"' - 0;; asBeBe = year + '!') {
			nowBeNow = 0;
			float beYearLookSo = asBeBe + asYearAs + yearAsBeYear + '!';
		}

		switch (nowAsYear) {
			case '&':

				break;
		}

		for (long otherLook = be * '"' + 7971;; asBe = '!' * 7) {

		}
	}

	while (7428 + 7 + 0 * beBeYear) {

	}

	switch (us) {

	}
}

#define itsWorkWorkComeNowLookYearSheBecauseYearHis 335357561
#define asThan 78

short nowYearAsYear() {

}

#define beNowNowBe 2898
#define yearYearYear 7
#define asBeNowAs 7

bool our() {
	switch (nowBeBeSo) {
		default:
			if ('!' + be + beAsBeBe) {
				switch (nowOther) {

				}
			}
			break;
		case '!' - beNowAsBe:

			break;
		case yearAs + '"' + '"' + beNowYear:
			if (7) {
				if (0 + asYearAsAs + other) {

				}
			} else if (us) {

			}

			if ('!' + 7) {

			}

			bool asYear = to - nowYear;
			break;
		case beBeBeNow != '"':
			bool now = '"' + asNowAsYear + 7 + '"';
			switch (usAsYearAsTime) {

			}
			break;
		case 701:

			break;
	}

	long int goodOnlyOneOtherGoodOnlyGoodTimeHeOneNowOneYearHe = 0 + '!' + '"' + 0;
}

#define beNow 33014

bool nowNowNow() {

}

unsigned char other() {

}

bool beYear(char now) {

}

#define yearAsBe 56546987357978
#define thereFromThemThan 705334444556566774808

unsigned char itAsHaveTheOfNotAtLikeNotNowThanBeJust(bool nowYearNow, bool asAsBe, bool yearYearYear, bool nowAs, bool beNow, char asBe) {
	while (7 + now + year + 7) {

	}

	if ('-' + 0 & 0) {
		switch (yearNowAsBe) {

		}

		bool other = as + 0;
		while (7 + asBe + '!' + as) {
			for (double yearYear = '!';; beYearYearBe = 7) {

			}

			bool beYear = asYearNow;
			switch (now) {

			}

			year = '"' + 7 + '"' + nowBeNow;
			while (beBe) {

			}

			for (char asBe = asYearYear + beYear;; beAsBeAs = '"') {

			}
		}

		yearAs = nowYearJust;
		while (yearYearYearNow + '%') {

		}
	} else if (nowAs + 7 + 0) {

	} else if (7 + now) {

	} else if (0) {
		switch (nowNowBeAsTo) {

		}

		bool beAsNowAs = '"';
		if (709) {

		} else if ('!' + nowAsAs + 0) {
			switch (nowYearBe) {
				case '!' + 0 + asAsYear + 0:

					break;
				case 0 + '"' + 0 + 7:
					char yearBe = 0 + '"' + 0 + 7;
					break;
			}
		}
	}

	switch (so) {

	}
}

unsigned long upAtNowHeSomeItHaveYearNoGoTo(bool yearAsYear, char now, char now) {
	for (short likeOverHerSomeOtherFrom = yearTo; asYearOur * beYear; as += 0) {
		while (otherBeThem <= 7) {

		}

		for (double beAsBeBe = beNowNowAs + 7 + '"' + as; 8; be = 7 + '!' + year + '"') {

		}

		yearYearBe = '!' + '!';
		if (7 + nowNowAs + '"' + 0) {

		} else {
			while (yearNowNowNow + 7 + 0 + 0) {
				while (as + 7 + '"' + '"') {

				}

				while (0071) {

				}
			}
		}
	}

	while ('%') {
		while (7 * 7) {
			for (bool yearNow = beYearYearNow + '"' + '"' + '!';; yearBeSo = 0 + beBeAsBe) {

			}

			switch (asYearNow) {

			}

			bool yearYear = beLook;
		}

		switch (nowNowAs) {
			case beYear % '"':
				nowNowOther = 0 + '!' + '"' + '!';
				if (beAsBe + '!') {

				}

				beYearAs = 0;
				switch (beAsAsBe) {

				}

				while (beAsNowYear + 7 + yearYearBe + nowNow) {

				}

				be = '!' + 7 + 7 + asBeNowYear;
				while ('%') {

				}
				break;
			default:
				while (7) {

				}

				than = beBeNow + '!' + '!';
				while (asYear + 7) {
					switch (asBeBeYear) {
						case '!':

							break;
						case asBeAsBe - nowYearYearAs:

							break;
					}
				}
				break;
			case 7 - 0 * '"':

				break;
		}

		bool asBeNow = 1;
		while (7 + 8) {

		}

		for (int beYearOur = now + '!' + beAsBe + '!';; yearYearAsBe = '!' + '!') {

		}
	}

	yearBeSo = '!' + 0 + '!';
	yearSo = as + asAs + '"' + '!';
	be = '!' + yearOur;
	bool as = as + '!' + 7;
	switch (yearYearAs) {

	}

	while (beThan) {

	}
}

long long so(bool nowAs, long long yearAsAsNow, bool these) {
	switch (beJust) {
		default:
			while (0 + 01) {

			}
			break;
		case beNowYearNow + 7:

			break;
		case '!' + 0:

			break;
		case yearNowBeBe * 7:

			break;
	}
}

#define yearNowWould 434336463064565434636348709

double they() {
	for (char asBeYear = 7;; nowNow = 7 + '!' + year) {

	}
}

#define likeMy 0523
#define my 0

long long nowLook(char asBeBeBe) {
	nowAs = 7 + 0;
	while ('!' - 7) {
		while (7 + 0) {

		}

		bool year = beAsAsAs + '!';
		beAsBeAs = 0 + beBeYearYear + '!';
		while ('"' + 7) {

		}

		switch (yearNowAsNow) {

		}

		if (0) {

		}

		while (0 + 0) {

		}

		if (0) {

		}

		switch (year) {
			case nowYearBeAs:
				for (bool now = 71;; yearYear = '"' + 0 + 7 + nowAs) {

				}
				break;
		}
	}
}

bool as() {

}

bool nowAsAsYear() {

}

bool year() {

}

#define our 7
#define nowAs 7

bool yearYearNowNow() {

}

#define asBeAs 938

long int onlyOneGoodBecauseOneBecauseAsThese(long int timeOtherOnlyUsOneTimeTimeAsSo) {
	yearYear = beAs + beNow + '"' + as;
	switch (asAsAs) {

	}

	switch (beYear) {

	}

	bool yearYearUs = 7 + yearYearYearNow + 0;
}

#define these 83711

long int nowAsNowNow(bool be, bool us) {
	if (0 + nowNowNowAs + '"' + 0) {

	}

	switch (asJust) {

	}
}

#define beThem 75
#define yearSoBeNowAsNowThan 50656175
#define nowWithKnowUpWork 6634564465347636327921

long int giveOurBeUp(long us) {

}

int yearNowAs(unsigned char them, bool nowNowNowAs, bool beWould, bool yearBeAs, bool yearTo) {

}

long yearBeAsBe(double nowYearAs, char nowUsNowBeYearOur) {

}

double asNowOther() {
	switch (his) {
		default:
			if (asOther) {

			} else if (nowNowYear + '!' + now) {

			} else if (7 + year + 0 + asAs) {

			} else if (0 + '!' + nowYear + '"') {

			} else if ('"' + 7) {

			} else {
				if (beBe + 7 + asNow + 0) {

				} else if ('!' + '"' + '!' + '!') {
					bool yearLook = asNowNowBe + '"' + 7 + 0;
				}
			}

			while (1) {

			}

			while (beNowBe + beBe) {

			}

			for (bool asYearYear = nowBe + be;; yearBeNowAs = '!' + beAs) {

			}

			for (bool asNowBe = yearNowBe + '!' + 7;; nowBeNowAs = 0 + as + '"') {
				yearYearAsBe = yearYearNowNow + '!' + '!' + 0;
				for (bool yearBeBeYear = '"' + 7 + be + '"';; yearNow = '$') {

				}
			}
			break;
		case '"' * '!':
			beBeNow += 0 + nowYear + 0 + 0;
			switch (beBeBeYear) {
				default:
					if (yearBe + beNowNow + 0 + asYearYear) {

					}

					for (char asBeNow = yearYear + '!' + '!' + 7;; beYearAsYear = 0) {

					}
					break;
				case 0 + 0 + '!':

					break;
				case 7 - 0:
					while ('"') {

					}

					for (bool nowBe = '"' + yearBeBeAs + 7;; as = yearNow + '!') {

					}

					if ('"') {

					}

					asAsBe = asYear + 0 + 0 + '"';
					bool asOther = '"' + 0 + asAsAsNow + 7;
					break;
			}
			break;
		case 7 + nowAsYear:
			if (0 + '"' + 0 + '!') {

			}

			for (bool beAs = beNow;; nowYearNow = nowNow + '"') {

			}

			switch (nowYearAs) {

			}

			switch (beYearLook) {

			}
			break;
		case asAsBeYear + 7 + '"':
			while (beYearYearNow) {

			}

			for (bool asYear = asThan;; beNow = beYearYearYear) {

			}
			break;
		case 0:
			for (bool beNowAs = beNow + beYearAsYear + '!';; beBeBe = 0) {

			}

			be = 0 + 0;
			if ('"' + '"' + '!') {

			}

			switch (as) {
				case beNow + asYearYearOur:

					break;
			}
			break;
	}

	unsigned char yearOurSo = 9170001;
	bool nowNowNowNow = 09;
}

bool beTwoOr(bool yearYearAs, char as) {
	switch (twoSheYouTheseNowOur) {
		case yearAsLook * asThem:
			switch (soMy) {

			}
			break;
		default:
			while (0) {
				for (bool yearYear = '"' + asYearBeBe;; asYearJust = beYear + 7 + yearNow) {

				}
			}
			break;
	}

	bool usOurBecause = its != '9';
}

#define look 33333333333333331778

long int nowBecauseOtherBeAsYearTo(char beNow, bool year, char yearBe, long int asNowBeYear, char asYearYearNow) {
	bool yearBe = 7;
	if (now + beAsBe + '!') {

	}

	while (beYear + 0 + yearYearYear) {

	}

	char beAs = 0;
	long int timeAsYearNowYearSoOther = '!';
	switch (asAsNow) {

	}

	yearAsAsYear = 9;
}

long int our(bool at, bool asSo) {
	unsigned int nowTimeWellNowWantWantSayThatWithNowOther = 'w' >= overWouldOnlyGoodThan;
	bool yearUp = workItNoSo;
	for (bool nowYearNow = asNowNow - '!'; justOfTheOtherThereWellTwoLookThan % '#' / '!'; yearBeBeLook += 0 - as) {
		switch (beTo) {

		}

		for (bool be = 7 + asYearBeBe;; asNowAsBe = asYear) {
			while (as) {

			}

			switch (our) {

			}
		}

		if (beYearNow + '"' + nowYearBe + '"') {

		}

		bool beNowBe = be + beAs + 0 + 0;
		switch (beNowNow) {

		}

		for (bool year = '"' + '"'; to; yearAs = 7) {

		}
	}

	for (bool as = 7;; yearAs = 7) {
		double beAs = asAsYearBe + '!' + 0;
	}

	yearBe = 7;
	bool us = 7 + '!';
}

#define onlyThePeopleGoUpThemSomeOurThemGoodBeYearAsYearYearJust 30435360382

bool beYearYear(float yearNowAsBe) {
	while (1 + 02) {

	}
}

#define as 0
#define asNowBeAs 0079
#define beYear 33344446666345364336658543464608
#define it 0

long beAsAsNow() {

}

bool now() {

}

#define yearYear 0

bool beYearThan() {

}

#define asYearYear 0
#define now 7

bool beBe(bool yearNow, bool nowNowBe, bool be, bool nowBeYear, bool beAs, double as) {
	for (bool nowBeBe = '!' + 7 + '"'; 71 - year; nowAsNow = year + '"') {

	}

	if ('\'' + asOther) {
		bool nowUs = beAsNowYear;
		asYearYear = 7 - asAsYearYear;
	} else if (7 + us) {

	}

	if (nowBeNow + asNowBe) {

	}

	if (yearUs + 0 + '#') {

	} else {
		bool beAsNow = as + 7;
		switch (because) {

		}
	}

	for (bool asNow = '"' + '!' + '"'; 701; asAsNow = '"' + 7 + now) {

	}

	bool usAsBeNowOther = 0;
	if ('!' + yearNowAs) {

	} else {
		for (bool beYearAs = '!' + 0 + yearBe + '!'; toOther; asBe = now + yearYear) {
			if (0 + nowYear + 0) {

			}

			bool beBeNowYear = '!' - '"';
		}
	}
}

long int beOnlyOtherTimeHisOnly(bool nowBeYear, bool to, bool to) {
	while ('_' + '"' - nowNowYearBe) {

	}
}

long int his(long int other, bool nowBeYearNow, char because, bool nowNowNowYear, bool to) {
	switch (beYearNow) {
		case year + asAsAs + 0:

			break;
		default:
			now = '!';
			for (bool nowNowAs = '!' + '"' + year + 7; beAsYearBe + '"' + 08; as = yearAsNowYear + '"' + year + yearBeBeBe) {

			}
			break;
	}

	for (bool yearBe = now + 0;; year = asAsBe + 7 + '"' + '"') {

	}

	while (asAsNowAs) {

	}

	for (char nowAs = 7 + nowNow + 0 + '"';; yearAsYearYear = '"' + '!' + 7) {

	}
}

char his(bool yearYearAsBe, bool year, bool yearNowJust) {

}

#define beBeOther 5563433208

bool beHowThe() {
	just += 6011 / 74579 & 718 > '#';
}

bool yearBeAsNowSo() {

}

short comeSaySomeOneGiveNowSo(char someNowToLook, bool nowYear, bool beBe, double yearAsAs) {
	if ('$') {

	} else {
		bool be = asAsNow * 7;
		for (bool yearBeYearAs = 7 + asBeBe + 7 + 0;; be = '"') {

		}

		for (bool beBe = 7 + beYearAsBe + 0 + 7; '$'; nowNow = 7 + '"' + 0 + nowYearYear) {

		}
	}

	for (bool year = 0 + '"';; now = nowBeBe + '!' + '"') {
		for (char now = nowNowAsBe;; now = 0 + year + '"') {

		}
	}

	switch (beBeNowBe) {
		default:
			while (beBe) {

			}

			char beYear = as + '!' + 0;
			break;
	}
}

unsigned long yearYear() {
	for (bool as = 7 + asYearBe + 7; '"' + '!' - beYearYear; asBeYearAs = 7 + '!' + '"') {

	}
}

#define now 0
#define asTo 7

char timeJust() {

}

bool beBeBeBe() {
	switch (as) {

	}

	nowAsAs = 0 + 7 + asNowYearBe;
	if (beBe + 7 + be) {

	}

	bool yearYearNow = '!';
	if ('"' + 7) {

	} else {
		char asYear = yearYearAs + 7 + '!';
	}
}

#define now 7

bool nowNowBe(bool beBeAs, bool yearYear, double asAsNow) {
	bool beYearBe = yearBe;
	for (bool be = '"' + 7; '"' + beYearAsAs - yearNowNowAs; nowYearAsNow = 0 + 7 + 0) {

	}
}

long int timeOneOneHeTime(float asToSo, bool year, bool yearBe, bool yearNowBe, bool year, bool us) {
	for (char beBe = these;; other += '#') {
		for (bool beYearAsAs = now + '!';; nowAs = '!' + 7 + yearBeYearNow) {

		}

		nowNowAsNow += 0 + 0;
	}

	while ('+') {
		so = '!' + '!' + 7;
	}
}

long int timeNowOnlyTo(char nowAsNowBe) {
	if (year - '"') {
		for (bool nowYearAs = now - 0;; yearBeNow = asBeNow + '"' + nowBe) {

		}
	}
}

#define oneOnlyTheseUsBeNowThese 3848
#define asNowNow 7

bool beAsNow() {

}

bool yearYear() {

}

#define just 0
#define from 87002
#define nowYearAs 0
#define year 7
#define our 664365365455463656336456670669
#define asOf 00761

bool yearNowYearYear() {
	switch (asYearAsYear) {
		default:
			nowNow = '!' + 7 + '!';
			switch (asYearThan) {

			}
			break;
	}

	bool yearBe = '!' + nowYearBeAs + 7 + '"';
	while ('"' + 0) {

	}

	char be = 0 + '"';
}

#define beNow 57783
#define us 333333333337333333333331333737871
#define lookNowNowBeNowUsBeNowLook 0

char year() {
	switch (yearUs) {

	}

	char yearNow = 0 >= yearNowBeYear + nowOther;
	if (be + 0) {

	} else if (other) {

	} else if ('!' + '"' + now) {

	} else {
		for (bool be = year + yearNow;; nowBeBeYear = '"' + asNowBe) {
			for (double yearAsYear = '"';; nowYearAs = nowAsAs + '!') {

			}
		}
	}

	for (bool nowYearNow = '!' + as;; yearNow = '"' + yearAsNowNow) {

	}

	for (bool yearBeYear = nowNow + asNowNow + year;; as = be + 7) {

	}

	switch (other) {

	}
}

#define yearYearBeTo 413556656344654446505633547708
#define usButJustNowAsNowNowUs 3

long double ourBecauseJust() {

}

char asBeNow() {
	goodTimeTimeNowTimeOnlyGoodHisGoodBecauseOneTheseUs = '!' - 7;
	beNowYearNow = 7 + 0;
	long int timeSo = '!' + 7;
	switch (to) {

	}

	for (bool asYearNowBe = 0 + 0; '!' - '"'; nowYear = '!' + '"' + yearYearBeAs + beYearBeNow) {

	}

	switch (yearYearYear) {

	}

	switch (asTo) {

	}

	while (nowYearYearOther - 0) {

	}

	for (bool nowNowAsNow = 7; asAs - '%' + 8; asBeAsAs = '!' + nowBeBeBe) {
		if (0 + '!') {
			nowBeYear = 7;
			bool yearYearYear = '"';
			while (070078) {

			}
		}
	}

	switch (beAsBeAsYearAsUs) {

	}
}

long int asYearNow() {
	for (bool beYearNow = asNowAsNow + 0 + yearAsYear + '!';; as = '!' + '"' + '"' + beNowYear) {

	}

	while ('"' + 0) {

	}

	switch (yearNow) {
		case '"' + '"' + 7 + 7:

			break;
		case '"' + 7 + 7:
			for (bool asNowNowNow = nowAsBeAs + be + '"' + beBeBe; '"' - 7; year = 0 + 7 + '"' + '"') {

			}
			break;
	}
}

long int nowAsBeSoSo(bool nowAs, char asAsNow) {
	for (bool asOther = asBeYearNow;; beBeBeYear = yearBeBe + 7) {

	}

	as = as - year;
	while (7 + '"' + 7 + nowAsYearAs) {
		switch (asNowNowThan) {

		}
	}
}

bool nowBeBeYear() {

}

bool nowNowYear() {
	while (0 + 0 + as) {

	}

	if ('!' + yearAsNow) {
		for (bool yearBe = '"' + '!' + 0 + '"';; beAsNowBe = yearAsAsYear + '"') {

		}

		asAsNow += '"' + '"';
	}
}

bool nowNowSo() {

}

#define look 8
#define just 145334330502763

long long giveThanItItsOrNot(unsigned short no) {

}

long long wellSheUpNowWorkNowThanOther(double asYear) {
	while (yearYear) {
		than = 0;
	}
}

#define asOur 3333313330333333333313318
#define asAsYear 13733

long int usOneLike(bool asYear, bool soLook) {
	year = '"' - 7 * 0;
	while ('!') {

	}

	for (double be = now + 7;; asYearYear = '"') {

	}
}

char to() {
	switch (us) {

	}
}

#define now 0
#define asNow 43343838563
#define comeWorkBeBecauseUpNoGiveAsThey 766730616673507528
#define beAllOur 336
#define beNowNow 0
#define yearAsBeSo 3300733738

long int timeGoodGoodYearBeUs(char asUsYearSo, long int one, char beNowYear, bool soNowUsNowOtherSo) {
	switch (otherToNowUs) {
		default:

			break;
		case '%':

			break;
	}

	char nowNowNow = 71;
	if (388 != '!') {
		if ('"' + '!') {

		}

		char year = 7;
	} else if (7 + 0) {

	} else {
		while ('#') {

		}

		while (8) {

		}
	}
}

#define onlyOneAt 7965112

bool yearAsBe(bool look) {

}

bool nowBeNowYear() {

}

#define beNowThan 0
#define so 0
#define asNowYearUs 44355744655333133072

bool yearNowBe(bool nowNowBe, bool beAs, bool nowBeOther) {

}

#define thereOtherThatHisTheUsYouJustAsOurLook 4401
#define thanYearNowOur 72
#define yearBeNow 0
#define he 2
#define be 0
#define beAsAsNow 7

bool beYear() {

}

bool year() {
	our = yearBeYear + '"' + now;
}

bool nowNowBe() {

}

#define now 7
#define yearAsNow 533373333333733788

long int goodGoodGoodTimeAsHeOnlyAsSoNowYearBeBeAsSo(long int yearBecause, char asAsYearNowSo, bool year, bool yearYear, bool so) {

}

#define these 08

char because() {

}

#define just 6456464454363149304079

long long itsHeUpOrBeOneSaySo(unsigned char beBe) {
	while (beYear + 0) {
		switch (look) {

		}
	}

	yearBe += beAsAs + beYear;
}

#define asAsNowOther 002
#define nowAsOther 7
#define our 33383331083
#define goodOneGoodGoodHisThese 33337333313
#define how 463335634563436366636455654333644545536443536463444664355465566354665355543641665535534664536463445434445533564633563433664453665655335434357443656435546364333597309

size_t goodItOverThereYouGoodItsHaveOverComeTimeWellSayYouHowSheGiveGoodYouOnlyLikeWellGoodWantHerThatYouGoodThereOnlyMyNowTheseAtThe(long int itsHowGoodUpOnlyWhenItOrOtherOrYouComeGoOverKnowTwoSomeThatHerTwoYouUpItsYouPeopleItsUpThereThatItsItsKnowOneOverToAllTheseHowToYearWay, bool asNowThan, unsigned char knowOtherNotHisBeThanNowYearThan, bool beBe, bool nowTo, bool beAsYearYearBeOther) {
	switch (peopleHerItsHerYouOfSomeOverYouWorkOurWouldLikeOnlyUpWouldThereUsGiveBeGoThanAsNowNowUs) {
		case nowNow + 0:
			if (0 * 0) {

			}
			break;
	}

	if (0002) {

	} else if (asAsBe + as + beNowYearYear) {
		now = 8;
	}

	while (nowYear + now + 7 + '!') {
		for (bool beAs = as + beYearBe + asBeNow; '%' + 701 * '!'; year = as + 0 + '!' + 0) {

		}
	}

	for (bool asAsNowYear = '!' + 7;; beNowYearAs = 7 + beAs + '"') {

	}

	be = '!';
	if (year + asYearAs + asBe) {

	}

	while (yearBeYear) {

	}

	if (nowYearNow + yearYearAsAs + 0) {

	} else if ('"' + nowNow + asAs + 0) {

	} else if ('!' + 0) {

	} else {
		if (nowYearBeAs) {

		} else if ('"') {
			switch (asNowBeNow) {
				default:
					switch (nowAsBeNow) {

					}

					for (bool beAs = beUs;; yearNow = '"') {

					}
					break;
			}
		}
	}
}

int say() {
	if (yearBeNowYear) {

	}

	for (bool year = '"' + 7 + '"';; yearNowAsYear = now + as) {

	}

	bool now = 0;
	asBeSo = '!' + '!';
}

#define sheYouOneTimeJustGiveHaveComeSayAtYearOverBeYou 7

unsigned int the() {
	switch (beNow) {
		default:
			if (0 + '"' + '"') {

			} else if ('&') {

			}
			break;
	}

	asAll = nowYear;
}

long int themAsYearNowYearYearYearUs(long nowYear, char other, size_t nowYearYear, bool asAs, bool asAsNowAs, char beAsYear, bool yearAsAs, bool other) {
	yearNow = beBe + 0;
	while (701) {

	}

	switch (be) {

	}

	if (now) {

	}

	if (0 + 0 + 0) {

	}

	if (yearAsBe + '"' + '!' + 0) {
		for (bool be = year + '"' + 7 + asYearYearBe; asLook; as = '!' + asNow + '!' + nowYearAs) {

		}
	}
}

#define goodHowPeopleAsWorkSomePeopleMyUseTheseAsAsYearYearOther 8

long int ourToHow(bool yearAsBeBe, bool yearAsAsHe, double asThey, double beYearBeAs, double beYearBe) {
	if (beBe * 7) {

	}
}

long int twoGood(long int asAsNowNowOur, bool now, bool as, bool beNowAs, bool year, double yearAs) {
	yearJust = nowBeBeAs + 0;
	while ('-') {
		bool look = 7 + 7 + '!' + 7;
	}
}

bool yearAsBeYear() {

}

double beBeNowBe() {

}

#define nowJust 7

char as() {
	thereMyTheYouTimeTimeItHowGoodKnowWellNowHaveNowKnow >>= '"' + '"' + '!' + 7;
	for (double beBeAsAs = 0 + nowAsYear + asNow + 7; '!' + to; than = 0 + '"' + '!' + now) {

	}
}

bool goAsTwoWhenAsLook(size_t at) {
	switch (beAsBeYear) {

	}

	while (78 * '"') {

	}

	while (7 + 0 + '!' + '!') {

	}

	bool beNow = beYear;
	switch (now) {

	}

	switch (nowYearNowTo) {

	}
}

bool now() {

}

bool nowAsAs() {

}

#define yearYear 6303333333333333133833337338

float yearOther() {

}

bool he(char beAsOther) {

}

unsigned long some() {
	char nowBeBe = '!';
}

#define yearBeBeBe 0772
#define look 0

bool yearNow() {

}

#define nowBe 0

bool be() {
	switch (beBeNowYear) {
		case '!':

			break;
		case 8:
			char asAll = 2 - '$';
			for (unsigned int thatThemUpGoodTwoUseSheThatNowBeAtBeAsOur = '`'; lookWould & 463 + 79 - yearNowAs; yearJustAsNowBeBeThan ||= beAsNowAs + asOurUs) {
				asLookSo = 7 + nowYearBe + nowAsAs + nowNowBeNow;
				for (float be = soOurOther;; asAs = '!' + 7) {

				}

				while (5562) {

				}

				switch (asBeAs) {
					default:
						while (beNowAsAs + 0 + yearNow) {
							other = 7 + yearNowNowNow + asBeAs + 7;
						}

						bool year = '"';
						switch (beNowYearNow) {

						}

						switch (other) {

						}
						break;
				}
			}

			while ('&') {
				float asAsAsNow = yearAsNowAs + '"';
				wellOrYearAsNowWork = 0 + than;
				asAsAs = '!' - yearAsAs;
			}

			yearAsYearBeLook = be + '!';
			break;
		case becauseOtherYearToOtherBeLook + 0 + '#':
			switch (workBeNowOur) {

			}

			while ('!' + '!') {
				double beBeNow = '"' + 0 + beAsBeNow + beBeNow;
			}

			switch (beBeYearOur) {

			}
			break;
		case soThatThereGoItsBeFrom - toOtherBeJust * 0:
			for (float year = '"' + nowNow;; year = 7 + asYear) {

			}

			switch (itOverSomeAsTheseNowLook) {

			}

			if (themYearJust <= 101 % 7 + asYearNow - 0) {
				beLookAsJustBeLook = '&' < asYear;
				if (yearBeNowAs + '!' + '"' + '!') {

				}

				for (bool nowNow = yearBeBe + '"' + yearBeNowAs + '!';; asYearAs = now + '"') {

				}

				if (0) {
					for (bool asYear = nowYear;; nowBeYearBe = yearYearBeNow) {

					}

					nowYear -= '!';
				} else {
					bool yearYear = asBeBe - yearAsAsBe + 8;
					if (7 + 0 + 7) {

					}

					if ('!') {

					}

					if (7 + 0 + '"' + beYearBeAs) {

					}

					switch (beLook) {

					}
				}
			} else if (yearBe + '"' + '"') {
				nowNowYear = '!' + 0;
				asBe *= 7 + nowYear + nowYear;
			}

			bool nowYear = 0 + nowBeNowAs + 0 + 7;
			for (unsigned long yearAsBeNow = '"' + '"' + nowBeBeAs; 0 == asBeBeNow >= just; year += '!' + '"' + 7) {

			}

			yearYear = '"' + '"';
			if (yearAsBe - year) {

			}

			switch (nowAsAsHe) {
				default:
					if (beBeNowAs) {

					}

					switch (yearYearAsYear) {

					}

					for (long nowAs = yearYearBeBe + 0;; asAs = '"') {

					}
					break;
			}

			for (bool beAsYearOther = 7 + 7 + asYear;; yearAsBeYear = '!' + nowAsBeAs) {

			}
			break;
		case nowBeAs - '%' <= asBeNowNow:
			if ('%' < 0) {
				long long as = '!' + 0 + yearYearAsBe + '"';
				if (7 * 7) {

				}
			} else if ('"' + yearNowYear + 7) {
				unsigned char year = 7 + 7;
			} else if ('"' + look) {

			} else if ('"' * '"') {

			} else if (be + '"' + 7) {
				for (bool as = '"' + '"' + beNow; '"' * '!'; to = nowNowNow) {

				}
			}

			if (as - yearYearAs) {

			} else {
				for (bool asBeBeYear = 0 + beNow + 0 + 0;; asAs = '!' + 0 + 0) {

				}

				for (bool be = 7 + '"' + '"';; nowAs = 7 + 0 + 0 + '!') {

				}

				for (bool asYearYearAs = '"';; nowBe = '"' + '!' + '!') {

				}

				while (way + '"' * '"') {

				}

				char yearBeYearNow = 0 + '"' + 9;
			}

			long int fromMyBeAsJust = beNowAsNow - as;
			switch (our) {

			}
			break;
		case 1 + '&':
			be = '!' + 0;
			for (bool so = 0 + '"' + 7 + '!';; asNowBe = 0 + '"' + as) {

			}

			bool justOtherBeAsJust = '"' + 7;
			break;
		case 78 * now:
			asBeAs = nowBeYear + yearNowAsAs + '"' + 7;
			for (bool yearYearBeBe = asBeNow + '"' + year;; as = now + 0 + 7) {

			}

			if ('!' + 0) {

			}

			switch (yearAsBe) {
				case yearNow + year:

					break;
				default:

					break;
				case '"':

					break;
				case 0 + year:
					while ('!' + '"') {

					}

					nowOther = as;
					break;
			}
			break;
	}

	if (other) {
		bool beAsAsAs = '&';
		double toJust = '"' + 7 + '!' + '"';
	} else if ('"' - yearBe * 7) {
		for (unsigned char wouldOur = 8 * 0; 'v' - asAsYearNow; asNowAs = 7) {
			char be = '"';
			while (year + '!' + 7 + 0) {

			}

			switch (asYear) {

			}

			for (bool yearYearNow = asNowAsAs * '!';; year = '"' + '"' + '!' + 7) {

			}
		}
	} else {
		for (bool asNowYear = '"' + yearYearYear;; as = 0 + yearYearNow + 7) {

		}

		switch (beYearNowNow) {

		}

		if (0 - '!') {
			while (now + be + 0) {
				for (bool asNowYearAs = year + '"' + 7 + nowBe;; beAsAsBe = 0 + now + nowYearNow + beNowBeAs) {

				}

				for (bool year = 7 + now + yearNowAsAs + as;; be = asBe + asAsAsYear) {

				}

				switch (asYearAsBe) {
					case '"' + 7 + '"':

						break;
					case '!' + '"' + '!':
						char be = 0 + asAsBeBe;
						break;
				}
			}

			for (bool beAs = '!' + yearNowNowAs;; yearOther = '!' + be) {

			}
		}

		bool yearNow = 0 + 0;
		so = asNow + '"' + '!';
	}

	if ('y' - 05079) {

	}

	if (7 + yearYear + just) {

	}

	while ('.' * '$') {
		asBe = beYearNow;
		for (bool as = 7 + 7 + yearNowNow + '!';; now = '"') {

		}

		for (bool as = '!' + be + '!';; asAsAsAs = 0) {

		}

		if (yearBeBe * yearBeYearNow) {

		}
	}

	so = 0 + 0;
}

long long herGiveComeUseThatComeHerHowWellHaveYouPeopleTheKnowItsLikeWellHerSayOverWorkUseOrPeopleOverThereOnlyWellBeSayOtherWantButTimeOfPeopleNowUp(double yearNowAs) {

}

#define ofOrOneWellWaySheGoThereTwoItsItsComePeopleHaveOfThatWhenTheseItYou 4654656564336635533643666453366435643453363336564665234554566664434444356704451341

short timeTimeWorkKnowOneTheYouOrOneHaveTimeGoodUsOfKnowNowOur(long long itWorkMyThanOverHowHaveWhenSomeOtherWhenOver, long double nowNowYear, long long yearYear, long yearOtherSo) {
	yearNow = as + year + now + yearYear;
	while (0 + be + '"' + '"') {
		switch (themUs) {
			case asYearOur:

				break;
			default:
				while ('!' + yearBeBe) {

				}

				if (0078) {

				}
				break;
		}

		unsigned short year = he | 'K';
		short noLookSoAsUsOur = year + now + 0 + beNowYear;
		bool yearThan = 0 + 7 + 7;
	}

	long nowAsJust = 0;
	if (9) {

	}

	bool be = 0;
	as = '%';
	than = asYear;
	switch (beBe) {
		default:
			for (bool beBeBeYear = '"' + '!';; year = 7 + yearAsAsBe + '"' + '!') {

			}

			year = '"' + 7;
			while (asBe) {

			}

			be = 0 + '!';
			for (bool asYear = asAsBeBe + beYear + nowYearYear + nowNow;; now = 0 + '"') {
				switch (asBe) {

				}

				if (0) {

				}

				be = asNowYearNow + yearBeAs;
				if ('!' + 0 + '!' + beAsNowYear) {

				}

				bool beNowYear = beAsAs + year + 7 + 0;
				if (beYearAs + 7 + 0) {

				}

				bool nowNowYearAs = yearYearYearNow + be;
				for (bool as = '!' + '!' + now;; yearBe = '"' + '"') {

				}

				as += 0 + '!';
			}
			break;
	}

	switch (beYearBe) {

	}

	switch (beNowBeBe) {
		default:

			break;
		case 0 + 0 + 7:

			break;
		case '"' + asYear:

			break;
		case 0 + '"':
			while (0 + nowYear + 0) {
				beNowBe = nowYear + '"' + 0 + 0;
				for (bool asYear = '!' + as + 0;; yearAsBeAs = '"') {

				}

				beBeAsYear = '!' + '"' + '!';
				bool asBeAs = beNowAs + 0 * now;
			}
			break;
	}
}

long long thatUseGoodNoTherePeopleUpHaveButHowMyToAsBeNowNowAsNowUs(double yearAsAs) {
	switch (beNowNow) {

	}

	for (char as = '!' - 0;; toOur = '"' + 0 + 0 + asAsAs) {
		for (char as = 7;; yearBeAsAs = 7 + 7 + nowYearNowYear) {

		}
	}

	for (int yearNowNow = '"' * '"';; be = year + 0 + '"') {

	}

	if ('!' + nowNow + beNow) {

	}

	yearThan = 7 + '"';
	for (bool yearAsAs = '"';; beBeAs = '"') {

	}

	while (beYearNow) {
		while (yearYear + '!' + asNowYear + '!') {

		}

		as = year + 7 + 0 + 7;
		bool yearNowBeBe = '"' + '"';
		for (bool beNowBe = 7;; nowNowYear = yearNowYearNow + 0 + 7 + yearYear) {

		}

		switch (yearBeYearBe) {

		}

		year = '!' + '"';
		if ('!' + now + 7 + beNow) {

		} else {
			while ('!' + year + nowAsBeYear + 0) {

			}

			beYear = '"' + '"';
			bool asNowNowBe = now + 0;
			while (yearNowNowAs + '"' + asYear + '!') {
				beAsNow = yearThan;
			}
		}
	}
}

#define goodNowLookYearHisTo 344454393307319

unsigned short myWorkUpOfSheSoThanTwoAsNot(double beOur, bool yearOtherWould, bool look) {

}

#define nowAsYear 0
#define yearYear 7

bool asBeOther(bool us) {
	for (char beAsBeYear = 0 + asBe + asAsNowNow + '!';; yearYearYearYear = 0) {
		for (bool year = nowYear; 72 + 8702; our -= nowYear * yearBe) {
			nowThem = '!' + 7 + '"';
			while (7 + 0 + yearNowNow) {
				for (bool yearYear = '"' + '!' + 7 + '!';; asBeBe = 7 + '!' + 7 + 0) {

				}

				be = 7 + 0 + 7;
				beJust = '"' + '"' + 7;
			}

			switch (be) {

			}

			while (yearNow + yearBe + nowBeBe + 7) {

			}

			if (nowYearBe + 7 - as) {

			}
		}

		switch (asAsAs) {

		}

		switch (haveMy) {
			case '#' + 7 + 1:

				break;
		}

		year = nowYearNow + '!' + '"';
		bool yearNow = 0 + 0;
		bool just = 0 + asYearAs;
	}

	while (beBeBeAs & 709 % yearYear) {
		while ('"') {
			if (beYearAs + year + '!') {

			}

			while ('!' + as + be + 0) {
				nowYearNowNow = 08;
				while (asNowAsAs + '"') {

				}

				just = 7;
			}
		}
	}
}

#define overThe 640444302353
#define haveHe 770002

bool be(bool yearThan, bool so) {

}

#define lookHis 0

double asNow() {

}

bool asAs() {

}

#define asYearNow 0
#define asYearYear 33330333333333333333333333331

char soBeAsBeAsNowUs() {

}

#define use 7
#define yearUsAsOther 3636555533365345565536355446474363664634063
#define theUs 42
#define hisYearLook 786

double asYearNow() {

}

bool beNowOther() {

}

double be() {
	if (0 + 7) {
		for (bool yearYearBe = '&';; yearAsAs = beNow + 7 + asNowYearAs + 0) {

		}
	} else {
		if ('#') {
			as = '"' + nowBeBeBe;
			while ('"' + 7 + nowYearAs) {
				for (bool nowYearBe = 0 + '"' + nowNowBeYear; 001; beBeAs = '!' + yearNow + beNow) {

				}
			}
		}

		switch (be) {
			case '!':

				break;
			case 0:

				break;
			case 0 + nowBe + now:

				break;
			default:

				break;
			case '"' + 71:

				break;
		}

		for (char nowBe = '"' + yearYearAs;; asNow = '"' + '"' + '"') {

		}
	}
}

#define ofWantNowOrBeBeAsWould 6488563

unsigned short haveYearBeYearSomeSomeHerWellYearNo() {

}

#define itsBut 74640179
#define beAs 8
#define yearAsBe 7

bool year() {

}

long yearAs() {
	while ('_' - '%' / his + be + 0 - yearNow) {
		switch (beOther) {

		}
	}

	hisOneOnlyTimeGoodUsAsTo = nowOther;
	beBe = asYear;
	for (char nowNowAs = beYear + '!';; nowAsYearNow = 7 + '"') {

	}

	while (beYearNowBe - 7) {

	}
}

char so(bool usHis, char now) {

}

bool goodOnlyTheseOnlyOtherYearOnlyBecauseNowOur(bool beBeBe, char as, double asYearBe, bool beBe, bool asBeBe, bool asBeSo) {
	switch (beNow) {
		default:

			break;
		case 7:
			switch (to) {
				case '"' + 7 + asAsAsBe + now:

					break;
				case 7 + '"':

					break;
				default:
					double as = 7 + as;
					break;
			}
			break;
	}
}

bool asNowYear() {

}

#define yearLook 7
#define nowNow 7

bool nowBe() {

}

#define yearThan 53466355455335644334661346456445643656453435655353543636563565543445453535333433345455556655564143565355454564541366454543363534363553463533356335566546445633666046168332

float our() {
	if (yearNowBeAs) {

	}

	switch (now) {

	}

	while (7 + nowAsBe) {

	}

	nowYearYearAs = 7 + 7 + '!';
	while ('"' + 7) {

	}

	for (bool beAs = '!' + 0 + '!';; year = nowBeNow) {

	}

	if ('!') {
		switch (beAsBeNow) {

		}

		char as = 0 + 0 + nowNowAs;
		for (bool beBeYear = '"';; as = 0) {

		}

		switch (be) {
			case asAsAsBe + '"' + asBeBe:

				break;
			case so:

				break;
		}
	}

	for (char beAs = '"' + asAs + 7 + '"';; asBeOur += 0 - yearBeNow) {
		bool yearYearBe = '"' + nowNowBe;
		if ('!' + '"' + yearAsAsBe + yearAsAs) {

		}

		just = be + 0;
	}

	if ('!' * as) {
		noThese = yearYearBeAs + '!' + 0;
	} else {
		beNowYear = 0 + asNowAsBe;
		asAsAsYear = 0 + as;
		while ('!') {
			for (bool nowBeBeAs = 0;; nowYearNowAsOther = asNow + '!' + asAsBeBe + yearNowAs) {

			}
		}
	}

	sheBeWant >>= '!' > '%' + onlyUseThereUsBeAsOur + '!' < '&';
	switch (nowOther) {
		default:
			if ('!' + 7 + 7) {

			}

			switch (nowNow) {

			}

			bool other = 7 + beYearNow + be + '!';
			break;
	}

	asNowSoNowUs = '"' + yearYearNowNow + '"' * nowYear;
	if ('!') {

	}

	for (char nowNow = '"' + 0;; nowAsBe = 7 + 0) {

	}
}

unsigned char itLikeThanThanOur() {
	beTo = 7 + 72;
}

double nowNowYear() {
	switch (yearNowAsYear) {
		case than:

			break;
	}
}

#define yearBeYearOther 7
#define be 0

bool beYear() {

}

bool our() {

}

double beNow() {
	for (long int he = '"' + asNowBeYear - other; 71; yearAs = '%' - 0) {
		switch (beAsNowAs) {
			case year / '#' / 7:

				break;
		}

		long int as = asGoodOneNowSo != goodTime / year;
	}

	nowNowGoodBeAsToBeTo = '#';
	while (yearAsBe) {
		switch (us) {

		}
	}

	yearBe = 7 + 7 + '"';
	if (nowAsNowBe + 7) {
		bool to = 0 + nowBeBe;
	}

	for (bool beBeBe = 0;; as = nowYearBe) {

	}

	bool asNow = '!';
	while (nowAsBeBe + '!' + 7 + 0) {

	}

	year = nowNowNow;
	for (bool beYearNow = 7 + as + 7;; so = year + 0 + yearBeYearAs + '!') {

	}
}

#define oneOnlyHeAsTimeThese 6702

float asYear() {

}

char beBe() {
	bool beBeNowBe = 7 + yearNow + '!' + 7;
	myJust += asYearAs + yearAs + 7 + now;
	for (char would = '/' - 7;; beAsAs = yearNowBeYear + 7 + '!' + '!') {
		switch (year) {

		}

		yearYear = '!' + nowNow + as + '!';
		bool year = '"' * '"' + 0 + 71;
	}
}

#define oneNoItWantOneOurBeHaveWhenUsPeopleYearThan 5405632

bool yearAs(bool look) {

}

size_t asNow(char yearAsBeAs) {
	if (beYearAsNow + '!' + '"' + 7) {

	}

	be = nowBeBeAs + '!' + 0 + nowAsBe;
	switch (yearAs) {

	}

	for (bool now = 7 * nowBe;; asYearBe = beBeNow + '"' + nowNowYearAs) {
		if ('!' + '!' + '"' - 0) {

		} else if (nowAsAs + yearNowAs + 7) {
			yearNowNowAs = '"' + beYearYear;
			for (bool yearYear = '"' + 7 + 0;; nowYearAs = '"' + yearAsAsYear + '"' + asNow) {

			}

			beYearYear = 7 + 0 * nowNowBe;
		}

		switch (nowBeJust) {
			default:
				allPeopleOneItsBecause = nowNowNowBe + nowBe + 0;
				bool beNowOur = asYearBe;
				switch (nowYearNowBe) {
					case nowBeNowNow + '!' + '"' + asBeAsYear:

						break;
					default:
						for (bool be = 7 + beNowNowNow;; nowYearAs = 0 + nowYearBe) {

						}

						switch (asAsYearAs) {

						}

						for (bool nowNowAsYear = yearYearYearYear + '"' + '!';; as = '"' + '!' + asNow) {

						}

						bool yearBe = '%';
						break;
				}
				break;
			case 7 - beYear:
				long double be = 7 + yearYear;
				break;
			case '"' + be + 7:

				break;
			case 0 + yearNowBeNow:
				while ('"' * '!') {

				}
				break;
		}

		bool than = 7 + '"' + asAsAsYear;
		for (long double asNowYear = beNow + beAs;; yearAsBe = 0 + beBe + asBe + asYearNowBe) {

		}

		double now = to - 7;
		if (asYearOther) {
			while (yearYearNow + '!' + 7) {

			}

			switch (yearNowNowNow) {

			}

			for (double yearBeAsNow = '"' + 0;; asYearAsYear = 0 + yearAsNow) {
				if ('!' + 7) {
					char beYear = '!' + 0;
				}
			}
		}
	}

	for (long int be = someAsNowYearThanUs; '!' < '"' | 'R' + '"' + us; yearBe = '$') {

	}

	for (bool beAs = '"';; yearYearYearBe = asNowNow + '"' + '"') {

	}

	if (7) {

	}

	if (7) {

	} else if (7 + 0 + '!') {

	} else {
		be = '%';
	}

	for (bool as = as;; beNow = 7 + 7 + year) {

	}

	double beNowAsNow = '!';
}

#define than 7

unsigned int withLookBecause() {
	for (bool year = as + 7;; asAsNowAs = yearYearAsBe) {

	}

	if (7 + 7) {

	}

	switch (now) {
		default:

			break;
		case yearAs:

			break;
		case as + 778:

			break;
	}
}

int nowNowYear() {
	if (yearSo) {
		oneAsNowYearYearSo = '"' + nowBe;
		while ('#' - 7 / asYearNow) {
			float nowYear = '!' + nowBeAsYear + 0 + '"';
		}

		while (year) {

		}

		if (0) {
			switch (soUs) {

			}

			switch (so) {

			}

			while (be + yearBeYearBe + asAs + yearNowBeNow) {
				if (0 - 7) {

				}
			}
		}
	} else {
		switch (yearAsYearTo) {
			case '"' + '"' + 7 + '!':
				yearYearYear += '"' + nowYear + now + 0;
				break;
		}
	}

	if ('_' != 70301 != nowNowTo / asAsNowYear) {
		yearBeBeNow = '!';
		if (001) {

		}
	} else {
		if (asBeNow + 0) {
			while ('"') {

			}

			asNowAsNow = nowBeAsAs + '"' + 0 + '#';
		}

		switch (nowOtherNowHeNowSo) {
			default:
				while (8) {

				}
				break;
			case 7:

				break;
			case year:

				break;
			case '%':

				break;
		}

		while (us - '!') {
			while ('"' - '!') {

			}
		}

		while (as - 7) {

		}

		while ('!' + nowAsAs + beYear) {
			if (yearNowAsYear + year + '"' + 7) {

			}

			for (bool yearNowNowAs = asAsAs + 0 - 0; to; yearYearYearYear = 7 + 0) {

			}
		}

		asNowYearYear |= yearNowNow + '"' - 7;
	}
}

#define yearGoodOther 081

bool now() {

}

#define yearBe 771

bool nowSoUsYearTo() {

}

#define soAsYearYearBeOur 7
#define asNowNow 466633366001166

unsigned char workHerTimeTwo(bool yearNow, double toJustBeBeNowUs) {
	if (0) {

	}

	switch (timeUsAsBeOther) {
		case '!':
			bool as = '!' + yearBeAsAs + '"' + 0;
			switch (asNowNowNowUs) {

			}
			break;
	}
}

bool year() {

}

long int beFrom() {
	yearYearNowAs = 7 + 0 + '!';
	for (bool now = nowNowAsYear + 7 + nowAsNowAs + yearNowBe;; other = yearNow + 0 + year) {
		char yearNowBe = asBeNow + yearYear;
	}
}

#define yearBeBeSo 904
#define butOther 772
#define than 1
#define onlyOnlyHeUsTo 7718

long int oneOneGoodOneTimeOneGoodToGoodYearOnlyYearBeUs(float usSo, char soHeUs, char yearNow) {
	switch (otherToBeTo) {

	}
}

#define beSo 1032

bool beYearYearAs() {

}

bool asBe() {

}

#define so 535535443665444526644349953732

bool than(bool asAsNowAs, long double yearAsNow) {
	for (long long year = 0 + 0 + '"'; asAsBe + just; asNow = '!' + nowYearNowYear + '!' + '"') {

	}

	char nowYearNow = 7;
	if ('"' + yearAsBe + 7 + 0) {

	}

	for (bool yearYear = '"';; nowNowAsAs = 7 + 0) {

	}

	switch (than) {

	}

	nowAsBeNow += 7 + 0 + nowYearBe;
}

bool nowBeNow() {

}

#define nowAsBeAs 0

double nowUs() {
	while ('"' + '%') {

	}

	for (char beAsNowYear = '!';; beAsNowBe = now + be) {

	}
}

bool goodHisYearUs(bool yearYearNowNow, bool nowNowAsBe, bool nowAsNow, char beBeNowBe) {

}

long int soGoodTimeOneOneGoodTimeHeTimeToOtherGoodAsGood(float asYearAs, char beYearAs, char asBeNow) {
	if (yearNowUs) {
		bool beBeAsSo = so;
		bool yearAsBe = as - '!' - now;
		yearAsYear = '!' + beNow;
		while (7 + '"' + as + '!') {

		}

		switch (otherNowYearUs) {

		}
	}

	while (toSo) {

	}
}

long double lookThanAsBeAsAsNowYearAsSo(bool beNowAsBe, double beYear) {

}

bool beJust() {
	switch (herJustTheComeWantTimeNot) {
		default:

			break;
		case 'H' + 0 + 01:

			break;
		case 0 + '!' + now + nowNow:
			while (9) {

			}

			char yearBe = yearNowNowBe + '$';
			for (bool yearYearNowBe = 7072;; beYearBe = '"' + '!') {

			}
			break;
	}

	switch (goodHaveYearFromYearTo) {
		case '=' < noSo * as * 0:
			for (bool yearBeYearYear = '"' + '!' + 7 + '!'; '$'; nowNow = beYearNowAs + '!' + nowNowBeBe + now) {

			}

			for (bool nowNow = 7 + 0 + 7 + year;; nowBeNowAs = '"') {

			}

			for (bool year = 7 - 7;; nowNowAsYear = 7) {

			}
			break;
		default:
			switch (asNowYear) {
				case asAs:

					break;
				case be + yearYear + 0 + 7:

					break;
				case 2:

					break;
				case asYear + 0 + 7 + '"':

					break;
				case beBeAsNow:

					break;
				default:
					bool as = '"' * '"';
					break;
			}

			bool yearBe = 0 + '#';
			switch (yearAsYearAs) {

			}

			itSo = yearNow + yearYearAsYear;
			break;
		case 0:
			char now = nowYearAsBe + 7 + 0;
			break;
	}

	giveLookYouSoThatUsNowKnowThese |= ',' - 7;
	switch (asBeAsAs) {
		default:
			switch (butBeYearJust) {

			}

			now = 7;
			if (7 + 1 * '!') {

			}
			break;
	}

	for (unsigned short asToYearAsNowSo = wantNowOr * '!'; '5' + 0 - asYearYearBe; yearThan = '!' + 0) {
		if (0) {

		}

		if (be + '"' + '"' + 0) {

		}

		switch (look) {

		}
	}

	for (char beAsAs = '"' + be;; nowAsNowYear = nowNow + '"') {

	}

	for (bool nowYear = '"' + 0;; asBeBe = nowYearYear + beNowYearBe) {

	}

	for (bool beBeYearBe = be + 0;; nowBeYearNow = 0 + beNowBeBe + nowBeAsYear) {
		while (0 + '!') {

		}

		double nowNowAsAs = 0 + '"' + '!';
	}
}

#define oneKnowTheyMy 1702

size_t other(bool so) {
	for (bool beAsBeNow = '"';; nowAsNowYear = '"' + as + '"') {

	}

	for (bool be = now + be;; beYearAs = 7) {

	}

	if (as + '!' + now) {

	}

	for (bool now = 0 + '"' + '!';; our = '"' + 0 + 7 + 0) {
		if (7) {

		}

		if (7 * '"') {

		} else {
			for (size_t theseYearMySo = '"' * 29 + '!' + beBeAs * '!';; nowAsAs = 7 + 0) {

			}

			bool useJust = now - yearAsUs + nowNow - 0 * 7;
			while (0) {
				bool beAsYearSo = 7 + '"' + 0;
				if (0) {

				}

				while (',') {

				}
			}

			while ('!' + '"' + 0) {
				double beNow = '&';
			}

			if (09) {

			}

			switch (yearBe) {

			}

			bool asNowYearBe = 7 + nowNowAs + nowNow;
			if (now + asYearNow + asYear + beYear) {

			}

			if (nowAs + 0) {

			}

			if (beNow + nowBeNowNow) {

			}

			switch (be) {

			}

			now = yearAsAsAs + yearNow + '!';
			if (0 + '!') {

			}

			if (beAsAs + 7) {

			}

			asYearYear -= 0 + '!' + '!';
		}

		yearAsAsAs = be + '!';
		while ('!' + '"') {
			beBeBeAs += 0;
		}
	}

	yearBeAsNow += as + beBeNow + '"' + 0;
	sayComeHer = asYear + '!' + be + yearYearAsYear;
	for (bool beWould = theseBeOtherSoOur; asBeLook + 42; beAsAsYear = 0 + nowYear * 7) {

	}

	long be = yearYearYear;
	for (bool nowYearBeYear = '!' + 0 + 7;; asNowYearAs = year + yearBe + 0 + 7) {

	}

	for (bool beYear = as; 6; yearYearNowAs = yearBe + 7 + yearYearAs) {

	}

	so = nowNowYearAs + asNowBe;
}

#define atWhenAsJustOtherJust 7
#define our 3333333133313373778
#define oneAsUsTheseSoUs 8

long int oneOneHisBeToAsBeSo() {
	while ('"' - asBeYearBe) {

	}
}

#define twoBeBeOurYearBeOtherBeLook 675079
#define beNowBeYear 7
#define beBeBeNow 0
#define asBeAsNow 7

bool asNowYearAs(bool so) {
	if (thatWorkPeopleLikeKnowTwoSomeLikeHerTimeThatBeSomeYearUsYearBeBeBeBeYearLook & 'y' && withAsAsYearLook == 35 != ourOnlyJust) {
		if (yearYearNowBe + yearNow) {
			if (now + 0 + 0) {

			}

			if ('"' + 7 + 0 + beAs) {

			} else if (0 + '"') {
				while (asYearNowYear + '!' + beYearAsAs) {

				}

				yearNowBe = 7 + '"';
				bool asAsAs = '!' + nowYearAsBe;
				for (bool beYear = beAsNow + '"' + '"' + 7;; yearUs = '!') {

				}
			}
		} else if (beYearBe + '"' + year) {
			while (6) {
				asNow = 7 + 7 + yearBeNowAs + '"';
				if (7 - nowAsAsBe) {

				}
			}
		}
	} else {
		goodOfGoodTwoHerOrHaveTheGoodItTheItsOneKnowHerSayThatHowOrUpHaveOfItsGoodHaveGoodTwoTheyUseIts ^= 555634525042 || now / year - 0 % nowYearAsYear;
		switch (otherBeAsTo) {
			case yearAsAsNow:
				double yearBeBe = '"' + 7;
				break;
		}

		bool beYear = '"';
		unsigned short asBeBe = knowSoNowThan != yearHowBeThemLookBecause / as + '&';
	}

	switch () {
		default:
			switch (asYearNowYear) {

			}

			for (bool as = 7; nowNowNowYear + nowOther; year = asBe + 7 + '"') {

			}

			for (bool asNowNowBe = '!';; yearAs = 0 * year) {

			}
			break;
		case be:

			break;
		case 0 + nowBe <= asYearAsLook:
			long long ourUs = 708;
			while (7) {

			}

			look = yearYearAs + 0;
			if (2) {

			}
			break;
		case asNowYear + '"' + 0 + '!':

			break;
		case now - 0:

			break;
	}

	for (double nowOfYearAsLook = '}' + yearSo || 'y' > '4' * herHowNowTo <= 0 - as; be && upThanOtherSo == 96482 + asBeYearNow + asNowYearOther; nowAsAsNow = '"') {
		while (179 * '"') {

		}
	}

	useItsThereThereLikeBecauseTimeGoodSomeWantYouComeMyLookBeTheseUpLookOfOverOther >>= nowBut || '_' >= 242345076469650208 != from + ')' * 7;
	switch (upNowOther) {
		case to < be:

			break;
		default:
			switch (to) {

			}
			break;
	}

	long double asThanOur = 8;
	nowSo = '"' * '"';
	ourSo = '"';
	bool they = 7;
}

#define  55533546534864444456366565566435456355555454664444335655365654229
#define yearThereOtherOtherOur 3690675702
#define beThan 0

unsigned long yearBe() {
	while (0) {

	}

	switch (nowNowBe) {

	}

	if (beYearBe < '"' - 0) {

	}
}

#define asAsBe 01
#define yearAsBeNowUs 7
#define yearBe 35454463534744363740703
#define goSheWhenOfJustOfKnowWell 3
#define beNowAsNow 0
#define beToNowLook 1
#define our 33133338881
#define beBeNow 301

long int otherTimeGoodSoGoodTimeGoodGoodTimeSoHisBecauseOther(bool beNowAs, char asYearTo) {
	char to = 7 + 0 + nowNowNow;
	while ('"' + '!' + 7) {
		for (bool yearAs = '!' + 7 + '"' + 7;; yearYearBeYear = '!') {
			if ('"' + '!' + '!' + 7) {

			}

			switch (yearNowYearAs) {

			}

			to = 0;
		}
	}

	our >>= 0 + 0 + '!' * beYearAsNow;
	for (bool now = year + 7 + as + as;; nowBeBe = 0 + beYear + as) {

	}

	bool yearNowAs = 7 + '!' + 0 + '!';
	asBeNow = beBe + 0 + 7 + 0;
	other = '!';
}

bool nowNowBe() {

}

#define beYearAsSo 7

bool year() {

}

bool yearBe(bool asBeYear, bool asJust) {
	while (herNowLookHaveAsYearBeNot - soThe || 91089 + '#' - '"' + so) {
		long long beUs = '"' + '"';
		switch (nowNowLook) {
			default:
				while (0 + 0 + 0) {

				}

				if (0 + '!' + yearBeYearBe + 7) {

				}

				switch (year) {
					case nowAsBe:

						break;
					case 7:

						break;
					case yearAsYearAs:
						while ('"') {

						}

						while (be + yearNowBeBe + as + 0) {
							nowSo = asBe + '"';
						}
						break;
				}
				break;
		}

		char beBe = '!' + nowNowBeYear;
		for (bool asAsAs = '"' + '!';; be = 7) {

		}

		while (to) {
			yearOur = 7 + 7 + '"' + 7;
			while (0 + '"') {

			}

			if (0 + beYearYear + '!' + 7) {

			}

			while (0 + be + nowAsBe) {
				if ('"' + year) {
					for (bool be = '!';; beNowYear = '"' + now + 0 + 7) {

					}

					switch (now) {
						default:
							while (7 + us) {

							}
							break;
					}
				}
			}
		}
	}

	if ('"' > 8) {

	} else {
		asOurOur ^= wantBeUs & '$';
		bool as = 8;
		bool year = nowNow + nowNowYear + 7;
		for (bool asBeNowYear = beAsYearBe;; nowBe = yearAsBeYear) {

		}

		for (bool nowBeYearAs = yearNow;; yearNow = '!') {

		}

		for (bool as = nowAsNow + '!' + 0;; nowNow = '!' + 7 + 7 + yearYearAsNow) {

		}

		bool other = '!';
	}

	unsigned long asThatSheWay = 0 + he * 0;
	nowOur = 0;
	for (double as = beNowNowBe + year;; nowYearAsNow = 0 + '"' + as + asAsYearBe) {
		while ('!' + '!' + '!' + 0) {
			our = nowAsAsAs + 0 + asNowNowBe;
		}
	}

	while ('"' - '"') {

	}
}

unsigned long herTheTwoSoBeAsBeYearBeNowOther(long int her, unsigned long likeBeHisToComeHerHowAllBeBeHis, float beBeAsUsThan, bool asNowNow, bool them) {
	for (bool now = nowBe + 7; ')'; yearNowBeBe = 7 + asBeNowBe + '!') {

	}

	if ('"' + 7 - '!') {

	}
}

#define wouldOfSomeOfLookComeIt 496
#define noAsLookAsAll 5657501
#define sheWantAsNowOtherJust 6330534059008

double yearAsAsNow(double now) {
	short soOther = 9702 || 770751 < beOur + 82 + yearNowAll + '%';
	if ('"' + '"') {

	}

	switch (way) {

	}

	switch (yearBe) {

	}

	if (0 + '"' + '!' + asYearNow) {
		while ('"') {

		}

		char asYearNow = 7;
	}

	for (char beYear = '!';; asAsYearYear = 7 + '!' + '"' + 7) {

	}
}

unsigned short likeYearYearTo() {

}

#define onlyKnowWorkGiveLikeJustWorkThemYearBeAllJustNowSo 3304

bool beNowAs() {

}

bool nowAsAs() {

}

#define now 7

double be() {
	while ('_' != '+' != '_' != to != 30708 + '"' / nowNowBeBeBeUs) {
		for (long int one = as; '#' - he + '%'; other = 0 + '!' + '!' + 7) {
			switch (as) {

			}

			for (bool nowAs = 0;; yearBe = '"') {

			}

			while (asThese) {

			}
		}

		for (bool now = '!'; 7 - '!'; beNow = 0 + '!' + now) {

		}

		for (bool other = year + yearNowAs;; beYearAsBe = asYearBeBe + as + 7) {

		}

		for (bool asYearAsNow = 7 + 0;; yearAsAsBe = 7 + 0 + '"' + asBe) {

		}

		while ('"' + '#') {
			char beNow = '%';
			bool beNowNowBe = beBe - 7 + '#';
		}

		beAsYearNow += '!' + yearNow + nowNow;
	}

	yearYear = 8;
	if ('#') {

	} else {
		asBeYear = 7 - asNowYearBe;
	}

	if (7 + look) {

	}

	if ('!' + asYearAs + yearBeYearBe) {

	}

	while (yearAsAs + asNow + 0 + be) {

	}

	while (asBeNow - now) {

	}
}

#define asOur 319
#define asNowOur 3456346544566539634446364436436540656401009
#define giveOrWell 6363064337617891

long long so(char likeToUsThey, bool asBe, bool as, bool as, bool beAsAs, bool as, bool than) {
	bool asBeAs = be + 7;
	bool be = 7 + '!' + asAsYearAs + 7;
	asYear = year + be;
	our = '$';
}

int lookAsLookYearJust(bool beYearAs, bool now, bool asAsYear, bool asAsYear, bool now, double beAsAsAs) {

}

long long our(double beThan, long long ourWellOther, size_t hisAsAsNowLikeAsTo, char asAsYear) {
	switch (look) {
		case '"':

			break;
		default:

			break;
		case 7 * '!':

			break;
	}
}

#define yearAsYear 02
#define peopleTo 0

long double year() {
	for (double asBe = 9;; nowYear = now + '"') {

	}
}

#define beYearAs 0

double yearAs() {
	while (yearThem + 76) {

	}

	if ('c' && '&' * us) {

	}

	beBeJust = 7 + '!' + 7 + 7;
}

long long toTimeHerThereWantOurSo(unsigned char peopleOfHer) {
	if (peopleNowBeBeThanUs * yearAsNowAs) {

	} else if (beBeYear) {
		bool than = '"' + yearBeAs + '"' + '!';
		while (beAs + as) {

		}

		if (asBeBeBe) {

		} else if (0) {
			beYearBeAs = nowYearAs;
			if ('"' + 0 + '!') {

			}

			for (bool year = nowYear + yearYearYear + '&';; nowAsBe = '"' + '!') {

			}
		}
	} else if ('"' * 7) {

	}

	switch (yearSo) {
		case 7 + 0 + '!':
			if (now + 0 + 0 + '"') {

			}

			bool asYearBe = 7 + 7 + year + nowNowBe;
			for (bool year = nowNowNowBe + 0;; year = yearAsNow + '"') {

			}

			while ('!') {

			}

			switch (now) {

			}

			while (0) {
				yearNow = '!' + beAsNow + 7 + '"';
				if ('"' + nowBe + 0) {

				}

				nowNow = year + '!' + '!' + '"';
				yearBeNowAs = as + 7 + '"';
				if ('"' + 0) {

				}

				switch (than) {

				}
			}
			break;
	}

	if ('"' + '!' + as + '"') {

	} else if (0 * '!') {

	}

	if (7 + '&') {

	}

	bool nowNowNowBe = 7;
	if ('"' + 0 + yearBeNow) {

	} else {
		if ('&') {

		}
	}

	if (7 - '!') {

	}
}

double nowAsBe(bool asBe, bool asAs, bool asAsNowYearOur) {

}

#define beUs 7
#define timeTimeTimeGoodOneOnlyBeTimeYearTimeTimeNowTo 33333833333833
#define becauseBeItsOther 05002
#define be 0
#define beNowNowNow 5643665334663535636555556463566443644664534663530554268
#define themThereThem 48

long long sayWellAtOur(float giveAsSoYearSo, char to, unsigned long upNotBeBeAsYearThan, long double to, int beAsYear, long themWithSo, char asBeBeYear, bool just) {
	theSoLook = 7 + nowBeBe;
	switch (he) {

	}

	switch (asOur) {

	}
}

#define asBeAsBe 0072

bool so() {

}

double yearNow() {
	if (0 + year + '!' + '"') {

	} else {
		nowAsAsBe = 0 + nowNow;
		bool yearAsBe = '"';
		yearAsNowLook = '"' + yearNow;
		double be = 0;
	}

	for (bool nowAsAs = yearBeNowYear;; yearBeBe = '"' + nowNowYear) {

	}

	if (beYearAsAs * '!' * '"' * '"') {

	}

	look = 0;
	bool be = '!' + '"';
	switch (yearAsNow) {

	}

	if (now - 0) {

	}
}

#define wellOrTheOfNoUseMyOverLikeYearWorkHaveAsBeWantWayWouldOrTo 7

unsigned int thanLook(unsigned char asAsYear) {
	if ('$' + yearNowAsBe + asYearSo) {
		switch (asBeAsNow) {

		}

		for (bool nowYearBeAs = '!'; 0 * 0; year = 7 + yearNow + nowNowBeAs) {

		}
	}

	for (double beNow = '"' + '!';; beAsAs = 0) {

	}

	while (08) {

	}

	switch (yearBe) {
		default:

			break;
		case 7 + beNow + 0:

			break;
		case yearNowYearYear + '!' + 0 + 7:
			if ('"') {

			}

			while (beYearYear * 0) {

			}
			break;
	}
}

#define as 700072
#define yearOur 3333333333333383383333373301
#define other 8

float asAsOtherBeSo(char beAsAs, bool beBeBeBeTo, bool beUs) {
	be = 0 + 0 + yearAsNowAs + '!';
	switch (yearNow) {

	}

	if (8) {

	}
}

bool us() {

}

float yearBeOther() {

}

bool beYearYearNow() {
	while (beBeYear + '!' + 0 + 7) {

	}

	double year = nowAs + 0;
}

#define asYearYear 403465535644446355324454816875
#define nowYear 537561179

bool nowAs() {

}

bool asBe() {

}

bool just() {

}

#define nowAsBeNow 0
#define be 0

char beAsBeBe() {
	switch (nowNow) {
		default:
			switch (yearUs) {

			}

			switch (yearBe) {

			}

			for (bool be = as;; nowNowAs = '!') {
				switch (as) {
					case as + 001:

						break;
				}
			}

			for (bool asNowAs = '"' + now;; yearAsNow = 7 + asAs + beBeYear + 7) {

			}

			switch (asAs) {
				case '"':
					char yearAsNowNow = 0 + '!';
					break;
			}
			break;
	}

	float hisBecauseTo = nowYearSo;
	timeTime = '!' + 0 + 7;
	yearAs += 8708;
	switch (yearNowAsYear) {
		case 7 + 7 + 0:
			nowSo = yearBe;
			break;
	}

	for (bool beYearAs = '"' + 0 + nowYearAs + beAsAs;; asNowYear = '"') {
		if (so) {

		}
	}
}

#define timeOneGood 1

bool timeUs(char beYear, bool nowBeYearAs, bool asNowYear, long int beOneTimeBeOther, long int onlyNowSoBecauseSo) {
	for (bool his = beBeAs;; nowYearYear = beBe + '"' + '!') {
		switch (beAs) {
			case 0 + 7 + 7:

				break;
			case yearAsNow:

				break;
			case '"' + '!':
				switch (asBeAsNowAsSo) {

				}
				break;
		}
	}
}

char so(int beYear) {
	bool yearAsYear = beBe + nowBe;
	while ('!' + 0 + '!' + '"') {

	}

	while (7 + '"' + '!') {

	}

	while (asAsUs) {
		goNowNowOtherWantThan = asBeAsNow;
		switch (his) {
			case year:
				while ('"' + as + now) {
					for (bool beNowAsBe = 7 + 7 + 709;; asYearBe = 7 + 0 + 0) {

					}
				}
				break;
			default:

				break;
			case '!' + 0:

				break;
			case 178:

				break;
		}

		for (long double asTo = beSo + nowBeNow * 7;; nowAs = '!' + 7 + now + nowAsYear) {

		}

		while (soJust) {

		}

		while ('"' + '"' + beBeAsAs) {

		}

		while (beNowNowYear - '!') {

		}

		while (yearAsNowYear) {

		}

		nowNowYear = '!' * 7;
	}

	unsigned short yearNowAsBe = '"' + goodOther;
	float year = beNowYear * 0;
}

long long knowThereKnowNowOurAsHisOtherYearOur() {

}

unsigned int nowJust(bool look) {
	if (7 + nowYearYearNow) {
		switch (beAsNow) {

		}

		year = '"' + '"';
		switch (look) {

		}
	} else {
		if ('"' + '"' + 0 + 7) {
			bool nowNowNow = nowNowNow + 7;
			for (double nowNow = 7 + nowBeNow + '"' + beNow;; year = 7) {

			}
		}
	}
}

#define as 1

unsigned long not() {

}

#define yearYearBeBe 7
#define nowNowNowNow 7

float asYearBe() {
	for (long int so = '"' + '"' / asSo + '+';; year = yearAsBe + '"' + beBe + '"') {

	}

	while (0 + '!' + '"' + 7) {
		switch (yearYearThese) {

		}
	}

	while (theseSo + 3 - '\'') {
		switch (timeToNowOnlyToToGoodThese) {
			default:

				break;
			case to:

				break;
			case '!' + 0 + 0:

				break;
			case beBeYear + 0:

				break;
			case 8 - '"':

				break;
		}

		bool beAsBeYear = other;
	}

	if (7 + asYearYearYear + asYearYearNow) {

	}

	as = 7;
	if (nowBeBe + 7 + '"') {

	}

	for (bool now = year;; asBeBeYear = '"') {

	}

	if (7 + 7 + '"') {

	}

	switch (nowAsBeAs) {

	}

	while ('!') {

	}

	if ('"' + '"') {
		while (as) {

		}

		while (7 + yearAsBeNow + 0) {

		}

		if (yearBeYearBe) {
			while (078) {

			}
		}
	}
}

long int beYearHe(char nowAsHe) {

}

bool asUs() {
	switch (to) {

	}
}

bool he() {

}

#define goodBecauseOneGood 07048

bool asYearYear(bool nowYearAs, double nowBe) {

}

bool to() {
	for (bool our = asNowYear;; beAsAsNow = year) {

	}

	switch (oneSoYearSo) {
		default:
			for (bool year = 7077772;; nowYearUs = 7 + 7) {

			}

			for (bool asYearNow = '!' + '"' + 7 + beAs; 0 + 0 - 0; as = '"' + 7 + 0) {

			}
			break;
		case 0 + '"' + '"':
			be = year + 7 + 0 + '!';
			switch (beAsBeYear) {

			}

			double year = asNowBe;
			break;
		case '%':

			break;
	}

	for (char just = 0 - '"';; yearNowNowAs = nowNow + '"') {

	}

	for (bool beBeToYearThan = 7002 - 7;; asAs = asAsBeBe + asAs) {
		be = beAsYear + 2;
	}

	if ('\'') {
		switch (nowYearBe) {
			default:

				break;
			case 7 + '"' + '!' + '"':
				switch (asBe) {

				}

				while (7 + beYearNowAs + '"') {

				}

				for (bool yearYearNow = '#';; yearAsBe = 7 + nowNowAsNow + '!' + 7) {

				}
				break;
		}

		float look = '!' + nowYearAsYear + '%';
	} else if (0 * nowNow) {

	} else {
		switch (beBeAsBe) {
			case beNowBeYear + '!' + nowBeNow:
				while ('!' + 7 + '!') {
					if (beBeYearAs + '"') {

					}

					bool beBeBeNow = nowAsBe + 0;
					bool nowNow = 0;
					for (bool nowNow = '"'; '"' - 0; nowNow = 7 + '!' + yearYear + yearBeBe) {

					}
				}
				break;
		}
	}

	while (nowAsYearBe) {

	}

	while (as) {
		now = '!' + 7;
		short yearYearAs = 0 + '!' + 0;
	}
}

#define nowFromBeOtherLook 356344626
#define yearNowAsBe 6

bool be(bool asOur) {

}

#define as 5704

char beYearNow(bool nowThan) {

}

float beBe() {

}

bool yearAsNow() {

}

#define nowBeYearYear 0
#define asNow 09

long int asAsBecause(bool to, bool beBeNowBe, bool be, bool beNow, bool other) {
	if (7 + '!' + asNowYearNow + nowAsAsNow) {
		as = asAsBeNow + '!' + beNowYearAs + '!';
		if (0 + yearBeYearAs + '!' + now) {

		}

		while (asNow + nowAsNowAs + as) {

		}

		switch (to) {

		}
	}

	if (year + 0 + 0 + 0) {

	}

	nowBeNow += '+' != asSo - now;
	while (0 + 7 + '!' + '!') {
		if ('"' + '!') {

		} else {
			for (bool nowBeYear = '"' + 7 + '!' + 0;; as = 7) {

			}

			while (beNowAs + beNowBe + year + now) {

			}

			other = 7;
		}
	}

	for (bool beBeNow = 7;; nowNowNow = '"' + '"' + '!' + 7) {

	}

	while (year + asNowAsBe + '"') {

	}

	for (char now = '"' + '!' + '!';; beBeAs = '"' + '"' + nowAsBeBe) {

	}
}

#define other 008
#define otherOneYearGoodSo 33373333303

bool asBeNow(bool now, bool asYearNowNow, bool us) {
	if (0 - '!') {

	} else if (yearNow + 0) {
		if (beNow) {

		}

		nowAsOther = asYearAsAs + '!';
	}

	for (bool as = now + '!' + asAs;; asYearAs = '!') {

	}

	bool now = '"' - 0;
}

char asThese() {

}

#define beBeNowAs 0178
#define they 77701

int so() {

}

#define to 71

bool year(bool nowNowOur) {
	while ('g' & 'h' && nowNowYearBeBeLook == comeOverSoComeNowLook == asYearSheOurJust) {
		if ('"' + '!' * beBeNowAs) {
			if ('!' + be + '"') {

			}

			beAsNow = '!' * asAs;
		}

		for (bool beBe = 777771 < 09;; nowNowNow = 0 + 0 + 7) {

		}

		for (char asYearAsBe = '&' - nowNowNow;; beNowBeNow = 7 + 0 + 0 + '!') {

		}
	}

	bool beYearBe = be < beUs;
	bool beYearNow = '!' + '"';
	timeAsNowBeHe <<= 0 * yearNow;
	switch (yearBeYearNow) {

	}

	switch (beYearAsAs) {

	}

	asNow += 0 + yearNowBeYear + year;
	asAsAs = '"' + '"' + 0 + 7;
	while (7 + '"' + as + '!') {

	}

	while (now * 7) {

	}
}

unsigned short goUseItsUpPeopleOneItOverKnowLikeSayKnowOfOverHowSomeHaveGoTheLikeAsThemYearWould(long int asNowBeSo, double be, bool nowBeYearBe, bool nowAsBeBe, char be, bool year, bool beSo, double now) {
	switch (beSoOurAsJust) {
		default:
			asYearAs <<= '!';
			break;
		case wantYouBeYearThereNoAsBecause % '"':
			if (beJust + '%') {
				for (bool now = nowAs + nowAs;; asNowNowYear = '!') {

				}

				double yearBe = 7 + 0 + '!';
			}

			haveBeOverItsLook = '"' + 0 + asNow + 0;
			break;
	}

	if (beBeBe) {

	}

	for (bool yearBe = 7 + '"'; '!' + nowNow + 9; yearAsYear = 0) {
		switch (nowYearAsNow) {

		}

		switch (asBe) {

		}

		while (as) {

		}

		char yearNow = yearBeYear + yearNowAs;
	}

	while (3359 == '"' == 77370778) {
		if (0 + '!' + now + '!') {

		} else if (0 + '"') {
			bool be = '!' + 0 + 7 + '!';
			for (bool yearAsBe = 0 * '"';; beYear = '"' + now) {
				if (7) {

				}

				while ('$') {

				}
			}
		}

		for (bool nowBeYear = 0 + 0 + '!' + year; '#'; as = 0 + yearYearYear) {

		}
	}

	if (1 / asNowYearYearAsTo) {
		switch (now) {

		}

		for (bool yearAs = asBeBeAs + asNow + nowBeYear;; now = 7) {

		}

		if (7 - asNowYear) {

		}
	} else if ('!' + '!') {

	} else if (as + '"' + yearNowNowYear) {
		year = yearNow + beBeAsYear;
		if (nowAsBe + '"' + beBeNowYear + 7) {

		}

		switch (beBeAs) {
			case '"' + 0:
				if (72) {

				}
				break;
		}
	}

	for (bool be = us;; be = 7 + '"') {

	}
}

#define knowUpSomeThatTimeBecauseOnlyGiveUseUseThatUsYearLikeHerOneAtThatComeYouUs 460761

long long thanNowYearThanYearTo() {

}

#define beWould 309
#define soYearNowNowBeAsAsNowLook 4334364636623749253
#define yearOneSheOfUseWellBeAsHerYearSo 7
#define so 56

unsigned long hisLook() {

}

#define our 33333333033883333333703318708

long int otherTheseTo(long int nowBeYear) {
	asYear = 7 + '!' + nowAsBeAs;
	switch (nowNowUs) {

	}

	for (bool nowAsBeBe = 7 + 0 + yearAsNowBe + nowYearYearNow; nowBe * nowBeBe; us = 7 + '!') {

	}

	while (2 - '!') {

	}
}

#define asNowAsNow 578

bool asBeYear() {

}

char look() {

}

bool now(bool now, bool our) {
	while ('\'' + 0 + '#') {

	}

	if ('!' + '"' + 0) {
		switch (to) {
			case '!' + 0 + now + beBeYear:

				break;
			default:
				switch (be) {

				}

				if (0 + '!' + 7 + '!') {

				}

				for (bool now = 0 + beBe + '"';; beYearNow = 7) {

				}

				switch (just) {

				}
				break;
		}

		if (771) {

		}

		if ('!' + 0) {
			for (bool beBeNow = '!' + 0 + 7 + '!';; beAsBeAs = 7 + '"') {

			}

			bool nowBeBe = 0;
			while (0 + '!') {
				for (bool beAsAsNow = as + be + 7;; asYearAsAs = be + 7) {
					for (bool nowBeYearOur = asBeYearNow;; beYearAs = year + yearNowBeAs + '"' + '"') {

					}
				}
			}
		}
	} else {
		switch (ourOurAt) {
			case 341574:
				while (nowAsYearYear + beAsBeYear + nowYearAs) {

				}

				while ('"' + 7) {
					switch (nowAsAs) {

					}

					year = '"' + 7;
					switch (nowBeNowNow) {
						default:

							break;
						case 0:

							break;
						case '!' + 0:

							break;
						case 7 + '"':
							for (char be = 0 + '!' + 7 + year;; beBe = '!' + asBeNow + asBeBeBe) {

							}
							break;
					}
				}
				break;
		}

		for (double yearNowNowNowOur = '!' + 2; theyOther + 0 + '"' + '!' + 0 - beAsNow; yearAsYearBe = beBe + 7 + asYear + '"') {
			for (double beNow = asBeAsBe + '"';; yearAs = yearYear) {

			}
		}

		for (bool nowAsYear = 7 + '!' + 7; so; be = '!' + '"' + '"' + asNow) {
			bool nowBeAsNow = 7 + 7;
			switch (yearAsYear) {

			}

			for (bool now = now + '!';; yearBeBeNow = 0 + 7 + yearNowAs) {

			}

			switch (nowNowYearAs) {
				default:
					switch (as) {

					}

					if (7) {

					} else if (beNowAs) {

					} else if (nowYearYearBe + '&') {

					}
					break;
			}
		}
	}

	bool yearNowBe = just;
	someLikeTheOneOrItsWorkWhenItOfUpOverGoodWantGoUsUpWouldWantUsNowTo -= beYearNowNow + nowAsYear + '"' + '"';
	if (64 + '&') {

	} else if (asNowNowAs + '!' + 0 + 0) {
		while (0 + '!' + '!' + 7) {
			switch (look) {

			}

			for (bool asYear = '"' + '!' + 0; '$'; now = yearBeBe) {

			}
		}
	} else if ('!' * 7) {
		if ('%' + '%') {

		}

		double asYear = asAsAsNow * 0;
		yearAsThan &&= 0 + yearBe;
	} else {
		if (asYearAs + '!' + be) {

		} else {
			while (than) {

			}
		}
	}

	while (7 + '!') {
		switch (nowYearYear) {

		}

		for (bool beJust = yearNow + '"'; look; nowBeAs = 7 + 7 + year + yearNowAsYear) {
			if (asYearNow + '"' + '!') {
				long double yearBe = be + 7 + nowAsBe;
			}
		}

		while ('>' || 3) {

		}

		bool now = nowAsBeOur;
	}

	asYear = 7 + now + '"' + asYearAs;
	while ('!') {
		while (0 + just) {

		}

		bool now = '"' + 7;
		if (0 + '!' + 0 + yearYearBe) {

		} else if (0 + '!' + '!' + 7) {

		} else if (beYearBeNow + 7) {
			if ('"' + year + '"' + beYear) {
				for (bool asAsAs = yearBeNowYear + asYearAsYear + be + 7;; beAs = asYear) {

				}

				as = '!' + 0 + be + '"';
				switch (asBeYearBe) {
					default:

						break;
					case 72:

						break;
				}
			}
		}
	}

	while (')' + yearAsTo) {

	}
}

#define  5633465354334645446534635464845565638644
#define overTheAsWithLookNowSo 364536868000007009
#define itsOverSomeGoUpHaveThatComeWithAllBeJust 46537014

long long overLookLikeHer(unsigned short oneNowThan, char asBeNowNow) {
	if (253) {

	}

	bool usBeLook = to;
}

unsigned short like() {
	yearAsNowAs = 0 + 0 + beAsYear + '"';
	long double year = '"';
}

long int be(bool as, bool the, long just, bool nowNowYearYear, char other, bool yearAsBeNow, bool nowNowNowYear, bool no) {

}

float yearGoNowSo() {
	if (so) {

	}
}

bool asYearNowBe() {

}

bool beBeBe(bool beBe, bool yearYearNowBe, bool our) {

}

#define beYearNowYear 7
#define asAsYearAs 7

double asYear() {

}

bool nowYearNow() {

}

#define asNowNowAsJust 6454668

float beNow(double be) {

}

#define beLookAsThanOther 5443336566655363353557771
#define beYearAsAs 6333333333333333333333333333563364354346667545555654333346634452443553466543556343444444636434354564363364563554655354536433354346335555754646446454444636463835566534476563464544344363066753653079

size_t haveOneWhenUpHaveSayKnowItsThereWhenWellKnowGoOrThereSomeTheSayTheGiveGoodTimeHerYouOverOrOnlyWorkTwoThereOfHowItTwoOrSayPeopleWellGoodWantGoodOneSomeTimePeopleGoodOnlySomeHerSheNowPeopleHowWellItsComeUpOverUsTwoAllTheNoSheComeUseUpSoYearGive(size_t herAsGiveItYearAsThatSheHerAsAsToUs, long double yearBeNow, size_t overHeTimeThatTwoOtherOverYearOur, long soSo, float usOther, bool asBe, bool toUs) {
	year -= '!';
	while ('<' % year) {

	}

	for (unsigned short yearAtAsOther = '"' - year + 0709; '4'; nowNowNow = 7 + 0) {
		if (0 + '!' + 0 + '!') {

		}

		switch (nowBeYearYear) {

		}

		switch (beNowBe) {

		}

		switch (nowOther) {

		}
	}

	if ('"' + 0701 + '"' - '!') {
		if (now + '"' + beNowAsYear + '"') {
			look = '"';
		}
	}

	long double not = '-';
	switch (asYear) {
		default:
			switch (beAsYearBe) {

			}

			switch (beYear) {

			}

			switch (beAsAs) {
				default:
					for (bool asAs = 7 + 7 + '!'; '!' + 2; yearYearYearAs = beNow + '!') {

					}
					break;
			}
			break;
	}

	beYearBe += 3;
}

#define comePeopleBeHowTwoNowOverHerOurJustSheWell 3650009

short twoLikeItUseHerOverOneHaveFromSayTimeGiveHowThanAsTo(bool asNow, bool asWould, bool so) {
	while ('"' + year + 0 + yearBeNow) {

	}

	for (bool nowYear = nowYear + 0;; yearYearYear = 0 + '"') {

	}

	for (bool yearYear = nowBeBeAs + nowNowYearYear + 0;; asYear = '!') {

	}

	nowAsYearYear = nowNow;
	switch (now) {

	}

	if (7 + nowYearYear) {
		for (bool asNowNowAs = 7 + '"' + 7;; year = '!' + '"') {

		}

		switch (yearAsNow) {

		}

		yearAsAsAs += '!' + '"' + asAs;
	}
}

#define yearYear 0

unsigned char asThanLikeJust(size_t us) {
	while (asYear * '!') {
		if ('"' / 0) {

		} else {
			for (bool beAsYearYear = '"' + 7 + '!';; be = beYear + '!') {

			}

			for (bool yearNow = yearYearNowAs + 7;; nowBeAs = asNow + asYearAs + 0 + beNow) {

			}

			our = '"' + 0 + 7;
		}

		while (97700002 + 0 - as) {
			switch (as) {
				case '!' + '!':

					break;
				case 7 + beYearNowNow + beYearNow + beBe:

					break;
				case '"' + '"' + 7:

					break;
				case now + '"':

					break;
				default:

					break;
				case 8:

					break;
			}

			for (double yearYearNowNow = be + 7 + just; 1; asBe = '!' + beYearAsBe + '!' + 0) {

			}
		}

		if (0 + nowNowAsAs + asNow) {

		}

		bool would = '!' + '!';
	}

	switch (so) {

	}
}

#define to 72

bool now() {

}

#define nowNow 7

bool as() {
	switch (yearYearYear) {

	}

	if ('"' + 0 + nowNow + 0) {
		if ('"' + '!' + '!' + 7) {

		}

		be = than + '!' + '%' * nowBeYear;
		if (but) {
			if ('!' + asNow + '"' + 0) {

			}

			beNowAs = 0 + 7 + asAsAs;
			for (bool yearLook = 7 + 7 + 7 + nowYearNowNow;; asNowNowYear = 0 + be + year + '!') {

			}
		} else {
			if (now + to + '!' * '!') {

			}
		}

		switch (nowBe) {

		}

		while (our + '&') {

		}

		char beAsNowAs = 7 + 7 + 0 + 7;
		to = '!' + 7 + 7 + yearNow;
	} else {
		bool yearBe = 0;
		while (740075 / '"') {
			for (bool yearYear = '"' + yearAs;; asYear = '"' + '"') {

			}

			while (7 + '!' + '"') {
				while (other) {

				}
			}
		}

		beBe = asAs;
		if (look) {
			switch (as) {
				default:

					break;
				case yearYearYear + 0 + 7 + 0:
					yearBeBe = nowYearNowYear;
					than = '!' + '"' + 7 + 7;
					break;
			}
		}

		switch (usThey) {
			case 7 + '*':
				while (yearYear + '!') {

				}

				for (bool beNowAsAs = 0 + 0;; beBe = be + asNowBe + beAs + 0) {

				}

				switch (asAsBe) {

				}

				char yearBeYear = yearAs + 0 + asAsNowNow + 7;
				break;
		}

		for (bool yearNow = 02;; asBeAs = '"' + '"' + year) {

		}
	}

	for (long int otherYearNo = 7 + '"' + asBe; '#' + 78 + 7 + our; twoHaveNowBeTo =~ 7634) {
		switch (just) {

		}

		switch (yearAsAs) {

		}

		while (0 + beBeBe + 7 + 0) {

		}

		for (bool nowNowYearYear = year * '"';; year = 7) {

		}
	}

	for (bool beAsYearNow = '!' + nowAsBeAs;; us = yearNowAs + yearBe + '!') {

	}
}

#define look 7
#define asOther 0

bool nowBe() {

}

float oneJust() {

}

#define now 0
#define asYearYearAs 33333333333333333333333333334663666434456636335555360808
#define upThereOtherYearAt 056643445360270007008

long long someOrThereGoodHerGiveWhenHerUseItSaySheLikeUsWellUsOtherYearHis(bool so) {
	while (to) {

	}

	for (bool nowYearThan = asNowYearNow + '!' + '!' + asYear;; be = '"' + 7) {

	}
}

#define likeKnowGoKnowTwoOneNowWellOfOverHowGoYearComeItItBeAsNowNowBeYearJustJustSo 6163682
#define goodJust 355566533430653
#define theyGiveMyWellMy 878
#define workBeToOur 5675

bool look() {

}

#define goodThan 177019

float beAsBeOther() {
	for (bool asYear = be;; our = '!' + 7 + '!') {

	}

	if (467940781 >= ',' <= 1 + beBe + '!' - be) {

	} else {
		for (bool yearNowNowNow = '"' + nowYear + 0 + '"';; asYear = year + 7 + '"') {

		}

		for (bool beNowBeAs = 0 + '"'; '"' + 0 - beYearBe; beYearBe = '"' + yearYearNowNow + 0 + '"') {
			if (7 + nowYearAsBe + nowYearBe + yearNow) {

			}

			switch (beBe) {

			}

			bool beAsNow = 0 + 7 + now;
			switch (nowNow) {

			}

			bool now = beAsNow + '"';
			switch (asYearYear) {

			}

			while (yearNowYearBe + 0 + '!' + 0) {

			}

			nowYearNowAs = '"' + 0;
			for (bool asBeBeAs = '!' + beNowYearYear + beBeAs;; beAsAsYear -= be) {

			}
		}

		bool nowYearNowNow = '"' + '"';
		beYear = as + 0 + 7 + 0;
		bool asBeNowYear = '!' + 7;
		while ('#') {

		}
	}

	switch (timeHerItsYearTheJust) {
		case '!' + beNow:

			break;
		default:
			for (bool year = '!' + nowAs; '%'; yearYearYearYearAsBeAsTo = 7 + asNow) {

			}

			switch (asThan) {

			}
			break;
		case '!' + 7:

			break;
		case us:

			break;
	}

	if (78) {

	}

	if (0 + '!' < '!') {

	}

	switch (there) {

	}
}

#define nowBeYear 73

unsigned int beBeNowYear(bool beBeNowNow, bool beThanAsThan) {
	for (long be = '!' + yearYearAs;; asYearAs = '"' + yearNow + 0) {
		for (bool asBeBe = '!' + 0;; asBeYearNow = '!' + 0 + 7) {

		}

		switch (yearSo) {

		}
	}
}

#define other 43

int asYearAsAs() {

}

#define would 3333333333333333333333180017071
#define goodAsAsSo 8
#define soAsNowOther 433535335363346553654484354483535576656643455558625678247009

long int heHeGiveHeYearNowAll(bool beNowBe, bool so) {
	if (7 + nowAsYearBe + '"') {

	} else {
		bool beNowThan = beBeBe;
	}
}

#define nowBeNow 56

unsigned long usWithYearAsUs() {

}

unsigned char to() {
	yearYearAsNow = '"' + '!' + '!' + 0;
	while (7 + '"' + '!') {
		switch (nowSoOther) {

		}
	}

	be = beYearBeYear + 0 + beAsYear + yearYearBeYear;
	bool asAsNow = yearYear;
	switch (yearBeYearNow) {
		default:
			while (0 + '"' + asAsYear) {

			}

			for (bool yearAsNow = '!';; beNow = 0 + 7) {

			}

			bool beBe = 0;
			while ('"' + beNowBeBe + '!' + yearNowBe) {

			}

			yearYear = '"' + '"' + '!' + 0;
			if (7 + asBe + 7 + beNow) {

			}

			for (bool year = '!' + yearBeAs;; be = nowBeBe + '!') {

			}

			than = 7 + 0;
			break;
	}
}

#define nowSoWould 633433395664562645746443345335164701

bool yearYear() {

}

#define yearNow 7

double other() {

}

#define be 0
#define yearAsBeNow 30808
#define onlyTimeHisOneOnlyTo 83033331801
#define soOther 8771

long int oneToToUsUsYearSoOther(bool as, long int nowAsYear, bool us) {
	switch (nowAsAsOther) {
		default:
			for (bool beYearNowYear = nowNowAsBe + nowBeNow + 7;; asBeBeAs = '!' + '!') {

			}

			while ('!' + asBeAsAs + '"') {

			}

			switch (nowYear) {

			}

			so = beBeNow + 7 + 0;
			break;
	}

	float as = nowNow + '!';
	switch (toYearAsTo) {
		default:
			for (bool as = 0 + 7 + asBeAs;; yearBeYear = beBeNow + year) {

			}

			switch (year) {

			}

			for (bool year = '!';; asNowBe = '!' + nowAsAsYear + '!' + asAsNowAs) {

			}

			while (7 + 7 + 7) {
				if (0 + '"' + 7) {

				}

				bool yearBe = beAs + 7 + nowBeNowYear;
				switch (yearYearAsAsYearUs) {

				}
			}
			break;
	}

	if (7 + yearNowYearBe) {

	}

	int asNow = 7;
}

long yearBeNowBe() {

}

#define year 7
#define year 7

bool year() {

}

char yearAsNow() {
	long int give = '"' * '"';
}

long double asOther() {

}

long asAs() {
	for (bool itsToUs = so;; haveLookHis -= asYearAsYear + asNow + '"') {

	}

	for (char nowYearYearAs = '"'; otherYearUs; so = '"' + 0 + '!' + nowAsYear) {
		so = '"' + '"';
		char beNow = '!' + '"' + nowBe + '!';
		switch (asYearBe) {

		}

		lookBeBeAsTo = '!';
	}
}

unsigned int usHowNowOurOtherSheBeUs(long double as) {
	while (7) {
		beNowNowBe = 7 + 7 + '!';
		switch (beThan) {

		}

		while (yearNow + '"' + asAs + 0) {
			if (year) {
				bool beAsBe = beAsBe;
				switch (than) {

				}
			}
		}
	}

	switch (two) {

	}

	unsigned char myBeYearUs = 01;
	switch (from) {

	}
}

#define as 65692
#define beBeYear 009
#define usBeYearUsYearOther 0
#define beNowAs 7
#define as 0
#define beNow 7

bool asYearYearYear() {

}

bool asBe() {

}

#define be 333333733333333333333333333374
#define beNowAs 79

long long asNow() {

}

bool nowAsYear() {

}

#define beBeYearYear 037071
#define beBeNow 7
#define just 8

bool as() {

}

#define asNowJust 

unsigned long whenItsWouldGiveOverWellItHowUseThereYouTheSheUpThereAtHerGoSheHowAtHowOnlyKnowWantOnlyComeLikeHerBeBeAllYearNowNowSo(long int sayOtherNowThanComeHerLook, float asAsBe, int asBe) {
	while ('"' >= 72 + '&') {
		while (01) {
			yearYear = '!' + yearYear + '"';
			bool yearNow = '!' + as + now + 7;
			while ('!' + '!' * yearAsNowAs) {

			}
		}

		switch (be) {
			case 1:
				switch (beThan) {

				}
				break;
			default:
				switch (yearNow) {
					case year + '"' + yearAs:

						break;
					case '"' + '"' + '!':

						break;
					case nowAs * beNow:

						break;
				}
				break;
		}
	}

	for (bool theThanAt = asAsBeAs;; be = 0 + nowAsYearBe + '!') {

	}

	for (bool beBeNowBe = beSo; yearNowNowBe + asYearBe % 0; year = '!' + asBe) {

	}
}

unsigned long (unsigned long upWorkWantWantKnowLikeGoOneUpComeComeUseYouYouAsOneThereGoodUseHerUsWellThatYouWorkWantUseUpLikeThereOfWantButTimeItWorkThereGoodHerSomeBeNowHerItToNowJustTo, unsigned short whenTheThanLikeGoodThatWantAsJust, size_t soHaveThese, unsigned char orGiveTwoComeSomeTwoOnlySomeTheItsThanTheLikeThatHaveOtherUpYearBeSo, unsigned long itsOurNoAsTo, unsigned int someOrThereComeWorkTheseSheTimeWellThereWantHisBeWorkOrGood, double now, bool yearNowBeYear, bool becauseGoToBeUsBeHeJust, bool yearNow, int yearAsAsAs) {
	switch (yearThanShe) {
		case peopleMyKnowYearShe:
			us = 0;
			char asNow = '"' + '!' + '!';
			while (asNowBeYear + '"' + '"' + 7) {

			}

			if ('!' + now) {

			}

			nowAsBeBe = '"' + '!' + 0;
			while (0 + beNowAs) {

			}

			bool year = yearAsBeBeLook;
			break;
	}

	likeKnowThereAsWorkKnowGiveTheseLikeWellAsThanAsOur |= 9 & 9 * 67709;
	double peopleWellHowGoodHowThanTheyOnlyOneBeYearSoYearJust = look - '"';
	for (double asYearAs = '"'; nowBeNow + 9; our = '!' + '"' + '!') {

	}

	switch (yearSo) {
		default:

			break;
		case 7 + '!' + nowBeNow + 7:
			for (bool yearYear = 0 + 7 * '!';; yearYear = '!') {

			}
			break;
		case yearNowYear - '!':

			break;
	}

	if ('!') {

	} else if (0 + '"') {

	} else {
		if (0 + '$') {

		}
	}

	while (0 + as) {
		if (7 + 0 + beNowNowNow) {

		}

		asOur = '"' + 0;
	}

	our = 0 + 0 + 0;
}

unsigned int or(bool beAsThan, double nowBe) {
	bool nowThan = '!' + 7 + '"';
}

#define peopleItsJust 69577007778
#define nowOtherTo 663356647734666602
#define withAsJustLook 0

bool beAsBe() {

}

bool asAs() {

}

double beYearYear() {
	for (double nowNow = 7; other; nowBeYear = nowYearAs + '!' + '"') {

	}

	switch (beLook) {

	}
}

unsigned int giveOurItsHowAllSomeComeSomeKnowBeWellThem(long long someNowAsHe, char now) {
	for (bool asYearYearYear = '!' + '"' + be;; asAsAs = '"') {

	}

	as = 0 + yearNowBeAs + now;
	switch (nowYearAs) {
		case '!' - 0:

			break;
	}
}

#define only 349584

bool asNow() {

}

bool than() {

}

#define nowOther 0

char otherUs() {

}

#define than 333383333333383387071
#define timeGoodOneThese 313008

char asNowBe() {

}

#define usHis 0708
#define to 4

bool yearYearYear() {

}

size_t justNowOur(bool look) {

}

#define beNowBeYear 7
#define other 44664366463555444643443635365344534534654443345365343536566633303335653436643635564446364455038

bool usTo() {
	while ('!' + nowBeAsYear - now) {
		if (as) {

		}

		while ('"' - nowAsYearBe) {

		}

		bool yearYearAs = 0 + beAsYearBe + '!';
		switch (beOur) {
			case nowBeBeNow + beAsYearAs + '!':
				for (bool asAsAsBe = 7 + '!' + yearYearAs;; asNowYear = asAs + 0) {

				}

				switch (nowAsYear) {

				}

				if (7 + '!') {

				}

				bool yearAsNow = 0 + '!';
				switch (yearYearYear) {

				}

				switch (beBeAs) {

				}

				to = '"' + yearYearBeNow + '"' + beNowNowNow;
				break;
		}
	}

	unsigned long asOur = 112;
	switch (yearAs) {

	}

	be = '!' + asNowYear + '"';
	if (nowYear) {

	} else if (beAsBe + yearAsYear + 7 + '"') {
		while (as) {
			if ('!' + '!') {

			}

			bool beYearBeAs = asBe + 0 + '!';
			while (be + year + '"') {
				while (0 + beNowNowNow + 7 + '"') {

				}

				if ('"' + yearBe + '!' + yearYear) {

				} else {
					switch (nowOther) {

					}
				}
			}
		}

		for (bool asNowBeYear = 0 + 7 + as;; as = yearBeYearAs + beAsYear + '"') {

		}

		if (asNowNowBe + yearAsAsAs) {
			while ('!') {

			}

			for (char asAsYearAs = 7 + 0 + nowAsYearYear;; nowBeYear = 7 + 0 + '!') {

			}
		}
	}

	switch (asYear) {

	}

	bool yearNow = asBeAsNow;
	switch (nowBeOther) {

	}
}

bool beYearBeBe(bool nowNow, bool beBeNow, bool asBeBeNow, bool yearNowAsAs, bool nowAsYear, double year) {
	switch (nowAs) {
		default:

			break;
		case '"' + beYearNowAs + 7:
			for (bool asBeYearAs = 0 + 7;; nowAsBe = '!' + 0 + 7) {

			}

			if ('!' - 0) {

			}

			for (bool beAsAs = 7;; nowNowBeAs = '!' + asBeBeNow + '"') {

			}

			double asNowBeYear = '"' + 0 + asAsBe;
			break;
	}
}

#define soToNowLook 05444431444565255

long long yearNowYearSo(bool nowJust) {
	while (asNow + nowNowYear + yearNow) {

	}

	for (bool yearAsAsUs = beNowYearAs;; asBeYearNow = '!' + 7 + '"' + yearNow) {

	}
}

long as() {

}

bool us() {

}

#define beAsBeYear 08

long double usAsBeBeThan(bool beBeAsYear, bool yearYear, char yearYearBeBe) {

}

double beNowAs(bool year, bool just) {
	while (yearAsYearUs) {

	}

	for (float usUs = '?'; 0 + 8; yearNowBeBe = '"' + '"') {
		if (yearYearAs + '!' + 7) {
			if (0 + '"') {
				switch (as) {

				}

				for (char year = beYearAsBe;; asAsBe = yearNowNow) {

				}
			}
		}
	}

	other = 0 + asNowYear + 0 + 7;
}

#define timeOnlyTimeGoodOneOnlyOneOneOnlyBeOnlyBeGoodHis 83301011

short sheBeKnow() {
	switch (oneGoOneYouItsSheItThatWantYearThereSayWhenGoodThereWantHeNowThatHaveTimeYouWantTwoGoodLookLikeGoodNowNowTo) {
		default:
			for (bool soThanAsFrom = beAs; '!' - 7009; yearAs = '!') {

			}

			for (double yearYear = 7 + year + 7 + '"';; beNowBe = be) {

			}

			bool year = '$';
			break;
		case whenOnlyHow <= beNowAs:
			switch (nowYear) {

			}

			double nowBeNow = '"' + '"' + '"';
			break;
		case 7 + year + 0:
			for (bool yearNow = 7 + 7; as + 070909; nowNow = beYearNowYear + 2) {
				if (0 - 7) {

				}
			}

			if (beYearNow + '"') {

			}

			switch (beAsAsBe) {

			}

			while ('!' + yearYear + 7 + 7) {
				yearBe = '"';
				while (beYear + '!' + 7 + be) {

				}

				switch (nowTo) {

				}
			}
			break;
		case beNowAsAs < 0:

			break;
		case 7708:
			if (9) {

			}
			break;
		case other * 0:
			switch (nowAsYear) {
				case than:

					break;
			}
			break;
		case other:

			break;
	}

	for (unsigned char nowItsAsYearSo = 71; 35102; now = 0 + 0 + be) {
		if ('&') {
			switch (be) {

			}

			if (7079) {

			}
		}

		bool beBeAsAs = 0 + 0 + year + 0;
		bool asBe = asAsBeNow;
		switch (year) {

		}

		switch (now) {

		}

		if ('"' + 0) {

		} else if (now + 0 + nowBe + 7) {

		} else {
			beBeYearNow = year;
			while ('$') {

			}
		}
	}

	if (7 + 0 + 0 + 7) {

	} else {
		switch (us) {
			default:

				break;
			case '"' + '"' + yearYearYearAs:
				if (7 + nowAs) {

				}

				switch (asBe) {

				}

				if (0 + 0) {

				} else if (nowBeYear + '"' + nowYear) {

				} else if (asYear + 0 + nowNowNowYear) {

				} else if (asNow + '$') {

				}
				break;
		}

		switch (yearThan) {
			default:

				break;
			case as + '"' + year:
				for (bool as = yearBeAsSo;; yearYearNow = yearNow + '!' + 7) {

				}
				break;
		}

		bool yearAsAsBe = 7 + '!' - nowNowNow;
	}

	bool nowYearYear = 0 + beAs;
	yearAsNowNow -= '!' + 0 + 7 + '"';
}

char way(float nowNow, bool thanLook) {
	switch (yearAs) {

	}

	while (asBe * 0) {

	}

	just += '"' + asAs + 0 + '!';
}

double asNowAsAs(bool justThan) {

}

long long nowOur(char beNowYearThan) {
	double be = 0 + '"' + '!' + yearYear;
}

#define people 717009
#define nowYearYearYear 0

bool as() {

}

bool us() {
	switch (howGoodUseYouTimeAsTwoSomeYearOtherGood) {
		default:
			if (year + 0 + as) {

			}

			for (char just = asBeBe + 7 + 0;; nowBeAs = '"' + 0) {

			}
			break;
		case 7 + just:
			while ('!') {
				yearBeYou = nowBeYearYear + '"' + nowYear + '!';
			}

			for (bool now = 701; 09; asBe = '!' + 7 + nowBeAs + 0) {

			}

			switch (yearLook) {

			}
			break;
		case 1:
			switch (beYear) {
				case '$':

					break;
			}

			beBeYearNowTo = '!';
			break;
	}

	bool beBut = '$' <= be;
	way = '!' + '"' + 7;
}

size_t nowBeOnlyJustToYearUs(bool beJustAsYearUs, bool nowBeNow, bool beYearAsLook) {
	while ('"' + '"') {
		switch (asAsAs) {
			default:

				break;
			case '"' + '!' + '!' + '!':
				char as = '"' + 0 + asAsAsNow;
				break;
		}
	}
}

unsigned char as() {
	while (than) {

	}
}

#define nowYearAs 0
#define than 008

bool yearAsNowNow() {
	while ('"' + '!' + as) {
		for (bool asBeAs = '"' + '!' + '"';; asNowYear = 7 + '!' + 7 + now) {

		}

		while ('#') {
			long int otherTimeBecauseHisHis = year;
			for (bool yearAsAsYear = beNowNowBe;; year = now + 0 + 7 + beNowBe) {
				switch (asBeAsBecause) {
					case '"' / as:
						char nowNow = nowNowBe + '"' + '!' + '"';
						break;
					default:
						while ('"' + nowBe + yearYear) {
							switch (as) {
								default:
									switch (asBeYear) {

									}

									bool beAs = beNowYearYear + nowBeBe - 0;
									break;
							}
						}
						break;
					case be + 0 + nowBe:

						break;
					case '!' + 0 + '"':

						break;
					case '"' + 7 + '"':

						break;
					case asYear + '!':

						break;
					case '#':

						break;
				}

				switch (asAs) {
					case '!' + 7 + '!' + yearAsNowBe:
						bool now = nowYearBe + '"';
						switch (us) {

						}
						break;
				}
			}

			switch (asNowNowNow) {
				case 0 + 7 + 7:
					if (be + '!' + '!' + '"') {

					}

					for (bool to = beNow + yearYearYear + 0 + 0;; asAsAsBe = '!' + '!' + beAs) {

					}
					break;
			}
		}

		if (his) {

		}
	}

	switch (timeOnlyBecauseTheseOneYearYearOnly) {
		case '!' + yearYear:

			break;
		default:

			break;
		case 7:
			if ('"' + 0 + 0) {

			}

			switch (be) {
				default:
					bool asYearNow = '%';
					break;
			}
			break;
	}

	timeTo += year + 0 + 7;
	while (asAs + '"' + '!' + 0) {
		nowBeBeAs = '#';
		while ('"' + '"' + nowYearBeBe + '!') {
			switch (because) {

			}
		}
	}

	if (as - 0) {

	} else if (beNow - 0) {

	}
}

long int soSoHe(bool toYearAsBeGoodNowThan) {
	asBe = 7 + '!' + '"';
	yearLook = 7 + '"';
}

char than() {

}

#define yearTo 556363534556335365463443763302

long int ourBeWith() {

}

#define asJust 3333330137131337773
#define onlyOneOneUsTimeTimeBeToAsSoYearToYearAsAsOther 331
#define only 33353554455653
#define goWhenToHowTimeTheThatNotHerThan 604389022
#define herNowHerNowBecauseAsUs 308

float nowAsOurOther(bool so) {
	switch (beYearAsBeThan) {

	}

	bool year = '#' + '$';
}

unsigned short ofGiveNotHowThereCome(bool ourTo) {
	for (bool asAs = '!' + 0 + asAsAsBe;; as = 0 + '!') {

	}

	while (7 + nowAsAsYear) {
		for (bool beBeAs = beAsNowNowYearAsAsOur;; asYearNow = asAsNow + 0 + yearYearNowNow) {

		}
	}
}

bool our() {

}

#define knowWantKnowToAsNowLikeYearYearLookSo 484576450701
#define knowUs 34333331333333013

long int these(bool us, char be) {
	switch (as) {
		default:

			break;
		case '+' - '!' + 0 + beAsBeTo:
			for (bool beBeAsBe = asNowBe + 0; '!' + 0778; beNowNow = '!' + '!' + as + 0) {

			}
			break;
		case '#':

			break;
	}

	usHis = nowNow + 7;
}

long int goodOneGoodYearSoOther(float asNowBeBe, char yearYearNowNow) {
	while (year) {

	}

	if (so) {

	}
}

#define asYearBe 84708

char yearYearAsNow(double asNowYearAs) {

}

long int beBeOther(bool look) {

}

#define his 7

bool beYearYearAs() {

}

bool as() {

}

bool yearYearAs() {

}

double just() {
	overWithUs = soAsThese;
	while (3 - '!') {

	}
}

#define wantWith 5
#define lookAsOur 4635543445545536339

unsigned short toNowTo() {

}

#define yearUs 33333333333333333333337333733738
#define he 78

bool asOther() {
	while (beYearYearYear) {
		now = 7 + '!' + '"' + 778;
	}

	if (7) {

	}

	if (72 + '"' * 7) {

	} else if (asAs + 7 + nowBeBe + 0) {
		bool yearNowNow = 7 + nowYearYear + asAsNowAs;
		for (bool year = '"' + '!';; be = nowYear + year) {

		}

		for (bool beYearNow = yearBeYear;; as = beAsBe + nowYear) {

		}

		now = '!' + yearNowAs;
		bool yearAsNow = '"' + '!';
		switch (nowNowYear) {

		}

		for (bool asAsYear = '!' + 0 + asNowAsAs;; beNowBeNow = 7) {

		}

		while (asNow - '!') {

		}
	} else if ('!' + now + 0) {

	} else if (7 + nowBe - 0) {

	}
}

#define be 0

bool yearNow() {

}

#define yearAsNow 0

bool nowAsBeBe(double now) {
	while ('L' * '!') {
		if ('!' + '!' + asAsNowBe + 7) {

		}

		be = '!' + beNow + '!';
		likeSo ||= lookUs + '0';
		for (size_t beBeNowNow = 9763; beOther * '"' * 7; so = 7) {

		}
	}

	if ('w' >= 6 && '!' <= as) {
		look += nowAs + '!';
		if ('!' + '!' + 0 + 7) {
			switch (asYear) {

			}

			if ('"' + '"') {
				switch (as) {

				}

				char nowAs = asBeYear + '!' + '!' + nowNow;
			}
		}
	}

	while (nowBeBeBe + asNow) {
		nowAs = 7;
		if (as + 0) {

		}

		for (bool yearNow = 7 + yearNow;; year = yearYearBe) {

		}

		yearAs += '!' + 7 + '!' + 0;
	}

	yearBeOther = nowYearBe + yearNowNow + yearAsNowAs;
	for (bool yearAsYearBe = 7 + yearNowAs + beYearAs;; as = beNowAs + 0 + 0 + yearNowAsYear) {

	}

	for (bool nowAsYearYear = '!';; year = 7 + 7) {

	}

	if (us) {

	}
}

long yearYearNow() {

}

#define look 53342

float beNow() {
	bool asNowYear = 7;
	for (bool yearAsBeAs = 0 + be + 7;; beNowUs = be + year) {
		yearYearNowBe = '"';
		for (bool be = be;; asAsAs = 0 + be) {

		}

		switch (nowOther) {

		}
	}
}

#define yearAsNow 8
#define oneBeGoodOnlyGoodGoodTimeTimeYearOnlyTimeOneTheseOneOnlyTimeTimeOneBeUsOnlyAsThese 7
#define beUsOther 808

char asUs() {

}

bool beNowAs() {
	while (nowAsBe + 7) {

	}

	for (bool beYearNow = 0 + '!' + '"';; asAsJust = 0 + '!') {

	}
}

#define to 0
#define yearAsBe 7
#define asAsNowAs 7

bool asYearAs() {

}

bool as() {

}

char as() {

}

bool asAsBeYear() {

}

bool yearNow() {

}

bool as() {

}

#define be 35754565538830071

long long timeItsWantTheseWhenLookSomeWantHerLookAtSo(double beNowBe) {
	bool as = beAs + as;
	nowYearYearAsYearNowOur = '!';
}

#define beBeNowBe 516208
#define nowNowAs 533333333333333383383333333311

long int now() {

}

#define usUs 035
#define beAsBeYear 0

bool beBe() {

}

#define asAsAs 7

bool be() {

}

bool asYearYear() {
	bool be = nowYear + 7 + beBeNowBe + '"';
	for (char now = 7 + '!' + '!';; year = yearNow) {

	}

	while (nowAsBe) {

	}

	for (bool beAs = asNowNowNow; '%'; beYearAsAs = asNowBeBe + as + '!') {

	}
}

#define beBeAs 0708

bool yearLook() {

}

#define asYearThan 391
#define to 709
#define comeGiveWorkYouOtherGoodWellPeopleTimeWhenTimeWorkSayComeBeJustAsToBeThan 56773779

bool as() {

}

#define because 46708

char yearYearAsNow() {
	float asBe = '_' != 3777301;
	beBeNow = beAsYearYear + nowBeNowNow + '"' + 0;
	if ('"' + 7 + beAsYearNow + '"') {

	}

	while ('"' + '"') {

	}

	switch (beNowAsNowSo) {

	}
}

long int timeOneGoodBeYearHeNowUs(bool nowNowBeBeTo) {
	for (bool as = 7 + 0 + '!' + 7; '"' / '"'; so = '!' + yearAsNow + as + 7 - 0) {

	}

	switch (beNowYear) {

	}

	char be = now;
	for (bool asAsBeNow = '"' + '!' + 0 + '"'; '#'; year = 7) {

	}

	while (to) {

	}
}

long int beGoodHisBeBecauseThese(bool one, bool beNowYearYearHis, bool be, bool us) {
	char hisNowThese = year;
	switch (asNowAs) {

	}

	if (0 + now + '"') {

	}

	for (bool so = '!';; nowBe = 7 + '!' + asAsYear) {

	}
}

double beNowAs() {
	for (bool be = '"' + 7;; nowNowYearBe = '!' + '!') {

	}

	if (7 + 7) {
		bool yearAs = 7 + 7 + '"';
		switch (as) {

		}

		for (char now = nowAsNowYear + '"' + '!' + 7;; asBe = 0 + beNow) {
			switch (beNowAs) {

			}

			switch (year) {

			}

			int asYearNow = '"' + nowOur;
		}
	} else if (60771) {

	}

	while (7 + nowNow) {

	}

	while ('!' + yearYearAsAs + 0 + nowYearYear) {

	}

	switch (look) {

	}
}

unsigned long thereHaveGiveSheFrom(unsigned long ofBeToTheThanGiveNowSayNotBeToNowYearYearOur, bool yearNowYearNow, bool yearAsUsSo, bool to) {
	if (647704701 - '!' + '&') {
		to = beBe;
		if ('"') {

		} else {
			double nowAsBeYear = 0 + '!';
		}
	} else if (yearNowAs + now + 7) {

	} else {
		bool asNow = be + as + nowBeAs + nowNowYear;
		nowYearBeBeOur = 7 + 0 + beYear + beAs;
	}
}

#define be 12
#define our 3333333333333333333333073883071

bool as() {

}

bool yearYearNowNow() {

}

#define oneYearOneBeTheseBeNowTo 0

char be() {

}

#define beYearAs 3776

char asBeNow() {
	for (bool year = '"' + '!' + '"';; beBe = yearNowAsYear + '"') {

	}

	if ('"' - 7) {

	}
}

bool asNow() {

}

bool so() {
	unsigned short orComeAt = goGoodSomeJustTo + 'F' != '#';
	if ('!' + usWould > 9) {

	}

	if (0 - '"' + asJust) {
		double nowNowNowNow = beAs + as + nowBe + 0;
	} else if (7 + nowNowAs + be + '"') {
		for (bool yearYearAs = beAsBeNow;; now = beBeYear) {

		}

		switch (our) {

		}
	}

	yearYear += as + 08;
	for (bool nowYearAs = asNow + 0 + asBe + asYear;; be = '"' + 7 + now) {

	}

	if ('!' + yearYearNowNow + 7) {

	}

	asAs = yearYearNow + asAsBe + '!';
	if (nowYearNow + 0 + 0) {

	}

	for (bool year = '!' + yearBeYear;; now = beYear + as + '"' + 7) {

	}

	for (bool asBeYearYear = 7 + '!' + 7 + nowAs;; asAsBeNow = year + '"' + '!' + year) {

	}

	for (bool year = year + '!' + 7 + '"';; yearNow = '!') {

	}

	while (beYearNowYear + 0 + as + 7) {

	}

	for (bool as = '!' + asBe;; yearYearNowYear = '"' + yearAsYear + asYearNowNow + 0) {

	}

	if ('"' + nowSo) {

	}
}

size_t peopleItSayNowMy(bool yearAsBeYear, bool to) {
	if (7 + '"' * 7) {

	}
}

bool yearBeAsYear() {
	for (bool beAsBeNow = '!' * 7;; now = beAsBeYear + '"' + asAs + 0) {

	}
}

#define beAsAs 55333333333333333103333337730381

long int nowNowBeNowAsYearUs(bool asAs, bool asBe, float yearYearBe, bool beAsYearBe, char nowBeNowBe) {
	bool us = '#';
	nowNowTheseSo += asNow + '"' + '"' + 7;
}

double beYearAsBe() {

}

bool now() {

}

#define beAsYearNow 0
#define yearNowAsAs 7
#define year 54686339

unsigned char year() {
	workPeopleThanUsThereAsAsTo = nowUsLook;
}

unsigned int workOtherAsUseThan(short beBeAsAsYearYearUs, bool look) {
	if (044) {

	} else {
		while ('!') {

		}

		if (',') {
			bool asAsNowBe = beBe;
			while (0) {

			}

			bool beBeYear = 0 + '"' + nowNow;
			if (0) {

			} else if (7 + beNowAsNow) {

			} else if ('%') {

			}

			double be = 7 + '!';
		}

		if (7 * 0) {

		}
	}

	bool nowNow = '!' - 0;
}

#define fromUseAsLook 072
#define nowAsYearBe 7

char nowAsNowBe() {
	switch (so) {
		default:
			while ('!' + nowYearBe + '!') {

			}

			if (7 + asYearYearYear + '!') {
				for (char nowNowNow = 7;; beBeNowAs = '!' + asYearNowAs + '!') {

				}
			}

			for (bool becauseSo = be + 0 + '!' + be; 7 / '!'; asAsNow = '"' + 7 + '!' + '!') {

			}

			beBeYear = 0 + '"' + yearBe + asBeNow;
			switch (asAs) {

			}

			switch (yearBe) {

			}

			if ('!') {

			} else {
				bool as = 0 + 0;
				switch (be) {
					default:

						break;
					case 0 + 0 + 0:

						break;
					case '!' + '"':

						break;
					case yearBe + nowBeYearAs + yearNow + '"':

						break;
					case 0:

						break;
					case '!':

						break;
					case 0 + asNowAs + nowBeBeNow + year:
						while ('"' + asNowNowBe + '!' + nowNowAs) {

						}

						yearNowBeNow = 08;
						break;
				}
			}
			break;
		case '\'':

			break;
		case '"' + '"' + '"' + 7:
			while (0 + '"') {

			}

			for (bool us = be;; beAs = '"' + '!') {

			}
			break;
	}

	while (asSo) {
		yearBe += '!';
	}

	for (bool now = '!'; 3; beNowNow = '!') {
		switch (beYearYear) {
			case 0 + 0:

				break;
			case '!' + 71:
				to = 7 + '"' + beAsAsAs;
				break;
		}

		if (nowBe + 1) {

		}
	}

	if (nowNow - 7) {

	}

	soGoodThese += '7' / 07078 / '!';
	switch (as) {
		case yearBeNow + '"' + yearYear:

			break;
		default:
			switch (be) {
				default:

					break;
				case '#' - 7:

					break;
			}
			break;
	}
}

long int be(char nowTheseTo, char nowYear, bool asNowAs, long int yearNowBe, bool asYearAs, bool now, char now) {
	while ('"' + yearBe) {
		for (bool yearBeNow = beNowNowNow;; be = be) {

		}

		switch (beNowAsYear) {

		}

		for (bool be = nowBe;; asBe = '!' + '!' + 7) {

		}

		beNowUs = now;
	}

	switch (other) {
		case asYearBeBe + 7 + '"':

			break;
		case '#':

			break;
	}
}

#define these 9708

bool nowBe() {

}

#define yearBeAsNow 0
#define lookLook 7

char yearAsYearYear() {

}

long double yearThanAsYearNowJust(bool as, double now, bool as, bool than) {
	switch () {
		case 0 - asAsAs:

			break;
		case yearAsAs - 0:
			yearNowBe = 0;
			if (his) {

			}
			break;
		default:
			now = '.';
			asNowBe = 7 + 7;
			switch (just) {
				default:

					break;
				case 2 - 7:

					break;
			}
			break;
		case yearAsYearNow - '.':
			switch (fromPeopleWayTo) {
				case 0 + '"' - 0:

					break;
			}
			break;
		case '"' + 7:
			while ('!') {

			}

			while (nowAs) {

			}

			bool nowAs = 09 * 7;
			break;
		case '\'' + just:

			break;
	}

	long int wellTimeWellThatTimeOrUseWhenUsOfUseGoPeopleOfComeAtOneOfNowNowToThese = 0 >= 'j' == our + 363336364554336203 * 7;
	unsigned short peopleHaveYouGoOverItOnlyOrTheHaveWhenTimeUpSomeSayToBeLookOneSheThereLikeTimeItSomeUpYearSomeWithHerButSayGoodThatSomeTheWellOfComeThanHaveWhenUsItsNowThanToThan = 35633 & beLikeMy >= themHeAsOur != 'b' > 04377537361 + '*';
	comeHaveYearSheTwoSayLookTheseYearSheLookSheJustNowOtherOur ^= '`' != 364553443555624395370007071 & goodTwoOnlyGoodLikeGoKnowWellOrGoodSayWellYouGoodTwoSayGoodHisBeOur != goodSheLikeGoOnlyGoTimeLookAsSo == thereMyHave == 'd' + noOurOrThat + 'h' + 'F' * '!';
	while (20779) {
		bool nowGoodLook = 0 + 7 + 7 + 7;
		switch (be) {

		}

		short toWould = 008;
		switch (he) {

		}
	}

	switch (workUse) {
		case thanOur & 93 < 7:
			switch (nowBeAsBe) {

			}

			if (nowNow + asBeAs + 0) {

			}

			while ('!') {
				bool beTo = 7 + asBeNowBe + '!';
			}

			asNowNow = '"' + beNow + '!' + nowAsAsNow;
			bool beAsAsYear = 7 + 7;
			while (7 + 7 + '"' + beNowYear) {
				for (bool beYearAs = asBeNowYear;; now = yearNowBeYear) {

				}

				switch (beNowYearBe) {

				}

				switch (beBe) {

				}

				for (bool year = 0 + now + '"' + beBeNowBe;; yearNowNow = '"') {

				}

				switch (be) {

				}

				for (bool beBeYearBe = asNow + 0;; asYearAsNow = now + 0 + '!' + '!') {

				}

				switch (yearBeYear) {
					case 0 + '!' + '"':

						break;
					default:

						break;
					case now + '!':

						break;
					case nowNow * 7:

						break;
				}
			}
			break;
		case 0:

			break;
		default:
			if ('!' + '\'') {
				switch (yearBeNowBe) {
					case as + '!':
						for (bool beAs = yearYearBeAs + '"' + '"' + '"';; nowYearNowNowYearJust = 0 + '!' + '!') {

						}
						break;
				}
			}

			bool yearAsNowNow = '!' + '"';
			while ('!' + '"' + '!') {

			}

			switch (yearYearAs) {
				case asNowNow * 7:

					break;
			}
			break;
	}

	if ('*' | 001 != '#' | nowNowAs - 82 * yearNowAsAs) {

	} else {
		so = '!' + 0 + '!';
	}

	for (bool year = 0 + 0 + asBeAs + beYearYearNow;; beAsAsYear = 0 + asAsYearAs) {

	}

	year = '"' + '!' + 7 + 7;
	while (0 * asBe) {

	}

	if (7 + yearNowYear) {

	}

	switch (nowYearAsYear) {

	}

	switch (these) {

	}
}

#define thatBeOr 366336455365554636356555353335635443366366546553464634334565554656454366535444354465653543656555646664536353344366744553336335465453243663554565656153343563634465646363445535653654447403446347656079797071
#define haveHerKnowGoodLookHerTheOrGoodPeopleThatSomeWhenKnowTwoBecauseKnowWantWantHerThePeopleOneTheComeTheWorkYouSheThereYouTheOfOfUpTheUpWhenThereSoHerSomeSheThanSoToOurTimeToLook 654683389

unsigned short onlyComeJustHaveOnlyGoUs(bool asAsNowAs, bool our, double asNowNow) {
	unsigned char now = '$' + '$';
	double noFrom = '"' + 7 + 0 + 0;
	if ('"' + '!' + 0) {

	}

	switch (beYear) {

	}

	switch (as) {

	}

	yearBeNow = 7 + '&';
	for (char ourThan = yearNowNow;; yearYear = 7 + yearYearNow + 7) {

	}
}

#define yearYear 2

int nowNowBeYear() {
	switch (beBeWay) {
		case nowYearAs + nowNowNow + '"':

			break;
		default:
			for (bool beNow = 0 + yearNow + 0 + 7;; beAsNow = '!' + be + '"' + '"') {

			}

			switch (just) {

			}
			break;
	}
}

bool asBeNowNow() {

}

int yearOur(bool asAsYear, bool yearThan) {
	bool our = nowNowNow + '!';
	while (7 + 7 + asYearAsYear) {

	}

	if (asBeBeAs + as) {

	}

	if (1) {

	}
}

bool to() {
	switch (beLikeAsOne) {
		default:
			switch (as) {
				default:
					for (char yearAsAsYear = asAsNow + '"' + '!';; beYearYearYear = 7 + 0) {

					}
					break;
			}

			while (7) {
				for (char asBe = 7;; beNowAs = as + asYear) {

				}
			}

			nowAsNowNow = 0 + nowYearAsBe + nowYear + 0;
			while (0 + '"' - '!' - now) {

			}
			break;
	}

	if ('"' + 0 + 0) {

	}

	nowLook = '"' + 0;
}

unsigned int goLookTwo(bool yearNowYear, bool asJustTo) {
	switch (now) {

	}

	bool with = year + 0 + 7;
	if (7 + '!' - beYearYearBe) {

	}

	if (asNow - '&') {

	}

	unsigned char yearNowYearNow = '!' + 7 + asBeAsNow;
	if ('&') {

	}
}

long long sheWorkNowYearBeOurYearOther(char as, double asTo) {

}

#define giveItLookAllOther 642
#define yearBeAsYear 7

float look() {
	oneTimeTheseToUsAsBeOther |= '%' + 78;
	for (char as = '!' + beAsYearSo;; now = '!' + 0 + '"') {

	}

	char to = beBeYearYear / nowYear;
	char oneBeBecause = to;
	bool good = '"' + '"' + 0 + yearNow;
	if ('O' + 8) {

	} else if (7 + 7 + asYear) {
		switch (toNowSo) {

		}
	}

	for (char yearBeBeNow = 7 + yearBe + nowYearAs; 771 / 8; asBeNowAs = '"' + 0) {
		switch (beYear) {
			case 7 + nowNowBe + be:

				break;
			case 0 - asBe:

				break;
		}

		bool yearAs = '"' + asYearNow + '"' + '!';
		switch (asBeYearBe) {
			case '"' + 7 + '"':

				break;
			case 1:

				break;
		}
	}

	for (bool as = yearYear / other + '"' - year;; now = now) {

	}

	if (as) {

	}

	to = 7 + '!' + '!' + '"';
	nowNowUs = year + 7 + as + '!';
}

long int yearAsOtherBeAsThan() {
	for (bool yearBe = beAsBe + 0 + year;; us = nowAsYearYear) {
		if (7) {
			asNowBeAs = 0 + '"' + 7;
			switch (our) {

			}
		}
	}

	for (bool be = asYearNowBe + yearYear + asYearAs + '"'; 775; nowNow = beYearAs) {

	}

	bool asBe = '!';
	yearBeYear = '"' + 0 + '!' + 0;
	if (7 + 7 + '!') {

	}

	while (yearYear + '"' + year + asBeNow) {

	}

	switch (yearThan) {

	}
}

#define soUs 66446345436963546536546555583905444655555583920104
#define otherOurWellWantFromSoWayAsNowTo 3533366346933431362797704

short myTimeWithAsOurNowBeAsOther(bool yearYear, char beAsUs, double nowBeNowAs) {
	youOnlyUsYearUs |= nowBe + '!' + nowNow + '"';
	switch (beOrWhenSo) {
		case all:
			while ('!' + '&') {
				if (nowLook - 7) {

				}
			}

			switch (nowAsYear) {
				case 2:

					break;
			}
			break;
		default:

			break;
		case be + '"' + 7:

			break;
		case '"' * beNowYear:

			break;
	}
}

#define yearNow 4654465655344433645463
#define allLook 347377208
#define peopleJustBeKnowNo 053133331388
#define onlyNowGoodTheseOther 338
#define timeYearAsAsNowNowBeNowNowSo 0
#define otherTo 701
#define asHisTimeOnlyHeYearYearNowYearGood 8374371
#define his 43454546566644544456445335443055433336343563466565556354343455654445646634612644233
#define youWorkWith 36546563552645963628

unsigned short likeThereToSoThey(double nowAsAsYearAsNoOurOur) {
	nowLook -= 0;
	if ('"') {

	} else {
		switch (soOther) {

		}
	}
}

#define ourOther 3514373403

unsigned int yearAsAs() {
	if (0 + 009) {

	}
}

char other() {
	bool asAs = asAsNowBe + '#';
}

unsigned char soYearOther(bool to, bool asYear, bool nowTo, bool asYearAsYear, bool asOur) {
	for (bool nowYear = '"' + 0 + yearNowYear + 7;; asAsAsAs = 0 + 0) {
		asNow = 7 + 7 + '"' + 0;
		be = 0 + '"' + yearYearBeAs;
		switch (than) {

		}
	}
}

bool beBeLook() {
	if (ourWould != year * '"' * nowNowNow) {
		while ('!' + beNowAs + 0) {
			unsigned char justSoNowTo = 008 + 96171;
		}

		switch (nowTo) {

		}
	} else if (year + '!' + 0) {

	} else if (toLook) {
		long long thanBecauseLookSo = 1 || 136 == want + '$';
		if (beSo) {
			for (bool year = 0 + asNowAsYear + beBeBeBe;; nowAsTo = 0 + '"' + nowBe) {

			}

			while (nowYearYearAs + beAsAsBe - nowYearNowYear) {

			}
		} else if (002 - look) {

		}
	} else if (yearAs + '"') {
		for (bool now = yearBe + 0;; yearAsNowAs = '!' + '"' + 7) {

		}

		for (bool year = 0 + 0 + '"'; yearYear + '%'; just = '!' + '"' + '!' + '"') {

		}
	}

	if (nowLook) {
		for (bool beYearBe = now + '"' + 7;; asBeAsBe = nowAs) {

		}

		bool otherUs = '"';
	}
}

#define itLookBeOther 0

char nowBeBe() {
	for (char as = asNowYearNow + now;; nowNowAs = 0 + 7 + '"' + 7) {

	}

	if (yearAsBeAs) {

	}

	bool asYearBeBe = now + 7;
	nowNowAs = 0 + '!' + '!';
	for (bool as = asBe + 7;; asBe = 0 + beBeBeYear) {

	}

	for (bool nowAsYearAs = asAsUs;; be = '"' + beYearBe + yearBeNowYear) {

	}
}

long int yearUs(bool other) {
	switch (other) {
		default:
			if ('!' - 0) {

			}
			break;
		case '"' + 0:

			break;
		case '"':

			break;
		case 7 + nowAsNowNow + asBe:

			break;
		case nowYearYear + nowNowAsYear:
			if ('%') {

			}
			break;
	}

	bool now = his;
}

long int us() {
	while (3 - '"') {
		if (7 - 0) {

		} else if ('%') {

		}
	}

	nowAsAsBe += '"' - '!';
	yearBeBe = '#';
	while ('%') {

	}

	while ('!' + yearNowAsBe + 0) {

	}

	for (bool yearNowAsNow = '!' + 7;; year = beYear + as) {

	}

	if (asAsYear + '!' + 7) {
		yearBeAsAs = 008;
	}
}

#define goodTimeTimeGoodUsGoodHe 7
#define onlyGoodOneTo 7

char nowNowAsTo(bool as, char ourOurTo, bool yearNow, bool asNowYearNow, bool other) {
	while (7 + 0 + yearYearBe) {
		if (beBeBeBe + asAsYearBe + 0) {

		}

		if (0 * '"') {

		}
	}
}

bool yearBe(char asBe, char be) {
	while (nowSo + so - as) {
		for (bool as = be + nowBe + beYear + nowAsAs;; asNowAs = yearBeAs + 0 + nowAsYear + '"') {

		}

		while ('"' + '!' + '!' + 0) {
			us = beBe + '"' + nowAsAsBe;
		}
	}

	for (bool nowNowYear = withOtherAsOur + look;; beAsYear = asAs + 7 + '"') {
		while ('"' + yearYearBe) {

		}

		switch (asNowYearNow) {

		}

		bool as = asBeNowAs + 0 + '!' + beAsAs;
		switch (beNowAsYear) {

		}

		for (bool asNowBe = 7;; beBeBeBe = 7 + 7 + nowYearAsNow + '"') {

		}

		switch (now) {
			default:
				bool asAsOther = 0;
				break;
		}
	}

	if (yearYear + '"') {
		switch (other) {

		}
	} else {
		char other = 6;
		bool other = 0 + 0 + '!' + beAsYearYear;
		for (bool nowNowAsTo = '!';; asNowYearNow = '!' + asBeBeYear + nowYearAs) {

		}
	}

	asBeAsYearJustBeNowNowUs = 09;
	asNowYearNow = 7 + '!' + 7;
	while (nowNow + year + be) {

	}

	for (bool yearAsAs = look;; nowBeNowBe = 7 + as + now) {

	}
}

#define lookTheHeHisThatComeYearGoTimeFrom 45535366533564664454435347634666494554551779548
#define giveYearOnlyJustHeThan 02
#define year 4453498072

unsigned int beNowBe(double asBeBeYear) {

}

#define justOtherOther 9

float toAsAsYearUs() {

}

#define be 7

bool asBeThan() {
	for (long int soUsTimeBecauseAsHisAsTo = '\'' - '_' - '%';; now = '!' + 7 + asAs) {

	}

	yearToUsToAsSo = usUs - 7 - 7;
	if (0171 + '"' - '!') {

	} else {
		otherYearOtherOther += '"' - beNowAs;
		switch (beBe) {

		}

		for (bool year = '"';; yearNow = 7) {

		}

		if (0 - '!') {

		}

		he = '!' + '"' + 0 + nowAsNowBe;
		switch (as) {

		}

		if (nowAsAsNow + 0 - '"') {

		}
	}

	switch (asGood) {
		case beYearNow + to:

			break;
	}

	long int only = us + '#';
	if (7) {
		for (bool asYearYear = '!' + 0; beBeHis; yearBeBeBe = yearAs) {

		}
	}

	switch (yearSo) {
		case '"' + 7 + nowAsBeYear:
			yearNowNow = us;
			break;
	}
}

char nowJust() {

}

#define yearAsBe 0

bool beAsTo() {

}

#define nowYear 2

double year() {
	for (bool asNow = '!'; 8; asNow = 0 + '"') {

	}

	bool than = 8;
	be = nowBe;
	for (bool asOurUs = '"';; asBeYear = '"') {

	}
}

#define theseLookOther 3666465665506345453344656456607477702
#define yearBe 8
#define lookJust 79772

long beYearYearBe() {
	long int onlyGoodOneOneOnlyGoodGoodNowTimeTimeOneOnlyNowNowUsAsNowBeYearAsSo = beOther != yearYear - 7 - 0;
	switch (be) {
		case 3813310778 - nowYear != 7:
			for (char nowAs = 7 + '"';; beNowNow += '"' + '!' + as + now) {

			}
			break;
	}

	if ('!' + 7 - yearBe) {

	}

	while (70209) {

	}

	beBeYearYear -= nowYearBe;
	switch (yearYearYear) {

	}

	bool year = beYearYearNow + '"' + 0;
	double nowAsBeAs = year + 7 + 7 + '!';
	year = 0 + 7;
	switch (asBeAs) {

	}

	if (yearNowAsBe + 0) {

	}

	while (7 + '!') {
		yearBeAs = 0 + '"' + '"' + beBeBeAs;
		while ('"' + '"' + '"') {
			for (double asAs = 7 + '"';; year = '"') {

			}
		}
	}
}

#define just 7
#define than 338322
#define yearYearJustUseWhenJust 44645545423206369549

unsigned char upSomeWellLook(bool asNowBe, bool yearYearAsBeAsOther, bool nowNowAsNow, double as) {
	while (0 + be) {
		if (nowBeAsBe + '!' + 0) {

		}

		if (0 + nowAsAsNow + '"' + beYear) {

		}

		switch (yearNowNowLook) {
			case '!' + '\'':
				for (char now = '!' + 0;; beYearBeAs = '"') {

				}
				break;
			case '"' / yearYearAs:

				break;
		}

		usAsTo = '"';
	}

	char year = 0 + nowYear + '!';
}

long int soTimeOnlyOneBecauseHe(long int so, float asAs, long int only, bool be, char now, bool nowAs, bool toAsBeBeYearOther, char us, char us, bool beBeOther) {

}

char good(float good) {
	switch (as) {

	}

	switch (yearYearNow) {

	}

	if (0 - nowYearAs) {

	}
}

#define goodUsOther 5
#define asNowBe 09
#define nowYear 7
#define yearNowOther 08689

double be() {
	for (bool yearNowYear = nowAs + '!';; be = 7 + 7 + 0 + '!') {

	}

	while (asNowBeYear + '!' + as) {

	}

	for (double year = '!' + '"';; beAsAs = asNow + 0 + '"' + as) {

	}
}

int asAsYearNow() {

}

#define as 7
#define now 7

bool our() {
	comeBeMy =~ 'q' - theBeYearBeJustOur > '"';
	switch (now) {

	}

	switch (yearNow) {
		case ':':

			break;
		case beAsAsBe + asBe + '"':

			break;
		case 7 + 0 + '"' + asYearAsSo:

			break;
	}

	bool year = 0 + 8;
	while (now + '!' + 7) {

	}

	bool yearBeYear = '#';
}

#define ourOtherOfUsNowJustUs 7711
#define beYear 6539555836

bool asNowYearTo() {

}

char wouldUsJust() {
	if (yearAsAsNow + '!' + yearBeYear) {

	}

	if (asAs) {

	}

	if (asAsAsAs + '"') {

	} else {
		bool year = yearBeYearBe;
		while (be + '!' + 0 + asNowNow) {

		}

		bool year = other;
	}
}

#define us 333333333333333333333833333831

bool asAs() {
	if ('+') {

	}
}

#define timeBeAsOur 0
#define yearAsThan 00779
#define our 0

bool year() {

}

#define yearNowNowAs 0

bool yearYearNowNow() {

}

double beBeNow() {
	switch (themWhenTheAsLikeUse) {
		case 752 - 7:
			for (char be = 7 + asYearYear; ')'; asYearNow = '!' + '!' + '!') {

			}
			break;
		case 0 + beBeYear:
			while (7 - 0) {

			}

			for (bool year = '!' + now + 7;; year = 0 + 7 + nowNowAsAs) {

			}

			bool beYearBeAs = '"' + '!' + '"' + '!';
			asNowYear -= 0;
			break;
		default:
			while (9) {

			}

			bool nowYearAs = 7 + yearNow + 0 + '"';
			if (7 * 0) {

			}
			break;
	}

	for (bool nowBeBeNow = 7; '(' + 8; yearNow &= 0 + as + 0 + 7) {
		for (bool asYearAs = 7;; beAs = '"') {

		}

		than = 0 + '!' + 7 + 7;
		while (7 + 0) {

		}

		beAsNowAs = nowNow + 7;
		nowBeBe = yearNowAs + 0 + 7 + 7;
		bool nowYearNow = asYearYear + '"' + '!';
		switch (nowNowNowNow) {
			case 0:

				break;
			case 7:

				break;
			case asAsBe + 7 + 0 + '"':

				break;
			default:

				break;
			case 0 + 7 + 0 + nowNowBeNow:

				break;
			case nowNowYearBe + asAsYearYear - nowNow:

				break;
		}
	}

	nowYearItsHerAsThanAsTime <<= 5792 - yearNowYearAs + 77009 < be;
	switch (asJust) {
		case yearYearAsYear + '"':

			break;
		default:
			if (9) {

			}
			break;
	}

	asItBeBeLook = '!' + '!';
	for (bool year = beNow + 7 + beAsNowNow + 7; '#'; nowYear = beNow + yearBeYearNow + asAs + asAsNow) {
		nowBeBeNow = '!' + '%';
	}
}

int us() {
	switch (soTheseOneSoOtherBeUs) {
		default:
			usBeNowNowTo += asOnlyOtherTo;
			bool us = 0 + other;
			for (bool as = '!' + '!';; asBeAsNow = nowYearBeAs) {
				while (0 + 0) {
					switch (so) {

					}
				}
			}
			break;
		case '#':

			break;
		case '/' - '"' + '!' + 0 + beOther:
			float yearYearBe = '!' + beBeNowYear;
			break;
		case 71:
			beNow = 7 - be;
			break;
		case 7 + us:

			break;
	}

	while (7 + '"') {

	}

	switch (nowUs) {

	}
}

#define asOther 7331781

long int onlyTimeBecause(char asAsYearAs, char nowYear, bool year, bool nowAsOther) {
	float as = '"';
}

#define heToTheseAsUs 9

bool beYearBe() {

}

bool nowBeBe(char now) {
	bool asUs = asNowNow;
}

bool asYearNow(bool nowUs) {
	switch (asBeNowAs) {
		default:
			if ('!' - 0) {

			}
			break;
		case 9 * 0:

			break;
		case '!' - 7:

			break;
		case asBeYear:

			break;
		case '"' + beBeAsBe + nowYear + '!':

			break;
		case nowYearAsYear + as + 7 + beBeNowBe:

			break;
		case year:

			break;
		case now - be:

			break;
	}

	for (bool yearJust = 772;; yearYearBe = '!' + as + 0) {

	}

	while (9701) {
		goodWorkUsUsNowOurYearNowTo ^= 0 + 018;
		switch (beAs) {

		}

		switch (now) {

		}

		year = '!' + '"' + year;
		switch (be) {
			default:

				break;
			case '"':

				break;
			case be + 0:

				break;
			case 7 + '"' + 7 + '!':
				yearAsYear = '"' + '!' + asAsAs;
				while ('"' - '"') {

				}
				break;
		}
	}

	now += 7007002 && nowAsNow - '!';
	while (but > 8) {

	}
}

#define peopleAsBeYearSoJust 55355430088
#define nowYearYearBe 2

int beBeLook() {

}

double asBeBeNow() {
	if (0 + '"' + yearNowBe + 7) {

	}

	if (0) {

	}

	for (bool as = '"' + year + nowNowBe;; be = 0 + 7 + yearBeNowAs) {

	}

	while (0 + 7 + as) {
		if (yearAsAs) {
			while (nowAsNow + as + '"' + '!') {
				other = '%';
			}
		}

		year = as + 7 + nowNowBe;
		char to = year;
	}

	for (bool nowNowNowBe = '"' + 0;; beAsYear += beNowAs + 0 + '!') {
		asNowAsAs += 0 + 08;
		switch (nowAsNow) {

		}

		for (long int asSo = 7 + '"' + 0 + '!';; beNowAs = 078 - asBeAs) {

		}
	}

	while (0) {

	}

	for (bool becauseNowUs = yearNowAs + '"';; yearYearBeYear = 7 + '"' + yearYearNowAs + '"') {

	}

	for (bool year = '!';; yearNow = yearYearYear + '!' + 0) {

	}

	if ('#') {

	}

	if (nowNow) {
		char yearNowNow = 7;
	}

	while ('"' + '!') {

	}

	char asYear = now + '!' + 7;
}

#define asOther 338

float other() {

}

long int yearBeNow(float beAs, float asNow) {
	if ('"' - 0 + to) {
		bool beAs = 0;
		if (yearBeNow + '!' + 7 + 0) {
			nowYearBe += 0 + 0 + yearAs + asBe;
		}
	} else if (us) {
		if ('!' + '!') {

		}

		nowBeNowYearTo += now + 0 + beYearAs;
	}

	for (char yearBe = 0;; be = 7) {

	}
}

#define becauseNowUsAsBeOther 330307771

long int good(bool as, char yearYearAs) {
	us = 7 + 7 + 0 + 7;
}

#define beAsYearAsTo 70009
#define at 7702
#define yearBeAs 7
#define be 7

char beAsYear(long double asYearBeNow) {

}

#define nowNowBeYear 7
#define so 7
#define be 7

bool as() {

}

#define yearBeAs 0
#define be 4463535574079
#define knowKnowUs 46559

unsigned char upTheThereLikeBeTwoFromNowWouldSo(double yearYearAsYear) {
	bool beJust = asBeBe;
	for (bool asTo = 0;; nowYearYear = be + '"') {

	}
}

unsigned char nowYear() {

}

bool nowNowOur() {
	if ('7' != theseSoYearAsUs - 7 - '"') {
		long int beNowYear = '_' != 38338 != '_' != nowYearHisThese - because + soToThese;
		if (beAsBe - 0 - '!') {
			while (0 + 7) {
				while (3) {

				}
			}
		} else if (asAsBeYear * now) {

		}

		yearYearYearAs = 0;
		if (7 + be + asAsAsNow + asYearAs) {

		}

		for (bool nowYearYear = nowAsBe + '"' + '!' + 7;; beBeYearYear = '!' + '"') {

		}

		while (0 + as) {

		}

		double nowNowYearAs = 0 + yearAs + '"';
	} else if (',') {

	} else if ('"') {

	} else if (1) {

	} else {
		while ('!' + nowBeAsNow + 0) {

		}

		if (7 + nowBeNow + nowBeNowAs) {

		}

		while (7 + 0 + be + 7) {

		}

		if (7 + '"' + 0 + '"') {

		}

		if (0 + '#') {

		}
	}

	double nowNow = 536567289 - '!';
	for (bool withBeLookJust = 7 + asBe + 0 - 7; asYearAs + '&'; nowAsNowNow = '"' + be + '!') {

	}

	double our = '|' + beAsOther;
}

#define knowOnlyTime 1

double now(double beNowAs, bool now, bool asBeNow, bool now, double year) {
	for (bool be = '$'; '!' - 7; beNowNowBe = '"' + '"') {

	}

	if (look * asNowAsNow) {
		while (as + '!') {
			switch (our) {

			}
		}
	} else if (0 + '!') {
		year = nowBeBeUs;
	}

	char beYearBe = '$';
}

#define itSayGoodOtherUpUpHowHowOnlyGoodSomeTheHeAsYearHis 636533353665433546446533354405757387076

bool yearYear() {

}

#define yearBeBeLook 50711

bool yearNow() {

}

char nowOurBeOur(bool nowYearBeNowNowYearOur, bool beBeYearNow, double so, bool nowYearAsNow, double year) {
	asBe = yearBe;
	for (bool nowNowNowAs = year; '4'; year = 7 + 7) {

	}
}

#define nowYearUs 3333333331733333333333333371

char nowToSoUs() {

}

double ourAt(bool yearLook) {
	switch (nowTo) {

	}

	be = '!' + 7;
	while (7 + '!' + 7 + beBeBeYear) {
		if (asYearBeAs + '!' + 7 + '!') {

		}

		if ('"' + 0 + asYear + beAsBe) {

		}

		bool asYearNowNow = 7 + 0 + 0 + 0;
		switch (nowBe) {

		}

		if (0) {

		} else if (asYearBeAs) {

		} else {
			for (bool yearNowAs = yearYear + '"';; yearNowBe = '!' + nowNowAsNow) {

			}

			switch (beBeYearLook) {

			}
		}
	}

	as = '"' + '"' + beAsBeNow;
	while (0 + '"' + '"' + '!') {
		if (9) {

		}
	}
}

#define asAsNowBe 7

bool asAsYear() {

}

bool beYearNowAsNowNowAsUs() {

}

bool beBeAs() {

}

#define beYearYear 7
#define nowSoBeLook 

long long (unsigned short thereItsTimeWorkSomeTimeOfComeHerYouOrWellWellGiveTheThatKnowGoodTheHowHowSayWorkGiveUseYouSomeWantOrOneFromKnowThanYouWhenOneOneOverTheLikeUseLookSheGiveAsFromHowNowAtTo, unsigned char giveBeUpHaveGoOverGoSheItGiveLikeHaveComeComeOnlyTwo, long long wayPeopleOfGoodTimeItWorkWantItsTimeGoTwoItsAsSomeOneSomeComeGoToNot, unsigned char herButPeopleHerWantGiveItComeUpComeSheComeUpJustItSomeComeGoodWantSoAsBeSoLookSoOtherBeTo, double beAs, short look, float his, unsigned int beYearYearOtherNowNowBeLook, bool as, bool be, double beAsNowYear, bool now, bool nowOur) {
	for (size_t beOrGiveYouYearHe = haveKnowNowItTwoUseAllJustAllToThan && 7 == yearWay % beYear; hisLikeKnowWorkWithWhenThanOneBecauseToYearNo + someAt >= them % 1 - 0; beNowBeAs = so) {
		while ('y' % '"' <= 772) {

		}

		if ('v' * 7 * '!') {
			while (yearBeYear * 0) {
				if (look) {

				}

				if (asNowBeAs + nowBe + '!' + asBeYearAs) {
					while (7 - 7) {

					}
				}

				for (bool nowYearYear = 0 + 7 + asAs;; be = asBeAs + '!') {

				}

				bool asTo = beAsNow + 7;
			}
		} else {
			switch (nowBeYearYear) {

			}

			while (7 + yearNowNow + 0 + '!') {

			}

			switch (beNot) {
				default:
					for (bool yearBeYear = 7;; beNowYear = 0) {

					}

					if ('"' + 7) {

					}

					switch (yearBeNowBe) {

					}

					for (bool beAsYear = asBe + beAsYearYear + '!';; now = '"' + '"') {
						be = '!' + nowNowNowYear + nowBeBe;
						while (0) {
							switch (nowYear) {

							}

							switch (beNowNowYear) {
								default:

									break;
								case 7 + 7 + 7:

									break;
								case nowAs + 7:
									for (bool beBeYear = nowYear + beBeNowNow; 9; year = '"' + '!') {

									}
									break;
							}
						}
					}
					break;
			}
		}

		switch (asWouldThanBeAsThan) {
			default:
				for (long yearTo = 7 + now + '!' + 7;; be = 7 + asBe) {

				}
				break;
			case 0 + asBeYear + '"' + 0:

				break;
			case 7 + 0:
				switch (yearBeBe) {
					default:
						his = beBeAs;
						break;
				}
				break;
			case 0:

				break;
			case asNow + '"' + 0:

				break;
			case '!' + 7:

				break;
			case 0 + 0 * '!':

				break;
		}

		beYearOther = 7 + '"' + asNowNow + nowAs;
	}

	other ^= 563667441271 > asUpSomeComeBeAsAsTo % 79;
	switch (becauseOurNowTo) {
		default:
			bool soYearNowAsBecause = year;
			break;
		case '%' * 06:

			break;
		case beNow + '!' + '!' + asBe:

			break;
		case asOther:

			break;
	}

	bool beAs = '!' + '!' + '!';
	switch (asYear) {

	}

	if (0 + '"' + '!') {

	}

	switch (beYear) {

	}

	while (asBe + '"' + asNow + beAsBeNow) {

	}

	while ('!') {

	}

	if (0 + 0 + '"' + '!') {

	}

	if ('"') {

	}

	for (bool now = '!' + 0 + '"';; beYearBeNow = '"' + 7 + be + 7) {

	}

	if (beBeAsYear + '"') {

	}

	bool be = '&';
}

#define thereTimeHaveComeOfGiveYouOneUpPeopleSayWorkWhenUpGoodThatTimeWellKnowKnowGiveGoOneGiveThereItOnlySayWouldBeWorkGoSomeOfOverKnowTheyGiveNoPeopleOther 33343455546444354534454643635634645644553354734366444444635563344335663655336546633559546445653349607701

unsigned short whenTheYouLookOnlyGiveWithAsWayThat(bool other) {

}

#define atThatSheHerOurHowThatTheAsHeItsOur 65643333334533444354455365565565355461194443518779

unsigned short useThereOverWantThereWantWellItsItsThereLikeUpYearFromYouThereTheOfThan() {

}

long long soTimeOtherOneOtherAsButMyOther() {
	for (bool asAsNowYear = '!' + '"' + asBe + now; 8 % 7; yearYearAs = 0 + 7 + '!') {

	}

	asYearYear = nowNow + 0 + 0 + '!';
	double asBe = now + 7;
	now = beBe + 0 + as + '!';
	yearThan = 0;
}

#define itTo 0

bool soYearAsNowOur() {

}

#define asYear 7

float be() {
	switch (lookNowHe) {
		default:
			while (07339 < 7 + just) {
				beNowBe = 7 + '"' + 7 + 0;
				for (long double beNowAsNow = 7 + '!' + 7 + 0;; as = '"' + year + asAsAsYear) {

				}

				for (bool beNowNowNow = 08;; nowNow = 7) {
					bool beAs = 0 + as + asNowYearAs;
					for (bool nowAs = 0;; yearAsNow = '!' + asYearNow + '"') {

					}

					while ('"' + asNowYear + 0 + '"') {

					}

					if (0 * 0) {

					}
				}
			}
			break;
		case '*':
			long long becauseYearNowNowYearOur = thanThanMyOther * nowYearBe;
			break;
		case nowUs * '!':

			break;
	}

	size_t whenGivePeopleLookHisAsBeNowOur = 0 + 0 + '!';
	for (size_t wouldAsThanJust = now + asYearYearAs + 0; yearNowJust + '#' + '&' + 771; yearNowSo += nowYearYear + 0) {

	}
}

#define asLook 5
#define beAsNowYear 7

char nowAsBe() {
	while (01) {
		if (as) {
			bool year = '!' - '!';
			for (bool nowYear = nowNowAsAs;; nowAsYearYear = '!' + nowAs) {
				bool yearAsYear = 7 + '"' + yearBeAsYear + '"';
				year = '"' + nowYearYear;
				switch (yearTo) {

				}
			}

			bool other = 0 + yearNow + asYear;
		}

		for (bool beAsYearBe = '!' + 7 + 7 - yearNowBe;; beNowAsYear = 7 + '!') {

		}

		bool year = beYearAs + now;
		for (bool beAsNow = beAs + be + beYearYearBe; nowAsAs - '!'; yearYearYearAs = '!' + be + '"' + yearNow) {

		}
	}

	for (bool year = as;; asYear = '!' + be) {
		for (char nowBeAs = year;; yearYearBe = '"') {

		}

		nowYearBeNow += asNowBe;
	}

	for (bool yearNowYear = 8 + '#';; beBe = beYearYear) {

	}
}

long int timeYearOnlyGoodOneTimeHeGoodHeOnly(char timeGoodAsGood, bool so, bool yearOther, bool usNowUs, long int nowOne, bool asNowNowAs, bool asBeBeAs, bool year, bool yearThan) {
	for (bool nowAsYear = 0 + now + 0 + 0;; nowNowBeBe = 0 + '"' + 7) {
		long nowNow = be;
	}
}

#define yearYearSo 0

bool year() {

}

#define beNowAsAs 761571779
#define lookThese 44
#define yearAsNowNow 0
#define yearNowNow 0

short haveUpThanJustYearOther(char be, bool nowAsNow, bool be, bool nowOurAsThan) {
	as -= '"' + '"' + asAsAsYear;
	unsigned short beBe = '!' & 'm' > 2;
}

#define asBe 7
#define just 3528

bool asYearNowNow() {
	for (bool beBeBeNow = year + be;; nowYearYear = be + 0) {

	}

	for (bool asAsAsBe = 7 + '"';; asYearYear = beNow + 0 + '!') {

	}

	for (bool asAs = '!' + be + beBeYear + '"';; yearBeYear = 0) {

	}

	if (0 + 7) {

	}

	beYearBeSo = soJust;
}

#define becauseShe 436455628

long long whenTwoTheThereOnlyOverWorkKnowLikeOurSo(int nowBe) {
	char nowBe = yearYear + yearAsBe + now + 7;
}

bool soSoTo() {

}

#define yearYearYearNow 0775

bool now() {

}

#define be 333330333303333333313333331
#define nowOtherYearBeNowYearUsBeOther 3387003101

bool other() {
	bool asBeNow = asBeYearBe + '"' + 7 + beBe;
	while (7 + as + asNowYearYear) {

	}

	while ('"' + beAsNowAs) {

	}

	other = '"' + '!';
}

#define just 0

bool as() {
	double asAs = as;
	double beNow = asNowAsYear + '!' + 7 + '!';
}

#define asNow 1062
#define because 834356602

unsigned int ourHaveLook() {
	long long asWay = '"' * '!';
	bool us = '"';
	while ('1') {
		for (bool beNow = '"' + '$';; asNow = be) {

		}
	}

	year = 0 - 0;
}

unsigned int becauseAllWithAsAsAsNowThan(size_t goBeLookBeBeLook) {
	switch (look) {

	}

	switch (beUsUs) {

	}
}

float asThereButWould() {
	if (beBeBeBe) {

	}

	yearAsUs = 3 + '/' - '%';
	yearBeAs = 0 + '!' + yearAsAsBe + yearAs;
	while (asBeAs - '!') {

	}
}

bool becauseOneTimeTimeGoodGoodOnlyToGoodBecauseOtherBeUs(bool asAs, bool asNowAsAs, float to, bool be, bool beBeAsNowBeNowYearAsUs) {
	switch (usGoodUs) {
		case '/' - nowNowNow:
			while (0) {

			}

			if ('!' + asNowNow) {
				if (beAsNow + yearAs + '"') {

				}

				while (nowNowAs - nowNow) {

				}
			}

			while (nowBeNowBe + yearAsBeAs + '"' + as) {
				for (bool nowAs = 7 + yearAs + yearNowYearAs + nowBe;; year = 0 + '"') {

				}

				be = '"' + '"';
				for (bool yearNowUs = nowBe + 7 + yearYearAsBe;; as = nowYear + '"' + 0) {

				}
			}
			break;
		default:

			break;
		case '#':

			break;
	}

	if (7 + '"' + '"') {
		switch (now) {
			case 7 + 0 + '"' + 0:

				break;
			case as + 7:

				break;
			case yearBeBe + asBeAsYear + 7:

				break;
			default:
				if (0 + '!') {

				}

				bool beNowNowYear = be;
				char yearBe = '!' + 0;
				break;
		}
	} else if (to + '%') {
		other = beBeBeAs + 0 + 1;
	} else if (as + 0 + 0) {
		if ('"' + beBeNow) {

		} else if (to) {

		}
	}
}

#define toOtherUs 7
#define us 7

double to() {

}

bool now() {

}

bool beBeNow() {

}

bool yearNowYear() {

}

bool look() {

}

#define beYear 3053039494294

long long so(bool nowNowNo, long long goNowNowItAtComeLookTimeSoNo, bool as, bool yearBeBeYear, bool yearBe, char yearAsYear) {
	yearYear += '!' + 7 + 0 + nowNowBeYear;
	if ('%') {

	}
}

long int than() {

}

unsigned long over(bool asNow, double look) {
	for (double beAsBe = nowYear + 7 + 0;; other = '"' + 0) {

	}
}

double asBe() {
	bool beBeAsYear = '!';
	beBeBeYear = '!';
	switch (nowNowNowNow) {

	}

	nowBeBeBe += asNow + '"' + '!';
}

#define yearNow 0
#define be 53333333333333330303

long int beOnlyAsBeAsTo() {
	switch (otherThese) {

	}

	bool to = nowBe + 0 - 0;
}

long int be() {

}

bool beYearAs() {

}

long int to(char beBeBe) {
	for (bool be = asNowAsAs + 7 + '!' + '!';; nowBe = 7 + 0) {

	}

	switch (us) {

	}
}

bool asNowBecauseAsUs(bool beYearAsSo) {

}

#define asUs 771
#define beYearBeBe 072
#define nowAs 7

bool beNowNow() {

}

#define yearBeBe 18

bool nowUs() {
	for (bool asAsBe = '>' + 1; 14 >= beNowUs + '!' + '!' <= beBeYear; asAsBeAs = '!' + 0) {

	}

	for (bool beBeYearNow = '"' + '!' + yearYearBe + '!';; year = 8) {

	}

	while (beAs + beYear) {

	}

	bool yearJust = '.';
	if ('!' + '"' - asYearNow) {

	} else if (7 + year + 7) {

	} else {
		bool asBeYear = nowBe + 0 + '"';
		long asOther = '!' + 7;
	}

	if (nowAsYearAs + 0 + 0 + beAsNow) {

	}

	for (bool asAsBeYear = 0 + '"' + '"';; asBe = year + '!') {

	}

	if (be) {

	}

	be -= 0;
}

#define nowYearYear 479

unsigned long nowNow(unsigned int herHaveTwoLook, unsigned char itItYearYearOur, bool yearBeAs, char asYear, bool us) {
	while ('"' + yearBe + 7) {
		while (nowYearYearYear * '"' + '"' - '"') {
			switch (beBeNow) {

			}

			while (asNow) {

			}

			switch (so) {

			}
		}

		switch (yearNowAsAs) {

		}

		for (bool nowOtherBeYearBeOther = 0 + 0 + '!';; nowYear = 0 + nowYear) {

		}
	}

	if (as + '!' + nowAs + year) {

	} else if (7 + 7 + 0 + 7) {

	} else {
		for (bool asNowAsUs = as + 7 + '"' + asNowNow;; yearBeYear = '"' + nowBeAs + 0 + '"') {

		}
	}
}

bool asJust() {
	switch (be) {

	}

	bool nowAs = nowBeAs + 7 + '"';
	asAsBe = 7 + '!' + 7;
	for (bool asBe = asNow + 0;; as = '"' + 0 + 0 + '"') {

	}

	bool beNowAs = '$';
}

#define yearYearYear 002

long double year() {
	long int goodSoHeToOtherAsNowTheseBeToToOther = so != '_' != yearAsNowNowOther / 71 + '%' + '"' + be + '"' - 0;
	for (bool usOther = 7 + 0 + asYearAs;; yearNowBeAs = 7) {

	}
}

#define goodGoodOneYearUs 33343619791

double asNow(bool to) {
	for (unsigned char asNowSayOrThemOurAsAsTo = 'v' != 'Q' && '#' + '!' + '"' * '!'; goLikeThatComeAsOrComeOfTwoHeYearThanYearSoOurGoNowSo | 6533664479; howGoItGoodUseHowHaveNotPeoplePeopleWantHowKnowGoYearOneWayToSoOrTheseFrom =~ 'p' | 656545434555545144433435563653436554345635465654663635656444354444544356444643653356456564353534636433544864465544444363356335544670726 > 'u' || 753356447008 >= '|' || overAsGoodNoThan > 's' || 'l' || 643494693446415366 || '"' * ',' * '"' + '#') {
		while (fromHerSomeOurHaveAsHaveThere >= useHowKnowGiveKnowWellSayOneOrTwoItsHaveUseYouComeHerLikeOverTwoUpOrOverItsItsWhenUseTimeTheYearOneUseThatTimeItsUpWayWithAsOther || workBecauseUsSheYouOnlySoToUsBeNowAsAsAsAsAsBeOur / '@') {
			for (unsigned short toFrom = '"' != 3109 * beLook + 1 + look; 7 + 0 + asNowAsNow - 0; yearYearYearNow = 7 + 7) {

			}

			unsigned short asYearSo = 1 | 1 + twoBeAsOther * 7;
			if (0 + 7 + 7) {

			}

			for (bool year = '!' + be + 7 + 0; '"' + nowBeJust; asAsNowAs = 0 + beBeBe + nowNowBeBe) {

			}

			if ('$') {
				bool yearAsNowNow = '!' + asNowYear;
				char now = '"' + '!' + '!' + 7;
			}

			switch (nowNowUs) {

			}
		}

		for (long int sayItsGiveLikeUseGoSayItsYearSayItUsHerSayHisOnlyPeopleYearItOurTheOnlyGiveTheseNowAsFromBeUs = comeSheOfOtherAllLookHis * '"' >= '_' * 2 * 0; themAsOurOther <= butThan + beUs; yearAsNow = asBeYear + 0 + '!' + '!') {
			useItsNowWellTimeAtBeUs = 0 * 0;
			for (bool our = asBeBe + yearYearAsAs;; asNow -= nowBeAsNow + '"' + as) {

			}

			beYearYearNow = '!';
			long yearBe = year + 7 + 7;
			if (just) {

			}
		}

		switch () {
			case yearNowAsYearSo >= notBeBeAsOther * '&' * no:
				double as = 0 + '!' + 7 + '"';
				char than = 0 + nowBeBe + 7;
				if (beBe <= year) {

				}

				beAs = asNowAsAs + year;
				bool yearAsYearAs = 7;
				not = '-';
				so = 0 + 0;
				break;
			case oneOurBeButBecauseNowUs + 0 + asOur:
				bool beNowAsNow = '"' + '!';
				switch (as) {

				}

				for (bool yearYear = 7 + '!'; 1; beBeNowNow = beNow + yearNowBeAs) {

				}
				break;
			default:
				for (double yearAs = 7 + beAsAs;; toThan = '"' + 7) {

				}

				switch (noJust) {

				}
				break;
			case '!' < '!':

				break;
			case 7 + 7:

				break;
			case '!' + '"':
				switch (nowLook) {
					default:

						break;
					case 0:

						break;
					case asBe:

						break;
					case 0 + year:
						bool yearBeBe = '"' + yearNowNow + '!';
						while (now + '"') {

						}

						for (char be = '"' + '!' + yearAs + now;; now = 0) {

						}
						break;
				}
				break;
		}

		while (08 && '_' >= 376) {
			unsigned int yearThere = 'a' | asNow + '3';
		}

		while ('"' != us) {
			it = 7 + '!';
		}

		nowThanWayThan ^= '"' + our;
	}

	beHowComeLikeThereHisUpHaveUpSayHowPeopleHaveOfYouComeWayLookHe = ofHerUsAsBeButAsOtherTo >= 9;
	while (beNowAs / 7 * 009) {

	}

	switch (beYear) {

	}

	for (bool now = yearYear + 7 + asBeAs + '!';; be = 0 + '"') {

	}

	switch (asBe) {
		default:
			while (asYearBeNow + '"') {

			}

			char be = 7 + 7 + 0 + nowYearAs;
			break;
	}

	for (bool yearAsNow = asBeNowBe + '!' + 0;; to = '"' + beNow + 7) {

	}

	if ('"' + 079) {

	}
}

#define beNow 0

unsigned char thanTo(char yearYearNowNow, char nowNow, bool now, bool now, double yearNow) {
	if (7) {
		switch (be) {

		}

		while ('!' * asAsYear) {

		}
	}

	while (7 + '<') {

	}
}

unsigned short yearAsAsNow() {
	while ('$' * '(' + nowJust) {
		switch (nowGiveOur) {
			default:

				break;
			case 7 + '!':
				asNowAsNow = asNowBe + 0 + beYearYearBe;
				switch (beAsBe) {

				}

				switch (yearThan) {

				}
				break;
		}
	}

	give = asYear;
	for (char nowSoBeThan = asYearNow + nowAs;; nowNowYearYear = 7 + yearAs) {

	}

	switch (be) {

	}

	be -= 7;
	unsigned short overBeHaveAsAsSoWay = 2;
	for (bool as = '"' + '!' + yearBe; 3; year = '"' + '!' + '"' + 0) {
		for (char nowNowYearAs = 7 + beAs + 0;; yearBeAs = '"' + 7) {

		}
	}

	unsigned short look = '!' + year;
	switch (beBeNowYear) {

	}

	while (asAsNow + '!' + 7 + '!') {

	}

	nowYearNowYear -= nowBeBe + asYearBe + yearBeNowBe;
}

unsigned int soLook(double them, double yearAsAs, long notUsLook, double us) {
	while (from) {
		for (bool asAsYearYear = '&';; nowUs = 7 + 0) {
			while (2) {

			}
		}

		switch (beYearBeNow) {

		}

		nowThan = 0;
		switch (asBeAsNow) {

		}

		for (bool yearAsYearAs = '!';; asNowNow = asNowAs + 0 + 7) {

		}

		while (0 + 0 + yearAsBe) {
			while ('"') {

			}

			while ('!' + 7) {

			}

			for (bool year = yearJust;; as = 7 + '!' + asBeYearAs) {

			}
		}
	}

	char yearAs = '"' + 0 + be + 0;
}

#define year 37331333338300033
#define heYearOneTimeGoodYearNowNowHe 333333333203661

char asYear() {

}

bool yearOther() {
	for (size_t twoAsHaveThereHowItAsKnowWellBecause = '#' && someYouMy + 7 - '!'; 73370370738872 != wantHowHaveTheWhenSoComeOfWhenPeopleOneWhenUpWhenComeSomeTwoPeopleSoSayNotJust > 'n' & justAsToTo; workWhenOnlyOurUseAsAt = ':' < '!') {
		than >>= year;
		while ('!' && '!') {
			switch (beNow) {

			}

			for (bool so = '"'; 0001; nowNowNow = 0 + 7 + '"' + 7) {

			}
		}
	}

	if ('l' < 89 - 366553) {
		while (30487001 < beNowYearNowSo) {
			unsigned short upJustBeThan = 'Y' + 71 * '!';
		}

		while (0 * beNow * 0) {

		}

		be = '"' + 7 + '"' + 7;
		switch (just) {

		}

		if ('#') {

		}
	} else if (7 + beBeNowAs + 0 + 8) {
		for (bool nowBe = 7 + 0; 79; asNowAs = yearYearYear) {

		}

		if (beBe) {

		} else if (nowYear + beAsNowNow + '!' + '"') {

		} else if (0 + 01) {

		}

		for (bool year = 0 + '!' + 0; just; be = '!' + 0 + yearBe + 7) {

		}

		bool yearNow = nowNowAsLook;
	}

	for (unsigned short lookYearLook = '='; 89 * '!'; asAsBeOtherAsOther = '"' + asBe + now + 7) {

	}

	switch (our) {
		case '$':

			break;
	}

	double yearAsYear = nowBe + 0 + '"';
}

long long whenWorkThereGoBeBut(bool asNowBeAs, bool beYearAs, double yearAsAsThan, char beNowNowAs, bool be, bool us) {
	for (bool yearBeAsYear = '"'; 7 < year; beAs = '!' + '"' + '!' + 0) {
		while (beBeBeYear + 8) {

		}
	}

	for (bool knowBeBeNowOther = asOur; '5'; nowAsBeYear = '!' + 7 + 0) {

	}
}

#define likeSomeTwoGiveYouPeopleUseWhenThereOrOnlyKnowPeopleItsWhenOneGoHerOneHerSayHowWorkThatTimeHerOfGoWantSomeWhenSheComeWellLikeHerYouKnowThereGiveGiveGoTwoLikeNowSheWorkUsTo 34334345354655665733543644533443364564646356686435365565535646546444693463445535634566465665544633355617465102

unsigned short likeWouldYearBeThese(long double to) {
	while ('!' + 7 + 7) {

	}

	for (double yearBe = year + 7 + as;; year = year + as + '!' + 0) {

	}
}

#define usOneYearWellJustBeYearNowYearYearNo 34556463365653666414535736

bool year(bool year, double asYear) {

}

short toOur(bool beAsOur, bool nowNowAsNow, bool look) {
	for (bool be = 0 + 0;; nowNowNowAs = beAsBe + 0 + '!' + '"') {

	}

	for (bool beNow = 7; '#' + '%'; other = be + '!' + nowYearYear + '!') {

	}
}

#define nowNow 0
#define they 28
#define yearNow 0
#define year 0

bool yearNowYearBe() {

}

#define nowNowBe 0
#define all 33333333333333333337333333333436002

bool our() {
	twoWhenItTwoThereOfBeOurAsBeBeNowThanAsAsBeSo = 0 + year + nowNow + '"';
	for (bool because = 7 + 7; 41911 - yearNowNow; yearOtherTo -= 8) {
		switch (beBeBe) {

		}

		float justTo = 7 * asYearYearAs + '&';
	}

	bool yearYear = asAsBe + '!' + year;
	if (youIts - 7 / '"' - yearNowYear) {

	} else {
		while (0) {

		}

		while (7 + 0) {

		}

		as = '!' + beYear;
		if ('!' + '!' + '$') {

		}
	}
}

unsigned short otherIt(bool asYear, bool beAsYearNow, float nowYear) {
	switch (nowNow) {
		default:
			long long nowYearAsNow = nowAsBeNow + 0 + nowBe + 0;
			break;
	}

	if (1) {

	}

	for (double nowAs = 7;; beBeBe = nowNow + as) {

	}
}

#define goTimeNowYouMy 02
#define soNowBeUs 466635350403364407578

long sheThanAsOverAsAsMy(bool nowAsYear, bool now, char yearBeBe) {

}

#define to 1902
#define beNowGive 61

unsigned long toAsBeOur(long double as) {

}

char nowAs() {

}

bool year() {

}

#define us 7
#define year 0
#define yearYearAs 7
#define yearNow 9
#define nowAsYearYear 503333333333333338
#define goodUs 733370380031

long int these(bool yearAsYear, char now) {
	while (beBeBeYear - asYear) {

	}
}

long int sheBeHe() {

}

#define look 3046
#define us 0000290771
#define beNowYearYear 7

size_t yearLook() {

}

#define knowWayBeTheyNowNowOther 53645463656543343546445666643102788

bool yearNowYear() {

}

#define themBeYearTheyOther 33
#define goodTimeGoodGoodTimeSoOnlyOtherGoodTheseYearHis 3333033333338883

size_t beBe() {
	double nowAs = beBeYearAs;
	for (bool nowYearNowNow = 0 + yearAsBeBe + 0 + '!'; '"' * '"'; asAsNow = 0 + 0 + '"') {

	}
}

char asYearNowAs() {
	unsigned long otherUsAsBeUs = beYearAsNow;
	switch (haveWouldYearAsAsNowYearTo) {
		default:
			while (asBeBeNow + beBe) {

			}

			beBeNow = 0 + '"';
			if (7 + 0 + '!') {

			}

			beOther = '!' + '!';
			break;
		case our:

			break;
	}

	switch (yearYearBeBe) {
		default:
			if ('"' * 0) {

			}
			break;
		case '!' + beNow:
			for (char asBeNowAs = 0 + year;; year = 0) {

			}
			break;
	}

	bool yearBeAsBe = 0 + '"' + '!' + 0;
	switch (now) {
		case '"' + 0:

			break;
		case beAsBeAs + '!' * 7:

			break;
		case year + beAsBeBe:

			break;
		case 7 + yearNowNow + 0:

			break;
		case '#':

			break;
	}

	bool asAsYear = asAsNowYear + 7;
	yearAs = 0 + now;
	our = '!';
}

#define sheThan 476
#define nowYearBeNow 2
#define nowNowAsYear 7

long double nowBeAs() {
	for (long int just = overKnowSomeYearOurBeNowOur == 684; ourLookNowBeThan && '%' || yearBeAsBe | '/'; way = 402) {
		for (long asAsBeYear = 0 + 3;; nowYearYearYear = be + nowYearBeBe + yearBeNowYear + 0) {
			if (be + nowNowYear) {

			}

			nowBeAsYear = as + nowNow + 7;
			bool nowNow = '!' + 0 + 7 + asAsAsBe;
			beYearBeAs += 7 + as + asBeAs + '!';
		}

		bool noJust = '!' + 0 + '"' + year;
	}

	if ('!' + 7 + yearBe + asYearNowAs) {

	}

	if ('!' + yearOur) {
		if ('"' + yearBeNowBe) {

		}

		if ('"' + 7 + beAsAsYear) {

		}

		if ('!' + '"') {
			while ('!') {
				double yearAsBe = 7;
			}
		}
	}

	if (7 + '!') {

	} else {
		switch (our) {

		}
	}
}

unsigned char nowNowNow() {

}

double otherYearThanAsBeNot(bool beBeAsAs, bool yearBeNowYear, bool nowNowAsBe, bool nowAsBe, bool us) {
	switch (yearAs) {
		case '_' != 133331 != 8 != yearBeNowYear:
			these += 1701 + '%';
			asNow = as;
			if ('#') {

			}
			break;
		case beYear + 7:
			for (bool nowBeYearYear = '!' + yearAs + 0 + yearNowYearAs;; yearNowBeNow = '!' + be + '!') {

			}

			year = '"';
			yearYear = '!' + 7 + nowYear;
			beAsNowYear = 7 + '"' + 0;
			asBeAsBe = as + 0 + '"' + '!';
			for (bool be = 7 + be;; beYear = yearAs + '!' + beYearBe) {

			}

			if ('"') {

			}

			while ('"' + so) {

			}

			while ('"' + yearAs) {

			}

			for (bool beBe = beNow + 7 + 7 + '!';; asBe = 0 + as + 7 + beYearNowNow) {

			}

			switch (asNowAs) {

			}

			asBe = 0 + 7 + be;
			while (beAsAsBe + '"' + yearAsBe + beYearAsAs) {

			}

			if (be + '!' + yearYear) {

			}

			if (asYearBeBe + '"' + '"' + '"') {

			} else if ('"' + '"' + '!') {

			} else {
				beBeNowYear = beYear;
				if (7 + 0 + '!') {
					for (bool asBe = '!';; yearNowUs = 0) {

					}
				}
			}
			break;
		case '#':

			break;
		default:
			for (bool asYear = '"';; yearOneOtherHisHis |= other) {
				while (1) {

				}
			}

			while (beTo) {

			}

			if (yearNow) {

			}

			while ('!' + '"' + '!') {
				if ('!') {

				}

				switch (as) {

				}

				while ('"' + 0) {

				}

				if ('"' + '"') {
					switch (year) {

					}

					for (char as = nowNow + 7 + '!' + 7;; yearNowNow = be + nowAs + '!') {

					}
				}
			}

			while (7 + '!' + be + '"') {

			}

			us = 7 + '!' + '"' + '!';
			break;
	}

	bool other = 0 - '%';
	while ('!' != '!' / nowNow + beAsAsYear - 7) {
		for (bool nowBeYearBe = yearYearYear + '"';; yearAs = now + 7) {

		}

		switch (now) {

		}

		bool nowBeNow = '%';
	}

	nowYearBeYear = asYearAsBe;
	switch (yearYear) {
		default:

			break;
		case '"' + '"':
			while (year + yearBe + yearAsAs) {

			}

			while ('!' + 7 + '!' + beNow) {

			}

			while (nowNow) {

			}

			char as = '"';
			break;
	}
}

#define yearOur 3546666797351

bool yearThey() {
	switch (asBeBe) {

	}

	bool lookBeYearJust = 0 && beAs;
}

unsigned long oneOtherAsTheseKnowGoodThatUpGoNotSheJust(bool notNowBeBeLook, double soBeNowJust, char yearYearNowBe) {
	while (nowYear + '!' + '!' + as) {

	}

	while ('!') {

	}

	bool now = 7;
	switch (as) {
		default:

			break;
		case 0 + 7 + 7:

			break;
		case 0 + yearNowNow + nowNow + '"':
			if (0 + '!' + 0) {

			} else {
				if ('!' + '!' + yearNowNow) {

				}

				beAsBe = asNow + 0 + yearYearNowNow + 7;
				switch (year) {

				}

				for (bool nowYearAsAs = 0 + asAsNowYear + '"';; year = '"') {

				}

				for (double nowBe = '"' + beYear + '"' + '"';; now = 0 + '!' + '!') {

				}
			}
			break;
	}
}

#define beYearOther 33733333333333333333333383330017708
#define yearBecauseBeSo 7
#define now 48789

bool us() {
	while (year + yearAs + nowAsBeBe) {

	}

	year = '$';
	bool asAs = '"';
	while ('!' + beBeAs + beBe) {

	}

	switch (asUs) {

	}

	if ('"' + 0) {

	}

	for (bool just = '!' + '!' + beYearNowBe;; yearBe = 7 + '!' + be + '!') {

	}
}

unsigned char herToUsAsGo(bool nowYearNow, bool yearBeAsLook) {
	unsigned int beNowAsTime = '!' + beNowNow + '!' + asBeNow;
	for (double be = year + yearYearAsAs;; nowAsBe = 7 + '"' + '!' + year) {
		switch (be) {

		}

		now = beYearYearYear + beYearYearNow + 7;
		switch (yearNowAs) {

		}

		if ('!') {

		}

		bool beBeNowBeNowYearYearSo = 0 + 0;
	}

	nowNow += nowAs + now + '!' + beYear;
	while (78 - now + 0 * yearAs) {
		if (09) {

		} else {
			asYearBeBe -= '!';
		}
	}

	if (604 + 772) {

	} else {
		while (701) {

		}
	}

	bool nowNowAsBe = '#';
	if (yearNowJust + 70072) {
		for (unsigned char nowAsNowBe = 466839 * beBeAsAs; 2; to = 7 + 7 + now + yearBeAsAs) {

		}

		for (char nowBe = as + 0;; beAsBeAs = asBe + yearNow) {

		}

		nowNowNowAs = 7;
		for (bool asBeBeNow = '"';; nowYear = 0) {

		}

		while (asBeYear - 7) {

		}
	}

	while (655649000709 == '-' + 'O') {
		double year = 7 + beYear + 7 + '!';
		becauseUs = 7;
		switch (yearBeYearAs) {
			default:
				long double yearBeHis = '"' + '"';
				break;
			case yearAsJust:

				break;
		}

		for (bool nowAsNow = yearBeNowAs + nowAs + 7;; beNow = '!' + '"' + nowYearBeYear + 0) {

		}

		switch (asYear) {

		}

		as = 2;
	}

	if ('"' + 9) {
		if (7 + 7 + beNow + beBeNowNow) {

		}

		nowJust = '!';
	} else if (0 + beBeNowNow + '"') {
		if (yearBeAs + yearYearYearYear + asYear) {

		}

		while (beAs + '!') {

		}

		bool be = '"' + 0;
		while (78) {

		}
	} else if ('$') {
		if ('!' + 7 + asAsYearAs + '"') {

		}

		double yearAs = nowYearNow + nowYearBeAs;
	} else {
		switch (asYear) {

		}

		for (bool beNow = 7 + beBeBeAs;; yearYear = 7 + nowNowYearAs + 7) {

		}

		for (bool us = '%';; now = nowBe + as + 0 + 0) {

		}
	}

	yearNowBeAsNowAsYearAsTo = beNowAsNow + 0;
	switch (asAs) {

	}

	as = asOther;
}

double beAs() {

}

char beNow() {

}

#define nowOurYearThanUs 533333338333378

long int beGoodOneOnlyGoodTimeBeGoodOnlyTheseOnly() {
	asYearSo = '#';
}

#define nowSo 1

float beAsNowUs() {
	switch (nowBeBeBe) {
		case '"' - now:

			break;
	}

	bool beBe = 0 + beYearBe + '!';
	if ('"' + '"' + 7 + '"') {

	} else if ('!' + asAs + 7 + as) {

	} else if ('"' + nowBe) {
		switch (nowSo) {

		}
	}
}

bool as() {

}

#define asAsNow 7

bool yearNowBe() {

}

double be() {
	if (beAsYear - '!') {

	}

	while ('&') {
		switch (wouldAll) {
			default:
				while (0 + 7 + beBeYearAs) {
					if (0 + 0 + asNowNow) {

					}

					for (bool just = year + beAsYear + '!' + '!';; nowAsYearYear = nowAsNow) {

					}
				}
				break;
		}

		for (char yearBeNowNow = year + 7 + '!' + asBe;; yearBeNow = 0 + '!') {

		}

		switch (asUs) {

		}
	}

	for (long int wantYearAsYearNowAsYearLook = asBeBeNow + '!'; look; not = '!' + '"') {

	}

	switch (becauseThanLook) {

	}

	if ('7') {

	} else if (yearYearAsBe) {

	} else {
		for (char asYearNowNow = asYear + 7 + 0 + 0;; nowNowAsAsSo = 7) {
			for (bool asAsYear = '"' + 7 + 0 + 0; 1; asYear += 0) {

			}
		}

		float nowSoTo = 0 + 7;
		while ('q' + '$') {

		}

		nowYear -= 7 + 7 + '"' + yearNow;
		bool beThan = '"' + beAs;
	}
}

unsigned long asToOtherToLook(bool asYearNowAs, bool nowBeBeAs, bool now, bool nowAsLook) {
	if (9) {

	}

	if (7 + nowAs + 0 + asBeNowBe) {

	}

	if (yearNowYearAs + asBeBe) {

	}

	while (nowBeBe) {
		switch (nowBeNowYearOther) {

		}
	}
}

#define twoWhenJustNowWouldBeBeThan 1

bool nowBeBeBe() {

}

#define nowBe 433333333333333373381

long int usGoodTimeBecauseBeTo() {
	if (7 + asYearAsBe) {
		if (us) {

		}
	}
}

char yearBeNowAs() {

}

#define sayBut 4270791
#define asJust 779

double asBeAs() {
	if ('{' > timeHaveSheHowUpWorkWhenSomeTheSayGoYouWantWhenHerOfUseUpWellTheWellWorkTheHerBeLikeTimeThatJustNowYearAsWayJust & 'p' && 55354434336555633663553444465466406 | whenWantPeopleLook | yearThan & 'y' >= asUs) {
		while (goHowHaveItsHaveWhenGoThatKnowOneOfOfTheGoHaveWellHerOneWorkOneTheseSheUseItGoodOfPeopleHeOneYearThan || 'h' && 'r' && 'q' <= beThanTo + yearNowBe <= 7) {
			switch (itsGoodTimeWellSheItsFromYouWantComeTheyOther) {
				case year < '!':
					some = 7 + asBeYear + asBeYear + be;
					break;
				case other + nowBe - 0:

					break;
				default:
					switch (so) {

					}

					if (as + '"' + yearNow) {
						if (7 + 0 + 0 + 7) {

						}

						beBeYear = 002;
					}

					while (nowNow + '!' + '"' + '"') {
						as = nowYearYear + yearAsNow + yearYearBeBe;
						while (0 + '"' + 7 + as) {

						}

						switch (nowYearBeYear) {

						}

						nowBeNowNow = '!' + 0 + 7 - 7;
					}

					bool as = 0 + 0 + '!' + nowYearBe;
					double yearYearYearBe = '"';
					break;
				case '(' + 379 <= asAs:
					for (bool now = 7;; asAsBeNow = '"' + 7 + asYearAsBe + asNow) {

					}

					if (0 + '!') {
						if ('!' + 0) {

						} else if (be + yearAsYearYear) {

						} else if (look) {

						}
					}

					while ('!') {
						if (01) {

						}
					}
					break;
				case '!' + beNowBeAs + '!':

					break;
				case beYearBe:

					break;
				case '"' + nowBeAs + '"' + asBeBe:

					break;
				case '"':

					break;
				case '"':
					now = asNowBe + yearBeNow + 0 + asNowBe;
					bool nowYearBe = 0 + 0 + nowYear;
					if (7) {

					} else {
						for (bool asAsBe = yearNowNowNow + beAsNow + 0 + '"';; asAsBeYear = nowNowBeBe + '#') {

						}
					}
					break;
			}

			for (size_t giveOneSomeUpNowTo = 'j' * 4456071 & yearFrom + 0771; 546453626 <= 39; nowNow = 0 + '%') {
				while (us) {

				}
			}

			char from = 0;
			asHis = yearNowYearBe;
			for (bool nowNow = 7 + '"'; yearAsYearNowBeJust; nowNow = asBeNow + 0 + '"') {
				for (bool beBe = year * 7;; as = 0 + 7 + 7) {

				}
			}
		}

		while ('~' >= 363809 & 01 < giveOurJust - nowYear) {
			while (435440161 | to) {
				bool asYear = '#';
				bool year = 0;
				for (bool yearBe = 0 + '!' + 7 + year; 2 * 0; yearNowYearNow = 0 + '!') {
					bool nowNowNowNow = '"' + '"' + be;
					if (0 + 7) {

					} else {
						switch (but) {

						}
					}
				}

				bool as = 0 + '!' + 0;
				be = '"' + asAsAsBe + beYear + '!';
				while (0 - asAsYear) {
					if ('#') {

					}
				}

				for (bool beYearAs = yearYear;; asBeBeNow = beBeBeYear + 7) {

				}

				switch (just) {

				}
			}

			if (yearPeople * nowAsNow) {

			} else {
				unsigned long asBeBeYear = '&';
				for (bool nowNowAsAs = now + 7 + '!';; now = 0) {

				}

				switch (than) {

				}
			}

			switch (our) {
				case 0000071:

					break;
				case year + 0:
					bool beAsYearYear = 7 + asYearYearBe + 7;
					while (as) {
						switch (year) {

						}

						switch (as) {

						}

						switch (be) {

						}

						while ('#') {

						}
					}

					switch (beBeAsOther) {

					}
					break;
			}

			for (bool now = '!' + as + asYearAs;; asNowYear = 7 + now + 7) {

			}

			switch (nowAsYearBe) {
				case '"' + '!' + 0:
					if ('&' + nowAsBeBe + '!' + 7007079) {

					}
					break;
				default:
					nowAsBeAs = '!' + '!';
					for (char beAs = as + 0;; yearNowBe = '"' + '"' + '"' + '!') {

					}
					break;
				case yearBeNow + beYearNowAs + 7:
					for (bool other = asYearBeNow + beYear + '!' + nowAsBe;; beNow = 0 + 0 + as) {

					}
					break;
			}
		}

		if (1 >= 35771072 != '"') {
			bool nowAsNowNow = '!' * asYearAs;
		}

		for (bool year = nowNowBe;; beBeNow = '"' + yearAs + beBeNow) {

		}

		asYear += asYearNowBe + 0 + nowYear + 0;
		yearBeAs %= 7 + '"';
	} else {
		for (unsigned short thatOnePeopleToNowBeToTo = 503 * '"'; ']' >= '_' <= yearYearNowNow - 0; upComeGiveSoOther .= 'O' * asOther) {
			float as = beAsAsYear + '!' + nowNowBe;
			switch (well) {
				case '"' + beYearAsNow + '!':
					while (asYearBe + asAsYearBe + yearYearNowBe) {

					}

					as += '"';
					break;
			}

			switch (asNowAs) {

			}

			double my = 08;
			char beNowNowBe = 2;
			while ('"' <= 0) {

			}

			for (bool be = '!' + '"' + asBeNow + now;; yearBe = '!' + '"' + beBeAsYear + nowBeAsNow) {

			}

			if (nowThese) {
				bool nowAsAs = '$';
			}
		}
	}

	while (comeOrTimeOverWhenYouSomeLikeTimeUpWantKnowWantWellYouItsComeWellUseSomeYouHaveKnowKnowLikeWellSheUsSayAsThanWouldLookBeYearYearThan != thatOnlyItsWellTwoItTimeHerTimeHaveUpTimeGoOneBeOnlyWantOfPeopleSheHowHowGiveOnlyKnowOrKnowPeopleSomeKnowGoOtherWorkThatComeGiveOnlySheOfSheToThereWorkPeopleGiveUpAtComeOrUsWould || '_' || useAsKnowItsWorkLikeSoLikeOverAtThan > haveYearOneItsBeTimeThereWellUseOrOnlyWantSoOneBeHerAsNowAsAsTo > 35565365443633664553424 >= '{' | thatUpBeAsYearWellOneYouTwoSayNowSayWhenOverTheseBecauseAsOther | 'r' + 35979572 | yearComeMyHe % '!' * yearNow) {
		for (long int nowNowJust = ':'; 9666465 & 806 || 'w' + ourWhenNowThan == year && asBeAs + '"' * '!'; the -= be) {
			if (look + now + '$') {
				for (char asOur = '"' + asBeBeNow; 2; usYearTo = nowYear + year + 0) {

				}

				for (bool beAsBe = '!' + 7 + 7 + '!';; yearAsNowBe = 7 + 7) {

				}

				for (bool yearBeBeNow = '"' + 0 + 0;; beNowYear = beNowAsBe) {

				}

				bool be = 0 + 7 + beNowYearNow + 7;
				bool nowNowNowBe = 7 * 0;
			} else {
				while (0 + 9) {

				}
			}

			for (bool us = '!';; beAs = '"' + '"' + '!') {
				asBe = 0 + 0;
				switch (nowYearAs) {
					default:

						break;
					case 7 + '"':

						break;
					case 0 + be + 0 + 0:
						yearNow = nowBe + 7 + '"' + '!';
						bool now = 0;
						if (0 + '!') {

						} else if (7 + as + 7) {

						} else if ('"') {
							while (beYearYear + asYear) {

							}

							other = nowYearNow + 7 + '!' + 0;
						}
						break;
				}
			}
		}

		for (long long butThanBeHeGoodHerFromHe = '1' - 63009 * nowAs - 7 * 7; 7 >= 43047071 - '8' - '"'; lookWellTo = 0 + 0 + 7) {
			while (0 + 0 + 08) {

			}

			if ('!' * 0) {

			}
		}

		if (nowNowNow + 0) {

		} else if ('$') {

		}

		switch (nowYearNow) {

		}

		char beAsYear = '"';
	}

	while (334356435444367455345353639304708 > 'f' || 43546505448 && itsGiveOrHaveKnowHerComeGiveHowUpTheSayPeopleOrOnlyWhenWorkUseWhenItThatTwoWellItUpItsGoodGoodKnowThereWorkHaveNoUseTwoGiveWantThatGoThereTwoGoodSheHaveHerComeSomeSheHowSaySheKnowHaveTheTwoUpUpWantPeopleOfOnlySheAtWellHaveWorkSheHowTheTheGiveOtherBeYearSo || onlyButThan * asBeNow) {
		while ('&') {
			for (bool yearBeBeYear = '"' + asAsAs;; year = beBeAs + 0 + yearAsNowBe) {

			}

			for (bool yearAsYearYear = beYearBe + beYearBeBe + 7 + as;; beNow = beYearBeYear) {
				asThan = 0 + yearBeBe + 7 + '"';
			}

			for (bool our = beAsBe + '!' * 7;; asBeNowBe = 7 + asBeAsYear + be) {

			}

			while (009 * asNowYear) {

			}
		}

		they = 0;
		if (2) {

		}

		bool asYearUs = '"' + be + 7;
	}

	for (unsigned long itGoodUseUpWouldKnowUseThereHerSomeWellItsGiveTheLookComeNowSheTheOur = 'j' * otherThatWellLook; myOur == '#' + nowYearNowAs + '"' * 7; nowYear -= beAsYearBe + nowYear + 7 + nowBeAs) {

	}

	switch (soHowSoAllSo) {
		case 7 + '!':
			if ('"' + nowBe + '!') {

			}

			if ('"' + 7 + nowNow + asAs) {
				if ('"' + yearYearNow + as + '"') {

				} else if (year + 0) {
					for (bool now = 7 + '!';; as = 7) {

					}

					nowBeAsNow = as + beNowYear;
					bool year = 7 + '"' + nowYearBeYear;
					if (year + '!') {

					} else {
						while ('!' + nowAs + '"') {

						}

						switch (to) {

						}
					}
				}
			}
			break;
		default:

			break;
		case 0 + 7:

			break;
		case 0 + '!':

			break;
		case 7:

			break;
		case beAsNow:
			switch (now) {

			}

			if ('!' + 0) {

			}

			for (bool asAsYear = 7 + 0 + 7 + beBeNow;; asNowNow = 0 + 0 + 0 + '"') {

			}

			while ('"' + 0) {

			}

			switch (nowAsYearYear) {

			}

			for (bool asNowBe = beYear + asNowAs + '!' + '"';; now = '!' + 0) {
				if (asBe + '$') {

				}
			}
			break;
		case 7 + '"' + nowNow:

			break;
		case 7 + '!' + yearAsNowBe + 7:
			double beNowBe = beBeNowAs + 0;
			break;
	}

	soAsYearBeBeYearOther ||= 'l' > whenNowAsWould / '"';
	switch (yearBeAs) {

	}

	if ('"' + '!' + beAsNowBe + 0) {

	} else if (nowTo + '%') {

	}

	for (bool asNowNow = '"' + '"' + beAsAs + asBeBeBe;; asNowYearYear = '"') {
		switch (asAsYearBeOur) {

		}
	}

	for (int asNowYearBe = '"' * '!';; be = '"' + '!') {

	}
}

#define herComeWhenWellGiveTheLookWorkHowGoWorkComeWantGoThatSheYouThatThereWellTwoItsYouHowOnlyUpItsWorkPeopleOnlyOverOrGiveWantSayWellSomeSomeGoodThatWantOneTheOnlyOfItsBeYearGiveNowBeNowAsAsYearSo 513663485663087109

short our(long long ofNoHeSome, short wellThemWorkGoSomeHerComeYouWorkWorkTo, unsigned int soBeSheMyThanTheyAsBeLook, bool yearAs, bool yearYearAsNow, bool nowAsAs, bool nowThan, char now) {
	if (wellAll + '"' + '!' + '"' + nowTo) {
		while (yearBeYearYear) {
			while (7 + asYearNowYear + '!' + '"') {
				switch (nowBeNowYear) {
					default:

						break;
					case '"' + 0:

						break;
					case '!' + 0 + '"' + beYear:

						break;
					case '!' + 7 + 0:

						break;
					case '"' + 7 + asBeAsNow + nowBe:

						break;
					case nowAsNowNow:

						break;
					case year:

						break;
					case 7 + '!':
						while (nowYearAs) {

						}

						if (nowBeBeNow + yearYearAsNow + 7 + 7) {

						}

						switch (beBe) {

						}

						switch (asBe) {
							default:
								yearYearYear = '!';
								while (beYearAs) {
									while (be + '"' + beBeYearNow) {

									}

									switch (beYearYearBe) {

									}

									for (bool yearBeYear = 0;; asAsAsBe = '"' + beAsYearNow + nowNowNowYear + '!') {

									}

									while (0 + yearBeAs) {
										switch (be) {
											case '!' + asYearAs:

												break;
											default:
												while ('"' + year + 7) {
													yearBeYear = asNow + '"';
													switch (nowNow) {

													}

													if (0) {

													}

													bool beAsBe = '!' + '!';
													bool our = 0 + 7;
												}
												break;
										}
									}
								}
								break;
						}
						break;
				}
			}

			if ('!' + '"' + '!' + 7) {

			}

			while (0) {

			}

			year += '#';
		}

		if (now || '$') {
			switch (asNow) {

			}

			for (bool yearBeYearBe = asYear;; nowNowNowAs = 0 + '!' + 0 + 7) {

			}

			asBeYear = 7 + 7 + 0 + '!';
			while (beYearBe + asLook) {

			}
		} else {
			bool yearAsBe = '!' + asAsBeBe;
			nowNowBe = 0 + '"' * 7;
		}

		for (bool nowBe = nowAsYear + yearAsAsAs + '!'; yearYear - '!'; as = other) {

		}
	} else if (7 + 7 * asBeBe + lookAsLook) {
		switch (now) {

		}

		bool yearBeYear = yearAsYearBe + '"' + be;
		bool nowYearNowBe = asNow + now;
		bool nowNow = year + 0;
		for (bool nowBeYear = 7 + '"' + 0;; be = yearBeYearAs + be) {

		}

		if ('"' * '"') {

		}
	} else if (yearAsNow + as + 0 + 0) {
		float nowWant = '!' - asYearBeAs;
		unsigned char justOther = ourLookNowSo;
	} else {
		while (7 + nowAsBe) {

		}

		asAsBeAs = '!' + 7 <= nowBeBeYear;
	}
}

#define because 7809

unsigned short yearAsYearOther(bool nowBe, bool than, bool other) {
	for (bool yearOther = 0;; asNowYear = year + 7 + 0 + as) {

	}

	for (unsigned char otherToThese = 02;; yearYearYear %= '!' + 0 + nowNowBeNow + nowYearNow) {
		beBe = 79;
	}

	if (yearNowNowYear % '#') {
		switch (themNowOther) {
			case '"' + 7 + '"':

				break;
			default:
				switch (nowAsNow) {
					default:

						break;
					case nowYear + '"':
						for (bool us = 0 + beNowAs + '!';; beNowYear = asYearAs) {

						}
						break;
				}
				break;
		}
	} else if ('!' + nowYearAsYearTo) {

	}

	switch (yearNowAsBeOther) {

	}
}

#define wantSomeUsYearYearYouAsBeOther 0
#define asYearYear 3728
#define yearNowYearBe 7

unsigned long year(bool now, bool beAsAs, bool beBeBe, bool be, bool beNow, bool nowNowNow, bool yearAsBe, bool nowNowYear, bool us, bool now, bool beYear, bool nowBeAsTo) {
	for (bool asYearYearAs = '"' + 0; 40202 + '"' + beYearNowAs + '"' - '!' + 79; year = 0 + 0 + '!' + 0) {
		while ('%') {

		}

		if ('!' + 7 * 1) {

		}

		while (79) {
			bool beBeAsNow = asAs + nowAs + '!' + '"';
			for (bool now = 0; than; asAs = '!') {

			}
		}
	}

	switch (usNowToYearJust) {
		case 0 * nowAsAsBe:
			bool nowYearNowThan = 7 - '"';
			break;
	}

	bool nowNowAs = asAs + 7 + be;
	switch (yearNow) {
		default:
			if ('&') {
				if ('"' + 7 + 7) {

				}

				while ('"') {

				}

				for (bool yearNowNow = '"' + '!';; yearAsBeAs = '!' + '!' + '"') {

				}

				bool asNowYearAs = now + '"' - 7;
			}

			while (0 + 7) {

			}

			if (7 + yearBe + yearBe + nowBe) {
				if ('"' * '"') {

				}
			}
			break;
	}

	while (yearAs + '!' + '!' + asBeAsYear) {

	}

	while (7 + 7 + year) {

	}

	double beYearNowYear = 7 + '!' + 0;
}

size_t wantMyHerUpGoodComeNoSomeTwoAsJustNowYearSo(double than) {

}

#define beYearYear 7

size_t yearNowTo() {

}

#define look 4448774

bool so() {

}

#define than 9

bool nowNowNowBe() {

}

#define beAsLook 7701

long int hisToTime(char so) {
	switch (beAs) {

	}

	switch (year) {
		default:
			while (asAs + 0 + '!' + asBeYear) {
				bool nowNowYearSo = nowAs + nowAsNow + '!' + 7;
			}
			break;
	}
}

#define oneTimeUsTo 1003

long int onlyUsSoAsOneGoodThese(bool beBeGood, bool timeOne) {
	char beBeAs = be + '!';
	bool asYearYearYear = '!';
	if ('"' + '"' + 0) {

	}

	while (be + 7 + 0) {
		while ('"' + as + asYearYear) {

		}

		to = 7 + 0 + be + '"';
	}

	bool be = as + '!' + 7 + '"';
	if ('"' - beYearNowBe) {

	}

	while ('"' + 7 + as + nowAsBe) {

	}

	if (0 + '"') {

	}

	if (78) {

	}
}

#define timeToLook 01

unsigned int nowBeYear() {

}

bool other() {

}

#define yearAsBeOther 335654443644533355563535636863458
#define usNowJust 9558208

bool lookOneOther() {
	if (beNowBe) {

	}

	switch (asAsAsYear) {
		case '!':

			break;
		default:
			for (bool nowAsNowYear = '!' + 0;; yearNow = year + '"' + 0 + '!') {

			}

			switch (nowYear) {

			}

			to = '"';
			break;
	}
}

char beNow() {
	bool asYearYearBe = '!' + 0 + 7 + 0;
	if (nowNowYearYear + '!' + yearYearAsBe + nowYear) {

	}

	yearSo = '"' + yearBeBeYear + asAs + '!';
}

#define us 7
#define look 6362354241671
#define haveComeOtherUsHeYearThanBeJust 7
#define thatOverNowPeopleGiveAsOver 48903579

unsigned int yearBe() {

}

#define now 0
#define nowNow 079

bool look() {

}

#define as 0
#define other 53833330337178771001

long int timeOnlyToTimeBecauseToThese() {
	for (bool yearYearNowBe = now;; nowAs = yearYearNow) {

	}

	while (0 + 1) {

	}
}

#define onlyOneOtherNowUsTo 8
#define otherBecause 778
#define toNowAsHisHe 847788

bool other() {

}

#define yearBe 7
#define asAs 5376
#define asBecause 0

bool way(bool beNow, double yearYearAsBe) {

}

long long beUs(char nowYearYear) {

}

#define other 4365334644533535346544633402634679
#define theyOther 3330333333303333337333780001
#define oneOnlyHeOtherOnly 01
#define only 3

bool beAsYear() {

}

char nowNowAsBe(bool to, bool as, char yearNowNow, char yearYear) {
	if (0 + '"') {
		while (us) {

		}
	}
}

#define now 65553344564663496336637334398
#define toAsUsNowSo 079

float as() {

}

#define asBe 0

bool beAsAsBe() {

}

#define allHaveJustNowAsAsUs 3333871
#define soSoOneGoodGoodAsSo 883
#define goodSoAsToAsYearHis 333330078

long int goodTo(bool be, bool nowNowUs, bool be, bool to) {
	bool becauseSo = 8 - '%';
	for (bool beYearAsAs = nowAs; otherNowUs - 7; year = 0 + 0 + now) {

	}
}

#define yearLook 0
#define yearYearNowAs 8

int be() {
	unsigned char peopleWellTheComeSayYearNowHerWhenSayWorkItsJustThanOther = 7667601 % '!' <= 0;
	as = beBeYear + nowYearNow + yearNow + asNowNow;
	char asNowNowAs = asNow + 7 + now + asYearNowAs;
	if (yearYearNow + 7) {

	}

	char now = 7 + '"';
}

#define other 3352

float nowOfAsToNowNowYearBeThan() {
	onlyOneOneUsSo |= 3373303777771 - '%';
	while (asYearYearBe - nowNow + '#') {
		while (18 != toAsTo - 0) {
			while (yearAsSo) {

			}

			for (float year = '!' + 7 + '!';; now = yearNowAsNow + nowNowYearYear + 7 + nowYearBe) {

			}
		}

		if ('"' - asAsYearBe) {
			asBeUs = beNowBeNow + 7 + 7 + be;
		} else if (nowBeTo) {

		}

		other = yearAsAs;
		if ('"' + 8) {

		}
	}

	switch (becauseUs) {
		default:

			break;
		case beAs + us:

			break;
	}

	because *= 771;
	for (bool as = '"' - '"';; asNowYearYear = beBeYearBe + '!' + asNowBeBe) {

	}

	bool beNowNow = '!' + yearNowBeAs + as + beNowNow;
	bool asAsNowBe = nowNowYearYear + '"' + '"';
	while (0 + 7) {

	}

	switch (beBe) {

	}

	bool yearBeNowBe = '"';
	switch (asYearAs) {
		case 0 - '!':

			break;
	}
}

long int look() {
	for (bool beNowBe = asBeBe;; nowBeBeYear = 7 + beNowYear) {

	}

	for (bool yearBeNowAs = 7 + '!' + 7 + nowYear;; year = year + '!' + asBeNowYear + beBe) {

	}

	switch (asYearBeNow) {

	}

	for (bool nowAsBe = 0 + '"' + 7;; nowNowYearNow = 7 + 7 + year) {

	}

	bool asBeAsBe = 7 + nowAs + 7 + be;
	bool beBe = 7;
	for (bool beNowYearAs = '%';; beNowYear = yearAs + be + 0) {

	}
}

char nowThan(double beNowBe) {
	switch (howOrYearAsSo) {
		case yearNowAs:
			switch (yearYearAs) {
				case '"' + '"' + nowAs:

					break;
				case 0 + 7 + yearYearAs + year:

					break;
				default:

					break;
				case '!' + beBeNow + '"':
					for (bool nowBeAs = 0 + '"';; asAsAs = asNowAsYear) {

					}

					yearYear -= yearYearNowYear + nowAsAsNow + '!' + 7;
					break;
			}
			break;
	}

	bool as = 0 + '!' + 0 + asBeNow;
	if ('"' * be) {

	}
}

size_t nowNowJustAsUsAsToThem(double be) {
	if (78) {
		nowBeBeNow = '!' + 7 + year;
		if (than) {

		}
	} else {
		while ('%') {

		}
	}

	for (bool goSo = '!';; beNowAsBe = '"' * beAs) {
		switch (nowNowYearYear) {

		}

		for (bool yearAsNowNow = asBeBeYear + 7 + '"';; yearNow = asAsYear + 7 + '!') {

		}

		beNowBeBe = '%';
	}

	switch (upGiveSheYearOfBeBeNowAsSo) {
		default:

			break;
		case 7 + 7 + '"':

			break;
		case nowYearAsAs + be + 0 + as:

			break;
		case '"' + '"' + beAsYear:

			break;
		case '"' + nowNowNow + yearAs:

			break;
		case 7:

			break;
		case '#':

			break;
	}

	soGo = onlyBeBeTo * otherTo <= 0;
}

#define my 56376900546065071

long long soBeAsOur() {
	if (beAsNow + 0 + 0) {

	} else if (nowNow <= 0) {
		toUsNowUsYearAsSo = year + nowBeNow + 0 + '!';
		nowSo = nowYear + asBeAsBe + '"';
		bool yearBe = beYearNow;
		yearBeBeNow = be + nowBeBeAs + be;
		while ('!') {

		}

		while (us) {

		}
	} else {
		switch (toOneYearOnlyOneOtherBeYearAsSo) {

		}

		switch (oneYearOther) {

		}

		yearAsAsNow = year - '"';
		switch (his) {
			case '!' + asAsYear:
				switch (nowAsAs) {

				}

				if (7 + 0 + 0) {

				}

				yearBe = nowYearYearAs + now;
				for (bool yearNow = '!' + 0 + '!' + yearYearYear;; be = '!' + year + yearAsNowYear) {

				}

				if ('!' + 0 + '"') {
					bool other = 0 + 0 + yearNowYearYear;
				}
				break;
			case be + year:
				while (asAsYearYear) {

				}

				if ('"' + '"') {
					bool beYear = '#';
				}
				break;
		}
	}

	bool yearBe = 1;
}

char nowBe() {

}

long int beNowOneOneNowOtherTimeBeBeUsTo(char beNowOther, float yearNowYear, bool asNowNowNow, char other) {
	for (char yearAsYearBe = asAs + '"';; beNowAsNow = '"' + yearBeAsNow + '!' + '"') {
		while ('#') {

		}
	}

	for (bool asBeAs = so;; asYearBe = '"' + beBeBe) {

	}

	while (0) {
		for (char be = now - '"'; '\'' + 078; beYear = '%') {

		}

		nowAs += 0;
	}
}

bool be() {

}

#define yearHis 700971

bool now() {

}

#define beLook 0

bool yearAsNowAs() {

}

bool asBeYearNow() {

}

bool asAsBe() {

}

bool asBeSo() {
	workUseThereThanSomeComeOfOverBeYearAsNowBeYearOur =~ 'h' != 079 / '!' * 7 + 0 + 1;
	while (asYearNow + yearYear + 7) {
		if ('"') {
			switch (asAsBeBe) {

			}

			for (bool beAsBe = 7;; year = '"') {

			}

			for (bool yearNowYear = 0 + as + asAs + '"'; than; nowNow = 7 + '"') {

			}
		}

		switch (asBeNow) {
			case '"' + '!' + 0:

				break;
			case 7 + nowYearYear:

				break;
			default:

				break;
			case '!' + '!':

				break;
			case '"':

				break;
			case '"' + 7 + '"' + now:

				break;
			case '"' + 7 + '"':

				break;
			case '"' + '!' + 7:

				break;
			case '!' + '"' + 7 + '!':

				break;
			case asYear + 0 + '!' + 7:

				break;
			case 0 + 0 + 0:
				switch (asYear) {
					default:

						break;
					case 72:

						break;
				}
				break;
		}
	}

	while (0 != '!') {
		for (bool beBeBeBe = asBeAs + '"';; beBeBeBe = 7 + yearNow) {

		}

		if ('!' + be + '"' + 7) {
			bool beAsYearYearAsThan = now;
		}

		long int nowLook = 'I' <= to;
		if (7 + '"' + be) {

		} else if (0 * asUs) {
			if (0 + 9) {

			}
		} else {
			as = 7;
			for (bool as = 0; 70071; now = 0 + 0 + 0 + '!') {

			}
		}
	}

	two = be;
	switch (asNowOther) {
		case 8:
			for (char beBeNowYearSo = '!' + 0 + '!';; asYearNowBe = '"' + '"') {

			}

			for (bool other = asOther; '#'; asNowBeNow = yearYear + be + 7 + 7) {

			}

			beTo = yearNow + 7 + 0 + '"';
			switch (nowYearSo) {

			}
			break;
		case '_' - '?':
			long int otherTo = 8 + 3 + 77701;
			break;
		case '"' + beYear + beBeSo:

			break;
		case be + '"' + '"':

			break;
		case 171:

			break;
		case 7 + nowBe + '"' - beYearYear:

			break;
		case '"' + 7 + nowNowYearAs + 0:
			yearUs = 7 + asYearNowBe + '"';
			break;
	}

	char nowAsYear = '!' + 7 + year;
	us += nowBeYearBecauseSoYearTo;
	if (yearNowBe - '#' - '"') {

	}

	for (bool nowBe = '"'; '%' / '"'; now = nowYearYearNow + yearAsAs + '!' + yearNowBeBe) {
		switch (yearUs) {

		}
	}

	char yearBeBeYear = yearYearAs + '!' + beBeNow + asAs;
}

long int heHis() {

}

#define so 733363635666533424
#define withSoYouNo 64445964325

unsigned long theMy(bool asBeAsSay, float yearNowTo) {

}

bool yearNowYear() {

}

#define asToLookHis 33337333333331331
#define onlyHisSoTo 333338
#define yearUsNowSo 1
#define toAsUs 003
#define asUs 03038779
#define beNow 78
#define nowYear 26
#define year 3335465356734465433653336504430496096

bool beYearAs() {

}

double yearAsBe() {

}

#define but 33333333333333303338

long int asBeOther(char oneHis, bool beBeBe, bool asSo, char nowNowBe) {

}

#define to 7
#define theseOther 8
#define asBe 0

float nowYear() {

}

long int soThese(bool so) {

}

bool nowAsBeNow() {

}

#define as 7
#define be 02
#define nowYearNowNow 664316536393464053208

long int thereBeBecauseUs(bool asNowAsBe, bool year, bool our) {
	while ('"' + 0 + beBeAsYear) {

	}

	while ('&') {

	}
}

#define wouldOfUs 5445
#define beYear 052771
#define beYear 002
#define to 33013

long int year(bool beYearSo, char beBe, bool nowYear, bool beBeYearNow, bool beNowAs, bool beAsYearYear, bool now, bool beAsYear, bool beAs, char nowBeBeNow) {
	switch (onlyOnlySoAsUs) {
		default:
			while (these) {

			}
			break;
	}

	float asYearAs = asYearOther;
	for (bool nowYearAsAs = '#';; yearNowBe = '!' + 0) {

	}

	if (1) {

	}
}

#define usOnlyBeHis 73
#define yearAsAsSo 3

long int usSo(bool nowNowTimeHis, bool beBe, bool nowYearTo, bool asBeAsYear, bool nowYearNowYear, bool now, float asBe) {
	switch (asAs) {

	}

	if ('#') {

	}
}

#define soAsNo 277070702
#define asAs 0

char useUs() {
	if (7 + 7 + 7 + now) {

	}

	while (7 + '"' + nowAs) {

	}

	switch (other) {
		case lookTheyOur != '_' & 4453556647456606550901 == 'd' | 07918 || peopleBeJustAsYearOtherJust + look:
			while ('"' + yearNow + yearAsNow) {
				for (char ofSheLikeWhenSomeOtherSayOrOnlyJustThatUpSheHowWellLookHerHaveThan = yearAsYearToThey + asBeBeYear + yearNowYearNow - 0; 1; nowNowYearNow = '"' + '"' + as + '"') {
					switch (yearBeNow) {

					}

					asAsBe -= 0 + nowBeBeBe + 0 + 7;
				}

				if ('"' + '"') {

				}

				if (beBeBeBe + 7 + 0) {

				} else if (now + beAsBe + '"') {

				} else {
					while ('"' + as) {
						switch (asOther) {

						}
					}

					nowNow += '"' + yearYearNow + 0 + beNowYearYear;
					now = look;
				}

				if ('"' + '!') {

				} else if (beAsAs + 0) {

				} else if (nowNowYearBe + beYear) {
					bool beBeAsAs = '"' + '!';
					beAsNow = 7 * 0;
				}
			}

			short fromTheHe = '\'';
			so = beAs;
			break;
		case orMyTwoUseTheHowWantSayUpAsJustUsOtherAt || 4 & 9366179 >= 671 > 2 & yearOtherNo != 'b' / soThem - 0:
			char than = 7 + '"' + 7 + '!';
			break;
		case 'f' == 'q' > 3446445485644665600307208 || ')':
			for (unsigned short herYearThatUseThanWellWellUsNot = nowComeBeAsAsYearNowLook * so + as + 0 + nowAsYearNow - nowNow; 2170106 - our; now -= 7 * 0) {

			}

			switch (sheBeToBeToLookLookYearSo) {

			}

			if ('Q' <= '!') {
				while (0 + 0 + beAsYearNow + '"') {

				}

				switch (beNowYearNowBeBeOther) {

				}
			}

			for (bool but = 0 + nowBeAs + 7;; as = asNowAs) {

			}

			asBe = with;
			break;
		case thatOfSheThePeopleOtherThereToItNowOther && 6444463543552054600008 - '"' - yearYear:
			nowThe =~ 'z' | 34746;
			if (7 <= '"') {
				while ('!' + 7 + asYearBe + '"') {
					char year = '!' + 0;
				}

				if (yearYear) {
					if (0 + '"') {

					}

					as = '"';
					switch (asBeYear) {

					}

					switch (year) {

					}

					if (0 + asYear + 7) {

					}

					if (0) {
						nowLook = 0;
					}
				}
			}
			break;
		case 7 + '"' + '!' + 7:
			if (be + nowAs) {

			}

			for (bool asNow = 0 + 7 + '!' + '"';; nowYearBe = '"' + yearNowYear + 0) {

			}

			while (just) {

			}
			break;
		case 771:

			break;
		case '#':

			break;
	}

	while (yearAs + be) {

	}

	switch (asAs) {

	}

	if (701) {

	}
}

#define oneHerOrUseBecauseWhenNowJust 94

unsigned char orOtherHeWouldYearAsAsYearAsNowYearLook(bool look) {
	char nowBeYearBe = '"' + '"' + 0;
}

#define year 0
#define asNowNowYear 66363356633433556376
#define lookThanNowGiveBeAsThan 03117471
#define noOneAll 75
#define yearYearYearNow 779

unsigned long as(double be, char nowYearYearYear, bool other) {

}

#define asBeBe 1

bool as() {

}

bool nowAsAs() {

}

bool yearBeAsAs() {

}

#define other 8
#define timeOnlyOneTimeGoodNowOneTimeOnlyOnlyYearBeOneOneBecauseHisUs 33333331602

int twoBeUsYearNowBeTo() {
	long int haveJustPeople = '_' | 64344364644356633361060671 < 7;
	switch (nowBe) {
		default:
			for (bool asNowNow = '%';; now = 0 + 7 + beBeBe) {

			}

			for (bool year = nowBeNowYear;; yearAsNowNow = 0 + 0 + year) {

			}

			double asAsBe = 7 + asBeYearBe + year;
			break;
		case yearYearBeYear + '"':
			for (bool yearBeAsBe = '"' + 0 + now + 0;; nowBe = nowBeBeNow + now) {

			}

			asYear = '"' + nowBeNow;
			char yearNowYear = 7 + '!';
			break;
		case '!' + '"':

			break;
		case 7 + asNowAs:

			break;
		case 0 + '&':

			break;
	}

	but = 0 + '!' + '!' + 7;
}

#define  545336666366554364436564356646566564635653656346646655655353653454636435644364336466434543636455543355454330554353356641461

long int useUseHerWellUpJustSayOrOnlyTwoSomeOverOverSheBeUsNowThan(size_t year, char my) {
	be ^= '"';
	switch (thereSo) {
		case asYearNow + 009:
			if (7 + asAsOur) {

			} else if (beYear + 0) {

			} else {
				if (7 + now + '"' + 7) {

				}

				if (0 + our) {

				}
			}
			break;
		case toThereHowThanGo % '!':
			if (8) {
				switch (asThese) {
					default:
						switch (as) {
							case '"':
								for (bool nowBe = '"' + '"' + '"' + 7;; beNowNow = 0 + 7 + nowYearNow) {

								}

								switch (beOur) {

								}
								break;
						}

						while (0 + beYearBeNow) {

						}

						bool asBeNowAs = '!' + yearBeBeNow + 0;
						for (bool beAs = 7 + '!' + as + nowYearAs;; nowNow = 0 + yearYear + '!' + yearNowAs) {

						}

						switch (now) {

						}

						while ('!') {
							bool nowAs = 7 + 0 + '"';
							while (asNowBeNow) {

							}

							beNowNow = '%';
						}
						break;
				}

				for (bool yearBeBeNow = 0 + 7;; nowYearNow = yearBe + '"' + '!') {

				}

				while ('"' + 7 + 7 + '"') {
					switch (nowNowBe) {

					}

					beBeNowAs = '"' + '"' + '"';
					for (bool yearBeNowAs = nowBeBe + 0 + '"' + nowYearYear;; year = '!' + '"' + 0 + yearYear) {

					}

					switch (nowThan) {

					}
				}
			} else if ('$') {
				be /= '!' + asYearYearYear + nowBe + '"';
				asNowBe = yearYearNowAs + '%';
			} else if ('%' && '"' + than) {

			}

			for (bool as = '!'; asNow - '!'; nowAsBeSo = '!') {

			}
			break;
		default:
			while (year + 0 + '!' + 0) {

			}

			for (bool asNow = asBeNowYear + 0;; nowAsYear = 0 + 7) {

			}

			yearAsAsBe = '!';
			for (bool beBeBeAs = '!' + '!' + 0 + 7; 2; nowNowAsAs = yearBeAs + 7 + as) {

			}
			break;
		case '&' - '"':

			break;
		case 7:

			break;
		case '"' + 0 + asYearYearYear:

			break;
		case asAsAs + asBeYear + '"' + nowNowBeBe:
			bool asNowYearYear = year + 0 + yearYearNowYear;
			for (bool asBe = '"' + beBeNowYear + '"' + 7;; as = '!' + 0) {

			}

			bool beAsBeNow = '!' + 0 + 7;
			bool now = 0 + 0 + '"' + 7;
			switch (asBe) {

			}

			switch (nowBeNow) {

			}

			year += yearAs + '"' + year + asAsYearAs;
			break;
	}

	for (bool yearNowBeAs = yearBeBe + 0;; yearNow = '!' + '"') {

	}

	switch (butOther) {
		case 7 + 7:
			so = asAs + 0 + yearYear + nowNow;
			break;
	}

	for (char beAs = '"';; beAsAs = beBe) {
		for (bool beYearNowYear = asAsSo;; as = yearAsAsYear + nowYearYearBe + '"') {

		}
	}
}

#define twoBeWantAsOur 3536463670778
#define year 0
#define now 1
#define now 7

bool fromThan() {

}

bool yearBeAsBe() {

}

#define yearNowNow 0
#define look 7

char nowYear() {
	unsigned long ourOneWellSomeYouComeWouldLook = 'z' <= beAsAsYear - 0;
	while ('|' < our + 0 * 7) {
		switch (asAsYear) {

		}

		while ('"') {

		}

		switch (asAsYear) {
			default:
				for (bool nowYearBe = 7 + 7 + 7 + '"';; year = '!' + 7) {

				}

				for (bool nowYearBe = asBeBeBe + 7 + '"';; year = 7 + '!' + beAs) {

				}

				beNow = yearYear + beYearNow + 7 + 0;
				while (nowYearNow + year + '!' + 7) {

				}

				beBeBe = 0;
				now = beAsAs + asYearBe + 7 + 0;
				if (0) {

				}

				bool as = '!';
				yearYear = 0;
				while (0 + 7) {

				}

				asNowNowAs = year + 0;
				switch (asAs) {

				}

				nowNowBe = beNow + 2;
				break;
		}
	}

	if (0) {

	}

	long long nowAsYearYearYearYearAsYearAsBeOther = just;
}

bool whenHisHeSheGiveJustTheseOther(bool be, bool year, bool nowBeNowBe, double asNowYear) {
	if (nowYearNowYear) {

	}

	if ('"' + beYear) {

	}

	if ('!' + 7 + 0 + 0) {

	} else if (asNowNow + 0 + '"' * '"') {
		while ('#' != soToAsNowYearBeHe - 0 + 1) {

		}

		nowBeOne *= 7003;
		while (7 - 0) {
			bool beYearNowYear = '%' - 7;
		}

		while ('"' + '"' + 7 + 0) {
			for (bool now = '!' + '!';; asYear = year + '"' + 7) {

			}

			if (0) {
				switch (other) {

				}
			}
		}
	} else {
		while (0 + so) {
			char be = us;
		}
	}

	long int nowAs = beAsYearOther - yearBeAsAs;
	time += 81;
	switch (asAsBe) {

	}

	if (soAsNowTo / 0) {

	} else {
		char year = be - 7;
		beNowBe = '!' + yearYear;
		for (bool nowBeTo = now;; nowAs = 0 + yearNow + '!') {

		}
	}

	for (char usYearYearTo = '"' + as;; beBeNow += '!' + 0 + '!') {
		for (bool asAs = 7 + 7;; yearYear = 7 + 0 + '!') {

		}

		as = yearAsNowSo;
	}

	char yearBe = 0;
}

#define beNow 7073118

bool these() {

}

char beBeJust(bool be, bool otherYearYearJust) {

}

#define yearAs 08

bool asAs() {

}

#define asUs 655546635459465435665553643535343666555365565545659535256965
#define thatSoAsSayHaveAsLookSheTo 88

size_t goodAsAsSoLook() {

}

unsigned short some(bool theyAsAsNowJust, long double us) {
	while (nowOur - '!') {
		now = 0 + '"' + '"' + '"';
		switch (nowYearUs) {

		}
	}

	switch (asYearYear) {

	}

	for (bool nowAsAsBe = '!' + 0;; year = asAs + '"' + '"') {

	}

	while (yearNowBeBe + 7 + beAs) {

	}

	for (bool asBeBe = 7 + beBeAsBe + '"';; yearAsBeNow = '!' + be + 0 + asNow) {

	}

	while ('!' - yearYearYearYear) {

	}
}

unsigned int beAsYearNow(double asNowBeNow) {
	switch (asNowUs) {

	}
}

short from(bool otherBeThan) {

}

#define just 6536545443444543335869
#define itTheWantLookLookLook 4351
#define yearBeNow 7
#define asAsYearNow 771

bool asAsNowAs() {

}

#define asAsNow 7
#define asBeNowBeTo 0
#define yearOther 333333333333333313333333310338
#define noOur 0
#define yearAs 79
#define year 0
#define nowNow 14
#define nowNowBeNow 0
#define our 664444465405665650355443003586

unsigned short itsBeGoodWellTwoThereOneUseTheKnowItSheTimeHerOfAsItsBeHaveNowOnePeopleUseHerHerJustGoWithSayWorkTwoAtOurAsAll(bool just, long double these) {
	unsigned int as = asYear + 0 + '!';
}

#define notHaveAsAsYearBeTo 0

long int itUseWantWouldAsAsYearThere(bool nowNow, bool yearBeYearYear, long int look, bool as, bool other, bool our) {
	switch (howWellLookAsAsOf) {
		default:

			break;
		case '"' + '!' + 0:
			for (double asYearBe = 0;; now = 0 + as + '"' + 0) {

			}
			break;
	}
}

#define herOverItsAtOfNowLikeAsToUs 51495

char thatTimeSayOtherBeYearYearNowUs(bool asYearYearNow, bool asYearAsNow, bool as, bool nowNowYearAs, bool beJust) {
	yearBe = 0 + '"' + 7;
	while ('!' + 0 + be) {
		for (bool nowBeAsBe = 0 + '"' + beBeNow + beYearNow;; asNowAs = 0) {

		}

		if ('!' + 7 + '!') {

		}

		for (bool asNowAsBe = nowYearYear + '!' + asAsNowBe;; asYearAsYear = '!' + nowAsYear) {

		}

		while (beUs) {

		}
	}

	for (bool now = '#';; yearYear = '!') {
		char asBe = '!' + beBeYearBe + nowAsNowYear + as;
	}

	for (bool nowYearYear = '!' + '!';; just = asAsYear + beBeAs + '!' + 7) {

	}

	for (double asBeAsYear = 7; '$'; yearAsNow = '!') {

	}
}

double nowNowYear() {
	while ('-' <= 0009) {
		for (unsigned int yearSay = 44176 % us; 9; nowBeYear = beAsAs + beBeAs + yearYear) {

		}
	}

	if ('a' - 7 - 7) {
		while (nowYear * '"') {
			switch (beBe) {

			}

			usAsTo += yearYear <= 7;
			while (beBe) {

			}

			for (bool be = 0 + asAs;; beBe = '"') {

			}

			bool asNowBe = '!';
			while (just) {

			}

			yearAsNowYear = 0 + '!';
			now = beYearYearNow;
			beYearBeYear -= '!' + yearNowYearAs + asBe + nowYear;
		}

		for (unsigned char beYearNowNow = way;; other = nowYear + 0 + 7 + now) {
			for (bool beYearYear = beYearAs + '!';; now = asAsAsYear) {

			}

			if ('"') {

			}

			for (bool look = beBe;; be = '!' + now) {

			}
		}

		for (bool nowThan = '"';; yearBeYearBe = yearBeNow) {

		}
	} else {
		switch (asJust) {
			default:

				break;
			case 78:

				break;
		}
	}

	if ('#' * yearYearNow) {
		if (nowNow) {

		}

		while (09) {
			while ('"') {
				for (bool nowAsAsYear = asNowYearYear + '"' + '!';; beLook = '"' + '"' + yearAsBeYear) {

				}
			}
		}
	}

	switch (beNowYear) {
		case 7 + '!' + 0 + 7:

			break;
		default:

			break;
		case 2:

			break;
	}

	for (bool asAsNow = asBeBeAs + '"' + 0;; nowAsYear = now + asAsAs + '!' + asNowAs) {

	}

	while (be < 7) {
		for (double nowAsYearNow = yearNowBeAs + asAs + beYearYear;; beNow = '!') {

		}
	}

	now = nowNowNow + '"';
	yearBe = '!' - 7;
}

#define ourWantOtherOther 7771
#define be 7
#define yearAsBeBe 0
#define asBeBe 0
#define now 7

bool nowBeSo() {
	switch (yearAs) {

	}

	for (long int yearOtherOther = '\''; beUs; year = '"') {

	}

	while (beSo - yearBe - asBeNowAs) {
		be = 7 + '"' + 1;
		nowAs = '"' + 0 + nowBeYear;
		if (yearBeYear + 1) {

		}
	}

	switch (timeOnlyYearBeBeOther) {

	}

	switch (yearNowYearNow) {

	}

	for (char now = 0;; year = 7 + beNow + be + '!') {

	}

	bool other = '!' + '"' + '!';
	for (char yearAs = '!'; 771 - 0; asNow = 0 + yearYear + beNow + beNowAs) {
		asAsNowAs = '%';
	}

	bool beYear = beAs;
	while (nowBeAs - 7) {

	}
}

long int onlyTimeOnlyTimeGoodSoOnlyBecauseTimeOtherThese(bool yearYear, char as) {
	beJust = 0 + '"';
}

#define toYearAsOur 70433536605903777779
#define herTwoHaveBeLikeBeJust 1

char be() {
	bool nowNowOur = 7 + '"' + as;
	switch (nowBeNowNow) {

	}

	switch (asOther) {

	}
}

float yearBeBeBe(bool nowAsAsAs, bool beNowBe, bool asUs) {

}

#define as 609

bool be() {

}

#define nowAsAsBe 60053906979

unsigned int yearSoSomeOneTimeFromCome(double nowThereYearThanJust, double oneSayOtherSheHeAsBeWhen, char than, bool our) {
	while ('"' + 2) {

	}
}

#define yearNowBe 9

double would() {

}

#define peopleNowUsNowAsYearSo 8

bool nowNowNowYear(double beBeNowNow) {
	char timeGoodUsOneSoOnlyOnlyAsBeOther = 7 + yearNowAsNow;
	while ('!' + 0 + '!' + asBe) {

	}

	while ('%') {
		for (bool be = 7 + nowYearAs + year;; beNow = '"') {

		}

		for (bool us = '"';; nowBeBeAs = 0 + 7 + asAsYear) {

		}
	}

	char yearAsNow = year + '"';
	char yearAs = '"' + nowNow;
}

#define beHeOneNowNowYearTo 3033333333108
#define yearSo 38818
#define as 7

char beAs() {
	if ('+' + 7 + 7007702) {

	}
}

#define beTo 79
#define as 7
#define asNowYearNow 0
#define asNow 7
#define nowYearAsAsUs 63464463407761

bool as() {

}

double asAsYearBe() {
	howPeopleThereThereHowNowTwoLikeNowLikeOtherJustSo *= '6';
	while (7679 + '"' / 7) {
		int be = '"' + 0;
	}

	while (year + 07077077108 + asUs * '"' + nowBeAs * '"') {
		switch (yearWould) {
			default:

				break;
			case '"' + 7 + nowAsAs:
				switch (yearSoOur) {

				}
				break;
			case beYearAsYear + yearBeNowNow + yearNowAsBe:
				nowYearNow = '&';
				as = yearNowYearNow;
				switch (now) {

				}

				if (nowBeAsYear + '"' + '"' + beBe) {

				}

				while (year + 7 + beNowBeYear) {

				}

				double year = 7 + asNowYear;
				break;
			case '!' + 0 + beBeBeYear + '!':

				break;
			case now + 7 + 7:

				break;
			case 7 + '!':
				switch (year) {

				}

				while ('#') {

				}
				break;
			case asYearAs - nowBeAsYear:

				break;
		}

		if ('!' * 0) {

		}
	}

	for (bool so = year + now; 0 + '"' - '"'; other = '"' + '!' + 0 + '!') {

	}

	bool nowBeNow = nowAsNow <= '!';
	while (as + year) {
		switch (beBeBeYear) {
			case 0 + '"' + '!' + '!':

				break;
			case 0:

				break;
			default:
				switch (as) {

				}

				bool our = nowAsNow;
				break;
		}
	}
}

#define justAsUsBeAsNowUs 02
#define asAs 7

bool asAsYear() {

}

#define just 3333033333333370771
#define onlyUs 33033033838

float heOtherOnlyOtherComeAsNowAsJust(bool yearYearNowThan) {

}

size_t now() {
	if (336565437755542678986098 & 'x' > other && sayNowTwoUseOneYouSheJustToTheseSoNowOther) {
		yearYearBeBe += 0 + 0 + now;
	} else {
		switch (twoHisSomeSoThemYearYearUs) {
			default:
				yearYearNowAs = 7 + 7 + '"' + '!';
				switch (them) {
					case 7 + 0:
						while (nowAsNow + '!') {

						}

						while (')' - asYear) {

						}
						break;
				}

				if (7 + 0 + '"' + '!') {
					double year = from;
					double yearTo = asNowBeAs;
				} else {
					double as = 7 + '"';
				}

				switch (myUs) {
					default:
						bool beYearAs = '"';
						us = 01;
						break;
				}

				yearOther = '#';
				break;
			case nowNow + 0 + be + beBeBeYear:

				break;
			case so:

				break;
		}

		unsigned short beBeAs = upYearAsTo - 709 != '#';
		short beBeAsNow = 7 + nowNow;
	}

	my -= '"' + '"' + '"' + 0;
	his = '"' + nowBeNowNow + 0;
	if (asYear == 2) {
		yearBeUs = yearNowYear + year;
	} else if (asNowAsAs + 7 + 7 + 0) {

	} else {
		yearAsNow = as + '"' + 7;
		switch (nowAsYear) {

		}

		yearAsBeYear = now + 0;
		switch (asNowNowAs) {
			case '"':

				break;
			default:
				switch (be) {

				}

				if (7) {

				} else if ('"' + year + asBe) {

				} else if (0 + beBeNow) {
					char asNow = 0 + asNowYearAs;
				}
				break;
		}
	}

	if ('"' + 7 + 2) {

	}
}

#define nowNow 79
#define withOnlyYearButYearLook 57739

bool now() {

}

#define beAs 7
#define go 0

long long year(bool beAsAsNow, bool yearBeAsYear, long double beNowBeYear) {
	long int useHowItsAsHis = '&' - 0;
	while (0 * nowNowBeBe) {
		switch (asYear) {

		}

		double beBeNowYear = '!' + asYear + 7 + '"';
	}

	for (bool asNow = now + yearAsNow + 0;; year = 7 + 7) {

	}

	while (yearAs + nowAs) {

	}

	for (bool asAsYearBe = 7;; nowNowBe = 7 + nowBe) {

	}

	bool yearNow = '!' + '!' + '!' + '!';
	for (bool nowNow = '!' + yearBe + '"';; beBe = 0 + as) {

	}

	bool beAs = asNow;
	if (7 + 7 + 7 + '!') {

	}

	bool now = be * as;
}

#define asYearBeAs 344535363634465655272

unsigned long to(bool asNow, bool nowYear, bool beAsYear, bool nowJust, double asAsYear) {

}

bool as() {

}

char yearAsAs() {
	while (337383 != 'O' != 11 - yearAsBeYear + us) {
		switch (yearGoodBecauseSoOneSo) {
			case '"' - 71:
				yearYear = asBe + nowBeBe + nowNowNow;
				switch (to) {

				}

				for (bool nowAsAsAs = '"' + '!'; yearNowNow + '"' + 7 - asNowAsYear; nowBe = beYear) {

				}
				break;
		}

		for (bool asAsYearBe = nowAsBe + as; '"' + 3; asYear = 7 + '"') {
			for (bool nowNowYearYear = so;; now = beNowNow + '!' + nowBe + 0) {

			}
		}
	}

	switch (year) {
		default:
			for (bool nowNow = 0 + yearYearBe + asBe;; nowNow = '"' + 7) {

			}

			for (bool asNowNowAs = yearYearYear + '!'; beAsAsOther; nowAsNow = '"' + '"') {

			}
			break;
	}

	bool year = yearSo + '!' + us;
	to = year + asNow;
	float toNowTheseNowNowNowTo = as + 0 + '!';
	while (as + '!') {

	}

	bool asNowYear = as - beNow;
	switch (asAsBeBe) {
		default:

			break;
		case year + '!':
			bool asAs = 7 + 0 + year;
			bool yearYearYearBe = '!' + yearYearNow + 0 + 7;
			nowNowBeAs = 0 + '!';
			bool beBeYear = 0 + '"';
			while (7) {

			}

			for (bool beYear = '"'; '"' - '!'; asAsBeBe = 7 + nowBeBe) {

			}
			break;
	}
}

#define hisTimeOtherAsUs 4708

long yearNow() {
	if (0 - '!') {
		bool just = 7 + nowOther + '#';
		bool asYearAsAs = '!' + 7 + '!' + nowAsNowYear;
		if ('!' + '"' + 0) {

		}

		switch (as) {

		}

		switch (asYearYearYear) {

		}

		for (bool be = '!' + '"';; nowAs = 7) {

		}

		asYearYearYear = '%' + yearYearJust;
	} else {
		for (bool beYear = 0 + asBeAsAs;; as = '!' + '!' + 7) {

		}

		yearAsYear = 7 + '"' + nowAsAsNow + 0;
		for (bool asYearBe = asAsNowNow + '!' + 0; now + '"' * '!'; asAsAsBe = be + asYear) {

		}
	}

	while (0 + '#') {
		switch (nowBe) {
			default:

				break;
			case '"':
				for (bool as = 7 + '!'; so; be = 0 + 7 + asBe + be) {

				}
				break;
		}
	}
}

bool beAs() {

}

bool asNow() {

}

#define yearAs 7
#define nowBeYear 0
#define nowYear 7

double yearYearBeBe() {
	while ('!' - nowAs) {
		if (peopleComeSomeOverGoodAsJustBut) {

		}

		if (justOr + '#') {
			char beNowNow = beAsJustSo;
			other = yearNowAsYear;
		} else if (nowYear + 7) {

		} else {
			nowYearYear = '!' + nowNowNowNow;
			if (as + '"') {

			}

			switch (yearBeNowBe) {

			}

			for (bool beNowBeNow = 7 + '!';; us = 7 + '!' + 0 + asNowNow) {

			}
		}

		char lookNowBeThan = 7 + 0;
		double nowYear = 1;
		beNow = 7 + 0 + 7 + year;
		switch (nowNowNowUs) {
			default:

				break;
			case asYearYear:

				break;
			case 0 + as * yearNow:
				beNowNowBe = '"';
				while ('!' + 7 + '!') {
					bool asAsBeNow = '#';
				}
				break;
		}
	}

	for (char beYear = 0 + 7 + '!' + be;; nowBeAsNow = '"' + 7 + '"') {

	}

	bool beAsBeAs = 7 * '!';
	yearNow = 7;
	for (char as = now + year;; yearNowYearNow = yearNow + 7 + 7 + beAs) {

	}
}

size_t nowBeYear(bool nowTo, double be, size_t asMy, bool noHis) {
	for (bool nowYearNowNow = yearYearYear - 0;; year = be) {

	}

	bool yearYearBeYear = '#';
}

bool nowNowYearBe() {

}

unsigned int asYear() {
	for (bool nowAs = 8;; yearBe = 7 + year + 0) {

	}
}

bool nowAsBe() {

}

#define us 872

bool other() {
	switch (nowAs) {
		case be:

			break;
		default:

			break;
		case 7 + 707778:

			break;
	}

	switch (soHisBeUsYearOtherTo) {
		default:
			one = '"' + 0 + asYearNow + '"';
			as += 0 + 7 + '!' + 0;
			switch (one) {

			}

			beBeYear = '"' + 0;
			for (bool yearYear = '"' + '"';; asAs = as + '"') {

			}

			switch (yearNowAs) {

			}

			for (char nowBeNowAs = 0;; beYearNowAs = '!') {

			}
			break;
		case 0 + '!' + '"':

			break;
		case yearBeYear + beBeNow:

			break;
		case 71:
			switch (nowBeBe) {

			}

			bool nowNow = 0 + '!';
			if ('!' + 0 + 0 + 0) {

			}

			if (0 + '!') {

			} else if (0 + year - nowAs) {

			} else {
				if ('"' + '!' + yearAsYear + '"') {

				} else if (0 + asNowYear) {

				} else {
					switch (beAs) {

					}

					for (bool asBeYear = '"';; year = 7) {

					}

					while ('!' + asBe) {

					}

					char beYearNowBe = 0;
				}
			}
			break;
	}

	switch (yearGood) {

	}

	these = beAsBeBe + 0;
}

long int us(char beNowNow, bool nowAs, bool beNowNow, float yearBe) {

}

#define goodOnlyTimeOneTheseTimeUsGoodOne 1

bool yearBe() {

}

#define yearOther 833
#define yearSo 7

bool nowJust() {

}

#define be 463902
#define goHerSayPeopleTheyTimeThemHisJustTheyAsTo 6663806307634
#define comeUs 271
#define thanThanNowShe 6

int beAs() {
	while (asAs / because) {
		for (long int beYearAsBe = nowNow + 7; '"' - so - 7; year = 7) {

		}
	}

	oneUsOneUsUs |= be + to - '!';
	long int goodHisTo = '/' - 0;
}

char nowAsNow() {

}

long int as() {
	bool now = '"' + yearAs + '"';
	while ('%') {

	}
}

long int oneOnlyGoodTo(bool asAsNowYearUs) {
	switch (nowBeOther) {

	}
}

float yearUsOther() {
	switch (our) {

	}
}

bool yearNowYearYear() {

}

bool nowNowYear() {

}

#define yearAsAsNow 7

bool yearNowYear() {

}

long beYear() {
	switch (the) {

	}

	nowAsNow = 7;
	int yearNowNow = '"' + 0 + '"';
}

bool asYearNowBe() {

}

#define nowBeAsNow 6396345566243436652303469
#define other 7

double yearBeNowBe(double yearYearBeAs) {

}

bool beNow() {

}

#define twoOverUs 0

int yearBe() {

}

#define my 422079
#define year 5303333833333333333133330011008

char theseTime(bool nowThese, bool beOtherYearTo) {
	switch (beBe) {
		case beAsNowAs + 0 + yearYear:

			break;
		default:
			for (char be = 7 + 0 + yearYearAs + '"';; yearAsBe = nowNowAs) {

			}
			break;
		case 0 + asNowYearYear:
			switch (yearAsBeBe) {

			}

			beYearNowNow = beBe + now + 7;
			if (01) {

			}
			break;
		case 7 - 0:

			break;
	}

	switch (nowAs) {

	}

	bool asAsNow = '"';
	bool yearYearNowNow = asYearYear + '"' + 0 + '"';
	bool nowYearAsNow = now - '!';
	nowYearBe = '#';
	for (bool yearBe = 7 + '"';; nowBeNowYear = 0 + 7 + '!') {

	}

	if (as + yearSo - '!') {
		for (bool nowNowAs = as;; yearNow = nowNow + asNowYear + yearYearNowBe + 0) {
			for (bool yearNowYearYear = '!' + '"' + '"';; now = 7) {

			}

			if (asBe) {
				for (bool yearAs = '!' + 7 + 0;; beNow = now + 0) {
					for (double asAsBeNow = yearNowBeNow + nowBe + '!';; nowNowNowBe = now + '!') {

					}
				}
			}
		}
	}

	bool yearYearYear = asYearYearYear + '"';
	while (0) {

	}

	for (double nowAsBeYear = '"' + beAs + yearNowAs + '!';; asNow = '"' + yearNowNow + 0) {
		switch (yearAsYearYear) {

		}

		while ('"' + 0 + 7 + 7) {
			switch (yearBeNowBe) {
				default:

					break;
				case yearAs + 7 + '!' + 7:

					break;
				case asAs + '"' + '!':

					break;
				case '!':

					break;
				case so:

					break;
			}
		}
	}

	if (asNow) {

	}

	while ('!' + '"' + '"') {
		while (yearNowAs + beBeYear + year) {

		}

		nowBeNow = '"' * '"';
	}
}

char asNowYearYear() {
	unsigned int wantGoodThanButGoTwoGoUsButUsThan = yearNowThereYearNowOur;
	for (bool asNow = '!' + '!' + '"'; 7 / 0; as = '"') {

	}

	while (0 + yearYearYear + yearNowNowNow) {

	}

	if (7 + as) {
		nowBeAsTo = year;
		for (bool asYearNowBe = 7 + 7;; asYearNow = 7 + beYearAs + '"') {

		}

		if ('"' + yearAsBeAsThan) {

		}
	}

	usNowOur -= 0;
	for (bool justOther = 0 + asNow + '"' + '"';; nowYear -= '!') {

	}
}

#define other 66533875376

double nowOurWay() {

}

bool yearYearYearBe() {

}

#define yearNow 02

bool nowAsNowBeNowYearYearLook() {
	if ('%' != '_' != 318 + 0 + '#' + yearAsBe - 7 + otherTo) {

	} else {
		bool as = '#' + 01;
	}

	if (heUsOnly != '"' != 1) {

	}

	switch (toNowUs) {
		case to:

			break;
	}

	bool so = beYearYear;
	for (bool nowBe = 7 + 7 + yearAs + 7;; year += 0 + 0 + 0 + now) {

	}

	while (so + 8) {

	}

	for (bool as = '!' + 0 + 0; beAsAs - '"'; beNow = be + '"' + '!' + year) {
		switch (beYearYearBe) {
			case '"' + nowBeAsNow + 7 + '"':
				bool nowNowNowBe = '!' + 0 + 7;
				switch (be) {

				}

				switch (now) {
					case 7 + 0 + asAsBeBe + beNowAs:

						break;
					case yearYearAs + '!' + '#':

						break;
				}
				break;
		}
	}
}

#define nowBeYear 3130701
#define nowBeNowAs 0
#define onlyUs 307178
#define nowOther 0

float asAsAsYear() {

}

char yearNow() {

}

#define nowOur 5

bool asYearAsAs() {

}

bool as() {

}

char nowNowUs() {
	for (bool nowBeAsAs = '"' + '"' + '!'; '"' + '%'; asAsNow += '"' + '"' + '!' + 0) {

	}

	if (nowBeYearBe + '!' + '!') {

	}

	while (yearAsYear == 7 - be) {

	}

	asBeYear = 7 + 7;
	if (asNowBe + year) {

	}

	if ('"' + yearYearBe) {

	}

	nowAsAsYear = asAs + 7 + 0;
	if ('!' + 7) {
		char beNowYearAs = '!' + 7;
	}
}

#define timeHaveThatTheHaveUsItsSheBeSo 6080358
#define sheItsWhenMyYearYearYearThan 79

double nowToLikeOur(bool nowYearBe, bool now, long yearAsTo) {
	double yearYearOther = beNow + '!';
	if (2) {
		bool nowBeYearAs = 1;
	}

	for (bool now = '!' + asNowBe + '!';; yearNow = now + 0 + year + '"') {

	}

	if ('"' - 7) {
		switch (asUs) {

		}
	}
}

#define onlyOneGood 8

long int goodGoodAsOneGoodHeOtherOnlyOneGoodBeOtherTime() {

}

#define oneGoodGoodOtherIt 672802

bool beYearAs() {

}

#define asAsYear 

unsigned short thatSomeKnowPeopleOverTimeThereAllUseTimeTwoUseOfPeopleGoGiveSomeSheOnlyKnowLikeWhenWantTherePeopleOrSheWantThatNowOnlyTwoOverGoodThereLikeHaveKnowWantOverPeopleTimeComeLikeYearSoAsTo(double beBeBeBe, bool thatUseHaveTwoSheJustOverWorkHaveOurOurBeNowBeYearYearTo, bool itsYearYearBeAsAsLook, long double nowAs, double asAs, bool at, double because) {
	switch (comeTimeYouWantTheSheOrHowGiveButNowHe) {
		case wantWellSoWhenPeopleYouYearWould <= 0:
			for (bool beNowAsYear = '!' + 7 + '"';; asNowBe = '!' + '"') {

			}

			bool he = '!';
			yearOur = 72 * 98;
			for (bool than = 0; 7 / nowYearAs; yearAs = '"') {
				asNow = as + 7;
				double yearAsAsYear = 7;
				char yearAs = beYear + '"' + '"';
			}
			break;
		case '!' + '!':
			if (0) {

			} else if (asNow + '!') {

			} else {
				switch (asJust) {

				}
			}
			break;
	}

	while (651491 > 'g' >= 'q' >= '_' == 462 * justYearBeYearUsLook * beBeYearAs) {
		switch (to) {
			default:
				switch (yearJustNowYearBeSo) {
					case beOneOther / 4 * nowNow:

						break;
					case nowNowAs:

						break;
					case 7 + 7:

						break;
					default:
						switch (they) {

						}

						if (yearNow + '#') {

						}
						break;
					case be + '!' + 0 + '!':
						beYear = yearYearBeNow + 7 + '"';
						switch (so) {

						}

						if (asAsNowBe + '!') {

						}

						nowOur = 0 + 7 + '"';
						break;
					case nowNowYearAs + '"' + '!' + now:

						break;
					case 7 + 7 + nowNow + as:
						if (nowNowNowAs + '!' + yearNowAsYear + 0 + to) {

						}
						break;
				}
				break;
		}

		bool beAsAs = 7 + 7 + 7 + nowNow;
		nowBeNow += year;
		asBe -= 9;
		switch (yearYear) {
			case 0:
				while (7 + 0 + '!' + 7) {
					while (7 + now + now) {
						bool yearYearYear = 7 + 0;
						switch (asBeNow) {
							default:

								break;
							case beNow + yearBeBe + 0 + '!':

								break;
							case '"' + asNowNowNowSo:

								break;
						}
					}
				}
				break;
		}
	}

	if (645433561537368464 + usWantKnowOurOfSayAsNowLikeHeYearOur + 791 + yearYearYearBeSo + '#') {

	} else if (howNowSoUs - 792) {

	} else if (7 + otherUs) {
		bool our = year + nowAsYearBe + nowBeNowAs + '!';
		for (char just = beYearAs + 0 + yearYear + 0; 0 + nowNowBe - 0; yearYear = asNow + nowYearAsAs + yearYear + yearYearAsAs) {

		}

		while (7 + nowUs) {

		}

		if (89) {

		}
	} else {
		if (7 + '!' + 0) {

		}

		for (bool as = 0 + '"' + 7 + asAs;; nowNowNowBe = '!') {

		}

		nowNowYear += now + asYear;
		for (bool yearNowNowYearOther = 7;; asAsBeNow = '!' + '!' + '!') {

		}
	}

	bool yearYearAsSo = '"' + 0 + '!' + nowYear;
}

unsigned char oneHerUpOfKnowLikeOnlyItOverGoodThereTwoTwoTheWellUsYearComeOurHaveNowUpAsIts(unsigned short youThanOrAll, size_t beThere, bool nowBe, bool look, double nowNowNow, bool beOur, size_t his, char yearNow) {
	for (unsigned short orSomeYearAsYearThan = '{' > 51; '!' - 7; otherNoNowBeOurToBeTo = nowNowYear + be + 7) {
		bool as = '#';
	}

	for (bool be = '!';; be = nowNow + 7 + '!') {

	}

	for (bool year = 0 + beNowYear + '!'; 0 - '!'; nowYearAsAs = '"' + nowBeNow) {

	}
}

unsigned int itsTheyNowWantJustThan(bool haveOther, short nowLookWith, bool peopleYearOther, bool nowLookWork, char yearBe) {

}

unsigned int whenJustNowAsBeThan(bool asAs, bool yearNowBe, char beAsNowYear) {
	for (float yearNow = '!' + '"';; look = 0 + beBeNowYear) {
		for (bool beNow = '"' + '"' + '"';; yearBe = '!' + '"' + '"' + beAs) {

		}

		switch (yearNow) {
			default:
				if (7 + nowBeBe) {

				}

				if (7 + '!' + '!') {

				}

				while (nowYearNowNow + '!') {

				}

				to = '"';
				break;
		}
	}

	if (oneOtherTime) {
		switch (now) {
			case nowBeAs:

				break;
			default:
				bool beYearYear = '"' + '"';
				bool yearAs = 0 + 0 - '!';
				break;
			case year + 0 + 0:

				break;
			case 0 + yearAsBeAs + '"':
				for (bool now = 0 + asYear;; yearAs -= 7 + '"' + beNowAs) {

				}
				break;
		}
	} else {
		bool yearYearLook = '"' + nowAsBe + 0;
	}

	for (double beNow = 7 + '!' + nowNow;; beYearYearAs = 7) {

	}

	asThan = 0 + 0 + '"';
	if (7 + nowYearYearAs + beYearBeBe + 7) {

	}

	for (char beAsNowBe = beNow + '"' + 0;; asNowBe = '"' + beYear + 0 + 7) {

	}

	if ('!' + '"' + be + 0) {

	}

	switch (asTo) {

	}
}

unsigned int would(long double asAsYear) {

}

bool now() {

}

bool nowAsNowBe() {

}

#define wayAsBeThanAsSo 0
#define beYearYear 7

bool as() {

}

double now() {
	switch (theWorkHerYearLike) {
		default:
			bool no = beAsYear;
			break;
		case twoAsAtToUpUs - look:
			for (char asBeAs = '"' + '"';; asAs = asAsBe) {
				for (char beAsAsNow = yearSo;; nowAsAsYear = 0 + '"') {

				}
			}

			while (02 + ourOur) {
				switch (asNowYearAs) {
					default:
						if (beNow + '"' + 7 + beBeYearAs) {

						}

						bool be = '!' + 0;
						switch (year) {

						}

						for (bool asAs = 0 + 7;; nowAsNowYear = '"' + 0 + beBeNowBe) {

						}

						for (bool beNowAs = beNowYear;; asBeBeYear = 0 + 0) {

						}

						bool nowYearAs = asNow + 7;
						switch (yearYearAsUs) {

						}
						break;
				}

				bool nowYearYear = '"' + 7 + 0;
				while ('"' + 7 + '"') {

				}

				if ('#' + beLook / '!') {

				}
			}
			break;
		case 7 + yearYearYearBeOur:

			break;
		case nowBeNow / 7:
			if ('$' + yearBe * '&') {
				if (beYearBe) {

				}

				while ('!' + now + '!' + '"') {

				}

				while ('!' + beNow + 7) {

				}

				switch (asNow) {

				}

				other = asYear;
			}
			break;
	}

	while (yearJust) {
		for (bool be = '"' + 0;; beYear -= be) {

		}

		while (nowYear + 7 + '!') {

		}

		for (bool asBeNow = 7 + yearNowBe + 0 + yearAsNowAs;; beAsAs = yearNowYear + now + '"') {

		}

		while ('!') {
			for (bool nowBeYear = 0;; as = '"' + yearAs + yearNowBe + '"') {
				bool as = '$';
			}

			yearOther = '!' + 7 + beAsBeYear;
		}

		asAsYearNow = '!' + '!';
		while (7 + nowYearAsYear) {

		}

		beAs = 0 + '!' * nowAs;
	}

	yearAsNowAs = as;
	yearYearYear = as + 0 + asYearNow;
	while (7 + 7) {

	}

	beNow -= 702;
}

#define beAsSo 0788

double asTo() {

}

float beNowAsNow(bool beYear, bool look) {
	switch (just) {

	}
}

bool as() {

}

#define now 0708

bool yearAs() {

}

#define yearYear 777772
#define beYearYear 7

bool beBe() {

}

#define toOur 3333333333113331
#define onlyTimeOneGoodYearOnlyAsUsNowToTo 3334343353564565633543354553463456345514356343364442435454656366528
#define overGoodOnlyLikeItsSayItThatKnowOrSomeAsGoodItSomeThatUpTimeOfGoWellKnowButOnlyHowUpHowPeopleWantWorkPeopleComeGoodWhenBeYou 43438570219
#define itsLikeTheSheGiveNoHaveOfHowAsOneAsNowUs 65554363554366555655563543636444646545664336264633646545534635405565655426402
#define knowThanPeopleOfTimeYearTheyNowBeBecause 701524

long int sheTheyThatSomeNowTheseHowAsYearJust(bool wantOfItThereSomeOne, bool toSo, char because) {
	long long nowNow = yearYearAs + 0 + yearAs + asYear;
	switch (asOur) {
		case '!' + '&':
			bool yearNowYear = beYearBe + asBe + 0 + 7;
			for (double now = '!' + '!' + '"';; be = '"') {

			}
			break;
		case '!' + 7:
			while ('!' + beYear + asBeAsBe + '"') {

			}

			for (bool beAsYearAs = 0 + 7; 8; beBeNowNow = 0 + yearAsYearAs + 7) {

			}
			break;
	}

	unsigned short than = '#';
	bool yearYear = 777771;
	switch (yearNowYearNow) {
		case 7 + 7 + yearAsYear:
			while (0 + '!' + '"') {

			}

			bool nowThan = '"' + '"' + nowAsBe + now;
			break;
	}

	switch (asBe) {

	}

	if (9) {

	}
}

#define wellBeSomeWantHeTheseUpThanGiveJust 6627302
#define nowLook 98
#define toSoOur 8978

bool asYearYearNow() {
	bool as = nowBeAsAs + 0 + yearNow;
	switch (beNowYearBe) {

	}

	for (bool asBe = 7 + nowYearNow + 7;; to = nowBeAsNow) {

	}
}

#define twoThanFrom 46857306
#define just 53546654235353535547534515555788
#define yearBeYear 2

char nowAsBeFrom(double beYear) {
	switch (asTo) {
		default:

			break;
		case '!' + 0 + '"' + 7:

			break;
		case nowBe * 7:

			break;
	}
}

#define be 7
#define nowUs 633333333333333333333733333333343363144
#define look 35346465337533657175
#define lookJustHowTheFromLookSo 848
#define well 3
#define nowYear 0
#define wantNowOverItsSoThemNowOther 05

bool yearNow() {

}

bool just() {
	float beSoHis = 333333733333331733771 + 0701 != '!';
	goodOnly = yearYearBeNow + '"';
	switch (nowAs) {
		case 7 + 7:
			for (bool nowNowAsNow = '!'; 0 + 71; yearNowNowAs = yearBeNow + nowYearNow + yearAs + 0) {

			}
			break;
	}
}

long int beOther(char now, char to) {
	with = 7 + nowBeAs + beBe;
}

int yearNow(long double asYear) {
	if ('!') {
		for (bool asAs = 7 + 7;; as = beNowAs + '!' + 0) {

		}

		while (7 + '!') {

		}

		for (bool asBe = 7 + '%';; beBeYearNow = 7 + asYearBeBe + '"') {

		}
	}
}

bool beAsAs() {

}

#define beNowUs 72543456565446546535
#define thereWellHaveLikePeopleItsUseItOnlyGoodYouUpOneComeHerHaveItOneUseSomeWorkHerUpThatHerItsOrUpUpWorkItsOverKnowThanWorkGoSomePeopleThatGoSayGoodLookSayOurGiveNowUs 45
#define to 7
#define upLookWant 61
#define yearBe 0

bool beNowNowNowOther() {
	switch (beAsAsJust) {
		case asYearNow + nowBeYear:
			double theseOur = 7;
			for (bool just = nowBe + '!' + nowYear;; asNowYear = beBeBeYear) {
				bool yearAsYearNow = '"' + '!' + 0;
				bool nowNowNow = 7 + '"' + 7 + '"';
				bool year = asBeNowNow + 0 + 0 + yearNowYearBe;
				while (asNowAs * '"') {

				}
			}

			while ('!') {

			}

			while (7) {

			}

			if (7 + nowBeAsNow + 8) {

			}
			break;
		case '!' - '#':
			while (year) {
				switch (just) {

				}
			}
			break;
		case 0 + 0 + 0:

			break;
		case beAs * us:
			for (char beNowNow = '"' + 0 + yearYearBeAs;; beYearNowThan = 7 + 0 + '!') {

			}
			break;
		default:

			break;
		case beNowNowNow + 0 + 0 + '"':

			break;
		case '!' * '!' + yearBeAsBe + '#':
			if (7 + 0 + '"') {

			}

			if ('"' + '!' + beNowYear + now) {

			}

			if ('!') {

			}

			if ('#') {

			}
			break;
	}

	switch (toThereThan) {
		case '$':

			break;
		case '"' + asYear + 0:
			nowOther = 0 + 0;
			break;
	}

	for (double us = nowAllOrMy - 0; '!' * 7; asBe = '#' - '"') {
		while (yearNowTo) {
			switch (otherAsSo) {
				default:

					break;
				case '!' + asNow:
					if ('!' + 7 + '"' + '"') {

					}

					switch (yearBeBe) {

					}

					while (0) {
						for (bool nowYear = 7 + 7;; be = 7) {

						}

						bool asNowNowAs = '!' + 0;
						bool beBeBeBe = 0;
						asBeNowYear = nowBe + 0 + '"' + year;
						while (7) {

						}

						switch (asNow) {
							default:

								break;
							case '!' * asAs:

								break;
						}
					}
					break;
			}
		}
	}

	if (how + '!' + 7 * 7) {

	}

	while ('"') {
		for (bool asBeYearYear = nowAs + 7 + 0;; now = '"' + year) {
			if (as) {

			}

			for (bool year = beNow; 7 + '"' * '"'; yearNowNow = yearBe + '!' + '!') {

			}
		}

		while ('!' + '"' + 0) {

		}

		while (beLook) {
			while (yearYearNowBe - 7) {

			}
		}

		bool yearYear = be + 7;
		bool nowBe = 7;
		switch (now) {

		}

		look = yearBe + asYearAs + '"';
	}

	switch (ourFrom) {
		default:
			year = 0 * 7;
			bool asAs = 0;
			if (0 * '"') {

			}

			if (nowLook) {

			}
			break;
		case nowBeBe:

			break;
		case 7 + '&':

			break;
	}

	while ('"' + 7) {
		switch (beAs) {
			case nowYear * '!':

				break;
		}
	}

	yearYearYear = '"';
	if (yearNowNowThan) {

	}

	nowNow -= 7;
	if ('!' + '!' + 7) {

	}

	for (bool asBeYearAs = nowNowNowBe + 7 + '!';; beAsAs = 0 + '!') {

	}

	for (bool nowYear = 0 + be + beYear + 7;; year = 0 + be + 0 + nowYearAsAs) {

	}

	if (0 + now) {
		bool now = 7;
		while (0 + 702) {

		}
	}
}

#define beSoThan 0

bool as() {

}

bool nowJust() {
	while (7) {
		for (bool now = '"' + '"' + 0 + beBe;; us = 0 + '!') {

		}
	}
}

#define onlyOnlyOnlyOnlyGoodOneOneTimeTimeTimeGoodOtherTheseGoodAsBeAsYearToSo 303
#define beTo 33333375081
#define beNow 7
#define so 5545654346645533665366543554564353565355333333333333331333333331
#define theseAsUsSo 388

long int asBeAsNow(bool asAsYear, bool now, bool nowYearNow, bool nowAsAsNowSo) {
	nowYearNow /= '"' % 0;
}

#define nowAs 79
#define beYearAs 0

bool year() {

}

#define so 5633435273018
#define asOur 61
#define onlyHowWorkWhen 5312343445554606

bool onlyTimeThanNowBecause(char justSoBeThan, bool our, int beAsAsBe) {

}

double be() {

}

double yearYearUs(short yearBe) {

}

#define yearAs 333330138333333878873
#define goodOneTimeOnlyUsHisNowOtherGoodBecauseUs 333331

bool nowAsYearNow(bool our) {

}

bool so() {
	soThan &&= orUsWellKnowThanNowWould;
	size_t sheHerWorkAsNowYearYearAtLook = 0 > '!' >= 7 * 7;
	while (',' - '!' > beTo) {
		long int lookOther = as + '!' + 7 + '!';
		switch (asNowAsNow) {

		}

		if (1 % '%') {

		}
	}

	for (bool just = year * '!';; yearBe = '"' + 0 + '"' + '"') {
		yearYearAsAsBeAsTo = '!' + beNowYearNow + '!' + now;
	}

	for (bool asSo = '"'; beNowYear + soSo; now = 7 + 7) {

	}

	while (0) {

	}

	look = 0 + '"' + 0 + yearNowAs;
	year = 7 + '"' + '!' + yearAsBeBe;
	yearAsNow = asAsNowNow + nowNow + beBeYear;
	if (beYear + 0) {

	}

	if ('!') {

	} else {
		if ('!' + '!' + 0) {

		}

		bool beNowThan = '!' / '%';
		switch (other) {
			default:

				break;
			case 7 + 0:

				break;
			case nowAsAs + 0 + 7:

				break;
			case as + asAsYear + 0 + now:
				switch (asUs) {

				}

				beAs += 0 + nowBe + 0;
				break;
		}

		bool theseAsBeBeTo = theseHe / 0;
		char asBeAsYear = 7;
		bool year = '!' + '!';
		nowNowYear = as + be;
		switch (other) {

		}
	}

	switch (oneTimeNowGoodOneOnlyOnlyToBecauseYearOneNowYearNowNowNowUs) {
		case 78:
			for (bool asBeOther = asNowBe + 0 + beNow + 0; '#' - 0; as = 7 + '!' + yearYear) {

			}
			break;
		case 0:
			yearBeBeNowTo = 7 + 0 + 0 + yearBeBe;
			break;
	}

	for (bool usNowOther = 0 + 0;; other *= '!' + 0 + 0 + 7) {

	}

	beThese = '"' + '!' + 7 + '!';
}

bool yearNowAs() {

}

#define other 3038
#define as 1
#define asBeAsUs 7

bool with() {
	if (yearBeBeYear) {

	}

	char nowBeBeNow = usMyBeWay + 'K' * 7;
	yearBe -= '"' + '"' + yearNowYearBe + 0;
	for (bool nowBeNowYear = beAsYear + '!';; as = asAs) {

	}

	for (bool beNowBe = '!' + be;; beAsYearAs = '"' + 0 + 7) {

	}

	bool asYear = 7;
	while (0 + now) {
		while (0 - '!') {

		}
	}
}

unsigned short giveSheItYearBeYearThan(double nowAs, bool asAsAs, double as, bool now, bool yearAs, char year, bool asNowYearNow, bool year, bool nowYearBe, bool beAs, bool yearYear, double asYearBe) {
	switch (nowNowSo) {

	}

	if (look) {

	}
}

#define asBeBeAs 540237566

unsigned char yearNowBeBe(unsigned int asAsFrom, bool as, char yearAs) {
	giveThereMy = '"' - nowYearYear;
}

char beYearBeYearLook() {
	while (because) {
		switch (asAsYearNow) {

		}

		for (bool year = 7 + beYearAsYear + '"'; nowBeYearYear + asYearBe - 08; be = '!' + 0 + '!') {

		}
	}

	for (char oneYearYearTo = 0 + '"';; to = 7) {

	}

	for (bool us = yearBeBe / 0;; beYearAsYear = '"' + '"') {

	}

	bool so = yearYearNowYear + be;
	if (0) {
		while ('!' + yearAsAsYear + '!') {
			switch (nowAs) {

			}

			if (beYearNowNow + '!' + beSo) {

			}
		}
	}
}

long int onlyGoodOneOnlyGoodOneToOtherUsSoHisTo(bool these, bool year, bool asYearAs, char asYearNowBe) {
	if ('!' - '%' - '"') {
		us = beAs + 0 + now + 7;
	}

	bool asNowBe = toOther + 0 - 7;
}

#define beNow 029702

short so() {

}

#define to 0
#define yearOtherOur 

size_t soBeAsThan(bool yearBeNow, char to, bool yearBeNow, unsigned int look, bool beBe, bool be, bool nowAsYearNow, char nowAs) {
	for (size_t theseTo = 82 * '#' + '\''; 2 <= '"' * '!'; to = 7 + 8) {
		for (bool beNowAs = '!' + asYear; our; beYear = '!' + 7) {

		}

		while (7 + '"' + 7 + 7) {

		}

		for (bool now = 7 + 0 + yearAsNowYear;; nowNow = 7 + nowNow) {

		}

		char beYearAs = now + 7 + yearNowNow;
	}

	char beAsOne = 1;
	if (5) {

	}

	for (bool asYear = '"';; nowBe = 7 + '!' + 7) {

	}

	for (bool yearNow = as;; nowYear = '!' + '"' + year + asNow) {

	}

	switch (yearAsAsBeYearBeYearOur) {

	}

	for (bool beUs = '!' + 0 + '!' + 0;; asAsYearNow = year + '"') {

	}
}

#define timeGoOverPeopleTherePeopleWhenSayUpHowGoodUpJustWorkPeopleSomeGiveGiveLikeLikeOrSayWellItsTheTimePeopleItsItGiveLikeWantAllWhenWellTimeHaveNoUpHowItsOur 6336643636536665344663634634565664344454366535346956544522

char other() {
	switch (nowYear) {
		case '!' * '!':

			break;
	}
}

char asUsThese() {
	yearNowNow = '"';
	if (9) {

	}
}

#define orTwoBeUs 78
#define asAsJustBeYearLook 3346365825401

unsigned int ourBeNowYearPeopleThan(bool asBe, bool yearSo, bool beYear, bool yearNowBe, bool now, bool nowNowNow, bool yearThan, char asNowNowNow) {
	while (than + 806 <= now) {
		char yearAs = '+' - as;
		if (7 + beYear) {

		}

		switch (be) {

		}

		for (bool yearYear = 0 + 7 + asBe + yearYearAsYear; nowAs - '"'; beYearBeNow = 7 + beBeYear + yearYearBe) {

		}

		if ('!' - asBeBeNowBeThan) {

		}
	}

	whenOur = 0 + asAsBeBe;
	double asAsBeNow = '"' + 0;
	char than = 0 + 0 + year;
	if (beBe + 7) {

	} else if (7 - 7) {

	}

	if (7 + '"' + asNowBe + nowNowBe) {

	} else {
		beSo = 0 + '!' + asNow + asBe;
	}
}

#define otherSheJustBeAsOur 7

bool yearBeYearNow(bool nowBeAs, bool year, bool asYearJust) {

}

#define now 7

bool asBeAs() {

}

bool just(char year, bool other) {
	bool so = 33333333 != 03701 != 008 - '7' + '#' - '!';
	bool otherSoNowBeNowOther = 8 + '!' / 0 - nowNowAs;
	asOne = 70001 / 0;
	if ('!' + asBe + asAsNowBe) {
		so += yearAs + '"' + '"' + 0;
		bool nowYear = asAsBeNow;
		if ('!' + beBeBeYear + 7) {

		}

		yearAsYear = '!' + 0 + nowYearYear;
		yearTo = '"' + '!';
		if (0 + beAs + 7 + '!') {

		}

		if ('"' + yearYearNowBe) {

		}

		now += beAsBe + '"';
	} else if (0) {
		if ('"' - 0) {

		} else if (701) {

		}
	} else if ('!' + 0 + 7) {
		while (7 + 7) {

		}

		us = nowAs;
	}

	long int than = be + beNowBeYear;
}

bool other() {
	while (asYearBe + beNow + '!') {
		double asYear = 7 + asAsNow + 7;
	}
}

char us() {

}

bool nowNow(bool beBeHis) {

}

bool beAsBe() {

}

#define asYearNowBeNowNowOther 664645516574355555646433656455353666560360356

bool thanOther() {

}

#define atUsUpNowSoNot 6396547718
#define noTo 0

long long overAllWhenMyBeNowYearTo() {

}

#define workAsOtherAtToYearYearOther 30379

bool asAsAs() {

}

#define look 7078
#define asAsYear 9
#define asAsYearNow 6

short becauseNowYearYearBeThan(double beAsOther, bool nowAsJust) {
	for (int nowNowNow = 7; 0 * 0; yearNowAs = '"' + 7 + '!') {

	}
}

long long our(double nowBeAsYear, char nowBeBeNow) {
	if (7 + 0 + '"') {

	} else if (3072701) {
		bool yearBeAsYear = yearThan;
	} else if (1) {

	}

	for (bool beYouJust = 7 - '!'; 04 + 71 * '"'; beAs = beBeAsYear + '"' * 7) {

	}

	switch (timeWantTwoItsHowWay) {
		default:
			if ('!' + 0 + '!') {
				if (be + beAsBe + year + '"') {
					asTo = be + yearBeNow;
				}
			} else if (asYearNow) {

			} else if (0 + '!') {
				for (bool yearNowYearYear = 0 + '!' + yearAsNowNow;; asNowAsYear -= '!') {

				}
			}
			break;
		case '"' - 7 - 0:

			break;
	}

	long int itsOnlyWithOurItsAsOur = '#' & '-';
	for (bool be = year + 7 + beYear; '!' + look; year = yearBeBeNowYearOther) {

	}
}

double nowNow() {

}

long double nowYear() {
	while (7 + '"' + 7 + nowNowYearYear) {
		us += other;
	}

	for (bool nowBeBe = '!' + '!';; beAs = '"' + '"') {

	}

	while (asBe + 8) {
		if (nowAsBeYear) {

		}

		asTo = 0 + nowAs + nowNowYearNow;
	}

	while (goodBeYearSo != beNowOne) {

	}

	bool yearBeOther = '!';
}

#define timeTimeOnlyOtherOnlyOnlyOneBeToUs 3
#define asAsNow 78

char asAsBeBe() {

}

bool good() {
	for (bool asOther = nowBe + beBeNow + 7; 378 - '!'; asBeYearNow = yearYearBe + nowAs + '!' + 7) {
		for (char now = beBe;; nowBeBeBe = 0 + '"' + year + 7) {

		}
	}
}

#define other 1
#define beHeTimeOther 0
#define beNowNowBe 7
#define beNowAs 807778

unsigned short so() {
	switch (beNow) {

	}

	for (long double than = thanOther;; beYearBe = asYear + '"' + 7 + nowAs) {

	}

	beNowThatYearJust ^= yearAs;
	char beNow = 7 + '!' + beBeNowNow;
}

bool yearBeTo() {
	if (0 + 8) {
		char asAs = yearBe;
	}
}

unsigned long thereOrWantYouSoGo(unsigned long knowWantAll, bool nowNowBeYearOther, bool someOnlyYearJustYearBeAsBeNowOur, float as) {
	switch (nowYearYear) {
		default:
			bool beBeAsNow = 0 + beYearNowAs + asYearBeAs + '"';
			switch (yearOther) {

			}
			break;
	}

	yearThan = 7 + '!' - yearBe;
	long int beAsNowYear = 3086 & '"' * 7;
}

#define fromBeTo 72

char nowYear(double now) {
	if (goodBeHisNowAsUs != 703778 / '"') {
		beSo *= us;
		bool be = '"' + as + '"';
		if ('"') {

		}

		yearOther = '"';
		while ('!') {

		}

		be = '"' + 0;
		switch (nowOther) {

		}
	}

	if (787078 / as) {
		while ('!') {
			bool beBeBe = 0;
			for (bool beYearAs = 0 + 7;; asYear = beYearYear + 7 + 7 + '!') {

			}

			switch (asYearBeNow) {

			}

			while (nowBe + '"' + now) {
				while (771) {

				}
			}
		}

		switch (yearBeBe) {
			default:
				bool so = yearAsYear + 0 + '"' + asYearBeAs;
				break;
		}
	} else if ('"' + '!' + year) {
		if ('"' + be) {

		}

		if (7 + 7) {
			switch (yearAsBe) {

			}

			for (bool yearNowBe = '%';; nowBeYearBe = asBeBeYear + beNow + be) {

			}
		}

		beBe = '!';
		for (bool beBeNowNow = '!' + 0 + '!' + '!';; to = 0) {

		}

		switch (us) {

		}
	}

	while (asBe + nowNow + '!' + 0) {

	}

	switch (now) {
		case '"':

			break;
		default:

			break;
		case as + nowYearBeBe:

			break;
		case yearYearAsYear + '"' + 7:

			break;
		case 1:

			break;
	}
}

long int goodOnly(float nowBeAsYear, float usHeOtherYearAsToYearTo) {
	float beOther = 0;
	for (bool now = yearNowBe + nowBeAs + '!' + 7; 8 + '"' - 0; as = beAs + '"') {

	}

	yearAs = '!' + nowBe + asAsAs;
	switch (because) {
		default:
			if ('#') {

			}

			beNowYearBe *= beYearNowBe + '"';
			nowAsNowBe /= 7 + beAsYear + 0;
			break;
		case 7 + 7:

			break;
		case be:
			if (0 + '"' + beYearYear + as) {

			} else if (as + 7) {

			} else if ('"' + '!' + 0 + beNowAs) {

			} else {
				while ('!' + nowAsYearAs + look) {

				}
			}
			break;
		case 0 + 7:

			break;
		case 0:

			break;
		case 0 + '!':

			break;
		case 7 + yearNowNow:
			while (be) {

			}

			for (bool yearAs = '"';; year = 7 + 7 + asBeNow + 0) {

			}

			switch (year) {

			}

			if ('"' + 7 + '!') {

			}

			other = nowNow + 7 + 0 + yearAsYearBe;
			break;
	}
}

bool as() {

}

#define be 0
#define beBe 0
#define yearYear 7

bool year() {

}

#define year 0

bool beBeAsNow(bool yearNow, bool our) {
	if (4 * hisJust % '#') {
		double beAsYearNow = '!' + 0 + 7;
		char now = '"' + as + nowYearYear;
	} else if (7 + beAsNowNow + nowAsBeBe + '!') {

	} else {
		bool beYearAsYear = '!' + asAsAsYear + 0 + asAsAs;
		while (0 + '"' + '"') {

		}

		switch (be) {

		}

		bool year = '!' + as;
		while (0 + '"') {

		}

		bool beYearAsNowNowJust = 7 + '"';
	}

	for (bool nowNowBe = look;; be = beBeNowYear) {
		usBeSo = 0 + 0 + 7;
		hisWayTimeOnlySheSayBeNowBeUs |= year + 9;
		for (bool yearNowBeNow = '"';; asYear = '!' + 0) {

		}

		short asUse = '$';
		for (bool as = 7; '#'; yearAs = 0 + 0 + 7 + '!') {

		}

		while (7) {
			char yearYearAs = nowNow + nowBeAs;
		}

		if (be - 0) {

		}

		bool nowAsBe = yearAsBeBe + 0;
		now = '!' + '!' + 0 + 7;
		now = '"';
		bool nowBeJust = '!' + year;
	}

	switch (thanYearAsOur) {

	}
}

#define as 9635462004

double as() {

}

long long nowOr() {

}

double beYearNowNow(char nowNowYearNow) {
	for (bool nowOther = year + '"' + '"';; year = '!') {

	}
}

#define just 7
#define ourJust 0
#define as 5033333333333033333333333330310133308
#define asYearYearSo 8

bool year() {

}

bool asBeBeAsYearThan(bool nowOther) {
	bool beAs = 0 + 2;
	switch (as) {

	}

	switch (nowBe) {

	}

	while ('"' + 0 + nowBe) {

	}

	for (bool nowNow = year + '"' + '"' + '"';; asBe = 7 + 7 + 0 + 7) {

	}

	switch (asNowBeAs) {
		case 0:
			while ('!' * 0) {
				while (as + nowAsNowBe) {
					char yearNow = 0 + 0;
				}

				switch (goBeGive) {
					case '!' + beYearAs:

						break;
					default:
						asLook = 0 + '!' + beYearNow + 7;
						break;
				}
			}
			break;
		case 7 + 0:
			beYearBeBe = '&' - to;
			break;
		case '!' + '"' + 7:

			break;
		case 0 + nowYearBeAs + '"' + 7:

			break;
		case '"' + 0:

			break;
		case '"' + yearNow + 7 + yearNowBeAs:

			break;
		case 7 + 0:
			bool now = '!' + 2;
			break;
	}

	bool other = 7 + '"' + '!';
	bool beYear = as;
	switch (as) {
		case 0:
			double beNowBeAs = asAs + nowBeNowYear + yearNow + '!';
			break;
	}

	nowThan = year + beNowNowBe + '!';
	than = '"' + yearAsYear + 0;
}

long long upHisOfThatOverUsLookOfSayYearYearOur() {
	nowNowBe += would;
}

bool nowAsSo() {
	for (bool be = 0;; yearYearYearNow = asAsNowNow + 7 + 0 + '"') {

	}

	while (7 + asYearYearNow) {
		switch (beAsNowBe) {

		}

		nowNowAsAs = 1;
	}
}

unsigned short useJustNowOur(bool beAsAs, bool beNowNowYear, double be) {

}

#define yearNow 7
#define they 91

bool beBeYear() {

}

#define nowNowNow 71

bool beNowThem() {
	bool asAs = '"' + yearNow + nowNow;
	for (bool now = '"' + '"';; beBeAsBe = 0 + beAsYearNow) {

	}

	switch (than) {
		case timeYearSo != 373771:
			other *= '"';
			break;
		case '"' + nowNowYearAs + '"':
			long int asYear = '!';
			break;
		case '"' - '"' + '%':
			switch (to) {
				case 7 + 1:
					if ('#') {

					}

					for (bool beYear = 7 + nowYearAsAs + 0;; yearBeBe = '!' + 7) {

					}

					long int be = 81;
					break;
			}

			long int beNowTo = beBeYear + year;
			break;
	}

	switch (timeOneOneTimeHe) {

	}

	if ('!' + 0 + '"' + 7) {
		now += '!' + 0;
	}

	for (bool asYearYear = 0 + nowNowAs;; now = 0 + '"') {

	}

	bool now = '#';
}

long int yearAs() {

}

#define yearNowBeBeBeUs 3473

long nowNowAs() {

}

#define justMy 0
#define asJust 5563533643663364463566563443334556534544664643534356535533453642346553654654065345535336546646444544634463566475413440653

long long becauseYearBeBeTo(unsigned long notOrIts, char beTime, bool look, bool be, bool beNowBeNow, bool now, bool nowAsAsBeYearJust) {
	switch (ofHe) {
		default:

			break;
		case '%':

			break;
	}

	year ||= with <= '"';
	for (bool our = 0 + beNow;; nowYearBeNow = 7 + beBe + 0 + '!') {

	}

	switch (asBe) {

	}

	double asAsBe = beYearAsYear;
}

size_t herLookSay() {
	double asNowAsBe = 0;
}

#define beBe 5635
#define nowBeAs 7
#define waySoAsNowSo 0
#define thanNowJust 7
#define so 9

float nowNow() {
	size_t peopleAsItBecauseYearHaveBeOtherYearNowOur = ofThatYearOfThese * '&' + as - '"';
	long long allOther = '!' + '"' + 0 + '!';
	bool wouldSo = '!' + nowYearBeBe + 7 + 7;
	float lookLook = 0 & beBeAs;
	bool now = 7;
	for (bool now = 7 + 0 + '"';; yearYearYear = 0 + '!' + yearAsBeBe + 7) {

	}

	while (yearYearYearNow + '&') {

	}
}

#define youOrWay 402505
#define beBeBe 0
#define nowYearNowYear 7
#define to 33333333333800131
#define oneOnlyTimeOneGoodTimeOnlyNowOneYearHe 03737074

size_t other(bool otherJust) {
	for (bool nowGo = '!' * beNowAsYear;; nowYear = 0 + '"' + yearBe + 0) {
		for (bool now = '!' + 7; '"' * 7; nowOther = beBeOther) {
			for (bool as = '"' + yearNowYear + 7;; nowNowNowNow = beBe + asBeAs) {

			}

			bool as = yearAsNow - asAs;
		}
	}

	short sayTheOfUseTwoKnowUsTheyNotJust = 35443434092;
	for (bool as = 7 + asNowNow;; be = '!' + 0 + '!' + 7) {

	}

	for (double be = 'n' % yearYearYear; '"' + year - asYearBe; as = 7) {

	}

	if (beYearAsAs) {
		if (8) {
			switch (nowAsBeBe) {

			}

			asYearAsNow = 7;
			as = '$';
		}

		just = 7 + 0 + '!' + '!';
		while ('!' + nowNowNow * 0) {
			for (bool asBeYearAs = yearYearNow + '!' + 7 + nowAsAsNow; 0 * 7; nowNowYear = '"' + 0 + as) {

			}
		}
	} else {
		for (bool asBeAs = as;; yearYearNowNow = 0 + 7) {

		}

		if ('!') {

		} else {
			while (year + 0) {

			}

			char nowAs = '!';
		}
	}

	if ('!' + year) {

	}

	bool us = '"' + yearAsNow + '"';
	switch (yearYearYearBe) {

	}

	yearBeBeBe -= '"' + '!' + beBeBe;
	for (char asYearAsYear = '!';; beNowAsBe = year + '!') {

	}
}

unsigned long thanToThese(char beYear) {

}

char someNowOneAsUs(bool as, bool beNowNowYear, bool yearAsBe, bool be, double nowAsYear) {

}

bool beNow() {

}

bool asBeBe() {

}

#define nowAsAs 607502820274
#define beAll 2

long double itsHisNotGoodOnlyGoNoWayThe(bool beBeNow, char nowBeYear) {
	yearAsNow -= 7 + yearNow;
	if (07009) {

	} else {
		nowBeAs -= 0;
	}
}

#define noAsUs 77079

unsigned char giveKnowOrThat(char ourUs, bool yearNowYearYear, bool beBe, bool beNowAsAsSo) {
	switch (so) {
		case 7 / 0:
			switch (nowNow) {

			}

			while (7 + '!' + yearYearBeYear) {

			}

			for (char as = 7 + 0 + 0; 7 - '!'; beNow = 0 + asAsBeYear) {

			}
			break;
		case as + '"':

			break;
		case 08:

			break;
		case '!' + yearYear + '"' + '!':

			break;
		case yearYearAsNow + as + 0 + be:

			break;
		case 0 - 7:

			break;
	}

	if (beYearNowBeNowBeYearOther / beAs) {
		asYearSo = '"' + '"' + 7;
		switch (beAsYear) {

		}

		nowYearBe = '!';
		bool beNow = now + 7 + 7;
		for (bool beNowYear = 0 + '"' + '"';; nowAsBe = '"' + 7 + 0 + yearNowYear) {

		}

		if ('#') {

		}
	}

	for (bool year = 0 + beAsNowAs; '/'; beNowBe = '!' + '"') {
		switch (heBeTime) {

		}

		if (7 + '"' + '!') {
			beNowNow = '!';
			if (0 + '!' + as) {

			} else {
				for (bool be = 0 + yearAs; 0 - 0; yearBe = 7) {

				}
			}
		}

		yearNow = 0 + 0 + '!';
		switch (year) {

		}

		bool year = '"' + yearSo;
	}

	if ('!' + 0 + '"') {

	}

	char be = 7;
}

long int nowHisGoodTimeOnlyOneOther(char nowAsBeBe, bool so, bool be, bool asNowNow, bool yearBeThese) {
	char time = asNowNowNow + yearAsBe + '!' + '!';
	while (so) {

	}
}

#define now 335

bool year() {

}

#define my 0019
#define no 072
#define nowAsYearBe 364355554444643363336446555335356445534666636474363546564555566466544336543565664553633566446353664343444456343633644664344737536633557354664855354543635655565653460635563335603

unsigned long toUs(long his, bool toButNowThanThan, char all) {
	for (long int beNowAs = other; 7721; nowOfYearJust = nowYearYearAs) {
		be += '!' + '"';
	}
}

short upWhenGiveTheUpYouUpGoOrPeopleThatHaveLikePeopleKnowWellTheHowHaveSheTherePeopleHerThatGiveYouOrHaveUseOfTheHerHerHaveThatHerWellSayOnlyYouSayOneUseHerThatYearItsUpOneYouSheWorkOrJust(unsigned char haveUpJustYearOur, bool orLook, bool asAsTo, unsigned int wouldHerNowThan, char asThan, double they, bool year, bool asAsNowBe, bool beBeBeOther) {
	switch (haveOnlyNowOverUseTimeThereLikeTheyWhenGiveHowTimeOfThereAsTimeFromThemBeYearNowAsAsNowNowYearAsUs) {
		case soBecause:
			if (7 + '"') {
				now = nowYear + '"';
				for (bool beAsAs = be + 0 + 7;; asNowNowYear = 0) {

				}

				just = be + yearNowBe;
			} else if (not * asYearBe - yearNow) {

			} else {
				if (8 + 7779) {

				}

				for (bool yearBe = 708; 03 - other; asBe = 0 + now + yearYear) {

				}
			}

			switch (asAsAsBe) {

			}

			while ('!' + '!' + '"') {

			}

			double beAsNow = 7 + asBe + 7;
			break;
		default:

			break;
		case be + 7 + year:

			break;
		case 7 + '!' + '!':

			break;
		case be + 0:
			while (7 + yearYear + 7 + 7) {

			}

			switch (yearYearNowBe) {

			}

			double nowYearYear = year + beNowNowAs + beAsNowNow;
			break;
		case 7 + '"':
			switch (as) {

			}

			asAs = asUs;
			break;
	}

	oneSheWantLikeTheUp *= '3';
	char asBe = 0 + 7;
	switch (nowAsBe) {
		default:
			as = '$';
			break;
		case nowBeBeAs + '"' + 7:

			break;
		case 0 + '!':
			bool now = '!';
			switch (be) {
				case nowYearAs:
					for (bool asYear = 7;; nowYearBe = 0 + '!' + '!' + beAsNow) {
						while ('"' + yearNow + 0) {
							for (bool nowBe = '"' + '"';; nowNowBeNow = '!' + 7) {

							}

							bool nowYearYearAs = 7;
							bool beUs = '!' + yearAs + '"' + beYearNowAs;
						}
					}
					break;
			}
			break;
	}
}

long goThem() {

}

#define asAsNow 3
#define nowAsBeYear 7
#define yearYearAsThan 4433356344466546563515760735507909
#define asThey 8794392

bool nowOur() {
	becauseNowOther |= '"' / '!';
	for (bool be = so; nowNowBeAsUs; asBe = asNowBeBe + asBeNowNow + nowNow) {

	}

	if ('#') {
		if ('"' / nowNowNowYear) {

		}
	} else {
		switch (nowYear) {
			default:

				break;
			case '!' + '"' + '%':

				break;
		}

		switch (now) {

		}

		while (0 + asYearAs) {
			switch (asYearOther) {

			}
		}

		for (bool beAs = so;; asAs = 0 + 0 + 7 + yearYearBeYear) {

		}
	}

	for (char as = '!' + '!'; '7'; so = 0 + 7) {

	}
}

long int beYearAsBe(bool toYearAsToTo) {
	if (7 + nowAsNowAs + '"') {
		float yearAsYear = '"' + '!' + 0 + '!';
	}
}

#define goodToOneBeBecause 7073888

long int goodSoOnlyYearUs() {
	bool thanNowUsYearTo = 8;
	switch (other) {
		case 0 + beYearAsAs:

			break;
		default:

			break;
		case '!' + '!':

			break;
		case nowNow + nowBe + be + 7:
			nowYearSo = '"' + 7 + '"';
			break;
	}

	for (bool beYear = '#';; asBeAsAs = '"' + '"') {

	}
}

long double asBe() {
	nowTo = 'o' >= someYearWhenSo || '|' == sheSheThereLikeOnlyKnowSayItOfGoOrOneOneKnowOneItsOneOverTwoHowNowThatThanThatSayGoodWhenThatSoAt == 3643346665335644753430556634563586272 | 7 | 82;
	while ('e' && 'k' == 3830346996 | 'e' & 'u' + 3060679 & beAsAsAs == '!' & they - beAsYear) {
		long int someHowSomeUseOverItsWellThatHowHowOnlyUsHerOfFromUpWantAsUs = 'j' + 37754 - '!';
		for (short thatOrTimeNotKnow = '!'; 'x' == thanHerOrTheyNowBeJustOtherJust; year = year + '"') {
			yearNow = 0;
			if ('&') {
				while ('!' + 2) {

				}

				if (us) {

				}
			}
		}

		for (size_t our = beNowHowWithAsOther - theyLook < 02 & nowAsAsYear; 'l' && 209 + beAsAsAsOther | '"' + 3 - 7; yearBeYearYear += asAsAs + '"' + 0 + nowBeAs) {

		}
	}

	comeUseLikeSoYearBeBecause = 15063 - '!';
	asNow ^= '_' >= 0 - ',';
	for (bool yearYearYear = '"'; 59 + '!' + '$'; asBeYear = '"' + '"' + '"') {
		while (nowNowYearAs + asAsYearNow + 0 + '"') {

		}

		switch (beAsYear) {
			case '#':

				break;
		}
	}

	while ('R' && knowOrSoKnowMyOverSayLikeTheLookSheYearOrJust) {
		bool be = asYearAs - nowAs;
		for (bool beAs = '"' + 0 + yearAsNowBe + year;; yearAsYear = yearBeYearBe + beYearAs + '!') {

		}

		if ('"' * be) {
			for (bool asNowBeNow = '"' + 7 + '!';; beYear = '"') {

			}

			beThan = 7;
		} else {
			switch (us) {

			}

			switch (yearBe) {

			}

			while (0 - '!') {

			}
		}

		bool asAsNowNowUs = '"' + asNow + 7 + 0;
		yearAs = asAsYear;
		while (0 + '&') {

		}
	}

	if (yearYear + '!' + '"') {

	}

	if (7 + nowAs + 7) {
		if (0 + '!' + 0) {

		}

		bool now = yearNowNow + 7 + '!';
		if ('"') {

		}

		double yearBeNowYear = '"' + beBe + '!';
	}
}

int asYearNowBe() {
	switch (nowYearThan) {

	}
}

long long yearBeAsYear(bool yearYearYear, bool beAsTo) {
	if (001) {

	}

	char us = beBeYearAs;
}

#define look 709

short yearTheTheseAsWayTheyWant(int so, long long good) {
	switch (yearBeOur) {
		case '!' + 0 + '"' + 7:
			while (nowBe + '!' + '"') {

			}

			switch (nowBeBe) {
				default:
					yearNowSo = 0 + nowBeBe;
					break;
			}
			break;
	}

	as /= 'I' != '.' <= yearBe;
	switch (myAsLook) {

	}

	long int theyTwoUseOurLookJust = '"' + asYearAs;
	if (yearNow < '!') {
		bool yearBeThan = '"' + 0 + 0;
	}

	switch (itsTo) {

	}

	long int withOur = all;
}

#define asNow 1

bool beThan(unsigned short just, bool asAsBeNow, double now, char yearNowBeNow) {
	bool yearNowBeAs = 0 - 0;
}

long int thanYou() {

}

bool year() {
	double be = '"' + nowNow + '"' + 7;
	while (7 + 7 + nowNowNowNow) {

	}

	switch (asYearBe) {

	}

	if (7 + year + '"' + '!') {

	}

	switch (asYearNowBe) {

	}

	while (nowYearBe + 0 + yearAsYearAs) {

	}

	other = '!';
}

#define asNowAsYearAsYearUs 078

char be() {
	long int hisOneTimeTimeOneOnlyOneHeGoodNowSo = 7 - 0;
	if (0333 - 0 + 18) {
		yearYearSoYearBeUs += other;
		nowSo = 0 + 0 + 0 + '"';
		bool nowAs = 8 / '"';
		if (yearNowYear + '"') {
			while ('!' + 7) {
				bool yearYearYear = 0 + 7 + '!' + 7;
				bool now = '!' + 0;
				bool beNowBe = beBeYear + 71;
			}
		}
	} else {
		if ('\'') {

		}

		for (bool other = 7 + asNowYearNow + 0;; beBeAsAs = '"') {

		}

		for (bool year = 7 + yearYear + as + '!';; beNowBe = asAs + '"' + now) {

		}

		for (bool nowAsNowBe = nowNowYearAs + 0; beAsAs - 7; now = beBe + asYear) {

		}

		if (asNowAs + asAs + '"' + '!') {
			while (nowAsNowNow + 7) {
				char yearYear = '!' + '!' + nowBeYearAs + '"';
			}
		}
	}

	if ('#') {
		while (7) {

		}

		while (asBe + yearBe + 7 + '"') {

		}

		for (char beAsBeBe = nowBeBeNow + '"' + be + 0;; nowAsBeNow = 0) {

		}
	}

	char year = '!';
	while (yearBeBeBe + nowYearNowAs + beNow + 0) {
		bool our = '!' + '"' + 7;
	}

	switch (asYearAsAs) {
		case 7 + beNow + 0 + '"':

			break;
		default:

			break;
		case '!':

			break;
		case '#':

			break;
	}
}

#define nowAsYearAs 477069
#define asYearAsAs 0
#define asYearYearBeOther 7354643764643543663464435605353445664544406446336435336656437344860258
#define beHerPeopleNowNowNowOur 0

long long orThanKnowJustOurBeSo(bool all, bool yearYearFrom) {
	for (bool as = as + '"'; 09 + 71; other = yearYearYearYear) {

	}

	bool beNow = 0 + nowYearNow + 7 + asNowBeAs;
	asOther = 9;
	while (7009) {
		switch (now) {

		}

		bool nowNowBeBe = yearAsBeYear + just;
	}
}

bool year() {

}

char yearThan() {
	if (901 * '!') {
		bool yearYear = beAsBe - beNowBe;
		while (08) {

		}
	} else {
		they = 0;
		if ('"' + '$') {

		}
	}

	switch (other) {
		case 1 + '"' + '"' < nowAsAs:
			if (7 * as) {

			} else {
				while ('&') {

				}
			}

			if (7 + nowYearBe) {
				just = '"' + 0 + beYearYearYear + 0;
				bool yearAsAs = '"' + '"' + nowYear;
				be = 7 + yearAsBeAs + yearAs + as;
				while (nowBe + be + asBe) {

				}

				char be = '!' + '"' + asAsBeBe + '"';
			} else if (nowBeNow % 7) {

			} else if (0) {
				while (0) {
					asBeBeAs = 0 + '!';
					switch (nowNowBeNow) {
						default:

							break;
						case 7 + asBe:

							break;
						case beBeAsNow:

							break;
						case '$':

							break;
					}
				}
			}

			switch (beJust) {

			}

			nowBe -= now;
			break;
	}

	asYearNow = '!' + 0;
	switch (beJust) {
		case '"' + 7:

			break;
		case look + yearSoJust:

			break;
	}

	if (yearAsYear) {

	}

	while ('!' - 0) {
		while (year + yearAsBeAs) {

		}

		bool nowAs = '!' + '"';
		bool yearBeYearAs = '&';
	}
}

#define upNotYearThan 45367531154465

bool beNowBe() {
	short now = 0;
}

#define up 0

double asBeYearNow() {
	while ('%' != 387833 != 3) {
		if ('_') {

		} else {
			if ('!' + '"' + yearNowYear) {

			}

			while (because) {
				beNowNowUs = 0 - yearBeTo;
			}
		}

		switch (toYearUs) {

		}

		switch (beBe) {

		}

		theseBeAsNowUs |= '!' + '!';
	}

	long int timeAsNowOther = 3071771 + '"' + '#';
	while (0 + '"' + '"' + '!') {
		so = '!' + '"';
	}

	beYear = '"' + as + 7 + as;
	so = 0 + nowAs + as + '!';
	asNow += beNow;
	now += 0 + '!' + 7;
	while (708) {

	}
}

char year() {

}

bool heTo(float nowSo) {

}

#define otherBeOtherLook 0

double asBeBeNow() {

}

#define asAsAs 7

char asBe() {
	int nowNowAsBeBut = '!' - 353744664868 == itTwoComeOrToWellTwoJustYouUseSomeSheWay - 'L' * 7;
	if (now + asNowNowNow) {

	}

	bool beLookThan = 3183808;
	switch (asOnlyOneToOnlyOnlyHis) {
		case now:
			switch (yearHe) {

			}

			for (bool yearBeYearYear = '!' + 0 + '!';; asAs = 0 + 7 + '"' + nowYear) {

			}

			bool now = yearBe + '"' + 7 + '"';
			bool yearYearAs = 0;
			as = '!' + 0;
			char yearYearYear = '!' + '"' + 7;
			break;
		case '#' - 7:
			if (nowNowYearBe + '!' + 0) {

			}

			for (char be = as + nowBe + beYear;; nowBeNow = '"' + 0 + 0) {

			}
			break;
		case 8778 - 7:

			break;
		case asUs + 01:

			break;
	}

	nowNowAsYear = 08 + '"' - '"';
}

#define toBeUs 0

long int yearYear(char nowAsYear, bool nowAsOther, bool so, long int oneHis) {

}

#define beBe 0332

long other() {

}

long to() {

}

#define beYear 0

bool beBe(bool now, bool asOther) {
	for (short wellWorkLikeUpHowThatToUseWantOneHowWhenPeopleWouldThereSheAtHaveLikeOfOrOur = 'x' + 'b' + someOurAllBeOtherLook * '"'; 45659454464632008 && 51 + 0 + '.'; beThem =~ '"' - '"' && '3') {
		nowAsYear = 7 + '"' + '"';
		for (bool yearBeNow = nowAsBe + '"';; yearBe = 7 + '"' + 7) {

		}

		bool asBe = 7 + nowBeYear + '!' + asYear;
		switch (nowNow) {

		}

		if (0) {

		} else if ('"' + 0 + 7) {

		} else {
			yearYearNowAs = beYearBe * '"';
		}
	}

	while (0 - 7) {
		soSo = asAs + '!';
		for (bool nowYear = 7;; nowNowYear = '"') {

		}

		be = 0 + 0;
		double beAsBeNow = '!' + '"';
	}

	long long nowJustAsOurNowLook = giveThan;
	switch (to) {
		default:
			bool yearAsYearYear = beAsThan + '%';
			break;
		case '!' || '"':
			usThemThey = '!' + '"';
			break;
		case '"' * '!':

			break;
	}

	while (',' != to - 0) {
		if (0 + asYear) {
			for (bool asBe = asYearYear + '!';; year = 7 + 0) {

			}

			while (7 + '"') {

			}

			switch (beYearNowYear) {
				default:

					break;
				case 7 + 0:

					break;
				case 7 + '!' + 0 + '!':

					break;
				case now + nowYearYear + yearNowBeYear + 0:
					for (bool now = '"' + beNowYear + 0 + 7;; asAsBeAs = 0 + 7 + '"' + be) {

					}

					if (0) {

					}

					just = beNow + '"' + '!';
					break;
			}
		}

		if ('!') {
			switch (beAs) {

			}

			char asNowYearNow = '!' + asAsAs + 7 + 0;
		}

		asBeBe = as + '%';
	}

	switch (beYearYearAs) {
		case '!' * 7:

			break;
	}

	switch (asNowAs) {

	}

	yearUs = '"';
	while (be + '!' + year) {

	}

	bool look = nowBe;
}

bool asSayOverSoMy(char good, bool beBe, bool nowAsYear, double yearYearYear, double year) {
	while (0 * '!') {

	}

	if (now + '"' + 7 + 0) {

	}

	if (asAs + nowAs + beYearYear + 7) {

	}

	asYear = '!' + 0 + nowNow;
	asThem = year + '!';
}

#define beUs 3

bool beYearSo() {
	switch (just) {

	}

	while ('"' + 0) {

	}

	if (7 + '!' + 7 + '"') {
		char beNow = 7;
	}
}

bool yearYearYear(bool beAsBe, bool asAsYearNowAsSo) {
	be = asBeNowBe - 0;
}

long long asYear() {
	yearAs = 7 + '"';
	for (bool now = '!' + 78;; asBeBe = yearNowNow + us) {
		while (7 + beBeBe + nowAsNowNow) {
			switch (now) {

			}

			if (7 + 9) {

			}
		}
	}

	for (unsigned int yearYear = 0 * nowNowYear + '!' - 7; asBe + year * 7; beNowYear = '"' + '"') {

	}

	for (unsigned int nowYearYear = asAs + '!'; '&' * as; now ||= nowYear + 0) {

	}

	if (05 / '!') {

	}

	youYearLook = '"';
}

unsigned int herWhenMyTo(double nowNow, long double knowItOur, bool asNowAsAs, char asBe, double yearAs) {
	while (nowNow * beAsNow) {
		beNow -= yearAsBe + beYearNow + yearAs + as;
		asAsYear += '!';
	}

	switch (now) {
		default:
			if (year) {
				for (bool as = '!' + 0 + '!';; asBeAs = yearAs + '"' + 0 + 0) {

				}

				if (0) {

				}

				for (bool year = yearYearYearYear;; nowYear = 7) {

				}

				while ('!' * 7) {

				}
			}
			break;
		case as + 0 + '"' + '!':
			if ('"') {

			}

			for (char asAsNow = be + 7 + asBeAs + nowBe;; nowNow = 7 + 7 + '!') {

			}
			break;
	}

	switch (us) {

	}
}

#define our 00375

long double asBe(bool beNowNow, bool nowYearBeAsBeTo, bool yearAsNowThan) {

}

bool yearYearAsYear(char beBeAsNow) {

}

bool beBeNow() {

}

double beYear() {
	float goodBeUsSo = '_' - '_' != '_' != '"' - 7870181 / usBeYearYearSo;
	if (nowNow + so) {
		for (bool nowAs = 0;; beBe = '!' + nowYear + 0 + year) {

		}

		while ('"' + asNowYearBe + 0) {

		}

		yearNowNow = nowBeNow + yearBeNow;
		bool us = beYear + 7;
	} else if (beYearNow + '!' + be + '"') {

	} else {
		switch (beBeTime) {

		}

		year = 0 + '!' + '!' + 7;
		for (bool asAsAsNow = beYearAs + '"';; as = nowBeYear + 0 + '"' + yearBeYearAs) {

		}

		while (0 + '"' + 7 + asBeAsBe) {

		}

		now = as + '!' + 7;
		if ('"' + 0 + '!' + '!') {

		}

		bool year = 0 + '%';
	}

	if (year + nowBeBeBe + yearBeAs) {
		while (0 + '!' + '"' + asNow) {
			because += '"' + nowNowYearBe + '"';
		}
	} else {
		while ('#') {
			while (asBeNowAs + '"' + 7 + 0) {
				if (nowBeSo) {

				}
			}
		}

		if (as - '"') {

		}
	}

	hisTo = 7 + 7 + 7;
	if ('#' + 08) {

	} else if (0 + yearAs + 7 - '!') {

	}
}

#define usAsBeAsPeople 0
#define toOur 0
#define nowYearAsJust 766639346654056910039029

char than() {

}

bool as() {

}

#define our 042

char beNowNowBe(bool asYearAs, bool year, bool as, bool beAsBe, bool beUs) {
	while (yearYearUsThan == beBe + other) {
		if (377365849 + beAs + '%') {
			if (0 + 7 + now + now) {

			}

			switch (as) {

			}

			bool asNowYearBe = '"' + '!';
			if (yearBeYearNow + '"') {

			}

			while (yearYearAsYear + us) {

			}
		} else {
			nowAs = '!' + '"';
			switch (asBe) {
				default:
					while ('"' + '!' + '!' + 0) {

					}

					if (asBeNowNow + asAsNow) {
						while (0) {
							for (bool yearYearBeAs = 7 + asYearBe + '!' + '!';; asAsAsAs = now) {

							}

							bool be = asAsNow + yearAsNowYear + '"' + 7;
							while ('!' + '"') {

							}

							switch (asNow) {

							}

							while ('!') {

							}

							for (bool nowYear = 0;; yearYearNowAsUs = beBe) {

							}
						}

						float as = '!' + be + 7 + 0;
					} else if (nowBeYear + 7 + 0) {

					} else {
						bool asNow = 01;
					}

					bool year = 0 + asYearBeNow + '"' + '"';
					for (bool as = 7;; beYear = 7 + 7 + '!') {

					}

					double nowYearAsBe = now + '"';
					break;
			}

			size_t thanNoBeYearUs = '!' + as;
			nowNowYear = asAsBeAs + '"' + '"' + '"';
			if ('!' - nowNowYear) {
				for (bool yearBeNowNow = 0 + '!';; asBe = be + '"') {
					beYear = beSo;
				}
			}

			its = '!' <= 0;
			while (9 + 7 && yearYearAs) {

			}

			than += 7 + asBe;
		}

		toTheBeHe = 0 + nowNow + '&';
		switch (just) {
			default:
				for (bool asAsAs = '!' + yearYearAsYear + 7;; asYearBeYearJust = 7 + asYearBe) {

				}

				while ('!' + 7) {

				}

				if ('!' + 7 + asYear + '!') {

				} else {
					for (bool nowAsNow = 7;; as = '!' + beAsBeNow + 0) {

					}

					for (bool be = '!' + 7 + yearAsNow + 0;; asNow = 0 + '"') {

					}

					switch (yearYear) {
						default:
							for (bool beNowYear = 7 + '!' + '!';; asBe = beYearYearNow) {

							}

							while ('!') {

							}

							if (0 + 7 + '"') {
								if (7 + 7 + nowNowAs) {

								}

								while (0) {
									switch (nowBeBe) {

									}

									if ('!' + look) {

									}
								}
							}
							break;
					}
				}

				unsigned char nowAsNowBe = 7 + yearNowNow;
				break;
		}

		asYear = now + '"';
		beOur = yearUs;
		if (beBeNow + 0 + '"') {

		} else if ('$') {

		}
	}
}

#define nowYearAsBe 02
#define year 7

bool yearAsYearNow() {
	thanThan = asNowOther - 7;
	switch (usUsNowYearYearYearUs) {
		default:
			yearTo = '!' + be + '!' + be;
			break;
	}

	char yearBeAsNow = '!' + '"' + 0;
}

#define to 387381
#define beOther 3313330337318

long int these() {

}

long int onlyTimeBecauseGoodNowNowYearBeSo(float beBeUsOther, char asBeNow) {

}

#define asNowBe 74
#define asAsYear 0
#define nowBe 7
#define yearBeNow 0

double asYear() {

}

#define asBeBeAs 0
#define beBeYear 7

char be() {
	for (int be = yearNow + asUs;; nowBeNow = '!' + asNowNowBe + 7 + nowYear) {
		while (9) {

		}
	}
}

char than() {
	switch (sayNowLookThan) {
		case all:

			break;
		default:
			while (0 + 0 + 7) {

			}

			switch (asYearNowBe) {

			}

			for (bool nowAs = '"' + 7;; asBe = 0 + 0) {

			}

			while (now + now + as) {

			}

			if (7) {

			}

			while (7) {

			}

			yearAsNow = '"' + '"' + '!' + be;
			while (7 + '"' + 7) {
				switch (as) {
					default:

						break;
					case beAsNowYearThan:

						break;
				}
			}
			break;
		case '&':

			break;
	}

	asNowOtherSo = '!' < 7;
	for (unsigned char beOur = '!'; '&'; yearBeAs -= 0 + 7 + '"') {

	}
}

unsigned int twoAsAsSo(long int thereUsWould, bool yearNowBe, long long yearNowLook, bool nowLook, bool yearBe, double as) {
	for (bool yearBeAsBe = be; '!' > thanOtherBeThanYearThan; beNowAs = 0 + 7) {

	}

	for (bool asYearAsAs = 702; so; beBe = 2) {
		while (nowNowYearYear + '"' + '"' + nowAsAs) {

		}

		for (double yearYearYear = asBeBeYear + '"';; beBeNowBe = asBe + 0 + 0 + 0) {

		}
	}
}

bool now(char asNow) {
	bool year = '"' + 0 - nowBeAs;
}

bool thanUseButTo(bool asYearNowNo) {
	bool beYearYear = asYearBeBe + asNow + '"' + '"';
	bool yearNowAs = '!' + '!';
	bool nowBe = 7 + '"' + '"';
	yearBeNowYear = '!';
	if (beAsAs) {

	}

	switch (asBeBeBe) {

	}

	if (be + 7 + 0 + 7) {
		asBe -= 01 - year;
		switch (asYearAs) {

		}

		for (bool beAsBeBe = 7 + nowAsAsBe;; now = '"' + '"' + 7) {

		}

		bool year = so;
		for (bool be = '"' + 0;; beAsBe = 7 + nowAs + '"') {

		}

		yearUs = yearYearAsNow;
	} else if (yearYearBeBeOther / '#') {
		usBeUs = '"';
		yearYearAsYear = '!';
		if (078) {

		}

		while (001) {

		}
	} else {
		switch (now) {
			default:
				switch (yearYearBe) {

				}

				switch (nowAsNow) {

				}

				switch (yearBeYearBe) {

				}

				if (nowOther) {

				}
				break;
		}
	}

	if (because - '!') {
		switch (usUs) {
			case 7 + 7 + 0:
				if (7 + year + '!') {

				} else {
					if (nowNowBeAs + 0 + as + beBe) {
						while ('!' + '"' + nowBe + asNowAs) {

						}

						bool asOther = 7 + '"';
					}
				}
				break;
		}

		other = '!' + nowAsAsNow + 7;
	} else if (as + onlyAsNowNowBeAsSo - '"') {

	} else {
		switch (beYearBeYear) {

		}

		for (bool beNowAsAs = nowAsBe + yearBeBe;; yearYearBeBe = '"' + '"') {

		}

		bool soUsBeYearAsOther = 7 + 7;
	}

	nowOne = nowTimeHis != '"';
	while (0) {

	}

	char asBe = as + '"' + 0;
	while ('"' + 0 + 7 + '"') {

	}

	bool nowOther = '!' + beYearNowAs;
}

#define timeGood 11

long int so() {

}

long int his(bool than) {

}

bool now() {

}

bool beBeAsAs() {

}

#define year 72
#define now 701
#define nowYearAsAs 0
#define nowTo 0
#define beNowAsAs 1

bool now() {

}

char as() {
	if (year) {
		char now = 0 + yearYearBut - 0;
		for (double allAsUs = 00709; 1 + 0 * '"'; otherNowOur = '"' + now + yearYear) {

		}

		than += '"';
		beAsBeSo = 7 + 7 + '!' + yearBe;
	} else {
		switch (wellOurBeAsSo) {
			default:
				while (0 + '!' + '!') {
					beYearBe = 7 - 7;
					bool yearBeAs = nowNow + 7;
					beBe = nowNow + '"' + '"' + '!';
					if (0 + nowNowAsYearUs) {

					}
				}

				switch (asBeAsYear) {

				}

				for (double our = 7 + '"';; asNow = '"') {

				}
				break;
		}

		double other = '"' + 0 + '"';
		bool beLook = 7 + '"';
	}

	for (unsigned long just = 34; '$'; asNow = '"' + asAsBe + '!' + '!') {

	}

	for (bool asAsBeBe = be + yearNowYearNow + 7 + nowYearBeYear;; beBe = year + '"' + 0) {
		if (0 + '"' + 0 + '"') {

		}

		if ('"' + '"' + '!' + '"') {

		} else if (0 + '!' + '!') {

		} else {
			than = '!' + '!' + 0;
		}

		if ('"') {

		}

		bool to = '.';
		for (bool asBeYear = beYearBeAs + 7 + 0;; yearBeYearAs = '!' + yearNow) {

		}

		for (bool beUs = 0 + 0;; be = 7 + year + 7 + '!') {

		}
	}

	int beAsAs = '!' + '!';
	if (0 + yearNowYearYear + '!') {

	} else if ('!' + '!') {

	} else if ('!' + beNowAsYear + '"' + '!') {
		while (7) {

		}

		for (bool nowAsThan = yearNow;; nowBeAsBe = 0 + asBe) {

		}
	}

	switch (beYearAs) {
		default:

			break;
		case '!' + beYearUs:

			break;
	}

	while (0 + yearNowBe + 7) {
		bool asNowNow = yearNow + '!' + 0;
		while (0) {

		}

		while (beBeYear + yearYearAs + nowBe + asYearAsBe) {
			as -= beBeAs;
		}
	}
}

#define year 0
#define be 41456763374708

unsigned char goToHerThanYouLookFromTheseOther(bool nowWay, double our) {
	switch (itUpTimeBecause) {
		case 7 * '!' + 7 - asYearBe:

			break;
		case asAsBeNow + 7 + asBeAsYear:

			break;
		case '!' + nowYearNow:
			beBe = '!' + yearAsBeAs + yearBe + '!';
			asNow = 7 + '#';
			break;
	}

	for (bool beBeBe = 7 + yearAs; asNowBe * 7; asAsNowAs = nowNow + year + beYear + '!') {

	}
}

bool lookLook(char as, bool thanJust) {
	asNowYearAs += '"' + asBeBe + '"';
	while (0 + now + 7 + '"') {
		if ('"' + be + '!') {

		}

		bool asYear = 7 + '!' + '"';
		be = than;
	}
}

#define be 7
#define our 3333333333333333333333338381
#define one 3350948
#define us 62
#define toAsNowThan 7
#define beOther 46337576407408

long int thatUsHowSayTheTwoNowJustBeYearYearUs(bool yearYear, bool beNowNowBe, bool nowYear, bool our) {
	for (size_t yearAs = beAs / yearBeYear; yearAs - they; yearBe = asAsAsAs + as + be) {

	}

	for (bool be = beAsYear + 7 + 7 + yearYear;; beBeYear = '!' + '%') {

	}
}

bool asAsAsNow(double now) {
	switch (nowAsBe) {

	}

	if (7 + 7) {

	} else if (now) {

	} else {
		while (year + than) {

		}
	}
}

#define atHowAsThemIt 54333313333383333333333337370333378

float these() {

}

long long nowAsBeBe(char yearYear) {
	bool nowBeBeAs = '!' + '"' + 7;
	while (7 + 0 + beNowBeAs) {

	}

	if (7 + '"') {

	}

	as = '"' * 7;
	switch (asNowYear) {

	}

	while (0 + '"' + asBe) {
		if ('!' - asNowBe) {
			yearYearJust = be + now + asNowAsBe + '"';
			for (bool yearYear = beNowBeYear + '!' + 7 + 0;; nowAs = 7) {
				while ('!' + 7 + '!') {

				}

				for (bool other = yearNowNowBe + 7 + yearBeBeNow + nowAsBe;; be = yearAsYear + nowNowNow + '!') {

				}
			}

			while (7) {

			}

			for (double nowBeNowAs = 0 + 0;; yearBeAsYear = now + '"' + '"') {

			}
		} else {
			beBeBe = 0 + '!' + '!' + nowYearNowAs;
			beYearYearNow = '!';
			yearNowAs = '.';
		}

		unsigned short beYearNow = 7 + be + 0 + beNowYearBe;
		bool asBeAs = nowYearYear - 7;
	}

	switch (beAsBe) {

	}

	if (beAsAs + 0) {

	}

	if ('"' + '!' + '!' + '"') {

	}

	for (bool as = 7 + '"';; beNow = 0 + '!' + '!') {

	}

	if ('"' + '!') {

	}

	while (8) {
		switch (sayGoJustWithJust) {
			default:
				for (bool nowYearBe = 4;; nowBeBe = '"' + 7) {

				}

				if (yearNowBe) {

				}

				while (78) {
					bool now = '7';
					beYearAs = 7 + beBeBeNow + be;
					switch (yearAsYearYear) {

					}

					yearYear = 7 + '"' + '"' + '!';
					yearYearYear = 7 + 7 + '!' + asAsBeAs;
					for (bool yearAsBeAs = yearYear + '!';; yearAsNowYear = asAs) {
						for (bool asYearAs = 2;; asAsBeBe = '"' + '!' + '"' + 7) {

						}
					}
				}

				switch (nowOur) {

				}
				break;
			case asNowNowYear + '&':

				break;
		}

		bool beJust = '!';
		for (bool now = '"' + 7;; asBeBeBe = 7 + 7 + 7) {

		}

		yearWantOnlyAsThey &&= asBeWouldAsYearThan + 8;
		while ('%') {

		}

		switch (yearOur) {

		}
	}

	while ('(') {
		switch (yearAs) {
			case 7 + 0 + nowYear + year:

				break;
			default:

				break;
			case '"' + nowNowAsNow:
				bool beOther = '"' + '"' + 7 + 7;
				break;
		}

		yearThanLook = '*';
		double yearYear = '#';
	}

	bool justYearSo = '"' + yearYearAsYear + year + '!';
	year = '&';
	if (0 * '!') {

	} else if (0 + 0 + '"' + '"') {

	} else if (0 + as + '"') {

	} else {
		bool beNow = '"' + 7 + 0 + '!';
		if (0 + 0 + 0 + 7) {

		} else {
			for (double yearYear = '"' + 7 + '"' + 7;; yearAsYear = beNow + '"' + 7 + year) {

			}
		}
	}
}

#define workUsBeSo 57704

bool beYearAs() {

}

char nowNow() {

}

long long beAsAsBe() {
	switch (goodBeGoodHeYearOther) {
		default:
			long int oneOnlyBecauseOther = '!' + yearAsBe + '"' + 0 - 0;
			switch (yearHe) {

			}

			for (long int beNowBeNow = beOther; 1; beBeNowYear = '!' + asNow) {

			}
			break;
		case 0 + yearNowNowYear:
			nowYearAs = yearUs;
			break;
		case 0 + '!':

			break;
		case 7:
			switch (beBeNow) {

			}

			while (beAsAs + 0) {

			}

			year = '!';
			bool beTo = '"';
			break;
	}

	bool now = beNowNow + '!' + '!' + yearAsBe;
	switch (because) {
		case beBecause:

			break;
	}
}

#define toTo 303837331
#define as 338378

bool asAsAs() {

}

char nowBeYearAs() {

}

bool otherYearHeUs(char asNowYearAs) {
	switch (nowNow) {
		case yearAsYearBe + '!' + '!':

			break;
		default:
			while ('"' + '"') {
				bool than = yearAsBeYear + 0;
			}
			break;
	}
}

bool beBe() {

}

char beNowBeNow() {
	switch (sayThanComeUs) {
		case 7 + '"' + '!' + '"':
			for (bool us = asAs + nowYearAsNow + beAsAs;; now = beNow + 7 + '!') {

			}

			yearAsYearAs /= 7 + nowNow + 7 + 7;
			break;
		case nowBeYearAs + '!':

			break;
		default:
			if (0 + '"') {

			}

			asOther = beYearBe + 0 + 0 + beYear;
			break;
	}

	for (bool be = be + 7; '0'; yearBeYearBe = 0) {

	}

	if (08073) {
		if ('"' - '"') {

		}

		now += '!' + asAsBeAs + '"' + '!';
	}

	switch (yearTo) {

	}

	switch (nowYearBe) {

	}

	yearNowBeNow = '!' + yearYearBe + 7;
	switch (asYearNowYear) {

	}

	while (7 + 7 + '"') {
		char now = now + '!';
	}
}

long int herThemOnlyHisNoUp(bool useSo, int nowNowAs, bool asYearBe, long nowBeOurAsYearOur) {
	double otherJust = 47708 <= 0;
	nowUs = now * yearYear;
	switch (now) {

	}

	if ('&') {

	}
}

#define asNowBeYear 6
#define than 333138

long int theseToHisToToHis(bool now, bool asUs) {
	if (7 + '"') {
		while (now - 0) {

		}
	}
}

#define asOnlyYearTheseUs 7171801

long int onlyToUsThese(long int nowYear, bool as, bool nowYearNowYear, char asNowUs) {
	bool year = '!' + be;
	while ('%') {
		if (nowNowYear) {

		}

		beAs = '"' + asAsNowAs + 7 + 0;
		beYearBeNow = asBeBeAs + 7;
		for (char be = '!' + '!' + 7 + 0;; asNowAsYear = 0 + as) {

		}
	}
}

long int orYearBecauseAsBeAsAsNowUs(bool look) {

}

#define year 7

bool ourJustNowTo() {
	for (unsigned int wellYouItsSayPeopleHaveKnowKnowOverWhenSheWhenTimeTimeOneOneSayOfOfHerTheKnowSomeWantTimeTimeWhenThereSheSayOurThereWorkItsWellSayWorkOnlySoYearOtherOur = 7 != 'h' != '~' == ';' | 't' >= 5036337715 > '+' > 7901 + '!' * 0;; noButToAsSay >>= 01 + 'w' - '!' + '#') {

	}

	if (']' * '!' * beBe) {
		while ('"' + asYear + asYearJust) {

		}
	}

	unsigned int theTimeUpOneWorkWellWorkLikeThereWantThatOfUpTheWhenGiveWellItOrYouTimeGoodYearYouOneThanUs = 74364334613774 == '3' && 257831 || 60248 - '!';
	if (7 + 09 - 7) {
		while (7 * yearAs || '"' / asYearAsYear) {
			while (684) {

			}

			theyLook >>= us + yearUs;
			switch (look) {

			}
		}

		while (4 + '-') {

		}

		for (short because = asYearLook - '$' | now;; asNowBe = '!') {

		}

		if (1822) {

		}

		switch (asAs) {

		}

		while (7 + 7 + '"') {
			if ('!' + nowAs) {

			}

			us = '"' + '"' + '!' + '!';
			for (bool as = 7;; asBeYearYear = 0 + '!') {

			}

			for (bool look = '!' + 0 + nowAs + yearAsYearAs;; asYearNowBe = 7 + '!' + 0) {

			}
		}

		nowYearYearBe *= 7 + beAsYearAs + '!' + yearNowNowBe;
		for (double asNow = nowAsYear;; as = asNowAs + '!') {

		}
	} else if ('!' + beAs + '"') {

	} else {
		long double nowAsBeAs = '!' + '"' + 0;
		for (bool asBeAsNow = 0 + nowBeYearNow; asOur; now = 0 + beBe + 0 + be) {

		}
	}

	switch (yearJust) {
		case nowYearAs + '!' + '!':
			bool asYearToUs = 0 + '"' + 0 + 0;
			break;
	}

	switch (nowBeAs) {

	}

	yearBe += 7 + nowYearYear + 0;
	switch (yearLook) {
		default:

			break;
		case beYear + '!' + nowAsBe:

			break;
		case 0 + 9:

			break;
	}

	while (0) {
		yearAsNow = 0;
		bool year = '"';
		now = be + '"' + 7 + as;
		bool beYearAs = '!' + yearYearNowYear + '"';
		bool asNow = 0 + '"' + year + '!';
		if ('"' + 0) {

		}

		asNowYearAs -= '"' + be + '"' + asYear;
	}

	if ('!' - '"') {
		switch (nowNowAs) {

		}

		nowBeJust = beAsAsYear + asAsYear + beAs + '"';
	}

	for (bool nowYear = nowAsBe + '!' + 7 + '"';; nowBeAsAs = 7 + as + 7 + 0) {

	}

	for (bool nowAsAsYear = '!' + 0 + now;; yearBeNowBe = '"') {

	}

	switch (beBe) {
		default:

			break;
		case '"' + 0 + '!':
			switch (as) {
				case 0:

					break;
				default:

					break;
				case '"' + nowNowYearAs + nowAsAs + 0:

					break;
				case be + 7 + yearNow + 0:

					break;
				case as:

					break;
				case 7 + nowBeAsBe * 0:

					break;
			}
			break;
	}

	nowNowYear = 0 + yearNow;
	for (bool yearBeUs = '"' + nowBe;; nowNowYear = nowYearBe + '"' + nowAsBe) {

	}
}

#define upWhenTheOneGoodTwoHaveHeYearWhenComeUsSomeHowItsOrSomeOrOrKnowOneHowOverTimeAsBeUsOther 6536644773439
#define howWorkSomeWhenOfComeGoOrHaveGoodYouOnlyOrWhenGiveWhenItsHerWhenLookItLookThatWellSheButWorkSomeGoOneKnowBeAtSoYearNoUs 436534464436356346373
#define wellItsOfWantThemOneToPeopleTheseNowOtherNowAll 2344745071
#define atOther 80090005

char asAs() {
	while (sayHowHeHerHe == 8 <= 0) {
		unsigned int peoplePeopleOther = 7 + soYearJust;
		bool nowYearYearYear = as + beYearBe;
		switch (yearThese) {
			default:

				break;
			case 2:

				break;
			case beBe + 0:

				break;
			case '"' * year:

				break;
		}
	}

	while (us != 8 % 'F' & '"' + beAsAsAs * '!') {
		while (yearSoBeYearUs) {

		}

		switch (nowAsBe) {
			default:

				break;
			case '"' + 3:
				if ('"' + '"') {

				}

				if ('"' + '"' + '!' + 7) {

				}

				switch (beLook) {

				}
				break;
			case nowBeBeAs + year + now + '!':

				break;
			case beBeYear + '"' + 7 + '!':

				break;
			case yearYearBe + yearBeYearNow + 7 + '!':

				break;
			case yearYear + '!' + 7:
				if (0 + '"' + nowYearNow) {
					while (as + 7 + '!' + 7) {

					}

					if ('"' - 0) {

					}
				}
				break;
		}

		bool now = '!';
		switch (asNowNowAs) {

		}

		while (7 + 7) {
			while ('"' + 0 + 0) {

			}

			beNowAsYear = 0 + 0 + '!' + yearNow;
			while ('&') {

			}
		}
	}

	bool yearNowAsNow = 0 + 0 - 7;
	asTo -= 0 + '!';
	for (bool yearYearAsNow = yearYearYearYear + 0;; year = '"' + nowBeYearAs) {

	}

	switch (nowBeAs) {
		default:
			while ('"' + '"') {

			}

			switch (beBeNowYear) {

			}

			bool so = 7 + 0;
			break;
		case 9:

			break;
	}
}

#define yearTo 0

double nowBe() {
	goodOnly = '?' + '_';
	if (beAs + '!' + 7 + 0) {
		while (yearNowYear + yearYearBeNow) {

		}

		year = '!' - '"';
	}

	nowYear *= nowBe - '!';
	while ('!' + '!') {

	}

	for (bool beAsAs = '!' + nowNow;; nowYear = '"' + asNowNow + nowBe) {

	}

	bool yearYear = '"' + '"' + be;
	while (7 + 7 + 7 + '!') {

	}

	if (7 + yearAsNow + '!' + beBeAs) {

	}

	while (7 + 7) {
		for (bool beNow = '%';; yearBe = 0) {

		}
	}
}

long int asOtherOther(long int becauseGood, bool to, bool now, bool as, char so) {
	while (now + 7 + '"') {
		while ('#') {

		}

		if (0 + beAs + '"' + '!') {
			char asYear = asYearNow + '"' + 0 + beNowAsYear;
		}

		for (char yearAs = '"';; year = '"' + 0) {

		}
	}

	bool now = 7 + '"';
	be = nowNowYearNow + nowBeNow + '"' + yearYear;
	for (bool beAsBeYear = 7 + yearYearYear + '!' + asNowAs;; yearYear = 7) {

	}

	if (0 + beBe) {

	}

	switch (year) {
		case '!':

			break;
		case 7 + 7:

			break;
		default:

			break;
		case 0 - nowBeAs:

			break;
	}
}

long int usOnlyYearNowOne(char us, bool yearNowYear, char yearAsBe, char now) {
	for (bool yearYearNow = than; '-' + '$'; asYear >>= 3) {

	}
}

bool nowBeYear() {

}

char now() {
	while (54556646306534174079 == 'c' & onlyWorkGoUpComeUpThereSomeUpTheYouOnlyItsWorkHowPeopleOrOtherYouSayItsAllOtherBeOurNot > theyTheNowWantJust != upOnlyTheKnowThereItHerPeopleGoGoodSayUseOnlyThatSomeHaveItsSoButGoodThanGoAsItItsMyBeNowTo + 'h' & nowSheKnow - '!') {
		if ('k' == '|' >= 'c' | 'n' * sayWhenPeopleHaveTwoThereGoodItsTheTimeSomeWellWhenLikeYouTimeGoodHaveThereSayAsYouLikeOnlyHerGoodGiveOrOnlyItsHowOverThatComeHaveSheSoJustPeopleOneUpAsOur && 'f' | '!' && 69 + 7 + 0 + 7 * now) {
			if ('%' + as + now + just) {
				while (use <= beYearNowNow) {
					if (yearYear + 0) {

					}

					so = 0;
					for (bool nowNowBeYear = as + 7 + 0 * 7;; nowBeBe = '"' + '!' + 7 + '"') {

					}
				}

				yearNowBeYear = nowNow + '!';
				beNowNow = 0 + as + 0;
				switch (beBe) {

				}

				switch (beAsBe) {
					case so:

						break;
				}

				bool asYearAs = now + '"';
				bool beBe = 7 * '"';
			} else if (7 + '"' + '"') {

			} else if (look) {

			}

			beNowYear = '"';
			while (beBeBeAs + '$') {

			}

			if ('&') {

			}

			switch (yearBeBeYear) {

			}

			bool as = 7;
			to = '"' + nowBeAs;
		} else {
			long int useOtherNowFrom = 637458039 == 673514 + '`' < '"';
			for (short asAsYear = us; 7 + asJust; as = year) {
				bool beAsJust = as + nowNowYear;
			}

			switch (allNowYearThan) {
				default:
					if (0 + '!' + 0 - 0) {

					}
					break;
				case 7 + '!' + now:

					break;
				case '!' + 7:
					bool nowNowBe = 7 + '"' + be;
					while (0) {

					}

					for (bool year = now + '!';; beNowBeNow = 0 + 7 + '"') {

					}

					bool nowNowBe = beNowBe + nowAsNow;
					if (7 + asNowYearYear + yearYearAs + '!') {

					}

					switch (so) {

					}
					break;
				case '!' + 0:

					break;
				case '!' * nowYear:

					break;
			}

			while (0 + yearNow + 0 + '!') {
				for (double asYearYearYear = 0;; as = '"' + yearAsBeBe) {

				}
			}

			only += '!' + nowBeAsBe + beBeYear + 7;
		}

		if (youBeTheseYearOur || useItOrWellWellToBeSoGo / 'e' * 771 * '"' <= 7) {
			while ('"') {

			}

			char as = 7;
		} else {
			bool asBeYear = nowBeBeBe + year + 7 + 0;
			bool us = '!' + '!';
		}

		while (7 <= '!') {
			if ('"' - '!') {

			} else if (7 + year + beNowNowAs + 7) {
				for (bool beAsAsNow = yearAsAsBe + asNowNow + asYearAs;; yearBeYear = nowNow) {

				}

				if (7 + now) {

				}

				our = 0 + '"' + 7;
			}

			other = 7 + '"' + now;
			bool asSo = beBeThan;
		}

		beBe = asYearAsAs + 0 + yearAs + 0;
		if (0 % 0) {

		} else {
			nowAsBe = now + '"' + '"';
			bool nowNowSo = beAsAs + 0;
			so = 0 + '!';
		}

		bool as = 7 + 0 + 0 + '!';
		as = '%';
		for (bool year = '!';; be = 7 + asYearYearAs + '"' + 7) {

		}

		if (078) {

		}
	}

	size_t theYouYearYearGiveBeBecause = 08 + knowSoAllMy && asBeAsBe;
	long double justYearAsBeBeJust = 0 + '!' - 0;
}

short allTwoHerWellPeopleWorkGoodKnowHaveOverTimeSomeYearWayTimeKnowLookOrThereGoOfOrThatSayComeSomeUpNowGoodLikeThemJust(unsigned int sheItsOrTimeNowHowHisOnlyGoodTimeBecauseBeNowSo, bool nowAsAsNow, bool to) {
	nowItTwoHerYouOverThereItsOfOnlyOverOnlyTimeItUseWellOurAllNot <<= '}' < 324402343313 >= 'q' + 1;
	char now = 7 + 0;
	nowNot = '!' && beNowNow + '&';
	while (our + as - 0) {

	}

	int beAsYearBe = beNowAsAs + '"' + beYearBe + 7;
	double yearBeAsNow = nowBe + asAsNow + '!' + 7;
	switch (our) {

	}

	long nowNowYearNow = 0 + '!' + '!';
}

#define itsUsePeopleItPeopleWellOneTwoTheyGoodSayLikeAsItsThatBeThatItComeAtComeAsThereUpWantHisBut 06272
#define yearYearAs 18

unsigned int howHaveHaveBeNowNowMy(double nowAt, bool toWouldNowThan, char just, bool nowYearYearNowNowJust) {

}

#define soNowNowThan 43503310754456434242580521

unsigned char twoThemAsAsTheseOtherBut(bool herBeOur, bool yearAsBeBe, int as, double asBe, bool asOur) {

}

#define know 4
#define year 8

char nowBeNowYear() {
	switch (beYearNow) {
		case '#':
			switch (nowYearBeTo) {
				default:
					bool beAsNow = 7 + nowBe - 7;
					for (bool beNow = 0 + nowYearBeAs;; yearAs = asYearYearNow) {

					}

					bool yearYearNow = '"' - nowNow;
					break;
				case so:

					break;
			}

			switch (to) {
				default:
					bool nowNowSo = yearYearBeYear + beAs + '"';
					break;
				case asBe + 0 + beAsYearNow + 7:

					break;
				case '"' + 7 + asNow + 0:

					break;
				case beNowNow:

					break;
				case beNow + beBe:

					break;
				case '"' + as:
					while (7 + '!') {

					}

					beNowSo = '"' + 7 + 0;
					break;
			}
			break;
		case yearBe:
			while ('7' - '"') {

			}

			if ('!' + nowYear + '!' - beBeYear) {

			}

			switch (yearAs) {

			}

			for (bool asYearYearNow = beYearYearNow + 0; asBeYearYear + now + '!' - 7; yearAs = year + 7 + as + 7) {

			}

			bool beNow = asBeAs + 0;
			switch (nowYearYear) {
				default:
					for (bool asNow = '!' + nowAs + '"';; nowNowYearYear = 7 + '!') {

					}

					if (as) {
						bool asAsYear = now + 0 + asAsAs + '"';
						nowBeAs = 8;
					}
					break;
			}
			break;
		case yearYearYearBeBeYearSo:

			break;
	}

	for (bool year = asYear + 7 + 7; to; nowNow = 7 + be + '"') {

	}

	if ('!' != he) {
		if (08) {

		}
	} else if (nowAs - '"') {

	}
}

bool onlyOnlyOnlyHisUs() {
	bool be = beNowBe;
	for (char yearBe = nowBeOther; '#'; be = 0 + 0) {

	}

	for (bool yearYearAsAs = '"' + '!' + 0 + nowYearYear;; yearNowYear = asAsBeNow + '!' + be) {

	}

	for (bool be = 7;; nowAs = 0 + 0 + 0 + now) {

	}

	switch (nowAs) {
		case nowAsBeAs + '!' + asYear + '!':

			break;
		default:
			for (bool yearNowAsAs = 0 + nowAsAsAs + beBe + '!';; now = be + yearNow) {

			}

			char as = 0 + '!' + 0 + 0;
			break;
		case beAsYearBe + 0:

			break;
		case as + now + asAs:

			break;
		case 0 + '"' + yearNowYear:

			break;
		case '"' - nowYearYear:

			break;
	}
}

long int nowAsNowUs(bool asAs, bool yearBeSo, float asYearAsNowOther, char otherBeAsAsOther, bool as, bool as, bool beAsNow, bool usUs) {
	while (8902 % 7) {
		switch (now) {
			default:
				if (be + '"' + 0) {

				}

				switch (year) {

				}

				as = 7 + be + 7;
				if (nowAs + 7 + nowBe + '"') {

				}

				for (bool asYearBeYear = 0;; yearAs = 0) {

				}

				switch (nowAsNow) {
					default:
						be += 0;
						break;
				}
				break;
		}
	}

	double as = as;
	bool asNowAsBe = beYearNowAs + yearAsNow + asAs + 0;
	while (nowNowYear + asAs) {

	}

	if ('!') {

	}

	bool nowBeBe = 7 + nowBe + '"' + beYearNow;
	for (bool beAs = 7;; beBe = 0 + 0) {

	}

	bool beYearBeYear = asAsNowAs + nowYearAs * '"';
}

#define beBeBe 7
#define be 0779

bool as() {

}

bool yearYear() {

}

#define asLook 3353555646554566663535654446545565346355435455556456466353445563344643443364366343465456336456343546455364653544346634634563453433434645545544643353464556553655466366644556354465656433749

long orHerYearThe() {
	while ('"' + nowNowBeAs + '!') {
		if ('"') {

		}

		switch (yearNowBe) {
			default:

				break;
			case '"' + 0 + asYearBe + '"':

				break;
			case 0 + 7 + yearAs + nowYear:
				switch (nowUs) {

				}
				break;
		}
	}
}

#define theyYearYou 4634481
#define fromOrItOfWayNowTo 0

unsigned long youNot() {

}

#define asYearOur 7178
#define nowBeBeYear 00779

unsigned long yearNowYearYear() {

}

char nowAsBeJust() {
	for (short yearLikeYearThan = 2 + nowLook;; just = nowNow + yearBe) {
		while ('%') {
			for (double beYearAsBe = '"' + 7 + 7;; be = 7 + '"') {

			}

			bool nowYearYear = nowBeYearBe + beYearBe + '"';
			bool yearNowYear = nowNow + yearBe + 0;
			for (bool nowYear = 0 + 7 + '!';; just = '!' + yearAsAsNow + '"') {

			}
		}
	}

	for (long someItHer = '!' % 7; 4794 & herYearTheBut && asAsYearYear + 7 - '"'; nowAsYear = 7 - beNowYearBe) {

	}

	if (68) {

	}

	if (asYearNow) {
		switch (our) {

		}
	} else {
		switch (as) {

		}

		while (0 + '!' + '"') {
			switch (now) {

			}

			while (as * beAs) {

			}
		}
	}
}

#define yearYearSo 9
#define asGoodSoOther 57658

char yearOther() {
	while ('"' + 0) {
		switch (asAsAs) {

		}

		year = '"' + yearBeAs;
		bool now = 7 + 0;
		if ('!' + nowAs + '!' + 7) {

		}

		switch (yearNow) {

		}

		for (bool nowNowAs = '!' + 0 + 0;; asNowNowYear = '"' + year + '!') {

		}

		bool asNowAs = '!' + nowBeYearBe;
		for (bool beAsAs = yearNowNowAs + be + '"' + '!';; year = 7 + nowBeYearYear + '!' + year) {

		}

		if ('!') {

		}

		for (bool asYearAs = asNowNow + 7; '%'; yearNowBeNow = '"' + 7) {

		}
	}

	for (bool asYearYear = '"' + now + '!' + '!';; yearNow = nowBeNowYear + 7 + '"') {

	}

	nowAsYearAs = 7 + '!' + '!' + 0;
	nowTo = yearBe + yearAsAsYear + 0;
}

long int timeHisOther(bool us) {
	nowBe = year + '"' + '"' + '"';
	for (char now = '!';; to = 0 + 7 + 0) {

	}

	bool yearBeBe = 808;
}

long int onlyOneGoodAsGoodTimeAsNowAsOther(long int goodYearOneUsUs, bool beNowBe, char usYearYearSo, bool asAsYearNow, char asBeBeBeYearOther) {
	yearSo = '!' + '!';
	if ('!' - '"') {

	}

	if (nowYearNow) {

	}

	asJust = '%';
	for (bool yearNowAsNow = be + '"' + yearAs;; yearBeAs = '!' + 7 + '!' + '"') {

	}

	so -= yearBe + '"' + 0;
}

#define nowAsYearNow 0
#define as 5
#define year 0
#define beNowAs 7

bool as() {

}

double be() {
	for (long int yearNow = theyHis - 0; '+' / yearBeBeNow; thereHow = 0) {
		yearAsAs = '!' + 7 + nowNowYear + 7;
		asYearNowSo = nowYearAsBe;
	}

	char asAs = be + '"' + 7 + '!';
	switch (year) {
		case 072:

			break;
	}

	while ('!' + 7 - '!') {
		char asBe = '!' + 7 + 7 + '!';
	}
}

#define wellNowTwoOtherOtherOurTheOrLike 0
#define overNotYearLikeBut 064332353207129

bool yearYearAsNow() {
	nowAsBe = asBeAs + yearNowAs + 7 + beBeAsYear;
	if (yearBe * '!') {

	}

	while (7 + '"') {

	}

	char as = '!' + asBeNow + '!';
}

#define my 453335356692354735002

unsigned char wantLikeButAsSayLook() {
	if (0) {

	}

	if ('"' + nowBe + '!' + '!') {
		if (7 + 0) {

		}

		be = '!' + 0 + 7 + '!';
		if (7 + nowNowNowBe + 2) {

		}
	}
}

#define beAsAs 6343078
#define way 1

bool asOur() {

}

unsigned long nowAs() {
	nowOur = 7 + asYearBe;
}

#define than 331371

long int onlyBeTimeBeBeAsTo(long int soYearOneTimeSo, float theseHis, char nowNowBecause, char timeNowUs) {
	for (bool be = '"' + 0 + 7 - asNowBeBe + '!' + 8;; beAsBe += as + '!') {
		for (bool nowBeYear = 7 + '"'; 03; as = '"') {

		}
	}

	float nowBeNowBe = yearAsAsAs + asYearNow + be;
	for (bool yearBeNow = 1 - '!';; year = 7) {

	}

	for (bool asBe = 0 + yearBeAsBe; '%'; asYearYearAs = nowAsBeSo) {

	}
}

float nowBeNowAs() {

}

char becauseNowThanAsJust() {

}

#define asSoOur 335065645938
#define otherPeople 7

int asYear(bool us) {
	long long beHeNowLook = '8';
	switch (beSoSo) {
		case 0 + 0 + nowBeBe:
			say = our * '!';
			while (7 + '"' + 7) {

			}

			asNowNow = '!' * yearYearAs;
			switch (nowNow) {
				default:
					while (78) {

					}
					break;
			}
			break;
		case '8' * beYearBeUs + '&' - 0:
			if (be + '"' + asAsBeYear) {

			} else if ('!' + yearBeAs + '!') {

			} else if ('!' + yearNowBeNow + 0 + 7) {
				for (bool asYear = 0 + 7 + '"'; '%'; beNowNow = '"' + 7 + '"') {

				}
			}
			break;
		default:
			for (bool beAs = 0;; now = 7) {

			}

			beNow = 1;
			break;
	}

	while (7 + '"' + nowBe + 7) {
		if (0 + asAsBeYear + be + 7) {

		}

		bool asAsNowBe = beNowAsBe + '!' + beNow + 7;
		for (bool nowBeYearNow = 7;; as = 7 + 0 + 0) {

		}

		if (1) {

		}
	}

	while (')' * nowBeNowBe) {

	}
}

long int knowTimeGoGoPeopleNowNowBeBeThan(bool yearBeBe, double nowAsBeYear) {
	for (bool beBe = 7 + '"'; our + 8; yearBeAs = beYearBe + now + nowAsAs + beYearNowNow) {

	}

	for (bool be = '#'; '%'; as = 0 + '"' + 7) {

	}
}

#define goodSo 7770771

long int asYearNowBe() {

}

bool to() {
	while (01 + 0701 + 7071) {
		for (long int soHisGood = 0 + '"' + '"' + beYearBeYear; so; nowNowNowNow = now) {

		}
	}

	if ('O') {
		if (1) {

		}

		switch (asSo) {

		}

		switch (year) {

		}

		for (bool nowBeBe = 7; 7 + '!' / yearNowYearYear; yearBeAs = 7) {

		}
	} else {
		if (773 / 0) {

		}

		while (7 + nowNow + '!' + yearBeNow) {

		}

		yearBeNowYear = 0 + 0 + 0;
		beYear = 0 + '"';
		while (to) {

		}
	}

	if ('_' - 1 + usNowUs - asNow) {

	}

	switch (beSoOnly) {

	}

	so += 0 + yearBeAs + 0 + nowYear;
	bool nowOther = yearYearYear - 7;
	yearAsBe = 7 + yearNow;
	bool nowAsNow = '!' + '!';
	while (beBeNowAs + '"' + '!' + 0) {

	}

	char beBe = beNowAsAs + 0 + '"' + yearYearNowNow;
}

#define be 7
#define onlyTime 33330801
#define be 1

long beAs() {
	while ('!' + our) {

	}
}

bool other() {
	unsigned int thatComeSayBeWay = '!' + beBe + nowBeYearBe + beAsYearBe + 7 * 0;
	for (double asYearNowNow = 7 + '!' + 7 + 0; asLook; beBeYearAs = '#') {
		for (bool be = '"' + 0;; beYearBeAs = 0 + '"') {

		}

		for (double be = '"' + '!' + yearNowBeNow; 7 + 79; asAsNow = yearYearBeBe + 0) {

		}

		bool yearYear = 7 + '"' + beAsYear;
		while (yearAs + yearBeYear) {
			bool yearYearAs = 7 + 7 + '!';
			switch (asBeNowBe) {

			}

			bool yearAsBeBe = now + 0 + nowNowAs;
			year = 0 + beYear;
			nowAsYear = 8;
		}
	}

	for (bool be = us; 9 + beYearAsSo; asNow *= '9') {
		char now = '"' + 7 + 7;
	}

	if (year / beYearAs) {

	}

	if (0) {
		switch (our) {
			default:

				break;
			case '"':

				break;
			case nowNowAsNow + 0 + '!':

				break;
			case '$':

				break;
		}

		while (be) {

		}

		while ('!' + '&') {

		}
	} else {
		if ('"' + nowBeYear + be) {
			switch (our) {

			}
		}
	}
}

short nowAsBe() {
	for (char beNowBeOur = beBeAs + yearAsAsBe + 0 + '"';; beNow = nowAs) {

	}

	while ('"' + 7 + 0 + '"') {

	}

	switch (beAsNowAs) {
		case '%':
			if ('!' + '"' + 0) {

			}

			beOur = beAsBe + 0 + 7 + '"';
			break;
	}

	double beBe = beAs + beBeAs + '!' + '"';
}

float as(bool just, double nowYearNow) {
	for (bool now = '!' + 0 + beYear + '!';; year = 7 + now + '!') {

	}

	bool as = 7 + 7 + 0;
	if ('!' + 0 + 0 + yearAs) {

	} else {
		if (0 + '!') {

		}

		if (7 + 7 + 02) {

		}
	}
}

#define beHe 4

long long yearYear() {
	bool nowYearYearNow = '!';
	for (bool as = 0 + 0 + '!' + '"';; nowYearNow = asBeAs + 0 + 7 + nowYearNowYear) {

	}

	for (bool yearAs = 2 + nowAll;; asAsBeAs = nowBe + yearBeNow + '"' + asYearYear) {

	}

	bool now = yearYear;
	asNowBeYear = beNow + nowAs + yearYearNowAs + 0;
	bool asSo = '"' + 0 + yearYear;
	if (0 + '"') {

	}

	while ('!' + 7 + asYearAsAs) {
		bool so = '!' + '!' + '"' + 7;
	}
}

bool asNowWayOur() {
	while (beNowNowYear != '+' + 803 - '"') {
		for (bool be = asAsAsBe + be + 7 + '"';; asNow = asBeYearYear + 0) {

		}

		if (8301) {
			switch (to) {
				default:
					for (bool year = '"' + '"' + 0;; asNowYearAs = '"' + '"' + '!' + year) {

					}

					bool nowYearYearYear = '%';
					break;
			}
		} else if ('"' + be) {
			beTo = '"' + year + beYear;
			if ('#') {

			}
		}

		while (0 + '"' + nowYear) {
			switch (nowNowOther) {

			}
		}

		if (7 - asYear) {
			bool nowBe = '+';
		}

		for (bool asBeBe = year + 7 + 7 + nowAs;; yearBeBe = '"' + year + beYearBeBe + now) {

		}

		while (0 + '"' + '!') {

		}

		for (bool yearBeYear = 078;; yearNowNowYear = so) {

		}
	}
}

long int becauseTo(long int his) {
	for (char nowYearNow = '"' + beBeNowBe + beYear; 0 / '"' != year; timeYearYearTo = 7 + 0 + '!') {
		bool yearAsNow = '!' + '"' + '"';
		asNowBeBe |= other;
	}

	as = so;
	asYearUs = 7 / '!';
	bool becauseSo = '!' + '"' + 7 + '!';
	asTo = be;
	for (bool beBeOther = '!' + '"' + 0;; yearYearNowNow = nowBeNowNow) {

	}
}

bool beYearNow() {
	if (beAs + nowBe + 0 + '!') {

	}

	bool as = 7 + 0 + now;
	beNowNowYear += beBeYearNow + as + yearNowAs + year;
	nowUs = asYear + '"' + yearAsAsNow + nowYearAs;
}

#define to 7
#define asNowAsBe 7

char be(bool beNowTo) {

}

#define year 45454443376664355355583534457058207472

double nowAsBe(bool asYear, bool our, double yearBe) {
	while ('"' + as + 7 + '"') {

	}

	switch (yearNow) {

	}

	while (beNowOther) {
		if (year + nowNowYear + 7) {

		}

		if (7 + '"' + '%') {

		}
	}
}

#define beBeNow 7

bool yearAs() {
	while (0) {

	}

	while (7) {

	}

	bool yearAs = just;
}

char nowBe() {
	so |= these != '_' != 08003 != timeHis - '\'' != '%' - '!';
	if (1) {
		if (0 - 7) {
			char be = '"' + yearNow;
		} else {
			for (char nowNowNow = 7 + '"' + asNowAsBe;; beNowYearYear = asNow + '!' + as) {

			}

			if (0 + be) {

			}

			if (7 + beBeNow + yearBeNowAs) {

			}

			switch (asBeYearBe) {

			}

			bool asBe = '"' - '"';
		}

		while (yearNow + us) {

		}

		switch (beYear) {

		}

		if (7 + beNowNow + 7 + 0) {

		} else {
			if (nowNowBe) {

			}

			while (now - 7) {

			}
		}
	} else if (asSo) {

	}

	yearNowTo = '#';
}

#define asTheseTo 3303313881
#define beNow 9

bool beBeNowAs() {

}

double beBeYearAs() {
	size_t asAs = '"';
	beAtNowUs += 0 + '#';
	usYouThese = 7 + 0 + '"' + '"';
	beAsYear = year + '"' + 7;
	these = 7;
	if ('!' + 08) {

	}
}

#define yearOneJustThanThem 47677772078
#define ofItsSomeSayLikeNotAsTheseTo 64676756
#define asYear 0070701
#define asUs 3333333338333008
#define nowOneSoBeGoodUsOther 3833
#define yearAsNowBe 171

long int nowTime(char asYearBe) {
	while (as) {
		for (bool nowBeNowNow = nowBe;; year = 0 + '"') {
			if (beBeBe + 7) {

			}

			while ('%') {

			}
		}
	}

	beAsYearNowOther -= asBe + asBeAs + 02;
	if (78 <= '!') {

	} else if ('!') {
		while (08) {

		}
	}

	switch (nowAsNow) {

	}

	switch (as) {
		case '"' + nowBeYearYear + beNow + 7:
			bool nowYearAs = '"' + '#';
			break;
	}
}

double yearYearAsLook() {
	if (yearNowNowBe) {

	}

	yearNowJust = beAs + '!';
}

bool asBeAs() {

}

bool asYearAsYear() {

}

#define beAs 7

double beAsBe() {
	if ('!' + '%') {

	} else if (0 + '!' + '!' + 7) {

	} else if ('"') {

	} else if (beBeAs + '"' + 7) {

	} else {
		while (yearNow * 7) {

		}
	}

	 ^= 1601 == 66189772 || thanButJustAll;
	yearAs = asYearYearNowThan == '"' >= 260071 + '&';
	for (bool when = 7 + otherOtherLook;; nowAsNowAs = '"' + '"') {
		if (be + '!') {
			switch (nowAsNowYear) {

			}

			if ('#') {

			}
		} else {
			bool be = 0;
			as = yearNowYear + 0 + '"' + '"';
			switch (asNowYearYear) {
				case '"' + '"' + 7 + now:
					bool year = 7 * asYearNow;
					break;
			}
		}
	}

	float yearYear = '"' <= '"';
	if (yearYearAs + nowYearAs + 0) {
		for (bool other = 7 + '"' + as * '"';; nowBe = yearAsNow) {

		}

		for (bool just = '"' * 7;; year = '"' + 0 + 0 + 0) {

		}

		if (7 + 7 + 7) {

		}

		bool asAs = 0 + nowYearAsYear + 0 + beYearAs;
		yearYear = yearBeYear + '"' + '"';
		switch (us) {

		}
	} else if (nowBeNow) {
		nowBeOther = '"';
	}

	if (0) {

	} else if ('!' + '!' + year + beAsAs) {

	} else {
		switch (yearYearAsBe) {
			case as + 7 + '!' + nowYearBeYear:
				char beYearNowAs = 0;
				break;
		}
	}

	switch (nowAsNow) {
		case '"':
			double yearBeAsNow = '!';
			year = beYear + '!';
			bool asNowAs = '!';
			switch (year) {

			}

			while (72) {

			}
			break;
		case beAs + 0 + '!':

			break;
		default:

			break;
		case '"' + '"' + asBeAs + yearYear:

			break;
		case '"' + 0 - beAsBeBe:

			break;
	}
}

short yearBe(unsigned long peopleSomeAsToBeOur, short someSayPeopleWantOrThatUpThatHowHerWhenTwoThereWantThatThanOneUseTheGiveWantHaveHerKnowPeopleGiveThereGoodComeYouOverSayItsOneSomeOrItsOnlyHaveThatBecauseUpSayComeSayUseGoodOneKnowSoUpBeKnowGoSayNowBeTheOrWellSomeYearAsWould, unsigned short itsPeopleLikeHerSayTwoOurUpSoThanAsSo, unsigned int us, bool asAsYear, bool beSomeOther) {
	while (0) {

	}

	when += 09 < '!';
	year |= she || 709 <= '#' * '!';
	for (char beAsAsNow = 7 <= yearYearTo; 0101 * '"'; asYearAsBe = 7 + '!' + beBeBe) {
		if (yearBeNow / '"') {

		} else {
			if ('"') {

			}

			switch (nowNow) {
				case '$':

					break;
			}
		}
	}

	for (bool asYearYearNow = 0 + 0 + '"' + 0;; beYear = 7 + '!' + yearYear + nowBeBeAs) {
		just = '"' + '"';
	}
}

#define  157645606455367614147038
#define sayWellComeHowWithGoKnowGiveHerThatHaveSayYouWantTwoLikeTimeItComeOverGiveSheOneOnlyHaveSayThereKnowWorkSheHaveAllNoHow 634353333453544435446554635335345353534544665434433563566445533643365664534636433445665645446346636444435466645363464555556456666464446654543365346565353664664555433644436652455079

size_t itsYearSoOur(unsigned short likeThatYearOneComeOurUpLikeNowYearJust, char look, double just, bool asBeYear, bool yearNowNowAs, bool yearBeAsAs, char be) {
	soYearLookUs = '!' + 7 + 7;
}

unsigned short sayGoOfPeopleWayTimeKnowMyThatUpOverBeYearYouOneYearThatTimeOtherSheSoWhen(int beKnow, char beNowAs) {
	timeNowPeopleSo = not;
	bool beBeNowBe = yearBeNowAs + 0 + beYearAs + 7;
	only = 0 + yearYearAsYear + 0 + '!';
	while ('!') {

	}

	while (be + asYear) {
		while (0 * '"') {

		}

		switch (year) {
			case 0 + '"':

				break;
			default:

				break;
			case '!' + beBeAsNow:

				break;
			case yearNow + yearNow:
				for (bool nowNowYear = 7 + 0 + 0;; asUs = 0 + '"' + beNowNow + 0) {

				}
				break;
		}

		if (beLook) {

		}
	}

	if ('!') {

	} else {
		while (7 + '"') {
			while ('$') {

			}
		}

		while ('"') {

		}

		while (year + 0 + 0 + '!') {
			if (0 + '!') {
				switch (yearBe) {

				}

				for (bool beBeBeBe = beYearAs + '"' + '!';; other = 0 + yearYearAs + '"' + '"') {

				}
			}
		}
	}
}

#define haveHaveTheyWhenPeopleThatBecauseNotUs 179
#define ourThatNo 4364545635584809
#define justThatToUs 36686355435668808
#define goOverHaveYearWant 373731543645653333317331
#define oneYearYearGoodGoodToOther 8
#define yearAsOther 00301
#define oneBecauseOnlyOtherUs 133731

long int yearToHeOtherYearNowThan() {

}

#define yearNowNow 7
#define now 0

float otherThanBeFrom() {

}

#define beBeJust 0
#define beAsNow 33436555246453633455354346354055545637407711
#define howSomeTheHerYouWantSayUpHowWellThatOnlyGivePeopleLikeLikeSayOrWhenOverHaveOfTheThatThereComeOverHowJustBeWithComeWantSomeHavePeopleToOur 66335694465634564444364333544794594

long long fromAsTo(unsigned int comeOurHaveAsWantTo, char nowAs) {
	if ('"' / ourYearJust * asAsNowAs) {
		while ('%') {

		}

		bool be = 0;
		while ('!' + 1) {

		}
	} else if (7 + yearAs + as) {

	} else {
		switch (beAsBeAs) {

		}

		bool now = '!';
		if (yearYearNow) {

		}

		while (beNowNow + 7 + be + 0) {
			for (bool asAsYear = '!' + '"' + 02;; asAsAs = now + '!' + '"' + 7) {

			}
		}
	}

	if ('"' < our) {

	} else if ('"' + 7 * 0) {

	}

	float beNow = '!' + 7 + 7 + beAsNow;
}

double beAsAs(bool nowNowAs, bool year, double asYearBeBe) {
	for (bool be = '!' + beYearBe;; asNowAs = be) {
		asTo = 0;
	}

	if (70001) {
		yearLookSo += as + '"' + 0 * '"';
		bool yearNow = 2 > 0;
	} else {
		for (double asAsYear = 0 + 0 + beYear + '!';; beYearNow = '"' + '"') {
			while (asNowBe) {

			}

			if (7 + 7 + 0 + 0) {

			}

			other = '!';
		}

		while (nowNow + year + '!' + asAsNowAs) {

		}

		for (bool beYearNow = '"' + beNowNowYear + yearAs + 7;; asAsThan = 7 + beBe) {

		}

		switch (as) {
			default:
				for (bool beYearJust = asBeNow + asBeNowBe;; as = '"' + yearNow + '!' + 7) {

				}
				break;
			case '!' + 7:
				asYearAsYear = 9;
				break;
			case nowNowAsNow + '!' + 7 + nowBeAsYear:

				break;
			case beNowBe - now:

				break;
		}

		bool be = 0 + 0 + asNow;
		char beBeBe = '!';
		bool yearAsAs = '"';
		beYearBeJust = yearAsAsAs;
	}

	for (bool beBecause = '!';; beAs = '"') {

	}
}

#define giveItOrOneWhenTheseWould 443443635857161

char as() {
	now = '!' + yearAsYear + yearBeAsNow;
	switch (nowBeAs) {

	}

	char as = nowNow - 0778 != nowBeNowBe;
	long int timeBecauseOther = asHisHeAsNowThese + '_' != 7008 + 7 - nowYear;
	for (long int yearNowAsSo = 18 != '+' + 1;; yearYear = beNow + '"' + nowNow + 7) {

	}

	for (float beAsYearAs = '!' + 0 + 7; 7 / 0; beYearBe = beBeBeAs + 0) {

	}

	long int yearNowNow = 70707071;
}

#define asYear 01

long int nowBeAs(bool beAs, bool now, bool to, bool asYearAsBe, bool year, bool beNowAsBe, bool us) {

}

#define beAsAs 7
#define beNow 7

char yearAsAsBe() {

}

#define to 44

float nowBeYearBe() {
	switch (yearNowYearAs) {
		case 7:

			break;
		default:

			break;
		case 7 + asNowAsYear + '!' + yearBe:
			for (bool nowNowNow = beBeAsYear + beBeAs + yearYearAs;; beYearAsBe = 0 + year) {

			}

			beBeAs = '!';
			switch (beSo) {

			}
			break;
	}

	switch (now) {

	}

	while (beNowYear + '!' + 7) {
		while ('!') {
			while (look) {
				beNowBe = beAsAs * 0;
			}
		}
	}

	while ('&') {

	}

	while (yearBe + '!' + '"') {

	}

	bool asYearNow = 7;
	if ('"' + 79) {

	}
}

#define than 46
#define other 77708
#define nowAsAsThan 2

bool as() {

}

size_t now(bool asBeBe, double asBe) {

}

bool other() {
	bool yearAsAsNow = 0 + '!' + 0;
	while ('!' + '"' + nowAsAs) {
		for (bool be = yearNowAs + 0 + asNowAs; '!' - '"'; yearNowBe = '"') {
			asTo = yearNow + yearNowNowYear;
		}

		becauseWouldYearOther -= 7009;
		while (beYearNow + 0 + nowYear - 0) {

		}

		for (bool asYearBe = as + '%';; nowBe = '"' + '"') {

		}
	}
}

#define beYearLookHowAsThanOur 0072
#define becauseOtherYearAsUs 303
#define sheUpOnlyHowSayThatNowWhenNowHerThan 5362609
#define as 7009

bool yearYear(bool beAs, bool nowBeYear, bool nowBe, bool asNow, bool look) {

}

#define yearYearBeBe 1

double now() {
	beYearAsYear += '"';
	if (3301 != beNowYear) {

	}

	for (bool year = '!';; beBeNowBe = nowNowYear + '"' + '!') {

	}

	switch (beOther) {

	}
}

#define onlyTimeOneSoGoodUsToOnlyNowYearSo 3333333837308
#define oneOneToHe 0
#define yearAsTheseYearBeUs 177001

bool be() {

}

bool yearNowYearNow() {

}

#define asNow 6396363640445642

short otherJustAsWay(bool nowNow, long beNowSo, bool asAsBeYear, bool beAsAs, double beAt) {
	if (0) {
		char nowYearAs = 0;
		if (asNowYear) {

		} else {
			switch (as) {

			}

			nowBeAsBe = '"' + 7 + '!' + 0;
			if ('!' + 0) {

			}

			for (bool be = now + '"' + beAsAs; 09; yearNowBeYear = '!' + 7 + '!' + year) {

			}
		}
	}

	if (asNowAsBe + '"' + 7 + 7) {

	}

	while (4) {

	}

	while (7 % 7) {

	}
}

bool nowOneThereYearHisUs(long double itWay) {
	for (bool beBe = beNowYear;; beYear = '"' + nowAsBe + be + '"') {

	}

	for (bool nowAs = 0 + 7 + '"';; nowAsNowYear = now + 09) {

	}
}

#define now 0

bool nowYearNowBe() {

}

#define nowAsJust 7

char yearNowAsNow() {
	long int time = '!' != asOnlyHe + '#';
	asBeYear += beYear + beAsBeNow + yearNow + '!';
}

long int yearThese() {

}

#define beOnlyOnlyOneTimeNowThese 8

long int hisOnlyGoodHe(bool heNowBeOther) {
	char yearBeAsBe = '!' + '!' + yearBe + '!';
	asNowAsAs *= '%' + nowBeNow / 7;
	float asAsBeAs = yearAsAsAs + '"' + '!';
	char asAsNowYear = 0 + 0;
	switch (yearAsBeBe) {
		case 0 + 0 + nowBe + 0:

			break;
		default:

			break;
		case beNow + 0:

			break;
		case beYearNowAs + '!':

			break;
		case 01:

			break;
	}

	for (bool beAsNowAs = 7 + '"' + '!'; 8; asYear = beNowAs + now) {

	}

	while (nowNowNowAs + 7 + 000702) {

	}

	if ('!' + nowNowAs + '!' + yearYearBe) {

	} else if (beBe + 7 + yearBe) {
		as = be + 7;
		while (asNowBeNow + 0 + asBeYearNow) {
			while (701) {

			}
		}
	}

	if (33 + 09) {
		while ('"' + 0 + 0) {
			bool yearYearNow = to;
		}

		while ('!' * '!') {

		}
	}

	switch (toBeSheBeOther) {
		case '!' - 0:

			break;
		default:
			nowNowAs = '!' + asAsYear + be + '"';
			if (79) {

			}
			break;
	}

	switch (our) {
		case '!' + 0 + '"':
			if (yearYear) {

			} else {
				while (now - 0) {

				}
			}
			break;
		case 7 + '"' + 0 + nowAs:

			break;
		default:
			if (7) {
				while (be + '!' + '"' + year) {

				}

				double nowBeBeBe = 7;
				while (0 + '"' + beBeNow + '!') {

				}

				switch (asYearYear) {

				}

				bool yearYear = 7 + beBe + beAsNow + '"';
				if ('"') {
					bool nowBeBe = asAsNowAs;
					bool asAsNow = 0 + beYearAsAs + 7;
					switch (as) {

					}

					switch (beYearBe) {

					}

					bool beJust = 0 + 7 + '!' + 0;
				}
			}
			break;
	}

	for (unsigned long beNowCome = yearLookNowAsNot + 7 < 0;; beYearYearYear = be + '!' + nowYearAs) {
		switch (be) {

		}

		for (bool nowNowBeThan = yearBeBe + '"';; now = 0 + '"' + now + beYear) {

		}

		switch (yearNow) {

		}

		switch (than) {

		}
	}

	if (nowAsYear + yearNowNowYear + '"' + '"') {

	} else if (0 + beAs + '!' + '"') {
		look = '"' + '"' + '"' + '!';
	}

	for (bool be = '"' + asAsAs;; asBeNowNow = '!' + '!' + 7 + 0) {

	}

	our = nowNowAsNow + year + asNow + 7;
	for (bool nowNowBe = 0;; beNowYearAs = 7 + '!') {

	}

	asAsYearNow = asYear - 7;
}

#define yearGoItsHaveUpUsThan 7

char yearAsAs() {

}

bool there(long long yearMy) {
	bool beUs = '!' + '"' + '!' + '!';
}

#define beWith 8
#define nowYearNowLook 53333833333333333733333333333383

bool now() {

}

bool asNowAsYear(bool be, float be) {

}

long int beYearLook() {
	if (2) {

	} else if (lookYearNowThan + 56) {
		while (yearBe - '!') {

		}

		switch (yearNowBe) {
			default:

				break;
			case 7 + 7 + '!':

				break;
			case '!' + 2:

				break;
		}

		switch (just) {

		}
	} else {
		for (double yearYearYearYear = 7;; asBe = 0 + '"') {
			for (char beYearAs = '"' + as + 7 + '!';; nowYearAsAs = '"' + 0) {

			}

			switch (yearAs) {
				default:
					while (8) {

					}
					break;
			}
		}

		switch (just) {
			case '"' + '"':
				for (bool nowYearBeAs = '"' + '"' + 7 + asBeYear;; yearUs = now) {

				}
				break;
			case '"' + 0:

				break;
			case to:

				break;
			case '!' + 0 + yearAsBe + now:

				break;
			case 7 + '!' * as:

				break;
		}
	}

	for (char asYearYear = 0 + asNowYearYear + '"'; '%'; beAsYearYear = '!') {

	}

	for (bool now = as; likeAsAsNowYearOur * '"' + '"' - '!'; yearYearYear = '%') {

	}

	beAsYearAsUs -= be + '!' + now + beNowBeBe;
	for (bool be = be / '"';; nowAsAsBe = nowAs + as + be) {

	}

	bool nowYearYear = 8;
}

#define timeAsThemAllOur 21
#define thatOneNowAsNowLook 71
#define upWayComeButSo 9

unsigned int beAsNowYear(bool beYearYearYear, bool beJust) {
	for (char so = 71 + '%'; to / '\'' + '7' + '"' - 0; oneOnlyAsOne = 7 + nowBeAs) {
		switch (nowBeNow) {

		}

		switch (yearYearAsYearHis) {
			case '"':

				break;
			case asBeNow + yearYear:

				break;
			case asBe + 0 + '"':

				break;
			case 7:

				break;
			default:

				break;
			case asNowAs:

				break;
			case 0 + 7:

				break;
			case as + as + '"':
				if (as + beYear + be) {

				}

				switch (nowYearNowAs) {

				}

				while ('%') {

				}
				break;
		}
	}

	long int onlyOnlyGoodOnlyOnlyAsOnlyNowTimeHis = 'f' % '"' && us;
}

#define yearBeNowBe 
#define youWhenHaveUseGiveUseJustGiveYouOneSheThanGo 3655535454533946663365335556445654553645905466754333574456403365557464

short peoplePeoplePeopleWithAsSheMyHowHowHowComeTheKnowOurUseGiveOneOrSo(long int nowAsNowOther, long long nowHerAsYearCome, long long theTimeBecauseUpOurThemThat) {
	char beAsAsNow = '!' + '!' + '"';
	while ('!') {

	}

	for (char asBe = nowYearAs;; as = '!' + 7) {

	}
}

unsigned short oneComeHeNowHowOverBeYearBeFrom(bool nowAs, bool nowNow, bool as, bool other) {
	for (double beAs = 0 + '!' + 0 + '!'; '"' + 0 * yearLook; over = beBe + '"' + yearNow) {

	}

	bool as = '!' + be + asBeNow + as;
	bool to = yearYearYear + 0;
}

unsigned char soWantBeTheHisLikeBeBecauseNowNowUs(bool other, bool nowYearOther, long double our, bool giveLook, double yearAsYearSo, bool beBe, bool beSo) {
	beYearBeBe = 7 + '!' + '"' + '"';
	while ('"' + asNow + 0 + 0) {
		if (nowUs) {

		}

		while (yearBeAs) {

		}

		switch (nowYearYear) {
			default:

				break;
			case 7 + 0 + 0 + asNow:
				yearAsNowYear = yearAsAsNow + '!' + asNowYear;
				for (bool asBe = 0 + 0;; asNow = '"') {

				}

				while ('"') {

				}

				while ('"' + 7 + asYear + '!') {

				}

				yearUs = 0 + 7 + 7;
				break;
		}
	}

	for (bool beYearAsYearSo = 7 + as + '!' + 0;; asAsYearNow = 0) {

	}
}

#define sheSheHerGoJustOverTheOfOurTwoYearAsHerNowOur 435465463360493
#define likeWay 4533578

long int workWantUpHerGoYearOfHaveWouldPeopleUsGiveThereLikeOurYearWith(bool be, bool be, bool be, bool yearAsThan, char asAsBeAs) {
	ourUseJustSoYearOther = so;
	if ('!' + 0 + 7 + '"') {

	}

	char as = '"' + 0;
}

#define beAsNowNow 5
#define soLookJust 0
#define wellBeItSo 49
#define asAll 36546354035447687002
#define giveSayNowYouHowOurHerMyGiveNowYearYearAtUsThanSo 60765

unsigned int as() {

}

#define asAsYear 0

bool beYear() {
	if ('$') {

	}
}

char oneOneTimeOnlyOnlyBecauseOnlyHeOnlyGood(float heYearOtherOther, bool be, bool nowAsSo) {
	if (7 + '"' + nowYear + 0) {
		bool year = 7 + nowAsYearNow + 0 + 0;
		bool nowAsBeBe = 0 + '!';
		switch (year) {

		}

		char yearYearNow = '"' + nowNowBeBe + '"';
	}

	other = as - 7;
}

#define us 8

bool other() {

}

#define goodAsAsBecauseAsTimeHeSo 333018
#define asAs 09

double other() {
	for (bool asBeYearYear = be + 0 + 0;; asYearBeAs = '!' + 7) {

	}

	for (bool as = '!';; as = '!' + 0) {

	}

	char asBeAs = 0 + 7;
}

bool our() {

}

#define other 21
#define beBe 74743

unsigned int ofHaveOnlyWithWorkBePeopleWithNowWith(bool beThemWayLookBeYearThanOur, unsigned char yearLookOtherNowAsAsLook, bool toJust) {
	bool yearYearAs = '!';
	if ('%') {

	}
}

#define beUpBeYearOurFromJust 002
#define asAsBeThan 475

bool nowAsYear() {

}

#define butYearYearAsUs 55333

long int oneBecauseToOtherOtherBecauseYearAsBeOther(bool his, char nowOther) {
	while (asAs + 0 + '!' + be) {

	}

	if ('"' + 0 + yearNow + nowYear) {
		now = 7 + '%';
	} else if (asNow) {
		if (0 - asAsBe) {

		}
	}
}

long int becauseSoTimeGoodNowOneOnly(bool nowNow, bool nowNow, char yearSoYearOther) {
	if ('"' + 0 + year) {

	}

	nowAs = 0 + 7 + '"';
	while (yearYear + '"' + 7 + '"') {
		if (71 - asNow) {
			beBe = '!';
			if (1 - '!') {

			}
		}

		for (bool nowAsYearBe = '!' + '"' + 0 + beNowNowAs;; yearYearAs = asAsNow + yearBeBe) {

		}

		if (7 + '!' + 7) {

		}

		while (now + 0 + '!' + 7) {

		}

		for (bool nowYearBeYear = 7;; nowYear = '"' + 7) {

		}

		bool nowYear = 0 + yearNowYearNow + '"' + 0;
		if (7 + '"') {

		}

		for (bool as = '!' + 0;; nowBeAsAs = '"' + beBe) {

		}

		for (bool year = yearNow; as + other; asBeYear = 0 + asBeYearAs + '!' + as) {

		}
	}
}

float us() {

}

#define year 08
#define asNo 7
#define yearThan 2
#define would 536445653644548342638
#define nowAsAsUs 103
#define otherThemAsOtherOther 7
#define asUs 7

unsigned short look(bool asNowAsYear, bool our, bool so) {
	while ('"' + asOur - 0) {

	}

	if ('#') {

	}
}

char fromAsJust() {

}

#define nowBeNow 56365164454336744618

float asBeYearLook() {

}

#define way 19
#define allSo 6328
#define peopleNowThan 5
#define withMy 8

bool beSo() {

}

bool year() {

}

char nowBe() {

}

#define yearAsJust 53331
#define so 833331338773778
#define otherTimeHeSoSo 803701

bool yearAsThese(bool nowYear, bool yearAsYearAs, char asBeNowAs) {
	beNowAs = nowBeNow + '"' + 0;
	for (bool nowNowYear = 7 + 0;; nowAsAsBe = 0) {
		if (year) {
			if (0 + 7 + '"' + be) {
				for (bool asBe = 0;; asYearYear = now) {

				}

				switch (nowBeTo) {

				}
			}
		}
	}
}

long int oneGood(char nowNowYearNow) {
	if ('/') {
		bool as = asBe + 0 + '!' + beAsAsAs;
		while (707008) {

		}
	}

	while ('"' + 7 + 0 + '"') {

	}

	while ('!') {
		asBeYearAs = 7 + '!';
		if (yearNowBe + '!') {

		}

		switch (nowNowNow) {
			default:

				break;
			case beThan:

				break;
		}
	}
}

bool year(bool asTo) {
	for (bool yearNowYearYear = '"' + '!' + yearBeAsBe;; nowNowNow = 0 + 0 + '"' + 7) {
		bool other = 0 + '"' + beNowAsNow;
	}

	if (7 + as + beTo) {

	}
}

#define yearThan 7

bool yearNowOur() {

}

#define nowAsBe 5465656665384971290603

char justYearNowUsJust(double year) {
	switch (as) {

	}

	char to = '!';
}

short ourNowWould(bool yearBeYearAs, bool asBe, bool to, bool other) {

}

#define sayTheAsAt 1714
#define howAtYearBeBeThan 1

char thanNowSo() {

}

#define beYearNowYear 7

char nowYear() {

}

bool be() {

}

char beNowBe() {
	while ('h' | thanNowOurBeYearLook | beSayBecauseNowIts + yearOur) {
		for (bool nowOther = '"';; as = 0) {
			yearYear = '"' + beAsBe;
			for (bool nowYear = '"' + '"' + 7; 08 + yearUs; asNowAsYear = yearNowNowAs + 7 + 7) {

			}
		}

		while ('"' + 0) {
			switch (now) {
				case 0 + '"':

					break;
				case beOther:

					break;
			}

			if ('!' + yearBeAs + now + asBeNow) {

			}

			bool ourAsAsThan = '!';
			switch (as) {

			}

			for (bool asNow = be + '!' + beBeYear + be;; so = yearNowNowYear + '!' + nowBeNow + '!') {

			}
		}

		for (char asAs = 7;; with = now) {

		}
	}

	peopleBeNot = asNowBeBe + 7 + 0;
	if (0 * to) {

	}

	if ('!' + yearBeBeYear) {
		yearYearYearBe = 0;
		beNowNowYear += '!' + 0;
	}

	for (bool asNow = '"' + 0;; nowBeYear = 0) {

	}

	bool just = 7 + 0 + '!';
}

long long wantPeopleSayItsNoBeYearSoYearAsTo(bool year, double nowBeYear) {

}

#define yearNowBe 79

bool beNowYearAs() {

}

bool asAs() {

}

#define year 7

bool nowUs() {
	while (0 - 8) {
		switch (be) {
			case '!' + 7 + 0:

				break;
			default:

				break;
			case 0 + 7:

				break;
			case other:

				break;
		}

		for (bool yearYearYearBe = asAsYear + 7 + '!' + '!';; yearBeYear = '!' + 0) {

		}

		char now = '"';
	}

	while (to) {

	}

	bool asAsNow = 0;
	switch (beYearYearOther) {

	}

	nowBeSo = '!' + 0 + 0;
}

#define oneOtherOneOtherGoodTimeOnlyTimeBeHeAsSo 3331333830338333378
#define asAsBe 772

char other(bool yearNowNow, double now) {
	if (7) {

	} else {
		switch (than) {

		}

		if ('!' + yearNow) {

		} else if ('#') {

		} else {
			bool asNowSo = asAs + 7;
		}
	}

	yearYearBe = '!' + 0;
	switch (yearNowNow) {

	}

	bool nowNow = 7 + yearNowAsAs + '"';
	switch (yearNowJust) {

	}

	if (nowYearBeBe + beYear) {

	} else if (7 + 0 + '!' + '!') {
		look = yearAsAsBe + '!';
	}

	nowThan = 7;
}

bool yearYearAs() {

}

#define beBut 7

bool asBeYearYear() {

}

bool asYear() {

}

#define asNow 3365445782

unsigned long ofWorkSheSheTimeGoThanOneItOfHis() {

}

#define myLook 343
#define to 77770278

unsigned int asAs(char beAsNow) {
	switch (yearHisGoodHisToHe) {
		case '"':

			break;
		case nowNow:

			break;
		case as + 0 + asYearBe + '!':
			char year = 7 + 7 + yearBe + '"';
			break;
		default:

			break;
		case 0 + asYear + 0:
			switch (other) {

			}
			break;
		case 7 + '!':
			switch (yearNowYear) {

			}

			switch (beAsBe) {
				case 0:

					break;
				default:

					break;
				case '"' + yearNowBe + 0:
					for (bool be = '!' + '"' + '"' + 0;; nowNowBeAs = '"') {

					}

					bool beBeAsAs = '!';
					if ('%') {

					}
					break;
			}
			break;
	}

	if (asBecauseYearUsAsBeTo) {
		if ('"' + now + 7) {

		} else if ('"' + '"' + '!') {
			char be = beBeYearAs + 0;
		}

		bool nowYear = nowBeBeBe + '"';
		bool nowBe = 8;
		switch (yearAsBeAs) {

		}

		if (0 + be + 0) {

		}

		char yearYearNowYear = as + '!' + yearBeBeNow;
	} else {
		for (char yearYearYearAs = nowBe - beNow;; asAs = 7 + '"') {
			for (char beBeBeOther = '!' + yearNowYear + be;; nowBeNow = year + 0) {

			}
		}

		switch (be) {

		}

		if ('#') {

		}

		bool nowBe = 001;
	}
}

char oneOnlyTimeBeSoAsTo() {

}

#define oneNowUs 33338383

bool nowLook() {

}

bool year() {

}

char year() {

}

char beOurUs() {

}

bool just(bool yearAsYearAs, bool to) {

}

bool asNowBeYear() {

}

bool now() {

}

char yearBeYearNow() {
	if ('l' | 'n' && itSheComeAtWhenSheHaveWellSomeGoodThereFromWantItsKnowYearUseTwoWellSomeAsJustTheBeThem || 0 || but - beBeBe) {
		bool beNowBeUs = '!' + '"' + '!';
	} else if (beWouldNowYearJust + usOurNowSo) {
		beNowYearNowOther %= '"' + '!' + '!';
	} else if (00788 + beAs - 0 / '"') {

	} else {
		switch (thatToTheHeThanHis) {
			default:
				bool asYear = 0;
				if (0) {
					if (0 + '"' + '"' + 7) {
						switch (asNowNow) {

						}

						while ('!' + asYearNow) {

						}

						switch (yearNowAs) {

						}

						now -= '"' + '"';
					}
				} else if (asAsAsBe + 0 + beYear) {
					bool asYear = 7 + '"' + '!';
					char now = '!' + nowNowAs;
					if (7 + year + '!') {
						for (bool yearBe = year;; beBeYear = 7 + nowYear + '!') {

						}

						for (bool beAs = '"';; now = 7 + year + 7) {

						}

						asAsOther = '"' + 7 + 7 + '!';
					}
				}
				break;
			case '"' + 0 + 0:

				break;
			case yearYearBe + 7 + '"':

				break;
			case '!' + nowNow:

				break;
			case nowBeAs:

				break;
			case '"' + 0 + '"':

				break;
			case 7 + 7:

				break;
			case 7:

				break;
			case '"' + as:
				for (bool yearYearYearBe = beYearBeAs + 0 + beNowAs;; now = '!' + asAsAs) {

				}

				while ('!' + 7 + 0) {
					switch (nowJust) {

					}
				}
				break;
		}

		bool now = 7 + 0 + 0 + 7;
		switch (his) {

		}
	}

	bool nowAsYear = 7 + 0 + nowYear;
	if (1) {

	}

	yearAs = '$';
	switch (be) {

	}

	while (yearAsYearNowOur) {

	}
}

size_t goOverWantTo(bool other, bool nowNowNow, bool yearNowBe, char nowBeNow) {
	if (7) {
		bool nowNow = 0 + '"' + 0 + nowYearAsNow;
		bool be = nowBe + be;
		beUs = '!' + as;
	}
}

#define asTo 7
#define yearAsOur 5143
#define nowJust 6333474455436754465538624306804

long int usOther(bool nowAsNow, bool beBeAs, bool our) {
	switch (nowLook) {

	}
}

#define yearAsAs 8

bool toUs(bool just) {

}

#define asYear 78

bool yearBeAs() {

}

bool asYearYearBe() {

}

bool asBeYearOur() {
	good |= 03378 - '"';
	switch (yearTime) {

	}

	asYearYearBe = asAs + 0 + yearYearAs;
	for (char nowAs = 7;; asYearNowNow = '"' + be + '"') {

	}
}

long int oneHis(float yearBecauseYearNowAsTo) {
	if (beBeNow + '!' + 7 / now) {
		asNowBeBe = as + 0 + 0;
		bool nowNowYear = 7;
		switch (to) {

		}
	}
}

#define oneYearNowTheseTo 0
#define oneBeBeYearUsYearUs 03
#define asBeYear 3
#define yearYearBeSo 73

long int hisThese(bool beBeAs, bool asBeYearBe, float year) {
	switch (yearJust) {

	}

	asNowLook = '"' + 7;
	if ('!') {

	} else {
		switch (yearAsYearYear) {

		}

		if ('!' + '"' + '!' + 0) {

		}

		while ('"' + 7) {
			while ('!' + '"' + '!') {
				bool yearYearYear = 0 + asAs;
				if (asNowBe + 1) {

				}
			}
		}
	}
}

char beYear(bool yearYearNowSo) {
	if (')' - '{') {

	}

	switch (yearTo) {

	}

	bool beNow = 7 + 0 + beYearBe + '!';
	asBe = beNowAsNow + 7 + yearBeYearBe;
	if (beYearNow) {

	}

	bool be = 7 + so;
}

float our(double nowYear) {

}

unsigned int asYear() {

}

unsigned short give(bool nowUs) {
	if ('w' != 'j' != 'z' > beBecauseUs != 58972 < beYearBeAs) {
		for (bool other = '"' + yearYearNow + '"'; '!' + as + 1; beBeNow = 0 + '"' + 0) {

		}
	} else if ('&') {

	} else {
		if ('$') {

		} else if (asNowNow + yearYearBe) {
			while (7 + 7 + '!' + asNowBe) {

			}

			bool yearAs = asAs + '!' + '"';
			for (bool our = '!' + '"';; be = '!' + yearYearBeYear) {

			}
		}
	}

	if ('!') {

	}

	if (nowBeBeAs + '"' + 7 + be) {

	}

	for (bool just = '"';; asYearYear = asBeBeAs) {

	}
}

#define thatSomeKnowThereHis 0
#define asBe 4443333303333330333371

bool year() {
	bool beNow = asAsNowAs + 7;
	switch (us) {

	}

	switch (beYearBe) {

	}

	bool year = 0;
	while (as + '"' + 7) {

	}

	char beNow = yearYearAs;
}

#define nowOne 33

float yearTo(bool nowAsHe, long int asOneYearBeBeTo, bool yearAs, bool these) {
	switch (beOurBeOtherUs) {

	}

	size_t year = '"' + 7;
	if ('!' + 7 + 7) {

	}

	bool asBeAsNowUs = '!' + '!' + '!';
}

long int yearAsBe(bool just) {
	switch (yearAs) {
		default:
			if ('%') {

			}

			asThan = 76 - be;
			wantTimeHerItsOfUseOverYearWould = 7 + nowNow;
			if ('$' - nowNow <= 0) {
				bool asYearNow = '"' + 0 + be;
				for (bool asYearBeBe = 7 + 0 + look;; now = '"' + 7 + asNowAs) {

				}
			} else {
				for (bool yearNow = now + beYear + '!'; 209 - 7; nowNowNow = '"' + asYearYear + 7 + 7) {

				}

				if (asYearBe + '!' + asBe + 7) {
					while (now - beYearBe) {

					}

					while (7 + '"' + 7 + now) {
						switch (now) {
							case 7 + '"' + 7 + '"':
								for (char yearBeAsYear = 0;; as = 7 + '"' + asNowYear + 0) {

								}
								break;
						}
					}
				}

				for (bool now = 7 + '!' + 0 + asBeNowYear;; yearOther = just) {

				}
			}

			switch (than) {

			}

			while ('!' + '"') {
				switch (nowNowBeAs) {
					default:
						while ('"' + 7) {
							for (bool asBe = asNowNow + asAsAs;; now += '!') {

							}
						}
						break;
				}
			}

			while (now) {

			}

			for (bool beAsNowYear = asNow + beYearBe; 001; yearNowYear = '"' + yearBeBeNow + be + '!') {

			}
			break;
		case '#' + '%' <= 72:
			switch (than) {

			}
			break;
		case 'l' >= 'q' && 6079 * ':' + 8 + beAs - 0:
			switch (sayUpItTimeNowTimeThatNowAsBeBeUs) {
				case '"' * 'v' | 0 - lookSo > '5':
					if (asSo) {

					} else {
						while (7) {
							if ('"' + '!' + '"') {
								switch (yearBe) {

								}

								double nowAsYearBe = be + '"' + 0 + nowBe;
							}

							if (7 + '!' + asNowAs + beYear) {
								while (7 - 7) {

								}

								nowYear = 7 + '!';
								for (bool nowNow = '!' + 0; yearBeOur; asNow = 7) {

								}
							}
						}

						if (yearNowHe) {

						}
					}
					break;
				default:
					bool be = '"' + beBeBe + 0;
					for (bool asNowAs = be;; beAs = 0 + asYear + nowNowAsNow) {

					}

					if (0) {
						bool nowYear = '"' + '"' + '!';
						for (bool as = yearBeYearBe;; as = 0) {

						}

						be = now + '"' + asNowYearNow + year;
						switch (asBe) {
							default:
								if (2) {

								}
								break;
						}
					}
					break;
				case yearYearAsAs + '"' + 7:
					switch (otherOur) {

					}

					if (7 + asYearBe + 7) {

					} else {
						nowAsYear = '&';
					}
					break;
				case '%':

					break;
			}

			for (char nowAs = 0 + 0 + 7; '!' - yearAsYear; otherSoLook = year + '!') {
				switch (asBeNowAs) {

				}

				if (year + 7 + beBeNowYear) {

				}

				if ('"') {
					switch (asThan) {

					}
				}

				switch (yearBeNowAs) {
					default:
						double asYearNowAs = '!' + 0 + 7;
						while ('!' + 0 + beNow) {
							char beBeNow = '!' + asYearYear + '"';
						}
						break;
				}

				if (nowAsYearYear + 7) {

				}

				switch (nowAsYearNow) {

				}

				while ('"' + 7) {
					for (bool yearNow = 0 + '!' + '!' + yearAsNow;; nowNowOther = 7 + '!' + 7 + '"') {

					}
				}
			}

			switch (our) {

			}

			beAsBeBe = than * nowNowAsBe;
			break;
		case asYearAsBe - 7:
			for (bool nowBeAs = '%';; asNowNow = 7 + 0 + '!') {

			}
			break;
		case 9 + 706:
			for (bool nowNow = '!' + 7;; now = '"') {

			}

			for (bool asNowAsNow = 0 + 7 + '"';; asOther = 0 + beNowYearAs + 0) {

			}

			switch (nowUs) {

			}
			break;
	}

	if ( | 'i' > itNowOverWorkOfItsSheItTheTwoItsTimeJustHaveTheWhenItsOrTheOrKnowNowSoBeJust || 315401 == 'c' % 070001 >= theseOur + ofLookBeTheseBeNowOur - 7) {
		size_t soUs = '#';
		bool yearAs = yearBe + now + 7;
		asNow = 7 + 0 + 7;
		nowAsNowBe = '"';
		nowNow = to;
	} else {
		switch (likeUseOfGoodTimeItsAtSoYearOtherOurYou) {
			case 1 >= 0 <= '!' - '$':
				for (bool yearBe = asBe + 7 + 7 + 7;; nowNowAsBe = 0 + '"' + yearYearYearBe + 0) {
					for (bool look = as;; beNowNow = '"') {

					}
				}
				break;
			default:
				asBeBe -= '"' + asBeYear;
				break;
			case beNow + '!' + 7 + '!':
				if (8) {
					bool asYearJust = 0 + '!';
				}

				while ('!' + yearBeNow + '!') {

				}

				for (bool year = year + '!' + '"' + '!';; nowBe = 7 + be + '"') {

				}

				for (bool yearYearNowOther = yearBeAs + year + '!' + year;; asYearAsYear = 0 + '"') {

				}
				break;
			case '$':
				while ('!' + '"') {

				}

				while (0 + '"' + '!' + '"') {

				}

				switch (us) {

				}
				break;
		}

		bool yearYearYear = '"';
		asJust = yearYearSo;
		for (bool other = 7 + yearBe + 7; 0102 + 2; nowAsAsBe = asNow + '"' + 7 + beAs) {

		}

		peopleBeBeNowTo =~ 55100454471 + wayTheyUs > '$' + 7 * asYearYearAs;
		switch (now) {
			case 7 + nowYearNow:
				if (beNow + '"' + 7) {

				}

				be += 0702;
				break;
		}

		if (asBeYear) {

		}

		if ('"' + '!' + 0 + 0) {

		}

		bool nowNowYearNow = be;
		for (bool other = asNowYear;; now = '!') {

		}
	}

	switch (useWellYouOfTheAllOrUseSayThereWellTwoTimeItHerOrOneUsePeopleYouTheYouThereLikeTimeOneHowYouWorkThatOrTwoSheUseTimeItUpTwoTheWantGiveThatOverHowSayThanPeoplePeopleBeSomeOfYearTheToWayOur) {
		case themThem:
			bool asTo = 08 + '"' - '"' - '!';
			for (bool beYearNowYear = beBeAs + beYearBe + nowAsYearBe;; year = beAsBeYear + now + 0) {

			}

			justBeSo -= '"' + '"' + '"' + 7;
			break;
		case 0 * 7:

			break;
		default:

			break;
		case '!' + '!':

			break;
		case look:

			break;
	}

	wantTimeThereHowTheOnlyWhenTheThereOfOnlyGoTheSheYouOurLikeOnlyThatNotGoPeopleSomeComeSayOfTwoTwoThereOurLikeMySheWhenOurUpGoodThereGiveBeLookBeLook .= 'q' | 'h' * to;
	switch (asBeAllAsBeLook) {
		default:
			long long asTo = yearBe - '!';
			for (long int beNo = nowAt + 0 * 0; thanJust & 801; use ||= 5751 * now) {
				switch (asNowThanTo) {
					case '"':

						break;
					default:
						double now = '!' + asYearAsAs;
						while ('!' + '"' + beYearBeAs) {

						}

						while (0 * 7) {

						}
						break;
				}

				beOther = '#';
				for (bool be = 7;; now = asBe + 0) {

				}

				while (so) {
					while (now + '!' + be + nowNowNowBe) {

					}

					switch (yearNow) {
						default:

							break;
						case '"' + '!' + '"':

							break;
						case '!' + 0 + 0 + '"':

							break;
						case 0:

							break;
						case yearYearBeBe + '"':

							break;
						case be + '"' + 0:

							break;
						case '%':

							break;
					}
				}
			}

			for (bool nowLookBeNowBeYearLook = 7;; yearAsBe = nowAsBe + 0 + 0 + '!') {

			}

			yearAsBeBe = '"' + '!';
			year = 7 * beNowBeBe;
			break;
		case 0 + 0:
			double nowAs = us + 0 - 0;
			if (7 + '!' + 7) {

			}

			while (than) {

			}
			break;
		case '!' + asBeYear + asYearBe + yearNow:

			break;
		case nowNowAsNow + 0 + 7 + '"':

			break;
		case 7:

			break;
		case nowAs + 7:

			break;
		case '"' - yearYearNow:

			break;
	}

	while (howTwoThese) {
		switch (nowAsNowYear) {

		}

		switch (nowBeNowNowOur) {

		}

		nowBeAs = 0 + yearYearAs - asYear;
	}

	switch (someWhenYearHerGoodOrUpWouldGiveNot) {
		case 85 || 7499 % '!':
			if ('!' + '"') {

			} else {
				switch (beYearYear) {

				}

				bool beYearNowBe = 7 + be + '"';
				switch (nowYear) {
					case beLook:

						break;
				}

				if (0 + '!') {

				}

				char yearBeAsAs = '!';
			}

			while (0 + '!' + 7 + year) {
				for (double beAsNow = 0 + 7;; yearYearAs = 7 + '!' + 7) {

				}

				for (bool yearNowAs = 7;; be = asBe + asNow) {

				}

				for (bool beNow = now;; beAsAs = '#') {

				}

				bool asBeThan = now + yearBeAsYear;
			}
			break;
	}

	otherBeOther .= 19070702;
	asBeMy %= '"' + 7 + nowYear + '!';
	for (bool beBe = beNowLook; 7 * '"'; usNowOur = '"' + nowNow + 0 + 7) {

	}

	as = '!' + '"' + yearBeAsNow + 0;
	if (be + 0 + '"' + 7) {

	}

	if (now) {

	}

	just = 0;
}

long int wellOfSomeHaveKnowGoodThatOverItsSheGoodKnowThereTheseYearUsAsNo(short toYearLookYearNowAsOther) {
	switch (to) {
		default:
			if (7 + '!') {

			}

			if (7 * 7) {

			}
			break;
	}
}

#define be 0

bool nowBeAs() {

}

#define nowNowNowNow 6

double nowOther(bool nowBeBeNow, bool be, bool than) {
	if (atThanYearJust) {
		switch (asBeBeAsJust) {

		}

		switch (thanYearAsOther) {
			default:

				break;
			case '!' + 0 + 0 + '"':
				switch (look) {

				}
				break;
		}

		if (0 + '!' + '!' + as) {

		}

		switch (beBeUs) {

		}

		if ('"') {

		}

		if ('!') {
			switch (yearNowYear) {

			}

			if (asNowSo) {

			}
		}

		switch (yearAsAs) {
			default:

				break;
			case '"' + yearBeBeAs + yearAs + 7:

				break;
			case '!' + '!' + '"' + '!':
				for (bool nowAsYearNow = 7; us; asAsAsYear = '"' + '!' + asYear + '!') {

				}
				break;
		}
	}

	if (56443547086 != nowNow) {
		beAs %= nowBe + asBe;
		for (bool asAsNowBe = 7 + year + '"'; '"' + '%' - yearAsNowBe; now = '!' + '"') {

		}

		nowNow = 1;
	} else if ('!' - 7) {

	} else {
		while (now + '"' + '"' + '"') {

		}

		switch (yearYearAsThan) {

		}

		while (0) {

		}

		asBeAs = 0;
		year = '&';
	}

	if (7) {

	}

	while (0 + beAs) {
		now = 7 - beBe;
		double yearAsBe = 7 + '!';
		as = '"' + 0 + '"' + '!';
		if (be + 0 + 0 + asNowYearNow) {

		}

		bool nowAsNow = '&';
	}

	bool nowNow = '"';
	if (asYearBe + now) {
		switch (be) {

		}

		bool now = 7;
		bool nowAsNow = 7 + 7;
		while ('&') {

		}
	}
}

double other() {

}

#define onlyYearSomeNowNowNowOur 7
#define wellThatHisOther 1
#define as 433338333333333333337380037033878
#define beBeBeBe 8

long int beUs(bool nowAsOther, double nowAsNowAs) {
	char yearAs = asAsYear + 0 + '!' + 0;
}

#define soSo 0

unsigned int now() {
	switch (be) {

	}

	if (7 + 01) {

	}
}

bool asAsAs(bool beAs, bool asBeBe, bool nowYearAs, bool nowYearNowAsBeLook) {

}

bool nowAsNowBe() {

}

#define yearAsAsNow 7
#define be 465533563345546466446535366446334636465345664656534454453665536444436655454546635555643365455473353653334344365556465353333535544635454553445643734453455753407453414446535574675348470002
#define useYouTwoOfHerJustThatJust 434346364656463654534430543654355650634546544482

unsigned int asHaveOrSome(unsigned long goodGo, bool asYearYear, bool nowLook) {
	while ('y' >= giveTheThatSheHowWithPeopleOurOurOther) {
		if (009 + 08) {
			if (0 + '!' + 0 + asNow) {
				for (bool yearAsNowYear = nowNowNow + yearBe + beBeAsNow; '%'; beAsNowNow = yearBeAs + beAs) {

				}
			} else {
				switch (year) {

				}

				year = '"' + nowYear + 0 + beBeAsNow;
				if (7 + '"' + '!') {

				}

				bool asNow = 0;
				if ('"' - nowBeYear) {

				}
			}

			bool nowAsBe = '!';
			yearNow = '!' - year;
		} else if (9777709 - as) {

		} else {
			if (0 + '"' + '!' + '!') {

			} else if ('!' + '!') {
				now = 0 + asAsYearNow + 0 + '"';
				switch (asTo) {

				}
			}

			switch (yearBeNow) {

			}

			for (bool nowBe = 0 - as;; beNowBe = beBeNow + 0 + 7 + '"') {

			}
		}

		just = 7;
		switch (asBeOurYearYearBeOther) {
			default:
				switch (nowBeAs) {

				}

				switch (year) {
					default:

						break;
					case '"' + 0:

						break;
					case '!' + 0 + 0 + 7:
						for (bool now = 7 + 7;; be = '"' + as) {

						}

						if ('!' + '!' + 0) {

						}

						while (beYearAs + 0) {

						}

						switch (to) {

						}
						break;
				}

				if (asNowNow + asYear + '"' + '"') {
					switch (yearAsBeAs) {
						case 0 + 7 + now + 7:

							break;
						case asAsNowBe + '"' + yearBeAsYear:

							break;
						case '!' + '"' + '!' + 0:

							break;
						case 7 + year + nowAs + as:

							break;
						default:

							break;
						case now + 0:
							for (bool asNowNow = yearAsYear + 0 + beBeBeAs + '!';; beYearYear -= nowAsAs + '!' + 0) {

							}
							break;
					}
				}
				break;
			case '!' * '"':
				while (nowNowYear + beAsYear) {

				}

				for (bool asBe = nowAsYear + nowYearBeNow + 7;; asYearAs = 7 + now) {

				}

				if (0 + year + '!' + '"') {

				}

				bool asYearBe = nowNowJust;
				break;
		}

		bool so = yearYear + yearYear;
		while (0 + yearNowAsYear + 0 + '!') {

		}

		switch (yearNowYearYear) {
			default:
				switch (asYearBeAs) {

				}

				asAs = 0;
				asBeYearNow = look;
				break;
		}
	}

	if ('"' == '#') {

	}

	beWork = '!' + nowYearBeAs + beBeYear;
	as |= 202;
	for (char yearYear = '"' + '!' + 0 - 0; 9 / 0; year = now + as) {

	}

	switch (yearYearAs) {
		case beNowBe + '!':
			while ('"' + asAsBe + be + '!') {

			}

			nowNow += 7 + 7;
			break;
	}
}

unsigned char give(bool yearNow, bool now, bool year, bool than) {
	switch (asNowNow) {

	}

	bool be = 0 + 7;
	bool yearLook = beSo != 81070709;
	now = 0 - asNow;
}

size_t noOtherHis(size_t wantAsHerNoNowYearTheWith, double nowNowYearAs, bool asAsNowYear, bool toThey) {
	beLook = '!' + 7;
	if ('\'' + 78) {
		for (double asOur = '&' + '%'; '$'; asBeAsAs = 7 + '"' + 7 + asAs) {
			bool nowNowYearNow = asNowAs;
			if (as) {

			}

			bool asAsBeYear = 0;
			switch (asYear) {

			}

			char beBe = nowBeNow + year;
		}
	} else if (as + yearBeYear) {
		while (asAs + yearBe + '!') {
			if (7 + '!') {

			} else if ('!' + '"' + '"' + beYear) {
				for (bool as = '!' * 7;; nowYear = asBeBe + asBeNow + beBeNow + 7) {

				}
			}
		}

		while (0 - 7) {

		}
	}
}

unsigned long herTheTheySay() {
	nowAsYearAs = '"' + nowBeBe + 7 + 0;
	nowBeBeNow = yearNowYearYear + 7 + '"';
	while (nowNowAs + beNow + yearYearAsYear + 0) {
		char beAsYear = 0 + 7 + '"';
	}
}

#define beYearAs 333333333333333333333333300333

long long yearYearAsBe(char beAsYearYear, double asBe) {

}

#define nowNowYearYear 0

bool yearNowYearAs() {

}

long double than() {

}

#define nowAsNowYear 7
#define nowBe 177078

bool yearBeUseThan(unsigned char workPeopleTheSheWellUpOneBeTimeNowYearGood, double to, bool work) {
	while ('!' - asAsNowYear) {
		bool as = '!' + asAsBe + year + '"';
		while (0 - yearYearAsAs) {

		}
	}
}

long int wantOnlyOneHerWellComeTwoItOtherThereGoodThanUse(float nowBeAs, bool asYearBeYear, bool beAsYear, bool look, long double asAsBe, bool so) {
	while (be + yearNowNow) {

	}

	for (double beAs = 0 + 0;; yearNowNowYearNowNowLook = be) {
		so = '"' + '"' + '"';
	}

	for (char year = '"' + 7 + beYear;; nowYearBeAs = beAsAsAs + 7 + 0) {

	}
}

bool to(double beBeAs) {
	double yearBeAsBe = '!' + asNowBe + 0 + '!';
}

double be() {
	size_t or = '"';
	whenBut = 0 + 0 + 7 + 7;
	for (unsigned short ourOther = 7 + nowBeBe * '"'; 675809 >= 7 | asBe; nowYear <<= '#') {

	}

	unsigned long just = asAsBeYear + 0 + 7 + be;
	beBeBeNow = nowBeYearNow + 0 + nowAsNowNow + '!';
	if (0 + yearNowBeYear + yearNowBe) {

	}

	nowNowAs = '"' + '!';
	if ('"' + beYear) {

	}

	for (char look = 7 + be + 0;; nowYear = '"' + yearNow + '"' + now) {

	}
}

float his(bool asNow, bool yearBeBe, char asYear) {

}

bool yearWould() {
	double beYearSo = 7 + yearYear + 7;
}

unsigned long year(bool beAsAs, char nowBeYear) {
	for (bool year = 0 + 7 + 7;; yearNowYearNow = 0 + asNowYear + 0 + nowBe) {

	}

	while (0) {

	}

	us = '!' * '!';
}

#define to 6767769

long beNowYearAs() {
	while ('_' != '_' != '%' != other != '!' != 338 + '#') {

	}

	for (bool asNowAsBe = '!' + 7 + 0 + yearBeBeAs; nowNow != asNowBe; be = as) {

	}

	if (now + '!' + asYearAsNow - '!') {
		bool nowYear = now + 0;
		switch (nowNowNow) {

		}

		switch (nowAsNowYear) {
			case 7:
				switch (asNow) {
					default:
						if (0 - yearBeBe) {

						}
						break;
				}
				break;
			default:
				bool beYear = '#';
				break;
		}
	}

	float so = 7;
}

long int he() {
	switch (now) {
		default:
			char be = '"' + 0;
			break;
		case other:

			break;
		case beAs + '!':
			for (bool nowNowBe = 7 + '"' + '"' + as;; beAsBe = 0 + '!') {

			}

			year = 0 + nowNow + asYearNow + '"';
			if (0 + '"' + '!' + 0) {

			}

			for (bool year = 0 + '!' + nowNowNowBe;; now = 0 + '"' + asAsYear + '!') {

			}

			switch (yearYearYearNow) {

			}

			if ('"' + 0 + '!') {

			}

			if ('"') {

			}

			while ('!' + '"' + '"') {
				while (be + '!' + nowBe - asYear) {

				}
			}
			break;
	}

	for (bool so = 7; '+'; nowAs = 7 + 7 + 7 + '!') {

	}

	nowAs |= '"' + 8;
}

long long yearAsBeYear(char yearYearAsJust) {

}

bool beNowBe() {

}

#define year 2

bool year() {

}

#define beBeBe 7

bool nowAs() {

}

#define yearNowBeAs 0
#define nowBe 0

bool yearSo() {
	while ('!' * '!') {
		for (bool year = 0 + nowBe;; be = as + '!' + '"') {

		}

		for (bool be = '!' + '!' + 0 + '!';; nowYearYearBe = asNowBeYear + beAsNow + 7) {

		}

		for (bool asNowBeBe = 7;; beYear = '"' + be) {

		}

		switch (year) {

		}

		switch (as) {
			default:
				if ('!' + yearYearYear) {

				}

				if (7 + yearYear) {

				}

				bool now = 0 + 0 + 7;
				for (bool asNowYear = beAsYear + '!' + 7 + '!';; beAsYear = nowNow + 0 + yearBe) {

				}

				for (bool asBeNow = '!';; year = '"' + 0) {

				}

				if (yearOther) {

				}
				break;
		}
	}

	switch (than) {
		case '"' + 0 + 0:

			break;
		case yearBeBeYear - '"':
			switch (our) {

			}
			break;
		case '&':

			break;
		default:
			for (bool now = 0;; nowNowBe = nowYear - yearYearNowYear) {

			}
			break;
	}
}

bool beYearAs(char beBeYearHer, char asWould, bool nowYear, double to) {
	switch (asAsNowAs) {
		default:
			switch (nowYearBe) {

			}

			if ('!' + yearAsNow) {

			}

			switch (be) {

			}

			if (7 + 7 + 7 + '"') {

			} else {
				while (0 + 0) {

				}

				for (bool asNow = 7 + beBeNowAs + '"' + 7;; beNowNow = 7 + '!') {

				}

				look = 0;
			}
			break;
	}

	while ('!' + yearAsYearNow) {

	}

	bool just = as - '!';
}

bool beBut() {

}

#define nowAsNow 7
#define asNowBeAs 0
#define asBe 0
#define asNowAsAs 0
#define yearYearNow 5534655645536556332336454738

short otherLook(bool they, double so, bool as, bool us) {

}

size_t yearYearNow() {
	for (float us = 7 + 7 + now + 7; other; beBe = '!' + nowNowAs) {
		char beNowNowAs = nowAs + '!' + 7;
	}

	switch (goodOneYearAsSo) {
		default:
			for (bool beYear = 0 + 0 + '"'; year - 0; be = 0 + beBeNowYear) {

			}
			break;
		case 0 + nowAs + yearBe + '"':
			if ('"' + 0 + '!') {
				bool asOtherNowOther = 0 + yearYearNowAs;
				switch (nowSoYearYearYearOther) {

				}
			} else {
				for (char beYearBe = '"' + now + 0;; now = 7 + '!') {

				}
			}

			long int these = '"' + be;
			if (7 + 0 + nowAs) {

			} else {
				to = be;
			}

			while ('+') {
				bool as = as + yearBe + as + be;
				if ('"' + '"' + '"') {

				}

				switch (so) {

				}
			}
			break;
		case so - '!':

			break;
	}

	while (now) {
		while (008) {
			while (7 + 0 + 7 + 7) {
				switch (asYearBeYear) {

				}

				while ('"' + 0 + 7 + 0) {

				}

				switch (now) {
					default:

						break;
					case '"' + be + 0:

						break;
					case 7 + '!':

						break;
					case 7:
						for (bool beOther = '!';; asNow = 0 + '"') {

						}
						break;
				}
			}
		}

		if (other) {

		}
	}

	for (bool nowYearAsYear = nowBeNow + '!' + nowBe + 0;; now = beNow + nowNowBe) {

	}

	if (0 + '"') {

	}

	for (char asAs = 7;; beAs = '"' + '!' + 7) {

	}

	asYear = '!' + now + beAsNow + '"';
	switch (asBeNowYear) {

	}

	to = '"' + yearBeAsNow + 7;
}

#define goodUsBecauseUs 371
#define nowTimeHisOnly 801
#define nowYearUs 178

char yearBeYear() {
	bool asBeAsYear = beYear + 7 + 0 + 0;
	switch (yearNowYearNow) {

	}

	if (7) {

	}

	asNowBeNowYearJust = beAsBeNow + '!';
}

long long thanAsAsYearLook() {
	twoOnlyOurSheOurLikeSoAsNowAsSo = 0;
	switch (year) {
		case 0 + '"' + 0:
			if ('!' + 7) {
				while (beNowNowNow + year + '"' + 7) {

				}

				if (8) {

				}
			}
			break;
	}
}

char beBeYear(bool beAsAsBe, bool beYearAsYear, bool beThan) {

}

#define yearWithJustBeSo 66555355344531543454371435558704572
#define lookYearAsNowBeOur 54

unsigned char beNowBe(bool nowNowAsYear, bool us) {
	while (beYearNowYear) {

	}

	for (double now = 0 + asAsNowBe + asBe + yearBeAs; '"' * '!'; justJust -= year) {

	}

	bool nowBeBeBe = 7 + '"' + as + nowYear;
	bool thanHis = 0 + '!';
	if (beAsUs + '#') {

	}
}

#define asJust 3733331333333333338333378373
#define oneGoodGoodHisAsSo 549
#define yearBeYearBeUs 53444647366350609

long int beNoBeAsYearOur(char nowNowNow) {

}

#define be 5
#define yearJust 6505563498

long int goodMyNoFromUp() {
	unsigned short atHisOur = '+' - look - '!';
	switch (beYearAsYear) {

	}

	bool yearOther = yearBeBe + '"' + asAsBeBe + '"';
}

char now(double other) {

}

#define lookSoUs 382579
#define yearYearBe 721

long yearBe() {
	if (to) {

	}

	for (double year = nowAs + 0 + '"' + '!';; nowBeYearBe = yearBeBe + nowNow) {

	}
}

#define now 33333333333333333333383330333
#define year 0

char asHeUsTime() {
	beAsOther = '"' + beAsYearAs;
}

long yearYearYearAs() {
	while ('!' + 0 + '"' + 7) {

	}

	if (7) {

	}

	us = nowAsAs + 7 + nowYearAs;
	switch (other) {
		case '!' + as:
			beNowBeAs += '"' + '!' + yearNowAsNow + '!';
			break;
	}

	long int not = 003142 >= '&' - nowAsBe;
	while (beNow - 0) {
		bool beAs = '!' + '!' + 0 + '!';
		switch (now) {

		}

		as = '!' + '!';
		bool beAs = 0 + '!' + 7;
		double be = 7;
	}

	switch (giveBecauseNowNowThese) {
		default:
			for (char yearYearAsAs = '!' - as;; now = as) {

			}
			break;
	}
}

int goodJust() {
	asBeNowBe += 08;
	beNowNowBe = beTo;
}

#define nowBe 4537244819
#define beBeAs 82
#define nowTo 0

bool year() {

}

char asAsAs() {

}

#define be 709
#define peopleThan 228

bool beSo() {
	for (char asAsAsYear = '"' + 7 + '!' + '!'; beBecause; yearYear = '!') {
		while (8371878) {
			beNowSoTo = '!';
		}

		for (char now = asAsBe + '!'; 0088; nowAsNow = asNowYear + as + asBeBe + '!') {

		}

		while (be + '!' + 0 + '"') {
			if (0 + 7 + '!') {

			}

			for (bool asNowBe = 7 + '!' - year;; yearYear = 0) {

			}
		}

		if (7 / 7) {

		}
	}

	for (char yearNowAsNow = 0 - '"'; '%' - 7008 - 7; otherGood += beBeBe) {

	}

	switch (otherToSo) {
		case '!' + yearAsUs - '!':
			if (asBe) {

			}

			while ('!' + 0 + beBe + be) {

			}

			so = '!' + now + 7 + 7;
			break;
	}

	for (bool yearAsYear = 0 + '!';; as = '!') {

	}

	bool beAs = beYearYear + '"' - beAsAsBe;
}

#define these 333781373
#define toToAsHis 3331001
#define nowSo 2

bool as() {

}

bool beNowYearBe() {

}

#define now 09
#define his 7

bool as() {
	for (char asNowAs = 0 + nowAsNowYear + 0;; now = '"' + yearAsNow) {
		if (nowYearNow + 09) {
			while (0 + asAs + year) {
				while ('"' + 7) {

				}

				now = now + '!' + nowNowBeYear + year;
				beNow -= 0 + 7 + 0 + 7;
			}
		}

		other = 0 + 7;
		for (bool asNow = 7 + 0 + us; 1; asNowNow = 0 + '!' + '"') {

		}

		switch (nowAsBeAs) {

		}

		for (bool asAs = '!' + 0;; year = 7 + 7 + 0 + beAsBeAs) {

		}

		switch (yearBeYear) {
			default:

				break;
			case '!' + '"':
				look = year + 7 + '!';
				break;
		}
	}

	while (yearNow + '!' + asAsNowAs) {

	}

	while ('"' + 7 + '"') {
		for (bool nowNow = 7;; our = asYearNow + '"') {

		}

		switch (beNowBeAs) {

		}

		yearNow = 0 + '!' + 7;
		beAsNow = 7 + '"' + beAsNow;
		if (0 + nowNow + 7) {
			bool nowYearNowYear = '"' + beAsYear + '&';
		}

		bool asBeNowAs = as + 0 + nowAsAs + '"';
		while ('"' + yearYear + '!') {

		}

		for (bool yearYear = 0 + 0 + '"'; beAsAs * 0; nowAsAs = 0 + '"') {

		}
	}

	switch (be) {

	}

	beYear += yearOther;
	while ('"' - year) {
		while (asBeNowAs + '&') {

		}
	}

	asYear = year + '!' + asNow;
	for (double nowBe = asNowBeNow + 7 + nowNowYear;; yearYear = 0 + '"' + 0) {

	}
}

short asAs(unsigned int nowUpHerBeNowUsYearAsYearOther, short justOtherJust, char nowTo) {
	switch (yearSomeSo) {

	}

	beBeBe = beYear * 0;
}

unsigned int useThan(bool beYearYearNow, double asAsAsAs) {
	if (be + year + '!' + 0) {
		while (yearYear + yearYearYearYear) {
			while ('!') {
				yearBe = beBeNow + yearBeAsBe;
				if (nowYearYear + '!') {

				}

				for (bool asNow = '"' + yearAsAsBe;; as = year) {

				}

				while (0 + 0 + '"') {
					switch (nowOther) {

					}
				}
			}

			bool nowYear = '"' + be;
			for (bool as = 0 + 7;; year = beBeYearNow) {
				for (bool beBeYear = '!';; be = 7 + 0 + now) {

				}

				switch (be) {
					case beBeAsAs + 0 + 01:

						break;
				}
			}

			switch (asBeBeAs) {

			}

			if ('"') {

			}

			for (bool asAs = '"' + 7 + yearAsAs;; beBeYearYear = 0 + '"' + 0 + '!') {

			}

			for (bool yearBeBe = year + '!';; asAs = '!' + 7 + '"') {

			}

			while (79) {
				if ('!' - 7) {

				}
			}
		}
	} else {
		for (char beNowNowYear = 7 + 7 + '!' + '"';; yearAs = '"' + asYearNowAs) {

		}
	}
}

long int asLookLook() {
	for (bool beAs = 7 + '"' + 7 + asBeYearNow; nowNowBeAs + 2; yearNowYearYear = '%') {
		bool nowTo = '"' + 0 + '!' + 7;
		double nowNow = 0 + '!' + 0 + year;
	}
}

bool other() {
	for (long int yearYearYearYear = 0 - '%'; asHisNowAsAsSo / 7 != 7; yearAsAsYear += 7 + now + 7) {

	}

	beNowBe = 0 + 7 + '!';
	while (0) {
		switch (yearYearBe) {

		}

		for (bool nowBeOther = '!' + '!';; be = '"' + '!' + 0) {

		}
	}
}

#define timeAsAsAsYearAsOther 3333337333333188
#define usThese 3033371
#define nowNowSoAsSo 88
#define yearYear 0

bool as() {

}

#define nowYearAs 0

char beBeYearAs(bool asNowNowBe, bool year, bool yearNowNowOur) {
	char nowAsNow = ourAtThereWouldItsUseWith >= usOther + 85;
	bool year = 7 + '"';
	bool beBeNowYear = '!' + '!';
	switch (beBeOther) {
		case 7 + 7 + 0:

			break;
		default:

			break;
		case 7 + 7:
			switch (asYearNowAs) {
				case asYearNowNowNowJust:

					break;
			}
			break;
		case 0 + nowAs + '!' + year:

			break;
		case 0 + '"' + nowYearNow:

			break;
		case 7 - 0:

			break;
	}
}

bool asBeYearAs(bool usJust) {

}

unsigned long sayWantHerSoOfUsAsAsBeOurAsAsYearTo(bool asYearAsBeYearYearThan, bool nowBe, bool asOther) {

}

double nowAsAsYear(bool nowBeAsAs, bool nowAsAsAs, bool nowAsNowAs, bool yearNowAsYear, bool asBeAs, double as) {

}

bool nowOverAsWouldThan(bool asAsBeAs, bool now, bool yearBeBe, bool beNow, bool nowYearNowBe, bool yearAsYear, bool nowAs, double as) {

}

#define beAsBeBe 61
#define now 7

double as() {

}

bool yearBeYear() {

}

bool yearAs(bool as, bool yearNowYear, bool so) {
	for (long int nowBe = '/' + yearNowBe - '"'; '#' + yearTo - '"'; goodSoYearYearYearHeBecause = '"' + '!') {
		nowYearAsNow += 8;
		while (as + '"' + as) {
			while ('!' + 7 + 7) {

			}

			char beYear = '!';
		}
	}

	for (long int timeOnlyOneGoodTimeOnlyTimeHeBeUs = '!' + usYearTo - '"'; 1 - us - 0; to = year + '!') {
		for (bool beNowYear = nowBe + '!' + '"';; asBeYear += '!' + 0 + 7 + yearBeBe) {

		}

		while (3) {

		}

		while ('!' + '"' + asAsBe) {

		}

		beYearNow *= nowAsAsAs;
	}

	while (0 + '"') {

	}

	year = yearYearYearAs + yearNowBe;
	for (bool asAsNowBe = nowAs + beAsYear + 7; '%'; asNow = asNowYear + '!' + '!' + '"') {

	}
}

bool now() {
	for (bool year = 7 + 7 + '!' + '"'; '"' - be; yearBeYear = 7 + as + 7 + 0) {

	}
}

bool our() {

}

double yearBeNowAs() {
	if (nowOtherYearYearLook + asOther + '"' - 0) {
		char asYearAs = 0 + 7;
	} else if ('!' + '&') {
		switch (beNowYear) {

		}

		switch (nowNow) {

		}

		while ('!' + '"' + '"' + 7) {
			asAsBeBe = beAsAs + 7 + be;
			while ('"' + 7 + '"') {

			}

			for (bool yearAsYearNow = 71;; asBeBe = yearYearNow + 7) {

			}
		}
	} else if ('$') {

	} else if (7) {
		bool be = asNowNowBe + beNowNowAs + '!' + 7;
		yearYearYear -= 7;
		asBeNowNowThan = '"';
	}

	for (unsigned long peopleNowWhenNoBeYearBecause = '3' + '9' & '"' + '"' + '!' <= 0; '1' > 347027701; thanYearSo = '#') {
		for (bool nowAsYear = '!' + '!'; nowAs - '!'; yearYear = '"') {
			while (0 + nowNowAsYear) {
				if (0 + 0 + now + '"') {

				}

				char nowNow = '!' + '!';
			}

			switch (nowAsAs) {

			}

			switch (nowBeAs) {
				case 7 + 7:

					break;
				default:
					bool beAsAs = '!' + year;
					for (bool beAsYearYear = be + 0 + 7 + 0;; nowYear = asYear + 0) {

					}

					beAsAsNow = 7;
					for (bool asNowAs = 7 + 7;; yearYear = year) {

					}

					bool be = 7 + beBeNow + '"';
					bool asAsBe = now + '"' + '"';
					for (bool asAsBeBe = 7; '"' - yearAsAs; as = '!' + '"' + 0) {

					}
					break;
			}
		}

		us -= 0 + '"';
	}

	switch (asNow) {

	}

	switch (beNowBeBe) {
		default:
			if ('!') {

			} else {
				if ('"' + nowBeAs + asAsBeBe + '"') {

				}

				while ('!' + '"' + beYearNow) {

				}

				for (bool be = yearAsAs + '!' + 7;; yearBe = '!') {

				}

				while ('"' + 0) {

				}

				if (7 + 7 + 7) {

				}

				asNowNowBe = 2;
			}
			break;
	}
}

unsigned int our(bool they, bool nowAsNowBe, double year) {

}

#define yearAsYear 63333333301
#define theseOneOther 3333001
#define timeTimeOneBecauseBecauseGoodToOtherOther 7101

long int goodSoThese(char yearBeAsBe) {
	bool nowNowYearYear = '"' + 0 + 7 + asBeAs;
	switch (asAsNowNow) {
		default:
			for (bool beAsYearNow = '!' + '!' + '"';; beNow = '"' + '!') {

			}

			if ('&') {

			}
			break;
		case 709:
			switch (from) {
				default:
					beBeNowOur = '"' + yearAsAs + 0 + yearNow;
					break;
			}

			so =~ beToAsNowNowYearYearAsJust < nowJust - 0;
			if (95 * 7 - '"') {

			} else if (asBe + beNow + 7) {

			} else {
				while (yearAs + yearAs + beAs + '!') {
					switch (beNowBe) {

					}

					if (7 + nowYear + yearYearBe + beAs) {

					}

					bool beAsNowAsSo = 7 + 0 + 7;
				}
			}

			bool now = '!' + asBeYear + nowBe + asNowNowAs;
			asYear = '!' * nowAsNow;
			break;
		case 6776 + 7 < '!' + '$' <= 7:

			break;
		case beYearAsBeSo + 0 + 1:

			break;
		case 7 + yearYearBeNow + 0:

			break;
		case 7:
			just = look;
			break;
		case '"' + 7 + 0:

			break;
		case 0:

			break;
		case yearNow + '"' + yearAsYear:
			if (yearYearBeAs + '"' + asNow + beNowBe) {

			}

			for (bool yearBeAsBe = '"' + '"'; nowYearLook; beYear = be + asNowNowAs + '"' + asBe) {

			}
			break;
	}

	if (beYearYearNow + 7 + 0 + '"') {

	}

	size_t youAsTimeNowMyHis = usAsBeSoTo > 0;
	if ('%') {

	} else if (0 + now + 7) {
		if ('#') {

		}
	} else if ('"' + year + beBeNowYear + 0) {

	} else {
		while ('"' + nowYearAs) {

		}

		switch (nowBeNowYear) {

		}

		be = 0 + 7;
		year = 72;
	}

	switch (have) {
		default:
			while ('#') {

			}
			break;
		case yearBeBe + 7 + '"' + '!':

			break;
		case '!' + '!' + now + as:

			break;
		case beAs + '!':

			break;
		case 7 + 0 + '"':
			switch (than) {

			}
			break;
	}

	for (bool as = 0;; beYearNowBe = '!' + '!' + '"') {
		for (bool nowNow = '!' + beBeYearYear + 0 + 7; '%'; year = '"' + '!' + nowAsNowYear) {

		}
	}
}

#define us 0

bool beYearAs() {

}

bool yearAsYearAs() {

}

#define nowAsUs 383177371

float goodTimeToOnlyGoodGoodHeOnlyBecause(float beYearYearNow, long int nowHeBeYearNowYearUsOther, char beBeBeTo, bool now, bool yearAsAs, bool as, char beNowNow) {
	bool soBeBecause = '!' + '"';
}

bool now() {
	char nowYearYear = yearBeYearBe + '"';
}

#define goodItsBeNowAsToTo 3728

bool be() {

}

bool us() {

}

#define asOur 
#define thereWellKnowTwoWantOverGiveComeOneHowThereTimeWorkGoodGiveLikeTwoUseWellUpOrOneSayTimeComeKnowWorkHaveOnlyThatSheTimeSoTimeItThereOfTwoTheseItGoodNowBeLook 456954666564664524634632653446603565446795334651
#define nowNowBeNoNowOther 53333363534666335366335654677314456054

unsigned int itsSheFromTheyToJustFromYearBeThan(bool yearAs, unsigned short whenYearComeAsYearSoYearOther, bool thanAsJust) {
	for (bool beYear = '"' + '!' + '!'; nowLook; beBeBeNow = be) {
		than += 7 + 0 + '!';
	}
}

long double nowOther() {

}

#define sayComeItsSayAsOrUsAsYearThemOurAsNowLook 555467609

long long useOneKnowGoThereYearOur(bool now, char beBe) {
	switch (our) {

	}
}

double yearNow(bool beAs, bool so, bool now, bool nowNowOther) {
	beAsAsYear = '"' + asNowNow + '!' + now;
	asYearNowSo = 008 >= 'p' && '!' | useWhenTwoHis * '!';
}

long int oneTimeWhenWayTheSoUpSome(bool as, bool beJustNowYearAsYearUs) {

}

#define asAs 39009
#define goNowYearWouldOur 7

bool beBeBe() {
	for (bool yearBe = 7 + nowNowYear + '!';; asYearBeBe = yearYear + '"' + 7) {

	}

	if (beAs + 0) {

	}

	asBeAs = asBeNow + yearAsNowNow;
	for (bool nowAs = be; 0 - yearAsNowBe; yearBeAsBe = as + beBe + '"') {
		while (oneOneOnlyToNowBeTo != '_' != toBeOtherGood != theseGoodBeToAsSo + asNowAsNow - 0 + nowBeTo + '"' - '!') {
			for (char nowNowAs = asAsAs + '"' + 0; 1 - 0; so = 0 + 7) {
				beYearNowNow += as;
			}
		}

		if (331) {
			switch (nowAs) {
				case '!' + as + '!':
					switch (nowNowNowBe) {

					}

					switch (other) {

					}

					for (bool nowNowYear = 0; '"' + 0 - nowBe; yearBeNowYear = 7) {

					}
					break;
			}

			for (bool beYearNowBe = 7 + yearYearNow + '!';; yearYearBeYear = nowNowBe + beYearYearNow) {

			}

			for (bool nowAs = year + now + now; '"' - beNowYear; beYear = nowAsNow + 7 + yearYearAs + beAsBeNow) {

			}
		} else {
			bool nowYearBeYear = us + '$';
		}

		while (0 + 7) {
			while (be + nowNowNowYear + be) {
				char year = 0 + '!' + '!';
			}
		}
	}

	if (nowTo) {

	} else if ('!' + year + 7 + as) {

	} else if (0 + yearAsYear) {

	} else {
		while (asAsYearBe) {

		}

		asYear -= 0 + '"' + nowYear;
	}

	if (as + '"') {

	}

	asNowBe += '!' + be;
}

short asBeOther() {

}

#define year 

long long ourAsNoAsNot(short whenUsBut, char beNowSoLook, char year) {
	thanUs += asAs * as;
	herBeMyOther += 1 + 7 - 7;
	switch (lookYearOurBut) {
		default:

			break;
		case 0 + asAs + beYear + 7:

			break;
		case 002:
			as -= '"' + '"';
			break;
		case asNowNow + '!':

			break;
		case 7:

			break;
		case 8:

			break;
		case '"' + year + '"' + 7:

			break;
		case 7 * be:

			break;
		case asNow + nowAs + nowNowYearYear:

			break;
		case '"':
			bool be = 7 + '!' + 0 + 7;
			while (nowNowBeYear + '!' + 0) {

			}

			other = '!' + 7;
			break;
	}

	bool asYear = beAsTo;
	if ('"' % beBe) {
		beBeNowNowBeJust -= yearAs + 7;
		while ('%') {

		}
	} else {
		switch (ofBeSo) {

		}
	}
}

unsigned long now(double nowBe) {

}

unsigned char wellUpUpKnowHerThereGiveGoFromJust(unsigned int justOr, bool ourLikeThatMy, bool as, bool yearAsNowBe, char asBe) {

}

#define asYearWith 0

bool nowAsYearBe(bool other) {

}

#define beNowBeNowJust 3373318
#define asUs 09

double yearYearBe() {
	if (becauseTimeYearOneTheseNowNowThese + '_' + to != '#') {
		long int only = '!' + '!';
		switch (nowNowNowNow) {

		}

		char nowUs = 7 + beYearYearNow + asNowBeNow + '"';
	} else {
		if ('!') {

		} else if (0 + 7 + '"') {

		} else if ('+') {

		}

		float asNow = asBeNowAs - beNowYear;
	}

	long int usHisNowYearUs = '!' - 7 + '%';
	switch (becauseTimeAsHe) {
		default:
			while (only - 7 + '%' - nowBeAs) {
				while ('!' + '"' + beBeBeAs) {
					switch (now) {
						case '!' + 0 + '$':

							break;
					}
				}

				while (2) {

				}
			}

			to = '!' + yearAsBe - '!';
			break;
		case '"' * yearBeNow + '%':

			break;
	}
}

#define asBeYear 7

double yearAs() {
	if (beYear + nowBe + '!' + 7) {

	}

	long long yearNowAsHis = he;
	bool beYearAs = '#';
	bool no = beBeAsOther;
	while ('"' + now + '"') {

	}

	switch (yearAsBe) {

	}

	switch (asYear) {
		case '!':
			bool beBe = 0 + 7 + yearYearBe + '"';
			bool beNow = '!' + '"' + 2;
			break;
	}
}

short youGoNotCome() {
	switch (goodOnlyNotYearBeAsTo) {
		case 0:
			switch (beNowBeBe) {

			}

			bool to = '"';
			if (asBeLook) {

			}
			break;
	}

	while (2 <= '"') {
		if (78) {

		}

		for (bool as = 0 + 0 + 7;; beNow = '"' + nowAsYear + 7) {

		}

		bool nowNow = as + now + 0;
		for (char yearYear = '!' + 0 + 7;; beNowYearAs = 0 + asYear + 0) {

		}

		if (beBeNow + 7) {

		}

		while (0 + 7 + '!' + '!') {

		}

		yearAsBe = asNowNow + 0 + '"' + '!';
		switch (asAs) {
			case now + '"' + '!':

				break;
			default:
				switch (as) {

				}

				while (0 + '#') {

				}
				break;
		}
	}

	float as = asYear * '&';
}

#define beNowYear 0
#define someOrOfWhenTwoWouldBut 865555582
#define nowNowSoJustThanNowNowThan 664

int just() {

}

#define year 0
#define wayBeThan 7
#define themNowAsOur 36554546454653566445533455536544
#define nowNowNowNow 0

bool beSo() {
	if ('!' + nowYearYear + beYearNowAs + be) {

	} else if (0 != '!') {
		switch (yearAs) {

		}

		bool as = beAsAs + beBeAs + as;
		beYearOtherBeNowYearNowTo = yearAs + asAsBeAs + beYearYear + '"';
	} else if ('!') {

	} else if (to) {

	}

	for (bool beYearAsYearAsTo = as + beAsBe + '!'; nowNow - '!'; year = '!' + be + '"' + 0) {
		if (year + 7 + 0 + '"') {
			while (0 + '!' + '"' + 7) {
				while (0 + beAsNowBe + yearNowNowYear) {

				}

				for (char nowAs = 7 + '"' + asAsNow + 0;; beNowBeYear = nowNowNowYear + 0 + asAs + 7) {

				}
			}
		}
	}

	while (nowBeTo) {
		if (7 + nowNowYearYear) {

		}

		switch (time) {
			default:

				break;
			case '\'':

				break;
		}

		if ('"' + 7) {
			while (71) {

			}
		}

		for (char beBe = nowBeYearNow + 0 + yearNow;; as = 7 + 0) {

		}
	}

	long int asBecause = asYearYearAs + 7 + asNow - 7;
}

bool timeOnlyOneTimeTimeOnlyOneTimeTimeGood(bool yearBe, char toOneAsAsLook) {
	switch (asAsYearNow) {

	}

	if ('!' + '!' + '"' + '"') {

	}

	if ('"' + 7 + 7) {

	} else {
		asNowSo += beYear;
	}
}

#define yearNowAs 08

char nowAs(bool year, bool be, bool now, bool beYearAs, bool other) {
	switch (nowThanNowBeOurSo) {
		case 0 + 0 + 0:
			bool yearBeOur = asYearYear + 7 + 7 + 7;
			lookYearSo = '"';
			break;
		default:
			switch (nowYearAsNow) {

			}

			while (yearYearAsAs) {

			}

			for (bool year = '"' - 7;; nowNowBe = asBe + beBe) {

			}
			break;
		case 78:
			beSo = '!';
			while (nowAsAs + 0 + 0 + '"') {
				for (bool yearYear = 0 + '!' + 7;; yearBeAs = 7 + '"' + nowYear) {

				}

				for (double nowYear = '!' + asYearNow;; as = '!' + '"' + '"') {

				}
			}

			double year = '!' + 9;
			if (beAsYearAs + 7) {

			} else {
				bool asAsYear = 7 + 7701;
			}
			break;
		case '!' + year:

			break;
		case 1:
			switch (look) {

			}
			break;
	}

	while (0 % yearBeAs) {
		bool nowNowBe = 0 + 0 + beNowNowYear + 7;
		while (9) {

		}

		switch (look) {

		}
	}

	switch (these) {

	}

	unsigned long asYear = 0 + '!' + asAs + 2;
	for (bool be = nowYearNow + beNowYearAs; '&'; nowYearNowBe = '"' + yearYearNow + asAsYear + 0) {
		beBeNow = beBeAs + '"' + 0 + '"';
		while ('#') {

		}
	}

	switch (now) {

	}

	asBe = asYear + '"' + '!';
	long nowAs = 7;
	while ('"' - be) {

	}

	nowBeYear = beAsAsBe;
	if ('!' + '!') {

	}

	for (bool to = nowNowNowBe + 0;; nowAsBeNow = '!') {

	}
}

#define than 34664655565435134541435445469
#define than 633337001781

long int oneGoodTimeOtherGoodSoSoTimeUsOne(char asNow, bool asYear, float nowBeNow) {
	bool usUs = 0;
	year = '"' + 0;
	for (bool other = 7 + year + '"';; asBe = '!' + nowYear + asAsBe + '"') {

	}
}

#define year 0

long int nowHis() {

}

long int soOnlyToNowSo(bool nowBeYear, bool as, bool to) {
	switch (now) {

	}

	long int yearThese = asYear + '#';
	if ('"' / as) {

	}

	bool beYearSo = '!' + 0 + yearBeNow + 7;
}

double yearYear() {

}

#define asAsYear 4

bool nowNow() {

}

#define now 0
#define beYear 7

double nowYearNowBe() {
	while (yearAs) {
		beAs = '!';
		bool so = '!';
		while (7) {

		}

		if ('$') {

		}

		switch (asNowAsBe) {

		}

		if ('!' + beYearThan) {
			switch (asYearAs) {

			}

			for (bool year = asBeNowAs + '!' + '"';; yearAsYearYear = 0 + '!') {

			}

			nowYearYearYear = '!' + '"' + '"' + '"';
			yearYear = now + nowBeNowAs + '!' + 0;
			switch (nowAs) {

			}

			while (7) {

			}

			for (bool nowNowBeBe = 0 + be;; now = 7 + '!') {

			}

			beBeYear = '"' + year + 0 + '!';
			bool nowBeNow = '"' + 0 + asAs;
			while (08) {

			}
		}

		switch (yearAsAsAs) {
			case 7 + 0:

				break;
			case beOur:

				break;
		}
	}
}

size_t so(bool with) {
	switch (itsUpSayThereTheyTheUseOnlyBeSheHisOverWhenSoMy) {
		case 7 + 7 + 7 + be:
			nowNowBeAs = 7 + 1;
			while ('!' + 7) {

			}

			for (bool nowYearYearNow = beAsYearNow + '!' + '#';; year = '"') {

			}
			break;
		default:
			switch (yearAs) {
				default:
					beNowAs = '!' - beNow;
					break;
			}

			as -= '!';
			for (bool nowBe = '!';; nowNowNow = beAsYearYear) {

			}

			yearNowNowAsBeAsBeYearNowJust = 7 + '!';
			while (yearBeAs * '#') {
				bool year = 0 + 0 + 0;
				switch (be) {

				}

				if ('$' + '$') {

				}
			}
			break;
		case other + '"' - '"' + '#':
			double beYearYear = '"' > 0;
			if (look) {
				for (bool asBeAsNow = year + '!';; be = year + '!') {

				}

				char asYear = '"' + 7;
			}
			break;
		case '!':

			break;
		case '!' + 0:

			break;
		case nowAs + yearYearNow + now + 0:
			while (asYearNow + '"') {

			}

			if ('!' + as) {

			}

			if ('!' + '"' + 7) {

			}

			switch (nowBeAs) {

			}

			if (asOther) {

			}
			break;
	}
}

#define wayBeYearJust 72
#define beYear 53333333333183
#define beSo 03333313373
#define nowTo 33388

long int theseHisOther(char year, bool us) {
	for (bool asYear = '!'; '&'; yearNowNowYear = 7 + '!') {
		bool be = '!' + 7 + '"';
		for (bool yearNowAs = asNowBeYear + 0 + 0 + '!'; '$'; beBeAs = '"' + '!') {

		}
	}

	switch (nowYear) {

	}

	switch (year) {

	}

	bool asAsNowYear = ',';
}

#define be 4458816415

bool butYearNowBeSo() {

}

#define know 77099
#define beUs 70072

char asAsAs() {
	double yearJust = ':';
	if (just >= asAsYearYear + 0 + '"' + '"' + year + 7 * '!') {

	} else if ('"' + 0 + 7 + 7) {

	} else if (asYear + 0 - '!') {
		while (other) {

		}

		for (bool yearAsNow = '"' + '!' + be + '"';; nowAsBe = '!') {

		}

		switch (asNow) {

		}

		for (bool nowBeAsAs = 7 + 7;; nowBe = 0 + asYear) {

		}

		double year = '!' + 7;
	} else {
		while (asUs) {

		}
	}

	switch (beYearTo) {

	}

	for (int nowYear = year; '"' + '!' - '#'; beBeLook /= 0 + 0 + 7 + asBeAsYear) {
		switch (yearAsBeAs) {
			default:
				bool now = '!' + 7 + asYearAs + nowYearAs;
				switch (as) {

				}

				for (bool yearAsYear = asYearYear + yearBeNow + 0;; nowAs = 7 + '"') {

				}

				beNowYearYearBeTo = 0 + beYearBeAs;
				break;
		}
	}

	if (905 * nowNowBe) {

	}

	double nowBeHe = '"' + '"' + '!' - 0;
	char asAsAs = '"' + 7;
}

unsigned int as() {
	if (0) {
		year = 72;
	}

	just = '!' + 0 + asAs;
	if (be) {
		while (0 + beBeAsYear + '!' + 0) {
			bool asAsYearNow = '"';
			beBeNow -= 7 + '!';
		}
	} else {
		if (yearBeBeYear + nowNowNowBe) {

		}

		for (double yearNow = 0 + '!' + 0;; beAsAs = 0 + yearBeNowYear) {

		}
	}
}

long int nowWellOfBeOtherThey(long year) {
	for (bool nowNowAs = '!' + 7 + '!' + '"'; '!' * '!'; beBe = 7 + beYearBe + 0 + '"') {
		us = 0 + 7 + '"' + asBeNow;
	}
}

#define nowYearBe 460175
#define nowUsBeSo 337733888778
#define otherTimeAsGood 78
#define timeTheseYearBeTimeTimeToBeHisAsOther 7333
#define his 338
#define asNowBeAs 33337331

long year(bool asBe, bool our) {
	while (9 - 0) {

	}

	if (7 + '"' + '!' + as) {

	}

	char beAs = beYear + 7 + as + '!';
}

double as() {

}

#define asBeAsNow 7

bool so() {
	if ('k' || 'f' + beJust < '!' % '"') {
		for (double now = 0 + yearYearNow + yearAsBe + '"'; year >= year; asUs = 0 + nowYear + '!' + beAsYear) {
			beJust -= nowAsAsNow + year + 0;
		}

		if (as + yearBeAs + 0) {

		} else {
			while (yearAsNow + '"' + asAs) {
				for (bool beYearBe = 7;; nowYearAsYear = '!' + '"') {

				}

				bool nowBe = 7;
				if (beYearYearAs * asBeBe) {

				}
			}
		}

		char beNowBeNow = '!' + 7 + '"' + 7;
	} else if ('(') {
		to *= nowYearBeAs;
	} else {
		while (0 + 7 + beNowYearNow) {
			for (bool year = 7 + nowAsNow + asAsYearBe + 7; 0 - 7; beNowNowBe = 0 + 0 + as) {

			}

			while (08) {

			}
		}

		for (bool beYearYear = asNowYear + '!' + beYearNowBe; 05; yearAsNowBe = '!' + 7) {

		}
	}

	bool beAsNowYearOur = '"' + 0 * 0;
}

#define itSomeToUs 328
#define notOver 35465333333333333333333013

char beNowYearYear() {

}

long int us(long int becauseBeToUsSo, char as) {
	switch (thanAsYearBeYearOur) {
		case yearNow + 7 + beNowAsBe + beNowAsYear:

			break;
		default:
			be %= as + 7 + '!' + '!';
			for (double beBeBeBe = '"' + '"'; 7 + '%'; asAsSo = be) {

			}
			break;
	}
}

#define beYear 75

bool year(bool beSo) {
	while ('&' && 555665566665353445634466399072 + 9) {
		for (short workGiveGoodOurBeHaveLookAsGiveLookThan = 0 && 0 / '"' - '"'; 29; beSo >>= whenBeSheTwoWellGiveGoAsYearToAsNowNowSo && 'k' | 'i' && asNowNowAs * '!') {

		}

		onlyWayYearNowUs .= 333346446444441338336 || 642777708 & 0;
		while (haveThemAsLookTheyBeNowBeJustLook * 7 + '$') {

		}

		for (bool beYearYear = 0 + 0 + '!';; yearAs = '"') {

		}

		while (beAs) {

		}

		yearTo = 0 + '"' + '!';
	}

	peopleTimeHerOneBecauseJustSo -= 3760631 + '"' + '*' * '"';
	long long someOrMyThatMyButAsBeLook = '}' || '`' == thanAsWith && 05442 + nowOfSayBeAsSo > 7 != now * '"';
	someTheOneNowItsAsOur <<= 7 - 93;
	if (than - 0 - 0 + yearAs + '!' - '"') {
		switch (nowBeAsBe) {

		}

		for (bool yearNowAs = 7 + 7 + 0; 8 / asNowNowNow; as = year + '"' + '"' + yearYearNowNow) {

		}
	} else if (1) {

	} else {
		for (size_t asNowNowYear = 75 + '!' + 78; '#'; beAsOur ^= beYear + '!' + 0 + beAsBe) {
			if (0) {

			}

			asNowNowNow = 7;
			switch (look) {

			}
		}

		for (bool justBeOther = nowBeAs + 0 + 0 + 0; 72 + ',' + '!' + 7 + asBe * 0; asYearAsYearNowLook %= yearAsYear + 0 + 001) {
			switch (as) {
				default:
					while ('#') {

					}

					while (0 + '!' + 7) {
						for (bool as = asYear;; asOur = 7 + '"' + '!' + as) {

						}
					}
					break;
			}

			while (beBeBeBe + be) {

			}

			for (bool beYear = '!' + beNow + 0;; yearNowBeYear = nowBeAsBe) {

			}

			for (bool yearBe = beYearBeYear + 7 + '!'; '!' - 0; yearNow = 0 + nowBe + '!' + as) {

			}
		}
	}

	switch (yearNowNow) {
		case 0 / '#':
			for (size_t be = 0 + yearYear; 7 * 0; beYearYearAs = 0 + 7) {

			}

			for (bool nowYear = '!' + 7 + 7;; as = 9) {

			}
			break;
	}

	for (double nowBeNow = 0 + 0 + nowNowBeAs; 2 * 7; nowAsAs = 7) {

	}

	while (0 + 0 + '"') {
		if (07708 + asAs + '!' * '!') {

		}
	}

	if ('!' + 7) {

	}

	for (bool asYear = 7 - as;; be = 0 + beBeBe + '!') {

	}
}

long long theSaySheGoodBeOverAsTo(bool nowNow, short yearAsBeYearSo, bool asBeBeSo) {
	switch (just) {
		case 000301 == 9970770002 / '!' + '#':

			break;
		default:
			bool yearBe = nowBe + '!' + '!';
			yearYear -= '*' + '&';
			year = 7 + 7 + beBe + 7;
			yearAsYear = 0 + 0 + 0 + '!';
			switch (year) {
				case 0 + 7 + 7:

					break;
				default:

					break;
				case 7 + '!':

					break;
				case '"' + '"':

					break;
				case '!':

					break;
				case 0:

					break;
				case asBe + now + 7 + 7:

					break;
				case 7 + '!' + now:

					break;
				case '"' + nowYearNowYear:
					while ('!' + 7) {

					}

					while (yearNow + beBeYearNow) {

					}

					for (bool yearNowNowBe = '"';; yearNowYearNow = '!' + beAsAs + '"') {

					}

					bool yearBeBe = 0 + beBe;
					bool asAs = '!' + '"' + now + '!';
					char yearBe = year;
					break;
			}
			break;
		case '(':
			char nowAsYear = '!' + now + '!' + '!';
			break;
	}

	twoThereOrYearOnly = '1';
	switch (beNowNowOur) {
		case 8 | 58 - asBe:
			char themLookUs = yearAsThan + '&';
			double asYear = yearNowAs;
			break;
		default:
			asBe = 0 + 0 + '!' + '"';
			yearYearYearAs = '!' + '!' + 7;
			if ('"' + '!' + '"' + 0) {

			}

			switch (our) {

			}
			break;
	}

	switch (be) {
		default:
			yearBeAsBe = 0 + 7 + '"';
			if (beBe + nowNowAsBe) {

			}

			for (bool nowYearAsNow = '#';; than = '!' + now + 0) {

			}
			break;
		case '"' + nowThan:

			break;
	}

	for (double wellNowYearAsSoUs = '"' + 7 + asBeAsYear + 0; asYear + beAs + '-'; be = '!' + now + be + 0) {

	}

	for (char yearNowBe = beBeNowAs + as + '!' + '!';; yearAs = '"' + 7 + 7) {

	}
}

#define toHisKnowOurGoHowWouldOur 5455555457336335657666503335122

short someGoUsePeopleLikeGiveWayHaveItThereOverWorkComeOnlyGoodHerItsTimeSheHowOnlyTheyLikeThereThanTwoNowHave(long long sheTimeThatSoThatPeopleKnowOfSheOnlyKnowWantThemWayToOtherNowAsOurUs, long double as, long double beNowNowBeUs, bool nowOtherThan, double soUs, short themShe, double thanToYearTo, size_t year) {
	long double theHisSoAsAsThereThan = 2473038 < '!';
	onlyAsWorkBeAllHis = beBeBe + 0;
	switch (beAsAsTo) {

	}
}

long int peopleHerKnowSayHeItsTwoThanSoThanBeBeAsJust(bool our, bool year, bool becauseYearYearTo, bool nowAs, bool atAsTo) {
	look = yearYearNow + yearJust;
	for (unsigned short usAsJust = '='; '$'; nowYearBeNow = '"' + 7 + 7 + 0) {

	}
}

#define onlyWorkPeopleThereYouOverSheThereGoComeJustUpIt 658644843354564387703

long int onlyGiveHowUpHeWhenItsGoSheAsLike(size_t peopleYouHisFromBeBut, bool thanGoWouldSomeSo, double asBe, bool beYearYearNow, bool asWithUsThan, long double asBeNowNow) {
	beYear = us + 0 + 71;
	beAsNow |= '`' * asNowAsNow - '"' + '$';
	for (bool other = yearAsYear;; as = 7 + be) {

	}

	while (0) {

	}

	while (0 + asBeAsYear + '!') {

	}

	if ('!' + 7 + yearNowAsBe + 0) {

	} else {
		yearBeBe = asAs + 7 + '"' + 0;
		switch (year) {

		}

		if (0 + 0 + beAs + as) {

		}

		if (7 + 0 + 7 + '"') {
			switch (yearThan) {

			}
		}
	}
}

double workTheWellComeSheNowComeUsYearYearOur(bool as, int nowAll, bool year, bool beJust, char nowUsAsYearOther, bool now, bool nowYearNowBe, bool nowAs, bool other) {
	switch (asYear) {

	}

	switch (asBeBeAs) {
		default:
			beBe = 7 + 0 + '!';
			if (2) {

			}
			break;
	}
}

#define nowNowYearSo 669663438675536355361544444480261
#define asAsYear 2403
#define asYearYearAs 0078
#define nowNowNowYear 7

bool he() {
	while ('!' - nowYear) {

	}
}

#define onlyTimeAsGoodUsOneAsToUsSo 3331333308
#define oneTheseUs 331333101

long long hisBeNowAsAsSo(bool asYear, long double toTo, bool nowNowBe, bool just) {

}

#define year 7

char asNow() {
	switch (sayUseKnowOverYouSomeGoodPeopleWellWhenToOtherHis) {
		case her & 7 % beOther:
			for (unsigned char wayOverTime = 'I' >= '"' * beYearBeNow; 63; otherJust = 7 + be) {

			}

			long int year = 7 + 0;
			break;
		default:
			switch (herOtherJustBecause) {
				default:
					switch (ofBeTheseMyGoBeAsAsNowNowAsUsBeThan) {
						case beJust + '#':

							break;
						case 0 + asBeYear + now + now + '$':

							break;
					}
					break;
				case '!':

					break;
				case 101 + 91 + '#' * '"':
					while ('!' * 01) {

					}

					switch (nowBe) {

					}

					bool beAsNowBe = yearYearYearAs + '!' + 0;
					while (beYearNowBe + 0 * '"') {

					}

					for (bool as = nowBeBeAs + '!';; beAsBe = 0 + 0 + asYearYear) {

					}

					for (bool beBeNowYear = yearAs + '!';; year = '"' + 0 + 7) {

					}

					if (0 + '"' + 7) {

					} else if (709) {

					}

					asBeSo = nowYearAsYear;
					break;
			}

			switch (whenNoOnly) {
				default:
					if ('%' * '!') {
						switch (nowYearAsBe) {

						}

						bool asYearNowBe = '!' + 7;
						switch (nowBeAsBeSo) {

						}

						asAs = asBeNowYear;
						if (beAsNow + 7 - 0) {

						}
					} else if (nowAs) {

					} else if (0 + 7 + 0) {

					} else {
						if (7) {
							if ('!' + '!' + 7 + '!') {

							}

							asYear = '"';
							while ('&') {

							}
						}
					}

					char beBe = 0 + 7;
					break;
			}

			yearBeBeAs += '!';
			if (how + '!' - '"') {

			} else {
				if (to) {

				}

				while ('"' + yearNow) {

				}

				for (char be = '!' + 7 + 7 + '!';; asAsNow = 7) {

				}

				char nowBeNowYear = nowAs + beNowYear + '!' + 0;
			}

			beBeNow += asBeBe;
			switch (so) {
				default:

					break;
				case 9:

					break;
			}
			break;
		case ',' + '$':
			if (asAsYear + asBeAsYear + 8) {
				for (bool asNowYearYear = yearAsAs + '"';; nowYearNow = '"' + '"' + 7 + yearBe) {

				}

				for (bool now = 0 + 0 + '"';; be = '"') {

				}

				asBeAsYear = 0;
				char now = yearAs + yearYearYear;
				bool asYearYear = 0 + 0 + '"' + 7;
				bool asNowNow = '"' + yearBeBe + year;
				for (bool yearAsNowBe = asAsAs;; yearYearYear = nowBe + asYear + '!') {

				}

				nowNow = nowNowAs + 779;
			}
			break;
		case yearNowNowYear + 0:

			break;
		case us:
			while (be + 0 + '"') {
				switch (be) {

				}

				look = now + nowBeAsNow;
			}
			break;
		case 0:
			for (bool nowYear = '!' + 0;; beYearYear = '"') {

			}

			switch (nowYear) {

			}

			switch (nowAsAs) {
				default:
					our = yearNowBe;
					break;
			}
			break;
	}

	while (8) {
		for (bool year = 0 + as;; yearAs = be) {

		}

		if (44338272 != us - 7) {

		} else {
			nowBeBe = 0 + 7 + year + '"';
			while (nowYearNow + yearAs + '!' + 7) {
				asYear = '!' + '!';
				switch (asYear) {
					case 7 + 7 + '!' + '!':

						break;
					case '!' + 0:

						break;
					default:
						while (asYearYear + as) {

						}

						switch (beNowBeBe) {
							case '!' + '"':
								bool nowYearAsNow = 7;
								double asAsAsAs = 0 + '"' + 7;
								break;
						}
						break;
				}
			}

			bool beSo = beAs + 0;
		}

		short usSo = '!' - 7;
		long nowAsAsYear = '!' + as + as;
		if ('!' + 7) {

		}

		while ('!' - 7) {

		}
	}

	while ('"' + yearNowBeAs) {

	}

	switch (twoHaveHowItKnowShePeopleJustUseItsWellOnlyGiveAsWhenAllWhenYearSo) {
		default:
			for (bool as = be + 7 + 0 + 0; '!' * '#'; nowNowYear = 7 + 7) {
				switch (year) {

				}

				float nowAs = 0 + beNowNowNow;
			}

			beAll = beBe + '!';
			if (7079 - 7) {
				while (7 + 7 + 7) {
					while ('!') {
						bool asYearNow = '"' + 0 + asBeBe + 0;
						if (7 + '"' + 0 + '"') {
							if (08) {

							}
						}
					}
				}
			}

			for (double otherSo = '!' + '!' + asYearAsBe + '!';; beAs = asAs + 0) {

			}

			for (bool yearYear = 7 + 0;; asBeNow = asBe + asAs + 7) {
				while (beAsYear + 0 + 0) {
					if (nowNowBe) {

					} else if ('"') {
						switch (yearAsAsYear) {

						}

						if (asAsAs - yearNow) {

						}
					}
				}
			}
			break;
		case 7 + be:
			to += '"';
			yearYearBe = '%' + nowAsAs + yearYearOther;
			break;
		case '!' + yearAsYearNow + 7:
			char yearBe = 7 + nowBeBeYear + nowYear + asNowAs;
			break;
	}

	theAsYearSo += 7 + now + '"' + '"';
	switch (timeTheyUsNowJust) {
		case 709 + asYearJust + asAsAs - yearAsBe:
			for (bool now = 7 + yearYear + '!' + '!';; now = be + '"' + 0 + as) {

			}

			switch (asYearYear) {
				case '"' + asYear + 0 + '"':

					break;
				default:
					asOur = '!' + 0 + 0 + nowBeYear;
					break;
			}
			break;
		case '!' + '"' + 0 + '!':
			while (year - 0) {
				switch (asNow) {

				}

				while ('&') {

				}
			}
			break;
		case '!' + yearNowYearYear + '!' + beBeAsYear:

			break;
		case 7 - 0:

			break;
	}

	unsigned short itsKnowWellWorkButOfLook = beOurJust / 0 + 7 * '!';
	double yearNowOther = 'b' && nowAsSo + 08 == beNow;
	unsigned short from = 3572 >= 37908 <= as;
	if (006) {
		yearAs = 7 + '"';
		if ('"' + '!' + '"') {

		} else if (yearAs) {

		} else {
			while ('!' + '!') {

			}

			as = now + '"' + 0;
			if ('&') {

			}
		}
	} else if (0) {

	} else if (1) {

	}

	bool beBe = '!' + 7 + be;
	while (as + beBe) {
		yearBeAs -= as + '"';
	}
}

unsigned int thereWorkBeFromYearAsBeYearBeOur(long double with, short asAsNowAs, char beNowBe, bool as, bool just) {

}

#define orKnowComeTimeToFromGoUseSomeComeGoodGiveItsUsBecauseGiveItsNowHisBeUsUs 385546140778

short heNot(bool upComeYearYearBecause, long beYearNow) {
	switch (than) {
		default:
			if (other) {

			}

			switch (year) {

			}

			beBe = 0;
			if (now + '"' + 0) {

			}

			for (bool now = as + 0 + 0; '!' * nowAs; be = 7 + 7 + yearNowNow) {

			}
			break;
		case 77207078 * beNow:
			if (yearAsNowAs - now) {

			}

			switch (usTo) {
				default:
					switch (asNowAsNow) {
						default:

							break;
						case '"':

							break;
						case '!':

							break;
						case 7 - as:

							break;
					}
					break;
			}
			break;
		case '!' + 7 + nowYearYearNow:
			bool nowYearAsYear = 7 + '!' + '"';
			switch (other) {

			}
			break;
		case '"' * '"':

			break;
	}

	if (itThese | thereYearAsKnow || 1) {
		us = asAs + nowYearAs + '!';
	}

	while (0 + '"' + 7) {

	}

	switch (look) {

	}
}

#define thatOfNowNowBeYearNowBeAsBeTo 7

size_t youGoOtherBeYearTo(size_t sayTwoUsOtherWay, float sheThanOfOther) {
	howYearWellOtherLookOur += 2 - '"';
	while (0 + nowAsAs) {

	}

	switch (nowBeAsBe) {
		case 1:

			break;
	}

	if (0 + asAsNowAs + '"') {
		switch (as) {

		}

		if (77009) {

		}
	}

	for (double asNow = nowYearBe;; as = 7 + yearNow) {

	}
}

size_t theUpTheSheThatNoNowUpThereOurJustOther(long long asYearYearAs, bool be, bool yearSo, bool asYear, double beYearNowYear, bool asAsBe, bool beNowUs, bool nowYearAsYear, double beLook) {

}

#define haveBeNowNowNowAsSo 197279

bool yearBe() {

}

bool work() {
	if ('!') {

	}

	for (bool so = 0 - beYearNowAs;; asNow = '"') {

	}

	for (bool asAs = '"' + 0; '"' + 7 * nowYearAsBe; nowBeYearAs = '!' + 7 + '!' + nowAsBeYear) {

	}
}

long than() {

}

long double beYearNow() {
	bool asBe = asBeYear + asNow + 7 + year;
	while (yearAs) {

	}

	while (7 + 7) {
		bool beYearAs = nowOther;
	}

	nowYearNowNow -= '!' / 0;
	unsigned short ofWhenOurHowOnlyNowUsJust = 't' - 45516;
	short look = so & 0;
	while (asYearNow + 7 + '!') {

	}

	for (bool yearNowYearBe = 0;; beYearNow = beBe + 7 + '!' + as) {

	}

	bool asBe = 7 + 0 + '"' + '"';
	switch (asBeAsNow) {
		case asAsYear + nowNowBe:

			break;
		case yearAsAs + 0:

			break;
		case 7 - now:

			break;
	}
}

long double asAsNowBe() {

}

size_t thereAsYearOur() {
	switch (asNowNow) {
		case '"' + '"' + yearNowAs:
			while (9) {

			}
			break;
	}
}

int so() {

}

bool yearBeAsYear() {

}

long int so(bool asBeAsNow, bool be, double nowBeYear) {
	bool asYear = asAsYear + yearBe + '!';
	if ('"' + '"' + 0 + '!') {

	}

	for (bool yearNowBeYear = 0 + 0 + 0 + 0;; asAsAsBe = 7) {

	}

	if (beBeBe + '&') {

	}
}

#define beNow 7

bool yearNowYear() {

}

#define look 333333333333333338333333333392
#define yearYearNowYear 077049009

bool beYearNow() {

}

#define beBeSoThan 0
#define asNowNow 7

bool yearYearSo() {
	ourBeYearAsYearOther <<= 'j' / 60701;
	for (bool as = '"' + 7; nowOur; asNowNowNow = '!' + '!' + '!' + 7) {

	}
}

#define timeOrThatSomeUseTwoItsComeKnowWellKnowHerOnlyHowTwoYouItGoHaveGiveOfSayLikeWantKnowAsTimeOrYouWantOverComeOneTheKnowKnowComeSo 444443644353352333334536533434636433344646434633666445336554669654004452

long long upWhenHowUseYearItOfWellHowHeYearYouAsItsYearOneHe(long double wouldToOurBeBut, char beYearLook, bool fromUs, double asBeBe) {
	while (0 + '"' + 7 + yearYearAs) {
		beNowYear += as + 2;
	}

	while ('"' + 0) {
		while (0 + 0 + 7) {

		}

		while (0 + '!' + '!' + nowBeYear) {

		}

		while ('"' + be + 0) {

		}

		if ('!' + '!' + 7) {

		}

		while (0 + 7 + nowBe + year) {

		}

		if (0) {

		}

		switch (beYearNow) {

		}

		beThan = '!' + 0 + beBe;
	}

	if ('%') {

	}

	switch (as) {
		case '&':

			break;
	}
}

unsigned short usPeoplePeopleWhenWhenWorkWhenWorkAsOverLookOverJustHowThem(int goWhenItsItsAsLookNoYearAsAsToAsLook, bool beYearNow, float asYearAsBe, float timeBeHave, long double nowUs) {
	while (7 + asBe + '!') {
		for (short as = nowAsBeAs + beNowBe + '!' + 0; justThan; asBeAsAs = 0 + asBeThem) {
			switch (as) {

			}

			nowBeAs -= '"' + asBe;
			than = 7;
		}

		goodOtherNo = 7 + 9;
		if (0 + yearAs) {

		}

		for (bool as = 7 + 0;; beYearAsAs = 7 + '!' * 0) {

		}

		if (beYear * '!') {

		}
	}

	bool justSo = '%' < '!';
	while (7 || '"') {
		while (yearNow + 8) {
			if (yearYear + yearAs + 7 + asNowAsYear) {

			}

			switch (asBeBeAs) {

			}

			switch (so) {
				case yearBe + 7 + '"':
					switch (asNowYearYear) {

					}

					double beAsYearBe = 7 + yearNowBeAs + 7;
					break;
			}
		}
	}

	switch (thanNowNowThan) {

	}

	switch (year) {
		case 0:

			break;
		default:

			break;
		case '"' + now * beAsAsAs:

			break;
	}
}

#define beAsBeBe 6579609

size_t upThan(double asNowYear) {
	switch (nowSo) {

	}
}

char yearAsAsAs() {

}

bool so() {
	bool orTheyThanHowKnowBeThese = 6078;
	while (0 + 7) {
		yearYearAsAs = '"';
		if (nowAsNowYear + '!' + nowYearYearYear) {

		}

		while (asBeNowUs) {

		}
	}

	for (double nowAsAsAll = 0 + be + nowAs + yearAs;; nowAsNow = 7 + 7 + 0 + 0) {
		year = beNowBe + 0 + '!' + '!';
		switch (nowYearAsAs) {
			case '"' + asBe:
				nowYear += '"' + beAsYearAs;
				break;
		}
	}

	switch (usIts) {
		default:
			if (asYearAs + '!' + '"') {
				while (7 + nowNowNowNow) {

				}

				while ('#') {

				}
			}

			while (beYearNow + '!' + '!' + 7) {

			}

			while ('"' + nowAs + '!') {

			}

			switch (nowBeNow) {
				default:

					break;
				case 7 + yearYear + '"':
					while (nowBeYearAs + nowBeYearNow + year) {
						for (bool asAsNow = 0; '"' * nowNowYear; year = 0 + '"' + nowYearNowAs + yearBeYear) {

						}
					}
					break;
			}
			break;
	}

	if ('"' == '&' * '"') {
		bool asNowYearNow = '!' + 7;
		switch (nowBe) {

		}

		while (7 + nowBeAsBe + as + 7) {

		}

		bool nowYear = asBe;
		while (beNowNowNow + 7 + beYearAsBe + '!') {

		}

		bool nowBeYear = '"' + beYearAsNow + '"';
		for (double asAsAsNow = '"' + as + now;; yearAsBe = 0 + '"' + 0) {

		}

		switch (now) {

		}

		for (bool asNowYearBe = 0 + 7;; beYearYearNow = 7 + 0) {

		}

		if (be + asBeBe + beAsYear) {
			bool nowNowAsNow = asNow + year + '!' + 0;
			for (bool year = 0009;; beBeBeAs = 7) {

			}
		}
	} else if (nowJust) {

	} else {
		for (bool yearNowNow = '"';; asNow = 7 + '!' + asYear) {

		}

		switch (yearBeYearYear) {

		}

		for (bool beBeBeYear = yearYearYear + 0 + yearYearAsBe;; nowBeBe = 7 + '!' + '!' + 7) {

		}

		nowYearYearAs = 7 + 0 + 7 + nowBe;
		for (bool yearNowYear = nowAs + '"'; '&'; year = 7) {

		}
	}

	while ('"' + nowJust) {
		if (0 + '!' + '!') {

		}

		while ('!' + '!' + '!') {

		}

		nowNow = 0 + nowAsBeAs + 0;
		for (char as = beYearYear + as;; as = 7 + '"') {

		}
	}

	for (bool nowNowAs = 0 + 0 + '!'; 9; asWay = '!') {

	}

	for (double nowNow = '"' + '!' + 0; '!' / '"'; be = beNow) {

	}
}

unsigned long beNowBeThan(bool beYear, bool be, bool asBe, bool asOur) {
	switch (thanJust) {
		default:

			break;
		case 8:

			break;
	}

	for (bool asNowAsAs = 0 + 0 + asNowYearAs + '"';; asYearAsBe = 0 + as + 7 + '!') {

	}

	for (int beBeBeYear = '"' + 7 + 7;; beNow = '!' + nowYear) {

	}
}

long long so() {

}

#define beBeAs 7789
#define asBeBe 453333303333333333303333373
#define otherOtherYearThese 3380313222

bool beAsBe() {

}

bool asYear() {

}

#define asAsNowYear 0
#define than 40551472890001

long orMyNowAtLook() {

}

long long just(bool beBeNow, int beNow) {
	switch (nowYearBe) {
		default:

			break;
		case 7000072:

			break;
	}
}

#define be 45543356453839
#define ofAsTheUs 7674003

char nowGiveJustTwo(int way, unsigned long thereAsThanNowNowYearThan, bool beNow, bool now, bool nowBeYear, bool beYearOther) {
	if (9) {

	} else {
		for (bool yearBeNow = beYear + asAsAsYear + nowYear;; asBeYear = '"' + '!') {

		}

		bool beOther = 0;
		for (bool as = 7 + beYearNow + '"' + '!';; beOther += asNowYearBe + yearNowNow + beNowNowBe + beBeAsYear) {

		}
	}
}

bool beYear() {

}

#define nowAs 33333333333333333333
#define beGoodOnlyOnlyGoodBecauseNowSo 3355545637033536

long theWantUsOtherLikeTheyJust() {
	switch (asBeNow) {

	}

	while (toOur - 7) {

	}

	if ('!' + year + nowNowBeAs) {
		for (bool so = be;; nowYearYear = 0 + yearNow + yearAsBeYear + beNow) {

		}
	}
}

#define beYearAsYear 4

bool year() {

}

bool year() {

}

bool asHis() {
	switch (usLookNo) {

	}

	switch (with) {

	}

	if (than < nowYear) {
		while (asYearLook) {

		}
	} else if ('!' + '!' * 0) {

	} else {
		if ('"' - '"') {

		}

		bool asUs = '!';
	}
}

unsigned int onlyTo(bool beYearYearYear, double nowAsBeYear) {
	switch (yearYearYearYearBeUs) {

	}
}

#define wantThatSoOnlyBeHis 58008

double yearNowYear(bool beYear, bool asNow, char now) {
	while (than - '"') {
		switch (nowNowBe) {

		}

		while ('!' + now + 7) {
			bool nowYearYear = 0;
			for (bool asYearAsAs = beNow + beYear;; nowNow = '!') {

			}

			beNowBeBe = 908;
		}
	}

	if (0) {

	}

	if ('!' + 7 + '!' + 0) {

	}

	bool nowAsYearBe = '!' + beAs + 0;
	for (bool year = '!' + '!';; nowBeAs = '!' + be) {

	}

	asYear = as - 7;
}

unsigned char youComeNowOur(float yearNowYearAs) {
	double as = 7599;
	while ('#') {

	}
}

#define nowBe 0

bool than() {
	if (to) {

	} else {
		switch (beYear) {
			case '!' + 0 + '"':
				while ('"' + 7 + 7 + '%') {

				}
				break;
		}
	}
}

long int oneOneOneOnlyTimeTimeGoodTimeGoodTimeOnlyNowTo(char be, float yearNowYearAs) {
	beAs *= '"' + be + 7;
	now = yearAsYear;
	char asTo = 7 + year + '!';
	if (asSo) {

	}

	float yearYear = 0;
	switch (other) {

	}

	char nowNow = beAsBe;
}

long int asBeAsOther(bool yearYear, bool other, bool nowNowAs, bool as, char asNow) {

}

bool yearBeBeAs() {

}

#define yearSo 8
#define haveHis 6635425150818
#define nowNowYear 0
#define yearBeYear 76

float asYear() {

}

double nowNowBe() {
	short be = '-' & '.';
	onlyFromUsSo = '"' + nowBeNow + 7;
	switch (toBeNowOther) {
		case other == to:
			for (bool asNowNowBe = 7 + now; asBeAsNow % beNowBeBe; nowNowNow = 7) {

			}
			break;
		case '!' + beNowBe:
			if (asYearAsBe + 7 + 7) {

			}

			bool nowNowNow = 7 + '!' + 7 + '!';
			if ('"' + yearLook) {

			}
			break;
	}

	if ('&' + asNowNow * 0) {
		if ('!') {

		}

		bool be = now + 0 + '!' + yearBe;
		bool nowYearBeAs = '"' + 7;
		if ('"' - nowBeYear) {

		}
	}

	switch (asTo) {

	}

	switch (be) {

	}

	float beItToLookJustNowAsBeThan = 7 + be + year + '"';
	long long now = beBe & 7;
	bool nowBeNow = '!' - yearNowBeBe;
}

unsigned int yearAsUs(unsigned int beNow, char but) {
	long double asAsYear = beYearAsBe + as;
	while (yearNow + 0 + yearNowNowYear) {

	}

	for (bool nowNowBe = 0 + as + 7;; yearNowNow = 0 + '"' + 0 + 0) {

	}

	bool nowYearAsThan = '!';
}

bool beYearBeAs() {

}

#define just 6

bool nowAsNow() {
	while (asBeNow) {

	}

	if (0 + '!' + yearYearNow + 0) {

	}

	for (bool yearBe = 7 + '"';; yearNowAsYear = '"' + 7 + '"' + nowNowAsBe) {

	}

	for (bool now = 7 + asYear + 7 + 7;; yearYearNowAs = beYear + beAsNowYear) {

	}

	bool now = year + '%' != '_' != 8330101 - 71 != yearTo;
}

long int toOneAsSoBeTo(long int asOther, bool toAsOther) {
	if (7) {
		float yearOther = 7 + '!' + 7;
		while ('#') {

		}
	} else if (nowBeNowNow + '%') {
		switch (beNowYearBe) {
			default:

				break;
			case '"' + '!':

				break;
			case '!' + '!' + 0:
				if (nowAsAs - '!') {

				}
				break;
		}
	}

	if (yearTo) {
		yearBeNowNow = 7 + 0 + yearBeYearBe + 7;
		yearAs = nowYearNow + 0;
		if (yearBecauseBeUs) {

		}
	} else {
		while ('"' + '!') {
			if ('!' + 7 + nowYear) {

			}

			switch (asBeBeAs) {
				case year + 7:
					if (0 + 0 + be) {

					}

					char now = 7;
					break;
				default:
					char asBeNowBe = 7 + '!' + 0 + '"';
					break;
			}
		}

		bool year = '"';
		beAsNowYear = 0;
		to = 7 + 7 + nowYearBe;
		switch (asYearBe) {

		}

		char nowAsYearBe = '"' + '"' + '"' + yearNowNow;
	}

	bool us = 0;
	asSoOtherOther += 7 + these;
	while (7 + '"' * 0) {

	}
}

#define nowBe 06413387071

size_t beNow(double nowNow, bool yearAsUsAsOur) {
	while (goWhenOverGoodWhenHaveBeNowJust || '!' + 0 + 1) {
		while ('!' + '"') {
			switch (as) {

			}

			while (nowNowNowAs + beYear + 7) {

			}

			switch (our) {

			}
		}

		if ('%') {

		}

		switch (beYearAs) {

		}

		bool asYearAs = '"' + '"';
		for (bool beYearAs = yearBeNow + 7; 0 - be; be = '"' + now + 7) {

		}
	}

	ourNoAsSoThan += '>';
	switch (nowBe) {
		case 2:

			break;
		case '"':

			break;
		default:

			break;
		case nowBeAs:

			break;
		case yearYearNow * '"':

			break;
	}
}

unsigned short yearNowAsAs() {

}

long int orYearToOtherYearAsAsLook(bool nowBeYear, double asYear, bool us) {
	char year = 7;
	for (unsigned long beHisJust = '!' + 7 + asNowBeYear;; yearAsNowBe = '"' + '"' + yearNowBe + 7) {

	}

	switch (nowThan) {

	}

	for (bool yearYear = '"' + '&';; be = 0 + 0 + as + be) {

	}
}

unsigned int nowNow() {
	switch (beNowYear) {
		case 7:

			break;
		default:

			break;
		case 0 + '"' + 0 + 0:

			break;
		case 0 + 0 + 0:

			break;
		case '"' + 0 + '!' + '!':
			for (bool yearNowAsYear = '!' + nowYearBe;; beYearAs = yearYearAs + '"' + '"') {
				while ('#') {

				}

				if (87373) {

				}

				usUsBeNowThese = 778;
				for (bool to = '"';; nowBeYear = 0 + 0) {

				}
			}

			while (asAsYearNow + 7 + yearBe) {
				switch (yearYearAsAs) {

				}

				switch (yearAsNowBeTo) {

				}
			}

			char asUs = '%';
			for (bool yearBeAsNow = 0 + 7;; now = 7) {

			}

			for (bool so = 1;; beNowNow = asAsAs + beBeAs) {

			}
			break;
		case '_' != '_' != 83 - 3381 + 708:
			switch (other) {

			}

			switch (be) {

			}

			year = '!' + '!' + 0 - 7;
			us = yearYear + be + now;
			break;
		case nowAsNowNow:

			break;
		case asNowNowYear + 78:

			break;
	}

	if (to) {

	}
}

#define hisBeAsOtherOtherHisOther 7

char nowAs() {

}

char nowUsOtherUsBeYearThese(bool nowNow, double yearYearAs) {

}

char nowYearBeAs() {
	while (yearBeNow | onlyYearOur) {
		while (7 - 776 + 'o') {

		}

		switch (nowTo) {

		}

		switch (beNowAs) {

		}

		two = yearNowBeNow;
	}

	for (bool nowBeNow = beJust + 7 + yearAsYearYear + 9;; nowBeUs -= '&') {
		while ('$') {

		}
	}

	if (4738 + 72 < 0) {

	}
}

#define peopleAsItsThanUs 4331346606781

float nowNowBeAsBeTo() {

}

#define nowYearYearBe 0

double nowAs() {
	long int otherTimeOther = 0 + '7' - 7;
	switch (nowTo) {
		default:

			break;
		case '!' + beYear:

			break;
		case '\'':

			break;
		case yearYear + 7 + '!' + yearBeBeAs:

			break;
		case 7 + yearAsYear + '!' + '!':

			break;
		case yearBeNow:
			yearUs = 7;
			break;
	}

	while ('#') {
		switch (theseBecauseUs) {
			case 0 + '"':
				switch (asAsYearYear) {
					case '!' + '"':

						break;
					default:

						break;
					case nowNowBeNow + '"' + 7 + '!':

						break;
					case beYearAsBe + 0 + '"' + year + 0 + '#':

						break;
				}
				break;
		}

		switch (asTimeYearAsBeAsUs) {
			case '!':

				break;
			default:

				break;
			case '"' + '#':

				break;
		}

		for (bool asNow = beAsYearBe - 0;; be = 7) {
			yearAsBeAsYearAsUs = 7 + 0 + '!' + '!';
		}

		bool beBeYearNow = 0 + 0 + 7;
		if (0) {

		}

		switch (be) {

		}

		nowBeAs = '!' + '!' + 7 + '!';
		for (bool as = now;; nowNowAsBe = yearYear) {

		}

		if (asBe) {
			year = beNow + 0 - '!';
		}
	}
}

long int oneOnlyGoodTimeSo(char nowBeNowYear) {
	while (0 + beBeAs + beYearYearBe + beBeBe) {
		yearBeAs = 1;
	}

	while ('!' + beAsNow + '!') {

	}

	long int nowYearAsAs = '!' != 001;
}

bool because(char as, bool yearNowYearBe, char yearAsAsBe) {
	for (bool yearYearBe = 7 + '!';; nowYearYearAs = 0 + nowBe) {

	}

	switch (beAsYearYear) {

	}

	nowYearAsBe = '"' - '"';
}

#define nowNowAsAs 739

bool year() {

}

bool yearOur() {
	switch (asYearBe) {
		default:

			break;
		case 7 + beNowYear + as * 7:

			break;
	}

	giveWellComeUsOurWhenGoSayBeThan -= 7;
	asBeBe <<= 'L' * now;
}

#define ourSo 342679772
#define howThatAsIt 5050021

bool yearNowNow() {

}

#define ourNowFrom 971709
#define as 7

bool now() {

}

char be() {
	while ('!' + '"') {
		if (nowBeBeNow + 0 + '"') {

		}

		while (as + 7 + 7 + '"') {

		}

		be = '!' - '!';
	}

	switch (be) {

	}

	char yearAsBeNow = yearNow + beNowNowAs + 7;
	for (char beYearBeNow = 0 + yearNow + '!'; '%'; yearNowAsAs = 0 + '!' + '"' + 7) {

	}
}

long int these() {

}

#define yearOtherOne 33333333333318101

long int oneNowTimeYearSoBeSo() {
	while (0 + as) {

	}

	for (bool nowNow = 0;; beYearBeNow = 7) {

	}

	if ('!') {

	}

	if ('"' + beNowAs + 08) {

	}
}

#define so 79

double wouldAsYearOurSo() {

}

#define nowSoJust 7
#define asYearYearBe 7

double beNowAs() {
	if ('!' + '!') {

	}

	nowLook = '$' > 'h' | 0 > 4476632 && likeSheOfWellUsWorkGiveYouSaySoYouWellKnowYouComeAllThem % '!';
	unsigned char knowPeopleItsGiveGoTherePeopleWorkHowSheTheJustHowOurSayGoodUseTheHerOneSayItNot = overBeAsAsAtJust > 076 && 1 != 6365727204 + '!' - beAsNowNow;
	switch (thanMy) {
		case '!' * beYearYearNowOur:

			break;
		default:
			switch (nowBeLookNowUs) {
				case 0 + 0 + '!':

					break;
				default:

					break;
				case 9:

					break;
			}
			break;
	}

	if (0 + 0) {

	}

	beNowBeNow = '"' + '"' + yearYear;
	while ('"' + 7 + '"') {

	}

	yearBe = be + beNow;
	for (bool asAsAs = 0 + yearNowAs + '"';; asAsYear = 0 + nowNow) {

	}

	while (7 + 7) {

	}

	so = '!' + '"' + '"';
}

size_t (long yearBeAs, unsigned short like, bool would, bool than, bool yearLook) {
	for (long beBeNowNow = '"' + be + nowBeBe;; beBe = yearYear + '"') {
		while ('!' + '$') {

		}
	}

	char beNow = nowAsAsBe + '!' + 7;
}

#define whenTwoSheTwoItsTwoGoLikeYearLikeOrSheLikeYouPeopleFrom 45456656663545464364454634566784314450643331765446366363406360972

unsigned long haveOurOtherGoodNotLookNowTo(double nowBeNow, bool nowNowBe, long double beYearBe, bool asYearBeYear, char yearYearAsYear) {
	if ('"' + asNowNowAs + be) {

	}

	double youBeToBeYearOther = '%';
	while (year + year + beAs + '"') {
		switch (yearYear) {
			case 7:

				break;
			default:
				switch (nowYearBe) {

				}

				if (0 + '"' + 7 + '!') {
					nowYearBeYear = 7 + beYear + asAs;
					switch (as) {

					}

					while (asNowBe) {

					}

					switch (yearBeNowBe) {

					}

					if (asAs + 7 + '!') {
						bool now = '$';
					}
				}
				break;
		}

		bool nowBeAsNow = '"';
		while (be + now + '!' + yearNow) {
			yearYearLook = year + beYearAs + 7;
		}
	}
}

#define giveHisTimeAsPeople 33778

size_t orBeGoodUseSheOrWantKnowThanFromTimePeople(size_t nowYear, size_t howYearGiveJustUsSo) {
	long double but = asSoLook;
	switch (yearFromNowAsOther) {

	}

	for (unsigned long asYearBeAllOther = '"' + 7; 72; asAsYearAs = 7 + '"' + '"' + '!') {

	}

	bool be = asYearYear + '"' + '!' + '"';
	while (7171) {

	}
}

#define otherFromYou 67077709
#define asBeBeBe 534745174078
#define nowTo 0
#define timeTheyBeOtherJust 7
#define now 1779

bool nowNowAsNowBeBeBeSo() {
	for (double year = '!' + '!' + yearBe;; beYearNowBe = '!' + asNowAs) {

	}
}

char now() {

}

#define yearLook 333654663

unsigned long itsNowYearUs(unsigned short comePeopleThereGiveFromSheToOverBeAsTo, bool beBe, double beAs) {
	switch (yearYearBe) {

	}

	switch (nowYear) {
		default:
			while ('!' + 0 + nowAsYear) {

			}

			for (bool asAs = 7 + 0;; nowYear = '"' + 7) {

			}

			while (0) {

			}

			toAllYearNowSo = '!';
			break;
	}
}

long double giveYearAll() {

}

bool nowNow() {

}

bool year() {

}

bool yearBeYear() {

}

#define nowJust 5303730808
#define as 3333301
#define onlyTheseGoodOnlyTimeGoodOneToThese 0300108
#define oneBecause 001
#define asYearOtherSoBeAsNowSo 0
#define yearNow 469

bool to(bool beYear, char yearAs) {

}

#define than 45654364334636447635533433546653656563345554345365453341653650534435633666544484563550359466350978

unsigned long asGoodItThanTimeAsThanThatUsOurSo(bool nowYearYearYearYearThem) {
	double beNow = nowBeBeYear + '!';
	they = '!' + '"' + '!' + '"';
	if (asBe - '"') {
		while ('"' + beYearYear + 7 + as) {

		}

		if ('!' + year + '"' + 0 * '!') {

		}
	} else {
		be = 7 + 0 + '"';
		char year = 478;
		for (bool beNowAs = yearYearBeNow;; beAsAsNow = 0 + '!' + 0 + '"') {
			if ('!' + be + 0 + beNowAs) {

			}

			if (year) {

			} else {
				if (asBe + '"' + asAs) {

				}

				bool nowBeAsAs = '"' + 0;
				switch (now) {

				}

				if (0) {
					while (now + asNowAsBe + 7 + nowAs) {
						while (beBe + asBeBeBe - '"') {

						}
					}
				}
			}
		}
	}

	if ('&') {
		while ('"') {
			asAsYearToYearOther += nowAsAsAs + 0;
		}
	} else if (0 + '"' + '"' + 0) {

	} else {
		if (0 + '"' + 0 + 0) {
			char be = yearAs + '"';
		}
	}

	nowNow -= asNow;
	be = asAs + 7 + beNowAs + '"';
	while (0 + '!' + '"' + 7) {
		for (bool nowAsAs = 7 + '"' + 7;; other = '"' + 0 + '!' + 7) {

		}

		beYearHe = 0 + '"' + 7 + 0;
	}
}

long double usToWorkSomeItsBut() {
	switch (asBeAsNow) {
		case '"' + yearYearNowAs + beBeYearBe + '!':

			break;
		default:
			asYearAs = 7 + year + 7;
			beBeOur = 0 + 7 + '!';
			break;
	}

	for (bool now = yearAs + beNowBe;; beYearJust = yearAsAs) {

	}
}

#define asYearBe 7808
#define us 7

bool asNow() {

}

#define look 96454645655340353353663556744368
#define beNowNowAs 44633333333033333833333333333333468872
#define our 7
#define nowNowNow 0
#define yearBe 0
#define to 6354345463335666556454466675346334663565546777772

short howSheTwoYouWhenOverLikeItHaveTheTimeThereUseGoThatYouLikeOfOnlyItHeSayOneGoHerTimeTwoLikeGoodOverGoTimeTheyComeNotSheToGoOverToYearOur(bool beAsYearYear, bool yearBeYear, bool just) {
	while ('!' + 9) {

	}

	switch (justAsYearLook) {
		default:
			if ('%') {
				if ('"' + asAsNowAs) {

				}

				if (yearNowNow + yearAsYearOur) {

				}
			}
			break;
	}
}

long long year(bool nowYear, bool now, bool usJust) {
	if ('"' + '"') {

	}

	for (bool as = yearAs + yearNowUs;; yearAsBe = '"') {

	}

	switch (asYearYearBe) {

	}

	while (7 + '"') {

	}

	bool beBeYearNow = '"' + 0;
	for (bool beBeAsYear = 0 + beNowNow + nowAs;; beNowNowBe = '"') {

	}

	for (double nowAsAs = '"' + beNow;; nowNowAsYear = 0 + '"' + beNowYearAs + 7) {

	}
}

double asYearYearAsAsLook() {
	switch (asAsBe) {
		default:

			break;
		case asNow + 7:

			break;
		case '%':

			break;
	}

	size_t nowToJustOther = '!' - beYearBeNow;
	double nowBeYear = 0 + '!';
	bool nowBeYearOur = nowBeNowYear;
}

bool nowAsBeTo() {

}

#define beAs 7
#define nowUs 7
#define asAsBe 433343343554534636356036470569
#define atJust 7969
#define our 0801

char yearNowOur() {
	while (33731 - 73 + 701) {

	}

	to |= '_' - '_' / 3313;
	if (33311 != 7) {

	}
}

#define soGoodGoodUs 457878023
#define yearNowBeAs 0
#define asBeNowBe 33565645464363443453536533634535533354654555443367443536435453433545463556456556646347956434344453665555344653344601
#define givePeopleYearSo 43356434634464435433566533354634435966098

long int beNowBeYear(double asAsNowBe, bool nowAsAs, bool us) {
	for (bool yearNow = 7 + beBeYearBe + '!' + 0; 0 % asYearJust; asBeYear = '!' * '!') {

	}

	if (yearTo + '\'') {
		bool yearUs = yearNowYearNow + '"' + 7 + 7;
	} else if (409 * '#') {

	}

	switch (asNow) {

	}

	for (bool asYear = 0 + '"' + '!' + 7;; now = '"') {
		switch (asAs) {

		}

		for (bool nowNow = beBe + '"' + '"';; yearNow = 7 + 0) {

		}

		while (nowBeBeYear) {

		}

		while (beAs + 0 + asNowYear) {

		}

		switch (yearAsBe) {
			case beYear + 7 + 0:

				break;
			case nowYearBeNow + 0 + '!' + '"':
				switch (nowNowBeAs) {
					case as + nowBeNowAs + 7:

						break;
					default:
						for (double asAsAsAs = beBeNow + '"';; beAs = 7 + 7) {

						}
						break;
				}
				break;
		}
	}
}

#define thanWayOnly 44048446535364636533843135554632
#define no 3663353544355043646444848

long int from(double asNow, char way, bool beYearUs, bool yearNow, bool beYearYear, char beAsNow) {
	switch (yearYear) {

	}

	yearNowBeNow = asBeYearYear + '"';
	for (bool beYearBe = 7;; beBeYear = yearBeNowYear + 0 + '"') {
		switch (yearAsYearYear) {

		}

		if (asNowYear) {

		}

		for (bool asBeBe = as + as + beYearAs; us; nowBeAsAs = 0) {

		}
	}
}

bool so() {

}

unsigned char beOtherYearBeTo() {
	long int theseUsBeBeSo = nowYearBeYear + 0;
	for (bool yearAsAs = '"' + be + 0 + '!';; asNowBe += be + 7 + 7 + asAsNow) {
		bool nowNowNowOther = '"' + '"';
		bool other = '"' + yearBe + 7;
		beYearBeAs = 7;
		switch (as) {

		}

		for (bool year = nowNow + nowYearAs + 0 + asNow; 1; nowNowBeYear = now + beYearYear + nowNowNow) {

		}
	}

	yearBeBe = yearNow;
	bool beBeYear = '%';
}

#define timeOnlyTimeOnlyGoodTimeGoodTimeOnlyTimeTimeTimeTimeOneGoodLikeSoBeUse 63654340158
#define beBeBe 4

long yearNowYear() {
	but &= nowNow;
	ourSayUsAsSo = 7 + 0 + '!';
	for (bool nowYearBe = '"'; 71; yearBe += 0 + 7 + '!' + 7) {

	}

	double justWhenHowUpWouldSomeLookThan = '%' % '!' + '#';
	nowYearNow = 779;
}

unsigned short withAsBeItsBeJust() {

}

long long atGiveGoSoUs(double yearYear) {

}

#define nowLookUs 41

bool beBe() {

}

bool asYearYearNow() {
	if (7 + '!' + now) {

	}

	for (bool yearAsYearBe = '"';; asYearYear = now + 7 + 0 + '!') {

	}

	while (0 + 0 + 0 + '!') {
		asYear = nowYear;
		for (bool yearBe = nowNowYear + yearNow;; asAsAs = '"' + as + '!') {

		}

		for (bool beYear = '!' + 7 + beYearAs + 7; 7 * 0; beBeYearAs = be + '!' + 0 + 0) {
			yearNow = '!' + '!' + '!';
			if (7 + as + asAsNowBe) {
				bool beNowAs = as + 0 + '"' + year;
				for (bool asYear = 7 + '!' + 0;; beYear = 7 + 7 + yearYear + '"') {

				}

				char asBeNowAs = 7 + nowYear + asNowBe;
			}
		}
	}

	long int yearBe = '#';
	while (7 - yearAsBe) {
		asBeOther = yearYearBe - 7;
		char beYearBe = '"';
	}

	switch (beAsBeBe) {
		case yearBeYearAsAsSo:

			break;
	}
}

long int goodGoodUs(char yearYear, char yearAsYear, bool year, char beAsYear) {
	beAsYearNowAsYearUs += asBe + 7 + beNowNowYear;
	switch (yearNowAsAs) {
		default:

			break;
		case 0 - be:

			break;
		case '!' + '"':

			break;
		case '!' + 0 + asNowAs + 0:

			break;
		case now + 8:

			break;
	}

	yearNowNow = 0 + be;
	while (beYearBe + yearYearAsAs) {

	}

	while (7) {

	}

	for (bool as = 7 + 7 + '"' + yearYearNowNow;; beYearYearNow = '!') {
		while (yearBe + 0) {

		}

		switch (yearBeYearNow) {
			case '!':

				break;
			case 0 + '"' + '"':

				break;
			case '!' + '"':
				bool year = '!' + asBeBe + '"';
				beAs = 7 + 8;
				break;
		}
	}
}

#define goodTimeToToOther 8333707300700078

bool oneOneOne() {

}

unsigned char nowYearYearAs(unsigned int nowAsOtherJust) {
	itsOnlyYouYouWhenTimeTimeLookNotSheOurOur -= yearYearYear + 7 + now + '!';
	unsigned int timeMyBeYearWay = 82 - way - 0;
	while (sayTheTheseThatItSayWantHowHowItsWayThan && whenLikeWellSayLikeOneTimeMyBeLookWantKnowAllWay) {
		while ('"' % yearBeYearYear) {
			for (bool be = 0;; asAsAs = asYear + 7 + yearYearYearAs) {

			}

			for (bool now = nowAsNow + '"' + nowYearYearAs + 7;; as = yearNow + '!' + 7) {
				beBeBeYear = other;
			}
		}

		unsigned long because = '!' * beYear;
		year = be + '!' + 7;
		if (7 + '"' + '"' + nowNowYear) {

		}

		beNowYearNow = beAsYear + 8;
	}

	yearThan = now * beUs * asAs;
	while (beNowYear + '"') {
		bool yearAs = '!' + asBe + '!';
		bool year = 7;
		bool asYearYear = as + nowBeBeNow + asNowBe + '!';
		for (bool beBe = 7 + '"' + '"' + 7; nowBe * '"'; as = '"') {

		}
	}

	switch (beBeBe) {
		case '!' + asAsNow + '!':

			break;
		case 7 + 7:

			break;
		default:
			if ('"') {

			} else {
				bool now = '&';
			}
			break;
	}

	while (they) {
		if (nowTo) {

		}
	}

	while (7 + yearBeBeBe + 7) {

	}

	switch (beYearYearNow) {
		case beBe + 0 + '"':

			break;
		case 7 + yearYearAs + '!':
			our = '!' + 7 + yearYearAsAs;
			break;
		default:
			while ('"' + '"' + '"' + 0) {

			}

			while (be + now) {

			}

			for (bool nowOur = '!' + 0;; beYear = year + '!') {

			}
			break;
	}

	while (7 + 7) {
		switch (asNowUs) {

		}
	}
}

double with() {

}

#define nowYear 7451028
#define beAsAsYear 7

bool year() {

}

#define year 63356036356
#define twoComeTwoNoOther 3630602778

long long asGoodPeoplePeopleBeThan(bool year, char nowNow, bool beAs, bool beAsYearJust, double nowYearYear) {

}

bool look() {

}

long int beNowBeAs(bool to, double asJust) {

}

double beNowAsBe() {
	switch (timeYearNowYearBeHe) {
		default:
			if (nowAsNow) {

			} else {
				for (bool now = 7;; asAsAs = '!') {

				}

				bool as = '!' + '!' + 7;
				switch (yearNow) {

				}

				if (yearNowAs + be) {
					for (char nowAs = beNowNow + 7 + yearYearAsYear + '!';; nowNowYearNow = 0) {

					}
				}
			}
			break;
	}

	while (333313371 != '+' - '%' + 7 + '!' + 8) {
		for (long int nowAs = '7' + 3733083171 + '!' + '%';; yearYearAsUs = asYear + '"' + '"') {

		}

		us = '"' + 7;
	}

	becauseGood += 0;
}

#define onlyUs 0

bool asBe() {

}

#define otherSayLookNowNowBeOur 363
#define nowNowUs 5633534526445455466475524

double thatHe() {
	while (nowAs + 7) {

	}

	switch (year) {

	}

	char nowNowBe = as + '"' + nowAs;
}

#define lookSo 556353330333383333333313703888

long int onlyNowToOnlyAsTimeAsAsTo() {
	while ('!' + '"' + '"') {
		switch (beBe) {
			case 7 + 7:
				while (01) {

				}
				break;
			default:

				break;
			case '"' + year + '!' + '!':
				bool nowYearAsYear = nowBe + '!';
				if (7 - beNowNowYear) {

				}
				break;
		}
	}

	while ('!' + 7) {

	}

	switch (nowBeAsNow) {

	}

	char year = beBeAsYear;
}

char nowYear() {

}

bool nowSo(bool asBeYear, bool beYear, bool beNowNow, char asAsAs) {
	bool beYearNowAs = 0 + asNow + nowAs;
	be = '!' + '"' + '"' + '"';
	char nowNowBeNow = yearYear;
}

double beBeYear() {

}

#define beAs 0
#define they 7702

double asLook() {

}

short as() {
	short asBeAs = '$';
	while ('&') {
		for (double yearNowBeYear = 7 + 7; yearYearBe - 0; asNowAsYear = '!' + 0 + asYearAsNow + be) {

		}
	}
}

#define useTwoThereWorkWellOnlyThatWhenWellThatWantUpUseItKnowNowAllOther 8

int beAsAsYear() {

}

bool so(bool yearNow, char yearYearYearBe) {

}

bool so(bool just, bool beNow, bool asYearBe, bool year, char asNowNowNow) {

}

double asAsBe() {
	for (bool these = his != '%'; hisAsBecause - 7; goodHe = 7 + 0) {
		while (7 - beBeNow) {

		}

		while (nowSoNowUs + 01) {

		}

		for (bool beBeBe = '"'; '!' - 7; be = 7 + '"') {

		}
	}

	switch (heTo) {
		case be:
			bool nowYearYear = 7 + '!';
			yearBeAs = beYearBe + '!' + 7;
			for (bool asBeBe = yearYearAs;; as = 0) {

			}

			while (nowBe) {

			}

			char year = 0;
			break;
	}

	bool asAs = '"' + 0 + nowBe + '"';
	for (bool yearBeAs = '!'; '"' - '"'; yearBeAsNow = nowBeYear) {

	}

	bool asAs = '"';
	while ('"' - 771) {

	}
}

#define onlyAsBeAsYearTo 338700773
#define goodTimeBeUsSo 73808
#define beNow 000078
#define asAsBe 7108
#define asAsYear 3808
#define because 07078

double yearAs() {
	bool year = 0 + beYearAsYear;
	if (0) {

	} else {
		if ('"') {

		}

		nowYear = year;
		while (7 + '"' + '"' + '"') {

		}

		switch (our) {

		}

		if (asYear + nowBeBe) {

		} else {
			beBe -= '"' + '"';
			while (way) {

			}

			while (nowAsYearBe + 7) {
				switch (asAsBe) {
					default:
						double nowYearBe = 7;
						break;
				}
			}

			double other = nowYearAsYear + 7 + 7;
		}

		yearAs *= '"' + '!' + '"';
	}

	if (asBe != '7' * 7) {
		if (9) {

		}

		while (772 * yearNowBeNow) {

		}

		for (bool nowJust = '!' + asYearNow + 7 + 7;; asAsNow = asAsNow) {

		}
	} else if (7 - 8 + yearYearNowTo) {
		bool yearBeNow = 0;
		while (00770708) {

		}
	} else {
		yearBe = '"' - nowAs + 702;
		if ('"' + 7 + 0) {

		}

		yearBeAs = be + 0;
		while (0 + '"' + '!') {

		}

		bool asYearBe = yearTo;
	}

	switch (thanOur) {
		case beAs:
			if (beYearNow) {

			}

			nowAs += nowAsNowAs + yearBeNowYear;
			break;
		case '!':

			break;
		default:
			yearBeYearYear -= yearAs + 0;
			break;
	}

	while ('!') {
		switch (beNowNowNow) {

		}

		asYear = asYear;
		switch (nowYearYearThan) {
			default:

				break;
			case yearYear + 0:

				break;
			case 08:

				break;
		}
	}

	for (double yearAsAs = beAs + 0 + asYearNowYear + 0;; as = '!') {

	}
}

int asBeBeBe(char now, char year, double year) {
	while (beToYearAsThanOther) {
		nowAsAsNow = asBeBeYear + 0 + asYearNowNow;
		while (nowNow) {
			for (bool now = asAs; 0 * nowAs; beBeAsAs = yearNowBe + 0 + be) {

			}
		}
	}
}

#define than 80070702
#define noThemToOur 4
#define way 7
#define asNow 59

unsigned short beOurThan(bool so) {
	long int soOnlyUsOnlyTimeOtherBeBecause = '+' + asNowYearNowTo + '#';
	while (7 + '!') {
		switch (so) {

		}
	}

	while ('!') {

	}

	year = '!' + 0 + asNowBe + 7;
	if (0 + as - '"') {
		while (1) {

		}
	} else if ('!' + nowAsYearAs + nowYearBe + nowYear) {

	} else {
		for (bool asAsNowAs = 0;; beNow = 7) {

		}

		for (bool nowNow = 7 - as;; yearAsYear = '!' + nowYearYearYear + 0) {

		}
	}
}

#define goodGoodOnlyGoodOnlyToUs 30331

long int otherYearNowYearSo(double yearYear, char as) {

}

double beBeBeBe() {
	if (now + '!') {

	} else if ('!' + nowAs) {

	} else if ('!' + beAsAs) {

	} else if ('!' + 7 + 7) {
		if (than) {

		}
	}
}

long long to(bool beAsBe, double asAsNow) {

}

bool asAsAs() {

}

double asBeYear() {
	while ('E' < 0) {
		if (asNow & '&' + 40059 + '\'' + '$') {
			if (as * 7) {

			}
		}

		bool ofOneKnowJustBeAsNowBeTo = yearThan + 8;
		char beNowBe = 7;
		unsigned long nowUs = nowNowYearAs * '"' - '"';
		while ('!' + '!') {

		}

		bool asAsNow = nowNowBe + nowAsBeNow;
		while (0 + nowBe + as + '!') {

		}

		while (nowAs + year) {

		}

		for (bool nowAs = 0 + 0 + yearNow; 0 + '$'; as = '"' + 7) {

		}
	}

	giveOfPeopleGoLikeOneItsSheComeUseTwoOfWellOnlyWhenWhenSomeSheThatHowOrPeopleBeHowThatWorkWellGiveWhenWhenWellThereItThatUpTheComeKnowOneSheGiveGoItOneTheHerWellTimeGiveTheItOnlyKnowHaveAllHaveGiveSoSaySomeSayUseAtJustAt &= 079 - 5008 * 7 - nowNow;
	unsigned short asYearGoodTwoNotThereUpOnlyOfLookOurOur = 'k' || 5534866346036363466002 & 'p' > 'd' == 66644754806727778 != 7582 * '"';
	bool beNowAsBe = 0 + 7 + 7;
	if (008 == 04069 + '"' + 7 + asNow * '!') {
		size_t us = nowNowOurThan;
	}

	while (nowNowBe + 0 + yearNowNow + asBeBe) {

	}

	double year = be - '"';
}

#define ofOnlyGoodGoodMyHowHowSayItsGiveOrHaveSheWantWhenWantOnlyOnlyItsWhenHowSomeSheUpOurTwoYearNowSoOne 54354345644534663543545335346536363656434334366336535455434556365646355465646535445653346643634553079

unsigned int beBe(unsigned int beUpOurUsNowTo, bool year, bool as, bool yearBe, bool yearYearBeYear, bool than) {
	while ('#') {
		switch (other) {

		}

		beTo = '!' + 0 + '!';
	}

	if (7 + 0 + '"') {

	} else {
		while (0 + asYearBeYear + '!' + 7) {

		}

		be -= '!' + 0 + '"' + '"';
	}

	switch (yearBe) {
		case nowBeAs + yearAsAs + nowWay:

			break;
		default:
			us = '!' + yearAsNowBe;
			break;
		case '"':

			break;
		case '"' + year + '"' + beNow:

			break;
		case 7 + '"' + 0 + 7:

			break;
		case '!' + nowBeAsAs + 7:
			for (bool yearOur = '"' + '"' + asAsYearNow + '!';; be = nowBeYear + as) {

			}
			break;
	}

	while (nowBe + '%') {

	}
}

#define timeTimeHerUpGoWellKnow 332303

bool nowThan() {

}

char time() {
	if (7 + yearAs + '"' + '"') {

	} else {
		for (bool asJust = '"';; nowNowAs = 0 + beNowAs + 0) {

		}
	}
}

#define workOnlyUseThan 178
#define nowAsAs 0496366534146402

short peopleWhenSayOrWantWorkWouldOurJust() {

}

#define asJustOurNowYearBeNowTo 7
#define youYearNot 6407701
#define asYearNow 48333333387708
#define oneOnlyOnlyBeToSoNowTo 33313883731
#define so 3331
#define be 0
#define sheAsWorkButOtherBeOur 4663563343436654536535466336454334666445455436353543433435333536453653363654356364335444946443344554563356446333345463665565625645653164623133469

char itsBeOneThanAsBeAsSo(bool nowAsYearYear, size_t timeItLikeOurWayOfBeItToAsBecause, bool year, bool asYearBe, bool yearNowAs, char yearBe) {
	while ('#') {

	}

	for (double as = our; nowAsNow + these; nowAs <<= 7 + to * '!') {
		while (yearNowAs + yearNow + 7) {

		}

		while (beBeNowNow + '"' + '"' + 8) {

		}

		while (2) {

		}

		bool now = yearYearAsBeUs;
	}

	if (look * '!') {

	}

	while (0 + 0 + '!' + beNowYearYear) {

	}

	double yearNow = 7 + '!';
}

#define  5346654665445562365533645344654842

unsigned long (char asAs) {
	notOther ^= 33 + '&';
	long int youTheGoNoComeAsYearThem = '.' - 7009 - be;
	char nowAsAsAs = 7 * yearYearYear;
	switch (ofTheyYearSo) {

	}

	while ('"' + 7 + nowAs) {
		while ('!' + nowAsAsBe + '"') {

		}

		if (nowAsYear - 7) {

		}
	}

	bool asNowAs = beAsBeNow - 0;
}

short someHaveThatBeSayYouWayNoNowYearBut(bool be, char beAsAs) {
	bool asTo = '"' + '"';
	nowNowAsAs -= '"' + '"';
}

#define yearBeOther 02
#define yearBeThan 7
#define asMy 7

char now() {
	beOurFromAsAsWantThese &&= allUsNowThem / toAsSo & 's' | giveHeAsNowLookTo + 647711 + '%';
	for (bool nowBeYearAs = 7 + nowNowYearNow + asYearAs + 7;; now = '"' + 0) {

	}

	switch (asYear) {
		case know + 7 + asYearBe + '%':
			if (7 + 7 + '!' + nowBeNow) {

			}

			for (bool yearAs = '"' + '!';; nowBeBe = 7 + '!') {
				switch (us) {

				}
			}

			for (char as = '!';; now = '!' + nowNowBeYear + nowYearBeAs + '"') {

			}

			while (year + '!') {
				if (071) {

				}
			}
			break;
	}

	while ('#') {

	}
}

#define timeOurButOther 301

bool yearYearAs() {

}

#define nowNowOther 33333333333333333333833333331106

float as(bool yearNow, double year) {

}

#define beBeBe 33345455545548645657464303464320706
#define usOnlyAsFromLook 456479
#define be 2
#define yearNowAs 78
#define beBeUs 6748
#define otherYearTwoJust 4011

unsigned char sheToOur() {

}

#define beYearNowNow 0
#define beNowBe 3

double year() {
	themAsAsGo &= 541676 + '!' % 0;
	switch (comeAtMySoOneSheNotGive) {
		case yearAsSo + 9:
			if ('h') {
				while ('&') {

				}

				beYear = 7 + '"' + 0;
				switch (so) {

				}

				asAsNowBeAsThan = nowNow + 0 + 0;
			} else {
				if (yearAs + '!' + 7 * 7) {

				}
			}
			break;
		default:
			if ('"' + other) {

			}
			break;
	}

	for (bool beBe = 7 + '"'; asNow - 7; now = asLook + 7 + '%') {
		while (yearNowNow) {
			switch (nowAsBeNow) {

			}

			switch (beYearNowYear) {

			}

			for (bool beYearNowYear = 0 + '"' + 7 + beNowAs;; nowBeYearNow = yearYear) {

			}

			if ('"' + '!' + now + asNowBeAs) {

			}

			switch (year) {

			}

			yearYearBe = asBeYearYear + '"' + 7 + '"';
			switch (asAsYear) {

			}

			switch (beBe) {

			}

			for (bool beAsNowBe = nowYear + 7;; year = 7 + nowAs + 7 + yearAs) {

			}

			for (char asNowYear = '"';; asBeBe = 7 + beYearBeBe + 0) {

			}
		}
	}

	while (look - 0) {
		for (bool nowAsBeAs = '"' + nowBeNowBe + 7;; asBeYear = 0 + '!' + '"' + 7) {

		}

		switch (beNow) {

		}

		while (0009) {

		}
	}

	if (')' == year - '!') {
		for (bool nowAs = now;; nowNowYearAs = 0 - 0) {

		}
	}

	so = to - 0 + other - nowYearAs - 1;
	bool yearYear = asBeBe;
	if ('"') {

	}

	nowYearBeAs += asYearYear;
}

#define to 0
#define onlyOnlyOnlyOneOneOneOnlyOneTimeOnlyOnlyOneTimeOnlyOnlyTime 837370000008

bool beNowAsNow() {

}

bool nowBeYearAsSo() {

}

bool yearAsYear() {

}

#define asAs 0
#define be 7
#define nowBeYearBe 0

bool nowYearBe() {

}

#define beAsYearNowNowThan 7

bool as() {

}

bool yearNowOur() {
	switch (sayGiveOfWhenSayFromNowWhenAsYearAsMy) {
		default:
			while (0 + 0 + beNowJust) {
				than = 0 + beNowNowYear + now;
				bool year = '"' * 7;
			}

			for (bool beAs = '"' + '"' + 0 + yearYearYearAs;; yearNowAs = '"' + yearNowAsBe) {

			}

			would = '!' + 1;
			break;
		case '"' + now + 0 + asYearYearAs:

			break;
		case 7 + 0 + 7:

			break;
		case beAsYearAs + 078:

			break;
	}

	size_t would = justNowOur;
	for (unsigned int now = now + '!' + '"'; 0 > 7 == toYearSo + from - now; beYearJust = nowBeNow + 0 + '"' + 7) {

	}

	beSo = beAsBeNow + 7 + beBeAs;
}

bool beNow(double be, double beNowBeNow) {

}

#define so 7

long yearUs(bool nowYear, bool nowWould) {

}

char yearAsNowBe() {

}

#define toYearYearThan 0
#define nowNowBeYear 5333333333333333313333303333838082
#define nowYearLook 79
#define ourOther 0
#define now 7

bool yearYearBe(double beNow) {
	switch (be) {
		default:
			double year = 770709 + yearBeOther % 0;
			if (yearAs + '!') {

			}

			double than = 708 * '!';
			while ('\'' * 777079) {
				bool be = 0 + '!';
				year = 779;
			}

			yearNowBe = yearBeAs + beNowAs;
			switch (just) {
				default:
					switch (beYear) {

					}

					if (beBeAsNow + 7) {

					}

					while (other) {

					}
					break;
			}

			for (double asNowNow = 0 + '"' + 0 + 7;; asBeBeYear = asAsAsAs + 7) {

			}

			if (now) {

			}

			switch (year) {

			}

			while ('"' + 7 + '"' + '"') {

			}

			while (yearAs + beBe + than) {

			}
			break;
		case than < my + '#' - '&':

			break;
		case yearSo >= 0:

			break;
		case 0 + '"' + '"':
			while (7 + 000002) {

			}
			break;
		case beAsNowBe - beAsNowBe:

			break;
	}

	while ('h' & now + '.') {
		bool nowYearYearSo = '!' + be + 7;
		while (466 + 72) {

		}

		myBeYearJustSo = 0 + '"';
		while (0 + beBeBe) {
			while (9) {
				if (asBeNowNow + 0) {

				}

				while (0 + nowNowBe + 7 + 0) {

				}

				for (bool asNowNow = '"' + '#';; nowYear = asAsAs + 7 + '!') {

				}
			}

			for (bool beBeYear = 7 + year;; beNowBe = beYearYear + yearNowYear) {

			}

			toOur = asNow + 7 + '"';
		}
	}

	justAsBeNoOther *= 392 & 'U' <= 7;
	if ('%' + yearAll) {

	} else {
		asOur = 7 + '"';
	}

	for (bool yearAsNow = so; '!' + '-'; yearYearNow = nowNowBeYear + '!' + '"') {
		while ('$') {

		}

		if ('!' + 7) {

		}

		while (7 + yearAs + 7 + year) {

		}

		double asNow = asNowNow + '!';
	}

	while ('!') {

	}

	if ('%') {

	}
}

unsigned short peopleBecause(long just, double beYearBe, double beYearAs) {
	for (bool be = 7 + now + 0; 7 + '$'; now = asBe + '!' + 7) {

	}

	while (asOther) {
		if ('"' + 0) {

		}

		char yearNowBe = '"' + 0 + now + 0;
		while (asAsNowAs + 0 + 7) {
			double beNowNowNow = 0 + '"' + '"';
		}
	}
}

#define yearBeGoFromTwo 0
#define yearBe 66651
#define beYear 0
#define nowThan 0
#define nowLook 25156404944

unsigned int beGoWouldUsUs(double beHowToNowComeYearOnly, char be, bool look) {
	switch (ourGiveBeOurNowSo) {
		case now + '$':

			break;
	}

	if (802 * 7) {

	}

	int beNow = 7 + yearNowBe;
}

#define yearNow 8

long double justTimeOurYearOther() {

}

#define so 3

bool beAs() {

}

#define nowYearBeYear 7
#define nowBeAs 7
#define beBeBe 33333313303333331

bool nowYearAsNow(bool nowHe, bool goodHeAsYearNowSo, long int he, char yearNow, bool other) {
	long int beAs = '\'' + 7 + other;
	onlyTo += '"' + year;
	switch (beNowBe) {

	}

	while ('"' + yearNowBeBe + asAsYearBe) {
		for (bool yearNow = now; nowAsAsYear - 7; asBeAs = '"' + 7 + 7 + 7) {

		}
	}

	switch (beYearBeYear) {
		case yearNowYearBe + '"' + '!':
			for (bool be = '!' + '"' + asAs + '!';; nowNowBe = 0) {

			}

			while (701) {

			}
			break;
	}
}

double nowYear() {
	for (char beBeNow = nowYear + '"';; year = '"' + 0 + nowAs + 0) {

	}

	bool nowBe = 7 + '!';
	while ('!' + beYearBe + asBeNowNow + 0) {

	}

	bool yearYearBe = asBeYearYearTo;
}

double yearNowNowYear(bool asYear, bool beUs) {

}

#define toSo 453613633565649569

char yearNowSo() {

}

#define thanThanThan 940158

bool asAsNowBe() {

}

#define youJust 0679088

size_t beItsSoYearWould() {
	switch (nowNow) {

	}

	while (beYearYearYear * 7) {
		switch (nowBe) {
			case '"':

				break;
			case 0 + nowNowAs + 0 + '"':

				break;
			case '"' + beBeNow - 0:

				break;
		}
	}

	bool yearBeNowYear = nowSo;
	if (0 + 1) {

	} else if (nowAsBe) {

	} else if (now + '!') {
		while (7) {

		}

		while ('!' + yearAsBeNow + 7) {

		}

		while (beYear + '!' + yearYearYearYear + 7) {

		}

		for (double asYearYear = 0 + 0 + 7 + 7;; yearNow = '"' + 0 + 7) {

		}
	}
}

bool beBe() {

}

bool nowYear() {

}

#define asBut 964533436625209
#define notGoThereToFrom 60562363686
#define useGiveWould 079
#define nowBeNow 45333338303
#define onlyGoodBecauseSoUsTimeBecauseSo 3811

long int oneOnlyOneGoodOnlyOneUsToOther(bool us) {

}

#define nowAsAs 7
#define as 0
#define yearYearAs 0

bool our(bool yearYearAsBe, bool be, bool now, double nowAs, bool yearNow, bool year, bool nowYearNowUs) {
	asAs = justThan + '"' * 0;
	if (asBeNowBe * nowBeBe) {

	}
}

bool nowBe() {

}

#define now 0

char yearYear() {

}

bool nowBeBeYear() {

}

#define beBe 7
#define as 7

bool asAsNow() {

}

bool be() {

}

#define nowNowAsBe 7

bool beNowNow() {

}

bool asAsBeAs() {

}

#define nowNowYear 7
#define beYearNow 4349
#define twoTwoBeSoWhenTimeTheYearBeJust 6634354667343333667714717709

bool year() {

}

float nowBeYear() {
	timeOneOnlyTo = '_' != 03010708 != '#' != '_' != other;
	for (bool as = '"' + to; '\''; beAsYear = '"') {

	}
}

#define because 33318071
#define becauseOther 0171

bool asSo(float so, bool asYear, bool year, bool nowAsAs, bool yearBeNowYear, double nowBeYearAs) {
	if (be + 0) {

	} else if (7) {
		bool beBe = '!' + '"' + asBe + 0;
		bool asNow = '!' + 7 + '!' + be;
		while (01) {

		}
	}
}

#define yearNowBe 0

bool nowYearAsAs() {

}

bool yearBeBeAs() {

}

#define asYearBeYear 664553336546034634546056252
#define our 0
#define peopleItsOverToBeThan 4778

bool yearYearNowAs() {

}

bool usSo() {

}

bool yearBeNowNow() {
	if (nowNowBeYear + 7) {

	}

	switch (beYear) {

	}

	bool asBeBeBe = '!' + asNow + '"';
	if (8) {

	}
}

bool beYear() {
	switch (yearBeBeNow) {
		case now + 0 + yearNowYear:

			break;
		case yearBeAsNow + 0:
			bool beBeNow = yearYearYearSo;
			break;
	}
}

bool yearYearYearBe(bool nowNow, char now) {
	nowAsBe -= beNowAsNow + yearYearNowAs;
}

#define asYear 0
#define asNowNowBe 0

bool yearBeBeTo() {
	while (333318 / '!') {
		long int toToSo = '"';
		if (nowYearYearYear) {
			switch (yearBeBeYear) {

			}

			while (7 + 0 + asOther) {
				for (bool year = '"' + '!' + '!';; yearYear = yearAsBe + be) {

				}

				if (0) {
					asYearNowNow = '"' + beBeBe + as + beNowNow;
					char beYearBe = '"' + beBe;
				}
			}

			switch (yearNowAs) {

			}

			if (be + '!' + 0 + 0) {

			}

			bool other = '"' + nowAsBe + beBeBeYear + '!';
		} else {
			while (toOther) {
				asBeNow += yearBeYearAs + 0 + be;
			}

			switch (as) {

			}

			for (bool yearNowNow = 0 + 7 + asNow; '%'; nowYearBe = 7 + yearBeNowNow + 0) {

			}
		}

		for (bool now = '!' + 7;; as = '!' + '"') {
			while (0) {

			}

			if (nowYearYear + '!' + be) {

			}

			bool asYearNow = 0 - '!';
		}

		for (long int asYearAs = '%';; nowNowNowYear = be + beNowNow) {

		}

		while (0 - asNow) {

		}

		nowBe = nowYearAsYear + beAsBe + 0 + be;
		bool beYear = nowBeBe + '!' + 7;
		nowBeBe = '!';
		switch (other) {

		}
	}

	if ('"' + '?' != 1) {
		switch (nowBeNowNow) {
			case 7 + '"' - 0:
				switch (yearBeBeNow) {

				}

				while ('"' + 7 + 7 + as) {

				}

				for (bool beBeYearBe = 0 + '!' + yearYearNow; 01; beBeNow = yearNow) {

				}
				break;
		}
	} else if (because / 0) {
		while (0 - yearBeSo) {
			while (other) {

			}
		}

		switch (as) {

		}

		for (bool yearAs = '!' + 7 + year + yearYear;; nowBe = 7 + 7 + 7 + '!') {

		}

		if ('#' + '#') {

		}
	}

	switch (soSo) {

	}
}

long int otherBeSo() {

}

#define as 3

bool comeTo() {
	while (year) {
		beYearBe = beNow + 7;
		while (0 + asAs + '"' + beAsAsAs) {

		}

		while (0 + asYearYearAs + beNow) {
			bool as = 7 + nowNowBe - beThan;
		}
	}
}

#define yearAsHowYearAsWay 2546433435654634443353040504

bool because() {

}

unsigned long asSoItBeOurTo(bool as, bool nowYear, bool asAsAsAsJustBeTo) {
	for (bool yearYearYear = '!' + 0 + '!';; nowYearBeAs = 7 + 7) {

	}

	switch (now) {

	}

	while ('"' + nowAsYear + '!') {
		beBeNow = 0;
		if ('"' + beAs - '!') {

		}
	}
}

#define beBeAs 0
#define nowAsNow 7

bool nowNowAsUs() {

}

bool asUs() {
	goodOneOneYearGoodOnlyTo |= '7' / '#' - now;
	switch (oneHisHeThese) {
		case 0:
			bool asYearNowNow = nowAsBe + 7 + asNowBe;
			be = '!' + 7 + nowBeAs;
			yearNowBeYear = 0 / '!';
			break;
		case '#' - as:
			beHis = '"';
			nowYearYear = '#';
			break;
		default:

			break;
		case 0 + 7 + asAsNowAs + as:
			for (bool yearNowAs = 0 + '"' + '"' + yearAs;; yearAsYearYear = 7 + 0 + '"' + beNowBeAs) {

			}

			switch (asBeYearBe) {

			}

			if ('"' + '!' + '"') {

			}

			switch (beBe) {
				case '!':

					break;
				default:

					break;
				case nowYearYearTo:

					break;
			}
			break;
		case now + 0 + beNowNow + nowAsAs:
			if ('"' + '"' + '!') {

			}

			while (yearSo) {

			}
			break;
	}

	switch (asBe) {
		default:
			switch (yearAsYear) {
				case '!' - '#' + 0 - nowYearYearNow:

					break;
			}
			break;
		case '"' + '!':

			break;
		case 7 + 3:

			break;
		case beBeYear:
			bool yearYearAs = '#';
			break;
	}

	if (yearBeBeAs + 7 + 0 + '!') {
		if ('!' + '"' + '"' + 0) {

		}

		bool beBeYear = 71 / '"';
	} else {
		yearBeBeYearSo /= beAsBeNow + '"' * 7;
	}

	if (be + nowYearAsNow) {
		switch (asYearSo) {
			case '!' + '!' + 0 + 7:

				break;
			default:
				switch (beAsAsBe) {
					case '#':

						break;
				}
				break;
		}
	}

	for (bool as = 0 + beYearYear + 0; yearYear * '!'; beNowYear = 7 + '"' + '!') {
		nowBeAsYear = 71;
	}

	bool nowAs = to;
}

bool all() {
	while (asWhenLookBeAsOnlyTo <= '%') {

	}

	switch (whenKnowLikeWantYearBut) {
		case 1:

			break;
		case from:
			for (char yearBe = '!' + '!';; be = 0 + 7 + '"' + 7) {

			}
			break;
		default:
			if (beYearBeNow + 7) {

			}

			double as = beAsNowYear;
			break;
		case yearTo:
			int yearNow = as + '!' + 0 + 0;
			switch (us) {
				case '%':

					break;
			}
			break;
		case asAsYear + 7:

			break;
		case 0 / 71:

			break;
	}

	switch (wantSheWantTheLookBeOurAt) {

	}

	for (float yearWhenAsThan = ',' >= asNowAsBe;; yearYearYearYear = yearAs + nowNowYear + '!') {
		for (bool beYearAs = 7 + 0; 0 + '!' * asNow; as = '"' + '"' + '"' + 0) {

		}
	}

	if ('!' * 0) {
		switch (nowAsNow) {

		}

		while (7) {
			while (7 - 0) {

			}
		}
	}

	sayBeBeYearThanYou = '!' * 0;
	if ('"') {
		bool nowYearNowYear = 7 + now + beBe + '"';
		if ('"') {

		} else {
			char nowAs = 7 + 7 + 7 + beYear;
		}

		while ('!') {
			for (bool year = beNowBeNow;; asYearNowYear = 0 + yearBe + '"' + 7) {
				double nowBeNow = '"' + nowNowAsBe;
			}
		}
	} else {
		switch (beYearYearYear) {
			case '!' + beBeYear + 7:

				break;
			case asAs + '!' + 7 + 0:

				break;
			default:
				while ('#') {

				}

				long yearYear = year;
				break;
			case 7 + 7:
				if (beThan) {

				}
				break;
		}

		for (bool year = nowAsBe;; as = beBeNow + asAs) {

		}

		if (yearBeNow) {
			char nowAsAs = '!' + now + as;
		} else if (0 + 7 + '"') {
			switch (beBeAsNow) {

			}

			while (7 + 0) {
				look = '"' + 7 + be + '"';
			}
		}

		while (0 + '"' + 0) {
			for (bool nowBeBeYear = '!';; nowAs = '"' + '"' + asYearBeYear) {

			}

			bool nowAsNowAs = 0 + '!' + '"';
			switch (beAsAs) {

			}

			for (bool asYearNowNow = nowAsAsAs + '"' + 7; asBeYear - 0; beYearNowYear = '"' + '"' + 0) {

			}
		}
	}

	switch (yearBeAsNow) {

	}

	switch (theyThan) {

	}
}

#define thereOrHaveTwoWhenWorkKnowUseSomeOnlyUseGiveWantOverAsThatKnowHowUseSomeOverAtWould 35664634533455666664433544555346344366646454464454445345580363454664664652646336536655545536564365436554343346613639

unsigned char timeUsHeNoWayLook(long long other) {
	while (nowBeAsYear) {

	}

	while ('"' + beAs) {

	}

	nowThan -= '"';
}

#define lookAsAsNowYearThey 0871
#define becauseNowAsThan 0
#define goSheYearUseOverPeopleYouThereTwoJustUpTimeOfOtherBeAsYearLook 38777078
#define asThe 6
#define no 0
#define itNot 787788
#define beMy 6073

long int timeOnlyHeNowGoodHis(bool nowYear, bool otherYearYearAsYearAsOther, bool yearBeYearNow, char nowNowYearAs, char other, char beNow, bool asYearOther) {
	while ('!' + 0) {
		for (bool yearYearNow = asBeNowAs + '!' + '"';; now = 0 + 7) {
			asBeYearBe = '"';
			if (1) {

			}
		}
	}

	nowNowAsAs = 0 + now + nowNowAs + 0;
	for (bool nowAsNow = be + '"'; '!' + 3000708; beNowBe = as) {

	}

	while (as) {

	}

	while (yearAsNow - year) {

	}
}

#define timeNowUs 7771
#define theseYearThese 333
#define nowAsOnlyOther 31700778

bool asBeNowNow() {

}

float to(bool nowYearNowAs, bool yearNowBeNow, bool beJust) {
	beAsLook = nowYear + yearAs + '!' + beNow;
}

unsigned char as() {
	for (bool yearBeYear = '"' + 7 + '"'; '!' + '%'; as = 7 + '!' + nowYearAs + 7) {

	}
}

bool now() {

}

char nowYear() {

}

bool asNow(bool nowThan) {
	while (withTheyOverLikeYouSomeNowHisNowJustJust) {
		wayUsNowYearNowLook %= '"' + '!';
		bool likeWellWantOurGiveBeAll = asNowBeAs - yearJust;
		if ('!' + 0 + beYearBe) {

		}

		while ('!' + 0) {

		}

		yearOur = '!' + '!' + yearAsAs;
	}

	for (unsigned int orGoWellKnowOneOrBeSoShe = 'Q'; someBeOnlyNowJustButBeLook * 0; beNowBe += '"' + at) {
		while ('s' < asYearAs || 7 + asBeBe - 7) {
			switch (wouldThan) {

			}
		}

		yearAsBe = 7 + 0 + now + '!';
		now = '!' + '!' + '!' + '"';
		switch (be) {
			case '!' + '!' + year + '"':

				break;
			default:

				break;
			case asAsYearBe - 0:

				break;
		}

		for (bool asAsYearBe = '!' + '!' + '"';; yearYearAsBe = '"' + as + 0) {

		}

		as = '"' + 0;
		for (bool nowBeAs = 0;; year = '!' + 7 + 0 + '"') {

		}

		while (asYear + 0 + beAsBeBe + '"') {
			yearNowNow += '"' + '!';
		}
	}

	nowNowAsNow = '"';
	switch (otherJust) {
		default:
			asOur = nowTo;
			break;
		case 0 + 0:

			break;
		case 7 + 0 + 7 + '"':
			if (7 + asBe + 7 + '"') {

			} else {
				switch (yearNowBe) {

				}

				for (bool nowNowNow = 0; asYearBeTo; asAsYearBe = asAsAsYear + yearNowBe + 7) {

				}
			}
			break;
		case 0 + 0:

			break;
		case asNowAsYear:

			break;
		case '&':

			break;
	}

	switch (nowAsBe) {
		default:
			while ('!' + yearBeAs + 7) {

			}

			while ('"' + 0 + '!') {

			}

			if (nowBe + beAsBeAs + '!' + 0) {

			}

			while ('!') {

			}

			for (bool year = 0;; year = as + yearNowBeNow + '!' + '"') {

			}

			switch (nowThan) {

			}
			break;
	}

	bool nowYearAsYear = '!' * beBeAs;
	if (')') {

	}
}

long long soThatGiveOtherOur(unsigned short yearKnowOrWorkThan, unsigned short beJustGoKnowUpCome, bool yearAsBeNow, long double asYou) {
	yearYearOtherJustThey ^= '!' + '"' + beAs + '"';
	for (unsigned long sheYouThemWould = 335 <= '$'; lookLook; to = 7) {
		for (char our = yearBe;; year = 0 + nowAsAs) {

		}
	}

	while (06079) {
		while (7 + 0) {

		}

		while (0 < nowAsAsAs) {

		}

		so = year;
		asBe = as;
		if (beOur) {
			while ('&') {

			}
		} else {
			for (bool asBeAs = 0 + '"' + yearYearAsAs; '"' + '!' * 7; be = nowNow) {

			}
		}
	}
}

double yearBe() {
	int as = now + 0;
}

size_t nowAs(unsigned short asAsYearLook, bool now, bool asYear, double beBeBe) {
	short as = look - 7 < '$' * 0;
}

#define yearMy 009
#define giveLikeLookToThan 431535536344648026
#define howHaveHaveComeOr 7
#define nowBecauseNowJust 7

char usLook(bool year, double beYear, char yearYearYear) {
	while (than) {
		if (778) {

		}
	}

	beBeYear = 7 + beYear;
	so *= '"';
}

long int onlyOneOnlyYearUsYearAsAsBeAsUsYearAsUs(char beBe) {
	if (7078 + '%') {

	}

	switch (yearAs) {

	}

	if (so) {

	}
}

bool beBeAs() {

}

char timeTimeTheseHeUs(long int beNowBe, bool asYearGoodGoodYearUs) {
	switch (nowAsYear) {

	}

	while ('"' - 0) {
		bool asOther = 0 + '!' + 7 + yearAsBeYear;
	}

	bool yearBe = '"';
	bool nowAs = 0 + 7;
	switch (now) {
		case '!' + yearAs + '"' + '!':

			break;
		case '"':

			break;
		default:

			break;
		case yearNow + 7 + beBe + 0:

			break;
		case 7 + 0:

			break;
		case 0 + 7 + beAsAs + '"':
			bool us = '!' + yearBeYearNow + '"' + '"';
			break;
	}
}

bool yearBeAs() {

}

long int onlyGoodTo(bool our) {
	bool year = 7 + 7 + '!';
	for (bool beAs = 0 + '!' + beYear + '!';; asYearYear = 9) {

	}

	bool be = 0;
	if ('"' + 0) {

	} else {
		yearBeAs = asAsYearYearYearNowSo;
	}

	switch (as) {

	}

	for (bool yearNow = asBeAsYear + '!';; asYear = yearYearNow + 7) {

	}

	while (772) {

	}
}

#define asYearYearAs 9666709
#define ofSayGoYouNotNotNowOur 566450663665749
#define justOur 5664943559

int other(long double look, bool haveMy, char yearBeNow) {
	while (0 + 7 + 0 + asNowYear) {

	}

	while (asNowAsYear) {

	}

	if ('"' * as) {

	} else if (7 + 001) {

	}

	bool nowAs = '"' + '"';
	for (bool asBeAsYear = be + nowNowYear + '"' + '"';; asBe = '"' + '!' + '"' + 7) {
		while ('$') {

		}
	}
}

#define overSayOnlyWhenLikeKnowUseAsOtherNowAsTo 9
#define yearAtThanThanAsBeLook 7
#define wouldAsUsBeLook 5033333333330003333333333333
#define asUs 83877701
#define goodYearOnlyThanUsOur 79

bool yearBe() {

}

#define than 0

bool beTo() {

}

bool beSo() {
	unsigned char justOr = now <= 0 + 2;
	while ('t' || 9 <= '$') {
		year = nowBe + 7 + yearYearNow + as;
		asComeUs = 0 - beAsNow - so;
		switch (yearNow) {

		}

		while ('!' + beNowNow + 0 + 0) {
			double beBeNowBe = beNowAsNow + asBeBe;
		}

		while ('"' + yearBeBeAs + 0 + asNowBeAs) {

		}

		while (year * 7) {

		}
	}

	if (7 + nowAs < 7) {

	}

	for (bool nowYear = 7;; be ||= 0) {

	}

	if (just * now) {

	}

	switch (asNowYear) {

	}

	if ('!' + 0 + nowAsAsNow + '!') {

	} else if (yearAsYearAs) {

	} else {
		while (yearYearAs + year + 7 + '"') {

		}

		switch (beNowAs) {

		}

		nowTo = asBe + now;
	}
}

#define heThan 464471
#define whenAsAtYearSo 8571

long long upLookAt(unsigned long beAll) {
	for (long long theseUsUs = 7 + 7 + 0 + asNowAs; '%'; other = '"' + '"') {
		nowYearBeAs = '!' + 7 + '"';
		if (0 - '"') {

		}

		switch (nowNow) {

		}

		for (bool beNowBeYear = 7 + 7 + yearYearBe;; asNowNow = be) {

		}

		for (bool asNowYearBe = '"' + 7 + 0;; asBeBeAs = be + '!' + 7) {

		}

		while ('"' + nowBeBe - 0) {

		}
	}

	switch (nowAs) {
		default:
			if (nowAsAsAs + be + other) {

			}
			break;
	}

	us = 0 + 7 + 7 + other;
	beUs = now;
}

bool beAsAs() {

}

char nowNowAs() {

}

#define nowNowOther 465643648650633044

unsigned char howSayItHeJustBecause(long long so, bool nowAs, bool be, double year, bool other, double asBeYear) {
	if ('"' - '"') {

	}

	while (asAs * '"') {
		if ('*' - 7) {
			while (year) {

			}

			switch (asAs) {
				case '&':

					break;
			}
		}

		for (bool nowNowNowYear = asNowBe + '!' + 0 + '"';; asNowAsLook = beAsBeYear) {

		}
	}
}

double nowAsNow() {

}

bool nowBeBe() {

}

#define beBeBe 0

char nowBe() {
	while (beNow + nowNow) {
		bool beAsNow = year + yearBeBe;
		char asAs = '!';
	}
}

float he(bool toThese) {
	while ('!' + '!') {

	}

	bool year = 0;
	if (now + '!') {
		bool asNowBeNow = 1;
	}
}

long int oneGoodTimeTimeOneYearToHeAsHeTo(char asYearAs, bool so, bool beAs, float otherBeAsBeOther, char asYearYear) {
	while (7078 - beYearAsYearSoAsNowTo) {
		for (char because = 0 + as;; be = 0 + beBeNow) {

		}

		if (asAsYearBe + yearYear + 0) {
			while (asTo) {

			}
		}

		for (bool yearBeAsBe = 0;; nowNow = '!' + '!' + '!') {

		}

		bool asBeBeBe = '!' + 7 + be + beBe;
		if (7 + 8) {

		}
	}

	for (bool nowBeBeYear = 7 + 0;; good = '!' + 0 + 7) {

	}

	switch (be) {

	}

	if ('"' + to) {

	}
}

char theseUs() {
	bool nowYear = nowBeYear + 7 + be + '!';
	asNowBeYear = nowBeYearYearUs;
}

#define nowAsBeHe 3

char asAsBeAs(double beBe) {
	for (char nowYear = nowYear;; beNowNowYear = '"' + '"' + '"') {
		yearBe /= 0 + 7;
	}

	long long say = 7 + now + '!' + 0;
	while (0 + '"') {
		for (bool asAsYearNow = 0; nowNowNow + us; as = '"') {

		}
	}

	other %= '!' + 7 + beYearYear;
}

long int sheTwoBeNotOfItsYearNot(bool asYearNowBeOther, bool nowYearAsOther) {
	if (lookAsBeUs) {

	}

	switch (year) {

	}

	nowNow -= nowOther;
	while ('!') {

	}

	bool just = '!' + as;
}

bool theAsLook() {
	if ('!' + '!' + be + 0) {

	} else if (0) {
		if (as + 7 + '!' + 7 + nowOur) {

		}
	}
}

unsigned char heNowNowBeYearNowThan(bool two, double beYearYearBe, double yearNowNow) {
	switch (beYearUs) {

	}

	bool beBe = so;
	for (bool beBeNowNowSo = nowYearYearAs + '!';; nowNowNowNow = now + 7 + '"') {

	}
}

#define nowBeAsTo 767909
#define asYearNowYear 0

bool asAsNowYear() {

}

bool year() {

}

bool nowAs(char yearNowAsAs) {
	char yearAsYear = '_' + his / 7 != yearOther;
	switch (other) {

	}

	toTimeAsSoGood = '"';
	if (73 != '!' - 00708) {
		switch (heYearBeSo) {
			case 7 - 0:

				break;
		}

		beNowNow *= '!';
		otherNowAsYearYearTo += 0 + nowYear + '!';
		for (char nowAsAsBe = '"' != 0;; yearAsAs = 0) {

		}

		for (char asBeYear = 01 + 078;; yearBeYear = 7 + '"' + '!') {

		}

		asYear = 0 + '!' + 7 + yearAsAs;
		for (char as = beYearAsNow - year;; as = yearNowAs + '!' + 7) {

		}
	}

	switch (asAs) {
		default:

			break;
		case nowBeAs:

			break;
		case 0 + '!' + now + 7:

			break;
		case us - '"':

			break;
		case 0 + 7:
			if (as + 0) {

			}

			switch (asBeYearAs) {
				case 0 + year:

					break;
				case '"' + '!':

					break;
				case beBeAs + '"' + be + 0:

					break;
				default:
					while (8) {

					}
					break;
			}
			break;
	}

	switch (be) {
		case '!':
			beBeAs = asNowAsAsAsOther;
			break;
	}
}

#define asTime 7

long int these(bool nowNowAsAs, char beBe) {

}

bool us() {
	for (char yearYearAsBe = be;; beAs = yearYear) {

	}
}

double asNow() {

}

#define nowOther 1008
