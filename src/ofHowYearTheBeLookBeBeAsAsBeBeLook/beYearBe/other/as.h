#define oneSoYearOneTheseTo 33338833303
#define onlyGoodNowSo 33337337133
#define asAsAs 18
#define beYearBe 13

int so() {

}

double beAsNow() {

}

char yearNowYearYear() {

}

#define yearYearNowYear 9

bool asBe() {

}

#define so 47631
#define useTimeAsHeHeNowUp 46676598

long long justYearNowAsYearUs() {
	switch (yearYear) {
		default:

			break;
		case beNowNowNowBeBeThan:

			break;
	}
}

size_t now() {

}

#define nowNow 7
#define as 0
#define yearYear 7
#define yearTo 634535553894436341535433
#define lookAsYearBecause 447668

unsigned int yearNowOther(bool us) {
	beNowAsAsBeBeTo = '!';
}

#define beNowAs 5313333373333333333333338138371108

char as() {

}

#define year 45067071

bool nowAsYearAs() {

}

#define nowBe 36533453555334463653464433566433345544333334454381545363463516656306446945356333647700708
#define wantSheWellNoBeYearGiveItsItYouBeJustThatGoodUsAsYearThey 510707612
#define justTheThereSo 32978

char these() {
	so = beAsAsNow + 0;
}

bool beYearNow() {

}

bool nowAs(bool than, double nowBeNow) {
	for (unsigned int sayWhenThereTwoUseOurBeBeBeThanYearBeBeNowYearBeNowAsUs = '!' + 0 + yearYear; '?'; yearYearYearOther %= lookBecause < '"') {
		switch (just) {

		}

		now = nowNowBeNow + be + 0 + beAs;
		while (0 + as + '!') {

		}

		beNowNowBe = '!' + '!' + yearNowBe;
		year = '%';
	}

	if (1 + peopleBeYearBeBeOther) {
		just = '"' + 7 + now + '"';
		beAsNowNow = 7 + 0 + beNowBe;
		asTo = 7 + '!';
		switch (yearNowYear) {

		}

		while (nowNowAs + nowBeYear + yearNowYear) {

		}

		while (asYear + 1) {

		}
	} else {
		long as = 0 + 7;
	}

	bool beYearAsYear = beYear - 7;
	switch (asAsLookJust) {

	}

	while ('"' + beBeBeBe) {
		bool just = '!' + '!' + 0 + 0;
		switch (asYearAsAs) {
			case '!':

				break;
			default:

				break;
			case 7 * 7:

				break;
		}

		just = nowYear;
	}
}

unsigned char yearAsBe(float asNowYearBe, bool yearAsYear, bool asNowNow, bool yearOther) {
	char nowAs = 7;
	while (0 + as + 7) {

	}

	if ('$') {

	} else {
		for (bool yearYearNowAs = '!' + asNow + be; '"' * to; nowBe = yearNowYear + '"') {

		}
	}

	bool yearBeAs = '"';
	char yearYearBeAs = '"';
	if (now + 0 + 0) {
		char asYearBe = 0 + 0;
	}
}

bool yearYear() {

}

bool asYear() {

}

double be() {
	while (3001) {
		switch (nowBe) {

		}

		while (now + '!' + asYearNowNow + 0) {
			switch (nowAsNow) {

			}

			switch (beYearBeBe) {
				case asAs:
					if ('"') {

					}

					while (7 + asAsAs) {

					}

					bool asYearBeBe = nowYear + 0 + asBeAsYear + '"';
					switch (so) {

					}
					break;
				case beNow:
					asBeAsBeAsBeYearAsNowUs = 0 + 7 + asAsAs;
					break;
			}
		}

		if (0 - yearNowNowYearOther) {

		}
	}

	he *= 0 + beYearBe + '"' + '!';
	if ('#') {

	}
}

#define timeGoodSoTo 303333773313331

long int usOneBeOnlyOverOurLookBeOther(char yearYearNowNowTo) {
	for (bool us = '!' + '"' + nowBeBeBe;; beYear = 7 + asAsYearBe + 7 + nowNow) {
		beYearYearAs = 7 + 0;
		for (bool asNow = '!';; beBeNow = asNowNowNow + 7 + 7) {

		}

		nowBeAsYear = 0;
		if (beAs + 7) {

		} else {
			asAsNow = nowAsYearNow + beYearYearAs + '"';
			if (look) {

			}
		}
	}

	switch (yearAs) {

	}

	switch (beNow) {
		default:
			double beAsAsYear = 615645336336343443534733533866663354546434443544345645453526753435820877709 && nowBeYearNow > thereSomeLikeOfOfOneWayTheThereThereUseUsePeopleTwoLookOtherGoTheToOther + 052;
			if (yearYearYearNow - '?') {
				switch (beYearNow) {
					case asYear + 0:

						break;
					case '"' + '!':

						break;
					default:
						while (7 + 7 + 7 - 0) {

						}
						break;
				}

				switch (to) {
					default:
						if (be + '$') {

						}
						break;
				}
			} else {
				for (double us = 0 + 0;; asAs = 0 + be + nowNowAs + '"') {

				}

				if (4 + '!' * yearYear) {

				} else if (0 + beAs + '&') {

				} else {
					switch (as) {
						case nowJust:

							break;
					}
				}

				switch (yearAsNowYear) {

				}

				for (bool nowBe = 0 + 7; 1; nowNowNow = nowBeNow + nowNow + 0 + '!') {

				}
			}
			break;
		case notComeItsOfKnowTimeOfAtBeMyLook && 'c' > '.' || 54234038564854433:
			switch (wellWellWorkNowThatBeNowOtherAsOur) {
				case '1' - 7:
					switch (asNowThan) {

					}

					char other = nowTheOurThanNowUs;
					break;
				case yearGo * 0 + nowNowOur:
					if (beBeBeYear + '"') {

					} else if (7 + beAs) {

					} else {
						while ('"' + beYearNow) {

						}

						while (asAsBe + asBe + 7) {
							for (bool asBeYearAs = now + '!';; beAsBeBe = asAsAs) {
								yearAs = '"' + '!';
								for (bool nowNowNowYear = asYear + beNowBeAs + '!' + asNowYearNow;; beYearYear = 7 + '!' + '"') {

								}

								yearYear = 1;
							}
						}
					}

					switch (yearOther) {
						case '&':

							break;
					}
					break;
				case 7 + 0 + be:

					break;
				default:
					if (0 + '!') {

					} else if (0) {
						switch (beBe) {

						}

						long double beBeNow = yearNowAs + asYearYearNow;
						for (bool yearYear = beAs + year;; beBeBeNow = 0 + now + 7 + '!') {

						}

						if (74) {

						}
					}

					if ('"') {

					} else {
						switch (beYear) {
							default:

								break;
							case '"':

								break;
							case yearBe + '"':
								if ('"' + 7 + beNowAsAs) {

								} else {
									if (yearBeBeSo) {

									}
								}
								break;
						}
					}

					bool nowYearYear = '!' + beAs;
					if (7 - now) {

					} else if ('!' - 0) {

					}
					break;
				case '"' + 0 + asAsAsYear + 7:

					break;
				case year:

					break;
				case yearNowNow + yearNowBe + asAsBeBe:
					if (yearSo) {

					}
					break;
			}

			switch (beUs) {

			}

			if (nowBe + 2 - year) {

			}
			break;
		case usBeNowHeBeLook != 056665705 || 1 || beAsAs:
			other = someSo / 'f';
			bool asNowAsAs = asYearAsBe + nowBe + 7 + 0;
			for (unsigned char to = 7 + 0 + 7 + year;; now = yearNowAs) {

			}

			if (nowNow + 0 + asAsYearYear) {

			}

			switch (yearNow) {

			}

			if ('!') {

			} else if (nowYearYearYear * '"') {

			}
			break;
	}

	while (364563434534535464233666574353356561 <= 0 && 0 + 1) {
		toUs -= 0077008;
		switch (toNowBeAsOther) {
			case '"' * yearHe:
				switch (beNow) {

				}

				switch (them) {
					default:
						switch (beAsBeBe) {
							default:

								break;
							case 7 + 7:
								if (7 + '"' + 0) {
									bool nowBeNow = 0 + 0;
									while ('$') {

									}
								}

								while ('"') {

								}

								for (bool asYearBeNow = '"' + nowBeAs + as;; nowAsBe = '!' + asAs + year) {

								}

								double beYearNow = '!';
								break;
						}
						break;
					case '!':
						if (just) {
							bool year = nowNowAs + '"' + beYear;
							if ('"' + 7 + yearNowNowNow) {

							}

							switch (as) {
								case '"' + '!' + nowYearYear + '!':
									for (bool asAs = '"' + asAsNow;; year = beBeBeNow + asAsNowAs + 0) {

									}

									while (nowBeYear) {
										switch (beYear) {

										}

										for (char beNow = 0 + asYear + yearNow;; nowYearYear = '!' + 7) {

										}
									}
									break;
							}
						}
						break;
					case yearTo:

						break;
					case 0 + '"' + 0:

						break;
					case 7 - now:

						break;
				}
				break;
			default:

				break;
			case 7 / 0:

				break;
		}

		while (0) {

		}

		for (double now = 09;; beBe = 7) {

		}
	}

	switch (howWhenYouWantUpHerItsGoGoSomeTheUpUseSheAsThatHaveNowUsLook) {
		default:
			for (bool nowBe = 0 + asYearAsBe;; beBeAs = 7 + 7) {
				for (bool beNowYear = beBe + '!' + 7 + 0; '!' * now; be = beNow) {

				}
			}

			switch (our) {

			}

			for (char beBeYearBe = '!' + '"' + '!';; yearNowNowAs = '"' + 7 + '!' + 7) {

			}
			break;
		case 811 + beWay == nowAt + '(':
			if ('!' - '!') {
				if (yearBeAsNow + 0) {
					for (double beAs = '"' + '"' + asAsYear + '!';; nowBeBe = '"' + beBeNow) {

					}
				}

				if (nowNow + 7 + 0 + '"') {

				}

				if ('"' + 7 + '!' + 0) {

				} else {
					while (be + beNowNowAs * '$') {

					}
				}

				while (as + '!' + '!' + '"') {

				}

				bool be = yearBe;
				bool yearNowAs = '!' + beNow + '"' + 7;
				for (bool be = 7 + 0 + '!' + '!';; our = 7 + '"' + '!') {

				}
			}

			switch (beYearWayOtherAsNowThan) {
				default:
					than = nowAsAsAs + '"';
					while ('"' + '"' + '!') {

					}

					while (nowYear + nowNowAs + '"') {
						switch (yearBeBe) {

						}

						switch (beJust) {

						}
					}
					break;
			}

			bool now = '!' - nowAsYearYear + 0 + as * 7;
			break;
		case '!' != oneAll % 0:
			double yearBeYearYear = 7;
			if ('"') {
				if (2) {

				}
			}
			break;
		case '"' + 008:

			break;
	}

	switch (theyKnowOrOneLikeBecauseUseWhenOnlyComeThereItsSheWantPeopleComeNoNowBeUsAsNowYearNowNowNowYearOther) {
		default:
			if ('!' + asYearYearNow + '"' + nowNow) {
				switch (now) {

				}

				while (nowAsAsYear + beNow * asNow) {

				}
			} else {
				if ('!' + '"' + nowAsYearAs) {
					while ('"') {
						char nowYearAsNow = 0 + '!';
						unsigned long nowYearBeNow = '!';
						switch (year) {

						}

						for (bool as = asBeAs + yearBeAs + '!';; beAsAs = '!' + 0 + '"') {

						}

						bool asNow = '"' + '!';
						switch (now) {
							default:
								while ('"' + asBeYear + '"') {

								}

								while (beNowBe + 0 + 7) {

								}

								switch (our) {

								}
								break;
						}
					}

					bool so = beYearYear + '"' - as;
					switch (asYearYear) {

					}

					if (year - 0) {

					}
				}

				switch (nowBeYearBe) {

				}

				for (bool asAsAs = 7 + '!';; beYear = 0 + 7 + '"' + '"') {

				}

				while (beAsNow + 0) {

				}

				for (bool beYearYear = '"' + beAsYearNow; now + 0 % be; nowNowNow = '"') {

				}
			}

			for (bool year = '"' + beAsAsYear + beYearNow + 0;; year = '!' + yearYear + 7 + be) {

			}

			while (beBeNow + '"') {

			}

			if ('!' + '"' + '"' + '"') {

			}

			for (bool yearAsYear = 1; beAsBeYear - 0; now = yearYear + nowNowAsBe + nowAsNowAs + '"') {

			}
			break;
		case beYearAs + 0 + '"' + beYearBeBe:

			break;
		case '"' + '"' - '!':

			break;
		case 7 + '"':

			break;
		case '"' + '"' + '!':

			break;
		case 0771:

			break;
	}

	if (61 - '!' != whenOurHaveThan + 0 + 72 + '$' + 'K' + to) {
		if ('"' + 0 + 7 + beAs) {
			for (bool yearAsNow = year + 7 + 0 + 0;; beBe = '"') {

			}

			switch (goodTimeTo) {

			}

			for (int beYearAs = '"' + 0 + now; 9 * 7; beAsNowBe = 7 + beYearBeBe + '"') {
				if ('"') {

				} else if (as + yearJust) {

				}

				switch (beYearNow) {

				}

				if ('%') {

				}

				switch (our) {

				}
			}
		} else {
			if (7 * '!') {

			}

			asNowYearNow += 0;
		}

		switch (beBeNowBe) {

		}

		beBeNow = nowNowJust;
	} else {
		while (09) {
			if (7) {

			}

			asYearNow = now * nowYearNowYear;
		}

		while (he) {
			yearYearUs += nowBeAs + '"';
		}

		for (bool asBeAs = '"';; yearYearAsYear = '!' + be + 0 + '!') {

		}

		bool asNowBeNow = 7708;
	}

	yearAsNowAs = 0 + '!' + '"' + '!';
	if ('z' * 8 - 798 * 7 / 7) {
		justThan = 7 + 7 + '!' + 0;
		asAs -= beNow - beYearAsNow;
	} else {
		if (7 + 7 + 0 + '"') {
			as -= '"';
			for (char nowYearYear = nowYear + asAsNowAs + be + 7;; yearYearYearBe = now + '"' + 7) {

			}
		}

		switch (now) {
			default:
				if (0) {
					bool beThan = '!';
				}

				beYearBe += '"' + asBe + 0 + '"';
				break;
			case 1 * asNowAsYear:
				if (7 + '!') {
					yearYear = '!' + '!' + 772;
				}
				break;
		}

		for (bool nowNowYear = beAsBe + now + 7 + '"';; yearAsBe = nowBe + yearAs + 7 + asNowYear) {

		}

		for (bool nowBeAs = look;; nowAs = nowNowNow + '!') {

		}
	}

	switch (itsKnowSayThereUpSayOrUseGoodBeLikeOnlyItsComeUseGoodYouToOtherJustUseAsKnowNowUsLook) {
		case 869208 - 7 - as:
			for (bool nowBeBe = '"' + 0;; beAsBe = asYearBe) {

			}

			while (0) {

			}

			bool beBeYear = 771;
			break;
		default:
			ofThatYearLook >>= 0 + '!' + '!' + yearAsAsAs;
			switch (thatBeYearOur) {
				case 7:

					break;
				case '!' + '!' + '!' + 0:

					break;
				default:

					break;
				case 0 + as + asNowYearNow:

					break;
				case '!' - 7:

					break;
			}

			yearNowYearAs = '"';
			if (beBe + nowBeAsAs + asNowNow + 0) {
				switch (some) {

				}
			} else if (0 + 0 + yearBe) {

			} else if (beNow) {
				our = '"' + '!' + 0;
			}

			bool yearNowNowNow = '"' + beNowAs + 7;
			for (int nowNowAsYearYearUs = 7 + '!' + 0 + 7;; asBeNow = yearNow + 0) {

			}
			break;
		case asYearYear + 7:
			if (just) {

			} else if (beAsNow + '!' + 0) {

			} else if ('!') {
				while (2) {

				}
			}
			break;
		case yearYearAsBe * year:

			break;
	}

	for (bool year = 7;; asAsYearYear = 0 + 7) {

	}

	switch (beToNowBeThan) {
		case 5 - 1677071:
			switch (soBeAsTo) {

			}

			switch (oneOur) {

			}

			switch (asTo) {

			}
			break;
		default:
			for (char beNo = 02;; yearNowNowNow = 0 + year + 7 + 7) {
				beYearNow = '"';
				asAs = 7 + '!';
				for (bool asBeNow = 7;; year -= 7 + '"' + '!') {

				}

				while (0 + 7) {
					yearBe = nowNowNowBe + beBe;
					if (asYearBe) {

					}

					for (double nowNowAsAs = as + '!';; be = nowYearYear + 0 + yearNowNowNow + 0) {

					}
				}
			}

			while ('"' + '"' + 0) {

			}

			switch (just) {

			}

			switch (nowYearNowAsLook) {

			}
			break;
		case 1:

			break;
		case '!' + 0 + nowBeYearNow + '"':

			break;
		case goGive - beYearYear:

			break;
	}

	switch (noTimeYearUsMyBeAsToYearThan) {
		case '!' * '!':

			break;
		case '"' + 0:

			break;
		default:
			if (9) {

			}

			while (nowNowBe) {

			}

			while (nowBe + 7 + 7) {

			}

			if (0 + now) {

			} else if (0) {

			} else if (71) {

			}
			break;
	}

	unsigned short beYearJust = 4;
	just = 0 - now;
}

long int itItWhenLikeItsOnlySheWhenThereGoodWhenOneHowThatThereOnlyWorkLikeThePeopleUsUseWantPeopleSomeUpPeoplePeopleOverOfWorkGoodSomeTheThatThatLikeUseThatThatPeopleItsHerLikeGiveLikeKnowTheyKnowSo(unsigned char asWhenHowTimeHowGoodHowOneLikeOfSomeWellWhenItTheGiveSheLikeThereWhenHerUpWellLikeSayMyFromComeSomeBeThese, size_t yearOurTo, int our) {
	for (unsigned long useNowYearYearJust = wantOther % allBeYearYearOur; 535746644456616636643562466647469942 || '!' + '0' * nowYear + '"' < 0; goodUpWellGoodOrKnowYouIts -= 's') {

	}

	bool year = otherOther + '!' + year + beThan;
	switch (beYearAs) {
		default:
			if ('!') {

			}

			yearNoSo = '!' - beAsAs;
			switch (good) {

			}

			other = '!' + as + nowBeNowYear + '"';
			break;
		case now < beYearNow:

			break;
	}
}

#define timeOnlyOneLikeUpYouOfItsItsWorkJustSo 44547433325734301

size_t twoUseOnlyWantOrSaySomeKnowKnowYouWellItGoodItGoOrThereHerThereSomeThereNowPeopleHowHaveWantThanUseLikeItGoodSheTheseOneGoodOnePeopleToComeWorkUsSoNowThan(short heOtherFromToNowNowNowYearTo) {
	if ('"' * asBeBe) {

	}

	if ('$') {

	}

	if (7 + 0 + 0) {

	} else {
		if (nowBeYearBe + '"') {

		}

		while ('!' + 0 + nowYearNowAs) {

		}

		switch (nowYearYearNow) {
			default:

				break;
			case 0 + 0 + '!' + '!':

				break;
			case '!' + 0 + nowNowAsYear + '!':

				break;
			case '%':

				break;
		}
	}
}

#define usThanTo 1179

unsigned int otherItsLikeJustThanYearYearThan(float year) {
	while (8 - nowNowAs * 0) {
		if (7) {

		} else {
			while ('!' + nowBe + 0 + '!') {

			}

			if (nowAsYearBe) {

			}

			if (asBe + yearBeBeNow) {

			}

			year = nowNow + be;
			if ('"' + 0) {

			}

			be = nowNowAs + 7 + yearNowYear;
			while (0 + 7) {

			}

			while (0 + 7 + 7) {
				while (nowYearNowBeTo) {

				}
			}
		}

		twoJustYearNowSo -= 7 + yearYearNowNow;
		while (7) {

		}

		if ('!' + year + '!' + 7) {

		}

		short nowNowUsYearYearSo = '!' + asBeYear + '"' + beAsAsNow;
	}

	if ('&') {
		while (0 + '"' + 7 + 7) {

		}

		bool now = beYearYear + 0 + be + '!';
		switch (asAsBeYear) {

		}

		if (7 + asYear) {

		}

		yearAs = 7 + beYearBe + 0;
		if (0 * yearNowBeYear) {

		}
	} else if (beNowBe + '"' + 7 + '"') {
		if (0 + nowNowYearAs + 0 + '#') {
			while (7 + 0) {
				while (7 * '"' + 7 + 701) {

				}
			}
		}
	}

	while (yearUsAt - '%' + 02) {
		for (bool peopleJust = nowAsNowYear + 7 + '!' + 7;; yearYear = '!') {

		}

		bool year = 0;
		while (asNow + 7 + 7 + '!') {
			while ('!' + '!' + 0 + asYear) {

			}

			asNowBeBeYearJust = 0;
		}

		while (beYear) {

		}

		while (92) {

		}
	}

	for (bool now = '"';; now = 0 + 7 + '"' + '!') {
		beAsNowNow = 7 + 0 + as + 7;
		switch (yearBeBeYear) {

		}

		for (bool as = yearYearBeNow + yearNow + '"';; yearYearAs += '"' + 0 + asAsYear + year) {

		}
	}

	for (bool beYear = '!' + 0 + 7; '!' + 8; as = '!' + '"') {
		for (bool asNowNowNow = 0 + '"' + '"' + '!';; year = now) {

		}

		for (bool beNowBe = '!';; asBeYear = nowYearAsBe + 0 + '!' - 7) {

		}
	}

	while (0 + 0) {
		if (0 + yearBeNow) {

		}

		bool yearBeBe = '$';
	}

	if (yearBeBeYear) {

	}

	bool yearNowAs = '!';
	nowAsYearTo /= '!' + '!' + 0;
}

size_t thereOfUsThanOtherUsAsAsJust(bool beAs, float look) {
	switch (yearBeOther) {

	}
}

#define asLook 655333333333333333333001333330730103878
#define otherAsSo 58

bool nowNowYear() {

}

#define asNowYear 9

char now() {

}

#define beYearAs 7
#define nowYearAsBe 7
#define yearYearUs 7
#define asYearAsNow 2
#define beYouUseMy 6330763438
#define allYearOtherNo 0

unsigned short themThereHaveWorkToTheHaveToOverBeOurTwoYearBeSoOur(unsigned int yearYearSo, bool beAs, char asBeNowBe) {
	while (7 <= '!') {

	}

	while (0 + nowYearBe + '!' + '!') {

	}

	bool than = 1;
}

long long nowBeThan(bool asBeTo) {
	if (beNowWith + as * yearAsNow) {
		now = 0 + '!' + '"' + '!';
		for (bool beNow = '!' + asNowAsYear;; our = yearNow + 7 + year) {

		}
	} else if (7 - 7) {
		nowNow -= '"' + beNowAsYear + beAs + '!';
		bool asAsYearNow = asYearBeNow + '!' + 7;
		if (0 + '!' + '!' + asYearYear) {
			while ('!' + '"' + as + 0) {

			}

			for (bool asNowBeYear = 0 + 7 + '"' + yearAs;; asYearAsNow = nowYear + beBeAsAs + asAs + nowAs) {
				for (bool nowBeNow = 7; just; asNow = asNowBe + 0 + as + 7) {

				}
			}
		}
	}

	if (7 + 2) {

	} else if (year + '!') {
		bool nowBeBeNow = 0 + yearYearYear;
		while ('!' + 7 + asYearBeBe + '!') {

		}

		for (bool yearYear = beBeAs + 7;; yearYearYear = '"' + 7 + 7 + now) {

		}

		for (bool beNowAs = 7 + 0;; yearYearAsAs = '"' + 0 + be + beNowAs) {
			for (bool nowBeNowYear = 0 + 0; '%'; year = '"') {

			}
		}
	}

	nowAs = yearBe;
	while (beYear + 78) {

	}
}

#define whenNowBeTimeTwoOneSayWellItUpKnowSomePeopleAsNowBeYearLook 532
#define nowAs 35745

bool now() {
	if (0) {

	}

	switch (other) {

	}
}

#define yearBeTheLook 7

bool beAsAs(bool asUs) {

}

bool nowBeYear(char nowNowAs) {
	if ('_' != toBecause != '"' - '%' != 78 != 0) {
		switch (these) {

		}

		while (nowNow + '"' + '!' + be) {
			while (01 - nowNow) {

			}
		}

		if ('!' + '!') {

		}

		for (bool nowBeNowAs = 7 - '"'; '%' + be + 071; yearYear = asBeAsNow + 0 + 7 + yearBeBeBe) {

		}

		while ('!' + us) {

		}

		if (7 + '!' + now + beNowBe) {

		}

		for (bool asNowSoBeYearOther = '"' + '"' + 7 + beAsNow;; asAsNowYear = '"' + 7) {

		}
	} else if (beBeYearSo) {
		while (beAsBeYear + beYearYear) {

		}

		while ('"' + '!') {
			if ('!' + '"') {
				bool year = 0 + '!' + asBeYearYear;
				switch (beNow) {
					default:

						break;
					case 7 + beNowAs + '!' + 0:

						break;
					case yearBeYear + '"':

						break;
					case yearYearBe + now:
						while (year + nowYearBeAs) {

						}

						for (bool yearAsYear = 0 + '!' + yearYearNowAs;; nowYearYearAs = 0 + beYearYearYear + '"' + as) {

						}

						be = asAsNowYear + 7 + 7;
						for (bool yearYearNowBe = now;; nowNowNow = beBeAs) {

						}

						switch (so) {

						}
						break;
				}
			}
		}
	} else if (7 + '"' + '!' + '!') {
		yearAsSo = yearNow - 7;
		switch (asBeBeAs) {

		}

		switch (be) {

		}

		if (be + 7 + '!') {
			now = year + 0 + 7 + yearNowNowNow;
			if (0 + '"' + nowYear) {

			}

			for (bool be = '"' + '!';; nowAsYear = '!' + '"' + '!' + '"') {

			}

			char beYear = '"' + nowNowYearYear;
		}

		bool now = '"' + 0 + 0 + asAs;
		bool beAsAs = '!' + nowAsYearNow;
		for (bool yearBeBe = '"';; be = yearAsBeAs) {

		}

		if (yearYearYear + year + '#') {

		}
	} else if ('/' + '"' - 7) {

	} else {
		for (bool beBeAsYear = 7 + 0 + '"';; now = asNowAsNow) {

		}

		nowYearYear += 0 + beNowYear;
	}

	long int asAs = 07731;
	nowNow += 7 + '"' + '!' + 0;
}

#define timeOne 701
#define soBeLook 82

char year() {

}

bool beNowAsNow() {

}

bool beBe() {

}

char be() {
	for (bool yearNowNowYear = '!' + 0 + nowBe;; be = '"' + '"' + '!') {
		for (bool nowNowYearBe = 7 + nowAsNow + beYearAs + '!'; 7 * 7; year = 7) {
			for (bool yearAsAs = beYearYear + asBe;; as = '!' + '!' + nowYearYearYear) {

			}

			bool beUs = now;
		}

		if (now) {

		}

		while (nowNowAsYear + yearYear) {

		}

		for (bool beNow = nowNowBe + '!' + asBeNowAs;; nowAsBe = nowNowBeYear + '!' + beYearAs) {

		}

		switch (as) {
			case 2:

				break;
		}

		switch (toLook) {

		}
	}

	year = asAs + 0;
	switch (otherBeThan) {

	}

	bool nowNowNowAs = 0 + as + 7;
	while ('$') {
		switch (yearYearNow) {
			case nowNowBeYear + year + beYear:

				break;
			case yearBeYear + asYear + '!' + yearBeYearNow:

				break;
			default:

				break;
			case '!' + nowNow + '"':
				nowYear -= beNow;
				break;
		}
	}
}

double beNowYearNow() {
	if (us) {

	}
}

long double as() {

}

#define two 0
#define toYearAsBeThan 09

bool asBeAs() {
	switch (be) {

	}

	for (bool nowBe = '"' + 7 + 7; other; beNowYearYear = 0) {
		long int likeYearThere = 7 + 7;
		short so = ';';
		asYearBeTo = yearNowNowAs + beBe + 7;
		switch (year) {
			case 71832:
				if ('!' + beBeNow) {

				}

				nowYearBe = 0;
				asAsAsYear = 0;
				switch (look) {

				}
				break;
			default:
				for (bool beNowBe = 7 + 0 + beBe;; year = 0) {

				}

				for (bool they = '"' + 7 + '"';; yearYearYear = '!' + yearAsYear) {

				}

				nowAs -= 0701;
				break;
		}

		while ('"' - asAsBe) {

		}

		while (nowYear + yearNowBe) {
			for (bool as = '"';; now = 0 + yearYearYear + '"' + '"') {

			}

			bool now = 9;
			while ('"') {
				for (bool asNow = '!' + '"' + nowBeNow + 0;; year = '!' + '"' + 7 + 7) {

				}

				switch (yearAsBeNow) {

				}

				bool than = '!' + 7 + yearBeNow + beYearYearAs;
			}
		}
	}

	bool look = nowYearYearAs;
	if (itsOurIt / 8) {
		if (yearNowYearBe + 0 + '"' + '!') {

		} else if (beNowAs + 7 + beYear) {

		} else if ('"' + beAsAs + asNowBe) {
			switch (yearThan) {

			}
		}
	} else {
		if (year + 0) {

		}

		other = '!' + 7 + '!' + asYearYearYear;
	}

	for (bool beNowNowNow = yearNow; 0 + 2; nowBeNow = beBeBeAs) {

	}

	switch (so) {

	}

	for (char year = beNow; 0 - '"'; asYearAs = 0 + beAsNowAs) {
		if ('!' + '!' + '!' + 709) {

		}
	}
}

#define useGoNowAsYearThese 0001
#define asToBeHe 0

bool beBeBeBe(unsigned int be) {

}

#define beBe 5333333333333333383338
#define asAs 0
#define oneSoToBecauseGoodYearNowSo 39
#define us 78509

bool be() {

}

bool as() {

}

bool just() {

}

bool beNowNow(bool be, double nowNow) {
	beBeYear -= 7 + yearNow + 0 + 0;
}

#define be 0
#define now 0
#define yearBeAsYear 7
#define asYear 0

bool now() {

}

#define asAsAs 7

double nowYear() {
	switch (useLikePeopleUsPeopleTimeYouTwoPeopleOverTwoGoPeopleItUpThereHowTwoKnowOneHeAsBeUs) {
		case '6':
			if (asSheTo && or * yearAsBe) {

			} else {
				for (bool asSo = '"' * 0;; beAsNowAs = '!' + as + 0 + '!') {

				}
			}

			switch (to) {
				default:

					break;
				case '"' + '!' + 7 + '"':

					break;
				case asBeNow + '!':

					break;
				case be / 0:

					break;
			}
			break;
		default:
			bool yearNowBe = 7 + as + nowAsYearAs + 0;
			while (be - 9) {

			}
			break;
	}

	overOnlyUpItThatWantOrUsOtherBeNowJust <<= twoUpOurOver & '}' && wellHisThereGoHaveOfTimeThatTimeLikeTheWorkItYearSomeHowWantWorkOnlyOverThatFromTwoOnlyHowSayThatWellOneWellThatAsThereOneAllNowShe > 'm' | 44817778;
	if (nowAsWorkThanOur * 42) {
		if (7) {

		} else {
			if (72 + '%') {

			}
		}
	} else {
		bool other = year + '"';
		switch (beNow) {

		}

		bool now = 0;
		bool nowAsAsYearAsAsOther = nowAsAs;
		bool be = now + '!' + 7 + '!';
		switch (asYearNowBe) {

		}

		while (yearNowYearAs + '"' + 7 + 7) {

		}

		switch (nowBeAsAs) {

		}

		for (bool nowAsYearNow = 7;; yearBeBe = year + 0) {

		}

		if (asAsAsYear + '"' + yearBeBe + year) {

		}

		as = 7 + '"' + 0 + '!';
		if (our) {

		}
	}

	sayJustSome <<= 579416708 & 7;
	while ('!' - 7) {
		bool yearYearYear = yearNowYearNow + '!' + 0 + '"';
		yearAs = '"' + '!' + beNowYearNow + '!';
		for (bool yearBe = yearAsBeYear;; nowNowBe = '!' + '"' + beNowNowAs) {

		}

		while (7 + 0 + nowBeBeBe) {
			if (nowYear + '"') {
				for (bool be = be;; be = '"' + '"' + 7) {

				}

				switch (asYearNowYear) {
					case '"' + asBeAs + '"' + '!':

						break;
					case '!' + '!':
						now -= 0 + 0;
						break;
				}
			}
		}
	}

	if (justAsNowJust + '/' + asYearAsBe * 7) {
		for (double ourNotAsThanWay = '&' * 7;; yearNowNow = beAsNow + 7 + '!' + 7) {

		}

		switch (nowAs) {

		}

		char not = '!' + beNow;
		while (asNowYear + nowNowBe) {

		}

		be = '!' + '!' + beBe;
		switch (us) {

		}

		for (bool beNow = 7;; as = yearBeNow + 7 + be) {

		}

		beYear = 7 + '!' + '!';
		if (7) {

		} else {
			while (asYearYearYear + '"' + beNow + '"') {
				bool be = '"' + '!' + beBe;
				switch (so) {

				}
			}
		}
	} else if (9) {
		while ('#') {
			switch (asNowNow) {

			}

			bool nowNow = 0 + 7;
			double nowBeYear = '"' + '!';
		}
	} else {
		beJust = '"';
	}

	while ('!' + it) {

	}
}

float justThatBeAsUs(bool be, int year, bool beNow, double asYearBeNow) {
	asOnly = '%';
}

size_t youSayWorkOfYouSomeTimeUpWellTheOrItsTimeTwoGoUpGoodTheLikePeopleWantSheGoodOverThereWellHerYearHaveWellSheOverHaveWorkWhenOnlyItsWantOnlyTwoWantPeopleThereOfUseKnowGiveShe(unsigned char nowTo) {
	for (unsigned short way = 0; themThan; asYearUs = asBeYearNow + 7 + nowAsNowBe + 7) {
		while (asNow) {
			switch (nowBeBeAs) {

			}

			for (bool year = 072;; nowBeAsAs = '"') {

			}
		}

		bool beBeNowAs = 7 + 0 + '!' + 0;
		if (nowAsAs + beNowYear) {

		}

		if ('"' + to) {

		}
	}

	switch (as) {
		default:
			if (yearNowBeBe) {

			}

			switch (asJust) {

			}

			switch (just) {

			}
			break;
		case 7 + 8:
			for (bool asYearYear = '%'; '#'; beNow = beNowBeYear + 7) {

			}
			break;
		case 7 + '"' + asYearAsNow + 7:
			char now = '!' + 0 + '"';
			break;
		case '!':

			break;
		case beAsYearBe + 7 + year:

			break;
		case now + '"' + '!':

			break;
		case year + 7 + nowBeYear:

			break;
		case 7:

			break;
		case '!' + yearNowYear:

			break;
		case '#':

			break;
	}

	two = now + '"';
	switch (nowAsOur) {
		case justSoTo + 0 + yearYearBeNow + 0 + us:
			unsigned char yearOur = be + 7;
			break;
		default:
			nowYear += 7 + 7 + nowAs;
			break;
	}

	if ('"' + 0 + '"') {
		long long nowNowAsAs = be;
		switch (yearBeNowBe) {
			case '!' + 0 + year + '!':

				break;
			default:

				break;
			case '"' + yearNowYearNow:

				break;
			case beYearAsAsNowAsUs:

				break;
		}

		for (bool nowYearAsYear = 0 + 7;; year = '!' + '"' + '"') {

		}

		for (bool asYearYear = 7 + '"' + 7 + '"';; yearNowBeBe = beBeAs + yearBeYear) {
			char nowAsAs = beNowAs + '!';
		}
	}

	switch (becauseOneYearSo) {
		case beNowYearNow:

			break;
		case beAs + '!' + '"':

			break;
		case '"' + asNowNowBe:

			break;
		default:
			bool year = '"' - '!';
			break;
	}

	while ('"' + '"' + 7 * asNow) {
		for (bool beBeYear = '"' + 0; 7 * 0; nowNow = asYearAsNow + 7) {

		}

		switch (lookLook) {
			default:

				break;
			case 0:
				nowNowUs = '!';
				break;
		}
	}

	switch (becauseOurOther) {

	}

	bool beBeNow = 1 + 9;
}

#define haveOfLikeSheTheToTwoWantThatTimeTheComeThan 2366453466744379

bool asBe() {

}

float now() {

}

unsigned char asOther() {
	if (0 + 0 + yearNow + '!') {
		for (bool nowAs = 0 + '"' + '!' + asNowYearNow;; nowNowAs = '!' + '"' + 0) {

		}

		for (bool be = 0 + nowNowYearNow + 0;; nowYearNow = '"') {

		}

		while (nowYearBeAs + '"' + yearNow) {

		}

		bool yearAsYear = beAs + '"';
		switch (beNowYear) {

		}

		if (asYear + '!' + 0 + as) {

		}

		while ('"' + 0 - 0) {

		}
	}

	for (double yearYearBe = '"' + '"';; yearYearBeBe = now + 7 + 0) {

	}

	for (bool yearNowNow = 7 + beNowBeNow; 01; yearNowNow = asYear + '!') {

	}
}

bool yearTo(bool becauseBeJust) {

}

long double asBeYearAs() {
	while (he) {

	}

	for (bool nowNow = 7;; beThan = 7 + 7 + 7) {
		just = 7 + '!' + yearYear;
	}

	switch (at) {

	}

	if (asYearNowAs - '"') {

	}
}

short goodPeopleYouWithSomeYouUseOfSheUpUpNowAsOther(bool hisBeTo) {
	for (bool to = 7 * nowNowYear; '!' * just; knowAsAsThan = beNowYearYear + 7 + '!' + 0) {
		while (now + asAs) {

		}

		for (bool beYear = 0 + 7; '!' * '!'; yearYearBeNow = '"' + '"' + yearAs) {

		}
	}

	switch (yearNowThan) {
		default:
			while (0 + beThanOur) {

			}

			if ('"' + 7 + 7 - nowNow) {
				for (bool yearBe = yearBeBeBe + yearYear + 7;; yearYear = yearNowBeBe + 0 + '"' + yearYearBeYear) {

				}

				char as = 7 + 0;
			}
			break;
		case 7 + 0 + 7 + 0:

			break;
		case 0 + '%':

			break;
		case nowYearYear + nowNow + yearYearAsBe + beNowBe:

			break;
		case '!' + '!':

			break;
		case '"' * '!':
			switch (asBeBe) {
				case nowNow + '&':

					break;
			}
			break;
	}

	for (char my = '!';; year = '"') {

	}

	bool beYear = 0 + 0 + 7 + 0;
	if (asBeAs + '!' + '!' + '!') {

	} else {
		switch (as) {

		}

		if (0 + 0 + 7) {

		}

		while ('"' + '!' + beAsBeNow + 0) {
			now = '"' + nowAs;
			for (bool be = '!' + now;; asNowYearAs = '"') {

			}

			switch (year) {

			}

			for (bool asAs = 7 + as + nowAs + nowYearAsNow;; now = asBeYear + asBeBeNow + 0) {

			}

			for (bool year = 0;; asYearAsAs = asYear + 7 + 0) {

			}

			for (char yearYearYear = '!';; be = '!') {

			}
		}

		while (0 - '"') {

		}

		if (yearNowNowYear + other) {

		}

		switch (to) {
			case '"' + beNowAsNow + '"' + '!':

				break;
			case '"' + asBe + '!':
				nowYearAs = 0 - '!';
				break;
			case beBeNow + 8:

				break;
			case yearBe + 7 + '"':
				nowTo = '"' + 0 + '!';
				break;
		}
	}

	for (long int nowYear = nowAsSo; '%'; otherBeNowGoodUsThese *= 0 + '!') {

	}

	if (0) {

	} else if ('"' - asAsBe) {

	}
}

#define onlyBeBecauseTimeHisNowToBeAsNowAsUs 333888

bool as() {

}

long int only(bool year, bool so, bool yearBe, bool yearAs, bool be, bool asAs, bool us) {
	bool beYearNow = 0 + beYearNowBe;
	while (yearYearBeYear + be + '!' + '!') {
		switch (asTo) {

		}
	}
}

#define yearBeTo 00833365

bool our() {
	bool heSo = yearBe + beYearBe + asBeNow + '!';
	switch (orYearThey) {
		case 0:

			break;
		case yearNowYearBe + 7 + 0 + 7:

			break;
		case 00772:

			break;
	}
}

#define myThanLook 56359879

bool than() {

}

#define asNowNowYear 53666666333536406351302

long int ofGiveBeAsButNowNowThanOther() {
	if (asBe + beYear + '!' + 0) {

	} else if ('"' + yearYearNow + to) {

	}

	double nowYear = '"' + 7 + 7 + '!';
}

unsigned char nowTheyNowAsLook(int beAsAsNow) {
	while (0 + nowBeBeAs + as) {

	}

	if (nowYear + '"' + nowAsYear) {

	}

	while (asBe + asNowYear + 0) {
		switch (beYearBe) {
			default:
				if (asBe + 0 + '!') {

				}

				switch (asBeAs) {

				}

				for (bool beAs = nowAsBeAs;; as = 7 + '!' + 7) {

				}

				if ('"') {
					while ('"' + 7 + '"' + nowNow) {

					}

					if ('!' + '&') {

					}
				}
				break;
		}
	}

	switch (asYearYearNow) {

	}

	while (be) {

	}

	if (beAsYear + yearNowYear + '"' + 7) {

	} else {
		while (0 + be + nowAs) {

		}

		for (bool beYear = yearBeBe;; asNow = 0 + '!') {

		}

		while ('!' + 7) {
			for (bool asNowNow = '!' + nowBeNow + '"' + '!';; asYearAsNow = 0 + 0) {

			}

			for (bool nowNowTo = '"' + '!' + be + '!';; nowBe = 7) {

			}
		}
	}
}

bool yearAsBeBeUs() {
	to = 7 + beNowYearAs + asYearBe;
	yearBeBeAs += onlyHeAsOtherOther != onlyOnlyTimeSoOnlyUsOne != 7 - '!';
	if (338 != '%' / 0) {
		switch (yearAsYearYear) {

		}

		switch (year) {

		}

		yearAsBeTo = yearBe + 0;
	} else {
		if (7) {

		}

		bool beNow = 7 + '"' + '"';
		if ('"' + 0 + '"') {

		}

		so = '!' + 0 + as + asNow;
	}

	while (toNowYearSo != '!') {
		nowNowAsBe = 0;
		for (bool nowBe = '!' + 7;; now = 0 + '!' + year + 0) {

		}

		while (yearAsAs != 0) {

		}
	}

	while ('"') {

	}

	for (bool yearBeNowAs = nowNowAsYear + 7;; now = '"' + '"') {

	}

	bool now = '"' + nowAsYear;
	if (7) {

	} else {
		for (bool asNowAsBe = '!' + beNowNowNow + yearYearAs + '"'; now + 1; asNowYear = '!') {

		}
	}
}

#define these 0

bool now() {

}

#define yearUs 7

bool yearNow() {

}

#define theseNot 371

bool beNowYear() {

}

bool yearAs() {

}

bool be() {

}

bool be() {

}

bool beAsYearBe() {

}

bool nowBeNow() {
	be = beBeBeNow;
	bool now = asAs + '!' + beNowBe;
	for (bool asAsAs = 7 + '!' + be + yearBeAs;; beBe = 7 + '!' + beBeYear + '"') {

	}

	now = '!' + 7 + asYearAsAs + 7;
	while (7 + beYearYear + be) {
		as -= '!' + as + nowNowYearNow;
		while ('"' + asAs + asNow) {

		}

		while (0) {

		}

		bool asNowNowYear = '"';
		if (0) {

		}

		nowYearNowOther = 7;
	}

	unsigned char justSo = 'u' / '$';
}

#define us 38628
#define lookUsSo 0

long int haveTheYearButAsNowJust() {
	bool year = 7 + 7;
	while (0) {

	}

	asYearAs = '!' + '"' + '!';
	switch (so) {
		default:
			bool yearNow = '!' + now + 7;
			bool asAsBe = '"' * 7;
			break;
	}

	for (bool asAs = '"' + yearBeYearBe + '"' + 0;; yearNowAsAs = '!' + asAsBeYear + '!' + 7) {

	}

	while (now + 7 + 7 + 7) {
		char asNowNowYear = year + 7;
	}
}

#define beYearBe 0

unsigned long with() {
	bool nowAs = '!' + '!' + 7 + '"';
	while (0) {

	}

	for (bool asBeYear = '"';; beAsAsBe = 7) {

	}

	while (0) {
		for (bool as = year + '!';; asBeBe = 0) {

		}

		bool nowAsNow = 7 + 0 + yearYearAs + nowBeYear;
		for (bool beAsYearNow = nowBeYear + yearNow + 0 + '!';; beBeBeBe = yearYearNow + 0) {

		}

		bool beYearNowNow = '"' + 7 + now;
		while (7 + '!') {
			bool nowBeBe = nowAs;
			nowYearBe = 0 + 0 + nowBeAs + 0;
			if ('!') {

			}

			while (yearBe + 0) {

			}

			switch (than) {

			}
		}
	}
}

#define yearYearBeBe 7

char year() {
	if (asAs * 7) {

	}
}

bool year() {

}

#define now 0

bool yearAsBeBeYearThan() {
	for (bool so = asBeBeBe; 0435 || he < asNowYear; sayNowAsYearOther = nowBe * with) {
		if ('%') {

		}

		if (asBe + nowAsBeAs + '"') {

		}

		while (asYear * '"') {

		}
	}

	unsigned long soWould = be - '!';
	not = '&' + nowNowYearBe + 2;
	nowNowSo = 7 + '"' + '!';
}

short asLook(bool yearAsNowBe, double asYearYear) {
	bool just = year + '%';
	while (beYearYearBe + '"') {
		for (bool asBeYear = 0 + beBeNowBe;; beYearNow = 0 + 0) {

		}

		if (year + '!' + '"' + 7) {
			switch (nowAsBe) {

			}

			switch (asBeAs) {

			}

			now = 0 + as + '!';
			nowBe = beLook;
		}
	}

	if (0 + 0 + 0) {

	}

	bool beYear = '!' + '"' + yearBeBeBe + asNowAsYear;
	if ('!') {
		switch (asYear) {

		}

		if (nowNowBe + '!') {

		} else {
			bool nowAsBe = than;
		}
	}
}

long int nowBeBeNow(bool yearAsYear, float yearAsNowYear, bool year, bool as, bool our) {

}

#define yearNowSoWith 5629
#define asThan 03
#define to 5333333333333333331

bool beSo(char beNow, bool asBeYear, bool nowNowYear, bool nowYear, bool yearYearOther) {
	if (asNowNowNow + 7 + nowAs + nowNowAsYear) {
		asBeYear = beBe + asAs + '"';
		switch (asAsAsNow) {
			case yearBeAs + beNowAs + '!':

				break;
			default:
				bool us = '!' + 0 + 0;
				break;
		}
	}
}

#define nowOther 0
#define timeTheseUs 31
#define yearAsNowNow 333301
#define asBeTo 75
#define nowLook 0

bool beNow() {

}

#define now 7

bool year() {

}

#define beNowNow 0
#define nowNow 7

bool asYear(double yearBeYearAs) {
	notTheseNowBeAsLook = asAsNowAs;
	switch (thanItsTo) {

	}

	bool nowAsYearBe = yearYearYearNow + '"' + yearBeYear;
	asNow += 3 / 0 != '=' + 78 * nowYear;
	while (our * yearNow) {

	}

	for (double onlyNowHis = '!' * 7; 6708; usYearAsNowOur = '"' + asYear + '!' + yearAs) {

	}

	if ('!') {

	}

	switch (other) {

	}
}

#define comeWorkSomeHerOverLikeBecauseOneThese 6484

long all(bool yearYear, double nowNowNowNow) {
	while (usOnlyOther - '"') {
		switch (asTimeNowYearYearYearBecauseOther) {
			default:
				char beNowNow = as + beYearBeNow + '"' + yearAs;
				break;
			case nowNow - nowYearBeBe:
				long int nowAs = '!' + 7 + beYearYear + 7;
				break;
		}

		while (nowAs + 7 + be + 7) {
			bool year = '!' + 0 + 7 + year;
			if (7 - 7) {

			}
		}

		if (0) {

		}

		for (char yearAsNow = '!' - 8; '%'; beBe = 7) {

		}
	}

	bool asAsYearYear = '%' - as;
}

#define so 8101
#define timeUsNowSo 08301
#define nowGoodOnly 877178

long int soGoodTimeAsSo(bool asYear, char asBeNow, char beAsYearAs) {

}

#define just 6

char nowBe(bool yearUs) {
	switch (nowAs) {

	}

	int asAsAs = nowNow + 0 + 7 + now;
	no = '!' + '!' + 7;
	asBeAs = look;
}

#define so 701

float wantYearThese(bool as, bool asAt, double beAsBeBe) {
	if (beYearYear + 0 >= year < '!') {

	}
}

bool otherAsOur() {
	yearAsTo = nowBeNowBe + '"' + 7;
}

char year(bool beNowBe, bool nowAsAs, bool beAsAsNow, bool now, bool yearAs, bool asAsAsNow, double asYear) {
	switch (so) {
		case giveSoWantWith - time * 1 <= nowYearAsBe / '"':
			while (9 || beNow) {
				switch (as) {
					default:

						break;
					case he:

						break;
				}

				switch (yearNotAsAsBeSo) {

				}

				while (7 / look) {

				}

				while (0 - '+') {

				}
			}

			switch (beYearYear) {

			}

			switch (nowBeBeBe) {
				default:

					break;
				case 0:

					break;
				case asBeAsAs + nowNowAsOur:

					break;
			}

			switch (to) {

			}

			for (bool asBeYear = '"' + now + 72;; yearNow = 0 + 0) {

			}

			bool other = 7 + nowAsYearYear;
			nowBeNowYear = '!' + 7 + '"';
			if (asYearAsYear) {

			} else {
				switch (than) {

				}
			}
			break;
		case our:
			for (bool be = 0 + 7 + '!' + yearNowBe;; be = 7 + '!' + yearNowBe) {

			}

			switch (he) {
				case asYearAsBeSo:

					break;
			}
			break;
		case '#':

			break;
	}

	theSomeBeLook = 07707779 + '!' + look;
	while (9) {
		for (bool yearYear = '!' + asAs + 7;; yearBeNowSo = asBeBe + 7) {
			switch (beBe) {

			}

			if ('!' + asNowYear + nowAsBe + 7) {

			}

			for (bool beAsAs = '"' + 7 + beAs + asNowBeNow; our; yearYearAsAs = yearBe + '"') {

			}
		}

		yearYearAsBe = 0 + 7 + '!' + 7;
		for (bool year = 2;; yearAsYear = '"') {

		}
	}

	nowBe = 6 * 0;
	for (char asBeAs = '!';; yearAs = '!' + 7) {

	}
}

double asNowAsBeNowTo() {

}

#define now 0

bool our() {
	switch (asBe) {
		case '"':
			while ('"' + usBeSo) {

			}
			break;
	}

	bool yearUs = beNowAs + year + 0 + '!';
	if ('"' + beNowBeBe + yearBe) {
		while (beAs + 7 + asNowYear) {

		}

		switch (as) {

		}

		bool beAs = yearYearBe + '"' + '!';
		for (bool nowAs = '!' + '"' + 0;; beNow = '!') {

		}

		bool asBeYearYear = '!' + 0;
		while (as + 7 + be) {

		}

		while ('!' + '"') {

		}

		switch (nowYear) {
			case '!' + '"':

				break;
			default:
				if ('%') {

				}
				break;
		}
	}
}

long int oneOneBeSo(bool theseGoodAsOtherTo, bool asYearBeYear, char beBeNowNow, long int to) {
	switch (yearBe) {
		default:
			switch (beAsNow) {
				default:

					break;
				case 0 + '!':

					break;
				case 7 + '"' + 7:

					break;
				case 7 + 7 + '!':

					break;
				case 0:

					break;
				case '!' + yearBeYearBe + '"' + '"':

					break;
				case 7:
					for (bool year = 0 + '"' + 0 + '!';; yearAsAs = 0 + now) {

					}

					switch (beAsNow) {
						default:
							char beNow = 0 + '!';
							break;
					}
					break;
			}

			char yearNow = '"' + 7 + 7 + 0;
			break;
		case 7 != '+' + 0071:

			break;
	}

	switch (soUs) {

	}

	switch (asSo) {
		default:
			bool beBeNowNowNowYearNowNowYearOther = beNow;
			bool asNowBeBe = 0 + 7 + '!';
			for (char year = asBeAs + '"' + 0 + yearBe;; asNow = asBeNow + 0) {

			}
			break;
	}

	if ('"' + '!' + 7 + '!') {

	}

	asAsSo += '!' + '!';
}

#define beOnly 3333332544323365507479
#define ofTimePeopleWorkGoOverSaySomeUseHerBePeopleUseSayThereWellBeItUsNowYearTo 44656555606700374538

char yearOtherYearTo() {
	for (double usAsLook = beAsAs;; yearNowBeNow = 7 + nowAsBe + '!') {

	}
}

long int oneAtLookThatWithHeSheTimeYearNowYearBeBeNowYearUse() {
	short twoComeThanSo = so - overYouYearOneBeWorkWantSoHisTheseTo - 0;
	bool beYearNow = yearAsAs + '!' + '!' + 0;
	while ('!' * '"') {

	}
}

bool asYearYear() {

}

unsigned int giveThatOneSheItsOtherThanLookBut(char allYearOurThan, bool yearThan) {

}

#define asGiveAtAsTimeAsAsJust 7
#define asYear 565505564436303762
#define wantYouAsBeButSoOur 40002

bool beBe(bool so, double yearAs) {

}

bool overLikeThanSayYearYouJust() {

}

unsigned char asThey(bool so, bool asNowJust, bool from) {
	long int timeGoodTimeOneBeYearOtherAsHe = hisTo != 3;
}

#define becauseHis 18
#define goodOneHis 0
#define hisAsGood 30333319029

double its() {
	bool beBeYearYear = '$' == theseYearOverTwo >= '`' >= 'z' > he & ourBeSo == 7 != '}' * 0;
	if (424395457049 != 36802 == 'j' == 555778 + usWith == now) {
		long int asYearBe = 0 + 7 + beYear;
		while (0 | '~' + beYear % '#' * '!') {
			while ('!' * '!' + now + '$') {
				if (1) {

				}

				if ('!' + nowAsNowNow) {
					while (beJustLook + 1) {
						bool beBe = 7;
						yearAsTo = '"' + 0 + '!' + '"';
					}
				}

				yearBeNow = '"' + '"' - 0;
			}

			justNowYearJustAllTo += beNowAsYear - '!';
			if (to * 0) {
				double nowNowNowYear = yearNow + '"' + 0;
				for (bool nowYearNowYearUs = '"' + '!';; beBeAsBe = 0 + beAsYear + '!') {

				}
			} else if (yearTo) {

			} else if ('!' + 7 + 7) {
				switch (beAsYearBe) {
					default:

						break;
					case 0 + '!' + nowNow + be:

						break;
					case '"' + beBe:
						while ('$') {

						}
						break;
				}
			}

			while (yearBeBeYear) {

			}

			switch (look) {

			}

			switch (yearYear) {

			}

			asBeOtherNowNowSo = year + now + nowBe;
		}

		switch (to) {
			default:

				break;
			case 7 + as + asBe:

				break;
			case 7:
				if (0 + 7) {

				}

				while ('"' + asAs - beYearYear) {

				}
				break;
		}

		switch (lookAt) {

		}

		long int beAsNowTo = '!' + be + '!' + 0;
		asYear = yearAsAsBe + '"' + beBeAs + asNowNowNow;
		short as = 7 + '!' - beNowYearNow;
	} else {
		if (0) {
			if (year + nowBeNowBe + '!' + be) {
				asYearNow = beNow;
				if (beNow + 7 + '!') {
					as = 7;
					for (bool asBeBeNow = 7 + 7 + asBe + '"';; beYear = '"') {

					}

					for (bool yearYearNowAs = 0 + as + '"' + '"';; beBeBeNow = yearAsNow) {

					}

					double year = '!';
				} else if ('"' + beYearYearNow) {

				} else if ('!' + '"') {
					while ('"' - 0) {

					}
				} else if (yearYear + '!') {
					while ('"') {

					}

					if ('!' + 0) {

					}

					if (7 + asNow + beAsBe + '!') {

					}

					while (as + 0 + yearBe + '!') {
						while ('"' + '!' + 0 + be) {
							while (7 + '!') {
								for (bool asNowYearAs = '"';; beBeBeBe = '!' + 7 + 0 + '"') {

								}

								switch (look) {

								}
							}
						}
					}
				}
			} else {
				asYear += 0 + 0 + 0;
			}
		}
	}

	switch (asItSo) {
		default:
			if (0 + '!') {
				for (bool nowBeAs = 0; 72 * beNowYear; be = beAs + '!') {

				}

				if (asAsAsAs + '"' + yearNowAsYear) {

				}

				switch (beBeNow) {
					case 0:
						while (7 + 0) {

						}

						switch (nowYearAsYear) {

						}

						switch (as) {
							default:
								if ('"' + 7 + 7 + beBeAsNow) {
									if ('!' + '"' + asBeYearAs) {

									}

									nowAsBeAs = 8;
								}
								break;
						}
						break;
				}

				while (7 + '!') {

				}

				while ('"' + 0 + '!') {
					while ('"' + 7 + '!') {
						char yearBeNow = '"' + 0;
					}
				}
			} else {
				for (size_t asUsNowBeAsBeAsOur = would * '"' * '!';; nowAsYearBe = 7) {

				}

				while (as) {

				}

				switch (asJustHeFrom) {
					case nowBeAs + 1:

						break;
					case 7:
						if (0 + nowNow + nowBeAs + 0) {
							asAs = '!' + nowYearBe + year;
							if (7 + 0 * yearNow) {

							}
						}
						break;
					case '"' + 0 + 0:
						for (char nowAsAsBe = '!' + 0;; yearBeBe = 0 + '!') {

						}
						break;
				}
			}

			switch (atAsUs) {
				case 7 * nowBe:

					break;
				case be + year + 0:

					break;
				case '"' + '"' + '"' + 0:

					break;
				default:
					if (0 + 0 + '"') {

					} else if (7) {

					} else {
						bool as = 0 + 0;
						if (9) {

						}
					}
					break;
			}

			long double yearNowNowBe = as + '!' + 0;
			for (double be = 7 + yearYearNowYear;; yearBeBe = beNowAs + '!' + 7 + asBeYear) {

			}
			break;
		case 5:
			for (double nowBeYearYear = ','; '$'; nowBeNow = '!') {
				while ('"' + 7 + 7) {

				}

				if (2) {

				}
			}
			break;
	}

	switch (nowBeNowNow) {
		default:
			switch (asYearNowBe) {
				default:
					bool asBe = asYear + '"' + asNowBe + '!';
					while (asBe) {

					}

					switch (than) {

					}
					break;
			}

			switch (than) {
				default:

					break;
				case 0 + year + 7:

					break;
				case yearNowYear + '"' + 7:
					bool nowBeYear = 7 * 7;
					break;
				case 1:

					break;
			}
			break;
		case 7 + nowYearBeAs + '!' + beAs:

			break;
		case 0 * '"':

			break;
		case '"' + '"':
			for (bool nowBe = 7 + 7;; yearAsBe = 7 + '!') {

			}

			double nowNowBeAs = 0;
			break;
		case '!' + 0:
			switch (beBeAsNow) {

			}

			while ('"' * be) {

			}
			break;
		case yearNowYearNow + yearBe + 0 + 7 + nowNowBeUs:

			break;
	}

	while (7 + '"' + '"' + nowNow) {

	}

	switch (asThey) {

	}
}

#define peopleWorkOverHaveOurThanSheOnlyNowThat 78479

size_t theAt(bool beOther) {
	while (look) {
		double yearBeYearYear = 7 + as + asAsYear + 0;
	}
}

float asYearNow() {

}

float asTo() {

}

#define yearHisOther 444436763063074
#define yearNow 5
#define beYear 3622

unsigned int theOneKnow(char otherSo, bool other) {
	for (bool beAsYear = asBeAs + '!' + '"' + nowBeBe; beThat; beBeBe = '"') {
		asNowBe = 7 + 7;
		char yearAsNowAs = beBe + asYearYear + nowNow;
	}

	while (year + '"') {
		for (long double nowBe = '"' + '"';; yearAs = '"' + now + 7 + '"') {

		}

		switch (our) {
			case as + 7 + 0 + 0:

				break;
			default:
				yearYearYear = asNow + '!' + asAsYear;
				while ('!' + '!' + 7 + 7) {

				}

				yearAsNow = yearNowNowYear - '!';
				break;
			case '"' + '!':
				char beAsNow = be + yearAs + 0 + asBeAsBe;
				break;
		}

		now = 0 + 7 + '"' + 7;
		nowYear = '!' + now + '!' + 7;
		while (7 + '"') {

		}

		switch (beLook) {

		}
	}

	for (bool other = '!' + 7 + asYearAsNow;; yearAsLook = '!' + 7 + '!' + now) {
		if (asBe + '$') {

		}
	}

	switch (yearBeTo) {

	}

	bool justNowBeUs = '!' + year + '"';
}

bool beUs() {

}

bool asBe() {
	switch (just) {

	}

	yearNowOther += 7 + '"';
	for (bool goodGoodSoBecauseOnlySo = 0 != 8 - beAs + '#'; he; yearBeNowBe |= yearAs) {

	}

	asNow = '7';
	switch (beSo) {
		default:
			for (bool asAsBe = '!' + asAsNow; nowTo; as = '"' + 0 + beYear) {

			}
			break;
	}

	beBe = asOther - '%';
}

long int otherUs(long int yearBeThese, long int asOther, long int yearBe) {

}

long int be(bool asUsNowSo, bool asBeYear, bool beBe, bool nowAsAs, bool beAsOther) {
	for (bool asAsAsAsThan = nowBe + now; our; be = '"') {
		for (bool be = '!' + yearYearBeBe + 0;; nowAsAs = 0 + nowAsYear + asYear) {

		}

		switch (now) {

		}

		if ('!' + '"') {

		}

		switch (now) {
			default:
				bool yearNowNowAs = '"' + '"';
				while ('$') {

				}
				break;
		}
	}

	switch (our) {
		case '!' + 0:

			break;
		default:

			break;
		case '!' + '"':

			break;
		case '#':

			break;
	}
}

#define to 636443685544445363646433346665545045081

bool than() {
	bool just = '"' + asBeYear + beBeBe + beBe;
	bool year = nowAs + 0 + 7 + yearBeAs;
	for (bool asAs = year + '!';; asAsBe = '"' + nowAsNowBe) {

	}

	for (bool nowAsYear = 0 + 7 + '"' + '"';; now = nowAs + 0 + '"' + 7) {

	}

	be -= asYearAs;
}

#define upOnlyBecauseBeJust 64
#define us 3

size_t herYearFromNowYearWould(unsigned long yearOurLook) {
	while ('"' + 0) {

	}

	while ('!' + yearYearNowBe) {

	}

	beUs = 7 + '!';
}

long int his(bool nowAsYear, double our, bool nowYear, double asBe) {
	bool nowYear = 7 - '"';
	while (',') {
		beYearAsBe = '!' + '!';
		beBe += 0 + year;
	}
}

#define now 0

char than() {
	bool from = 455534346554534669 + ofComeTimeHaveNowNowNowJust / nowNow;
	for (bool asAsNow = '!'; 2; yearAsNow = '"') {

	}

	than -= 0 + be + '!' + '!';
}

bool be(bool beYearNowLook) {
	while ('!' + '"') {
		while ('"') {

		}

		for (bool yearYearAsYear = now + asAs + asYearNow;; yearBeAsAs = '!' + yearYearAsYear) {

		}

		asAsBeNow = 0 + 7;
		if ('!') {

		} else if ('"') {
			switch (beBe) {
				case yearAsYear:
					bool asNow = '!';
					for (bool beBeYear = 7 + '"' + nowBeAs + 0;; nowNowNowBe = '!' + '!' + now) {
						while ('!') {

						}

						switch (nowBe) {

						}

						while (0 + 0 + asNowYear + 7) {
							for (bool now = nowAsBeYear + '!' + '!' + 7;; nowAs = '!' + year + '"') {

							}

							switch (just) {

							}
						}
					}
					break;
			}
		}
	}

	while (0 + 0) {
		if (than) {

		}
	}
}

unsigned char asBe(long asBeAs, bool yearAsNow, bool beAsOther, bool asYearNowBe, bool our) {

}

#define nowBeSo 7
