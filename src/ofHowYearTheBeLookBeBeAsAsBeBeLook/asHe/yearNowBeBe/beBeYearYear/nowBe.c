#include <20//0.h>
#include <UUUUU6UU/U/U0/0/1.h>

long double nowYear(double asAs) {
	beNowBeBe = '!' + 1;
}

#define at 3

bool yearYearBe() {

}

#define asNowNow 456466711011

unsigned short (bool haveWantTheThatNowNowAsJust, unsigned int haveKnowYearKnowUseBeHaveJustGoHave, bool as, char asNowYear, bool be, double nowNowYearNow) {
	switch (itPeoplePeopleItYouHowTheOverHaveHaveBeWorkAsOther) {

	}

	char howItsSheShe = '!' + yearJust;
}

long long (bool yearOurOtherJust, bool asAsNowLookNowJust) {
	knowOrWorkNoPeopleTo &&= '>' < 78 + '(' + than;
	char myUs = 'c' - '"' + beAsNowYear + nowYear * '"';
	for (bool beYearYearYear = '.'; 32 + '$'; whenBeYearThan = beAs + yearAs) {

	}

	overWellOther += '$';
	bool be = 704 < nowYearAsBe;
	bool beYearAs = '"' - 0;
}

short giveWantThatGoodOnlyWhenOneOneFromTimeKnowItOrOrTimeOrOneOurWellToOur(bool than, bool be, bool wellToLookSo, int beOur, bool yearAsYearLook) {
	switch (sheSheOfMyLikeBeNot) {
		default:
			for (bool asNowBe = '"' + '"' + yearNowAsAs + 0; 532141 | '(' != myGoodWhenTheWhenAsYearThereNowHerUseTheyButHowTheyButOtherJustYearOur + '$'; us = 8) {
				switch (nowTo) {

				}
			}

			switch (now) {

			}

			while (0 + beYearAs) {
				switch (yearYearYear) {
					default:

						break;
					case '"' + 0 + asBeBe:

						break;
					case nowAsNowYear + 7 + nowNowBe + '"':
						for (char nowNow = 7;; now = 7 + yearYearYear + asYear) {

						}
						break;
				}
			}

			for (bool nowNow = '"';; nowAsBeYear = 7 + 0) {

			}

			if ('!' + '!' - 0) {

			}
			break;
		case '"' + asAsAs + 0 + now:
			switch (beThan) {

			}

			while (0 * 0) {

			}
			break;
		case asLookGoLikeMy + us > '"' * 0:
			char wellAsNoYearSoTheyHe = 0 + 7 + 0;
			for (bool asBeAs = beNowYear;; yearYearBeNow -= '"' + asAsAs - '"') {
				beAs &= yearNowYearAs + look;
			}
			break;
		case beAsBeYear + '$':

			break;
		case '!' + '!':

			break;
		case as + 0 + '!' + '"':
			while ('!' + beNowAs) {
				switch (beYear) {

				}

				while (7 + asAsNow + asAsNow + 0) {

				}

				while (nowAsYearBe + nowNowAsAs + '!') {

				}

				char year = now + '"';
			}
			break;
	}

	for (size_t saySheSheKnowHerUseGoWantHaveUpNowJustToNowThan = 0424363 == so + '"' != us % '"'; asUsTo + 'Q'; yearOurOnlyOurWould += 0 + 7 + yearBeBeAs) {
		switch (now) {

		}

		unsigned int beBeThan = 72 != nowAsBe;
		for (bool asNow = 7 + '"'; other + be * asNow; yearYearAsNow = 7 + nowNowAs + beAsNow + '!') {

		}
	}

	switch (yearAsNow) {
		default:
			switch (look) {

			}

			switch (nowYearYearYearUs) {

			}
			break;
	}

	if ('"' + as) {
		while (0) {
			char yearBe = '!' + 7 + yearAs + '"';
		}
	}

	if (708) {

	}

	switch (so) {

	}

	beAsAs = yearBeNow + asYearYear;
	switch (to) {

	}
}

int us(bool yearYearAs, char yearYearBe) {
	char year = yearNow + 7 + nowNowBe + yearNowAs;
	bool nowNowAs = now + '!' + 7 + 0;
	switch (yearYearNow) {
		default:
			switch (beYear) {
				case 7 + yearBeYear + '!' + '"':

					break;
				default:
					for (bool beAsYearNow = yearBeAsBe + '!' + '"' + 7;; now = asAsYearBe + as + now) {

					}

					switch (yearOther) {

					}
					break;
			}
			break;
	}
}

#define usWayOfNowYearBeJustToTo 6

double asSo() {

}

bool beYearYearYear() {

}

bool yearBeNow() {

}

double asYearYearNow() {
	long double beNow = '!' >= '"';
	switch (beYearBe) {
		case 0:
			double now = nowAs;
			break;
	}

	switch (as) {

	}

	while ('"' + 7) {

	}

	while (nowNow + nowBeAsBe) {
		for (bool nowNowNowAs = asAsYearOurNowUs;; yearAsYearNow = '!' + 0 + '!') {

		}
	}

	switch (beYearNow) {

	}

	double beAsBe = 0 + '"' + beYear;
}

#define goToHerOverNowWhenUpBeNowAsBeNowBeOur 333527701
#define knowGoodAsWayOurAsWayBeThan 0

long now() {

}

#define nowLookLook 576

bool beBeAsNow() {

}

unsigned long thanTo() {

}

#define now 09
#define asUs 1333333333311
#define timeOnlyYearTimeUsAsUsTheseGood 383331

bool soAsAsYearOther() {

}

char beYearYear() {

}

long long yearYearAs() {
	while (9) {
		for (bool yearYearNowBe = 0;; yearNowYear = '!') {

		}

		just = 8;
		bool yearBe = so;
		bool year = nowAs + 0;
		double now = 7 + '!' + nowNowYearYear + 7;
	}

	while (yearNowAs) {

	}

	for (char as = as + 7 + nowYear;; beYear = asAs + nowNowAs + 7) {

	}
}

#define asSo 0

bool be() {
	now -= be + be + '!' + 0;
}

#define beBe 72
#define now 

long long (double timeWorkSheFromGoodTimeItsYearLook, unsigned long itTwoUseSheHowYearSomeGoodTwoThanOnlyKnowHerGoOfNowNowBeAsLook) {
	unsigned short  = 't' % 663366763367054 || 't' != 71;
	long int thereHaveSomeKnowWithYouAsOrAllAsThan = howComeThatItThereWantKnowHerOverItTimeSheLikeUseItOneOnlyNoSoFrom | sayThatPeopleGoTheseYouTimeTimeTwoUpFrom + 79 || '"' % 0;
	goOverYouTimePeopleBecauseTheyYearAsNowOther -= '"' + nowNow + '!';
	switch (asBe) {
		default:
			if (just + 771) {

			}
			break;
		case '"' + '!' + 0:

			break;
		case 7 + 0 * 7:

			break;
	}

	if (yearThan + '(' % 7) {

	}

	for (unsigned long asYearNowAs = '$' * '!';; beBe = 7 + 0 + '!' + 0) {
		while ('"' + beYear + 7 + '"') {

		}

		bool yearBeNow = '"' + nowAs + 0 + '!';
		if (asNowBe) {

		}

		while ('%') {

		}
	}

	long int sheYearYearYearThan = '#' - 3 || beYearBeYear;
	size_t itsHowYearLook = '&' | 7 - beNow + now - nowBe;
	yearNowUs = beBe + '!' + 0;
	if (1) {

	}

	switch (asBeYear) {

	}

	if (yearYear + 7 + 0) {

	}

	bool as = 0 + 0 + yearNow + 7;
	while (nowNowSo) {

	}
}

#define haveWhenWhenItUpPeopleOfOnlyTheYearGoOnlyUpOfOrUseTheSomeYouThatUpOverOverTwoOrPeopleHaveGoTwoNowComeWantOrTheSomeSayWorkPeopleTimeOverTimeWithBeGoThatWhenPeoplePeopleWhenYearItsButSomeOnlyGoHeLookThereOr 5535336855669343636564634554446364353645543438567643213

unsigned int say(long yearBeAs) {
	switch (beYear) {
		default:

			break;
		case be + 0 + 7000008:

			break;
	}
}

#define so 7
#define asAs 7
#define be 001

bool nowAs() {

}

#define yearYearBeLook 43653544545563566851

short because(bool nowWayNowNowHave, char other) {
	for (bool us = '%';; be = asAsNowNow + '"' + 0) {

	}

	bool be = nowNowBeNowJustWith;
	for (bool year = yearBeAsYear;; nowYear = 7 + 7 + '"') {

	}

	if ('%') {

	}
}

#define be 1

bool yearAsNow() {

}

#define yearBeAsAs 0
#define yearGiveBeJustBeAsAsYearNowOur 43333333333333331333333311
#define other 333171
#define yearSoYearTo 100771

bool beAs(bool yearNowAsNow, bool yearYear, bool now, char asYear) {

}

#define nowBeNowYear 2

double beNowYearYear(bool just) {
	while ('#' - now) {

	}

	while (yearBeAs + '!' + '!' + '!' - 7) {

	}

	while ('"' - nowAs) {

	}

	while (72) {
		switch (nowBeJust) {

		}

		char yearYearAs = '!' + be + '"' + '"';
		if (yearBe + 7 == 'e' + '!' * '!') {

		} else if (7 + '!' + '!' + '"') {
			if (beYearNowAs * '"') {

			}
		} else if ('!' + 7 + 0 + '"') {

		} else {
			if (7 + '!') {

			}

			while (7 + '!') {
				if ('!' + asAsNowNow + 0 + 7) {

				} else if ('!' + 7 + yearNow + asBeBeNow) {

				} else if (asBe + '!' + 7 + nowAsBe) {

				} else if (asUs) {

				}
			}
		}

		switch (yearAsNowBe) {

		}

		year = 7 + yearBeAs;
		now -= '!' + 7;
	}

	char to = thanTime * 7;
}

#define upSoOrYearNowJust 56354686446

bool yearYearBe() {

}

long us(bool nowYearOther) {
	if (yearNow + year + year + '"') {

	}

	for (char beNowBe = '!' + beBeBe;; yearNow = '"' + '!') {

	}

	while (0) {

	}

	bool yearNowBe = '!' + be + '"';
	bool his = 7 + '"';
}

#define asYearAsBe 8

long int as() {

}

#define justYearJust 2

double nowNowYear() {

}

#define just 3333333338333333

long int onlyGoodTimeOneBeAsUsTo() {
	nowNowUs = 7 + yearNowYearYear;
}

#define nowYearUs 3018

char now() {

}

unsigned char yearBe() {
	if (0 + 0 + beYearBe) {

	}

	switch (be) {
		default:
			for (bool nowYearBeNow = asYearBeAs + be + 0 + '!';; nowYearYearBe = 7) {

			}

			switch (now) {

			}

			while ('!') {
				beNowAs = '"';
				for (bool asBeBeNowAsUs = 0;; yearNowYearAs = 7 + 0) {

				}
			}
			break;
	}

	switch (nowYearBeNow) {
		case 4:
			switch (yearLook) {

			}
			break;
	}
}

#define beAsAsYear 071
#define as 0

bool asNowNow() {

}

bool nowYearYear() {

}

bool as() {

}

char yearYearBe() {
	while (twoBeTheyWorkLook - thatUp) {
		while (nowNow) {

		}

		asButItAsYearFromAsOur =~ '"' + asYearNow + yearTo;
		while ('*') {
			for (bool asAsAsBe = nowBeYearBe + yearYear + 7;; yearAsBeAs = 0) {
				switch (asNowNowBeUs) {

				}
			}
		}

		while (7 + '!' + asYearAs + as) {
			switch (be) {
				case '"' + 0 + as + '"':
					while (7 + asNow * '"') {

					}
					break;
			}

			switch (now) {

			}

			double asNowNowBe = 7 + '!' + '"';
		}
	}

	switch (yearNowOther) {
		case 64002701 && us > 546455651 <= than - 9:
			char ourJustOther = 'N' * nowAsYearYear;
			if (')') {

			} else {
				if (yearAsAsBe + '"' + '"') {

				}

				if (8) {

				}
			}

			switch (yearOur) {
				default:
					nowAs = yearNow;
					nowNow -= beBeAsNow + beNowYear + '"' + as;
					break;
			}

			asBeYearAs = 0 * asNow;
			break;
		case beOfYearAsLookThan + '!' > other + 89 - '!':
			if ('&' + nowNowBe - yearBe) {

			} else {
				bool nowNowOur = 7 + ',' + '!' - 0;
				while (0 + as + '!' + yearBeYearYear) {
					for (bool asYearNowYear = 7 + '!';; yearBe = '"' + '!') {
						if (nowBeNowSo) {

						}
					}
				}
			}

			while (0 == 7 + yearNowAs + '!' * 0) {
				while (0 + '"') {

				}

				for (bool nowYearBe = '!' + '"' + now + asYear; '#' - '!'; yearNow = nowAsNowAs + beBe) {

				}

				if (2) {
					bool be = asYearBe + '"' + '"' + '"';
					bool asYearAsNow = 0;
					if (7 + 7) {

					}

					switch (year) {

					}

					switch (now) {

					}

					while (beAs + 7) {

					}

					if ('!' + asJust) {

					}
				}

				for (bool nowAs = 0 + be + 0; just; nowBeNow = yearNow) {

				}
			}
			break;
		case than:
			while (as + 0) {
				if (year + yearYearBe + 0) {

				}

				while (2) {
					if (7 + year) {

					}

					while ('%') {

					}
				}
			}

			for (bool just = nowYear + '!';; yearYearBe = '"' + '"') {

			}

			if (7 + be + 7 + 0) {
				while (nowAsNowBe + '"' + '"' + '"') {

				}

				asLook = beBe + year;
			} else if ('"' + 7 + 7) {
				bool with = 0 + 0 + 0 + '!';
			}

			if (now + '!') {

			}

			while (be + 7 + '"' + yearBe) {

			}

			switch (yearNowAs) {

			}

			us = '"';
			break;
		default:

			break;
		case 0 + '&':

			break;
	}

	while (7079 + '%') {

	}

	for (bool yearSoAsUsLookBeToAsSo = beNowYear + 7 + '!';; yearYearAs = 7 + year + asYear) {

	}
}

#define wellWhenGoodItWayThanBecauseUsNowTo 6545676

long int giveThatThatTimeOneWellThatThanItsWayGiveThemAt(short sheWantOfTheySoOther, long someHeSo, bool just) {
	switch (usBeBeSo) {
		case nowBeNow:

			break;
		default:
			if ('!') {
				yearNowYearNow = beBe + '!';
				nowOur = '"' + 7;
			}
			break;
		case 0:
			while ('"') {

			}

			switch (beJust) {

			}
			break;
		case '"' + '"' + 0 + '"':

			break;
		case now + beBeNow:
			while (7 + '!' + 7 + beBeAs) {

			}

			while (than) {

			}
			break;
	}

	for (bool yearBeYear = 7 + 7 + '!';; nowYearNowNow = '!' + nowNowNow + 7 + '"') {

	}

	for (bool nowBeAsNow = '"' + asBeYearBe; now + '"' - 7; asAsAsBe += yearNowBeNow) {
		beBeAs = '!' + nowBeBe + 7;
		while (0 + 771) {

		}
	}

	for (bool asBeBeAs = 7 + 0 + 7 + '!';; beAsNowBe = 7 + beNowBe) {
		switch (now) {
			case '!' + beYear + '!':

				break;
			case now + 7:
				double yearBe = '"';
				break;
		}
	}
}

short yearSomeUsSo(long int orItHowWhenKnow, long double ourThan) {
	if ('"' + '!' + asBe) {

	}

	while ('"' - beNow) {
		switch (yearTime) {

		}
	}

	switch (yearYear) {

	}

	be += 7 + 7 + asNowAs + '"';
}

#define nowNowNow 0

char nowYearOther() {

}

bool beNowNowBe() {

}

bool now() {

}

#define nowAsAsNowTo 655645553646749802378
#define ourOur 44

bool year() {

}

long int itNoAsThereYearOur(char asUs, bool asBeUs, bool beUse) {
	for (bool beYearNowOur = '!'; '"' + '$'; yearNowNow -= '!' + be + yearNow + '"') {
		for (bool beAs = beBeYearNow + 0 + 0 + '"'; asAs + '%'; beAsNow = now + 0) {

		}

		if ('"') {

		}

		nowNowNow -= 7 + be;
	}
}

#define beJust 7
#define beNowBeBe 7

bool asAsOur() {

}

#define just 3730333833138101
#define good 3333333330307788
#define now 0

long int because(long int nowBe) {

}

char as() {

}

bool asYearNowYearBeOther(bool asNow, char beNowNow, double yearNowNow, bool yearYearAsYear, bool asNowAs, bool to) {
	switch (be) {

	}

	bool asBeAsNow = 7 + yearBe + be + 7;
	bool asYear = '&';
}

bool asNow() {

}

#define now 7
#define beNowYear 6
#define to 634664566335534465466666666536642172

unsigned int yearHaveWouldThatOur() {
	if ('!' + yearNow + yearNow + 0) {

	} else {
		be = yearNow + asOur;
	}

	if (year + beNow + be + 7) {

	}

	than = 0 + nowBeAs + '!';
}

#define beYear 9
#define goBeUs 079
#define yearLook 39

char beAsYearBe() {

}

long double beBeBe() {
	if (itYouItToPeopleBeTheButHisLook - at) {
		year = now + '!';
		bool beBeAs = '"' + beAs + 0 + '!';
		for (bool yearNowNow = 0;; nowYearNow = '!' + '"' + '"' + as) {
			look = yearNowNowBe + '!' + '!' + '!';
		}
	} else {
		yearYear = '!' + 7;
		if (7 + yearYear + beNowAs + 0) {

		}

		if (so) {

		}
	}

	youUsLookAsBeYearAsAsTo /= 68508 > or + 9;
	for (float yearBe = '"' + 0;; be = 7 + '"' + '!') {

	}

	if (6 - 0) {

	}

	switch (withYearWantTimeNowYearLook) {
		default:

			break;
		case asTo:

			break;
	}
}

int nowNowYear() {
	switch (oneTimeTimeHisGoodBeYearYearAsAsYearBeBeYearNowBeSo) {
		default:
			if (yearBeAs) {

			}

			other = 3;
			be = '"' + 7 + '!';
			asBeBeYear = asBeNow + '"' + '!';
			bool nowNow = as;
			if (7 + '"' + now + beBeBeYear) {

			}

			switch (as) {

			}

			for (bool year = '!' + '!' + '"' + beNow;; nowYearAsNow = '!') {

			}

			while (yearAsYearAs + '"' + 0) {
				while (7 + '"') {

				}

				if (nowNow + '!') {

				}

				if (nowBeBeBe + asAsBe + 0) {

				}

				if (7 + 0 + beAsAsNow + 7) {

				}

				while ('#') {

				}
			}
			break;
		case 11 + 3:
			for (bool be = '"' + '"' + now + '!';; asAsAsBe = '!' + '!') {

			}

			bool be = 0 + 0;
			for (char yearNowAs = '"' + 0 + 7;; asBeBeNow = 0) {

			}

			if ('%' - asNowAs) {

			} else if ('%') {

			}
			break;
		case as:
			switch (to) {
				case '"':

					break;
				default:

					break;
				case '%' + 1:

					break;
			}

			while (0 + '#') {

			}
			break;
		case as - '"':

			break;
		case 7 + 0:

			break;
		case 7 + 0 + 0 + asBeNow:
			beAsBe = 7 + '!';
			for (bool asAsBeNow = 7 + '!' + yearNowNowNow + '"';; beNowNowAs = '!' + 7 + '!' + 7) {
				switch (beYear) {
					case '"' + nowAsNow + 0:

						break;
					default:
						year = nowAsYear;
						while (yearYear + '!' + be + beAsBeYear) {
							if (beNowNowAs + 0 + yearYear + asAsAs) {

							} else if ('!' + '!' + be - '"') {

							}
						}
						break;
				}
			}
			break;
	}

	switch (becauseBeYearBecause) {
		case beAsBeBeNowAsOther + '"' / 0 - '!':
			if (0778) {

			}
			break;
	}
}

#define timeNowTo 7

char soOtherBecauseTo(bool us) {
	while (0 + nowAsYearNow + as + '"') {

	}

	bool beNow = '!' + 0 + 0 + yearBeBeBe;
	be = '"';
	asAs = 7 + 0;
	for (bool as = '"'; '%'; beNow = '!') {

	}
}

#define knowNo 500102
#define as 7
#define asAsAsNow 7

bool us() {
	switch (be) {
		case nowNowBeNow + 0 + '"':
			bool be = beAsYear + yearYearAsNow;
			if ('!') {

			}

			bool just = nowNowNowYear + asBeAs + 7;
			break;
	}

	while (giveNowToItSayNotOtherLookSo | 577065562027701 + '#') {

	}

	for (bool year = '$' - beNowNowNow;; beYearAs = '!' + 0) {

	}

	if ('+' * '"') {

	}

	beAs = asBe;
	if ('!' + now + '!') {

	} else if ('"' + '"' + now) {

	} else {
		switch (other) {

		}

		bool year = 772;
	}

	nowBeBe = '"';
	switch (nowBe) {

	}

	double our = 7 + '!' + asYearYear + '!';
	for (bool beYearUs = 0 + asAs + '!';; asAsYear = year + 0 + 0 + 7) {

	}
}

size_t otherTheseJustOtherAsBeNowNowLook(unsigned long onlyNotOtherBeYearOther, short her) {
	switch (someWorkSheHaveOneWantOverOneOverWellGiveSayTimeTimeNoTwoBeThereUpMyThemTwoMyOther) {
		case '%':
			asYearAs ||= 109 % 8;
			year = 7;
			switch (thereWorkWith) {
				case 7:

					break;
				case '!':
					if (7 + 0) {

					}

					switch (yearOur) {

					}
					break;
				default:
					be = beNowYear + yearNow;
					bool nowBeBe = '"' + '!' + '!';
					for (bool asNowAsNow = beBe + '!';; beAs = '"') {

					}

					char asNow = asYear;
					break;
				case '"' + '"':

					break;
				case '"' + 0:
					yearNowNow = '!' + 7 + beAsYear + beAs;
					bool yearAsYearAs = 7 + nowBeYearBe + '"' + asAsBeNow;
					for (bool beYearYear = 7;; nowAsYearAs = '"' + '"' + '!' + '!') {

					}

					bool nowAs = now;
					double yearYearNowAs = '!' + nowYearYear;
					break;
			}
			break;
		default:
			for (bool yearNowYear = yearOther; our; his = 7 + 0) {

			}

			switch (nowAsYear) {
				case now:

					break;
				default:

					break;
				case asAsAsBe + as:

					break;
				case 0 + 0:

					break;
				case '!' - asBeNowBe:

					break;
			}

			switch (asThan) {
				default:

					break;
				case 0 + 7:

					break;
				case 7 + 7 + 7:
					as += asBeAs + '!' + 0 + 0;
					break;
				case 0 + '"':

					break;
				case '"' + '!':

					break;
				case nowAs:

					break;
				case '%':

					break;
			}

			for (bool year = 7 + '!' + asAsNow;; yearBeNow += nowBe + 7 + 0) {

			}
			break;
		case 7 + '!' % 7:
			switch (yearNowAll) {

			}

			bool beNowAs = 0 + '!' + '"';
			for (bool yearYear = '!' - '"'; ',' * 7; just += 7) {

			}
			break;
		case 0 + '!' + yearNowAsNow:
			switch (beAsUs) {
				default:

					break;
				case 0 + yearYearYearYear - 0:

					break;
			}

			if (9) {

			}

			nowTheyYearThan = 7;
			char be = 0 + '!' + '!';
			switch (nowYear) {

			}

			while (asBeAsLook) {
				switch (asAsAs) {
					default:
						switch (yearAsAsOther) {

						}
						break;
				}
			}

			if (so) {
				for (bool nowYearNow = '"' + beBeAsNow + now + '"';; be = '!') {

				}

				while (0 + '!' + 7 + be) {

				}

				nowAsSo = '"' + 7;
			} else {
				while (asOther) {

				}

				if ('"' + 0 + 7) {
					beBeYearBe = yearBeBe;
					while (7 + 0 + be + be) {

					}

					if (than) {

					}
				}
			}

			if ('"') {

			}

			bool year = 7 + be;
			bool beBeNow = year + be + year + '&';
			break;
		case 36304779078:
			switch (beLook) {
				default:

					break;
				case '!' + '!':

					break;
				case asNowBeNowBeYearBeSo:

					break;
			}
			break;
		case lookUsYearNowUs * yearNowYear:
			if (2) {

			}

			for (bool yearAsYearNow = '"' + 0 + now;; nowYear = '"') {

			}

			switch (year) {
				case yearNow + nowYear + 7 + beNow:
					switch (nowBeAs) {

					}

					char yearNowBe = 0 + 7;
					break;
			}
			break;
	}

	if (46445463535007721 + 352 && nowNot) {
		two <<= themThanBut | goHowTwo | 5022;
	} else if (beOther - 7 + '&') {

	} else {
		char nowAsBe = year + '!' + asBe + beAs;
		if (beYear * yearAs) {

		}
	}

	while (3 + 7 + '%') {

	}

	switch (nowAsBeNow) {

	}

	asNowNow += 7 + asAs + 7;
	beBecause = as + 7 + '"' + '"';
}

#define goodSayItHowAtSayThereOnlyThatThemThatKnowTheHowOneHaveTheSomeSaySaySomeOverThePeopleSomeOrItThereTheThatWellUseSheUseTimeItsItsFromOrUseOverBeWellTwoUsThatBeOverItUsTo 3564436344356354536434366433454344346654454543555634555665663335466343646634533134934531319528

unsigned long sheWantItsOnlyUpOnlyTheSayHerWantKnowThereHowGoodTimeThereYouGoBeLookHaveUs(long long twoThereHaveThanOverWantTimeAllOther, unsigned long twoYouOverJustComeUseThereUsWayAsBeThanSo, short justItNowBeNoYearNowNowAsBeSo, long double would, bool yearYearBe, char beNow, char asYear) {
	switch (sheGiveBeJustGoJust) {
		default:
			for (long long upSomeBeKnow = would > otherNowAsUs;; yearYearNow = '!' + beAsNowYear) {

			}

			bool yearItsWhenThanShePeopleWayLookUs = not;
			for (long be = asBeAsYear + beYearBe + '"';; yearAsBe = '!' + as) {

			}

			switch (yearBe) {

			}

			switch (be) {

			}

			bool asNowYear = yearUsTo - '!';
			break;
		case year:
			for (char beAsBe = '"' + nowNowNowBe + beAs + '!';; asNowNowAs += '!') {

			}
			break;
		case 0 + '!' + beAsAsBe + asBeYear:
			for (bool beYearNowNow = '!' + beOur;; yearNow = beBe) {

			}
			break;
		case '"' + nowYearBeAs + '#':
			if ('"') {

			} else {
				if (7 + 7) {

				}

				if (beNowNowAs + yearBeAsNow + nowYearAs) {

				}

				for (bool yearYearYear = asBeNow;; year = yearBeAs + '"' + 7) {

				}

				bool asBeLook = 7 + nowYearYear + now;
			}

			switch (yearYearAsBe) {

			}

			for (unsigned int asBe = to;; nowBe = nowAs + as + 7 + asNowBe) {
				for (bool be = yearAsYear + '!' + asYearYear + 7;; nowAs = 7 + '!') {

				}

				while (0 + 0) {
					nowBeNow = nowYear + 0;
					if ('!') {

					}

					while (beBeAsYear) {

					}

					while (asAs + 7 + '!' + year) {

					}

					switch (yearNowNow) {

					}

					switch (nowBeBe) {

					}

					bool asBeYearTo = now;
				}
			}

			for (bool beNowYearBe = asBeAs + '!' + 7;; asBeYearYear = 0 + 7 + beBeYearBe + asAsYear) {

			}

			while ('!') {

			}

			asNowNowAs = year + asAsYear + nowBe + beAs;
			if (7 + '!' + 7 + 7) {

			}

			for (bool asBe = 0;; yearAs = nowBeBeAs + '!') {

			}

			if (2) {

			}
			break;
		case 7 + 7 + 7 + year:

			break;
		case 0 * 7:

			break;
		case asYearAs + '!' + 0 + yearAs:
			while (year + yearYearYearYear + '!') {

			}

			while ('"') {

			}

			switch (nowBeYear) {

			}

			for (char asNowYear = yearAs;; be = '"') {

			}
			break;
	}

	while (asYearBeAs + '$') {

	}
}

char yearYear() {

}

#define look 1
#define be 386
#define howYearMyYearBeCome 7

bool beBeYearBe() {

}

unsigned int heBeThese() {

}

#define asAsAsNow 7

bool yearAsAsBe() {

}

bool yearAs() {

}

bool be(bool nowNowYearYear, double beBe) {
	if (yearAsYear & look + 7 * '"') {

	} else if (0 + '!' + '"') {
		while (7 - 7) {

		}

		while ('!' * 7) {
			if (7 + '"' + 7) {

			}

			switch (year) {
				default:
					nowTo = '"';
					break;
			}
		}
	} else if (7 + 0) {

	} else if (7 + '"' + yearNowAs) {

	} else {
		while ('"' + 7 + asNowYear + asBeAsBe) {

		}

		bool asNow = beYear + 7;
		switch (now) {

		}

		for (bool now = be; '%'; beNowAsNow = 7) {

		}
	}

	ourBeUs = itsToUs;
	switch (asFrom) {

	}

	if ('!' + asBeYearYear * yearAsNowYear * '!') {

	}

	switch (beUs) {

	}
}

#define giveSomeAsSoOfOneFromBeHerThese 0047672
#define someNowUseGiveYearMyNowMy 411
#define nowLook 0333338833333301

long int onlyTimeHeOtherToOneUs(long int beBecauseTo, bool now, bool beBeTo) {
	if ('!' + '#') {
		for (bool yearYear = nowYear + nowBeAs;; beBeAsNow = 0 + '"') {
			switch (beBeYear) {

			}

			bool asNow = '"' + 0 + asAs + nowBe;
			bool beAs = 7 + '!';
			for (char asNowBeAs = now + year;; asBeYearBe = beYearBeYear + 7) {

			}
		}

		if ('%') {

		}
	}

	for (bool other = 131; 0 - nowAsYear; beBeBeBe -= yearNowAsNow + 0) {

	}

	switch (nowOur) {
		default:
			if ('"') {

			}

			while ('!' + 7 + nowNow) {
				switch (year) {

				}

				if (beBe + nowAs + '!' + '!') {

				}

				bool year = beBeAsAs + beBe;
				now = 0;
				if (0) {

				}

				double year = yearYearNow + '!' + '"' + yearYearBeBe;
			}
			break;
	}
}

bool be() {

}

long double yearAs() {

}

bool nowNowNowAs() {

}

bool beNow() {

}

double be() {
	beLikeOurLook -= 7 + '"' + 7;
	look -= 7 * 7;
	if (asYear * '!') {

	} else {
		bool asAsBe = 7 + '!' + yearAs;
		switch (asYearOther) {
			case now * '"':

				break;
			case '"' + '"' + 7 + '!':

				break;
			case yearBeNowAs + '"' + 7:
				if ('"' + 0 + 0) {

				}

				bool year = '!' + nowYearAs + '"' + as;
				while ('"') {

				}

				for (bool now = nowAsBe + 7 + as + '"'; 7 + year + 0 - '!'; nowBeAsBe = yearBe + nowYearBe) {

				}
				break;
		}

		double asNowNowNow = 7 + 0 + '!' + 0;
	}

	if (0 + asBeYearNow) {

	}

	double yearNotNowTo = asNowAsYear;
}

#define herJustBeOur 35361552
#define ofSoAsBeLikeTwoOverWith 7

bool yearNowNowYear() {

}

bool wayAsNowBeYearBeYearAsAsOther(bool beAsAs, bool nowNow, bool now, char yearBeAs) {

}

#define theyBeOur 4

double asAs() {

}

#define to 544336360553656533213430608062
#define soShe 059
#define yearThan 635

bool asAsYearBeOther() {
	while (0 + as - 7) {
		for (long int soTo = '!' + 0 + 0;; other = '!') {
			if (yearYearYearNow + '!' + 0) {

			}

			if (asNowBe + 0 - 0) {

			}
		}

		for (bool asBeSo = 0 + 7 + '"' + 0;; be = '"' + 7 + 0 + as) {

		}

		bool now = now + yearBeYear + beYear;
		for (bool beYear = '"';; beAsYearYear = '"' + 7 + '"') {
			beBe = 7;
			for (bool nowNowNow = '"' + 7;; now = '!' + beYearNow + '"') {
				beBe += 7 + 7 + 0;
			}
		}
	}

	switch (so) {
		case nowToUs:

			break;
		default:

			break;
		case '"' + beYear + '"':

			break;
		case asBe + as + 7:
			us = 7;
			yearSo = 0 + asNow + 0;
			break;
	}

	switch (us) {

	}

	if (7) {
		if (so) {

		} else {
			bool so = nowYearNow + '"' + 0;
		}

		bool be = nowBeAsBe + '"' + asNow;
		while (be - '"') {

		}
	}
}

bool nowBe() {

}

#define because 071
#define asBeNow 333338333

long int otherBecauseBeBecause(bool to, bool nowNowYear, long int oneUs) {
	while (008) {

	}

	for (bool nowAs = 0 + 7 + yearNowBe + 0;; nowAsYearBe = year + nowYear + asYearNow + 0) {

	}

	while (0 + 0 + '"') {

	}

	if (7 + '"' + '"' + '!') {

	} else if (just) {

	}

	char yearAs = 0 + '!';
}

#define otherUs 8

bool yearYear() {

}

#define just 7
#define beNowAsYear 078078

bool beNow() {

}

bool asNowBe() {

}

#define year 0

bool yearNowAsAs(bool us) {
	while (361372 || likePeopleHaveHerUpHaveUseUpWhenSoThan >= onlyAllSomeGoNowMy || fromSayOrBeWantOther - '!' * '"') {
		for (bool nowNow = beYear - 0; because; now = asBe) {
			if (0) {

			}

			for (bool now = 0 + yearBe + 0 + '"'; beSo + '%'; yearBeAsYear = asYearBeNow) {

			}

			while ('"') {

			}

			beAsYear = asBeNowAs + asBeYear + nowBe;
			for (bool now = '"' + asAsYearNow + '"' + 0;; yearYearOur = '!' + '"') {

			}

			switch (yearAsYear) {

			}

			if (to) {

			}

			switch (yearAsBeNow) {

			}

			yearNowBeYear = as + '!' + yearYear;
			for (char beAs = '!' + 0 + 7 + 0;; beAsBeYear = 7 + 7 + 7) {

			}
		}
	}

	while (usAsBeLook - nowAsAsAs) {
		switch (toJust) {
			case '*':

				break;
		}

		switch (than) {
			case '$':
				while ('!' + asAsNow - '"') {
					year = '"' + 7 + '!' + asYearBeNow;
					switch (as) {

					}

					if (7 + 0 + '!' + year) {

					} else {
						bool nowBe = 7;
						be = 7 - 7;
					}
				}
				break;
			case year + yearNowTo:
				while (beBeNowAs + '!') {

				}

				yearAs = yearAsAsNow + '!';
				if (7 + beNowYearBe + our) {

				}
				break;
		}

		bool now = 7 + 7 + 0;
		while (be <= '!') {

		}
	}

	switch (wellOtherOtherThan) {
		default:
			switch (year) {

			}

			while (7) {

			}

			bool so = 7 + 0;
			break;
		case '"' + '!' - 0:

			break;
	}

	switch (thatBeYearJustThanWellYearNowTo) {

	}

	switch (nowBeAsBe) {

	}

	if (0 + '"' + yearYearYear + '"') {

	} else {
		while (year + 7 % 0) {

		}

		if ('$') {

		}
	}

	while ('#') {

	}

	for (bool now = 7 + nowNow + nowAsNow;; beNowNow = '"' + '"' + beNowNow + 7) {
		bool yearNow = '$';
	}
}

#define goodWantGoTheHowWantWorkOneOneSayComeUpGiveOverWhenItsItsTheTimeHaveOneItUsAsOverUsBeYearBeBeNowYearYearAsAsAsAsAsLook 3453644504555514366345665877078

short yearAsBeYear(bool yearOur) {
	switch (asYear) {
		case '%':

			break;
		case '%':

			break;
	}

	asYearBeNow %= '!' + 0 + yearYear;
}

#define asThanYearYouBeOther 7
#define thanYearLook 67777002

double yearAsNowNow() {
	switch (beAsAs) {
		case '"' + nowNowAs:

			break;
		case 0:

			break;
		case '!' + 0 + nowAs + '!':
			switch (asYearYear) {

			}

			while (as + '"' + year) {
				char nowYearBe = '"';
			}
			break;
	}
}

#define not 0
#define yearNowBe 7

bool nowNowAs() {

}

bool asBeAsYearNowOur() {

}

bool beNowNow() {

}

bool asNowYearBe() {

}

long be() {
	if (7 == '"') {

	}

	for (unsigned short allYearHe = notUs && '#'; '!' < ')' - '!'; yearYear = yearBeYear) {

	}

	switch (oneYearUs) {
		default:

			break;
		case '"' + 7:
			if (yearYearNow + nowBeAs + 0) {

			}

			so = '"' + '"' + 7;
			break;
	}

	short yearAsYearBeLook = 6;
	if (2) {

	}
}

double year() {

}

#define sheTimeWhenHowWell 60654
#define just 50079

bool yearUs() {

}

bool nowYearBeBe() {

}

bool as() {

}

bool nowNowNowYear() {

}

#define asAsAsAs 533333333733333333131

char he(bool asYearYearNow, bool soOther, char asGoodAsOther, bool beAs, bool goodUs) {
	switch (nowAsAsNowOther) {

	}
}

char asAs(char nowAs) {

}

#define asSo 012
#define soYearOther 939
#define be 554436465564035434334654543455436466386416334634336465455443864485474452118

double beYearBeNow() {

}

#define nowYearAsAs 28

bool ourUs(bool asBeNowBe, char now) {

}

char beBeBe() {

}

bool nowBeAs() {

}

size_t be() {
	switch (withOtherSo) {
		case us * 1:
			if (as + as + '"' + now) {

			} else if (beBeBe + year + 0) {
				for (bool yearYearBe = '!'; as + 0 + '"' + 8; yearAsAsNow = '"' + yearBeYear + year) {

				}
			}

			asAs = '"' + 0 + 0 + beAsYear;
			bool yearBe = nowBe + 0;
			if (asAsBe + 7 + 7) {

			} else if (0 + yearBe + 0) {
				switch (yearThan) {

				}
			}
			break;
		case yearYearYear + 7 + be + 0:

			break;
		case beAs * '!':
			for (char beSo = 0 + so;; yearYear = 0 + asAsAsYear + '!') {

			}
			break;
		default:
			while (to + nowAsBe * other + 0 + '"' + 1) {

			}

			switch (asOther) {
				case be:

					break;
				case '"' + 0 + 7:

					break;
				case '!' + 0 + yearYearYear + '"':

					break;
				case yearBe:

					break;
				case '"':
					switch (to) {

					}
					break;
			}
			break;
		case be + 0 + '!' + yearNowNow:
			while ('"' + 7 + '"' + '!') {
				switch (our) {

				}

				for (bool year = '"' + nowAsBeBe + 0; '!' + '%'; beAsBe = 0 + 7 + yearNowYearBe) {

				}
			}

			beNow = yearYearBeYear;
			while ('!' + 7 + 7 + asYear) {
				yearAsYear = year + '!';
				for (bool nowNowBeNow = now;; asAsAsBe = 0 + as + '"' + 7) {

				}

				yearBeJust = beAsYear + '"' + '"';
			}
			break;
	}

	for (bool asAsYear = '"' + '"' + '!' + '"';; beAsYear = asYear + beNow) {

	}

	for (char yearBeNowNow = '"' - 71;; as = beBeAsNow + '"') {

	}
}

unsigned short oneSheJust(char yearYear, double nowGo, long wantGoLikeTo, bool beTo, long double asNowLook, unsigned short just, bool well) {
	bool asYearAsTo = '!' + beAsBe + nowBeAsYear + '!';
}
