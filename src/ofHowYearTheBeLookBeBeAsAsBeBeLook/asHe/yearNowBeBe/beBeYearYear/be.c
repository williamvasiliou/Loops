long int so() {
	yearYear += as + 7 + 7;
}

#define onlyGoodGoodGoodOnlyOnlyGoodGoodOtherOnlyTimeSoBecauseGoodOneToTo 8711
#define soYearSo 88335
#define nowYearBe 06

bool now() {

}

bool be() {

}

#define beYearAsAs 701

bool beYearAsYear(bool nowNowYear, double yearYearNowYear) {
	while ('"' + beNowAsYear + '"') {
		switch (just) {

		}
	}
}

unsigned long now(bool nowYearOur) {
	while ('2' && 2) {
		to = 7 + '!';
		if (nowOur) {

		}

		switch (nowBe) {

		}

		while (700079 * 1 * '"') {

		}

		for (bool yearNow = '!'; 9; yearYear = 7) {

		}

		bool beYearYearNow = 0 + yearBeYear;
		bool beBeAsNow = '!' + 0 + yearYear;
		if ('"') {

		} else {
			switch (beYearBeBe) {

			}

			switch (nowAsNowUs) {

			}
		}
	}

	for (long double notOnlyYearSoThan = 7 + 0 || 7 * '!';; beBeBe = '!') {
		you -= beNow + 0 + '"';
		beAs = '!' + 7 + year;
		while (asNowAs + '!' + '!') {

		}

		be = '!' + '"' + '"' + yearAsAs;
		switch (now) {
			default:
				while (nowBeBeYear) {

				}

				bool asNowBe = 7 + '"' + 7 + 7;
				char be = '"' + yearNowNow + 0;
				break;
		}
	}
}

#define upLookBeSo 000079
#define other 6355655434453361636206
#define howJust 56387079

bool yearAs() {

}

double beAsAsNow(bool yearAsSo) {

}

#define thanOurSoBeYearMyAsBeJust 0

bool than() {

}

bool asYearYearBe(bool beBeLook, bool nowNowSo) {

}

bool asBeYear() {
	if (yearAsYearNow + nowNowYear + 0 + yearYear) {

	}

	now = 7;
	year -= '\'' != becauseAsBeNowUs;
	if (7 + nowAsYearYear + 7 + '"' + so) {
		switch (so) {
			case as:
				if (to) {

				}
				break;
		}

		switch (asAsAsAs) {
			case '"':

				break;
			case 0 - '!':
				if (78) {

				}
				break;
			default:

				break;
			case yearNowBe + 7:
				asAsNow = '!' + '!' + asBeNowAs + '!';
				for (bool as = 0 + 0 + 0 + '"';; nowAsYearYear = asYear + 0 + 7) {
					if (1) {

					}
				}
				break;
		}
	} else if (0 + 07701) {
		for (bool yearNow = 7 + beAsYear + '!' + year;; asAs = 7 + 7 + 7 + '"') {

		}

		if (7 + '!') {

		}

		if ('"' + '!' + year) {
			if (7 + asNowNow + 0) {
				now += 0 + asYear + '!' + '!';
			}
		}
	}

	for (float beNowBe = 0 + yearAs + beNowNowAs;; be = nowBe + '!' + '!' + 0) {

	}
}

long int goodOnlyTheseTimeUsUs(char yearNowAs) {

}

long int nowSo(bool only, long int so) {
	otherNowBeSo = 338;
	while ('%') {
		bool other = '"' + 7 + 0 + '!';
	}

	char yearBeNow = 7 - 7 + nowYear + 778;
	for (bool beYearYear = 0; '"' + '"' - '"'; yearYearAs = 7 + asYearBe) {

	}
}

long int beBeOur() {

}

bool nowNow() {

}

#define asYearAsYear 908
#define us 5666453464667006343563778
#define goodSoToThan 555855930532
#define nowNowNow 8333333033331313383718

long int otherOneOneOnlyHisTimeBeAsTheseTo(bool other, char yearUs, float yearJust) {
	switch (from) {

	}

	while (yearAsBe + 0) {
		switch (beNowNowYear) {

		}

		switch (beYear) {

		}

		for (bool now = 7;; beNowYear = '!' + '!' + '!') {

		}

		asYearBeYear = 0 + asBe;
		beAsLook = 0;
	}
}

double now() {
	lookBeNowOther ^= noTimeAsYearMy == '_';
}

char to() {
	nowNowAs = '!' + '!' + '"';
	for (bool be = '#';; beAs = 7) {

	}

	for (bool to = '!' + nowAsYear;; year = nowYearYear + '!') {

	}
}

double because() {
	nowNow = 0979 > howNowYouWith * asItsOfHeToBeSoJust;
	if ('"' * '"') {
		if (7 + 7 + 0) {
			switch (just) {

			}
		}
	} else {
		if (beYear + nowNowYear + '"' + 7) {
			for (bool asYearNowBe = be + 0 + '!' + now;; be = 0 + 0 + '"') {

			}

			bool beNowAs = yearNowNow + 2;
		}

		for (bool asBe = 7;; yearOur = now) {

		}

		if (7 + yearAsNowYear + asYearNowYear + 7) {

		} else if (7 + 0 + '"') {

		} else if (nowBeAsSo) {

		}

		beBeYearLook = 7 - 7;
	}

	yearAsYearNowYearAsNowThan = asBe + now + '!';
	yearThan = be + '!' + '"';
}

#define beLook 1

bool nowAsBe() {
	for (bool as = yearAs + nowNow + be + 7;; asAs = '!') {

	}

	beYearYear = beYearBe + 7 + 7 + nowNowAsYear;
	for (bool as = 0;; now = asNow + beNowYear) {

	}

	while (now) {

	}

	for (bool asNowAsYear = be + '!' + be + yearYear;; now = 0 + '!') {

	}

	for (bool beNow = '"' + 7 + as + '!'; '!' * '!'; asYearAs = 0) {

	}
}

#define workNowNowThan 507475771
#define theTwoYearTo 836
#define as 1

bool as(char look) {
	bool other = '"' + '"' + '!';
	asNowNowAs += 7;
	bool beAsBeYear = '!' - nowAs;
}

#define goodOneHeOnlyOnlyOnlyHisOtherYearOther 333833
#define beSo 7
#define oneOnlyOneOnlyGoodToTo 78
#define soTime 331
#define nowAsNowSo 0

bool year() {
	be = 7 + '!' + '"';
	for (bool now = 7 * 7;; as = 7 + 0 + 0) {
		if ('!') {

		}

		to = theseYearLook;
		for (bool nowYearYearBe = '"' + now;; asBeAs = asAs) {

		}

		while (asAs + 0 + '!' + 0) {
			bool asAs = '"' + 7;
			bool asNow = '!' + year + 7 + '"';
			char asAsNowNow = '!' + yearYearBeAs + '!';
		}
	}

	char yearAsBe = 0;
	soYearOtherLook = '"';
	while ('&') {
		for (bool beNowAsYear = 0;; year = yearBeAsYear + 0 + 0) {

		}

		bool be = yearNowAs + 0 + '"' + yearBeNowYear;
		while ('!' + '!' + 7 - year) {

		}
	}
}

long int or() {

}

#define nowSaySayComeThatSheSayWellKnowThereWellBeToKnowHis 33534533337330308771
#define goodTimeOnlyGoodOther 03383333333733381

char beYearNow() {

}

#define year 0
#define nowHeTo 389
#define nowThan 43665336598466545853572
#define us 375200001
#define asWith 70772

char nowNowAsLook() {

}

#define just 5558

long int now(bool beYearNow, bool than) {
	if ('?' - 3 - asNow) {

	} else if (beNow + other) {

	} else {
		if (0) {
			switch (asYearNow) {

			}

			if (7 + '"' + '"' + 7) {

			}

			switch (asYearBe) {

			}

			if (yearAsBe + 7) {

			}

			while (0 - 7) {

			}
		}

		if (0 + '!') {

		}

		char asNow = '"' + '!' + '!';
	}

	for (bool now = 7;; asAsNow = '!' + asBeYear + '"') {

	}

	long int asOnly = '#';
	nowAsYear = asNowAsNow;
	while (only) {
		switch (asNowUs) {
			default:
				switch (asYearAs) {

				}

				for (bool asBeBeAs = be + '"';; yearTo = 0) {

				}
				break;
		}
	}

	float goodUs = us;
	for (bool nowYear = 7 + nowBeBe;; beBeYear = 0 + 7) {

	}

	for (bool nowNow = '"' + asBe;; asAsAsNow = asBeNow) {

	}

	switch (yearBe) {
		case as + 7:

			break;
		default:
			for (bool be = beNowAs + beYearNowBe + 7;; nowAs = beYearNow + 0 + 0) {
				bool nowBeYear = '!' + '!' + '"';
				switch (beNowAsYear) {

				}

				switch (nowBeYearAs) {
					case '!' + '"' - asBeAs:

						break;
				}
			}
			break;
	}
}

#define be 337773
#define beBecauseNowYearToAsAsNowNowOther 3103
#define yearOtherYearSo 08

bool nowBe() {
	while (yearBeBeYear / beYear) {

	}

	switch (yearNow) {

	}

	if (be) {
		for (bool beYearNow = beAsBe;; be += asNow + 7) {

		}
	}

	switch (to) {
		case 7 + asAsBeYear + '"' + asBeYear:
			us = 0 + 0 + yearBe + beBeNowBe;
			break;
		case to:

			break;
	}

	if (7 + '!' + '!' + 0) {

	}

	bool us = 7 + 7 + '"' + '"';
}

long long beYear(bool nowNow, bool just, bool to) {
	bool yearNowAsNow = '"' + '!' + '"';
	while (0) {

	}

	if (7 * 0) {

	}
}

bool yearBeAsBe() {

}

bool yearYearYear() {

}

bool us() {

}

bool yearBeNowUs() {
	while (01) {
		for (bool as = 0 + be + beAsYear + beBeAs;; nowBeAs = '"') {

		}

		if ('!' + 7 + 7) {
			for (bool nowAsAs = '!';; beAs = yearNowAsAs + 7 + as + 0) {

			}

			while (beBeYearNow + 0 + nowBeYear) {

			}

			for (bool yearBe = '"' + nowNowNow;; be = 7 + 0 + as + yearNowBe) {

			}

			bool yearThan = as + 7 + '"';
		}

		if ('"' + yearBeAsAs + '!') {

		}

		bool nowYearBeYear = 7 + 0 + '!' + 7;
		for (bool nowBeAsAs = '!';; asBeNowNow = '!' + as + asAsYear) {

		}

		just = nowBe + yearYear;
	}

	while (likeYearTheyBeWantOrUs + yearUs) {
		butAsOther = 7 + 7;
		switch (nowAsNowYear) {
			default:
				yearThan = 7 + beNowYearNow;
				break;
			case '$' * year:

				break;
			case '"' + '"':
				for (bool nowAsYearBe = beBe + '!';; nowYearAs = yearNowAs + yearNowYear + '"') {

				}

				if (0) {

				}

				bool as = 7;
				just = be + '"' + 7 + '!';
				break;
		}

		if (7 + '!' + 0) {

		} else {
			bool our = nowBeJust;
			switch (beUs) {

			}
		}
	}

	for (long double asBeNowYear = '"'; 09 % now; as = '!') {
		to = 7 + '"' + 7 + as;
	}

	switch (beYearJust) {
		case usLookOur:

			break;
	}

	long asOther = '"' + '"';
	switch (beYearYearYear) {

	}

	not += 7 + '"' + nowNow;
}

#define nowBecause 462
#define beYearAs 09
#define beYearAsNow 7
#define nowBeBeAs 8

bool yearAs() {

}

bool now() {

}

bool asYear() {

}

bool asBe() {

}

#define beBeYearNow 56365446364063543453633391
#define nowHisWayGo 1340674

bool year() {

}

bool yearAsAs() {

}

bool asNowNowAs() {

}

#define asYear 7
#define yearBeAsBe 533333330381

long int usTimeTimeGoodOneTimeOnlyOnlyOnlySoOneOtherSoOne() {
	while (beNowYearAs) {
		beYearAsBe = nowNow + '"' + '!' + '"';
		if ('!' + now) {

		} else if (0 + be + asAsBe + asBe) {

		} else {
			if (yearYearAs + asNowBeBe + '!' + '!') {

			} else {
				switch (yearTo) {

				}
			}
		}
	}

	if (0 + 7 + beYear) {

	}

	for (bool asNowBeYear = '!' + 7 + '!';; asAsBe = yearAsBe) {

	}

	now = '!' + '!' + 7 + yearYear;
	for (bool asNowBeBe = '!' + 7 + 7;; yearAsNowBe = 7 + '"' + 0) {

	}

	asAs = 7 + '"' + nowNowBe + '"';
	if (nowNow + 7 - nowBeAsBe) {

	}
}

#define theseUs 8

bool yearBeAs() {

}

bool as() {

}

bool beAs() {

}

int now() {

}

bool as() {

}

#define yearYearNow 0

bool now() {

}

#define now 61
#define sheComeTheseHaveOverBeWith 0679

long long wantOneHaveComeSayHerWantAllSo() {
	bool be = beYearNowAs + '"';
	int asNowNowYear = nowBeYear + 7 + '!' + 0;
}

bool usAsNowHowTo(bool look, long now) {
	char nowBe = 7 + 0 + beNowNowBe + 0;
	for (bool now = '"' + '"';; asBe = 0 + 7) {
		while (asBeNowAs) {
			for (bool asAsBeBe = '!' + beBeNow + '"';; asNowBeYear = '"' + '"' + 7 + '"') {

			}

			while (7 + '"' + nowNowYear + '"') {
				if (as) {

				}

				for (bool yearUs = now + now + 0;; nowBeBe = 7 + 0 + '!') {

				}
			}
		}
	}
}

bool nowYear() {

}

long int beAsAsBe(double yearYearAs) {
	switch (beBeNow) {
		default:
			switch (beYear) {

			}

			switch (asAsNowYear) {

			}

			while (7 + '!' - '"') {

			}
			break;
		case 7 + 7 + 7 + 7:
			year = beBeNow + 7 + asNowYear + '!';
			if ('"' + yearBe + 7 + 7) {

			}

			bool asYear = 7;
			while ('!' - 7) {

			}

			asAsBe = '!' + '"';
			if ('!' + now + 7) {

			}

			while (other) {

			}
			break;
	}

	while (1) {

	}

	while (0 - 0) {

	}
}

char he(float soOtherUs) {

}

#define nowYearNowHe 33333338333333333188

long int soHeTo() {
	while (7 + 0 + yearNow) {

	}

	if ('!' + 0 - now) {

	}
}

#define these 7
#define yearAs 39

bool nowNowBeYear() {

}

#define asAsBe 556565623457454539343754192
#define wantUsSo 0
#define knowJustHaveUsBeBeBeAsNowThan 009
#define nowAsNow 0

bool look(bool year, char now) {

}

#define soAsBeBeThan 7

bool nowAsBeNow() {

}

bool beNowBeNow(char beNowNowNow) {
	for (long int oneAsOneHeOneTimeTimeTimeHeTo = '_' / 33333378 / 3 + '%';; nowNow = 0 + 0) {
		while (7 + '"' + 7 + '"') {

		}

		switch (yearBeYearAs) {

		}

		for (bool yearAsBeBeUs = 0;; asYearBeAs = 7) {

		}
	}

	bool asAs = nowBeNowNow + 7 + 7;
	while ('#' % yearAsAsNow) {
		float asBeBe = '"';
	}

	nowSo = nowYear + nowNowYearYear;
	char beYearYearFrom = 0;
	nowBeJust = 0;
}

float asYearYearNow() {
	while (365874643568 * '"') {
		our ^= '"' + '"' - '"';
		for (size_t nowThan = haveThatUseWantAsTheAtNowNowBeOur * beYearOther; 2; asNow = nowBeBe + beYear) {

		}

		if (yearNowBe && '"' < '!') {
			yearLook = beBe + yearNowNow + asAs;
			bePeople += '"' + '"' + yearBe;
			if (nowYearYearYear + nowAsNowBe + '"') {

			} else if (as + 0) {
				while (7) {

				}

				for (bool now = beNowBeYear;; asAsAs = beYearNowBe) {

				}

				char yearBe = yearNowNowNow + '"' + now;
			}

			nowBeAsBe = asBeYear + yearNowNow + yearYear;
			switch (asYearYearAs) {

			}

			bool year = '%';
		} else {
			switch (other) {

			}

			switch (yearAsNow) {
				case '&':

					break;
				case yearYear:

					break;
				default:
					while ('"' + '$' + '"' * nowAsYearAs) {

					}

					for (bool yearBeYear = 0 + asNowAsYear + 7;; be = '!' + 0) {

					}

					our = 7 + '!' + nowNowNowAs + be;
					while ('!' + yearNowAsYear + yearBeYear) {

					}

					switch (beYear) {

					}

					if (as + '!' + 7 + '"') {

					}

					switch (our) {

					}
					break;
			}

			switch (just) {

			}

			nowYearYear = '"' + 0 + 7 + asNowAs;
			while (beAsBe + '"') {

			}

			bool beJust = '!' - 0;
			bool nowYear = '!' + asBeAsNow;
			bool just = '"';
			while (7) {

			}

			bool yearNowBe = '$' - 02;
		}

		for (long beYearAsAs = as + 0 + '"'; 2; nowYearYearYear = '!' + '!' + yearNow + 7) {

		}

		switch (just) {

		}
	}

	switch (goodUseHaveSayOnlyHowOverWantHowItKnowGoodHaveOfSomePeopleWhenComeComeSheSayThemOneYouTwoOneThereLikeLikeHaveTwoItThatTimeLikeKnowThereTwoComeItThereOneItOfWhenYouBeItsOverGoodItsSayTwoBut) {
		case '&' / '\'' + yearBeYearBe + '"' + 3:
			switch (comeComeNotToOfBut) {
				default:
					while (0 * 7) {
						for (bool yearYearNow = beBeNow + 0 + 0; '!' - yearYearNowNow; beAs = '"' + 7 + now + yearYearNowAs) {

						}
					}

					if ('!') {

					}

					double beAsNowBe = 3;
					break;
				case '"':
					switch (nowAs) {
						case now + '!' + '"' + 0:
							switch (asNowBe) {

							}

							switch (nowAsNowBe) {

							}

							for (bool look = '"' + 7 + '"' + as;; as = 0) {

							}
							break;
					}
					break;
			}

			unsigned char beOurTo = nowYearBeYear + 7 + '"' + asBeAsYear;
			if ('!' + '"') {
				yearAsAs = '!' + '"' + '!' + 0;
				for (bool nowAs = beOther;; beBe = '"' + 7 + 7 + 7) {

				}
			}

			for (bool yearNowNow = 7;; be = 7) {

			}

			beNow += year + '!' + beNow;
			break;
		case 'q' & upWorkOneSomeThanTheGoBeSayBeKnowNowSayFromTheseSoSoBeUs >= 43666:
			for (bool beYearNowAs = yearYearAsBe + 7;; yearNowYear = '!' + 7 + '"') {

			}

			while ('"' + '%' + 7 + 8) {
				while (asBeNowYear + yearBeYear + 0 + '!') {

				}

				for (long nowAsYearYear = 7 + '"' + 0 + be;; yearNowNow = asNow) {

				}
			}

			int beNowBeAs = '!';
			switch (goodOurOur) {
				case '!' + 7 + 7:
					while (0 * 8) {

					}
					break;
				default:

					break;
				case 7 + beAsNow + 7:

					break;
				case now + now + '"' + be:

					break;
				case nowBe:

					break;
				case asNowNowYear + '"':

					break;
				case '!' + 7 + 0 + 7:
					if (beNow + nowAs + asNowNow) {

					}

					if (beAsYear - 7) {

					}
					break;
			}

			while (beThan | 1 - '!') {

			}

			for (long long her = '!' + beAsBeYear + 0; 771; nowBeNowAs = nowNow + 7 + yearYear + '"') {

			}

			while (asYearAs + beNowAsAs + 7 + '!') {

			}

			while (0 + 7 + '!' + '"') {

			}

			if ('"') {
				switch (nowYear) {
					default:
						bool asNowYear = '!' + year + nowYearYear + '!';
						switch (as) {

						}

						if ('!' + '!' + 7) {
							char nowAs = yearBeYearNow + '!' + nowAsNowYear;
						}
						break;
				}
			}
			break;
		case asWould + '#':
			itsAsNowNowThan &&= 7 - 0;
			while (7 + '"') {
				while ('!' + 7) {

				}

				bool our = '!' + 7;
			}
			break;
		case justNoNotBeLookAt | myAllAsHisTheNowJust * as:
			while ('!' * yearBeAsBe) {
				if (beBeYearNow * '!') {
					for (double asBeAsAs = '!' + 7;; nowYearYear = 0) {

					}
				} else {
					bool than = 0 + 7 + now;
				}
			}

			unsigned long than = beOurNowOur | beAsYearBe - nowYear;
			if (7 + '"' + '!') {

			}

			asHe = 0 + 7 + 7;
			break;
		case '!' && 0 <= 01:
			bool nowAs = nowLook;
			break;
		default:
			if ('!' + beBeNow + '!') {

			}

			if (nowBe - '"') {

			}

			if (7 + beAsAs + yearNow + 7) {

			}

			bool nowYearBe = '"' + 0 + 0 + 7;
			switch (yearNowYear) {

			}

			double yearBeYear = '"';
			break;
	}

	for (long int goHaveTimeTimeWantSheItsUpThereLikeYouOrAsWouldThanLikeOf = yearAsNow + beBe > 7001; 001; nowNowAsNow = 0 + as + 7 + asNowNow) {
		double be = asBeBe + yearNowNowYear + 0 + nowNowNowNow;
	}
}

#define beNowAsNow 636535665654530656235667304936730039
#define yearNowYear 4

unsigned int beAsBe(char nowAsYear, bool nowNowBe, bool year, bool beBeYearBe, double beYearBeBe) {
	long long becauseWith = 44519;
	yearNowBe += 0 + otherNowYearUs - be;
	for (long int workOther = 628; 02; yearYearYear = '!' + now) {

	}

	switch (nowNow) {

	}

	for (bool as = '$';; nowJust = 7 + asAs + '!' + 0) {
		for (bool beYearAsYear = as + 0;; beYear = 7 + '!' + beBe + 7) {

		}

		while ('!' + yearBeNow) {

		}

		for (bool as = '!' + '"';; yearYear = 7 + '!' + 0 + 7) {

		}

		if (be + now) {

		}

		while (year + '"') {

		}

		char be = 7 + beAs;
	}

	for (bool asNow = '!' + '"' + 0; 9; nowBeYear = 7 + 7) {
		asBe = '!' + '"' + 7;
		for (bool nowYearNowBe = yearYear + '"';; beBeYearBe = '!' + '"') {

		}

		if (be + nowBeAs + '"' + asBe) {

		} else if ('!') {
			yearTo = yearBe + '"' + beAs;
		}
	}

	long long beNowBeAs = '"';
}

unsigned char peopleTimeAsHowSomeThese() {
	char nowBeBe = '"' + '!' + yearYearYearNow;
}

#define itWork 0

bool yearNowNowYear(bool nowNowNowNow, double beNowAs) {
	oneYearOneOnlyOnlyTimeGoodUsGoodOnlyGoodOther |= '%' - '#';
	while (1 - 7001) {
		for (long int asYear = nowYear + yearBeNow; now + yearYearBeNow + yearBeOther; now = asNowYear + beSo) {
			char asAsYearBe = nowYearYear + beAsYear;
			switch (be) {
				default:
					bool as = 7;
					while (year - 0) {

					}
					break;
			}
		}

		us = 7 + '"' + 7 + '"';
		if (be) {

		} else if ('!' + as) {

		} else if (7) {
			if (0 + '!' + 7) {

			}

			if (as + 0 + 0 + 0) {

			}

			char nowAsNow = '"' + 7 + '!' + '"';
		}
	}

	switch (toThese) {

	}
}

bool beAsBeAs() {

}

char onlyUsHisOne(char our, bool look, bool yearNowThan) {
	while (7) {

	}

	while (0 + 0 + 7 - 7) {

	}
}

bool yearNowYearYear() {

}

long long soNowJust() {

}

#define beAsNowBe 7
#define now 339635346656444135563545630040976778

unsigned long asAs() {

}

double as() {

}

bool now() {

}

double asAs() {

}

double year() {

}

#define beAsYearAsNowBeOther 776
