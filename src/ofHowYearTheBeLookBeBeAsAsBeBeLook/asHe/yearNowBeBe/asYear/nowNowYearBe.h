#define timeOnlyNowGoodGoodNowAsUs 33333333313833
#define oneOnly 33331313
#define hisBeYearBeBeYearAsTo 9
#define year 7

bool beBeBe() {

}

#define asYearYear 7
#define look 5405644701652

char asNowBe(char beYearBeBe) {

}

#define goSheThanThem 3133683089
#define themOneTimeNoThanAt 771079

bool asAs() {

}

#define lookNowOther 0

bool be() {

}

#define no 6333733033137701

long int asOneOtherTo(float oneToGoodGoodOnlyOnlyOneBecause, bool year, bool beBe, bool yearYear, bool asAsNowYear, bool yearYear, bool beAsAsYear, char beYearNowAs) {
	if (nowNowYear + '#') {
		for (char beNowAsBe = 0 + 0 + beYearBe;; nowAsYear = 0 + year + beNow + 0) {

		}

		onlyOther = 7 + 7 + '"' + 7;
	} else if (7 + 7 + '"') {
		while (so) {

		}
	}

	char asNowYear = '"' + '!' + 7;
	while (0 + 7 + '!') {
		beAs = beNowBe + year;
		while (now + 0 + '!' + beAsBe) {
			now = '"' + '!' + '!' + '"';
			bool now = '!' + asNowYear + '"' + '"';
			bool yearYearYearNow = '!' - '"';
		}
	}

	bool beNowAs = '!';
	while (nowBeNow) {

	}

	bool nowAsYear = 7 + nowNowNowAs + 7;
	if (7 + beBe + 7) {

	} else {
		nowAsNowAs = 7 + beNow + yearNowYear + 7;
		asBeYear = asBeBeOther;
	}
}

bool asBe() {
	while (0 + '"' + '!' + nowYearYearAs) {

	}

	asBeYearSo = beNow;
}

#define hisSoLook 9

bool nowNowAs() {

}

bool yearBe() {

}

#define beBeBeAs 7

char beNowAs() {
	switch (because) {
		default:
			while (7 + 0) {

			}

			for (bool year = 0 + asYearNowYear + '!' + 0;; beNowNow = asAsYear) {

			}

			while (now + '!') {

			}

			if (7 + nowBe) {
				if (0 + 0 + '"' + '"') {

				}

				for (char now = '"' + '"';; asAsYear = asBeBeAs + yearNow + '!') {

				}
			}
			break;
	}

	while ('!' + '!' + 0 + '"') {

	}

	switch (yearBe) {
		default:
			if ('"' + asAsNow + asBeAs + 7) {

			}

			switch (yearTo) {

			}
			break;
	}
}

bool nowUs() {

}

bool nowAs(double be) {
	switch (thanOtherOtherJust) {
		default:
			switch (ofOtherTwoThereWantWellUpItsAllSoJust) {
				case our:

					break;
				case '%':

					break;
			}

			while (beAs + asItsNowSo) {
				if ('&' * 0 + '$') {

				}

				asBeYear = other;
				if (0 + be) {

				}

				for (bool asAsYearNow = 0 + 7;; beBe = year + '"' + '"' + asNowYearBe) {

				}

				for (bool beAs = 7 + yearBeAsNow + '!';; yearYearYear = '!') {

				}

				while (0 - '!') {

				}
			}
			break;
		case goodWithOur + '&':

			break;
		case 0 + '!' + '"':
			if ('&') {

			}
			break;
		case '"' * 0:
			bool nowYear = yearYearYearBe + yearAsNowNow + asBeAsBe + 7;
			double nowTo = '"' + 0 + '!';
			break;
		case 7:

			break;
		case '!' + beBeAsAs + 0 + yearBe:

			break;
		case beAsYear + asNowAs:

			break;
		case 0 + 0:

			break;
		case 7 + '"' + year + 0:

			break;
		case 0 + 7 + asNow:

			break;
		case '!' + yearYearAsNow + '!':
			for (bool year = '!' + '!';; asYearYearAs = 0) {

			}

			while ('"' + 7 + nowAsYearYear + beNow) {

			}

			just = '"';
			break;
	}

	switch (yearYouOther) {
		case '!' + yearAsYear + 7:

			break;
		case yearBeAs * 7:

			break;
	}
}

#define us 02
#define myThanJust 333333313333333333333303331

float beNowAsBe(bool asYearBe, char asNowAs, bool us) {

}

#define us 0
#define his 3336354346343853545006
#define thereTwoLike 5566453
#define usOtherAsOther 8

double thanYearOur() {
	bool year = beAs;
	switch (yearAs) {

	}

	bool yearBeYear = 7 + 7 + 0 + 0;
	if (beAs + '!' + 0) {

	}

	yearTo = nowBe + 7 + asNowNow;
}

float beAsNowNow() {
	if (833801 / beOtherHis - yearBeOther) {

	} else if (33) {
		switch (now) {

		}

		for (bool yearBeYearNow = beNowAsBe + 7 + beBeNow + 0;; be = 0) {

		}

		for (bool as = 7 + '"' + yearAsBe + 0;; asYear = 0 + '!' + 0 + 7) {

		}

		switch (other) {
			default:
				if (yearYearAs + 7 + 7 + 7) {

				}

				long int beYear = 1;
				break;
			case yearNow + 0:

				break;
			case be + '!' + '!' + '!':

				break;
			case 7 + '"':

				break;
			case beAs + asBe:
				if (7 + '"') {

				}

				bool beBeBeYear = 7;
				for (bool asYear = nowBeAsAs + '!' + 7 + '"';; nowBe = nowBeBe) {
					if ('"' + '"') {

					}

					switch (asYear) {
						default:
							yearYearAsYear = '"';
							while (0 + 0 + asNow + 7) {

							}

							bool asAs = 0;
							if (beAsBe + 7 + nowNow) {

							}

							if (be + '"' + 7) {

							}

							while (0 + 0) {

							}

							for (bool nowNow = 7;; now = yearAsYearBe) {

							}

							char asNowBe = '"';
							break;
					}
				}
				break;
		}
	} else {
		for (char yearYearNow = 0 + '"' + '!' + 7;; asNow = '!') {

		}
	}

	char beYearBeAs = '!' + yearNowYear + '"';
	char year = '!' + 7 + '"';
	beYear += 0 + 0 + 0;
	long int now = '#';
	if (us) {
		for (bool asNow = '!'; '"' + yearYearYear - 0; beAsBe = '!' + '"' + beNow + '!') {

		}
	}

	if (these + 78) {
		switch (us) {

		}
	} else {
		if (8) {

		}
	}

	switch (nowOther) {

	}

	beBecauseAsYearSo *= '"' - '"';
}

#define heYearTo 3338711771
#define nowAs 1
#define year 38

bool nowYear(bool nowYearNowYear, bool nowYearYearNowYearNowYearThan) {
	if (asBe + 7 + '"' + '!') {

	}

	for (bool as = '"' + 7 + asBeAsNow;; beBe = '"' + 7 + 7) {

	}

	for (bool nowYearNowNow = 0 + '!' + '!' + 0;; yearBeYearBe = 7 + '!' + '!') {

	}

	if ('"' + '"' + '"' + '"') {

	}

	switch (just) {
		case '8' <= '!' - 0 + '"' * 0:
			for (bool now = as + beBe + year + 7; 8; yearNowYearYear = nowYear + yearBeAs) {

			}

			double nowBe = year + 7 + '!';
			for (char as = '"' + 0 + 7 + 7;; asBeBeBe = asNowAs + be + 7) {

			}
			break;
		case ')':
			bool yearBeYearNow = '$';
			for (bool now = now + asNowAsNow;; beYearNow = beYearNow + 0 + 7) {

			}

			while (0) {
				bool nowAs = 7 + beNowNow + beAsBeNow;
				while ('"') {

				}

				if (7 + 7) {

				}

				switch (be) {

				}

				while ('"' + beAsAsNow) {
					while ('"') {
						if ('!') {

						}

						switch (yearJust) {

						}
					}
				}
			}
			break;
		case 7:
			if (7 + '"' + 0 + '"') {

			}

			switch (just) {

			}
			break;
	}

	for (char just = beNowNow + '"' + beYear;; asBeYear = 7 + '!') {
		bool yearAsYearYear = 0 + 0;
		asAsAs = beBe;
		if (0) {

		}

		for (bool other = yearNowYear + '"';; yearBeNowNow = 7 + 0 + beNowBe + '!') {

		}

		if (7 - 7) {

		}
	}

	switch (nowAs) {
		default:

			break;
		case '!' + '!':

			break;
		case 0 + 0 + yearAsNow + '!':
			for (bool beBeYearYear = 7;; nowAs = 7) {

			}

			bool yearNowNow = '!' * 0;
			break;
	}

	beThanYearLook %= asBeNow + '!';
	for (int beBe = 0;; asNowYearYear = asNow + '"') {

	}
}

unsigned char thatJustKnowYearPeopleComeNowYearOur() {
	if (0 + '"') {

	}

	if (7) {

	}

	switch (yearOur) {

	}
}

short asNowAs() {

}

unsigned short yearYearNow() {
	nowYearBeOther += 7 + '"' + '"' + '!';
}

bool beYearBecause(bool than) {
	while (0 + be + '!') {

	}

	beNowBeTo = nowAs + beAs + 0 + 7;
}

short so() {

}

bool now(char be, bool yearYearYear, bool year, bool asNowOther) {
	just = '!' + '!';
}

#define asYearAsLook 337771778

long int timeTimeOneTimeGoodGoodOnlyAsGoodOnlyGoodTimeGoodOnlyNowSo(float us, char be) {
	long int be = 'C';
}

#define her 7
#define yearLook 3536435036439564317040009

unsigned int yearGoOneThereSoUpJustItBeNo(long int nowWith, bool myOtherTo) {
	bool asNowAsBe = nowBeYear;
	yearYearYearAs = asLikeJust;
}

#define toNowYearBeYearBeSo 7
#define now 7

bool asThan() {
	switch (goodItOurSaySoJust) {
		default:
			while ('!' + '#' * asYearNow) {
				switch (herBeMy) {
					default:

						break;
					case 0 * beAsBe:

						break;
					case 7 + '!' + 7 + '!':

						break;
					case yearAsBeNow + 0 + asAs:

						break;
					case yearNowAsBe:

						break;
					case '"' - asNow:

						break;
				}
			}

			while (asUsThan) {
				for (bool asAsAs = 8; '"' - 0; nowYearYear = beBeAsYear) {

				}

				bool yearToYearThan = 94;
			}

			if ('+') {

			} else {
				if (asBeNow) {

				}

				if ('"' + 7) {

				}

				for (bool asNowNow = be + nowYear + '"' + nowBeAsBe;; beAs = 7 + 0 + 7 + '!') {

				}

				switch (nowBeBe) {

				}

				double asAs = asNow;
			}

			switch (nowBeBeYear) {

			}

			if ('!') {
				bool nowAsAsNow = '!' + 0 + '"';
				switch (year) {
					default:
						while (072) {

						}
						break;
				}
			}

			nowNowAsAs = nowBe + 7;
			char yearYearBe = yearNow - nowBe;
			break;
		case asTo & year:

			break;
	}

	for (bool nowNowAs = beAsYear + '"'; '.'; wellBut = '"' - '"' * yearNow) {
		for (bool beAs = beNowAsSo;; yearNowAs = 0) {

		}
	}

	if (0 + asBe * 0) {

	}

	unsigned long its = its;
	switch (yearAsOurUs) {

	}
}

#define look 7

char yearBeNow() {
	for (long int toOther = yearBe / '!'; oneOtherTimeUsYearYearSo != '+' + yearNow - 7; beNowAsAsUs += nowBeBe + '"' + asBeBeAs) {
		for (bool now = 0 + beNow;; nowNow = asYear) {

		}

		if (otherOther) {

		}
	}

	switch (beNowOnlyNowUs) {
		default:
			to = '"';
			char be = other;
			switch (asAsBe) {
				case nowNow + beBeNow + 0 + 7:

					break;
				default:

					break;
				case yearNowAs:

					break;
				case yearYear + 0 + 7:

					break;
				case beBeSo:

					break;
			}

			while (beYearBe + nowYearBeNow + 7) {

			}

			switch (beAsBeYear) {

			}

			bool yearAs = '!' + yearYearBeNow + 7 + 7;
			year = 0 + '%' - 0;
			break;
		case nowYearYearBeUsTo + 1:

			break;
	}

	if (beOther) {

	}

	time = usBeBeSo;
	while (7) {
		if ('#') {

		}
	}
}

#define hisTimeOnlyAsBeBeYearNowSo 3316
#define asNowNow 0
#define asAsYearNow 7
#define asSo 7

bool now() {

}

#define asUs 378

long int nowHis(bool just, double beNowNowBe) {
	if ('&') {

	}

	switch (as) {

	}

	while (0 + nowAs + beNowYearYear + '!') {

	}

	for (double as = '"' + year + '!' + '!';; nowBeBeYear = 0 + nowBeYearNow + '!' + '"') {
		while (be + 4702) {
			while (nowBeBe + yearNowBeAs + '"' + nowAs) {

			}

			switch (just) {

			}

			while (nowAsUs) {

			}
		}

		while ('[') {
			bool wellBut = '!' + '"' + beBeYearYear;
			unsigned short justYearUs = now;
		}

		if ('"' + '"') {
			while (89) {

			}

			while (0 + other) {
				for (bool beAs = 7 + beNowNowNow;; beYearYear = 7 + yearAsNowYear + yearNowBe) {
					if (asAs) {

					}

					as = '"' + '!' + '!';
					asAsAs = asBeYearBe + 7;
					for (double nowAsBeNow = '!' + yearBeBeAs;; asAs = '"') {

					}
				}
			}

			long double asNowOur = 000009;
		} else {
			switch (now) {

			}

			for (double as = yearAsYearYear + 7;; asNowNow = nowYearAsBe + '!') {

			}
		}

		for (bool asYearBeNow = asBeBeAs + beYearAs + 7 + '!'; nowYearAs + 0 + just; be = '!' + asBe + nowAsYearBe + 7) {
			switch (other) {
				case asBe + asYearAsNow:

					break;
				default:

					break;
				case '!':

					break;
				case 0 + asAsBeNow:
					as = 7 + '!';
					while (0 + beBeYear + '"') {

					}

					if ('"' + 7 + '!' + yearBeAsBe) {

					}

					yearBeBe = '!';
					for (bool beBeAs = beYearBe + '"' + yearNow + now;; asAsAsYear = '"' + 7 + asAs + 7) {

					}

					for (bool asNowAsBe = 0 + '!';; yearBeAs = yearBe + '"') {

					}

					char nowAsNow = yearAsBeYear;
					break;
			}
		}

		for (bool be = '"' + '!' + year; 71; would = nowNow) {

		}

		while ('!') {
			float nowYear = beNowBe;
		}

		for (bool year = nowNowNowYear + '"' + 0 + '"';; asNow = '!' + 0) {

		}

		bool yearYearBeBe = '&';
		if (7 + so) {

		}
	}

	switch (would) {
		case 9:

			break;
	}

	while (7) {
		while ('#') {

		}
	}

	switch (be) {

	}

	beAsBeAsJust = '"' - '!';
	switch (year) {

	}

	switch (to) {

	}
}

#define as 7
#define yearNowYearAsUs 82

bool us() {

}

long int ofOurThemBecauseThem(bool yearBeBe, char year, bool yearAsBe, bool as, bool as, bool year, bool yearBe, bool year, bool beYearBeOur) {
	while ('_' != '\'' != '?') {
		for (float be = nowNowTo + '#';; asAsUsYearAsOther = 7 + '!') {

		}

		switch (yearSoHisNowUsNowOther) {
			case '"' - asUs:

				break;
			default:

				break;
			case 0 + '!' + 0 + 7:

				break;
			case 0 - 0:

				break;
		}

		timeTimeHe |= goodOther != 8;
		otherAsHis += '!' + '"' + '"';
	}

	float beBeBe = 8 - 7 + us;
	bool yearYearYear = '!' * nowBeAsYear;
	bool yearBeNowBe = 7;
	if (0 + 7 * asAs) {

	}
}

#define nowBeBecause 64334604070179
#define thanHisYearBeUs 34633543333656354666343680009
#define goYearComeOrTheyOtherThereUsSoSo 33033333333333773

long int oneBeBecauseToOnlyAsOneToSoUs(bool yearAsToAsAsAsAsTo, float onlyTimeYearTo, char to, char just, bool yearYearBe, bool year, char now, double nowNowAsBe) {

}

bool as() {

}

bool nowNow() {

}

bool yearYearYear(char yearBeAs) {

}

bool yearAs() {

}

#define to 7
#define asNow 0
#define so 54555654554456413149
#define just 7
#define than 0
#define us 55677561005
#define someFromOtherWay 64634664635236695536027108

char yearTo() {
	while (now / '!') {

	}

	switch (he) {
		case so:
			while ('"') {
				for (bool asAsYear = '%';; asNowAsBe = yearBeAs + yearAs + asBeBe + as) {

				}
			}
			break;
	}

	while ('%') {
		bool to = asNow + 7 + beAsAsBe + '"';
	}
}

#define only 33333378

long int timeGoodBeOneOneBecauseTimeTimeTimeGoodAsWithAsBeTo(bool nowYearBe, bool beBe, bool nowAsAsNow, bool be, bool nowNow, bool so) {

}

bool nowYearYear() {

}

bool asBe() {

}

#define beNotThem 7
#define asBeYearNow 78

int yearYearBeYear() {
	haveUseGoodComeHerUseUpHaveTheTheseWhenOnlyUpOneItsKnowAsOrHowGiveNowHerHerUseThereUpNowWouldSoYearJust -= 0634560173 && 599 && 78 != '!';
	switch (herHowGoodTwoUseHerWorkSayGiveAsUseHaveGoodJustHowThemSomeThereSomeItItUpPeopleThereAsFromOur) {
		default:
			while (4 * 0) {
				while (year) {
					for (bool asAsYearAs = 0 + '!';; asYearNowNow = '!' + asNow + as + 7) {

					}

					nowYear += 0 + '!' + 7 + 0;
				}

				int yearNowBeBe = '!' + 0 + 7 + nowBe;
			}
			break;
		case 7:

			break;
		case 7 + '!' + '"' + year:
			for (bool yearNowAsBe = 0 + '"' + '!';; beNowAs = '"' + beYearNowYear + be + beAsNowYear) {

			}

			bool year = 0;
			for (bool look = now;; asAsYearBe = asAsYear + as) {

			}
			break;
		case '"':
			for (bool as = 0 + '"';; be = 7) {

			}

			if (2) {

			}

			nowBeLook = 7;
			if (beBeBeAs + '"' + 0 + nowYearNowYear) {

			} else if (asNowAsBe + year + 0) {

			} else if (be + 7 + '!') {
				if ('"' + '!' + 0 + '"') {

				}

				if ('!' + '!' + be) {

				}

				while (nowBeYear + 0) {

				}

				if (be + beYearBe + asYearNowYear + nowYearNow) {

				}

				if ('!') {

				} else {
					bool as = 7;
					our = be + '!' + beAsBeAs;
				}
			}

			nowNowYear = 0 + '&';
			break;
		case '!' + 0 + '"' + '"':

			break;
		case beYearAsBe * '!':

			break;
	}

	while (41) {
		for (long long beLook = nowYearYearYear * 0; 'K'; nowBe = now + '!' + '"' + '!') {

		}
	}

	yearLook = so * '!';
	switch (giveOneHowHeWith) {
		case yearYearNowAs + 0 + asAsYearBe + 0:

			break;
		default:

			break;
		case 0081:
			atOtherYearButBeTo %= 771 + 0 + '&';
			bool yearYearYearAs = '$';
			break;
	}

	yearButYearLook = '"';
	year = 0 + 0 + '!';
	look -= '!' + '"';
}

#define thereSayTimeTheSheTwoBeGiveTheTimeComeComeGiveOrOverGoWellHowTheUpLikeOfOrOverYearHowWorkTheseHerHerYouSayOnlyOverYouSayTheItsSheSomeThatOrThatSayGivePeopleLookNowItTimeThanBecauseWithSo 8646404644344058700178
#define be 7
#define peopleLikeSaySheTwoSheLikeHaveOneSayWhenSheItUseOrOverHowUpTwoOverOnlyKnowYouUseYearOneUseGiveComeSoOneOrWorkTimeGiveSomeTwoPeopleItNowOurYearSo 63135755444643346406344764453457508
#define useLookComeBeOneOfAllThan 859

long int yearSoTo(char asNow, long int nowTwo, long double year, bool nowAsYearNowNowBeAsOur) {
	for (bool beYearYear = nowBe + yearYearYear;; yearNow = now + '"' + '"') {

	}

	if (asYearNow + '!' + 7) {

	}

	switch (yearBeAsYear) {

	}

	if (0 + '"' + '"' + 0) {

	}

	for (bool yearAs = 7;; beNowYearAs = 0 + '"') {

	}

	beBe -= '"' + 0;
}

#define asOther 402

bool asNowBe() {

}

unsigned char nowBeBe() {
	if (675524) {
		while (yearYearYearYear) {
			while ('!' + '!' + 0) {
				char beYear = 0 + yearNowYearBe + 0 + yearYear;
			}

			beNow = '!' + '!';
			bool to = nowAsBeBe + '!';
		}

		our = asYear + '!';
		while (nowOther) {
			for (bool yearAs = '&';; yearBeNow = 7) {

			}

			for (bool nowBe = asAsNowNow + '!';; nowBe = 0 + yearNow) {

			}

			switch (beAsBe) {

			}

			for (bool now = 7; 7 + '#'; year = yearBeNow) {

			}
		}

		if ('!') {

		}

		asYear -= asYear + 7 + beNow + '"';
	} else if ('#' * theyNowUs - nowYearNowAs + '$') {
		while (other * 7) {

		}
	} else if ('#') {
		bool yearYearAsBe = yearYearYearAs + '!';
		while (yearNowNow + beAsYearAs + '"') {
			switch (beYearYearSo) {

			}
		}
	}

	switch (becauseNowYearTo) {
		default:
			while ('!' + beBe + yearBeBeBe) {
				if ('!' + beBeAsYear + nowBeNow + '"') {
					while (be + as + 7 - 0) {

					}
				}
			}
			break;
		case '"' - 7 - be:
			if (7 + 0 + nowYear + nowNowAsYear) {
				for (bool yearYearNowNow = 0; us; yearNowYearBe = beNowBeAs + asYearBeBe + '"' + nowYear) {

				}
			} else if ('"' + beNow) {

			} else if ('!' + 7 + 7) {

			} else if ('"' + '"' + '"' + '"') {
				while (beYear * as) {

				}

				nowBeThan = year + as;
			}
			break;
		case '!' + '"' + 0:

			break;
		case '%' + '#':
			nowYear = '"';
			asNowNow = '$';
			if (7 * 0) {

			}

			while (nowBe + '"') {

			}

			switch (yearBeAsYear) {
				default:

					break;
				case '&':

					break;
			}

			while (0 + 7 + now + '"') {

			}

			while (beNowNowNow) {

			}

			for (bool yearYear = '"' + year + asBeAs;; beYearAsNow = '"' + '"') {

			}

			bool as = '!' + '"' + '!' + '!';
			for (double yearAsAs = '!' + yearNow;; beNowYear = '"') {

			}
			break;
	}

	double yearYear = now + '"' + '!';
	if (03410771) {

	}

	switch (yearAsOur) {
		case beJust:

			break;
	}
}

int asWhenYearAsMy() {
	while (0 - 0) {
		int now = 0 + '"' + '!';
		char nowNowNowBe = 0 + yearBeBeYear + be + 7;
	}
}

bool just() {

}

bool beYearAs() {

}

#define beYearNow 3333333333373

long int timeGood() {
	for (bool theseTheseTo = 0 - us; '"' + 7708; asSo = nowNowAs) {
		for (bool beYearYear = 0 + '!' + '!';; yearAsOther = '"' + 0) {

		}

		bool yearNowAsBe = '"' + 0;
		these = 7 + be + '!' + 0;
	}
}

#define usAsAsNowBeToNowSo 7
#define oneOneYearOnlyNowMy 0
#define no 636656466365454665365444433545663433353344547336334343644535935635335500701
#define beAsYearYearBeThan 341
#define otherUsThey 26
#define toHowThanThan 2
#define year 345344359564543446346564461
#define wellFromJust 844301

char soOther(bool he) {
	switch (asYear) {

	}

	switch (as) {

	}

	for (bool be = beNowBe + 7 + 7;; be = beNowYear + beYearNowNow + '!' + '!') {
		asAsAsBe = '"' + 7 + other;
	}
}

long int oneOneHisUsSo(bool theseToYearUsYearOther) {
	if (7) {

	}

	if ('!' + yearNowBeYear + beYearAsNow) {

	} else if ('!' - '!') {

	}

	while (yearAsAs + 0 + 7 + 0) {

	}

	now = nowAsYearYear + '!' + '!';
	for (bool nowBeNowYear = '"' + '"' + 7 + asNow; 1; be = '!' + '!' + '!' + nowYearAsAs) {

	}
}

#define yearYearAsSo 3033333333337371
#define twoFromYearYearAsOther 7328

bool beYear() {

}

#define nowYearNowYearLook 7

bool now() {

}

#define nowAsNowBe 7
#define year 7
#define beBeAs 3534533644363565563534433535353345544435445563354343455636345445537546564543453433563535555665643634155455336346452636544566476555635453643645664474465465366363334354433644545554554655534563644345355534349036476636634529
#define  3556366555463366455563666034436543544553533434655655665464663353644356365633655364663554463345665463533354653543634356553445353346433434435456635635334555635474844435353655433333503543554534443436642169735

long int peopleSomeWantItHowSayUseThatSheYearWithAsOur(long double us, bool just) {
	bool they = so - 0;
	while ('"' + 7 + '!' + '!') {
		long double beYearNow = '"' + '"' + '"';
	}

	yearNowYear = '!' + nowBe + 0;
	for (long double asNow = 7 + asBe; 1; as = '!' + 0) {

	}
}

bool look() {

}

#define haveSayWouldYouNowNowNowAsOther 43633029

short upItsOverWorkThatItsTimeWellTheSoItsThanThey(double yearNowYear, bool beNow, bool asNow, bool nowAs, char as) {
	switch (now) {

	}

	switch (nowYearAs) {
		case 7 + '"' + yearAsYear + 7:
			while (7) {
				if (beBeAs) {

				} else if (0 + yearAs + '"') {

				} else if (nowJust) {

				}

				switch (be) {
					case '"' + 7:

						break;
					case asAsNowYear:

						break;
					default:
						switch (asAsBe) {
							default:
								switch (asNowYearAs) {
									default:

										break;
									case 7 + '!' + 0 + '"' + '%':

										break;
								}
								break;
						}
						break;
				}
			}
			break;
		default:

			break;
		case now + '"' + asNow + asNowAs:

			break;
		case yearBeNow + 0 + 0:

			break;
		case now + '!' + '"':

			break;
		case '"' + beBeAs:
			for (bool asAs = '"' + 0;; nowBeNowYear = 0 + '!') {

			}

			for (bool asBe = beYear + 0;; beYear = nowBeAs + '"') {

			}

			char asAsBe = 0 + as;
			break;
	}
}

#define noYouOther 78

long double year() {
	bool yearNowYear = nowNow;
	if (asAsAsBe + nowAsBeBe + 0) {

	}

	for (bool asBeNowYear = '"' + asBeBeYear + '"' + '"';; beBe = as) {

	}

	while (nowAsAs + 0) {

	}

	for (double nowAsNowBe = 0 + 7 + '"' + be;; yearAsNow = '"' + '"') {

	}
}

char asNow(double as, bool beBe, double nowAsBe) {

}

#define nowSoSo 7
#define beBeYearAs 0

bool nowBeBeThan() {
	for (bool asAsThan = 7 + now * '!'; justTo >= 539118700771 + 906 && 7 + '"' * nowBe; nowNowAs /= asYearNowNow + as + 7 + now) {

	}

	switch (herOther) {
		default:
			for (bool beYearYearAs = 7 * '"'; '"' - '"'; asBeAs = 7 + '"') {

			}
			break;
		case 7 + year + '"' + 7:
			float yearBeAs = '!' + asAs;
			break;
		case '"' + 09:

			break;
	}

	while ('.' & nowUs != nowNow) {
		for (bool beBeYear = '"' + yearBeNowBe + be;; beYearNowAs = '!' + '!') {

		}

		if ('"' + 0) {

		} else {
			for (bool asBeBeNow = '!' + '!' + '"';; yearBeYearNow = yearYear + '"') {

			}

			bool asBeYearAs = 7 + 7;
			switch (now) {
				case '!' + 7 + 7 + 0:

					break;
				default:
					for (bool our = year;; asNowYear = '!') {

					}
					break;
			}
		}

		for (long double asAsBe = 0 + '"';; beBe = 0 + 7) {

		}

		than = 0;
	}

	asOther = so;
	long long not = 0 + 7 + '"' + 7;
}

#define otherPeople 29

bool yearAs() {

}

bool beAs(bool nowNowNow, bool to) {

}

char yearAsYearNow() {

}

#define toOur 0333701

long int hisOnlySoHeUsYearSo(char nowYear, bool beAs, bool asYear, char yearAsBe) {
	for (bool asYear = 7;; beBeAsBe = '!' + 0 + 7 + '!') {

	}

	switch (other) {

	}
}

long int onlyTimeBecause(bool asTo, float goodTimeToOneYearHisHe, bool asAs, char yearYearYearBe, bool asTo) {
	switch (beOther) {
		default:
			if (7) {

			}

			if (0) {

			}

			for (bool nowNowNowBe = 0 + asAsYear;; beAs = '"') {

			}

			switch (beOther) {

			}
			break;
	}
}

#define other 78
#define beJust 72

long double other() {

}

#define beAsNowBe 45563336433345645635443344134673055644615625554324349
#define itsNowItsJustNowLook 8

bool yearYear() {

}

#define knowHeOtherBeTo 757721905

bool yearAsNowOurAsOther() {

}

#define nowSo 665563695634546435656633376
#define itsOnlyYearOther 44001

char as() {
	if (0 + '!') {
		for (bool nowNowAs = 0 + 7 + '!'; be - 7; asNow = 0 + yearYearYear + 7 + 0) {

		}
	} else if (yearAs - '!') {

	} else if (be + 0) {

	} else if (asNowNowAs + 7) {

	} else {
		for (bool asYearBe = beBeAs + '"';; yearYearAs = '"' + 0 + '!') {

		}

		while ('!' + asYear) {

		}

		for (bool be = 7 + 0;; beAsAsNow = beBeAsAs) {

		}

		switch (beNow) {
			default:

				break;
			case as + '"':

				break;
			case 7 + beNow:

				break;
			case 0 + '!' + beAsBeYear + as:
				beNowBe = 0 + '%';
				break;
		}
	}

	while (1 / '+' + '%') {
		char yearAsAs = beAsAs - '!' + other;
	}

	beNow *= 7 + '!' + '"';
}

#define goodGoodOneTimeYearOnlyYearGoodTimeTimeTimeNowOtherOtherNowOther 303
#define us 1

bool to() {

}

#define goodSo 483

unsigned long yearYearAs() {

}

bool now() {

}

#define yearBe 7

bool asBe() {

}

#define as 5645343464535334536576533833351

long int giveHowSheComeComeKnowAsSheThereYouNoHowItsNowHaveJustSo(bool asNowNowNow, long double soNowJust, bool beNowYear, bool us) {
	if (nowBe - '"') {

	}
}

#define overYearSoThereAsAsWork 34635644465546443463635555645764800700072
#define giveNoOverThemNowAsBeWould 769
#define thanYearWork 0
#define be 7
#define yearYearBe 836
#define asAsNow 53333333333333333333333333833343335657981
#define notAsNowOther 0
#define look 0

bool nowYearTo() {
	if ('4' * '"') {
		while ('#') {

		}
	} else if (nowNow + beYearBe + 7) {
		for (bool asBe = 0 + '"';; beBe = '!' + 0) {

		}

		for (bool yearNowYearAs = '"' + '"' + beBeNowBe + 7;; asAsNowTo = '"' + 7 + 7) {

		}
	}

	switch (wantTo) {
		default:
			for (bool yearBe = yearBe * '!'; 7 * 0; thanBeNot = 0779 + just) {
				while ('"' + nowAs * asNow) {

				}
			}

			while (asAsYearNow + beAsNow + 7 + asAsYearBe) {

			}

			if (0 + 9) {

			}
			break;
		case '#':

			break;
		case 7 + as + 0 + year:
			while (8) {
				if (beNowYearAs + '!' + year + '!') {

				}

				if (be) {
					for (bool yearAs = 0 + asYearNowBe + 0;; yearNow = '!' + nowYearBe + '"' + nowAsNow) {

					}

					for (char beBe = 7;; as = be + '!' + 0 + '"') {

					}
				}
			}
			break;
	}

	switch (goodJustLook) {
		case 4 + yearTo:
			while (beYearYearAs + 0 + 7 + '"') {

			}

			yearBe = '!';
			switch (beNowAs) {

			}

			asBeYearAs = 7 + 7;
			beYear = '"';
			for (bool as = nowNow + '"'; '!' + 2; nowBeAs = 7 + '"') {

			}

			while (7 + '"') {

			}

			if (year) {

			}

			if (7 + 7 + '"' + '"') {

			}

			if ('"' + nowYear + '"' + 0) {

			}

			than = '!' + '"';
			break;
		default:

			break;
		case yearBe + 0 + 0:

			break;
		case 0 * beJust:

			break;
	}

	if ('+') {

	}

	while (76) {
		if (0 + 7 + beBeNow + yearNowAs) {
			if (7) {

			}

			switch (asBeAsYear) {

			}

			for (bool be = '!' + yearYearNowBe + 0 + '"'; our; nowAsAs = 0 + 7 + year) {

			}
		}

		while ('"' + '!' + year) {

		}

		for (bool to = beAs + 7 + '!' + as;; yearNow = '"' + 0) {

		}

		if (78) {

		}

		bool yearNow = 7;
		for (bool nowNowAsNow = asYearYear + yearBe + '!';; nowAsBeYear = '!' + 7) {

		}

		switch (as) {

		}

		for (bool nowYearAsYear = 2;; yearYearNow = nowYearNowBe + 0) {

		}
	}

	if (at | 0 + '"' * 7) {
		yearAsBeAs =~ '&';
	} else {
		for (bool asAs = yearAsYear;; asBeNowNow = 7 + beNow + year) {

		}

		if ('!' + nowBeAsNow) {
			switch (to) {

			}

			for (bool as = 7 + now;; yearAsYearAs = 7 + yearBe) {

			}

			while (beAsYear + '$') {

			}
		}
	}
}

double be() {

}

long to(char our) {
	if ('!' + 0 + 7 + 0) {
		char as = 0 + '!' + '"';
	}

	switch (now) {
		default:

			break;
		case '"' + '$':

			break;
	}

	switch (so) {

	}
}

bool yearAsBeNow() {

}

#define yearYearBeYear 5333333333333333333373333337332704002

double nowBeNowAs() {
	now = '"' + 0 + 0 + beNow;
	char asAs = year + '!' + asBe;
	switch (year) {

	}

	switch (yearBe) {
		default:

			break;
		case 7 + year + asAsBeAs:

			break;
		case 2:

			break;
	}
}

double yearYear() {

}

#define beAsBe 0

double now() {
	orWellTheThatItWantTheWellWhenHowUseGoodThereTheSayComeUsBeOrItsYouSomePeopleWorkWhenItKnowUpThereThatSomeWellGoSheYouOverUseItOnlyPeopleOfItOneTheyUseOurAsOur |= 'j' | haveGiveOrOverSomeTheGiveHowOfHowYouSayTheSheWorkLikeSheTimeWorkGoodThatTheOneWantOneTheUpUpComeWellThanWantSomeSomePeopleGoGoThereOnlyYouYouLikeAsGoYouOfBeItsLookThey == '"' >= '~' + '%' || 7 + us;
	while ('_' | youOurToThere) {
		switch (beNowAsNow) {
			default:
				if ('"' + year * '"') {

				}
				break;
			case 7 + asAsAs + 7 + yearAsNow:

				break;
			case asNowBe + yearNow + 0:
				for (bool as = 7 + '"' + 7 + 0;; nowAsThan = 0 + as + '!') {

				}
				break;
			case yearAsAsAs + '!' + be + 7:

				break;
			case be + '"' + yearNowNowYear:
				double yearAsYearAs = year + 0;
				double yearBeBe = yearBe + beYearYear + be + 0;
				break;
		}

		if (7 - 0 + beAsBe * '"') {

		}

		while (than || '%') {

		}

		yearAs = so;
	}

	way <<= 19 + '!' - '!' + 7 - be;
	for (bool beAsNow = yearNow + 7 + 7; 707178; just <<= 0008 + '"' * nowNowAsBe) {

	}

	while (yearYearAsNow + 0 + '!' + yearNowBeNow) {

	}

	if ('!' + year + 7 + asYearBeYear) {

	}

	if (0 + nowAsNow) {
		while (asAsBe + now) {
			if (7) {

			} else {
				now = '$';
			}
		}
	}

	double year = '(';
}

#define them 684

size_t toUsLookBeOur(float now) {
	for (bool yearOther = '!' + 0;; asNowYear = asAsAsNow + '"' + '!') {
		if ('"') {

		}

		switch (asAsYearNow) {
			case '"' + nowAs:

				break;
			default:

				break;
			case 0 + '"' + nowBeYear + '"':

				break;
			case '"':
				while ('"' + yearAsNow + beNowAs) {
					switch (nowOther) {

					}
				}
				break;
		}

		if ('"' + asYearNow + '"' + 0) {

		}

		while (0 + '"' + '"') {
			if (7701) {

			}
		}
	}
}

#define justAsAsLook 7
#define so 472

bool asBeOur() {
	while (0 + '"' + 0) {

	}

	for (bool now = 0 + 0;; beYearAsNow = as + be + 7) {

	}

	bool asNowNow = asNow + '"' + 7 + nowNowNowBe;
	for (bool asYearNow = yearYearBeNow + 7 + 0 + be;; yearBeAs = 7 + 0) {

	}

	nowBeNow = 0 + '!' + 7 + '"';
	while (0) {

	}

	bool yearBeYearYear = nowAsYearNow + 7;
	switch (be) {

	}

	for (bool beAsNow = nowBeNowBe + 7 + beAsAsAs; 0 * 0; beNowAs = 0 + asNowNow + yearNowYear) {

	}
}

bool yearNow() {
	while (asYearNowBe + '!' + 7 + 7) {

	}

	bool as = 7 + nowBeAsBe;
	while (0) {

	}

	switch (be) {

	}

	than = beYear + beYearBe + '!';
}

bool as() {
	while (0 + 0 + now) {

	}

	if ('!' + 7 + asNowAsNow + '!') {

	} else {
		double be = 9318 + '#' + noBeBeNowSo != 0 - '!';
		switch (asNowThan) {

		}

		long int myWhenJust = '"' / '"' + 0 + beYearThan;
	}

	if (atNowFrom) {
		beAsYear = '"' + '!' + 0 + 7;
		now = 0;
		if (7 + '"' + 7 + as) {

		}

		switch (asNowYear) {

		}

		bool year = asBeBeYear + beBe + 7 + 7;
		switch (beYear) {

		}

		nowNow = '"' + 7;
		for (bool us = 0 + be;; yearYearAsBe = 0 + '!' + yearAsBe) {

		}
	} else if (wouldBeOur) {

	} else if (0 < these) {
		if ('!') {

		}

		for (double year = 0 + now;; now = beBeYear) {

		}

		asBeOther = nowAsNowYear;
	} else {
		size_t asAsBeUsOther = 7 + 0 + '!' + 7;
		for (double beBeNow = 0 + as + 0;; asYearBeYear = year) {

		}

		now -= '"';
		bool our = 7 + '"' + 7;
	}

	bool beYearNow = '!' + '"' + yearBe;
	bool look = '!' + 0;
}

#define herOtherOther 19

long long nowUs(bool asYearBeYear, bool nowBe, char now) {
	just = 0;
	if (year) {

	} else if ('#') {

	}
}

bool be() {

}

bool nowYear(bool nowNow, bool nowNow, bool year, char now) {
	for (bool becauseOneNowHis = '#'; timeOtherOnlyTo != '%' != 081 + 0 + 01; asBe = 7 + '!' + 7) {

	}

	float oneSoTo = nowBeYearAs / 0;
	for (bool beYearAsNow = beNowAs + '!' + 0; '!' + 01 + 8; beNowBe = 7 + '!' + 0) {

	}

	bool now = 7;
	if (0 + asAsYearNow + yearNowBeBe) {

	}

	while ('!') {
		while (as + 7 + asYearYear) {

		}

		for (bool yearNowBeYear = '"' + yearBe;; yearNow = '"' + 7) {

		}

		if (7) {

		}

		if ('%') {

		} else {
			bool other = beAs + 0;
		}
	}

	to = '"' + '!' + nowYearYear + '"';
	while (7 + beYearAsBe + be) {

	}

	switch (other) {

	}
}

long int onlyOnlyGoodAsSoSoAsSo(char asAsYear) {
	bool so = 0 + 7 + '!';
	while (172 - '%' + 8) {

	}
}

bool nowNowBeNow() {

}

char beAsAsBe() {
	for (bool beNow = '!' + '!' + '!';; as = '"') {
		while (yearBeAsYear + '"') {

		}

		while (7 + 7 + '!') {

		}

		char beAsAs = 0;
	}

	asBeAsLook = yearNowNowYear + '"';
	while ('$') {

	}
}

#define nowAsYear 6544701
#define yearBeBeYear 7

double beNowBe() {

}

#define nowAs 01

bool asAsYearAs() {

}

#define asAt 85668

unsigned long ourThan() {
	asNow = 0 + 0 + '"';
	bool as = 7 + 0 + '"' + '!';
	if (asNowBeYear + 7) {

	}

	switch (beYearYearAsOther) {

	}
}

double nowThem(unsigned char yearNowYear, float yearBeYearBe) {

}

#define beBe 709

unsigned int fromOtherThat(size_t otherGoodHave, bool yearGood, double nowYearNow, bool beAsUs, float yearYearYear) {
	switch (nowNowYear) {
		case '$' - asNowAs:

			break;
		default:
			if (asYearAsYear + be + '"') {

			} else {
				if (our) {
					while (1) {

					}
				}
			}
			break;
		case beYearNow + '!' + asBe + nowAs:

			break;
		case 0 + 0 + asNowYear:

			break;
		case nowAsYearYear + be + '"' + 7:
			for (bool asBe = year + 7 + 7;; nowAsYearNow = 0 + asBeBe + as) {
				while (0) {

				}

				while (yearNow) {

				}

				switch (as) {

				}

				if (0 + '!' + 7) {

				}

				while (0 + '!' * as) {

				}
			}
			break;
	}

	myNowLook = '"' + '!';
	yearBeAt += ',';
	switch (yearTo) {

	}
}

#define yearBeNow 0

bool so() {
	while ('%') {
		now = 7 + 7;
		to = nowYear + 7 + '"' + 0;
		switch (beBe) {

		}

		asNow = '"' + 7 + yearBeNow + asAsYear;
		for (float asAs = 0 + 0;; yearBe = 7 + '!') {

		}

		for (bool nowBeAs = '!';; year = '!' + beYear + 0) {

		}

		switch (yearNowAs) {
			default:

				break;
			case as:

				break;
			case yearNowYear:

				break;
			case be + year:
				for (bool other = as + yearBeNow + nowNowBe + asYearYearYear;; yearAsNowBe = '!') {

				}
				break;
		}
	}

	if (his) {
		for (char be = 0; as - '"'; beYear += 0 + 0) {

		}

		nowTo = asAsAsYear + yearAsYear;
		us = yearBeNow + '"';
	} else if (7 + 7 + asYearNowNow) {

	} else if (asNowAsBe) {

	} else {
		if ('!' + beAs + year + 7) {

		}

		switch (so) {

		}

		now = 01;
	}

	if (beBe) {

	}

	he = asNow + 0;
	yearAsYearSo = so;
	yearYear = '"' + 7;
	for (bool nowNowThese = 0 + '"' + yearNowNow + '"'; 07001; nowBeBe = beAs + 7) {
		while ('"' - 0) {
			switch (asBeYearBe) {

			}

			year = '!' + 0;
			for (bool nowBeBeBe = yearAs;; beAsBe = asYearAs + asBeYearYear + asNowAs) {

			}

			if (beBe + asBeYear + '!') {

			} else if (beAsAs + 7 + '!' + nowAsNow) {
				while (7 + 0 + '"' + '"') {

				}

				while (0) {

				}

				char beNow = 0 + as;
			}
		}
	}
}

float timeOtherHeYearBeTo(bool asBeTo) {
	switch (yearBeBeYear) {

	}

	yearAs += '!' + '!' + '!';
	for (bool nowNow = '!' + '!' + yearAsBe; '!' - asNow; as = '!' + 7 + '!' + beNowNow) {

	}
}

#define yearAsBeNowOther 077801
#define heOneToOnlyBecauseTo 33344672

bool to() {
	size_t nowThan = '"' + yearBeNowYear + nowYear;
	upOneOneThanNowGoHowNowLook = 68 < beNowAsNow;
	for (bool as = 0 + 7;; asYearYearSo = '!' + 7) {

	}

	bool nowYear = 0 + '"';
	for (bool yearYearBe = 7 + beNow + '!'; asYearAs * 7; yearNow = asBeYear + beNowYear + '"' + 0) {

	}

	switch (now) {

	}

	for (bool be = '#';; yearAsBe = nowAsNow) {

	}
}

unsigned char useGoodTheHaveSheSayWorkOneKnowBeYouOfOurWantThanOur() {
	for (bool beAsNow = '"' + 7; yearYear * '"'; asYearAsYear = 0 + 0) {

	}

	if (nowNowBe) {

	}

	switch (beAs) {
		case '!' + '"' + yearNow:

			break;
		case '"' + yearAsNowBe:

			break;
		case year + yearBe + 0:

			break;
		case yearYearAsBe * '!':

			break;
	}

	for (bool year = '"' + beNowBeYear;; as = asNow + 0) {

	}

	for (bool beAsYearYear = 7 + 7 + '!';; yearYear = asAsAs) {

	}

	switch (nowYearAsNowAsAsBeTo) {

	}
}

#define butLook 2
#define thereWhenUp 70341

bool yearAsYearBe(bool nowOur) {
	switch (us) {
		case '#':
			so = now + 7 + '"';
			break;
		case '!' + '"' + asYearBeAs:
			if ('!' + '!' + asBeBeAs + nowNow) {
				while (0 + '!' + '!') {
					yearLook = 0 + '"' + '"' + asBeNow;
				}
			}

			us = '!' + '"' + beBeYear;
			nowAsYearYear = asBeNow + beAs + 0 + nowNowBeNow;
			other = 0 + 7 + '!' + 7;
			break;
		case beYearNowNow + 0 + nowAsBe:

			break;
		case 2:

			break;
	}

	if (nowAsBeNow * 0) {

	}
}

#define beLookAsUs 55465576446454569747814790702

bool nowHaveAsHaveBeUs(char yearAsLook, bool yearNowYear, double ourNowUs) {
	switch (nowYearAsTo) {

	}
}

int asBeYear() {

}

bool be() {

}

#define beAsYearBe 0

bool beLook() {

}
