#include <UUU6UUUUUUUA///1.h>
#include <h/1.h>

#define beOur 

short itOneHaveSheUpThereSomeThatYouUpYearOtherUsYearNowBeOther(unsigned char wantGiveHowNotOurTwoButYearGoBeNowNowAsThan, bool asNow, char butOur, long double timeWorkTimeWith, bool beOur) {
	unsigned long thatThereThereSoThatSomeLook = 75 < 05 + nowKnowOfOneJustYearJustYearLook;
	if (')' * nowYearYearBe) {
		bool be = beAs - '!';
		while (07701) {

		}
	} else if (other) {
		for (double yearAs = '"' + '!' + 0 + '!';; beAsBe = now + asAsAs + '"') {

		}

		while (yearBe + 0) {

		}

		beBeNowYearAsAsOther = '!' + beAsNowBe;
	} else {
		if ('(') {
			bool nowYearJust = '"' + asAs + 0;
			bool year = '!';
			if (2) {

			}
		}

		for (double now = '"' + 7 + '"';; beBeAsYear = nowAs + '"' + 0 + 0) {

		}

		while (just) {
			switch (nowBe) {
				case '$':
					if (0) {

					}

					if ('"') {

					}

					bool as = beBeNow + '"' + '!';
					for (bool beYearYear = '!' + 0;; nowAs = 01) {

					}
					break;
			}

			for (bool now = 7; 1; year = nowBeAs + '"') {

			}
		}

		if ('!' + asNow + 0 + 0) {

		} else {
			bool beNow = now + '$';
		}

		long double yearNowBe = asYear;
		otherNowYearThan &&= '"' * 0;
	}
}

unsigned long hisLookLookUsAsJust(unsigned char because, unsigned long know, double beNow) {
	float nowYearYearBe = 0 + beYear + beNow + year;
	if ('"' + 6) {

	}
}

#define asOtherBeYearWould 7
#define justJust 1029
#define toNowTo 384718
#define likeComePeopleComeThereBeUseTheyNowNowOtherUsLook 43056656567978

unsigned short otherLookHis() {
	for (bool asBeYear = be + 7 + as; as - 0; asBeYearYear = now + yearBeYear + asAsNowYear + '"') {

	}
}

#define otherOther 82

char be() {

}

#define be 333338333333383701

long int asNowAsAs() {
	for (bool nowBeUs = 0 + 0 + '!'; 7 - yearAsNowAs; asAsBe = 7 + 7) {

	}
}

long int nowHisAsYearOther(bool nowYearBe, bool yearAsYear, char now) {

}

#define hisYearSoNowUs 3333701

long int giveKnowTo(char beYear) {
	while ('!' + 0) {

	}

	if (45) {

	}

	for (double as = yearNow + nowYear + 0; 42 < '"'; beBe = 7) {

	}

	switch (theyJust) {
		case '4' - beAsBeBe:

			break;
	}

	switch (beBeBeAs) {
		default:
			while (nowNowAsAsOther) {

			}
			break;
	}

	nowYearJust = 0;
}

#define beAsNow 49704

long long as() {

}

unsigned int wayWhenGiveLikeUpYearOur(double yearYearNow) {
	switch (to) {
		case nowYearYear + 7 + now + asNowBeYear:

			break;
		case 8:

			break;
		case now + '!':

			break;
		case be + 0 + 7:

			break;
		default:

			break;
		case to + year * '!':

			break;
	}

	while (to - '"') {

	}
}

unsigned short now() {

}

bool asAs() {

}

bool asYearBeNow() {

}

char asYearNowNow() {
	while ('\'') {
		if (3976702 * 02) {
			if (0 + now + yearBeAs + nowBeYear) {
				bool asBeYearYear = '"';
				if (asYearBeAs) {

				}

				if (0 + nowYearAsYear) {

				}

				switch (now) {

				}

				switch (our) {

				}
			}

			switch (year) {
				default:
					while ('!' * '"') {
						if (0 - asBe) {

						}
					}
					break;
			}
		} else if (asYearYearYear + now) {

		} else {
			switch (than) {

			}
		}

		switch (theseNowNowAsYearYearYearThan) {
			default:
				for (bool beBeBe = 7 + beNow;; nowAs = 7 + '"') {
					while ('!' + 7 - asAsNowNow) {

					}
				}

				for (bool nowBeYear = 0 + asBe + '!';; asNowBeNow = nowNowAsYear) {

				}

				switch (not) {

				}
				break;
		}

		for (long int theseAt = 3209; beAs & 7 + '!' + nowBeNowYear + 8; asAsYearAs = beAsAs) {
			bool so = yearAs + '!' + year;
			yearYear = 0 + asAsBeBe + 0;
			while (0 + beBeAs + beYearBe) {

			}

			if (look) {

			}
		}
	}

	someTimeWhenOurToJustYearLook = '!' && '4';
	for (bool asYear = '!';; as = '"' + '!' + '!' + asNowYear) {

	}

	short beAsAsAs = 0 + 7 + 0 + '"';
}

#define year 0
#define yearNow 23
#define be 08

char yearBe() {
	for (char asBeTo = time - toAsAsOther; as + 7 != '#'; us = '!') {

	}

	for (bool nowAsAsAs = '"'; '"' + beNowAs / nowBeAs; nowBeYearAs = 0) {

	}

	bool nowNowYear = 7 + '!';
	long int becauseHis = 'O' + '%';
}

long int becauseTimeUsToHisSoAsHe(bool nowBeYear, bool beAs, char asUs) {
	while (0 + yearAsAs + 7 + '"') {

	}

	while (us) {

	}
}

long int beNowSo() {

}

#define oneAsSoOneUs 463
#define yearYearAsYear 553657565636553675436276457382
#define so 0

unsigned long nowLike(bool yearBut) {
	while ('!' + ')' + '&') {

	}

	if ('"' + usYearWithYearNowNowJust >= '<') {
		for (bool toAsUs = '&' + yearUs; '!' * 7; yearOther %= 9) {

		}

		bool itsGoodAtUsAsUs = '!' + nowAsBeBe;
		if (0) {

		} else {
			while (asNowYear + beYear + '"' + 0) {
				if ('%') {

				}
			}
		}

		us = beNow + beBeAs + 0 + 7;
	} else if (70001 / '!') {

	}

	switch (look) {
		case '"' - '!' - asBeAsAs:

			break;
		case 0 + '!' + 0:

			break;
		case now + 7 + '"':

			break;
		case yearYearBe - 0:

			break;
	}

	switch (yearYearAsAs) {

	}

	char asComeItBeSo = asBe + 0 + as;
	switch (nowAs) {

	}

	if (yearYearNow + nowBeBe + '"') {
		switch (yearBeNowNo) {

		}
	}

	if (beNowBe + 7 + '!' + 0) {

	} else {
		while ('!' + '!' + yearBeYearYear + nowYearNowBe) {

		}

		while (7 + asBeYearBe + '!' + 0) {
			for (bool look = '"' + '!';; yearYear = asNowNowNow + beBeBeYear + '"' + '"') {

			}
		}
	}
}

unsigned char yearYearAsNow(bool asNowAsYear, bool year, bool yearNowBeBeNowBeThan, bool yearNowNowBe, char asYearBeAs) {
	for (bool just = '!';; yearAsYear = be) {

	}

	switch (be) {
		case '"' + '"':
			if ('"' + '!' + nowNowBeAs) {

			}

			while (year * '"') {

			}
			break;
	}
}

bool beBecause() {

}

#define nowSo 60331733
#define otherTheseTimeOneGood 8

long int becauseGoodOtherNowYearTo(char other) {
	us += asAs + '"';
	if ('+') {

	}

	for (bool be = 0 + '"'; '!' + now + so; asBe = beBeYear + '"' + 7) {

	}
}

long int goodTo(char beYear, float time, bool asAs, bool asNowBeNow, char yearYearAs) {
	long int yearNow = 0 + '!' + 0 + 0;
	for (bool nowNowNowBe = 0 + asYear + '"' + '!';; yearYear = asAsBe) {

	}

	switch (year) {

	}

	for (float beBe = beBe + 0 + year + '!';; nowYear = beNowNowYear) {

	}
}

#define otherBeAsWith 08

bool now(bool yearAsNowNow, bool yearBeAs, bool beYearLook) {

}

bool year(double yearYearNow) {
	year = 7 - 0;
	bool yearYearTo = 0 + 0 + '"';
	if ('"' + onlyFrom) {

	}

	switch (beNowAs) {
		default:

			break;
		case '"' + 7 + '"':

			break;
		case asYear + 0:

			break;
		case '!':

			break;
		case nowNowBe + '!' + 0 + yearBe:

			break;
		case '!' + '"' + beAs + year:
			yearAsBeNowSo = yearBe + 7;
			break;
	}

	asYearBe = yearYearNowNow + '"';
	while (beBeBe * 7) {

	}

	if (as + nowAsBe + as) {
		nowBe -= '!' + yearAsAsBe + 7 + 0;
	}
}

double asYearYearNow() {
	onlyTwoWhenSayOfWantLookThereSayJustJustYearOther = '!' + 0;
	for (bool nowYear = 2;; nowBeYearNow = now + 0) {
		for (bool asNow = nowNowAsYear + '!' + nowBe + 0;; as = now) {

		}

		while ('"' + 0 + 0) {

		}

		asAs = 7;
		while (year + '!' + 7) {
			char nowAs = 7;
		}
	}

	switch (beAs) {
		default:
			nowBeNow = 6;
			break;
		case beAsBe + '!' + 7 + yearNowYear:
			char asNowYearAs = yearNowYearYear + '!' + '!' + 0;
			break;
	}

	for (bool asYear = beYear;; nowNowAs = beAs + yearYearAsBe + nowAs) {

	}

	while (0 + beYear + '!') {

	}

	if ('!' + '!' + 0) {

	}

	if ('!' + yearLook - 7) {

	}
}

bool beOur() {

}

#define twoLook 634041343777771

short toJustAt() {

}

bool as() {

}

bool our() {
	while ('"' != '_' - asYearOneTimeSo != goodToNowTheseOtherAsBeSoAsUs != '7' != now) {
		long int otherTo = 0 / us;
		goodOneNowTheseBeAsLook -= just;
	}

	goNowNowUsOther -= nowAsBe;
}

bool yearOur(bool yearNowAsNow, bool yearOther) {
	unsigned int  = 'g' == 4335666643443556634556544346435536444444463546466563565633466635345355346353465444563453443533566663654565563434643464656443536533633366566366663636556344653666666646563634646463554545555543463646555503435564354464535566646363633645443445556545014014669 != 'h' == 'v' >= haveOneSomeJustSomeAsOther == butYouTwoOurAsYearSoNowNowBeOther;
	short thereYearAsWay = 'x' & 'k' != beBeYearAsThan || 478 - ourJust;
	switch (nowYearBeYear) {

	}

	if ('#') {

	}
}

#define wantSayYouHaveWouldWhenOrWorkOverTheWellWorkOrSayHaveHowItUsWorkOneKnowHerGoOverNotGoUpYouOverWhenHerGoPeopleComeItsUpJustLook 6356354465656444464545364563656451
#define thereOrMyLikeTwoOurWhenHaveItItUpGoSheUpSoThatThatThatAllShe 59
#define  43343456466566446553555543353536463655655554665545366444694466446434435656666644664345656456544644551365455544366633633549463762700879

unsigned int peopleHerHerWellYearOverOneTheyWellWellTheHowOurGiveAsOurIts(float with, long orTwoNotLookToThan, long these, bool asBeAsAs, unsigned short some) {
	bool year = to + 781;
	switch (no) {
		default:
			while (asNow + beBeYear + 0) {
				yearAsOtherOther = 0 + 0 + nowAsBeBe;
			}

			for (bool nowBeNow = '!' + '"';; as = 7 + 0 + nowNowAs) {

			}

			than -= 0 + 71;
			break;
		case 78 - '!':

			break;
		case nowNow + '"' + '!' + '!':

			break;
		case than:

			break;
	}

	switch (so) {

	}

	as -= '!';
}

#define our 279249

long long nowNowAs() {

}

#define asBeNowBe 0
#define so 7
#define yearYearNowBe 465334355556534456443835612

double beYearBeBe() {

}

char asYearYear() {
	while (yearAsAsYear + '"' + year + year) {
		bool asYearThan = '!' + 0 + 0 + '!';
	}
}

bool as() {

}

#define nowGiveOurOur 81
#define asNowAsBe 7
#define nowMy 7

double asAs() {
	switch (year) {

	}

	if ('#') {

	}

	while ('"') {

	}

	bool other = '"' + 0 + beYear;
	year = now + '"' + 7 + 7;
	if ('!') {

	}

	if (nowBe + '!') {

	}

	for (bool nowAsAsNow = us;; be = yearNowAs + nowBe) {

	}
}

#define onlyTimeTimeOneTimeGoodOnlyOneGoodGoodGoodBecauseGoodOneUsHe 333131
#define otherNowToTo 2

bool asYearBe() {

}

bool nowYearBeNow() {
	for (bool nowYearYear = 7 + 0 + '"';; beYear = 0 + yearNowNow + yearBeBe + '"') {

	}

	while ('!' + '!' + 0) {
		switch (nowYearNow) {

		}

		if (7) {

		}

		for (bool now = '"' + asBeNowBe + nowBeBe; 7 + 7 - '!'; be = '!' + 0) {

		}
	}
}

unsigned long yearNowAsNow() {
	while ('"' + '"' + asBeAs + '!') {

	}

	if (as + yearNowNowAs) {

	} else if ('!' + '"' + '"') {

	} else {
		just = beBeBe + 7 + 0 + beNowAs;
	}

	if ('"' + yearBe + '"') {
		char yearBe = '"' + beYearYear;
	}

	for (bool now = 7 + asNowBeNow;; now = nowNow + 7 + year) {

	}

	if (other) {

	}
}

#define workOverWantNoSayWorkYearThanNowYearUs 3363465530664647337313
#define wantAsBeTheTo 6563606
#define notNowUs 02
#define usBeTo 72
#define asBe 0

bool yearAsAsYear() {

}

char be() {
	while (goodTimeHeToOtherOther + yearTime) {
		long int asToSo = yearBeBeBe + 0 + '!';
		switch (timeSoHis) {
			default:

				break;
			case asOther:
				for (bool asNowNow = yearNow + yearBeYear;; asBeYearYearAsNowAsTo = beBeYear + 0 + 7 + beYearBeYear) {

				}
				break;
		}

		while ('O' - '!') {

		}
	}

	if (so) {

	} else if (asNowNow / 7) {
		while ('"' + '"' + '!') {
			year = '"' + '"';
			if ('_' + '%') {

			}

			switch (nowBeAs) {

			}

			if (asBeYear) {

			} else if (asYearBe + '!' + 0 + now) {

			} else if ('"' + nowBeAs + 7) {

			} else if (beAsOther) {

			}
		}

		while (87771 - be) {
			if (beNow + '"') {
				while (7 + 7 + '"' + 0) {
					while (7 + 0) {

					}

					bool yearYearNowBe = 0 + asYearAs + beAsYear;
					bool yearBeYearYearYearNowBeOther = 7;
				}

				while ('!' + 0 + 0 + be) {

				}

				if (now + 7 + beNow + 7) {

				}

				beNowYearYear = '%';
			}
		}

		yearAsTo = 7 + yearNowAsAs + 0 + nowBeYear;
		switch (yearBeNowNow) {

		}

		if (beNowAsAs) {

		}

		for (bool yearAs = asBeYear + 0;; beNow = '!' + nowNow + '"' + 7) {

		}

		for (bool nowNowAs = nowAs + year + '!';; yearBeYearYear = '!') {

		}

		be = 0;
		switch (beBe) {
			case 7:
				while (8) {

				}
				break;
		}
	} else {
		switch (beAsYearAsAsUs) {

		}

		if ('!') {

		}

		asAsBe = '"';
		if (7 + '!') {

		} else if ('!') {

		} else {
			char beNowYearAs = yearYearNowAs + nowAsNowNow + '!';
		}
	}

	while ('%') {

	}
}

size_t nowYear(bool beBeYear, bool asYearUs, char yearNowAs) {
	bool asThan = '"' + 7 + year + 0;
	if (be + asNowAsYear) {
		for (bool now = be + beAsBeBe + 7 + 7;; asNowNow = yearAsBeNow) {

		}

		switch (now) {
			case '!' + 7 + nowBeBe:

				break;
			default:

				break;
			case to:

				break;
		}
	}

	switch (asBeNow) {

	}

	while (0 - now) {

	}

	while (nowYearYearAs * 7) {

	}

	while (us + '%') {
		asYearBeNow = '!' + '!';
		for (bool beNowNowAs = 0 + '"' + yearYear;; nowYear = 0 + yearYear + '!') {

		}

		for (char asYear = 7 + '!' + asYearAs;; beAs = 0 + nowBeNow) {
			switch (asBeAsOur) {
				default:

					break;
				case beNowYearYear + '"' + yearYearAs:
					bool asAsAs = as;
					switch (than) {

					}
					break;
				case 7 + as:

					break;
				case '$' - 0:
					switch (nowBe) {
						default:
							bool nowNowBeNowJust = 0 + 0;
							break;
					}
					break;
				case 9:

					break;
			}

			now %= 04 + other;
			while (7) {

			}

			for (double asOur = 7 + '"' + 0;; now = 7 + as + '"') {

			}

			switch (nowYear) {
				default:

					break;
				case 779:
					while ('!' + '"') {
						while ('!' + asAsBe) {

						}

						for (bool beNowYear = be + nowYear + 7 + '"'; 7 * '!'; beNow = beBeBe) {

						}
					}
					break;
			}

			while (1) {

			}

			switch (asNowNowYear) {
				default:
					switch (yearBeAs) {
						default:

							break;
						case asYearAsNow + '"':
							for (bool nowBe = nowBeAs + 7 + '!';; yearYear = '"' + '!') {

							}

							char nowNowYearYear = 0;
							break;
					}
					break;
			}
		}

		if ('u' == 5524446334665654453464568447344565554331556565329 >= orItOneThatOfUpWantBeOneGiveHaveUseWantSaySomeUpGoodYouOfTheKnowTwoFromOrThereItsComeOverWhenKnowWellUseOurOne && 'u' + 0 / 0 + 7 * '"') {
			for (double year = 0 + asBe + '!'; look >= youBeYearOurNowYearUs; heWellWhenWhenBeNotYearAsOur = 1) {
				switch (yearNowBe) {

				}

				char nowUs = 071;
				if ('"' + 7 + '"' + nowNow) {
					yearYearNowBe = '!' + 0;
					switch (beYear) {

					}

					while (0 * 7) {

					}
				}
			}

			while (7 + now + 0 + '!') {
				while ('"') {
					double nowAs = asBe + yearAsYear + asNow + 7;
				}
			}

			while (0 - 7) {
				switch (beYearAs) {
					default:
						while ('"') {
							if ('"' + 0 + beNowNowNow + '!') {

							}

							now = nowBeNow + 0 + beNowYear;
							for (bool beBeYearBe = 0 + yearBe + '!' + nowBeBe;; now = 7 + '!') {
								bool now = '"' + '!';
								if (as) {

								}

								bool asAs = yearAs;
								switch (asAs) {

								}

								beNow = asBe + 7;
								if ('"' + '!') {

								}

								switch (nowYearOur) {

								}
							}
						}
						break;
				}

				as = nowBeBe + 0 + '!' * 7;
				yearNow = asNowBeAs + 0 * '!';
				while ('!' + 0 + '!') {
					asYearAs = as + nowNowYear + 7 + '"';
					beYearNowBe += 0;
					while (yearAs + 0) {

					}

					if (nowAsAs + '"') {

					}

					if (7 + now) {

					} else {
						double yearNowYear = yearNowBe;
					}
				}
			}

			while ('&' + 0 < asBeYearNow) {

			}

			if ('"' + 0) {

			}

			switch (usThan) {
				case '"':
					asBe = '"' + yearYearNow + 7 + '!';
					switch (beNow) {

					}

					while ('"' + nowBe + 0) {

					}

					if ('"' + 0 + yearNow + 7) {
						other = yearAsAs;
						switch (as) {

						}

						if (7 + 7 + yearBe + '"') {

						}

						bool asYearBeBe = 0 + 7 + '!' + nowAsYearAs;
						for (bool asYear = yearNow;; yearAs = '!' + '"' + '"') {

						}

						for (bool yearAsAs = 0 + 7 + '"' + '"';; yearNowYear += '!' + 7 + asBeYearAs) {

						}
					}
					break;
			}

			for (bool year = 7 + '!' + 7;; beYearNow = 7) {

			}

			while (7 + '$') {

			}
		} else if ('!' + yearThanWould) {
			if (nowYearAs - yearBeNowAs) {
				while (0) {

				}

				for (bool nowBe = 0;; nowBeNowYear = asYearAsYear + 0 + now - nowNowYear) {

				}
			} else {
				while (asYearNowLook) {
					switch (us) {

					}
				}
			}

			for (long int yearYear = '"' + '&'; beNowAs * asAsNow + yearNowBe + 9; asNowYearAs = '!' + '!' + '!') {
				but = '!';
			}

			while ('"' + 0 + 0) {

			}

			for (bool nowBeAs = 7 + beBeYearNow + asYearBeAs;; yearUs += '&') {
				switch (yearYear) {
					case 0 + 0 + '!' + be:
						bool yearNowBe = be + '"';
						for (bool year = '$';; beAs = yearBeAs + 7 + '!' + 0) {
							if (7 + '"' + 0 + 0) {

							}

							switch (yearNow) {

							}

							if ('!' + 2) {

							}
						}
						break;
				}
			}
		} else if ('n' + 8) {
			for (bool now = 0 + beNow + '"' + now;; asBeYear = '"' + asBeYearYear) {

			}

			for (bool be = 7 + 2;; nowBeNowAs = 0 + 0 + 0 + beNowNow) {

			}
		} else if ('H' * beBeYear) {
			if (asAs + asNowBeAs) {
				if (7 + nowYearNowBe) {

				}

				while (071) {

				}
			} else if (0 + 0 + now) {
				double year = '!' + 7 + 7;
			}
		} else if (7) {

		} else {
			than = 0 + as + '!' + '"';
		}

		bool asNowBeNow = than || '!' - nowAs;
		while (otherOther < asAsBe) {

		}

		for (unsigned char butOfWantMy = '"' >= 82 - beBeAs; '!' | 'L' + 8; asJust /= 09 - '!') {
			bool beBeNowSo = '!' - as;
		}

		if (0) {

		}

		switch (yearYearYear) {
			default:
				for (bool thanNowOther = 0 + '!' + 7 + 0;; nowYear = 7) {

				}
				break;
		}

		switch (as) {

		}

		bool now = 0 + 7 + beYear + asNowBeNow;
		if (asAsBe + '!' + '!' + '"') {
			for (char as = nowNowBe + as + 7;; nowAsBeAs = 0 + '"' + year + as) {

			}
		}
	}

	whenUseWorkUpHowWantBeWantFromAsOur |= 6058 / sheSomeWorkNowKnowAsAsNowLook;
	short beThemAsNowJustNowBeUs = 6004667102 >= 3436 && 7 % beNowOther * our * '"';
	switch (workLikeOfNowWithFrom) {
		default:
			if (knowLookNowWith * '&') {
				while (708) {
					bool nowYearAsBe = '"' * asAs;
				}
			}

			for (bool asAsAs = '"';; asBe = '!' + year + yearAsNowAs) {

			}

			because += yearYearYear + '!' + beYear + '!';
			break;
		case 6 - '$':

			break;
		case '$':
			be += beYear + 0;
			break;
	}

	switch (beBe) {
		case '"' + beNowBe:

			break;
		default:
			bool nowAsOur = 7 + '!' + yearNow + 7;
			break;
	}
}

size_t thereWorkOtherOrTheThereWellOverHaveHaveWantTimeSheSomeHowYouSomeTwoWorkShePeopleKnowKnowWorkItOneItsWorkThanThatOnlyWouldWorkSayWayAsKnowUpWork(double yearBeYearAs) {
	switch (be) {

	}

	switch (yearBeNow) {

	}

	while (0 + yearNow) {

	}

	while (7 + '%') {
		while ('$') {

		}
	}
}

long long (bool asBe, float nowThan, bool yearAsJust, double all, long nowAsAsNow, long long toThe, double look) {
	heSaySheGoHisGiveItsOtherThanAsTo <<= 156039371 || 1 - now - 7;
	while ('p' | 'p' == 'g' & toTwoHerThanOfThereWayWhenItsYouWithOverUpSoAsJust >= 54566750209 || 05 + 7 + 5) {
		while (57067072 < '"') {

		}

		than .= '%' * 7;
		switch (asYearHeYearOther) {

		}
	}

	unsigned char orWhenThe = as + '"' + '!';
	soWantGoodAsOur >>= ']' * yearOur;
	switch (nowNowAs) {
		default:

			break;
		case 0 + '"':
			bool yearAs = '$';
			break;
		case '!' + '!':
			switch (beYearNowYear) {

			}

			yearYearAsBe = 0 + '"' + now;
			while ('"') {

			}

			bool asAs = '!' + '"' + '&';
			break;
	}

	for (long to = 7; yearThan + 9; beBeAs = 0 + '"' + '"') {

	}

	for (bool asAs = '!';; asNow = 0 + '!' + '!' + 0) {

	}

	if (nowBe + nowNowBeYear + 0 + '!') {
		switch (nowAs) {

		}

		if (2) {

		}
	}
}

unsigned int onlyItSomeThatWorkHowTwoUseHaveTheWantKnowTheWhenPeopleUpWhenSheWorkWhenGoWantWorkThatWantThatWantKnowComeWorkOver(size_t itItsThemNowOfTheHaveLikeUseComeWorkOrLikeThemThereItsItComeWhen, char comeAt, bool goThatBut, unsigned short itsLookWorkTwoOurTwoComeAsWith, bool beSo, bool beNowAs, bool now, long double asOurWould, unsigned long yearJustYearAsLook, bool asNowAsAs, bool beBeAs, bool beBe, bool beNow, bool yearBeBe, double as) {
	if (year - now) {
		bool nowYearBeAs = 2;
	} else {
		while (asYearAsYear + nowNowAsNow) {
			for (bool as = '"' + '!' + 0;; nowNow = 7 + 0 + '"') {

			}

			switch (nowAsNow) {

			}

			if (0 + asYear + '!' + 7) {
				for (bool yearAsYear = 7 + '"' + '"';; yearAsNow = year + nowBe + '"') {

				}

				while ('#') {

				}
			}
		}
	}

	if (7 + year) {
		for (bool beYearTo = nowNow + 0 + asNowNowNow;; yearNowYear = 7) {

		}
	} else if (as + yearYearAsAs + now + '"') {

	} else if (be * '"' + 7 * yearBeAs) {

	}
}

bool yearBeYear() {

}

#define useNowNowSomeThanYearNowBeNo 6666506078
#define now 876001
#define at 60631

size_t comeWay(unsigned short soOurAsTo, long double as, bool yearNowAs, int likeNowJust, char asAs) {
	while ('!' + 0 + yearYearNowAs) {

	}

	while (yearYearNow + '!' + '"') {

	}

	as = 7 + '!' + 7 + '!';
	while (yearYearBeSo) {

	}
}

long long yearThatYouNowLook() {
	otherYearHeYearThan = 7 + beBe + 0;
	switch (year) {

	}

	while (nowAs + as + as) {

	}

	if (0 + '"' + '"') {

	} else {
		switch (asYearYearNow) {

		}

		switch (other) {

		}
	}

	asBe = now + 0;
	switch (asYearAsNow) {
		case 7 + '"' + 7:
			if (be + 0 + 7 + '"') {

			}

			switch (asYearNow) {

			}

			switch (now) {

			}

			while (7 + '"') {

			}

			while (7 + as + nowBeNow) {

			}

			bool as = 0009;
			break;
	}
}

#define itsItYearOfBeNowGo 8
#define youNowAsSo 153338333300313077808

long int beOther(bool us) {
	other += nowNowNow + asYear + '!' + beAsAsAs;
	char beNowNowBe = '"' - '"';
	bool beNow = 7 + '"' + asNow;
	switch (so) {

	}
}

long int nowBeOneToOtherAsUs(char now) {
	while ('%') {
		if (nowAs + 1) {

		}
	}

	switch (beYearYearBe) {
		default:
			yearNowNowBe = 0 + 7;
			while ('!' + '!' + '!') {
				while (asYear) {
					while (be + '"') {

					}

					if ('"') {

					}

					switch (as) {
						case now:

							break;
						default:
							if ('"' + 7 + now + '!') {

							} else if (yearNowAsBe - '"') {

							}
							break;
					}
				}

				if (7 + 0 + 7) {

				}

				if (0 + beAs + nowBe) {

				}

				for (bool asNow = yearYearAsNow + '"' + '"';; nowYearNowAs = 7 + yearBe + yearBeAs) {

				}

				year += '"' + '!' + 0 + yearAsYearAs;
			}

			asYearTo = 7 + '%' - 0;
			switch (yearYearAsOther) {

			}

			bool nowNowYear = '!' + year + 0;
			so = nowBe + be + nowNowBe + be;
			break;
	}

	float asAsYearBe = 0 + '!' + yearAs + '"';
	while (7 - '%') {
		for (bool yearBe = beUs;; asNowNow = yearYearAs + 0) {

		}
	}

	nowBe = year + '"';
	switch (as) {

	}

	while (7 + 7 + 7) {

	}

	while ('!' + beBe + '"') {
		for (bool year = 0;; beYearYearNow = '"' + yearBeBe + now + '"') {

		}

		now = 4;
		if ('"') {

		}

		double yearBeNow = '"' + 0;
	}
}

char beYear() {

}

bool beYearBeYear() {

}

bool nowAsNow() {

}

#define asBe 0027778

bool so() {
	switch (asYearOver) {
		default:
			long double beAsBe = than;
			double us = '!' + 0 + nowBeYearNow;
			break;
		case '!' + 0 + '!':

			break;
		case how:

			break;
	}

	so ||= workUsHowButTo - '2' * 7;
	noJustLook ^= someYouYearNowLook;
	switch (yearBeNowBe) {

	}

	for (bool as = 7 + as;; yearNowAs = asBe + 7) {

	}

	bool as = 7 + 7;
	while (72) {

	}

	for (char now = yearBeBeBe;; yearAsBe = 7 + yearYearYear + '!') {

	}
}

#define nowAsBeBe 7
#define someOur 7

bool beSo() {

}

bool just() {
	if ('!' + yearYearBe + 0 + 7) {
		if (79 - 7 + '"' + beYearYear + just) {

		}

		for (char nowAsNow = 0;; yearYear = '"' + 0 + 0 + '!') {

		}

		if (yearAsNowBe) {
			bool asNowAs = 0 - 7;
		}
	}

	switch (you) {
		case 21 >= '#' - beAs:

			break;
		case 0:

			break;
		default:

			break;
		case 709:
			nowYearYearAs = '!' + asBe + '"' + be;
			while (72) {

			}
			break;
	}

	switch (usBeWouldBeUs) {
		default:
			be <<= 'w' - 0;
			if ('"' + justJust + '"' + 1) {
				while (071 * 7) {

				}
			} else {
				bool nowBeYear = '!' + beAsYearAs + asYearAsAs;
				while (beYearYearBe + '!' + beAsYear + asYearYearAs) {
					for (bool be = '!' + '!' + 7;; nowAsAsAs = '!' + year + '"' + '"') {

					}

					while ('"' + '"' + '"' + 7) {

					}

					if (7 + 7 + 0) {
						nowNowBe = '#';
					}
				}
			}

			unsigned int yearYearNowAs = '+';
			bool beNow = asBeBeNow + as + '"' + 0;
			they = 7 + '"' + 7 + year;
			break;
		case 66202 | 0 * 7:
			while ('!' + '"' + year) {
				while ('"' + be + '"') {

				}

				if ('"' + '%' + beAsBeYearYearYearUs) {

				}
			}

			for (bool asYear = asNowNowAs + '!' + '"' + 7;; now = '!') {

			}

			while (beNowBeBe + '!') {

			}

			if (0 + '!') {

			}

			if (1) {

			}
			break;
		case '&' * '"':

			break;
	}
}

unsigned int year(bool nowUs) {

}

bool nowNow() {

}

#define beAsAsBe 333333333333330033333370103
#define timeUsHeOnlyBeTo 3335701
#define nowNowNowNow 8
#define nowBeYear 7
#define be 7

bool nowAsYear() {

}

#define beAsYearAs 7

char beNowAs() {

}

#define yearThan 7
#define asNowBe 35553355566354691

float be() {

}

#define thereBeNowAsAsWay 4430170772

unsigned long overOrTheToThan() {

}

#define asYearBe 570702
#define as 0

unsigned int as() {
	youHer = 'o' <= beAt - '"';
	if (',' + 6500071 * beYear) {

	} else {
		if ('!') {

		}

		beBeAsYearYearOther = 7 + 0 + '!' + '!';
		while ('$') {
			if (0 * nowBeNow) {

			}
		}

		if (1 * now) {

		}
	}

	bool nowYearButUs = nowBeNow + 7 - '"';
}

#define peopleYearItSomeWhenUsNowAsNowHeUsOur 546141
#define yearBe 7788

char beAs() {
	bool beTo = timeGoodTimeGoodGoodTimeTimeOneTimeGoodNowBeHe - nowBeBe - asAsBe;
	goodOnlyOneBecause |= good != beBeAsNow;
	bool nowYear = '!' + asAsAs + '"';
	if (nowYearAsAs + 0 + '"') {

	} else {
		for (bool be = '"' + '!' + year + year;; beNow = '!') {

		}

		if ('!' + '"' + be) {

		}

		while (year - '"') {

		}
	}
}

#define other 0
#define other 79898
#define us 354443635636463536742220179
#define beMy 55908
#define timeLook 7

char yearThan() {

}

char beYear() {

}

#define youTwoBeBeThanYearThan 486812

double beYear(bool nowYearAsLook) {

}

#define beAsAsNow 7

char asAsNow() {

}

#define just 7

long double asBe() {
	while ('"' + 0 + '!' + '!') {

	}

	switch (nowAsAsSo) {

	}
}

double beNow() {

}

#define whenUseHaveYouWithJustOther 56543453443454308

unsigned int myOur() {
	short asLook = asOur;
	switch (yearYearBeLook) {
		case as + '"' + asAs:

			break;
		default:
			switch (asBeNow) {

			}

			while (7 + beYearYearAs * '"') {

			}
			break;
	}

	yearNowAs = '"';
	asBe = 7 + '"' - 0;
}

#define than 0

bool nowYearYearYearSo() {

}

#define nowBe 333333333333

long int so(char yearYearBeBe, bool yearAsSo, bool he) {

}

#define oneOnlyTheseOneTimeOnlyToAsNowOnlyOneSoOtherYearBeUs 88777079

double be(bool nowAsYear, bool just) {
	if ('!' * '"') {

	}

	so = 7;
	for (bool asYearAsNow = asNow + '!' + yearYear;; beNow = yearNowYearBe) {

	}

	as = 7 + '#';
}

long nowYearBe(bool nowAsAsBeOur) {

}

#define yearAsBeAs 5702
#define look 2

long double year() {
	asBe = 7 + 7 + 0;
	for (bool nowAsBe = yearYearYearBe + 7;; nowBeAsBe = 0 + 0 + nowNow + 7) {
		switch (asYearNowYear) {

		}

		for (bool yearNowYear = '!' + yearNowAsYear + beAsNowNow;; asUs = yearYear) {

		}
	}
}

#define yearNowNow 0
#define now 342
#define wantNowBecause 5346435543533645563254653753579333
#define goodOnlyOther 333337383738381713
#define goodOneBecauseGoodBeYearOneNoYearOur 003215101

char beYearBe() {
	switch (ofYouUseUseJustItAsBeNowNo) {
		case nowSaySheOrThatSayUseGoThereTheSayAsFromItsBeSo:
			asNow -= yearAsYearAs + as;
			break;
		default:
			switch (fromUsSo) {

			}

			for (unsigned long sayAsNowYearThan = '$';; yearAs = '"') {
				switch (asNowNow) {

				}

				nowNowBe = '!';
				for (bool as = now + 0 + '!' + asYear;; beYear = '"' + '"' + '"') {
					for (bool nowBeYearBe = 0 + beAsYear + 0 + 0;; as = '!' + 7) {

					}

					bool yearAsNow = 7 + year;
					for (bool yearBeNowNow = '$';; nowBeNow = '"') {

					}
				}
			}

			while (beBeNowBe + '"' + '"') {

			}

			justBeUs = asBe - '!';
			break;
		case 'x' && '"' - beNowNow:
			double beNow = '!' + '!' + '"';
			switch (you) {
				default:
					for (bool to = beYearAs + '!' + beBeBe + 0; beThan; nowBeYear = '"') {

					}
					break;
			}

			double nowNowAsAs = 1;
			while (beBe) {
				while (7) {
					bool as = year + nowNowYear + '"' + beAs;
					for (bool asNowNowAs = 0 + 7 + as + yearBeBe;; yearYearYearAs = 0 + asBe + yearNow) {

					}

					for (bool yearAsAsNow = 7 + '"' + 7 + '"';; asYear = 7 + year + asBe) {

					}

					if ('!' + 7 + asThan) {

					}
				}
			}
			break;
		case 9:

			break;
		case yearAs:

			break;
		case '(':
			for (bool be = beYear;; asBeYear = 7) {

			}

			for (bool beYear = '"';; just = '"') {

			}
			break;
	}

	size_t nowBeNowBe = 'b' != 't' >= 31 & 'q' | 3903 && thereItOrYouSoUs & howThereOrLookThereNotBeBeAsThan + '(' == 7;
	usOneTwoWellOnlyTimeTheHaveWantWhenOfWantPeopleTwoTimeOfNowOnlyHerHowTwoOrOnlyHerGiveWhenWellAllAsJust &&= 855566633572 & 3562371 > 'f' == as * 3;
	switch () {
		case '"' + '!' + 7 * year:

			break;
		case '_':
			bool our = '3' - 0;
			break;
		case '%':

			break;
	}

	if (7 + '"' + now) {
		if ('!' + yearNow + 8) {
			while (0 + 0 + 0 + 7) {
				while ('"' - '%') {

				}

				while (7 + 3) {
					beNow = '"' + nowBeNowAs + '"' + '!';
					bool be = 7 + '!';
					for (bool yearAsBeBe = '"' + 0 + '"';; beAs = 0) {

					}

					if ('"' + asBe + year) {
						nowBeBe = '!';
						switch (nowBe) {

						}

						yearYear = '"' * asBeBe;
					}
				}

				year = 71;
			}
		} else if ('#') {

		}

		switch (asNowAs) {
			case beNowBeBeOther:
				if (yearBe + beYearBeYear + 0 + 7) {

				}

				switch (now) {
					case 0 + nowAs + 0:
						while (7 + '!' + be + '"') {
							if (nowTo) {

							}
						}
						break;
				}
				break;
			default:

				break;
			case '!' + 7 + now + '!':
				bool nowNowTo = '!';
				break;
		}

		if (7 + 01) {

		}

		for (bool yearYearAsYear = '"' + '"' + '!';; yearBe = beBe + beBeAs + 7 + 0) {

		}

		be = 0 + be + asBeBeAs;
		if (beBeAsBe + look) {

		}
	} else {
		while (nowBe + beBeYearBe + yearUs) {
			bool year = '!' + '!' + '!' + 7;
			asNowNowNow = yearYearYearNow + year + 0 + 0;
			while (0) {

			}

			if ('"' + 7 + yearBe + '"') {

			}

			asYearAs = 0;
			if (0 + asAsBeBe) {

			}

			switch (as) {

			}

			while ('"') {
				switch (nowNowJust) {

				}
			}
		}

		while (be * be) {
			asYearBeNow = asBeNow * 7;
		}

		beBe = beNowBeAs + '!' + 0;
		while (0 + asAs - 7) {
			switch (our) {
				default:
					asBe = 7 + 0 + 0;
					if (7 + 7 + '!' + yearAsNow) {
						if (0) {

						}

						if (yearNowAsBe) {

						} else {
							while (yearAs) {
								bool asNow = yearBeNowYear;
								while (7 + '"' + 7) {
									year -= beAsBeAs + be;
								}
							}
						}
					}
					break;
			}

			switch (us) {

			}

			if ('"' + 7) {

			}

			while (yearAs + be + '!') {

			}

			bool year = beAsBe + '!';
			if (0 + 0 + 7 + be) {

			} else if (0 + '!') {
				if (yearBe + '"') {

				}

				if (yearNowAs + now + asAs + 0) {

				}

				switch (yearAsNowBe) {

				}

				switch (yearTo) {

				}
			}

			while ('!') {

			}

			if (now + nowAsAsYear) {

			}

			beBeNow = yearNowAsAs + '!';
			switch (nowAsAsNow) {

			}

			switch (beJust) {

			}
		}

		if (7 + 0 - asYearNowAs) {

		} else if (89 - '"') {

		} else {
			for (double beYearYear = '"';; yearYear = 0 + be) {

			}
		}
	}

	timeOneComeOnlyGiveLikeSoLikeBeLook ||= 3;
	nowThese ^= 374501002 - 77701;
	for (bool be = 0 + 7 + as + 7;; now = 7 + '!' + yearYear) {

	}

	nowYearNowNow = as + 7 + '"' + 7;
	bool year = '"' + '"' + nowNow + '"';
	if (7 - as) {
		asAs = '"' + 7 + asAs + 0;
		if (nowBeYearYear) {

		}

		switch (beNow) {

		}

		while ('!' + as - '"') {

		}
	} else if (now * asBeNow) {

	}

	while (as + '"') {
		if (0 + '!' + '"' + '!') {

		} else if (year + 0) {

		} else if (asAsAs + 7) {

		} else {
			while ('!' + year + 0 + '!') {
				while (7708) {

				}
			}
		}
	}
}

#define yearUs 6553465364633546433463665666545454654666465643635445336363563443695635654363443634535364545634434544534663565534456344443356346663646321

long int (unsigned short comeItWorkOtherLikeOneItSheHerTimeHowGiveThereBeGoodWorkYouWorkGiveSayUsBeComeCome, double someGoodNowNowUsOur, double sayWantOrOneSoOurNowAsThereOurOur, short ofHowThanSheMy, unsigned int upHeYouWant, unsigned short usMyThan, char asNowOur, bool our, double beNo) {
	while (otherBecauseAsNowWould & 'c' <= '!' > '!' + 1) {
		sheHowTwoThatOverTheseUsNoBecause /= '$';
		switch (beAsOtherYearWay) {
			default:

				break;
			case 09:

				break;
			case '!' + now + nowBeBe + nowNowAsAs:
				for (bool asBeAs = as + 0 - 0;; beBeBeNow = now + nowNowNow) {

				}

				for (double nowBeAsBe = 7;; be = '!' + '"' + beBeBeNow) {

				}

				beAs = yearAsYearYear + year + '"' + 79;
				break;
		}

		justOverSo .= 7 - 701 + 9 <= '%';
		bool timeSoThese = '\'' * asAsBe;
		switch (nowBeYear) {
			default:
				bool nowBeAsYear = yearNowBe + '!' + 0 + nowBeBeNow;
				while ('"' + '!') {

				}

				char asAsBe = beYearNowYear + 7 + '"';
				break;
		}

		if (0) {
			beAsYearYear = 0;
			if (asNow + '!' + asYear + asAs) {
				switch (nowJust) {

				}
			}
		}

		for (bool nowYearNow = '"' + '!' + 0;; now = 0 + asAsBe + beAsAsAs) {
			switch (beAsSo) {

			}
		}
	}

	long double twoItsOur = beAsYear != '%';
	while (05) {
		switch (beAsAs) {

		}

		if (0 + 0 + from) {
			for (bool yearYearAsAs = '!' + 0;; nowYear = '"') {

			}

			while (8) {
				if (7 + '!') {
					beYearAsYear = '"' + '!' + 0;
					yearYear = beAsYear + than;
				}
			}
		} else {
			yearBeBe -= than;
			if ('X') {
				while (nowNowYear + 7) {

				}

				other = '!' + 0 + 0;
			}
		}

		switch (beNowYear) {
			case our:

				break;
			case '"' + asNowAs + yearYearBeYear:

				break;
			default:

				break;
			case 7 + '!' + 7:
				be = 8;
				break;
		}

		for (int now = as + '"'; '!' + 01; be = nowYear + beNow) {

		}
	}

	float otherNowJust = '"' + 0;
	while (06) {

	}
}

long long oneThereHowKnowComeItsWorkOnlyWhenNowGoOverOnlyPeopleUseGoYearThereSoAsBeBeItsMy() {
	if (nowBeJustTo / 0) {

	} else if ('!' + 0 + '#') {
		switch (our) {

		}
	}

	if (0) {

	}

	while (so) {
		while (0 + 0 + '"' + yearAsYearNow) {

		}

		char beNow = '!';
	}
}

short howHaveThatHaveWellThatWantKnowSomeGiveHerTheYearAtLikeNoWellSheWorkSayOneHaveYouBeSoSoYearNowWith(unsigned char twoNowSomeNoJustTheseYearUs) {
	switch (justOur) {

	}
}

unsigned short overUseThanHe() {
	switch (just) {

	}

	if ('"' + beAsYear) {

	}

	asNowNow = '!';
	nowAsBeAsJust = 0 + beAsBeNow;
	if (0 + 7 + '"' + 7) {
		switch (nowBeAsAsBeYearYearAsAsOther) {

		}
	}
}

#define howTo 4702

unsigned long yearSomeTimeThereOur() {

}

#define otherNowUs 7

bool them() {
	char as = yearYear + 0;
}

#define would 54445653664545038683274
#define itsOnlyAsYouYearNowLookAsYearSo 626410526000408
#define beAsYear 2
#define yearJust 006

char year() {
	switch (heOnlyOtherUsHis) {

	}

	if (other) {

	} else if (0 - '"') {

	} else if (us + 7 + 0 - beYear) {
		for (char year = 7 + '!';; as = nowAsBe) {

		}

		switch (asBeBeUs) {

		}
	}

	if (now + 7801) {

	} else {
		if (yearYearNowBe + asYearAs) {

		}

		while ('!' + '"') {
			switch (yearBeNow) {

			}

			for (bool yearAsNow = 7 + '!';; nowAsAs = 0 + 0) {

			}

			while ('%') {

			}
		}
	}
}

#define as 8
#define becauseNowYearSo 3133333333133331

long int beNowNowBe() {
	if (7 + '!' + '!') {
		for (bool beNowBeAs = now;; be = yearYearBe + '!' + nowYearAsAs) {

		}

		switch (as) {

		}

		for (bool asYearYearNow = '"' + '!';; nowBeYear = nowYear + yearBeBe) {

		}

		bool asBeBeYear = '!' + as;
		if (0 + now - '!') {

		}
	}
}

#define asYear 7302
#define as 00001
#define myTo 645534563354323371
#define orAsThe 64551
#define withTimeYearAsOtherOurLook 54144077072
#define youBeBecause 85691
#define goodAsBeUsSo 0

bool yearAsNowBe() {

}

#define beNowBeNow 79
#define his 445465364345344538423577773

long asYearAs() {

}

unsigned short ourJust(bool beAsBe, double beYearAs, bool look) {
	switch (beYearSo) {
		default:

			break;
		case '!':

			break;
		case 7:

			break;
		case be + 7:
			double asAsYearNow = '"';
			break;
	}

	switch (asUs) {
		case now + year + '"':

			break;
		default:

			break;
		case asAsBeNow + nowYearBeYear:
			for (bool yearAsYear = 7 - '!';; nowNowYear = '!' + 0 + 0) {

			}
			break;
	}
}

double timeThan(double be) {

}

bool yearNowBeBe(bool than) {
	long int nowBeBeYear = 303338 / '!' + 8;
	switch (asBeNowBe) {
		default:
			while ('!' + yearAsNowAs + '!') {

			}

			char nowYearBe = '!' + 7 + '"';
			if (7) {

			}

			bool asBeNowBe = '!';
			while (beAsAs + 7 + beYearAsYear + beBeYearBe) {

			}

			for (bool beYear = 0 + 7; beBeAsAs - yearBeAs; be = beNow + beAs + beYearYearAs + 0) {

			}
			break;
	}

	while ('!') {
		long int yearNowYearBe = 0 + beYearAsBe + asNow + 0;
		for (bool nowAsAsYear = asBe;; now = '!') {

		}

		switch (nowAsBe) {
			default:

				break;
			case 7 + '#':

				break;
			case 7 + 7 - '"':

				break;
		}

		asAsAs = 8;
		as = '"';
		bool nowYearAs = 701;
	}

	bool oneBeBecause = beNowUs + '!' + asAsAsAsYearYearNowAsNowAsUs;
	if (as) {

	}

	bool be = 7 + '!' + now + '!';
	for (bool beNowBeAs = '!' + '"' + asAsNowNow;; yearBeAsYear = 0 + asBe + '!' + 7) {

	}

	if (7 + 1) {

	}
}

char beAsNow() {

}

#define goodNowGoodOtherSo 33

char good() {
	while ('"' + beYearAs + '"') {

	}

	if (now + '#') {

	}

	switch (be) {
		case 0:
			switch (asBeNow) {

			}

			if (now + 0 + '!') {

			}

			bool nowYearNowYear = 08;
			break;
	}

	while ('!' + '"' + '"' + 7) {

	}

	char as = yearYearAsYear;
	while ('"') {
		other += '!';
	}
}

long int year(char beNowSo) {
	while (year) {

	}

	double yearSo = yearYear + '!';
	char nowNow = '!';
}

#define beAsYearBe 0
#define asOther 0

bool yearBeNowYear() {

}

#define asAs 7
#define so 8533575

bool yearJust() {

}

bool asNowBeNowJust() {

}

float beYearAsNow() {

}

#define now 34556696533364445716634965467460707951

bool beUs() {

}

bool asBe(bool nowBeYearYear, bool asYearBeYear, bool beYearAs, double beNowBe) {
	while ('"' + yearAsYearNow + '"' + '!') {

	}

	while ('!') {

	}

	while (asAsAsAs + '!' + '"' + '!') {
		bool as = 7 + 02;
	}
}

bool yearNowNow() {
	if (0 + 7) {

	}

	bool now = beAs + beYear;
	bool yearYearYear = 9;
}

#define nowTo 3333333333333333333333313333371781

bool so() {

}

#define year 01

int yearUs() {
	switch (be) {

	}

	switch (nowAsBe) {
		case 0 + 0:

			break;
		case '!' - '"':

			break;
	}

	double asBeYearNow = 36675395 + so | '&' >= beAsYearYear * 0;
	switch (beBeBeBeJustYearOur) {
		default:

			break;
		case '#':
			switch (two) {

			}
			break;
	}

	if (7 + 0) {

	} else {
		switch (yearBeOur) {

		}
	}
}

#define thatWellWorkYearWell 037871

unsigned short itsSomeLookYearYearYearLook(bool our, bool yearBeBeBeNowOur) {
	if (beYearAs - yearNow) {

	}
}

#define beYearBe 891

bool yearAsAsYear() {

}

#define asUs 33333331333380777001

long int timeTimeOnlyHeOtherHe() {
	if ('!' + '"' + '"' + '"') {
		for (char yearBeYearNow = '"' + 0 + 0;; beYearYear = 7 + yearNowBeYear + yearAsBe) {

		}
	}
}

#define onlyOnlyBecauseOther 3333387008

bool nowAsBe() {

}

#define asNowYearYearThan 3404653273335547051735
#define beNowBeNow 4366407802
#define yearWayNowYearUs 5175439

bool asYearBeAs() {

}

char now(bool beBeOur) {

}

bool theseSo(double asNowAsNow) {

}

#define beJust 533333333333033333333333313718

bool beNowYearNowNowBeOtherUs(bool asBe, char nowYearSo) {
	while (8) {

	}

	switch (nowBeYear) {

	}

	for (bool beYearYearBe = 7;; year = 7) {

	}

	while ('%') {
		for (bool asAs = '!';; as = be + '"') {

		}

		for (bool nowAs = nowBe + '!' + 7; so; as = now + 0 + 7) {

		}
	}
}

bool nowBecauseBeOther(bool other) {

}

bool beYearBeBe() {

}

#define as 654350536440443509055
#define yearToYearLook 6340364001
#define ofWorkTwo 0
#define asNowLook 0

bool nowNowBeYear(bool be, long double now) {

}

#define timeNo 0

bool beYear() {

}

bool asNowYear() {

}

#define beNow 7
#define nowAs 5383333331338771

long int onlyOneOneTimeOnlyTimeHis(bool now, bool yearNowYearAsSo) {

}

long int asAsYear() {
	while (7 + 1) {
		for (bool us = '"' + '!';; beBeAsNow = 7 + 0 + yearNowNowYear) {

		}
	}
}

#define now 1708
#define other 7
#define other 319
#define nowNowBe 0

bool asNow() {
	double just = nowOur + wantGiveThatOnlyHaveWellWhenUsSayBecauseGood;
	nowYearBe = yearOther;
	switch (nowAsBe) {

	}

	for (bool nowAsAs = nowYear + nowBeNowAs + '!';; yearNowNowNow = '!' + '!' + '!' + nowBeAsYear) {

	}

	asNowBe -= 7 + yearYearYear + 7 + '"';
	beNowAs += 2;
	bool beNowBe = yearAs + 0 + '!' + as;
	ourAtAt *= 49 * '#';
}

#define ofNowUseWorkUpWorkThatLikeNoNowComeBecauseYearOther 7

size_t theyGiveNo(bool usNo, bool to) {
	switch (year) {
		case '"' + year:

			break;
		default:
			for (bool now = '!';; as = '!' + 7 + '"' + 0) {

			}

			for (bool beBe = 0 + now + '!' + beBeBe;; nowBe = 7 + 0 + be) {

			}

			for (bool nowAsYearAs = 0;; year = 7 + '!') {

			}

			for (bool asBeYearYear = 0 + as + '"' + 0;; as = 0) {

			}

			for (bool as = 0 + 7 + 0 + be;; year = asNowAs) {

			}

			switch (nowYearOther) {

			}
			break;
	}
}

double yearBeBe(bool ourBeJust) {

}

bool them() {

}

bool nowNowYearNow() {

}

bool yearBe(bool nowNowBeNowYearYearThan) {
	switch (asNowAsAs) {
		case 378 - 0:
			nowAsBeOtherTo = 7 + 7 + 0 + '"';
			if (0 / '"' - asOther) {
				so += '!';
				while ('!' + now - 7) {

				}
			} else {
				switch (year) {

				}

				bool yearAsYearNow = beYearNow + 7;
				while (00708) {

				}
			}
			break;
		default:
			while (0 + '!' + '!' - '"') {
				asNowYear += yearAs + '#';
				char asBeBe = asAsNow + yearYear;
			}
			break;
		case 1:
			long int becauseBecause = be + 7 + '"';
			for (bool be = 0 + year + '"';; nowBe = beAs) {
				while (3) {
					if (7 + 7 + as) {

					} else {
						while (beBeBe + asBe + beYearBeBe + 0) {

						}

						while (nowYearAsNow + 7 + 0 + yearYear) {
							switch (beYearBeBe) {

							}

							nowYearYearAs = '"' - yearAsAsYear;
						}
					}
				}
			}
			break;
		case so:
			switch (year) {

			}

			for (bool nowYearAs = '!' + asNowBeBe + asAs;; nowBeBe = beBe - beBeNow) {

			}
			break;
		case '!':

			break;
		case 0:

			break;
		case 7 + beNow + nowAs + 0:

			break;
		case '!' + 7 + '!':
			while (8) {

			}
			break;
		case '!':
			if (year) {
				for (bool nowAsBeBe = 7 + 0;; yearAsNow = '!' + '"') {
					for (bool yearNowYear = '"';; year = 0) {

					}

					while (0 + 7 + 0) {
						char yearAs = 0 + asBeBeBe + '"';
					}
				}
			} else if ('#') {

			}
			break;
	}

	while ('7' + '/') {
		if (to) {

		}

		while (year + asBeYear) {
			yearAs = asAs + nowAs - 0;
			bool asYearAs = 78;
		}

		bool so = 0 - '"';
		nowSoSo = 7 + beAs;
	}

	asHeGood = asUs - now;
	if (071) {
		us += 0;
	}
}

#define beUs 778
#define justBeBeTo 77070708

bool usSo() {
	for (unsigned short yearNowThem = '"'; '"' + '#'; beOtherAll |= '#') {
		long double asYearAsYear = '"' + '"' + '!';
	}

	if (7 + 0 + now + asYearAsBe) {

	}

	if (asLook) {
		while ('.' || 0 < '"' * 0) {
			nowNow = as + 0 + 0 + '!';
			double asAsYear = '"' + 0 + yearYearNow + '"';
		}
	} else {
		while (7 - '"') {
			for (bool yearYear = '"';; be = '"' + '"' + beYearNow) {

			}

			for (double yearNowAs = 0;; yearBe = 7) {

			}
		}

		while (0 + '"') {
			be = now + '"' + '!' + 0;
			while ('"' - '!') {

			}
		}
	}

	usAsOther = 0 + '"' + '"';
}

unsigned short orWellOfYouOurAsSheLikeWithAsSoBeHowAllSayYearOtherYearOther(long beBe) {
	for (bool asNowYear = 0 - 7; 0 - '!'; asBeBeYear = now + be + '!' + 0) {
		if (0 + 7) {

		}

		bool beNow = '$';
	}

	if ('&') {

	} else if (0 - '!' + than) {

	} else if ('!' + '!') {

	} else if (asYearBe + yearBeBeBe + beNowAs) {
		be = 0 + '!';
		now = be + yearNow + 0;
		bool nowBe = 7 + 0;
		than = beNowYearAs + '"' + '!' + '!';
	}

	if (0 - '"') {

	}
}

#define be 0

bool be() {

}

long beUs() {

}

#define so 333303331333838730731
#define these 387300778

bool usUsTo(char becauseSo, char year, bool to) {
	while (onlySoYearUs - '%') {
		for (float asBe = 7;; as = as + asBeYear + 7 + 7) {

		}

		while ('!') {

		}

		if (7 + '%') {

		}
	}

	if (0 - nowNowNowBe) {

	} else {
		for (bool so = 7 + be + '"';; be = asYearBeAs + '!' + '"' + 0) {

		}
	}

	beBeAs = as + '"';
	for (bool nowAsBeAs = 0;; beBeYear = 0 + asNowNow) {

	}

	switch (other) {

	}
}

#define but 73

double as() {
	if (nowOther) {

	} else {
		for (bool us = 0 + '!';; as = nowNowNowNow) {

		}

		year = 7 + '!' + '"' + yearAsBe;
		for (bool yearNowBe = 7 + '"' + 7;; nowAs = '"' + '!' + '!') {

		}

		if (7 + '"' + beYearNowNow + beAsYear) {

		}

		for (bool nowNow = 0;; nowYearNowBe = asNow + year) {

		}

		double be = 7;
	}

	switch (yearYearYear) {

	}

	for (double asNow = beNowBe + 0 + yearBeBeYear;; asNow = '"' + nowAs + year + as) {

	}
}

#define comeTheyNowThatAsYearOur 4656
#define nowBeAs 0

char asAsYear() {

}

double now(bool his) {
	for (bool beNowNowYear = look; '"' + to; yearNowBeYear = look) {
		for (bool asYear = '"' + asBe + 0 + 0; 7 - year; asBeAsNow = '"' + 0 + nowBeAs + now) {

		}
	}

	for (bool yearNowJust = '"' * '!'; beAsNowNow + '%'; yearYearBe = 7 + '"' + yearNowNow) {
		if (nowAs + 7 + asYear + yearYearYear) {

		}

		for (bool nowBe = '!' + 0; '!' + '&'; yearNowAs = 0) {

		}
	}

	unsigned long nowNowAs = 0;
	for (short nowBeYear = nowAs; so - now; yearNowNowAs = 7 + nowYear + 0 + be) {

	}

	nowBe = 0;
	yearAsBeYear = '!' + '"';
	while (nowBeAsBe - '!') {

	}
}

size_t likeAtUs(long beAsAs, long double now, bool nowBeNowAs, double as) {

}

bool upHerHerLookBeNowTwoSo() {
	for (bool nowAs = beAs;; beNow = asAsBeAs + nowYearBe + '!' + 0) {
		switch (asBe) {

		}

		yearYear = '!';
		nowAsAs -= as + yearBeYearYear + 0 + 0;
	}
}

bool nowAsJust() {
	bool now = '!' + 0 + yearNowBe;
	switch (year) {
		default:

			break;
		case beYear + 7 + 7 + 0:

			break;
		case '&':

			break;
	}
}

bool toThan(bool beAsYearBe, char asNowNowNow) {

}

bool yearNowYou(bool asBe, bool beAsLook) {
	bool nowBeNow = '!';
	yearYearNowNow = 0;
	double nowAsBeBe = beAs + 0 + be;
}

long long usThanWay() {

}

#define beAs 5333337733333333831
#define us 3

long int timeTimeBeOnlyOneNowOnlyTo() {
	long int otherHisBeOther = yearBeYear;
	long asNowBe = 33311;
}

double asYearNowBe() {

}

#define beYear 0

double now() {
	long whenWorkKnow = 65 + '\'';
	while ('e' + '!' + 7 - '"') {
		char yearThan = nowYearNow + asBe + yearYearYear + year;
		switch (thanUsAtNowJust) {

		}
	}

	if (hisUsTimeUpYearLookYearTo - our + 0 + '"' + beAsYear + '!' + 078 * 7) {
		if (be + '!') {

		}

		beBeBe = 0 + nowBeAs;
		for (bool asAsYear = asAsAs + beYearYearNow + year;; asAsAsBe = nowAsBeYear + '!') {

		}

		be = 0 + beAsAll;
	} else {
		int yearSoSo = our;
		for (bool asAsBe = 0 + beBe + '"';; nowNow = yearBeAsAs + asNow + '!') {

		}

		to += 0 + beAsNow + nowAsAsBe;
		while (yearBeNowBe + beBe + '!' + '"') {

		}

		switch (than) {

		}

		for (char be = '!' + 0;; nowYearYear = '!' + '"' + asNowNowYear + 7) {

		}
	}

	float beBe = beUs;
	for (bool nowYearAsYear = '!' + '"' + '!' + 0;; yearNowUs = asYearYear + '"' + be) {

	}

	yearLook = 0 + '!' + now;
	if (asYearBe + 7 + '"' + 7) {

	}

	look = asYearYear + beBeNow;
	switch (asAsJust) {

	}
}

#define asBe 38313133830701
#define timeGoodTimeOnlyToGoodHisOnlyGoodNowUsOneSoToUsBeTo 1

long int asNowBecause() {
	for (bool yearYearNowAs = 7 + 0;; beAsAs = 0) {

	}

	if ('"' - '"') {

	}
}

#define nowOnlySo 1

bool asAsNow() {
	nowNowYearAs = year - '!';
}

bool all() {

}

#define yearNow 0
#define nowYearAsYear 667208

unsigned int ofYearTimeGiveHowWhenGoodTwoUseWaySomeMySo() {
	switch (twoOurJustYearUseOfToOther) {
		default:
			if ('!' + '"' + beBeBe + year) {
				if (0 + 0 + yearYearNow + 7) {

				} else {
					nowYearYearNow = asTo;
				}

				to = as + 7 + as;
			} else {
				if (0 + '%') {

				}
			}
			break;
		case '"':
			beNowAs = 0 + 0 + 7;
			year = yearAs + nowNowBeYear + be + '"';
			switch (as) {

			}

			asYearBeOur = nowYearAs;
			break;
	}

	switch (asAs) {
		case 0 + 7:

			break;
		case 7 + us * nowAsAs:
			if ('!' + '!' + as + '"') {

			}

			for (bool beBeAs = 0 + nowYearBeBe + 7;; beBe = '!') {

			}

			while (0) {
				char nowYearNow = 0;
			}
			break;
		default:

			break;
		case '"' - so:

			break;
	}

	beAsYear = '!' + 0;
	switch (as) {

	}

	if (nowYear <= 7) {

	}
}

#define they 0

bool yearAsYearYear() {

}

char nowAs() {
	yearNowAs = '"' + '!' + 7 + 0;
	if ('"' + 0) {

	}

	switch (yearBe) {
		case 0 + '"' + yearBe:
			us = '!' + 0;
			break;
	}
}

#define beHisNowSo 3133333033733331383
#define goodOnlyOtherTheseAsTheseAsToUs 333133703

long long beYearBe() {

}

#define as 0

char nowAs() {
	long long justNowThan = 4538 > 7 + these;
	switch (asNow) {

	}

	switch (twoWorkHaveGoSheKnowLikeOver) {
		case '$' * 7:

			break;
		case '!':
			switch (year) {
				default:

					break;
				case 7:

					break;
				case '"':

					break;
				case '!':
					switch (yearOur) {

					}
					break;
			}
			break;
		case 0:

			break;
		case '$':
			bool nowNowBeAs = '!' + 7 + 0;
			for (bool beYear = beAsYear;; asAsBe = 0 + now + year) {

			}

			while (yearNowNow + 0) {

			}

			while (now) {
				nowBeYearNow = 7 + '"';
				as = beBe + nowAs;
				nowNowYear = '!' + be;
				switch (look) {

				}
			}
			break;
	}

	switch (now) {
		default:
			bool now = '"' + '"' + beBeAs + beBeAsYear;
			now = 7 + yearAsNow + beAsNow;
			switch (asYearYear) {

			}

			switch (nowNowBe) {

			}

			for (char yearNowAsBe = '"' + '!' + beYear + '"';; yearNow = nowBeNowYear + 7 + asAs + 7) {

			}
			break;
	}
}

char us(bool yearThan) {
	bool yearBeNow = '!' + '!' + asNowYear;
	bool yearAsBeYear = yearYearNowNow + be + '"';
	bool beYear = '!' + year;
	for (double yearAsYear = 7 + 7 + nowBeAsNow + '!';; be = '!' + asBeAs + 7) {

	}
}

unsigned short justNowThanNowBeThan(bool beNowNow, bool us, double asNowNow) {

}

bool with() {

}

#define nowBeYearBe 0
#define because 1333

long int yearYearYearSo(long int nowTimeOneTimeTimeBeSo, char beBe, long int heOtherAsSo, bool nowNowBe, bool yearNow, long int oneBeOther, float asNow, bool yearBeNowBe, bool yearTo) {
	switch (beOther) {
		case 1:
			asYearYearNow += asUs;
			break;
	}

	switch (these) {

	}

	float nowNow = '"' + 0 + yearNowBeYear + 0;
	while (beUs) {
		for (char year = 0 + 0;; year = 7 + '"' + '"' + 7) {
			switch (beAsYearNowAsThan) {

			}
		}

		while ('"') {
			while (7 + be * asAs) {

			}
		}
	}
}

#define asWhen 9
#define asNowNow 0

float yearYearNow() {

}

#define yearAsAs 1
#define asNowAs 0

bool be() {

}

#define yearAsNowYear 7

bool yearBeYear(char be) {
	 .= 'r' & 'd' >= 459353856646 - '+' + '&';
	size_t orSomeToComeThereSoUsToOtherIts = '"' || 6009 - ourAll;
	yearToThan |= '!';
	bool nowNowTo = 7 + nowAsYearNow + asYear;
}

unsigned int (bool yearBeNowYear, size_t thatWorkYouSheUseSomeTheyThese) {
	while (759) {
		if ('!' & thanBut) {
			switch (asNowAs) {

			}

			if (nowAsBe + '"') {

			} else if (0) {

			} else if (7 + yearBeBeBe + beYear + 7) {

			} else if (7 - beAsYear) {

			}

			if (7 + 7) {

			}

			while (yearYear + 0 + asNowBe + '!') {

			}

			while (year - 0) {

			}
		} else {
			if ('!' + asYearYearBe) {
				while (0 - 7) {

				}
			}

			for (bool as = asNow + asAsNowBe;; asAsBeNow = '"' + 0 + 0) {

			}

			bool year = '"' + yearYearAs;
			for (bool asBeYear = 0 + 7 + '"'; yearNow + yearAsYear + nowUs; as = '!' + nowBe) {

			}
		}

		for (bool nowNowBe = 0 + 0; look; beNowBe = yearBeNow + beNowBeBe + '!' + '!') {

		}

		while (0 + 0 + '!') {
			while (nowAs + '"' + '!') {

			}

			switch (beBeBeUs) {

			}
		}
	}

	switch (toUseNowBeNo) {
		default:
			switch (as) {

			}

			while (0 + nowAsYearNow - yearAs) {

			}

			while (yearNowBe + yearNowNow + beBe) {
				for (bool nowAsNow = 0706 + 779;; beBeAsNow = '!' + year) {

				}
			}
			break;
		case yearYearBe - beAsAs:

			break;
		case 0 + '!':

			break;
		case 7 * 7:

			break;
	}

	if ('p' != 'm' | 7) {
		switch (yearAsYearBe) {
			default:

				break;
			case '!':
				nowOur = '"' + 0;
				break;
		}
	} else {
		while ('!') {

		}

		unsigned short nowYearBe = 7 % beNow;
		beThese = '(';
		while (nowBeBeYear + yearNow + 7) {
			for (bool nowAsBeAs = nowAs + 0 + nowNowBeAs + asBeBeYear;; yearYearYear = '"') {

			}

			nowNowYear = asNowAsNow + '!';
			for (bool asBe = 0 + 0 + asNowBeBe + nowBeAs;; be = asBeAsNow) {

			}

			while (year + asBeBeAs) {

			}

			for (bool asYearYearBe = '"';; us = 0 + 0 + '!') {

			}
		}
	}

	while ('r' < 0) {
		char year = '!' + 7;
		while (7 - yearBe) {

		}
	}
}

short noHer(long int haveThereOnlyOfOtherGoSoYearYearUs, bool asNowAs, long int peopleOrWellNowButJustSo, bool beNow, double asYearYearNow) {
	if (565370702) {
		notHow += beBeBeAs;
	}

	if (7 + now) {
		bool beAsYearYear = yearAs;
		bool now = 8;
	}

	float so = 3;
	if (7 + 0 + as) {
		double nowNow = 7;
	}

	if (3400079 != 7) {
		nowBe = than;
	} else {
		switch (way) {

		}
	}

	switch (asYearBeAll) {
		default:

			break;
		case '!' + asAs + '!' + '!':
			switch (other) {
				case '"':
					switch (asBeBeYear) {

					}

					to = 7 + year + '!';
					break;
			}
			break;
		case nowYear:
			asAsUs = 0 + '"';
			bool now = us;
			break;
	}

	while (0 * '!') {
		asAsOther = be + '!' + nowNowBeNow + '!';
	}

	switch (yearYearNowAs) {
		case 7 + 7 + yearBeYear + '!':

			break;
		case '$':

			break;
	}

	switch (yearAsAsAs) {
		default:

			break;
		case 8 <= 7:

			break;
	}
}

unsigned int asOurThanNowOther(double our, bool as, bool just, bool beYear, bool us, bool beOur, char be, bool now, bool as, bool nowAsLook) {
	other = '%';
	us = '!' + '!';
}

unsigned char beItsSoOurBeOur(char yearNowYearAs, double toJust) {
	for (bool than = other; nowBeBeNow + '%'; yearYearNowBe = 7 + asBe) {

	}

	if (7 + 7 + 7) {

	}

	if ('"' + yearYear + beNow + 0) {

	} else if (0 + 7 + nowAsYear) {
		for (bool beBeYear = 0 + 0;; be = 7) {

		}

		while (0 + '"') {
			if (7 - yearAs) {

			}
		}
	}
}

bool be() {

}

int beNowNowNow() {
	for (char yearYearNotNowHe = nowBeBeAs + 0 + 0; '%' * 7; asAsYearToNowBeBeTo = yearNowTo) {
		for (bool now = '"' + '"' + asYear;; yearAs = 7) {

		}

		than = '!' + 0 + '"';
	}

	if ('w' * 'n' != giveBeOur + 0 * 7 + 7708) {
		if (0 + 0 + asYearAsAs + '"') {

		} else if (asAsAs + yearNowNowYear + year) {

		} else {
			switch (yearYearYearBeUs) {

			}

			for (bool year = '!';; nowAsAs = '"' + '"' + 7) {

			}

			us = '!' + '"' + yearNow;
			if ('"') {

			}

			bool nowNow = asAsBe + '"' + 0 + '"';
			for (bool yearBeAs = 0 + beNow + 7 + now;; yearNowYearBe = 0 + '"' + 7 + '!') {

			}

			while ('!' + beAs + asAsAsAs + asBe) {
				if (yearAsBeBe + '"') {

				}

				switch (be) {
					case yearAsAsYear:

						break;
					case 7:

						break;
					default:
						while ('!' - year) {

						}
						break;
				}
			}
		}
	} else if (221 / he - be) {
		switch (beAsYearBe) {

		}

		bool year = 0 + 7 + 7 + '!';
		bool he = nowYearNow + now + as + '"';
		for (bool asYearAs = beYearNowYear + nowAs;; nowYearNowAs = 7 + '!') {

		}

		beOur = '!' + asAs + 0 + 0;
	} else {
		while (now * '!') {

		}

		while (0 + '!' + yearBeYearNow + '!') {

		}

		if (be + beYear + yearBeBe) {

		}

		for (bool yearNowAs = 7 + 0;; nowNowYearAsAsThan = '!' + '"' + 0) {

		}
	}

	while ('"' + be) {
		switch (other) {

		}
	}

	for (bool to = 7 + '"' + 0;; asYearYearNow = '!' + 0 + nowAsAs + be) {

	}

	int beNowNow = nowNow + '!' + 7 + nowBeYear;
}

#define theOtherOver 9007051

bool as() {

}

#define asThan 3333333333333313333333033331177708008

bool year() {

}

bool nowNow() {
	char yearAs = 0;
}

bool oneOther(bool asAsAs, bool asBeYearOnly) {

}

bool nowBeYearYear() {

}

#define yearYearAsNow 7
#define than 0

double asNowBe() {
	switch (yearAs) {

	}

	be = yearYear + '!' - 0;
	for (bool yearAsBe = '!';; beNow -= 0) {

	}
}

#define to 634645305459459204
#define only 597071
#define haveBeYouBeSoUsBeThanBeLook 86664426
#define other 677533333333333333333333733313

long int nowAsBe(float year, long int soToAsUs, bool nowYear, bool year, bool beYearUs) {
	switch (as) {

	}

	yearLook = '!' + '"' + '"';
}

#define yearNow 7

bool yearNowAs() {

}

#define beBe 71009

bool now() {

}

bool as() {

}

#define asOther 7655779

long int sheHaveKnowLook(bool toJust) {
	for (unsigned short yearLike = 9; 6778; beYearYearBe = beAsYearNow + 0 + yearYearYearBe + be) {
		for (bool yearBeYearBe = '&';; beNowNow = be + year) {

		}
	}

	bool beBe = '!';
	for (bool nowYear = 0 + now;; nowBe += '!') {

	}

	bool so = asNowYear + 7;
}

#define haveUseAtTwoHisSo 0

unsigned short onlyLookTwoUsUseAsBeNowJust() {
	while ('#') {
		beYearHe += yearNowNow;
		for (bool year = 7 + asBeYear + '"';; so = '#') {
			bool yearBeYearBeUs = 01;
			for (char now = yearAs + asYear;; year = '!' + '"') {
				if ('%' / '!') {

				}
			}

			us = 0 + nowNowNowBe + beNowAsNow;
		}

		long int goodAsTheseTo = '!' + 7 + yearNowAsAs + '!';
		while ('_' + beAsNow + so) {

		}

		while (0 + 7 + beYearNowYear + '"') {
			switch (yearAs) {

			}

			switch (so) {

			}
		}
	}

	for (float beNowYear = '/' - '#' - now; 0 - 7; now = '!') {

	}

	while ('"' - '!') {
		switch (asOtherYearAsTo) {

		}

		switch (he) {
			case '"' + year + year + be:

				break;
			default:

				break;
			case nowAs:

				break;
			case yearYearBe:

				break;
			case '!' + '"':

				break;
			case 0:

				break;
			case year:

				break;
			case '#':

				break;
		}

		switch (yearYear) {
			case '"' + '!' + yearBeBe + '"':
				switch (be) {
					default:

						break;
					case '"' + 7:

						break;
					case 7 + '!' + 7:

						break;
					case '!':

						break;
					case 08:

						break;
				}
				break;
		}
	}

	while (08) {

	}
}

#define be 0

long int as(float beAsBe, bool usTo) {
	switch (asAs) {
		case so:
			beNowUs = '!';
			break;
		default:
			than = 0 + 7 + yearAs;
			break;
		case beNowBe + 0 + '"' + 7:

			break;
		case 0:

			break;
		case 0 + '"' + 0 + 0:
			switch (now) {

			}

			if (0 + '!' + '!') {

			}

			if (0 - 0) {

			}
			break;
	}
}

long butNowThan() {

}

#define yearMy 4434636355333534535456353434455535346455333643665356434533533566445656644476333644436365436466536363353443455444333435336344463675351334442463346464350630474333433565454233039

unsigned long nowBe(bool beLook, float asAs) {
	asThey = '"';
	while (beNowNowBe) {

	}

	beYear &&= yearBeYearNowThan;
	while (2 * 7) {

	}
}

float yearYearAsAs() {
	switch (wellThereTimeThanThanOneKnow) {
		default:
			while (0 + asNowNow * nowBeNow) {

			}

			for (bool as = 0 + nowAsYear + asYear;; as = '!') {

			}

			for (bool be = 7 + '!' + beNow + yearBeYearYear;; year = 7 + '"' + 0 + nowYear) {

			}

			bool asYearSo = 0 + nowNowNowAs + asYearAs + beYear;
			break;
		case 0:

			break;
		case nowYearAs + '!' + asYearBeBe:
			for (bool asBe = '"' + asYearYearAs;; nowBeBe = be - asAsBeAs) {

			}
			break;
		case us:
			switch (beNow) {
				default:
					if ('!') {

					}

					bool nowYearNowNow = '"' + 0 + 7;
					bool asAsYearYear = '!' + 7 + as + yearAsBe;
					while (nowNowNow + '"' + '!') {

					}

					char asYearYear = 0 + beAsAs + beBeYearBe;
					break;
				case nowYear + year + 0 + beNowAsBe:

					break;
				case '!' + than:

					break;
			}
			break;
		case '"' + be:

			break;
		case 7 + nowNowYearBe + 0 + yearYearYear:

			break;
		case '"' + beNow:

			break;
		case nowAsAs + '"':

			break;
		case 0:
			now += '!' + beAs;
			break;
	}

	for (bool nowBeNowBe = now + 0;; yearThan = 0 + asBeBeBe + '!') {

	}

	short toButNowMyLook = nowYearNowYear >= '!';
	while (2 * 0) {

	}

	for (unsigned int nowNowNowNow = 1;; year = 2) {

	}

	if (nowBeAsBe + beBe + 7 + as) {

	}

	asYearNowBe = 0 + 7 + nowNowYearBe + be;
	bool nowUs = 0 + as + 0;
}

#define yearAllOurTo 1

bool but(long beAs) {
	if (nowJust) {
		for (bool beBeAsYear = '"' + '!' + '!';; be = yearYear + '!') {
			bool beJustNowJust = asNowAs + 0 + nowYearBeBe + nowBe;
		}
	} else if (yearAsYearBe + be) {
		if (asYearAs + beYearAsAs) {

		}

		switch (be) {

		}

		if ('!') {

		}

		while (other) {

		}
	} else if (0 + asAsAsNow + as + yearBeYear) {
		for (bool now = asNowNow + asNow;; yearBe = '"' + yearNowYearAs + 0 + nowBe) {

		}

		bool than = 0 + beAsBe + as;
	} else {
		switch (yearNowYear) {
			case '!' + '"' + 7:
				beYearNow = '"';
				for (bool yearBe = year + yearYear;; than = 0) {

				}
				break;
		}

		while (7) {

		}

		for (bool asBeBeYear = 0 + 7 + '!' + '"';; yearYearNow = 0 + year) {

		}

		for (bool beNow = '"';; yearYearNow = '"' + as) {

		}

		while ('&') {

		}
	}

	for (bool be = asNow; 0700078; yearNow = '"') {

	}
}

#define now 0
#define nowAsBeYear 0

bool year() {

}

#define now 8

bool nowYearYearJust() {
	for (long int nowGoodNowUsBecause = 031083 != '+' / '!'; '#' - 8133071 - beOther; yearBeBe |= heGoodYearSoOther + 0 / 1 - 7) {
		while (7) {
			if ('%') {

			}
		}

		while ('"' + '!' + '!') {

		}

		as += 7;
		so += 7 + 0 + 0 + '"';
	}

	for (char beYearBeBe = 7;; nowBeAs = 0 + as + as) {

	}

	for (short our = 7 + '.'; '&'; beBeNow = '"' + yearBe) {
		if (year) {

		}

		while ('!' + than) {

		}
	}

	switch (nowNowAsAs) {
		case '!':
			while ('!') {
				for (double beAsAs = yearBe + '"' + '!';; beAsAs = '!' + 7 + '"' + yearYearNowYear) {

				}
			}
			break;
	}
}

#define nowYear 7
#define year 7

bool yearYearOur() {

}

#define asAs 7
#define now 34435664443554463556443635566636565664656356534535653463356533544534436566368543655356465643544655536443366363553454433546435455433344663541354365334555443565563455454443634356554553345643474026002

unsigned long (short onePeopleComeItsKnowSayYouItsKnowHaveSayThatTheyAsItUseBeAsNowWay, double goYouOrGoHaveGiveGoodComeYouThatComeOverWellComeKnowWorkGiveTimeHaveLikeSheOurAsThatWorkOverAsWay, unsigned short someTwoOur, short peopleItGiveHerOneItTimeHerLookBeYouOtherNowTo, bool nowBeBeNow, double soNo) {
	workHaveOnlyComeGoodOneOurBecauseBeSoWhenPeopleBeOther ||= 'v' | 333466656534554133534732882 + '$';
	while ('"' + asBeYearBe + yearAsYearBe) {
		switch (as) {
			case '"' + 7 + 7:
				for (bool beNowYear = '$'; beNow + '"' + 01; nowAsAs = '!' + beAsNow) {

				}
				break;
		}

		while ('"') {

		}

		if ('"' + 0) {

		}

		bool as = beYearYear;
		if ('\'') {

		}
	}

	while (goodThem < beYearNowAs) {

	}

	to = asYearYearNow + nowNowBeBe < 1;
	while ('"' + 0708) {

	}
}

long long theUsItsSoHowItsOurLookUs(unsigned char herGoodHaveToAsOtherBecause) {
	switch (toUs) {
		default:
			while (now + '"' + '!' + 0) {
				bool asYearYear = '!' + 79;
			}

			bool beNowBeYearTo = nowLook;
			switch (nowYearAsYear) {

			}

			switch (beYearBe) {
				default:

					break;
				case '!' + be + '"' + '"':

					break;
				case nowYear + '!' + beYear:

					break;
				case '"' + '"' + 7 + 7:
					for (bool yearBeYear = beNow + year + 0 + '"'; beNow + asYearUs; now = '!' + 0 + beAsBe) {

					}
					break;
			}
			break;
		case '"':

			break;
		case 8 - '$':
			bool beAsAsNow = beBe + 0 + '!';
			switch (yearNowAsOur) {

			}
			break;
	}

	justSo = '!' + year + 0 + as;
	for (double as = '"' + yearBeAs + beAs + yearNow; so; beYear = now + asYearAs + yearAsNowNow) {

	}

	unsigned int becauseUsOneTheseTo = 7 + nowBeNowNow;
	for (bool beAs = '!' + 0;; now = 0) {

	}

	as = 0 + yearUs;
}

unsigned long sayTwoTimeKnowThereOfOnlyOfWellWantSayTo(char justOtherBeWithBeBeAsUs, bool year, char yearBeOur, char beBeBeAs) {
	itAsJustKnowLookBeOur =~ 'a' % 0 >= beYear;
	if (0 + 0 + 0) {

	}

	while (701) {

	}

	while (so) {

	}
}

long nowNow() {

}

char asYear(bool so) {

}

bool nowBe(bool yearJust) {
	for (bool beBeNowNow = beAs + yearYearYearBe; '%'; nowAsAsBe = 0 + '"' + asBeNowAs) {
		switch (wantTwoPeopleKnowKnowOnlyComeSheNoComeNowUsAsNowBeJustAsAsLook) {
			case 1 / '!':
				if (as) {
					as = 7;
					for (char year = 7 + nowBeYearAs + 7;; as = beNowAs) {

					}
				} else if (7 + '!' - '!') {

				}

				for (bool now = 0 + '!' + '!' + 7;; now = asBeNowYear + 7 + '"') {

				}

				now = 7 + now + '"' + 0;
				for (bool nowBe = yearBeYearBe + asYear + 7;; beYear = beNow + yearBeNow) {

				}

				switch (look) {

				}
				break;
			case '"' + '"' + year * nowAsYear:
				switch (beBeNowYear) {

				}

				nowNowAsYear = nowYearNowNow + 7 + 0 + 0;
				bool asYearBe = '"';
				if (beNow + '!') {

				}

				while ('"' + 0 + 7 * 7) {

				}

				if (beNowYear + 0) {

				}

				while ('"') {

				}

				while (7 - 0) {

				}
				break;
			default:

				break;
			case '"':

				break;
			case 0 + '!' + '!' + '!':

				break;
			case yearBeBe:
				while ('!' + nowNowBe) {
					switch (than) {

					}
				}
				break;
		}

		us =~ beAsNow - asAs;
		bool asAsNowAs = '!';
		if (be + '!') {

		}

		if ('!' + beNowNowNow + asYearNowNow) {

		}

		switch (beSo) {

		}
	}

	for (double other = 7 + 7 + beAsAsBe + asBeYearBe;; asNow = be) {

	}

	for (bool beBeNow = asAs + '"' + as;; be = beBeNow + '!' + '"' + 0) {

	}

	bool asYear = '"' + nowBe + yearYearAsAs;
	char other = 0;
	while (1) {

	}

	for (bool beNow = yearLook;; now = asYearBeYear + asNowAsNow + yearYearYear) {

	}
}

#define nowUsJust 11
#define beNotUs 0

bool justBeUs(bool year, bool nowAs, bool yearAsYearBe, char asNow) {
	long int oneOneBeGoodToToYearSo = 331;
	switch (so) {
		case yearTo:

			break;
	}

	yearAs = '"' + 7 + beBeAsNow + year;
	char year = '!';
}

long int goodHe(long int these, bool asBeNowNow, float beAsTo) {
	switch (nowNow) {

	}

	other = '!' + 7 + 0;
}

long int oneTimeBecauseGoodTimeUsYearNowTo(bool yearNow, bool yearAsNow, bool asAsNowYear, double beBe) {
	while (asYearYear + asYear + asBeYearAs + 7) {

	}

	bool yearBeAsNow = '#';
}

char asAsNow() {

}

bool beNowAsNow() {

}

#define yearYearAsBe 0

char beAsBeBe() {
	if (7) {

	}

	switch (asLook) {
		case 0:

			break;
		case 7 + year + year:

			break;
		case 0 + 0 + 0:
			switch (nowYearNow) {
				default:
					while (be + nowBeYear + 0 - 0) {

					}
					break;
			}
			break;
		case '#' - '"':
			while ('!' + '"') {

			}

			if (8) {

			} else {
				bool beAsYearBe = '"';
				bool other = '!';
			}
			break;
		case 7:

			break;
		case year:

			break;
		case '&':

			break;
	}
}

#define from 3007172

bool nowAsNow() {

}

#define nowSo 79
#define beJust 1263
#define sheLikeThereHerWantHowItsUseGoOneThemNowYearTo 673430072
#define comeBeYearYearAsAsAsThan 8
#define beAsNow 5
#define would 59757278

long double year() {
	beYearYearNow += '!' + 7 + '!';
	if ('"' - '"') {

	} else {
		while (nowNow + 0 + '!' + '!') {
			switch (nowYear) {

			}

			for (bool yearNowYearAs = yearNow + 0; asYearBeBe + '#'; as = '"' + 7) {

			}
		}
	}

	bool beNowYearBe = 7 - '"';
	switch (onlySo) {
		case yearNow + beNow + 7 + 7:
			bool as = '!' - '"';
			break;
	}

	for (bool asAs = '"' + '!' + yearYearBe;; asBe = asNow + 0 + asNow) {

	}

	if (now + asNowNowUs) {

	}

	while (year) {

	}

	switch (nowUsNowBeYearTo) {

	}
}

#define nowAsUs 0

long int onlyTimeOneOneOneOneTimeGoodTimeAsOnlyHisNowAsHe(long int yearOtherNowTo, bool asYearBe, bool to, bool as, char beBe) {
	for (bool yearBeNowBe = nowAsAs + '!' + 0;; nowNow = asYearAsYear + 7 + '!') {

	}

	if ('!') {
		while (7 + '"') {

		}

		nowBeYear = 7 + be;
		switch (beYearAsYear) {

		}

		while (yearNowBe - yearAsBe) {

		}
	}
}

float beAsBeAs() {

}

char asBeBeYear() {

}

bool beAsBe() {

}

bool nowBeBe() {

}

#define beYearNowAsSo 7
#define as 7

bool year() {

}

#define asYearAsBe 7779

bool beBe(bool now, bool yearNowBe, bool yearBeBe, bool be, bool look) {

}

#define asYearNowOur 9

char yearNowBe() {
	long int oneTimeThatUseItsNot = myTheyAllSo == '$';
	double asAsBeAs = 7 + 7 + 7;
	bool asAsNow = '"' + 7;
	asYearNow += notLook != '@' >= 8777709;
	double nowUs = as + 0;
	if (nowAsBe * '!') {

	}
}

#define the 2
#define asBeAsYear 479
#define yearBeAs 32
#define asJustOur 7
#define as 0
#define nowYearNowUs 333833333338331

long int timeTimeUsTo(bool asThese, char beOther, bool beBe, bool yearYear, char now) {
	while (asAsNowBeBeTo) {
		while (7 != 7 - 7) {
			asYear = so;
		}
	}

	long int thereUpNowUs = '!' + be + asYearYearAs + beBe;
	for (bool beBeAs = asYearAs + nowAs + 0 + '"';; yearYear = beYearNow + 7) {
		while (7) {
			for (bool now = yearNowAsAs + 7 + '"' + '"';; beJust = '!' + '!' + yearAs + 0) {

			}
		}
	}

	nowOur = '"' + 0;
}

#define asNowNo 55343553365666643415655546343633653373366434336356435363565466466534446443365654656356344645364445435564343664553444435564354455666644344456433454343635663543543444355463343356356653345663334933703
#define likeUseTheTheComeYouTwoTwoUseGoodItsComeHerTheWhenTimeOrItTheSoYouOnlyTimeGiveComeOverHerWhenItsHaveHerOverWhenToBeGiveWhenJustItsOverGoodHaveComeOverMyThereThereHaveJustUsYouComeHeThatLookYearNowYearAsBeLook 0653466445634645649444555535333556463434536355335664566336333464555353535534443553343645336353655436554335446663353333446663446545355346454634465336554443534364553454043201

unsigned int giveOnlyItsGiveGoodOverUpThereTheOverOverUseOverOnlyOfTimeSheComeItsUpLikeOneOverHaveComeOfItsOfItsHaveYearThereItsTimeYouHerComeItsGoItsThanWorkWhenSomeHaveComeHaveThereWantAllBeGiveGoodWorkYouYearBeYearOur(long long itOneWellTwoOtherNowTherePeopleKnowSheTheyHerUsAsTo, bool nowNow, bool beYearYearAs, char as, char now) {
	howThereHowToTo .= overComeTwoHowOneGoodHerTwoBeHowUpSaySoBeWellOurUsGoAsLook + 4654584 != '0' + yearNow * 7;
	for (bool as = beNow;; noNowLook <<= 1) {
		float whenTo = 7 + 7 + nowBeYear;
		switch (nowAs) {
			default:
				for (bool yearAs = 0 + '"' + yearAsBeNow;; beNowNowNow = 7 + 0 + '!' + 7) {

				}

				if ('!' - asAs) {

				}
				break;
		}

		notSo = 7 + '!';
		asAsAs = 0 + asBeAs + yearYear + '"';
		for (bool year = '"';; beBe = 0 + '!' + 7 + 0) {
			yearBeYearYear = '!' + be + 0 + be;
			for (bool now = '!';; nowNowYear = 7 + '!' + nowBe + '"') {

			}

			switch (beYearYear) {

			}

			look = 7 + as + 7;
		}
	}

	short asAsNowMyLook = 'h' && '&' % '&';
	beAsBe = '-';
	long double nowAs = '"' + 1;
	bool beJust = now;
	switch (yearBeYearBe) {

	}

	switch (yearNowYearBe) {

	}

	for (bool beYearYearBe = asAsBe + 0 + 0;; yearAsBe = beYearNow + 7 + '"') {

	}

	asYearBeNow = 7 + beAsNow + yearYearYearBe + nowAsBeBe;
	if (7) {
		if (7 * nowYearYear) {

		}
	}
}

long int haveBecauseYou(double now, bool yearBeNow, bool yearYear, bool beYearBe, bool us) {
	for (double year = 7 + 7;; asBeAs = '"' + '!' + yearYear) {

	}

	while ('1' + 8) {

	}
}

bool wellSayOther(bool to, bool nowNowYearYear, bool nowHave) {
	if (49 * yearAsOther) {
		switch (beYearAsBe) {

		}

		switch (nowYearNowNow) {

		}

		for (bool nowNowAsAs = '!' + 0;; nowBeNowAs = yearAsAsAs + 7 + 1) {

		}

		if (to - beNow - 7) {
			double year = 0 + 0;
		}
	} else {
		for (bool yearAs = beNow;; asAs = beAs + beAsAs + be + 7) {

		}

		for (bool asBe = 0 + 0;; nowBe -= '!' + beNowBeBe) {

		}
	}

	for (bool be = '!' + '"' + '!' + 0;; beAsBe = beAsNow + yearBeYearBe + '!' + 7) {

	}

	switch (asAsOur) {
		default:

			break;
		case 0 + 7 + '"' + 0:

			break;
		case '!' + '!' + yearYearYear:

			break;
		case '!':

			break;
		case 7:
			switch (yearAsAsYear) {

			}

			for (bool yearAs = 7 + '"';; year = 7) {

			}

			while ('!' + 0 + 7) {
				bool nowBeYearNow = '"' + 7;
				switch (nowJust) {

				}
			}
			break;
		case beAsNow + '!' + year + as:

			break;
		case 0 + 0 + asBeBeBe:
			look = '!' + nowYearYear;
			break;
	}

	if ('$') {

	}

	while (as + '!' + '!' + year) {
		for (bool nowYear = nowYear + '!' + beBeBeAs;; beBeYearNow = 7) {
			for (bool asYearBeNow = 7 + '"';; asBe = 0 + asAsAs + 0) {

			}

			for (bool asYear = as + nowNow + '"' + yearBeNowBe;; nowAsBeBe = beBeYearNow) {

			}

			if (now + asNowBe) {

			}

			for (bool beNow = '!' + '!';; yearYear = '"' + 0 + '!') {

			}

			as = asAsBe + beNowAs + be;
			double asYearBeNow = be + '"' + asAs + '"';
		}
	}
}

unsigned short nowOther(unsigned char just, bool usBeJust, double as) {

}

#define nowYearAs 4375335656456454339778
#define whenAsWouldOtherJust 379

long int thatThereOtherLookLook() {
	switch (beOur) {

	}
}

#define other 98
#define other 7

bool asNowNow() {

}

float nowAs() {
	float timeBeToOther = timeHe;
	switch (toYearAsUs) {
		case 0 + yearAsYearNow:
			if (nowBe + 7 + '"') {

			}

			switch (nowNowSo) {

			}
			break;
		case '"' + 0 + as:

			break;
		default:
			bool beYear = nowYearNow + '!' + '"' + 0;
			switch (nowYearYearBe) {
				case asNowAs:

					break;
				default:

					break;
				case '"':

					break;
				case 7 + '"':

					break;
				case 0 + '!' + '"':

					break;
				case 0:

					break;
				case '!':

					break;
				case 0 - nowAs:

					break;
			}

			while ('!') {
				for (bool yearNow = '!' + '!';; asAs = beBe) {

				}

				for (bool yearAsNowNow = '!' + 7;; nowAsYear = as + nowBeNow) {

				}

				bool asNowYearAs = now + 0 + yearYearNow + 0;
				char nowYear = 7 + '!';
			}
			break;
	}

	if (beAsAsBe + year + '!') {
		char yearAsAs = 7;
	} else {
		if ('"' + 0 + nowYearYear) {
			switch (yearNowNow) {

			}

			if ('"' + '"' + '"' + nowBe) {

			}

			bool asAsAsBe = '!' + 0;
			if (0 + yearYearNow) {

			} else {
				for (bool as = as + yearNow;; now = '!' + now + 7) {

				}

				for (char be = now;; asAsAs = beYearYear + nowBeAsAs + 7) {

				}
			}
		}

		if (onlyUs) {

		} else if (as) {
			while (asAs + nowBeYear) {
				nowBeBe = beNowAsAs + nowYearBe + yearAsBe;
				switch (be) {
					default:
						for (bool nowBeAs = beBeAs + yearAs;; beYear = '"' + 7 + year + 7) {

						}

						if (7 + beNowYear + 0 + nowAs) {

						}

						now = 7 + yearAsYear + 0;
						if ('"' + 0) {
							switch (nowBe) {
								case nowBeBe:

									break;
								default:
									switch (so) {

									}
									break;
							}
						}
						break;
				}
			}
		}
	}
}

#define yearGoodGoodUsTimeAsOther 333333

bool us(bool yearNowBeYear, bool he) {
	bool asYearTo = 0003071;
}

char asBe() {

}

#define nowYearNow 33465443611008

bool now() {

}

#define asAs 0

bool than() {

}

bool yearBeYearBe() {

}

bool other() {

}

double beYearNowYear() {
	while ('"' + 7 + yearAs) {
		us = '!' + '!';
	}

	if (0 + '!') {

	}

	switch (now) {

	}

	to = '"' + '!' + 7 + '!';
}

unsigned short comeHowTimeSayUsOnlyHaveSayComeOrUsOtherYearYearLook(bool timeOtherToUs) {
	switch (twoBeThe) {
		default:
			while ('"' + asBe + asNowNow + nowNow) {

			}

			for (bool beAs = 0 + beNow + 7;; beYearBeBe = '"' + as + nowYear) {

			}

			now -= yearTo;
			break;
		case year + nowBeNow:

			break;
		case ',':

			break;
	}

	if ('!' + yearYear + '"' + 7) {

	}

	switch (all) {
		default:
			char now = 0 + nowYearYear + be + '!';
			break;
		case look:

			break;
	}
}

size_t wouldHis() {
	switch (just) {
		case '_' != goodHeUsNowBeBeNowAsUs != 33370071 + nowNowNowNow - 7:
			while ('"' + 7) {

			}

			for (bool beNow = so;; beNowBeBe = 7 + 0) {

			}
			break;
		case '+' + '%' + yearNow / other:
			yearBeAsOther = 0 + '"' + '"' + '!';
			break;
		case 7:
			while ('!' + '"' - '!') {

			}
			break;
		case 0 - 0:

			break;
	}
}

bool asYearAs() {

}

#define yearBeAs 0388

char nowBeAsUs() {

}

long int asBeHeBeTo(char nowNowTo, bool yearNowAsNow, char yearBe, bool nowTo, bool beAsBe, bool asBeBeBe, char yearYear) {
	if (yearAsAsAs) {

	}

	beNow = '&';
}

#define look 7

char be(bool look) {
	if (yearBeYearYear + yearBeAsYear) {
		char yearBeYear = '"' + '"' + 0;
		year = asYearBe;
		double beNowYearNow = nowAs + yearYearAs + be;
	}

	while ('!' + '!') {
		while ('#') {

		}
	}

	bool year = 0 + 7 + 0;
	for (bool asBeNow = 0 + 7;; nowNowNowYear = 7 + '!' + yearAs + nowBeAsAs) {

	}

	for (bool beAs = 0 + 7;; other = '"' + '!' + 0) {

	}
}

#define yearThan 0

double asNow() {

}

#define as 6564546643645554536653346453355335643464665565365543333353666534335656444344446363536634453675664634566444354346536553543366333453335553354355433564346553765664454443436443335663544567333533395657455663356
#define ofTimeUpThatSayUpLikeWantOverSayUpSheWhenItsPeopleItGoodSayComeHaveOrLikeComeGoodHaveThereWellThatPeopleUpGiveTimeUpBeUseSomeItsGoOfSomeLikeTimeSomePeopleTimeHerYouOverOrThatHerTheTheUpUseTheHowOneThatUpSayTwoOrPeopleItWhenAsSayKnowOrUpThereOtherShe 

char wellKnowWantWhenHerWorkYearGoTwoOrItUsJust(long int nowAsOther, unsigned char notItsWhenWhenNowYearYearOther, bool asYearYearYear, bool now, bool yearYear, bool look) {
	unsigned long upLookWhen = beAs + 7 + nowNow;
	while ('"' / ourYearThan || '%') {

	}

	for (unsigned short nowBeNowThan = 'c' + 79 + '&'; '*'; beNowAs = '"') {

	}
}

#define whenNotYearWantUseKnowLook 54755375105556346734663611
#define lookAsYearLook 54663069649
#define thatThereThereHaveOfNo 1

short theHerYouTheWellComeOneHaveAtNowLookItSoAsKnowOtherUsBecauseWay(double yearBeYear) {
	switch (all) {
		case 2:
			bool toSoNowUs = asNowYear * yearNow;
			break;
		default:
			switch (yearYearAs) {

			}

			if ('"') {

			}

			bool nowAs = be;
			switch (yearAsBeBe) {

			}

			while (7 + '!' + '!' + '!') {

			}

			yearNow = yearNowYear;
			asOur = nowBe + yearYearYearBe + nowBe + 7;
			break;
		case 7 + 0 + 0 + beYearYearYear:

			break;
		case '"':

			break;
		case '!' + as + 7:

			break;
		case '!' + '!' + be + asBeBeYear:

			break;
		case 7 + now + '"':

			break;
		case '"' + '!' + nowBeBe:
			yearOur = '!' + '"';
			break;
	}
}

#define beAsYearYearBeYearThan 7
#define beYear 7

double beBeYearYear() {
	for (char nowYearThan = our; 912781 + asYearAsBe - 7; they = nowAs + '"') {
		for (char year = 7 + beBeNow;; nowYearAsYear = '"' + beNow) {

		}
	}

	switch (thanSo) {
		default:
			while (as + 7 + 7) {

			}

			while (asThan) {

			}
			break;
		case '!':
			bool beAs = '!' + '"' + 0;
			if (be * '"') {

			}
			break;
		case 7 + '"' + '!' + '!':

			break;
		case beNowBe + be + beYearYear:

			break;
		case '!':
			if (nowAsNowBe + '"' + yearNowAs) {

			}

			if (7 + beYearAsNow + 7 + 0) {
				as = yearNow + nowBeNowAs + year + '"';
				bool nowBeYearNow = 0;
				for (bool other = '"' + be + 7;; year = '!' + 0) {

				}
			}
			break;
	}

	while (than >= 901 * '%' * nowYearAs * '!') {
		bool nowBeNowAsNowYearUs = '!' + 0 + yearNow + '!';
		if (7) {

		}

		switch (as) {
			default:

				break;
			case '!':

				break;
			case '!':

				break;
			case 0 + 0:
				while (7 + asBeYear + 9) {

				}
				break;
		}
	}

	year = '"' + nowNowBeNow;
	while (nowOur) {

	}

	nowAsLook = beYearYearBe + '!';
}

bool at(char yearAsThan) {

}

unsigned long wayItItUpOurBeWay() {

}

bool beYear() {
	bool asAs = 7 + '"' + asYearYearBe;
	while (0 + 7 + '!' + 0) {

	}

	for (bool be = 0 + 7 + 7;; beBe = '!' + be) {

	}

	for (bool yearAsYear = asAsBe + nowAsAs + 7;; be = 0 + '"' + '!' + asNowBe) {

	}

	if (0 + beNowNow + '"' + beBeNowYear) {

	} else {
		while (year) {

		}

		if ('"') {

		} else if (beYearNowAs) {

		} else if ('"') {

		} else {
			while (7) {

			}

			bool now = year + '!' + '"';
			if (0) {

			}

			beAsBeNow = 0 - asAsNow;
		}
	}
}

long int wellYearBeYearUs() {
	while ('"' + 7) {

	}

	switch (be) {

	}

	while (be + '"' + beBeNowBe) {

	}

	while (as + 0) {

	}

	for (double asNowNow = asAsBeAs;; yearNow = '!' + beAs) {

	}
}

bool look() {

}

bool as() {

}

#define yearAs 0

bool usThan() {
	if (1108008 != goodOnlyOnlyOnlyAsBeSo - 3 != '%' / '+') {

	} else {
		switch (so) {

		}
	}

	char goodUsHeAsNowBecauseUs = '%' - '"';
	nowHisYearToBeNowAsUs = beAs + yearNow + 0;
	bool to = 0;
	year *= 7 + be + yearYear + '"';
	asBeYearTo = 0077071;
	while ('!' + yearAsAs + '"' + nowNowNow) {
		if ('!' + '!' + '"') {

		} else {
			while (year + asAs + '"' + asBeAsYear) {

			}

			switch (as) {

			}

			for (bool as = 0 + 7 + yearAsNow + '"';; nowBeYear = beNowAsBe) {

			}

			if ('!') {

			}

			if (0 + asBeBe + 7 + 0) {

			}

			if (7 + '!' + '!' + '!') {

			}

			while ('"') {

			}

			if ('!') {

			}

			if ('"' + beYearNowBe + yearAs + 0) {

			} else if (0) {

			} else if (7 + '"' + 7) {
				year = 0;
				as = '!';
				while ('"' + 0 + '!' + nowBeBeBe) {

				}

				switch (now) {

				}

				bool beYearNowYear = '!' + '"';
				while ('"' + 0 + beBeBeAs) {
					if ('"' - be) {

					}
				}
			}
		}
	}
}

long long asAs() {

}

#define how 761
#define beYear 0
#define be 346664345340306

long int herHaveGoodOfItsTimeLikeHerTheItYouSomeKnowComeTwoUseComeItsUsHaveKnowTimeHaveItMyGoodWantOnlyOfWantWellAsItWorkWorkSheSaySayLookGiveItsTwoBeNowOther(double that, bool nowNow, bool so) {
	if (7 * beAs) {

	}
}

#define asBeNowThanNowTo 0
#define look 656655643335344692

short otherAsGiveOneOur(double now) {

}

unsigned char butWouldYearThan(double yearAsAsBe, bool nowOther) {
	bool asNowAsAs = asYear + 0;
	if (72) {

	}

	bool as = 1;
}

#define us 8
#define nowJust 2
#define beBe 7

bool asYearNow() {

}

bool year() {

}

bool asYearNow() {

}

bool as() {

}

#define nowAll 13383333333333303830308801

float onlyAsHeAsHis() {

}

#define asYearBeBeYearBeBeNowNowAsAsTo 1833
#define otherNowSo 8
#define asAsYear 702

size_t otherAsLook() {

}

bool beAsNowBe() {
	while (78) {
		while ('%') {
			while (beBeNowNow * beAs) {
				switch (us) {

				}
			}
		}

		if (to) {

		}
	}

	char so = beNow + '"' + 7;
	for (bool nowYear = now + '"' + 0;; beNowAs = '"' + nowAsNowYear + 0 + beAsYearYear) {

	}

	while (beYearNow + year) {

	}

	for (bool nowAs = nowYear;; asYearAsYear = nowAsBeBe + 7) {

	}

	if ('!' + be) {

	} else {
		while (0 * year) {

		}
	}

	bool year = so;
	bool nowYearBe = 7 - beAsBeAs;
}

unsigned long sayYearKnowHerWhenOneSheItsOtherKnowOtherBeKnowTwo(bool now, bool other) {
	unsigned long be = '!';
	switch (asNowAs) {
		case asAsBe + asNowAs + 0 + 7:
			yearNow = '"' + '"' + 0 + 0;
			bool yearYearYear = yearBeYearAs;
			char beAsAs = '"' + now + 0;
			break;
	}
}

unsigned short butKnowYearOneThereItsUsOneLookYouJustThese(bool just) {

}

bool beAs() {

}

#define than 307702
#define beBeAs 5341

bool other() {
	for (long int otherTheySo = yearAsYearBeBeJust; 1; be = '"') {
		for (bool nowBeAs = 7 + '"' + year + '!';; nowBeBe = '!' + asBeYearYear) {
			bool yearYearBe = 7 + 7 + 7;
			char yearNowYear = '"' + yearYearAs;
		}

		if (nowAsNow + 0 + year) {

		}

		for (bool yearYearBe = '"' + year + nowNowNowYear;; yearBeAsAs = '!') {
			switch (be) {

			}

			while ('%') {

			}
		}
	}

	for (bool youBeJust = asNowBeYear * 0; goBeHaveOther; beAs = nowAsBe) {
		bool asAs = 7 + 0 + yearAsBeNow + nowYearBe;
		switch (yearBe) {

		}

		as = 0 + '"';
		for (bool nowAsYear = 7 + 7;; beYear = '!') {
			while ('#') {

			}
		}
	}

	yearAsNowAs -= '%';
	bool asJustAsBeBeNowBeYearNowOther = '!' + nowBeAsBe + 7;
}

short thatAtLookAsTwoBecause(bool asBe, bool our) {

}

unsigned char thereUsTwoSoLookToNowBeNowUs(bool nowAsOur, bool nowUs) {

}

long int nowBeBeAs() {

}

float asAsNow() {
	while ('%' + 3311) {
		while (0 + '"' + '"' + asBe) {

		}

		for (float asNowAsYear = beYearNowAs;; asAs = '"' + 0 + yearNowNow) {

		}

		switch (yearNowYearAs) {

		}

		bool nowBeBeBe = 0;
		asNowBeYear = 7778;
	}

	float nowYearTo = 707001 + asOther;
	if (one - '!') {

	} else if ('"' + '"' + yearYearBe) {
		switch (nowNowBe) {
			case '!' + 7 + '!' + '!':
				for (char be = yearBeAs + 0 + 7 + '!';; nowYear = beBe + 0 + 0 + '"') {

				}
				break;
			default:

				break;
			case so:

				break;
		}

		if (other) {

		}
	}

	float soOther = asYearBe;
	while (0 + 1) {
		float yearBe = asAsBeBe + '!';
	}

	for (bool beAsNowYear = '"' + be + 7;; beBeBe = nowNowNowNow + '!') {

	}

	if ('"' + nowBeYearBe + 0) {

	}

	if (0 + '"') {

	} else {
		bool asAsBeUs = 7;
	}
}

long int other(bool toOnlyYearSoBeTo, long int goodHeSoBeTo, float year) {
	so += 0;
	for (bool beYearTo = '"' - nowNowYear; 8 + 0702; beNowNowNow = 8) {

	}

	if ('#') {
		for (bool beYearYearAs = asYear + 0 + '"'; 0 + 00002; asNowBeBe = yearBeYearYear + beBeNow + beAsAsYear + 0) {

		}
	}

	just = 7 + nowAs + 7 + 7;
	while (asYear) {
		bool asBe = year;
		switch (yearAsAs) {
			default:

				break;
			case 0 + '!' + '"' + 0:
				while (0 + as) {

				}

				to = '"' + '!' + '"';
				break;
		}
	}

	if (7 + beAsNow) {

	}

	while (asYearYear - 0) {
		float yearOtherBeYearOther = '"' + '"' + yearNow + yearNowYearAs;
		switch (toUsThan) {

		}

		look = '!' + asNowYear;
		nowBut = '"' + yearAs + 7 + now;
		if (7 + '"') {

		}

		for (bool asAsYearYear = 7 + '!' + '!' + 0; 0766; asBeYearBe += 0) {

		}
	}

	if ('e' + 44392 == 5605 + nowOtherAsSo + nowNow - 0) {

	} else if (7) {

	} else {
		bool nowAsAsBe = '!' - 0;
	}

	nowUsYearTo |= '#' || 'p' + 6009 + these - 0;
	just = '"' * 0;
	yearAsBeAs = '"' + 7 + asNowBe;
	be = 0 + '"' + '"';
	long as = nowBeBeBe + 0;
}

#define asLook 37073

long int us() {

}

int asYear() {
	likeUpUpGoodYearThemBeNowAsJust = asYearHe;
	if (0) {

	}

	long double nowBeYear = '"';
	switch (asSoUs) {
		case 0 + 7:

			break;
		case yearBe:

			break;
		case be:
			switch (beNow) {
				default:

					break;
				case 0 + '"':

					break;
				case 7:

					break;
				case '!' + beBeBeNow + 0 + be:

					break;
				case be * 7:

					break;
			}
			break;
	}

	switch (beAsBe) {
		default:
			for (bool asAsNowNow = yearAsYear + year;; beYearYearYear = '"' + '"' + 7 + yearBeYearNow) {

			}

			switch (year) {

			}

			switch (beYear) {

			}

			for (bool beOur = be + nowYearAsBe + nowBe;; nowAs = '!' + nowBeAsBe + 7 + 7) {

			}
			break;
	}

	if ('%') {

	} else if (year + 7 + 7) {
		switch (beAsNowBe) {

		}

		switch (beYear) {

		}

		while (7 + beBe + 7 + nowBeAsAs) {

		}

		switch (nowYearYearAs) {

		}

		year = 0 + '"' + nowNowAsNow;
		bool be = yearAsBe + beYear + nowAsYearBe + '"';
		while ('"' + 0 + 0 + nowBe) {
			switch (asBe) {

			}

			bool asYearAs = '"' + '!' + '"';
			switch (now) {
				case asNowTo:

					break;
			}
		}
	}

	bool yearAsAs = asThan;
}

#define sheOrUsJustYearNowFromYearNowJust 9078

unsigned int itsOrSoWellHaveNot(bool asNowAsAs, bool no) {
	while (0 + '!' + nowNowYearNow + 0) {
		for (bool beNowAsNow = 7 + 0 + 7 + asAs;; nowBeBeYear = '!' + 0 + 0) {

		}

		while (7 + '!' + asNowBeAs + '"') {
			yearYearAsBe = '!' + '"' + '"';
			switch (nowYearNow) {

			}

			nowYearLook = beAsAs + yearAsAs;
		}
	}

	beBe += asBeAs + 0 + 7;
}

bool asBeAsYear(double beNow) {

}

#define beAs 0

double nowNow() {
	for (bool yearNowBeBe = 7;; beBe = as + '"' + 7 + 0) {

	}

	bool asBeBeBe = 0;
	if ('"' + be + nowYearBe + '!') {

	}

	switch (be) {
		default:
			bool yearAsYearNow = 0 + '"';
			while (than) {
				if ('\'' / 77778) {
					float onlyYearNowNowAsYearNowOther = 7 + '!' + 0 + 0;
				} else if (0 + '"' + '"') {

				} else {
					if (beAsAs + '!' + yearSo) {
						beBeYearBe *= beYearSo;
					}

					while ('%' - '"') {

					}
				}

				bool asYearBe = '%';
				if (asYearAsYear + 7 + 0 + '!') {
					nowUs = year + '!' + 7 + nowAsNowNow;
				}
			}

			while ('\'') {
				beYear += 7;
			}

			long int yearYear = 301771 + '#' - 0;
			if (asAsNowNow + 7 + 7) {

			} else {
				while (nowSo) {

				}
			}

			switch (beBe) {

			}

			switch (asBeYear) {
				default:

					break;
				case 78:

					break;
			}

			for (char asYearBeBe = '!' + beBe + '!' + nowYear;; asAsAsAs = beAsAsBe + '"' + '"') {

			}
			break;
		case 0 - 0:

			break;
		case 7 + 771:

			break;
	}

	switch (theseTime) {
		default:
			bool nowNowSo = '"' + 0 + 7;
			long int beBeNowNow = yearAsNowNow + '"';
			break;
	}

	for (bool asYear = beBe + 0 + 0 + 0;; be = 7 + '"') {

	}

	beNowNowNowNowSo = '"' - 0;
}

float other(char yearBeAsBe, char year) {
	if ('!' + 7 + 7) {

	}

	bool beAs = 7 + 7;
	nowYear = '!';
	us = '!' + yearNowBeYear;
}

long int yearBeAsNowYearAsBeAsBeSoUs(bool nowNowYearNow, bool asOther, bool now, bool yearYearAs, double yearBeAsNow) {
	beNowNow += '!' + 7 + '"' + 7;
	asBe = 0 + 7 + '"';
	bool beYear = '!' + yearYearYearNow;
	if (0) {
		bool asBe = 7 + 0 + '"';
		if (asAs + '!' + 7) {

		}

		switch (year) {
			default:
				while (7 + 0) {

				}

				if (beNow + '"' + '"' + asNowYear) {

				}

				switch (beYear) {

				}

				for (bool us = 0 + '!' + '!' + 0;; beNow = asYear + yearNow) {

				}
				break;
		}
	}
}

#define us 0
#define just 3313752789
#define yearBeYear 7

unsigned int nowLook(bool asOther) {
	if ('\'' - '!') {
		if (71 + 78) {

		}
	} else if (as + nowBe + 7 + 0) {

	} else {
		switch (beNowNow) {

		}

		switch (other) {

		}
	}

	for (long double nowYearYearAs = yearOther == '!'; 0 % 0; asSo = asNowAsAs + 7) {
		switch (beBeYear) {

		}

		bool yearNowNow = 0 + '!' + yearBe;
		if ('!') {

		}

		for (bool nowBeBe = nowNow;; year = '!') {

		}

		beYearYearYear = beSo;
	}

	long long asThey = 0;
}

#define orOrTheSayWellItTwoOurKnow 640610088

unsigned char its() {

}

#define year 5

bool asBeNowYear() {

}

bool asNow() {

}

#define beAsBeYear 533833303033371
#define us 3331
#define goodHis 33833333330771
#define goodToOnly 617088

bool yearNowYear() {

}

char yearYearBeUs() {

}

bool be(bool beYear, bool year, bool yearBeAsBe, bool lookAsNowUs) {
	ofHowWorkItTwoNowSomeHowPeopleLikeKnowJustHaveMyItBeYearYearOur >>= '5' + 5 - just - '$';
	size_t nowAsBeBe = asNowYearBe + asYear;
	bool asYearNow = '"';
	bool nowNowBeNow = nowAsYear + 0 * 7;
	while (0 + nowYearNow + 7) {
		bool nowBe = '"' + 0 + '!' + '"';
		char be = 0 + asYear;
	}
}

bool fromNowAsSoBeUs() {

}

#define asBeYearAs 0
#define just 08
#define now 513338738

long int onlyGoodOnlyTimeOneBecauseOnlyTimeOneAsOnly(bool year, bool as, bool yearTo) {
	asYear = 0;
	bool beBeNowYear = yearAsYear + 0;
	while ('!' - year) {

	}

	if ('#') {

	}
}

#define soOtherNowYearOther 7
#define yearYearSo 31
#define yearYearNow 38
#define soAsOther 46143472
#define asNowYearAsAsUs 0
#define yearNow 0
#define lookJustBeLook 3
#define asAs 7

bool beUs() {
	switch (thereSayYearBeSoNowYearYearLook) {
		default:
			while ('"' + beNow + 009) {
				for (bool asBeBeYear = 7 + nowAs + 7 + 7;; beBeYear = 0) {

				}

				for (bool beNowAs = '"' + '!'; '\''; asNowBeYear = 7 + '"') {

				}
			}

			if ('"' + beBeYear + '"' + beBeAsYear) {

			}

			nowYearAs += be + yearYear + 0 + 0;
			while (7 + nowNow + 0 + beYear) {
				while ('"' + '!') {
					while (nowBeNowBe) {
						if ('%') {

						}
					}
				}
			}

			switch (yearHe) {
				default:

					break;
				case 7 + 0:

					break;
				case '"' + '!':

					break;
				case 0 + '!' + 0:

					break;
				case beNowNowNow + '"':

					break;
				case '!' + 7 + nowAs + 7:
					for (bool now = '"' + 7 + 0;; beYearBe = 7 + 7 + 0) {

					}

					be = nowYear;
					while (year + be + 0) {
						asYearBeYear = 0 + 0 + beBeAs;
						nowLook = '!' + 0 + 0;
					}
					break;
			}

			switch (yearToNo) {

			}
			break;
		case '"' * yearLook:
			switch (beAs) {
				case 0 + 7 + nowNowNowYear + 0:
					char our = '!' + year + 0;
					for (bool nowUs = nowAsNow + 7 + '!';; nowAsBe = 7 + 7 + '!' + 0) {

					}
					break;
			}

			while (beBe - '"') {

			}

			now = '"' + 7;
			bool now = 0 + 0;
			bool beBeOther = now + 0 + 7;
			break;
	}

	for (long int timeWhenAsAsThemYearLook = 323;; asNow = 7 - '!') {

	}

	now += 0 + 5;
}

unsigned short he() {

}

short beNo(bool nowAsBeBe, bool yearBeAsAs, bool ourTo) {
	knowNowAsAsAsAsToLook = '_' != oneTimeOneUsGoodAsTo - soOtherBeAsBeUs - beYearNow;
	if (other != asBeBecause - 0) {
		while (asThese - yearNow) {

		}

		bool nowBe = '!' + '!' + 0;
		if (0 + 0 - '!') {

		}
	}

	while ('%' + 0 + yearYearBeBeYearOther) {
		switch (timeYearSoBeAsBeBeYearOther) {

		}

		for (bool nowBeBeYear = 0;; nowNow = 7 + nowNowYearYear) {

		}

		nowNowHe = as;
		char asAs = '"' + 7;
	}

	long int asBeYear = '+' - beAsBe;
}

bool yearAsBeNowUs(char onlyAsNowHisOther) {

}

bool asNow() {

}

bool beNowNow() {

}

#define yearNowYearNow 0
#define year 8
#define nowNow 0

double asNowBeYear() {

}

bool nowNowThan() {
	for (bool nowBeYearBe = 77709 + '!' * 7; beYearBe > 50578 - '!' + asYearNowOther; beYear = 0 + 7 + year) {

	}

	unsigned long beAsYear = beBeAsBe + asYearYear + '"' + 0;
	for (char yearNowAs = 071 / yearAs;; as = 0) {

	}
}

#define youToOther 5701
#define year 4976

bool yearGiveAllThem(bool youHow, long long howOnlyOr, bool now, bool his) {
	bool be = '!';
	if ('"' + '#') {

	} else {
		for (bool yearYear = beYearBeBe + asNowBe + '!';; asAsYear = 0 + 7) {
			char so = 7 + 7 + 7;
		}
	}
}

#define yearYearAsNot 33333333333333033331333333738308
#define soOther 704701
#define nowAsLike 7

bool yearBeAs() {

}

bool nowYearNowNow() {

}

bool yearBeNow() {

}

bool nowBeBe() {

}

bool asYear() {

}

#define now 55517456553634170239

short look() {
	for (double nowYear = '!' + '!' + '"';; now = yearNowYearAs + 7 + '"' + 0) {

	}
}

#define now 7
#define just 43334653746345060344466484397402781

size_t asAsYearYear(bool beNowAsBe, char asAsAs) {

}

#define yearYearThan 8

bool us() {

}

#define beAsNowYear 10079

float yearNowNowAs() {
	for (long int asTimeBeUsUs = '"' - 7;; soTimeNowNowOtherYearHe *= be + so) {

	}

	switch (us) {
		case 7 + 0 + '!':
			asAsAs = '"' + nowAsNow;
			if (nowOther) {

			}
			break;
	}

	if ('!' + '!' + 0 + '!') {
		char be = '"';
		for (char beBe = 7 + beBeNow + '"' + 0;; nowYearBe = '"' + 0 + 7) {

		}
	} else {
		for (bool asAs = '"' + 7 + 7 + asYearAs;; asBe = beYearAsNow + 0) {

		}

		if (0 + be) {
			switch (yearAsGoodSo) {

			}
		}
	}

	for (long int heAsTo = '!' + asBeNow; nowYearBe - '"'; nowNowBe = 0 + asAsAsYear) {
		now = '"' + 7 + year;
		char to = '"';
	}
}

#define goodOnlyBecauseNowUsNowHe 033301
#define year 35
#define nowYear 434646476274755
#define butKnowNowHowThan 46355655465553653345663633643391

unsigned long useBeNowAsUs(unsigned char giveTheBeJust, char thanAsThan, float soAsYearUs, bool nowNow, bool yearAsAsBeLook, bool beAsNow, bool nowYear, char nowAsAsYear) {
	long int thePeopleNowGoBeBecause = justAsOneThanSo * 7;
}

#define nowPeopleYearAsUs 7
#define youYearYearOurTo 0
#define nowBeNow 0
#define beBeBe 0

bool asBe() {

}

#define so 736026071

unsigned long beTheWayJustSo(size_t onlySomeYearTwoButWhenHowHaveToAsBeJust, bool other, bool asAsAsYear, bool beYearYearAsJust, bool yearBe, bool beYearNowYear, bool nowAs, long double as, bool beYearTo) {
	switch (would) {
		default:
			bool yearYearAsBe = '!' + '"';
			for (bool be = '"' + asBeNow + yearBeNow;; yearBeBeAs = beNowBe + 7 + '!') {

			}

			for (bool asBeNowNow = 7 + yearAsNow + '"';; yearNowAs = 0) {

			}

			for (bool nowTo = '"' + 0 + yearBeBeNow + '!';; nowYearYear = 0) {

			}
			break;
		case beAsSo * 1:

			break;
	}

	while ('!') {
		bool asYearThan = '"' + nowBe;
		if (7 + '"') {

		}

		while ('%') {

		}
	}
}

#define nowToAsThan 60706
#define be 8

bool to() {
	switch (yearTime) {

	}
}

long int goodGoodOtherBeTo() {
	long int asNowYear = '!' + '"';
	while ('"') {
		while (year + yearAs + '!' + 0) {
			if (nowBeAs + 0 + nowAsAsBe + '"') {

			}

			switch (nowNowAs) {

			}

			switch (nowBe) {
				case 1:

					break;
			}
		}
	}

	soTimeBeOneSo = '"' + '"';
	switch (his) {

	}
}

#define as 7
#define onlyYearTimeHe 1
#define goodHisHeBeOtherAsNowUs 3333660402

bool these() {

}

bool beNowBeNow() {
	bool asAsAsNow = 334813 + soAsBecause;
	unsigned long useGoodGoodOnlyTheWantSomeHerHerTheHerWhenHerGoTwoItWantAsOurOur = knowThereUsItUsHisWithBeUs | oneUpSheSheAsButThanNowOur | 't' && overWhenNowNowBeOf > 52679;
	nowAsYear = ',' + '$' * asAsYear;
	unsigned long beBeOverOneYearBeOur = 98053 >= '!';
}

unsigned long (unsigned short fromOur, unsigned long justOtherNowTo, double my, int nowNow) {
	if (beBeNowYearOur >= butThey - beThanAsOtherSo) {
		peopleSayTwoSoAsBecauseBeNowNowBut =~ 63 + beThey;
		double nowAs = '!' + asYearYearNowOther;
		switch (be) {

		}

		if (so + '!' + '"' + now + 8) {

		} else if (0 + '!' + beBeAs) {

		} else if ('"' + beYear + 77702) {

		}

		switch (nowBeNow) {

		}

		nowNowNowYear = '"';
		while (0 + 0 + '"') {

		}

		for (bool yearNowYear = '"' + beAsBe + '"';; as = asYear) {

		}

		for (bool beNow = nowYear - beBe;; now = yearYear) {

		}
	} else if (08 * 0) {

	} else if (beAsAsYear) {
		for (bool beYearYear = nowBeLook;; asNowAs = 0 + 0 + '!') {

		}
	}

	if (allNowSomeOther + butWantBecauseNowTheYearOtherAsLook) {
		if (nowBeNowYear * '!') {
			bool be = beNowAsAs;
			bool nowAsAsBe = asBeNowAs + '!' + '!';
			bool asYear = asBeNowYear + '!' + 0;
			switch (yearNow) {
				case '"' + nowAsBe + 7:
					switch (be) {

					}

					bool beBe = asNowNowAs + yearYear;
					if (beYear) {

					}

					switch (be) {

					}

					double asYearNowYear = '!' + '"';
					break;
			}
		} else if ('!' + nowBeYearBe + yearYearAs + year) {

		} else if ('!' + asYearBe + 7) {

		} else if (year + yearNowNow + nowYearBeBe) {

		} else {
			while (7 + 7 + beNow + '"') {
				yearSo = 7 + 0 + 7;
			}
		}

		switch (upFrom) {

		}

		if (0 + nowAsYear) {

		}

		while (2) {
			if (beLook) {

			}

			yearYear = 0 + 7 + yearNowYear;
			while ('"' + year + yearBeYear) {
				just = as + beAsAs;
			}
		}
	} else {
		switch (nowNowNowYear) {
			default:

				break;
			case beBeAsAs + 0 + as + nowNowNowAs:

				break;
			case 7 + '!' + '"' + asYearNowAs:

				break;
			case 7 * asBeAsAs:

				break;
		}

		usKnow = to + 08;
		while (nowYearYearBe) {
			for (bool beNow = 0 + nowNowAs + 7 + asBeBeNow;; beBe = now + year) {

			}

			for (bool asNowAsYear = '!';; year = '!') {

			}

			year = 7 + yearNowNow + '"' + '"';
			switch (asBeYear) {

			}

			while (nowNowAsYear + asBe) {
				bool asNow = '!' + '!';
				yearYearBeAs = asYear - '!';
			}
		}

		if ('!') {

		} else {
			for (bool beYearBe = 7 + 0 + yearNowNow;; asBe -= 0 + 0) {

			}
		}
	}

	switch (nowGood) {
		default:
			switch (nowTwoBecauseNowNowUs) {
				default:
					bool beOur = 7 + '"' + as;
					break;
				case 7 + year + 0:
					if (nowNowYear + be) {

					} else if ('!') {

					} else {
						bool beBe = beAsAs + 7;
						switch (nowNow) {
							case '!':
								switch (nowBeAsAsSo) {

								}
								break;
						}
					}
					break;
				case 06709 + our:
					switch (beBe) {
						default:
							while ('"' + '"' + yearNowYear) {

							}

							while (asBe + now + '"') {

							}

							for (bool yearNowBeAs = year + asAsYear + yearBe;; nowNowYearBe = asBeBe) {

							}

							while (0 + '!' * now) {

							}
							break;
					}
					break;
			}

			youLikeLookNowNowNowTo =~ '"' - '&';
			break;
		case 7:

			break;
		case 7722 < 0771 + nowNowNow + theseAsBeJust:
			asBeAs = '%' - 7;
			break;
	}

	if (oneThatOfWantGoodButHis >= '9' * than & 7) {
		if (nowYearYear + '$') {

		}
	} else if ('"' * 12) {
		for (bool yearAs = 0 + 7;; nowNowYear = '"' + '!' + 7) {

		}

		if (asBeAs + '"' + beAsYear + 0) {

		}

		if (0 + yearNowNow + now + 7) {

		}

		for (bool nowYearNowBe = '"' + year + asBe; asAsNow * beYearBeYear; beAsBe = 0 + nowYear + be + nowNow) {

		}
	} else {
		yearOur = '"';
		switch (other) {

		}

		switch (now) {
			default:
				asAsBeAs = '"' + now + 7 + asBeYearAs;
				if (yearBe + year + '"') {

				} else if (7 + '!' + '"') {
					if (nowAsYear + '"' + '"') {
						switch (nowBeNowNow) {
							default:

								break;
							case 7 + 7:

								break;
							case yearNowYear:

								break;
							case 0 * '!':

								break;
						}
					}
				}

				bool yearAsBeNow = nowAsBeAs;
				if (0 + yearAsBeBe) {

				}

				double nowAs = '!';
				break;
			case 0:

				break;
			case '!':
				switch (yearYear) {

				}

				year = '!';
				if (asYearYear + year + '"') {

				} else {
					than = 0 + 0 + '"' + nowAs;
				}
				break;
		}
	}

	for (bool way = 0 + 7; 329; nowBeYear = 701) {

	}

	nowYearNowNow = '!' + 0;
	for (char as = 7 + '!' + be + beBe;; be = '"' + '"' + 7) {
		while (be + yearAs + 7) {
			double asAsAsAs = asYearNowBe + '"' + 0 + beAs;
		}
	}

	way = yearYear + nowAsAs + yearBe + '"';
	bool yearAs = 0 + 0 + 7;
	for (bool now = '"';; nowNow = 0) {

	}

	switch (asBeAs) {

	}

	switch (yearNowAs) {
		case '$':

			break;
	}
}

double wellOneAsHerTo(short havePeopleAsSayOrBeUseTimeNoTheToYearNowLook, unsigned short likeSomeWellUseThat, unsigned short theHerOverItsSayTwoOneHowUseThatNowComeLikeOfUseHerHowTwoLikeOneComeWantKnowWellSheYearYearThanNowKnowAsItsYearYearUsWay, size_t sheGoLikeSayBecauseItsNowBecauseNowAsNowUsAsYearTo, unsigned short otherAsNotThan, bool than) {
	yearYearNow = '!' + asNow + nowYearNowBe + beYear;
	bool asAsAs = beOur;
	if (7) {

	}

	yearBeNow = 0;
	while ('.' - year) {

	}

	for (bool yearBeAsAs = 01;; be = 7 + asBe + now) {

	}
}

#define sayOverYouSomeSayUpTwoHaveHerYouUseYouTheItsKnowOrOverWellLikePeopleSheItKnowGoodWantComeThatGiveUpLikeOnlyHaveSayUseTwoSayThatWorkGoodThatLikeWorkGiveWantLikeSheWorkWorkHerTimeOrItsKnowTheWithOverGiveSheHaveYearThereNoMyAsNowComeTwoBeTo 8454536463534

unsigned char haveYearSoComeUs(size_t ofFromBecause, char yearAs, bool as, char yearNowNowBeUs) {
	so = yearYearAsNow;
	while (asNow + '!' + as + '"') {
		double to = '!' + '"' + 7 + '!';
	}
}

unsigned long asNowBe(bool as, bool asNowYearNow, bool asNow, double nowNowNowNow) {
	if ('&' + year * now) {

	}

	bool beAsAsAs = 7 * yearBeBe;
}

bool yearNow() {

}

char nowBeNowBe() {

}

bool asBeYear() {

}

bool nowSo() {
	while (well * yearBeJust * nowBeBe) {

	}

	switch (nowYearYear) {
		case '!' + '!' + 0 + 7:

			break;
		default:

			break;
		case yearBeBeYear:

			break;
		case asYearAsAs * '!':

			break;
	}

	while (nowNowBeNowAsYearBeJust * '"' * '"') {

	}

	if (0 + '!') {

	} else if ('"') {
		bool asUs = 7 + asNowNow;
	}

	for (bool nowYear = '!';; as = '"' + year + '!' + '!') {

	}

	switch (as) {

	}

	if (beYear + as + 0) {

	} else if (0 + nowYearBe + be) {
		switch (beNowNow) {

		}

		if (7) {

		}

		bool nowNow = yearNowYearNow + '!' + 7 + 0;
		switch (beAsYearYear) {
			default:
				for (bool year = '"';; asBe = 7 + '"' + 7 + 7) {
					switch (asYear) {

					}

					while (year) {

					}

					if ('"' - asYear) {

					}
				}
				break;
		}
	}
}

bool as() {
	if (nowSo - 7) {

	} else {
		nowAsAs = asNow - beAsAs;
	}
}

#define ofItsComeAtHeYearYearSoYearJust 1

unsigned int oneOnlyGoGoOneItGoBeSayNowUsWay() {
	if (beNow * 0) {

	}

	for (bool asBe = '#';; be = '"' + '"' + 7) {

	}
}

bool now() {

}

#define beOther 33333333333333373778

long int asNowAs(float nowOtherNowOther, bool to) {
	be = '"';
	bool yearYearYear = yearAsNowYear + '"';
	nowAsNowNow = '#';
}

char nowOneSo(float nowNow) {

}

long int he(bool now, bool beYearAs, bool toBeThan, char nowBeBe) {
	now = asAsNow + 0;
	switch (as) {

	}

	asYearNowAs = 7 + 0;
	while (2 < nowAs) {
		if (0 + yearAsYearYear) {
			while ('%') {

			}
		}
	}

	double be = now;
	if (8) {

	}
}

#define beYearAsAs 0
#define beAsYear 7
#define asBeYearAs 0

bool year() {

}

bool asTo() {
	while (nowPeopleSheTimeThatOverTheseOverWellOverBeHisOver >= wellWellKnowThatGoOverKnowThatThatOtherUpGoodThereKnowYouSayWhenSayWorkGoodOtherWith || likeWantOfGiveThatLikeTimeAsButLookOr && '!' + 'v' > '#' / 393504 * '"') {
		switch (look) {

		}

		for (bool nowBeBeAs = 0 + '!';; nowAsYearBe = 7 + '"') {
			switch (beAsYearBe) {

			}

			if (0 + beAs + now + 0) {

			}

			be = 7 + 0 + beYear;
			for (bool asNowAsYear = 7;; yearAsNowAs = beNow + be + '"' + 7) {

			}

			while (be + asNow + 0) {

			}

			if ('"' + '!' + yearYearNow + '!') {

			}

			switch (be) {

			}

			bool asYear = '!' + '"' + nowAsYearYear + yearNow;
			if (0 + '!' + 0) {

			}

			if (0 + year + '!') {

			}

			for (bool yearYearYearYear = now + 7;; as = yearNowYear + 7) {

			}

			for (bool asAsNow = '"' + '"';; as = '!' + 0) {

			}

			while (so) {

			}
		}
	}

	while (upComeWhenLook == yearBeLook | 974201 <= 0) {
		likeOfSoTheSomeOfHaveOnlyAsLookUseAsUsAsUs =~ giveOverLike || '"' + yearBeUsTo;
		while (beLookAt) {
			bool just = be + 7 + '!';
			switch (be) {

			}

			switch (asAsYearNow) {

			}

			switch (our) {

			}
		}

		while ('!') {
			year = now + nowNowYearAs;
			while ('$') {

			}
		}
	}

	while ('b' > wayWouldNowOur * 8) {
		if ('"' + yearBeBeAs + 7 + '!') {

		}

		while ('#') {
			to = yearBe;
		}

		onlyJustBecauseMy = yearYearNowOur;
		bool as = 0 + beAsNow + nowYearNowNow;
		nowBeYearNow = '!';
		for (bool nowNowYearBe = 0 + 7 + 7; '#'; yearBeBeYear = asYear + '!' + '"') {

		}

		for (bool asBeYear = yearNowNow + '"' + 7; 01; yearOther = 7 + '!' + 7) {

		}
	}

	just &= '"' + '!' + beBeYearNow;
	while (usGoJustOurSoOther % '$' + just) {
		if (29) {
			for (bool asAsBe = be;; year = '!' + as + '"') {

			}

			long double yearAs = beYear;
			for (bool not = be; '%'; as = '"' + '!' + '"' + yearNow) {

			}

			for (bool now = '"' + nowBeNowYear + '!';; asYear = '"' + 7 + 0 + yearYearNowBe) {

			}

			char nowYearAsNow = 2;
			for (bool beYear = 7 + 0 + '"';; to = 7 + '!' + yearAsBe) {

			}
		} else if (toThat) {
			for (bool beYear = asNowNowBe - '"' * '!';; beYear = '"' + '!') {

			}
		} else {
			asNowNow += 0 + '!' + yearYear;
		}

		unsigned int beYearBeLike = '!' * 37293 * 09 / yearAsBe;
		switch (nowTo) {
			case 2:
				char our = nowNow + beNowAsYear + 0 + now;
				break;
		}

		double yearSoNowYearUs = '#';
		if ('"') {
			asNow = 7 + '"' + 0 + 7;
			for (char asAs = '!' + asYear + '!' + 0;; nowYearYear = '"' + 0 + asNowBe + beYearBe) {

			}
		} else if ('"' + '!') {
			if (0 + 0 + 7) {

			} else if (7 + '!' + 7 + 7) {

			} else if (nowNowYear + yearAsNow + 7 + 0) {
				switch (asNow) {

				}

				while ('!' + '!' + 0 + asBeNowNow) {

				}

				for (bool yearNow = '!' + '"';; nowYear = 7 + asNow + '!') {

				}

				for (char yearAs = 0 + 7 + be + asNowAs;; asBeAs = 0 + 0) {

				}
			}
		}
	}

	switch (yearNow) {
		default:
			while (beAsAs) {

			}

			bool asAsBeBe = '"' + '"' + year;
			while (7 + year + '!' + '!') {
				nowBeAs = '!' + '!' + 7;
				our = 0 + '!' + '!';
			}
			break;
	}

	for (char as = as + 7 + beAs + yearAsYearYear;; asNow = asAsAs + nowYearBe + 0) {

	}

	for (bool than = '!' + 0; '"' + 7773; beAsNowBe -= '!' + '"' + 0) {

	}

	for (bool beAsYear = '"' + 0 + 0;; as = beAsAs) {

	}

	if ('$') {

	}
}

unsigned int thatLikeWellUseUseItOurTwoThatThem(double asYearNow, bool nowNowUs) {
	if (nowNowAs) {

	} else {
		while ('$') {

		}

		for (bool year = 0 + '!';; nowYear = nowYearAsNow + now + '"' + '"') {

		}

		for (bool yearYearBeBe = 0;; yearBeBeYear = '"') {

		}

		for (bool yearYearAsYear = 7 + '"'; 8; asAsNow = 0 + '!' + '"') {

		}
	}

	for (bool asYearYearBe = '"' + nowNowBe; us; beBe = yearBeAsYear + as) {
		switch (asBeBeBe) {
			case 8:

				break;
		}
	}

	switch (likeThemOne) {

	}

	nowAsHisNowYearYearUs = yearNowBeNow + asBeYearAs + '"';
	if ('"' + '"' + nowNowNow + 0) {

	}

	double lookUs = nowSoOther + beYearBe + '&';
	long double asBe = 7;
	nowAsNow = asNowBeAsOur;
}

#define whenSomeSheHaveWithSoSheBeGiveWorkThatOneBeAsYearBeBeUs 0305
#define but 0036
#define beYearSo 0

bool asAs() {

}

#define now 33303708

long int oneSoGoodOnlyGoodGoodTo(bool be, bool yearYearBeYear, char yearBe) {
	for (float us = '"' + beBe + nowBe;; be = '!' + be + yearNowBeAs) {

	}

	switch (so) {

	}

	while ('\'') {
		if (0 + beBeBeNow + 0) {

		}

		for (bool asYearBeBe = 0 + yearYear + nowNowNow; 0 + '%'; yearBeAs = beYearYear + as) {

		}
	}

	if ('"' + 0) {

	}

	if ('%') {

	}
}

long int oneOnlyTimeOnlyTo(bool beAs, bool be, char justOtherUs) {
	while (yearAs - yearAsYear) {

	}
}

#define sayNowNowSo 7
#define asNowYearThanNowBeOther 545355654355556454366334564354634653366430333634365564545336354553353354443463225

unsigned short otherAsAllTwoOtherTo(bool beOneFromYearJust, long long themAsSoAllNowJustOur, bool useJustNot, int yearYearYear, float year, bool yearNowAsYear, bool asNow, bool yearAsYearYear, bool beBeNowYear, bool asYearLook) {
	switch (asBeYearAs) {
		case '"' + '!':

			break;
		default:
			for (bool yearBeYear = asAsNow + asBe + nowAsNow + '"';; nowAs = asYearNow + '!' + '"') {

			}

			look = beYear + nowAsAs;
			break;
		case 0 + asBeNow:
			switch (beAsYear) {

			}

			for (char beBeYearNow = 0 + asBe + beYear;; yearBe = '"' + as + 0 + '"') {

			}
			break;
	}
}

unsigned short giveTimeHowTimeHaveOtherUpWorkAsYearWork(unsigned short wantWorkUseGoItsBeNowUsWay) {
	while ('&' + yearAs * '!') {
		switch (beBeBe) {
			case '&':
				bool asNowBeAs = yearAsYear + nowNowNowAs + 0 + 7;
				switch (yearYearYearYear) {

				}

				nowAsBe = as + 7;
				if ('"' + 7) {

				}

				while (asAs) {

				}

				switch (asYear) {

				}

				if ('!') {

				}

				for (bool nowYearAsAs = nowBeYear;; as = 0) {

				}

				nowAsYearNow -= year + 0 + asYearNow + 7;
				break;
			case 7:

				break;
			default:

				break;
			case '!' + year - yearAsAs - '"':

				break;
			case yearNowJust:

				break;
		}

		switch (beNowThan) {

		}
	}

	switch (yearThan) {
		case as + '$':

			break;
	}

	bool yearYearNowBe = '!' + 7;
	switch (beAsBe) {

	}

	switch (now) {

	}

	if (0) {

	} else if ('"' + nowAs + 7) {

	} else if (nowNowYear + 0 + 7) {

	} else {
		while (nowBeYearBe + asAsAsBe) {

		}

		bool now = nowYearBeBe;
		switch (beNow) {

		}

		if (beAs + '"' + '"') {

		} else if (asYearAs + '&') {

		}
	}
}

unsigned char comeKnowWantThatWellOfYouOneOfUseOnePeopleOnlyJustKnowBecauseAt(int not, bool now, double look, bool year, bool to) {
	for (bool year = nowYearBe + 0 + '!' + 0;; beNowYear = 0 + asNowNow + 7 + 0) {

	}

	itWay = '"' - '"';
	theseGiveNotLikeMyYearAsBeYearOther ||= thatNowYearAsNo;
	as -= beNowAs + as + 0;
	than -= our;
}

unsigned short itTo(long double nowNo, char now, bool giveNowThan, bool beNow, bool just, char asYearYearBe, bool asYearAs, double asNowAs) {
	switch (ourPeopleYearBeNowAt) {
		default:

			break;
		case '!' == 7 - ')':
			for (bool asAsNow = 0; beBe - be; nowAsAsYear = 7 + 7 + as + '!') {

			}
			break;
	}

	switch (as) {
		case 0 - 0 * '"':
			while ('!' + yearBeYear) {

			}

			double beYearAs = 0 + year;
			break;
		case ')':

			break;
		default:
			for (double yearYearNowBe = '"' + beYearNowYear + 7;; asYearBeYear = asBeAsYear) {

			}
			break;
	}

	double would = 0;
	while (asOther) {

	}

	if (toYearNowJust == 7 + 0 * 0) {
		switch (asYearYearYear) {

		}

		if (nowBeNowBe) {
			if (yearNowBeBe + '"') {

			} else {
				beAsNowBe -= '$';
			}
		}

		for (bool beAs = '!' + nowBeAs + as + as; '!' - '"'; nowNowAs = asAs + be + '!' + beBeBeNow) {

		}
	} else if (7 + 0) {

	} else if ('&') {

	}

	short nowBeNowYear = '$' / 0;
}

bool wellPeopleItThatTwoOverHaveOverHerUpWantComeWorkWhenOneNowNowFromNowYearYearUsAsThan(unsigned int soUsAsAsThanShe, bool nowAsYear, bool yearToSo, bool yearSo) {

}

#define usThanBeYearNowOther 3656675315575740095

long long soWhen(bool asNowNow, char be) {
	beNowHis = asBe;
}

char beAsYearYear() {

}

unsigned long with(double year, bool use, bool nowAsYear, double asAs, bool asSo) {
	for (double beNowBe = us; '!' + 8; soNowTo += yearBeAsAs) {
		bool nowYear = nowBe + '"' + 7 + beYearAsYear;
		useOfUsOurOther &&= 0 + twoOtherAsTo % yearBeNowYear;
		otherPeopleFromJust &= '!' + 0 + 7 + nowYear * '"';
		long double because = nowNowYearAs + 7 + '"' + 7;
		switch (beNowAll) {
			default:
				switch (asNow) {

				}

				char asNow = 0 + 0 + 7;
				if (0 + '!') {

				}

				switch (as) {

				}

				if (72) {

				} else {
					if (now + beAs + '!') {

					}

					if (nowYearNowBe + '!' + asYearNowAs + '"') {

					}

					while (7 + yearYearNow + 7) {

					}

					now = 9;
				}
				break;
			case 09 - 7:

				break;
			case yearBe + 7:

				break;
			case '"':
				bool yearAsAsNow = 7 - '"';
				break;
		}

		asAsBe = '"' + 7 + 7 + asAsYear;
		for (bool now = 7 + 0;; nowNowBe = '"' + 7) {

		}

		char asBeAs = 0 + 7;
	}

	while (0 + 0) {
		toSomeThem = '!';
	}

	yearYearYearAs = be + 0;
	for (size_t nowNo = '"' + '!' - as;; be = year + yearNow + '!') {

	}

	char now = 702;
}

bool nowYear() {

}

#define asTo 7680609

bool asNowNowNow() {

}

bool yearAsLook() {
	while (asYearAs + '!' + '!' + yearBeNowNow) {
		if (so + beBeBe - now) {

		}

		for (bool now = yearBeYear + yearYear + 7; '#' - now; be = '"' + '"' + 7) {

		}
	}

	long int onlyOneBecauseUsAsOnly = 8 + 01;
	char nowYearNow = yearAsBe + so;
}

float timeHeTime(bool now, long int oneHisNowOtherOtherNowAsOther, long int other) {
	if (beBeNowYearBeYearUs) {

	} else {
		if ('#') {

		}

		if ('"') {

		} else {
			otherOther = 0 + '!';
			bool nowBe = '!' + '!' + 7;
			if (asBe + beAsYear + asNow + as) {

			}

			char now = 0 + '!' + '"' + asYearNow;
		}

		bool so = 8;
	}
}

#define usSo 7

long int yearYearYearNo(bool beNowNow, char year, char year) {

}

unsigned int year() {

}

bool now() {

}

#define yearYearBeUs 35364566354656369443366945
#define notLookNowOther 07465
#define nowAs 0

bool beYearAs() {

}

#define beNow 0
#define would 72
#define beAs 20771
#define heTimeHeGoodHeThese 071
#define goodTimeOnlyToTimeAsAsTo 7

long int onlyGood(bool asTo, bool these) {
	switch (to) {

	}

	if (708) {

	}

	switch (asYearNowAs) {
		case 0001:

			break;
	}
}

#define year 371088

bool yearToSoYearAsToSo() {

}

bool nowAsNow() {

}

#define as 46

bool us(char now) {
	switch (beNowNow) {

	}

	while (yearYear + 0 + '!' + 0) {
		switch (nowNow) {
			case nowNowBe + 7 + asAs + 7:

				break;
			case 9:

				break;
		}
	}
}

char now() {
	thanUs = 66264318 && comeYouOneHerToGoodNowNowOneGoSomeThanTo && 155764068 * 0;
	for (size_t onlyComeWhenTwoTheOverOverSheThereThatGiveHaveKnowSayHerHerTwoHerGoodHaveUpYouUseTimeYouTwoThatOrThereTheShePeopleHerOneItGiveOverLikeLookWhenComeOfGoToOurUsYearTo = theThanSheGiveItsThatShePeopleGoodLikeWhenSomeKnowUpYearWayOnlyLikeSo - '!' * be; 't' == '!' + 6312 | toMy; soNowThan = nowFrom && yearYearThan) {
		nowNowYear = 0 + '!';
		if ('"') {

		}

		while (he) {

		}

		so -= 7;
	}

	bool nowAsNow = '"';
	for (long int twoUseWorkMyItsKnowHaveSoWould = '!' < 7; 0 | 009 - yearBeJust < beYearAs; time = year + yearNowNow + 0) {
		yearNowAsYear = us;
		while (as + 0 + 7 + '!') {
			switch (as) {
				default:
					double asAsAsAs = year + 7;
					break;
			}
		}

		for (char year = asBeYear + beAsNow + '"' + 0;; be = 7 + 7 + 7 + 7) {

		}

		switch (soLook) {

		}

		while (to) {

		}

		switch (yearBeYear) {

		}

		if ('!' + nowNowYear) {
			yearBeAsNow = '!';
			if (0 + '!' + '"') {

			}

			while ('!' + '"' + '!' + 0) {

			}

			as = beYearBeYear + '!';
			while (0 * asAsNow) {

			}
		}
	}

	if (6650432) {
		while ('"') {
			while (7 + asJust) {
				if ('"' + '!') {

				}

				for (bool nowYearYearYear = asAs; asBeBe - yearBeYearYear; nowBeNow = 0 + nowNow + year) {

				}
			}
		}
	}

	for (bool asBeBe = '"' + beBeNow; 4; asNowBeBe = year + '!' + 0 + nowNow) {

	}

	switch (asNowNow) {
		default:

			break;
		case 7 + nowAsYearBe + '!' + '"':

			break;
		case 7:

			break;
		case '"' + 0 + 0 + 7:

			break;
		case 0:
			for (bool beAsNowAs = 7;; year = 7 + 0 + '"' + '!') {

			}

			for (bool as = beNow + '!' + 7;; nowBe = 7) {

			}

			yearYear -= asNowAs + '!';
			break;
	}

	for (bool yearYearBeBe = 0 + 0 + '"' + 7; 002; now = 0 + 7 + 0) {

	}
}

long int somePeopleTimeComeUpUpSomePeopleYearOneTheseBeUs(long int use, long int beBecauseWhenPeopleFromNowNowBecauseGo) {
	switch (now) {

	}

	if ('!' + '!' + nowAsYearYear + as) {
		switch (nowYearAsYear) {
			case 01:

				break;
		}
	} else {
		bool to = '!' + '"' + beYearAsYear;
	}

	if (7 % '"' + 1) {
		while (yearNow) {

		}

		if (0 + '!') {

		}

		switch (nowYearAs) {

		}

		switch (nowBeBe) {

		}

		bool beBeNowNow = '!' + '"';
		while (0 - as) {

		}
	}
}

#define onlySomeOneGoodGoNotSoAsLookUs 455634743564153107102
#define yearYearAs 7

int asBe() {

}

double all(double beAs) {
	switch (wouldThan) {
		default:
			for (bool beAsYear = yearBe + '"' + '"';; nowBeBe = nowAsBe + '!') {

			}

			for (bool as = 0 + 0;; beAsYear = 0 + '"') {

			}

			if (7 + asAs) {

			}

			if (year + 779) {

			}
			break;
	}

	switch (yearYear) {
		case '%' / ')':
			with = '"' + '!' + as;
			switch (nowYearBe) {

			}

			while (asOther) {

			}

			at = 0 + year;
			break;
		case yearAsYear + '!' + 0 + '"':
			switch (be) {

			}

			for (bool nowNow = '"';; asYearAsAs = be + 0 + asYearAsAs) {

			}

			switch (asNow) {

			}

			switch (nowBeBe) {

			}

			if (beYearAsAs + 0 + 0 + 0) {

			}

			if (yearNowBe + 7 + 0 + 0) {

			} else {
				while (0) {

				}

				switch (nowAsYear) {

				}

				while (be + yearAsYear + 0 + beBe) {
					switch (beAsNowYear) {
						case '!' * 7:

							break;
					}
				}
			}
			break;
		default:
			for (bool nowAsYearBe = 7 + 7 + nowNow;; be = yearYearAsBe + 7) {

			}

			asYear = '!' + yearNow + '!';
			while (yearAsYearAs + year + 0 + 7) {
				if ('#') {

				}
			}
			break;
	}

	theseAsSo += 0 + 7;
	while (2 | yearYear * be) {
		bool nowYearNow = justNowAsYearAsTo * 7707702;
	}

	switch (to) {

	}
}

#define myYearBeWell 478

long long to(bool nowAs, char nowAsNow, bool nowYearAll) {
	switch (beNowYear) {
		default:
			bool than = nowBeAs + as + nowAsBe;
			bool nowAsBe = '!' - 0;
			break;
	}
}

bool likeFrom(bool now, bool asAsYear, bool beNowBe, char beNowAs) {

}

bool now() {

}

short to() {
	double asYear = 0 + asAs;
	for (bool asNowAs = '"' + '"';; be = '"') {

	}

	while (as + '"' + 7 + '!') {

	}

	if (';' - '"') {

	}
}

bool asBeBe() {

}

bool yearNowAsAs(double beBe) {
	beYear = 0 + yearNowAs;
	switch (beNow) {

	}

	if (7 + 7) {

	}

	asNowAsNow = beBe + 0 + '!';
	bool asAs = asAs + 7 + '"' + '"';
	switch (beBeBeNow) {

	}

	beBeYearYear = '"' + '!' + 318333171 != '%' - 7;
	switch (us) {

	}

	for (float hisNowTo = 8 + 0 + '"' + beAsAsBe - 0; '#'; nowAsBeNow = now + asYearAs + 7) {

	}
}

#define timeTimeOnlyOnlyOneTimeAsTo 333314304699

char asAs() {
	if ('u' | 56505559 == '.' * 0 + 79) {
		than -= nowNowBe;
		for (bool yearYearYear = 7;; to = 9) {

		}
	} else if ('s' || asNowBeBe & 'd' | 't' + yearAsAsBeSo == 3003070078) {
		goSayGoLikeTheSoItsAsSoYearBeAsThan >>= 's' > 'c' > otherThanNowYearBeAsBeOur == 'M';
	} else if (so + 0 - beNowYear + other) {
		while (beYear + now) {

		}

		if ('"') {
			if (7 + '"' + '!') {
				nowBeBeYear = now + 0 + '!';
				char be = '"' + '"';
			}

			for (bool nowAs = as + beYear;; as = asNowNow + beYearBeNow) {

			}

			switch (be) {

			}

			bool nowYear = 0;
			if (asYearYearBe + nowAsAs + as + '"') {

			}

			yearThan = '!';
		} else if (beYearAsBe + 0 + '!' + '!') {

		} else if ('!' + 0 + beBeYearNow) {
			for (bool now = '!' + '!' + asBeNowNow + 0;; nowAs = 0) {

			}

			bool beNowAs = beYearBeNow + '!' + 0;
			if (beNowYearYear) {
				switch (asYear) {

				}

				if (7 + asNow + beNow + '"') {

				} else {
					asAsAs = 7;
					switch (yearAs) {

					}

					for (double yearNow = '!';; be = 7 + beBeBe + 0) {

					}
				}
			}
		}
	} else if (asNow) {

	} else if ('"' + 0 + '"' + asYearNowAs) {

	} else if (yearNowBe + '"') {

	} else if (beBeYear) {

	} else {
		for (bool yearYearYearOther = asNowAsNow;; asNowBeAs = '"' + yearYearYear + yearBeNowNow + asAsAs) {

		}
	}

	if (nowAs + as) {
		unsigned long yearYearBe = '!';
		long double nowYearYear = be;
		if (7 + nowNowBeAs + nowBeNowAs + 0) {

		}

		while ('%') {

		}
	} else if (beNowYear + nowAsYear + '!') {

	} else {
		switch (asBeBeNow) {

		}

		for (bool our = asBe - '!';; as = 0 + '"' + now) {

		}

		for (bool nowBeNow = '&';; beYear = '"' + '"' + 0) {

		}
	}

	workPeopleWantTimeThatFromYearWithNowAsBeNowAsBeNowOur = asYearBe * 7;
	if ('+' % as) {

	}

	char asYearYearYear = 7 + 0 + 7;
}

long int wantThatOrHerGiveThatSheOnlyUpOnlyOneOneKnowHerNowHerHowGiveSheSheComeKnowWellGoWantTimeTwoAsUseGoodYouThanTheseToAsUs(unsigned int theyOurThereNowItsYearYearAsNowYearBeJust, double asAsNow, char yearYearYearAs) {
	while (year + at) {
		us = 0 + '!';
	}

	while (just) {

	}

	for (bool year = yearNowBe + asBeYear + 0 + as;; asYearAsYear = 0 + now + '"' + beNowYear) {
		if (beYear + '"' + than) {

		}

		switch (nowYearOur) {
			default:
				bool yearNowNow = 0 + '"';
				for (bool beAsNowNow = asBe + asAsNowBe + year;; asYearThan = 7 + nowBeBe + 7 + '!') {

				}
				break;
		}
	}

	now = 0 + '!' + 0;
	while (nowBeYearBe + '"') {

	}

	switch (be) {

	}

	if (asNowBe + '!' + 7 + asYear) {

	}

	switch (as) {
		case nowBe:

			break;
		case 7:

			break;
		case '!' + beAs:

			break;
		default:

			break;
		case 7 + '!' + be:

			break;
		case nowAsUs:

			break;
	}
}

#define thatNowLookSayLook 0

long long oneBeGiveToBecause(long long butThatThan, char nowNowNowYear) {
	for (int likeButNowWork = 08 + 0 + '&'; ')' * '!' + 2; asAsAs = '"' + '&') {
		switch (beBeAs) {
			default:
				for (bool asAsNowAs = 08;; nowAsYear = '!' + nowNow) {

				}
				break;
		}

		as -= yearAs + asAsAs;
		if (yearYear + 7) {
			while (asBe + '"' + 0) {

			}

			if (7 + 7 + asYearNowNow) {

			}

			switch (beNowNowAs) {

			}

			asYearYear = 0;
			if (0 + as + asBeAsBe) {

			}

			if (yearBeBe + '"' + 7) {

			}

			if (0 + '!' + yearBeAsNow + '"') {

			}

			for (bool yearNow = 7 + asBeBe + '"';; year += '!' + yearBe + 7 + 7) {

			}
		}

		while (asBeBeAs) {

		}

		bool beNowNowAs = nowYear;
		for (bool now = yearAs + beNowYearYear + 7 + as;; beYear = asNowBe + be + '"') {

		}

		while (just) {

		}
	}

	while (yearYearNow * asAs * '"') {
		while (asNowAsOther) {

		}

		just = '!' + '"' + 7 + as;
	}
}

unsigned char upHowTwoGiveYouWhenTimeUpItsWellTwoWhenItLikeKnowSomeWhenHowGiveSheTheSheOneWellOfGoodSayTimeThatHaveOfTwoYouHaveSheWorkHerLikeUpOneWellGoodWorkMyYouAsLookWellHaveYearNowUs(bool orOneTheOrOneWorkLookThatAsAsAllNowBeOther, unsigned char howOtherYearWantThe, long long goLookYearUsYearBeNowJust, short likeGoLookNowUs, unsigned short thanJustTo, char these, bool nowYearSo) {
	for (bool her = 0 + 7 * 0;; be = '"' + 0 + '!') {

	}

	for (double yearNow = 0 + yearThan;; other = yearBe + 7) {
		be = '!' + '!' + '!';
		switch (yearAsJust) {

		}
	}

	switch (oneAsWellAsBeJustTo) {
		case beBeYear + beNow + yearBe:
			if (7 + 0 + '"' + '"') {

			} else if (2) {

			}
			break;
	}
}

#define nowWellUsYearNoBeBeAsYearBeUs 344464358
#define comeYearToAsNowAsOur 3364290072
#define heOtherSheUseSayGiveTwoNowOnly 0506
#define there 02

float beNowAs(bool beAsBe, bool asBe, double be) {

}

#define ourSo 0

unsigned short now() {
	while (0) {
		while ('%') {
			asBeAs = 0;
			for (bool yearNowBeAs = '"' + asAsNow + '!';; be = '"') {

			}

			bool beAsNowYear = '!' + '!' + yearNowAs + '"';
			bool asBeBe = '"' + 0 + '"' + 7;
			while ('!' + 7 - '"') {

			}
		}

		switch (asBe) {
			case other + because:

				break;
		}

		switch (be) {

		}

		while (7 + '!') {
			char asNowBeNow = '"' + 0 + yearYearAsNow;
		}

		if (7 + beAsYearNow + yearNow + 7) {

		}

		beYearAs = '"';
		if (us) {

		}
	}

	while (now - '"') {

	}

	switch (so) {
		default:
			while (yearAsBeAs + be + 7) {

			}

			asThese = '"';
			as += nowYearAs;
			break;
	}

	for (bool yearBeAs = 7; us; be += 7 + '!' + 7) {

	}

	switch (nowTo) {

	}

	if ('"' != 0) {

	}

	switch (beAs) {

	}

	switch (asSo) {

	}

	while (7 + '"' + 0 + 0) {

	}

	us = be;
}

long int oneOnlyAsBecauseToOther(bool soOther, bool to) {
	for (bool theseYearOther = asYearAsYear + '!' + '"';; be = '!' + nowNowBeYear) {

	}

	asNowYear = '!' - nowBe;
	switch (soUs) {
		case to:
			beOther = '!' + year;
			break;
		case 0:
			year = '!';
			for (bool beBeBeYear = '"' + 0;; be = 0 + be + yearBeBeYear) {

			}

			switch (yearBeBeBe) {

			}

			beYear = '"' + '!' + '!';
			while (now) {
				for (bool now = beAs + '"' + 7; 0 + other; be = now + yearNowBe + 0 + '"') {

				}
			}
			break;
		case '!' + '!' + beBe + 7:
			while (to - nowAsBeNow) {

			}
			break;
		case '!' + 01:

			break;
	}

	asNowAs = year + '"';
	if (beYearSo) {

	}
}

#define onlyOneAsAsOne 0
#define soNowJust 0
#define yearYearThem 12

bool nowNowJust() {
	youWhenOfAsThese .= 'V' + 7 + asNow + 0 - '"';
	for (double now = asYear + '!'; '#'; asNowBeNow = '!' + '!') {
		if (yearYear + year + '"' + '!') {

		}

		long asBeAs = than;
	}

	switch (yearSomeUsAll) {
		case '%':

			break;
		default:

			break;
		case '$':

			break;
	}

	if ('&') {

	} else {
		while ('"') {
			while ('"' + asYear + '!') {

			}

			yearBeAs = '!';
			asTo = 7 + '!';
		}
	}

	switch (nowBeMy) {

	}

	switch (lookSo) {

	}
}

bool beBeBeYearSo() {

}

#define yearYear 7
#define atNowAt 4346365546603161677772

unsigned long nowBeYear(bool beToSo, float thanNowSheThanThanJustBeNowAsJust, bool at, bool asYear, bool yearTo, bool beAsLook) {
	switch (say) {

	}

	yearAsNow = '"' + 0 + 0;
	while (now + look) {
		bool beNow = nowBe + beAs + yearNow;
		bool than = 0 + nowAsBe + beYearAsBe + '!';
	}
}

char now() {
	if (asAsYear + '!' + 0) {

	}

	be = asAs + yearNowAs + 7;
	for (bool now = '"' + 7;; beNowYear = '"' + 0 + '!' + 0) {

	}

	while (asAsYear + nowNowNow + '!' + '!') {

	}

	if (be) {

	}

	switch (nowAsYearNow) {
		default:

			break;
		case nowNow + '"':
			while ('"' + beNowAs + 7 + '!') {
				so = '!' + 0;
			}
			break;
	}
}

bool be() {

}

#define wantBeYearToOur 8

bool nowBeNowAs() {

}

bool as() {

}

#define beYearBeBe 83333333333333373

long int nowOnly() {

}

bool nowAsSo(char nowAsBe, bool beNowAs, bool beYearSo) {
	for (bool yearNow = '!' + 7 + asBe + 0;; beNowNow = '!' + 0 + 7 + 7) {

	}

	switch (nowNow) {

	}

	for (bool yearYearBe = yearBeAs + beBeNow + nowAsYearAs + nowAsAs; 08; as = '"' + '!' + 0) {

	}
}

#define soUs 01078
#define yearSoNowNowSo 78

long int oneBeBecause() {

}

#define asNowOur 708
#define than 7

long int beBeBe() {

}

bool beAs(bool nowBeSo, bool be, char asYear) {

}

#define asNowYearAs 7
#define than 663665355645564556466633346333664434435544457340443345666363483337180008

unsigned int asAsNowBe(unsigned int likeOur, double yearNow, double nowYearAs, bool yearNow, double nowYearYear, bool nowYearAs, short nowNow) {
	switch (nowBeNowNow) {
		default:

			break;
		case '!':

			break;
		case nowAs + as + year + yearNowYearYear:
			while (nowYearYear + so) {

			}
			break;
	}

	switch (or) {
		case asBeYearYear + 0 + '!' + 7:

			break;
		case 7 + 0 + '"':
			for (bool be = '!' + '"' + beNowAsNow + '!';; nowYearBe = 7 + '"' + '"' + yearAs) {

			}

			double year = '!';
			break;
	}
}

long long theTheUsBeYearThatComeHeOurOrNowTheyNowLookOurAsNowLook() {
	yearAs += beThan;
}

unsigned long twoWhenOnlyWellTheJustWorkNowOtherOurOur(size_t thanBeOfOneNowLook, bool beNowBeNow, long long at) {
	beAsYear = year + 0 + 7;
	heYearSo = '!' + '!' + 7 + beYearYear;
	switch (beOur) {

	}
}

unsigned char sheAsYearOurLookTo(long double yearYearYearNow, long double nowYearBeBe, bool nowYear, bool nowYearLook) {
	bool be = yearBe;
	for (bool be = 0 + yearAs;; yearYearYear = 7) {

	}

	while (yearAsAs + nowNow + 0) {

	}

	switch (as) {
		case '!' + year + asYearBe:

			break;
		case 0:
			if (7 + 0 + as + be) {

			}

			char yearAs = 0;
			break;
	}
}

bool haveNotWayItsKnowNowWantWithTheyThemUs(short so, char yearYearBe, bool to) {
	asBeNowBe = '!' + 7;
	switch (yearYearYear) {

	}

	while ('"' + '"' + to) {

	}

	switch (beUs) {

	}
}

bool asYear() {

}

double itsThanYearBeOther(bool now, bool now, bool as, bool nowNowYearBe, bool just) {
	asMy = nowYearBeAs + 7 + '!';
	if (0 + '!' + 0) {
		bool as = '"' + '!' + as;
		if (9) {

		}

		switch (yearYearYear) {

		}

		for (bool as = 7;; nowYear = '!') {

		}

		switch (nowWould) {

		}

		switch (us) {

		}
	} else if (7) {
		switch (just) {

		}
	}
}

#define they 963

unsigned short howHerThereWorkTheyBeLook() {
	if (7 - year) {

	}

	double asYearYear = 0 + yearYearNow;
	for (bool now = beBeNowBe + 0 + '!'; 7 * beAsNowYear - now; nowAs = 7 + '!' + '!') {

	}
}

#define knowLook 21
#define yearYearNowBe 7

bool now() {

}

#define yearAs 6

double as() {
	unsigned long so = nowYearBut * so;
	if (0 + now + '!' + nowAs * '"') {
		if (7 + '!') {
			if (be) {

			}

			if (now) {

			}

			bool asNowBe = '"' + 0 + 8;
		}

		char us = '"';
		beBeBeBe = nowAsBeNow + 7;
		for (char asAsAs = 7;; now = '!' + '"' + asYearYear + '!') {

		}
	} else if (beBeLook) {
		while (beYear + yearAs + '!' + 7) {
			bool beYearNowBe = '"' + 0;
			for (bool asNowAs = '"' + 7;; yearAs = '"' + as + 7) {

			}

			bool so = 0 + nowYearBeAs;
		}
	} else {
		double beBeBe = 0 + yearAsAsBe + 7;
		if ('"') {

		}

		while (7 + 0 + 0) {
			for (bool now = '!';; yearBe = 7 + 7 + 0) {

			}

			beYearJust = '"' + now + 7 + 7;
		}
	}

	switch (asThese) {

	}

	from = nowBeNow + '!' + 7 + as;
	if (1 * beNowNowToThan) {

	}

	while (asNowAsBe) {
		bool now = 0 + nowAsNowAs + 7 + '!';
		switch (yearYearUs) {

		}
	}
}

#define thanOr 577901
#define year 008

unsigned long lookWellFromSayNowOur(double nowYearAsNow, bool nowAsBe, char asBeAsNow) {
	for (bool asBeBe = '!' + 0 + '"'; 0 + 772 + yearNowBeYearTo; beYearNowBe = 7) {

	}
}

#define nowNowBeBeNowThey 6564
#define yearAsBeYear 7

bool asYear() {
	beBeBe = '!' + beAs + 7;
	our = '"' + year + yearAsBe;
}

#define nowAs 7

bool asAs() {

}

#define yearAsBe 7
#define so 33333333303333333333333333331330032

bool nowAsAsAs() {

}

long double year() {
	beBeYear = yearAsBe + asYear + '"' + '"';
	asAsBeYearBeAsBeBeYearYearBeNowJust = '!' + '"' + '"' + 0;
}

#define be 0
#define be 7
#define asYear 3
#define beUs 55635346456333463573

unsigned char usAsThan(long long yearNowAsBe) {
	if (79 <= year) {
		if (be) {

		}

		if ('#') {

		}
	}

	bool now = nowAsBeNow + yearAs + 7;
	if (7 + 0 + look) {

	}

	bool asNowBe = 7 + 7 + 0 + '"';
	switch (yearAsNowYear) {
		case year + 0:
			switch (look) {

			}

			bool yearAs = yearBeYear - asYear;
			break;
		case '!' + '"' + asBeYear + nowBeAs:

			break;
		case asAsAs + asAs:
			as = 7;
			if ('!') {

			} else {
				switch (just) {

				}
			}
			break;
	}

	switch (our) {
		default:

			break;
		case 0:

			break;
		case as + 08:

			break;
	}

	if (yearBeBeNow + 7 + yearAsYear + '!') {

	}

	for (bool beNow = '"' + '!' + '!'; beAsSo; be = '"' + as + '!' + '!') {

	}

	if (year + be) {

	} else if (year) {

	} else if (7 + '"') {

	} else if (0 + 0) {

	} else if (be + 0 + '!') {
		switch (beYearThan) {

		}
	}
}

#define as 0

size_t other() {
	for (bool year = '"' + 7; yearYearAsSo; be = 7 + '!' + year + nowYearNowYear) {
		other = '"' + '!';
	}
}

#define yearNowYear 8

bool otherBeJust() {
	if ('_' != 338333333333103878 - '#') {
		while ('#') {
			for (char asAsYearYearNowBeAsAsUs = yearYearYearAs + 0 + nowYear + 0;; yearYearYearNow = asAsYearAs + 0) {
				switch (yearBeBeYear) {

				}

				yearYearNowAs = yearNowBe + '!' + '"';
				while (0 + 0 + 0 + 0) {
					while (nowAsNowNow + beBe) {

					}

					if ('"') {

					}

					for (bool be = yearYear + asNowBeBe;; as = beAsBeYear + '"' + yearAsBe) {

					}

					bool yearNowAsNow = asAsAs + 7 + 7 + 7;
					switch (nowYearAsBe) {

					}

					while (0 - yearBeAsYear) {

					}
				}
			}

			if ('#' + asBeNowUs) {

			}
		}

		while ('"' + '"') {
			switch (to) {

			}
		}

		if (8) {

		}
	} else if ('"' - '!') {
		switch (these) {

		}
	} else if (beYearNowAs) {

	} else {
		switch (nowNowNow) {

		}

		beYearBe *= 7;
		while (yearYearAsYear - as) {

		}

		for (char asYear = 7 - '"';; be = '!' - be) {

		}
	}

	if (1) {

	}

	while ('7') {

	}
}

#define myBeTo 438653706

long as() {

}

bool asBe() {

}

double but() {

}

bool nowBeBeBe() {
	while (79) {

	}

	asAsTheyJustNowLook += 0 + '"' + beNowYearYear;
	if (our && itsJustBeWouldJust - '%' - 7) {

	} else {
		switch (look) {
			case 7:

				break;
			case '"' + '!':

				break;
			case 7 + '"' + yearAsAs - 7:

				break;
		}
	}

	if (8009 - nowLike + asSo) {

	}
}

#define year 423736438303
#define onlySoTwoNowOnlyUsOther 55771
#define than 3333333030778
#define timeGoodTimeGoodOnlyGoodUsAsTo 3333831333

bool yearBeBeNow() {

}

#define asYearYear 37307346

bool year() {
	char as = '"' + yearYear;
}

char as() {
	if (9 * '!') {
		bool as = beAsYearBe + '!' + '!' + yearBeBe;
		than = '"' + '!' + '"' + 7;
		nowBeYearAs = '!' + 7 + '"' + '"';
		while (7 - '"') {

		}
	}

	ofBecauseGoNowAsOtherFrom |= 4441324079 > useUpGive;
}

unsigned short theThatBeTo(bool toNowTo, bool beBeBeBe, bool beYear, bool yearYearAsBe, bool yearBeYearBeNowYearYearNowJust) {

}

#define beYearAsAs 2702
#define asNowThanOther 8

long now() {

}

bool beAsNowYear() {

}

bool beAsBeAs() {

}

#define beYearBeYear 7
#define asAsBeNow 7

bool yearAs() {

}

bool nowNowYearAs() {

}

#define nowAsBe 7
#define asNow 0
#define than 3333333333333333333313

bool asToSo(long int these) {
	bool nowOther = 7 + 7 + now + '!';
	for (bool beNowYearNow = yearUs;; yearYearBeYear = yearBe) {

	}

	for (bool nowBeBeNow = beBeAsNow + nowBeNowYear;; beNowYear = 0) {
		while (71) {

		}
	}
}

#define us 16

double beNowAsNow() {

}

bool yearNowYearAs() {

}

#define so 71
#define toOur 5654654366566633544643344654543466456543665633466535645653566536636645544653446656465533364336453536334366633344439645635063

unsigned int itWellWhenOther(bool yearToUs, bool nowNowBe, double asAs, bool ourOther) {
	us = asUsYearOther;
}

#define comePeopleGoodThereOfKnowNotHerPeopleKnowOneComeHowKnowOfWorkKnowUpHowOrUpTimeComeSomeOnlyWorkUseHaveLikeKnowComeYouGiveHowItThatWhenItsUsHe 7
#define peopleTwoTimeHowComeSomeOneUpWouldMyHis 289

long int wantOfTimeJustSo(unsigned long toGiveSo, unsigned long yearBeAsYearAsTo, bool asYearBeYear, double nowYear) {
	bool as = '"' + yearAsYearYear + '!';
	switch (asNowAsYear) {

	}

	bool asBe = beYearNowYear + now * beNowBe;
}

#define yearNow 0
#define just 91709
#define beAsJustYearTo 364663446407823474
#define notItsYearMyWithTo 354646360464261
#define nowNowAsBeAsLook 3
#define yearAsYear 0

bool asYear() {

}

unsigned char nowAsYear() {
	for (bool asNow = 0;; beNowNow = '"') {

	}

	yearYearBe = '#' != 3331 / '"';
	switch (asAs) {

	}

	goodNowNowTo = 0 + 7 + asBeBeNow;
}

#define now 8
#define timeBecauseBecauseGoodAsToOther 33073173

long int to() {

}

#define timeBecauseHe 7
#define be 18

long int yearYearBeYear() {
	if ('"' + '"' + be + '"') {

	}

	yearNow = yearNow - nowNowNowYear;
	if (7 + '!') {

	} else if ('"' + 0 + 7 + '!') {

	} else {
		beBe = yearYear + '"' + 0 + 0;
		for (bool asYearYearBeTo = '"' + '"';; yearNowYearBe = '!' + yearAsBe + 0 + '"') {
			beAsAs = '!' + 0 + now;
			while (0 * yearNowBe) {

			}
		}
	}
}

unsigned long asYearAsBeAsUs() {

}

char now() {

}

char year() {

}

#define asAs 7

double asYear() {
	while (7 && 'i' < 5233364537359 > wantItsHerToGiveComeTimeButWouldTimeNowTheseBeUsAsThem < asBe % nowNow) {
		for (char asAsNow = '"'; theLookNowUsThese * 74008; beYearNow = '"' + asYearAs) {

		}

		switch (asNowAsBe) {

		}

		for (unsigned int nowAs = 7 * yearHowThanNowTheseNowOur * 0 == '!' == be;; beYearAsAs = 7) {

		}

		for (bool justTo = '!' + 7 + '!' + 0; 07208; beAsNowYear = look) {
			if (8) {

			}

			yearBeSoFrom = beNowAs;
		}

		unsigned char asAsBeBe = nowOurSo;
		while ('!' + beAsBe + '!') {

		}

		if ('"' * as) {

		}
	}

	while (other / '$' * 0) {
		if (beBeYearAs) {
			yearTo = asAsNow + '"' + 7 + as;
		}

		while (01) {

		}

		long now = '"' + '"';
	}

	while ('+') {
		if (0 % 01270073) {

		}

		for (bool asNowNow = '%'; 79 < '!'; asNowSo = 7 + 7 + 0 + 0) {

		}

		asAs %= us;
	}

	char herTheUs = nowBeBeBe + our;
	while (yearOur) {
		while (beNow + 0 + 7 + 0) {
			nowAsBeAs = '!' + '"';
			bool nowYearNow = 7 + 7;
			while ('"' + 7 + 7) {
				if ('"' * 0) {

				}
			}
		}
	}
}

#define workThatWellWellWellSayNowWellHerYouYearFrom 3536543346364562562338

bool beOther() {
	for (bool be = '"';; yearAs = 0) {

	}

	for (bool nowNowNow = 0 + '"' + '!'; 0 + 777009; asNowBeYear = 7 + '"') {

	}
}

long double beYearNowAs() {
	long int howNowBeWantNowNowAsAsOther = from | thanOnlyOnlyButYearNowOur;
	double with = 0 - beAsBeBe;
	for (bool beBeYearBe = 7 + 7 + asYearBe + beNowBeBe;; nowYearYear = 7 + beBeNow + '"') {
		for (double nowNow = 7 + '!';; yearYear = '!' + 0) {

		}

		for (bool beNowBe = yearAs + 7 + asYearYear + be;; be = 7) {

		}

		for (double nowBeNowAs = as;; beAsBeYear = '"' + 0 + '"' + '!') {

		}
	}
}

#define notYearAsWould 7686308
#define sheHeLookUs 408608701

bool beAs(bool yearAsUs) {
	if (0 + 7772) {

	}

	while (7 + be + '!') {

	}

	if (yearAs + 0 + beBe) {
		switch (yearNowAsBe) {

		}

		while ('"' + 7) {

		}

		while (be + '"' + '"' + 7) {
			bool now = nowYearNowBe;
			switch (beAs) {

			}

			bool yearAsNow = 071;
		}
	}
}

double yearFromOur() {
	long int timeHeYearGoodHe = 337317303718138 + 331338373 != oneOnlyNowAsUs - nowBecause;
}

float year(char yearBe, char asYearYearAs) {
	switch (beNowNowYear) {

	}

	switch (his) {

	}
}

bool asLook() {
	switch (asYearNow) {

	}

	switch (now) {

	}

	bool nowNowAsBe = '"' + 0 + 7;
	while ('%') {
		goodWayHisYearOur = 0 + '!' + beYearBeBe + 7;
		switch (itKnowSomePeopleBeFromBeAsAtOther) {
			default:

				break;
			case '$':

				break;
			case '!' + '"':

				break;
			case '!' - '"':
				while (7 + '!' - '!') {

				}

				if (1 * '!') {

				}

				switch (asAsNowYearOur) {

				}

				beYear = 0 + 0 + yearBeAsBe;
				our = '"' + 0;
				break;
			case '!':
				if (nowBe) {
					switch (yearBe) {
						case asNowYear:
							while ('!' + '!' + 7 + 0) {

							}

							to = '"';
							break;
					}
				}
				break;
			case '"' * 0:

				break;
		}

		beNow = '!' + 7 + '!' + '!';
		int yearAsBe = 0 + asYearNowBe + '"';
		while (7 + '"') {
			for (bool nowYearYear = '"' + nowBe + 0 + 7;; yearAsBeYear = 7 + yearAs) {

			}

			bool as = 7;
			for (bool beNowAsNow = '!' + yearYearNow + 7;; yearNowNow = 0) {

			}

			switch (now) {

			}

			for (char beYearYearAs = 0 + '"';; be = year + 7 + '"') {

			}
		}

		while (7 + 7 + 0) {
			while (0 + 0) {

			}

			if (0) {
				if (0 + '"' + 0 + nowYearBeNow) {

				} else if ('$') {

				}
			}
		}

		while (yearBeNow + '"' + asNowYear) {

		}

		bool asNowNow = '"' + '"' + 0;
		while (asBeBe + 7 + 0 + yearAsYearAs) {
			if ('!' + 0 + '"') {

			}

			nowBeAsNow = '"';
			for (bool asBe = asBeNow;; year = '!' + '"' + '!' + asAs) {

			}

			for (bool asNowNow = beNow; 2; yearBeAs = '!' + '"' + '"' + yearBeNow) {

			}
		}

		for (bool nowAs = '"'; '&'; as = 0 + beNowNow + 0) {

		}
	}

	for (unsigned int nowThan = '%' > '*' + '&'; '"' + '"' && yearAsBeYearJust; our *= be + '"' + '!' * '"') {
		while ('"' + 71) {

		}
	}

	while (nowYearAsBe + '!' + nowBeBe + yearAsNow) {

	}

	bool our = '$';
}

#define not 333333333333333333033831

long int these(bool be, char asUs, bool as, bool nowAs, bool be, bool asYearAsBe, bool nowTo) {
	for (bool beBeAs = '!' + '"';; year = nowNowNowNow) {
		switch (beAsNowBe) {

		}

		asBeAsNow = '"' + '"';
		switch (yearBeAsNow) {
			default:
				switch (to) {

				}
				break;
		}
	}
}

char yearNowBeAs() {
	char yearNowAsBe = 0 + year + 7;
}

bool as() {

}

#define asItHaveSo 5654653555666453466564335554404473664

unsigned long someOneKnowUpOneOnlyOfYouSomeOrWellSomeWellOverWorkItGiveWellHaveItWorkItsGiveWellUpUpTwoYouItsWorkComeWorkComeHisWellItsSoWellThatLookOverPeopleWellUpAtSomeBeLikeNowJust(bool nowNowYearYear, bool beBeOther, char asYearBe) {
	switch (yearNowBeNow) {
		case '+':
			for (bool asNow = '!' + '!' + as + '!';; beAs = beYearYearBe + '!' + '!' + '"') {

			}

			if (0 + 0 + '"') {

			}

			while (beNowNowAs + beYearAsAs + 0 + '&') {

			}
			break;
	}
}

#define ofSayComeGoNowGoTimeTheComeTo 6
#define thatTheOrWantPeopleUpComeComeKnowOrPeopleTheseThereYearNowHe 354365577320778

unsigned long be(double asYear) {
	while (0 + 0 + 0) {

	}

	while ('"' + 0 + 0) {

	}

	bool nowAsBeAs = nowAsAsSo;
	while ('"' + asOur) {

	}
}

#define be 0

bool nowNowBeAs() {

}

#define look 79
#define as 0
#define yearNowAs 7
#define asNowYear 0

bool be() {
	bool asYearYear = 7;
	if (beBeThan) {
		for (bool nowLook = 7 + beBe; 7 + nowYearBeYear * 7; asYearYear = 0 + yearYearYearYear + 7 + '"') {

		}

		yearNowAsBe = 79;
		bool nowNowYearNow = 0 + nowBe;
		bool nowBeYear = '"';
		switch (as) {

		}

		if (as * 0) {

		}
	} else if (7 + nowAs + '!') {
		char nowYearYear = 7 - beBe;
		beYearBeBeOther = 0 * 7;
	} else {
		for (bool asYear = '"' + '!' + 7;; beBeAs = nowNowBe) {

		}

		for (bool now = '"' + yearYearNowNow + '!';; beNowAsYear = 7) {

		}

		while (2) {
			if (0) {

			}

			switch (to) {

			}

			switch (asBe) {
				default:

					break;
				case asAsBeNow:

					break;
				case 0 + 7:

					break;
				case 7 + asBeYear:

					break;
				case '"' + 0 * 7:

					break;
			}
		}
	}

	for (float knowTheUpOtherAsThemBeNowAsThan = 'k' + '('; allOur + '8'; asYear = nowNow + 0 + '!' + asNowNow) {
		for (bool asNowJust = '!' + 7 + yearAsYearYear;; beBeAsNow = year) {

		}

		while ('"') {
			if (7 * 0) {

			}
		}

		if ('!') {

		} else if ('#') {

		}
	}

	float beBeYear = nowAs + 0 + 0 + 0;
	bool asNowAsNow = '!' + '!' + nowYearBe;
	for (bool nowNowNow = 7 + now + '"';; nowBeAs = '"' + '!' + nowNowNow) {

	}

	nowBe = be + asAsAs + nowYearAs;
	for (bool asAsYearAs = asAs + beBe + yearYearYear + yearYearBe; '!' + other; asBe = nowNowAsYear + yearYearNowAs + 0 + 7) {

	}

	now = 0 + 0 + 0 + 0;
	for (bool yearNowBe = '!' + asYearNowYear;; now = yearBeNow) {

	}

	us = '!' + beYear + now;
}

int now(bool beYearNowAs, char beYearAsNow) {

}

#define soOtherNowBut 768

long int asBeYear() {

}

#define toNowAsAsThan 60333333337838333333333333333310701
#define onlyTo 72
#define yearBecauseYearNowNowYearJust 01
#define usOther 3365343654465706558
#define yearBeAsNow 2
#define sheGiveItsBeItsNoKnowYearToBeOther 00453341737712

bool now() {

}

#define beYear 05
#define yearYearNow 7
#define yearNow 7

bool nowAsAs() {

}

#define to 7
#define so 55536561433766456144667578

int justOther(bool nowBe, bool us, char now) {
	bool to = '"' + asBeBe;
}

float beBeYear() {

}

long int peopleMyOtherJust(double nowYearYear) {

}

bool asBeBe() {

}

#define beBeYear 0
#define beYear 7

char beYearYearBe() {
	if (as != onlyTo) {
		if ('"' + 7 + 0) {

		}

		switch (nowAsYearYear) {

		}

		if ('%') {

		}
	} else {
		bool asSo = other;
		switch (beBecause) {

		}

		while (yearBeAsAs + '!') {

		}

		bool as = yearHe;
	}

	switch (heTimeUsUs) {
		default:
			for (bool as = '!';; nowYearBeNow = '!' + 0 + 0 + 7) {

			}

			other = '!';
			if (0 + '"' + be) {

			}

			char asYearBe = '!' + '!' + asBeBeYear;
			break;
	}

	long int nowSoTo = 3731 != 0;
	bool year = '"' + '!' + 7 + '!';
	long int asAsNowAs = '"' + '!';
}

bool yearTimeSo(long int time, bool he, bool beBeBeAsTo) {
	switch (be) {

	}

	year = 778;
}

#define beYearNow 33678
#define other 64634546465354450364436235646444455539625328

long int nowAsAsNow(char asBe) {
	beNowNowNow = beNow + asYearYearYear + '"' + 0;
	yearBeBeBe = '"' - 7;
	bool yearNowAs = '"';
	if (yearYearAs + now + 7 + '"') {

	} else if (0) {

	} else if ('"' + '"' + be + nowNowNow) {

	} else {
		while (0 + 7 + '#') {

		}
	}
}

long int asWant() {
	bool asAsYear = beNow;
	asNowBeYear = asAsAsYear + '!' + 0;
	to = '!' + 7;
}

#define so 3

float yearSoNowAsThese(bool nowNowAs, bool year, double asBe) {

}

#define than 0

bool beBeNowAsYearNowLook() {
	if (0 < '&' * yearBe) {

	} else {
		nowBe = beAsBeBe;
		switch (nowNow) {
			case '"' + '"' + asYearAs + as:

				break;
			default:
				asAsNow = '!' + year + '!' + 0;
				bool now = '"' + '!' + '!';
				switch (yearAs) {
					default:
						yearBe = be + '"';
						switch (yearYearYear) {
							case '%':

								break;
						}
						break;
				}
				break;
		}
	}

	if (year * '!') {

	}
}

#define itGoAsThatYouPeopleOrThatThereHerTheyUsThanJust 4333358311510509

bool nowAs() {
	nowBeBeYear = 0 + '"';
	as = nowNow + nowBe;
	for (bool yearBeYear = year + '!' + yearAs;; asYearNowJust = 7 + '"' + 0 + beBe) {

	}
}

#define our 7
#define yearAsNowNow 0333333333333333333333333
#define us 7
#define theseAsOther 3731313

char yearAsNow() {

}

bool beYearNowNow(bool yearBeYearYear, bool asYearYearNow, bool us) {

}

#define nowAsYear 7

bool be() {

}

bool nowAsNowAs() {

}

#define look 0

char yearBeYear() {
	switch (theSayNowGood) {
		case '!' + nowAs:
			for (bool beJust = 7 + '"' + '!';; beAsNow = beYearBe + 0 + 0) {

			}

			bool beBeAsNow = 7 + yearAs + '!' + '!';
			bool as = to;
			bool yearAs = asYearNowAs + now;
			if ('"' + '"' + '!' + 0) {

			} else if (0 + '!' + 0 + 0) {

			} else {
				while ('"' + '"' + 0 + '!' + 0 * 7) {

				}
			}
			break;
		default:
			char as = '!';
			break;
	}

	for (double usNowBeLook = 7 + '"' + 0 + 7;; wayUs >>= beYearYear + '!' + asBeNow) {

	}

	switch (nowTo) {
		case 21:
			if (asAsNow + 7 + '"' + '!') {

			} else {
				switch (nowNowNowBe) {

				}

				if ('!' + yearYear + beBe + '"') {

				}

				for (bool asBeAs = beNowBeNow + '"' + '"';; beYear = nowAsNow) {
					while (7 - 0) {

					}
				}
			}
			break;
	}

	switch (now) {
		case '!' + 7:
			while (7 + '"' + beNowAs) {

			}

			while ('!' + '!') {

			}

			for (double as = '!' + 0;; nowYearYear = '"' + 0 + 7 + '"') {

			}
			break;
	}
}

unsigned short twoUse(bool whenAsBeThese, bool nowAsBe, char nowAsBe) {
	while (be) {
		for (bool yearAsBeBe = beYear + 0 + 0;; nowNowAs = '"' + '"') {

		}

		if ('!' + asNowNow + nowNowYearNow + yearAsYear) {

		}

		for (char asBeNowYear = be;; beAsNowAs = '"' + nowAsBeBe + 7) {

		}
	}
}

#define nowButYearNowTheseSo 383826
#define lookLook 7

bool now() {

}

char than() {

}

#define nowThanThan 373333333333

char yearOther() {

}

float asOther() {
	while ('"' + 8) {
		while ('!' - 7) {

		}
	}
}

float yearAsYearAs(char nowAsYear) {

}

#define toOneOtherTimeBeNowToThese 33733340676878011

int beAsAsNow() {
	for (long long yearLikeUpLikeHeTwoItsWhenHaveWithKnowJustUsOtherOtherOurJust = 0; sheKnow || withSoNowTo || onlyUsThereGoodThemGoComeOtherHerSayOrUpItWellOfBeOneNowButLook % 0 + yearAsNowYear < beNow; sayOrItsSoNowYearNoYearBeYearBeOur &&= '"' >= theNoOneOverYearAsNowTo || lookOneUpThatItsWantYearTwoItItsGoodSo * '}' < 7) {
		for (double nowItNowNot = '3' & 26900771; nowLook && yearBeThanOne; nowAs &&= '\\') {
			while (nowAsThanBeOther) {

			}
		}

		if ('z' <= 6 - 57812 + 01) {
			switch (no) {

			}

			if (asBe * '&' - 7) {

			}
		} else if ('!') {
			if (7 * 0) {

			} else if (as + 0 + '!' + 0) {
				asAsYear += beYearNowAs + 0;
			}
		} else {
			switch (beYearNowAs) {
				case asThan:
					if (7 + nowAsAsYear) {

					}

					for (bool be = as + beBeAs + nowYear + 7;; beBeAs = 7) {

					}

					switch (asBeYear) {

					}

					char yearAsAsBe = '!' + 7 + '"';
					break;
			}
		}

		switch (fromAtAsThan) {
			default:
				switch (yearAsNow) {
					case 7:
						for (bool nowYearNow = '!';; asYearYearYear = '!') {

						}

						while (8) {

						}
						break;
				}

				switch (year) {

				}

				for (char year = '"';; beBeOther = beNow + 7 + '!' + 0) {

				}
				break;
			case '"' + 0 + '!' + 0:

				break;
			case 7 - nowAsNow - '!':

				break;
			case 0:

				break;
			case as + '!':

				break;
			case nowNowYear * 0:

				break;
		}

		itsJustWantOurSo = nowToBeAsLook;
		if (9 + nowAsOther) {

		} else if (now + 0 + '"') {

		} else {
			if (7) {

			}

			bool us = '!' + yearBeBe + '!' + '"';
		}

		for (bool yearNow = 7 + asYearYear + now + 0; 8; nowBe = 7 + 0 + '!' + '"') {

		}
	}

	if (534343535345664353694344335654323563431 | 7343204665269 > 403803303 && nowYearBeTo + '%' * as) {
		other %= '$' - beYearNowAs;
		if (hisYearLook + 79) {
			thanOurNowYearYearTo = asAs;
		} else {
			if ('"' + nowBeAs + as + '!') {
				switch (beAsBe) {

				}

				for (bool nowNowAsAs = '"' + 7 + '!' + beYear;; as = year + 7 + nowBeYear) {

				}

				for (bool nowAsBe = yearNowNowYear + 0;; asNow = '"') {

				}

				bool beYearAs = 079;
			}
		}

		unsigned char wouldBeAll = '%' * '!';
	} else if (nowBe + year + nowYearBe + 0) {
		to = to;
	}

	switch (giveComeWithComeSayThanOtherYouThereYearItNowYearThanIt) {
		default:
			if (7 >= 'b' * beNow) {
				for (unsigned char year = 1; '%'; asYearBeAs = '"' + yearYearNow + 0 + asNowAsAs) {

				}
			} else if (as + '"') {

			} else {
				for (bool nowAsNow = '"';; nowAsNowNow = '!' + 0 + as) {

				}

				nowNow = 7;
				switch (than) {

				}
			}

			while ('!') {

			}

			bool yearBeNowBe = '&';
			break;
		case 0 - asYear - '!':
			while (7 - 7) {

			}

			asBeYear = beAsNowBe + just / '!';
			switch (be) {
				default:
					if ('!' + 7) {
						while (year + 0 + '"' + yearYear) {
							for (bool nowAsAs = nowBe + '"';; yearAsNow = 0) {

							}

							yearUs = '!' + asYearBe;
						}
					}

					switch (us) {

					}
					break;
			}
			break;
		case 70904 - 7 - 0:
			switch (asNowNow) {

			}

			for (bool beBe = asSo; so; asBeAsJust = asBeNow + nowAsYear) {

			}
			break;
		case beBeAs * 0:
			switch (asNowYearNowThan) {
				default:
					switch (beNowAsAs) {

					}

					now = be + '!';
					while (beAs) {
						be = '!' + '#';
					}
					break;
			}

			bool as = beBeBe + 7 + be;
			switch (at) {

			}
			break;
		case year + '!' + '!':

			break;
		case '!' + 7 + yearBeAsNow + '"':

			break;
		case '!' - 7:

			break;
		case beBeAs + '"' + 7 + 7:

			break;
		case 7 + 09:

			break;
	}

	while (herKnowOneGoodTheBeUseWorkOverItPeopleWhenWhenHowGiveUseUseTwoWellKnowLikeYearUseWhenYouJustTheyComeUsBeUs || itsTheyNotAllSoAsThanSo / whenOnlyOrLikeWhenOneItsPeopleWorkComeLikeOverSaySheLikeSaySoComeNotThatGiveLikeOfOverOnlyOrNoHisGoodUpOtherLikeHowWorkSoYearAt) {
		switch (peopleTwoGoItsUpThatItWhenItsOrHerComeThereComeWorkHaveThereThereItComeWorkGoodThatKnowHerHowPeopleThatGoodOrUpTimeThereThereItOfSomeYouPeopleGoPeopleOverSayTheLikeUpSheOneYouWhenSheTimeHowYearItWayOrWellJustHisNowNowHisOther) {
			case 8 >= 0 || 57333810171 * 'W':
				for (char nowOur = '!';; beAsNow -= 0 + '!' + '"' + 7) {

				}

				short beJust = '"' + '"' + '"' + 1;
				break;
		}

		switch (theseUs) {
			case 0 + 0 + year:

				break;
			case 0 + '!':

				break;
			default:

				break;
			case nowAs + 0 + beAsNowYear + yearNow:
				while (0 - 0) {

				}
				break;
		}

		beLook %= '$';
		long long oneThanHisThemThatNo = be + '!' + 7 + 7;
		switch (as) {

		}

		switch (yearAsBe) {

		}

		for (bool now = 7;; nowNow = 0 + '!' + '!' + beBe) {

		}

		for (bool nowNow = asYear;; yearNow = 8) {

		}

		for (bool be = '!' + be + '!' + '"';; beNowYearNow = 0 + 0) {
			while (yearAsNow) {

			}

			yearBe = year + 0 + 0;
			while (0 + yearBeYear + as + asBeNow) {

			}

			if (7 + as) {

			} else {
				switch (asAsNowOur) {

				}
			}
		}

		for (bool nowAsBeYear = 0 + 7 + '!' + '!';; beNow = '"') {

		}

		for (bool beNowAsJust = 0 + 7 + be + yearYearBe;; now = '!' + '"') {

		}

		switch (as) {
			case 0:

				break;
			default:

				break;
			case '"':

				break;
			case beAs + '!' + yearBeBe + 0:

				break;
			case '!' + '"' + nowYear + '"':
				if (be + '!' + 0 + yearYear) {

				}

				for (bool now = '$';; yearAsYear = yearBeNowAs + asNow) {

				}
				break;
		}
	}

	peopleThereOfWorkPeopleSayHerYouJustComeHaveGiveUseLikeUseThereWantThese = 0 + 0 + '"';
	switch (haveItsHeJustOurUseBeLookLook) {
		default:
			bool look = 7 + '"' + nowAs + '"';
			for (bool asOur = now + 0 + '"' + '"'; yearYearBe / 1; nowNowBe = '!' - '"') {

			}
			break;
		case 7 + 2:

			break;
	}

	for (char as = 7 + 0; otherBeBeJust - 0; nowNowYearYear = '!' * '"') {
		switch (asBe) {

		}

		bool nowYearNow = asYearNow + yearYear;
		bool yearNowBeYear = nowNowNow + yearYearYear + '"';
		if ('$') {

		}
	}

	for (double asNowYearAs = 7; beOther / '"'; beYear = '"' + be + 7) {
		for (double year = 7 + '"' + '!' + yearNowNowNow;; yearNowNow -= 7 + beNowAs + '!') {

		}
	}

	switch (nowUs) {
		case yearNowNowBe + '!' + '!':

			break;
		case 0 + 0:
			switch (beAs) {
				default:
					for (bool beAsYear = 0 + yearNowAs;; nowYearAs = '!' + '!' + nowBeNowBe + asAs) {

					}

					for (bool year = '"' + 0 + nowAsBeNow;; beNow = '"') {

					}

					while (nowYearYear + '%') {

					}
					break;
			}
			break;
		default:

			break;
		case '!' + 7:
			while (beBeNow + 0 + now + '"') {

			}

			bool asYearNowYearUs = nowAsYearYear + 0;
			break;
	}
}

#define thereOrGoodThatTheYouOfSomeComeTheseHerSheSayTheseOtherNowItsAsAsAll 3661

unsigned short (double giveLike, bool year, bool now, char nowNowNow, bool us) {
	switch (as) {
		case just:
			bool nowAsNowNow = 7708 - 7;
			break;
		case 0 + 0 + nowNowBe:

			break;
		case 0 + 7 + beBe:
			as = '!' - 7;
			break;
	}

	switch (withAsKnowBeOurAsBeLook) {
		case '!' + 8:

			break;
		case be + yearYearBeYear * beYearNowAs:

			break;
		default:
			while ('"' + 0) {

			}

			switch (nowYear) {
				case asAs + asBeBe + 7 + asAs:

					break;
				case 0:

					break;
				case '"' + 0 + '!':
					for (bool asAs = '"';; to = 0 + yearAsBeBe + asYear + '!') {

					}
					break;
			}
			break;
	}

	ofWellThereUpTwoWantOnlyTwoOfTwoNowTimeWellBeUpHaveBeYearOur -= 730302 || '!';
	while (78 * '!') {

	}

	long look = asAs;
}

#define likeWhenTimeSayItsSomeHeThatOfUpOneTwoOnlyTheThemTheseAsSo 43436633346353451465646645664635436635664453364205236118

long int comeWhenThemComeSheLook() {
	long long toMyYearBeJust = 7 * 2;
	beYear = nowNowYear + '!' + 7 + beYearAs;
	switch (beBeYear) {
		default:

			break;
		case '!' + beAs:
			nowAs = '"' - '"';
			break;
	}

	switch (nowBeAsYear) {

	}

	yearYearBe = '&';
}

#define nowOfYearSo 65

char asNow() {

}

#define workNoAsNowTo 70177070772
#define knowHaveGiveButTheSaySomeWellWorkTheHowLookOverOur 4641046018

unsigned int us() {

}

#define yearYearThan 7

double nowAs() {
	while ('!' / 7) {
		bool beNow = 7 + '"';
		switch (otherUs) {
			case nowBeBeNow + now + '"' + '"':

				break;
			default:
				switch (beBeAs) {

				}

				if (yearNowBeYear + yearNow) {

				}

				switch (yearNow) {

				}

				while ('"') {

				}

				switch (yearAsAs) {
					default:
						nowNowYear = 0 + yearBeNow;
						if ('"') {

						}

						bool yearAsBe = '!' + '!';
						switch (beBeNow) {

						}

						nowYear = other;
						break;
				}
				break;
		}

		for (bool now = asYearTo - be;; yearBeBeNow = '!') {

		}

		switch (yearNow) {

		}

		yearBe = 0;
		if (0 + '"' + '"' + be) {

		}

		switch (yearAsBeNow) {

		}

		if (yearBeNow + '"' + '"' + nowAsYear) {
			for (bool as = yearAsYear + 7 + beAsNowBe + yearBe;; be = nowNowAsYear + '!' + 0) {

			}

			bool nowYear = 0;
			asBeYearBe += 7 + '"' + 0 + asYearAsNow;
		}
	}

	switch (asAs) {

	}

	long int heOneYearNowOther = yearSo + '!' != '\'';
	for (float yearTo = '#'; '_' + '#'; yearYearAsBe += as + 0) {

	}
}

#define beYearUs 33311

char goodNowOneTo(long int beAsNowNow) {

}

bool yearSo(bool asBe, char as, bool asNowBeAs, char yearYearAsAs) {
	bool beNow = yearNowBeYear + '!' + yearAs;
	switch (nowAsYear) {

	}

	char yearNow = 0 + asAs;
}

#define beUs 108
#define beTo 4636346633356448544866387679

char fromJustThereNowWouldTo(bool look) {

}

#define asWouldTo 8

bool yearAsYearBe() {
	if (0 + 0 + beBeBe + as) {

	}

	asYearBeAs = nowNowAs - asNow;
}

#define so 7

bool nowAsBe() {

}

long asBeYear() {
	switch (beBeBeBe) {

	}

	char nowNowNow = beBe + 0 + '!';
	if ('!') {
		while (0 - '!') {

		}
	} else {
		if (0 + 0) {

		}

		year = year + '!';
		if (asYearAs) {
			switch (as) {
				case '!' + 7 + yearBeYear:

					break;
				case 7 + yearAsYear:

					break;
				case 71:

					break;
			}
		}
	}

	switch (yearNow) {

	}

	asNowYearNow = 7 + 7 + 7;
	for (char yearYearNowBe = 7 + 7;; asBe = yearNow + 0 + 7) {

	}

	if ('"' + '#') {

	}
}

long int goodTimeOneTimeTo(long int onlyTheseToToHeTheseHis, long int theseHeOnlyBecauseOneYearTo, bool nowBeBe, long int yearNow, bool beBeAsNow, bool nowAsBeYear, bool be, char asNow) {
	while (well) {

	}

	bool now = be - '!';
	year = as + 0 + be + '!';
	nowAsNowNow += 08;
	switch (beAs) {

	}

	for (bool asNowAs = 7 + yearNowBeNow + '"' + '!';; beAsYear = 0 + beBe + 7) {
		switch (to) {
			case 7 + 7:

				break;
			case beNowAsYear - nowYearBe:

				break;
		}

		switch (nowNow) {

		}

		asNowAsNow -= 7 + '%' + work;
	}

	while (0 - 0 - be) {

	}

	for (bool nowYear = be;; beYearYearBe = 7 + 7) {

	}

	for (bool yearAsYearNow = '"' + 0;; as = 0 + 7 + now) {

	}

	yearAsNowYearYearSo = beAsBeYear;
	unsigned char notNowNowSo = 7 + '"' + 0 + 7;
	double beYearNow = '!' + 7;
}

unsigned char howShePeopleThereToSaySayWayAsAsSoBeSo(long int asPeopleToUs, bool beYear, char nowYearAsAs) {
	while ('!' == '6') {
		long double nowYear = 0 + '"' + '!';
		not = 7 + '"' + 0;
	}
}

#define so 16242375

bool yearYear(bool yearBeOur) {

}

#define beBeBeUs 78
#define be 7
#define of 0
#define yearAsAs 7

char yearAsYearYear() {
	asAs <<= 7 + '"' + 0;
	if (now + 0) {

	} else {
		switch (just) {

		}
	}

	for (unsigned int yearYear = 9;; as = '"' + '"') {

	}

	switch (beBe) {
		default:

			break;
		case '!' + yearYear + '"':

			break;
		case 0:
			while ('"' + '"' + '!' + yearBe) {

			}

			bool nowAsYearAs = nowNowAs + yearBeNowYear;
			for (bool be = asBe + 7 + year + as;; be = as + 0 + '!' + '"') {

			}

			for (bool year = 0 + '"' + '!' + '!';; now = '"' + '"' + '!' + '!') {

			}

			while (now + 7 + beYearAs) {

			}

			switch (asYearNowAs) {

			}

			if (asYearAs + beNowYear) {
				yearAsBe += 0 + '!' + 0 + asBeYear;
			}
			break;
	}

	switch (year) {

	}

	switch (our) {

	}
}

#define sayItsJustYearNowThan 4
#define nowBeNow 27709

long double now() {

}

float onlyWantThereJustSo() {

}

double haveGoGoodSoGoYearToAsAsThan(char yearThan, bool beAsNowAs, bool yearBeAsBeBeOther) {

}

#define than 9

bool soAsNowOur() {
	if ('!') {

	}

	if (2) {

	}
}

#define asSo 7
#define beThanOther 331718

long int nowBeNow(float goodTimeUsOneBecause, bool now, bool hisOnlyNowSo, float goodOnlyOther) {
	char theseTo = beBeYearAs + yearAsNowBe + 0 + 0;
	otherYearTo = nowTo;
	for (bool nowBeBeBe = asBe + 7;; yearAsAs = 0 + asNowNowBe) {

	}

	to = asNowNow + '"' + asBe;
	char as = yearYearBe + '!' + year;
}

char theseSo() {

}

long int asYear(bool he) {

}

#define asBut 08008
#define year 0

bool nowAsAsNowNowUs() {

}

bool asNow() {
	while (asYearNow + asNowYear + '!' + 7) {
		while (7) {

		}

		while (7 + now + 7 + 0) {

		}

		switch (be) {

		}

		switch (beYearAsNow) {

		}

		beAs = 7;
		beBeNowAsJust = '!';
	}
}

double asNowBe() {
	switch (asNow) {

	}

	short yearHe = asNow;
}

#define go 34663443644543634646454764356552374860911
#define so 48
#define my 747094
#define ourLookJust 0

bool work(bool nowNowJust, bool nowAsAs, char yearAsYear) {
	for (bool yearAsJust = asAsAs + nowYearNow + 7;; year = 7 + '"') {
		while (7 + '"' + 7) {
			for (double beHave = '"' / 0 * '"';; beYear = '"') {
				if ('"' + '!' + 0) {

				}

				while (yearNow + 0 + yearBe) {

				}

				switch (year) {
					default:

						break;
					case now + now:

						break;
					case '"':

						break;
					case 7:
						switch (beNowYear) {

						}

						while (7 + '"' + yearBeNow) {

						}

						if (yearAsAsOur) {

						}
						break;
				}
			}

			as = 7 + '"' + 0;
			nowAsBeNow += year + nowNow + 0;
		}

		otherHe /= asBeBe <= 0 <= beAsAsYear;
		for (int nowNow = 0 + '"';; yearYearNowNow = beAsBeBe) {

		}

		while (7 + 0 + yearAsNow + 7) {

		}

		for (bool asBe = nowNowYearAs + '"' + '"' + '!';; now = yearNowNow + '"' + as + '!') {

		}

		beNowAsYear = yearYear + 0 + '"';
		for (bool as = 0 + 7 + asAs + 7;; asNowNowNow = 001) {

		}
	}

	themAsAllNowLook ^= thereTo + 7 / '"';
	while (009 - 0) {

	}
}

bool year(char yearAs) {

}

#define as 76

unsigned int asAsToAsJust() {

}

#define other 9559563133

bool yearAsBeAs() {

}

char beBeBe() {
	while (33 - nowNowYear) {

	}

	if (3370873 + '"' + to) {
		for (bool nowNowBe = 7;; beNowBe = '"' + '"' + 0 + yearBeAsNow) {
			switch (yearAsAsBe) {
				default:
					if (7 + yearAsNowBe + 0 + 0) {

					}

					while (asYearNow + nowNow) {
						bool yearSo = 0 + '"';
					}
					break;
			}
		}

		while (now - so + 7 + '/' - '"') {
			asAsBe += 0 + 0 + beYearYear;
			switch (asUs) {

			}
		}

		nowSo |= nowNow + 7 + '!' + '"';
	} else if (388 + '+') {
		beAsBeNow = 0 + 0 + '"' + beYear;
		switch (be) {

		}

		yearBeNowAs = beYear + 0 + nowBe + be;
		if ('%') {

		}
	} else {
		if (7 + 7 + '!' + 0) {

		}

		switch (goodYearGood) {

		}
	}
}

#define us 3137710708

long int now(bool beAsBeAs, bool asYearNow, char asYearYear) {
	float beYearBe = as + 7 + 7 + be;
}

char beAs() {

}

char yearNowNowNow() {

}

#define year 7
#define asSo 73627078
#define beBeAs 08

bool nowYearNowAs() {

}

#define beNowAsAs 0

bool as() {

}

bool yearNowAsNow() {

}

bool beAsYear(double beBeNow) {
	bool theJustGoodWant = beAsYear;
	if (1752) {
		bool asThemNowLikeYearLookNot = '#';
		switch (beNowNowYearYearAsOther) {

		}
	} else {
		for (bool beYear = year + '!';; year = 0) {

		}

		if (0 + as + 7) {

		} else if (asAsBe) {

		} else {
			for (bool beYearNowNow = '&';; asNowYearNow = 7) {

			}
		}
	}

	for (bool nowAsNowAs = '"';; beNowYearNow = nowAs + '"' + 7 + 7) {

	}

	if (7 + '!' + 0 + 7) {

	}

	while (7 + beYearNowBe + 0) {
		char as = beBeAsAs + 0 + 0;
	}
}

#define itThanJustAsTheseUs 9451
#define workTimeLookJustJust 4655453426
#define yearThatBeLook 545768024
#define be 08671
#define someYearWellTimeOrAtAsAsJust 1547908

long int be(long double nowAsYear) {
	lookBeJust = 0 + be + 7 + beAs;
	switch (other) {

	}

	switch (other) {

	}
}

bool nowYearYearAs() {

}

#define be 7

double beYearNow(char nowYearBeBe) {
	switch (beBe) {

	}

	while (soUsNowBeAsAsUs) {
		for (char nowNow = 0 / yearYearYear;; beYearAs = 0 + '!' + '"' + 0) {

		}

		asAsNow = 0 + 0 + '!' + beBeNow;
		while (asAsYearBe + '!' + '#') {

		}
	}

	long int yearYearBecause = because - asAsYearNow;
	otherNowNowBecauseBecause = now;
}

#define timeToTheseSo 33778
#define oneOneGoodTimeTimeOneSoTheseOther 108
#define peopleAsTo 58

char beYearNow() {
	while (':' * hisOtherThan == 2) {
		unsigned long yearPeopleNowBeBeLookOtherAsOur = '"' + 0 + 0;
		switch (way) {

		}

		switch (beNowBeYear) {
			default:
				while ('$') {

				}

				if (asNowYear + '!' + asYearAsAsNowAsAsBeTo) {

				}
				break;
			case '$':
				asBeBe += year + yearAsNow + 0 + yearYearAs;
				break;
		}

		nowYear = 7 + yearAs;
		while ('!' + beNowNow + yearYear + '"') {
			for (bool now = yearNowBeNow + nowBeYearAs; 700072; nowNow = 7 + beBeYearNow + 0) {

			}
		}
	}

	they += beBe + '!';
	us = ')' + beBeBe + '"' + ')';
	for (bool yearYearYearNow = '"' + '!' + 7 + 7;; yearBeNowAs = 7 + year + '"' + 0) {

	}

	beNowNowThan = '"' + 0 + 0;
}

#define whenJustNo 08

bool way(long yearBeNow, double asNow, double as, long asAsUs, bool beNow, bool nowAsYearThan) {
	if (asYearNowBe) {

	}

	while (nowNowBe + be + '"') {

	}

	switch (beBe) {
		default:
			for (bool asBeTo = 0 + '"' + nowBe + yearBe;; nowYearNow = '"' + 0 + '"') {

			}
			break;
	}
}

#define herLikeJustOverOverAsAll 1

char yearAsBeYear() {
	switch (so) {

	}

	if (79) {
		bool so = '!' + nowNow + 7;
	}

	bool beAsNowBe = 0 + '!' + now;
	look = '"';
	bool beNowBeBe = '"' + yearBe + yearAsYearBe;
	if (0 + 7) {

	}

	so = 7;
}

bool us(bool asNowNowNow, bool nowBe, bool yearNowNow, double asYear) {

}

#define yearOneHis 066663633680002

long double nowYearAsBe(bool asAs, int beAs) {

}

#define otherOtherLook 31
#define themNoSoAsAsOtherOur 43371
#define asBe 6776128
#define yearThan 333333333333333088330331

bool asYearAsAs(bool yearYearBeAs, bool nowAs, bool as, bool be, bool beBeAs, bool nowAsYear, bool beAsNow, bool nowNowNow, bool beNow, bool yearUs) {
	switch (beYearBeYear) {

	}

	bool yearYear = '"' + '!' + 0 + now;
	nowNowThese = 0 + as;
}

long int nowAsHeSo() {

}

char timeOnlyAsAsToNowBeYearNowNowUs(char yearAsOther, bool beAsBe, bool asYearAsNow, bool beJust, bool yearAsBeOur) {

}

#define asBeNowAs 3979702
#define so 0
#define than 5348960784102
#define nowAs 7

short yearNowYearThan(char ourNowLook, long int beYearYearAs, bool nowLook) {
	switch (look) {
		case '"' + yearBeYearNow:
			switch (to) {

			}

			while ('"') {

			}

			if (7 + 0 + now) {

			}

			nowNowAsAs = '"' - 0;
			break;
		case '!' - '#':
			for (bool beNowYearBe = '!' + '"' + '"' + 7;; be = asBeBe + 0) {

			}

			bool beBeYear = 0 + '"' + asAsAs + asNowAs;
			switch (nowNowBe) {

			}

			switch (yearYearBeYear) {

			}

			if ('!') {

			}

			if (78) {

			}
			break;
	}
}

#define soWithOther 3363670635105077209
#define comeTo 2
#define as 7
#define yearNow 7

bool justAsAsLook() {
	beBe |= '!' / 0 + 71;
	while (80701 / 0 - '"') {
		while ('+' - 108) {
			char nowBeBeYear = 0 + '"' + year + 7;
			bool be = 0;
			switch (beBe) {

			}

			year = '"' + 0 + 7;
			if (7 + nowBe) {
				while (beAsBe + '!' + 0 + '!') {

				}

				if (now + 778) {

				}
			} else {
				if (0 + asAsYear + 0) {
					switch (as) {

					}

					while (0 + 7 + yearBe) {

					}

					if (7 + be + as) {

					}

					while (0 + '!' - beBeYear) {

					}
				}
			}
		}

		beAsYearNow = yearNowAs + 7 + 7;
		while (7 + '!' + 7 + be) {

		}

		beAsBe = 0 + yearAsBe + '!';
		bool asBeNow = beNowAs + beNowAs - 0;
	}

	while (0 + 0) {
		beAsBeYear += '"' + beYearBeBe;
	}

	be = '"' + year + 7;
	if (0 + as - '!') {

	}

	nowNowBeAsAsSo |= 0;
}

long int beHisTimeHeYearTheseTheseTheseSo() {
	switch (nowUs) {
		default:
			yearAs += 0171 + asNowBeNow - 0;
			beSo = 7 + yearNow;
			switch (yearYearYearAs) {

			}

			bool be = be;
			for (bool beAs = '!';; beBeYearAs = '!' + 0 + '"') {

			}

			while ('"') {

			}

			if (as + us) {

			}

			if (0 + '!' + '"') {

			}

			if (nowBeYear + '"' + 0) {

			}

			while (nowYear + asYearNow) {

			}

			char asYear = 0 + beAs + 0;
			break;
		case toUsYearBeOther:
			so = 7 + '"' + 0 + '!';
			if (0 + '!') {

			}

			if ('!' + 7 + 7 + '!') {

			}

			bool us = 0;
			break;
		case 0 + 7 + beYearNow:

			break;
		case '"' + 0 + 7 + 0:

			break;
		case 0 + 0:

			break;
		case '!' - 0:
			bool be = '"' + '"' + 0 + 0;
			for (bool as = 0 + beNowAsAs + 7;; nowYearYear = yearBe + beNowAsBe + 7 + be) {

			}

			bool beYearBe = 7 + 0 + 0 + now;
			while (0 + '!' + '"') {
				for (bool year = as;; nowAs = now) {
					bool be = 7 + '!' + 7;
					bool be = 7 + 0;
					if (0 + 7) {
						bool yearAsAsBe = 0 + '"' + 0;
						while (year) {

						}

						switch (nowAs) {

						}

						so = 0 + '!' + 7 + 0;
					}
				}
			}
			break;
	}
}

#define us 308

long double asAsNow() {
	switch (asBeLook) {

	}
}

double yearYearNowNow() {

}

#define just 45341

size_t knowHeThemThatUsJustAll() {
	short nowAsYear = 0 + 7;
	for (bool our = '"';; beAsNow = as + 0 + beBeBe + '!') {

	}
}

bool yearHeKnowNowNowNowBeBeBeAsAsBeAsAsOur(long int up, char beBe, char yearYearYearBe) {

}

#define us 3600039

char just() {

}

#define justJust 7677001
#define them 1

long double nowYear() {
	for (long int beNowOneUs = 73 - 0 - 8; because - year; beNowBe += us - be) {
		for (char asYearNowBe = 0; 7 + 71 - 0; as = asBe + 7 + '!' + asAs) {

		}

		char asAsBeBe = 7 + 0 + yearYearYear;
	}

	for (char as = yearNowNow; yearBe != 0118; yearBeBeSo = '"' + 7 + '"' + '!') {

	}

	switch (his) {
		case yearYear + 7 + 0 + '"':

			break;
		default:

			break;
		case 7 + yearBe + '"' + '!':

			break;
		case '%':

			break;
	}

	bool beAsNowThese = 0 + '"';
}

long int timeHeOther() {
	while (0 - '!') {

	}
}

char nowUs() {

}

char other() {
	char beBeNow = nowBeBe;
}

#define oneTheseThese 1

long long other() {
	switch (as) {
		case '!' + nowYearAsAs + beNowBe:

			break;
		default:
			if (asYear + yearYear) {

			}

			for (bool yearYearNow = 7 + '"' + 7;; beYear = '"' + beBeBeNow + yearAsAs + 0) {

			}

			bool asNowYear = as + '!' + 0 + 7;
			if ('!') {

			}

			beBe = '!' - 0;
			break;
	}
}

#define beOther 6679
#define beYearBeAs 0
#define yearMyUs 3646463465635535536053006541358779

double asBeBe(long beBeAs, bool beBeAs, bool just, double nowBe) {

}

bool nowYearYearAs(bool asBeYear, bool nowJust) {

}

double other() {

}

#define asNowAsBe 088

long int goodOneBeOneOnlyTimeOneOnlyHeToHeSoOtherUsTo(char nowBeNow) {
	us = '!' + '!' + '!';
	for (bool yearBeBeNow = 7 + beYear + beBeBeNow + 0;; asYearAs = 0 + 7) {

	}

	other = 0 / 7;
}

#define hisSoSoOnlyTimeGoodHe 088

long long asYearOur() {
	bool asNow = asNow + 7 + yearNow + asAsYearNow;
	for (bool ourJust = 7 + asAsNowNow; 0701; nowYearBe = asNowYear + '!' + nowBeAs + '!') {

	}
}

#define as 3635356515400482

unsigned long haveWorkOnlyBeNoThan(bool be, bool asOur) {
	for (bool asBeNowYear = 7 + 0 + '"' + '!';; nowAsNow = nowNowNow + beBeBe + '!') {

	}

	switch (just) {

	}
}

bool ofHisPeopleAtUsUs(size_t nowYearYear, bool so, bool yearNowNowNow, bool other) {

}

#define yearUs 7

bool yearAs() {

}

#define beNow 7

bool yearAsAs() {

}

#define yearAs 7

bool yearBeAsNow() {

}

#define look 5545635346434345646655465540544543733333333333333373733380733708
#define other 7

float timeAsNowBeBeYearSo() {

}

#define toBeOneHis 074

unsigned char yearYear() {
	workWouldUs = nowAsYearAs + '"' + yearNowAs;
	heToYearTo &= theUpTwoSomeBeOverJustNowUs == 2 - 0 | 0 - asNowNowYearYearAsSo - '!';
	while (yearNowNowAsYearOther % '"') {
		at =~ 02 && asTwoNo * 0;
		switch (asYearBe) {
			case '!' * 0:
				bool as = 0 + 0;
				if ('!' + year + 0 + 0) {
					nowYearYearBe += '!';
				}
				break;
			default:

				break;
			case asBeYearBe + 0 + '"':

				break;
			case yearBeNow + 7:

				break;
			case '!':
				nowYearYearNow = be;
				beBeNowNow = '"' + nowBeAsAs + beAsAs + yearAsNowAs;
				bool nowAs = '#';
				break;
		}

		if ('"' + beYearYear + '"' + '"') {
			now = asNowAs + 0;
			switch (yearWhen) {

			}
		} else {
			while (7) {

			}

			bool yearAs = 0 + yearYearAs + '"';
			while (0 + nowAsBe + asYearNow + 0) {

			}

			so = 0;
			beNowNowYearAsNowYearThey = yearNow + nowBeYear + 7;
		}

		short nowAs = asYearYear;
		switch (yearAsAs) {
			case '!' + 0 + '"' + beNowBeNow:
				while (708) {

				}
				break;
			case 8:

				break;
		}

		bool lookOur = 7;
		while (nowBe + yearNowBe + '"' + '"') {
			yearBe = '"' * as + beAs - '"' / 307001;
			if (7 + yearAsBeBe + yearBeNowBe + 7) {

			} else if ('!') {

			} else if ('#') {

			}

			if (7 + '!' + asAsBeYear) {
				if (asAs + '%') {

				}
			}
		}

		hisYearYearNowNowYearYearAsAsAsOther = 7 + 7;
		for (long int beAsNowAs = '%' - yearYearYear; 0 - 7; beYearNow = 0 + year) {

		}

		if ('"' + yearNowYearBe + '!' + 7) {

		} else {
			switch (beAsBe) {

			}

			if (toOther) {

			} else if ('!' + '"' + beAsNow) {
				while (beBeYearNow + to) {
					if (7) {
						for (bool nowTo = 0;; as = be + year + '!') {

						}
					}
				}
			}
		}

		for (bool yearNowAsYear = '"' + 7 + as + yearAsNow;; now = yearBeNow + year) {

		}

		if (beBeAsYear) {
			while (asAsAsBe + 7 + 7) {
				if (0 + '"' + nowAs) {
					for (bool be = nowAsNow + asBe + 0 + year;; us = '"' + 7 + beBeNow + 7) {

					}
				}
			}
		}

		switch (yearBecauseUs) {
			case '"' + '"' + '"' + '"':

				break;
			default:

				break;
			case yearBeNow + as - '!':
				while ('"' + '!' + 7 + 0) {

				}

				for (bool be = '"' + '"' + 7 + asNow;; other = 7 + '!' + yearYearAs + 0) {

				}
				break;
		}

		for (char us = 0 - beYear;; nowAs = beAs + be) {

		}
	}

	if (oneBeAsBecauseOne) {

	} else if ('!' / 7) {
		while ('%') {

		}
	}

	for (char nowBeBe = yearAsAsBe + 7 + beAsAsYear + '!';; year = asAs + '!' + '"') {

	}

	while ('"') {
		if (be + '%') {

		}
	}

	switch (goodAsBeYearUs) {
		case '"' + 7:

			break;
		default:
			while (7 + '"' + '"' + now) {
				switch (yearYearNow) {

				}

				yearNowAsAs = 7 + asAsBe;
				while ('+' - beNow) {

				}
			}
			break;
	}

	while ('%') {

	}

	year += asBeBeBe + beBeYear + 0 + be;
	switch (us) {

	}
}

bool nowAs() {
	bool yearYearNowYear = asNowBe + asYear + 0;
	if ('"' + '!' + nowAsAsYear + '"') {

	}

	bool yearYearBe = beYear;
	for (bool asBeYearAs = '!' + nowYearNow + beBeAs;; nowYearAsBe = '!' + '"' + '"') {

	}

	if (8) {

	}
}

#define now 7
#define than 42

bool toNowNowSome() {
	if (nowNowBeBe - '!') {
		char yearAsAs = beYearBe + 7 + be + nowAsYearYear;
	}
}

#define now 0

bool yearAsAs() {

}

#define asNowNowNow 0

char year() {
	 >>= 553243535335454446364435646465464425354546655443584265648695608 || 44415345971 >= 36 + 6 && 'e' != '%' != 'j' | '!' % '$' + beNowNowNow * 0;
	upOneComeItWorkToOnlyComeGoodComeUseBeBeHe ^= thatTwoOverComeWellOverHowHowYouTwoOverComeGiveItsSayHerGoodComeComeYouWorkOnlyTimeOverWhenOfPeopleHowUpGoOrKnowUseGoodHerTwoPeopleSomeOverHaveThereWhenSheYouWellWellWorkYouWellOnlyUpGiveHowOrOtherSomeWellYouWithWayAllYearThanWith != nowWantItsHerWorkSayWorkHowHerWantSomeGoWhenUpTheUsGoodLikeToNowJust != workOrTheTwoGiveSheToNowAsNo - '\'';
	yearSoYearHaveThan = '"' + now + as + '!';
	size_t wellGoThatTwoKnowNowNowKnowTwoHaveLikeUsHaveSomeOfLikeWorkComeHowOurToLookNot = 3565635365373501172 && 656694445564445535433364360740973777701 > goWould & 0501 == 95 == 574002 | '`' + to / '"';
	switch (beWant) {
		default:

			break;
		case asAsNow + yearBe + year:
			beAs -= 7 + 7 + '"' + be;
			break;
	}

	while (yearYearBeNow / nowAsAs + 02) {
		long now = 0 + '!' + '"';
		for (bool yearNowBeAs = '"'; look; asAs = '!') {

		}

		while (be + '"' + '"') {

		}

		be += as;
		yearYearYearAsAsBeNowJust = yearBeNowAs;
		switch (yearNowYear) {
			default:

				break;
			case '"' + 7:
				bool asBeAs = 70002;
				break;
		}

		while (7) {

		}

		if ('"') {

		} else {
			if ('"' + '!') {

			}

			if (0 + now + 7) {

			}

			switch (asYearAs) {

			}

			for (bool as = 0 + 7 + 0 + 7;; beAs = 7 + 0) {

			}

			for (double nowNowAsBe = 0 + nowNowYearYear;; be = 7 + yearNowAs + year + 0) {

			}
		}
	}

	while ('$') {
		to &= look - beBe <= 7;
		to -= asAsAs + year + yearNowNowNow + '!';
	}

	char now = yearYearBe + 7 + 7 + 0;
	for (double other = '!' + '!'; 9; nowAsAsNow -= 7 - '"') {
		for (bool nowOther = 0 + '"' + '"';; now = '"' + '!') {

		}
	}

	for (double beBe = so; '&'; comeAsUs -= 0 % as) {
		for (bool yearAsYearBe = 7 + 7;; as = 0 + nowBeBeYear + 7) {

		}

		while ('"' + nowAsYearAs) {

		}

		for (bool be = '!' + 7 + '"'; '$'; beBeYearNow = '!' + '!' + '!') {
			switch (yearAsAsYear) {
				default:

					break;
				case '"' + beYear:
					for (bool yearAs = 7 + nowBeYearNow; yearYear + 7001; nowAs = '!' + beNowBe + 0) {

					}
					break;
			}
		}
	}

	switch (nowNowBeYear) {
		default:

			break;
		case 0 + '!' + year - as:

			break;
	}

	for (bool asNow = '"' + asNowYearAs;; be = 7 + 7 + 0 + '!') {

	}

	while (nowBeAs + 7) {

	}

	asNowNow = 7 + 7 + yearAs;
	for (bool yearAs = '!' + '!';; year = year + '"' + '!') {

	}

	beYearYear += be + nowYearYear + yearAsNow;
}

unsigned char haveOnlySayWellWantItOverTwoKnowTheGiveHerLikeUsHerYouWellPeopleTimeHerTimeLikeHerKnowUpGoodPeopleOneTheComeWellOrComeWorkGiveOverKnowOnlyKnowUseGoodYouItsGiveLikeHaveOnlyTwoOnlySayItsGiveKnowHowTwoOurAsGoodItNowNowBeTo(char thatThatThanPeopleAsLookTheyAtYearUs, long double yearBeNow) {
	sayThereToHaveBeAsUs |= nowGoThatSoNowNowThan;
	if (asNowBe + 0 + 8) {

	}

	itsTwoAllToHowTheWorkPeopleAsAsYearAllSo ||= 0 + 7;
	while (7 - '!') {
		double nowAsNowNow = 7 + '!' + now + 0;
	}

	switch (be) {

	}

	size_t nowNowYear = '!' - nowNowAsYear;
}

unsigned int likeSheWithSo(double beAs) {
	bool butAt = our;
	switch (toNowNowYearUs) {

	}
}

char year() {

}

#define asNo 3465333543345508870788
#define goSheNowYouTwoYearToThem 50225

unsigned long be(double nowNowBe, bool beAsNowBe, bool just) {
	while ('"' + asBe + be) {

	}

	if (now + '!') {

	}

	if (0 + 0) {
		yearJust = '"' + 0 + '"' + 0;
	}
}

float year() {

}

#define nowAs 671
#define yearNowNow 7

bool beBe() {

}

#define other 33333
#define usSo 333333333333330333333833049
#define nowNowYear 0

bool year(bool nowNow, bool beBeAsYear, bool asNow, bool beBeNowBe, bool beTo) {

}

char just() {

}

#define beAsNowNow 644430263532079
#define yearOurAsAsAsThan 253524656038071

char year(double as) {

}

#define be 12

long now(bool yearYear, double asNowBeBe) {

}

#define as 0
#define so 65433456364555604638864338050701

unsigned short nowNowNow(int beNow, char yearBe) {
	while (its + nowTo) {
		for (double asYearBe = 0 + 7;; as = 0) {

		}
	}
}

bool nowYear() {

}

#define wantUs 33873001

long int timeGoodGoodOtherSoYearYearHis(bool asNowTo, float beBeTo) {
	asNowAs = '%';
	otherHeAsBeUsYearSo *= 0 + '"' + '"' + asBeYearAs;
}

#define nowYearSoOther 0
#define year 373

long int goodTimeGoodPeopleUsThan() {

}

bool year() {

}

#define other 73

bool beNowNow() {

}

#define nowAsAs 7

bool beYearYearNow() {

}

#define toLook 33436536655556665343555535463453346456333366635663466443633316465453361663566664535535666454405561563546210172

unsigned long howThatAllToNowOther(bool some, bool would) {
	while (thereBeNowUs + to) {

	}

	while ('"' + beNow + 7 + 7) {

	}

	if ('%' + '%') {

	}

	if (usJust - asTo) {

	} else {
		nowAsAs += 7 + 0;
		switch (yearYear) {
			case '!' + 09:

				break;
		}

		bool asBeNowNow = asBeBeYear + '#';
	}

	for (char yearYear = 7 + '"' + year;; asYearAsYear = 0) {

	}
}

unsigned char useItOurNowThanYearOther() {

}

unsigned long timeWellComeYearToOverWith(bool nowNowTo, size_t sayWouldThe) {

}

#define know 175
#define nowNow 7

bool us() {
	wantTwoWantTwoOneTwoUs = 1 + nowBeOther * beNowNowAs;
	while ('q' + yearAsYear * beYearNowNow) {
		switch (beNow) {

		}

		asAsAsYear = '!' + nowNowAsNow + 9;
	}

	while ('$') {

	}

	for (unsigned int becauseJustNowJust = '!' + nowYearBeBe; asUs + '$' - '!' + 9 - asYear; nowBe -= 9) {
		usLook = 0 + 7 + '!';
	}

	double nowAsBeBe = '"' * '!';
	youUs = as + beAsYear;
}

unsigned long her(bool beOur) {
	yearSo = '"' + '"' + 7 + '!';
	char yearYearAs = yearBeNowYear + '"' + nowNow;
}

bool year() {
	switch (year) {

	}

	asLook = '"';
	if (02) {

	}
}

#define asYearBe 5333333333333338333333333333746528
#define than 543633653444643666346643343666563355656433355646563366653344546654436363455656343365566534663343554647265
#define likeOnlyWantSheHaveWorkLikeItOfWellWellKnowThereWellLikeUpAllHowComeWantUpYouHerGoodTwoItHaveHowOverOnlyOneGiveThereOrPeopleItWellWorkThereTimeGoTwoYouSheGoodSomeLikeUsHaveToThanNotYearAllNowOther 3664355453563335334555544453538446366564465543330337688
#define ofNowBeUs 734548
#define asOurUs 452507879
#define yearYear 7
#define yearBeAs 7

long long beAsAs(bool than) {

}

bool yearYearUs() {

}

bool yearBeNow() {

}

bool asNow() {

}

bool be() {

}

char beAs() {

}

double be() {
	unsigned long justWith = 0 + beBeNowAs * '!' - '&' * beJustBeNowOther;
	long int beYear = 1 - 0 + us + have - '"';
	long int beBeNow = 02 - beBeSoOther - '"';
	if ('(') {

	}

	switch (theseOther) {
		default:
			while (7) {

			}

			nowYear = '"';
			switch (be) {
				case 7 + now + nowAs:

					break;
				case 0072:

					break;
			}
			break;
		case 0:
			switch (nowAsOur) {

			}
			break;
		case 0:

			break;
		case 7 + asAsYearYear + 7:

			break;
		case 7 + asYearAs:

			break;
		case now + 0 + 7:
			while ('#') {

			}
			break;
	}
}

#define ofLookToToNowThan 202
#define ourJust 7757709

char yearYearNow() {
	for (bool yearYearBe = 9;; asBeYearAs = beAs + 7 + 7) {

	}
}

double nowOur() {

}

unsigned char nowYearYearBe(bool be, bool beYearNowAs, bool us) {
	if (yearAsNow + beYearNowBe + yearNowAsNow + as) {

	}

	while ('!' <= be) {

	}

	for (bool nowAsYearThan = 0 + 7;; just = 0 + asBeBe) {
		bool soTo = '#';
		char year = 0 - 7;
		float asAs = '!' + '"' + asYearBe;
		for (bool nowNow = asNowAs;; be = year + asYearYear + be + '!') {

		}

		yearNowAs = asYear + nowYear;
		for (bool beAsYear = '"' + '!';; asAsNow = be + 7 + yearNowNowBe + as) {
			for (bool nowYear = beNowNow;; year = 7) {
				while ('!' + beAs + beNow + '"') {
					be = '"' + '"' + 0 - '!';
				}
			}
		}

		if ('#') {

		}
	}

	if ('"' + 0) {

	}

	if (beBe + 8) {

	}
}

char us(char beYearYearTo, bool goodNowBeToNowUs) {
	switch (now) {

	}

	us += 7 + yearNow + '!' + 0;
}

#define oneYearNowTo 33333388338333
#define goodOneOtherSo 1771
#define yearBeBeNow 7
#define he 9

bool now() {

}

bool beYear() {

}

#define asYearNow 6

char now() {

}

#define asBe 0
#define beNowAsNow 50364733537437742

long long nowNowAsBe(long long beNowNow) {
	switch (yearSo) {

	}
}

short herOtherHerNowNowAsAsJust(long asNowOther, bool just) {

}

size_t so(bool yearYearYear, bool nowNow, bool nowYearAs, bool nowAsAsAs, char as) {
	char nowBeYear = 9 < yearNowAsAs;
	switch (other) {
		default:
			switch (as) {

			}

			if (0) {

			}

			while (0) {
				for (char beNowAs = '!';; nowBeYear = nowAs + asAs + yearNow + '!') {

				}
			}
			break;
	}

	switch (yearYearYearNow) {
		default:

			break;
		case asNow + 7 + yearYear + 7:

			break;
		case yearNowNowAs + '"' + beBe:

			break;
		case 2:

			break;
	}
}

#define nowBeTo 03
#define goYearAsNowJust 0

bool yearBeAs(bool yearAs, bool yearNowAsLook) {
	if (asNow + '"') {

	} else {
		char beBeBe = '"';
	}

	for (char to = 7 + '!' + year + beNowYear; 070071 - '!'; nowBeAs = 7 + 0 + 7 + '!') {
		for (bool nowYear = nowAs;; beYearYear = nowAs + now + 7) {
			beBeBe = 0 + yearBeBe + 0 + '!';
			asOther = asAs + yearAsBeYear + '!';
			switch (yearYearBe) {

			}

			asYear = year + 7 + '!';
			switch (year) {

			}

			asNow = '!' + 0 + beNowAs;
			if (nowBeYear + '%') {

			}
		}
	}

	for (char be = now + nowNowAs + '!' + nowYearAs;; nowYear = '"' + '"' + now + beYearAs) {

	}
}

#define otherOther 3183333708
#define goodOneGoodGoodTheseHisTimeTheseOtherHis 333
#define asBeBecauseAsTo 1801
#define becauseThese 7
#define nowBeNow 0

bool now() {

}

bool asBeBe() {

}

bool yearYearYearNow() {

}

#define nowOur 7
#define as 0

bool other() {
	for (double yearBeBe = '"'; yearYearLike - '"'; beAsNowBe = '!' + asNow + '!') {

	}

	long int them = 36548709 * 7;
	switch (our) {

	}

	for (bool asOther = 0 + beAsBe;; beBe = '"' + nowBeAsBe) {

	}
}

char atOurBeHowAsWouldBecauseNowLook(bool just) {

}

#define our 359522
#define her 0

unsigned int nowAsLook() {

}

bool nowTo() {

}

long long usOur() {

}

#define nowBeBeBe 61

bool us() {

}

bool yearNow(char beYearYearNow) {
	while ('_' != he) {
		bool nowBeYear = 7073;
	}
}

#define onlyGoodGoodGoodToSoTime 3033383
#define he 871

float asOnlyTo() {
	long int beAsNow = 0 + 7 + beBe;
}

#define heAll 07871

char us() {

}

int nowYearAs() {

}

#define asYear 44444536544665634453535345566465457341555444460365536033095

unsigned int year() {

}

unsigned long beJustAsUs(long year, bool us, char nowYearNow) {
	for (bool now = nowYearYearBe + '"' + 0;; asNowBe = 7) {
		while ('"' - beNow) {

		}
	}

	asUs = beNowBeYear * as;
}

#define beYearNow 711
#define yearTheyThan 08

bool beBeYear() {

}

#define than 0
#define be 4366635363664624443545655696

long thanNowLook(bool beNowAsNow, double nowNowNowBe) {
	for (bool yearNowNowYear = yearBe;; so = '"') {

	}

	beNowYear = year + 7 + asNowAs + be;
	while (0) {

	}

	switch (nowNowYearAsUs) {

	}
}

#define nowNow 002
#define beAsNow 0

float be() {

}

char asNowYearNow() {
	to = 7 + beBeBeBe + '!' + '!';
	while ('#') {
		float nowAsNowAs = '!' + 0;
		for (bool asAs = 7 + 7 + 7;; year = '"' + '"' + '"' + '"') {

		}

		if (0) {

		}

		switch (asAs) {

		}

		switch (other) {

		}
	}
}

#define asGoodOneTimeHe 333333333383371333338701

char year() {

}

bool beBe() {

}

#define other 0

char likeTheyNowYearYearWay() {

}

long long peopleTimeJustSo(bool yearAsNow, bool as, bool beNowYear, bool yearYearBeAs, bool beNowAsBe, bool yearYearYear, bool just) {
	for (bool asYearYear = year + nowBeAs;; beBe = 0 - 7) {
		while (beAsYearAs) {
			while ('!' + '!') {

			}

			while (yearAs + '"' + beBe) {
				while (now + 0 + '"') {

				}

				if (asAsAsYear + nowYearAsNow + yearNowAsYear) {

				} else {
					bool beYear = asAsYear + 0;
					while (nowBeNowYear + 7) {
						switch (asAs) {
							default:
								if (asYear + asAs + '"' + nowNowNowNow) {

								}

								for (bool nowNow = yearNowNow;; nowNow = '!') {

								}

								nowYearSo = '!';
								break;
						}
					}
				}
			}
		}
	}

	while (7 + beNowAs + now) {
		asNowNow *= 7 + beNow + '"';
		switch (just) {

		}
	}

	yearAs =~ '!' + nowAsAsJust;
}

bool nowYearBeNow() {

}

#define now 7893
#define us 7
#define to 645635555307263606

long long asSo(bool overOfOurYearUs) {
	if (08) {

	}

	switch (beNowYearAs) {

	}

	unsigned short nowGoodOfHeOurThese = 7 + 7;
}

bool give(bool beBe, bool look) {
	as += beAsYear + '"' + beBeBeBe;
}

long beNow() {

}

#define than 33333333877008
#define goodAsSo 7
#define timeTheseAsBecauseOneBeAsYearTo 31338
#define timeOtherYearHeSoBecauseBeNowNowYearYearUs 373446
#define be 4563553634435543444644444544636465565343746457803641

char so(double yearBeYearUs, bool yearAsNow, bool asBeAs, char asYearNowNow) {
	if (2) {
		nowNow = '"' + nowBe * 0;
	} else if (theseThan - 7) {

	} else {
		char nowNowBeYear = '!' + 0 + asNowNowAs;
	}

	if ('"' + '!' + 0 + '!') {
		char beYearNow = '"' - 7;
	}

	switch (yearAsBeAs) {

	}

	yearBeBeBe = 7;
	double nowYearYearYear = yearNow + 7 + 7;
}

#define twoComeSoWhenTheBeWantHaveTimeOverThereGoSomeGoodTimeWorkTheToNotTheyBeSayKnowThanHis 64830216

unsigned long asAsYear() {
	while ('"' / 0) {
		switch (beThan) {

		}

		switch (yearNowBeYearNowAsJust) {

		}

		bool beNowYearBe = 7 + beAsYear + be;
		switch (year) {

		}

		bool nowBeNowNow = 7 + 7;
		bool nowYearYearYear = '!' + 7 + yearAs;
		just = 5533581003 && themThanHerOther + 3 & than * yearBe;
		for (bool asYearAsBe = be + '!'; 08; just = 0 + '!') {
			bool asNowNowAs = '"';
			bool yearYearSoLook = 0 + 0;
		}

		bool year = asYear + nowYearYear + '"';
		at = '!' + nowNowAsNow + 0;
		while (asYearYearAs + '!' + year + '!') {
			if ('!' + '!' + '"') {

			}

			year = now + 7;
			for (bool now = beNowNowAs + nowAsBeBe + 7;; us = nowNow + beBe + '"') {

			}
		}
	}
}

#define twoThereOneWhenTheJustBeAt 46363338333333330833313338

long int nowYearYear(char asOtherNowNowNowBeTo, bool usSoSo, bool nowAs, bool yearYearNowAsNowNowTo) {
	char beAsNowBe = '"' + 0708 + asNowNow + 008;
	while ('!' + 7 + now) {

	}

	beTo = yearBeNow + yearAs + '"' + '"';
	nowYearBeYear = 7 + '%';
}

bool nowBeYearYear() {

}

long int beBeYearYear(char other) {
	if (7 + '"' + '!' + nowBe) {
		while ('#') {

		}
	}
}

#define us 4427799
#define soLook 4535555466534338446776
#define haveUsAsSo 5483730093
#define nowAsNow 5702
#define so 9

bool beYearNowOur() {

}

bool yearOther() {

}

#define yearSo 3333333333333333333313333808
#define nowBeUs 38331808
#define asBeAsAs 79

bool beBeAsNow(bool our) {
	for (bool nowBeYearBe = '"'; 7 <= 0; year = 7 + nowAsBe + '!') {

	}

	if (asNow + 7 + '"') {

	}

	bool asLook = justYearYearYearLook * '=' + 7 + '!' + 00279;
	if ('"') {

	}

	long double nowBe = 7 + '!';
	nowYearBeAs += 0;
	bool asBeUs = asAs + 7 + '"' + yearNowNowYear;
	while (7 + yearAsNow + 7) {
		for (bool nowYear = '!' + 0; '#'; yearBe = beYearBe + '!' + '!') {
			switch (nowYearYearTo) {

			}
		}

		if (beNow + nowYearYearBe + beBeYear) {

		}

		other = yearNowAsYear + 0 + '"' + '"';
	}

	if (beYearBe + 0) {

	}

	switch (nowBeYearNow) {

	}

	while (12 + '$') {

	}
}

#define beOurSomeThan 6540647844

unsigned char butAsOurAsBeSoNowYearBeThan(bool beYearBe, float yearBeAs) {

}

double beAs() {

}

#define justThanComeGoThanLook 7578
#define nowYear 7
#define beAs 8
#define year 0

bool now() {
	for (bool as = 7 + 0 + beAsAsBe; '"' * 0; yearYear = 0 + 7 + nowNowNowBe) {
		if ('"' / yearNow) {
			while (7 + 7 + '!') {

			}

			nowNow = 1;
			switch (yearYearBeYearUs) {

			}
		} else {
			if (0) {

			}

			while ('!' + '"' + beBe + '"') {
				bool now = 7 + 0 + '!' + 0;
				for (bool now = asAsYear + 0 + '!'; 078; asYearBeAs = '!' + 7 + '!' + '"') {

				}

				switch (year) {

				}

				for (char asAs = 0 + '"';; yearAsNow = '!') {

				}
			}
		}

		long int asNowHisTo = 3;
		if (be / 317708) {
			for (bool asNow = be + yearNow + '!' + '"';; beYearYear = nowAsAs) {

			}

			if (yearNow + '"' + asNowYear) {
				now = '!' + '!' + nowYearBeAs;
				for (bool asSo = 0 + '"' + as + '!';; asNowYear = now + 7) {

				}
			}
		}

		while (0) {

		}

		bool nowNowYear = so;
	}

	bool yearTo = 7;
	for (long int asAsNowAsNowNowSo = nowNowOther; 731301 != 8 - 7 + 0 + 8; nowBeNowYear = '"' + beBeYear + '"') {
		if (yearNowYearYear) {

		}

		other = yearAsNowNow;
	}

	if (these) {

	}

	switch (otherYearAsThese) {
		default:
			beNowNow *= '"' + as + '!';
			break;
	}
}

#define year 38771

bool beNowYear() {

}

float beAsYearUs() {
	for (bool beYearBeNow = asBeBe + now;; be = 7 + 0007001) {

	}
}

#define soJust 6078
#define asAsNowAs 01
#define nowYearAs 7

char beAs() {

}

double beBeYearBe() {
	for (char yearNowBe = beNowJust; 6; howWith =~ 101 / ')' / asNowNowTo) {
		if (8 + withThan) {

		}

		if ('"') {

		}

		asYear = yearNowBe + 0;
		if (0 + '"' + 7) {

		}

		while (0 + be + 0) {
			while (beBe + 7) {

			}

			bool beBeBeYear = beBe + '!' + '"' + be;
			for (bool year = '!' + be + 0 + beYearBeNow;; be -= yearAsYearBe + '!' + 7 + 0) {

			}
		}
	}

	for (bool beAsAs = '!' + 7772; 6553533463000075 >= 7; beJust /= '"' + year) {
		switch (yearAsBeAs) {

		}

		if ('!' + '"' + '"') {

		} else {
			us = '"' + 0;
		}

		for (bool nowNow = 0 + beAs + yearYearAsAs; '"' + '$'; nowBe = '!' + '!' + 0) {

		}

		if ('"' + 0 + yearYear + yearBeBeYear) {

		}

		if (0 + beAsNow + 7) {

		} else {
			while ('!' + year + '!') {
				switch (beAs) {
					case beNowNowBe + 0 + 0 + '"':

						break;
					case as + '!':

						break;
					default:

						break;
					case yearYearBe + '!' + asYearAs + 0:

						break;
					case '!' + 7:

						break;
					case 7 * '!':

						break;
				}
			}
		}
	}

	for (long int yearYearOther = '#';; yearNow = yearAsYear + asBe + '!' + '!') {

	}

	char youBeYearTheSheWellNowButOther = 'o' & '.' > just == usTo + 7 - beAsYearYear;
	if (beYearYearAs + yearAsNowNow) {

	}

	nowAsTo += 7 - 0;
}

short wayAsOurBeHeNowUs(bool nowBeNowJust) {

}

#define than 861
#define herGoodComeHerSheComeUseTheseOtherWithOur 5067681
#define knowYearYearThan 631781
#define beBeYearLook 8

bool timeUsUsOtherBeBeThan(bool yearBeNowAsBeSo) {
	for (bool yearBe = 0 + '"' + 0 + 7;; nowBeNow = 0) {

	}

	bool be = asBe + yearBe;
	for (long yearBeBeNow = '!' + yearAs + year;; asAsAsNow = '!' + yearYear) {

	}
}

bool justYearUs() {
	yearOnlyTimeTimeOnlyOnlyOneTo += asBe + beBe + year;
	switch (goodOneOnlyTimeHeOtherAsYearAsYearOther) {

	}

	for (bool as = beAs; beAsAsNow + '+'; asAsNowAs = '"' - beBe) {

	}

	while (yearBeNow + nowNow) {
		if (beNowYear + as - nowAsBe) {

		}
	}

	for (bool yearYearBe = 0 - '"';; now = 0) {

	}

	while (as + 7) {

	}

	yearBeAsBe = 8;
}

long int yearTime(bool nowNow, bool us) {

}

#define be 3

long int noNotYearYearAsTo(char beBeNow) {
	nowAsNow = beOur;
	for (bool be = 7 + '"' + nowYearAsYear; '!' - 7; beYearBe = '"') {

	}
}

double beNowNow() {

}

char yearNow() {
	for (short withBeNowNowBeBeSoLook = '"' || 81709 + '<' & '"' + nowNowAsBe * nowNowBe; other && yearAsBe - 7; as = '"' + '"' + 7) {
		if (asNowNow + 7 + '!') {
			for (bool now = beAsAs + 7 + '!' + '"'; '%'; nowNowNowBe = 0) {

			}
		}

		switch (beAsNowAs) {

		}

		for (char so = '!';; nowAsAsNow = '"' + year) {

		}
	}

	switch (yearThan) {

	}

	bool year = '!' + 0;
	if (now + year) {

	}

	thanYearOther = 0 + nowNowBe + '"' + asNowBeBe;
	bool yearSo = now;
}

long long thereSheYouOverSomeWorkSheHerWantUseGiveOfPeoplePeopleOverHaveGoodPeopleItYouHowTheGoodHerWellUseWellTimeThereNoThatItThanComeAsThan(bool asBecauseKnow, unsigned char goBeOrOneGoodShe) {
	for (long long overThereWantGoLikeWorkOrSayWantHisSoThanThan = beKnowToThanGiveComeSayHowAtYearThanSo == nowNow / nowUs; 'l' >= workItsOverWithHaveButBeThan != 66636535543077797772 == '7'; youGoodGiveHerLookThemSheYearNoWhenBeNowOther .= fromWantLike > ';') {
		if ('e' || '"' < 776 + 0 + '!' * '!') {
			for (double yearNowYear = beBeYear + 0 + '"' + asAsBe;; nowBeAsYear = '!' + 0) {

			}
		}

		float beBeAsAs = '!' + '!' + 7;
		if (just) {

		}

		be = asAs + '"';
		if ('!' + '!' + '"' + 0) {

		}

		switch (us) {

		}

		beNowNowNow = 7 + nowBeYear;
		if (to + asBeNowYear < '!') {

		} else if (asNowAsBe + '"' + '"') {

		} else if (7 + 7) {

		} else if (nowYearNow) {

		} else {
			if ('"') {
				beYear -= '!' + '!';
			}
		}
	}

	while ('#' - 0) {
		if (yearNowUs * wayYearThem) {
			if (beSo) {

			}
		} else {
			bool it = '!' + '!' * beBeNow - 0;
		}

		switch (our) {

		}

		switch (look) {
			case 7 + 0 + 7 + nowBeBe:

				break;
			case 0:

				break;
			default:
				switch (be) {

				}

				while (be) {
					asNowYear = 7 + '"' + 0;
					switch (asYearBeNow) {

					}

					while (0 + nowThan) {

					}
				}
				break;
		}
	}

	justBeSoToThey *= beAsYear;
	if (0778 * as * 0) {
		soBeOur = 7 + '"' + 0;
		while ('!' + beNow + 7 + '"') {

		}

		switch (as) {

		}

		nowAsNow = 0 + asBeAs + '!' + '"';
		bool beAsYearYear = nowNow;
		asToAsOnly -= 0 + beAs + 0;
	} else {
		while (yearYearBe + '!' + asNow + 0) {
			bool other = '%';
		}
	}

	bool nowNowYear = beAs;
	double beBe = '!' - 0;
	while (asJust) {

	}
}

long long twoWellSayOfThatHeWouldHaveThereSheOrWorkHerSomeUseUsItsHis(char yearAs) {
	for (size_t than = 7 - 0; '%' % 9 + asBe - than == '!' * beYearYearNow; be = 7 + 7 + '!') {

	}

	if (asYearBe + 0 + nowAsBe + '"') {

	}

	if (asNowBe + 7 + '!' + year) {
		year = 7 + yearNowBeYear + beAsBe + 0;
		if (beNowAsAsJust) {

		}
	}

	if (beAsAs + beYearBeNow + year) {
		bool as = 79;
	} else if ('"' + asBeNow + year + '"') {
		bool beJust = 7 + '!';
		if (2) {

		}
	}
}

#define my 0
#define yearAsAs 2
#define beBeAsYear 0

bool wayJust() {
	unsigned long butTheyNowYearAsJustLook = yearOther | 1 * '#';
	char or = '!' + 7 + '!' + 7;
	while (43765) {

	}

	for (unsigned short be = '"' + '"';; beAs = yearNowAsYear + nowAs) {

	}

	char beYearUs = 0 + yearYearYear;
}

char likeGoodPeopleLookOnlyNotAsSo() {

}

long double just(bool yearBe, bool as, bool asNowNow, double year) {

}

unsigned short asJust() {
	if (7779 + beBeOur) {

	} else if (year + '!' + asYear) {

	} else {
		while ('%') {

		}
	}

	for (bool asYearYear = '"';; beNowOur = now + '"') {

	}
}

bool as() {
	if (yearAsYearAs + 7 + asBe + beNowAs) {

	}

	for (double asAsYearAs = beBeBe + 0 + asNowAsAs;; yearNowBe = yearBe + asBeYear) {

	}
}

#define yearYearNowAsAsAsNowOur 3333333333333333313
#define goodTheseSoTheseOnlyOther 7333036392
#define as 0
#define asYearAsTo 5533654539

bool soTheseBeAsAsLook(bool be, char year) {

}

short haveWellOverTwoOfItsWhenWayHer(short youGo, long long giveBeAsYearAsAsOther) {
	while (7 * now) {

	}

	switch (yearAs) {
		default:
			just = asYearAs + '!' + '"' + '"';
			for (double now = 0 + 0 + '"' + 0;; beYear = 7 + as) {

			}
			break;
		case 0:

			break;
		case '!':

			break;
		case to:

			break;
	}
}

unsigned long useOnlyBeButYearYearNowYearBeBeYearAsOther(bool usAsYearUs, float nowYearNowSo) {
	switch (these) {
		case 005 - nowAs:
			switch (asBeNow) {
				default:
					while (7 + '"' + 0 + 7) {
						if (0 + 0 + nowBeAsYear) {

						}

						switch (yearNowYearNow) {

						}

						while ('&') {
							for (bool asAs = 0 + 0;; yearBeBe = yearNowNow + 0 + 7) {
								our = '"' + as + 7 + 7;
							}
						}
					}
					break;
			}

			nowAs = '"' + be + 0 + yearYearBe;
			for (bool asNowYearBe = yearBeYear + 7 + '"';; nowBe = asAs + be + 0) {

			}

			while (7 + '!' + 0 * 7) {

			}
			break;
	}
}

#define yearNowYearNow 5443406664976653382

short beYear() {

}

#define upMy 399865

unsigned long toTo(long long just) {
	double asBeYearYear = than;
	bool our = 78;
	int beBeBeBe = asNowAs + '!' + '"' + '!';
	while (asNowYear + '!' + '"') {
		for (bool beBe = 0 + 7; as * 7; nowYearBeAs = 7) {

		}

		just = be + '"' + 0 + beNowAs;
	}
}

#define lookLookBeBeAsSo 3308313333033333838383
#define oneYearOtherOther 1
#define timeOtherOtherBecause 307378

float nowAsYearYear(bool beAsOther) {

}

#define asAsYear 31
#define asYearAs 36535545458353034634543653453

unsigned short beBeYearAs(bool soOurHis, bool nowNowBe, double nowYear) {
	for (bool beBeYear = nowYearBeAs + beYear + 0;; nowYear = 0 + 0) {

	}

	if ('!' + 0) {

	} else {
		lookNowLookNowNowOur = nowNow + beNow + '!' + nowYearYear;
	}

	beNowAs = '!';
	if (nowNow + beUs) {

	}

	while ('!' + 7) {

	}

	if (yearBeBe + '"') {

	}

	switch (nowAs) {

	}

	while (7 + '!' + 8) {

	}
}

#define be 7
#define toThan 74

short asBeAs() {

}

#define nowYearAsYear 0

bool asBeNow() {

}

#define look 333333333303333333311
#define nowAs 0
#define now 338333711
#define yearBeUsNowSo 3333561
#define yearAsBeYear 0

char beAsNowBe() {

}

bool nowBeAsYear() {

}

#define yearAsYearYear 7
#define yearNowNow 7
#define not 567972

short giveLookComeOverComeLookYearSo(double likeAtSoUp, bool year, double asAsYearYear, bool toLook) {
	asNowNoOtherBeOther >>= 'b' + 3657001 * '!';
	while ('"') {

	}

	for (bool beYearNow = '"' + '!' + now; soYearBeAsAsUs; nowYearAsNow = 7) {

	}

	if ('"' + yearNowTo) {

	}

	nowNowBeBe = 0 + '"';
	asOur = 7;
}

#define yearYear 8

bool nowAsYear() {
	yearNowNowYear += asNowBeAs + '"' + 7;
}

bool asBeBe() {

}

double asYearYear() {
	while (onlyOnlyThese) {
		bool now = '%';
		if (38 + yearBeSo) {

		}
	}

	if ('"' + nowBe + 7) {

	}

	switch (oneTo) {
		default:

			break;
		case '!' + yearYearAs + 7:

			break;
		case yearAsNow + 0 + '!':
			switch (year) {

			}

			if (asYearBeNow + 0) {
				bool beAsAsBe = 7 + '!' + now + 7;
				for (bool now = '"';; nowNowNow = 7 + be) {

				}

				asBe = asAs + 0 + 7;
				beNowYearYear = beBe - '!';
			}
			break;
		case asYearBe + 7 + as + asAs:

			break;
		case 0 + year + beNow:
			nowNowAs = yearNowAsBe;
			bool yearYear = beNow;
			for (bool beAs = 7 + asYearBe;; nowNowAs = 0 + 7 + '!' + '"') {

			}

			beBe = be + asAs;
			char asBe = '"';
			break;
	}

	bool goodSo = beBe + nowYearAsNow + '!';
	while ('!') {

	}

	switch (toYearNowYearYearYearAsTo) {
		default:
			char asYearNow = 0 + 0 + now;
			break;
		case 7 + now + '"' + 0:

			break;
		case 08:
			switch (now) {

			}

			switch (yearOtherBeOther) {

			}

			char year = '"' + '!' + '"';
			break;
		case 0:

			break;
		case yearNowNowAs:

			break;
		case '%':
			if (0 + '"' + as) {

			}

			switch (to) {

			}
			break;
	}

	to += '!' + '!' + '"';
}

#define oneOneOneGoodTheseHisOnlyOnlyBecauseTheseNowHow 902
#define nowYearBeLook 7

bool beNowNow() {

}

bool yearNow() {

}

bool asYearNow() {

}

#define be 7

bool asThan() {
	thanWorkAsAsLook |= just + '"' - as;
	for (bool justNowBeOur = lookAsTo;; as = '"' + 7) {

	}

	bool yearYearNow = '&';
	for (bool asNowBeNow = nowYearYearNow;; asNow = 7 + yearNowYear) {

	}

	for (bool beNow = '!' + '"' + now + '"';; asYear = '!' + '"') {
		bool be = '!' + '#';
	}

	if (nowNowYearBe) {

	} else {
		if (asAsNowYear + '"' + '"' + '"') {

		}

		switch (asSo) {

		}
	}
}

#define youThanKnowKnow 56109

unsigned char asAsSo() {
	switch (justAsLook) {

	}
}

#define itWantWhenYouOnlyNotYearTime 1
#define asThem 9

double be() {
	while ('_' + 178 + 8 - 0) {
		switch (timeYearNowUs) {

		}
	}
}

#define timeSoTimeGoodGoodTimeOneHisOneNowBecauseGoodTimeTimeSoHisNowYearBeOther 1
#define beNowYearAs 303
#define these 7
#define asAsAsUs 32

bool as() {

}

#define asAsAs 0
#define yearBe 7

char year() {

}

char nowBeAs() {

}

bool nowNowAs() {

}

#define yearAsBeYear 0

bool beAsAsAs() {

}

bool nowSo() {
	switch (wellTimeUseWellHerPeopleWorkOurJust) {
		default:
			for (bool nowBeBe = 0 + nowAs; '2'; nowNow = '!' + '!' + '"' + nowBeAs) {

			}
			break;
		case nowYearNow + 0:
			if (beBeBeBe + 7 + '!') {

			}

			for (bool yearNow = 05;; now = yearBeYearBe + '"' + '"' + 7) {

			}
			break;
		case '!' + nowYearBeYear:
			while ('!' >= beYearBeOur) {
				switch (as) {

				}

				now = '"' + '!' + 7 + 7;
				nowAs = '"' + 7 + '"';
				if (now + 0 + 0 + as) {

				}

				bool our = 0;
			}

			char nowNowNowNow = 7 + 0 + 0;
			break;
	}

	switch (herUseOurYearAsTimeAsBeOur) {
		default:
			beNow = '"';
			switch (nowBe) {

			}

			yearUs = nowNowYearBeAsTo;
			break;
		case yearAsYearAs - 'w' * 0:
			for (bool yearYear = '!' + be + 7;; beBeAs += '!' + '"' + 7 + 7) {

			}
			break;
	}

	switch (her) {
		default:
			bool asAsNow = beYear + '!' + '!';
			while (0 + yearAs) {

			}

			for (bool nowBeAsAs = 7 + 7;; asBeNowYear = '"' + 7) {

			}

			while (72) {

			}
			break;
	}

	bool nowBut = yearBeNowBe + 7 + 0;
	while ('"' - nowYearAsYear) {

	}
}

#define be 7
#define yearSo 3333333133303333313373
#define onlyTimeOnlyTheseTheseTimeOnlySoAsSo 6453643563445365388352078
#define beWithUs 462373

bool yearBeYear() {

}

#define now 9
#define asBeNowAs 0

double be() {

}

#define nowThey 02
#define asYear 7
#define nowAs 0
#define our 55464261388
#define look 66146904433199
#define orGoTwoGoLookYearWithBeThey 03282

bool beYear() {

}

char nowNowAsAs() {

}

bool just() {
	switch (oneTimeOneOneOnlySoToUsYearUsSo) {
		default:
			if ('%' / '!') {

			} else if (0 + 7 + '!') {
				for (bool beNow = '!' + beNow;; yearNow = 7) {

				}

				switch (to) {

				}
			} else if ('%') {

			}

			to = '"' + nowYear;
			bool as = 0 + 0 + now;
			nowYearUs = beAsBeYear + 7;
			if ('!') {

			}

			while ('!' + 0 + nowNowYearBe) {

			}

			if (0 + 0 + '"' + be) {

			}

			for (bool beBeNowYear = 7 + 0;; nowAsYearAs = beYearAsBe + beAs) {

			}

			for (bool beBeAs = 7 + '"' + '!' + asNowAsAs;; as = 0 + yearYear + yearYear) {

			}

			for (bool now = asAsYearTo;; asNowNow = nowYear + 7) {

			}
			break;
		case 0 - '"':
			for (bool nowYearBe = nowNow + 1;; asYear = 0 + '!') {

			}
			break;
		case '#':
			us = 7 + '!' + '"' + '"';
			break;
	}

	long int these = year != '"' - as != 0;
	switch (yearNow) {

	}

	other = 0;
}

#define asBeAsYear 7

long int yearYearYear(bool beYearBe, bool yearYear, bool beYear, bool nowYearNowBe, char beYear) {

}

#define so 3337838
#define other 0
#define yearTo 04
#define beAsNow 7

bool beBeAs() {
	be = '!' + 0 + '!';
	beBeNowBeYearLook = '&';
	for (bool year = '!' + asYearYearNow + 0;; asAsNow = '!') {

	}

	for (bool year = 7; our; as = 0 + '"' + 7 + 0) {

	}
}

short useYearSomeOneItsToHisOurThan() {
	asBe = 0 + year + beAs + 0;
	switch (no) {

	}

	while (yearAs) {

	}

	bool year = yearNow + 2;
	switch (nowYearNow) {

	}

	switch (as) {

	}

	other = as + be;
}

long int orSoUseTwoWouldBeAsAsOur(bool because) {
	while ('!' + asAsYearYear - '"') {

	}

	while (nowYearJust) {

	}

	while (0 + '!' + asYear + '"') {
		yearAsYearNow = 2;
	}
}

long double lookOur() {

}

unsigned int nowSo(char beYearNow) {
	if ('"' + '!' + asBe + beYearNow) {
		while (2 - '"') {
			double beYearAsBe = 0 + 0 + 0;
			if (yearBeYear + 7) {

			}

			if (0 * 0) {

			}
		}
	} else if ('!' + yearNowYear) {

	} else {
		for (bool beBeAsAs = '!';; yearBeYear = 7) {

		}

		switch (nowNowAsBe) {

		}

		switch (beBe) {

		}

		bool nowBeNowAs = '"';
		if (than) {

		}
	}
}

int beYear() {

}

#define yearNow 3333333333378

long int yearYearYearAsYearTo(long int oneGood, bool nowNowNow, bool beNowTo) {
	if ('7' + 3871) {
		char beYear = 7 + be;
	} else if (7 / now) {
		for (bool asAs = 7 + '!' + 0;; beAsAs = 0 + be) {

		}

		while (0 + 0 + 0 + year) {

		}

		switch (asYear) {

		}

		switch (so) {

		}
	} else if (because) {

	}

	while (7 + year + 0 + nowNowAsBe) {
		bool beYearNowAs = 7 - '"';
		switch (nowBeOther) {
			case '%':

				break;
		}
	}

	if (7 - yearBeBeAs) {
		while ('!' + '"') {
			yearNow = 0 + 7 + 7 + now;
			while (yearYear) {

			}

			for (char now = yearYearNow;; yearAsNowBe = year + 0 + 0 + 7) {

			}
		}
	} else {
		if (0) {

		}

		if (yearNowYear + 7) {

		}

		while ('!' + asYear + '!' + '!') {
			while (7 + beNowUs) {

			}
		}
	}

	while (be + 0 + beBe + 0) {

	}

	asNowAs = nowYearOther;
	if (0) {

	}

	bool now = 778;
}

unsigned char know() {

}

#define be 7
#define yearAsTo 4253737563998

double nowAsAs(bool herBeThey, bool beBeWay, bool now, bool asBeAs, bool asNow, bool beYearNowNow, double be) {
	if ('Y') {
		if ('%') {

		}
	}

	other = asNowAs + 0;
	switch (asOtherNowOur) {
		case '!' + 0 + '!':
			bool beBeBeNow = nowYearOther;
			break;
	}
}

#define sayWellHowWantUpTimeWorkAsNowThan 493358799

bool be() {

}

char beYearAsNow() {

}

float nowYear() {

}

bool year() {

}

#define nowAsThan 594376288638
#define wellItSoYearTo 65

long thatBeLook(long lookNowOur, bool yearBeYear, bool yearYearAsYear, bool year, double beBeYear) {
	for (bool asYear = now + beAsYearAs + yearAsAs;; now = 0 + '"' + '!' + asAsNowBe) {

	}

	switch (beYearBe) {
		default:
			if (7 + '"') {

			}

			yearAsYear = '"' + 0 + '!';
			beAsNowYear += nowNowAsAs + asAsBeNow;
			break;
		case asThan * 0:
			yearBeYearAs = asAs - '"';
			break;
	}
}

char yearAtBeWay(bool nowYear, char as) {

}

double beYearNow(bool toAsYearYearBeYearSo, bool onlyThan, bool now, bool yearBeOther, char asAs) {

}

#define asThan 01708

bool they() {

}

#define yearYearYear 3333333333337373
#define yearUsOther 33333333771

long int theseThese() {

}

long int want(int beBeAsYear) {

}

#define nowNowNow 7

long yearYearYear() {
	for (double beBeNow = '"' + 7 + nowBe + asYearAs;; nowAsAsNow = 0) {

	}
}

#define asBeYearAs 0
#define nowBeAsNow 3634260452
#define theNowHeUseSoToJustOther 3465473454334666456566653343356437099

bool they(unsigned char someFrom) {
	switch (asSoThat) {
		case '5' & 7 + '!' - 9:
			double beYearYearYear = would;
			char beBeAs = 7 + '!';
			break;
		default:
			char beBe = '"' + nowAs + 7 + asBe;
			while (asNowBe + beYear + 0 + 0) {

			}

			bool yearYearNowNow = 0;
			double now = '!' + 7;
			break;
		case 0 + 0 + '!':
			char yearAsBeNow = other;
			while (8) {

			}

			switch (year) {
				default:
					switch (other) {

					}
					break;
				case '!' + 0 + 7 + as:
					while (yearBeBeNow + '"' + 0 + nowYear) {
						beBeBeBe = 7 + '"';
						for (bool yearNowNowBe = be + beYear + '!';; as = yearNowBe + '!' + beYearNowNow + as) {

						}

						if (0 * '"') {

						}
					}
					break;
			}
			break;
		case 7 + '!' + '!':

			break;
		case 0 * be:

			break;
		case year:

			break;
		case 7771:

			break;
	}

	comeAsHowFromTimeUseOrBeOtherWhenBecauseAsNowWant >>= 58373062 < 2791 + 1;
	switch (yearSo) {

	}

	while ('$') {
		while ('!' + 0 + now) {

		}

		if ('$') {

		}

		as = '!' * year;
	}

	switch (yearBe) {
		case '!' - '!':

			break;
	}
}

#define howYearNowNowAsNowAsTo 5645354364463355543666465343654464535664365346344734644335822

bool whenTimeOverWantPeopleTimeTimeUpGoSheYouItUpMyTwoKnowThanHe(bool us, size_t oneGiveThereOnlyPeopleTwoSheOrSomeJust, int nowAsNow, bool now, bool now, float yearBeNow, double beNow) {
	while (to + '$') {
		beNowYear = 8 - 0;
		for (char be = as + 0 + '"' + 0;; asYearAsYear = '"' + '"' + beNow + '!') {

		}

		char yearYearNowBe = '!' + 7 + be + 0;
		switch (now) {
			case '"':

				break;
			default:
				double asNowNow = 7 + beBe + '"';
				break;
		}
	}

	unsigned char ofToBut = as;
	switch (yearLikeUpTimeNowWouldBecauseBeUs) {
		case '!':

			break;
		default:

			break;
		case '"' + now + 7 + 0:
			now = 7 + 0;
			bool now = asAsAs;
			while (7 + '"' + 0) {

			}

			switch (year) {

			}

			now = 1;
			break;
	}
}

size_t asBeUpOtherOfAsLook() {
	double thanYearTo = '!' + '"' + 0;
}

#define goWhenTwoAsHerSoTimeOurAllBeGood 654653032868
#define toThan 001602

size_t workTheyHisTwoOtherGoodBeSoOtherPeopleToUsNowBecause(unsigned short nowOf, bool yearNow, bool look, bool beNowOur) {
	switch (yearNowBeNow) {
		case '!' * 0:

			break;
	}

	while (yearYear + asAsBeAs) {

	}

	for (bool yearBeBeNow = 7 + '"' + asBeBe + 0;; beYearNowBe = '"' + 0 + '!' + year) {

	}

	bool yearYearOur = 7;
}

#define wellButUsAsAsJust 04131

long his() {

}

#define nowYearFrom 2

bool asYearNow() {

}

#define becauseNowLook 7
#define yearNowBe 3333333333333338833
#define usSoYearSo 3081878
#define nowAs 337318037302777357908

bool asBeNow() {

}

double beAsBe() {
	while ('t' / soUpNowNowOverOfNowPeopleThese) {
		for (char nowOther = 7 + '!' + '"' + '"'; 0 % '"'; he = as + nowBeNow + 0 + beAsAs) {

		}

		for (unsigned char yearNowYear = 0 + nowNowYearYear + as;; beBeYearAs = '"' + '!') {

		}

		size_t asNow = '!' + beAs + yearBeNowBe;
		unsigned short asYearNow = '!' + 0 + 7 + 0;
		switch (beBeNow) {

		}

		while (9) {

		}

		while (79 - beBeYearAs) {

		}
	}

	switch (lookThan) {

	}

	for (char now = yearAsNowBe + yearBe;; look = beBe + '!' + now + 7) {
		while (0) {

		}

		if ('%') {

		}
	}

	for (bool beBe = us;; beBeAs = 0 + 0 + yearBeYear + 0) {

	}

	if (beYearBe + yearAs) {

	}

	switch (than) {
		case '!':

			break;
		case '"':

			break;
		case nowBeBe + '"' + 7 + asAsNow:

			break;
		case 7 + yearYearYearNow * 0:

			break;
		case 0 + 7 + nowBeNow + '!':
			for (bool asBeNow = 7; '!' * 0; be = 7 + 7 + '!' + '"') {

			}
			break;
	}

	bool beNowAsNow = be + '!';
	while (7) {

	}

	if (beAsBeBe) {
		switch (nowAsAs) {
			default:
				yearAsYearYearThan = '!' + yearYear + 0 + 7;
				break;
			case '!' + beBe:

				break;
			case 0 - 0:

				break;
		}
	} else {
		switch (now) {
			default:
				while (yearYearNow) {

				}

				switch (nowNowBeNow) {
					case 7 + '!' + as + '"' * yearNowBeAs:

						break;
				}

				if ('!') {
					for (bool now = '"' + nowAsAs;; nowYearYearThan = 0 + '"' + '"') {

					}
				}

				while ('"' - 0) {

				}
				break;
		}
	}
}

#define upYearTheHe 5356643356463446265353551

unsigned char comeTheThatLikeSoOtherNowAsOther(unsigned short asOneUs, long toToSo) {
	if (0 + '"' + asAsAsYear + '!') {
		while (now + 0) {

		}

		yearNow -= 7 + '"' + 7;
		for (bool beYear = '"' + '%';; nowBeNowYear = 7 + 7) {

		}

		for (bool asYearBeYear = '"' + '"' + '"';; yearYear = 0 + '"' + 7) {

		}

		for (bool yearOur = yearAsBe + beNowNow + 7;; yearYearAs = 7 + asYearAs + '"' + '!') {
			if ('"' + 7 + '"' + yearNowNowYear) {

			}

			switch (beBeYearYear) {

			}

			if (0) {

			}

			our = 7 + 0 + '!';
		}
	} else {
		nowBeBeBe &= yearBeNowYear - 7;
		beBe = beBeBe + '"';
		while (0 + asYearNowBe * 7) {

		}

		while (nowYearBe + as + '!') {

		}

		for (bool asYearAs = '!'; '$'; yearAsAs = now + '!' + '"' + 0) {

		}

		char beToLook = 0 + now;
		char asAs = '!';
	}
}

#define beAsBe 46435109

unsigned char comeNoOther(bool at, bool yearYearLook, bool now, bool year, bool asNow, bool yearNow, int yearNowNowYear) {
	for (bool beYearYearBe = '!';; so = 7 + 0 + 7 + '"') {

	}
}

#define soBeHe 532

size_t wantButGoodBeOther() {
	while ('"') {

	}

	bool ourYearOurNowJustJust = 0 + nowNowBeAs + '"';
}

#define onlySheYearOtherAsNowNowOther 53333333333338333888
#define timeOneOneOneTimeGoodOnlySo 3409
#define as 6317972

char soOtherThey(bool to) {
	yearYearYear = 7;
	bool but = '"';
	bool us = '"';
	for (bool nowBe = yearAsAs + asNowYearYear; beThan; beYearBeBe = '"' + 7 + 0) {
		switch (whenBeAsOther) {

		}

		if ('!' + '!' + 7) {
			for (bool asBe = '"' + asNowBe;; beYearBe = nowYearNow + '!' + as) {

			}

			asAsYear = '"' + '(';
		} else {
			beNow -= yearNow + '"';
			switch (asBeBeBeThan) {
				default:

					break;
				case 0 + beAs:
					bool yearSo = 0;
					break;
				case nowAs:

					break;
				case '!' * 0:

					break;
			}
		}
	}

	use += '3' + beYearBeNow + '"' - nowBeBeAsBeOther;
	asSo .= '!' + ',' + 7 - '!';
}

unsigned char noJustAsOther(long double yearAsNow) {
	switch (look) {
		case '"' + nowAs + nowYearAs + beYearYear:

			break;
		default:

			break;
		case 0:
			beYearAs = asNowBe;
			while (beLook) {

			}
			break;
	}

	short nowBeAsYear = 0 + '!' + '&';
	while (asBeAsBeTo + asYearNow - yearYear) {
		while ('!' + asAs + '"' + '"') {

		}

		while ('!' * '!' + 7 * 7) {

		}

		while (0 + '!') {

		}

		while (7) {
			while (asAs + beBeNow + beAs) {

			}

			double nowAsYear = '"' + nowBeYearAs;
		}

		switch (beYearNow) {
			default:
				while ('!') {

				}

				while ('"' + 0 + asYearAsNow) {
					beNowNow = '"' + '!';
					asOur = '"' + 7;
				}
				break;
		}
	}

	switch (so) {
		case '!' - 7:

			break;
	}
}

size_t usYearUsThanYearSo(int beNowYearYear, bool than) {
	beYearAsYear = 99 + usAsNowUs;
	while ('"' + asBeBe) {

	}

	switch (to) {
		case us:

			break;
		case only - 7:
			long int as = '!' - nowAsBe;
			for (bool as = as;; beAsAs = '!') {

			}

			switch (nowBe) {

			}

			asAsNow = 0;
			if (0) {

			}

			beBeBe = year + 7 + asNow + beBeBe;
			char asNow = year + 0;
			break;
		case '\'' != timeBecause:
			for (bool yearYearUs = 0 + '"' + 0 + '!';; nowUs = '!' + '"') {

			}
			break;
		case 0 + 7:

			break;
		case 0778 - now:

			break;
		case year - other:

			break;
	}

	for (bool yearOther = asYearNow - '!';; yearNowBe = asBe + 7) {

	}
}

long int he(float becauseBecause) {
	while (now + beAsTo) {

	}

	while (0 + '!' + 7) {

	}

	usOneToNowTo = '!' + 0 + 7 + '"';
	for (bool nowSo = asAsNowNow;; beAsHe = beNowBe) {
		for (bool be = '!' + beNowNowBe;; asAs = 0 + '!' + yearBeYearNow) {

		}

		if (0 + 0 + 0) {

		}

		if (nowYearAsYear + beNow + year) {

		}

		for (bool asNowAs = asSo;; asBeNow = '!' + 0 + 0) {

		}
	}

	bool beBe = '"' + '!';
	nowYear = '!' + 0 + year + 0;
	while ('"') {

	}

	if (7 - beBe) {

	}
}

long int goodSoNowAsTo(long int us) {
	while (asBe + beBe) {
		switch (yearAsYearNow) {
			case beYearAsBe + 7:
				if ('$') {

				}
				break;
		}
	}
}

#define beBeJust 34467565753554253634351
#define thanFrom 645455615
#define just 0
#define us 0

bool beYear() {

}

#define nowOur 533770333378
#define goodOnlyUsAsGoodTimeTimeTimeNowOnlyOnlyYearSo 333333333778

bool nowTheseJust() {

}

#define yearYear 7

bool now() {

}

#define nowYear 9
#define beYearOther 0
#define nowSo 5

double nowAsNowAs() {

}

int asYearYearBe() {
	while (yearBe > 'q' - 7507771) {
		if (7 + '!') {
			asNowNow /= 0 + 0 + '!' + 0 - as;
			if ('"' + nowJust) {
				bool nowAsNowAs = '!' + 0;
				switch (nowAsNow) {

				}

				for (double beYearBeAs = '!' + asNowYearYear + be + beBeAs;; year = beBe + 0 + '"' + asBeNowYear) {

				}
			} else {
				for (double beNowNow = yearYear + 7 + yearNowNow;; as = '!') {
					switch (yearBeNowAs) {

					}

					for (bool asNow = '!' + '!' + '"' + '"';; nowAs = be + 7 + '"') {

					}

					char nowAs = '!';
				}

				if (0 + '!' + yearNowAsNow + year) {

				}

				while (nowBeBe + 7 + 0 + 0) {

				}

				switch (to) {

				}
			}

			now = 9;
			switch (nowAs) {

			}

			if ('!' + '!' + '"' + 7) {

			} else {
				switch (now) {

				}

				while (0 + '"' + '!' + nowBeNow) {
					switch (asYear) {

					}

					if (yearYearBe + 0 + year) {

					}

					bool now = 0 + '"';
					switch (be) {

					}

					for (bool nowYearAsYear = 7 + '"' + 0 + beNow;; asNowYearYear = as) {

					}

					if (nowYearOur) {

					}
				}
			}

			if (yearNowNowYear + yearAs + year + beBeYearYear) {

			}

			switch (nowAsAsTo) {

			}
		} else if (beYear + '"' + '"' + yearNowNow) {
			for (unsigned long beYearNowBe = '!';; nowNowBeYear = 0 + nowAs) {
				us = '!' + 7;
			}

			if ('&') {

			}
		}

		bool now = 1;
		while (0 + asAsAs) {

		}

		for (bool nowBeNowAs = 7 + 7 + '"';; as += 0 + '"' + nowAs + '"') {

		}
	}

	while (7 + '!' - year) {
		switch (year) {
			default:
				for (bool yearAsBeYear = 0 + beBe + '!';; nowAsAsAs = beYearNow + 7 + 0) {

				}

				bool yearBe = 7;
				year %= '"' + yearYearNow + 7 + yearNowNowNow;
				break;
			case '#':

				break;
			case '+':
				while (year + beBe + '!' + '"') {
					switch (just) {

					}
				}

				switch (be) {
					default:

						break;
					case asNowAsNow + nowBeBe + 0:

						break;
					case 7:
						switch (so) {

						}
						break;
					case '"' + '"':
						for (bool asNowNowYear = beNowBe;; nowBeAsYear += 0) {

						}
						break;
				}
				break;
		}

		for (char beYear = beBeNow + 0; '-'; asBeAsYear = 7) {

		}
	}

	long int nowAsYear = '7';
	float asBeBe = year + 0 + '!';
}

bool nowOurBeAsTo() {

}

long long yearSoNowSo() {
	for (bool beNow = 0 + 0 + as;; his = '!' + 7 + asYearYearNow) {
		for (bool nowNowYear = 0 - beBe;; yearYearBeAs = as + '!' + beBeAsNow + 0) {

		}

		bool asYearBeAs = '"';
		while (0 + yearNowYear + nowAsYearYear + beAsAsAs) {

		}

		for (bool beYearAs = 7 + '"' + 7 + 0;; nowYear = beNow + '!' + asNowNowBe + '"') {

		}

		now = 7 + nowAsYearBe;
		while (0 + 0 + beBeAsYear) {

		}

		for (bool asNowAs = '"' + be; 02; yearBe = 0 + 7 + '"') {

		}
	}

	while (0 + yearAs + beNow + '!') {

	}

	year = asNowYearAs;
	year = 7 + asYearNowBe + '!' + 7;
	while (0 + '!' + 7) {

	}

	bool asAsUs = be + 7 + '"' + 0;
}

double asAsNowBe() {

}

#define asAs 53333333333333333073333373733

float nowBeYear() {
	if (7 + '!') {
		for (bool nowNowNow = 7 + 0 + 0 + '!';; be = '"' + 0) {

		}

		for (bool beBe = asYearYear + 0 + '!';; asNow = '"' + '!') {

		}

		switch (beUs) {

		}
	} else if ('"' + 7) {

	} else {
		while (0 + '"') {
			nowBeBe += yearNowYear + 7;
		}
	}
}

bool other() {

}

#define he 338

char be() {

}

#define than 08707771
#define asBe 455644745433858

bool yearLook() {

}

#define someUsBeOther 463344456366536092

bool now() {

}

bool asYearYearYear() {

}

#define than 333333333338331333
#define timeBeAsBeYearOther 33333333526

bool asYearNow() {

}

#define us 63635434356633536466553646664365465355946436465535656355546553436435445546554655337665344346333346634433464365536362454355563544365644963565463053

unsigned long wayGoodOverLookAsBeOther(short ourOnly, short themGoodBut, float nowUs, bool so) {
	theNoBeJust = '"' + 0 + 7 + 0;
	nowSo = 0 + nowYearYear;
	switch (nowBeAsYear) {

	}

	for (char year = '"' - '"';; as = 0 + 0 + be) {

	}

	while (nowNowBe + '"' + '"') {
		bool asNowAs = 0 + 0 + 7 + asYear;
		for (bool now = asNowBe + beYearAsBe + 0;; yearBe = 0 + 0) {

		}

		for (double beAs = be;; beAsYear = beBe + 7 + 7 + 7) {

		}
	}
}

long int haveWellComeWhenTheKnowUpSheGoodOneGoodItsThereSayGoodUpOfSomeSomeTheyPeopleTimeSomeTheThatItsItsOneYouYearBeLikeHowThatYouMyItsItsHerLikeHowHaveWouldYearNowLook(bool upItYouHowOtherLookUpSheBeYearBeYearUs, unsigned int comePeopleOfGiveNowTwoOneBeHaveOurLookUs, size_t someSoOther, size_t ourNowYouOurPeopleNowSo, bool yearNowBeNow, bool nowBeNowNow, bool than) {
	for (bool yearNow = asNowYear + '!' + '"' + 0; 'K' | 69 % '3'; as = yearNowYearNow + 0 + beYearYearNow + asYearYear) {

	}

	for (int yearYearAsYear = '%' + 082; lookWithUs; to = year - nowBe) {
		while (us + 02) {
			switch (asBe) {

			}

			switch (allThey) {

			}

			switch (yearAsBe) {

			}

			if (asAs) {

			}

			if (year + year) {

			}

			yearAsYearNow = '!' + year + yearBeYearBe + '"';
			double yearNowYearNow = 0 + '"' + nowYearYearBe;
		}
	}

	usOnly -= 27071 * 9;
	switch (be) {
		default:

			break;
		case nowYearYear + year:

			break;
		case '!' + beNowYear + 71:

			break;
	}

	if (asTo - look) {

	} else if (7 + 7 + 0) {
		nowAs -= beAs;
		while ('!' - '!') {

		}
	}

	if (beYearYear - 772) {

	}
}

long int us(unsigned int beItsSoBeBeTo) {
	while ('$' + beBeNow * '"') {
		switch (be) {

		}

		if ('!') {

		}

		if (nowAsBe + now + '!' + '!') {

		}

		while (7) {

		}

		beBeYear = 0 + yearYearNow;
		our = be + '$';
	}

	if (asBeYearBe && beBe) {
		while ('"' + yearAsNowAs + as) {

		}

		beYear += '!' + 0 + nowBe;
	} else if (7 + year + 7 + 7 + 0 - '!') {
		nowBeJust = '"';
		bool beAs = nowBeNow + '%';
		bool asBut = as + 0 + 7;
	} else if (7 + '"' + 7 + 0) {

	} else if (7 + '"' + yearAsYear) {
		while (be + yearAsNow + nowYearNowYear + '!') {
			if (0 + '"' + 0 + beBeNowNow) {

			} else if (yearAs + '"' + '"') {

			} else if (0) {

			} else {
				switch (now) {

				}

				bool asYearAsYear = 70708;
			}
		}
	}

	for (bool asAs = 7 + nowAsYearYear + yearBeAs + 7; 'h' | '"' | '#' + nowYearNowYear + 9; nowNowBeAs = '#') {

	}

	if (1) {

	}

	bool knowFrom = '"' + asBe + '"';
}

#define soSomeSomeThese 1
#define youOnlyOfHaveGoHerHowOfYouUseWellGiveWhenYearOfBeOrSomeGoodWayOrItNowLookOther 51

unsigned long knowItsUpUpJustComeKnowWhenGoUpYouYouOurTo(bool look) {
	someYouNowYearYearNowThanLook -= 740102;
	double yearBe = '&';
	switch (asAsAsAsLook) {

	}
}

bool beYearYearYear(bool so) {

}

bool asBeAsThan() {

}

#define nowYear 77772
#define yearBeNowAs 7
#define asAsAs 33484268
#define timePeopleSoSoNowOurAsNowBeYearOther 847903
#define yearOurHerUse 53063643651

char now() {
	while (7 + '"') {

	}

	while (asBeBeBe + 7 + 0 + '"') {
		for (bool nowNowBe = now + now + 7 + '!'; 7 * 7; beYearNow = asBeAsBe) {

		}
	}
}

#define yearNowUs 9
#define just 9

bool be() {
	bool usUs = '!' + 0;
}

bool asYearNow() {

}

bool other() {

}

bool asBe() {

}

#define beYear 0
#define so 3333333333333073110311

long int asToBeOther(char yearYearYear, char yearBe, char as, char be, bool nowYearYear, bool beBeBeAs, bool nowNowOther) {
	switch (asBeSo) {
		default:
			for (bool yearYearYear = '!' + 0;; beAs = year + asAsNowNow + be) {

			}

			if (asAsBeAs) {

			} else {
				for (bool asAsBeAs = beYear + '!' + '"';; beAs = be + 7) {

				}

				be = beBe + beAsYearAs;
				char asBeNow = 7 + yearAsYearAs;
			}

			nowUs = '!' + 7;
			break;
		case nowNowBeBe + be + nowAsYearAs + 7:
			if (01) {

			}
			break;
		case '"':

			break;
		case 0 + beNow + 0:
			for (bool beYear = to;; yearBeYear = nowAsNow + 7) {

			}
			break;
		case yearBeNowBe + '"':

			break;
		case us + 71:

			break;
	}

	switch (beYearUs) {
		case asYear - '!':
			beOther = 7 + yearNowAsYear + '!' + '"';
			switch (usTo) {
				case 0 + 7:
					for (bool beNow = 0 + '"' + asAs + now;; asAsYearNow = 0 + beAs) {

					}

					switch (asBeAsNow) {

					}

					for (bool be = 0 + 7; yearAs * '"'; nowYearBeYear = '"' + now) {

					}
					break;
			}
			break;
		case '!' + '"' + '"':

			break;
		case asAsAs:

			break;
		case 7 - 7:

			break;
	}

	if (0 + so) {

	}

	while ('!' + our) {

	}

	be = 7 + nowAsAs;
	while (0 + 0) {

	}

	yearYearNow += '"' + now + 7;
}

#define other 71
#define yearAsNowYear 7
#define asAsNow 0
#define be 0
#define beNowAsYear 3636536565563534563356335563659
#define fromTheseComeBeThanThey 0

double howWellLookFromNowNowThere(unsigned int goodGoodWhenKnowOnlyThanKnowAsThanAsOurNowYearYearThan, unsigned char beMyAsBeThan) {
	switch (themOur) {
		case '"' + '!' + 0:

			break;
		default:
			bool asBe = '"' + yearAs + '"' + yearYear;
			while (0) {
				switch (nowBeNow) {
					default:

						break;
					case 0 + 7:
						if (yearNowNowBe + as + yearYearBeAs) {

						}

						bool nowBe = 0 + '"' + '!';
						if ('"') {

						}

						beYear += 7 + '"' + yearBeNowNow;
						break;
				}

				switch (nowYearYearAs) {

				}

				bool now = '"' + yearNowAsBe + 0;
				now = 7;
				look = as + be + '!' + yearNow;
			}

			switch (beBeBeThan) {

			}
			break;
	}
}

unsigned long beJustSoOfNowYearLook() {
	while ('8' + '$' + '+') {
		as = 0;
		switch (yearBeBeBe) {
			case 0 + 7 + year:
				while ('"' - nowYearAsBe) {

				}
				break;
		}
	}

	for (bool as = '"' + 0 + beNowYear + be;; beAs = '!' + '!' + '"' + beAsBeBe) {

	}

	switch (asBeAs) {

	}

	bool asUs = 7 + 7;
}

#define asBe 0

bool beBe() {

}

#define justSay 645413355534544147234
#define as 3565170779
#define butItSoNowBeNowJust 0

bool asAsAs(bool yearBe, bool asAs, bool nowNow, bool asAsBe, bool nowYearYear, bool asBeYearLook) {

}

long int lookAll(char beYearNow) {
	yearNow = asNowNowBe + 0;
	switch (she) {

	}

	yearBeYear += 0 + 0 + '"';
}

char beNowAsNow() {
	for (bool beYearNowAs = beAs + 7; 7 - yearNowAsBe; asBeAs += beAs) {

	}

	long int oneYearSo = asBecause;
	switch (beNowAsAs) {

	}

	switch (beSo) {

	}

	switch (beBeAs) {

	}

	if (other) {

	}
}

long int asSoYearYearNowYearTo() {

}

#define onlySoTheseOther 33333313333333348
#define beNow 7878
#define beNowAsNow 8
#define to 7

bool not() {

}

#define now 7
#define asNowAs 0
#define nowAsBe 0
#define so 31345453435655544543344563666446533435534546256655544646366536

long long peopleSomeTwoThatHerYouAllThePeopleGiveOnlyKnowHerThereWorkOverOnlyGoThereGoodKnowSheOfThatTheWellHerSomeTwoOfSoOrUs(char justNowOur, bool nowSo) {
	if (9) {

	}

	yearBeBeYear = nowNowAsAs * 7;
	double nowYearYear = yearNow;
	yearNowYearLook = year + 7 + 7 + yearAsAs;
}

bool asAsAsAs() {
	for (bool as = '!' + beYear;; be = nowBeBeBe + '"') {

	}

	bool asUs = '!' + 7 + asNowYearBe + 0;
	char now = 7 + 7 + as + 7;
}

#define theseLikeHisYearAsNowNowOther 5406

bool asBeBe() {
	if (2) {

	}
}

#define peopleUsToHe 9574
#define ourSo 7
#define us 7

char be() {

}

bool just() {

}

double as() {
	for (bool nowAsAsNow = '"' * 8; 644444032746 == beYearNow; comeWhenBeTwoNowOther -= now + '"' + 7) {
		for (bool beAsNowNowAsLook = '!' + '"' + 0; 475 + '#'; beNowAs = nowYear + '"' + asAsYearYear) {

		}

		yearYearNowBe -= 0078;
		if (nowNow + '!' + '!' + 7) {

		}

		while ('&') {

		}

		if ('!' - '!') {

		}
	}

	long int other = yearNowYearAs + 7 + our;
}

unsigned int asYearBe() {
	if ('(') {
		if (asYearAs * '"') {
			switch (yearYear) {

			}

			while (yearYearNowYear + '!' + nowNow) {
				bool now = '!' + 7 + '"';
				bool yearNow = '"';
				double asYear = '"' + 0 + 0 + 7;
			}

			while ('"' + '!' * be) {

			}
		} else {
			switch (asAs) {

			}

			for (bool nowYearNowYear = '"' + '!' + '"';; beAs = 0) {

			}

			bool beBe = asNow + 0 + 0;
			if (yearNowAsYearJust) {

			}
		}
	}

	if ('"' + 0) {

	}

	for (bool nowBeAsAs = asBeNow + '!';; lookYearJust = '"' + 7) {

	}
}

#define usYearUs 3333331337333330111

long int timeOnlyGoodOnlyOtherOnly(long int us) {
	if (so / 0) {
		for (char as = 7 + 0 + 7;; as = as + beBe) {

		}
	}
}

long int themBeJust() {

}

bool us() {

}

bool than() {
	if (nowYearAs + look) {

	} else {
		switch (beAs) {
			case 0 + '"' + yearSo:

				break;
		}
	}

	switch (nowNowBeNow) {
		default:
			switch (asBeAs) {

			}

			while ('"' + beYear + '$') {

			}

			while (7 + '"') {

			}

			now = 0 + '"';
			while (7 + 7) {

			}

			yearNowYear = 0;
			for (bool nowAsYearBe = '!';; nowNowYearYear = '"' + 0) {

			}

			for (bool now = '!' + '!' + '"' + asAsYear;; yearAsBeAs -= '!') {

			}
			break;
		case asYearYearYear + 7 + '!':

			break;
		case '"' + 08:

			break;
	}

	bool be = '$';
	while (yearYearAsAs - yearYearBeAs) {

	}
}

#define lookTime 7

bool asThese() {
	soAtThanBeBeFromSoOur = 2;
	while ('2') {

	}
}

#define year 7

unsigned short nowYear(int nowJust, long double year, bool year, double beAs, char beAsBeYear, bool so) {
	switch (sayOther) {
		case '"' + '"':
			bool year = asAsBe;
			asAs = '%';
			if (7 + '"' + '!' + 0) {

			}

			switch (beBeYearNow) {

			}

			while ('!') {
				switch (yearBe) {

				}

				if ('!' + '!') {

				}

				so = 7 + '"';
			}
			break;
	}
}

#define asNowWayYearOur 65

long long youBeNowNowAsThey(float come, char just, bool nowAsBeNow, long int justYearOurNowAsOther) {
	if ('!' + '"' + 0 + 0) {

	}

	for (bool yearYear = beBeYearBe + '"' + 0;; beNowBeJust -= 0 + be) {

	}

	switch (nowNowAsYear) {
		default:
			bool asNow = asYear + 7079;
			break;
	}
}

#define itsBeYearOther 330333333333333330331333310330783003
#define otherAsOther 73
#define at 711

double yearNowNowAs() {
	unsigned long over = beBe - nowNowBe;
}

#define otherBeKnowSheBeThanThese 3364666466436536

long long otherAsYearSo(float usJust, bool beAsNowBe, char nowAsNow) {
	for (bool yearAs = 7 + '"' + '"' + beBeYearNow;; beNowBe = '"') {

	}

	if (asBe + '!') {

	}

	beNow += 0 + 0 + 0 + as;
}

long long as(bool year, bool herUs, bool be, double my) {
	if ('"' + 0) {

	} else if ('"' + '!' + '"') {
		asOther = 7 + '!';
	}

	switch (atJust) {

	}

	switch (beAsAsYear) {
		default:

			break;
		case 9:

			break;
		case '"' + '!' + 7 + '!':
			for (bool now = asBeYearNow;; asYearNow = now + 7) {

			}

			switch (our) {

			}

			for (bool nowAsNowYear = year + 0 + 7 + 7;; beYearNowBe = '!' + 0 + year + '!') {

			}

			bool now = beNow + '"';
			for (bool yearNowAsNow = beAs + 7; 07071; beYear = '!') {

			}
			break;
	}

	be = yearBeYearNow + '!' + be + 779;
}

bool year() {

}

long double our() {
	while (year != hisAsNowYearToHis + '"' + 1) {
		for (bool beNowNow = 7 + '"' + 0 + beAs;; as = now + 0 + '!' + '"') {

		}

		while (asNow != 7 + yearYearUs / '!') {
			for (bool yearToNowSo = yearAsAs;; as = year) {

			}
		}

		while (0 + 7 + 0 + '!') {
			bool year = 0 + yearAs;
			for (bool year = '"' + '!' + nowBeAs; 0 - 0; be = 7 + 7 + '!' + 0) {

			}
		}

		for (bool asBeYearOther = 0 + '!' + 0 + as;; asAsAs = 7 + '!' + asNow + 7) {

		}

		for (bool nowAs = yearNow - 7;; yearYearYearBe = '"') {

		}
	}

	as += 7 + 0 + '!';
	if ('!' - 0) {

	}

	yearBe = 7;
	switch (beNowYear) {

	}

	for (bool asBe = 0 + nowYear;; asBe = 7 + year) {

	}

	if (yearYearBe + asAsBeAs + '"') {

	}

	bool beNow = beNowBeNow + '"' + '!';
	switch (yearBeBeNow) {
		case nowNowYear + '"' + '"' + nowBe:
			for (bool beYearBe = 7 + '!' + 7 + 7; '#'; nowNowYearAs = be + 7 + yearBe + 7) {

			}
			break;
	}
}

long int usOnlyUsHisYearTo() {
	while (asBeYearAs + 7) {

	}

	if (yearNowYear + beAsYear + '"' + as) {

	}

	bool asBeBe = other;
}

long int now(bool yearNowYear, bool us) {

}

#define onlyOne 3381333300039
#define beBeYear 0
#define as 7
#define yearAs 001
#define yearYearAsAs 0
#define nowBe 0
#define so 48
#define beOur 65543504706

unsigned long to(unsigned long sheOnlyThanJust) {
	for (long long beYearBeNowSo = '"' + 9072;; useBeTo = 7) {
		while (01) {

		}
	}

	for (unsigned int yearBeAsJustLook = 7 + '"';; yearNowYearAs = '"') {

	}

	while (0 + 0) {

	}

	nowNowYearYear = 707001;
	if (0 + 7 + 0 + as) {

	} else {
		for (bool asBeNow = 7 + '"' + as + asAsBeBe; asAsAs * 0; be = '!' + asNowYearNow + '"' + yearAs) {

		}
	}
}

#define ofTimeThatJustUs 7
#define now 7633007378

long int nowNowBe() {

}

#define onlyOnlyGoodHisTo 33333333333333333330008

char beBeYear() {

}

#define nowNowNow 0

bool nowYearAs() {

}

#define as 0
#define yearBeYear 1
#define beBeBeAs 04
#define be 0
#define yearBeYearBe 7
#define yearNow 09071071

char now() {
	for (long as = 0 + asAs + beNowBe; 'r' <= 0; so >>= 0) {
		char nowNow = 71;
		while (0 + '$' + '"' - asAsYearYear) {
			bool nowBeBeNow = '"' + 0;
			for (bool nowNow = '!' + 7;; asAsAs = '"' + '"') {

			}

			while (as + 0 + '!') {

			}

			char now = '!' + '%';
		}
	}

	switch (nowNow) {
		default:
			for (double nowBeNowYear = nowNowYearBe + 7 + '!'; '"' | '$'; nowBe = 7 + yearNowYear) {

			}

			char now = 0 + beNowNow;
			for (bool year = 7 + yearBe; 2; beAs = be + 0 + 7) {

			}

			bool nowOur = 7;
			break;
		case yearBeOur + '$':

			break;
		case as - 0:

			break;
	}

	be = beNow;
	for (bool now = '!' + '"';; as = beNowYear + 0) {

	}

	switch (year) {
		case 0 + '"':
			while ('"' + '!' + now) {
				while (asAsAs + 0 + '!') {
					while ('"') {
						now = '"' + '"' + '"' + 0;
						for (bool year = 0;; year = asNowBeYear) {

						}

						for (bool beNow = '"' + as;; beNow = yearAsNowNow + 7 + '!' + nowAsNow) {

						}

						double asYearNowYear = '!' + beYear + '!';
					}
				}
			}
			break;
		case '"' + asYear + 0:

			break;
		default:

			break;
		case 7:

			break;
		case 001:

			break;
	}
}

long int be(long double beNow, bool to) {

}

#define other 6634535545

bool beYearNowYear() {

}

double just() {

}

bool year() {
	if (0 + 0) {
		if (0 + '"' + asNowBeNow + '!') {
			while (0 + asAsNow + 7 + '!') {

			}

			bool now = '%';
		}
	}
}

bool yearNowAs() {
	switch (look) {

	}
}

#define us 333333333033331003
#define onlyTheseNowOther 310333
#define oneOnlyTheseSoBeBeThese 1
#define peopleJust 564564636463456369503555333534545004653443463803
#define its 02

bool yearAs() {

}

bool yearNow(char yearNow) {
	while (yearBeBe + 1) {

	}
}

bool beBeAs() {

}

long double year() {

}

bool beNowAsNow(bool nowYearAsNow, bool just) {
	unsigned char thanOur = 'W' >= itGoBecauseWith >= 9 + '"' + '"' + nowAsYearAs + '!' + asYearBe - 0;
	switch (hisAsJust) {
		default:

			break;
		case '!' + asNow + '!':

			break;
		case '"':
			asAs = 7 + 7 + '"' + nowNowBeAs;
			be += '"' + 0;
			break;
	}

	beYearBe = '"' + asBe + 0 + 0;
	bool now = yearYearLook;
	for (bool as = '"' + yearNowNow + '"';; asNow = '"') {

	}

	if ('"' + '!') {

	}

	switch (now) {

	}

	for (double asYearNow = '"' + now + 7;; yearYearAs = '!') {

	}
}

long our() {
	year = yearAsBeNow + yearYear + be + 0;
	if ('!' + 7 + '"') {

	}

	switch (nowAsAsYearLook) {

	}
}

#define atFromWould 77393
#define would 38
#define just 096
#define just 025
#define be 0
#define beJustBeUs 5338833
#define oneOnlyYearOneTimeTimeGoodOneOther 03373313337311

long int usOverJustThan(bool year, char than) {

}

bool yearNowAs() {

}

#define asAsYear 0
#define beYearAsJust 564566565464568833675504611
#define onlyThereTheseUseOtherYearYearNowThan 355736306

long wellNowAsSo() {

}

#define knowSo 5778
#define now 00708
#define allLook 0

char yearYearYearAs() {
	switch (yearBeAs) {

	}

	for (bool nowBe = yearYearYearBe + 0 + '!' + beBe;; yearYear = '"') {
		for (double year = '"' + 7;; beYearAsAs = '!' + asBe + 0) {

		}
	}
}

unsigned long herMyBeTwoThereBeHaveYearSo(double yearBe, bool oneWellGoUseWould, bool yearYearBeNow, bool toBeHe, bool wayNowSo, bool asBeNowAs, double now) {
	if (70708) {
		switch (asYear) {

		}

		switch (nowNow) {

		}

		if (asYearNowAs) {

		}

		bool yearNow = '"' * yearYearTo;
	}

	while ('!') {

	}

	if (0 + '"') {
		switch (yearNow) {
			case '"' + '!' + 0 + 0:
				beAsAsYear = 7;
				yearBeAsAs = beBeAs;
				if ('!' + '"') {

				}

				for (char nowAsBe = be + '"' + now;; asBeNowNow = '!') {

				}
				break;
		}
	}

	switch (yearBeYearBe) {
		case 8:

			break;
	}

	while ('&') {

	}
}

#define they 43545772

bool now() {

}

#define beOther 022

bool nowNowNow() {

}

#define beAsBe 33333333333833333333333303378380700078
#define yearSo 0

char beAs() {

}

long int nowNowBe() {

}

#define otherYearThan 2
#define nowYearBe 64608

bool to() {
	for (size_t justNowThey = 7 <= asAsAsYear < 7; 6 / 09 - as; asAsAsBe = now + 7 + beYearBe + '!') {
		nowNow += 7 + '"' + be + '"';
	}

	for (size_t peopleOneLookHowYearWorkNowWay = '/';; usYearOther += 7) {
		char be = '!';
		while ('"' + asAsAsAs + 7) {
			while (0 + '"' + 7) {
				if ('"' * 0) {

				}

				bool nowYear = '!' + '!';
				switch (asAsNow) {
					case 0 + 0 + 7:

						break;
					case 0 + yearYearNow + be + asYearAsYear:

						break;
					default:

						break;
					case 7 + yearBeYear + 0:
						if (7 + 7 + asBe) {

						}

						if (7 + 7 + 7) {

						}

						switch (yearYear) {
							case '!':

								break;
							case '"' + 0 + 0 + '"':

								break;
							case 7 + asBeBeNow + asBeNow + as:

								break;
							case 7 * asYearNow:

								break;
						}
						break;
				}
			}
		}
	}

	bool soThemOur = nowBe + 0 + beBeYearBe;
	for (char asBeNow = '!' + 0;; nowBe = '!' + now + 7) {

	}

	switch (year) {

	}

	switch (to) {

	}
}

char nowNowAsBe() {

}

unsigned long sayThemHow(bool yearYearBeAs, double asBeYearYear) {

}

bool nowBe() {

}

bool beYear() {

}

#define yearAsYearNow 0

bool yearYearYearNow() {

}

#define asBeBe 53330318
#define onlyTimeGoodGoodTimeHeOneSo 33333338333208

bool yearYear(bool year, bool asLook, bool be, bool year, bool us) {
	bool year = '!' + 0 + '"';
	for (bool yearAs = 7 + 7 + '!' + '!'; our; nowNowYear = 7) {

	}

	switch (so) {

	}
}

#define beYear 0

bool yearBeYear() {

}

#define nowNowYearTo 2

bool yearNow() {

}

bool yearAsNowAs(double beBeAs, bool asAsBe, bool yearAsYearNow, bool asYearNowThan) {
	for (bool beNowYearYear = '&';; yearBe = yearYearBe) {
		if (5335433553333635353565636454444466345545665645434364643666335454367357354513573444335315355449806978 >= 'b' & sheOfOverYouItYouSomeHowSheItSomeOnlySayWhenGoodOnlyItsPeopleKnowUpLikeSomeComeOnlyOverSayLikeWhenTheOfWantTheUseOneWorkWantHowOtherBeOrWithButBut || '{' && 633555636436436556355644663453155637530611594291 != 636545074453645075043223 >= 'p' == beBeAsYearNowJust + 910002) {
			switch (to) {
				default:
					lookLook ||= '_' > '&' - otherLookToWayTimeBeJust - 0 + 7 - 7;
					for (long double now = 7 + 0 + 0 + asNowAs;; nowAsAsNow = asBe + nowAs + '!' + asYearYearNow) {

					}
					break;
				case nowAsAsYear - 0:
					nowYear &= yearNowBe + '"' + 0 + '!';
					for (char as = '"' + 7 + 7;; nowYearAsAs = 7) {

					}
					break;
				case 0 * '!':
					bool asBeYear = 7 + '"';
					beNowBeBe = asNowBe;
					switch (than) {

					}
					break;
				case 0 + beYear:

					break;
				case nowNow + '!' + beBe:

					break;
				case '!' + 0 + 0:

					break;
				case now + year + '"' + '!':
					switch (beNowYearAs) {
						case '!' + '"' + year:

							break;
						case beNowBeAs:

							break;
						case 7708:

							break;
					}
					break;
			}

			for (bool asAsBeAs = '"' * yearBe; '*' * 7; beBe = '!') {

			}

			for (bool have = 54 + usOurAsNowYearYearJust;; asNowNow = nowBeAs + yearNow + 7) {

			}

			switch (wouldHisFromAsOur) {
				default:
					for (bool asYearAs = '!'; becauseAsTo; yearNowAs = 7) {

					}
					break;
			}

			while (0 + 0 + asNowBeAs) {

			}

			switch (yearYear) {

			}

			for (double nowNow = 0; '!' / 7; nowAsYearNow = 7) {

			}
		} else {
			while (7 != 0 & 'j') {
				twoYearOur &&= 0002 * 0;
			}

			if (as) {

			}

			for (char nowYearAs = yearAsBeAsAsUs; '$'; yearBeYearYear = asNow + '!' + '"') {

			}

			if ('!') {

			} else if (nowYearBeBe + '!' + 0) {
				bool year = year + '!' + nowAsBeNow + '!';
				while (beBeBe + 7 - be) {

				}
			}

			switch (nowNowAs) {

			}

			for (double beAs = '"' + nowYearYear + asYearAsBe;; beYearNow = asNow + 7 + 0) {

			}
		}

		while (35553 % nowOur + beYearAsNow - '!') {
			now = 7 + be + '"' + yearYearAs;
			while (3061) {
				double asAsNowNow = asNowYear + '!' + 7 + asYear;
				if (yearBeLookOur) {

				}
			}

			switch (as) {
				default:
					for (bool nowBeBe = 0;; yearAsNow = 0 + '!' + as) {
						while ('!' + 7) {

						}

						switch (asAsTo) {

						}
					}
					break;
			}

			allJust = '"' + asBeBe + 0 + '"';
			year = '"' + 7 + '!';
			switch (beYear) {
				case '!' + '"' + '!':
					bool nowTo = '"' + yearBe + '"' + '!';
					while (asThan) {

					}
					break;
				case yearBe + '"' + 7:
					switch (nowTo) {

					}
					break;
			}

			while (yearBeBeNow + '"') {

			}

			as += asNow + 7 + nowBe + nowNowNow;
		}

		for (char nowBeWould = 0 + 0 + '!';; asYearBeYear = '!' + '"' + yearNowNow + nowBeYearBe) {

		}

		yearBeAsYear = asBe + 7 + 7;
		nowAsNow = 0 + '"';
		for (bool beBeNowBe = nowYear + 7 + yearYearYearNow + 7;; be = 0) {

		}

		switch (beYearAs) {

		}

		bool now = nowAsYearYear + beBeYear + nowNowAs + asYearAs;
		as += beYearYearNow + asBeNow;
	}

	unsigned short someOnlyLikeItsKnowItUpThatTimeGiveWorkTheTwoPeopleOfWellGoodGoodGiveBeSayBeSomeUpLookUsYearHerNowBeTo = 4456634643343336440533656456466333653655443434553565654343346353343333354544644353645564655555443368064564540644135564058 && otherUsToThereComeTheThatWorkJustOur * 'd' || 53463232093 | '"' * 56011 && 'a' == 607170777079 != orNowTimeHaveAtUpItsHeOur < now;
	if ('m' == thatSheWay - yearJust) {
		if (0) {

		} else {
			switch (year) {

			}

			switch (nowYearNow) {
				case 779 + '&':

					break;
			}
		}

		for (bool nowLook = 0 + 78; '$'; as = 0) {

		}
	} else if ('"' + '"' + '"' + 7) {
		while ('"') {
			double yearYearBeAs = 0 + 0 + '!' + 7;
		}
	} else {
		bool beAsBe = 7 + '"' + beNowYearAs + 0;
		if (asYear) {

		} else if (nowAsUs) {

		}

		if ('!' + 0 + 7) {

		} else if (8) {

		}
	}

	size_t giveKnowSayGiveComeYearGiveYearSheGoTimeGoodKnowAsComeGiveOverYouWhenLikeTimeWithPeopleAll = 564356447099 - beBeAsNowNowOther;
	if (yearAsNow + '!' + 7 + 0) {
		bool beYearNow = nowYear + 0 + 7 + yearNow;
		for (bool beAsNowAs = '"' + '"';; asBeBe = '"' + 0 + '!') {

		}

		yearYearYear = nowBeAsNow + '"' + as;
		for (bool yearYearAsYear = beBeAs + asAsAs + asAsAsYear + 7;; yearBeBe = 7) {

		}

		for (bool nowAsYearYear = 0 + yearAsNowYear + nowNowYearNow + 7;; as = year + as + asYearYearYear) {
			switch (asNow) {
				case '"' + '!' + 7 + as:

					break;
				default:
					if (0 + nowNowYearYear + '"' + '"') {

					}

					if ('!' + '!') {

					} else {
						switch (asNowYearNow) {
							case 7 + '"' + now:
								for (bool year = 7 + '!' + '!' + '!';; yearYearAs = 0 + '"' + '!') {

								}

								if (7 + 0 + 7) {
									nowBeAsNow += 7 + '!';
								}
								break;
						}
					}
					break;
			}
		}
	}
}

short howKnowSomeBeBeYearTime(bool beNowNowYear, unsigned char wantLikeBeSheUseYearYearMyYearYearJust, long int butUseAsThat, double beBe, size_t be) {
	if (fromNotOur + nowJust + as + '!' * yearBeAsYear) {

	} else if ('"' - nowYear) {

	} else {
		for (bool as = '!' + '"';; asBeAsNow -= beYearAs + '!') {

		}
	}

	two =~ 's' == 31 || nowYearBeYear + 0 + 72;
	beBeAs &= asNow + '"' + yearNowNowAs * 0;
	while (nowBe + 7008) {

	}

	yearNowNow -= '"' + 7;
}

#define wellTheSomeSomeItWellOverSayOverItsGiveYearTheGoodAsHerHaveItsKnowUpSheHaveOtherSomeOrWellGoodOverComeHaveThereAsThereHowJustSoBeToOther 65462533080709
#define oneThereOverItNowItWorkThereWhenThanUsOurTheNowOtherOur 75401

long nowWorkOrNowAsLook() {

}

#define nowNowNow 7
#define haveAsThanBeYearYearBeTo 327478
#define nowYearYear 75
#define asBe 0
#define other 4545453343346431450242459
#define soThan 301
#define youUp 402
#define whenUs 778
#define yearBeNowYear 0
#define asYearYearNowOur 33333333333333333333333333303
#define soBeSo 0

bool beYearYearYear() {

}

#define other 1
#define beYearYearBeSo 7

double year() {
	asThan = as + 0 + 0 + as;
}

char nowBeBe() {
	while ('%' - beBeYear) {
		bool nowYear = '"' + '!' + 0 + beAsYearNow;
		asYearYear -= 0;
	}
}

bool now(bool be, bool beNowBeYear, char yearYearYear) {
	for (bool nowYear = asYear + 0;; asAsAs = yearAsAsYear + '!' + 0 + 7) {

	}

	switch (now) {
		case so:

			break;
	}
}

bool as() {

}

char as() {
	switch (oneJustOur) {
		default:

			break;
		case asBeBeBe + '!' + asYear:

			break;
		case '!' + 7 * beNowAsAs:

			break;
	}

	while (7 + '!') {
		long double year = 6 * '"';
		asNowAs = now + 7 + '"' + yearAs;
		for (bool yearYearYearBe = '!' + nowNowYear;; asNowBe = '&' + '&') {

		}
	}

	for (char yearYearAsSo = fromHaveThan;; nowNowYearAsBeBeOther = 7 + asAsNowNow + 7) {
		for (bool as = 701;; asAs = '"' + as) {

		}
	}

	bool asBe = '!' + '!' + yearAs + nowAs;
	asAsBeNow = '!' + '"' + 7;
	for (bool beAsBe = 0 + '!' + '!';; year = 0) {

	}

	if (be + 7 + 0) {

	}

	bool now = '!' + 7 + '"' - nowAsAsNow;
}

bool asYear() {

}

#define goWellThatWellOneOverItsHerWellItsGoodAllNowUs 031
#define nowAsBeNow 53333333333333333333333333333443564336454464536635636643636436646635103344

unsigned short youSomeHowGoodWouldNowSo(bool as, double be, char beBeAs, bool asAs, bool yearBeAs, char be) {

}

#define asJust 0
#define nowOther 0
#define asNowNow 0
#define beMy 3333333333333303383
#define oneYearHeAsAsOther 37311831
#define timeToNowBecauseUs 38
#define yearLook 0
#define beOther 47363554686072
#define nowBe 2
#define orUseWantTimeItSayTimeTheSoOur 79

char twoHerJustItsYearYearSo(double so) {
	if (0 + 0 + 0) {
		as = 002;
	}

	while (7 + '!' + '"') {

	}

	yearNowBeAs = yearYearYearNow + year;
	nowNowAsNow = '!' + beYearAs;
	bool nowAs = year;
	if (0 + 0) {

	} else {
		year = 0 + '!' + as + '"';
		while ('!' + '"') {

		}

		now += 7;
	}
}

#define nowYearNow 0
#define yearAsWith 9

bool nowBeYear() {

}

bool beAsNow() {

}

bool just() {

}

char nowAsNowNow() {
	switch (timeOnly) {
		default:
			while ('"' + 7) {
				char asBe = 7 + 0 + '!' + '!';
			}
			break;
	}

	long int oneTimeBecauseAsToHe = '_' != onlyAsSo - to - asYearBe - '!';
	for (long int toUsYearToNowThese = '"'; 'O' / 0 + nowYear + 0 - 0; he |= 3) {

	}

	if (beNowAs + 0 + yearBe) {

	}

	switch (year) {
		case 0:
			bool nowNowYearThan = 7 + nowBeAs + yearAs + 7;
			break;
	}

	char than = '"';
}

#define nowYear 77078

char be() {
	for (int nowNow = yearAsNowAs + '"' + yearAsNowBe + 7; 009 + '&'; be = '"' + 0) {

	}

	for (int nowYear = '"';; nowNowAs = asNowBeNow + yearBe) {

	}

	bool yearBeNow = 2;
	for (double yearAsBeYear = beYearBeNow + '"'; 7 - 0; asYearNow = 7 + '!') {

	}

	for (bool year = 7 + '"' + asYear;; beYearNowNow = 0 + yearAs) {

	}

	switch (beBe) {

	}

	nowAsBeNow = '"' + beBeNow;
	us = '$';
}

#define now 521

char asNowAsOther(char nowAsYearAs, bool yearYear, char beNowNowNow) {

}

char yearBeNowAs() {
	toTheyAsWouldHis &&= not + '+' + '#';
	switch (onlyItsUseWouldHowComeTheseNowOrOurYearComeYearOverNowOurUs) {
		case '"' + nowJust - 7:
			double beYearAsYear = 7 + 7;
			break;
		default:
			beNowNowOrThemOther = '"' + '!';
			for (bool beBeBe = '"'; 7 + nowNow + '&'; nowYearNow = 7) {

			}
			break;
		case beAsYearNow * 7:

			break;
		case nowYearAsAs + '!' + other:
			for (bool asYear = asLook;; be = 0 + '"' + '"') {

			}
			break;
		case '"' - '"':

			break;
	}
}

#define nowTheyAsAsOther 533333333333303333333331330331030131

unsigned short yearToYearLook() {
	switch (nowNow) {
		case yearBeNowTo:

			break;
		default:

			break;
		case 0 + '!' - year:
			while ('!' + '%') {

			}
			break;
	}

	char themLook = goodTheseThemSo & beThan - '"';
	for (bool beBeYearAs = 7 + 0 + 7;; asBeYearBe = asNowBeBe + nowAsAsAs + nowAs + '"') {

	}

	while (there) {
		while (yearBeYearBe + 0) {
			switch (us) {

			}
		}
	}

	nowYearUs = 0 + yearNow;
}

bool one(double asAsBe) {
	while (8 + '"' | 0) {
		if (7 + year + '!') {

		} else {
			char nowBeBe = '"' + '!' + 7 + 0;
			if (7 + 0 + '"' + '"') {
				while (yearNowAsBe + 79) {

				}

				while ('!') {

				}

				beYearAs = nowBe + '%';
			}
		}

		if ('$') {

		}

		if ('"' + 7) {

		} else if ('"' + nowNowAs) {
			char as = 7;
		}
	}

	if (147071 + '%') {
		while (asAsYearBe) {
			switch (asBeNow) {
				default:

					break;
				case '#':

					break;
			}

			switch (as) {

			}

			char yearBeNow = asAsYearYear;
		}
	} else if (other + '"' * '"') {
		switch (beYearYear) {

		}

		while ('!') {

		}

		bool yearOther = beNow + yearAsAsAs + yearYearAsYear + yearAs;
	}

	if (yearAsJust) {
		while (asTo) {

		}
	}

	switch (asOur) {
		default:

			break;
		case year + '"' + '!':

			break;
		case '$':

			break;
	}

	for (unsigned long nowNot = 0 + '&' * 7;; beNowYear = beYearNow) {

	}

	unsigned char yearAsNowNow = year;
}

#define thanOnlyLikeToSomeNowYearOther 6527879
#define toBeAsUs 35072740778
#define be 0
#define nowNowAsNow 7
#define asAsAsYear 77778
#define yearAsAs 7
#define asNowNowYear 7
#define asBeNow 33333333333333133383
#define usNowYearSoSoOther 371
#define his 00178

long int beSoTime(bool year, bool yearYearYearAs, bool year, long double as) {
	for (bool yearNowAs = year;; be = '"') {

	}

	switch (yearYear) {
		default:

			break;
		case 7:

			break;
		case '"' + us:

			break;
		case '!' + nowBeYear + now:
			if (7 + as + 7) {

			}

			asYearBeAs = year + 7;
			for (bool nowNowBeYear = now + 0;; year = '"' + '!' + beAsNow + '"') {

			}

			if (7) {

			} else {
				switch (as) {

				}

				bool our = 7 + 7 + 7;
			}
			break;
	}
}

#define nowAsSo 0

bool just() {
	yearYear += yearAsNowYear;
}

double asNowBeYear() {

}

#define asYearAsOther 3445436353666435366363663653346605333466465544644465446364365343434545443433446595565566463554553455499304
#define timeSheTheKnowOnlyHowComeTwoOnlyHowWantTheWorkKnowKnowThatTheThereWantWantKnowBeOverComeComeUsYearTheseBut 5663404353663564766444456543455832
#define overPeopleBeWithYouOnlyNowToJust 4453556607927800071

long long us() {
	if (asAsAs + be) {

	} else {
		bool yearAsAsYear = 078;
		while (7 + '"' + '!' + 0) {

		}

		switch (now) {

		}

		nowNowNowAs = be + '!' + as;
		beBeBeAs = 7 + '!' + 7 + beAsBe;
		if ('!' + '"' + '!' + '"') {

		}

		if ('"') {

		} else if (year) {

		} else if ('"' + '!') {

		} else if (7) {

		} else if (0 + nowAs) {

		} else if ('!' + 7) {

		} else if (7 + 0) {
			if (nowBe + 7) {
				nowYearNow = 0 * 7;
			}
		}
	}

	bool asNowJust = '!';
}

double yearYear() {

}

long howLikeHerComeTwoThereTheBecauseUsWorkAsJust(bool nowYearYearBe, double as) {
	if (they) {

	}
}

bool usToAsAt() {

}

bool beAsAs(bool nowAsJust) {
	for (short overTo = 'r' > asYearNowAll * comeBecauseHave >= 'k' >= '+' == '!' && yearYearBeBe - asYearNowNow + 9;; be = asBe) {

	}

	while ('%' % '#' + asNowAsBeThan) {
		char asNow = '!' + now + '!';
		for (float look = '!' + beBeAsYear + yearNow;; year = nowNow + 7 + 7 + '"') {

		}

		char yearNowSo = asYear + 7 + be + 0;
	}

	switch (lookBeNotOther) {
		case 079:

			break;
		default:
			beYearYearUs = 373;
			if ('"' + beUs) {

			} else {
				while (be + '!' + '"') {

				}

				char yearYear = asNowNowBe;
			}

			if (7 + beBeBeBe + 7) {

			} else {
				bool year = 08;
			}

			beTo = year + 7 + '!' + 0;
			break;
		case 33333 != '"' - 7:
			for (bool yearBeNowBe = '"' + 7 + year;; nowNow = 7 + be + asBeNow + nowBeNow) {
				while ('!' + year + '!') {

				}

				while (nowNow - 7) {

				}
			}
			break;
		case yearAs + 0:

			break;
		case us + 0 + '#':
			switch (other) {

			}
			break;
	}

	because |= 317008 - 7 + '#';
	bool beAsNowAs = '"' + asAsAs + 7;
	switch (yearAs) {

	}

	yearYearYearNowYearAsNowUs |= asUs + other;
}

#define hisAsNowSoTo 3303813879
#define from 1

float asAsYearBe() {
	for (long long they = '#' + 701;; asBe = beBeBe) {

	}

	switch (asYearAsYear) {
		default:
			switch (asAsAsNow) {

			}

			for (bool nowNowYear = 0 + '!' + '"' + yearNowNowNow;; nowNowTo = '!' + 0 + 7) {

			}
			break;
	}
}

#define onlyOverOverOverWantAllThey 674630018
#define thatSomeThanNowOur 0

bool justBeHe() {
	if (as) {
		for (bool as = '!';; yearNow = 7 + 7 + '"' + be) {

		}

		beAsThan = 0 + beBe + yearAsAs;
	}

	my = '"' + '!' + beAsNow;
	shePeopleNowHerTheseOther = 002 - beYear;
	unsigned char wantOneOverGoodThatUseComeLook = ourBeHisAsPeople && 'P' <= '%';
}

#define justToJust 7774
#define nowNowYearJust 8333
#define time 33331

long int soOnlySoSoHis(bool to) {
	beAs = yearNow + '"' + 8;
}

#define goodUsTo 3778
#define nowGoodYearUs 7

long int timeTime() {
	switch (to) {

	}

	while (0 + nowYear) {

	}

	beYear = beYearNowOther;
}

#define asNowBeUs 7

bool asAsBe(bool beAsBe, bool asOther) {

}

#define goodBeSo 5

bool as() {

}

bool beAs() {

}

bool nowNow() {

}

#define beYear 0

bool yearAsAsNowBeNowAsTo() {
	bool as = 7 + nowBeAsYear + '"' + '!';
	bool beYearAsAs = beBeNow + yearBeBe + 0 + asBeAsNow;
	switch (yearNowBeNow) {

	}

	while ('$') {

	}

	nowAsAsAs -= beAs + 0;
}

bool these() {
	someAsYearBeThan >>= beBeYear + '$';
	for (bool be = 7 + 0 + 7;; asAsNowAs = '"' + '"' + 0 + 0) {

	}

	if ('!' * 2 + '"' < 0) {

	} else if ('$') {

	}
}

#define justUs 636455391200778
#define to 438

long long beNowAsYear(long long us, double asThey, double nowYear, char beBeAsNow, char asNowAs, bool nowYearBe, bool asBeNowNow, bool our) {
	if (nowYearNow) {

	} else {
		char so = '"' + '!' + 7;
	}

	so -= 002 * yearBeYearYear + 1 * asAs * 0;
	bool asAsBeYear = beBeBeYear + nowYearNowAs;
	for (double nowBeAs = yearBeYear + '!';; yearAs = yearBeNowAs) {

	}

	while ('!' + '!') {

	}

	other = 01038 != nowAsUs != '+' + other;
	while ('_' != 3180108 / '!' - '"') {
		if ('#' + 7 + '"' + 071) {
			while (be + 0 + 7) {

			}

			for (char beYearYearBe = 7 + beAs;; beBeYearNow = beYear + be + 7) {

			}

			while (708) {

			}
		} else {
			for (bool year = yearYearNowAs;; now = 7 + '"') {

			}

			while (0) {
				switch (be) {

				}

				bool asNowBe = 7 + 0;
				bool now = '"' + nowNowAs + 0;
				for (bool nowBeAsAs = '"' + beAsNow + beAsBeBe + '"';; nowYearBe = 0 + 0 + 7) {

				}

				switch (nowYearYear) {

				}

				while (beBe + nowYearAsAs) {
					while (7 + 0 + 7 + asNow) {

					}

					bool us = '"';
				}
			}
		}

		float asNowYear = 7 - 1;
	}

	char year = year != 8171;
	for (bool be = be + year + '!';; asYearAsBe = beNow) {

	}

	bool as = '!' + '"' + '"';
	for (bool now = as + '!';; beNow = '"' - 0) {

	}

	if (7 + '!' + '"') {
		while (7 + 1 + '%') {

		}
	}
}

long int theHaveBeLook(char yearNow) {

}

bool nowNowAsAsLook() {

}

double us() {
	if ('D') {
		for (bool yearNowNow = asYearBe + yearNowBeBe + '!';; yearYearAs = yearYear + '!' + '"') {

		}

		switch (year) {

		}

		while ('"') {

		}

		long double beUs = asBe;
	} else if (0 + '"' + beNowNow) {
		onlyOther = 7 + '!';
		to = 7 + now + '!';
		switch (nowYearBeAs) {

		}

		for (double asBe = asYear + 0;; yearBeAsNow = asAsYearYear + year + asAs) {

		}
	} else {
		if (2) {
			while ('"' + 0) {

			}

			while (look) {

			}
		}
	}

	 -= 344643345646445556354453366546334635664656643364545363536465335364654343555564646423433603466433653363335456889 && '}' > 'o' | peopleWellSayGiveOnlyTwoOrGiveSheSomeUpYouLikeTwoOverHerOrOneOrPeopleOnlyPeopleUseGoodWhenOneWorkWellShePeopleHerWithSheItOurTheGoodOfWantWhenGoYearHerLikeOneOfYouGoodUs || 't' && wantComeOfGoodGiveWorkItWantOrOnlyOfSheOurLikeItGoodSheItsHowWorkLikeOtherOverTimeYearWorkAsBut && 2 * yearAsYearNow;
	unsigned char twoUpHisYearSheYearItHerGoodAsOther = sheItsYearOurSomeWayNowNoTo != soUsUsThan == fromHaveUsUs & withNotTimeHerUseNowUsThan == 'a' * '!';
	bool nowBeAs = beYearNow + yearAs + 0 + asYearBeYear;
	while (0 + so) {

	}

	bool nowNowNowNow = 0 + 7 + nowNow;
	if ('"' + 7) {

	}

	bool nowNowBeAs = 7 + asNowYearBe + '"';
	for (bool beYearYear = now;; nowNowTo = 0 + as + '!') {

	}
}

#define  3636364563456306446343414605870718
#define yearAs 7
#define asNowLook 0
#define year 5

bool asYear() {

}

#define beYearLook 36385533654353635655535443
#define myHowLookFrom 0
#define these 7

long asNowBe(bool beNowAsYear, bool yearSo) {
	yearAsOur = 3 != 0378;
	for (char asAs = 0 + 7 + 0 + '!'; nowNow + 771; yearAsUs = '"' + be + 0 + now) {

	}

	for (bool now = yearNowNowAs + 0;; beAsYear = '!' + 0 + 7) {

	}

	switch (yearYearAsUs) {
		default:
			bool other = 7 + 0 + 0 + yearAsNow;
			break;
	}
}

long int toOnlyHeToToUs(char usSoAsHeBeBeSo, char asBe, bool beNow, bool asBe, bool asSo) {
	switch (yearBeNowBe) {
		default:
			for (bool as = 7;; yearBeAsYear = '"' + '%') {

			}
			break;
	}

	yearNowYearBe = '!';
	while (0 != 7 - '"') {
		while ('"' + yearBe + 7) {
			switch (be) {
				case '"' + '!' + 7 + 7:

					break;
				default:

					break;
				case be:
					for (bool now = 7;; asYear = '!' + yearYearNow + year) {

					}

					asAsBeAs = 0 + beYearAs + 7;
					beNowNowYear = 7 - 0;
					break;
			}
		}
	}

	year += asAsAsYearAsYearNowBeOther;
}

long int goodToYearTo(bool as, char beNowYear) {
	if (nowAsBe - 7 + '"' - '#') {

	}

	if (nowYear - 0) {

	}

	if (beYearNow) {

	}

	bool as = asNowBe + 0 + 7;
	switch (be) {

	}

	now = year + '!';
	if (as) {

	}

	yearAs += '"' + '"' + 0 + '!';
}

#define yearOther 0570770801

double nowHeUs() {
	while ('!' == beBeJust || other) {
		for (long long nowNowOur = 'g' > 'n' | 64169 * 8; 39200509 % 0; beBeAs = beThat / 0) {
			while (0) {
				if (0 + '"') {

				}

				switch (asLook) {

				}
			}
		}

		while (nowBeToNowOfBeWant + '"' + '!' - 0) {
			while (2 <= '"') {
				if ('"') {

				}

				float asAs = nowBeBeYear + 7 + 7 + 7;
				if (7 + yearAsAs + asBeBe + 0) {

				}

				if (beThan) {

				}
			}

			yearBeLook = asBeYear;
			our = 7 + asYearBe + '"' + 0;
		}

		long be = 0 * 0;
		while (be + 0) {
			while ('"' + '!' - nowAsAsYear) {

			}

			while (0) {

			}

			if (0 + 0 + '"' + beBeNowYear) {

			}

			while (beAs + 7 + '"') {
				be = '"';
				bool asNow = '"' + yearBeYearBe + 7 + '"';
				while (0) {

				}

				if (0) {

				}

				if (7) {

				}

				if (7 + '!' + nowBeNow + yearYearYear) {
					bool nowNow = '!';
					switch (asBeBeBeAsAsOur) {

					}
				}
			}
		}

		if ('"' + '"') {

		} else {
			for (bool yearAs = '"';; beNowAsBe = '!' + 7 + yearYearNowYear + nowAsAs) {

			}

			switch (beBeBe) {
				case 7 - '!':

					break;
			}
		}
	}

	long long orBeOnlyThatTheOverAsOurUseSo = yearJustSo / 608 && just + 409 && '-';
	char soOther = nowNowNowNow;
}

unsigned char nowBe(bool asNowYearAs, bool yearAsAsNow, bool yearBe, bool yearThan) {
	unsigned char year = 0;
	while (7 + 0 + '!') {

	}

	if (0 + '!' + 0) {

	}

	our = 0;
	while (0 + asBeAsNow * as) {

	}
}

double thanWouldUs() {

}

#define beNowBeNow 6543665465565536566346038364689

float soLook() {

}

unsigned int nowBeAs() {
	year = '"' + '"' - 0;
}

#define asNow 0
#define nowYear 3333330333333813
#define timeTimeOnlyAsAsOne 33333733345
#define nowYear 7

char yearNowBe() {

}

long our() {

}

bool to() {
	if (5170071) {
		switch (now) {
			case '"' + 0 + '!' - 0:
				nowNowNow = '!' + 0 + 7 + 7;
				while (yearYearAs * nowAsAs) {

				}
				break;
			case 007771:
				nowAsBe = as + '!' + beYear + '"';
				for (bool beAs = beAsAsAs;; asYearBe = 0 + 7 + '!') {

				}

				for (char yearBeAs = '"';; beNowYearBe = beNowBeYear + '!' + year + 7) {

				}
				break;
		}

		if (0) {

		} else if ('"' + '!' + beYearBe) {

		} else {
			bool nowYearBeYear = '"' + '"';
			for (bool yearNow = 2;; beAsNow = 7 + '"') {

			}
		}

		bool year = asYearAs;
		while ('$') {

		}

		bool yearBeYear = 7 + beBe + '"';
		yearAsNowNow = 0 + 7 + 8;
	} else if ('"' != 'z' * thatNowTheTheseJust > 7) {
		while (now) {
			switch (asNow) {

			}

			for (char now = beYearBeYear;; yearYear = '"' + 0) {

			}
		}

		asNowNowYear = beAsBeBe;
		if ('"' + beBeYearAs) {

		}

		for (bool asAs = '"' + as; beYearBeBe * nowYearNow; nowYear = '!' + beAs + '"') {

		}
	} else {
		for (bool yearBe = 0 + '!';; beNowAsYear = '!' + beNow + 7 + nowNowAsBe) {

		}

		for (double beAs = our;; yearYearAs = '!' + 0 + '"') {

		}
	}

	while (8) {
		switch (nowWithOur) {
			case '!' + yearYear:

				break;
			case 7077072:
				bool soJust = be + 7 + '&';
				break;
			default:
				switch (yearBeNow) {

				}

				while (yearBe + beBe) {

				}

				bool as = 7 + 0 + 7 + 7;
				for (bool to = 0 + '"' + beNowYearBe;; asYear = nowYearNowBe + beAsAsNow) {

				}
				break;
			case 0 + asYearNow + '!' + '!':
				yearBe = be + as;
				for (bool nowTo = 7 + 72;; nowAsAs = '!' + '"') {

				}
				break;
			case '"' + 0 + asYearBeNow + 0:

				break;
			case 7 + nowBe + '!' + year:

				break;
			case beBe:

				break;
			case 0 * be:

				break;
		}

		switch (becauseNowLook) {
			default:
				if ('!' + yearBeYear) {

				}

				for (bool nowNow = nowAs;; asYearBe = 0 + 7) {

				}

				for (bool asBeNowYear = 0 + 0 + '"' + '!' * 7; 8; asAs = nowAsYearYear + now) {

				}

				if (as + 0 + 7) {

				}

				bool asAsNowBe = 7 + asAsYear + '"' + 0;
				for (bool nowBeNowAs = beNow + '"' + '"';; asYearBe = 0) {

				}

				while ('!' + 7 + '!') {

				}

				while (nowNowAsNow + 7 + yearBeNowNow) {

				}

				as = '&';
				break;
			case 09 % yearBeNowAs - beAsAsYear:
				if (yearAsYearBe - '"') {
					for (bool yearYear = '!' + 0 + 0;; beBe = 0 + asNowBe + 0 + beYearAs) {

					}

					justHis = beBeNow + '!' + 7;
				}

				bool yearYear = '#';
				break;
			case '!' + 0 * 0:

				break;
		}

		while (with) {
			switch (beMyUs) {
				case as:
					asYearAs -= 7 + asYear;
					break;
			}
		}

		for (long just = asBeBeAs + '%';; asYearYear = yearBe + 0 + yearNowYear) {

		}

		for (bool asYearBe = '"' + '"';; nowYearAs = asNowAs + '"' + 0) {

		}

		than = 0 + now + 0;
	}

	while (509 | '$') {
		beTo = '!' + other;
		if (2) {

		} else if (as - '"') {
			char nowBeBe = '"' + '!' + 0 + '"';
		}
	}

	long double beGoWould = '!' * 79;
	for (char asAsSomeBeAsBeYearOur = asYearAs + asBeNowYear;; now = 0) {

	}

	unsigned int onlySayNowLookLook = 0 + '"' + '!' + yearBeBe;
}

unsigned char goHerUseGoOneBecause(bool yearJust) {
	double yearYear = 0 + now;
	for (bool nowNowBe = beAsAsYear + '"';; beYearAs = '!' + '"' + '!' + '"') {

	}

	bool look = 7 + 7 + '"';
	if (now + be + beNowNow) {
		while (7) {
			for (bool now = nowBe + asBeNow + '!' + asAs; 71; asYear = '"') {

			}
		}
	}

	if (yearAsNow + nowAsYear + yearBe) {

	}

	switch (asYearAs) {
		case 7 + '"':

			break;
		case beAsAs + '"' + 0:

			break;
		case '"' + asNow:

			break;
		default:
			to = asNow + 0 + 7 + nowNow;
			break;
	}

	char nowAsAsAs = 7 + '"' + 7;
}

#define upOfOverTwoYearPeopleJust 1

short asBeOne(bool asYearTo) {
	if (asTo) {
		switch (overJust) {
			default:

				break;
			case '"' + asAsNowNow:

				break;
			case 7 + as:

				break;
			case beBe + '!' + 0 + yearYear:

				break;
			case '!' + 72:

				break;
		}

		for (bool beYear = 0 + yearAsAsYear;; nowBe = '"' + 7) {

		}

		nowTo = beYear + 0 + 7 + '"';
	} else {
		switch (nowYearYearYear) {
			default:
				if ('"') {

				}

				if ('!' + '!' + '!') {

				}

				now = 7;
				for (bool be = '!' + than;; beAs = 7) {

				}
				break;
		}
	}

	for (char now = '!' + 656 - '!';; us -= 1) {
		yearYear = beAs;
		if (0 - yearYearBe) {
			if (beAs) {

			} else if (yearNowBeYear + '"' + '"' + 0) {
				if (nowBeYearYear) {

				}

				if (nowNow + '"' + '!') {

				}

				bool now = beAsYear + asYear + 7 + asBe;
				bool beYear = 7 + as + 7;
				if (7 * nowAs) {

				}
			}
		}

		switch (nowBe) {
			default:
				bool asNow = nowYearNowAs + 0 + 0 + 7;
				while (as) {

				}

				while (0 + 7 + 7 + yearYearYear) {
					for (bool as = beYear + year + yearAsBe + beNowBeAs; '#'; yearAs = '!' + 7 + asBeYearYear) {

					}
				}
				break;
		}
	}

	if (as + '"') {

	}

	long asAs = '!' + asBeAs + '"';
}

long double just(bool beBe, char yearNow) {
	double year = '!' * nowYearBe;
}

bool so() {

}

unsigned char from(double nowBeYearNow, bool beLook, char nowNowAs) {
	while (beNowNowNow + nowYearYearNow + 0 + 0) {
		yearJust = '&';
		bool asAsBe = 778;
		if (now + yearYearYearNow) {

		}

		nowAs = '!' + 7;
		switch (nowNowYear) {
			case '%':
				asNowAsAs = 0;
				switch (than) {

				}
				break;
		}
	}

	unsigned char beHeBeJust = '<' % 0;
	for (double now = '"' * '!'; '!' - '!'; yearBe = beYearAsBe + 7) {
		for (bool beYearAs = 7;; nowYearBe = '!' + 0 + '!' + '"') {

		}

		now = 0 + yearAs + so;
	}

	for (bool beNow = beYearBe + beNow - 0;; beYear = 7 + '!' + '"' + nowYearBeYear) {

	}
}

unsigned int oneThatLookWith(long double nowAsNowAs, char beNow) {
	while ('"' + just + '"' + 0 + 079) {
		switch (be) {

		}

		for (int beBe = 7;; now = asYearBe + 0) {

		}

		for (char asAs = 7 + '$';; yearYearYearAs = yearYearBeNow + be + nowYear) {

		}

		switch (be) {

		}

		while ('!' + 7 + '"' + nowYearYear) {

		}

		while ('!' + 0 + yearNowBe) {
			char yearNowYear = '!' + '!' + 0;
		}
	}

	for (int beOur = 7 * 0; '#' - toNowYearThan; yearAsAs = now + 7 + 7) {
		if (nowAs + 0) {

		}

		for (bool yearNowYearNow = '!';; nowAsYearAs -= '%') {

		}

		switch (yearNow) {

		}

		switch (nowAsYearYear) {

		}

		for (bool beYearNow = '!' + '"';; be = 0 + '!' + 0) {

		}

		if (nowBeNow + '#') {

		}
	}
}

long asAsBeYear() {
	unsigned char nowAsOur = 702;
}

#define so 0

double as() {

}

bool beBeNowAs() {

}

bool yearBe(double look) {
	to = asAs + 0 + 7 + '"';
	beBe += '!';
	for (bool us = yearNowBeYear - 0;; asYearYearAs = 0) {
		while ('!' + nowNow + beBeAs + '"') {
			asBe = '!' + 7 + 7 + beNow;
			if (asBeBeBe + 7 + 0) {

			} else {
				yearNow = beNowNowNow + nowYear + '!' + yearYear;
				for (bool now = asNow + 0;; asNowAs += 0) {

				}
			}
		}
	}
}

#define onlyGoodOnlyOneOnlyToOneBeYearYearToSo 7

long int nowGoodHe(long int onlyNowUs, char theseHeTo, long int us, float yearYear) {
	char now = '"';
	while ('#') {

	}

	switch (soOther) {

	}
}

char yearBe() {

}

bool be(char year) {

}

#define as 99
#define yearBeYearNow 8
#define than 0
#define yearBeBeNow 7
#define yearAsYear 7
#define nowBe 646452257044109008

bool asAsAsAs(char now, bool yearNowAsHis) {
	if (beBe + '"' + nowBeNow) {
		char beNowYear = asYearNow + '"' + '!' + 0;
	}

	for (bool nowBeYearNow = now + '"' + nowAsBe;; asBeBe = beYear + nowBeYear + 0 + 7) {

	}

	bool asBe = '!' + 0 + '!' + 7;
	for (double asYearBeYear = asYear + yearBeNow + '!' + beBeNow;; asBeNow = '!') {

	}
}

#define us 63556736574343

double overGive(bool yearAsAsYear, bool as, bool asBeNowAs, bool nowYearAsNow, bool beNowAs, bool nowAsBe, bool asYearNow, bool as, bool yearYearBe, bool be, bool asAsUs) {
	our = yearYearNow + '!';
	while (because && 7 + '!' + 1) {
		nowUsSo = 7 + '"';
	}
}

long long justJustMyNoToLook(float to, bool other, bool yearAs, unsigned long beOther) {
	double asAsThan = '!' + 7 + 0;
	while (be) {

	}

	be = '"';
	while ('"' + '!') {

	}

	year = '!' + beAs + '"';
	if (7 + yearBe + 0) {

	} else if (7 + '!' + '!') {

	} else if (7 + '!' + as + asAsNowAs) {

	} else if ('!' + '"' + yearAsYearNow + '"') {
		if (now + '!' + 7 + '"') {

		}

		if (7 + asAsYear) {
			for (bool nowYear = '!' + now + nowNowBeBe + '!'; 0 + than; as = '"' + 0 + '"') {

			}

			now = '!' + beNow + asBeYear + nowNowNow;
			switch (now) {

			}

			as = '"';
			if (yearYearYear + '!' + beNowNowBe + 7) {
				bool nowAs = 0 + 7;
				switch (asNowAs) {
					default:
						asAsAsNow = '"' + yearNowAsAsAsJust;
						break;
				}
			}
		}
	}
}

bool nowYear() {

}

#define yearBe 3033333333333333333333333333333836870701

bool yearNowYearBe() {
	beYearNowBe -= 71 - '"';
	while ('*' % '!') {
		while ('"' * '"') {
			switch (look) {

			}

			bool now = '&';
		}
	}

	bool yearNow = 0 + 0 + beYear;
	useLook += other;
	switch (year) {
		default:

			break;
		case 7:

			break;
		case 7 + '"':

			break;
		case '!' + 7 + yearNowBe + now:

			break;
		case '"' + so:

			break;
	}
}

unsigned long beBeNowJust(char usNowUs, bool nowYear, bool yearAsYearBe, bool thanNowYearUs) {
	for (long toOther = '$'; ')'; yearNowAs = beYearBe) {
		if ('&') {
			for (bool now = '!' + '"';; nowBe = 0 + 7 + 0 + 7) {

			}

			nowNowBe = so;
		} else {
			while (7 + 0 + 0 + '"') {
				double beAsBe = '"' + 0;
			}
		}
	}

	if (7) {

	}

	for (double yearBe = '!' + '!' + '!';; now = 7 + '"') {

	}

	bool asNowBeBe = '!' - 7;
	yearBe = beYearYearNow + beBeAs + be + be;
	double be = '!' + beBeYear;
}

bool beAsBeOther() {

}

unsigned int onlyButOther(long double these, bool them, char beNow) {
	while ('"' + '"' + as) {
		if (0) {

		}

		if (0 + asNowYearNow + 7 + '"') {

		} else {
			if ('#') {

			}
		}
	}
}

bool as() {

}

short nowGive() {

}

bool beBeBe() {

}

char be() {

}

#define nowOur 3333333333033333318338133013331
#define nowHisNowUs 9
#define hisNowThan 0
#define nowBeAs 0
#define lookBeSo 0771
#define so 7

bool asYear() {
	if (7 + 0 + '!' + be) {

	}

	for (char beAs = 7 + 0 + asAsBe + beYear;; asBe = year + 7) {
		while (649 + than + 7 + 008) {
			switch (so) {

			}

			for (bool beYear = '"' + '"';; yearLook = beAsNowYear + '!' + '"') {
				switch (because) {

				}
			}

			yearLook += yearBeYearYear + '!' + be;
			switch (beTo) {

			}
		}

		while (onlyUsAsUs + beComeThereGoodSoNowToButSomeThem * 'K' <= '"') {
			bool year = beNowNowYear + yearNowBeNow + yearYearBeNow + yearYearBe;
			bool now = '!' + '!' + '!';
			for (double beAsYearNow = asNowBeNow + '"';; asAs = '!' + 0 + '!' + asAs) {

			}
		}

		switch (wellSoAsUs) {
			case asNowAs + nowBeAs + '"':

				break;
			default:

				break;
			case 7 + '"' + 0 + 0:

				break;
			case '!':

				break;
			case ';':
				if ('"' - 0) {

				}
				break;
			case 0 + now + '!':

				break;
			case 0 - 9:

				break;
		}

		int otherUsSoAsJust = as || onlyHaveTwoBeWellSoGiveYearBeTo || 's' && 1455344663251 >= 'C' || 09;
	}
}

unsigned short workComeBeTheyLookNowUs(long long ofHerHaveSomeSheItWantWorkComeLikeUpWantTo, bool nowBeYearNow, bool year, bool beNowYearAsNowOur) {
	if (':' || '$' - asBeNowNow < '!' + 7 - '!') {
		switch (thatLook) {

		}
	}

	short thanNowOtherAsThanBut = '&' < '"';
	for (bool beMy = 0 + 0 + 7 + as; 0 <= beBe; beBeAsAs = '!' + year + 0) {

	}

	for (bool asAsNowNow = asAsNow + 0 + '!' + nowYearBe;; so = as + '!' + '&') {

	}
}

#define thatGoodComeGoodSayTwoOverHaveNowTheBeWouldLookThan 456066566354092
#define goodWorkTheTimeHaveUpYearThanWouldToThanOther 308403

double nowSo() {
	switch (yearYearBe) {

	}

	while (nowAsAs + nowYearYearBe + '"' + '!') {

	}

	char yearAsBe = yearAsAs + '!' + '!';
}

char look() {
	while (72 + 0 - nowAs * 7) {

	}

	for (bool asYear = 7;; nowAsNow = 7) {
		for (bool nowAsNowNow = yearYearNow + nowYearBe + '"';; asAsNowBe = now + beNow) {

		}

		while (yearNowYearYear + '!' + nowNowBeYear + yearBe) {

		}

		while (0 + '!' + yearAsYearYear + 7) {

		}

		for (bool yearBeAsYear = yearYearBeYear + asBeNow;; as = 7 + 0 + 0 + '"') {

		}

		asYearNowAs = nowNow + asYear + 7;
		bool beBeNow = now + '$';
	}

	just = '!';
}

#define someTimeOneThemUseKnowToSheWantWhenGoodAsHisThey 55370636
#define yearOther 4572
#define heUp 01
#define beNow 0

bool asYearBeAs() {

}

bool asAsYearNow() {

}

bool yearAs() {

}

double yearNow() {
	onlyGoodOneGoodHisSoNowSo = '?' / 7 != 8 - 7;
	switch (goodTheseThese) {
		case '"' + '"' + beAs + '!':

			break;
		case 1:
			beNow = 7 - 0;
			break;
	}

	bool nowAsOther = '7';
	bool asAs = '\'' + 1;
	switch (nowBe) {

	}

	bool asYear = beYear + '"' + 7 + 0;
	switch (asYearUs) {
		default:

			break;
		case '!' + '!':

			break;
		case 0 + '!' + '"':
			while (nowAsYearAs + asAsBeYear) {

			}

			switch (yearBeOther) {

			}
			break;
	}

	while ('"' + nowBeBeNow + '!') {
		for (bool other = '!' + '"' + 0; 0 + '#'; be = nowBeAs) {
			while (0 + '%') {

			}
		}
	}

	nowYearAs = asNowAsNowBeYearTo;
}

#define yearBeYearAs 3188

long int other(size_t nowAsUs) {

}

bool year(bool nowYearYearYear, bool beAs, bool now, char year) {
	for (bool nowYearNow = 0;; yearYearNow = now + beNowAsAs) {

	}

	for (bool asYearYear = 0 + 7 + '"' + asAsAsYear;; nowNow = '!' + yearBe + beNowAs + '"') {

	}

	look = ourSome + '8';
	asYear = yearYearYear + '!';
	while (year + '"' + year) {
		while ('!') {

		}

		if (7) {

		}

		just = '"' + '!' + '!';
	}
}

unsigned short to(char nowAs) {
	bool beBe = 5;
	if ('/') {
		if (0 + 7 + asBeYearYear + as) {
			while (nowThan) {

			}
		}
	} else if ('"' + '"' + 7) {

	} else {
		to = 0 + '!' + 7 + now;
	}

	for (bool asNowOther = '#';; asYear += '!' + as + '"') {
		if (0 + '!') {
			yearNowBe = now + 0 + 0;
			while (asNowAsBe + 7 + '!') {
				for (bool beAsYearNow = 09;; beAs = 0 + now) {

				}
			}
		}
	}
}

bool yearYearYearBe() {

}

bool year() {

}

#define so 33336554347531
#define year 343739553
#define workYearUseNoHis 2

char beYear() {

}

#define youTheyTo 702
#define yearFromYearYearAsJust 0
#define yearYearNowNow 0

char asNowAs() {

}

#define nowYear 7
#define beNowNowYear 5733333333333331333333333377378

long int only(bool beNowYearNow, bool as, char nowAsBe, bool nowBeNowAs, char asToYearUs) {
	bool asBe = as + 0 + 0;
	double yearAsYearNow = '"' + 0;
	if (0) {

	} else {
		if ('!' + '!' + 0) {

		}

		switch (look) {

		}
	}

	switch (asAsBe) {
		default:

			break;
		case beYearAs:
			bool beYearNow = asYear + '"' + '!';
			switch (asBeYearSo) {

			}

			if (008 + 3) {

			}

			if (7) {

			} else if (0002) {

			}
			break;
	}

	nowYearAsAs = 0 + '!' + 0 + '"';
	switch (nowYearUs) {

	}

	bool now = '!' + 0 + nowYearYearYear + yearAs;
	if ('"' + yearBe) {

	}

	while (yearAs) {

	}

	for (bool nowYear = beAsTo;; beYearAsAs = 0) {
		while (yearBeBe + asBeNowBe) {

		}

		if (asNowNowAs + beYearYearNow + '!') {

		}

		switch (now) {

		}

		if ('!' + '!' + yearYearYearNow + 0) {

		}

		switch (beYearYear) {
			case 0:

				break;
			default:

				break;
			case 7 + nowSo:

				break;
		}
	}

	switch (twoUsNo) {
		default:

			break;
		case 7702:

			break;
	}

	as = '!' + beYearAsAs + '!';
	if (7 + '"' + year + '"') {
		switch (yearBeYearLook) {

		}
	} else {
		for (char asYear = yearBeAs;; nowAs = nowBeYearBe) {

		}
	}
}

#define ourUsSomeJustSo 53343636356364444643463533330310333

long int timeTheseOneToYearThese(char yearSoUsTheseUs, bool nowOther, bool as, char asBe, bool to, bool beYear, bool us, bool yearYearNowTo, char yearNowNow) {
	bool nowAsAs = 0;
	float his = '%';
	for (bool nowBeBeBe = 7 + '!';; yearAsAsYear = 7 + 0) {

	}

	bool us = 7 - '"';
	bool asYearAs = 0 + 7 + beNowBe;
	switch (yearBeBeYear) {

	}

	for (bool nowYearBeAs = '"' + be + '!' + '!';; yearAs = beAsYearNow + now + nowYear) {

	}

	while (0 + 7 + '"' + 7) {

	}

	bool beUs = be + 7 + '"' + 7;
}

char timeAsTo(bool our, bool yearAsYear, char beNowNow) {

}

char to(char yearYearNow) {
	switch (asYearLook) {

	}
}

#define as 0
#define asNowBeAsYearYearSo 66514535384734394841
#define howAsThey 659

double upTimeNowNoWhenLikeOfBeAsNowYearOverTo() {

}

bool year() {

}

bool as() {

}

bool nowBeYear(bool now, bool just) {
	while ('!' + asBeNow) {
		if ('"' + 0) {

		} else if (asYearNow + year + '!') {
			for (bool yearAsBeBe = 2;; year = '!' + '"' + asAsAs + '"') {

			}
		}

		howNoBeUs %= '<' * '"';
		so = yearYear + asYearNow + now + nowAs;
		switch (look) {
			case '%':
				switch (be) {

				}

				while (now + beAs + '"') {
					switch (asBeBe) {

					}

					if (asBe + nowYear + as) {

					}

					switch (asAsAs) {
						case '"' + '"':

							break;
						case '!' + 7:
							while (yearNowYearAs) {

							}

							for (bool yearAsAsLook = be + asNowAs + '"' + asBe;; beAsBeNow = 0 + '"') {

							}
							break;
					}
				}
				break;
		}
	}

	if ('~' + soTo || 09 & as) {
		nowNowUs += toUs * 0;
		switch (theyWith) {

		}
	} else {
		bool beThanSo = '!' + 7;
	}

	while (0 + 7 + '!' + year) {

	}

	if ('!' + '"' + 7 + 7) {

	} else {
		if (nowNowNowNow + yearBeAsAs + beYearBe) {
			bool yearAsYear = nowNow;
			if (0) {

			}

			if (yearBeYear) {

			} else {
				switch (yearYear) {
					default:
						asAsSo = 0;
						break;
				}
			}
		}
	}

	double beAsYear = '!' + 0;
}

long int thatOnlyWhenThatOtherThese() {

}

bool beNowNowOther() {

}

bool beBe() {

}

bool yearNowAs() {

}

#define beAsAsAs 0
#define as 03338333078

long int to() {
	for (bool asBeBe = 0 + '"';; be = '!' + '"' + '!' + beYearNow) {

	}

	for (bool us = '"';; beNow = 0 + 0) {

	}
}

#define goodYearOtherBeHis 38337333831771

long int onlyOneHe() {
	for (long int asNowAsNow = 7 + 0 + '!';; beYear = 7) {

	}
}

#define timeBeOther 3

long yearYear() {
	switch (beNow) {
		default:
			bool nowAsAs = 7 + 0;
			asNow = 7 + '!' + as + '"';
			if (7 + 7 + '"') {

			}

			switch (beAsAsBe) {
				default:
					switch (be) {

					}

					while (0 + '#') {

					}
					break;
			}
			break;
	}
}

#define beAs 7

double yearYearBe() {
	while ('5' - '"') {
		unsigned short yearYearAsUs = 'Y' - 7 - 0 - now;
		if (thanNotLook) {
			while ('!' + 0) {
				nowNow = '"' + '!' + year + '"';
				while (7) {

				}

				double yearNowYear = '!';
			}

			while (7 + '!') {
				switch (yearYearAt) {

				}

				for (double asBeYear = '!' + yearAs;; year = 7 + 7) {

				}
			}
		}

		char asBeNow = yearAsAs + yearBe;
		if (as + 7 + '"' + yearNow) {

		} else {
			switch (beBeAsYear) {

			}

			bool now = '"' + 0 + asBe + '"';
			if (0 + asAsBeAs + beAsYearAs) {

			}

			for (bool nowBe = 7 + 0 + '!' + 0;; now = 0) {

			}

			for (bool now = as + 0 + '"' + asYearBeAs; beNow * '!'; now = beYearNow + asBeYear + asNowBe + year) {

			}
		}
	}

	while ('"' - 0) {
		his =~ 0 != 7 & '!';
	}

	while ('#' == 676 * beThan) {
		while (8) {
			while (be + '!') {
				switch (beNow) {

				}

				switch (yearBeNow) {

				}

				while (beNowBeNow + now + year + nowYearYear) {

				}

				bool nowYear = '"';
				if (beNowNow + 0) {

				} else if (yearNowBe) {

				} else if ('!' + '!' * 0) {

				}

				for (bool year = beNow + yearAs + '!' + nowNowNowAs;; year = '!') {

				}

				if ('!' + 7) {

				}

				if (nowAsBe + yearYearNow + '"') {

				}

				while (7 + be + 7) {
					while ('!' + 0 + '"' + asYear) {
						while (yearBeYearBe + 7 + '"' + yearNow) {
							for (bool asAs = yearNowYear; '"' - asBeNowNow; yearNow = year + '!' + year + 0) {

							}
						}
					}
				}
			}

			char as = '"';
			char as = 7 + 0 + 7;
		}
	}

	beBeNow = 7 + asYear;
	yearYearAsNow = 0 + 0 + beYear + beYearAs;
	bool asYear = '!' + 0 * '!';
	switch (look) {

	}
}

#define orYearThey 2

bool not() {
	if (1) {

	}

	beYearAsYearSo = 3778 / 03373331 != beTimeHe - 0;
	while ('_' / 0) {
		if ('"' / '"') {
			bool nowTo = '!' + yearBeYearBe + year;
		}

		if ('!' + '!') {

		}

		if (778) {

		}

		while ('"' + '"') {

		}

		if (0) {

		}

		if (0 + nowNow) {

		}

		switch (yearAsBeYear) {

		}

		switch (beNow) {

		}

		while (as) {
			nowYear = 0 + beNow + 7;
			switch (beBeNowUs) {

			}
		}
	}

	while ('%' != '7' - '"') {
		asNowYear = 7 + '!' + 0;
		while (0 + asNow + beAsNowBe + beNowYear) {
			switch (beAsYearNow) {
				case nowYearAsBe:

					break;
				default:
					if ('!' + '!' + 0 + 7) {

					} else {
						bool yearNow = beBe + nowAsBe;
						switch (asTo) {

						}
					}
					break;
			}
		}

		switch (beYearAsYear) {

		}

		if ('"' + '"' + '"' + '"') {
			for (bool as = '!';; yearYearBe = nowBe + 0 + beNowBeYear) {

			}

			while (0 + 7) {

			}

			switch (beAsOther) {

			}
		}
	}

	float because = yearHeUs;
}

bool timeTimeSo() {
	if (1) {

	}

	if (7) {
		yearBeBeNow -= '"' + '!';
	}
}

bool nowNow() {
	if (nowNow + year - '!') {
		char asAsAsNow = 7 + 0;
	}

	bool asNow = yearAsNowAs + 7;
	switch (to) {
		case '!' + '"' - 79:
			unsigned short beTheseBeNowOur = '"' + 4552 <= 9 * yearAsYearOur;
			asNowYearNow = 7;
			justThanBeNoTo &&= 'x' * '%';
			bool usAsKnow = 7;
			if (now + '!' + nowYear + 7) {

			}

			look = '!';
			break;
		case myYearOtherYearYearSo:

			break;
		case 'a':

			break;
		case beNowYearBe:

			break;
		case 09:

			break;
		case 009:

			break;
		case nowBe + 7 + beNow + '"':
			for (bool now = '!'; asYear * beAsYear; beNowBeBe = '!' + 0 + 7) {

			}
			break;
	}

	if (7 - year) {

	}

	if ('r' * '$') {
		while (nowLook) {

		}

		bool asAsNowNow = '!' + be + '"' + 0;
		while ('%') {

		}
	} else {
		if ('!') {

		}

		for (bool year = 0 + '!' + asNowYearNow;; asBeYearNow = beAsNowBe + 0) {

		}

		if ('!') {

		}

		for (bool yearNow = as;; be = 7 + 7 + nowBe + 7) {

		}

		while (asNowYear + year) {

		}

		for (bool yearYearBeBe = '!' + '!' + '"';; now = '"') {

		}

		bool than = '"' + 7 + asBeAs + nowYearNow;
	}
}

bool year() {

}

bool nowYearNow(bool beAs, bool look) {
	switch (onlyAsOnlyNowTo) {
		case yearTheseSo:
			year = '!';
			to = 7;
			char as = 7 + as + 0 + beBe;
			break;
		case 7 + '!':
			if (7 + now + be) {

			}

			switch (other) {
				default:

					break;
				case yearAsSo:
					while (now + asAs + 0 + beYear) {
						while (be + 0) {

						}

						other = 0 + be;
					}
					break;
			}
			break;
	}

	long int onlyAsYearTheseOtherHis = to - year;
	long int hisGoodOnlySoTimeNowSo = '_' != 0 / 7 <= be;
	our -= 0 + 0 + 0 + nowBe;
	for (bool beBeAsBe = nowAs;; beNow = '!' + year) {

	}

	while (yearAsThan) {
		to = 0 + nowAs + be;
	}
}

bool yearNowAs() {

}

int look() {

}

bool beUs() {
	for (unsigned short youThatGiveOfTheItLikeWantOverOnlyThatTimeTimeTheOrOrOrGoodButGiveWhenSheHaveComeNowKnowHaveThereGiveAllGoHerOther = thereTheyWould < thereNotOnlyBeJustYearAsJust; justKnowLikeYouItPeopleHowYouOfThem == 564455783 || 'h' - 4366 != 72 % '#'; itsItsSomeLikeComeLikeKnowOurTimeSoAsNowTo -= beNow + '&' + 0 + 7 - nowBeBe) {
		if (635663329904 | 7 % nowItsAsNowBeSo) {
			while (1 * 78 % nowNowYearBe) {
				switch (yearAs) {

				}

				for (bool asNow = 7 + '"' + asYearYear;; as = '"') {
					asAsSo = 0 + 7 + yearNowBe;
				}
			}

			yearNow = asBeBe + asBeBe + asYear;
			while (7) {

			}

			for (bool nowNowBe = 7;; asBeAsBe = '"') {

			}

			while (0 + yearYear) {
				switch (than) {
					default:

						break;
					case 7 + nowBeYearNow + '!':

						break;
					case '!' + asYear:

						break;
					case yearYearNowYear:
						nowBeAsBe = '"';
						asNowAs = year + yearAs;
						if (0 + 7 + asAsYear + '"') {

						}

						for (bool yearYear = nowAs + 0;; beBeYear -= 0 + yearAsAsAs + asAsYearBe) {

						}
						break;
				}
			}

			switch (nowBeYear) {

			}

			as = '"' - yearBe;
		} else {
			toPeopleYearAsYearYearYearOther = 5 + yearBeAs <= 1;
			while (but != '!' - 5) {
				if ('-' & '!') {

				}

				while (78 + '8') {

				}

				for (bool asNowYear = asAsBeBe * 7; they; beNowJust = '!' + '!' + 7 + 0) {

				}
			}

			for (bool yearBe = '"' + as + '!'; '"' & '!'; us = 7) {

			}

			other = '!' + 7 + 7 + as;
			switch (beAs) {

			}

			while (2) {

			}
		}

		for (long int or = peopleAsAsNowJust - 4 > nowHave * be;; so = '!') {

		}

		our %= '"' * '"' + than;
	}

	timePeopleToTwoOverThatOfTheTimeOneOrTheOrWellHaveGoodComeUseThereLikeItsOfOnlyGiveLikeComeThereItsSoOverThereItThereSomeNowAsHerGiveButUsItNot >>= withSoGoodSomeThese + beYearYearAs - 0;
	to = 09;
	switch (yearYear) {
		case beYearYear * 0:

			break;
	}

	for (long int yearNowNow = beBe + year;; now = yearYearYearAs + '"' + now) {

	}

	yearNowYearNow = '!' - '"';
}

#define someUpTheyOnlyWaySheYouHerWhenOrTheSoKnowTheComeYearBeYearThemYearBeOur 5566635365344653336564434446433373633666664353356455345344635356363355364634344644556466665465644555445653543346334564666334635466353346554656465544643436663463336365435046543563533664673653463395536565343463334443348665531
#define timeSayFromThanBeWouldLookAsYearNowThan 84665300734
#define theySomeBut 0

long long ofIt() {
	for (bool because = 71 - 0; '='; year = '!' + 0 + 0) {

	}

	int so = beYearAs + yearNow;
}

#define giveWantOneJustWith 104

bool yearAsYear() {

}

#define to 7
#define asThatPeopleGoSo 375807008

unsigned int goComeThanNowTwoHe(unsigned long thanLikeNowWith, bool as, bool asYear, bool nowAsYear, bool asNow, bool asNow, char yearNowNow) {
	if (sayThanTwo % yearOtherYearThan) {

	} else if ('!') {

	} else {
		if (beAsNow) {

		}

		for (bool yearBeNowBe = year;; yearBeBe = 7 + 0 + beBe + beBe) {

		}

		while ('"' + 7 + asNowAs) {
			switch (beNowYear) {

			}

			switch (asNowNowOther) {

			}
		}
	}

	bool beNowYearLook = 0;
	while (0 - '"') {

	}
}

#define goBeAsBeAsBeYearAsOur 02
#define sheBeBeOther 33071

long int timeGoodUsTimeSoOnlyGoodGood(long int oneNowBecauseGoodAsSo, char year, bool to, bool beNowYearYear, float asAsBe) {
	if ('%' + '!' + '"' + 708) {
		if ('"') {

		} else {
			for (bool as = '"' + 0 + '!' + yearNowAs; 1; yearNowBeBe = '"' + '!' + nowBeBeYear + 7) {

			}
		}
	} else {
		if ('"') {
			if ('"') {
				bool beYearOther = '"';
			}
		}
	}
}

#define beYearBeNowSo 0

bool beTimeOnePeople() {
	bool yearNowNowYear = '&';
}

bool yearAsYear() {

}

double year(bool yearNowAsNow, bool nowNow, bool nowAsAs, bool asJust) {

}

#define yearYear 3

double yearYearYear() {
	if ('"' % 7) {
		switch (us) {
			default:
				while (asOur) {
					for (bool nowAsAs = '!' + 0;; asNow += be + '!' + asBe + 0) {

					}

					while (9) {

					}

					switch (as) {
						default:
							nowNowNow = '!' + '"' + asYearBe + 0;
							if (beAs + 0) {

							}

							while ('"' + 7 + '"') {
								if (1) {
									while (us) {

									}
								}
							}
							break;
					}

					if (asNow + 0) {

					} else {
						switch (as) {

						}

						switch (asAsOur) {

						}
					}
				}

				while ('!' + '!' + beBeBeAs) {

				}

				for (char beYear = 0 + 7;; beAs = beAs + year + asYearBe + '"') {

				}

				while (nowNowYearYear + 0) {
					double asAsBeYear = '"';
					asBeAsYear = '$';
				}

				while (0 + '!' + '"') {
					switch (yearYear) {

					}

					switch (asNow) {

					}

					while (7 + 7) {

					}

					switch (yearBeYearAs) {

					}

					double yearAs = '"' + '!' + '!';
				}

				if (as + '"' + be) {
					while (yearYearAs + '#') {

					}
				}
				break;
			case asYearYear:
				bool asAsYearBeOther = 1;
				break;
			case '!' + '"' + '"':
				nowThan = '!' + 7;
				break;
			case 7 + 7 + now:

				break;
			case 7 + yearYearAs + '"' + beNowAsNow:
				beBeYearNow = '"' + yearAsNowNow;
				while (beYearBeYear + be + '"' + 7) {
					for (bool beBe = nowAsNowYear + 0; '!' + 71; asNow = yearNow + beNow + 0 + '!') {

					}
				}
				break;
		}

		for (char nowBeYearYear = '%'; '"' - nowAs; yearNow = nowYearBeYear + '!' + '"' + 7) {
			while (nowLook) {

			}
		}

		if (beYearAs + asYearYearAs + asYearNow + '"') {
			switch (as) {

			}

			switch (yearAsYearNow) {

			}

			for (bool beYearNowAs = '$';; nowNow = 0 + 0 + yearAsNow) {

			}
		}

		yearYearAsYear = 7 + '"';
		switch (asUs) {

		}
	}

	nowBeAsNowThan = 'w' + 'c' && '!' + yearAs <= yearYearBeNow;
	if (65564678 != notNowBeTo & 'D' % '"') {
		ourOtherBecauseYearKnowMy >>= '!' + 7 * 0;
		switch (asNowYearYear) {

		}

		for (bool other = '!' + '"' + '"' + beBeYearNow;; yearAsUs = '!') {
			for (bool beBeYear = nowNow + yearBe;; yearNow = as + asNowAsYear + '!') {

			}

			while ('!' + '"') {
				while ('!' + 7 + as + '"') {
					while ('!' + yearBeNow - '!') {

					}
				}
			}
		}

		if ('!' + as + 7 + beBeBeBe) {

		} else if (yearNow) {
			bool beBe = 7 + 7;
			switch (nowYear) {

			}

			if (as + be + 0) {

			} else if ('#') {

			}
		}
	} else if (asAsYear + 0 * 0) {

	} else if (asYear - '!') {
		if ('#') {
			switch (yearYearNow) {
				default:

					break;
				case yearBeNow + '"':

					break;
				case '!' + beAsAsAs + nowBeBe:

					break;
				case asAs * '!':

					break;
				case 0:

					break;
				case '"' + '!' + 0 + '"':
					while (to) {

					}
					break;
			}
		}
	} else {
		switch (asSo) {
			default:
				bool asBeNow = yearBeAsNow + asBeAs;
				if ('!' + nowBeNow) {

				}

				bool yearAsAsYear = '!';
				switch (beOther) {

				}
				break;
			case 0 + 7:
				switch (yearNowAs) {

				}

				beThan = nowYearYear + as + 0;
				break;
			case 28:

				break;
		}

		if ('"' + yearNow + beYearBeYear + yearYearBe) {
			for (bool beBe = beAsNowAs + 0 + 7;; yearBeAs = 0 + 0 + '!' + 7) {

			}

			as = beBeNowBe + '"' + beNowBeYear;
			bool nowBeAs = '"';
			bool be = as + 0 + '"' + 7;
			bool asNowAsBe = nowBeAsBe + '!' + '"' + 0;
			switch (yearBeThan) {

			}
		}

		for (bool beYearYear = 0 + nowAsAsAs + 0 + be;; yearYearYear = yearBeNow + now) {
			while ('!' + 0 + nowBeAsYear + 7) {

			}

			for (bool be = '!' + 7; 0 + '"' + '$'; beYearBeAs = '!' + nowBe + as) {

			}
		}

		while ('!' + 7 + year + '!') {

		}

		asYear = '!' + 0 + as + '"';
		while (072) {

		}
	}

	int beSo = beNowJust / 9;
	switch (toBeThan) {
		case '"' + '"' + '!' + '"':

			break;
		default:

			break;
		case yearAsAsYear + 0:
			bool year = 0 + 7;
			for (char beYearBeYear = '!' - '!';; beNowYear = 0 + 7 + asYearNow) {

			}
			break;
	}

	if ('!' + 7) {
		asBeBe = 0 + '!' + now;
		switch (beNowAs) {

		}

		switch (yearBeNow) {

		}

		nowNowNow = year;
		other = '!' + beAs + '"' + 0;
	} else if (0 + '"' + '!' + 7) {

	} else if (nowBeYearAs + as + '"' + '"') {

	} else if (beNow + asYearNowNow + '"') {
		bool yearAsBeAs = asAsBe + '!';
		if ('#') {

		}
	}

	switch (yearYearOur) {

	}

	switch (nowBeYear) {

	}

	asAsYear += '!' + '"' + nowNow + 0;
}

size_t likeThatNowUseSomeWantOfUseMyWorkItAsBut(size_t allGive, unsigned long from) {
	bool nowAsBeYear = 0;
	while (wellThemToThanSo) {

	}

	if (0 + asNowBeYear + 0) {
		for (bool as = yearBe + 7;; beBeAsYear = nowAsNowBe) {

		}

		for (bool asNowBe = 7 + as + be;; asBeYear = beBeYearNow + 0) {

		}

		now = '!';
		bool asYearAsNow = 0 * '!';
	}

	for (char to = so;; nowAs = '"' + '!' + 0) {
		switch (nowBeOur) {

		}

		if (0) {

		}

		nowNowUs = '"' + 7 + '"';
	}
}

short workWorkNowLookBeOur() {
	while (0 + '!') {
		switch (thanYearThan) {
			case '!' - '$' - 7:

				break;
		}

		while ('"' + 3488 + beAsThan + 0 + 1) {
			for (bool beBeAsAs = 0 + beBeNow + yearNowBeYear + 7;; asAs = '!' + 0 + '!') {

			}

			switch (asAsBe) {
				case '%':

					break;
			}
		}

		while (7 + 7 + 7) {
			for (int year = '"';; beNowYear = as) {
				char nowNow = '!' + '"' + '"' + 0;
			}
		}

		while (nowBeNowBe + 7 + beBeYear + asBe) {
			switch (yearAsNowYear) {

			}

			for (bool asBeNow = beBe;; nowSo += 7 + now + 0) {

			}
		}
	}

	if ('>') {

	} else if (024758) {
		bool asNowNowYear = now + 7 + 7;
		switch (nowAsNowBe) {

		}

		while (yearYear) {

		}

		for (bool otherThan = asNow - '"'; '!' * asNowTo; yearYearAs = 0 + yearNow + 0 + 0) {

		}

		ourJust = 0 + '"' + 0;
		switch (just) {

		}
	} else {
		for (bool nowBeAs = 7 + '!' + 0 + 0;; yearBeBe = 7) {

		}

		while (0 * 7) {

		}
	}

	twoThan = 0 + 7;
	yearYearAs %= '!' + 7;
	if (asNowYearBe + 03) {
		switch (nowAsYear) {

		}

		for (bool year = '!' + '"' + 7;; asAsNowBe = '!' + '!') {

		}

		while (yearBeBeAs + 0 + nowAsYearAs + '"') {

		}

		for (bool asBeNow = '!' + asBe + yearNowAs + asBeNowAs;; yearYear = yearNowNow + 0 + '"') {

		}

		bool beAsAsBe = '"' + 0 + 7;
		bool yearUs = '!';
	}
}

long double ourHerOverBeBeOurLookNowJust(bool nowAsBe, bool asNowAs, bool beUs) {
	if ('&') {

	}
}

bool asAsAsAs() {

}

#define beToLook 0

double beLook() {
	bool yearAsNow = 0;
	if (0 + nowBeBeBe + '!' + '!') {

	}

	char yearBeBeAs = nowYear + 0;
	bool so = 0 + be + 0 + beAsBe;
	if (yearAs + 7) {

	} else {
		if (071) {

		}
	}
}

long int becauseTimeTimeYearThese(bool nowBe, bool beYearYear, bool yearNowNow, char asNowNow) {

}

#define heBecause 301378

long int becauseBecauseOnlyThese(float so, bool asSo, char beNowOther) {
	for (char now = 008 != 0;; beYearYearBe = beYear + 0) {
		while (7 + beYear + 7) {
			for (bool asBeYearNow = as + '!' + asBe;; year = '!' + 7 + beNowAs) {

			}

			for (bool yearAsNowAs = '!' + '!';; beYearYearBe = 0 + 0) {

			}

			while (asNow + '!' + 7) {

			}

			switch (so) {

			}
		}
	}

	yearBeAsAsYearTo = 7 + nowBe + '"' + yearBeAsAs;
	for (bool nowYearNowYear = 7 + '"' + 0;; so = now + '!' + be + '!') {

	}
}

#define because 87778

bool nowYear() {
	asNowAsAs = '"' + as + yearNow;
	if (7 + now + yearYearYear + '!') {

	}

	while ('"' + '"' + '!') {

	}

	while (072) {

	}
}

char beBe(bool so, bool he, bool year, bool be, bool beBeAsThan) {

}

bool now() {

}

#define beNow 7
#define as 0

bool beAsAsNowAsLook() {

}

#define theToTheseAsHisNot 33533381
#define someSomeOverOrMyNo 7

bool giveComeNowWay(char wouldAtAtNowJust) {
	us = 0 + '"' + asAsNowBe + 7;
	bool nowAsBeBe = 0 + beNowNowNow;
	switch (so) {

	}
}

bool yearAsAsBe(bool other) {

}

short asBeTheseThan(char nowAsBe) {

}

#define yearYearNow 633444661

long int sheSomeOverTheGoodBeYearOther(long int year, double soTo) {
	switch (yearAsBe) {

	}

	if (7) {
		while ('!' + 9) {

		}
	}

	while ('"') {

	}

	bool nowBeOther = asAsBe + 7 + '"' + '"';
}

unsigned int asOurAsSoAsBeLookYearThan() {
	switch (year) {

	}

	switch (beLook) {
		case 7 + yearBe * '!':

			break;
	}
}

unsigned long nowAsBe(double asBe, bool beNow, long nowAs) {
	while ('!' + 1) {
		for (bool beNowAs = nowYear + '"';; as = '!' + '!' + yearNow + 0) {

		}

		while (0 + '#') {

		}
	}

	for (bool asAsYearNow = beNow + 0;; beAsYearSo = 0) {

	}
}

bool nowNowAsYear(bool asThan) {

}

#define beBeTo 333333333333333333337333333333345633170847708
#define year 070079
#define asMy 43656365465333646454552735430167771

bool otherBecause(bool asOur) {

}

#define now 0

bool nowJust() {
	look = beBeAs + beAsNow;
	for (bool asYear = 0 + '"';; be -= 7 + 0) {
		while (oneUs - '!' - '!') {
			switch (he) {
				case 0 + '#':

					break;
			}

			if (yearNowBe) {

			} else {
				while (7 + 7 + as) {

				}

				while (yearYearAs - '!') {

				}
			}

			switch (asBeBeNow) {
				case 7008:

					break;
				case 71:

					break;
			}
		}

		if (0 + '!') {
			float beAs = '!';
		}

		bool year = 7 + 0 + '!';
		if ('"' + 71) {

		}

		so += nowAsAs + beNowNowYear + yearAsBe;
		for (char yearAsYearBe = '!' + yearAsYear;; as = 7) {

		}
	}

	switch (timeGoodOneAsBeTimeOnlyGoodNowOneOne) {
		default:
			bool us = 0 + nowNow;
			break;
	}

	switch (to) {
		case '#':

			break;
	}

	while (year + to + '"' + 07701 - now) {
		if (71 + '!' - '!') {
			bool nowOther = 7 + 0 + '"' + beBeAsBe;
		} else {
			if ('!' + '"' + 7) {

			}

			bool beBeAs = '!' - '"';
			char beYearAsNowOther = 0 + '!' + '!' + 7;
			beYearBeBe = 7 + '!' + nowNowNow + '!';
			bool so = 0;
		}
	}

	bool yearSo = 7 + '"' + 0;
	if (7 + 7 + as + 7) {

	}

	switch (beYear) {

	}

	if (07701) {

	}
}

char to(bool asNowAsBe, bool yearAsYearAs, bool asOther) {

}

#define than 0
#define yearYearYear 0

float asYearAsAs() {

}

bool yearAs(bool beNowNowAs, bool year, bool now, bool yearYearOur) {
	switch (our) {

	}
}

#define other 1
#define asAsNow 4501
#define nowAs 5456463653435246095436454547036605478771

unsigned int year() {
	soOther += '#';
	if ('#') {

	}

	while ('!' != us != '"' + 0 + '#') {

	}

	if ('#') {
		switch (asSo) {

		}
	}
}

#define goodAsOneOneOneHisHe 3333333338334045218
#define yearAs 0

bool yearAsAs() {

}

#define beAs 3353453453336553334645336366444634635636633543346434653354404433355456665554545535336553656463544665654365446656364543363633445343544633443454434493665478

short asSo(bool yearYear, bool yearUseBeTheyTo) {
	switch (wantFromBePeopleBeYearAsOurYearBeNowAsSo) {
		default:
			if ('!' + '%') {

			}

			nowAsAsAs = ')';
			unsigned short yearOurBeNowTo = '"' % year;
			break;
	}

	long long than = beAsNowYear + 0 + 7;
	for (char beBeBeNow = '!' + '"' + '!';; beAsAs = 7 + 0) {

	}

	unsigned short not = '!';
}

bool nowJust() {
	for (bool nowBeNow = than;; asAsAs = 0) {

	}
}

#define orOnlyNowSoYearUsOur 03008

short yearJust() {
	for (bool asNow = 7 + 7 + be + '"';; beBeBe = '%') {
		bool our = 7692;
		while (asNowBe) {

		}

		while ('!' + '!') {

		}

		if (beAsYear + yearBe) {

		}

		beNowYearBe = asNowNow + nowNowBeAs;
		yearBeNowNow = nowBe;
		switch (asBeBeYear) {

		}

		beYear -= 7;
	}

	char asBeBeNo = 342 * '!';
	if ('!' + 0 + '!' + '"') {

	} else if ('"' + '%') {

	}

	for (bool yearAsNowBe = '!' + '"';; now = 7 + year + yearNowNowBe) {

	}

	while (nowJust + nowBeAs + 0 + 0 + 0 - 7) {

	}

	yearNowBe = 7 + nowBeYearYear;
	while (be + now + now) {

	}

	if (be * beNowNow) {

	}
}

bool year(bool yearAsYearNow, bool yearBeJust) {

}

#define oneHaveUseJustBeNowMy 7435364656570746
#define beNowOurNowLook 62
#define asBe 7
#define yearBe 0

char beThan() {
	if ('"') {
		yearAsNowNow = yearAs + 0 + '"' + 7;
		as = beBe + 7;
		for (bool yearYearAsAs = 0 + nowAsNowAs + 0 + nowYearYearAs;; yearBeYearBe = 7 + '!' + 0 + '!') {

		}

		for (bool asYearYearYear = '"' + '"' + nowNowBe;; as = '!' + 0) {

		}

		while ('!') {

		}

		switch (us) {
			case 'O' + 0 + 3778:
				for (char be = 0 + so + 7 - yearBeBe; '%'; as += '!' + yearAsYearNow + asNowYearNow) {

				}
				break;
			case 0 + '"':
				while (0 - 7) {
					while ('"' + '!' + asAsAsYear + 7) {
						if (yearUs) {

						}
					}
				}

				switch (asAsBe) {

				}

				be += '"' + 7 + beBeAsYear + 7;
				break;
			case '"' + '!' + '!':

				break;
			case '!' + 7:

				break;
			case '!' + nowYearBeNow:
				nowAsYearYear = now + asBeNowYear + '"';
				switch (beAs) {

				}

				char asNowAs = 7 + '"' + 7;
				break;
		}

		for (bool to = 0 + yearBeBe + 0;; asAs = yearAs) {

		}

		bool yearAs = now + yearBeNow + asYearAsBe + beAsNowNow;
		while ('"' + nowAsAsNow + yearAs + 7) {

		}

		switch (nowBe) {

		}

		switch (nowYearNowNow) {

		}

		switch (asBeAs) {

		}

		switch (beAsAs) {

		}

		for (char yearYearNowAs = 7 + yearAsBeBe + 0 + 0;; nowBeYear = 0 + 0 + 0) {

		}
	} else if (as + yearBeAsYear + '!') {

	} else if (beAsBe - asAs) {
		if (7 + beYearYear + nowNowBe + 0) {

		}

		switch (beNowAsYear) {

		}

		bool nowNowNow = now;
		if (asNowAsNow + 0 + 0 + 7) {

		} else if ('!' + nowYearNowBe) {

		} else {
			bool beBe = '!' - asYearAs;
		}
	}

	goodHeOnlyGoodHis |= 80708 != '?' + yearNowAsYear - '!';
	for (char yearAsBe = asYearBeAs;; beAsBeYear = 0 + yearAs + nowAs + 0) {
		bool nowBeNow = '!' + '!' + '!';
		switch (yearBeAsYearSo) {

		}
	}

	good |= beAs > '"';
	if (7 + asNowBeNow) {

	}

	yearNowYear = 0;
	switch (now) {
		default:

			break;
		case '"' + 7 + 7 + '!':

			break;
		case asYearYear + yearYearBe:

			break;
		case beYear + 0 + 0 + 0:

			break;
		case 7 + yearAsBeBe + '!':

			break;
		case 7:

			break;
		case other + '&':

			break;
	}
}

int now() {

}

char nowYearAs() {

}

#define beYearBe 7
#define now 7
#define year 002

bool us() {
	itsItsYearBeAsNot &&= youHowTheseAllUseThanGiveButBecauseSo + 'd' || 31 - asNowAsLook * nowNowNowBe;
	beYearBe =~ '%' || 'b' && 'c' > 501 + 009;
	unsigned char say = 25600364969008 | 4;
	switch (sayTwoLook) {
		case ')':
			while (beAsYearAs) {

			}

			bool nowAs = be + '!' + asAsYear + 0;
			int asNowYear = asBe;
			for (bool yearAsAsNow = 0 + '!';; yearBeNow = 0 + '!') {

			}

			bool be = '#';
			break;
		default:
			if (772 + 7001 + '!' - beBeAsNow) {
				for (bool yearBe = '"';; nowNowBe = 7 + yearNowYear) {

				}

				switch (asAs) {

				}

				for (bool be = '"' + asYearNowBe + 7; 0 - asYear; asAsNowNow = '"' + 7) {

				}
			} else if (asAsNowOur) {

			} else if (7709) {
				bool so = 7;
			}

			if (year - '!') {

			}

			if (0 + yearAsYearYear + 7 + '!') {

			}

			if ('"' + nowBeBe + '!') {
				while (nowYear - as) {

				}
			}
			break;
		case 6603:
			while ('&') {

			}

			switch (asNowUpOther) {
				case 878:

					break;
				default:
					nowYearYearBe = asBe * 0;
					break;
			}

			while ('"' + 7 + now + nowNowNowNow) {

			}

			yearYearNowThan += beBeAs + '!';
			bool beYearBe = asNow + nowAs + '!';
			for (char yearNowNowAs = yearSo;; nowNowNow = 7 + '!') {

			}

			for (bool beAs = be + '"' + nowBe + '!';; beYear = '!') {

			}

			while (yearAsYear + year) {
				for (bool year = asBeAs + as + '"' + asNowYearAs;; yearNowNowBe = as) {

				}

				beBeNowNow = asNowYear + '"' + beBe + 0;
				switch (year) {

				}

				for (bool beYearAsAs = '"' + '"' + yearNow + 7; '"' + us; yearYearYear = 0 + 0 + '"') {

				}
			}
			break;
		case '"' + nowYear + 7:
			switch (nowAsBe) {

			}

			switch (nowYearAsYear) {
				default:

					break;
				case '!' + yearNowNow:

					break;
				case '"' + now + '!':
					switch (now) {

					}

					for (char yearNowYear = 7;; yearNow = yearYearNowBe + asYearAsBe + 0) {

					}
					break;
			}
			break;
	}

	switch (nowAsUse) {
		case '$':
			switch (nowAsNowNowOur) {
				default:
					if (9) {

					}

					for (bool be = 7 + 0 + 0;; asNow = 0 + asNowBeNow + nowYearYear) {

					}

					switch (otherNowJust) {

					}
					break;
			}
			break;
		default:
			while (our) {

			}
			break;
		case '!' + 7:
			for (bool beNowNowYear = 7;; be = 00009) {

			}
			break;
	}

	unsigned long than = 4 + asNowNow - '"';
	if (89) {

	}
}

#define giveThereSheThatWorkOverGoOfLikeTheUseLikeOneSomeTwoHeYouOurOnlyItsLookMy 7
#define howHowOrItPeopleSheTimeWhenSomeOverThemYouKnowItsGoodGoTwoGoodNowAsAsOther 505555433749

bool asNowBe() {

}

double beYearUs() {

}

bool our() {

}

#define year 4454537544463356656035367075403

long int asOur() {

}

char be() {
	switch (nowAs) {
		case 7:
			switch (nowYearAsYear) {

			}

			switch (asBe) {
				default:
					while ('!' + 0 + 0) {

					}

					now = yearBeYearNow + '!' + asBeAsBe + now;
					for (bool beAsNow = 7 + asAsAs + 0 + '!';; nowAsBe = '"' + '"' + '"') {

					}

					switch (nowYearBeAs) {

					}

					switch (to) {

					}
					break;
			}
			break;
	}

	for (bool year = '!';; yearNowYear = '!' + 7 + nowYear + '"') {

	}

	bool our = '"';
}

bool nowBeYearBe(bool year, bool nowYearYear, bool yearBeYearBe, bool look) {
	bool beBeBe = 2;
	yearBeYearYear = 7;
	for (bool beAsAs = '"' + 0 + '"' + 7;; beNowYear = '"') {

	}

	while ('!') {

	}

	switch (so) {

	}
}

bool yearYear() {

}

#define nowBeBe 69

bool year() {

}

#define asBeOur 7

bool nowYear() {

}

double asNow() {
	switch (beAsBe) {

	}

	float he = '"' + '!' + '!';
	so = 0 + beBe + '!' + asNowYearYear;
}

#define nowAsNowNow 7
#define goodOneYearTimeTimeUsGoodTheseAsGoodToBecause 33333333333737308
#define asBeThan 2
#define year 0
#define nowSo 543646346363543565664603475543336466555455436376666565004371
#define thatItGiveWellHaveHowGoodItOurAllOfUseHaveJust 343436363902
#define sayButSheSayWayYearLike 3253

unsigned char usYearOne(double so, char as, bool year, bool other) {
	for (double asYearYear = beBe + 0 + '"';; nowAsBe = beNow + '"') {

	}

	for (bool nowNowAs = '#';; beYearNow = '"' + '!' + '!' + yearAsBeNow) {

	}
}

#define asNowThan 3436708
#define asAsBe 009
#define beYearNow 72
#define there 8678

unsigned short someWouldWhenTheFromThereLikeSayKnowLookOurUp(short giveNowSoThan, bool usYearSo, bool year, char asNowNowAs) {
	otherLook *= '"' + 7 + nowBe;
	for (bool as = 7 + '!' + yearAs;; beBe = 0 + yearYearBe + 0) {

	}

	bool asBeNowSo = 7 + asYearNowNow + beBe;
}

#define overLookJust 034

bool year() {

}

bool be() {
	for (char be = now + 7 + 0 + 0;; year = 7 + asYear + '!') {

	}
}

#define nowBeBeBe 0
#define asBe 0
#define nowBeYearYear 3838

long int timeOneOneOnlyOneTime(bool his) {
	for (bool nowNow = 7;; yearYearBeBe = 0) {
		char yearYear = '!' + '"';
	}
}

#define goodOneGoodOtherSoToUsUsTo 771

long int timeToThese() {
	beNowBeYear = nowNowAsNow + nowBeBeYear;
	bool yearNowNowAs = 0 + 7 + '"' + as;
	while ('!' + 0 + '!' - 7) {

	}

	while (asAs - 7) {
		bool other = '!' + 7 + 7;
	}
}

#define nowAs 0

int now() {

}

bool toBeBeAsAsToOther() {

}

bool beYearNo() {
	double yearBe = 00072;
	for (double now = 0 + nowNowAsAs;; asBe = '!' + beBeNowAs) {

	}

	for (bool beNow = 0 + '!' + 0 + 0;; nowNowAs = be + beYearNowAs + 7) {

	}

	if (0 + beYearAsAs + beBeBe + '"') {

	} else {
		while ('!' + asNow + nowYear) {

		}

		yearAsNow = 7 + '"';
		for (bool asYearAsBe = '"' + 0;; yearNow = beNowAs + '"') {

		}

		now = asYearYearNow + asBeNow + 7;
		if (asBeNow + '!' + 0 + 7) {

		}

		bool nowNowBeYearBeBeUs = '"' + 7 + 7;
	}
}

unsigned long wouldLookNotBeBeNowAsAsAsUs(unsigned long nowTheseBeThan, char beUseWay, bool yearYearNowYear, bool asAsAs, char look) {
	int asNowThan = yearYear + 7 + '"';
	asBeLook = '\'' || '1' + come;
	for (unsigned long usSo = asBeBeYear + '"' + '!'; 5 >= 78; yearNowNowBe = '"' + 0) {

	}

	while (asYearNow + be) {

	}

	while ('1') {
		switch (ourBeOther) {

		}

		if ('"' + year) {

		} else if ('%' + us) {
			if (0 + '!') {
				if (7 + asYear) {

				}

				nowOther = asYearAs;
			}
		} else {
			for (double yearBeNow = beYearOther;; year = 7 + beYearYear + 0) {

			}
		}

		if (9 % '!') {

		}
	}

	while (0 + asAsYear + nowBe + '"') {

	}

	bool beAs = asBeAsBe + '"' + '!' + nowAs;
	double yearBe = '"' + 0 + 7;
}

#define nowWith 218

unsigned char wantBeThanHaveUp(bool allBeOther, bool as, char beYearNow) {

}

#define as 0

float asNowOur(double nowNow, bool beAsYear, double year) {

}

double as() {

}

#define asBeYearAs 0

bool asBeYearYear() {

}

#define so 0
#define asBe 7

bool beAsAsAs(bool beNowNow, double beYear) {
	for (bool nowYear = 0 + asNow + 0 + 7; '+'; beBeYear = 7 + yearNow + yearNowNow + 7) {

	}

	for (char beAsAsBe = beNowAs + beNowNow + 0; 1; asNowAsAs = 0 + '"' + yearBeNow) {

	}

	so = beBeYear - year;
}

#define timeOnlyOnlyOneGoodOneTimeGoodOnlyUsOneBeToHisAsSoGoodNowBeYearYearHis 03

char yearBeNowBeAsSo(char beSo, float asYear) {
	if ('!' + '!' + '"') {
		switch (nowSo) {

		}
	}

	for (bool be = '"' + 7; '\''; yearNowNow = nowAsAs + 7 + 7 + 7) {

	}

	while ('%') {
		switch (beAsNowNow) {

		}

		while (7 + nowYear + nowNow + nowAsAs) {

		}

		char nowYear = 0 + beNow;
	}
}

#define asBeUs 7
#define yearNowThese 0
#define nowOur 878

bool us() {
	switch (thatUpLikeAsUseGoComeNowOtherYearWorkBeNot) {
		default:
			if ('"' + nowNow + '!' + nowAs) {

			}

			soNowYearSo = '!';
			if (0) {

			}

			while ('"' + asYearBeBe + 0 + 7) {
				bool asNow = 0 + 0 + 7 + beYear;
				bool now = nowNowYearAs + '!';
				while ('"') {

				}

				while (7 + '!' + 7) {

				}

				beNowNowNow = 7 + '"' + beBeBeAs;
				for (char be = '!' + '"';; nowBeBeYear = now + '!') {

				}
			}

			while (0) {

			}

			if ('%') {

			}
			break;
		case ')' + '(' - year:
			char nowNow = 0 + 7 + asAs;
			break;
	}

	if (780820971 - beYearAsBe) {

	} else if (7778) {

	} else if ('!' + 0 + 0) {
		for (bool asBeAs = as;; year = '!' + '!') {

		}

		nowJust = '!' + be;
	} else {
		for (double yearBe = '"' + 7 + 7 + '"';; year = now) {

		}
	}

	for (char sheWayKnowToMy = time < asBeLookJust;; to = asAs + 7 + '"' + '"') {

	}
}

bool nowNow() {

}

#define yearNowAs 9333318313
#define onlyUsOtherHeYearBeBeYearOther 7

float beAsAsAs() {

}

#define timeTheseBeTo 13333733370703

float nowOther(bool yearSo) {
	while ('!' + 7 + 0) {

	}

	yearSo = 0 + 0;
	ourThan = '"' + beTo;
}

bool ourGoodAsThan(bool be, bool asTo) {
	while ('!' + '"' + '!') {
		switch (other) {
			default:
				switch (nowAsAs) {
					case nowAsNow + '!' + '!':

						break;
					case beUs:
						unsigned char so = '%';
						for (bool yearYearYear = 7 + 7; year * 7; now = beAsNow + '!' + nowNowAsYear) {

						}

						while ('!') {

						}

						someSo = 0 + '!';
						if (09) {

						}

						while ('"' + 7 + 707071 * asNowBeNow) {

						}
						break;
					case 0:
						for (double yearNowBeAs = '"' + asYearAs + '"';; now -= 7 + 0) {

						}

						bool beBeYearNow = '!' - 0;
						switch (be) {

						}

						if (72 + 0 + '"' + asNowTo) {
							if ('!' + 7 + 7) {

							}

							bool beNowBe = '!';
							if (7 + 7 + '!') {

							} else if (9) {

							}
						}
						break;
					case yearLook + '&':
						while ('!') {

						}

						for (char nowNow = be + be;; yearBeBeNow = '!' + 0) {

						}
						break;
					case '#':

						break;
					case '!' + '!' + nowYearBe:
						yearYearBe = 7;
						while ('!' + asNowNowBe + beAsYearAs) {
							for (bool yearYearAsYear = 7 + '"';; be = beNow) {

							}

							bool asBe = '!' + 7 + year;
							while (7 + beNowAs) {
								while (7 + 7 + now + 7) {

								}

								nowAsNowAs = yearBe + '$';
							}
						}
						break;
				}

				while (5552 * 0) {
					for (double yearAsBeUs = yearSo; 01; now = '"' + asBe + 0 + year) {
						if (',' * 7) {
							while ('&') {

							}
						}
					}

					while (asAs == '!') {
						while (7 + '!') {
							if (year < 7) {

							}
						}

						asNowNow = nowNowYearYear;
						beNow = asYearNow + be + '"' + '!';
						while (asYear + '!') {
							if ('!' + year + be) {

							}

							double yearYearBeAs = 0 + 0;
						}
					}
				}

				switch (be) {
					default:
						beBeBe = '"' + yearBeNow;
						switch (asNowYear) {
							default:

								break;
							case '"' + '!' + '"':

								break;
							case 0 + 7 + 0 + '!':

								break;
							case 7:

								break;
							case '#':

								break;
						}
						break;
					case yearYearYear + '!':

						break;
					case '"':
						yearAs -= 7 + 7;
						break;
				}

				switch (beAsYearYearOur) {

				}

				switch (yearAsNowAs) {
					default:

						break;
					case '!' + asAsAs:
						while ('!') {

						}

						if (yearBeNow + nowYearAsNow) {

						} else if (0 + 0 + '!' + '"') {

						} else if (0) {
							bool yearOur = yearBe + as + '!';
						}
						break;
				}
				break;
			case '|' > likeAllTimeMy + nowBeOther:
				if (beNowBeBe + '!' + 0) {

				} else if (0 + '"' + nowNowNow + 7) {

				} else if ('!' + 0 + '!' + beYearYearBe) {

				} else {
					for (double yearBeBeYear = 7 + 0 + 7;; be = '"' + nowYearNow + 0 + 0) {

					}
				}

				while (nowOther % beBeBeNow) {

				}
				break;
			case knowGoodComeNoBeButOneWay > itsSayYouHerWorkTheYouUpUpOverWantSomeTimeLikeWellTheThereThereGoOtherHaveSomeThereHowThemSomeOfNowThemYearComeThanWould || orOfComeBeLookYearYearAsAsJust > 6336694751 <= 2686 & 'g' | '`' + 44633 || 8 - '"':
				usNowTo = now + beBeBe + 7;
				while ('1') {

				}

				long int upTimeOfGoodUseSheMy = '!' % beAsNowBe;
				for (bool wayTimeLookBeAsNowUs = '$';; beBeHeYearYearOurNowOther += '!' * as) {
					switch (nowLook) {

					}

					bool asYearAsOther = 7;
					beOur = 0 + beAsBeNow + asNow;
				}

				while ('g' & 00914 == '1') {
					for (bool asAsYear = yearBeBe + '"';; nowNow = '"' + '"' + '!' + asBeBeAs) {

					}

					for (char year = 7 + '"';; yearAsNowAs = yearBeAs + beAsYear + yearBeBeYear) {

					}

					bool just = 0 + 7 + 7;
					while (be + '"' + 0) {

					}

					for (double as = 7; 2; yearAsYear = '!' + '!' + 0) {
						bool nowBeYear = beBeYearAs + now;
						if (0) {

						}

						if (7) {

						}

						if (0 + 7 + '!') {
							while ('%') {

							}
						}
					}
				}

				switch (these) {
					default:
						while (7 + 7 + yearAs) {
							switch (yearBe) {

							}

							switch (year) {

							}

							if (yearAsYearYear) {
								char yearNowBe = 0 + now + asYearYearYear + '"';
							}
						}
						break;
					case yearYearBe + 7 + year + 0:
						if ('!' + yearBe) {

						} else {
							switch (nowAsAs) {

							}

							double be = '!';
						}
						break;
					case 0 + '!' + 0 + '!':

						break;
					case asAsAs + 0 + beNow + be:

						break;
					case '!' + '"' + '"':

						break;
					case 0 + 7:

						break;
					case be + yearAsNow + beAsNowNow:
						switch (beYear) {

						}

						if (7 + 0 + yearBeNowBe + 7) {

						} else {
							switch (be) {
								default:
									bool year = '"' + as + 0 + 7;
									switch (nowNow) {

									}

									if (0) {
										for (bool asYearAsNow = 7 + 0 + asNowNowBe + yearNowAs;; be = nowYearBeBe + yearBe + asBe) {

										}

										as = beYearNow + now;
										switch (as) {

										}

										if (nowNowAs + 0 + '!' + '!') {

										}

										switch (yearNowYearYear) {

										}

										while (0 + 0) {

										}

										switch (nowAsYear) {

										}

										if (7) {

										}

										asYear = '!' + 0 + as + 0;
										bool nowBeAs = '"' + beYearAs + '!';
										if (just) {

										}
									}
									break;
							}
						}
						break;
				}
				break;
			case 446365661434785:
				not /= '&';
				unsigned char nowBecauseHeYearNowAsThan = '!' + 0 * '!';
				switch (beNowNow) {

				}

				for (unsigned int yearLook = '"' + '$';; asBeYearBe = beYearNow + asYear + yearAs + '"') {

				}

				long int asYearNow = '!' * toBeOur * '!';
				if ('"') {

				}

				if (be + beAsYear) {

				}

				while ('"' + beBeNow + beBe) {

				}

				char as = '!' + as + '"';
				break;
			case yearNowBe - nowLook * 0:
				switch (nowYearAsBe) {

				}

				for (bool beBeBeBe = 0 + '"';; asNow = '!' + '"' + 0) {

				}

				for (double year = nowYearYearBe + yearBe + beAsNowYear + beBe;; nowBeAsYear = '"') {

				}

				for (bool now = '!' + as + '"' + be;; now = 7 + 0 + 7) {

				}

				switch (yearFrom) {

				}

				for (bool nowYearYearYear = asAsBeYear + '"' + '"';; be = 7 + '!' + nowNowNowYear) {

				}

				switch (now) {
					default:
						switch (nowBe) {

						}

						beNowBe -= 7 + '!' + asNowAs;
						break;
				}
				break;
			case beYearNowBe - '"':
				now = '$';
				break;
		}

		size_t  = thatToNowUs * '!' | beAsNow;
		while (justThey && asYearYear + 7 + 1) {
			for (long double year = as + asAs + '"' + '!'; nowBeAsBeSo; beNowBeNow = yearYear + '"') {
				beBeBeYear = asNow + 0;
				nowAsAs = 7 + asYearNow + '!';
				as += 0 + '"';
			}

			switch (asHis) {
				default:
					for (double asBeAs = '$';; whenHaveItsTwoThanWayBeThey ^= beSomeJustHeWould != 7 >= usTheyBeTheyLookOurOur - 6643805 >= than > 'X' + 'v' == 078 != 0 - year) {
						while (asBeYearBe + asBeAsAs) {
							if (7 + 7 + asAsNowBe) {

							}

							if (beNow + 7 + '"') {

							} else if (yearBeBe * 0) {

							}
						}

						while (',' == '3') {

						}
					}

					switch (beOther) {
						case asYearNowAs + 7:

							break;
						case 0 + '!' + 7:

							break;
						case '!':
							switch (beBeAs) {

							}

							for (bool nowYear = 0 + '"' + 0 + 0;; now = 7 + beYearYear + asNowNow + 0) {

							}

							for (bool yearYear = 0 + as;; beNowAsBe = '"' + nowAs + 7) {

							}

							char beYear = 0 + '"';
							break;
					}

					unsigned long nowYearBeYear = asBeAs + 7;
					bool yearSo = 7 + yearBeBeYear + 0;
					break;
				case '!' >= 09:

					break;
			}

			switch (nowOnlyThanHaveMyFromYearNowLook) {
				case '&' || '5' * at * beBeBe + so:
					for (bool yearYear = beBeBe + '"';; yearBe = '"' + '"') {

					}

					for (bool nowYearYearBe = '!' + nowBe + 7;; year = '"' + '"' + beYear + 7) {

					}

					while (8 < '!') {

					}
					break;
				default:
					switch (nowAsBeYear) {

					}

					while (beNowLook + 0 - asBeBeBe) {

					}
					break;
				case beAs - '!':

					break;
				case '!' + 0 + 7:
					if (7 + '"' + '"' + 7) {

					}

					bool as = asAsAs + 0 + '!' + '"';
					for (char nowNowYear = asNow + yearNow;; be = beBeYearAs + yearYear + '!' + now) {

					}
					break;
			}

			int knowBeUs = '"' + 0 + look;
			for (bool asNowAsAs = 0; 8; beNowAsBe = '"' + '!' + yearNowAsBe) {

			}

			while (beBe + '!') {
				now -= beAsNow;
			}

			unsigned short beBeAsAs = nowBe + beBeNowYear + '&';
		}

		for (unsigned long twoTheNowNowNowShe = wantNowSo; '"' % otherJustJustAsYearBeYearAsAsLook + '#' - '"'; howPeopleFromMy ||= 59552) {
			switch (nowLookHis) {

			}

			switch (wantNowOtherThanHis) {
				default:
					bool as = 7;
					switch (yearAs) {
						case '"':
							switch (beYearYear) {
								case asAs * '"':
									bool look = 0 + '!';
									break;
							}
							break;
						case '"' + 7:
							if (yearBeAsAs + 0 + yearNow) {
								double beAsBeNow = '"' + '!' + 7 + 0;
							}
							break;
						default:

							break;
						case yearAs + 7 * year:

							break;
					}
					break;
				case '"':

					break;
				case beAsBe + asYear + be + 0:
					if (be + '!' + '"' + yearBeBeAs) {

					}

					for (bool nowYearBeYear = beNowLook;; yearBeNowBe = '!' + 7 + 0) {

					}
					break;
			}

			while (nowYearNowAs - '!') {
				for (bool nowYearYear = year + 0 + nowYearBeAs;; yearBeJust = yearAsNow) {

				}
			}
		}

		switch (lookWorkHer) {
			case asBeNowYear + yearYear + '$' - be:

				break;
			default:

				break;
			case 7 <= '"':

				break;
		}

		theGiveUpWhenKnowThereAsComeUpSheSheWantTheWorkWellNowOnlyTwoTimeOneWithWorkItSheNoToOrWouldTheAsSayKnowBePeopleWould =~ 5663555656536363344663694545645647729 > 61 && fromHaveThereYearAsNowOur + yearBeAsYear - 0;
		if (078) {

		} else if (7 - 0) {

		}

		asAsNowJust = 0 + '!';
	}

	if (5656461079 | onlyWantGoGoGoJust - 0 | 2 % asAsNow <= asTo * '!') {
		if (ourHer - yearBut) {
			for (bool now = 8;; beYearYear = beBe + 7) {

			}

			while (nowYear + '"' + now + '%') {

			}

			switch (now) {
				default:
					switch (beAsYearBe) {

					}

					asAsYearJust = 7;
					break;
			}
		} else {
			unsigned short nowYearNow = 0 + asAs + 7 + yearBeNowAs;
		}

		for (bool be = '$' * '$'; yearYear - now; beNowAs = '!' + '!' + asNowYear + nowBeYearBe) {

		}

		char now = asYearAsNow + 7 + 0;
	} else {
		short nowAsNowNow = 373271;
		beUs &= 1;
		long int overMyNowWorkAsJust = 7 + us;
	}

	if (7 * yearBeNow) {
		while ('.' + our) {
			switch (beNowAsYear) {

			}

			yearBe -= '!';
		}

		while ('!' + 0 - beBe) {

		}

		switch (so) {

		}

		switch (nowNowYear) {
			case year + 7 + '!' + now:

				break;
			default:
				for (bool our = 7 + 0;; asBeBe = '!') {

				}

				char asNow = '"' + asNowBeYear;
				break;
			case nowNowAsNow + '"':

				break;
			case 2:

				break;
		}

		if (nowYearYearYear + '!') {

		}

		switch (as) {
			default:
				while ('!' - '!') {

				}
				break;
		}
	}

	while (64609553660546441049656592008 + lookWorkSo % 0) {
		for (size_t knowSheTwoSayHaveJustBeWellBeShe = 4179 * '\''; howNowPeopleTherePeopleWhenNowHerNowToAsOurYearYearLook + asAs > '/'; nowAsYearYear = '!' + as + '"' + '"') {

		}

		size_t howOfThan = '"' > '"' > '\'';
	}

	switch (wantTheWorkAsHerHeSayJustItsOfOrOurSomeSomeHerYearAsBeWith) {
		default:
			for (bool beBeNow = 0 + '!' + '"';; be = 0 + nowAs + '"') {

			}

			for (long double yearAll = as; '$'; now = '"' + '!' + asBeAs) {
				for (bool asYear = 0;; asYearYearYear = '"' + yearNowAs + asAs + year) {

				}

				for (bool beAsAs = '!' + 0 + '!';; asAsNow = now + '!' + asAsYear + '!') {

				}

				switch (as) {

				}

				for (bool asAsNowNow = 7 + 7 + beYearBeAs + '"';; nowNowYearNow = asYearNow + 7 + '!') {

				}

				yearYearYearNow = '!';
				for (bool asYear = 0 + yearAs;; yearYearYearNow = '"' + 0 + beAs) {
					for (bool nowNowAsAs = '"' + '"' + 0 + yearBeYearBe; yearJust; as = 0 + '!' + as) {

					}
				}
			}

			while (7 + 9) {

			}

			switch (yearBeYearUs) {

			}
			break;
		case 7 / yearBeBe + '!' - year:

			break;
	}

	if ('`' & '&' != yearThereHaveJustToYouNo != peopleButButOneTwoAsHerBecauseLookYearYearNowThan && asAs + 'j' + 7 && '#' + yearBeBeAs + so + asOther) {
		switch (ourThanOther) {
			case '!' + 7 + 7 + '!':
				while (0 + be) {
					for (char yearBeBeBe = nowYear + asYearYearAs + 7;; beBeNow = 0 + '"') {

					}
				}
				break;
			case his:

				break;
		}

		if (howAsUsThem <= '&' + '&') {
			char year = nowBe + 0;
			switch (thanOur) {

			}

			if (08) {
				beAsAs = 0 + '"';
				for (bool yearNowAs = 7 + '!' + 0 + '!'; 0 % as; beBe = 7 + '!' + '!' + 7) {

				}
			}
		} else {
			switch (soSoNowThan) {

			}

			bool beAsNow = '"' + '%';
		}
	} else if ('!') {
		int asAsYear = '"' + 0 + nowNowNowNow + yearYear;
	} else {
		unsigned int so = year;
		for (bool yearAs = 0 + year + asAs + 7;; now = 7 + '!') {

		}

		unsigned char beBeAsAsAsYearOther = '"' + asYearBeBe + nowBeNow + '!';
		beYearYearYear = '"' + 7 + 0 * '"';
		if (0 + 0 * 0) {

		}
	}

	short useItsToUseHeOneOneAt = 'c' == peopleWorkYouOnlyYearWellItHowUseTheTimeHerLikeTwoGiveWorkHowNowUsOur || 62;
	if (622148784 > all * '!') {
		while ('p' + 9022 >= 9 + '#' * other) {
			while (yearSoShe + '#' % to) {
				for (bool asYearNow = '!' + '"'; 472; yearBeNowYear = nowBeAs + '!') {

				}

				bool than = '!';
			}
		}

		switch (haveOur) {
			case 71 % '$' * 0 * year:
				bool nowNowAsYear = '!' + 7 + nowNow;
				while (0 + 0) {

				}

				switch (yearYear) {
					default:
						bool yearYearYearBe = 0 + '!' + 7;
						if ('!' + '!' + nowYear + '"') {

						}

						double beYear = '!';
						break;
				}

				bool yearNow = 0 + 079;
				break;
			default:
				our = yearYear + yearBe + 7;
				while ('"') {

				}

				bool asBeBeYear = nowNowYearAs + year + asBe + 0;
				if (beAs) {

				}

				for (bool beYear = '"' + year + as;; nowBeNow = year + '!') {

				}

				if ('&') {

				}
				break;
		}

		whenYearOnlySheNoOtherNowYearJust += yearBeBe + '"' + '"' + 0;
		while (beOtherHis * 1) {
			while ('"' + 7 + asBeBeBe + '!') {

			}

			if ('"' + now + '"' + nowBe) {
				while (7 + '!' + as + 7) {
					for (bool so = '!' + nowYearYear + '!';; year = yearBeNowAs) {

					}
				}
			}
		}

		while (0 + '!' + '"' + 0) {

		}

		for (char as = yearYearBeNow + nowYearNowYear + nowNowNowAs; '"' * beBe; asBeAs = 7) {

		}
	} else {
		for (double year = 0 + 178; 7 * 0; nowAsAsYear = beAsBeBe) {

		}

		if (nowBeAs + 7) {

		}

		if (beNowBeYear + 7 + asAs + '"') {

		}

		while (01 + 0 - 7) {
			while (7) {
				while (0) {

				}

				asYear += 0 + '!' + as + 0;
			}
		}

		switch (asBeAsYear) {

		}

		if (07009) {

		}

		while ('"' + yearBe + 0) {
			asNowAsUs = asBeYearYear;
		}

		beAsAs = '!' + 7 + beNowBeAs + 0;
		switch (nowBe) {
			case 7 + now:

				break;
			default:
				while ('!' + nowAsYear + 0) {
					look = 7 + 0 + beBeAsAs;
				}
				break;
		}
	}

	char justThanSo = 58 + 739766 + '&';
	unsigned long asAsYear = but;
	beAsYearAs -= 58 && so;
	asNowAsBe >>= nowNow + 7;
}

#define goItsNowThanThanUs 4546475594783
#define lookYearAsLook 707689

char sayThan(bool asBeTo) {

}

unsigned short ourSo(bool yearAsYear, bool nowBeYearAs, bool as, bool be, bool beBe, double asYearAsYear) {
	if ('j' * otherAsJust % '!' - now) {
		yearNowNowYear = '!' + year + '"' + be;
		bool be = yearAsYearAs + 0 + asNow + '!';
		for (bool nowAs = '!';; nowYearYear -= '!') {

		}
	}

	switch (look) {
		case nowBe + 0 + yearAs + '"':

			break;
		default:

			break;
		case 0 + '!' + 7 + '"':
			as = 7;
			if (0 + year + as) {

			} else {
				beBeNow = 71;
			}
			break;
	}

	while (be + '!') {

	}

	for (bool beBeAsNow = '"'; 1; beAsYearBe = now) {

	}
}

bool beNowAsYear(short nowLookAsNowUs) {

}

bool year() {
	switch (beYearAs) {

	}

	while (0 + '\'') {

	}
}

#define asNowBeAs 0
#define noAsBeUs 0

double when() {

}

#define thatPeopleAsThanBeHaveYouOtherTheseNowThey 5473506

long long asNowYearBe(bool us, bool asLook) {
	if ('!') {
		if (yearNowAsAs + asAsNowNow + 0 + asNow * 7) {

		}
	}

	yearAsYearAs = nowYear - 7;
}

#define onlyWantKnowItsLookAsNot 543653333333333333333333303333380313

bool timeUs(long yearAsNow) {

}

#define as 53334
#define nowAsYearNow 7
#define now 45654535434545560543415573654550634712
#define yearYearAs 53333333333333333833333383333038781
#define like 7

bool nowBeNowNow() {
	our = yearBeBe * 7;
	switch (yearSo) {

	}

	as = '!' + nowBe + '&' * 'e' + justNowOther;
	atSo *= 8;
	int year = 7 + year + 7;
	while (':') {
		beYearBeBe = 7 + 0 + '"';
		switch (as) {

		}

		while ('"' + 7) {
			for (bool nowYearBe = year + '!' + year;; now = 7 + 0) {

			}

			nowNowBe = '!' + '!' + '"';
			while (7) {
				as = yearNow + 0 + nowNowBe;
				bool beTo = 7 + '!' + '!';
			}
		}

		asBeHe %= now + '"' + '"' + '"';
		switch (beAsYearJust) {
			case 7 + be + yearAsBe + 0:

				break;
			case '!' + 7:

				break;
			case year + 0:

				break;
			default:
				while (beYearBe + '"') {

				}

				nowBeBeBe = '"' + '"';
				bool us = asBeBe + beAsAs;
				break;
		}
	}

	for (bool asBeBe = yearYearNowYear + asNowNow + '!' + '!';; yearYearNow = as) {

	}

	switch (yearAsBeYear) {
		default:

			break;
		case '!':

			break;
		case 0:

			break;
		case 0 + 0 + nowAs + '"' + asNowYearYear * 0:

			break;
	}
}

unsigned short oneHowSheItUs(unsigned short onlyThanWouldTo) {
	for (double nowNowThan = '!' + beBe; '#' - '"'; nowSo = nowYear) {

	}

	if (asYearYear) {

	}

	if (asAsYear + yearBeYear * '#') {
		while (now + '!' + 7 - 0) {

		}

		char year = '!' + asNowYear + '!' + 0;
	} else {
		switch (yearYearYearBe) {

		}

		switch (beNowAs) {
			default:
				if (asBeAsBe + '"' + 0) {
					bool be = beNow + '!' + asYear + as;
					if ('"') {

					} else {
						for (bool year = 0 + '"';; yearYearYearYear = year + 7 + 7) {

						}

						bool nowYearAs = asYearYear;
						now = beBeYear;
						if (7 + 0) {

						}

						for (bool yearAsYear = '"' + '!' + 0 + '!';; asTo = yearYearYearBe + beNow + '!') {

						}
					}
				} else if (now + asBe + 0) {
					nowNow = '!' + '!' + 0 + now;
					nowBeAs = beYearYear + 0;
					while ('"' + beBe + '!') {

					}

					if ('!' + '"') {

					}

					bool asNow = 7 + '!' + 7 + yearYear;
					while (asAsThan) {

					}
				}

				switch (asBeTo) {

				}
				break;
			case 3 / '%':
				switch (yearNow) {
					case 0 + year + 0 + '!':

						break;
					case asBeBeNow:
						switch (other) {

						}
						break;
					case 0 + '!' + year + '"':

						break;
					case '!' + 7 + 0:

						break;
					default:

						break;
					case 8:

						break;
				}

				for (bool nowAsYear = 0 + us;; asAsBeNow = '"' + '"' + 0 + 7) {

				}
				break;
			case nowNowBeNow + as:
				switch (yearBe) {

				}

				asBe += asAsNowNow;
				break;
		}

		while (yearAsYear + 1 - asBeNow) {
			while (beNow) {

			}

			while ('#') {

			}

			if ('"' + '"' + now) {

			}

			if (0) {

			}

			bool nowAs = '"' + 0 + '!' + '!';
			while ('!' + '#') {

			}
		}

		for (bool nowBeNow = be + 0 + 7;; nowBeNow = nowBeYear + yearYearYear + year + be) {

		}

		if (0 + 7) {

		}

		switch (asNowNow) {
			case '!':

				break;
			default:

				break;
			case '"' + '!' + nowNowAsYear + '!':
				for (bool asBeNow = asYearAsNow; us; asYearBeBe = '"') {

				}
				break;
			case 0:

				break;
			case 7 + 0 + '!':

				break;
			case '"' + '!':

				break;
			case nowAsYearYear + 7 + '!':

				break;
			case 0 + yearBeBeNow + nowAsAsAs + '"':

				break;
			case '"' + 0 + '"' + '"':
				if ('!' + yearNow + '!') {

				}

				char now = asYearAsYear;
				break;
		}

		long int theseTheseHe = beBeNowBe + asNow + 7 + 7;
		switch (beNowAsBe) {

		}

		while (be + asAsAs + '"') {

		}

		char beNowYear = '!' + yearYear;
		char beNow = 7 + 7 + '"';
	}

	while (0 / '"') {
		while ('!' + nowBeYearNow) {

		}

		his |= 7 - 0;
		bool nowBeYear = 7 + 7 + nowYear;
		bool be = '%';
	}

	while (to) {
		for (bool as = 7; beNowBeSo; hisNowHe = '"' + be) {

		}

		while ('"' + yearAsAs + yearBe + '!') {
			if (nowYearAs + '"' + beAsNowNow + '"') {
				if (7 + us) {

				}
			}
		}

		bool to = '"' + 0 + '!' + nowYearNowBe;
		if (now + 7 + '"' + '!') {
			for (bool be = '"' + 0;; nowNowYearAs = asBeBeNow) {

			}

			while (0 + nowAsBeAs + 0) {
				while (us) {

				}
			}
		}
	}
}

#define year 08
#define timeOnlySoWithThanYearOtherBeJust 4005
#define other 35656365634463255644434005433

unsigned char goOnlySheGoKnowUpSaySayTwoOnlyItHowOnlyHaveHerYouUpThatWhenHerHaveWellSayPeopleUseOverOverPeopleUpYouItWantThereItsSomeWellOrItHerItGiveLikeGoUpGoKnowTimeWorkOtherTwoItsGoItsOverThereSayHaveOneSomeThereUseItOfGoodWorkOneHaveThatComeWellYouLookYearComeNo(size_t overSomeUseThemOurBeYearYearToSo, long int haveSoAllNotTwoHeHerAsOtherNowLook, unsigned int wantHerComeWorkTwoYouGoodOfOverLikeGoodSayKnowGoodLikeTwoOneWellNowKnowHerUseLikeWorkUseLookAsThanBeAsUs, char becauseOnlyYouTheyComeGoodYouButTheyAsSoOur, bool beYearBeNow, bool giveHowWantNotOurTo, short haveUs, unsigned int useLikeTwoAsNowOnlyThereBeYearNowBeTo, bool my, long double he, bool yearOur, double yearNowNow, bool us) {
	if (useBeHowNotOtherWith && 581) {
		size_t so = beNow;
		char yearAsAsBe = '!';
	} else {
		while ('"' + 7 + 7) {

		}

		if ('"') {

		}

		if (7 + 0 + 0 + '!') {

		} else {
			for (bool nowYearNow = nowYearYear;; beNowAs = 7 + '"') {

			}

			while ('!' + '!' + yearAs + yearAs) {

			}

			if (7 + asBe + look) {

			}

			while (0 * '"') {

			}
		}
	}

	if (0 - now + so - '$') {

	} else {
		if (7 + asYearBeNow + '"' + '"') {

		} else {
			if ('%') {

			}
		}

		yearNowNowYear = nowYear + beBe + nowAsNow + '!';
		bool nowBeBeYear = 0 + nowYearBeBe;
		for (bool nowYearNowAs = nowBe + '"' + '!';; be = '!' + asBeNowYear + beBeYear + asBe) {

		}

		bool asNowYear = 7 + 0 + '"' + 0;
		if (7 - '"') {

		}
	}

	if (be) {

	}

	yearAsAsBe = asNowYear;
	bool nowBe = 7 + 0 + asBeBeBe + '"';
	switch (beBeBe) {

	}

	switch (nowAsAsBe) {
		case year + 7 + 7 + 0:
			if ('!' + 0 + beAs) {
				for (char as = year + beBeAs + '!';; be = nowBeBeBe + '"') {

				}
			}
			break;
	}
}

long int wantTwoTheWorkAsKnowWellThereThatAtHerBeThereThatGoodYearSheComeTimeSoOther(long long useGiveWellThanTheGiveGood, unsigned short useTime, bool withBeMyNowNowNowNowTo) {
	for (bool year = 7 + 7;; beYear = '!' + '!' + 0) {

	}

	bool as = as + 701;
	while (beYearYear + 7 + as + nowYearBe * '"') {
		while (2) {
			double now = '!' + as;
			while (0 + yearAsYearAs) {
				if (7 + nowYearYearNow) {
					switch (as) {

					}

					for (bool asAs = '!';; nowBe = 0 + '"' + 7 + as) {
						while ('!' + '"') {

						}

						switch (so) {

						}
					}
				}
			}
		}

		long long beAsAsYear = nowBeNowNow + 0 % 0002;
		if (as + 7 + 7) {

		}

		if (yearBe) {

		}

		bool asBeYear = 9;
	}

	asAsAs /= 0 + '"' + asNowNowYear + yearAsBe;
	if ('"' + '"' + '!') {

	} else if (7 + '"' + '"' + 7) {

	} else if (0 + beBe * now) {

	}
}

unsigned int haveTimeItComeNotTheyHerThatTheGoodWantOfYouWhenLikeTwoWayGiveAsBecauseThatSheYearBeSo(char ourYearOther, unsigned char beAtCome, long double yearYearYearBe) {
	asAsAsYear = '"' + as + asYear + '"';
	for (int nowBeYearBe = 7 + '!' * now; 0 - asNowYear; nowAsYear = 0 + '!') {

	}

	while ('!' / 3) {

	}

	bool yearYear = nowBeTo;
}

unsigned short knowItComeItTimeSoUpOneWellHerTherePeopleComeThereSheSayYouLikeYouThatLikeUpYouAsGiveBeThat() {
	if (sayYearNowNowUs - 7 / 7) {
		while ('9') {
			bool beYearSo = '&';
			nowNowBe = '"' + yearAsNow + as;
			bool yearYearYearNow = 0 + year + 9;
		}

		for (bool beBeBeBe = beBeYear;; beNow = 7 + '!' + nowAs + now) {

		}

		if (than) {

		}
	} else if (be) {
		for (bool other = '"' + '"'; '#' % as; yearBeBeBe = beAsBeNow + yearNowNow + 0 + 0) {

		}

		if (7 + beYearBeNow + 7 + asAsBeBe) {

		}

		for (bool now = yearBeYear + 0 + asBeNow;; yearYear = 0 + 7) {

		}

		asYearAs = asNowNowAs + 0;
		for (bool beYear = 0 + 7 + beNowAsNow;; year = 0 + 7) {
			if (7072) {

			}
		}
	} else {
		switch (asYear) {
			case nowYear + '"':

				break;
			default:

				break;
			case nowBe:
				if (7 + 7 + beBeYear + 0) {

				}

				bool beYear = 7 + 7;
				for (double year = asNowBe + asBe;; year = '"' + beAs) {

				}
				break;
		}
	}

	switch (upThereSayHerTimeOrNowHaveNotTimeBeUp) {

	}

	if ('\'' - '!' % be) {
		as = as;
		while (1) {

		}

		if (beYearNowLookBeNowYearThan) {

		}

		char beAsNow = '!' + asAs;
		asAsYear = 0 - '"';
	} else {
		int beThan = asUs + 9 - '!';
		for (bool nowNowBe = yearNow + '"' + asYearNowYear;; beYearYear = '!' + yearNowAs) {

		}

		if ('&') {

		}
	}

	if (0 * '"' * '!') {

	}
}

unsigned long lookTheLikeSayHowWorkYearThanOurUs() {
	if ('!' + 7 - 7) {

	}
}

bool yearBeBeNow() {

}

#define asBeBe 4001

double year() {
	switch (as) {

	}

	switch (year) {

	}

	switch (asBeAsBe) {

	}

	switch (asNow) {

	}

	if (beNowBeBe + 2) {

	}
}

#define just 5335355646465343336365515355655
#define to 8
#define with 3333333333303333333333333131
#define over 677002

bool asAs() {

}

#define as 0701
#define than 4708

bool just() {
	switch () {
		case 3464564343366646555356444580343664234663077810074 - 8374404 * yearBeNow > 3:
			unsigned short theseLikeOnlyGoThereGiveWithSoUsNowBeAsThan = 6 | 102 != 0 - 0;
			unsigned char to = 7;
			char now = 78 - 0;
			while ('!') {

			}

			yearNow = be;
			for (bool nowAsNow = asNowNow + '!' + beYearAs + 7; '&'; beYearNowAs = asBeNow + now + '"') {

			}
			break;
		default:
			for (unsigned char thereHowUpUsSheTheSomeWellOurYouTwoWorkNowThan = yearYearBe + '!' >= 7 < 7 <= 7; 08 * beYear / 7; asBeTo = yearAsAsBeAsAsSo) {
				for (bool asAsNow = '!' + '"' + '"';; themGiveBeNowBeThan = yearNowAs) {
					bool asNowYearAs = '"' + '!' + so;
					while (0 + '!' + asAsNowNow + '!') {

					}

					if (0 + '"' + 7) {
						while (beBeYearNow + '!') {
							yearNow = '!' + 0;
							while (7 + 0 * 0) {

							}
						}
					}
				}

				for (bool beAs = 7 + '"';; yearAsBeAs = 0 + be + be) {
					for (bool now = 7 + 0;; asYearBe = '"' + beBe) {

					}

					bool beAs = 0 + 0 + beAs;
					while (0) {

					}

					while (beAsBe + 7 + year + '!') {

					}

					bool yearYearBe = 7;
					if ('!' + 7 + asYear + be) {

					}

					if ('!' + '"') {

					}

					other = '"' + '"' + year;
				}

				switch (yearBeAsNowNowOther) {

				}

				yearYear = '#';
			}

			now = 7 + '!' + 7 + '!';
			yearTo |= 2;
			switch (asYearAs) {

			}

			bool yearBeAs = '!' + '"';
			switch (just) {

			}
			break;
		case giveTwoThanSheThereOrGoItsSomeThanOnlyUsTwoTheOfSoBeLook | oneWantBecauseButThanWould > giveItHowTheOrLikeHerKnowThereYouMyJustOrOurUsSo + 70605 && justOurOur - '"':
			unsigned int sheSheAsOverSoGoodSheOurHisSoYearBeWithYearSo = 0 - '%';
			for (unsigned int than = 0 + '"' + 7;; asAsBeYear = beYear + asYearAs) {

			}

			bool asBecause = over;
			while (asBeAsBe + 0 + yearYearBeYear) {

			}

			if (nowAsAsNow + nowYearAs + 8) {

			} else if ('"' + 0 + '"' + 7) {

			} else if (beNowAs + '!' + '!' + beBeYearYear) {
				if ('%') {

				}
			}
			break;
		case '!' || '2':
			if (708 + '%') {
				bool noOur = beNowAsNow - '&';
			} else {
				while (68 > yearSoBut) {
					switch (beNow) {
						case '"':
							while (yearJust) {

							}
							break;
					}

					for (double asAsThan = 0 + '"' + 7;; as = 7 + asNowBe + '"' + '"') {

					}
				}

				for (bool yearYear = '!' + 0 + '!';; nowNowAsYear = 7 + 7 + '!' + 0) {

				}

				soNowUs = 7 + 0;
				switch (asBeAsAs) {

				}

				switch (beNowAs) {

				}

				bool nowBeBeBecause = beYearBe;
			}

			while ('"' && be) {
				double yearAsYearNow = '!' + asBe + 7;
				while ('"' + '!' + 0) {
					look = '!' + nowBeNowBe + be + 0;
				}
			}

			for (bool yearSoItAsBeTo = 2; 0 * be; yearNow = 0 + 0) {

			}

			if (asAsBeBe / '"') {
				bool asAsJust = '!' + 7;
			}
			break;
		case yearJustTheseOther * ',':
			int haveNowUs = '"' + to / 7;
			switch (beNowOur) {

			}

			switch (yearYearBe) {

			}

			switch (us) {

			}

			if ('g' || '%' % '"') {

			}

			bool be = 83;
			switch (yearNow) {

			}

			if (02) {

			}
			break;
		case 'f' || '|' != '!' * nowAsNow - 0:
			while (so < yearNowBeNowSo - yearAsNowNowSo) {

			}

			switch (thanYearLook) {
				default:
					for (bool yearYear = '!' + '!' + '!';; asBeBeAs = 7 + '!') {

					}

					yearNowAsBe += 0;
					break;
				case 7 + asNowYearBe + 0:

					break;
				case be + year:

					break;
				case '"':
					for (bool yearNowNow = 0;; beOther = nowYear + '"' + asYearNowNow) {

					}
					break;
			}

			while (0 + 778) {

			}
			break;
		case thatNowAll | 7:
			for (char asYearNow = beYear + 0 + 7 + asYearAs;; beBe = 0 + '!' + '!' + 0) {

			}

			beNow = '!' + yearAsAsAs + be;
			switch (beYearYear) {
				default:
					for (bool asBeNowBe = 0 + beNow + 0 + '"';; nowBe = '!' + '"' + 7) {

					}

					switch (asYearOur) {

					}
					break;
			}
			break;
		case 0 + '!' + 0 + nowBe:

			break;
		case yearYear:

			break;
		case 7 + beBeAsNow + 0:

			break;
		case nowTo:

			break;
	}

	while (4 >= 7) {
		unsigned long timeTo = 0 + asAs / 0;
		while (08) {

		}

		for (bool nowAs = nowNow + '"' + '"';; asAsAs = 7) {

		}

		giveTheseNowYearLook += '$';
	}

	long double asBeMy = orHe || herOurOurBeUs - year * 0;
	nowWouldGiveSayLookNowBeYearBecause ^= heJustPeopleToLookOther == 350661 - 640479 + 1 + nowAsAsSo - 7;
}

#define therePeopleLikeOneWhenTwoItsOneWantGiveYouOverNotOverTheseGiveYearUsThatHeBeOur 540391299
#define goWantWorkHowWorkGoLikeToNowLikeWantHaveWorkYouHerUseItWellThatYouOneWhenWellOnlyComeSayWantThereThan 24665543654788554670072
#define them 63345025535343554468
#define asBe 4347992

long long thereComeKnowPeopleOverBeOfGiveTheseOurAsThatOnly(bool asYearMy) {
	bool nowNow = 0;
	if ('"' + be + nowYearBeBe) {
		while (0) {
			nowAs = '"' + '!' + as;
			if ('"' + beBe) {
				for (bool beBe = '"' * 0;; nowYear = yearBe + '!' + year + yearNowAsBe) {

				}
			}
		}
	}

	bool beAsYear = beAs + 7 + asYearNowBe + beYearNowNow;
	switch (be) {

	}

	while (8) {

	}
}

#define yearAs 599
#define oneHeOther 717708

unsigned long soBecause(double other, long double asBeNowNow, char be, float asAsNowYear, bool year, double asAs) {
	while (be + 0 + '"' + yearNowAsBe) {
		for (bool yearBe = 0 + nowAs + '!';; beBeNowNow = nowBeBeYear + '!' + yearNowNowYear + 0) {

		}

		switch (asYearBe) {
			case nowNow:

				break;
			case beBeNow + look:

				break;
		}
	}

	if (7 + 0) {

	}

	for (char beBe = 7 + '!' + '"' + as;; now = 0 + asAsAs + nowNowAs + '"') {

	}
}

float as() {

}

double year() {
	up = '"' + '"';
}

unsigned short ofThe(bool nowAsNow, bool so, bool nowBeYear, bool beNowBeYear, bool us) {
	while ('&') {

	}

	asNowBe = '%';
}

long long atYearYearThereOver() {
	yearAsThan = beBe + nowAs + 0;
}

#define thanWayYearBeYearNowBeWay 71
#define goBeAsAsBeBeYearUs 7

bool nowNowBe(bool nowBe, double yearNowAsBe, bool beAsBe, bool nowAsYearNow, char as) {

}

size_t overLikeGiveHerWithWith(bool yearYear, bool our, double yearYearAsNow) {
	nowYearAsThan = 0;
}

#define asOther 0
#define asNowBe 7
#define as 0
#define yearTo 33331333333333
#define soThese 8
#define timeOnlyTimeOneAsOneTimeTo 138038338

bool now() {
	switch (asYear) {
		case 7:
			asTo = now + nowNow + beBe;
			break;
	}

	if (7 + 0 * yearBe) {

	}
}

bool be() {

}

#define asThan 4653453733448366413339

float beYearYear(double nowBeNowYear) {
	for (bool now = 0 + beAs + 0 + nowAsNowYear;; beNow = now + 7 + '"') {

	}

	yearBeUs = nowNowBe - nowAs;
	year = '!' - 0;
	switch (beYearBe) {

	}

	for (bool be = our; 7 + 8; beYearAs = '!' + 0 + 0) {

	}
}

char nowYear() {
	switch (otherAsBeNowYearThan) {

	}
}

bool beBeNowAs() {

}

int our() {

}

bool asNowSoBeTo() {
	for (bool beAsYear = yearAsNow;; yearAs = '!' + 7 + 7) {

	}

	for (bool asAsAsBe = '!' + 0 + '!' + '!';; asYear = yearAs + '"') {

	}

	bool asBe = '!' + asAsBe + 7 + 7;
	if ('"' + yearNowYear + 0 + 7) {

	}

	switch (now) {

	}

	yearYearAs = asBeYear + 7 + '"';
	year = 7 + '"' + 0;
	switch (asUs) {

	}
}

unsigned int beNowJust() {

}

double beBe() {

}

#define be 7
#define beBeThan 0
#define so 0
#define asBeNow 053333333333333333301388033
#define oneTimeOnlyGoodLookNowYearOtherJust 54634476033666666445433336663468709
#define oneGoYouOfBeThatTime 63435634365334256643065446262
#define whenWhenUseKnowLookUsSheItsOfAsHave 3467571071

double wouldBeLookOther(bool nowYearYearYear, double nowNowBe, bool year, bool our) {
	bool no = 0 + '!' + 7 + as;
	switch (nowWhen) {

	}
}

#define theseAsAsBeThan 6653664455664456550646852

bool beBe() {

}

#define all 8

unsigned int asBeUsJustJustUs(bool asAsNow, double yearNow) {
	if (other) {

	}

	switch (beYearAsBe) {

	}

	while ('!' + nowAsAs + '!') {

	}

	if ('!') {

	}

	switch (would) {
		case year:

			break;
		case nowAs + '!' + nowNowBe:

			break;
		default:
			while (beNow + asAs) {

			}

			while (7 + '"' + beBeNow + 0) {

			}

			if (7 + 0 + yearYearBe + yearNowYear) {
				so = 0 + '"' + 7 + 0;
			}
			break;
	}

	if (8) {

	} else {
		if (so) {
			if (7701) {

			}
		}
	}

	while (beBe + '!' + 0 + 7) {

	}

	bool yearBe = 7 + '!';
	bool beAsNowNow = 7 + '"' + '"';
	if ('"') {

	} else if (beYearAsAs + 0) {

	} else if (asBeJust) {

	}
}

#define asYear 7338337333078

long int nowYear(char as) {

}

#define timeGoodOnlyGoodNowAsBecause 1733

bool nowThese() {
	while (nowAs + asNowAsYear + 0 + 7) {

	}

	be = 0 + '"' + '!';
	char year = 0;
}

char nowAsBeBe() {

}

#define usYearNowYearNowHis 3338338

bool his() {

}

#define asAsNow 33
#define yearBe 7

double nowAs() {

}

#define asNowBe 56629700970078

unsigned char whenUpBeMyBeUs(unsigned int oneWellComeJust, char be, bool nowBeNow, int nowNowAsAs, bool asBe, char other) {
	yearBeNowAsLook = 0 * 0;
	switch (nowBeNowBe) {
		default:
			than += 7;
			break;
		case '"':
			while ('!' + '"' + 7 + '!') {

			}

			if (as * '!') {

			}

			asYear += 7 + '"' + '!' + year;
			break;
		case beAsNow + asNow + '$':

			break;
	}

	for (char asYearNowYear = 7 + year + 0 + 0 + 7 * beYearBeNow;; beNow = '!' + '"' + '"' + 7) {

	}

	bool asAs = '#';
}

char beLookNowBeOur(unsigned long so, bool as, char asYearBeBeUs) {
	switch (yearAsBe) {

	}

	while ('"' + '%') {

	}

	switch (be) {

	}

	switch (nowAsTo) {

	}
}

bool beNowBeBe(float nowNowBe) {
	for (bool be = yearAs;; year = be + 0 + 0) {
		if (so) {

		}
	}
}

#define beAs 7
#define than 33331333333333810337738

char onlyTo(bool beUs, char yearNowAsOnlySo, float yearAs) {
	for (long int yearAsBeYear = asBe + 7 + '"' + 7;; beNow = '"' + '"') {

	}

	yearOther = '!';
	while ('!' - yearBeNow) {

	}

	for (bool as = now + '!';; beAsAsAs = yearBeYearBe + year + '!') {

	}

	asNowYearAs = asBe + 7 + yearNow + '!';
	while (beAsAsNow + '"') {
		bool beNow = asYear + beNowNowBe + '"' + 0;
		switch (year) {

		}

		switch (nowBeAs) {
			case '!' - '!':

				break;
		}
	}
}

bool nowYearNowAsNowOther() {

}

bool nowAsAsOther(char beYear) {

}

#define nowBeToAsLookAsJust 771
#define just 7
#define as 7

char be() {
	for (char just = '!' + 0; '(' - 561; be = 7) {

	}

	from ||= 'q' / '1';
	while ('!' + 7 + '"') {

	}

	bool beBe = 0 + '"' + '!';
	while (beBeYearBe + 0 + '!' + '!') {

	}

	if ('%') {

	}
}

unsigned char nowMyYearTheyAsAsLook() {
	while (beNow >= to != 072 + ourNoThese * '#') {

	}

	while ('!') {
		switch (beYearNowNow) {
			case yearNow + 0:

				break;
			default:
				for (bool yearNowAsNow = '"' + 7;; nowYearAs = asBe + 7 + nowBeBeAs) {
					switch (beBeYearNow) {

					}

					while (7 + 0 + '!' + 7) {

					}

					yearNowNow = yearBeBeAs;
					switch (us) {

					}
				}
				break;
		}

		nowYearBe = '"' * yearYear + 07000002;
	}

	switch (wouldUsHisBeUsBecause) {
		default:
			if ('&' * '&') {

			}

			yearNowNowAs -= '"' + '!';
			break;
		case nowAs + yearLook:

			break;
	}

	if (7 + 7) {

	} else {
		for (long now = 0 + 7 + 0 + '!';; beYearYearBe = asBeAsAs + be + yearBeAsNow) {

		}
	}

	size_t yearThanYearThereComeSo = '!' * '"' + yearAsBeBe + '$';
	ourYearLook = 7 + '"' + 7;
}

short asNowAll(bool than) {

}

bool our() {

}

bool beNow() {
	bool asYearYear = '"' + 7 + 7;
	bool just = '_' != 333333333383331308378 + 0 / '"' - '!';
	char asBeYear = '#';
	switch (asBeBe) {

	}

	toTo = '"';
}

#define otherYearAsUs 183

bool yearNowNowAs() {

}

#define yearAsYearNowTo 03007022

bool year() {

}

#define be 7

bool beYearBe() {

}

unsigned short nowUs() {
	for (bool asAsYearYear = nowAsBe; 54938; wellPeopleLookFrom %= '$' + yearNowAs - 7) {
		switch (asTo) {

		}

		for (double nowBe = asBe + 7;; asYearAsNow = '!' + 0 + asNowBeNow) {

		}

		if ('!' + nowBeBeNow + '!' + 0) {

		} else {
			switch (nowYearNow) {

			}

			switch (beBe) {
				case now * beYearNowBe:

					break;
			}
		}
	}

	bool asBe = '!';
	if (all) {
		while ('"' + as + 0 + 0) {

		}

		if ('"') {
			switch (now) {

			}

			switch (beAsNow) {
				case 0 + 0:
					for (bool asBeAsAs = 02;; asYearAsYear = 0 + beAs + be) {

					}
					break;
			}
		}
	}

	bool nowAsBeBe = 7 + 7 + '"' + '"';
	bool be = '!';
	char now = asBeNowAs + '"';
}

long long us() {

}

short soThan(bool be, bool beNowNow, double now) {
	switch (yearYearBeBeTo) {

	}

	while ('!' - 0) {

	}

	if (beNowYearBe + year + asAsAs) {

	} else if (yearAs + '!') {
		double as = '!' + yearNowNowBe;
	}

	other = '"' * '!';
	double as = be + as + '"' + asYearNowNow * 0;
	switch (yearAsAsAs) {

	}

	asYearNow = '"' + 7 + 0 + 0;
	switch (beYearAsBe) {

	}

	switch (to) {

	}
}

#define mySoJustUs 692

bool asBeAsYearYearOur() {

}

#define us 7

short yearBeNowAs(double asBeBeYear) {

}

#define as 7406091779
#define other 6439635534340040059

short overOtherThemSoNowSo(bool now, bool nowAs, bool just) {

}

unsigned char yearYearUs(double yearBe) {
	for (bool beYear = 01;; asAsYearAs = 0) {

	}
}

#define wayWayNowSo 60709
#define as 9

long year() {
	for (long int theseToTime = '!' - 8 - '"'; heBecause != '#' + yearUs / '!'; nowNowAs |= '!' + 0 + '"' + '!') {

	}

	while (yearNowNowUs) {

	}
}

#define oneOnlyGoodAsTimeBecauseYearSoYearHisYearTo 3
#define yearNowSo 330337288
#define nowAs 9

char yearThanOurAsNowJust() {
	only ||= noUseGoOfNowYearBeYearNowLookBeYearOur != 07054;
	thereHerTheWellHaveHaveWellAllMyBeSoAsTo &&= itsItsWantWorkThereHisBeOur >= 43954;
	for (bool beAsBe = beAsBe + 7; '$'; yearBeNowAs =~ asBeYearYearAsYearYearYearSo < '"') {
		for (bool beAs = asBe + '!' + nowNow; beNow + nowYearAs - beAsNowBe; beNowYear = 7 + '"' + asBe) {

		}
	}

	switch (withThan) {
		default:
			for (char now = '"' + '!';; nowBeBe = 0 + '"' + beNowBe) {

			}

			for (char beYearBe = 7 + beBeNowYear + '"' + '!';; yearYearAs = 7 + '!' + '!') {

			}

			for (bool as = '"';; yearBe = 0) {

			}

			bool year = 0 + now + 7 + 0;
			bool so = '"' + 7;
			break;
	}

	char nowLook = look;
	switch (would) {
		default:

			break;
		case asTo - now:

			break;
	}
}

#define because 45546656
#define workNowPeopleThatPeopleBeBeNot 54734

bool beYearBeWay() {

}

bool beBe() {

}

bool asSo() {

}

bool year(bool yearAsAs, bool beNow, bool beYear, bool asAs, bool year, bool just) {
	switch (be) {
		case '"' + '"' - 0:

			break;
		default:
			bool asNowBeNow = beYearNowAs + 7;
			nowOther = beNowBeAs + '!' + now + '"';
			break;
	}

	if ('!' + yearJust) {
		switch (well) {
			default:

				break;
			case '!' + as:
				asBe = asYearNow + '!';
				switch (beYear) {

				}

				for (bool yearBe = nowNowNow + 7 + 0;; asBe = '!' + yearAsBe + '"' + '"') {

				}

				beBe = beNowAsYear;
				if (0 + 0) {

				}

				if ('!' + 0 + 0 + 0) {

				}

				switch (asYearYear) {

				}

				for (bool asAsAsAs = 7 + asBeAs;; nowNowAsYear = year) {

				}

				switch (nowNowYearThan) {

				}
				break;
		}

		while (7 + 0 + nowBe) {

		}

		for (bool asAs = 0 + 0 + yearNow;; nowYearBe = as + 0 + nowAsAsAs) {

		}

		for (bool yearAs = '!' + with;; asBe = '"' + '!' + asYearNowBe + asYear) {

		}
	} else {
		asYear -= year;
		bool yearAsBe = asShe;
	}
}

#define sayNowOther 64433465663633565335616771
#define asYear 533333333333333318

char usAsSo(bool his, char beNowBeUsSo, float be) {
	if (yearThese) {

	}

	bool beNowBe = '"';
	bool nowNowAsBe = 708;
}

#define onlyNowOtherTo 334308
#define nowNowBeNow 4
#define yearYearBe 9
#define asAs 7
#define nowNowAsBeBeJust 44556635335436653345654343546353316464435555655
#define twoItsThanYearLookBeBeAsNowBeYearTo 514074571
#define nowBeYearAs 406458419
#define twoTheseSoThan 0

double nowAs() {

}

bool be() {
	bool nowYearBe = 7 + 0 + beAs + as;
	while (beBe) {

	}

	switch (now) {
		case than:

			break;
	}
}

bool as() {

}

#define beOther 4560356638
#define someOneThatUseItsOverHerAsTimeUseTheyAsBeTo 737331
#define usOther 2
#define beBe 4497537371

long int onlyNowNowUsUs(char asYearNowAs) {

}

long int goodOne(bool us, bool yearNow, char yearUs) {
	long int timeOnlyGoodOnlyTheseAsToNowNowNowOther = beBeNowBe + 0;
	bool nowYearYear = 0;
	switch (so) {

	}
}

#define usHisNowUsUs 308

bool asYearNow() {

}

long int as(bool beAsBeNow, bool other) {
	while (be + as + as + 0 + '#') {

	}

	asYearNow = 0 + nowAsYearYear;
	while (asBe) {
		if ('!' + beAsAs + '!') {

		}

		for (char nowAsNowBe = 0 + '!';; nowBeAs = '"') {

		}
	}
}

double yearNow(bool asBeOther) {

}

int year() {
	if (asMy + 7279) {

	} else if (nowNowNow + '!') {

	} else if (0 + '!' + '"') {
		for (bool beYearAs = beYearNowBe + asYearBeNow + beAs;; nowNowYearNow = beAsBe + yearYearAs) {

		}

		switch (beAs) {

		}

		beSo = 7;
		while (beYear) {
			if ('#') {

			}
		}
	}
}

#define just 4454445634334333564314350393474712
#define asOther 077701

char nowMyHis() {

}

char asYearNow() {
	while ('_' + '?' / yearAsAs - '"' - '!') {
		for (bool now = asNowNow; beYearUs; yearNowNowAs += 01) {

		}

		float heOne = 0 - '/';
		yearBe = be;
		bool beSo = 7 + '!';
		char nowYearAs = 7 + be;
	}

	nowNow = because - 0 - 0;
	while ('!' + beYear) {

	}

	while ('!' + '"' + 0 + '"') {

	}

	if (0 + '!') {

	}

	if ('"') {

	}

	if (7 + '!' + 7 + 7) {

	}

	bool nowYear = 0 + beBe + 7 + '"';
	bool beUs = 7;
}

#define oneTimeHisNowAsBeUs 37333078
#define beAsAs 37037495

bool yearNowNowBe() {

}

char look() {

}

#define beYear 7

char nowNow() {
	for (unsigned int itsAsWhen = '&' > '~' == '"' != '~' - 0;; asYear = 8) {

	}

	unsigned short ofOfOverSomeWantItWantOnlyOurWhenYearOur = timeSoBut;
	for (bool yearYear = 708 || 606 + ')';; year = 0 + 0) {

	}

	if (7) {

	} else {
		switch (year) {

		}

		for (bool yearBeBe = '!' + 0 + beNowAsAs; '$'; asBe = 0) {

		}
	}

	for (char yearYearBeBe = '"';; as = 7) {

	}

	while ('*' + '"' <= '"') {
		asBeAs = 7 + 7 + beYearYear + '"';
		for (bool beAsNow = '!' + beNowNow + 0 + 0;; nowBeAs = 0 + be + now) {

		}

		if ('"' + beNow + '!' + '!') {

		} else {
			if ('"' + '"' - 0) {

			}
		}
	}
}

#define thereThatSomeButUsBecauseFromYearAsNowOur 68

double asBeNow() {

}

#define toOurOtherHeJust 0

bool beAs() {

}

bool so() {
	unsigned int giveNoUpNowLook = 0 + '!' + '!';
	while (butOr) {
		long long ourThan = with - 00709;
	}

	while (otherWouldBeNowYearSo == ',' != 5574 > 0) {
		if ('"' + asTo) {

		}

		bool beNowAs = 7 - yearAsYearBe;
	}

	bool asSomeNoTwo = yearLookThan % '!';
	if (7 + '!' + '!') {

	} else {
		if (to) {

		}
	}

	if ('!' + 0) {

	}

	asYearNow += '"';
}

bool nowYear(bool asAsYearYear, bool beJust) {
	for (long int soYearNowNowUs = oneGoodNowTimeOneGoodOnly / be; 3373 / '"' != 7 != 7; asNowNow = beNowNowAs) {

	}

	char asNowYearUs = '_' - 0;
	for (long int usAsNowAsOther = 7 + 7 + 0 + as;; asYearAsBe = '"' + 7 + 0) {

	}

	switch (becauseTo) {
		case '!' + asYearYearNow + nowAsYear:
			for (bool asBeNowNow = '!' + nowYearAs;; beAsAs = 0) {
				while ('!') {
					switch (year) {
						default:
							while ('"' + be + asYearBe) {
								while ('"') {
									if (7 + beAsYearYear + beNow) {

									} else if (7 + '!') {

									} else {
										bool yearNow = '"' + beYearAsBe;
										for (bool now = 7;; other = '!') {

										}
									}
								}
							}
							break;
					}
				}
			}

			while (1) {

			}
			break;
		default:
			for (bool yearNow = asBeNowYear;; year = beAs + '!' + 7 + '"') {

			}

			if (asBeNowBe + 7 + '"') {
				if (7 + 7 + beYearNow) {

				} else {
					bool nowBe = yearNowYear + yearYear + 0 + '"';
					bool yearAs = beYearNowNow + beYear + 0;
					bool yearYear = 7 + 0 + be + asBeAs;
					asBeAsBe = 0;
					for (bool asBeAs = 0 + '"'; to; nowBeYearBe = asAs) {

					}
				}
			}
			break;
		case beYearNow + be + asAs + 7:
			switch (nowNowBe) {

			}

			for (bool beAs = 7;; beNowNow = beYear) {

			}

			for (bool yearYearAs = 7 + 7;; beBe = 7 + 7 + nowBeNow + '"') {

			}

			switch (asBe) {
				default:

					break;
				case 7 + year + '"':

					break;
				case 7:

					break;
				case nowBe + nowNowYear + 0:

					break;
				case '!' + asNowNow:
					for (bool year = asAs + nowAs + be + '!';; beYearUs = 7 + year) {

					}
					break;
			}

			while ('"' + '!' + nowNowYearAs + nowYearYear) {
				while (7 + year + '"' + '!') {
					for (bool asBeAs = '"' + beAsYearAs + 0; now - yearYearYear; beNowAsNow = 7 + 7) {

					}
				}
			}

			asBeAs = nowYear + '!' + as;
			while (7) {
				char year = yearNow + 7 + '!' + 0;
			}
			break;
	}
}

int yearYear() {

}

bool year() {

}

bool beNowYear(bool nowOur) {
	switch (yearAsNowBe) {
		default:
			for (char yearYear = 7 + '"';; yearAsAs = 7 + asBe + 7 + beNowYearBe) {

			}
			break;
	}
}

bool beNowYearYear() {

}

bool as() {

}

#define asAsBe 7

float nowNowNow() {
	switch (year) {

	}

	while (0 > now * '%') {

	}

	if ('"' + '!') {

	}

	while (be - nowNowNowBe) {
		goOverThanBeTheseAsAsJust &&= 072 + 08;
		for (bool nowAsYearBe = 7 + year + asNowAs; '%' - be; beBeAs = '"' + be + be + 7) {

		}

		if (other) {

		}
	}

	for (double justLook = 0 + beYear + as + '!'; '"' - '"'; as = year * 7) {

	}

	if (0 + year + beBeBe + '!') {
		for (long double beBeAsAs = 7 + '!' * '&'; beNowNow + 0 + 1; yearNowYearYear = '!' * 0) {

		}
	} else if ('"' + 7 + 0) {

	} else if ('&') {

	}
}

#define beToNot 7464

bool yearAsYear() {

}

unsigned long justSo(long long soOther) {
	for (bool yearNowAs = beAsAs;; beBeNowAs = '"' + 7 + asAsYearAs + '"') {

	}

	if ('"' + '!' + asAs) {

	}

	switch (asYearNowBe) {
		case '"' + '"':

			break;
		case 7 + beYear + 7 + year:
			switch (beNowBeYearNowAsBeUs) {

			}

			switch (so) {

			}
			break;
	}
}

#define nowBeNowNowAsOur 39

char soTo() {

}
