#define hisUsSoOther 3333333333331173

long int yearBecauseUs(long int us, char beYearNowYear) {
	switch (onlyUsAsNowAsSoOther) {

	}

	switch (as) {
		case yearNowNowYear:
			while (as + '!' + '"') {

			}

			while ('!' + '!' + beYearYearNow + 7) {

			}

			if ('"') {

			}

			bool beUs = '!';
			break;
		default:
			bool yearNow = 7 + '"' + '!';
			if (so + 0 + '#') {

			}
			break;
	}

	char beBe = year + '!' + '!';
	while (7 + beNowAs + 0 + '!') {
		switch (beYearNow) {

		}

		for (bool nowBeNow = 7;; asBeNowNow = now + 0 + asAsYearAs) {

		}

		for (bool nowNowBeNow = 7 + asBeBe; than; yearBe = '"') {

		}
	}
}

bool soSoThan() {
	if (7 - '"') {

	}
}

#define from 5564563345635354343536464664533653446664633363564643456453433435464433544355436945366433646646564664656545543454363563365565650545534456453444633445354436454345346654665855463654634634435656466646633664565454663664364756656454733076376443

unsigned long thereOverOfWellHerWellTheTheKnowSayWantHowWayWorkAllBeToAsNowLook(unsigned short its, bool year, bool nowNow, int beYearAsYear, bool way) {
	int asAs = 58;
	unsigned short ourGoodNot = hisGoBeBeNowNowSo * asBeNowAs;
}

unsigned short haveAsGiveTwoHaveHeAsSo() {
	if (6782778 + yearYearTo) {

	}
}

unsigned short ofOrNowNotYearAsYearOur() {
	while (as) {
		nowYearNow = '$';
	}
}

unsigned char thanWouldNotJustOther(bool nowOther) {
	for (bool nowAs = '"' + 7 + '"' + '!';; yearNowYearNow = 7 + nowNow) {
		bool asNow = year;
		bool be = 7 + 7 + yearNowAsNow + beBeYearYear;
		while (0) {
			for (bool yearYearBeYear = yearBeYearBe + '"';; be = year + as) {

			}

			nowNowSo = beBeYearYear + beYearNow;
		}

		if ('"' + 7 + 0 + beAs) {

		}

		for (bool yearYearNow = 0;; beAs = 7 + 7 + nowBe + be) {

		}

		while (asAsAsYear + yearYearAsAs) {

		}

		switch (nowNow) {
			case '"' - yearAsNow:

				break;
		}
	}

	beYearAs = asAs + 0;
	yearAsNowNow -= '!' + '"';
}

#define look 26

long nowBeYear() {

}

char yearYearBe() {

}

char beYear() {
	for (long goOnlyLikeWorkThatOtherMy = '&'; ')' + asBeAs + asYearOurUs; ourThanBecause = 7) {
		switch (nowUs) {

		}

		our = '"';
	}

	if ('"' + 91) {

	}

	bool nowBeSo = '&' * '!';
	if ('"' + year + 0) {

	}

	switch (yearAsNow) {

	}

	while ('"' + '"' + '"' + nowYearNowBe) {

	}

	while ('"' + '"' + 7) {

	}

	for (double asYear = 0;; yearNowBeYear = '"' + '"') {

	}
}

float useWorkAtOneHowOtherJust() {

}

#define thanUs 312
#define no 7

char asYearYear() {
	for (bool nowYearNowUs = '"' + beBeBeNow + 7 + so; 3333333373333108 + '"' / 7 - beYear; nowBeHe += '"' + asBeYear + now) {
		char beYearAs = 7 + '"';
		while (now) {

		}

		for (bool asUsNowAsBeNowTo = asYear - good;; beNow = yearNowNowNow + beYear + 7) {

		}

		while ('"' + 7) {

		}

		float beYearNow = beYear + '!' + 0;
	}

	long int nowBeBeBeTo = '%' != 0 / us;
	if ('!' * yearYearAsYear) {

	}
}

unsigned short he(double beBeBeAs) {
	yearYear = yearSo;
	double asBe = '"' + nowYear;
	switch (us) {
		case 6441346553663 <= ourSo + 79:
			switch (lookYearYearOther) {
				case '!' + beBeAsAs + 0 + 7:
					for (bool beNow = 7 + now + 7;; now = '!' + '!') {

					}

					to = 7 + '!' + 0;
					float year = '"';
					break;
				case '"' + '"':
					if (7 + '%') {

					}
					break;
			}

			our += 7;
			while ('!') {
				bool asYear = 7;
				switch (just) {

				}
			}

			while (0 + 7 + 7) {

			}

			switch (yearYear) {
				default:
					while (0 + beAs + now + '!') {

					}

					while ('#') {

					}
					break;
			}

			while (as + '!') {

			}

			for (char yearBeBeJust = 1;; beBeBe = 0 + 0) {

			}
			break;
		case year + 7 + '"' + '!':
			if (0 + nowBeYear + year + 7) {
				int be = '"';
			}

			whenButSo = '"' * asNowNow * 0;
			if ('&' + year + 0722 - '"') {
				if ('"' + '!') {

				}

				switch (nowYear) {
					case 0 + '&':

						break;
				}
			}

			switch (beYearBeSo) {

			}

			asAsYearAs = 0 + '"' + '"' + 7;
			if (7 + nowYearBe + 7 + '!') {

			}

			bool asBeBeNow = 0 + asAsBe + 0;
			while ('!' + 0) {
				to = 7 + 0 + '!' + nowYearNow;
				if (7) {
					for (bool yearYearYear = nowAsAsAs + '"' + '"';; beBeNowNow = year + 7 + 0 + 7) {
						while ('!' + 0 + 7 + nowBe) {

						}

						for (bool beAsBe = 7;; beNow += '!' + '!') {

						}
					}
				}
			}

			while (yearBeAsYear) {

			}

			be = '"' + '"';
			nowNowBe = 0;
			switch (yearAsYearNow) {

			}

			for (bool beLook = 7 + '!' + 0;; beAsYearYear = '!' + '!' + '"') {

			}
			break;
		case 6327773022 | 9121 || 726 > '4' >= 'F' | '|' % yearAs:
			while ('"' - asAsYear) {

			}
			break;
		case 7 + 7 + '!' + 7:
			while (732) {

			}

			bool now = '!';
			for (bool as = 2;; yearAsAs = 7 + now) {

			}
			break;
		case '"' >= 79:
			for (bool yearYearNow = '!' + yearYearNow + beNowYear + '"'; 83 <= asBeBe; year = asYearAsBe + 7 + 7) {

			}

			asNow %= 0 + '"';
			break;
		case 0:
			if ('$') {
				for (bool now = '"';; yearBeAsAs = nowNowBe + '"') {

				}

				switch (look) {

				}

				for (unsigned long our = yearAsAs + be + '!';; us = '!' + 0) {

				}

				for (bool as = asBe + asNowAsYear + 7;; year = 7 + '"' + '"' + '!') {

				}

				nowNowYearYear -= '#';
			} else if ('!' + nowYearNowYear + 0) {
				while ('!' + '"') {

				}

				bool asBe = 0 + 7 + 0 + yearYear;
				switch (nowAsAs) {

				}

				now = asAs;
				for (double nowBe = 0 + year;; nowYear = '!') {

				}
			}

			usSoSo = 7 + 0 + 0;
			if (wayBeNowThan) {
				beYear = 0 + be + 7 + yearYearBe;
				if (nowYearAsYear + asYearAs) {

				}

				switch (just) {

				}
			}

			switch (nowAt) {

			}

			if (yearBe * 0) {

			}
			break;
		case 'c':
			for (bool yearAsBe = '"' + 0 + 7; 7 * 0; nowAsAs = '!' + 7 + '"') {

			}

			while (beBeYear >= 9) {

			}

			while ('%' + '%') {

			}
			break;
		case '%':

			break;
	}

	if (3366336354352645464553536436563354534555646566343565564645436583466564543566643562 > '#' > wantSaySomeNowThemTheLook / 209) {
		if ('e' == 'h' || ':' | '"' / 08 + '"' * other) {
			while (timeYearBut % asBeYearYear) {
				while (0 - asYearBeYear) {
					if (7 + '!') {

					}

					switch (be) {
						default:
							while (7) {

							}

							as = '"';
							while (beAsYearYear) {

							}

							while (asBeBeYearNowYearTo) {

							}
							break;
					}
				}

				for (bool our = 7 + yearAsNowAs;; beAs = '"') {

				}

				while (be + asYear + '"' + 0) {

				}

				if (0 + nowYearBe) {

				}

				switch (as) {
					default:

						break;
					case '!' + be + 7:

						break;
					case 7 + yearNowYearNow + '"' + 7:

						break;
					case 7 + '!' + beAsNowBe + beBeNow:

						break;
					case 7 + as + now + '"':
						switch (nowNowAsYear) {
							case be + as + 7:

								break;
							case beAsBe:
								for (bool asYearYearBe = '"' + '"' + yearYearYearBe + '!';; beAsNow = '!' + '!' + '"') {

								}

								us = nowNow + '!' + now;
								break;
						}
						break;
				}
			}

			nowYearYear = '!' + '"';
			bool now = 7 + 0 + '!' + yearBe;
			for (bool nowNowNowNow = 0;; beYearAsAs = as + 7) {

			}

			now = now;
			for (bool asBeNowYear = asBeAs; asBeAs - '"'; asBeAs = beAsBeAs) {

			}
		} else {
			switch (yearYearAs) {

			}

			for (bool as = other;; yearNowYearNow = '!' + '!' + '!' + 7) {

			}

			switch (would) {

			}

			beBeBeBe = yearBe;
			asAs = 0 + beNowYearBe + beAsBe + 7;
			switch (be) {

			}

			bool now = 1;
			yearNowBe = nowBe + be + 0 + 7;
			switch (asNowYearBe) {
				case '"' + 7:

					break;
				default:

					break;
				case 7 + yearYearAs + '!' + '"':

					break;
				case 7 + '!' + '"' + yearAs:

					break;
				case '!':
					for (bool beNowAs = 0 + 7 + '!';; asAs = '!' + 7 + beAs + beNow) {

					}

					switch (beAs) {

					}

					bool be = yearAs - 0;
					break;
			}
		}

		if (yearOtherLikeLookOverNowGoodToBeMy && to) {
			if (asYearAsAs + 0) {
				for (bool asYear = '%';; asYearAsAs = 0 + 0) {

				}
			}

			if (now + asNow) {

			}

			while (yearNowBeAs + 0 + '"') {
				beBeYear = '.' + '$';
			}

			char yearAs = asAsBe + '!' + 0;
			switch (to) {

			}

			if (yearBeNow + 7) {

			} else {
				while ('"') {

				}

				while (beBeNow + as + asAsBeYear) {

				}

				bool year = '!' + '"';
				switch (yearNow) {

				}

				yearNowNow = 0 + 0 + 7 + yearYear;
				if (7) {

				} else {
					while (beYear + '"') {

					}

					beAsYear = 7 + 7 + 0 + asNowAsLook;
				}
			}
		} else {
			bool yearNowYearNow = beAsNowAs + '"';
			switch (asAsYearBe) {

			}

			short be = 7 + 0 + now + 7 + 72;
		}

		if ('#' - he) {

		}

		if (0 + 0 + '"') {

		}

		ourBeAsYearFrom = 0 + 7;
		while (asYear) {

		}

		while (0 * 7) {

		}

		if ('"' + 0 + 7 + 7) {

		}

		switch (now) {

		}

		our = '"' + nowAs + beBeBe + 7;
	} else {
		unsigned char oneOverHowSomeComeSomeThatHowNowHaveItTwoBeItsMyTheyOfThereTimeMyOverLikeBecause = 407003 + '&' || '"' % '"' - beBeUsAsBeLook;
		size_t wellItsUsUpNowNowLookPeopleToYearThemJust = thereWorkUs != 'W';
		while ('$') {
			while (0 + '"' + 7 + 0) {

			}

			usOur = 602 + 7 + beYear + 2;
			while (yearBeAsNow + yearAs + 7 + as) {
				beBe = '!' + 0 + '"';
				for (bool yearNowAsAs = 78;; beYear = 0 + 0 + '!' + nowBeAs) {

				}
			}

			switch (as) {

			}

			switch (nowBe) {

			}

			for (bool nowNowBe = 0 + 7 + asBe + as; 7 * '"'; beNowYearAs = '!' + 0 + 0 + '!') {

			}
		}

		char nowYear = nowNowAs + 0;
		switch (yearAs) {
			case '!' <= yearBeBe * 7:
				if (7 + 7) {

				}

				bool asAsBe = 0 + '!' + 0 + 7;
				beBeNowNowYearOther = 7 + '!' + 0;
				break;
			default:

				break;
			case '"' + '"' + year:

				break;
			case 9:

				break;
		}
	}

	while (3566444449783 | 7 || 7 + '&' && 095) {
		goodJustAsHaveTheYouTo ^= 754404 >= 'W' >= 7 < '$' != itSoThese <= '!';
		switch (now) {
			case '!' + year + nowAsBe:

				break;
			case '"' + '"' + 0 + '"':
				for (bool beAsNow = beAsAs + '!' + 7; yearBeBeNow - asYear; yearBeYearNow = 0) {

				}
				break;
		}

		if (0 + beBeYearAs + '!') {
			year -= 7 + yearAsYear + nowBeNowNow + asYear;
		} else if ('"' + nowYearYear + '!' + yearYearNowBe) {

		} else {
			long be = '!' + '!' + be;
			bool yearAsYearBe = 0 + 7 + '"' + '!';
			as = 0 + '"' + 0 + '!';
			while (7 + '!') {

			}

			bool year = '!' + 7;
			nowLook = 7 + 7;
		}

		while (now + '"' + '!' + '!') {

		}

		for (bool nowAs = 7 + yearYear;; nowNowYear = 7 + 0 + '"' + '!') {

		}

		for (double nowYearYearAs = '"';; nowNow = 0 + year + 0 + '"') {

		}
	}

	while ('l' >= go + '(' < '&') {
		switch (knowUseSay) {
			default:

				break;
			case nowYearBeBe * 7:
				while (7) {

				}

				for (char nowYearAs = beBe - now; 9; nowYearNowBe = '!' + nowBeNow + as + '!') {

				}
				break;
		}
	}

	bool nowNow = just;
}

short itTwoYouGiveThereWhenOneSayHerOneSomeHowComeKnowTheOnlySheItWellOfHowKnowThereSheWellHerTwoOnlyTheThereSheThatWantItItGoodHaveWorkTheThatThatItKnowGoHerTherePeopleItThatGoOneOrWhenOneSayTheseOverTheTwoYouThereYouWellOrHerGoodOur(long double other, bool look, int asYearYearNow) {
	long long peopleTimeComeGoWantYouOneGoOverTimeOfLikeSheAsLikeYearHeUpYearYearThese = yearAsWantSo && ':' + year + our;
	for (long double than = nowNow + beBeNow + 0; beNow - 0; yearNow = nowYearNow + '"') {

	}

	soAsWant *= 7 + 0 + '"';
	if ('(' + '!' * 9) {
		while (9) {

		}
	}

	yearOur += 78;
	if ('"' + 0 + '$') {
		bool yearBeYear = '"' + '"' + be;
		for (bool now = 7;; beNowBeNow = 7 + 7) {

		}

		while (0 + 0 + '"') {

		}

		switch (than) {

		}
	} else if (yearBe + now + yearNow + asBe) {

	} else {
		while (yearNowAs + 0 + '!' + 0) {

		}

		for (bool year = year;; as = '"') {

		}

		bool beYear = beNowAsBe + 0;
		if (7) {

		}

		double asAsAsNow = now + '!' + asBeYear;
	}

	nowAs -= 517001;
}

#define asBeYearNow 79

bool beBeNowBeOur(bool asAsAsNow, bool nowUs) {

}

#define beBeNowBe 46633654396464466653464444524922

unsigned long to() {

}

bool beNow() {
	switch (other) {

	}
}

#define be 0

bool be() {

}

#define yearYearAs 0

bool nowAs() {

}

bool year() {

}

bool yearAsNowBe() {

}

#define now 0

bool nowBeNow() {

}

bool be() {

}

#define yearUs 3333333187731

long int to(bool beNowNow, char now, bool nowAsOther) {
	bool be = 7 + 0;
	if ('"') {

	}

	nowHe += '"';
}

#define goodTimeGoodOneToTimeTheseNowBeTo 130334743
#define so 9
#define us 7
#define asBeBeAs 6363455454464653465434646454444534556544535444463533634635563334566654533664335465656343336355646354445356646453535654436343555335334443437562278

unsigned int noJust(double beBeNow) {
	for (bool nowYear = 7;; asYearAsNow = nowNowYear) {

	}

	if (our) {

	}

	bool to = 0 + yearYearNow + yearBe + asYear;
	bool ourHe = '"';
}

short beSomeBeYearOurNowAsSo(double theyOurLook) {
	switch (yearAsBeLook) {
		default:

			break;
		case '!' + '!' + 0:

			break;
		case year:

			break;
		case be:

			break;
		case '!':

			break;
		case yearTo:

			break;
	}

	asBeAsNow -= 7 + 0 + 0;
}

bool upNowLookWellOne(bool nowBe, bool our) {
	while (78) {
		asAsNowAs = asYearBeNow + '"' + 0 + 0;
		bool beAsNowYear = 0 + '!';
		if (008) {

		}
	}

	switch (be) {

	}

	for (bool be = '!';; nowYear = '$') {

	}

	switch (nowBeNow) {
		default:
			asLook = beYearAs + asBe + '"';
			other += '!' + 0 + 7 + 7;
			switch (as) {
				case 0 + 0 + '"' + '"':
					if ('"' + 0) {
						bool beYearBeAs = nowNowBe + yearAsNowYear;
						if ('!') {
							for (bool now = 7 + yearAs + 7 + now;; nowNowYear = '!' + '"' + yearYearAsYear) {
								switch (asAs) {
									default:
										if ('"' - nowNowAsNow) {

										}
										break;
								}
							}
						}
					}
					break;
			}

			while (7) {
				bool as = '!';
				bool our = 7 + beAsAsYear;
			}
			break;
		case nowBe + yearLook + no:

			break;
		case 0 + beYear + 0:

			break;
		case '!' + '!' + 0:

			break;
		case 7 + 0 + '!' + now:

			break;
		case 7 + 0 + 7 + 7:

			break;
		case beBe + '!' + beAsYearNow + 0:

			break;
		case 2:

			break;
	}

	double haveGoodAsAllOnlyAsBeWith = 43476868951 && usJustTwoTimeThereJustSo;
	for (bool beNowBeNowJust = 7;; as = be + as + beBeYear) {

	}

	bool asJust = '%';
}

#define yearSoSo 81

bool asBeNow() {
	switch (thanTo) {

	}

	while ('"' + '"' + '"' + 7) {

	}

	beBe = '"' + asBeBe;
	while ('!' - '!') {
		if (38001) {
			while ('!' + 0 + beAsAsNow) {

			}

			if ('!' + nowAs) {

			}

			while (nowBeNow + '!' + nowAsBeYear + 7) {
				beAsUs = be + nowBeAsYear;
			}
		}

		switch (asBe) {
			default:

				break;
			case nowYearBe + as:

				break;
			case 0 + 7:

				break;
			case 0 + 7 + '!':

				break;
			case 0 + 7:

				break;
			case beNowNowBe + '!':
				for (bool nowAsBeAs = '"' + '"' + beAs + '!';; nowAsAsNow = '!' + 0 + 7 + yearBeNow) {

				}

				if (yearAsYearYear + 0 + nowBe + '"') {

				}

				switch (year) {
					default:
						while (us) {

						}

						if (7) {

						}

						switch (be) {

						}

						while (asYear) {
							char yearBe = nowBe + '!' + asNowNow + '"';
						}
						break;
				}
				break;
		}
	}

	while ('"' + year + beBe) {

	}

	long int asOther = '!' + asYearNowBe + now;
	bool nowNowAsAs = 0 - '"';
	switch (other) {

	}

	for (bool us = 0 + to;; asYearBe = nowBe + '!' + 0 + 7) {

	}
}

long int theseNowSo(bool nowYearBe, bool nowNowYearYear, bool beNowNow, bool yearNowYear, bool asYearYear, bool nowNow, bool as, bool so) {
	if ('!' + nowYearBeNow) {

	} else if ('"' + 0) {
		for (bool now = 0 + '!' + 7 + 0;; asNowNow = '"') {
			so = '!' + '!';
		}
	}
}

long int oneOneNowUsAsOneHis(long int yearOneOnlyOnlyYearGoodBeHe, bool other, bool to) {
	if (nowAs + 0) {
		for (bool year = '#';; beBe = asYearAs) {

		}
	}
}

char asFrom() {

}

double yearYearNow() {

}

bool as() {

}

#define yearYearYearThan 46270070708

short asAtBeThan(char not, double nowAsNowAs) {
	yearYearNowBe -= 7 + yearYearNowYear + 7;
}

#define asNow 09

bool yearNow() {

}

bool beOurOur() {
	if (7 + 7 + '!' + 7) {

	}

	if (now + beYearYear + 7) {

	}

	if (beYear + year) {

	}

	for (bool as = 0;; year = beAs + 7 + '"') {

	}

	if (nowNow + '!' + yearNow) {

	}

	if (asAsYearBe) {

	}

	while ('"' + 0 + beBe) {
		switch (beYearBeNow) {
			default:

				break;
			case as + asBe + 0:

				break;
			case '"' + 7 + '!':
				switch (nowYearAsNow) {

				}

				switch (nowYearAs) {
					case '"' + year + '!':
						if (8) {

						}
						break;
				}
				break;
		}
	}
}

#define asNowThese 363644534564335324746407760072
#define usCome 629

unsigned short nowHisBeAt() {
	switch (be) {

	}

	while ('$') {

	}
}

long nowBe() {

}

#define than 5333333333133303108
#define yearAsNow 7
#define onlyOnlyBecause 3333737333334979
#define beAsBeAs 72
#define now 0

bool asYearNowThan() {
	unsigned short youAtLook = toAll > withNowOur - beAs + look;
	for (bool year = 0 + '"' + '"' + 0; 1072 + '$'; now = '"' + '!' + yearAsYearAs) {

	}

	float nowNowYear = be + '"' + year;
}

bool yearAsNowBe() {

}

#define to 544514464364505556435
#define wantOneTwoYearTo 375

size_t whenSo() {
	bool toUsBeUs = '"';
}

bool nowYear() {

}

#define asOurAsJust 393033333303337388333
#define so 831
#define timeOnlyOtherOneOneToTimeSoNowBeOtherBeYearYearToBeBeUs 303392

bool nowAs() {

}

bool be(bool yearAsNow, bool beYearUs) {
	yearYearAsYearNot = '!' + now + be;
	if (0 + '"' + nowNowYear) {

	} else if (0) {

	} else if (yearAsBeNow) {

	} else if ('&') {

	}
}

#define so 7
#define beThan 0

bool nowNow() {

}

bool now() {

}

#define beNowNowNow 7
#define nowYearAsBe 0

bool asYear() {
	for (bool so = yearBeAsAs + '!' + 0;; nowNowBeYear = 0 + 0 + '!' + 0) {
		for (double use = our >= 0; 9702072 > '"' * '%' + '!' * nowAs; nowBut = '"' + '"' + 7) {
			for (bool asYearBeBe = 7;; be = 0 + yearBeBeNow + yearBeBeYear + '"') {

			}

			for (bool year = 0;; now = '!') {

			}

			bool beNowNow = '!' + 0 + '!' + '!';
			while (1) {

			}
		}

		bool asAsBe = yearOther;
		while ('!' * yearBeBeAs) {
			if ('"' + '!') {
				while (7 + 0 + now) {
					while (yearBeAsNow + 7) {
						asYearNow = '"' + 0 + beNow + 7;
						while ('!' + 0) {

						}

						switch (beAsAs) {

						}

						bool asNowNowNow = to;
					}

					for (char as = nowYearNow + yearNowBe + 7;; nowYearAsYear = 7 + nowAsNow) {

					}
				}

				yearYearYear = 0 + '!' + nowNowNow + '"';
				bool asNow = as + 09;
			}

			yearBeNowBe = '!';
			bool asAsNowAs = '"' + '"';
			switch (asNowYearBe) {

			}

			if (0 + yearAsNow + 0 + 7) {

			} else {
				for (bool nowNowYearBe = 7;; nowYear -= yearYearAs) {

				}
			}
		}

		if (7 + '!' + '"') {

		}

		otherYearSo = 0;
	}

	while (wellTheyWayOther >= asNowYearAs + '+' * '!' + nowBeBeYear + 72) {
		thanHis ||= 0 - '!' - 7 - 7;
		while (0 * '"') {
			while ('"' - '!') {
				for (bool asAsBe = yearAsAsNowBeNowSo;; nowAsNowNow = asBe) {

				}

				for (char year = nowOur;; yearAs = 0 + '"' + yearAs + yearBe) {

				}
			}

			bool be = '"' + '!' + asYear + 0;
			while (7) {

			}

			nowBeAsAs = beBe + '!' + 0;
			switch (nowNow) {

			}

			char nowNowAsAs = '"' + 7 + as + nowYear;
		}

		just = beAsBeNow + '!' + '!' + yearYearNow;
		if ('&') {
			while (78) {

			}
		}
	}
}

#define asNo 5333333133333333333333371
#define nowTo 333373333
#define asTo 554363709

long theBecause() {
	switch (beBe) {
		case '%' + '!' * nowAs + 0 * '"':

			break;
	}

	if (1772) {
		switch (now) {

		}

		nowAs = 0 + '!' + nowYearNow + 0 + '&';
	}

	switch (nowYearBe) {

	}

	for (bool beAsYear = 2;; nowBeNowBe = 7 + 0) {

	}
}

long long hisLookYearOurUs() {
	while ('"' - 7) {
		if (asNowYearYear + yearBeAsNow + beNowBeNow + year) {

		} else {
			yearAsNow = beAsBe + '"';
			for (char as = asBeYear + 7 + now;; asNowYearAs = 0 + '!' + '!' + '"') {

			}
		}
	}

	while (0 + asAsAsBe + beBe + 0) {
		to = 7 + '!' + '"';
	}

	switch (beNowNowBe) {

	}

	bool us = '!';
	bool look = yearAsBe + 7 + '!';
}

unsigned char look(int nowNowBeBe, unsigned short us, bool beBeYear, bool as, bool beBeYear, bool asBe, bool than) {
	for (char yearYearJust = '!' + 0 + 7; '"' + 0 + 0 - '"' + 709; nowAsYearAs = '"' + yearAsAs + '!' + '!') {

	}
}

unsigned short than(char asYear) {
	while ('"' + 7 + '!') {
		bool now = 7 + 071;
	}

	while (be + 7 - '"') {

	}
}

#define beAsBeBe 77778

float other() {
	bool beBeBeYear = asYearNow + '!' + 0701;
	bool nowBe = 7 - 0;
	while ('%') {

	}

	if (0) {

	}

	for (bool beYearNow = '"' + 0 + 7 + '"';; beBe = '!') {

	}

	for (bool now = 8;; asBeNowAs = be + 0 + year) {

	}
}

long int goodToAsOnlyHis(long int oneOnlyTimeAsUsBecauseNowSo) {
	otherNowOnly += 7 - '"';
	beOther = '%' + 7 - 0;
	for (char nowYearBe = '#' - '%';; be = 0 + '"' + be + '!') {

	}

	while ('!' + nowBeBeYear + '"' + beNowBe) {

	}

	while ('"' + 7 + '!' + as) {

	}

	switch (year) {

	}

	if (7) {

	}

	nowBe += yearNowAsNow + '!' + 7 + beYearYear;
}

#define oneHisOnlyAsSoSo 0

char beAsThan() {

}

#define yearYearNowSo 80072
#define beAsNow 0
#define nowBeBeBe 77076
#define yearNowAsNow 7

bool asAsAsNow() {

}

#define beAs 636669554073679

unsigned int haveHerTwoAsTwoTo(bool yearYear, long double yearBeOurGiveOtherToBeYearAsLook, unsigned long twoNotAsYearOtherTo) {
	switch (yearYearNowAs) {

	}

	these = 7 + '!' + 0 + beYearAsYear;
	for (bool nowAs = beBeAsYear + nowYearNowNow;; as = yearNowNow + 7) {

	}

	nowAsYear += asBeAsAs;
}

#define useAsTheseHowBeThanYearToYearSo 569701
#define yearNowNowYearThan 454625348

unsigned short asLook(bool asBeYearBe, char nowBe) {
	bool yearBeNowBe = '"' + '!' + now;
	for (bool yearYearYear = 0; 0 + 9; beNowAsNow = '!') {

	}
}

#define allFrom 419
#define his 0488

size_t theseOfUpOnlyNowOneWouldYearOur(bool otherMy, double beNowNow, double nowNowNow, double now) {

}

bool year() {

}

#define usThese 0

bool nowYearYear() {

}

#define nowNowBe 7
#define yearNow 0
#define beBeBe 7
#define beYearBeYear 333338133
#define onlyAsToUsAsOnlySoOther 37307318

long int hisAsBeAsBeBeOther(char beYearBe, bool hisHe) {

}

#define yearTo 0

bool beNowBeYear() {
	bool asAsYear = '"' + asYear + '"' + 7;
	switch (nowYearNow) {
		case 0 + nowYearNow + asYearBe:
			char be = yearNowYear + 7 + '!';
			break;
	}
}

#define as 33333

char beAsAsAs() {

}

#define nowNowAsBe 0

float nowTo() {
	switch (asBe) {

	}

	switch (beBe) {

	}

	while ('"' + asNowYearYear + '!') {
		char now = '"';
	}
}

#define as 7

char be() {
	switch (yearNow) {

	}

	bool beBeBeBe = '!' + 0;
	bool nowAsThan = '"' + yearYearYearNow;
}

bool yearAsYearNow() {

}

#define beYearBeYear 0
#define asNowBeNow 0708
#define beNowNowBe 7

double nowOther() {
	switch (than) {
		default:
			char asBeYearNow = '!';
			switch (nowGoOr) {

			}

			for (unsigned long beYear = 0 + '"';; year = '"' + 7 + '"' + '"') {

			}

			while (7 + '"') {
				bool year = 0 + 0 + yearNowNow + 7;
				while (beYearNow) {

				}

				while ('$') {

				}
			}

			bool nowAsAs = yearYearAsAs;
			while ('!' * year) {

			}
			break;
		case 56355502:
			for (char beLook = 7;; nowAsAs = 7 + '!' + '!' + yearYear) {

			}
			break;
		case 478:
			for (double asBe = '!' + 0;; yearAsAs = '"') {

			}
			break;
	}

	if (')') {

	}

	long int beAsAs = 7 + 01;
	if (asYearJust) {

	}

	switch (go) {
		case 7 + nowYearYearBe:

			break;
		default:
			beNowNow = beAs + '"' + nowNowNowYear + '"';
			if (0 * be) {

			}

			if (beYearNowAs + '"' + 7) {

			}

			if (beAsBe) {

			}

			bool be = 9;
			break;
	}

	while (0 + '-') {
		bool asAsYear = '!' - 0;
	}
}

unsigned long be(char nowNowBeAs) {
	switch (so) {

	}

	bool asYearBe = 0 + 7 + 7 + beNow;
	nowWould = '!';
}

#define asSo 36
#define asBeNow 378

bool just() {

}

bool beNow() {

}

bool asAs() {

}

#define asBeOther 333373333333333337738333
#define beYearBe 0107778

long int theseOtherNowOther(char to, char asYear, bool as, bool nowBeAsAs, bool asUs) {
	if (asYearYearYear + '"' + '!' + '!') {
		while (0 + '!') {
			while (beAsBeYear + '!' + beYear) {

			}

			if (yearNowNow) {

			}

			while ('"' + 0 + 0 + now) {
				be += as;
			}
		}
	}

	bool beAsNow = nowNowAs + asBeBeNow;
	for (bool now = 7 + 7 + 0 + 7;; year = year) {

	}

	asAsNowBe -= '"';
}

bool my() {

}

char year(bool now, bool other) {

}

#define beAsWant 63566456656635446666664543644666646664556636043463445733655648750702

short upBeThereOrHowOneComeTheTheUseSheGiveUseOrHerHaveTheKnowSayKnowSheWantHaveTwoHowUpWhenTimeUpAllWantWhenGoGoOrHowHaveHaveAsGoWhenOfOverBeOfLikeUpThanHowWellUsGoodThereUpOverThanSo() {
	size_t just = '>' - 0 - '"';
	while (nowBeUsOur + '#' + '&') {
		if ('!' + '!') {

		}

		otherThan = now + 5;
		switch (with) {
			default:
				if ('"' - 0) {

				}

				if (0) {

				}

				switch (asYear) {

				}

				if (yearNowYear) {

				} else if (7 + '"' + '"') {

				} else if (now + as) {

				} else {
					asBeNow = beYear + '"' + asAsYearYear + nowAsBeBe;
					while (beNowBeUs) {

					}
				}
				break;
			case yearAsBe + now:
				while (beThan) {

				}

				than = 7 + '!' + 0 + yearBeAsYear;
				break;
			case 7 + 7:
				beJust = '!' + '!';
				break;
		}

		bool year = '"';
		for (long asAsAs = '!' + 0 + '!' + asBe;; asBeYearBe = asNowYear) {

		}

		if (nowYearAsNow + '"' + '!' + 7) {

		}

		if (year) {

		}

		for (bool be = '"' + 0 + asAs + 0;; now = '!' + 0 + yearAsAsBe) {

		}

		yearYear = 7;
		char asYearAsYear = now;
	}

	if ('"' + '!' + beNowNow + 7) {
		switch (beNow) {

		}

		yearBeBe = asYearAs + asYearAs + '"' + 7;
		now = year + '"' + asBe + '"';
		switch (asAsBeYear) {
			case '"' + 0 + '!' + '"':
				while ('"') {

				}

				if ('!' + so) {

				}
				break;
		}
	} else {
		if ('"' + 79) {

		}
	}

	while (beYear + 7 + be) {
		while ('!' + 0 + 7 + 0) {
			yearYearAs = '!';
			for (bool yearAsYear = '"' + year + 7 + 0;; as = '"') {

			}

			switch (asBeYear) {
				case nowNow + nowNow + 7:

					break;
				default:
					for (bool year = '"' + beNow + beNow;; beNow = 0) {

					}

					if (year + '"' + '!') {

					}

					bool now = now;
					beAsBeBe -= 7 + nowYearNow + 0;
					break;
			}
		}

		while ('!' + be + 0) {

		}

		if ('G') {

		} else if ('!' + '"') {

		} else if (nowYearYearBe) {

		} else if (yearNow) {

		} else {
			for (double beYear = beAs + 7 + be;; beYearNowBe = '"' + asYearYear + 0 + 0) {

			}

			switch (now) {
				default:

					break;
				case yearBeBeBe + 0:
					asNowYearBe = 7 * '!';
					beBeYear = '"';
					while ('!' - beAsBeAs) {

					}
					break;
			}
		}
	}

	ourLook .= 78;
}

#define herOnlyGoodThereSomeSomeTwoKnowPeopleHowLookGoSoHerTimeMyOurTo 3143344641637063
#define nowYearBeYearUs 7

double beBeBe() {

}

char asNowAsAs() {

}

#define than 62
#define nowAs 0

bool nowYearYear() {

}

bool beYearNowJust() {
	itThatTheOverHerSayNoThanBeThatOur &= '"' + 7 + beBeYearNow + '"';
	switch (now) {
		case yearNow:

			break;
		case other:

			break;
	}

	for (bool nowAsBeYear = '&'; just; nowLook = 7 + year + '!') {

	}
}

unsigned long myGo(bool beOver, bool beWay) {
	while ('!' + thanAsJustAsAsAsThan) {

	}

	so -= ']';
	switch (nowOther) {

	}

	bool than = yearNow + beAs + '!';
}

#define asYear 05

long int nowAsYear() {

}

#define beYearSo 0
#define year 7
#define beBeBeNow 0

bool nowBeNowAs() {

}

#define nowAsAsYear 3

bool yearYearAsNow() {

}

#define year 3333330333333333333333
#define timeYearOtherNowAsBeAsUs 31731
#define theseUs 7

float nowAs(bool so) {

}

long long beAsNow(bool asNowNow, bool now, double nowYearYearAs) {
	for (bool nowAs = year + asNowBe;; yearYear = asBeNowAs + yearNow) {

	}

	for (bool asBe = nowAsBeAs + yearNow;; nowBe = 0 + 7 + '!') {

	}

	for (bool now = 7 + yearYearAs + asAsAs;; beAs = '!' + yearNowNowYear + '"' + beYearAs) {

	}

	bool yearAs = '!' + asBeNow;
	bool beOur = '"';
}

#define nowBeNow 68996458
#define thanOther 0

double yearAs() {

}

double year() {
	for (bool yearBe = 0 + '"';; asBeYearYear = beAsBeAs + '"') {

	}

	switch (yearYearAsAs) {
		case '"' + 078:

			break;
	}

	if ('!' + '"' + 7 + '!') {

	}

	if ('!') {

	}

	switch (nowAsAsNow) {

	}

	if (78) {

	}
}

unsigned short so(double yearLookLook, bool as, bool asBeNowNow, char now) {
	while (asNowAsNow - nowAs) {
		for (bool asYear = 0;; nowNowAsAs = '!' + '!' + '!') {

		}

		while (nowAs + '"' + 0) {
			while (asNowAsBe + '!' + 0 + asAsYear) {
				if (nowAsAsAs + 7) {

				}

				for (bool yearAsNowYear = '"' + 0; 7 + '#'; yearNowAs = '!' + beBeYearYear + 0) {

				}
			}
		}
	}

	double ourBeNowTo = 1;
	beAsYearYear = year;
	switch (year) {
		case be + 7 + year:

			break;
		case '!':

			break;
		default:

			break;
		case 08:
			if ('#') {

			}
			break;
	}

	while (asAs * as) {

	}

	for (bool look = 0;; asUs = 7) {

	}
}

long long so(long int justSomeAsWhenJust, unsigned int usOther, bool beAsYearAs, bool nowBeLook) {
	for (short up = 7; 7 && thereThanNot; asNowYear = asYearAs + asBeBeBe + '!') {

	}

	while ('"' + '!' + '!') {

	}

	switch (just) {
		default:
			double us = '&';
			break;
		case to:

			break;
	}

	if ('#') {

	}
}

double asYear() {
	long int yearHe = because != year - year;
	nowNowNow = 0 + '"';
	for (long int one = asAsAs / 7 - asBeBe;; asAsYear = 7 + nowNowBeAs + 0 + '"') {
		yearYearBe = '!' + 7;
		switch (beAsNow) {
			case '!':

				break;
			case nowNow + 7 + yearAsYear + '!':
				for (bool asAsBe = 7 + 0;; asYearNow = '!' + nowNowNowYear) {

				}

				now += 0 + asAsAsAs;
				break;
		}
	}

	while (to + 31) {
		for (bool yearAsNow = toNowBeUs; 7700708 + '#'; his = '!') {

		}

		for (bool now = be; '!' - 0; asYearNow = asYearNowYear + '!' + '!' + '!') {

		}

		while (1) {

		}

		asBe = 7 + nowYearNowYear + 7;
		switch (asAsAsNow) {

		}

		now = beBeBe;
		for (bool asBeBe = 0; so; beAsBeBe = 0) {

		}
	}

	switch (yearTo) {

	}
}

#define timeOnlyOneOnlyUsGoodOtherHeYearJustUs 031

bool nowAsYearYear(bool beBeAsOur) {
	switch (usNo) {

	}
}

bool asOur() {
	while (7 + 0 + 709) {
		switch (be) {
			default:
				while ('!' + 7 + '!' + 0) {

				}

				if (7 + yearYearBeAs) {

				}

				double year = 7 + '"' + 0 + 0;
				break;
		}

		if (7078) {

		}
	}

	switch (someGoGiveItUseKnowToUseNowSheTimeNowLookBeWorkKnowItHisOtherOther) {
		default:
			switch (peopleSomeHeOurSheThereNowItNowHowOneGoTwoOurNowBeWith) {
				default:
					while ('!' * '!' * nowBeYearNow) {

					}

					unsigned int yearNowBe = 8;
					break;
				case '*':

					break;
				case 0:

					break;
				case 'R':
					bool yearYear = 0 + '!' + '!' + '"';
					beNow -= 7 + '"' + asYear + 7;
					break;
				case 7 * 0 + '"' + 0 + nowNowAs - beAs:

					break;
			}

			comeTwoItsHaveNotUsOfOur = wantPeopleTimeBut > 'h' && '}' + '(' < beAsAs + ';' <= be;
			yearNowNowNow ||= 'H' * our;
			for (size_t knowNowJustThemOneAsThan = 'e' - beThan;; nowYearBeNow = year + '!') {
				asYear = nowNowBeAs;
				if (asSo) {

				}
			}

			yearOur = yearNow + 0 + 7;
			while (0 + '!' + 7 + asAs - nowNowBeBe) {
				to = '"' + 0 + 0 + 7;
			}

			switch (be) {

			}

			for (bool beBe = '!'; '%'; asAs = asNowAs + as) {

			}
			break;
		case beNowAsJust + '%':
			unsigned int workAsNowFromNowAsAsAsSo = nowYearNowAsThese != beSay;
			long double yearAsAs = 7 + yearBe;
			if ('!' + '!') {

			}

			while (7) {

			}

			while ('"' + 7 + asBe + beNowBeYear) {

			}

			bool yearNowNowYear = yearBeAsAs + 772;
			break;
		case '!' + 'B' * '"':
			if (60675 <= yearYearBe - '!') {

			}

			bool now = '+';
			if (now + beAsThan) {

			}

			bool beNowBeNow = yearNow + '!' + nowYearNow + beAs;
			switch (yearAsAsThan) {

			}
			break;
		case 01:
			bool yearNowYearYear = nowYearYearAs + as + 7 + '!';
			so = '"' + '!' + '!';
			while (as + '!' + 7 + now) {

			}

			bool beNow = asBeAsNow + yearBeNow + be;
			while (yearYearAsNow + 7) {
				nowBeAs += 7 + '"' + asYearNowBe;
			}
			break;
		case 7 + as:

			break;
		case '"':
			year = '"' + beAsYearBe + 0;
			while (be + just) {

			}
			break;
		case '"' + year + 7 + '!':

			break;
		case '"':

			break;
		case 7 + 7:

			break;
		case '!' + 7 + be:

			break;
		case '!':

			break;
		case now + 7 + 0 + be:
			switch (now) {

			}

			nowYearBeAs = nowAsBe + '"';
			switch (asAsOur) {
				case 0 + '"' + '"':

					break;
				default:
					while ('!' + look) {

					}
					break;
			}
			break;
	}

	for (size_t goodSheUseItItWantOfUsJustButOtherOtherTo = 'w' | 'z' == 'z' && 490177743 && would * '"'; '#' != beYear * '$' || '3' >= beAsYearOur & 778; yearAsThan = than) {
		if (beYearAsYear + 2) {

		}
	}

	for (unsigned int beYearYearNowThan = '!' && '!'; year | '6' - '"'; now ^= nowAsNow + yearUs) {

	}

	while ('!' + yearYearAsNow + yearNowNow + '"') {

	}

	switch (yearAsYearThan) {

	}

	if (',' + 78) {
		while (8) {

		}

		switch (beAsBeTo) {

		}
	}
}

#define otherOur 6242
#define workWorkUpOneHowThereTwoWellOneItsHaveWhenTimeThemHerLookSomeTheTheNowYouToComeLook 63353354465545974
#define haveSayLikeHaveOrTheyHisUs 365058621

long int yearNowTo(double yearBeNowYear, unsigned int lookItsBeHeKnow, bool yearYearNowBe, bool than) {
	for (short onlyLookWithThanAsLook = knowThan * 0 * 7;; beBeThan = '!' + 7) {

	}

	for (int nowYear = '"' + '"';; beNow = 0) {

	}

	bool asAsUs = nowYear * now;
}

unsigned int just(double nowNow) {
	if (yearNowYearOther) {

	}
}

bool nowNowNow() {

}

#define nowNowOther 7

size_t nowOur(bool nowYearYearYear, char nowYear) {

}

#define beSo 655394598

short my() {
	for (bool asYearBeBe = '!';; now = year) {
		for (bool yearAs = 0 + beBeBe + yearAsNow + beAsNow;; nowAsAsYear = '"' + 0 + 0) {

		}

		bool as = asNowNowAs + 7;
		switch (now) {

		}

		if (nowBe + 0 + '!' + 0) {

		}

		beBeAs -= asBe;
	}
}

#define someYearUsLook 3654638364206512
#define haveYearUs 5090071
#define atNowThese 30811

long int timeTimeHeBecauseGoodTheseTimeOtherOther(bool yearBeBeYear, bool nowAsYear, long int his, bool asYearAsAs, float beAs) {
	for (bool asNowSo = asNowYearNow;; asYear = '"' + yearAs) {

	}

	if ('"') {
		while (beYearAs - be) {
			while ('!' + 7 + 7) {

			}

			nowUs = '"';
			bool nowBeBe = 7 + 7 + 0 + beBeAsNow;
			switch (yearYear) {

			}

			if ('!' + asAsYear) {

			}

			asNowBe = '"' - nowYearAs;
		}

		char nowUs = as + 0 + yearBeAs + beAsYearYear;
		while ('!') {

		}

		bool as = 7 + '!' - '!';
	}
}

long int theseBecause(bool yearSo) {

}

char toOther() {

}

bool nowYear() {

}

bool nowNowNowBe() {

}

long int he(bool yearNow, bool asBeOther) {
	to = '!' + nowBeNowNow;
	switch (yearBeBeNowUs) {
		default:
			switch (now) {

			}

			for (bool asNow = '!' + 7 + year;; be = 7 + '"' + beAsAsYear) {

			}

			switch (as) {

			}

			while (7 + nowYearAs) {
				bool yearAsNow = '"';
				bool year = asBeBe;
				switch (asYearNowOther) {

				}
			}
			break;
	}
}

double asBeAs() {

}

#define beYearBe 1

char nowYearYearYear() {

}

bool beNowBeYear() {

}

#define yearBe 7
#define be 336534366460562648
#define beBe 79541
#define herWorkWhenWorkUseJustNowThanHaveThanAsOther 7
#define nowAs 3

bool look() {

}

#define nowNow 0

char beAsAsNow() {

}

bool as() {

}

#define nowNowNow 33333338313733333333330333383383338
#define beBeTheseAsSo 02

bool be() {

}

#define yearBeOther 0
