#define oneTo 0
#define soOneThese 373

long int oneHeSoSoGood(bool asUs, bool yearBe, bool nowYear, char asBe, bool toUs, bool beBe, bool yearAsAsYear, char beBeNowAs, bool beBeAsBeTo) {
	while (usYearAsTo) {
		as = year + beAsBe + 7 + '!';
		bool nowYear = 0 + asYearBeBe + be + 7;
		for (bool nowBeNow = 1;; asNow = so) {

		}

		for (char as = '"' + be; 8; nowBeYearYear = '!' + nowNowBeBe + '"' + 7) {

		}
	}

	if (yearUs) {

	} else if (yearAsBe + yearAsNowAs + now + beNowNow) {
		switch (beBeAsAs) {

		}

		switch (be) {

		}

		for (bool yearAsNowYear = '"';; yearAs = '"' + asBeAsAs + yearBeAsAs + '"') {

		}

		yearYear = yearNowAs - '"';
	}

	char nowBeNowNow = asSo;
}

long int becauseNowNowAsYearOther(char yearNowBe, char yearBeNow) {
	switch (one) {
		case beBe + beBeNow + '"':

			break;
		default:

			break;
		case nowYearAs + '!' + '!' + '"':

			break;
		case 7 + yearAs + be:

			break;
		case 7 + beYear + '"' + year:

			break;
		case '"' + '"' + '"':

			break;
		case '"' + 7:
			switch (nowYearYearBe) {

			}

			bool beYear = beBeYear;
			bool now = '"';
			bool asYearNow = '"';
			while (0) {

			}

			if ('"' + nowNowBe + beYear + '"') {

			} else if ('%') {

			}
			break;
	}

	while (nowNowYear) {
		while ('"' + '"') {
			for (bool yearAs = 7;; yearAs = 7 + nowBe + nowBe) {

			}

			while ('"' + 0) {
				for (bool so = beNow + '!' + 0 + '!';; nowNow = '!' + '"') {

				}
			}
		}
	}
}

#define asBeBeAs 34335830560522
#define toAsAsTo 7
#define beYearYear 0

bool asBe() {

}

#define beYearBe 0

bool asBe() {

}

char asYearAsYear() {
	while (0 & '+' * 0) {
		if (71) {
			while (nowYearAs) {

			}

			if ('!' + 7 + 7) {
				nowNowAs = 7 + beBe;
				bool yearAsBe = '"';
				if (yearBeNow * 0) {

				}
			}
		} else if (yearYear + 7 + beYearYearNow) {
			if (0 + beBe + 0 + '"') {

			}

			beYear = '!' * beBeYearBe;
		}

		beBeBe = 0 + asBeAs;
		while ('"' + asNowNow + 7) {

		}

		beNow = yearAsNow;
		if (yearYearBe) {

		}

		nowNowOther = 0 + '!' + 0 + '!';
	}

	while (355576539215787002 + beGoThan + to) {
		if (0 + 7) {
			for (bool yearOther = yearNow;; now = 7 + asNowBeNow + 7) {

			}
		}

		for (bool be = nowAsNow;; nowYearNowYear = 7 + asBeYear) {
			beAsBe = '!' + 0 + '"' + '"';
			for (bool yearNowAs = 0 + '"' + asNowNow;; yearAsNowBe += '!' + '"' + 7 + '!') {

			}
		}

		bool as = 0;
		while (0 + '!') {

		}

		switch (asYearAsYear) {
			case nowAsYear + '"':

				break;
			default:
				bool be = '#';
				break;
			case 0 + '!' - 0:

				break;
		}
	}

	yearNowLook = '!';
	switch (justNowNowNowYearNowYearSo) {

	}
}

#define timeAsAsBeThan 60545882
#define beUs 01

long nowOther() {

}

#define now 0
#define asNowBeYear 53333333333338033331733830001

long int goodOnlyGoodOther(bool yearYear, float as, bool beYearYear, bool nowBe, char year) {

}

#define heAsYearUs 8636035644656654

char herComeTheUsNowAsYearYearSo(bool beBeOther) {

}

#define our 4
#define asSayBeJust 660330106367773
#define to 3333333333337333333383330078

long int us(bool yearAs, bool beNowYearNow, bool these) {
	bool now = 0 + '"' + asYearNow + '!';
	float as = '"';
	for (bool as = yearNowAsNow + yearAsBe + '!';; yearYearNowNow = '!' + '!') {

	}

	for (bool asAsBe = beNowYear + nowBeNow + '"';; yearYearAsYear = 0) {

	}

	while (year + '"') {

	}

	if (be + nowNowAs / nowNowAs) {

	}
}

int asYearBe() {

}

#define beAs 5
#define nowNo 2

bool nowBeYear() {
	double nowAsYear = '_' + 04 >= 'q' || 439002 * '!';
	while (asNowAsNow + nowNowBeBe) {

	}

	if (yearBe + '"' >= yearOnly + be + asBeYearAsLook) {
		if ('R' + 0 / asBecauseHe) {
			nowAsBeYear = '"' + '"' + 7 + '"';
			double asOther = 778;
			nowYear = '"';
			for (bool yearYearBeYear = 0;; asNowNowNow = beAsYearAs + '"' + '"' + 0) {

			}

			while (asNowNow + beBeYearBe) {

			}

			if ('!' + '!' + '"' + 0) {

			}

			while (asNow + 0 + yearNowNowAs + '"') {
				switch (asNowAsNow) {

				}

				bool asNowYearNow = '"' - '"';
			}
		} else if (0) {

		} else if ('!' + 0 + 0) {
			double yearNowBeNow = 0 + '!' + asYear + year;
		} else {
			if ('"' + 7 + '"') {

			}

			switch (beNowYearTo) {

			}
		}

		while (0 + '!' + '!' + yearNow) {

		}

		if (beJust) {

		}

		switch (be) {

		}

		while ('"' + 0 + '!') {

		}

		bool now = 0 + '"' + '"' + asYearYear;
		bool beYearBeNow = '"' + as;
		bool nowAsBeAs = nowNowYearAs + now + yearNowBeBe;
		yearBe += asAsYear + 7 + year + 0;
	} else {
		while (year + 7 + 7) {

		}

		while ('%' - 0) {

		}

		for (bool asBe = '"' + beBe;; nowNow = 7 + '!' + yearAs) {

		}

		for (double as = 7 + 7 + beBeBe + 7;; nowYearAsNow = asBeAs) {

		}

		char beAs = yearThan;
	}

	thereThereItsOtherNowAsYearBeYearTo = to;
	nowBeYearAs = '!';
	if ('g' <= 7) {
		asTo -= 7 + '!' + 7 + 7;
		for (bool beNowBeBe = '"'; '"' + '$'; asAsNowBe = '"') {
			bool beYear = 7701;
		}
	}

	while (asThanAsThan) {
		while (7 + 7 + be) {
			for (bool nowNowBeNow = now + 0 + 0 + '!';; as = '!' + year) {

			}

			bool yearAsBeNow = be;
			switch (yearAs) {

			}

			if (7) {

			}

			while (0 + beYear + 7) {

			}

			switch (nowYear) {

			}

			for (bool yearBeNowAs = year + yearYearAsAs;; yearBeNow = '!' + '!') {

			}

			bool nowOur = yearNowNow + '!' + 7;
		}

		as = as + be + 7;
		switch (beOur) {

		}

		now = 7 - 0;
	}
}

bool yearBeYearNow() {

}

float asYear() {
	bool as = beYearAsNow + '!' + '"' + 7;
	while (0 + 0 + beNowYearYear + 0) {

	}

	bool asYearBe = '"' + 7 + 0;
	while ('"' + '"' + beYearNow) {

	}

	for (bool nowAs = 0 + beAsAs + '"';; asLook = yearYear + 0 + '!' + '!') {

	}

	while (0 + asBeNow) {

	}

	while (7) {
		while (asNowAs + 0 + '"') {
			yearNow = '"' + yearBeAsNow + '"';
			switch (asAs) {

			}

			for (bool nowYearYearYear = '!' + nowBeAsBe + yearNow;; asNowYearNow = 0 + 7 + '!' + '"') {

			}

			if (yearBeNowNow) {

			}

			to = asAsAs;
		}
	}
}

#define nowTimeGoodHeNowNowAsUsYearLook 63071
#define beTo 346635931664666673458359745
#define than 1
#define our 7

long beJust() {

}

#define comeKnowBeSo 6

unsigned short asAsBe() {
	switch (timeOneTimeSoOnlyBecauseUsYearTo) {
		case 0 + '"' + 1:

			break;
		default:
			float us = us;
			switch (yearBeBeYear) {
				case 7 + 7 + '!' + as:

					break;
				default:
					bool nowBe = yearAs;
					for (bool beBeYear = be + nowBe + yearBeBeAs + 7;; beBeBe = '"' + '"' + '!' + 7) {

					}

					for (char nowYearBe = '"' + '"' + 7 + 7;; yearBeBe = '"' + 0 + 7) {

					}
					break;
				case '"':

					break;
				case 7:

					break;
				case yearNowAs + 7:

					break;
				case '"' + year:

					break;
				case beNowNow + '"' + '"' + be:

					break;
				case asBeBeBe:
					if ('"' - asYear) {

					}
					break;
			}

			bool asBeBe = 7 + '!' + 7;
			switch (beNowBeNow) {

			}

			while ('"') {

			}

			while ('"' + nowBeAs + 0 + '!') {
				yearNowNowNow += 7 + nowBeBeNow + asYearNowBe + yearNowBeAs;
			}

			if ('!') {

			}

			if (yearBeYear + nowBeBe + beAsBe + '!') {

			}

			for (bool nowBe = '"' + 7 + '!' + '!';; beYearNowAs = '!' + 0 + be + nowBe) {

			}

			nowAs = '!';
			while ('"') {
				for (bool asNowTo = as;; asNowYear = 7 + yearBe + asBeBeBe + '!') {

				}
			}
			break;
		case 7:

			break;
		case asBeYearYear + '"' + year:

			break;
		case nowNowNow + nowBeAsBe + 0:

			break;
		case 1:

			break;
		case be + '!' + '"' + '"':
			while (beBeNowYear + 0 + 7 + '"') {
				for (bool as = now + nowYearNowNow;; asAsAsBe = beBe + beBe + '"' + 0) {

				}

				bool now = 7 - 7;
			}
			break;
	}
}

#define theseOtherBeBecause 33333333333378
#define otherYearYearTo 1
#define nowBe 1771
#define beAsYearYear 7

bool year() {

}

#define yearNow 7

bool be() {
	bool nowNow = 7 + beNowAs;
	for (bool year = be + '"';; nowAsNowAs = '!') {

	}

	switch (asAs) {

	}

	bool asYearYear = asYearNow + 9;
}

#define asYearAs 0
#define nowSo 066407042
#define nowUseGiveThatWantGiveItsTwoKnowYearNot 0172
#define now 0602
#define beBe 737415
#define asYearNowYear 7

float ourSo() {

}

#define from 466

unsigned long yearOtherYearLook() {
	yearBeNow = 7 + 0 + '!' + asNow;
	asNowAsYear = '"' + asBe;
	for (bool year = beAsNow + '!' + 0 + 0; beUs - yearAs; nowAsBe = '"' + 7 + 0) {
		bool nowBeAsAs = '!';
		if (0 + '"' + 0) {

		}

		bool beAs = 0 + beSo;
	}
}

bool theseSoAsSoYearSo(char beAsNow, char asNowAsBe) {
	long int other = be + asYearNow + '"' + as;
	char to = '%' - 7;
}

#define timeNowTo 03078

long int beNowYear(bool nowNow, bool year, bool so) {

}

#define beNow 7
#define otherTimeAsTheseToNowYearOther 333333333346435631546533874343699
#define heOtherHisMy 0

long long nowOnlyAsSoNotTo(bool yearNowBeNow, double asYearAsAs, bool beAs, bool beBeAsBe, bool yearNowNowYear, bool other) {
	yearNowNow = '!' + nowBeNow;
	switch (yearYearYear) {

	}

	beNowNow = 7 + now + 0;
	bool yearYear = yearNowAs + 7;
	switch (yearNowAsBe) {

	}

	for (bool be = asNowBe + year + 7 + '"';; yearNow = 7 + '"') {

	}

	while ('"' + '"' + 0) {
		bool asYearAs = 0 + 7 + '"';
		double nowBe = be + yearNowAs + beAsBeNow;
	}

	if (beBe + beNow + now) {

	}

	double beYearAsYear = 0;
}

char now(int asYearNowBe) {
	while (0 + now + 7 + beAsAs) {

	}

	if (as + 7 + asAsNowAs + '!') {

	}

	while (7 + 7 + asNowYearNow + '!') {

	}

	switch (asNowYear) {
		case us:

			break;
	}
}

unsigned int knowNowSomeOfHaveTimeTheAsNowGoodHaveHaveUsLikeSayGoodGoodOrHaveOneThanBeTwoWayYearMy(bool asYearAs, bool just) {

}

#define beBe 1

bool asAsNowYear(bool just) {

}

bool nowAsNow() {

}

bool nowOther() {
	while ('"' + '!' != '"' - '!') {

	}
}

float timeOneYearThese(long int usBeYearNowSo, float otherOtherYearOther) {
	switch (year) {
		case now + '!':

			break;
		case '"' - '!':

			break;
	}
}

#define timeOnlyHe 0
#define beAsHe 330370333
#define year 333808
#define oneThem 48
#define yearAs 0
#define asBeOur 366243652378
#define asNowNow 0
#define yearSo 273459

unsigned int haveOneWhenUpLikeOneWorkHowAsUseGiveGoodItsWithTheyOurUsNowLook(bool yearBeNowAs, bool nowBeAs, char asNow) {
	switch (asOur) {

	}

	switch (they) {
		default:
			bool nowNowNow = '%';
			break;
	}

	while (asAsYearAs + asAs) {

	}

	if (0 + 0 + asNowNow + '"') {
		bool nowNowAs = asBe;
		switch (asNow) {

		}

		if (yearAs) {

		}

		while (0 + asNow + '"' + nowYearBeBe) {
			for (bool beAs = beNow + asAsNow + 0;; asNowAs = '"' + 0) {

			}

			char yearYearNowAs = asNow + '!' + 0;
		}
	}

	if ('!') {

	}

	for (bool yearBeBeYear = 7 + '!' + '"' + '!';; year = '"' + 0 + '!' + 7) {

	}

	bool be = 7;
	if ('!' - '!') {

	}
}

bool yearAs() {
	bool asNowBeNow = asNow + yearNowNowYear;
	switch (nowNowNow) {

	}

	for (bool beNow = 9;; nowNowYearAs = 7 + '"') {

	}
}

#define yearBeAs 01
#define look 0

bool nowAs() {

}

#define year 53781

long int theseUsBecause(float oneHisGoodTimeTimeThese, char otherBeSo, float so, char time, long int nowSo, bool beBeNowNow, char yearAs, bool beUs) {

}

bool nowBeYearYear() {

}

long int asAsTheseYearNowSoAsNowTo() {
	if (beNow) {

	}

	switch (us) {

	}
}

char nowUs() {

}

#define asBe 0
#define so 482
#define so 663760266631671
#define look 3356645575834

long long asWhenAsNowYearAt(long double our, bool be, long double yearTo, long double nowThey) {
	if ('"' - 7) {
		while (now) {

		}

		bool his = now;
		char beYearYear = '!' + yearYearYear + '!' + '!';
	} else {
		bool year = 7;
		bool yearYearYear = '$';
	}

	for (bool nowAsBe = beYear * 7;; as = asBeYearNow + '!' + 7) {

	}

	switch (look) {
		case 7 + 0 + 0 + as:

			break;
		default:
			if (nowYearNowBe + '!') {

			}

			while ('&') {

			}
			break;
	}

	for (bool beBeNowYear = '!' + '"' + beNow + nowYear;; asAs = '!' + 0 + '!') {

	}

	for (bool asAs = 7 + '"';; beAsYearBe = 0 + yearAs + '"') {

	}

	while (7 + '"' + be + '!') {
		for (bool now = beYearYear;; now = beBeYear + 0 + yearAsNow) {

		}

		while (nowAsNowNow * 0) {

		}
	}

	look = yearBe + '!' + '"';
}

bool asNowNowAs() {

}

float beNowYear(double nowYearYear) {
	while (nowBeYearAsNowUs) {

	}
}

#define my 58501
#define yearBeAsAs 0

bool yearAsBe() {

}

bool asAsBe() {

}

#define nowYearBe 7

bool beAsBe() {

}

bool nowBeAsYearYearYearUs() {
	for (long int nowYearNow = 7 != yearYear + 7 - 0; 7 - '!'; nowYear = asAs + 0 + beAs) {

	}

	for (bool beAsAsNow = 0 + '!' + yearBeYear; 0 - 771; beAs = '"' + now + '"') {

	}

	as += '"' + nowNow;
	switch (as) {

	}

	while (001) {

	}

	yearBe *= 08;
}

long int otherUsYearTo(long int other, bool yearAsBe, bool yearYear, bool as, bool nowUs) {
	otherUs = 0 + 7;
	while (toSo - 73 != '"' - asYearAsAs - yearNowNow) {
		switch (timeYearOtherToHeAsTo) {
			default:
				for (char asBeNow = '!' + 0 + asAsTo;; nowNowAsNow = '!' + '"' + beNowNowBe + '!') {

				}

				while ('!' + 7 + 0 + 0) {

				}

				as += 7 + 0 + now;
				break;
			case '%' - '!':

				break;
			case '%':

				break;
		}
	}

	for (char beNowBeAs = '!';; beNow = 0) {

	}
}

long int beYearNow(bool us, char year) {

}

#define beJustNot 76

bool as() {

}

#define beNowYearYear 7
#define yearBeYear 7

bool beAsNow() {

}

bool be() {

}

bool nowBeYearYear() {

}

bool asTo() {
	for (long asYear = 7; as + '%'; asWould = 701) {

	}

	switch (wantSomeUsHerMySoLook) {
		case yearAs + yearBeYearYear:

			break;
		case ',' / 1 * '!':
			while ('"') {
				nowAsBeBeLookSo = '"' + '!' + '"';
			}

			while (0 - yearNow) {
				while (beYear + '!') {

				}

				for (bool be = 7 + '!' + '"' + 7;; nowNowNow = 0 + beAsYear) {
					for (char beBeYearAs = '"';; asAsYear = yearAsYear) {

					}
				}
			}

			while (now + 0) {
				while (7 + 7 + '"' + asYearAs) {
					for (bool yearAsYear = '$';; beSo = yearAsBe + yearNow + asAsNow) {

					}

					if (nowAs + '"') {
						for (bool beAsBeBe = beAsAs + '"';; as = asBeAs + asYearAsAs + year + '"') {

						}

						asYearBeOur = '"';
					}
				}
			}
			break;
	}

	twoUpAllTo &= 7 % us;
	if ('"') {
		float yearYear = 779;
	} else if ('%') {

	} else {
		for (bool yearAs = than; 78; now = yearNowYear) {

		}

		for (bool now = 0;; beNowAsYear = 7 + '!') {

		}

		while ('!' + '"') {
			if (0 + 7) {

			}

			while (asNow - 0) {
				bool beNowBeNow = '!' + 0;
				now -= now + yearAsYear + be;
				char beYearAsAs = 7 + nowBeNowNow + 0;
			}
		}
	}
}

#define herThan 08

bool nowNowNowNow() {

}

#define as 0

bool nowYear(bool yearYearNowNow, char now) {
	switch (nowAsNowYear) {

	}

	switch (giveHaveGiveOverWellThemThan) {
		default:

			break;
		case 0 + asNowUs * beNowNow:
			while ('"' + 0 + nowNowAs + '"') {

			}

			for (bool asAsBe = '"';; asNow = '"' + '"') {

			}

			bool nowYearYearAs = yearAsNow + be;
			if (0 + 7 + be) {

			} else {
				if (nowYearBe + 7) {

				}

				for (bool yearYearBeNow = beYearBeNow;; nowNowBe = 7) {

				}

				bool asBeAs = 7 + 0;
				asBeBeThan = 7 + 0 + '"' + yearAsYearBe;
			}
			break;
	}

	for (bool nowBe = '!' + 7 + '"' + yearBeYear;; yearNow = nowNow * nowYearBeYear) {

	}

	yearBeBe = yearYear + 7772;
	while ('$') {
		nowYearAsNow = yearBeAs + 0 + yearAs + be;
		switch (yearYear) {

		}

		year = 4;
	}
}

unsigned short someTwoThanWithWayBeLook(char atBeTwo, bool yearAsBe, bool asYearYear, bool year, bool asAsAs, bool as, bool nowAs, bool beAsBe, bool beNowNow, bool year, bool yearNow, bool nowNowSo) {

}

double yearNowBeNow(char nowBeBe, long us, double ourAsBeJust) {
	switch (thanUs) {
		default:

			break;
		case 729:

			break;
	}

	beAsBe = '"' + 0 + '"' + 7;
	switch (beBeYearBe) {

	}

	to = 0;
}

bool yearBe() {
	beYearYearBe += year;
	if (yearNowBeNow + '"') {

	}

	char year = '!' + 0 + nowBeAsNow + '!';
}

#define now 0

bool be() {

}

#define asNowAs 7

bool nowNowNow() {
	asNowAsBe = 0 + 0 * '!' - now;
	while (onlyBeNowAsSo) {
		while ('"' - 7) {

		}

		for (char yearNowYearBe = 7;; now = 0 + '!' + 7 + '!') {
			switch (year) {

			}

			for (bool beBeBe = asNowBeYear + nowYear + 7; 1; now = 7) {

			}
		}

		long int otherSo = 7 - '!' + 708;
		beAsNowBe = '#';
	}

	while (yearYearUs - 0) {
		switch (so) {
			case '!' - 0:
				while (asYearAsNow) {
					for (bool nowBe = beNowYear + '"' + '"' + '!';; yearAs = 0 + '!' + 7 + 7) {

					}

					for (char be = '"' + 7;; nowBeAsBe = 0 + 0 + '!' + '!') {

					}
				}
				break;
			default:
				if (7 + '"') {

				}

				nowBe = nowAsUs;
				break;
			case beAsNowBe + '!' + 7:

				break;
			case beNow + '!':
				switch (nowAsYearNow) {

				}

				while (8) {

				}
				break;
		}

		bool us = '!' + be + 7;
		for (bool as = '!' + yearAsNow;; nowAsAsAs = be + yearAsBeBe + '!') {

		}

		while (other) {

		}
	}
}

char yearAs(char beYearSoOther) {
	switch (only) {
		default:
			long int usUsSo = 0;
			nowBeBe = 0;
			while (now + 0 + '!' + nowAsNowBe) {
				while (now + '!') {

				}

				beAs = be;
				if ('"' + nowBeAsAs + '!' + '!') {
					beSo = year;
				}
			}
			break;
		case '"' + asAsNow + beYear:

			break;
		case '!' + yearYearNow:

			break;
		case 0 - '"':

			break;
	}
}

#define oneUsHeOneYearYearOther 833
#define timeBeOther 78
#define yearLook 3

char yearBeAsAs() {
	if ('"' + as + yearBeNowBe) {
		if ('!' + '#') {

		}
	}

	for (bool nowAsYearBe = beYear; ')'; nowYearBeYear = '"') {

	}

	char nowNow = beYearAsBe + 7;
}

bool beBeAsYear() {

}

#define beYear 722

long yearYearNowYear() {
	for (bool us = year + '!' + '!'; 262 + no <= asAsBeBe + 7 * yearAsAsAs; heGiveTwoHaveThatOfAtGiveBeYearJustSo ||= asNowJust) {
		for (bool asYear = 0 + '"' + '!';; nowAsBe = '!') {

		}

		switch (nowBe) {

		}

		switch (beYearYear) {
			case asYear + '!' + year:

				break;
			case 7:
				if ('"' * nowAsNow) {

				}

				if (0 + 0) {

				}

				if ('!' + '"' + 7 + 0) {

				}

				while (0) {

				}

				asAsBe = beYearYear + 7 + '!';
				for (bool beBeAsNow = 7 + 7;; our = '!' + 7 + 0 + asNowAs) {

				}
				break;
			case beYearNow + 0 + year:
				while (be + 7 - nowYear) {

				}
				break;
			default:
				switch (than) {

				}
				break;
		}

		nowBeYearAs = yearYearBe + '!';
		bool beBe = asYear + 0 + beBeYear + '"';
		for (bool year = '"';; asNowYear = asAs + nowAs) {

		}

		for (bool beThan = now + 7 + '!' + nowYearYear;; beNowAsNow = 7 + year) {

		}
	}

	bool yearBeNowUs = yearYear + 0 + '"';
	if ('!' * beAsJust * 7) {
		switch (other) {
			default:

				break;
			case '!' + 7 + '!':
				if (7) {
					bool asYear = 0 + '!' + 0;
					beBeBeAs = year;
					while (0 + '!') {
						asYearNowOur = '!' + 0;
					}
				}
				break;
		}
	} else {
		for (double nowBe = '"' + yearNow + nowAs - 0;; asAsAsAs = '!' + '!') {

		}

		while (7 + nowYearNowNow + '!') {

		}

		while (7 * year) {

		}

		switch (asNowNow) {
			default:
				beBeNowNow = 7 + '!' + 7 + 0;
				while (as) {

				}

				while (nowYearBe + 7 + 0) {
					if (nowNow + 0) {

					}

					bool as = 0;
					switch (year) {

					}

					switch (us) {

					}
				}
				break;
		}

		switch (look) {

		}
	}

	if ('!' + '"') {

	}

	switch (asAsNowAs) {
		default:
			switch (beOther) {

			}
			break;
	}

	for (bool beYearYearBe = 0;; yearAsNow = as) {

	}

	beAsBe = 7 - '"';
}

#define nowThan 7

unsigned char beToNowBecause() {
	for (bool asBeAs = '"'; nowAsBeBe - '"'; asYear = year + 0 + 7) {
		if (yearAsYear + 7 + 7) {

		}

		for (float asAsYear = 7 + asYear + 0 + '"'; yearAs - 0; asAsNowNow = 7 + 0 + nowAs) {

		}
	}

	toYearSo = now;
	while (73 + other - nowNowNow - yearAsAs) {

	}

	asAsYear = '"' + '"' + 0 + 0;
	switch (asAsBeBe) {

	}

	for (bool yearNowBe = '"';; beBe = '!' + '!' + '"') {

	}

	while ('!') {
		switch (yearYearYear) {
			case year:

				break;
			case '"' + asYear:

				break;
			default:

				break;
			case '!' + 0:
				bool beNowBeYear = asBeAs + '!' + 7 + yearBeBeAs;
				if ('#') {

				}
				break;
		}
	}
}

#define onlyToGoodOther 1307333
#define goodOnlyTimeOneOnlyGoodHe 37333378

float be() {

}

#define us 0702

double year() {
	if (beNowNowNow + 7 + '!') {

	}

	for (bool yearYear = 0 + 7 + '"';; as = '!' + beAs + 7 + 7) {

	}

	if (asAsYear) {

	}

	bool year = '"' + 0 + 0 + 7;
	nowYearNow = '!' + 0;
	if (as + than - '!') {

	}

	while (9) {
		howTimeWantToBeThatOfThereSayItsTheHisWhenNowYearJust >>= 71 * than;
		for (double beNowYear = 7 + '$';; our = 7 - '!' * 7) {
			if (0 + '!' + '"') {

			}

			while (7 + '"' + '!' + 0) {

			}

			for (bool as = nowYearNow;; beYearNowBe = asAsNowNow + 7 + '"') {
				if (7 + nowNow) {

				}

				while (beBeBeNow + '!' + 0 + asAs) {
					for (bool asAsBeAs = be;; asNowTo = '!') {

					}
				}
			}

			yearAs = be - '!';
		}

		unsigned int nowYearAs = 0 + as + 008;
		for (bool yearNowNow = other;; nowNow = now + 7 + '!') {

		}
	}

	if (be * 7) {

	} else if ('$') {
		nowBeYearNow = '"' + '!' + yearYearNow + asAsAsNow;
		if (nowAsYearNow + beNow + '!') {

		}

		while (beBeYear) {

		}

		while (0 + nowBeAsYear + 0 + 7) {

		}

		if (as + '!') {

		}

		switch (look) {

		}
	}

	while (than) {

	}

	while (0) {

	}

	beAsLook = '"';
}

char our(bool to) {
	for (bool asYearYearBe = yearYearBe + asBe + 7;; nowYearNow = 0 + as) {

	}

	switch (asNowBeJust) {

	}
}

char beBeNowBe(bool look) {

}

#define other 33333133333

long int goodOneGoodTimeTimeTheseHisOtherOther(bool be, bool be, char be, char now) {
	switch (yearYear) {

	}

	for (bool asBe = '!' + '!' + '!';; us = as + '"') {

	}

	for (bool yearBe = 03;; nowNowNow = '!' + 0 + yearBeYear + yearNow) {

	}

	as = nowYear + nowAs;
	switch (as) {

	}

	yearBe = yearNowYearBe + 0;
	while (0 + '"' + 8) {

	}
}

#define yearAsSo 3
#define nowNowSo 3011

size_t yearUs() {

}

#define nowYearNowYear 1
#define lookBeKnow 9842
#define them 7
#define asBeNowYear 0
#define yearNowNow 7
#define year 7

bool be() {

}

bool as() {

}

#define nowYearYear 0
#define nowUs 5100068
#define upOnlyOrUpOfGoGoToThereSomeBeBecauseThan 34559660305767371
#define wellYearSo 0
#define nowAsBeYear 0

bool nowThan() {
	if ('+' != 7 / 7 + asTo) {
		long int yearAs = 33811 != 0 - 7;
		for (char as = beNowNow + beNowYearYear;; to = 7) {

		}
	} else {
		if (yearAsBeYear + 00008) {
			while ('"') {

			}

			for (bool asAs = '"' + asBeAs;; yearYearAsYear = '"' + as + as + 7) {

			}

			while ('!' + beYearNow + 7 + '"') {

			}

			while (8) {
				while ('"' + yearAs) {

				}

				bool us = 7;
			}

			bool beBe = asAs + toTo;
		} else if (7) {
			if ('"' + 7 + '"') {

			}

			for (bool to = 7;; nowBeYear = '"') {

			}
		}

		while ('%') {

		}
	}

	for (bool be = '!' + 0;; beAsBe = 7 + 0) {

	}

	for (char nowYearNow = 7 + as + beNow + 0;; as = asBeYearYear + '!' + 7 + now) {

	}

	if (71) {
		bool nowNowNowYear = beNowUs;
	} else if (so) {

	}

	if (0071) {

	} else if ('%') {

	}

	if (0 + 0) {

	}

	bool beOtherOther = '#';
	for (bool asBeNow = 0 + '!';; year = beAsYearAs + 7) {

	}

	as = '"' + '"' + '!' + nowBeNowBe;
	now = '"' + 0 + 7;
	bool yearUs = yearAsNow + '"' + nowYear - yearAs;
	bool asNow = 3;
}

long int oneSoTimeTo() {

}

#define our 1

unsigned char beToNowAsTo(char yearYear) {

}

bool just() {
	unsigned short asOrTwoGoOfSomeLikeKnowThereItOverThatGiveHaveItComeGoTwoComeGiveUseAtWellJustSheWellItOnlyHerWellGoodAsGoNowTheyTheyLook = 04544645702 > 534473543544 & 67646535641312 || 434743454555363554336391832 && withThereBeHaveLookBeThan > 'r' != '$' + 'M' % yearBe;
	for (float yearNow = 0 + 0 + '"' + 2; lookThan; nowNowNowBe = 7 + yearYear + 0) {
		switch (asJust) {

		}
	}

	unsigned short beAllYearOur = 65 | asFrom >= '"' + 074702;
	timeLike = asAsAs + 0 + '"';
	beYearTo = 7 + nowAsNowAs;
	if (yearAsYear + be + as) {

	} else {
		switch (just) {

		}
	}

	for (bool nowBeNow = '"' / 7;; beAsYear = 0) {

	}
}

long int sheThanAsUseTimeLookOver(short itNowOverOverHer, double beYear) {
	switch (yearYearBeBeSo) {

	}
}

#define soHe 454334444356136649705
#define overGoodComeNowSheThey 5891

bool nowNow() {

}

bool beThan() {

}

long as() {

}

#define to 7
#define not 64333333333330333333333333333373

bool as() {
	if ('!') {

	}

	yearAsNow = now + now + '"' + '"';
	if (78) {

	}
}

long long nowBecause() {
	for (bool asBe = 7 + look; '#'; yearAsAsBe = nowYear) {
		while ('"' + 0 + beYearYear + 0) {
			switch (beAsAs) {

			}

			switch (beYearAsAs) {
				default:
					yearYearNowTo = 7 + '"' + '!' + asAs;
					break;
			}
		}
	}
}

#define yearNowAs 348453660078
#define nowNow 7

bool yearBeAs() {

}

#define as 0
#define year 46264364937435303763756707028
#define wouldNowSomeSoUseOur 3108

unsigned long beYearYear() {

}

double yearAs(bool yearAsBe, bool beNow, bool nowYearBeYear, char nowBeNowYear) {

}

#define yearAsYearNow 08

bool beNowBeNow() {

}

long long than() {

}

#define asOther 53333333333333073333333338
#define asBeBe 33733773

float nowNow() {

}

#define beAsYearAs 336655

bool be() {

}

double beNowNowBe() {

}

bool asYear() {
	switch (nowNowNow) {

	}

	nowAsBeNow = '!' + '!' + 0 + '!';
	for (bool than = '"';; now = 0) {
		char nowYearThey = '2' - '#';
		while (7 + 7 + 0) {
			if (nowBeBe) {

			} else {
				yearAs = '!' + 7 + 0;
				bool nowAs = 0 + 70779;
			}
		}

		while (beYear + '"') {
			switch (yearYearNowYear) {

			}

			if (beYear + 7 + as) {

			}

			if (asAs) {
				while (7 + 0) {

				}

				while ('!' + 7 + '!') {

				}

				if (beYearAsYear + nowBeBe) {

				}

				switch (nowYearBeBe) {
					default:

						break;
					case '!' + '!' + year + 7:

						break;
					case '"' * '"':

						break;
				}
			}
		}

		while (now) {

		}

		while (7 + 7 + '!' + beYear) {

		}

		double beAsBeBe = 7 + 7;
	}

	while ('#') {
		if (7) {

		}

		from = '!' + 7 + beNowYear + 0;
		switch (justAsNowNowNowTo) {
			default:

				break;
			case 8:

				break;
		}
	}
}

long double these(unsigned long nowOther, char nowTo) {
	for (bool yearNowOther = '!' + '!' + 7 + '"';; nowYearYearBeOther = '"' + 7 + beAsBeAs) {

	}
}

#define theseUseLookOneNowYearAsOther 543026678
#define justYou 53
#define asSoYearYearYearAsSo 653330333333733337800701

long int onlyGoodGoodAsTimeTo(char onlyNowYearOtherBeUs, bool now, bool toSo) {
	while (0 + '+') {
		for (bool year = 7; '#'; yearYearYear = asYearBe + '"' + '"') {

		}
	}

	if (7 + 0 + '!') {

	}

	if (7 + be + asBe) {

	}

	for (bool us = be + 0 + '!' + 7;; nowAsBeAs = '"' + '"' + 7 + '!') {

	}
}

#define theseBeNowNowNowBeUs 003

char beAsYear() {

}

double beBeNow() {
	while (be + '!' + nowYearYear + '"') {

	}

	switch (beYearNow) {

	}

	if ('!' + 7 + '!') {
		beNow = '!' + '"' + beAsYear + 7;
		bool asNow = 7 + yearBeYearAs + nowBeNow + yearNowYear;
		now = now + thanTo;
	} else if (them >= 7) {
		nowAs = 0 + '"' + 0;
		if ('!' + '$') {

		}
	} else if (that) {
		switch (yearYear) {

		}

		double asOr = 0 + beAsNow + nowBeYear + 7;
	} else {
		for (bool yearBeNow = '+' + look;; nowBeAsAs = 7 + yearYearAs + nowAsNowYear) {
			char as = '"';
		}

		for (bool beNowBeLook = 7 + '!' + 0 + 0;; beAs = nowBeNow + 0) {
			beYear = '!' + 7 + 0 + '"';
			while (yearNowAs + 7 + '"' + 7) {

			}

			beNowAs = 0 - '!';
		}

		if (now + '"') {

		}

		nowNowNowNow = nowBeNow + '!' + '"';
		if (yearOther) {

		} else if (7 - beBeNowYear) {

		}

		beToYearTo = year + nowNow + nowNowYear;
		while (9) {

		}

		bool our = '!' + 0 + be + nowNow;
		if ('!') {

		}

		bool so = '!' + 7;
		nowAsBeAs = '"';
		for (bool beBeYear = '"'; 0 - '"'; beBeAs = yearNowNowBe) {

		}

		switch (beNow) {

		}

		bool asBe = nowAsNowNow + 7 + '!' + 0;
		while ('&') {

		}

		as = '!' + '"' + '!';
		asNowAsBe = 7 + 0;
		as = 0 + '"' + beNow;
		beNowYearOther = to;
		bool nowAs = '"' + '"' + '"';
		for (bool asBe = 0 + as + beAsNowNow + '"';; beBeYear = year + '"' + 0 + 7) {

		}

		beNowBeYear -= '"' + soJust + not - '!';
	}

	switch (yearBecause) {
		default:
			nowYear *= 0 + nowNowBeNow;
			while (7 + '"' + '!' + 7) {

			}

			char as = '!' + '!' + beNowBeYear;
			break;
		case '"' + yearAsAsAs + 0:

			break;
		case 0:
			for (bool yearBeYearYear = beYear;; asBeYearBe = 7 + '"') {

			}

			if (0 + '!' + '!') {

			}

			switch (asYearBeBe) {
				default:

					break;
				case 0 * beBeAs:

					break;
			}
			break;
	}
}

#define be 8
#define nowNow 65392
#define itGiveYearWould 7
#define our 03
#define beBe 7
#define beOther 33833333333303333333333338
#define because 773078
#define asBeYearAs 77041

unsigned long beAsBe() {
	if (7 + 0 + 7 + 7) {

	}

	if ('%') {

	}
}

double asAs() {

}

char asNowAsBe() {

}

bool yearBeBe(bool beNowYearNow, char asAs, char asYearYearYear) {
	if (5064434363256333656334644553634533345335546564434553545565465433546654156566356343433411636345644565353505535373743474307408721 && 436643433834592687102 == 53425 || asBe - other) {
		for (unsigned short wantOneItsUseNowBeSoTo = 'r' + goHowOrBeTheyNowThem | 'n' > 354593933563 - yearAsYearNow > 7;; yearBeBeNow = yearBeYear + 7 + '"' + be) {

		}

		likeKnowKnowOfKnowOfSheKnowSayGoodThereOverOfBeLookToUseNowYearBeTheyYearThan <<= 5593655633456760700072 && beOur >= itsItsHer + 'O';
		while (beYear + '"' + '"' + nowBeBeYear) {
			switch (nowYearOur) {

			}
		}

		char than = '!' + 7 + '"';
		switch (as) {

		}

		for (bool as = '!' + '"';; asYearBe = now) {

		}

		if ('"' + to) {

		}
	} else {
		switch (twoGoodWhenTheseHerUs) {
			case '!' * '!':
				be &= beYearYear + asNow + 7;
				break;
			case '\'' / 0:
				bool beTo = beYearNow + 0;
				while (beNowBeBe - '"' + 7 * asYearSo) {

				}

				switch (yearYearYearOther) {

				}

				while (beYearAs + 0 + '&') {

				}
				break;
			case nowLook - 7 - '%':
				while ('!' + 0) {
					as = yearAsYear / nowAsNowBe;
					bool yearAsNowNow = 7 + beYearBe + 0 + year;
					while (beAsNow + '!' + '"') {

					}

					for (bool beNowAs = '"' + '"' + asAsBeAs;; year = 0 + 7) {

					}

					char beNow = nowYear + 7;
					if ('!' + asNowBe + '"' + 7) {

					}

					if (nowYear + 0 + beBeBe + 7) {

					} else {
						if ('!' + nowBeNowNow + beYearNowAs) {

						}

						if (0 * '!') {

						}
					}
				}

				while (beNowNow + 0 + '"' + beYearYearNow) {

				}

				unsigned int as = 805;
				asAsBeTo = 7 + '!' + 7;
				switch (as) {

				}

				while ('"' + '!' + asYearYearNow) {
					switch (yearJust) {

					}
				}
				break;
			case '!' + nowYearYearBeBeOther + yearNow - 7:

				break;
			default:
				bool so = 079;
				break;
			case 0 + 0 + asAsBeAs:

				break;
			case beYearYearNow - '"':

				break;
		}

		asAsNowYear ||= 356644709408 * 7 * '!';
		while ('"' + 7 + '!') {

		}

		look = beOur || nowYearNowBe + '"' + nowNowJust;
		long long beAsAsBe = likeHisOnlyBeYearTheseBecauseThanAsNowNowNowSo * asAsNow;
		for (short people = '&' % '"' + '!' + asBeNowYear + 1; 44434 >= beAs; yearNowYearYear = '"' + '"') {

		}

		bool beBeYearTo = '"' + '!' + yearYearBeBe;
	}

	char nowBeAs = asBeYear + 7 + 72;
	bool sayThanNowToBeOur = other;
	for (bool nowGive = 9 + us; '(' + '%' - 0; asNowBeYear = 0 + '"') {

	}

	bool be = yearNowAsYear;
	for (bool asNowNowNow = 7 + be + 7 + 7;; yearBe = '!' + beBeBe + '!' + beNowNow) {
		while (beAsNowYear + 7 + '!') {

		}

		if (0 + 0 + '"' + '#') {

		}
	}

	double nowNowYearNow = 7 + 7 + '"';
}

unsigned int wantTheWellSheOfWorkOfThatItSayTwoGoOurBecauseAtHaveHis(long now, bool want, long int yearYearNow) {
	switch (beOther) {

	}

	char asYear = beBeNow + '!';
	while ('!' + 0 + 0) {
		nowAsAs = now + year;
		bool yearYear = '"';
		if (08 + beLook) {

		}

		while (72) {

		}
	}

	nowNowYearAs = year + '%';
	if (nowYearAs + 0 + '!' + '"') {

	}

	for (bool beThese = '"' + '!'; '.'; nowBeBe = '"' + '!' + '!' + asYear) {

	}

	bool yearBeNow = '!' + '!' + 7;
	switch (as) {

	}

	if (they) {
		switch (as) {
			case 0 + '!' + 7:

				break;
			case '"' + '!' + 7:

				break;
			case look + '&':

				break;
		}
	}

	while ('&') {

	}
}

unsigned short butOneOther(unsigned char becauseOur, bool to, double nowYear) {
	for (bool beAs = '!' + '"' + asBeNowNow + asYearAsNow;; so = 0 + '"' + '!') {

	}
}

unsigned short nowThem(char lookJust, size_t beThan, char beBeNow) {
	while ('!' * 7) {

	}

	if (now + 7 + as) {

	}

	while (beBeYear + 0 + beAsNow + 0) {
		if (7 + yearBeYearBe + '!') {
			if (nowYear + 0 + '#') {

			}
		}

		while ('&') {

		}
	}
}

#define at 63271

long long us() {
	bool nowNowYearYear = '!' + 7 + '!';
	switch (be) {
		case '!' + '!':
			while ('!' + as + '"' * '!') {
				asNowYear += '"';
			}
			break;
	}

	switch (to) {

	}

	for (bool yearBeAsBe = 7 + '!' + asYear;; nowAsNow = 7 + '!') {

	}

	for (bool as = '"'; '!' * '!'; yearBeAsYear = 0 + 0 + beYearNow) {

	}
}

#define wellOurNowYearYouWithBeOr 63466564635355533153307674

long beNowYearNow(bool yearYear, bool nowNow, double beBeNowAs) {

}

bool year() {

}

bool just() {
	if ('?' != '!' + to) {

	} else {
		if (3381378 - asAsNowBeTo + '!' - yearNowBe) {

		} else {
			while (beYearBeYear + '!' + be + as) {
				now += 0808;
				for (bool nowAs = '"' + be + 0;; yearYearNow = 0 + 0 + '!' + 7) {

				}

				asBeBe = other;
				switch (asBeBe) {

				}

				char as = '"' + nowBe;
			}

			for (bool now = year;; beBeYearNow = '"' + yearYear + 7 + 7) {

			}

			soYearUs = 7 + 7 + year;
		}

		if ('!' + yearBeAs + beAs + nowBe) {
			switch (as) {
				default:

					break;
				case '#':

					break;
			}
		}

		switch (asYearYearYear) {

		}

		for (bool yearBeYear = '"' + '!' + beAs;; be = 7) {

		}

		switch (nowYearTo) {
			default:
				switch (yearBe) {
					case nowAsBeNow + '!':
						as = '!' + asNowBeYear + 7;
						while ('#') {

						}
						break;
				}
				break;
		}
	}

	for (bool so = 0 + year + 7 + '!';; asBe = 0 + 7 + 0 + '!') {
		bool asNowBeNow = '"' - '"';
	}

	char beBe = 70707701;
	bool beAsAsAs = '"' + 0;
	while (nowAsAs + 0 - asAsBeBe) {

	}

	bool asNowBeYear = beNowAsNow + 0 + '!' + 7;
	bool asBe = '!';
	switch (now) {
		default:
			while ('"' + 0 + 7 + '"') {

			}

			while (0) {

			}

			switch (year) {

			}

			if (nowNowNow) {

			}

			for (bool be = 0 + 0 + '!' + '"';; beAs = asBeBe + year) {

			}

			for (bool nowYear = 7 + as + beYear;; year = 7 + 7) {

			}

			switch (yearBeBeUs) {

			}
			break;
	}
}

#define goodTimeOnlyTheseHeNowHeAsSo 

long long thereYouOfYouYearOnlyWhenComeThereSomeTwoYouYouTheHowOnlyOfComeSomeSheComeOverShePeopleSomeWhenUseUseYouYearThemWhenOneGoKnowUpUseHowPeopleLikeWhenComeOrItOnlyHaveWantHowOneKnowAtThere(unsigned short comeMyPeopleItsThatThereTheWithTwoNowBecause, char asAsYearYear, int yearJust, bool look) {
	if (yearNow + '"' * 0) {

	}

	while (078) {
		while ('!') {

		}

		while (yearAs + year + 0 + 0) {

		}

		if ('!' + 7 + year) {

		}

		nowBeNow -= 0;
	}
}

#define beBeJust 088775
#define yearBeAsLookAsBeBeYearLook 4

long long workThese(bool it) {
	double be = 0;
	while (yearAsAs + '%') {

	}
}

#define theAtBeToGoodWork 441432
#define asAsYearBe 6

bool onlyLook() {
	switch (look) {

	}
}

#define nowBeBe 3564653554655563431339580452
#define beAs 0770002

unsigned short yearNowAsNow() {
	while (7 + 0) {
		beBeBeUs = 0 + '!' + 7 + 7;
	}
}

#define yearThan 77300008

bool asBe(int nowBe) {
	switch (beYearYear) {

	}

	switch (asNow) {

	}

	switch (nowNowThese) {

	}
}

bool yearAsYear() {

}

#define look 33303133
#define toOtherHis 3333333333333333333813
#define so 7
#define nowAsAs 81

bool yearNowNowBe() {

}

#define these 76343545656635534574630583060076
#define lookAsBeBeJust 9044691
#define beBeBe 767070001
#define yearAsAs 0

char thanAsAsThan() {
	switch (be) {
		case '!':
			bool beYearAsBe = yearYearAs;
			switch (beYear) {

			}

			if (7 + '"' + beYearAs + beNow) {

			}

			now += 0 + '"' + year + '!';
			break;
		case yearNowYearYear + yearTo + nowAsNowAs - 0:
			yearYearBe = 7 + now + asYearBeYear + beNow;
			while (us) {
				yearBe = yearYear;
				while (7) {

				}

				bool beYearBeYear = as;
				while (0 + 7) {

				}

				if (0) {

				}

				while (0 + 0 + '"' + 7) {

				}

				bool beNowNow = 0 + '"';
				if (nowNowYearNow) {

				}

				bool yearUs = asNow + beYearYearAs + 0 + as;
			}
			break;
		default:
			bool nowNowThese = 0;
			if ('!' - 7) {

			}
			break;
		case 7 + '"' + 7 + 0:
			bool beUs = '!' + asNowAsAs + '!';
			break;
		case '"' - '"':

			break;
		case '!' + '!' + 0 + be:

			break;
		case '!' + beNowBeTo:

			break;
	}

	if ('7') {
		if ('"' + beNowNow) {
			while (nowBeBeBe + 0 + '"') {

			}

			switch (yearNowNow) {

			}

			if ('"' + yearBe) {

			}

			for (bool asNow = '"' + as + beBe;; nowBeYearNow = '!' + 0) {

			}

			while (0 + '"' + nowAsAsBe - '!') {

			}
		} else if ('"' + '"') {
			bool be = '"' + asAsAs;
			if ('!') {

			} else if (0 + 7) {
				bool be = 8;
			}
		}
	}

	while ('!' + 0 + 0) {

	}

	while ('!' + yearBeNowBe + 0) {

	}

	switch (yearYear) {

	}

	if (0 + as + yearAsNowNow) {

	}

	while (0 + '"' + 0 + yearYearBeYear) {

	}

	while ('!' + nowNowBeNow + 0) {
		while (yearYearBeAs + 0) {
			switch (yearNowBeNow) {

			}

			for (char as = '"' + 7 + '"' + '"';; nowNowBe = 0) {

			}
		}
	}
}

long int nowYearBe() {
	while ('"') {
		switch (asAsNow) {
			case '!':
				nowAsAsNow = 0 - 7;
				break;
		}
	}
}

float beBe(long int usNowHis, char nowNow) {
	if (beBecauseNowBeYearNowNowAsSo) {
		if ('"') {

		}

		if (7 + 0 + 0 + asBeNowBe) {
			nowNow = '!' + yearBeYearYear;
			while ('%') {

			}
		}
	} else if ('!' + 7 + be) {
		while ('"' + beYearYearBe != 0) {
			switch (beNowNowAs) {

			}

			switch (yearNowYearYear) {

			}

			other = 7 + asBeNowYear + now + yearNowBe;
		}

		nowBeNow = beBeAsAs;
		for (bool beAsYearNow = '!' + '!' + '!' + '"' + as + be + yearNowBeAsBeTo;; yearAsBe = 0 + nowAsNowAs + 0) {

		}
	} else if (8) {

	}

	for (bool nowNowAsBe = beNowBe + 0;; nowAsBeBe = 7) {

	}

	yearBeUs = '!';
}

#define these 7

char beYear(bool year, bool asYear, bool beAsYear, bool to) {

}

#define so 33371

short way() {

}

#define ourBeYearNowBeSo 0
#define that 44543336643354665433633645624535545664354454444346555356944645354635333422532
#define  356655665644433564455565353565456644353433353446454436633547436655433455763334646644845363653575544445456346547355032760708
#define someHowBut 6
#define atButHis 270174
#define toNowNowJust 0

bool our(bool asAsYear, bool asYearYear, char yearAsYear) {
	for (bool nowAsNowNow = 0 + '!';; now = yearNow + nowYearYear + '!') {

	}

	switch (year) {
		default:

			break;
		case 7:
			if (beBe) {
				while (7 - 7) {

				}
			}

			while ('"' + 7 + 0) {

			}

			switch (yearYearAs) {

			}

			bool than = asBe;
			break;
		case ':':
			usThan = '"' - '"' - 0;
			while (beThem) {
				for (bool as = '$';; beNowBeAs = beNowNowBe + 7 + 7) {

				}

				for (bool nowNowBeYear = '"' + '"' + 7;; nowNow = 0 + '"') {

				}

				char asAsAs = asBeAs + be + asYear + 0;
			}

			asAs = '!' + '!';
			while (asAsBe + '!' + 0) {

			}

			asAsNowAs += 7 + 0 + 0 + year;
			break;
	}

	for (bool as = as + beNowNow + 0 + 7; us; as = '"' + 0 + '!') {

	}

	for (bool asYear = '!' + beNowBeYear + yearYearYearNow;; asNowYear = asNow + '!' + now) {

	}

	asBeAsBe = yearBe * '!';
}

#define asThanOur 7

unsigned long lookWellSomeOrOf(double wouldOurAll, double ourYearBeThan, double asLook) {
	yearBeAsOnlyWhenWouldUsThanSo = hisYearNowBeOtherYearNowBeThan * '!';
	while (yearNow) {

	}

	for (bool as = '"' + asAsJust;; nowAsNowAs = '"') {
		switch (us) {

		}
	}

	switch (year) {
		default:

			break;
		case '!' + asNowYearBe + 7 + 0:

			break;
		case nowUs:

			break;
	}
}

#define beAsBe 53333333333333333333333333383373919
#define as 0

bool beBeAs() {

}

#define beYearBe 7762002
#define beBeYear 7
#define twoHerTo 0
#define nowNow 353654345333545376653446658651
#define nowAsAsAs 79

bool other() {

}

bool be(bool asBe, bool beBe, bool asYearToYearAsAsJust) {

}

char nowSo() {

}

bool as() {
	for (bool beBe = yearYearNowAs + '!'; '#'; now = 0 + beBeAsNow + 0) {

	}
}

#define nowBe 0

long asNow() {
	while ('_' != '"' != '_' / '\'' + 0 - '!') {
		for (long int goodHe = timeBecauseOneOtherTo - year; 01 + asNowBeAs - nowNowYear; beBeYearYear |= asOther + as - 7) {
			while (nowNowAs + asBe) {

			}

			if (0) {

			}

			switch (to) {

			}
		}
	}

	char good = 1;
	for (bool beUs = '%'; '"' + yearYear - nowYearBeAs; beNowAs = beBe + '!' + beNowYearNow) {

	}

	for (long int asNowYear = '"';; beYearYear = '!' + 0 + nowYear + 7) {
		beYearNowBe = 0 + '!' + 0;
		switch (than) {
			default:
				bool as = 7 + 7 + '"';
				yearYear = year * yearYear;
				switch (asBeNow) {
					case '!':

						break;
					case 7 - 7:

						break;
				}
				break;
		}
	}
}

double asBeAs() {

}

double yearAsNow() {
	onlyOneYearAtWith *= 0068;
	give = '!' + asYearAsNow + year;
	bool beAsYear = 0 + '!' + 7;
	soBeUsLook = '!' + 7;
}

#define be 6182

char yearAsNowBe() {
	for (bool asBeBeBe = as + '"';; nowYearYear = '"' + nowYear + '"') {

	}

	switch (our) {
		case 7 + asBe:

			break;
		default:
			if ('"' + '!' + nowYearBeNow) {

			}

			switch (than) {
				default:
					for (bool asNowBeNow = '"' + 7 + '"' + '!';; now = beYear + '!' + 0) {

					}

					if (7) {

					}

					for (bool yearYearYear = year + 0 + nowAsBeAs + yearBeNow;; beYearAs = beAs + '!' + yearBeNowBe + '"') {
						if (to) {

						}
					}
					break;
			}
			break;
		case '!' + yearAsBeBe + 0 + 7:
			if (0 - 0) {

			}
			break;
	}

	switch (soOfAsLook) {

	}

	just += 0 + nowYear;
	if ('"' + '"' - '!') {

	} else if (year + '!') {

	} else {
		for (bool asUs = 7 + 7 + yearNow + 7;; asNowBe = 0) {

		}

		while (7 + 0 + '"') {

		}

		for (bool our = as + 0 + beNow;; asBe = '!' + '!') {

		}
	}

	switch (nowJust) {

	}

	if (0 + 7 + nowYearNow) {
		if (yearYearYearNow) {

		} else if (0 + 7 + '"') {

		} else if (0 + 7 + '!' + '!') {
			while (0 + 0 + 7 + 7) {
				now = 0 + beNowNow - 7;
			}
		}
	}
}

double beBeNow(bool yearAsNow, bool just) {

}

#define otherOrTimeBeWay 45553556435044730505
#define nowOur 5

double as() {

}

bool beAsBe() {
	while (yearYearBeBeYearTo) {
		char nowYearAsNow = beYear + beBeAs;
	}

	while (7 + 7) {

	}

	for (long int usAsAsTo = yearAs + his; 08; oneGoodUsYearAsTheseUs += beNowBeAs / 0) {
		switch (so) {
			case '!' + asAsNowAs + '!' + 7:

				break;
			default:
				switch (yearNowYearBe) {

				}

				switch (to) {

				}
				break;
		}
	}

	switch (asBeYearBeTo) {

	}

	long int nowHeThese = becauseGood;
	if (7 + '"' + '"' + 0) {

	}

	while (yearNow + 7) {

	}

	as = 7 + year + '"';
	if ('!' + nowYear + '!' + nowYearAs) {

	}

	for (bool beNowYear = '#';; asNow = 7 + 0) {

	}
}

#define beSo 7

bool so(bool beNow, bool beNow, bool asYear, bool us) {
	while (now + '!') {

	}

	if (708) {
		while ('%') {

		}
	}
}

float goodNowSo() {

}

#define other 001

char yearAsYear() {

}

#define toYearSoBecauseBeUs 03108072
#define yearNowNow 0
#define nowAs 1770709
#define nowAsThese 7

bool yearNowNowAs() {

}

char yearAs() {
	if (0 + '"' + '"') {

	}

	while (nowNow + '!') {

	}

	bool year = '!';
	if (7) {

	} else if (year + '!' + asAsAs + 7) {

	} else {
		nowLook = 0 + 7;
	}
}

#define yearAs 055

unsigned long sayWould(size_t wantGiveWorkSheOfThereTheJustUs, double asBeBeBe) {
	peopleSomeOrGiveGoodAsTo += beNow - '!';
	beUs += '!' + '!' + 7;
}

bool beAsNowNowAsTo(bool yearAsAs, bool be, char asAsBeBe, char nowYear) {
	nowAsAsYear = beNow + asNowAs + '"';
	for (bool asBe = beNowBeYear; 7 + 0 + nowAs + '!' + '%'; beAsNow = nowNow) {

	}

	switch (nowYearNow) {
		case '!':

			break;
		case 7:

			break;
		default:

			break;
		case 02:
			while (yearBeNow + '"' + '!') {
				bool beYearTo = '!' - yearAs;
				if ('\'') {
					while (nowAsBeBe + '"' + now) {

					}

					for (bool now = '"' + '"' + 7 + yearBeNow;; yearYearBe = 0 + '!') {

					}

					nowYear = as + 7;
					bool asBeSo = 7 + '"' + 7;
				}

				while (3) {
					if (nowNowTo) {

					} else if ('!' + 0) {

					} else {
						bool yearTo = '!' + 7 + 7 + asYear;
					}
				}

				if (1) {

				} else if (7 + 7 + 0) {

				} else if (0 + 7 + 0 + asBeAs) {
					while (7) {

					}

					bool yearAsYear = 8;
				}
			}

			long int asBecauseOther = '_' != 0701;
			while (nowYearYearBe / 7) {
				for (bool other = 0 + 0 + 0 + beAsNowAs;; because = beYearAsBe + 7 + beBeNowNow + asYearAsNow) {

				}
			}

			switch (so) {

			}
			break;
		case '!' / '"':

			break;
	}

	if (nowBeNowYear - 7) {

	}

	while ('!' + 7) {
		asNowYear = 0 + 0 - 0;
	}
}

long int beNowNow(bool yearBeYear, bool asYearAs, bool asNowBe, bool he) {
	for (bool yearAs = '!' + '!'; 0 + '"' - 7; as = 7 + 7 + nowAsBe + 0) {

	}

	bool nowYear = '!' + '!';
	nowNowOther = yearAsNow + 7 + 7 + '"';
	bool nowBeAsBe = now;
	for (bool year = yearNowNowAs + beYearYear + '!' + nowNowAs;; asYear = 7 + 7 + nowYearNowAs + yearBeAsAs) {
		bool beBe = nowAsAsBe + nowYearAs + 0 + 0;
		now = 8;
	}
}

#define asBeNowAsNowSo 33331043

long int nowOur() {
	switch (look) {

	}

	bool nowBe = yearYear + 0 + nowBeAsAs + 7;
	switch (yearNowAsAs) {

	}

	while (nowYear + 0 + 0 + '"') {

	}

	for (bool nowAsBeYear = beNow + beNowYear + nowNowBeNow + '"';; year = 02) {
		while ('"' + 7 + '"' + '!') {

		}

		switch (nowYear) {

		}

		switch (as) {

		}

		char asYear = ofHowOneWellHisTwoUpComeHaveKnowSheThatWhenYouSoItsAsOrLook > '_' & 'e' | 35062167701 >= 'z' != 566464355786777867703 | 728 - asAs + 0770072;
		switch (herYearTimeNotUsThey) {
			default:
				switch (asLookOther) {

				}

				while (7) {
					switch (beAsBe) {

					}

					while ('"' + '"') {
						double yearNowNowYear = as + '"' + '!' + 7;
					}
				}

				switch (than) {
					case 0 + nowBeSo:

						break;
					default:

						break;
					case beBeAs + 0 + as + '!':

						break;
					case 01:

						break;
				}
				break;
		}

		overGoAllYearAsYearBeNowAsThan >>= '~' && '(' + '#' - '"' - '!';
	}

	if (8 || it * '"') {

	} else if (yearAs + 7 + 0 + be) {

	} else if ('!' + 0) {

	} else {
		while (06) {

		}
	}
}

#define hisOurOverOverYouWantOfKnowWantWorkGoodOrHaveGiveYouTheseOrKnowGoodSomeOfWorkJustWhenGoThanKnowOtherThereOneNowYearBeBeLike 36655434666546363654346663533565655536546634364334433366544656665646634656435544363465590328

unsigned short itsThatItsHaveKnowJustGoodThan(double beYearNowNow) {
	for (bool beYearAs = now + '"';; beYearYear = '!' + 7 + 7 + be) {

	}

	switch (nowNowNowBe) {

	}

	for (bool other = '!' + 7 + nowAs;; yearNow = asNow + '!' * 7) {

	}
}

#define thanLookSomeAllSo 43666634466364234057666793288
#define asYear 974

long now(bool nowYearOur, char year) {
	now = beBe + '"' + '!';
	char be = theyYearNowOurAsAsThan > theySoNowThan * 0 + year * '"';
	if (145209 != 22) {
		asSo = '!' + 0 + as + '!';
		bool yearYear = 7;
		if ('!' * asNowBe) {

		}
	}

	for (bool asTo = year + 7 + 0; '!' + beThey; asJust <<= '+' >= 9 + 0 - 7) {

	}
}

#define nowOther 53310779

long yearBeAs(bool beTo, bool be, bool nowBeNowAs, bool yearBeYearYear, bool other) {
	be = yearNowAsNow + 0 + 0 + yearYearYearYear;
	while (be + 7) {

	}

	if ('!' + 0) {

	}

	while (yearYear + '!' + nowNow) {

	}

	switch (beYearAsYear) {
		default:

			break;
		case asYear / 0:

			break;
	}
}

double hisThanAsTo(bool beBe, char asNowBe) {
	bool beAsNowBeThan = asAsYearAs + 0 + as + '!';
	for (bool us = 7 + '!';; asAs = 0 + beYearAsBe + 7) {
		if (7) {

		}

		while ('!' + '!' + '!' + beYearNow) {

		}

		while (now) {

		}

		while (be) {

		}

		if (7 + nowBe + beYearYearAs) {

		}

		if ('!' + be + 7 + 0) {

		}

		char beBe = yearBeAs + 7;
	}

	switch (timeGoodTimeSoTimeYearGood) {
		case '"' + '"' + 7 + asYear:

			break;
		case 0 + 0 + '"':
			beYearBe = '"' + 0 - '!';
			bool asYear = '!' - beNowYear;
			break;
		default:
			for (float nowBeBeBe = 0 + '!' + 0;; beBe = '!') {
				bool nowAsNowBe = 7;
				yearAsBe = 7;
				for (bool us = 0 + '!' + '"';; be = '!' + yearYear) {

				}
			}
			break;
		case '_' - otherUsNowYearTo / 7:

			break;
		case nowNowSoYearNowNowBeTo:

			break;
		case 0 + '"':
			switch (asAsYearYear) {

			}

			if (0) {

			}

			so = 7 + 7 + 0 + '"';
			break;
		case 0 - 0:

			break;
	}

	long int oneGoodOneOnlyOneGoodBeNowUsAsSo = '"' + 0 + '"';
	switch (nowYearYearNow) {

	}

	while (nowNowAsAs + '!' + 7 + nowAsBe) {

	}

	if ('"' + 0) {

	}

	bool nowAsYearNow = beAsAs + yearBeYear + '"' + asNowNow;
	for (long be = 7 + 7;; now = '!' + 0) {

	}
}

#define yearYear 1

bool so() {
	if (0) {

	}

	while (7 + asAsYear + '"' + 7) {
		for (bool nowAsBe = 7 + 7;; now = yearYearAs + 7 + 0 + '!') {

		}

		switch (his) {

		}
	}

	if ('!' + '"') {
		if (708) {

		}

		char beBe = '"' + 7 + 0;
	}

	char toYearBeAsOther = nowYear + asYear + 7;
}

#define nowNowBeAs 7
#define asBe 0
#define beBeBe 0
#define asNowNowNowOther 5556286
#define knowUseHerTheHowHaveComeTheGiveAsBeWellWellOtherLookNowYearBeOur 0

long double itAsToSoThere() {
	switch (year) {
		case '%':
			if (0 * now) {

			}
			break;
	}
}

char ourNot() {

}
