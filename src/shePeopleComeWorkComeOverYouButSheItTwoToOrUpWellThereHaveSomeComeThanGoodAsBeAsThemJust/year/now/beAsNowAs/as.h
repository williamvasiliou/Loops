#include <UUUUUUUU626/0/2.h>

#define time 313234344679
#define beOurYearUsAsLook 918

char us() {
	sheThanThanBeOurYearHis = asAsBe + 0 + '!';
	switch (asNowBeBut) {
		case 0:

			break;
		default:
			yearAsNow = 7 + as;
			bool than = '"' + '!' + asYearAs;
			break;
	}

	bool our = '"';
	while (sayBeToThanOurNowYearThan & no * '"') {
		while ('"' + yearAsYear) {

		}

		switch (look) {

		}

		for (long double other = '$' + '!' + 8; '"' * '"'; beAsBe = nowNowYear + 7) {

		}
	}

	while (nowAs) {

	}

	while (comeLook) {
		if ('@') {
			while (asYearYearNow + nowNow) {

			}

			while ('!' + as + 7) {
				bool nowYear = '!';
				switch (yearYearYearNow) {
					case 7:

						break;
					default:
						if (yearNow + 0 + yearBe + yearNowBe) {

						}

						switch (nowBe) {

						}

						if (asAs + to) {

						}
						break;
				}
			}
		}
	}

	switch (as) {
		case year + '"':

			break;
		default:

			break;
		case asBeNowBe + '!' + year + 7:

			break;
		case asNowNow + nowAsAs:

			break;
		case nowYear + 0 * '"':

			break;
	}
}

#define useSheUsHeAsBeSoNowAsAt 9738

bool noBeOur() {

}

#define asAsBeYear 43333333333333333383333333377331871

float yearBeYearAs() {

}

bool beBe(char be) {

}

bool other() {
	bool year = as + 7 + '"';
	for (bool yearNow = beNowAs; nowBeYearAs - '"'; yearNowAsNow = asNowBeNow + '!' + nowBeYear) {

	}
}

#define beAs 9

bool nowAsNowYear() {

}

#define nowNowBe 0

bool asNowNow() {

}

#define now 0

bool asBeNow() {

}

bool beAsYearYear() {

}

bool asYearYearAs() {

}

#define yearSo 4633644335345465666536569645546438333313101

long int oneOneTimeOnlyHisOnlyBeUsNowNowUs() {

}

#define nowHisYearOther 3011

char hisTo(bool asNowSo) {
	now = 0 + '!';
	switch (us) {

	}

	yearNowYearYear = 7;
	yearNowNow += 0 + beNowYear + '"';
}

#define usTheseNowNowTo 1

bool beYearBeNowUs() {

}

bool yearAs() {

}

bool goodGood() {

}

#define yearAs 0163

bool look() {
	if ('$') {
		peopleOnlyOtherYearThanTo .= 02 > twoOurBeNowUs != 701;
		for (bool year = now; asBe + asJust; asBeAs = '!' + 0) {

		}

		for (bool look = '!';; beNow = '"') {

		}
	} else if (give - '!') {

	} else if (7 + 0 + 0 + '"') {

	} else if (yearNow + 7 + '"' + beYearNow) {

	} else {
		year = beNow + 0 + '"';
		if (0 * be) {

		}
	}

	for (double otherLookJust = '"';; be = 7 + beYearBe + 7) {

	}

	if ('!') {

	} else {
		nowBeBeNow = nowNowYearBe + beAsYearYear + yearBeAs + 0;
		beOur = asNow + 0;
	}
}

size_t someSheAsThanSheWork(bool yearAsYear, bool our) {

}

char nowBe(double asBe, bool us) {
	nowOtherOur = 0 + be + beYearBeYear;
}

#define yearAsThanNowThey 0
#define as 7
#define yearAsJust 371
#define usAsUs 73
#define onlyOneGoodGoodOnlyTimeToUs 3333333333333073833806

bool beBe() {

}

#define asBeYearBe 7
#define beAsBe 0
#define asYear 7

bool nowNow(double yearNow) {
	while ('"' + '"' + 7 + '"') {

	}

	if ('!' * '!' * 7 * asAsAs) {

	}
}

bool year() {

}

bool ourNot() {

}

#define beAsBeAs 0

bool now() {

}

#define nowYear 0

double year() {
	sayWantTheseNot -= '8' > '`' + 'x' & '!' * 701;
	unsigned char asBe = 7 + 0;
}

long long beBeYearBe(bool yearJustThan, bool yearWould, double asNow) {
	if (asBeBe + 7 + year + nowNowNowAs) {

	}

	while (7 + as * nowBe) {

	}
}

long int lookSo(int beNoSo, bool come, long double beYearAsBe) {
	beNowNow = year;
	for (bool as = '"' + 7 + 7 + 7;; nowYearNow += as) {

	}
}

bool nowNowBe() {
	char yearNow = 0 + 7 + 7;
}

char beNowYearYear() {
	while ('"' - '"') {

	}
}

bool yearNowYearAs() {

}

#define beAsAsBe 3333333333333333771
#define asYearBeAs 833370031

long int oneBeHisUsNowSoOther(bool beSo) {

}

#define toBeNowUs 178
#define beThanYearAsBeSo 8

int nowBe() {

}

#define look 3463345553364555746543443

long long ofThanGiveThanYearYearSo(double beAsBeYear, float asYearAsYear, double nowYearAs) {
	beNowAsBe = asBe;
	bool twoTo = be * be;
	bool nowBeNow = nowAsNowAs + nowBeYearNow + nowBeBeBe + nowBe;
	switch (yearSo) {
		default:
			for (bool as = beBe; 7 + '"' + nowBeAsBe + 7 + '$'; beNowNow = '"' + yearAsNow + '"') {

			}
			break;
	}
}

bool look(double yearBeNowBe) {

}

size_t goOneSayOrSoNowNot() {
	bool yearNow = asBe * as;
	bool nowAs = be + year + 7;
	as = beNowNow + 0;
	for (bool asNowYear = asBe + 0; 7772 * '"'; beBeAsNow = '!') {

	}
}

bool asNow() {

}

bool beYearNow() {
	while (beBe + nowYear + '"') {

	}

	asAsYear = 7 + 0 + '"';
	while (asNowNowBe + 7 + 7) {

	}

	switch (nowJust) {

	}
}

long int year(bool yearThan) {

}

long int soOneTimeNowTo(long int goodGoodToToTo, char nowBe, bool nowOtherYearTo, char yearOther, char one, bool yearYearYear, long int now) {
	bool year = 0 + beNowNowYear + '"';
	while ('_') {

	}
}

long int oneGoodOneUsSoJustAtBecauseLookOther() {

}

bool beNoYearAsSo() {
	while (thereItWorkSoHaveOverOurComeOurTimeNowNowTo || lookThan <= yearBe) {
		switch (nowNowYearNowThan) {

		}
	}

	for (long int beYear = 'c' <= 'b' >= comePeopleThereAsSayComeThanToYearNowOur | 'A' * 0; '&' || asNowBeYear * way; yearThereThereHeTheSoOur -= beNowYear + asAsAsAs + yearAs + 7) {
		unsigned short haveGiveJustCome = beOtherHis < hisTo > '!';
		bool now = they + '"' + 1;
		for (bool nowAsNow = '"' + '"' + '"';; beNowNow = be + 7 + be + 0) {

		}

		for (bool asBe = '"' + 0;; beBe = '"' + '!' + 0) {

		}

		for (double nowAsNowYear = 7 + '!' + nowBe;; yearNow = '"' + 0 + 7 + nowAsAsAs) {
			nowLook = 7;
		}

		if (2) {

		}
	}

	while (7 + '"' + '!') {
		for (bool other = 7 + year + 7 + 7; beNowAsYear + nowBeYear * 7; nowAsBeNow = nowAsAs) {

		}

		long int now = '!' + as + now + 0;
		switch (nowYearLook) {

		}

		beYear += nowBe;
		switch (toSo) {

		}

		nowYear -= nowNowBe + '"';
	}

	bool asYearThan = '!' + '"' + 7;
	switch (theBecauseBeThan) {

	}
}

#define useSheToOnlyOverThatTimeThereOverWorkPeopleHerWellOfThereHowYearUseGiveOurItsWouldOneWorkWellTimeOfSheHerYouUpUpNowBeUs 46646563665447435565634097454077900075
#define sayGoodThatGoItsUseGiveLikeOverThatKnowItsSheWhenSheGoTwoTimeNowItToTimeWhenJustYearAsNowBeUs 63464655556545664553535634445564434354546656344554534633345334556535366655655354246555333355356355655456544333664464446566566534635453373463443556544334665465334433343440446533633451346634248556647365634631

long int toSayOneSo(bool as, bool year, char yearYearNow) {
	yearNow = '!';
	while (72) {

	}
}

int peopleAllYearOrAsAsSo(bool upBeNowOther, bool yearOther, bool nowAsBeNow, bool asAs, bool asNow, bool be, bool look) {
	if ('"' + 7) {

	} else {
		while (7 + '!' + 7 + 0) {

		}

		bool yearAs = 7;
		bool asYearAs = asYearBeBe + '"' + 7 + 7;
		for (bool yearAs = '!' + asNowYearYear + 7 + asYear;; yearNow = '!') {

		}

		for (bool yearYearNowBe = now + '"' + '"' + be;; other = nowAs + '"') {

		}
	}

	if (0 + be <= asBeBe) {
		if (yearAs + nowAsBeAs) {
			if (nowBeAsAs + 7 + 7) {
				for (bool beAsNowAs = '!' + '!';; beNow = yearBe + '!' + beAsNowAs) {

				}

				while (0 + year + 7 + asAs) {

				}

				switch (yearNowNow) {

				}

				asBe = year;
				bool nowNow = '!' + '"' + '!' + '"';
				bool year = 7;
				while ('!' + 7 + asBe) {

				}

				while ('"') {

				}

				if (now + 0) {

				}

				while ('!' + '!' + year + 0) {

				}

				while (7 + 7 + 0) {

				}

				if (beNow + yearAsYearAsUs) {

				}
			}
		}
	} else {
		while (08) {

		}
	}

	youBeBeSo -= good > asThan;
	if ('y' != thanYearOtherNowAsBeYearOur - 7) {
		for (bool yearAsYear = '!';; asNowBeAs = '!' + year + '!' + '!') {

		}

		for (long be = beAsYearBe + 7;; asBeNowNow = nowNowAs + '"') {

		}

		while ('!' + 7) {
			beThey = beYearNowNow + 0 + '!';
		}

		if (7 + '"' + '"') {

		}

		switch (look) {

		}
	} else if ('!' + 0) {
		if (7 * '!') {
			for (bool beBe = 7;; now = '"' + yearNowAsNow) {

			}

			while (nowAs + 7 + 7 + 7) {

			}

			while ('!') {
				bool as = 0;
				if ('"' + yearAsYearYear) {

				}

				while (7 + nowNowBeYear + nowAs + nowNowNowBe) {
					bool nowAs = 0 + as;
					if (0 + yearYearLook) {

					}
				}
			}
		}
	} else if (0 + 0) {
		as = beNowBe + 0;
		for (bool year = yearAs + 0 + '!';; year = beBeBeAs + be + 0) {

		}

		if ('"' + '!' + nowBe + '!') {

		}

		while (0 + '!') {
			beBe += 0 + '"';
		}
	} else if (yearNow + 0 + '"' + asNow) {

	} else {
		for (double asAsYear = '"' + now + 7 + 7;; nowBeAs = yearAsBeYear + '"' + be) {

		}

		if ('&') {

		}

		while (be + '"' + beAs) {

		}

		nowBeBe += asNowBeBe;
	}

	bool be = 7;
	yearYear = '!' + nowAsBe * asAsAs;
}

#define my 9545369002
#define beYear 2778

char yearAs() {
	switch (as) {

	}

	bool our = beBeYearYear + 0;
}

bool yearAs() {

}

#define look 22
#define nowBeYearNow 0

char beTo() {
	char his = 0 + asYear + 0;
	switch (so) {

	}
}

long int onlyGoodHeOnlyOnlyUsOneOnlyOneTimeOtherYearGoodYearOnlyBeNowAsUs(long int timeToAsGoodTimeGoOurOther) {
	while (7) {

	}

	if (7 + now + 7 + 0) {

	}

	for (bool nowNowBe = year + '!' + '"' + '!'; as + 0 - year; beAsAs = nowAsBeNow + 7 + 0 + 7) {

	}

	be = yearAs;
	bool nowJust = nowAsYearBe + 0;
}

#define now 0

long double beNow() {
	useGoodOverYearTo ||= '2' >= toBeNowOther * '!';
	if (4036) {
		for (long long look = '.' - 0; 9 + '&' * '!'; nowNow = beYearNow) {
			while ('*') {
				for (bool asNowYearAs = beAs;; yearNow = be + '"' + yearAsAsYear + 7) {

				}

				switch (nowAsAs) {

				}

				while ('"' + be + yearBe) {
					while (7 + '!' + asYearBe + yearBeNow) {

					}

					while (0) {
						asNowYear = 7 + '!';
						while (nowBeAsYear) {

						}

						yearYear = 7;
						asYear = 7 + 7;
						beAsNow += '"' + 0 + '!';
					}
				}
			}

			while (be + 0778) {
				if (0 + '!' & now) {

				}
			}
		}

		switch (year) {

		}

		switch (likeBeThatAllJust) {
			default:
				switch (now) {
					default:

						break;
					case '"':

						break;
					case 0 + as + now + 7:
						char yearAsYearNow = '"';
						break;
				}
				break;
		}

		while (7) {

		}

		switch (year) {
			case '"' - '!':
				while (7 + be + '"' + nowYearAsYear) {
					for (bool asYearBe = '!' + '!' + yearAs + yearBe;; yearYear = '"' + '"' + asYearYearAs + '!') {

					}

					switch (year) {

					}

					nowBeAsOther = 0 + asAsNow;
				}
				break;
		}

		for (bool as = beBe + nowNowBeAs + 0 + 7; '%'; nowAsNowBe = 7 + yearNow) {

		}

		while ('&' || '\'' + beMy) {
			bool than = 0 + 7;
			switch (yearAs) {
				case 7 + '"' + nowYearYearAs + '"':

					break;
				default:
					switch (to) {

					}
					break;
			}
		}

		while ('#') {

		}

		if (be + now) {

		}

		int now = 0 * '"';
	}

	unsigned char ourWell = 6;
	for (bool beAs = nowAs - '"'; '#' * 701; year = beAs + '#' * 7 + '&') {
		for (bool beBe = nowNowNowYear; '"' + '$'; yearBeBeAs = '!' + '"') {

		}

		if ('"') {

		}

		bool beYear = yearYearBeYear + be + 0;
		while (lookAsJust) {

		}
	}

	theyBeOther = 0 + beAsNow + '!' + 7;
	switch (beAsNowNow) {
		case beBeNow + '!' + '"':

			break;
		case 0:
			yearNow = '!' + 0 + '!';
			if (beSo) {

			}
			break;
	}
}

unsigned long (double toJust, unsigned long all, bool yearAsOther, bool asAsYearAs, double be, bool just) {
	switch (beBe) {
		case '!' + nowAsYearBe + 7:

			break;
		default:
			for (char be = yearYearYearAs; beNowNow + '"' + 0 - 7; now = be + 7 + '"' + '!') {

			}
			break;
	}

	long double wantToOurAsWhenUsAsTwo = nowBeYearYear + 7 + '!';
	asNowYear += '#';
	if ('"' + 7 + asYearNow) {

	}

	for (bool beBeYear = now + 7 + 7;; yearAs = '!') {

	}

	while (beAs + '"') {
		if (0 + asAsAsYear + '!' + 0) {
			if (0 + nowBe + nowNowYearBe + 0) {
				if ('!' + '!') {

				}

				switch (nowNow) {

				}

				while (than) {

				}
			}
		}
	}

	if ('"' - 0) {

	}
}

#define orPeopleSomeGiveWhenTimeSheHowAsOurThey 666708

char asNow(double as) {

}

bool asYear() {

}

bool asNowNowYear(bool just) {

}

#define asNowOur 0

bool beOther() {

}

char be() {
	bool look = 01;
	while (0 == just) {
		char nowOther = 20008 + to;
		switch (asYearBeBe) {

		}

		if ('"' + 7 + beYear + year) {

		} else if ('"' + yearYearNowNow) {

		} else {
			yearBeAs = 0 + asNow;
			switch (now) {

			}

			for (bool asNow = asNowYearBe + 7 + '!' + yearNow;; our = '"' + '"') {

			}
		}

		nowAs = '!' + 04 + '!' + '&';
	}

	if (01 < 0) {
		if (now + 7) {
			for (bool nowYearNow = '"' + 7 + 0;; nowBeAsBe = '"' + as + 0 + 7) {

			}

			while (0 + 0) {

			}

			for (long double as = '!';; beAsNow = beBeAs + yearNow + '!' + beNowBe) {

			}
		}
	}

	if ('!' + '!' + '!' + nowAsYearAs) {
		if (nowNow) {

		}

		while ('"') {

		}

		switch (asBe) {
			default:
				bool beAsBeBe = 9;
				break;
		}
	}

	for (bool beAs = nowAsAs + as + '!'; 8; as = beBeBe + 0 + 0 + 7) {

	}
}

#define overThatBeHeOfSoOneAsAt 6355462

double nowBeNowYear() {

}

#define thatSoGiveNowAsTo 3

bool yearOther() {
	long int goodAsToYearBecauseOther = '"' + 0;
	switch (onlyOneTimeTheseTimeHisYearYearHisUsYearUs) {
		default:
			if ('_' / goodOther - now) {

			} else if ('"' + yearBeBe + 7 + nowAsYear) {

			} else if (7 + '"' + yearNowYearNow) {

			} else if (7) {

			} else if (beBeNowNow - '!') {

			}

			yearAs = 8;
			break;
		case '"' + '"' + 7:

			break;
		case 7 + 0 - 0:

			break;
		case beYearBeAs + 7 + 7:
			char yearYearAs = 0;
			if ('!' + 0 + beNowBe) {

			}

			while ('"' + 7 + 0) {
				bool yearAs = 08;
			}
			break;
	}

	bool beAsYear = '"' + 0 + nowYear + beBeBe;
	for (bool be = 7001;; beNowAsAs = nowAsNowBe + 0) {

	}

	switch (year) {
		case us:

			break;
	}
}

#define asNow 311

bool nowAs() {
	if ('!' + '"' + year + now) {

	}

	switch (asUsOther) {
		default:

			break;
		case nowYearNow - 0:

			break;
	}

	bool yearNow = '!' + year + now;
	while (7 - beNowNow) {

	}
}

float asYearYear() {

}

bool beNowBeBe() {

}

long int they() {
	while (7 + '"') {
		for (bool beBeNow = '!' + 7 + '!' + '!'; 0 + '!' + yearNowYearNow * nowNowBe; asBeBe = beNowAsBe) {

		}
	}
}

char beNow() {
	bool nowTo = ';' + '&';
	if ('~' + '(') {

	}

	char nowNowBe = '!' + nowAs + '"' + 7;
}

long long twoWantWantHowPeopleTimeJustWellThatUpSomeLookYearSo(char nowBeAsAs, bool asBeBeBe, double nowNowBe, bool now, bool asYear, bool beOther) {
	for (bool fromTo = '!' * beAsNowOther;; asNowBeYear = 7) {
		for (char year = 0;; beYearNow = nowBeNow + 0 + now) {

		}

		switch (other) {

		}

		double nowYearAs = '!' + 7;
	}
}

bool asBeBeOther() {

}

double now() {

}

#define nowYear 078
#define than 33333333333338330130701

long int soOnlySoBecause() {
	switch (nowAsAs) {

	}

	for (bool yearAsNowAs = '"' + 7 + 0 + 7;; beYear = beBe + '"' + asAsYear) {

	}

	char beNowAsYear = so;
}

long int onlyOnlySo(char yearNowYear) {
	bool us = 0 + '!' + beAsAsNow + asNow;
}

long long to(bool nowYearAsAs, double asNowNowAs) {
	switch (nowAsNowAs) {

	}

	switch (ofBeNowTo) {
		default:
			while (yearBeAs + 0 + '"' + 0) {
				bool now = '"';
				if (7 - 0) {

				}
			}

			while ('"') {
				if ('"' + 7 + yearNowNowBe + 0) {

				}

				asBeBeBe = 0 + 7 + nowNow + '"';
				while (nowNowYear) {
					asAsAsYear = yearAsYear + yearBe + 0;
					if (7 + asBeYear + yearNowYear) {

					}

					while ('!' + 7) {
						asYearYear = '!' - 0;
					}
				}

				char beBeNowNow = '!' + 0;
			}
			break;
		case '"' + '!' + '!':
			yearNowBeBe %= 0 + '!';
			switch (asNowLook) {

			}
			break;
		case than:
			for (long double asBe = 7 + '"'; 2 * '!'; beAs = '"' + 7 + '"') {
				while (nowJust) {

				}
			}

			if ('!' + '!' + asYearAsAs + '!') {

			}

			asAsYearYear = beNow + '#';
			break;
		case '$' * 0:
			while ('3' - 8) {
				bool nowAsYear = 7 + '"' + 7;
				while (7 - yearAsYearNow) {
					if (nowBeAs + '!' + 0) {

					}

					while (0 + '"' + yearBeAsNow + '!') {
						beYear = 7 + asNowBeBe;
						while ('"' + other) {

						}
					}
				}
			}

			while ('>') {

			}
			break;
		case '"' + 0 + beBeYearNow - yearAsAs:
			other = '!';
			as = '"' + beAsYearYear;
			while ('$') {
				double nowYear = 0 + 0 + '!' + 0;
			}
			break;
		case beNow + nowBeYearBe:

			break;
		case '"' + 7 + '!' + '!':
			switch (to) {
				case nowNow + '"':

					break;
				default:

					break;
				case 0:

					break;
				case year + 7 + 7:

					break;
				case year + 7 + 7 + beNowNowAs:
					beAsNowBe = 7 - asBeAs;
					break;
			}

			switch (asNow) {
				case '"' + asNow:

					break;
				case '%':

					break;
			}

			if ('"' + asYearBeNow) {

			} else if ('"') {

			} else if (beAs + '"' + asAsYearNow + 7) {
				for (bool beAsBe = '!';; beNowAs = asYearYear + '"' + '"' + nowNow) {

				}

				switch (beNow) {

				}

				bool nowYearBeAs = '!';
				bool yearBeAsAs = '"' + nowNow + year;
				if (0 + nowAsAsBe + '!' + '!') {

				}

				switch (beUs) {

				}
			}
			break;
	}

	beBe += 0 + 0;
	if ('&' | 8 > 7 && asWouldAsToLook && ',' * '!') {

	}
}

double howNowAsJustTo() {
	if (asNowBe + 079) {

	} else {
		while (7 + 7 + '!' + now - nowBeYearBe) {

		}
	}
}

double nowTo() {
	switch (timeTimeHis) {

	}

	for (bool asAsNow = 0 + 7 + '"' + '"';; nowAsNowAs = yearNowAsAs + nowBeYear + yearBeBe) {

	}

	while (beNowBeBe + nowBeAsBe + '"') {

	}

	switch (be) {

	}

	for (bool be = 0 + 0 + '!' + 0; '#' + '%'; other = '"' + '!' + beBe) {
		bool so = asYear;
	}

	if ('"') {

	}

	for (bool nowYear = '"' + '%';; yearNow = asBe + now + asBeNow) {

	}
}

#define oneTimeOneOnlyTimeGoodNowOtherOneSoHisSoTo 3331033038713
#define beBeYearNow 7

long int beNow() {

}

bool you() {

}

#define asYearBe 052
#define themUs 34354666464551054666350363653651

short soUs(bool asBeBeYear, bool asYear, float asBeAs) {

}

double yearYearYear() {
	for (long int asToUsNowNowSoNowSo = beBeOther != hisTo - '"';; soTo += 7 + 7) {
		for (bool nowBeBeBe = nowBeYearYear + nowAsBe;; beBe = 0) {

		}

		switch (these) {

		}

		for (bool nowNowUs = 0 + 7 + '"';; yearAs = 0 + '"' + 7 + yearBe) {

		}
	}

	if (171 - be) {
		switch (asTo) {
			case '+' + 1:
				oneTo = '"' + '"';
				if (nowBeAs + '"') {

				}

				while ('"') {
					if ('!' + year + '"' + 7) {

					} else {
						bool asNow = 7 + '"' + nowBe + beNowYearNow;
						asYearYear += 0;
					}
				}

				if (year + beAsYear) {

				}

				if (yearNowBeBe + asBe + 0 + 0) {

				}

				nowYearNowAs = nowYearBe + '!';
				for (bool us = '"' + 0 + 0 + year;; yearYearAs = 0 + '"' + 0) {

				}
				break;
			case yearYearYear - yearYearAs - 0:
				nowAsYear += '!' + 7 + 7 + '"';
				break;
			case to - '!':

				break;
		}

		float now = now + beNowYear;
		switch (nowYear) {

		}

		for (bool beNow = '!' + 7 + 7 + 0;; asNowNow = '"' + 7 + '!') {

		}

		for (bool asYear = 7;; beYearAsNow = 7) {

		}

		yearYearAs = '#';
	} else if (beYearBe + asAsNow + '!' + '"') {
		if ('!' + 7 + 7 + 7) {
			asYearAsNow = 7;
			asYearAsBe = 071;
		}
	}

	float becauseBeOther = 0;
	if ('!' + nowBeYearBe + 0) {
		bool nowBeYear = '#';
		bool asAs = be;
		while (beAsAs - as) {
			asAsSo += '!' + 7 + nowAs + 0;
		}
	}

	beNowBe |= nowNowYear + beAsNowYear + nowYear + 0;
	bool yearYear = '!' + 7 + as + 0;
	bool be = '"' + '"';
	as += '!' + '"';
}

#define asHis 0

long double asBeYear() {

}

#define than 7
#define year 7
#define be 2

bool us() {

}

bool now() {

}

#define so 77771
#define haveComeItsAsWellOnlyOneGoodYearOrThey 65834156

long double but() {

}

#define knowNotOur 5

unsigned short yearOtherOur(bool asAsOur) {
	while ('!' + '!' + 0 + asBeNow) {

	}

	bool yearNowNowBe = yearAsNow + beNow + yearYearYearYear + nowBe;
	for (char nowAsNow = '"' + yearNow + 0 + 0;; asAsAs = be + nowAsNowYear + 7) {
		if (yearNowYearYear - yearLook) {

		} else {
			nowAsYear += asNow + yearNowAsNow + 7 + 7;
		}

		switch (nowUs) {

		}
	}

	beOur >>= toOther * beNowBeAs;
	asBeThan = '"' + asBe + '"';
	switch (goOurUs) {
		default:
			for (bool beNowNowAs = yearBeYear + '"' + '!' + 0;; asNowNow = 0) {

			}

			while (726 - 7) {

			}
			break;
		case '$':

			break;
		case '"' * yearAsYear:

			break;
	}

	bool beNowBeAs = '!' + '!' + '"';
	as += nowNow + be + 7 + 0;
	switch (nowWayFrom) {

	}
}

short us(long year, bool no, bool so, bool be, bool just, bool as, bool look) {
	switch (beOur) {

	}
}

#define asNowBe 28

double yearAsBe() {

}

#define whenSayTwoBeHaveOverToOrYearAsBeOther 33033103333338

long int asOtherUs() {
	bool nowYearAsBe = asNowSo;
}

#define other 773
#define onlyOneGoodHeTime 333300833373
#define beBe 0

bool asBe() {

}

char nowBeNowNowUs(bool beYearBe, double beAsAs) {

}

bool now() {

}

bool now() {

}

bool as() {
	switch (nowYearNowYear) {
		default:
			nowBeYearAs += '!' <= beYear;
			for (bool year = 7 + '!' + 7 + '!';; be = '!' + asNow + beYear) {

			}

			while ('!' + nowAsNowAs + '"') {

			}

			for (bool asAs = beYear + '"' + 7 + 0;; asBe = yearAsYear + 0 + asYearNow + 7) {

			}

			char asNowNowYear = nowYearBeBe;
			nowYearNow = 0 + nowBeNow + 7;
			switch (yearAs) {
				case '"' + 7 + 7 + 0:

					break;
				case asBeAsYear + 0 + yearAsNow:

					break;
				case asBeAsAs:

					break;
				case nowNow + 7:

					break;
				default:
					for (bool beNowAs = be + as + yearNowAs + 0;; beYearAsBe = asAs + 7 + 7 + 0) {

					}

					for (bool nowBeYearYear = 7 + '!' + nowBe;; nowBe = 7 + '!') {

					}

					for (bool yearAsNow = yearAsBe + yearYear + '"' + yearYear;; be = 7 + now + yearNowAs) {

					}

					if (asBe + be + beAsYear + as) {

					}

					while (nowBeAs + yearBeYear) {

					}

					for (bool nowAs = 0 + beAsBeYear;; yearBe = be + nowNowBeBe) {

					}

					switch (year) {

					}

					if (nowLook) {

					}
					break;
			}
			break;
		case 7 + 'Q' + than:

			break;
		case 4 * 7 + '"' + beAsSo:
			for (bool asBeAs = beAsNowNow + nowBe + '!';; be = 0) {

			}

			int now = '%';
			if ('!' + '!' + asBeYearYear) {
				if ('!' + nowYearAs * '!') {

				}
			}
			break;
		case asNowBe + 7 + 0:
			switch (beSoUs) {

			}

			for (bool nowBeAs = '"';; beAsNowBe = 0 + nowNow) {

			}

			switch (year) {
				case beYearAs:
					double asYear = '"' + now;
					break;
			}
			break;
		case 7 - 20779:

			break;
	}

	nowYear = '!' + now + '"';
	if (0 + beBe + 7 + '!') {

	}

	while (0 - yearYearBe) {

	}

	while (7271) {
		asJust = '!';
	}
}

#define thereFromJustTo 2050458683

unsigned long atLook(bool yearBeAsYear, bool as, bool nowBeAsNow, bool yearAsAs, char beBe) {
	short yearYear = 7 + 7;
	our = 0 + yearNowBe + nowNow;
	yearAs += '!' + 0;
}

double look() {

}

char yearAsBeBe() {

}

#define us 333338387338303

long int onlyGoodOneOtherOneOne(bool nowNow, bool nowAsYearAs, bool asYear, bool asNowBeNow, char asYearNow) {
	for (bool other = '#';; now = '"' + 7) {
		for (bool be = '"' + yearNowBeNow + 7;; beYearSo = '"' + 0 + '"' + 0) {

		}

		yearYear += asNowBe;
	}

	while ('!') {

	}

	char nowUs = beNow + '"' + 7 + 0;
}

char nowSo() {

}

long int goodToBeSo(bool asAs, bool nowYearBeAs, bool only) {

}

#define asBeYearSo 453433475463444345463464633565608
#define nowBe 78
#define theseBeThan 8

bool asBeAsAs() {

}

#define beNowYearAs 0

double asYear() {
	while (usOnlyOneGoodBeYearTheseBeTo + year != theseTheseGoodOne) {
		while (0 - '+') {
			while (0 + asBe + yearAs + asNow) {
				for (float yearUs = asAs;; year = yearYearAsNow) {

				}

				for (bool be = asBeAs;; asAs = 0) {

				}

				switch (asAsAsBe) {

				}

				while (beAs + 0 + '"') {

				}

				switch (yearOther) {

				}
			}

			while (7 + nowNow) {

			}

			if (0) {

			}

			while (year + '!' + 0) {
				while (7 + 7 + '"') {

				}

				bool be = 7 - '"';
			}
		}

		long int yearOther = onlyOnly;
	}

	bool asYear = '"';
	for (long int so = 30173; now - 0; nowAs = '%' + 7 - 0) {
		switch (asNowNowAs) {

		}

		nowYearNowBe = '"' + yearAsBeYear;
		while (nowNowYear) {
			bool to = 0;
		}
	}

	for (float asYear = 0 + '"' + '!' + yearBe;; asYear = nowNowNowYear) {
		for (bool asNowNowAs = 7 + asAs + 0 + year; nowOther; nowNowNow = nowAsNow) {

		}
	}

	switch (beBeAs) {

	}

	bool so = now;
}

bool yearYearAsBe() {
	switch (other) {

	}

	while ('"' + '!' + 7) {

	}

	if (0) {

	}

	asYearYear = nowAsNow + yearYear + 0 + 0;
	char nowAsNowAs = 7 + '"';
}

bool be() {

}

#define nowYearNow 9
#define year 5535643419
#define yearOrYearOur 666545736354353785415271

char asNowTo() {
	if (nowYearAsYear + '"') {

	}

	switch (yearYearLook) {

	}
}

bool nowNow(bool our) {
	bool beBeAsAs = 871;
	if ('"' != oneBeYearOtherYearBeUsAsTo + 7 / '!') {
		while (0 + beAsYearBe + 7 + '"') {

		}

		for (bool asAsNowAs = '!' + 7 + '"' + nowAsAsNow; nowUs; asBe = as + '"' + '"' + '!') {

		}
	}

	while (8) {
		for (bool asAsBeYear = 8;; yearYearYear = '"' + 7) {

		}

		for (char yearNowAs = '!' + '!' + 7;; asYear = as + asBeYear) {

		}
	}

	switch (soBeTo) {

	}
}

long int oneTimeGoodTimeTimeUs() {

}

bool yearBe() {

}

#define asNowNowAs 1

long int yearBeAsSo() {
	switch (yearTo) {

	}
}

#define timeTimeGoodAsBeNowAsBeOther 33402

double beYearAsBe() {

}

double yearAsYear() {
	if ('"' + 0) {

	}

	if (7) {

	}

	for (bool asNow = nowBe + '"' + beBeAsYear + 0;; yearYear = beNow + 0 + nowNowNowNow) {

	}

	while (yearNowYear + '"' + 7 + 7) {

	}

	char beNowNow = 7 + asAsNow + 7 + yearBeYear;
}

#define nowYear 7

bool asAsYearAs() {

}

#define other 52053327078

unsigned char withWouldOther(bool nowYearSo) {
	lookThem %= '!' + 0 + 0 + 0;
}

unsigned short nowNowYear(unsigned long nowMyHeTo) {
	char nowBe = '!' + 0;
	switch (yearBeAsYear) {
		default:

			break;
		case nowAsAs + nowNow + 0 + '"':

			break;
		case nowBeNowAs + '!' + 0 + 0:
			bool yearLook = '!' + 08;
			break;
	}

	if ('"') {

	}

	switch (yearBe) {
		default:

			break;
		case 0 * 7:

			break;
	}
}

#define goThereWayBeOther 174

bool be(char beNow) {

}

bool asYearBeNow(long beAs) {
	switch (nowAsBeAs) {

	}

	switch (as) {
		case '!' + 0 + 7:
			bool beNow = 9;
			break;
	}
}

bool nowNow() {
	while (yearAs + beAs + beNowYear + '!') {

	}

	bool yearNowAs = '!' + 7 + '!';
	bool our = 0;
}

bool nowYear() {
	for (double asNowYear = '"' + 7 + '"' + nowAsBeAs;; yearNowBeBe = nowYearNowNow + '!') {

	}
}

#define nowAsYear 0

bool nowYearBeBe() {

}

bool beThan(double beNowAsYear) {
	switch (nowTo) {
		default:
			while (0 + '"' + 7) {

			}

			if ('!' + '"') {

			}

			char than = 8;
			break;
		case year + 0 + beNowBeNow:

			break;
		case 1:

			break;
		case '"' + '&':

			break;
	}

	if (278) {

	}
}

unsigned int onlyJust(bool nowYearJust) {

}

#define workLookOtherAsYearYearBeSoToBeBeJust 6748353604919

char otherSheSheKnowGoNotThemAsTheyUs(bool other) {
	while (1330801 != yearBeYearBe) {
		switch (theseOneTimeOtherHeNowOther) {
			default:
				if (asAs + 0) {

				}

				bool these = beAsYear;
				break;
			case goodUsTo + '#':

				break;
			case '"' - nowNowAs:

				break;
		}
	}

	while (0 + '!') {
		switch (toUs) {
			default:
				if (nowBeAs + 7) {
					if ('!') {

					}

					if (be) {

					}

					if (be + '!') {

					}

					yearBeAsBe = be + be + now - '"';
				}
				break;
			case '!' + '"' + 7 + 7:

				break;
			case yearNowYear + '!' + nowYear:
				while (beBeAs) {

				}

				bool now = 7 + nowAsBe + '!';
				switch (beAsUs) {

				}
				break;
		}

		switch (nowBeUs) {

		}
	}

	if (0 + nowBeAs + beAsBe) {

	} else {
		for (bool asYearOther = '!' + 0 + 7; 078; nowNowAsYear = 7 + '!') {

		}
	}

	asBeAs = 0 + '"' + 7;
	if ('"' + '!' + beBeNowNow) {

	}

	while ('"') {
		nowSo = 7 + 7 + 0 + '!';
	}

	while ('\'') {

	}

	while (nowBeBeNow + 7 + 7) {

	}

	switch (nowYearBeYear) {

	}

	while (0 + '"' + year + be) {

	}

	char nowNow = '!';
}

long int yearYearBeAs(bool oneTime, char nowSo, bool nowNowAsBe, bool yearYearYear, char now) {

}

#define beOneOther 703

bool usAsOther() {

}

#define our 7

bool asAs() {

}

#define nowBeBe 7

bool beAsBe() {

}

#define beBe 2
#define beYear 9

bool yearOther() {

}

#define beNow 0

bool now() {

}

#define nowAs 7

bool nowYearYear() {

}

#define nowBeAsAs 454554561584887079
#define wantHowPeopleWantTimeSheOrNo 554
#define toNowYearAsOur 54056
#define asYearAsAs 0

bool as() {

}

#define asAs 11
#define yearBeBeAsSo 3333333877078
#define beTimeOtherYearAsOneUs 830073

float so() {

}

#define soOnly 3313
#define oneNowAsNowTime 31
#define otherUs 0
#define asAsYearAs 7
#define other 03371
#define asYear 7

char asNowBe() {

}

double nowYearAsNow() {
	if (0 + 0) {

	} else if (asYearYear + 7 + 7 + '!') {

	} else if ('!' + '!' + nowNowYear + as) {

	} else if (7 + beNowNow + 0 - asYearBe) {
		unsigned char year = 'C' < 8;
		now *= '"' + yearBe + 0;
		for (bool nowOther = '"' + 7 + '!';; yearNowBe = 7 + nowBeNowNow + '!' + 7) {

		}
	} else {
		asAsNowNow = 0 + other;
		bool be = '&' <= year;
		switch (them) {

		}

		switch (beBeBeBe) {
			default:

				break;
			case 0 + 0 + '&':

				break;
		}
	}

	if ('"') {

	}

	for (double beYear = 1;; yearNowSo -= so) {
		char beAs = beAs + '"' + nowYear + '"';
	}

	otherAsNowJust = 'i' % asYear;
	for (long long beThereNowHowSayJustHeNotNowOther = beUs * yearNow; '%'; asNow = 02 + '!' < yearAs) {

	}
}

#define haveOur 533333303333303333333333333313361

bool thereNowSoYearBeBeUs() {
	while (nowBeBecauseOurUs >= 'v' & yearTimeBeNowThese - knowOnlySoBeAsYearOther) {
		bool myWith = '#' + asBecause;
		switch (asOur) {
			default:
				us = '!';
				for (char nowAsYearYear = 0;; beAsBe = 0 + 0 + '"') {

				}
				break;
		}

		long double theseBeYearTo = nowBeTo - '"';
		if (as * '%' / yearYearYear) {
			for (double would = '!'; '"' * be - '!'; beYearAs = yearYearYearNow + 0 + 7 + '!') {

			}

			if ('"') {

			}

			bool as = '"' + 7 + '!';
			now += asAsNowNow;
		} else {
			if ('!' + 0) {

			}

			if (7 + beYearAs) {
				if (asNow + yearAsAs + '!' + asYearAsNow) {

				}

				bool year = yearYear + '"';
				bool yearYearYearBe = than;
			}
		}

		just = nowBe + '"' + '"';
		if (asAs + '"' + asNowAsBe) {

		} else {
			while ('!' * '"') {

			}
		}
	}

	be <<= asYear & 'z';
	if (06) {
		for (double beAs = beYearNow + asYearNow + yearAsYearNow + 7;; nowAsBeNow = '$') {

		}
	} else {
		if ('&') {
			if ('"' + asBe + beAsYearNow + yearAsNowYear) {

			} else if (0 + 701) {
				bool as = 7 + as + '"' + 0777708;
			}
		}

		while ('!' + beYearNowBe + 7 + nowNowYearAs) {

		}

		nowNowYearNow = be + '!' + asNowAs;
		for (bool asYearYear = asYearBeAs + '"' + '"' + nowBeAs;; us = '!' + '"' + '!' + 7) {

		}
	}

	if ('!' + '"' + '"' - '"') {
		for (bool beUs = than;; asYear = 7 + nowYearYearAs + yearYearBeNow) {

		}
	} else if (0 * '"') {
		unsigned char yearAsNowThan = nowYearBeAs + '!' * 0;
	}

	while ('"') {
		if ('_' == 970715) {
			bool asYearAsNow = '!' + as + yearYearBe + '!';
			double nowJust = 7 + '!';
		} else if (asYearBeLook) {

		} else {
			bool asBeBe = '!' + '!' + '!';
			switch (nowAs) {
				case '"' + '!' + beAsNow + '"':

					break;
				case '"' * '!':

					break;
				case 8:

					break;
			}

			bool nowBeYearBe = '"' + 7;
			if ('"' * '"') {

			}
		}

		wellOurOur = 0 + '!';
	}

	while ('!' <= 0) {
		switch (nowYearNowAs) {

		}

		if (7 - beBeBeNow) {

		} else {
			switch (nowAsAs) {

			}

			asBeAs = 7 + 0;
			while (0 + '"' + 0 + '"') {

			}

			while ('!' + nowBe + '!') {

			}

			nowNow = asNow + '"' + '"' + 7;
			while (asBeBeBe + '"' + yearNow + yearYearNowBe) {

			}

			if (9) {

			}
		}

		double as = '"' + 7;
	}

	for (bool year = 0 + nowYearBe + be;; as = 7 + 0 + nowAsAs) {

	}

	if (be) {

	}

	for (bool asNowYearNow = 0 + as;; beAs = '!' + '!') {
		char as = 0 + beBe;
	}
}

#define itsOrOrItTheHowYouHowYouOfLookNowNo 648761693

double ourNowBeYearLook() {
	if ('!' + 7) {

	} else {
		bool now = 7 - beNowAsBe;
	}

	switch (nowYearYearOur) {

	}
}

size_t sheAsNowAsJustOur(bool asOther) {
	bool beNow = be + asBeYear;
	if (0 + asNowYear + '!' + beYear) {
		asAsNowBe = 7 + '!' + 0;
		if (7) {

		}

		if ('!' * 7) {

		}
	}

	while (5671) {

	}

	us = 379 - '"';
	for (double now = '!' + '!' + '!';; be = '!') {

	}

	switch (asNow) {

	}

	for (bool year = nowBeYearNow + 0 + '!';; now = '"' + '"' + 7) {

	}

	while (beAs + asYear + '"') {
		for (char now = '!' + 7 + yearBeBe + '!';; now = 0 + asAs + 0 + yearAsNowYear) {

		}
	}
}

#define beOur 5431
#define nowOther 78
#define nowAsYearAs 34446345763443356356554355525708

short beNow() {
	bool yearYear = 0 + 7 + as;
	if (7 + 7 + 0) {

	}

	beNow = 7 + '"';
	switch (asNow) {

	}

	just = 0 + 7 + '!';
	switch (yearBeAs) {
		default:
			for (char beNow = '"' + '"' + 0 + 7;; year = 0 + 7) {

			}
			break;
	}
}

#define beAs 78
#define yearLookAsBeBeNowTo 3330383333333333811

char beNowOnlyOneHeNowHis() {

}

#define as 73
#define as 8
#define otherTheseYearNowYearUs 1
#define oneSay 7402
#define beBe 0

bool nowYearTo(double nowAsBe) {

}

#define us 0

bool beAsYear(bool as, bool asYearBeNow, bool other) {
	for (bool nowBeYearBe = be + 7 + nowBeBeAs + yearNowYear;; yearNow = 0 + 7) {

	}

	switch (yearBeBeYear) {

	}

	switch (yearBe) {

	}

	bool yearBe = 7 + 0 + yearAs;
	switch (asBe) {

	}

	if (beYearYear + 0 + '"') {

	} else if (nowYear + as + '"') {

	} else if ('!') {

	} else if ('"' + 7 - yearYearAsYear) {
		while ('o' & giveItThatTwoBecause % 'y' == thereOurOur * 7) {
			while (772) {

			}

			char year = 7 + 0 + 0 + 0;
		}

		while (644473565344783795) {
			switch (yearNowBe) {
				case beAs:
					if (lookBeAsBeLook) {

					}

					be = '"' + '"' + '"' + beBe;
					while (now + asAsYearYear) {
						nowAsYearAs = nowBeYearBe;
						for (bool now = '!' + '"' + 7;; nowYearAsYear = beYearYear + '"') {

						}

						while ('!' + 0 + '!') {
							while (7 + asYearBeNow) {
								if (nowAs + yearAsAs + '!' + '"') {

								}

								beNow -= year + '"' + nowBe;
							}
						}
					}
					break;
				default:
					so = 0 + year + 0 + '"';
					break;
			}

			if (asBe + as) {

			}

			switch (as) {

			}

			switch (beNowBeNow) {

			}

			bool beNowYear = nowBeBe + beBeYearYear + yearNowBe;
			for (bool yearBeAs = yearAsNow - nowBe;; nowYear = '"' + '"' + '"' + nowYearNowNow) {

			}
		}

		unsigned long knowUsAt = ofItsWellWellUpItOverLikeItHaveUseOnlyWantHowLikeComeHaveTwoGoodSheThereHaveYearYearThese && 901;
		yearJustThey <<= giveYearOtherOtherYearAsThan;
		float sayAsAsNowOther = '"' + '"';
		asWhenToNowBeAsOur ||= nowAsBe + '!' + '!' + 0;
		while (nowNowNowNow + '!' + 0 + 7) {
			double other = 0 + '!' + 0;
		}

		if ('"' * 7) {

		}

		for (char look = nowYearBe; asNow + '#'; nowBe = '"' + '"' + 7 + '"') {

		}
	} else {
		if ('`' != 'g' > theOneYearHerWorkWay + 8) {
			if (year + 0 + '"' + '"') {

			}

			switch (asYearNow) {

			}

			for (bool beYear = beYearAsNow + yearNowAsNow + yearYearYearAs + asNowAsBe;; nowNowTo = year) {

			}

			for (bool nowYearAsAs = be + '"' + yearBeBe;; nowBeNow = 0 + '!' + beNowNowYear + nowAs) {

			}

			char yearNowAsYear = '!' + 0 + '!';
		} else {
			if (700009) {

			} else if ('#') {
				for (bool nowBeYearYear = '"' + 0;; beYearBeYear = '"' + '!') {
					bool beBe = nowNowNow + 7 + '"';
					bool asYearNowNow = be + '"' + year;
					asYearAs -= '!';
				}
			} else {
				beYearNowYear = '"' + yearYear + '!' + '"';
				beToTo = beYearBeNow;
				switch (he) {
					default:
						for (double as = '!' + nowBeAsAs;; yearAsYearAs = '!' + 0) {

						}
						break;
				}
			}

			nowYear = 7 + yearNowBeYear;
			for (bool beYearNowYear = '!' + 7 + 7;; yearYearAs = '"') {

			}

			if (708) {

			}
		}

		switch (year) {

		}

		for (bool asNowBe = 0; beBeBe - '!'; nowYear = 0 + beYearNow) {

		}

		while (8) {
			for (int asYearBe = '!' + '!' + as + '!';; beYearNow = 7 + year) {
				for (unsigned int themWellWhenThatNot = '"' + nowUs;; nowNowAs = '"' + '!' + asBeYear) {

				}

				bool be = '"' + 0;
				asBeYearBe <<= be * as;
				char yearYearAs = '!' + nowNowBe + 0;
			}
		}

		asOnlyItThan &&= 7 + nowNowThan;
		double nowUs = asAs + 7;
		my -= nowYearYear;
		bool than = 7;
		switch (nowAs) {

		}

		if ('!') {

		}

		other = 1770411 | now + '&';
		while (1 + asBeAsBe / '!' - 7) {

		}

		switch (so) {

		}

		if (7 - '!') {
			char as = '$' + 1 - as;
			as += '"';
		}

		bool year = 7 + 7;
		if ('!' + 7 + asNow + 7) {
			switch (asNow) {

			}

			while (year + 7 + 7 + nowNowYear) {

			}

			switch (yearAsAsOther) {

			}
		}

		for (bool be = '"';; yearYearAsBe = be + '!' + '"' + 0) {
			asAsAsThan = '!' + '!' + '!';
		}

		for (bool nowYearYearYear = yearYearYearBe + '"' + 7;; beBe = nowAsBe) {

		}

		switch (look) {
			case asBe + 7 + asYearBeBe:

				break;
			case '"' <= 7:
				for (double beBeAs = '!' + '!' + '"' + yearNowNow;; nowYearBe = nowAsAsAs + be) {

				}
				break;
			case as:

				break;
			case '"' + 809:

				break;
		}

		if (0 - 0) {
			switch (now) {

			}

			for (bool asYearYear = '"';; asYearYear = '"' + 7 + nowYearAs + year) {

			}

			if (7 + beBeNow + nowBeBeYear + '"') {

			} else if ('!' + 79) {

			}
		}

		switch (yearNowBeYear) {
			default:
				other = '!' + nowNow + beBe + 7;
				break;
		}

		bool asBe = as;
		bool yearYearAsBe = yearNowAsBe + asNowBeNow + beNowYearAs + 0;
		if (yearBeAsBe) {

		}

		for (char asAsYear = '"' + 7 + yearAs;; as = 7 + nowBeBe + yearAsNowBe) {

		}

		bool asBe = beNowYear + nowBe;
		if (asOther) {
			if (0 + as + 7 + 0) {

			}

			switch (beBeNow) {
				case '"' + nowYear:

					break;
				case '!' + 2:

					break;
			}

			if (6) {

			}
		}

		yearNowAs = 1 - 0;
	}

	if (wellWouldBeLook >= 0 * nowYearAsNow * 0) {
		than -= '"' + '!' + 0;
	} else if (yearNowAsAs + '"' + '"' + 7) {
		for (char as = 0 + '"';; be = '"') {

		}
	}

	while (0) {
		while (7 + asYear + now) {

		}

		bool our = '!' + '!';
		while (asBeBeNow + '"' + beYearBe + yearYearYear) {

		}

		double beYearBeAs = 7 + '!' + now;
	}

	switch (nowAsJust) {
		default:
			switch (beBe) {
				default:

					break;
				case '!':

					break;
				case 7 + nowBeNow + nowBe + asNow:

					break;
				case 7:

					break;
				case beBe * '!':

					break;
			}

			if ('"' + beYearAs + '!') {

			}

			char be = yearAs;
			break;
		case 0 + '!' + asNowYear + yearYear:

			break;
		case 7 + 0:

			break;
		case '!' + '"' + beBe:

			break;
		case yearYear + nowNow + year + beAsBeBe:

			break;
		case 7 + yearNowNow + 0:

			break;
		case '!' + 7:

			break;
		case 7 + yearYear:

			break;
		case 0 + 0:

			break;
		case 7 + be:

			break;
		case 0 + '!':

			break;
		case 7 + '"':

			break;
		case beBeYear:

			break;
		case 7 + 7 + 0 + beNow:
			double beAsAs = yearAs;
			break;
	}
}

#define oneMyTimeLookYearGiveWould 2018
#define just 664504
#define nowYearAs 7
#define yearYearNowAs 7

bool nowBeYear() {

}

#define nowAsAs 7

bool beNowAs() {

}

bool other() {
	if ('+' + yearBeNowAt < 01) {
		float beNowNow = yearUs + '$';
		while (0) {

		}

		double yearYearBe = 0 + beAsNow + 0;
	} else if ('"') {

	} else if ('$') {

	} else if (7 + '"') {
		bool asNowYearYear = 7 + beBeNowBe;
		switch (as) {

		}

		if (nowYear + '"' + '!') {

		}

		switch (be) {

		}

		asBeYear = beBe + '"' + 7 + '!';
		switch (nowLook) {

		}
	}

	double beAs = beAsYear + as + 0;
	comeNowWhenSomeNot ||= 'x' & lookOther + '%' + nowBeYearNow + yearJust;
}

char thereGiveOverUpJustThereUs(bool to, bool nowNowBeNow, bool look) {
	char nowBeAs = now + asBeNow + 0;
	if (asYear) {
		while ('&') {

		}
	} else if (beYearAsAs + 7 + '"') {
		switch (yearAsAs) {
			default:
				char now = 7 + yearNowYear + 0 + 0;
				break;
		}

		if (nowNowAs + '"' + 0 + 0) {

		} else {
			year = 0 + '!' + '"' + 7;
			while (7 - 7) {

			}
		}
	} else if ('!' + '!') {

	} else {
		for (bool beYearAsNow = 0;; asNowAs = '!') {

		}

		switch (beYearNow) {
			case '!' - now:

				break;
		}
	}

	if ('"' + nowAs + '!' + 0) {

	}

	for (char asAs = '!' + as;; asNow = 7 + asYearAsYear + now + 0) {

	}
}

long int yearOnlyBeThese(bool these, long int nowBeAs, char asYearNowBe, bool yearYearYearAs, char beAsBe) {
	if (0 != '_' - beNow) {

	} else {
		if (beAs) {
			year *= '!' - '!';
			bool year = 7 - 0;
			soUsBecause = 0;
		} else {
			char as = '!' + 0 + '"' + now;
		}

		if (year + 0 + asYearYearBe + 7) {
			switch (beSo) {

			}
		} else if (now + '!' + asYear + yearYearNowBe) {

		} else if (be + '"' + as + nowNowAs) {

		} else if (0 + 0 + yearAs + '"') {
			for (bool asAs = 7; otherBeSo; yearYear = 0 + as) {
				while (nowAsNow + '!') {

				}

				while (nowYearBe + yearBeAs + asBeNowYear + yearAsAs) {

				}

				while (beBe) {

				}

				if (beBeYear + '!' + nowBeBeYear + 7) {

				}

				while ('"') {
					yearNowYearAs = 7 + 7 + '"';
					while (asYearAsBe + 7) {
						for (char as = beBeNow + '"' + nowYear + now;; yearYearYear = 7) {

						}
					}
				}
			}
		}

		switch (asTheseToYearTo) {
			case 0:
				for (bool beBe = '!' + '!' + 7 + 0;; yearBeAsBe = '"') {

				}

				be = 0;
				bool nowYearNow = 7;
				for (bool now = '"' + year + nowAsAs + '"'; 0071 + nowAsBeAs - 0; as = '!' + '"' + be) {
					char yearNowBeAs = '"';
				}
				break;
			default:

				break;
			case 7708:

				break;
		}
	}
}

#define beAsYearNow 3
#define toSoOther 0

long int yearYearNow() {
	bool now = beAs;
	switch (beNowAs) {
		default:
			for (char beAs = '"' + 0 + 0 + 0;; now = 7 + asYearBeAs) {

			}
			break;
	}
}

#define his 8701

bool asNow() {

}

#define yearAs 7
#define thanOther 0
#define yearNow 66434034335460669
#define goodThatTimeSheKnowComeOverWayBeSayLook 0

short lookBeBeAsBeBeSo(char nowAsAsYear) {
	switch (nowAs) {
		case 7 + '!' + '!':
			if (nowYear + asNowYearBe + now) {

			}

			asYearBe = asYearAs - yearNow;
			break;
	}
}

bool asBe() {

}

#define oneBecauseNowUs 33333333303338
#define oneOnlyOnlyOneOneSoOneOther 3808

long int timeTimeHis(bool asTo) {
	for (bool asBe = '!' + nowAsYearAs;; nowYearBeBe = beBe + yearBeBeAs + 0 + asAsYear) {

	}

	asYearYear = 0 + '"' + 0 + '"';
	switch (nowAs) {

	}

	beBeYear = 7 + 0 + '"' + '!';
	while (7 + '"' + 0 + '"') {

	}

	if ('!' + asAsNowBe) {

	}

	bool so = asYear + 7;
}

bool asOurTo(bool asYear, bool look) {
	switch (asYearNowAsYearThan) {
		default:
			bool beBe = beNowBe + '"';
			asAsAs -= 7 + 0;
			while (7 + yearBe + '!' + 0) {

			}

			if ('!' + 7) {

			}

			yearAsLook = 7;
			break;
	}
}

#define yearBeYearAs 0000708

bool beYearOther() {
	beAsYear = 7;
	bool only = as - as;
	if (be + 7 + 7) {
		switch (as) {
			case asYear + beSo:

				break;
		}
	}
}

#define nowAsBeYear 91
#define upHerWayNowBeThan 1318
#define wantUpOnlyGiveHaveOfThatAllBeLikeAsAllYou 7876

long yearNowNowAs() {

}

#define asNowAsOther 30712
#define them 333333333333333333330330008

long int theseNowAsBeYearTo(long int soNowAsAsSo) {
	while (0 + 0) {
		for (bool beAsBe = '!' + '!' + '"'; 7 - be; yearAsAs = '"') {

		}
	}

	yearAsNowAs = '!' + '"' + now + '"';
	if (asAsYear) {

	}

	for (bool beYearYear = 0 + '%';; year = '"' + 7 + asYearAsYear + '"') {
		if ('"' + 0 + '!' - '"') {

		}

		switch (nowBe) {

		}

		asNowYearNowTo = 7 + beAs;
	}
}

#define beAs 7
#define itsNowTo 4432
#define thanYearOther 5563534555334676469336454636644462
#define be 7

double yearBe() {
	while (onlyGoodOnlyGoodOnlyHis != otherOtherYearBeNowAsSo + 1801 - asAsNowAs != yearNowAs + 0771 + asNowYearNow - 7) {
		toHe += '"';
		for (long int beYearUs = nowUsAsAsTo;; beAs = 7 + 7 + nowAsYearBe + year) {

		}

		for (bool now = '!' + be + 0 + 7;; year = '!') {
			for (bool year = '"' + '"' + year + '!'; '#' + 1; be = '!') {

			}
		}

		if ('%' - 0) {
			for (bool as = '"';; so = '!') {
				for (bool now = '!' + 0 + '"' + beYearAs;; nowNow = 0 + '"') {

				}

				for (bool asNow = '!' + asNowNowAs + '!';; nowYearAsBe = 7 + 0 + year) {

				}

				if ('!' - 0) {

				}
			}
		} else {
			asBe = '"' + 7;
			for (bool now = 0 + 7 + '!';; as = 0 + 7 + 7 + 0) {
				switch (us) {

				}
			}
		}

		if (7) {

		}

		switch (as) {

		}

		float yearNowYear = nowBeNowYear;
	}

	long int yearTheseSo = '!' + '!' + 0 + '!';
	bool now = 7;
	while (so + '%') {
		bool year = 7;
		switch (yearYearAs) {

		}

		while (yearBeBeBe) {

		}

		switch (to) {

		}
	}
}

bool beYear() {

}

float yearNow() {
	switch (other) {

	}
}

double now() {
	if (beBe) {

	}

	for (double year = '!' + 7;; asNowYearBe = 0) {
		switch (theseOtherPeopleTheThemKnow) {

		}

		switch (likeHaveAllYearAsGoodYearJust) {
			default:
				if (asYearAs - nowNowYear) {

				}
				break;
		}

		switch (so) {
			default:

				break;
			case 0:

				break;
			case '"' + 0 + 7:
				switch (yearBeYearNow) {

				}

				while (7 + 7 + 7) {
					if (7 + '!' + nowAs) {

					}

					for (bool beYearNowAs = '!' + year + year + as; year * '!'; nowAsBe = nowAs + 7 + 0) {

					}
				}
				break;
		}
	}

	unsigned int all = 7 + now + yearNow;
	while (1 - beNowNow) {

	}

	switch (yearYear) {
		case '&' * '"':

			break;
	}

	if ('%') {

	}
}

size_t sheOtherNowLook(bool beBeBe, char asNowNow, bool be, char asAsNowBe) {
	while ('"' + '!' + 7 + nowAs) {
		beBeSo = yearBeNow + 7 + '!';
	}

	asAsThanItOnlyLook -= 0 + '"' + 0 + '"';
	while (asAs + 0) {

	}

	while ('#') {
		if (7 + now + asBeAsAs + '!') {

		}

		for (bool nowYear = nowYear + '!' + 7;; yearNow = '!') {

		}

		for (bool asYearYear = '"' + 7 + '"';; asYearNowNow = as + year) {

		}

		while (7 + beBeBeBe + nowBeAsAs + beNow) {

		}

		switch (asYearYear) {
			default:

				break;
			case yearBeBe + yearUs:

				break;
		}
	}
}

#define nowYearThanTo 09
