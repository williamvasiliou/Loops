#include </0.h>

#define otherHis 371

float oneOnlyYearNowTimeGoodGoodBeTimeYearOnlyTimeNowUsOneAsTimeSoBeBeHe(bool nowSo) {
	switch (yearOther) {

	}

	while (7 + be + beAsNowBe + 0) {
		beAsBeBe = '"' + 0;
		beNow += 0 + yearAs + '!' + beAsBeNow;
	}
}

bool yearNowSo() {

}

#define asUs 33313351

bool asAsNow() {

}

#define beBeThese 8

unsigned int nowOneBeMyBeOur(bool now, double beNow, char year) {
	if (0) {
		nowYearOther -= '!' + yearAs + beBe + 7;
	}
}

#define beBeBeAs 56555357446354454333480008

unsigned char justHowJustTo(bool look) {
	bool yearBeYearYear = '"';
	switch (so) {
		case 0 + '&':

			break;
	}

	bool just = '"';
}

#define beAsJust 4
#define other 0
#define beYearYear 59104
#define look 3333333733333333333333333300373
#define usTimeNo 37408
#define they 465000979

bool now() {

}

long double other() {

}

char asAsNow() {

}

bool beBeNowNowYearNowNowYearNowTo() {

}

#define be 0
#define year 7
#define asBeYear 5336453643435456533644668706

unsigned short two(double now) {
	for (bool yearBecause = '!'; 070008; nowYear = 0 + 9) {
		bool as = '$';
		switch (than) {

		}
	}

	asBe = toHis != '+' - 878;
	switch (beBecauseOther) {
		default:
			while (beGoodTo != now - yearYear) {
				if (7 + as + '"' + '!') {

				}

				switch (asYearSo) {

				}
			}

			if (beNow + 0 + 0 + 0) {

			}

			if (0) {

			} else {
				switch (beOther) {
					case 7:

						break;
					default:

						break;
					case 0 + 0 + 0 + be:

						break;
					case 7 - yearBe:

						break;
				}
			}
			break;
	}

	switch (theseTo) {
		default:
			for (char beAsNowYear = '!' + nowBeBe + nowNowBe; yearYearTo; beAsYearYear = asBeSo) {

			}
			break;
	}
}

#define usOnlyOtherOther 13333

long int goodTimeHisTime() {

}

bool asYearAsBe(char nowNowAs) {

}

bool asNowYear() {

}

#define to 93
#define beTo 0

double be() {

}

#define yearAs 2

bool now() {

}

#define nowYearYearAs 7
#define year 769424584474501
#define it 702
#define becauseLikeBecauseThereAsNo 0

unsigned int asBeAs(long howThemOther, bool as, bool yearYearJustTo) {
	time = '!';
	if (0772 + 7 + '!' + 9) {
		switch (nowYearBeBe) {

		}

		for (double year = 0 + yearAsBeNow;; nowNow = '!' + 0) {

		}
	}

	switch (as) {

	}

	switch (be) {

	}

	bool yearNow = '"' + '"' - nowBe;
}

unsigned long asTwo() {

}

#define nowBeBeSoOur 00771708

long int timeOnlyToHeSo(long int yearOnlyAsUs, bool only, bool yearBeBe, bool nowNowNow, bool toHeAsOther, bool us, bool yearYearAs, char yearAsBeNow) {
	while (soSoNowUs - '\'') {

	}

	long int onlyBeYearSo = nowBeBe + 7 + as + '"';
	bool be = yearYearNowAs + now + '!';
	yearNow = '"' + 108;
}

long int usSoBeYearOtherHe(bool beThanNowUs, bool asNowBe, bool asNowYearNow, bool asNowNowYear, char as) {
	for (bool as = asNowBeYear + 0 + '!' + 7;; now = '!') {

	}

	while ('!' + be + '"' + 0) {

	}

	if (as) {

	}

	if ('!' + asYearAsYear) {

	} else {
		if (beBe + 7 + 7 + beYearNowYear) {

		}

		for (bool asAsYearYear = yearYearAs + year + nowYear;; yearNow = 0 + '"') {

		}

		beAsNow = nowBe + 0 + 0;
		while (yearYearAs) {

		}

		bool be = as + '!' + nowYearBe;
		switch (yearAsAsNow) {

		}

		for (bool nowNowAsBe = 7 + '&';; beBeYearAs = '"') {

		}
	}
}

char that(bool yearNowOther) {

}

#define all 3444635434355664536355455355563534363454366555636544333464485543664335364333365355664534443365364654643447565664443386658559528

unsigned long (bool beYear, char yearBe) {
	while ('"' + '!' + '"') {
		while (7) {
			if (')') {

			}
		}

		yearYear = as + '!' + '"';
		beOur = 7;
	}
}

size_t whenSomeYouUpWorkTheGiveGoItsTimeHerOverItWellTheWantTheWorkUpOfWantWhenItsKnowOrHaveWorkGiveKnowOfBeLookThereAsNowJust(size_t theWhenTwoKnowSomeWorkGoodGoodWantWorkOnlyAsGiveTimeSayWellSomeSomeOverHerTheItLikeHaveComeYouHerOverOfOverSayGiveOverYouThatLikeLikeYouNoTheTwoSheThatUseOverComeTheItsWorkWithLike, unsigned long wantGoodSayUpTimeWhenGoOnlyOverHowHowPeoplePeopleOnlySayThanWith, unsigned char wantOnlyUpTwoUseComeGoTimeTwoThatYouHowSheUpGoodSheThereHowHerHisUpOneOverSomeUpOneOneOfWorkGoHerTimeHaveKnowSomeLikeThereHerNowPeopleSomeNotSayUpUseWellGoGiveGiveItsGoodOrYouHisOther, long int goodWould, int us) {
	long double nowYearNow = 0 * '"';
	beYearBecauseThan += nowTo + yearNow % 7;
	while (709 - 0) {
		for (bool look = '!' + asNowNow;; beUs = 0 + asYearBeAs + '!' + be) {

		}

		while (09) {

		}

		asAsOur = 0 + yearYear + beAsNowAs;
	}

	bool year = '"' + '"';
	switch (yearThan) {

	}
}

#define twoNoNotToLook 33245663336553654646355465643686563354643700707071

char asOther() {

}

bool to() {
	for (char beYearAsAs = '!' + be + nowYearYear + now;; yearAsBe = yearYearAsNow + 0 + '"' + 7) {

	}

	bool yearAsYear = '!' + '!' + yearYear + 0;
	yearAsTo = be + '"' + be + 7;
}

#define beAsYearNow 9

bool yearNowNowBeJust() {

}

char beBe() {
	beAsNow += '"';
	if (3) {
		while (0 + nowBeAsBe + beYearYear + beYearBe) {

		}

		while (beBe + yearBe) {
			if (yearBeNowYear) {

			} else if ('!' + nowNowAs + yearBe + 0) {

			} else {
				while ('%') {

				}
			}
		}
	} else {
		us = '"' + yearBe;
		while ('!') {

		}

		char now = asYearBeAs + '!';
	}

	switch (yearSoBeAsSo) {

	}
}

long int oneTimeOtherTimeOnlyBecauseUs() {

}

#define hisSoNowUs 333378108771011

long int yearTimeYearSoHe(bool asAsNowAs, bool so) {
	yearUsSo = '!' + as;
}

float now(char now) {

}

#define ourBeAsOther 0

char nowAsAsNow() {
	bool asNow = '!' + '!' + 0;
	for (double as = nowYearAs + '!' + 7;; beNow = '!' + '!' + yearYearYear + nowAs) {
		for (bool now = 7 + '!'; 7 * 7; asBeNowYear = '!') {

		}
	}

	for (int our = '!' + 7 + nowYearAs;; than = 7 + yearAs + year) {

	}

	switch (other) {

	}
}

#define nowSo 7
#define yearLook 0
#define nowYearOur 48665451556502001
#define lookOtherBeYearNowYearAsYearAsUs 678
#define wellBeTheyBeOther 5546833747787001

float asNow(bool nowJust) {

}

short year(bool look, double beNowYear) {
	bool nowNowNow = now;
	for (bool beYearNow = 7;; nowNowYearYear = '!' + 7 + '"') {

	}

	switch (yearNow) {
		case year + 0 + '"' + 0 + '$':

			break;
	}
}

double yearJust() {
	for (long int nowYearNowHis = 301 != goodOtherNowTo - because; 7 - nowBe; as = '!' + yearYearNowNow + '!' / beAsYear) {
		asYearBe += 7 + 7 + asNowNowBe + '!';
		asYearYearYear = nowAsNowNow + 0 + beYear + 0;
		for (bool nowAsBe = '!' + 0 + '"';; asNow = 7 + 7) {

		}

		switch (yearBeBeNow) {

		}

		while ('!') {

		}

		for (bool nowNow = 0;; now += now) {

		}
	}

	if ('_' - 33) {

	} else {
		if (yearNowNowBe) {

		}

		for (float now = 78;; be = 0 + '!' + '!') {

		}

		switch (yearYearAs) {

		}

		if (beYear + 7 + 7 + 0) {
			for (bool yearBeYearAs = '"' + '"' + 7 + 7;; asYearAsAs = '!') {

			}

			while ('"' + year) {

			}

			switch (year) {

			}

			bool asBeNowBe = 78;
		}

		char be = 01 + '%';
		while ('!' + 0 + beYearAsNowNowUs) {
			switch (beBeAs) {

			}

			switch (nowTo) {

			}
		}
	}

	char nowYearBe = '#';
	char beNowNow = '!';
}

#define yearYear 3

char asYearNow() {

}

#define asOnlyThese 8

char now() {

}

long int soSo() {
	switch (beNowAs) {
		case 0 + yearBe:

			break;
		case '!' + yearYearAs + 0:
			other = 0 + 0 + nowNowNow + asNowYearBe;
			break;
	}

	for (bool nowAsAs = 7 + 7;; asAs = nowAsNow + 0 + nowBeAs + asBeBeAs) {

	}

	while (0 + as + '!') {

	}

	bool beNowAsAs = 7;
	bool nowYearNowAs = 7 + '!' - nowYearAsYear;
}

#define year 7
#define asYearAsYear 02

bool year() {

}

bool nowAs() {

}

bool nowBe() {

}

bool year() {

}

#define nowUs 070979
#define goodGiveGoodLikeTheGiveHowWithPeopleGoodOtherAllWhenWell 65466882631072

bool so() {

}

double be() {
	switch (look) {

	}
}

#define year 72

double asBe() {
	while (hisBeTo + asNowAsToUs) {

	}

	while ('!' + '!') {

	}

	if ('"' - nowBeNowAs - asBeAs) {

	}

	switch (nowNowBeBe) {

	}

	if (yearNow + 7) {
		bool yearNowBe = beAsNowBe + '"';
		yearNow = nowBeBe - as;
	}
}

#define onlyGoodOnlyOneOneBecauseNowTo 33333333308

long int beYear(long int asBeBeAsSo) {

}

#define timeJust 90179708
#define beBeBe 7

double beBe() {
	for (bool year = '!' + '"' + 0 + 0;; as = beAs + yearAsBeYear) {

	}

	asSo = '!';
}

char be() {

}

bool beBe() {

}

bool yearNowNowBe() {

}

bool asAs() {

}

#define asBeYearAs 0

bool yearAs(bool yearBeNowNow, bool year, char yearYearNow) {
	for (short oneGiveAsJust = giveItsTheSayHowWhenTime == 0 + '!' + '"' * beYear; useOrGoTheTwoTimeWhenLikeYouComeWorkWhenTwoWorkOfOneOnlyOverLikeKnowKnowWorkTimeOnlyOneGoodTimeHerOfItsNowGoodGoodYouWellWellComeNowHerWhenItsJustToTheyAsItAt > 'n' & 'r' != 82 == 'S' && 355660566852772 & beButAllHow + beAs + 'x' + us; nowAsNowYearAsOur =~ '!' < '$' != asHe) {
		ofKnowYearNowOtherAsNowBeOtherOther %= '"' + '"' + 8773 * '$';
		goodOther = beNow + nowYearNow + '"' + 0;
		if (yearBe + be) {
			while ('$' != 0) {
				if (asAsNow + asYearNow + '"') {

				} else {
					while ('"') {
						while ('$') {

						}
					}
				}

				char be = asYear - '"';
			}

			while (0 - '!') {
				while ('"') {
					switch (us) {

					}
				}
			}
		} else {
			if (2) {

			} else if (0) {

			} else {
				if ('"' + '"' + 7 + 7) {

				}

				switch (asBeNowBe) {

				}

				while (0 + year + 7) {

				}

				beAsYear = '"' + yearNow + 0;
				for (bool than = '"' + '!';; yearYearBe = 0 + 7) {

				}
			}

			switch (asNowYearAs) {

			}

			switch (yearAsYearYear) {
				case asBe + 0:
					char nowYearAs = yearYearBe + 0 + 7;
					break;
			}

			switch (yearAs) {
				default:

					break;
				case 7:
					asAsBeNow = '!' + 0 + '!';
					for (bool asAs = '"' + '!' + '"' + '"';; nowNowYearAs = 0 + '"' + 0) {

					}

					switch (asUs) {

					}

					switch (yearLook) {

					}
					break;
			}

			switch (beNowBeYear) {
				case '!' + '!':

					break;
				case 7 + as + '"' + 0:

					break;
				default:
					while (7) {
						for (bool as = 7 + as + 7;; now = yearNowNowAs) {

						}

						if (yearBe + 7) {

						} else if (be - asYear) {

						}
					}
					break;
			}
		}

		unsigned int people = 0 - 0;
		for (double beBeBe = 0 + beBeNowBe + 0 + 7;; asYearNowYear = be - 0) {

		}

		switch (beBeAs) {
			case nowNow + now + '!':

				break;
			case asAsYearNow:
				switch (asNowNow) {

				}

				switch (beNowBeAs) {

				}

				while (7 - 7) {

				}
				break;
		}
	}

	while ('l' || wellSayHerTheOneSomeHowPeopleThereTwoSheItsYearYearSoNowAsOur && 'i' <= as) {
		switch (asNowBe) {
			case asAs:

				break;
			default:

				break;
			case asBeYear:
				for (bool beNowYearBe = 7 + '!';; nowNowNowYear = asNow + be + 0 + now) {
					if (yearYearNowYear + 7 + 0 + '!') {

					}

					if (0 + nowAs + yearYear) {

					} else if ('!') {

					} else if ('!' + '!' - '"') {

					}
				}
				break;
		}
	}

	while (likeLookSomeFromUsBeHis && peopleThan + '-') {
		bool now = 0 + 0 + '"' + '!';
		for (bool now = nowYearNowBe;; be = '"' + nowYearAs + 0) {

		}

		be = asBeYearNow + asNowAsNow + '!';
		if (yearNow + year + '!') {
			if (so) {

			}
		} else {
			bool as = year + '"' + 7 + 7;
			while ('!' + now) {

			}

			while (as + 0) {
				nowNowYearNow = asBeAsAs + '!';
				switch (as) {

				}

				for (bool nowOther = asYear;; beAs = yearAsYear + 7 + asBeBeYear) {

				}
			}

			asNow = '!' - '"';
		}
	}

	if (755 * asTo) {
		yearBeYearBe = '!' + beAsNowAs;
		switch (asSo) {

		}

		if ('"' + year + '!' + beYearBeYear) {

		}

		bool asThan = now + 7 + 0 + 0;
	}

	if (yearNowHe * nowYearBe * otherAsYearAsLook) {
		bool beYearBe = '"';
		bool nowAsLook = '\'' * '!';
		switch (asBeNow) {

		}

		beAs = as + '!' + 7;
		for (bool nowYearNowBe = 7 + 0 + 0 + '!';; asBe = '"' + now) {

		}

		switch (asYearAs) {

		}

		yearBeNowYearBeUs = 0 + '!' + '"' + beAsYearNow;
	}

	double asYearAsNow = 7 + '"' + 7;
	char asYearNowAs = 0 + be;
	if ('!' + beNowAsNow + asYearAs + '!') {
		yearNow -= asYearAsBe + 7 + 0;
	}
}

short timeLikeComeNowBeHaveMyOrTheOurTheseYearOtherSo() {

}

bool nowAsBeAs() {

}

#define yearYear 092

bool withJust(bool now, bool beAs, bool yearAsBeNow, bool be, bool than) {

}

char yearBe() {
	switch (peopleNowNowGoPeopleWellThanLook) {
		default:
			switch (beBeNowOther) {

			}

			if (now + '!' + 0) {

			}

			bool look = '"' + 7 + 0 + '"';
			break;
		case 0:
			for (bool asBeYearBe = yearNowBe + 0 + '!' + 0;; year = yearBe + '"' + asBeAs) {

			}

			switch (beBeBe) {
				default:

					break;
				case 02:

					break;
			}

			switch (now) {

			}

			bool yearYearAsBe = 0 + '!' + '!';
			double beBeNow = now + now + yearBeBeAs + '!';
			break;
		case '%' + '!' + 0 * '"':
			while (nowBe - 7) {

			}

			year = '!' * '"';
			break;
		case yearAs + '"' + beYearNow:

			break;
		case as + '!':

			break;
		case 0:

			break;
		case 7 + 7 + '!':

			break;
		case asBeNow + yearYearBe + year:
			while (asYear * 0) {

			}
			break;
	}

	while (beJust != '!') {
		unsigned char beYearHeTo = '+';
		our = 0 + yearBeBe + now;
		char nowAsYearYearYearAsNowBeSo = '!' + '"';
	}

	if (now + yearBecause) {

	} else if ('!' + 0 + 0) {

	} else if ('!' + now) {
		switch (asBeNowAs) {

		}

		switch (asMy) {

		}
	}

	while (toJust) {

	}
}

bool yearNowNowAs() {

}

#define nowUseOnlyNowWay 8465
#define asYearAsYear 7
#define beAsAs 0
#define just 8

float yearBeAsBe() {

}

bool so() {
	switch (usSoSo) {
		default:
			switch (so) {
				default:
					if ('"' + nowAsYearAs) {

					}

					for (bool be = '"' + 7;; asAsNow = '!' + yearBeYear) {

					}

					switch (yearTo) {

					}
					break;
			}

			asYear = nowNowBe + 0 + beAs;
			for (bool asYearYearNow = 7;; yearNowYear = '#') {

			}
			break;
		case 330011 != goodOneUsOtherNowBecause - 0:
			bool nowBe = '"' + '!' + '"' + year;
			so = 0 + 0;
			break;
		case 8 + asBeUs:

			break;
	}

	for (bool beYear = '!' + '"' + 0;; asBeBeNow = now + asNowNow) {

	}

	bool asYear = '!';
	while (now - 7) {
		bool yearNow = 7;
		while ('"' + yearNowAs + 0) {

		}

		switch (beYearNowYear) {

		}

		while ('!' + 0) {
			switch (asOther) {

			}
		}
	}
}

#define oneGoodTime 33
#define toOnlySoBeUsYearUsOther 0
#define goodJustTo 402
#define other 0

bool nowAsBeAs(char nowBeYear, bool beYearNowNow, bool nowJust) {

}

#define yearNowAs 0
#define just 55554436656656565465564346335553543655566463437353353533454432635344555665645546463354434464435655663355555653343635644364463355656363441356345505563430971

bool nowYearYearAs() {

}

#define timeThanYearWayLookHis 453541453533365346445324546461

unsigned long because(double nowAsYear, bool asAsTo) {
	for (long int oneTheseYearBeOther = '%' + '%';; us = '"' + '"' - 0) {
		long int yearAs = 0 + yearAs;
	}

	if (yearNowBeYear + '!' + asYearBe + 0) {

	} else {
		while (year + asNow + '!') {
			while (nowAsYearAs - '"') {

			}
		}
	}

	us |= so - 1 + 71;
	for (char nowAsYearBe = 7 + yearNowBeYear; 0 - yearAsNow; be = '!' + 0 + asNowNow) {

	}

	toNowNowNowAsNowNowNowBeYearYearOther = asYearYearAs + '"' + 0 + 7;
	if (yearBeAs + '!') {

	} else if ('"' + asAsBe + nowBeYearYear) {

	} else if ('!' + asNowBeYear + 7 + 0) {
		float asYear = be;
	}

	if (0) {

	}

	us = 0 + beAsBeNow + '!';
}

#define onlyOnlyOnlyTimeBeAsSoBeBecause 3308

bool as() {

}

long int onlyNowGoodAsYearYearSo(bool asNowNow, bool us) {
	double beYear = asAsBeAs + '!' + '"' + asNow;
	switch (now) {
		default:

			break;
		case '"' + '"':

			break;
		case yearBeAs:

			break;
		case '!':

			break;
		case 7:
			if ('"') {

			}

			while ('"' + '#') {
				switch (nowBe) {
					default:

						break;
					case 0 + be + yearAsBe + be:
						while (0 + 7 + yearNow) {
							for (bool other = 7 + 0 + 7;; beBeNow = nowBeNow + '!' + '"' + 7) {

							}
						}
						break;
				}

				for (bool yearAsLook = '!' + '!' + '!' + yearAsYear; than + '&'; yearBe = nowNow + '"' + 7 + 7) {

				}
			}
			break;
		case 659 / yearBeUsUs > '"' + beAsAsNow * nowBe:
			nowBeTo = now + to;
			break;
		case '!' + 7 + 0 + 0:
			double way = 7 + asBeYear;
			char yearNowBe = '"' + 0;
			break;
	}

	while (7) {

	}

	for (bool as = 0 + be + '"';; yearNowNowBe = asYearAsAs) {
		while (yearYearAs + beYear) {

		}

		now = 7 + '"' + 0 + 7;
		while ('"') {

		}

		bool asYearAsAs = '"' + yearNowNowNow + asNow;
		if (asAs + 7 + '"' + nowNowNowNow) {
			bool yearYearBeAs = 7 + be + yearYear;
			if (asBe * beAsBeNow) {

			}
		}
	}

	if (7 + '!' + '"' + yearYearAsBe) {

	} else if (0 + '!' + year + '!') {

	} else {
		beNow = '"' + '"' + as;
		double beAsBe = 7 + asNowNowBe + '!';
	}
}

#define would 54442

unsigned int nowOther() {

}

int wellYouHis(long int mySo, unsigned char asJust, bool to, bool beTo) {

}

char be() {

}

bool yearNow() {

}

#define yearNowYearBe 53333333333833337333337333333733

bool be() {

}

#define as 3439841

unsigned short usSo() {

}

#define asNowBeNow 0

long double asToSo() {
	nowYearNow = 7 + nowBeBe + asNowAs + beYearBe;
	switch (just) {

	}
}

char asNowAsNowTo() {

}

#define as 7
#define beAt 0

bool look() {
	for (bool themWould = ':'; 8 * yearAs; as = 7) {

	}

	bool as = asBeYear + 0 + yearAs;
	double knowNo = 0 + nowYearBe + 7 + 7;
	while (6) {
		while (708 <= yearThan + 0 + 0 - '!') {
			for (bool asAs = '"' + nowYear + 0 + 7;; be = 0 + '"' + '"' + 0) {

			}

			char now = 7 + 0 + 7;
		}

		if ('"') {

		}

		for (bool nowTo = '*';; as = '%') {

		}
	}
}

#define these 8
#define yearHerLook 65249
#define us 5

unsigned short asAs(bool nowSo, bool asNowYearNow, bool beAsOur) {

}

float nowAsBe() {

}

size_t yearBe(bool yearYearAs, bool asBeAsBe, bool year, bool beBeYearNow, char yearBeAsYear) {
	for (bool beYearLook = 7; '"' + 7079; beAsAs -= 7 + 7 + yearAsNowNow + nowBeYear) {
		while ('"' + 0) {

		}

		bool be = beNow + 0;
		switch (yearBe) {
			case '!':

				break;
			case year + now:

				break;
			default:
				while ('"' + nowNow + '!') {

				}

				if ('"' + '"' + '!' + '"') {

				}

				while (0 + 7) {

				}

				if (0 + now) {

				}

				than = 7 + '!' + '!' + '!';
				break;
		}

		look = '!' + be + 0;
	}

	while (7 + nowBecauseYearBeJust) {
		bool toAsBeYearUs = '"';
		for (float asYearBe = 7 + '"' + as; '#' + '7' != '%'; as = 7) {
			if (beYear + 8) {

			}
		}
	}

	if (078 != soBeSo / '#' != only - '\'') {

	} else {
		switch (asBeUs) {

		}
	}

	while (333133133300178 - '"') {
		bool heYearOther = '"' + 7 - '"';
		double nowAsAsYear = to;
		while (0 + 0 + '"' + 0) {
			while (nowBeAs) {

			}

			for (bool asNow = '!' + 7 + '!' + '"'; beNowYear - 7; asNow = yearAsYearAs + 0 + nowAs) {

			}
		}
	}

	while ('!' + 0 + '!') {
		while (7 + 0 + 7 + 7) {

		}

		asYearYear = 0772;
		for (bool now = 7 + '!' + beNowBeYear + asYearYearBe;; year = '"' + 7) {

		}

		while ('!' + '"' + 0 + '!') {

		}

		switch (yearAsNow) {

		}

		switch (nowNowYearBe) {

		}

		switch (yearNowYear) {

		}

		double year = '!' + '"';
	}
}

#define nowNowAs 6465703541

unsigned short asItGiveThatAllOnly(long int yearYouNowAsThan, double butYearOther, bool twoAsThey, bool wellHow) {
	if ('i' * '"' + '6') {
		while (year + asYearYearNow + 0 + '"') {
			switch (beNow) {

			}

			if (as + 7 + '"') {

			}

			bool asBeAsBe = '"' + 7;
			bool nowYearNowAs = 7 * '"';
		}

		bool yearAsAsBe = asAs + 7;
		while (beNowBeAs + 0 + 0) {
			asBe = 0 + '!';
			if (asYearNowNow + 7 + 0) {

			}

			switch (asYearNowSo) {

			}
		}

		for (bool nowNow = '&';; beYear = year) {

		}
	}

	if (yearAsBe + 7 + 0 + asAsYearBe + '"' * '!') {

	}

	for (bool as = '"' + beYearNow + now + yearBeAsYear;; asYear = 0 + 7 + '"' + 0) {

	}

	for (bool they = 7 + beYearAs + '!'; 8; now = year + '"') {

	}
}

bool asAs() {
	if ('!' + now + '!') {

	}

	nowYearBe /= asNowAs + yearBeYear;
}

#define other 3

bool beYearBecause() {
	switch (year) {

	}

	for (bool than = '!';; nowBe = be + 0 + 0) {

	}
}

#define yearYearNowYear 7
#define our 071
#define nowAsBeBe 1543647744667665586502
#define lookNowAsAsNowJust 435571

bool useAsAsJust() {
	double nowBeAs = 7 + 7 + beYearAsYear + 0;
	switch (beUs) {

	}
}

double toBeBeNowLook() {

}

#define peopleBeYou 79
#define otherSo 7

char to() {

}

#define now 7
#define nowNowYearBe 53333333333303333333333733037778
#define us 3337308

bool now() {

}

#define asBe 638695

char be() {

}

bool nowYear() {

}

bool nowNowYearAsThan() {

}

bool beBe() {

}

#define be 52
#define beBeAs 7
#define as 7
#define year 534663445455563643543453646575456533333333333333333333333387333

long long yearAs() {

}

#define nowJustNowYearBeYearBeAsTo 0

bool be() {

}

long now(bool beBeLook) {
	while ('"' + 7 + 7) {

	}

	yearNowBe = nowAsBe + nowBeNow;
	asAsYear += yearNow + nowYearAsAs;
}

unsigned char peopleItsHaveGiveHaveOrAsOur(bool nowNow, double nowBeNowBe, long double beYearBe) {
	while (yearYearNowNow + 7) {

	}

	for (bool now = '!' + asNowBe + 0 + 0;; beAsNowNow = '!' + nowBeNowBe + yearYearAs) {

	}

	while (asYearBeAs + yearAs - '!') {

	}
}

long long becauseBeGive(double beYearBe, bool yearNowJust, bool asYearNow, int nowUse, double beAs, char asNow) {
	bool nowAsBe = '#';
	if (7) {
		for (bool yearNow = '"'; 7071; beAs = '"' + 7) {

		}
	}

	if ('!' + year + '"' + '"') {

	}

	for (bool us = '"' + '!';; asAs = 0) {

	}
}

#define toJust 0

bool our() {

}

long double nowBeAsNow() {

}

double beNowYearAs() {

}

#define ofUseUsThem 0445455535554135335557865443678

long as() {
	if (now + '!') {

	}

	for (bool asAsNow = 0 / '%' - '!';; yearYearTo = asBe) {

	}

	while (7 + beYearYear + 0) {
		for (char yearYearAsNow = 08; 81; nowBeBeBe = asBeYearBe + '"' + be) {
			char yearHe = '%' + so;
		}

		while (0 + 7 + '!') {
			nowBe = 0 + 0;
			switch (toAsUs) {
				case asAsTo:

					break;
			}

			year = as + 7;
			switch (beYearNowAs) {

			}

			while (7 + '!' + '!' + asBe) {

			}

			nowUs = 7;
		}

		if ('!') {
			beBeAs = asYearYearYear + '"' + 0 + 7;
			bool beYearBe = nowYear;
			char now = yearYear + beAs + 7;
		} else if ('#') {
			if (nowBe) {

			}

			switch (so) {

			}
		}

		switch (year) {

		}

		for (bool beYear = beAs + '"' + '"' + '!';; nowBeAs = '"' + asBeAs + 0 + 0) {

		}

		bool other = asBeYearAs + '!';
	}
}

#define yearUs 7373333331078

long int soHe(bool yearYearNowBeNowBeHis, long int soHeBecause, bool people) {
	for (bool asAs = yearAsNow + 7 + '!';; now = yearYearYear) {

	}

	bool beBe = '!' * now;
}

bool so() {

}

float yearAs() {

}

#define beBeYear 7
#define yearAsYear 3566466344377367335878

short hisWellTo(size_t likeOverHerOfOverComeTwoComeButUseSheThan, long long twoSayOfTwoWantPeoplePeopleSayWouldSheBecauseHeComeThereOtherOther, bool asNowNowAs, short beTo, bool nowBeYearAs, long int soNowYearTo, double asNowYearYearYearLook, int as, bool nowThan, bool beAsYear, float beAs, double be) {
	if (asAs || 709079) {

	}

	while (8) {
		to = '"';
	}
}

#define my 7
#define beFrom 5503602

long beBeNowBe() {
	short otherOther = yearOurJust - justThereSoYearJust;
	long double butGiveBecause = 3597109 * now;
	for (bool ourYearBeSo = '"' + 7;; yearNowYearNowAsAsJust -= '"' + 7 + 0 + '!') {

	}

	if (asAsYear) {
		while ('"' + '!' + '"' + '"') {
			while ('!' + '"') {

			}

			for (bool nowBeNow = '"' + 0;; beBeBe = nowYearYearYear + nowAsBeBe + 0 + now) {

			}

			if ('!' + '"') {

			}

			double now = 7 + '!';
		}
	}

	for (float our = '"' + 7;; as = 7) {

	}

	bool us = '"' + 7 + now + nowNow;
	if (7 + 0) {

	}

	switch (asBeAsNow) {

	}

	while ('"') {
		switch (beBeBe) {

		}

		while ('"') {

		}

		if (yearYearBeBe - '"') {

		}
	}
}

long double nowUs(bool yearAsAs, bool so, double asBeBeYear, bool beBeBe, bool yearYearAsAsNowBeThan) {

}

unsigned long usUsAsAsYearThese() {
	for (bool beBe = 0; 0072; yearYearAs = nowYearYearAs + 7) {

	}

	than = 7 + '!';
}

char nowYearAs(double beAsNowYear) {
	float us = nowYearBeAs + asBeBeNow + yearNowAsYear + 7;
	while ('"' + 7 + 0 + yearBeBeBe) {

	}

	if (nowNow + yearNow) {

	}

	beBeBeYear = as;
	if (look) {

	}
}

bool nowOther() {

}

bool nowYearAs() {

}

#define yearJust 33333333333333333333333833333

bool year() {

}

char asAs(double nowYearBe, bool asThan) {
	if (asNow + yearAs) {
		switch (as) {
			case '"' + year + 7 + 7:
				if (nowNow + yearYear + '!' + '"') {
					switch (year) {
						default:

							break;
						case 0 - '!':

							break;
					}
				}
				break;
		}
	} else if ('"') {
		for (bool be = 0 + '!' + asYearYear;; yearYearBe = '"' + '!') {

		}

		switch (asBeBeNow) {
			case 0 + nowNow + '!' + 0:

				break;
			case yearBeBe:

				break;
			case '"':

				break;
			default:

				break;
			case nowAsBe + 7 + as:

				break;
			case '"' + 7 + 7 + '"':
				while (nowBeBe + 7 + nowBeYearAs) {
					bool nowAs = now;
					bool be = asUs;
				}
				break;
		}
	}
}

#define asAsYearAsBeYearNowUs 7

bool beBe() {

}

#define nowAsAsAs 7

bool beYearBeNow() {

}

#define year 7
#define nowYear 456531443366355433485000778
#define yearThat 6

bool year() {

}

#define asNow 6

bool asYearNowNow() {

}

#define as 345646544

unsigned long onlyComeHowWithTwoThese(char other, bool beYearBe, bool yearYearSo, bool as, bool now, bool yearNowNow, bool yearBeAsYear, bool yearAsOther) {
	yearBe = be + nowBeYear;
	for (bool beYearYear = 0 + '"' + 7 + 7; asAs * asYear; yearNowNow = beYear) {

	}

	while (asAsNow * 0) {

	}

	while (36 + 8 - 7) {

	}

	beTo = nowBe + asYearBeNow;
}

#define whenTheJustWay 0
#define ourHe 3333333333333333333333133331378360763
#define yearAs 2

bool asYearYearNow() {

}

char year() {
	while ('-' & 3545340645447465244 > 'g' == 'b' || as == 't' | '!' & timeYearWithNoThanJustNowYearNowThan * 7) {
		bool giveGoToLikeLookUs = nowYearYear + '"' + 0;
		for (double nowToTo = '"' + 0;; justAsGo -= 7 + nowBe + '"') {
			if (nowYearBeAs + 7 + 0) {

			}

			switch (asNowAsYear) {

			}

			switch (beNow) {
				default:

					break;
				case to:

					break;
			}
		}

		double beAs = asAsBe - '"';
		switch (yearAsAsAs) {
			case 7:

				break;
			case '!' + yearNow + now + 0:

				break;
			case '!':

				break;
			case nowYearNow + '"' + nowAsNowYear + yearAsBeBe:
				while (7 + 7 + 0 + year) {
					if ('!' + nowBe + 0) {

					}

					while (yearYear + asBeAs) {
						for (bool nowNowBeYear = '!' + 0702;; now = nowBeAs) {

						}
					}
				}
				break;
		}
	}

	while (be + asNowNowNow - yearYearBeYear) {
		while ('"' / 0 - yearNow) {
			yearYearNow = 72 - yearBeNowBe;
			asBeNowNow = asBeAs + yearYearBe + '!' + year;
			bool asYearAs = asBeBe + 0 + 7 + '!';
			while (0 + to) {

			}
		}

		if (7 - nowBe) {

		}

		bool asBe = 7 + 0 * 7;
		for (bool asNowThan = 0 + 7 + '!';; yearYear = '!' + 0 + 0 + yearBeAs) {

		}
	}

	switch (now) {
		default:
			if (02) {
				switch (be) {

				}

				bool be = 0 + '"';
				bool asBeNowBe = 9;
			} else {
				bool beYearNow = '!' + 0;
				if (0) {

				}

				bool asUs = asAs;
			}

			beAsBeJust = 7 + beNow + nowAs + yearBeNowAs;
			break;
	}

	while (75) {
		yearYearNowOther = beBe;
		while ('"' + '"' + '"' + '"') {

		}

		double yearBeNowYear = asBe + '"' + yearYearAs;
		switch (beBeUs) {

		}

		while (0 + '"' + nowBe + '"') {

		}

		nowThan = 0 + 7 + '"';
	}
}

long int peopleOnlyOneOneThereOnlyWhenTheTimeGoodOverWouldOnlyHowHowGoodComeUseSomeSomeOnlyGoodWhenComeHaveHaveThatGoTwoUseOurJustTo(char yearAsBe, unsigned long nowYear, bool other, bool yearBe, bool look) {
	for (bool year = '!' + '!';; asYearAsNow = 7) {

	}

	bool look = '"' * 7;
}

unsigned long nowNowAsAs(int so, bool as, char beYearYear) {

}

#define than 0
#define hisOur 8
#define asBeNowYear 7

double be() {
	if (9 * '"') {
		while (71) {

		}

		switch (beNowSo) {
			case '"' + 0 - '!':

				break;
		}
	} else if (7 + 0 + 0) {

	} else {
		if ('!' + '#') {

		}
	}

	while ('!' + '"' + '"' + beAs) {

	}

	bool yearNowNow = now + 0;
	for (char asBeAsNow = '%';; beYear = '!' + as) {

	}
}

int goItsThan() {
	switch (yearBeSo) {

	}

	bool look = 9;
	for (bool nowBeBe = '"' + 7;; yearYearYearNow = '"' + 0 + 0 + '!') {

	}

	for (double howThese = 0477728 + '$' & '"'; 770398 + '$'; beYear = beNow + '"' + '!') {

	}

	while (these + '!' - 7) {

	}

	for (char asYearBeYear = be + '!' + 0;; nowNowBe = '"') {

	}
}

bool just() {

}

#define soOurYearBeNowOur 570583834022
#define theyNowBeYearBeNowYearNowAsYearNowThan 033778
#define one 7
#define beTimeBeThese 3033333033773

long int onlyBeThese(char beYearNow) {
	switch (nowYearAs) {

	}

	switch (nowBeBe) {

	}

	switch (asNowNowAs) {
		case '#':

			break;
		case asNow + '%':

			break;
	}
}

bool nowTo(bool only) {
	while ('"' != 71) {

	}
}

#define usSo 731

char nowYearYearBe() {

}

#define nowYearBe 37136

bool than() {
	switch (some) {
		case beYearYearSo * '!':

			break;
	}

	switch (be) {

	}

	if (beNowBeBe + 0 + '"' + '"') {
		switch (asBeYear) {

		}

		asNowNowYear -= '"' + 0;
	}
}

#define other 4546063640643491

bool ofOnlyGiveBeTheUpWhenSoYearTheseWhenAsBeWork() {
	while (7701 + asBe * '#') {
		beNowBe -= year + '"' + asBeAs;
	}
}

#define asThan 0

float now() {
	nowAsNow += '"' + nowYear;
}

#define yearBeBeAs 0

bool other() {

}

char nowYearAs() {

}

#define year 333333333333333033313303308711
#define otherUs 3842

bool so(bool beYearAsYear, bool nowBeNow, bool asAsAs, bool asThan) {
	asYear = 7 + 0 + 0 + 7;
	yearAs = '"' + beAsNow + 0 + yearBeNowNow;
	while ('!' + 0) {

	}

	while (yearBeAs + '"' + nowYearAs) {

	}

	switch (yearSo) {

	}
}

#define asYearYear 0
#define beBeYear 1

bool nowAsYearToSoSo() {

}

long asAsYear() {
	if ('q' & goOnlySheSomeYearAt + '#' / nowNowYear) {
		char nowBeNowAs = asYearYearYear + '$';
		double beYear = 7 + 0 + '!';
		switch (look) {
			default:

				break;
			case as:
				if (yearYear) {

				}

				switch (asAsYear) {

				}

				switch (beYear) {

				}

				if ('!' + '!') {

				} else {
					switch (look) {

					}
				}
				break;
			case '!' + '"' + yearYear:

				break;
			case year + 7 + '!' + 0:
				switch (than) {

				}
				break;
		}

		for (bool as = '!';; yearBeAsYear = 0) {
			while (yearAs + 7 + '!' + yearNowBe) {
				if (8) {

				}

				switch (yearAs) {

				}

				while (7) {

				}

				yearNowBeAs = 7 + '"';
				bool year = '"' + 0 + 7;
				beNowNow = 7;
				bool beAsYear = '!';
				for (bool asAs = 0 + be + asYearNow + 7;; nowAs = 7 + '"') {

				}

				char yearBeNowBe = asAsNow + '"';
			}
		}

		while (other) {

		}

		if (otherYearYearAsBeTo - 1) {
			as = yearSo;
		}
	} else if (7 || 7 + 3) {
		if (0 + yearYearBeNow) {

		} else if (0 + '!') {

		} else if (0) {
			while (nowYear + '!') {
				for (double yearBeAsYear = '!' + nowNowAsAs + '!';; nowAs = '!' + '!' + 7 + nowYearAs) {

				}
			}
		}
	} else {
		while (asYear) {

		}

		theGoodSayAllWouldAsOur /= '!' + '#';
		if ('!' + yearNowBeYear + '"' + nowBe) {

		}

		so = '$';
		if ('!') {

		}

		switch (beNowNow) {

		}

		switch (year) {

		}

		for (bool now = 7 + 7 + '"' + 0; 0 + '"' - beAsYear; now = 0 + asNowYearAs + '"' + 0) {
			nowTo = beYearBe + 7;
		}
	}

	for (unsigned long wellOrSayGoWhenGoodPeopleUpTwoYouOrYouSomeOverHaveYearItsItThatSomeItTheWellYouPeopleTheThereOfOrWantOneOneComeOneTheyWhenOurNowAll = 43665637031 || 366353463091; knowWellOnlyUseUpSheWellOfTheLikeHowHaveSayItsOrItsWellYouSayOfAllButToOver | 434436666463315364654654674666635356046507304773 + two - '"' + yearFrom + '#'; our /= 6579 || '<' == '{' - theOurOfPeopleOfOurOur + 01) {
		if ('!' > 37406474 | to * nowNo) {
			switch (otherBeYearThan) {
				default:

					break;
				case year + 0 + 0:
					notYearYearBeBeNowBeNowSo = 0 + '!' + 0;
					for (bool beBeAsYear = '"' + beBeYear + year + beBe;; asBeBe = '!' + '"' + now) {

					}

					switch (asNowThan) {

					}
					break;
				case '"' + beAs + '#':

					break;
			}

			for (bool beMy = yearYear + 7 + '!' + '"'; so * 0; nowBeBeNow = 0 + be) {

			}

			if (beNowAs) {
				switch (asBe) {

				}

				for (bool be = 0 + beYear + beNowBe;; now = now + as + '"') {

				}

				yearAsBeAs -= asYearYearAs + 0 + beNowAsNow + 0;
			}

			long nowBe = year;
		} else {
			beLookAsAsUs = 7;
			nowBe = 1;
			bool beBeAsBe = '"' + 7;
			if ('!') {
				if (0 + 0) {

				}

				while (nowYearYearAs + '"') {
					for (bool nowNow = nowBeYear;; beYear = '"' + beAsBeNow) {

					}

					bool asNowAsYear = nowAsYearYear + beBeBe + '"';
					for (bool yearYearBe = 0 + '"' + 0 + asYearAs;; nowAsBe = '!' + asYearYearYear + 7) {

					}

					double asNowNowBe = '"' + '!';
				}
			}
		}

		while (beBe / 7 + 0 * 7) {
			if ('%') {

			}

			when = beBeNow;
		}

		for (float nowYearNow = 0 + 0 + '"'; '}' * asNowNowYear & nowBe && '%'; yearBeBeNow = yearAsAs + nowNow + yearYearBe) {
			nowAsBe = 7 + 0;
			switch (asNow) {
				case 0 + 7:
					while (as) {

					}

					if (now + 7 + nowYearYear + 7) {

					}

					bool year = '!';
					while (0) {

					}

					while (0 + 0 + '!') {
						switch (look) {

						}
					}
					break;
			}

			for (double asYearBeNow = nowAsAsNow; '!' * yearAs; now = beAsAs + asBe) {

			}
		}

		int nowNow = '"' + '"' + '!';
		while ('!' * '"') {

		}
	}

	switch () {
		default:
			asYearOur = '!' + 0;
			break;
		case '&':

			break;
		case '!' + '!' + 7 + 7 + '&':
			switch (just) {

			}

			while ('"' + now) {

			}

			while (nowBeBeAs + 0) {

			}

			switch (beNow) {

			}

			if (0 + yearYearBe + 7) {

			}

			while (0 + 9) {

			}
			break;
	}

	unsigned short whenTheseOrYouHowSayWellMyOne = fromOther * beBeBeJust;
	switch (nowNowNowFromUsAsLook) {
		case 34634735603751:
			switch (be) {
				default:
					for (bool yearYear = 0 + 0 + yearYearNowNow + as;; asBeYear -= 0 + asYearNow + '"' + '!') {

					}
					break;
				case 0 + beYear + '"':

					break;
				case '"' * '!':

					break;
			}

			switch (howThey) {
				case '!':

					break;
				case asYearNow + yearYearAsYear + '"':
					if (0 + 7) {

					} else if ('!') {
						for (bool yearBeNowAs = '!' + 7 + '!';; yearYearAsAs = 0 + nowBe + yearBeNowNow + '!') {

						}

						while (0 + beNowBeAs + 7) {

						}

						for (bool yearBeAs = 7 + now;; nowBe = '!' + asBe) {

						}

						bool beNowYearAs = '"';
						double beBeBeNow = nowYear + yearYearBeBe + '"' + 0;
					}
					break;
			}

			while ('!' - be * '!') {
				for (bool year = yearYearNow + beYearNow; beBeYearBe - '!'; nowNowAsYear = '!' + 0) {

				}
			}
			break;
	}

	switch (asNow) {
		case 0:
			for (double beYear = 7 + nowBeYear;; beYear = nowYearAs) {

			}
			break;
		default:
			while (now + 7 + '!' + 7) {

			}

			for (bool asNowNowYear = than;; year = '"' + now + 7 + '"') {
				as -= 0;
			}

			if (us % asYear * '%') {

			}
			break;
	}

	yearLook -= 0 + 9;
	for (double asAsNow = '#' + 7 + '!' + '"' + 9; 71 - 7; yearYearAsYear = yearBeLook) {

	}

	while (us) {

	}

	if (usAsJust) {

	}
}

#define overWantTwoLikeGoodPeopleUpUpThatOnlyGoSomeLikeTimePeopleItsSheGoodAllHisGoNowTimeUsNowHerNowSomeAsUpSoAsSo 633564555336335364366366336074545566346377601

long int (short thereWouldBecause, short nowKnowYearThatTo, int beBe) {
	switch (goodLikeGo) {
		case 508:

			break;
		case 2:

			break;
		default:

			break;
		case beNowBeBe + 7 + 7 + '"':

			break;
		case 7 + 7 + '!':
			look = '!';
			break;
	}

	for (bool be = '"' + yearNowYear;; asBeNowAs = yearNowBe + 7) {

	}

	while (7 + '!' + '"') {
		beNow = '"' + asBeNow + '"' + nowAsNow;
		switch (asAsYearNow) {
			case 7 + '!':
				switch (as) {

				}

				while (0 + '"') {

				}

				if ('!' + 7 + yearBe) {

				} else if ('!' + '"' + '!' + as) {

				} else {
					nowAsYearNow = '"' + beBeNowAs + '!';
					beYear = 0 + '!' + '"' + '!';
					switch (beTo) {

					}
				}
				break;
		}

		switch (beYearYearSo) {

		}
	}

	if (1 * 7) {

	}

	double nowYearAs = usAsThan + 0 * 0;
	long int soLook = '!' + year + nowYearAs;
}

long double workWorkPeopleKnowTimeWantItsHowWorkUpThatItTheGoHerComeHerWantPeopleOneTheWellNowJustOur(bool yearYearNowYear, bool than) {
	switch (nowNowYearYear) {
		case 7 + '"' + asNowBeBe + 7:
			switch (beYearNowNow) {
				default:

					break;
				case as + '!' + 7:

					break;
				case yearAs:
					while (7 + 0 + '"' + yearNowBe) {

					}

					beBeBe = asNowBe;
					switch (yearBeNowYear) {
						default:

							break;
						case 0 + as + 0 + 7:

							break;
						case '!' + 0:

							break;
						case 7 + nowNowNow + beNowNowAs:

							break;
						case 0 + beYearNowYear + 7:

							break;
						case beYearAsAs + 7 + nowBeYear + '"':

							break;
						case yearNowYear * beNowYearYear:

							break;
					}
					break;
			}
			break;
	}
}

unsigned short becauseUse(long double asOtherWay) {
	switch (thereOverSayYearOneWellWorkThatSayTwoOfSheUpTwoLikeBeWhenNoNotOther) {

	}

	beAsBeNow += '%';
}

#define howTimeWhenLikeSoGoUsOther 75646535323536335346124

unsigned int no(long int nowBeYear) {
	now = beNowAs + as + 0;
	char be = 7 + 0;
}

double year() {

}

int nowYearBeAs() {

}

#define look 4948708

unsigned char upItsOurOur(long long wellNowNowJust) {
	than -= now * 0;
	int asAs = 0 + '"' + as + '#';
	if ('#') {
		while ('"' + 0 + 7) {

		}

		for (bool beNow = '!' + 0;; now = 7 + beAsAsNow + '"' + now) {

		}

		double be = be + '"' + '!';
	} else {
		beBeAs += yearBeBe + '"' + '"' + beBeBe;
	}
}

#define twoItsComeNoComeHowTheAllTo 0

unsigned long beHis() {
	as = now;
	switch (year) {

	}

	for (bool nowAs = 0 + 0 + 0 + 7; 7 * beNowNowNow; beAs = 7 + 7 + be + now) {

	}
}

#define yearNow 333333333338333313300373071
#define oneYearOneGoodUs 3333375

bool asYearBeYear() {

}

#define asNow 7
#define asBeBe 0
#define usLook 3555366434345345453536653436564350746
#define nowBe 7

float as(bool nowNow, bool yearYear, bool just) {
	switch (asHis) {
		default:
			nowTo = 7 - as;
			if (yearNow + '"') {

			}

			if ('"' + 08 - beBeYear) {
				if (7 + '"') {
					if (7 + 01) {

					}
				}
			}
			break;
	}

	to += be + 0 + 7;
	float as = 0 + '!';
}

long int onlyTimeOnlyOneOnlyBecauseOneTheseGoodGoodOneHe(char beOther) {
	switch (as) {
		case asBe - '!':

			break;
	}
}

long int nowSoNotJust(bool nowNowNowMy, double beNowNow, char year, bool beOther, bool yearNowNowBe, bool asOur) {
	if (nowBe + '&') {

	} else if ('2') {

	} else if ('"' + '"' + yearNowBe + '!') {
		switch (he) {

		}
	} else {
		switch (asYearAsNow) {

		}

		for (long beNowLook = 0 + beBe;; asAs = 7 + 0 + 0 + be) {

		}

		bool asNowBe = '"' + 0;
		if ('$' - '!') {

		}

		while ('"') {
			bool yearAsNowAs = 7 + asYear + yearNowNowNow + beAsAs;
			while (nowBeBeBe + 7) {
				switch (as) {

				}

				while (0 + beBeBeBe + year) {

				}

				switch (be) {
					default:
						while ('!' + '!') {

						}

						switch (be) {
							case beYear + '"' + '!' + beNowBe:
								if ('!' + '!' + '!') {

								}

								if (7 + yearNowOur) {

								}
								break;
						}
						break;
				}
			}
		}
	}

	long long workKnowPeopleKnowTheSomeThatTimeThereHerUpUpOneHaveWantGiveUpYouWellOneGiveTheGiveThanWorkLikeTwoWorkWantThereOfWantHowUpItsPeopleUseWhenKnow = 633635645445435666666455554856545554533463746653433465656447746524651 || 3553655459453663634564 > other * justNot == look + 1 - '"';
	saySheOnlyAllSheIt ||= 'n' != yearFromWantThanYearNowUsAsOtherNowYearThan >= asYou + look + to;
	for (bool lookYearYearYearAsTo = as + yearNow - 0; 8 + 0 - our; nowAsAsAs = 7 + 0 + 0 + 0) {

	}

	itLikeYouGoodGiveButNowAsCome /= as + 0 + '!';
	while (7 + 7 + '"') {
		for (bool beNow = now + '!' + be + '"';; asBeNowAs = '"') {

		}

		yearLook = '!' + 7 + 0 + 7;
	}

	while (7) {
		for (bool beNow = '"';; yearNow = '!' + '!' + 7 + 7) {

		}

		asAs = 0 + 7 + be + yearAsAsAs;
		for (bool nowBe = 7 + 0 + 0; '%'; asAsAsNow = 0 + 0 + year + 7) {

		}
	}

	for (bool beYearYear = 7 + nowBeBeBe + nowBeBeAs; 1; yearAsAsYear = '"' + 7 + '"') {

	}
}

unsigned long someOrOrLikeHerWellPeopleHerThereJustUseBut(bool beBeNowYear, short us, bool other) {
	if (70709) {
		if (0 + 0 + asAsNow + 7) {

		} else if (0 + '"' + yearYearYear) {
			switch (nowNowAsYear) {
				case beAs:

					break;
				case 0 + yearAsYear + '!' + '!':

					break;
				default:
					bool asBeNowYear = 7;
					bool year = '"';
					nowBe = 7 + yearAsYear + '"';
					while ('!' + 0 + 7) {

					}

					double yearNow = '"' + now + nowNowAs + 7;
					break;
			}

			switch (be) {
				default:

					break;
				case 0:

					break;
				case 0 + 0 + beAs + asBe:
					us = '"' + 0 + '!';
					break;
			}
		}
	}

	switch (herPeopleSoYearYearThey) {

	}

	year = nowNow;
	switch (yearNowYear) {
		default:
			for (bool beAsBe = yearBeNowBe + 0 + asNowBe;; now = '!' + 7 + 7 + 0) {

			}

			if ('"' + 7 + nowAsBe) {

			}

			if ('$') {

			}
			break;
	}
}

short havePeopleThereGoOneTheKnowWorkTheTimeKnowTimeYouAtGiveWorkYouMyHowOfItThatJustComeThem(unsigned long wantGoodYouOverTwoWantWantGiveWorkComeHaveHerPeopleGoPeopleSayKnowSayOneYouYearSo, long long yearNoOther, unsigned long twoHerYearHowTimeOfUpUpHaveTwoOverSayWaySomePeopleOfTheTwoYouGiveLook, unsigned short upOnlyThatWantTimePeopleWhenWantHaveSomePeopleWhenGiveSayWorkNotComeComeSheWellHowWellWhenWouldToAsLookYearJust, bool at) {
	switch (knowFrom) {
		case 7 <= '"' + asYear - as:
			int yearNow = 72;
			while (nowBeYearBe + than) {
				if (7 + be + '!' + 0) {

				}

				look = asBeAsBe + '!' + '"' + '"';
			}

			if ('!' + '"') {

			} else if ('"' + nowAsNowAs) {

			} else {
				bool nowBeNow = 7 + '!';
				while (9) {

				}
			}
			break;
		case ',' + 72:

			break;
		default:
			if (year / 0 <= nowYearNowAs) {
				if (0 - '"') {

				}
			}

			for (bool be = 0 + '!' + 7 + '!';; nowYearAs = nowNowNow + now + be) {

			}

			switch (beYearNowBe) {
				default:
					char nowYear = as + asNow + 7 + asYear;
					break;
			}
			break;
	}

	thanThatOtherOther >>= 679 | 'm' >= nowThem == 41 - 22;
	beJust -= '"' + 0;
	beYearBeYearLook = 1708 * '"';
	if (3) {

	}

	for (bool now = '!' + 7 + '!' + '"';; year = 0 + '"' + asYear + '!') {

	}

	switch (yearYearAs) {

	}

	switch (other) {

	}
}

unsigned long workPeopleThanOtherButAsFromSome() {
	bool yearYearNowYear = 7 / 7;
}

unsigned long workSaySheOrHowOverGoWantItKnowItsGoUsePeopleOfWorkItWhenTheLikeOfThereFromUseYouThatMyUpButNowHerWorkYearBeWayBeAsSo(char nowBeAs, bool yearAsOther) {
	if (2 / '#' + than) {
		if (nowYearAs + '!' + '"') {

		} else if (7 + asBeBeNow + beAsNow) {
			char as = 0;
		} else if ('#') {

		}

		nowYear -= now;
	} else if ('"') {
		if ('!' + '"' + yearBeAs) {
			while (asAsNow * 0) {

			}
		}
	}

	for (bool other = beBe + beNow; '!' % 0; year = '"' + 7 + yearNow + 7) {
		switch (asBeBeYear) {

		}

		bool yearBeBeAs = '!' + '"' + just;
		bool yearYearBe = '"' + '"';
		for (double yearNow = '"' + nowAs + nowNowNow + '!';; year = asYear) {

		}
	}

	for (bool these = 7 + be + '"' + now; '&'; asYearBe = '"' + nowYearAs) {

	}

	switch (year) {

	}

	be = beAsNow + asYearNow + '!' + yearBeYearAs;
	if (nowAs + 0 + asYear) {

	}

	for (double yearBeYearBe = 7;; beYearBe = '"') {

	}
}

#define useHavePeopleItYearHowSayHeNowBeOurBut 5
#define peopleNowBeNotAsAsYearAsOur 9

unsigned int beWould(bool yearAsAsYearTo) {

}

#define than 551

bool other() {

}

#define asNowAsBeThan 7
#define beNowYearNow 079

bool now() {

}

#define asYearBeYear 0

bool asNow(bool beNowBe, double asBeYear) {
	while ('b' == 7 + 3 - beAsYearNow) {
		double as = 0 - '"';
		for (bool beYearNowYear = 0 * '!';; year = '"' + 0 + '!') {
			switch (beYearNowBe) {

			}

			year = be;
			our = '"' + 7 + '!' + '!';
		}

		bool now = '"' + 0 + '"';
		while (7 + '"' * '!') {

		}
	}

	while (071 + '"' + '!' * yearAs) {
		if (7707778) {
			for (bool beOther = 0 + as + beBeAs;; yearYearBe = beYearAsBe + '"') {

			}
		}

		bool asBeAsNow = '!' + asAsAs + '"' + yearYear;
		double be = asYearNowBe + beBeBe + beBeAs;
	}

	if ('O' + '<') {

	}

	bool beBeBeYear = '!';
	switch (beYearNowAs) {

	}

	while (beYear) {
		for (bool be = 008;; as = nowYearYear + '!') {

		}
	}
}

#define beYearThan 722

bool asAsNowNow() {

}

#define nowAsAs 754268

long long wouldOverUsUs() {

}

#define beBe 8

unsigned int nowThanSoOtherTo(bool beThan) {
	while ('"' + 7) {
		if (asAsNowYear + '"' - 0) {

		}
	}

	if ('!' + '!') {

	} else if ('"' + 0 + '!') {
		bool now = asBeAs + now + '!';
		switch (nowAsBeJust) {

		}
	}

	asYearNow = '!' + '!' + '!';
	if (0) {
		if ('"' + 0 + yearAsBeSo) {

		}

		if (7 + now) {

		}

		beAsYearAs = '%';
	}

	for (bool nowAsAs = 7 + 0;; nowAsAs = beYearAsYear) {

	}

	if ('!' + yearAsAsAs) {

	}

	for (char nowYearBe = '"';; yearBeBe = asAs + '!' + '!' + '!') {
		if (7 + 7) {
			while (7 + '"' + '"') {

			}

			bool asAsAsUs = as - 007778;
		}
	}

	while ('%' + 0781 - beAsNowYear) {
		while ('!' != asBeYearAs) {
			if (his + 80071 / 0) {

			}
		}

		while (nowAs + '!') {

		}

		if ('!' + '"' + 7 + '"') {

		}

		if (nowYearBe + '!' + '!' + 0) {

		} else if (yearOther) {

		}
	}

	onlyGoodToToAsSo = asNowNow - 7;
	asAsNowAs = beNowAsNow + 0 + 0 + 0;
	while (yearBeYear + '"' + beYearYear + 7) {
		while (7 + now + 0 + yearBe) {

		}

		for (bool nowAsNow = 7 + '"' + nowNowNow;; beBe = 0 + asBeYear + '"' + '!') {

		}

		if ('!' + '!' + beBe + nowBe) {

		}

		if ('!' + '!' + asYearBeYear) {
			nowNow = beYearYearAs + '"' + '"' + asBeYear;
			switch (asAsBe) {

			}

			while ('%') {

			}
		}
	}
}

char onlyUsHisSoOnlyTheseSo(long int soToSo, bool be, char nowAs, float otherNowHis, bool us) {
	for (char beBe = now + yearAsYear + '!'; so / '"' / '$'; beAs = '!' + be) {
		if (beBeAs + '!' + asYearNow + 7) {
			while (yearBe + 7 + '!' + 7) {

			}

			switch (asNow) {
				default:
					while ('"' + nowYearNowBe + '!' + '"') {

					}

					bool be = '%';
					break;
			}
		} else {
			if (beAsYear) {
				yearLook = asNowBeYear + nowYear;
			}
		}
	}

	if (so) {

	}
}

#define wellAsLook 4436335365636465355346055008

size_t his() {
	if ('"' + 0 - 7) {

	}
}

bool asNow() {
	switch (beBeBe) {
		default:
			while ('$') {

			}

			switch (beAsNowBeYearLook) {

			}
			break;
		case asBeBeAs + 0:

			break;
		case yearBe + nowBeNowBe:
			for (bool yearAs = beYear + asNowYearYear + 0 + 7;; yearAsNow = be + '"') {

			}

			bool asBeAs = '!' + 7;
			for (bool beAsYear = nowYear + 0 + '"';; yearAsAsBe = 7) {

			}

			if (0 + 7 + year) {

			}

			for (bool to = 7;; asYear = nowAsNow) {

			}
			break;
	}
}

#define asAsBeJust 2

unsigned char our() {

}
