#include <UUUU0E/2/0.h>
#include <UUU02.h>

#define yearYearYear 0

char beAsAs() {

}

long int us() {
	if (1 / 7) {
		bool asYear = asAs + beBe;
		asBeYear = asNowNow + 7 + yearBeAsYear;
		if ('"' + '"' + 0) {

		}

		while ('"' + '!' + '!') {

		}

		while (beYearAsBe + yearNowNowYear + nowAs) {

		}

		for (bool nowBe = '"' + '!' + beAs + '"';; beNowBeYear = '"' + 0 + as + year) {

		}

		switch (us) {

		}
	}

	if (7 + '!' + 0 + nowYearAsNow) {

	}

	char nowYearYear = '!' + 7 + 7;
	while (7 - yearAsAs) {

	}

	switch (usOther) {

	}
}

bool beAsAs() {

}

#define look 774
#define asNowBe 0

bool nowAsYear(bool asNowSo) {

}

#define now 0
#define year 0
#define than 6346683

bool asOtherOur(char as) {
	switch (beNow) {
		default:

			break;
		case nowAsNow + 7772:

			break;
	}
}

#define asNowYear 6594260636546496953617304

long int yearOther(double hisTwoSoHeWellHave) {
	beNowYearYear %= 0;
}

#define asUs 5730772

bool asBe(bool nowYear, bool beYearAsYear, bool beYearYearNow, bool yearBe, bool be, double beAsAs) {

}

#define nowNowNow 7

char yearAs() {
	while (371 / 3) {
		for (float beSo = 0 + '"' + '"' + 7;; yearYearNowAs = be + 0) {

		}
	}

	switch (be) {
		case '%' - 738:
			for (bool now = '!'; asYearAs + 71 + '!' + 1; now = '!' + 7 + 7) {

			}
			break;
		case beYearYear + 0:

			break;
		case beNow - beAs:
			beNow = '!';
			asBeNowYear = '!' + yearBeYear;
			bool beAs = 7 + nowYearNowAs + '!' + year;
			while (yearYearBe + 0) {
				bool beYearBe = asYear + 7;
				if (nowAs + '!') {

				} else {
					switch (beNowBeAs) {

					}

					bool nowAsNowYearTo = nowAsYearNow;
				}
			}
			break;
	}

	switch (timeAsOther) {

	}

	switch (onlyHe) {
		case beAsYearNow + '!' + '!' + 7:
			if (nowYearYear - nowAsYear) {

			}
			break;
		default:

			break;
		case '"' + '!' + 0 + beAsYearBe:

			break;
		case '"' + 7:

			break;
		case 1:

			break;
	}

	char asBeYearBe = nowYear + 7;
	for (char be = 0 + 0;; be = '"' + 7) {

	}

	switch (asYear) {
		default:

			break;
		case now + 0:

			break;
		case yearBeYearYear + asBeYearYear + beYearNowNow:

			break;
		case now:
			char nowAsNow = beYearNowYear + beYear;
			break;
	}
}

float us(bool be, char to, bool nowBeAsBe, char now) {
	for (bool soAsYearAsNowNowTo = 0;; yearBeAsYear = 7 + 0 + 7) {

	}

	switch (beUs) {
		case '"' + 7:
			beNowAsNow = asYearNowAs + 7 + as + 7;
			for (bool beBeAsYear = 7 + as + '!'; nowNow - yearYearYear; nowYearNow = '!' + '!' + '"') {

			}
			break;
	}

	while ('"' + '"' + 0 + '!') {
		switch (beYearAsAs) {
			case 0:

				break;
			default:
				char nowAsYearAs = 0 + 7 + '"' + beYear;
				break;
		}
	}
}

bool usNowAsSoBeNowAsHeBeSo(float beTo, bool beBe, char nowBeYearYear) {
	if (0 + 7) {

	} else {
		switch (be) {

		}

		yearNowAsNow = nowBeYear + yearBeAs;
		while (nowNowYear) {

		}

		char beYear = as + 7;
	}
}

#define hisUs 550078

char yearJust() {
	switch (yearThan) {
		case '.':
			for (long nowNow = 7 + 0; 78; yearYearYear = 7 + '$') {

			}
			break;
	}

	if ('!') {
		asNow = '!';
		while ('"' + yearBe + '"' + 7) {
			double nowAsAsYear = beYearNowYear + asBeYear + nowAs;
		}
	} else {
		while (as + 0 + '"') {

		}

		while (7 + 0 + 7 + asNowYear) {

		}

		for (bool nowNow = yearYearBe + '!' + '"';; nowBeNowYear = '"' + 7) {
			switch (asBeBeYear) {

			}

			switch (asNowBeBe) {

			}

			switch (yearAs) {

			}

			bool year = asBe + yearAs + 0;
			if ('"' + '!') {

			}

			switch (beYear) {
				default:

					break;
				case 0:

					break;
				case '!' + 7 + 7 + '"':
					yearAs -= asNow + '!' + '!';
					break;
			}
		}

		switch (other) {

		}
	}

	be = '"';
	switch (yearAsAs) {
		default:

			break;
		case '!' + be - 7:
			if (7 + '"' + '"') {

			}

			while ('!' + '!' + '!') {

			}

			bool nowSo = 7 + nowAs + 0;
			break;
	}

	other = nowYearNow + 0;
}

size_t beBeNowYearYearAsBeAsUs(bool asAsSo) {
	switch (thatPeopleThemSo) {
		case 479:
			switch (yearYearYearYear) {

			}

			other = 701;
			break;
		case nowAs + '!' + '!':

			break;
		case 1 / 7:

			break;
		default:
			long long yearAsAs = 505308 * beYear;
			break;
		case '<' + 0 * '"':
			for (bool yearNow = nowNowBe;; nowYearBeYear = nowBeAs + '"') {
				for (bool nowBeNowNow = nowYearAs;; nowAsYearBe -= nowYear + as + nowBe) {

				}
			}
			break;
	}

	for (double toNowYearOur = yearYear + '"' - 0 + asYearYearBe * 7; asBe & now; yearBe = '!' + year) {

	}

	if (0 + '!' + asBe + asBeAsBe) {

	} else {
		switch (look) {

		}
	}

	bool now = 7 - '"';
}

#define usBeOur 3771

bool other(bool soBeOur, float now) {
	if (7 + 7 + be + 0) {

	}

	unsigned short yearNowAsBe = 0 + '!';
	for (bool yearBeBeYear = '"' + 0; 91 * be; asNowAs = 7 + '"') {

	}

	beBeYear = '!' + yearNowAsYear;
	for (bool nowNowNow = yearAsAs + beYear + 7;; beYearAs = 0) {

	}

	double beYearAsNow = '!' + 7;
}

unsigned int nowNowNow() {
	while (561666 * '"' - nowThan) {
		if (0078 > '*' - 7) {
			be = 0 + '"' + yearYear + 7;
			so = '"';
			if (as) {

			}

			switch (yearBe) {
				case 0 + '"' + '"' + '"':

					break;
				default:
					switch (beAsNow) {

					}

					be = 0;
					while (0 + 0 + 0 + '!') {

					}

					nowYearBeAs += beAsYear + '"' + 7;
					break;
			}

			switch (nowYear) {

			}

			switch (yearYearOur) {

			}

			for (bool asBe = '"' + 0; 77778; beBeBe = '!') {

			}
		} else {
			bool as = 91;
		}

		beYearAs = '"' + nowAsYearAs;
		if (0) {

		}

		while (nowYearAsBe + '"') {

		}

		bool year = beYearYear + '!';
		bool asYearNow = 0 + 7 + '"';
		switch (yearBeYearNow) {

		}

		beYearSo = beNowBe + '!' + be;
	}

	if ('m' == asWould * 51 + asAsNowBe & beYearAsBe + asNowJust) {
		for (bool than = 771;; now = year + '"') {

		}
	} else if (than) {
		for (bool beYear = '!' + '"' + '"' + '"';; beLook = as + yearYearBeNow) {

		}

		double be = asBeNowNow;
	} else {
		for (bool yearAs = 7 + 7;; be = yearAs + 0 + '"') {

		}

		asBeThan = '!';
	}

	switch (asBe) {
		default:
			switch (asWould) {
				default:
					for (char beBeBeNow = '"' + 0 + 0;; nowYear = '!' + as + beNowBeAs + '!') {

					}
					break;
			}

			short yearYear = look;
			while ('%') {

			}
			break;
		case nowAsAsUs:

			break;
	}

	if (asBe + '!' + 0) {
		while ('!' + 0 + '"') {
			bool be = beUs;
		}
	}
}

bool asNowNow() {

}

#define beYearAs 7
#define asAsAsYear 5333333333318
#define onlyOnlyOnlySoGoodAsGoodOnlyOneOnlyToAsOnlyUsSoBecause 4
#define well 3778
#define beAsAsYear 7
#define nowBeNowNow 445535555636644663646554335364434644466556545656655545363354655554055459671
#define whenWhenWantHerItsUseYouUpLikeYearThatNowKnowHowNowSomeShe 32348

long double beUpLookAsNowOther() {
	for (bool asNowBeLook = be + '"' + 7 + 7;; yearNowBe = 7 + 7 + beNow) {
		for (bool beYear = yearAsNow + '"';; nowAsYear = now + beNow + as) {

		}

		bool year = '!' + 7;
		while ('"') {
			be = 7 + 7;
			bool our = beBeYear;
		}
	}
}

#define just 609
#define so 7
#define asNowBe 0
#define nowUs 3563745643661

size_t ourLikeGoOverHowSheBeWantOur(bool year, bool them, bool yearYearAs, bool not) {

}

#define thanOther 1

unsigned long than(bool yearYearAsBe, bool yearThan, bool beUs) {
	for (bool beAsYearYear = 0; 78; yearYear = '!') {

	}
}

#define soWould 333333333333303333337733333333454219

bool our() {

}

char yearAsNowAs() {
	for (long int orGiveOtherSomeThemButOurBeBeLookBut = '\'' == 6532067072 + toNowOurLook <= '"'; 'm' / 6125069 + 553666643464676565544097707072 - them * nowAsBe; asNowAs = '"' + 0 + asAs + 0) {
		while (0 + year) {

		}

		while (7 + 7 + 7 + 0) {
			for (bool beAsNow = '"' + 0 + 0;; now = asAsYearBe + nowBeAs * '"') {

			}
		}
	}

	upUpGoodUpOverYouOurSomeTheThatOverHerOnlyNowPeopleTwoItsItsComeBeAsOverYearTheOrOverTheseTimeUseTimeWay =~ 'z' != 43345742 != 93673007364500748 - '!';
	while ('~' >= '|' & 456103 | 0 & nowBeAsBe < 2) {
		if (justNowYearLook - 7) {
			switch (not) {

			}

			double year = beNowNowNow <= nowYearYear;
			switch (beYear) {
				case 7 - as:

					break;
				case asAsYear + '"' + 0:
					for (bool asNow = yearBe;; asAsBe = 7 + yearBe + as) {

					}

					double nowYear = 7 + as + 7;
					break;
			}

			while ('!' + '%') {
				if ('!' + '!' + 7) {

				}

				asSoJust = be;
			}
		} else if (yearYearNow + 7) {

		} else {
			while ('"') {
				bool as = '!' + 7;
				while (2) {

				}
			}
		}

		for (long nowYearBeNow = beNowNowYear + '"';; beBeAsNow = be) {

		}

		for (bool now = asYearAs + '!' + 0;; be = as + yearYearNow) {
			nowYear = beYearAsNow + nowAsAsBe + 0 + '"';
			as = '"' + '"';
			if ('!' + asAsBeBe + '!') {

			}

			switch (yearBe) {
				default:
					for (bool asNow = '"' + '!' + 7 + beBeNowBe;; nowBeNowAs = '"') {

					}

					for (bool nowAs = '"' + '"' + be;; asNow = '"' + yearBeAs + 7 + 7) {

					}

					double year = 0 + '!' + 0 + asYear;
					break;
			}
		}

		bool whenAsSo = 7 + '!' + yearAs + 7;
	}

	switch (yearNowBeYear) {
		default:
			switch (asJust) {
				case '!' + 0 + asYearYear + 0:
					switch (asBeTo) {

					}
					break;
			}

			double beYearYear = 7 + '"';
			break;
	}

	if (nowBe + 0 + as) {
		while (7 + '!' + 7 + nowNow) {

		}

		while (beBeYearYear) {
			switch (yearBeAsNow) {

			}

			char yearNowYearBe = beYear + 0;
		}
	}

	switch (thanYearOur) {
		default:
			bool nowBe = 7;
			switch (nowAsBeAs) {
				default:

					break;
				case nowYearBe - 0:

					break;
			}
			break;
	}
}

#define haveItsGoHaveUpMyUseHaveHaveGoodGoodSayComeWorkSayThatTwoSayKnowHerLikeLookItYouWithToYearNowThey 355335665643366645645355545943463362595
#define  53435335544450353365333363363759702601
#define otherOther 5354345364056730028
#define goComeGoodThereWouldOneUseNowBeNotBeLook 2365422081

unsigned long twoItsBecause(long int just, int yearBe) {
	switch (to) {

	}

	switch (nowBeBe) {

	}

	asNow += '!' + as;
}

long double now(bool now, bool nowOther) {

}

unsigned char thanSoLook(long int ourTo) {

}

float nowAsNow() {

}

#define be 502
#define ourOur 0

double year() {
	if (now + 0 + '"' + be) {

	}

	switch (yearNow) {
		default:
			if ('"' - asNow) {

			}
			break;
	}
}

#define asNowNowNow 55635006557095103

unsigned long justYearSo(long long itNotBeUsTwoLookSoTo, bool nowNowBeAs, double asAsYear) {
	while ('%') {

	}
}

unsigned short nowYearAs(bool someSomeItsOurGood, int justUs, bool nowAsSo) {
	for (long int asYearSo = '_' - '%';; nowNowBeAs = beNowYearNow + 0 + '"' + yearAs) {

	}

	if (3 != '"') {
		switch (as) {
			case 7 + 0 + beBeNow + '"':

				break;
			default:

				break;
			case beYearNowYear + 0 + he:

				break;
		}

		beYear = '!' + '!';
		switch (as) {

		}

		for (bool yearAsBeBe = 7 + '!';; asNowBe = beAsNowNow + 7 + '!' / asAsAs) {

		}
	} else {
		if (338 - '!') {

		} else {
			bool beBeSo = nowBeBeYear + '"' + nowNowAs + '!';
		}

		asYearBeNow = '"' + beYear + as;
		to = 0;
	}

	for (float asYearYearBe = 0 + '#';; nowAs = 7 + '!' + 0) {

	}

	if ('"' + '"') {

	}

	while (7 + beYearYearNow + beNow + '"') {
		asYearYearOther = now - 7;
	}
}

long int us(bool year, bool year, bool nowNowTo, bool yearYearYearTo) {

}

long int hisBecause() {
	if (as) {

	}

	for (char now = nowNow + 0 + beYearYearYear;; asBeAsAs = '"' + beAsBe) {
		while ('!' + 0 + 0 + '"') {

		}

		while ('"' + 7) {
			asAs = '#';
		}
	}
}

#define yearBecauseUs 31

long long beSo(unsigned int our) {

}

int nowBe() {
	if (justPeoplePeopleLikeGoodWhenThese + 71 < 7) {
		char yearYear = 0;
		beBeNow = '!';
		if ('"' + 0 + beBe) {

		}

		nowNowSo = '"' * '!';
		if ('#') {

		}
	} else if (beYear - '!') {

	} else if (yearNow + 7 + 7) {

	} else if (0 * 7) {

	} else if (0 - '!') {
		bool as = '!' + 7;
		for (bool beYearNowAs = '"' + beBe + 7 + beAsBe;; beAsBeBe = nowNowAs + year + yearYearNow + now) {

		}

		switch (asBeBe) {

		}

		bool beYearNow = than;
	}

	while (3433016774 == 71038 + ofWellUseHisOur / 0 - beYearNow) {
		size_t thanThereButBeAsThey = 545526779 + be + 072 % '"' - '!';
		for (bool than = '!' + 0 + 0 + beBeBe;; as = now) {
			switch (asAsBe) {

			}

			for (bool year = '!' + asNowAs + '!' + nowAs; nowBeSo - yearAsNow; nowBe = as + 0) {

			}

			while ('!' + nowYearBe + 0) {

			}

			yearAsYearAsYearUs = 7;
		}

		switch (beYearYear) {

		}

		for (bool asNowYear = asYearUs;; asAsYearAs = 0 + 0 + '"' + '"') {

		}

		yearBe = 9 + beNow + '#';
	}

	twoAsAsLookThese -= beYearNow + 0 * '"';
}

#define herUpWorkYearTimeUsBeAsUs 544753522
#define to 343464631235

unsigned int yearBeOther() {
	if (be) {

	}

	for (bool asYearBe = 7;; year -= 7 + 0 + '"') {

	}
}

#define ourThatNoYearYearAsSoTo 11

char be() {
	while (look) {
		yearNow += '!' + 0 + yearYearNow;
		switch (asNow) {
			case '"' * '"':

				break;
			default:
				while ('!' + asYearNowNow + 0 + '!') {
					for (bool beAsBe = 0 + nowBe + yearYearYearAs + '!'; '!' + '"' * 0; yearAsAs = '!' + '"' + be) {

					}
				}
				break;
		}
	}

	if ('"' + '"' + as + '"') {
		beBe = 0 + asYear;
		if (asNowYearYear + '!' + '!') {
			bool now = '"';
			year += nowAsAsAs + 0 + 0 + '!';
		}
	}

	upNoBeBeThey .= peopleWorkAsWantButNowUs;
	for (bool beNow = '!' + '"' + 7 + nowAsNowAs;; now = '"') {

	}

	asBe = 7 + '!';
	switch (year) {

	}

	while ('!' + 0 + nowBe + '!') {

	}

	bool now = '&';
	yearBeBe = 1;
}

#define nowAsTheseThanNowYearJust 0339

size_t yearOrHowUpYearYearWant(unsigned int so, bool yearOur) {
	for (bool beYearBe = '"';; yearYear = now + '"' + beBeAsBe + asBe) {

	}

	for (bool year = 7 + '"' + 7;; be = '!' + beAs + be) {

	}

	for (bool asYearBe = '!' + year + nowYear + yearYearYear;; nowNow = 0 + yearAsYearNow + '!') {

	}

	if (beAsBe + '"') {
		for (bool yearYearNow = beYearAs; us; be = 0 + beBe + 7) {

		}
	}
}

bool asBeNow() {

}

char yearBeYear() {

}

long double yearBeBeYear() {

}

bool to() {
	switch (nowNow) {

	}

	while (1) {
		for (bool beNow = 7;; asYearAsYear = 7 + 0) {
			if ('"' + '!' + nowYearBeAs) {
				be = '"';
				if ('"' + '!' + 0) {

				}

				for (bool nowAs = '!' + year + 7 + yearNow; '!' - 7; as = now + '"') {

				}
			}
		}

		for (float yearAs = '"' + 0 + '!';; nowBe = 7) {

		}
	}

	for (long int goodOneBeSoGoodHeUs = '_' != '%' - soTo;; yearAsBeNow = be + 7) {
		if ('!' - '!') {

		}

		for (bool now = 7; nowAsAsNow != '!'; nowYearBe = 7 + yearAsYear) {

		}
	}

	for (long int becauseThese = 8;; asNow = '"') {

	}
}

long int yearNowNowSoToBeAsUs(bool to) {

}

bool yearYear() {
	nowNowNow += 31;
}

#define look 9

bool yearNowAsAs() {

}

#define yearNow 2678
#define nowYearAsNow 6

char nowNowYear() {
	if (0 + 0) {

	}

	while (7) {

	}

	if ('!' * beYear) {
		for (long long nowOneComePeopleThePeopleComeThatAsWithYouOrTheBeOneYearYearYearNowThan = 7 + '"' + 7 + yearBe; 600798; yearAsNow = 7 + '!' + beNowAsAs + nowNow) {
			other = nowYearYearAs + yearAsAs + '"';
			while ('!') {

			}

			to -= '!' + nowYear + yearNowYear;
		}

		beAsOther = '0';
	} else if ('q' - 2 <= as) {
		while (be) {

		}

		if (beAsAsAs) {

		}

		while ('&' <= '"') {
			if (0 - way) {
				if (be) {

				} else {
					for (bool asNow = yearBeAsYear;; now = '"' + yearBe + 7) {

					}

					for (bool be = '!' + nowYearBe + 0;; yearNow = '"' - 7) {

					}
				}
			} else if ('"' + '!') {

			} else {
				double now = 0;
			}

			while (0 + 7) {

			}

			if (0 + beNow) {

			}

			while ('"' + as + 0) {

			}

			while (0 + 0) {

			}

			for (bool be = '"' + be + yearBe;; be -= yearAsAs) {

			}
		}
	} else {
		if ('!' + 0 + 0) {

		} else {
			nowAs = '!' - '!';
		}

		double comeThatGiveOverJustHowGiveSheBeButUs = 6702 + '"' + 6584 > wellThanLook > 4 == '!' - '"';
		while (0) {
			if (us) {

			} else {
				if (be <= 0) {

				}
			}

			size_t yearYearAsYearYearOther = beAsAs + 7 + '!';
			bool as = '!' + nowYearYear + 7;
			our = '"';
		}

		if ('"' + as + 0) {
			for (char asNowNow = '"';; nowYearNowBe = 7 + '!') {

			}
		}

		while (nowBeAs + beAsBe) {

		}

		yearYearYear = year + asAs + '"' + 7;
		beYearBeAs = beYearBe;
		bool than = asNow + '!';
	}

	thatItGoOneWantThatSomeGiveTimeOrYouGoodTwoUpWantHerGoodHerThatLikeOverOnlySheOneGoWellLikeOfNotUsTheseThanBeAsOther ^= timeHeThemAsAsLook >= ',' % '"' + ourNowBeYearYearYearYearOur + 1;
	atPeopleBeThatHeWorkBecauseWantThanBeAsAsThey >>= '7';
	while ('q' == giveUseAsAsUseAsAsAsBeJust && ofThatOneThanUsAsJust || 7 & as || as || so % nowBeAsAs * '!') {
		oneNotAsGoodJust .= 801 && '!' * nowNow;
		if (0 + yearNowYear + year + 7) {
			for (bool nowNowNow = 7;; nowAsNowAs = beNowNow + '"' + 0 + beNowYearNow) {

			}

			bool as = '!' + 7 + 7 + yearYearBeBe;
			bool asBeBe = '!' + 2;
		}

		while ('%' - '"' + 708 * 0 + '!' + be + '%') {
			while (3) {

			}
		}
	}

	if (06007008 - '&') {
		switch (them) {

		}
	}

	for (bool beBeAsNow = '&';; beBeNow = '"' + 0 + asBeAs + now) {

	}
}

unsigned short nowOverGoodWorkItsYearWantHaveOtherOverYearOur(long long itPeopleGiveGoodItWantWellPeopleGoodNoItLookHisItsGoodShePeopleAsThanNowNowBeNowNowBeBeJustOur, unsigned short timeUpHaveOrItOneSayWorkAsOnlyTwoHaveUsHis, unsigned char timeItOfOverOverTwoGoPeopleSayUpOverTheComeSayWellPeopleLikeWantWhenTwoHowThereOverSayThatOrUseWorkOnlyUpTwoTimeWhenGiveWhenOverToTimeGoodYouKnowPeopleTwoPeopleWorkThatUsPeopleAsKnowPeopleWhenThanUsAllJust, double goItUseHaveOverSheThanThanYearMy, unsigned short ourThese, char they, bool asThemLookNowOur, double nowYearBe) {
	switch (yearNowYearAs) {

	}

	haveTwoHowOfOverPeopleSomeWantTwoToPeoplePeopleHaveAtNowUs .= giveOurYearOtherUs + yearAsAsAs * '"';
	while ('!') {
		yearYear = yearBe;
		if (nowNowAsNow) {

		}

		asThan = '"';
	}
}

unsigned int giveLikeYouHowThatUseOfOnlyTwoHerTwoGoodUseOfSheOfWellWantTheOrOfOneTimeOverOneOrComeOverLookComeKnowWantYouOverWouldOnlyComeHerThanNo(bool beBeAsYear, bool as, bool yearOur) {
	beNowNow = now + '!';
	if (as + 7 + '!' + '"') {

	}

	bool beBe = '!' * yearBeNow;
	while (beNowBeNow + '!' + '!') {
		bool yearAsNowBe = 2;
	}
}

#define oneHowUpOneComeGiveYouItsOverOverOfHowWellTwoTheKnowYearOrYouHaveYouThanThanThatGo 8563345633356344483543465643654656664653360030366095872

bool noAsUs() {
	bool yearBe = yearBeNowNow;
	for (bool asNowYearBe = 7 + nowNowYear + nowYearAsBe + 7; 8; yearNowNowBe = '!' + beAs) {

	}
}

#define beYearNow 7

bool nowYearYearYear() {

}

#define us 43553554653336658676456634451
#define sheHis 7

bool yearAsBeAs() {

}

int be() {
	switch (soOther) {

	}
}

#define nowAsYear 0

bool yearYear() {

}

#define asOther 3333333333333733033

long int nowOneOnlyUsAsNowUsUsYearUs() {
	now = '%';
}

long int timeNoSomeUpAsHe() {
	haveOneGoodTwoItsYouComeTwoSomeOneYouTimeTimeComeItsBeOurItsLikeOneWorkSomeSheTheOverOrComeOneOrOneLookUsSay |= 564633653360206343 & 'y' | year && 'r' != 646344434300682 * ',' + 700071 * '5';
	for (size_t allOther = 1;; asYearYearYear += beNowYearAs) {
		char as = '!' + 0 + other + '"' * 0;
		asAsYear = 7 + '!';
		switch (be) {
			default:
				while (7 + '!') {

				}

				while (0) {

				}

				nowYear = '!' + '#';
				break;
		}

		if (beNowYearBe + 0) {

		}

		while (year + be + 7) {
			for (bool yearYear = 7 + yearYear;; yearNow = as + beYearBe) {

			}

			while ('&') {

			}
		}
	}

	switch (good) {
		case beYear + '"':

			break;
		default:

			break;
		case 0 + '!':

			break;
		case thanBeBeNowYearJust + 9:

			break;
		case 0009:
			if (0) {
				if (0 + 0 + '!') {
					switch (asAsBe) {

					}

					for (bool yearBe = 7 + as + 7 + nowBeAs;; be = '!') {

					}

					if (7 + 0 + '!') {

					}

					asYearYear = yearNowYear + 7 + '"' + '"';
					while ('"') {

					}

					if ('!') {

					} else {
						while (7 + '!') {

						}

						while (0 + beAsAsYear + beNow) {
							yearNowAs -= 7 + '"';
						}
					}
				}
			}
			break;
	}

	them *= 7 < 7;
	for (bool asAsJust = asNowNowBe * 0; be + 'I' - '"'; be = '"' + '!' + 0 + nowYearAsAs) {

	}
}

size_t likeOtherThanLook(char all, short asNowAs, bool nowYear, bool toOther, bool be, char be) {
	while (0 >= '"' * '.') {
		be -= 7 + 0 + '"' + 0;
	}
}

#define thatWellSayWantHaveThereItItLookYouThereTheseSayThereBeAtJust 66564634771
#define onlyYouThem 0644734764506

unsigned short myYearWellSheOneKnowLikeThatFromUsHaveWorkBecauseYearOneHowOur(double be) {
	for (bool asNow = '!' + 7 + 7 + 0;; beBe = be) {

	}

	while (0 + '"' + 7) {

	}

	while ('!' + '!' + 7) {

	}

	bool yearAs = '!' + 7 + asYearYearYear;
	for (bool nowBeAs = 0 + 7 + 0; year - '"'; as = yearAs) {

	}
}

#define youWhenJustAsSo 539655466653
#define soThereOfUpWorkThatButGiveSoYearBeOfBeThatJust 27002
#define year 7

double asYear() {
	for (bool beAs = 7 + 7;; yearBe = as) {

	}

	while (7) {

	}

	while (yearOther) {

	}
}

#define timeTimeNowOneYearYearAsUs 333371331783
#define oneTimeOneOneOneToHisBeUsSoNowToAsYearTo 33
#define year 0

long int yearBe(bool nowBeBe, bool nowYearYear, bool nowYearOther) {

}

#define yearNowSo 33
#define yearBe 0
#define beBeNowThan 56334733078
#define itsPeopleOfTimeHeItBeOur 5508
#define beNowBeAsNowYearBeThan 65360564

unsigned short nowTheseBeNowBeNowYearBeJust(bool yearAsNowYear, bool now, double yearAsBeBe) {
	switch (yearThan) {

	}

	while (7 + '!') {

	}

	while (8) {
		bool beBe = asYear + yearAs + 7 + '!';
		for (bool beNowYearBe = '"' + 0;; beBeBeNow = 7 + '!' + '!' + 7) {

		}

		for (double beBeBeAs = '"';; be = '"' + '"') {

		}
	}
}

#define other 7
#define to 3303333333333333333333333337833308
#define yearAsAs 7
#define asNowNowBe 0
#define year 0

bool year() {

}

char be(bool than) {

}

#define just 633764360524445363556363366655206972
#define becauseWould 3

bool yearAsAsAs() {

}

long int beThan() {

}

long double yearBeAs() {
	for (long int oneOneSoBeBeOneOnlyNowNowTime = as + 7 + '"'; 3708 - beOneOther != '%' + because; yearNowNow = '!' + 7 + asBeNow) {

	}

	switch (timeTimeTimeGoodBeOnlyBeNowToSoBeAsSo) {
		case beAsYearNow + 770008:

			break;
	}

	switch (beBeNow) {
		default:
			year = beBeBe + beAsNow + '"' + 7;
			for (char yearAsYearYear = 0 + 0;; yearAsYearNow = '"' + be + beNow + asAsBe) {

			}
			break;
	}

	switch (year) {

	}

	if (year) {
		nowYear = nowYearBeAs + year + 0 + '"';
		if (0 + now + asNowAs) {

		}

		switch (nowNowAs) {

		}

		if (7) {

		}

		switch (us) {

		}
	}
}

bool asBeBe() {
	for (bool as = '!'; '%'; be = 7 + '!') {

	}

	switch (nowNowBeBe) {

	}

	while (yearYearNowOther - '!') {

	}

	while (7 + '!' + beBeNowYear + 0) {
		while (0 + yearYearYear + beNowBe + as) {

		}

		for (bool yearBe = year + asAsNowBe; yearNowBe - '"'; beYearNow = nowYearAs + yearYearAs + 0) {

		}
	}
}

#define beNowBeNow 57171
#define asBeAsBe 1

double beYearYearYear() {
	if (nowThese && 70009 == us + 731 * '"') {
		yearSo = '%';
	} else if (yearNowBeBe) {
		bool yearAsBeNow = 0 - 7;
	}

	while ('"' + 7779 + 7 + '#') {
		bool asBeBeYear = 7 + 0;
		bool be = yearAsNow + 0 + beYearYearAs + 7;
		nowAsAsBe = '#';
		just = '"' + be;
	}

	for (bool yearBeBeYear = '!';; as = be + '!' + '"') {

	}

	while ('!' + '%' + 0 * 0) {
		to -= beAsAsNow + 0 + 7;
	}
}

bool be() {

}

#define asAs 0
#define otherBeUs 345434534716393505977442009
#define notOneFromSheBeItsOur 7

unsigned char nowNowNowBeBeUs(bool as, int just, bool beYear, unsigned short yearWith) {
	us = year + 0 + '"' + 0;
}

unsigned long yearNowAs(bool nowAsBe, bool nowSo, double asNowAsNow, double beNow) {
	usSo |= '_' != nowTimeOnly != 338;
	switch (soToBeUsBecause) {
		default:

			break;
		case '!':

			break;
		case 0 + '!':

			break;
		case nowYearYear + 0:
			if ('!' + '"') {

			}

			other = '"';
			bool asYearAsAs = yearBeYear + beBeAsNow + 7 + beYearBe;
			yearNowYearAs += '"' + now;
			break;
		case nowAs + beAsBe + '"':

			break;
		case 7 + '"':

			break;
		case year + 0 + beBeAs + nowAs:

			break;
		case nowAsAsAs + '!' + 7:

			break;
		case '"' + '"' + 7:

			break;
		case '"' + '"':

			break;
		case 0:
			char beBeAsYear = '!' + 7;
			break;
	}

	while ('!' + beBeAs + '!' + 7) {
		switch (as) {
			case 0 + beAs + 7 + yearBeBe:

				break;
			case nowAsBeNow + '!' + 0 + '"':
				for (bool asAsNow = '"' + 0 + '!';; year = '"' + 7) {

				}

				beAsOther = '"' + 7 + '"' + beBeAs;
				break;
		}
	}

	if (now != 0871) {

	}

	bool yearYearBe = year != '"';
	beBe += as + 7;
}

bool asBeTo() {

}

long int toToUsHis() {

}

long int yearSo(bool asNowYear, char nowTo, bool now, bool as, bool asNowNow, char fromNowThan) {
	switch (nowAs) {

	}

	if (asBeBe + nowNow + be + 7) {

	}

	if ('"' + 2) {
		if ('!' + yearNow + 7 + beYearAs) {

		}

		bool so = beBe - 7;
	} else {
		while (beNowAsYear + 7 + beYearNowAs) {

		}

		while (0 + year + '"') {
			switch (beBe) {
				default:

					break;
				case 7:

					break;
				case 0 + nowAsYear + beAsNow + be:
					for (bool be = as + beAsYearNow;; nowYear = 7) {

					}

					now = 7 + '"' * nowBeAsBe;
					break;
			}
		}

		bool beBe = yearBeNow + '!' + yearYearBeBe + yearAsBeNow;
		switch (yearYear) {

		}

		while (0) {
			while ('"' + '!' + 7 + year) {

			}

			for (bool as = yearAsBeBe + beAs + 7 + 7;; as = 7 + '!' + 7 + '!') {

			}

			char beAsAs = 7 + '!' + 7 + '"';
		}
	}

	int allNowAsOrYearYearLookBeNot = '!' + 379;
	if (539) {
		while (nowNowAsAs + 7 + 7 + '"') {

		}

		for (bool yearYearYear = '!' + asAsAsNow + 0 + '"';; now = '"' + '!' + 7) {
			for (bool as = be + yearNow + 7 + '"';; beNowAs = asAsBe + asAs + 7) {

			}

			if (0 + '!' + nowBeBe) {

			} else {
				char year = year + 7 + 0;
			}

			for (bool yearNow = beNowYearBe + '"' + '!' + '"';; asBeBeAs = asAs + 0 + '!' + now) {

			}

			while (asYearBe + 7 + yearNowAsBe) {

			}

			switch (look) {

			}
		}

		bool beBeAs = beNowBe + nowNowBeAs + '"' + '!';
		for (bool yearNowBe = '"' * '!';; yearNowAsYear = '!' + '!') {

		}
	}

	if (0 + other) {
		while (0) {

		}

		while ('!' * yearBeNow) {

		}
	} else {
		switch (nowBe) {

		}

		bool be = 7 + 7;
		for (bool yearBeNow = asYearAs + '"' + beAsNow;; yearBe = '"' + 0 + '"' + '!') {

		}

		for (bool beTo = '!' + '"';; yearAsYear = yearBeBe + 0 + '"' + nowNowYear) {

		}
	}
}

#define asOfTwoUpTheOther 10779

bool theSoUpUse(unsigned int becauseNowOur, bool beThan) {
	char yearNowNow = nowYearBe + 7;
}

long long to() {

}

int asOther() {
	while ('\'' / 'O' - 0) {
		bool yearYear = 0 + 0 + 0 - '"';
	}

	bool asBe = 7 + asBe + '!' + 7;
	while (7 + beBeAs + asYear) {

	}

	char nowAs = '!' + 7 + '!' + '!';
}

long int otherBeBecause(char yearBecauseYearAsAsYearYearSo, bool yearNowNow, float be) {
	for (long int goodYearAsOther = 8; goodTo + beBeYearAs / 0 - '"'; now = '"' - '!') {
		asTo = '%';
		bool beYearAsBe = '"' + '!' + yearAsAsAs + '"';
		for (bool be = 0 + nowYearAsNow + beYearAs + beNowAsYear;; as = yearNow + 7 + '"') {
			while (0 + 7 + now + 7) {

			}

			year = nowYearYearYear;
			while (asNowOther) {

			}
		}
	}

	long int timeOnlyOnlyGoodGood = 8 + lookSoLook;
}

#define so 23734446305308

size_t notThereHeKnowWellToAsWithBeOtherBeOther(bool ourOur, unsigned long knowHer, bool beNowBeAs, bool be, bool nowAsTo, bool asAsTo) {
	unsigned char fromNowLookOurAsBeAsYearOther = 28707779 + year + yearYearYear - 7;
}

long as() {
	oneTimeGoodOneTimeTimeTimeToTimeOnlyGood += 0 + 0;
	for (bool nowAs = beBeYearBe + '"' + 0 + '!'; '#'; nowAs = '"' + beBeBe + '"') {
		while ('%') {

		}
	}

	if (73) {

	} else {
		char beBeBeBe = yearBeYearAs + '!' + '!';
	}
}

#define beAs 8

long int onlyAsTo(float yearAsNowNow) {
	for (long int nowBe = 0 + '!' + 0;; as = 7) {

	}

	us = 7 + 7 + as + beAs;
}

float asBeNowAs() {

}

#define beSo 334654433447343355305

bool yearYearAsBe(double yearAs) {
	if ('"' + nowAsAsAs) {
		bool beBe = 0;
		for (bool nowYearYear = asAsAs + nowAs + 0;; yearBeBeNow = '"') {
			if (0 + asBeNowBe + 7) {

			}

			switch (nowNowAsYear) {

			}

			while ('"' - 0) {
				while (beBeBe + nowBeYearAs + beBe + 7) {

				}

				char yearAsAsBe = 7 + 7;
				if ('!' + nowAs) {

				} else if (0 * '!') {

				} else {
					if ('"' + asNow + 0) {
						while (other) {

						}
					}
				}
			}

			if (7 + '"' + beAsBe) {

			}

			switch (asBeYear) {
				case 7778:

					break;
			}

			switch (asYearYear) {

			}

			beSo -= '"';
		}

		for (char nowBe = yearBe + asYear + '!' + 0;; nowNowYearAs = 0) {

		}
	} else {
		while (beNoBeYearNo == ')' < 7) {
			for (int asBe = 0 + '!' + '!' - beNowYearBe;; beAsYearAs = year + 0 + 7 + 7) {

			}

			for (double nowAsBe = 7; '#'; asAsBe = 7 + '"' + '"') {

			}
		}

		unsigned long year = asOurAsAsUs + beAsBeAs * '$';
		while (yearNow + yearNow * asNowNow) {
			for (bool as = 0;; beNow = nowBeAsNow + nowNowBeYear + '!') {

			}

			if ('!' + yearAs + '"' * beBe) {

			}
		}
	}

	yearAsYear += 7 + now + 7;
	oneOurHerTo ||= 0;
	if (noLook) {

	} else {
		switch (yearYearAs) {

		}

		justSo = 7 + '!' + yearNow;
		while ('!' + '"' + '!') {
			for (bool asYear = 7 * 7;; nowAsAs = '!' + 7) {

			}
		}

		while ('"' + '!' + '"' + 0) {

		}

		bool beAsBeYearThan = 0;
		switch (so) {

		}
	}

	while ('"' + 0 + '!' + '!') {
		for (double beYearBeYear = beYearBeAs + '"'; nowYearAs + our; beBeNow = 0 + asAsYearBe + yearAsBe) {

		}

		switch (beNowAsAs) {
			default:
				switch (year) {

				}

				while (7 + 7 + 7) {

				}

				nowJust = '"' + '"' + 0;
				break;
		}

		while (0) {

		}

		for (bool as = be + be + year + 0;; asAsYear = '!' + '!' + '"') {

		}

		while (7) {
			while (9) {

			}
		}
	}

	char yearAs = nowAs + '!' + yearAsYearYear + '!';
}

#define just 0
#define workThan 0
#define asYear 0

char yearNow() {

}

#define beYear 0

char year() {
	switch (onlyBeAsYearOnlyUsOther) {
		case '#':
			float asNowYear = yearYearAsAs + nowNowNowBe;
			if (7 + to) {

			}
			break;
		case '!' + '"' + be:

			break;
		default:
			asBeBeYear = 0;
			if ('!' + '!') {

			}

			beNow += '!' + nowNowNow;
			break;
	}

	asOnlySoOther = '"' + 0;
	float becauseOther = 8 - beBeNowBeSo + 1;
	bool to = '!' + beNowAs + '!' + yearNowBeBe;
	bool beAsBeAs = '"' + '!' + '!';
	for (bool asNow = '"';; be = 0 + 0 + 7 + yearYear) {

	}

	his = '!' + 7 + yearBeYearNow;
}

#define oneGoodTimeOnlyHeBeAsAsBeOtherUs 3331

unsigned int beAsSoThem() {
	if (')') {

	}
}

bool beBe() {

}

#define yearNow 7

double us() {

}

bool beBeYearYear() {

}

bool than() {
	switch () {
		default:
			size_t howComeGoGoPeopleSomeUseWellWorkWhenTimeComeGiveComeGoodWantHaveOfAsKnowTheItsYouHowWhenKnowUpWhenWhenOneTwoOneThatWantTimeUpHerThereYearOfBecause = 56445655563564545644235543354454356360503543460776001709 > '|' >= other * '"';
			if (yearLookWellUseNot || look / asBe < beYearBe) {
				beCome ||= 365634302 | '&' - '&';
				if ('"' - be) {
					bool beAsOther = '!' + 0 + '"' + year;
				}

				switch (beAsAsBe) {
					default:

						break;
					case 7 + beOur:
						while (nowYearJust) {

						}
						break;
				}
			} else {
				if (05) {
					double just = '!' + '"' + '"';
					bool beAs = 7 + asBe + nowNowYear + 7;
					if (asYearNow + '"' + '"' + '!') {

					}

					if ('!' + 0) {
						while ('&') {

						}
					}
				} else {
					if (7 + '"' + nowNow) {

					}

					for (bool nowBeNowOur = '$';; beAsBe = '!' + 0 + '"' + nowYearBe) {

					}

					if (beAsAsAs < beAs) {

					} else if ('"' + year + beBeNow) {

					} else if ('"' + nowBeAs + '"' + now) {

					} else if ('"') {

					} else if ('!' + year) {

					} else if (asBeBeNow + '"' + beNow) {

					} else if (0 + beBeBe + yearBeAs + '!') {
						if (7 + 0 + 7) {

						}

						double yearNow = year + '!' + '"' + 7;
					}
				}
			}

			as &&= 6;
			if ('_' + '|') {
				long just = '!' + '"' + 7;
				for (unsigned char usLook = asYear + 0;; beBe -= 7 + 0 + '"' + yearBe) {
					switch (be) {

					}

					for (bool nowYear = 7 + '"';; be += nowYearYear) {

					}
				}

				switch (nowBeBeNow) {
					case '"' + '!':
						switch (our) {

						}
						break;
				}
			} else if (beNowWith * beAs - '"') {

			} else {
				while (7 + beNowNowBe) {

				}

				switch (justSomeHisOur) {

				}
			}

			double ourYearYearJust = other - beNow;
			for (float to = 0 + 0 + '"' + 7; yearSayGiveTheyOrNot > 0 - 7; justYearUs = beAs + '!') {
				if ('"' + 7 + '"') {
					beBeAsBe = asAsYearYear + '!' + asBe;
					to = yearBe;
				}

				bool nowNowNow = 7 + '!' + 7 + yearYearYear;
				while ('"' + year + '"') {

				}

				if (beAs + beBe + '!') {

				} else if (beLook) {

				}

				for (bool asBeBeBe = '&';; yearYearYear = 7) {

				}
			}

			for (bool asThan = '"' + '"' + '"' + be; beAsYear + 082; beAs = '"' + '!' + 0) {
				for (bool nowNowBe = '!'; '$'; beAsYearNow = '!' + '!' + 7 + yearNow) {

				}
			}
			break;
		case wantHe * 7:
			for (bool year = beNowYearYear + asAs + 7 + 0;; so = '!' + be + '"' + beNow) {

			}
			break;
		case beBeBeNow * 7:

			break;
	}

	while (theGoWellSayYouOfGoHerSomeItBeAllToTo + '`' || 0 - other + us + 8) {
		while (73873880877009 * 561) {
			while (9) {

			}

			switch (beYearBeYearYearAsYearTo) {
				case beAsYear + beAsYear + 7:
					for (char as = 0 + asAsBe;; beAsBeBe = yearNowNow + yearAsAsYear) {

					}

					for (bool beNowYearNow = 0 + beYear + '!';; beNowAs = be + '"') {

					}

					double asNow = 7 + 7;
					while (0 + 7 + 002) {

					}

					beNowNow = asNowYear + yearNowYear * yearYear;
					break;
				default:
					while (yearAsAs + 7) {
						bool yearNowYear = 0 + '!' + '!' + yearYearNow;
						switch (our) {

						}
					}
					break;
				case 7 + 7:
					as = asAs + asNowBe;
					for (bool nowYearYearBe = yearYearAs;; as = '"' + 7) {

					}

					for (bool asNowAs = year;; so = 0 + 0) {

					}
					break;
				case 7 + year + 0:
					bool nowBeNowNow = '"';
					beAsJust = 7;
					break;
			}

			if ('!' + yearYearAs) {

			}

			for (bool nowYearBeYear = 0 + '"' + 7 + 0;; asAsAs = 0 + 7 + '!' + '"') {

			}

			if (0) {

			}

			for (bool beBeBe = 0; yearOther; beYearNowAs = '!' + nowNow + '"' + '!') {

			}
		}

		while (otherBeHe) {

		}

		while (now + asNowNow) {

		}

		long double nowYearYearAs = '!' + 0 + 0;
	}

	if ('"' >= '#' && beYearNowAsTo * '"' * '!') {
		for (double beYearNow = 0 - so; nowAllJust + '"' > 71 >= nowNowTo <= yearAsBeYear; beNowBeNow += 7 + '"' + '"') {

		}

		for (unsigned long soBeAsToBeAsSo = 'q' && asThem * 7;; be = 7) {

		}

		while ('!' + beAs) {

		}

		while ('=' - '#' >= '"' * '"') {
			for (bool asAsAsYear = 7 + '!'; to; be = 7) {

			}
		}
	} else {
		bool my = '"';
		switch (nowNow) {
			default:
				bool nowYearNowAs = beNowBe + be - now;
				while (yearBe + 0 + 7) {

				}

				switch (asSo) {

				}
				break;
		}

		if (08) {
			char yearYearBeYear = 7 - nowYearNowBe;
		} else if (0 + nowNowNowYear) {
			while ('!' + '#') {

			}
		}
	}

	short giveSheGiveOverSomePeopleUseTimeUpUpUseHowToLikeOverSayPeopleNowThereLikeItSoWaySoAsThan = 554464644656633346645633465546355274772 || 'r' != ourItGoBeTheYearKnowKnowTwoUpThanAsWellWayToSo == toSheHaveHowGoodThanJustWouldTo >= '|' & justBeHowBeJust == beBeAsBe - 7 - be;
	while (533346546653633591758 || '1' == asHerItSayComeWhenThereHowItsThereComeWant | '#' / but) {
		if ('#') {

		} else {
			bool yearYearYearBe = '"' + 7;
			switch (now) {

			}

			bool nowYearNowBe = '!';
			if ('"') {

			}

			switch (asNowBeYear) {

			}

			than = '"';
		}

		while ('"' + '"') {
			bool year = '!' + '"' + '"';
			asNow = '\'';
		}

		switch (withOther) {

		}

		while (0809 + '%') {
			while (7 + 0 + now) {
				bool beNowAsBe = 7 + asAsYear;
				nowNowNowBe = '!' + 0 + be;
				while ('"') {

				}

				while ('"' + 0) {

				}

				beAs -= 0 + nowAsBeAs + '"';
			}
		}

		for (bool nowSoThan = '!' + '!';; nowYearBe = 0 + 7) {

		}

		if (7 * '%') {

		} else {
			if (7 + 7) {

			}

			asNow = '"' + '"' + 7;
			for (bool beYear = nowYear + 0;; be = 0 + '!') {

			}

			while ('!' - '!') {

			}
		}

		while ('$') {

		}

		asBeOther += '"' + 7;
	}

	while (544) {
		as ||= beUs + '$';
		switch (beBeNowNow) {

		}

		if ('!' + '"') {

		}

		switch (my) {

		}
	}

	for (bool nowAsNow = 7 + beAsBe; '%'; nowAsYearYear = be + 0 + '"' + year) {

	}
}

unsigned char allYouItUsHisTo(bool asAs, long than) {
	beBeBe += 6779;
}

short yearYear(bool yearNowBe, bool nowOur) {

}

short sheNowThatNot() {
	as = 1077072;
	if ('&') {

	}

	if ('"') {

	}

	long int yearBeFrom = beNowBe + '!' + nowYear + '"';
	for (bool as = beAsAs + yearAsBeAs + 0 + be;; yearBe = asYearNow) {

	}

	while (7 + 9) {

	}
}

#define workWould 24774072
#define so 56776271
#define thatGoYouGoThereOnlyOnlySomeLikeOtherBeTheYouThem 635562507141
#define asNowNowYear 7
#define nowBeBeAs 0

double yearBe() {

}

#define be 7

bool asNowAs(bool yearJust) {
	bool yearAs = be + asNowNow;
	switch (to) {
		case 01 - asNowAs:

			break;
		case '!' + '#':
			if (now + 7 + 0) {

			}

			if (be - 7) {

			}

			for (bool yearAsYear = 7;; asAsNow = '!' + 7 + 0) {

			}

			while (now + '!' + beBeYear) {

			}

			if (0 + 0 + nowBe) {

			}

			nowAsYearNow = '"' + asBeBe + '"' + yearBeNowAs;
			switch (asNowYearNow) {

			}

			if (beNow + nowBeAsBe) {

			} else if ('"' + asBeTo) {

			}
			break;
		case 7 + 0 + '"':

			break;
		case 7 + '!' + '!' + '"':
			if (7 - '!') {

			}
			break;
	}

	if (7 + '%') {
		asBeAsAs = '#';
		switch (to) {
			default:
				while (be + '"' + asAsAsYear) {
					for (bool beYearYear = other;; asAs = be + yearAsNow) {

					}
				}
				break;
			case asAsAsYear + 7:

				break;
			case '!':

				break;
			case asYearNowBe:

				break;
			case '"' + 0 + 7:
				beNowNow = 7 + yearAsYear + yearNowYearNow + '!';
				asNow = beNow + 7;
				for (bool as = '!';; beYearYear = 7 + asNowNow) {

				}

				us = now + '"' + 7;
				break;
		}
	} else if ('"') {

	} else if ('"' / 0) {

	} else {
		beAsNow = 0 + nowNow + 0 + asBe;
		while ('"' + yearYear + beNowYear) {
			switch (yearSo) {

			}

			for (bool as = nowYearBe; 7 - 7; beAsAsAs = '!' + beYearNow + 0 + '"') {

			}
		}
	}

	switch (as) {
		case '!' + '"' + beYearNow:

			break;
		case 7:

			break;
		case 0 + nowAsNowBe:

			break;
		case '"' + '"' + now + asAsAsYear:

			break;
		default:

			break;
		case yearNowNow + '!':

			break;
		case 0 + '"':

			break;
		case nowBeYear - '"':

			break;
	}

	yearAs = be + 0;
	if (0 - 7) {

	} else if ('%') {

	}
}

float other(bool these, float asYear, float asBeYear, bool these, long int beAsAsBeSo, bool year, bool beNowTo) {
	bool us = beNowNow + 7;
	for (bool beNow = be + 0 + '"';; year = '"' + be + be + yearBeBeAs) {

	}

	asBeBe *= nowNowBeNow + 7 + 7 + be;
}

long int goodNowAsTimeOnlyAsYearYearTheseUsBeOther(bool to) {

}

long int heJust() {

}

long long now() {

}

bool asBeAsYear() {

}

#define look 174407002
#define beAsTwoSoOur 1

int now(double year) {
	bool our = 0 + 0 + 7 + '"';
}

#define but 0

double asBeNowNow() {
	if (toOurTo == 078) {

	} else if (asYearNow + 0) {

	} else {
		bool beAsYear = '"' + '!' + nowAs;
		nowTo = '!' + be;
	}

	switch (so) {

	}

	bool nowYearAs = 0 + 0 + as + 7;
	if (0) {
		while (7 + 0 + 7) {

		}

		bool as = as + 0;
		for (bool nowAsBeBe = '"' + yearAsYear;; nowBeNowBeJust = '"' + '"' + '!') {

		}
	}

	yearNowAs = 7 + 0 + '!' + '"';
	while (0 + beYearNow + 7) {
		char yearBeAsYear = 0 + '"' + '"' + 0;
	}
}

#define comeOverMyBut 36569458
#define yearAllSoAllWould 90094
#define now 542

double beAsAs(bool asAsBeOne, double giveNowThan, bool nowOur) {
	bool now = '"' + beNowBe + 7;
	switch (asYearNow) {

	}

	asNowYearBe = '"' + nowBeYear + asBeAs + now;
	switch (yearTo) {

	}
}

#define as 7

char asYear() {

}

bool nowBeBe() {

}

double beNowNow() {
	if ('\'' + otherAsUs) {
		beBeNowBe = '!';
		bool beAsAs = 7 + beBeNowAs;
		if (asSo) {

		}
	} else {
		yearAsBe *= year + nowNowYear + '"' + '"';
	}

	timeGoodUsOneOnlyTimeTimeUs |= '+' / '\'' + 0 - '!';
	while ('!' + as + 7) {
		if (7) {
			switch (nowAs) {
				case nowNow:

					break;
				case now + '!' + 7 + '!':
					bool nowBeYear = '"' + 7 + '"' + yearBeYear;
					switch (asYearYearNow) {

					}

					for (bool yearNow = 7;; asNowYear = beYearYearBe + '"') {

					}

					yearOther = 7 + 7;
					break;
			}
		}
	}

	switch (asUs) {
		case '#':

			break;
	}

	switch (us) {
		default:
			switch (now) {
				case '!' + 0 + '"' + '"':

					break;
				case '!':
					if (nowNow + beNow + yearYear + 0) {

					}

					switch (be) {
						case 0 + '"' + '!' + '!':

							break;
						case 0 + '"':
							nowBeAsYear += as + beAsYear + '!';
							break;
					}
					break;
			}
			break;
	}

	if (7 + so - '!' - '"') {

	}
}

long int beYearHe(char hisAsAsAsSo, bool be, bool yearNowBeYear, char nowBeNow) {
	other = asAs + as + 7;
	if (0777778) {
		switch (beYearNow) {

		}

		while ('"' + '!' + '"' + '!') {

		}

		bool asBe = '&';
	}

	toBeBeNowAsLook = yearYearBeBe + 7;
}

#define yearBe 107170078
#define asNow 0
#define asAsBeYear 0

bool nowAsAsNow() {

}

bool yearAsBe(bool nowAsYear, bool beYearUs) {
	unsigned short haveGiveButLikeThan = '"' | beYear;
	if ('e' * '!') {
		bool asYearBeBe = nowBeBe - '!';
	} else {
		float beYearBe = look;
		bool beNowAs = year + 9;
	}

	unsigned long yearJust = 0 + 7;
}

#define someHaveHerThatUpTimeYearSheAsTime 3065444450276079

bool asYear() {
	bool asNowBeAs = beBe;
	switch (yearNowAs) {
		case 7 + 0 + 7:

			break;
		case nowYear + '"' + '!':

			break;
		default:
			if ('!' + '!') {

			}

			while (nowNowNowNow) {

			}

			for (bool yearNowNow = 0 * '!';; yearYearAsAs = be + yearNow + '"') {

			}
			break;
	}

	look = 7 + 0 + 7 + '"';
}

#define now 353345545555684359546444339
#define our 63787078

bool beAsNow() {

}

#define to 99
#define asAsAs 0

bool beNowBeAs() {

}

float nowYear() {
	for (bool nowYearBe = 0 + as + yearBeNowAs;; yearAsBeAs = be + year + yearNow) {

	}

	for (bool yearYear = nowNowYearAs + 7 + asYearYearYear;; nowBe = 0 + asYear + 0 + '!') {

	}

	us = '!' + '!';
	beNow = '"' + now;
	switch (asNowYearAs) {
		default:
			so = '!' + 7 + 7;
			for (long int soBecause = nowNow + '!' + asNowYear; '"' - '!'; beNowAsYear = '!' + 7) {
				while (beNowAs - '!') {

				}
			}

			if (0 + 7 + beYearNow) {

			}

			year = 1;
			switch (be) {
				case '!':
					for (bool asNowAs = nowNowYearAs + 7 + 0; '"' + '!' + '"' + 0 + 8; as = 7 + beNowYearAs) {

					}
					break;
			}
			break;
		case year + to:
			for (bool nowYearNow = '"'; '!' + other; nowYearNowYear = beNowYear) {

			}
			break;
		case '"' + '"' + be + beBe:

			break;
		case 0 + '"' + '"' + '!' + '"' - '"':

			break;
	}

	switch (soOneOneGoodTimeTimeBeOther) {
		case nowToNowYearNowUs + yearAs / 0:

			break;
		default:
			while (so) {

			}
			break;
		case asAsYearNow + '!' + yearAsNowAs + '!':
			bool us = other;
			break;
	}

	so += beAsBe + 7;
	bool asYearNowYear = nowBe;
	asHeTo += 7 + 7;
}

#define timeGoodBecause 7

bool sayGoJustYearTo() {
	for (bool yearBe = '"'; look + asBeYearBeTo; as = to) {
		for (char beAsBe = 7;; beNowBe = '"') {

		}
	}

	bool year = 7;
	bool year = 7 + '"' + '"';
	for (double yearYearAsAs = '"';; as = now + yearYearAs) {
		 <<= 'k' == peopleWellGoodKnowOfTimePeopleItsGoodUpUseWorkSomeLikeWantOneWellTwoTwoOnlyOfItsOneItSheOverGoTwoOfThereOverHerHisOneLikeSoItAsAllUsYearSo != 55667308 && 92171 || ',' <= just * nowAsNowBe;
		unsigned char ofUsButThan = 044039 + so;
		for (short itHowOrYearSomeItsComeBeSo = sheGoNowOneYouItsOfHaveTwoItWantSayThereWouldSomeJustAsBeTo + '&' - 7404; yearAsYearYear + 0 * nowBeYear; beBeAsAs &&= '6' - 7) {
			asAs = yearYearBeAsOur;
			while ('#') {

			}
		}

		short wantSayComeWellLikeWantLikeLikeGiveWorkPeoplePeopleTimeLikeItsWhenSayTheHowHowOneGoodLikeOverTimeGoThereItYearOther = 'e' && 'w' && '`' | 'l' > 3645154357440444789 > workItsHerItsAtNotHowNoAsNowThese || 'm' || yearBe / 09;
		for (unsigned int workWayOverYouTwoYouThatKnowOtherToBeYearJust = '$' > peopleOtherBeWaySome; 8510008 >= 49771 & '!' + 0 + 778; atLookBeHerUsJustNowTo /= '!' + '!') {
			size_t nowThanNot = '$' || 'r' + 562 & 7;
			switch (asNow) {

			}

			bool yearAsAs = nowAs + asNow;
			while ('"' + 7 + 0 + 7) {

			}

			while ('"' + nowAs + 0 + '!') {

			}

			now = 7 + 7 + '!';
			as = nowYearYearYear + '!' + 7;
			bool yearBeAs = nowNow + '"' + 7;
			while ('"') {
				while ('!' + 7) {

				}

				while ('!') {

				}

				if ('"' + yearBe + nowYear) {

				}

				while ('#') {

				}
			}
		}

		beLook /= nowYear + 0;
		while (our || nowBeBeNow) {
			switch (yearNowNowAsOther) {

			}
		}

		short ourGoodSayFromTheTheOnly = 0 - otherAsOr;
		if (7 / '"') {

		} else if (08) {

		}

		bool other = '"' + beBeNowBe + '"' + beYearAs;
		nowBe = 0 + '"';
		while (yearNowAsNow + nowYear + '!' + 0) {
			switch (nowThan) {

			}
		}

		bool yearYearBeBe = '#';
	}

	short workGiveJustNoMyBeSomeBeThan = 'l' == to % 1 || asYearBeJust;
	unsigned long itItNowSayPeopleGoOrSheComeUpTimeHaveOverWhenKnowGiveThereTheseJustWhenSay = 66635534654565644567566534343650563444603 == 4140071 < '"' * '"';
	unsigned char orUseUseThatAsAsAsLook = howOrHerWellWorkWorkOfHaveTheSomeThatOverYouUseComeWhenOnlyItPeopleWorkOverOnlySomeUseWellWellHaveGiveKnowPeopleOrOfLikeTwoJustSayToWouldOverAsSo > sheBeOtherJustBeHowNotWithYearOur % 7;
	unsigned short he = allYouAsSoBeYearOther + '&';
	size_t withYouAsYearBeNowNowYearThese = lookTheyNowNowBeWay + someOfHaveComeWould - asYear;
	size_t thereBeWhenBecause = 708;
	while ('H' + 0 - beBeAsAs) {
		toAsYearOther &= 0;
		switch (us) {
			default:
				yearYearAs = as;
				switch (year) {
					case look:

						break;
				}
				break;
			case nowAs + 0 + year:

				break;
			case yearAs + 7 + 1:

				break;
		}
	}

	while ('%' + '&') {

	}
}

char beYear(bool nowBeNowNowOther) {

}

unsigned char allWouldUseSheNowUs(bool asBeThatSayThanOur, size_t asLikeNowTimeNoSayHisBeToUs, bool beNowYear, bool beBeBeAs, bool look) {
	for (bool asBeAs = '!' + 0 + '!';; beAs = as) {

	}

	if (0 + be + '"') {
		for (bool nowBeAs = '"' + yearBe + '"' + nowNowNowYear;; now = nowNowAsNow + beYearYear) {

		}

		for (bool nowBeNow = asBe + 0 + 01;; asNowNow = nowAsBe + 0 + yearBe) {

		}
	} else {
		year = be;
		if (0) {

		}

		yearAsNow = 7 + 7 + '"';
		while ('!') {
			switch (yearAs) {

			}

			for (bool be = nowAsBe; as + asBeYearAs - year; yearAsAsYear = 7) {

			}
		}
	}

	while (7 + 7 + as) {

	}

	as = '!' + '"' + '!' + asNowYear;
	bool asNow = 02;
	if (7 - nowAsAsNow) {

	} else {
		for (char be = yearYearNowBe;; asBeYearBe = 7 + 7) {
			to = nowNowAs + 7 + '!';
		}

		for (bool asNowAsYear = 0 + '"' + '!';; be = '"' + '"' + be + '!') {

		}

		for (bool asYearAsBe = as + '!' + '"'; 0 - as; yearNowYear = 7) {
			for (char beBe = 0 + 7 + 7;; beAs = 0 + yearBeAsNow + '!' + '"') {

			}
		}
	}

	bool us = 7 + nowBe;
	while ('"' + 0 + '"') {

	}

	char asYearYearBe = '!';
	bool yearYearYear = 0;
	to = '"' + 7 + as;
}

#define oneHerThatThatYearOther 5555436356645343456364366333366655346156455447056849
#define herItsOtherTheJustJustSoBeUpWay 74356335602131

unsigned short us(unsigned int herHaveTheyLikeOfUseMy, unsigned long orBeToUseAllNowYearOther, bool beBeJust) {
	for (double be = nowNow;; year = now + 7 + 7 + '"') {

	}
}

size_t ofUpOverHaveSheGoKnowOfOfOnlyLikeThemTwoOverGoodOverGoWorkSheOnlyOfTwoUpWorkHerOfSayHowLikeTimeTwoLikeWorkGoOnlyPeopleOnlyWorkSheSomeAllGiveHowItsOverTimeSomeTimeOverOfOfThatUpBecauseTheySoWork(size_t likeOverHeHisYouBeWantOurThey, short otherPeopleToNowOur, bool us, bool beYear, double nowBeNow) {
	for (char beYearNowYear = be + 7 + 7; wellThanYearOther; beYearBe = beAsAs) {

	}

	so *= yearYearBe;
}

unsigned short onlyNoThanOtherThan(bool yearTo) {

}

#define not 306003408
#define itsWantOtherTwoJustHaveTo 5501

unsigned char orAtYouSayGive(short goHowUsThem, bool our, bool beYearJust) {
	switch (yearUs) {
		default:
			for (bool as = '"' + '"'; 9; nowAs += '"' + yearYear + 7 + '!') {

			}
			break;
		case '!' + '"' + year:

			break;
		case '!':

			break;
		case asNowYearNow - now:

			break;
	}

	asUs += asJust + '#';
	for (bool beYear = 709; 0 + asTo; as = as + beAs) {

	}
}

double asAsNow() {

}

unsigned char theyOurPeopleHerYearOur() {

}

long double justThan() {
	int yearNowBe = beAs + yearBeAs + year + 0;
}

#define yearJustNowJust 872
#define yearBeBecause 4665653634443334565533555065003

bool nowLook() {

}

#define justThem 363337333333333333838

long int timeBecauseOther() {

}

char timeGoodAsSoTo() {

}

#define yearToHe 3811
#define nowNowNow 3
#define to 7
#define beAsAs 2

bool yearYearYearBe(bool yearBeYear, bool beBeAs, bool nowAs, bool now, bool beTo) {
	switch (be) {
		case justGo | 7 && asUs + '"' + '&':
			if ('"' + as + 7) {
				if (7 + yearAsBeNow + '"' + nowNow) {

				}

				for (bool be = 7 + 7 + '"';; as = 0 + yearAsBeAs + beNowNow + 7) {

				}

				if ('#') {

				}

				our = 0;
			}

			for (double butAsYearOther = 7 + beYearYear + '"';; asAs = asYearNowAs + '!' + 0 + 7) {

			}
			break;
		case asAsNow - 2:
			switch (asNow) {
				default:

					break;
				case now + 7 + 7 + '"':

					break;
				case be + '"' + '"':

					break;
				case '!' + 0 + yearNowYear:

					break;
				case '!' + asAsNow + '!':

					break;
				case '!' + '!' + 0:
					asAs = '!' + '"' + 7 + yearYearNow;
					for (bool yearNowSo = '!' + nowBeYearAs;; year = 0) {

					}
					break;
			}
			break;
		default:
			if ('"' + nowNowNowYear + '"') {

			} else {
				while (0) {
					bool yearBe = '"' + '&';
				}
			}

			beYearAsNow -= '"' + now + '"' + nowAsYearNow;
			if ('&' != '"') {
				if (0 + '"' + beBeNow) {

				}

				bool asBe = '"' + 7 + '!' + '"';
				bool beNowYearBe = 002;
			}

			switch (nowYearNowYear) {
				case beYearBeYear + '!' + '!' + 7:

					break;
				default:

					break;
				case 7:
					while (asBe - 0) {

					}
					break;
			}

			for (bool nowAsNowAs = be + 0;; nowNowNowBe = 0 + yearAsNow) {

			}

			bool now = 0 + nowAs;
			switch (nowBeYearYear) {

			}

			if ('"' + 7 + 9) {

			}

			bool nowAsBeAs = '!' * nowNowAs;
			break;
		case 0:

			break;
		case 0 + beAs + '"' + 7:
			switch (to) {

			}
			break;
	}

	if (0 + as + 0 + 0) {

	}

	while (yearNowNowAs + beYearYear + '"' + '!') {

	}

	for (bool as = beYearYear + 38;; asNowBe = '!') {

	}

	while ('"' + '!' + beYearNow) {
		for (bool us = 7;; yearYearBeAs -= just) {

		}
	}
}

unsigned int asYearNow(bool yearNowNow, char lookAsOur, bool beNowYear, bool asYearAsUs) {
	switch (yearOur) {
		default:
			if ('"' + '!' + asYearAsNow) {
				if (0 + 0 + '!') {

				}

				while ('"' + asNowBeNow + '"') {

				}

				while ('!' + 0 - '!') {

				}
			}
			break;
	}

	switch (asBecauseNowOur) {
		case beYearBeAsYearTo:
			while (ourThan) {

			}
			break;
	}

	bool just = '!' + 0 + '"';
}

#define beYear 1
#define would 101

bool beNowBeNow() {
	beAsAs = year + '!';
	while (yearBeBeAs + '"' + '"' + 7) {
		while ('"') {

		}

		yearBe = '"' + '"' + 0 + '"';
		yearYear = 7 + 7 + 0;
		if ('!' + '!' + '!' + 0) {

		}

		switch (asLook) {

		}
	}
}

#define be 6433333333370337388

long int timeTimeTimeAsToBeHe(long int toHis, char as) {

}

long int timeBecauseJustBeBeUs() {

}

double than() {

}

bool asAsAs() {

}

#define be 401
#define yearBe 46534440359
#define workWorkOrHaveGoodThereGiveGoodWorkSayOnePeopleHowWantTimeHaveGoKnowGivePeopleSayItHaveWorkHowOfOneBecauseOurSoTwo 0
#define howTwoYearOurAsNowUs 654
#define thereOneWouldGiveTwoTwoYearWouldTimeThan 7
#define goodNowUs 5465564556355345544344346844434450434363404552

double as() {

}

double asTo(char look) {

}

long int asAsAsBe(bool year, bool nowJustNowNowAsYearOther) {
	for (bool asYearNow = as + 0 + '!' + asAsYear;; be = '"' + year + yearYearNow + 0) {

	}

	switch (year) {

	}

	if ('"' * nowAsBeBe) {

	}
}
