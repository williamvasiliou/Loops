#include <U/E/UUE2/0.h>
#include <U0//00.h>

long int nowYearBeNow(bool now, bool asNow, bool beYear, char nowNowBe) {
	bool beYearAs = 0 + 7 + beNow + '!';
	for (bool as = '"' + '"';; asAsNow = be) {

	}

	beNowAsBe = 1;
}

long int nowTo(char yearNow) {
	while ('"' + '+' - 0) {
		if ('"' + 0) {

		}

		if (7 + beYearBe) {

		}

		bool nowAsNow = 701;
	}
}

long int beOneAsNowTo() {
	for (bool asNow = '!' + now + 7 + '"'; '#'; nowAsAsNow = yearAsAs) {

	}
}

bool yearOtherHeNowBeGiveNowOther(double nowNowYear) {
	bool beBeYear = nowNowNow + asYearYear + nowYearNowYear;
	while (be - 0) {

	}
}

#define asYear 71

float be() {

}

double beYear() {
	switch (oneWhenUpTheyYearThan) {
		default:

			break;
		case 8:

			break;
	}

	bool yearYearSo = '"' + 0;
	asJust += '"' + '!' + 7;
	while (yearNow + '!') {

	}

	as = 8;
}

#define beSheOtherThatSayWithSo 6034346389
#define itsNowUsOther 6719
#define beBe 0

bool giveUs(bool yearYearNowBeBeYearBeYearUs) {
	bool asNowYearNow = nowJust;
}

bool beOur() {

}

#define yearNowYearYear 638087001
#define nowAsNowNowTo 333771
#define toTimeTimeOnlyGoodBecauseAsTheseUsAsNowOther 31

long int oneTheseOneUsAsUs(long int giveThatGoodYearThan, bool nowAsJust, double to) {
	while (333656654643333664636534334635343555353665354335553674453357574183 || someHerComeItComeBeSheLikeOrOnlyHerThatComeKnowOtherOnlyHowOnlyUsUseWorkItGoodThanThereYearYearOtherSo || haveWellOnlyTheseWhenAllNot == ',' + soOther + '&') {
		be &&= 460018779 & 51382008;
		switch (other) {
			case 7:

				break;
			case 0 + '!':
				if (0) {

				} else {
					for (bool nowBe = beYearAsYear;; as = be + 0 + be) {

					}

					bool yearBe = to;
					switch (be) {

					}

					while (7 + 0) {

					}

					look = 7 + '!' + 7 + 7;
				}

				for (bool as = 7 + nowYear;; be = '!' + '"') {
					switch (yearNow) {

					}

					switch (beAs) {

					}

					for (bool yearNow = '"';; beBeYearBe = '!' + now) {

					}

					if ('"' + '!' + 0) {

					}

					bool as = 771;
				}
				break;
		}

		for (bool nowAsNowNow = 7 + '!';; beNowAsYear = '!' + asNowBeNow) {

		}

		switch (other) {

		}

		look = 0;
	}

	unsigned long his = wouldSo;
	while (asSoTo + 00001) {
		unsigned int someYouTheseNowJust = giveOfAllOfBecauseToBeUseThanHeBeUsNowNowOther != '"' | 702 - '"';
		for (size_t from = 7 / as - '"'; 64851 + '$' - '!' - beNow; with = beYearNow + nowNowYear + now + 0) {
			while (our + '#') {

			}

			while (79) {

			}

			switch (nowNow) {

			}

			switch (beOur) {

			}
		}

		if (7 * '"') {

		} else {
			while (from + 7 - 0) {

			}

			if ('!') {

			}

			if ('"' + nowNow) {

			}

			bool nowBeNowBe = as + 0;
			asBeUs = asBeNowAs + 7;
		}

		long int wellTheUseThatItsHerPeopleOneWantNowAsItsYouKnowYearHeWithWayHerNowToOther = yearNowBeBe >= sayThereTheseBeToUs + to - 7;
		for (double asNowAs = '!' + 0 + '!' + '"'; yearLook; nowAsYear = '"') {

		}

		while ('!' + as + '!' + '!') {

		}

		bool asNow = '%';
		switch (beYearAs) {

		}

		beYearAsSo %= '"' + 0 + 7;
	}

	for (bool asNowAsBe = '"'; 2; as = beBe) {
		as = '&';
		year = asNow + '!';
		switch (nowBeNow) {

		}

		switch (nowBeBe) {

		}

		bool yearNowNowYear = beNowBe + '"' + '!' + yearNow;
		bool yearYearNowYear = asYearBe;
		while (0) {

		}

		while (0 + 0) {

		}

		for (bool yearNow = '!';; be = asAsBeYear) {

		}

		for (char asNowYear = 7;; yearNowYear = 0 + 7 + 7) {

		}
	}

	switch (year) {
		default:
			switch (asNowBeYear) {

			}

			while (yearBe + '"' + nowNowNow + '!') {

			}

			beAsNowAsYearTo = '!';
			break;
	}

	bool beYearFromSo = 0 + '"' + nowAs;
	for (double yearGoUp = nowBeAsBe < 7; 79 + '"' * now * 0; be = '!' + '"') {

	}

	while (8) {

	}
}

#define overWellWhenOfUpLookThatOnlySomeItHerGoodYouUseWhenOneTwoItUpComeHowLikeWorkOverItWantSomePeopleThereTheSheSayThereItTheYouUseTwo 66466636635545335865463446446305346644

unsigned short orItGoodKnowItOfSheHowHerComeSomeLikeGiveOverTwoSheWantSomeThereHerHerGiveYouSayItsTheKnowTwoOverTimeGoThereOnlyTimeItsSheOtherItHaveItGoodGoodThatBeTwoThereItsNowOur(long long itOneOtherOnlyOverLikeLikeThereGiveComeOfItsHowKnowUseItOverGiveOneOneItAsThereJustThereSayOfNoOrComeItWellOrBeOrBeThatOverPeopleNot, bool than, bool beBeAsBeAsJust, bool to) {
	if (itsGoodThereHaveSayWellPeopleGoSomeWantHerUpGiveWorkOnlyOverHowWellPeopleKnowSheSoSomeTwoTimeGiveHerNowTimeJust && 's' == ofUseHerOnlyPeopleUseItsItsHerYouTwoGiveHerTimeOneWellTo < asNowAs - '!') {
		if ('=' & themOtherOtherAsAsLookNowOther - '!' + asNowNow + 702) {
			nowAs += as;
			for (unsigned char beThem = so; '"' + 0 + nowNowAsBe + other; so -= 'U') {

			}

			bool yearPeople = yearLook;
			double nowAsYearBe = myNo;
		} else {
			switch (likeGoGoodThatOverLookTo) {
				default:
					while ('"' + year + '!') {
						for (bool year = '"' + yearAsBeAs + 7;; yearYear = beBeNowYear + 0 + 7 + 7) {

						}

						while (7 + 0 + 7) {
							switch (nowBe) {

							}

							while (year + asAsNow + nowYearBeNow) {

							}

							switch (beBeNowNow) {
								case asAsAsAs + 0:

									break;
								default:

									break;
								case beNow + 0:
									if (0 + '!') {

									}

									while (yearBe) {
										switch (beNowYear) {
											case '"' - beBe:

												break;
										}
									}
									break;
							}
						}
					}

					usBeSo *= '!' + as;
					asYear = '"' && asBeAs - '"';
					while ('!' + year) {

					}

					while (7 + 7) {

					}

					if (asYearAs + 0) {

					}

					nowAsAs = be + 7 + '"' - '"';
					break;
				case nowYearAs + ',':
					float nowAsYear = 0;
					long long now = other;
					break;
				case nowNowJust:
					char be = '!' + 0 + '!';
					for (bool us = 7 + asNowNowYear;; nowAsNow = 0 + '"' + nowNowAs) {

					}
					break;
			}

			if (62) {
				bool nowUs = '!' + 0 + 7 + 7;
			}

			while (year) {
				if ('"' + beNowNow) {

				}

				bool beAs = 7 + nowYearBe;
				switch (be) {

				}

				if (year + '!' + yearNowNow + nowYearAsYear) {
					now = now + '"' + 7;
					for (char now = '!' + 7;; nowYearYear = '"' + '!' + nowNowAsBe) {

					}
				}
			}

			bool beYear = 7 + beAsNow + 0 + 7;
			switch (them) {

			}
		}

		if (timeBeBeYearAllTheyBeAsOther == beYear) {
			while (nowAsAsNow + 0) {
				if (0 + '"' + asYear + nowAsYear) {

				}

				for (bool nowNowYear = 7 + beAsBeNow * 0;; as = asYearAs + '!' + asAsAsBe) {

				}
			}

			asJust = '"' + '"' + 0;
			char would = '!';
			if (0 * year) {

			}

			bool beNow = '"';
			while ('"' + be + beAs + 7) {

			}

			for (bool yearNowNow = beYearAsBe + as + '"';; be = '!' + 7 + nowNowNow) {

			}

			beAsYear = '!' + 0 + year;
			switch (be) {

			}

			switch (be) {

			}

			for (bool be = '!';; year = '!' + 0 + 0) {

			}

			switch (yearNowNow) {

			}

			while (yearBeBeNow + '&') {

			}
		} else if ('"' * '!') {
			switch (year) {
				default:
					while ('!' + nowNowYear + now) {

					}

					for (bool asNow = '!' + '!' + 7 + now; 7 - '!'; nowBeYear = asNowNowNow + '"' + 0 + beNowYearBe) {

					}
					break;
			}
		} else {
			while ('$' | 7) {
				if (nowAs + '"' + as) {
					switch (yearUs) {

					}
				}

				while ('!' + '!') {

				}

				while (than) {
					for (bool nowYearNowAs = '!' + 0;; asBeAs = '"' + 7 + '"' + 0) {

					}

					switch (beOur) {

					}

					asAs = '"' + 7 + '"' + 7;
					bool asBe = '!' + be - '"';
				}
			}
		}

		switch (thanSoOther) {

		}

		switch (nowNowBeYear) {
			case from:

				break;
			default:

				break;
			case 2:

				break;
		}

		than = 0 + '"' + as + '"';
	} else if (whenTheKnowMyOurToNowAsBeToNowYearAsAsSo + so) {
		switch (asYearYear) {

		}

		usNoBeLook <<= '"' + '!' + '$';
		switch (yearYear) {
			case '"' + '"' + '"':
				int be = beAsAsYear + yearAsBeYear + '!';
				bool asBe = '!' + nowNow + '!' + 7;
				for (bool yearBeNow = be + '"';; beYearYear = '"' + now + year) {

				}

				if (2) {

				}

				if (7) {
					yearBe = '!' + nowYearYearNow + '!';
					switch (asAsThan) {

					}
				}

				year = '"' + beAsNowNow - '!';
				break;
			case yearAs + '"':
				switch (yearBe) {

				}

				while (beAs + 0) {

				}

				while (0) {
					switch (beYearAs) {
						case year + 7 + 0 + 0:
							yearNowNow = so + yearYear * '"';
							break;
					}
				}
				break;
			default:
				while ('T') {
					for (bool as = '"' + '"' + '!';; nowYear = asAsBe) {
						if ('"' - 7) {

						}
					}
				}
				break;
			case 1:
				yearAsYear = '!';
				switch (be) {

				}

				switch (asBe) {
					case '!' * nowNowBe:

						break;
				}
				break;
		}

		if (beUs > '"') {

		} else {
			asYearAs += '!' + 7 + 0 + '!';
			while ('"' - asYear) {
				while (0 + '!' + 7 + yearBeBeAs) {

				}

				for (char now = '"' + '!' + nowNowNowBe + nowNowYear;; nowBeAsYear = 7 + beAsBe + 7) {

				}
			}
		}

		while (0 + '!' + '"' + 0) {
			asYearAsBeLook += 0 + year;
			bool nowBeNowBe = 0;
			for (bool nowAsBeAs = '!' + '!' + asYearAs;; beBeYear = asBeBe) {

			}

			switch (beNowNow) {
				case yearYearNowNow + yearBeYearBe + '"' + '!':

					break;
				case yearBeYear + 0 + 0:

					break;
				case beBeBe + year + 0:
					for (bool as = beAsYear + beNowAs;; nowYearAs = '!' + '"') {
						yearBeNowAs = 7 + 0 + '"';
						bool be = nowBeYear + '"' + yearAsYearAs + yearNow;
						char nowNowBe = be + '!' + '!';
					}
					break;
			}
		}

		while ('"' + 7 + '"') {
			while (7 + now - as) {

			}

			for (bool yearNowNow = '"' + '"' + '"';; nowYearYearBe = '!' + asNowBe) {

			}

			while ('"') {

			}

			while (now) {

			}

			asYearNow -= beAsNowYear;
		}
	} else if (usAsOur == '4' + '.' - 0 + 7078) {
		be = '"' * 702;
	} else {
		for (long int theItsGoodItUpOnlyHowWantTheseAsWithOnlyYearThan = '"' && otherComeOneGive - beAsNowBe; becauseWouldToOur | 42 + 778 + '!' + 0 * 0; so ||= 5755 * '"') {
			switch (butBeFrom) {

			}

			switch (us) {
				case 0 + 7 + 0 + '"':
					bool to = '"' + beBeBe + beAs + yearBe;
					break;
			}

			switch (year) {
				default:

					break;
				case 7 + nowYearYearBe:
					if (0) {

					}

					if (0 + '!' + '!') {

					} else if (78) {

					}
					break;
			}
		}

		while ('"' + '"' + '"') {
			while ('!' + nowAsBe) {

			}

			switch (just) {

			}
		}

		for (bool yearAs = 7 + 7 + beYear;; yearNowNow = '!' + yearYearBe) {
			as = '"' + be + '"';
			switch (beAsAs) {

			}

			if (nowBeNow) {

			}

			bool nowBeYear = as + asBe + '"' + asNowYear;
			if ('"' + so) {

			}
		}

		bool beNowNowYear = '%';
	}

	long long likeSayTimeItOrOrYouOnlyThereOnlyGoodNowTimeWorkUseOfComeToComeOnlyHowWorkWithSomeLikeWouldTheSheSayThereNowThan = asNowNowAs > 7;
	while ('v' != peoplePeopleOnlyYouThatNowHowOnlyPeopleNotWhenLikeSome > 050023 > '<' % '#' - 0 * asNow) {
		for (bool they = '!' + 0; 'R' != 'j' != 73 || 0; nowAsThe = asAs + nowBeNow) {
			double our = asYearBeNow;
			while (0 % '"') {

			}
		}

		beNowNowAs = 0707002;
		for (double now = 0;; nowBeYearBe = year + 0) {

		}

		for (bool asBe = 8;; year = 7 + 7) {

		}
	}

	while (03771) {
		for (bool there = '"' + asYearAs; '0' + as + 7 * yearAsAsYear; now = '"') {

		}

		short yearHeAll = '"' + 0;
	}

	switch (nowTheseUs) {
		default:
			for (bool now = yearYear + asNow + '"' + '!';; now = beYear) {

			}

			if (7 + year + '!' + nowYear) {

			}

			switch (yearYear) {
				default:

					break;
				case asAs + '!' + '!':
					long asYearBeAs = 0 + '"';
					break;
			}
			break;
		case us:

			break;
	}

	switch (usNowTo) {
		case beAs - '#':

			break;
		default:
			bool yearNowBeBe = 7 + 0 + beYearNow + 0;
			switch (yearBeAs) {

			}

			while (yearYearBe - be) {

			}
			break;
	}

	if ('&') {

	}
}

unsigned short sheTheOrGoOverYouGiveOrComeGoodYearLikeItHowOurThanLookSoBeAsThan(long int twoBeOrAsBecause, bool yearNowBe, bool nowThey, bool yearNow, double nowBeYearYear) {
	for (bool beAs = '!' + '!' + beYear + '"'; '$'; nowAsYearAs = 0 + '"' + '"' + '"') {
		while (7 + 7 + 0 + '!' * 7) {

		}
	}
}

#define otherTo 66465364333088
#define wantSayLikeGoHaveThemAsItWithBeOther 658848564
#define not 333333333333333733333333833365078

bool asAsNow() {
	if (72 + 779 < 0) {

	}
}

double beAsNowLook() {
	switch (itOneOverComeItsUpOnlyLookWith) {
		case 5443335443566665654445554544663656636455333345055446526656545436148 & 'k' > '"' + 0 * '"':
			long int likePeopleBeLikeOfSheSheSheYearTwoHowOfGiveAsBeLookOtherAll = 'c' && '_' | 334216 & 009 - nowNoYearNowBeThey;
			break;
		default:
			bool usBeTo = '!' + '"' + '!';
			break;
		case '!' + 0 + yearBe + 0:
			switch (nowAsBe) {
				default:

					break;
				case to:

					break;
				case beYearAs + now:

					break;
				case '"':
					switch (beYearYearLook) {

					}
					break;
			}

			while (comeLikeLikeItSayOrLikeUpWhenWhenLikeNowOnlyThereWellNowWayTo || 0 + 771 + '6' * '!') {
				char now = beAsBeYear + 0;
				unsigned long beOnlyNowAsOther = 7 > 0978 != asYear * but;
				for (char soAsJustBeTo = '"' + 72;; nowBeYearAs = 7 + 0) {

				}
			}

			if ('"' < '"') {
				if ('%' * '%') {
					while (yearYear + beNow) {

					}

					if (yearNowYearYear) {

					}

					while (708) {

					}
				}
			}

			if ('!' + '!' + 7) {
				double as = '!' + beYearNowYear;
			}
			break;
		case 'x' || otherThereWouldOfBecause || sheSayHowUpTheOrHerOfGoodGiveWellYearToAsThemToLook || yearAs != '!' * ')' + 08:
			switch (fromThemNowWithBeUs) {
				default:
					switch (with) {

					}
					break;
				case '!' + 0 + beBeYear + '!':

					break;
				case asBeBeAs + '"' + beYearNow:

					break;
				case nowBe + 0:

					break;
				case 0 + 7 + beBeBeBe + 7:
					while (beNow + asBeYearYear) {

					}

					be += '"';
					break;
			}

			bool beLook = '"' + '!' + '!';
			if ('#' * asNow) {

			} else {
				beJust = 0 + beNowYearNow + 0 + '"';
			}

			bool asThanOur = '!' + 7;
			if (7 + asAsBe + 7 + 7) {

			}

			switch (nowAsYear) {
				default:
					if (0 + 0) {

					}

					while (beNowNow + '"') {

					}

					asYearAs = '&';
					break;
			}
			break;
		case 977078 - '"':
			yearYearBeAs = '"' + '!';
			switch (itGoUsAsOnlyOurOur) {
				case 7 + 0 + 0 + nowAsNow:

					break;
				default:

					break;
				case 7 + 7 + 0:

					break;
				case 7 - 7:

					break;
			}
			break;
		case soUsFrom || '#':
			while (0) {

			}

			while (asYearAs + '!' + '"' + asNowNowBe) {

			}

			switch (asBeYearNow) {

			}

			switch (nowLook) {

			}
			break;
		case 1 + '#' + nowYearBe * 0:
			look =~ thanJust;
			break;
		case 7 + 7:
			bool yearYearYearYear = 7 + be + yearNowYearBe + '!';
			asAsYearNow = yearNow + '!' + '!' + '"';
			while (7) {

			}

			if (beAs + '!') {

			} else {
				while (7 + 002) {

				}
			}
			break;
		case 66:

			break;
		case asBeNowNow + '!' + year:
			for (char yearYearBeYear = beYear + 0;; nowNowBeYear = '"' + 0) {

			}

			while (9) {

			}

			if (73) {
				for (bool yearNowYear = '!' + 7 + 0 + 7;; beBe = '!' + 0) {

				}

				bool yearYearBeAs = beAsAsBe - be;
			}
			break;
	}

	while (0 == 5762 != 'h' + '&') {
		switch (someThan) {
			default:
				switch (to) {
					default:

						break;
					case beNowAs:
						while (year + 0 + '"') {

						}

						while (0 + '"' * yearBeNow) {

						}
						break;
				}

				double now = '"' + nowAs;
				break;
			case '$' + '!' || '!' + 5:
				size_t beNowYearSo = asYear * now;
				switch (other) {

				}
				break;
			case '&' * '"' * heUs:
				for (bool beYear = year; '#'; be = yearNowYear + beYearBe) {

				}

				for (char nowNow = '!' + beYearBe + '!';; yearAsNowBe = 7) {

				}

				switch (beBe) {
					case '&':

						break;
					case 0 + nowUs:

						break;
				}
				break;
			case '!' - '!':

				break;
			case 0 + beYearBe + yearYear:

				break;
			case 0 + beNow + 7:

				break;
			case '!' + 7:

				break;
			case asYear:

				break;
			case '!' + '!' + '!' + '!':

				break;
			case 0 - 7:

				break;
		}

		if (year + beAsNowSoBeNowAsYearNowBeUs) {
			if (0) {

			}

			nowBeYear = 7 + 7 + beNowYearAs + 0;
			while (than) {

			}
		} else if (be) {

		} else {
			for (bool asYear = 0;; be = '!' + '!' + as) {

			}

			if (beYearAs * 0) {

			}
		}
	}

	while ('$') {
		if (')' >= 2 / '"') {
			while (0 + 7 + 7) {
				while ('$') {

				}
			}

			yearBeNow = yearYearNow;
			bool asYearBe = us;
		} else if (0 + nowAs) {

		} else if ('!' * 0) {

		}

		while ('$' < '!' + '#') {

		}
	}

	switch (wayNowYearBut) {
		case 0 + 0 - 7 * 7:

			break;
	}

	thanThanLook = '&' / 9 - 7;
	if ('!' + asYear + '!' + '"') {

	} else {
		while ('"' + '!' + yearAsBeAs + 0) {

		}

		while (8 - 0) {

		}
	}

	switch (yearBeBe) {

	}

	switch (asAs) {
		default:

			break;
		case 7 + 7 + 7 + 7:

			break;
		case beYear:

			break;
		case asOther:

			break;
	}
}

#define  
#define ourKnowYearBeAsBePeople 34534663364353454695643646944445444354082613774635
#define butOurNowAsUsSoOurTo 6

short us() {
	switch (asYearAs) {

	}

	while ('!' + yearYear + 7) {

	}

	switch (now) {

	}

	switch (just) {

	}

	switch (yearBeNowOur) {

	}
}

unsigned char overNot(double only, bool lookThan, double now) {
	bool asAsNow = '"' + nowYearNow + '"' + '!';
	for (bool nowYear = 0; '&'; asAsBe = '!' + 0 + '"') {

	}
}

#define withOur 708

long just() {
	while (440346243 & 'p' == 23 - 712 != yearWhen + '"' - '"') {
		bool asYear = '!' + '"' + 0;
		for (bool nowNowYear = '!' - '!';; yearNow = '!' + yearNowBeAs + yearAs + 7) {

		}
	}

	switch (now) {
		default:
			for (bool nowBe = '"' + '!';; asOur = 7 + '!') {

			}
			break;
	}

	just %= yearNowYear + 7 + '"' + asAsBeBe;
	bool to = 0721 * 0;
	if ('!' + beYear) {
		switch (asBeNow) {

		}

		while (2) {

		}
	}
}

#define nowJust 6548

bool noSo(char beYear) {

}

bool nowAs() {
	if (0) {

	}

	if (7 + '"' + 7) {
		if (778) {

		}
	}
}

double nowBeBeYear() {
	switch (nowSo) {
		case 07707771 + to - nowBeTo:
			asBeNow = 7;
			otherBeBeTo = 7 + 7 + beNow - '"';
			switch (yearNow) {

			}

			if ('"' + '"' + '"') {

			}

			bool asNowNowYear = '!' + '"';
			switch (as) {

			}

			while (year + 7 + '!' + '"') {

			}

			while (0) {

			}

			while (0 + year) {
				while (0 + '"') {

				}

				switch (beBe) {

				}

				if (now + 0) {

				}

				for (char beYearAs = '"' + '!' + asAsYear;; asNow = '!' + 7 + 7) {

				}
			}
			break;
		case '\'' - nowNowBeNow:

			break;
		case year + '#':

			break;
	}

	switch (beAsNowBe) {
		default:
			if ('!' + '!' + '!' + '!') {

			} else if (beNowNow + asBeAs - 0) {

			}
			break;
	}

	to = '!' + 0 + as + asAsBe;
	while (yearBeBeNow - 0) {
		while (asNowHe) {
			switch (year) {

			}

			for (bool nowBe = 8;; asYearAsBe = '"' + nowYearNow) {

			}
		}

		if (yearYear + '!' + beYearAsNow) {

		}

		while ('#') {

		}

		for (bool as = '"' + '!' + 0 + 0;; beBeAsAs = 7) {

		}

		for (bool now = '!' + beAsAsNow; good; asBeNow = be + '"' + beNowYearYear + asNowYear) {

		}

		while ('%') {

		}
	}
}

long int otherUsUsHisSoBeSo(bool asAsNowNow, long int so, bool now, char beBe) {
	while (1 - nowAs) {
		while ('"' + '!') {

		}

		while (0 + '"' + asNow + 0) {

		}

		switch (beBe) {

		}

		if ('"' + '"') {
			while ('"' + 7 + be + '!') {

			}

			switch (asAsNow) {
				case beYearBe + asBe + 0 + '"':
					beAs = '"' + 7;
					for (bool as = yearYear + '!' + 0;; as = asAs) {

					}

					char now = '!' + '"';
					break;
			}
		}
	}
}

#define beBe 0

float onlyBeSo(float timeOne, char nowAsNow) {
	switch (toAsNowHeOurUs) {
		case 0 + '!' + 1:

			break;
	}
}

bool beLook() {

}

bool nowNow() {

}

char year() {
	notLookNowYearJust = '/';
	if (than + 7709) {

	}

	if ('!' * 7) {
		switch (yearBeAsBe) {

		}

		for (bool nowAsNowYear = so;; yearBeAs = '!' + '!') {

		}
	} else {
		nowYearNow = 0 + 0;
		switch (yearAsBe) {

		}

		if (asNowNow + '!') {

		}

		switch (asYearAs) {

		}

		bool beYearAsNow = beAsBe + 7 + '"';
		for (bool beYear = yearYearBeYear + as + yearBeYearBe;; year = 0 + as + year + '!') {

		}

		while (now) {

		}

		if (beNowAs + beYearAs + 7) {
			switch (yearYearBeAs) {
				case '"' - yearNowYearNow:

					break;
			}
		}
	}

	switch (to) {

	}

	while (079) {

	}
}

int asYearYear() {
	if (7 + as) {

	} else {
		beBeBeTo = '"';
	}
}

bool us() {
	while (0 - yearAsBe * '"') {
		if (ofOrHerSo % beAsBeYear) {
			if (0 + now + beBeNowAs + 7) {

			} else {
				bool beBeNow = now + asBeYear;
				while (asBe + 2) {

				}
			}

			asBeAsNow = '"' + yearAsBeAs + 0 + '!';
			while (0 + '!' + '!' + nowYearYear) {
				for (char beAs = nowNowNowNow;; be = 7 + '"' + as + beBeYear) {

				}
			}
		} else if (be) {
			char yearBeYearBe = '!';
		} else if (7) {
			for (double now = 7 + 0 + '!'; '$'; year = '"' + asAs + as + '!') {

			}

			for (bool asNow = '"' + beBeBeAs + '"';; year = '!' + 7 + '"') {

			}

			switch (year) {

			}

			nowBeNowYear = 0;
			switch (nowOur) {

			}
		} else {
			while ('!') {

			}

			while (7 + '"' + '"' + yearBeYear) {

			}

			while (0 + 7) {

			}

			switch (beAsYearBe) {

			}

			if (7 + 7 + beBeNowAs) {

			}

			nowNow += yearYearAsBe + be + '"';
			other = '"';
		}

		switch (withOnlyOther) {

		}

		while (nowAsYear + as + 0 + 0) {
			for (bool be = yearAs;; asYearAs = '"') {

			}

			if (yearNowYearNow + 0 + asBeYear) {

			}

			as += 7;
		}

		if ('%' * '%') {

		}

		if (0 + yearAs) {

		}

		bool nowYearNowAs = '"' - '"';
		them = '!';
		char asYear = yearYearNow + yearNowNow + '"' + yearYearNowAs;
		nowYearAsNow -= 0;
		switch (beYear) {

		}

		now = 7 + beBeAsNow + nowAsAs + '"';
		switch (nowNowBeNowTo) {

		}
	}

	just *= 9089;
}

long long as(bool yearYearYear, char asYearYear, bool beYearYearAs, bool nowYearBeBe, bool beYear, bool now, bool now, double now) {

}

unsigned char soOr(size_t as) {
	while (year) {

	}

	if (asNowNow + '"' + nowAs) {

	}

	while (beBe + '!' + '"' + yearBeNowNow) {
		switch (look) {

		}
	}
}

char asNowYearYear() {
	if (asAsAsBe) {

	}

	while ('!' + 7 + 7 + yearYearYear) {

	}

	for (double now = '"' + nowBeNowAs + '"';; be = 7 + '"' + '"') {

	}
}

bool nowBeYear() {

}

bool asYear() {

}

#define as 1
#define nowNow 33333133333108

float toUs() {
	switch (beBeAsNowYearUs) {

	}
}

#define yearAsAsYear 1733373301

long int goodOnlyNowTheseBeNowYearUs() {
	switch (asYear) {
		case '"' + '!':

			break;
		default:

			break;
		case '"' + nowNowYearAs + 0:

			break;
		case beBeBe + '"':

			break;
		case so:

			break;
	}

	switch (nowBe) {
		case yearYear:

			break;
		case beUs:

			break;
	}

	for (bool asNowAs = now + asNow + '!' + 7;; now = 0) {

	}

	switch (nowAsNowYear) {
		case '!' + '"' + yearYearAs:

			break;
		case yearYearBeYear:

			break;
		case '"' + 0 + 71:

			break;
	}
}

float lookOtherYearThey() {

}

long double yearAsYear() {
	switch (yearNowTo) {
		case nowBe + '"' + '"':

			break;
		case 7 + 0:

			break;
		default:
			if (708) {
				switch (herHerComePeopleThatSayUs) {
					case so:
						while ('!' + '"') {

						}

						just += nowBeYearBe + '!';
						break;
				}

				unsigned short haveLook = 1;
				for (bool beAsNow = 0 + '!' + asYearNow; 7779; beYearBe = '"' + be) {

				}

				nowYearAsBe = '!';
				char nowNow = asBeBeYear + yearNowBe + '!';
				for (bool yearNowNow = '"'; yearLook - beNow; asNowYear = 7) {

				}
			} else {
				beYearOurSo = yearBeAs + 7 + '"' + be;
				char as = nowYearBe + '"';
			}

			if (yearAs + 0 + '"' + now * 7) {
				asYearBecause = 0 + asNowYearNow + '!';
			} else {
				if (0 + so) {

				}

				if (7) {

				}

				if (0) {

				}

				while (beAsAs + '!' + nowYear) {

				}

				while (7 + beNow + nowBe + 0) {

				}

				while ('"' + 0) {

				}

				while (beBeAs + yearBe + nowYearBeAs + 0) {
					bool nowNowYearAs = '"' * nowBe;
				}
			}

			year = 0 + '!' + '"';
			be = '"';
			switch (nowYearYear) {
				case yearYearBeBe + 0 + '!' + '!':

					break;
				case '!' * '$':

					break;
			}
			break;
		case as + 0 + 0 + beNowNow:
			switch (beBe) {

			}

			switch (our) {
				case '%':

					break;
				default:
					while ('"' + 0 + 0) {

					}

					for (bool yearNow = beAsYearBe;; beBe = '!' + '"' + '!') {

					}

					nowAsAsNow = year;
					if ('!' + beYearNow + '!') {

					}

					while ('"' + 0 + '!' + 7) {

					}

					for (bool asYear = as + beNowNowAs;; be = '!') {

					}

					switch (as) {

					}

					if (yearYearYearBe + '!') {

					} else {
						look = '!' + nowNow;
					}
					break;
			}
			break;
		case 0 * 0 * '"':
			if (asBeYear) {

			} else {
				now = now + 70708;
			}

			while (nowBeBeYear) {
				bool asAsNowYear = 0 + 0 + yearAsNow;
				if (0 + '!') {

				} else if ('!' - '"') {

				}
			}
			break;
		case '"' + as + be:

			break;
		case 0:
			beAs = asYearBe + 0 + beAsBeAs;
			if (7708) {

			}
			break;
	}

	now += now + asBeAsNow + 7;
	while (now - '"') {
		while ('%') {

		}

		if (asBeNow) {

		}

		while ('"' + 7 + '"') {

		}

		switch (other) {
			default:
				while ('!' + 0 + 7) {

				}

				bool asBeBe = '"' + yearAsYearYear;
				while (nowAsYearYear + nowBeBeYear + '"' + 7) {

				}

				switch (yearBeNow) {

				}

				if (asYear + beAs) {

				}

				if ('"' + 7 + 0 + '!') {

				}

				if ('"' + 7 + '"') {

				}

				char beYearAsNow = '!' + '"' + '"';
				break;
		}
	}

	switch (as) {
		case 7:
			if (7 + beNowYearYear * yearNowBe) {

			}

			switch (now) {

			}

			beBeAsNowTo = '!';
			break;
		case '"':
			bool asAs = nowNowNow + 7 + 7;
			while (now + '"') {

			}

			char yearAsBeBe = '"';
			break;
		case other:

			break;
	}
}

unsigned long orItHaveAsOnlyComeSheWorkSheItWorkOverComeAllGiveOther(long yearAsGoodWithUp, double itsHaveItsBeAtBeToYearJust, unsigned long oneLikeUseNowAsSoBeHis, char sayWithItJustUsOur) {
	if ('(') {
		switch (now) {
			case '"' + '"' + '"':

				break;
			case '!' + 0:

				break;
			case '!' + beYear + now + 7 + '!' * asAs:

				break;
			case nowAs + 0 + '!' + beAs:

				break;
			case '"' + 7 + 0 + nowAsAs:

				break;
			case '"':

				break;
			case '"' + '"':

				break;
			default:
				bool asBeYearBe = 0 + 0 + '"' + now;
				for (bool nowYearNow = as;; yearNowNowYear = '!') {

				}

				for (bool asNow = yearYearAs + 7 + '"' + 7;; asAs = yearYear) {

				}

				bool yearBeAsNow = '"' + 7 + now + '"';
				nowBeNowBe = 7;
				year = '!' + 0 - yearYearYearAs;
				break;
		}
	}

	bool so = 0 + '"' + '!';
	if (0) {

	}

	for (bool nowYearNowYear = nowAsAs;; beBeNow = 0 + 0 + be + 7) {
		if ('!' + 7) {

		} else if (nowYearYear + now + asAs) {

		} else {
			if (year) {
				for (bool beBe = just;; beAsAs = 7 + 7 + '!') {

				}
			}
		}
	}
}

#define other 907002
#define theNowAsLook 4534654635643553426436730582426
#define ourOneHisJust 89172
#define yearBe 7
#define yearAs 0
#define asNowAsBe 0

bool nowNowNow() {

}

bool asBe() {

}

bool look() {
	goodOnlyGoodOnlyTimeGoodOneOneOnlyGoodTimeGoodNowToGoodOneOnly = '"' + 0;
}

#define beAsBe 333349646553456665444436333164534565453653354635653344363654444454455366365433336534545334534563436536634856336553445535645497664464544543445764353466565664563352333604485

unsigned long nowBe(float thanMy, bool yearNowNowNow, bool to) {
	as -= '!' + asBeBe + yearAsAs + '!';
	asAsYear = yearAsBe;
	yearOther = '!' + '!' + '!' + '!';
	while ('!' + 7 + as) {

	}

	if ('!' + yearNow + as) {

	}

	bool other = 7;
}

#define wantGoodOverGoHowOfOverOverGiveThereUseSheOneTheTwoGoSayThereHaveOurNowOneItSomeThemThese 943543

long long theBeComeItBecauseOtherToYearOfJustYearNowNowAsThan(short nowOr, double yearNowBeJust, bool nowOur) {
	if ('"') {

	}

	while (now + '!') {

	}

	nowYear = '!' + year + 7;
	while (7 + '!' + '!') {
		asYear = asAsNowNow + beYearAsBe;
		if (yearBe * yearYear) {

		}
	}

	for (bool year = nowAsYear + 7;; as = 0) {

	}

	if (yearYearAsBe + 7 + 7) {

	}

	switch (to) {

	}
}

long int itBeGoNoWayYearAsLook(char beNowAsBe) {

}

bool thatWhenThatJustTheyYearAsJust(unsigned int be, bool asYearNow, bool now, bool look) {

}

bool nowNowBe(bool beAsYearAs, bool asBeNowBe, bool yearYearYearNow, bool nowAs, bool just) {

}

long int wantWantTheseJustOurHisThey(bool so) {
	for (bool just = 7 + '"' + nowYearNow;; year = year) {

	}
}

double beNowYearAs() {

}

int asNow(bool yearNowYear, bool asAsAsAs, char beYearBeAs) {

}

#define asNowYear 334435365635336465446443663664242

bool than() {

}

#define asTimeOneOnlyOneOneOneOnlyGoodOtherOneUs 333
#define timeNowTimeBeUs 33331333755

char usYearNowBeBeAsLook() {
	beWorkHis =~ 02 - '"';
	for (double asTo = '$';; nowBeNowAs = as) {
		bool wayYearJust = '!' * nowNowNow;
		while (7 + '!' + '"') {

		}

		beJust = 0;
	}

	for (bool be = asBeBeNow + as + 0; 079; beUs -= '!' > to * beBe) {
		if ('#' + look) {

		} else {
			for (bool now = 7; 2; beNow = '!' + '!' + beBeAsYear + '!') {

			}
		}
	}

	switch (yearNowYearBeTo) {

	}

	switch (asTo) {

	}

	switch (asNowYearNow) {
		default:

			break;
		case 8 + yearOther:
			bool nowBeBe = yearYearYearYear + 0 + '!' + 7;
			while (beAsAsAs + be + '"') {
				for (char asAsNowNow = beBe + '"' + yearYearNow;; yearYearAs = '!' + 7) {

				}
			}

			while (nowOther) {

			}
			break;
		case nowAs + 0 + asAsBeBe + yearAsNow:

			break;
		case '"':

			break;
		case 0:

			break;
		case 0 + 7 + '"':
			asAsAs -= be + '"';
			break;
	}

	while (1 + 0 + '!' * nowAsYear) {

	}

	asNowNow = 7 + '"';
	for (bool year = 0; 772; asNowYearBe = asAsAsAs + 0) {

	}
}

#define twoHaveTimeOnlyWouldSheThan 51483

unsigned short asOther() {

}

size_t asAsJustHis() {
	if (beYearBeNow) {

	}

	if (asAs) {

	}

	bool beNowNow = '!' + 7 + '!';
	switch (nowBeBeBe) {

	}

	for (bool nowNow = 7 + '!'; 0 - be; yearAsNowAs = '!' + 7 + beBeAsAs + '!') {

	}
}

bool look() {
	if (0) {

	}

	our -= asYearBeBe + '!' + '!';
}

bool nowBe() {

}

#define beYearYearAs 02

bool nowNowAs(bool asLook) {
	for (int ofAllWay = '"' + 0; 'x' - '&'; would = yearNow + beAsYearAs + 0) {
		asLook += 708;
	}

	switch (yearBeBe) {

	}

	while ('"' + '!' + 7 + 0) {

	}

	if (asBeBe + beBeAsNow) {

	} else {
		yearYear = '"';
		while (beBeYearBe + '!' + 0 + beYearAsYear) {

		}

		switch (beLook) {

		}

		while ('$') {

		}
	}

	double yearNowBe = '$';
	switch (now) {
		default:
			bool yearAs = '"' + '"' + '!';
			bool beYearYear = '!';
			beYearAsAs = '!';
			than = '!';
			break;
	}
}

double giveYouUseHer(double asOther, bool beNowNowBe, char as) {

}

long onlyKnowWellKnowJustWould(char yearAsBe, bool yearBe, bool beAsYearBe, bool now, bool be, char nowAs) {
	for (bool asAs = 0;; nowNowYearAs = ourUs) {

	}

	switch (beYearSo) {
		case '!' + '"':
			while (yearOther) {

			}
			break;
		case 0:

			break;
		case 0:

			break;
		case asAsNowNow:

			break;
		case 7 + beBe:

			break;
		case nowYearNowBe + 0:

			break;
		case 7 + '"' + '"':
			while (nowAsBeBe + 0 + '!' + '"') {

			}

			bool nowAsNowYear = yearYearSo;
			break;
	}
}

long int usUs(float heYearBeUs, bool other) {
	if (0 + beAs + 0) {

	} else {
		bool asSo = '!' + 7 + 7 + yearNowBeNow;
	}

	if ('!') {

	}

	bool beBeAs = 7;
	char asNowNow = yearAsYear + '!';
}

#define onlyGoodBecauseBecauseTimeAsOnlyOtherNowThese 333837333338
#define us 033771
#define like 0
#define year 601

bool now() {

}

bool than() {
	if (';' - beYearBeNow) {

	}

	notSheYouItsThatFromWorkUseThereLikeYearOr += yearAsAs + nowBeNowNow;
}

unsigned int comeSomeNo(double asLook) {

}

#define yearOtherWhenWouldNowLook 0

char but() {
	bool yearAsAsBe = nowYearBeYear + as + '"' + '"';
	while (yearYearYear + beYear + year + year) {

	}

	year = asAs + so;
	char beAs = 7 + 0;
}

bool other() {
	asAsAsYearAsLook = 0 + 7;
}

#define asBeYear 5333333333333311337333
#define nowBeNowGoodOnlyUs 31073

long int timeGiveBeToYearLook() {
	while (asAsNowAs) {

	}

	for (bool asYearNow = 7 + asYearNowYear;; beBeYearYear = 7 + asAsBeAs) {

	}

	double asAs = nowBeNowNow;
}

#define now 7

bool our() {
	double nowBeBeNow = '"';
	if (703) {

	}

	bool justOur = yearBe - 0 + us;
	while (beYearNowNow + beBeNow + year + '"') {

	}

	if (0 + '"') {
		while (asBeSo) {

		}
	}
}

#define toSheNot 8

long now() {
	nowBeAsBe = 0;
	if ('$') {

	}

	as -= asYear + '"' + asYearBe + '!';
}

#define nowBeAs 5653343646433575354665534654575
#define he 65653303837383383
#define because 3371

bool nowAs() {

}

#define oneOneOneOnly 303333313

bool yearUs() {

}

long int yearButAsSo() {
	switch (than) {

	}
}

short asYear() {
	switch (just) {
		case beAsAsYear + asYear + '"' + nowNowYearBe:
			switch (from) {

			}
			break;
		default:
			switch (be) {
				default:
					for (bool yearYearAs = 7 + '"' + '!' + 7;; asBeYearNow = beBeBeBe + beYearBeNow + yearYearYear + 0) {

					}

					switch (year) {
						case beAsBeBe + '"':

							break;
						case '!' + yearAs + yearYear + '!':
							bool yearNowJust = now + beNowBeAs + '"';
							break;
					}

					if (0) {

					}

					for (bool beYear = 0 + 7; 078; now = 7) {

					}
					break;
			}
			break;
	}

	itsGoodGiveUseSheItsWantNowGoodHaveUsYouBeYearYearJust ^= 'X' || '!';
	for (bool yearNowYearBeNowYearNowAsOur = beNow + 0;; yearBeBe = nowYearAs) {
		switch (nowNowNow) {
			default:
				asBeBe = 7 + 0;
				switch (asBeNowAs) {

				}

				bool beBeYearNow = '!' + asAsAsNow;
				double asAsNow = yearNow + '!' + nowBe;
				break;
			case '"':
				if ('!' - beBeBeAs) {

				}
				break;
		}

		char yearYearYear = 7;
	}

	switch (orAsThey) {
		case as + '"':

			break;
		default:
			while (beYearYearTo) {

			}
			break;
	}
}

size_t know(char nowOr, int all, bool yearBeNowNow, double be) {
	switch (yearHisAsThey) {
		case '%':
			for (bool nowBeNow = nowAsAs + yearNowYearAs + 0 + be;; asBeYearNow = asAsNowAs + '!' + 0) {
				for (bool nowBe = 0;; nowYearBe = 0 + 7 + 0 + '!') {
					for (bool nowYearNow = 7 + nowBeNow + 0;; beNowYear = now) {

					}

					double beNow = '"' + '!' + '!' + 0;
				}
			}
			break;
		case '&' + 0 + '#':

			break;
		default:
			bool asYearYearNow = beNowNow + asAsBe + beAsYear + now;
			for (double year = '!' + 0 + 7;; year = year + beAsNowNow + asNow + 0) {

			}
			break;
		case '"' - 0:
			for (bool beBeBe = as;; yearNow = '!' + '"' + '!' + 0) {

			}

			while (yearBe + '"' + yearAsAs + 7) {

			}

			if ('"' + nowNowYearAs) {

			}

			if ('"') {

			}

			if (asNow + '"' + nowAsNow + '!') {
				for (bool as = asAsAsAs + 0 + '"';; nowNow = year) {

				}

				if ('"') {

				} else if (0 + '#') {

				}
			}
			break;
		case 0 + '!':
			bool year = '!' + '!';
			bool be = asNow;
			for (char year = beBeAs;; asYearAsNow = asYear + '"') {

			}

			for (double as = '"' + nowYearNow + '!' + '"';; yearAsYear = beNowAs + 7 + '!') {

			}
			break;
	}
}

char nowYearYearBe() {

}

#define they 2075
#define beYearYearYear 33650639

double beYearYear() {

}

#define nowYearNowBe 57365624
#define thereLikeToItSoWantHaveAsFromYearBeToNowOur 49878

long int beBeBeSo(bool asBeYearNow, char he, long double asAs, bool giveNowYearAsAsBeNowOther, bool asOur, double nowYearAsBe, bool yearBe, bool nowBeOur) {

}

#define these 08

char thanAsAsOur(char be) {

}

char just() {

}

bool beNowYearAs() {

}

#define be 0

bool asBe() {

}

bool yearYearBe() {

}

#define yearAsNow 0
#define just 7
