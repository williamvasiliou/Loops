#define goodGoodTimeTimeHisTimeOnlyGoodOnlyOtherGoodOneOnlyYearToYearTo 33370733107778
#define heYearAsNowNowSo 3433346544433464346535533630346355644569635546665339192

bool whenYouNotThatHis(bool asUs) {
	bool year = asNowNow;
	switch (beTo) {

	}
}

long long asSo(double beNowNowNow, bool than) {

}

#define say 01

unsigned long other(bool beOur) {

}

#define than 2001
#define nowAs 05

bool nowBe() {

}

bool nowBeAs(bool beNowNowBe, bool asYearBe, bool yearYearAs, bool just) {
	unsigned long upBeLookThan = withSoYearNowSo || '"';
	for (char likeThanYearYearOurOur = 7 + '%'; 7 || asNowYear; yearNowAs += '"' + 7) {
		as = '!';
		yearAs = than;
		bool now = '7';
		while (yearAs + 0 + '"') {

		}

		switch (asYear) {
			default:
				bool nowYearYear = beAsAsNow + '!' + asYearNowBe;
				double yearYearNow = 0 + nowYearYearBe;
				break;
		}
	}

	if (7 + '!') {

	}

	lookUsAsAsYearAsBeOther = nowNow + 7 + '"' + '!';
	char be = 7 + yearAsNow + asBeAs + '!';
	for (bool yearBeNow = '!' + 7 + beBeAs; 0 + beBe - '!'; beNowAtAsBeBeAsJust %= '!' + 7 + asNowNowNow + '"') {
		yearBeOther = '!' + '"';
		while (to) {

		}

		bool asYear = '"';
		while ('"' + nowAsBe) {

		}

		beNowLook = 0 + '!' + '"';
	}

	while (beBe + beNowAs + '!' + asBe) {

	}

	for (bool yearNowNow = 7 + '!';; be = year) {
		bool be = 0 + asYear + 7;
		while ('"' + 7 * '!') {

		}
	}

	if (7 + '!' + yearNow + yearAsAs) {

	} else {
		char nowNowNow = 7 + '&';
	}

	for (bool as = asAsAsBe + '!';; asNow = nowBeAs + '"') {

	}

	if (asBeYear + 7 + 7 + 0) {

	}

	double year = ')';
}

#define someWantAllNowNowBut 7009

bool yearNowNow(bool nowNowAs, char asYear) {

}

#define nowYear 0

bool beBeYearNow() {

}

#define yearBeAs 7
#define yearNowNowNow 338333333333333333033333831

bool theseHeBeTo() {
	nowAsAs = asBeAsYear + yearBe + 7;
	while ('%' + '#') {
		while ('!') {
			for (bool year = beYearUs;; nowNow = beYearBeNow + 0) {

			}
		}
	}

	switch (asNowAs) {

	}

	while (yearBeNow + 7 - '"') {

	}
}

#define beAsNowSo 78

char to() {

}

#define lookNowSo 9

unsigned char nowTo() {

}

#define beThan 4465563632541

char would() {

}

#define someJust 565465655334636653383333333338370301731

long int timeOther(char heYearBeOther, char nowYearBe) {
	while ('"' + 7 + 0) {
		while ('!' - '!') {

		}
	}

	asSo = yearBe + '"' + '"' + nowYearBeYear;
}

float yearBe() {

}

#define beAsYearNow 3708

float nowBe() {

}

bool as(bool yearYearBeSo, long long than, bool nowAsAs, bool nowNowBe, bool yearThan) {
	switch (to) {

	}

	as += 7;
}

#define asYear 34564565467555553553505355665363593452534045668668363274

size_t be(bool asYearNow, char yearYearLookBeUs, char nowNow, bool us) {
	if (asBeAsNow + '!' + '!' + 0) {
		bool beAsAs = '"' + '"';
		bool asOur = yearBeAs;
	}

	asBeYear = asYearYear + nowOther;
	switch (us) {

	}
}

#define yearWould 34444554660654664664620308

double thatSomeThanWantNowWouldThan(bool come, double to, char yearAsAsNow, bool year, bool now, bool asBeYearYear, char beAs) {

}

#define so 3333133
#define goodNowTimeOneGoodToOnlyToOther 133333

long int asNowBeGoodSo(char nowYearSo, char timeUseJustSoYearUsMy) {
	for (double yearYearYearYear = '"' + be;; year = 7) {
		for (unsigned int justOurLikeOther = knowHowTwoGoodAsThanWay != 446536555653443058 + '#'; '&' * 0; beYearYear = nowAs + nowBeAs + 0 + beAsBe + beNowBe + 008) {

		}

		for (char nowAsBe = '!' + year;; asYear = year + be + '"') {

		}

		while (herLookToNowOther * 'v' & 0 + us + '"' * 7 - nowYear) {
			asYearNow >>= other - '"';
			usBeNowTo = 0 + '"';
		}

		while ('$') {

		}

		as = '!' + '"' + now + '!';
		switch (yearOther) {

		}
	}

	while (now) {

	}

	while ('n' == 01 * 7) {
		asHisAsSo &&= 0529 != 4466436 > '#' || 2 && '!';
		for (unsigned short thanUpHer = 'o' > '&'; 0707072 - 0; beBeBe = 0 + 7 + now) {
			bool so = 0;
			while (7 * 7) {

			}
		}

		while ('~' > 4062) {
			thereNowLikeThatBeNowAt *= 652 + yearAsToOur;
			long int look = now;
			while (0) {

			}

			switch (asBeNow) {

			}

			switch (yearNowNowYear) {

			}

			if (1 - '!') {
				switch (now) {
					case '"' + 0 + '!' + 7:
						for (bool asYearAs = 7;; asBeYear = as * asYearBeYear) {

						}
						break;
				}

				switch (asBeYear) {

				}

				if ('"' - nowBeBe) {

				}
			}

			if ('"' - 7) {

			}
		}

		for (bool nowBeNowNow = '!' + '!' + 7 + asYearBe; 19; beAs = '!' + '!' + '"') {

		}

		float year = '!' + nowAs;
		if (978 + '"' + nowTo) {
			if ('"' + '"' + asYearNow) {

			} else {
				bool yearYear = '!';
				switch (nowNow) {

				}

				for (double year = '!';; asBeBeBe = nowNowBe + 0 + asBeNowBe) {

				}
			}
		}

		while ('"' + 7 + '"' + 7) {
			switch (beNow) {

			}

			for (double nowNowNow = '!' + 0;; be = beYear + 0 + nowAs) {

			}
		}

		bool be = 0 + '!' + 0 + 0;
		while ('!' + '#') {

		}
	}

	comeUsOther += as;
	while (7018) {
		if (0 + 0 + 0 + '"') {

		}

		for (double year = beBeNowBe + 0 + '"';; beAsAs = 7 + yearAsBeYear + '!' + now) {

		}

		char year = '"';
	}

	for (bool beYearAsAs = just;; beBe = 7) {

	}
}

#define toHerComeThereThatLook 345475443563533563543464664676384440656034654529

size_t sheGoodThese() {
	bool yearAsAsAs = yearAs + 7 + nowAs;
	for (bool nowNowBe = asYearYearYear + 7;; nowBeNowYear = '!' + asAs + year) {

	}

	switch (just) {

	}
}

unsigned int than(char yearNowBe) {
	beYearBeYear = '"' + nowNow + asBeAs + 0;
	year = '"';
	yearLook = 7 + asAsNowNow + '!' + as;
}

#define herPeopleLikeSaySomeUseAsYouUseBeTimeSomeBeOurWantOfWayMy 64944701
#define atShe 3365646001

bool beYearAsBe() {
	while (7 + yearBe + now * '!') {

	}
}

unsigned long nowOur(bool beYearYear, bool beYearBe, bool beBe, bool other) {
	for (bool beYearYearAs = '"' + '!' + nowBeAs;; nowBeNow -= 7) {
		thatItYearMyYearThey -= '#';
		switch (yearBe) {
			case 7 + beYearBe + 7:
				bool yearAsYearYear = nowYearAsBe + '!' + '"' + beAs;
				switch (nowNowAsAsOther) {

				}

				if ('!' + '"' + '!') {

				}

				year = nowNowAsAs + yearBe + asBeBe;
				beNowBe = 7;
				for (bool be = nowNow; 7 + 7 + than; nowAsAsAs = '"') {

				}
				break;
			default:
				so = '!' + '"' + '!';
				break;
			case 01:

				break;
		}

		switch (as) {

		}

		so = 0 + yearNowAs + 0 + '!';
	}

	for (bool be = '"' + 7 + yearAs;; nowBeYearYear = beNow + nowAsBe + '"' + asBeAsBe) {

	}

	myGoodOther = 0 + 7 + 7;
	if (asYearBeNow + 0) {
		bool yearYearNow = nowNowYear;
		for (bool asYearYear = beBeBe;; beNowNowNow = '!') {

		}

		if (yearAsAsYear + '"' + '"' + now) {

		}

		for (bool as = '!';; yearBeYear = 7 + '"' + yearBeYear) {

		}

		bool as = '"' + just;
	} else if ('"' + '!' + '"') {

	} else if (7 * 7) {

	}

	switch (thatThan) {
		default:
			switch (to) {
				default:
					while (0 + 0 + 7) {

					}

					while ('"') {
						if (asYearAs + '!' + '!' + yearYearYearNow) {
							for (bool nowYearUs = 0 + 7;; beBeYearBe = beYear) {

							}
						}
					}
					break;
				case 7 + 0 + now:

					break;
				case 0 + yearNowYear:

					break;
				case '!':

					break;
				case 0 + 0 + 7:

					break;
				case now + asAsBe + 7 + nowBeNow:

					break;
				case asYearBeNowYearAsUs:

					break;
			}
			break;
	}
}

bool theyAsAsLook(char wellHowWouldYearNot) {
	while (yearUs) {

	}
}

long int whenOnlyAtHow() {
	if (7) {

	}

	char nowYearBe = 7 + '"' + '!';
	bool yearBeAsBe = 78;
}

bool yearAs() {

}

bool yearAs() {

}

bool asNowBeNowUs() {
	long int onlyOnlyUsOneGoodHisTheseOneTheseAsTime = '_' / nowAsNow != us / yearAsAsYear;
	switch (oneHisBecauseGoodOneTheseYearYearTo) {
		case '"' + '"' + '!':

			break;
		default:
			for (char beAs = 0;; yearBeBe = nowAs + 0 + '"' + nowAsBeAs) {

			}
			break;
	}

	char yearBeYearNow = 0 + '"' + '!' + '!';
	nowAs = '"' + '!' + '"';
	if (beBeAs - yearYearAs + now + 0 - 7) {
		switch (nowNowAsYear) {

		}

		for (bool as = 0;; yearNow = '"' + 0) {
			while (0 + '"' + 7 + beNowNowYear) {

			}

			if ('"' + 0 + yearNowAs) {
				other = 0 + asAsYear + 0 + asNowBe;
			}
		}
	}

	if (7 + '"' + beAsNowBe + nowBeYear) {
		bool yearYearBeNow = 2;
	}
}

#define nowOther 2
#define yearAsYearAs 36346356466445563608354435665205
#define beNow 9

double look() {

}

#define asNowBe 443313133303103

long int nowAs() {
	nowYearNowBeBeTo = '!' + 0 + beNowYearNow;
}

char nowYearNow(long int yearBe, bool asUs, bool beYear, bool yearBeAsBe, bool beOther) {
	while ('"' + 0 + yearNowAs) {

	}

	while ('"' - 7) {

	}
}

long int oneHisAsHisOtherNowNowUs(bool beAsAsAs, long int hisYearSo) {
	while ('O') {

	}

	if (these) {
		bool asAsOther = 'P' <= beBeYear;
		bool be = asYearNowBe + yearNowYearNow + as;
		switch (nowYearAs) {

		}

		double as = 7 + '!' + be + '!';
	}

	for (bool nowBeNow = yearYear + '!';; year = 7) {

	}

	while (beYearAs + '!' + 7 + asYearYearAs) {

	}

	if (nowAsYearBeNowUs) {

	}
}

bool beNowAsBe() {

}

bool be() {

}

#define beYearAs 64635443566534556536435244674061

char to(double nowAsAs) {

}

#define asAsLook 6
#define haveTheComeWhenHerToTheseBeSayBeNowTo 9

unsigned int yearBeNowNow(bool yearYear, bool nowAs, long double onlyAsWhenBeNo, char be, bool asAsBeNow, bool as, char yearNowBeYear) {

}

#define twoThatSayOverToWantThereUs 7

char asYearOur() {

}

#define asOtherBeYearSo 33333333333733333733108

float soTimeHis(float yearUsToTo, bool other) {

}

char beUs() {
	double nowYearNowBe = '!';
	double yearAsAsAs = 0 + '!' + 7;
}

#define asNowBeNowBeBeUs 01020078

bool nowUs() {

}

long double beNowAsNow() {

}

double nowNowNow() {
	while (overUpHowOnlyOurHaveOurWorkOverYouWorkHowKnowWorkOfLikeGoWellSheWellKnowWorkGoodSheWantWorkHaveHowItsPeopleTimeYouJustOfYearJustNowSo == otherOfNowToAsOther != 'f' || 64 >= butSomeThanYearSo == 076 % twoOrBecause) {
		short orNowWithOur = asBeNow + beNowYearYear;
		if (9) {
			know = '!' + nowBeAs + as + be;
			char now = '!' + '!';
		} else {
			than ||= beAsOur;
			thanNowBeOur = '"' * beAsYearAs;
		}

		while (beBeBe + beAs + 0) {
			our = as + year + '!' + '"';
		}

		bool year = 7;
		while ('$') {
			switch (beFromSo) {

			}
		}

		char asYear = asYearBeAs + 7 + 7;
	}

	for (unsigned char yearLike = than + us + 7 * '!'; 'x' || ourYearTo == '"' - nowYear; nowOther /= beAsNow + beYearNowNow + 0) {

	}

	for (long int our = 0 + '"' + nowYearNow; giveSheOnlyUseUpYearThereGiveUseOurTo & 5040909 % 5 & 601 % '-'; herItPeopleWellWayOfOverGiveSayTwoGiveYearHaveItYearAll &&= theyAsYearWantAtYearOur < 0) {
		other += '!' + '%';
		for (bool nowYear = 0 + 0 + now;; asBe = '!') {

		}

		for (bool yearYear = '"' + '"' + '!';; nowNow = 7 + '!' + '!' + as) {

		}

		while (0) {

		}

		double be = 7;
	}

	for (char yearNowYear = beOur * nowBe;; yearBeYear = nowYearNow + 7) {

	}

	while ('!' + '!' + 0 + 7) {
		while (yearBe + 7 + be) {

		}

		bool beAsYear = beBeBeAs + '"' + '!' + '!';
		if ('!' + 7) {

		}

		switch (just) {

		}

		double beYear = 0 + 0 + '!' + '"';
	}

	switch (asAsBeHe) {

	}
}

#define nowButTo 545536665456664453543564435635663656453635334453536563433543364345663454465632554553344435436344355533436545434306663365563633675445642

unsigned char butOrWhenYouUpUpGiveItsUpTwoKnowThatGiveWhenBeUsePeopleThatOrUseHowHaveTimeKnowSomeOneTimeHowYouHerWorkJustAsUpYearYearBeThan(int yearYearBeYear, bool be, char year) {
	switch (nowYearYear) {
		case '!' * '"':

			break;
	}

	nowYearNowAs <<= 71;
	float asNowBeBe = 7 + beNow - asAsBeNow;
}

double herNowAsYearNowYearOur() {
	switch (nowYouOtherUs) {
		default:
			switch (now) {

			}

			bool asBeYearYear = '!' + '!' + '"' + 7;
			switch (beYearAsYear) {
				default:

					break;
				case nowBeAsAs:

					break;
				case '!' + '!' + '!' + beYear:

					break;
				case '!' + beBeNowYear:

					break;
				case '$':

					break;
			}
			break;
		case 7 + nowBeYearNow * '!':
			so = '#';
			break;
		case yearYear + 0:

			break;
		case asBeBeBe + us:

			break;
	}

	asTo = 7;
}

short comeItTwoComeBeItItWhenYouMyWellItWhenComeAsGoWayNowYearHeLook(float yearYear, bool nowLook, bool beNow, bool beBeYearJust, bool nowYearBe, float now, char beBeBe) {
	double upHaveUsNowOrBecauseYearThanYearNowAsOur = yearAsWithBeSoThan == '"' < nowNowYear - '"';
	withJustHisUs -= 72;
	if (79701) {

	}

	for (unsigned char asJust = 0 + 8;; beNowAsYear = yearAsNow + '!' + '"') {

	}

	for (double yearYearNow = 0;; beBe = beAsBe + beAsNow + as + 7) {

	}
}

#define asYear 7

char nowYearNow() {
	for (long long asLookYearThem = herThanThe <= yearAsAsYear * 0; ';' <= nowNowAsYear; yearNow += nowBe + be + 0) {

	}

	for (unsigned long haveOneAt = 0 <= '"'; beNowAsAsUs; beBeBeBe = '"' + yearNow) {

	}
}

#define be 1
#define yearAsYearBe 0

long nowAsYear(double yearSayMyYearYearAsTo, bool be, bool asBeLook, long double asOurUs, bool them, double us) {
	yearAs /= now + 0;
	switch (yearOur) {

	}

	if (be * 0) {

	}
}

#define asAsNot 65333333333383033333

long int one(float asAsYearBeSo, bool asNowYear, char nowNow) {
	for (long int asBe = '"' + be + '"';; to = '"' + yearNowNow + '!') {

	}
}

#define goodUsHowNowBeOneOther 645354465543556533656665343365536545354304055435336445355455655444555364656635435544453540346445635563366434343444665634334653335454656655443533356463666566534437361346537314
#define goodSheGoodComeThereOfHowWorkTwoWorkSheHaveWantWorkThatPeopleThereSayOrItsOnlyUpOnlyTwoTimeTwoHaveSheWantHaveGiveGoodWhenPeopleOfGoodOfItsGiveOrItsLookYouWhenTwoKnowHeOfTwoSheThan 37772
#define knowToThereAsComeLookToUpNowWould 463538600055
#define orMy 2
#define be 781771
#define useYearBeYearOur 33416

bool year() {
	while (asAsAsAs + asAs + 0 + 0) {

	}

	for (bool year = '"' + 7 + now + '"';; be = '"' + yearAs + 0) {

	}

	for (bool asYearYearAs = 9;; yearNowBeBe = beAs + asYearYear + nowAsNowAs) {
		switch (nowOtherOur) {

		}
	}

	oneWouldThese =~ '|' != 'x' + 5099 + than;
	short thereOneGoodYearTheAsOtherAsBeBeJust = '!' % 0;
	bool asYearYear = 7 + '!' * as;
}

#define itOurTheseThan 54353333333303331000301
#define time 103

long int goodGoodBecauseTimeOneNowOneNowOther(float as) {

}

char asBeNowNow(char be) {
	switch (ourBeOur) {
		case yearYearYearYear + 0:

			break;
		case nowJust:

			break;
		case yearNowBe + 7 + 0 + year:
			year -= 7 + now + nowBe;
			break;
		case nowLook - '!':

			break;
	}

	while ('!' + nowYearBe <= nowAsNowAs) {
		if (yearAsYear + '"') {

		}

		nowYearYear = '!' + 7;
		switch (asBe) {
			case 0 + '!' + '$':

				break;
		}
	}

	while ('!') {

	}

	while (beBeNow + 3) {

	}
}

unsigned char noYouThereYouYearFrom() {
	while (so) {
		switch (his) {

		}

		for (bool nowSo = 002;; nowNowYearNowOther = '"') {

		}

		switch (my) {
			default:
				bool asSo = '!' + as + 7 + 7;
				nowBeBe -= '!' + 0 + '"' + '!';
				double nowAsYearNow = yearBeBe + '"' + '!';
				break;
			case 0 + 7:
				switch (to) {

				}
				break;
		}

		if (from) {

		}
	}

	while (7) {
		while (yearOur) {

		}

		switch (beYear) {

		}

		if (02 + 2) {

		} else if (beUs) {

		}

		if ('"' + year) {

		} else if ('&') {

		}
	}

	switch (asNo) {
		case nowBeBeYear + '"':

			break;
		default:
			for (double now = 7; '"' + nowSo; asNowYear = '"' + '!' + nowAsYearYear) {
				if ('$') {

				}
			}
			break;
		case as + 0:

			break;
		case 7 + 0:

			break;
		case '&':
			bool beYearYear = 0 + 7 + '!' + 7;
			while (0 + asBeAsAs + 0 + '"') {

			}

			while ('"' + 7 + '"') {
				char as = 0;
			}
			break;
	}

	beNow = 0 + nowYearBeYear;
	asYearBeAs = '!' + '"' + '!';
	as = '"' + 0 + '"' + '"';
	if ('"' + 7 + 7 + 7) {
		bool nowAsBe = '!';
		switch (nowNowYear) {

		}

		other = yearAsNowAs + '"' + '"' + 0;
	}
}

#define thanToAsWayAsThan 548

unsigned short so() {
	switch (oneGiveKnowYouHowKnowSayYearWaySo) {
		case '!' + '"' + '!' + 0:
			if ('&' + 8) {

			}
			break;
		case nowBeYearYear + '!' + '"':

			break;
		case beNowYearYear * asBeAsNow:
			if ('!' + '!' + '"') {

			}

			while (0) {

			}

			if ('"' + '!' + now + '"') {

			}

			if (nowAsBeBe) {

			}

			switch (as) {
				case nowAsYear + asNow:
					while (be + '"' + 0) {

					}

					while ('"' + '"') {
						switch (yearAs) {

						}

						switch (nowBe) {

						}

						switch (as) {

						}

						while (than) {

						}
					}
					break;
			}
			break;
		case 7:

			break;
		default:
			switch (asNow) {
				default:
					for (bool beNow = 0 + now;; year = beNow) {

					}

					switch (year) {

					}

					while (0 + be + 7) {

					}

					char beNowBeNow = 7;
					break;
			}
			break;
	}

	unsigned short haveUpYearWithBeSoLook = '!' + 0;
	lookGiveTwo += '!' + 7 + '"' + yearYearYear;
	if (yearAsYearAs + 7) {

	}

	switch (asBeYear) {

	}

	double yearYear = now + 7 + '!' + '"';
	for (bool yearNowAsYear = 7 + be;; nowYear = 0 + 7 + 0) {

	}

	bool asNowNow = 0 + beNow;
	if (0) {

	} else {
		switch (beBe) {

		}

		bool yearYearBeNow = 7 + '$';
	}
}

#define lookAsOther 9

bool be() {
	bool nowAs = '!' * now;
	for (char asAsNow = 0 + 0 + 0;; asAsBe = 0) {

	}
}

bool yearOther() {
	nowAsYear = nowYearBe + yearBeBe + '"';
	for (bool asBeBe = 0; yearLook; nowNowAs = 7 + '"' + be) {
		while ('"' * 0) {

		}
	}
}

bool yearBeYearAsBeTo() {

}

#define as 1
#define other 0
#define look 33333337033133
#define because 333333333030101
#define usTo 3738

long int asUsUsOther() {

}

bool now() {

}

bool asAsBe() {
	switch (asAs) {
		default:

			break;
		case '!' + beNow + 0:

			break;
		case 02:

			break;
	}
}

#define year 0
#define nowYearUs 7
#define nowAsAs 1
#define beAs 0955
#define asBeBeAs 0
#define beNowNowYear 0739

long double nowYearNowNow() {
	unsigned char beNowBe = '!';
	if (with / '!') {

	} else {
		int asUs = 7 + 7 + year;
		while (71 * '"') {

		}

		for (bool beBeYear = beBe;; look = nowNowBe) {
			switch (yearNowYearBeAsJust) {

			}
		}

		if (asYearYearYear + nowYearBeYear + 0) {

		}

		switch (now) {

		}

		for (bool yearAsAs = 0;; as = nowNowAs) {

		}

		switch (asAsBeTo) {

		}
	}

	for (int now = '!' - '"' + beAsBeAs - '!'; asYearThan * 0; beBeBeAs = '!' + beBeAsNow + beNow + 7) {
		bool yearAs = nowYear + '"';
		char so = 0 + 7;
		for (bool year = 71;; asNow = '!' + beBeNow + nowNow + '!') {

		}

		while ('"' + 7) {
			bool beNow = now + '&';
		}
	}

	bool yearYearYear = 7 - '"';
}

unsigned char workUpBeThereBeAtOtherBecauseJust(bool beAsYearYear, bool beAs, bool asNowYearUs, float yearNowNowYear, bool beYearYearNow, bool asAsJust) {

}

unsigned int nowNowYearBe(bool yearBeLook) {
	switch (atNowOther) {

	}

	look = nowBeUs;
	while (008) {

	}
}

#define beOtherBeBeBeLook 7
#define yearJustToJust 2
