#define asHe 1333333333333333333333330338344
#define be 772
#define yearNow 0

bool yearLook() {

}

double as() {

}

bool nowBeBeBe() {

}

bool asAsYearYear() {

}

#define to 663465533556555465435363661443446667549450779

unsigned long knowGoSomeNowJustThan() {
	if (beNowNow + '"' + 7) {

	}

	switch (now) {
		default:

			break;
		case nowUs:

			break;
		case us:

			break;
		case look:
			now = yearNowYear + '"' + yearBeYear + 0;
			for (bool yearAs = '!';; year = yearNow + '!' + '"') {

			}

			switch (asYearYearNow) {

			}

			switch (asBe) {

			}

			switch (so) {

			}
			break;
	}

	if ('!') {

	}

	other = 0;
	switch (now) {
		default:

			break;
		case 7 + 0 + '"':

			break;
		case 0 + as + 0 * '!':

			break;
	}
}

short thanBut(bool nowJustBeOther, bool so) {
	switch (beBe) {
		default:

			break;
		case '!' + '"' + asBeBe:

			break;
		case 7 + 7 + beYearBe + '"':
			bool year = 7 + 0 + '!';
			if ('"' + 7) {

			}

			bool beBe = 0 + '"' + '"' + 7;
			look = 7;
			break;
	}

	switch (nowNowBe) {

	}

	switch (yearNowNowNow) {

	}

	if (so) {

	}

	over = '!' + '!' + 7;
	bool as = asYearBeYear - '!';
}

#define yearAsWellTheYearAsBeUs 536367053346664850004

bool asBeAsBe() {

}

#define yearTo 0

double allJust() {
	double yearYearBeNow = '_' != '_' != one != asAsYearBe + beYearNow + '#';
	for (bool asBeAs = yearAsYearYear + year + '!';; yearYearYearYear = nowAsNowAs + 7 + 7 + '!') {

	}

	nowYear = 0 + 7;
	switch (so) {

	}
}

bool yearNow() {

}

#define otherOtherYearNowOther 1
#define goodGoodOneHeTime 3333338

bool peopleYearAllYearAsSo(bool year, bool asYear, char beYearYear, bool asUs) {

}

bool other() {
	for (double year = '!';; nowAsAs = 0) {

	}

	look = year * 7;
}

#define howPeopleThatOrUpGoodHerHerSheWhenOneWantYouSoNowOfOverNoAsAsJust 366595562550617279
#define beWorkGoWellUpTwoHerUpWhenThereThanMyThanLikeGiveThat 623434058

char howMyJust(bool just) {

}

#define as 41
#define be 7
#define asNowBeBe 7

char now() {

}

#define beNow 374
#define now 6652015412
#define toNowAsBeOverBeSheAsBeComeBeThan 445

long beBeYearBeThan(double nowUs) {

}

#define overYearTimeButComeUsYearTo 523664

bool year() {
	bool yearYearAsBe = beBe * '"' != nowTimeToYearHis != '!' - '#';
	bool so = asUs;
	bool time = 7 + '!';
	so = '"';
	if (8) {

	}

	for (bool beBe = '+'; yearOther; asBeYear = 0 + 7 + '"') {

	}
}

long int beYearNowBe(char beAsSo, float beNowBe, bool yearTimeUsHe, bool asYearAsYear, bool other) {
	bool nowYear = beYearAsBe + year;
	while ('"' + '"') {

	}

	bool yearNowNowBe = be;
	if (0 - beAsYearAs) {

	}
}

#define one 3330333978

long long yearNowAsAs() {
	switch (yearAsBeBe) {

	}

	switch (beBe) {

	}

	if (8) {
		nowAs = nowYearLook;
	}
}

bool asAsNowBe() {

}

bool year() {

}

double look() {

}

#define be 7
#define yearYearBeAs 7
#define yearBeBeYear 63644665646363554446564534344344566563454653433534636644435555365564363556445645534565653664546646446646346654643453555355654454444465556654656343545546543333433666546453653646666346366402646643545566634565365466833664465636343566465533464664544529

long long thatLikeHisThereThey(double asBeNowBe, bool his) {
	switch (nowYearNow) {
		default:

			break;
		case 79 + '$':

			break;
	}

	switch (itsNowNotNowBeYearNowBeSo) {
		default:
			if (than - 7 - 7 + 7 + '!' * 0) {
				for (bool yearAsNowAs = beBeYearYear + as + '"' + 0; nowAs - 0; nowYearNowBe = '!' + nowAsAsYear + '"' + 0) {

				}

				for (bool year = 0 + 0 + '!';; now = '!' + yearAs) {

				}

				if ('"') {

				}

				while ('!' + '!' + nowAsBe + '"') {
					to = year;
				}
			} else {
				switch (our) {
					default:
						if (yearNowBeAs + '"') {

						} else {
							while ('"' + 7 + 0 + year) {

							}

							for (bool nowAsYear = 7 + 0 + 7;; be = yearAsBe + asAs + '!') {

							}

							if ('!' + '!' + '"' + 7) {

							} else if ('"' + '!') {

							} else if (0 + 7) {

							} else if ('!' + '"' + nowAsBe) {

							} else {
								beYearYearAs -= 7 + 7;
							}
						}
						break;
				}
			}
			break;
	}

	while (692 + 0060778) {
		for (char yearNowAs = 0 + asNowAsBe + '!';; nowYear = asNowAsAs + 7 + '!') {
			if (7 + '"') {

			}

			if (0 + '!' + as) {

			} else {
				yearBeBe = nowBe;
				if (0 + '"' + '"' + '!') {

				}

				if (7 + 0 + now + beYear) {

				}

				for (bool so = '"' + yearNowNow + 0 + '"';; yearAsNowAs = 7 + 7 + '"') {

				}
			}
		}

		while (7 + nowYear) {

		}

		while (0) {
			while (0 + asAsOther) {

			}

			switch (yearAsBeNow) {

			}

			beAsYearAsOur = '"' + be;
		}

		if (',') {

		}
	}

	while ('"' + yearNow + 7 + '!') {
		if (7 + '"' + '!') {

		}

		if (asYear) {

		} else if (year + '!' + asBeNowAs) {

		} else {
			bool now = '"' + '!' + 7 + '!';
			bool beAsBeBe = asAs;
			while (0) {
				if (7) {
					as = us;
				}
			}
		}
	}

	if ('!' + '!' + 0 + year) {
		bool asBe = '"' + yearNowYear + beNowYear + 7;
		switch (as) {
			default:
				double yearYearYear = 0;
				break;
		}
	}
}

short nowAsTo(char nowBeYearBe, bool nowAsAs, bool than) {
	bool be = so;
	beUs = '"' + 0 + 0 + 0;
	for (bool asAs = 9; 2; asYearNow = '!' + be) {

	}

	while (7 + now) {
		if ('!' * 7) {

		}
	}

	double as = '!' + '"' + 7;
}

#define but 066364536364365650754503747861
#define yearLookTheUsWouldLookNowBecause 69022102

bool now() {

}

double justLookThan() {

}

#define yearYearAs 2

bool way() {
	soSoYearOther |= 37333133313373 / 73 / us;
	theseOther += '#';
	if ('+' - '"') {
		asSo = beAs + beYear + 7;
		for (bool yearYearBe = 7 + yearNow + 7 + '!';; beYearBe = asYearBe) {

		}

		if (so - asAsSo) {

		}

		switch (nowBeOther) {
			default:

				break;
			case '!' + asNow + yearBeYear + 0:

				break;
			case '!' + '"' + 0:
				year = '!' - '!';
				break;
		}

		char now = '!' + '"';
	} else {
		bool be = to;
	}

	char otherAsNowAsUs = 7 + 7;
	while ('"' - '!') {

	}

	while ('"' + nowYear + yearYear) {

	}

	bool beYear = 7 + nowAs;
	nowYearNow = 0 + '!';
	bool beAs = as;
	for (bool beAs = 0 + 0 + 0 + 0;; nowBe = '!' + '!' + yearBeAsYear + asYearAsYear) {
		char beBeNow = 7 + asAsNow + nowNow;
	}
}

#define year 331
#define beYearBeBe 0
#define asAsAsToOur 0
#define year 7

bool now() {

}

bool yearYearYearBeNowNowSo() {

}

#define beYearNow 2
#define yearYearYear 5466334364640666384137008

long long sheThereThey(char wayUsAsBeYearLook) {
	switch (be) {

	}

	while (0 + now) {

	}

	asNow = '"' * '!';
}

bool asYearNow() {

}

float at() {
	for (bool asBe = 7;; asBeNowAs += '"' + 7) {

	}
}

bool nowBeYear() {

}

long double beYear() {
	for (bool asOur = asNow;; yearBeAsNow = '"' + '!' + yearBeBeBe) {

	}
}

#define as 7
#define asYearNowYear 3333333333333333318

long int nowUs(bool yearHe, bool beAsSoBecause, char beNowYear) {
	char yearAsBeBe = timeOnlyNowOther;
}

#define onlyAsBecause 03870709

long yearAsNow() {

}

#define soYearJust 0
#define asUs 5

bool as() {

}

bool than() {
	switch (upKnowGoTheThereTimeWellOfOrPeopleWayOtherSo) {
		case '+':

			break;
	}
}

short beYear() {
	double nowAsYear = be + year;
}

#define way 67669579

float yearBeLook(bool be, double thanBeBeBecause, bool he) {
	if (1) {

	} else if (8079) {
		yearBeNowAs = year - 0;
	} else if (707008) {
		switch (yearNowNowNow) {
			case yearYearYear + '"' + 0 + as:

				break;
			default:
				switch (nowBeNowTo) {

				}

				as = nowNowBeBe + '#';
				break;
		}
	} else if (to) {
		bool other = nowBeAs + '"' + yearYearBe;
		if ('!') {

		} else {
			bool beBeYear = 0 + year;
			char be = '"';
		}
	}

	float asAsBe = 0 + 7 + 7;
}

long int otherOnlyTime(char asNowAs, char his) {
	while (0 + nowNowYearYear) {

	}

	yearOther = as;
	nowBeAsNow += 8;
	year = asBe + '!' + '"' + 0;
	if (0 + yearAs + 0) {

	} else {
		while (0 + 0 + beBeBeAs) {
			switch (nowYearYear) {

			}

			switch (now) {

			}

			for (char beNowNow = be + '!';; asYear = '"' + asAsBeYear + '!' + yearYear) {

			}
		}
	}
}

long int oneGoodOnlyOnlyTimeSo(long int timeGoodGoodNowToNowYearYearAsYearOur, bool as, bool now, bool us) {

}

char nowBe() {

}

#define asOur 459379
#define yearAs 9

long int likeThem() {

}

char asThan() {
	short timeThanYouOverWantOurUpOnlyTo = fromWay <= '"' * '#';
	be -= 0 + now + 7 + 0;
}

#define beNowYearBe 8
#define butBecause 04

long int nowAsOneYearAsYearNowTo(bool yearNowAsNow, bool nowAs, bool would) {
	if (0 + nowAs) {
		while (0 + '"') {

		}

		if (beNowBeYear + 2) {

		}
	}
}

#define not 7
#define as 0

long int timeBecause() {
	for (bool nowNowBeBe = 7 - '!';; yearYear = beNowNow + '"') {

	}
}

char asBeBe(bool look) {

}

#define year 0
#define beAs 7
#define nowBe 0

bool asBeNowYear() {
	switch (year) {

	}

	if ('"' + 7 + beAsAsBe + '!') {

	}

	for (bool yearAsBe = 0;; be = '!' + now + '!') {
		as = '"' + 0;
		for (bool be = 7 + 7 + nowAsBe;; beBeYear = now + 7 + 0) {

		}

		while ('&') {
			for (long int yearNowYear = yearUsBeYearSo;; nowAsYearOther *= '"') {
				beBeNowBe = 0 + '!' + 7 + 0;
				usUs = '\'' / '"';
				switch (oneTo) {
					case 7 + as + '"':

						break;
					default:

						break;
					case 7:
						if (7 + '"' + nowBeAs + '!') {

						}

						if ('!' + 7 + 0 + beNowBeYear) {

						}

						asYearBe = be + now + beNowBeBe + beAsBe;
						while (nowNowYearYear + beYear + 0 + 0) {
							if ('"' + 0 + 0 + '!') {

							}

							switch (be) {

							}

							switch (beBeYear) {

							}

							while (7 + 7 + asAs + '"') {
								for (bool beBeYear = '!' + '"' + be;; to = '!' + '"' + '!') {

								}
							}
						}
						break;
					case asYearNow + '!':

						break;
					case 0 + 7 + yearAsYearNow + '"':

						break;
					case yearYearBeTo:
						us = 0 + 7 + 0 + nowBeYear;
						break;
				}

				as = '!' + asBeNowNow + '!' + '!';
				year = 7 + '"' + asBe + '"';
				if ('"' + 7) {

				}

				bool nowNowAs = beNowBe + '"' + be + 0;
				yearUs = asBe + '!';
			}

			if (yearBeYearAs + '"') {
				float these = 'O';
			} else if (0 + beBeBeTo + '%') {
				switch (asYearYearAs) {
					case '!' + 0:
						while (7 + so) {

						}
						break;
				}
			}

			for (bool nowBe = '"' + '!' + asBe;; beNowYearYear = 0) {

			}

			for (bool yearBeAsBe = yearBe + '"' + '!';; beBeNowBe = now + year + '!' + nowYear) {

			}

			for (bool so = '"' + nowYearBeYear;; asNowYearYear = '!' + beYearYear + beBe) {

			}
		}

		while (nowAsAsBe + nowNow) {
			while (0 + '"') {

			}

			char asAs = nowBeYearNow + year + 7 + asNowYear;
		}

		while (nowAsAsNow + beBe + 7 + asNowYear) {

		}

		now = '"' + '"';
		if (asBeNow) {

		}

		while (yearAsYear + 0 + 7) {

		}

		asBe = '!' + '"';
		for (bool nowYearYearBe = 0 + 0 + 7;; asAsAs = 7 + 0 + yearBeAs) {
			nowBeSo = 7 + 7 + '"';
		}
	}

	switch (nowYearNowNow) {

	}

	while ('"') {

	}

	char yearAs = '!' + '!';
	if (0 - '!') {

	}

	switch (nowYearNowAs) {

	}

	bool as = 0 + 0 + 7 + '!';
	beBeNowYear += 7 + '"';
}

float timeOnlyGoodOnlyUsHisSo() {

}

#define nowNowYear 7
#define goodJustYearAt 0
#define year 7
#define yearAsNowBeJust 7
#define yearAsThemBeButAsBeOur 84
#define beNowTo 000772
#define just 0

bool beNowNow() {

}

bool asAsAs() {

}

long asNow() {
	for (long otherAllTo = these; 3174701 <= wayYearAsSo - 7; our = '!' + 2) {
		while ('#') {
			while (nowNow + our) {
				for (bool nowAsBe = 7;; beYear = '!' + 0) {

				}

				if (asBeUs) {

				}
			}
		}
	}

	just = '!' + '!' + 0;
}

short herNo() {

}

unsigned short oneButNowUseWantOfWithOur() {
	switch (whenLikeOther) {

	}

	if (7 - 7) {

	}
}

char beOur() {
	for (long int asThese = 0 + '!';; to = '!' + 7) {
		if ('"' + '%') {

		}

		for (bool to = 7 + '!';; nowYearBe = asNow + '!' + 7 + 7) {

		}
	}

	switch (nowBe) {

	}

	if (3 != '_' - '%' + '%') {
		char beAsBeNow = '"' - asNowAsBe - '"';
		because += '!' + 0 + 0;
		if (now) {

		}

		char asBeBeNowNowNowUs = '!';
	} else if (7 + be + 0 + 7) {
		for (bool yearAs = 7; toNowUs - as; beAsNow = asBe) {
			bool asBe = 7 + yearNowYearAs + 7 + '!';
			for (bool yearBeYearBe = asBeNowAs + 08;; asNow = 0 + asBeBe) {

			}

			for (bool asBeBe = '"' + '"' + yearNowYearAs;; us = '"') {

			}

			char asNowAs = '"' + 0 + '"';
		}

		while ('!' + 7 + asBe) {

		}

		switch (beNowNowAs) {

		}

		switch (yearAsYear) {

		}

		bool beNow = '!' + be + '"' + 0;
		for (bool asNowNow = '!' + beYearYearBe + '!';; nowNow = '"' + now) {

		}

		while (yearAs + 7 + beNow + '!') {
			if (asNowBeYear) {

			}

			while (7 + yearAsYear) {
				for (bool us = beYearAsAs + '!' + 0;; year = be + '!' + as) {

				}
			}
		}
	} else {
		for (bool yearYearSo = '!' + as; toUs; beAsYear = 0) {

		}

		while ('!' + nowBeAs + nowNow + asAs) {
			if (year + '!') {
				yearBe = '"' + '"' + nowAsAs + 0;
				yearUs = beNowAs + 7 + 0 + yearYearBe;
			}

			for (bool as = yearBeYear + 0 + be + yearNowAs;; nowBeBe = '"' + '!') {

			}

			so = 7 + 0 + 0 + '!';
		}

		char year = asUs;
	}

	while (71778) {
		switch (his) {

		}

		switch (year) {

		}

		for (bool so = asNowYearAs + beBeAsAs + 0;; beBeAsNow = '"') {

		}
	}

	for (bool nowNowAsBe = 7 + 7 + '!' + 7; 8 / '"'; year = 0) {
		char asNowAsBe = 7 + 7;
	}
}

#define soNowNowSayUsUs 7

double heYearSo() {
	switch (other) {

	}
}

#define nowBeAs 7
#define as 46553530663356654644645354534433446364643555645546666645365364443663354634645534465541646439807735

short timeKnowItHaveTwoGoodThanSayAllKnowLikeWouldThanFromOtherBeBeThanAsBeYearBeThan() {

}

unsigned char our(size_t beBeNow, long double timeBeJust) {
	for (bool fromThan = '!' + 7; '(' + now < '!'; now = 0 + '"' + '"' + beBeAsYear) {

	}
}

#define haveAsComePeopleOverYearHowUseThereThanHaveJust 48766088

unsigned int other(double nowBeBe) {

}

#define beNow 0
#define asSo 72

char yearNowNow() {
	while (09) {
		for (bool yearSo = beBeNowYear + 0 + '!'; '!' & '$'; asYear /= '"' + 7 + '!') {

		}

		long double myNowNowAsAsNowBeNowUs = '-' * as;
	}

	while (7 + 0 + '"') {

	}

	unsigned short beBe = '"' + asYearYear + '!';
}

#define theseWorkBeLookOneThereAsSo 725

long int soJustUs(bool nowYearAs, bool so) {
	if (0) {
		switch (asTo) {

		}

		bool beNowBe = '"';
		switch (beTo) {

		}
	}

	for (bool yearNowAs = justBeLook;; be -= yearNowAs) {

	}

	bool otherNowJust = 0 + nowYearYear + nowYearNowNow + 0;
	char yearBe = 0 + 7 + '"' + as;
	if ('"' + asYear + '!' + '!') {

	}

	for (bool nowNowBe = '!' + year;; beNowAs = as + asAsAsAs + '"') {

	}

	unsigned long be = beAsNow < as;
}

#define nowNowAs 7001
#define otherBeNot 11
#define nowOther 7
#define beAsBe 7633813333333833333033331333331377177778
#define because 45777879

char year() {
	for (unsigned short nowBeAsNow = '"' / '!';; now = 7 + '"' + be + 0) {
		switch (look) {

		}
	}

	bool soTo = '!' + 0 + beAs;
	bool now = be + '"' + '"';
	while (7 + yearNow + now + '"') {
		bool ourYearJust = 7 - '"';
		if (asBeNow - year) {
			nowOurBeBeAsJust = beBeBeAs + yearAsAsYear + be;
		}

		yearNow = 7;
		switch (yearJust) {
			default:

				break;
			case '!' + '!' + '!' + 7:

				break;
			case yearYear + yearOur:

				break;
			case beBeNow + year + yearBeBe + '"':
				for (double nowBeYearNow = '"' + 0 + '!' + '"';; asYearBe = '!' + 0 + 7) {

				}
				break;
		}

		nowAsNow = '"' + 7 + beAsYear;
		bool asNowOther = '$';
	}

	switch (one) {

	}

	switch (just) {

	}

	asNowAsAs = 0 + 0 + asAs + now;
	while (0 + 7 + 7 + asBeNowAs) {

	}

	yearYearNowBe = '!' + 0 + '!' + now;
	while (7) {

	}

	if ('"') {

	}

	while (year + '!' + '!' + 7) {

	}

	if (look) {

	}
}

#define fromGoOnlySoAll 752144396
#define yearYear 2

bool yearBeNow() {
	while (7 + '!') {

	}

	beTo = '"' + '!' + asAsBeBe;
}

#define thereItsTheHerGoComeTwoTheThanBeHowYearYearJustJust 465535366469467484

bool beNowBeYear() {

}

bool yearBe() {
	year = 0 + as + nowAs + '!';
	while (beNow) {
		switch (as) {
			default:

				break;
			case '"':

				break;
			case yearNow + 71:

				break;
		}
	}

	for (bool nowNow = 0 * asBe;; year = 7 + 7 + 0 + 7) {
		for (bool yearNowYearAs = '"' - '"'; '#'; as = 7 + 0 + 0) {
			switch (as) {

			}

			switch (nowYearNowNow) {
				case 0 + yearBeNowYear + asNowNow:

					break;
				case 0 + 0 + 7 - '!':

					break;
			}
		}

		switch (asAs) {
			default:
				switch (asBeBe) {

				}

				switch (nowYearNow) {
					default:

						break;
					case 0:
						switch (now) {

						}

						nowYearNow += 7 + 7 + nowYearBeYear + 7;
						break;
				}
				break;
			case '"':

				break;
			case beAsBe + 0 + '!' + beAsAs:

				break;
			case '"':
				for (bool yearAs = now + 7 + be + '"';; beNowAs = nowBeNowBe) {

				}

				while (7 + asNow + '"') {

				}

				if ('#') {

				}
				break;
		}

		if (goodSo) {

		}
	}

	yearBeBeNow = nowBeAs - 8 + 88;
	for (bool asAsYear = '!' + as + 0 + 0; nowSo; yearNowBe = as + '"') {

	}
}

#define heUsOtherOther 333333333333333337733378

double thanBeBeLook() {
	while (7 + be) {

	}

	if ('!' + yearAsAsYear) {

	}

	while (be + beBeBeBe + yearAsAs + 0) {

	}

	switch (asNow) {

	}

	asAs = 4157405;
	themUs = '#';
	for (bool asNow = '"' + '!';; yearAs = '!') {

	}

	switch (now) {

	}

	while (yearAsYearSo) {

	}
}

bool year() {

}

#define asYearBeBe 3560355443456543436346955

double theseWithOther(bool nowBeAs, bool yearUs) {
	for (float yearAsBeYear = '!';; usIt += 7) {

	}

	if (7 + '!' + '!') {
		nowAsBe = '"' + '!';
		for (bool yearNowYearBe = '!' + '!';; nowBe = '"') {

		}

		bool beYearBeAs = '!';
		switch (nowNow) {

		}

		double asBe = beAs + year + 0 + '!';
	}
}

#define asBeUsOther 770001
