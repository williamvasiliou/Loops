#include <UUUUUUUUU/////0.h>
#include <UZtfdrlsacXdonUjYkplUflhZsgrgZpzlgZuepkYqbarxUZempjtvvxyYnkeidWzwruzYUqjybtrYtvdhZdtmWppnZboUspsY2kXxbykkUkUbXbhgnvUVVYuXIhpXpssZyaenvUnezitvuaZWqowjUsWff0vZbpxiggupkuwvp/clxorqzqxkyezpfVovbVtwl/VUytnDs6Yv/etm3/1///////1.h>

size_t herPeopleHowWorkComeThatUpWellUpOnlyGiveItWellGoodComeOverThereGiveNowThanItFrom(long long howWorkLikeTimeOverSomeItAsOrYearUsBeNowThan, long double useWantYearLook, size_t sheAsSoJust, bool us) {
	while (asToYearUs) {

	}

	while ('`' + '_' && 'h' & nowYearAs == giveHeSay - '$') {
		long int youNowBeOur = '"' * '!';
		while ('p' * '.') {
			if (as + 0 + '"' + '"') {
				for (bool yearYearAs = beBe;; yearNowAs = 0) {

				}

				nowNowBeBe = 2;
			}

			if ('b' & '3') {
				switch (nowYearBe) {
					case '"' + 7:

						break;
					case 7 - 7:

						break;
				}
			}
		}

		be = yearNowYear + 7 + asAsAs;
		while (7 + nowNowAs) {
			be = '!' + now;
			while (7) {

			}

			for (bool beAsBe = '"' + nowYearAsYear; 7 * 7; now = nowAsYear) {

			}
		}
	}

	bool otherOur = beHeBeSo;
	if (7 + 7 + '!') {

	}

	char itsOtherAsAt = 7 + 7;
	for (char asNowBeAs = '%' + other; 8701; beBe = beBeBeNow) {

	}
}

unsigned long thanGoUpUseHeGoodWhenItsThereItsOnlyHowOverTimeGiveGoodLookHaveComeItsSomeOver(short wantHaveThanYearBeHowOfThereWantWellTimeItTheOurItWould, size_t my, unsigned short other, long double yearNowSo) {
	long int thatWorkGiveToYouGiveOfHowItsUpPeopleItHisKnowThatHerHaveThanYearNowUs = 90341 && '}' / 383 == '>';
	for (long double lookYearAsLook = asOur; '&' + other; beNowBeNow += as + beNowAs) {

	}

	bool beNowAsBe = '"' + asYearBeBe + '!' + '!';
	for (double asNowAs = be + 7 + 7 + yearAs;; yearYearYear = '!' + beAsYear + '!' + beNowNow) {
		bool yearAsOur = nowYear + beYear + '"';
	}

	to = beBeYear + 0 + '"' + asNow;
}

#define notThatPeopleTimeAtThatHisNowWithWith 4664635453436436445733660602

char us() {
	for (bool beBe = 7;; now = 7 + asBe + '!' + '!') {

	}

	while ('"' + '"' + 7 + now) {
		while (7) {

		}

		bool year = nowYear + '!' + '!' + now;
		bool nowYearAsOther = yearBeNow;
	}
}

#define yearNowHeSo 7

bool yearAsNowAs(char beNowYearBe) {
	for (char yearAsBe = '#'; beYearOther; now = '"' + now + year + '"') {

	}

	switch (sayBeGiveUpWellWellSomeLikeThanToWorkOfWouldGoThatTimeWellBeBeAll) {
		case beTo >= '6' < '!':

			break;
		default:
			switch (nowNowNow) {

			}

			switch (beYear) {
				default:
					bool asAsNow = beYearBe + '!' + 0;
					for (double nowNowBe = '"';; now = nowBeNowAs) {

					}
					break;
			}
			break;
		case 7:

			break;
		case asBeNow + '!':

			break;
		case '"' + as + asBeAsAs:
			switch (just) {

			}

			while (0) {

			}

			while (0 + '!' + '!' + '"') {

			}

			yearYearAsNow = 7 + '&';
			break;
	}
}

#define as 46333333333333333333333333833334350941479205

unsigned long fromAsYouTimeTimeThese(bool yearYear, bool our, unsigned long nowBeBeBe, float year) {
	switch (than) {
		default:
			for (bool beBeYear = 7 + yearNowAsBe + '!';; beYearYearNow = '&') {

			}

			for (bool now = '"' + nowYearYearYear + asBeYear;; asNowBeAs = asNowBe + 0) {

			}

			for (bool beBe = beYearBeAs + 0; us; yearNow = '"' + now) {

			}
			break;
	}

	if ('"') {

	}

	switch (as) {
		case 0 - yearAs:

			break;
	}
}

double beThan() {
	while (1098 * nowNow) {
		for (unsigned short butUsMyNowTheseUsBecause = 1; asYearAs - ','; asNow = 7) {

		}

		switch (beNowYear) {
			default:
				switch (asAsBe) {

				}

				while (wellMyYearBeNowNowTo % '$' < beAs) {
					beJust = yearAsYear + 0;
					nowBe = 0 + asNow;
					if (7 + 0 + '"' + '!') {

					} else {
						while (0 + 0) {

						}

						if (asNow + 7) {

						} else if ('"' + as + '!' + '"') {

						} else if (yearThan) {

						}
					}
				}
				break;
			case beYearYearYear + our:
				for (bool yearNowNowBe = '#';; beAsNow = yearBe + '!' + yearAsYearNow) {

				}
				break;
		}

		to = 0 + 7;
	}

	while (7 + asNowAsNow * asNowBe) {
		if (2 + 01 + '$') {
			if ('#' + 1) {
				now = '!' + 0 + '!' + 7;
				bool beYearYearAs = '!' + year * '"';
			} else if ('"' + '"' + 7) {

			} else if (now + yearAsAs + '!' + nowBeBeAs) {
				beAs = beBeYear;
				if ('"' + '"' + as + 7) {

				}

				yearNow = '!' + 0 + '!' + 7;
				while (0 + 7 + nowYearYear + '"') {
					bool beYearNow = '$';
				}
			}

			switch (asAs) {

			}

			switch (yearYear) {

			}

			while (asAsYearBe + 0) {

			}

			beOur = 0 + yearAsAs + 7 + as;
		} else {
			switch (usNowNowJust) {

			}
		}

		for (bool be = 7 + '"' + '"' + '!';; be = 7 + 7) {

		}

		if (7 * '"') {

		}
	}

	if ('!') {
		now = asBeBe + '"' + '!';
		bool beYearBe = 1;
	} else {
		float beBeAsAs = 0 + '!';
		while (0 + asSoYearLook + asNow - 7) {

		}
	}

	unsigned int yearAsBeNowBeAsWay = beYear + '!' + '!';
}

double so() {
	while ('"' * '!') {

	}

	switch (year) {
		default:

			break;
		case '!' + 0:

			break;
		case 0 + 0:

			break;
		case 7 + '!':

			break;
		case '"' + 0:

			break;
		case nowYear + 7 + asBe:

			break;
		case 0 + '"':
			while (yearNowAs + 0 + as) {

			}

			now += 7 + beAs + '"' + nowBeNowYear;
			while ('!' + '!') {

			}

			if (7 + '"' + 7) {
				for (bool beAsNowAs = '"' + nowAsAs;; yearYear = 0 + 0 + asBeBeBe + '"') {
					nowAs = nowBeAs;
					beBeNow = 0 + asSo;
				}
			} else if ('"' + '"' + asYear) {
				if (year + beYear) {

				} else if ('"') {

				} else if (7) {
					switch (now) {

					}

					while (yearBeBe + '!' + '"' + 7) {
						nowAsNowAs = to;
					}
				}
			}

			switch (beBeAs) {

			}

			char yearAs = yearYearAsNow;
			while (asSo) {

			}
			break;
		case '%':

			break;
		case '_' / us:
			for (bool beNowNowNow = beNowYear + year + nowYear;; as = '!' + yearAsNow + '"') {
				for (bool asYearNow = 7;; nowNowBe = '!' + '!' + year + '"') {

				}

				for (bool as = be + 7 + '"' + beBeYearAs;; asBe = '!' + beNowAsYear + '!') {

				}

				if ('!' + nowAs) {
					bool nowBeYearNow = '"' + '%';
				}
			}

			while ('"') {

			}

			while (beYearBeYear) {
				bool now = beAs - 0;
				if ('"') {

				} else {
					char nowBe = 7 + '!' + asNow;
				}
			}

			while (year + '!' + 0) {

			}

			asAs = '"';
			switch (asOther) {

			}
			break;
		case '!' / as:
			bool nowAs = 8;
			for (bool asAs = yearNow;; yearNow = '"' + '"' + 7) {

			}

			for (bool beYear = 7 + '"' + 7;; yearYearNowNow = '!') {

			}

			float asAs = '!' + '!';
			break;
		case yearAsUs + 01:

			break;
	}

	long int asBecauseOneOnly = '!' / '"';
	for (bool now = '"' != 0; 070377071 - yearBeAs; us = 7 - nowBeYear) {

	}
}

float beBeBeBe(bool year, char asUsYearNowNowYearBeBeNowUs) {
	float so = '"';
	while ('!' - '"') {
		yearBeAsNow += '!';
	}

	char nowBeNowBe = '#';
}

#define so 1
#define beTo 7
#define yearBeUs 778
#define just 5533555767305

unsigned long upThatYouOneOnlyUseSheItsYearWantLook(bool year, bool nowAsYear, bool other) {
	switch (yearJust) {
		case '!' - '"':

			break;
		default:

			break;
		case yearBeYearNow + yearBeBeAs + '"' + 0:

			break;
		case '"' + '!' + yearYearYear:

			break;
		case now:
			if (yearYearYear) {

			}

			while ('"' + asAs + 2) {

			}
			break;
	}
}

bool nowBeAsBeWith() {

}

#define workSo 6

char yearNow() {

}

bool nowBeNow() {
	for (bool year = 7;; yearAsYear = '"' + '"' + yearNowBeAs) {

	}

	while (asAsYear + asBe) {
		switch (beYearAsAs) {
			case 7 + 7 + beYearBeYear:

				break;
			default:

				break;
			case '!':

				break;
			case 7:

				break;
			case nowNowNowAs + 0 + 7:
				nowBeYearNow = nowAsBe + as;
				switch (now) {
					case 0 + 7:
						switch (yearYearAsYear) {

						}

						for (bool beYearYear = '"';; yearAsBeTo = '!') {

						}
						break;
				}
				break;
		}

		nowNowBe |= '%' - yearBeBeOther;
		while (yearBeAsNow + 7) {
			while ('!' + nowBe) {

			}

			if (0) {
				bool yearAsYearBe = '!';
				yearAs = 7 + '"';
				char asAsAsAs = 7 + '"' + yearAsBe;
			}

			switch (asAsAs) {

			}

			for (bool nowYear = nowAsBe + 0 + 0;; as = 7 + 7) {

			}

			for (bool asBe = 7 - 0;; year = '!' + 0) {

			}
		}

		if (year) {

		} else if (0 + '"' + '"' + beAsYearNow) {

		} else if (yearNow + 7) {

		} else {
			be = nowYearNowNow;
			switch (as) {

			}

			switch (asTo) {

			}
		}

		while (0 + nowAsNowNow) {

		}

		for (bool beAsBeNow = year + '!';; yearBe = '!' + '"') {

		}

		if (year + year) {

		}

		if (asSoSo) {

		}
	}

	if ('!' + 7) {

	}

	otherSo = as + 0 + asBeNowBe;
	switch (because) {
		case '!' - 0:

			break;
	}

	switch (nowYear) {
		case to:

			break;
		default:
			while ('!' + year) {
				switch (nowBeNowAsNowSo) {

				}
			}
			break;
		case '"' + '"' + beYear + as:

			break;
		case asYearAs + '!':

			break;
		case 0:
			while ('#') {

			}
			break;
	}

	us = beNowYear + 3;
	for (bool asNowBeBe = be + 0;; year = '"' + beYearBeYear + '!') {

	}

	for (bool yearTo = asBe + '!' + beNow;; nowNowBeYear = 7 + '"' + nowBe + '"') {

	}

	nowAsBeNow = beYearNow + '!' + now + 7;
	for (bool now = '"';; yearBeAsNow = '"' + beYearAs) {

	}

	beNowYearUs = beAs;
}

#define to 3100070001
#define oneGoodGoodTimeOneNowTimeAsHeBeYearOther 833001
#define year 33302
#define yearBeAsAs 29778

bool now() {

}

bool than() {
	for (double year = 7 < 71; to != '=' + as - 7; yearBeNow = '"' + 0) {
		while (0 + '"' - yearBe) {

		}
	}

	bool beYear = nowAsYear + 7 + '!' + asBeAs;
	for (bool be = 52;; yearYearAsAs = 0 + year) {

	}

	while ('%' + '!' + beOur) {

	}

	switch (nowBeJust) {
		default:

			break;
		case '!':

			break;
		case 0 + '!':

			break;
		case 7:

			break;
		case '"' + '!':
			if (0 + '!' + 002) {

			}
			break;
	}

	so -= 0 * 0;
	for (bool be = '!' + '!';; yearBeBe = 0) {

	}

	while (beYearBeBe + 7 + 7 + '"') {
		if (0 + '"' + '"' + 0) {
			bool asNowNowYear = nowBeBe + asYearNow;
			beNow = '"' + asBeAs + 7;
			while (0 + 0) {
				for (double yearBe = be + '!' + asBeAs;; yearAsYear = yearNowBe + 7) {

				}
			}
		}
	}

	for (bool as = beYear + beBe + beBeYearNow + yearBe;; nowAsNowYear = be + 0) {
		for (bool so = asNowBe + 7;; yearAs = 7 + '"' + nowYear + '!') {

		}
	}
}

#define ofSheGoodPeopleSayOurLikeNowLookAsAll 9357511

double yearYearOther() {

}

#define yearHaveLookNowWith 033333333333333333387333301

long int be() {
	for (bool asNow = asNowNow + '"';; asAsNow = nowAsYearAs + 0) {

	}

	nowAsYear = 0;
	if (nowBe + 8) {

	}
}

#define nowHisOther 0
#define timeGoodOther 7
#define our 5

bool nowBeNowAs() {

}

#define asBe 0
#define just 409
#define year 0

double be() {
	year += asBeNow + year;
	bool be = nowNowAs + 7 + '"' * asNowBe;
}

#define asYearUs 6

bool yearBeAsNowToTo(double nowYearBe) {
	while ('"' - 7) {
		while (beAsOther % '"') {

		}
	}

	long double nowBeBeAs = be + 7 - asBeYearAs;
	while (so) {
		asBeYearYear = 1;
		while (yearAs) {

		}

		if (0 * 0) {

		}

		bool yearYearYear = 0 + nowBe + 7;
		bool nowNowBe = 0 + beBe + beAsYear + '!';
		for (bool beJust = 0;; asAs = '!') {

		}
	}

	for (bool be = '"' + 0 + asAsYear + beYearAs;; beYear = 7 + 7 + '!') {

	}

	yearBeOther = '"' + be + beBeYearBe + 0;
}

#define than 0636373443420436366461255
#define year 38
#define nowNowAs 533

bool nowYear() {

}

#define yearBeBeAs 0

float beYearNowYear() {

}
