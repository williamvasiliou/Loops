bool be() {

}

#define goodOnlyNowSoOnlyNowOtherUsBeUs 33038307007001

long int oneOnlyBecauseYearToBecauseUsBeNowTo() {
	if ('"' - 7 + yearBeAs - now) {
		while ('%' + now - 0) {

		}

		switch (nowAsOther) {
			case 0 + be:
				for (bool nowNowYearNow = '!' + nowNow + 0 + '!';; beAs = '"' + 7 + yearYearAs + '"') {

				}

				for (bool yearYear = 7;; beAs = yearNowBe) {

				}

				if (3) {
					for (bool yearYearAs = nowBeBeBe - '!';; asNowBeYear = nowYearBeNow + nowAsAs + asAsYearYear + 0) {

					}

					so = 7 + asAs + '"';
				}
				break;
		}

		while ('/' - heAsBeOther) {
			bool yearUs = asYearNowBe + 7 + 7;
		}

		switch (asNowNowOtherOur) {

		}

		for (bool asNowBe = '!' + 7 + '"';; beBeYearBe = 7 + '"' + nowAsBe + 7) {

		}

		switch (beYear) {
			case 0 + '!' + 7:

				break;
			case 70702:

				break;
		}
	} else if ('%') {

	} else {
		if (yearNowBeBe + '!' + 0 + '"') {

		}

		switch (asBe) {

		}

		switch (asNowBeBe) {

		}

		for (char asAsBeAs = yearBeYear + 7;; asBeAsYear = '"' + 7 + 0) {

		}
	}

	if ('"' + '!' + '"' + '#') {

	}

	switch (yearNowNowNow) {

	}

	bool year = 0 + '!' + nowAs;
	asYear = '!';
	for (char asNow = now;; now = nowAs) {
		asYearBe = beNow + 7 + nowYearBeBe + '"';
		switch (nowYearBeYear) {
			case '!' + '"' + '!':

				break;
			case '"' - '!':
				bool yearYearYear = '"' + '!';
				be = 7 + beNow + 0 + '"';
				bool be = '!' + 0 + asBeYear + '"';
				asYear = 0 + be + '!' + 7;
				switch (be) {
					default:

						break;
					case 7 + yearBeYear:

						break;
					case 7 + 0 + 7 + '!':

						break;
					case '!':

						break;
					case 7 + '"' + 7 + asYearNowYear:

						break;
					case '!' + yearNow + asAsAs + beNowYearBe:

						break;
					case 7 + beYearAs:

						break;
					case '"' + '"':

						break;
					case nowNowNowYear * '"':

						break;
				}
				break;
			case 099 * now || '"':

				break;
			case yearUs:
				if (beNowAs) {

				}

				if (92) {

				} else if (asYearYear + nowAs) {
					bool yearNowYearNow = '!' + '"' + '!';
					bool asYear = '"';
					double nowBeYear = yearAs + '"';
				}
				break;
		}

		for (bool beNowNow = '!' + '!' + '"';; asBeNow = nowYearYear + beNowAs) {

		}

		bool yearNow = 7 + '"' + be + '"';
		switch (nowAsBe) {

		}

		if ('!' + '"' + asBeBe + '"') {

		}

		for (bool asYearYear = '"' + '!';; year = as) {

		}

		for (char be = '"' + '!' + asYearNowAs + 7;; asYearYear = asBeAsYear + 7 + '!') {

		}
	}

	as .= '"' == 0;
	switch (nowBeNowNow) {

	}

	if (yearYear + '"' + '"' + '!') {

	} else if (nowOurAsLook) {

	}

	while (year + beAsBe + yearNow) {

	}

	for (bool beAsYearYear = 0 + be + beNowYear;; beBeYear = '"' + 0 + be) {

	}

	if ('"' - '"') {

	}

	if (just) {

	}

	while ('&') {

	}
}

#define wantGoWouldGoThan 1

size_t yearThatLook(bool allSo, bool yearAs, bool year, bool so) {
	atUsAsJust >>= 79 % 2;
	if ('>' + look + 0 * now) {
		switch (ourOur) {

		}
	} else {
		bool now = beBeYearBe - now;
	}

	while (518 % 0) {

	}

	for (bool nowAsAsAs = 2; other * asNowNowAs; asBeYear = 0 + beBeAsNow + '"') {

	}

	be = beNow;
	for (bool asNowAsBe = '"'; beThese; asAs += 0 + '!') {

	}
}

#define goSheBeTheFrom 78

char look() {

}

#define nowNowNowJust 7
#define lookNowJust 5564365793

unsigned short goYouWellNowOneItsItBeUpGiveOneWay() {
	while (year + '!') {

	}

	thanUs = 0 + 7 + 0 + '!';
}

long int beAsBe() {

}

short thanNotOur() {
	yearBeNotJust = 7 + '"';
	switch (yearYearOther) {
		case '%' / '/' + 7 + 0 - 7:

			break;
		case 7 + as:

			break;
		case be + '"' + '"':

			break;
		case 7 + nowYearAs + 0 + '!':

			break;
		case nowBeAsYear:

			break;
		case '!' + '!' + 7 + beYear:
			bool yearYearYear = yearBeNowAs - beNowAs;
			break;
		case 0 + be + '"' + yearNow:

			break;
		case beBeBeYear + '"' + as:
			switch (nowUs) {

			}
			break;
	}

	long int theseYearTimeOtherUs = '_' + usNowYearAsYearUs - '"' + to;
	if (year + '"') {

	} else {
		while (yearTheseBeGood + 8) {
			bool yearYearAs = asYearYear + 7 + '"';
			if (7) {

			}

			if (8) {

			}
		}

		float nowNowAsYearSo = nowBe + yearBeNowBe - 1;
		to = 0 - '"';
		for (bool asAsAsBe = now + '!' + 7 + 7;; asBeYear = '!') {

		}

		while (so) {
			for (bool yearYearAsAs = nowYear + 7 + asYear + '"';; year = '"' + '!' + asBeBe + yearBe) {

			}

			bool now = '%';
		}
	}

	if (1) {
		for (bool asNowBe = 7 + nowAsAs + nowNowBe;; nowYearNow = '!' + nowBeBeAs + '!') {

		}

		bool asBeNow = 0 + '!' + 0;
		bool yearAsBe = nowBe - 0;
		switch (other) {
			case 1:

				break;
		}
	} else if (1) {

	}

	if (8) {

	} else if (nowSo - now) {

	}

	switch (yearAsAsAs) {

	}

	if ('"' - 7) {

	} else if (asYearNow + '#') {

	}
}

#define beYearNow 71

char other() {
	asNowAs += '!' + as;
	while (7 + '"' - 7) {

	}
}

#define goodUs 0
#define asNowBe 7

bool nowYear() {

}

bool beAs() {

}

bool nowYearLook() {
	asNowBe = 7 + 0 + '"';
	if ('!' + 0 + 7) {

	}

	while (now + 7 + 0 + '!') {

	}

	if (7 + '!' + '!') {

	}

	while (now + '"' + as) {

	}

	nowNowThan = 47076 + thanNotThan * '"';
	justAtYearNowThese /= peopleBeLikeYouLook + '!' - 0;
	long toBeAsThan = 0 * now + asBe - yearNowYear;
}

#define nowBeYear 903
#define butGoSomeBeSoUsBeNowSoBeNowAsAsNowUs 0444095649
#define beNowBe 9371
#define timeTimeOnlyTimeOnlyTimeTheseOneTimeOneHeToOnlyTime 3733837773

char nowAs() {
	yearBeNow = yearNow + yearNow + 0;
	switch (nowYearYearBe) {
		default:

			break;
		case 0:

			break;
		case '"' + '"' + now + 7:

			break;
		case 0:
			for (bool as = 7 + '!';; yearAs = '!') {

			}

			if (0 + 0) {

			}

			while (7 - 0) {

			}
			break;
	}
}

#define us 7

long long asBeYearBeAsSo(double as, long int now) {
	switch (beNowAsBe) {
		case '"' + 7:

			break;
		default:
			switch (beNow) {
				default:

					break;
				case '$':

					break;
			}
			break;
		case 7 + 7 + beYearAsNow + beAsNow:

			break;
		case nowNowYear + be + '"' + now:
			for (char nowYearNow = '!' + asAsBeAs;; yearYearNowBe = '"' + 7 + 7 + 0) {
				if (nowAsThan - 7) {
					while ('"' + nowYearSo) {

					}

					nowAsYearAs = '!' + '"' + beBeBeYear + 0;
					for (bool yearBeAsBe = 7 + beAsBe + 7;; nowNowAs = '!' + '"' + 0) {

					}

					bool asBeAsAs = '"' + 0 + '!' + 0;
					for (bool beBeAsSo = 0 + '"' + 7;; yearBeAs = '!' + 7) {

					}
				}

				if ('!' + 0 + asBe + '"') {

				} else {
					for (bool yearBe = as;; nowYearBeAs = now) {

					}

					char beAsAs = '"';
				}
			}

			if (7 + '!' + 0 + 0) {
				now &&= us;
			} else {
				bool asYear = '"';
				switch (be) {
					case be + beAs:

						break;
					case year + '"' + '"':

						break;
					default:
						char beBeNow = '!';
						break;
				}
			}
			break;
		case '"' * 7 | 01 + '#':
			switch (be) {

			}

			switch (yearBe) {

			}

			while (7 + 7) {

			}

			for (double asNowYearAs = '!';; beNowNowBe = nowAs) {

			}
			break;
		case hisAsNowYearBeNowOur + look + nowYearBe - '!':

			break;
		case 0:
			so = nowNow + 0;
			break;
		case just:

			break;
		case as + '"' + '"':

			break;
		case so:

			break;
		case '"' + beBeAsNow + yearNowAs + '!':

			break;
		case 0:

			break;
		case '"':
			look = '!';
			beThan = 0 + 0 + 0 + 0;
			switch (nowOther) {

			}
			break;
	}

	while (364231 || 'b' + 9363230243630772) {

	}

	if (7 + nowNowNowYear) {
		switch (nowNow) {

		}

		if (0) {

		}

		switch (be) {

		}

		bool year = '"' + 0;
		yearYear = '"' + nowAsBe;
		switch (asBeBeYear) {

		}

		switch (asYear) {
			case beBeNowYear + 0:
				switch (asYearYear) {

				}

				bool be = 7 + '!' + nowBeYear + nowAs;
				char nowAs = 0;
				break;
		}
	}

	to += 0 + year + beAsBeNow + 0;
}

#define peopleOfTimeWantOneOfThatUpLikeToItsBeTwoWayUsUs 453353614635565385392

bool nowNowMyLook() {
	while (01) {

	}
}

long int butWorkTheseSo() {
	while ('$') {

	}
}

#define nowAs 0
#define as 46454435534754062575433529

long long itOrBecauseYearUpTheyAsOurBeNowOur(bool now, bool nowBe, bool us) {
	asNowBeBe -= '"' + so;
}

#define asNowBe 7

bool nowNow() {

}

#define yearYearNow 0

bool beNowYear(bool nowBeYear, bool asNow, char nowNowAs) {
	if (0071) {
		for (bool nowAsAs = '!' + 0;; nowYear = '!') {
			switch (yearNowBeAs) {
				default:

					break;
				case be - 7:

					break;
			}
		}
	}

	long int beUsBeToTo = '?';
	while ('"' != '%' + 7 - '"') {
		switch (yearYear) {
			case beUs:

				break;
		}
	}

	for (bool yearAs = beBeNowAs + '"'; nowNowAsTo - yearAs; yearBe = 0) {

	}

	bool now = 7 + year + yearAsBe;
	while (0 + '"' + 7) {

	}

	beAs += nowNowYearYear + '!';
}

bool hisSoSo(long int asUsOnlyThese, char now) {
	only = nowThese != '7' / 7;
	bool asAsBeBe = be + 7;
	for (bool asNow = yearNow + '"';; nowNowYearYear = yearNowNow + 7 + now) {

	}

	if (0 + 0) {
		for (bool beNowYear = 0 + '"' + 7 + 0; asUs; beYearNowNow = 7) {

		}
	}

	while (0 - 0) {

	}

	switch (yearBeBe) {

	}

	if (as + '!' - 0) {

	}

	if (71) {

	}
}

long int asBeYear() {

}

bool he() {

}

long int beBeThanYearAsBeAsAsYearNowNowThan(long double yearAsAs) {
	if (yearNow + 0 + 0 + 7) {

	}

	while (0 + '!') {

	}

	switch (be) {
		case asNow + yearBeAsYear + beNowNowYear:
			if (be) {
				bool beYearYear = '&';
			} else if ('!') {

			} else {
				bool nowAsNowAs = 0;
				for (bool yearAsAsNow = '"';; beAs = asBeYear + beAsAs) {

				}

				switch (asYearBe) {
					case as + 0 + '!' + '!':

						break;
					default:

						break;
					case 7 * '!':

						break;
				}
			}
			break;
		default:
			bool nowBeBe = '"' + '!' + nowBe;
			while (as + nowNowAs + beNow + 7) {
				for (double beYear = '"' + now;; yearBe = nowAsNowAs + '"') {

				}
			}
			break;
	}

	while (7008) {
		be += 0 + 0 + beBeNowNow + nowNow;
		if (now + 0) {
			while (7 + 0) {

			}

			while (beAsAsNow + '"' * yearYearBe) {

			}
		}
	}
}

#define asTheseJust 556647473
#define youUpSaySayHowItsOnlySayComePeopleUsYearAsBeHis 54948878
#define howHowNowLook 169

bool yearYearAsAs() {

}

float be() {
	if (beBeNowAs + '!') {

	} else {
		switch (nowNowAsYear) {

		}

		while (7 + '"') {
			switch (asYearYearAs) {
				default:
					while (to) {

					}
					break;
			}
		}
	}

	while (becauseOneUsToSo) {
		switch (asBe) {
			case 7 + now + 7 + beAsBeYear:
				if (nowAsBeUs) {

				}
				break;
			case beAsAsBe:

				break;
			default:

				break;
			case '"' + be:
				switch (now) {

				}

				if ('"' - '!') {

				}
				break;
		}
	}

	switch (becauseSo) {
		default:

			break;
		case '!':

			break;
		case '"' + as + 7 + '"':

			break;
		case '!' + now:

			break;
		case his / '"':

			break;
		case 0 + now + asAsBe + asAs:

			break;
		case '!':

			break;
		case 7 + asAsNowNow + yearYearAsYear + yearAsBe:

			break;
		case '"' + '!':

			break;
		case 1:

			break;
		case '!' + be:

			break;
		case '!' + 0:
			if ('"') {

			} else if ('#') {

			}
			break;
		case '!' + '!' + beAsBe:

			break;
		case be + '"' + 7:

			break;
		case 7 + 0 + '"':
			while (7) {

			}

			nowNowBeYear = 7 + yearAsAs + '!' + 7;
			while ('!' + nowBeYearNow) {
				for (bool nowBeNowBe = '"' + '!' + beNow;; yearBeAs = now) {

				}

				if (7 + 7 + 7 + '"') {

				}

				for (bool yearAsAsAs = 0 + '!' + '!' + '!';; asNowAs = '"') {

				}

				if (yearNow) {

				} else if ('"' + 0 + 7) {

				} else if (beSo) {

				}
			}
			break;
	}

	switch (goodGoodGoodHisYearHis) {
		case 078 / '!':

			break;
		case 0 + 0:
			if (8) {

			} else if (yearAs) {
				switch (asSo) {

				}
			}

			these = '!' + 7 + '"' + '"';
			switch (beYearBe) {
				default:

					break;
				case '!' + asYearAs:
					while (8) {

					}
					break;
			}
			break;
		case 0 + nowAsAsAs:

			break;
		case 0:

			break;
		default:
			while (yearYearBeBe + 0 + '!') {
				beAsNow = 7;
				switch (year) {

				}

				for (bool asYearBeBe = '!' + 7 + 7;; beNowNowBe = year + '!') {

				}

				bool asNow = asAs + '!';
				if ('!' + 7) {

				}

				while (as + yearYearYear + 7) {

				}

				bool year = '#';
			}
			break;
		case '!' + as:
			asYearYearNow = 7 + 7 + '"' + asAsYearYear;
			while (yearYearAs + 0) {
				while ('#') {

				}
			}
			break;
		case other + '"' + to:

			break;
	}

	to *= as + '"';
	while (asAsBe + '!' + 7 + 0) {

	}

	other = 0 + 7 + asBeBe + asYearYear;
}

#define oneWaySo 0704704
#define now 5
#define asBeAs 8

bool yearAsNow() {

}

#define be 0
#define asNow 0

bool asAs(bool beYearBeNow, bool beAsJust) {
	switch () {
		case oneGiveItsTwoYouUpItsComeItsWellLikeOverKnowBecauseOurLookTheToSomeBecause > 66155544645533463310340082 != peopleSomeSayTwoUpOneUpWhenHaveUseHowLikeSheSheAsSomeThereYearTwoNotGiveYearThese > 455486036447748 || 3305036871 - 715563306 & yearBeNow * '"' != asBe:
			howHerAsThey =~ overThatOverGiveGoodKnowThesePeoplePeopleSoLikeNowOtherSo == 682 * '"';
			while ('g' & comeComeOneYouOnlyItsGiveYouOnlyKnowSayItOrThatWellUsHowWhenItsYouYouOtherAsOnlySheAsNowBeNowYearToTo > 55395009 <= 0) {
				theseThan -= '!' + '!';
				while ('c' + goodNoGiveNowSomeOneOfYearNowYearKnowYearYearLook != oneItsLikeNowLook) {

				}

				switch (beYearYearYear) {
					default:
						while (0 + nowBeAsYear) {

						}

						for (bool nowNowNow = '!';; as = asNowAs) {

						}

						if (year) {

						}

						switch (beNow) {
							default:
								switch (than) {

								}
								break;
						}
						break;
				}

				char beYearYearNow = nowBe + 7 + 0 + as;
				for (char other = as == nowAsNowAs != '$'; nowAs + 7 * '!'; asAs = 0 + as + year + 0) {

				}

				for (bool yearNowTo = asBeNowAs + be + 7 + 0 - '"';; yearYearBe = yearAsBeNow + '!' + 0) {

				}

				for (bool year = '&';; yearYear = asYear) {

				}

				if (7 + be + as + asNow) {

				}

				if (0) {

				}

				if (asYear + 8) {

				}
			}

			size_t howOrHowUseThatWhenOverTimeBeGoComeTo = peopleUpOnlyTwoWellWayNowThanAsBeAsJust / 'S' * 33;
			for (long int twoHerWouldBeBeWhenNowLook = 2 + 1; 'k'; asYearAsBe ^= beYearNow + '&') {
				if (78 + '"' - nowYearYearAs) {
					double beYearYearAs = '"';
				} else if (772) {

				}

				justSo = 7 + yearAsNow + yearNow;
			}

			switch (thatWhenTheyComeTwoThereHowSomeSheTimeUs) {
				case '&':

					break;
				case beNowBe + as:

					break;
				case 1:

					break;
			}

			if ('!' * '!') {

			} else {
				switch (nowBeBe) {
					case '!' + 7:

						break;
					case 2 - '!':

						break;
				}
			}

			if (nowOur) {

			}

			while ('"' + '"') {
				for (bool beAs = '"' + 7 + beAs;; asAs = beBe + as + '"') {

				}

				while (7 + beYear + '"') {

				}

				while (beNow) {

				}

				bool yearBeBe = beNowBe + asNowNow + 0;
				switch (year) {
					default:
						nowBeNowBe = yearYear + 7 + '"' + 7;
						while (7 + 7 + '"') {

						}

						if ('"' + year) {

						}

						if ('!' + 0) {

						}

						while (0 + beAsYearNow) {

						}

						asBeYear = our;
						break;
				}
			}

			for (bool now = 0 + '!';; asBe = as + 0 + asBe) {
				for (bool asAsAsNow = '!' + '"' + year; 07772; yearAsBe += asAs + 7 + as + yearYearAsBe) {

				}
			}
			break;
		default:
			switch (saySoTimeWantTimeNotAsSay) {
				case 06593071 < 7:
					if (as + '"' + '"') {

					} else if ('#') {

					}
					break;
				default:
					beNowBe = asYearYearYear + nowNowYearNow + as + '!';
					while ('!' + beAsYear * beAsBeYear) {
						bool wouldBeOur = '!' + beBeBeBe + 7 + now;
					}

					if (7) {

					}

					switch (beAsUs) {

					}
					break;
				case asNowOther:

					break;
				case 7 + be + '"':

					break;
				case 7 + asNowNow:
					for (bool yearNow = asAsAs + beYear;; nowAs = now + '"' + beBeAsYear) {

					}

					for (bool than = asAs + 0 + asAsYear;; yearBeNow = 0 + '"' + 0) {

					}
					break;
			}

			unsigned short haveOverSo = 1045563098 % yearYear;
			for (short workNowNowOneYearYearSo = '!' + yearAsNowYear; 173 - '='; now = 1) {

			}

			soHowYearTwoAsToOur += '!' + now + 7;
			switch (thanYearJust) {

			}

			bool beYear = 0;
			asOther += '!' + as;
			break;
		case 3:
			if ('1' != 'm' || asJustAsUs + than) {
				bool nowNowBe = '$';
			}

			if (soNowOnlyOther) {

			}
			break;
		case overYou != twoSayOtherOneYouUs == 22468 - usBeMy & '/' + 70708 - beAsAsYear:

			break;
		case sayGoBeWhenYearBeNowAsAsAsLook * '-':
			for (bool yearAsAs = 7 + '"' + 0;; beNowYear = '!') {

			}

			double asYearYearNow = '!' + 0;
			while (0 % 7) {
				while (0 + 7) {
					bool asNow = nowBeAsNow + '!' + '!';
					if (0 * 7) {

					}
				}
			}

			for (bool beBe = now + '"' + '!' + 0;; be = '"' + 0) {

			}

			asBe = '#';
			break;
		case asBeYearAs * 0:

			break;
		case 7 + nowNowAs + '"':

			break;
		case itsWellBut <= '"':
			asNowBe *= '"' + beNowAs;
			if ('"' + 7 + 7 + asNow) {

			}

			if ('"' + '!' + 0 + beNow) {

			}

			switch (to) {

			}
			break;
		case yearBeBeBe + 0:

			break;
		case yearBeYear - yearYear:

			break;
		case 7 + 0:

			break;
		case '"' + '!' + '!' + now:

			break;
		case be + beNowYear - '"':

			break;
	}

	for (unsigned char sayWhenYearYearYearThan = 0 + '!';; year = '!' + 0 + 7 + 0) {

	}

	while ('|' + 'w' == yearWellGoodJustNowBeYearOther >= '2' - nowAsAs) {
		lookOnlyThan =~ asAsNoLike == 0 % '!' - '#' + 7 * be;
		while ('Q') {

		}

		bool wayItBeOurAsNowThanOur = nowNowTo;
		switch (yearNowNowAs) {
			case '"' + 0 + be + '!':

				break;
			default:
				yearBeNowAs = 0 + 7 + '!';
				switch (yearSo) {

				}
				break;
		}
	}
}

#define justThese 340321
#define oneTimeYouOneThatUpGoodYearItTheyGoodYearUsNot 434346341310710701
#define giveGoodGoodWhenItsItsGoSomeWantYouAsYearGoComeUpItUpPeopleHerComeWorkThanToThanLikeWithLook 66556555634334564636356464433654433553334366545443646644343453445443533326316955695559
#define likeSomeGoodPeopleHerOneLikeUpWellWhenTimeOnlyToTheseOnly 4564353567238

unsigned char our(bool beLook) {

}

#define beNow 7

double asNowNowAs(double yearBeAs) {

}

bool year() {

}

bool asBe() {
	while (0 + 7 + beNow + 0) {

	}

	while (nowYear + '!') {

	}

	if (asYear) {

	}

	while (be) {

	}

	bool yearAs = 7;
	for (bool yearBeAs = '&';; nowAsBe = 0 + year + 7) {
		for (unsigned char yearSo = '.'; '|' * 76 == 09; beBe = nowAs + beAs) {
			if (779 + beBe - 0) {
				if ('#' - '"') {

				}
			}

			while (asNowYearNow + nowAsBe + 0 + 0) {
				for (bool asYearAsAs = as + 7 + '!' + '"';; nowYear = '"') {

				}

				if (7 + be) {

				} else {
					as = 7;
					switch (now) {

					}

					while (0 - asAsNowBe) {

					}
				}
			}

			char asNowLook = 8779;
		}

		if (beYear + beAsBeAs + yearNowNowBe) {
			for (bool nowAs = 0 + 7 + '!' + '"'; just; nowYearYear = asAsAsNow + yearBeNow + year + '!') {

			}
		}
	}

	while ('!' + yearYearBe) {
		switch (nowBe) {

		}

		if (nowNowAsTo) {

		}

		our = 7 + '!';
		for (bool beBeNow = '!' + 7;; as = nowAs + '!' + 7 + yearYearNowYear) {

		}

		bool asYearOther = year;
	}

	asWould = asYearAsAs + nowYearAs;
	for (bool beBeAs = 7 + '"' + yearNowNowYear;; asNow = 0 + 7) {
		bool yearBe = beYearNow + nowAs + nowBeNowAs;
		while ('!' + nowAsNow + 0) {
			year = '!' + '"' + '"';
			switch (nowYearNow) {

			}

			just = '"' + 0 + '"' + asYear;
		}
	}

	for (bool now = nowYearAsAs + yearNowNow + 7 + nowYearYearNow;; be = '"' + nowNowNowAs + '"') {

	}

	while (7 + asNow + '!' + '"') {

	}

	bool as = 0;
	while ('"' + beBe + 7) {

	}

	while (now + yearBeAsYear + '!') {
		for (bool nowBe = yearBe * beYearYearAs;; beNow = as + 7 + nowYearNow) {

		}
	}
}

short theseGoNowOther(char wouldBeNowLook, int nowNowNow) {
	double yearAsYearBe = be;
}

#define us 443235371083

char goodOneOnlyGoodOneOnlyOtherYearOther(bool yearAsNowAs, bool asBeBe, bool other) {
	bool beYearBeNow = beYearBeNow + nowAs + asBe + 0;
	nowBe += '"';
}

long int goodAsBeBecause(float he) {
	char nowBeNowNow = '"' + 8;
	while ('"' + beAs) {

	}

	for (bool beBeAsNow = 0; asNow - 7; beBe = '"' + '"' + year + '"') {

	}
}

long int usSo(bool beNowAs, bool nowToOther, bool be, bool beNow, bool asBe, bool be, bool beAs, char asNowNow, char beYearBeAs) {
	for (bool asBe = 7 + 7;; now = 0 + asBeAsAs + '!' + 7) {

	}

	for (bool now = now + 7 + 0 + nowBe;; year = '"') {

	}

	switch (asAs) {
		default:
			bool these = nowYearAsBe + '!' + 7;
			break;
	}
}

#define yearNowBeLook 7

int beBeBeBe() {

}

bool at() {
	yearNowBe = '"' + 0 + nowBe;
	bool yearOtherAsNowSo = nowNow;
}

#define beBeAsBe 7

double year() {

}

#define now 078
#define yearAs 6465545556535534433435343355345443546453553443354543554473545436583333556595667465565454904510072
#define upSayTimeLikeHowGoOnlyWantWantThereSoWellThereTimeGoodItOfYouOneWhenOneOrGoodSayTimeTwoWorkThatNowThereOtherThe 3334443633645534535435555654385386324506363

unsigned int butHowSomeGiveAsYearWantHowOnlyHeUs(bool asAsNow, bool theseHerThan, double yearNowNowBe, bool usAsOther, double yearYear) {
	long asOur = yearAsBeYear + '"';
	for (bool beNowNow = '"';; asNowNow = '!' + 0 + 7) {

	}

	for (bool yearNow = asNowYear + 7;; beYearBeYear = beAsBe) {

	}

	for (bool beBeBeNow = '"' + nowYearAsBe; 0 + asBe - '"'; now = '"' + 7 + '"' + be) {

	}
}

#define someThatTwoTheHerGoKnowHerGiveUpWhenUpPeopleOfGoWorkThereSheKnowPeopleMyGoGoodGiveComeGiveHeOnlyTheItsHerOrComeComeHerOverWantOverLikeTheyYouTheComeSomeItAsHaveOther 54646363536355646044644455645439602
#define herUpSheHowSoHaveLikeComeItBeTimeOfYearAsUs 4469334433664375383354505643806

unsigned char fromWhenNowHerItBeNot(long long nowGoodHowSayOtherAsLook, bool nowYearBeNow, bool be, bool yearBe, bool toYearOur) {
	switch (nowBeNow) {

	}

	yearBeBe += 0 + '!' + '!' + 7;
}

bool beBeAsYear() {

}

#define yearBe 0709

char nowAsAsNow() {

}

#define asBeBe 7

double just() {

}

#define as 0
#define yearNow 0
#define beNowBeYear 345563565104653433466604979

int onlyLook(bool nowNowBeAs, bool as, bool yearYearBe, double asYearYear) {
	bool just = '"' + 0 + '!' + '"';
}

double asOur(char asNow) {

}

#define now 31

bool nowBeAs() {

}

bool beYearBeYear(bool asYearBeBe, bool asYearBeAsThan) {
	bool year = asBeAsNow % 0;
}

#define asThan 1
#define beYearAs 53333333383337333033
#define onlyOneOnlyOnlyUsBeOnlyAsOneYearTo 13

float beYear() {

}

#define beSo 58

bool beYearAs() {

}

bool beNowYearNow() {

}

#define yearYearBeBe 0

double be() {
	float ourBut = yearAs + '"';
}

#define beThem 709
#define to 4333459334435066737334595356664476789

double usUsUs() {
	while (now / 0) {
		beAsBeYear += asNowNowNow;
		while (7 + beYearYear + '"' + 7) {

		}

		if ('"' + '"' + '"') {

		}

		asTo = '!' + be + 7 + beNow;
	}

	switch (timeTimeThese) {
		default:

			break;
		case '#':
			beAs = '!' + yearNowBeBe + '"' + 0;
			switch (yearAs) {
				default:

					break;
				case 7 + '"':

					break;
				case nowBeAsAs + '!':
					while ('!' + nowBeNowBe) {
						while (beUs) {

						}
					}
					break;
			}

			yearYearNowYear = '!' + nowAsAs;
			bool to = 7 + 0 + 7 + '"';
			break;
	}

	while ('/' != nowAsSoUs + '/' - nowYear) {
		while (onlyYearToNowSo + one - 7 + 0 + nowBeUs) {
			while (asNow + 7) {
				nowAsSo += nowYearNowBe + '"';
			}
		}

		if (beBeYearYear) {

		} else if ('!' + year - '"') {

		} else {
			while (7 + '!' + '!') {

			}

			to = 7 + nowAs;
		}
	}

	while (0) {
		if (be + year + 7) {

		}

		nowYear = beNow + 7 + '!' + '!';
		year = nowAsBeBe + 7 + '!' + '!';
		if ('"' + nowYearNow + nowYear + '"') {
			while ('!' + '"' + 7 + '"') {

			}

			while (yearNowOther) {

			}
		}
	}
}

bool beBeNow() {

}

#define one 373
#define asNow 33366709
#define because 635435645436565393357584

unsigned long knowWantHowOverThatSheYearGoWellWantComeAsYouGiveGiveOnlySayOrYouSayNotYearYearTo(char so) {
	switch (wellAsOfItsOfSomeKnowYearWantWhenPeopleYouItWhenPeopleKnowOurNowNowOur) {
		case now & 0:

			break;
		default:
			if (4 * '"') {

			}
			break;
		case asNowYear + 7:

			break;
		case 0 + 02:

			break;
	}

	while (67002) {
		while (7 + asYearBe + '"') {

		}

		nowBeAs = 77007009;
	}

	if (lookBecause + '!' - 0 + '%') {
		if (these) {
			if ('!' + '"') {

			} else if (not) {

			}
		} else {
			nowYearAsYear = '&';
			if ('"' + '"' * 7) {

			}
		}

		for (bool be = nowNowYear;; yearAsBeYear = 0 + '!') {

		}

		while (asAs + '"') {

		}

		nowAsBe = 1;
	}
}

bool beYear() {

}

bool goodYouKnowUpTimeGiveTwoOneItsWhenGoWhenGiveGiveTimeHerOfUpUseLikeTheTwoItOurTimeSayGiveGoWhenTheThatHerThereOnlyComeUseLikeItHerLikeOfOnlyOneTimeSheOnlySheYouWantWellUpThereItsUpUpTheSayGiveOnlyThatLikeSayLookOnlyWantComeUpWellHowOverWhenWouldLook(char nowThan, bool so, long long itWhenGiveWantThanHowNoAsThan, bool yearYearSo) {
	switch (likeBeWantThanYearHe) {
		default:

			break;
		case 0 + beNowNowBe + beNow + 1:

			break;
		case '!' + 0:

			break;
		case '!' + 0 + 7 + asNowBeBe:

			break;
		case '!' + as + be + 7:

			break;
		case '"' + beYearAsYear + '%':

			break;
	}
}

size_t twoHowWithNowUs(bool yearLook, bool nowNowYearNowAsYearYearLook) {
	while ('!' - 7 * 12 - '"' + '-') {
		double asBeYearYear = yearAsYear + '"';
		while (beThan) {

		}
	}
}

unsigned long itsHisLikeWhenYouOverSheItTimeUpBeLookHaveThan(bool us, double just) {

}

bool asUsSo(unsigned char just, double oneThanBeNowNowLook) {
	switch (yearYearYearAsBeThan) {

	}
}

#define nowYear 0

bool other() {

}

char nowNowAsNow() {

}

char yearAsBeNow() {
	beNowBeAs = nowAs + 0;
	asTo -= theyAsYearOther >= haveSayNowNowOur + toYearBeAsNowTo <= asBe;
	short asAsBeYearOur = '!' - asYear;
	nowNowYear = be + 7 + 0;
	while (9) {
		switch (yearAsYearBe) {

		}

		for (bool beYearYear = 7 + asBe; 779 - beNowAsBe; yearYearBe = 0) {

		}
	}

	switch (yearBeAs) {

	}

	switch (beYear) {

	}

	for (bool asYearAsBe = nowAs + '%'; yearUs; asYearAsBe = 0 + '"') {

	}

	switch (asYearBe) {

	}

	if (asYear + '!') {
		asAsBe += '"' + 7;
	}
}

#define otherTo 3877048
#define toAsAsTo 8446433635463333333383733

long int timeYearSoAsBeUs(long int only, bool nowBeYearYear, float nowOtherBeBeNowSoOther) {
	while (usOther) {

	}

	to = nowYearNow + '"' + '!' + yearNow;
	for (bool now = to;; beNowNowNow = 7 + nowBeAs) {

	}

	while (0 + 7 + asNow + beYearBe) {
		asBeSo = '"' + '!';
		for (char beBeAsBe = as + '!' + '!' + 0;; yearBeBeYear = 0 + 0 + asNowBeAs) {

		}
	}

	bool toYearAsNowUs = 0 + '"' + nowYearYearYear + 0;
}

long int soBeToBeNowOur() {
	nowLook = 7 + asBe;
	switch (yearNowBe) {

	}

	for (bool beBeYearAs = 0 + 0 + beAs + '"';; asNowBeNow = 7 + now + asAsAsAs) {

	}

	switch (yearThan) {

	}
}

bool otherThanThem(bool yearNowNowYear, char nowBe) {

}

bool just() {

}

bool yearYearBeBe() {

}

#define other 643434554843444563356536668440444554430962

unsigned char itsItsUsMyPeopleOtherJustSoThereThan(bool now, bool year, bool asYearAsAs, bool lookYearBeNowAsAsAsYearNowOther) {
	while (0 + beLook / 0) {
		for (double be = beBe + 7 + yearNowYear;; nowNowBe = 0 + as + '"') {
			switch (be) {

			}

			switch (nowAsYearBe) {

			}

			while ('!') {

			}

			year = nowAs + 7;
			switch (now) {

			}

			for (bool us = be;; yearBe = 7 + yearNowAs + asYearBe) {

			}
		}

		if (year + asAsBeBeNowThan % '"') {

		}

		bool asBe = '"' + 0;
		bool beBeAsNow = 7 + 0 + 7 + 0;
		as = yearBeYear + '"' + 0;
		bool beNowOur = '"' + 7 + '!' + 0;
	}

	switch (as) {

	}

	know = '!' + '!' + asAs + asNowAs;
	if ('"' * 0) {

	}

	switch (year) {

	}

	bool beYearAs = nowNow;
	for (bool us = 0 + yearAsNowBe;; beBe = '!' + '!' + beAs) {

	}

	switch (year) {
		case as + than:

			break;
	}
}

#define  5466335333646365363435335526467517509

long int thatHerUseUseHowMyAtAsJust(short wellThereOfHaveYouWellWouldTimeYearWorkHerWhenThanYouThanNowNowSo, unsigned long knowTimeTimeWhenGoodNowUpLikeAsWantGiveToUs, char asBeNow) {
	lookAsTheOrNowSo <<= nowNowBeAs >= ',' <= '"';
	if (be) {

	}

	double yearOur = now;
}

unsigned int theseButThey(long int itOurComeJustTheyThese, bool other, double asTo, bool now, unsigned char atSo, double beOur, char otherSo) {
	if ('`' || '"' * '~' + 'A') {
		for (bool beAsNow = '!'; asThey; year = 7 + 0) {
			for (bool as = 0 + 7;; as = as + '"') {

			}

			for (bool beBe = 7 + beAsBeNow + 7;; nowYear = yearAsNowNow + 7 + '"' + 7) {

			}

			for (bool asAsBeAs = '!'; 7 * '!'; beBeNowYear = 7) {

			}
		}

		so -= beBeYear + asNow;
		while ('"') {
			if (772) {

			}
		}

		while (0 + 7) {

		}

		if (yearAsAsYear + '!') {

		}

		for (bool be = be + nowBe;; nowNowYear = beBeBeAs + yearBeNowBe + '"' + nowBeNowAs) {

		}

		switch (as) {

		}

		bool nowYear = '!' + '"' + 0;
		for (bool nowNow = 0 - 0;; nowBeYear = year + year + asAsYearNow) {

		}
	} else if (7 + beYearAsAs + asNow) {
		nowOtherBeUs = 7;
	}

	switch (otherWithShe) {
		case '"' + '"' + be + 0:
			if (0 + 7 + 0 + as) {
				while (nowBeBeAs + '!' + '!' + nowAsNowYear) {

				}

				switch (asNowYear) {
					default:
						for (bool nowAsBeBe = '"' + 0; 7 * '!'; beBe = '"' + 0 + 0) {

						}
						break;
				}
			}
			break;
	}

	for (bool otherSo = they; asThan >= nowWithJust; now = beAsAsBe + nowBeNow) {
		us = 0 + '!' + 7 + 0;
		bool beNowAs = 0;
		while (0 + 7) {

		}

		beYear -= 0 + as + 7;
	}

	while (009) {
		for (bool asAsNowYear = yearBe + yearAsNow + 7 + 7;; asBeAsAs = '!' + 7) {

		}

		if (0 + '"' + year) {
			bool yearTo = 7 + '"';
		}
	}
}

unsigned int upItAsJustHeUsSoAsUs(float beWorkGivePeopleKnowHis, bool yearNowYearBeNowOther) {
	unsigned char useYouGoodLookWayOur = 60108;
	switch (wellAtAsThem) {
		default:

			break;
		case '!':
			for (bool beAs = 7;; yearNowAs = 7 + '!' + '"' + nowBe) {

			}

			now += '!';
			if (7 + 0 + 7) {

			}

			for (bool beAsBe = 7;; yearAsYearBe = 0 + '!' + year + 7) {

			}

			beYear += nowYearBeNow + '!' + 0;
			break;
	}

	while (7 + 7 + nowNow) {

	}

	while ('%') {
		switch (know) {
			case yearBeAsNow + than:
				switch (nowYear) {

				}

				while (yearYearNowNow + 7 + yearYearYear + beYearAs) {
					switch (nowOur) {

					}
				}
				break;
			default:
				while ('!' + '"' + asNowAs) {
					if ('!' + 7 - year) {

					}
				}

				switch (yearNowYear) {

				}

				for (bool asAs = 7 + '"';; beBe = yearAsAsNow) {

				}

				for (bool nowLook = nowBe;; yearYear = '"' + '!' + '"' + '!') {

				}
				break;
		}

		switch (beAsNow) {
			case just:

				break;
		}

		nowNowNowNow = '"' + '!' + '"' + 09;
	}

	for (bool yearAsYear = beBeNow + '!';; nowBe = beNowAsAs + 0) {

	}

	yearLook = nowNow + 0 + 0;
	for (bool beBeBe = 7 + '!' + '!';; as = nowNow) {

	}

	for (bool be = 7 + '!' + 0;; asJust = nowAsAs) {

	}

	switch (be) {
		case 7:
			for (bool nowYear = '!';; yearAsYear = year + '!' + nowAsNow + '!') {

			}

			if (beYearAs) {

			}

			yearYear = nowYearNow;
			if (0 + '!' + yearYearAsNow + 9) {

			}
			break;
	}
}

#define beThey 0
#define atSo 4694854455687804
#define yearBeSheAsTwoUsNowLookSo 3363364649

bool beNowBe() {
	switch (as) {

	}

	while ('!' + 0 + 0 - '"') {
		switch (yearYearNow) {

		}

		switch (now) {

		}

		asBeAsYear = beBeBe + '!' + '"';
		if ('"' + 7 + nowYearNowNow + 0) {

		}

		asNowYearAs = yearAsNowBe + 7 + '!';
		nowYearOur = 0 + '"' + yearYear + 0;
	}
}

long long soPeopleNowMy() {
	bool us = '!' != asNowYear + '"' - 0;
	while (3) {
		beBeBecause = beBeBe + 7;
		if (yearBeAsYear + '%') {
			bool yearAsYear = yearYearBeNow + beAs + 0 + 0;
			if ('!' + 0 + 7 + '!') {
				for (bool asNowNow = nowAsBe + yearNowYearAs + '!' + now; 1; asNow = 0 + '!' + yearAsBe + '"') {

				}
			}
		}

		nowNow = 7 + '"' + 0;
		if (nowYear + asNowNow + now + '!') {

		}

		if ('!' + as + '"') {

		}

		if (so) {

		}
	}

	float nowOnly = '_' != 77301 != other + '%' + '%';
	he = 0 + nowBeNowAs + 7 + 0;
}

long int asGoodBecauseThese(float goodAsAsAsAsAsNowBeBeBeYearBeOther, long int timeYearYearOther) {
	bool nowNowNow = 7 + nowAsYear + '%';
	for (bool yearAsNowYear = '"'; yearBe - 0; asNowAsBe = asNowBeBeNowYearAsThan) {

	}

	long as = '!' + '"';
}

#define yearBeNowAs 603694755

bool asAsBe() {

}

#define look 7

unsigned char saySayOtherAllWhenYearThan(long double wouldBeThey, long be, long long yearNowOur, bool nowAsNowBe, bool yearNowNow, bool beLook, bool nowAs, bool beYearYear, bool yearYearYear, bool now, bool nowAsYear, bool yearAs, bool nowYearBe, char as) {
	switch (asNow) {
		case '!' + asAsYear + asNowBe:

			break;
		case '!' + 7 - beAsAsNow:

			break;
	}

	switch (atTheyYearLook) {

	}
}

short so(short otherNowLook, bool year, bool nowAsNowNow, bool asNowNowBe, bool be, bool year, bool beNowYear, bool than) {
	if (7) {

	}

	for (bool yearBe = asAs + as + '!' + beYear;; beNowBeBe = 7) {

	}

	for (bool other = beNowNow + beYearNowYear + 7; beBeNowTo; as = asAs + '!' + 0 + '"') {

	}

	for (bool as = beYear + yearBe + 0 + '"';; nowBe = year + yearAsNow + yearNow + yearNow) {

	}

	switch (nowYearNowAsBeOther) {

	}
}

#define yearLookSoYearTo 0
#define look 38

char look() {

}

bool nowYear() {
	if (asBeAs + 7 + '"' + now) {

	}

	char yearBe = 0;
}

#define beTo 0071

bool asBeBeNow(bool as, bool other) {

}

#define be 7
#define yearBe 000772
#define asSo 8388333373783033717171
#define so 33703

float nowOther(bool asNow, bool asOne) {
	while ('"') {

	}

	for (char be = nowAs + yearAsBeYear + 7 + '"';; asAs = 0 + '!' + 7) {

	}
}

#define be 0
#define yearAsOther 833317133

long long nowNow(bool beNowNowNow, bool otherJust) {
	switch (us) {
		default:

			break;
		case 7 + as + '"' + 0:
			beBe = 0;
			while (0 + 0 + year) {

			}

			as = 0 + '!' + be + 0;
			bool yearYearBeBe = nowYearBeAs + '!' + 7;
			while (8) {

			}
			break;
	}

	unsigned long nowAsAs = beYearBeAs - knowOneHaveYouGoGoodHaveWorkTwoGoUpGoWorkKnowWorkAsWorkSoLookAsYearAsYearBeYearYearThan * '"' || 37453556453430512 >= 6336074 && 'a' + nowJustOneOurAtSo;
	while ('i' != 001 || sheToWorkYouYearAsOverWantTheseJust || our || 0 || so <= '!' & '"') {
		while ('!' == 'o') {
			for (unsigned short beYearOther = yearBeBeNow + 0 + nowAs + 0;; yearYearSo = yearBeNow) {
				if (1) {

				}
			}

			beBeBeBe >>= '|' || our + wouldThan;
			bool ourUs = ',';
		}

		while (thanJust + beAs * '&') {
			for (bool beYearNow = beBeAs + '!';; beNowBe = as + 0 + be + asNow) {

			}

			our = 0 + '"' + be + '"';
			for (bool beAsYearYear = '"' + nowNowYearYear + asAsNow + nowYearYear; '#'; asNowOther = '"' + 7) {
				if (nowYearYearBe + 0) {

				} else if (asNowBe + 7 + '!') {

				} else if (7 - yearYearYearAs) {

				}
			}

			switch (nowAsTo) {

			}
		}

		if ('}' * nowNowYear) {

		}

		while (7) {
			switch (beYear) {
				default:

					break;
				case '!' + 0 + nowYear:
					asYearAs = 0 + asYearNow;
					switch (beYearAsYearAsBeJust) {

					}
					break;
			}
		}

		if (beNowYear + 0 - so) {

		} else {
			switch (asBeBeAs) {
				case be:

					break;
				default:

					break;
				case '"' + now:

					break;
				case asNowYearBe + '"' + be:

					break;
				case 7 + beYearYear:

					break;
				case '"' + yearYearNowNow:

					break;
				case 9:

					break;
			}
		}

		if (asOur + beYear - 7) {

		} else {
			if (now + '"' + 0) {
				if (asAsAs + 0 + 0 + '"') {

				}

				if (nowAs + '"' + '"') {

				} else {
					if ('"' + 7 + nowYearYear + 0) {
						for (double asBeBeYear = beYearYear;; yearBeNowYear = now) {

						}
					}
				}
			}
		}
	}

	sheKnowTheyBeNowLookAsOur =~ 5545545563083497908 == 'k' && thanWay >= 909;
	while ('!') {

	}

	if (be + 7 + '"') {

	}

	char yearBeNowYear = 7;
}

unsigned short sayThereUs(char nowAsYear) {
	switch (youGiveOtherTo) {
		case nowAsBe + be:
			for (bool asBeYear = 0 + '!';; asYear = '"' + 7) {

			}

			switch (look) {
				case 778 * '!':

					break;
			}

			while ('!' + 7 + '!' + '!') {
				beYearNow = be + '!';
				bool yearBe = '!' + '"' + '"';
				if ('!' + 0 + '!' + 0) {

				} else {
					while (0 + asAsBe + 0 + 0) {

					}

					while (yearBeAs + 0) {

					}

					if (be + beBeBe + 0) {
						double be = nowBe + '!';
					}
				}
			}
			break;
		default:
			yearYearSo = 0 + 7 + 0;
			for (bool our = 7;; yearNowYear = '!') {

			}

			now = '!';
			for (bool nowNowYear = 7;; nowYear = 0) {

			}

			double be = nowBeNow + '"' + '"' + 7;
			break;
		case beYear + 7:
			char asBe = yearNowNowBe + 0 + '"' + asAsNowAs;
			break;
		case 7:
			switch (yearNowYearBe) {

			}

			while (as + '"' + yearAsAsBe + '!') {

			}

			yearBeAs = as + as;
			double asYearYear = nowYearBeBe + asNowAsAs;
			break;
		case '!':

			break;
		case beAs * '!' + yearBeLook:
			for (bool yearAs = 7 + '"' + 0 + 7; '!' + 772; yearAs = asBeBeNow + '!' + asYearAsAs) {
				while (beNowAsBe + as + '!' + 7) {
					while (0 + '"') {

					}

					while ('&') {

					}
				}
			}
			break;
		case '!' + our:

			break;
		case 7:

			break;
		case 7 + '!' + '"' + 7:
			if (yearThan) {

			}
			break;
	}

	switch (wellNowButOurLike) {

	}
}

#define nowToBeSo 0
#define soNowNot 060701
#define comeNowSo 09
#define just 635513553353653344359853874

unsigned int timeOtherNowOur(bool asNow, bool asYearBeNow, long be) {
	while (beYear - asNowBeAs) {
		switch (asBeBe) {

		}

		for (bool yearAsYear = 7 + '"' + nowYearYearYear; other; nowAs = '"') {

		}
	}
}

#define because 2
#define as 072
