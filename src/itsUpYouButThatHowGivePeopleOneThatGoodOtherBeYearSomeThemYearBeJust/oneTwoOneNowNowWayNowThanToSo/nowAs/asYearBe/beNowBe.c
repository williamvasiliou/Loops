#define oneNowBecauseNowSo 3337333333303333
#define goodBeYearSo 30333333333
#define other 8773
#define year 46564663043666045664382
#define otherLook 8

unsigned short sheWhenPeopleSheThanLookIt(bool nowAs, double beTo) {
	while (1) {

	}

	for (char yearAs = 7 + beNowAsBe + '!' + asYearNowAs;; be = 7 + yearNow + '!') {
		long int yearBeAs = us;
		for (bool year = 7 + yearYearBeNow + '"' + 7;; yearAs = asYearNowYear + now + '"' + nowBe) {

		}

		one = '\'';
		if (818) {

		}
	}

	if (0 + beBeAsBe + '#') {

	}

	if (beBe) {

	}

	switch (asAsYear) {
		default:

			break;
		case to:
			now = '"' + 7 - as;
			break;
		case 0:

			break;
		case '!':
			while ('!' + 7 + asBe + '"') {

			}

			bool nowYear = 0 + 7;
			switch (asYearNow) {

			}

			if (beOther) {

			}
			break;
	}
}

#define goodTimeOneGoodOneHeGoodOnlyGoodOnlyOnlyTheseGoodThatNowThan 609
#define now 1

bool to() {

}

#define beYearAsYear 2
#define be 7

bool as() {

}

char be() {
	switch () {
		case 28 + yearNowOur:
			switch (nowWithNowToWantThemUsThan) {

			}

			bool so = '!' + 0 + '"';
			switch (beYear) {
				case '!' + '"':
					for (bool yearYearNow = 7 + '!' + beNowNowAs;; beAsBeNow = 0 + yearYearBeYear) {

					}

					if (7 + asNowAs + '!') {

					}

					bool beAsAs = 7 + '!' + asBeNow;
					switch (asYearNowNow) {

					}

					beNowYearBe = asBe + 7 + '!';
					bool asNowLook = beYearAs;
					break;
				case '"' - yearAs:

					break;
				case be:

					break;
				default:
					bool asAs = now;
					nowAsYearNow -= '"' + '!' + 0 + nowNowNow;
					break;
				case 0 + 7:
					bool nowYear = nowBe + '#';
					break;
			}

			if (7) {
				for (bool other = '!' + '!' + '!' + 0;; nowBeYear = '"') {

				}

				yearAsAsNow = '!' + '!' + asBeNow + '"';
				if (0 + '$') {

				}

				for (bool nowYearYearAs = 70000002;; year = 0 + beNowYear + as) {

				}
			}

			just = 7 + 7 + nowAs;
			break;
		case yearNow * '*':
			while (yearAsAs + '"' + '"' + 0) {

			}

			if ('!') {

			}

			if (nowAsYearOther) {

			}
			break;
		default:
			for (bool beYearYearAs = '!' + 7 + '"';; now = 0 + 0 + nowBe + 7) {

			}

			if (asSo) {

			}

			while (72 + beBe * now) {

			}
			break;
		case with + '(' - 7 * '!':
			for (long double likeOur = 0 + '!' + nowNow + 0; beYearYearNow + be - 0; as = '"') {
				look += 7 + 0 + 7;
			}

			while (beNowAsBe + 0 + '!') {

			}

			look += 729 * '"';
			long int asAsYear = 978;
			for (double look = asBeYear + '$' * 7; '"' % yearNow; beAs = 0 + 0 + 0 + '!') {
				while ('"') {

				}

				bool be = 0 + 0 + yearYear;
				beBeBeBeNowSo = 0 + '!' + '"';
			}

			bool beAsAsNow = 0 + 0 + '!' + nowYearNowNow;
			switch (as) {
				case 0 + 0:

					break;
				default:
					while ('!' + 7) {

					}

					switch (as) {
						case '!' + '%':

							break;
					}
					break;
			}

			switch (nowBeBeTo) {

			}
			break;
	}

	switch (goodGiveItWellGoHowComeOneOfOnlyUseWhenTwoTimeComeGoodOrKnowGoOnlyOrHerBecauseNowThan) {
		default:
			while ('d' || 'y' || 'd' + 613454555541032 < theseOtherHe != 'd' % 7) {
				switch (asYearYear) {

				}

				for (bool asNowYear = '!' * '!';; year = '!') {

				}

				if (yearAsOur - 7) {
					switch (so) {

					}
				}

				while (7) {

				}

				while (now + '!' + 0 + 0) {
					nowYearYear = '"' + nowNow + '"' + 0;
					if (be) {

					}

					switch (beYearNow) {

					}

					if (7 + 7 + nowBe + asAs) {

					}

					beNowBe = '"' + '"' + 7 + '!';
					char asNowAs = yearAsYear + yearAs;
				}
			}

			if ('"' + 7 + yearAs + '"') {

			}

			oneWorkSaySoWell |= justNowAsOther & 4;
			switch (beNow) {

			}

			to = nowYearTo;
			break;
		case '!' + '"':

			break;
		case 0 - '!':

			break;
		case beNowAs + year + 0 + be:
			beBe = '"' + 0;
			nowBeYear = nowAs;
			if (7 + now + yearAsYear + yearAs) {
				bool asAsYearNow = yearAsBeNow + beYear + nowYearBeNow + beBeYear;
				if ('"' * as) {

				}
			}

			for (bool yearBeBeLook = nowBeYear + '!' + nowYearBeAs;; beNowYearAs = yearBeAs + 7) {

			}
			break;
		case 0 + 0 + beYear + '"':

			break;
		case 0 + 7 + '!':
			while (7) {

			}

			bool asAs = '!';
			beNowBe += '!';
			for (bool asYearNowBe = 7 + as + asBeNow + 0;; asAsYearBe = '"' + now + now + '"') {

			}

			for (bool beYearBeNow = '"' + 0 + now;; beYearAsYear = 7 + 0 + 0 + '"') {

			}

			while (7 + 7 + 0 + 0) {
				bool beBeYear = '!';
				switch (year) {

				}

				bool yearNowAsYear = beNowNowYear + 7 + 7;
				switch (yearYear) {
					case beAsYear + 7 + 0 + beBe:

						break;
					default:
						if (just) {

						}
						break;
				}
			}
			break;
	}

	switch (knowItsJustHaveKnowBeOnlyUsBeAsAllHis) {
		default:
			notAsThan = 0 + yearYearNowYear + 0;
			onlyHowSayUseTimeSoOneAsOnlyItSoYearTo &&= 004702 > with || nowAsAs;
			if (71) {

			} else {
				bool nowAsAsNow = asBe + 0 + now;
				if (7 + asBeNow + be) {
					char beAs = '!' + '"' + '!' + nowNowBe;
				} else if (78) {

				}
			}

			for (double asAs = beYear; '!' * '&'; ourThanYearUs += '!') {

			}

			nowYearYearOur = '!';
			break;
		case 667041:

			break;
		case us || 52:
			nowBe = '"' - '"';
			for (bool asNowYear = asNow + 0;; beUs = '!' + '"') {

			}

			if (beBe * asNow) {

			}
			break;
		case '"' * 0:
			for (long double year = 0; 701; now += 7 + nowAs + nowNowBe) {
				bool yearBe = 0;
				if ('$') {

				}
			}

			switch (beYearYear) {

			}

			switch (us) {
				case 7 + '"' + '"' + 7:
					switch (just) {
						default:

							break;
						case 7 + '!' + '"' + '"':
							switch (asYear) {

							}

							if ('"' + asYearNowNow + 7 + 7) {

							}

							bool yearBeYearYear = 7 + 0 + '!' + '!';
							bool beYearThan = year + 7 + '!';
							break;
					}
					break;
				case year + '!' + 7:
					for (bool asYear = '"' + 0 + yearAs + as;; nowYearNow = '!') {

					}

					while ('-') {

					}
					break;
			}

			double beAsBe = 1;
			for (bool asYear = '!'; '"' + 0 + beOther; yearBe = 7) {

			}

			bool nowSo = '!';
			break;
		case justAsOur || 705089:
			switch (beNowYear) {
				default:
					switch (asBeAs) {

					}

					if (7) {

					}

					if (nowAsYearYear) {
						for (bool beBe = 7 + as + beAsAsYear;; yearJust = 0 + 0 + '"' + '"') {

						}
					} else {
						switch (beNow) {
							case 0 + asBe:
								switch (beNowBe) {

								}

								while (nowBeYear + beYearYearYear + yearAsAs) {

								}

								if (0 + yearYearBe) {

								}

								while (yearAs) {

								}

								switch (yearYear) {

								}

								while (nowYearBeYear + beBeBeBe) {

								}

								if ('"' + '!') {

								}

								while (7 + 0 + year) {

								}

								if (0 + asYearAsNow) {

								}

								while (nowAsNow + beYearNowNow + 0) {
									bool nowAsBe = 0;
									switch (beNowAsYearThan) {

									}
								}
								break;
						}
					}
					break;
				case '!' + asAsNow + nowBe + '"':
					for (bool asBeBeYear = '"'; '$'; nowYearBeAs = year + nowAs) {
						for (bool year = '!' + asAs + 7 + '"';; yearLook = year + 0 + 0) {

						}
					}
					break;
				case 0:

					break;
				case as + yearBeNowBe + 7:
					for (bool year = nowNowBe;; now = asBeNow) {

					}

					switch (asAsYearNow) {
						case '!' + 7079:

							break;
					}
					break;
			}

			now += 7;
			int nowAsYearBe = 0 + 7;
			nowYear = yearBe + nowAs;
			be -= 0 + beYearYearBe;
			break;
		case wellLikeLikeUsBeYearNowBeYearBeYearOur:
			if (as + as) {

			}

			for (bool beBe = 0 + yearYearBe + 0;; beAsAsBe = '"' + 7) {

			}

			switch (nowYear) {

			}

			if (0) {

			}

			while ('&') {

			}
			break;
		case beNowNow:
			switch (nowAsYearYear) {

			}

			while (7 + 7 + 0 + 0) {
				while (yearJust) {

				}
			}
			break;
	}

	howOneSheWorkUseSomeLookWithAtTheyNot >>= orHowUseThereSomeWellPeopleGoWorkWorkKnowSomeKnowUpWorkBecauseAsThan != 'Z';
	as = now;
	bool as = asYear + '"' + 7;
	if (0 + 2) {

	}
}

short useSheWantWorkHowThatThereHerOneComeYouThatKnowTheUseTheOneTimeUpGiveYouPeopleOverThatHerHerHaveOfYouThatNowOfToOnlyKnowHerOverComeTimeOnlyHowThemYearBeNowAsOur(long int ofThey, short lookTheyYearBecauseOtherTimeUs, bool beNowNow, bool beNowBeNow, bool beYearYear, bool now, bool asAsNowNowOur) {
	if (beUs) {

	}

	while (7 + '!' + '"' + nowBeYear) {

	}

	nowAs = asYearBeBe;
	char nowBeAsBe = 0;
}

#define twoGiveYearThanAsOfNoAsNowYearYearOther 3342019
#define nowAtOtherOur 53634564478453448193468

unsigned int yearBeWay(unsigned int ourAsSo, bool yearYearYearNow, bool now, double beAsYearYear) {
	asSayFromThemNowNowUs &= 8 | other;
	for (short yearAsBeAs = 292; look; look += '!' - '"') {

	}

	switch (year) {
		default:
			if (year + nowNowYear + nowBeAsNow) {

			} else {
				bool year = 7;
				switch (yearAsYearAs) {

				}

				if (0 + '"') {

				}

				bool year = nowNow + '"' + '"';
				while (7 + year) {

				}

				bool asBeAs = as;
				bool asYear = 0 + 0 + '"';
				while (0 + nowAs + yearBeNow) {

				}

				while (now + 0) {
					while (asAsBe + '!' + 7) {

					}

					while (779) {

					}
				}
			}

			if (7 + '"' + 7) {

			}

			if (nowBe + 7 + 7) {

			} else {
				switch (now) {
					case 7 + 7 + nowYearYear + '!':
						while ('#') {

						}
						break;
				}
			}
			break;
		case 9:

			break;
		case '!' + year + 0 + '!':

			break;
		case '7':

			break;
		case 7 + beAs:
			year = '!' + 0 + nowNowNow + 0;
			while (yearAs) {

			}

			for (long double yearYearBeNow = '!' + '"' + asAsBe + 7;; beBeYear = 0 + '"') {

			}
			break;
		case yearUs:

			break;
		case 0:
			year = 1;
			break;
	}

	switch (yearNowNowYear) {
		default:
			switch (nowNowYear) {

			}

			switch (beAsBe) {
				case 7:
					while (7 + nowBe) {

					}

					while ('!' + 7 + '#') {

					}
					break;
			}
			break;
	}
}

#define asYear 975

int be(bool us) {
	for (size_t asBeNow = '&';; beYearOther = yearBeBe + '!') {

	}

	if ('!') {

	}

	asBeBe = 0 + 7 + beTo;
}

#define goSheHowComeHerTimeSomeUpThereAsNowKnowTimeNot 463345643605
#define year 3330833333370733003

bool beAs(long int yearHis) {
	if (year + '!' + be + 0) {

	} else if (01) {

	}
}

#define nowBeNowBe 330333873330003

long int asBeTimeThan() {

}

#define beYearYearNow 0

long asTo(bool look, bool yearNowNowBe, float now) {
	while ('!' + '!' * asNowAsYear) {

	}
}

bool be() {

}

bool yearYearNowNow(char now) {
	switch (beSo) {
		default:

			break;
		case beBeBe + nowBe + '"' + '"':

			break;
		case '=':
			nowBeBeLook += 0 + 0 + 0;
			while ('$' + be + beAsAs * 0) {

			}
			break;
		case 7:
			char beYearYear = '!' + beBeYearYear + '"';
			asJust = 0;
			break;
	}

	if ('+') {
		unsigned long yearSome = asYear;
	} else if (72071) {

	}

	timeKnowHeSomeKnowTimeThatNowThem <<= be;
	bool yearUsAsOther = 653705409 || 'w' > myThan && 'g' * 0 && '(' && asBeAsAs < asAsNowBe;
	beAsYear = 7 * now;
	char asAs = 0 + asBe + nowYearNowNow;
}

#define onlyPeopleTwoComeGiveYouTheWantWorkOneUseTheThereHowTheSheYouBeItsSomePeopleSoLookAtKnowWayOtherAsAsNowAsThan 564565535554346545644654445543546436633675536963772
#define thatWhenSayMyToSo 4846418
#define nowYearYear 664326436634335543335651607272
#define onlyBeSheWhenToAllYouGoodOtherTo 454564454559303
#define otherYouNowOurLook 0
#define just 858305

long nowSoBecauseNowNowTo() {

}

#define asBeBeBut 636636454665446336263380466551

float beYearNowNow() {
	while (7 * beBe) {

	}
}

long double nowBeYear(bool beFrom) {
	yearYearBe = beYearYear + now + 7;
	nowBeNow = now;
	for (bool asNow = '!' + '"';; nowAsThan = 0 + '!' + 0 + yearYearBe) {

	}
}

bool asYearBeYear() {

}

bool beYearNowNow() {

}

bool as() {

}

#define nowBe 7
#define nowBeYearBe 7
#define beAsBe 3333333333333311171

char beOneOtherTimeHeHis(float yearNowYearNow, bool yearBeUs) {
	if (beBe + 7 + '"') {

	} else if ('#') {

	}

	if (toUsHis + 7 / '"') {

	}
}

short asYearNow(bool year, bool but) {

}

#define his 0

char beNowYearYear() {
	asNowNowNow = 0 + as + year + as;
	double yearBeNow = '!' + nowAsNow - '"';
	bool look = '|' > 66455554640403656045346345 & so <= herWellHaveJust > look;
	switch (goodHerThereHaveItOverWellPeopleKnowItsAllAsOtherBeNowThem) {
		default:
			for (unsigned long nowAsBeNow = other;; beYear = be + 0) {

			}

			if ('&' + asLook) {

			} else {
				switch (asNowAsAs) {

				}

				switch (beBeOur) {

				}

				while ('$') {
					bool asAsNowAs = 7 + 0 + '!' + 7;
					switch (other) {

					}
				}

				be = asBeBe + '!' + be + '"';
				if ('"') {

				}

				if (0 * 0) {

				}
			}

			for (double yearYearAsNow = 0 + 7 + '!' + asNowBeBe;; yearBeAs = be + asYearBeYear + '!' + 7) {

			}

			switch (time) {

			}

			while (7 + '!') {
				for (char yearAsBe = 0;; now = '"' + '"') {

				}
			}
			break;
	}

	switch (yearNowBeNow) {
		default:
			for (bool yearNowAsAs = '!';; asNowBeBe = 7 + 7 + '!') {

			}

			for (bool yearNowAs = '!' + 0 + '"' + 7;; beBeBe = '!' + asYear + asAs + '"') {

			}

			nowNowNowYear = nowBe + 7 + yearAs;
			be = asBeNowNow + '"' + as;
			for (bool yearNowBeNow = asTo;; as = '"' + beYear + 0 + 7) {

			}
			break;
	}

	while (asYearBe) {

	}

	switch (nowNowAsOur) {

	}
}

short (size_t giveSomeFromTheWhenThereOnlyGiveWellWantNowNowWorkOverWellThePeopleTimeComeBeNowBeYearThey, bool otherAsThanSo, long double so, bool asYearAsYear, bool yearAsBeBe, double us) {
	if ('x' < 7) {
		goNowWhenFromHave ^= ourBeSo & 7;
		switch (nowAsBeBe) {
			default:
				if ('!' + yearBeAs) {

				}

				if (078 * '"') {

				}
				break;
		}

		bool nowUs = now && '&';
		bool beUsNowNowAsYearBeYearBeAsBeOther = '!' + yearAsYear + '"';
		bool be = 0;
		double now = '!' + 7;
		if (7 + 7) {

		}

		beAsBe -= '#';
		switch (year) {

		}

		beNow = asAs;
		just = '"' + yearBeNowYear;
	} else if (year + asNow) {

	} else {
		yearLook = 0 + asNowNow + '"' + 0;
	}

	while (saySayAsUs - '"') {
		while ('w') {

		}
	}

	if (775 - 0) {
		if (0702 + 7 * 0 - 7) {
			if ('"') {

			}

			if ('"') {

			} else {
				while (yearAsAsYear + '"' + '"' + asNowNowNow) {

				}

				bool year = 7;
				beYearAs = nowNow + year;
				while (asAsBeBe + beBeAsAs) {

				}

				asNowAsBe = beYearBeYear + 7 + year + beBeBeNow;
				switch (yearYearBe) {

				}

				bool asBeYearNow = nowAsNow + '"';
				bool nowBeUs = as + beAsBe + 0;
			}

			switch (asBe) {

			}

			for (bool nowNowAsNow = nowNowYear + yearYearYearAs;; nowNowAs = '"') {

			}

			be += '!' + asYearBe + 9;
		} else {
			nowAsAsAs = 7 * 7;
		}

		beYearLook -= 02 - 771;
		switch (nowNot) {

		}
	} else {
		for (double asYearNow = beYear; our + our; beBe = 0) {

		}

		beBeNow += 78;
		while (beYear + 79 * 0) {

		}
	}

	for (bool as = 2202; now - nowAsBeYear; nowYear = nowNow + asYearAsNow) {

	}

	switch (yearBe) {

	}

	while ('!' * 7) {
		bool beNowBe = beYearOther;
	}

	switch (nowLook) {
		default:

			break;
		case 0 + nowYear - 7:

			break;
	}
}

short haveGoNowSomeOnlyItHerHaveTheThanYouPeopleTimeWantGoOverTheAll(char beAsYearAsAsBeYearThan, bool us, char asBe) {
	than = '!' + '"';
	justTo = 7 * 0;
	while (009 + '"' + '#') {
		goodThan = beYear + 7 + '!';
		for (bool yearNowYearBe = 0 + yearYearNowBe; 7 + year * 7; beAs = yearBe) {

		}
	}

	beBeNowYear <<= be + 0 + 772;
	char nowBeOther = beNowNow + '"';
	yearBeBe = '!';
	while ('"' + 0 + 0) {
		bool beAsBeNow = '!' + asNowYearNow + yearBe + 0;
		switch (beYearUs) {

		}
	}

	while ('&' % 0) {
		while (7 + 7 + 7) {
			bool nowAsNowYear = 7 + '!' + asBeAs;
			bool as = '!' + yearNowBe + '!';
			if ('"' + asBeBe + be) {

			}

			bool yearNowAsBe = yearAs + beNowAs + 7 + asAsNow;
			while ('!' + '"' + 0) {

			}

			while (yearAsYearBe + 7 + beNow) {

			}

			switch (as) {
				case 7:
					bool nowOur = '!' + '!' + beNowYearAs;
					break;
			}
		}
	}
}

#define nowBeAsBe 7

unsigned short nowHe(bool yearBeUs) {
	for (float yearBeBeAsSo = '$'; beYearNowUs; year %= nowYear) {

	}

	asOther = '!' + beYearYearNow;
	for (bool beBeBe = now + beYear + '!'; 0 + 0 + yearBeBeNow + 9; nowNow = yearBe + '"' + as) {

	}

	switch (beBeBe) {

	}

	yearNowNowYear = '!' + as - beNow;
}

#define comeBeSoThey 7776
#define beLook 665071

bool sayWellOtherBeJustHis(long int notYearTo) {
	for (char so = nowNow;; be = '!') {

	}
}

#define itsAllUsThanNowThan 64436744069730079

short timeUseThatGiveAtBeSoBeNot(unsigned long yearWouldToWork, size_t justAsThan, float yearYear) {
	if (beNow + asAsBeAsThan) {

	}

	nowWay = 7 < '!';
	for (bool yearBe = beBeNowBe + 7 + '!' + 0;; nowNowAs = '"' + yearYear) {

	}

	for (bool yearNowAsBe = nowBeBeTo;; now = 0 + '"' + asYear) {

	}

	switch (nowBeOur) {

	}
}

bool now() {

}

#define now 0

bool yearTo() {
	while (313070701) {
		yearYearHeUs = 'O';
		if (yearAsAs + '!' + '!' + '!') {

		}

		switch (year) {

		}

		bool other = '!' + beNowNowAs + 7;
	}

	bool beNow = year + 0 + nowNowYear;
	long int beYearAs = 0;
}

#define timeBecauseGoodOneOneOnlyTimeToTo 303333333371

char beAsNow() {

}

#define so 0800008

bool asJustYearNowLook() {
	while ('"') {
		if (nowBeYearBe + asYearNow + '!') {

		} else {
			if (0 + nowYearBe + '"' + '"') {

			} else {
				switch (asBe) {

				}

				for (bool beNow = '"' + '!';; be = 7 + yearNow) {

				}

				while (0 + be) {
					while (now + '"') {

					}

					for (bool beNowNow = asYear;; nowNow = 0 + 7 + '!') {

					}

					for (bool yearAsBeYear = 2;; be = 0 + '"') {

					}
				}
			}
		}

		bool asYearAsAs = '"' + 7;
		bool nowBe = than;
	}
}

#define asAs 4936563563356433544698693708

unsigned long nowNowAsBe() {

}

size_t beNowUseItsOur(short as, double nowNow) {
	double yearBeYear = '_' + oneOneOneYearTimeYearOneOneHeBeOnlyHeThese != timeGoodTimeGoodGoodAsAsAsSo - yearNowYear - now;
	for (bool beAsYearYear = '!';; year = '!') {

	}

	while ('%') {
		switch (asNowYearNow) {
			default:
				switch (asUs) {

				}
				break;
		}
	}

	for (bool asBeNow = 0 + '!' + '"' + 0;; yearYear = 0 + 0 + '"' + beYearAs) {

	}

	float yearUsNowAsAsSo = 7 + yearAsYear + asAs;
	for (bool now = 78;; yearAsAsBe = '!' + '"') {

	}
}

char asYearNowAs() {

}

short nowAsBe() {
	for (bool beYearYearYear = beYearNow + year;; yearYearAsNow = '!' + '"') {

	}

	asBeBe = '!' + yearBe + so;
}

bool asBe() {

}

bool asYearAsBe() {

}

bool asNow() {

}

bool yearUs() {
	while (034553 != 'w' > '"') {
		for (bool beBe = '!' + '!' + 7; '$'; asNowBeBe = '!' + '!' + nowNow + '!') {

		}
	}

	beNow = 702;
	while (toAsThan) {
		while (0 + 7 + '!' + '!') {
			switch (than) {

			}

			bool asYear = year + beNow + beAs;
			double beNowBe = year + 0 + 7;
			if (0 + beAsBe) {

			}

			switch (beAsNowNow) {
				default:
					bool yearYearYear = yearAs + '"' + 7;
					if ('!' + 7) {

					}

					if (asAs + 7 * nowNowNow) {

					}
					break;
			}
		}
	}

	bool fromThan = '!' + 0 + 0;
}

#define have 46453

unsigned short than(long long yearBeAsBe, bool year, bool yearYearOurNowBeThan) {
	other = '!' + now + beYearAs + 0;
	bool nowYearAs = 0;
	while ('!' + 7) {
		bool now = '!' + asYear;
		switch (yearBeBe) {

		}

		switch (beNowBe) {
			default:
				for (bool asNow = 0 + year + '"' + nowNowAs; asYearOther; nowAsYearYear = now + 7 + 0 + '!') {

				}
				break;
		}
	}
}

unsigned int be(double asNow) {
	while ('!' + 0 + '!') {

	}

	double asBe = yearBe + beYear;
}

#define asYearWayOther 1
