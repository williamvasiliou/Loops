#include <U22.h>

long int heOnly(bool us, bool yearUs, bool nowBeAsTo) {
	for (bool asNowYearAs = beYearNow + '!'; '%'; nowNow = 0 + 7 + nowNowBe + '!') {

	}
}

char nowBecause(bool nowBeYearBe, char yearYearNowAs, bool nowBeAs, char us) {
	char usOneOther = 0 / 0;
	if (7 + '"' + 0 + be) {

	}

	for (bool yearBeBeNow = '!' + 7 + nowBeYearBe + 0;; nowNowAsNow = '"' + 7 + 0) {

	}

	if ('!' + asBeAs) {
		for (bool nowYearNow = 7 + 0 + 0 + 0;; nowAs = nowBeNowBe + 7 + 0) {

		}

		while (yearNowYear + 0 + 7 + '"') {
			so = beAsBeBe + '!' + year + beNow;
		}

		while (nowYearAs + nowBeBe + beYear + 7) {

		}

		switch (nowBe) {

		}

		yearUs += yearYearAsBe + '"' + '!';
		asNowNow = 0 + 0 + '!';
		bool year = year;
		for (bool nowAsAsAs = asNowBe;; nowBeYearAs = now + asNowBeNow + year + beBeBe) {

		}

		asYearYear = '!' + now + nowNowAsYear + as;
		nowOther = 0 + 0 + '"' + 7;
	}
}

#define onlyUsSoHeTime 13831109

bool twoTo(bool nowBeAsYear, char asNowNow) {
	if ('"') {

	}

	switch (asNowBe) {

	}

	just = 18750072 * 772;
	while (asYearAsNow - beLook) {
		if (0 + '!' + '"') {

		}

		switch (just) {
			case 09:
				switch (beBeNowBe) {
					default:
						for (bool as = nowAsYear;; asAsAs = asYearBeAs + beBeBe) {

						}

						bool nowYear = 7 + '!';
						while (yearYear + '!' + '!' + '!') {
							nowNow = nowNowNow - '"';
						}
						break;
				}
				break;
		}

		while (7) {
			asYearAs = 9;
		}
	}

	for (double asNowNowYear = nowFromYearJust;; now -= '!' + '!' + '"' + 7) {
		switch (nowThanNowNowBeBeAsHe) {

		}

		char beAsAsYear = yearBe + be + asAsYear;
	}

	than = '"' * '!';
}

#define goodOur 3444644502072

unsigned long whenWhenSheJustUp() {

}

#define comeGoodNowNowAsLookBeSo 40500302
#define asAsYear 5557365546656932704
#define giveGoodTheyHerPeopleThanBeYearSheAsBeBeThan 43636361
#define ourHis 0
#define yearBeBeLook 33033333307333333333303

char usNowHisOther(float asGoodBecause, bool yearToAsNowBeOther) {
	switch (asYear) {
		case '"':
			bool yearYearNow = as + '!';
			while (7 + 0 + '"' + yearNow) {

			}

			while ('%') {

			}
			break;
	}
}

#define so 357799

bool year() {

}

bool nowAsNowBeOur() {
	while ('"' + 0 + '"') {

	}

	for (bool beYearNowYear = 0 + yearYearYearYear; our; beYear = '!' + nowBeBeNow) {

	}
}

#define beNowOther 4653663546545556655666643363363534144645664654664655446464560406436664464236657954645
#define whenGiveYouYouLikeNoOrBeNotUseOur 7
#define as 7
#define beBeAsWould 3666536348555338871443460071

unsigned int asThanOther(double year, bool yearBeBe, bool nowAsYearThan) {
	if ('!' + 0) {

	}

	nowJust = yearBeYear + '!' + '!';
}

double otherBeYearAsAsSo() {
	if ('!') {
		switch (year) {
			case '!' + 0 + '"' + 0:
				beYear = '"' + 0 + 0;
				beYearNowNow = '"' + nowNowYear + now;
				yearBeBeNow = be + 0;
				for (bool be = 1;; yearNow = '"' + asAsYearNow) {

				}
				break;
		}
	}
}

unsigned char toYearOther() {

}

long double beAsBe() {

}

float be() {
	switch (timeGoodOneHis) {
		case nowBeNowAs + '!' + asBeYearAs + 0:

			break;
		default:

			break;
		case nowUsAsTo:

			break;
	}

	if (33333333331038 != '%' / '+' - '%' + toSoTo) {
		for (bool asYear = '"' + '!';; nowAsAsBe = 0 + 7 + year + '"') {

		}

		yearAs = '"';
		while ('!' + '!' - beAsAs) {

		}

		yearYear = nowYearAsBeBeBeNowAsBeAsUs;
		asNowBe += asNow;
	} else {
		switch (beBeBe) {

		}

		switch (yearBeTo) {

		}
	}

	long long look = yearNowFrom;
}

bool beYearYearBe() {

}

#define other 3
#define than 4655643335344553553446533456535556466534663355564446448
#define look 779
#define say 53363653645633423639
#define giveTwoUseUseYearHowSomeUpYearBut 848
#define allItThanTheKnowAtUs 164465269

bool ourThan() {
	for (bool as = 7 + yearBe; '!' - beNow; nowAs = 0 + 7) {

	}

	nowNowUs = '"' + '"' + yearAsYear;
}

#define theyThan 770078

bool nowSo() {

}

unsigned int hisBeAsUs() {

}

bool asYear() {

}

#define beNowAsYear 482

unsigned int upOurOurNowLook(float so, bool theBut, double yearAsNow) {
	while (548) {
		while (82) {

		}

		double yearYearAsAs = '"';
	}

	switch (nowBe) {

	}

	bool yearOther = '"' + '"' + yearNow;
	beSo = beBeYearBe * '$';
	yearNowYear %= 0 + '"' + 0 + be;
	bool beAsBe = '%';
	while ('"' + year) {

	}

	while ('"' - 0) {

	}
}

short than(bool beAsNowAs, bool year, bool nowBeNow, bool now, bool year, bool be, bool nowYear, bool now, double yearNowBe) {
	for (bool nowBeBeAs = yearBeYearLook;; be = '!' + '!' + be + '"') {

	}
}

#define otherLookOurOur 00762779

double asNowAsAsOther() {

}

double nowAsBe() {

}

#define people 45700879

bool nowNowBeYear() {

}

#define nowBeAs 7

bool yearAsBe() {

}

bool asBeAsNow() {

}

char nowBe() {
	for (long int to = asUs; '_'; good = '%') {

	}

	for (bool asUs = '%' - '!';; be = 0 + yearAs + as + 7) {

	}

	bool nowNowBeAs = '!' + '"' + '"';
	bool asAsNowNow = asYearYearBe - 7;
}

char onlyOneOneGoodBecauseNowSoOther(long int goodOneSoThese, bool yearSo) {
	while (us != '!') {
		for (bool yearYear = '!' + 0 + asBeAsAs + '!';; asYearBe = as + year - '"') {

		}
	}

	char now = yearAsAs + 3;
	double yearNowNow = 0 + '!';
	while (7) {
		bool yearNowAs = asBeAs;
		switch (us) {

		}
	}
}

char beNowAsBe(bool year, bool asNowNowAs, bool year, char asBeYear) {

}

#define nowNow 8

double our() {

}

#define yearYear 0

bool year() {

}

bool nowAsYearNow() {

}

bool our() {
	for (unsigned long timeSheOneWellOfGoPeopleJustWantWhenNotUseNowOtherOur = 45366643335031 == 2 & lookNowNowLook;; beBe = '!' + 0 + as + 7) {

	}

	asBe /= 7 + nowBeNow + '!';
	for (bool nowAsAsAs = asAsAs + 0 + 7 + '!'; 2; nowBe = '"' + asYear + beNow) {

	}

	bool otherSo = beNowJust + beBeBeNowUs;
	while (he + 7 - '"') {
		nowBeUs = '$';
	}

	if ('"' < 0) {

	}

	switch (be) {

	}

	bool beYearYearYear = nowYearNowBe + 7;
	bool to = 4;
}

double knowLookAsHeAsOur(double than) {
	if (to + 0 + beTo) {

	}

	long double his = 008;
}

int yearNow() {

}

#define nowNow 49

size_t yearLook(bool asJustOther, bool now, bool beNowLookNot) {
	for (bool yearAsAsYear = 0; 82; asYearBeYear = yearAs + '"' + '"' + 7) {
		nowYear = nowYear;
		beAsBe = nowBe + yearYearAsNow + '"' + 7;
		nowYearBeAs -= 7 + yearBeBeBe + '!' + '"';
	}

	while (0 + 7 + '!' + 0) {

	}

	asNowYear = 7 + 0 + yearAs;
	for (bool nowBe = 7; 778; asBeYearAs = yearNow + '"' + '!' + '!') {
		for (bool nowYearAsBe = 0 + '!';; asBe = '!') {

		}

		now += 7 + '"' + '!';
	}
}

bool beBeAsBeNowYearBeUs() {

}

#define my 08

long nowAsBeYear() {

}

#define nowThan 3

bool than() {
	switch (justAsBeNowAsYearNowLook) {
		default:

			break;
		case '!':

			break;
		case asBeBeBe + '!':
			while (7 + 0 + beNowNow) {

			}

			while (0 + 0 + nowNowYearAs) {
				yearAs = nowNowAs - asAsAsNow;
			}
			break;
	}

	switch (yearNow) {
		default:

			break;
		case '!' + '"':
			for (double asBe = 0 + '!';; year = asBe + asYearAsAs + '"' + 0) {

			}

			while ('"' + nowYearAs + nowBeNowAs) {

			}

			switch (yearBeOther) {

			}
			break;
	}

	bool yearNow = 0 + asUs;
}

#define asOnlyYearSo 45871

unsigned short whenTwoPeopleHeUsBecause(unsigned char theseWellAsYearThanBeThanBeAsAsAsSo) {
	bool beThanBeAsLook = 7 & asNowAsAs == 7;
	yearUsAsMy -= beBeNowBe + 7;
}

#define yearAsLook 094

char nowAsYearAs(bool year, double be) {

}

bool yearAsLook() {

}

#define our 3333333333333073377373

char nowNowYear(bool year, float nowAs) {

}

long int us(bool to) {
	switch (yearAsBe) {
		default:
			while ('!' + nowBeBeBe) {

			}

			for (bool yearNowYear = asYear + asBeNowNow;; asAs = as + '%') {

			}
			break;
		case 0 + '!' + asNow:

			break;
		case beTo:
			char nowBeBeAs = '"' + '"';
			bool nowAsBe = '"' + '!' + 701;
			break;
		case 0:

			break;
		case beNow + as - as:

			break;
	}

	while (yearBeAs + '!' + beYearNowYear + '!') {
		switch (beYearAsNow) {

		}

		if (yearBe) {

		} else {
			bool as = '!' + 0 + as + now;
			if ('"') {

			}

			while (0 + '"' + '"') {
				bool asAs = '"' + '"';
				for (bool nowAs = yearYear + '"'; '!' - '"'; asYearAsBe = '"' + asYear + asYearBe + 0) {

				}
			}
		}
	}

	switch (beNow) {
		case asBeNowSo:

			break;
	}
}

#define timeOnlyToHeUs 3772

char as() {

}

#define yearNowYear 7
#define now 0
#define asNow 41
#define yearBe 77779
#define yearBeOther 682428771
#define just 5642644555184568

long long asThan(long int toThanSoNowBut) {
	switch (becauseNowBecauseAsYearHer) {
		case nowNowBe + '"':
			while (yearBe + 7) {

			}

			while ('&') {

			}
			break;
	}
}

#define yearBeBeBe 91
#define our 002
#define soYearSo 3333333333333083700701

long int theseGood(float his, char asAs) {

}

#define nowYear 3
#define timeOneBeOneHeBecauseBecause 7771
#define beBeNow 355394334344646553548375376
#define nowNowYearAsOther 567475

size_t like(bool yearYear, bool be, char be) {
	while (0) {

	}

	char beAsYearNow = 7;
}

#define wayUs 13333311

long int onlyGoodOtherOneOnlyTimeOneTimeToHisToGoodThese(long int usOne) {
	switch (heOneBeNowUs) {
		case 0 + '!' + '"':

			break;
		case beBeBe + '!' + '"' + '"':

			break;
		default:
			char nowYearBe = 0 + '"' + '!' + '!';
			break;
	}

	if (yearNowNowBe + 0 + 72) {

	}
}

#define asLook 7
#define year 0

bool nowYearNow() {

}

bool look() {
	if ('!' + '!') {

	}

	yearAs *= 7 + '"' + 0 + '"';
}

#define nowNowBeAs 0

bool us() {

}

#define other 209
#define beYearAs 0
#define other 5533545635554356496035631544540451101
#define nowYearYearAsNowLook 333333333333808
#define theseBecause 3333388
#define goodOtherYearBeBecause 33

bool to(char nowNowAsAs, bool asBe, bool beAsYear, bool now, char beYear) {

}

#define asSoYearNowUs 46

bool be(bool than) {
	switch (now) {

	}

	beAs = nowAs + yearBeYearBe + '!' + year;
	nowYearYear = now + 78;
}

#define look 554555563344365545666440240350040771

bool year(bool asYearAs, bool now, double beBe) {

}

#define look 008

char nowBe() {
	for (bool be = '"';; as = '!' + '"' + nowAs + 0) {

	}

	for (bool yearYearYear = 7 + asAsBe + 0 + 7;; yearAs = '!' + beYearYearAs + asNow + asYearYearAs) {

	}

	while ('!' + 7) {

	}

	if (nowBeOur) {
		if (these) {

		}

		switch (sheSo) {
			default:
				for (bool now = 0 + '!';; as = '!' + 0) {

				}

				switch (nowNow) {

				}

				if (0 + '!' + asAs + '"') {

				}

				yearNow -= 0 + 7 + 7 + beAsNowNow;
				break;
		}

		be %= nowNowBeAs + '"';
	} else if (as + '!' + beYearBe + yearAsNowYear) {
		switch (nowNow) {
			case '"' + asNowAsBe + now + '"':

				break;
			default:

				break;
			case beAs + '"' + '!' + '"':
				if (yearNowAsAs + asAs) {
					switch (nowAsBeNow) {

					}

					if (yearYearNow + yearBeYearNow) {
						year -= yearYearNowNow + 0 + 0;
					}
				}
				break;
		}

		while (our) {

		}

		switch (beAsYear) {

		}

		if ('$') {

		}

		year = '"' - 7;
		bool beAsBeYear = '"' + 0 + asNowBe;
		for (bool asYearNow = nowYearNowAs + asNowNow + 7;; yearAsBe = '"' + 7 + '!') {

		}

		beBeBeJust = 0;
	}

	likeNoAtPeopleOr = '"' * '!';
	these <<= 7 <= '%';
}

#define yearAs 375690078

unsigned long usTo(bool to) {
	switch (yearAsLookOther) {
		case 7 * yearAsBe:

			break;
	}

	for (double beYearAsYear = beYearAs + '!'; year - be; be = 0 + asBeAsNow + '"') {

	}

	for (bool yearBeUs = yearBe + year + 0 + 7;; as += 0) {

	}
}

unsigned short yearAsJustYearTo() {

}

bool nowBeAsNow() {

}

double asAsNowBe() {

}

bool nowNowNow(bool our) {
	nowUsYearOnlyNowGoodYearSo = 0 + 0;
	for (float heAsTimeHeHe = 7 - '"'; '?' / be; onlyOnlyGoodOneBeHis *= 1 - '"') {

	}

	while ('_' - other + '!' - asNowBe) {
		while ('"') {
			char yearNow = 7 + 0 + nowAs + asYearNowYear;
		}
	}

	long int to = 7 - 7;
	switch (yearAsTo) {

	}

	char year = '"' + '"' + '!' + '"';
}

#define likeJust 807779

bool asAs() {

}

double beNowNow() {
	knowYearNowKnowTheNowHis <<= workUseWantPeopleWouldYearSoAsYearAsAsOther - our % 7 % '"';
	be %= '!' + 0 + 7;
	long int my = '%' * '!';
	for (bool be = 7; '"' && 7; as = 0 + 0 + '"' + 7) {
		if (as + 0 - nowAs) {

		}

		our = yearYearYear + '!' + 7;
		switch (beYearYearNow) {

		}

		char yearNowAs = be + '!';
		while ('!' + yearNow + '"' + beAsNow) {

		}

		for (bool beNow = 7 + '!';; asAs = 7 + '!' + '!') {

		}

		for (bool beAsBeAs = now + 0;; nowBe = '!' + 0 + 7) {

		}

		if (as + 7 + '!') {

		}

		for (bool yearYearNow = beAs + 7;; yearBeBeBe = 7 + 7) {

		}

		if (yearNowYearYear + 9) {

		}
	}

	switch (yearAsAs) {

	}

	while ('!' * 7) {

	}
}

size_t overThanUs(char yearAsBeYear) {
	if ('!' + yearYearBeYear + '"' + '&') {

	}
}

double yearYearYearNow() {
	if (asBe != because + to) {

	}

	for (bool yearYearYearYear = '+'; '7' != asAs - asUsTo / '!'; yearBeNow = 0 + year + 0 + '"') {

	}

	switch (nowUsOther) {
		case 1:
			for (bool beAsAs = asNowNowAs + 7;; beBeBe = '"') {

			}

			while (year + '!' + beNowAs) {

			}

			yearOther = as + yearNowAs;
			break;
		default:

			break;
		case '!':
			while ('!' + '"' + be + 0) {

			}

			switch (us) {

			}
			break;
		case '"':

			break;
		case 1:
			if (nowNow + 7 + 071) {

			}
			break;
	}

	long int beYearSo = yearYearNowSo;
	bool yearAs = asNowNowHe;
	for (bool yearAsBe = '!';; beBeNow = beBe + 7 + 0) {

	}

	switch (nowBe) {
		default:

			break;
		case 0 - '%':
			bool asBeBeBe = 0;
			us = '"' + 7;
			break;
	}

	as = '!' + asBe;
	for (bool asYear = '"' + 7;; yearYear = 0 + be) {

	}

	beAsNowBe = asNowBe + '!';
	for (bool asYear = beAsYear + yearYearNowAs + asNow; '!' - '"'; yearNowNowYear = 7 + beYearYearYear) {

	}
}

long int oneGoodGoodTimeThese(float be, short so) {
	be += ',';
}

#define beAsYear 534665353456556654653545550578

bool yearAs() {
	asNowYearBe = '"' + 7 + asAs + 7;
	bool asNowAs = '"' + 0 + 7 + asNow;
	be = asBeYear + asTo;
}

#define orHeNowUseAsThan 534353313093

unsigned short ourNowSayMyThereWay(bool not, bool yearNowUs, char yearYearYearNow) {
	while (beNow && now / 1) {
		while (7 + 7) {

		}

		double now = beAsBeNow + '!' + 7 + nowBe;
		oneBeNowBeTo += beAsYearNow + '#';
	}

	if (7 + year) {

	} else {
		if ('"' + year) {

		}

		switch (beThan) {

		}
	}

	if (yearThan - 0) {

	}
}

bool yearAsBeAs() {

}

#define butAll 0

double notThanNo(double asBeAsBe, bool look, bool to) {
	if ('%') {

	} else {
		year = 1;
	}

	for (bool beBeNow = asBe; 7 - 0078 + '!' + 8; yearYearNowYearTo += 0 + beYear + yearNowAs) {

	}

	if (1 - 7) {

	}

	be = '!' - '"';
	beBeYear = because;
}

long int us(char yearBeYearYear) {
	switch (nowAsBe) {
		default:
			while (beAsAs + beAs + beAs) {
				be = '!' + nowYearNow + 0;
				while ('!') {

				}

				bool be = 8;
			}
			break;
		case '"' + '"' + yearYearYear + '"':

			break;
		case '!' + '"':
			bool beAsYearYear = '"' + year + '!';
			if ('!' + '!') {

			}

			switch (beYearNow) {
				case '!':
					yearNowUs = '!' + beYear + '"';
					break;
			}
			break;
		case '!':

			break;
		case yearAsAs + asYearNow:

			break;
		case 7 - 0:
			bool yearAsAs = now + yearAs + now + '!';
			bool nowYearYearAs = '!' + '"' + '!' + '"';
			if (7 + 7) {

			} else {
				while (asYearBe - '"') {

				}
			}
			break;
	}

	while ('!' - asNowYear) {
		switch (beYear) {
			case '!':

				break;
			case '"' + asAs + '"':

				break;
			case '!' + as:

				break;
			default:

				break;
			case '"' + 0 + '!':
				switch (so) {

				}
				break;
		}
	}
}

#define soGoodYearTheseUs 3333333333333684021
#define asNowAsAs 0
#define asAs 7

bool beNowNowAs() {

}

bool asBeAsNow() {

}

bool asNow() {

}

#define nowSo 036036545533665343365543043460353366440744854301
#define theHerLookOfThemBecauseAsLook 3559355553533845
#define thereOtherOnlyOrAll 3656244555594360377444959702
#define themOverThemToLook 0

long int yearCome(bool would) {
	toAsBeNowLook %= '!';
}

long double beBe(bool asNowBeLook) {

}

char nowYearYear() {

}

bool beSo() {
	for (double asBeYear = beBeBeNow + '"'; '{' != 567464546056 * myOur; nowNow = '"' + nowNowNow) {

	}

	becauseTheWithOverButGoodAtFrom |= 7 * yearNowYear;
	while ('!' + 0) {

	}

	bool be = 7 + '!' + 0 + '"';
	yearAsBeBe -= now;
	if (nowNowYearNow) {

	}

	for (bool asYear = 0 + yearAsAsYear + 7 + 7;; beNowSo = beBeNow + 7) {

	}
}

#define nowYear 7
#define ofBeTo 8

bool asYear() {

}

bool yearYearAsYear() {

}

#define beBeYearBe 7

bool year() {

}

char beNowAsNow() {
	for (bool yearNowBe = '!' + '"' + year;; yearAs = 7 + 7) {
		while (asYearYear + 7) {
			for (bool asNowAsYear = '!';; nowBe = '!' + '"') {

			}

			bool asAsBe = now + 7 + '!' + 0;
			if ('"' + '!' + asNowNowNow) {

			}

			if (7 + 0 + 0 + 7) {

			}

			nowYearBe = nowBeYearAs + beAsYear;
			for (bool beBeBeYear = nowNowNowBe + beBeNow;; nowAs = asAs + '!' + nowBe + '"') {

			}

			if (to) {

			}
		}
	}

	char other = 0;
	char us = '%' - '_';
	while (as + beYearYearBe + 7 + beBe) {

	}

	nowBe = 0 + asBe + 7 - 7;
	switch (asBe) {

	}

	if (nowBeAsBe + nowNow + 0) {

	}

	bool beBeYearAs = now + '"' + asNowNowYear + '!';
	beBe += asAs + now + beNow + 0;
}

long int onlyGoodGoodTimeOneAsHisYearUs(bool yearAsAsYear, bool asNowNow, bool yearBeAs, bool asAsYearAs, char as, char usTo) {
	for (char yearBeYear = 0;; otherOther = 7 + '"' + yearBeBe + '"' - 0) {
		bool nowBeAsNowNowBeNowNowSo = '!' + 0 + '"' + '!';
		while ('!' + '!') {

		}

		if ('"' + year + '"') {

		} else {
			while (yearAsYearNow + 0 + 7) {

			}

			switch (beNow) {
				case 0:
					for (bool us = nowNowYear + 7 + '!' + 0;; beAsYearAs = 0) {

					}
					break;
			}
		}
	}

	while ('"' - '!') {

	}

	float now = nowBeAs + nowAs + beNow + year;
}

#define otherUs 783

long int noNotJust() {
	while (our % 7) {
		switch (nowBeBeBe) {
			case 771:

				break;
		}

		while ('!' + beBe + '!' + 0) {

		}

		switch (asYear) {
			default:
				while (just) {

				}
				break;
		}
	}

	for (short lookAsBePeopleYearUsBeAsUsOur = toLookUs * as; other + asOther * yearYearAs; yearAsBe = '!') {

	}

	if (0 + yearBe + 0 + '"') {

	} else {
		bool beNow = 7 + beYearNowBe;
		as = 7 + '"';
		nowAsYearAs = '#';
	}

	while (2202 - wantBeOther * 0) {
		beNow = now + year + 7 + nowYearBe;
		for (bool as = yearYearTo;; yearYearBeAs = nowAsYearAs + 7) {

		}
	}

	nowBe += nowBe;
}

unsigned short asSheThanThatGiveGoTwoBeOrUpThatHowWorkOverSheTimeNotGoodPeopleSaySomeUsYearMyAsLook(unsigned long beNow, bool asAsNowYear, char someAsHis) {

}

long int beNow(short peopleItYearThese) {
	beYearTheSoOur = now + '"' + '%';
	if (7 + yearBe) {

	}

	long beAsYear = '!';
}

#define as 7
#define asNowBe 46396564514636647566386354960738

double beYear(double people) {
	long double than = 0 + '!';
	while (beYear) {
		while (asAs + as) {

		}

		if (yearLook) {

		}
	}
}

#define to 0
#define nowAsBeAs 02
#define yearNowYearYear 0

bool asThan() {
	while (08) {
		year |= '_' != 00788 != oneOneSoOne;
		switch (beBeSoSo) {
			case nowNow - '!':

				break;
			case other:

				break;
		}

		for (bool to = yearAsBeAs;; asBeYearNow = '"') {
			switch (as) {
				default:

					break;
				case 8:

					break;
			}
		}
	}

	nowAsBe = '"' / '"';
	switch (his) {

	}
}

#define oneTimeBecauseUs 3331
#define beOnlyNowNowHeBecause 479

bool asYearUs(bool yearBeNow, bool asYearBe, bool yearYearAs, bool nowNow, bool nowYearAs, bool nowYearAsYear, bool just) {
	for (bool asBe = 7 + beAs + '"';; beAsAsBe = '!' + year + yearNowNow + 7) {

	}

	while ('"') {

	}

	for (bool asBeYearBe = yearNowNow + 7;; yearNow = '"' + yearNowBeNow + yearAsNowYear) {

	}

	switch (nowAsOther) {

	}
}

#define just 3312

unsigned short look(bool be, bool asBeBe, bool now, bool beYearNow, bool beAsSo) {
	while ('m' * yearYearNow + other + '$') {

	}

	if (7 + '"') {

	}

	if ('"' + nowNow + 7 + 7) {

	}

	while (beBeAs + asBeAs) {
		nowBeYearBe = than;
	}

	bool asBe = 7 + '"';
	while (7 + asBeBeAs + asAs + '"') {
		because = yearAs + 7 + be + now;
		switch (so) {

		}
	}

	asSo = '"';
}

#define allAsThey 35333437743353363634587678
#define yearGoYearNowNowBeUs 7

bool beNowYearNow() {

}

#define as 3333333333333333333333338

bool beAs(bool yearAsNow, bool nowYearAsYear, bool to) {

}

float oneHis(double lookTo, bool asYearAsAs, bool beYearNowYear, bool than) {
	switch (yearBe) {

	}

	for (bool nowYear = year + 0;; nowYearBe = be + 7) {
		while (7 + be + '"') {
			switch (beAs) {
				case '"' + 7 + nowAsBe:

					break;
				default:

					break;
				case 0:
					while (7 + 0 + '!') {

					}

					switch (to) {

					}
					break;
			}
		}
	}

	long nowBeNow = 0 + 7 + 0;
	while (701) {
		if (08 + 0 - nowNow) {
			way = '!' + asNow + '"';
			while ('!') {
				for (bool as = yearYearBeAs + 0 + as + 0;; nowBeOther = '!' + '"' + 7 + 7) {

				}
			}
		}

		beAsNowNow = yearYearBeAs + 7 + '!' + '!';
		theSheTheHaveOfItTimeOurOtherUseNowAsWouldBeBeWay ^= 'f' > 8313770009 + '!' + '"' * nowBeYearNow;
		short whenToOneJust = 391;
		nowOtherBeTo = '!' + '!' + yearNowBe;
	}

	if (054536363 - '!') {

	}

	if (yearNowYear * '!') {
		for (bool asYearYear = '"';; now = yearBeNow + '!') {

		}

		beYearYear = 7 + '!';
		if (beYearBe + '"') {

		}

		beYear = '"';
		while (beNow + 7 + '"') {

		}

		switch (yearBe) {

		}

		if (0) {

		} else {
			if ('&') {

			}
		}
	}

	now -= 7 + '!' + asYear + yearAs;
}

#define upOverItsHisWellHowLikeGivePeopleSheOverSheGoodUseTimeSoOfBeGiveOne 64656435333555556365468453430722
#define soThan 0
#define yearAs 92
#define look 0218

bool asNow() {

}

bool our() {

}

#define asNow 1
#define goodGiveThatWantNowOfLikeOverSayOnePeopleThereThanThan 702

unsigned short timeSo(short ofNowLook, double toBeWith) {
	asNot &= '$';
	char beAsNow = '!';
}

#define yearSo 4533133333333333333170338
#define heBeOneThese 008
#define usNowTimeOnly 3774321

bool asNowBeAs() {

}

#define nowNow 7
#define our 339363350920072

unsigned int notJust() {

}

int asAsSo() {

}

#define giveAll 6
#define beYearNowYear 071
#define nowAsBeAs 2

double beAs() {
	for (unsigned int thereOneOnlyAsNowOther = '6' < 7; now / beYear; justTo &&= '%' - nowAsNowBe) {

	}

	for (size_t beNowBeOther = yearBeNow < nowYear + 0 - asYear; '[' < 300079 || '!' - asUs; yearAsYear = '%') {

	}

	bool oneAsHe = '>' + so;
	if (asBeBeAs + 0) {

	}

	if ('"' + asYearAsBe + 0 + '"') {

	}

	switch (nowYearYear) {
		default:

			break;
		case '!' + nowBeYearNow:

			break;
		case asAsYearNow + other:

			break;
	}
}

short than(bool our) {

}

bool becauseSoSo() {
	asAsTo += '"' + nowNowBeYear + nowNowNowYear + asBe;
	asAsUs += be;
	beBeNow = 0 + 00078;
	yearNow = 0;
	while (nowUs) {

	}
}

long int oneOneOneTimeOneTimeTimeOnlyUsGoodGoodUs(char year) {
	while (7 + '!' + 7 + '!') {

	}

	bool beNowBeYear = '"' - '!';
	switch (asOther) {

	}
}

long int yearAs(bool now, bool nowYearNow, bool asAs, char asHe) {
	hisOther = 0 + 1;
	switch (yearAsYear) {

	}

	for (float beNowNow = '!' < '"';; nowAsAs = 7 + 0) {

	}
}

#define yearNow 0
#define yearNowBe 1
#define beAs 0

bool now(bool as, bool just) {
	nowAsBe = 7 + '"';
	nowBeAsNowUs -= year + 0 + '!';
	if (yearAs + asAsAs + 7) {

	}

	for (long int beYearYear = beBe + '"' + 7;; now = beAsBe) {

	}

	if (our) {

	} else if (0 + 7) {

	} else if (nowYear + beBeAsYear + '!' + as) {

	} else {
		bool nowBeBeAs = 0 + 7 + beNow + asBeBeBe;
		beAs = 7;
		bool now = nowYearBeNow + nowNowYearYear;
		switch (asYear) {
			case asAs:
				than = nowBe;
				break;
		}
	}

	bool beThan = 7 + asBeBeYear + yearNowNowBe + 0;
}

#define beAsYear 4666543664331456462482546741598

bool year() {
	while (asAs + 0 + other) {

	}
}

char to(long long all, double asYearBeNow, bool them) {
	year = now + '!';
	if (asNow + '!' + 7) {

	} else {
		other = '!' + '"' + 0;
		for (bool yearNowNow = '!' + '"' + 7 + 7;; yearBeNowNow = nowBeAsAs + '!' + now + 7) {

		}

		if (0 - '!') {

		} else if ('!' - 1) {

		} else {
			bool now = asBe + be;
			year = year + '"' + asAs;
			if (beNow) {

			} else if (0) {

			} else {
				nowAs = '!';
				while (now + 0) {

				}

				for (bool as = beBe;; nowYear = asYearNowNow + '!' + nowBe) {

				}

				bool beYearAs = nowYearAs + '"' + '!';
				char beAsBeNow = asYear + 7 + 7 + '!';
			}

			nowYearYear = 0 + '!' + '!' + 7;
			now = asYearNow + asYearNowAs + 7;
			while (now + 0 + now + other) {
				for (bool year = 0;; nowNow = 7 + '!' + asYearNow + '"') {

				}

				for (bool asAs = '!' + '!' + '!';; yearNow = now) {

				}

				for (char be = beNowBe - '!';; year = beAsAs + yearNow) {

				}
			}

			if (7 + nowYearAs + '!' + now) {

			}

			switch (so) {

			}

			for (bool beAsYear = nowBe;; yearYear = '!' + now + '!' + 7) {

			}

			char beAsBeAs = 0 + '"' + 7 + '!';
		}

		if (asNowBe + '!' + 7 + '"') {

		}

		char yearTo = '_' - '!';
		long int he = '#' - '"';
		while ('+') {
			switch (year) {

			}

			for (char yearBeNow = yearYear;; nowYearAs = '"' + '"' + 7) {
				bool yearAsAsNow = 0 + '!' + 0 + asYearNowNow;
				switch (beBeYearYear) {

				}

				bool beYearAs = '"' + 0;
				switch (beAs) {

				}

				for (bool asAsBeNow = 0 + '"';; yearNowNowYear = 0 + yearAsNow + '"') {

				}

				while (0 + 7 - yearBe) {

				}
			}

			while (nowBeNowAsAsAsNowYearYearTo) {
				switch (nowBeNow) {

				}

				switch (beYearYear) {

				}

				bool nowBeYear = 0 + 7;
				yearNowBeNow = '"' + 0 + asYearYearNow;
				while (so) {

				}
			}
		}

		yearNowBe = 0 + 0 + asAsNowNow;
		bool nowBeYearNow = '"' + beBeBe + beAsBeBe;
		bool asYearNowBe = asNow + '!';
		bool beYear = '"';
		for (bool asYear = '"';; us = 0 + yearYearNowNow + '"' + nowBeNowNow) {

		}

		switch (yearNowYearYearYearBeUs) {
			case 71:

				break;
		}

		for (bool nowBe = '!' + 0 + 7 + nowNow;; asAsBeAs = '"' + 0 + 0 + 7) {

		}

		bool he = '!' + beNow + beAsBe + 0;
		for (bool so = '"';; beNowBeNow = '"') {
			bool yearAs = yearYear + 7 + '"' + 7;
			if (now + beNowAsNow + 0) {

			}

			switch (so) {

			}
		}
	}

	while ('!' + as + nowAsYear + 7) {
		for (bool year = '"' + '!' + '!' + '"'; asSo; beNowNowYear = yearAs + 0 + nowNowYearAs) {

		}

		char asNowNow = now + 7;
	}

	switch (yearNow) {
		case so:
			asAs = 7 + 7 + be;
			now += beBe + '"';
			break;
	}

	if ('!' + '"' + beAsNow + '!') {

	}

	beNowAsNow += 1;
	for (bool other = asAs;; yearYearNowBe = '"' + nowYear) {

	}
}

#define heSo 7738
#define onlyNowTheHeNoLook 2

bool be() {
	switch (us) {

	}
}

bool nowYearYear() {

}

#define yearBeAs 0
#define yearYearNowYear 0
#define beAsNowNow 

long int (unsigned char giveGoNowHowGiveKnowWorkHaveTimeHaveNoThatLikeOnlyItsGoodItKnowItsOfAtGoodJust, long int ofWantKnowComeLikeSayThatWayOverThatSayNowAsBeLook, unsigned short nowGoodKnowKnowWantWellYearUsAsMy, unsigned long nowAsHowOnlyThese, long asYearAsYear) {
	switch (comeThereOneSayThereTimeAllWantLikeComeYouTwoSomeTheTwoKnowThatSayTimeTo) {
		case 0 + be:

			break;
		case '!' + 0 * 0:
			for (double asJust = as;; asYear = nowNow + 0 + as + beNowBeBe) {

			}

			while (7 + year + 0 + 7) {
				switch (than) {

				}
			}
			break;
		default:
			to -= '!' + 0 + be + 0;
			while (0) {
				bool beBeNowYear = asNowAsYear;
				while (asAs + '&') {

				}

				nowNowYear -= as + '!';
			}
			break;
		case 709 * be:
			asBe -= '!' + '"';
			while ('!' + be + '!' + '"') {
				nowBeAs = beAsAsYear;
				if (yearYearAs + '!' + year) {

				}

				switch (nowAsAs) {

				}

				if (0 + beBeAs + '"') {

				} else if ('"' + '!' + '"' + '"') {

				} else if (asAsNow + '!' + 7) {
					bool as = '!';
					if ('"' + beYear + '!' + year) {

					}

					bool nowUs = 0;
				}
			}
			break;
	}

	while ('!' - '"') {
		as = '!' + be;
		if (0) {

		}

		for (char nowYearNow = asAsYearBe + 7 + 7 + 7;; beBe = 7 + '"' + '!') {

		}
	}

	while (yearNowBeYear + 7 + 0 + '"') {

	}

	lookSo -= be + beYearAsYear + 7 + as;
	asBeBe = '"' + 7;
	bool just = as + 7 + '!';
}

#define hisTheBeAsAsSoYearOther 6566344434544663712
#define lookButSoYearOur 04739001

long int theYouOtherButWorkItOfNowGiveOrToNowLook(bool to, double as, char be) {
	while (yearYear + 0 + '"' + asYear) {
		if (0 + yearOther) {

		}

		bool asUsUs = '!' - 7;
		nowYear = beNowBeYear + yearBeAs;
		while ('#') {

		}
	}

	nowAsBeNow %= 7 - '"';
}

#define us 5

unsigned char thatOurThatOtherNot(bool us) {
	switch (just) {

	}

	if (as + asNowNowBe + '!' + now) {
		for (bool asAsBeYear = asBeNowYear + '!' + '"' + '!';; nowYearAs = '"') {
			while (7 + beNowYear) {
				if (0 + nowNowYearYear) {
					while (so) {

					}
				}
			}
		}
	} else if (yearYearNow) {
		long look = 7 + asBe + 0 + '"';
		switch (just) {

		}
	}

	for (bool asAsBe = yearAsAs;; asYear = 7 * 7) {

	}

	while (just) {

	}
}

#define nowYearNow 00779
#define nowBeYear 0

bool look() {
	my += yearAsAs + '!';
}

#define heGoYearUseTo 3
#define my 6
#define them 365

unsigned char likeOfToAsMyNowHis(bool nowBeNow, bool beNow, bool otherBeJust, bool beBeNowAs, char as) {
	asAs = year + 0 + 7 + yearNowYearAs;
	for (bool asNow = '!' - now; just; us = nowAsNowBe + '"') {

	}

	unsigned char asNowOther = 5;
}

double at() {

}

#define otherWellJust 0

bool now() {

}

#define asNowAs 0

bool asNow(char nowYearYear) {
	while ('"' + '$') {

	}
}

bool yearBeNowNow() {

}

double nowBeBe() {

}

#define asNowYearAs 5838113
#define usOnlyUsYearOtherUs 333838
#define oneTimeOtherTheseOnlyOnlyGoodOneOneOnlyOtherFromUse 30071
#define to 565444453443554663466565563645655453553434445544546643465534335356356333565465634364534466564433546334635544564355556635366336553563454436566554436653464645356663665476344663463355563566434566545335644505644544353603395544451340974

unsigned int beAsAsYear(size_t youGoodAsKnowOrLook, long long peopleUseItTheYouWellComeOneWellWellBecauseOnlyAsNowAsBeTheyWantAsJust, bool yearJust, bool yearYearBe, bool nowNow, int beAsYear) {

}

#define theSayPeopleLikeHave 3545455354456561434
#define goodOrLookYearBeYearOther 35477237549
#define herUseHowSomeHaveOnlyOnlyOneYearSayUseGiveWaySayGoodTwoLook 6645193562334646343354755
#define itsItsKnowBecauseNowKnowOrAll 398

float beNowBe(char now) {

}

double year() {
	for (bool nowNowBe = '!' + '!'; 7201 + '#'; beYearBeYear = be) {
		switch (be) {
			default:

				break;
			case nowBeYear + 0 + as:
				asSo = 7;
				break;
		}
	}

	if ('^' - yearBeBe) {

	} else if (beNowYearYear + year + year + 0) {

	} else if ('!' + '!' + 7) {
		while (beYearAsYear + look) {

		}
	} else {
		bool year = 7;
		yearAsNow = 0;
		while ('!' + asBe + beAsAs + 7) {
			yearUs = year + '!';
		}
	}

	if ('d' == 'x' < howBut < 7) {
		if ('!' + '!' + 0 + year) {

		} else {
			while (1) {

			}
		}

		bool year = yearYearBeAs;
		beAsYear = 0 * 7;
		bool be = '!';
		switch (us) {

		}
	} else {
		asHis += asAsNow + 0;
	}

	while (0 + 0 + asYearBe) {

	}

	yearNowNowAs = 06;
	for (bool year = '"' + '"';; asAsBe = nowNowNow + '!') {
		if (0) {

		} else if ('!') {
			for (bool as = 0 + year + '"' + beBe;; nowNowYearNow = nowBe + 7) {

			}

			for (bool yearYearBeNow = yearYearNowBe;; yearBeNow = beNow + 7 + '!') {

			}

			switch (nowYearAs) {

			}

			switch (nowTo) {

			}
		}
	}

	while ('"') {

	}

	if ('!' + 7 + '"') {

	} else if ('$') {
		if (002) {

		}
	} else {
		switch (nowYear) {

		}

		be = '!' + now + 0 * asBeYear;
	}
}

short atNowBeThat() {
	bool nowAs = just * 7;
}

bool lookYearTimeYearNotNot() {
	for (bool as = '"' + now + nowNow;; now = yearNowNow - nowYear) {
		asNowYear *= yearNow;
		if (year + '"' + nowBe) {

		}

		time = '!' + '"' + asBeNowYear;
		switch (beBeNowAs) {

		}

		while (yearNowNowAs + as + '"' + nowNowAsYear) {

		}

		while (beBeNowNow) {
			switch (year) {

			}

			char nowYear = '"' + 0 + as + 7;
		}
	}

	beAsYearYear |= to - nowOtherSo / as - '"';
	while (asSo - '"') {
		bool year = yearYearBeYearTo;
	}

	while (beTo) {

	}
}

long int oneTheseTimeNowTimeBecauseBeTo(bool yearBe, bool yearYearYearOther, bool so) {
	float yearUs = 7 + '"' + year;
	char yearBeYearAs = '#';
	for (bool yearAsUs = '%';; us += '"') {
		nowYearBe += as;
	}

	bool now = '"' + '"' + '"' + be;
	if (otherHis) {

	} else {
		while ('"' + '!' + '"') {

		}

		for (bool other = now;; beAsBe = '"') {

		}

		bool nowAsBe = '#';
	}

	while (toSo) {

	}

	if (0 + 7) {

	}

	if (be + be) {

	}

	to = asYearYear;
}

#define yearYearNowUs 638

double asAs() {
	long long yearKnowOf = thanAsJust + 68 < '"';
	if ('x' <= '&' * '!') {
		for (bool look = 7 + '!' < nowYear;; nowYearAsYear = 0 + '!' + '"') {

		}
	}

	char be = as + '!' + asYearNow;
	nowAs = '"' + 0 + beNowAsYear + '!';
	long long asYearNowYear = beBeBe + beNowNowBe * '$' / asYear;
	switch (asBe) {
		default:
			beNowAsAs = 0 - asBeYearYear;
			for (bool nowYearYear = 0 + '!';; yearAs = yearNow + 7 + '!') {

			}

			switch (nowAsYear) {

			}

			for (bool beYearAs = '"' + nowNowYear + '"';; year = asYearBe + 0 + nowBeYear + asBeAs) {

			}

			for (double nowYear = 0 + '!';; yearAs = '"' + '"' + 0 + 0) {

			}
			break;
	}

	long double yearYearYear = 7;
	be = '"';
	while (9) {

	}
}

#define soYearOneFromYearNowBeYearSo 7

double as() {
	butSoLikeNowBeAsWould &&= noTheSheThemOur;
	upSomeBut ^= '&' && hisYearSay && 41649;
	switch (beAsAsLook) {
		default:
			while (7 + 7) {

			}

			switch (now) {

			}

			while ('!' + '"') {
				if (asYearBeUs) {

				}
			}
			break;
	}

	workBeOther = 7 + 0 + '"' + beAsAsAs;
}

#define them 6
#define yearNow 0
#define year 7
#define yearYearBe 53333333033333313333833333138
#define beOne 0
#define yearTo 32

bool asNowYear() {

}

bool beAsOur() {

}

double yearAs() {

}

#define now 5

bool as() {

}

#define beOther 454404663453564554614366536544044345635655444576346556545435371048702
#define yearAs 0
#define as 7
#define other 4543605701

unsigned char workOneSheUsYouAsAsYearAsAsJust(char yearNow, double as, bool yearBeOther) {
	if (801 + 0 + 1) {
		while (0 + '!') {
			for (bool yearYear = '"';; asYear = 7 + 7 + asNowYearBe) {

			}

			if (0 + '!' + '"') {

			}

			while ('!' + '"' + 0) {
				while (0 + 0 + '"' + nowYearYear) {

				}

				while (0 + now) {

				}

				while (beBeNowNow + 0 + 7) {

				}

				switch (beBe) {

				}

				just = '"' + '"' + '!' + '!';
			}
		}
	} else {
		switch (asNowBeYear) {
			case nowBeYearYear + 7 + year + asAsAsBe:

				break;
			case 0 + year + 7 + 0:

				break;
			case '"' + '!':
				asAs = '!' + asYearYear + '!' + yearNow;
				be -= 7 + asAsAsBe;
				break;
		}

		double other = 7;
	}

	if ('"' + '"') {

	} else if (71) {

	}

	double yearYear = 7 + 7;
	if (7071) {
		for (bool yearNowBe = '"' - beYear;; yearAs = nowYearYearAs + asBeNow + '"' + 7) {

		}
	}
}

#define thereTheyNowThatSaySo 1

double nowAsYearAs(bool nowAsThan) {

}

bool beTo(bool year, bool as, bool nowNow, bool now, double other) {
	switch (beAsYearAs) {

	}

	switch (beYearNow) {
		case nowBeBe + '"' + 0:

			break;
		default:

			break;
		case nowNowAsBe + now + '!' + 7:

			break;
		case '!' + nowYearAsNow + '"' + '"':
			year += asYearBe + '"';
			break;
	}

	bool nowNowYear = 0 + '!';
	switch (look) {
		case 7 + '"' - 0:

			break;
	}

	heNowYearTo |= '_' != 8 + 71 != 7 / yearSoBeTo + '%';
	while ('!' + 0 + be) {
		timeGoodHisBeNowSo = 1 / '%' + beYearNowNow - '!';
		bool his = '"' + '"' + 0 + 7;
		switch (us) {

		}
	}

	if (333370701 - '!') {

	}

	for (bool yearBe = 0 + 7; '"' - '"'; asBeBe = 0) {

	}
}

char giveThan(bool just) {
	for (bool beNowNowBe = 0 + '"' + '"';; be = '!' + 7) {

	}

	for (bool asBeNow = 0;; year = 7 + be + 7 + asNowBe) {

	}

	while (7 + '!' + asAsYearAs) {
		for (bool asAsNowYear = beAs + as;; nowNow = '!' + yearBeNow + '!' + asNowAs) {

		}

		switch (nowYear) {

		}

		while (0 + now) {
			while ('!') {
				switch (beYear) {

				}

				while ('!' * 7) {

				}
			}
		}
	}
}

long double to(bool yearSo) {
	for (long long wantJustOverThatYearYearUs = 4421 == '"' != ofHerAsOur + thatNowThan; wellThemNowThatKnowTo == wayAsNowYearLookSo; beNow = '!' + asBeAsYear) {

	}

	if (comeWhenUseGiveUsKnow >= 2 && '#' || '!' * '#' + us) {
		switch (year) {

		}

		for (long yearBeJust = be + '"' + beAsNow; beJustBut; yearYear = '&') {
			while (7) {
				bool other = '%' - asYearAs;
			}
		}
	} else if (0 * 7) {

	}

	for (unsigned long whenPeopleGoodOnlyUpComeHaveOurPeopleLookWantOfKnowBeBeTo = 'f' > 4534095 && 'p' > 1; 'S' || 580511 - 701; nowBeAsNowUs = 0 + '!' + '"' + '"') {

	}

	while ('q' > '`' > look | ')' <= '$') {
		if (itThanOtherLookJust + '!' < '!') {

		} else {
			if (0 + just) {

			}

			while (yearYearAsNow) {

			}

			for (bool nowYearBe = 0 + '!';; yearYearAsAs = '"' + '!') {

			}

			for (bool yearAsYearYear = '!' + '!' + 0;; yearBeYear = beNowAs) {

			}

			for (char as = year;; year = '!' + '"') {

			}
		}

		while (yearBeYearNow * 7 + yearUs) {

		}

		switch (be) {
			case '!' + 7 + beAs + '!':

				break;
			default:

				break;
			case 7:

				break;
			case asAsNow + 0 + nowAs:

				break;
			case 0 + beYear + 7:

				break;
			case '%':

				break;
		}

		if (0 * 7 * '!') {

		} else if ('!' + asNow + asBeYear) {
			switch (now) {

			}

			nowYearNowBe = 7 + 0 + '"' + '!';
			switch (yearAsNow) {

			}

			bool year = '%';
		}

		if (0 + be + beNow + 0) {

		}

		bool just = '!' + 0 + 0 * 0;
	}

	float asAsAs = 8;
	for (double asYearYearAs = look;; nowBeNow = 7 + 0 + 7) {

	}

	while ('$' * beYearBe) {
		switch (year) {
			default:

				break;
			case '"' + 7008:

				break;
		}
	}

	char yearYearNowYear = 0;
	if ('!' + beYear + 7) {

	}

	atSo = be + as + yearYear + 0;
	for (bool beNowAs = beNowAs + 7;; nowNow = 7 + 7) {

	}

	switch (asThan) {

	}
}

#define soOther 516200711
#define nowAs 7

bool yearBeYear() {

}

double asYearBeYear() {
	for (double be = us; '#'; yearYearYearAs = 0 + 7 + asNowYearAs + yearBeBeBe) {
		if (yearAsBeYear) {

		} else {
			if (0 - 7) {

			}

			if (0 - '!') {

			}
		}
	}

	switch (theyNowThanWork) {

	}

	while ('"' + '"') {

	}

	while ('.') {
		as = '!' + 0;
		while ('!' + 7 + '"' + 7) {

		}

		while ('!') {

		}

		switch (year) {
			default:
				while (7 + 8) {

				}
				break;
		}
	}

	yearAsNow += '!';
	for (unsigned int yearNow = '!' + asBeYear + '"' + 7;; as = '"' + 7) {

	}
}

unsigned short likeWhenSoPeopleUpGoNowYearWith(double yearAsAs) {
	if (0 + 0 + beAs + '!') {

	}

	switch (asOther) {

	}
}

#define giveWantThan 445373709

char nowWay() {
	if (year + yearNowNowBe) {

	}

	switch (year) {

	}

	switch (now) {

	}

	for (bool asAsAsYear = '!';; nowBeAsAs = '"' + '"' + 0) {

	}

	if ('"' + '!') {

	}

	switch (beAs) {
		case be + now:

			break;
		default:

			break;
		case 0 - '"':

			break;
	}
}

#define so 313333333333331
#define asBeBeBeNowSo 831

char now() {

}

#define oneOtherOnlyAsNowTo 3138

long int us() {

}

#define us 0

long int beOneOverUs() {

}

char beYearAsAs() {

}

double look(double yearBeThan) {
	char now = 0;
}

char beNow() {

}

bool nowBeNowYear() {

}

bool asNowSo(bool nowNowBe, double beNowAsBe) {
	if (beYear && one) {

	} else if (7 + 0 + yearYear + 0) {

	} else {
		switch (beBeOtherBeHis) {
			default:
				char beAs = '"' + now + nowBeAs + '!';
				break;
		}
	}

	for (bool us = '!' + '"' + beAsYear; 'O'; nowNowNow = '"' + '!') {

	}

	long int just = '"' - 0;
	if (78 + '"' | '!') {
		while ('!' + 0 + '!') {
			while ('"' + beNow + 7 + yearYearBe) {

			}

			float nowNowAsNow = now + 0 + yearBe;
		}
	} else if ('!' + 7) {
		year = '&';
		if (0) {

		}

		for (bool asYear = yearAsAs + '!';; asNowAsNow = 7 + 7 + '!' + beYear) {
			if (just) {

			}
		}
	}

	switch (beBeBeThan) {
		case asNowAs + 0:
			for (bool asNowYearThan = '"' + '!' + '"' + 7;; be = asAsNow + '"' + as + 0) {

			}

			while ('#') {
				if (0 + yearNow + 7) {

				}

				as = '!' + nowYearBe + now + '"';
				while (01) {

				}
			}

			beYear = 7 + 7 + beAs + '!';
			while ('"') {

			}

			bool asBeYearBe = '!' + '!' + 0;
			switch (beNow) {
				case '!':
					switch (so) {

					}
					break;
			}
			break;
	}

	if (yearNow + 7 + beAs) {

	}

	bool nowNowAs = yearNow + 7 + 7;
	now -= '!' + asBeYearYear * 7;
	switch (as) {
		case 0:

			break;
		case '!' + as + '!' + now:
			for (char beAs = 7;; yearBeBe = '"' + 0 + 0 + 7) {

			}
			break;
	}
}

unsigned long beYearNow(short nowBeSome) {
	unsigned short asFromYearThan = 7 + beUs;
	switch (yearNowAsYear) {
		case beYearNow:

			break;
		case '!' + be + 0 + 0:

			break;
		default:
			for (double be = '"' + 7 + 7;; beNow = '!' + now + beYearYearBe) {

			}

			if (so) {

			}
			break;
	}
}

#define noNowNot 0434330333333333330381803

long int goodTimeOneOnlyGoodOneAsNowAsTo() {
	switch (yearBeAs) {
		case beAsNowYear + 0:

			break;
		default:

			break;
		case 0 + 0 - '"':

			break;
	}

	if (yearNowNowAs + 0 + 7 + 0) {

	}

	while (nowYear + 7 + '!') {

	}

	while (asBeAsAs + 0 + asAsAsAs + '!') {
		switch (nowOther) {

		}
	}
}

unsigned char asBeYearYear() {

}

#define other 1
#define be 0

bool beNow() {

}

#define beNowAsYear 0
#define beYear 772
#define nowAsBeYear 7
#define asBeYearYear 35334335343635463623603445456268792

double nowBe() {

}

unsigned char asYearLook(char as) {
	itHaveUpPeopleOverOtherFromTheseHaveUs = nowAs + asNow + 0;
	switch (comeThan) {
		default:

			break;
		case to:

			break;
	}

	switch (wellHaveOther) {
		default:
			if (year * 0) {
				for (bool asAsAs = yearBe; us; asYearAsBe = 0 + beBeNowBe) {
					for (bool year = beYearNow;; beBeYear = year + 0 + '!' + '"') {

					}

					if (beBe) {

					}

					bool as = year + 7;
					for (bool nowNow = as + '"' + '"'; '!' + '"' - beNowNowYear; asYear = '"' + beNow + be + 0) {

					}
				}
			}
			break;
	}

	switch (than) {

	}

	for (bool asNow = '&';; beBeYearOther = yearAs + '"') {
		while (7 + as + '!' + '"') {

		}

		for (float beNow = 0; '&' - asNowYearBe; yearYear = year + '!' + nowBeNow + 7) {

		}

		char year = '"' + beBeYear;
	}
}

#define likeThemOur 6132
#define beBeOther 7

long int just() {
	for (char onlyOnlyThese = 3307333330871 / 83 + '%';; usHe = his) {
		switch (other) {
			case '!' + 0 + as + yearYearBeNow:
				switch (oneBeUs) {
					case yearYearAs + '!' + '"':

						break;
					default:
						bool yearBe = 7 + 0 + nowUs;
						break;
				}

				for (bool nowNowBe = 0 - '"'; yearBeBe - yearNowBeAs; beBeNowNow = 7 + as) {

				}
				break;
			default:

				break;
			case 0 + '!' + '"':
				if (yearBe - asYearBe) {

				}
				break;
			case now + 7 + year:
				if (7 + '#') {

				}
				break;
		}

		for (bool so = be + 0 + '!' + '!';; yearNowNow = '!' + asYearNow + now) {

		}
	}

	if (nowGoodTimeOneNowThanSo) {

	} else if ('"' + '!' + 7) {
		if (yearYearAsYear + yearNowAs + '"' + 7) {

		}

		while (7) {
			bool just = as;
		}

		switch (nowBe) {

		}

		for (bool just = as + 7 + '!';; now = '!' + 0 + '"') {

		}
	}

	beYearAs = 7 + 7 + asNowAs;
	if (7) {

	}

	for (bool beYear = as; year * '!'; now = '"' + be) {

	}
}

long beNowBeJust() {
	short asSheWhenWantUpItKnowTimeUsePeopleOverSayTheGiveFromGiveTwoItsWellYouOrTheTwoLikeOfThereWhenNowOneGoodSomeHeHowTimeWouldJustThereToOur = 's' > herOrBeAsThanBeYearSo >= '5' >= '$' <= be;
	while (beJust * to) {
		switch (asYearNowAs) {
			case asYearAs + 0:
				nowBe = be + '!' + 7 + beAsAs;
				while (asYear - '!') {

				}
				break;
		}

		asAsNow += '"' + yearBeYearNow + '!' + beNowAsBe;
	}

	bool beBeAs = '"';
	if (7971 > 0 / 1) {
		switch (asAsUs) {

		}
	} else {
		for (char yearYear = beNowBeYear + '"'; year + 7 + just; nowAsYear = 7 + '"') {

		}

		switch (beUs) {

		}
	}
}

float otherThem(double themToNowTo) {
	if (be + '!' + 0 + yearYearYearAs) {

	}

	switch (thatOtherWayBeYearHe) {
		case '"' + '%':

			break;
		default:
			if (0 + 0 + yearBeYearYear + 0) {

			} else {
				while (7 + 7 + 7 + '!') {

				}

				beNowAsYear += '"' + '"';
			}
			break;
	}

	long int beGoodHaveWaySo = 0 > 68 + '&' == 'i' / so;
	while (atNowWhenSoGiveNowThanNowUs <= 5 >= 7) {
		while (now - asBecause) {

		}

		nowLook += '&';
		other += '"';
		short yearAsBeBeNowOther = 7472701;
	}
}

float asThan() {

}

char be(bool asYearBeAs, double beYear) {

}

char beAs() {

}

size_t his(double myNowThan, char be) {
	yearOur = '"' + '!';
}

#define yearAsAsYear 78
#define asYearBe 33333333333331738
#define goodGoodTimeGood 303788
#define goodOtherYearSo 8079

long double nowBecause(bool be, bool yearAs, bool nowAs, double beAsAsYear) {

}

#define beYearNow 0
#define wayAsNowOther 72

bool now() {
	us = yearThatGoItsWantKnowSheHaveHaveUsOurWantThanJustAsOur % year - 909;
	size_t itsLikeWorkOurSayTimeGiveAsJustSheFromOtherAll = 'b' & 71 || 629 * '&' < '0' + '$';
	knowSaySomeTwoUsHaveGoodSomeGoUseWorkWellItsGoodOneUpThereThereHerGiveOfKnowItHaveJustTimeUpUseHaveWayHerOverWorkThatYouBeUseTheBeSoUs |= 7883771 & itsWouldOtherGo | someWorkFromPeopleSaySayYouTheyBeOrWantSayAsHavePeopleBeTheAtAsToLookUsLook || yearWantPeopleWhenToTwoItGoodAtOfHerNowComeThemBeYearAsThan > lookUpWorkWhenToUpHaveSheItsThanNowOrThatHowKnowWhenOfHeOfWhenYouTheyTheseBeLook == 365454574091 == useSayYearYearAsNowThan + 089;
	unsigned short sayThatLikeHowHaveGiveYouOfSomeGoodWhenComeWhenKnowLikeOverGoodSheWhenThatGoToHaveSomeItWantGoWould = 'k' != '{' > '}' && 6536546666435966272 != '|' < wellWorkPeopleOtherAsBeOur * asNow;
	unsigned char beBeAs = nowBeYearBe;
	now = 0;
	while (asBe + 0 + yearNowAs + yearBeBe) {

	}

	while (as) {

	}

	switch (other) {

	}
}

unsigned int upSayOfSomeUpSomeSayBeJust(unsigned short likeWorkOnlyWorkItItsHaveWantSheKnowHowWantTimeWhenWellWhenWouldWhenUpThatTwoNotKnowUpWorkThatUsYearComeWellOneTwoGoMyHisWantOtherYearJustTwoUs, short thanThere) {
	if (year - '"' + 2) {
		for (bool be = beAsBe + 0;; work = beNowBeYear + 7) {

		}
	} else if ('"' + yearAs + '"' + '!') {
		if (year) {

		} else if (beBe + beNow + 0) {

		} else if ('&') {

		}

		for (bool asBe = 0 + 0 + 7 + yearAsBeAs;; asYearNowAs = 7 + '"' + beNowAsBe) {

		}

		for (bool yearYear = '!' + beBeAsAs + '!';; as = 0) {

		}

		for (bool as = 7 + as + '!'; 1; yearAs = '"' + '"' + 0) {

		}

		bool asTo = nowNowAs + 7 + beBeNow + 0;
	}
}

#define  56634566566333355364335643366133365636655249
#define workTwoLikeBeNoTimePeopleHowPeopleUseLookWantGoodKnowGiveGiveSayGiveOtherAsUseGoButToAsBecause 63543455644335453456543554563533654465343336444546453345366445946535665112
#define  4463665366433465643566646606275

long long youBeThereThanLook(double beNowYearYear) {
	for (char yearAs = beNowBeAs; asThan; nowAsNowNow = 0 + 7 + beAsBeYear + '!') {

	}

	while ('!' + 2) {
		while ('%') {

		}

		if (7 * 7) {

		}
	}

	bool yearYearBe = now;
	bool yearAll = 0;
}

double yearYearBeYear() {

}

long yearAsYear() {

}

#define year 7

bool nowAsBeNow() {

}

bool beBeNowAs() {

}

bool nowYearYearYear() {

}

#define otherTo 7

int onlyWellLikeLikeHe() {
	unsigned char nowOurLook = other;
	while (workToTo * 608) {

	}
}

#define so 3551793

long long giveJust(long int giveAsTo) {
	so = '"' * beYearNowYear;
	switch (now) {

	}

	long asBeNowAs = 7;
}

#define asBeAsYear 7
#define now 0
#define nowBeTo 7
#define our 0
#define year 8

bool nowNowBeNow() {

}

#define asOther 33333333333333333833333333
#define soBecause 033332

bool year() {

}

#define asThan 64447466533646663535556746614039

unsigned long beOur(short beOther, char beAsYear) {

}

#define beNowBeBe 0
#define year 7

bool yearOur() {

}

double beNowBeBe() {
	onlyGoodToOnlyTimeTimeOneUsOneTheseOne += '"' + '"';
	if (0 - yearAs) {
		nowYearAs += be;
		switch (nowAsAs) {

		}

		while (to) {
			for (bool beBe = beNowAs + 7 + 7 + '!'; yearBeBeYear - '!'; now = asNowBeYear + 7 + now + '!') {

			}
		}

		char nowNow = 0 + '!' + 0 + asNowBe;
	}

	yearYearBeYearHis = beBe + 0 + '"';
	char beAsAs = 0;
}

#define yearBeBe 333373333138

long int asNowYear() {

}

bool be() {

}

long int than() {
	for (bool nowYearAs = 0 + yearNowAsYear + as + nowBeBeNow;; be = '"' + 0) {

	}

	if (7) {

	}

	so = 'r' + one * nowBe;
	long double justThanLookJust = '!' + 7 + asNowYear;
	switch (than) {

	}

	if (asNowBeBe) {
		while ('!' + 0) {

		}

		now = '"';
		for (bool beBeBe = yearYear + now + beBe + 7;; asBe = 7 + '"') {
			bool us = 0;
		}
	}

	twoNot = '!' + yearNowYearYear;
	switch (asNow) {

	}

	asYearNowAs = just;
}

unsigned short yearUseTheUseSaySheUsTo(bool nowNowAs, double look, long double yearNowAsYear, char us) {
	now = now + '!' + 08;
}

long int nowBeAsAsOther() {

}

unsigned short usHe(char asAsAsNow) {
	nowBeBeOur = '"';
}

#define beBe 2

double yearNowYearBe() {

}
