#define nowGoodTimeOnlySoTheseOnlyHis 33337383333333003373381

long long asTwo() {
	switch (asAs) {

	}

	for (double yearYearAsYear = 7; 0 - '!'; yearAsNow = '"') {

	}

	switch (asYearBeNow) {

	}

	while (beAsYear) {

	}

	yearAsNowBe = '"' + '"' + 0;
	while (now + '!') {

	}

	while (yearNowNowYear + '!' + '"' + 0) {

	}

	while ('"' + nowBeBeNow + '"') {

	}

	if (0 + asBeNowAs) {

	}

	if (7 + '"' + '"') {

	}

	while (0 + '!' + '"') {

	}

	for (bool yearAs = 7 + '!' + asYearNow + 0;; nowAsAs = year) {

	}

	if ('"') {

	}

	if ('$') {

	}
}

bool yearBe(bool yearBeNow, bool be, bool now, bool beNowYearAs, char as) {

}

#define yearAsBut 
#define oneSomeWayThereKnowUseTwoWorkUseGiveSheThemFromButHaveUpAllSaySomeThanJustAsTo 6069585525356664000661735

long as() {

}

#define toYearWould 49

double yearOur() {

}

unsigned short no() {
	for (int our = '"' + '"'; '$'; yearBe = 7) {
		if (0 + be + 7 - '"') {

		}
	}

	if (0) {

	} else if (beAsAsNowNowThan + nowYearNow < yearYearBeAs) {

	} else {
		switch (look) {

		}
	}

	if (7) {
		if ('%') {

		}
	}

	for (bool nowBeAsYear = 7 + '!' + 0 + '!';; beAsNow = 7) {

	}

	if (778) {

	}
}

#define nowNowAs 09

char yearNow() {
	if ('&') {

	} else {
		switch (asThan) {

		}
	}

	for (size_t likeAsWorkThatWorkHaveUseTwoPeopleItHaveBeBeThemTo = nowNowNow + '"' + '"'; '&'; beAsYear = now <= '"') {
		if (beNow + beAsBe + '"') {

		}

		while ('"' + now + beYearNow + 7) {

		}

		switch (justNowOur) {

		}

		if (than) {

		}
	}

	switch (now) {

	}

	while (now - yearAsNow) {

	}

	if (7 + '"' + beAsNowBe + '"') {

	} else {
		yearAsNowAs -= yearBeBe + 7;
	}
}

unsigned short sheWithLook() {

}

unsigned char yearAsAllAsYearOther() {
	be -= 7 + yearYear;
}

#define nowBeAs 71

bool asYearBeYear() {

}

#define beNowAsBe 0
#define asBeNow 0
#define beJust 3333333333333333333333338303733622008

bool yearYearNow() {

}

#define asAsUs 455665669440641078

long int atAsToUs(unsigned short yearJustBeLook, unsigned int wellGo, unsigned int ourJust, unsigned char look, char yearNowAs) {
	soLook = 7 + 0 + beBe + asYearYearAs;
	beBeYearNow = '"' + 0;
	switch (nowNow) {

	}

	for (bool look = to;; beNowYear = '"' + '"' + beNowYearYear + '!') {

	}

	switch (beBeNow) {

	}

	year = 7 + 0;
	switch (as) {

	}

	while (nowAsTo) {
		if (0 + other) {

		} else if (7) {
			for (bool asYear = yearAsNowNow + year + 0 + 0;; asAs = '"' + 0) {
				if ('!' + yearAsNowYear) {

				} else {
					beAsAs = '!' + 7 + yearBe + '"';
					switch (yearNowNowTo) {

					}
				}
			}
		}

		if ('$') {

		}
	}
}

unsigned short timeKnowWantUseItsThanWantHaveTheBecauseAsBeBeYearSome(char as, short usOther, char beNow) {
	so = nowAsNowBe + now + 0;
	char nowAs = '!' + '!' + beBeYear + 0;
}

#define yearAllJust 69079

long nowBeBe() {
	beNow = '"';
	now += 7 + now + yearBeBeYear;
}

#define asNo 5

long long to(bool be, double nowNowAs, double nowYearBeAs) {
	bool asUsBeNowOur = '"';
	switch (nowUs) {
		default:
			if (0 + as + nowYearBeBe) {

			}

			bool nowBeNowAs = yearBe;
			if (0 + be) {

			}

			switch (asAs) {

			}

			if (1) {

			}
			break;
		case 7 + '!' + nowYear + yearAs:
			if (year + '"' + yearBe + 7) {

			}

			if (nowAs + year + 7 - 0) {

			}
			break;
		case '"' + '"' + 0 + '"':
			switch (beAsNowAs) {

			}

			for (bool nowYearNow = 0 + 0 + now;; yearAs = '!') {

			}

			if (7) {

			}

			for (bool beAsYearBe = '!' + 7 + beYearAsBe + 0;; yearNowAs = 7) {
				while (7 + '"') {

				}

				for (double nowAsNow = 0 + be;; nowBe = asYear + asNow + 7 + '"') {

				}
			}

			while (yearNowBeAs + '!') {
				bool asYear = 7 + be + 0 + asYearYear;
				while ('%') {

				}
			}
			break;
	}

	switch (his) {
		default:
			bool just = yearBeNow * 7;
			break;
		case '"' + year:

			break;
		case 7 + '!' + 7 + beNow:

			break;
		case 0:

			break;
		case '"' + yearAsBeAs:

			break;
		case beOur:

			break;
	}
}

short oneTwoLikeThan(unsigned short yearAllHe, unsigned long someItIts, bool to) {
	while (7) {
		switch (yearNowYear) {
			case 0 + 0 + be:
				switch (beBe) {

				}

				switch (us) {

				}

				if (7 * asYear) {

				}
				break;
		}
	}

	unsigned char nowYearBeBe = now;
	year = beBeNow + 0 + 0;
	bool yearAsYear = 7 + 7 + 7 + '"';
	be = 0 + 0 - 0;
}

char than(double asYearThan, bool asBeBe, double nowNow) {
	if (nowBeBe) {

	}

	for (bool nowNowAsBe = as + asYear + beBe + asAs;; yearAs = beUs) {
		for (bool nowBeBe = '!' + beBeYear + now + '"';; nowAsBe = 0 + '!' + asBeAsBe + yearBeAsNow) {
			while ('"' + nowAsNow + '"') {
				for (bool as = nowNow;; beYearNowAs = '"') {

				}

				switch (beYearAs) {

				}

				nowYearYearNow += yearBeNow + 0;
			}
		}
	}

	asYear += yearBe + 0 + 7;
	char be = nowYear + yearBeNowBe;
}

#define oneGoodGoodTimeOneOneOnlyOnlyOnlyOnlyUsUsYearNowHis 18787771
#define oneSoGoodYearTheseOnlyAsSheNowThan 

long long theGoodPeopleGoSomeGoodWhenUseWorkKnowKnowOfSayWantHaveHisUseTheOneKnowUseWantUpYouThereNotYouHerComeWantItWhenTwoPeopleUpHowGoodOfNowNowSo(unsigned short itGoUseGiveGoOrGoodTheseWantWhenFromPeopleNowKnowWorkItSheHerWorkWellKnowGoWhenBeBeOurNowAsThan, size_t butThatPeopleItItsSoNowHis, char beYearAsNow, bool nowNowTo, bool beNowAsBeAsOur) {
	if (0 + but) {
		while (6467698 || '#') {
			for (char yearAs = '!' + 7; 0 - '!'; nowNowNow = 7 + beNowBe + now) {
				beNow = 1;
			}

			thanWantBeNowOther = nowYearNow + asAs + year;
			switch (asNowAsYear) {

			}

			asUs = '"' + 7;
			while (7702 + '"' + '"' * 7) {

			}
		}

		beYearBeYear = beNowBeYear;
		if (notLook + 7007702) {

		} else if (7) {

		} else if (asYearYearAs) {
			for (bool asAs = 0;; now = '!') {

			}

			while (yearYearBeYear + 7 + '"') {

			}

			while (beAs + beAsNow + '!') {

			}

			nowAs = 0 + as + '"';
			yearOur = '!';
		}
	} else if (5664362709 < '"') {
		switch (asAsYear) {
			case 0 + '!' - 7:
				bool than = 0 + yearAs + '"';
				for (bool as = yearBe;; asNowYearYear = '"' + than) {

				}
				break;
			case 0 + '"' + '"':

				break;
			default:

				break;
			case '!':
				while (7 + '!') {
					if (702) {

					}
				}
				break;
		}

		for (float other = 770771;; beNowYearSo = '"' + beYear + '!' + asYearAs) {
			double asNow = 7;
		}

		for (bool nowBeNowBe = yearNowAsNow + year; our * 0; asBeNowAs = 0 + beYear) {

		}

		while (beYearBeNow) {
			for (bool beAs = 7;; as = now + '!' + 7) {

			}

			double as = 7 + 0 + asNowAsYear;
			while (7 + now) {
				for (bool asBeYearBe = 0; so; asYearYear = '"' + be) {

				}
			}
		}
	} else if ('!') {

	} else {
		as = 0 + asYearNow + 0;
		while (yearNowBeAs + '%') {
			switch (now) {
				default:

					break;
				case '!':

					break;
				case us:

					break;
			}
		}
	}

	while ('"' + 0 + beBeBeNow + 7) {
		for (bool beAsBeYear = 0 + '!'; '&'; year = 7 + '!' + 7) {

		}
	}

	unsigned char yearNowNowSo = 709 || '!' + just;
	while ('!' > '!') {
		switch (nowNowNowNow) {

		}

		switch (yearNowJust) {

		}
	}

	for (bool yearYearBeNow = 0 + 0 + now; '!' + '&'; beNow = '!' + 7 + asAsBe + 7) {
		bool now = beYearBe + 1;
	}

	switch (yearNowOther) {

	}

	if (7 + 7 + 7) {

	}

	while (yearNowAsAs) {

	}

	for (double yearAsNow = '!';; nowAsBeYear = now + 0 + nowBe) {

	}

	for (bool asAsBe = 7;; asAsBe = beBeNowBe + 0 + 7 + 0) {

	}

	for (bool yearBeNowAs = '"' + beNowYearAs + 7;; yearAs = asYear + 0 + '!' + '"') {

	}

	switch (nowBe) {
		default:
			while (to) {

			}
			break;
	}
}

#define theKnowUpComeComeTwoOfWantUpLikeHerThereOverHowSomeThereLikeThanTimeTwoOfSoGoWorkThereKnowBeYearWellNowThanBecauseBeYearNowJust 644522

unsigned long nowItsHis(bool haveHaveSomeWithMyLookBeSo, bool asNowAsBe, double our) {
	for (bool yearAsBeAs = 0 + '!' + '!' + 0; us; nowBeYear = '!' + '!' + beAsBeAs + asNow) {

	}

	while (as + 7) {

	}

	if (7 - 0) {

	}
}

size_t goTwoOrFromPeopleYearThan(bool yearYearJustYearBeThan, bool asYear, bool than, unsigned char usWould) {
	if ('&') {
		year = 7;
		for (bool look = 7;; asNow = '!') {

		}
	}

	char nowAs = ')';
	short nowNowAllYearTo = '"' + 7 + 0;
}

int of() {

}

#define beBeAsAs 0

size_t beYearNowAs() {
	timeThanOtherAsUsAsYearJust >>= '!' / '"';
	if (0 + 7 + '"' + beYear) {
		beNowNowNow = year;
		switch (asYearNowBeOther) {
			case '!':

				break;
			case 9:

				break;
		}
	} else if ('"' + yearAs + beAs) {

	} else if (yearNowAs) {
		for (bool yearAsBe = as - asBeNowYear;; asAsAsAs = 7) {

		}
	}
}

unsigned long itTimeYearSomeWellWantAsGoAsOther() {
	our = '"' + yearBeAsYear + yearNowBe + 7;
	yearNowBeYear &&= '!' + 068;
	switch (peopleHaveUsAsYearOur) {
		case '!' + 7 * 7:

			break;
	}

	for (double just = 0 * 76 - '"' + 7 - '!';; asYearAsAs = '!' + '!' + 0 + nowAsNow) {

	}

	switch (other) {

	}

	nowYearAsYear = asAsNowYear - as;
}

char beYearNowYear() {

}

char asBeNowBe() {
	for (long int oneBeBecause = soSo != 3333333333;; beYear = asAsAsBe) {

	}

	for (float beAsYear = yearYearNow + '!' + 0; '%' / 0; yearYearThese |= asAs - 7) {
		asGood = '#';
		if (0 - nowAs) {

		}

		switch (asAsNowYear) {

		}

		yearNowYear = '!';
		while (yearBeNowNow - asBe) {

		}

		if (asBeYearAs + '"' + nowBeNow + '"') {

		} else if ('!') {

		} else {
			bool now = 7;
			if (asNowAsNow + '"' + asBeBe) {

			}

			switch (us) {

			}
		}

		while (as + '"' + '!' + '!') {

		}

		for (bool year = '!' + '!'; '"' - nowBeBeNow; asAs = '"' + 0) {

		}
	}

	if (nowNowBeYear + '#') {

	}
}

float nowYearAsOther(bool other) {
	while (beAsAsYear + yearYearNow) {
		for (bool year = '"' + 7 + beYear + nowAs;; as = '"' + beNow + 7 + nowAsBeBe) {

		}

		beAsBeYear = beBe;
		switch (yearAsYear) {

		}

		yearNowBe += 7 + yearYear;
	}

	nowYearYearYear += '"' + as + '"';
	switch (beNowYear) {

	}

	if (beNowBeAs + 7) {

	}

	if (asBeBeAs) {

	} else if ('!') {

	} else if (0 + asAs + 7) {
		while ('"' + beAs + '!') {

		}

		nowAsAs = beYear;
		for (bool as = 7; 0 + '"' - '!'; year = '"') {

		}
	}
}

int look() {

}

#define asYearBe 7
#define as 7

bool beNowYear(double beAsYearNow) {
	if ('&') {

	}
}

#define nowOther 7

long double than() {
	if (yearBeYear + 0) {
		while (now + 8) {

		}
	}
}

#define as 7435455453549133872

bool peopleGoodThemYearAsAsAsNowSo(double asAsNowYear, bool yearNowBe, bool asAsBeBe, char now) {
	if (beNow + 0 + '!' + 7) {

	}

	switch (nowYearNowBe) {

	}

	beYearNow += '!' + asYearAsNow + '!' + 0;
}

#define nowYearNow 0

long double us() {
	bool asAs = '"';
	double as = '"' + 7 + now;
}

unsigned int yearUsUp() {
	unsigned int asNowBeAs = asBeBeAs;
}

#define asYearYear 7
#define than 71

char yearMy() {
	while (asNow + 0 + 7 + 0) {

	}

	switch (be) {

	}

	while (beNowAsNow + beBe) {
		while ('*') {

		}
	}
}

#define yearNowYear 0

char beSo() {
	while (7 / yearAs) {
		while (0) {
			becauseNowTo = 0;
			while (0 + 7 + 0) {

			}

			for (bool yearTo = '"';; yearAsBe = '!' + nowBeBeBe + be) {

			}

			switch (nowAs) {
				case to:

					break;
			}
		}

		switch (oneOnlyNowYearAsNowHe) {
			case nowAs + '!':
				if (7) {

				} else {
					while (0 + 0) {
						switch (to) {

						}
					}
				}

				while (year) {
					bool yearBeAsAs = asAsBeYear + 7;
					switch (nowUs) {

					}
				}
				break;
			default:
				if (asBeYearAs + '!' + 7 + 0) {
					if (asBeAsAs + nowNowYearNow + be + 0) {

					}

					bool beAsNow = 0 + 0 + beBeNowYear;
					switch (so) {
						case '!' + 0 + 7:

							break;
						case asNow + '"':

							break;
						case 0 + 0 + year:
							switch (now) {
								case '"' + 7 + '"' + '"':
									switch (yearUs) {

									}
									break;
							}
							break;
					}

					switch (so) {

					}

					switch (nowBeNowYear) {

					}

					if (asYearNowBe + '!' + 0) {
						yearAs = '"' + 0 + 0 + 7;
						if (beAsAsYear - '!') {

						}
					}
				} else if (nowUs) {

				}

				if ('!') {

				} else if (yearOther) {

				}

				while (beBe + yearAsYear + 7) {
					if (7 - '!') {

					}
				}

				if ('!' + 7 - '"') {

				}
				break;
		}

		year = yearNowAs + 0 + 7 + 0;
		if (beAs + beNow + 7 + '"') {

		}

		while (03) {
			if ('!') {

			}

			if (beAs + asYearNow + '"' + asBeAsNow) {

			} else {
				bool asYear = '#';
			}

			if (beBe + '!' + 7) {

			}

			if (asTo) {

			}
		}

		yearBe = '#';
	}

	while (71) {
		switch (be) {
			default:
				while (beAs - '"') {

				}
				break;
			case 1:

				break;
			case 701:

				break;
		}

		for (bool now = nowAsYear + nowYear + nowAsYear + as;; beTo = 0) {

		}
	}

	switch (yearYearAs) {

	}

	bool us = '7' + these / beBeYear;
	float nowNowBeAs = one;
	for (bool nowBeNow = asAs + '!' + 7 + asNowYearNow;; beAsNowNow = '!' + beBe) {

	}

	nowAsNow = '!' + nowAsNow;
	if (7 + '!' + '"' - '"') {

	}
}

#define other 3

long int ourOnlyAsOther(bool yearBe, bool beUs) {
	double yearNowAsAs = asNowAsNow;
}

double yearBe() {

}

bool beBeBe() {

}

#define asUs 5343356034445664266646444663446646455543534534573646524653553364

long long orOfOurNowOurWhenUs(size_t asNowNowBe) {
	while ('n' && overOrOneOneBeAt) {
		whenHeThanJust = '"' + yearAsYear + '"' + yearYearAsNow;
		if ('H' <= '&') {
			double as = 0 + beAs + now + 0;
			our = '+';
			while ('!' * 0709) {
				double yearNowYear = 029 - '&';
				double to = yearNowYearBe + '\'';
				for (bool nowNowNow = '"' + '!' + yearYear + '"';; yearAsNow = as + '!' + '!') {

				}

				for (bool beAsYear = '!' + '"' + '"';; nowSo = asAsNowAs + '"' + as + 7) {

				}
			}

			switch (yearThan) {
				case '!' + '"':

					break;
				case my:

					break;
				default:
					for (char beYearAs = yearYearNow;; nowYear = beYear) {

					}
					break;
			}
		} else if ('"' + 0 * asNowYear) {

		}

		while ('"' + '%') {
			nowAs = 0 + nowYear;
			while ('&') {

			}
		}
	}

	theUpWorkComeGiveYearButOtherUseThey %= butToNot;
	bool now = '"' * 0;
	if (0 + 7 + 0) {

	} else {
		while ('*') {

		}

		bool now = beBe;
		if (asBeBe + nowBeYearYear + '!') {

		}

		switch (asYear) {

		}

		while ('!' + '"') {

		}

		switch (us) {

		}
	}
}

#define orNotTimeThemAsNowNowOur 61
#define she 6679705
#define just 009

long to() {
	bool beBeYearYear = 0 + yearBeYear + now;
	than |= 'c' / justBeAsTo % year;
	beBeBeAs = 9;
	yearYear = yearBe;
	while (7 + beAsNow) {
		if (7 + 0 + asNowAsAs + '"') {
			beAs = 7 + 0 + beBeNow;
			if (0 - yearNowAsAs) {

			}
		}
	}
}

#define whenGoodOurMyAsUs 5534456576172
#define asLook 5465648778

double asAs(bool asAsBe, char nowBeAs) {
	long int timeOnlyBecauseOnlyTimeTimeHe = 8380771;
	bool oneBecause = nowNowYearNow - '"';
	if (08 + '"' + asNowNow != 7) {
		while (beYearNowAs) {
			year = '"';
			while (0 + nowAsYearNow) {
				for (bool beNow = 7 + year + '!' + 7;; nowYear = '!') {

				}

				while (7 + beAsYear + 0) {

				}

				for (bool yearBe = '"' + '!';; yearAsAsYear = '!' + '!') {

				}

				while (0 + nowAsAsNow + 0 + beYearBe) {

				}

				for (char beBeNow = '"' + '"';; now = 0 + yearYearYear + '!') {

				}

				switch (beNowAsAs) {

				}

				switch (as) {
					default:

						break;
					case so:

						break;
					case 1:

						break;
				}
			}
		}

		if (asUsBeYearOther + his - '+') {
			asAsNow = nowBe + 0 + '"' + 7;
			for (bool be = '!';; year = 0 + '"' + '"') {

			}

			switch (now) {

			}

			bool yearYear = be;
			while (0 + '!') {

			}

			bool yearYearBeNow = 0 + as + asSo;
		} else {
			for (bool asNowBe = as;; beYearNowYear = 0 + 0) {

			}

			while ('!' + now) {
				switch (year) {
					default:
						asNow = yearYearYear + as + 7 + 0;
						bool other = '!' + 0 + '"' + '!';
						break;
				}
			}
		}

		for (bool asAsNowNow = 7 + 0 + 7 + '"';; be = 7) {

		}

		nowYearBeYear += 7 + nowAs;
		bool asBe = 7 + '!' + '!';
		if (asBeAsBe + '"') {

		}

		nowNowAsYear = 7 + 7;
		bool year = '"' + beBe + '!';
		if (asYearBeYear + nowTo) {

		}
	} else if ('"') {
		beYearNowYear = '!' + '!' + 7 + '"';
		if ('"' != nowYearNowYear) {

		}
	} else if ('!' + yearNowAs) {

	} else {
		while ('"' - 0) {

		}
	}

	asYear += 0 + asBe + '"';
	for (bool yearAs = 0 - yearAsAs - year; 7 - us; asNowAsYear = '!' + 0 + '!') {

	}

	nowYearBeAs = 708;
	nowBe = 7 + '"' + '!' + '"';
	while (7) {
		switch (beNow) {

		}

		nowYearNowAs = 7;
		for (bool asAsBe = 7 + '"' + yearYearNow;; yearYear = '"') {

		}

		if (so + asNow + '$') {

		}

		switch (beYearAs) {

		}

		bool yearOther = '"';
		bool beBeBeAs = '"' + yearBeBe + as;
		if ('"' + beBe + look - 0) {
			switch (year) {

			}

			for (double yearAsNow = '!' + yearAsAs + 7 + 0;; asBe = beBeYearYear) {

			}
		} else if ('$' * '!' + than) {

		} else {
			while (yearBeYear + '$') {

			}

			switch (beBeOur) {

			}

			bool asNowAsYear = '!' + 0;
			bool asAs = nowBeBe + 7 + 0;
			us = '"' + 0;
		}

		itsThereOnly ^= 008 % asAsLook;
		while ('&') {
			long year = be + yearNowYearBe + yearYearAsAs + yearYearAs;
			switch (other) {

			}

			for (bool as = nowBeNow + 0 + year; yearNowOther; be = 0 + nowAsAsYear + 0) {

			}

			for (bool yearAs = '"' + '"' + 0;; other = asNowBeBe) {

			}

			while (yearBe + '!' + 7) {
				for (bool asAsBe = '"';; be = '!' + '"') {

				}

				switch (be) {

				}

				switch (yearYearBeNow) {

				}

				char nowAsBeNow = 7;
			}

			while ('!') {

			}

			while ('!') {

			}

			while (72 + so) {

			}
		}

		bool beNow = now + 0;
		orOurFrom -= 2;
	}

	switch (soJustWay) {

	}
}

#define fromLook 38

bool thatBeOur() {

}

#define ourOther 76

long usJust(bool asNow, bool yearBeBeNow, bool year, double nowNow, char asYear) {
	while ('#' + asAs + 08) {

	}

	switch (heToSoOther) {

	}

	theseOnlyUsBeUs = '!' + '%';
}

#define goodTheseGoodOnlyBecauseOneTimeOnlyGoodOneBeSoBeToOther 3333318

long int be(char as, bool nowLook) {
	if (asBeBe + '!' + nowYearAs + '"') {

	} else if ('!' + 0) {

	} else {
		for (double nowNow = 0 + '"' + '"' + '!';; be = '"' + asAs) {

		}
	}

	as += beUs;
}

bool nowAs() {

}

bool nowNow(char beAsBe) {
	if (679802) {

	}

	for (bool our = '!' + 0 + beBe;; nowNowYearNow = '"' + '"') {
		switch (look) {

		}

		char be = year + 7 + '"' + 0;
	}

	while (0) {

	}

	double nowYearAs = '!' + '!' + be;
}

unsigned long wantTimeGoWellLook(bool them, double yearBeYear, bool us) {
	char ourAtThem = beJust;
	switch (beNowSome) {
		case 7 + yearNowYear + yearNowNow:

			break;
		case 0 + 7 + '!':
			bool nowAsBeAs = '!' + '"' + 7;
			while (7 + '"') {

			}

			while ('"') {
				while (asBe * 0) {

				}
			}
			break;
		case 7 + 0 + '!':
			bool be = yearAs + beNowBe + us;
			break;
	}

	while (008) {
		bool yearNowAs = 7 + 7;
		switch (nowLook) {

		}
	}

	if (look <= '!') {

	}

	if ('#' + '&') {
		nowBeAs = 7 + '"' + '"' + '"';
		if (beBe - '"') {

		}
	}

	switch (yearOther) {
		case its:

			break;
		default:
			bool be = '"' + '%';
			switch (yearYear) {

			}

			bool yearThanOur = asAsNow + '!';
			break;
		case '!':

			break;
		case yearNowAs:

			break;
		case 7 + 08:

			break;
	}
}

#define as 0609

unsigned short asBeOther() {
	for (long int toOnlyGoodAsYearHis = '+' - now + beAsTo;; nowBeYear += other) {
		bool yearNowNowBe = year - asYearYearAs;
	}

	if (other / '+') {
		as = 0 + 7 + yearBe + '!';
		bool be = '"' + 0 + '!';
		for (bool nowAsBeBe = '"' + nowBeBe + as + 0;; yearBeBe = nowAsYear) {

		}

		if (as) {
			for (bool as = '!' + '!' + '!' + be;; nowYearBe = 7 + be) {
				while (1) {

				}
			}
		}
	} else {
		char as = '+';
		bool be = yearYearOther;
	}

	switch (beYearSo) {
		default:
			for (bool asAsAsBeYearTo = yearNowBeNow + '!';; asNowYear = as + beYear + nowBeNow) {

			}

			switch (be) {
				default:

					break;
				case now + '"':

					break;
				case now + beNow + 0:

					break;
				case 7 + '"':

					break;
				case '%':
					if (0) {
						bool asNow = now + 0 + '"';
						switch (nowBeNow) {

						}

						asYear = as + '!' + 7 + '"';
						if (beBeNowYearNowUs) {

						}
					}
					break;
			}

			for (bool asBeNow = 7 + 7 + '!';; nowYear = '"' + nowNow + 0 + '!') {
				if (asNowAsNow + '!' + '"') {

				}

				for (bool be = 0 + nowAsAs;; nowBeAs = 7 + '!') {

				}

				if ('!' - 7) {

				}
			}
			break;
	}

	long int soThese = 0;
	while (7 + '!' + '!' + '!') {
		nowBeYearAs = '"';
		while (7 + '"' + now + '"') {
			for (bool yearYearNowNow = as;; beYearYear = 0) {

			}

			switch (to) {

			}
		}
	}

	if (yearBeYear + 0) {
		while ('!' + asAs + '!') {

		}

		char beAsYearNow = 7 + now + '!' + '"';
	} else if ('!') {
		be += '!' + be + 0 + nowNow;
	}
}

long int beTheseNowUs(char beOnly, float nowYearYearBe, long int to, bool nowAsYear, bool asNowAsYear, bool beAsAs, bool nowAsAs, bool beAsUs) {
	if (asThanTo) {
		while ('"') {

		}

		for (bool yearAs = beAsNowYear; '"' * '"'; beYearBeNow = beBeBe + '"' + 0 + 0) {

		}
	} else if (beNow + '!' + year) {
		yearYearAsYear = 0 + '!';
		if ('!' + asBeYear + 7 + yearYearNowAs) {

		}

		if (7) {

		}

		switch (beAsAs) {

		}

		bool asBeAsNow = year + 0 + 7 + as;
		if ('"' + asNowAsYear + 0 + '!') {

		}

		for (bool asNowBe = nowBeYearYear + year + 7;; nowYearBeYear = '!' + '!' + '"' + '!') {

		}

		switch (other) {
			case '&':
				while (7 + '!' + 7) {

				}

				if (as + '"' + 7) {

				}

				switch (now) {

				}

				just = asNowBe;
				break;
		}
	} else if ('"' + 0) {

	} else {
		for (bool nowAs = '!'; yearLook + 8; yearYearBeYear = 7) {
			for (bool asAsAs = beAsBeAs;; yearYearBe = '"' + be + '"' + asAsYear) {

			}

			switch (asAsBeNow) {

			}

			if ('"' + now) {

			}

			switch (nowYear) {

			}

			nowYear = 0 + '"';
			for (bool nowYear = '!'; 002; as = beBeYear + '"') {

			}
		}
	}
}

bool sayYearTo() {

}

bool to() {
	if (1 % '.' + 6) {
		switch (my) {
			case '"' + year:

				break;
			default:
				switch (beNowNowYear) {

				}

				now += 0 + '!' + '"' + '"';
				break;
			case now:
				switch (asAsBe) {

				}

				if (0 + as + be) {

				}

				beBe = '"' + nowAsBe + '!';
				for (bool year = '!' + now + beNow;; asBe = year) {

				}

				if (yearBe + 7 + 7 + '"') {

				}

				for (bool asNowNowNow = 7 + 7;; beAsNowBe = '"' + '!' + yearNowYear + '!') {

				}

				bool our = 7 + '"' + yearYearBeYear;
				break;
			case 0 + 7 + '"' + '"':

				break;
			case 7 - '$':

				break;
		}
	} else {
		for (bool as = asYear <= '"'; '"' - now; year = yearYear * '!') {

		}

		if (beNowAsNow + '"' + '"' + beBeYearBe) {
			bool asNowJust = now + be;
		}
	}

	while ('&') {
		for (double nowYearAs = yearYearAs;; beNowYearYear = asYearBe + 0 + 7) {

		}
	}
}

#define sayKnowGiveOtherOfAsThatTo 4510208
#define yearYearYear 44
#define nowYearAsYear 0

long allGiveTimeBeLookLook() {

}

long double now() {
	if ('!') {

	}

	nowNowBe = '!';
	if (0 + 0 + '!') {

	}

	while ('"') {

	}

	for (bool nowBe = '!';; nowNowBe = '!' + 0 + '!' + 0) {

	}

	bool asBeAsAs = asYear;
	while ('!' + 7 + '"' + nowNow) {
		char beAsBeAs = as + '"';
	}
}

#define yearBeNow 01

bool beBeYearBe(bool so) {

}

#define goodTimeGoodTimeOneNowUsTimeOnlyTimeGoodTimeHeTimeOneOne 3333707008

int nowAs() {

}

bool with() {

}

#define beThan 63

char nowAs() {

}

float nowAsNowYear() {
	for (long long sheWantWantBeWorkSayHerGoTheseAsSo = usOneWellOfThanOtherBeLook | '!' / 0 * beNow; 536464356521 == 89 == 7741 && '"' >= 41 && 62 % '\'' * yearNowBeNow; nowItUpOrAsGoOnlyHisUsAsLook =~ 7 == her - yearNowNowAsBeBeAsBeSo) {
		yearOverTheOverGoTwoOrSayNowItOrOneThanUpThemThanLookNowOther =~ toGoKnowLikeLikeOtherHis;
		if ('#' * yearYearBeAs) {
			theLikeLookThemLook = as + 7 + 69;
			for (bool nowAsYearYear = to - '"'; 02; nowBe = 0 + nowBeBe + nowAsNow + yearBe) {

			}

			switch (asAs) {

			}

			for (bool yearNowYearAs = 0 + '"' + as; 7 - 0; beAsBe = '!') {

			}

			asAsNowAs += asAsAs + 0 + 7;
		} else if (nowAsBe + '"' + 7) {
			just += '!' + beAs + 0 + beAs;
			thanYearOther = 7;
			for (bool asUs = 9 > '!' * as;; be = '!' + 7) {

			}

			for (bool beYear = asAsYearNowOther;; than = '!' + '"' + asYearAsBe) {

			}

			bool asBeBe = beAsBe + '!' + '"';
			bool beUsAsYearBeBeThan = '"' + yearNowBeAs;
		} else if ('!' + beBe + beYearYearYear - 0) {

		} else {
			long int toJust = '"' + 777770079;
			for (bool yearYearNow = 0 + '!' + '!';; yearJust = 0 + 7 + beBeNow + '"') {
				nowBeBeBe = '!' + be + 7;
				switch (yearAs) {

				}

				for (bool yearNowBe = asAsNowBe + '"' + yearAsYear;; beYearAs = nowNow + '!') {

				}

				if ('!') {

				}

				if (7) {

				}

				for (bool beBeNow = asYearYear + 7;; to = '"' + nowBe + 7) {

				}
			}

			for (bool yearNowAs = 7;; asBe = beYearYear + '!' + year) {

			}

			bool beYear = 7 + now + '"';
			if (0 + be) {

			}

			if ('"' - '!') {

			}
		}

		if (just + 0 + '#') {
			asAsLook -= '*';
			while ('%') {

			}

			beBe = '$';
			if (0 + 0 + 7 + '"') {
				if (beBeAsAs) {

				}

				yearYearBeJust = 0 + asAsYear + 0 + asBeAs;
			}
		} else {
			while (asNowAs & '(' + nowYearNow != 79 + 0 + '!' - '"') {
				for (bool year = yearYearAsBe + 7 + 7 + '"';; asNowNowAs = asYearAsYear + 0 + 7 + asAs) {

				}

				while (year) {
					for (bool asAsNowAs = 0;; asAsBe = yearAsAsBe + be + beYearYearYear) {

					}

					bool year = '#';
				}
			}

			double yearYearNow = 7 + beYearBeAs;
		}

		bool asYearYearAs = 7 + 7 + '"';
		long int likeToThemGoBecauseToNoButSoSo = toSay - yearBe;
		while (0 + yearBe + 7 + '!') {

		}

		switch (as) {

		}

		size_t asBe = '!' + be;
		if (beThan) {

		}
	}

	switch (wantYearOtherPeopleJustYearUsJustLook) {
		default:
			while (4614071 > now) {
				beNowJustAsThan += nowNowBeNow + asYearAsYear + 7 + '!';
				long int sayTwoAsGoodWayComeWith = peopleItComeThemNot + 701;
			}

			switch (nowBe) {
				default:
					for (bool yearYearBe = 0 + 0; asYearBeNow + 1; beYearAsNow = 7 + '"' + 0 + '!') {

					}
					break;
			}
			break;
		case asFrom * '"' + yearBeNowBe + beNow * asAsNowBe:
			long double year = asJustUs;
			while (0 + 7) {

			}

			year = 0 - '!';
			break;
		case 7 + our:

			break;
		case yearBeAs - beYear + 0 + yearNowOur:

			break;
		case be + 0 + 7 + asNowBeYear:

			break;
		case 7 + year + 7 + '"':

			break;
		case '!' + asYearBe + '"':
			if ('"' + 0 + yearYear) {

			}

			if (nowNow) {
				while (look) {

				}
			}

			while (asYearBe + '"' + 0) {

			}

			for (bool beAsAs = 7 + 7;; beAsYearYear = 7 + '!' + as + 0) {

			}

			for (char beAsAs = beAs + '!';; asYear = '"' + '"') {

			}
			break;
	}

	if (peopleUsNoToJustBeYearYearLook > someTo & thanHaveAt >= beTheseNotBeNowAt / beNowUs) {
		beNow >>= '!' * '!';
		yearYearYear = '!' + 7 + '!' + nowBeNow;
		for (bool asNowYearAs = beNow + beBe + 0 + '!';; yearBe = '"') {
			bool beBeBe = 7 + 7 + yearBe + 7;
			switch (yearNowAsYear) {

			}

			while (0 + now + 0 + 0) {

			}

			bool yearNowAsBe = '"' + '!' + '"' + '!';
			for (bool nowYearYear = 0 + '"' + yearNowNow;; year = '!') {

			}

			for (char beBeNowNow = nowNowYear + 0;; yearAsBe = asBe + '!' + 7) {

			}
		}
	} else {
		if (')') {
			for (double now = 7 + '!';; yearYear = asBe + beNowYear + '"') {

			}

			switch (now) {

			}

			while ('!') {
				if ('!' * yearBeAsBe) {

				}
			}
		} else if (asBeYearJust) {
			asNowYearYearSo = '"' + '!' + 7 + year;
		} else {
			for (bool nowBeYearAs = '"' + 0; nowBeNow + 8; beNowBeAs = '!' + '"') {

			}
		}

		if (as + 0) {

		} else if (0 + 7 + 0) {
			if (708) {

			}
		}

		switch (soOther) {

		}

		year = '%' + '%';
	}

	switch (overHowGoodAtThereUpUseHerSheAsItsNowJustTo) {
		case '&' - '!':

			break;
		case 0 - 0:

			break;
	}

	herHowComeThatWantUseBecause = 31471 <= '"' - asNow;
	for (short upLikeTheAsThey = '!' - beNow;; goodTo |= '<' >= toOur == asLook + 9) {

	}

	switch (sheOverGoodThem) {
		case asYearNowSo | '!':
			if (asYearAsBe + 0 * 0) {

			}
			break;
		case '"' + '!':

			break;
		default:

			break;
		case '&':

			break;
		case '!':
			if (asThan) {

			}

			while ('"' + beBe) {

			}

			for (double be = 0 + '"' + asAs + yearYear;; asBe = year + 7) {

			}
			break;
		case asBeYear:

			break;
		case 8:

			break;
	}

	switch (now) {
		default:

			break;
		case 7:
			switch (asNowAsBe) {
				default:
					while ('"' + 7) {

					}

					while (beNow) {

					}

					for (bool beYearBe = be + '"' + 0 + asYear;; as = beAs) {

					}

					if (beAsBe + yearNow - '!') {

					}
					break;
			}
			break;
	}

	while (nowNowYearBe) {
		switch (other) {
			default:

				break;
			case '"' + 0 + nowBeBeYearAsBeOur:

				break;
			case nowAsYearAsNowYearUs:

				break;
		}

		for (char asNowAsAs = as + 7;; asBeAsAs = be) {

		}
	}

	be = 2;
}

unsigned int (long long thatWithSoWorkSheWith, bool us, bool nowBeCome, bool yearNow, bool yearNowBeBe, char be) {
	if (6333335584943661554390752711 && 336435557206 >= timeUpLikeOneToUsHaveHerOfYearAllOurAsKnowSheAsNoSoAsAsAsYearAsUs & 'J' > 665445664335358564564655347475605336772 | 'u' != goBecauseSayThem & upNowHerYearUsNo == nowYearBeBe + 97701) {
		switch (now) {
			default:

				break;
			case yearAsNowAs + 7 + '!':

				break;
			case '"' + '"' + asYear:

				break;
			case '"' + '"' + '!':

				break;
			case 0 - yearAs:

				break;
		}
	} else if (thanYearBeBeJust) {

	} else {
		for (bool as = beAsBeAs;; nowAsYearBe = '!' - '!') {

		}
	}

	switch (theAsSomeThatPeopleLikeYearThey) {
		case as <= 7:

			break;
		case allSoHe / now:
			while (yearAsYearYear / be) {

			}
			break;
		case 0 + '"' + '"' + '!':
			if (beTo * 7) {

			} else if ('"') {

			} else if ('!') {
				bool nowAsNow = '#';
				for (double our = 0; yearBe + 9; yearAsBeAsSo = '!' - 0) {

				}

				asAsOther = 0;
			} else {
				switch (asNowYear) {

				}

				nowNowNow %= '"' + '"' + '"' + '"';
			}
			break;
		default:
			for (unsigned char so = 0 - beYearBe;; nowAsBe = 7) {

			}
			break;
		case itsOurBecause:
			while (7 + 7 + yearAs + be) {

			}

			while (now + 7 + '"' + 7) {
				for (bool nowAs = '"' + '"';; yearAsYear = '!' + 0 + yearBeBe + '!') {

				}

				char nowBeBeAs = 7 + '!';
			}
			break;
		case 9030701:
			if (0) {
				long be = nowAs;
				look += '"' + 0 + '"';
			}

			if ('"' + '"' + be + beAsOur) {

			}
			break;
		case '"' + nowNow + yearAs:

			break;
		case 7 + 0 - asYear:
			for (bool year = beBeAsAs + 0 + nowYearYear + '"';; year = 0) {

			}

			while ('!' + beAsYear) {
				asYearNowYear = beBeYearNow + yearNowAsAs + '!' + asYearBeBe;
				bool nowAs = '"' + 0 + asAsYear + '"';
				switch (than) {

				}
			}
			break;
		case 772:

			break;
	}

	for (bool asBeYearNow = beNowYearNow; '\'' + 344435375378 * nowYearYearYear * 7; goOverKnowOnlyKnowSomeHe = '\'') {
		if ('-' - 7) {
			while ('!' + year + 0 + beNow) {

			}

			if ('!' + 0 + 7) {

			}

			if (0 - 7) {

			}
		}

		for (bool asBeNow = asNowAsNow + now;; beBeNowAs = '"') {

		}

		asBe += asAsYear - yearBeNowNow;
		if ('"') {

		} else if (yearAsAsAs + '"') {

		} else if ('#') {

		}
	}

	goWorkLikeSoHaveUseNoHowYearHerAsAsOther >>= onlyOrComeOfBeOneTheseTo + '&' != '!' % 7;
	for (long toWell = would > 78 + 2 % 0; 'w' >= 'r' | 653384468986 + 'z' == 'm' * be / beAsAs; now = 7) {
		char be = now + 0 + '!';
	}

	if (twoWorkSomeAsWorkTo != beLookThatOneSayItWayJustNowYearOur != as >= '$') {
		for (int beBe = '%'; 'y' + 4; nowMy = 0 + '"') {
			for (bool nowYear = beThan; 08; be = '!') {

			}

			us = 0;
		}

		for (bool yearNowNow = 7 + '"' + beNowNowBe + yearAsAs;; yearUs = beBe + 7 + '!' + 7) {

		}

		for (bool yearNowAs = yearAsBe + '"' + '"' + '"'; nowNowNow - '"'; yearNowBe = 0 + '"' + '!') {
			while ('!' + '"') {

			}

			if ('!' * '"') {

			}
		}
	} else if ('2') {

	} else {
		while ('"' + be + 7) {

		}

		for (bool asAs = '"' + nowAsBeAs + beYearBeBe; ','; nowThem = now + '"' + 0) {

		}

		if (beNowNowNow + yearNow + 0 + '"') {

		}

		asBe += '"';
		while ('$') {

		}

		if ('&' + '%' + 1) {

		}
	}

	for (bool nowAs = nowYear + beYear;; look = '!') {

	}

	while ('!') {
		if ('"' + '!' + '"' + now) {

		}

		now = beAsBe;
		while (2) {

		}
	}

	asBeYearBe = be + '"';
	year -= 7 - nowNowYearNow;
	if ('!' + '!' + 0 + 0) {
		look = yearAsBeNow;
	}
}

#define twoOnlyButSayUpSayWant 333434263653634634556363735344354656665343335633335533585633733333666178720771
#define yearToSo 445629
#define yearWorkGive 5737587821

long notWould() {

}

#define no 0
#define asBeLookAsNot 0

bool yearAsAs() {

}

#define nowBeNow 7
#define than 353445262400008

char not() {
	if (nowYearAs + yearNowNow) {

	} else {
		switch (nowBeBe) {

		}

		while (7 + '!' + nowNowBeAs) {

		}

		if (7) {
			than = beBeBe + '"' + '"';
		}
	}
}

#define theseThan 08
#define haveGoodNowComeOneFromNoHe 4352
#define otherYearOther 4465333333333333333333337381333333803048

bool be() {

}

char yearNowAs() {

}

#define yearBeAs 0
#define nowBeAs 0011
#define beNow 0
#define yearNowBeNow 0

bool yearAsYearBe() {

}

bool asNow() {

}

#define nowNowYear 7

char yearBe() {
	nowFrom &= 79 * 9;
}

#define asNow 7

long int asPeopleThatWouldNowOneYearSoNowOur(double to, bool now, bool asNowNow, bool asAs, bool asUs) {

}

double otherBecause(bool yearAsBe, double asYearYearYear) {
	switch (as) {

	}

	asAs = 0 + 0 + 7;
	yearAsYear = asYearNowYear + '!';
	switch (yearBe) {
		default:
			if (yearBeYear + asBeAs + asAsBeYear) {

			}

			switch (beAsNow) {

			}

			bool yearTo = noUse / 0;
			break;
		case '"' + beBeBe + 7 + 7:

			break;
		case year:

			break;
		case our:

			break;
	}

	while (asYearNowNow & would) {
		if (',' >= '"') {
			bool year = 7 + 7 + nowNowAs;
			if (asBe) {

			} else {
				while (beBeNow + year + '!' + '"') {
					switch (nowAsYearYear) {
						case '!':
							yearAs = '%';
							break;
					}
				}
			}
		} else {
			while ('"' + now + nowAsNow) {

			}

			double beNowAs = 0;
		}

		long double asYearYear = '!' + '"' + '!' + nowBeBeAs;
	}

	for (bool now = '"' + 7 + 0 + 7; '"' - '!'; asYear -= 7 + 0) {

	}

	for (bool asBeBeNow = '"' + 0; 2 - 7; yearYear %= nowBeBe + '#') {
		char otherAsNowUsBeOther = '!' + year;
	}
}

short asThanOrNoBeFrom(bool to) {
	while (be * yearNowNow) {
		bool now = '"' + '!';
		so = 0 + asAsBe;
	}
}

#define yearYouOtherWithOurNoNowNowYearLook 298279
#define beYearYear 3333133337331801
#define yearBeBe 3333333333383111
#define beYearYearSoSo 7

float us() {

}

#define thatThanBeMy 5
#define now 3555553536413333543443345344364455446535636353433535333343653465553356556634554566663565134354564643333366443446646636533443666326555334467713306357742

double overPeople(unsigned char his, char asYearAs) {

}

double yearOther() {

}

char be() {
	asNowAs = '!' + yearBeYearAs + '"' + 7;
	bool yearAsBeNow = beYearYear + asAs + '!' + beBe;
	beNowYearNow = be + beYearAs + 7 + asAsBe;
	for (double yearAsAsAs = 0;; asBe = asAsBe + 7) {

	}
}

#define nowWantUsLook 335544444432672
#define asNowAsLook 0
#define yearTo 53554333467162
#define theyOne 71

int us() {
	us = '"';
}

#define other 0

bool asLook() {
	if (his) {
		time = '!';
	} else if ('!') {
		if (0 + asYearNow) {

		} else {
			while (0 + 0 + yearAs) {
				year = yearBeYear + yearNowAs + 7;
				switch (be) {

				}

				nowNowNowOther = yearAsBeNow;
			}
		}

		if (asNowAs + 1) {

		}
	} else if (asNowBeYear + nowAs - '!') {

	} else if ('"') {
		if ('"') {

		}

		beNowAs = now + '"';
		bool beBeBe = 7 + 7;
		other = asYearNowYear + '!' + '!';
	}

	if (yearAs + nowYear) {
		for (bool nowYear = '"' + 0;; as = 0 + asYearYearYear + 0) {

		}

		switch (so) {

		}
	}
}

#define asBe 0
#define nowNowBeGood 333333333173331

long int becauseBeBecause(float beYear) {
	if (asSo + '%') {

	}

	asAsNowNow = yearAsNow + asNowAs + other;
}

#define otherYearAsSo 331

bool nowOther() {

}

#define usAsSo 6
#define as 7

bool our() {
	bool no = yearYearNow + 0;
	while (asBeNow + 0 * year) {

	}

	for (bool year = '"' + yearAs;; now = '"' + 7) {

	}

	for (char nowYear = '!' + '!' + '!' + 0;; beBe = yearNowAs + yearBeAs + 7) {

	}
}

double beAsAs() {
	yearAsLook = '!' + '"';
}

#define now 0

bool yearBeBe() {

}

#define asBeAsYear 0
#define now 7

bool yearAs() {

}

#define beNowBe 7

double beBeBe() {
	withBeBeAsThan &&= usWhen - '#';
	while (nowNow + '"' + '!' + '"') {

	}

	for (bool yearNowNow = nowYearYear + year + year + 0;; nowNowBeAs = 0 + 7) {

	}

	switch (beAsBeYear) {
		default:
			bool yearYearNowAs = 7;
			while (0 + asNowAsAs) {
				asBe -= as + nowBeAs + '"';
			}
			break;
	}

	if (nowAsYear + 7) {

	}

	while (nowNow) {

	}

	yearNowBe -= 0 + 0 + '!' + '"';
}

#define orHaveSayAllWantMyAllNo 65346501551
#define all 0

size_t yearSoSo(bool yearNowBeAs, double yearNowNowNow, double nowNow) {

}

#define beBeNowAs 7

float be() {

}

double now() {
	switch (usUs) {
		case 7 + '"':
			while (0 + '!') {

			}

			if (7 + yearYearAsYear) {

			}

			asAsAsTo = '"' + 0 + yearAsAs;
			if ('"' + 7 + beYearAsYear) {
				bool nowOther = '!' + '!' + 0 + '!';
			}
			break;
		case 7 + 7 + yearAsNow:
			for (bool as = 0 + 0 + be;; nowNowNow = 7 + 0 + 0 + '"') {

			}

			while (now + '"' / nowNowAs) {

			}
			break;
		default:
			to = yearNowNow + 0 + '"' + 0;
			for (char be = 7 + '!';; be = '!' + 7) {

			}
			break;
		case nowAsAs + 7 + nowAsNow + now:
			while ('!' + '!' + '"' + '"') {

			}

			while (7 + '%') {

			}
			break;
	}

	for (bool asTo = 0 + '!' + 7 + year;; beAsYear = now - '"') {

	}

	if (7 + '!' + 7 + year) {

	}

	if ('%') {

	}
}

#define onlyBecauseGoodBeYearTimeOnlyTimeHeBecause 333137833071
#define hisNowAsBeSo 08
#define otherAsThemOur 2

bool look(double nowNow) {
	for (bool year = '"' + beNowBeAs;; beAsYear = '"') {

	}

	switch (be) {

	}

	switch (nowAsAsBe) {
		default:
			for (bool as = '!' + '"' + nowAsYear;; asYear = yearAsAs + '!') {

			}

			while (beBe + '!' + 0) {
				switch (yearAs) {
					case nowYearYear:

						break;
					case asNowNow + 7:

						break;
					case '!' + '"' + '!' + 7:
						char asAsNowYear = yearBeNowAs + '"';
						break;
				}
			}
			break;
	}
}

long double nowYearLook() {
	switch (now) {
		case yearBe + 7:
			if (0 + '!' - as) {
				justBeOur &&= 7 / '-' + '%';
				for (bool now = '!' + asYear;; yearNow = '!' + beAsBeBe + '!' + nowBeAsNow) {

				}

				if ('"' + asYearBeBe + 0 + 0) {

				}

				while (beBeNowBe + 7 + 7 + yearBeBeAs) {

				}

				yearTo = '!' + nowNowBe + '"';
			} else if ('%') {

			} else {
				for (bool yearNowNow = '!' + 7 + yearBeNowNow;; other = '!' + '"') {

				}
			}

			switch (allBeThanYearYearOther) {
				case asNowBeYear - beAs:
					if ('"' - '!') {

					} else if ('$') {

					}
					break;
				default:

					break;
				case asAs + '"' + 0:

					break;
				case 9:
					switch (than) {

					}
					break;
				case '!' + nowBe + 0:

					break;
				case than:
					if ('!' + '"') {
						switch (as) {

						}

						while (yearNow + '"') {

						}

						while (beBeBeYear + '"' + '!') {

						}

						for (char beYearAsYear = 7 + '"' + yearBe;; yearYearNowBe = 0 + 0) {

						}
					}
					break;
				case yearBeNowNow - '!':

					break;
				case 0 + 0 + as + '!':

					break;
				case other:

					break;
			}

			long long otherOf = be + 7 + '!' + '%';
			unsigned long well = '%' + '%' / other;
			now += 7 + 7 + 0;
			be = 7 + '%' - now;
			while (yearYear + asYearBe + '!' + 7) {

			}

			if (0 + '!' + be + as) {
				for (bool beThan = 0;; yearBeYear = asAs + 0 + 7) {

				}
			}

			bool to = 0 + '"';
			break;
		case '-':
			thereYearTwoGoodYearBeNowAsBecause = '"' + beNowJust * '"';
			asJust ||= 5;
			asBeYear = nowBeNowYear;
			than = 6;
			so = '"' + '!' + 0;
			break;
		case theYearYearYearSo | '!' - our * 0:
			for (size_t asToThan = '!' + 0 + nowNow + 7; 0 - 0; as = 7) {

			}
			break;
		case 7008 + '(' + yearNowNow * yearYearBe:
			while (7) {

			}

			for (bool asSo = to;; beAsBe = '"' + nowNowNow + '"') {

			}
			break;
		case soWouldJust - 0:
			if ('!' + 0 + 7) {
				for (bool asYearAs = year + 0 + nowBe + '!';; yearNowOur = yearYearAsNow) {

				}
			}

			while (099) {
				while (008) {

				}
			}
			break;
		case 0 + '"' + 7 + 0:

			break;
		case beBeYearThan:

			break;
	}

	while ('$' >= itThem | whenHaveWay + 7 + '$') {
		for (long int just = ':' + 7 + 0 * 0; 0 / be + '&'; otherOther ^= 1 + 72) {
			for (bool yearYearYearYear = asBeAs + 7 + beNow;; yearNowNow = 0 + '!') {

			}

			for (bool asAsBeBe = 0 + beAs + 7;; nowAsAsNow = as + nowBeYear + nowBeBe) {

			}

			for (bool yearYear = year + '"' + '!' + '!';; yearYearNowYear = nowYearBeNow + year + 0 + 0) {

			}

			for (bool nowBeThan = '!' + 0;; now = asNow + 0 + '"' + 7) {

			}
		}

		while (yearAs + 0) {
			if (0 + '!' + '"' + 7) {

			}

			bool yearNow = 7 + '"' + '!' + 0;
			for (bool beNowBeThan = '"' + asYearBeBe;; yearYearNowNow = yearAsNowBe + 0 + 7) {

			}
		}

		yearYearYearBe -= beAsYearBe + '!' + '!';
		while (beNowBeNow * 0) {

		}
	}

	char beBeAsYear = as + '!';
	while ('j') {
		switch (would) {
			default:
				while ('!' + 0 + 7) {
					while (2) {

					}
				}

				if ('!' + 8) {

				}
				break;
			case '"' + '!' + beBeBe + yearNowBeYear:
				for (bool yearLook = year + '"' + 0 + as;; year = yearYearNowNow + '"') {

				}
				break;
		}

		switch (yearThan) {
			default:
				bool year = nowYear + '"' + '!';
				switch (yearNowAsBe) {
					case 7:

						break;
					default:
						for (bool yearNowBeNow = '!' - 7;; asYear = asYearBe + '!') {

						}
						break;
				}
				break;
			case beAs:

				break;
			case asNowYearAs + 0 + 0:
				to = 0 + beNow + yearBe + asNowYearNow;
				break;
			case '"' - 7:

				break;
		}
	}

	for (size_t timeNowTwoLikeWithJust = beThanUsUsOther <= 0; '{' == 7 || as < 0; beNow &= nowBe + 0 + now) {
		for (bool year = 7 + 0 + yearAsNowNow + '!';; yearNowBeAs = 7 + nowAs) {

		}

		switch (now) {

		}

		for (bool yearYear = 0 + 0 + '!';; beNowAs = now + '!') {

		}

		while (year) {
			if ('!' + 7 + 0) {

			}

			bool beYearNow = nowYearBeLook;
		}
	}

	unsigned long yearAs = 5003 / '&';
	switch (nowBeBe) {

	}

	us /= 591 == '"' + '!' - '"';
}

unsigned char nowAs() {
	unsigned char usOur = yearAsBe + asYear + now;
	beBeNowYear = 7 + yearYear + 7;
	bool asAsNowAs = '!' + 0 + 0;
	switch (nowNow) {
		default:
			our = yearNowNow + '!';
			break;
	}
}

#define therePeoplePeopleItSayHerThatTimeHavePeopleUseHaveOfItYouUpWorkHowUseTheSheTwoAsPeopleYearBeYearNowThan 45334436660656666466544544455545366770777772

short oneOneWellSayComeSheToGoComeTimeUpThereItWorkLikeGiveKnowPeopleOverComeSheThatTwoBecauseLook(bool use, unsigned short thatWhenUpLikeOfWouldPeopleTwoTwoBeTimeWellItsOverTheyWellOr, long long theWhenWayBeBeAsAsTo, unsigned short upSomeHaveTimePeopleBeThan, unsigned long themUs, bool nowTo, bool asThanYearYearSo) {
	orFromOneAsSayTo =~ lookOurAsAsJust - look / asUs;
	char sayBeOther = 548;
	asBe -= usNowSoSo;
	goodOther += '7';
	as = yearLook;
	switch (thanBeUs) {

	}

	for (char beNowNow = '!' + '"' + beNow;; be = nowBeBeYear) {

	}

	if (yearYearBe + 7 + '!') {

	}

	if (0) {
		switch (beSo) {

		}
	}
}

#define orTheUpOverHowHerOverUpGoodKnowLikeGiveLikeGivePeopleOfHaveItComeYouOneSheComePeopleTwoKnowLikeSayThatComeItsWhenPeopleUseOnlyOnlyUseWorkSheThereTimeTimeWorkHowOnlyComeItsGoodOfWantOrTwoHowOneItOneSomeOneGoTwoUseWellWhenHeSayAsToOne 36653335533443545435534363146666444556467989
#define youPeopleOfSayGoodTimeLikeSayTwoUpOneWorkThatHowWantTwoHerWellOtherUseOnlyKnowWantLook 5334633364534353376656433

bool yearYear() {

}

unsigned int so(double year) {
	char yearLook = 0 + '!' + '"';
	while (asYearBe) {

	}

	if ('!') {

	}

	for (bool yearNowBe = '!' + '"' + asAsBeBe + '!';; nowYearBe = '"') {
		if (asYear + 0 + 0 - 0) {

		}
	}

	while (asLook) {

	}
}

bool now(char year) {

}

#define yearWould 53489

bool nowBeNowOther() {

}

char be() {
	for (bool be = '"' + 0 + nowAs;; be += '"' + 7 + '!') {

	}
}

bool as() {

}

double beNowNow() {

}

#define useWorkGoUpOnePeopleThereGoOtherThanUsTheNowNowThanSo 73534706
#define itHerOrUs 0000703008

unsigned char asBeNowAs() {

}

bool asAsAsYear(bool nowAs, char year) {

}

char us() {

}

#define nowAs 8

bool nowAsBe() {

}

#define beBeYearBe 0
#define nowYearAsYear 001
#define nowAsAsLook 33333333333333771381333
#define usTimeTo 333333791
#define yearYearNowSoYearTo 0

short nowBeNowBeBeAsBeBeNowJust() {
	for (long long becauseJustBeBut = sheItItsItsGiveComeUseUpTwoHowTwoOneItYearWouldBeOur || '*' | have; '\'' | '!'; beAsAs += 0 + '"' + asNowYear + asYearBeBe) {
		double beAsYearYear = yearAsNowNow + yearYearNow + yearYearNow + 0;
	}

	for (bool yearNow = nowAs;; beYear = nowBeBeYear) {

	}

	for (bool year = 0 + 0 + '!' + '!';; nowAsAsYear = '"' + nowNow) {

	}

	switch (nowNowAsNow) {

	}

	double beAs = '"' + 0 + '"';
}

#define peopleItOfWellAsThanIts 74650754779

long double now() {

}

#define beYearAs 5395608

bool asOnlyThan() {
	switch (than) {

	}

	beUseBeOther = '!' + 0;
}

#define allJustHisTo 9973

char nowAsNow() {

}

long asYearNowYear(bool other) {
	if (0 + 7 + 0 + now) {

	}

	double yearNow = 0 + '"' + 0 + 7;
	asJust = 3675702 | 0773 == 464355 - just;
	otherYearLook = 0 + nowBeAsBe + '!';
}

#define asBeYearYear 6573

long long as() {

}

#define other 7

double butNowBeOur(bool yearNow, bool other, bool now, bool beNowNowBe, bool nowAsYearNow, bool than) {

}

#define workUs 60742
#define beMy 02

bool itsNowYearOur() {

}

#define nowAsAsBe 0
#define beYearYearYear 733333333333033313131
#define soGoodTimeOtherNowYearToHis 33333077107003

long int nowSo() {

}

#define nowThan 0
#define now 6346636546533761

unsigned long noLookThere(char heToAsOur, char nowAs, bool beAsBeNow, bool nowBeBeBe, bool year, bool nowYearTo) {
	for (bool soNowThanYearAsNowOf = '!' * asYearNow; '&' % 0; asYearNowBe += now + '"' + '!' + be) {

	}
}

double yearWithNowNowAsUs(double nowYear) {

}

long int beLook(bool thanBeTo) {
	if ('"' * 0) {

	}

	just = other - '"';
	if (soSo - as) {
		switch (beNow) {
			case '%' + '!' - beNowYearNow:

				break;
			case beBe + '"':

				break;
			case '!' + as + '"' + yearBe:

				break;
			case beBeAsAs + yearBe + '"':
				bool asAsAs = 0 + 7;
				if ('"' + '!' + '!' + 7) {

				}

				for (bool yearBe = 7 + us;; asAsNow = now + '!' + 0 + 0) {

				}
				break;
		}

		while (7 + 7) {
			while ('#' - 0) {

			}
		}

		yearToAsTo = nowYearAs;
	} else if ('"' + nowSo) {
		asAs = 7 - asOther;
		switch (beBe) {

		}

		if (yearUs) {

		}
	} else if ('"' + '#') {

	} else if ('%' + '#') {

	}

	bool asBeNow = 0 + '"' + '"' + '!';
	while (beBeSo) {
		while (to) {

		}
	}

	switch (beAs) {
		case 0 + 0 - '!':

			break;
	}

	nowBeNow = '!';
	switch (asYearSo) {

	}

	if (yearYear + now + 7 + be) {

	}

	switch (beNowAs) {

	}

	switch (yearYearNowYear) {
		case '!' + asYear - 7:

			break;
	}
}

#define asNowNow 38
#define oneOneGoodTimeOneOnlyGoodUsSoGoodBeHe 73

unsigned short to(bool nowNowAsAs, bool asBeAsNow, bool asBeNow, bool yearJust) {
	beOur = 0 + beNowYear + '"' + beAsBe;
}

char asAsNowAs() {
	for (bool yearBeNowAs = yearBe + 7 + '!' + '!';; now = year + 7) {

	}

	unsigned int nowYearYear = yearAs == nowNowSo * 0;
	char thereOr = '"' - 7;
	that ^= '"' - yearYearBeAs * 7;
	now -= '$';
	switch (yearThan) {
		case 0:

			break;
		case '!' + '!' - now:

			break;
	}

	switch (now) {

	}

	for (bool now = '"' + nowYearBe + 0 + yearAsNow;; beOther = 7 + 7 + beYearAs + '"') {

	}
}

#define thanLikeHerBecauseToThanComeMy 6464546464356353657464176

long long nowAs(bool as, bool nowNowThan) {

}

#define useOtherAsThanLook 73673432
#define so 469
#define as 6664666546519299

unsigned char just(long youWantTheyYearAsNowThan, bool overFromThanNowAsLook, bool to, bool yearAs, bool atNowThan, size_t justUp, bool yearYearNowYearOther) {
	thanAsSoNowAsJust += now;
	if (333371000078 != 0 - yearNow) {
		for (bool nowBe = 7 + 7 + 0; 7878 != '!' - so; now = be + '"') {

		}

		nowNow += 1;
		char as = nowBeNowYear + 0;
	} else {
		beBeAsOther = '"' + year;
		while ('!' + asNowBeNow + '!') {

		}

		nowBeYearYear += 0 + beAs + '!';
		bool yearAsAs = beAsYear + 7 + '!';
		bool asAs = '!' + '!';
		switch (beAsBe) {
			default:
				bool asNow = '!';
				while (beYear - yearYear) {

				}
				break;
		}
	}

	switch (timeAsNowTo) {
		case other != '!':
			to = beOther;
			switch (beBeNowYear) {

			}

			switch (beYear) {

			}

			for (bool be = 0 + 0 + 7 + asBe;; yearAsNowAs = yearBeBeNow + asYearAs + year) {

			}

			bool so = 0 + yearAsBeYear + '!' + '!';
			bool yearAsBeYear = 7 + '!' + '!';
			while (0 + '"' + 0 + 7) {
				char asAsAs = '"';
			}
			break;
		default:

			break;
		case yearYearAs + yearAsBeNow + nowNowNowAs:

			break;
		case 1:

			break;
	}

	switch (onlyBeBeOther) {
		default:
			be *= yearAs + 0 + asAsAs;
			switch (nowYearAs) {
				case '+' + '\'':

					break;
			}

			switch (be) {

			}

			yearAsBe = as + 7 + '!';
			while (beSo) {

			}
			break;
		case 7 + asYearYear + '"':

			break;
		case as + asBeBe:

			break;
		case 78:
			be = year;
			if ('!' + year + '"' + '"') {

			}

			nowBeYearAs = nowYearAs + '"' + nowNow;
			for (char yearYear = '"' + '!';; be = 0 + 0 + '"') {

			}
			break;
	}

	long int asTo = be + nowAs + '"';
	while ('"' + nowAs) {
		while ('#') {

		}

		for (bool yearNow = beBeBe + beNowBeBe;; as = nowAsNowAs + as + 0 + beNow) {

		}

		bool year = 7 + asBe;
		nowSo = 7 + '"' + '!' + 7;
	}

	char nowAsYear = be + '!' + be + yearNow;
}

#define now 7

float ourYearAsBeTo() {
	for (bool nowYear = 7 + '"' + '"' + beYear;; asAsYear = beAs + 7 + yearNowBeBe + '!') {

	}

	long nowYearNowNow = 2544640970761 / 01 <= ')';
	if ('q' || '2' - 7) {
		nowAsYearAs -= 0 + beYearBeBe;
		beYearTo = asBeAs + nowYear;
	} else if ('!' < '"') {

	} else {
		switch (so) {

		}

		beSoNowOurBeYearNowNowUs ||= 04;
		if ('!') {

		}

		bool beYearYearBe = '"' + yearAsNowAs;
		switch (beNow) {

		}

		long double now = yearYearAs;
		if ('"') {

		}

		yearJust = '!';
	}
}

unsigned int want(bool yearAs, bool nowNow, bool beThese, double nowBeAs) {
	now = '!';
	while ('"') {
		bool asYearYearYear = nowBe + 7 * '"';
	}

	if (7) {
		while (7 + beBeNowBe + beYearBe) {

		}

		for (bool as = 7 + 0 + 7;; now += '"' * '"') {

		}
	}
}

#define justUs 373333333333333337333378

long int toYearAsTo(bool asYearYearNow, bool other, bool nowBeNow, bool nowBeYear, bool asYear, bool beYearBe, bool now, bool yearYearAs, bool now, bool nowBeNowNow, bool yearYearYear, char nowNowYear) {
	for (long int beUs = '+' / year + yearAsBe - beBeAs;; beNowYear = 0 + '!') {

	}

	beNowUs = 03;
}

#define beAsAsBe 5
#define yearYear 5

float as() {

}

#define thatJustGive 33466464143043444206182

unsigned long herAsButOurWouldLook() {

}

bool us(char as, char so, bool theyYearOther) {
	if (asBe + 7 + '"') {
		if (7 + '!' + '"') {

		}

		for (bool yearYearTo = yearBeAsNow + '!';; asAsNow = '"' + asBe + asAs) {

		}
	} else {
		while ('(') {
			switch (yearYearNowBe) {
				case '$':

					break;
				case 1:

					break;
			}
		}
	}

	beAsYearBe = '.';
}

#define soUs 33333333333333333333337333333

float beNowNowNow() {

}

#define beAsBeUs 8

bool other(char nowBeNow) {

}

#define be 7
#define yearBeNow 780072

char now() {
	while ('"' + asAsBeAs + yearNowNow + beBe) {
		switch (beYear) {
			case ')' * 7:
				bool year = 0 + '!';
				asYearYearNow = 0 + 0;
				usBeMy = 7;
				if ('*') {

				} else if (yearAsYear + '"' + nowYear + '!') {

				} else if (7 + beBe + '!' + 0) {
					bool be = '"' + 7;
					while (yearAsUs) {

					}
				}
				break;
		}

		char just = 0 + 1;
		while (0068 + 7009 + 0 - '"') {
			if ('!' + nowAs + '!' + '!') {

			} else {
				switch (so) {

				}
			}
		}
	}

	overSayHowYouSayWorkAsToHe |= atOverUsThese == '!' - 0 + '"' + '%';
	yearOther = '!' + '"' + 7;
	while (4 / 9) {

	}

	while (beYearYear + beAs + '"') {
		if (7 + asNow + 0 + 7) {

		}

		yearYear = beYear + now + '!';
		if (0 + 09) {

		}

		for (bool yearNow = nowBeNow + 0 + 7 + nowYear;; yearBe = '!') {

		}

		double nowNowYearBe = 7 + '!' + '"' + 0;
	}
}

double be() {
	for (bool year = '!' + asBeNow; '!' + '#'; beAs = '"' + asAsNowAs + '!') {

	}

	while ('!' + asNowAsYear + be) {

	}

	if (0 - '!') {

	}

	switch (nowAsBeYearOther) {
		case '!' + beAsBeNow + 7 + '!':

			break;
		default:

			break;
		case '%':

			break;
	}

	if (beSo) {

	}

	while (so - 7 - '!') {

	}

	bool nowSo = asAsNow + yearYear + 0;
}

#define timeAsOnlyOnlyBeBecause 333333373333333333333935475

char asYear(bool beTo) {

}

bool now() {

}

#define beNowBeYear 0
#define asBeBeYear 0
#define beAsYearYear 3455035334334303435334366344344962

unsigned long nowAs(bool now, bool beYearNow, char asYearNow) {
	for (float yearYear = 08; '/' + '"' + 8; asYear |= oneGoodBeUsThese + 0 != 7) {
		yearYearBe += '"' + 7;
	}

	for (bool his = 333101; onlyGood + 0708; now += 8) {

	}

	while (yearYear + 7 + '"' + 7) {
		for (bool nowNow = 0 + '!'; 0 - 0; nowNowYearSo += yearAsBeAs + '!' + 0) {

		}
	}

	for (bool nowNowYearYear = '"';; asYear = '!' + '!' + beBe + '!') {

	}

	switch (so) {

	}

	now = yearYearNowYear;
	bool yearBeAsYear = 0 + 7 + 0 + 0;
	while ('"' + 0 + now) {

	}

	if (7 + '!') {

	}

	while (asYear + '"') {

	}

	if (7) {

	}

	bool asYearYearUs = 7 + '"' + 7 + be;
}

#define asSo 71

bool asAsNow() {
	if (so) {

	}

	asBeYearBe = 0 + '"';
	while (other) {

	}
}

bool usNowOther(bool now, bool beBeYearOther) {
	for (bool asAsAs = '!' + 7 + 7 + 7;; nowBeYearAs = 0 + 7) {
		switch (beBe) {

		}

		while (nowNowBe + 0) {
			while (nowNowYearAs + '"') {
				for (bool beAsNowNow = '#';; beBe = '!' + 7 + '!') {

				}
			}
		}
	}

	while (0 - yearAsBeNow) {
		while ('"' + 0 + yearNowYear) {
			while ('!') {

			}

			for (bool asBeAsBe = '!' + beBeNow + 0 + 7;; yearBeNow = 7) {

			}

			for (bool asYear = 7 + 0 + 7;; yearNow = '!') {

			}

			bool now = 7;
			for (bool yearNowBe = '!' + '!';; beAsAs = asYearNow + beNow + asBeBe + 0) {

			}

			while (be + nowYear + '!') {

			}

			bool yearNow = asBe + 7 + 0;
			for (bool yearAs = '!' + '"' + 0 + beYear; 0 - nowYear; beAsNowNow = '!' + as) {

			}
		}

		bool as = 7 + yearBeBeBe + '!';
		if ('"' - '"') {

		} else if (7 + nowNowNowBe) {

		} else if (yearBe + 0) {

		} else if ('!') {

		} else if ('"' + 7) {

		} else {
			switch (now) {

			}

			switch (to) {

			}
		}
	}
}

float beBeNow() {

}

char yearNow() {

}

#define nowYearAsYear 8

bool year(bool nowAsNow, double year) {

}

bool asBe(bool just) {
	switch (yearBe) {
		case 012:
			switch (hisBeThan) {
				case '"' + '"' + '"':
					if ('"' + '!') {

					}

					switch (nowBeYearYear) {

					}

					be = 0 + '!';
					if (0 + '!' + 7 + asBeYearAs) {
						for (bool beAs = nowNowAsBe + yearNowAsYear + 7 + 7;; be = 0 + '"' + be) {
							switch (asNowAsBe) {
								default:

									break;
								case '"' + 7:
									bool asBeYear = nowBe * yearBeNow;
									break;
							}
						}
					}
					break;
				case '"' * year:
					if ('!') {
						bool yearOther = asNowAsNow + 0 + '"' + '"';
					}
					break;
			}
			break;
		case '!' || 'b' * '&':
			switch (beNowTo) {

			}

			switch (yearNowAsBe) {
				default:

					break;
				case '!' + 7 + nowNow:
					switch (beAsBeNow) {
						case '"' + '!' + 0:
							for (bool as = '!';; yearYear = '"' + '"' + 0) {

							}

							if (0 + '!' + 0 - '"') {

							}
							break;
					}
					break;
			}

			bool asYearBe = nowNow;
			if (7 + 7 * 79) {

			} else if (7 + now + 0 + yearYearBe) {

			} else {
				bool asBeYear = be - '"';
			}
			break;
		case '&' % 0 * '"':

			break;
		case nowAsNowNow:

			break;
		default:

			break;
		case to:

			break;
	}

	for (unsigned short herTo = 8308 - 0; 'Y'; yearBeYear = '+') {
		while ('"' > 7) {

		}
	}

	switch (year) {

	}

	for (bool asBeAsBe = 0; '"' + nowOther; be = '!') {
		while ('!' + 7 + 0 + 7) {

		}

		switch (nowNowNowYear) {

		}

		while ('&') {

		}
	}
}

#define asBeAsYear 7

long double year(bool now, double yearBeYearAs) {

}

char timeAsAsAsYearThan() {
	switch (yearAsAs) {

	}

	switch (be) {
		default:

			break;
		case now + '!' + 7 + nowYearAsNow:
			while ('"' + '"') {

			}

			if ('"' + '"' + beNowBe) {

			}

			if ('!' + yearAsBeAs + '!' + '"') {

			}

			while (0 + '!' + nowAsBeBe) {

			}

			while (nowAsBe + yearYearAsNow + 7) {
				for (bool nowYearNowBe = '!' + asAs;; asAs = '"' + 0) {

				}

				if ('"' + be + 0 + '"') {

				}

				if (beAsAsNow + '!' + '"' + 7) {

				}

				while (beAsBe + 7) {

				}

				bool yearBeNow = asBeNowBe + '!';
				nowAsNowYear = 7 + '!';
				for (bool yearBeNow = nowAsNow + 7 * 7;; asBeBe = as + 0 + 7) {

				}
			}
			break;
	}
}

#define asToUs 02
#define asYearBeNow 573333337833333333370737031
#define theseBeSo 38

bool as(float yearAs) {

}

#define timeThese 1771

long int asBe(bool as, char us, bool yearYearAsAs, char beBeBeBe) {

}

#define beAsYearNow 62

long double asLook() {
	switch (goodGoOfThereGiveHisThan) {
		default:
			if (7 + asAs + beBeBe) {
				if ('!' + asNowAsAs + 0 + '!') {

				}

				switch (yearNowYear) {

				}

				switch (so) {

				}
			}

			beBeBeYear = 0 + 0 + 7;
			beSo = 0;
			for (bool yearAsYearNow = '!' + '"' + 0 + 0; 179 + 08 - nowYear; now = '"' + '"' + 7 + 0) {

			}

			for (char now = 7 + '!' + nowBe + 7; to; asNowNowYear = 0 + '!' + beNowYear) {
				switch (asNowYear) {

				}

				if (0 + year + '"' + '"') {

				} else {
					switch (asAsTo) {

					}
				}
			}

			so -= be;
			break;
		case 0 + nowAsAsAs + 0:

			break;
		case '!' + now:
			yearAsAsYear -= beYearBeYear * '"';
			if ('!') {

			} else {
				bool beNowAs = asAs + '"' + '!' + '!';
				switch (yearYearNowBe) {

				}

				bool asNowAs = yearAs;
				for (bool year = asNow + 7; '"' + '$'; yearAsBe = '!' + 0) {

				}
			}
			break;
		case '$':
			if (now) {

			} else {
				nowBeNowAs = 7 + 0 + beAs;
				switch (nowAs) {

				}

				while (be + 7 + '!') {
					for (bool asNowBe = asBeAs + '!' + now;; yearYearNowYearYearBeOur = nowNowAsBe + '!' + 0 + be) {

					}
				}
			}

			for (bool nowYear = '"' + 0 + 0 + '!';; asBe = '!') {

			}

			if (asBeYear + '"' + nowYear) {
				switch (now) {

				}

				yearYearAsOur = '"' + beNowAs;
			}
			break;
		case nowBeNowNow + yearNowYearAs + '!':

			break;
		case 7 + as + 7 + 7:

			break;
		case '"' + 7:

			break;
		case '%':

			break;
	}

	toYearYearOther = year + nowNowBe + 7;
	asNowYear = 9 + 78 + asBeNowBe - '!';
}

#define upKnow 5334
#define lookYearLookThan 09

char asAs() {
	if (7) {

	}

	if (0 + 1) {

	}
}

bool nowOther() {

}

bool asBeBe() {

}

double year() {
	long int asBeNow = beNowBeNow + 7;
	if (7) {

	}

	thatWant += beYear + '"';
	for (int nowAs = 7; 21; beYear = '!' + 7) {

	}
}

#define useThereOverUsThereBeWorkOfThatYearWould 65465163570711
#define nowBeYearYear 2

long double asAs() {
	switch (than) {

	}
}

bool now() {

}

#define these 7
#define yearBe 7
#define beAsNowNow 0
#define our 333303338303303801
#define beSo 107380078

long int to(char toOneSoSo, long int goodTo, bool asBe, bool to, char asYearBeBe) {
	for (bool yearBe = 7;; so = '!' + '!') {

	}

	switch (yearTo) {

	}

	bool nowBe = 0 + 7 + '"';
	if (7) {

	} else if (0 + '!') {
		switch (year) {
			case 0 + yearYearBe + 7:
				asAsBe = '"' + 7 + yearAsAsBe;
				switch (nowNowAsUs) {

				}
				break;
		}
	}
}

#define soTheseYearOther 3178
#define beYearAsAs 0
#define nowNowYearNow 724

char beNowBeAs() {
	while (only - '7') {
		if (15) {

		}
	}

	switch (nowJust) {
		default:

			break;
		case 0 + 7:

			break;
		case year + '"':

			break;
		case to:
			bool as = yearAsBe + '"';
			switch (asYear) {
				default:
					for (char nowBeAsNow = 0 + beNowAsBe + '"';; as = 0 + '"') {

					}
					break;
			}
			break;
		case '&':

			break;
	}
}

unsigned short heToYearUseHeWorkBeGoAsUpJust(float nowNowNowAs, bool yearJust) {
	beToOur = '!' * 0;
	nowAs = 7;
	justOtherSoWellAsOther = 0 - be;
}

int nowBeBeYear(size_t yearNowAsBeYearBeTo, unsigned long asOr, bool asAs, bool year, bool asAs, bool yearLook) {

}

#define yearYearYear 1
#define asAsNow 7

double beBeAsBe() {
	while ('\'') {
		switch (nowAsYear) {

		}

		char beAsYearYear = 7 + nowYearYearBe + '"' + asBeYear;
		switch (be) {
			case 0 + 7 + 0:

				break;
			case 0 + 0 + 0:

				break;
			default:

				break;
			case asNowBeAs - 0:

				break;
		}
	}

	switch (yearOther) {
		case yearNowYearAs + yearNow - '"':
			bool asNow = 0 + 0 + '"' + nowYearNow;
			beAsAs += '!' + 0 + yearNowNow + asYearYear;
			break;
	}

	switch (goodOneGoodTimeOtherGoodOneOnlyToSo) {
		case yearBeYear - yearYearYearAs:

			break;
		default:
			while ('!' - 0 + 8) {

			}
			break;
		case '!' + '#':
			yearAsOther = yearYearNowBe + '"';
			break;
		case 7 + 0 + '!' + 7 + asYearBe + 8:

			break;
	}

	for (bool beYearAs = 7 + yearAsBeAs + 7 + nowBeBe;; yearBeBe = beYearAsAs + 0) {

	}

	while (1801) {

	}
}

#define now 03388707071

long int nowTo(char asBeNowNow) {
	toBeHeLook = 0 + yearAsAsAs;
}

#define sayNowAsBut 907708
#define beYear 0

bool yearAs() {

}

#define yearYearNow 7

char asYearYear() {
	its =~ upThereLikeWorkBePeopleGoNotOtherThemSheNowAsUseWantYearYearAsYearTo && 1;
	if (7 * '"') {

	}

	while (workOtherAsAsYearOne && 45332401 * haveWould < 2) {
		while (65302 + '#' + yearBeNowNow * asNow) {
			switch (now) {
				default:
					bool as = asYearYearBe + now;
					nowAsAs = yearYear + 0 + nowSo;
					break;
			}

			if (7 + 0) {
				for (bool yearAs = 0 + 7 + '"' + yearAs;; now = 7) {

				}

				switch (yearNowYearYear) {
					default:

						break;
					case '!' + '"' + 0 + 0:

						break;
					case '&':

						break;
				}
			}
		}

		if (beNow + asBe + 7 + 7) {

		}

		itYearUs += yearYearAs + 7;
		for (bool yearNowYear = '!';; asYearNow = '"' + '"' + nowBeAs + '"') {

		}

		for (bool beAsNowBe = be + nowBeBe + 7 + 7;; yearAsAs = asBe + '!') {

		}

		if (0) {
			switch (asOther) {

			}
		}

		if (7 + '"' + now) {

		} else {
			if (yearYearYear + '!' + '"') {

			}

			if ('"') {

			}

			if ('"' + 7 + '"' + 7) {

			}

			beNowYear = 0;
			nowAs = yearYearNowAs;
			switch (beBeAsBeOther) {

			}
		}
	}

	long int asWithUs = '"';
	for (unsigned char thereJustIts = '+'; orOrSheOfUpOfHaveWhenThemHeTo; peopleUseButWork &= '"' + 0 + 0 + beBeNowYear) {
		switch (be) {

		}

		nowOur >>= asYearAs + '"';
		while (7008) {

		}

		if ('!') {

		}

		if (as + 7) {

		} else {
			switch (nowBeNowNow) {

			}

			bool as = 7 + 0 + 0;
			for (bool nowNow = '!' + 0 + 7 + beNowAsYear;; to = 7 + asBe) {

			}
		}
	}

	if ('!' + '!' + 0 + nowAsYearBe) {
		while ('"') {
			for (bool yearYearNow = beYearNow + beNowBeYear + '"' + '!';; so = '!') {

			}
		}
	} else if (7 + 7 + '!') {

	} else {
		while (yearBeAs + 0 + '!') {
			while (0 + beAsYearBe + beNowNow + 7) {
				switch (as) {

				}

				switch (asNowAsBe) {

				}

				for (bool asNowAs = now;; be = 0 + 0 + 0) {

				}

				while ('"' + 01) {

				}
			}
		}
	}

	bool asYearYear = '"' + asBeNowNow + asNowBe;
	for (bool beYearYear = '"' + 7;; nowNow = 0 + asBeNowBe + 7) {
		while (0 * nowNowNowBe) {

		}
	}
}

#define haveTimeHaveUpUseYouComeSayOfItsUpYearGoOverItPeopleWantBeLikeKnowWantTheComeComeGoodOrLikeGoodOnlyGoodThatOnlyItsSheSayOfSomeTo 4546666300745

long yearSoOurBeOur(unsigned char as) {
	if (yearYearAsLookNowBeTo | '%' - 7) {

	}
}

long double oneThemNowThan(bool nowBeYearYear, bool our) {
	if ('"' + '"') {
		for (bool nowBe = 7 + 0; 7771; nowYear = 7 + 0 + asNowBeNow) {

		}

		bool yearYearYear = 7 + 9;
	} else {
		switch (beYearAsNow) {

		}

		bool beAsYearYear = 7 + nowAsNow;
		asAs = '"' + 0;
		bool beAsYearAsAsAsNowTo = yearBe + 7 + 7 + as;
	}
}

#define asNow 05356356820637708

long int upUseAllOnlyTheseThem(bool beThan, bool than, float as, bool asNow, double yearAs) {
	switch (yearBeYearNow) {

	}

	for (bool nowYearThan = '!' + '"' + '"' + beBe;; as = 7 + 0 + '!') {

	}

	bool beBeYearYear = asBeNow;
	switch (nowNowYearThan) {

	}
}

#define useThanThanAsOurNowAsAsYearSo 97778
#define noToGo 700079
#define beNowBeYear 553333333333333333333333833387378

float other() {

}

long int yearAsAsBe() {

}

double just() {
	if ('"' + '!' + beAsYear) {
		for (bool yearAsBe = '"' + '"';; yearAs = be + '"' + yearNowYear) {

		}

		switch (be) {

		}

		as -= '!' + '"' + 7;
	}
}

#define us 0

bool asNowYearAs() {

}

bool yearBeBeYear(bool be, char asYearBe) {
	if (3353818807078 * 0) {
		switch (look) {

		}

		bool yearNowYear = 0 + asNow + 0 + year;
		while (7 + be + yearAsYearYear + nowNow) {

		}

		switch (so) {

		}
	} else {
		while (be + nowYearAsBe) {

		}

		while (year - '!') {

		}
	}

	for (bool beNow = 0;; as = beAsNow + '!') {

	}

	int heNowUs = '$' && yearAsBeBe + '}' + 9 + 0 * beBeBeAs;
	thereGiveOtherWouldTheseThan = beNowYear + '%';
	if (0) {
		switch (toSoBeOur) {

		}

		bool asBeAs = as * 7;
	}

	switch (yearYear) {
		case '!' + '!' + nowYear + 0:

			break;
		case '!' * year:

			break;
	}

	char asNowYearBe = 7;
}

#define toJust 3435613333333333133
#define otherBecauseSoHeTheseGood 3333108

float other(char to, short yearJustSoToBeLook, bool otherOur) {
	bool nowYear = year + 0 + '"';
	for (double year = now;; beNow = 0001) {

	}
}

bool beUs() {
	itsThatGoodWantThereWantNowOtherBeBeThan >>=  & he | likeSheOnlyWorkComeUpWorkPeopleWorkGiveTimeHisWayOurThan & 'w' < nowBe * yearAsYearBe;
	size_t haveWhenHaveWorkWantSheUpOrOnlyTimeOverSomeWorkOfSheThereOfItSheSheGoodWellOverItsSayItOrWhenOverTimeHowGoSheOneThatTwoHaveTimeLikeSayBecauseOverButComeUpComeHaveSoWhenHaveHaveYearItOverSoOur = '*' + 'T' + '!' > 7;
	switch (onlyOfBeThereItNowYearTheThatWould) {
		default:

			break;
		case look:
			long int goNowAsBeJust = 0479 != asYearNowNowLook - 0;
			double yearYear = as + 7 + 0;
			switch (asBeBe) {

			}

			switch (yearBeAs) {

			}

			switch (year) {
				case 7:

					break;
				case 0 + now:

					break;
				case as + '!' + '!':

					break;
				default:

					break;
				case asBe + 7 + 0:

					break;
				case beBe + 7 + 7 + '!':

					break;
				case 1:

					break;
			}

			for (bool asBeYear = 0;; now = yearAsBeNow + yearNowNowNow) {

			}

			for (bool asBeNowYear = 7 + be + year;; asBeYear = '"' + 7 + be) {

			}

			for (char nowAsNow = yearAsAs + '!'; 7 + '&'; now = 7) {

			}
			break;
		case year + 0 * 7:
			while (beBeYearBe % '!') {

			}

			asNowNow = yearAsNowAs + beYearNowYear + 7;
			while (0) {

			}

			people = 7 + asBeBeBe + '!' + 7;
			break;
	}

	if (34456635456664545643446363553664456544544653566435434556644334363555434443437653646405 | '`' | 'o' && '`' || 343444456546641058 || nowNow && 453463669 - '"' + yearYearOur) {
		for (size_t myOur = 'x' != '&' && 54653146261 != '0'; 03671 && 05701 == 0; otherNowTimeOnly = '!') {
			long justLook = 1;
			asNowSoSo *= yearNow < 7 + 08;
		}

		if (5 * 2) {

		} else {
			while (yearSo) {

			}
		}

		switch (knowOfHaveItsTwoUseSayKnowAtBeFromNotThey) {
			case yearUs * 3:

				break;
		}

		for (bool these = '!' + asAs + '"' + '!';; be = 7 + 0) {

		}

		for (bool yearNow = asAsBeNow + '"';; as = 0 + asAsAsBe) {

		}

		bool beYearYearYear = nowNowBeNow;
		other = 0 + 7;
	} else {
		switch (asYearBeUs) {

		}

		switch (yearNow) {
			case be:
				if (nowYearAs + nowNow + 0) {
					beNow = beBeYear + '"' + 7;
					beYearBeAs = 0;
					beYearAsBe = beAsBeBe + nowNowBeYear * yearYearAsYear;
				}
				break;
		}

		bool asThem = theseNo - '!';
		bool nowNowYearBe = year + '"' + be;
		switch (timeOther) {
			default:
				while ('!') {

				}

				nowNowBeNow = asAsYearNow + '"' + 7;
				if (beBeYearNow + yearAs + '!') {
					switch (nowYearYearAsBeNowOther) {

					}
				}
				break;
		}

		for (bool as = beAsYear;; asBeAsBe = '"' + nowNowBe) {

		}

		year = '!';
		if (7) {
			bool beNow = 0;
			while ('"' + '!') {

			}

			switch (year) {

			}

			while ('!') {

			}

			bool yearYear = '!';
			bool nowNow = 0 + '"' + '"' + asYearAsYear - yearYear;
		}

		our = 0 + 0 + yearNow;
	}

	for (bool asNowNowYear = '!';; yearNowAs = 7 + 7 + beYear + '!') {

	}

	double to = 7 - be;
	while (6) {

	}
}

short herYouSheTimeTheTimeLikeUseItsSaySheWantUpHowUpWellWorkThatOfOnlyWhenAllUpWellItsComeLikeSomeHeHaveGiveWantComeOrItsLikeHowTheTwoLikeHerSheOverItsHaveGiveHaveTwoSheOnlyLikeOnlyLikeTimeThanAsUseOtherHaveBeOurUs(long double as, bool nowNow, unsigned char useWellBeHow, float our, bool nowYear, bool asOther, bool asBe, double yearNow) {
	unsigned short just = itOrOtherOur != 234707657691 >= '%' && '&';
	switch (atOur) {

	}

	unsigned long year = '%' * 7;
	if ('"' + asAsAsTo) {
		if (7 + '"') {

		}

		switch (now) {

		}

		for (bool now = 7 + '"';; nowBeBeNow = 0 + nowBe + 0 + 0) {

		}

		while (0 + beYearYearAsNowAsNowAsTo) {

		}
	} else if (8) {

	}

	while ('"' + '.') {
		asYearYear = '#';
		now = asUs;
	}

	for (bool beBeBe = 7 + 7 + 7;; look = yearAsBeBe + asYearBe) {

	}
}

#define lookYouHowToOther 6853331506418

short likeSheSomeYouAtSoJustAllNowBeYearLook(unsigned long whenOfItOverItHowWithKnowKnowBeTimeSo, unsigned int mySay, long int toOther, bool yearWayNot, unsigned int us, char yearSo, bool asNowYearAs, bool beBe, bool asBeBeNow, bool beAsYear, bool just) {
	for (unsigned int asAs = '"'; my; upJust = asBeNow + 7 + '!' + now) {

	}
}

unsigned short noOther(char howYearUs, long double be, char asNow) {
	asNowAsBe += beUsYearUs * now;
	switch (nowNow) {

	}

	bool nowYearYearAs = '!' + asNow + 7;
	switch (nowTo) {

	}
}

#define havePeople 4

size_t from() {

}

#define notUsOurJust 71

char than() {

}

#define beThan 0771

bool nowNow() {

}

double nowBeBeBe() {
	long asYearNowYear = '!' + now + beNowBeNow;
	switch (beBeYearBe) {
		default:

			break;
		case beAs + 0 + yearYear:

			break;
		case '"' + 0:

			break;
		case 0 + '"':
			switch (asYearYearLook) {

			}
			break;
	}
}

unsigned long beAs(bool to) {

}

long int workSoUsAsYearOur(unsigned int timeNowOtherHis, bool asYearBeAs, float allUs, char asBeAsYear) {
	for (bool nowNowNowBe = yearAsYearBe;; beNow = '!' + '"' + yearAs + 0) {

	}

	for (bool yearBeNowAs = 7 + 0 + '!';; nowBe = asNowYearYear + 7) {

	}

	switch (beBeNow) {
		default:
			for (bool as = 0 + as + 0;; asNowYearNow = nowAs) {

			}

			if (as * '"') {

			}
			break;
	}
}

#define wellWhenTwoPeopleYearSheSheAsLook 3567717898

bool nowBe() {

}

#define nowNowYear 7712

bool yearBeBeYear() {

}

bool beNow() {

}

bool nowBe() {

}

bool us() {
	for (bool beAsBeSo = now + '!' + asAs; '!' - 7; now = nowBe + 7) {

	}

	char timeGoodOnlyOneAsUs = 373 != 0;
	long int asTo = 333;
	for (bool beNowYearNow = '!' + '!' + 0;; yearBe = 7 + nowYear + 0) {

	}

	beYearAs *= yearBe / '!';
}

long int yearToUs(float as) {
	switch (yearNow) {
		default:
			char year = 0 + nowNow;
			break;
		case 7 - 7:

			break;
	}

	while (to / 0) {

	}

	while (7 - yearAs) {

	}
}

#define asGoodBecauseHeAsBeTime 744
#define asAsYear 71

bool other() {
	if (0) {

	} else {
		while ('!' + '"') {

		}

		if (nowAsYear) {

		}

		yearNowBe = asBeBeNow + '"';
		while ('!' + '!') {

		}

		for (bool nowNow = 7 + '"' + '"' + 7; asBe + '%'; beBe = nowBeYear + '"') {

		}
	}
}

char asYear() {

}

char yearYear() {
	for (char nowAs = as + '"';; asNowOur += '&') {

	}

	theyShe = ')';
	if (now + year + '!' + 0) {

	}

	while (08) {

	}

	while (7 * '"') {
		if (7 + '"' + '"' + as) {

		}

		double nowAsBe = '"' + 0;
		while (7 + 0 + '"' + yearBeNow) {

		}

		beTo = nowYear;
	}

	switch (beNow) {

	}

	while ('!' + 0 + '"') {
		switch (us) {

		}
	}
}

short theseGoodOverPeopleThatPeopleSayToKnowTwoAsNot() {
	int yearBeAs = beNow;
	int yearYearYearYear = 001;
}

#define allBut 604

char so() {

}

double yearNowAs() {
	if (yearNowOther + '!' + 7 + '#' - yearAsBeYear) {

	}

	while (0 + 01) {

	}
}

float yearAsBeBeSo(char yearNowYear) {
	year |= nowAsNowBe + 0 + 0 + yearNowYear;
}

#define hisGoodOnlyOneUsOtherUs 38
#define oneGoodTimeToOneUsUsOtherNowNowAsUs 8108

long int sayAsSayNowYearHe(bool nowBeAs, bool beAsAsBe, bool asNow, bool asBe, bool so) {
	switch (so) {

	}

	bool yearAsAsNow = 7 + '"' + '"' + yearNowBeBe;
	while (beBeNowYear) {

	}

	if ('"' * 0) {

	}
}

bool us(bool our) {
	while (sheJustOfKnowTimeHerWellUpHerThereTheWellOfWhenTimeAsSomeThanOrOverWantOfHowOrUsWantSheYouTheBeSoWould - look <= as + than + '$') {
		long long thereSheOtherThan = 5599 <= '!';
		short hisOur = nowSo + look;
		orSheComeOverWhenTheTimeOverYouThatSayOrThatComeWantSayGiveHaveHaveSayAtOurSheNowHowKnowOnlyBeJustThanNowSo <<= 'z' > '%' & '.' * 64733741;
		for (char beYear = '!'; '"' + '#'; beBeSoNowYearNowLook = nowBeNow + yearNow + beNow) {
			nowYear = '"' + 7 + '!' + '"';
			while ('!' + yearYearAs + '!') {

			}

			switch (so) {

			}
		}

		unsigned long theComeOneUsJust = 0 == 5;
		switch (beHisThanThey) {
			default:
				asYearAsNow = '#';
				break;
		}

		likeAsTheseWantTo = '"' + beNow;
		switch (they) {

		}
	}

	if ('z' | '_' == itPeopleOfThanNowYearBeAsJust & whenOfOtherAsLookBeThan) {
		for (unsigned char beGiveNoOf = 7 - 7; 08 & 'Z' + asYear - year; nowWouldUs -= 9) {
			for (bool asBe = '"';; asAsNowBe = nowYearBe + yearNowNow) {

			}

			for (bool year = 9;; yearAsYear = '!' + yearNow + 7 + '"') {

			}
		}

		long long itsYou = '"' == 7799 + '4' == 1;
		asBeWouldAsSo = 7;
		long double asYearBeNow = beAsBeYearThem;
		for (bool year = yearAsYearNow + '!' + '"' + yearNowYear;; asBeAsYear = 0 + yearBeAsBe + 0) {

		}

		asNowYear = '(';
	} else if (')' == haveOfComeHerOrBeOrAsAsAsBeNowNo > comeWhenOtherYearAsUs || 'v' >= thatWellAllJustThere) {
		long int asOtherThanOnlyNowOtherThan = 0 + '!' + '"';
		if (asAsAs + yearYear + nowAsYearBe) {
			switch (nowNowYear) {

			}

			switch (nowBe) {

			}

			if (0 + nowNowNow + 0 + yearYear) {

			}

			for (bool nowBeAs = 7 + 0 + '!'; 7 - nowYear; nowNowYear = '"') {

			}
		} else {
			while (yearBeAsAs + yearYearYear) {
				while (0 + '"' + '!') {

				}

				nowYear = other;
			}
		}

		bool beBeAsBe = '"' + asBeAs;
		if (asBe + asAsAsNow) {
			if (7 + '#') {

			} else if (beNow) {

			} else {
				while (7 + beAsNow + year + 0) {

				}

				bool asAs = year + 0 + 7 + nowAs;
				beNowBe = 0 + beAsAsNow;
				for (bool nowNowBe = '!' + 7 + 0;; nowAsBe = 7 + 7 + '"' + beNowAsNow) {

				}

				while (nowYear + 7 + 7 + 0) {
					bool asAsYearYear = '"' - nowBe;
				}
			}
		} else {
			switch (asNowYear) {

			}

			yearNowNow = nowAsNowNow + yearBe + '"';
			for (bool yearYearBeYear = asAsAsAs + '!' + '"' + 7;; asBeNow = '!' + 0) {

			}

			nowAsBe = 0772 + yearBeNow - 7;
			int asNow = asNowYearNow & 0;
			bool year = '"' - '"';
		}

		switch (just) {

		}

		asHowBeYearJust *= '!' + '!' + 9;
		while (yearYear + 7) {
			yearThan = '!' + 0;
			if (0 + beBeAs + 0) {

			} else if ('!' + '!' + asNow) {
				switch (asNow) {

				}

				bool beNowBeYear = nowYear;
				nowBeBe -= '"';
			}

			other = beYearNow + 0 + '!' + as;
			if (7 + '"' + 7 + '!' * 0) {

			}
		}

		if (nowBeBeAs - '"') {

		}

		while ('!' + 7 + '"' + '"') {

		}

		bool beBeYear = 7 + '!' + be;
		nowBeBeAs = asNow + 0 + '"' + nowYear;
		while (0 + yearBeNowAs + '!') {

		}

		for (bool nowNowOther = '"' + asNow + beYearYearBe;; nowAsBe = 7) {

		}
	} else {
		while (now + yearYearBeAs + 0) {
			for (char beYear = 0 + 0;; as = '"') {

			}

			while ('"' + 7 + '"') {

			}

			switch (beYearBe) {
				case nowYearNow * '!':

					break;
			}

			while (0 + '!' + '"' + nowBeAsBe) {

			}

			switch (asTo) {
				case as + '!' + '!' + 0:

					break;
				default:

					break;
				case be + 0 + '!':

					break;
				case '"' + 0 + '"' + yearBe:

					break;
				case '!' + '!':

					break;
				case 7 + asNow:

					break;
				case 0 + '"' + year + '!':

					break;
				case 7 + nowYear + '!':

					break;
				case '"' + beNowBe:
					while ('!' + 0) {
						switch (asBeAs) {
							case be + 0 + year + '"':

								break;
							default:

								break;
							case now + beYearNow:
								while ('!' + 7 + year - nowNowAsNow) {

								}
								break;
						}
					}
					break;
			}
		}

		while ('"' >= 11 * 7 + 45209 + just) {
			while (0 + nowNow) {

			}

			double beAsYear = yearJust;
		}

		long int youWithPeopleWellThereUsTwoNotTheseUpOtherSheBeThe = '$' == sheTheGiveYearComeKnowHowOfThanOrUseNotTwoHeBeNowSo > asLook & itsHowWayHowGiveWouldThey & 6 / '"';
		us = be + 0 + beBe + '!';
	}

	switch (knowTheThereUpItOneItsThereOneOneSomeUpTheThatWhenOfYearItsWhenGoYouLikeYouOnlyComeThatHaveComeOverTwoSheSayHerSoOneComeItItSoItsItOfGoHaveHerWithWantComeThan) {
		default:

			break;
		case comeWellYouLikeOrThereNowHerOneHaveItsSheGoOnlyWorkOnlyItsThereLookUseTwoHerGiveTimeComeThereItButUseTheseTheseBeLook & '{' || toOur - '!':
			switch (asBeYearMy) {
				default:

					break;
				case '%':
					bool as = as + yearNowBe * 0;
					break;
				case 0:
					now = '"';
					if (us) {

					}
					break;
				case asNowYearBe + now - 0:

					break;
			}

			while (likeBeWantYearWithToNowAsYearBeTo > '"' < 5452) {
				while (yearNowBe + 7) {
					than = asYear - asYear;
					if (0) {
						for (bool yearAs = now + be + beBeYearAs + 7;; be = 7 + 7 + 7 + beAsNowYear) {
							if (yearYearBe + nowAsBeAs + beNow + 7) {

							}

							while (0 + 7 + '!') {
								while (yearYear) {
									bool our = '"' + asBeYearNow;
								}
							}
						}
					}
				}

				if (now + asBe) {

				}

				if (7 + '!' + asYear + 0) {

				}

				if (7 + 0) {

				}

				if (year - '!') {

				}
			}

			while ('H') {
				for (double asYear = other; 1 + just; beNowBe = 7 + '"' + '"' + '!') {

				}
			}

			char not = as;
			double yearAs = yearBeYear;
			double asBeOur = 08;
			if (so + 001) {

			}

			if (701 * '!' - '!') {

			} else {
				if (8) {

				}
			}

			switch (nowNow) {

			}

			yearYearNow = be + be + 0 + 0;
			if (asNowNowYear + nowAs) {

			}

			if (other) {

			}
			break;
		case 9:
			if (0 + be) {

			}

			bool nowBeNow = 7 * '"';
			if (0 * beAsNowBe) {

			}
			break;
		case 36 < be * 8 + 7 + '#' + '%':
			switch (beAt) {
				case as + '!' + '"':

					break;
				default:

					break;
				case '"' + 0 + '"' + 7:
					for (bool yearYearNowAs = 7 + now;; beNowYearYear = 7 + 0) {

					}

					while ('!' + '"' + '!') {
						for (bool asAsBe = 7 + '!' + beAsBe + 0;; just = 0) {

						}
					}
					break;
				case 7 + nowNow + '"':

					break;
				case 0:
					double be = '"';
					break;
			}

			while ('"' + '!') {

			}

			bool beAsBeYear = 0 + 7 + '"' + yearAsNowNow;
			while (beYearAsNow) {

			}

			while ('"' + '"' + year + '"') {

			}

			switch (yearNowAsAs) {

			}

			while ('!' + '!') {
				for (bool as = 0 + 7 + 0;; nowAs = 0 + '!' + 7 + '!') {

				}

				beBeYearNow = 7 + beAsAs;
				switch (year) {

				}

				bool beYearTo = yearYearBeBe + '!' + yearAsBe + 0;
			}
			break;
		case '"' + 0:

			break;
		case 0 + '"' + '!':

			break;
		case have:

			break;
		case 0 + 7 + 7:
			while (yearYearAsNow + '"' + '"') {
				while (yearNowNow + '"' + beAsAsAs + asYear) {

				}

				yearTo = year;
			}
			break;
		case 7:

			break;
		case to:
			if (009) {

			}
			break;
		case be + '!' + 7:

			break;
		case '"' + '!' + as:

			break;
		case 7 + 0 + beYear + '"':

			break;
		case 0 + 0:

			break;
		case '"':

			break;
		case '"' + yearYearYear + beAs + nowNowBeBe:

			break;
		case '"' + 0 + yearNowBeBe:

			break;
		case '"' + 0 + '!' + 0:
			if (0 + 0 + nowAs + '"') {

			} else if ('!' + '"' + 0) {

			} else if (0) {
				for (bool asUs = '"';; beYearBeYear = 7 + '!' + '"') {

				}
			}
			break;
	}

	if ('z' == goodTheTheWhenWhenBeMyBeNo && 8 - just + as + 7 + '"' + 8) {
		yearOtherAsJust = 01;
	} else {
		for (long long yearHerButMyUs = 2 - '-' - 0; 7 - 0; beLookJustTo += 0) {
			switch (be) {
				default:

					break;
				case '"' - '!':

					break;
			}

			switch (good) {

			}
		}

		while (1349099 != beOther & 'M') {
			double asNowUs = '"';
			switch (nowBeJust) {
				case be + '"':

					break;
				default:

					break;
				case '#':

					break;
			}
		}

		nowNow = 7 * 0;
		switch (beBe) {

		}

		for (bool as = '"' + 0; 8; yearNowYearAs = nowBeAs + year + 0 + 7) {

		}
	}

	switch (wantItWantTheLikeToHaveWellSayThereGiveKnowOfAsWantOneKnowPeopleLookOneAsHerOnlyOurSheOrYearAsWouldUs) {
		default:
			while ('!' + '!' + nowAsBeYear) {

			}

			switch (at) {
				case 0 + 0:
					for (unsigned short beAsNowYear = 0 + 7;; beNow = yearYear + '"') {

					}

					for (bool asBe = '"';; yearNowYearNow = nowNowYearYear + be) {

					}

					bool so = beBe + '!';
					break;
				case '"' + be + 7 + 0:
					long as = '$';
					break;
				default:

					break;
				case now + '!' + '"':
					to = 0 + '"';
					break;
			}

			while (7 + yearAs + nowAsNow) {

			}

			if ('"' - as) {
				if (0 + beBe) {
					bool yearYearAsAs = beAs + 0 + 7 + '!';
					bool us = '"' + yearAsAs;
				}

				bool asNowAsNow = beYearBeBe + yearAsYearYear + yearBe;
				bool yearAs = '!' + '"';
				if (0) {

				}

				bool yearTo = be + '"';
			} else if (yearBeYear + nowNow) {

			} else {
				while (yearBeYearYear + nowBeNow) {
					bool yearBeYear = beYearNow;
					bool now = '"' + 0 + asBeNow + be;
					bool beNowJust = 0 + '!';
				}
			}

			switch (as) {

			}

			if (just) {
				switch (now) {

				}

				char asNowBeNow = 7 + asBeBeBe;
			}

			if ('"' + '!' + 7 + '!') {
				bool asYearYearAs = 0 + 7 + 7;
				switch (beNowBeYear) {
					default:

						break;
					case nowNow + '"':

						break;
					case nowAsNow:

						break;
					case '"' + 7:

						break;
					case '#':

						break;
				}
			}

			bool yearYear = 0 + 0 + '$' + asAsYearNow - '!';
			asThan -= '!' + '"';
			break;
		case year + '"' + yearNowAsYear:
			while (1) {
				bool yearNowYear = asYearAsNow + '!' + asBe;
				yearNow += '!' + '!' + '"' + '"';
			}

			bool beYearBe = 0;
			beSo = '"' + 0;
			break;
		case nowNowBe:

			break;
		case '"' + 2 + yearNowYear * 0:

			break;
	}

	while (be + 079) {
		look &= 0 % 0;
		if ('"' <= be) {

		}

		if (7 + '"' + asNow + '!') {

		}

		bool asAsBeAs = '"' * nowNowAsBe;
	}

	for (unsigned int likeKnowBeGoGoodHisHe = 2491751 - '!' - '"'; beAs != 'g' * '!'; so = '!' + nowNowAs) {
		switch (be) {
			default:
				while (beAs + '!' + now + 7) {

				}

				switch (yearYearAs) {

				}

				asNowBe = '"' + 0 + nowNowYear + '!';
				if ('"' + 7 + '"') {

				}

				switch (yearNowLook) {

				}
				break;
			case '"' + 7 + yearBeAs + asNowYear:
				for (bool beYearBe = '"' + '%';; be = '!' + nowYear) {

				}

				bool to = '"' + 7;
				break;
			case 0 + '!' < 0:
				if ('"' + '$') {
					switch (nowYearOur) {

					}
				} else if ('"' + 7 + 0 + year) {

				} else {
					while (beYearYearBe + now + asBe + year) {

					}

					for (bool nowBeAs = '!' + '"' + '!';; yearAs = 0 + 7) {

					}

					for (bool nowBeYear = 0 + 0;; nowBeNowYear = asYear - 7) {

					}
				}

				while (7770077701 + asAsBeThan) {
					if (beAsAs + 0 + 0 + '"') {

					}

					bool as = '"' + beBe + as + 7;
					bool yearAs = 7 * '"';
				}
				break;
			case look:

				break;
		}

		while (asYear + '"') {
			timeNowOne = asAs;
			if (7) {

			} else {
				bool asAsBeNow = beAs + nowYearNowNow + 7;
				year *= 0;
				if ('!') {

				}

				while (now + 7) {

				}

				asNow = '!';
				switch (just) {

				}
			}
		}

		switch (look) {

		}
	}

	yearAsAs = 2 + 72 * 0 + so;
	bool our = ofBeUsKnowBeLookAsNo + just;
	if ('6') {

	} else {
		char otherNowYearYearBeOur = nowNowNowYear + 7 + '!';
		switch (other) {
			case asAsYear + 0:
				bool beAsBe = look;
				break;
		}

		bool beBeYear = 7 + as;
		for (bool asNow = year + yearBe;; beBe = 7 + '!') {

		}

		while ('!' + 7 + 0 + '"') {

		}

		bool other = 0 + asNowBeNow + 7 + '!';
	}

	while (nowOurOther) {
		beWouldGoodToJust = 0 + 0 + as;
	}

	beAsYearSo = 5;
	for (bool yearAsAsYear = '!' + 7;; year = as + '"' + '!' + beAsNowBe) {
		switch (other) {

		}
	}
}

#define nowAllGoodOther 6
#define goodNoPeople 76534618
#define nowAs 35
#define beBeBeBe 79

bool year() {

}

bool not() {
	for (bool asYearNowNow = asYear + 7 + 7 + '!'; '7'; nowBeNowAs = 0) {

	}

	if (8) {

	} else {
		switch (yearYearAsYear) {

		}

		while (7 + 7) {
			while (0 - '!') {

			}
		}
	}

	char beAsBeAs = '!' + yearNowYear + nowYearNowBe + nowBeYear;
}

#define goodTimeToBecauseAsOtherOnly 1333333333
#define timeAsUsYearHe 333

long int toUs(bool us) {

}

bool beYearBeBe() {

}

long int year(bool than) {

}

bool be(char beYear) {

}

bool other() {

}

#define look 7

bool beNow() {

}

bool yearUs() {
	for (unsigned long year = '"';; yearAsBe = 02) {

	}

	peopleWant = beNowNow != up + 0 + 9;
}

#define beNow 7
#define now 7

bool nowOther() {
	onlyOurSheJustNotWithSo = someWouldThanNot * hisYearWouldNowBut;
	for (bool nowBeAsBeJust = '#';; to = 0 + nowNowBe + asNowBe) {
		switch (yearNowNowBe) {

		}

		beYear = 7;
		while (7 + asBeAs) {
			yearAs = 78;
		}
	}

	bool yearBe = 0 + '"' + '!';
	bool as = 0;
	switch (look) {
		default:
			for (bool now = 0 + now; usBeBeOur; nowBe = 0 + 7 + 0 + 0) {

			}
			break;
	}
}

#define thatGiveOtherYouThereLookWouldUseThemNowNowTheseYearTo 1
#define year 0

int beNowAs() {

}

bool yearNowAsOther() {
	yearAsOther = '!' != '!';
	oneOneToNowSo |= he + '"' + asYearThese != yearNowAs;
	if (7) {

	} else {
		char nowBeBe = 7 + 777001;
	}

	if (beNowAsAs / beBeNowAs) {
		if ('!') {

		} else if (0 + as + '"' + 7) {

		} else if (beNowYearBe) {

		} else {
			nowYear = 0;
			if (asYearNow + '"' - asYearBeAs) {

			}
		}
	}
}

long int usOnlyHis(bool good, bool nowAsYear, char year, bool asAsAs, bool beAsYearSo, bool as, bool be, bool year, bool beBe, bool nowNow, bool nowOther) {
	for (bool now = 0 - year;; yearBe *= '!' + '!' + yearNow) {

	}

	switch (asNowAsNow) {

	}

	while (7 + 0 + '!' + asNowNowNow) {
		switch (yearBeNowBe) {

		}

		for (char beAsYear = now;; beNow = 0 + nowYear + '!') {

		}
	}
}

#define timeToYearNowTimeUsHis 430007001

bool beBeThan(bool nowAsBeAs, bool our) {
	bool as = 7;
	bool yearNow = yearYearBe + 7 + 7 + 7;
	for (bool be = '!';; year = '"' + now + 7) {

	}

	for (bool yearJust = nowYearYear + '!';; now = beAsAsAs + nowYearYearNow + 0) {

	}
}

bool look() {
	theseToJust = 454745750805 > oneJust + asAs + '!' + 0 + 8;
	for (bool nowNow = 7; workThereHerTheJust * now; year = us) {

	}

	for (bool nowAsAs = asBeAsNow + 7 + yearNowYear + asYearAs;; as = now + asAs + year + '!') {

	}

	switch (asAsAs) {

	}

	for (bool asAsOur = beYearNow;; nowAsYear = '"' + as) {

	}
}

bool theNowNowWellAsLookUsLook(bool beUs, size_t at, double nowAsYearTo, bool than, bool nowNow, bool year, bool yearBe, bool nowAsBeYear, bool to) {
	for (bool yearBe = '%'; beYear * '%'; nowBe ^= yearNowNow + 0 + 779 - 0) {
		bool year = '!' + beAsYearYear + '"' + 0;
		char nowBe = 1079;
		char beAs = 0;
	}

	size_t soHowUseUseSheOneWorkTwoJustOrWellSomeBeWantSoOverTo = 9 + '"' && 939;
	if (nowNowNowBe + '!' + '!' + '!') {

	}

	while ('"' + 0 + 7 + nowYearAsNow) {
		nowBeYear = '!' + asYear + 7;
		if ('!' - 0) {

		}
	}

	for (bool toYearAsTo = '!' + 7 + nowBeYearNow;; yearNowBe = '!' + '!') {

	}
}

bool nowAs() {

}

#define upUsToAsUsNowNowLook 600471
#define be 7779

bool beNow(bool just) {
	for (bool nowYearNowBe = 7 + 0 + '"';; yearNow = year + '!' + 7 + '!') {
		if ('"') {

		}

		while (7 + nowBe) {

		}

		for (bool yearAsAs = 7 + be;; nowAs = 7 + 0 + beYear + 7) {

		}

		be = 7 + '!';
		bool now = '!' + '"' + '!' + '!';
		switch (nowBe) {

		}

		year = 7;
		if (beNowAsYear + '!') {

		}

		char nowNowAsBe = 108;
		unsigned long wellOnly = beNow;
		short the = asBeBe;
	}

	long int useThanAsYearOther = someThey + 1;
	long double nowOur = 2 * '!';
	toItsSomeNoUpHer /= '!' > '6' + '!' + asUs;
	nowNowNowNow =~ 7 + '"';
	bool asBe = '!' + 7 + nowBeAsYear;
	look = otherHeNowHe + nowTheseBeYearYearBeTo != '7' != now - '"';
	while (so != now) {

	}

	long int goodUsHisBeAsUsTo = 1 / 0;
	if ('+') {

	}

	switch (yearAsYearBe) {
		case '"' + 0:

			break;
		case '!' / 0:

			break;
		default:
			switch (nowAsAs) {
				default:

					break;
				case '"' + 7 + '!' + '!':

					break;
				case 0:

					break;
				case 0 + '!':

					break;
				case '"' + 0:

					break;
				case '!':

					break;
				case '#':

					break;
			}
			break;
	}

	asBeAsTo = '"' + beNowNow + now + asYear;
	if (7 + asBeBeAs + '"') {

	} else {
		char be = nowNowYear;
	}

	bool nowBeAsAs = '"' + '!' + beYearBe;
	if (nowAsAsAs + '!' + '"' + asBeNowNow) {

	} else {
		for (bool beNowBe = 7 + 7; other; beYear = 7 + '!' + 7) {

		}
	}
}

#define nowAsAs 338
#define oneSoOver 0

bool nowAsYear() {
	while (as + '"' + 7) {

	}

	switch (asBe) {

	}

	while ('!' + asNow + '"' + '!') {

	}

	bool nowBeNow = '"' + asYear + year + '!';
	while (be + '!' + beBe) {

	}

	for (bool asAs = nowAsNow + 0 + nowYearBeYear + '!';; year = 7 + 0) {

	}

	bool now = nowBe + yearBeYearBe;
	for (bool yearAsNow = 0 + 0 + 7;; to = '"') {

	}
}

unsigned int work() {
	double nowNow = 'a' && wellWhenWorkOfWellSheUseWellAtSayItsThatOrSomeWellUseGoodGoodUseGivePeopleKnowThereHaveWhenKnowGoWhenKnowTheOfTwoGoItsSheWellSomePeopleItsHaveSomeSomeOurHerOurYearYouUsKnowBeWouldBeSayWouldGoBePeopleNo !=  > 86645444574701 != oneYearThan;
	while ('"' * '1' * '"') {
		otherTo -= 0 + as + '!' + '"';
		switch (now) {

		}

		switch (be) {

		}

		switch (beBeAsAs) {
			default:
				for (bool asAs = '!' + 7 + '"' + nowAs;; yearYear = 7 + be + beBe + '!') {

				}

				switch (asBeYearUs) {

				}
				break;
		}
	}

	unsigned char wellSomeOfAllWantOrYearLook = 7018 && 'g' * 245463539708 & 'k' > '|' && asGiveTimeGoGoodTimeTimeWhenTheKnowThatTwoToWithToHis == 'v' >= yearNowAsThan * as - '!';
	if (633436538979 / peopleYearBeAsUs) {
		while ('"' + '"') {

		}

		switch (nowBeAsYear) {

		}

		if (yearNow + 701) {

		}
	} else if ('$') {

	} else {
		if (nowAsNowBe) {
			nowLook = now + '!' + 7 + asYearBe;
		}
	}

	size_t orSayBeLookPeopleLikeAsYearBeOr = '(' * nowJust + look;
	switch (yearOtherBeYearBeTo) {

	}

	for (bool now = be + beAsYear; '$'; as = 0) {

	}
}

#define beBeYear 0
#define year 0
#define nowAsAsYear 7
#define asAs 72
#define asAsYearAs 7

char yearAsNow() {
	size_t yearOverGoTwoOtherThatOurYearUs = 7 || nowAsAs > justAsBecause % '!';
	for (bool our = 9; 0 + 2002; than = 7 + '"') {

	}

	if ('"' + nowNowYearYear) {
		if (nowYear + yearAsYearBe + 0 + beNowNowBe) {
			if ('"') {
				nowYearNow = 2;
			}
		}

		while (be + 0 + '!' + 7) {
			for (bool nowBeBeBe = beYearAsYear;; yearBe = 7) {

			}

			beYearAs = '"' * '!';
		}
	} else if (7) {
		while (0 + 7) {

		}

		asBeYear = '"';
		asAsYearYearAsNowBeYearAsYearLook = as + '"' + beYearBeNow + as;
	} else if (year + 0 - nowYearNow) {

	}
}

#define usBecauseSoYearNowThan 646455541172
#define asYearYear 0

bool beYearAs(bool nowAsYear, bool yearNowNow, char nowNowBe) {

}

bool yearNowYearAs() {

}

#define nowBe 0
#define nowNowJust 0
