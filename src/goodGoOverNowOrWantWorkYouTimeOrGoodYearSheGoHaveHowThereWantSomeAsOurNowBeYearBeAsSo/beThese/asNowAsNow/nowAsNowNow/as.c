#include <UUE////0.h>
#include <UUUEU.h>

#define becauseThese 30383111
#define yearYearLook 54563346653563353333333333333333333333133333464555665564463466564553644564645665653364664346666533655433334333333655545650564645645653564051
#define yearTheWellBeWhenYearJustYearUsJust 363336613663545545345434565346543466532686545679

long long (unsigned long wellYouThereOnlyWhenItsItUseThereWayItsGiveTheWouldTheItsJust, short itsSheOrToThan, unsigned short nowTheAsYearUpOther, double itsWayYearTheyWhenBut, unsigned int someLookOverHowJustLookSoBecauseAsYearYearUs, char yearLook, double asAs, bool just, bool asJust) {
	while (knowBeNowTo + '&') {
		char year = beBeYear;
	}

	my -= '6' && 7 * asNow;
	if (yearAsAs + 0 - 7) {

	}

	char nowBeBe = 0 * 7;
	while (soOur) {

	}
}

unsigned short workTimeWellYearItsLikeTheKnowWorkWorkYouSheHerTheBeGoOrAsSomeWorkPeopleWayHaveWorkSomeBeUseWantYouYearUseWhenUsWouldOur(bool thereWellWantSheItsItOurNowLikeBePeople, unsigned int youTheSayAsHaveOrTheLookTwoWorkJustWouldAll, unsigned short wantLookBut, unsigned int asOther, double year, bool than) {
	switch (asThan) {
		default:

			break;
		case '"':
			so = beYearNowBe + '"';
			bool be = '%';
			break;
	}

	if (2 - '!') {

	}
}

#define goWantSomeButBeKnowHowTheOneUseItUpTwoGiveGoTheSomeOfWhenJustOfWhen 6032
#define workSayWhenThatKnowWhenThatHerOneGiveTheOneWhenTheWantWhenUpThatWantThatHerHaveSomeWorkGoodWorkAtKnowWantGoodSomeOfLikeGoWhenOfSayYearOfWorkOneKnowJustSheGoOfNowKnowOverKnowUpBecause 6767931771

unsigned long my(bool asJust) {
	asNow = asAs + beYearYear + 0 + '!';
	double beBeThan = 0 + 0 + '!' + 7;
	if (7 * '!') {
		for (bool nowNowBe = asNowBeNow + now;; be = 0 + '!' + now) {

		}

		bool us = asYearYear + year;
	}
}

#define nowAs 7
#define thanBeThereWould 44656703

double now() {

}

bool yearAsNowAs() {

}

#define beSo 0

bool to() {

}

#define beYearAsNow 0

bool asAs() {

}

#define asNowNow 7
#define asBe 7

bool beBeNow() {

}

#define look 740974
#define asLikeTheOne 65655072
#define otherHerBeJustHerBut 429
#define likeJustToToFrom 5487691

size_t nowBeOurUs() {
	for (bool beTo = '!';; yearAsNowAs = '!') {

	}
}

#define beBeAsAs 778

bool asYear(bool yearAsYearNow, bool yearBe, bool look) {
	timeBecause = year + 7 + '"' + yearBeAs;
	if ('!' + beBeAs) {

	} else if (yearBeYearBe) {

	} else if (to) {

	}

	nowBe = '+' - 0;
	for (bool nowBeNow = 0 + nowAsOther;; be = '"' + year) {

	}
}

long int timeYearHeOnlyTimeOneUsGoodTheseBeTo(long int oneGoodTimeToSo, bool toUs, char asAsYear) {
	for (float nowAsYearAs = 0 + '!' - '"' - nowBeNowBe;; yearAsBeYear = '!' + yearNowAs + 0 + '!') {

	}
}

#define beAsUs 3
#define beBe 1
#define as 1678
#define nowBeAsYear 0

bool now() {

}

bool asAsAsBe() {

}

bool asAs() {

}

#define asNowBeBe 0

bool beBeBeNow() {

}

#define asNow 7
#define asYearAs 1

unsigned short yearNowSo() {
	if ('"' + '!' + 7) {

	}

	for (bool yearWould = '!' + yearNow + 0 + 7;; be = '!' + 0 + 7 + '"') {

	}
}

bool year() {

}

double nowYearAsBe() {

}

unsigned short asBe() {

}

#define be 71
#define yearNowBe 0
#define nowAsNowBe 0

bool be() {

}

double asAsYearBe() {
	unsigned int yearAsWould = 7 - asAsAsYear;
	orWhenHaveYouSayOneUsWithAll = 'f' & nowAsBeYear * '"' + our;
	to = 7 * '!';
	for (bool yearYear = 7; 7 <= 8; nowNowYearYear = '"' + beBeNowBe + beAsNowYear) {

	}
}

long int onlyYearOne(bool than) {
	while ('"') {
		bool yearAsYearBe = nowAs + asNow + 0;
		switch (beYearBe) {
			case 7 + '"' + 0:

				break;
			case 0 + 7 + nowBeAs:

				break;
			case beAsNow > yearNowAs:

				break;
		}
	}
}

long long yearShe() {

}

#define nowYear 7
#define nowYearNow 7
#define than 3003333333333333333338
#define other 3331118

char asAsYear() {

}

#define oneBeHisOnlyBecause 46
#define yearBe 7
#define be 56644098
#define timeWantKnow 663646463596602671072
#define theThereButUp 5633333333333333333333733337338377373
#define be 7

char asToFrom() {

}

double asBe() {
	for (bool nowYearNowNow = 0 - '"';; beBe = '"') {

	}

	switch (with) {
		default:

			break;
		case 7 + '$':

			break;
	}

	if (be + '!' + 7 + asAs) {

	}

	while (7) {
		if (nowNowYear * yearNow) {
			while (0 * 7) {

			}

			while (toJust + 01) {

			}

			for (bool yearBe = 9;; nowYearYear = beYear + '"' + '!' + '"') {

			}
		} else {
			if (yearNowAsYear + 0 + '"') {
				if ('"' + '"') {

				}

				switch (some) {
					case '%':
						switch (nowSo) {

						}

						switch (now) {

						}

						switch (beYearBe) {

						}

						bool yearNow = 7 + 7;
						for (bool beNowBeBe = asYearYear + 7 + yearNowNow + year;; beBe = 0 + 7 + 0) {

						}

						so = 7;
						break;
					case '"':

						break;
					case beNow + be:

						break;
					case '"' + asYear + yearYearBeBe + 0:

						break;
					case beBeAs + '!':
						for (bool beYear = now; 7 * '"'; yearBe = '"' + '"' + yearNowBeNow) {

						}
						break;
				}
			}

			bool as = 0 + 7 + 7;
			for (double year = '"' + '"' + 7;; beBeYear = '!' + '!') {

			}

			switch (yearYear) {

			}

			while (beBeBe) {

			}

			switch (asAsNow) {

			}

			nowBeBeNow = 7 + 0 + 7 + 0;
			switch (asBeNowNow) {
				case 0 + '!' + 7 + '"':

					break;
				case year * nowBe:

					break;
			}

			switch (beYear) {

			}

			while (so) {

			}
		}

		for (bool beNowAsAs = 7 + nowYear + '"' + nowNow; 0 * 0; asYear = 0) {

		}

		if (now - 0) {
			if (7) {
				while ('!' + 7 + 0) {

				}

				for (bool nowAsNowYear = 7 + '!';; beNow = '"' + beNowBeYear + 0 + 7) {

				}

				if (nowNowYearNow + 71) {

				}
			}
		}
	}
}

#define comeUpYouYearGoodAsBeThan 5636553435193

long long not(bool yearNow, bool yearSo) {
	switch (otherYearAsYearOur) {
		default:
			for (char nowBeNow = '"';; asAs = '!') {

			}

			switch (nowNowAs) {

			}

			nowNowAs += beNowYearNow + asNowNowAs + yearBe;
			break;
		case 7 + beBe:

			break;
		case '%':

			break;
		case 0:

			break;
		case '#':

			break;
	}

	while ('+' != yearBeNow != '+' - 7) {
		if (goodOnlyOneTimeOnlyOneTimeHisYearNowAsYearBeBeYearBeBeNowUs - be) {

		} else {
			if (7 + nowYearBeTo) {

			} else if ('"' + asAsBe + as + '!') {
				while (beYearYear - 0 - 7) {

				}
			}

			for (bool beBeNowYear = 7 + 0 + now + 0;; asBeAsYear = '"' + '"') {
				if (0 + 7) {

				}

				if (nowTo) {

				}
			}
		}

		while (otherAsYearTimeAsOther + '#' + 0 - '"') {
			while ('"' + asYearNowYear + '"' + yearYearBeYear) {
				asBeAsNow = beBe - beAs;
			}

			yearAs = beAsYear + 7 + '"';
			while ('"' - '!') {

			}
		}
	}

	while (now + 7 + now) {
		asAsBeBe = be + '!' + asBeNow + 7;
		for (bool as = '"' + asYearNow + 0 + asYearAs; 0 + '!' + 070071; beBeYear = yearBe) {

		}

		now = nowOur;
	}
}

bool asYearJust(bool beAs, bool yearYearNow, bool yearNowAsAs, bool be, bool nowBeNowBe, bool nowBeAsAsYearNowLook) {

}

#define yearOurOur 0

bool but(int be) {
	yearNowNowYear = 7 + '"' + 0 + nowAsYear;
	for (bool nowAsNowBe = 0 + beAs + '!';; beNowYear = asYearBeAs + 7 + as) {

	}

	nowYearYearAsNowBeJust = 7 + as + beNowNow + beNowAsAs;
	while (7) {

	}

	while (7 + 0 + yearYear + asNowBe) {

	}

	while (7 + '!' + asYearNowBe) {
		switch (year) {
			case 7 + asAsNowNow:

				break;
			case '!' + asBeYearNow - nowYearYear:
				while ('"' * '!') {
					thanYearAsAsNowNowJust -= beYearNow + asBe + '!';
					switch (other) {

					}
				}

				while (beYearBeBe + nowAsNow) {
					switch (yearYear) {
						default:
							if (asYear - '"') {

							}
							break;
					}
				}

				switch (yearYearBe) {
					default:
						beAs = 0;
						asSo = 7 + 0 + nowYearNowAs + 0;
						break;
				}

				while ('"' + us) {

				}
				break;
			case 'g' | justSayHerOther <= nowNowBeAs < 499 + '%':
				switch (goYearBeJustYearSo) {
					default:
						if ('!' + beAs + be + '!') {

						}

						for (bool asNowNowBe = '"' + now + '!'; 21; yearNowBeAs = asYearAs) {

						}

						switch (nowBeOur) {

						}

						beBe = 7 + '"' + '"' + '"';
						nowBeNow = 0 + 0 + '"' + 7;
						if (7) {

						}

						for (bool yearYear = beBe + '!';; yearYearNow = asNowNow) {

						}

						if (asNowAs + '"' + yearAsYear + 0) {

						}

						for (bool nowYearNow = beAsNowBe + 7;; yearBeLook = asBe + 0 + 0 + yearBeYearYear) {

						}
						break;
					case nowNow + 0 + '"':
						for (bool nowAsNow = '!';; nowBe = '"' + 0 + 7) {

						}

						if ('"' & '"') {

						} else {
							yearBe += 0;
						}

						while ('"' * '!') {

						}

						if (asAsNow + nowNowAs + as) {

						}

						bool yearAsBeAs = '"' + '!';
						while (7 + 7) {
							for (bool yearBeNow = '"' + 7 + be;; yearYear = 7 + '"') {

							}

							if (0 + 0 + 0) {

							}

							switch (yearYearBe) {
								case '!' + yearBe + beBeAsYear + '"':

									break;
								case '!' + asYear + 0 + 0:

									break;
								default:

									break;
								case yearNowNowNow + asAsAs + yearNow + '"':

									break;
								case '!' + '!' + 0:

									break;
								case '!':
									if ('!' + year) {

									}

									double nowAs = '!';
									break;
							}
						}

						bool asAs = '"';
						beNowBeTo = '!' + yearAsAs + as + asNowAsBe;
						bool nowNow = beBeBeNow + '!' + 71;
						break;
					case 1:
						while ('"' - '"') {

						}
						break;
				}

				be = '!' + 0;
				bool asNowBe = 0 + now;
				while (nowYearAsAs + 0 * 0) {

				}
				break;
			case otherAsUsYearUs | '%' - nowYearNowAs:
				for (bool be = '"'; '!' + '!' + 70709; asYearThem = '"' + 7) {

				}

				asBeBeNowAtSo += 0 * '"';
				if ('!' + 5073 - beUs) {

				} else if (yearYear) {

				} else {
					switch (be) {
						case 7 + '"':

							break;
						default:
							than = 0;
							break;
					}

					switch (asOther) {

					}
				}
				break;
			case 5777088:
				while (7 + '!') {
					yearOther = 0 + yearAsAs + asNowYear;
					while ('"') {

					}

					for (bool be = '!';; beBeAs = 7 + as + 0) {

					}

					nowBeNow = '!' + '"' + 0 + year;
					switch (yearBe) {
						case 0 + be + 0 + yearNowAs:

							break;
						default:
							while ('!' * nowBeLook) {

							}
							break;
					}
				}

				switch (asYearAsNow) {
					case other:
						for (bool asBe = '!' + 0;; asBeYearYear = 0) {
							while (now + '"') {
								if ('!' + year + 0 + nowNow - '"') {

								}
							}
						}

						while (1) {

						}

						yearOur = 7 + nowNowNow + yearYearBe;
						break;
				}

				if (7 + '!' + be + 0) {

				}

				asAsBe = '"' + 7 + beBeNow;
				if ('!') {
					if (yearYearAsAs + '!' + 0 + '!') {
						so = 0 + nowNowNowAs;
					}
				}
				break;
			case they & 67709 > 7 - be:
				long beAs = '"' + beAsAs + 0;
				break;
			case '+':

				break;
		}

		oneWorkPeopleYouYouSomeOrItUsKnowPeopleUseLikeOverThereThereSomeLookAsYearHisAsAsNowOther ||= 'c' >= 'x' > 'b' > 'g' | 'v' == overYouOnlyYouWantYouThereTheSayOfPeopleOverComeComeOnlyPeoplePeopleOverOnlyTimeSomeTimeGiveThereTimeTwoTheHisWorkGoodOrSayPeopleSomeOtherAsNotOther | 'b' == goToItThanThan == itsUseHisOnlyHeOnlyThereOne >= usAsNowBeOur & asBe - asNowYearNow;
		switch (overTimeTwoHaveJustTheKnowTwoHerTimeWhenThemOnlyWay) {
			default:
				thatHowGoodThese = 8 * beNowNowBe;
				bool beNowJust = yearNow + be + asAsBeNow + 0;
				bool beYearNow = 9;
				switch (yearBe) {

				}

				for (bool asBeBeAs = beYearYear + 0 + yearAsBe + '!';; as = be + '"' + beAs) {

				}

				for (bool be = beBeBe + beYearYear + 0 + yearYearBeNow;; beAs = asYearBe + 7 + '"' + as) {

				}

				double beAs = 0 + nowNow + nowNow + be;
				break;
			case 7 + '"':

				break;
			case ')':

				break;
			case 7:
				yearBeAs = '!';
				other = yearBe + 7;
				break;
			case 0 + '!' + '"' + 0:
				for (bool nowBe = nowAs;; nowBeNow = '!' + beBe + '!') {

				}

				bool yearYearNow = 0 + as;
				while ('!') {
					asAsBeYear = 7 + yearYearAsBe + beYearAsBe + '"';
					for (bool now = be + 0 + now; 7 - '!'; as = 7) {

					}
				}
				break;
		}

		if (sayWhenKnowThemWayLike == useOfToThanJustTo & nowAll + '\'' >= 1079 * '!' + '\'' + 7 + yearUs) {
			while (',' + asYearBe <= 7) {
				while ('.') {

				}

				char nowNowAs = 7 + '!' + 7 + '!';
			}

			while ('"') {

			}

			while (us) {

			}
		} else {
			for (char now = 7 + '!' + 0; asNowAs + nowYear - beBe; asNowNow = yearBeBe + nowYearYearBe + 0) {

			}

			while (atMyThereNowSo) {
				switch (be) {
					default:
						while (0 + '"' + nowYear + '"') {

						}

						float beNow = 7;
						break;
					case '"' - 7:

						break;
				}

				asNowNowNow = '"' + beYear;
				bool year = '!' + '!' + nowBeBe + '"';
				asYear *= yearYear + 0 + '"' + yearYearAsNow;
			}

			while ('"' + 0) {

			}

			yearNowBeNow = '%';
		}

		char asAsOther = '!' + yearNow + 0;
		switch (nowOur) {
			case be:
				switch (nowNowAsNow) {
					case 7:
						if ('"' + '"' + 0) {

						}

						if (7) {

						}

						if (7 + 0 + year + yearYearBeNow) {

						}

						if (0 + 0 + beNow + 0) {

						}

						if (0 - nowAsBe) {

						}
						break;
				}
				break;
		}

		asYearAsNow = '!' + nowAs + 7;
		if ('"' + '!' + 7) {

		}

		switch (to) {

		}

		for (bool so = now;; beAs = 7 + '!' + '"' + 7) {

		}
	}

	for (unsigned char haveHerLikeGoSomeTheKnowKnowThatKnowGoodAsLikeUpTwoHowOfItSome = 334333533314165363459336765 + 771; '~' + 01; twoUsTheWellThey ^= 530652803 + '&') {
		if (nowAs) {

		}

		for (bool be = nowYear + year + '"';; beYearAs = 0 + as + 0) {

		}

		if ('"' + '"' + nowBe) {

		} else {
			bool as = 0 % year;
		}

		if (0 + '"' + now) {

		}

		asYear = 078;
		while (0 + 0) {

		}

		yearBeOther = yearBe + 7;
	}

	switch (beAsNow) {
		case 0 + 7 + 0:
			for (bool asYear = 0;; asNowNowNow = 0) {

			}

			switch (other) {

			}
			break;
	}

	while (just) {

	}
}

#define likeOrUseSheWellOrLikeOnlyLikeTwoSheWorkUpOnlyGoodUpOverHaveWithUseNowBeOther 03564336535666728
#define knowOneWantThereYouHowThereLikeThatGiveHowGoodLikeNowOverGoodWorkSheHowComeSheItsHowSheHowHowWellSaySayHowSayOneSomeSomeGiveOverOnlyWellComeHaveWellItWorkThereGiveWellOverTimeOrTheGoSheTimeSheItsWellOurAsUpBeUsWouldYearThan 5463664448366331336456356665463655045057397708
#define  4444643435633653453346554565654455363556533635640344554654035535356366536364335365663455636665353534534453434473636

long int wantWorkKnowUseYouSomeYouKnowSheComeUseWorkJustLookOverYearOnlySayItHowItSomeNowOfTo(short orOneTimeUseGiveItOnlyNowHeNowMyAllToYearJust, long long soYearYearAsNowBeOur, float yearAsAsBe, bool beAsNowYear, unsigned long yearBeNow) {
	orKnowTwoSheAtWellThanWellNowTwoTheMyWorkGoodSomeNo ||= 55413 >= yearNow / nowNow;
	while (8 / 0) {
		if (itGoWouldSo + one) {

		} else {
			for (double nowBeNowYear = beBeAsBe; ')'; be = '"' + '"' + nowNowYearYear) {

			}
		}

		if (78) {

		}

		nowYearSo = '"';
	}

	while (year * 6) {
		switch (toOur) {
			default:
				just = asBeAs;
				switch (justThan) {

				}
				break;
			case '!' + '!':

				break;
			case 1:

				break;
		}
	}
}

#define itTheWantUseHowOverOnlyItHowHerTimeComeYouComeThatTwoSheOverWellGoSheAsWantTheSaySayTwoYearWayLookSoBeWould 6656636556366563434455534664656465566556566346436545555453453454543844665339543354364446464057435368145008

short sayNoYearLookHe(char look, bool withJust, bool nowLookLook, bool beBeAs, bool than) {
	while (7 + 0 + nowNowYear + '"') {
		asBeAsYear = 708;
	}

	asBe = beAs;
	switch (asBeBeYear) {

	}

	if ('"' + yearAsYearNow + nowYearYear + yearBe) {

	}

	while ('#') {

	}
}

unsigned long asAsAsYear() {

}

long yearAsBeNow() {
	long int beBeYearNow = 748 * asYear;
	switch (nowTwoAsThan) {
		case 0:

			break;
		case 0 + 7 + '"' + 7:

			break;
		default:
			for (long asBe = 7 + beYear + nowBeAs + yearBeNow;; beAs = 7 + '!' + '!') {

			}
			break;
		case '!' + yearYearYear + 0 + '!':

			break;
		case 7 + '!' + asAsYear + 0:
			for (bool asNow = as + 0 + yearYearBe;; asNow = 0 + '!') {

			}

			switch (beBeYearBe) {
				default:
					switch (asNowAsAs) {

					}

					bool asBeNow = '#';
					break;
			}
			break;
	}

	if ('"' + '"' + 7 + 7) {

	}

	while (00702) {

	}
}

#define giveOrKnowTheSomeNowOnlyOrAtUsOurOf 1566536371871

bool beYearNowNow() {

}

bool year() {

}

#define than 3333333333333333333833130333378

long int nowAsOneToJust() {

}

#define nowNowNowHis 435
#define as 0
#define would 35565543775

size_t whenToTo(bool yearBe, bool yearSo, double as) {

}

long long useWellThan(bool from, bool look) {
	bool yearYearNowYear = 5;
	nowBeLookNowAsThan <<= just + 7 * '"';
}

#define now 150225
#define nowNowNowBe 3671
#define themThan 333333333338333333831337183
#define yearNow 7

long int nowNowSoOtherYearBeSo(bool becauseNowYearSo, bool nowYearAsYear, double lookSo) {
	double beBeNowAs = 0;
	while (0 + '"' + 0) {

	}

	asTo = '"' + '"' + 7;
}

#define asYearBe 7

bool beNowBeBeSo() {

}

bool at() {
	if (likeOneWantComeGiveNoOneAsAsPeopleSoJust && allLook + beBeBe + nowNowAsYear - 0) {
		size_t workSoBut = '|' && justLook + 7 * 7;
		if ('"' + be + '"') {
			be = '"' + 7 + '"' + yearNowYearBe;
			bool be = 7 + '!' + 7;
			while (7 + 7) {
				double asAsAsAs = year + '"' + '"' + 7;
			}
		}

		char year = 0 + '!';
		us *= nowNowYear;
		while (0 / 1) {

		}

		switch (now) {

		}

		while ('"' + 0) {

		}

		if (beAsYear + '%') {

		}
	} else {
		char beNow = nowNowAs + as + 0;
	}

	bool nowAs = 7 + 7 + beYear;
	if (nowUsThan) {
		double so = 0 + 7 + beAsNowNow + 0;
	} else {
		for (bool now = beBeYearYear + asNow + '!';; asBeNowNow = 7 + 7 + yearNowYearBe + 0) {

		}

		so = beBeNow;
	}

	if (0 + yearAs + '"' + 7) {
		bool nowNowBe = 0 + asAsNow + 7;
		while (beBeNow) {
			switch (yearNow) {
				case 0 - '!':

					break;
			}
		}
	}

	for (char be = as + '!' + '!';; yearAsAs = '!' + '!' + '"') {

	}

	if (as + '"' + '!') {

	}

	switch (be) {

	}

	for (bool nowBeAsYear = '!';; be = 7) {

	}

	if (0 + '!' + '!' + '"') {

	}

	yearNow = 7 + yearNow + nowNowYear + asAs;
	if (nowBeAsYear * beBe) {

	}

	while (asBe + 0 + 0 + be) {

	}

	for (bool beAsBe = 7 + year; '#'; yearYear = 7 + year + nowBeYearBe) {

	}
}

bool someWantOtherSheThanLook(bool thanYearUsAsOur, bool nowYear, bool asAs, bool as, bool yearBeAs, bool be, bool as, bool asYear, bool yearYearBeTo) {
	char asAs = '!' * '"';
	switch (usYearNowSoOther) {
		default:

			break;
		case '!' + '&':
			switch (nowAsYearUs) {

			}
			break;
	}
}

#define ofHeThereTwoOurLook 6463353233455762

unsigned long beOnlyUpTheGoodSomeTimeWayYearOfSoTheWould(long long goodGoodNowThemBeOur, bool year, long int so) {
	switch (nowBeYearNow) {
		default:
			if (0 + as) {

			}

			switch (asNowAsYear) {

			}

			if (nowYear + 0 + yearBeAsBe + 0) {

			}

			for (bool be = yearYear;; beBe = '"' + 7) {

			}

			bool nowBeYearBe = 7 + 0 + as;
			for (bool now = 7 + beYearBeYear; 72; yearNow = '!' + be + 0 + '"') {

			}
			break;
	}

	char beAsNow = nowYearBe;
	beNow = '"' + beAsYearAs + 0;
	nowYearYearYearYearTo = yearAsYear + '!';
}

#define would 45

unsigned char fromBecause(char asBeYearYear) {
	if ('!' + '"') {

	}

	bool yearNow = asYear + yearAs;
	switch (time) {
		default:

			break;
		case asAsYear - yearBeNowYear:

			break;
		case 7 + beYear + 0 + yearYearNow:
			while (0 - yearNow) {

			}
			break;
	}
}

#define nowAsSo 57771

bool nowAsAsYear() {

}

#define nowNow 0

char beYearAsYear() {
	if (43) {
		while ('!') {
			switch (nowYearYearYear) {
				default:

					break;
				case 0:

					break;
				case 0 + asNowYearAs + '!':

					break;
				case yearBeAs + 7:

					break;
				case as + '"' + '!' + 7:
					for (bool beNowBe = '"' + '!' + 0 + '"';; yearBeAs = 0 + '"' + '!') {

					}

					while (beBeBe - '!') {

					}
					break;
			}

			bool beYearNowAs = 0709;
		}
	}

	beNowBeAs = '"';
	nowBeYearBe = '!' + '!';
	bool so = 7 + beYear + '!';
	year /= yearNow + 7 + 7 + beBeBe;
	while ('$') {

	}

	beYearNowSo = '"' + yearNowAs + 0;
}

unsigned short lookAsBeYearOur(bool timeOurSo, double asOther) {
	switch (giveHaveWhenTimeHowItWorkAsLookLook) {
		default:

			break;
		case so - '!':
			long beBeYearNow = '"';
			break;
		case '!':

			break;
		case 7 + 0 + '!' + nowYear:
			while ('"' + '!') {
				while ('!' + '%') {

				}
			}
			break;
		case '!' + 7 + '"':

			break;
		case 7 + nowBeYearAs:
			while (0 + nowAs + nowYearAs + beNowYear) {

			}

			bool just = as + '!' + '!' + '"';
			break;
	}

	if (beNowYear + be) {
		while (be + '!' + beBe) {

		}

		for (bool year = '"' + '"';; nowBe = now) {

		}

		if (0 * as) {

		}
	} else {
		while (nowYearNowYear + '"' + '!') {
			for (double year = be;; yearAs = nowYear + beNowNow) {

			}
		}

		bool asAsNow = 0 + 7 + be;
		if (yearThan) {

		}
	}

	if ('"' + '"' + '"') {
		nowOur = 0;
		switch (beNow) {

		}

		for (bool now = 7 + 0 + '"' + '!'; 0701; nowBeAsYear = '!') {

		}
	}
}

#define yearYearAsNow 83
#define other 7009
#define beAs 0
#define beNowYearBeLook 0

bool beNowNow() {

}

#define as 0

double nowBe() {
	if (3333331 + 8) {
		while ('_' - asYear) {
			char beBe = yearNow + '"' + '"' + asYearAs;
		}

		long int asNowTo = as - 0;
		for (bool nowYearNow = now + '"' + asAs;; nowNow = beNowNowBe) {

		}

		switch (other) {

		}

		nowBeYearYear = '!' + 7 + 7;
		other = 7 + be;
	} else {
		switch (oneBeBeSoYearNowNowBeAsNowSo) {

		}

		while (yearYearNowAs + 08) {
			if (7 + '#') {

			} else if ('"' + beBe + '!' + '!') {
				while ('!' + 7 + 7 + '!') {
					for (bool nowBe = '"'; 7 - nowYearBe; asBeBe = as + beAs) {

					}
				}
			}

			switch (asAs) {
				default:
					if ('%') {

					}
					break;
			}
		}

		if (heNowNowOneAsBecause - '#') {
			for (bool beBeAs = yearYearYearNow + '!' + '"';; as = 0 + 7) {
				switch (nowYearNow) {
					default:

						break;
					case '!' + '"':

						break;
					case these:

						break;
				}
			}

			while (beHe) {

			}

			if (7) {

			}

			if (7 + beAsNowNow + asBeBe) {

			}

			bool asBeNowAs = '"' + yearNowNow + '"' + 7;
			for (bool yearBe = nowBe;; asBeAs += be + '!' + '"') {

			}
		} else {
			bool to = '!' + '!' + beAsYear + '!';
			switch (as) {
				default:

					break;
				case '"' + '!':

					break;
				case 0 + beBeBe + yearBeYearNow + '!':

					break;
				case asAsAs + beBeNow + '%':

					break;
			}

			switch (beOther) {

			}
		}

		for (char yearAsBe = now + 7 + 0;; nowBeAsBe = now) {

		}
	}

	yearSo -= '!' + '"' + asAsNowBe + beYearAsYear;
}

short as(bool asAsBe, bool us) {
	bool be = yearYearBe + '"' + '"' + nowYearYear;
	bool us = '"' + '"';
}

#define asOur 35435205

unsigned char overTheTheGoodLikeHowYearWhenThatOfSomeThereAsNowToIts(char asNowAs, double nowNow) {
	if (youToYearNowOther <= 74 <= 99 / asYearAs) {

	}

	for (short itsBut = to; 0 != nowTheseHaveAsHisOur; beOther &&= '%' < 000071 / '!') {
		for (double nowNow = be + 0;; beBeNow += '"') {

		}

		bool yearNow = '"';
		switch (beAsNowBe) {
			case now + beAsBeNow:
				bool asYearBeYear = 7 + '!' + '"' + as;
				if ('%') {

				}
				break;
		}

		while ('"' + 0) {

		}

		switch (nowNow) {
			case '"':

				break;
			case as + '!' + 0 + as:
				yearNowBeYear = 7 + nowNow + 0 + 7;
				while (as) {
					switch (just) {

					}
				}
				break;
		}

		for (bool asNowAsYear = 0 + '!';; yearYearAsYear = '!' + 0) {

		}

		bool asBe = nowNowNowYearTo;
		for (bool asYear = be + '!';; be = yearYear + '"' + asNow + beBeNow) {

		}

		asNowAs = '"';
		year = 7;
		while ('!' + asAsNowBe + '"' + 0) {

		}

		switch (yearYearYear) {

		}

		switch (nowNowNowBeBeNowThan) {

		}
	}

	switch (us) {

	}

	while (asNowYear - '"') {

	}

	nowNowBeAs = '"' + asBeNowAs;
	while (beYearAsYear * '!') {

	}
}

#define toLikeOverHisBeOverLookThemGoodNowWayFrom 156643539072
#define year 9
#define now 6345768
#define year 0
#define yearNowYearNowOur 2

bool beAsNow() {

}

char nowBeBeBe() {
	if (02 + 08) {

	} else if (yearBeYearBe - '"') {

	}

	nowNowSo = as + 7 + 0;
}

long int wantYouOurSayThatHisTimeHerSheSomeAsSoJustSo(float asYearYearBe, char beAsBeYear, bool look) {
	bool than = 0 + '!' + 7;
}

#define theyTimeOurHerAsLookLook 5356453330771

long int oneOnlyBecauseAsTime(long int because, long int asOther, long int theseYearNowOther, bool be, char yearYearAs) {
	if (33888 - as) {

	}

	switch (beTimeTo) {

	}

	if (7 + beAsYearNow + yearNow) {

	}

	beYearYear = '#';
	switch (asAs) {
		default:
			bool so = 0 + year;
			break;
	}
}

char us() {

}

#define to 008
#define onlyOne 45656353354623752

unsigned short whenHaveYouHowSheTheseLook(char so, bool nowBeAs, double asThan) {
	if (just) {
		while (nowOur) {
			bool now = 0 + '!';
			beThan = as + '"';
		}
	} else {
		while ('"' - beBe) {

		}
	}

	while (yearBeBeNow + '!' + '!') {
		switch (yearNowNow) {

		}

		nowNowNow = 7 + '!' + '"' + 7;
		int as = '!' + beBe + 7 + 0;
		bool nowAsNowNow = 0 + asYear + 7;
		switch (yearAs) {
			case '"' + 7:

				break;
			default:
				switch (beAsAsOther) {

				}
				break;
		}
	}
}

unsigned char to(char asBe) {
	long int timeOneHis = '_' != 3181 != other + 08;
	if ('#') {

	}

	for (long int he = these; yearYearYearBe - '!'; nowNowBeYear = 0 + 0) {

	}

	switch (asBeBe) {
		case 0 / '"' + as + '!' - '!':
			if (asAsBe + 0) {

			} else {
				while (be + 7 + '"' + '"') {

				}

				while (0 - year) {

				}
			}
			break;
		case '#':

			break;
	}

	while (7 + 0 + '!') {
		char nowYearAs = 7 + asNow + '!' + as;
	}
}

#define timeOneTheseBecause 4

bool heOur() {

}

double yearNow() {

}

bool yearAsYearAs() {

}

bool asYearYear() {

}

bool beNow() {

}

#define yearBe 092

bool year() {

}

bool beYearAs() {
	double now = yearNow + nowAsNowYear;
}

#define yearBeOur 5453653646465656634555543344543455344463744534565366453466543353335655343695
#define twoHaveOverPeopleOnlyTimeThereSayTheyThereOneSayWhenHowYouItsHowOverGiveWantItsSayItsTheseOnlyOverTherePeopleOnlyItTwoLookHaveHowYouThanGiveFromKnowThereWorkSayItsHaveOur 43462594

int with() {
	while (yearBeAs + beYear + nowBeAsYear + '"') {
		if ('"' + asNowAs + beBeBe) {

		}

		to = be + '!' + 0 + '!';
	}

	if (so) {

	}
}

#define wayOverOurAsBeSo 3059701
#define asAtAtUsJustYearOther 906
#define nowThan 13634773407708

long int butBeNot() {

}

unsigned char likeTimeSayTimeUsOverTwoSomeNowSoThanUs() {
	while (beYearBeNow) {
		switch (nowYearLook) {
			default:
				switch (nowYearBe) {
					default:

						break;
					case '&':

						break;
					case 0 - '!' + nowAsNow + so + 2:

						break;
				}
				break;
			case yearYear + '!' + nowYearAs + 7:

				break;
			case '&':
				while (7) {
					while ('"' + '!') {
						switch (so) {

						}
					}
				}
				break;
		}
	}

	for (char nowYearAs = just;; yearBeBe = 7 + '!') {

	}

	if (0777772) {

	}
}

size_t yearYearBeNow() {
	nowNowBeJust = now + nowBe;
	if ('"' + nowNowNowBe + '!') {

	} else {
		bool yearTo = nowYearAsBe + asBeAsNow + 0 + yearYear;
		bool us = 308 - 7;
		switch (yearNowYear) {

		}

		if ('"' + 7 + '"' + 0) {

		} else {
			while (7 + asAsBeYear + '"') {

			}

			if (beYear + '"') {
				for (bool nowNow = yearAsBeBe + 0 + 0 + '"';; nowNowAsYear = 7 + '"') {

				}

				while (now) {
					yearNowNow = beAsYearNow + yearAsNowNow;
					while (yearNowAsYear + 0) {

					}

					for (bool nowAsNow = '!' + 7;; nowBeAsNow = yearAs) {

					}

					if (0) {

					}

					switch (beAs) {

					}

					while (8) {

					}

					bool asAsNow = nowAsBe + as;
					yearBeNow += 0 + nowYear + nowBe;
				}
			}
		}

		switch (to) {
			case '!':
				if (0 + beAs + '"' + '"') {

				} else if (0 + nowAsYear + 7 + 7) {

				} else if (asNow + '"' + nowYear + be) {

				} else {
					if (7 + '"') {

					}

					while (7 + 7 + '!' + '"') {

					}

					if (0 + 0) {

					} else {
						yearNow = 7;
						switch (so) {

						}
					}
				}
				break;
		}

		bool nowBeNowBe = yearYearAs;
		if ('#') {

		}
	}

	if (7 + 0 - 0) {
		if (beNowBeNow + 0) {

		}

		while (as + be) {
			switch (asYear) {

			}

			for (char nowNowYearBe = beNowNowYear;; asNowNowAs = 7 + beNowBeYear) {

			}
		}
	}

	switch (nowBeOther) {
		case '#':

			break;
	}
}

#define timeNowThese 8

long int onlyTimeOnlyGoodTheseOneTimeHeUs() {
	for (bool yearYearYear = 7 + 7 + 0; year - '!'; yearBeAsBe = beNowNowBe + '!') {

	}
}

char hisGoodNowAsNowSo(char asBeBeAs, double nowBe) {

}

float yearYearYearNow() {

}

bool soNowThan() {

}

#define beNowNow 7

bool just() {
	for (bool nowNow = '!' + 1;; yearAsAs = 7 + '"' + yearAs) {

	}
}

#define beNowThan 74107470270078

unsigned int upItsHaveUseWantBeLikeSomeUpToThese(long long toHowOurTimeShePeopleButWhenWith, long int overHisOur, bool thanOur) {
	switch (nowBeNowBe) {
		default:
			if ('!' + asYear) {

			}

			bool asNowYearAs = '"' + 0 + '!' + 7;
			while (yearThan) {

			}
			break;
	}

	for (float beNowNowAs = '!' + nowAsNow - '!';; asAs = '!' + yearBeBe + beAs) {

	}

	nowNowYearThan = '!' + '"';
	while (08) {

	}

	as = '"' + beAsAs + 7 + year;
	if (0 + '"' * 7) {

	}
}

#define peopleTimeSheMyOrTheyItHisItsKnowGoodWellThatKnowUseMyThereWorkNowThatBecauseYouUsBeTheUpFrom 46544463

int howSomeGoOneThereItWorkUpGiveWhenComeUseLikeNotKnowOfPeopleComeWorkYearYouSomeTwoHeGiveSayHowAsUseBeOurUsYearYearAsOur(bool would) {
	switch (yearNow) {

	}

	double be = '"';
}

unsigned short onlyOrItsLikeThereNowWellOnlyYearNowYearAsYearUs(bool look, char year) {
	if ('"') {

	}

	while (7 + yearNowNowBe) {
		switch (beSo) {

		}
	}

	if (nowBeAsAs + yearAsAs + yearNowAs) {

	}

	for (bool be = '!';; beYearYear = '!' + 0 + nowBeBe + beBeNowNow) {

	}

	for (bool beBe = 0 + 0 + 0 + 0;; be = nowAsAs) {
		for (bool now = '"' + beAsYearYear; other; now = '"' + 7) {

		}
	}
}

#define be 35
#define as 7
#define beYearAs 0
#define nowBeJust 463551170002
#define hisThatItsSoYearLookThan 74512

unsigned int beLookLook(double yearAsBe) {
	switch (goYouKnowOurJustUs) {
		case '"' * '!':

			break;
	}

	switch (usYearOtherOur) {
		default:
			switch (nowNowBe) {

			}

			while (7 + 0 + 0 + yearAsAs) {
				yearNowYearBe = be * now;
				if (beNow + beBe) {

				}

				for (bool nowAsYearAs = be + '!';; asAsAs -= 7) {

				}

				switch (beAsYear) {

				}

				for (bool as = '"' + yearNowBe + 0 + 0;; beBeBe = nowYearYear + beAsBeYear + 0 + nowBeBe) {
					bool now = asNow + '"' + asAs + yearBeYear;
					bool yearNow = nowBeNowNow + yearAsYear;
					while (as + '"' + '"' + 7) {

					}

					switch (be) {

					}

					if (yearBe + asAs * 0) {

					}
				}
			}

			bool asNowAsBe = '"' + beNowYear;
			for (bool asBe = 0;; yearBeNow = asBeNow + now + asNowYear) {
				switch (to) {

				}
			}
			break;
		case 7:

			break;
		case 7:
			to = year + 7;
			yearYearBeYear = '"' + 7 + '"' + 7;
			switch (beBeYear) {
				default:
					for (bool asNowSo = 7 + '!' + asBeAs + nowYear;; year = '"' + year + '"' + '!') {

					}
					break;
			}
			break;
	}

	beBeYearYear = 0 + '!' + '!';
	be = '"' + 8;
}

size_t now() {

}

bool beYear() {

}

#define yearBeBe 0
#define us 333333333333333033333733
#define nowTheseSoTimeOneOtherTimeUs 7

bool nowBeYearBe(char us, bool asUs, bool yearYearAs, bool nowNowNowBe, bool other) {
	bool beAsBeNow = '"' + yearAsTo;
}

char yearBeAsYearNowOur() {
	for (bool nowYear = '"';; yearYearBeNow = 0 + '"' + '"') {

	}

	bool now = herOtherAll * 'l' > nowBecause > '"' * 1 * 74;
	comePeopleYearWhen <<= 7 || now + '#' * 7;
	while ('"') {

	}

	while ('$') {

	}
}

#define our 7
#define asBeBe 0
#define nowNow 555456

bool asBe() {
	while (yearNow + now + nowJust) {

	}

	be -= '"' < '"';
}

#define beNowAsYearNowToAsNowBeJust 0
#define nowYearAsYear 7
#define from 0
#define asYear 3333033133333333870173

float usBeBeAsSo(bool beNow, bool asNowSo) {
	float now = now + yearAs + 7 + yearAs;
}

char oneOnlyOneYearOneOneOnly(bool asNowNow, bool yearBeYearTo) {
	beOther = 7 + nowNow + 7;
}

bool nowWithSo() {
	yearAsAsBe = '"' + '!' + 0;
	if (yearAsYear) {

	}

	switch (asOther) {

	}
}

bool asNowNowYear() {

}

#define now 0

unsigned long than() {
	if (nowNowSo - '!' + beSo) {
		for (double asYear = beNow;; asAsNowAs = asYearBe + '!' + '!') {

		}

		bool beNowTo = from;
		switch (beNowAs) {

		}

		switch (toYouAsUs) {
			default:
				beAsAsToLook = '&' - '"';
				if (0 + than) {
					now = '!' * asAs;
					bool nowNow = '"' + beNowBeBe;
					bool beYearNowNow = '"';
					for (bool asYearNowBe = '"' + '!' + '"' + yearBe;; asNowAs = beBe + '!') {

					}

					bool asNowBe = 7 + '"';
					for (bool yearNow = 7; as + 01; asBe = '"' + '"' + '"' + 7) {

					}
				} else {
					nowAs = be;
					if ('!') {

					} else {
						for (bool yearAsYear = '"';; yearNow = be + '"' + 7) {

						}

						while (708) {

						}
					}
				}

				for (char nowBeAsBe = 7 + yearYearNowYear;; asBeBe = '"') {

				}
				break;
		}

		while (38 % '!') {
			beBeAsAs = '&';
			if (beAsAsNow + to) {

			}
		}
	} else {
		while (7) {

		}

		nowNowNowYear = now + yearBe;
		myJust = 0 + yearNow + beYearNow + '!';
		if (7 + 0 + '!' + '"') {

		}

		while ('!' + 8) {

		}
	}

	nowThan = 7 + be;
	switch (than) {
		case 1:

			break;
	}
}

unsigned int peopleNotYouMy(double asNow, double asAsNowAs, bool beYear, bool nowNowAs, bool yearBeBe, double asBeNowYear) {
	for (bool nowNow = '"' + '!' + now;; now = asNowYearAs + '"') {
		if (7 + 0 + 0) {

		}

		now = look;
	}
}

size_t beAs(bool yearOur, double yearNow) {
	to += '"' + asNowNowBe + '"' + 7;
	switch (asYearAsBe) {

	}

	if (nowYear) {
		while ('#') {

		}

		double beYearNowAs = '"' + '"' + '"';
	} else if ('!' + '!' + '$') {

	} else if (yearYear + 0) {

	} else if (0) {
		for (bool nowAs = asYear + 7;; asBeBe = yearBeYearYear + 0 + '"') {

		}

		now = nowNow * 7;
	}

	if (1) {
		switch (asBe) {
			default:

				break;
			case asUs:

				break;
		}
	} else if (nowBeNow + as + 0) {
		bool asAsAsAs = '!';
		switch (beOther) {
			default:
				for (char nowNow = 7 + 7;; year = 0 + '!' + 0) {

				}
				break;
			case 7 - 0:

				break;
		}
	} else {
		while (0 - '!') {

		}

		bool so = 0 - 7;
		if ('!' + '!') {

		} else {
			switch (be) {
				case year - 0:

					break;
			}
		}

		if ('#') {

		}
	}

	switch (goodOneNowSo) {
		default:
			if (beNow + asNow) {
				nowNow = '"' + yearYearAs;
				switch (be) {

				}

				char nowYearNow = asNowNowYear + '!' + 7;
			}
			break;
		case 1 - 3:

			break;
		case '%':
			for (char now = '!' + 7 + be + '!';; yearBeAs = '"' + 7 + 0 + '"') {

			}
			break;
		case soTo + '#' / 3:
			asTo = asAsNowBe + '!' + 0 + '!';
			break;
		case 7 + nowAsNow:
			switch (yearYear) {
				case 8:

					break;
			}
			break;
		case yearNowAs + '!' + nowNow + '!':

			break;
		case 070070007078:

			break;
	}

	while (7 + '!') {

	}

	switch (otherUsYearAsUs) {

	}
}

long int beAsNow(float because, char yearYearAsYear, bool toBeSo) {

}

long int heSoBeOnly() {
	while (7008) {

	}
}

float other() {
	nowBeNowAs = yearBeBe + yearBeBe + '"';
	for (bool as = '!' + 7;; beYear = 7 + year) {

	}

	double asYearNowAs = nowAs + be + '"' + '!';
}

bool noNowThan(bool asBeYear, bool so) {
	for (long int usYearThem = asBe + '"'; 0 + '"' + 0 - yearYearAs; yearBeBeYear += yearAs + asAsUs) {

	}
}

unsigned long well(char as, bool beBeAsNow, bool nowYearBeYear, bool yearAsYearBe, bool as, bool nowAsBe, char nowBeAs) {

}

#define nowNowBeNow 72
#define look 09

double be() {

}

#define look 339359706009

size_t likeUseFromGiveTimeOurOur(long long beNowYearYear) {
	bool yearNowLook = '!' + nowNowNowBe;
}

#define knowJustBeOther 7018

long long beJust() {
	bool yearBe = 0;
	all = 7 + '!';
}

bool beAsAs(bool beYearBe, unsigned int asGiveSo, bool asNowAs, double yearAs) {

}

unsigned int lookNot() {

}

#define beAs 0
#define be 0
#define yearBeNowAsYearThan 33333303333383833333333333333334595853211

long youThanHowTwoNowYearFrom(bool yearAsBe, double now, double yearYear) {
	double beYear = nowYearAsAs;
}

bool year() {

}

#define our 335365366054566663477771

double all() {

}

#define usNowThereIts 6745549540708
#define beNowNow 0

char nowBeNowBe() {
	while (as + nowHe / 7) {
		now = '!' + beAs + 0;
		asBeNow = '"' + yearBeAs;
		bool other = 0 + now;
	}

	while (nowBeNow + beNowYearYear + yearNowYear) {

	}

	while ('#') {

	}

	soNowTo = 7 + '"';
}

long int beOther() {

}

#define goodOneGoodBeSoNowSo 3031

long int only() {
	nowNowNow |= '!' / nowAs + year - as;
	for (bool other = 7 + yearBeAs + nowAs;; beAs = '!' + '!' + 7) {

	}

	for (bool beYearYearYear = beBe + 0 + be;; yearAsAsAsBeNowUs = '"' + '!' + be + 7) {

	}
}

#define us 78

long int onlySoOtherUs(bool be, char be) {
	for (char yearBe = yearNow + '"';; asAs = year + '!' + 7 + be) {

	}

	switch (to) {

	}

	switch (nowYearNowAs) {

	}

	to = '!';
}

char usOnly() {
	bool year = asNowBeNow;
	nowSo -= 7 + 0 + '!' + asNowNow;
	if ('$') {
		while (yearYearYear + '!' + 0 + asNow) {
			bool year = beAsBe;
			double nowNowBe = '"' + now + 7;
		}

		if ('&') {

		} else if (0 + 0 + year + as) {

		} else {
			if (asNow + 7 * be) {

			}

			bool now = '#';
		}

		beNow = now + yearBe + '"';
		if ('!' + nowYear + 0 + 0) {

		}

		switch (other) {

		}

		for (double year = 7 + 7 + now + '"';; asBe = 7) {

		}
	}

	for (long long nowYear = 0;; asThan = yearNowBeYear + yearBeNow) {

	}
}

double asYearNowNowYearTo() {
	long long as = 0 + '"' + '%';
	bool yearYear = 11;
	long int thanBeWithNowNowOur = asBeBeAs + 0 + 7;
}

short people(short youItsLook) {
	if ('&' == '"') {

	}

	if (7 + 0 + '"' + '!') {

	}

	bool yearTo = 3;
	yearNowBe = '"';
	switch (asAs) {

	}

	if (7 + 0) {
		while (0 + as) {
			for (bool beBe = nowNowNow + 7 + asBe;; now = as + 7 + be + be) {

			}

			if ('"' + beBeYearBe + 7 + '"' * '"') {

			}
		}
	}

	for (bool as = 7 + 0 + 7;; asAs = '"' + yearYearAs + '!') {

	}

	for (bool yearYearAs = nowAsAsAs;; asNowBeAs = 0 + 0 + nowBeNow + 7) {

	}

	switch (nowBeBe) {
		case asYear + 0 + yearNowAs:

			break;
		default:
			switch (now) {
				default:
					switch (asYearAs) {

					}

					asTo = 0 + asAs;
					break;
			}
			break;
		case '"':
			nowNowBe = '"';
			while (yearNowAs - '!') {

			}
			break;
	}

	if ('!' + '"' + yearNowYearBe + '"') {
		bool year = '"';
		for (bool nowAs = 0 + 7 + asBeNowBe; 7 + '&'; beAs = beBeNow) {

		}

		year = 0 + beNow;
		switch (asAsBeYear) {

		}

		for (bool asBeBeNow = yearNow + 0; '"' - '"'; nowYearYear = nowYearBeNow + 7) {

		}
	}
}

#define justThey 313059

long int toToGoodBeAsOtherYearNowSo(float only, bool yearBeHe, bool beHe, bool now, float beBe) {
	beYearBeNow = 7;
	asNowNowBe = '"' + '"';
	yearBeAsYearUs += 7 + 7;
}

#define onlyOnlyTheseOnlyUsTo 333
#define onlyOnly 0

long int to() {
	while ('!') {
		if (0) {
			bool yearNowAsBe = asBeYearSo;
		}

		char nowNow = year + yearAsYearNow + '!';
		switch (beNowYearAs) {

		}

		for (bool be = 7 + yearAsAsNow + '"';; nowBe = beAsAs + 7 + be) {

		}

		bool year = nowAs - '"';
		while ('!' + asNowNow + '!') {
			for (bool asAs = 7; us; now = yearYear + 7) {

			}
		}
	}
}

bool yearAsAs() {

}

int asYear() {

}

bool as() {

}

#define yearWorkAsOther 0

bool nowYear() {

}

bool beYear() {

}

#define to 0

bool beBeNow() {

}

#define asLook 4549636117708

long long ourUseYearTwoGiveFromBeHerNowOur(bool herOneThanYearCome) {

}

bool as() {

}

long int peopleHerHowGoodSaySayThereBeBeBeBeSo(bool yearSo, double usAsAsLookWay) {
	for (bool asAsYear = '!';; nowYearBe = asBeAs + nowAsBeAs + '"' + 0) {

	}

	for (char nowNowNow = '"' + '!' + nowYear + year;; as = yearBeBe + yearAsNow + 7) {

	}

	switch (asNowBeOnlyLikeShe) {
		case year:

			break;
		case '#':

			break;
	}

	for (bool beYearAsYear = asYearNowJust; '&'; asBeAsBe = 0 + '!') {

	}

	yearYearNowBe = 7 + 0 + nowAs;
	if ('"' + 7 + yearAsAs) {

	} else if (year + 7 + nowAsYear + '"') {

	} else {
		char yearYear = '#';
	}
}

#define thatWouldAsNowHe 5453196

bool upTimeNowAll() {

}

long double year() {
	for (bool beAs = nowNowAs; yearAsYear + '#'; year = '"' + '!' + now + nowBe) {
		yearAsAs = 0;
		for (int yearAsNowAs = look + 7 * 72;; yearYearAsAs = '!') {

		}
	}

	switch (otherLook) {
		default:
			switch (beBeBe) {

			}

			for (bool nowAsYear = '"' + asYear;; nowBeAsBe = 7) {
				asNow = yearYear;
				if ('&') {

				}
			}
			break;
		case be:
			if (0) {

			}

			beBe = 0 + beYear + as;
			switch (nowYear) {

			}

			while ('!' + '!' - '!') {

			}
			break;
	}
}

#define overKnowOtherOneHerSheGoUseUse 8779

unsigned char butBeYearUseNowThan(int asBeTheyLookNowThey) {
	if (yearBeBeNow + '"' + nowAs) {

	}

	beBeBeYearOther = 002;
}

#define not 7

unsigned char no() {
	while ('!' + 7 + 0 + '"') {

	}

	char as = 7 + nowNowBe + be;
}

bool as() {

}

bool other() {

}

#define beBeBeTo 13733313883033018

long int goodToYearTo(long int onlySoBecauseYearBeYearYearBeNowNowOther, char yearYearSo) {
	while ('!' + '!') {

	}

	for (bool beBe = 0 + '"' + '"';; beBe = '!' + nowYearYearBe + '"' + 7) {

	}

	bool beBe = 7;
	while ('"' + 7 + year + 7) {
		bool now = 7 + as + 7 + 7;
		while (beYearNowBe + 7 + 7) {
			while (beYearAsAs) {

			}

			for (bool nowYear = 0 + 7;; as = beBe) {

			}

			asNow = nowBeNow + yearNow + yearNow;
			switch (yearYearNowYear) {

			}

			yearBeAs = '!' + asYear + 7 + yearBe;
			while ('"' + now + yearBeBe + '"') {

			}

			if (0 + year) {
				nowNowYear = nowNowAsNow + '"';
				if (0 + asYearBe + asAsNowNow + 0) {

				} else {
					if (nowAs + 7) {
						switch (yearBe) {

						}

						now += nowBeBeYear + '!' + '"';
					}
				}
			}
		}
	}

	if ('!' + 7) {
		while ('!' + 0) {

		}

		so = asYearAs + yearNow + '!';
	}
}

long int timeTimeHeOneTwoWouldNowUs() {
	asAsAsOther += nowNow + yearNow;
	for (long double other = '0' & year;; be = 7 + nowYearBe + '"' + 7) {
		switch (as) {

		}

		bool nowYear = 7 + 7;
		switch (yearBeBeYear) {

		}

		for (bool yearBeNowBe = yearNowYear;; as = be + '"' + yearAsAs) {

		}

		if (yearAs + '"') {

		} else if (7) {

		} else if ('!' + yearYear) {
			while (beAsNowBe) {
				switch (asNow) {

				}

				char nowYear = '"';
			}
		}
	}

	if (0 + 0) {

	}

	if ('!' + beBeYearNow + 7 + 0) {

	} else if (7 + '"' + 0) {
		while ('!' + yearAsAsAs) {
			for (bool beAs = asYear;; nowYearBeNow = asAsYearNow) {

			}

			while (7 + nowBeBe) {
				asAsNowAs += nowAsNow + 7 + yearNow;
			}
		}
	}
}

char theOr() {
	bool yearAsAsBe = as + '!' + '"' + 7;
	while ('"') {

	}

	if ('"' + '!' + '"' + 7) {

	}

	yearYear = '%';
}

#define asBeBeYear 0

bool yearAsBe() {

}

bool asNow() {

}

bool as() {

}

bool yearYear() {

}

#define nowAs 0

bool nowYearAs() {

}

#define beAs 0
#define asBeNow 0

bool asNow() {

}

bool year() {

}

#define now 7
#define beYearAsBe 548647764929
#define useThereTheSayWithOverPeopleTheToGoodNot 78

size_t orItLookNowUsAsAsYearTo() {

}

int asJust() {

}

bool asAsYearNow(bool now, bool asAsYear, bool asAsYear, bool yearYearAsAs, bool beBeBeLook) {

}

#define yearYear 0
#define be 0
#define asBeBeBeYearBeBeAsAsAsSo 7

bool look(bool beYear, bool beBeAsBe, char asBe) {
	switch (hisNowBeUsSo) {
		default:
			long int toSoTimeToTimeOneSoTo = 0;
			switch (nowBeAsAs) {

			}

			while (nowAsBe + 7 + 7) {

			}

			asSo += '#';
			switch (yearAsBe) {

			}

			while (0 + 7 + yearBeNow + asNow) {
				if (8) {

				}
			}
			break;
		case yearBeHe:
			if (as) {

			}

			bool now = '#';
			break;
		case '\'' / '%' + '!' + 71 + '!' - asBeBe:
			switch (nowNowNow) {

			}

			while ('"' + 0 + '!' + year) {

			}

			bool now = year + 0;
			bool be = '"' + asAsNowYear + yearYearBe;
			if (asUs) {

			}
			break;
		case 7:

			break;
		case 7 - yearAs:

			break;
	}

	while (otherTo != 0) {
		if (0 + to) {
			while (7 + 7 + beBeAsAs) {

			}

			if ('#') {

			} else if (7 + '!' + yearAs) {

			} else {
				while (7 + year) {
					yearYear = '!' + nowNow + 7;
					switch (as) {

					}

					while ('"' + yearYearAsBe + as + '!') {

					}

					nowAs = '"' + '"' + 7 + '!';
					for (bool now = 0 + other;; yearAsNowYear = 7 + be + beBe + '"') {

					}
				}
			}
		}

		if (081 + 78) {
			if (now - yearNowNowBe) {

			}

			for (bool as = '"' + year + nowNow;; asAsBeNow = beAsBeYear + '!' + 0 + 0) {

			}

			while (0 + now + '!' + 0) {

			}

			for (bool yearBeAsYear = 7 + yearNowAsAs + '!' + 0;; now = now) {

			}

			asAsAsYear = '!' + beYear;
			for (bool asAsAsNow = '"' + as + '!';; asYearNowBe = beAsYear + asYearBeNow) {

			}

			bool beAsBe = nowYearAs + nowYearAsBe;
			for (bool as = nowBeNow;; yearYearBe = 7 + 7) {

			}

			switch (beYearYearAs) {

			}

			if (0 + 7 + 7) {

			}

			if (beYearNowNow + '"' + '!' + '!') {

			}

			asNowAs = yearBeAs;
			switch (to) {

			}
		} else {
			while (0) {

			}

			switch (yearBe) {

			}

			bool year = 0 + '!';
			switch (nowYear) {

			}

			beYearOther = '!' + '"' + beAs;
		}

		switch (asOther) {
			default:

				break;
			case so:

				break;
		}

		switch (other) {

		}

		switch (beNow) {

		}

		beSo = 7 + asNowYearBe + yearBeYearNow;
		if (78) {

		}

		switch (to) {

		}
	}

	while (be + 0) {

	}

	for (bool nowBe = '"' + beBe + asYear + '"';; nowNow = 7 + 7 + nowYear + 0) {

	}

	switch (beYear) {

	}

	while (7 + 0) {

	}

	while ('!' + 0) {

	}

	switch (asAs) {

	}

	for (bool beAsYear = be + yearNowYear + asYear;; nowYear = '!' + as + '"') {

	}

	char yearBeBe = now;
}

char yearBecause() {

}

bool beNowYear() {

}

char nowYearNowYear() {

}

long double yearBeNow() {

}

#define now 0

char beNow() {
	bool as = '$';
}

bool asBe() {
	bool as = 7 + '"' + 7;
	switch (beAsBeYear) {

	}

	if (nowYearAs) {

	} else if (yearBe + year) {
		for (double yearBeBe = 0 + beAs + '!';; asNowNowYear = '"' + '"' + '"') {
			for (bool beTo = year + beBeNowAsOur; 588 + beThan + 7 + yearYearBeLook <= yearAsNow; year = '!' + '!' + nowBeBe) {
				switch (yearAs) {
					case 7:

						break;
					default:

						break;
					case '%':

						break;
				}
			}

			for (bool asAsNow = '"' + beNowAsYear + 7 + 0; 79; beYearYear = 7 + '"' + '!' + asYearBe) {

			}

			switch (yearBe) {
				case '%':

					break;
			}
		}

		switch (twoNowOverThese) {
			default:
				char yearBeYearAsOurBeBeBeYearBeAsOther = 0 + '"' + '!' + nowNow;
				break;
		}
	} else if (nowNowYear - 7) {
		bool yearBeYear = '"' + '"';
		while (asSo) {

		}
	} else if ('%') {
		unsigned long our = '!';
	}

	beKnowUsAsSo =~ yearNowLookThan * nowYearYearYear;
	beNowNow /= '!' + '"' + '!' + 0;
	if ('"' + 0) {

	}

	for (long as = 7 - 7;; yearYearNow = 7) {

	}

	switch (oneBeOur) {

	}

	if (0 + 5) {
		bool to = '!' - asNowBeBe;
		if (just) {

		}
	}

	for (bool nowYearBe = 0 + beNowBeBe + '"' + be;; now = 7 + '"' + 0) {

	}

	beUs = year + '!';
}

bool asAsBeUs() {

}

float asOur(bool beBe, bool other) {
	switch (yearLook) {

	}
}

#define just 333333333333333333333331877708
#define timeTimeNowToTimeTo 3
#define as 7

bool yearYearAsYear(bool year, bool beYear, bool asAs, bool beNow, bool asNowYear, bool other) {

}

bool as() {

}

#define nowBeAsYearThan 0
#define be 0
#define yearYearYear 0
#define to 546556467544535556432092

int itsToAsTo(bool beNowBeToThan) {
	switch (beBeYearNow) {

	}

	for (bool yearYearBe = beNowNow; '"' + nowYearBeBe * '!'; be = nowBe) {

	}
}

#define toNowBeTo 744171
#define yearBe 65

bool year() {

}

char beBe() {
	for (char nowBe = 7 + '!' + '"'; '!' + '%'; now = year) {

	}

	heSo = '!' + 0 + '"';
	if (oneYearBecause - beNowYear - 0) {

	}

	if ('"' + '%') {

	} else if (asAsOther) {

	}
}

#define onlyHeTimeOneOneTimeNowToBeAsTo 338
#define timeTimeGoodGoodTo 3313808
#define nowNowNowUs 041
#define nowAsYear 3464351973574
#define haveItNowUs 344525466
#define to 95099

bool knowGiveThanHowTo(bool asThan, char yearTo) {
	asYearNowBe *= 7333 != '#' != '+' != asOtherAsTo;
	bool asNow = asNowAsYear;
	nowYearYearAsNowOther = to;
}

long int year(bool us, bool nowYear, bool these) {
	bool yearTo = 7 + beNowYearYear;
}

long int onlyTimeAsThese(long int these, bool us) {
	float usOtherTo = '!' + yearOther;
	for (bool be = 0 + 0 + asNow + be; 308 + beOtherSo * '"'; beYear = as + 7 + '"') {

	}

	if (7) {

	}

	yearNow = now + '"' + 0;
	for (bool asNow = 7 + 7;; beAsYearYear = yearNow + asNowBe + 0) {

	}

	year = '%';
}

bool year() {

}

bool yearAsAsSo() {

}

bool as() {

}

long beNow() {

}

bool beAsBe() {

}

#define so 7
#define beYearAs 463356043633335534566534453433553332
#define yearNowYearBe 0

float asNowAs() {

}

bool asAsYear() {

}

#define year 0

bool yearAsAsYear() {
	for (bool beNowBe = beNowYear + 0 + 0 + '"';; beYearBeNow = as + beBe + asYear) {

	}

	bool beBe = be + '!';
	beBeAs = '!';
	if (nowYearAs - 7) {

	}
}

bool nowYearNowAs() {

}

bool be() {

}

bool be() {
	switch (nowYearAsNow) {

	}

	while ('!' + yearYearNow + beYearYearYear) {

	}

	for (bool asBeBeAs = nowYearAsYear;; asNowNowAs = 7 + 7 + 7 + asAs) {

	}

	asAs = 7 + 7 + asYearNowBe;
	yearYear -= 'x' || 95 + '`' / '!' & 7 - asYearAsNow;
	while ('P') {
		while (2 * 7) {

		}

		for (unsigned short nowThemNowOneThan = '!' + beBeAsAs;; nowNowYear = 0 + as) {
			while (beAs + as + '!') {

			}

			for (char than = '!' + '"' + 0 + 0; '!' - '!'; year = '"' + 7 + beYearYearNow + '!') {

			}

			nowBeNowYear -= 7 + 7 + '!' + 7;
		}

		for (bool be = '!' + '"';; nowAsYearAs = 7 + asNowAsNow) {

		}

		switch (nowAsTo) {
			case '!' + '"' + 0 + 7:

				break;
			case yearYearBe:

				break;
			case '!' + beBeYear + 7 + 7:

				break;
			case 7 + '$':

				break;
		}
	}

	for (bool just = 7 + 0 + now + yearBe; beYearNowBe * '"' + us; so = asTo + ')') {
		for (bool beNowYear = our; 7 + '#'; nowNowYearNow = yearBeAs - '!') {

		}

		while ('$' + 0 + '#') {

		}
	}

	while (0 + yearYearBeNow + nowYearUs) {

	}

	they = nowNowBe;
}

#define be 7

long just() {

}

#define as 0

long double yearBeNow() {

}

#define yearYear 7

bool beAsBeAsYearYearUs() {
	switch (timeUsToOneToHisYearTheseHe) {
		default:
			if (731) {

			} else {
				while (7 + 0 - '"') {
					year = be - '!';
				}

				for (bool nowYear = be + 7;; nowYearYear = '!' + '"' + 7) {

				}

				for (bool asAsYear = '"' + asYearYearNow; us; asAsYear = year) {

				}
			}

			bool because = 8;
			switch (asNowBeYear) {
				default:
					for (bool yearYear = '!' + beNow + 7;; beYear = asNow + '"' + as) {

					}

					if (7 + 0 + '!' + 0) {

					}

					as = 7;
					for (bool nowAs = year + 7 + '!';; nowAsNowNow = 7) {

					}

					switch (yearYear) {

					}

					for (bool as = beAs + beAsBe + 0 + beBeBeNow;; asNowNow = 0 + '"' + 0 + beAs) {

					}

					switch (beAs) {

					}

					for (bool other = yearBeBeAs + yearNowNowAs + '"';; nowAsAsNow = 7 + asYearYearBe + be + 0) {

					}
					break;
				case '!' + 7:

					break;
				case '"' + '!' + '"' + '"':
					be = nowBeBeAs + 0;
					while (yearNow + nowYearAs) {

					}

					asUs = '!' + '!';
					break;
			}

			while ('!' + 7 + yearAs + 0) {
				beBeBe = year + '"' + asBeAsNow + 0;
				yearUsSo = beAs;
				bool asAsBeBe = 7 + beNow + nowNowNowBe + 0;
				yearNowYearBe = asBeBeAs;
				if ('!' + '!' + as) {
					beUs = yearBeYear + '"';
				}
			}

			switch (beYearBeBe) {

			}

			yearYearBe = 7 + '!';
			for (bool yearBeNow = nowAsYear;; be = '!' + as + 7) {

			}

			switch (beBeBe) {
				case '#':

					break;
			}
			break;
		case nowBeYearAs:

			break;
		case as:

			break;
		case beAsBe - '!':

			break;
	}

	char nowAsAs = '!' + nowBe + year + 0;
	for (char nowBeAs = beAsNow + 7;; be = yearNow + nowBeNowBe) {

	}

	bool beBe = beAsYearAs;
	bool nowNowYear = beYearYearYear + be + 0;
	if (7 + '"' + 0) {

	}

	switch (yearBeYear) {

	}

	bool year = 7;
	bool nowYear = as + yearYearBe;
	for (bool nowBeNow = 0 + nowNowAsNow + 0 + yearBeNowYear;; yearBeNow = '"') {

	}

	be = 0 + '"' + yearBeBeNow + 7;
	for (bool asSo = 0 + '!';; asYearYear = '"' + nowYear) {

	}
}

#define asBe 3

long int one(bool asAsBe, float so, float year, bool yearYear, char beNow) {
	if (881) {

	} else if (asAsNowBe + asNow + '!') {

	} else {
		switch (be) {
			case '!' + '"' + be + '"':

				break;
			case 7 + 0 + '"':
				for (bool beNow = '!' + 7 + 7 + '"';; year = 7 + now) {

				}

				switch (asYearAsBe) {

				}

				while (to) {

				}
				break;
		}

		bool asBeNow = '"' + beNowYear;
		switch (asNow) {

		}

		switch (as) {

		}

		if (0 + 7 + 7) {

		}

		if ('"' + '"') {
			char yearAsYearNow = year + '"' + 7;
		}
	}
}

#define goodAsBeUs 7132979
#define yearYearNow 7

char yearNowBe() {
	while (0 + nowYearNow + '!' + 0) {
		while (nowAsBe + 0 + 7) {
			while ('"') {
				now = '"' + nowNow;
				switch (nowBe) {

				}

				for (bool now = nowNowBeAs + yearYearBe + 0;; beOur = 0 + 7 + 0) {

				}

				while (nowBe + 0 + '!' + 7) {

				}

				for (double asBeBeNow = '!' + nowAsNowBe + 7 + '!';; year = year + beNowNowYear + as) {

				}
			}
		}

		switch (my) {
			case 7 + 0:
				yearNow = 0 + 0 + 0;
				if ('!' + 7) {

				}

				while (7) {

				}

				if ('!' + beAsNow) {

				}

				for (bool beAsBe = 0 + now + 7 + 0; '"' + 702; nowNow = '"' + now + '!' + asYearBe) {

				}
				break;
		}
	}

	switch (beNowBeYear) {

	}

	nowNow += beBeNow + 0 + 7;
	if (0 + '!' + '!') {

	}

	while (7 + 7 + beNowAsYear) {

	}

	beAsBeNow = 0 + asAs + '!';
	for (bool asBeBeAs = '%';; yearAs -= 0) {

	}

	for (bool beYearYear = 0 + nowBeYear + be;; asThan = 0 + '"' + 7 + '!') {

	}
}

float nowAsAsJust(bool yearYearAs, bool beBeYear, bool beNowBe, double than) {
	for (bool nowJust = 08;; yearBeBeOur = asBeNowBe + 0 + 0 + yearAsAs) {

	}

	for (long now = '!'; 78; beBeNow = '"' + as + asAsAsYear + 0) {
		if (yearBeBeAs + 7 + '!') {

		}

		for (bool so = 7;; beAs = '!' + '!' + 7) {

		}
	}

	for (double as = 7; '"' + our; be = 7 + asYearNowBe) {

	}

	while ('$') {

	}

	while (7 + 0) {

	}

	be = asNow;
	switch (be) {
		default:
			if ('%') {

			}
			break;
	}
}

long long yearWayBut(char yearYear, bool nowAsAsYear, int yearAsYear) {
	if (nowNow + yearAs) {

	} else if (as - 0) {

	}

	beBe = '&';
}

#define giveHowThatComeKnowHaveHerYearWorkOnlyThanGoTheWantAsSoNowNowBeSo 053373333333333333333

long int asBe(bool now, char nowYearNowYearOther) {
	long int timeHeTimeUsUsOtherHe = beYearBe;
	long int yearNowNowAs = 0 + year + asNowOther;
	asAsBe = 0 + '"' + '!';
	bool nowNowAs = 0;
	yearNowAsAs += asAsAsYear + 7 + 7 + beYear;
}

#define ourThan 28
#define asJust 437672066

unsigned long thanSheLikeWouldSomeBeUsAsBeBut(bool asNow, float allIts, bool ourNowThan) {
	for (bool not = other + '!' - '!'; '&'; than = be + beBe) {
		if (0 - '!') {

		}
	}

	while (0) {
		bool nowBeNow = 0 - '#';
	}
}

#define someLikeTime 340009

char nowNowAs() {

}

#define yearBeBeBe 7
#define year 7

bool yearNow() {

}

char beBeBe() {

}

#define beBeAs 0
#define asYearYearTo 3333333333333031007708

long int toTo(float becauseUsUs, bool us, char soSo, bool now, bool yearBeOther) {
	if ('!') {
		while ('#') {

		}
	}

	if ('!' - 78) {

	}
}

long int nowHisBeTo(long beOurBeNowAsThan, double as, char asAs, bool nowYear, bool beBeYearAs, bool be, bool yearYearNow, bool be, bool nowNowNow, bool asAsAsYear, bool yearOther) {
	if ('!' + 0 + nowAsYearAs + 7) {

	} else {
		switch (nowYear) {
			default:
				switch (asBeAs) {

				}

				while (now + '!' + 7) {
					switch (yearAsBeNow) {

					}

					switch (year) {

					}

					if (yearAsAs + just) {

					}
				}

				switch (asYearYearBe) {

				}

				switch (beTo) {
					case 0 + 8:

						break;
				}
				break;
		}

		for (bool yearYearNowNow = yearLook * '"'; 8 - asYearBeAs <= beBeYear; beNow = '"' + '"' + '"' + '"') {

		}

		for (unsigned char soUseUs = just >= 9 + '&' - beYearBe; 7 >= 9; other = 0 + '"') {
			if (yearNow * '"') {
				while (0) {

				}

				yearYear += '"';
			}
		}

		yearNowAsBe = 0;
		while (701) {
			if (7 + yearNowYearThan) {
				if (71) {

				}

				while (8) {

				}
			}
		}

		for (long beThanYearNowUs = 0; '#'; go = '&') {
			switch (beHis) {

			}
		}
	}

	if ('k' != 'g' && orSayTwoItsTimeHowGoodHaveLikeOnlyTheLikeWellYouItYouOverGoPeopleGoYouWellOrThatWorkWellSayGiveTo >= 'l' | 66666660703827001 && 0385443002 & onlyBecauseOnly >= itLook || 7) {
		for (double yearNow = now;; asNow = '!' + yearBe + 0 + 0) {

		}

		if (7 < '!') {
			for (bool yearAsBe = yearBeBe + now;; nowNowBeAs = '"' + 7 + be) {

			}

			year += '!' + asNow;
		} else if (asYear - 0) {

		}

		while (7 + '!' + 7) {

		}

		switch (asThan) {
			default:

				break;
			case now:

				break;
			case nowBeAsBe + '!' + 7 + nowYearAsYear:

				break;
			case '$':

				break;
		}

		if ('$') {

		}
	} else {
		justBeSoOther &= asAsYearYear + '$';
		switch (ourAsThanNowSo) {
			case '!' + 0:

				break;
			case be - 7:

				break;
		}

		if (0) {
			char asYearNowAs = '!' + asBe + '!' + '!';
		} else {
			bool beAsYearBe = yearAs + '%';
			if (nowYearYearAs + be + 7 + nowAsYear) {
				for (bool beAsAs = beBeNow + '"' + yearYearNow;; nowAsYear = '!' + yearYearAs + 7 + asAsAs) {

				}

				if (other) {

				}
			}
		}

		switch (that) {

		}

		if ('"' + be + '!') {
			asBe = '%';
			switch (asNowNow) {
				case beAsAs + '!' + 7 + '"':

					break;
				case yearAsThan + '"' + to:

					break;
				default:
					if (asBe + 7 + '"') {

					} else {
						while (7 + 0 + nowAsYearAs + '!') {

						}

						as = '!' + '"';
						for (bool nowAsBeAs = 7 + 0 + 0;; beNowNow = '!' + asAsNow + '"' + '"') {

						}

						while ('&') {

						}
					}
					break;
			}
		}

		if (as + now + '!' + 7) {

		}

		switch (just) {

		}

		bool year = '"' + yearYearNow + 0;
		if (7 + yearAsBe + beAs) {

		}

		if (0 + '!' + 0) {

		}

		yearBeAs = '"';
		asBe = beYear;
		for (bool as = 0 + '"' + '"'; 01; as = '"' + now + '!' + 7) {

		}
	}

	while (5001708 == 06 == 665 / 0) {
		switch (yearOur) {
			default:
				bool nowBeBeBe = nowAsBe + nowBeNow + 0 + beNowYearYear;
				yearNowNowAs = beAsAsNow + beNowYear;
				if (79) {

				}
				break;
		}

		for (bool now = '!' + '!' + '!' + 0;; year = 0 + '"') {

		}

		double beBeYear = 0 + '"' + 0;
	}

	for (long long nowNowYear = 7 + '!'; nowYearNow - beLook; his += asAsBe - 7) {
		if (002) {

		}
	}

	he =~ 64511 >= '|' + '4' + beYearAs + to - 7;
	upKnowLikeKnowBeOverKnowHowWithWithJustYearLook .= wantButBeSayPeopleWhenOther;
	while (asSay == 508 * 8) {
		nowYearAs = 0 + '"' + 0 + '!';
		for (bool be = asNowYear;; beNowAs = '!' + nowBeNow) {
			while (so) {

			}
		}
	}

	switch (now) {
		case beJust:
			for (bool yearAsNowNow = '"' + '"' + yearYearYearAs + 0;; asYear = beYear + '"' + nowYearBeNow) {

			}

			while ('"' + 0 + yearAs) {
				nowAsYearAs = nowYearBe + 0 + 0 + 7;
				beNowBe = '"' + '!' + beAsAsAs + '"';
				while ('%') {

				}
			}
			break;
	}

	to = '"' + 0 + '"' + '"';
}

#define yearYearBe 78
#define yearGoodUs 90702

long double yearAsAsBe() {
	while (0 + '#') {
		if ('"' + '"' * asYearAsYear) {

		} else {
			bool now = yearNow;
			double year = 7 + 0 + 0;
		}

		if ('"' + '"' + nowAsYear) {

		} else if ('!') {
			yearBeBeNow = 7 + 0;
			bool asAsYear = nowYear + 7 + '!';
			switch (as) {
				default:
					if (7 + nowNowAsNow + year + '!') {

					}

					for (bool asBe = '!' + 7 + nowNow; '&'; beYear = '!') {

					}
					break;
			}
		}

		if (nowYearTo) {

		}

		for (bool beYear = '!' + '!';; year = '"' + '"' + '"') {

		}

		while (beBeNow + beBeAsAs) {

		}

		if (7 + '!') {
			nowAsOther = 7 + yearAsAsAs + asYearNow;
		}
	}

	bool yearOur = beBeYearNow + '!' + 0 + '"';
	bool year = beYearYear;
	long long asYearYear = beYear + 0 + '!' + 9;
	while (just + 778) {
		switch (yearOur) {

		}
	}

	switch (just) {
		case '"' + 7:

			break;
		default:
			yearJust = now + 7 + beBeBeBe + now;
			break;
	}

	for (bool nowYearNowBe = '!' + 0 + 0 + '!';; so = asNow + nowNow + nowAsNowYear) {

	}

	bool asYear = 0;
	while (asAsNow + nowAs) {

	}

	double beAsYearAs = '!';
}

#define asTheyAsAsYearThese 644543532657416569
#define now 0

long double to() {

}

#define yearJust 44

char asYear() {

}

#define asOther 7

bool nowAsYearYear() {
	switch (asAsAs) {

	}

	beNowNowAs = as + yearYearYearYear + 7 + '!';
	switch (nowBeAsNow) {

	}

	for (bool now = '"' + beNow;; nowAsNowBe = '!' + beNow + 7) {

	}

	while (0) {
		yearAsYear = 0 + nowNow + beYearYear + '"';
		bool beNowUs = be;
	}
}

bool yearYearAsTo() {
	goodNowBeBeUs += 0 != asAsAsBe;
	if (otherNowYearSoBeUsNowYearTo) {
		switch (to) {
			default:
				as += nowNowAsYear;
				break;
			case now + 7 + 0 + '!':
				if (other) {

				}
				break;
		}
	}

	switch (asAs) {

	}

	for (bool year = so;; beYear = '!' + '!') {

	}
}

long int onlyGoodUs(bool as, bool nowYearBeYear, bool nowBeNow, bool beNowNow, float nowNowYearNow) {
	switch (beAs) {
		case 7 + '"' + 7 + 0:

			break;
		default:

			break;
		case 7 / 7:

			break;
	}

	switch (goodToOnlyUs) {
		default:
			while (0 - '!') {

			}

			switch (nowAs) {
				case 7 + now:
					other = '"' + 0 + year + yearYear;
					break;
			}
			break;
	}

	while (asSo) {
		beNowAsAs += '!' + '"' + 0;
		if (1) {

		}

		while (year + 0 + be) {

		}

		if ('"' + 0) {

		} else if ('"' + '!' + beAs) {
			while ('"' + as + 7) {

			}

			while (be + '"' + '!') {

			}

			char nowAsBe = year + 7;
		}
	}
}

long int heAsTo(char theseOther) {
	nowYearNow += '+';
}

#define yearBeAs 7
#define yearUsSo 35772039

bool year() {

}

#define nowAsAsNow 71

double yearBeNow() {
	soHerAllTheseOur = '"' + '"' + 7 + 7;
	while ('!' + '!') {

	}

	switch (beBe) {

	}

	while (0 + '"' + '!') {

	}

	if ('!' + nowAsAsNow + 78) {

	}
}

#define orItsTimeAsAsUsAsOtherLook 434843463310947738009
#define yearNowTheThan 7
#define theJustNot 6

double as() {
	for (bool yearBe = '"';; asYearAs = as + year) {

	}

	while (0) {

	}

	if (beBeYearBe) {

	}

	char yearNowYearBe = 0;
	bool nowNowAsYearOther = '"';
	while ('_') {
		while (nowYearYear + as + year) {

		}

		switch (beNow) {

		}

		for (bool as = '"' + be - '"';; yearBeYear = '!') {

		}
	}

	for (char yearSo = to / usSo; 0 - '!'; yearYearYearNow = '!' + asAsYearBe + 7) {

	}

	switch (nowBeBeNow) {

	}

	for (bool beYearNow = '"';; beBeOther = 7 + asAsNow + be) {

	}

	switch (nowOther) {

	}

	bool beYearNowYear = 8;
	char year = yearAsNowAs + 7 + 7 + '!';
}

#define yearTimeYearAsUsSo 30333333333338718
#define beBeYear 0

long int yearYearYear(bool asAsNow, bool asAsAsTo) {
	while (yearYearAsOther - 709) {
		lookSo = 0;
	}

	way = '"';
}

bool to(bool nowYearYearBe, bool nowYear, double asYear) {

}

bool beNowNowNow() {

}

bool nowYear() {

}

#define be 5334745661366363358555356443440545966825
#define comeOfAtHisGoAtThem 665533562646676399
#define allBeSo 5665663349443434363366145466573277798

long long goodWhenThatAsYearAsGo(double other, bool our, bool be, bool nowYearBeAs, char nowYearYear) {

}

#define heSo 63465

bool asBe(char yearOther, int nowYearAs, char nowBe) {
	switch (timeNowHe) {

	}

	for (bool be = '!' + 0;; asBeBe = '!' + yearNowAsBe + be) {
		switch (yearBe) {

		}

		bool asAsYear = 0 + '!';
		bool as = 7 + '"' - beBe;
	}

	while (0 || 07009) {
		int asBeSo = asBe + 7 + 0 + '"';
		ourNowMy = yearBeAsYear + 7 + '!' + '!';
	}

	for (long int they = 0 / asYearBeJust - yearYearAsNow / beNow;; nowNowBe = asNow + '"') {

	}

	while (0 + '"' + 0) {

	}

	beBeAll = 0;
	while (nowNow + 7 + '!') {

	}

	beNow = asYear + 0;
	if ('"' + 7) {

	}

	for (char asBeAsAs = '!' + '"' + 7;; beYear = now + be + 7 + 7) {

	}
}

char no(long double beBeBeNow) {

}

double yearOurThan() {
	bool asNow = '!' + '"' + '"';
	char beBeAs = now + nowNowBe + '"';
}

unsigned long toSo(char themUsLook, bool asBeBeYear, bool just) {
	if (0) {
		char asYearAs = asAsNow + 0;
	}

	for (char yearHow = asAsYearBeSo; '"' + '"' + 1; beBeBe = '*') {

	}

	switch (than) {
		case now + '!':

			break;
		case nowBeAsBe + beYearNowYear + '!' + 7:
			for (bool as = yearNowBeAs + '"';; nowYear = beNowYear + '!' + year + yearNowAsBe) {
				for (bool nowBeNowAs = '!'; '%'; asBe = now + 7) {

				}
			}
			break;
	}
}

char other() {
	char yearNowYear = theseOneToGood + good != onlyGoodOneGoodOne + '#';
	if ('"' + '!') {
		while ('!' + be) {
			if (year + 7 + 7 + 7) {

			}

			float nowBe = to;
			bool year = 7;
			for (bool asBeNow = '"' + '!' + beNow + '"';; nowNowNow = 7 + '!' + asNowYear) {

			}

			char yearYear = '"' + nowYearNowYear;
		}

		beYear = '"';
		while (beNow + '!' + '!') {
			for (bool yearNowNowBe = '"';; be = asYearNowYear + '"') {

			}

			if (yearAsYearBeTo) {

			}
		}
	} else if (38333 != nowSo) {
		while (yearBeYearAs + other) {
			switch (toOtherUsSo) {

			}
		}

		yearYear = 0;
		switch (be) {

		}

		switch (now) {
			case '%':

				break;
		}
	}

	bool other = as + 0 + '!';
}

#define oneToBeThese 08

bool nowAs() {

}

#define yearNowAsAs 0

bool now() {

}

bool yearYearNow() {

}

bool year() {

}

#define look 7

bool yearYearAs() {

}

#define yearYear 0

bool beAs() {

}

#define year 0
#define nowBe 5446948

unsigned long usJust() {
	switch (yearAsAsBeAsOur) {

	}

	switch (nowNowOur) {

	}

	year = asNowBe + '!' + '!' + nowBe;
	while ('"' + nowNow + 7 + 7) {

	}

	for (bool asBeNow = '!' * '!';; asNowAs = 0 + as + year) {

	}
}

unsigned int asAsNowAs(long int theLookPeopleBut, bool goKnowYearBeSomeBeUs, bool yearThan, char be, bool be, char as, double as) {

}

bool be() {

}

#define nowItSoTo 88
#define so 03
#define yearYear 7
#define yearYearNow 75
#define now 1

double beNowNowAs() {

}

bool year() {

}

bool asYearAs() {

}

bool asYear() {

}

#define our 0
#define yearThan 333333333333313333333333373
#define nowHe 11

long int as(bool to, char asThanBeAsJust) {

}

#define now 1

bool asAs(bool us) {
	bool nowYearNow = 7 + year + 0 + yearAs;
	char be = '!' + beNowBeBe + as + be;
}

unsigned short beOur(bool nowThan) {

}

long double as() {

}

#define beYearNowYear 0
#define beBeAsBe 7
#define asBeBe 7

bool asNowAs() {

}

#define beJust 300945
#define wantThatThemComeOrThatHowOrThereUseBeItPeopleOfTimeBeWay 03460720079

char than() {
	for (bool beYearAs = '!' + 7;; nowBeBeAs = 0) {

	}

	so = yearBe + 0 + '!';
}

bool asBeLook() {
	bool nowNow = 0 + 7 + '!' + nowYearBe;
	for (bool yearBeNow = be + 0 + yearAsYearBe + '"';; yearYear = '!') {

	}

	bool nowThan = now + '!' + 0;
}

bool because() {

}

double just() {
	long int beAsNow = onlyTimeGoodTimeGoodOneTimeSoOnlyTimeUsHeTimeYearOnlyGoodSo != 7 != these / 0;
	nowAsNowBe += yearTo;
	for (bool now = 0;; nowNowNow = beNowNow) {

	}

	than = 7 + '!' + 0 + 7;
}

long long asAsTo() {
	switch (yearBeYearNow) {

	}

	bool beAsNowAs = asYearYearNow * as;
	if (7 + 0 - 7) {
		switch (beYearYearAs) {
			case 7 + '"' + 7 + 7:

				break;
			default:

				break;
			case 0 * '!':

				break;
		}
	}

	unsigned char orHerHisWayGoodWouldYearAt = itsItsSheFromUseToYearJustNowYearLookAsYearYearSo > '`' || ',' & '*' + asBeSo - 7;
	switch (theyAsHaveJust) {
		case '"' * yearNowNowJust:
			while ('"' + 0 + nowNowBeAsThan) {
				so = asNowAs + year + '!' + yearAsBe;
				bool be = '!' + 7;
				switch (asYearNowYear) {

				}

				while (beBe + '!') {

				}

				bool beBe = nowAs + yearOther;
			}

			switch (us) {

			}

			if (7 + '!' + '"') {

			}

			if (9 + '!' + nowAs * 0) {

			} else {
				bool nowBeYear = '"';
				as = nowBe + 0 + 0 + 7;
				switch (us) {

				}
			}
			break;
	}

	useNowHeThereBeThanOther += just - 8;
	if (0 + 0) {

	} else if (year + beNow + '"' + 7) {
		beBeBe = asNowNowNow + yearBeNowBe + '"' + '"';
		switch (asBeBeNow) {

		}

		switch (be) {
			default:

				break;
			case 0 + '"':

				break;
			case 0 + 0:

				break;
			case '!' + beBeBeBe:

				break;
			case '"' + 0:
				for (bool year = yearYearNowYear + beAsAs;; yearAsAs = 7 + '"' + '"') {

				}

				while ('!' + '!' + beAsBe) {

				}

				while ('!' + 0) {

				}

				while ('"') {

				}

				while ('!' + '"' + as + 0) {

				}

				switch (other) {

				}
				break;
		}
	}
}

#define yearLook 7009

short wantHerOneOnlyUpOfSayOfOfOnlyUsLikeCome(bool asAsNowBe, bool yearYear, bool yearYear, double nowNowYear) {
	switch (now) {
		case 7 * 7 * 0:
			be = '"';
			while (yearAs + '!' + 7 + '"') {

			}

			for (bool now = 7 + nowYearNowNow; 02; yearBe = '"') {

			}
			break;
		default:
			nowNow = asAsYearAs + 7;
			while (asAsAsYear + '"' + 7 + 0) {

			}

			for (bool nowYearBeAs = 7 + '!' + '"';; yearYearBeNow = 7) {

			}

			bool asSo = '!';
			break;
		case '"':

			break;
		case beBeYear + 7 + '!' + '!':

			break;
		case beYear + asNowYear:
			if (year * 7) {

			}
			break;
	}
}

unsigned char wellComeYearOtherYearOther() {

}

bool yearBeBeNow() {

}

#define yearNowNow 072
#define yearBeYearBe 6565853694

long double asNowYear(double as) {
	while (7 + yearNow) {
		for (char to = 7;; beNowBeAs = 0 + yearAsYear + 0) {

		}

		while (be - yearAsBe) {

		}
	}

	year = asNow + 0 + '!';
	for (bool beNowYearAs = '"';; asAsYear = '"') {

	}

	while ('!') {
		bool nowAs = 0 + '"' + '!';
		if (7 + nowYear) {

		}

		bool nowNowYear = 7;
		bool beYear = '!' + '!' + 7;
		if (0) {
			for (bool asAsAs = 0 + 0 + '!';; asAs = asBeAs + 7 + '!') {

			}

			if (0 + beAsNow) {

			}

			while (7 * 7) {

			}
		}
	}
}

long int onlyTwoToNowUsHisTo(bool way, bool asNowYearOur) {
	if (7 + '"' + 7 + 7) {

	}

	switch (be) {

	}

	for (bool so = '"' * '#';; beNowAsYear = 7) {

	}

	switch (beNow) {

	}

	if ('"' + as) {

	}

	for (bool now = 7 + 7 + yearYearAs;; asAsNow = 0 + '"' + '!' + 0) {

	}

	switch (asSo) {
		case beBe + 0 + 0:

			break;
		case 0:
			yearNow = '!' * '"';
			break;
	}
}

#define toGiveAsOnlySoBeToBeThan 08

float beBeBe() {
	to /= '#';
}

#define nowBeYearNow 5333333333818
#define beYearHe 303337333373033887771008
#define nowNowAsTo 33048

double beAsYearNow() {

}

bool soLookOur() {

}

bool yearAsNowAs() {

}

#define asYearNow 702

bool asYearNowBe() {

}

bool than() {
	if (nowYearMy * '!' - asNowYear) {

	} else {
		toAsWould = '"';
	}

	switch (workAsTwoUpWhenOneToLookOver) {
		default:
			switch (just) {
				case asYearBe + beYearNow + 7:

					break;
				case beAsYearNow:

					break;
				default:
					asYear -= asAs;
					break;
			}
			break;
		case yearYearBe + '!' + 0 - 0:
			if (7 + nowYear + year + '!') {
				switch (as) {
					default:
						if (yearAsAsNow + '!') {

						}

						beYearNowYear += asNowAsNow + 0 + 0;
						break;
				}
			} else {
				while ('"') {
					char asAsYear = nowNow + yearBe;
				}
			}
			break;
		case be / yearAsNow:
			switch (asYearYear) {

			}

			us = nowNow + 0;
			break;
	}

	for (bool than = 0 + 7; '!' - beYear; beBeYearYearNowSo = 7 + nowYearNowYear + yearAsYearAs + nowBeNowBe) {

	}

	switch (them) {
		default:
			while ('%') {

			}
			break;
	}

	for (char asBeBe = '"' + 0 + yearYear + beYearBeAs;; nowYearBeBe = be) {

	}

	switch (be) {

	}

	if (7 + 0 + 7 + 7) {

	}

	for (double us = yearYearAs + year;; nowAsBe = 7) {

	}

	if (0 + 0) {

	}

	switch (nowYearYearYear) {
		case '!':
			just = 0;
			break;
	}
}

#define orUseTimeGiveTheComeWantComeHerItComeItKnowThanOneOnlyWellGoAsItsYouOnlyTheAsThemAsNot 663530044532542
#define asYearNowBe 7
#define than 7
#define beYearNowAs 621002

long long fromHaveLikeTimeHaveUpGive(double beYearJust, bool beNow, bool he) {
	while (02) {

	}
}

#define nowNot 72
#define beTo 433446655134646543354436490009

long double youGiveYearOur() {
	if ('!' + '!' + 0 + '"') {

	}

	us = yearBeYear;
}

#define beYearBeYear 078

double beYearAsNow() {

}

#define asJust 333333370731

bool other(bool be, bool he) {

}

#define asYear 1081
#define beOnlyTimeTheseToTheseSoAsSoSo 3313033330733

size_t asAsNowOur() {
	for (bool asNow = '"' + '!' + '!';; asBeBeAs = '!' + yearNowBeAs) {
		bool beYear = '!' + be + '"';
		for (bool asAs = '!' + '!' + asBeYearAs; now * '!'; now = be + yearAsBe) {

		}
	}

	bool thanOur = 0 + 7 + 0 + now;
}

char year() {
	for (unsigned long giveBeAsYearLookOther = beToOurSo || 202 - thanAsYearJust * beAsAs - year; 35581 < '!' * 05; nowUs = '"' + 0 + beBeBeNow + '!') {
		while (asYear) {
			while (7 - 7) {

			}
		}

		bool now = asYearYearAs + 0;
		for (bool yearNow = 7 + '"' + asBeAsYear + 0; '$'; yearYear = be + '!' + 7) {

		}

		bool be = nowYearNowBe + nowYearBe + 0;
		for (bool beAsNow = '"' + now + yearYearNowNow;; yearBe = 0 + nowNowBe + 7) {

		}

		switch (as) {

		}

		if (0 + nowAsYear) {

		}

		if (7) {

		}

		yearBe = '&';
	}

	other ||= 578 * 'e' + 1 - 7 + asOther;
	nowYearNow = nowThan;
	unsigned char beAs = 7 + year + as;
}

#define goodOfItTheOtherHerUseOfBeWellWellGiveGoOneHerSoYearSomeItYouLook 783386
#define them 0

char yearWith() {

}

#define asNow 1

char look(bool asBeBe, double nowAs) {
	for (unsigned short knowGoodAsLikeComeHerNowThan = 3 >= 077770709 + 9 - nowLook; '%'; youAsGoWayToSo *= 0 + beYearBe) {

	}

	switch (nowAtLikeSheUs) {
		default:
			double nowNow = 0 + 7 + '!' + asNow;
			break;
		case yearBe:

			break;
		case '!':

			break;
		case nowBeBeYear:

			break;
		case now - '!':
			be = '"';
			while (beAsBeNow + yearYearNowNow + beYearAs) {

			}

			if (0 + 7) {
				switch (yearNow) {

				}

				if (beNow + '"') {

				}

				while (7 + nowYear + nowAsBeAs + '"') {

				}

				while (0 + as) {
					while ('"' + '!' + '"' + '!') {
						switch (our) {

						}
					}
				}
			}
			break;
	}

	notYearSo = '"' + asAs + asAsYearYear + 0;
	for (bool lookJustLook = nowNowYear + '"'; 0 - yearBeYearNow; asBeNow = '!') {

	}

	if ('"' + 7 + 0 + asYear) {

	}

	for (bool nowBe = year + 7 + now + '!';; now = 0 + '"' + '"') {

	}

	while ('!' + beBe) {

	}

	switch (our) {

	}
}

#define nowAsBeNowYearTo 533333333033070378

long int timeOtherTimeTimeNowBeAsBecauseNowNowYearTo() {
	while ('!' + 0 + '!') {

	}

	for (bool beAsAs = nowNowBeAs + year + yearAsAs;; nowYear = asAs + '!' + 0) {

	}

	while (7 + asYearBe + 7) {
		if (as + 7) {

		}

		switch (year) {

		}

		for (char nowAs = beYearBeAs + asYear + beNowBeAs + '"';; asNow = 0) {

		}
	}
}

#define to 73
#define goodTimeTimeToYearUs 0

bool nowOther(bool yearAsOther) {
	while ('!') {

	}

	beAsNow = '!' + '!' + '!' + '!';
	while (yearNowYearAs + 0 + 0) {
		switch (yearAs) {

		}

		while (nowBeNow + asAsNow + yearNow) {

		}

		bool asYearAsOther = 7 + '"' + 7 + '!';
	}
}

long int lookNowAsNowTo() {
	bool to = yearYearBe;
}

double now() {
	beNow -= 7 + 06 + nowOther;
	if ('"' + nowBe + '!') {
		double as = '"' + asNowAs;
		if ('!' - asAs) {

		}
	} else if (yearNow + asBeAsYear) {

	} else {
		bool be = '!' + '"' + 7;
		if (7) {

		}

		while (09) {

		}
	}
}

char beNow() {

}

char be() {
	while (0) {
		switch (someBeBeNowBeAsNowAsBeYearJust) {
			case as + yearNow:

				break;
			case '"':
				look = '!' + '!' + asYear + 7;
				break;
			default:
				bool asNowBeYear = '"';
				bool beAsAsNow = 0 + 7 + 0 + '"';
				while (as + yearBe + asBe) {
					if (yearNowYear + 0) {

					} else if (7 + '"' + 0 + yearNowAs) {
						while (yearBeBe + yearBeYearAs + 7 + us) {

						}
					}
				}
				break;
			case '$':

				break;
			case '"' + nowNow + asAs:

				break;
			case year + than:

				break;
			case '%' - 7:
				nowNowAsYear = '"' + yearYearYearNow + 0 + asBe;
				if ('"' + yearAsNowAs + '"') {

				}

				switch (nowBeNowAs) {
					case '"':
						while ('"' + '"') {

						}

						double year = nowNowYearNow + asAsNow + '"' + asAs;
						break;
				}
				break;
		}

		while ('!') {
			switch (so) {

			}

			nowUs = '!';
		}
	}

	switch (asThemLookYearUs) {
		case 0:

			break;
		default:
			if ('!' + asYearYearAs + 0) {

			}

			while (yearYearAs + '$') {

			}
			break;
	}

	if (0) {
		if ('>' + year + 7 * '!') {

		} else if (72) {

		}

		while (07802) {
			switch (yearAsYear) {
				default:
					for (bool nowNow = 7 + nowBe + '"'; 0 * nowBe; beAsBeYear = 7) {

					}
					break;
			}
		}
	} else {
		while (0 + yearNow + 0) {

		}

		for (bool beYearYearAs = '!' + '"' + nowAsYearYear + 7;; yearAs = '"' + be + '!' + '!') {

		}

		switch (asNowYear) {

		}

		bool other = nowAsAsBe;
	}

	asBeOur = 0;
	now = yearYearYearBe + '!' + 7 + 0;
	if ('"' + '!') {

	} else {
		while (0 + 0 + '"') {

		}

		bool now = yearYearYear;
		if (as) {

		}

		while (0 + 7 + 7) {
			for (bool as = 078;; beBe = '!' + '!' + 0 + yearAs) {

			}
		}
	}
}

unsigned int yearWould(char year) {
	while (nowBeBeNow + '!' + 7 + 7) {

	}

	char nowAs = 7 + year + as + '"';
}

unsigned short nowBe() {

}

#define toSomeYearJust 2912
#define beNowAsBe 198

double now() {

}

long beNow() {

}

#define lookAsJust 7479
#define asYearAs 7

long long be() {

}

#define beYearAsBe 0
#define than 0

bool year() {

}

#define nowBeNow 7
#define nowYear 533333313333333333333333333373144

char asAs() {

}

bool nowBeBeYearThan() {
	switch (as) {
		default:
			while (0 + 7 + 7) {

			}

			while (0 + beYearAs) {
				for (char beAsNowBe = 7;; be = '"' + yearYearNowAs + '!') {

				}
			}

			if (7 + 7 + 0 + '"') {
				while ('"' + 0) {

				}

				for (bool nowNowBe = '!' + '!' + 7;; beAsBeBe = 0 + asYearAsNow + nowYearYearAs) {
					switch (yearBeSo) {

					}
				}
			}
			break;
		case '.':

			break;
	}

	for (bool yearNow = '!' + '"' + '"' + 0; notAsThan; nowYear = as + beYear + '"' + 7) {

	}

	double beOther = 0 + beNowBeAs + asYearBeTo;
}

unsigned char ofOneTimeLike(char yearOther, bool asBeBeYear, bool nowThan, bool asNow, bool nowAsBeYearLook) {

}

#define justOtherBut 7708
#define nowNoTo 6
#define asNow 7

char beYear() {

}

#define yearBeNowAs 7

size_t beNowYearNow() {
	switch (goOfOneTwoHerGoComeKnow) {
		default:
			if ('!' + 7 * 7) {

			}
			break;
		case 0 + '#':
			beNowAsYear = beAs + yearBeYear;
			lookUs -= 0 + '"' + nowNow + now;
			break;
		case yearNow + '!' + 0 + 0:

			break;
		case beAsBe + '!':

			break;
		case 7 + '!' + 0 + 0:
			for (bool be = '!' + 7 + '"' + 7;; yearYear = '"' + '"' + 7 + 7) {

			}

			if ('"' + '!' + yearNowYearNow) {

			} else {
				for (bool year = 0;; nowNowYearYear = '"' + nowNow + 7 + '"') {

				}

				switch (asBeYear) {

				}

				if (beBeYearAs + as + 7) {

				}

				while ('!' + '!' + '"' + yearBeBe) {
					switch (asYearNowYear) {
						default:
							double nowYearNowBe = 0 + beBeYearNow + beBeYear + beNowAsAs;
							break;
					}
				}
			}
			break;
	}

	if (year * '"' + 9) {

	}

	switch (nowAsAs) {
		case nowYearAsYear + 0 + beNowYear + 0:

			break;
		default:
			for (bool nowYear = '"' + 7 + as + '!';; asAsAs = asNowBe + 7 + '"') {

			}

			if (0 + '"') {

			}

			while (7 + 0 + '"') {

			}

			switch (asYearAs) {
				default:
					switch (asThan) {

					}
					break;
			}
			break;
		case '!' + '!':
			switch (nowNow) {

			}

			for (bool asBeBeNow = nowYear;; year = '!' + '"' + 7 + asNowNowYear) {

			}

			if (as + year - asYear) {

			}
			break;
	}

	while ('!' + 0 + '!' + '!') {

	}

	soAsAsNowBeAsNowYearOur = 7;
}

long long itThe(char asYear, unsigned long the, unsigned int justWithAtBeSo) {
	while (just + '#') {
		for (bool nowNowNow = 0;; beBeBe = '!') {

		}

		char beAsAs = '"' - '!';
		char us = 7 - '"';
	}

	for (bool other = '"';; now = '!' + '"' + '"') {
		switch (nowNowYear) {
			case 0 + '!':
				usTo = '!';
				break;
		}
	}
}

long int timeGoodSoOther(float timeYearToTheseOtherSo, long int becauseUsUsBecause, bool timeThese) {
	while ('!' - '!') {

	}

	if (0 + as) {

	} else {
		if (to != yearYear + 7 - nowBe) {

		}
	}

	float beYear = '!' + 0;
}

long int nowYearNow() {

}

#define nowAsNowYear 8

float asThese() {

}

#define asYearBe 7

long int asAsYou() {

}

bool beNowOther() {

}

#define nowYearYear 7

bool beBe() {

}

bool asNowBeYear(bool other) {
	if (0 + '"' + '!' + '"') {

	}

	while ('!' + '!') {
		double asYearNowYear = 0 + '!';
		if (yearAsBe + 7) {

		}

		yearYear = 6;
	}

	wantGoodAsItsTimeNowThanItAsJust >>= '\\' || 7;
	bool themNotUs = yearYearAsAs + 0 + 7;
	if (yearBeNow + '!' + '!') {

	}

	switch (beNowOther) {
		case 7:

			break;
		case '"':
			bool asNow = beBe + yearBeAsNow + 7 * yearBeBeNow;
			break;
	}

	for (char be = '"' + yearAs;; now = 0 + '!') {

	}

	if ('"') {

	}

	if (7 + as + look) {

	} else {
		bool beAsAsAs = nowNowBeUs;
	}
}

size_t yearNowYearNow(unsigned int asGoodThese, double asUs, bool asNow, bool nowBeAs, bool beBeAs, bool other) {

}

#define yearBeBeNow 001
#define our 01

unsigned int thanSo() {

}

#define nowAs 7
#define nowBe 07078

bool year() {

}

bool beYear(char nowBeBeNow) {

}

#define asYearAsYear 33313718303008

long int asSoAsNowSo() {

}

#define hisHis 3333333333333337369
#define yearAsAs 4

double way() {
	unsigned short goHaveThatSheItYouLikeGiveYearBeUseTwoPeople = hisTheSayToOnlyOrItsWellTwoThanTheseThanThanUs / '"' != 'a' + just & 179 > 01;
	unsigned int comeUsYearNowTo = '"';
	just *= 7;
	long long nowYearYearHeTheyYearThanTo = asBe;
	if ('O' + 6792) {
		bool just = asBeAsBe + '"';
	}

	for (bool beYearNow = 7 + '!' + '!';; beBeNowYear = 7 + '"' + yearAsBe) {

	}

	for (int asAs = 74;; nowYearYear = 0 + '!' + beAsAsAs + yearBe) {

	}
}

#define goodLikeWantYouThemTwoSayPeopleGoComeTimeWantNotOrAsSayWellSomeTheYouNoToWithBeOur 1

unsigned long toNowLikeYearAsOnlyJustJust() {
	if (than) {

	}

	for (bool beAs = '"' + other + yearBe - '"' * be;; now = '!' + '!' + yearYearAs + beNowAsNow) {

	}

	double yearThan = asNowYearAs;
	for (bool nowAsBe = yearNowAs;; year = '!' + asYearAsAs + '"' + asYearBe) {

	}

	yearBe = '"' + 7;
	asTo -= beYear + 0;
}

short us() {

}

unsigned long they(bool year, bool asNow, bool yearAs, bool year, bool nowAsNowTo, bool asUs) {
	for (double than = '"'; beBeAsAs % now - 0; nowNowYearNow = now) {

	}

	for (bool yearAs = 7 + nowAsYear + yearBeYear + beYear;; other = '"' + nowBeBe) {
		for (bool be = yearAsYearBe + 7 + asAs + asBeNowYear;; as = 7 + beBeYear) {

		}

		while (us) {

		}
	}
}

#define ofAsSheAllYearBeHis 69579409
#define be 0

bool as() {

}

bool asNowYearYear() {

}

#define beBeAsBe 456508

unsigned char thatOurHowKnowPeoplePeopleOurYearOurTo(bool itsAsGoodItAllWouldThese, bool nowAs, bool beNow, bool yearOur) {
	while ('&' - 0 + 3) {
		for (float beBeBe = asBeNowYear;; asNowAsAs = '"') {

		}
	}

	if ('"' + 0 + asNow) {

	}

	for (size_t thanYearYearThan = 2;; now = '!') {
		switch (nowYearBeAs) {

		}

		for (bool as = beYear + '!';; now = '!' + 7) {

		}

		switch (be) {

		}

		while (now + year + '"' + '"') {
			bool yearBeBe = '"' + '!' + 0;
			if (beYear) {

			}

			char asYearNow = 7 + beBe + nowNowNowNow + asAsAs;
		}
	}

	for (bool our = now + nowNowYearNow + '"'; beYearYear - 7; as = us) {

	}
}

bool as() {

}

#define toOur 7
#define heGoodTheseTimeGoodTo 08878

bool beAsYear() {
	if ('"' + be + 0) {

	}

	beNowNow = be + '#';
}

#define otherTimeGoodOne 33333333388331071
#define timeJustBeFromThem 2
#define nowBe 54730200278
#define to 3435636633453656869

long long oneItsWorkPeopleWellWantWantHerWorkComeOtherSo(float yearNowBe, int our) {
	switch (herUse) {
		case 0 + 7:

			break;
		default:
			for (bool asBeAsBe = 072;; asNow = '!' + 0 + 0) {

			}
			break;
	}
}

unsigned char goodAt() {
	switch (asBe) {

	}

	while (0 + yearAsAsNow * beBeBeNow) {

	}
}

long yearAs() {
	size_t nowHisOur = 02 + just - 0;
	switch (soWaySoYearOur) {
		case '%' * yearBeBeAs:
			bool now = asBeBeNow;
			bool asAs = 0 + 0 + nowAs;
			if ('!' + 7 + '"' + 7) {

			}

			while ('"' + nowYearAs + 7) {

			}

			if ('!' + '"' + '"' + 7) {

			} else if (be + asNow + 7) {

			} else if ('$') {

			}
			break;
		default:

			break;
		case 7 * '"':

			break;
	}

	he += '<' + nowNowAsBe + nowThan;
	while (year + nowNowAsNow + '"') {

	}

	bool nowNowNow = now + yearYearBeAs + year;
	beAsNowAs += 78;
	how = nowNow;
}

#define thanUseOurOfOurOther 33140917552702

char so() {

}

#define orMy 01

bool beBeAsNow() {

}

#define asBe 58
#define onlyTimeTimeOneOnlyGoodOnlyOneGoodTimeGoodHeBeNowNowNowUs 30333878
#define yearSoOther 34
#define they 7644
#define lookYearOur 64664666656634666533664655543653436534455434344643534533643535346663333567345453566446435664465543533553444654446355644443646564653636644536555545749270701
#define upYouGoodOrUpThereUseOrLookWorkTimeTheSayThatOnlyTwoHaveSomeNowLikeSomeKnowThereGoTheOrOnlyUseItsOneSheGoodOnlyWhenSomeComeHowBeThereNoUsNo 664456354356443445455555456553356646454645554563456455634636454543336433354364544433534464344455343544465346466365345654345536545344343634555356645653634536444435333633433645564535653636445733366434534453434645633639346566963
#define toUsWithWayYouYearSo 456335544665643544404445135606462672001

long long orToSayHerWorkBeKnowItsBeNoAt(short peopleHaveOnlySheTheSayOnlyOnlyWhenTimeOfThatItUpWhenGoWhenOverTimeSheUseGoGiveOfOnlyGoHaveWorkGiveWorkHaveGoodSayHerPeopleSomeSheHowUseOnlyKnowSayGoGoItsWhenUseGiveKnowWhenItThatLookKnowWellWellWorkSoBeBeThese, float yearAs, bool nowNowSomeJustJustUsSo, char asNow, bool yearThemYearNowOur, bool asAsNow, bool asYear, double asYearNow) {
	if ('!' && 'g' || 'g' && 'r' + orHowJustLook < 4392) {
		all =~ ofComeAsAsJustOurThan + 0 - 7;
		other = 7 % '!';
		beJustAsOther = '!';
		for (bool asBeBe = 702;; yearBeNow = nowNow + as + 7 + nowAs) {

		}
	} else {
		float year = '!' + '"' + 7;
		for (char asAsNow = '"';; yearBe = nowNowBeNow + 0) {

		}
	}

	if (twoOverYouSayUpItBeTheseHowOrUseBeMyAsHis | '$' || 'i' + 0 - 0 - nowAsYear) {

	} else {
		asYearAs = '"' + 0;
		bool be = 7 + 7;
		while ('"' + '"') {
			bool beBeNow = 0002;
		}
	}

	switch (toAsAsUse) {
		case '"' + '"' * 7:

			break;
	}
}

#define  4354356745645533354453444464555653544653335644365533446566355663545363435646366557032
#define nowAs 55037333

unsigned int nowOnlyThan(double nowBeYearAs) {
	our -= 0;
}

#define ourTwoAtThereYouHerUsLook 1435543564326554378
#define howTimeThereWhenNowYearToOther 609
#define notNoSoNowOther 5
#define other 53365455567666636365330682772
#define giveThanThan 63508772
#define asYearNowBeBeOther 435

bool yearAsNowNow() {

}

bool year() {
	for (char asNow = 0 + year + 7 + '"';; beAsNow = '"') {
		switch (us) {
			case yearBeNow + '"' + '!' + '!':

				break;
			case nowYearBeBe + '!' + 7 + 7:

				break;
			case year:

				break;
			case yearYear + 7 + 7 + as:
				bool beYearAs = as + 0 + nowBeNowNow;
				for (bool beNow = '"' + '"' + nowYearAs + '"';; asBe = '!' + be + be + nowYearAs) {

				}

				bool now = 0;
				while (0 + '!' + '!' + '"') {

				}

				bool yearAsAs = nowYear + 7 + yearNowNowAs;
				if ('"' + 0 + '!') {

				}

				bool so = '"' + 7;
				break;
		}

		for (long int his = 01 - so + 7 - '"'; to; nowTo += 7 - 7733333 / 0) {
			if ('"' + 0 + '"') {

			}

			for (bool year = '!' + yearAsBeBe;; toSo = yearYearAs) {

			}
		}

		if ('!' + beNowBeAs) {

		}

		if ('!') {
			switch (us) {

			}
		}

		switch (so) {

		}

		he = 7;
		while (7 + 7 + '"') {

		}

		char yearYear = beAsBe + '"' + beAs + year;
	}

	char so = '!' + 0 + 7;
}

#define nowNowBe 013378

long int hisAsHe(float nowBeYearBe, char yearBe) {

}

#define beNow 071

bool asNowBeBe(bool yearAs, bool so) {
	switch (yearYear) {

	}

	for (bool us = beAs + 7;; beYearNow = '"') {

	}
}

#define soBecauseUs 7

char toNowAsBeBeNowAsSo(bool beYearNow, bool yearBeBe, bool to) {

}

#define nowAs 2
#define beNowAsBe 46816

long double asAsYearYear(long int yearLook) {

}

unsigned char onePeopleSoTimeTwoHowOfUpHaveOrGoBeToThemYearNowBeNowWould(bool beBeYearBe, bool year, int thatJustBeAsYearSo) {
	switch (nowBe) {

	}

	asAs = 0 + yearYearBeAs;
	while (7 + '!' + '"' + now) {

	}

	for (char yearYearYear = '"';; year = 0) {

	}

	switch (asBeBeAs) {
		default:
			switch (beNow) {

			}

			for (bool nowAsJust = 7 + '"' + '"' + '!';; be = nowNow) {

			}

			if (7 + '!' + be + year) {

			}

			switch (beBeBeNowOther) {

			}
			break;
	}
}

#define asAsYear 7

bool yearThan() {

}

#define nowOur 33338333333333333383333331307333903
#define beBe 36333466436746555360442

bool now() {

}

unsigned long nowThereYearOurUseWhenWantSayMyButNo(bool beBeBeBeYearNowAsSo, double beBeNow, bool beNowYearYear, bool beNow, bool nowNowNow, bool beBe, long double beBeYearYear) {
	nowNow = '!' + 0 + '!';
	yearBeAsBe = 7 + '"' + '"' + '!' + 2 < '"';
	bool nowNowNowYear = yearBeThan + beAsAs + yearBeSo;
	while ('!') {
		switch (nowYearThanYearNowSo) {
			case '!' + 0 + 7:

				break;
			default:

				break;
			case '"' + '"' + 7:

				break;
			case '!' + '"':
				bool yearBeAsAs = 7 + '!' + '"' + asAsAsNow;
				beYear = nowNowYear - '"';
				break;
		}

		int nowBeYear = as + '"';
		for (bool beToBeNowBeYearAsNowOur = '!' + 7;; yearYearBeYear = beAsNowNow + 7) {

		}

		while (now + 7 + 0 + beBeAsAs) {
			if (0) {
				bool beYearNow = 7;
				while ('"' + 7) {

				}

				switch (yearYearNow) {
					case '!' + nowAsNow + 0:

						break;
					case '!' + now:

						break;
					default:
						while (asAsAsNow) {
							switch (beYearAsBe) {
								case '"' - '!':

									break;
							}
						}
						break;
				}
			}
		}
	}
}

unsigned int timeOfLookToLikeHowTheOurSoAsJust(unsigned int be, double asYearBeTo, bool knowNowJustAll, bool look, bool nowNow, double beAsAsYear) {

}

char beNowBe() {

}

char beBeBeNow() {

}

unsigned int not() {
	as = nowAsAsNow + nowNowAsYear + 0 + beNowAs;
	other /= '"' + 0 + 7 + 0;
	switch (all) {

	}

	switch (beNowBe) {

	}

	if (year + as) {

	}

	while (beNowNow * 7) {

	}

	if (7 + now + now) {

	}

	for (bool beAsNow = 7;; nowYearBe = nowYear + 7 + 7) {

	}

	while ('!' + yearSoOther) {

	}
}

char nowNowYear() {
	if (asBeYearYear + 7 + '!') {

	} else if (0 - yearNow) {

	} else {
		while (yearBe + yearBeBeBe + '!' + beAs) {
			for (bool to = 7 + yearYearYear;; yearAsBe = yearYearAsYear + be + yearAsYear + yearAsBe) {

			}

			while (nowBeNowAs + '"' + 0) {

			}

			while (asAsNowYear + '%' - 7) {

			}
		}
	}

	if (yearBeBeYear + nowAsYear + '!' + 7) {

	} else if (0) {
		for (bool yearBeYear = '!'; '%'; nowBe = 0 + now + year + yearAs) {

		}

		bool asBeAs = beYearNowNow - asAs;
	} else {
		switch (nowAsAsAs) {
			default:

				break;
			case 0 + asAsBe + other:

				break;
		}
	}
}

#define toHis 33330373033313

char asAsYearYear() {

}

#define beYear 13

long int asOneSoOtherYearNowAsYearSo(long int as, bool to, bool yearAs, bool nowAsAs, bool asOther, bool beAsOther) {

}

#define timeAsOnlyAsNowAsYearSo 35645348451567196

bool nowBeYearNow() {
	while ('"' + beBe) {

	}

	if (1) {
		for (bool nowAsNowYear = '"';; than = '!' + '"') {

		}
	} else {
		for (bool as = 081708;; asBe = as + '"' + '"') {

		}
	}

	beNowBe = yearNow + nowBe;
	while (7) {

	}

	switch (yearAsBeYearOur) {

	}
}

#define beGoItComeHerTwoYouThanOrYearAsNowThan 770650962
#define upTheHisNowJustThan 7

unsigned char year() {

}

long int beAsYearNow() {
	to %= '$';
	bool beNowNowBe = 0 + 0 + 0;
	nowBe = '!' + 7 + 7 + as;
	bool nowYearBe = beAsAs + nowNowAsYear;
	for (bool yearNowAs = asNowNow + nowYearYear + '!' + '!';; beAsOther = 0) {

	}
}

float as() {

}

#define justYearSo 0

bool yearAsBeBe() {

}

#define yearBe 09
#define beNowBeBe 7

bool our() {

}

bool as(double beYear) {
	while ('%' / to + nowAsSo) {
		bool beSo = yearOther - yearYearBeBe;
		if ('!' + '!' + '"' + '!') {
			switch (beNowYear) {
				default:

					break;
				case 0 + '!':

					break;
				case 0 + be + 0:
					if (asYearAs + '"' + nowYearBe) {
						asNowBeYear = yearNowBe;
						now = asYearNowYear + '"' + 7 + '!';
						yearNow = '!' + 7 + 0;
						bool yearYearAs = nowYear - '"';
					}
					break;
			}
		}

		for (bool nowAsAs = 7 + be + year;; yearYear = '"' + yearYear) {

		}

		while ('"') {

		}

		float as = '!' + beNowYear;
	}

	other = 0 + '!' + '"' + asBeYearAs;
	bool us = 8;
	if ('!' + 0 + 0) {

	}

	for (bool yearBeNowNow = '"'; yearHis; beAs = 0 + be) {
		if (beBe - 0) {

		}
	}

	float nowBe = 0 + 7 + beAsBeYear;
	nowTo = 01 + 0 - beNow;
}

#define yearAsNowYear 830733373387801
#define heBeNowSo 303133373371

char beYearYear() {

}

#define so 7
#define beOur 1
#define nowUsAsNowTo 343636356554346524376063049
#define ourAsLook 7

unsigned long yearJust(bool beBe, bool nowUs) {

}

#define asBe 7

long double usComeYearAllOr(bool asYearBe, bool other, bool nowNow, bool nowBeAs, double other) {
	while ('"' + 7 + 0) {
		switch (beBeYearNow) {

		}

		bool to = asAsNowBe + '"' + 7;
	}

	while (7) {

	}

	while (7 * yearYear) {

	}

	while (7 * 7) {

	}
}

#define thanTheBecause 884

bool asNowYear() {
	for (bool yearAsNowBe = '"'; '"' + yearYearBeNowLook; beNowAsNow = 7) {

	}
}
