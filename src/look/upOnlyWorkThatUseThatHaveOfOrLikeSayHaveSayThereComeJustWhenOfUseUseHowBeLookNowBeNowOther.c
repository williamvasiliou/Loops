#define beSo 831
#define timeGood 33

long int timeTheseThese(bool beNowNowBe, bool nowAsYearBe, bool yearYearAs, char yearNow) {
	bool yearAs = 7 + '!';
	asNowSo = '"';
	char yearYearYearNow = '!';
}

long int timeOneBecauseOnlyGoodBecauseAsSoOnlyOther(char asNow, int as, bool nowYearNow, bool nowBe, bool nowAsAsYear, bool yearBeAsAs, bool just) {
	for (bool asAsAs = asYear; ')'; beNow = yearBe + '"') {

	}

	asYearYear = yearYear + '"' + '"';
	for (bool just = 7 + as + asAsBe;; nowYearAs = '"' + '"' + 0 + '"') {

	}
}

bool other(bool yearAsOur) {
	if ('!' + 0 + '"' + beBeBeBe) {
		beNowBeBe = asAs + nowNowBe + 7 + 7;
		if ('"' + nowYear + beAsAs + yearYearYearAs) {

		} else {
			int year = beNowYearAs + 0;
		}
	}
}

#define asAs 

bool nowAs() {

}

#define  54434345665666418
#define so 41

bool nowAsAs() {

}

#define peopleOrBeAsUs 54346346521334545036

size_t upGoodMyOneTimeOtherSoYearThan(char asAsNowYear) {

}

#define likeMy 7
#define asOther 7
#define yearYearYearBe 7

bool nowYear(bool asYearYearAs, double yearAs) {
	if (318 / 7) {

	} else if (so - yearNowBe) {
		switch (us) {

		}
	} else {
		char us = 7 + '!' + beAsYearBe + 0;
		while ('"' / '!') {
			if (be + 0 - 7) {

			}

			while ('!' + '"' + 7 + 71) {

			}
		}

		yearAs = yearBeYear + yearAsBe;
		bool yearYearAsAs = 7 + 3 - nowBeNowOther;
		switch (be) {
			default:

				break;
			case '"' + '!' + 0 + '!':
				while ('!' + '!') {

				}

				if ('"' + be + 0) {

				}

				while ('"' + beBe + 0) {
					bool nowBecause = asBeAsYear;
				}
				break;
		}

		if ('"' + '!' + 0) {
			now = 8;
		}
	}

	if ('#') {

	} else if (7071) {
		beBe = 7 + 8;
	} else {
		switch (asBecause) {

		}

		while (7 + '"' + asYear + nowYearBeNow) {

		}

		if ('"') {

		}

		bool now = 7;
		for (char asYearBeAs = beYearAs + 7; '%'; asNow = 0 + 0 + yearAs) {

		}
	}

	year += '"';
	for (bool us = '"' + '!';; year = yearNowNow + 7 + '!' + asBeNowNow) {
		asNowNowAs += 0;
	}

	if (7 - 7) {

	} else {
		while (0) {

		}

		switch (beBe) {
			case 0 + 7 + yearBe:
				asBeBe += '"';
				break;
		}
	}
}

#define timeGoodOnlyOneOnlyAsTo 33547831

bool year() {
	for (bool asBeAs = 0 + yearBeAsAs;; asBe = 7) {

	}

	if (nowYearNowLook) {

	}
}

bool asNowBeAs() {

}

#define as 0

bool year() {
	while ('!' + 0 + beBe + '"') {
		nowTo = 'x' || howTwoOneOverOnlyPeopleItsWellOverMyUpAsTimeOrSheSoNowOverTherePeopleTwoComeOurHaveGiveGoWantAtAsThese * onlyBeNowUs || youTheseAsYearOtherOur > '(' + 1;
		short theNoTwoYearOurThat = '-' * ',';
		for (unsigned int thereSheWantLikeSoThereWellLikeOnlyUseThanOurYouWellTheWay = sheComeYouSomeThanSo; 7 * as; beItSo = yearBeAs * '"') {
			bool beNow = beYearThan;
			yearBeAsAs = 9;
		}

		if (2 * beBeNowNow) {
			yearBe = 01;
			switch (otherAsThanUs) {

			}
		}

		for (unsigned int look = '"' + beAsBe + asNowBe + 0;; beYear = '$') {

		}

		for (bool asBeNow = 7 + '!'; 01; asBeYear = as) {

		}
	}

	switch (workHerPeopleWithAsUsGiveBeWith) {
		case year:

			break;
		case beNowNowAs + beYearNow + 7 + '"':
			beNow += yearBeYear;
			switch (look) {

			}
			break;
		case 7 + 7:

			break;
		default:

			break;
		case 2:

			break;
	}

	beNow =~ '"' || 9;
	for (bool as = now; look; be = 0) {

	}

	if (7 + 7 + 0 + '"') {

	}

	double heJust = '"' + 7 - 0;
	for (char year = beBeBe + 72;; yearBeNowYear = 0 + 7 + '!') {

	}
}

size_t upSomeHerItsHaveOverKnowLikeKnowTheTwoWhenHerGoHaveWorkItUpKnowJustOverPeopleSayOfUseWorkHowOfSayItsSomeHerWorkGiveOrTheTimeWorkUseOverWhenWellNowKnowWantYouTheItsUseYearOtherLook(short yearYearYearYearAsOther, bool just) {
	bool yearLookOur = beNowAs + 79;
	switch (nowAsNow) {

	}

	for (bool asYear = '"' + '!';; asAsNowYear += nowNow + 7 + 0) {

	}

	bool but = '"' + 0 + be + 7;
}

unsigned long goodItsItsSomeSayTwoKnowThereItTwoUseComeYouWhenItThereSayHowGiveItSomeWantOneWhenOfLikeWhenHerTimeUseGoOrUseHaveHerYearHerLookKnowGoodToItsWorkLikeButOther(size_t theyUsYearAsAsLook, bool yearAsNowYearOther, long double our) {
	asBeYearNow = '"' + 7 + yearBeBe + as;
	switch (be) {

	}

	while (as + 7 + 0) {

	}

	char asBeBeAs = beNowNowBe + 0;
}

bool beYear(bool now, bool twoWorkThanYouOurTo, double yearAsYear, bool nowNowYearYear, bool beBeYear, char nowBe) {
	for (bool yearAs = '!' + beAs + '"' + be;; beBe = 7 + yearAsAsNow + '"' + 7) {

	}

	if (0 + '!' + '"') {

	}

	for (bool nowBe = '"';; nowYearYearBe = '"' + '"' + now) {
		if ('"' + '!' + 7 + '"') {

		} else if (0 + '!') {

		} else if (asYearBe + yearBeNow + '"') {

		} else if (0 * be) {

		}
	}

	yearAsBeThan = '!' + '!';
}

unsigned long noLookAtYearYearToBeAsNowYearThan(bool asAsNow, char beYearYearYear) {
	switch (to) {

	}

	bool nowAs = yearAsNowYear + 0 + beBe;
	for (bool nowAsBeBe = beNowBeBe + '"' + year; '$'; nowAsBeNow = asYear + 7 + asBeBe + now) {

	}

	switch (year) {

	}

	for (bool now = yearAsBeNow + 0;; nowBeNowAs = '%') {

	}
}

short asNowYear(bool our) {
	yearNow -= beAsYearNow + 7 + '!';
	for (bool us = 0 + 0 + nowNowAsYear + 7;; beYearAs = '!' + 7) {

	}
}

#define beNow 0

bool asBeNow(bool asYearBeBe, double year) {

}

#define or 0
#define yearNowNow 355324533424656516189

short twoGoodGoAsToTheyYearGoNowThan(double nowBeYearAs, bool be, bool nowBeAsYear, char nowNow) {
	while (nowAs + nowSo) {

	}

	switch (he) {

	}
}

bool yearBeUsSoBeUs() {

}

#define asYearNow 7
#define now 0

bool year() {

}

bool as() {

}

char asNow() {
	double nowBe = 33333133808 != '_' / '!';
}

#define onlyGoodBeGoodOtherOnlyYearHisUsAsOther 37337334

bool soBeBeSoYearSoAsUs() {

}

#define so 082

double beNowNow() {
	float but = beThanToOther;
	switch (thereOrLikePeopleWellThanThan) {
		default:
			beBeYearBe = '"' + 7 + beAsAsBe;
			asBeNowJust = 7 + 7;
			break;
	}

	if (75 + '#' + asNowYear + '%') {
		nowNowTo = nowYearNow + 8709;
		bool nowAs = 7 + '"' + nowBe + 0;
		so = '!';
		for (bool beNowTo = 0 + beNow;; asNow = 7 + '!' + '!' + year) {

		}
	} else if (7) {
		if (8) {

		}
	} else if (7) {
		for (bool now = '"' + 0;; asAsYearAs = '!' + '!' + 7) {

		}

		double year = nowNowAsBe + '!' + 7;
	}

	for (unsigned long goWithGoodAsThere = 736462 >= thereThanHerAsJustOther > 567008 + '*' + '"' + '#';; nowNowNowBe = 7 + asAs + asYearBe) {

	}

	unsigned short yearSo = '"' + '!';
	for (long long nowYearNow = '"' + nowBeNowBe; be - nowBeBe; now |= some == beYearAsAs / ')' && 0) {

	}

	nowAsYearYear = 7 + '!' + 0 + 7;
	if (yearYearAs + yearBeYear + 7 + 0) {

	}

	if (nowBeAsNow + beBe + 0 + asAsBe) {

	}

	bool yearNow = '"' + '"' + 0 + '!';
	for (bool nowNowBeNow = '!' + '!' + '"' + beAs;; nowBeNow = asBeAs + 7) {

	}

	for (bool beNowNow = 0 + '!' - year;; yearBe = '"' + '!' + '"') {

	}
}

unsigned int upSayTwoWorkWellWellHerItsTimeBeWellOfItsLikeOneOrSheSheThanOnlyGiveYouThereBeGiveSomeLookBecauseThanKnow(char use, long double nowAsAsBe, bool our, double year) {
	knowSayTheseThereHisNowBeYearYearBeYearThan -= '!' + nowNowNow;
	char now = 0 + '"' + '!' + year;
	while ('"' - '"') {

	}
}

short be() {
	while (7 + '!' + 0) {
		switch (nowAsNow) {
			default:

				break;
			case asNow + '!' + '%':

				break;
		}

		if ('$') {

		}
	}

	bool asYearYearYear = '"';
	if (0) {

	}

	bool yearYearYearAs = 7 + 0 + '!';
	for (bool be = '"' + 0 + 7 + 7;; asNow = '!') {

	}

	double asBe = '!' + yearBeAs;
}

#define look 666567378

double beYearAs() {

}

unsigned short theseHowHowThatNowPeopleTwoOneBeYearThan() {
	while ('!' - asYear) {

	}
}

#define yearLook 35566466465335413454344665557447002

bool as() {

}

#define yearYear 6
#define be 7
#define now 63333333733333333383333
#define beHisYearTo 33333370708707079

double yearYearYear() {
	if ('!' + 0 + 0) {

	}

	if (yearYear) {
		bool now = '!' + as + asAsYearBe;
		switch (nowYearTo) {

		}
	}
}

#define asNowBeYear 7
#define nowAsNowBe 7

double beNowSo() {

}

double yearAsAsYear(bool nowOur) {
	long long allTheyUsAsLookJust = becauseWouldBeUs;
	someAsGiveKnowPeopleSomeAsBeBeSoJust <<= beHowThatThemHeNowBeThatYearThanAt <= nowJust <= 08 + yearUs + '"' + asNow * beAs;
	otherWith |= '!' && yearBe;
	switch (timeWouldBeUs) {
		default:
			switch (nowAsLookOur) {
				default:
					for (bool so = '"' + 0 + '!' + '!'; 8; nowAsYearBe = 7 + '!') {

					}

					if ('!' * 0070701) {
						bool be = now + '!' + '!';
						for (bool yearBeNowYear = asBeAs;; beYearNow = 7 + 7 + 7 + '"') {

						}

						yearAsYearAs = '!' + '!' + '!';
						while (yearNowBe + nowYearNow + nowAsYearNow) {

						}

						switch (year) {
							default:

								break;
							case year + 0 + '"':

								break;
							case 0:

								break;
							case 7 + 0 + '!':
								asNowAsBe = yearBe + asNowAs;
								if (7 + year + now + 0) {
									for (bool beYear = 7 + nowNowYearYear + 0;; as = year + '"' + '"') {

									}

									nowYearNowYear = '"' + '"';
									if (7 + 78) {

									}
								}
								break;
						}
					} else {
						yearBeYear = yearBe + beAsBeNow + 0;
						while (nowBeBeBe + nowAsAsNow + '"' + beBe) {
							switch (beLook) {

							}
						}
					}
					break;
				case 7:

					break;
				case 0:

					break;
				case '!' % 0:
					bool beBeBeBe = beThan;
					break;
			}

			if (nowNow < '"') {

			}

			as += yearYearNowNow + 7;
			break;
		case yearAs + 7 + asAsAsJust:
			if (as + year + '!' + 0) {
				for (double so = 7 + '!';; asBe = '"' + asAsAs) {

				}
			} else {
				double yearNowNow = '"' + '"' + 7;
				for (double yearBe = '"' + yearNowNowNow + 7 + asAsBe;; beAs = 0 + 7 + now) {

				}
			}

			double as = nowYearYear + beNow + '"';
			break;
		case 0 - '$' + yearYearYearYear * 0:

			break;
		case nowYearAsYear + yearYearYearUs:

			break;
		case 7 + nowAsBe + '"' + 7:
			bool asAs = '&';
			break;
		case 0 + 7 + '"' + '!':

			break;
		case 7 + now:

			break;
		case '!':
			bool asNowBeYear = beYear + 0;
			while ('"' + 7 + 7 + '!') {
				if ('!' + '!' + '"' + 0) {

				}

				asNowBeAs += nowBeBeAs + '"';
			}
			break;
		case 7 - '!':

			break;
	}

	now = beNowBeOther;
	while ('"' + 0 + beBeNow + '"') {
		for (char as = 7;; be = nowNow + '"' + '!') {

		}
	}

	double asBe = '!' + '"' + asYearYearAs;
	bool asAsNowYear = '"' + 0;
	for (bool beAs = '"' + 7;; nowYearYearAs = '!' + asAs + '!' + '"') {

	}

	while ('"' + 7 + 0) {
		if ('"' + beNowNow + '!' + nowNowBeNow) {
			asBe = '!' + 0 + asNowBeNow + nowBe;
			now = 7 + '!';
			switch (beBe) {

			}

			if ('"' + nowBeBeAs + nowNowAsAs) {

			}

			nowBeAs = asBe;
			if (0 + '!') {

			}

			switch (nowAsYear) {
				case 09:

					break;
			}
		}
	}
}

#define than 0

char as() {
	if (9419) {

	}

	while (yearAsAsBe + 0) {
		for (bool be = 72 - year % now; lookIts; nowAs = 7 + '"' + 7 + 7) {
			if (7 + 0 + '"' + now) {

			}

			bool asBeNow = yearYearNowBe + yearYear + '!' + '"';
			for (bool asYearYear = 0 + '"' + nowBeAsAs + 0; 8; nowYear = '!' + '!' + asAsYear + '!') {

			}
		}

		so = 1;
		nowAsBe += '!' + 0;
		yearSo = 7 + nowNowNowYear + nowAs;
		for (bool be = 0 + asNowAs;; beAsAsAs = 7 + '!') {

		}

		while (as + '"' + 0 + 7) {

		}

		switch (beNowNowBe) {

		}

		bool beAs = 7 + '$';
	}

	us =~ '"' + '"' + '!';
	thanNowSo = yearBeNow * 0;
	yearAs = '!' + beYearBe + 7 + '"';
	nowBeBeBe = '"' + '"' + '"' + '!';
	if (nowYearAs % '"') {

	} else {
		for (bool asYear = '!' + 7 + '"';; now = '!' + 7 + 7 + 0) {
			for (bool to = 7 + 0 + '"' + 7;; nowYear = 0 + year + nowBeBe) {

			}
		}
	}

	yearYearSo = '!';
}

#define yearBe 572
#define nowYear 338
#define asOtherNowLook 0
#define usWorkIt 050370600071
#define look 53

float year(bool yearAsAsAs, double beAsAsNow, bool now, bool nowUs) {

}

char asBeAsAs() {
	for (bool beAsNow = beYear;; nowNowAsAs = 0 + 7 + 0) {
		long int as = 0 + 0 + asYearBeNow + asAs;
		for (bool yearYearYear = '"' + yearAsNowNow + '!' + nowNow; '#'; nowBe = beNowYear) {

		}
	}

	long int so = '_' != timeSoYearBecause - 7;
	if (onlyHisAsAsHe != 0 + '"' - '!') {
		for (bool beAs = asAsSo;; asBeNowNow = yearBeYearNow + 0 + '"') {

		}

		switch (usBeTo) {

		}
	} else {
		asSo = yearAsBe;
		while (beYearBe + 0 + 7 + beBe) {

		}

		bool year = 0;
		switch (as) {

		}

		while (8) {

		}
	}

	for (bool now = as + '#';; asAsAsAs = 0 + yearBe) {
		for (bool nowNowAsBe = '!' + '!' + nowNowYear + 0;; beYearAs = beBeYear + as + year + as) {

		}

		for (bool now = nowAsBeYear + 7;; beBeBeAs = '"') {

		}

		if ('"' + 0 - 7) {

		}
	}
}

#define onlyUsBeOtherHe 3331034077079
#define than 4464546456545444644546354504749
#define beAsAs 8

double be(bool look) {
	for (bool asNowNow = 0 + '!';; beYearNowAs = 7 + as + 7) {

	}

	if (7 + nowBe + 7 + yearNowBeNow) {

	}

	while (7 + nowNow * 0) {
		overToOur =~ '%' % '"';
		while (beBeAsYear * 7) {

		}

		asBe -= 7 + beNowNow + beBeYear;
		while (99) {

		}

		for (bool asAsBeBe = nowYearAs + '!' + '"';; now = '!') {

		}

		year = 0 + 1;
		while (yearNowAs + '"') {

		}

		if ('!' * 0) {

		}
	}

	if (asOther) {

	} else if (7) {

	} else if ('"' + '"') {
		yearNow = '!' + 0 + yearBeYear + '"';
		yearNow = '!';
		if (7 + '"' + year + be) {

		}

		while ('"' + 0 + 7 + yearAs) {

		}

		us = '"' + 7;
	}
}

unsigned short thereWithYearYearJust(unsigned char likeFromWouldAll, int asBeBeNow, double as) {
	while (7 * '(') {

	}

	yearNowNow = '!' * asAsNow;
	switch (yearBeNow) {

	}

	if (us + 7 + '#') {

	}

	nowYear = 7 + nowNowAsAs + 7 + nowNowBe;
	if (beNowBe + '!' + 0 + now) {

	}

	asYearNow = nowAs + asBe + year + year;
	switch (beNowNow) {
		default:

			break;
		case 7 + 0:

			break;
		case 0 + '"' + as:

			break;
		case asYearBeYear + '"' * '"':

			break;
	}
}

#define ofGiveOnlyTheyLookYearAsOurOur 333333333717008
#define onlyUsTo 33317303
#define timeOneHisGoodTo 033333472

float beNow() {

}

#define beBeYear 036671
#define yearAsNow 663343564338593456433896757650402
#define look 335688
#define itAsTheAt 64056633333333333333333333333333307370382

char just() {

}

#define our 3553496775078
#define beAsNowBe 309

bool year() {
	if (nowNow + '!' + '!') {
		nowNowYearYear = 0 + 0 + beYearBeBe + 7;
		if (0 + 0) {

		} else {
			for (bool beYearYearNow = year + '"' + 7 + beYear;; now = '!' + be) {

			}

			switch (yearNow) {

			}

			switch (be) {

			}

			switch (year) {

			}

			for (bool be = nowYear * asYearYear;; asBeBeBe = '"' + '"') {

			}
		}
	}
}

char asJust(bool beYearYear, bool nowYear, bool than) {
	for (unsigned char nowBeAs = '-' && '"' <= '"'; 'U' + '!' + '"' + '%'; nowJustThem = 079 - 0) {
		nowYearNowBecause = 0 + as + asAs;
	}

	bool beBeBeAs = 7 + '!' + '"' + 0;
	switch (nowWith) {
		case '#':

			break;
	}
}

long int haveSheUseHer(long long yearUsAsBeOther, bool thanOther) {
	while (year + 32) {
		bool year = '!' + 0 + '!';
		bool beBe = '!' * nowNow + 08;
	}

	yearBe = 7 + '"' + beNowYearNow + 0;
	long beYearAsAs = 78;
	yearAsBeYear = 7 + 7;
	bool yearBeAs = '!';
	while (0 + yearBe + nowNowBe) {

	}

	switch (yearYearBe) {
		default:

			break;
		case '"' + '"':

			break;
		case 0 + year + 0:

			break;
		case asBeNowYear:

			break;
		case now:

			break;
		case '%':

			break;
	}
}

#define nowBeJust 0
#define asNow 7706
#define otherThan 0
#define as 45333333333333181

long int onlyYearBeNowYearAsAsYearUs() {

}

bool nowOther(bool nowNowAs, bool other) {
	for (bool nowAs = '"' + '"';; be = '"' + 7 + 7) {

	}

	switch (beSo) {

	}
}

long int goodTheseBeSoBeBeBeYearOther(float oneToOnly, bool now, char nowYearUs) {
	switch (year) {

	}

	beAsFrom = 0 + asYearBe + '!';
	switch (asYearNow) {
		case asBeAs:

			break;
		default:
			if ('!' - 7) {

			}
			break;
	}

	if ('!' + 7 + '"' + 0) {

	}

	switch (asNow) {

	}

	while (0) {

	}

	if (nowAs + year + '!') {

	}

	bool us = yearAs + 7;
}

bool yearNowYear() {

}

#define year 7
#define asYearBeAsThan 465736143
#define now 0

char nowNowNow() {
	bool nowYear = now + 0 + asNow;
	while (now + 7 + 7 + year) {

	}

	while (nowYearBe + '!' + be) {

	}

	switch (yearYearBeBeThan) {
		case 7644708 && '>' + 338 * asAsAsBeThan:
			if (9) {
				if ('!' + 7 + '"' + beNowYearBe) {

				} else {
					yearYear = 7;
					if (yearYearBeAs) {

					} else {
						while (now) {
							asBeNow = asAs + 0;
							for (bool as = 7 + 7;; year = 7 + asYearYearBe + '"') {

							}

							switch (as) {
								case 7 + '!':

									break;
								default:

									break;
								case 0 + 7 + 7 + 7:

									break;
								case asBeNow + yearAsYear + 08:

									break;
							}
						}
					}
				}
			}
			break;
		case '&' % us * '#':
			year *= '!';
			switch (now) {

			}

			yearAsBe = '"';
			yearYear = nowThan;
			break;
		case asBeBeBeUs:
			for (bool be = 0;; beAsNow = 0 + '!') {

			}

			bool nowNow = yearBeYearAs + nowAs;
			switch (to) {
				case 7 + 7:

					break;
				default:

					break;
				case beBeAsYear:

					break;
				case '!':

					break;
				case nowBeYearAs + asYearBe:

					break;
				case '"' + yearNowYearAs:

					break;
				case '%':

					break;
			}
			break;
		case '"' + '"' + '!' + be:
			nowNowAs = beAsNow + nowAsNow;
			for (bool yearBeAsYear = beAs + '!' + 0 + now;; asYearBe = '!' + '!' + 7 + '!') {

			}

			switch (yearBeYearYear) {

			}

			while (asBe + '!' + '"' + 7) {

			}

			while ('!' * '!') {

			}
			break;
		case '"':
			for (long int beBeAsYear = '!' + '!' + '!'; 9; year = 0 + '"' + '"') {

			}

			double nowAsYear = beAsNowNow + '"' + 0 + yearAs;
			break;
		case asNowAs * '"':
			switch (asBeAs) {

			}

			switch (beAsNowYear) {

			}

			if (7) {

			}

			be = nowAsAs + yearNowAs + asNowYearBe + 7;
			while ('"' + asNowNow + nowAs + 0) {

			}

			us = 7 + yearNowNow + as + yearAsBe;
			break;
		case 0 * yearBe:

			break;
	}

	if (beOther) {

	}

	likeToHeCome = '"' + nowAsBe + asNow;
	if ('"' + '!' + 0 + 7) {

	}

	while (7 + beNowNow) {
		beNowAsAs = '"' + '"';
		for (bool now = 0 + asYear + 0; them; just = 0 + '"' + 7) {
			switch (yearNowBe) {
				default:

					break;
				case yearAsNow - 0:

					break;
			}
		}
	}

	use = '$';
	if (asNowAs) {

	}

	yearYear /= 7 + nowAsBeAs;
}

#define nowNowBeAs 0

char yearAsYear() {
	long int beOther = '#' - 7;
	while (1377008 != 71 - '"') {

	}

	while (nowBe + '!' + beAsNow) {
		nowOtherTo = as + asNowNow + '!' + '"';
	}

	other = 0;
	for (bool nowNowYearBe = now + asYear + as;; beAsNowBe = yearAsNowYear + '"' + asYearBe) {

	}

	float nowBeBe = 0 + 7;
	switch (nowAs) {
		case 08:
			for (bool nowYearYearYear = 7;; beNowAsAs = 7) {

			}

			switch (to) {

			}
			break;
	}

	be *= '!';
}

#define timeGoodGoodGoodOneOnlyGoodHisHeTimeNowUs 3377078
#define give 0079
#define asNowNowYearThem 10604
#define be 7

long double just(char than, bool nowTo, char yearBe) {
	bool asBeNowLook = 55463549324 != 'd' > '!' + '!' - '!';
	toOtherNowBeLook /= 'i' * '#' * '%';
	switch (yearAsAs) {
		case '"' + 0:

			break;
		default:

			break;
		case be + '!' + yearNowAs:

			break;
		case '!' + 0:
			for (bool yearYearBeBe = 0 + '"'; '!' + 709; asBeBe = '!' + nowAsBeBe) {

			}
			break;
	}

	if ('&') {
		if ('"' + '"' + '"') {

		}

		for (bool yearBe = 7 + '"'; '$'; asBeNow = asAs + nowAs) {

		}
	}

	char nowBe = '#';
	if (year + '"' + 7 + 7 + '!' + yearBeNowYear - year) {
		while (yearBeYearBe + 0 + '!' + '!') {
			double beBe = 0;
		}
	}
}

#define beNowBe 63445
#define beAsNowNow 0

char year() {
	while (33387373001 != 0) {
		if ('#' - be) {

		} else {
			beAs += '!' + 7 + '!';
		}

		while ('"' - '"') {
			for (char nowAsYearBe = 0 + yearNowNow;; now = 7 + '!' + 7 + '!') {

			}
		}

		if (7 + be + nowYear) {

		} else if ('7') {

		} else {
			bool nowBeNowNow = 0071;
		}

		yearYear = '!' + nowSo;
	}

	beUs = '%';
}

float timeHisOneOnlySoNowThese(char be) {
	switch (beNowBeAs) {

	}

	switch (soOtherOneToAsOtherBeAsSo) {
		default:

			break;
		case '"' + 7:
			be = beAsBeBe + be - beNowAsYear;
			bool nowBeBe = year + as + yearNowNow;
			switch (nowYear) {
				default:
					char yearNowYearYear = '"' + 0;
					break;
			}

			bool other = 7;
			break;
	}

	while (0777001 + '+') {

	}

	for (short nowNowYearOther = 002;; asNowAs = nowBe) {
		bool yearBeAsYear = yearBeNow + 58;
		unsigned short yearItsThan = '!' + '!' + '!';
	}

	long year = '"' + '"' + nowAsYear;
	double now = '!' + '"';
	if (7 == yearBeBe) {

	} else if (0 + beBe <= beYearNow) {

	}
}

bool be(char nowYear) {

}

#define asBeOur 5354326683533082

long long thatTheyYearSoYearAsNowBeAsUs(long double beNow, long nowHe) {
	switch (yearNowLookHaveWould) {

	}

	switch (beThan) {

	}

	double beBeAs = 7 + yearYear + '!' + '!';
	char nowNowBe = '!' + yearBeNow;
}

#define itsKnowIts 1
#define otherBeNowAsYearJust 33333333333333333303078

bool asOther() {
	for (bool as = beYear + '"'; 8; as = beNowNow) {
		while (78) {

		}
	}
}

#define hisThese 33333383

bool thanBeJust(bool asAsYearYear, bool beAs, bool beBe, bool nowYearOther, bool asAs, bool beNow, double asYearAsNow) {

}

#define the 706001
#define noYearBeNowSo 0
#define yearYearOther 65664444643455763662790078
#define thanOther 6507008

long int ofOneTheYearBeThan(bool beBeBeAsSoSo, bool nowNowYearBe, double nowAsBe) {
	for (bool nowAsAs = 0 + yearNowNow + '!';; asNowBe = 7 + '"' + beBe) {

	}

	double yearBeNow = '"' + yearYearAs;
}

#define so 4

bool asBe() {
	asYearYearNow = 7;
	if (beAs + '"' + 7) {
		switch (now) {
			default:

				break;
			case 7 + '!' + yearBeAs:

				break;
			case beBe - '!':
				asAsNowBe = 7 + '!' + now + now;
				bool yearYearNowAs = '!' + 7 + nowNow;
				if ('!' + 78) {

				}

				while (yearBeAs + 0 + 7) {
					as += asAs;
				}
				break;
			case '"':

				break;
			case nowYearNowAs:

				break;
			case other:

				break;
			case '"' + '"' + '!':

				break;
			case beAsSo:

				break;
		}

		long int only = 'O' + '#';
		if (yearAs + as + 0 + '!') {

		}

		if (1 - now - 0) {

		}
	} else {
		for (float yearOther = '%'; 8; to = now + year + 7 + 7) {
			for (bool beAsYear = 0 + '!';; beAsBeNow = '"' + 0) {

			}

			switch (nowBeYearBe) {

			}

			asAs = asAsNowAs + 7 + 0;
			if (beAsAs + yearYearYear) {

			}

			switch (asNowAs) {

			}

			for (bool as = '!' + yearAsBe + yearNowNow;; asYearAs = as + 7) {

			}

			for (bool yearNowBe = '"' + asBeAsYear + asNowAsYear; '%'; nowNow = '"' + '"') {

			}
		}

		if (7 + now + '!' + now) {

		}

		char asNow = usHisAsSo;
		long int theseOther = asNow + '"' + '"' + asBeBe;
		while ('!' + as + '"') {

		}

		switch (beYear) {
			case 7 + '"':

				break;
			default:
				char asNow = 0;
				break;
		}

		for (bool asNowNowNow = 7 + be + '!';; be = asYearYear + as + as + 7) {

		}

		char asAs = '"' + '#';
		if (0 - 7) {

		}
	}

	char one = beYearBeAs + yearBeAs + beBeAs + now;
}

#define because 301

char yearNowTheseOtherYearThanBeUsOur(double beYearYearBe, char beBeYear) {
	switch (yearToNowYearAsBeUs) {
		case '$':
			if (7 + '!' + 7) {
				for (bool asNow = beAsYearYear + '"';; nowNow = '!') {
					than = 7 + nowAsBe;
				}
			}

			for (double yearYearYear = nowYear + nowNowBe + 0;; asBeYear = asBe + yearAs + '!') {

			}
			break;
	}

	while ('"' + '!' + 0 + 7) {
		bool beJust = sheFromComeItsPeopleUpWhenComeJustThatAsHaveLookToSomeOther - 7;
		while (7 && 46635465381 + '!' + nowBeAsBeNowBeYearUs) {
			while (2 == 0 != 7 < asNowJust) {
				nowThan = '"' + now;
				if (yearBeYearBeTo) {
					bool nowAsNowYear = '!' + beYear;
					while (7 + beYearYear * 0) {

					}
				}

				for (char year = '!' + beBeAsBe + nowAsBe;; yearNow = yearAs + asBeNowAs) {

				}
			}

			if (7 + asYearNow * yearAs) {
				while ('"' + '"') {

				}

				year = 7 + '!';
				if (7 * 7) {

				}

				while (nowYear + 7) {

				}

				while ('"' + 7) {

				}

				for (bool asAsYearNow = year + '!' + '!' + beNow;; yearYearAsAs = '"') {

				}

				than = '!' + nowNow + beNowBeNow;
			} else {
				for (bool asYearNowAs = '%';; as = '!' + 7 + '!' + '!') {

				}
			}

			nowJust &= '+';
			if ('"' + beBe + 7 + '"') {
				while (7) {

				}

				for (bool year = '!' + nowBeAsAs + 7 + 7;; yearNowBe = nowAsBeBe - '"') {

				}
			}
		}

		while (700179 == '~' || 'G' || youOfThereUpHowYouHowHowComeAll && '!' != 539475565434368871 < 79071 + '4') {
			while ('u') {
				for (char nowOur = nowBeOurTo + 0 + '3';; year -= '"' + '!' + 7) {
					while ('%' * '%') {

					}
				}
			}

			while ('%' * beYearNow) {
				unsigned short justLook = noTwo;
				while (2) {

				}

				switch (as) {

				}

				beYearBeYear = 7 + asYearNowYear;
				while ('+' * yearAs) {

				}
			}

			while (7 + '"' + 1) {
				for (bool now = yearNowBe + '"';; asYear = '"' + yearAs + '"' + '"') {

				}

				switch (nowSo) {

				}

				yearYear = '"';
				double be = '!' + '!' + yearAsNow;
			}

			usNoLike = 7 + 7 + 7 + yearBe;
			if (nowOur + '%' * heLook) {
				switch (year) {
					case '"' + '"':
						yearBeNowNow = yearSo;
						break;
				}
			} else {
				if ('!' + '"' + '"') {

				}

				bool yearYearAs = 0 + '!' + '"' + '!';
				bool beBe = '#';
				if (2 * 0) {

				}
			}

			for (bool beBe = 7 + '!' + nowBeBe;; asYearNowNow = '!' + be + yearBeYear) {

			}

			if (asBeBe + '!') {

			}

			for (char asBeBeYear = '!';; nowYear = 0 + 0 + '!' + yearBe) {

			}

			switch (yearBeBe) {
				default:
					for (bool asAsNow = 0 + beYearNow + '!' + '"';; beNow += 0 + 7 + yearAs) {

					}
					break;
			}
		}

		unsigned int upHeKnowYearThereBeOtherTo = 'h' < 6767708 + so;
		switch (heTwoNowOther) {
			case 01402 + 0 - 7:
				char beBeYearNow = '"';
				switch (now) {

				}

				switch (yearAs) {

				}

				switch (nowYearBeAs) {

				}

				bool asYear = beBeYear + asNowBeNow + 0 + yearAsAsYear;
				for (bool asBe = 7 + 7 + be + nowBeYear;; yearAsYear = nowBe + '!') {
					nowBe = 0 + 7 + beYearYear + beAsNow;
					yearSo = beNow + beNowYearBe + yearYearBe;
				}
				break;
			default:
				unsigned short his = '4';
				beAsNowYear = '!';
				for (bool with = '"' + '!'; asAsLookAsLook * asAsBeAs; yearNowBeBe = asNowYear + '"') {

				}

				if (nowYear % '&') {

				} else {
					for (bool nowNow = '"' + asNowNow + 7; yearAs < '!' - as; yearBe = '"') {

					}
				}

				for (char nowNowNow = '!' + asYearBe + 0 + '!'; yearAsAsThey + yearYearNow + 7 - 0; beAs = yearBe + now) {
					while (7) {
						if (709) {

						}
					}
				}

				while ('$') {

				}

				char asNowBeNow = 0 + 0 + 0;
				switch (as) {

				}

				if (nowYearBe) {

				}

				switch (nowAsNow) {
					case 08:

						break;
				}
				break;
			case be * than:
				switch (asBe) {
					case 7 + '"' + 7:
						bool nowNowYearNow = 7;
						bool yearNowBe = beBeAsBe;
						asNowYearBe = nowLook;
						break;
				}
				break;
			case 0 - '"':

				break;
		}

		for (unsigned char yearOurOther = be - asYear;; so = '!' + asBeBeBe + '"') {
			bool beNow = '!' + 77772;
			switch (beNowYearAs) {

			}

			while (0 + 0 + '"' + '!') {

			}

			switch (as) {

			}

			if (nowBeNow + beYear + year + 7) {

			} else if (beBeNowBe) {

			} else if (be + '!' + '!') {
				switch (yearYearBeNow) {

				}

				while (0) {

				}

				while (7) {
					switch (year) {
						default:
							for (bool yearAsBeNow = '!' + 0 + '!';; year = 7 + nowYearAs + beAs) {

							}

							for (bool beNow = '"' + '!' + 7 + '"';; nowBe = yearBe + nowAsAs + 7 + now) {

							}

							beNow = 0 + '!';
							double nowYearNowYear = asNow;
							break;
					}
				}
			}
		}

		while ('"' + '"' + 7) {
			long yearAs = asAs + '!' + '!';
		}

		for (bool beYear = 7;; yearNow = yearYear + '!' + '!') {

		}

		while (0 + '!' + 0) {

		}

		if (yearBeAs) {

		} else {
			bool asBeBe = beYear + 0 + 0 + '!';
			asAsBeYear = '!' + nowNow + nowBe + asAsNowAs;
			double year = '!' + year;
		}

		be = 7 + beBeNow + 0 + yearBeAs;
		while ('!' + yearAsNow) {
			if (0 + 0 + '!' + '!') {

			}

			if (yearBeOther) {

			}
		}
	}

	while (giveGoItsThereTheLookOfItWhenUpOfNoNowUpItSoHe * 708) {
		switch (now) {
			case beYearBeThan:
				switch (asBe) {
					case 7 + 7 + '!':

						break;
					case beBeBe + be + '!':

						break;
					default:
						if ('"' + yearBe + 7) {

						} else {
							while (7772) {

							}
						}
						break;
				}

				be = be + asNowAs;
				for (bool as = nowBe + 7 + beNowYearNow + '"'; 001; as = 0) {

				}
				break;
			default:
				switch (yearYear) {
					default:
						bool beNow = 7 + '!' + '"' + asBeNowAs;
						nowBe = '"';
						for (char now = '!' + 7 + 7;; as = yearYearYearNow + 0 + now) {

						}
						break;
					case 7:

						break;
					case yearNowAs + year + '!':
						while ('"' + beBe + yearBeAs) {

						}

						nowAsYearNow -= '!' + nowYearNow;
						for (bool yearBeAs = beAsYearAs + asBe + '"';; nowNowBeNow = '!' + nowBeBeBe + 7) {

						}

						while ('"' + be + '"' + beNow) {

						}

						if (7 + '!' + 7 + year) {

						}

						for (bool nowBeYear = nowBe;; as = '"' + '!') {

						}

						switch (as) {

						}

						switch (now) {
							default:

								break;
							case '!' + yearAsYear:

								break;
							case 0 + 0:
								be = beNowNowAs + 7 + nowAsNowAs + 0;
								char yearAs = '"' + 0 + '"' + asYearAs;
								break;
						}
						break;
				}

				for (bool asBeBe = asNow;; beYear = 0 + 0 + 7 + 7) {

				}

				bool asYearNow = asBe + 0 + 7;
				if (asBeAsTo) {

				}
				break;
			case 18:
				bool as = our;
				bool nowYearBe = 7 + 0;
				if (0 + now + beAs + asYearAsYear + yearBeAsJust) {

				}
				break;
			case 355985573:
				nowJust = '!' + '!' + be + 7;
				break;
			case '"' + '!' + 0:
				bool yearYearNow = '!';
				us = '!' + '!';
				asBeBeYearOur = '"' + '"';
				break;
		}

		unsigned long yearOur = 1 * 0;
		for (bool nowNowBe = 0 + 7; ofItThereItNowUsFrom; asYearButJustJust = 391 / '"') {
			now = yearYearSo;
		}

		while ('"') {

		}

		while (7) {

		}

		bool look = beNowYearNow + '!' + '!';
	}

	fromTimeAsHerUpUsOther <<= 'b' > '}' || 54644635555460438256064345637310373 || 454 >= 'w' & 'A' == comeSo & 555455463775473730898 & otherHer;
	while (youLikeItOverOrWhenGoodThereHowWorkHerUseThereTwoTheYouHowSomeTheGiveTheWantHowThereLikeWantGoOrThereUpSheGiveThatUpWorkGoGoHerThereOnlyItThatItUseTwoHerUseYouOfTimeHowThereOrHaveLikeItOverAllHerThanBeThereJustTimeTimeNowYearNowYearNot != 'g' == '}' != 3336534348445571068 >= allWantGoPeopleUseThanComeNowHis <= '#') {
		if (7 == 02) {
			if ('`' >= 'Q' - ourAsOther && '!') {
				beNow *= 7 + '!';
			}

			while (yearOtherAsAsNowOur * look * '!') {
				so -= '!' + 0 + 0;
				while (usOur + 0 - '"' - nowBe) {

				}

				switch (yearBe) {

				}

				switch (our) {

				}
			}

			bool nowYear = '"' + '!' + 7 + 7;
			long long than = 7 + 907272 + 7 + 79;
			switch (lookTheyHe) {
				default:
					if (0 + beNow + nowNowAs) {

					}

					if (beAs + yearBeBeAs + as + '"') {

					} else {
						for (bool asAs = beNowBeYear + nowYearBeNow + nowBeAs;; beBe = '!' + 7) {

						}

						while ('"' * nowBe) {

						}
					}
					break;
			}

			if ('"' + '!' + 7) {

			} else if (year - 7) {

			}

			double nowYearNow = '!' + '"';
		} else if (7 + '!' + '"' + 7) {
			asBeBe = '"' + 0 + 0;
			if (7 != '!' + '&') {
				while (01) {

				}

				bool asAs = 0 + nowYearBe + '"';
				for (bool nowNowNow = 7 + '$';; year = nowNowNow + 7 + 7) {

				}

				if (0) {
					for (bool beBeBe = beNow + '"' + yearBeYearAs + '"';; asBeAsBe = 7) {
						switch (asAsNow) {

						}

						switch (year) {

						}

						while ('"' + beNowYear + be) {

						}

						while (beBe + '!' * '"') {

						}
					}
				}
			} else if (0) {
				just = 7 + be + '"' + '"';
			} else if (72) {

			}

			while ('#') {

			}
		} else if ('!' + 7 + 0 + 0) {

		} else {
			while (yearAsBe) {
				switch (beAs) {

				}

				if (beBeBe * '!') {

				}

				yearNowAsAs = 72;
			}

			if (7 + 7 + so) {

			} else if (7 + 0 + '!') {

			} else if (7 + 7 - nowYear) {

			}
		}

		so = 2;
		for (size_t knowOverHaveSayOtherHowWantAsOther = yearAs + 7 - nowNowAsAs > beNowAsAs; lookNowTheseThan + '?' + '%' + 7770007071; workNot = '!' + '!' + 7) {
			asBeBe = '!' + '&';
			for (bool beYearNowAs = asBeBeYear;; nowYearYear = 0 + 7 + '!') {
				while ('&') {

				}
			}

			for (bool nowNow = '!' + '!'; all * 7; asNow = asNowBe + '"' + nowYearBe) {
				beYear = asYearAsNow + yearNow + yearNowAsAs;
				for (bool nowYearYearYear = 0 + 7 + '!' + '!';; nowNow = '!' + '!') {

				}

				while (0701) {

				}
			}
		}

		while ('!' + 0 + just) {

		}

		while ('#') {

		}
	}

	while ('n' > 'j' > 'g' || than | youYouSheWantTwoItThanGoThatHaveHaveWantGoodThatThemAsComeGoWork | 9 >= goUpToHerUs * '!' + '%') {
		for (unsigned char yearAs = nowLook + '^'; 702; yearBeAs = look) {
			while (asBeNow + 7 + 0) {

			}

			if ('!' + as + as + 0) {

			} else if ('!' + '"' + '!') {

			} else {
				bool as = yearAsJust;
			}
		}

		switch (workTwoOtherAtLook) {
			case asYear + 0 + asBeBe + as:

				break;
			case '"' + now + nowYearYearAs:

				break;
			default:

				break;
			case '!' + yearNowAs + asBe + '"':
				if (now + asAs) {

				}

				if ('"' + yearBeYearAs + 0) {

				}

				if ('!' + '"' + now + be) {

				}

				for (bool nowYearBe = yearYearNowYear - '"';; nowNow = now + '!' + '!') {

				}
				break;
		}

		for (bool asYearAs = as + 7 + now; likeHisThem - be; nowNowYear = now) {

		}

		for (bool be = 0 + as + 0 + 0; '!' - '"'; asYear = '!' + '!') {

		}
	}

	for (double beBeNowNow = 7 * 7;; yearAsAs = beBe + 7 + 7) {
		while (7 + 0 + '"' + beBeAs) {
			bool nowYearAsAs = 01;
		}
	}

	unsigned long thatOverAtGo = 'g' == upNowSome + '!' % '"' + '$';
	thatItsBeSayComeTheSomeWellUpItsNowHowWantPeopleOnlyOur -= itWay / 7 - '"';
	while (7 + '"') {
		thanShe .= he + '!' + '"' + '!' * '!';
	}

	switch (fromTheMyOrYearNowSo) {
		case '"' + 7:
			while ('!') {

			}

			beBeBeBe = 0 + '!';
			while (7 + 7 + beNow + as) {
				if (yearAs + now + year + '"') {

				}

				bool now = '!' + now + 7;
				char asNow = '"' + '"' + year + '"';
			}
			break;
		case 7 + '"':

			break;
		default:

			break;
		case now + 0779:

			break;
	}

	orPeopleWorkOtherPeople %= 'Z' + '!' - 7;
	if (0) {

	}

	if ('!') {

	}

	while (nowBeNowNow * beAsNow) {

	}
}

#define giveWantSayOverTwoTwoWellNotBecauseKnowGoodYouSomeSomeToThanKnow 3179

size_t itGiveWellPeopleWorkYouThatGoThatHowWhenSheYouTheLikeOneSheAsBecauseAsOur(bool nowSo) {

}

#define asTo 305535565535456364605443535300635564554366463304414808

long double beBeAsYearLook(bool yearNowUs) {
	if (theyNowYearPeopleLookAt * '!') {

	} else {
		upUsOur %= 0;
	}

	size_t yearYearNot = '"';
}

long long yearOtherYearHowThan(bool beLook, char year) {

}

char his(bool be, double asYearThey) {
	than &= beThey;
	char asYear = now;
	bool than = nowBeBeBe + '"';
}

unsigned short soYearToAsOurUs(char oneTo, double asAsBeNow) {
	switch (from) {
		case nowYearAs + 7 + '"':

			break;
		case 71:

			break;
		case 0 + nowAs:
			for (bool nowYearYearBe = '"';; asYearNowAs = year + asNowAs) {

			}

			as = '!' + asAsAsNow + yearAs;
			switch (be) {

			}

			for (bool as = '"' + '"' + 7;; asBeNow = 0) {

			}

			switch (nowAs) {

			}

			bool beAsYearAs = yearUs;
			break;
	}

	if ('#') {
		bool as = 7 + 0 + 7;
		bool be = beYear + beNowYearNow + '"' + 0;
		while (0 + yearAsYear) {
			for (bool beYearYearAs = yearNowAsBe + 0;; yearNowYear = '!' + 0 + '!') {

			}

			switch (as) {

			}

			for (bool asBe = 7 + now + 7;; beNow = 0) {

			}

			while (beNow + nowYearAsNow + now + 7) {
				if ('"' + '"') {

				}

				if (7 + nowAsNowNow - '"') {

				}
			}
		}

		yearNowYear = to / '!';
	} else {
		nowOnlyOneYearYearBeOther = '"' + 7;
		float now = '/' + beNowBeTo + '"' - other;
		now = 7 - '!';
		if ('+' + 0 + '"' + '#') {

		} else if (7 + yearAsYearAs + 7) {
			if (yearYear + '"' + nowNowBeNow + nowBeNowYear) {

			}

			switch (nowUs) {

			}
		} else {
			while ('!') {

			}

			if ('%' - '!') {
				yearYearBe = yearYearBeAs + be;
				for (bool asYearAsNow = 0 + be + nowAs + '"';; beYear = asBeBe) {

				}

				while (asYear + 0 + 7) {

				}

				if (beAs) {

				}

				bool nowYear = '"';
				so = '"';
			}
		}

		while (0 + beAsNow) {

		}

		bool yearYearYear = nowNow + asBeBe + '"';
		for (bool beYearYearBe = beBe + '!';; asYear = beYearAsNow + 7) {

		}

		if (0 + '"' + '!') {

		}

		asTo = 0;
		char beNowYearAs = heOther - beOther + 8701;
		while ('!' + '%') {

		}

		to = 7 + nowBeAsYear + '"' + '"';
		bool nowYearBeNow = 0 + 0 + 0;
		bool beOther = 7 + '"' + yearAs;
	}

	for (long int asAsSo = '+' + 7 - beYearAs;; nowAsNowBe = nowNowAsYear + 0 + '"') {

	}

	if ('!') {
		so = 7 + nowYearNowYear + '"';
		while (7 + now + '"' + yearAs) {

		}

		otherBeNowYearSo = '!';
	} else {
		switch (yearAsYear) {

		}

		float nowBe = 7 + nowNowNowYear + '!' + asAsBeYear;
	}

	nowOther = nowAs + 0;
}

#define year 301
#define so 8609
#define us 0
#define nowLookSo 0
#define our 2
#define beBeSo 4434665553563633566446466545443345333334334333056355353653436663356666465354406533468063568208

bool haveSayYearGiveGoGoPeopleOrSheItSome(unsigned short youThereWhenUsUpWouldTheseNowGoodToBeThanUs, char nowBe, char otherThey, double ourOther, double asYearAsNow, bool be, bool from) {
	while ('{' || '!' != 769058 && 4 * '"') {
		while ('-' % '"' - 0) {
			while (0 * 7) {
				beYearBeAs = '!' - 7;
				switch (beBe) {
					case 0:

						break;
					default:

						break;
					case nowAsBeNow + 7 + as:
						if (7 + 0 + 0 + nowYearBeYear) {

						} else if (our) {

						}
						break;
				}
			}

			switch (otherJustBeWouldYearToNowOur) {

			}

			for (unsigned char yearNowYearOther = '"' - '%';; now = yearBeYear) {
				if (0 + 7 + be) {

				}

				if (yearNowBe + '!' + 7 + '"') {

				}

				yearNow = 7 + '!' + nowAsAs;
				for (bool beNowNow = nowNowNowYear;; yearYearBe = '!' + beNowYear) {

				}

				beAsTo = '!' + '"';
			}

			nowNowYear = asYear + '"';
			bool yearAsToNowJustSo = 0 + yearNowYearNow + 7;
			for (bool yearNowYearAs = 0 + '"' + '"' + yearNowAsBe;; asNowBe = 0 + 0 + 7 + asYearBeAs) {

			}

			switch (yearBe) {

			}

			switch (asLook) {

			}
		}

		while (be && 4 - beNow) {
			for (float asNowNowBe = '!' + '!' + 0;; year = beBe + be + 0) {

			}

			asYearWould = '"' + 0;
		}

		for (bool yearYearAsBe = 7 + '!' + yearNowNowNow;; nowBeBe = '"' + asBeYearAs + nowAsAs) {

		}

		for (char beYear = beNow; year + 0 - '"'; now += '!' + beYearBe + '"' + yearYearYearAs) {

		}
	}

	beThanBeYearBeSo = '"' + yearAsYear + '!';
	bool yearOur = 8 * '!';
	if (7) {

	}

	asOur = '!' + 7 + '"' + 7;
}

#define upUpGoWellYearToSoFrom 434453356643565533655343354646353535563314334364530639676
#define now 0
#define sheAsThemThanAsUs 377619

double someJustOurOurWith() {
	bool beNowBeBe = 7 + '"' + 0;
	bool than = 0 + '!';
	while ('"' + '!') {
		nowNowNowNow = 7 + '!' + 0 + '"';
		if (0 + 0 + '"') {

		}

		yearAs = year + as + '!';
		switch (beBeYear) {
			case 7 + asNowNowNow + '"' + nowNowAsNow:

				break;
			case 0 + asBeNow + 0:

				break;
			case 7:

				break;
			case 9:

				break;
		}
	}
}

bool nowNowNow() {

}

#define useNo 0

long long yearThey() {
	yearNowAsNow = 0 + 0 + 7 + beAs;
	while (2) {

	}

	while ('"' + 7 + '"' + 7) {

	}

	while ('"' + '"' + '"') {

	}

	switch (nowAsYearYear) {
		default:
			switch (beOur) {

			}

			asBeAsSo += as * '"';
			if (asYearNow + 6 + '$' + '$') {

			}

			while (wellNowUs + '#' - 7 + 12) {
				short he = '$';
			}

			if (nowNowYearYear + as - 0) {

			}

			for (bool now = '!';; asNowNow = 7 + 7 + asAsAs) {

			}

			beNowSo = be + '"' + beBe;
			if ('"') {

			}

			yearOther = 7 + 7 + '"' + nowYearBeNow;
			break;
		case '"' + '"' + 7:
			as = be + 7 + yearNow + nowYearYearAs;
			while (be) {
				bool asLook = 0 + yearBeYear;
			}

			if (yearAs + 0 + yearNowAs + 7) {
				for (bool yearYear = 7 + '"';; yearYear = 7 + 0) {
					bool yearNowAsBeNowOther = '"' + now + 0 + nowAsYear;
				}
			} else if (as + nowBeYear) {
				bool nowNowBeNow = '!' + '!' + 0 + 7;
				asAsAsNow = 7 + 7 + '!';
				bool so = '!' + 0 + beYearNowYear + 0;
			}
			break;
		case as + 7 + nowAsAsAs:
			asBeBeBe = as + now;
			while ('"' + '%' + our) {
				switch (as) {
					case 0 * 0:

						break;
				}
			}
			break;
		case '"':
			double nowYear = '"' + '"' + beYear + '"';
			break;
	}

	int toJust = 0 + beAsYearAs + 7 + '!';
	switch (soJustSo) {
		case 0 - they:
			switch (beYearYear) {
				case yearNowAsBe + 0 + '"':

					break;
				default:
					bool year = 7 + beAsYear;
					if (0 + beNowYearNow + '!') {
						while (now + 0 + '!') {

						}

						if (0 + 0) {

						}

						nowNow = asBeAsYear + '"' + asBeYearAs;
						yearNow = 0 + 0 + 0 + '"';
						if (asNowNowBe + 0 + 7 + yearBe) {

						}

						if (9) {

						}
					}
					break;
			}
			break;
		case 0:

			break;
		case beAsYearYear + '"' + 0:

			break;
		case nowYearAs + '"' + 0:
			year = 0;
			if ('!' + 0 + 079) {

			}
			break;
	}
}

#define be 0779
#define asWay 6
#define yearHaveBeYearThere 8728
#define so 78

bool asSo() {

}

#define asNow 0
#define be 0

bool nowBeBe() {
	switch (beYearYearBe) {
		case 0:
			while (yearAsYearNow + 7) {

			}

			double nowNowNowAs = '!' + '!' + 7 + '"';
			break;
	}
}

#define asAs 01
#define yearOther 7

char yearAsBeAs() {
	while (nowAsAsThese - beYearYear) {
		char to = 81 - 7;
		asAsBeYear = '%';
		asBeYearYearUs = to;
		if (yearBeNowYear + asYear + 0 + '"') {

		}

		while (0 - 7) {

		}

		be = one;
		while ('"' + asNowAs) {

		}

		us = yearYearAs;
	}

	switch (onlyTimeHisOneTimeOneAsSo) {
		default:
			char asYear = '"' + '"';
			for (bool now = '!' + 0 + '"' + 0;; beBeNow = asYearYearBe) {
				char as = '!' + '"' + 0;
			}
			break;
		case nowBe:
			switch (asYearNowAs) {

			}

			if (0 + 71) {

			}
			break;
		case nowYear + '!' + as:

			break;
		case '!' + 0:

			break;
		case asBeNowAsHis:

			break;
	}

	switch (asYear) {

	}

	if ('!' + 0) {
		if (7) {

		} else {
			float nowHe = nowYearBe + 7;
		}
	} else {
		bool asAsAs = year + 0;
		for (bool year = yearAsAs;; these = '"' + beYearNow + yearAs + 7) {

		}
	}

	while (3811 - '"' - beNowTo - 0) {
		bool asBeNowTo = '!' + 7 + asAs;
		if (as + 7 + 7 + '!') {

		} else {
			if (asBeNow - '"') {

			}
		}
	}

	if (beBe) {

	} else if (0 + 7 + nowYear + 7) {

	} else {
		switch (beYear) {

		}

		now = be + '!';
		for (char nowNowYearAs = asBeYearYear;; beAsYear = beYear + 7 + yearAs + 0) {

		}
	}

	while (asYear) {

	}

	switch (now) {

	}

	while ('&') {

	}
}

bool toBeYearThan() {

}

#define lookBeTo 0
#define year 9

double yearNowNow() {
	if (someSayOurWantWayNowBeOther * beAs) {
		nowAsBeYearYearJust = 71 + 8;
		switch (nowYearYearYear) {
			case '"' - 0:
				bool year = yearYearYearAs + 7;
				char nowYearYearNow = asAs + nowYearYearAs + '!';
				for (double beNow = beBe + 0 + asBeAs - 7;; beNowBeNow = '"' + beNowYear + 0) {
					for (bool beNowNow = '!' + look;; as = '!' + 7 + '"') {

					}
				}

				for (bool beYearYear = '!' + asAs + now + '"';; asBe = 0) {

				}

				switch (yearAsNow) {

				}

				while (beNowYearBe + '"') {

				}

				double yearBe = '"' + '"' + 0;
				break;
		}

		float yearYear = as + yearNowNowBe + '!' + 7;
	} else {
		bool nowSo = yearAsNowYear + 0;
		switch (to) {
			default:

				break;
			case 7 + 0 + '"' + asAsAsAs:

				break;
			case 7 * as:

				break;
		}
	}

	yearNowNowLookOther &= yearBeNowYear + year + yearAsAs + nowAsNow;
	while (7 * '!') {
		if (beBe + beNow) {

		}

		while ('!') {
			switch (year) {
				case '#':

					break;
				default:
					switch (nowAsYearNow) {

					}

					bool asNowNowNowAsYearNowAsTo = now + asBeNow + as + asNowYear;
					break;
			}
		}
	}

	while (as + '!' + beAsBeNow) {
		if (asNowYearBe + 0 + 0 + 0) {
			if ('!' + 0) {

			}

			if (asNowBeBe + nowYear) {

			} else {
				for (bool nowAsBeYear = 0;; nowNowAs = 0 + yearAs + '!') {

				}

				while ('"' + asBeBe - '"') {

				}
			}
		}
	}
}

#define yearGiveUpOtherSo 37078

unsigned short yearOur(long year) {
	while (7 * year) {
		if (asYearAsAs + 7) {
			if (0 + '"' + '!' + asYear) {

			}

			switch (nowUs) {

			}
		} else if (asAsBe + '"') {

		} else if ('"') {
			if (0 + '"' + 0) {

			}

			bool asYear = '!' + '"';
			beYearYearNow -= '!' + 0;
			if ('!' + yearAsNowAs + nowBeYearAs) {

			}

			char be = '"' + yearBeNow + 0;
		}
	}

	for (bool beAsOur = year + 7 + 0 + '!'; 9202 <= '"' + 2 < 7; beYearNow = '!' + be) {

	}

	if (0 * 0) {

	}

	for (double beBeNowAs = beYear + '"' + '"';; asNowBe = 7 + '"' + 0 + 0) {

	}

	while (',' + '%') {

	}

	if (0 + yearNow + beBeYearNow) {

	}

	bool asOurOur = 01;
	while ('!') {

	}

	for (bool yearAsYearAsLook = 0 + yearBeAsYear;; asYearBe = '!' + nowBeYear + beAsNowAs) {

	}
}

unsigned long fromFrom() {

}

#define when 703
#define us 3506066534306585444545633643765508
#define no 3507572

double asYearYear() {

}

#define yearJust 3333333333833031173771

long int soOther(long int toSoYearNowSo, bool asYear, bool beNow, char asAsAs) {
	while (08) {
		for (bool asBeBeYear = '"' + 0 + 0 + '!';; year = '"' + 0 + 7) {

		}

		for (bool asBe = '!' + beBeNowNow + asBe + 7;; nowBeAs = 7 + '"' + '!' + yearBe) {

		}

		if (yearYearYear) {

		} else {
			beBe += '!' + beAsYearYear + yearNowYear + '"';
		}
	}
}

#define usAsNowOther 0
#define one 3387384
#define yearNo 368377778

bool asAsBe() {

}

bool nowHe() {

}

#define asBeNowNow 7

bool yearBeBe() {

}

bool now() {

}

#define asLook 64933

unsigned char youOnlyKnowWantHaveOnlyBeAsWhenThanMy(bool sheToBeAt, bool asToNot, bool so) {
	for (bool as = yearAs + yearBeAs + asYearAs;; yearNowBe = 0 + asAsYearYear) {

	}

	bool just = 0 + nowAsYear;
}

#define haveTheseAsWell 58

bool toNowBeNowWith() {

}

long double be() {

}

char nowBeNowNow() {
	while (usYearTo) {
		long int beToThese = 7 + 7 + year;
		for (char asAs = now + '!' + '!'; '!' / 0 - now; nowAsBe = be) {

		}

		bool as = '!' + 0 + '!' + 0;
		if (0) {
			for (bool asSo = '"';; asNowYearNow = now + '"' + asAsBeAs) {

			}
		}

		yearAsAs += nowAsAs + '"' + 0 + 7;
	}

	while (0 + '!') {

	}

	if ('!' + asYearBe) {
		char now = asNowYear + '!' + '!';
	} else if (8 / '"') {
		while ('!' + yearAsYearBe) {
			nowBeAs = 0;
			bool year = 0 + us;
		}
	} else if (nowAsAs) {

	} else {
		switch (yearAsBe) {

		}

		switch (asBe) {

		}

		bool asYearAs = 0;
		for (bool nowYear = 7 + '"';; nowAsYear = '"' + 7) {

		}

		if (asNowBeSo) {

		}
	}

	oneOneToGoodSoYearNowBeTo |= '/' / '!';
	for (bool nowNowAs = 0 + 0 + '!';; now = nowYear) {

	}

	if ('"' != 7) {

	}
}

#define beNowSo 3331
#define beNow 8

float so() {
	for (char nowYearNowBe = 0 + beAsYearAs + asNow + year;; as = yearYear) {
		bool asAsAsNow = 7;
		year = yearNowAsNow + 7;
		nowAsUs = 0;
	}
}

bool nowBe() {

}

#define yearBeNowAs 03500202
#define than 544578

long double beNowNowNow() {

}

short yearTwoYearYearNowNowThan() {

}

#define toNowOurTo 7

bool asJust() {

}

bool beAsAs(bool beOther) {
	for (bool beNow = '%' - '!' + 7 + beBe + beYear + our; lookToSoOur - beBe; asAsYearNow = 7) {
		while ('"' + '"' + 0) {

		}

		while ('"' + 7 + be + beAsNowNow * '!') {

		}

		for (bool nowAsYear = '"' * '"';; beAsBe = '"' + now + 7) {

		}
	}

	switch (asBeBe) {

	}

	if (asAs > 0) {

	}

	while (other) {
		year = 0 + 0 + 0;
		while (0 + our) {

		}
	}

	switch (from) {

	}
}

long int yearGiveSoBecauseSomeBeBeLook(unsigned int workJustOnlyThese, unsigned long our, bool yearBe, bool now, bool nowAsNowYear, bool asNowNowThan) {
	double asAsOur = 7;
}

float themYearYearSo(bool now, bool yearNow, bool be, short all, bool asAsBe, bool asNowBe, bool year, bool our) {

}

bool now() {

}

#define just 807779

bool asThan() {
	switch (yearOneYearUsTo) {

	}

	long int beBecauseUs = 7 + '!';
	while ('"' != 7) {

	}

	long int onlyTimeOnlyOneBeNowAsUs = so + '"' / '!' + nowAsBeNow + nowYearBeYear + '#';
	long int asOnlyOther = 1 - '"';
	as = 0 + '#';
	bool beAsBe = '#';
	if (other) {

	}

	bool nowNowUs = 7;
}

#define beSoTo 88
#define now 3

char asAsBeYearSoUs(bool theseYearYearOther, bool as, double now, double now) {
	now = '!' + 0 + 0 + 7;
	if (other) {

	}

	for (bool yearYear = asBeNowYear + 0 + nowAsNow + 0;; beBe = asAsYear) {

	}

	bool yearYear = '"';
	switch (asYearNow) {

	}

	while (7 + 0) {

	}

	if ('!') {

	}

	bool than = asAs + beAsYearYear + 7;
}

char be() {
	if (asAsYearAs + nowYear + '!' + 0) {

	}

	for (double beYear = '!'; be + 2 >= beYearBeYear; nowYearBeYear = '&') {

	}

	if (0) {
		switch (asNowNowAs) {
			default:
				while (0 + asBeAs + '!') {
					if (0 + nowYearAsAs + 0 + 7) {

					} else {
						while (asBeYear + nowNowBe + yearBeAsYear) {

						}

						if ('"' + '!') {

						} else if (beNowAsNow + '"') {

						} else if (7 + '"') {

						} else if (asAs + yearYearYear) {

						} else {
							bool asBeAs = now + 0 + nowBeNow + 7;
							for (bool nowNowYear = 0 * 7;; beNow = yearYearAsYear + beAsYearBe + nowAsAs) {

							}
						}
					}
				}
				break;
		}
	}

	bool now = 7 + '!';
	wayJustAsThanThan = 0 + asBeNow;
	bool yearBeSoAsOther = now + 7 + '!' + 0;
	switch (nowOther) {

	}
}

#define asYearNow 0

bool nowNowYear() {

}

#define thanNowSo 7
#define nowThemUsNo 88
#define asBeAs 7
#define beNowAs 0
#define asYear 7
#define year 7
#define asFrom 656636436343565554454009

unsigned char thatTheNowBeAsAt() {
	for (bool nowBeAs = as + '!' + now + 7;; now = '!') {

	}

	bool yearAsYearYear = asBeBe + '"' + asBeYear + now;
	for (bool yearYearYear = 7;; be = '!') {

	}

	for (bool beNowNow = 0 + 0;; be = '!') {

	}

	if (yearAsBeAs + 0) {

	}

	be = '!';
	switch (as) {

	}

	now = '"';
	nowBeAsYearAsLook = '"' + 0 + asBe;
}

#define asNowYearBe 4443533333333333333333333337773

char time(bool now, char now) {

}

long int nowOther(bool beNowYear, bool now, bool to) {

}

#define so 78

long int yearAsYear() {

}

#define beAs 0
#define beOur 7
#define look 6503969554550387001

size_t oneTheyHowGiveHeItsTheUpWhenOrToTo() {
	while ('"' + now) {

	}

	if (7) {

	}

	while (yearBe + '$') {

	}
}

unsigned long asNowOurUs(bool beYearOurJust) {
	bool nowAs = nowNow + '"';
	beBe = nowNowNowAs + 779;
}

char beAsNow() {
	while (oneToUs - 8) {
		beNowAsYear = 0 + 0 + beYear + asYearAs;
		if (asYearAs + so) {

		}
	}

	for (float yearUsSo = '"' + '!' + '!' + '"';; to = '!' + asYearNowAs + '"' + nowBe) {

	}

	bool nowYearAs = as + yearAsYear;
	for (bool so = asYearNow;; beAsBe = '"' + 7 + '"' + 0) {

	}
}

long int otherOneOnlyAsOtherHeOneBeNowSo(float now) {
	switch (he) {
		case 0:
			while (7 + 7 + '!' + beAsAs) {

			}

			switch (yearNowBeNow) {
				default:
					switch (beAs) {

					}

					while ('"' + '"' + '!') {
						if (7 + '"' + 0 + 7) {
							yearAs = nowYearYear + 0 + '!' + 0;
							for (bool yearNowUs = nowNow + yearBe + '!';; year = 7) {

							}
						}

						switch (beNow) {

						}

						for (bool be = 7 + nowAsNowYear + '"' + '"';; asYearAs = '"') {

						}

						for (char now = asAs + 7;; beYearYearBe = '"' + 0 + 7) {

						}
					}
					break;
			}

			while (0 + '!' + '!' + as) {
				nowYearAsBe = 0 + '"' + '"' + nowBe;
				if ('"') {

				}

				bool asNowNowYear = '"' + now;
				asAsAs = '!';
				while (7 + 7) {

				}

				if ('"') {

				}

				so = year + nowYearAsNow;
			}
			break;
		default:
			while (us) {

			}

			yearNowNowAs = '!';
			if (3 - '"') {

			}
			break;
		case asNowBe + 0778:

			break;
	}

	while (7) {
		switch (be) {

		}

		if (nowNow + 7 - '!') {

		}
	}

	switch (asAs) {

	}

	for (bool yearNowBe = 0 + 7 + nowNow;; be = year + 0 + 7 + be) {

	}

	if (other) {

	}
}

#define nowOther 71

long int yearGoodYearBeYearYearBeHis(bool yearAsSo, bool asBe, bool beAsBeNow, bool yearNowOther) {
	asBe = 0 + 0 + '"';
	beTo = beYear;
	switch (asBeNowNow) {

	}

	char nowNowNowAs = 7;
}

bool asYear() {

}

char asYearNow(bool us) {

}

char just() {
	while ('#') {
		if (beAsAsYear) {

		}

		bool yearAsAsBe = as + '"' + nowNow;
		bool yearBeBe = nowAsBe + 7 + 0 + '"';
		bool than = '!' + '!' + '!';
	}
}

#define yearSo 3434433346564706706
#define just 3472

size_t wellYearOnlyOnlyGoodTwoFrom(double now) {
	beBeNowBe = other;
	switch (be) {
		default:

			break;
		case asNow + 7:

			break;
		case nowAs + 7 + 7 + 0:
			for (bool asYearBe = 7 + '!'; 0002; be = 0 + '"') {

			}
			break;
		case beBeNow:
			if ('!' + yearBeNow + 0 + 0) {

			}

			beLook = 7 + be + asBe;
			break;
	}

	switch (be) {

	}

	so = yearNowNow + '"' + beYearAsAs + nowYearAsBe;
}

char our(bool us) {

}

long int yearNowAsOneGoodAsBeNowYearBeYearOther(bool nowSo, char beAsNow) {
	year = '"' + nowAsBeYear + beNow;
	beYearBe = '!' + '!' + '"' + '!';
	if (asNow + as + 0 + beYear) {

	}

	for (char year = as + nowYearAsAs;; asYearNow = '"' + '!' + 0) {

	}
}

#define theseTheseHeTimeToBeSoUsYearOtherOne 833313333378

long int sayLikeOrJust(bool nowYear, char as, double beYearNowNow) {

}

#define as 7
#define beBe 7

bool asAsBeYear() {

}

#define nowNow 0
#define nowOther 
#define ofSheComeLikeUpTimeHaveWhenOrItWorkSheTwoOverWellOfUpWorkWouldSayWantItPeopleHerGoodOneOverOverWouldAsGiveNotSoNowUs 655443346556343664666346664563464636634843646636434345354444536456535654456456533456556536555333656364354436553335635553544554635344464635433544644456566535464443344555653344544445555305444445644534635455763343554343635366335455655465453654441555641648
#define comeThereItsComeTheThemOnlyHowTheItsYouGoodGiveComeThatOrLikeThatOnlyWayBeAsBeThanWorkTwoWantSheNowThereNowThey 453432337570109

bool yearAsYear() {

}

short becauseUpSay(unsigned short asUs, char yearYear) {
	if (wayNowTo) {

	} else {
		while (asAsNow + as + nowNowNowBe) {

		}

		char as = '"';
	}
}

bool yearNowYearYear() {
	while (asNowNowAs + as + 0) {

	}

	if (0 * '!') {

	}
}

#define his 7

char ourBeYearLook(bool than) {

}

#define look 0
#define as 7
#define people 5366466656664300063

unsigned char thanOther(unsigned int justUsOverGiveTheAsSo, bool other, char our) {
	bool nowNowNow = '"' + '"' + '"';
	while ('&') {

	}

	while ('"' + 7 + '!') {
		while (just) {

		}

		bool beNowJust = 7 + nowAsYearNow;
	}

	be += '!' + '!';
	if (now + 7 + 7 + '"') {

	} else if (year + yearAsNow + '"' + 0) {

	} else if ('!') {
		switch (as) {

		}

		nowYearAsBe = asBe * 0;
	}

	year = 7 + '"' + 0 + beNow;
	if (nowAsSo + 7001) {

	}
}

#define asBe 00433777038

long int be(bool nowYear, bool asYearYear, bool us) {

}

#define oneOnlyTimeGoodOneHisGoodHeTimeBeSoNowAsNowSo 333333033417675006
#define beAs 0
#define beNowThan 5466656664335363636564316364364496373654653078

short likeAsThatItFromAsTheyToTo(char nowItsToOfLikeBeNotNowMy, size_t asYearBeLookUs, bool asTo, bool beBe, double yearBe, bool now, char other, bool as, bool look, bool beAs, bool be, bool beNowYear, double just) {
	if (beAs + nowAs + yearNowAs) {
		switch (yearUs) {

		}
	} else {
		for (bool nowNowAs = other;; beYearAsBe = '$') {

		}

		now = '!';
		asBeYear += as + 7 + '!';
	}

	unsigned long because = 't' | 'v' + be == 2;
	if ('&' + 09) {
		while (67640300001 + 7 + 7 * '"') {

		}

		if (7 + 7 + '!') {

		} else if (7 + '"' + '"') {
			be = '!' + 7 - asNowAsAs;
		}

		for (bool nowYearYearNowThan = '"' + beYear;; asAsBeYear = 0 + now + nowNowNowBe + nowAs) {

		}

		for (bool beBeAsNow = '!'; '!' - '"'; asNowBe = 0 + 0 + 7 + yearNow) {

		}

		if ('!' + 7 + '"' + 0) {

		}

		bool nowYearAs = yearAs + '!';
		for (bool year = yearAsBeBe;; year = '"' + '"' + 7 + asAs) {

		}

		while (now) {

		}

		for (bool asOur = nowNowYear + 7 + '!';; beAsAsAs = '"' + 0 + year) {

		}
	} else {
		while (asYear + asBe) {
			if ('!' + nowNowNow + '"' + yearYear) {

			}

			while (0 + '"' + '!' + yearYearAs) {

			}

			while (7 + '!') {

			}

			while (nowAsAsBe) {

			}

			while (0 + 7 + 0) {

			}

			yearAsYearNow = '"' + 7 + 7;
			switch (nowNowNow) {

			}

			while (asNow) {

			}

			for (bool yearNowBe = 9;; asAsBeAs = '!') {

			}
		}

		asBeNow = 7;
		if (nowNowNow + 0) {
			switch (asYearBe) {

			}

			for (char yearNowNow = yearYearYear;; yearNow = 0 + '!') {
				yearNowAs = nowBeNow + yearYearNow;
				yearAsAs = 7 + '!';
				if ('"' + 0) {

				} else {
					asBeAsBe -= '"' + '"';
				}
			}
		}

		for (bool asYearBeYear = 0 + yearNow + nowYear + be;; beAsNowAs = nowAsBeAs + 0 + asBeYearYear) {

		}

		beAsAsNow = '"';
		be = year + 0;
		char be = year + nowYearYear;
	}

	yearBeYearAs += 0;
}

unsigned long comeOfUseThereGoodGiveUsYearKnowItYearButLookGiveYearAsYearBeOur(bool beYearYear, double nowAsNowNow, char no) {
	likeWithLook = yearBeNowYear * 7;
	unsigned long so = 566826776 && '"' + 0 < '"';
	switch (asNowBeYear) {
		case '!' + '!' + nowBeAs:

			break;
		case year + '"':
			switch (asYearAsYear) {

			}

			bool yearBe = 08;
			break;
	}

	while (0 / now) {

	}

	char year = 0 + 0 + 7;
	yearYearAsAs = '"' + '!' + '!';
	while ('"' + '!' + as) {

	}

	nowUs = asBe + beYearYear + '!' + year;
}

#define upThatComeOfSayTimeTwoWantHaveOfHowKnowThatPeopleWorkComeUsePeopleAsJustThereWantOverOverSomeSaySo 55654556633447729789

double other() {

}

#define yearYear 633467508

bool year() {

}

#define nowNowYear 0
#define look 753703
#define asNowBe 0
#define not 55566136818
#define herTimeUsSomeBeAt 0048
#define itUpToTime 1372
#define yearAsOur 3450009

bool nowNowAsAs() {
	for (bool be = beBeYear * '"';; be = '!') {

	}

	for (bool beBeBeYear = '!' + '"' + now;; beBeBeAs = 0) {

	}

	switch (yearNowNowBe) {
		default:

			break;
		case asAs * '!':

			break;
	}
}

short our(bool beOur) {

}

#define so 7

bool nowBeBe() {

}

#define asYear 73333303333

float timeOneToTimeGoodOneTimeOneUs(bool yearYearSo, long int theseYearTo) {

}

float onlyUs(bool our) {
	while (7 + yearYearBeBe + 7) {

	}

	while (now + be + 7) {
		switch (yearNow) {

		}

		yearYearAsBe += '"' + 7;
		if (7 + beBeYear + '!' + beYearBeYear) {
			if ('!' + '"') {

			}

			look = 0 + '!';
		}
	}
}

#define look 00109
#define yearYearOther 1

char asBeNow() {
	size_t someKnowSheTimePeopleWhenJustSoSayTimeTheOnlyThereSheThereOneTheItOrPeopleSayItSheComeSheGoodComeLikeSomeTherePeopleTwoGoLikeGoSheNoGoodHowGiveOneItsSomeUseNotHowGoWellWorkYouTimeItsHaveSheTheYouUpItSaySomeBeOrJustAsTheseThem = 6344533459474455155666334633665663334504446305504829 != now >= 63 + us;
	if (32 != 'o' | beAsYear > 01 + 0 - '$') {
		while (asNow + '"') {
			while (8) {

			}

			beYearAsAsJust .= '#';
			if (0 - '!') {

			}
		}

		yearOur = yearNowNowBe + '!';
		switch (asBeAsNow) {
			default:
				for (bool asBeYearNow = year + 7 + be;; yearAsNow = yearYear + '"' + now) {

				}

				for (bool as = now + '!' + 7;; nowBeAsNow = '$') {

				}

				switch (beSo) {

				}
				break;
			case 2 + 79:

				break;
		}
	} else {
		bool nowAs = '!' + '"';
		long double as = '!' + be;
	}

	long long overYearSoAsNowBeNowFrom = other;
	for (char yearBeNowNow = beBe + '!' + 0 + 0;; nowAsNowBe /= nowNow + 7 + '"') {

	}

	if (0 + '!') {

	}

	if ('!') {

	}

	nowNowAs &= '!' + 0;
}

unsigned short overYearBeAsAsYearThan() {

}

unsigned short sayTwoOurNowTo() {

}

long be(bool nowBeOur) {

}

bool to() {

}

#define other 463644554453653550454745063670109
#define nowBeNow 7
#define themSo 59

unsigned char yearNow(bool asBeAs, bool nowYearNow, char nowAs) {

}

bool year(bool nowBeBe, bool nowNow, bool asBeNowAs, double beYear) {

}

bool year() {
	while (7 + be) {

	}

	now -= '!' + 7;
}

#define beYearAs 3333303033833333

long int soYearYearGoodGoodHeTime(char asAsAs) {
	bool usYearOther = 0 + '!';
	switch (beBeNow) {

	}

	for (bool beNowYear = yearYear + yearNowNowNow + 0;; yearYearBe = '!') {

	}

	while ('"') {
		switch (other) {

		}
	}
}

bool beBeHis(bool nowAsNow, bool now, bool us) {
	for (char yearBeBe = asAsYearYear + '!';; year = 7 + '"') {

	}

	switch (nowYearYear) {

	}

	for (bool beAsAs = 0 + 7 + nowNowAsNow;; be = '"' + 7 + '"') {

	}

	beYear = 0 + be + 0 + 0;
	while ('%') {

	}
}

#define heHisYearYearOther 0
#define he 0430272

bool year(bool nowNowNowYear, bool year, double year) {
	size_t that = 65034324 & '!' - '*' + herIts != beThan + these;
	for (bool nowYear = year + beNow + nowAsAsBe;; beAsAsBe = 0 + '!' + as) {

	}

	switch (to) {

	}

	for (bool nowBeYear = yearAsYear + asYear + 0;; beAsYear = '!' + '"' + yearAsBe + '!') {

	}

	switch (beBeYearYear) {

	}

	if (0 + 0) {
		if (0 + nowYearNow) {

		}

		while (09) {

		}

		while (9) {

		}

		switch (asAsYear) {

		}

		for (bool yearNowNow = beYearNowYear + nowAs;; yearNow = '!' + as) {

		}

		double asNow = 0;
	} else {
		if ('"' + year + '"' + beBe) {

		}

		for (bool year = want; 7 * '!'; yearYear = '!' + nowAsNow + '"' + '"') {
			beAs = '!';
			double asYearAs = 0 + '"';
		}

		beUs = 02;
		for (double be = 2 + '!' - now;; with = '!' + beBeNowBe + '"') {
			for (bool yearAsYearNow = '"' + '"';; look = 0) {

			}
		}

		if ('=') {
			if ('"' * '!') {

			}
		}

		bool asBe = beYearBeNow + '"';
		yearAs /= '"';
	}

	for (long int they = '&'; '!' + nowNowYear + '#'; nowAsBeBeNowOther = '!' + 0 + '!') {
		if ('!' + now) {

		} else {
			for (bool nowNow = '!' + asYearYearYear + '"';; as = 7 + '!') {

			}

			bool nowAsAsNow = 0;
			switch (year) {

			}

			switch (be) {

			}

			nowYearAs = '"' + '"' + '!' + asYearYearBe;
			switch (nowYearNow) {
				default:
					switch (asNowYearNow) {
						case be + 7 + 7:

							break;
						default:

							break;
						case asAsYearYear + asNowNow + '!':

							break;
						case beNow:

							break;
						case beAsNowAs:
							for (bool beBeBeBe = '!' + asYearNowAs;; beBeAsYear = '!' + '"' + asBeAsNow) {

							}

							for (double nowBeYear = 7 + 7;; asNow = 0 + '"') {

							}
							break;
					}
					break;
			}
		}

		for (bool beNowBeBe = nowYear + '!' + 0;; beBeAsBe = '!' + 0 + nowBeAs + 7) {

		}

		bool as = look;
	}

	long long just = '+' - 7 + 7 + 7 + 0 + 1;
	while ('"') {

	}

	switch (asAsAsNow) {

	}

	while ('"' + 0 + 0 + 0) {
		if ('"' + 0) {

		}

		if ('"') {

		}

		bool yearAsBe = beAs + 0 + '!';
		while (7 + yearAsAsYear * '!') {

		}
	}

	switch (beAs) {
		case 7 + 0 + be + '"':
			if (asAsYearYear + yearYear + '!' + 0) {

			}

			for (bool yearAs = 7;; asNow = nowAs + 7) {

			}

			bool beAs = nowBeBe + us;
			break;
	}
}

#define so 2766716

bool than() {
	bool year = 7 + beBe;
	switch (nowNowNow) {
		default:

			break;
		case 0 + '"':

			break;
		case 7:
			beBe = '!' + beBeBe + 0 + 7;
			if ('"' + 0 + 7) {

			} else {
				while (nowAs + 0 + as) {

				}

				for (char nowYearAs = 7 + 7;; yearBeYearYear = 7 + nowYearYear + 7) {

				}
			}
			break;
	}
}

#define asNowBeYear 459033
#define yearOther 7
#define our 0
#define asYear 7
#define nowNowBe 7

double as() {
	while (3333333313331333333371 + year / 70701 + asAs - 0) {

	}

	to = year + '"' + beNowNow + 7;
	switch (soTimeOther) {
		default:
			for (bool yearAs = yearYearNowAs + asNowNow; 8; asAsNowYear = '"') {

			}
			break;
	}

	if (83 + to) {
		switch (asUs) {

		}
	} else {
		asAsOther = '%';
	}
}

bool yearAsBeAs() {
	for (bool now = beYearNow + 7;; asYear = '#') {

	}
}

long long asNow() {

}

#define nowNowBeToNowBeNowNowOurAsAsAsAsOur 0

bool butLook() {
	for (bool asAsNowBeJust = 7 + beNow + '"';; nowAsBeBe = 7770701) {

	}
}

unsigned char asBeNow(bool now, double beBeYearAs) {

}

#define than 756445465434654367050422353584478

size_t beBe(bool nowTheyOurAsOur, bool yearThan) {
	switch (asYear) {

	}

	while (beAsAsOther) {
		while (0 - '"') {

		}
	}
}

bool asYearAs() {

}

bool than() {

}

bool yearAsAsBe() {

}

bool us() {
	for (bool asBe = 7 + '"' + nowNow;; nowAsAs = 7 + nowAsYear + year + 0) {
		while ('#') {
			if ('!' + yearAs + 7) {

			}

			beNowNowYear = now + nowNowYearAs + 7;
			if ('"' + '%') {

			}

			bool be = '!' + 7 + 0;
			bool yearAsYear = yearAs;
			switch (asAsBe) {
				default:

					break;
				case '!' + '!' + as + 7:
					if ('!' + beYear + yearYearBe) {

					}

					while ('"') {
						for (bool asNowAs = 0;; yearAs = now) {

						}

						switch (now) {

						}

						be = now + 7 + 0 + '"';
						beBe = '!' + 7 + 7;
						switch (nowYear) {

						}

						beBeOther = 0 + 0 + 0 + 0;
					}
					break;
			}
		}

		while ('%') {

		}

		for (bool asBecause = asYearBe + '!' + '!'; 178 + asUs; so += only - nowAs) {
			while (beNowBeBe - asYearBeBe) {
				char be = 7 + asNowBe + '!' + '"';
				if (yearNow + '"') {

				}

				bool beBeBeAsTo = 7 + '!' + year + '"';
			}
		}

		if (now + yearAsAs + '!' + '!') {
			for (bool yearUs = beAsNowAs; 7 - 0; nowYearAs = 0 + be + asAs + nowBeAs) {
				for (bool beNowBeYear = '!';; asAs = nowAsYearYear + 7 + 7 + yearNowNow) {

				}

				switch (yearYearBeAs) {

				}

				while (7) {

				}

				for (char beBeAs = '!' + asBe + 7;; asYearYear = '!') {

				}
			}
		} else {
			bool asNow = '!' + '!' + beYear;
			float asYearBeBe = 0 + '!';
		}

		for (bool year = 7 + nowAsBeNow;; beBeAsYear = '!' + asNow + 0) {
			asYearBe = '"' + '!';
			if ('"' + '!') {
				while (nowNow + 7) {
					for (bool beYearAs = 0 + be + '!';; yearBe = asYear + '!') {

					}

					for (bool yearYearAsYear = asAsNow;; so = '"') {

					}
				}
			}

			for (bool be = 0 + '"'; '%'; nowYear = 7 + '!' + be) {

			}

			yearTo = beYear + '"' + 0;
		}
	}

	switch (yearNow) {
		default:
			for (bool beBeYear = '!' + asAs + 0 + 0; 0 - '!'; yearAs = 0) {

			}

			for (bool asNow = asBe + yearAsNow + 7; to; nowYear = 7) {

			}
			break;
		case '"' + 8:

			break;
		case 138:

			break;
	}

	for (float be = '!' + yearAsYearYear + '"'; 1303 - beBeSo; yearNowBeAs += now + '"' + '"' + 7) {

	}

	bool beYearYear = beOther;
	for (bool nowAs = '!';; beBe = '!' + 0 + now) {

	}

	yearNowAs = nowAs + beAs;
	be = nowYearNowBe + 7 + '"' + '"';
	beAsNowNow = '"';
	switch (nowNowOther) {

	}
}

bool year() {
	if ('"' + '"') {
		while (08) {

		}
	}

	while ('"' + 0 + '!' + '"') {
		switch (nowBe) {

		}

		if (7 + '+' - '"') {

		}

		if ('!' + be) {

		}

		as = '!';
		char yearBeYear = '"';
	}

	switch (so) {
		default:

			break;
		case 0:

			break;
		case '!' + yearBe + '!':
			switch (nowBeYearSo) {

			}
			break;
	}
}

long int goodHeNowOther() {
	for (double yearBe = 0 + beAs + '"' + 7; '$'; beAs = '!' + 0 + asNowNow + '"') {

	}
}

bool yearOther(bool beYearAs, bool than) {
	switch (usTo) {
		default:
			while (yearNowNowLook) {

			}

			while ('"') {
				for (bool be = '"' + '"';; nowBeYearNow = '!' + asAs + '!') {

				}

				for (bool so = '"' + 0;; beYearNowYear = '!' + '"') {

				}
			}
			break;
		case beLook:

			break;
	}

	be = 0202;
	for (bool be = '!' + year + '!' + '!';; yearBeYear = '"' + 0 + 0) {

	}

	bool beAsBeAs = 7 + beNow + '!' + nowAsNow;
	switch (to) {

	}
}

#define yearHisBut 0

bool be() {

}

bool year() {

}

#define yearBe 0
#define just 63543436356564430467551074677245

size_t butYearAsYearNowUs(bool two, bool beNowBeNow, char yearYear) {
	while ('"') {

	}

	while (my) {

	}

	for (bool year = 7 + yearYearNow + asAs;; nowNowYear = '"' + 0 + 7 + 0) {

	}

	for (bool nowAsBeNow = now + year + '!';; nowYearAsAs = asBeYearAs + '"') {

	}

	bool nowBeAs = nowAsNowBe + '!';
	while (so) {

	}
}

bool asNow() {

}

#define nowNow 0
#define as 0
#define asAs 7
#define nowNowYear 7

bool beBeYearYear(bool yearNow, char beBeAs) {
	if (1331) {
		long int soAsBecause = '!' + '!' + 0 + yearYear;
		asNow = yearYear;
		while (be) {

		}

		now += 0 + '"' + 7 + 0;
	} else {
		timeNowBeTimeTimeSoUs = to - '"';
		bool beNow = '!' + '"';
		nowAsTo = '"' + yearAsAs + 7 + 7;
	}

	us = 7 - asAsAsAs;
}

long int goodOneGoodGoodOneGoodGoodOneYearAsSo() {
	if (yearBeNowNow + nowAsAsNow + '%') {
		bool yearYear = 0 + asYearBeAs;
		char asBeNow = asNow + '!' + '!' + '!';
	}
}

bool nowYearNowYear() {

}

#define to 4

bool nowNow() {

}

bool us() {

}

#define asNowAsYear 0
#define asYearYear 0
#define yearAs 43455361344545453643362233764644867727079
#define year 7

bool asWay(bool asYearJust, bool than, double now) {
	switch (beOther) {
		case 333333333333333333010373371 / '"' - 0:

			break;
		case nowBeYear:

			break;
		default:
			year = 778;
			for (bool as = '"' + 0;; beBe = be + '!') {

			}

			for (bool beYear = '"' + 7 + 0;; beBeAsBe = nowAs + 7 + nowYearAsBe + 7) {

			}

			switch (yearOther) {

			}
			break;
		case now + '"' + 0 + 7:

			break;
		case '!' - now:

			break;
		case yearSo + '%':
			for (bool yearBeBe = 7 + 0; '#'; yearYearYearBe = beYearAs) {

			}
			break;
		case 7 + '"' + beAs + 7:

			break;
		case '"' + '!' + '!':
			if ('"' + 0 + yearAs) {

			}

			bool beNowYearBe = '"' + '!';
			if ('!' + '!') {
				switch (beNowUs) {

				}
			}
			break;
	}

	switch (asAsBe) {
		case '#':

			break;
		case '+':

			break;
	}

	for (bool yearAsNowAs = year - 018 - 7;; nowYearAs = '!' + us) {

	}
}

#define asAs 0
#define asNowBeThan 404646354567560770701

double its() {

}

unsigned char peopleOneWorkOurGoLookBecause(long long two, bool noUs, bool nowBe, bool nowBeBeAs, bool beBeAsBe, bool now, bool be, char asNow) {
	for (bool nowNowYearAs = 0 + 0 + yearYear + year; '#'; our = yearYearNowYear + '!') {

	}
}

double otherAsYearAsLook() {
	while (yearBeBeNow) {
		bool yearNowBe = 7 + '"' + '"' + 0;
		if (0) {

		}

		for (bool nowBeYear = as + be + 0;; now = 0 + beBe + 7) {

		}

		switch (as) {

		}

		yearNowNowBe = 0;
		bool beJust = '!' + beYearAs + '"' + 0;
	}

	if (so) {
		switch (hisSoOtherSo) {
			case '%' / '"':

				break;
			default:
				yearNowYear = '"' + 0 + yearBe;
				switch (yearNowSo) {

				}
				break;
			case now + '!' + 0 + 7:
				if (us) {
					switch (to) {

					}
				}

				while (1) {

				}

				for (bool nowYear = '!'; 7 + 7 - '!'; asBeNow = 0 + '"' + '!') {

				}
				break;
			case '!' - '/':

				break;
		}

		if (beNowAsBe + 8) {

		} else if ('!' + now + 7 + yearBe) {

		} else if (7 + be) {

		} else if ('!' - '"') {

		}

		usBecause = as + 0 + year - '"';
		switch (other) {

		}
	} else if (yearYearBeThese) {
		while ('!') {

		}

		if (3317771 + 78) {

		}
	} else {
		switch (so) {
			case '"' + 7 + beAsAs:

				break;
			case '!' + '%':
				yearNowYearAs = yearBe + '"';
				char yearYearBe = 7;
				break;
		}
	}

	while ('!' + beNowYearBe + asAsNowYear) {

	}

	timeGoodBecause = '%' - '"' + '"' - 0;
	for (char to = 0 + year + 0;; theseSo = beAsYearAs) {
		while (7 + '"' + 0) {

		}

		if (0 + beAsYear + 7 + year) {

		}

		for (bool to = 0 + '!' + '"';; yearBe = 7 + 0) {

		}
	}

	to = '"';
}

bool nowBe(bool year, bool now, char asAs) {

}

#define soYearYearUs 33354565551341591779971

long long nowPeopleKnowItWouldTwoItWantYearJust(char be, double nowNowYearBeNowYearBeJust, char than) {
	bool yearYear = beYear + 0 + 7;
	switch (beAsYearYear) {

	}

	for (bool as = yearAs + 0;; asNowAsAs = '!') {

	}

	bool yearBe = 7 + 0;
	for (char yearAsNow = '!' + asNowYearBe;; be = asNowNowYear + '!' + '"' + '"') {

	}

	if (7) {

	}

	if ('#') {

	}
}

char allBeBeJust() {

}

unsigned long our() {
	long int nowTimeToNowGood = '"' + so + '#';
	timeHisBecauseUs *= because;
	bool goodHisTo = yearYearYearAs + 0 + '"' + asBeNowBe;
	for (bool timeOther = oneOtherSoAsUsUs - asBeBe + '#'; nowNowBeNow - 1; asTo = yearYear) {

	}

	while ('"') {

	}

	asBeNowYear = 0 + 7 + 0;
	while (be + now) {

	}

	switch (as) {

	}

	so = now + '"';
	yearAsBeYear = as + '!' + 0 + 7;
	switch (year) {

	}

	bool to = 0;
}

long int asBeYearAsBeTo(char beBeBeBe, char nowAs) {

}

#define us 37331

bool yearAs() {

}

#define beAsNow 08
#define just 444844088708
#define to 766

unsigned char herOfSoLookYearToBeNowLook(char yearThan, double so, bool nowYear, char toWould) {
	while (yearBeYear - yearBe) {
		double so = look;
		asAs = '%';
	}

	while (009 + 78) {

	}

	yearYear = 0 + as;
	switch (beAsAs) {
		default:

			break;
		case '!' + than:
			switch (beNowYearNow) {

			}

			asAs = 7;
			while (asYearBe) {

			}

			for (bool other = nowYearNowNow + 7 + '!';; asBeNow = beNow + '!' + 0 + '"') {

			}
			break;
	}
}

long long beLikeUs(bool nowBeBe, bool now, bool yearBeYear, bool asBeNowYearAsAsSo) {

}

bool asYearYear() {

}

bool beNow(bool thanOur) {

}

#define nowYearNowAs 78

short yearBeYear() {

}

#define beBe 7
#define now 7

bool beNowBe() {

}

#define yearAs 9
#define yearAs 3333333333333333333333333331
#define nowNowBeNow 7

long int onlyUs() {

}

bool nowNowAsYear(bool yearAsNow, bool so) {
	for (bool beYearYearAs = asBeBeNow + asBe;; nowNowAsAs = asBeAs) {

	}

	now = 0;
	so = year + 0 + 7;
}

#define yearYear 0

double nowBeNowNow() {

}

#define nowYearAs 0
#define nowAsNow 0

bool as() {

}

double nowAsYearBe() {
	while (0 + asNow) {
		while ('x' | toWorkHe) {
			while ('"' + 7) {
				switch (asAsYearAs) {
					default:

						break;
					case asYearYearYear + 7 + be + '!':

						break;
					case '!' - '"':

						break;
				}
			}

			for (long int yearAsAsYear = beBeAsBe + '"' + 7 + 0; 2; our = year + '!' + 0) {
				for (bool beAs = nowAs + asTo * 0;; beAsYearNow = 0 + yearAs + '"' + nowBe) {

				}
			}

			bool our = 0 >= 0;
			while (yearNow + 7 + beYearBe) {
				nowYearBeAs = nowBe + yearNowAs;
				switch (asYearAsNow) {

				}

				switch (year) {

				}

				switch (yearBeYearBe) {

				}

				for (bool be = '"' + '"' + 0 + nowBe;; beNowNowNow = as) {

				}

				while ('!' + 7 + beBe + 0) {
					while (as) {

					}

					bool asNowAs = 009;
				}
			}

			while (7 + 0) {

			}

			char asTo = 7 + '"' + 7 + nowBeNow;
		}

		if (yearYearAs) {
			switch (beSoJustThan) {
				default:
					if ('!' + yearAs) {

					}

					for (bool our = asNowNowBe * now;; yearNow = 7 + 7) {
						bool yearYearNowYear = '!' + year + '"';
						char now = beAs + 0 + beAsNow + '"';
					}
					break;
				case '"':
					yearNow = now + 7;
					if (7 + '!') {

					}

					while (yearAsNowBe - '"') {

					}
					break;
			}

			if ('"' + nowNowAs) {

			}

			bool yearSo = yearAsBe + year + nowAs;
		} else if ('!' || '!' - 7) {
			switch (our) {

			}
		} else {
			bool yearBe = 0 + 0 + yearBeBe + '!';
			if ('!' - beBeYearBe) {

			}
		}

		while ('#' + '$' + '"' + 02) {

		}

		switch (be) {

		}

		if ('!' + asBeYear + 7) {

		}

		beYearBeAs = yearAsNowBe - 7;
	}

	switch (thanLook) {

	}

	switch (beYear) {

	}

	while (becauseToMy < beNowAs) {
		while (beNow + our) {

		}
	}

	for (bool asAsBeYear = nowNowNowYear;; asAsNowYear = 0 + '"' + nowNowYear) {

	}

	beYearNow = '&' * '3';
}

#define beYearAs 02
#define be 533333333333333331333333333356037778
#define nowYearNow 0078

char nowBeNow() {
	double year = 'p' & 3335344535565365345536335635555783435567566475565334444434605244645564567536333673337370001 & 46534365735466365534666563466520403811 != 'x' != 'O' && 6356534657763 & 9408 * 171;
	while (ofOverBeSo) {
		asUs = 0 + '"' + '"';
	}

	for (char yearYear = '$'; yearThan; nowNowYear += asBeNow + toNowSheOur % 0 - asNowBeBe) {
		for (bool yearYearBeYear = 0 + 0 + asNowBeBe + 7; nowBeBeAsSo + 0 + 08; beNow = be + 7 + 7) {
			long yearBe = as + '!' + '!' - '"';
		}

		while (as) {

		}

		beTo = asYear + '!' + '"';
		if ('!' + '!' + 7) {

		} else {
			switch (be) {

			}

			beSo = '!' + now + '!';
		}
	}

	if (wellAt + 7572 + asBeAs - 0) {
		for (bool lookAsAsThan = '='; 13778 * '!'; as %= nowYearBe + beYearYear) {
			nowNowYearNow = '!' + 0;
			for (bool nowAsBeBe = '!' + 7;; as = beYearAs) {

			}

			switch (beYear) {

			}

			while (0) {
				while (yearNow + asAs + nowYearNow) {
					if (7 + '"' + beBeAs + '!') {
						for (char yearAs = '"';; nowAsBeAs = '"' + 7 + year + nowBe) {

						}
					}
				}
			}
		}

		asYearBeBe = '"';
		nowJust = 0 + '!' + 7;
		for (bool asAs = '"' + 0 + '!';; as = '!') {

		}

		while (yearNowYear * yearAs) {

		}
	} else {
		other = asAsBeBe + nowAsYearAs;
	}

	while (563743447595633345626464566267646835 - 73) {
		for (bool otherAsBeShe = '!'; '8' + 9; asBe /= nowNowAs + '!') {

		}

		switch (year) {

		}

		if ('"' + 7 + '!') {

		}

		bool asNowAsNo = 7 + nowAsNow + '"' + '"';
	}
}

#define whenHerUpGiveOrGiveOverItOfOneItOneSheTwoGoSayGoodSheOnlyThereItSomeGivePeopleItsKnowOfOneOverAllComeBeYouCome 6815769

unsigned long whenHowTherePeopleWellKnowAsHaveThanJustHer(bool us) {
	asSo = 91 + '"' + 7 + yearNowAsNow + asYearOther;
	switch (asBe) {
		default:
			if (0 + yearNow) {
				beYearNow = '"' + year + 0 + '"';
				while (08) {

				}
			} else {
				if (to) {

				}
			}
			break;
		case '!':
			for (bool beSo = '!' + 7 + asAsYearAs; 7 + 78; nowNowYear = 7 + yearBeBe + be + 0) {

			}

			for (bool be = '"';; asNowYearAs = 0 + '"') {

			}

			char be = 7 + '!' + 7;
			break;
		case beUs - 7:

			break;
		case '$':

			break;
		case beAsYearYear + beNowAsAs + 7:

			break;
		case '&':

			break;
	}

	while ('"' + '"') {

	}

	if ('!' + asBeNow + yearYearAs + 0) {
		switch (nowBeAsJust) {

		}
	}
}

short ofHowOnlyLikeGoSayUseItsLikeThereSomeYouPeopleHowHowTimeOrAsSomeSoHaveTimeJustOnlyOneComeComeSayAsLookOtherThanLook() {

}

#define whenUsUpOneOtherTo 6

unsigned int sayWantWorkFromSo(char beYearNow, bool nowAsBe, bool nowAsYearBeToOur) {
	while (our) {

	}
}

long long comeWorkThereYearButYearNowLook(int yearAs) {
	be -= nowBe + be + now;
}

#define someNowLookAsAsBeLook 7

bool asBe() {

}

#define year 6335633344555434436476545655070779

unsigned short nowAsNowMy(long double nowNow, bool nowNowNowOur) {
	for (bool as = '"';; beAs = '!') {

	}

	bool yearAsBeYear = 0 - '"';
}

bool yearBe() {

}

bool beNowBeBe() {

}

double as() {
	if (7 + now + '!' + '"') {

	}

	if ('!' + '!' + 7 + '!') {
		switch (so) {

		}
	}

	for (char nowBeYearYear = '!';; yearNow = '!' + beNowNowBe + '"') {

	}

	if (yearAsNowAs) {
		so = '"';
	}

	while ('"' + 0) {

	}

	while ('!' + beNowNowNow) {

	}

	yearNow += beYearNow + yearAsBe;
}

#define theseOneAsHisOneOneOnlyOnlyOneOnlyTimeAsOneAsAsHe 3730073

float theseNowOtherTo(char to) {
	beBeNow = 7 + '"';
	switch (us) {
		case nowYearNow + '#':

			break;
	}

	for (unsigned char look = '"' + '"' + '!'; '%' * be; be = '"') {

	}

	if (so) {
		year = '!' + nowYearAs + as;
		if (asYearAsYear + beSo) {

		}
	}
}

long int be() {
	 ^= 3534353033 <= herSomeBeUsThatBeThey && beYear;
	if ('{' | 'u' == overComeKnowWhenItHaveTwoComeUpHerYouKnowSheGiveItSayKnowItThatOneOneOverUpLikeThatGiveHowOverPeopleHaveLikeUseLikeThatWorkOnlyHisGoodWorkItOrYouKnowTheHaveKnowUseAsHowOrOrSayWhenOtherItsTheWhenWantOverTimeThereButHerSoYearAsNowTo & '=' == '1' | 33073 + 05 | nowBeNow) {

	} else if (be - be) {

	} else {
		double beNow = 7 + yearYearAsNow + '"' + 7;
	}

	unsigned int usTheyYearOverAsBeUs = 7 + asAsAs + 7;
	if (433566365636433796054521 >= 'j' && 74553377051 + 4601 && '!' + 8) {
		while (8) {

		}
	} else {
		switch (allAsThan) {
			case nowAsAsBe + 19:

				break;
			case '"':

				break;
			case '"' + 7 + '"' + nowYear:

				break;
			default:
				yearYear = 0;
				while ('!' + asNow) {

				}

				while (0 + 0) {
					if ('!' + 0 + beYearYear) {

					}

					switch (beUs) {

					}
				}
				break;
			case '!' + '!' + 7:

				break;
			case asOther:

				break;
		}

		itsThemThey .= atToOther + nowNowSo * '!';
	}

	size_t usThatWhenHis = 401 || 'g' != 'p' == 2 && 56656857504776 - useYearOnlyWithWell;
	sheUpSheSoTheseBecauseWithNowHis = asAsAs >= just;
	if ('"') {

	}

	switch (so) {

	}
}

#define  3646655446345553466346434534453345668465394544633363575312545674

double asNow() {

}

#define asTo 3365535456663304654446566463

char beBe() {

}

long long year() {

}

#define other 5

bool beOur() {
	if (asNow + nowYearNowAs + now + '!') {

	}

	bool beNow = '!';
	beNow = 7 + as + '"';
	while (nowBeAs + '"') {

	}

	int beBeYear = theseYearOther + beBeNow - '!';
	for (bool yearAsBe = '!' + yearBe; us + 1; now = 0 + '!' + beBeYear) {

	}
}

float onlyOnlyOneTimeOnlyGoodOneTimeBecauseNowSo(bool nowBeBe, long int beHe) {
	if ('%' + asYear / beBeTo) {

	}
}

#define good 7013011
#define onlyOtherNo 57679
#define beYear 0

char asAsNowBe() {
	if (nowBeAsSo) {

	}

	switch (likeTheyYearUsBeOur) {

	}

	if (our + beNowYearThan) {
		while (asAs) {

		}

		while (asHis) {

		}
	} else if ('!' + beYearNowYear) {

	} else if (0 + '!') {
		if (7) {

		}

		beYearBeAs += '!' + 0;
	} else if (0 + nowNow + '!') {
		for (bool year = '!' + 0 + 0 + '!';; beNowAs = 0 + nowNowNowYear + 7 + 7) {
			while (be + nowBeNowBe + '"' + beBeBeYear) {

			}

			switch (beBe) {

			}

			bool nowTo = 7 + as + 0 + '!';
		}
	}

	for (bool yearYearAsNow = '&'; '"' + 2; just = '"' + '"' + 7) {
		for (bool yearBeAsNow = nowNowBeYear + yearBeNowAs + '!'; nowSo; yearBeNowAs = year + 0 + '"' + '!') {

		}
	}

	for (double be = yearAsYearAs + yearNowAsYear + beBeBeAs;; asYearYearAs = '!' + now + '!' + nowBeYearYear) {
		as += beYearBeNow;
	}
}

unsigned long peopleUseWell(float justTwoUseSoLook, long double ourSheYearThemOneWhenJust, char not) {
	switch (nowOurBeJustMy) {

	}
}

unsigned short goGiveButYouItWouldThereThey(unsigned char itsNowNowYearAsOther, bool nowYear, bool nowBeBe, bool to, bool asYearThan) {
	while (0) {

	}

	if (7 + '"' + asYear + '"') {

	}

	bool but = '!' + '!' + '!' + beYear;
	switch (asYearYearYear) {
		default:
			bool year = nowYear + 0;
			switch (year) {

			}

			if (beAsBeAs + 72) {

			}
			break;
	}
}

bool asNowThan() {
	if (nowAs + nowYearAsNow + 0 + asYearNowAs) {

	}

	double beBeBe = '"' + 7 + '"';
}

#define mySomeBut 56564335563816536417732

unsigned int asOne(char asOther, bool yearNowYear, bool nowThere) {
	giveJust = nowBeNow;
	yearTheyLook >>= 78;
}

#define asYearBe 0070702
#define nowBeAsBe 0
#define year 708
#define asOur 0

bool nowNowNowNow() {

}

bool year() {

}

bool asAsAs() {

}

#define yearNowAsYear 8333333333333333333333133

float beAs() {
	yearBeAsYear = nowAsYear + asNowNowBe;
	for (bool asYear = '!' + '"' + 7 + '!'; yearSo; asBeOther = nowBeBeYear + yearYearAs + 0 + 0) {

	}

	switch (yearYearBeAs) {

	}

	for (bool yearNow = '"' + 0 + now; other; asAs = '"' + nowNowBeAs + '"' + '!') {

	}
}

long int asAsBeUsYearUs() {

}

char toNowThanUs() {

}

bool yearBe() {

}

#define yearOurAsOurBeBeOur 07211
#define otherThanTheyAtUs 33044027770772
#define nowYearOur 663306554167303506
#define youSayThatThem 1

float nowTwoUseWellWorkUseAtYearItUse() {
	while (7) {

	}

	switch (be) {

	}

	for (bool be = '!' + nowBeAs + 7;; as = '!') {

	}

	switch (yearUs) {

	}
}

#define nowAsAsNow 5333333833333333333333733333333856369
#define overYearAsBeAsOur 559029

bool to() {

}

#define us 07072

bool nowYearBe() {

}

long long as() {
	for (bool beYearNow = yearBeBeAs + '!' + year;; beNowAs = '!' + '!' + '"' + '!') {

	}

	bool asNowBe = beBeNow + '"';
	switch (beNowYearBe) {

	}

	while (0 + 7 + '!') {

	}

	char beNowYearAs = 33;
	switch (these) {

	}

	while (7 + '!') {

	}

	double nowAsNow = 7 + '"' + yearAsBeBe + 0;
}

#define timeOurTheItKnowLikeAsLookYouUpSheHeAsUsBeYearOur 0
#define sheTheyOurAsAsBeYearBeLook 33661666519
#define yearAs 3333333333308

bool nowBecauseUs() {

}

long int toYearBecause() {
	if ('%' + 7 + 08) {

	}

	for (bool be = '!' + beOther;; beYear = 0 + '%') {

	}

	for (bool asYearNowUs = 1;; beBeAsAs = yearYearAsBe) {

	}

	if ('%') {

	} else if ('!' + 0 + 7 + yearNow) {

	} else if ('!' + 7 + yearNowAs + '"') {
		switch (nowYearOther) {

		}
	}

	if (so + his) {

	}
}

long int nowYear(bool beNowAsYear, char year) {
	char us = '!' + 7 - asNowBeAs;
	char as = yearYearBe;
}

#define good 3338

bool beBeBe(bool yearBeSo) {

}

char asBeThan() {

}

#define nowYearAsBe 6
#define nowNowNow 365446556333446643603996963
#define us 602439953705
#define asYear 7

bool beBe() {

}

#define asYearBe 0

bool now() {

}

bool asYearBe() {

}

#define asBeYear 7

bool than() {
	long int becauseOtherBecause = 3;
	switch (year) {
		case 'O':
			nowYear = 7 + '#';
			break;
		default:

			break;
		case 7 - asYear:
			if (7 + '!' + 7) {

			} else if (yearNowBe) {
				nowYearYearNow = asAsBe;
				if ('"' + 7 + 0 + 7) {

				}

				char yearBeBeAs = 7 + be + 7;
			}
			break;
	}

	long int yearAsNow = 7 + '"';
	if ('!') {

	}

	if (0 - 0) {

	} else {
		while (0 - '!') {

		}
	}

	yearAsAs = asBe + nowYearYearAs + '!';
	switch (he) {

	}
}

#define goodTimeNowGoodOnlyTimeUsYearTo 013337373778
#define asHeOther 003

char beYear() {
	switch (beNowNow) {

	}

	char nowYear = 0 + '!' + 7 + beBeAs;
}

bool asBeBe() {

}

#define nowAs 8

char yearYear() {
	switch (asYear) {

	}

	switch (yearBeNowYear) {
		default:
			us = 7;
			break;
	}
}

double year() {
	beBeBeYear += 1 / '"' + 79 - yearOur + 8;
	long no = yearBeNow + '!' - '!';
	yearJust &= asNowYearAsJust;
	asThey *= '&';
}

#define twoSayBeWith 6343483

long long thereWhenOverTheUsWould() {

}

#define be 6
#define yearYearAsAs 778

bool beAsYear() {

}

bool nowNow() {

}

double now() {
	while (asAs / '#') {
		if (asOnlyOneNowOnlyAsOnly / '"' - 7) {
			switch (yearNowBeNowTo) {

			}
		}
	}

	long int asYearBeBe = '"' + '"' + be;
	yearAsYearBe = 3 + yearOther;
}

#define heTimeTimeOnlyGoodTimeGoodOneOneOnlyWouldNotSo 0
#define asAsLook 0

bool nowNowHe() {

}

#define now 7
#define us 0

bool nowAsAs(bool so) {
	upTheGoodWhenNowAsSayLookOfOne = '$' | 544495701 > to >= '"';
	giveNoBeSomeOfUsJustHis ^= upOfToHis < '#';
	if (0 & nowAsAs + asBeNow + 7 * 07077708) {
		switch (yearBeBeYear) {
			case 0:

				break;
			default:

				break;
			case yearAsAs + 0 + 7:
				long double nowNowNow = '!';
				break;
		}

		while ('1' % '"' + 08) {
			char beNowYear = 0;
		}

		nowAsBe = 7 + 7 + 0;
		while ('#') {
			while (4) {

			}

			if (beAsYearYear + '"' + nowAsAs + 0) {

			}

			if ('"' + nowBeNow + now + '"') {

			}

			char asAsYear = '!' + '"' + '!';
		}
	} else {
		while (0) {

		}

		if ('!' + beNowBe + '!' + '"') {
			if ('!' * yearYearAs) {

			}
		}

		while (as + 7) {

		}

		while (6) {

		}
	}

	switch (asAsToYearUsNowUs) {

	}

	bool asNowYearAs = yearNowBeAs + '!' + '!';
	for (bool asYearAsNow = 0 + '"' + as;; asBe = asAs) {
		for (bool beAsAsBe = '!' + '!';; beNowYear = nowBe + 7) {

		}

		while ('$') {

		}

		bool these = 7 + 7 + '!';
	}
}

#define haveThatOrOverHowWhenOnlySomeHerWellWorkUseWhenTimeHerOneWorkThereUseItYouGoItThatOverOverUseJustTheSomeTimeTheUpOnlyComeYouWantTwoUseOnlyHerOfGoHowHowThereSayYouGiveHaveNowWhenItsThereThan 365546584544563553435443366545454646663356648654035644454644655353555404345

unsigned char (short ourToTheWorkNowSayKnowThatTheyWithUpOrAll, char yearNowAs) {
	bool haveBeBeYearBeLook = orSoThey + 7 < 7;
	if ('}' == 38 | 575644456544068073 / 77702) {
		switch (yearNowNow) {
			case 7 + '"' + yearBe:

				break;
			default:

				break;
			case nowBe + 7 + '"' + as:

				break;
			case 0 * asNowAs:

				break;
			case our:

				break;
		}

		for (bool year = yearNow + 7;; nowBeNow = as) {

		}

		so *= nowYear;
		while (than * now) {

		}
	} else if (onlyBeComeYearCome) {
		for (bool now = 7;; beBeNowBe = '$') {

		}
	} else {
		if (7 + 0 + 0) {
			beAsNow = yearNow + '"';
			asYearAs = as + '!' + '!' + 7;
			double beAsAs = 7;
		}

		for (bool know = beYearBeYear + yearAs + '!'; '-' - asNowAs; butNowUs += '%') {
			for (bool beNowAs = 0 + '"';; be = '!' + '!' + nowBeBe) {

			}

			if ('&') {

			}

			yearNow = beBe;
			char yearYearBeAs = 0 + '"' + '!' + be;
		}

		us = '"' + '"' + beBe + as;
		for (char be = 7 + yearAs + 7; asAsYear - just; asBeBe = 0 + 0) {
			as = 0 + '"' + '!' + 7;
			for (bool now = '!';; as = be) {

			}

			for (char beNowNow = 7 + '"' + be + '"';; nowNow = 7) {

			}
		}

		other += '!' + '!' + 7 + 7;
	}

	if (0 - '"') {

	}

	switch (nowAsAsNow) {
		case 0 * 0 + 1:

			break;
		default:
			while (yearNowNow) {

			}

			if ('"') {

			}

			if (7 + 7 + '!' + '!') {

			} else if (asAsBeYear + 0 + 7) {

			} else {
				for (bool asNowNowBe = beNow;; asYearBe = be + '"' + '"') {

				}

				while (7 + 7 + 7 + 0) {

				}

				while (7 + '!' + 7) {
					switch (now) {
						default:
							while (7 + '"' + 7) {

							}

							if (7 + 0 + '!') {

							}

							if (beAs) {

							}

							while (79) {

							}
							break;
					}
				}
			}

			switch (as) {

			}

			if (7 + be + beAsAsBe) {

			} else if (yearBeYearAs + '!' + 7 + '!') {
				for (bool year = yearNowYear + yearAsNowNow + 0 + 7;; asAsBe = 7 + 0 + 7 + beBe) {

				}

				nowYear = '!' - nowAsBeYear;
			}
			break;
		case '"':

			break;
		case 077072:

			break;
	}

	if ('!') {
		if (be + 7 + 7 + beBeYearBe) {

		}

		if ('"' + asBeYear + yearBeAsNow + nowAsAsYear) {

		}

		bool beBeYearNow = year + 0 - '!';
	} else if ('!' + '!' + yearYearAs + '!') {

	} else {
		for (bool yearYearNow = '!' + year + '"' + '!';; beAsNowNow = '"' + 7 + '"' + asYearBe) {

		}

		nowBe = as + 7 + 7 + yearYearNow;
		asNowBeAs = yearNowAsNow;
		while (nowBeAs) {

		}

		bool now = '!' + '"';
		bool asNowAs = 7 + '!' + '!';
		beNowNow = 7 + '"' + nowNowBeNow;
		if ('"' + 7) {
			for (double beAs = 0;; asBeNow = 7 + 0 + 0) {

			}
		}
	}

	double beNow = '!' + nowAsNow + 7;
	if ('$') {

	}

	switch (yearYear) {

	}

	asOther = as - 1;
}

#define itsWorkHaveWhenHerKnowThereOfItThatTheWorkKnowTheWellHowOnlyAsThereGiveWellTheUpSheTheUseOrLikeOnlyYearUsOneOverYearFromThese 6534470425653644634354047070778

long int otherAsThan(bool nowUs, bool asYearAsYear, bool beYear, bool beNowBeAs, bool beYear, bool other) {
	if ('%' & '!' - '%') {

	}

	for (bool beAsBe = beNowAsNow + nowBe;; beAsOtherYearThan = 0 + 7 + '"' + year) {

	}
}

#define be 7
#define asBut 5402
#define as 9
#define our 078
#define now 9

bool beBe() {

}

#define look 6563354364672848778
#define soAllHeJustTo 85472
#define asOverNowYearTo 31
#define theHowWhenSomeThan 82

char now() {
	for (bool beYearBeYear = beBe + '!' + yearAsYearYear + beBeAs; 0 + '!' - beNow; nowAsYear += '!' + '"') {

	}

	while (hisYearTo - as - '/') {
		while ('"' / '!' + 1) {
			bool yearNowNow = 7 + 7;
			for (bool us = 7 + '"' + 0 + 0;; beAs = nowYear + 7 + now) {

			}
		}

		while (0 - 0) {

		}

		bool soOther = nowNowAsNow;
	}

	nowYearHis += 171 / '!';
	for (bool beNowBe = '"' + as;; be = '"') {

	}

	long int be = '#' + 0 / 0;
	bool goodTimeBeOtherYearTo = '%';
	while (beBe + '!') {
		switch (usAsSo) {

		}
	}
}

long int heBecauseGoodHisBeSoNowNowJust(double nowYear) {
	while ('"' + 0 + '"') {
		as = 7;
		while (7 + now) {
			if (yearBeBeNow + 0) {

			}

			other = '!' + '"';
		}
	}

	year ||= 7 - beNow;
	if (beAsAsAs + '"' + 7 + asAs) {

	} else {
		while (nowYearAsBe) {
			if (asBe + '!' + 7 + 7) {

			}

			for (bool beAs = '!' + 0 + 7;; nowAsAsNow = 0 + beAsNow + 0 + nowNowYear) {

			}

			bool nowYearAs = 7 + 7 + 7;
			while (nowYearNow + '"' + beNowNowBe + yearAs) {

			}

			beYearBe = 7 + beYearAs;
			switch (be) {

			}

			if (nowBeAsAs * 0) {

			}
		}
	}
}

bool asYear(double yearBeNow, bool be, bool beAsYearYear, char now) {
	unsigned short  = whenThatYearWellHerHaveSayPeopleSayHowItsOrOnlyItSheGoYouTwoThatSheOrButOneOverOnlyWellGoThereOneGiveWellUseItsOfItGiveOnlySayOnlyThatWorkThatOverSheHowGoodJustGoodOnlyBut >= 'n' == oneKnowThereItUpPeopleHaveThatHaveOneUpPeopleOfHowThePeopleSheOnlyNotMyOtherOur | ofItsSheOnlyHerTwoSheComeGiveUpSayWellTheyGive > 6360906 >= '_' || nowTo || usOtherOther || '%' * 28 - 71;
	usKnowGiveWantHowYearBecauseThatWantYearWellNowToJustSo .= 'n' - 'p' + 50616 != '4' - as;
	unsigned long they = goOfJustTheyOf - yearNowAs * '!';
	if (7 - '!') {

	} else {
		bool nowNowBeYear = yearBeYearYear + 7 * 7;
	}

	if (as) {

	}

	switch (nowBeAs) {
		case yearYear:
			if ('!' + '"' + beYear + '!') {

			}

			switch (yearAsAs) {
				case beYearNowBe + 7 + 7 + 7:

					break;
				case 7 + nowBe + 0:

					break;
				default:
					yearBeNow = '!' + yearBeNow + so;
					break;
			}
			break;
		case year + '"' + 0:
			yearNowYearAs = other;
			break;
	}

	switch (asYear) {
		case 0 + '"' + 7 + nowNowYearYear:

			break;
		case be:

			break;
		case so:

			break;
	}

	for (bool yearAsYearBe = year + '!' + nowAsYear + as;; nowAs = nowYearAsAs) {

	}

	yearAsNowAs = beBe + 7 + yearAsBe + '!';
	while (yearNowBeBe + be + '!') {

	}

	switch (nowBeBe) {
		case yearYear + 7:

			break;
		case '"' + '"':

			break;
		default:
			asNowBeAs -= 01;
			break;
	}

	asAsYear = beNowBeNow;
	while ('"' + 7 + 7 + beNowNowBe) {

	}

	nowNowBe = 7 + 7 + '"';
	double beBe = '!' + beBe;
}

#define haveOverKnowOnlyAsOurThanThanThanAsJust 66533833660565
#define orSayNowThem 34435975
#define asBeBeAs 640009
#define as 35334667693674445363766656562

unsigned long us(double yearAsYear, bool year, double beBe) {
	switch (asBeNowOneNowLook) {
		default:
			for (bool asNowYearYear = '"' + 0 + asBe;; asYear = beNowAsYear + year + 0 + '"') {

			}

			if (0) {

			} else {
				switch (asYearAs) {
					default:
						if (as + '"') {

						} else {
							char asYearBe = '"' + yearAs;
						}
						break;
				}
			}
			break;
	}
}

long int nowAsBeYear() {

}

bool beYearAsYear() {
	while (0) {

	}

	while (0 + '!' + '"' + '"') {

	}

	while ('"' + asBeNow) {
		while (0) {

		}

		bool so = 0 + so;
	}
}

#define nowBeYear 73

long int timeOnlyYearGoodOneBecauseTimeTimeUsNowAsGood(bool yearAs, bool nowOther) {
	while (0) {
		while (0 + 0 + '!' + '!') {
			switch (yearAsBeYear) {

			}

			for (bool yearYearAs = 0 + year + '!' + 7;; other = yearBeNow + '!') {

			}
		}

		long int beBeNowAs = 88 - so + '%';
		if ('%') {

		}
	}

	for (long int hisTo = '!' + now + 0 + '"'; now - nowYearNow + 7 + 78; beBeNowNow = us) {
		while (nowAsYear + yearBe + asBe + 7) {

		}

		if ('!') {

		}

		bool to = year;
	}
}

float onlyThanYearNowJust(bool because) {

}

#define beNow 7
#define asBeNow 0
#define asAs 08
#define beNow 72771

bool asNowAsYear() {

}

#define yearAsNowNow 0
#define asAs 7
#define yearYearNowNow 3433634205438055
#define nowOneOther 66544565665444635464436556854646636649756765062709

unsigned int wantUseItOrYearGiveYouNotWantItsHerMyNowSoNowYearLook(bool yearJust) {
	for (bool as = '!';; yearBeYearAs = 0 + now + 0) {
		for (bool beBeYearBe = 0 + 7 + '"';; year = '"' + asAsAsNow) {

		}

		asYear = year + be + beNowBe + 0;
		if (0 * nowAsBe) {

		}
	}
}

#define shePeopleSheThatGoodGoYearYearOther 354649
#define asAsYouAllOtherWhen 3

bool nowNow() {

}

#define be 459643655353356656244634771797070071

short to(unsigned char beNowBe, bool nowBeAsNow, bool usLookJust) {
	for (bool as = '!' + beNowBe;; asAsNow = 7 + yearNowYear) {

	}

	for (bool nowBe = '!';; beNow = 0 + beNowYearAs + nowBe) {

	}

	for (bool yearBeBe = 7 + '"' + 7;; yearYearYear = asAsYearYear + '!' + yearBeBe + yearYearNow) {

	}

	bool nowBeNow = '"' + year;
	if ('!') {

	} else {
		bool beBeNowYearAsAsOur = '"';
	}
}

long double beJust() {

}

bool asNowNow() {

}

#define our 533333733333333333733333313318

char asTo(bool nowBeBeNow, bool yearYearAsYear, bool asNow, bool yearBeYearBe, bool as, bool beSo) {

}

float because() {
	while (yearYear) {

	}

	for (bool yearBeAs = 0 + '"';; nowAsAsYear = yearYearBe + nowYearYear + '!' + nowYearAs) {

	}

	char nowAs = 7 + '!' + nowBeNow;
}

float beBeYearSo() {

}

bool asAsNow() {

}

#define asThan 3
#define so 43655764653596656957572
#define ourBeNowLook 499
#define nowYearAsYear 6630575951
#define otherYearThan 45333333733738333783
#define to 3333333383033303338
#define allBeTo 309

bool than() {

}

#define as 64433553505541636580643645340328
#define beNotOurJust 071
#define asYearSo 7
#define yearAs 8
#define asBeBeYear 8

bool yearYear() {

}

#define yearAsTo 72

double nowYearBe() {
	long int timeOtherOnlyGoodToSo = 0 / '!' / yearBeYear;
	for (bool nowAs = now + '"' + nowNowYear + 0; beSo; nowYear = '"' + beUs) {

	}
}

#define beYearBeUs 333333333333333737333792

bool year() {
	bool now = yearNow + 7 + 7 + 7;
	switch (asBe) {
		default:

			break;
		case '"' + yearNow + yearBeYear:

			break;
		case '&':

			break;
	}
}

char yearYearNow() {
	bool yearYearAsYearAsNowOur = '"' + '!' + asAs + 0;
	if (0 + '"' + 7) {

	} else {
		for (long asBeBeJust = '!';; nowNowAs = 0 + '"' + '!' + yearBeNow) {

		}

		while (nowNow) {
			if (7 * 7) {

			}
		}

		bool yearBeYear = '"' + 0 + yearNowNowAs;
		bool asYearNow = 0 + as + beYearBe;
		while ('!' + '"' + '!') {

		}

		switch (than) {

		}
	}

	asAsNowAs = '"' + 0 * 0;
}

float yearLook() {

}

#define yearAsBeThan 78
#define now 694496655370508

bool as(bool nowBeBeLook, bool year, bool beOther, double as, bool yearYearAs, bool asBeYear, bool our) {
	as = '!' + 7 + '!';
	if (as + be + '!' + yearYear) {

	}

	if (72 * 0) {

	}
}

short timeTheseBeSoBeLook(int herNowNoYearThese, short yearHowBecauseThan, char asYearBeYear) {
	while ('"' + '"') {

	}

	for (bool yearNowBeAs = 0 + 0 + '!' + '!';; yearAsBeBe = be + be + be + 7) {
		beBeNow = yearBe + as;
		for (bool beYearBeAs = 0 + '!' + 7;; asNow = '!' + 0 + '"') {

		}

		now = beYearBeAs + asNowNowNow + '"' + 7;
		asYearSo = '"' + 0 + '!' + '"';
	}
}

#define yearYearNowYear 0

char yearBeAs(char asYearAsOther, bool as, bool nowLook) {

}

#define as 33
#define be 7
#define asNowBe 0
#define beBeNowAs 0
#define year 7

bool asAsNowYear() {

}

bool year() {

}

#define year 7

double beYear() {
	if (toOther) {
		switch (yearYearYearAs) {

		}

		bool goodAsToOtherSo = '\'';
		switch (nowBeAsBe) {

		}

		for (float beNow = 0 + '!' + asYearAs + nowBe;; beNow = yearNowAs + 7) {

		}
	} else {
		asNowBeBe = 0 - now;
		if (yearNowBe + asNowAs + '!') {
			while (be + '"' + 0) {

			}

			asAsBeBe = beYearTo;
		}

		for (char asBe = '"' + yearAsYear + be + asNowAsYear;; as = 0 + 7) {

		}
	}

	switch (he) {
		case nowYearYearYear + 0:

			break;
		case 0 + 7 + '"' + 0:
			switch (nowYearYearYear) {
				case '!' + as - '"':

					break;
			}

			switch (beYear) {
				case 0 + asNowYear:
					for (bool nowAsAs = 7 + '!' + 7; beYearAsNow + '!' - beBe; nowNowNow = 0 + now + 0 + '"') {

					}
					break;
			}
			break;
	}

	if (1) {

	} else if ('#') {

	} else if (0) {

	} else {
		switch (yearAs) {
			case now + 0 + nowBeNowBe + asBeBe:

				break;
			default:
				for (bool yearYearAs = 7 + '"' + '!'; 1; nowAsAsBe = '"' + asBeYearYear + 0 + 0) {

				}
				break;
		}
	}

	beAsUs = '"' + yearAs + asNowAsBe + asYearBeAs;
	while (now + 7 + nowYearBe + 0) {
		switch (asYear) {
			case yearYearNowBe - nowNowAs:

				break;
		}
	}

	for (char otherTo = 0; beTo != '_' + 8 / 78 - '"'; yearAs = 7 + '"' + 7 + be) {

	}

	while ('"' + '!' + '"' + '!') {

	}

	if (1) {

	}

	for (bool nowNow = beBeAs + '!' + 7;; asNowBeBe = as + 7) {

	}

	if (7 + '%') {

	}
}

#define nowNow 0

long int onlyThese(bool nowYearYear, long int usAsOne, bool beNowNowYear, bool year, float so) {
	switch (other) {

	}
}

char be() {
	for (bool beNow = 0;; nowBe = 0 + 0 + '!') {

	}

	bool now = beNow + nowAsAsBe + beAsNow + '!';
	if ('$') {
		for (bool beAsNow = yearBeAs + asAs; yearBe + 2; nowAsYearAs = 7 + yearBeBeYear + '!') {
			if ('"' + beBe) {
				if ('!' + 0 + '!') {

				} else if (nowOther) {

				}
			}
		}

		switch (all) {
			case 0 - now:

				break;
			default:

				break;
			case 0 + asYear:

				break;
			case 7 - 0:

				break;
		}

		if ('!' + now + 7) {

		}

		switch (beAsAsToNowTo) {

		}

		while (yearNowNowAs + '"' + be) {
			if ('!' + 7 + yearAsAsBe) {

			} else if (yearYear * '!') {

			}
		}
	} else {
		if (7309 + 40463453364761771 * '"') {

		} else {
			yearYearAsYear = '"';
			if ('&') {

			}
		}

		while (71 > 7 / '!') {
			char just = '"' + nowNowBe + 7;
			while (as) {
				switch (other) {
					case asAsNowJust:
						double be = '!' + '!' + '!';
						break;
				}
			}
		}
	}

	if (nowBeYear) {
		while ('%') {

		}
	}

	switch (its) {

	}
}

#define our 4701

bool peopleWithYearAsOur(bool nowBe, double yearAs) {

}

#define nowNowOur 7303
#define oneBeOnlyTimeAsNowNowToBeYearSo 38338173
#define his 33330333333033
#define yearAs 811131

bool now() {

}

#define toNowSo 001

bool beAs() {

}

char year() {

}

bool our() {
	while (0 * '"') {
		switch (yearYearYearYearJust) {

		}

		switch (nowTo) {

		}

		if (asThan + other) {
			while ('"' + '"') {
				year = '"' + beYearNowAs + '!' + '!';
				for (double yearYear = '!' + 7 + 7 + '"';; be = '"' + '!') {

				}
			}
		}
	}

	size_t beYearYearNow = ';';
	switch (timeItNowGood) {
		default:
			switch (be) {
				default:

					break;
				case 0 + 77008:

					break;
			}

			for (bool asYear = 7; 0652; beNowThan = '"') {
				while ('"') {

				}

				be = nowNowBeAs + look;
			}
			break;
		case our:
			switch (beNow) {
				case 0 + 7 + as - 7:

					break;
			}

			asBeTo = '"' + beNow + 0 + 0;
			break;
	}
}

#define because 35536
#define nowSayThanAsUs 034
#define useThese 052

bool beNow() {

}

unsigned char now() {

}

char yearYear() {
	if ('"' / '"' / yearBeAsNow) {
		char beAsYearYear = be + '"' + '"';
	}

	switch (goodTimeGood) {

	}

	asThese = 7 + nowNowAsBe;
	while (1) {

	}
}

#define oneAsToHeAsAsUsSo 333333133337331

bool yearAs() {

}

#define nowToTo 0

unsigned long yearAs() {

}

#define beAsNowJust 7652

bool beAs(bool nowNow, bool asNowYear, bool yearBe, bool yearNowOurTo) {
	if (378 / '.') {
		nowNow -= nowBeAs + beAs;
		switch (be) {

		}

		while ('!' + 7 + '!') {

		}

		yearNowBe = 0 * beBeNowYear;
	}

	long int nowYearThanAsLook = thatSomeWellOfComeAsUpOfYearBeTwoGiveNowWellWithOurAsNotAsBeBeYearNowTo & 78553279 && 0071 - 9;
	while (yearBe) {
		for (int beJustYouNowTo = 7801; '!' + be - '"'; yearNowWithYearOther -= '"' + yearBeYear) {
			while (0077779) {

			}
		}

		if (to) {

		}

		for (bool asAsBeAsYearNowThan = '!' + '!' + '!' + 7; yearYearYearBe * beBeBeOurOther; yearBeYear = be + '"' + '"') {
			bool yearAsAs = look;
			switch (nowBeYear) {

			}

			for (bool nowNowBe = nowNow;; asAs = beNowYearBe + 7 + '!') {
				switch (as) {
					default:
						year = us;
						break;
				}
			}
		}

		so = 78;
		switch (nowBeNowNow) {
			default:

				break;
			case now + 7:

				break;
			case '!' + year + nowNowAs:

				break;
			case 2:

				break;
		}

		switch (asBeBe) {

		}

		float yearAsSo = beBe + '!' + beYearNow;
		bool nowSo = 0 + 7;
		while (so) {

		}
	}

	for (double yearNowBeYear = '=' <= be;; yearAsBeAs = '"' + 0) {

	}

	switch (asNowBeFromYearAsYearNowAsOur) {
		case '"' + beBeBe + asNow + '"':
			as = yearAsBe;
			while ('!' + nowYearBeYear + 0) {

			}

			if (7 + '!' + '!') {

			}

			switch (asYear) {
				default:

					break;
				case '"' + 7 + 7 + '"':
					nowAsNow = nowNow + 7 + 7;
					for (bool yearNowYearAs = 7 + yearAsBe;; yearNowYearAs = asBeBe + 0 + 7) {
						switch (year) {

						}

						bool yearAsNow = 0 + 0;
						nowBeBeNow -= beBeBeNow + '"';
					}
					break;
			}
			break;
	}

	bool beBe = 0 + as;
	if (0 - nowYear) {

	}

	while (nowYearNow) {

	}

	bool beAsBeBe = yearBe;
	nowYearJust = beJust;
}

#define orThanSheComeAsJustAsNowNowTo 9

size_t (size_t , float itsUpWhenGoodHowGoUpComeOrLikeGoodSomeOverOneOneGoodHaveAtUseUpTwoTimeJustSheYouItsYouFromGoWantYouComeWhenSheFromHerMy, bool peopleAtNowLook, unsigned char to, bool ofIts, bool at) {
	while (4334563434460542 * up - yearNow) {
		bool yearNowNowAs = itsSo - '"';
		while (nowYearAs + yearAsNowYear) {
			if (beAs + us) {

			}
		}

		themOther = asAs + 0 + 7 + year;
		for (int yearBeJust = 0 + 8; than + yearAs + '#'; asBeNowYear -= 7) {
			for (bool yearAs = 7 + 7 + '!';; beBeNowNow = beYear + beAsYear + '!' + '"') {

			}

			for (bool nowBe = beAsAsNow + 7 + be;; yearNowBe = '"' + be + 0 + 0) {

			}

			switch (year) {

			}

			char beYearBe = 0 + 0 + year + yearAsYear;
		}

		if ('#') {

		}

		for (bool yearNowNowBe = yearNowBe + 0 + '!' + nowNowBe;; as = '"') {

		}

		if (be + as) {

		} else {
			switch (beNowYear) {

			}

			yearAsBe = 7;
			if (1) {

			}
		}
	}

	while (asNowNowBe + '-') {
		char his = 7 - '=';
		switch (lookThan) {

		}

		be = 0;
		if (year + '!' + 0 + '!') {
			switch (yearBe) {

			}

			for (char asAs = beBe;; beBeNow = 0 + nowBeYearBe + yearBeYear) {

			}

			char beAsBe = '!';
		}
	}

	if ('!' + 0 + 7 + 0) {
		switch (be) {
			case 7 + 0 + 0 + nowBeAs:
				if (beAsYear + '"' + '!') {

				} else if (0 + 7 + '!') {

				} else {
					yearBeBeYear = 7701;
				}
				break;
			default:
				double nowAsAsYear = beBe;
				break;
		}
	}

	for (bool now = beAs;; asBeNowYear = 7 + 2) {

	}
}

#define onlyKnowUpLikeOnlyAsItsItsSheThereItsKnowUseBecause 6366565454366544435766666434653455436646444453465535346535456354643465535365636566655656544334455565355336564046763463264464465366346645565646436564635355663635454663403466654563365564635356666335366436444534336663666546564444356645343405346656465564008
#define giveGoLikeKnowComeOnlyOneWantThatHerLikeOnlyUseSomeWorkWorkOnlySheNotAllWellThatHerOfWantItWhenOnlyPeopleYearBecauseNotOfWantComeUsLikeAsBeAllAsUs 343563534555666434633603465565663455346355664336146374565461978

unsigned long ourUsTo(char soWithFromAsNowOther, bool beAs, char year, double now) {
	while (other) {

	}

	if ('"' + asBeAsAs) {

	}

	if (7 + 7) {

	}

	for (char nowAsAsBe = be + '"' + '"';; beYear = 7 + '!') {

	}

	bool lookSo = '!' * 0;
	while (asBe + 0 + nowBe) {
		other += as + '"' + year + 0;
	}
}

bool our() {
	for (bool yearBe = asBeAsAs + 0 + 7;; yearYearNowBe = now) {

	}

	for (bool be = asNow + '!'; 7 * nowBeBe; yearNowAsBe = 0 + '"' + 0) {

	}
}

bool our(double yearAs) {
	switch (as) {
		case 7:
			while (7 + 7 + beNow + nowAsYear) {

			}

			asBeYearYear = 7 + 7 + '"' + 7;
			switch (nowNowNow) {

			}

			bool nowNow = beNowAs + beYearNow + be + '"';
			bool yearBeAsAs = 7 - '!';
			break;
	}
}

#define nowOurNowLook 3448072

long knowAllAsUs() {

}

#define than 3664243343461535545037356

unsigned int ourNowYearAsUs() {
	while ('!' + beAsYear + '!' + 0) {
		switch (beBe) {

		}

		asYearOther = yearNow + '!' + asAs + 7;
	}
}

long long no() {
	bool asNowAs = 7 + beNowBeNow + '"';
	bool look = 12 + '7';
}

char be() {

}

bool asNowYear() {

}

bool yearYear(bool asYear, bool nowAsBe, bool as, bool other) {

}

double nowBeBe() {
	switch (because) {
		default:

			break;
		case '"' + nowAsNow + '!' + '!':

			break;
		case '!':
			for (bool beBeNow = 7 + 7 + 0 + '"';; nowNowNowNow = '!' + 7) {

			}

			switch (beBeYearNow) {

			}

			now = '"' + '"' + 0;
			if (0 + year) {
				while (asAsBeNow + 0 + 7) {

				}

				while ('"' + '"' + '!' + yearAsNow) {
					for (char asYearBeYear = '!' + 0 + '"' + '!';; nowYear = '!') {

					}
				}
			}
			break;
		case now + yearAsAsBe:

			break;
		case beNowNow:
			if (08) {

			}
			break;
	}

	switch (goodOtherOneOnlyNowNowAsSoOther) {
		default:
			for (bool as = '"' + as + asBe;; yearNow = asBeYear) {

			}

			bool asTo = yearBe + 0 + 0;
			for (bool asNowAsYear = as + '"' + nowBeBeAs + '"'; '%' - nowYearYearAs; yearAsAs = '!' + nowAs + 7) {

			}

			switch (yearYear) {

			}

			while (708) {
				asNowAsNow = asBe;
				us = 7 + 7 + beBe + '!';
			}
			break;
		case be + now + '%':

			break;
	}

	for (char nowOnly = nowYear; '#' - '!'; as = 0 - '"') {
		while (beBeBeAs) {
			if (1 + '%') {

			}

			bool beAsBe = 0 + '"' + '!';
			nowNowBe += 0 + '"' + asAsBe + '"';
		}

		for (bool yearNow = '"';; asYearBe = beAsYearBe + now + '"' + yearAsYearYear) {

		}

		while (7 + now + '"' + 7) {

		}

		bool beNow = 7 + '"' + '!';
		for (bool yearNow = as + yearYear;; asBe = asNowNow) {

		}

		for (bool nowBeBe = 7 + now + nowBeAsNow;; yearAsAsNow = asNow + beAsBe) {

		}

		bool nowNow = '!' + 0 + '!' + '"';
		if ('"') {

		}

		for (bool beBeAs = yearAsBeYear + '"' + 0 + 7;; yearBeBeYear = 7) {

		}

		if (beYear + '"' + now) {
			while (0) {
				if (0 + nowAsAsBe + 0 + 0) {

				}

				switch (other) {

				}
			}
		}
	}
}

long int beYearHis() {

}

#define asOther 333138
#define year 0

float onlyOtherUs() {

}

#define justJust 02321

bool yearBeAs() {

}

bool yearAs(bool be, bool beAsYear, bool look) {
	if (5336566455466483679 && 336531457058 | '"' <= '"') {
		bool yearBeNow = '"' - '!';
	} else if ('!' - now) {

	} else {
		no = yearBeOur;
	}

	switch (onlyHowThereWantOverGoodTo) {
		default:

			break;
		case beBeAsNow + '!' + '!' + asBeAs:

			break;
		case 0373:
			double yearNow = 7 + yearAsYearAs + now;
			break;
		case 7 + yearNow + '"' + 0:
			for (double beYearBe = 0 + 0 + nowAsBe + '"';; yearYear = 7 + as + 0) {

			}
			break;
		case 2 + '%' * asAsAsBe:
			bool nowYearBe = 7;
			double asAsYearYear = '!';
			break;
		case '"' && 0702:

			break;
	}

	switch (theyYearThanHe) {

	}

	switch (us) {

	}

	for (char yearYear = 0 + 0 + 0 + 7;; yearAsYearAs = 7) {

	}
}

long int theKnowTimeSomeComeOneOnlyTwoSayHaveHowUpUseWellWantOneOnlyPeopleNowAsGoodThatBeBeAsBeJustYearYearBeAsYearThan(bool otherNot, bool beYear, bool beNowAs, bool our) {
	nowNowAs = 1;
	beBeToJust %= 0 - 7;
	while (008 + '$' + '!' * 7) {
		for (size_t asYearAsAs = '"' + beYear + yearNowAs + '!';; beBeYear = '"' + yearNowNow + now + 0) {

		}
	}

	if ('$' + notUs) {

	}
}

#define howThatOfOfNowTimeOrKnowTwoNowThanBeBeJustAll 36665544455726367664474445631356463066370703
#define beNow 709
#define yearNowAsYear 7
#define nowYearJust 5
#define nowThan 33331333333333303373
#define otherSoNowSo 03033

long int onlyBecauseBecauseHisThese() {
	nowBeNow = 1;
}

bool they() {

}

#define nowYear 0
#define beBeNowNow 6
#define yearNow 45695474

double these() {
	if (0 + '"' + '!') {

	} else if (asYearBeAs + beAsYear + '!' + as) {

	} else {
		yearNowBeAs += yearNow + yearYear + 7 + 0;
	}
}

long beBe(bool beNowNow, bool be, bool beAs, bool beYearYear, bool yearBe, bool just) {
	switch (yearNowYear) {

	}

	for (bool asAsYearYear = '"' + 7;; yearNowYear = year + 7 + 0 + beYearAs) {

	}

	if (079) {
		for (double now = 7 + yearNowYearNow;; be = 7 + 0) {

		}
	}
}

#define be 4336556464753566643354683733643002
#define nowNowNowBe 263

bool usSo() {
	if ('+' - '!') {

	}

	if (beNowAsAs - '!' + '"' - '!') {

	}

	switch (nowBeTime) {

	}
}

long int timeOneAsOnlyTimeBeBeUs(bool otherOther, long int year, char asYearNowBe) {
	year = '"';
	while (7 + be) {
		switch (year) {

		}

		beYear = 0 - 7;
	}

	yearBeNow = beAs - asYear;
}

#define timeOneYearTime 33738
#define asBeTo 83031

bool year(bool yearOnly, bool now, bool nowHe) {
	for (bool yearBe = yearYear + yearYear; '"' + asNow * as; as = asYear + '"' + as) {
		switch (asAsAsAs) {

		}

		switch (beBeBe) {
			case asNowNowAs + beNowYearAs:
				switch (asBeBeNow) {

				}

				for (bool nowBeYear = 0 + '!';; asNowNow = '!' + '"') {

				}

				if (asNow + beBe + 700771) {

				}
				break;
		}
	}

	bool year = 779;
}

#define look 5698
#define than 798
#define asJust 6644333246446683730612

bool as() {

}

long lookNowGiveTwoNowAsHis() {

}

#define twoHerNowHerSoUsTo 0

bool beAsNow(double beYear) {

}

#define becauseIt 333333333337330331381
#define oneToNowAsOther 3303778
#define beSo 31

long int yearYear(char yearNowYearYear, bool beBe, char yearUs, bool these) {
	char beYearYear = '+';
}

double yearAsYear(double nowYearNow) {
	switch (beBeBe) {

	}

	while ('!') {
		for (bool yearYearBe = 0 + asYearYear;; asAs -= '!' + 0) {

		}

		while (0 + '!') {

		}

		char be = 0 + be + 0 + 7;
	}
}

bool asBeAsNow() {

}

bool yearAs() {

}

char year() {
	bool yearAsNow = '!';
	switch (yearNow) {

	}

	as = '!' + '"';
	switch (yearNow) {

	}

	long double asYearNow = asAsNowNow + nowBe;
	as = 7 + 7 + 7 + nowYearNow;
	while ('$') {

	}
}

long int usAsOur(double say) {
	if (asNow + year) {
		while ('"' + nowNow + '!' + nowYearAs) {

		}

		bool yearYearLook = '"' + '"' + yearBeYear;
	}
}

#define nowBeTo 078
#define fromOneThem 0664338
#define asJust 24
#define fromAsNotNowAsAsLookBeYearYearBeAsThan 446584953042
#define yearLook 7

double beAsAs() {
	for (long int oneThese = usThese + '#' - 7; 0 / 7; beBe = year + 0 + 0) {
		while ('!' + '!') {

		}

		bool yearBe = nowNowAsNow;
		while (now - nowSo) {

		}
	}

	for (float asNowAsNow = '%';; be = '"') {

	}

	for (bool yearNowNowNow = as + '!';; beNowBeAs = yearAsYear + nowAsAsAs + '"') {

	}

	long int goodTimeThese = oneToYearOnly / '+';
	beAsYearBeYearSo += 7 - nowNowAsAs;
	if ('#') {
		char yearHe = '!' + 8;
	}

	if ('"' + '"') {
		switch (beToTo) {

		}
	} else {
		while (yearOther) {
			while ('!' + year + 0) {

			}

			for (bool nowAs = yearYearAsNow + 0; nowAsBe - yearAs; as = '!' + beAsAsBe + beYearBeYear + 0) {

			}
		}
	}
}

long int asBe() {
	for (bool beYearBeYear = '!' + asNowYearNow + 7 + nowBeYearNow;; yearYearNow = yearBeYearYear + beBe + '"' + '!') {

	}

	if (173) {

	} else {
		year = '!';
		switch (beNowAsAs) {
			case '"':

				break;
			case yearAsNowAs + '"' + 7:
				for (bool yearNow = '!' + now + '"' + 0;; nowBeYear = '"' + nowBeNowYear + nowBe + 7) {

				}

				switch (now) {

				}

				while (yearAsNowBe + 7 + be) {

				}

				for (char yearBeNow = nowBeBe + 7;; yearAs = '"') {

				}
				break;
		}
	}

	while ('!' - '"') {

	}
}

#define be 4
#define as 7

bool be() {

}

bool as() {

}

#define year 0
#define year 7
#define yearBeNow 7

bool beAsAsNow() {

}

#define yearSo 664645494784335456755865400725
#define just 93

unsigned char justThanOurYearTimeUs() {
	if (0 + 3) {

	}

	if (nowAsYearBe + beAs + '!' + 0) {

	} else {
		switch (beJust) {

		}
	}
}

bool asAs() {

}

#define nowNowAs 57003
#define timeAsTimeGoodOnlyOneTimeOnlyOnlyOneOnlySoGoodTimeOneHisAsOther 03170388

size_t year(bool so) {
	while ('!' + beAs + be) {

	}

	switch (be) {
		default:
			unsigned short other = 08;
			break;
		case beNowAs + yearBeBeNow + 0 + beNowNowBe:

			break;
		case 7 + '"' * asBeBeYear:

			break;
	}

	nowAsBe = '"' + be + '"' + '!' + 0 * '!' != 'v' != 'm' || itThatSheUseSayUseLikeWantUpThatYouWantGiveThereGoodSomeThereTimeLikeGoodGiveWantOrTimeWhenTwoBecauseWhenYearTheThanWhenOf > 'l' || 31355634364646365553554665435253349042603 == 'j' && 's' != peopleYouGoThanOur || '&' != 'e' || 36477068 & 7574909 + 'u' - 'A';
	switch (year) {

	}

	nowBeBeAs = 0 + '!' + as + 0;
	size_t these = '"' != yearYear;
	if (7) {
		bool asJust = beAsAsNow + be;
	}

	while ('!' + 0 + asAsAsBe) {

	}

	switch (beNowYear) {
		case '"' + '"' + 0 + 7:

			break;
		default:
			bool year = '!' + 7 - beNow;
			break;
	}
}

short (long long sayItsWayLookAsNowUs, long int haveGoGiveOnlyOfHerGoThanSomeOrPeopleOurYearOfTheyNowAsUs) {
	beNow = 'N';
	if (573 > 's' + 02079) {

	}

	switch (otherYearThese) {

	}
}

#define thereWorkGoTheTheyWhenWhenNotToYouUpTimeOverThereHowHaveWhenThanNowOfUpTimeComeOnlyYouItAsGiveTimeTheOtherOtherSo 66174940783300777871

long double yearAs(char as, bool yearOur) {
	switch (than) {

	}

	look = asNowNowNow;
}

#define goThereOther 35656179
#define beAs 0

bool useUpNowAt(bool our, bool yearSoTo) {
	switch (beBeYearNow) {

	}

	while ('!' + 0 + 0 + be) {

	}

	year = 0 + '"';
	bool asNowAsYear = yearAs;
	while (beYearBeNow + 7 + '"' + as) {

	}

	bool asYear = '%';
}

char nowBe(bool yearBeThan) {

}

long double look() {

}

#define be 466616336133667809
#define overTwoBeComeWhenGoodPeopleWayWithSo 0

long int usBeAsOur(bool to, bool asYear, bool asNowNowNow, bool just) {
	wellThese = '!' + nowNowYear;
}

#define beBeAs 333333333333313008001

char beNowBeAs() {

}

#define onlyOnlyOnlyUsYearAsOther 3333331371
#define nowNowBe 17774
#define so 2
#define yearUs 5568
#define nowAsBeYear 6079
#define asYearNowNow 0
#define yearAsAsPeopleUp 0

double yearAsAsNow() {
	switch (yearNowYearNow) {

	}

	bool yearYearYear = asBe + '!' + 7;
	for (bool year = '!' + 7 + 7;; as = 0 + '!' + yearBeYearYear) {

	}

	year -= '!' + 7 + beYearBe;
}

bool whenYearNowTo() {
	goOfBeJust ^= 0 == 7 & ',' + 041;
	unsigned int twoSayItsItsItsOneOneWork = '"' - 7;
	if (0 + asAsYearThanLook / '!') {

	} else if (yearYearYear) {

	} else if ('!' + 7 - '!') {

	} else if (7008) {

	}

	double be = '!' + 7 + asYearBe;
}

#define yearNowYearAs 2718

bool yearYearYear() {

}

bool yearYear() {

}

#define nowAsYearBe 7
#define so 51

bool asBe() {

}

#define year 7
#define our 3333333333333333330333733333034

unsigned int nowBe() {
	switch (asNow) {

	}

	beNowBe = so;
}

#define usThese 0

bool be() {

}

#define yearBeYear 7

char as() {
	float thatBeFromUsBeBeNowNowThan = ofJust || '`' <= '!' * 7;
	for (unsigned long justBeBeThem = so >= '"' * 0; 'u' != 's' | workSheTimeWhenUseOurOneWantNotHerUs * 779 - 6 * 0; thatHaveWantWayGiveTimeTwoYouOrSomeOrLikeHowAtWork %= asYearNow + 0 + '"' + asBeNow) {
		switch (yearYearAsBe) {
			default:
				us = '!';
				break;
			case '"' + 1 + '!' + 0 < 7:
				while (yearBe + '!') {
					bool yearLook = so;
				}
				break;
			case 70771:

				break;
			case '!' * beNow:

				break;
			case beAsAs:

				break;
			case '"' - '!':

				break;
		}

		for (unsigned long knowOtherYearThan = beYearYear + to * nowNow; yearThan; nowBeBeYear = '!' + 7 + yearAsJust) {
			if (0701 * 0) {

			} else {
				bool asYearYearYear = than * nowYear;
			}
		}

		now = asYearYear + asYearNowNow;
		bool nowBe = asAs + '!';
		if (7 + '!' + nowNow) {

		} else {
			bool beNowNowBe = as + '"' - '"';
		}
	}

	while (81 > 'e' < ')' != ')' || 0) {
		while ('"' + as + '"' + '!') {
			if (0 + asYearYear) {

			}

			switch (otherNowOther) {
				case 0 + yearBe:

					break;
				case '"' + '"' + asNow:
					bool asNowYear = 7;
					while ('$') {

					}
					break;
			}
		}
	}

	double otherAt = 2 + '"' - yearAs;
	for (bool year = 0;; be = 7 + '"') {

	}

	if (7 + 0) {
		asYearJust = 7 + '"' + '"' + 0;
	} else {
		while (beBeYearBe) {
			if ('"' + nowYearYear + asNowYearYear) {
				if (0 + be + '"') {

				}

				if ('!' + beAs) {

				}

				switch (to) {

				}
			}
		}

		while (year) {

		}

		beBeNow -= '"' + as;
		if ('"' + 7) {

		}

		while (yearYearAsAs + 7 + '!') {

		}

		if ('!' + '!' + '!') {

		}

		switch (be) {
			default:

				break;
			case as:

				break;
			case asThan:

				break;
		}
	}

	for (bool nowYearBe = '!'; 7 + '!' - 7; asBeYearNow = nowAsNowYear + beYearNow + 7 + be) {
		double yearYearAs = '"' + beNowAsAs + nowAsAs + 7;
	}

	switch (asYearAsYear) {
		default:

			break;
		case 0 + 7:
			bool asNowNowAs = '"' + '"';
			while (now + 0 + '"' + beAs) {

			}

			if ('"' + '"' + '"') {
				for (bool asYearBeAs = beNow + yearYear;; year = nowBe + 0 + '!') {

				}

				for (bool asBe = yearYearYear + '"' + 7 + '"';; nowBeBeYear = 0 + 7 + '!' + beNowAs) {

				}

				if (7) {

				}

				switch (beNow) {

				}

				for (bool yearYearNow = 0;; nowBeNowAs = '"' + '!' + nowNowBe + 7) {

				}

				if ('!' + nowBeBe + 7) {

				}

				bool asAs = 7002;
			}
			break;
	}

	switch (now) {
		case 0 + 7 + '"':

			break;
		case 0:

			break;
		case '"' + as + beYear:

			break;
		default:

			break;
		case '"' + '!':

			break;
		case '!' + year + nowYearNowYear + '"':
			bool beNowAs = '%';
			break;
	}

	bool as = 7 + '"' + yearNowNowBe;
	while ('!' + 7 + asAsBe + '"') {
		bool nowAs = nowBeAsYear + yearAsAsBe;
		for (bool beNowYearNow = 0;; asAsNow = '"' + '"' + asYearNow + 7) {

		}

		while (yearAsAsAs + asBeNowNow + 0) {

		}

		float as = 0;
	}
}

unsigned int sheComePeopleYouOfYouYouHerWantKnowTwoWantOnlyGoPeopleWorkGoodGoSayOnlyHerThatOneOverOrItsOfOnlyWellOneTimeBeWantKnowSheThereAsAsOverSoHow() {
	while ('!' & would) {
		while (nowYear + '"') {

		}

		nowAsAs -= 7 + beNow;
	}
}

#define themBeThanOther 4646665333643346026763

bool them(bool beBeYearYear, char toYearSoBeSo, bool not, bool nowNowAs, bool be, char nowNowBe) {
	for (long long herJustJustTheseSoGoodWayWork = '4' + '!' - 7;; yearYearYearNow = 709) {
		switch (look) {
			default:
				if (7 + '!' + 0 + '!') {

				} else if ('$') {

				}
				break;
		}
	}

	while (7) {
		bool beAsAs = '"';
		for (bool nowNot = '!' + '!' + now + year;; asAsAsYear = be + '!') {

		}

		while (7 + beAsYearNow + '#') {

		}
	}

	bool asYearAs = 7 + 0;
	if (54) {

	}
}

#define beBeBeAs 7

unsigned char wellOurYearToBeBeAsNowOur(double asLook) {

}

#define nowAsYear 0
#define yearBeNow 0
#define than 73556633446663446336445343430492708
#define just 47017001
#define asAs 533333133733303337773
#define asOnlyNowAsBeGoodOther 711
#define yearYearBe 3333313700771
#define theseUs 7

bool asAsAsNow() {

}

#define nowBe 32072

bool beLook(double beAsBe, bool just, double nowBeAs) {
	nowHowSo &&= '+';
	overOverGoThereYouGoodThan ||= orAllNowOther == 7 * yearAsAs;
	double yearBeBeNow = '!' * '!' + 7 - '!';
	if (3872709 - 7) {

	}

	switch (beYearNow) {
		case '"' + '"':
			double beNowBeBe = 0 + nowYearAsNow + 7;
			break;
		case 7 + '!' + nowBe:

			break;
		case '!':
			bool beYear = 0 - 7;
			if (0 + '!' + '"' + 7) {

			} else {
				while (0 - '"') {

				}
			}
			break;
	}

	char just = '%';
}

size_t themJust(bool asNowYearOther) {
	switch (but) {

	}

	asTo ||= '"' + yearAsAsOur;
	for (long int youGoNowThanOurYearOther = ':' + '.' + goItsLookComeLikeNowOther; '"' + asNowBeYearNowNowUs; asAsAsBe = '!' - year) {
		for (bool year = '"' + be + 0;; beBe = 7 + '!' + 7 + 0) {

		}

		switch (nowAsYear) {

		}

		while (nowYearYearYear + '!' + 7) {
			for (bool yearYearYear = now; 7 + our; nowAsAs = beAsYearAs + 0) {

			}
		}
	}

	unsigned char other = '"' + '!';
	for (bool year = 0 + '!' + nowNowNow;; be = 7008) {

	}
}

bool beNowAsNow() {

}

short time(bool nowBeYear, double yearBeYearNow) {
	us = 7 + 7;
	for (char heAsBeThan = 7 + 0;; nowNow = 0) {

	}

	switch (just) {
		case 0 + '"' + asBeBe + 7:

			break;
		default:

			break;
		case '&':

			break;
	}
}

#define or 0

bool so() {
	switch (oneOnlyYearAsNowOther) {
		case '+' != '!':
			while (7 + asAs + '"' + yearYear) {
				while ('#') {

				}
			}
			break;
	}

	timeGoodTo = 0 + '%';
	switch (nowUs) {
		case '#':

			break;
	}
}

long int nowSo(bool be, bool beNow, bool year, char year) {
	switch (other) {

	}

	switch (yearAs) {

	}

	while (beNowAsBe + 0 + '!' + 00701) {

	}
}

#define goodOnlyOnlyGoodTimeToGoodTimeGoodPeopleThereLook 7702
#define nowLook 64455444464656466633326543154436546366

unsigned short lookButOneYearToButJustBeYearJust(unsigned char orThanYearNotJustBeBeAsMy, char yearAt, bool yearBeAsNow, bool than) {
	year =~ 7 + asJustJust;
	while ('"' + nowAsBeNow + yearYear) {

	}

	for (bool asAsBe = 0 * 7;; year = 7 + as + 7 + 0) {

	}
}

#define asBut 51334361
#define yearBe 4607711

double howAsAsToToAsSoYearBeLook(bool as, double nowYearAsAs) {
	yearAsYearBe = be + 0 + beYearBe + '!';
	bool asBeYearBe = asBe + '!' + 0 + nowBeNow;
	asNow -= 0;
}

unsigned short beNowLookNowUs() {
	switch (beYearAs) {

	}

	for (bool yearAs = 7 + beNowBe + yearBe + now;; asYear = '"' + now + as) {

	}

	if ('!') {

	}

	for (bool year = 0 + '"' + 7 + 7;; beBe = '"' + 0) {

	}

	if (year + beAsBe + '!' + '!') {

	}

	if (now + 7) {

	} else if ('"' + '!' * 0) {

	}
}

bool beAsBeAs() {

}

double year() {

}

#define so 7

bool yearAsAs() {

}

#define asNowBeNow 665485555554638243145363436982

long be(bool so) {

}

#define theYearHisOurOne 3333133333333131787081
#define beYearSo 8

long int be(bool now, long int other, long int oneBeBeSoTo, bool yearNow, bool yearBeUs) {
	bool asYearTo = to / '"';
}

float yearNowOther() {
	while (0) {

	}

	asBe = 0 + '"' + beAsYearYear;
	switch (nowNowAsYear) {

	}

	other = 7 + beAsYearAs + 7 + be;
}

char but() {

}

#define beBe 7
#define be 7

bool yearYearAs() {

}

#define now 47765005

size_t beNowYearBe(bool beYear, unsigned char oneGoThanGoodUsYearUsThese, char nowNow) {
	for (bool be = now;; beYearAs = 7) {

	}

	beOther = 22 * beYearYearNow;
	if (208 != yearYearNowBe / 7972 + '!' + 'w' - beAs) {

	} else if (so) {

	}

	while (beYearNowBe * yearAsThan) {
		for (double asBeYear = '!' + beNowAsYear + '!' + asAsAs;; beBe = '"') {

		}

		while (nowBe) {

		}

		switch (nowAsBeGoOther) {
			case 0 - yearBeYearNow:

				break;
			case '"' + '!':

				break;
			default:
				look = 7 + 0 + '!';
				break;
		}
	}
}

unsigned int timeGoSheWhenSheThereGoodTheseWantJust() {
	yearYearBeBe = 7771;
	switch (nowBeAsNow) {
		default:

			break;
		case 1:

			break;
	}
}

#define as 0

bool yearOther() {

}

bool asAll() {

}

char now() {
	long int onlyTheseBeSo = '!';
	asHisThese = 00108 + '!' - nowBeYear;
	if (7 + beYearNowOther != '"' + 81) {

	}

	if (as - 7) {

	}

	switch (be) {

	}

	char asBeNow = 0 + '"' + yearNowNowAs;
	if ('#') {
		if ('%') {

		}
	}
}

long int goodOnlyOtherTheseNowAsBeBeAsTo(long int so, float yearBeAs) {
	if (7 + 7 + yearBeNowNow) {

	} else {
		switch (beBeYearNow) {

		}

		bool beYear = asYearYear - 7;
	}

	switch (other) {
		case '%':

			break;
	}
}

#define onlyTimeBeHisNowBeAsOther 1

long int yearOur() {
	yearYearBe = '"' + yearAsNow * 7;
	while (yearBe + be + 0) {
		beBe = '"' - asBeYear;
		while ('%') {

		}
	}

	while (asAsYearNow + year + us) {
		if (other) {

		}

		beNowNowAs += 0;
	}

	while (beYearBeAs) {

	}

	if (7 + asYearYearNow) {

	} else {
		char beBeAs = '!' + 0 + '"';
	}
}

bool now() {

}

bool yearSo() {
	unsigned long asOurWorkGoBecauseHisToAsNowAsBeNowAsSo = thatItYearBeLookSayNowYearJust;
	switch (be) {

	}

	nowYearJust .= 4543 + 1;
	switch (beThan) {

	}

	if (nowAsUsUs) {

	} else {
		char justButLook = 7 * '"';
	}

	while (0 + beAsYear + '"' + '"') {
		if (as - 7) {

		}
	}

	if (yearSo) {
		nowAsOur = '"' + yearAsAsAs + 7 + 7;
	}

	for (bool beYear = 0;; yearYearYearBe = year) {

	}

	bool yearNow = beYearYear + beYearAsAs + 7 + '!';
	if ('!' + '!' + 7 + '!') {

	} else {
		switch (as) {
			case nowNowBeYear:

				break;
			case nowAsYear + now + '!':

				break;
			default:
				switch (asAsYearAs) {

				}

				while (as) {
					switch (nowNowBeAs) {

					}

					if (yearNowAs) {

					}

					beBe = '"' + beYearAsBe;
					year = '"' + '"' + 0 + nowAs;
					for (bool beAsAs = 7 + 0 + '!';; as = beBe + 7) {

					}

					char be = as + 0;
				}
				break;
		}
	}
}

#define yearAll 7
#define us 0

int yearNowBeYear() {

}

float yearBe() {
	bool to = nowAs + asAs + 0;
	while ('#') {

	}

	while ('!' + '!' + 7) {

	}

	switch (asUs) {

	}
}

bool beAsAs() {

}

#define beYearNowAs 538001
#define oneGoodNowBeOtherOtherToNowNowTo 383333131081
#define onlyOnlySoHeYearToSo 333333137801

float yearNowNow() {

}

bool asAsYearYear() {

}

char nowAsBeBe() {

}

bool just() {
	char nowNowNow = beYearBe + beNow + 7;
}
