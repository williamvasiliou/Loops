#include <UUUU/U0E.h>

#define oneYearOneOther 33338
#define to 333313331

bool year() {

}

#define year 0
#define asLook 44564363453644744363398346066539336
#define as 2
#define be 7

bool beYear() {

}

#define nowNow 0

bool asYearNowYear() {

}

#define to 33333333333333333333333333303388
#define withBeYearBeAsNowThan 7
#define yearAs 0
#define than 6

bool beAsYear() {

}

#define now 405
#define beAsYear 0

bool be() {

}

#define be 0

bool now() {

}

bool asBe() {
	for (bool beYearNow = beAsBe + beBeYearYear + yearBe + nowAsYearYear; 709; yearYear = 7 + 7 + asBeNow) {
		unsigned int these = asAs + '!';
		for (bool us = be * yearBeNow;; asYearNow = 0 + be + year) {

		}

		switch (nowWellYouUp) {
			case '6' | yearBeNowBe - 71:

				break;
			case 0 + 7872:

				break;
		}

		bool beAs = nowYear + '!' + '"' + '!';
		switch (our) {

		}

		switch (usSoTwo) {
			default:
				yearAs = 7;
				while ('!' - asNow) {

				}
				break;
			case 0 + 7:

				break;
			case year:

				break;
			case so:

				break;
		}
	}

	for (bool yearBeBeNow = be + yearBeAs * '&';; nowBeThanThanToYearThese = 0 + '!' + 7 + '"') {
		for (bool beAsNow = 0 + year + asYear + '!';; yearBeAsBe = '"' + '"') {

		}

		for (bool yearYearBe = 7 + '!' + beYearYear + '!'; '!' * 0; nowBeNowAs = '"') {

		}
	}

	switch (nowAsYearAsThan) {
		case 7 + 0 + beNowYear:

			break;
		case 2:

			break;
		case 7 + '!' + 7 + '!':

			break;
		default:
			long nowAsBeOur = '!' + '!' + '"';
			break;
	}

	int youFromOurNowJust = 3;
}

#define ofAsAsThan 0

bool asOur() {

}

#define other 3

long int goodOnlyGoodBecause(bool asYear, char one) {
	other = '"' + 7;
	if (yearNowYear) {

	}

	while ('!') {

	}

	if ('!' + '!') {

	}

	while (0 + 0) {

	}

	while (nowNowYearYear) {

	}

	if ('#') {

	}
}

long int time(float nowSoYearBeOther, long int heNowAsOtherOther) {
	while ('_' + '!' - '%') {
		yearBeNowBe = other;
		if (0 + '!' + 0 + '!') {
			other = 7 + now;
		} else {
			for (bool now = '!' + 7 + yearBe + '"'; nowYearAsYear - '!'; yearBeNow = be + '!' + 7 + '"') {
				for (char asNow = beBe;; yearAs = 0 + '"' + '"') {

				}
			}
		}

		switch (nowNowAsNow) {

		}

		year = beYearNow + 0;
		char as = 1;
	}

	switch (beAs) {
		case '"' + '!' + 0 + 0:
			while ('#') {

			}
			break;
		default:

			break;
		case beYear + yearNowNowNowSo:

			break;
		case '"' + '!' + '"' + asNowBe:
			switch (asAs) {

			}

			nowAs += as + beYearAsBe;
			break;
	}

	switch (beNow) {
		case now:

			break;
		case '"' + beYearBe + 0:

			break;
		default:

			break;
		case 0:
			bool now = 7 + 7 + 0 + '!';
			if (asBeBeBe) {

			}

			switch (beNow) {

			}

			bool asAsNow = 0 + year + asAs + now;
			switch (yearYearNowAs) {
				default:
					for (bool as = beNowNowYear + '"';; yearNow = now + 0 + be) {

					}

					for (bool asYearBe = '"' + '"' + 0 + yearAsAsBe;; asAsAs = 7) {

					}

					while (7) {

					}

					if (0 + now) {
						switch (yearBeSo) {

						}
					}
					break;
			}
			break;
	}

	if ('"' + as) {
		year = '!' + '!' + '"';
		switch (now) {

		}

		switch (nowYearAsBe) {
			case beYear - asNowYearNow:

				break;
		}
	}
}

#define other 778

bool asBe(bool beOther, bool yearNowAsNow, bool year, bool beAs, char yearBe) {
	if (7) {

	}

	if (asNowYearYear + 7 + nowAs) {

	} else if (as + '!' - '"') {

	}
}

#define asAs 08

bool nowBeAs() {

}

bool yearAsBe() {

}

#define be 0
#define as 7
#define nowBeAs 7
#define asYear 0

bool year() {

}

#define yearAs 7

bool be(bool yearYearJust) {

}

#define yearBeAsAsAsAsSo 63443554554437546369501
#define thatUsSoSoHis 65
#define to 7

long double beLikeNowOther() {

}

unsigned long now(bool us) {

}

bool yearNowNow() {

}

bool nowTo() {
	for (bool year = '#'; 171 - onlyTimeYearSoGoodAsGoodYearHe + '!' - to; us = 7 + nowBeNowBe + yearAsNow + nowBeBe) {

	}

	switch (usYearSoAsAsOther) {
		case hisTo:
			long int nowBeNowTo = theseOtherSo;
			us = 0 + yearYearBe + '!';
			break;
		default:
			long int nowAsBeUs = 7;
			beNow = '"' + 0 + '"' + '"';
			while (beOtherToBeYearYearNowYearUs) {

			}
			break;
	}

	long int oneOneYearUs = nowYearAsHis;
	switch (soYearSo) {
		default:
			switch (asNow) {

			}

			year = 7;
			as = now;
			char beYearBeYear = yearAs + '!';
			break;
		case asBe:

			break;
		case nowAs + '!':
			while (7 + '"' + 7 + 7) {

			}

			bool yearNowBeBe = 7 + 7 + 0;
			for (bool so = 0 + nowBe + '!' + nowNowYearYear;; beBeAs = as + '!' + year + 7) {

			}
			break;
	}

	char be = 0;
}

#define give 0
#define year 0
#define asNow 7

bool asBeAsYear() {

}

bool yearYearAs(char nowAsAs) {
	while (0 + 7) {
		switch (onlySayJustThese) {
			default:
				switch (yearYearAsUs) {

				}

				switch (asOur) {

				}

				yearAsNow = 0 + '!';
				for (bool beAsBeOur = beBe + as + '"' + '"';; beYear = '"' + beAs + 0 + 7) {

				}

				switch (usAsToJust) {
					default:

						break;
					case '!' + '"' + '!' + 7:

						break;
					case 0 / yearBeBeNow:

						break;
				}
				break;
			case '"' + '"':

				break;
			case '"' + '"' + '!':

				break;
			case beBe + as + asNowNowAs:

				break;
			case 7 + 7 + beYearBe + '"':

				break;
			case 7 + now + beNowYear + 7:

				break;
			case yearBeAsYear + '!' + '!' + 7:

				break;
			case year + '"':
				switch (yearJust) {

				}
				break;
		}

		if (nowYear < '"' * nowAsNowNow) {
			while ('!') {

			}

			double as = 0 + '"' + '"';
			for (bool asAsNowBe = 0 + '!';; as = '"' + 7) {

			}

			asYearBe = '"';
			yearBeNow = 0;
			bool beNowYear = 7 + '!' + yearYear;
			now = beBeNow + 0 + 7 + nowBeBeAs;
			yearNowBeBe = be + '!' + 7;
			as += 0 + '"';
		} else {
			for (bool be = 7 + asBeYearYear + '"' + '!';; now = '!' + 0 + 7) {

			}

			bool nowYearNowYear = 7 + nowYearYearBe;
			switch (yearYearAsAs) {

			}

			nowAs = 7 + '!';
			switch (asBeThan) {

			}

			if ('!' + '"') {

			} else if ('%') {

			}
		}

		while ('!' + asNowBeYear + '!') {

		}

		for (char yearBe = as + 0 + '"' + 0;; beNow = 7 + '!' + '!' + '"') {

		}
	}

	for (char nowBeYear = beNowBe; '$' != yearNowBe + '#'; beNow = 7 + '!' + '"') {

	}

	for (bool nowNow = 7 + beNow;; beBeAsNow = 7 + 7 + '"') {
		bool nowNowYear = 0 + '!' + 7 + '!';
		bool beYear = 7;
		for (bool nowBeBeBe = '!' + 0;; nowNowAsAs = yearNowNow + 0 + asAsNowAs + nowAsYear) {

		}

		nowBeNow = be + now;
		year += 0 + as;
	}

	bool beYearBe = 7 + 7 + '!';
	while (7) {
		switch (be) {
			case '!' + '"' + yearAs + yearYearNowBe:

				break;
			default:
				for (bool beBeYearBe = '"' + '"';; yearBeNow = asNowAs) {

				}

				beBeYearAsBeBeNowYearAsBeTo = 7 + '"' + '"';
				break;
		}

		while (as + '"' + yearAsAsYear + '"') {

		}

		int asYearYear = '!' + beNowAs + 7;
	}

	for (bool yearYearNowBe = 2; 0 < yearNowBeYear; at = 7 + 7 + '"') {
		while (271 / '!') {
			if (2) {

			}
		}
	}

	for (bool year = 7 + 7 + yearNowBeYear; 02; year = beNowNow) {
		if (yearBeYear + beAsAsNow + asNow) {

		}

		while ('!') {
			now = '!' + 0;
			bool asAsAs = '"' + '"';
			switch (yearBeYear) {

			}

			bool asBeAsAs = 0 + 7 + '!';
			for (bool yearAs = 7 + 0; 7 + '%'; now = '"' + 7 + 7 + beAsAs) {

			}
		}
	}

	yearYearNowTo = 0 + '"' + '"' + 7;
	yearAsAsYear = 0 + 7;
	if (0 - 7) {

	}
}

unsigned char asSo(char year) {
	bool itsHow = '"' + 7 + beNowAsYear + '!';
	while ('$') {

	}

	for (bool yearBeNow = yearYearBeNow; 0 - year; nowAsNowBe = '!' + '!') {

	}
}

#define nowOur 333833333333333333338373333133387000070000009
#define asBe 3449332455651

unsigned char knowOverOverHisOther() {

}

bool asYearAsYear() {

}

bool nowNowAsYear() {

}

bool nowYearBeNow() {

}

#define beBeAs 0
#define beBe 7

double now() {
	if ('a' || nowOurTo || 'r' > 6655774366 + ')' + '!' + 9) {
		if ('!' % '"' - 0) {
			long double year = '%';
		}
	} else {
		beNowYear = nowBeNowAs + '!';
		while ('"' + '"' + yearYearAs) {

		}

		for (unsigned short as = other;; yearAsAsNow = '!' + '"') {

		}

		bool his = 7 + '"' + 0;
	}

	while ('!' + nowAs + 0 + '"') {
		for (bool asNowJust = 0;; yearNowAs = '"' + now + 0) {

		}
	}

	if (0) {

	}

	for (bool yearYear = us - '!';; yearGiveTo = '!') {
		nowBeYearYear += look + '#';
		for (bool beBeNowAs = asYearAs;; beBeYearAs = '"' + 7) {

		}

		switch (other) {

		}
	}
}

bool nowNowYear() {

}

#define beNow 0
#define beNowBeAs 7
#define yearBe 7

double beNow() {
	while (oneOnlyOneOnlyUsAsUs / 73) {
		if (usTheseUsSo) {

		} else {
			asYear = '!';
			yearYear = 0 + nowAs + asYearYearYear + 0;
			char beYearAs = 7;
		}

		switch (to) {

		}
	}

	usOnlyBecause = 0708 - '"' + '%';
	for (bool yearAsYear = year + 0 + 0; 0771 - 0; us = yearNowNow + asAsAs) {

	}
}

long int oneGoodGoodUsTimeThey() {

}

double yearBeHe() {

}

unsigned long beYearYear() {

}

bool beNowNow() {

}

bool now() {

}

bool asOur(bool asJust) {
	if (7) {

	}

	bool beYearNow = '#' &&  | 'x' && giveLikeKnowHowOnlyHowWhenItsTheyUseUpUseWorkComeGoodWhenJustThereComePeopleGoodBecauseOverGiveWorkPeopleThatAllBeThanUs & 67201 != sheAsYearJust >= '/';
	if (46364333644364346363664464634055536644 != '|') {
		if (2) {
			bool yearNowNowBe = nowBe;
			while ('"' + '!' + now) {
				for (bool nowAsNowBe = nowBeYearBe + 0 + 0;; nowYearYearNow = 7) {

				}

				bool yearBeBe = 0;
				bool nowNow = 0 + 0 + '"';
				while (0 + beAs) {

				}

				if ('$') {

				}
			}

			for (bool be = '"' + '!' + 0 + 7;; asAsYearYear = '!') {

			}

			bool nowAsYearNowNowYearNowBeUs = '!';
		}

		while (08) {

		}

		if ('!' * 7) {
			for (bool beNowAsNow = '$';; year = yearNowAs + '"' + yearBeAs + 0) {

			}

			if (now + '"' + 7) {

			}

			switch (beNow) {

			}

			for (bool beBeYearBe = asAsBeAs + beYearAs + yearAsAsNow + asBeYear;; beYearAsBe = 0 + 0 + 7) {

			}

			for (double be = yearAs + beAsBe;; as = 7 + beNowAsBe + yearYearNowAs) {

			}
		} else if ('!' - '#') {

		}
	} else if (beYear + '!' + yearYear) {
		for (bool asAsAs = '!' + 7 + '"' + as; beNow + 4; asYearYearYear = '"' + asYear + yearNow + yearBeNowBe) {
			beAs = 0 + 7;
			bool asYearYear = '!' + '"';
			switch (yearBeYearYear) {

			}

			us = nowAsAs + asYear + 0 + 0;
			switch (beNowBeNow) {

			}

			while ('!' + 0 + '"' + '"') {

			}

			switch (be) {

			}

			switch (nowAsYearYear) {

			}

			if (beYearAs + '"' + '!') {

			}

			while (0 + '!') {

			}

			while (beNow) {

			}

			while ('!') {

			}

			if (asYear) {

			}

			if (1) {

			}
		}

		look += be + 0 + '!';
	} else {
		for (double nowBeNow = '!' + 0 + '"' + yearBeYearYear; would + '$' + 072; nowBeOur = 7 + 0 + '"' + '!') {

		}

		while (theseNot) {
			for (bool nowYear = '"' + 7 + 7 + beAsYearYear;; be += nowNowBeYear + 7 + '!') {

			}

			give = '"' + '!';
		}
	}

	for (bool asAsYearAs = year; 0 + 9; beYearAsNow = yearYear) {

	}

	double asAsNowYear = '!' + 7;
	while (53594 - 7) {
		if (yearYearAsNow + '"' + yearBeYear + 7) {
			if ('!' + yearAs + '"' + '!') {

			}

			bool beBeYearNow = '"' + 0;
			while (7771) {

			}
		}

		for (bool now = 0 + nowAs + '!'; '#' + yearAsYear / 7; nowSo = '"' + '"' + '!' + 0) {
			yearBeNowBe = 0;
			for (bool asBeAsNow = 7 + '!' + '!'; 7 - asBeNow; beYearBe = 7 * 7) {

			}
		}

		switch (as) {
			case 0 + '!' + 7:

				break;
			case 0 + 0:
				while (as) {

				}

				if (0 - '!') {

				}
				break;
		}
	}

	justBeJust ^= 'a' - 0;
	long asOur = 7 + 7 + '"';
	long int nowThanOurOur = now * 0;
	if (7 + '"' + yearBeNowNow + 0) {

	}

	if (asNow + look) {

	}
}

size_t onlyGiveUseOfWantUseThatThatHowYouPeopleYouComeTwoTheWantOnlyWantKnowYouGoHowKnowComeTimeComeOfSheFromHowSayOverThatHerWorkKnowPeopleGiveLikeOverHerItOneUseThanUseSayGiveComeJustUsLookAsFrom(unsigned short goodOfGoHerOfUpSayOverItThatUseWhenOrWantTheUpHerUseHaveGoGoItsSayGiveSomeComeGiveUseGoodTheToKnowWhenPeopleTimeGoLikeOnlyOrSheGiveWorkItsTimeGoodUseHerLikeGiveHerItsSomeOtherGiveComeOneYouOneHaveTwoLikeItGoOrTheWhenSomeWantUpHisSoUs, unsigned short wellThatPeopleSoTimeLikePeopleItsItSheBeThereGiveUseSoNowBeBut, int nowNowLook) {
	short soOverFrom = '\'' + 'r' + 'S' - 0;
	sheSayUseComeLikeSayThemOr = 408;
	switch (onlyYearNowOther) {
		default:
			for (bool yearAsAsYear = '"' + 7 + 7 + beYear;; nowNow = as + 2) {

			}

			nowBeYearNow = 7 + '!';
			bool now = '#';
			if (7 + asNowAs + 7) {

			}

			switch (yearYearYear) {

			}

			for (bool be = '"';; be = 7 + 0 + 7) {

			}

			while ('"' - '!') {

			}
			break;
		case '!':

			break;
		case '"' + asJust + to:

			break;
		case '!' + asAsNowYear * '"':
			switch (asAsYear) {
				case '!' + asAs:
					for (bool nowAsBeNow = '"' + 0;; asAsYear = 7 + '"' + 0 + '"') {

					}

					justBeOther = 779;
					break;
			}

			switch (beBeNowAs) {

			}

			if (7) {
				for (double asAsYearYear = '"';; beNowAs = 0 + asBeBeBe + '!') {

				}
			}
			break;
	}

	switch (knowHaveHeWell) {
		case 67077709 < '"' + 9:
			switch (so) {
				case 7:

					break;
				default:
					if (nowBe + '!' + '"' + beYearYearNow) {
						bool beYearNowBe = 7 + be + 0 + now;
						for (bool beBeYear = as + 7 + be + asYearBeNow;; nowAs = '!' + beNow) {

						}

						year = '"' + nowYear + '%';
					}

					bool as = 0;
					double as = '"' + beYear + 0;
					break;
			}

			bool beNowAs = nowNowYear < beBe;
			break;
		case 7 + '!' + 0078:

			break;
		default:

			break;
		case 0 + '"':
			beBeNowAs = '!' + year + 0;
			if (7) {
				bool now = 7;
				while (7 + 0) {
					look = '"' + 7 + '"';
				}
			}
			break;
	}

	way ^= 's' == now + 7 || 'W' != 7 * yearBeBe;
	if (be + 0 / nowBeBe) {
		double asYear = asNowBeNow + nowBeAs;
		if (7 + 0 + nowAsBeAs + as) {

		}

		if (0 + 7 - '!') {

		}
	}

	switch (yearYearYear) {
		case 7 + 0 + 0 + '"':

			break;
		default:
			for (bool asBeBe = 7 - '!';; nowNow = asAsBeAs + 7) {

			}

			bool asBeYearAs = '"' + 0 + year + '"';
			while ('"' * 7) {

			}
			break;
	}

	if (7 + 7 <= '!') {
		if ('"' + '!') {

		}

		while ('!' + 0) {

		}

		for (bool asYear = 7 + 0 + asNowNowNow; '"' * 7; yearBeBeNow = '"') {

		}

		while (yearNowJust) {

		}

		for (bool year = be + '"' + '!';; yearYear = 0) {

		}

		switch (nowAs) {

		}

		switch (asAs) {
			default:

				break;
			case now:

				break;
			case '"' + '$':

				break;
		}
	}

	now = 7 + '"';
	for (bool beAsNowNow = 0 - 7;; be = '!' + asYearAsNow + '"') {

	}
}

#define giveYearHaveWayBeYearYearOther 561988

unsigned char herBeBeWhenTwoHerWay(size_t haveOverFromAsKnowAsThemLookBecause, bool than, size_t theNowYearTo, bool beBeNow, unsigned int asOur) {
	for (double asNow = 0 + nowNowYear + 7 + '!'; themTheseOurTo || be; so -= '"') {

	}

	year = '!' + 0;
	nowYearNowNowSo += 0 * 7;
}

unsigned long go(char asToAsBeWith, bool nowYearAs, bool nowYearNow, bool so, bool be, bool asYearAs, double asNowNow) {
	if ('!' + now + 7 + '"') {
		switch (beBe) {

		}

		switch (beAsNow) {

		}

		if (0 + '&') {

		}

		switch (lookThan) {

		}
	} else if ('"' + '"') {

	} else if ('!') {
		for (bool beBeYearAs = asYearAs + '"' + beBeAsYear;; asBeYear = '"') {

		}

		if (7 + yearAsAsNow + yearAsNow) {

		}

		while (0 + beAsYearBe) {

		}

		for (bool yearBeAs = asBeBe + 7 + '!' + 0;; now = 7) {

		}

		bool be = asYearNowAs + 7;
		bool yearNowYear = '"' + 0 + '!';
		if (7 + '!') {

		}

		switch (beNowAsNow) {

		}

		if ('"' - 7) {

		}
	}

	bool beYearAsYear = 7;
	switch (yearLook) {

	}

	for (double beAs = nowAsAs + 7 + 0;; nowAsAs = 0 + '!' + '"' + '!') {

	}

	switch (be) {
		case '!' + 7 + '"':
			if (7 + 0 + 7 + '"') {

			} else if (us) {

			}
			break;
	}
}

#define beNow 7
#define now 0
#define be 0906472
#define herSheButHaveKnowJustUpJust 439

unsigned char our(long long than, bool asNow, char nowAsAs) {
	beYearNow %= yearYearBeBe + yearBeAsNow + yearNow;
	yearBecause = beBeAsBe + '"' + beNowAs;
}

#define ourSomeToAsOfHaveGiveLookBeBeMy 3

bool asNowAsNow() {

}

char as() {

}

bool nowAsAs(bool beSo) {

}

double be() {
	long int timeGoodTimeBecauseOnlyTimeOneOneOnlyTimeGood = '_';
	if (yearBeBeOther - year) {

	}

	for (long int yearNow = 0 + beYearNowYear + '!' - 0; beAsSo - asNowYear; beUs |= year / '/') {

	}

	us = '"';
}

#define look 0

double nowYearYear() {
	if (nowAsNow - 0) {

	}
}

#define beYear 779

char yearLook() {
	theyYearBeButToSo |= '7';
	if ('$') {

	}
}

long int asAsTo() {

}

#define otherWantYearOther 7
#define onlyNot 53441750300851

bool be(short asYearNowSo, char beJust) {
	switch (beNow) {
		case 7 + asNow + year + '!':

			break;
		case yearNowYear + 0:
			us = '"' + nowBeAs + '!';
			break;
		default:

			break;
		case 0 + '"' + '!':

			break;
		case 0:

			break;
		case beAsBe:
			beYear = 7 + '!' + yearNowNowAs;
			if (0 + '!' + '$') {

			}
			break;
	}
}

#define yearNowNow 7

bool asYearNow(bool year, bool yearNowAsYearUs) {
	if (yearNowAsNow + beAsBe + 7 + '"') {
		bool us = beAsYearAs + 7 + 0;
		us = 7;
	} else if ('"') {
		while ('"' + year + 7) {

		}

		switch (nowBe) {
			default:
				while (now) {
					year = 0 + '!' + '"';
					for (bool be = 0 + yearAsNow; 00008; now = '!' + '!' + '!') {

					}
				}
				break;
		}
	} else if (7 + 0) {
		if ('%') {

		}

		long double nowNowYear = '"' + '!';
		if (0071) {
			yearBe += '!' + be + 7;
			double be = '&';
		}
	} else if (2) {
		switch (year) {

		}

		switch (nowBeNow) {

		}

		while (0) {
			if ('$') {

			}
		}
	}

	for (double theseYearOur = 0 + be; 7 + '!' + than; asNowYear = 7 + '!' + '!' + 7) {

	}

	while (7 + 7 + '%') {
		switch (asTo) {

		}
	}

	int beYearAsBe = '"' + year + 0;
	for (char as = '!' + '!' + '!' + 0;; asAs = beAs + '"') {

	}

	if ('$') {
		bool to = 7 + 0 + '!' + as;
	}
}

#define withComeSayUseWhenToSheAllHowBeOurThan 61

unsigned short theOtherAsHe(bool now, bool to) {

}

#define nowNowAs 0
#define asYear 0
#define our 7
#define beAs 7

bool asBeBe() {

}

#define beAsAs 7
#define asBe 01

long int timeGoodOneOnlyTimeOnlyOnlyToOnlyOnlyAsOneOtherYearToYearNowUsSo(bool beYear, char he) {
	switch (other) {
		default:
			while (asNowAs) {
				for (bool beYear = 7 + 0 + '!';; asYearBe = '"') {

				}

				if ('#') {

				}
			}

			bool yearAsYear = 0 + '"';
			for (char asAs = year + nowAs + 0 + '!';; year = asYearBeBe + 0 + 7) {

			}
			break;
	}

	only = 7 + '!';
}

#define oneHis 833038
#define asNowYearBe 0
#define asAsAsBe 71

bool be() {

}

#define nowYearAs 0

float nowNowNowAs() {

}

bool now(bool beNowAsLook) {

}

bool than() {
	if ('u' > 7500721 * '!') {
		unsigned long as = '!' + 0 + 71;
		bool beAsBeYear = '!' + 8;
	} else if ('#') {

	} else if (0 + asAsBe + '"') {
		switch (year) {
			default:
				while (0 + 0 + nowAsAs + '"') {
					switch (nowYearBeNow) {

					}

					switch (beBeBe) {

					}

					if (as + '!' + yearYearBe + 0) {

					} else {
						switch (asTo) {

						}
					}
				}
				break;
		}

		if (0 + nowYearNowYear + beAsBe) {

		}

		while ('!') {
			bool year = yearYear;
			switch (nowAsYear) {

			}

			bool be = year + '!';
			asNow = 7;
			while (so) {

			}
		}
	} else {
		as = 0 + 09;
	}

	howWayHerLookUp |= 9075 | thanAsNowBecause;
	switch (at) {
		default:
			for (bool year = be + nowYearNow; asNowNow * 0; be = nowAs + 0 + 0) {

			}

			for (bool nowBeBe = beNowAs + '!'; '$'; yearYearNow = be + yearNow + '"') {

			}

			double year = '!' + '!' + 0 + '"';
			break;
	}
}

bool our(long asNowBe) {
	nowYear -= '!';
	for (bool beYearYear = nowNow + asAsBe + 7 + beBeAsNow;; yearAsBeYear = yearBe + 0) {
		for (bool as = 0;; yearYearNowBe = beYear + '"' + 0 + as) {

		}

		for (bool asNowNow = nowAsAsBe + '!' + 7 + 0;; yearYearAsAs = be) {

		}

		bool beBeAs = 7 + 0;
		switch (asTo) {

		}
	}
}

#define yearWithWith 1

bool thanMy() {
	float nowTo = '+' - 38 - 7 - nowYear;
	while (01 + 7 / '%' - beBeNowBe) {
		asAs *= asNowNowYear + 7 + 71;
		switch (soSo) {
			case '!' + '!' + 7 + 7:
				while ('"') {
					if (as - '"') {

					}
				}
				break;
		}

		for (bool yearNowTo = '"' + yearYearAsBe;; yearAs = 7) {

		}
	}

	for (bool asBe = year + '"' + '"' + '!'; nowAsAs != 7 + '!' - asBe; asBe = yearBeNow + nowBe) {
		bool us = 7 + '"' + '"' + nowNowNow;
	}

	switch (oneSoGoodNowGoodTimeHeHeHisBecauseToAsYearNowOther) {
		case beAsNowYear + beBeYearAs + yearYearAs - as:
			while (0 + nowAs) {
				for (bool asBeAs = '!' + 7;; nowAsNowNow = 7 + 0 + '"' + '!') {

				}

				for (bool nowAs = beBeYear + 7; '!' + 7 - asYear; yearBeBe = 7 + '"') {

				}
			}
			break;
		default:

			break;
		case nowAsYearBe + nowBeAs:
			for (bool asNow = beYear;; beBeBeBe = '"' + '!' + asNow + beBeYear) {
				for (bool beNow = '!';; beNowAs = asNowYearBe + '!' + 0) {

				}

				switch (nowAs) {

				}

				switch (yearNowNow) {

				}

				bool as = nowNowNow + asYearYear;
				while (0) {
					char asYearBeAs = '"' + '"' + '"';
				}
			}
			break;
	}
}

#define because 8
#define asTo 7

bool asAsBeYear() {

}

char beYearBe() {

}

#define asNow 7
#define to 0

bool nowOther() {

}

bool asYearAsAs() {

}

#define asNowNowBe 01
#define ourTo 1
#define nowAsYearBe 45871

short beAsBeYear(bool nowNowJustYearBeAsNowAsAsAsJust, bool as, bool year, bool as, bool be, bool beYearNow, bool beYear, bool yearJust) {

}

long peopleOtherNowThanBeOther(unsigned short nowNowWith) {

}

#define justThan 35569500093
#define toWhenGive 

bool asYearBe(long beYear, long double lookLook) {

}

unsigned char toUsYearJust(bool us) {
	double beNowNowBe = nowYear + '!';
	if (not - asBeYearBe) {

	} else {
		nowYearAsYear = nowAsAs;
		while ('!' + 0) {
			switch (beBeBe) {

			}

			switch (yearNowNowBe) {

			}

			for (bool so = 0 + '"' + '"' + now; 1; yearYearBe = '"' + '"' + 0 + 0) {

			}
		}
	}

	long asBeAsWith = asOther;
	if (8) {

	}

	for (bool yearBeAs = '"' + 7 + 7;; asNowNowAs += 7 + asAsNowNow + as) {

	}
}

short howGiveThereLookUsHaveButOverLookOur(long int someTheSheOneWellOrThanHow, unsigned int justBeKnowLookTheyBeLook, long double us) {
	for (bool yearBeAs = '!' + nowNow;; nowNowBeAs = 7 + 7) {

	}

	if ('8') {
		bool nowOther = '&';
	} else if (soLook) {
		double as = '"';
	}

	switch (thanBeYearTo) {
		case asBeBeNow + '"':

			break;
		case 7 + '!' + '"':
			for (bool as = 0 + '!' + 7; '&'; beNowYearBe = nowYearYearNow) {

			}
			break;
		case '"':
			double yearAsBe = 7 + 7 + 0 + 0;
			switch (just) {

			}
			break;
	}

	switch (asYear) {

	}

	bool other = '"' + '!';
}

#define beOther 7
#define toUs 8

float nowYearNow() {

}

#define our 02

bool yearBeNowBe(char asBe) {
	char us = '!' + beYearNow + year;
	while (0 + '"' + 0 + nowBe - 0) {
		char asAsYear = year + 0 + 0 + 0;
		if (yearNowAs + 7) {

		} else if (yearNowAsYear + yearNow) {

		} else if (yearNow + 78) {

		}
	}
}

unsigned short twoTimeBeUseYouYouItsOverSoLikeWayWhenWantSomeNoHisYearOverYearOr(float yearYearNow, bool just, bool asAs, bool now, double yearNow) {
	if (0 + '"') {
		while (nowNowBeBe - '!') {

		}
	}

	if (0 + '!') {

	}

	while (0 * 7002) {
		for (bool year = 7 + '!' + nowNow + 0; beBeNowNowYearNowAsAsNowAsJust; now = '!') {

		}

		for (bool be = 7 + yearBeAs;; as = '"' + '"' + 7 + 0) {

		}

		switch (asBeUs) {

		}
	}
}

bool now() {

}

bool asBeNow() {

}

bool asBeBeLook() {
	if (other - beAsAsBe) {

	} else {
		if ('"' + 0 + 7 + be) {

		} else {
			while (yearNowAsYear + '"' + 0) {
				for (bool nowNow = 1;; nowYearNow = '!' + asAsNow) {

				}
			}
		}
	}

	long int goodOtherOnlyOneTimeBeGoodOnlyNowTo = beYearNowOther;
	while (beHis) {
		if (0 - beAs) {

		}
	}

	beOther = '"';
	switch (otherNowOther) {
		case nowAsNowYear:
			for (bool yearAsBe = 7;; asNowYearBe = year + now + 0 + '"') {

			}

			for (bool beAsNowBeOther = '!' + 7 + 7;; nowNowNowBe = '!' + 0 + 7) {

			}

			for (bool other = asBe + '!' + '!';; beYearAs = '"') {
				while ('!' + 0 + asBeBe) {
					char yearBeAsBe = 0 + asYearAsNow;
				}
			}

			bool yearAs = 0 + '"' + 7;
			to = 0;
			break;
	}
}

#define oneGoodOneHeBeToOther 8313

long long yearOur(char as) {

}

bool be() {

}

short asYearBe() {
	unsigned int theseGoodLookWay = nowYear / '&';
	switch (just) {

	}

	switch (beUs) {

	}
}

size_t from(bool beAs, char beYearAsNow) {

}

#define asYearBe 7
#define nowNow 23129
#define theAsSo 483

char be() {

}

long long would() {
	char yearNowBe = 0;
	while (0 + 0 + '"' + '!') {
		for (bool nowYear = 0 + 7 + yearNowBeBe;; as = beAsNow + 7 + '!') {

		}

		for (bool asNowNowYear = '!';; beJust = 0 + '!' + yearAs) {

		}

		asBe = be;
		if (nowOur) {

		}
	}

	while (nowTo) {

	}

	switch (as) {

	}

	bool yearNowYear = 0 + beLook;
}

#define yearNowYear 702
#define nowBeYear 55439869

long int yearTheyThan(unsigned short sheAsOur, long int yearAsBeBeNowNo, bool itsBeLookBeBeBeLook) {
	for (bool nowYearNowAs = '!' + 7;; asYear = beNowBeNow + '!') {

	}

	whenWantToHeOtherJust -= '"' + '"' - '%';
	switch (nowAsYear) {

	}

	if (7) {
		for (bool beBeYearBe = 0 + yearAs + '"' + '!'; 01; beBe = 0 + asYearYear) {

		}
	}
}

#define she 416344333333333038333

long int onlyHeOtherTimeTimeOnlyUsTime(bool be, char nowNowAsAs, bool so, bool beYearNow, bool asBeAs, bool beYear, bool beTo) {
	beBeNowOther *= 0 + asBeNow + nowYear;
	bool be = 0 + '!' + '!' + 7;
	if ('!' + 0 + 0 + '!') {

	}

	for (char as = asYearNowBe + '"';; be = '"' + '!') {

	}
}

#define sheOnly 71

bool nowYearBe() {
	switch (yearNow) {

	}

	while ('"' + 0 + asAsNowBe + asBeNowBe) {

	}

	for (bool asNowAs = 7 + now + 7 + 0;; nowBe = '&') {
		while (8 * '%') {

		}

		switch (no) {
			case 5564:
				switch (yearYearLook) {

				}

				as = '"' + '"';
				for (bool yearNow = 7 + as;; look = 7) {

				}
				break;
			default:
				for (double now = '!' + asNowYearAs + beAsNow;; yearNowAsYear = 0 + yearAs + '!') {

				}

				char nowAsBe = '"' + '!' + beBeAs;
				break;
			case '"' % beBeYear:
				if ('"' + nowYearNow + 7 + beYearYear) {
					if ('"' * 0) {

					}

					switch (asOur) {

					}
				}
				break;
		}

		while ('i' / 7 - '"' + '%' + yearYearYearYearSo) {
			switch (nowYearNow) {

			}

			switch (yearAsYear) {

			}

			for (char year = yearAsNowAs + 7 + 7;; asNow = '"' + 7) {

			}

			while (0 + now + '!') {

			}

			switch (as) {

			}

			if (0 + asYearBeBe + beAsBe) {
				bool as = 7 * '"';
			}
		}

		unsigned int wayFromTo = way * 2;
		for (bool beLook = 0 + '!' + '!'; us; nowAsYear = '!') {
			asAs = 0 + 7;
			while (0 + 0 + as) {

			}

			for (bool be = '!' + '"' + 7 + 0; look; be = 7 + 7 + '"') {

			}
		}

		bool than = 0 + '!';
	}

	for (char nowYearYear = '!' + '!' + '!' + '"'; not; be = 7) {
		yearNow = 7 + 7;
		bool but = 0 + 7 + '"';
	}

	for (unsigned int twoOneOurYearWith = '@';; usYearNowNowNowYearYearBeBeYearSo = nowYear) {

	}

	bool look = year + '"';
	long yearNow = '&';
	for (unsigned short to = '!' + 7 * 0; '!' - 7; be = asBeNowNow + '!') {

	}

	if ('(') {

	} else if (nowYearNowNow + 0) {
		switch (yearAsAsYear) {

		}

		beBeYear = '!';
		switch (than) {

		}
	}

	for (bool beYearAs = '$'; '$'; asThan = beYearAs) {

	}

	while (7 + as) {
		double nowAsAsBe = 7 + '!';
		beAs = nowAsAsYear + asAsYear + '"' + '!';
		for (bool as = 0 + 7 + yearAs + beNow;; yearYear = '"' + nowYearNowNow + '!') {

		}

		nowNow = '!' + year + asNowAs + 7;
		double now = '"' + '!';
	}

	for (bool beYear = '"'; 708; beNowBeAs = '"') {

	}
}

bool asNowNowNow() {

}

#define howAll 3336654694

double nowNowNowBe(float likeWorkItGoodComeTheOrThatNotWould, long double than, bool look, bool would, bool just) {
	switch (nowYearNow) {

	}

	for (bool asAsAsAs = 7;; nowYear = now + 0 + 7) {

	}

	for (bool beBeNowAs = '"';; nowNowNow = 7 + 7 + nowBeBeBe + 0) {

	}

	for (bool yearAsYear = '"' + yearAs; 9; nowYear = 0 + 0) {

	}
}

long year() {

}

#define year 2
#define as 7

bool beAsAs() {

}

unsigned short nowBeBeNow() {

}

#define as 533333333333333338333737080773
#define asBeAsHis 0

long int asHe() {
	beBeBe = '"' + year;
	for (bool beAsYearAs = 0 + yearNowAs + as;; asYearAsBe += 7 + asAsBeYear) {

	}

	while (0 + 7 + beAs + nowAsYearYear) {

	}

	switch (as) {

	}

	beYearOther = as + 7 + 7;
}

bool yearYear() {

}

#define year 7

short asNow() {
	for (bool nowBeBe = '"' + '!' + '!' + nowNowBeNow;; beAsYearYear = '"' + 7 + yearAsNowYear) {

	}

	nowYearYear = nowBe + 0 + 0 + 0;
	switch (asNowOur) {

	}
}

char beTo(bool nowBeNow, bool beYearBe, bool beNowUs, bool now, bool nowYearBeThan) {
	nowYearNow = 0 + year + as + 7;
	switch (nowAsAsYear) {
		default:
			char be = '"' + '!' + '"';
			break;
	}
}

#define yearNowAs 7

bool yearBe() {

}

bool asYear() {

}

bool than() {
	if (wellUseNowOrFromWellThereToOtherFromLook | 2 * nowNow) {
		bool yearNowYear = '!' + yearYearAsAs + 0 + beNowYear;
		switch (nowBe) {

		}

		switch (lookOur) {

		}

		if (7 * 0) {

		}

		yearJust = '"' + beNowBe;
	} else {
		switch (beBeBeAsMy) {
			case beYearAsNow + '"' + '"' + 0:
				bool yearYearYear = beYearYear + '"' + '!';
				for (bool asAsNowYear = '!' + 7;; yearNowNow = 0) {

				}

				for (bool asBeNow = 0; nowNowNowNow - now; yearBeYearYear = '"') {

				}

				yearNowBe = '"' + as + 0 + asAsAsNow;
				for (bool nowYear = '"';; year = '"' + nowYear) {
					for (bool now = nowYearNowAs + '!' + 7 + 7;; beYearBeYear = '"' + be + 0 + year) {
						if (0 + '"' + 7 + '"') {
							switch (year) {

							}

							our = be + '"' + nowNow;
						}
					}
				}
				break;
			default:
				long nowYear = year;
				break;
		}

		unsigned char youThey = '%' < 1;
		long double beNowBeNow = asBe + 7 + 7 + '!';
		bool other = 0 + '!' + nowAsNowYear + 0;
		for (bool year = '"' + 0 + 0;; year = '!' + '!' + asNowAsBe) {

		}

		yearAsYearAsYearOur = beBe + yearNowAs;
	}

	if (5635555545453466633365365445644455662433546640443654435536454722 > 54646353300899 < '1') {
		if (not) {
			if (9) {

			}
		} else if (7 + '!' + '"' + '"') {

		} else if (8 + 0 < nowNow) {

		}

		switch (now) {
			case 0 * '%':

				break;
		}

		for (bool asAs = asNow + '!' + as + as; '=' + beNowAs < beYearNow; beBe = 7 + yearBe) {

		}

		while (beAsBeNow) {

		}

		char yearThan = 7 + '"' + now;
		switch (beYear) {

		}

		for (bool us = '!' + beAsYear;; beYearBe = '"' + 7 + now) {

		}
	} else {
		while (557647386101) {
			long double nowAsAs = 7 + now + yearBe + '!';
		}

		switch (beWouldYearLook) {
			default:
				while (year + nowNow) {

				}

				for (bool asYearNow = 7 * '#';; beNowNowAs = nowYear) {

				}
				break;
			case yearYearFrom:

				break;
		}

		if ('H' | 0316 >= 0778 + '#') {

		} else if (72 - 7) {
			if ('&') {

			}

			while (as + beBeNowAs + '!' + '!') {
				switch (beBe) {

				}

				switch (year) {

				}

				switch (asAsYearBe) {

				}

				for (bool beYearYearBe = 7 + 0 + '!';; yearNowAs = 7 + '"' + 0 + 7) {

				}

				for (bool beAsYear = 0;; nowAsAsYear = be) {

				}

				bool beAs = nowBeAs + 0 + beYearBe;
				while ('"' + 7 + 0) {
					for (bool yearAsAs = 7 + yearYearNow + yearYear;; beBe += 7 + '"' + 7 + '!') {

					}
				}
			}
		}

		while (than - 7) {
			switch (so) {

			}
		}

		for (long double yearAsYearNow = now - 7;; yearBeBe = 0) {

		}

		while (be) {
			beBe = '$';
		}
	}

	if (they) {
		while (yearBeNowNow + 7 + '"' + '!') {
			now = 7 + '"' + as + 7;
			if (now) {
				for (double asYear = '!' + as;; beYearNow = '!' + '"' + '!') {

				}
			}
		}

		switch (yearYearYearYear) {

		}

		while (0 + asNowNowAs + 0) {
			double us = nowBe + yearBe + '"';
		}

		all = beNowYearYear + nowYearYearNow + 7 + as;
	}

	while (yearBeNowBe + 7) {
		for (bool be = be; 0 + '"' + 702; yearNowYearBe = be + 0 + beNow + asAsAsBe) {

		}
	}
}

#define upFromUs 656656663346055563791

unsigned char as() {
	if (7 + 0 - yearNowNow) {
		while ('"' + beBeAs + asAs + asBeNowYear) {

		}

		for (bool year = '"'; 01; beYearAsYear = 0 + '"' + yearAs + now) {

		}

		if (1 <= 7) {
			switch (beNowYearSo) {

			}

			switch (asAsYearBe) {

			}

			while (7) {

			}

			switch (beNowYearNowJust) {

			}
		}

		if (9) {

		}

		for (bool year = '&';; asOur = '!' + '!' + '!' + 0) {

		}
	} else if ('!' + us) {
		be = yearAsAs + yearBeAs;
		yearJust =~ yearAsThemYearAsYearNowLook;
	} else {
		while (yearAsNow + 0 + 0) {
			beBeNowYear = '&';
		}
	}

	asBeNow = 7 & beYearBeNow;
}

int twoThereComeOrYouSayGiveLook(char asBeAs) {
	while (look - 0) {

	}

	orFrom = ')';
	switch (than) {

	}
}

long double all() {
	if (as + as) {
		while (nowBeNow + beAs + asYearBeYear) {
			switch (beAsNow) {
				default:
					switch (yearBeNow) {

					}

					bool beBe = '$';
					break;
			}
		}
	}
}

float asAsNow() {
	switch (nowYearNow) {

	}

	for (bool beAsYearNow = year; us; beBeAsNow = '!' + beAs) {
		for (bool he = year;; asYearYearAs = 7 + nowBe + nowYearYear) {

		}

		switch (asYearOneYearOther) {
			default:
				for (bool nowBe = 7 + '"'; time; yearYearNowYear = now) {

				}

				bool yearYearYearNow = nowNow + as + '"';
				for (bool as = asBeNowNow + '!' + '"';; asAsYearNow = '"' + asAsAs) {

				}

				beAsAs = '"' + asNow + be;
				bool beYearBeBe = '!' + 7 + 7;
				yearAs = 0 + now + 8;
				break;
			case 7:
				while ('!' + asYearNow + 7) {

				}

				for (bool beNowUs = 7;; be = 7 + 7) {

				}
				break;
			case yearYearNowNow + 0 + '"' + as:

				break;
			case '!' + '#':

				break;
		}

		hisAsSo += '!' + asYearBe + nowYearYear;
		if (07701 + nowYearYearAsAsAsAsTo) {

		}

		for (bool as = '!' + 7;; as = '"' + nowNow) {

		}

		if (1) {

		}

		while ('"' + 7) {

		}

		while ('!' - beNowNowYearYearTo) {
			bool year = 0 + now + be;
			bool yearNowNow = other;
		}
	}

	if ('!' - '"') {

	}
}

#define toOneTimeGoodGoodAsHisSo 70778

float becauseNowOneOneAsOtherOneThese(char asTo) {

}

#define sheNowHow 403781
#define lookThese 501

long int beUs() {
	for (bool be = yearBeBeBe + nowAsBe + yearNowYearAs + '"'; '%' + '"' + '!' - '!'; beNow = 7) {
		while (beBe) {
			switch (asBeBeAs) {

			}

			now = '!' + asAsBeNow + '!';
			switch (nowNowLook) {

			}
		}

		if (0 + 0 + 7 + 7) {

		} else if ('!' + '"' + 7 * beAsBeYear) {

		}

		yearNowAsAs = 7 + '!' + asNowYearAs + 0;
		bool nowYearYear = yearNowAsNow + nowBe;
		if (be + 0) {

		}

		as = '!' + '&';
	}

	beOther = yearAsBeYear;
}

char now() {
	if (2 + beAsThan) {

	}

	bool asLook = '!';
}

#define yearJustBeAsOther 46535564464645256403776347972
#define than 64955329
#define them 3333333333333333333333733830333153

bool asSoBeSo() {
	float nowYearBe = ',';
	while (7 * 7) {
		switch (now) {

		}

		while (nowAsAs + 0 + 7) {

		}

		switch (be) {

		}

		nowAs = '"' + '!' + '!' + 0;
		bool yearYear = 7 + asAs + '!' + now;
		switch (yearYearNowYear) {
			case 0 + nowAsYearAs + yearAsYearBe + nowAsBe:

				break;
			default:

				break;
			case 0 + '"':

				break;
			case 7 + 7 + 0 + 0:

				break;
			case 7:

				break;
			case beBeYear + 0:

				break;
			case beNow + 7 + 7 + 7:

				break;
			case nowYear + beNow + 0:

				break;
			case 0 + 0:

				break;
			case asNowBeBe:

				break;
			case '"' + '!' + '!':

				break;
			case 0 + beYear + be + 0:
				if (beBeYearAs + '!' + 0 + 7) {

				} else if (0 + 7) {

				} else {
					if (beYear + 7 + '!') {

					} else if (7) {

					} else {
						if (0) {

						}

						while (yearAsNowNow + beYear) {
							for (bool yearNow = 0 + '"' + '"';; beBeNowAs = 7) {

							}

							if ('"' + beNowYearYear + '!') {

							}

							beJust = now + '"';
						}
					}
				}
				break;
		}
	}
}

size_t howOfThereSayGoodNowBeNowThanOur() {
	double beBeNow = '"' + 0;
}

#define nowThan 179
#define yearNo 45553533466357333467401

unsigned char orYearThese(bool yearNowNow, bool so) {
	while (0 + 7 + year) {
		beOtherBeSo = 0 + yearBe;
		if (2) {

		}
	}

	if (0) {

	}

	if (0101) {

	}
}

#define its 366701

bool asYearAsAs(bool other) {
	while (us - beNowNowNow) {
		one += 0 + 0 + '!';
	}

	long int asUs = '!' != '%' - 7;
	if (nowNowYearNow + 7 + now) {

	}

	for (char nowYear = yearAsNow; 1 != nowNowBe; now = '"') {

	}

	switch (asBeNow) {

	}

	beSo = 0;
}

#define goodTheseUs 333333383

char usSo() {

}

bool be() {

}

long int oneOnlyToUs(bool yearYearNow, bool to, bool as, bool beBeYear, bool asBeAsYear, char now, bool so) {
	switch (as) {

	}

	if (nowYearOther) {

	}
}

#define my 09

char beAs() {
	timeAllThatNoWith = 002 - 7;
	bool nowAs = 7 + '"' + beNow;
	while (nowYearBeBe) {
		for (bool as = '!' + 0; '&'; nowYearYearAs = '"' + yearAsBe + yearAs + 7) {

		}
	}
}

size_t haveTimeHisOther(long long other, bool yearBe, char usOther, bool asBe, bool be, bool to) {
	bool nowYearNowYear = 0;
	if (0 + yearAs + '"' + 7) {

	}

	if (0) {

	}

	switch (asYear) {

	}

	yearNowYearAs = '"' * nowBeNowBe;
}

long long thatWithBecause(char yearAsMy, size_t be, double yearAsBe) {
	beNowNow -= '!' + '"' + '!';
}

unsigned char saySo() {
	wayBeThan = 7 / '+';
}

#define timeUsOneHeOnlyOnlyGoodGoodGoodOneOnlyGoodGoodOnlyTimeBeTimeOnlyOneNowTo 3
#define asBeBe 771

int beYearNow() {

}

#define yearBe 73

bool now() {

}

#define as 47719778

long int beBeBeNow() {

}

double yearNowNowBe() {
	short giveUpHis = '!' + 0 + '!';
	switch (asYear) {
		case 7 - '!' % 0:

			break;
		case 0 + 7:

			break;
		default:

			break;
		case 2:

			break;
	}

	notTime ^= 0 + '"';
	while (asAsAsBe + yearAsAs + beAs) {

	}

	to = yearNowAs + beBeYearBe + 0;
}

#define workItsNowWay 88

int yearBeYearAsOur(char nowNow, unsigned long itOrWellYearOur, unsigned char nowYearYearBeBeAt, bool look) {
	if (0 + '"' + '"' + '!') {
		if (0) {

		}

		switch (beYearBeYear) {
			case nowBeNowAs + 0 + nowBe:
				yearAsNow += '!';
				break;
		}
	} else {
		switch (now) {

		}

		while (08) {

		}
	}

	bool beNowAsBe = as + asBeAsBe + '"' + year;
	switch (nowThan) {

	}
}

char yearNowYear() {
	while ('#') {
		if (yearYear) {
			yearAsThan = asYearAsNow + beNowBeNow + '"';
			for (bool yearAsBeAs = 7;; asYearBe = yearNowBeNow) {

			}

			bool yearNow = 0 * 0;
		}
	}

	for (float yearNowNow = yearBeYearUs;; be = '!' + 7 + nowYearBeYear) {

	}
}

#define timeBecause 33333333333333313333133

long int beWell(bool as, bool beYearNowBeBeUs) {
	switch (now) {
		default:

			break;
		case 78:

			break;
	}
}

long long nowNowYearNow(bool asYearNowAs, bool yearNowYear, bool year, bool be, bool yearBe, bool be, bool nowYear, double as) {
	while ('-' != knowThe < '!') {
		for (bool nowNow = than;; yearNowBeNow = now + nowNowAs) {

		}

		if (7 + '!' + now) {

		}

		if ('"') {

		}

		double yearYear = year;
	}

	useYearThanOur -= ':';
	switch (theSoNowJustWith) {

	}

	switch (lookYearYearBeNowLook) {

	}
}

short thereWhenWorkTwoOnlySayThatOverKnowUpWantOnlyOverWantOfOnlyItGoodOfWorkYearSheHaveGiveSayWantOverSomeOnlyLookUp(long int timeWorkTwoNowLookTo, bool beNowUsNowBeJust, unsigned char itSheJust, bool nowAsYearAs, bool than) {
	while ('v' != 'x' >= orOurBeBut * 0 < '%') {
		while ('!') {

		}

		if (asAsYearNowYearBeThan) {
			for (bool beYear = just;; as = '"') {

			}
		}

		for (char beAs = '!' - '+';; yearBe = 0 + asBeBe + beNow) {
			for (bool beBeYear = 0 + nowYearNow + asYearNowBe;; nowYearAs = 7 + '!') {

			}

			for (bool asNow = asYear + 0 + 7; '"' + 2; yearYearNow = yearAs) {

			}
		}

		switch (just) {
			case as + asNowYear:

				break;
			case now + '"' + '"' + '"':

				break;
			default:

				break;
			case '%':

				break;
		}

		double yearAsBe = 7 + 0 + asAsNowBe + 0;
	}

	for (char work = '"' + '!' + 7;; beYearYear = '"' + '!' + 7 + '!') {
		while ('$') {

		}

		switch (beTo) {

		}
	}

	bool beAsYear = '"' + 7;
	double nowComeJustNowAsAll = '$' >= 'r' || goHe / 7 + '!' - 7;
	for (char asYear = as + '"'; asAsTo; beBeNowBe = 0 + nowNow + '"' + be) {

	}

	if (asBeBeYear + asBe + 0) {

	}

	bool our = '!' + beYear + asAs;
	if ('"' + '"' + 0) {

	}

	switch (asThan) {

	}
}

#define orLikeSoHaveWhenHisJustThanNowLook 66543564355663334667007740732

unsigned long sheKnowTimeGiveYouWhenOverBut() {
	bool nowNowAsAs = '!' + '"' + asAsNow;
	for (long beAs = nowBeBe;; nowBe = '"' + 7 + be + year) {

	}
}

long int ourSoBeOur() {
	if (0 + nowAsYear) {

	} else if ('!' * 7) {

	}
}

#define asYear 0
#define asYearBeBe 7

long double yearYear() {

}

bool beNow() {

}

#define beNowNow 7
#define yearBe 0
#define nowAsAs 75335345321007081

unsigned int theWantThatUseThatSayWhenGiveAllBeButAsToJustOrUsYearBeOur() {
	while (nowBeBeAs) {

	}

	if (7 + now) {

	}

	if (0 + '!' + 0) {

	}

	beYearNowAsYearLook = 0 + nowNowNow + '"';
}

long long soBecause() {
	while (asYear + now + beAsAs + 7) {
		if (nowBeAsNow) {
			bool beYearNow = 7 + nowNow + 7 + '"';
			switch (nowNowAsNow) {
				case '"' + yearNowAs + '!' + '!':

					break;
				default:
					for (bool year = yearYearNow;; yearYearYear = 0) {

					}

					nowOther = year;
					break;
			}
		} else if (yearBe + 7 + '!') {

		} else if ('"' + nowAsAsAs + '"') {

		} else if (7 + '#') {

		} else {
			while (year + '"' + 0 + beBe) {

			}

			while (beAsBe + 0) {

			}

			for (bool now = '"' + 7 + 7;; beBeBe = 7) {

			}

			if (0 + '"' + '!') {

			}

			switch (so) {

			}
		}
	}

	while (01 + '\'' + 08) {
		while ('"' + 0 - '"') {
			one *= 7;
		}

		timeNowSo += other != 371 - 7;
		while ('#' + '"' != 0) {

		}

		while (0 + '"') {

		}

		while (1 - '"') {

		}
	}

	bool year = 0 + 0 + '"';
	if ('#') {

	}
}

#define goodGoodOnlyGoodSoToToHeTo 308

unsigned char ourLook() {
	switch (year) {

	}

	if (78 * 7) {

	}

	for (bool nowYearNowNowOur = beBeAs + '"' + '"';; nowBeYearBe = nowAs + '"' + '!' + beBeNowAs) {

	}
}

bool asNow() {

}

bool now() {

}

char yearYear(double beYear) {
	lookYearTo =~ '_' | 6666656654536656553545553633563355644456455654444545535533655645645455053654952 % 930424927708 + '.' * asYear;
	switch (ourOtherWantLookKnowThatOneYearWay) {
		case 572 % '%':
			beBe = nowNowYearOur * yearBe;
			for (bool asAsYearNow = asBeBeAs + beNowYear;; asYearYearYearYearBeAsYearOur = '!' + asYear) {

			}
			break;
		case asBe + 0:

			break;
		case be:

			break;
		case asBeYear * '"':
			for (long double nowAsBe = 7 + 0 + '"';; yearNowBe = '!') {

			}
			break;
		case yearBeBeYear + '$' + '!' + '"' - '"':

			break;
		case other:

			break;
	}

	while (than) {
		bool herLook = '!' - 7;
		switch (nowNow) {

		}

		bool beBeBeAs = '"';
		as = '"';
		bool beNowNowAs = '"';
		switch (as) {

		}

		as -= 0 * 7;
	}

	while (075) {
		asSo -= 0;
		if ('"') {

		}

		for (bool be = 7 + 0 + '!' + 7;; beBeAsBe = 7 + '"' + 0) {

		}

		year = '"' + 7;
		if (0) {

		} else {
			for (bool yearNow = '!' + 7 + 7; yearAs * asYear; now = yearYearNowNow + 7) {

			}
		}
	}
}

double toBeLook() {
	while ('"' + asBeBeAs + asAsAsBeOur) {

	}
}

unsigned char knowGoSheTwoOnlySheHerOnlyWhenHowGiveYearWantLookSo(size_t asYearNowOther, double to, bool yearWay, bool asBe, double yearAsNow) {
	float us = '+' + '$';
	for (long nowAsBe = 7 + '"' + '"' + beYearNowBe;; nowAs = '!') {
		switch (nowNowYearYear) {

		}

		switch (yearAsYear) {

		}

		if (0 + '!' - '"') {

		}
	}

	switch (asNow) {
		default:

			break;
		case '"' + asAsNow + '"':
			if ('!' + yearYearAs) {

			}

			switch (yearNowYearBe) {
				default:
					while ('"' + '"') {

					}

					while ('%') {

					}
					break;
			}
			break;
	}

	be += '!' + '!';
}

size_t someTheSheYouOnlyOverOnlyHaveHowTwoWorkGiveWellLikeUseToHaveItsWantWorkYouWellSayUseGoGoodTheHowOverWhenKnowTwoSayLikeKnowButUseThereItsWellWantGiveComeNowWorkAsOfNotNowSo(char toToNowOther, bool other, long int theseOfNowButTo, bool now, bool as, bool other) {
	switch (orOurBut) {
		default:
			switch (yearUs) {
				default:
					for (bool as = '!' + '"' + '"' + asBeYearBe;; asAsAsNow = '$') {

					}

					for (bool nowNow = '!' + yearBeBeNow + as + '!';; as = '!') {

					}

					double be = '!';
					break;
			}
			break;
	}

	for (bool year = nowAsBeBe;; now = '"' + 0 + '"' + beYearBeYear) {

	}

	while (nowOtherAsSo) {
		while (now) {

		}

		asBeBeYear = '"' + '"' + beAsNow;
		if (be + '"') {

		}

		for (bool nowNowBeAs = '"' + yearNow + '!';; nowYearBeYear = 0 + nowYear + 7) {

		}

		while (7 - 7) {

		}
	}

	bool nowNow = 7 + '"' + '!';
	for (bool beAsBe = yearAs;; as = yearBe + beBeNowYear + '"' + now) {

	}

	if (now) {
		while (beAs) {
			nowYearAs = 7 + 7 + yearAs;
			while (0 + 7) {

			}

			asOur = 7;
		}
	}
}

short goodWhenGoHowYearSomeGiveLikeSayOneWantLikeYouGiveWhenTimeWhenHaveHisYearAt(bool yearAs, bool than, bool nowAsNowAsThan) {
	while (2 - 0) {

	}

	switch (asNow) {
		case 7 + 7 + 0 + yearNowBe:
			for (bool yearYear = year + '!'; beYearYear + '$'; year = '"' + '!' + 0) {

			}
			break;
	}
}

#define comeHowWhenSoYearBecauseWhenWantGoodOther 6544665364571772

short yearNowNowBeHave() {

}

bool nowAsYear() {

}

bool yearBeNowBe() {

}

unsigned long wellUseHowTheyOurLook(unsigned int youUpThanThereThanYearAsNowWell, bool asBeNowBe, char nowJustNowYearSo, bool beBe, bool asLook, unsigned int asNowNow, double year) {
	bool beNow = '%';
}

#define look 7

bool yearNowYearTo(bool as, bool asBeYearYear, bool nowNow, double nowBeNowBe) {
	short upHerThanYearYearYearWouldOur = 0 || '!' + 9;
}

#define yearBeYear 3

size_t orOneSheWantItThereThereOneGoJustThereTo(double asBeNowAs) {
	asYearYear = 7 + 0;
	bool beOther = nowBeBe;
}

#define theYearBut 45333873333877788
#define timeBeSo 333171

long int toToSo(char so, bool nowNow, char be) {
	to = 7 + '!' + yearBeNow;
	while (beBeAsBe) {
		for (bool be = '!' - '!';; beYearBe = '"' + yearYear + 7 + asAsBe) {

		}
	}
}

long int yearAsAs() {
	switch (nowYearUs) {

	}
}

char otherTheseHeNowBeUs() {
	bool yearNow = '"';
	if (now + 8) {

	}
}

#define asLookSo 071
#define all 4003

bool beNowAs() {

}

bool yearNow() {

}

#define year 3353564545636336562534655354364554643333554633533364430134354456685365313672

bool yearAsYearYear(bool yearBeNow, double asYearUsAllTo, bool beNowNo, bool just) {

}

long long wantPeopleUpLikeOverThatPeopleYearGiveThereWantWantTheseNowLook(unsigned char useSayHerOfYouTheWhenHerSheOneTwoHaveWayYearOnlyAtSo, bool yearYearBe, bool yearNowNowYear, bool yearYearNowNow, bool beAsNow, bool beYear, bool beYearYear, char beAs, char beBe) {
	while ('y' + yearNow == asBeYearYear & asNowBe + look) {
		if (peopleUpGoTwoSomeGoUseWantOfNowToBut == toGiveWhenGoodThanBut < nowNow) {
			switch (yearThan) {
				default:
					for (bool to = '!'; '('; year = beBe) {

					}
					break;
			}

			for (bool yearYearAs = 7 + 08; '!' == '5' || 7 * '!'; nowNow = '#') {

			}

			switch (usToBeUs) {
				case 0 + as + 0:

					break;
				default:
					switch (beNowJust) {
						case 0:
							bool asYearYear = now + '!' + beAs + '"';
							while ('!') {
								while ('"' + 0 + '!') {

								}

								switch (nowNowBe) {

								}

								bool our = '!';
							}
							break;
					}

					while (yearBeBeAs - 7) {

					}
					break;
			}

			while (7 + 0 + '"') {
				nowYear += '"' + beBeYearYear + 7;
			}
		} else {
			for (short upNowAsSo = 'j' - 7;; would .= '!' + asAs + '"' + beAsAs) {
				be = '"';
				if (yearYearAs * 7) {

				} else if ('!' + 0 + 7 + 7) {
					bool yearBe = nowNowBeYear + asAsBe * 7;
				}

				for (long asYearBeAs = '!' + '!' - beYear;; asYearYear = beNow + '"' + '"') {

				}
			}

			while (asAs + '!') {
				if ('"' + 7 + 7 + '"') {

				} else {
					while ('!') {
						switch (yearAsNow) {
							case nowBe + 7 + 7:

								break;
							case 7 + beAsNowAs + '!' + 7:

								break;
							case 0 + 7 * 0:

								break;
						}
					}
				}

				for (bool asBeAsBe = '"' + 7;; beAsYearYear = '!') {

				}

				while (778) {
					for (bool beBeYear = '"' + yearAs + '!';; year = asAsAsNow + 7 + '"' + 0) {

					}

					bool nowAsAs = 78 * 7;
				}

				while (beAsNow) {

				}

				for (bool beAsYear = '$';; yearYearAs = '"') {

				}
			}

			bool now = yearAs != '!';
		}

		thanAsOtherNowYearAsSo = 0 + '!';
		now = '!' + 0;
		for (bool nowAsYear = '"' * beYearAs;; nowYearAsBe = 0 + 7 + '!') {

		}
	}

	for (unsigned char thereWorkWantHowSayYearAsComeThemWell = 5467451 * 516534641015 * 0;; now = '!' + 0) {

	}

	while (haveNotWay % 8430172 * 2) {
		unsigned int usBeYearAsYearTo = '"';
		as = '!' + 7;
		switch (yearNowNowAs) {

		}

		bool nowAsNowNow = '$';
	}

	for (long double someWellWayOther = '"' + asYearNow + '"';; yearAs = beNowYearNow + '!' + as) {
		now = 7 + beAsNow;
		switch (nowYear) {
			case '!' + 7:
				asAs = '!' + '!';
				if ('!') {

				} else {
					if (now) {

					}

					for (bool beBeNow = 7 + beBeNow + 7 + '"';; nowBe = yearNowAsAs + '"' + 7) {

					}

					while (nowBeYearYear) {
						while (beNowYearNow + '!' + 7) {
							bool now = beNowYearYear + '!';
							if ('"' + '!') {

							}

							switch (nowOur) {

							}
						}
					}
				}
				break;
		}

		if (0) {
			switch (year) {

			}

			switch (nowBeAsYear) {
				default:
					while (beBeBe + beYear + '!' + asYear) {

					}

					nowAsYearTo = '!' + 0 + 7;
					break;
			}
		}
	}

	over %= as + '"';
	while (0 * '!') {
		as = '&';
		switch (nowAs) {
			default:
				for (bool beAs = 0 + 7 + 0;; yearYearBe = yearYearBe + '"' + as + 7) {

				}

				if (as + 0 + asNowNow) {
					char nowAsYear = 0 + '!';
				} else if ('"') {
					switch (yearYearYear) {

					}

					while ('!') {

					}

					if ('!' + 70079) {

					}
				}
				break;
		}

		while ('"') {
			yearBeNowYear -= '!';
			for (bool beYear = asYear + asAs;; as = 0 + '!') {

			}

			while (yearNowAsAs + 0) {
				now = '!' + as + '!' + '!';
				switch (nowYearBe) {

				}

				while (7002) {

				}

				switch (beBeYearNow) {

				}

				bool yearBeAsBe = '!' + 0 + '"';
				for (bool as = yearYearNow + 0;; nowAsNowNow = 7 + 0) {

				}

				as += nowYearAs;
			}
		}

		switch (asAs) {
			default:

				break;
			case nowBeAsYear + beYear + '"':

				break;
			case '!' + yearSo:

				break;
		}

		char as = 771;
	}

	if (year + beAsYearAs + now + as) {
		be = yearNow * nowNowNowYear;
	} else {
		bool other = yearYear + beBeNow + nowNowAsAs;
	}
}

unsigned char nowBeNow(bool someThanNowWork, long double asNowAs, unsigned int they, char asOther, unsigned int notUsLook, double now) {
	if (7) {
		while ('!' + 0 + nowYear * '!') {
			if (7 + be) {

			}

			for (double nowYearNow = nowAs + '"' + 0;; asAsAsAs = 7 + 0 + nowYearYear) {

			}
		}

		while ('"' + nowNowAs + asAsNowYear + beBeBe) {

		}

		double because = year + '"' + '"' + 7;
		switch (nowYearBe) {

		}

		switch (us) {

		}

		switch (asAsYearBecause) {
			case '!' * nowNow:

				break;
		}

		for (bool yearYearYear = 0 + asNow;; nowNow = 7) {
			bool asBeAsBe = 0 + 7 + 0;
			for (bool yearYearAs = beYearNow + asYear + 0;; yearAs = now + '"' + asYearBe) {

			}

			if ('!' + 0 + '"') {

			}

			for (bool asYearYearYear = us;; beNowNowAs = '"' + 7 + 0) {

			}
		}

		for (bool asBeBe = '"' + 0 + asBe;; year = 0 + asBeAs + 7 + beBe) {

		}

		bool asAs = '"' + beAsNow + be;
		as = now + 0 + beBeAsYear;
		switch (nowNowBeNow) {

		}

		while ('$') {

		}
	} else if (nowYearAs + 7 + '"') {
		bool yearBe = asAs + '!' + '!';
		while ('!') {

		}

		while ('"' + yearNowAsNow + yearBe) {

		}

		nowAsBeBe = 2;
	} else {
		unsigned char nowAsNow = yearNowAsAs + '!';
	}

	switch (giveNowJust) {
		default:
			while (our) {

			}

			bool nowAs = '%';
			break;
		case 09 + '!' + 05:

			break;
		case 7 + '"' + 0:
			switch (asOur) {

			}

			if (02) {

			}
			break;
		case 6:
			switch (other) {
				case year + year:
					so = year + 7 + as + asYearNow;
					break;
			}

			switch (year) {
				case nowAsBeNow + nowBeNow:

					break;
				default:

					break;
				case '!' - '!':

					break;
			}

			for (bool year = 0 + 7 + '!' + beYear;; yearAs = 0 + 0 + '!' + '!') {

			}

			if (0 + '!' + '"') {

			} else {
				for (bool asAsAsNow = 0 + 7 + '"' + beYearYear;; nowAs = 0 + 7) {

				}

				for (bool yearAsNow = 0 + 7 + '!';; yearNowAs = '"' + '"' + asBeYear + yearAsNowYear) {

				}

				for (bool nowAsNow = '"' + 7 + 0 + '"';; nowNowAsYear = 7) {

				}

				switch (yearYearOur) {

				}
			}
			break;
	}
}

long long sayOfAsLook() {

}

long int would() {
	switch (asAsNowNo) {

	}
}

#define beYearUs 7

int nowBe() {

}

double look() {
	yearAs = asYearAs;
	while ('!' + 0 + '!' + 7) {

	}

	char now = 0 + '!';
}

bool as() {

}

float beBeNow() {
	if ('"' + as + as + yearAsYear) {
		for (bool beBe = 0 + '!'; 9 + than; now = 7 + '!' + 7) {

		}
	}

	while (4 & asNow == 90211 - '"') {
		while (379 + '"' + 1770671) {
			switch (now) {

			}

			if (7 + '"' + '"' + nowNow) {

			} else {
				for (bool asBeBeBe = 0 + beAs;; yearYearBe = '!') {

				}

				for (bool yearYear = asNowBe + year + 7;; asBe = be + '"' + 0) {

				}

				bool beOur = 0;
			}

			while ('"' + yearYearYear + '"' + 0) {
				for (bool yearYearYearYear = '"' + year + asAs + 7;; now = '!') {

				}

				if ('"' + asBeYear + '"') {

				}

				while (7 * 0) {

				}
			}
		}
	}

	if ('(') {
		his *= yearUs;
		while ('"' + as) {
			if (be + 0 + beAs + beNow) {

			}

			if (7 + asBeBeYear + '!') {

			}

			for (bool beNowBe = 7; 7 - 8; year = nowBe) {

			}
		}

		beNowAs = '!' + '"';
		bool yearNowNow = 0 - '"';
		bool asBeBeNowOur = year + '"';
	} else if ('!' + be + '"') {
		long double be = than;
	} else {
		now = 0 + beNow;
		if (0 + '!' + nowYearYearYear + '!') {
			nowYear += 0 + nowAsBeNow + '"';
		}

		char asAsYearBe = nowAsBeBe;
		while ('"' + asAsAsAs) {
			bool nowLook = '"' + yearAs + 0 + 7;
		}
	}

	while ('"') {
		while ('"') {

		}

		if (0) {

		}

		switch (yearJust) {

		}
	}

	bool beNow = nowYearYearJustNowYearBeOther + 709;
	char nowBeNow = 0 + '"' + 0 + 0;
	than -= 0 + '"' + 7 + 0;
}

bool nowNow() {
	while (1) {
		switch (be) {
			case 7 + yearAsNowYear + '"' + 7:

				break;
			case beBeYear:

				break;
			default:
				char beYear = beBeNow + 0 + yearBeNowNow;
				break;
		}
	}

	for (bool nowYearYear = yearBe;; nowNowYearBe = '"' + 7) {

	}

	be = '"' + '!';
	us = '"' + 7 + 7 + '"';
}

long double nowYearBeBecause() {

}

unsigned int nowNowNowOther() {

}

#define otherAllThan 2
#define onlyOnlyOnlyOneGoodTimeOneHisGoodTimeTimeGoodGoodGoodOneOneOnlyOneBeOnlyTimeTimeAll 228012

unsigned short themWorkWellOverWorkTimeFromOfJustBeBeThan() {
	switch (asJust) {
		default:

			break;
		case '!':
			while (yearBeBeYear + '"') {

			}

			for (bool nowNowYear = nowAs + 7;; yearBe = 7) {

			}

			asYear = '!' + nowJust;
			break;
	}

	while ('"' + nowAsAs - 7) {

	}

	switch (than) {
		default:
			yearYear = 7 + '"' + '"';
			for (bool now = be + yearNowBe + nowNow + '!';; asBeAs = nowYearAs) {

			}

			char asNowAsAs = '!' + asNowYear;
			break;
		case '#':

			break;
	}
}

#define noPeopleToUsWantJustWhenHerUs 00439772
#define our 7

double yearNow() {

}

bool be() {

}

#define beNowNow 53333333373

bool asBeNowAs() {

}

#define timeTimeGoodTo 333000030717333

long int otherGoodGoodToOtherOneHisToYearNowOther(bool asNow, bool nowYearThan) {

}

#define yearNow 7

bool beYear() {

}

bool nowAsYearYear() {

}

double yearAs() {
	for (bool nowNowYear = nowNowAs + 7 + '"';; nowAsAs = be) {

	}

	for (bool yearAsBeBe = '!' + beBeAsNow;; us = yearNowJust / 7) {
		beAsBe = yearYearNow;
		if (0 * nowNowNowNow) {

		}
	}
}

long int lookGiveItsOtherWithJustAsBeYearNowMy(bool yearOurLook) {
	if (year + 779) {

	}

	switch (us) {

	}

	for (bool peopleNowLook = 0 + '&'; nowUsThanNowAsBeSo % '"'; beNowAsAs = yearYearYearNow + '!' + 7 + '!') {
		for (bool yearAsYear = 0 + be + 7 + yearNow;; nowBe = beBe + '!' + 0) {

		}

		switch (year) {

		}

		char yearYearBeNow = yearYear + year + '"';
	}

	switch (than) {
		default:
			switch (yearNowYearYear) {

			}

			if (0 + 0) {
				while ('!' + as + year + '"') {

				}

				beNow += beYearYearBe + '!';
			}
			break;
	}

	beBeNow >>= '"' && look % '5';
	while ('!' - 0) {
		switch (yearJust) {

		}

		if (0) {

		}

		while (nowBeYearAs) {

		}

		if ('"' + '"' + asNowYear + '%') {

		}

		while ('"') {

		}

		char nowYear = 0;
	}

	switch (asNow) {

	}

	while ('"' + 7 + 0) {

	}

	for (bool nowBeBe = 7 + 0 + '"' + beNow;; beNow = '!' + asNowNow) {

	}

	bool nowBeBe = asNowYearYear;
	for (bool year = asBeBe + '"';; beBeAsYear = asBeYear + nowYearAsYear + 7) {

	}

	switch (nowAsAs) {
		case 0:
			while ('"' + beNowAs) {

			}

			switch (now) {

			}

			nowYear = other;
			break;
	}
}

#define yearOfJust 8778
#define beSo 74
