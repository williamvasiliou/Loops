#include <UUUUUUUUUU//66/S.h>

size_t nowNowAs() {

}

#define asBeBe 7

bool yearNowNowYear() {

}

#define than 7555634546546465646566663636634343465445545656533633536809

long int yearBeThanJust() {
	if (0) {

	}

	comeNowAsWouldThan *= '$';
	for (bool beAs = '!';; beNowAsAs = beAsNow + '"' + 0) {

	}

	bool year = '"' + 0;
	yearYearBeYear = '"';
	bool look = '"' + '!' + '"';
}

#define soLookThanThat 7
#define theKnowGoHaveItsOrOrWouldSoItsLookWantYearUsJustAsNowJustBeNot 6666563663666646646654654245160743

double yearNowAs(double nowAsYear) {
	while (0 > say > 0 <= '!') {
		unsigned int itsOnlyJustOther = '&' + now - thanTo + 0 <= 0;
		unsigned char beYearAsAs = nowYearBe;
	}

	as = asNowNowAs + be + be + 0;
	for (double beSo = '"' + nowYearYear + 7; '#' && asBeAsAs * '"'; beNowYearYear = 7 + nowAsBeBe) {

	}

	bool than = 7 + '"' + '"' + 0;
	for (bool beBe = '!' + beYear;; asYearYearYear = '"' + 0 + yearYear + 7) {
		nowNowAsYear += year + '!';
	}

	if (yearYear + '"') {

	}

	if (0 + '!' + '!') {

	}

	bool yearBeBe = '"' + '"';
	if (look) {

	}
}

unsigned char twoNowAtAsBeJust(char year) {

}

unsigned char lookNowThese(char yearBeNow, char be, char asBeAs, double yearAsNowNow) {
	if (as + 0 + '"' + '!') {
		if ('"' + '!' + 7 + '"') {

		}

		char as = 0 + '!' + nowYearYearAs + 7;
	}
}

bool year() {

}

bool beAsAsNow() {

}

#define than 7
#define beYearYear 0
#define asBeAsBe 0
#define yearBeBeBe 0
#define year 7

bool yearBeUs() {
	if ('_' != onlyOneOnlyTheseTimeOnly - '\'' + '#' - 8) {
		while ('!' / 0) {
			beYearYear |= 0 + 0 - 7;
			char beBeYearSo = asBeBeAs + '"' + 0 + 7;
		}
	} else {
		while (asBeSo) {
			for (bool yearBe = '"';; asAsYear = '!') {

			}

			for (bool as = beBeAsAs;; be = 0 + 0 + '"') {

			}

			to = 0 + nowYearBe + 0 + year;
		}

		if (7 + 0) {

		}

		switch (now) {

		}

		now = '"' + '!' + 0 + beYearNowYear;
		switch (now) {
			case 0:

				break;
			default:
				as = 0 + '"';
				for (bool yearYearBeBe = '"';; be = 7 + '!') {

				}

				while (7 + 7 + to) {

				}
				break;
		}
	}

	beYearBe = asAsAsYear != 3001 + '%';
	now = 7 + 0;
	yearYearAsYear = nowBeBeBe;
	if (7 + 7) {

	}

	bool asBeBeBe = 0;
	for (float asYear = '!' + 7;; yearAs = '!' + 7) {

	}
}

float nowBeYear(char nowYearAsNow, char us, bool be, bool us) {
	asYearNow = '!' + beAsAsBe;
	while (yearNowYearAs + 7) {
		char yearNow = now + 0;
	}

	while (0 + beYearBeYear) {

	}

	while (be + year + '!' + yearAsAs) {

	}

	if ('"' * '"') {

	}
}

char asAsYear() {
	char nowBeBeYear = '"';
	nowAsBe = 7 + now + asNowYearNow + be;
	nowNowAsYear = '"' - 0;
}

#define yearYear 7

bool yearNow() {

}

char yearAsLook() {
	for (long int usBeYearJustAsHave = asAsNowBe - nowYearYearYear; yearAsYearPeopleNot / '%'; beYearYearAs = '!' + beNowAs) {

	}

	while ('"' + '"' + beBe) {
		bool our = '"' + '!' + 7 + yearAsAsNow;
		for (bool yearBe = '"' + '!' + beYear + 7; 7 + 72; beBeYearNow = '!' + '!') {

		}

		switch (nowAsBeYear) {

		}

		for (bool yearBeNowBe = asYear + '"' + as;; be = '"') {

		}

		switch (yearBeNow) {
			default:
				if (0 * 0) {

				}
				break;
		}

		bool asAsBe = '!';
		if (7) {

		} else if (now - asAs) {

		}
	}

	thanAllBut =~ beNowBeNow + yearBeYear + '"' + '"';
	beBeAsNow = '"' + '#';
	for (bool beYearNowNow = '!';; asAsNowBe = 7 + now) {

	}

	switch (yearNow) {
		default:

			break;
		case other:
			beYearJust = 7;
			break;
	}

	for (bool year = 8;; asNowAsAs = '!' + beNowNow + yearNowAsAs + '"') {
		while (wellBeNowAtNotYearWhenSayAllToAsTo / 0) {
			switch (beNow) {
				case '!':
					while (nowYearBeAs + beNow) {

					}

					yearYearAsAs = '!';
					switch (our) {
						default:
							if ('&') {

							}
							break;
					}
					break;
				default:
					bool beNowBeNow = '$';
					break;
			}
		}

		unsigned int asBeNowThey = '"' + 7;
		yearUs = 0 + nowNowNow + '"';
		for (long double our = 7;; beNowAsNow = '"') {

		}

		for (bool yearNow = 7 + beAs + '"';; asAsBe = nowAsNowBe) {

		}

		beYear = 7 + now - 7;
		if (yearYearNow * 1) {

		}
	}

	for (char haveAll = '"'; '$'; as = yearYearBe + '!' + 7) {
		while (7 + beNow + 0) {

		}

		soThem = 7;
	}

	if (7 + '%') {

	}
}

bool beNow() {

}

short be() {

}

#define yearComeYouThanGo 723
#define us 0
#define beAsYearBe 0

char nowNowAsYear() {

}

#define yearNow 3333333333337333833333333333803

long int otherSo() {

}

#define other 0
#define theseAsThan 5013
#define beThanNowPeopleJust 34433333334760445074
#define toWant 4544311

char now() {

}

#define yearNow 2

unsigned char asBeJust() {

}

#define yearNow 0

bool as() {

}

bool yearBe() {

}

bool beBe() {
	switch (than) {
		case '?' != 8331008 != 3 != '/':

			break;
		case nowUs:
			while (7) {
				char nowNowAsNow = 7;
			}

			if ('!' + yearYearNowBe + 7 + '!') {

			}

			long int yearHe = beBe;
			break;
		case asYear + 0:

			break;
		case 7 + beNowNow:
			if (1) {

			}
			break;
		case now + yearYearAsYear + 7 + 0:
			switch (yearAsAsNowSo) {

			}

			for (bool year = '"' + 7 + 7;; yearNow = 7 + 0 + nowBeAsBe + '"') {

			}

			switch (asBeYearYear) {
				case '"' - be:

					break;
			}
			break;
	}

	if (7 + nowAsBeBe + '!' + 0) {
		beAsNowNow = 1;
	} else {
		while ('!' - '"') {
			switch (yearYearNow) {

			}

			if (year + year + '"') {

			}

			yearYearYearTo = '!';
			now = 8;
		}
	}

	switch (toOneBecauseOtherTheseTimeSo) {
		default:
			yearAs += year;
			break;
		case '"' + '!':
			bool asNow = '!' + 7 + 0 + '"';
			switch (us) {

			}
			break;
	}

	if ('#' + as + '&') {

	}

	asYearSo = 0 + 0 + 7;
}

#define asBe 0

bool yearAs(bool nowAsAsYearOther) {

}

#define asJust 53453653553465556336653453455363656364653563554563445363645333346695454336663365456454461668
#define nowNowTheSo 02

size_t itsItsGiveThereHaveWellTwoWhenGoOnlyTwoYearYearToOtherTheBecauseBecauseWith(char just, bool us, bool now, bool asYearNowYear, bool beBeAsBe, bool asBe, double than) {
	switch (at) {
		default:
			beNowAsSo |= 0 + '&' + 7 * '!';
			break;
	}

	for (double so = 7; beAs + 8; nowNow = '!' + '!' + '!') {

	}
}

unsigned char ofOfYouAsOur(unsigned short beYou, long nowYearYearOur, char asNowAs, bool he, bool look) {
	bool yearAsBe = 0 + yearYear + 7;
	bool they = '&';
	switch (its) {

	}

	bool asOur = '!' + '"';
}

#define wellHerHowThatNotOneHaveOrNowAsOtherNowOur 4443446545635354535713209

bool beAsAs(char beYearYear) {

}

unsigned short these(unsigned long nowAsBe, double be) {
	year = '!';
	asYear = be;
	double no = 7;
	if (7 + '!' + asYearBe) {

	} else {
		double be = be + 0 + year;
	}

	nowYearTo = '"' + nowNowAs + '"';
}

unsigned long nowAsUsSo(bool year, char look) {

}

char asBeBeNow() {
	switch (beAsNowYear) {
		case 70778:

			break;
		default:
			for (bool now = '!' + 7; yearYear + yearNow - 0; nowYearBeAs |= '!' + us) {

			}

			bool be = '!' + yearAsYearNow + 7;
			yearBeAs += nowUs;
			switch (us) {

			}

			bool yearNowYearBe = 7 + '"';
			for (bool nowBe = 7;; beBeYear = 7 + '!' + be) {

			}

			switch (nowNowAsYear) {

			}

			yearAsAs = asYearYear + be + 7 + yearAsYear;
			bool beBeBeYear = nowYearAs + 0;
			asAsAs = yearAsSo;
			break;
		case '"' + nowYear + as + '"':

			break;
		case 7 + asNowNowBe + asBe + 0:

			break;
		case asAsAs:

			break;
		case now:

			break;
		case yearAs + 0 + 0 + yearAs:
			bool now = '#';
			break;
	}

	beAsAs = be + 7 + 0 + 7;
	char now = '+';
	bool as = 0 + 7;
	for (bool as = 7;; yearYear = 7) {

	}

	now = '!';
	if ('#') {

	}
}

#define he 33878

long int soSo() {
	if (7 - 7) {

	}

	for (bool asNow = '!' + asAs + '!' + 7;; beBeAsYear = nowYearNow + yearAsYearNow + 7) {

	}

	if (0 - 0 / year) {

	}

	yearAsYearYear += '!';
}

long int so(bool nowYear, bool beAsUs, bool other) {
	switch (year) {

	}

	if (asAsAsBe) {

	}

	switch (asBe) {

	}

	for (bool yearBeYearNow = '#';; beBeYear = beAsBe) {

	}
}

long int beToAsNowSo(char asBe) {

}

long int nowTimeUs(char beAs) {
	beAsAsYear = yearYear - 0;
	bool as = 7 + 7;
	bool otherTo = year + nowNow + yearYearNowBe;
}

#define nowAsNowYear 7

char yearBeBeAs() {

}

short so() {

}

#define beAsBe 8

bool be() {

}

bool so() {
	all %= 0 * 0;
	bool nowThan = us + beOther;
	while (7 + 0) {

	}

	for (bool asAsAsLook = '"' + '"';; look = 7 + '!' + '!') {

	}
}

double asNowAsYear() {

}

bool yearNowBeAs(bool yearAsYearYear, bool as, bool year, bool beBe, bool yearBeNowNow, bool beNowAs, bool yearBeAs, char beNowAs) {
	us += yearAsBeAs + 0 + beAsNow + '!';
}

#define beBeYearBe 7
#define asNowNow 643364919

unsigned char goodOrOverGoItAtAsNowBeThan(bool nowYearAsSo) {
	switch (nowThan) {

	}

	switch (nowYearAsYear) {

	}

	switch (asYearBe) {

	}

	char beAs = asAs + 7 + 7;
}

#define justGoBeYearOther 131
#define beAsYearNowBeOther 64
#define yearAtMyJust 72
#define people 1
#define asBe 7
#define nowOur 6333333333333333337333733331383302

bool yearThan() {
	year = beBeNow + now;
	switch (as) {

	}

	while ('"' + '!' + '!') {
		for (bool asNowBe = nowBeBeAs + asAs + '!';; beOur = '"' + 7 + 7) {

		}
	}
}

int beJustBeUs() {
	switch (asNowAs) {
		case than:
			while (4524643374432 == comeFromSayAtNowThereNowWayOther || theOneUseTimeTimePeopleNowUsAsToAt) {
				now = nowSo + yearAs % '%';
			}

			while (her | 0 - yearBeAsYear + '&') {
				as = '"';
				switch (yearAs) {

				}

				switch (beAs) {

				}

				switch (as) {

				}

				for (bool yearUs = '"' + '!' + yearBeBe;; nowNowYear = asYearNow + '!') {

				}
			}

			now = 'L' - beAsNow;
			for (char to = '!';; year = year + nowYear + asAs) {

			}

			for (double nowBecause = yearAsNow + beNowYearBe; '"' + 0 - beYearBeYear; yearSo = 0) {
				while (0 + '"' + '"' + beNow) {
					while (as + 7 + 0 + 7) {

					}

					while (be) {

					}

					while ('&') {

					}
				}
			}

			for (long long itsTwoComeHaveBeYouBeYearTo = 75 * '"';; asYearNowAs = beBeAsYear + asNowBeNow + 0) {
				for (int our = nowAsBeAs + 7 + year + nowBe;; nowBe = '!') {

				}
			}

			while (wouldBeAsYearMy >= beYear - 0) {
				for (bool nowAsBeYear = '"' + beYearYearNow + 0; '!' + '$'; our = '"' + 0) {
					if (be + year + nowAs) {

					} else {
						if (just) {

						}
					}

					if (0) {

					} else if (1) {
						while ('"') {
							if (nowYearAs) {

							}

							now = 7 + 0 + '!';
							switch (be) {
								default:

									break;
								case yearAsAs + '"' + asBe + 08:

									break;
							}
						}
					}
				}
			}
			break;
		case 'x' / wayAsAsYearNowThan:
			unsigned int now = '\'' + 0 % nowYearAs;
			if (0) {
				justTo = beNowYear;
			}
			break;
		case '"':
			yearYearAs = yearNow;
			if (8778 && 7 + be - 7) {

			}

			while (as + 7 + '!') {
				if (nowNowBeAsBeYearSo) {

				}
			}
			break;
		case '.' - '"':

			break;
		case beNowYear + asNowAsBe:

			break;
		case nowNowYear + '"' + '!' + '"':

			break;
		case '!' - 7:

			break;
		case 70772 + '"' * '!':

			break;
	}

	if ('!') {
		beBeYear = '"' + nowNow + 0;
		if (0 + be) {

		}

		switch (yearNow) {

		}

		char as = '!' - 0;
		look -= 7;
		if ('$') {

		}
	} else if ('!' + '!' + nowAsBeNow) {

	} else {
		beBe = beAsYear + 7;
		if (7) {

		}

		for (char now = nowThan;; nowYear = '"' + '"') {

		}
	}

	if (useOfOfHerOther != beOurToWorkNowNowJust >= 04529777009 + nowOther * '!' >= now - '!') {
		while (look >= 0 >= '"' || yearBe) {
			for (bool nowYearYear = 7 + asBe + yearYearYear;; asYear = '!' + 7) {

			}

			if ('"' + '!' + '"' + 7) {
				while (7 + 0) {

				}

				bool beAsNowNow = 1;
			}

			char as = '!';
		}

		while (now + look) {
			if ('#' < just - 0) {

			}
		}

		switch (nowTo) {

		}

		us = 7 + asBe + 7;
		while ('!' + '"' + yearNowBeNow) {

		}

		for (bool beAsBe = 0 + 7 + asNowAsYear + '"';; yearYearYear = now) {

		}

		if (0) {

		} else if ('"' + yearBe) {
			if (7 + beBeAsNow + yearBe + be) {
				while (yearYearAs) {
					while ('"' + yearAsYearNow + be + 0) {

					}

					asAs = nowAs;
					nowNowNowAs = '!' + 0;
					if (0 + nowBe) {

					}

					while (7 * '!') {

					}
				}
			}
		}
	} else {
		while (nowNow + '!' + be + '"') {
			switch (asBeTo) {
				case 0:

					break;
				case '!' + '"' + '"':
					while ('"' + asBeBeAs + '"') {

					}

					if (year + '"' + '"') {

					}

					if (nowYearYear + 7 + asYear + 0) {

					} else if (0) {

					} else if (be + year) {

					} else if ('!') {
						year = '%';
					}
					break;
			}

			bool beBeNowAs = '!' + '!' + 7;
			char asAsYear = '"' + '!' + 0;
		}
	}

	bool yearNowYearBe = '!' <= as;
	asNow = yearBe + 7;
	while ('"' + '"') {
		switch (nowYearAs) {
			case 0 + yearYear + 0:
				for (bool yearYearNowBe = 0 + 0 + '"' + asYear;; beAs = 7) {

				}

				yearNowNow = '!';
				for (bool asNowBeAs = nowYear + '!' + '"';; now = 0) {

				}

				for (bool asNow = '"' - nowAsBe;; asYearYear = '!') {

				}
				break;
		}
	}
}

bool nowAsBe() {

}

#define asYear 0
#define nowNowBe 8
#define be 7

bool justJust() {
	if (0 + 0 + nowNowBe + asBeAsAs) {

	}

	if (asBe) {

	}

	if ('"') {

	}

	if (0 + '!' + '"' + asBeBeYear) {

	}

	yearNowYearYear = 0 + asAsNow;
	while (7 + be) {
		bool asNowAsYear = '!' + '!' + 7;
		other = asYearAs + '!';
	}
}

#define nowBeAs 07317178

unsigned short wellWantThereSayUseUseSomeThatSayItUsOur(unsigned long timeTimeWhenPeopleAsSo, char asNow, bool beBe, char beYear) {

}

#define our 6009

bool nowNowYearNowUs() {
	for (long int soHeOther = onlyOneOnlyTheseBecause != '\'' - '!'; 778 != '#' != '"' != 73711; beBe += 0 + 0 + '!') {
		while (yearOther - 0) {
			bool asBeAsAs = '"' + year;
			beBe = 0 + so;
		}
	}

	bool nowAsBe = 03 - year;
	for (char year = 0 + yearYearBeAs;; asNowBeYear = '"' + 0 + 7 + 0) {

	}

	char nowNowAs = 0 + '"' + '!' + as;
}

long int our() {

}

#define year 0
#define beAll 7

size_t asYearSo() {

}

long double toAsLook() {
	while ('s' | 6 % '!') {
		switch (beYear) {

		}

		if ('!' + asBeAsNow) {
			if (7 + 0 + be + 7) {

			}

			while ('"' * '!') {

			}
		}

		while ('!' % '!' + '"' - '!') {
			for (bool asAs = '!' + 0 + '"' + '!'; 7 + 0705; nowBeAsNow = '!') {

			}

			if ('-') {
				switch (yearYearUs) {

				}
			}
		}

		bool year = 7 + be + '!' + '!';
		yearBeAs = '!' + '!' + '!' + '!';
		year = nowBeBeYear + 7 + '!';
		while (beBeAs + as + beBe + as) {

		}

		bool nowNow = '"';
		double nowYear = nowBe;
	}

	unsigned char wantGiveHerWellPeopleSomeNoThereOrThatUs = 'u' >= 536343653545635537673545665396363639 >= '`' >= 'v' == 'z' & 'e' | 735395660634178 % 4433472 / '%';
	switch (yearKnowFromNowJust) {
		case '!' + 7 + year + 7:

			break;
		default:

			break;
		case like:

			break;
	}
}

#define  335663546556635645354544643554563366343636546544544675466546345443358446034364435555672
#define thereGoodWellItsOfOnlyGoodGoodItsUseComeWellUsSheSomeWhenLikeYouTimePeopleTwoHerAsOverTheItsBeGoSoBecauseAsYearUs 14455536463555355343545343445434553535645336963346555545353665463354463444653365356555666645056465333456356364646543564546355359
#define thereOnlyItsWhenToHeAsNowTimeBeBeYearAsSo 454646456635454333546654644455465364415645433446343664636635666455546634134643634643356645633444556535645556564354135464436641598541900779
#define asYearOtherAsTo 46055385
#define haveOneGoSomeOverOurIts 4606625576

size_t heNowNotNowYearBeSo() {

}

#define yearNow 72

bool asBeNow() {

}

#define nowBeThan 654453332566354444444560674704

bool asNowNow(bool ourOurThan) {
	double nowSo = '!' + '"' + '!' + beAsNow;
	switch (to) {

	}

	asBeNowNow = 07709 - '"';
	nowBeYearAs = beYear * 0;
}

bool just() {

}

#define to 3733333333333088
#define asTo 3333
#define goodAsGoodTimeYearNowGoodUsBecause 1733031
#define beToTheseUs 7

bool year() {

}

long nowBeAsBe(bool yearNowNowNow, bool just) {
	while (now) {

	}

	asAsYearBe = nowMy;
}

short asSo(bool nowBeBe, bool be, unsigned short yearYearBeAs, bool beNowNowBe, bool yearYearNowBe, char yearBeBeNow) {

}

#define year 0

bool as() {

}

bool asAsNowAs() {

}

bool beAsYearAs() {

}

#define look 0
#define so 564630335665265669702
#define look 09771

long long theThereTheKnowGo(double now) {
	now = 7;
	bool asSoNowOther = '!' + 0;
	switch (beYearNowNotBeAsLook) {

	}
}

char our() {
	switch (year) {
		default:

			break;
		case '!' + '!' + '"' + 7:

			break;
		case 72:

			break;
	}
}

#define yearBeAs 0
#define beNowThan 333333383333338
#define yearUsOther 3833808
#define goodUsNowOnlyUsNowGoodSo 1
#define toTimeHis 03021

bool be() {
	so = 7 + be + '"' + yearYearAsAs;
}

#define to 465

double yearSo() {
	itOneLookThem &= 'p' * knowYearNowUs >= other * 0;
	size_t soYearYearJust = comeUsNowGiveOrGood != soBeBeLookThanBeJust;
	if (0 + beAsNow + '\'') {

	}

	if (0 + '!' + '"') {

	}

	if (0) {

	}

	if (0 + '!' + nowYear + 0) {

	}

	bool beNowYearYear = 7;
	if (9) {
		while (beAs) {
			if ('!' + '"' + yearNowAs) {

			}

			beBeAsUs = '"' + '"' + '"';
		}
	} else {
		yearAs += 0 + '!' + nowBe;
	}

	if (72) {

	}

	switch (way) {
		default:

			break;
		case '!' + yearBe:

			break;
		case 0 - 7:
			for (bool asOur = asYearAsBe + now;; beYearAsYear = 7 + be + '!' + nowNowAs) {

			}
			break;
	}

	bool because = nowBeBe + 7 - '!' / 7 + 7 - now;
	long int otherBeOtherToNowSo = 77308 / 0;
	while ('!' + yearNow + as != '!') {
		while (nowNowAs) {

		}

		bool be = nowAs + 7 + '!';
		beAsAs += '!' + asNowAsAs - nowAsBe;
	}

	while ('"' != 77708 != beYear - '"') {
		as = nowBeNow + beBe + 0;
		while (asBeYear) {

		}

		while (as + asAs) {
			while (1) {
				for (bool beNowYear = beNow + yearYearBeAs + beBeAs;; beNow = '"' + '!' + 0) {
					year = 0;
					switch (us) {

					}
				}
			}
		}
	}
}

float beHe(bool as, bool us) {

}

#define other 333333

char hisYearBeOther(bool yearYearYear, bool as, bool beAs, bool beBe, char beNowBe) {
	bool nowBe = '!' + as + as + 0;
	if ('"' + '"' + nowNowYearYear + 7) {

	}

	if ('"' + '!' + 0 + '!') {

	}

	bool year = 07001;
}

long int soNowAsBecause(bool beAs, bool as, bool he) {

}

bool asAsYear() {
	bool asNow = '"' + 0 + '"' + '"';
	for (bool beYearAs = 0 + nowYearAsBe + yearNow; '#'; asNowYearYear = '"') {

	}
}

bool nowAs() {

}

#define now 50466666764333338666543665696377007708
#define oneThereBeIt 7
#define yearNowAs 7
#define nowAsBeNow 7

char as() {

}

char asBeAsNow() {
	switch (toOtherBeOtherHe) {
		default:
			char asBe = '"' + 7 + '"' + '!';
			switch (other) {

			}
			break;
		case '"' + '"' + 0 + 7:

			break;
		case 7 + '!':

			break;
		case nowAsNowNow:
			while (beYearNow + 0 + '!' + 7) {
				bool so = '!' + 7 + now + '"';
			}
			break;
	}

	for (char year = beAs; 7 - 78; asAsBeYear += beAsYear + 0) {
		switch (beBecause) {
			default:
				switch (yearSo) {

				}
				break;
		}

		bool yearNowNowBe = '"' + '"' + 0 + '!';
		for (bool nowAsYear = '!' + '!'; 1 + nowBeBe - 7; as = '"' + beBe + 0 + asYearBeNow) {
			if (0) {

			}

			switch (beBeAs) {

			}

			bool asAsBeAs = 7 + '"';
			while (as + yearNowYearAs + so) {

			}
		}
	}

	switch (asHeAsSoSoBecause) {
		case '"' / '"':

			break;
		case so - yearNowUs:
			switch (nowOnlySo) {

			}

			asNow = 7 - asAsAsYear;
			break;
	}

	so = 7 + so;
	bool other = 0 + 7;
	switch (other) {

	}
}

#define hisHisUsHis 7
#define otherSoYearOtherOnlyUs 1

bool beYearBeYear(bool asAs, bool asNowAs, bool beBeAs, bool to) {

}

bool theseAsTo(char nowBeYear) {

}

#define asBeBeBe 7
#define asNowYear 0
#define time 0
#define yearYearBe 0

bool now(char beBe, bool just, bool beYearOther) {

}

#define because 7
