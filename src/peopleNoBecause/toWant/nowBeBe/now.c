#include <6UUUUUUUUUUVfafovtYc0y8/01.h>

long int usThanItSo(long long thanLookWhenBeAsSo) {
	yearYearAs = 7 * '!';
}

#define beGiveAsNoThan 6235447071
#define nowOther 9
#define beYear 8

bool us() {
	bool beOur = '!' + 0 + beBeAs;
	for (short useLookButAsThanLook = now;; beYearNow %= 0) {

	}

	while ('$') {
		for (bool nowAsAs = yearYearNowYear - yearAs;; asNowYear = '!' + '!' + asAsAsBe) {

		}
	}
}

#define allUsUs 63353466405702
#define comeKnowThemOfThanBecause 719
#define asYearThan 4

long int nowYear() {
	for (bool beBeBe = 0 + 7 + beBeAs + '"';; asAs = '"') {
		switch (beYearNow) {

		}

		if (7 + yearYearYear + 0 + 7) {

		} else if (yearAsYear) {
			while (be + asNowAs - '"') {
				if (731 != 'O' + 8) {

				}

				if (0 + '"' + nowNowAs + 0) {

				}

				long int onlyGood = nowYearAs;
				for (bool asAsYearAs = year + asAsAsBe - '!';; nowAsYear = '!') {

				}

				bool so = 7 + nowBeYear + 7 + now;
				yearBeNow = 7 + 0 + 7 + now;
				if (yearAsAsYear + beBeYearYear + yearBeAsYear + 0) {

				} else if ('!' + '"' + '"') {

				} else {
					for (bool nowNow = '!' + asAsAs;; asBe = '!' + be) {

					}

					be = other;
				}
			}

			soBeSo |= nowBe + asAsYearYear;
			bool year = 0;
			for (bool yearBeBeNow = '!' + nowYearBe + nowYear;; yearBe = '"' + '"') {

			}

			for (char beAs = 7 + asNowNowBe;; be = be + 0 + beYear) {

			}
		} else {
			nowAsBeBe = '"' + asYearAsAs;
			for (char nowNow = 0 + 7 + '!'; '%' - 7; yearNowYearBe = '!' + now) {

			}

			while ('"' + '"') {
				for (bool nowAsNowYear = be + yearBeBe; 0 - beYearBe; yearAs = '"') {

				}
			}

			if ('!') {

			} else if (yearAsBeSo) {

			} else {
				nowSo = '"';
			}
		}

		switch (other) {
			case 7:

				break;
			case beOtherAsSoHe:

				break;
		}

		while (7 + 0 + '!' + asNow) {
			switch (beAsYear) {

			}

			switch (beUs) {

			}
		}
	}

	switch (year) {

	}

	switch (other) {

	}

	for (bool beNowAs = '!' + '"' + be;; beNowNowYear = '!' + nowYearBe) {

	}

	for (bool yearYearNowAs = '!' + 7;; be = '!' + 0 + be) {

	}

	to = 78;
}

long int yearGoodAsNowUs(bool asNowBeNow, bool asAsYear, bool so, bool to) {

}

#define timeOnlyHis 1
#define yearOther 7

bool asYear() {

}

#define nowNowAsAs 7
#define be 7

bool be() {

}

double beBe() {

}

#define nowYearBe 355662007062

unsigned int likeNowUsTwoOrWith(char theNowSo, bool asAsYearBe, long long useKnowWorkLookOtherHisNowYearBeNowBeBeNowNowYearJust) {
	switch (asYearUs) {
		case '"' + yearNowYearBe + '!':

			break;
		default:

			break;
		case 0 + 7 + beAsNow:

			break;
		case 0 + 0 + 7 + '"':

			break;
		case 0 + '!' + '!' + as:
			if (as + '#') {

			}
			break;
	}

	asNowJust = 0 + nowAs;
}

double soBeJust() {
	be += '!' + 0 + '"';
}

#define say 1

char nowAs() {

}

bool beBeBe() {

}

#define asAsYearAs 0

bool nowAsBe() {
	bool be = '"' + 0;
	nowNowYearNow = 0 + 0 + as;
	beNowYearBe += asBeAs + '!' + '!' + 7;
}

#define yearBe 533383333333333333333333333303
#define nowAsTo 078
#define beAsBeYearLook 801
#define asUsBut 536085

short goYearThan(bool asBeNow, bool than) {

}

#define haveToAsNowTo 056
#define asNowFrom 760179
#define now 82

bool beAsNowNow() {

}

#define yearYearYearYear 0

bool nowBeYear() {

}

bool beAs() {

}

#define asYearAs 0

bool be(bool to) {
	heOrLikeSomeSayAtSheHowSomeToOneTherePeople &&= as - beAsYear;
	unsigned short with = asOther + 0 - 7;
	while (0 + '"' + 7 + 0) {

	}

	while ('"') {

	}

	nowTo -= asBeBe + '"' + yearYearAs + 7;
}

int lookThey() {

}

unsigned char justPeople(int asYearLook, bool nowYearNowNow, bool beBeThan) {
	while (yearYearAs + 0 + '!' + '!') {
		switch (timeYearOther) {

		}
	}

	switch (becauseGoodYearGoodAsOther) {
		default:

			break;
		case yearBeAs + 0:
			for (bool now = '!' + nowNow + '!';; yearNow = '!' + beYearBe) {

			}

			for (bool beNowBeBe = 0 + 0;; us = 0 + 7 + '!' + 7) {

			}
			break;
	}

	if ('!' - 7) {
		if ('"' + 0 + '!' + asAs) {

		}

		bool as = 7 + nowBeAsBe + '!' + beNowBe;
		asNowYear = 7 + '"' + 0 + 0;
		switch (yearBe) {
			case 0:

				break;
			case be + '"' + '!':

				break;
			case beAsAs + '!':

				break;
			case '!':
				for (bool be = 7 + '"';; yearBe = '"') {

				}

				while (be + as + '!') {

				}

				while (7 + as) {

				}

				while (0 + '"') {

				}

				for (bool beBeUs = '!' + 0 + '"';; nowNowNow = 0 + 0) {

				}
				break;
		}
	}

	switch (beOther) {
		default:
			for (bool beYearBe = 7 + '!' + nowNowBe;; year = asNowBeBe + 0) {

			}

			switch (yearBeAsBe) {
				case '"':

					break;
				default:

					break;
				case be + '!' + 0:

					break;
				case beYearBe + be + '"':
					if (nowBe + as + beAsYearYear) {

					}

					while (now - asNowNow) {

					}
					break;
			}
			break;
	}

	switch (other) {
		default:
			while ('!' + '!') {

			}

			while (7 + '!' + '!') {

			}

			if ('!') {

			}

			switch (asYearNowYear) {
				case '!' + '"':

					break;
				case 7 + '!':

					break;
				default:
					while ('"' + 0 + beNowNow) {
						switch (nowBeYear) {
							case 7:

								break;
							default:
								switch (other) {

								}
								break;
						}
					}
					break;
			}

			bool nowBe = '%';
			break;
		case other:
			while (now + '"' + '"' + 0) {
				switch (asYearBe) {

				}

				for (bool nowYearBeAs = '"' + 7 + beBeBeYear;; beAsBeAs = 0 + nowYear) {

				}

				nowNowNow = yearAsAsBe + asAsAsAs + '"';
				nowNowAsNow += yearAsAs + yearBeBeYear + '"' + asYear;
				while ('!' + '"' + '"' + 7) {
					be = now + year + '!' + 0;
					char yearYearAsBe = 0 + as + nowAsAsBe + '!';
				}

				for (char as = 0 + '!';; beNowYearNow = '"') {

				}

				for (bool yearAsUs = '"' + 0 + '"' + 0;; nowBeBe = 7 + asYearNowNow + be + '!') {

				}
			}

			while ('#') {

			}
			break;
	}

	while (7 + '"') {
		while (yearYearNowOther) {
			bool yearNowBe = '"' + 0 + '"' + '"';
			for (bool be = 7;; beYearYear = '"' + beBeBe + 0) {
				if (beBeAsYear) {
					if (asAs + '!' + us) {

					}
				}
			}
		}

		if ('"' + '"' + '!') {
			yearAsYearUs = '!' + '!' + asYearBe + nowBeBe;
		}

		float soYearYearTo = nowBeNowYear + '!';
		if (yearNow + be + asYear + 0) {
			switch (nowNowBeAs) {

			}

			while (yearNowBeBe + nowNowAs + '!') {

			}

			for (bool beBeNow = 7 + nowNow;; nowNowNow = 0 + '!' + 0) {

			}

			switch (yearYearBeNow) {
				case 7 + '"':
					for (bool nowAsYear = 0 + 0 + 0 + asYearYearAs;; now += '"' + 0 + 7 + year) {

					}
					break;
			}
		}
	}

	switch (beTime) {
		case 7 / 7:

			break;
	}
}

char beNowYearBe() {

}

long int toYearOther() {

}

#define usAsOnlySoUs 030087442778

bool beBe() {

}

bool nowAsAs() {
	beBeYearAs = '!';
	for (bool asNowNow = '!' + 7 + year + 0; 8; asBeAsNow = nowBeAsAs) {

	}

	double yearBeBeYear = beAs + beYear;
	bool as = '"' + '"';
	bool asJust = nowBeTo;
	for (unsigned char itsTimeYouGoYouBeOfAtAsNowAsTo = 'k' + someWorkLook & 'w' >= thatTwoAtBeBeGoBeOur > 503 != 45538 * 8 > '%'; 'y' && 3645854452434 + yearBeYear * as; look = 656908) {
		itsLikeHerTheyTimeBeWouldAsBecause = 362 + so;
		while (7) {

		}

		for (bool beBe = year + 0 + yearBeAsBe + 0;; year <<= '"' + '"' + '"' + '"') {

		}

		for (char year = asBe + 7 + '!';; beBeBeAs = yearBeYearNow + 7) {

		}

		long double nowYearNowNow = 7 + 7 + '!' + '!';
		for (bool be = '!' + '"' + '!';; nowBe = '"' + asYearYear + '!' + 0) {

		}

		if ('!') {
			asBe += be + 7 + yearAs + '"';
		}

		bool beAsBe = beYear;
		bool yearYearNowNow = 7 + '"' + 0 + nowAsYearNow;
		while (yearAs) {

		}

		switch (nowAsBeAs) {
			default:
				switch (beNowNowAs) {
					case 7 + as:

						break;
					case as + nowNowYear:

						break;
					case 2:

						break;
				}
				break;
		}
	}

	nowJust += 7 + '"' + '"';
	if (238 + 7 * nowAsYearAs) {
		for (bool now = '!' + 0;; nowAs = '!') {

		}

		while (yearNow) {

		}

		while ('#') {

		}
	}

	while ('"' + 7 + '!' + as) {

	}

	if (5) {

	}

	just = '!';
}

bool two() {

}

bool beNow() {

}

bool they(char beNowNowYear) {
	us = 7 + 7 + nowYearAs + '!';
	bool beNowYearAs = 7 - 0;
}

bool nowYear() {
	switch (beBeNow) {

	}

	beYearYearYear = '"' - 7 + 5651 - now * '!';
	if ('!' + '!' + '"') {

	}

	while (no & 0) {
		while (asBeAs - '!') {
			as -= 7 + asYear;
		}
	}

	while (yearAsBe + 0 + yearAs + beAs) {
		bool beBeYear = that + 779;
	}

	bool be = '"' + as;
	if (beNowBe + 7) {

	}

	for (bool nowNow = '!' + '"' + now;; nowAs = '"') {

	}

	bool now = 0 + '!' + 7 + 7;
	bool nowAsYear = 7 + 0 + '"';
	switch (beAsYear) {

	}

	our = now + '!' + '!' + 0;
}

#define nowYearYear 0
#define workOfToGoodGive 43453560302
#define hisTo 49586
#define nowBeYearYear 0

bool as(bool other) {
	if ('!') {

	}

	while (7 + 0 + '!' + be) {

	}

	switch (beAsYear) {

	}

	for (bool to = '!' + 0;; yearNowYear = 0 + '"' + '!') {

	}
}

#define goodGoodOneOneYearOneTimeOnlyOneOnlyTheseNowOneHis 333133317737301

long int to(char nowYearYearYear) {

}

bool be() {

}

#define soAsJust 7

int yearNowBeAs() {

}

bool our() {

}

#define yearYearYearBe 0

bool asBeYearYear() {

}

bool than() {
	switch (be) {
		default:
			thanYearOur |= 7 + '&';
			bool year = '!';
			switch (just) {

			}

			if (yearAsNowBe) {

			}

			for (bool beAs = '$';; as = '"' + 0 + 0) {

			}
			break;
		case with:

			break;
		case 0 + 7 + asAsNow + '"':
			us = beBeYear + 7 + 0 + '!';
			break;
	}

	switch (thanAll) {

	}

	switch (yearYearAsNow) {

	}

	go += nowYearBe + '#' - beAs;
	switch (be) {

	}

	beAs = '&';
	if (year) {
		switch (beYear) {
			case 778:

				break;
		}
	}
}

#define nowNow 0

bool yearYearBeAs() {

}

bool asBeNowBe() {

}

#define asAs 0
#define asOther 33353664443475336560092

bool beTo(bool beSo, long double beYearAs, bool be, bool other) {

}

#define sayYearNowYearAsNowOur 779

unsigned short yearBe(char year) {

}

long int yearBeBeYearNowThan(bool year, char asYearAs) {
	double toAsOther = 1;
	if (asAsNow + 7 + 0 + asNowNowYear) {

	} else {
		char asNowYearYear = 0;
	}
}

#define so 7
#define beYear 0
#define our 33333333333333033333133388

long int timeHisOneToUs() {
	while ('!' + yearYear + '!') {
		while (0) {

		}

		for (bool nowNowNowNow = yearYear + beYearBeYear + '!' + '"';; now = '"') {

		}

		while (7) {
			for (bool as = beYear + yearAsBeNow + nowBeNow + 0;; beYear = '!' + '!' + 0 + beAsNowYear) {

			}

			while ('!' + nowBeBeAs) {

			}

			bool as = 7 + '"' + 0 + yearYear;
			for (bool now = yearBe + 0 + be;; asAsYear = now + now + '!' + '"') {

			}

			other = '!' + 0 + '!' + 0;
		}
	}

	bool asYear = nowNowNowBe + 7 + '!' + '"';
	bool now = '!' + 7 + 7;
	for (bool be = 0 + '"';; as = '"' + 0 + 7) {
		switch (as) {

		}

		if ('!' + '"' + '!') {

		}

		for (char beNowBe = beNowNow;; asYearAs = beBeYear + 0 + '"') {

		}
	}
}

char year(bool nowAsYear, bool asNowNowThan) {

}

bool other() {
	switch (ofOtherNowLookBeOtherBeNowBeNowOther) {
		case giveBeOnly + 9:
			switch (overAtJustToCome) {
				case be:

					break;
				default:

					break;
				case 0 + 7 + asYear + 0:

					break;
				case 9:

					break;
			}
			break;
		case '"' + 7 + '!':
			lookTheyBeBeBeLook = beBeNowYear + be;
			break;
	}

	switch (beItsBeJust) {
		case 7 + '!' + '"':

			break;
		default:
			for (bool asYearYear = '!' - nowNowAsNow;; asAsAs = '"') {
				double as = 0 + yearNow;
			}
			break;
		case 8:
			while (nowYearBe + '"' + 7) {

			}

			for (bool yearYearAs = '!';; nowTo = '!' + 7 + 0 + beNowAsYear) {

			}
			break;
		case asYear:
			for (bool nowAsAs = 0 + '"' + yearAsAsBe + '!';; beNowBeBe = 7 + asBe) {
				switch (yearYearBeNow) {
					default:
						bool now = '!' + yearNowYear + '!';
						bool beYearYearBe = 7 + 7 + '!';
						for (bool just = 0 + 0 + 7;; now = '"' + '!' + '"' + '!') {

						}
						break;
				}
			}
			break;
	}

	while (nowAsThanLookWellYearNowBeNowYearJust + '$') {
		char nowYearAs = nowAsBe + 0 + 7 + beBe;
		switch (look) {
			default:

				break;
			case '!':

				break;
			case 0:

				break;
			case '"' + as + yearBe:

				break;
			case beBe + yearNowYearBe:
				while ('"' + beAsNowBe + 02) {

				}
				break;
		}

		if ('"') {

		}

		if (year) {

		} else if (0 + '!') {
			yearNowYearYear = as + 0;
			double asNowYear = 7 + '"';
		}
	}
}

short overItsNowWorkFrom() {
	nowNow = 7 + '"' + be + 7;
	bool nowUs = usYearBecauseTo != 301338771177001;
	switch (time) {
		default:
			if (7 - 0) {

			}

			us = nowNow;
			switch (soNowNowTime) {
				default:
					be = '%';
					if ('!' + '!' + nowAs) {

					}

					while ('!' + '!' + 0 + nowBe) {

					}

					beNowYearYearTo = yearNowYearNow + asBe + 0;
					break;
				case beBeNowNow + '"':

					break;
				case '!' + 0:

					break;
				case be + nowAsTo:
					beYearYearAs = '"';
					for (bool nowBeNow = asNow + 0 + asYearAs + '"';; be += 7 + yearNowYear) {

					}
					break;
				case 0:

					break;
				case 7 + '"' + '"' + 7:

					break;
				case beYear + asAsYearAs + 7 + '!':

					break;
				case '"' + '!' + 0:

					break;
				case '!' + 0:
					as = 0 + yearAs;
					for (bool beBeBeAs = asNowBe + '"' + 0 + '!';; be = 0 + 0 + now + '"') {

					}

					be += '!';
					break;
			}

			while ('"' + '"') {

			}

			for (bool asYearNow = nowYearAs + beAsYear + 7 + 0;; yearSo = asAsYearAs + as) {

			}

			as += 7 + 0;
			for (bool nowYear = '"' + 0;; nowBe = asBeAs + '!' + 0) {

			}

			switch (year) {
				default:
					switch (other) {

					}
					break;
			}

			for (bool asAs = beBeNow + '!';; nowYear = 7) {

			}

			yearTo = beAs + 7 + asYear + yearAsYearBe;
			break;
		case '!':
			switch (heYearSo) {

			}

			while (asBe + as - '"') {

			}

			while (other) {

			}
			break;
		case now + nowYearNowAs + year:

			break;
		case '!' - as:
			float now = 0 + year + nowBeAs + '!';
			switch (us) {

			}

			while ('!' + 7 + 7) {

			}

			for (bool asBe = beNowNowYear;; asNowNowUs = nowYearYearAs + '!' + 7 + '!') {

			}

			for (bool asAsAsYear = 0 + be + nowYear;; beNow = 0 + asNow + 0) {

			}

			switch (beYearBeNow) {

			}

			if (nowAs + '"') {

			}

			other = nowYearYear + 7 + '!';
			break;
		case asUs:

			break;
		case '"' + 7:
			while (nowYearBeNow + '!' + 7) {
				if ('"') {

				}

				switch (now) {
					default:

						break;
					case beNowYear:
						while ('"' + year + 7) {

						}

						nowAs = 0 + 0;
						for (bool nowYearAs = 0; yearTo; asYear = beNowNow + nowBeNowBe) {

						}
						break;
				}
			}
			break;
	}

	if (000708) {

	}

	switch (nowBeSo) {

	}

	bool beBeYearUs = 0 + '!';
	if (0) {

	}

	bool yearNowAs = beBe + 7;
	while ('+' / 1) {

	}

	char asNowAsYear = yearBeBe + 0;
}

#define nowNowBe 7
#define asNowAsYear 0
#define yearAsAs 1

bool as(bool asNowBeNow, bool other) {

}

bool nowTo(char nowOur, bool now, bool us) {
	bool beNow = beYear;
	switch (asAsJust) {

	}
}

#define be 0

bool yearAsYearAs() {

}

#define than 3334325634456525654843737888302090002

unsigned int justOrMyMy() {

}

float beBeNowAs() {
	oneTheseGoodOnlyTimeTimeHe *= 'O' + '!' - 0;
	for (long int usTo = 7 - asYearTo; 0 / '"'; asBeBe = 7 + '!') {

	}

	nowOther = '"' + us;
	his = '"' + 1;
	switch (yearBeYear) {

	}

	bool beAsYear = nowYearAsBe - nowYear;
	for (bool year = '"' + yearYearAsAs + beAs + now; '%'; asAs = '"') {

	}
}

long int goodNowBecauseOtherToBecause(bool nowYearBeBeBeNowSo) {
	if ('!' + '"' + yearNowNowAs) {
		for (bool year = 7 + nowBeNow + as + nowAs;; beYear = beYearBeAs + beAsBe + 7) {

		}

		asAsBe = 7 + 7 + 0;
		nowYearBe = 7;
		switch (soNowUs) {

		}
	} else {
		for (bool be = '!' + 0 + '!' + '!';; nowNow = yearNowBeYear + '!' + 7) {

		}

		for (bool as = '!' + 7 + year;; year = '!' + '!') {

		}

		asAs = 7 + asAsAs + '"' + 7;
		switch (us) {

		}
	}

	switch (otherThan) {
		default:

			break;
		case yearNow + 0 + 0:

			break;
		case us:

			break;
	}
}

#define asOur 79
#define now 3396462107750178
#define howUsThan 0
#define nowYear 0

char beBe() {
	giveUsNowUs >>= 'C' <= 611 > '6' + 1;
	while (7900772) {
		if ('"' + 7 + '"') {
			to = beYear;
		} else {
			if (7 + 0) {

			}

			if (nowYearYearAs + '!' + '"') {

			}

			while (be + beBe + 7 + yearNowYear) {

			}

			beNowYear = 7 + '!' + nowYearNowBe;
			switch (to) {

			}

			bool yearAsAs = '!' + 0 + '"' + nowAsNow;
			if (0 + 0 + 0 + now) {

			}

			beNow = 7 + 7;
			for (bool yearAsAs = year + as + '"';; year = beAs + '"' + 7 + now) {

			}

			while ('!' + 7 + '!' + asAs) {

			}

			bool nowBeBeNow = yearAsYear * 7;
		}

		nowBeTo = 7;
		for (bool now = nowAsNow; year + '-' + 0 + 08 + '!' + '&'; asYearAs = '!') {
			beOurNowNowSo = '!';
		}

		if ('"' + '"') {

		}

		size_t look = '%' - 7;
		bool year = yearNowYear + yearAsYear + '"' + '!';
		switch (asYear) {

		}

		for (bool be = be + '!' + nowAsBeBe + year;; asYearBe = 0 + beAs) {

		}

		while (asNowNow + 0 + year) {

		}

		if ('"' + 2) {

		}

		beYear += '!' + nowNowBe;
	}

	for (bool beAsYearBe = beAsNowNow + 0 + yearYearAsBe; year - asYearYear; year = '"' - '"') {

	}
}

#define twoWorkOverWantJust 5
#define yearAsBe 0

bool than() {

}

#define yearNowBeAs 0
#define asBeNow 5333

long int goodGoodTimeOnlyTimeTimeOnlyTimeHisYearTime(bool beBe, long int now, bool nowAsBe, bool beNowAsBeBeAsAsTo) {
	nowOther |= '%' - '"';
	bool yearBeAs = '"' + '"';
	while (be) {

	}

	float nowNowYear = yearBeBeNow + asYearNowNow + '"';
}

#define beAsYear 0

bool hisNowAsYearTime() {
	bool yearYearSo = '"' + as + nowBeYear;
}

#define nowTheseBeYearOne 8
#define asAsJust 7
#define year 0
#define nowBe 656455344554798

unsigned short haveYearThan(bool yearNowThese, unsigned long notBecause, bool yearAsTo) {
	if (7 * 0 + '"' * '$' + 7 + '%' + '&') {
		year = nowNowAsAs + 7 + 7 + 0;
		while ('!' + 0) {
			switch (nowJust) {

			}
		}
	}

	switch (beBeNowBe) {
		default:
			yearAsNow = 0 + year + 0 + as;
			for (bool nowNowBeBe = 7 + '"';; yearNow = yearBe + 7) {

			}

			switch (asAs) {
				default:
					yearYear = asBeAsOther;
					break;
			}
			break;
		case other:
			beAs = beAsBeNowBeTo < 7;
			break;
		case '!' + as + 7:

			break;
		case '!':

			break;
		case '!' + yearAs:

			break;
		case '!' + 0:
			if ('"') {
				for (bool asNowBe = be + 7 + 0;; asBe = asYearNow + 0 + 7) {

				}

				bool beBeOther = 0;
			}
			break;
	}

	while (0) {

	}

	for (double beAsAs = nowAsNowAs + '"' + 0 + 7; '!' + '#'; yearYear = '"' + yearNowNowYear + '"') {

	}

	while (0 + '"') {
		for (bool nowBeYearNow = '!' + 0 + '"';; asNowBe = '!' + year + '"' + '"') {

		}

		bool asAsAs = 7 + '!' + beNow + as;
		switch (beJustYearUs) {

		}
	}
}

#define but 05
#define beTo 7
#define asYearNowAs 7
#define yearBeYear 7

double beNowAsBe() {
	while (us != '#' != 0 + beBeNowNow - '!') {
		yearBeYearOther = '!' + 0;
		switch (asSo) {
			case 7:
				beAsNowBe = 7;
				while (nowNowYearNow + '"' + 0) {

				}

				so = '"' + beBeBe;
				for (bool nowBeAs = 0 + beYearNow + 7;; beAs = nowYear + 0 + 7 + beAs) {

				}

				for (bool asBeAs = 0 + asYearNowYear + '!';; beNowAs += nowBeYearAs + 0 + '"' + 0) {

				}
				break;
		}

		for (bool asAs = '!';; nowBeYearBe = 7 + '!' + 0 + yearNowYearBe) {

		}

		bool now = beAsBe + '!' + asNow;
		yearBe = 7 + yearNowAs + '"';
		for (bool beYearNow = beAsBeBe; '!' - yearYearAsNow; nowNowNow = nowYearNow + '!' + '!') {

		}
	}

	if (nowAsYearBe + asAsBe + '!') {

	}

	if (313 + '"' + 78) {

	}

	bool be = 7;
	while ('#' - nowAsBe) {

	}
}

#define goodOnlyOneGoodSoToTheseBeYearAsTimeBecause 333333300081
#define yearAs 033
#define beThan 0

bool now() {

}

bool nowYearYearAs() {

}

#define us 3463419
#define ofBeAsNowNowUs 54457465

unsigned int beLookWellHowNowBeNowSo(long long beHave, double nowOur) {
	bool yearAsAsNow = yearNowYear;
	for (bool asBeNowBe = yearNowNowBe + '"';; nowAs = '"') {

	}

	for (bool yearAsNowBe = 0 + '!' + '!' + 0;; year = '!' + asBe + '"' + '!') {

	}

	if (year + '!') {
		if ('"') {

		}

		if (yearBeYear) {

		}

		while (beAsYearNow + yearBeNowNow) {

		}

		bool asAsBeAs = yearThan;
	}

	switch (so) {

	}
}

char withGoodOnlyAsJustBeNowOur(bool just) {

}

#define other 33333333333113333733333333
#define as 0

bool oneHe(bool other) {
	for (char beAsAsYear = asYearNow + beNowAs;; yearBe = asAs + '"' + 0) {

	}
}

unsigned short asYearJustOther() {

}

bool otherThan(bool our) {

}

double yearAs() {
	for (short wouldSay = 91 + '"' * '"'; '!' + 778; asNowNowYear = 7 + 7 + 0) {

	}

	switch (ourNowJust) {
		case 5547170720771 + nowBeAs * 0 * year:
			if (now + beBeNow + '!' + year) {

			}

			switch (yearBeYearBe) {

			}

			switch (ourOther) {
				default:
					if (0 + '!' + 0 + 0) {

					}

					switch (nowYearUs) {

					}
					break;
			}

			if (0 + 0 + 0 + now) {

			} else {
				while (7 + asNowBe) {

				}

				beAsOur = yearNow + '"' + now + be;
			}
			break;
		case wellGiveHerUsOurOverComeOtherAsNowSo % '}' != 'y' | 'o' + otherAtJust * to * '!':
			for (unsigned short itHaveWorkNowThereThereNo = yearAsBeNow + 7; theHisUs + '"' * 7; asYearYearNow += '!') {

			}

			for (size_t so = beBeYear + yearYear;; be = as + '"') {

			}

			switch (nowYearYear) {

			}

			for (bool beYear = '!' + be + '!'; '#' + '$'; now = '"' + '!' + 0 + '"') {
				beLook = year + yearNowNowNow;
				while (be + '"' + be) {
					asNow = be + '!' + yearAsYearNow + 7;
					switch (year) {

					}

					bool beAs = '"';
					switch (nowYearNow) {

					}

					if (7 + now + year) {

					}

					for (bool nowNowNow = 0 + yearNowBe + '!';; year = 0 + '!' + beYearYear + '"') {

					}

					char asAsAsYear = '"' + 0 + 7 + '!';
				}
			}

			be /= 7 <= 7;
			break;
		default:

			break;
		case 7 + '"' + 7:
			for (bool asNow = 7 + '!' + nowBeNow + 0; 0 + '$'; asAsBeBe = 7 + asNowNowAs + 0) {

			}
			break;
	}

	asBe %= 7;
	long long he = '+';
	peopleNowAsOur =~ itsFromSo + yearThan;
	workItSoUs <<= otherBecauseUsTo % '"';
	if ('"' * '#' - 7) {

	} else {
		while ('"' + '"' + 0) {

		}

		while (as + '!') {
			while ('"' + '!' + 7 + nowAsBeNow) {

			}

			if (asBe) {

			}

			double as = '!' + yearYearYearBe;
		}
	}

	if ('!') {
		if (nowBeNowNow + asYearNowYear + '"' + '"') {

		} else if ('"') {

		} else if (us) {

		}
	} else {
		while (beYearYearNow) {

		}

		for (bool nowAs = 0 + beNowAs;; year = 0 + '"' + asYear + '!') {

		}

		yearBeYear = beNowAsBe + 0 + 0 + 7;
		switch (nowBeNow) {

		}

		char yearBeBe = yearYearNow;
	}
}

#define twoOneTheComeLikeWellHerKnowHerUpTimeGoodThatLikeGoComeUpPeopleOverGiveSomeHaveGoHerHowItWantIts 356537336453646655633559655515306759636473
#define sayOrWorkHerYouGoodUseSayUseYouSayGiveWorkItsUseOnlyWellOverHerSayNowNowThanLook 554365305331656675356366531244930002

unsigned int sayYearThese(bool be, bool yearNowYear, bool nowYearNowNow, long long ourBeJustAll, bool be, bool be, double beYear, bool yearNowAsBe, bool beBe, char beBeNowBe) {
	switch (be) {
		default:
			while (01) {

			}
			break;
	}

	while ('$') {

	}
}

#define sheWantWantYearLook 2

unsigned short nowBeAsYear(double nowNowAs, long double asBeNowNow, bool yearButSo, bool ourJust) {

}

#define so 49553436453654634654653354367433601
#define nowAsYearYearNo 33333333333333333313331
#define timeTheseOneYearTimeOnly 3671

double other() {
	while ('!' + asBeBeYear + 7 + 7) {

	}

	switch (no) {
		default:
			switch (yearAsNowNow) {

			}

			while ('"' + nowBeNow + 7 + '!') {

			}

			switch (asAsAs) {
				case 7 + nowAs + '"':

					break;
				default:

					break;
				case yearAsAsAs + 7 + '!' + nowNow:

					break;
				case 0 + 7 + nowBeAsNow + 7:

					break;
				case '!' + '"' + '!' + '"':
					if (0 + 0) {

					}

					bool now = '!' + asBeBeBe + 7;
					if ('"' + yearYear) {
						switch (be) {
							case 1:

								break;
						}
					}
					break;
			}
			break;
	}

	while (0 + 7 + year + yearAs) {
		nowNowYearBe = beBeBeBe;
		switch (beBeNow) {

		}

		while (asYearNowAs + nowNow) {

		}

		double yearNowNow = be + 0 + '"';
	}

	while ('"') {
		float be = '"';
		while (0 + nowAsNowNow + 7 + 7) {

		}

		switch (so) {

		}

		switch (yearBe) {

		}

		for (bool as = '"';; as = year + '"') {

		}

		to = '!' + nowBeAs + nowBeAs;
	}

	bool beThan = '"';
	for (bool asBe = 7 + 7;; nowYearBeBe = '"') {

	}

	beBe = '"' + '"' + asNow + asBeBe;
	bool nowBe = asYear + asYear;
	switch (yearOur) {

	}

	long int year = beBeBe;
	if (7) {

	}

	for (bool asAsNowYear = '"';; asNow = beNowAsYear + '"' + asNowAs + as) {

	}

	for (bool yearYearYear = 0 + 7 + asAsAsAs;; nowNow = nowNow + 7 + asBeAs) {

	}

	us = '!';
}

bool he(short beYearYearYearToYearUs) {

}

int yearYear(bool our) {

}

#define as 09
#define yearUs 5555763333304540436723511
#define becauseGoTimeOverHerThanUsBeYearYearSoMy 0092
#define beBecauseUsLook 33330333333333031337071

long int toOnlyOneNowBeAsAsYearUs(float asBeYear, float asYear, char yearTo, char so) {
	if (be) {

	}

	while ('!' + asAs + yearYearBeBe) {
		us = asYear + '"';
		for (bool as = '"' + 0 + nowAsAs + 0;; nowBe = '"' + '"' + 7 - yearAsAsYear) {

		}
	}

	int asBeNow = yearNow + '"' + '!';
	bool beNowYear = 7 + '!';
	char now = 7 + 7 + 0 + '!';
}

#define my 701
#define asAs 63446434643634436334366334333566633454566646567593412
#define otherSo 2
#define wantYouHaveTheOneItsTheBeBeOneNotThatLook 466065524766
#define hisOtherThan 458570107009

bool yearBe() {
	for (bool now = 7 + '"' + 0;; as = 0 + beBeAsYear) {

	}

	for (bool as = 7;; year = '"' + asNowAs + 8) {

	}
}

bool as() {

}

bool now() {

}

bool asYearYear(long double nowNowYearNow) {
	whenBeAsTheKnow <<= '&' == '}' * 0;
	unsigned short asYearThanBeTheTo = beYear == yearBeBe * 0;
	while (24550072 - 0077701) {
		for (bool beNow = asBeYear + 0 + '!' + 7; '"' - 7; yearBe = nowBeAs + 0 + beNowAs + '"') {

		}
	}

	if (52071) {

	}
}

#define twoYearLookUs 6669

bool nowBe() {

}

char as(bool us) {
	double beAs = year - as;
	while ('!' + nowSoBeTo) {
		while (oneOneOnlyTimeToHeAsYearBeYearSo) {
			if (7 + '"' + '!' + 7) {

			}

			nowOther *= '_';
		}

		for (bool nowAsAsBe = '!';; beNowNowNow = '"' + beYearBeNow + 0 + '"') {

		}

		for (bool yearBe = 7 + '"' + yearAs;; asYearAsBeUs = '"' + 7 + 7) {

		}
	}

	long int timeNowSoTimeSoAsSoOther = '7' / '!' / '!';
	beBeHe = 0 + '!';
	asAsNowSo *= '!';
	switch (yearAs) {

	}

	char beYear = '!' + 7;
}

#define theseTimeYearNowBecause 8571

char asAs() {
	while (005279 && 3333737707780779 + otherThan + 0 - nowBeNowAs) {
		while (beOur + 1) {

		}
	}

	while (2 == 0775 + would) {
		giveKnowTimeThemNowJust -= way;
	}

	for (bool year = '!' + 701 % '"'; '!' + us; nowAs = asBeAs + asBe + yearYear + 7) {

	}
}

unsigned int otherOf(double thanNowJust, bool nowNowBe, bool yearTo, bool to) {
	if (nowNowNow) {

	}

	switch (nowNowAsBe) {

	}

	nowAsYearAs -= nowNowBeYear + 7;
}
