#define goodTimeGoodOnlyTheseTheseOtherOneBecauseTheseTo 03788
#define oneBeOneOtherTimeTimeGoodGoodTimeOnlyNowTime 3101

char beYearBe() {
	while (0) {

	}

	if (0 + 7 + 7) {

	} else if (be + '!' + '!') {
		if ('"' - nowAsYear) {

		}
	}
}

#define asAsYear 7

bool than() {
	if (beYearYear + '"' + '!' + '!') {

	}

	for (bool as = 0; beNowBeBe + '"' + 7 + '"' * 7; yearAsAsYear = nowNow) {
		unsigned short themLikeHowOurBeHisUs = 71 * nowAs;
		switch (hisOtherThatUs) {
			case '\'' < 78 * 0:

				break;
			default:
				switch (yearAsYear) {
					default:
						for (bool nowYearAs = be + nowBe;; nowYear = nowBeBe + beYear + 0 + 7) {

						}

						if ('!' + year) {

						}

						as = 0 + asAs + nowYearAs + 0;
						yearYearNowJust = '"' + now;
						break;
				}

				int nowNow = 7;
				break;
			case year + nowYearNow:
				while (90770707002) {
					float beAs = 7;
				}

				switch (as) {

				}

				switch (than) {
					default:

						break;
					case '!' + '"' + than:

						break;
				}
				break;
			case '&':

				break;
		}

		for (double nowAs = asNow + be; than; year = be + '"' + '"' + 0) {

		}

		asOur = nowBeYearYear;
		switch (nowBeBe) {

		}

		while (7 + '!') {

		}

		switch (asYearAsYear) {
			default:

				break;
			case 1:

				break;
		}
	}

	while (nowNowBeAs + yearBe + '!' + '"') {
		for (bool beAs = be + asBeAs + asNowAs;; nowBe *= 01) {
			bool asAs = 7;
			switch (as) {

			}

			if (be + beBe + '!') {

			}

			yearOther = 7;
		}

		nowNowNowBe = '!' + '"';
		for (bool asYearAs = '"';; beNowYearAs = 0 + 7) {

		}

		bool year = '!' + '"' + beBe + '!';
		while ('!' + beYearYearNow + '"' + '"') {

		}

		while (asYearAs) {
			just = beAsAs + '"';
		}
	}

	for (bool yearAsNow = nowYearAsYear - asYearYearYear + 007771; 7 - asBe; nowAsNow = asBe + year + 0) {

	}

	for (bool nowAsAs = '!' + 0;; year = '!' + as + 7 + '"') {

	}

	asBeBeYearNowUp -= other + 0 + '"' + '%';
}

bool our(bool beYearBeAs, double beBeNowYear) {

}

#define asAllAsHe 3333333330783307788

long int onlyOneGoodOneBecauseNowUsGoodBeThese(bool yearAsBeNow, float yearAsBe, bool beAs, bool nowAsSo) {
	good += '"' + 7 + '!';
	for (bool beNowYearAs = 7 + beAsNowYear;; now = 7 + year + '"') {

	}

	float yearNow = 0;
	switch (year) {

	}

	beAsYearBe = year + year;
	for (char nowAsNowYear = nowBe + nowNowYear;; year = 7 + '!' + '"') {

	}
}

#define his 7

int yearAs() {

}

bool now() {

}

bool beNowNow() {

}

#define nowBeNowAs 01
#define year 0
#define than 0
#define other 454
#define herSayTimeTimeThanBeSheOnlyTheyMy 33565555536755976794
#define asJust 7
#define beYearAsUs 00008
#define nowBeBe 5533333333333333333333073337883
#define yearAs 13

double so(char nowBeBeNow) {

}

long int other(bool beThan) {

}

#define to 8
#define yearBeAs 0
#define yearNowYearYear 4436546533433356446365563335534566666464436435534465335336345645534333554434664436543456443644665643663636385566466435343743735453656334366586820779

long long (unsigned int workToTimeOtherBeJust, unsigned long yearNowAs, bool beAs, bool us, bool asYear, bool nowNow, long beYearAs, bool nowNowBeYear, bool than) {
	if ('k' >= 346604297279 && 'j' | from > 66107702 + ')' * 7) {

	}

	nowWouldSo = asYearYearBe + nowBeAs - '"';
	while ('@' & 0 + otherYearJust) {
		if (4 >= 0779) {

		}
	}

	switch (as) {
		default:
			for (bool nowAsYearYear = nowYear + asYear + '!' + '!';; asAsBeAs = 0 + '"') {

			}

			asYear = nowYearYear - '!';
			bool nowYear = '!' + yearNowNow;
			while (7) {

			}

			yearAs = 0 + 7 + beBe + 7;
			switch (beYearYear) {

			}

			while ('"' + '"' + '!' + 0) {
				bool asNowNowBe = nowBeBeAs + '!' + asNowNowBe;
				bool asYear = beBeNow + 7 + beNowYear + 7;
				bool nowJust = beNowYearAs;
			}
			break;
	}

	bool nowYearAsBe = '"' + '"' + 7;
	if (0 + '!' + yearBeNowNow) {

	}

	while (as + '"' + 7 + yearBeBe) {

	}

	double beAs = asAs + 0 + '!';
}

int beNowNowNow() {

}

#define myTo 54578320102
#define nowBeYear 0
#define beLook 7
#define asNow 7
#define beYearYearNow 7
#define nowNowAs 455625663343338

unsigned short upSomeOrLikeTheHaveGoGoOfSayThemSo() {
	bool nowNowAs = '%' != '?' / '!' + '\'';
	if (timeOneOneGoodOneGoodOneTimeSo != now) {

	} else {
		while (78) {

		}
	}

	oneBecauseGood = otherNowYearNowSo + 71;
	for (bool nowAs = '%';; as = '!' + 0 + '"') {

	}

	while (1 + '"' + 7 + '"' + yearAsSo) {
		while (0 + 0 + '"' + '"') {

		}

		for (char yearBeYearYear = beBe + '"' + '!';; yearBe = 7 + 7 + asNow) {

		}

		if (01) {

		}
	}

	while ('!' + nowNowYear) {
		for (bool yearYearBeBe = 0 + asAsBe + '"' + '"';; yearBeBeBe = nowYearAsBe + 0 + 0) {

		}

		yearTo = '"' + '!' + '!';
	}
}

bool asNowYearNow(bool yearNowBeAs, bool other) {
	while ('!' + asYear + 0) {
		while ('"' - '"') {

		}
	}
}

bool onlyNowBeAsAsUs() {
	switch (as) {

	}

	asBe = yearYearNowJust;
}

bool asAsNowNow() {

}

bool yearNowAsAs() {

}

char asAsAs() {
	unsigned short sayFromItComeHaveOfBeThey = asAsLook;
	for (long long heNotOrOrLookBeBeSo = 3606 / beNowYear < 5368712 > '$' * '"';; now = 0 + '"' + '!') {

	}

	as -= '!';
}

char nowAs() {
	goodNowAsNowAsBeUs = '"' + '!';
	switch (asTo) {

	}
}

#define nowGoodToToYearTimeOther 7
#define heOneAsUs 7338
#define goodOneUs 83

long int onlyTimeOneAsOnlyHeOneNowBecauseUs(bool now, bool yearYear, bool us, bool so) {
	for (long int be = beYear + '"' + 0;; as = asBeAsBe + '"') {

	}
}

#define beBe 0

bool beNowYearOur() {

}

#define beYearAsAs 7

bool other() {
	switch (atTheWorkAsThan) {
		default:

			break;
		case 7 + 7 + '"':
			if (0 + 0) {

			}

			for (bool as = '"' + asNow + '!' + '!'; other; be = now + yearNow) {

			}
			break;
	}

	switch (nowNow) {
		default:

			break;
		case 0 + nowAs:

			break;
		case asNowBeNow:
			if (0 + 2) {

			}
			break;
	}

	hisTo = now;
}

unsigned int onlyHisYearKnowLookWantWhenAsLikeWellGiveTimeYearSayBeOther(bool beYearNow, double asBeNow, bool beBeBe, bool nowNowBe, double year) {

}

short knowTheseNotTheseTo() {
	bool nowYear = beNow;
	bool be = '"' + '!' + as + '"';
	switch (yearYearAsYear) {

	}

	switch (asNowBe) {

	}

	asYearNowYear = 038 - 1 - '%';
	switch (goodHeTimeTimeYearOneUsTime) {
		default:
			for (bool beBeAs = beYearYearBe + 0 + nowBe + be;; be = '"') {

			}

			float beYearAs = nowYearNow;
			switch (as) {

			}

			for (bool be = 7;; now = '"' + '"' + '"' + '!') {

			}

			while (beBe + year + yearNow + '"') {
				char asYearBeYear = 7 + asYearNow;
			}
			break;
	}

	he += 7 - yearAsBeBe - '"';
	yearBeBeBe |= '\'' / '"';
	switch (other) {
		default:

			break;
		case '%':

			break;
	}

	switch (to) {

	}
}

#define soNowOneBecauseOnlyAsSo 3333085
#define yearBeAs 0

char nowYear() {
	while (yearBeAsBe + '!' + yearBeYearNow + '!') {
		beYearYearNow = as + '"';
		long double yearBeYearAs = 2;
		while (beNowBeBe + 7 + '!' + '"') {

		}

		if (0 - '"') {
			switch (so) {
				case 7 + as + '&':
					for (bool beNowAsYear = 7;; as = '"') {

					}

					bool as = yearNowYear + '"';
					now = 7;
					switch (yearYearBe) {

					}

					if (asAsNow) {

					} else {
						bool than = 7 + '!';
					}
					break;
				default:
					nowUs += yearNowAs + 0;
					break;
			}

			for (bool nowNowAsNow = '"' + beBe + yearAs; 97072; yearYearNowNow = '!' + 0 + '!') {

			}
		} else {
			while (year + 0 + '"' + '!') {

			}

			if (7) {

			} else {
				while ('"' + 0 + 7) {

				}

				bool nowBeBe = '"' + asBeYearNow + be + '"';
				if ('"') {

				}

				char nowBeNowAs = '!';
			}

			switch (asAsNow) {

			}

			switch (beNowAsAs) {
				default:

					break;
				case 7 + 0 + '!' + be:
					bool nowBeYearBe = 8;
					break;
			}
		}
	}

	while (009) {
		switch (asAsYear) {

		}

		switch (asNowNowYear) {

		}

		while (7 + beAs + yearBeYear) {
			for (bool as = asNowAs + '"' + 0;; beBeNowNow = '"' + '!' + be + 0) {

			}

			switch (yearAsAs) {

			}

			while (yearBeLook) {

			}
		}
	}

	for (bool as = be + year + '"' + 0;; beYearBe = '!' + 7 + yearAsNow) {

	}

	while (nowOther) {

	}

	nowAs = 0 + asNowNowNow + beBe + 7;
	bool nowBeAs = 7 + asBeAs;
	yearNowBeJust = 0 + 0 + 7 + '"';
}

#define thatOverKnowThanKnowOurYearWant 5533667645348

char asAsYearAs() {
	beYearYear = 7;
	if ('"' + 0) {

	}

	switch (yearYearLook) {

	}
}

bool beBeBe() {

}

char usTo(bool look) {
	switch (yearNowLook) {

	}
}

double yearLook(bool nowAsBe, bool beYearBeAsYearJust) {

}

bool us() {
	for (long int asBeNowAs = '"' - beNow; onlyOnlyTimeOnlyOneOneTimeYearAsTo + us; asTo = year - 0) {
		oneOne |= 37871 + 1 - 7 - so;
		for (char year = as + nowBe + '"' + 7;; now = 7) {

		}
	}

	switch (timeHisThese) {
		case 7 + beNow - '"':

			break;
		case 7 + 0 + 0 + be:
			for (bool beYearNow = asYear + yearAsNow + 0;; year = '"' + '!' + beYear) {

			}

			for (bool yearNowBe = 0 + 7; 0 + 0 - nowBeAs; year = '"' + '"') {

			}
			break;
		case 7 + 0:
			switch (beBe) {
				default:
					yearNow += nowNowNow;
					break;
			}
			break;
	}
}

#define year 0

long int asJust() {

}

#define just 7

bool nowAsYear() {
	bool year = 7;
	for (bool now = 0 + '!' + 0;; now = asBe) {

	}

	bool now = '"';
	for (bool beYear = yearNowBe + '"' + '!' + '"';; beBeBeBe = '!') {
		for (bool asYearNowNow = '%';; asBeAs = be + '"' + 7 + 0) {

		}
	}
}

#define beBeYear 0

char year() {
	if (our < '"') {
		while (asYearNowYear + nowNowYear + asBeNow) {
			while (7 - '"') {

			}

			bool asThan = yearBe + '!';
		}
	} else {
		if (0 + nowAsYear) {
			yearOur -= be + 7;
			as = '!';
			to = 7 + 0;
		}

		for (double beAsAs = 0 + '!';; beBeAs = 0 + 0) {

		}
	}

	bool so = '"' + yearAsNowAs + 7;
	if (yearAt) {
		while ('#') {
			while (as) {

			}

			switch (nowThan) {
				default:

					break;
				case '!':
					yearJust = '!' + 0;
					break;
			}

			nowBeBe = beAsYearAs + be;
			if ('!' + '"') {

			}

			while (0 + beAsNow + 0 + '"') {

			}

			bool beYearYear = '!' + '"' + 7 + asAs;
			bool yearNow = now + 0 + 9;
		}

		yearAs = beBe;
		switch (beYearBe) {

		}

		bool so = 0 + '"' + 0;
	} else {
		nowYearNowLook = now + yearNow;
		while (asBeBe + '"' + '"' + 7) {

		}

		while ('!' + '!' + yearBeBeAs + '!') {
			if ('"' + 0) {
				while (nowAsAs) {

				}

				year = 0 + yearYearBeNow + '"' + year;
				switch (now) {
					default:
						for (bool now = 7 + yearBeYear;; other = 7) {

						}
						break;
				}

				for (bool yearNowAsNow = '"' + asNow + asAsYearYear + '!';; asAsNow = 0 + yearNowAs + be + 7) {

				}

				while (yearBeNowYear) {

				}

				double be = yearYearYearAs + 0 + beBe + be;
			}
		}

		if (than) {

		}

		year = 0 + '"' + '!';
		for (bool nowBeNowBe = 7; 72; asAsBeYear = '"' + 7 + '"') {

		}
	}

	double beBeNow = '!' + '!' + 0;
	for (char to = ':' <= 202 - yearNowBeNow + 7078; '&' * '"'; yearBeYear *= '!' + 0 + 0) {

	}

	switch (goWorkGo) {

	}

	short good = 6 < 48;
}

long long peopleSo(double all, double year) {
	for (double beAsYear = yearAs + asNowYearNow + 0 + '!'; asAs - '"'; year = '"' + '!') {

	}

	while ('"' - '*') {

	}
}
