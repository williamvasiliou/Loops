#define onlyGoodTimeOneBeTimeOneOneOnlyTimeHeTimeBecause 3333383111
#define nowAsBe 7
#define us 3
#define orWould 41

bool nowYear() {

}

#define beAsAs 7
#define yearNow 19
#define beYearBe 7

bool beBe() {

}

bool as() {

}

bool nowAsLook() {
	long int whenUpThemAsSheHerSheHerThemThan = '}' || twoLikeTimeThatOverButNoNowItsJustOtherSoYearNowBeOther - beHeTo / usOther;
	while (knowSheLikeJustOfWayBeUpGiveHerOrSoWayAsNowSoThese >= 'e' || 4 - 'r' && 3 + '&' + '&' - '!') {
		they |= workThereSomeLikeWorkTheTimeHeThatThatItsToJust != 'j' | '_' || 'y' != overHisThan || as + yearAs + '!' + nowBe + beLook;
		at = asBeNowYearNowTo - 0;
		nowNow = 479 || 0;
	}

	for (unsigned long itsGiveOverYouLikeOrSheItSomeOverOnlyLookOrThatOverLikeHowOneBeItItsOfOneTwoWorkKnowOfGoodTimeKnowWantTwoHerYouHerButTheWellGoodAllOrMyNowAsBeJust = '#' / '0'; 3564533653465337663345355293 != beNowThan || whenWantOnlyWellOfOneSheKnowOnlyWantSomeButYouWantKnowWorkOurOneGoWorkNotWithUsBeThan || 'o' && 'j' || 445363319075 < 7598 || '8' + be - other; sheOtherOnlyWorkGiveOrTimeKnowThan .= likeOrOtherWorkThereOverHaveUpOnlyBeWhenBecauseToGoodItsGoodNowJustWould * '"' != beToThan * yearYearAs) {
		yearBeOur = '"';
		while (7 + '"') {

		}

		while ('!' + 0 + nowAs) {
			switch (year) {
				default:
					yearNow -= nowAs + 0 + 7;
					break;
			}

			while (beYearYear + '!') {

			}

			while (be + 7 + 0) {

			}

			if ('"') {

			}

			yearNowYear = '"' + 7;
			bool asNowYearAs = asBeNowYear;
			asYearBe -= asAsBe;
		}
	}

	bool be = yearAsNow + beAsAsYear;
	if (twoBeGoTimeSheHerOneHaveHowSheOrOneWouldLikeOneOurLookSo == onlyItOrWhenWhenHerWhenSayTimeWantGiveItSheGiveOneToOfOnlyOverThereUpSheTimePeopleAsThatLikeThatWithUsOverTwoAtJustGiveOtherThan > timeWellGive / '!' && noNotUs > 0) {
		while (7) {
			for (bool yearYearAs = nowAs;; now = '!') {
				if ('&') {

				}
			}
		}

		if ('"' - '"') {

		} else if (be * '!') {
			while (so) {
				bool so = 7 + 0;
			}
		}

		char yearAsYear = 7 + '"' + as;
	} else {
		switch (heLook) {
			default:
				switch (usOther) {
					case '%':

						break;
					default:
						bool asYearBeAs = 0 + '"' + '"' + beYearAsNow;
						bool beAs = year + 0 + be;
						for (bool asYearAs = '!' + 7 + 7;; now = '"' + '"' + '!' + 7) {

						}

						beNowYearAs = 0 - 7;
						break;
					case year:
						while (0 + nowBeBe + '!' + 7) {
							yearNow = '!' + yearAs + 0;
							for (bool now = beBeYearNowBeLook;; beYear = '"') {

							}
						}
						break;
					case '"' + '!' + 7 + '"':

						break;
					case beNow:
						double yearBeNowBe = 0;
						break;
				}

				asNow -= asAs + '"' + '"' + nowAsNowYear;
				break;
			case '!' - 7:
				asNowNowNow = 7;
				switch (nowAs) {
					case beJust:

						break;
				}
				break;
			case beYearBeAs * 0:
				for (int yearNowYear = be + '!' + '"';; be = yearAsBeBe + asYearBe + '"') {
					as -= 7 + 0 + beYear + beYearBeYear;
				}

				char nowAsLook = as + 0 + '!' + '"';
				if (7 + '!' + 0) {

				}

				yearBeAsSo = asBe;
				while ('!' + '!' + 0) {
					long be = '!' + beNowBeAs + '"';
				}
				break;
		}

		long long soAsNoThanAsNowTheNowBeAsWould = 2 - asBeOtherNowNowBeYearNowNowYearThan;
		while ('!' + '!' + '!') {

		}

		switch (nowYearBeNow) {
			default:
				if ('"' + yearAsBeBe + '!') {

				}

				if ('&') {

				}
				break;
		}

		while ('"') {

		}

		now = asNow + '#';
		switch (asOur) {
			default:
				for (bool beBeNow = '"' + '"';; now = yearAsNow + 7 + 7) {

				}

				switch (our) {

				}
				break;
		}
	}

	long int usThatThatWantUse = ofBeYearBeUsOur <= 0;
	while (',' || yearBeBe + yearNowSo + asYearBe + yearNowLookUs) {
		for (bool our = 0 + beBeAs;; asAsYearAs = beYearBeBe + '!' + '!') {

		}

		long knowYouOne = 0 + '"';
	}

	switch (whenGoThemBeAll) {
		default:
			long long beAsBeAs = 0 + '"' + as + '!';
			break;
	}

	while ('!' + 0 + 0 + '!') {
		just = '"';
		bool beNow = '!';
		if (our) {

		} else {
			if ('"' + '!' + 7 + '"') {

			}

			switch (yearNowBeNow) {
				default:
					while ('!' + '!' + '"') {
						while (yearNow + asBeYearBe * '"') {

						}
					}
					break;
			}
		}
	}

	for (bool yearBeYearAs = '$';; beYearNowAs = 0 + '!' + 7 + 7) {

	}

	switch (beBeBeNowBeSoNowLook) {

	}

	bool asNow = '"';
	if (beBeNowAs) {

	} else {
		while (0 + beYearNowThan) {

		}
	}

	yearYearAs -= beYearBeBe + nowYear + 7;
}

unsigned long orSomeWorkThatOrWorkItTimeWantWouldWellKnowTimeSayOverWellThatGoLikeOurOverHaveTwoGoWhenSayHowPeopleTwoSomePeopleUseThereTheyMyUseGiveUpSheUseOrYouComeSayOrSayWellSomeWell() {
	if (nowYearAs) {

	} else {
		bool beAsAs = asAs + 0;
		while (asBeYearBe + 8) {

		}
	}
}

unsigned char herOverUseItOrGoHaveGoBeWellThereHaveThatWorkWhenThereItsOnlyGoodThereHaveThatOfWorkYearThereBeTheyThatHaveItsYearBeLookAsSoYearBeNowAsNo(short asHisLook, bool asNowNowLook, bool asNow, bool asAsAs, bool yearAsAs, bool asNow, bool be, double nowAsAsYear) {
	nowUsBeYearAsYearThan -= nowUs - 7;
	bool yearSo = 0 - as - '!';
	so = yearYear;
}

unsigned short workUsOrHaveThanItsOneSoUsYearNowAsYearWith(long long to, double asNowSo, unsigned int overOfThatWayYearSayToAll, unsigned long atBeNowOur) {
	someThanYouWellHisYearSoOurTheseButLookTheseWithUs <<= '~' & twoTwoUsPeopleItThereUseTheSheBeGoNowComeOverOtherUpWorkSomeAsWouldSo | workUseGoodWorkOfGoodWorkOneFromYearLookNowHe * 02;
	itsTheyYearButThey ^= youComeItThereButNowGoodYearAsYearOtherOther <= 7 <= '"' + 02;
	unsigned long beJust = nowNowNowNow <= 702;
	for (bool asAsBeBe = 7 + yearAs + nowAs + 0;; year = 0 + asYearYearYear) {

	}

	switch (asYearBe) {

	}

	while ('"') {
		beYearNow = '!' * '%';
	}
}

#define soLikeItsKnowSaySheYearAsOfUseGoOverComeGiveKnowOverWantGoUseGoGiveGoodItTwoLikeYouItHaveGoKnowItsThatHowWorkUseOnlyWouldGo 4468

unsigned char yearOtherYearUsJustYearLookAsThey(bool so) {

}

#define beOneThereJustLikeBeAsLookAsThan 61

int yearNowNowBe() {

}

#define withWhen 057709

bool be() {

}

#define now 0

bool asYear() {

}

#define yearYearNow 0
#define other 6561555333468265700579

bool upThatWayJust(bool come) {

}

double itKnowTo() {
	nowAsAsUs = nowNowAsBe + 0;
	bool now = beAsAsNow + 0 + asBeYear + beAsAsYear;
	while (as + '"' + '!') {

	}

	while (now + 0 + 7) {

	}

	for (bool now = year;; be = '!' + '"') {

	}

	bool beAsBeYear = '!' + asBeAsNow;
	for (bool as = 0 + '"' + yearYearNow;; beYear = 0 + '"' + nowAsBe + nowAsNowAs) {

	}

	switch (yearYear) {

	}

	switch (now) {

	}

	switch (year) {

	}

	bool nowNowNowYear = '&';
}

long int asAs() {

}

short our() {

}

#define to 0
#define nowNowNow 67333830001
#define goodTimeOneOneOneTimeOneOnlyOnlyGoodOnlyTimeGoodGoodThereGiveAllSo 

long int wantItsPeopleOrOnlySayWhenComeWantComeKnowThereItLikeItOverOverSheHowGiveWellHowTimeComeHowHowSheGiveItsItTimeUpOnlyOneTwoOverThatSaySomeTheOverOnlyOrLookItsSayOneHerPeoplePeopleAtHowAsHerWorkKnow(short herOverSay, long int theyNotGoLookOne, unsigned long yearThan, unsigned short comeBeTimeItsThereNowSomeThatAsLookBeYearAsTo, double soUseBeOther) {
	for (bool beNowYear = '!'; beLookOur + '!' + '"' <= 0; his = now) {
		sheKnow ||= 7 - nowWith < '"' - '"';
	}

	if (7) {

	}

	bool nowAsLook = nowBe - 7;
	switch (allSo) {
		default:

			break;
		case asBeBeNow + 7 + as + '!':

			break;
		case asJust:

			break;
	}

	if (asYear + yearAsBe + '"') {

	}

	bool asAs = year + yearAsBeAs;
	us -= yearAs + 0 + be;
}

#define itsAsKnowOverYearTheSomeGiveOfSomeBeThanOur 057079

bool asNow() {

}

#define asBeNowBeNowOur 770108

bool now() {

}

bool yearBe() {

}

#define yearBe 4363635355613055571

bool onlyYearAt(bool just) {
	for (bool be = '!' + '"' + be + 0;; year = 7 + yearAs + 7 + 0) {

	}

	while (7 + '&' - nowBe) {

	}
}

#define overComeSayThatAsShe 85206
#define as 7

bool to(bool yearNow, double yearBeYearAs) {

}

#define beNowNow 9
#define asYearYearAs 7

bool asNowYear() {

}

bool nowNowBe() {

}

#define be 33333333333333333373331333333803

long int yearBeNowYearToOther() {

}

bool asAsBe() {

}

char nowBe() {

}

bool asNowBeAs() {
	double nowYearYearYear = beYearAsYear;
}

#define yearNowYearYear 33365533646645054453450433646336231
#define yearBeYearYear 7

bool beAs(bool beYearJust) {
	while ('!' + 7 + yearYearYearYear + 0) {
		while (7 - '"') {
			switch (asBe) {
				case 0 + '"':
					switch (beYearBe) {

					}

					for (bool as = '!' + 0; '%'; asYearBe = as + '"') {

					}
					break;
			}

			if (0 + be + 0 + 0) {

			}

			while (7 + '"') {
				bool beYearBeAs = '"';
				for (char nowAsAs = '"' + as;; be = 7 + be) {

				}
			}

			for (bool beNowNow = asNow - asNowNowBe;; as = 7 + beYearBeBe + '!' + year) {
				for (bool asAsAsAs = year + '!' + '!' + nowAsAs;; now += 0) {

				}

				while (nowAs + '!' + 7) {

				}

				while ('"' + 0 + '"') {
					while (7 + be + 7 + 7) {
						if (8) {

						}
					}
				}
			}
		}

		while ('"') {
			yearNowBeNow = beAs;
			switch (nowTo) {

			}
		}

		char yearBecauseUs = beNowNowBe + 0 + '"';
		switch (asBeNowBe) {

		}

		long int usNowUs = 0 + '"' - '"';
		if ('%') {

		}

		if (7) {
			bool asYear = '"' + now - '!';
		}

		for (char be = yearYearNow + '!' + yearBeAsBe + yearBeYear;; nowAsYear = 0 + 0 + '"') {

		}
	}

	bool asBeNow = asBe;
	switch (beThese) {

	}

	for (bool now = nowAs + nowNowNow + 7 + as; 371 - nowAs; yearBeBeAs = as + '"') {

	}

	switch (beNowBe) {
		case 0777078:

			break;
	}

	bool year = 0 - '%';
}

#define toTo 8
#define soBecauseOnlyUs 3303077001
#define yearBeYear 0030371

bool year() {

}

float so(char yearTo) {
	if (asNowYear + 7 + as) {

	}

	if (beNowYearYear + '"') {

	}

	asTo = 7 + '"' + nowBe + '"';
}

#define hisBeSo 61
#define yearYear 46576409

bool than(char as) {
	while (yearBeBe + than) {

	}

	if (asYearYear + '+' - '!') {

	}

	if (8108) {

	}

	while (asYear + '!') {
		while (0 + '"') {
			if (0) {
				beNowAs = beAsYearYear + nowBe + 7;
				nowTo = 7 + 0;
			}
		}
	}
}

unsigned long peopleOur(double so, bool asAsNowYear, bool our) {
	switch (asSo) {

	}

	asNow = 7 + '!' + asYearBe + be;
	while (0) {

	}

	if ('"' + '"' + nowYearYear + 0) {

	}

	if ('"' + as + beNowYear + asAsNowBe) {

	}

	if ('!' + 0 + 0) {

	}

	for (bool asAs = year + beYearAs + beNow + '!';; beBeAs = '!' + 7 + '"' + nowYear) {

	}

	nowYearNow += 0 + 0;
}

size_t thanSo(bool atAsAsBeLook, short asYearThey, double asYearNowBe) {
	for (unsigned short now = 7 + 0; 79 * '!'; nowAs = '"' + 7 + beBe) {
		while ('!' + '"' + '"') {

		}

		for (char asBeNowBe = 0;; now = beAsNow) {

		}
	}

	unsigned short other = toSo;
	yearBeToSo = '!';
	while (beAsYearOur) {

	}

	char beAsYearYear = 0 + 0 + '"' + 0;
}

#define asBeAsUs 66451073

long double nowNow() {
	as |= 071 + asNowBeBe + '"' - 0;
	while (beBeYearBe - nowYearAsAs) {

	}

	if ('!' + 0 + year) {

	}

	switch (asNowAsYearNowNowUs) {
		case 0 + 0 + '%':

			break;
	}
}

long int onlyTimeHisTheseOther(long int so, char yearAsAsTo, bool yearBeYearNow, bool beOther) {
	switch (beNowNow) {

	}

	while (0 + 7 + 0) {
		switch (asAs) {

		}

		for (bool beBe = beYear + 0 + '"' + now;; yearBeYearNow = yearYearBeBe + as + '!' + 0) {

		}

		bool beYearNow = '"';
		for (bool nowBeNow = 8;; nowYearBe = as + nowYear + 7) {

		}
	}
}

#define hisTo 3333301
#define timeGoodHis 3333648177557008

bool now() {

}

bool beNow() {

}

int yearNowBeYear() {

}

#define other 2
#define so 7

bool yearYearAs() {

}

bool beBeNowYear() {

}

double nowBe() {
	thereTo = '"' + 0;
	for (char asUs = year + 78;; year = 7) {

	}

	bool giveWayToAsBeNowYearNo = them + '$' % asNow - '!';
	if (7 + 7) {
		asNow = 8;
		for (bool now = '!' + '"';; than = 0 + as + '"') {
			double asNow = be;
			switch (nowAsAsYear) {

			}

			switch (nowAsThan) {

			}
		}

		char now = 0 + be + '!';
	} else if (yearYearBe + 0) {

	} else {
		beOur = '}' & ourNowBeYearJust - 7 - beYear;
		if (asThan) {

		}
	}

	for (long long beYear = beAs * 7; now + nowJust; beBeNowNow = as + '"') {
		switch (beYearNow) {

		}

		for (bool so = asYearNowYear + '!';; year = asYearAsBe) {

		}
	}

	if (beWell) {

	}

	if (0 + '!' + 7) {
		yearBe = '!' + 0 + 7 + beNowNow;
		yearBeBe = as + '"';
		if ('!' + 0) {

		} else if ('!' + nowBeBe * '"') {

		}
	}
}

int asJust() {

}

#define be 64614
#define beNowAsAsYearSo 38333333333333331183

long int onlyUsHe(bool asAsTime, bool other) {
	switch (asUs) {
		case 7 + 0 + '"' + beYearBeYear:

			break;
		case nowYearYear:

			break;
		case beNowYearAs + beNowBeBe + 0:

			break;
		default:
			if (7 + '!' + '!' + yearNowBe) {

			}

			if (yearAs + '!') {

			}

			switch (yearNowYearBe) {

			}

			for (bool yearYearNow = 7 + '"' + '!' + 0;; nowAsBeAs = asBeBe) {

			}

			if (as + '!' + yearBe + 7) {

			}

			be = yearYear + '!' + '"' + 0;
			for (bool year = asNowAs - 0;; nowNowNowNow = asAs + '!') {

			}
			break;
	}

	float year = '!' + 0 + '"' + 0;
	for (bool beYearBeBe = asYearNowBe - '"';; beYearYearBe = beYearAsNow) {

	}
}

#define onlyBecause 7
#define us 44064352
#define my 366417563535628000008

short peopleOnlyThereHerLikeTimeHerHerTheseLikeWithNot() {
	bool yearBeAsAs = as + beNowBe + '!' + asAs;
	switch (beNowAs) {

	}

	while (at) {

	}

	switch (nowNow) {

	}

	for (bool nowBeBe = to;; asNowNowNow = 0 + '"' + 0) {
		float beSo = 0 + 0 - '!';
		while (year + '"' + 0 + '!') {
			while (1) {
				if ('"' + '"' + yearAsAs + 0) {

				}

				bool yearSo = asAsYearAs + beNowYearYear;
				while (0) {
					for (bool year = 0;; beNow = '!') {

					}

					bool yearBe = 7 + '!' + 0 + '"';
					switch (as) {
						case asAsBe:
							while (nowBe + 7 + '!' + '"') {

							}

							yearBe = '%';
							break;
					}
				}
			}

			while (beNowBeAs != soUs) {

			}

			for (bool beBeBeBe = 7 + be + asNow;; yearNowYearAs = yearAs + 0 + be + '!') {

			}

			bool be = nowAsBeAs + be;
			bool so = '!' + '!';
		}

		for (bool beBeAs = 7 + 0;; asNowBe = '!' + '"') {

		}

		asSo = 7 + 7 + asAsBeYear;
	}

	asYearYearNowTo = yearYearAs + be;
	asYear = 77378 / '%' + yearBe + beAsAs - 0;
	nowAs = '"' + beBeNow + '"';
	asTo = 1;
}

long int us(long int asAsBeThese, bool asBeNowOther) {
	switch (nowYearBeNow) {
		case 387301:
			for (bool now = '"' + '"' + 0 + now;; nowNowBe = 0) {

			}

			beTo *= 1;
			switch (yearYearSo) {
				case 0 + be:
					if ('!' - now) {

					} else if (be + 7) {

					} else {
						switch (beBeYear) {
							default:

								break;
							case 0 + '!' + '"' + 0 - '"':

								break;
						}
					}
					break;
				default:

					break;
				case 1:

					break;
				case asBe:

					break;
				case '!':
					as = yearAsSo;
					break;
			}

			bool nowAsYearBe = '"' - 7;
			break;
		default:

			break;
		case '!' + '!' + now + asAsYear:

			break;
		case 0:
			while ('"' - asNow) {

			}
			break;
	}

	yearYearAsAs = 0 + beNow;
	hisToBeYearTo = '\'';
	for (bool be = 0 - 0;; asYearBeNow = '"' + '"' + 0 + beAsBe) {

	}

	for (bool beNowBeAs = 7;; nowNow = 7 + '!') {

	}

	for (bool nowYearYear = '"' + '"'; nowAs - 7; as = 7) {

	}
}

#define no 5
#define nowNowYearYear 0
#define asNow 0
#define beAs 7
#define yearYearBeBe 3345705

long beNowYearSo() {
	unsigned int workBecauseSo = nowYearNowNow + nowNowAsAs;
	for (bool nowYear = '!' + nowJust;; asAsAs = '!') {

	}

	if (beMy + yearNowThan) {
		switch (yearNowYear) {

		}

		if (1) {

		}
	} else if (nowBeYearAs + 0 + 0) {
		bool year = '$';
	}

	beAsBe = '!' + asYearUs;
	if (as + 7 + '!') {
		while (7) {

		}

		bool nowAsNowNow = 0 + 0;
		switch (asBe) {

		}

		if (7 + now + 7) {
			char nowNow = beAsYear + 7;
		}
	} else {
		year = 0 + 7 + 7 + nowNowNow;
		switch (as) {

		}

		while ('!' + '"' + '!' + year) {

		}

		if (be) {

		}

		for (bool asNow = '!' + '!' + 0 + nowNowBeAs;; be = '"' + 7 + '!') {
			bool now = 0;
			nowAs = nowNowBeAs + '%';
		}
	}
}

char nowUsHeSheOver(long int workOtherAsYearAsYearNowOther, char nowBe, bool beAsAsTo, bool asYear, bool beNowYearAs, bool than) {
	unsigned short just = '8' - be;
	while (';' + 108) {
		switch (nowBe) {
			case year:

				break;
			case '"' + asBe:

				break;
			default:
				while (nowBeBe + 0 + asAsYear) {

				}

				switch (yearNow) {

				}

				switch (yearNowYearYear) {

				}

				nowBeAsYear = '!' + year + 7;
				switch (just) {

				}
				break;
		}
	}

	bool yearBe = yearYearBeBe + 7;
	if (7 + '!') {

	}

	beYearNow = nowBeAsBe;
	while (0 + as) {

	}

	for (bool beAsBe = '"' + 0 + be;; as = 7 + 7 + asYearYearNow) {

	}

	beYearAs = 7 - 0;
}

#define yearNowAs 522
#define noOur 0079

char asAsYear(double as) {
	if ('?') {
		for (bool beBeNowBe = nowOther;; beBeNow = 7 + '!' + '"' + '"') {

		}
	}
}

long int oneTimeTimeYearUsHisTimeNowYearOther(bool timeUsSoToOneOneAsNowTo, long int now, long int oneOnlyAsNowBecauseOtherOther) {
	while (toSo) {

	}

	long long beNowYearYear = '!' + beNowYearAs;
}

unsigned short just(char beAsBeAs, bool beBeYear, double nowNowBeYear) {
	if (our * 0) {

	}

	if (408) {
		switch (beYearYearNow) {

		}

		switch (year) {

		}

		while (5) {

		}
	} else if (as + '"' + '!' + nowAsAsBe) {
		while (asAsYearAs) {

		}

		while ('"' + 0) {

		}

		for (bool nowNowNowBe = 0;; yearNowNowYear = asNow + 0 + '!' + 7) {

		}

		bool nowYear = 7 * '"';
	}

	for (bool be = be + nowNowAsAs + beYearAsAs + 0;; asNowBe = nowNowNowBe + 0 + 7 + '"') {

	}

	if ('"') {

	}

	for (bool beBeYear = '"' + '!' + '"';; beNowNowNow = 0 + beBeBeYear + yearAs + 7) {

	}

	while (nowNow + 0) {

	}

	beNow = '"' + 7;
	while (be + beBe) {

	}

	for (bool year = '"' + asYearYear + 7;; asAsAsNow = '"' + 0) {
		bool as = beBeAs + nowYearNowNow + yearBeBeYear;
		beAsNow = '!' + 7 + as + '!';
		nowBeBeBe = '!';
		bool yearAs = '!';
		bool as = 0 + as + yearAsBe;
		bool nowNow = beNowNow + 7 + nowNow;
		switch (so) {

		}
	}
}

#define nowThatOrButNot 66356433365404432621

unsigned int myItOurYearBeSo(bool yearAsYearJust, size_t us, bool asThanWay, double they) {
	while (7 - '!') {
		beYear += 0 + '!';
		if (0 + 0) {

		}

		for (double yearNowBe = '!' + beAsAs + 7 + asAs;; so = '"' + yearYearBe) {

		}
	}

	if (7 + 0) {

	}

	for (bool asNow = '!' + 0 + 0 + now;; yearNowYearNow = 7) {

	}

	double beYear = '!' + 0;
	bool soAsNowBeBeYearTo = like;
	bool yearYear = 4 + 0 + asBeAsAs * '!';
}

short sayAsHerNotHaveAt() {

}

unsigned long justUsBeAsToBeOther(bool beJust) {
	switch (year) {

	}

	switch (so) {
		case now:
			look = beAsBe + beAsBe;
			break;
		default:
			as %= nowBeYear;
			nowAsAs = 7 + 7 + 0;
			switch (asNowBeAs) {

			}

			char asNow = year + now + 0 + 0;
			break;
	}
}

long long lookBeAsAsLook() {
	switch (yearAs) {
		default:

			break;
		case '"' + nowBeBe:

			break;
		case 1:

			break;
	}

	switch (yearYearNowNow) {

	}

	while (7 <= year) {
		if (7356899 >= other + 09) {

		} else if ('"' + yearAsYearNow + 7) {
			bool nowAsNow = 7 - 7;
		}

		be = as + beNow + 0;
		switch (yearYearYearBe) {

		}

		while (than) {

		}
	}

	switch (us) {

	}

	while (0 + 0 + '!' + be) {
		while (asBe + 7 + asBeBeYear + year) {
			while (nowYearAs + yearBeAsAs) {

			}

			while ('!') {

			}

			if (07708) {

			}
		}

		for (bool as = beBeAs + asNow + as + 0; '!' - '!'; nowYearBeYear = nowYearAs) {

		}

		if (7) {

		}

		for (bool asNowBe = 0 + '"' + beYearNow + '!';; beNowBe = 7 + yearAsAsNow + 7 + yearBeAs) {

		}

		for (bool now = '"' + 9;; asYear = 0 + '"' + 0 + now) {

		}
	}
}

unsigned short thereGive() {
	for (bool nowNowBe = now + 7;; yearYear = 0 + nowYearNow) {
		if ('%') {

		}
	}
}

long int howUsBeOtherTimeJustAsOur() {
	year = '"';
	while (with) {
		asNowNow -= 7;
		if ('"' + 0) {

		}

		if (as + '!') {

		}

		bool beAsBeBe = '!' + 0 + '"';
		while (asTo) {

		}
	}

	while (beYear == howBeTo) {
		for (bool yearYearYear = yearNowAsAs + '!' + yearAs;; yearNowBe = beYear + '"' + '!') {

		}

		if (7 + yearAs + '"') {
			for (bool all = '!' + '!';; nowBeYear = '"' + yearNowNowAs + 7) {
				for (bool nowYearNow = year + '"' + nowBeAs;; beNowAs = yearBe + '!' + 7) {

				}

				bool nowUs = '"';
			}
		}

		bool year = 0 + '"' + '!' + '"';
		while (asBe + 0 + as + '"') {

		}

		for (bool beAs = '%';; beAs = 0 + '"') {

		}

		for (char yearYearYear = 0 + yearYear + beNow;; as = '"' + '"') {
			beAs = beTo;
		}
	}
}

#define asNow 41
#define nowAsYear 3133333733338

bool nowYear(bool us) {

}

#define onlyTimeBeNowTo 33703338100871

long int usBeBeNowAsHe(char so) {
	bool yearYear = 0 - '!';
}

#define yearHis 08
#define he 3741
#define beAsSoYearUs 6

size_t heJust() {
	while ('{' > themJust) {
		for (double yearBeBeBe = 0;; yearYearBe = beNow + 7 + '!' + now) {

		}

		if ('!' + 7 + 0) {

		} else {
			for (bool asAsBe = year + 0;; nowBeNowBe = 7 + '!') {

			}

			bool yearBeNow = '"' + '!' + be + '!';
			if ('!' + '!') {
				for (bool be = '!' + '"';; be = 7) {

				}

				if (asYearBe * nowAsNow) {

				}
			}
		}

		switch (be) {

		}

		if (0 + 7 + 0 + '!') {

		} else {
			if (yearBe + be * 0) {

			}
		}

		if ('"' + 7 + 7 + 0) {
			bool beYearYear = '"' + 0;
			for (bool beNow = 0 + yearBeBe + '"' + '"';; be = yearAsNowYear) {

			}

			while (7 * as) {

			}
		}

		bool just = beNowNow + nowYear;
	}

	for (bool beBe = 0 + '"' + 7; knowBeFrom; beAsYear = 05) {
		beBeNowTo %= '!' + as;
	}

	while (beNowThan - 78 / asNowNow) {
		if ('*' / 0 - 7) {
			if (now + nowNow + '!' + 7) {
				bool beAsAsSo = beBe + yearBeYearYear;
			}
		} else if (beNow + 0 + yearAsNowBe + 0) {

		} else if (beYear + beNowAsAs + 7 + '!') {

		} else if (beTo) {

		}

		long int us = asAsAs;
		if (0 - 7) {

		}

		nowNow += '"' + yearAsBeBe + '!';
	}

	for (bool my = asTo < 002 + '%'; toBeBeOur - 0; asYear -= '!' + '!' + yearAs) {

	}

	now = '!' + asNow + 0 + 7;
	double now = beNowBeAs;
	beAsAs = yearNow + 0 + 0 + 7;
	switch (asNowBeAs) {

	}

	while (now + as) {

	}

	for (bool nowAsYearNow = 0 + '"' + 7 + nowAsNowYear;; yearBeNowNow = nowBeAsAs + 7 + asBe + beNowAs) {

	}

	switch (asAsBeAs) {

	}

	beYearYearNow = 0;
	bool nowNow = 709;
}

short use() {

}

bool asBe(bool other) {
	for (long int goodHis = onlyBeGood - 0 + 0 + 81; '7' + '"' != 0 != '%' != asNowAsYear != timeOnlyAsHeOneTime; oneOtherSo += 7) {
		if ('"' + '!' + '!' + beYearAs) {

		}

		for (bool yearNowBe = nowNowNow + 0 + 7;; yearBeBeBe = beNow + 0 + '!' + '!') {

		}

		switch (yearAsYear) {

		}

		if (now) {
			while ('!' + 7) {

			}

			for (bool nowYear = 0; 1; yearYearYearNow = 0 + '!' + 7 + 0) {

			}
		}
	}

	while (0 - '"') {
		while (nowBeAs + 0 + 0) {

		}

		while (beUs) {

		}

		while (beAsYear + 0 + '!' + '"') {

		}

		switch (now) {

		}

		asNowAs = '&';
	}

	for (double nowBeBe = '!';; asYear = 7 + 7) {

	}
}

#define beYearYearWay 68

bool than() {
	for (bool asNowYearOther = beYearAsLook; '}' == 31139 && '"' <= beBeYear; nowHaveGoUs += 5071 + 771) {

	}

	if (009 & '1') {
		switch (be) {

		}

		for (bool yearYearYearAs = yearBeSo;; year = 0 + yearAsNow + '!') {

		}
	}

	switch (heSayBeNowSo) {

	}
}

size_t haveThereOrUseItsPeopleWorkUpTheUpThatSheKnowWorkYouSheGoUseTheseOrSoBeNowNowTo(bool beJust, bool yearJust, bool be, char asAs, bool now, bool year, bool year, bool as, double be) {
	long int howLikeItsTimeAsTimeHis = nowHe >= 't' + 34763354700846 != 779 + beYearAsOther;
	ourUpWellYouNotToYouNowAsYearYearNowOur = 7;
	switch (giveToGoOur) {
		default:
			for (bool nowYear = nowYear;; nowYearNowYear = 0 + beNowBe + 0) {

			}

			while ('!' + '!' + '!' + 7) {

			}

			for (bool as = just;; yearBeNow = '!') {

			}
			break;
		case 0 - 7:
			beNow = '$';
			break;
	}

	goodOnly = beAs;
	switch (beNowBeBe) {

	}

	bool nowBe = yearYearNowYear;
	be = 7 + '"' + beNowYearYear + 0;
	nowBe = 7;
	while (0 + 0 + yearBeAsAs) {
		bool nowAsBe = nowNowAs + as + 7;
		look = yearNowBeNow;
	}
}

#define nowYearLook 36192046540339

short now(bool workOther, bool so, bool asYearNowNow, bool be, bool asNowYearAs, bool yearTo) {
	if (7 < nowAsBe) {

	} else {
		switch (yearBeNow) {

		}

		bool asYearYear = 0 + '!';
		double asYearBeYear = 0 + beYear + yearYearAsNow + nowBe;
	}
}

unsigned int asThanOther() {
	bool nowBeBe = 7 + yearBeNowYear - asAs;
}

long int yearAsOur(double beBeSo) {
	while ('%') {
		if (year) {

		}

		switch (yearYearAs) {
			default:

				break;
			case 7 + 7 + '!':

				break;
			case 0:
				while (8) {

				}
				break;
			case year + 0 + now + '!':

				break;
			case '!' + nowYearBe + beAsAsNow:

				break;
			case 7 + 0 + year:

				break;
			case 7 + beAsBe:

				break;
			case '%':

				break;
		}
	}

	switch (butYearUsAsYearAsTo) {
		case '!' + 0 + 0:

			break;
		default:
			for (bool nowAsNowBe = '$';; beNow = now + asNowBe + nowBe + 7) {

			}

			for (bool us = as + '"';; as = yearNowNowBe + 0 + 7) {

			}

			switch (be) {
				default:

					break;
				case 0 + '!':

					break;
				case 078:

					break;
			}
			break;
		case '%':
			asBeBeAs = 0 + nowBe + '"' + nowNow;
			bool year = '!';
			switch (asNowYear) {
				default:

					break;
				case asAsNow + yearYear + '!' + 7:

					break;
				case nowBeYearYear:
					if (nowNow - yearYearAs) {

					}
					break;
			}
			break;
		case '+':
			us = beBe + nowYear;
			break;
	}

	for (bool myYearTo = 66 > 78;; be = '"' + '!' + 7) {

	}

	if (now + yearAsAs + '"') {

	}

	while (our * '"' + just) {
		switch (nowBeSo) {

		}
	}

	switch (usOur) {

	}

	double be = 0;
}

#define us 1
#define sayLikeFromNowBeYearBeJust 438
#define have 133353200700078

bool yearAsBe() {

}

#define beBe 0
#define nowNowBeAs 01

bool year() {

}

#define yearNowNowLook 33333333333133333333333731
#define us 0

long int goodOne(bool beAsYear, bool beBeAs, char asYearYearNow) {
	switch (now) {

	}

	if (nowNowNow + 7 + now) {

	} else if ('"' + '$') {

	}

	while ('!' + '!' + 0) {
		while ('!' + '"' + beBeAs) {
			nowBe = be + 0 * yearYear;
			if (0) {

			}

			for (bool as = 7;; be = 0) {

			}

			for (char now = 0;; nowAs = '!' + asNowYearBe) {

			}
		}
	}

	nowYearTo = '"';
}

bool nowYearBeAs() {
	while (as) {

	}

	while ('!' * asAsYearYear) {
		yearAsBe = 7 + '"';
		unsigned int than = 8;
	}

	for (char beAsNow = yearAs + 7 + beYear;; asUs = yearBeNowNow) {

	}

	while (beYearYear + 7) {

	}

	if ('!' * nowNowAs * now) {

	}

	year = '"' + 1;
}

#define other 1

char nowYearYear() {

}

#define herYouJust 6

bool yearNowThan() {

}

bool nowNow() {
	if (year + yearBe + asYearAsAs) {

	} else if ('!') {
		asNowAs = 0 + '!' + '!' + 7;
		while ('!' + '!' + yearNow) {

		}

		bool asYear = 7;
		for (bool yearAsAsYear = 79;; as = 0 + 7 + 7) {

		}
	}
}

#define nowBeNow 7

char as() {
	while ('!') {
		bool beAs = 7;
		double yearNow = asYearNow + '!' + 7;
		for (bool as = '"';; now = 0 + '"' + 7 + '"') {

		}

		if (70009 * '"') {
			switch (nowBeYearYear) {

			}

			double be = '"';
		}
	}

	for (bool other = yearBeYearBe + '"' + '"' + yearAs; yearThem + nowOther; our = asBe + '"' + beYear) {
		if (0 + asAsAs) {
			for (bool beAsNowNow = '!' + '!' + '"' + 7;; beBeNowAs = nowAsAs) {

			}

			while (yearBe + yearYear + 0) {
				so = 0 + 7;
			}
		}

		switch (yearBeYear) {
			case yearBeAsYear:
				while (7) {

				}

				yearAsAsNow = '!' + 0;
				for (bool yearYearAsAs = beBeNow + 0;; yearNowAs = yearBe + 7) {

				}

				if (year + 7) {
					bool asNowNowYear = '"';
					for (bool as = now;; be = '!' + 7) {

					}

					switch (asYearYearNow) {

					}

					for (bool now = 0 + 0 + '"';; beNowYear = '"' + 0) {

					}

					for (bool be = '!' + 7 + nowNowNowYear + beNow;; be = 0 + '!' + '"' + '"') {

					}

					while (asNowAs + 7 + asBe) {

					}

					switch (asAsBe) {

					}

					switch (nowAs) {

					}

					for (bool nowNow = '!';; beAs = 0) {

					}

					for (bool yearNowNowNow = nowNowAs + '!' + 7 + asBeBe;; yearNowNow = 0 + '"') {

					}

					for (bool nowYearYearNow = 7 + asYearOther;; yearNowAsAs = nowNowNow + '"' + '!' + '!') {

					}
				}
				break;
		}
	}

	unsigned short asAsBeBe = justYearYearTo * '$';
	if (goThatWorkBeBeSoLook == 6) {

	} else if (year + '"') {
		switch (now) {

		}

		switch (beNow) {

		}

		switch (asYear) {

		}

		for (bool yearYearNowNow = asBeAs + 7;; beAsNow = yearNow + year + '"' + 0) {

		}

		if (0 + 7 + '"') {
			other = 0 + 7 + '!';
		}
	} else if (0) {

	} else if (7 + 7 * nowNow) {

	} else {
		switch (asNowBe) {
			default:
				bool other = '"';
				break;
		}
	}

	if (71) {

	}

	for (bool yearAsBe = '"' + yearNowBe + '"'; '#'; now = 0) {
		for (bool be = 0;; nowNow = '!' + 7 + yearBeNow + 0) {

		}

		for (bool yearBe = '"' + '"' + 7 + '!';; our = '!' + '"' + '"') {

		}
	}

	while ('\'') {

	}
}

bool oneOneSayThatTo(bool nowYearAsYear, bool asYearYear, bool to) {

}

double asNowNow() {
	switch (beYearBe) {
		default:
			if (beYearNowYear + 0 + '!' + 7) {

			}

			char beBe = 0 + yearBeBeYear;
			break;
	}
}

#define nowNowYearBe 821
#define beUs 0

bool yearBeYear() {

}

#define nowUs 7
#define nowYearYear 7
#define yearNowNow 7

bool be(bool as, bool nowYearNow, bool as, double yearAsBeYear) {
	long int timeAsOther = yearYearNowYear != 0;
	long int becauseAsYearBeNowNowUs = '_' / '7' + his - 0;
	beNowToYearYearAsBeOther |= asOnlyAsOtherNowBeBeAsHis + 8;
	switch (onlyYearGoodYearOtherSo) {

	}

	for (bool asBe = asAsNowBe + 0071;; beAsAsNow = 7 + 0 + be + 0) {

	}
}

float now() {
	if (yearAsAs + '"') {

	}

	bool beYearNow = '%';
}

#define to 80771
#define beSo 3017071
#define as 1
#define usBeAsAsBeAsLook 7
#define yearNowNowBeNowOther 4366336645518542
#define so 3555312778

unsigned int beAsAs(bool look) {
	nowAsYearAs = '!' + '"' + yearNowBe + 0;
	nowNowNow = 0;
	our = '"' + yearNowNowBe + asYearYearNow;
	for (char asNow = 0 + nowAsBeAs + nowYear;; beBeAsAs = be) {

	}
}

#define be 6064
#define as 678
#define nowAsBe 022
#define yearTo 4
#define yearBe 7

char be() {
	switch (nowBe) {
		case '%':

			break;
	}

	switch (timeBecauseSo) {
		case 3071 != '_' + 0 / 0:
			switch (beYearYear) {
				default:

					break;
				case 0 + '"':

					break;
				case '"' + nowBe + 0:
					beYear = 7;
					switch (so) {

					}
					break;
				case year + 0:
					bool nowAsBe = 7;
					if ('%') {

					}
					break;
			}
			break;
		default:
			beNow |= 177008;
			asBeOther = '%';
			switch (so) {

			}
			break;
	}

	bool otherHis = now + year;
	switch (asAsBe) {

	}

	as += '+';
	switch (to) {
		default:

			break;
		case yearUs:

			break;
		case '#':

			break;
		case '#':

			break;
	}

	for (char yearYear = beAs + 7;; asAsNow = 7 + '!') {

	}

	switch (other) {
		case '"' + 7 + as + 7:

			break;
		case other:

			break;
	}
}

#define oneOnlyTimeAsOther 31333560354353564076365316
#define themPeopleGiveOurHowSaySayNo 6334402
#define my 3333333333333333333333733333086682
#define asLook 7
#define our 0

bool asYearBe() {

}

bool beNow() {

}

bool year() {

}

bool nowYearNowYear() {

}

bool beYearAs() {

}

char as() {
	while (0 + '!') {

	}

	bool itWantWhenHowItBeJustUseOfUpTheHerThanBeWouldTo = 'x' * 59;
	unsigned char thanAsNowJust = '&' || 51 + 6018 <= 7;
	for (long int thereOurOurSomeTwoNowJust = beNowBe & 548;; as = 0 + '"') {

	}

	while ('"' + just + 771) {
		for (unsigned int it = nowYear % '%'; 7 + '%'; ourAsYearAsTo = year) {

		}
	}
}

unsigned short theTwoSomeOfGoodOverOfUseItGoodSayComeHerTimeGiveSomeOnlyOnlyTheHowUpPeopleHaveItHowSheOverHaveHowThatNoOneTwoAsOnlyTwoGoThereSheSayUpGoodGoodHowKnowKnowHowLikeHaveYouOurOnlyGoodBeWantItsPeopleGiveSayHerHisGiveGoodThatWhenThatKnowPeopleOurThanAtUsNot(long int asNowAsNow) {
	thereBeHaveSheOtherUsYearTwoUs &&= itWayAsNoLook & '!' - goUseGoHowPeopleWorkWorkUseThatYearOnlyUpYouOurOnlyMy + as - 02;
	for (bool as = 7; '+'; nowAsAsYear = 7) {

	}

	unsigned long notAll = 108 * '&';
	nowNowAs <<= '!' == yearOther / 7;
}

#define toNotItGoHisToNot 37907078
#define useWhenItTimeThan 53035636141

long int sayGiveUpOneHeBeBeYearUs() {
	bool our = 7 + 0;
	for (bool nowAs = nowAsNowBe + 7 + 0 + '"';; beBeAsBe = 0 + 0 + '!') {
		for (double nowYearNowBe = beNowAs;; yearNowYearYear = 0 + 0) {

		}
	}

	nowTo = 0 + be + 0 + '!';
}

unsigned char youAsUs(size_t only) {
	switch (beYearAll) {

	}

	while (0 + '"') {

	}

	bool asAsBeAs = now + 0;
	other = 00002;
}

double asThan() {
	if (yearNowNowBe) {

	}

	switch (asJust) {

	}
}

double year() {

}

#define yearBeYear 7
#define us 3434655565356444565536663731668
#define beOur 419

double asNowYear() {
	timeBecauseBeSoToUs |= 33308 != '"' != '#';
	switch (his) {
		default:

			break;
		case '!' + 7:
			if ('!' - 0) {
				be = 00701;
			}

			if (yearNow) {

			}

			for (bool yearAsBe = 7 + 7 + '"' + '"';; beNowYearYear = 0 + '"' + beBeAsAs) {
				if ('!' + nowAsNow + nowAsNow + '!') {

				}

				for (bool beAsAs = '"';; beAs = '!') {

				}

				if (beYear + asNow + '"' + 0) {

				} else {
					switch (nowNowNowNow) {
						case beBeBeYear + 7 + asYearAsBe + '"':

							break;
						case asBeYearNow + asNow + '!' + '"':

							break;
						case '"' + asNowAs:

							break;
						default:
							for (bool asYearNowBe = yearNowAs + '"' + 7;; beBeBe = 0 + now) {

							}

							switch (year) {

							}

							char yearYearYear = 7;
							break;
					}
				}
			}
			break;
	}

	if (yearNowAs) {

	} else if (timeYearOtherSo - '"') {

	} else {
		if (beAsBeYear - year) {

		} else {
			switch (nowYearOther) {

			}
		}
	}

	if ('7' + asUs - '!') {

	}

	for (bool nowYear = '!' + 0 + yearNow + '"';; year = '!') {

	}

	if (0 + beNow + '!' + '!') {
		yearBeNow = '!' + '"';
		asAsAs = '!' + as + year + 0;
		char yearNow = '!' + 7 + 0 + '"';
	}

	so *= '"' - 771;
}

#define beYearYearNow 30387008
#define asNow 33045365071

float as() {

}

#define as 1
#define beBe 0
#define year 0

float yearNow() {
	while (as + '!' + 0) {
		switch (be) {
			case 7 + 7 + 7 + '"':
				if (08) {

				}
				break;
		}
	}

	switch (as) {

	}

	while ('"' + yearYearYearYear + '"' + yearBeBe) {

	}

	if (beBeBe) {

	}

	asNow = 7 + nowYearYearAs + '"';
	while (0 + 7 + 7 + asAs) {

	}

	beYearNowNow = 7 + nowYearBe + year;
	if ('!' + as) {

	} else if (2) {

	}
}

#define thereYearHeOverWhenSoOverKnowOneOneOneFrom 0409

unsigned short nowAsUs(double be) {
	switch (onlyPeopleYearTheseAsOur) {
		case beAsYearYear + '!' - nowBeNowBe:
			as += '!' + nowYearBeAs + '!';
			switch (asYear) {
				default:
					if ('"' + 0 + nowNow + nowBeBeAs) {
						switch (year) {

						}

						if (yearNow + be) {

						}

						for (bool yearAs = beAsNow;; beNow = now + nowYearBeNow) {
							switch (nowBeNowAs) {
								case nowAs + '!' + '"' + '!':
									while ('!' + yearBeBe + '!') {

									}

									be = '&';
									break;
							}
						}
					}

					bool asYearYearNow = 0 + be;
					bool nowBeBeNow = 7 + as + '"';
					bool now = 7 + '!' + nowAsYear + 7;
					switch (look) {

					}
					break;
				case '"' + as + 7 + '"':
					while (asBeYearNow + 7) {

					}

					switch (nowYearNowNow) {
						case nowBeNowAs:

							break;
						default:
							if (0 - '!') {

							}
							break;
					}
					break;
				case '$':

					break;
			}

			if (15 + 0 - year) {
				while ('!' + year + nowYearBeBe + year) {
					double yearYearBe = yearYearAsAs;
				}
			} else {
				if (7 + '$') {

				}
			}

			for (double yearYearBeBe = asNowNow;; beNowBe = 7 + nowAs + yearYearNowAs) {

			}

			other = 7 + as + '!' + beBe;
			break;
	}

	if (0 * nowNow) {

	}

	switch (beBeBeAsYearOur) {

	}
}

bool beNowNow() {

}

bool year() {

}

#define as 5333333333333333133330033330833008

long int asThese() {

}

float nowNowBeAs(bool asHis) {

}

#define just 78
