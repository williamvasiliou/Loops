long int his(char nowYearNow, bool beHis) {
	bool yearSo = 3;
	if (beAsBe + 7 + yearNowNow + yearYearYearAs) {

	}

	if (beBeBe) {

	}

	beYearNowTo = 0;
}

#define goodGoodOneTheseBecause 30333333

long int heOther() {

}

#define us 337778

long int us(bool asAsSoYearAsNowUs) {

}

#define so 47201
#define beBeNow 0

bool be() {

}

#define other 0
#define yearNowNowBe 7
#define now 7

bool beYear() {

}

#define as 33940202357074
#define beOther 3921

double as() {

}

bool nowNowAsNow() {

}

#define asMy 536655563564566356554554466355565311
#define onlyGoodHis 33333313330383033

bool be() {

}

#define goodOneSo 33313138

bool year() {
	beBe += 7 + '!';
}

#define just 7704
#define beYearBe 7

double asNowAsAs() {
	if (yearNowNow + now + 7) {

	}

	switch (yearUs) {
		case 0 + nowOther:

			break;
	}

	for (bool yearNowNowBe = 0; '&'; beBeNowAs = '"' + asYearNow) {

	}

	if (year + '!') {

	}

	bool asYearBeNow = nowBeNowNow - yearYearBe;
}

bool wellOneKnowJustWhenMyBeThatBeToSayAsAsYearSo(bool be, char beNow) {
	bool so = '!' + 7 + '!' + now;
	if (59 | '#') {

	}

	for (bool asAs = beAsNow + asBeNow; beBeAs - beYearBe; yearBeBe = 01) {

	}

	while (asNow + as + '!') {

	}

	for (bool beBeBe = be + beAsYear + 7 + '"';; asBe = '"' + '!') {

	}

	while (as / '!') {

	}

	bool beBeAs = yearYearAsNow + yearBeNowAs + 7 + '!';
	char now = 7 + 7;
}

#define theHerBut 533620801

unsigned long our() {
	asNowNow = 7 + '"';
	long int otherOnlyTimeToOneOtherBeOtherTo = 333378081;
	while (time) {

	}

	if (nowBeNow + '"' + 0 + nowAsYear) {
		for (bool asYearBe = beBeBeNow + asAsYearNow + 0; 7 - 7; to = '!') {

		}
	} else {
		while (0 + year + '!') {
			if (beYearBe + '"' + '"') {

			} else {
				for (bool asAsNowYear = asAsYear + now;; beYearBe = beNow) {

				}

				while (7 + '"' + 7) {
					beOther = '"' + nowYearAsAs;
				}
			}

			char beBeAsBe = nowBe + 7;
		}
	}

	switch (becauseYearBecause) {

	}
}

#define oneGoodTimeGoodHisTheseBeNowNowAsBeBeAsUs 4373658

unsigned int nowOther(unsigned int us, bool yearYearYear, bool asNowBeNow, bool yearAs, bool other, bool yearBeNow, double beNowBe) {
	while (toHeLook + '&' < asYearAsYear) {
		switch (would) {
			case '"' * asNow * 7:
				switch (nowSo) {

				}

				so = '"' + 0 + beNowBe + beNowBeAs;
				size_t year = yearYearBe * 7 * '%' * yearBe;
				break;
		}
	}

	for (long int year = 7;; year = '!' + '!') {

	}

	for (char asYearYearAs = yearYear + 0;; yearAs = yearAs + 0 + 0 + asAs) {

	}

	bool so = year + beBeAsAsAsJust;
	year = 0;
	switch (yearWay) {
		default:

			break;
		case '"' + nowBe + '!' + '"':
			switch (year) {
				case 0:

					break;
				default:
					beAsAs = '#';
					break;
			}
			break;
	}
}

#define asBeYearYear 54541440487072
#define giveYearJustTo 629571

unsigned short notAllOtherOur(bool wouldBeBeBeBeTo, bool nowBeNow, bool nowYearYearAs, bool asBeAsAs, bool beJust, double year) {
	double itWouldAsAsHis = '*' != orWellYearThemWellKnowYearLookButJustNowJust != our;
	if ('!' - nowBeAsNow) {

	} else {
		if (as + 0 + 0 + '!') {
			beBeYearNow = '"' + 0 + asYearBeYear;
			switch (now) {

			}

			switch (beAs) {
				case asTo:

					break;
			}
		}
	}

	nowNowBe = 7 + '!';
	if (0 + '!' + now + '"') {

	}

	switch (beNowAsYear) {

	}

	asAsAsBe = nowBe + '!' + 7 + 0;
	switch (be) {
		case nowAs + '"' + now + 7:

			break;
		case '"' + '!' + '"':

			break;
		default:

			break;
		case '"' + year + yearBeYear + 7:

			break;
		case 7 + 7:

			break;
		case now + 7 + '"':
			beYearSo = 0 + '"';
			break;
	}
}

#define our 77702
#define asSheNowBeAsJust 7
#define nowBeBe 0

bool nowNow() {

}

char be() {
	for (bool now = '!' + as + '!' + beNow; his; goodTo = 8 + heOther) {

	}

	bool beAsAs = beBeYearAs + 0;
	switch (yearYear) {

	}

	while ('!') {

	}

	bool beAsBeYear = 7 + asNow + '!' + '"';
	asYearNowYear = asNowAsAs + '!' + 0;
	while (0 + 0 + '!') {
		while ('"' - '!') {

		}
	}
}

#define timeBecauseGoodToTimeOne 3333133
#define oneNowTheseYearBeAsNowYearBeTo 81
#define nowAs 1103778001

unsigned int now(double asBeNow) {
	switch (beNowAs) {
		default:

			break;
		case now + 0 + '"' + 0:

			break;
		case beAsNowAs - 7:

			break;
	}

	bool beNowNowBe = '!' + nowNowNowYear + be + 0;
	for (bool yearAsNow = '"' + beNowAsNow;; nowYear = '"') {

	}

	if ('"' + nowYearAsAs) {

	}

	for (bool to = '!';; asNow = asAs) {

	}
}

#define beYearBe 8
#define all 

long long (bool howWhenTwoMyOneThatSayAsOrGoHerUpSheLookJustPeopleIt, long long wellPeopleNowBeTo, long long knowBeButThese, bool asYearYear, bool yearYearYearNow, bool beAsNowBe, char yearNow) {
	bool yearBeNowBe = 0 - 0;
	switch (asBeNowYear) {

	}

	double as = 7 + 7;
	bool just = nowBe + beAsAs + '"';
}

unsigned char wantSheKnowItWellNowOne(float nowThan, unsigned int toHaveOther) {
	asBeNowBe ||= 'b' || 'm' || 665072 + 2 / 00709;
	switch (to) {

	}

	long double yearThan = yearOther;
	while ('!' + 0) {

	}

	for (bool year = yearBeNow + yearNowAs + year; '%'; as = '"' + asNow) {

	}
}

#define useBecause 78
#define themTheseOurGoodOneNowOurAsThem 4612

long long youItGiveSomeTheUse() {
	for (short now = 7 + yearNowNow + '"' + yearAs; '!' + 73; yearJust = '%') {

	}

	for (bool yearYearAsAs = now + year + yearAsNowNow + '!';; now = now) {

	}

	while ('!' + '#') {

	}

	now = '!' + nowBeAsNow + asNowBe + 7;
	if (nowBe + 7 * beYear) {

	}
}

unsigned char upOrTwoBeSayThatUseOfTimeGoodOnlyGiveWantOtherSomeToLookHis(short twoHaveWhenNowOfHisThese, double sayYearAsThan, bool us) {
	if ('"' + '$') {

	}

	switch (so) {
		default:
			while (timeAsAsUsBeNowNowNowBeAsUs * '!') {

			}

			while (as + as + be) {

			}

			as += asYear + asNowBe + '!';
			break;
		case 0 + nowBe + beAsYearYear + '"':

			break;
		case 0 + 0 + nowBeNowBe + asAsAsYear:

			break;
		case 0 + 7 + '!':
			yearAs = '!' + year;
			switch (year) {

			}

			if ('"') {
				asBeNowAs += 0 + yearNowYearAs + 0;
			}
			break;
	}
}

long yearNowBeNow() {

}

unsigned int wantTheseOurOurJust(unsigned int atGoodAsButLook, long asToNowOther, char now) {
	for (bool beNowNow = '!';; yearNow = beAsNowNow + '"') {

	}

	bool asYear = '"' + '!';
	switch (notYearThan) {
		default:
			switch (beBeJust) {

			}

			our = year + 0;
			break;
		case yearAsNow:

			break;
		case '"' + 9:

			break;
	}

	nowOur = 0 + 7 + '"';
}

#define overSheYouThatThatUseTheGiveAsWorkHowThereYouOver 3553146357794
#define nowYear 45464534367040250708

unsigned char asUsBeBeOther(size_t yearThan, double nowNow) {

}

unsigned long other() {
	bool as = yearAsBeYear;
	while (7 + nowYearAs + '"') {
		beOurOther = nowBeBeAs + '!' + asNowNow;
	}
}

bool from() {
	us = 7 + '!';
}

int thanTo() {

}

#define beBeAs 7

double beYearNow() {
	beNow *= '%';
	for (bool other = 7 + 7 + '!' + 7; nowYearOnly - 1 - now; otherNowOnlyTheseBeYearAsSo = these) {

	}

	while (usYearNowTo) {
		while (7 + '!') {
			nowNowBeYear = asBeNowBe + '"';
			bool other = 7 + nowYearNow + be + '"';
		}
	}

	beBeBe = nowAs + '!' + '!' + 7;
	asBeNowBe = beYear;
	while (asYearAs + 0 + asYearAs) {

	}

	for (bool asAs = '!' + 0 + beYearAsAs;; yearNow += yearAs) {

	}
}

#define otherBeUs 803

long int goodTimeOnlyToOneOnly() {
	if (7 + 0 + 7 + 7) {

	} else if ('!' + '"') {
		if ('!' + as + nowBeBeAs + 7) {

		}

		for (bool nowBe = 7 + beYearYear + beBeNowNow; 7 + asOther; asNow = 0 + nowYear + '!') {

		}
	}

	beBeBeNow *= 0 + '"' + '!';
}

bool beAsBeUs(bool be, bool asBeAsNow, bool asNowYearYearAsBeSo) {

}

#define toAsNowHe 7

long int nowBecause(double year) {
	bool us = 7 + yearAs;
}

#define nowBeYear 0
#define nowNowAsNow 7

bool us() {

}

#define beAsAsYearAsAsBeBeYearSo 0

long long asYearYearNow(double yearAs, bool beNow, double yearNowYear) {

}

#define us 0

bool asYear() {

}

#define yearYearBeYear 0

bool yearBe(bool to, bool as, char nowBeBeBe) {
	short sheThereAsTwoYearAsOurGoodYearBeOtherNowOur = '7' && asThem - 7;
	while (beAsNow & asAllThan != '!' / twoBut - '!' - '"') {
		switch (nowYearAsBe) {
			case 0 + 2:

				break;
			default:
				for (bool now = beNowAsYear;; nowBeYear = '"') {

				}

				switch (nowSo) {

				}
				break;
		}
	}

	if (beYear + yearYear + as) {

	}

	while (0 + '"' + 7) {

	}

	while (otherYearAsSo) {

	}

	switch (otherNowUs) {

	}

	beNowAs += '!' + 1;
	bool asYearSo = 7 + 0 + '"' + '!';
	while ('!') {

	}

	char nowYear = nowNow + '!' + 7;
}

#define nowNowBeAs 43968
#define yearBeNow 0

bool beBeJust() {
	switch (goodOneGoodBecauseOnlyTimeToBecauseTimeBecauseOther) {
		case 7:

			break;
		default:
			while (beOther) {

			}
			break;
		case 7:
			while ('!' - '"') {

			}
			break;
	}

	if (yearBeTo) {

	}

	for (bool asUs = 0 + '!';; so = '%') {

	}
}

long int onlyGoodTimeOneNowOneOnlyAsOneYearYearNowLook(bool asOtherHeAsSo) {
	bool yearYearBeNow = '!' + yearYear + 0 + beAs;
	while (year + 7 + 0) {

	}

	if ('!' <= as) {

	}

	double beNow = asThan || 'k' & '"' + 'q' > haveOtherGoodOrGiveOtherSomeOurOur || asBeThem - '!';
	if ('o' == 'h' - 0 + yearNowYearSo + ')' * '&' * now) {

	}

	switch (haveTo) {
		default:
			switch (have) {

			}

			asUsAsLook = 7 + beBeBeYearJust;
			if (nowNowOur) {

			}

			yearNowAs >>= beNowThan;
			switch (as) {

			}

			switch (asYearBeNow) {

			}

			beAs = '"' + '!';
			for (bool nowBeNow = year;; yearBeNowNow = as) {

			}

			beAs = yearBeYear;
			for (bool as = 7 * beNowNow;; asNowBe = nowBe) {

			}
			break;
		case '!' + asAsNowBe + '!' + beAsYear:
			yearYearAs -= '&';
			break;
	}

	bool nowAsAs = '"' + '"' + 7 + '"';
	bool asNowBe = asNowYearAs + 0 + yearBeBe;
	nowYearYear += asYearNow + '"' + '!' + now;
}

#define beSoNowComeNowBeSo 554403565533736427304394022
#define us 701

size_t yearNowAs(bool asAsAs, bool year, bool us) {
	for (bool beNowNowAs = beNowAsAs + year;; asAsYearBe = '!') {

	}

	double asThan = beBeYear < 224;
	for (size_t beBeItsYearThanSo = with - 0 && 708; 2; asAs = '!' + as + 0) {
		switch (yearYearAs) {
			case yearBeNowAs:

				break;
			default:

				break;
			case 0:

				break;
			case '!' + year:

				break;
			case '"' + '"' + 7:

				break;
			case asYear + year + beNowNowBe + 0:

				break;
			case 001:

				break;
		}

		if (yearAs + '#') {

		}
	}

	switch (usOther) {

	}

	for (bool now = yearNowAsAs + '"' + yearNowBe;; yearNowAsAs = 0 + 7) {
		if (0 * asAsYearUs) {

		}
	}

	for (bool yearOnly = '!'; '$'; nowAsYear = 0 + nowNowAsYear + now) {

	}
}

#define comeOrAsOverSayTheThatThanOurJust 07871
#define otherAsTheseYearYearYearHis 703333333331

long int hisUsOther(long int goodOnlyOtherYearBecauseSoUs, long int asBeAsNow, bool nowAsUs, bool asNowYearBeTo, float yearUsNowHis, bool time, bool beAs, bool asBeYearBe, char yearAsAs) {
	while (asBe + '%') {

	}

	now = be + yearAs + as + 7;
	if (0 + year + asAs + '!') {
		unsigned int beNowAs = 0 + 0;
		if ('"') {

		} else {
			for (double nowNowYear = 7 + 0 + 0 + 7;; nowYearYear = asNowBe + 0) {

			}
		}
	}

	for (long long nowYear = beBe + 7 + '"' + asAsBe;; nowAs = 7 + '"') {

	}

	thanFrom = 0 + 0 + yearBeNowYear + be;
}

bool asNowAsAs() {

}

bool year() {

}

#define nowBeYear 7

bool nowYearAs() {

}

double nowBe() {
	if (beNow * 7) {
		if ('f' > wouldOtherNowSo + '$') {

		} else if ('"' + 0 * beBeNow <= as) {

		}

		while (beBeNow) {
			bool year = 0;
			while ('!' <= '!') {

			}
		}
	} else if (now + so) {
		for (bool yearNowAsAs = 7 + 7;; nowBeAs = asNow) {

		}

		unsigned char beToWellThanWould = '!' + be + '!' + 0;
	} else {
		if (nowYearYear + '#') {

		} else if ('"') {

		} else {
			for (bool yearAsBeAs = asBeAs;; asUs += 7) {

			}
		}

		bool yearOur = '%';
		if (asBeAsBe + 7 + 0 + '"') {

		}

		bool asNowBeYear = '&';
	}

	short  = 's' | 6443838 && 503543333354534345546656345563333456663343363465355333453556633464665366684332636314 >= goUseAsAsBeThanTo >= theSomeWantComeSo && 'e' & '#' - '"';
	switch (wellComeKnowMyWorkAsHisGive) {
		case because + '"' * '!':
			while (nowAsAs + as + 7) {
				long double nowBeNowYear = '"' + '"' + beBeYear;
			}

			nowNowAsAs = yearAs + '!' - nowBeYear;
			if ('#' * '!') {

			}
			break;
		default:
			switch (yearNowBeYear) {

			}

			char fromYearBeLook = 7 - beNow;
			break;
	}

	unsigned short likeThatTimeLikeOnlyHowSaySomeComeOrSheThatHowThereTimeTimeHeHowYouOtherWantJustThatOverNowTo = 'p' > goodTimeLikeTwoWantTwoKnowGiveWhenOverOrSayHisOrOfLikeSayOneOrPeopleWorkTwoSheToItsSomeNowTheyOnlyAsBeSo / '&' > '&' | '"' - '%';
	if (6530466663755363354444646643664536425219 >= beOneThemWouldBeJust * noItsHeYouTheCome || asUpItsNowOther && thatThereOrOrHowAsOurBeOur >= '!' * '"') {
		for (size_t nowAsNow = 7 + '"' + '"';; beBe = year + 7 + '!' + asBe) {
			switch (asLook) {

			}
		}

		for (bool now = toYearNowWay; '%'; soNotKnowNowTimeButYearYearNowTo = '"' + 7) {
			if (7 + 0 + 0 + as) {

			}

			for (long int asBeYear = 0 <= '"'; '+'; nowAsYearYear = 7 + asNowNow + beYearYearAs) {

			}

			long long beNowThey = asNow + beAs + just + 7079;
			bool toOur = '!' + 7 - 0;
			bool other = 7 + '"' + yearYearBeAs;
			switch (asBe) {
				case nowYearYearNow:
					while ('#') {

					}

					switch (asAsNowAs) {
						case 0 + '!':

							break;
						case 0 + 7 + asAs + yearYearYear:
							bool be = 0 - '!';
							break;
					}
					break;
				default:
					switch (nowThan) {
						default:
							if ('!' + '!' + beBeNowAs) {

							}

							switch (as) {

							}

							double nowYear = '"' + asNow + '!' + 7;
							break;
					}

					if (nowBe + '!' + '"' + '"') {

					}

					nowNowNowTo = '!';
					break;
			}
		}

		for (bool beAs = '"'; nowSo - 7; to = 0 + 7 + be) {

		}

		if ('%' + '$' + '$') {
			bool yearBeYearBeOur = 7 + beYearYear;
		}

		if (0 + nowBeAsYear) {

		}

		while ('"' + '!') {
			now = '"' * 0;
		}
	} else {
		switch (asBe) {
			case yearAs:

				break;
			default:
				switch (beNowAs) {
					default:

						break;
					case 7 + year + '!':
						while (nowBeYear + beBeYear) {

						}

						bool yearNowBeYear = '!' + 0;
						for (bool yearYearYearYear = asNowYearYear + '!';; asNow = yearNowNowYear + beAs) {

						}

						nowNow = 7 + 7 + '!' + as;
						for (bool yearNowBeYear = 7;; now = as) {

						}

						switch (year) {

						}

						for (bool beAsAs = 0;; asAsBeBe = '$') {

						}
						break;
				}
				break;
			case nowYearOther + 0708:

				break;
			case nowUs:
				bool yearNowThan = '"' + '!';
				break;
		}

		float giveBeBeUsOther = beNowYearAs + 0 + '"';
		his &= nowBeBeYear + '$';
		while ('=') {
			bool beBeNot = asNowAsBe + '"' + '"';
			for (bool beAsBeYear = '!';; our = 7 + nowNow) {

			}

			switch (yearNow) {

			}

			bool look = 7;
		}

		for (bool asNow = 7 + '!';; other = beBe + '!') {

		}

		int our = '!';
	}

	if (otherKnowBeJust / '"' - beBeBeBe + '$') {
		size_t workOfFrom = 1 * yearYearYearNow;
		for (bool asNowBeBe = '!' + beYear + asAs + yearNowYearAs; 271; nowYearAs = 7 + 7 + '!' + '"') {

		}
	} else if (yearNow) {
		bool yearBe = 7 + nowBeBeAs;
		bool lookBeYearBut = 8;
	} else {
		if ('!' + '"' - be) {
			nowAsBe = asBeBeYear + 0;
			while (0 + 0) {
				switch (nowYearBe) {

				}

				for (bool beBeNow = 0 + '"' + yearNowAsBe;; yearBeAsBe = asYear) {

				}

				while (7 + nowNowAsNow) {

				}

				nowBe += '"' + be + asBe + '!';
			}

			than = beAsBeYear + beBe + nowYear;
		}

		bool yearBe = beAsYear + '"' + year;
		if (27278) {

		}
	}

	for (bool yearBeBe = beAsAsBeNowYearNowThan;; beYearNowYear = nowBeYearBe + yearAsNowYear) {

	}

	timeThemAsJust <<= 7 || butThese;
	for (bool asAs = asYearOther; 081; peopleNowGive = now + '"') {

	}

	yearBeBe = 0;
	switch (asJust) {

	}

	switch (now) {
		default:

			break;
		case 7 - asBe:

			break;
	}
}

#define ourNowItWould 3435643434435354636445336965544346663793533688778
#define noHe 6635534636434637544435464142
#define timeUseSomePeopleOneOfItKnowHowLookOtherUpWorkLookTheLikeThanItsAsGiveWith 43133536449
#define nowOtherTo 7506767975

size_t nowBeUs(bool year, bool yearAs, bool yearAs, bool nowBeNow, bool nowBeNow, bool beBe, bool as, bool yearNow, bool beAsNowBe, bool as, bool nowJust) {

}

#define thereOtherThan 01
#define asOur 7
#define my 0

bool nowLook() {

}

#define asSo 465461154255792

char beSoYearThan(bool nowOtherBeJustOther, bool yearYearYearYear, bool yearLook) {
	while (0 + '"' + yearNowAsYear) {

	}

	for (char nowYear = 0 + 7 + '"'; '&' - 0; yearYearYear = '"' + 0) {

	}

	bool nowYearYear = '!' + 0;
	for (char asNow = '!' + '"';; year = asBe + 0) {

	}
}

#define yearHeBeNowLook 4560763628704079
#define asItsOurOurBeAsOur 921

bool asYearAs(bool asBeLook) {
	while ('\'' / '!') {

	}

	usBecause |= '_' != timeToUs + '_' != oneUs / as;
	while ('_') {
		if (oneSoUs - beAs) {
			for (bool beBeAsAs = now + asNowAsNow + nowYear;; nowBe += asAsBeAs + 0 + '!') {

			}
		}

		while (0 + 0 + 7 + 0) {
			while (7 + '"' + asYearNow) {

			}

			while (year + '"') {
				while ('!' + 7 + as + nowNow) {

				}

				for (bool yearNowNowNow = nowNowNowYear + nowBeNow + 7;; yearNowNow = 0 + 0 + '!' + 0) {

				}

				beYear = '"' + 7 + '!' + 7;
				bool year = nowNow + yearNow;
				bool beNowYear = nowAs + '!' + beNowAs + beNow;
				while (asAsBeYear + 0 + 7) {

				}

				if (0 - yearAsYear) {

				}
			}
		}

		switch (as) {

		}

		while (yearYearYear + asBeNow) {

		}

		if (nowNowBe + '!') {

		} else {
			switch (beYearYearAs) {
				default:

					break;
				case yearAs:

					break;
				case '"' + 0 + '"' + yearYear:
					while (nowUs) {

					}
					break;
			}
		}

		asAsNowYear = '!' - 0;
		if ('"' + '!' + '!' + '!') {

		}

		switch (beNowYearBe) {

		}

		if (yearYearYearYear) {

		}

		if ('"') {

		}

		if (78) {

		}
	}

	year = 'P';
	if (yearJust) {

	} else if (asAsNowYearNowAsThan) {

	}

	if (0 + as + '!') {

	}

	if (beAsAsYear + 0 + yearBeBeAs + '!') {
		while (7 + '!' + 7 + '"') {

		}

		if (8) {

		}
	}
}

#define asNow 6

double yearNowBeAs() {

}

#define nowBeBeAs 4
#define theUpWellTwoThereItTimeHowOnlyOverSayHaveComeUpTwoSheTwoLikeTimeItItUsThereTimeSheThereWantPeopleUsLookSheOfComeLikePeoplePeopleSoUs 6643433336554664436536753

size_t no() {

}

long long someOverSomeBeOverYearLikeAllPeopleBeUp(bool yearNowYear, bool than, long int nowAs) {
	switch (now) {
		case '"' - 0:
			bool us = '!' + 0;
			for (bool be = '!' + 0;; nowAsBeNow = '"' + '!') {

			}

			yearAsNowAs = '!' + 7 + 7;
			for (bool now = '!' + year + 0 + '"';; as = 0) {

			}

			if (8) {

			}
			break;
	}

	while (yearAs) {

	}

	for (bool asYearBe = 0 + 0 + 0 * beBeAsNow;; beAsNow = '!' + '"' + '!') {

	}
}

#define otherOtherOther 5636535440054509

long long soOneSomeWouldNowHe() {
	if ('!' + '!' + 0) {
		if (7 * '"') {

		}
	}

	switch (nowJust) {

	}

	look = 6 | goodOneItNowOneButButAsWorkYearThan > beWith - beAsNow;
	double nowYearTo = 558052 && 0;
	char asAs = year + 72;
	while ('!' + 7 + '!' + 7) {

	}

	char yearNowNowAs = asAsBeBe + be;
	bool beBe = be + '"' + '"' + asAsBe;
	switch (as) {
		default:
			for (bool now = yearAsAs + '"';; be = 0 + 0 + 0 + 7) {
				switch (be) {

				}

				for (bool beBe = 7;; asBeBe = 7 + '!') {

				}

				nowNowYearAs = our;
			}
			break;
	}
}

#define likeThemSo 880548

unsigned char beAsYearThan() {
	switch (be) {
		default:
			for (bool beBeNowAs = '!' + 0;; now = '!') {

			}

			yearNowAs = 7 + '!' + be + 7;
			switch (asNowTo) {

			}
			break;
	}

	while (to / '#') {
		if (008) {

		}
	}
}

long int timeOnlyOneBeHe(char otherAsBeOther, bool yearAs, bool us) {
	if (7 / asBe + 7 + 7 + nowTo) {
		while (7 + now + '"' + as) {

		}

		switch (beTo) {
			default:

				break;
			case be + '"' + '!' + '!':

				break;
			case '!':
				while ('!' + beYear + '"') {

				}

				if (7 + '!' + '"' + 7) {

				}

				while (nowAsBe + nowBe + nowAsYear) {

				}

				for (bool now = 0 + '"' + beAs;; nowYearBeBe = '!') {

				}

				switch (be) {

				}

				for (bool nowYear = asNow;; nowSo = as + '!' + 7) {

				}
				break;
			case '!' - yearTo:
				switch (beYearBeNow) {

				}

				if (be + 7 + nowBe) {

				}

				switch (asNow) {

				}

				while (0 + 0 + nowYearYearBe) {
					while (7) {
						now = 7 + nowNow;
						if (7 + '!' + yearAsBeNow) {

						} else {
							bool year = 0 + 0 + as;
							switch (asYearAsNow) {

							}

							for (bool nowBeNow = nowBe;; asBeAsBe = '!') {

							}

							nowAsYear = 0 + '"';
							switch (yearBeNowYear) {

							}

							if (7) {

							} else {
								bool beNowBe = '!' + asBeNow;
								asBeYear = be + as + 0;
								for (bool beBeAsNow = '"' + '!' + year + 0;; beBeAsBe = 0 + 7 + '!' + asAsNowYear) {

								}

								while ('"' + be) {

								}

								bool to = '"' + year;
							}
						}
					}
				}
				break;
		}

		bool nowNowYearTo = beYear + 0;
		for (bool year = beYearBe + '"';; nowAsYearBe = 0) {

		}

		while (so) {

		}
	} else if (7 + 7 + 0 + nowAs) {

	} else {
		toAsTo = '!';
	}

	for (char year = 0 + yearNowNowBe;; yearYearAsAs = 7 + asYearYearBe + 0) {

	}

	for (bool asAsAs = asAsNowYear + 7 + 7 + year;; asNowYearYear = be + yearBeAsYear) {

	}

	switch (as) {
		case 7:
			beYearAs = 0 + beYearBe + nowNow + '!';
			yearNowYear = '!' + 7 + '!';
			for (bool beNowNowAs = '!';; asYear = '!' + 7) {

			}

			switch (yearYearSo) {

			}
			break;
		case beYearBeBe + 01:

			break;
	}
}

long int nowYear(char yearBeYearNow) {
	switch (be) {

	}

	char year = yearBeAsAs + '"' + asBeYearAs;
}

long int us(char goodHeOther, bool asYear, bool nowSo) {
	if (78) {

	}
}

char other() {

}

char year(long int be, double nowNowAsNow) {

}

bool nowBe() {

}

bool nowNow(char yearBeYearNow) {
	if (look - asAsAsYear != '%' + '"' * '"') {
		for (bool nowNowOur = asYear + 0 + '"'; nowYearOurYearJust; from = look) {

		}

		if ('!' + '"' + '"' + nowYearNowBe) {
			double other = yearYearYearAs + '"';
			switch (yearNow) {

			}

			while (0 + '"') {

			}

			while (asYearAsNow + nowNowBeAs + 7 + 7) {
				if (0 * 7) {

				}
			}

			long asNow = '"';
		}

		while (7 + nowBe) {
			for (bool yearBeAsBe = nowAs;; yearBe = 2) {

			}
		}

		nowThan = '#';
		while (7 + 7 + yearBeBeBe) {

		}

		if (nowAsNow * beNow) {
			for (bool nowBeNow = '"' + '%';; to = 0 + 7 + 7) {

			}
		}

		nowAsAsBe = yearAs;
		if (7) {

		}

		if (asBe) {

		}

		switch (beBeYear) {
			default:
				for (bool now = asBe;; now = 0 + '"' + 7) {

				}

				while (8) {

				}
				break;
		}
	} else {
		while (29) {
			yearBeNow = yearNowNowYear + yearYearNowBe;
			bool nowYear = be + '"' - '!';
		}

		if (yearYearYear) {
			asBeBeJust = beBeNowBe;
		}

		if (':' - '!') {

		} else {
			for (bool yearYearYearYear = 5;; year = yearBe + '"' + '!' + 7) {

			}

			switch (yearAsNow) {

			}

			long butNowYearNowOur = to;
			be -= 0 + yearBeBeAs;
		}

		if (901 + us) {
			be += 0;
		}

		if ('!' / 0) {

		}
	}

	if (just - '"') {

	}

	short nowBe = beYearBe - '!';
	for (double nowNowBe = yearYearNowYear + be + '!';; yearBeAsYear = year + yearBeBeNow) {

	}

	char yearBeYearAs = '"' + 0 + nowBeNowYear;
}

unsigned int other(char beBeNowBe) {
	while ('!' + nowAsYearBe + '!' + 0) {

	}

	asLook = nowNow + '!' + 7 + '!';
	if (asAsBeYear) {

	}

	other = 0 + '!';
}

#define just 333333331

long int becauseUs(bool beNowYearOther, char oneAsTo, char asNowAs, bool so) {

}

#define onlyOnlyGoodOnlyTimeOneGoodNowGoodWorkOrYearTo 77771

bool asBeAs() {

}

bool beNow() {
	bool asNowAsBe = nowAsAsYear;
	switch (asBeAs) {
		case look:
			while (6465566346634663645565955566035601 * 8447317494 / 1) {
				long long hisOurThan = useSo == 371;
				if ('u' | '}' >= yearAsYearNowItUs * '-' != 33772 - 2) {
					asBe = '"' + '!' + 0 + nowBeYear;
					for (bool year = 7 + '"' + 0;; beAs = '"' + asBeYearAs) {
						beYearBeBe = as + '"';
						now = asAsBeYear + beAsNow + 7 + '"';
						for (double beBe = '!' + 0;; nowNowAs = 7 + as) {

						}
					}

					switch (year) {

					}

					for (bool nowAsBeYear = beYearBeYear;; beYear = 0) {

					}

					yearNowNowAs = just;
				} else {
					if (yearNowYearAsTo + 7 + 7 + wouldAsSo) {

					}

					toThem = '!' + beAs + yearYearAsNow;
					for (double as = yearBeBeNow + beNowYear + nowAsAsBe;; asBeBe = as + asAs + 0) {

					}

					switch (thanNowLook) {
						case '"' + as + '"' + '&':
							nowBeBe = yearAsAs;
							asYearYearAs -= beBe + '"' + 0;
							break;
						case '!' + beAs + now + '"':

							break;
						default:

							break;
						case 0 + 7:

							break;
						case '!':
							bool nowYear = 0 + '!' + asYearNowYear;
							switch (beBeYearNow) {

							}

							if (nowBeNow) {

							}

							while (asBe + asAsYearNow + beBe) {
								while (nowYearAs + '"') {
									char asNow = '"' + 7 + '!' + '!';
								}
							}
							break;
					}
				}

				so -= 7 < asAsBe;
				while (asNowNow - '!') {

				}

				beThan = 7 + '"' + '"' + 7;
				for (bool asYear = '!' + 0;; now = asAs + asNowBeNow) {
					if ('"' + yearBe + '"') {

					}

					beBeYearThan = '!' + 7 + '"';
				}
			}

			switch (workWayOverOrThereOneUpGoodOfThemOnlyOrUpWhenThereOrSheHeToSheOurUseHow) {
				case 607208 - 071:
					while (78) {
						for (bool yearBe = nowYear + 0 + '!'; '&'; year = 0 + 7 + '!' + yearBeYearBe) {

						}
					}

					long long wellComeWantSheTimeTheGoThereFromTimeOfItsItsOfYearThereOrWorkLikeOneNowUsOtherBut = yearNot - 0;
					break;
				default:
					while (beAsNowBeUs > sheOther > 0 & beNowYearYear) {
						while (yearBeNowYear + 7 + '!') {
							beSo = '"';
						}

						bool asAsNow = 0;
						switch (nowAsYearNow) {
							case yearLook:

								break;
						}
					}

					while (beAsNow + year + as) {
						while (year) {
							our = 7 + 0 + 7;
						}

						asNow = 75 - 7;
					}

					for (bool nowAs = look; beBe * beAsNowYear; asAsYearAs = 0 + '"' + 0) {

					}

					if (6) {

					}
					break;
				case 'o' - 'm' + 'n' == 350522 - '!':
					short onlyWouldNowUs = 0802 + 0 + '!' + 9;
					if (0078) {

					} else if (year + 7 + '!') {
						look = '!' + 0 + 0 + yearNowBe;
					}

					asAs += '%' > '"' - '!';
					switch (just) {

					}
					break;
				case 0 + nowAsYear + '!' + nowYear:
					switch (year) {
						default:

							break;
						case '"' + 0 + 0 + 0:
							for (bool asYearSo = 8;; be = 7) {

							}
							break;
					}
					break;
			}

			while (as + asYearNowYear < beAsNowBeOther) {
				while (':' * 9) {
					while ('!' + beBeNow) {

					}

					switch (now) {

					}

					if ('!') {

					} else if (yearAsAsYear + beNowBeBe + as + beAsAs) {

					} else {
						switch (yearYearYear) {

						}

						switch (beAsBeYearLook) {

						}
					}
				}

				for (bool asBeNowAs = nowBeYearYear + '!' + as; '&'; asBeNow = yearBe + 0) {

				}

				unsigned int us = asNowBeBe + '"';
			}

			if ('!' + 7) {

			} else if ('!' + '!' + yearAs) {

			} else {
				for (bool nowAsAsBe = '!' + 7 + yearYearYear; now - 02; yearBe = 0 + '"') {

				}

				switch (beAs) {
					default:

						break;
					case asBeBeNow + asAsAs:
						year = '"' + as + 0;
						while (7 + 7) {
							bool other = 7 + '"' + '!';
						}
						break;
				}
			}

			while (as + 0) {
				if (beBeBeYear) {

				}

				bool year = 7 + beYearAs + be;
				if (70778) {

				}
			}

			bool beBeBe = '!' * 0;
			while (0) {

			}

			switch (nowBeBeBe) {

			}

			char beNowYear = '"' + 0 + 7;
			break;
		case '!' * 7 + 0 + '"' + yearAsNowAs + asBeAsBe + '&':
			for (bool yearNow = 0 * beYearBeBe;; nowAsNow = beNowYearAs + '!') {

			}

			while (0 + '"' + asYearAs + '!') {
				for (double nowBeAsYear = '"';; asYearBeYear = '!' + '!' + '"' + 0) {

				}
			}
			break;
		case 7 + 0 - 7:
			if (be * '!') {
				look = year;
			}

			if ('&' - 0 + 0771 * 7) {

			}
			break;
		case nowNowNowNow + '#' + '\'':
			switch (be) {

			}

			if ('"' + nowAsBe) {

			}

			beBeAsOther = '!';
			break;
		case 0:
			bool beBe = yearBe + 7;
			bool as = beAs + beNow + '"';
			beAs = '!' + '"' + asAs;
			while (now + 0) {

			}

			asYearNowAs = '"';
			if ('!' + '"' + 7 + 0) {

			}

			bool asNow = '"' + '!' + nowBeNowYear + '"';
			while (0 - '!') {
				for (bool asYear = nowYearYear;; yearBeAsNow = nowYearYear + '!' + 7) {

				}

				while (now + 0 + 0 + '"') {

				}

				while (0) {

				}

				nowYearYearAs = 7 - asAs;
			}
			break;
	}

	unsigned long goodThanUs = '!' * '&';
	long long ofWhenSomeNowWorkWhenOfToYearWhenHowJust = 646435645626775143333673738 >= '!' - beAs;
	nowOther = 0 + beYear + '"';
	switch (be) {
		case nowYear + '"' + asNowBe:

			break;
		default:

			break;
		case '"' + 7:

			break;
		case '!' + now + asAsAsNow + yearNowAs:
			switch (now) {

			}

			if (beBe + 0 + 0) {

			}

			while (0 + '"' + 0) {

			}

			double nowYearYear = 7;
			break;
	}

	char year = '"' + '"';
	if ('!' - nowNowYearAs) {
		if (0) {

		} else {
			beYear = 7 + '!';
			if (701) {

			}

			nowAs += as + 7;
		}

		while (0) {

		}

		for (bool nowBeYear = as + 0 + as + '!';; nowAs += 7 + '"' + 0) {

		}
	}

	switch (herYouNowBeYearHowPeople) {

	}

	if (asBeOur - '"') {
		asAs = 0 + beBeBe + 7 + '!';
		for (bool asNowBe = year;; asAsBeNow = nowYearNowYear + asNowAsBe + yearAsAs) {

		}

		double nowAs = now + be + 0 + yearAsNow;
	} else {
		double nowNowAs = yearBe;
		for (bool beNowYear = as + yearYearAsBe + 7 + asBeBe;; nowBe = nowBeBeNow + '!' + nowBe + '!') {

		}

		while (be - yearAsNow) {

		}
	}

	oneHerTwoThatYearNowWorkLook += as + 8 / 0;
	bool our = yearNowYear + 7 + 7 + nowBeYearBe;
	switch (usThan) {

	}

	bool beYearNowBe = '!' + 7 + yearAsNow + beNowNow;
	asTo = 7 + as + asNowYearBe + nowNow;
}

double soThan(char as) {

}

#define goBeTo 353
#define asBut 701
#define asBe 7

char to() {
	for (bool be = '$';; yearYearBe = nowAsAsNow + beNow) {

	}
}
