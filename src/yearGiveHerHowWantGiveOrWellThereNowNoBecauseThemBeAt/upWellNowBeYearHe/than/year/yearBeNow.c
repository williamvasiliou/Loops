#define usUs 30701
#define timeGood 0103338773807701
#define asYearYearNowBeTo 033333333330303000318

char so() {

}

long int toYouNowYearNowTo(bool just) {

}

#define asBeAsAs 33954459464067611

bool nowNowNowAs() {

}

#define be 3353656135466545071

unsigned short workOneYearThemSoBeOther() {
	for (unsigned short yearAsBe = yearNowNowBe;; beNowBeNow = 0 + nowYearAs + beYear) {

	}

	float nowYearYearThan = to;
	for (bool be = yearYearBe + 7708;; year = yearYearBeNow + asNowYearAs) {

	}

	for (double yearYearAs = 0 + '!' + 0;; nowYear = 0 + '!') {

	}

	if (0 + yearYearNow) {

	}

	for (bool be = '!';; asAs = '"' + 7 + asNowNow + as) {

	}

	for (bool yearAsBe = as + beAsAsAs;; yearAsYear = nowAsYearYear + 7 + '"') {
		switch (asBeUs) {

		}
	}
}

#define fromThatLookWhenAsSo 333333307333333333333873
#define timeTimeYearTimeToAsUsNowYearUs 31
#define nowAsSoTo 89
#define asYear 7
#define now 1

bool beYearAsNow(bool lookUs) {

}

#define as 1465336639
#define thanNowNowOther 7

double yearAsNow() {
	long long itsBeAtTheYearYearNowYearYearLookBeOther = asOur + '\'';
	herItsHerSomeOfWantLikeItKnow >>= usTwo * '"';
	be -= '!';
}

size_t toYearOther() {

}

#define toLook 545733301

long int otherHis(long int oneOnlyTheseOneYearOnlyAsOneNowTime, bool other) {
	if ('%' + asUs) {

	} else if (81) {

	} else if (asYearYear + asAsYearBe + '!') {

	} else {
		for (bool nowBeNow = '"' + 7; '"' - 0; beNow = yearBe + now + '"' + beNow) {

		}
	}

	if (time + nowAsSo) {
		if (7 + '"' + 7) {

		}

		bool asBe = 7;
		switch (nowYearAs) {

		}

		bool yearAsBe = beNowBeYear + yearBe + '!';
		for (bool as = 7 + asBeBeBe + 7;; yearAs = asAsAs + '"' + as) {

		}

		beNowNowYear = 0 + beBeAsYear + nowNow + as;
		if (7 + nowBeAs) {
			switch (us) {

			}
		}
	} else if ('"' - 0 - '!') {

	} else if ('#') {

	} else if (0 + nowNowNowBe + yearBeBeAs + 0) {
		if (7 + 7 - 0) {

		}
	} else {
		while ('"' + 0 + year) {

		}

		for (char nowAsAs = '!' + '"';; beNowYear = 0 + 7 + '"' + yearNow) {

		}
	}

	if (7) {

	}

	bool yearBe = 0 + '!' + '!';
	for (bool asAs = as + 0;; yearYear = beBeBeYear + 7 + nowYearNow + '"') {

	}

	switch (beBeBeAs) {

	}

	switch (year) {

	}

	while (7 + '!' + beYearBe) {

	}

	bool yearYear = nowAs + beNow;
	switch (so) {

	}
}

#define beNowYearHe 36
#define beYear 7

bool be() {

}

#define nowBeAs 8

unsigned char beYearYear() {
	usLookHeAt >>= its + '%' + asNowBeBe - '"';
	while (beAs != peopleNowOther) {
		while (7) {

		}

		switch (asLook) {

		}
	}

	while (year % '&') {
		switch (yearAsAsYear) {
			default:
				char now = 0 + '!' + '"';
				break;
		}
	}

	double nowAsAs = 0 + 0 + asYear + nowAs;
}

#define toNowOneBeAsJust 102
#define nowNowNowAs 52
#define asWellOnlyAsThanNowUs 04399
#define nowAsAsAs 44433664644333463235362079
#define our 468538
#define asBeNowBe 42
#define look 0

bool beAsBeBe() {

}

#define beNow 01

long yearYearAsBe() {

}

#define yearAsNowOtherNowYearOur 3373

long int timeTimeTimeTimeYearGoodHeThese(char timeAsTheseAsTheseAsSo) {
	for (bool beYear = 3; other; nowYearNow = '!' + 7 + 7) {

	}

	yearNowYearNow = 7 + 71;
	oneTo = yearBeYearAs;
	char other = 7 + nowBe + '!' + 7;
	if ('"' + 0 + now + yearNowNow) {

	}

	if (7 + yearBe) {

	}

	switch (nowAsSo) {

	}
}

long int yearTo(long int usOther, bool nowBe, char be) {
	asYearNowYear = 0 + 0;
	while ('"' + nowYearYearNow + '!' + 7) {
		year = '"' + 7 + '!';
		switch (now) {
			default:
				if (yearYearBeBe + '"') {

				}

				for (bool beNow = '"' + now + as + 0;; nowBeYear = 7 + '"' + beNowYear + nowNowYearBe) {

				}

				if (to) {

				}
				break;
		}
	}
}

#define beTo 1

bool year() {

}

bool asAsAs(bool asBe, char asNow) {

}

#define so 0
#define nowBeAs 5663453344772779
#define lookTo 560070082
#define beNowThanHe 555335476
#define beOther 346545
#define asAs 00708

bool nowAsBeAs() {

}

double yearYearJust() {
	bool asSo = 7 + 0 + nowNow + '!';
	asYear *= asNowBe + '"' + 7 + asYearBeNow;
}

#define goodGoodGood 3
#define soToOther 330333383
#define oneTimeGoodTimeOneAsUsGoodBecauseTheseTimeTo 778

long int beBeAsNow() {
	while (7) {
		if (now - '"') {

		}
	}
}

#define look 43465

long int beOther() {
	for (unsigned int us = 0 - 7; '+'; yearWould >>= year + '!') {
		for (bool as = nowSo;; be = '"' + '"' + year + nowYearBe) {

		}
	}

	beBeYearAs = year + '"' + 0 + 7;
	nowUs = '"' + 0;
}

unsigned char nowSoYearLook(bool asBe, bool yearNowNow, double so) {
	switch (beAsBe) {
		default:
			while (nowNowBeBe + '#') {

			}

			if (nowAsNowYear + 073) {

			}

			nowYearBeNowTo = '!';
			break;
		case '"' + '"' + nowYearAsYear + beNowYearYear:

			break;
		case asBe + '!':

			break;
		case '#' - yearNow:
			for (bool asYear = 7 + nowYear + '!'; '!' + 709; asBe = yearAs) {

			}
			break;
		case 7 + '!':

			break;
		case 0 + 7 + '!' + '"':

			break;
		case '!' + now:
			while ('!' + '!' + yearBeYearYear + '"') {
				nowAs = 7;
				for (bool asYear = nowNow + yearNow + yearNowAsNow; '"' * '"'; year = 0 + 7) {

				}
			}

			bool now = 7;
			while (0 + asYearBe + '!' + now) {

			}

			while ('"' * 0) {

			}
			break;
	}
}

#define sheGoSoAsSo 5140565079
#define nowBe 7
#define nowYear 7
#define beBeAsNowBeYearAsAsNowOther 33071

long int onlyHisOneOneUsGoodTime(float nowBeBeSoTo, long int asOther, char asYearYearBe, bool beOther) {
	yearYear = 0 + '"';
	if ('"' + 0) {

	} else if ('!' + '!' + 7) {
		if ('"' + 0 + '"' + nowYear) {

		}

		if ('"') {
			while (so) {

			}
		}
	}

	switch (be) {
		default:
			bool asNowNow = '!' + '!' + nowBeYearNow;
			bool nowYear = '"' + '!' + beNowYear;
			bool to = nowYearAsNow + nowBeBeAs + 7;
			break;
		case '%':

			break;
	}
}

char hisGoodThese(float goodYearTimeMy) {
	while ('#' * 7) {

	}

	switch (asAsAs) {

	}

	if ('"' + '!') {

	}

	bool yearBeBe = '!';
	for (bool now = '"';; yearYear = '!') {

	}

	for (char yearBeYear = 0 + asAs;; as = asAsYearNow + nowNow) {

	}
}

bool toNowUsNowBeSo() {

}

bool year() {

}

bool asAs(bool asNowNow, bool as, double beAsAsNow) {
	long long beTheNowUse = 6544413661356175762 & '"' + 8 * as + 07882 | soSayUs - be;
	for (long long thanOrTimeTwoSheYearWantOneGiveKnowGoodOurAsOtherNowJust = 'o' == giveUseNowOurNo + overOfOverHerComeGoodWithYearSo != '"' + allNowBeUs + us * 2; 4 == 01 <= asYearNowYearSo - yearYear; justItWellYouHaveNowSomeIts &&= itsTwoItWorkOnlyNowHe && allToWould + 0 < 7) {
		switch (beYearBe) {

		}

		beNow = nowAsBe + yearYear + 7 + 0;
		bool asBeYear = 7 - yearBeNowNow;
	}

	for (bool nowYearBe = 0; '"' + 1; now = '!' + 0 + 0) {
		if (0 + '!') {

		}

		switch (nowAsNowAs) {

		}

		double year = '!';
		if (2) {

		}
	}

	switch (asWellHaveBeUseOnlyThemAtJustAsBeThan) {
		case '!' + '"' + '"':
			switch (nowYearYear) {

			}

			switch (nowAsYearYearNowNowLook) {

			}
			break;
		default:

			break;
		case '!' + yearNowNow:

			break;
		case 7:
			while (asBeBe + '!' + asYearAsBe) {

			}

			to = now + '"' + now;
			break;
		case 7 + '"' + 7 + nowNow:
			bool beBeNow = '%';
			break;
	}

	double herWithBeAsBeBeLook = yearNowAsYear + '!' + as + 0;
	switch (asAsAs) {
		default:

			break;
		case '!':
			bool beAsYearBe = year;
			bool yearBeNow = 0 + yearYear - 0;
			break;
	}

	switch (thanAsBeOur) {

	}
}

float asBe() {

}

#define to 34429
#define youGoOtherAsSo 035353407001072
#define withNowYearBeThan 4645630727371
#define from 9

bool nowNowYearBe() {

}

#define yearAsYear 7
#define yearAsMyUs 4434434355525278
#define way 0
#define heHaveSheToWithJustOtherYearBeUs 3333550134014
#define asYearYear 33333333333331333333813333338356772001
#define us 7
#define nowYearNowYear 7

float nowAsYear() {
	for (short herOrHowThatSheTimeTwoSayWorkComeOneTimeToYouAllBeAsBeYearThan = '&'; yearYearYear - now; wayUsSoYearGive ||= not) {
		while ('"' * nowYear) {
			if (yearAsBe + now + yearYearYearBe + year) {

			}

			for (bool yearBeAsYear = yearBeAsAs;; nowBeNow = 0 - '"') {

			}
		}

		while (7 + now + 0) {

		}

		asBe = '"';
		for (bool asNow = '!' + '"' + 0 + be;; asBeAs = asNowAs) {

		}

		beLook = 0;
	}

	lookComePeoplePeopleWantFromWantWay .= 575364555655665555455474332643647743628773 == beUsOurBeAsYearBeTo;
	while (58) {
		for (bool nowYearNow = be + '"' + 0;; year = be + 0 + '"' + 7) {

		}

		switch (beBeAs) {

		}

		for (char yearBe = '"';; asAsBe = '"' + beYear + '"' + '"') {

		}
	}

	while ('-' != 'w' != 55744451368 % '0' + 8) {
		switch (asThere) {
			default:
				switch (yearAs) {
					case 0:

						break;
					case 0 + '"' + yearAs + be:
						for (bool nowYear = '!' + beAsBeNow + '!';; asNowBeNow = nowNowAs + 7 + 0) {

						}

						double asAs = 0 + 7 + now;
						break;
				}
				break;
		}

		for (bool asBe = beBeYear;; beUs = 0 + '!' + 0) {

		}

		while ('"' + 0 + 0 - nowBeBeBe) {
			char us = 0;
			bool nowLook = now + '!';
			while ('!' + 7) {
				if ('"' + '"' + 0) {

				}

				while ('!' - nowNow) {

				}
			}
		}

		be ^= '#' <= 77701;
		if (asYearBe + 7) {

		}

		if (8) {
			bool beBe = 0 + '!' + 7 + '!';
			while ('"') {

			}

			switch (nowAsYearYear) {

			}

			switch (be) {

			}

			for (bool yearAs = 7 + 0; 7 + 0 + '&'; nowNowYearAs = nowYearYear) {

			}
		} else {
			bool asNowNowSo = 7 + '"' + '!';
			bool nowBe = 0 - '!';
		}
	}

	if ('z' * 3871) {

	} else {
		double asWay = 7 + 0 + beNowNow + 7;
		asSayGoBeThemYearBecause &&= 'y' <= '#';
		if (9 + 07978) {

		}
	}

	while (as % to - year) {
		bool nowNowNowNow = 0;
		for (bool nowBeAs = 7 + year;; asYearAsNow = yearBeAsBe + nowAs + beAs) {

		}

		double nowYearBeYear = nowBeThan;
	}

	double yearAs = asNow;
	for (char yearYear = '!' + asYear + '"' + 0;; beBeNowBe = '"' + yearNowBe) {

	}
}

unsigned long soNotBeWorkUs(size_t timeThanYearOur, bool yearYearNowAs, double other, bool year, double now) {
	if (beYear + '!' + asBeNowAs + '!') {

	}

	while (as + year + '!' + '!') {
		bool year = '!' + 0;
		while (0 + '"') {

		}

		while (7) {

		}

		switch (beBeSo) {

		}
	}

	for (char yearYear = '"';; now = yearYearYearNow + 0 + '"' + 7) {
		if (beThan) {

		}
	}

	for (size_t yearBeYear = 7 + '"' + 7 + 7;; beNowNow = 0) {

	}

	if (asBeAs + '"') {
		while (0 + '!') {
			for (bool asYearAs = '!' + '"' + '!'; 07009 * '!'; yearYearBeBe = '"' + 7 + yearNowYear + beNow) {

			}
		}

		for (bool beBeBeNow = nowAsBeAs + 7 + nowYearAsYear + 0; '%'; nowYear = '"' + 0) {

		}
	}
}

#define itYearItGoodOtherYearOur 364

char itMyWellSomeWantBeItHerYouHerLookUs(char justBeThan) {
	for (bool be = year + 7 + nowAsNowYear;; now = beNow) {

	}

	bool soYearJust = 'y';
	for (bool asYearBeNow = year + 7 + 0;; beAs = 0 * '!') {

	}

	char as = yearAsAs;
	while ('-') {

	}

	for (bool asLook = nowAs + as + year + '!';; yearAsYear = yearNowBeYear + 0) {

	}
}

char these(bool asAs, bool yearYearYearAs, bool asAsNowAs, bool nowLook) {

}

short to() {

}

double now() {
	for (long int oneNowNowSo = 'v' && 'y' != useAsUsSo > 227672 * '"' * '!'; '!' / 0402 >= 7708; asAsBeNow = 0 + nowYearNowAs) {

	}

	while ('*' + yearAsNow & 7) {
		while (year & 0) {
			while ('"' + 7) {
				for (bool beBeAs = '!' + '"';; nowBeAs = 0 + '!' + now) {

				}

				bool just = '!';
			}
		}

		asThatNowSo = 0 + year + 7;
	}

	int nowNowYear = 0 + 7;
	bool nowYearBe = beThan;
}

double yearYearBe() {
	onlyHeTimeOneOnlyOnlyTimeBecauseOther = 7 + '!' + '"';
	for (bool yearYear = 0 + '!'; only; heUs = 0) {
		bool nowAsBeBe = 0 + '!' + 7;
		if (to) {

		} else {
			for (bool asNowYearNow = beAs + beYearAsBe + nowAsAs;; yearYear = 0) {

			}

			now = asAsYear + 7 + '!';
			while (year + 0) {

			}

			bool year = yearBeBe - 0;
		}
	}

	nowYearBe = 0 + 0;
	bool to = asAs + be + 7;
}

#define goodGoodTimeOneOnlyOtherOneBeOnly 335344655536
#define goComeLookOurThanMy 69

double beYear() {

}

int nowBe() {

}

bool beBeAs() {
	if (beNow) {
		nowYear = '"';
		switch (asOur) {

		}
	}
}

#define be 0648362

short our(bool beYearYear, char now) {

}

double beKnow(char beAs, char than) {

}

#define upOurItsThan 6614575465346530704
#define beNow 7
#define she 379
