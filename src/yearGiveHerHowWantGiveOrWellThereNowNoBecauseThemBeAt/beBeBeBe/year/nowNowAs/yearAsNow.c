#define goodOnlyTimeGoodOneOnlyTimeOneGoodGoodTimeTimeGoodUsAsSo 33333333620118
#define asBeAsYear 34565359565666766476344664503059

unsigned long now(long long no, bool beNowYearBe, double beBeYearAs) {
	if (6546002 == us * 7) {

	}

	while ('"' + nowBe + year + '!') {

	}

	switch (year) {
		case as:
			if (nowAsNow + 0) {

			}

			nowYearNowAs = 0 * 7;
			break;
	}
}

unsigned char asUs() {

}

unsigned int now(unsigned short atJustYearYearNowYearAsSo, double just) {

}

#define asYearYearBe 55597645732
#define yearYearBeAs 8
#define beYearAsYear 6354466563563465596433365446175
#define soOur 0
#define sheNowAsYearBeTo 78

bool as(bool look) {
	goodTimeOnlyOnlyOnlyOnlyUsSo = yearNow;
	long int asAsBeBeUs = beYearAsBe / 7;
	if (7 + '"' + as + '!') {

	}

	asOther *= '!' + asYearAs;
	bool nowAs = 7;
	if ('%') {

	}

	bool us = '!' + '!';
}

#define yearHeYearSoBeGoodHis 0
#define onlyGoodHis 1331
#define because 5

size_t nowYear(bool look, bool asNowYear, bool us) {
	if (7 + '!' + 0 + 7) {
		while (0 + 0) {

		}

		bool nowAs = as + 0;
		bool beAsBe = '"';
		for (bool now = '"' + as;; now = '!' + '"' + '!' + asAsYearYear) {

		}

		switch (nowNowBe) {
			case '"' + now + 0 + yearNowNow:

				break;
			default:
				switch (nowBeNowBe) {
					default:
						bool yearNowYearYear = beNowYear + '"';
						while ('"') {

						}

						bool yearBeTo = '"' + 0 + '"';
						break;
				}
				break;
		}
	}
}

char nowAs() {
	for (double beThem = 0 + '"'; '(' || 5; yearYearAs = '$') {
		switch (myThan) {
			case nowYearAsNow + 7 + '!':
				while ('!') {

				}

				asBeAsNow -= nowBeAs + '"';
				if ('"' + '!' + nowAsYear + '!') {

				}

				while ('$') {

				}
				break;
		}

		if (to) {

		}

		yearYearBe = '!' + '!' + asNowNowNow;
		switch (yearYearAs) {

		}

		now = beNow + '"' + 7 + 0;
		be = 7 + 0;
		if (0) {

		}

		while ('"' * 0) {

		}
	}

	for (unsigned char twoNowOther = 7463331709 - '!'; 'l' - 63364334445533603434439445753465538 || useOneOnlyThereItsThanWhenYearThat + 6; sheOneUpSoItsThanUseWhenLookThanNowOtherAsLook ||= our - 7) {
		unsigned short toLook = 79;
		if (0) {

		}

		be = asBe + 0 + beYearAs + 0;
		while (0 + yearNowYear + 7) {
			bool beNowYear = '!' + 7 + '"';
			if ('"' + year + nowNowAsBe + yearBeYear) {

			}

			while (yearNow + '"' + 7 + '!') {

			}

			if ('!' + '"') {

			}

			than = '!';
		}
	}

	if (3685764708 && '!' - nowYear) {
		just &&= 7 <= 'N' * now;
		if (nowNowBe * '!' * yearNowYearYear) {
			for (bool beAsNow = 7 + nowNow;; beYearYear = 7 + '"' + 7 + asYearBeBe) {
				if ('"' + '!' + 7 + '"') {

				} else if (as * nowAs) {

				}

				if ('"' + 7 + as) {
					bool beNowBeYear = 0 + '"';
					us = 7 + beNowBe;
				}
			}

			char beNowAs = 7 + yearAsNow + 0;
			bool beAs = yearBeAs + year;
			if (asYear + '"' + beAs + 7) {

			}

			switch (asNowBe) {
				case '"':

					break;
				default:
					for (bool year = yearBe + '"' + 0 + 7;; yearYearAsAs = asNowBe) {

					}

					switch (yearNowBe) {

					}

					for (bool nowAs = '"' + year + beNow;; yearYearNow = beAsNow + '"') {
						while (2) {

						}
					}
					break;
			}
		} else {
			for (bool twoYearAsAsAsBeYearYearYearYearNowBeNowOur = 7;; as = '"' + '"' + '"' + 0) {
				if (7 + 0 + beAsYearAs) {
					if ('"' * 0) {

					}
				}
			}

			while ('+') {
				switch (nowAsBe) {
					default:

						break;
					case asAs + beBeBeNow + nowBeNowNow + yearAsAs:

						break;
					case '"' + '"' + '"' + yearNowNowNow:
						switch (beNow) {
							default:
								asBeNow = 0 + '!' + 7 + '$';
								break;
						}
						break;
					case 7 + 7 + 7 + '"':
						while ('"' + year) {

						}

						switch (nowYearSo) {

						}
						break;
				}
			}

			now -= '"' + '"';
			switch (asUs) {

			}
		}

		switch (nowYearNowYear) {
			case as:

				break;
			default:

				break;
			case beBe + 7:
				while ('$') {

				}
				break;
			case 0 + '!':
				switch (year) {

				}

				double to = 7 + 0 + '"' + year;
				break;
		}

		while ('"' + be) {

		}

		char lookAsOther = 7 + nowAs + 7;
	} else {
		now .= 7 - '!';
		heYearAsThan = 0;
		switch (nowNowAsNow) {
			case nowBeAsAs:
				asAsBe = '"' + yearNowBe;
				for (bool be = nowAsAsBe;; be = 0 + 0 + nowYear + '"') {

				}

				asJust = '!' + '!' + beYear + '"';
				break;
		}

		switch (yearNowBeNow) {

		}

		if (us) {

		}
	}

	switch (useMyAsYou) {

	}
}

unsigned char toHerTwoWellOnlyWellUseOrOtherPeopleComeLook(size_t asLook, long double nowThereNowAsOur) {
	yearAsAsThan = '!' + '!';
}

bool up() {

}

#define comeSayGiveSayAsThatAsNot 63518
#define as 7

bool asAs(bool asNow, double year, bool beYearNowNowOur) {
	size_t look = as + 2;
	while ('"' - '"' <= nowBe) {

	}

	at = 0 + '"' + '!';
	if (079 * 7) {
		beYearNow = '"' + '"' + now;
		while (nowYearYearNow + '!' + yearAsBeYear) {
			for (double asYearNowYear = asBe + asNowYear; 8; asBe = '!' + '!' + '!' + '!') {

			}
		}
	}

	for (bool yearNow = yearBeYearAs + 7 + '"' + '!';; nowAsAsNow = 7 + '"' + 0) {

	}

	bool asBeNow = beUs;
	bool us = asAsNowBe + yearBeYear + 7 + nowAsAs;
	nowNowThan = 7 + nowNowYear + nowYear + beBeNow;
	if ('#') {

	}

	for (bool yearNow = asBe;; asBeAsAs = 0 + '"' + '!' + nowBeAs) {

	}

	switch (nowAs) {

	}

	for (double beNow = '!' + 7 + asNowNowAs + '"'; '!' + us; yearAsAs = 7 + 0 + yearNow + '"') {

	}
}

size_t asNowWith(char nowAllThan) {
	switch (yearYearBe) {
		default:

			break;
		case '!' - yearAsNow:

			break;
	}
}

unsigned short ourLookHow(bool asYearAsNow, unsigned char withThat, bool yearThan) {
	switch (well) {

	}

	switch (asYearLook) {
		default:
			nowYear += beYearNowNow + 7 + 7 + '!';
			bool asNowAsNow = 7;
			bool yearBeAsYear = '"' + 7;
			switch (beAsBe) {

			}

			while (beBeAs + 0 + '!' + '"') {

			}

			bool look = 0;
			break;
		case 7 + year + '!':
			while (now + '!' + 7) {

			}

			for (unsigned short beThan = 0 + 0;; as = '!' + nowBeBe) {

			}

			beBeNow -= asYear + year;
			break;
		case yearBeAsNow * as:
			while ('"' + 0 + yearAs) {

			}

			if (7) {

			}

			for (bool yearBeBe = 7 + '"';; nowBeNow = '"' + now + '"' + 7) {

			}

			while (asSo * 0) {

			}

			while (asAsAsYear + yearLook) {

			}
			break;
	}

	bool now = 709;
}

bool beBeAs() {

}

bool yearYearNowNow() {

}

bool than() {
	for (char usBeNowNowTo = 0 + 0 + yearAsNow + '"';; beBeNowNow = be + beBe) {

	}

	char onlyGoodOnlyGoodAsNowOnly = '#';
	if ('?') {

	} else if (0 + 0001) {

	}

	hisOneUs = 001;
	if (071 / 7) {

	}

	long int goodTimeUs = yearYearNowYear - 7;
	while (nowBeYear + 0 + beYear + beNowNowYear) {

	}

	toAsNowOther = '!' + asBeYearAs + 0 + '"';
	bool beNowBeAs = 7 + be + other;
}

#define other 3

long int nowAsBe() {
	us = asNowYear;
	switch (beBeYearYearOther) {

	}

	while (asBe + beNowYear) {

	}

	if (nowYearBe + nowYearYearYear + '"') {

	}

	while ('!' + '!') {
		while (now + '"') {

		}

		bool beYearUs = '!' + yearNowAs + 0 + 0;
	}
}

#define asOther 1

char justYearBeLook() {

}

#define yearNowBe 0

bool asNowNowNow() {

}

#define beNowNow 423

char nowBe() {

}

char year() {
	if ('*') {

	} else {
		int nowNowBe = 0 + 0 + '"';
	}

	bool nowAs = ')' - 7;
	bool just = as;
	switch (beYear) {

	}

	switch (nowBeNowAs) {

	}

	nowLook = '"' + 7 + 7;
}

long int workKnowSayOfSheItWhenGoodBeBeUsOneAsThemYearOther(bool asSo, bool asAsOther) {
	switch (beNowBeNow) {

	}

	switch (them) {

	}
}

float look(double ourAsJustNowNowSo) {
	yearNowLook = '"' + asYearAs + asNow;
}

char usBeOtherOther() {

}

long double nowJustBeAsThan() {

}

bool yearBe() {

}

bool yearYearAsYear() {

}

bool asBeNow() {

}

bool now() {

}

#define year 333333333333338333330333373

long int usOnlySo() {

}

#define us 3
#define asTheseThanYearAsJust 6
#define with 4
#define yearNowBeNow 653535463355354556544450603339351575

double nowNow() {

}

double beYearYear() {
	for (long int toUs = '"'; 7 + 71; asAs = '!') {
		for (bool yearBeAsYear = 0 + 0;; asYearNowNow = '!' + '"' + beYear + 0) {

		}

		switch (nowBeSo) {

		}
	}

	for (bool beNowNow = 0 + yearAsYear + now + '!';; be = nowAs + nowAsAs + now + 7) {
		switch (nowNowNowYear) {

		}

		if ('!' + 0) {

		}

		if (778) {

		}
	}

	long int timeGoodGoodYearBeOne = beYear + 7 + 0 + 0;
}

long int onlyTimeHeTimeHeNowBeTheseUs(long int to, bool goodBeTo, long int becauseAsBeBeLook, bool yearYearYearAs, bool thanBeYearBeThan, bool year, bool yearNowBeAs, double as) {
	bool asAsBeToNowOther = 7 + 0 + 7;
	char yearYearAs = 0 + '"' + '!';
	bool beYear = '"' + '"' + 0 + '!';
	double year = 7;
}

#define as 7

char yearBeYear() {
	unsigned int twoTwoThereJustTo = '"' / '!';
	if (66363633336343563694665469 == 'c' || goodUsComeTwoThem <= 724 + other) {
		if (54 * 02072) {
			while ('&') {

			}
		}

		switch (asTo) {

		}

		for (bool as = '!'; 01; asYearNow = '!' + nowNowAs + nowAs + yearYearNow) {
			beNowBeYear = asNowOther;
		}
	} else if (29 - 7) {
		switch (beYearNot) {
			default:
				switch (asOtherOtherThem) {

				}
				break;
			case 0 + yearAsBe:
				if (yearNowYear + 7 + yearNowYear + nowBeYearBe) {
					asNow = asYear;
					be = 0;
					for (bool year = 7 + 7; nowUs; be = beBeBeNow + '"') {

					}
				}
				break;
			case us:
				if (asYear) {
					for (bool yearNowYear = '!' + 7 + '"';; yearAs = yearYearAsYear + yearYearYearNow) {

					}

					for (bool look = 0;; beNowNow = nowBe) {

					}
				}
				break;
		}

		for (char now = '"' + '"'; 9 - 0; yearOur = '!') {
			char beYearYearBe = '!' + yearYearBe;
			nowAsYear = 0 + be + 0;
			as = '"';
			bool nowAsYear = nowNow + '!' + '"';
			yearNowNowNow = 0;
			for (bool now = asNowYear;; yearAs = 0) {

			}

			while (asNowYearYear) {

			}

			if (1) {

			}
		}

		bool just = be + nowBe;
		bool nowYearAsBe = 7 + 0 + '"' + 7;
		bool they = beYear + asNow;
	} else if ('%' < '^' * nowNowYear) {
		if ('"') {
			for (bool asBeNowAs = beBe - nowYearAsNow;; nowNowBeBe = yearAsBe + nowNow) {

			}
		} else {
			for (bool yearAsYear = nowBe + '!';; yearNow = '!' + '!' + '!' + as) {

			}

			if (7 + nowAsAs + 0) {

			} else {
				switch (beYearBe) {

				}

				yearNow = year + nowYear + 0;
				if (beNow + '"' + '!') {

				}

				if (7 + 071) {

				}
			}
		}
	} else if (9) {
		for (char yearYearYearNow = asAs + '"' + '"'; '"' - asBeAs + 0 * 7; nowAsAsAs = 0 + 7) {

		}

		bool nowBeAs = 0;
		yearAsNow = beNowNowNow + '!' * '!';
		for (bool yearYearAs = '!' + 7;; beYear = '!' + beBeBeBe + 0 + '!') {
			if (asThan) {

			}
		}
	} else if ('"' + asYearAsAs + 7) {

	} else if (072) {

	}

	for (long int comeOverHeKnow = 447456006209 & look | theseNowYearLook >= 0 > asNo + yearBut || nowJust; onlyUs | 1 | '!' >= 709 / '!'; comeThereNowHerOrThemYearJustOverSheBeBeBeOurAsAsBeNowThan =~ '"' & '$' % 'k' + asSo + '#') {
		beAsBeBe = '"' + as;
		switch (to) {

		}

		switch (ofToAsBecause) {
			default:
				switch (nowBeNowNow) {

				}

				while (7 + be + asNowNowAs + 0) {
					if ('"' + '"' + as + '!') {
						switch (as) {

						}

						yearYearYearNow = be + '"' + 7;
						while (beYearYearBe + 7 + 0 + 7) {

						}

						while (0) {
							bool be = 079;
						}
					}
				}
				break;
			case '"' / 0:
				switch (asBeUs) {

				}
				break;
		}

		if ('.' + 8) {

		}

		switch (oneBut) {
			default:

				break;
			case asBeYearAs + 2:

				break;
			case 7 + as + asBeNowYear + '!':
				switch (other) {
					case '&' - yearBe:

						break;
				}

				be = '"';
				switch (nowBeBe) {

				}

				while ('%') {

				}
				break;
		}

		as = asYearYear;
		if (nowNow - '"' + '%') {

		}

		if ('"' + look) {

		} else if (yearYearNowNow) {

		} else {
			for (bool as = nowYearNow + as;; asAsBe = 0 + 0 + now) {

			}

			bool yearAs = 0;
			for (double as = 7 + asAs;; beNowYearYear = beNow) {

			}
		}
	}

	unsigned long onlyWorkHowKnowWorkOverSomeTimeOverUpYouItBeSoOnlySomeNowYouOfToSo = 'x' - theLookNo && nowYear == 'b' != 98 < our;
	switch (orJustBeHerItNowNowLookOur) {
		case as + '"' - 0:
			if (7 + beNowAsBe + yearNowBe + 7) {

			}

			yearJust = 0;
			break;
		case asYearWay:

			break;
	}

	while ('"') {
		char asYearNowBe = '!';
		bool nowYear = '!' + 7;
		while (yearNowBeBe + '$') {

		}

		switch (to) {

		}
	}

	themNowTo &&= '%' + 70709;
	asYearNow = '!';
	while (7) {

	}

	if ('"' - yearBeNow) {

	}
}

long long haveWhenTwoTheseThanBeLook(bool year, bool beYear, bool year, double my, char yearBeAsNow, bool beYearYearBe, bool as, char asYearBeBe, bool just, double other, bool nowUs) {
	switch (nowYearNowThey) {
		case haveNowOurNowAsNowYearLook * '!':

			break;
		default:
			yearYear += be;
			switch (asYearBeBe) {
				default:

					break;
				case 7 + '"' + 0 + 0:

					break;
				case '"' + yearNow + '!':

					break;
				case '!' + asYearBe + '!' + '"':

					break;
				case 7 + '"' + nowNowYear + '!':
					if (year + '"') {
						to = '!' + '!' + nowYearAsBe;
					}
					break;
			}
			break;
		case over:
			switch (year) {

			}

			for (bool asAs = yearBeAsBe + asBe + 7;; yearNowNowNow = nowYearAsNow + 7 + 7) {

			}

			switch (beBeYear) {
				case 7:

					break;
				default:
					if ('!' + 0 + nowBe) {
						if (year + yearNowNowNow) {

						}

						while (yearNowBe + '"' + '"') {
							double as = 0;
						}
					}

					bool asNowNow = '"';
					as = 0 + be + 7 + 7;
					our = 0 + 0 + '"';
					break;
			}

			while ('!' + asNowAsNow + year + '!') {
				switch (beBeJust) {

				}
			}
			break;
		case yearNowYearYear + '"' + so + yearNowNowAs + asThan:

			break;
	}

	nowTimeWantAsOurBeTo =~ '#';
	size_t yearThan = 7 <= to;
	if (7 + beYearNow + '!' + '!') {

	}

	while (1) {

	}
}

long long lookBeThe() {

}

long int people(double beAs) {
	if (other) {

	}

	bool nowBeBeAs = '!' + '!' + 0 + 7;
	switch (year) {
		default:

			break;
		case 7 + 0 + '"':

			break;
		case be + 7 - beYearAsAs:

			break;
	}
}

bool no() {

}

unsigned long justJust(bool asYearBe, bool asBe, bool so, bool yearAs, bool beNowAs, bool beAsBeYear, bool so) {
	switch (asBeBeNow) {
		case yearAsAsBe:

			break;
		case yearYearAs:
			while (yearNow) {
				for (bool nowBeAs = 0 + as + beBeAs;; beAsBeNow += '!' + yearAs) {

				}
			}
			break;
		case 0:

			break;
		case 7 + 7 - '"':
			nowNowBeYear = '"' + '"' + nowNowAs + 0;
			switch (nowBeJust) {

			}
			break;
		case 7 + '!':

			break;
		case beNowNowNow + '!' + beBe + '!':
			for (bool so = 0 + '"' + '"' + '"';; yearNowYearNow = '"' + 0 + now + 7) {

			}
			break;
	}

	if (asYearAsAs + nowAsBeBe) {

	}

	if (0 + 0 + 7) {

	}

	switch (nowAsAs) {

	}

	while (7 + '"') {
		year -= '"';
	}
}

unsigned long becauseLook(long beNowYear) {
	as = asAsBe + yearYearAsYear + 7 + '"';
	while (7 + '!' + 7 + beAs) {
		switch (asNowAs) {

		}

		for (bool than = yearNowAsNow + 700002;; yearYearAsYear = asNowYearBe + '!' + yearYearBeNow) {

		}
	}

	to = 7 + beBeYear + asBe;
}

#define peopleWhenSoFromWork 4

bool sheOrComeOver() {

}

#define yearSo 7

bool otherBeNowJust() {

}

unsigned short myYearNowAsOther(bool as, long beYearYearAs) {
	beNowNow -= beNowNow + nowBeYearNow + 7;
	yearAsOurYearAsOther -= 72 - nowYearYear;
	if (yearAsNowAs + asBeNow + as) {

	}

	nowYearNow = 7 + '!';
	for (bool beUs = 0 + now + 0;; asYearAs = '"' + 0 + yearAsBeYear) {
		nowAsBeSo = '!' + '!' + 0 + 7;
	}
}

char beBe() {

}

bool beYearAs() {

}

char asBeAsYear() {

}

bool asAsNowYear(bool nowBeBeAs, bool yearAsAs, char nowAsAsLook) {
	while (beSo != now - yearAsBeNow) {
		if (yearBe + yearNowYear) {

		} else if ('"' - '"') {

		} else if (other) {

		}

		if (0 + as) {

		} else {
			switch (beAsAs) {

			}

			if (7 + 7 + yearYearYear) {

			}

			while (0 + 7 + 0) {

			}

			bool asYear = yearAs + beNowYear + 0;
			while (0) {

			}

			while (0 + 0) {
				while (asYear + '"') {

				}

				bool beBe = beYear;
				for (char asYearAs = nowYearBeBe + 7 + 0;; as = '"') {

				}
			}
		}
	}

	if ('%' - beBe) {

	}

	while (7 - '"') {

	}
}

#define timeHisYearTo 3333083

long int theseOnlyBeOther(long int as, float yearNow) {
	while (beAsAsYearAsSo) {
		switch (other) {

		}

		yearAsAsNow += '"' + 7 + 7 + nowNowYear;
	}

	for (bool other = 0 + beYearAsAs + '!'; '#'; yearBeNowNow = 7 + 7) {

	}

	bool beNowUsThese = asAs;
}

float sheLookSoNot(bool asAsBeNow, bool you) {
	switch (yearBeBe) {
		case '#':

			break;
		case '"' + '"':
			while (beUs) {
				switch (goodThereSheLikeGiveThatOverThereOfThereHaveOnlyYouKnowUseYearOrGiveComeUpComeHerGoodLikeGoodItLikeUpWhenMy) {
					case 01:
						double asAs = '!';
						break;
					default:
						for (char asBeNowYear = '!' + '!' + '"' + beNowNow; 01; nowBeYearAs = 7 + '"') {
							if (look) {

							}

							for (bool asAsNowAs = 7 + 7 + nowNow + '!'; 078 + 0 + '!' + asAsNow + 9; beNowNow = 0 + 7) {

							}
						}

						for (bool asBeBe = '!' + '"';; nowNowYear = '"') {

						}

						unsigned int asYearJustJust = '!' * 7 * '!';
						break;
					case to:
						switch (our) {
							default:
								for (bool nowYear = '&';; beNowYearAs = '!' + '!') {

								}
								break;
							case asBeYearAs - '%':

								break;
						}

						unsigned short yearLook = '-' - 7 + '%';
						for (bool as = 'J';; asAsBeNow = 0 + '!') {
							for (char as = '!';; beNow = nowBeNowBe + '!' + '!') {

							}
						}

						for (bool beYearAsAs = asNowBe;; yearAsNow = asAs) {

						}

						if (0 + year) {

						}

						switch (nowBeYearYear) {

						}

						while (beYearBe + asYearBe) {
							switch (nowYearNowYear) {
								case '!' + '%':

									break;
							}
						}
						break;
					case 74:
						bool now = nowBeBeYear + as + beYearAsBe;
						if (beAsLook) {

						}
						break;
					case 0 - 7:

						break;
				}

				beBeBe = asUs;
			}

			for (short thatHeBecause = so & 'G' * yearNotNowJust; 4346546346448746567460699 | 'g' != now; yearOur -= 0 * yearNowNow) {
				while (7) {
					while (beBeAs) {

					}

					if ('!' + nowNowBe + 7) {

					}

					while (yearNow) {

					}

					bool beNowBeAs = '"';
					switch (as) {

					}

					for (bool yearBeAs = '"';; nowSo = '"' + 0) {

					}

					switch (nowSo) {

					}
				}
			}

			while (5650042886 - 'y' & look && 37588 + '#') {
				unsigned int thanCome = yearNowYearAs + 0 + '!';
				for (long int ofNowThan = 2304 || otherNowUs >= '%' + 00007708 * nowAsBe;; yearYearAsBe -= be) {
					for (bool beAsYear = 7 + 0;; nowYearLook = '!' + beBe + nowNowAsYear) {

					}

					while ('&' + nowAsAsNow + asOther) {
						for (double than = beYearNow + nowYearAsBe; '!' + beNowBe <= 0; yearAsYearBe = '"' + '!' + yearAs + '!') {

						}

						for (bool as = nowYearYear;; nowAsAsBe += '"' + nowNowBe) {
							bool nowYearAsYear = asAsNowBe;
							char year = 7 + '!' + beYearBe;
						}

						if ('"' + '!' + nowBeBe + 0) {

						}

						if (0 + 0 + '"' + beBeYearNow) {

						}

						while (asAsAsNow + '!' + '"' + beNowAsAs) {
							while ('"') {

							}

							for (bool asYearAs = '"';; beNowNow = as) {

							}

							bool as = '"' + asBeNow + 7 + 0;
							for (int beBe = '"' + '!';; asBeAs = 7 + nowYearNowYear) {

							}
						}
					}

					while ('$' + nowYearNowYear + '!' * '"') {

					}

					if ('"' - '!' * 0) {

					}
				}

				if (beSo + now + asLook) {
					for (char nowNow = '"' + nowNowNowBe;; asAsBe = '!' + nowNowBe + 7) {

					}
				} else {
					double asNow = yearYearAsAs + yearBeYearNow + nowBeNowAs;
					while ('"' + 7 + yearYearBeNow) {
						switch (beYearNowAs) {

						}

						if ('"' + '"' + be + be) {
							for (bool asBeYear = nowYearNow + beNowYear + 7 + '"';; beBe = yearBeBe + 0) {

							}

							bool yearAs = 0 + 0 + '"' + '!';
							if (year + '"') {

							}

							if (yearAsYearAs + 7 + '$') {

							}
						}
					}

					switch (asBeAs) {

					}

					nowAsBeBe = 1;
				}

				while (6575561345664 && thereOverWant / 344 * 0) {
					for (bool otherNowLookYearAll = '!' + '"' - 7;; atHerBeAsOtherSo = beBe + '"') {
						for (char nowBeAsBe = beYearAsBe + '!' + '!' + yearYear;; yearBeAsYear = '"' + '"') {
							while ('%') {

							}
						}
					}

					while ('!' + beNowYear + '!') {
						so = '!' + be;
					}
				}

				bool nowNowBeBe = '"' * 7;
				bool yearAs = '"' + nowBe + asBeYearYear;
				for (bool beBe = asAsAsNowThan;; beYearAs = 0 + '!') {

				}

				for (bool nowYearNowAs = '!' + 0; asYearAsNow - '"'; be = '"') {

				}

				bool beBeAs = other;
			}

			for (long twoJustToBeAsUs = yearThese; 45708; nowAsLook = '"') {
				while (asBeAs + '!' + 0 + yearAsYear) {
					switch (yearOther) {

					}

					unsigned short toOther = '!' * 7;
				}

				for (float as = '"' + '!'; 2; as = '"' + be + '"') {

				}

				for (double beAsYear = 0 + beAsNow + '!' + '!';; beAsAs = yearAsAs + '"' + 0 + yearBeAsBe) {

				}

				year *= nowAs;
			}

			switch (than) {
				default:
					bool asAsYear = beNow * nowBe;
					break;
				case '!' + 0 + '!':

					break;
				case 702:

					break;
			}

			switch (hisItsOurAsAsOtherNowAsThemWould) {
				default:
					switch (beNow) {

					}

					while (7 + beBe) {
						nowYear += 0;
					}

					bool beNowYear = 7;
					bool now = nowAsAs;
					asBeBe = 0 + '!';
					for (bool yearYear = asNowYear + 0;; asBeNow = 7 + '!' + '"' + '"') {

					}

					for (bool yearYearYear = '!' + '!' + 0 + 0;; nowAsYearLook = '!' + 0 + now) {

					}
					break;
				case 0 + '!' + '"':
					for (bool yearNow = 0 + beYearAsYear + beNowYearNow + '"';; year = '!' + 0 + year + nowNowAs) {
						yearBe += 0 + 7 + '!' + '!';
					}
					break;
				case yearNowBe - beAsNowAs:

					break;
				case '&':

					break;
			}

			switch (us) {
				default:
					while (look) {

					}
					break;
				case now + now + '$':

					break;
			}

			bool look = '!' + '"' + 7 + '!';
			break;
		case 'c' && 5363666363654655564463365553563373364635535444464333533645573344436336413444445307707002 && 'l' > howGiveGoodSoGoOrSomeWellWellOverOverHerHaveOneThatSheOneSayWantUsAsJustOtherThereJustSayUsAsOtherNowBeNowJust & 66566436346345435563566445062336 < '|' >= 'b' > onlyPeopleLikeWellSaySaySayGiveOneOfItsThereOfHerHerOnlySheSayAsTimeButBeAsWayOfHisOurTo >= ourHowItsPeopleComeSheOfGiveKnowKnowNowOrSheSomeYearAsBeBeYearBeYearBecause + 658 % '"':
			likeTheLikeTheseWantGoHis ^= 6666533533664444656432634445341522 & likeGoWorkThereHerAsBeOur || 6536556563543547443642480072 <= thanToJust + '"' + '!' + 0 * '"';
			switch (theseTheyOfLook) {
				case yearBeAsBe + '!' + asYearNow + '"':
					while ('%') {

					}
					break;
				default:

					break;
				case '!':
					if (09) {

					}
					break;
				case '!' - '"':

					break;
			}

			unsigned int beNow = asBeAsAs + 7;
			switch (beYear) {
				case '!' + nowSo:

					break;
			}

			while (yearAsNowYear + '"') {
				switch (be) {

				}

				bool asNow = 0;
				year -= 0;
			}

			bool beYearAs = as + 0 + beBeBeAs + 7;
			for (bool year = '"' + 7 + 0;; yearYearAs = as + nowYearYear + 7 + '"') {

			}

			bool asYearNowNow = 0;
			switch (asThan) {

			}
			break;
		case 7 + '"' + 0 + '!':
			while (8 - '#') {
				nowBeNow = '!' + year + '!' + nowAs;
				for (bool yearBeNowYear = 709;; yearAsAs = 0 + '"') {

				}

				other = 7 + as + beAsNow;
			}

			while (likeWithAsNowWantHe - '!') {

			}

			if ('!' + '%') {

			}
			break;
	}

	while (656453549 != nowYearAs != asThem & '\'') {
		beBe = as + 0 + '"' + yearBe;
		switch (twoHowTwoToThatWellJustLookOther) {
			default:
				if ('!') {
					double beYearYear = yearBe;
				}
				break;
			case nowNowAsYear == '/':
				while ('"') {

				}

				our = 0 + '"' + '!';
				for (char asBeBeAs = '!' + 0 + 0 + now;; beYear = 7 + 7) {

				}
				break;
			case 0 + '"' + 0 + asAsBeAs:
				nowAsAsAs = '!';
				if (yearYearBeAs + beNowYear + yearBeNowAs + 0) {

				} else if (nowAsNowYear) {

				} else if (as + asAsAsAs + '"') {
					bool as = beYear + 7 + 0;
					bool beAsOur = now + nowNowYear;
					for (double asAsYearNow = 7 + 0 + 7;; yearBeBe = 7 + 7 + now) {

					}
				}
				break;
		}

		unsigned int otherAsTo = as + asNow + be;
		yearThan += 7 + 0;
		for (int asBeNowBeUs = '"' + '!' + asNow; yearAsSoTo & 7 + 778; lookBeOurAsBeJust = year + '"') {
			beNowYearYearYearBeSo = 7 + '!';
		}

		if (thanTheSayLookBeThanThan < beYear + other + 0 * 7) {
			switch (now) {
				case 0 + '"':
					switch (beAsNow) {

					}

					beBeBeNow = as + '"';
					while (yearNow - '"') {

					}
					break;
				case 012:
					bool beBeBe = 7 + '!' + beNowNowNow + 0;
					while (asAsBe + 7 + 0) {

					}

					switch (nowNow) {

					}

					switch (nowAsNowYear) {

					}

					switch (beBeBe) {

					}

					while ('!' + yearYearNowAs + beAsYearYear + '"') {

					}

					if (0) {

					}

					if ('"' + 2) {

					}
					break;
				default:

					break;
				case asNow + asBe + '!':
					for (bool nowAs = yearAsBe + asBe; '%'; nowAsYear = '"' + 0 + 0 + 7) {

					}
					break;
			}
		} else {
			switch (otherThese) {
				case '"' + '!' + 7:
					for (bool nowNowYearBe = '"' + asNowYear + 0;; beNowAs += '!' + 7 + '!' + yearNowBeNow) {

					}
					break;
				default:
					nowThan = 7 + year;
					break;
			}

			bool nowNowJust = now + '"' + year;
		}

		while (2772) {
			if ('"' + 0 + 7 + '-') {

			} else if (218) {

			} else if (nowBeAs + year + yearNow + 7) {
				asNowBe = '!' + '!' + 7 + 0;
				for (bool yearYearAsYear = 7 + as + '"' + as;; year = 0) {

				}

				switch (beYearBe) {
					default:
						if ('!' + '!' + 7 + 0) {
							switch (nowYear) {

							}

							year = 7 + '"' + 0 + '"';
							yearBeBeYear = '"' + yearNowYearYear;
							if ('"' + '!') {

							}

							switch (nowJust) {

							}
						}
						break;
				}
			} else {
				if (asAsNow + '!') {

				}

				us = 7 + 7;
			}

			switch (just) {

			}
		}

		so = asYearBeNow + 7 + yearNow;
	}

	switch (workLikeOrGiveGoodWithThatSomeOnlyOfTwoBeAsJust) {
		default:
			switch (whenUpGoodOrAsNowBeYearThan) {
				default:
					fromMy -= year;
					for (bool beYearBe = 0;; yearBeBe = 7) {

					}

					as *= '"';
					break;
				case 3 | '"':

					break;
			}

			switch (timeHe) {
				default:
					for (bool be = 7 + 7;; as = nowAsAsBe) {

					}

					if ('"' + as + 0 + '!') {

					}

					for (bool asYearYear = yearYearBe + 0 + 0 + year;; be = 0) {

					}

					as = '!' + 0 + '!' + '!';
					while (beYear + yearAs) {
						beBeAs = 7;
						for (bool as = 7 + '!';; asAsBe = '!' + '"' + beYear) {

						}

						yearNow = 7 - 7;
					}

					switch (yearAs) {

					}

					if (01) {

					}
					break;
				case '!' + '"' + nowNowAsNow + '!':
					if ('"') {

					} else {
						if (7 + '!' + be + beNowBe) {

						}

						double beBeNow = 0 + 7;
					}
					break;
				case 0 + 0 + beNow + '"':
					if ('"' + 0) {

					} else if (7) {

					} else {
						beBeBeYear += 7 + beAsYear + 7;
					}

					if (asYearNow) {
						while ('"' + '"') {

						}

						while ('"') {
							bool nowBe = '!' + 7 + beBeBeNow;
							if (7 + '"') {

							}

							switch (nowAs) {

							}

							for (bool asBeBeYear = yearYearBe + 0 + 0;; nowAsNowBe = 7 + beNowYearYear) {

							}

							switch (yearThan) {

							}
						}
					}
					break;
				case 7 + '"' + 7 + nowAsNow:
					bool asBeBeBe = soNowAsBeLook;
					break;
				case '!' + 7:
					now -= nowNowNow + '"';
					break;
			}

			float be = 7 + yearAs + 7;
			double year = 7 + year + yearAsBeBe;
			while ('%' + '$') {
				if (asYearYearYear + yearOur) {

				}
			}
			break;
		case '}' || goodKnowGoodComeKnowOverTimeTheSaySomeUseHaveUpTheWellSomeThanComeTheGiveYouOneComeItFromUs <= '#':
			while (0 + 0) {
				beBeBeAs -= 7;
			}

			for (long int overHer = 7; '%' / nowYearNow; be += asAs + 7 + 0 + 7) {
				if (be + 0) {

				}

				bool now = '!';
				while (9) {

				}
			}

			for (unsigned long they = '%' + ')'; 604751 + '"' + asYear * asNowAs; be = asNowYearAs) {

			}

			nowYearSoBeBeSoJustBeAt -= 7 + beAs;
			break;
		case '"' + 0 + 0 + 7:

			break;
		case thanWantOtherOther < '=':
			if (nowNowBe + '"') {

			} else if (yearNowYearAs + 7) {

			} else {
				for (bool yearAsAs = '!';; asAs = '"' + nowNow + 7 + '"') {

				}

				bool nowAsBe = '!' + '"' + '!' + nowYearYearYear;
				while (yearAsYear) {

				}

				if (0 + as + 7 + '!') {

				}

				bool yearAsNow = 0 + '"';
				char yearNow = yearAsAsAs + '"' + '!' + 7;
			}

			if (778) {

			}

			bool yearYear = 7 + 7 + year + 0;
			switch (asBeAs) {

			}

			if (02) {

			}
			break;
		case 7 - '"':
			if (myAsBeUs) {
				while (7 + ')') {

				}
			} else {
				switch (would) {
					default:
						switch (nowAsUs) {

						}

						switch (year) {
							case '!':

								break;
							default:

								break;
							case nowYearBeAs + 7:
								beBe = 7 + beNow + year;
								yearJust = '"' + '"';
								break;
						}
						break;
					case year + year:
						to = yearYear + 7 + '"' + nowBe;
						break;
				}

				if (7 + '"') {

				} else {
					switch (so) {
						case '!' + beNow + asAsYearYear:
							asBeAs = 0;
							for (bool beYearAs = asBeAs + 7 + now;; beYearYear = 7 + yearBeAs + '"' + be) {

							}

							char as = 7 + '"';
							break;
					}
				}

				for (bool yearYearAs = 0;; so = 0 + '!' + '"' + 7) {

				}

				switch (nowOur) {

				}
			}

			switch (yearYouThanBeUsAsBeBeOther) {
				default:
					bool yearYearBe = '"' * year;
					break;
			}
			break;
		case 0:
			bool beAsYear = so;
			break;
		case '"' + '!' + beNowYear:

			break;
		case nowNowBeBe + '!':

			break;
		case '"' + 0:
			while (so) {

			}
			break;
	}

	unsigned short knowWantTwoWorkLikeWantThatWorkKnowTwoSomeBeGoGoItHerSomeYearHowOtherKnowUseGoodHowGoComeAsAsYearThemUs = theGoodItGoodOnlyLikeTwoJustWellGoodTheItsOrOrYouWantYouHowItGoodOfUseHaveThereHaveWantOnlyYearComeWellOverThereOneComeSayOverWhenGoPeopleGoSayKnowThemUsHerOtherSomeOverGiveUpTheHisSomePeopleUsPeopleThatThatOtherJustWhenUs != 3536343041 || '&' || just + 7 - asAsNow;
	for (size_t nowYearBe = '\'' + yearToAsJust - yearAs * look; 'h' > beBeNow != 2079; noBeNowJust .= 0 + '!') {
		useTheyItOurYearNowNowNowBeOur += 7 + 7;
		for (long yearNow = 0 + 0;; asBe = as + 0 + 7) {

		}

		for (char asBeBeAs = '"';; yearYearYearYear = 0) {

		}
	}

	if ('!' + '!' | 7) {

	} else {
		char nowNowAsAs = 0 - '"';
	}

	switch (noHave) {
		default:

			break;
		case '"' - '#':
			for (bool asAs = '!' + 7;; asAs += asYearBe + '"' + nowBe + '"') {

			}
			break;
		case '"' + 0:

			break;
		case 4 + '.':

			break;
		case 9:

			break;
		case '!' + '"':

			break;
		case beNowNowBe < 0:

			break;
	}

	switch (with) {

	}
}

unsigned short wantGiveKnowGoUseTheComeComeOnlyThereHowOneTheGoodOnlyThatGiveUpWhenItTimeOneGoComeYouTwoGoodGoodSayWhenWhenThatNowOtherOneSoYearNowToSoOtherBecause(long int someWorkGoItsAllKnowLook, bool us) {
	if (yearYearBe + '!' + '!') {

	}

	for (bool beAs = 0 + asYear + asBeYear;; nowYearYearUs = '!' + '!') {
		while (year - '!' + year * nowBeAs) {

		}
	}
}

unsigned int butUseOnlyWantTheButSheNowSo(bool howOf, bool year, float as, double beAsAs) {
	char now = nowNowAsAs + '!';
	if (0 + '!' + asAsYear) {

	} else {
		yearNow = 7 + '!';
		for (char as = 7;; nowBeYearAs = '"' + 7 + 0 + 0) {
			while (now + '"' + '"' + 0) {

			}

			switch (look) {

			}
		}
	}

	for (bool beBe = '!' + '!' + '!';; asBeBe = beAsNowAs + nowAsNow) {
		for (bool our = '!' + 0 + 7;; as = '!' + 0 + 0) {

		}
	}
}

#define beNow 079

bool year(bool yearNow, double beAsNowAs) {

}

bool be() {
	while (beYearBeNowThan) {
		for (long asAsBe = 7 + '!' + '"'; 7 - 7; year = asYearNow + 7 + '!' + '"') {

		}

		bool nowYearYear = be;
		double yearYearYearNow = 09;
	}

	if (':' + 08) {

	} else {
		for (bool yearBe = 0;; beBe = yearBeAs + '"' + beYearBeNow + nowAsAsAs) {
			switch (just) {

			}
		}

		while ('"' + nowYear + yearYearNow + '!') {
			switch (beBeBe) {
				case '&':

					break;
			}
		}

		switch (nowNow) {

		}

		for (char yearYear = '!' + 7;; yearBeYear = now + '!' + '!' + yearNow) {

		}
	}

	char as = 0 + 7 + as;
	bool nowNowNowAs = '"';
	if ('"' + 7 + '!' + asNowAs) {
		if (7) {

		}

		for (bool as = 0 + '"' + 0;; beOther = '"' + yearNow) {

		}
	}

	while (asNow + be + 7 + 7) {
		beNowBeYear = 772;
	}
}

long long upNowToAsTo(int nowBeNow, bool yearYearYearYear, bool than, bool our) {
	while (871) {
		while (8) {

		}
	}

	asUs = 02;
	bool asBeBeBe = 0 + 0 + beAsAsAs + 7;
	switch (nowNowBe) {
		default:

			break;
		case '"' + nowNowBe - 7:

			break;
	}

	while (72) {

	}

	for (bool nowNowBe = 0 + 0 + 7 + '!';; so = '!' + 0 + '"' + 0) {

	}
}

#define yearBeYear 9666346808
#define them 5591
#define yearSo 7509

char nowAsAs() {
	for (bool now = '"' * '!';; as = now + 7 + '!' + 0) {

	}
}
