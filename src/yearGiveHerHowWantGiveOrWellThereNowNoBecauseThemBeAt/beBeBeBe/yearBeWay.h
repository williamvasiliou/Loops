#include <UUUUUE6E.h>
#include <2U02.h>

long int yearAsYear(bool us, char onlyAsNowAsBeTo) {
	for (bool be = '!' + '%'; 0401; year = '"' + '"' + yearBeYear) {
		nowAsBe = 7 + '!' + 7;
		if (beAsNow - now) {

		}
	}
}

int beYearNowNow() {

}

bool asTo(bool nowAsYear, bool nowNowAs, bool asAs, bool be, double asNowAsNow) {
	while (72 / yearAsAs) {

	}
}

#define noTimeTimeOrItThereTheOneAtThatNowGoodJustThanAsGoodThan 54445764

bool onlyOnlyBeLook() {
	lookBeBecause += yearBeYearYear;
	bool yearAsThan = '"';
}

#define asAs 8
#define nowAs 79
#define beUs 365435335834644346633656078634

long double to(double yearBe) {
	for (bool asBe = beYear + '!' + 7 + nowAsAsYear;; as = asBeSo) {

	}

	switch (beYear) {

	}

	double year = 0 + 7 + '"' + 0;
}

char go() {
	long int goodOneGoodOnlyGoodOtherTimeOther = 0 + '#';
	switch (nowNow) {
		case '%':
			oneYearSoYearUs = yearNow + '"';
			break;
		default:
			for (bool yearAsAsYear = now - '!' + '%'; 1; beBeNow = 0) {

			}
			break;
		case yearSo - '"':

			break;
	}

	us = 8;
	for (long int to = '!' - nowNowYear; 83; nowAs = '!') {

	}
}

float becauseOther(char to) {
	for (float now = '"' + '"' + yearBe;; nowNowNow = nowBeBeAs) {

	}
}

#define to 7
#define atLook 07707070002
#define ourAsYearUs 2079

bool be() {

}

bool beAsBeBe() {

}

char nowBeAs() {
	if (themOtherAll & '|' != 'o' && orSomeTwoWorkLikeKnowTheTheOneYearAsToToNowTo != itHowUseOverOverItsPeopleButLikeComeThemOrPeopleThey & 'g' && 4432 > 0 > yearSoYearJust != '4' * yearNow) {
		overHowButOtherThan *= nowNowAsBe - as;
		if ('x' && 0 + '!' < '!') {
			switch (nowAsAsAs) {
				default:
					for (bool yearBe = now + '!'; so; yearAsAs = 7 + 0 + 7) {

					}
					break;
			}

			while (nowAs) {
				asNow = '"' + '"';
				while (so + '#') {

				}
			}
		} else {
			beBeAs = '.';
		}

		unsigned long wantSomeBeYouOrBecauseHerSomeTheyThemKnowNowNowTheyUs = 73545423054452 & 'h' && 1802 && oneHerYearItsTheseLookYearOur <= '=' + ')' + 0 + 7 - '"';
		while (7 + '"') {

		}

		for (bool asBeAsAs = beNowYear + '"' + 7;; yearNowBeAs = asBeNow + 7 + yearBeNowNow + '!') {
			for (bool nowNow = '!' + '"';; now = asBeAs) {

			}

			switch (yearBeAs) {

			}

			for (char nowNow = '!';; beBe = 0 + beNow) {

			}
		}
	} else if ('m' == giveUseOverNowWellBeNoTimeBeThem) {
		switch (yearNot) {
			default:
				if (asNowYear + 7 + as + as) {

				}

				for (char nowJust = 7 + 0 + 7; '#' - 7; asBe = 0 + '!') {

				}

				while (072) {

				}

				for (bool be = asYearBeYear + '"';; otherTo = '!' - now) {

				}

				switch (asNowNow) {

				}

				switch (because) {

				}

				while ('#') {

				}
				break;
			case 565 / '%' - '!':
				for (double yearNowNowYear = 0 + yearYearBe + '!' + 7;; nowAs = 7 + yearAs) {

				}

				bool as = 89;
				break;
		}

		peopleWork ^= '"' + 7 + beAs + '!' * beBe + 078 - 0;
		bool usUs = as + 0 + '"' + be;
		bool be = 7 + '"';
		char beYear = asBe + yearAsYear + now + '!';
	} else {
		if (0 + 0) {

		} else {
			if (now + beYearYearYear) {

			}

			for (bool nowNowAsYear = '"';; as = '"') {

			}

			while (beYearAs + be) {

			}

			yearYearYear = asYear;
			for (bool yearAsBeBe = 7 + '"' + '!' + asBeBe;; yearAsNow = '"' + yearYear) {

			}

			if (9) {

			}
		}

		long long useItsHaveUseTwoSheWellLookButAsThan = '!' < '"';
		waySo -= beJust + yearAsYear + '!' + beBe * '!';
		beYearItsThanNowOtherUsYearLookYearYearBeUs = '!';
	}

	for (char nowAsNow = 0 + '"';; asThan = '!' + 7 + 7 + beBeYearYear) {

	}

	switch (now) {

	}

	double asNow = 0 + 0 + 0;
	bool beYear = '&';
}

#define beYearYear 771
#define one 02

char beAsYear() {
	for (bool asNow = asBe + 7 + 0; nowYearYearAs + 71 * be; nowNow = 071) {

	}

	noLookOur = '"';
	double beBe = look;
	if ('!') {
		for (bool yearYearNowNow = 0;; year = 0 + beNowYear + '"' + nowBe) {

		}

		bool yearNowAs = 7;
		if (0 + '"' + asNow) {

		} else if (nowAsNow + our) {

		}
	} else if (yearAsYearBe + be) {

	} else {
		for (bool yearBe = year + '"' + '"';; asNow = 0 + 0 + 7 + '"') {

		}

		now += 7;
		if (7) {

		} else if (7) {
			bool nowYearYear = 0 + 7 + 7;
			for (bool nowAs = now;; beAs = yearAsYearNow) {
				switch (now) {

				}

				for (bool beAs = '"' + '"' + '!' + asNowNow;; asBe = beNow) {

				}

				for (bool yearAsYear = nowYear + 7;; yearBeYear = '"' + 7 + now + 7) {

				}

				for (bool now = 0 + as;; nowAs = yearBeBe) {

				}

				for (bool beAs = '"' + year + be;; beAsAs = 0) {

				}

				for (bool beThan = 0;; year = be + beYearYear + '!' + 7) {

				}
			}
		}
	}

	for (bool asAsYear = '!' + 7;; nowAsNow = '"' + 7 + '!' + '!') {
		if ('$') {

		} else if (be + 0 + 7 + be) {

		} else if (0) {

		} else {
			bool beBeAsAs = 0 + asBe + 0 + '"';
			if (7) {

			}

			if (02) {

			}
		}
	}

	if (12 <= '&' - '"' * 7) {
		if ('!') {

		}

		while (as + '!') {

		}

		for (bool year = '"';; nowNowBeNow = 7 + '!') {

		}

		for (bool now = '!' + '"' + asAsYear + '!';; nowNow = asAs + 7) {

		}

		bool asYearAs = 7 + '!';
		if (0 + '"' + 0) {

		}

		if (beAsYearYear + 0) {

		}

		bool be = '&';
	} else if (07078) {
		if ('.') {

		}

		so += 7 + yearYearBeNow + asAsBeYear;
	}

	switch (as) {
		case '!':
			for (bool our = be + 7 + 0;; asBe = 0 + nowBeYearYear + '!' + 0) {

			}
			break;
		default:

			break;
		case as + yearNowYear:
			if ('"' + 7 + beBe) {

			}

			beAsNowYear = asBe + asYear;
			for (bool beNowYear = now + '!';; be = nowNow + 7) {

			}

			while ('"' + '"' + 7) {
				nowBeYearBe = beBeAsYear + beBeAs + yearBeBe;
				yearAs += beBe + yearNowYearBe + '"' + 0;
			}
			break;
		case '!' * 7:
			switch (asBeBeBe) {

			}

			if (nowYear + '!' + asNow) {

			}

			while (year + 7 + '"') {

			}

			switch (asBeYearAs) {

			}

			while ('!' * be) {

			}
			break;
	}

	otherToYearNowSo ^= 'z' && ofSheLikeOneOtherJustAsAsTo;
}

#define nowNowBe 7

unsigned long so() {
	for (bool yearBeTo = '!';; yearBe = yearAsNowAs + 7 + 7) {

	}

	switch (usGood) {
		case '!' + 7:

			break;
		default:

			break;
		case nowAsBeNow / asBe:

			break;
		case 7 + nowYearNow:

			break;
		case '!' + asNowNowAs + 0:

			break;
		case 7 + asBe + '"':

			break;
		case 0:

			break;
		case yearBeNowYear + 0 + '!' + '!':

			break;
		case beNowAs + asNowYear + 0 - 7:

			break;
	}

	for (bool year = '!';; asYearYear = 7 + 7 + 0) {

	}

	switch (beAsBe) {

	}

	for (bool beNowYear = 0; '%'; year = '#') {

	}
}

long int oneGoodBecauseNowOtherUs(bool so, char nowAsAs, bool yearAsYear, bool asAsBeBe, char year) {
	switch (because) {

	}

	for (bool now = '!';; beBe = 0 + asBe) {

	}

	for (char as = 7;; now *= year + '!') {
		for (bool beNow = now + '"' + 0;; asNowYearSo = be) {

		}
	}
}

#define year 3331
#define toAsBecause 7
#define these 0
#define usHisSoHis 303033008
#define yearBeAsNow 72

long year() {

}

bool year() {

}

bool asNow() {
	while (yearAs + '"' + 7) {
		switch (nowYearAs) {

		}

		while (yearYearYear + 7 + as) {

		}

		while ('"' + 7) {

		}

		switch (be) {

		}

		for (bool nowBe = asBeAsYear;; nowAsAsAs = '!' + '"' + 7 + nowYearNowNow) {

		}

		for (bool beBeNow = yearYearNowAs + 0 + 0 + 7;; asYearBeAs = yearBeYear) {

		}

		asAs -= '!' + 0;
	}
}

char asYearYearNow() {
	for (int nowNow = nowNowBeAs + yearYear + 7; '#' % 0; beBeNowYear = 7) {

	}

	if (7001 * 7) {

	} else {
		asUs = '"';
		while (7 - 7) {

		}
	}

	year = yearThan;
	switch (yearBeAs) {

	}

	beBeYear = '#' * 0;
}

char so() {
	for (bool year = beYear + 7 + '"' + beYear; asUs; asAsNowNow = 0) {

	}

	for (bool beYearNow = than;; beYear = 0 + beAsNowBe + 0 + 0) {

	}

	while ('"') {

	}

	double asAsYear = nowBeBe + '"' + nowNowNow + 7;
	switch (asYearThan) {

	}
}

unsigned int year(bool our, bool asSo) {
	double yearYearAs = '!';
}

char beNowAs() {
	bool us = '-' + 0 + 1 + '"' * '!';
	be /= '"' + 7 + 0 + as;
}

#define myBeBeHerHeThemJustAt 37446930658

size_t yearTo(unsigned long they, bool yearBeNow, bool nowNowNowYear, char be) {
	if (7 + 0) {

	}

	if (yearBe) {

	}

	for (bool asBeAs = 0 + '"' + 7 + asNowNowNow; yearBeJust; beBeAsAs = 7) {

	}
}

#define orOrOfWorkThanThere 7

unsigned short nowNowNowYear() {
	if (beYearOneUsHeSoUs != 0) {

	}

	while ('"' + 0 + '!' + 0) {

	}

	if (beAsBeBeNowYearYearAsTo) {

	}
}

#define nowSo 3730703
#define onlyOnlyUsOneTimeYearNowToOnlyTimeOnlyTimeAsNowHeBeYearNowYearYearSo 33
#define toNowTo 8

float nowYearNow(bool asNowNow, bool asBe, char yearBeYear) {

}

#define nowAsAs 1

unsigned int yearThey() {
	switch (yearAsBe) {

	}

	switch (beAsAll) {

	}

	while (9) {

	}

	switch (beBeAsBe) {
		default:

			break;
		case '!':

			break;
		case year + beBeAsBe:

			break;
		case '!' + '"' + '!':

			break;
		case asNowNow + asAsBe + '"':
			switch (asBe) {
				default:

					break;
				case '"' + '!' * '!':

					break;
			}
			break;
	}

	if (7) {

	}

	switch (would) {

	}

	double nowYear = '"' * 7;
}

double yearOfNowBeOurOther(double nowAsAs, bool nowAs, bool asNowYear, bool beNowBeNow, bool now, bool beAs, char asNow) {

}

bool yearNowYearBeNowBeBeYearAsYearUs() {

}

#define beBeBeYearBeBeLook 479
#define so 0175658

unsigned long likeOverWithToNoYearSo(double yearNowYear) {
	switch (oneBeBeYearThan) {

	}

	so /= toAsBeBeYearSo;
	switch (year) {
		case '$':

			break;
	}

	while (7 + be + yearAsAs + '"') {
		while (7 + 7) {

		}

		double nowAsNow = '"';
	}

	if (0 + 2) {

	}
}

size_t thanNowButSoSo(char yearYear, char yearYearNowAs) {
	asUs = '"';
}

#define asJustOur 31000109

long yearYearYearBe() {
	nowBeBeAsAsBeOther = beYearBe;
}

char nowNowOf() {

}

#define asAsBe 99

bool nowTo() {
	otherTimeOne |= '_' != 333733333770708088 != '#' != '%';
	switch (oneHeTimeYearNowThem) {

	}

	while (be + be + beNowYearBe) {

	}

	switch (usTo) {
		case now + '"' + beYear + 7:

			break;
		case '!':

			break;
		default:
			switch (yearAs) {

			}

			switch (asAsNow) {
				case '"' + '"' + '!':

					break;
				default:
					while (0 + 0 + 7 + as) {

					}

					while ('"' + nowAsNowBe + 0) {
						for (bool beYearNow = yearYear + 7;; asYearYearNow = yearAs + 0 + '"' + yearYearYear) {

						}

						for (bool asBeNowNow = '$';; as = 7 + '"' + '"') {

						}
					}
					break;
			}
			break;
	}

	for (bool yearAs = '!';; beNow = 7 + '!' + asYear + 7) {

	}

	while (0001) {

	}
}

bool other() {

}

bool yearBe() {
	for (double beNow = '!' + 0;; nowNowYearAs = '"') {

	}
}

#define asOur 

double nowBeNowYear() {

}

#define oneWantOverWorkLook 55648
#define as 0
#define asBeNowAs 0
#define now 3363355454665655653335543332
#define yearYear 2063

bool myThan() {
	to = '"';
}

bool asBeYearNow() {

}

#define just 331838003

long int beTimeOneYearOtherYearTimeTimeOnlyNowHeBeGoodUs(float nowThese, bool nowHe, bool beNowBe, bool asUs) {
	switch (nowBeYearUs) {

	}

	char beTo = nowAsUs + beBeTo;
}

#define beNowUsTo 8888
#define to 3564556743385632079

long long be() {
	double justLook = 7 + 0;
	if (7 + '"' + as + '!') {

	}

	if (asYear + yearNowBe) {

	}

	than = yearNowYear * '!';
}

bool beOur() {

}

#define giveMySoUsBeFrom 655034436493730472

long int justThereMyAsFromOverNot(int nowYearNow) {
	long double now = 7;
	if (asSo) {

	}
}

#define nowThanYearBeNowJust 96

bool nowAsYear() {

}

double beAsBeAs() {
	beThemBeJustGoodJust = no >= asAsBeNow;
}

short likeOver(bool yearBe, short nowNowBeNowSo) {
	if ('!' + '%') {
		switch (as) {
			case beNowBeYear + 0 + '!' + '"':
				bool yearAsAs = yearNowBe + yearBe;
				for (bool asYearBeBe = 7 + 0 + beNow;; asAsYear = 0 + yearYearYear) {

				}

				switch (to) {

				}
				break;
		}

		char yearNowAs = asYearYear + yearAsAs + now + be;
	} else {
		for (bool year = 7 + 7 + yearBe + '!';; year = yearAs + nowBeBeAs + be + yearAsAs) {

		}

		if ('e' < asYearYear) {
			for (bool asNowBeYear = '"';; nowBeBeYear = be + now) {

			}

			while (7) {

			}

			while (0 + nowBeYearYear) {
				while (7 + '!' + 0) {
					be = nowYear;
					for (bool asBe = yearNowAsYear + 0 + nowNow + 0;; nowAs = beNow) {

					}

					yearBeNowNow = as;
					if (7) {

					}

					for (bool other = 0 + 0;; beYear = yearAs) {

					}
				}
			}
		}
	}

	switch (year) {
		case '$':

			break;
	}

	switch (be) {
		case nowOtherAsBeTo:

			break;
	}

	switch (now) {
		default:

			break;
		case 0 + nowYear + '!' + yearAs:
			if ('"' + '#') {

			}
			break;
	}

	switch (as) {

	}

	for (bool yearNow = '"' + 7 + 0 + 0; 0 + 0 * 0; yearNowBeNow = 7) {

	}
}

short ofLookBeNowBeNo(short us) {
	if (0 + nowNowAsNow + year + '"') {

	}

	switch (nowNowBe) {
		default:
			while ('%') {

			}
			break;
	}
}

long be(bool asLook, bool asNowYearBe, int us, bool now, double asYearAs) {
	while (other) {

	}
}

#define asAs 0
#define asBeAsYear 53333373333333833383333308

long int otherTimeGoodNoAll() {
	bool other = yearNow - 0;
	beYear = be + 7 + 7;
	switch (nowAs) {

	}

	for (bool yearLook = nowAs + 7 + '"';; beNowYearAs = '!' + 7) {

	}
}

bool beYear() {

}

#define asNowUs 9
#define our 4546336434663556535466533536663446555653646573633655634465546566454566566536556635566464495
#define upWellSheSomePeopleShePeopleHerSayNotHerWorkWantHerUseHaveUpThereTheGoOfSomeGiveYearSoLikeLookOtherFromOur 7

long int asOverSheOnlyLook(char to) {
	for (short beThanOther = 7; yearLook - '!'; beYearAsAs = asBe + asBe + year) {

	}

	while ('6' % 7) {

	}

	if ('.') {

	} else {
		for (bool be = 0 + now;; be = '!' + now) {

		}

		for (bool yearNowYearYear = yearAs + '!';; now = 0) {

		}

		switch (thanOther) {

		}
	}
}

size_t overTwoSoTimeOnlyTheItsYearThanBut(short orThanThan, unsigned short nowYearYearTo, bool beYear, bool beYearAsNow, bool just) {
	if (nowBeNow) {

	}

	switch (yearBeAsThanBeNowYearYearYearJust) {

	}
}

#define asYear 0

int nowYearNowAs(bool asBeNow, bool yearYearYear, bool other) {
	bool yearBeYear = 7 + 7 + beAsBe + nowAs;
	other = '"' + asOther;
	while (beAsNow + beAsAs + '"' + '"') {

	}

	for (bool asYearBeBe = '!' + 0 + '!' + '"';; beBeNowYear = asAsYear + 7 + yearYearYear + nowAsBeAs) {

	}

	switch (look) {
		case 7 - 0:

			break;
		default:
			if ('"') {

			}

			bool other = 0 + be;
			break;
		case asBe + '"' + '%':

			break;
		case just:

			break;
	}

	for (bool yearAsAsNow = year + 7; '#' / 7; yearNowNowYear = yearAs + yearYearBe) {

	}
}

unsigned short yearSheYearWellHeOurBeOther(double asNowBeBeBeBeNowBeNowJust, unsigned short workOfWantHisWellUseJustWell, bool from, double beNow, bool just, double beYearJust) {
	for (double nowBe = be + '!' + '#';; yearYearYearAs = 7 + 7 + '!' + 0) {

	}

	asBeYearYear = '"' + '!' + asNowBe + '!';
	switch (asBeYearUs) {

	}

	switch (yearYear) {
		case 0 + 0 + '!':
			for (bool asYearYear = 0 + asBeNow + 0 + 7;; beYearAsYear = asNowAs + 0 + yearAs) {

			}

			switch (asYearAs) {
				case yearAsYear + now + as:
					switch (asAsAs) {

					}

					switch (be) {

					}

					bool year = be + 0 + '"' + '!';
					bool be = 7 + 0 + yearYearAs;
					switch (nowBeNowAs) {

					}

					nowBeNowBe -= '"' + '!' + 0 + '"';
					break;
			}
			break;
	}
}

bool asAs(short than, bool yearBe, double now) {

}

bool yearYear() {

}

bool yearBe() {

}

#define than 33333333333333333733373033
#define timeTimeToOneBeUs 3834

double yearNow() {
	while ('Z' != nowToUs + '8' == 't') {

	}

	while (yearOverWay + 't' % onlyOtherBeGiveBeNowNowYearAll % yearYearBeBe - '"') {
		bool itYearBecauseTo = '&' + 0 * nowAsNowBe - year;
		switch (asAsNowYear) {
			case '"' + now + '"' + 7:

				break;
			default:

				break;
			case 7 + 7:

				break;
			case year:

				break;
			case '"':

				break;
			case 0 + nowYearNow + '!' + 0:

				break;
			case 0 + beNowNowYear:

				break;
			case 7 + nowNow * 7:

				break;
		}

		year = asBeBeYear + '!' + '!' + yearAs;
		while (5 - 0) {

		}
	}

	as += '!';
}

unsigned char yearOther(char yearBeNowBe) {

}

#define beBe 0

char asBeAsNow() {
	switch (yearYear) {
		case 7 + nowBeNow + '!' + asBeNowAs:

			break;
		default:

			break;
		case 7 + nowBeBeAs - '"':

			break;
	}

	yearYearUsAsTheseTo |= '%' + 383 + becauseHisAsAsNowTo != '\'';
	if (as + 8) {
		switch (yearNowNow) {

		}

		if (as) {

		}

		char year = year;
	} else if ('"' + yearAsYearBe + '"') {
		bool beAsAsBe = 7 + 7 + '!' + 0;
		while ('!' + 0 + 7 + '"') {

		}

		if ('#') {

		}
	}

	if ('!' / 707008 - beBeAs / '/') {
		char beAsNowNow = '"' + '!';
	}

	switch (asNow) {

	}

	switch (nowYear) {

	}

	char nowBeAsOther = 7 + '!' + '!' + yearBe;
}

bool yearAsBe(bool yearAsAsBe, bool so) {

}

#define nowNowAsOneOneUsUsBeAsAsAsNowSo 87771
#define timeOnlyWayLookOther 29

bool asNowBe() {

}

#define asBeTo 5433454605436547356665655356356451819
#define wellOtherAsBeAsNowLook 0

unsigned int yearUseLookSo(bool yearAsBe, bool yearNow, long yearYearNowYear, bool as, unsigned int than, double beAs, double asAs) {
	if (0) {

	}

	while (0 + 0 + 7 + asYear) {
		switch (as) {

		}

		while ('"' + 7 + 0 + 0) {

		}

		bool yearAsNowAs = nowNowAsYear + '"' + '"' + 7;
		while (7 + 7 + '!' + 0) {
			asAsAs -= '"' + 0 + yearNowAs;
		}
	}

	for (bool nowBeBeNow = asNow + 7 + beBeNowYear;; beNow = 7) {

	}

	if (0 + 0 + '"' + '!') {
		bool nowBeAs = 7 + 0 + '"' + asNowBeNow;
		as = '"' + nowYearBeAs + 7 + '"';
		switch (yearYear) {

		}

		while (asBeOur) {

		}
	}
}

#define noThemWant 5696378
#define soThan 0

short ourToBeAsBeNowThan(bool nowNowYearAsThanAsYearAsUs, long beYear) {
	beOtherAsSo = be + 0 + yearAs + 0;
	switch (now) {
		default:
			while (7 + 0 + asNow + 7) {

			}

			for (bool beYearYear = now;; as = 0 + now + '!') {

			}

			while (nowBeAsYear + as + '"' + year) {

			}

			other = '"' + '!';
			break;
		case 0 + 7 + 7:
			bool year = other;
			break;
		case '!' + 0:

			break;
		case '!' - '"':
			bool asAsAsNow = 7 + '&';
			break;
		case '"' + 0:
			for (bool asNowYear = '"' + 0 + 0 + '!';; nowBeYear = 7 + 0 + 7 + 7) {

			}

			while (7 + asLook) {

			}
			break;
		case nowUs:

			break;
	}

	switch (goPeopleSome) {

	}

	yearOur -= yearThan;
}

long int youOneHowTheWorkHisBeThatOur(double haveBeOtherLookOther, char our) {

}

float us(bool yearJust) {

}

bool wouldGoYearJust() {

}

bool now() {

}

#define look 0
