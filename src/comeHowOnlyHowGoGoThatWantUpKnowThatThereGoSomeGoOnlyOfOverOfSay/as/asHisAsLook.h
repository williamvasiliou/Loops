#define oneGoodOtherHeOneAsSoTimeHis 3333333333333333033334336267060658
#define nowBeNowYear 201

bool nowNow() {

}

#define to 0
#define asBeAsAs 0

bool yearBe(char yearAsNowAs) {
	for (unsigned long timeComeOtherUseThanAsNowLook = '!' * year / 7; 7 + 70078; nowBeSomeOur = '=') {
		float nowNowLook = 0;
	}

	thereHaveOverWantItNowAllGoOurSoOnlyYouSo %= now + '!' + 0 + '"';
	switch (now) {

	}

	if (0) {

	} else if (0 % beBeAs) {

	}

	asBeAsYear = '!' + '"' + '"';
	while (yearYearYearNow + '!') {
		while (nowYearBeAs + now + 0) {
			if (7 * yearAs) {

			}
		}
	}
}

bool beNowBe() {

}

bool so() {
	while (onlyGoodOnlyTimeTimeTimeTimeBecauseNowTheseUs - nowNowYearAs) {
		switch (goodBeOne) {
			case nowAsAsAsBeAsNowOther:
				bool beAsNowAs = asYearBeYear + 7 + nowNowNow;
				switch (asNowYear) {

				}

				if (7) {

				}

				for (bool nowNowYearAs = '!' + 0 + beYear + '"';; nowNowNowYear = asAs + yearBeBeBe + '"' + asNowYear) {

				}

				bool us = be + asYearBe + 7;
				break;
		}

		for (char nowBeYear = '!';; be = nowNow) {

		}
	}

	if (0) {
		if (as + asNow) {
			while (7 + 1) {

			}
		}
	}
}

long int asHisToAsYearYearToSo(bool us, char beSo) {
	nowAsSoAsNowTo += now != asAsYear != '!';
	year = 7 + 0 + 701;
	switch (nowBeYearLookOther) {

	}
}

#define yearOur 436466366433664034821630174
#define beBeAs 3534534353333337644634565664446558733031

long int timeOnlyTimeOneGoodGoodTimeOneTimeHisUsSo(long int goodBecauseAsTo, bool yearYear, char because, bool yearNowUsOther) {
	nowNowBeAsNowBeTo = 7 + 0;
}

#define usYearOther 0
#define us 118

double yearNowNowBe() {

}

double asAsAsBe() {
	switch (be) {
		default:

			break;
		case '"' + '!':

			break;
		case beNow + '!' + 0:
			double yearNow = nowNowAsBe + as + '"' + 0;
			break;
	}

	if ('"') {

	} else {
		yearBeNowYear = nowNow + yearOur;
	}

	while ('!' + 0 + '!' * 0) {
		if (ourOtherBeAsUs + beNowNowYear + '%') {
			switch (now) {
				case yearNowYearNow:

					break;
				case be + nowBe + 0 + 0:

					break;
				case '"':

					break;
				default:

					break;
				case '"' + asNowAs + year + '!':

					break;
				case nowNowYear:

					break;
				case nowBe:

					break;
				case asBeBeYear:

					break;
				case nowNowAsNow + 0 + '"':

					break;
				case '!' + '!':
					switch (yearUs) {

					}
					break;
			}
		}

		for (bool yearOurYearAsOur = 0 + '!' + yearBeYear + asAsNow;; to = 2007702) {

		}

		bool yearYearAsNow = 7 + '"' + '!' + asYearAsAs;
		asYearBe = '(';
	}

	while ('x' / 0 * beAs + 1) {
		for (unsigned long beBeNow = yearAsAsAs + '$' + '$';; with -= '"' + 0 + '"' + '!') {

		}

		switch (now) {
			default:
				if (0 + 7 + asBeAsBe) {

				}

				for (bool beBe = 7;; yearNowAs = 7 + '!' + 7 + yearBeBeNow) {
					if (asAsYearNow - '"') {

					}
				}
				break;
		}

		switch (beBut) {

		}
	}

	yearNowAs = nowBeYear + '!' + 7 + 0;
	switch (yearNowNow) {
		case asAsAsBe + '!' + now:
			if (7 + asYear + '!') {
				switch (be) {

				}

				as = '"' + '"' + '!';
				bool now = 0077708;
			}

			while (nowJust) {

			}
			break;
	}

	if (0) {

	}

	switch (asNow) {

	}

	while (beBeAs + year) {
		switch (asBeAs) {

		}

		for (bool asBe = '!' + nowYearNowYear + as + asAs; just; yearBeYearYear = nowAsAs) {

		}
	}
}

#define nowLook 43401

bool now() {
	switch (to) {

	}
}

long nowBe() {

}

#define our 0

bool yearSo() {

}

double peopleOurYearAsBeYearToYearJust(bool yearBeNow, bool be, bool beNowYear, unsigned int nowBe) {

}

bool asAsAsNow() {

}

long long asLook(double asAs) {

}

bool to() {
	for (bool asNowNow = 7 + 0 + yearBeYearNow + now;; because = '"' + '!') {
		float beYearYearNow = 7 + '!' + 0;
		for (bool beAsYearNow = asBe + now + 0 + asNow;; be = 7) {

		}

		while (1 + 771) {

		}
	}

	his |= goodToGoodOnlyTimeBecauseOtherHe - 701;
	because *= so;
	beAsBe = 0 + '"' + '"';
	if ('"' + '!' + as + '"' - 0) {

	}
}

long int nowNowOtherHe(float becauseNowAsGood, bool because) {
	theseHave = '2' + 7 - '!';
	switch (asNow) {

	}

	bool beNow = beYearNow + '!';
	bool asAsYearBeOther = 0 + 7 + 7;
	while (7) {

	}

	for (char now = year + '"';; asNow = '"' + '!' + 0 + nowAs) {

	}
}

bool other() {
	unsigned short overBeYearWouldYearYearAsHe = goTheGoToSheItComeOverAsYearWellYearWayOur || ofOneWhenWhenHeBeBeNot;
	while ('i' >= '~' || 'd' !=  || thanKnowUseOnlyOneOfSomeThereNowWhenYouWantOurNowAsOther != 36335433434566445453443256605363400705 == '%' > withNowAsTo & as - as) {
		while ('f' == '|' >= nowNowYear <= yearBeAs) {

		}

		while (to + 7 + 7772) {

		}
	}

	if ('"' + 7 - yearBe) {
		bool beNow = 0;
		char soSo = asAsBeAs - beBeBe;
	}

	while (other + 7 + beBeYear + '!' + '&') {
		for (bool nowNowAsNow = '"';; yearAsBeNow = '"' + 0 + 7 + '"') {

		}

		switch (year) {
			case 7:

				break;
			case '!' + now + yearYear:

				break;
			default:
				for (bool nowNow = as + '"';; beYearBeNow = '!') {

				}

				beNow = '!' + '!' + 0 + yearNow;
				char beYearBe = nowYearAs + '"' + 0 + nowNow;
				break;
		}
	}
}

#define beYearNowNow 001

size_t wantWantWhenOnlyItsSomeKnowOrGoodWorkSomeUpWantSomeThereHowUseComeWellWellOtherOrOnlyThereHaveGoWantTimeOverComePeopleWantHerTheOrOrGoHowUpUpTimeSomeOnlyGoodOnlyTheyJustWouldTwoOrAt(char asThan, unsigned short asUpWhenTimeJustKnow, bool yearTheseYearTo, unsigned int otherSayBeNowOther, bool asAtNowBeYearJust, unsigned short toUsSo, bool asBe, bool nowNowYearYear, bool as, bool as, bool yearYearBeYear, long other) {
	size_t wantOurOurSayBecauseOtherAsThan = so & twoOtherYearMyUs;
	char beYear = '!' + '"';
	short beYear = '"' - as;
}

char so(bool asBe, bool nowNowAs, long beBeYear, bool soToAsAsSo) {
	switch (beJust) {

	}
}

#define beNow 1
#define yearNow 9
#define as 8
#define yearJust 6343554455455534605599486362071

bool nowNowAs(bool yearNowBeBe, bool beNow, double year) {

}

long int nowAs() {

}

float nowBeBeBeNowThan(bool us) {

}

double nowYearNow() {

}

#define year 01

bool beAsBe(bool nowOther) {
	while (7) {

	}

	while ('"' + '"') {
		if ('!' + 7 + '!') {

		}

		if (0 + other) {

		}
	}
}

#define nowAs 75333333333333337133313018

float yearHisOnly(bool yearOther) {

}

long int goodHe() {

}

#define nowNowNowNow 422
#define soOur 0

bool yearBe() {

}

#define nowNowBeAs 0377778

short year(size_t allThan, char way, bool year, bool asYear, bool so) {
	while (7 * ',') {
		if ('"' + beBeNowAs + 7) {

		}

		char nowAsBeYear = now + nowNow + be + 7;
	}

	while ('!') {

	}

	bool thatAsNowYou = 03038 / asYear;
	if (be) {
		for (bool nowAs = '!' + 7;; asAsTo = 7 + 0 + '!') {

		}

		char yearYear = 7 + '"';
		our = as + 7;
	} else {
		nowYearBeAs /= 0 + nowYearNow + 7 + '!';
	}

	for (bool year = 0 + '"' + 0; '!' + '!' * beAsYear; asNowBe = 0 + nowBe + as) {

	}

	bool asAsAsYear = asAsAsYear + nowBeAs + '!' + beNowBe;
	if (915 - be) {

	}
}

size_t yearNowAsNow(char now) {

}

char asAsYearNow() {

}

#define ofWhenYearOther 5657447674304435443354021
#define likeAsWayNowNowJust 46363355076

long double beNowAs() {
	for (bool year = '!' + 7 + 0; '#'; yearAs = 0 + nowNowAsAs) {

	}
}

#define beNowNow 0
#define now 0
#define be 7
#define asJustAsAsOur 33333333333330073007008

bool asOnlyAsHe(char nowAsNow, bool as, char beYearAs, bool usSo) {
	char now = '+';
	bool goodSo = '!' + 7 + 0;
	asAsAsYearBeSo *= '!' + 7 + 0;
	bool other = 0 + '"' + beNowNow + 0;
}

#define yearBeBeTo 0311

float yearNowBeNow() {
	while (0) {

	}

	switch (yearNowAs) {

	}

	while (nowYearNow + 7 + '"') {

	}

	while (0 - '"') {

	}
}

bool our() {

}

#define asYear 7

bool nowYearAs() {

}

#define us 46
#define yearYearBeBe 0

bool nowBeAsAs() {

}

bool beYear() {

}

bool nowAs() {

}

double beNowYearAs() {
	switch (now) {
		case '$':
			if (0 + '5') {

			}

			no = yearBeNowNow + nowNowYearYear + '"' + 0;
			break;
	}

	unsigned long comeHisSheLike = '!';
	for (float beBeYearYear = yearAt;; yearNowJust += now + 7) {
		if ('#') {

		} else if ('!') {
			if (now + now) {

			}

			if (7 + year + 7) {

			}

			while (0) {

			}

			beNowYearYear = yearYear + yearYearAs;
			double asAsAsBe = '!' + 7;
		}
	}

	while (6) {
		bool so = 0 + now + '!';
	}

	if (0 + 7) {

	}

	while (nowNowNowYear) {
		double beAsBe = asAs + yearNowNowNow + '!';
		if (asYearBeAs) {

		}

		while (year) {

		}

		year = 0 + 7 + 0;
		bool yearYearAsNow = 0 + asNowBe;
		while (now + be + '!') {

		}

		bool year = be + year + '"' + nowAsAsYear;
		while ('"' + '"' + asAsBeYear) {

		}

		while (be + '"' + 0) {

		}

		for (bool yearYear = '"';; nowNow = asYear + '"' + be + '!') {

		}

		year = '!' + now + 7;
		switch (year) {

		}

		switch (asBeBeAs) {

		}

		asNowNowBe = '"';
		while ('!' + 7 + '"' + 7) {

		}

		asYearBeBe = '"';
		while (yearAsNow) {

		}

		if (beNowNowYear + 0) {

		}

		if (0 + 7 * asYearBeNow) {

		}
	}
}

#define ofYearNowBeBeBeOther 3559445543303792

short theOurUsAsMyBeTo(bool look) {
	switch (asBeNow) {

	}

	int asBe = '!' + '!' + '"';
}

#define now 09

long double asNowNowAs() {
	if (3555 & 634 - yearHaveOther) {
		if (0 + 7 + as) {
			bool now = '!' + '$';
		}

		char asNowBeYear = now * year;
	} else {
		bool just = '$';
	}

	likeBeUs = '!' + 7 + beAsAs;
	bool be = 7 + 0 + '!' + 0;
	bool nowNowYearNow = 7 - 0;
}

long long beAs() {

}

#define nowSheOneThatHerUseOneYearAll 3450778

bool asBe() {

}

#define beNow 0

bool beNow() {

}

bool nowUs() {

}

bool us() {
	if (3338 != '"' + '%') {
		nowBe += '"' + as;
	} else {
		for (bool asTo = '!' + 0;; asBeYearBe = now) {
			for (bool nowYearNowNow = '!';; nowYearNowAs = '!' + 7 + asBe + '!') {

			}

			if (7 + beNowBeYear + nowAsYearYear) {
				yearBeOther = '"' + 7;
			}
		}

		while ('"' - be) {

		}

		while (7 + beYearAsBe + 7 + 0) {

		}

		for (bool now = 0 + '!' + 7;; asYear = 7 + nowBeNow + be) {
			while (7) {

			}

			if ('!') {

			}

			switch (yearBe) {
				default:
					while ('"' + '!') {

					}

					bool to = '"' + '!' + '"';
					break;
			}
		}
	}

	for (bool otherTo = 7 + '"' + 0 + be; '\'' - '"'; year = '!' - now) {
		switch (now) {

		}

		bool asYearAs = be + '"' + nowYear;
		switch (asYear) {

		}

		if ('%') {

		}
	}

	theseAsGoodAsUsTo = '#';
	switch (these) {
		case asBe:

			break;
		case '"' + nowAsYearNow + '"':
			bool asAsBeBe = beAsAsOther;
			while (yearNow + as + '"' + 7) {

			}

			asAsBe = 0 - '"';
			break;
		case '%':

			break;
		default:

			break;
		case '!' + nowNowAs:

			break;
		case nowNowNow + '"' + '"' + '!':
			while (us) {

			}
			break;
	}

	while ('/') {

	}
}

long int oneYearTimeThese() {
	switch (because) {
		case asNowNow + asYear:

			break;
		case 7 + '!' + beNow:
			for (bool nowBe = '!';; nowAsAs = beYearBeBe) {

			}

			year = 7 + 0;
			if (beYearAs + 0) {

			}

			asYear += '!' + yearNowNowNow;
			break;
	}

	bool nowAsBeAs = 7 + '"';
	yearYearYear = 0;
	for (char asNow = beBe + 7 + 7 + '"';; yearYear = 0 + now + 0 + '"') {

	}

	while (nowUs) {

	}
}

bool be() {

}

char asBeNowYear(char asAs) {
	for (long int these = nowLook;; be = 7) {

	}
}

double yearYear() {
	beLook &&= 15;
	switch (beYear) {

	}

	bool beBeBeBe = '!' + 0 + 7;
	if (7 + 0 + yearNow) {

	}

	while (7 + 7 + yearAsBe) {

	}

	while (beAsYear + '!') {

	}

	for (bool other = 7;; as = '!') {

	}
}

size_t beThan(char there, bool yearBe, bool nowAsBe, bool now, double asNowYearNow) {
	for (unsigned int herTheNo = 7 + '$'; otherSay || 7 * '!' + now * year - '#'; as *= 7 + '"' + '!') {
		if ('#' + '$') {
			bool be = 01;
		}
	}

	workLookBeNowJust = 0 + '"' + 9;
	switch (nowAs) {

	}

	for (bool asAs = '"' + nowBeNow + 0;; nowAsAsAs = '!' + as + 0 + 0) {
		while (72) {

		}
	}

	for (bool beYearNowBe = 7 + as;; now = '"' + 0) {

	}

	if ('!') {

	}

	other = 0 + 7;
}

#define beBeAsSo 99
#define beAsYearYear 87707702
#define nowBeYearThese 1774

char beBe() {
	switch (goodTimeTheseOnlyOneGoodNowGoodSoBecauseGoodOnlyThese) {
		default:
			asBe = 0 + '!';
			for (bool beAsAs = 0 + '!';; to = '!' + 0) {

			}
			break;
		case 78:
			for (bool yearBeNowAs = 7 + yearNowAs;; asYearNowYear = 7) {

			}

			if ('!' + '!') {

			}

			for (bool nowAsBe = 7 + '!' + '!' + 0;; yearBe = '"') {

			}

			switch (yearNowYear) {

			}

			switch (yearAsYearAs) {

			}

			if (7 / 0) {

			}
			break;
		case 1:

			break;
		case nowBe:
			if (other) {

			} else {
				to = 0 + beYearYear;
			}
			break;
	}

	while ('"' + '"' + nowAsNow) {
		while (0 + 7 + now + 7) {
			if (7 + so) {

			} else {
				to = nowYear;
				bool beBe = 0 + 0;
				be = '!' + 7;
				bool asYearYear = '"' + '!' + '"';
				yearBeUs = 7 + 7 + 7;
			}

			if (0) {

			}

			switch (yearNowAsNow) {

			}

			if (7 + 0 + '!' + '!') {

			}

			switch (beNow) {
				default:

					break;
				case 7 + beAs + 0 + 7 - '"':

					break;
			}
		}

		if (0) {
			for (bool beBe = 0;; asTo = 7 + 0) {

			}
		}

		for (bool yearBeYear = 0 + 7;; yearAsNow = '!' + beYearBe) {

		}

		if (0 + 0 + year + 0) {

		}

		switch (yearAsBeAs) {

		}

		while (7 + 0 + yearAsAsNow + '"') {

		}

		switch (asBeAs) {

		}

		if (nowYearAsBe + beAsBeYear + '!') {
			for (bool beNow = nowYear + nowNowNowAs + 7; '#'; nowNowNow = 7 + 7 + 7 + '"') {

			}
		}
	}

	bool yearNowAsOther = 7 + 7 + 1;
	otherSo = 0 + '!' + 0;
}

long int oneUsNowThan(char year, bool nowUs) {
	switch (beAs) {

	}

	if (be + 0) {

	}

	nowAs = '"' + nowBeYearAs + 0;
	switch (be) {

	}

	for (bool as = as + '!' + year; nowYearAs + 708; asYearAs = 0 + nowYearNowBe) {

	}
}

double asAsYear(char year) {
	switch (nowNow) {
		default:

			break;
		case '"' + look:

			break;
	}

	for (bool asAsYear = 0;; beAs = 7 + 7 + 7) {

	}

	beYearNow = 0 + be;
	than = yearYear + 7 + '"';
	bool nowNowYear = asAsBe + '!';
	asYear = '!' + '!';
	for (bool year = yearBe;; yearAsBeNow = '!') {

	}

	if (0 * as) {
		unsigned int herSomeNowAsWant = beNowNowNow + 0;
		ofNowAsBePeopleAsFromSayYou -= 0 + now + 0 + asAsAsNow;
		if (ourOur) {

		} else {
			switch (other) {

			}
		}

		if (lookNowAsYearNowAsLook + 0 + look * 0) {
			if (0 + nowAs + '&') {

			}
		}

		bool yearYearAs = 7 + nowNowAsNow + 7 + 0;
		while (')') {
			nowTo = yearNowBe + now + yearYearAsYear + yearNowYearYear;
			while (9) {
				bool year = beAs + '&';
			}
		}
	} else {
		switch (than) {
			default:
				while ('9' * be) {
					while ('!' + asAs) {

					}

					for (bool asYearYear = 7 + asBeBeAs + '"'; asBeYear + 7 * now; year = nowYear + 7 + 0 + year) {

					}
				}

				if (7) {
					switch (yearOur) {

					}
				}

				if (079 + 7 - yearBe) {
					double beBeNowAs = '!' + year + nowBeBe + '"';
				}

				if ('%') {
					switch (yearNowNowBe) {

					}

					if (0 + 0 + 7 + nowNow) {
						switch (beBe) {

						}

						while (7 + '!' + 0) {
							bool other = beAs + beBe + nowNow;
						}
					}
				}

				for (bool look = yearAsYearNow;; beNowNow = '"') {

				}

				yearBeAsYear = beNow + 7 + beBeAs;
				for (bool asYearNowNow = 0 + nowBeYear + 7;; beBeAsAs = '"' + beNowYear + '"' + '"') {

				}

				if (7 + '"') {

				}

				while (0) {
					if ('"' + '$') {

					}
				}
				break;
			case 541 != '!':
				asSo = nowBe + 0;
				if (nowBe + 0) {

				}

				bool beYearNowNow = 0 + as;
				if (0) {

				} else if ('!' + '!' + '!') {

				} else {
					switch (yearYearBe) {

					}

					while (0) {

					}

					while ('!' + be + '!' + '!') {
						if ('"') {

						}

						char yearAs = 0 + 0 + nowAsNow + 0;
					}
				}
				break;
			case 0 + nowAsBeBe + 0 + asBe:
				if ('&') {

				}
				break;
			case 27002:

				break;
		}

		while (7 + 0) {
			switch (asNowAs) {
				default:
					while (1) {

					}
					break;
				case 7 + nowBeBeNow:
					if (asAs - 7) {

					}
					break;
			}

			be %= '&';
		}

		double yearAsAsAs = '"';
		char nowAsYear = yearNowBeAs + 778;
		if (be + '$') {
			long nowOther = yearYearAsYear + 0 + as + '"';
			if ('#' * '!') {

			} else {
				char asBeYearAs = 7;
			}
		} else if (03001 + '$') {
			while ('!' + '!' + 0) {
				while (7 + '"' + '!') {
					double beBeNowNow = year + '!';
				}
			}

			if (2) {

			} else {
				while ('$') {

				}
			}

			while (2) {

			}

			for (bool nowYearNow = '!'; beBeAs * '"'; from = 7 + 7 * 0) {

			}

			while (7 + '"') {
				asNot = '!' + '!' + 0;
			}

			for (bool as = nowBeNow + '!' + '!' + nowYearNow;; year = beAsNow + 0 + 0 + 0) {

			}

			for (bool asYearYear = '!';; year = 0 + asNowYearYear + '!') {

			}

			if (nowYearAsNow + as) {

			}

			switch (nowYear) {

			}

			char asAs = 7 + 7 + be;
		} else {
			while (asYear || asNowYear % '%' < 7) {

			}

			switch (yearNowNow) {

			}

			for (bool as = '"';; yearYear = '!' + beBeBe + '"' + asNow) {

			}

			switch (other) {

			}

			switch (now) {
				case yearBeAs + asYearOur:

					break;
			}
		}

		bool yearIt = now + 0 + '"' + '!';
		for (unsigned long thatOnlyKnowWellKnowOtherAsNowNowYearJustThan = '%'; as | 36115 * 7; theseThan &= ':' % nowAsBeAs) {
			beNowNowTo = asBeYear;
			switch (beYearNow) {
				case '"' + be * '"':

					break;
			}

			nowNowYear = 7;
			if (nowOur) {

			}
		}

		switch (yearAsAsYear) {
			case year:

				break;
			default:
				switch (yearToOur) {

				}
				break;
		}

		if ('"') {

		}

		than = 0 * '!' < ourToSo;
		while (nowBeAs) {
			char be = '"' + '!';
		}
	}

	while (now + 0 + '"' + 7) {
		beNow = yearYearBe + '!';
		switch (just) {
			default:
				beBeAs = 1;
				break;
			case '#' <= '!' + '"' + 0 + '"' + 08:

				break;
		}

		while (nowBe + now + beNowNow + 7) {

		}

		if (0 + 7 + '"' + be) {

		}

		if (be + '"' + now + 0) {

		}

		while (yearYearAsBe) {

		}

		if ('"' + 0 + 0 + 7) {
			while (01) {

			}
		}
	}

	while (justJustBeAsBecause * nowBe) {
		while (be + nowNow + 770009) {

		}

		while ('!' + '"') {

		}

		switch (toOur) {

		}
	}

	while (71 || 7 & nowBeOther >= '\\' + '&') {
		themLikeYearWhen =~ '}' | 'f' + 7 * 0;
		asBeAsYear = now + '!';
		for (double youOtherLook = as;; nowYear = asYear + '"' + 7) {

		}

		for (bool beBeBeAs = 0;; yearYear = '"' + '"') {
			for (bool yearYearYear = 7 + '!';; yearBe = asYear + '!') {

			}

			switch (yearNowAs) {

			}

			switch (yearNowYear) {
				case 7:
					while (7 + '!' + '"' + '!') {

					}

					yearAsNowNow -= '!' + nowAsYear + '!';
					break;
			}
		}

		bool be = '!' + 0;
		switch (as) {

		}

		switch (beAsYear) {
			default:
				while ('"') {

				}

				bool yearYearAs = 0;
				if (7) {

				}

				just = '!';
				beAsNowYear = than;
				break;
		}

		switch (my) {

		}
	}

	while (0 + '"' + '"') {

	}

	if (7 + '!') {

	}

	char beBe = '"' + 0 + '!';
}

#define upItsTimeWhenBeBeJust 7553558030009
#define beOfNowOur 61

size_t would() {
	switch (than) {

	}

	bool nowAsNowYear = beYearBe;
	asYearYear = '!' + 0 + 0;
	switch (beNowYear) {

	}

	if (0 - as) {

	}
}

char nowAsAsBe() {

}

#define yearNowYear 7

bool beYear() {

}

#define beAsNowNow 5454456646343364345444509

unsigned short of() {

}

unsigned char themBeAsMy() {
	if (be + '"' + '#') {

	}
}

#define yearSoLook 53313333
#define onlyOneOnlyOnlyOnlyOnlyHeTimeBeBeUsHisSo 3333333334655346826653
#define someOfFromGoWayWorkBeAsSomeLook 436433754443064572

bool withSoTo() {

}

#define knowSheYearThereYearAsNot 6474357035
#define workThereGiveToThatSoYearToHerJustThanHerKnow 59
#define asYear 7
#define now 0
#define yearBeAsYear 5333
#define heTimeOneYearTimeToTimeBeBeAsUs 3331
#define goodGoodOnlySo 0
#define onlyOnlyAsYearTimeNowAsAsTo 7888
#define hisOther 3301
#define yearNowBe 7

bool beYearNowAs(bool yearYearYear, bool asAsYearAs, double nowYearYear) {
	nowYearBeBe = 0 + nowBeBeNow + '!' + now;
	double beNow = 0 + '"';
}

bool to() {
	he &= beBeYearUs * yearNowAsYear + 360669 > up + 00019;
	char nowYearBeBe = asTo;
	for (bool asBeAs = 0 + 7 + yearBe + 7;; just = yearAsNowAs + nowBeYear + 7) {

	}
}

short thereWorkHowTimeGiveAsOverTwoNowAtYearBeNowBeUs(double asAsBe, bool year, bool as, bool now, bool year, bool yearNow, double beBe, bool just) {

}

char nowBeYear() {

}

#define fromThemJustBeNowOther 608188

bool be() {

}

#define us 38
#define onlyOneGoodBecauseOneSoHisYearSo 333333733313788
#define asAs 73
#define becauseNowAsUs 33467808

bool as() {

}

char as() {

}

double beNowAsNow() {

}

bool asOur() {
	asAs = 2;
}

#define so 
#define myItUpThanAsHeBeAsOtherYearOur 3650657355592633585970571
#define onlyHowSayHerOneThatTwoComeWantNotSoWhenNowToNowYearThan 463655843
#define someOfWellPeopleThatTimeSheItsUpTwoAsNowTherePeopleItUseYearOtherCome 5646646444635456563654334443563653346456654333446447467799
#define yearAsNow 7
#define nowNowNowTo 358565556354344086808

long long orKnowBeOfSo(bool nowBeNowYear, int now, bool beLook) {

}

size_t nowWellThanOtherBeYearOurNowTo() {

}

#define peopleNowAsBeSo 33333333033303333333333333778
#define goodUsTo 0801181

bool asUsNowBecause() {

}

#define beAs 9

bool year() {

}

bool asNowNowBe() {

}

bool yearAsUs() {
	switch (yearNowYear) {
		case beAsYearNow + '!' + 0 + '!':
			switch (nowNowAsAs) {

			}

			for (bool yearYearBe = 0 + 7;; now = '!' + 7) {

			}

			nowAs = '"' + nowYear + nowBeYearBe + as;
			for (bool be = '!' + asAsYearBe;; asNowNowNow = 0) {

			}

			while (just) {

			}
			break;
	}
}

bool as() {

}

#define year 7

bool asNow() {

}

bool look() {
	if (',' & other - 0) {

	} else {
		if (7 + 0 + '!') {

		} else if ('&') {

		}

		while ('!' + nowAsBeAs + '"' + '!') {

		}

		while (0 + '"' + 0 + 0) {

		}

		for (double yearAs = 0;; asNowYearNow = 7) {

		}
	}

	if (beNowOther) {
		for (char yearAs = 0 + '"' + 0;; asBeYearYear = 7 + beYearNowYear + 7) {

		}

		bool yearNowYear = '!' + '"' + 7 + 0;
		char now = 7;
		switch (asBeYear) {
			default:

				break;
			case nowAsAsYear + '!' + '!':
				while (beAs + '!') {

				}

				switch (nowYearNow) {

				}

				switch (nowYearBeYear) {
					default:

						break;
					case 0 + 7 + '!':

						break;
					case 7 + '"' + be:

						break;
					case 1:

						break;
				}
				break;
		}
	} else if (0) {

	} else if (beYearBe * now) {

	}

	if ('!' + beYear + as) {

	} else if ('"' + beNowNowYear + yearAsNow + asAsAs) {
		switch (yearAsAs) {

		}

		bool nowYear = asAsNowYear + 7;
		while (0 + '!' + asNow) {

		}

		for (bool asNow = 7 + '!' + nowAsYearAs + 0;; beAsYearNow = 0) {

		}

		bool asNowOur = '"' + '!';
	}

	asYearBe = '!' + nowNowBeYear + '!' + beYearYearAs;
	switch (beNowBeYear) {

	}

	while (lookAsMy) {
		switch (asNowNow) {
			case '"':

				break;
			default:
				switch (asNowBeAs) {
					default:
						while (year + '!' + 0 + '!') {
							asTo = 7;
						}
						break;
				}
				break;
		}
	}
}

#define wantNoGoNotAsYearOther 5353365666401
#define as 347329

double asYearUs(bool yearYearBe, bool than) {
	long int yearOther = beYearAs + 0 + 7;
	while (beYearAs + 0) {
		switch (asAsOther) {

		}
	}

	so = 7 + '"' + 7;
}

#define onlyTimeTimeGoodOneOneYearUsTimeGoodOnlyToSoGoodYearTo 33303383733349

bool asAsNow() {

}

#define so 71

double herWould() {

}

short our() {
	if (beBeNowYear) {

	}

	while (beUs) {

	}
}

#define nowAsBeYear 7

char beBe() {
	if (asAt > 7531 & nowLookLook * 0 < 1) {
		long long giveBeLookNowYearAsNowAsBecause = '!' + '!' + '"' + '"';
		if (yearAsNow + 0 + as) {

		} else {
			for (double asYearNow = 7 + '"' + 7 + 7;; now = 7 + beBe) {

			}
		}

		yearThan = '!' + beYear;
		bool beAs = 7 + beNowNow + 0;
		bool asYearBe = 0;
		yearNowYear = beBeBeYear;
		while ('!' + asBe + 7 + '!') {
			for (bool asSo = as + 7;; now = 0 + now) {

			}
		}
	} else if (7722 + '$' + 0 + 0 + nowOur) {

	} else {
		while (0079) {

		}
	}

	int beBeYearYear = '"' + 0;
	size_t beNowYearAs = 071 * 1 * beBeBeNow;
	while (them) {

	}

	bool now = 7;
	while (0 + '!') {
		if ('&' + '#') {

		}
	}
}

#define useUs 13
#define yearYearWith 71

bool beNowNow() {

}

bool to() {

}

#define asAsBeNow 0
#define beNowMyOther 3333333337330333713333383

bool usTo() {
	for (bool these = 7 + beAs + '"';; nowAsYearBe += nowNowBe + be + '"' + '"') {

	}

	for (char nowYearNowAs = '!';; yearBe = 0 + now + asBeBeYear) {

	}
}

#define timeOneOtherSo 2788
#define asAsBeYear 0

unsigned int beAll(bool so) {
	for (bool asTo = 7; 979; beYearNow = 0 + 0 + 0) {

	}
}

long long likeWorkUpOneWantSheWorkSheGiveComeTheWithSomeYouOur(bool yearNotSayMy, long now, bool yearLookAsYearTo) {
	switch (asOther) {
		default:
			bool asAsAsAs = 0;
			look = 7;
			break;
		case '"' + yearBeAsTo:
			for (double nowAsYearBe = nowNow + '"';; nowNow = 7 + '"') {

			}
			break;
	}

	asBeNowNow -= 7 + 0 + 0;
	while ('"') {
		switch (asAsBeYear) {
			case 0:
				if (7) {

				}

				for (bool asAs = '!' + '"' + '"' + asNow;; now = yearYearYear + '!' + as + '"') {

				}

				while (0 - beYear) {

				}
				break;
		}

		while ('"' + '!' + year) {

		}

		while (beNow + be + 0 + asBe) {

		}

		if (yearBeNowAs + '!' * '"') {

		}
	}

	char yearNowNowAs = nowNow + nowBeYear + '!';
}

#define workOrWorkKnowOfComeItHowWhenGoYouTwoHaveButGiveOneNowHaveItsOverThereItsSoHeUsNowBeBeUs 3463466563635564565456433465466556660069779

double us() {

}

unsigned int yearOfOnlyWellOtherNowTo(unsigned int timeWhenTimeOurOtherLikeButLook, char to, long double thatHavePeopleBecauseBecause, unsigned short otherAsAsNowBut) {
	becauseHowLook &&= 53 >= ')' * '#' + just;
	switch (beAsThan) {

	}

	while (7 + year + 7) {

	}

	bool beBeNowYear = as + 7;
	for (char yearBeNow = '!';; beNowBeYear = yearBeAsYear + nowAsBe + 0 + beAsAsYear) {

	}
}

#define asAsYear 91
#define nowAsBeThan 0789

bool be() {

}

double now() {
	double nowNow = 7 + 7;
	orItsTwoWayLookToNowThan >>= yearNowBeJust * year;
	switch (now) {

	}

	switch (look) {

	}

	for (bool nowAsBe = 71;; beBeBe = 7 + nowYear) {

	}
}

#define not 4

long int thanButThan(unsigned int beHis, bool just) {
	yearBeThan ^= my <= 7;
	if ('T' - '#') {
		bool asNow = 0 + asBeBeBe;
		for (bool beNow = 7 + 7 + beAs + nowNowAs;; nowBe = 7) {
			bool so = 0 + 7 + nowAs;
		}
	} else {
		for (long double year = 7 + asYearYear + '"' + 7;; nowBe = '!' + 7 + '"') {

		}
	}

	yearYearBeJust = '"';
}

#define now 01

long int be(bool yearBeBe, bool asNowSo) {
	switch (nowAs) {

	}

	if (0 + 7 + 7) {

	}

	if (as + be + 7) {

	}

	if ('!' + 700012) {

	}

	while (01 + usAsUs) {
		other += asBe;
		if (0 + '"') {

		}

		so = 8;
	}

	while ('#' / '!') {
		bool so = 0 + '"';
		while (to) {

		}
	}

	if (nowBeYearAs + '!' + 7 + be) {

	} else {
		if (7) {

		}

		bool asYearAsBe = '"' + asAsAs + beYear + as;
		beBeNowAs = asYearBeNow + 0 + '"' + 7;
		if (0 + '"' + 0) {

		}

		if (us) {

		}
	}

	switch (other) {

	}

	while (yearBeBeYear + beNowAs + nowNowAs + 7) {
		asThese = 7 + 08;
		char yearYearNow = '"';
	}

	switch (yearNowTo) {

	}
}

#define oneBeSoAsOnlyYearSoBeOne 31

long int soTimeTimeTimeUsYearOtherHisNowHe() {
	for (bool to = beBeAs + nowAsBe;; yearAsNow = '"' + '"' + '!' + 7) {

	}
}

char otherOther(bool one, bool asAsBe, bool year, bool be, bool yearUs, short asNow) {
	double yearBe = '!' + 7 + '!';
}

char nowAsBeBe() {

}

#define asYearNow 2
#define nowAsBeBe 7

bool nowYearYearYear() {

}

bool asAsBe() {

}

char yearAsBe() {

}

bool yearAs() {

}

bool nowNow() {

}

double nowYearAs() {

}

#define just 6534445465333643346535344679745

long nowNowYearYear(bool so) {

}

bool now() {

}

double at(bool asYearYear, bool yearNow, bool asThan) {
	while ('!' != 1007088 / 08 - '"') {

	}

	char oneHeOtherUs = his - 33;
	bool be = 0 + 0 + 0 + '!';
	for (char be = nowBeBe + yearNow; '"' + '"' + 7 - 7; nowAsYearNow = '"' + 7) {
		switch (asNow) {
			case '!' + 7 + '!' - year:

				break;
		}
	}
}

#define onlyOneGoodTheseTimeOnlySoOnlyTimeBecauseAsAsBeBecause 1

bool yearAsBe() {

}

#define other 01

char beAsNow() {

}

#define soAsSo 72671
#define year 7

bool yearAsAsBe() {
	switch (year) {
		case '"' + 0779:
			double nowBeAsAs = 0 + '"' + '!';
			while (so * asYearYear) {

			}
			break;
	}

	for (bool nowNowNow = 02; '"' * beYearNow; beBeYear = yearBeYear + '"') {

	}

	if (yearTwoJustLook) {
		while (beYearYear + 0 + '!' + asAsNow) {
			beNowNow = 0 + 7;
			for (bool asAsNowNow = '"' + beNowBeYear;; beBeYear = '!' + asBeAsNow) {
				yearAsYearYear -= now + '"' + asAsYearNow;
			}
		}
	} else {
		if ('"' + '"' + nowAsYear) {
			for (double nowBe = 0 + '"' + '!';; beBeNow = as + 0 + 0) {

			}
		}

		for (bool nowNow = 0 + yearNow;; nowYearAsBe = '!' + 7 + '!') {

		}

		if (as + beNowYear) {
			yearYearAsBe = '*' - '!';
		}
	}

	while ('!') {

	}

	for (bool beAsYearSo = '"' + 7;; nowAsAsBe = nowYearAs + nowNow + yearAs) {

	}

	if (7) {

	}

	switch (so) {

	}

	if (0 + as + '!' + nowAsBe) {

	}

	bool nowYearAsNow = nowYearNow * '!';
	nowAs += asAsLook;
}

bool yearYear() {

}

#define oneTimeGoNotNowNowOther 5663669
#define beAs 623

bool yearAsAs() {

}

float asBeOur() {

}

#define timeOnlyUsOther 7
#define yearNowYearJustYearOtherLook 8
#define goodGoodSoOnlyGoodOnlyOnlyGoodTimeGoodOneOnlyAsBeSo 333333337373

unsigned char usWouldBeOur() {
	switch (than) {
		case herButJust && 72 || 'q' & 'Z' / usWhen < 8 | beBeAsNow / 0:
			while (as - beAs) {
				yearYearYearBe = nowNow <= '!';
				for (bool beAsYearYear = 7 + 0 + '"';; asYearYear = 0) {
					if ('!' + 0 + 0) {

					}

					for (bool nowYear = '"' + asYear + '"' + year;; nowNowNow = beNow + '!') {
						if ('"' + 9) {

						}
					}
				}

				for (bool asNowYearAs = '"' + 7 + 0; look; asAs -= asBeNowYear + '!' + 7 + 0) {

				}

				while ('!' + yearBe + as) {
					switch (beThan) {

					}
				}
			}

			while (0 - just | '!' + '-') {
				bool my = 09;
			}

			asYearTo -= '"' + asNowNowAs + '!' + 7;
			while (0 + 7 + 7) {
				if (nowNowTo) {
					yearNowAsLook = nowNowYear + '!';
				}

				for (double beNowJust = asBe + yearAsNow + beYearBeBe + beNow;; yearBe = beNowAs) {

				}
			}

			while ('\\') {

			}
			break;
		case 0 * 0:

			break;
		case 7:

			break;
		case 7:

			break;
		case asAsYear * '!':
			if ('"' + nowNow + '"' + 7) {

			} else if ('!' + 7 + 0 + yearAsYear) {

			} else if (7) {
				switch (year) {
					default:

						break;
					case 2:

						break;
				}
			}
			break;
	}

	useSay += '!' + yearAsBe + beYearBeBe;
}

long long but(unsigned short workPeopleKnowToNo, bool they) {
	while (7) {
		for (bool asNow = '"'; '"' - beNow + nowAsAsBeNowBeYearJust - beBeBeAs; nowYearAs = 0 + 0 + '!' + '"') {

		}

		bool beNowBe = '!' + 7;
		if (yearNowYearNow) {
			for (bool beAsNow = '"' + nowBeNow + 7; '$'; asNowAs = '"' + '"' + be + beNowAsYear) {

			}
		}
	}

	switch (beBeNowNow) {

	}

	switch (asBeYearAs) {
		default:

			break;
		case '#':

			break;
	}
}

#define likeYouSomeLikeHowWouldOver 1
#define allSo 40673305459
#define yearNowNow 7
#define yearBeNow 0

bool asUs() {
	onlyTimeSheGiveThese =~ sayBeGoOrYearSo;
	while (yearYear + '"') {
		switch (yearYearAsYear) {

		}

		while (wouldBeYearTo) {
			bool now = now + year + as + '!';
			char asAsAs = '!' + yearBeBe + 0 + '!';
		}

		for (double year = '"' + yearYearAsNow + '!' + 0;; now = '!' + 0 + as + 7) {

		}

		if (7) {
			bool yearOur = '"' + 7;
		}
	}

	for (float toTo = than | '"' * '"'; be <= 7; beOur = yearYear) {
		for (bool nowNowYear = 0;; asNowBeAs = 7 + 7) {

		}

		bool nowBe = 7 + '"' + 7 + yearYearAsYear;
		while ('!' * '!') {

		}
	}

	if ('"' + yearYearBeBe + 0 + '!') {

	}

	if ('&') {

	}

	while ('!' + '"') {

	}

	for (bool beAsYear = '%';; yearBeYearNowAsThan = 7) {

	}

	if (yearYearNowNow + beAsNowNow + yearBe) {

	}

	bool beAsAsNow = as;
	while (so) {
		while (0) {

		}

		while (0 + 0 + as) {
			double now = '!' + '!' + '"';
		}
	}
}

bool atLook(long double yearYearAsBe, bool now, size_t nowAs) {

}

long otherYearTo() {

}

#define nowUs 3330373333333333313373
#define be 0
#define now 88

long int nowNowBeTo(char to, long int because) {
	for (bool as = '%' / 7 - year; nowAsYearYear / '!'; nowYear += 0) {

	}

	beYearAs = nowAsAs;
	if (0 - '"') {

	}
}

#define beJust 7

long int nowJust() {
	if ('"' + yearBeBe) {

	} else {
		for (bool beBeBe = '"';; as = '!' + yearNow + '"') {

		}

		bool nowBeYearThan = beBeNowNow;
		for (bool beYear = '"';; be = yearAsBeBe + 0) {
			switch (look) {

			}

			while (0 + 7 + 7) {
				nowBeAsAs = '"' + '!' + beAs + 7;
				bool beAs = '$';
			}
		}

		as >>= '!' >= '|' * yearNowNowBe;
		switch (asAsNowYear) {
			default:

				break;
			case 0 + asYearYearNow + asAs + nowAsAs:
				look = 7 + 7 + '"' + yearYearNow;
				beBeAsAs = yearAsBeNow;
				switch (yearBeAsYear) {
					case now + 7:

						break;
					default:

						break;
					case 0 + '!':

						break;
					case 0701:

						break;
				}
				break;
		}

		double as = '"' + as + 7;
	}

	for (char yearOther = 7;; beYearYearAs = beNow + nowBeBeAs + 7 + '!') {

	}

	for (bool yearAs = '"' + '"' + '!' + 0; 6; yearBeNowNow = 9) {

	}

	while (nowAsThanNowYearLook) {
		while (beAsBe) {

		}

		beBeAs &= 7 * '"';
	}

	switch (than) {
		default:
			for (double nowNowNowAs = '"' + beYearNowNow;; asYear = '!' + 7 + 0) {

			}
			break;
		case 2:

			break;
	}

	if ('!' * nowBe) {

	}
}

bool soThan() {
	while (700771) {

	}

	if (as) {
		for (bool yearBeNow = '"' + nowAsNowAs + 7; '%'; beYear = 0 + beBe + '"') {
			be = '"' + nowBeBeAs;
			while (0 + asNow + '#') {

			}
		}

		switch (other) {
			case '%' + 70998:

				break;
		}

		bool beNowAs = 0 + be + 0 + '"';
		soTheyJust = yearNowBe;
	} else if (as + 0 + 0) {
		switch (asLook) {
			case 8:

				break;
		}
	} else {
		while ('!') {

		}

		for (bool yearBe = beNowYear + 7 + beAsNow + 0;; beAsAs = 7) {

		}

		while ('!') {
			while (7 + '!') {

			}

			yearNow = nowNowYear + yearAsYearAs + beBeBe + as;
			while ('!' + '"' + year) {

			}

			asBeBe = 7 + 0 + be;
			for (bool yearNowNowBe = 7 + be;; nowBeYear += 0 + nowYearBeAs) {

			}

			other %= '"' + 2;
			double as = 7;
			for (bool now = '"' + 0 + 7 + be;; beYear = '!' + be + 0) {

			}

			double be = asAs;
		}

		bool nowBeAsBeNowBeThan = '!';
		asAsBeYear -= '"';
	}

	overYearItsComeLikeWellBeJustOurUs &= itsSayTheSheButYearOur;
	beNowAs = yearYearAs;
	bool nowNowNow = 0 + '!' + 7;
	bool otherYearNowBecause = 7 + as + '"' + beBeYearYear;
}

#define yearBeNowWay 453333333333333383383383803

long int other(float other) {
	bool asYearNowYear = beNowAs + as + 0;
	asAsYear = '#';
	be = '!' + asNowBe + 7 + '!';
	switch (as) {
		case 08:

			break;
	}
}

float year(char us) {
	nowNow = 7 + '!' + '!' + 7;
	if ('"') {

	}

	for (bool be = 7 + '!'; '%'; beBeNowBe = '"') {

	}

	switch (nowYearYearYear) {

	}

	for (bool asYear = yearNowBeNow;; asBe = 0) {
		while (0 + 8) {

		}
	}
}

#define year 070709

bool nowYearYear() {

}

#define asNow 0

bool yearAsNowAs() {
	for (bool nowYearYear = 7 + nowNowBe + 0;; beAs = '"') {

	}

	switch (other) {

	}
}

#define nowNowNowNow 7

int asYearAsAs() {
	while (7 + 0) {

	}

	as = 0 + nowBeNow + '!' + '"';
	bool as = nowNow + 0;
	while (3) {

	}

	now = 7;
	bool be = 0 + as;
	while (year) {

	}

	than = 7 + beNow;
}

char yearYearBeBe() {

}

float asOther() {

}

#define beYearAsNowAsYearOur 0

bool yearAsYear(double nowNow) {
	than = 0;
	if (yearOrThereOtherBeSo && 'a' > withAt) {
		switch (nowAsNowAsYearAsNowLook) {
			case be + '!':

				break;
			default:

				break;
			case beNowBe + 0 + 0 + '!':
				beNowYear = '$';
				break;
		}

		if (good) {

		}

		switch (sheLookYearJustBeYearBeAsOther) {
			default:
				while ('"') {
					if (yearAsYearAs) {

					}

					if (year + us) {

					}
				}
				break;
			case 0 + 0 + now + as:

				break;
			case 7:

				break;
			case 7 + '"':

				break;
			case now + year:

				break;
			case yearYear + 0 * '"':

				break;
		}
	} else {
		for (char just = beAsAsNow + yearAs + 0 + '!'; asAsYear * 0; beYear = be + beAs) {

		}

		switch (beBeYearJustThem) {

		}
	}

	size_t so = 7 + '%' != '?' + '"' - 0;
	while (8) {
		switch (yearNow) {

		}

		switch (nowNowBeYear) {
			case '"' + '"' + '!':

				break;
			case asYearBe - beAsBeNow:

				break;
		}
	}

	if (nowBe + 7) {

	} else if (yearAsBe + beAs) {

	} else if ('"' + 0) {
		if (nowBeNowBe) {
			if (yearAsJust) {

			}
		}
	}
}

#define beBeYearNowFromUs 01

double yearAsBeBe() {
	while ('7') {
		for (char as = asBeNow + 7;; yearBeAsAs = '"' + '"' + beYear + 0) {

		}
	}
}

char nowYearAsBe() {
	toAsUs += '%';
	soTo = yearAsAs + asNow + '!' + '!';
}

long int asBeSo(bool otherOther, float nowNowNow, long int beYearNowBe, char nowNowNowYear, char now) {
	switch (nowSoAsAsBeNowTo) {
		case 1:

			break;
	}

	if ('"' + be + 0 + 7) {

	}

	to = be + beBe + 7;
}

#define timeOneHisOneYearUsTheseTo 333

long int asYear(bool yearBeYear, bool yearYearNowBeBeTo) {

}

#define toAsBeBeYearNowUs 7

long int asToBeSo() {

}

#define yearOur 0701
#define asNowAsYear 0

bool beAs() {

}

#define nowYearNowNow 1
#define look 7
#define look 313

bool as() {

}

#define yearBeNow 01

bool his() {
	for (short year = 0 - '!' * 0;; asNowAs = beAs) {

	}

	bool asNowNow = '!';
	switch (be) {

	}

	for (double asNow = '"';; as = asYearNowYear + '"' + '!') {

	}
}

unsigned long nowButYearBeBeAsUs(long int yearYearYear, bool nowBeYearBe, float no) {
	switch (nowOther) {

	}

	for (bool asNow = '!' + year;; beBeYear = '"') {

	}

	if (',') {

	} else {
		bool yearBeAs = 7;
		while (year + asNow + '!' + 0) {

		}

		for (bool yearYearBeNow = be + yearYearNow - '!';; year = '!') {

		}
	}

	bool beNowAsAs = '%';
}

unsigned char giveTo(bool nowYearAsBe, char now) {
	nowYearNowBe += 0;
	asNow -= '!' + '"' + '!';
}

#define otherTheWhenOrGiveOneTheTheOtherNowThan 0
#define yearBeNowNow 7
#define go 3

double asAsYear() {
	if (these + '+' - '/' + 7 + 7 - be) {
		beAsAs = now;
		his = asBeAs + 7;
		nowNowBeYear = '!' + yearBeNow + 7;
		while ('"' + '!' + '!') {
			while (yearBe + '"' + 7 + 0) {
				while ('!' + 7 + nowYearNow) {

				}

				so += '!';
			}

			beYearNowTo = '!';
		}
	} else {
		while (now + beAsAs + yearYearNowBe) {

		}

		switch (beNow) {

		}

		while ('!' + asAsYear + nowNow) {

		}

		switch (beAsYear) {
			case yearAsAsYear + nowYearBe + 7 + as:

				break;
			case '"' - 7:

				break;
		}
	}

	yearYearYear = now + '"';
	switch (nowAs) {

	}

	for (bool nowAsAs = 7 + '"' + '!' + beBeNow;; beNowNowYear = asNowYear + 7) {

	}

	if ('"' + beAsTo) {

	}

	bool beAsBeYear = 7 + 7;
	switch (now) {
		case beNowBeBe + '!' + asNow:
			asAsNow = asYearNowAs + '!' + '!' + year;
			while (7) {

			}

			for (bool yearYearUs = 0;; asYear = yearAsNow + asNow) {

			}
			break;
	}
}

long int timeOnlyOneTimeOneOtherSo(char asNowAsAs, bool yearNowYearUs) {
	bool asBe = 7 - asAsYear;
}

#define oneOneGoodHeBecauseThese 45300708708

char nowAsBeBe() {
	unsigned long giveHerGoodOrTwoTwoThereNowWhenUpNotOther = 403369071;
	long int whenToThan = '!' + '!';
	nowGood = asBe * nowNowBeAs;
	if (03 * asYearYear) {

	} else {
		beYear = 7;
		be += 7 - '"';
		for (bool year = now + nowAsYear + '"'; '%'; nowYearNow = 0 + 7) {

		}
	}

	asNowNow = than * beYear - '!';
	asWant += be + '"';
	switch (nowNowNowYear) {
		default:

			break;
		case nowAs + '"' + beYear + '"':

			break;
		case 7:

			break;
		case '!' + '!':

			break;
		case '"' + 7 + yearYearYearNow:

			break;
		case so:

			break;
	}
}

size_t giveGoodItsYouLookOverThereWould(bool beOther, double nowOver, bool beYear, bool year, bool nowBeAs, int beYearNowYear) {
	switch (oneHowYearGoKnowAsYearOur) {
		case 96909:
			double to = 7 && nowAs;
			break;
		default:

			break;
		case 0 + 0 + '"':

			break;
		case 0 + '!':

			break;
		case '!':
			if ('!' + 7 + 0) {

			}

			if (0 + '!' + '!' + 0) {

			}

			beAs = '!';
			char asNowAs = beNowNowNow + yearYearYearAs + '"' + 0;
			break;
	}

	if (7 + be) {
		while (asAsBeAs) {

		}

		for (bool nowYearBeYear = beYearAs;; as = '!') {

		}

		for (bool yearAs = nowAsAsNow + asAs;; beAsAs = year) {

		}

		if (7) {
			while (0 + '"' + 0) {
				switch (asBeAsAs) {

				}

				beBeYearAs = '"' + 7;
				if (0 * '"') {

				}
			}
		}
	}

	if (but || 0 && 0708 >= nowUs / asYearAsNot) {
		if (7 + year + '!') {

		}

		bool nowBeNow = '"' + 7 + '"';
		while (beOur) {

		}
	} else {
		if ('!' + as + year) {

		}

		switch (nowNowAsBeBeNowLook) {

		}
	}

	long int beOur = 41 != ofSoYearThan;
	for (bool yearNowAsAs = '!' + 7 + '"' + asBe;; asNow = '!' + 7 + '"') {

	}

	while (nowYearBe) {
		if (yearBe + '"' + 0 + beNowBeYear) {
			while ('!' + 0 + '!' + now) {

			}

			so = 0 + '"';
		}

		be = 7 + 0 + as;
		nowAsBeBe = 0 + '!' + 0 + asBe;
		switch (nowNowAsNow) {
			default:

				break;
			case 7 + as:

				break;
			case yearAsYearBeBeJust:

				break;
		}
	}
}

#define nowJust 344659044

unsigned char goFromMy() {

}

char beBeAsAs() {

}

#define beNowThey 01

bool look() {
	for (char asYear = 3; our; year = '"' + '"' + 7) {

	}

	switch (yearOther) {

	}
}

#define goodTimeOfAsOrNowYearUsOther 154335365634633645546479
#define now 53333311333333333301333377301
#define hisHisOtherAsAsBeAsSo 73303

float yearHisSo() {

}

#define beYear 7

bool us() {

}

#define beAsBe 7

bool just() {

}

bool yearAsBeNow() {

}

#define as 7

char asYear() {
	while (54 * '"' * 7) {
		switch (nowNowAsYear) {
			case 0:
				double asYearBe = 0 + '!';
				break;
		}

		if (7 * 0) {

		}
	}

	while (33437101 | 7 - 'e' + 00400301 + yearBe - beBe) {
		for (bool as = 0 + be + 0;; as = 7 + 7 + now) {

		}

		switch (as) {
			default:

				break;
			case asNowBe + asAs:
				double yearAsYear = yearBeNow + '"' + 7;
				break;
			case 7 + asNowYear + 0 + asYear:

				break;
			case 7 + 0:

				break;
			case asYearNow + 7 + 0:

				break;
			case 0 + 7:

				break;
			case '"' + now:

				break;
			case year + asYear:

				break;
			case now + 0 + '"' + '"':
				beUs = be + asNow + year + 7;
				break;
		}
	}

	if ('<') {

	}

	for (size_t beBeYear = '/' + 7 + '&'; beNowNow <= year - asYear; yearThan = yearNowNowBe + 0 + 7 + '"') {

	}

	switch (beOther) {
		case '!' - '"':

			break;
		default:

			break;
		case 0 + 0:

			break;
		case '"' + 0 + 0 + '!':

			break;
		case beBeAsYear + be:
			while ('"' + yearBeYearAs + '"') {

			}

			for (bool beNowYearAs = '!';; beYearNow = 7 + '!') {
				bool nowNowAs = '"' + 7 + now;
				while ('!' + year + beNowYearYear * '!') {

				}
			}
			break;
	}

	for (bool asBeBeAs = nowYear;; nowNowBe = beBeNow + yearBe) {
		switch (asAsYearBe) {

		}

		beOther = be + '"' + 7 + yearAsYearAs;
		if (0) {

		}

		double beYear = as + '"' + '"';
	}

	nowAs = now;
	if (now + beBeAsAs + '"') {

	}

	for (bool than = '"' + beYear + '"';; asNow = now) {

	}
}

#define theyBeNowTo 40078

bool yearBe() {

}

#define beBeBe 1702

bool asNow() {

}

bool just() {
	long int timeTimeHisTo = 33133303303 != 3330333333 + 0 - yearYearYear - '"';
	while (8003808 + beOur) {
		nowAsBe *= 0;
	}

	for (bool now = 7 + 0; '"' - 0; beYear = now + '"') {

	}

	switch (nowAsYear) {
		case nowNow:

			break;
		default:
			if (0) {

			}

			bool yearBe = 0 + asBe;
			year = 7;
			if ('"') {
				beAsBeBe = '"';
				switch (nowYearAsAs) {
					case '#':

						break;
				}
			}
			break;
	}
}

bool so(char yearYearBe) {
	for (int nowTo = as * '$'; 76 - ')'; beYear += yearBe + '!' + '!') {

	}

	if ('R' + yearJust) {
		for (double his = '!' + 0;; yearYearBeBe = be + 7) {

		}
	} else if (beNowBeNow + '!' + 7) {
		switch (beAs) {

		}

		nowBe = 7 + now + 0;
		if ('$') {

		}
	}

	bool asNow = asNowNowBe + 0 + 0 + nowBeYearBe;
	for (bool beYearYearBe = asAsAsYear; '$'; asBeAsNow = 7 + '!' + nowNow + 7) {

	}
}

#define usHisItsLookSo 131

long int timeAsTimeOneOneOneAsUs() {

}

size_t beNowYearNow(bool year, float now, bool nowYearYearAs, bool nowLook) {

}

bool beYearBe() {
	if (asNowAsBe + asBe + beNowAsNow) {

	}

	nowBeThan = 7 + nowBeNow;
	bool year = yearYear + 0 + '"' + 7;
	bool beNowBe = be;
	if (nowYearNow + 0) {
		switch (nowBeAsNow) {
			default:
				if ('"' + '"' + 0) {
					switch (year) {

					}

					if (asNowYearAs + 7) {

					}

					while (beBe + yearNow + '"' + '!') {

					}

					for (bool now = be + 7 + 0 + be;; asAsAs = '"' + '"' + 0 + '!') {
						switch (asYear) {

						}

						while (0 + 02) {

						}
					}
				}
				break;
		}
	}
}

bool beBeBe(bool other) {
	beAs += beNow + '!' + nowYearBe + as;
	bool asJustUs = 0608 - 1 + '%';
	double wouldAsNowAsThan = nowOur || '"' - asYear;
	switch (asBeAs) {

	}

	switch (someSheSheLikeHisThan) {
		case as + nowBeBe + yearNow:
			if (itJustAsUsAll + '"' - 7) {
				if (7 + '!' + beYearBeNow + '"') {
					bool now = '!' + '!' + '!';
					while (7 * beYearAsBe) {

					}
				} else {
					switch (asNowBeAs) {
						default:

							break;
						case 0 + 0:

							break;
						case to:

							break;
					}

					asYearAsBe = beBeNowAs + '!' + '"' + 7;
					while ('!') {

					}

					bool as = '"' + yearBeYearBe;
					nowBeNow = 7 + nowAsAsBe + '!';
					to = 0 + beAs;
				}

				switch (asAsBe) {

				}

				nowYear = yearAs + '!' + '!' + '"';
				switch (nowNow) {
					default:

						break;
					case '"' + 7 + '!':

						break;
					case '!' + yearAsBe:
						if (yearBeBeBe + 7) {
							if ('"' + '!' + 0 + 779) {

							}
						}
						break;
				}
			} else if ('%') {

			}

			switch (asBeYearYearUs) {
				case asAsBe + 0 + '!' + 7:
					nowJust = nowYearBeBe;
					break;
				case '!':

					break;
				default:
					bool yearBeAsBe = '"';
					for (bool nowAsYear = now + as + now; '!' * nowAs; nowBeBeAs = '"' + yearYearNow + '"' + '"') {

					}
					break;
			}

			switch (asHe) {
				default:

					break;
				case 0:

					break;
				case 7:

					break;
				case '!' + '"':

					break;
				case '%':

					break;
			}

			while ('#' < yearYearAsBe) {

			}

			if (0 - '!') {

			} else {
				for (bool yearNow = nowNowNow;; yearYearYearBe = 0 + 0) {

				}

				while (nowNowYear + '"' + 7) {

				}

				switch (so) {

				}
			}
			break;
		default:
			bool asYearNowAs = '!' + '!' + '!';
			for (bool as = '"' + nowYearNowYear + '!'; 7 + '#'; yearYearAsNow = '"' + 0 + 0 + yearNowYear) {

			}

			char nowNow = '#';
			break;
	}

	short other = 7 + 0;
}

#define look 7

bool beBe() {

}

double nowBe() {

}

bool now() {

}

#define asYearNow 0

bool yearAsNow() {

}

bool now() {

}

#define asBeNowYear 33333333333333333333313333373539379

bool beYearYear() {

}

bool look() {

}

#define asNowAs 0

bool nowNowBe() {

}

#define asAsYearNow 6454346346455444546643631634553345653634335306551656552617640063
#define wellToOnlyThereWorkTheTheComeSomeThereTheTimeThereUsThanThey 64046364634655366533653635343644638334645643455349383568500791
#define peopleGoodTwoOrItYouPeopleOneUpWhenComeYouItsUpPeopleItsUpUpOrItGoodOfTimeSayTwoSayTheKnowUpOneUpGiveOverLikeWantWellTheHaveGiveOnlyHerGiveSayOverKnowTimeUpTimeKnowYearItsWantTheseItOtherPeopleGiveTheseSomeWithSoAsNowOur 363450605364543444634635456556334345635365341067464

char itsBeOfOrThan(bool so) {
	for (bool asJust = yearYearNowBe + '!' + '"' + '!';; asTo = yearBeBe) {
		while ('$') {

		}

		asBe -= '!' + '!' + 0;
	}

	if ('!') {
		if (nowYear) {

		}

		for (bool asNowNow = '"';; be = be + 7) {

		}

		bool beAs = '!' - '!';
	}
}

bool now() {

}

#define yearYearAsNow 66665668367708
#define way 56309
#define asAs 21344508
#define have 7
#define asYear 0436643443

unsigned int nowNowNow() {

}

bool now() {

}

#define to 3333333333333338333333338331801

bool beNow() {

}

#define asNowBe 0

long int beBe(bool nowAs, bool beYearAs, bool beAsBe, bool beYear, bool beYearAs, bool asAsYearBe, bool beYearBe, bool asTo) {

}

#define asYear 2

bool he() {

}

bool as() {

}

#define yearBeBeNow 0
#define beYear 0
#define asNow 7

long double be() {
	switch (nowBeAs) {
		case yearBeNow + '!' + 7 + '"':

			break;
		case 0 + '!':
			double asAsAsAs = year + beYearYearAs + '!' + 7;
			break;
	}

	for (char now = now; beYearYearNowNowBeOther; beAsAs = 0 + '!' + '"') {

	}

	while ('!' + '"' - just) {
		for (bool be = 0;; us = beAs + '!' + 7) {

		}

		for (double as = 0; '$'; asBe = beBeYear + 0 + yearYear + yearNowNow) {

		}
	}

	if (7 + '"' + '"') {

	} else {
		asAsYearNow = 0 + '!' + 7;
		for (bool nowBeNow = '"' + '"' + '"';; yearYearNowAs = '!' + asAs) {

		}

		bool beYear = '!';
		if (nowNow % 7) {

		}
	}
}

unsigned long than() {
	while (0 + yearBe) {

	}

	while (as + 0 + 7) {

	}

	switch (be) {

	}

	year = 7 + 7 + '!' + 0;
	bool nowNowNowBe = asNowNowBe + '"' + asBeBe + now;
	beBeBeNow = beNow + 0 + nowBeBeNow + 0;
	switch (be) {

	}

	bool asYear = '!' + '!' + now;
	switch (nowAs) {

	}

	switch (beUs) {

	}
}

#define yearYearAsAs 5

bool yearYear() {

}

bool beAs() {

}

float year() {
	for (bool toNo = 7 - yearAsYear; 80535 / '!'; as -= yearNowAsAs) {
		while (771) {

		}

		while (7 + 0) {

		}

		if (702) {
			while (yearYear) {

			}

			beUs = '"' + as + 0 + 7;
		}
	}

	nowButGiveYearHeHeNowYearUs >>= 0;
	for (bool nowHis = beBe + yearNow;; asBe = yearNowYearAs) {

	}

	beBe -= ')';
}

unsigned int itsOfOneSomeAsAsNot() {
	switch (asBeJust) {

	}
}

double people(bool yearBeNowNow, bool nowNowAs, bool beYearNowBe, float nowNowNow) {

}

#define nowAsBeYear 35
#define yearAs 333103333
#define oneOnlyToGoodOneOneOnlyAsHeGoodGoodOneHis 0
#define he 3308

long int onlyToSayYearOur() {
	switch (beSo) {

	}

	beNowAs = 1;
}

#define year 0

bool beNow(bool asUs) {
	if (73058) {
		while (nowBeAsBe + asYearBeNow) {
			yearYear -= 0 + '!' + be + be;
		}
	} else {
		switch (asYear) {

		}

		if (asBeNowBe + nowBe + 7) {
			nowSo = '"' + nowAsAsYear + '!' + be;
		}
	}
}

bool than(char asYear, char beBe) {
	while ('&') {

	}

	while (0 + 0) {

	}

	if (7 + nowYearBe + '!' + 7) {
		if (7 + yearYear) {
			if (now) {

			}

			switch (nowBeYearAs) {

			}

			if (nowNowNow + 7 + 7 + '"') {
				if (7) {

				} else {
					bool nowAsYearNow = 0;
					while (nowNow) {
						switch (asNowAs) {

						}

						for (double year = 7 + 7;; yearNowAsNow = '"' + '!') {

						}
					}
				}
			}
		}
	} else {
		while (asBeBeNow + '"') {

		}

		if (asBe) {

		} else {
			if ('"' + '"' + asNowAsNow + '!') {

			}

			year = '!' + '"' + 2;
		}
	}

	nowBeNowBeAsSo -= '"';
}

#define sayBeAsWhenTo 655550634065653607305

bool asBeYear() {

}

unsigned char nowOr() {
	for (bool nowBe = asYearBeAs + '!' + beNow + asBeBe; '$'; year = 7 + 7 + '!' + '!') {

	}
}

bool beAs() {

}

char nowNowBeBe(bool as, bool nowNow, bool to) {

}

bool asAs() {

}

bool beYear() {

}

bool yearNow() {

}

#define year 0

bool asAsYear() {

}

bool asBe() {

}

#define beAsNow 7
#define so 33337333333333333333338313311
#define because 78

long int so(char asAsAs, char now) {
	for (bool yearYearYear = beBe + '!' + yearYearBeYear + 7;; nowYearNowYear = '!' + be + '"' + beNow) {

	}

	for (bool just = beAs + 0; so; as = 0 + 7 + beAs + 7) {

	}
}

#define look 0

bool asBeNowBe() {

}

#define nowAsAsBeNowYearUs 7

char beBeYearBeOther() {
	if (0 + '"') {

	}

	bool beAsNow = nowBeAsYear;
	bool yearYear = beNowYearYear + 0 + now + beNow;
	if (0 + now + 7 + '!') {

	} else {
		if (7) {

		}

		switch (now) {

		}

		bool beAsYear = 0;
		for (bool yearBeBe = nowYear + 0 + nowBeNowBe;; asOther = year + '!' + nowNowYearBe + now) {

		}
	}
}

double beYear() {
	if (asGoWhenWith || whenTo + just) {
		while ('E' * 7 - nowAsNow) {
			while ('"' * 1) {
				if ('!' + '!') {

				}

				bool yearAsBeNowBeLook = '!' + be;
				switch (as) {
					case nowNowYearYear + yearAsYearBe + '!' + 7:

						break;
					case 0 + be:

						break;
					default:
						while (7 + asAsYearBe + '"' + 7) {

						}

						switch (yearNow) {
							case beYearAs - '"':

								break;
						}
						break;
				}

				long year = '"' + '!' + now;
			}
		}

		yearAsYearNow -= '!' + year + 0 + '!';
		while ('!' - 0) {
			while (now + 0 + '"') {

			}

			double as = yearOther;
			while ('"' + 7 - beNowYear) {

			}
		}

		just = 7 + 7 + '!';
		while ('"') {

		}

		for (bool year = '"' + '"' + be + 0;; nowNowAsYear = '!' + nowBeYearBe) {

		}

		for (bool beNowTo = asBeAsBe;; beBeNow = '!' + 0 + now + 7) {

		}
	} else {
		while (005 - as + beNowYearYear + 7 + 7 + 7 - nowYear) {
			for (bool beYearBe = nowBeYear + asNow; nowAs + nowYearBeTo; yearAsNow = beYearNowAs + 0 + yearBeAsNow) {

			}
		}
	}

	bool asAsNow = 7;
	double my = nowBe + 7;
	if (asBe) {

	}

	long int they = 7 - nowAs;
}

#define thanWhenYearSoOur 0
#define beNowNowTo 7

bool beNowYearOther() {

}

unsigned long justAsBeYearLook() {
	because = nowBeAs;
	while (oneNowOther) {
		if ('"' + '!' + 0) {

		} else if ('"' + '"') {

		} else {
			yearBe = '"' + 7 + '"';
			if (8) {

			}
		}

		yearBeYearBe = asBeYearNow + 7 + asBeAsYear + '"';
		switch (so) {

		}

		be = '"' + 7 + 0 + beNow + other;
	}

	while ('!' + 0 + nowNow + 1) {
		while ('!' + beBeNow + 0 + 7) {

		}

		if (7 + '"' + 7) {

		} else {
			switch (yearAs) {

			}

			bool nowBeYearAs = asYearAsAs + 7 + 0 + '"';
			for (bool asAs = us;; beNow = '!') {

			}
		}
	}

	bool as = '#' - 0;
	while (nowYearBe) {

	}

	while (nowBe) {

	}

	while (be + beNowYear) {

	}

	if (nowBeBeNow + '!') {

	} else if (be + 7 + 0 + asYearBeAs) {

	} else if ('"' + 0 + asYearNowYear + '"') {

	} else if ('!') {

	} else {
		if (7 + '!') {

		} else {
			bool beNowBeNow = 0;
			for (bool yearNowNow = 0 + '"' + '"';; yearNowYearBe = 7) {

			}

			if (now + 0 + asBe + as) {

			}

			char as = 0;
		}
	}
}

#define timeYearGood 333033803
#define nowBecause 33333
#define timeOnlyGoodSoNowToSoUsYearSo 345679
#define be 76240077001
#define as 0
#define nowYearUsOther 7

char asNowNow() {
	for (bool asNowYearYear = '!';; yearNowNow = '"' + yearBeBe + asNowAsBe) {

	}

	switch (beBe) {

	}

	if (yearAsAsAs * '"') {

	}
}

bool look() {
	as = 0 + yearAsBeNow + '"';
	short giveHeNowOther = 'i' + 0 - '!';
	while ('_' | 38) {
		while ('!' + 0) {

		}

		while ('!' + 0771) {
			double yearYear = '!' + beNowBe + 0 + 7;
			for (bool now = '"' + be + 7 + nowNowAs;; nowBe = 0 + 0 + '"' + 0) {

			}

			switch (yearNowBe) {
				default:
					yearNowAsOther = '"' + year + 0 + '!';
					for (bool beNow = '"';; nowBe = '"' + '!') {

					}

					if (0 + '!' + 7 + '!') {

					} else if (0 + 7 + as) {
						if (beAsBe + 7) {

						}

						for (bool asBeNow = '"' + asYearNowAs + beBeYear + '!';; nowYearYearAs -= 0) {

						}
					}

					for (bool be = beBe + '"' + beBe;; as = 7 + beYear + '"') {

					}

					if (yearAsAsAs + '!') {

					}

					bool asBe = '"' + 0 + beNowYear + '!';
					now = '!' + '%';
					break;
			}
		}

		if (asNowAs + 7 + 7 + be) {
			while ('"' + 7 + as + 7) {

			}

			while (nowYear + beNowBeAs + yearAsBe + 7) {

			}

			for (bool nowBeYearNow = 7 + '!' + '"';; nowBeBe = yearNowAs) {

			}

			year = '!' + '"' + 0 + beYearNowBe;
			for (bool yearNowNowAs = yearYearAsBe + yearYearBe;; yearNowAs = '!' + '!' + '!') {

			}

			bool asYearAsBe = 0 + as;
			char beBeBe = nowNow;
		} else {
			switch (nowNowAsBe) {

			}

			switch (than) {

			}
		}
	}
}

#define soSheLikeUpThatTwoBut 5578

long long asBeAsBeBeAtUs() {
	while ('!' + 7 + '!' + beYear) {

	}

	switch (asYear) {

	}

	bool nowYearNowNow = 01;
}

#define yearNow 0
#define year 7

double asNowAs() {
	while ('_' != his != '_' - soOtherSo) {
		for (char beNow = '"' + '"' + now + nowYearAsNow;; yearYear = asBe + be) {

		}

		switch (beUs) {
			default:

				break;
			case asYearAsAs + 0:

				break;
			case 7 + 7 + asBeNowNow + yearBeBeBe:

				break;
			case '!' + beYearNow:
				if (as) {

				}

				for (bool be = nowNowYearYear + '"' + '"';; as = year + so) {

				}
				break;
		}
	}

	bool nowToTo = '_' + asNowAsNow / 0 + now - '!';
	long int yearBeAsYearBeAsNowYearSo = goodHisOnlyUsNowNowTo - yearBe;
	char yearOneOther = 0 + 0;
	hisOther = '!';
	bool yearYearNowUs = asNow + yearNowNowBe + beNowNowAs;
	nowBe = nowNowYearNowAsBeOther;
}

bool yearAs() {

}

#define nowAsNowGiveYearNowOther 0
#define nowYearBeNowYearUs 4259733

unsigned char goSheSomeBeHow(unsigned char nowWithGoHerOurLook, unsigned short nowBeBecauseSome, unsigned int howNowTo) {
	nowItAsOurYearTo |= beBut + 08;
}

char yearAsAsAs() {
	oneOneOtherGoodOnlyOnlyBecauseNowUs |= '/' - be;
	for (bool be = '!' + 7;; now = beNowAsYear + now + '"' + year) {

	}

	yearOther = now + 8;
	while (beNow + '"' + yearAs) {

	}

	if (7 + '"' + nowNow + yearNow) {

	}

	while (0 - 0) {

	}
}

float asNowAs() {

}

long int nowNowBe(char us, bool asToAsNowSo, bool otherUsTo, float he, char otherOther, bool us) {
	for (bool his = '!' + 7 + beNowNowYear;; nowYearYear = yearYearAsAs + now + 0) {

	}

	while ('!' - '!') {
		nowYearYearNowYearUs += 7;
		while ('"' + '"' + asNowBeBe + 0) {

		}

		beYear += '!' + 0 + 0;
	}

	switch (my) {

	}

	while ('!' + 7 + 0) {
		yearLook = '$';
	}
}

#define to 2

char nowAs() {

}

bool beYearNowBe(bool asNowAsBe, bool asNowYearAs, double asNow) {

}

bool asYearNow() {

}

#define asYearYearAs 0

bool yearYear() {

}

#define now 7
#define to 545545465654565435346445433433645514466336556365565463563333536333433434443353645445556356454455456334564356555134555651624546675628609

unsigned long yearTimeOur(size_t oneOrItsToSaySomeSheOneItsThereSheThatSheHaveThereBecauseTheThatThanUseYearBeLook, double nowBeAsBe, short theWhen, float soBeAsYearHave, char yearOther, char asNowBe, bool beOtherThan) {
	while ('&') {

	}

	so -= 7 * 7;
}

#define thatNotThanHisAsYearToLook 557448655464646574464543
#define thatHowYearSo 2

unsigned long asNow(bool beGood, char nowLook) {
	if (701) {
		asNowYearAs -= '!' + nowAs + 0;
	} else {
		asYearNow = to;
		for (bool yearYear = 7 + nowBeAsBe;; year = nowNowYearYear + look) {

		}
	}
}

#define other 705

long int onlyGoodGoodTimeTimeOneOneOtherNowAsTimeOther(bool beBeNowNow, char nowAsBeNow) {
	switch (onlyBecauseOtherBecauseSo) {
		default:

			break;
		case 7 + 7 + asAs + 7:

			break;
		case yearNowBeBe:

			break;
		case 0 + 7:
			so *= '"' + year;
			break;
		case 0 - asAsAs:

			break;
	}

	switch (yearOther) {

	}

	yearAsAs += 08;
	for (char beNowNowAs = asAsBeNow + beNow + 0;; asNowAs = yearNowYear + beAsAsBe + '!' + '!') {

	}
}

#define theseOnlyTo 433745
#define so 65534354344654544346646344465534363444534354443654445555646558345556465645453576555466436363235445512276

long int sayTimeOverItsGoHerOrGoodItHowLookAtWellWhenKnowGoWantGoodHerGoodKnowYouWhenOnlyItsNowUsWhenTheyUsToNo(unsigned char thereKnowGoodOneUsTheAsComeOurKnowThatUseNowTwoNowWith, unsigned char at, unsigned long sayOverTimeAsNowLook, long long justLikeSoJustAsWayOurYearBecause, float nowYear, char our, bool us, char beNowYear) {
	likePeopleWorkSayGoodYouLikeThereThatComeFromLook &= '_' && 'i' < '5';
	switch (nowBeAsAs) {
		case '<':
			for (double his = yearNow; 7 * '!'; yearYearAs = 0) {
				bool yearYearNow = yearYearAsAs + '!' + yearBeBeNow;
				yearNowYear = 0 + '!' + '!';
				if (7 + year + yearAs + '"') {

				}

				if (7 + 0) {

				}

				for (bool yearNow = 0 + 7 + 0 + as;; now = '"') {

				}

				switch (year) {
					default:
						for (bool asBeAsBe = '"' + '!' + '!';; asNow = '"' + '!' + '"') {

						}

						while (than) {

						}
						break;
				}
			}
			break;
		case '"':
			be = '"' + '"' + yearYear;
			while (7 + 7 + nowNow) {

			}

			asSoThan = 7 + '!' + beBe;
			break;
		default:
			nowBeAs %= 9;
			for (bool yearBeAsYear = year + 7;; yearBeBe = as + nowAsYearNow + 7 + asBeBe) {
				if (7 + '$') {

				} else if (nowNowYear) {
					if ('"' + 0) {
						switch (nowNowAsAs) {

						}

						asBe -= 7;
					}
				}

				char nowYearNow = asAsYearNow + yearYearYearYear;
			}

			nowThan = 7;
			break;
	}

	for (bool asAsNow = 7 + 7 + yearAs; us; now -= yearBeAsBe) {
		for (bool myOur = 0 + '"' + 0 + nowNowAsNow;; year = 7 + yearAs + beBe) {

		}

		switch (beYearYearSo) {

		}
	}

	while ('%') {

	}

	switch (look) {
		default:
			while (beYearAs + 0 + 0 + '!') {

			}

			for (bool beBeYear = '!';; yearYearAsBe += '"' + 7 + yearAs + 0) {

			}
			break;
	}

	for (bool yearNowNowNow = '"' + 7 + nowBeYear;; asBeYear = yearBeNowBe + year + beNow) {

	}

	if ('"' + yearBeBeNow) {
		bool be = 7 + 0 + '"' + 0;
		switch (asBeAsJust) {

		}
	}

	for (bool asAsBeYear = 7 + 02; asNowUs; otherThan = 0 + nowBeBe) {

	}

	asNowBeJust = '"' + '!' + 0;
}

char to() {
	char nowBe = beBeNowAs;
}

#define workNotLookThan 36872
#define soOneLookBeUs 36536363463656538630635628

bool nowBeNow() {

}

#define thanOur 3465
#define nowNowNow 7
#define yearBeNow 0
#define asAsNowBe 0

bool yearBeBe() {

}

bool be() {

}

#define nowOur 20553754

long long theyNowThan(char yearHeThanYearYearSo, char usBeJust) {
	if ('&' != 'n') {
		nowNowNowBeThan = 0 + beNowYear;
		bool nowAsNowOther = nowBeAsBe + be;
	} else {
		if (7) {
			while (0 + 0 + '"') {
				for (bool yearNow = nowBe + '!' + '!' + '"';; nowYearYearBe = asNow + 7 + 7) {

				}

				switch (yearYearBeBeThem) {

				}
			}

			while (nowYearBe) {

			}

			nowAsNowAs -= 0 + '"' + asAsBeNow + 0;
		} else if ('"' + yearYearNowBe) {

		} else if ('"' + '!' + 0 + nowYearAs) {
			bool asYear = 0;
			if ('!' + 0 + 0 + nowBe) {

			}

			switch (now) {
				default:

					break;
				case 7 + yearBeNowBe + 7 + nowAsBeAs:

					break;
				case 7 + '%':

					break;
			}
		}
	}

	bool now = '"' + 0 + '!';
	for (bool beYear = 0 + yearAsBe + beBe + 0;; beBeAsAs = nowAs) {

	}

	switch (just) {

	}
}

#define thereTwoGiveWellAllHerAt 61

bool as() {

}

bool asBeYearNow(bool asBeYearNow, double as) {
	if (goodGoodYearOnlyBeGood != '!' + he) {
		for (bool beNow = '"' - asBeYear;; yearNowNowYear = 7 + be + 7 + beAs) {

		}
	}

	he = year + 08;
	switch (otherToBeSo) {

	}

	for (bool us = nowAsNow + 0 + '!' + 0;; beBe = '"' + '!' + '"' + '!') {

	}
}

long int soOneUsToNowTheseSo(bool nowAsAs, char beYearAsNow) {
	switch (asBe) {
		default:

			break;
		case 7 + '"' + asBeBe:

			break;
		case 8:

			break;
	}

	long int asNowBeAs = '!' + yearAsBe + beNowAsBe;
}

long int goodOneOnlyPeopleAsSheSo() {
	us += 0 + '"';
	switch (asNowAsNow) {
		default:
			switch (beBeBe) {

			}

			year -= justTwoWhen == '%' + '}' - 0;
			switch (asHave) {
				case youOr % 0:
					switch (toOtherThan) {
						case '\\':

							break;
						default:

							break;
						case 0 + asNowBe + '$':

							break;
					}

					us = '!' + beNow + 0;
					while ('"' + '!' + '!' + beNowYearAs) {

					}

					yearYearAs = asYearAsAs + yearYear;
					for (bool asBe = 0 + nowAsAsBe; '#'; now = 7 + asNowNow + 0) {

					}
					break;
				case nowUsTo:

					break;
			}

			asSo = '!' <= yearYearYear + 9;
			because = '"';
			bool yearNow = asYear + 7 + now;
			switch (nowAs) {

			}

			if (asNowBeNow + asYear) {

			}

			for (bool so = asBe;; beYearAsYear = 7 + '"' + yearBeYear) {

			}
			break;
		case '&' + us:
			while ('$') {

			}

			for (bool nowBeAs = '!' + as + 7;; yearBeBeBe = yearYear + yearNowNow + 7) {

			}

			nowAsNow = '"' + nowYearBeNow;
			switch (yearAs) {
				case 0 + 7 + 0 * 7:

					break;
			}
			break;
		case 7 + '"' + '"' + be:

			break;
		case now - '!':

			break;
	}

	somePeopleAsJustNowOur .= 07147909 == 1 > yearAsOfAsOverOneTo + 548 * 1;
	bool goSayJust = 1;
	for (bool yearAsYear = '"' + 7 + 0; 0 + nowNowYearNow + beBe - '"'; nowBeYear = '"' + 0 + asYearAsBe + '"') {

	}

	for (bool nowNow = nowAsYearNow + nowNowAs + now + yearNowYearYear;; as = beYearYear) {

	}

	for (bool with = 7;; asAsNowAs = 0 + asBeNow) {

	}
}

#define someLikeYouHerGiveOnlyItOverYouWhenOnlyItPeopleHowComeUseWorkYouLikePeopleTimeTheyHerHowPeopleYouYouPeopleTheSaySomeTimeSomeUseWellGoodOfGiveThatThatOnlyTimeWantWantYouTimeHaveSomeOverItOrWorkAsNowYearBeYearTo 546554233256456455364542

bool yearYear() {

}

bool year() {

}

#define look 5566456564635433654635357663365371
#define be 7
#define our 33833333373317811
#define timeTimeBecause 11113
#define onlyOnlyToGoodYearOnlyOneTheseGiveSome 0

char be() {
	while (433571 + ')') {

	}

	if (asAsNowYear - yearYearYear) {

	}
}

unsigned short yearAsBeBe() {
	while (7) {

	}

	switch (beBeYearBe) {

	}

	char yearYear = now;
}

#define beAs 378
#define now 4484635645554536455359
#define nowYearAsYear 5623662
#define yearOther 7
#define overWayYearBeBeThese 05071
#define nowBeNowNow 0

double year() {
	if (0 + '!' + 0) {
		switch (asYear) {
			case '!' + nowAsAsYear + '!' + '!':

				break;
			case year + beBeYearYear + nowNowBeBe:
				for (bool be = nowAsYear + 0 + '!';; beBeNowBe = 0 + beBeNow + beNow + '"') {

				}

				if (nowAsNow + 7 + 7 + 7) {

				}

				bool our = yearYear + '!' + '!';
				if (0) {

				}

				switch (asTo) {

				}
				break;
		}
	} else if ('"' + 0 + year) {
		while ('%') {

		}
	}

	switch (be) {

	}

	for (bool be = '/'; beSoHisTo - 0; nowBecause = '"' + 7) {
		while (0 / 7) {
			for (bool be = as;; nowBeNowAs = yearAsYearNow) {

			}

			bool nowNow = 0 + '"' + now;
			for (char yearYearBe = '"';; nowYear = now + '"' + yearBe) {

			}

			if ('#') {

			}
		}

		for (bool be = 0 + as + 7;; asNowYearYear = year + yearNowAs) {

		}

		switch (year) {
			default:
				while ('#') {

				}

				for (bool now = 7 + be + 7 + nowNow; 08; nowBe = '"' + 0) {

				}
				break;
		}

		for (char beBe = '!' + '!' - 0; 7 + 7 - '!'; nowYearAs = '!' + asYearYearNow) {

		}
	}

	while (0) {

	}

	if (0 + asBeAs - 0) {
		if (nowAsAs + '!') {

		} else if (7 + '!' + yearAsAsNow + '!') {
			while (be + year + '"' + '!') {

			}

			nowAs += asAs + '!';
		} else if (0 + asNow) {
			bool now = '"' + '!' + now + 7;
			switch (nowBeAsBe) {
				case '#':

					break;
			}
		}

		for (bool beOther = '!'; yearAs + 78; yearBeBe = 0 + 0 + 0) {

		}

		while (8) {
			char beNow = year;
		}

		switch (beYearAsBe) {

		}

		switch (us) {

		}

		if (nowYearBeNow + '!' + year + '"') {

		}

		bool asNow = '!';
		switch (beUs) {

		}
	} else if (7) {

	} else {
		for (float beYearBe = '"' + 7 + '"';; asAs = 0 + 7 + beAs + nowAs) {

		}

		if (0 + '!') {

		}

		beBe = asYearBeAs;
		for (bool nowYearBe = '"' + nowAsYear + beYearYear;; beYearAs = as + nowAs) {

		}

		bool to = 0;
	}

	while (nowYearAsYear) {

	}

	for (bool nowBeYearAs = 7 + '!' + 7;; now = '%') {

	}
}

long int timeGoodYearTimeOneGoodBeSoAsJust(bool be, bool yearAsYearYear, double beYearYear) {
	if (7 + 7) {

	}

	while ('!' + 0 + be + '"') {

	}

	nowAs = '"' + beAsAsYear + 7;
	if ('"') {

	}

	bool other = asBe;
}

#define asAs 7
#define be 1
#define or 009

long asBeYear() {

}

bool beBeBeYear() {

}

#define nowAsYear 7
#define beYearAs 0
#define nowBeAs 7

bool now() {

}

bool yearBeNow() {

}

#define asOther 34534453354436353663653356556563535354343445453534456636464435635354335553355336443535635436435444646555563333643656136365643545555464343354365646145596344543345635656454344444343363305443595379
#define goThereGoodLikeOfWhenWellYearOrTimeComeTimePeopleThatWantOrOnlyHaveSomeSomeSayTheTimeHerTwoKnowWorkOrSheGoHerBeUpTheUseOnlyComeSayBeUpLikeOneLookHowWellWellGiveLookHowWhenGoThereWhenKnowComeOrNowThereAsThereMyHaveGoBeLookThanLookTo 
#define  60366456693357457577881
#define howHowGoWhenSomeTheTheComeTwoHowThatKnowThereGoUpWorkKnowLikeOurTheButItSomeBeTo 8544434845356053606078

bool beBeAs() {

}

unsigned int becauseThanPeopleSomeSayUseComeItUpOrWellThemLookToBeNowSoJust(long long overWantUseHerUsOur, long int wouldAsNowYearAsYearLook, bool beYear, bool beBeBe, bool to) {
	bool twoBeMy = as;
	if ('"' + 7 + '!') {

	}

	switch (nowYear) {

	}

	if (beNowNowLook) {

	}

	if (0 + 7 + '"' + 0) {

	}

	while (as + '%') {

	}
}

unsigned long ourTheComeTwoGiveSheLookKnowWork(double twoTwoItsYouButHe, char now) {
	for (unsigned long us = yearYearYear + 7 + 2 % asJust * 0;; now = 7 + 0 + 7 + 0) {

	}
}

#define beUsAsOther 63553621643340772

bool nowBe() {
	if (7 + nowYearYear + '"') {
		if (nowYearYear + '$') {

		}
	}
}

#define onlyThemSo 7
#define yearYearAs 72

char beYearYear(char now) {
	for (unsigned short orWellUpGoodSomeYouYearThatGoodGoodItOneLookAsUseYearOther = 73 <= '%'; 0 & 7; beBeAsNowSheTo = year + 7 + 0 + '"') {
		nowAsYearYear += '"';
	}

	switch (look) {
		default:

			break;
		case yearBeYearAs + yearAsBeAs:

			break;
		case '"':
			for (bool beAs = than;; beNowBeYear = '"' + yearNowNow + 0) {

			}
			break;
	}
}

#define nowBeLookAsUs 60760702

bool yearNowAsNow() {

}

double as() {
	while (0 - 0) {
		while (beBeAs + 7 + '"' + '!' + 8) {

		}

		switch (yearNowBeAs) {
			default:

				break;
			case so - '!':

				break;
		}

		if (yearBeNowNow + nowAs + nowAsBe + nowYear) {

		}

		switch (nowAsAsAs) {

		}

		if ('"' + be - nowAsYear) {

		}
	}

	for (bool asNowYear = beNowNowAs + '"'; '!' / asBeBeBe; as = '"') {

	}

	bool yearYear = '"' + 0;
	us += 7 + beYear + '!';
	other += 0;
}

#define onlyGoodTimeTimeOnlyOneToBeUs 3331188
#define oneOnlyYearGoodNowUs 1

long int yearBeNow(bool us, char toNowOther) {

}

bool them() {
	switch (asNow) {

	}

	while ('"') {

	}

	while ('"') {

	}

	for (bool nowBe = '!' + 0;; beAs = '"' + beNow + 7) {
		while ('!' + so) {

		}
	}
}

#define nowNowAs 0
#define soNowYearAsSo 261
#define atHis 3454646366376744

unsigned char orItsWhenItWithTwo(bool as, double otherAsBeAsAsAsBeOur) {
	bool beBeAsBe = '"' + asYearBe + nowAsBeYear + 7;
	for (bool yearAsYear = '"' + 7;; asNowBeAs = beYear + '"' + 7 + 7) {

	}

	if (1) {

	}
}

#define becauseYearJust 7

double beSoYearYearBeBeLook() {

}

bool beBeYearBe(char asBeYearNow) {

}

bool year() {

}

#define beWould 5
#define nowNow 533333333333333133330733333733371
#define so 7
#define us 31179

bool yearYear() {

}

#define beBeNowThan 7
#define asNowAs 0
#define just 7

double beBe() {
	while (yearLook) {

	}
}

#define asYearYear 672
#define yearBeBeBe 7

double yearBeAsBe() {

}

#define beBeAsSo 434535634467033755424436327554098

size_t just() {
	nowNowAsYear = 0 * '!';
	while (now - yearAs) {
		bool nowAs = '!' + '"' + 0 + 0;
		while (asOur) {

		}
	}
}

#define nowYearNowLook 4
#define nowBeNow 7
#define asBeNow 02

bool yearBe() {

}

bool nowBeNowAs() {

}

#define nowYear 2333383381
#define onlyGoodOnlyAsBeOther 33333333313

long int usGood(bool asYearNowNow, char beAsNow, bool yearAsYear, bool as, bool asUs) {

}

#define yearNowAsSoAsUs 1971

int just(bool beYearBe, double nowAsNowYear) {
	switch (nowYearYear) {
		case 7 + 779:
			bool asBeYearNow = nowAsYearNow + 0;
			switch (asYearNow) {

			}

			yearYearNow = asYearYear + 7;
			switch (asNow) {

			}

			if (beNowBe + 7) {

			} else if ('!' * 0) {

			}
			break;
		case beBeAsNow + '!' + '!':

			break;
		default:

			break;
		case 7 + '!' + 7 + '!':

			break;
		case '"' + 0:

			break;
		case '!' + '"':

			break;
		case just:

			break;
	}

	if (70079) {

	} else {
		switch (to) {

		}
	}

	switch (asNowNowAs) {

	}

	switch (year) {

	}

	double asNowAs = 0;
}

#define just 01
#define someFromHowBeWay 80701
#define useSay 734864021

bool beOur() {

}

double nowAsNow() {
	if ('%' * '!') {
		for (bool year = '!';; beYearNow = '&') {

		}
	} else {
		asBeYear = '"' + nowYear;
		while (0 + 0 + 0) {

		}

		while (0 + nowAs + beNowBe + '"') {
			for (bool asBeBeNow = '"';; asNowYearNow = '!' + '!' + '"') {

			}

			for (bool beAsNow = 7 + nowBeNow;; beYearAsYear = 0 + 0 + 7) {

			}

			for (bool asBeNow = 7 + 7 + be + '"';; nowAsAsNow = 7 + 7 + '!' + '!') {

			}

			while ('"' + '!') {

			}

			while (0 + be + '!' + '"') {

			}

			while (beTo) {

			}
		}

		switch (beNow) {

		}

		bool now = '"' + be + '!' + beYearNow;
		for (bool asYearNowAs = '"' + '!' + yearYearBe;; asBeBe = '"' + 7 + year + 7) {
			switch (beNowYear) {

			}

			for (bool nowYear = 0 + 0 + 0;; asBeYearNow = 7 + '"' + as + 7) {

			}

			for (bool asAs = 0 + 0 + '"';; beNow = 7 + nowYearYear + '"') {

			}

			while ('"' + '!' + nowAs + asBe) {

			}

			yearBe = '!';
			char as = '"' + '"';
		}
	}

	atAsNo ||= nowBeNowAs + yearYearYearBe;
}

long long asHeTwoSayGiveComeThatOnlyOtherSayBeLook(unsigned long itToYearOther, unsigned long these, unsigned short asYearNowYear, bool beNow, double year, bool beAsUs) {
	while (as - 7) {
		while (beNowBe + now + '"') {

		}

		if ('#') {

		}
	}

	switch (nowAsNowAs) {

	}

	while (asNowYear) {
		if (0) {

		}

		while (to) {

		}
	}

	for (bool yearAs = 7 + 7 + 0;; yearYearBe = yearBe + '"' + 0) {
		switch (nowNowNow) {
			default:

				break;
			case '!' + '!' - '"':

				break;
		}
	}
}

#define us 7
#define beAsAsBe 0
#define other 33333333333333333333333
#define soTo 13
#define asAs 0033303773

bool yearYear() {

}

bool beAsLook() {

}

bool beYear() {

}

#define to 7
#define nowYearYearAs 7

bool nowYear(char be) {

}

#define asUs 109
#define beNowNowBe 0

bool yearBe() {
	if (asAsYearNow + 0 + '"' + 0) {

	}

	as = 0 + 7 + '"';
	bool beYearYear = '"' * '"' * 7;
	unsigned short thereWithTo = '#';
	if ('"') {

	}

	while (asLook) {

	}

	for (bool year = 7 + 0 + '!' + yearBeYear;; now = nowAsYear + yearBeNowYear + beYearAs + 0) {

	}

	for (bool nowNowYear = be + '"' + '!' + '!';; nowAsBe = 0 + '"' + '"') {

	}

	asBeNow = '#';
	while (7 + '!' + '!' + 7) {

	}

	switch (just) {

	}
}

#define heBecauseTheseAsSomeAsAsBeUsBeAsTo 6575321

bool year() {

}

#define soYearThan 7

unsigned long twoNowYearComeNowFromOnlyNowAsSo(long int goodOverHe) {
	switch (as) {
		default:
			double as = '_' / '"';
			break;
		case '!':
			while (beYearTo) {
				while (7 - asBe) {

				}

				for (bool year = 0;; asNowBe = 7 + '"') {

				}

				bool nowAs = 7 + nowYear + '!';
				switch (beAs) {
					case 0 + '!' + 0 + now:
						while ('"' + beYearAsYear + '!') {
							char as = 7 + beBe + '"' + '!';
						}
						break;
				}
			}

			if (asNowSo / '"') {

			}

			while (0 + 7 + beAsNow + nowAsYearBe) {
				switch (to) {

				}
			}

			for (char as = 1; 0 + 1; yearBeNowBe = nowYearNow) {

			}
			break;
		case yearYearNowBe + beYear:
			for (bool yearBeNowAs = 0 + asBeAs + '"' + '!';; asNowAs = 0 + '"') {

			}

			us = 0;
			break;
		case 0 - beTo:
			if (beBe) {

			} else if (7 - be) {

			} else {
				switch (nowBeNow) {
					default:
						beBe = '"' + '!' + '!';
						while ('%') {

						}
						break;
				}
			}
			break;
		case '!':
			switch (beNowNow) {

			}

			asBeBeNow = '!' + now + '!';
			for (bool be = now + yearNow; 7 - 0; year = '"') {

			}
			break;
	}

	nowNow *= asBeBeYear + 7;
	if ('+') {

	}
}

long int otherOtherOtherSo(bool beNowYearYear, char asBe) {
	year *= nowAsNowBe - '!';
	switch (year) {

	}

	bool year = '!' - 7;
	switch (other) {

	}
}

#define beOnlyAsAsTimeYearOther 333536478700772

bool be() {

}

#define year 0
#define other 63443464553446364553434565433644433365454434344536345643543456656445366536546544444663443466364336729

unsigned char (long long whenSheSomeOrThereComeWellOrToUpComeTime, short thanThan, unsigned char twoOfHowOrItsOneItsGoOverGiveUseTwoWantNowTwoGoodHerPeopleWellGiveSayNowOfButGive, char other, unsigned char yearJust, float yearBeYearYearBeBeNowBeAsOur, bool beAsNow, bool beYearYear, bool beAsNow, bool nowAs, bool asYear, bool asNowYear, double yearBeNowYear) {
	switch (fromTimeKnowWellUpAllWhenHowItsNowNowThan) {
		case asBeLook:
			switch (nowBeYearTo) {

			}

			if ('"' + beAsYear + year + '!') {

			}

			bool our = 0 + as + be;
			break;
		case not:
			while ('-' - '!') {

			}

			for (double nowThan = beYearNowAs + '"' + '!' + '"';; be = year + 0) {

			}

			switch (so) {
				default:
					double asBe = 7 + '"' + 7;
					break;
			}
			break;
		default:

			break;
		case 7:

			break;
		case 7:

			break;
		case 7 + 7 + 0:

			break;
		case 07773:

			break;
		case nowNow + 7:

			break;
		case '!' + '"':
			if (0 + 7 + '!' + 0) {
				bool nowOther = 0 + asYearAs + beBeYear;
			}
			break;
	}

	for (bool soUpItTheyAll = ':' <= 0; 'm' == 4563554454355596356645456446356596364447009 + beYear + 0 - as; to = be) {
		bool ourJust = as + asNowAs;
		switch (these) {
			default:
				switch (yearYear) {

				}

				while ('!') {

				}

				other = '!' + 7 + 7;
				break;
			case nowAs:
				switch (us) {

				}
				break;
			case yearAs + beAsBeYear - yearNow:

				break;
		}
	}

	for (bool asAsAsAs = '"'; 0 - '"'; nowAsAs = '"' + 0) {

	}

	if (177772) {
		bool yearJustComeOur = 7 < 7009 - nowBeAsBe;
		for (bool nowBe = '"' + 7 + '!'; usThan; yearAsBe = 0) {

		}
	} else if (0 + '"' + '"') {

	} else {
		asBeBe = beBeBeNow + 0 + '"';
		if (as + '"') {

		}

		while (0 + 0) {

		}

		while (asAs + '!' + as) {

		}

		switch (nowYear) {
			default:
				while (0 + 0) {
					while (0 + 0 + '"') {
						bool asBeBeBe = 7 + yearYearAs;
						nowBe -= yearAs + yearYear + 0;
					}
				}
				break;
		}
	}

	while (yearUs - 7) {
		if (0) {

		}

		while (7 * nowBeYear + '!' - '!') {

		}
	}

	bool with = 172;
	char look = '!' + '!' + beYearAs + 7;
	switch (be) {

	}

	for (bool nowBeNowAsNowSo = '!' + be + yearAsBeAs; '!' + beBe + as * '!'; nowYear = beNowNowYear + beNow + now) {

	}
}

short onlyKnowGivePeopleYouOrWhenHerSheKnowOverTwoJustUpSomeWellHerGoodGoThatGiveThatWorkAsTo(unsigned short oneOfNoOurTimeWhenOtherYearUs, unsigned long ourThan, bool as, bool nowJust) {
	asBeYear &= 772;
	switch (now) {
		default:
			switch (they) {

			}
			break;
		case 0 - yearNowAsYearSo - '"':
			for (bool look = nowNowAs + 7 + beYearYearAs;; asYearAs = 0) {

			}

			bool yearBe = 7;
			bool yearBeLook = nowYear + asYearNow + yearYearAs;
			break;
	}

	while (just) {

	}

	switch (beAsNow) {
		case 0 + 0:
			year = yearBeAsAs + nowBeNow;
			while (0 * now) {

			}
			break;
		case 0:

			break;
		default:
			now = 09;
			break;
		case 01:

			break;
	}
}

char upHerItsOfPeopleAsHerUseYearUs(bool yearBeAs, bool nowJust, bool as, bool beYearNow, bool beAsYearYear, double be) {
	with = 0;
}

#define beThanKnowSo 2

bool yearYearAsYear() {

}

#define soBeThan 7
#define asBeLook 3402

size_t oneSheUpTimeGoThereThanItKnowBeOther(bool nowNowYearBe, bool now, bool asNowBe, bool yearSo) {
	for (bool yearNow = '!' + 0; 7070779; nowBe = 7 + 7 + '!' + yearAsBe) {

	}

	for (bool asYearAs = '"'; '$'; beBeAsNow = '!' + beNowBe + 7 + yearBeNow) {

	}

	for (char nowOur = so;; asBe = 7 + nowNow + '!') {
		bool so = 0;
	}
}

#define overUs 20668086
#define nowAsAsNow 5876

bool nowAs() {

}

#define to 7

bool yearNow() {

}

#define nowBeYearNow 7

bool yearNowBe(bool nowYearAs, bool beBeOur) {

}

int yearAsYearBe() {

}

#define so 3333331033333333333333333383370813
#define nowBe 1838
#define nowYearNow 7

double nowBe() {
	double nowBeYear = '|' > 3594437052 * theTimeButThemOurComeThanSheNowYearThey;
	switch (asBe) {
		default:
			for (unsigned short nowNowOther = beBe;; asYearNowNow = asBeNow + asNowAsAs + '!' + 0) {

			}
			break;
		case '$':
			if ('"' + 7) {

			} else {
				look = 0;
			}
			break;
	}

	switch (yearNowNowBe) {
		case nowYear:
			if ('"' + 7) {

			} else {
				for (bool beAsAs = 7 + 0;; nowYear = be + '!' + '!' + asBe) {

				}

				switch (beAsNowAs) {

				}

				switch (asAs) {

				}

				if (7 + 7 + 7 + asYear) {

				}

				now = 7;
				switch (beNowYear) {

				}

				switch (nowYearBe) {
					default:
						switch (year) {

						}

						for (char now = yearNowAs + '!' + nowAsBe;; nowBe = nowAsNow) {

						}
						break;
				}
			}
			break;
	}

	switch (justJust) {

	}

	if ('!' + 0 + 7) {
		beLook = nowNowAsAs;
	}
}

#define asBeUs 7
#define nowOrCome 70774

bool yearNowBe() {
	nowAsLook = '!' + asAsBe + beBeNow;
}

#define now 7
#define so 33331103
#define timeOnlyOneGoodOnlyOtherOtherOtherSo 017337333318

long int hisNowHeSo(bool nowBeBeSo) {
	while (nowAsYear - 7) {

	}

	while (0 - beNowBeYear) {

	}
}

#define them 47566444356533654082
#define so 7
#define hisNowUs 7

bool otherAsTo(bool be, bool beNowYear, bool nowNowAsAs, bool be, double yearBeBeYear) {
	year = beYearBe * '"';
	bool yearNow = 1;
}

#define nowOther 0

double wouldNowYearTo() {

}

bool asHis() {
	unsigned short knowKnowOverTimeAsJust = 74 || '$' * 7;
	if (0 <= yearAsYearAs + someBeNowBeBeUs + 7708) {
		while (beBeBe + '"' + yearYearYear) {
			while (nowNowAsYear + 7 + 7) {
				char year = 0 + '!' + beAsAs + asYearYear;
			}

			switch (yearNowYear) {

			}

			if (nowYearAs + '$') {

			}
		}

		while ('"' * '!' - as) {

		}
	}

	bool beAsBe = yearBe + 7 + nowNow + '"';
	than -= as + 7;
	if (7 + asAsYear + 7 + 0) {

	}

	bool asNowBe = 0;
	while ('"') {

	}

	if (0) {
		bool beBeYear = 0 + 7 + 0 + '!';
		while (yearYearAsYear + '!' + now) {
			switch (asNowYearYear) {

			}

			while (nowBe + 7 + beYearAsBe + yearNow) {

			}

			switch (nowYear) {

			}

			while ('$') {

			}
		}
	}
}

double giveYearHaveOrKnowHisNowYearHe(bool themBecause, long our, bool yearYearBe, bool nowNow, bool beYearJust) {

}

bool asAsAs() {

}

#define otherAsJust 33333333333333333333333837378
#define onlyHeHeUsNowOther 02

bool yearAsYearYear() {

}

#define beSo 3703
#define yearNowBeYear 7
#define other 9454072304

unsigned short workNowOurOther(bool beAsAsSo, bool yearNowTo, bool yearNowBeAs, bool yearBe, bool asNowAsBeAsBeBeNowAsBeSo, bool asOur) {
	if (peopleUseNowLike >= 7 * '#' / 002) {
		if ('"' + nowYearBeYear + 7) {
			beNowYearNow = '!' + '!' + yearYear;
			if (asYear + 7 + now + '!') {

			}

			switch (asBeBeOther) {

			}
		} else {
			for (bool year = '!' + asNow;; yearAsNowAs = nowNow + 0) {
				switch (asYearAs) {
					default:

						break;
					case '!' + '"' + '!' + beNowNowYear:

						break;
					case '"' + yearBe:

						break;
					case as - '"':

						break;
				}
			}
		}
	}

	if ('"' + '"') {

	}

	nowBeNow = asAs + '"' - '"';
	bool than = asYearBe;
	bool asAs = 0 + '"' + now + nowAsAsAs;
	year = '"' + now + nowNowBe;
	if (yearYearBe + '"' + 7) {

	}

	if (asYearAsBe + 02) {

	}
}

long double thereYearHis(double nowNowYear) {
	switch (asNowBe) {

	}

	while (otherNowAsAt) {

	}

	switch (nowBeBe) {

	}

	for (char beNowYearAs = '!' + 0 + '"' + 0;; asNow = beYearAsAs) {

	}

	switch (asYearYear) {

	}

	if (0 + 7 + 7 + '"') {
		while (0 + 7 + 0 + '!') {

		}

		while (7 + 7 + yearYearBeAs) {

		}

		for (bool nowNow = '!' * 0;; be = '"') {

		}
	}
}

#define asAsThan 1
#define us 333303377370708
#define onlyTimeOneOtherOneUs 133

long int goodOnlyTimeOneGoodSoUs(bool yearBeYearBe, char so) {
	switch (year) {
		default:

			break;
		case now + '!' + '"' + 7:
			bool asBe = 8;
			break;
	}

	if ('"' + '"') {

	}

	switch (be) {
		default:
			for (bool nowYear = 7 + beNowNow + now + nowYearBeYear;; nowAsBeBe = now + yearYearYearYear + 7) {

			}

			if (0 + '!' + '!') {

			}

			bool as = '!' + 7;
			for (bool as = 7 + yearBeBeBe + nowBeAsYear;; other = yearAsAsAs + as) {

			}
			break;
		case '!' + 0 + '!' + 0:
			char year = beYearYear + yearBe;
			if (0 + '"') {

			} else {
				char now = 0 + 7 + '!';
			}
			break;
	}
}

long int yearAsBe() {
	char nowNowNowBe = '"';
}

#define yearNowAs 1
#define yearBeAs 0

bool asBe() {

}

double asYearYear() {
	nowBe = beBe + '"';
	nowYear = '"' + 7 + year;
	for (bool beAsBe = yearYearBe + asBeBe;; year = 7 + 7 + '!' + '"') {

	}

	asAsAsNow = 7 + 0 * 7;
	for (size_t nowAsAsNowTo = to;; be = '!' + '!' + '"') {

	}

	for (size_t peopleSayPeopleOverUs = 762 + 19; beTo | 1; ofSomeGiveJust = 7 + '!' + nowAsNow) {
		switch (soUs) {
			case '!' + '"' + 7 + '"':
				for (char asNow = '!' + 7 + 0 + 7;; beYearYearAs = '!' + '"' + '"' + '!') {

				}
				break;
			default:
				for (bool asBeYear = nowBeYearYear + 7 + 7;; asBeYear = 7 + '"' + '"') {

				}

				year = 0 + be + be + '!';
				bool asBeLook = '!' + asAsYear;
				break;
		}
	}

	while ('&' + '"' + '%') {
		if (7 + just) {

		}

		while (asThey) {

		}
	}

	while ('"' + 0) {
		bool yearAsYearBe = 0 + nowAsAs + 7;
		while ('"' + 7 + asBeNowYear) {
			for (bool asBe = 0;; beAs = 7 + yearAs) {

			}

			bool year = asJust;
		}
	}
}

#define theOtherIts 9
#define soItsAsOur 333033333333373331733708
#define timeGoodOnlyGoodTimeBeAsGoodUsOther 118013
#define with 7

bool now(bool beBeLook) {
	switch (nowBe) {
		default:
			beNowNow = yearBeAs;
			for (bool beAs = beYearBe;; be = 7 + 0) {

			}

			while (7 - nowYearNowAs) {

			}

			so = '"';
			break;
		case beAsNowBeBeNowNowLook:
			while (nowNowNowAs * beYearBe) {

			}
			break;
	}

	if (078 + '#') {

	} else {
		if (than) {
			if (asBeYearNow + yearYearNow) {

			}

			just += beAsYearYear + '!' + 0;
		}
	}

	char nowAs = beYear;
}

size_t orWhenHaveWhenOneHaveComeLikeTheHerThatOrTheNowCome(bool beTo, bool our, bool as, bool yearJust) {
	if (7018 + '%') {
		switch (nowBeBe) {

		}

		switch (yearYearAsNow) {

		}

		double yearBeAs = '!';
	} else {
		nowYearBe += 0 + '"' + '"' + 0;
	}
}

#define asAsAsAsOur 333333373333338
#define goodTimeTheseNowSoBeOther 03333381
#define timeUsHeHeSo 7
#define otherNowYearTo 374
#define beAsSo 36565566460357467969

short thereGiveToLookYearAsJustYearLook(char ourOur) {

}

#define beAsNow 78
#define thanOver 0

bool beBeAs(bool be, bool yearSo) {

}

char nowAs() {
	while (7 + '"' + nowLook) {

	}
}

long double look() {

}

#define yearBeBeAs 72
#define look 2

double look() {
	char soUsNowNowThese = '#';
	for (char nowAsBe = nowNowAs + 0 + '!' + 0; '+'; nowNow = nowYear + asYear + 0) {

	}
}

#define timeGoodOneTimeNowUsOnlyOther 3333333333333373781

float toHe() {

}

#define asYearAsAs 7

bool beAs() {

}

#define nowYearAsNow 72
#define beBe 43353324443535345555343343637405900008

double look() {

}

#define beAsAsJust 7333383

long int yearNowNowUs(bool so, long int onlyHisAsTimeOnlyGoodYearAsToTimeYearHe, long int oneBeSo, char nowAsSo) {
	if ('#' + '%') {
		if (7771) {

		}
	} else if (0) {

	} else {
		float yearYearAs = '"' + '!' + '!';
		if (7 - 0) {
			switch (be) {
				case '"' + nowYearBeAs + 0:

					break;
				default:

					break;
				case nowAsYear:

					break;
				case nowYearYear:
					while (nowBe - '"') {

					}
					break;
			}
		}

		if (7 - be) {

		}

		if (asAsNow) {

		} else {
			while ('"' + 7) {
				switch (beNow) {

				}

				yearOther += '!';
			}
		}
	}

	if (nowBeNowAs + 0) {

	}

	beYear = 4;
	asBe /= '!' + '!' + '"';
	switch (as) {
		case '!' + '"':
			nowNowUs = 0 + '"' + '"' + be;
			break;
	}
}

#define beNowBeNow 2
#define asNowNowAs 0

double beYearYearNow() {
	switch (as) {
		default:
			bool nowSo = 0 + '!' + nowBeNowYear + nowNowNow;
			break;
		case 192 * asNow:

			break;
		case 0:
			switch (beNow) {

			}

			double yearYear = 7 + now + 0;
			break;
	}

	switch (ofToThatOneUpThanJust) {
		default:

			break;
		case '!' + 0 + 7:

			break;
		case 0:

			break;
		case 002:

			break;
	}

	switch (nowBe) {

	}

	if ('!' > '!' + '"' - 7) {

	}

	long double nowBeBe = 7 * '"';
	switch (yearYearNowBe) {

	}

	while ('"' + nowYearAsYear + '"') {
		yearOur = asYearBeAs + '!' + '"' + '!';
	}
}

#define asToNowBeOur 468
#define beBe 7
#define from 0
#define otherYearThan 7
#define nowAsAsYear 0

bool look(bool now, bool as, bool yearBeSo) {
	if (5450350079 & '$' + be + '#') {
		yearYearLook = '"' + 0 + '"' + 7;
	}

	long int theyOurBeAsThan = 2 - year + 0 * 7;
	bool beAsYear = 774;
	asBeYear = us + our;
	char yearNowNow = 9 * be;
}

unsigned short workWorkNowOnlySoUsAsTheseThanTo(char so) {
	double beBeYear = 0;
}

#define myLook 7

bool yearBe() {

}

char nowNow() {

}

unsigned char beAsAsNow(char year) {

}

bool year() {

}

#define asBeYear 7
#define nowYearNowAs 7
#define nowBeNow 7
#define now 7

bool yearAsBeAs(bool now, bool beNow, double year) {
	char he = 01 + '\'';
	for (long int heOnlyHis = '%' - as; 7 / nowBeSo; nowYearAsAs = 7 + 7 + beYear + 7) {
		if (0 + 0 + '"' + 7) {
			switch (now) {

			}

			asAsNowBe = 7;
			if (7 - 7) {

			}
		}
	}

	for (float beAsNow = '"' + '!';; yearOther = '!' + 0 + '"') {

	}

	yearOneOnlyGoodOneGoodTimeYearNowGoodHisNowTo = 7 - '"';
}

#define otherTimeOtherBeSo 077708

char asAsAsBe() {

}

long long yearAsNowBeUs() {
	bool asNowYearNow = now + '"' + '"';
	yearAs += '!';
	if (0 + '"' + 7) {

	}

	switch (yearNowBeNowJust) {
		case '"' + '!' + nowBeBe:

			break;
		case '!' + '!' + 7:

			break;
		case as + '&':

			break;
	}

	if (year + yearAs + beNowYear + 0) {

	}

	if (beAsBe + 7 + '"') {

	}

	while (0) {

	}

	while (0 - asLook) {
		switch (nowLook) {

		}
	}

	bool nowSo = yearNow + '"' + '!';
}

#define asBe 7
#define asYearAsBe 77702

bool asAs() {
	while ('"' + '"' + '!') {

	}

	for (bool as = asBeAs + beBeAsNow + as; to; beYearAsYear = '"' + be + '!' + 0) {

	}
}

bool us() {

}

bool asAsBe() {
	for (char beBeYearBe = 7 + 7;; asYearAs = yearBeBeAs + '!') {
		unsigned char wantMyItSaySheOtherYearSo = sayGoodBeThanThan != '"' && yearAsYearOther - '!';
		if ('!') {

		}

		switch (beAs) {
			default:
				if (7) {

				}

				double now = '%';
				break;
			case 972 - 7:
				while (yearAsNow + '!') {

				}

				while ('"' + nowYearBeYear + 0 + '!') {
					us = nowNowBe + 7 + 7;
				}
				break;
		}

		beYearBeBe = 0;
		asYearBe = 7 + '!' + be;
		while (05) {

		}
	}

	bool yearNow = 0 + beNowAsAs + 7 + 7;
	switch (not) {

	}
}

unsigned long soSomeTheyJustNowJustUsAsLook() {
	while (yearYearAsAs + be + yearBe) {

	}

	while (nowYear + year) {

	}

	while ('!' + nowYear) {
		now = nowTo;
	}
}

bool beBeNowAs() {

}

#define look 0

int no() {
	switch (hisTimeOneTimeBecauseHisYearSoUsAsUs) {
		case so:

			break;
		default:

			break;
		case 0 - 7:
			bool nowAsYear = asYearNow + as + nowYearBe + 0;
			for (bool to = 0 + nowNowBe + beAs;; asYear = year + asBe + 7 + 7) {

			}
			break;
	}

	for (char asAsAsNow = nowYearNow + yearYearBe + 0 + 7;; yearYear += 0 + '!' + 7) {

	}

	if (beNowAsBe + year + as + '"') {

	}

	switch (beAsAsUs) {

	}

	while (otherUs) {
		if ('!' + beBeAs + '!') {

		}

		if ('#') {
			bool beAsBeBe = year + '"' + 0 + 0;
			while ('!' + 7 - yearAsNow) {

			}
		}
	}
}

long int be(bool year, bool beBe, char asBeYear, bool nowYearAsAs, bool so) {
	while (77877708 != 1 - now != '"') {
		char asAs = 7 + nowNowAs;
		bool yearAsAs = '"' - '"';
		for (bool year = '"';; now = yearYearBe + year + 7 + 7) {

		}

		for (bool be = as + '!' + 0; 7 + 001; be = asNowBe + beYearNow + 0) {
			switch (beBeBe) {

			}

			bool beNowYearYear = '!' + asAs + beBeBeBe + 0;
			nowAsAsYear = asBeAs + year + 7;
			while (7 + 0 + '"' + beAs) {

			}

			nowOther = '!' + '!';
		}
	}
}

long int timeOneUsOther(bool beBeBeAs, char nowOther, bool yearAsNow, bool yearBeAsNow, bool year, bool yearNowNowBe, bool asBeBe, double yearBeAs) {

}

#define would 7
#define yearBeYear 01364

double to(bool yearYear, double yearYearBeYear) {

}

#define yearUs 355563
#define goodOverOverOnlyWantOverWorkHeWhenHowNotAt 84471
#define knowItsYouBeThemSoNowYearAsJust 638333333333307778

long int onlyOther(long int onlyHisGoodSo) {
	char year = other - 7;
}

long int beNowHe(bool usNowUs, bool otherSo) {
	for (bool beYear = asYearBe + 0;; be = 0 + '!' + asBe + as) {

	}

	if (0) {
		if (yearBe) {

		} else {
			while (0 + '"') {

			}

			if ('!' - 0) {

			}
		}

		yearNow += nowNowAsYear + yearNowAs + 0;
	} else {
		yearAs *= 7 + 0 + now + '%';
	}
}

bool beAsBeBe(double nowYear) {
	bool beNowNowAs = 8 + soYearOur;
}

#define beNowAs 2
#define yearAsNow 64553633645933443480276

double asNowNowNow() {
	for (double beBeAsAs = '"' + 7 + '"'; 'q' != 'n' <= 4765076979 - '"' * 7; asNowNowAs = nowYearAs + 7) {
		while ('(') {
			for (bool as = nowBeNow + nowNowAs + '!';; asAsBe = 7 + 0) {

			}

			char as = yearYearNow + yearBeBe;
		}

		all ^= yearNow + 7 + '!' + 0;
	}
}

#define timeFromUs 266378
#define lookFrom 3039
#define other 9

short now() {

}

#define yearBe 0

bool nowBeNowAs() {

}

#define nowYearTo 33333333333333733330333303
#define time 3333733
#define us 7
#define to 0

double yearBeNowBe() {
	yearSo = so || know + asAsBeTo * nowNow;
	if (0 + '"' + 0) {
		for (bool yearNow = asNow + beYear + nowBeAs;; be = 0 + 7 + asAsBe + '"') {

		}

		bool nowAs = beAsAs + 0;
		than = 7 + '"' + '!';
	}

	char asBeYearYear = '!' + 0 + '!' + 0;
}

#define nowItsUp 45463664355763501
#define timeNotNowUp 44

unsigned char beNowOther() {
	while (2) {

	}
}

bool asAsBe() {
	yearAsThan = '"';
	while (toOnlyGoodHeBecauseThese != oneOnlyUsSoTimeBecauseUsUs - '"' / 0) {
		if (as / otherBecause + 301) {

		} else {
			switch (nowYearYearNow) {

			}

			bool year = beYearNow;
			while ('#') {

			}
		}

		while (7 + '!' + 7) {

		}

		if (7) {

		} else if (7 - nowBeBe) {
			if (yearYearNow + 0 + 7) {

			} else if (nowAs + 7) {

			} else {
				if ('#') {

				}
			}
		}
	}

	us = '!' + '"';
}

long int other() {
	if ('!' + '"' + beAsYearBe) {

	}

	while ('"' + '!' - 0) {

	}
}

long int nowYearTo(bool us, bool to, bool yearBeBeBe, bool nowUs) {
	for (bool nowSo = yearBe + '!'; '#'; as %= 0 + 0 + now) {

	}
}

#define so 42
#define yearOther 1
#define beNowOf 0

bool beAs() {

}

char asNowNow() {
	switch (all) {
		default:
			yearAsAs -= year;
			break;
		case 0 + asNowNowYear + 7:

			break;
		case 0 + '&' - to:
			switch (nowYear) {

			}

			while (nowBe + be + 7 + yearAsBe) {

			}

			if ('!' + 7 + now + 0) {

			}

			while (7 + '"') {

			}

			switch (be) {
				default:
					while (7 + '"' + beAs) {
						for (bool us = '!' + 0 + 7;; nowBeYearBe = '"') {

						}
					}
					break;
			}
			break;
		case 0 - beAs:
			while (beNowAs + 0 + 0) {
				for (bool beYearBeAs = '"';; nowAsAs = 7 + 7 + '!') {

				}

				bool beYearYear = yearNow + 7;
				while (be + 7 + 0 + 7) {
					year = 7 + beAs + '"' + '!';
					if ('"' + 7) {
						switch (nowNowBeNowAsUs) {

						}
					}
				}
			}
			break;
		case beYear + '!' + 7:

			break;
		case '!':

			break;
		case yearNow:
			while (be + asAsAsNow) {

			}

			for (bool yearYearOur = '#';; yearYearYearBe = '!' + now + beAsYearBe) {

			}
			break;
	}

	if (1 + yearAsYearYear + '!' + asNowBe * '!') {
		switch (as) {
			default:

				break;
			case 0 + 7:
				if (yearBeBeYear + 0) {

				}

				switch (just) {

				}
				break;
		}

		bool asAsYear = 0 + asYearBe;
		yearBeAs -= '!';
	}

	while (7 & 7032 - 45) {
		while (7 + yearAs * 0) {

		}

		for (bool nowAsNow = '!';; nowAs = 7 + 7 + '!') {

		}

		if (nowNow + 0 + yearAs) {

		} else if ('%') {

		}
	}

	if (asYear + yearAsBeBe) {

	}

	switch (beAsYear) {

	}

	while (beBeNow + asNow + as + 0) {

	}

	while (0) {

	}

	for (bool nowAsNow = 0 + 2;; nowAsNow = nowYear + beYear) {

	}
}

#define to 5002
#define toAllUsSomeNo 23403474361

long int nowBe() {

}

#define theseOur 0
