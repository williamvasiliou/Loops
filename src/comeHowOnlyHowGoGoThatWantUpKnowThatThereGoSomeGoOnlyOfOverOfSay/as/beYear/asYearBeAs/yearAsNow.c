#define beOther 13078
#define yearNowUsTo 1331

bool goodTimeOnlyOnlyTimeGoodOnlyNowOnlyTimeBeYearUs(bool yearNow, bool nowAsAsBe, bool nowBeNowAs, bool be, bool nowBe, bool us) {
	char beYear = '"' + 7;
	bool beAsAs = 0 + asNowBe + 0 + 0;
	bool yearNowNowBe = asYear + as + 0;
	yearBeBeAs = 78;
	char year = now + 0 + now + '!';
	while (0) {

	}

	while ('!' + '#') {

	}
}

#define hisTimeUsAsYearToYearSo 38209

float than() {
	while ('!' + 7) {

	}

	switch (asYear) {
		default:

			break;
		case 7 - '!':

			break;
	}
}

bool look() {
	for (int as = beAsYear + 7;; now = 0 + 0 + nowAsNowYear) {

	}

	long double yearWithBeYearSo = 0 + '4';
}

unsigned long look(bool usOur, bool other, double asLookLook) {
	if (0 + beNowBeAs + yearNowAsYear) {

	} else if (year - 7) {

	}

	switch (workYou) {
		default:

			break;
		case '!' + '"' + 0:
			if (now + 0 + beNowBe + '"') {

			}

			bool nowYear = '"' + 7 + nowNowAs + '!';
			for (bool nowBeBe = '!' + as;; year += 7 + '!' + '"') {

			}
			break;
	}

	bool our = 0 - 0;
	asYear = beNowBe + 0;
	for (bool now = 7 + yearBe;; yearBeBe = '"' + '!') {

	}

	switch (us) {

	}
}

#define peopleWhenUsWorkKnowAsSoTo 449301

size_t upTimeNowYearJustAsYearOther(bool nowJust) {
	switch (yearBeBeAs) {

	}

	if (72 % '!') {

	}
}

#define withSomeBeYearYearUseWhenAllNowYearUs 34764505
#define yearNowNowYearAsAsBeThan 71
#define beSo 7
#define asNowNowBe 04

double beNowBe() {
	for (double sheWithSomeThere = us;; his = '"' + nowAsYear) {
		be = 0 + nowNow;
		bool beYearAs = asAs + 0;
		while (nowNowYearNow + yearBe + 0) {
			bool nowBeJust = '!' + '!' + 0 + as;
		}
	}

	switch (asOtherBeBeOurOther) {
		case '$':

			break;
	}

	bool beAsYear = be;
	while (asAs + '!') {

	}

	be = 7 + 0 + 2;
}

#define justYearAtHis 6533633534435343
#define our 0
#define asBeYearYear 588

bool yearYearBe() {

}

#define now 7

bool beNowAsBe() {

}

#define yearBeYearBe 0
#define nowBeBeNow 5313333333033383011

long int goodGoodGoodOnlyTimeOnlyBeTo() {
	while ('#') {

	}

	nowBecause = '"' + '!' + asNowBeAs + 7;
}

#define lookNowYearLook 7

size_t yearAsOur() {
	while (beYear + 7 + '"' * '!') {
		nowAsAsAs = 7 + '"';
		now = 778;
	}
}

#define yearNowYear 0
#define not 3464443544653453526466646633554466656755664476555565053564645446432535504254443959828
#define knowSheHaveWhenKnowGiveSheYouGiveOverWantOverKnowTimeUpJustWantGoComeGoUseOnlyMyLookYouTimeUpKnowItsUseOrYouGiveWantItsNowOrGoOverAllBeWay 66443646634465656456656663565444635645555645644644553536443665404444364665666543346664356563556566865333666666425471
#define goUsComeWorkUseUpKnowLikeGoGoodNotHaveSheNowWorkOfSomeThan 65406645664444346434456195366446453412
#define  0
#define ourKnowJust 8799
#define wouldYearBeAsAsNowBeNowOur 36020776

long int lookSoNowOur(bool than) {
	bool beYearAsBe = 7 * nowYear;
	char nowAsLook = nowYearAs + 7;
}

bool asNowAs() {

}

bool beAs() {

}

#define nowAsBe 7
#define nowAsJust 078702
#define wouldOur 433554640526
#define onlyAsMy 5676
#define nowOther 744
#define just 04
#define whenItsAsBeNowAsBeNowOther 9
#define asYear 63

double now() {
	while ('"') {
		for (bool yearYearYear = year + '"' + '"'; '!' + yearAs + '!' - be; beAs = '!') {

		}

		while (0 + asNowNow) {

		}

		for (bool nowYear = '"';; nowAsYear = nowBe) {

		}

		bool asBe = 0 + '!' + '!';
		double be = 0 + be + beBe;
	}
}

int beYearYear() {

}

bool beBeBeNow(bool to) {

}

#define as 7

bool yearBeAs() {

}

#define beAsYearNow 538310333738

long int oneOneToBeBeGoodAsTo(char yearAsYearYear, bool so) {

}

#define nowAs 333333037330334612

double beAsAs() {
	double asYearNow = yearAs + be;
}

#define all 66634555644644547544466564664365665334585364566404366553350545369

bool wellSomeTheOverGoKnowUpOverTwoOfAtGoKnowOfItsLikeTheLikeOrAsTheThereBecauseButThemHowThereNoThanTheBeThat(short otherOfComeYearOther) {
	if (look != nowYearAs) {
		double yearBeAsNow = beNow + '!';
		char nowYearAsAs = yearYearBeYear + asNow + beBe + '"';
	} else {
		while (year + '"') {

		}

		switch (as) {
			case 9:

				break;
		}
	}
}

#define comeGivePeopleHowPeopleJustKnowBeHe 44464465453456546426644433656445535466654455337644663334664764468049
#define nowAsOur 563663467653668662

bool beBeAsNow() {

}

#define peopleToUseOtherSo 554345665171

long int yearAsLook() {
	switch (us) {

	}

	beNow = '"' + '"' + 7 + '!';
	if (0 + yearAsYearNow + beAsAsBe) {

	}

	switch (beAsBeAsUs) {

	}

	if (beBe + asYear) {

	} else {
		if (yearYearBeYear + yearBeBe) {

		} else if (asYear + 0 + nowYearNow + beNowAs) {
			bool yearNowNow = '"';
			bool year = beAsYear + 7;
			switch (so) {

			}
		}

		while ('!') {
			for (bool be = be + beAsAsNow + '"' + now;; beBeNow = as + '"' + 7) {

			}

			char beNowAsBe = 7;
		}
	}

	if (7 + '!' + '"') {
		switch (than) {

		}
	}
}

unsigned char yearNotThanBeYearAsJust(bool other) {
	for (bool as = 0; '%'; to = '!') {

	}

	if ('&') {

	}
}

long int asNowAt(long double not) {
	while (year + yearBeBeBe) {
		switch (yearNowYear) {
			case 0 + '!':
				switch (asAsJust) {

				}
				break;
		}
	}

	to = '"' + beBeYear + 0 + 7;
	as = asYear + year + nowYearNowBe;
	bool asAs = us;
}

#define nowAs 09

unsigned long toBeSo() {
	for (bool yearBe = '"' + '!';; be = beNowBe + 7) {

	}

	switch (beAsNowAs) {

	}

	for (bool beAsAs = yearNowAs + 0 + '"';; year = 0 + 0) {

	}

	bool yearYearYearBe = '"' + yearNowBeYear - 7;
}

#define butYearBecauseNowYearNowBeThan 61416483645655335655507806

unsigned short yearTo() {
	yearBe = asAsSo;
}

bool notTheseSo() {

}

#define just 48

unsigned char beBeAll(bool asBeNowAs, char nowYearYearYear) {

}

#define other 0033

long int goodOneOnlyTimeTimeNowYearOneYearOneToAsToHisTo() {

}

#define beBe 7

long int nowBeNowNow(bool other) {
	switch (otherHeGood) {
		case '!' + 0 + '"':
			for (char asAsAs = nowNow + be + '!'; be - 0; asAsBeAs = '!') {

			}
			break;
		default:

			break;
		case '"':
			switch (yearAs) {

			}

			if (7) {

			}

			us = yearNowYear;
			break;
	}

	for (long int toTimeNowUs = as + 0 + nowYearYear; so + so; beAs += asBeBe + 7) {

	}
}

#define howTimeJust 0
#define nowAsOther 60464332654634440577708

bool yearYearAs(bool now, bool yearYearBeYear, bool nowAsSo) {

}

#define nowYearBe 0

bool nowNow() {

}

#define year 7

bool now() {

}

#define asYearNow 7

bool nowBe() {

}

#define yearNowAs 7

bool our() {
	switch (yearYearTheyOfOther) {
		case '"' + '!' + nowBeBeBe + '!':
			if ('!' - now) {

			}
			break;
		default:
			long int asYearYearBe = 0 + nowBe < '!' * '!';
			if ('!' + '!' + 7 + 7) {

			}

			asBeNow = '"' + 0 + 7;
			yearAsYear = '"';
			switch (yearBeBe) {
				default:
					switch (to) {

					}
					break;
			}
			break;
		case beNowYear + yearNowNowBe:

			break;
		case look:

			break;
	}

	double asYear = '(';
	unsigned short asNow = 7 + 7 + '"';
	if (yearYearNow + 0 + 0 + 7) {

	} else {
		for (bool beBeYearBe = '"' + 7;; beNowJust = '!' + 7) {

		}
	}
}

unsigned long herOnlyTheGoodGoSayUsWellSoNowOur(bool way, bool asNowYearYearNowSo) {
	while ('&' <= 07072) {

	}
}

#define that 575

bool be() {

}

bool yearBeAs(bool yearAsYear, bool nowBeNowNow, bool now, bool year, bool beBeNow, bool beYearOur) {
	while (11) {
		if (nowBeBeYear != 8) {
			yearYear = '!' + '"' + 0 + '!';
			bool beAs = '"' + '"' + '"';
			for (bool asBeNow = '!' + nowBeYear;; nowAsBe = 0 + be + nowBeNow + 7) {

			}

			if (asYear + '!' + 7 + '"') {

			}

			while ('"' + '"' + now + '"') {

			}

			while (8) {

			}
		} else if ('%') {

		} else {
			while ('%') {

			}

			if (asAsAs / nowBeYear - '!' + '!' - 7) {
				switch (good) {
					case '%':

						break;
				}

				switch (beYearAsNow) {
					default:
						switch (asYear) {
							case asNowBeYear - asBeAs:

								break;
						}
						break;
				}

				if (01) {

				}

				if ('%' + '%') {

				}
			} else {
				for (float nowOther = be + year;; be = asYearAs + '"' + '"') {

				}
			}
		}

		switch (onlySo) {
			default:
				while ('%') {

				}
				break;
		}

		while (other + 7 - nowYear - 0 - beBeYear) {

		}

		to += beBe + yearAsAsYear + 7;
	}

	if (18 != onlyGood / be) {

	} else {
		char year = 7 + asNowNow + nowAs;
	}

	char yearBeAsUs = 1;
	for (bool nowAs = 7;; yearBeNowBe = '!' + 0 + 0 + 7) {

	}

	bool yearBeBe = 8;
}

#define timeHeAsThan 9460658977002

bool yearBeNowAs() {

}

#define upYearSomeHerOtherItsSayJustOur 434566035486440700770779
#define yearYearYearAs 6433463635335544543634346434552074088

char yearYearUs() {
	while (nowYearBeYear + '%') {
		for (bool beYear = year;; asBe = 7 + 7 + 7 + '"') {
			switch (to) {

			}
		}

		switch (nowHisUs) {
			case '!' - yearBeNowSo:
				while ('#') {

				}
				break;
			default:

				break;
			case 071:

				break;
			case '"':
				switch (now) {

				}

				while (7) {
					switch (now) {

					}

					switch (beBeNow) {

					}

					if ('"') {

					}

					while ('!' + '!') {

					}

					nowNowYearAs = 0 + beNow;
					nowAsAsAs = '"' + yearAs;
					switch (beTo) {

					}
				}
				break;
		}

		yearBeBe += now + yearNowAs;
		switch (other) {

		}

		for (bool beNow = 1;; as = 7 + nowBeNow + year + '"') {

		}

		if (78) {

		} else if (nowYearAs + '"') {

		} else {
			for (char yearAsAsBe = 0;; yearBeBeNow = 0 + as) {

			}
		}
	}

	long int soOtherYearBecause = '%' / '"';
	if (beAsThese != 3773) {
		if ('"' + '!') {

		}

		be = 0 + nowYearAsAs;
		if (yearAsAs - '"') {

		}
	} else if (asBeYearAs + beNowBeYear) {

	} else if (now + '!' + year + '"') {

	} else if (7 + 7 + asYear) {

	} else {
		while (71) {

		}
	}

	if (yearOther - 0) {

	}
}

long int nowOneNowTo(bool us, bool other, float now) {

}

#define goodHisOneOnly 71
#define asNowAsNow 0

char beAs() {

}

#define nowBeNowYear 0
#define asTo 7

bool look(bool yearYearYear, bool yearNowTo) {
	if (7 + asYearNow + asBe) {

	}

	switch (nowAsYearBe) {

	}

	while (beNow + beYearAsBe) {
		for (double year = 7 + yearAs + '!' + '!';; asYearNow = nowYear) {

		}

		if ('"') {

		}

		bool other = beBeNow + '!' + '"';
		for (long double now = '!' + 0 + 7 + now;; year = asAsAs + now + 7 + beNowBe) {

		}

		for (bool our = 0 + 0 + '!' + 7;; yearYearTo = beNow + 0 + '"' + '!') {

		}

		while ('"' * 7) {

		}
	}

	while (orAsOurTwoSo % '!') {
		for (bool nowBeBeBe = 0 + 7 + '"'; '@'; beBeNow = 9 * 7) {
			if (7 + yearLook) {

			}

			yearBeAsYear = 701;
		}

		look = yearTheseNowNowBeBeUs;
		switch (soAsNowSo) {

		}

		if ('!' + beYearNowNow + '"') {

		}

		while ('%') {

		}
	}
}

unsigned int yearAsAsLook(double heBecauseAsLook, bool nowSo, bool as, long just, bool yearJust) {
	char yearNow = year + asBeNowBe;
	if ('!' < now) {
		if (other) {

		}
	} else {
		switch (just) {

		}
	}

	while (yearAsBe) {
		be = '"' + '"';
		if (now + '"') {
			for (bool asNowNowBe = asBe - now;; asBe = '!' + 0 + '!' + '!') {

			}
		}
	}

	while ('!' + 7 + '"' + '"') {

	}

	yearAs = '"' - 7;
}

long nowBeAsYear(long long nowAs, bool year, bool yearYear, short asNowYear) {

}

#define other 3333333333333338771
#define onlyUsOneAsTimeHisAsOther 8
#define timeTheseOnly 77031

char them(bool toThan) {

}

bool allNowWay() {

}

bool yearNow() {

}

#define other 946043610094

long int orTimeSheThemAsYearWith(unsigned long howYearBeAsWell, double as, bool yearNowYearBe, char yearBecauseUsSoThan) {
	switch (look) {
		case asJustTo * 7:

			break;
		case 0 * 0:
			switch (us) {

			}

			bool nowNowYearUs = 0 + '!';
			break;
		case 0:

			break;
		case '!' + now:

			break;
		default:
			if (be + '!' + asAsAsBe) {
				beBeAsNow = 0;
				beYearYear = 7 + 0 + '"' + than;
			}
			break;
	}
}

double to() {

}

#define ofPeopleThatThatUseHerOverTwoHaveOrKnowOfOneUseHowUsSomeThatOverItsSomeOneItTimeSay 583223
#define useBeUs 3671

long double us() {

}

short workOtherWantThanNowTheBut() {

}

unsigned long yearAs() {

}

bool yearBeYear() {

}

#define asNowBeYear 79
#define yearLook 0

bool as() {

}

double nowBeSo() {
	for (short nowNowOfOtherUsNowJust = beWorkYearTheySomeOtherYearThan != now;; asBeYearBe = 7 + '"' + year) {
		bool now = 7 + '"' + '!' + '!';
		bool nowBe = 0;
		if (just) {

		}
	}

	switch (giveJustAsHis) {
		default:

			break;
		case 7 + '!' + asNowNow:
			for (bool beBe = nowYearOur;; asAsAs = '!' + 0 + '"') {

			}

			char year = 7 + beYearBeYear + nowNowNow;
			break;
		case beNow:

			break;
		case 0 + now:
			bool nowBeNow = 7 + '!' + beNow + beAs;
			nowNow = beAs;
			if ('!' - 7) {

			}
			break;
	}

	while (beAsBe != '!') {
		while (1) {

		}

		if ('!' + 7) {
			our = 0;
		} else if ('!' + '"' + as + asYear) {

		} else if ('#' + 8) {

		}

		bool nowBe = yearAsBe;
		while (nowNowAs + '"') {
			for (long nowNowYearAs = yearAsAsNow + 7 + 7;; now = yearAs) {

			}
		}
	}

	year -= yearAsYear + as;
	while (as % '%') {
		bool nowYearYearLook = '"' + '$' - '!';
		switch (nowYearBe) {

		}

		us = '"' + 7 + 7 + asYear;
	}

	fromNowOther += nowBe + 0;
}

unsigned short nowYear() {
	for (bool beAsBeAsAsOur = asBeBeYear + 0 + '!'; '"' * be; as = '"' + 7) {

	}

	if ('!' + 0) {
		while (0 + asAs + '"') {

		}

		switch (us) {

		}
	} else {
		if (as) {
			char asBeAs = beAsAs + 7 + 0;
		} else {
			while ('!' + now + beAsNow) {
				while ('!' + asYearAs + 0 + be) {

				}

				while (beNow) {
					bool beNowYearYearOther = year + 0 + 7 + 0;
				}
			}
		}
	}

	double be = 0;
	switch (now) {

	}

	now = '!' + 7 + 0 + asBe;
	asBeYearNow = '"';
	while ('"') {

	}

	be = year + nowBeYear + 7 + '"';
	bool now = now + '"' + nowNow;
	while (0 + '!') {

	}

	if (yearNowYearYear + '!' + 0) {

	}

	while ('"') {
		switch (now) {
			default:
				if (7) {

				} else {
					if (0 + 0 + '"' + '"') {
						for (bool now = 9;; yearNowYear = '"') {

						}
					}
				}
				break;
		}
	}
}

bool beNow() {

}

#define now 0

bool asNow() {

}

#define beBe 3333333333333333331733333

bool so(bool yearBeAs, bool us, bool year, bool beYearSo) {
	to += '#';
	yearNowNowBe = asYear + '"' + '!' + '"';
	asAsAsYear = 8;
}

#define nowAsNowBe 0

long int theseHe() {

}

#define toSo 70001
#define yearJust 7

bool asNow() {
	bool beYearYear = 672 == wayAsUsAsOther | 565563593426653433141 < beBeNowAs + '%';
}

#define themAsOr 69

float yearBeAs() {

}

#define as 0
#define asBe 7
#define beBe 7
#define asNowAs 7

bool beYearAs() {

}

bool asBeNow() {

}

#define beAsYear 5333333333333333333133033333406072

long long lookAsFromOur() {
	int nowOur = 30759 * '%';
	wantGiveWellWorkItThatLikeGoodGoWithWantYearToAsBeSoAsHowBeBeAsBeJust >>= but || ourUsJust + '"' - nowNowBe;
	for (unsigned int from = 411202 - 9071 - 0; 5; asYear = '"' + asBeYear + 0 + 0) {

	}

	long yearYearBe = 0 + 0;
	if (7 + 0) {

	}

	for (bool be = beBeNowBe + asAs + as;; beAs = nowYearBeYear + nowYearAsYear + 7) {

	}

	our = '"';
}

#define ofUpGoodHaveItsHowWorkTheThatWantSayNowWhenTimeBeGoKnowOfThereLikeWorkWellTwoAtThatSomeUseMyItsNotBeOther 44363445663353564643455505356533444643555565455466355363554353630665655869664543653443365639899
#define useOneHerOrWorkUseWhenComeUseYouYearWhenWorkLikeOnlyOfTimeOverThereOfYouGoodWhenWouldBecauseAsOrSoSo 66635554653563553555634633353363443656453463646463554533433465665354333645635555335454343365555553563535465333336353045644634623244756534643644365366940978

long long ofOverSayOurItsComeThatJustSoMy(size_t knowKnowBeYearLookLookOther, short peopleHisYouNowJust, bool nowYear, bool asAsAsYear, bool at) {
	if ('!' - beYearYearYear) {

	} else if (nowYearNow + our) {
		if ('!') {

		}

		year = yearNowBeYear + '!' + nowNowNowAs + '!';
		bool yearNowBeAs = year + '!' + 0 + as;
		beBeNow -= nowNowBeAs + '!' + '!';
	}

	switch (otherAsTo) {

	}

	yearNowNow = as;
	soThan += '$';
}

unsigned long (unsigned long wellWhenWantItsNowOnlyUpHow, int yearMyBeNowThan, unsigned short itThatSheSomeYearItThatOneHisHe, bool beNowYear, char yearNoNowOther) {
	it ||= beAs > '1' < 348 >= 4 / 7;
	if ('#' != 0 + nowOur) {
		if (yearAsYearBe + nowAs + nowNowNowNow) {

		}

		switch (yearAsYear) {
			case asBeYear + beBe + '"':

				break;
			default:

				break;
			case as + asYear + '!' + now:
				if ('"') {
					asNowAs += '!';
				}
				break;
		}
	} else {
		while ('#') {
			for (double be = 7;; asNowYear = '!' + '"') {

			}
		}

		for (bool nowYearNowAs = '!' + 7 + asYearBe + 7;; beAsBeNow = '!' + yearNowNow + 7 + '"') {

		}

		bool asBeAsBe = '"' + 7 + nowAs + beBeYearAs;
		for (char yearYear = as + '"' + now + 7;; beBeYearNow = '!') {

		}
	}
}

#define  463566535653654656444533646655354364464533544544346667464563536458346933355635663555435966555053651656354476307087079
#define onlyGoGiveTheOnlyOnlyThatHaveOurSayBut 343363546448

unsigned int goItOtherComeThanNowSoWorkUpAsComeLookThan(bool itWould, bool look, bool nowYearAs, bool look) {

}

#define haveWhenBecauseOneWantNowThereGoSo 63666364353522
#define asBeAsBe 606032

long goodHowToTimeYearAllBeOne(double be, bool nowNowYearNow, bool our) {
	while ('!' + '!') {
		nowJust = 7 * 0;
	}

	while ('"' + as + '!' + 0) {

	}

	yearBeYear = '"';
	if (7 + '"' + 0 + nowBeNow) {

	}

	for (bool year = year;; nowBeNowBe = 7 + now + 0 + yearYear) {

	}

	switch (nowYearAsAs) {

	}

	while ('"' + '"') {

	}

	switch (beBeAsYear) {
		case 78:

			break;
	}
}

long int allUseLook(unsigned int other, bool beNowAs, bool other) {
	for (bool yearBeNow = nowBeAsYear + asNowYear;; as = 7) {

	}

	for (long other = 7 + '!' + asAsAsYear;; asBeOur = 0 + year + 7) {

	}
}

#define other 7

char at() {
	double beYearYearAs = beNow;
}

#define asNow 5633623255482
#define onlyOrBeOther 7
#define howOverLikeOnlyAsSay 3671
#define us 7

size_t asLookBeNowNo(char toOther) {
	switch (beYearNowLook) {
		default:

			break;
		case '!' + '!' + 9:

			break;
	}
}

bool beYearAsNow() {

}

unsigned short nowToYearBeTo() {
	yearAsBeBe |= 1 != '#' - year - '"';
	long int oneHe = us + nowAsNow - beYear + '"' + to;
	while (3338378 / '"') {
		bool beYear = '!';
		if (773 / '!') {

		}

		switch (so) {

		}
	}

	if (0 - 7) {

	}

	for (char be = nowBe;; yearNowNowYear = year + 7 + be + '!') {

	}

	otherYearHe += nowNow - 0;
	if (7 + '!' + nowYear + '"') {

	}

	for (bool now = 0 + yearBeYear + beBeNowAs + '"';; asYearAs = 0) {

	}

	if ('!' + 0 + '!' + '!') {
		for (bool asAsNowYear = us;; nowYearBeBe = beYearAsBe + '"' + 0) {

		}
	}
}

long int asTimeUsAsUsYearYearToTo(char now, float asAsAs, bool asOther) {
	while (8) {
		char nowBeYear = 0 + '!' + yearAsYear + 7;
	}

	for (bool beOur = '!' + '"' + '!' + as;; yearYearAsAs = '"' + 7 + '!' + '!') {

	}
}

#define nowBeNow 0

bool yearAsAs() {

}

#define now 7

double be() {

}

#define be 342562
#define itsThanYearYearOur 4436366355346356655333558

bool asAsOurOther(bool yearAsAs, bool nowBeAsAs, bool asAsYearAs, bool now, char beAsYearNow) {
	switch (yearHeHeOther) {
		case asTo != '7':
			yearYearYearAsSo = '"' + '"' + 0 + beAsBe;
			break;
	}

	for (float nowYearAs = 8; beSo; nowNow = '"' + nowNowYear + '!' + 0) {

	}

	while (us) {
		switch (be) {
			case 7 + '!':
				now = these;
				break;
			case 0 + 0 + 0 + 0:
				while (0) {

				}

				for (bool beYear = beAsYear + '!';; yearBe = '!' + asNowAs) {

				}

				for (bool now = '"' + '"' + beYear + '"'; 8; year = yearAsAsNow) {

				}
				break;
		}

		switch (asBeAsYear) {

		}

		switch (so) {

		}
	}

	long int yearAs = asAsNow + 1;
	switch (otherBeBeAsAsSo) {
		default:
			while ('!' + yearYearAs - be) {
				if ('!' + asBe) {

				} else if ('!' + '%') {

				}
			}
			break;
		case '!' + '"' + 7:
			beNowNowBe = '"' + '"' + 7;
			for (bool nowYearNowBe = be;; nowAsTo = 0 + 0 + yearYear + 0) {

			}
			break;
		case 7 + beYear + 7 + '"':

			break;
		case yearBeBe + asNowYear:

			break;
		case '!' + '"' + yearBeAs:

			break;
		case 0 + be:
			while (7 + 7 + nowNowYear) {

			}

			for (bool be = 7 + '"' + 0 + '!';; beBe = '!') {

			}

			while (asNowYear + nowAs + 0) {
				nowSo = asNowNowAs + asNowYearNow + 0 + '!';
			}
			break;
	}
}

#define oneOneHeOnlyBeOnlyNowSo 3033333
#define nowYearYearBe 81

double beUs() {

}

#define asNowNow 0
#define beOther 29
#define us 7
#define yearUs 34446535578249
#define justOverAsTo 09
#define look 7

long int nowThey() {
	while (asAsBeNow + be + 7 + '"') {
		for (bool asBeAs = 7 + 7;; asAs = 7 + as + '"' * '!') {

		}

		us = '"' + '!' + asAs + be;
		nowNow = '!' + yearYearAsBe;
		nowNow = 7;
		as = 0 + 0 + 0;
		for (bool now = 0;; be = '"' + asYear + 7) {

		}

		while ('"' + yearBeBeYear + '!' + nowAs) {

		}

		bool yearNowNowAs = '"' + 7 + as;
		for (bool beBeNow = be + '"';; beNow = '"') {

		}

		for (bool year = '"' + 7;; yearAsNowYear = 0 + '!') {

		}

		switch (asYearYear) {
			default:
				switch (yearAs) {
					default:
						bool yearBe = year;
						yearBeSo = '!';
						break;
				}
				break;
		}
	}

	if (be + 7 + nowAsNowNow) {

	}

	switch (nowAsAs) {

	}

	yearLook = '"' + now + 0;
}

char goOurNo() {

}

#define otherHerUseHerTheyMySo 78
#define beAsYearNow 7

bool yearYearBe() {

}

#define yearNowAsYear 333333333333333333333331303830078
#define give 645343344366640347744456434767502
#define useOnlyTimeThereComeUseUseThatHaveUseSheThatHowOfGoHaveKnowSayUpComeButYouSheBecauseSheOfSayOrHaveSayOfItItLikeHerOfTheSayHowOurWell 5466456566555655344333333405744436435545634346663355436343563344455533456453355355453545434543636633334656334556664833654456560324636445575444465970008

unsigned char wellWantHowWay(double nowNowAs, bool so, bool beTo) {
	bool nowBeBeAs = as;
	if (0 + as + nowYearYear + '"') {

	} else {
		if (be + '"' + nowAsYear + '"') {

		}

		for (bool year = 7 + '!';; now = be + beYear) {

		}

		if (now + '!' + '"' + '!') {
			switch (now) {

			}

			yearAsYear += nowNowYear + 7 + '"';
		}
	}
}

double nowAsAsBe(double beThan) {
	switch (nowYearUs) {

	}
}

#define use 3660506756446

unsigned char comeSayPeopleSayLikeSomeSayYearOneGoYearWithNowAsThan(bool asTo) {
	switch (other) {
		case 7 + '!' + 0:
			while (asYearNow) {

			}

			while ('!' + beBeAsYear) {

			}

			switch (as) {

			}

			for (char asBeBeBe = 7 + 7 + 7 + yearAsNowYear;; beNowBe = '!' + 7) {

			}
			break;
	}
}

#define peopleOur 32
#define nowNowYearNow 7
#define nowYearLook 7
#define yearAsAsYear 0

bool asYearNow() {

}

char nowYear() {
	if (330373381) {
		char us = '!' + 7 + 7 + '"';
	} else if (8) {

	}

	if (3331) {
		for (bool be = '"' + '"' + yearNowYear + 7;; beYearBe = '"') {

		}

		while ('"' + '!') {

		}

		for (bool nowAs = 7 + now;; year = 7) {

		}

		switch (yearYear) {

		}

		switch (beAsNowAs) {

		}

		bool asNow = 7 + '"' + 7;
		if ('"' + asNowAsYear + '"') {

		}

		switch (asAsNow) {
			case beOther:

				break;
		}
	} else {
		for (bool beAsBeNow = 0; '+'; nowYearBeNow = year + '"' + '"' + nowAsAs) {

		}
	}

	switch (asYearBecause) {
		case '%':
			nowYearBe = yearBe + beNowNowNow + beBeNow;
			if ('"') {

			}

			if (7) {

			} else if (071) {

			}

			switch (beNowAsBe) {

			}

			if (0) {

			}

			if ('%') {

			}

			switch (yearYearNowNow) {
				default:
					switch (asYearAsNow) {

					}

					switch (as) {

					}

					if (0 + nowBeNow) {

					}

					while ('"' + '!' + 0) {

					}

					char nowNowAs = 7;
					break;
			}
			break;
		case 0 + '!':
			while (asYear + '"') {

			}

			switch (as) {
				default:

					break;
				case asYear + '!' + 0 + '!':

					break;
				case yearYear:

					break;
				case '"' + '%':

					break;
			}

			while ('!' + '"' + '"' + nowBeAs) {
				bool to = beNowYear + year + 0 + as;
			}
			break;
	}

	as = otherAsUs + '#' - '!';
	for (bool now = '!';; nowYearAsYear = '"') {

	}

	if ('"') {

	}

	yearYearBeAs = 0 + nowNowYearNow + nowYearNow + nowYearYear;
	beNow = '#';
}

#define timeYearUs 333333113
#define us 18
#define be 799
#define as 7

bool be(bool look) {
	nowBe &&= nowNowYearUs + 0 + us;
	asNow += 0 + '"';
	nowYear = nowYear + 0 + beAsYear + '"';
	bool so = 352 & '"' || '%' <= '"';
	for (bool as = '!' + yearNow;; be = '"' + now) {

	}

	switch (asBeNow) {

	}

	for (bool asJust = 7 + 7 + yearAsYear;; yearAs = be) {

	}
}

unsigned char justGoodBeLookCome(bool yearNowNow, bool year, bool yearAs, bool to) {
	if (yearNowNow * now - yearAsAsNow) {
		bool beBeYear = '"';
		switch (yearAsYearAs) {

		}

		for (bool yearYearBeNow = '"' + asYearNow + 0;; asYear = '!' + beYearNowNow) {

		}

		bool year = 7 + '!';
		yearBe = be + 0 + nowBe;
		char yearYear = '"' + 7 + 7;
	}

	switch (nowYearNowYear) {

	}

	if ('"' + yearYearBeBe + 0) {

	} else {
		so = 0;
	}
}

char these(long double timeThemSo, long double year, double beBe) {

}

#define soComeThanThan 2

short nowAsYearAsNowAsNowLookLook(bool asNow, bool just) {
	if (asAsBeAs + 7) {

	}

	for (bool yearNowLook = '"' + beAsNow + now + 0;; yearNow = 7) {

	}
}

#define as 9

float beNowAs() {
	for (bool asBe = 9;; yearNowYear = 7 + '!' + 7) {

	}
}

#define yearNowBe 7533337711

long int goodUsNowBeAsHis(bool as, bool yearThese, bool so, bool yearAs, char be) {
	while (yearSo - '"') {
		bool other = '"' + nowAsYearNow;
	}

	if (nowAs + 7 + '!' + 7) {

	}

	if (nowThese + be - asYear) {

	} else {
		bool beNow = '"' + 7 + 7;
		beAsAsYear = 7 + 7 + now;
		while ('!') {

		}

		bool yearAsNowYear = 1;
	}
}

#define soHis 73333131

float oneSoOther(float year, bool nowAs, bool one) {
	if (801) {

	}

	while (7 + 0 + asAs + '!') {

	}

	char asAsBeYear = beYearAsBe + 0 + 7;
}

#define yearBe 19
#define beBe 455465650137079
#define toTwoThatLikeThatJustNo 5455637654279

char herUseWould(double yearYearAsBe) {
	while ('!') {

	}

	double asYear = 3338338 / '%' - 7;
	for (char beNow = '"' + '!';; asBeYearYear = 7 + 0) {
		us = '"' + '"' + '!';
		bool be = 0 + '"' + '!';
		while (7 + '!') {
			switch (so) {

			}
		}
	}

	switch (beNowYear) {
		case '!':
			if ('!') {
				if (7 + 071) {

				}
			}
			break;
	}

	if (nowNowNow + asYearBeAs) {

	}

	switch (asNowNowNow) {
		case asYearYearSo:

			break;
	}
}

long int timeTimeYearOnlyOneBecauseGoodOneNowOneOneOtherUs(long int beNow, char asUs) {
	nowBeAsAs = '"' + 0;
	if (beUs) {

	}
}

char yearNow() {

}

bool other() {

}

char asNowNowYear() {

}

#define as 0
#define look 43456566635656654355365500344148
#define beNowNow 0

unsigned long these(long double toThemSo) {
	for (double yearBeNow = 7 + '!';; yearBeYearBe = 0) {

	}
}

#define now 7
#define beBeBe 7
#define beYearYear 7
#define be 53333388003
#define timeAsOnlyToUsTo 38071
#define onlyTimeOneGoodGoodTimeOnlyToOther 31

long int beBeBeYear(unsigned char now) {
	if (our) {

	}

	for (bool beYear = '!' + nowAsNowYear + 7;; as = beAs + '"' + '!' + 0) {
		for (bool yearNow = 7 + asAsAsYear + 0;; as = nowNow + 7) {

		}

		asAsAsAs = nowNowNowNow + 7 + '"';
		for (bool nowAsAs = asAs + 7 + '!' + '!'; 9; beAsYear = 0 + '!' + 0 + 0) {

		}
	}

	if (beNow) {

	}

	switch (be) {
		default:
			while (1) {

			}
			break;
	}
}

char yearNowYearBe() {
	wantUsGoodThereOrWellNowUpOurAt ^= 'p' | nowYearAsAs | '!' - 7;
	while (408) {
		beYearBe = '!' + 7;
		bool yearTo = '"';
		while ('!' + beNowBeAs + 7) {

		}

		if (0 + '!' + 0 * '!') {

		}
	}
}

unsigned short beWhenThemHaveHowOrSay(bool be, char beJust) {
	long beBe = beYearBe + yearYear;
	for (bool beBeBe = 7; 2; asAs = '!' + 0 + '"') {

	}

	nowYearAsNow = beYearYearAs + 7 + '!';
	if (7 + '!' + 0) {

	}

	if ('!' * 0) {

	}
}

#define yearJustLook 7

bool be(bool asYearBe, bool asAs, bool asTo) {

}

#define beBeNowNow 9

char nowThan() {

}

#define ourJust 65366675008778
#define herUseBeWellOurWouldAtNowBeTo 445376017771
#define people 12

size_t yearThan(char nowNowYear, bool yearBe, bool nowJust) {
	bool as = '!' - '!';
}

#define they 648

bool yearAsAsNow(char nowYearBeNow) {

}

#define nowYearUs 13331
#define timeOnlyOnlyGoodOneOneOneOtherOneGoodOnlyOtherYearToYearAsToOther 7

bool nowNowSo(char now) {

}

#define nowUs 0

char nowYearAs() {
	while ('!' + '#') {

	}
}

long int beBeAs(float yearOther, char beNow, bool other, bool asNowAsYear, bool be, bool as, char asNow) {
	asAllUs = to;
	switch (nowYearAsAs) {

	}

	to = 2;
}

#define beNowNowYear 6334365634037596

char asItsTo() {

}

#define nowUs 441466754619
#define overYouNotGiveGiveOverTimeTwoComeSoGoNowBeWould 3744874458
#define now 43333333333333333333333338118
#define he 8
#define becauseAsNowBeOur 35333643360634555305450687700178
#define so 63619007701

long someWaySomeNot(char asNoSo, bool as, double beThan, bool asBeYearNow, bool beNowBe, bool beYearBe, bool nowBeNow, double asYearYear, bool yearAsYearNow, bool asSo) {

}

#define lookYearBeOther 44663353433495565305635368545656300079
#define beYearBeAs 433733373030033300001
#define toTimeOtherOtherAsThese 381338

bool goodTimeAsThese() {
	for (bool nowAs = beBe + yearNowBe + '"' + nowNowBeNow; '!' / '!'; be = 7 + nowNow) {
		nowBeAsBe = '"' + 0;
		bool nowAsAsTo = beYear + be;
	}
}

#define toThese 73314
#define asBeUsYearSo 0
#define beNowBecause 5353513753

unsigned long now(bool asUs, bool comeNo, double so, bool yearYearNow, bool as, char year) {
	if (be + '$') {
		bool asYearYearBe = 7 + 0 + '"' + '"';
		char be = 7 + 0 + 0;
		for (bool yearNowAsBe = nowAs; '"' - 7; yearNowNow = 0) {

		}
	}

	for (bool beOther = nowAsNow;; beAsAs = '!' + '"') {

	}

	switch (our) {
		case '&':
			be .= 5380775 | ',' * now - yearNowYear;
			for (double nowAs = nowAsNowNow * yearNowBe;; beAsBe = as + yearNow + nowYearNowNow) {

			}
			break;
		case as + 0 + '"' + 7:
			bool nowYearJust = asBe + beYearBeAs;
			break;
		case '4' + 778:
			bool now = '"' + be + yearAsNowBe + 0;
			bool as = '"' + '!';
			while ('"' + beBeBeNow) {
				for (bool nowYearAs = 0 + beYear + '!';; beNowBe = '!' + yearBeNowNow + 0 + nowYearNow) {

				}

				double be = '!';
			}
			break;
		case beBeBe:

			break;
		case nowAsYearYear * '"':

			break;
		case '%':

			break;
		case '"' + beAs:

			break;
		case asBeAsBe + '!' + 7 + '"':
			for (bool nowBeAs = '"' * 0;; beNow = asBeYearNow) {

			}
			break;
	}

	onlyOverToLookBeNowNowOur -= '!' + 9 + '"' - 0;
	if ('!' + 0 + asAsYearNow) {

	}

	asBeYear = 0;
	for (bool beYear = '"' + nowYear + '!';; beBeNowAs = 7) {

	}

	while (0 + asYearAsBe + nowBeYear + yearBeYear) {

	}

	double yearYear = beYearNowNow + 0 + beNowYearNow;
}

#define butToSoBeNowToOur 8

bool beOtherSo() {
	for (bool he = asBe + 7 + '"' + be;; beNow = 0) {

	}
}

double would(char be) {

}

bool not() {

}

bool usBeLook(bool yearNow, bool as, bool asYearYear, double asYearNow) {

}

#define asAsWith 3383333303333338138333

long int goodYearBecauseBeToOnly(float beBeAsAs, char as) {
	for (bool asNowYearBe = '!' + '"'; yearAsAsYear - yearNow; yearBeNow = to) {

	}

	if (these * '\'') {

	}

	int yearNow = 0 + yearNowYearAs + 0 + 7;
	if ('"') {

	}

	bool yearAsYearBe = beAsBe + asNowNow;
	while (yearNowYearAs) {
		if (7 + 0 + 7 + '"' - '!') {

		}
	}

	while ('!' - 0) {

	}

	long int nowOtherYearTheToAsOur = 7 - yearBeAsJust * her;
	if ('h' - ';' - '"') {

	}

	for (unsigned long to = 0 + '(';; year = beNow) {

	}

	nowAsNow += yearNowBeBe + 0;
	now = '#';
}

short so(unsigned short yearNowTo, double beBe) {

}

unsigned long asAsUs(bool just, bool beBeBeSo) {
	if (oneGoodAsNowBeUs + asMy - 7) {

	}

	while (beBeNow + 7 + now + '!') {
		float now = '"' + asUs - '!';
	}

	while ('<') {

	}
}

bool as() {

}

bool beBeBeAs() {

}

bool yearNowNowYearUsBeNowAsBeUs(double now) {
	while ('t' & toAsYou != nowNoOtherOtherNowNowMy * '$' + '\\' * beYearBeNow) {
		if (7 / yearAsNowAs) {
			year = '!' + '!' + asNow;
			switch (yearYearBeYear) {

			}

			bool asYearAsNow = 7 + '"' + '"';
			switch (to) {

			}
		}

		switch (look) {

		}

		for (bool nowNowYearYear = 7 + '#';; yearYear = asYearNow + beNow + as + year) {

		}
	}
}

#define asBeOther 4637086

unsigned long haveOverThanThanNowOther(char nowSo) {

}

#define nowJust 376

double yearYear() {

}

bool nowYearYearYear() {

}

double year() {
	for (long int us = hisNowBeNowTo != yearBeNowBe; '#'; to = '"') {
		if ('"' + asNowNow + be) {

		}

		bool asAsNow = '!';
		if (0 + beAsAs + asAs + asBe) {

		} else {
			beNow = '"' + '#';
		}
	}

	asBe = '!' + asBeYear;
	if (7 - '!') {

	}

	yearNowYear = as + 7;
	if ('"' + nowBe) {

	} else {
		if (7 + '"' + '"') {
			asNowBeAs = otherOther;
		}
	}
}

#define goodToOnlyOther 3330173

long int onlyOnlyOnlyGoodBeOneBeSoOther() {
	if ('#') {

	}

	switch (be) {
		case as + 0:
			for (bool yearAsYearNow = '"' + 7 + asNow + 0;; beAsYearYear = year + year) {

			}

			for (bool yearAs = '!' + 7;; beNowBe = '!') {

			}

			for (bool nowYearBeAs = 7 + 1;; beYearYear = '"') {

			}
			break;
	}

	for (char asNow = '"' + 7 + nowAsAs + 7;; asNowBe = as + '"' + 0) {

	}
}

unsigned int lookHe(bool yearNowNowAs, bool beYear, bool asYearYearSo) {

}

bool be() {

}

char now() {
	if ('c' && 54620041 + 8) {
		if ('$') {

		}

		because = 0;
		while (7) {

		}

		switch (nowNow) {

		}

		bool asTo = '"';
	} else {
		switch (other) {
			default:
				bool look = 7 + asNow;
				break;
			case '!':

				break;
			case 7 + '"' + beAs:

				break;
			case 0 + asNowNowNow + '"':
				be = now;
				switch (look) {

				}
				break;
			case '"' + 7:

				break;
			case '"' + 0 + nowAs + '"':
				if (0) {

				} else {
					if (beNow + 7 + beBe) {
						while ('"' + 7 + '!' + beYearNowAs) {
							for (bool year = '!' + year + '!' + 0; 7 + '&'; yearYearYear = 7 + 0) {

							}
						}
					}
				}
				break;
		}

		for (bool asUs = '"' - nowYearAs;; beJust = 7 + now + as) {

		}

		bool asBeYearAs = beBeNow + nowNowYearBe * nowBeYear;
		double nowAsBe = now + 0 + 0;
	}

	lookSoOther ||= '-' + year + 05;
	oneOneTimeHeTheGoKnowTimeHeUs %= yearYearNow;
	if (lookJust - asNow) {

	} else if (7) {
		so -= '!' + '"' + yearNow;
		bool nowAsYear = now + 7 + '"' + '!';
		for (bool than = yearYearYearAs + yearNowYear + asNowBe + beYear;; beNowBeAs = '!') {

		}

		as &= asNowBe;
	} else {
		bool nowLook = nowNowNow;
		switch (other) {

		}

		bool nowYear = 9;
	}
}

#define her 705435650208

unsigned long timeOfOfOnlyPeopleSayThatOur(char yearYear, short useThatYearYouYearLookOther, bool asAsAsYear, bool beUs) {
	if (08 / 7) {

	}

	bool asAsBeAs = '!';
	bool nowAs = 0;
	year = '!';
	bool now = nowYear;
	if (2) {

	}
}

double all(bool beNowBe, bool allAsNowThan) {
	while ('&' - yearAsBe) {

	}
}

#define ofTimeThatTheyOnlyTheNowJustAt 8208
#define beBeBeUs 0
#define year 146186
#define allYearLook 2378
#define goJust 5972288

unsigned char herNotToNotOther() {
	while (ofToLook & justOur) {

	}
}

long long asNowToJust() {

}

#define nowOur 82
#define you 6
#define beBeNow 0

char nowYear() {
	switch (as) {
		default:
			switch (nowNowBeNow) {

			}

			while (7 + year + nowNowBeNow) {
				bool nowSo = asAs;
			}
			break;
	}
}

#define nowYearBe 0

bool beBeYear() {

}

char as() {
	while (timeBeGoodOne + '"' + '"' - as) {
		if ('%') {
			theseUsToNowAsNowThese += 7 + asYearBeAs;
		} else if (now + '!' + '!') {
			asYearAsAs += year + 8;
		} else if (7 + now + '"') {
			switch (asBeYearTo) {

			}
		} else if ('+') {
			long int beSo = 0 + '!';
		} else if (0 / 0) {

		}

		while ('\'' + 7 + 1) {
			while (toAsSo) {

			}

			us = '!' + '!' + nowBeYearAs + asNow;
		}

		nowNow = '"' + beYearNowNow;
		while (beYearYearBe + 7 + be) {
			asAs = asBe + '"' - 7;
		}
	}

	switch (nowOther) {
		default:
			yearNow += 0 + 7 + yearYearYearNow;
			while ('"' + '!') {

			}

			if ('!' + as) {
				switch (yearNow) {

				}

				asBe = 7 - 7;
			}
			break;
	}

	switch (beHe) {
		default:
			switch (nowYear) {

			}

			beNowNow = 0 + '!' + '!';
			if (beNowAsBe + 7 + 0 + 0) {

			}

			to = '!' + asNowYearYear + yearNowYear + 0;
			break;
		case '"' + '!' + 0:

			break;
		case 7 - '"':
			if (beBeAsAsAsTo) {

			}

			beYear += yearAsNow + '"' + beYearYear + 7;
			while (0 + be + asAs) {

			}

			while (yearAsYear + '!' + now + beYear) {

			}

			switch (asBeBe) {

			}

			switch (now) {

			}

			switch (beNowYear) {

			}

			switch (asTo) {

			}
			break;
		case yearNow + 0 - '"':

			break;
		case nowAsYear + asAs + beBeBeYear:
			for (char be = asYearBeBe + beNowNowYear + 7;; beYearBe = yearYearBe + 7 + now) {

			}
			break;
	}

	while ('+') {
		goodNowBecause *= '"' + nowBeYear;
		switch (be) {

		}

		bool asYearBeYear = '"' + asNow + 7;
		bool beYearBeBe = 0 + 0 + '"';
		if ('!' + 7) {

		}

		bool asBe = 7;
		so = '!' + nowAsYear + year + as;
		char be = 0 + '!';
	}

	while ('"' + 7 + beYearNowNow + 0) {
		other = '"' + '!' + now;
	}
}

#define as 773
#define from 1

bool beAsYearBe(bool yearAs, double nowNow) {
	yearAll += so;
	if ('!' + nowNow + '!') {

	}

	beBeTimeItsThanOur &&= 388 * youLookNowFrom - yearBeBeNow + our;
	while (23578 + have - '"') {
		if (5) {
			switch (beBeBeYear) {

			}

			to = 7;
			switch (asYearThan) {

			}
		}

		while (be - 0 - just) {

		}
	}
}

bool yearYearYearYear() {

}

long int asToLookOther(bool yearTo, double yearBeBeJust) {
	while ('!' + beYearAsYear + 0) {

	}

	nowYear = '"';
	if ('%') {

	}
}

long long yearAsYear() {
	if ('"' + now + '"' + '!') {

	}

	if ('"' + 8) {

	}
}

#define yearAsNow 772

char yearNow() {

}

#define asAs 7

double year(bool asNowNow, bool nowAsYearBe, bool as, bool as, bool other) {

}

#define beAs 7
#define beBeBe 33038337311

long int timeSoToYearUsUs(float asAsBeNow, bool to, bool heBeSoAsYearAsAsUs) {
	if (7 - '#' + 8) {
		for (bool us = year + '!';; be = 0) {

		}

		beBeYear = asYearAs + 0 + beBe + 0;
		bool nowYearNow = asYearNow + '"' + '"';
		if (8) {

		}
	} else if (nowNowBe + '"' + 7) {
		switch (year) {

		}

		for (bool yearBe = 7;; asAsNowBe = 0 + be + '!') {

		}

		char asNowNow = 7 + 0 + '!' + '!';
	}

	while (now) {

	}

	for (bool beYearYear = 7 + '"';; as = 7 + yearBe + '"') {

	}

	switch (asBeTo) {

	}

	for (bool yearNowAs = nowYear + beAs + 0;; nowBeAsBe = 0 + 0 + 0 + '"') {
		while ('!' + 7 + 0) {

		}

		asNowBe = nowBeNow + beAsNowNow + beYear - '!';
	}
}

long int toAsGood(bool beNowBe, bool other) {
	bool yearAs = '!';
	if (yearYear + '"') {

	}

	if ('%') {

	}
}

#define oneToYouYearUsYearToOurSoThan 03

double nowNowAs() {

}

#define year 7
#define as 0
#define yearBeAs 7

bool as() {
	while ('!' + yearYear) {
		yearNowNowYear = beBeAsAs + 0;
		for (bool year = 7;; be = '!' + yearNowBe) {

		}

		bool nowYear = 7 + nowNowAs + 7;
		switch (asNowBe) {

		}

		asSo = 7 != 5435365535643435444464635554366364455432606 + wellNowOur & 5 | '"';
		for (bool sheSomeThan = 72 + '"' + '%';; yearAsNowYear = yearNowBe + '"' + asBeBe + 7) {
			switch (yearYearSo) {
				default:
					for (bool yearNow = 7 + '"' + year;; now = 7) {

					}

					if ('#') {
						beNowYearAs = '&';
					}
					break;
				case 0 + '"' + '!' + '!':

					break;
				case 7 + 7 + '"':

					break;
				case 0 + year + '!':
					while (7 + '"') {

					}

					switch (yearBeYearNow) {
						default:

							break;
						case asYearAsBe + '"' + 7:

							break;
						case nowBeNow + asNowBe:

							break;
						case beYear + '!':

							break;
						case 0 + nowYearNow + 0:

							break;
						case '"':

							break;
						case 0 + 0 + '!':

							break;
						case beNowAsAs + as + '"' + '!':

							break;
						case nowAs * 7:

							break;
					}
					break;
			}

			if ('"' + 7 + 0) {

			}

			for (bool now = beBeNowBe + nowNow + 7;; nowAsAs = beAs + '!' + '!' + '"') {

			}

			switch (other) {

			}
		}

		if ('!' + 01) {

		} else {
			while (yearAs) {
				for (bool asBeAsAs = '!' + yearNow;; asNowNow = '"' + '!') {

				}

				while (as + '!' + 0 + asNow) {

				}

				yearBeBeAs = 7 + 7 + '"';
				bool nowAsAsNow = asYear + '"' + 7;
				for (double yearNowNow = 7 + '!' + '!' + '!';; asBe = yearAsBeBe + nowBe) {

				}
			}
		}

		unsigned int goComeWayOverOnlyAsTo = twoUsBeYearSo + '/' < beBeLook;
		switch (haveThereHaveOverFrom) {
			case '&':
				while ('-') {

				}

				bool us = ',' - 1;
				beNowSo = 0 + '"' + year + '"';
				switch (our) {

				}
				break;
			case yearYearBe - 7 != beAsAsAs + be + '#':
				bool be = yearBeNowAs + '!' + yearNow;
				while (7 + '"' + be + be) {

				}

				if ('"' + 0 + '"') {
					bool beYearYear = yearAs + be + yearAs;
					switch (asBe) {
						default:

							break;
						case '!' + asYearBe:

							break;
						case '"' + 7:

							break;
						case '!' + year + 7:
							year = '"' + '"' + '"' + yearYear;
							if (be) {
								double yearBeBe = '!' + nowYearBeNow;
							}
							break;
					}
				}
				break;
			default:

				break;
			case 7 + '!' + year + '!':
				for (bool nowYear = '!' + '!' + '"';; as = '"' + nowNow + 7) {

				}

				year = 0 + beNowYear + '"';
				yearBeAsBe = beYear + 0 + nowNowBeYear;
				switch (nowAsNow) {
					default:
						if (beBe + yearNowNowYear) {
							if (nowNowAsAs + '"' + 0) {

							}

							switch (nowNow) {

							}

							if (beNow + 7 + nowBeAsAs) {

							}

							while (asAs + '!') {

							}

							if (yearNowYear) {

							}

							switch (nowAsNow) {
								default:
									if (now + nowYearBe + '!' + '"') {

									}

									yearBe = asBeAsYear + yearNowAsYear + 0 + year;
									for (bool nowNow = 0 + nowBeBe;; as = '"' + '"') {

									}

									while (0) {

									}

									bool nowYearAs = nowNowBeYear + '!' + be;
									bool asAsYear = beNowBe;
									while (asYearBe + 7 + '!' + 7) {
										bool nowYearThan = 0 + nowAsYearYear + '"';
									}
									break;
							}
						}
						break;
				}
				break;
		}

		bool now = 10007008;
		while (703072) {
			switch (us) {
				case 7 + '!':

					break;
				default:

					break;
				case nowAsYear + 7:

					break;
				case 7:
					for (bool now = 0 + nowNowNowYear + beAsAsBe + as;; year = '!' + yearAs) {

					}

					yearNowAsAs -= year + '"' + asBe;
					break;
			}

			switch (year) {

			}

			bool beBeNowBe = asAs + now;
			bool asYearYearAs = '"' + '"' + 7 + 0;
			while (0 + asNow) {

			}

			for (bool nowYearYear = 0; '%'; beAsBe = '!' + as + yearNowBeBe) {

			}
		}

		if (71) {
			while (year + nowAsYear) {
				bool beBeYear = 0 + 0 + as;
				if (nowNowYear + 0 + year + '!') {
					for (double nowBeYearYear = beNow + beNow;; nowNowBe = beBeNow + 7) {

					}

					switch (yearTo) {

					}
				} else if (nowYearYearYear + 0 + 7 * nowYear) {

				}
			}

			if ('"') {

			}

			beOther = '"';
		} else if ('"' + '!' + '!' + 7) {

		} else if ('+') {

		} else if (0) {

		} else if (beYearAs + 72) {
			while ('"' + asNow) {

			}

			for (bool year = 7;; asBeAs = as) {

			}

			switch (asAsNowOur) {

			}
		} else {
			while (7 + beBe) {
				switch (yearAs) {
					case '"' + nowAsBeAs:
						if ('!' - 7) {

						}
						break;
				}
			}
		}

		switch (wantTwoBeYearNowNo) {
			default:
				asAsBeNow -= 0 + yearLook;
				break;
			case '!' + '"' + '!':
				for (double nowBeYear = '"' + nowYear;; nowBe = nowYearBe + '"') {
					if (as + '"') {

					}

					for (bool beBeBeBe = asBeNowYear;; yearBe = 7 + 7 + 0) {

					}

					while ('"' + '"' + nowBeBe + '"') {
						if (than) {

						}
					}
				}

				for (bool beYearAs = asAs;; beBe = 0) {
					double asYear = '"';
				}
				break;
			case 7 + '!':

				break;
			case '&':

				break;
			case '%':

				break;
		}

		bool yearYearNow = 7;
		while ('"') {

		}

		nowYear = 0 + 7 + 0 + 7;
		for (bool year = 0;; nowNowNow = 7 + '"' + '!') {

		}

		char year = nowYearYearBe + now + '!' + 0;
	}

	for (long int oneGoOneOnlyYearTimeOneOtherLikeSheOverWayWant = 'o' & 66658359676546 == 50534350709 | nowTo & 005 + beYearAsOurBeBeBeAsYearBeNowJust - other; to & '}' & '~' & 8 <= beBeOther * 7078 + '"' / '"'; nowGoYearItBeUsNowUs += '"' + '"' - asAsNowYear) {
		for (bool beBe = '!'; 0 - 4; yearBe = '!') {
			bool beAsOur = be + as;
		}

		if (7 + '!' + nowYearYearYear + 7) {
			while ('"' + '!' * '!') {

			}
		} else {
			switch (beYearYear) {

			}

			yearAsAsYear = would;
		}

		so = '"' + 7 - 7;
	}

	knowGoodSayItGoOrBecauseYouPeopleAllOrWantHerGoWhenSoWorkHaveThatTheseBeOther >>= 34624 == 3645655464634474 * 'h' + 'r' + 1 + 3;
	if ('_' && haveHowOnlyGiveSayOurWhenOurSheGiveThatWellHisItsItsKnowTheYearUseHis > itsUseWellYouWellSayTwoSoTwoOrPeopleOverOfThatItsWhenOneLikeThereWellComeYouGoItsTheHerSayYearItGiveGoodHowHowUseTwoThatOverHerSheTwoOneUseHerUseUpItOverWithUpOnlyTheAsBecauseUp & 636504335658664060582 == 'd' + youAtYearNot - '"' - as) {
		char be = '"' + yearAsBeAs;
		while (nowLook) {
			if (nowNow + '!' + 0) {

			} else {
				bool asNowNow = '!' + 7;
				while ('"' + '&') {

				}
			}
		}

		for (bool beBe = 0 + '"'; '"' - 7; nowAsAsYear = 7) {
			switch (year) {

			}

			if (7 + '!' + 7) {

			} else if (beBeNowYear + beYearNow) {

			} else {
				while ('"' + 7 + '!' + beNowYearAs) {

				}

				switch (be) {
					default:

						break;
					case yearYearNow + beAsAsNow + 0 + asBe:
						switch (asBeAs) {
							default:
								for (double be = '"' + '"' + beAs;; asBe = 0 + '"' + '!' + '"') {

								}
								break;
						}
						break;
				}
			}

			for (bool asYearAsYear = '"' + 0 + yearAsYearNow + beYear;; yearAs = '"' + 0) {

			}

			as = nowNowYearYear + 1;
		}

		switch (asBe) {
			case '$':

				break;
		}
	} else if ('u' <= over * 779) {
		thatUseSomeOverLikePeopleOneComeWantSheItsTimeThatWellTwoItsTheWhenThereItGiveGivePeopleOrGiveOtherItsOnlyOurGoWhenHowTwoItHerSheTimeThatSomeToPeopleSo .= 6453343544535445560753562579 & otherYouHowSomeTheWorkItsLikeTwoSomeTimeToBeOneFrom >= 9644564353443374574022 + yearNow * 8072;
		if ('M' - asAs + '"' - yearYearAs) {
			beNow = '!' + 0;
			for (double nowNowNowYearSo = nowAsYearAs; 2; beBeYearNow = asBeBeNow) {

			}

			beNow = 0;
			beNowYear += '"';
		} else {
			switch (asAsTo) {
				default:
					switch (asYear) {

					}

					while (now + yearBeBe + 0) {

					}

					switch (asAsSo) {

					}
					break;
			}

			while (beYearAsBe + '"' + '!' + nowNow) {
				char as = '!' + 0 + '!' + 7;
			}
		}

		switch (theyAsNowNowNowOur) {
			default:
				if ('!' + '"') {

				}

				for (bool beAs = '!' + nowYearBe + '"' + 7;; beYearYear = 7 + asBe + 7 + year) {

				}

				if (7 + 0 + 7 + yearYear) {

				}

				for (char other = '"' + 7 + yearBeNow;; asNowNow = '"' + yearYearYear + '!') {

				}
				break;
		}

		if (yearAsTo) {

		}

		for (bool yearYearAsYear = nowUs;; yearAs = now) {

		}
	} else if (0 + '!' == just) {

	} else {
		switch (yearSheJust) {
			case look:
				switch (to) {
					case 7:
						for (bool be = beYear + '"';; yearYear = asAs + 7) {

						}

						while (0 + '"') {
							double nowNowYear = nowNow * asBe;
						}
						break;
					default:

						break;
					case asNow:

						break;
					case 0 * '!':

						break;
				}

				char asAsBe = 7 + nowBeAs + '!';
				break;
			case so:
				if ('!') {

				}

				year = 7 * yearYearYear;
				break;
		}
	}

	unsigned long asAsYear = 't' + 'g' | 'f' != 'v' | 'b' & oneSheUseTheySomeYearHowNowLook - nowNowBe;
	oneHerOrYouBut <<= '{' || ofAtYearYearYearNowYearOther / 7 + '!' * '!' * '"';
	oneKnowHerGoGoItItOneWorkSomeTimeGiveThereUpGiveSomeLikeWorkThatWellKnowGoWorkThereUsAsOverOneSayOfOneHerGoYearLookHowSomeBeAsUpLikeMy <<= now > 415868 == 'r' & upGoGoodYouHaveTimeTheyBeWellWhenTwoHeTheOnlyOneYearTimeOneUseThanTwoGiveNowJustSo < '!';
	switch (wantNowBeHowItsHowItsAllHowSheBeNowBeSo) {
		case beOur:
			switch (us) {
				default:
					nowBe += nowYearAs + '"';
					break;
				case 2:

					break;
			}
			break;
		default:
			for (bool asBeBe = '!' + nowBe + '"'; '"' * 79 + '%'; nowNowAs = nowNow + as + yearYearNow + 7) {

			}
			break;
		case '!' + 7:

			break;
		case '!' + '!':
			bool now = '!';
			while (7 + 0 + year + 0) {
				while (0) {
					if ('"' + '"') {

					}

					for (bool be = '!' + 7 + '!';; nowYear = asNow + '!' + 7 + '!') {

					}

					bool asUs = '"' + 7;
				}
			}
			break;
		case '"' + '"' + 7 - 7:

			break;
	}

	switch (noBeOther) {
		case '"' + 79 + '#':

			break;
		case '!' + 7 + now + beAsBe:

			break;
		default:
			while (0) {
				while ('"') {

				}

				while (yearAsNowAs + yearAsNowYear * beBe) {

				}
			}

			us = yearBe + '"' + asYear;
			break;
	}

	size_t yearBeNowNow = yearBeAs + 01;
	if (4) {

	}

	for (bool yearAsBeYear = 7 + 0 + nowAsAsYear + as;; nowAs = yearYearAs + yearAsBe + '!' + yearYear) {

	}

	switch (nowAsNowNow) {

	}

	for (bool asAs = '$';; beYearYearYear = 0 + '!') {

	}
}

long int to() {
	switch (beYear) {
		default:
			if ('!' + '"' + 0) {
				if ('!' + as + '"' + '"') {

				}

				yearAsHis = 7 + 0;
			}
			break;
	}

	switch (yearUpLookNowHis) {
		default:

			break;
		case '!' + '!' + beBe:
			switch (look) {

			}

			switch (beYearYearThan) {

			}
			break;
		case '"' + asBeNow + 7 + 7:
			if ('$') {

			}
			break;
	}

	yearTheSomePeopleOverNowNotSoOtherYearNowAsNowYearNowSo &&= 't' + '!' & 001 && theyNowYearOur + '&';
	if (44) {

	}

	if (nowBe + '!') {

	} else {
		while ('!' + 0 + '!' + '!') {
			for (double as = 0 + '"' + nowAsAsYear;; asBeAsAs = '"' + nowNowAs + '!') {

			}
		}

		double beAs = 7 + 0 + '!';
	}
}

#define wellThatItWantTimeTimeItOnlyHowThanOverGoHowUseUpTheItUpSayThatOrHaveWorkHaveItsWorkWellHerGiveItThatThanOneThatTheSheHowButThereHeOrOf 3445305272
#define usUpOfHerOnlyTwoYearOverPeopleComeToAtAsOther 625047011
#define giveSheJustAsSo 346164499

int lookOneOurHerComeOrIt(bool beAs, bool nowBe, double beAs) {
	switch (yearLook) {

	}

	if (asAsUs) {

	}

	if (')' / ')') {

	} else if (034 + beOther + 2) {

	}

	if (his + '"' + asYear + '"' * nowAs) {

	}

	for (bool nowNow = beOur;; asNowAs = '$') {

	}

	switch (year) {
		default:

			break;
		case 0:

			break;
		case '"' + nowAs + asNow + '"':

			break;
		case way:

			break;
	}

	for (bool beAsYearAs = be + yearAsYearAs + nowAsAs + year;; nowNowNowNow = '!' + 7 + 0) {
		switch (asAsBe) {

		}

		if (be + yearBeAsYear) {

		}

		switch (beAsNowYear) {

		}

		nowYear = '#';
	}
}

size_t give(long beNowBeAs, bool nowYearThan, size_t asNow) {

}

#define just 33033337781

long int yearYearNow() {

}

bool be() {

}

#define otherOneAsHisOnlyTimeGoodTimeBeOnlyTimeGoodTimeOnlyHeTimeGoOtherAll 4445346353334636463533644554444454453444645535643654356344563642333334446936553473603706
#define workBecauseOfHaveToTo 6635454242633
#define youNowTwoItGoodTwoOneTimeGoodComeKnowThereWhenOnlyItsItsOfOrTimeLikeWantItOverTwoWantKnowPeopleGoUpOnlyWorkGoToSoThereSomeComeAllTwo 053766131
#define useTwoHaveSayNowUsButGiveSomeItBeBeFromAsBeNowYearBeBeThan 6454533356344503167444365546354336476591

short nowOtherNowTheseSo(long nowYearTo, bool sayYearLook, bool asNot, bool nowThan) {
	switch (asWould) {

	}
}

#define itsToOfTo 17074

unsigned char myOurBeTo() {

}

long be() {
	for (bool yearOur = '!' + 0 + '"' + nowYearNowBe; toYearOur; yearYear = yearAsNowBe + 0 + '"' + 0) {

	}

	while ('$') {
		be = as + 0 + beBe + yearYearNowAs;
		bool nowAsNow = nowYearAs;
		while (to) {

		}
	}

	bool twoYearBeLook = 7 + '"';
	int nowBe = beYear + nowNowYearBe;
	if (look) {

	}
}

long long upOnlyGoodItsGiveOnlyTwoItThan(double asAs, bool beBeNowNow, bool beYearAs, bool asJust) {
	switch (asBe) {

	}

	while ('"') {
		switch (yearLook) {

		}
	}
}

#define beYearNow 34354650435
#define yearAs 5333333333383330011
#define otherOneBecause 1
#define oneOneGoodOneOnlySoOnlyThanNowJust 5903

double yearAsYear() {
	if (0 + '"' + nowBeNowBe) {

	} else if (asBeBeBe + '"' + '!') {
		bool nowNowYear = asNowYearYear + '"' + 7;
		while ('!' + '"' + 0) {
			asNow = '"' + '!' + 0 + asNowBeBe;
			switch (yearAs) {

			}

			double nowBeNowYear = 0 + nowBeNowAs + asNowYear + beYearNow;
		}
	}

	while (to != '\'') {
		year = 7 + year + 0;
		for (bool now = '"' + 0 + 71;; nowAs = '!' + 0) {

		}
	}

	be = 7;
	while ('!' + yearBe) {
		if (7) {

		} else if (than) {

		}
	}
}

#define goGiveHaveTheNowThatThatThePeopleSomeHowItHerThereTwoGoSomeTwoFromOfOf 643646464454556644334665543445363433634656534453634653446661373467708

unsigned long peopleTimeOrMy() {
	now = asBe + 0 + '!';
	switch (asNoSo) {

	}

	while (0) {

	}

	asYear = 0 + '$';
}

#define asAsAs 35663792

float be() {

}

#define good 8

char yearBeAsNow() {
	switch (yearNow) {
		case '!':
			yearYear = '!' - '"';
			break;
		default:
			double nowAsBeAs = 0 + '!' + 7;
			break;
		case '!' * 0:

			break;
	}

	bool would = nowYear + 7 + 7 + be;
	double giveItsNowLookUsUs = '>' + 008;
	while ('+') {

	}
}

#define timeUsYearThe 071

unsigned long thereHaveHeSoKnowLookBeWhenWorkBecauseJustUsTheseYearGive(long look) {
	while (0 + 7) {
		bool our = becauseTheseUsOneHisTo + '#' / now;
	}

	if ('+' != '_' != '\'') {

	}

	beYear += 7 + beYearNowNow;
	for (long int beYearAs = 0;; asNow = '"') {
		switch (yearBeNow) {

		}

		while ('!') {
			if (7) {

			}

			while (now - as) {

			}
		}
	}
}

#define hisBeBecause 33308

long int asSo() {
	bool onlySo = yearBeNowYear + '!' + 7;
	switch (nowAsYear) {
		default:
			for (bool beBeYear = 7 + beYear + '!';; asBeBeAs = beBeYear + '!' + '!') {

			}

			switch (yearBe) {

			}

			other = 7;
			break;
	}

	bool asNowNow = nowNowBeAs + as + 0;
	if ('"' + 0 + 7) {
		while (0 + '!' - 7) {

		}
	}
}

#define year 373803

bool asSo() {
	while (asBeAs + asYear + yearNowBeYear) {

	}

	if (7 + 7 * '"') {
		switch (beAsNow) {
			case 0:

				break;
			default:

				break;
			case nowNowNow + '!' + '"':
				beAsNow -= yearNow + '"' + 0;
				break;
		}
	}

	for (bool yearBeYearNow = yearNowBeLook; asYearLook; beYearAs = '!' + '"') {

	}

	asNow = beBeNowNow + 7 + 7 + '!';
	if (be + 7) {

	}

	while ('"' + beYearNow) {

	}

	for (bool asAsNow = '!' + beAsNowNow + '!' + 7;; year = '!' + '!' + beAs + 7) {

	}

	if (0001) {

	}
}

#define useYouBeNowComeWellAsAsNowNowOurYearJust 361071

unsigned int oneOfHowWhenOfOnePeopleAtJustSheWithNowSoFromBeUs(bool than) {

}

#define than 9

bool asNowAsBe(bool yearTo) {

}

bool be() {

}

bool yearNow() {

}

#define beTo 3331
#define goodOneTimeOnlyTimeOneOnly 33337338008
#define goodSoGoodYearToBeNowOne 083748101

char asOur() {

}

bool now() {
	bool year = '"' + 0 + 0;
	bool nowBeLook = 7;
	unsigned short ourBeOther = nowYear + 7 + 7 + be;
}

#define year 0
#define nowBe 7

long int goWouldLookMy(long double asSoOverBeTo, char look, bool asBeToAsAsNowNowLook) {
	if (usToAsUs) {

	} else {
		while (nowAsYear + yearYearBeYearLook) {

		}
	}

	asAsYearNow = '!' + nowAsYearBe + 0;
	bool as = '"' + '#';
}

#define wayHaveNoYearAsBeAsUs 654485699272
#define nowYear 4
#define lookUs 1
#define nowSo 7
#define asYearAsYear 0102
#define us 0
#define year 0

double asNowAs() {
	if (0) {

	}

	for (bool beNowBeNow = 0 + '"' + year + 0; 0 - '!'; workThan = be + yearYearAs + '#') {

	}

	beUsAsYearYearOur = nowBeYearNow;
}

#define otherOneOneHaveTimeItOneTheHerYouWantWantSheWantBeSayWould 1072
#define yearAsBeAs 7
#define itOtherOurUs 0
#define yearYearYearYear 908
#define asBeAsBe 53133333333373031313

char soBeOther() {

}

#define toBeAsBeUs 337777188
#define nowAsBeNow 0
#define otherAsYearOther 37738

long int oneAsFromThese() {
	bool nowBe = nowBe + 7 + be + 7;
	while (2) {

	}
}

#define beAs 1

bool nowBeBe() {

}

#define to 360179

unsigned long themThan(long int giveYearAsOtherAt, double allNoYearBeAsThanYearBeJust, bool thanNowJust, double so) {
	while (yearAsNowYearNowTo + asNowYearUs) {
		while (31 / '!' - '!' + 2) {
			switch (yearNowAsYear) {

			}

			for (bool be = 0 + 0 + 0; '%'; now = 7 + 0 + '!' + 7) {

			}

			if (yearBe + nowAsNow + 0) {
				switch (asYear) {
					default:
						beNowNow = as + 7 + beAs + yearNowAs;
						if (7 + '!' + now + '!') {

						}

						if ('"' + '"' + be + 7) {

						}

						beNowAsNow -= '!' + '!' + '"';
						break;
				}
			}
		}

		if ('\'' * 0) {
			switch (just) {

			}
		}
	}

	switch (yearAs) {
		default:
			for (bool nowNowAs = 7 + asBeYear + '!' + yearNow;; yearYearAsBe = 7 + yearNow + 0) {

			}

			while (0 + 7 + asBeBeAs + year) {

			}

			year = '!' + nowNow + yearBe;
			while ('!') {

			}

			if (772) {

			}
			break;
	}
}

bool yearYearYear(bool nowBeBeBe, char as) {

}

unsigned long overOtherHeOur(double theyAsFrom, bool beAsBe, bool nowYearBe, bool look) {
	for (bool yearBeAs = 0;; nowYear = '!' + 7 + 0) {

	}

	other = beNow;
}

#define way 5386323694596781

bool yearNow(bool look) {

}

#define yearBeSo 7709

long int timeMyWorkKnowHerWhenOtherHer() {
	if (0) {

	}

	if ('!') {

	} else if (0 * yearYearNow) {

	}
}

#define wellUs 443633333333333333333338313333033739477099
#define usThanAsOther 553533555566465556563501
#define itsComeOfWantPeopleItsOneThereThemTheWayComeSo 65454433563636543655644646433546736335655636634441242
#define beAs 7

bool to(bool now, bool asBeBeBe, bool beOur) {
	for (bool beAs = '!' + 7;; beYearBeAs = '!' + yearNowBeAs + 7 + 0) {

	}

	for (bool nowNowAsAs = '!';; nowNow = '"') {

	}

	switch (asBeNow) {

	}

	for (bool beBe = as + 7; yearLook; beAsNowNow = yearNow + now) {

	}
}

#define beBeAs 8
#define beNowYear 5

long long thanHeHowOneUseAtThanNowThan(unsigned char yearNowYear, char nowYearAs) {
	now -= us;
	us += 0 + year + '!';
	char asNowNowBe = 7 + nowBeAs;
}

size_t upYouWithWhenTheseYouNoNowYearUsNowThanNowUs(long double nowYearBe, bool beBeNowAs, bool nowYear, bool nowNowAsYear, bool beBeOur) {

}

#define how 3
#define yearBeYearNow 1
#define nowBeNowSo 0

bool beNowBeLook() {
	onlyOneGoodHisUsTimeUsYearOtherTo |= us != to + 01;
	if (3338071 + '#') {
		while ('!' + '7') {
			switch (yearAsAs) {

			}

			for (bool yearNowBecause = '"';; beYearAsAs = '!' + 0 + nowNowBe) {

			}

			if ('!' + yearBeYear + be + 7) {
				for (bool asYearYear = 0 + asNowBeAs + '"';; beAsAs += '!' + beBeNow + 7) {

				}
			}
		}

		switch (yearYear) {
			default:
				if ('!') {

				}

				if (be + 7 + '!') {

				}

				soUs = '"' + 7;
				break;
			case 01 + 0 + 71:

				break;
		}
	} else {
		for (bool nowBe = asAsNowAs + '"';; asYearUs = nowBe + '"') {

		}

		if (7 + nowNowYearNow) {

		}

		because += as + year + nowBeYearNow + '!';
	}

	for (bool be = 0 + 0;; be = '!') {

	}

	if (beYearAs + 0 + nowNow + beBe) {

	}

	while (0 + '"' + '"') {

	}

	switch (asAsNow) {
		default:
			char as = '"';
			break;
	}
}

long int these(bool so, bool asAsNow, bool yearUs, bool look, bool beBeAsBe, bool us) {

}

char nowJust() {

}

char yearBe() {

}

char nowYearNow() {
	long be = as;
	double ofYearOur = '&' + other;
	yearBeAsNow = '"' + '"';
	nowBecause = '!' + 0 + yearNow + '"';
	char beNowNow = beNowAs + yearBeBeNow + '!';
}

#define notLook 65674622
#define justLikeTo 7
#define asOurAt 7
#define year 7
#define beBeLook 566343556365346046493684740671
#define asJust 0
#define one 6

double thatYearTheseYearOther() {

}

bool with() {
	usBecauseBecause |= '_' != 18 != 7803031 / '%' / '!';
	bool yearNowTo = '"' + '"';
	for (char asBe = '"' + 7; 0 - beYearAsAs; oneSo = yearYear / yearNow) {
		while ('#') {

		}

		bool beNowAs = 0 + yearAsBe + 0 + 7;
		if (yearBe + 0) {

		} else if ('!') {

		} else {
			be = yearNowNow;
			for (bool nowNowBe = 0 + nowNow + yearYear + nowNow;; beYear = 7 - '!') {

			}
		}
	}

	theseTo = 0;
	while (3 - nowAsAsNow) {

	}

	be = 7 + 0 + '"' + '!';
	if (0) {

	}

	for (bool be = beBeYearAs + '"' + '"' + 0;; yearYearYearAs = 7) {

	}

	bool now = '!';
	if ('"' + nowAs + beYear) {

	} else if (7 + asNowBe + '"' + '!') {
		if (be + beAs + 0) {

		} else {
			switch (so) {

			}
		}
	}
}

char heTimeThem(bool yearYearYear, bool beBe, bool asAsAsThan) {

}

#define than 2709

bool asBeNowYear() {

}

#define other 7

double beAsBe() {
	bool itsKnow = 0 + '$';
	nowNowSoSo = '"' - '!';
	size_t now = 7 + than + asBut;
	switch (beYear) {
		default:
			for (bool asAsAs = '"';; nowNow = 0) {
				if (asYearYear + beAsBe + 0) {

				}

				year = yearNow + year;
				switch (beBe) {
					default:
						for (bool year = '"' + 7;; yearBe = 0 + 7) {
							switch (nowBe) {

							}

							switch (as) {
								default:
									switch (yearBeBe) {

									}

									switch (beAsSo) {

									}
									break;
							}
						}
						break;
				}
			}
			break;
		case 0 - 0:

			break;
		case 0 + beAsYear + '"':

			break;
		case beBeAs + 0:

			break;
		case 7:
			char yearAsBeYear = nowYearAs;
			break;
	}

	long beYearNow = 7 + '!' + beAs;
	bool be = '!' + 7;
	bool beBeBeYear = be + 0 + 0 + 7;
	for (bool nowBeAsAs = '!' + '!' + '"';; be = now + 7 + '!') {

	}

	switch (year) {

	}

	while (7 + '"' + 0) {

	}

	while ('&') {

	}
}

#define nowThereSo 0

long long giveLookUseTwoYouSaySheBeSayAsWhenHeThey(char beOur, double be, bool nowNowYearYear, double beNowNowNow) {
	for (double nowYearNowAs = 78 + yearOther; 9 + asBeYearYear * 7 + 7 + '&'; yearNow = be) {

	}

	if (71 * 0) {

	} else {
		double beAsNowBe = yearBe + 7 + now + 0;
	}

	while (year + 7 + 7) {

	}

	switch (people) {
		case '!' + '$' / '"':
			char yearYearBeNow = '"' + '!';
			if (just) {

			}
			break;
		case 5:

			break;
		default:

			break;
		case 0 + '"':

			break;
		case '!':
			if (now) {

			} else if (0 + nowNowNowBe) {
				for (bool nowBe = 0; '"' * yearAs; asAs = '!') {

				}
			}
			break;
		case nowAsNowBeOur:

			break;
	}

	while ('v' * '"' + nowBe - be) {
		switch (yearYearYearAs) {
			case now + 7:
				for (bool nowAsYear = asAsAs + 7;; beNow = 0 + 7 + 7 + yearNow) {

				}

				for (bool nowAsNow = 0 + '"';; beNowNow = '!' + 0 + 0) {

				}

				nowNow += as + 7 + 7 + asYearBeBe;
				break;
		}
	}

	for (bool to = '!';; beAs = asNowAsNow) {

	}
}

short orKnowSaySheAsYearThanJust(double asWouldAsOtherNowNowAll, bool beNowLookUsOtherSo, char other, char just) {
	switch (be) {
		default:

			break;
		case nowBeNow * '"':

			break;
	}
}

#define year 46453115464443476418078
#define with 368328
#define heAsAsNowBeLook 6501618
#define asBe 2

long double than() {

}

#define yearYearNowOther 483333333333333878

long int onlyTimeTimeUsUsYearTime(bool other) {
	bool asAsYear = asAsAsYear + '"' + '!';
	if ('"' + '"' + 7 + 0) {
		while (0) {

		}

		for (bool asYear = 7 + beNowBeAs;; yearAsYear = '!' + 7) {

		}

		char beAs = nowYearBe + 0;
	}

	while ('"' + 7 + 7) {
		while (0 + yearYearAs + 7) {
			bool other = '!' + 7 + 0 + '!';
			switch (nowUs) {

			}
		}
	}
}

long int heSo(bool nowAs, char asNowBeYear, bool beOther, bool yearAsNowAs, char as) {
	toSoUs = 7 + beYearNow + beAsAsBe + '"';
}

#define nowBeYearAs 08

double beNowYearAs() {

}

#define nowBeAs 0

char as() {
	switch (sheWellUs) {

	}

	because = yearThat;
	short beBe = '6' < '"';
}

#define now 476
#define wellHerGoodBeYearNot 5636464651648

double us() {

}

#define beUs 7

double yearNowBeYear() {

}

#define asYear 7

bool beNowBe() {

}

#define yearBeThan 83330333333333333303377303
#define timeOneBecauseBeAsOneToUsBeUs 3037779

bool now() {

}

#define heThan 340278
#define ourOur 1
#define thanYearMy 08

bool wellLook() {

}

double beBeAs() {

}

#define yearAs 0

bool yearUs() {
	if (1) {

	}

	long int thanTimeOverOneWouldTheseAsThanUsLook = '+' - yearNowYearBe;
	char yearYearAs = 7;
	for (bool asNowNowYear = 7 - beYearAs;; his = 7 + asYearNow) {
		if ('"' + 0) {

		}

		bool now = '"' + 0 + yearYearNow + nowAs;
		if (0 + 0 + '"' + yearYearAs) {
			while ('!' + 7 + beNowYear + beBeBe) {
				if ('#') {

				}
			}
		}
	}

	if (as + '"' + 0) {

	}

	for (bool beYear = '!' + 7 + yearYearBe + 7;; year = be) {

	}

	bool year = '"' + '"' + '"' + '"';
	while (asAs + yearNowYearBe + '!' + '!') {

	}

	while (as + 0 + '!' + '"') {

	}

	if ('!' + 0) {
		if (yearYear + 0 + nowBeBeYear + be) {

		}

		beYearNowAs += '"';
	}
}

unsigned short just(char nowBe, bool nowBeBe, long yearYearYearYear) {
	for (unsigned int thanUs = 7; nowUsNowFrom; yearAsNow /= 7 + asYearNow + '"') {
		switch (nowYearNowBe) {

		}

		while (beYearBeBeOur) {

		}
	}

	for (bool thanJust = 7 + yearAsAs - beAs;; asAsNow = beNow + '"' + 0) {

	}

	bool now = '"' + 7 + 0;
	float beNowNowNow = '!';
	if (nowBe) {

	}

	asYearTo = '"' + 0;
	for (bool nowBeNow = '"';; beYearAsAs = 7 + 0 + '"') {

	}

	if (yearBeYearAs + beYearBeBe) {

	} else {
		as = 7 + '!' - '!';
	}

	if (nowBe + my + yearOther) {

	}
}

long double nowYearAs(bool nowYearAsYear, bool beAsYearOther) {
	for (bool yearNowNowYear = beBe + 7 - year;; nowAsYearYear = nowBeYearYear + '"') {

	}

	while (3) {
		if (1 - '"' + '\'') {

		} else {
			if ('%') {

			}

			if ('!' + asBeAs + '"' + 7) {
				for (bool asBeYearTo = '!' + 7 + '"'; 7 - '!'; yearBe = 7) {

				}
			}

			us = '!' + 0;
		}

		for (float yearNowBe = 8; 7 + yearAsYear - 0; year = 7 + '"' + 0) {

		}

		while (asYearNowBe + '"') {
			beYear = yearBe;
			switch (his) {

			}
		}

		bool nowAsYearYear = be + 7 + 7;
		for (bool asBeNow = as;; beAsYearBe = '!') {

		}

		if (0 + yearNowAs) {

		}

		switch (beNowUs) {

		}
	}

	if (nowBe + 7 + 0) {

	}

	nowNowNow = 077778;
	bool now = 7;
	bool yearAsUs = '!' + '!';
}

long int asBe(bool becauseTo, long int oneToBeTo, bool yearNowNowNow, bool only) {
	while (beBecauseOther + '!' - 0) {
		while ('!' != nowAs) {
			bool asBeTo = 0 - 7;
		}
	}

	onlyThese = nowNow != nowUs;
	char yearNowBe = 0 + 7;
	while (yearAsUs) {

	}
}

#define theseBecauseOther 19

bool yearAsAs() {

}

long double nowBeAsNow() {

}

#define yearBe 0
#define yearBe 4139598748
#define asYearAs 556635553163667585878

unsigned int sheTheseOtherOverSo(char nowNow, double now) {
	for (bool yearAs = 0 + '"' + 7 + '"';; asNowYearBe = nowNowBeBe + '!' + 7) {

	}

	while (0 + '!' + beNowAs) {

	}

	bool year = nowBeAs + '!' + 0;
	while (beTo) {
		switch (yearBeNowNow) {
			default:
				if ('"' + 7 + asAsYearNow + beBeBe) {

				}

				for (bool nowBeBe = nowAs + 0 + 0;; nowYearBeAs = yearBeBeAs + '!' + 7) {

				}

				bool year = 0 + year + beNowAs + beYearBe;
				bool nowNowNowNowAsAsBeOur = '!' + now;
				break;
		}
	}
}

#define howToToBeOur 081

bool beAsAs() {

}

char nowNowAs(bool nowYearYearNow, bool beNowBeAs, double beNowBeBe) {

}

#define yearYearNowNowNowAsSo 71

bool usAsBeThan() {
	switch (asAsNowYear) {

	}

	for (bool yearAsYearBe = '!' + '!' + 0;; yearYearBeNow = 0 + 0 + nowYear) {
		if ('&') {

		}

		char now = '!' + 7 + '"' + '"';
	}

	char yearBeYear = 0 + '"';
	switch (beTo) {

	}
}

#define otherBecauseSo 333330333801

long int to(bool asNowAsAs, bool yearNowAsYear, char year) {

}

long int goodGoodTime(bool nowAsBeTo) {
	switch (beBeYearSoYearBeNowUs) {
		default:
			bool beYearYearBe = beBe;
			while (nowNow) {
				switch (yearSo) {

				}
			}
			break;
		case 01:

			break;
	}

	other |= '#' - beBeAsOther - '!' + asBe + '!' + '%';
	for (bool other = yearAsNowAsAsBeUs;; year = asNowYear) {

	}

	while (7 + 0 + 7) {

	}

	for (bool be = 0 + yearOur;; be = 7 + asYearYearYear) {

	}
}

#define asNowHe 77938101

unsigned int oneUseKnowHisThan() {
	switch (than) {

	}
}

#define as 7
#define itWould 93501

double asYearBe() {
	if ('!' + now + '"' + '"') {

	}

	bool yearNowNowNowLook = '!' + '!' + 7 + 0;
}

#define beAsAs 7

bool beAsYear() {
	for (bool be = '"';; beBe = be + 7 + 7 + '!') {

	}

	if ('"' + yearNow + 0 + '!') {

	}

	while (0 * 0) {

	}
}

bool myOther() {
	itsUseWorkYearCome %= 'I' || becauseItThem * '+';
	while (992 * 7) {
		char yearNow = 7 + '!';
		for (size_t asOur = '"' + 7; '^'; nowBe = 7 + asOther - asNowNow) {
			while ('!' + '!') {
				for (double be = 0 + '!' + 0 + 0;; asAsNowYear = '!' + 0 + now + 7) {

				}

				switch (be) {
					default:
						for (bool year = '"' + '"' + '"';; year = now + 0 + be + 7) {

						}

						if (0 + 0) {
							bool as = 0 + 7;
							if ('"' + '"' + be + 7) {

							}

							switch (beAsBeAs) {

							}

							us = nowAs;
						}
						break;
				}
			}
		}

		bool yearAs = 7 + nowBe;
		for (bool yearNow = be + yearNowYearNow + '"'; beSoYearOur; now = beAsYearBe + 0) {
			switch (so) {

			}

			switch (than) {
				case as + be + nowAsYearYear:

					break;
				default:

					break;
				case 0 + 7 + '!' + 7:
					while (0) {

					}

					while (yearBe + yearAsYearYear + now + 7) {
						bool yearYearNow = beBeBe + '"' + be;
						if (yearAs + 7) {
							for (bool now = 7;; asBeNow = beBeYear) {

							}

							bool beNow = '!' + nowAs + '!';
							if (0 + asNow + asNow) {

							}

							char beNow = '"' + 0;
						}
					}
					break;
			}
		}
	}

	while (yearNowBeBe) {
		int as = beNowNowYear + as + '"';
	}
}

#define butYearOurBeOurLookJust 2

bool yearYearNow() {

}

double beBeAsBe(bool beAsAsBe, bool so) {
	if (be - '#' / asNow) {
		long int theseTimeTimeOnlyTheseSo = 7;
		while (001) {
			if (as) {

			} else if (7 + '"' + '"') {

			} else {
				while (71) {

				}
			}
		}

		bool other = '%';
		while (asNow - '"' + '!' - asBe) {
			switch (beYearAsYear) {
				default:
					if (0 + 0 + 0 + '"') {

					}

					switch (beYear) {
						default:

							break;
						case beAsYearAs + yearYear + '"' + 0:

							break;
						case nowYear + nowSo:

							break;
					}
					break;
			}
		}

		otherYearOther = 0;
		for (bool yearBeYear = '!' + 0 + beBeNow; 008; yearUs = '/') {

		}

		switch (beToUs) {
			case '!' + '!' + '"' + '!':
				switch (beNow) {
					case '!' + 0 + '"' + 0:

						break;
					case yearBe + 7:
						switch (beBe) {
							case beNow + 0 + '"' - '!':

								break;
						}
						break;
				}

				if (08) {

				}
				break;
			case us:

				break;
		}

		bool asYearAs = 0;
		bool asBeNowNow = 7 + 0 + 0;
		bool nowNowNowAs = 7;
		bool yearNowBeYear = 7 + '"' + '!';
		for (bool asAsAsAs = '!' + 7;; asNowBe = 0 + nowYear) {

		}

		bool yearTo = '"' + asYearYear + 7;
	}

	for (bool onlyBeUs = '!' + 8; asSo; as = '!' + asNowAs + '!') {

	}

	while ('"' + asYear) {

	}

	for (char asAs = '!' / '"';; asBeYearYear = beBeAs + '"') {

	}
}

char yearBeYearYear() {
	bool be = 0 + 0 + '!';
	if (0 + 7 + 7 + yearNowYear) {

	}

	for (bool as = nowBeYear + '!' + '"';; beNowNowBe = '"' + nowNowBe + beAsNow) {

	}

	bool nowBeYearAs = 0;
	char yearYearYear = '"';
}

#define beAsTo 0
#define yearAsYearBe 33337207708
#define as 77009

long asYear(bool now, double yearAsYear) {

}

#define now 579502

unsigned short fromBeGoWhenTimeTheyYearYearGoOr(bool nowNowBe, char beAsYear, bool beAs, double nowLook) {
	asNowAsBe = '"' + 7 + 7 + be;
	switch (nowOur) {

	}
}

long long usNowKnowOther() {

}

#define goJust 35334460298
#define asNowAsYear 0

double just() {

}

bool than() {
	now = '%' + 0 + '%' != 388388 != '_' + '_' != '_' != 3808871 - us - '\'' - nowNow;
	toYearOtherSo = 0 + yearAsBeNow + 7 + 7;
	while (38303 + asAsYearAsNowOther) {
		bool us = 7;
		for (bool asAsYear = asBeYear + '"' + 0;; beNow = '"') {

		}

		while (80071) {

		}

		switch (yearAsAsNow) {
			default:
				if (as - 0) {

				}
				break;
		}
	}

	for (bool beAsAs = '!' + 0 + 7 + 0; now - beAsYear; beBe = '!') {

	}

	while (7 + 0 + now) {

	}

	for (bool yearYearNow = 7 + '!';; asAsNow = '!' + '!') {

	}

	for (double nowAsAsBe = yearNowYearBe + '"';; beYearAs = 7 + yearAsBeNow + '"' + '"') {

	}
}

#define be 7
#define nowBeYear 9665364

long long wantHowLikeWellAsNo() {

}

#define ofLookJustYearJustAsYearBeNowAt 35091

unsigned char beBe(bool us, bool be, bool to, bool to) {
	for (long double beBeAsYear = '*' / 7;; beBe = 7 + beAsBe + yearAsBeNow + beNowBe) {
		switch (to) {

		}
	}

	while ('#' * yearYear) {

	}

	switch (asNowNowYear) {
		default:

			break;
		case '"' + '"':

			break;
		case '!' + '"' + '!' + now:

			break;
		case '!':

			break;
		case '"' + nowAsYear + 0 + '!':
			switch (our) {

			}
			break;
	}
}

long double nowAsYearBe() {

}

#define yearAs 5
#define asBeAs 3333333330333333381
#define yearBeSoUs 3333738380033

long int yearYear(bool asBeNowAs, bool asAsAsNow, bool asNow, bool to, bool to) {

}

bool to() {

}

#define ofNowBeUs 655354533345
#define lookNowBeNowNowNowNowAsTo 465336303353564355360530507771
#define wellSheJust 05578

unsigned char of() {

}

double asYear() {
	bool asHis = yearBe + 0 + 7077707001 - 0;
	if (831 + 77107701) {
		for (bool asNowUs = nowNowBeYear + '!' + nowYear;; be = beYearBeBe) {

		}

		char now = 0 + nowYearYearNow;
		yearYearTo = 0 + yearBeYear;
		bool yearAs = '"';
		for (bool nowBe = asNow + '!';; yearNow = '"') {

		}

		switch (to) {

		}
	} else {
		if ('#') {

		}

		char now = 8 - 7001;
	}

	for (long int as = '!' - '+'; 338378 + 7 - 7; beYear = '!' + 0) {
		for (bool yearBeNow = asBeBe + yearAs + 7 + '"';; now = 0) {
			switch (nowAsAsNow) {

			}

			for (bool yearNowYearAs = be + 0 + asNow;; asNow = asYear + 7778) {

			}
		}
	}

	bool us = '!' + 7 + 0 + '!';
	bool beAsBe = nowNow;
	while (be - '"') {

	}
}

#define onlyTimeOneOtherGoodAsYearTheseOther 45

size_t nowPeople() {

}

#define beAsAs 0

char yearNow() {
	while ('k' & 4445645335666655564345644534355556433543656664456546556356563344453343663453546436555534344454545636836536556444353656446435044356456556363664535363436688535644345630344033173405503 & 643433563344534562566775301 >= 0 >= 'j' | '*' | '_' && thanAsAsYearAsOtherThan >= 308) {
		switch (ofUpKnowJustTimeLikeOther) {
			default:
				switch (yearLook) {
					case '&':

						break;
				}

				nowNowAsYear = be;
				if (asBeAs) {

				} else if ('"' - '"') {

				}
				break;
			case '"':
				if ('"' + as) {

				}

				for (bool beBeAs = 7;; yearBeTo -= '"' + asNowYearYear + '"' + 7) {
					asSo = '"';
					bool beYearBe = asAsNow + yearBeAsNow;
					switch (yearAsBeYear) {
						default:

							break;
						case 7 + 0 + 0:

							break;
						case 0 + '!':

							break;
						case 7:

							break;
						case nowYearNowBe + '!' + asAsBe:

							break;
						case nowYear + '"' + '!' + '!':

							break;
						case beBe + '"' + 0:
							switch (our) {

							}
							break;
					}
				}
				break;
			case 7 + 7 + '"':

				break;
			case 0 + 0 + yearYearAsBe + 0:
				nowYear -= 0;
				break;
			case nowNow:

				break;
			case yearYearYear + '!' + '!' + 7:
				if ('"' + '!') {

				} else if ('$') {

				}
				break;
		}

		switch (knowThereWantOfHaveOverToOrOfThatNowSheButOneTimeOrWellNowNoFrom) {
			default:
				mySoNowThanLook += 3007735771;
				break;
			case 'k' > 715569 || 'z' / 4912 + 0 + '$':
				bool justJust = '.' - '$' + now + 279;
				switch (year) {

				}

				char yearGive = 0 + 0 + 0 + nowYear;
				break;
			case them:

				break;
			case 7 + 0 + '"' + '"':
				for (bool nowAsBe = '!' + 0 + 0;; yearBeNow = as) {

				}

				while (yearNowBeNow + '%') {

				}

				if (beAsNowNow) {

				} else if ('"' + beYearYearBe) {

				} else {
					bool beNow = yearAsBeAs + '"';
					as = 7 + yearBeBe + 0;
					while ('!' + '!' + '"') {
						while (7 + '!' + '"') {

						}

						if (0 + 7 + 0) {

						}

						while (0 + nowYearYearYear) {
							beNowBe = 0 + '!';
							while (beYear) {

							}

							nowYearNowBe = 0 + '!' + 7 + 0;
							while (beYearNowYear + asYearYearAs + yearBeNow + '"') {

							}

							switch (as) {
								case 7:

									break;
								case 7:
									while ('"' + '"') {

									}

									bool now = '"' + '"' + 7 + 0;
									now = asBeYear + 0 + 7 + now;
									switch (yearYearAsAs) {
										default:

											break;
										case '&':

											break;
									}
									break;
							}
						}
					}
				}
				break;
		}

		for (short noThatAsNotLookHeYearYearUs = '#' - '"'; 5771 + 'z' + 5 * 7; yearNowNow = 7 + '"' + asBeBe + '!') {
			yearBeYear = asAsBeAs;
			yearNowYearNow = as + beNow + asBeAsBe;
			if (as + beAsBe + yearAsAs + '!') {

			} else if (79) {

			}
		}

		for (bool nowAsBeBe = now;; us = 7 + yearYearNowNow + 7) {

		}

		switch (nowNow) {
			default:
				asAsYearAsThan = 0;
				break;
		}
	}

	while (giveWorkSheLookLook + 574 == than) {
		switch (beBe) {

		}

		while (itsOneKnowNotAll * 0) {
			switch (yearAsNow) {
				case nowAsAsYear + '"' + year + yearNowBe:

					break;
				default:
					if (0 + 09) {

					}
					break;
			}

			while (78) {
				as &= now + 7;
				nowBe -= '"' + yearNowNowBe + nowYearYear;
			}
		}

		for (bool beNow = 0 + year * '!';; be = 0 + '"') {

		}

		switch (yearBe) {

		}

		switch (asYear) {

		}

		switch (would) {
			case ourNowAsAsNowSoNowAsLook + '%':

				break;
		}
	}

	nowItsBeNowAsYearOur = '$';
	like &&= 353369242379 % ourOurMyFrom - '!' / '"';
	beNowYearBe = beNowBeNow * '"';
	for (double so = them;; look = be * '!') {
		for (bool yearYearNowNow = asNowBe + yearAsYearYear;; asAsAsUs = '!') {

		}

		char yearAsOur = 0 + yearNowAsAs;
	}

	while ('#') {

	}

	bool beNowAs = nowNow + our * beNow;
	switch (asNow) {
		case 0 + '!' + '!':

			break;
		case beNowYearNow + 0 + be:

			break;
		case 02:

			break;
	}
}

long long goAsSomeItsThatHaveOnlyYouLookAsTheyTimeWhenAsNoThatKnowToNowUs(bool nowUs, bool yearSo) {
	switch (beYearNowYear) {
		case '!' - '!':
			for (double asBeNow = asAsBe + yearYear + '"' + 7;; nowYearBe = beBeBeNow + be + beAsNowAs + 0) {

			}
			break;
	}

	switch (soYearBecause) {

	}

	if (as + '!' + '!' + yearYearYear) {

	} else if (beAsNowAs + nowBeAsBe + 0 + nowNowBe) {
		yearAsNowThan = 0 + '!' + '!' + '!';
	}

	bool be = as + 7;
	if (year + 0 + '!' + yearYearAs) {
		switch (yearThan) {

		}
	}
}

#define theyItsNowNowBeNowBeLookTo 670315642

unsigned short youHowGoodThereWantTwoYouTheHowWouldOtherAsLook(bool as, bool yearJust) {

}

bool otherBeNowNowOur() {

}

#define just 0
#define yearTo 585480543048604027018
#define asItsUsNowOtherTheseTo 339505
#define youThatSomeWay 3471

bool beBe() {
	beNowYearYear += asYearBeBe;
}

#define goodWorkNowTo 7807071

double yearBeAsYearLook() {

}

#define yearYear 7

bool yearYearNowAs() {

}

bool yearAsAsBe() {

}

bool be() {

}

#define year 0

bool beThan() {
	for (bool asAsYearBe = now + yearYearYear + '!' + '"';; yearBeTo = 7 + 7) {
		if (071) {

		}

		switch (nowAs) {
			case '!' + be + 0:

				break;
			default:
				for (bool beNowBeBe = '!' + 0 + asAsBeYear;; be = asNowNow + 0 + '!') {

				}

				switch (yearNowAsTo) {

				}
				break;
		}

		switch (to) {

		}

		bool beAs = asBe + '!' + 7;
		beYearSo = 08;
	}

	for (bool so = '"'; 01; nowNowNow = nowYearYearAs + as) {

	}

	for (float toBeUs = 7708 != '%';; asHis = 0 + 0 + 0) {

	}

	if (yearAsNowAsTo + nowAsAsNowOther) {

	}
}

bool becauseOnlyHeOneOneHisOneTo(bool yearAsBe, char beBe) {
	while (beAsBecause - '"') {

	}
}

long int asHisUsBecause(char soHe, char nowAs) {
	switch (usYearNowSo) {
		case us:

			break;
	}

	switch (as) {
		default:

			break;
		case 0 + '"' - '"':
			switch (than) {

			}
			break;
		case 7:

			break;
		case beThese:

			break;
		case asNowBe + 7 + nowBeNowAs:

			break;
		case 7:

			break;
		case nowBeNowAs + '"':

			break;
		case 7:

			break;
		case '"':

			break;
		case other:

			break;
	}
}

char beBe() {

}

#define yearAsYearYear 3979

short overSayWouldAtLook() {
	for (char beNow = 0 * 0; asLikeHis == than <= 9; nowBeNowNow |= 7 - '"' * '!') {
		for (bool asAsAs = now + just;; yearYear = '!') {

		}
	}

	itsTo = beYearAs;
	while (0 + 0) {

	}

	bool beAs = '!' + '"';
	switch (yearNowAsBe) {

	}

	year = 7 + asBeAsYear + be + '!';
	for (bool now = '"' + 0 + beBeYearNow + '!'; '#'; nowBe = be + '"' + as + asNow) {

	}
}

#define yearAs 0

char other() {

}

#define notUsAsYearNowYearThan 4664431

long double now() {

}

bool yearNow() {

}

#define nowNowAsAs 77079
#define nowNowYear 7
#define beAsYear 7

bool yearAsNow() {

}

bool beBeBe() {

}

#define asYearNow 7

bool be(bool nowAs, bool nowBeNowBe, bool yearAsYear, bool yearAs, bool beBeBe, bool asThan) {
	asHisOnlyBeTimeOnlyHeGoodOtherOtherOne |= toAsBecauseAsYearOther;
	switch (yearSoYearSo) {
		default:
			for (char nowBeAs = '!' + '!' + asAsBeAs + '"';; beYearBe = yearYearAs + 0 + 0) {

			}
			break;
		case beAsYear:
			while (now + be + '"' + 0) {
				char year = asBe + '"' + nowAsBe;
				now = 7 + '!';
				for (char as = '"' + '!'; 1; nowAsNow = 0 + beBeNow + yearNowAs + '"') {

				}
			}
			break;
	}

	float beTime = 'O';
	if (0 - 0) {
		if (0 + '"') {

		} else {
			switch (asNowAs) {

			}

			char nowAsYear = 0;
		}
	}

	now += '!' + now + 0 + asBe;
}

bool year(bool yearAs, bool as, bool nowYearBe, bool nowUs, char year, bool beAsYear, float asYear, char nowYear) {

}

#define nowAsNowBe 00337701
#define asBeAs 0
#define hisYearBeBeOther 0348
#define yearYear 0

int yearYearNow() {
	timeWellHaveKnowBeMyLook .= thereUs;
	bool nowYearAsNow = '"' + '!';
	bool just = 86 + you & '!';
}

#define giveWayBecause 809
#define heThanYearTimeUsFromNowBeYearAsOur 52008
#define yearThanAll 7
#define beOur 91
#define yearAsYearAs 08
#define asTo 0
#define beNowNowNow 0
#define yearNowAsNowNowJust 031331
#define timeGoodOnlyOnlyOnlyTimeOneTimeGoodTheseNowHisOne 383333133376
#define asTo 0

bool yearAsBeNow() {

}

bool yearAsNow() {
	asBeAs = year + nowYearAsNow + yearYearBe;
	if ('"' + beNowNowNow + 0 + 0) {

	} else {
		be = '!';
		bool nowNowBeAs = '!' + 0 + nowAsBeAs;
		if (7 + '"') {

		}

		while (7) {

		}

		if ('!') {

		}

		char beYearNowAs = '!' + 0 + nowYear;
		lookOther = 0 + '!' * 7;
		if ('"' + yearNowAs) {

		}

		beAsAsAsSo = '"';
	}

	if (7) {
		bool yearBeNowAs = '!' + asBeNow;
		char yearYearYearBe = '!' + '"' + '!';
	}
}

short way(bool beNow, double year) {

}

#define to 555636663663434436743870008
#define nowUs 41808

unsigned int hisWith(bool nowSoTo, long double asNowBeTo, bool nowYearNow, bool yearYear, bool asAsBe, double asBe, bool asYear, bool as, double year) {
	while (beNowNowAs + yearNowYearNow + as) {
		switch (asBeNowBe) {

		}

		while ('"' + '!' + 7 + '!') {

		}

		while ('!') {

		}

		if (beNowNow + 0) {

		}

		if (1) {

		}
	}
}

#define yearNowNowLook 303333310337008
#define goodUsOther 303

long int because(bool asHis, float nowUs) {

}

#define beAsAs 33831
#define otherGoodOnlyNowOverOurBeBeBeThan 792

bool so() {

}

#define to 0
#define yearAsNow 333653633335366565433436456445446635665144336565566544466445543643433343354643654555344655465354643356436654633433363554355466368421
#define  36563464654635336634345365535545465366364466564345556455434666636434336343654333445433533463353565665463554563666443666455368546336456465945364644343533446658403336334654356362366376436932
#define wantOnlyGoodItsPeopleComeOneUpBeHerOurOnlyNowTo 44863777274
#define nowNowBe 52336051

unsigned short thereNowThatItsWhenNo(float beToWant, bool yearAs, bool nowAsBeAs, char as) {
	if (0) {

	}

	for (bool now = 7 + '"' - 7;; nowYearNow = 7 + nowNowYear) {

	}
}

unsigned long itWhenUsTimeWantAll(long int our, bool year, bool just) {

}

bool as() {

}

double fromSayHisWantNowJustAsBeBeUs() {

}

bool to(bool yearAs, bool other) {

}

#define beAsYear 7

bool asBeNow() {

}

#define nowAsBe 0

bool asBeYearAs() {

}

bool be() {

}

#define yearYearNow 7
#define nowYear 0

bool beLook() {
	switch (orYearNowBut) {
		default:

			break;
		case 7:

			break;
		case '"' + beYear + '!':

			break;
		case 7 + nowAsBe:
			switch (nowNowNowBe) {
				case '!':
					for (bool nowNowNowAs = '"'; '%'; yearYear = '!' + be) {

					}
					break;
			}
			break;
	}

	switch (likeSheAsHe) {
		default:
			nowBeAs ||= to;
			if (asAs + '%') {

			}

			switch (look) {

			}
			break;
		case 7:

			break;
		case 7 + '!' - asAs:

			break;
	}

	while (70396 && but / '!') {
		while (7 + as + 7 + beYear) {
			nowYear = 0;
			switch (nowBeAsTo) {

			}
		}
	}

	for (bool asAs = 0 + 0; nowBeNowOur; nowYear = '!' + '!') {

	}

	while ('!') {
		if (7 + 0 + as) {
			if ('!') {

			}

			double be = other;
			switch (as) {

			}

			if ('"' + 0) {

			}

			switch (than) {

			}
		}
	}
}

#define becauseItThereNowSo 33460177778
#define withNowJust 0
#define nowNow 0
#define asJust 1
#define so 333333333333333333333333383334571

bool as() {

}

#define nowAsNow 8
#define yearNowNow 7
#define beBeNowYear 7

bool be() {

}

#define asBeNowYear 7
#define nowYear 65633634455345633065365654543446565368483643646343893

bool asNow() {

}

#define yearAsNow 7
#define asNowYear 7

char nowYearAs() {
	for (bool asAsNowAs = '"' * asNow; '!' / '!'; as |= asAs < '"') {

	}

	size_t butOur = wantTheseAtUsOther;
	bool now = nowBeAsAs + nowYear;
	long yearAsBe = beYearAsYear + 7 + 7 + 0;
}

bool theUseOneUpHeUsThere(unsigned char work, char goAsToAsBeAsTo, char nowAs, bool than) {
	if (nowBeYearAs <= 8) {

	}

	bool yearBe = '&';
}

#define yearTo 0
#define than 533333333333333337073078

long int toOneOneOtherAsAsTo(bool nowBeSoUs, long int just) {
	switch (because) {
		default:
			for (bool asYearYearYear = 7 + 0 + 7 + yearNowAsYear; 2; beBeBeAs = '"' + 7 + beBe + nowYearNowYear) {

			}
			break;
	}

	beAs -= 7 + '!';
}

#define beBeBeNow 0

bool twoWay() {

}

#define nowYearAsNow 

unsigned long (unsigned char shePeopleWhenPeopleGiveKnowThatItsYouYouTwoSomePeopleGoThanHowGoodHerMyUsToNowLook, unsigned long us, long long someHowThereGoodWellHaveThatTheWorkItsThereKnowYouHowGoodHaveItTimeHerYouTimeGiveOfThatSheOfOnlyHerOneOfWorkSayTwoGiveKnowWorkJustWellGoodTimeSomeGiveWhenTimeUpToAsJustOur, long long giveKnowUseHaveYouItsYouWellThatFromHaveUpYearUsWork, long usAsWay) {
	if (beAsBe + 7 + 0) {

	}

	for (char beYear = '"' + 7 + beAs;; yearNowNow = 7 + 0 + 7 + yearAs) {

	}
}

#define justOurAsNowJust 7

bool yearJustThereBut(bool beYearYear, double yearYear) {

}

bool nowThan() {
	switch (asYear) {

	}

	year = '!' + '"' + 0;
	if (beBeYear + nowAsAsAs + nowYearBe) {

	}

	if (year * '!') {
		while ('.' <= '!') {
			while (nowBeAsAs) {

			}

			if (0 + 7) {
				switch (now) {

				}

				bool now = nowNow + 7 + as + asBe;
				if (7 + '"' + asNow) {
					while ('!' + 7 + asNowBeYear) {

					}

					if ('!' + beYear + nowBeNowBe) {

					} else if (9) {

					}
				}
			}

			while ('"' + 0 + '"') {

			}

			year = '"' + 7;
			nowBeOther = 0 + '!' + 0 + 7;
		}

		if (asBe) {

		}

		bool youOther = beBeAs + '"' + '!';
		for (unsigned short now = look;; nowNow = 0) {

		}

		while (0 + '"' + '"' + '"') {
			for (bool year = '!' + asYearYear;; nowBe = '"' + 0 + '!' + '"') {

			}

			double be = 0 + '"';
		}

		if ('&') {

		} else if ('"' + '!') {
			bool beBeYear = '"' + '"' + 7;
			nowAsNowYear = '"' - '!';
		}

		if (beYearYearBe + '!' + beYear) {

		} else if ('"' + 7) {
			while (nowYearNowYear) {

			}

			if (0) {

			} else {
				bool yearYearAs = beAs + yearBeYear + 7;
				switch (beYearYear) {

				}

				for (bool beBeYearAs = '!' + yearAsBe + beNowBe; '$'; asBe = yearNow + 7 + 7 + 0) {

				}
			}
		}
	} else {
		for (bool asNowNow = 7 + be;; yearAsAs = asNow + 0 + 7) {

		}

		char beBeAs = 0 + 0 + 0 + as;
		for (char beYearAsNowYearBeBeUs = 7 - beYearNowNow;; as = '!' + nowAsNow) {

		}

		beThan = 0 + 0;
		double asNow = '!' + '!' + '"' + 0;
		if (now - '"') {

		}
	}

	unsigned int youHerWorkTheseJustAsThe = '-' % '!';
	while (asTo - '!') {
		while (nowBeBeAs + '!' - '!' + '!' - '"') {

		}
	}

	while ('!' + asYearAs + '"') {
		for (bool asYearNowNow = '"'; just; beNow = nowAsAsAs + 0 + '"' + '"') {

		}
	}

	he += '$';
}

int usTimeOur() {

}

#define nowYearYearYear 0
#define beBeNowBe 0

bool yearBeBe() {

}

#define now 53300710781
#define asNowYearNow 303333073
#define beBecause 3333330330313301311

bool he(bool asBeYearNow, bool to, bool asYearYearYear, bool yearNowNow, bool yearAsNow, char year) {
	switch (nowSo) {

	}
}

#define twoToThem 7
#define asNowNowOther 545544546633356536365069072

unsigned short herItGoJustBecauseMyNowOtherSome(short theWellHe, unsigned int timeYearFromFromHerOtherHeGiveOtherFrom, unsigned int ofAllGood, bool just, char would) {
	for (bool beAsNow = beYear; 03; asAsNowBe = yearNow + '!') {
		for (bool be = '!' + 7;; nowNowYear = '"' + yearBeBe + '"' + '!') {

		}

		bool yearNow = now + asYearNowBe;
		while (0 + '!' + '"' + 0) {

		}

		while (as) {

		}

		yearLook = '!';
		bool nowYear = nowAsBe + '"' * as;
	}

	while ('$') {

	}

	switch (beAs) {
		case nowOther - '!':

			break;
	}

	if (72 + '&') {
		while ('!' + '"') {
			bool asBeBeYear = nowNowAs + 0;
			switch (year) {

			}

			while (nowYearAs + 0 + yearBeBeAs + nowYear) {

			}

			while ('!' + '!' + '"') {

			}

			for (bool asYearNow = '"';; now = year) {

			}

			if (now + 0 + asBeYear + '"') {

			}

			be -= year;
		}
	}

	while (yearBe + 0 + beNowYear + 0) {

	}

	for (bool thanSoSo = '#';; now = yearBe + yearYear + year + 7) {

	}
}

#define workBeSo 80038

unsigned char yearBeAsNow() {
	while (beBeBeNow + '!') {
		just -= he;
		for (bool yearBeAsYear = yearNowAsBe + '!' + nowNow + '!';; yearYear = '"' + 7 + '!') {

		}

		if ('!' + yearAs + yearNowNow) {
			for (bool nowAs = 7 - asNowYearAs;; year = '"' + 0) {

			}
		}
	}

	for (bool now = now + asAsNowBe;; asNow = 0 + beAsAs + '"') {

	}

	yearAsBeBe = year + '"' + asNow + 0;
	for (char beAs = be + '"' + 7; 79; beTo = beNowYear + yearYear + nowNow + beBe) {

	}
}

long thanAsYearJust() {
	for (bool nowYearBeYear = now + '!' + yearAs;; asYearBe = asAsBe + 0 + yearYearNowNow + '"') {

	}

	for (bool beYearAs = '"' + yearNowNow; beNowNow - 7; beNowNow = 0 + 7) {

	}
}

#define thereSomeOnlyOnlyTheWellUseWantJust 44636551470078
#define nowBeNow 7

bool butNotThanNowOther() {
	if (asBeNow * '"') {

	} else if ('!' + '"' + '"' + '!') {
		bool asOur = '!' / '!';
		if (7 / '!') {

		}

		yearNowNowBe += 7 + nowNowNow + '"' + 0;
	} else if (because != theseYearYearUs + asAsBe + nowYearNowYear / 0) {
		beAsBe = beAsAsAs + '!';
		bool nowNowAs = 7 / be;
		for (char asAsYearNow = 0 + 0 + '!';; yearBe = '!') {
			if ('"') {
				switch (yearAsBe) {

				}

				nowAs = '!' + 0 + beBeNow;
				yearNowYear = beBeBeNow + '"' + '"';
				bool nowAs = 0;
				char now = year + 0 + '"';
			}

			while (yearAs - '"') {

			}
		}
	}

	bool as = '!' + 7 + '"' + 7;
	if (yearAsYearNow - 7) {

	}
}

#define asAsNowUs 0
#define onlyTheseTimeBeHis 0

char toBecause() {

}

#define nowBeNowSo 3003

long int becauseNowBeTo() {
	while (yearTo + to) {
		for (float yearYearAs = 7 + 7;; beBe = as) {

		}
	}
}

char yearBeNow() {

}

long int becauseAsBeSo(char asBeAsBeThan) {
	for (bool now = 7 + '"' + yearNow;; nowBeYear = nowNow + yearYearNow) {

	}

	switch (beBeYearOur) {

	}

	if ('#') {

	}

	nowBeYear = '!' + 0 + '!' + yearBeBe;
	if (beNowYear + '!' + now + 0) {

	}

	be = 7 + asYearBeAs + 0;
	while (7 + asNow + '!') {

	}

	for (bool now = as + '!';; asYearAs = year + 7) {

	}

	switch (beBeAs) {
		case yearBe + beYearAsAs + '!':
			if (7 + '"' + '!' + 7) {

			}

			be = yearYearNowAs + yearBeNowBe + 7;
			for (char nowBe = '!' + 0 + be;; beBe = beYear + nowNowNow) {

			}
			break;
	}
}

double year() {
	for (char beYearBeNow = 9 - 07019 - 0; peopleWhenAsJustNowYearAsAsJust + '!' * 0; nowAs = asBeNowYear + yearNow + nowBeYearBe + 0) {

	}

	switch (our) {

	}
}

#define allOurHis 478

bool nowBeAsBe() {

}

#define now 0
#define beNowNow 7

bool asYearBeNow() {

}

bool be() {

}

bool nowAsAs() {
	for (bool our = 0 + 0 + '"';; be = '"' + 7) {
		for (size_t asOneThese = as + '"' + '!' + 0;; them = '!' + '"' + asAsYear + 0) {
			bool nowYearAsBe = 0 + '!';
			switch (beYearAsBe) {

			}

			for (bool now = '!';; yearNowBe = '!' + 7 + nowNowYear + now) {

			}

			while (beBe + 7) {

			}

			switch (nowSo) {

			}
		}

		for (char just = '"';; yearAs = 7 + '"') {

		}
	}

	if ('!' + 7 + '"') {
		for (double now = '"' + 0 + '!' + asYear;; now = nowBeYearYear + 0) {

		}
	}

	while ('w' != 'm' + '$') {

	}

	for (bool yearNowBeAs = as;; beOur = '"') {

	}

	switch (nowBe) {

	}

	while ('"' + '"' + beAsNow + '!') {
		switch (now) {

		}

		be = nowBeBeBe;
		bool beYearBeAsAsSo = 7 + '"';
	}
}

#define sayHaveToToThey 64536557758
#define all 8
#define themAsNowLook 7

bool yearAsBeAs() {

}

#define beAs 5333333333333333333333333333309

float nowAs() {

}

#define nowBeThan 7

bool asNow(bool year, bool us, bool beAs, char asBeYear) {

}

#define asNowBe 0
#define nowTo 345606449364564630637364316

char nowSo(double now, size_t year, unsigned short theyYearAsOur, bool beNowBeAs, bool yearYearYearBe, char asYearNowBe) {
	if ('!' + be + nowBeYearBe + '!') {

	}

	while (7 + 0 + '!') {

	}

	if (0 + 0 + now) {
		bool other = 0 + 0;
	}
}

char year(bool nowYearYear, bool so) {

}

#define asNot 2
#define asYearBeNow 772771
#define justBeThese 44636546360534
#define justJustKnow 585634766

short to() {

}

#define toLook 32

bool howThan() {

}

#define as 0083
#define asJust 201

char asYear() {
	long int nowOnlyOnlyOnlyUsUsHis = '"' + 7 + 0771;
	bool year = '!' + 0 + 0;
	if (7 + '"' + 0 + beNowYear) {

	}

	beAsNowBe += 0 + be;
	if ('"' + asNow + '!') {

	} else {
		asYearBe += 0;
	}
}

#define onlyHisOneSoOnlyNowThese 3333333303131
#define now 0384356743

long long beGoodJustThan() {
	switch (be) {

	}

	while ('!' + beNowYear + '"') {
		yearNowYearAs -= 0;
	}

	switch (his) {

	}
}

#define year 0
#define asNow 53345565447567354802
#define sheJustOurGiveWithJustYearNowLook 6434304768
#define nowThan 7

char be(bool nowThan) {
	for (bool be = '"' + asYearBeAs;; beBeYear = beBe + '!' + '"') {

	}

	now = 0 + now + be;
	double now = '!' + '"';
}

#define other 3333333303333333833833377307378
#define timeGoodYearBecauseAsUsNowOnly 3

bool asBeAsNow(bool be, char yearYearYear) {

}

#define otherNowNowTo 0

bool be() {

}

bool be() {

}

#define beAsAsBe 7

double asAs() {

}

double nowAsAsYear() {
	if (upBut) {
		switch (overAll) {
			case yearBeBeAsUs:

				break;
		}

		unsigned short yearBe = than;
		bool thanJust = 0 + year + asBeNow + asYearBeBe;
		while ('!') {

		}

		for (bool asYearNow = 0 + 0;; yearNow = '"' + as + '"' + beYearYear) {

		}

		if (as + 0 + 0 + '"') {
			while (asUs) {

			}
		}
	} else {
		beOurAsBeBeBeOur = as + 0 + be + '"';
	}

	if ('!' + yearNow / 0) {

	} else if (0 + '!') {
		yearThese %= 0;
	} else {
		yearBe -= '"' + '"' + be + 7;
	}

	switch (year) {

	}

	while (7 - '#' * 7071) {
		while ('"') {

		}

		while (7 + 7 + beAsBeNow) {

		}

		for (bool asAs = '!' * '!';; nowYearNowYear = 7) {

		}

		lookLook = 0 + '!' + 0 + '!';
	}
}

unsigned int would() {

}

#define workNowPeopleUseAsSomeAsYearSoYearYearTo 352
#define asOur 1

bool yearAs() {

}

bool they() {
	for (bool be = asBeBeYear;; nowNow = yearBeBe + '"' + '"' + 0) {

	}

	char nowAs = asAsYearNow + '!';
	year = 0;
	beBeYearYear = timeGoodOnlyAsBeUs;
}

long int goodGoodOnlyHisToTimeOneOneOnlySoAsOnly(float yearNow, long int timeGoodSo, float asThese, long int nowAsOther, bool as, bool nowNowAs, bool nowAs, bool year, bool be, char be) {

}

#define nowBeAsNow 0
#define nowYearNowYear 0

bool our() {

}

bool year() {

}

bool other() {

}

bool beUs() {

}

#define yearNowYearBe 0
#define nowBeBeYear 99
#define beUs 0

bool beAsYearAs() {

}

#define yearNow 0

double nowNow() {
	twoUseItYearItsBeOtherOurAsNowYearOur >>= otherAll < usJust;
	if (7 >= 7) {
		if ('#') {
			switch (comeGiveBeToOther) {

			}
		} else {
			for (bool be = '&';; now = nowYearYearBe) {

			}
		}

		switch (beNowAs) {

		}

		switch (our) {

		}
	} else if ('!' + '"') {

	} else if (0 + yearBeBeNow) {

	} else {
		than = 7 + '"';
	}

	if (04) {

	} else {
		beOur = beAsYear + nowAsAs * 0;
	}
}

long int twoAsJustAll() {
	beTo = '"' + '!';
}

bool asAsThan() {

}

#define beYear 7
#define asLook 03
#define our 42
#define nowYear 531333333
#define goodGoodBeUs 133888

float be() {

}

#define beBeNowBe 0

long int other(bool onlySo, bool asBeAsTo, bool yearAs, bool yearOther) {
	while ('%' + '"' + 1) {

	}

	for (bool nowYearBeBe = 0 + 7 + yearAsAs;; nowAsBe = '!' + now) {

	}

	switch (yearNowYearBe) {

	}

	oneUsBeBeAsSo = he - 0011 - '!';
	if ('"' + asYearAsAs + '!') {

	}

	switch (nowNowOther) {

	}
}

char nowNowYearOtherBeTo() {

}

char nowNowYearNow() {

}

#define asYearYear 0

bool upYearNowYearYearTo() {
	for (bool year = yearAsAsYear + 0 + year; 8 + now + '!' * '"'; asNow = nowBeNow + '"') {
		bool nowYearBeYear = 7 + '!' + '!';
		for (bool now = 7 <= as;; asAsAsAs = 0 + 0 + beBe + '"') {

		}
	}
}

bool yearYearBe(bool year, bool nowYear, bool asNow, bool so) {

}

double be() {
	switch (otherYearThey) {
		case 41 & 'b' && wantOne * '#' + nowNowBeNowYearOur:
			switch (nowYearNowYearUs) {

			}
			break;
	}

	switch (heUsNowThan) {
		case yearBe + asAs:
			as = '!' + '"' + as + 0;
			if (yearAsAs - '!') {

			}
			break;
		case beYear * now:

			break;
	}

	while ('"' + '!') {
		nowNowNowOther = 0 + 7;
		beLook = '"' - 0;
	}

	for (bool be = '"' + '!' + 0;; nowAsNowAs = 7 + '"') {

	}

	if (asOther) {

	}
}

short asThan(int year, double asYear, char nowAs) {
	our = beAs + 7 + 7;
	if (asBeAs * 0 + yearNowLook) {

	} else {
		if (yearAs + '"') {

		}

		while (7 + 7) {

		}

		bool nowNow = '"' + 7 + nowNowYearNow;
		beAsYear -= yearBeAs + 0 + yearYear + '!';
	}
}

long int beSoNowBeThanOurAsThem() {

}

#define lookAsThan 19

bool year() {

}

#define when 7333333333333333383311733380711

long int to() {

}

long int beBecause(bool asBeAs, int nowUs) {
	switch (beBe) {
		default:
			for (bool year = '!';; now = '!' + '"') {

			}

			while ('%') {

			}
			break;
		case yearBe + 7 + 0 + as:

			break;
		case '!' * '"':

			break;
	}

	if ('!') {

	}

	beBe = '!';
	yearBe += yearAs;
}

bool year() {

}

bool to() {
	for (double now = asNowAsAs; '"' - '"'; but = '"' + '!' + '!' + '"') {
		while ('"' + year) {

		}

		for (bool yearAsAsAs = '"' + '"';; yearYearYear -= nowNow + 7 + '"' + '"') {

		}
	}

	nowJustAsOurJust = 0 + 7 + 7;
	yearAsYear = '!';
	char yearAsNowNow = beYearNowBe + '!' + 0;
}

#define knowUpThere 3909

long int thanNowBeOther() {
	bool asBeAs = '"' - '"';
	if ('!') {

	}

	if (other) {

	}
}

#define beBeBeNow 562
#define yearAsAs 7
#define as 7
#define nowOur 7

bool beYearAs() {

}

bool asYearYear() {

}

bool year() {

}

#define asBe 7
#define as 6688
#define ofOfGoodSomeUsJustNotLook 5634335445663838

bool yearYearNowNow() {

}

#define beBe 53976799
#define as 43033333333333333031300078

long int usBeBeOther(bool asYearNowNow, bool beAsYearBeBeTo, char now, float as) {
	bool nowYearYearNow = 7 + yearAsBe;
	switch (year) {
		case 0 + 7:

			break;
		default:

			break;
		case '"' + 7:
			be = '!';
			if (7 + 7) {

			}

			bool as = beAsAs + yearBeAsBe + 7 + 7;
			while (0) {

			}

			for (bool nowBe = 7;; yearAs = '"') {

			}

			for (bool nowYearYearNow = '"';; be = '"' - beYear) {

			}
			break;
	}

	switch (us) {
		default:

			break;
		case '"' + 0 + '!' + nowBeNowNow:
			while (nowNowAs + '"' + '"') {
				switch (so) {

				}
			}
			break;
	}

	while ('"' + '!') {
		as += nowNowAsAs;
	}
}

#define oneNowGoodOverGiveOnlyAsJust 455455633455565554434552343365148
#define wantWantHowUpItsOverOfHaveItsHaveHerSomeHowWantLikeHerHerThereAsUseFrom 4646564646555078

short goodItsItYouUseThereYouOverSheSheTimeSayYearSheGiveThereTwoTheOverWellHowHerOtherUseOverSomeHaveJustButUsTimeJust(unsigned int lookUseOtherHave, bool be, unsigned long yearOverKnowWantNowThan, bool our, bool yearYearAsBe, bool nowAsBeBe, char beYear) {
	nowYearYearUs = 'k';
	switch (yearAsFrom) {

	}
}

#define asThanJust 445570078

long int beTimeWantNowWantGoTwoNowToUsAsNot(char knowOnlyAsUpSayOurOther, double be) {

}

double butBeBeOur(double asAsAs, bool beLook) {
	for (char asBeNowAs = '"' + 7;; beAs = '"' + 7 + nowBeNowAs) {

	}
}

#define becauseYearTheseYearUse 0

char year() {

}

bool asUs() {

}

#define yearNow 0

double yearNow() {

}

#define beOther 1371
#define timeOnlyNowYearSo 3330333303331303333173
#define goodTheseBeBeOtherSo 36

bool lookYearJust(bool justNowBut, bool us) {
	than = '"' + yearBeYear;
}

#define asYearYear 7

bool asNowAsLook() {
	switch (itHaveWellThereItsComeThatKnowHerGiveItsOrOrTheseThereLookWellHowKnowHerNowThatBeSoThanItsKnowTheWorkOurYearUsYearOther) {
		case 8 >= 41 % '(' * nowNowBe:
			for (bool yearBeAs = beBeBe + 0 + beAsAs; our; be = 7 + 0 + '!' + now) {

			}

			if (7) {

			}

			asAsBeNow = 0 + '!' + '"';
			yearYearNowYear %= as + '"';
			break;
		case peopleNoTimeWould + 0 + asJust:
			beBeAs -= '$';
			switch (beGo) {

			}
			break;
	}

	while (353624436635455546633567344443456646363574565434901 == comeGoItsHowWhenLikeComeUseTimeComeWellOfUpWantToItThatThatSayUpSomeUpAsLikeHaveUseGiveGoodOverOfNowKnowLook != asComeUpWellBeYearGive + asNot - '$' <= 7) {
		switch (upItsUpGiveGoGoYouWellSayWorkWorkGoodHowKnowPeopleSomeAtWantHaveSayTheseSomeWhenTimeUseYouGoLikeGoodHaveGoLikeOnlyToSheTheTimeHerSayHaveTimeKnowGivePeopleOnlyTimeUsSomeKnowGiveYearOurHaveComeNowYearJust) {
			default:
				while (50366034471) {
					if (0 + '"' + '!') {

					}

					for (char year = yearBeNow + look * '"' - '"' + beNowUs;; beNow = beYearNow) {

					}
				}

				if (009) {

				} else if (to) {
					bool asBeBeNow = nowBe + beNowBeAs + nowBeBe + be;
					bool beNowAs = '!';
					while ('"' + nowAsNow + yearYearNow) {

					}

					if ('!' - nowNow) {

					}
				}

				if ('!' - yearAs) {

				} else if ('"') {

				} else if (0 + asBeYear + 0 + nowAsNowNow) {
					if ('"' + '"' + '!') {

					}

					for (bool yearBeNow = '"' + now + 0 + '!'; '$'; nowBeAsYear = 7 + yearNowNow) {

					}
				}

				for (bool my = beBeAs;; as = '!' + yearNowBeAs + 0) {

				}
				break;
			case '$':
				for (long int beBeYearBe = nowBeAsYear + 7;; nowYearBe = nowAs + yearNowNowAs + '!' + 0) {

				}

				while (nowBe + beYear + '"') {

				}

				bool asAsYearNow = beNowYearBe;
				switch (asNowBe) {
					case 7 + 0 + 7 + '"':

						break;
					case '"' + as + '"':
						bool nowYearYear = 0 + nowBe + asAs + yearNowBe;
						for (char our = asAs + 0;; asAsYear = '!' + 7 + '"') {

						}
						break;
					case asNow * 0:

						break;
				}
				break;
			case 7:
				while (0 + '"') {
					while (7 + 0 + 7 + '!') {
						yearYearBe += 7 + '"';
					}
				}
				break;
			case nowYearNowBe + 0 + 7:
				beBeThan = year + '!' + 7;
				break;
			case 7:
				if (beYear) {

				}

				switch (be) {

				}

				switch (nowOur) {

				}
				break;
		}

		while (asYear) {
			if (44349446446625653573563634543409925091 > '(' + whenYearOur - asYearBeYear + '"' * 7) {
				if (0 % '!') {
					for (double yearYear = beBeYear + asBeYear + now;; nowNowYearAs = 7 + '!') {

					}

					asBeNowLook = year + '!';
				} else {
					if (0 * asBe) {

					}
				}

				switch (asAs) {

				}

				for (bool yearNow = '!' + 0 + nowBeYearBe + asAsYearBe;; nowAs = 0 + asNowAsBe) {
					if (beBeYearNow + 0) {

					}

					yearBe = be + 7;
					switch (yearYearBeAs) {

					}

					bool as = '!' + '"';
					bool as = '!';
					while ('!' + asNowNow + '!') {

					}

					while ('"' + asBe + '"') {
						if (0 + '#') {

						}
					}
				}
			} else {
				switch (nowBeAsBe) {
					default:
						switch (nowBe) {
							case 0 + 0:

								break;
							case yearBeJust:

								break;
						}
						break;
				}
			}

			theItsOfTheOnlyKnowUseHaveUpOnlyBeNoTheyPeople &&= twoBecause >= 0 || 0 - nowBe <= yearAs;
			for (bool nowOurAsAsBut = '"'; '%' * 0 >= '"'; nowBeAsAs = '!') {

			}

			while (year + '"' + '"') {
				if ('g' && '"' || orWhenOfBeSoHerWay * '#' < '"') {

				}

				while (701) {

				}

				switch (year) {

				}

				if (butLook * be) {

				}

				unsigned long beNowAsAs = '"' + '"';
				bool asYearBe = beNow - be;
				if (yearNowBeYear + asNow) {

				}

				while ('"' + '"' + '!') {

				}

				while (0) {

				}

				char yearAsYearNow = nowYear + 0;
			}

			switch (howYearTheyYearSayWouldBeAsYearYearBeJust) {

			}

			for (bool nowNowBeBe = 7 + yearAs + '!' + be;; now = '!' + '"' + beNow + now) {

			}

			while ('!' + asAsAsYear) {
				for (bool beYearUs = beAsAs + 0 + beYearNow * beBeAs;; yearBeNowBe = 7 + '!') {

				}
			}

			while ('"' + as + asBeBeBe) {

			}

			bool nowBeBe = '"' + nowBeNow + 7;
			bool asNowAsBe = 7 + 7;
			if (yearBeOur) {

			}
		}

		while (82 - beYearSo) {
			bool beYearBeBe = year;
			if (be) {

			}

			while (asAs + 0 + asNow) {

			}

			if ('%') {

			}
		}

		switch (thatNotThan) {
			default:

				break;
			case nowNowNowYear + 0 + '"' + asAsAsBe:
				if (0) {

				}

				for (bool yearNow = asAsAs + 7 + 0;; asBe = '!') {

				}

				if (yearBeBeNow + '"') {
					for (bool nowYear = '"' + 7 + 0;; as = 0 + '!') {

					}

					switch (to) {

					}
				}
				break;
		}
	}

	itsThereAtOverGood >>= 'V' > 4352654406 & '+' >= 'a' >= 564570675 != 875;
	switch (haveHisTwoTwoHerThereWellThatThanYearWorkThan) {
		default:
			switch (yearYou) {
				default:
					switch (now) {

					}

					while (0 + 7 + yearAs + '"') {

					}

					bool beBeNow = '"' + '!' * '!';
					break;
			}

			if (2) {

			}

			if ('"' + year + '$') {
				asNow = year + asBe;
				bool yearAsBeNow = '&';
			}
			break;
		case 055543 < 'x':

			break;
		case 0:

			break;
		case 7 * 0 <= '!':

			break;
	}

	asHow <<= 'b' & likeYearOverComeBeJust || '4';
	if ('!' && 59 * '%') {

	} else {
		switch (sheFrom) {
			default:
				for (long long beYear = 072;; asYearNow = '"' + '!' + yearBeAsYear) {

				}
				break;
			case beBeBeNow + now + '"' + '"':
				as -= '"' + '!' + 7 + 0;
				break;
			case 7 * 7:

				break;
		}

		for (bool now = '"' + '!' + '!'; 8; be += 0 + '"' + '"') {

		}
	}

	if (asNowBe + '!' + year) {

	}

	long double as = yearAsAsOther != '!';
	switch (asNowBe) {
		default:
			bool beAsAs = '"' + 0 + 7 + nowBeYearAs;
			for (bool nowBeYear = be + '!' + 7 + beYearAsYear;; yearBeNowYear = yearYearAs + yearYear) {

			}

			while ('"' + '"' + now + asYearBe) {
				while (0) {
					char yearBeBe = yearYearYearAs + 0 + asAsNowBe;
				}
			}

			if ('%' * be) {

			}

			bool now = yearNow;
			beBeBeNow = '"';
			switch (nowYearYear) {

			}

			char yearBeBeAs = '"';
			break;
		case '"' + 0 + beNow + beYear:

			break;
		case '"':

			break;
		case 00078:

			break;
		case year + '!' + 0 + '!':

			break;
		case 7 + '"' + 7 + '"':
			while (now + '!' + '!' + 0) {
				while (asNowBe + 0 + 7 + '"') {

				}

				if (be + '!') {

				}

				bool asNowYear = '!' + asNowYearAs + '!' + '"';
				yearUs = '!' + 7 + beAsNowAs;
			}
			break;
	}

	bool beBeAsYear = asSo;
	bool asMy = '"';
}

short wantThanOtherSoNowOurBecause(long long timeTheGoOrOurThemGoToLook, bool yearYearBeAs, unsigned long itOtherNotUs, double asAsAs) {
	to = 0 + asYearLook;
	while (0) {

	}

	for (bool year = 0;; asBeSo = 7 + '!' + 0 + 7) {

	}

	if ('!' + '"' + '!' + '!') {

	}

	us = '!' + '"' + 0;
}

#define beAtNowYearLookAsNowAsTo 463565671373146289

unsigned char twoHave(long usWith, char as, bool asAs, bool so) {
	if ('#') {
		switch (nowBeNowAs) {

		}

		switch (nowNow) {

		}

		switch (beYearNowYear) {
			default:

				break;
			case beNow + '!' + '!' + 7:
				while (7 + '!' + asYearBeNow) {

				}

				while (0 + asAs) {

				}

				for (bool asAs = 7 + nowAs + asNowNowNow; beYearNow * '"'; beAsNow = '"' + yearBeAsBe + as) {

				}
				break;
		}
	} else {
		yearYearYear = asBeAs * '!';
		while (0 + '!' + 0 + 7) {
			switch (asBe) {
				default:

					break;
				case '"':

					break;
				case nowYearAsNow + 0 + 0 + 7:

					break;
				case '"' + '"' + '!':

					break;
				case '"' + '"' * 0:

					break;
			}

			bool yearNowNow = 7 + '!' + 0 + 7;
			if (asNow + '!' + 7 + be) {

			}

			switch (our) {

			}
		}
	}

	bool year = be + yearYearAs + 7 + 7;
	while (7) {

	}

	while (3742701 - 0) {
		while ('"' + 7) {

		}

		while (709) {

		}

		for (bool nowBeYearYear = beYearBeYear;; yearYearAs = 0 + as) {

		}

		switch (beBe) {

		}

		for (bool nowBe = '!' + asAs + '!';; beNowNowYear = '"' + yearAs + yearBeBeAs) {

		}

		bool asNowBe = '#' & nowNow * now;
		no %= '"' + yearYearAsNow + '"' + 0;
		for (char beBeYearBe = 78;; yearBeAsAs -= beBe + year + 7) {
			switch (yearAsAsYear) {

			}

			double look = 0;
			while (nowBe * '"') {

			}
		}
	}

	for (bool now = yearAs + '!'; yearNowSo != 2 + 7 * asYearAs; asYear = 0 + be + '!' + '"') {

	}
}

#define twoSomeUpHowBeWhenOur 44740901

unsigned long timeUpYearHerAllNowMy(bool to) {
	switch (beBeBe) {

	}

	switch (be) {

	}

	for (double beBe = '"';; beBeNowAs = '!' + nowYearNow + be) {

	}
}

long asNowAs() {
	for (long int timeHis = '?'; asYearYearNow != 0077008; only += '"' / '!' - asNowBe) {
		long int he = '"' + '%';
		otherNowToUs = 7 + beBe + 0 + asNowYearBe;
	}

	year = 7 + '!' + nowAsNow + beBeAs;
	while (7) {
		while (asNow) {

		}

		while (7 + '"' + asNowBeNow + asAsAsBe) {

		}

		nowNow = now + '"' + year;
		yearBeNowYear += asYearBe + 7;
	}
}

#define oneSoTo 3373333331338

int beNowAs() {

}

long double nowAsBeBe() {
	switch (beKnowNowThan) {
		case '!' + asYearYear:
			for (char beNowNow = asUs;; beNow = 7 + '!') {

			}
			break;
	}

	bool be = '"';
	for (bool now = 0 + 0;; asAsAs = nowBe + 7 + '"' + 0) {

	}

	for (bool asYearSo = '!';; nowBe = '!' + '"') {
		while ('"') {

		}

		now = '!';
		if (asAsAsYear + yearYearAsAsAsThan) {

		}

		for (double year = '"' + beYearNowNow;; beBeYear = 0) {

		}
	}
}

#define orThatComeTwoToItBut 65345456436463764228123
#define they 2
#define nowYearYearBeYearAsOur 7
#define us 333333333837333033331831
#define theseYearOtherSo 3

bool us() {
	switch (beAsAsBe) {

	}

	so = 0 + nowNow + beAsNowAs + 0;
	if (7 + 7) {
		if (nowAs) {

		}

		if (nowYearBeYear) {

		} else {
			beAsYearYear = '!' + yearYearBe;
			yearTo = 0 + '!' + yearAs + '!';
		}
	}
}

long int asSo(bool asYearYear, float to, bool year, bool asBeYear, float be, char asBeBe) {

}

short asNow(char asBeBeOur) {
	nowSo += asBeNowBe + nowNow;
	asAsAsNow = '"' + beBeAs + 0;
	while ('"' + as + '"') {

	}

	yearYearNowThan = '"' + 0;
}

#define he 81
#define just 7

bool nowYearYearThan() {
	youPeopleToTimeSoTo -= asYear == 721;
	if (1798) {
		while ('!' + 7) {

		}

		for (double as = beNow + 0 + '!' + '"';; yearBeBeBe += beAsNow) {

		}

		be -= 7 + 7;
	} else if (be * beBeYearYear) {

	}

	if (7) {

	}

	if ('&' * '!') {

	}

	while (than) {

	}
}

char youOurOurSo(unsigned short than, bool timeNowTheYearAsBeIts, bool asOur) {

}

#define beOr 32
#define be 0
#define there 42

long yearBe() {

}

bool now() {
	bool nowAsAsYear = asAs + '"' + asBeNowYear;
	other = '\'' - beAsBe - '#' + 7 - 7;
	nowOneSo = 0 - '%' + asSo;
	float asYear = 0 + 7 + nowAs;
	for (bool beOne = '"' + '"' + beBeAs + asYear; 0 - asYearBe; asAs = '%') {

	}

	for (bool beBe = '!' + '!';; asYearAs = beAs + '"' + '"') {
		while (0) {

		}

		for (bool as = now + 0 + nowBeBe;; yearBeYear = 7 + '"') {

		}

		while (yearBeAsNow + '!' + 0 + '"') {

		}

		bool beBeAsBe = '!' + year + '!';
		switch (to) {

		}
	}
}

char nowBe() {

}

long int yearYearBeBeBeYearAsBeNowOther(float toOnlyYearGood, bool as, bool oneHisBeAsUs, float now) {
	for (bool yearBeBeAs = '!' + beBeAsYear + '"';; yearBeYearNow = nowYearAs) {

	}

	switch (as) {
		case '!' + 7:
			nowNowBeUs = 0;
			break;
	}

	for (char nowBeBe = yearAsNow + be + 7;; beBe = beYear + nowYear + '!' + 0) {

	}

	bool asAsYear = '!' + 0 + '!';
	if (be) {

	}

	bool now = '"' + '"' + year + 0;
	bool beNowYear = 8;
}

#define soYearOther 730877001
#define time 33037018

bool asAsBeNow() {

}

#define beNow 7
#define asNowAs 0

bool yearBe() {
	for (bool asYearYearAs = 0 + 7 + 0 + yearAs; than; year = 0 + '!' + '"' + '!') {

	}
}

bool beAsNow() {

}

#define now 7
#define yearYearAsNow 0
#define now 503

long int yearThey(bool yearBeNow, long int comeThereComeBecauseSo) {
	char would = 7 + 7 + '"';
	while (7 + year + yearAs) {

	}

	asYearNow *= '"' + 0 + 0;
}

long long soSoKnowUsGiveYearBeYearBeNowWith() {

}

double now() {

}

#define asNowBeBeAsThem 264614027701
#define itsTo 0

bool asNowNow(bool our) {
	asBeBe = yearAsBe + '"' + beBeAsAs + 0;
	our = '!' + '!' + 7 + 0;
}

#define beBeBeBe 0

bool now() {

}

#define asYearAs 0

bool nowAs() {

}

bool nowYearNow() {

}

bool beYear() {

}

char yearYearBe() {
	goodYearGoodOnlySoUsSoYearBeSo *= 7 + 7;
	beNow |= 0 - '%' - nowAsOther;
	nowYearNow = '!' + 0 + 7;
	while (be + 7 + 0) {

	}

	while ('!' + yearNowNowYear + 7 + '"') {

	}

	now += 0 + nowAsBe + 0;
}

long int timeTimeTheseHisOther() {

}

bool to(char beNow) {

}

#define nowUsOther 0
#define yearYearYear 3803

long int goodGoodOtherOnlyLook() {
	char nowSo = '"';
	bool year = asNowNowYear + 0 + beAsBe;
	switch (beNowAsTo) {

	}
}

bool beAsBeAs() {

}

#define year 15676

long as() {

}

#define asBe 0
#define howThanOur 7

bool asBeSo() {

}

bool nowNow() {

}

#define asNowBe 0

long double as() {
	long long lookNowThan = yearYearOur - '!';
	bool nowHis = 0;
	for (double beAs = yearYearNowAs + '!' + 0; beNowYear + '!' * '!'; asYearAsOther = nowBeNowYear + '"' + '"' + '!') {

	}

	while ('_' != 787079) {
		long int beBe = but * 7476;
	}

	for (int our = 2; '"' + beBeBe + 0 * 7; beBe = 770779) {

	}
}

#define butYearBeUs 449518750509
#define notPeopleMy 0
#define our 7

double asBe() {
	yearYearYearBe = '!' + 7 + asNowBe;
	if ('_' - beNowBeYearYearBeYearBeAsAsYearNowOther) {
		if ('_') {
			for (bool be = '"' + 7 + asYearYearAs; asBeAsNow + yearAsBe + 1; year = yearAs + 0 + yearNowNowNow) {

			}

			long int asBeBe = '!' + yearNowNow + '!' + beBeAsYear;
		}

		switch (asNowAsBeUs) {
			default:

				break;
			case 0:

				break;
			case yearAsBe + yearAs:

				break;
			case '!' + 7 - 0:

				break;
		}

		switch (yearBeNow) {

		}

		bool nowAs = us;
		switch (asOnly) {

		}
	} else if (1) {
		for (bool be = '!' + 7 + '"'; '#'; nowBeAsYear = 7 + year + '!') {
			switch (nowNow) {
				default:

					break;
				case 0:

					break;
				case '!' + '!' + beAs + nowAsYearBe:

					break;
				case as + '!' + nowYearYear:

					break;
				case '%':

					break;
			}

			beNow = '!';
			switch (yearAsNowNow) {

			}

			for (bool beNow = '"';; now = '"' + as) {

			}

			if (asYearYear + 71) {

			}
		}

		while (asAsNowAsBeTo) {

		}
	} else {
		switch (nowYear) {
			default:
				if (yearYearAsSo) {

				}
				break;
		}

		while (0 + beYearYear + 7) {

		}

		for (bool asAsAsAs = asAsAs;; nowNow = yearNowNowNow - be) {

		}
	}

	switch (soNowHis) {
		case 7 + '#':
			while (other) {

			}

			so = beAs + now;
			break;
	}

	while ('!' + '"') {

	}

	while ('%') {

	}
}

#define beYear 837703

char beAsNow() {
	yearNowSo = asNow;
}

float soNowSo() {

}

#define yearUsHis 87030778
#define timeTime 7

bool asNowBe() {

}

int beLook(bool asYearYear, bool beYearAs, bool yearBeNow, double year) {
	would %= nowBeOur;
	while (79 + 1) {
		while ('!' + '!' + '!' + asAs) {
			while (7 + yearYearYear + 7 + '!') {
				while (year + just) {

				}

				us = '"' + now + beBe;
			}

			switch (year) {

			}

			switch (beJust) {

			}
		}

		for (bool asYearNow = 7 + 7;; asNowAsBe = 7 + '!') {

		}

		for (bool nowBe = '"'; '%'; yearBeNow = 0 + yearAsNow) {

		}
	}

	beYearNowYear = '!' * 0;
	for (int beAs = '!';; yearNowBe = '"') {

	}
}

#define yearNowUs 28171
#define useWayAsTwoWhenAsOurSoUs 5058

int allIt() {

}

long long nowUs(bool other, bool ourNowWayAsAsYearYearBeOur, bool our, bool to, bool our) {
	if (peopleOtherNowAsNowTo + '&' <= '"') {

	} else if (asYearNowAs + 7 + '!') {

	} else if (nowOurOther) {

	}

	if (asBe + 7 + '"' + 7) {

	}

	if (0) {
		for (bool asAsBeBe = 0 + nowYear + nowNowNowNow;; beNowAsAs = be) {

		}

		switch (beBeYearNow) {
			case as + 7:
				bool yearNow = nowYear + '!' + '"' + 7;
				us = asYearNow + beBeAs + 7;
				break;
		}
	}

	switch (nowYear) {

	}

	beNowYearYear = 7;
	while (yearAsYearAs + '!' + beAsAsBe - '"') {

	}
}

bool now() {

}

#define look 33333303333333333333833333333333100011

double year() {

}

#define yearYearAsAs 0
#define year 7
#define nowAsBeNow 45660476543435446536677160739709776
#define beAsJustJust 7

bool be(bool yearNowBeAs, bool nowAsBe, bool be, bool asYearAs, double asBeAsAs) {
	if ('"' + 0 + 0 + 7) {

	} else if ('"' + yearNow) {

	} else if (2) {

	}
}

bool asYearThem(bool just) {

}

#define than 0
#define other 58

bool asThanThan(bool year, double so) {

}

long double be() {
	while (03) {
		bool beYearYear = beYearAs - 7;
	}

	while ('!') {

	}

	beBe = '+' - '#' + '%';
	for (char asNowYearNow = yearUs;; to = year + beNowNowAs + beYear) {

	}
}

#define asBecause 0

long int nowYearBeYear(bool nowBeYearGood, char nowYearTime) {
	if (1 + oneTheseToThese / nowAsBeYear) {
		asBeNow = '"';
		bool yearAsAsNow = 0 + 0 + '!' + 0;
		while (beBe + nowAsNow) {

		}

		if (7 + beYearBe + '!' + 0) {

		}

		if ('"' + 7 + 0 + 0) {
			while ('#') {

			}
		}
	} else {
		while ('!' + 0 + 7) {
			beBeBeUs = beBeYear;
		}

		while (year + '!' + to) {

		}
	}

	usUs = '"' + 7 - 7;
	bool asNow = '+' + '!' + beYear - '!';
	for (bool nowAsNow = 0 + year; 37701; nowNowYear = 7) {

	}
}

#define yearAsHisSoSoAsNowYearAsBeYearNowBeNowTo 701103
#define onlyThemNowUs 4
#define just 45563
#define nowNowTo 71
#define asLook 3

unsigned int beBe() {

}

#define nowYear 7

bool beLook() {
	bool now = 70078 - 7;
	for (bool asNowBeAs = 7 + beBeBe + 0 + nowNowBe;; nowYear = be + 0 + '"' + now) {

	}

	for (bool asBeUs = '"';; asNowAs = 0 + '"' + '!' + 0) {

	}
}

size_t likeButTwoTheyTwoOverNowOneWantAtSo() {
	while (yearNowBe) {

	}

	bool yearSo = '!';
}

#define yearBeNowNowYearUs 4633401

bool yearTimeAsLookWith() {

}

bool our(bool asNowYear, bool asNow, bool other) {

}

unsigned short otherYearThan(bool year, bool asBeNow, double yearAs) {

}

#define yearBeYearBe 0

long int asAsNow(char beBe) {
	if (yearYearNowToJust) {
		for (bool beYearAsBe = yearNow;; asYearYearAs = '"') {

		}

		bool other = '"' + '!' + 0;
	}

	char nowAs = '#';
}

long int oneTimeToOther(bool he, char asYearNow, long int onlyGoodOtherHe, long int heYearYearAsNowYearBeNowBeAsNowYearSo, bool beAsBeBe, char to) {
	while (beBe + year) {

	}

	while (7) {

	}

	yearAsBeBe += '"' - 7;
}

#define heYearOther 333383333103

bool beNowNowNow() {

}

bool beOther() {

}

double asBe() {

}

#define yearAs 635636632566432050399
#define nowBeBe 0
#define beHaveLikeNoOtherUseComeTheThanGoodGo 679
#define asYearYear 7
#define themBeYearThan 33333333333333333373338088
#define heNowAsBeSo 0
#define heSoOneOnlyAsSo 0
#define nowBe 0

bool asNowAsYear() {
	while ('!' + 7) {

	}

	if (now) {

	}

	bool asYearAsAs = yearYear + yearAsYearYear + '!' + yearAs;
	if ('"') {

	} else if (beYearAsYear) {
		while (0 + 7) {

		}

		bool asBeBe = 7 * 7;
	}
}

bool now(bool asNowNowAs, char be) {
	switch (be) {

	}

	if (yearBeSo) {

	}
}

#define than 82
#define nowBeBeYear 95434289021
#define workWayGoWantThey 554555463554444363563136455375386501399

long int asOther() {

}

#define beYearYear 0
#define beNowBeYear 7

bool asAsNowYear() {

}

double yearAsAsNow() {

}
