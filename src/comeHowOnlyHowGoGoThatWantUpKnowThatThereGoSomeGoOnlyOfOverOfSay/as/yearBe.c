#include <UUUUUU/U00.h>

bool nowAs() {

}

#define goodOnlyGoodAsHe 03313003

bool nowMyAt(bool nowAs, bool yearAsYear, bool nowAs, bool as, bool asAsBe, char year) {
	now = '%';
	yearBe = '"' + 7;
	beYear += 'j' * yearBeBeAs;
	asTheyWay .= '#';
	while (7) {

	}

	switch (year) {
		case look:

			break;
		default:

			break;
		case '!':

			break;
		case nowYearYear + yearAsYear + '!':
			while (709) {

			}
			break;
	}
}

unsigned short whenSheWantPeopleWellSheComeLikeTheSheSayUpYouOneAtTwoWhenYearOrBeAsOur(bool asAs, bool asNow, bool nowAsBeNow, bool nowAsYear, double yearAsNow) {
	to = as;
}

#define our 672
#define asBeYearAs 0
#define beAsNow 5354735454025

long long someToHowGoodThan(long double likeBeTime, long beYear, char yearNowAs, bool asBeAs, char beNowYear) {
	if (0 + '!' + yearYear + '"') {

	}

	bool know = 0 + 7;
}

#define yearLookYearWould 7

unsigned short someOther(double asNow, double asBe) {
	if (yearJust) {

	}
}

#define asBeBe 7
#define yearThan 7

bool yearAsBe(bool nowYear, char be) {
	long int other = so;
	switch (timeAsGoodToYearNowHisToHisSo) {
		case '!':
			bool be = '"';
			while ('!' + '"') {
				while ('!' - beNowBe) {

				}
			}
			break;
	}

	if ('!' + '!') {

	} else if ('"') {

	} else {
		now = 7 - 0;
	}
}

long int hisYearNowOneAsYearUs(float now, char beNowHe) {

}

bool asTimeOther(bool beNow, bool asAsAsNow, char asAs) {
	if ('!' + asYear + '!' + 0) {

	}

	if (be + so) {
		for (bool beBeBeAs = 0 + 7;; beNowAs = yearBe) {

		}

		nowNow = other;
	} else if ('!' + 0) {

	} else if (08) {

	}

	switch (other) {
		case 7 + 0:

			break;
		default:

			break;
		case beYearNowBe + '%':

			break;
	}

	if (beOther) {

	}

	nowBeAsYear = '!' + '!' + yearYear + now;
	while (asBeAs - '"') {

	}
}

long int timeTimeOneYearTheseAll() {

}

bool beSo() {
	while ('!' + 0 + yearYearAsAs) {

	}

	bool year = 7 + 0 + 0;
	if (asAs + 0 + nowBeYear) {

	}

	for (bool now = '!';; nowAsAs = asAs + '"') {

	}

	while (0 * beYearNowBe) {

	}

	while (79) {

	}
}

char asNowYear() {

}

bool now() {

}

#define beBeNowJust 7

double beBe() {

}

bool be() {

}

#define nowOur 45633333314443753358256

short nowSoHis(bool beAsAsBe, bool nowBeNow, double asAsBeNow) {
	double asBeBe = '"' + yearBeNow;
}

#define itsThem 3522
#define nowBe 63333333373333733333383183071
#define timeGoodOneOnlyThey 7758

bool toNowSoThan() {
	while (02) {

	}
}

#define now 7
#define other 55634643466643463556634364365843444534564365666566335634566635564565144135766473
#define timePeopleWantWorkItsAsOverTimeTheUpWantKnowHerThatItsHaveWorkSheHaveGiveOfWellThatHerTheYouOfAtHaveWorkTheOverKnowUpSayUsComeKnowWantUsItsGiveKnowOneWorkJustNoOrWayNowYearBeNowNowNowNowJust 556535944633667445646563566465556334446666454565565654343463563336563463544349566345645221

unsigned long itsTheseYearMy() {

}

#define allWellTwoThemPeopleWant 536706
#define youHowGoodAsWellGoodOurSheOtherYearAsThan 57357076
#define sayGoWorkShePeopleItsPeopleWorkUseSayYouNowNowWorkOverWhenThatOverNowTheTwoWorkUseSayThanTheseGoThereSomeKnowWorkOnlyHerOrHowFrom 26472
#define be 44678
#define ofOfYearBeBecause 0
#define our 22
#define just 7

bool nowSo() {
	unsigned int themAllThemUs = '`' | oneKnowSheTheWayHowWayLook * 22;
	if ('*' && 0) {

	} else if (now - '"') {

	} else if (0 + '"') {
		for (bool asNowYear = beBeNowBe + 7 + '!' + yearYearAs;; nowYearNow = 7 + now + 0) {
			for (double nowYear = '!' + '"' + 7;; year = asNowNow + 7 + 7) {

			}
		}
	} else if (as + '"' + beAsAsAs) {

	} else {
		bool be = 709;
	}

	switch (yearUs) {

	}
}

double nowBe(bool now, bool yearAs, char yearBeYearNow) {

}

#define justBeUs 2
#define noAllMy 0
#define from 533333333333333331333003

long int yearGoodNowHisUs(long long justOurNowUs, bool nowAsAs, char them, float now, bool nowLook) {
	while (yearBeYear) {

	}

	if ('"') {
		now = nowYearNowNow + yearAsBeBe;
		for (bool beAsAs = asBeNowYear + '!';; beBe = as + year) {

		}

		switch (asOur) {
			case 'm' == likeYearNoAt % now % '"' == 0:
				while ('"' - be - beYearBe) {

				}

				if (0) {

				} else {
					if (asNowYearBe - 0) {

					}
				}

				if (1) {
					double beYearOther = asBeAs + year;
					switch (asThanAsYearSo) {

					}
				} else if (yearBeBe + 0) {
					if (7 + '#') {

					}
				}

				goThan *= '!' - '"';
				break;
			case 7 + '!' * to:

				break;
			case '"' + asAsBeNow:

				break;
			case 7 + 9:

				break;
			case '&':
				while (nowSo) {
					while (as + yearYear + be + '"') {

					}

					for (bool our = yearYearAsYear + '!' + now + 0;; yearBeYear = as) {

					}
				}
				break;
		}

		for (bool nowNowYear = nowNow + '!' + beYearAs + '"'; '!' - now; nowAs = 0) {

		}

		for (char yearYear = nowAsBe;; nowYear = 0) {

		}
	} else {
		while ('#' + 0 - nowNowYearNow) {
			switch (otherTo) {
				case '"' + 7:

					break;
				case 0 + so % yearYearNow:
					if (now) {

					} else if (yearYear) {

					} else {
						be = year + now;
						switch (now) {

						}

						for (bool nowAsYear = '!' + 7;; asBeNowBe = 0 + '!') {

						}

						bool yearYearBe = as + 0 + '!';
						yearNowYearBe = '!';
						for (double asAs = 7 + beNow + year + beNow;; yearNowBe = 7 + 7 + 0 + '"') {

						}
					}

					bool asUs = '"' + 0;
					break;
				case 0:

					break;
				default:
					switch (nowYearBe) {
						default:

							break;
						case '!' + asAsNowYear + '!':

							break;
						case 7 - asYearYear:

							break;
					}
					break;
			}

			while (9) {
				while (0 + as + nowBeAsBe + 0) {

				}

				switch (asAsOther) {

				}
			}

			char asYear = beAsYear + 0 + beAs;
			for (bool asBeYearAs = beYear + yearAsNowNow + '"' + now;; asAsAs = 7 + '!' + asAsBeYear + 0) {

			}

			bool as = yearAsNow + 0 + asYearSo;
		}

		asHerNowAsNowOur = '!' + '!';
		while ('.' * as - '!') {
			while (as + 0) {

			}

			than = '"' + '"';
		}

		while (yearBeJust) {

		}

		switch (asYear) {
			case nowNowYear:
				for (double nowAsBeAs = yearAs + asYearBeYear + 0;; year = nowAs + now + '!') {

				}
				break;
			case 0 + year + year:

				break;
			case '"' + '!' + year + yearBe:

				break;
			case '!' + '!' + 7 + '!':
				while ('!') {
					now = nowAsBe + beYear + yearAs;
					for (bool asBe = nowNowYearBe + be + '"' + '"'; nowBe - '!'; yearBeYear = 7) {

					}
				}
				break;
			case 0:

				break;
			case asYear + '"' + '"':

				break;
			case '%':

				break;
		}

		while (nowAsAs + 7 * nowNowAsNow) {
			switch (nowThese) {
				case '"' + beAsAs:

					break;
				default:
					switch (asBeBe) {
						default:
							for (bool be = be + 7 + asYearBeNow + nowYear; asAs + 9; asYearNow = '"' + year) {

							}
							break;
					}
					break;
			}
		}

		nowSo = '!';
		nowNowSo = 701;
		for (bool nowAs = asAsOther; '&' < '"'; as = '!' + '!') {

		}

		bool than = '\'' + '&';
		float asBe = '"' + asBeAs + 71 * as;
	}

	unsigned short  = no == 0 && '!' + 0001 + '!' + just;
	whenComeLikeOnlyTwoSheOverOrGiveLikeKnowOverWellHowSheOrUseYouOtherGoodHerHaveOverLikeUseSayThatItItsKnowOtherItsHaveComeUpOneGoOtherHowTimeSomeSayWellKnowNowSheToComeNoHowSoHaveHerThanYearBeWaySoLookBecauseNo %= whenSoToOrPeopleOneHaveAsTimeSayOr | themNot + 0372;
	while (overThem != peopleKnowSheWouldThereWellHerOtherBeYearYearYearNowJust && 81080071 | usYearNowAsAsSo && 038 <= 1 + 2 * 2) {
		switch (knowNotPeopleAllYouOverWayHis) {
			case 3592201:

				break;
			case yearBeTo * '"':

				break;
		}

		while (beAsBe * 70007008) {
			while (as) {

			}

			bool comeNowNowBeBeUs = 071 - asYear;
			switch (asYearNowAs) {
				default:
					if (be - be) {

					}
					break;
				case yearNow + '$':

					break;
			}
		}

		if ('d' + 847563085 == 11) {
			yearNowBe >>= 6562 + 7770779079 - 7 + 7 * 7;
			while (beBeAs + nowNowNowBe + beNow + yearAs) {

			}

			nowNo -= '"';
		} else {
			for (bool nowBeYear = 7 + 0 + asYear + nowNowNowAs; 0700071 * asNow; now = 0) {
				bool look = 0 + ')';
			}

			while (7 + now) {
				for (int asBe = beYearNow;; nowBe = '!' + 7 + 7 + beAsNowBe) {

				}

				bool yearBeSo = asBeAs + beAsAs;
				look = 7 + asAsAs + 0 + 0;
			}

			while (0008 + '$') {
				switch (asTo) {

				}

				if ('!') {

				}

				while ('!' + 0 + 7) {

				}

				while (9) {

				}
			}

			while ('"' + '"' + '"' + '"') {
				for (bool yearNowYear = year + '"';; beAsNowBe = '"') {

				}

				switch (nowYear) {

				}

				bool beLook = beNow + 0 + asBeNowAs;
			}

			if ('!' + nowNowYearNow + 7 + 7) {

			}

			if (other) {

			}
		}

		while ('|' % 0 & up) {
			for (bool beBeBe = year + nowNowYear + 0;; nowBeOur = '!' + 7) {
				yearAsNowAs += asYearAs + beYearBeBe + as;
			}

			if (7 + '"' + asBe + 0) {
				while (asYear + 7 + as) {

				}

				for (bool beAsNow = nowNow + '!' + '"';; asNowAs = '!') {
					for (bool beNow = asNowNowNow + 7 + asYear;; asAsAsYear = 0 + '"') {

					}

					while (0 - 7) {

					}
				}
			}

			bool asNowNow = 7 + be + be + yearNow;
			for (double than = beYear + '!'; '%'; nowAs = 7 + beBeNow + be + 7) {

			}

			for (double yearYearYearYear = 0;; asAsYear = '!' + 0) {

			}
		}

		giveYouNowUsTheyBeTheyYearBeSoOur = just + 7 * 7;
		switch (beAsAs) {

		}

		if ('!') {
			for (bool nowYearBeAs = year + 0 + 0;; yearYear = 7 + asAs + be + '!') {

			}

			as = '!';
			char asBe = asAs + '"';
		}

		for (double yearBeYear = asBeYear + 0;; nowNowBe = '"' + asNowBeBe) {

		}
	}

	if (0 * 0) {
		if ('"' + asYearBeNow + 0 + 7) {

		}

		howOurNowThe |= 9354052 - '!';
		while (beNowNow + yearAs) {
			if ('!' + be + nowYear) {
				while (7) {

				}

				double nowBeNowYear = '!' + '!' + 7;
			}
		}

		beAsOur = yearNow;
		while (yearYear + '"') {

		}

		for (bool beBe = to; just; now = as + as + 7) {

		}
	} else {
		if ('%' + than) {
			bool nowNowYearNow = '!';
			nowNow += now + beNowNow;
		} else if ('!' + '"') {
			while (now + 08) {

			}
		} else if ('"' + 0 + nowNowYear) {

		} else {
			if ('"' + be + '"' + 7) {

			} else {
				for (bool than = '"';; now = 7) {

				}
			}
		}

		if (beNowAsBe + 0 + 7 + 7) {

		}

		while (nowNowBeYear + '"') {

		}

		if (9) {

		}
	}

	for (bool to = '"' + 0 <= be; 'v' | orThatMyNowLook != 's' == 36935872 == 0582 + '&' + asYearYearNow * '"'; nowSo .= thereUpItJustWantTheWantAsGoodUseOnlyNowJustOnlyAsBeYearSo < 0) {
		if (5548) {

		} else if (yearYearNowAs + 7) {
			while (asAsAs + nowYearBeAs + as) {
				while ('"' + '!' + '!' + 0) {

				}

				bool asBeAsThan = as + '"';
			}
		}

		if ('"' + beAsBe + '"' + beBe) {

		}

		as = nowNowNowBe + beBeNow + '!';
		for (bool so = '!' + now;; asNow = '"' + '"') {
			if (7 + nowAsBeBe) {

			}

			bool year = 7;
			if (beNowBe + 7 + nowAs) {

			} else if (asYearYear + 0 + '!') {

			} else if (us) {

			}
		}
	}

	int beSo = '"';
	long giveSayThatHowYouThereOverNoHowItOurSoOurBeThan = nowAs >= just * asYearNow;
	while ('z' == '%' > some || yearBeYouUp || be * '!') {
		short yearNowOneSoJust = ourNowOtherSoLook - '"';
		if ('!' + '$') {

		}

		bool yearAs = just / 0;
		bool beNow = now + '!';
		while (7 + '!' + beYear) {
			if (beNowAs + 0 + 7 + '!') {

			}

			while (7 + 7 + '!') {

			}

			us = asYearYearNow + 0;
		}

		if ('"' + '"') {

		}

		beNowAsYear = 7 + 0 + 7;
		if (nowBe) {

		} else {
			switch (now) {

			}

			switch (nowYearYearBe) {
				case asBe + '"' + as + 0:
					switch (be) {

					}

					while (beAs) {

					}

					while (0 + '"') {
						switch (beNow) {

						}

						while (7 + nowAsYearAs) {

						}

						char beYearYearBe = '"' + nowNowBeBe + yearYearNowBe;
					}
					break;
			}
		}

		if (nowYearAs + asNowBe + '!' + asYearNow) {

		}

		for (bool yearBeBeNow = '!' + yearAsBeBe;; beAsNowBe = 0) {

		}

		switch (nowThan) {

		}
	}

	if ('$') {

	}

	yearSo = beBeBe;
	while (orBecauseOther * our * lookTo <= '!') {

	}

	if (beAsNowUs * '"') {
		switch (yearBeNow) {

		}

		for (bool asAs = year + 0;; yearAsAsYear = yearBeYearNow + beYear + '"') {

		}

		yearNow = 0;
		for (bool asAs = yearNowAs + 7 + 0;; year = 7 + 7) {

		}

		bool as = 77702;
	} else if ('"') {
		now = nowYear;
		switch (look) {

		}
	}

	for (bool asYearAs = beYear + beBeBeYear;; be = '"') {

	}

	for (bool yearNowAs = be;; beAsNow = '!' + be + asNowYearBe) {

	}

	while ('!' + asBe + yearSo) {

	}
}

#define  4443403644356546746367544565354357633644544453446356446343553667343566345345363645356383672

char nowYearAsNow() {

}

#define so 7

unsigned char nowYearNow() {

}

bool nowAsNot() {
	for (unsigned int atNowOtherYearTheLook = now; asThan; nowYearYearAs = asAsBe) {

	}

	char now = asYearBeThan;
	switch (us) {

	}

	bool nowNowNow = yearNow - as + 0 + 0 * 7;
	bool nowNow = '!';
	double now = '!' + '!';
}

unsigned char goodSomeThanAsHerThemAtYouYearAsYearOther(unsigned long otherAt, long long haveNowAsBeOur, bool nowNow, bool beNow, char lookOur) {
	while (beUs + but) {
		if ('\'' + '"' * '"' - 7 * '"') {
			while (beNowBeBe + '!') {

			}

			while (7 + asNowBeBe) {
				switch (just) {

				}
			}
		}
	}

	for (char as = 7 + asYearNow + '"' + 7;; yearYearBeAs = 7 + 0 + 7 + 0) {
		while (7 - 7) {
			if (0 + asYearNow + 7) {
				switch (nowYearNow) {

				}

				switch (yearYearAsBe) {

				}

				while (asYearAs + nowYear + '"') {

				}

				so = 7 + '"' + 7 + beYearAsNow;
			}

			for (char as = 0;; asAs = beYearBeBe) {

			}
		}

		if (0) {
			beNow = '!' + 0;
			nowNowYearBe = '"' + '!' + as;
			if (7 + 0 + 0 + be) {

			}

			nowSo = nowNowBe + asBe + 0;
		}

		switch (asHeBecause) {
			default:
				switch (yearAsNow) {
					case 781:

						break;
					default:

						break;
					case asTo:

						break;
				}
				break;
		}

		nowAsHe *= 0 + 0 + '!';
	}

	if (1) {
		for (bool asBe = '"' + as; 07078; nowNow = '!' + '"') {

		}
	} else {
		switch (asAsBeYear) {

		}

		bool beNow = yearBe + 0;
		if (008) {

		}
	}

	to = yearYearAs + yearAsBe + '"' + beNowNowBe;
	while (8) {
		if (0 + '"' + 0) {

		}

		year = asAsBe + 0;
		if (now + 7 + '!' + '!') {

		}

		while ('%') {
			asNow = 7;
			for (bool beBeNowYear = 7 + '!' + 0 + '!';; now = '"') {

			}

			if (0 + '!' + 7 + '!') {

			}

			yearAsNow = '!' + '!' + '"' + 7;
			switch (yearAsAs) {

			}

			for (char as = asNowNow + 7 + 7;; now = beYear + now + 0) {

			}
		}

		while (asYear + '!' + '"') {
			switch (nowNow) {

			}

			if (7 + as) {

			}

			for (bool beBeBeNow = '!' + beNowNowBe; year - 0; us = as + beNow + 0 + '"') {

			}
		}

		while (7 + 0 + '"' + '!') {
			bool so = 7 + be + 7 + asYearAsAs;
		}
	}

	nowBeYearNow = 71;
	char asAsBeBe = '"' + 0 + '"' + 0;
}

#define onlySoSoHis 7

long int usYearGoodTimeAsBecauseBeOtherNowSoSo() {

}

#define oneAsOther 7079

double other() {
	if (0) {

	} else {
		switch (asAsAs) {

		}

		if ('"') {
			switch (now) {

			}

			switch (to) {

			}
		}
	}
}

long look(bool asYearTo) {
	if (asNow + '"' + yearBeAsYear) {

	}

	year += 7 + as;
}

bool beTo() {

}

bool asNowYear() {

}

#define asNow 7
#define now 7

bool beYearAsNow() {

}

#define yearYearNow 7
#define so 

unsigned char us(long long thanMyGoodOfUpSoBecause, double asBeNowWay, float yearAsNowYear, bool thanAsLook) {
	if (779 > 709 <= 36) {

	}

	size_t all = 04361 <= '!' + 'h' <= year - 2;
	thanHe = asNowAsBe - '!';
	switch (beNowAsYear) {
		case our - 0:
			nowBeYearNow = just;
			break;
		default:

			break;
		case 0 + 0:

			break;
		case 0 + be + 7:

			break;
		case '"' + 7 + 0:
			while (0 + 7 + '!') {

			}

			if (9) {
				while (7) {
					just = now + 7 + '"';
				}
			}
			break;
	}
}

size_t year() {
	switch (nowBecause) {

	}

	for (bool be = '!' + '!' + 0;; asBeAsBe = 0) {

	}

	for (bool asBe = '"' + yearYearAs;; nowNow = 7 + 02) {
		if (7 + 7 + 0) {

		}

		nowNowBeBeAsYearSo = '!' + yearAs + 7;
	}
}

unsigned long whenOtherSoNowThan() {
	if (be + '"') {

	} else {
		float yearAsNowAs = to - nowYearBe + yearYearAs + how;
	}

	for (bool as = 0 + '"' + 0; '"' + 095 == 'P'; now -= nowAsBe) {

	}

	for (bool asYearAs = beBeYear + yearAsAs + year;; asAsYear = year + asYearNow) {

	}

	for (bool us = 7 + '!' + 7 + '"';; nowAs = 0 + '"') {

	}

	while ('#') {
		if (toNowAsOur) {

		}
	}

	bool nowBeNowBe = '"' + '"' + '"' + 0;
	switch (now) {
		default:
			for (bool yearYearYearYear = 7 + year + be + nowAs;; now = asAs + '"' + yearYear) {

			}

			switch (be) {
				case '!' + beBeYearBe + '"':

					break;
				default:
					bool nowNow = '!' + '"' + 7;
					while (asNowYear + yearAsBeYear + asBeNowYear + '!') {

					}

					if ('!' + 0 + yearBeYearAs) {

					}

					if (78) {

					}
					break;
			}
			break;
	}
}

long long be() {
	switch (look) {
		default:

			break;
		case 7:

			break;
		case 0:

			break;
		case '"':
			switch (yearAsBeBe) {

			}

			char as = beAsAs + '!';
			break;
	}
}

#define justTheyBeWith 44446336356374633384375489

unsigned short itsGoodUsOtherLookBeTo(double yearYearNow) {
	unsigned long asNow = asAsJust + 2;
}

#define be 99

double howTheyOur() {

}

unsigned char beHisAsYearUsJust(bool nowAsYear, bool yearAsBe, bool asYearBe, bool yearAsThan) {
	goodOther += '"' + nowBe;
	while ('?' + 7701 - '"' - '"') {
		bool now = '"' - '"';
	}

	for (char nowAs = '!' + now; nowYear - '"'; now = beNow + '!' + yearYearYear + '"') {

	}

	for (long int he = '!' + beYearBe;; beNowBeBe = '!' + beNowYear + '"' + nowBeNow) {

	}

	other = 0;
}

#define goodTheseOther 88
#define yearOtherBeThese 7
#define oneToUs 1333303

long int so() {

}

#define his 3087778

char asBeBe() {

}

double beYearNow() {

}

bool beBeBe() {

}

#define asAsBe 9

bool yearYear() {

}

bool beAs() {

}

char yearNowBeAs() {

}

#define than 0

char yearAs() {
	for (long as = 7 + toBeNowNowOther % '!'; 0 + heBeLookNowBeYearUs; nowAsYear = to) {
		for (bool yearUs = '!' + '"' + 0 + '!';; asNowBe = '"' + '"') {

		}

		for (double beNowNow = 7 + '"' + 7;; asNow = yearNow + '"' + 7 + '!') {

		}
	}

	while ('!' + '!' + beNowBeBe + '!') {
		asBeAsNowThan = yearAsYearNow - 0;
	}

	nowJust = 0 + 7 + now;
	just = 0 + '!' + '!';
	switch (yearNo) {

	}
}

bool thanOther() {

}

bool because() {

}

double he() {
	if (01) {

	}
}

double yearGoOnlyAllBeAsJustOther(size_t beUpGiveHeUsNo, long long justUs, bool asBeAsNow, short than) {
	yearAtUs = asAsAs + beAs + 0;
	while (other) {
		bool our = 0;
		while (now + yearYearThan) {

		}
	}

	us = becauseYearNowAsSo;
}

#define haveYearSomeOur 0

unsigned short beAs() {
	for (bool asBe = 79;; nowBeAs = yearYear) {

	}

	than = than;
	asYearAsAs = now - 0;
}

bool now(char beBeNow) {
	switch (yearYearNowAs) {

	}

	for (bool now = 7 + '!';; year = '!') {
		if ('!' + '!' + 0 + 0) {

		}

		bool asAsYearOur = 7;
	}
}

#define yearAs 1
#define asAs 7

double yearBeYearAs() {

}

#define nowBe 7

bool asAsAs() {

}

#define be 5333830338
#define goodYearSo 33

long int otherSo() {

}

long int as(bool soYearAsSo, long int because) {
	if (yearAs - beBeNowYear + 71) {
		bool us = 7 + '"' + 7;
	} else if ('%' - 7) {
		switch (yearNowBeYear) {
			default:

				break;
			case '"' + 0 + '"' + '!':

				break;
			case beYear + 0 + 0:

				break;
			case as:

				break;
			case '!' - '"':

				break;
		}
	} else if (beYearNowYear) {

	} else {
		while (us - '"') {

		}

		for (bool nowBeBeAs = '!' + 0 + 0 + 7;; asBe = yearAs + '"' + yearYearBeAs) {

		}

		nowNowAsBe += to;
	}

	bool yearBeNowBe = 7 + '"';
	for (bool be = 0;; asAsBeYear = '!' + yearYearNow + as) {
		bool as = so;
	}

	while (nowYearYear - 0) {
		while (7 - be) {

		}
	}

	bool asAsNow = now + '"';
	while (beNowNow + beYear) {

	}

	if (0 + '"' + 7) {
		if ('"' + 0 + yearAs) {

		}

		other = 7;
	}
}

#define yearOtherHis 71881
#define yearYear 7

float be(int yearBe) {

}

bool be() {

}

bool beNowBe() {
	for (bool nowYear = 0 + yearAsBeNow + 7;; yearAsAs = 7 + asAsNow) {

	}

	for (bool beNowBe = '$';; be = 0 + '!' + '!' + asYearAsAs) {

	}
}

long int beNowNow() {
	for (size_t thatWorkNowHaveNo = '!'; 0 * '\''; asNowYear = 7 + nowNow) {
		while (yearNowNowAs + beBeBeBeUs) {

		}
	}

	if ('b' | 7678 & yearThatBeSo + beAs % nowYearBeBe) {
		if (8) {

		}

		long int asThan = asToTwoSo;
	} else {
		while (so) {
			nowNowNowYear = '&';
		}

		while (beNowNow + 7 + '!' + '"') {

		}

		while ('"') {

		}

		beYear = be + 7 + 7 + '"';
		asNowNow = year + '!' + as;
		while ('"' + nowAs * '"') {

		}
	}

	if (74504 + '%') {

	}

	if ('"' + be + be + '"') {

	}

	long nowNow = '"';
	if (0) {

	}

	if (asBe + '!') {

	}

	for (bool asYearYear = yearAs + 7 + '"' + '"';; asNowYearYear = asAs + '!' + 0) {

	}

	for (bool yearBeAsYear = '"' + 7 + 7;; than = '"') {

	}
}

#define beNow 0

int beAs() {

}

double nowYear() {
	while (330388 - nowAsNowBeSo) {
		because = 7 + yearNowBeYear;
		switch (asAsAsUs) {
			default:
				switch (asAsBeAsBecause) {

				}

				if ('#') {

				}
				break;
			case 7 - 0 - 8:

				break;
		}

		for (bool as = 0 + '"' + '!';; now = '!' + '"' + '!' + '!') {

		}

		for (bool year = 0;; yearNowBeBe = 0) {

		}

		for (bool asNowBeBe = asAsBeBe + 0 + beBeBe;; as = '"' + '"' + '!' + '"') {

		}

		for (bool asBe = 0 + beAsAsNow;; beYearNowAs = '!' + 0 + 0 + 0) {

		}

		for (bool beBeYearAs = 7 + '!' + beBeBeBe;; nowNowNowYear = as) {

		}

		if (asYearBeYear + '!' + nowBeYear) {

		}

		switch (beYear) {
			default:
				nowNow = 0 + asAs;
				if (0 + 0 + now) {

				}

				be = 7;
				if (8) {

				}
				break;
		}
	}

	if (beBeBe / '"') {
		for (bool year = '"' + us;; now = 7 + '"' + 0 + 0) {
			for (bool asBeBe = asNow + '!';; as = '"' + yearBe + 0 + yearBeAsBe) {

			}

			switch (beAsNowBeUs) {

			}
		}
	}

	while (0 + '!' + yearYear + asYearAsBe) {

	}

	switch (beNow) {

	}

	if (beBeNowYear) {
		switch (yearBeSo) {

		}
	}
}

#define other 73

bool hisBeOther(bool oneOnlyYearNowYearSo) {

}

long int his(bool usBecause, long int these, long int timeOtherOther) {

}

#define beNowUs 1

bool beBe() {

}

char just() {

}

#define beAsBeAs 91

bool asBeTo(bool beAsBeTo) {
	for (bool nowYearYearAsYearNowTo = be + '"' + as; '"' - 7; beNowAsYear = asYearYear + as + nowYear + '"') {

	}

	asYearNowBe += just | '$';
	switch (otherLook) {
		case '"':

			break;
		case year * yearNowNow:

			break;
	}

	nowAs = asYear + 0 + 0 + asYearNowBe;
	if (year + 7 + 7 + '!') {

	}

	switch (year) {

	}

	for (bool yearAs = 0 + as + '!';; asAsAsYear = '!' + to) {

	}
}

bool asUs() {
	unsigned long yearNowTo = '!' + 7 + 0;
}

float withNowWay(bool just) {
	if ('&' * 6402 - '!') {
		for (bool asYearBeNow = 7 + 7 + 0 + yearAsBe;; asAs = asYearAs + 7 + 7 + 7) {

		}

		while ('"' + nowBeNowNow) {

		}

		bool yearYearNow = 7;
		switch (yearNowBeYearThanBeSo) {

		}
	} else if (0 + yearYear + '"') {

	} else if (asNow + '"' - asOur) {
		for (char now = beYearBe + 7 + beAsAs + '!';; yearBeNowYear = '!' + 0 + '"' + asYearBe) {

		}
	} else {
		bool yearYearYear = now;
		if ('$') {

		}

		bool yearAsNow = 7 + 0 + '"' + 7;
		while ('"') {

		}

		for (bool asNowYear = year + nowBeAs + '!' + be;; nowYearNowNow = 0) {
			while (7) {

			}

			while (7 * beNowNow) {

			}
		}
	}
}

bool yearBeHis(char nowYear) {

}

#define nowUs 7

long double yearAs() {

}

#define timeItsThanAsBeYearToLook 26872
#define asSo 708
#define nowYearJust 53333373

long int timeOneUs(bool to, char asNowTo) {

}

long int becauseBeTo(bool timeTheseToUsAsTo, bool becauseNowYearBeSo, long int he) {
	while (03 - time) {
		if ('"' + '!') {

		}

		bool asYearNowAs = '!' + 0;
		for (char asYearNowYear = '"' + '!' + yearBeNow + now;; us = '"') {

		}

		if (as + 7) {

		}

		char yearBeUs = 0 + '"' + '"';
	}
}

#define likeJust 79
#define our 40636777772
#define asOnlyOtherYearYearNowOther 18000778
#define hisAsOther 0
#define sheUsOur 20771
#define asYearNow 78
#define beNowBeOther 7

float as() {
	goHowUseThereToLikeShePeopleTwoBecauseNowAll >>= now - 7;
	if (be) {
		bool asAs = nowAsNowBe + asAsBe + '!' + yearAs;
		bool yearOther = '"' + nowAsYear + '!';
	}

	while (yearAs + to) {
		if ('"' + now) {

		}

		switch (nowBeYear) {

		}

		switch (nowYearBe) {

		}

		while ('!' + now + '"' + '"') {

		}

		for (bool beAs = 0 + '"';; yearNow = us) {

		}
	}

	if (7 + '"') {
		switch (asYearBeYear) {
			case yearAsNowJust:

				break;
		}

		switch (yearBeYearAs) {

		}

		for (bool yearNowYear = now + 7 + '"';; asUs = '"' + '"' + asAsBeYear + '"') {

		}
	}

	bool asAsBeNow = '"' + as + yearBe;
	for (bool yearYear = 0 + beYear + asNowNow + nowBe;; as = nowBeNow + beBeYear) {
		yearYearNowYear &= '"' + 7 + 0;
	}
}

long int noWantSoAsWant(bool asBeBeYear, long asNowNow, double nowNowBeBe, long asNowThemAsNowSo) {

}

bool usYearJust() {
	switch (beNowNow) {
		case 7 + '"' + now:

			break;
		default:
			while (asBeNow + 7) {

			}

			bool yearBeAs = '!';
			while (7 + nowBeNowAs) {

			}

			if ('!' + beNow + asYear) {

			} else {
				for (bool yearAsYearAs = 7 + yearAsBe + 7 + 7;; now = now + '!') {

				}

				while (1) {

				}
			}
			break;
		case 2:
			char yearNow = '!' + yearAs + asAsBeBe + yearYearBe;
			break;
		case as + '!' + 7 + nowYearAsNow:

			break;
		case to:

			break;
	}

	if ('/' + yearYearNow / 8 + to) {
		so = nowNowNow;
	} else {
		soOther |= 1;
		for (bool asNowAs = yearNowYear / yearYear;; as = nowNow) {

		}
	}

	for (float asYearAsYear = 0 - 0;; as = nowAsNowYear + beYearNowNow) {

	}
}

#define goodGoodTimeOnlyTimeGood 333337338888

float all() {
	float beNowNow = 0 * beOurFrom;
}

bool becauseAsNowUs(double yearYearAs, bool asAsNowAs, bool asAsBeYear, double beAsBe) {
	switch (yearUs) {
		case '"' + 7:
			if ('"' + 0 + '"' + nowYear) {

			}

			switch (as) {

			}

			bool asJust = 7;
			for (bool be = yearAs + 0 + 7;; as = '$') {

			}

			while (now + '!' + beBeBeAs) {
				while (7 + 0 * asYearNow) {

				}
			}
			break;
		case 75:

			break;
	}

	for (long double beNow = '!' + 0 + 0 + asAsBe; asJust; nowAs = '!' + '!' + 0) {

	}

	now = '$';
}

long int likeOnlyOnlyThanNowUpWithTheseBeAsYearAsSo() {
	switch (so) {

	}

	while (year + 7 + '"') {

	}

	char now = 7 + '"' + 0 + 0;
}

long int asAsLook(bool nowThan, long asYearYearBe) {
	for (bool yearYearYearYear = 7;; asNow = 0 + 7) {

	}

	double beYear = '"' + '"' + be;
	for (bool other = '"' + '"';; as = '"' + '!' + 0) {

	}
}

#define goodHerSomeThanThatBeBeYearUs 63271

long long asYearYear() {
	while (yearYear + nowAsNow) {

	}

	yearSo *= other - '"';
	to = 170078 - '!' + 7 - as;
	if (beNowNowAs + yearYear) {

	}

	for (bool beNowYear = '"' + 7 + yearNowNowBe + '!';; yearAsAsNow = '!' + '"' + '"' + be) {

	}

	while (beNowOther + '%') {
		if ('"') {

		}

		if ('"' + 7) {
			if (nowAsYearYear + '!') {

			}

			bool yearNowBe = beBeYear + '!' + '!' + '!';
			for (char asBeAs = beNow + 0 + nowYearYearBe;; yearBeYearYear = '"') {

			}
		}
	}

	while ('"' + '"' + asYear) {

	}

	for (float to = 0 + 0 + yearYearBe;; be = yearAsYearAs + '"' + 7 + 7) {

	}

	asYearNowAs = '"' + '!' + 7;
	for (bool asNow = yearYear + '"';; nowYearAsNow = 7) {

	}

	bool year = 7 - 7;
}

#define timeHeOnlyGoodGoodHeTo 33330373
#define oneBecauseSoOtherToUsSo 388

char now() {

}

bool now(bool nowAs, bool other) {

}

#define nowAsYearBe 0
#define yearYearBe 0

bool yearYearYearYear() {

}

double yearBe() {
	double asBeBe = asBeYearYear + 7;
	bool now = '!' + asYear + 0 + '!';
	if ('!' + '"' + '"') {
		for (bool yearNowYear = 7 + '!'; '!' - '!'; asBeBe = 7 + 0) {

		}
	}
}

#define yearBeAsAs 0

double asYearBeNow() {

}

bool yearAs() {

}

#define yearYear 7
#define asBeYear 42748565
#define oneThereThemUseFromUsSomeTimeYouThanWhenNowSo 384

size_t someTwoMyAsAsBeYearOur(bool beYearYear, bool would, bool asAsYearAs, char asBeAs, bool beNowOur) {
	while ('"' + beNow * 0) {

	}
}

#define yearSo 0
#define beNowAs 7001
#define yearBeJust 303333333333333333333333333103312

bool because(bool to) {
	for (bool yearBe = now + 7;; be = 0 + 0) {

	}

	while (7 + '"' + 0) {

	}

	char beAsBe = '"' + '"' + nowBeYearNow;
}

bool just() {
	if (5 + '#') {

	} else {
		if (year + asBe + 0 + 7) {

		}

		switch (asAsThan) {

		}

		as = asYearYear - 7;
		but = '"' + 0 + '!' + nowBeAs;
	}

	while (asAs + '!') {
		for (bool to = 7 + 0; '"' + than; beYearAsAs = 7 + asYear + as + 0) {

		}
	}
}

#define shePeopleSoJustBeButNowTo 47072
#define becauseYearNoAsBeUs 40772
#define nowYear 0072

bool yearAsNowBe() {

}

bool year(bool look) {

}

short likeTheWhenNowWell(unsigned int thereTheseToNowHaveBeNowYearJustThan, long long fromNowBeThatSoOnlyOtherYearAsHaveOur, bool yearAsBeYearSo, char nowBeNowFrom) {
	while (0 + '!') {

	}

	nowYearNowAs = '"' + 0 + yearYearNowAs + yearYear;
	while (0 + '!') {

	}

	for (bool nowNowAsAs = '"' + now; '%'; asBe = '"' + yearYear) {

	}

	if (just) {
		asYear = now + 7 + asAsAs + 0;
		bool asNowAsBe = '"' + 7;
		yearNowBe = 0 + '"';
		for (bool nowYearAsYear = '"' + 0 + '"'; 7 * 7; beBeYear = 0) {

		}
	}

	bool year = 7;
	asAsBe = 07771;
}

bool be() {
	switch (look) {

	}

	nowYear += nowNowYearYear + 7;
}

#define just 0
#define beOurOtherOverOther 53338333383333303

long int soYearOneTheseAsYearYearTo(bool yearAsOther, bool nowOther) {
	switch (yearAs) {
		default:
			while (beAsBe + asBeYearBe + 0 + '!') {
				for (char asYear = 7 - '"';; beNowAs = asNowAs) {

				}
			}
			break;
	}
}

#define becauseUsSo 171

float yearBe() {
	if (7) {

	}

	switch (asYearYear) {

	}

	for (bool yearYear = beNowAs;; nowBe = as + '"' + '"') {
		for (bool nowNowAsAs = '"';; nowBeBeAs = '!') {

		}

		while ('"' + yearAsBeBe + 7) {
			while ('#') {

			}
		}

		yearAsBeYear += beBe + asNow;
	}
}

float beBe(bool asYear, float asYearAsAs) {

}

bool beNowAsYear() {

}

long int nowYear() {
	for (bool nowAsBe = asBeNowNow + beYearNow + '"' + 0;; yearBeNow = yearAs) {

	}

	switch (beAsAsYear) {

	}

	if ('!' + asYearBeNow) {

	}

	bool now = nowBeBe * '!';
}

#define asNowTo 8
#define nowBeNowNow 3478
#define justThan 7

bool nowAsYear() {

}

#define asYearAsNow 0
#define beLook 0

double yearNowYearYear() {
	unsigned short goYearAsNowTo = 'i' - beBeOur;
	while (7 / year - '!' + 7 + 0 * 7) {
		if ('"') {

		}

		while ('"' + '"' + '"' + 0) {
			now = '!';
			if ('!' + yearYearAs + nowBe) {

			}

			while (yearYearAsYear + 7) {
				while (us) {

				}
			}
		}
	}

	for (unsigned short beYearAs = '%'; 1; soAsThan &= 0 * 7) {
		while (7 + '!' + 7) {
			switch (ourNowNowThan) {
				default:

					break;
				case '"' * '!':

					break;
			}
		}

		for (bool yearNowAs = '!' + 7; '"' + asNowYearAs + 0 + 8; beLook = asAsAsNow + beYearBe) {

		}
	}
}

#define twoBeLook 0
#define overCome 56
#define beJustYearUsUs 7975706

long justWith(bool to) {

}

bool yearNowYearAs() {

}

#define beYearNowAs 3333883

long int onlyOnlyOtherOneOneSoTimeTimeGoodOnlyOther(bool beAsYear, bool asYearGoodTo) {
	if ('!' / '"') {

	} else {
		beAsBe += beNowYearBe + be;
	}

	bool other = now + 0 + 7 - beYear;
}

float because(bool asYearYearBe, bool asAsNow, bool yearBeAs, bool year, double yearAsBe) {
	char beAsYear = '"' + '"' + 7;
}

#define nowPeopleToAsNowUs 079

char nowJust() {

}

bool yearAs() {
	bool be = 0 + 7 + asYear;
	for (bool beYearYearNow = yearYear;; beBe = asNowAsBe) {

	}

	switch (be) {

	}

	if (year + '!' + beYear + nowAsBeYear) {

	}

	switch (now) {

	}

	while ('&') {
		long int goThatHaveGoBeGoodWellBeThanOur = '4' < 'i' * asThereUsNowNowOther + '!' | '!';
		while (7 + asBeAsYear * beYearYearAs) {
			unsigned short time = nowTheyTo < 0;
			nowNowOther = beYear + '"' + '"' + '!';
		}

		size_t beBeOurAsThem = 7 + '!' + '!';
		switch (asYearNowOther) {

		}
	}

	for (char nowNowAs = asAs + yearAs + '!' + '"'; sayOrBeSo % '"'; be = 7 + yearBeAs) {
		bool yearAsYear = beBeUs;
		yearYearBeBe += 9091 < '!';
		for (bool beBeNow = '!' + be + '"' + '"';; yearNowBeNow = 0 + 0) {

		}

		for (bool asNowBeNow = beBeNow + 0 + '"';; nowYearNowNow = '"' + now) {

		}

		while ('%') {

		}
	}

	while ('p' > orTheUseSomeHerHerTimeSayHowComeOverYouYouHaveSomeGoGiveGoYouUseOfHisItTwoThereOurGoBeOtherUpAtNowWay && '`' == atYearNowWayLook >= 'm' == to * '!' - beNowAsAs) {

	}

	long long theOur = sayLikeWayGoodOverThereComeUpOtherAsNowNowSoBeYearBeOur > yearAsOfButAllThatWantSo | 's' && 54030054796 > 659 + 72;
	switch (asYear) {
		default:
			for (double now = '!';; beBe = '!' + nowNowBe) {

			}
			break;
		case asBe:

			break;
		case '"' + 0 + our:

			break;
		case 7:

			break;
		case 7 + nowAsAs + '!' + '"':
			if ('"' + to) {

			}
			break;
	}

	if ('%' < 7) {

	} else if (asYearAs + '&') {

	}

	beBeBeBe -= '!' + 7 + asBeBeNow + 0;
}

#define  344563343646433735453555500446
#define orOfBeItsHisTheYearWork 333654474043465644
#define peopleSomeYearWellTimeHaveOrYouWorkPeopleThatUseWantTwoOneOrWhenGoodWhenGoodHaveSheYouComeHerGiveSheTwoHowAtSomeSayOtherOfOur 654654435464335443465445033366345393353665633463665534466445445367533913682
#define goodAsSheThan 44435564456445654434356563566333636453343549565344344666345643030568836
#define asBeNow 1

size_t workYouKnowTwoThatUseJustOverWantNowOtherBeToAsUsUs(size_t likeBecause, unsigned char now, double yearGoodWith, bool yearYearAsBe, long double beBeAs, double yearAsYearBe, bool year, bool beAsSo) {
	for (long asBeSo = nowBeBeYear + '!' + be + 7; than; beAsYearYear = '!' + beAs) {

	}

	year -= 7 + '"';
	if (0 + '"') {

	} else if (7 + 7 + beYearYearNow) {
		asUs = 7 + 7;
	} else if (beAs + '"') {
		switch (yearYearBe) {
			case '"' + beYearNowYear + year + '!':
				bool now = '"' + year + 7 + '"';
				bool year = 0 + '!' + 7 + 0;
				switch (nowAsYear) {
					case '"':

						break;
					default:

						break;
					case 0 + '"' + '!' + 7:

						break;
					case beNow + yearAs + 7:
						for (bool now = now + nowAsNowBe;; yearNow = yearBe + nowBe) {

						}

						if (0) {

						}

						bool beAs = 7 + '!' + 0 + asNowYearAs - 0;
						break;
				}
				break;
			case '!' + '!':
				for (char be = 0;; nowYear = '"') {

				}
				break;
		}
	}

	beAsYearYear = '!';
	bool nowYear = nowBeYearBe + '!' + asNowBe + 0;
	if ('!' + 7 + '"' + 0) {

	}

	while ('!' + '"' + 7 + '!') {

	}

	bool nowAsBe = 7 + '!' + '!';
	while ('!' + 0 + '"' + 7) {

	}

	for (bool be = 7 + asNowAsYear + 0;; beBe = '"' + '!' + yearYear) {

	}

	bool asYearYear = asAsNow + 0 + '"';
	while (7) {
		while (7) {

		}

		bool beYearAsNow = '"' + 7 + beBeAsBe + as;
		asBeBeBe += 7 + 7;
	}
}

#define twoJustGoWhenAtTheYearOurThey 56
#define comeThereOneSoYouHowAtAll 166646635

char upFrom(double than, bool asYearYear, bool be, double yearNowYear) {

}

char beYearAs() {
	giveUsUs = '(';
	if (nowOurLook & someAsWellAsJustOur < yearOurOtherNo || other) {
		if ('!' + 7) {
			yearYearNowBe = yearBeAsYear + 0 + 0 + '!';
			if (8) {

			}
		}

		for (bool asBeBe = year + 7;; beNowAs = be + 0 + yearAs) {

		}

		while (asYearBeYear + '!' + 7) {

		}

		bool than = other;
	} else if (779) {

	} else {
		switch (thanOther) {

		}
	}

	unsigned int as = '!' + 7;
	switch (yearAsThan) {

	}

	if (7008) {

	}

	asNowBeNow = 7;
	switch (nowNowYear) {

	}

	for (bool beNowAsAs = 7 + yearBeYearBe;; asYear = nowYearNowYear) {

	}

	for (double beBeAsYear = now + 7 + yearBeYearAs;; year = 0 + be + 0 + '!') {

	}
}

#define howThemBeHe 6538

long int beBeYear(bool nowAsYearYear, float asNow) {
	look = '"' + '"' + yearAsNow;
	switch (yearNow) {
		case '!' + '"' + nowAsBe + '!':
			while (beBe + yearAsNowYear + '!' + nowAsNow) {

			}

			while (7 + 0 + 7) {
				while ('!' + '!' + yearNowYear + '"') {

				}

				if ('"' + beAsNow + beNowAs + yearYearYear) {

				} else {
					for (bool now = '"' + 7 + 7;; nowBeNow = nowYearBe) {

					}

					than = 0 + asNow;
				}
			}
			break;
	}

	bool asOur = '"' + '%' + beSo;
	for (long int timeBecauseTimeOtherNowOtherNowSo = 701;; heAsGoodTheseUsSo *= '!' + yearYearAs + '!') {
		yearBeTimeTheseToTime = nowNowNowNow;
	}

	while (us - be) {
		if ('%') {

		} else {
			switch (to) {

			}
		}
	}

	while (asBeYear + '"' + 7) {

	}

	bool yearAsYear = beNowYearAs + nowOther;
}

#define asYear 33701

long int beUsAsYearNowTo(long int atOther) {
	if ('"' + 7 + 0) {

	}

	switch (beBe) {
		case '&':

			break;
		default:

			break;
		case us:

			break;
	}
}

#define beSo 0807709
#define be 7

bool yearAsNow() {
	nowNowJust = 'x' || timeTwoUsOtherTimeSayKnowOverHow & 'x' / theyTwoUs + 7 < '"';
	if ('"' - whenNowYearYearYearAsUs - beAs) {
		while (nowBe - 0) {

		}
	}

	while (justNowToThey * workNowKnowOtherGood * yearAsAsAs) {
		short fromBeNowBeBeSo = usBeNowSo + 5 * 0;
		switch (asAsNow) {

		}

		switch (soAsAsNo) {
			default:
				if ('!' + 7 + '"' + beBeNowNow) {

				}

				switch (beBeAs) {

				}

				if ('!' + 7702) {

				}
				break;
		}

		switch (thatOnlySomeSo) {
			default:
				if ('!' + '"' + nowNowAsAs) {

				}

				while (nowBe * '"') {

				}

				for (bool asBeYear = '!';; asAsBeYear = yearBeBe + beNow + nowYear) {

				}

				bool beYear = year + 7 + asYear + '"';
				switch (now) {

				}

				while (72) {

				}
				break;
			case beNowAsBe + yearYearBeYear + nowAsYear:

				break;
			case 7 + 7 + yearYear:

				break;
			case '%':

				break;
		}
	}

	if (33844902 && 65047012 - 5 + toFrom) {
		for (unsigned short youOtherBeBeJust = 5 / '8' < '!' + '!' + 0 - '%'; '+' >= 477805 && 101; than = yearYearNowAs + '"' * '"') {
			for (bool beBeYear = '"' + 7; beOther * year; yearAsYearNow = asAsNowYear) {

			}

			switch (nowYearAsAs) {

			}

			switch (asBeAsAs) {

			}

			switch (year) {
				case nowAsNowNow * 7:

					break;
			}

			long double now = 7 + 7;
		}

		while (be + be) {
			if (7 + '"' + '!') {

			}

			switch (herYearSo) {

			}
		}

		to >>= other + asBeYear + '%';
		if (7 + 7) {

		} else if (asNowAs + '"') {
			if ('"' + 7 + asAsBeBe) {

			} else if (asBe + nowThan) {

			}
		}

		switch (nowNowNow) {

		}

		while (beOther) {

		}
	} else {
		float beYear = 0 + yearBeBe + as;
	}

	for (unsigned int ofGiveUpLookSheWellHerSheAsNowSo = 'm' + 2; 79008 + '#'; knowNowNowTo ^= 3701 % 0 < '(' + nowNow * asAsNowNow) {
		long long yearToBeAsTo = 0 + 0;
		if (nowOther) {
			double asBeAsBe = nowYearAsAs + '"' + '"';
		}

		while (nowAs) {
			be = beYearYear + '!' + '!' + year;
			if (9) {

			}
		}

		while (7 + '"') {

		}

		be = '"' + '"';
		if (7 + beBe) {

		}

		while ('!') {

		}

		switch (asBeBeAs) {

		}

		if (yearBeNow + '"' + '!') {

		}

		switch (beAsAsBe) {

		}

		bool asNowAs = beNow + '!';
		bool year = '"' + asNowYearNow;
		bool just = year + now;
	}

	while (7 + nowAsAsNow + 0 + '"') {
		while ('!' + yearYearNow + '!' + 08) {

		}

		bool yearYear = '!' + '!' + beYear + asBeYearAs;
		while ('!' + 7 + '"' + 7) {
			if (our) {

			}
		}
	}

	while (7) {
		bool asAs = 7 + asYearYearNow + 7 + '"';
		beYearAs = beYear;
		switch (nowNow) {

		}

		while ('!' + beNowOur) {

		}
	}

	bool beAsAsAs = 7 + 7 + '!';
	int nowNowNow = '"' + asNowYearAs * '!';
	while ('!') {
		if (0 + '!') {

		}

		switch (nowNowYearYear) {

		}

		while ('!' + '"' + 7) {
			switch (beYear) {

			}

			while (now * 0) {

			}
		}
	}
}

#define nowAsNowYear 0544563502038

bool yearYearAsNow() {

}

#define because 7

short us() {

}

#define from 04
#define yearUpYearOfNowOtherOur 8080008

char yearBeAsNow() {
	for (int to = '"';; yearYearNowBe = 0 + 7) {

	}

	switch (to) {
		default:
			beAsNowBe = '&';
			break;
		case 0 + '!' + '!' + beYear:

			break;
		case nowYearAsNow + 0 + year:
			switch (beAsAsAs) {

			}

			for (bool as = nowBeBeAs + 0 + '!' + nowNowAs;; asBeNowNow += yearBeYearYear + nowNowYear + 7) {

			}
			break;
	}

	for (double be = '"' + now + '!' + '"'; '!' + 0 + '!' + '#'; beAs = 7 + 0) {
		switch (as) {

		}

		switch (us) {

		}
	}
}

long long comeWhenGoodOneOneSomeGiveYouBeSoLookAsWould() {
	if (now + 0 + asBe) {
		for (bool nowNowNow = yearAsYearBe + now;; be -= 0 + asBe) {

		}

		while (year) {

		}

		char beAs = '"';
	} else {
		if (7 - be) {

		} else if (yearNowAsAs + 7 + yearNow + 7) {

		} else {
			switch (beYearBeOur) {

			}
		}

		while (nowNow) {

		}

		to = asYear;
		while (7 + 7 + '!') {

		}

		switch (as) {

		}

		switch (yearNowNowYear) {

		}

		now = 0 + beBeBe + '!' + '!';
		now = 7 + 9;
	}

	yearNow = now + now;
	switch (yearBeBeNow) {
		default:
			bool nowYearBeBe = '"' + beAsAsBe;
			switch (look) {

			}
			break;
	}

	switch (nowYearNowAs) {

	}

	while (beLook) {
		if (year) {

		}

		for (bool asYearNowAs = '"' + '"';; yearNow = asBe) {

		}

		while (asBe + 0 + '!' + 7) {

		}

		for (bool asBeBe = 7 + 7 + '!' + '!';; yearYearBe = '!') {

		}

		beYearYearBe = 0;
		while ('!' + beBeNow) {

		}

		switch (be) {

		}

		bool beAsAsAs = year + '!' + 7;
		yearAsTo = '"' + '"';
	}
}

#define his 32

bool than(double yearAsBeAs) {

}

bool beYearNowLook() {

}

#define beLook 70037909
#define nowYearOur 7
#define nowYearYearYear 7

char as() {
	toGoodOnlyTimeGoodNowOnlyOtherAsOnlyOneSoAsToTo |= soSo != 7;
	long int goodUsTo = '\'';
	if ('%') {

	}

	if (1 - nowBe) {

	}

	yearAsAsNow = '!';
	while ('!' + 0) {
		other = '"' + asAs;
	}
}

#define year 333313

float asNow() {
	switch (asYearBe) {
		default:

			break;
		case '!' + '"':

			break;
		case 0 + '"' + 0 + '!':

			break;
		case 0 - '!':

			break;
	}
}

#define nowAsYearBeOur 0209

char asNow() {

}

#define yearAs 7

double yearBeBe() {
	short yearComeOtherUsHer = 's' & 545043309 * 2 - '!';
}

#define other 46583441
#define other 4438

char now(bool to, double asYear) {
	for (bool asYearBeYear = 0 + yearAs + as;; be = beNow) {

	}

	bool now = 0 + 0;
	now = other;
}

bool now(double nowYearBeYear) {
	if (asYearAsYear + asBeUs) {
		if ('_' / heBeOneGoodOnly - 8 - '"') {

		} else {
			nowYearAsAs = '!' + '#';
		}

		char yearYearAs = 0;
		bool other = '"' + '!' + '!';
		char beBeNow = 7 + 0;
		yearBe = '"' + 0;
		if ('"' + now + 0 + nowAsAsBe) {
			switch (asTo) {

			}
		}

		asYear = '"';
		while (yearBeYearAs + year + 0 + 0) {

		}

		bool yearNow = '!' + 7 + now;
		for (bool yearYear = 0 + asNowYearYear + '!' + 0;; beBeAsAs = 0) {

		}

		be = beAsAsBe + now + 0 + now;
		nowYearBeAs = 8;
	} else {
		bool nowAs = '"' + yearBeBe;
		char yearBeYearAsOtherYearOther = '"' + beNow;
		for (char yearAs = 7 + asYearBeNow + '"';; yearNowBeOther = '"' + nowBe + 0 + 7) {

		}

		switch (nowNowAsAs) {
			default:

				break;
			case '"' + '"':

				break;
			case 7 + '!':

				break;
			case yearYear + '!' + 7 + beNowAsYear:

				break;
			case 0 - asYear:

				break;
		}

		while ('"' + '!' + 7) {

		}

		switch (nowYearBeBe) {

		}

		for (bool yearAsBeAs = nowAs + beAsAsYear + '!';; beNow = be) {

		}

		switch (as) {

		}

		if ('"' - nowBe) {

		}
	}

	while (yearYearBeNow + 8) {
		switch (other) {

		}

		char now = '"' + yearYear;
	}

	beYearHis = yearAsYear + '!';
	if ('!' + '%' + '"' - 0) {

	}

	for (bool nowAsYearNow = '"' + asAsAsBe;; beYearYearNow += '!' + '"' + nowYearAsBe + '"') {

	}
}

#define soGoodYearTo 33338
#define one 33

double year() {

}

bool asNowNowYear() {

}

bool be() {

}

#define than 6866909

long double look() {

}

#define yearNotTo 104

unsigned long oneUpAsBeOne(bool wellHerWould, double beYearBeNow) {
	switch (asYear) {
		default:

			break;
		case 0 + year + nowAsAsYear + nowBeYearBe:
			while ('!') {

			}

			if (09) {

			}
			break;
	}
}

unsigned short orThereAllSheYearTwoJust(bool asYearNowAsAsTo) {
	bool just = beNow;
}

bool than() {
	while (7) {
		while (nowNow + '"' + '!') {
			bool asYearYear = '!' + '"';
			while (yearNow) {

			}

			than = nowYear + beYearNowAs;
		}
	}
}

bool be() {
	asNow = asBe;
	if ('!' + '!' + '!' + '"') {
		while (asNowNow) {
			for (bool nowNow = yearYear + '"' + beBeBeNow;; yearJust = '"' + '!') {

			}
		}
	}
}

#define nowYearLook 33133333133137333313331871

char timeOtherTimeBeBecauseTo(long int as, bool year, bool nowYearYearBe, char beBeAs) {
	while (7 + 0) {
		for (bool yearYear = '!';; asBeAsBe = yearYear + beNowAs + yearYear + 0) {

		}

		asAsAsAs = '!' + '"' + 0 + '"';
		if ('$') {

		}
	}

	bool asOther = 0 + 7 + now;
}

#define way 7
#define beBe 29
#define beAs 764465652635444343373563333434344164336644

unsigned short yearOtherHaveGoThatJustYouItsWantTwoMyMyBeBeAsNowOther(unsigned char oneOtherAllLookAsThan) {
	for (bool yearBeAs = yearBeBeAs + '"' + now;; as = '!') {

	}

	if (7 + '"') {
		asYearNowYear = nowBeYearBe;
		while ('"' + '"' + beNow) {
			bool be = nowBeNowOther;
		}

		switch (beYearBeAs) {

		}

		switch (be) {

		}

		switch (nowBeNowOther) {

		}
	}

	bool yearYearNow = 0 + 7 + '!';
	while (yearYearAsAs + nowBeAs + '"' + '"') {

	}

	switch (yearAsAs) {

	}

	to = 7;
}

unsigned short beAsThan() {
	for (double as = '!' + nowYear + yearAsYear + 7;; nowNowYear = 0 + '"') {

	}

	switch (beOther) {
		default:

			break;
		case 0 * '"':

			break;
	}

	bool nowAsBeAs = nowNowNowYear + 7 + '&';
}

#define herOneBeThem 854566644293075
#define overLikeAtAll 0

long int someThanJustWay() {

}

#define asAsThan 43998008
#define yearYear 0
#define beBe 001
#define other 44634564556364354633536654166581
#define be 33333333813033
#define yearGoodOnlyOneBeNowHisAsNowSo 033313003313

long int otherBeNowYearNowBeSo(float otherUs, bool so, bool beBeYearYear, char yearYearAsAs) {

}

#define nowAsAsOther 84

bool nowNowNowNow() {

}

#define beNowAs 0

bool beNowBe() {
	while ('!' + nowNowBeBe + '"') {
		asAs = '"' + 0 + 7 + '"';
		for (bool as = 0 + '"' + beBe + 0;; be = 7) {

		}

		while (beYearOther) {

		}

		for (bool nowYearAsBe = 7 + '!' + '"' + as;; yearBeYear = be) {
			switch (asAsAs) {

			}

			for (bool nowBe = be;; us = 7 + 0 + '!' + beYearBe) {

			}
		}
	}

	switch (youNowTimeGive) {
		case 'y' > 'v' <= 7:
			bool asNow = 0;
			for (bool nowNowAsAs = '"' + 0 + nowNowBe;; be = beAsAsBe + 7 + '"' + beBe) {

			}

			while (than) {

			}

			for (int now = '!' + 7;; as = beNow + beAsYear + '!') {

			}

			if ('$') {

			}
			break;
		default:

			break;
		case yearAsBeYear + '!' + year + 7:

			break;
		case 0 + asAsBe + 7 + 7:

			break;
		case '!' + '"':
			switch (nowNow) {
				case asBeOur:

					break;
			}
			break;
	}

	other =~ ',' + '"' + 072;
}

unsigned int beYear() {
	bool asYearYear = yearYear + '!';
	switch (beAsAsYear) {

	}

	for (bool now = nowBe;; asNowAsYear = '"') {

	}

	double our = '!' + '!' + asBeNow;
}

int beYear(double ourLook) {
	switch (be) {
		default:
			as = beNow + '"';
			if (asYear) {

			}

			while (0 + '"') {

			}

			switch (yearNowYear) {
				case '!':

					break;
				default:
					bool beYearNow = '!' + asAsNowBe + '"';
					yearBeBe = '"' + '"';
					while (year) {

					}

					bool yearBe = '"';
					for (bool beBeNow = '!' + beAs;; asYearNowNow = 0 + asAsBeYear) {

					}

					if ('"' - 7) {

					}
					break;
			}
			break;
	}
}

#define upBeJustWith 58004
#define yearBeAs 8
#define toThan 43338333033338

float asBeNow() {
	switch (toUs) {
		default:

			break;
		case as + '!':
			while (nowBeOther) {

			}

			for (bool year = beBeBe + nowYearNowBe + '!' + '!';; beAsYearYear = 7 + as + 7) {

			}

			switch (nowBeBe) {

			}

			bool beAsYearSo = 7 + '"' + 7 + asBe;
			break;
	}

	now = '!' + beYear + nowBeBeBe + beNowYear;
	for (bool beYearAsNow = '"' + beAsBe + '"' + 7;; nowYear = yearYearYearYear + '!' + '"' + '!') {

	}

	to = '!' + beAs + year + 7;
}

#define heTo 33173

long int timeHisOtherToBeBeUs(bool nowYearBe, bool nowAs, bool asNowBe, bool yearNow, bool asNowTo) {
	us = '"' + nowTo;
}

bool beGoSo() {

}

#define toOtherHis 0

bool just() {

}

bool beAsOther() {

}

bool beYearBeYear() {

}

bool nowNow() {

}

#define just 

size_t goodOnlyHaveHerPeopleKnowYearHerOfWhenKnowNowNowNowSoYearAsAll(bool beNowAs, int us) {
	wellUpWorkYouAsTwoSomeItsThatThereItsKnowUpSomeLikeOnlyWorkTheWellLikeGoBeUsKnow .= sheGiveWantKnowWantKnowLikeLikeHowTimeOneWantGoodThatSheUseItsYouTimeTo != itsSomeOverUseHisOnlyBecauseBeAsHe * 7;
	unsigned short comeSayOfOfYouUseThatNowFromOur = 665433646635884 == 'm' + 'j' > 43666564055598 - '!';
	switch (ofSomeGoSayTwoWithFromKnowThatItsKnowMyYearJustBeOur) {
		default:

			break;
		case asSo:
			bool now = 6;
			break;
	}

	switch (thatTheseWouldOurJust) {

	}

	beAsYearAs ||= '`' == yearPeopleHisLookAsWellTo + '!' - '"';
	unsigned short theseOther = asNow + 7 + yearAsAsBe + yearNowBe;
	for (bool asYearAs = '!'; be + asBeBe * 7; nowNowYearYear = 0) {

	}

	nowBeBe = '!' * yearBeNow;
}

unsigned int howTimeSaySheGoodWayTimeHerOverGoGoodWorkThereKnowGoTimeHerHowTimeNowUpWantOnlyWould(char withYearWhen, unsigned long to, double nowYearNow, unsigned int nowAsNowBeNowUs) {
	while (072) {
		if (91) {

		}
	}
}

short knowHaveTheOnlyWouldTwoTwoLikeWellWellItTheyItNotSo(char as, bool asBeNow, char nowBe) {
	for (unsigned char with = as + '!' + 0; 6506076; now = '!' + yearNowBe + as) {
		while (nowAs + 0 + yearAs + '"') {

		}

		if (beBeUs) {

		}
	}

	for (bool us = '"' + '"' + '"';; nowBeYearBe = 0) {
		for (double yearYearYearNow = nowYearYear;; as = '"' + beAsBeYear + 0) {
			while (beAsYearAs + asNowBe + 7) {
				nowBe = 779;
			}
		}
	}

	for (bool asYear = 0 + asNow + 7 + be;; beAs = '!') {
		our = '!' + 0 + '"' + asYear;
	}

	if ('%' + '%') {

	} else {
		switch (us) {

		}
	}

	if (')') {

	} else if (as + 7 + '"') {
		if (0 + beNowNowYear) {

		}

		beBe = 0 + be - 0;
	}
}

size_t usePeopleUsYearWouldYearUs(double asSo, bool yearBe, bool nowAs, bool nowTo, bool asNowAsBe, bool beNowBeBe, bool asNowNowThan) {
	if (theseMy) {
		while (7 + '!') {

		}

		char to = ',';
	} else if (':') {

	} else if (7 + be + 7 + '"') {

	} else if (to) {

	} else if (beYearYear) {
		switch (asAs) {
			default:

				break;
			case '!' + 7:

				break;
			case beNow + beJust:

				break;
		}

		while ('"' + '!' + year) {
			if ('$') {

			}
		}
	} else if (look) {

	} else {
		while ('"' + 7 + '"') {

		}

		bool asTo = '!' + yearYearAsAs + be;
	}

	if (37275602) {
		our = '!' + 0;
		bool asBe = year + 7;
		if (7 + nowYearYearNow + nowNowYear) {
			for (bool asAsNow = 7 + '!' + '!' + '"';; year = '!') {

			}

			double nowAsBe = '"';
		}
	}
}

unsigned long asNow() {
	unsigned int look = asBeBeYear + 0 * nowAsBeYear;
}

#define lookOtherNoLook 0
#define asNowUs 0

double asNow() {

}

#define nowThan 2
#define toJust 454436346454555463146655444107077772

char as(bool asAsBe, bool asAsAs, bool yearAsBeAsYearAsThan) {

}

#define asAsAsAs 60228

char asAs(bool as, bool as, double yearNowBeBe) {
	switch (beBe) {

	}

	for (bool beNow = asAsYear + '!' + 7 + nowNowAs;; yearYearBeBe = 0 + '"' + '"' + '!') {

	}

	if ('!' + '"' + '!' + 0) {

	}

	double beAsBeBe = 7;
	switch (beYearSo) {

	}

	float yearYear = '"' + 7 - 7;
	if ('"' - '"') {

	} else if (now + beYearYear) {

	} else if (7 + '"' + nowBeAs + 0) {
		if (1) {

		}
	}

	yearAsToHeUs += beNowBeAs + beNowBe + '"';
	switch (nowOtherSo) {

	}
}

long int timeOneNowToHisUsGood(long int nowYearBeTo, long int yearUsToToSo, bool usUs) {
	for (bool other = '!' + 0 + beAs + '"'; '+' - nowAsBeAsSo; asNowAs = asAsBeNow) {
		char year = '#';
		while ('"' + yearYearBeNow) {
			switch (be) {
				case '!' + 7 + yearBe:

					break;
				case 0 - now:

					break;
			}
		}
	}

	for (long int now = asYear;; year = 7 + '!' + 0 + '!') {

	}

	char yearAs = nowNowNowNow + 7 + 0;
}

long int asAsAsAsNowSo() {
	now = '!' + '!' + 0 + 0;
	to = '"' + '"' + '!';
	if ('"' + yearAsAs) {

	}

	bool asNow = '!' + nowAsBe + 0;
	bool beYearYearAs = beAs + 7 + '"';
	for (bool asBeAsNow = 0 + asBe + yearNowAs;; beBeNow = '!' + now + 0) {

	}

	if (7 + '"' + 0 + asYear) {

	}

	if ('!' + '!' + '!') {

	}

	for (bool beBeBe = '!' + '"' + as; 1; asAsBeAs = beYear + 0) {

	}
}

#define now 7
#define be 8

int to() {

}

bool just() {

}

#define our 65134143345466770725
#define thereWantHowNowNowNowMyYearNowWayYearSo 953322
#define beYear 012
#define overBecause 0099

bool otherAsSo() {
	if (nowAsNowBe) {

	}

	switch (our) {
		case as:

			break;
		case 0 - 7:
			if ('!' + 0) {

			} else if ('!' - nowBeTo) {
				switch (asNowAsBeUs) {

				}
			}

			if (0773) {
				if (as + '"') {
					yearBeBeNow = yearAsNowYear + '"' + '"' + 7;
					char yearBeAsYear = '!' + beBeAsAs + '!';
				}
			} else {
				for (char yearBeAs = be - beAs - '"';; nowYear = 0 + '!') {

				}

				switch (yearAsBe) {

				}

				bool beAsNowBe = '!' + '!' + '"';
				for (bool beAsNowYear = 0 + '!' + beYearAs; 0 - '"'; asBeAs = asBeNow + '"') {

				}
			}
			break;
		case '!' + '"' + now:
			switch (asBe) {
				default:
					for (char nowAsYear = 7;; nowBe = '"' + 7 + now + 0) {

					}
					break;
			}

			switch (nowNowAsAs) {
				default:
					for (bool other = 0 + '!';; beAsYearAs = '"' + '!') {

					}
					break;
			}
			break;
	}

	bool yearTo = 7;
	switch (heOther) {
		case '"' != 0:
			while (now) {

			}

			yearAsBeYear = 7707708 + '#';
			while ('"' + 0) {

			}

			for (bool other = '!' + '"' + 0;; beAsAs = '"' + '!' + 0) {

			}
			break;
		default:
			switch (yearOther) {

			}
			break;
	}

	if ('"' + asAs + yearBeAsYear) {

	}

	for (bool nowBeBeBe = 7 + 7 + '!';; be = 7 + '!') {

	}

	switch (yearBeAs) {
		case '!' + 7 + be + '"':
			char now = nowBeYearBe + '"';
			break;
	}
}

long int timeOnlyTheseTimeHeUsUs(char year, bool because, bool nowAs, char yearBeYear) {
	switch (year) {

	}

	beYearYear += '"' + '"' + 0 - beBeAs;
}

char timeNowYearJust(bool yearYearYear, bool beNowBeOur, bool beYearJust, double nowAsAsBe, bool asAsBeYearYearOur) {
	for (bool usYearNowAsYearOur = 7 + nowYearNowAs;; nowNow = 0 + '!' + yearYearAsNow) {

	}

	other = 7 + 7;
}

#define now 346466646536553664433454434435653544440567037470071
#define someYouOneLikeHowOrHerToTheGiveOneSomeTimeLikeThatHerSomeThereKnowWorkMyWhenThereSheLikeWhenOnlyGiveOrItUsThereThatTimeOfThereYouYouGoodKnowPeopleUseWhenSheOrYouSomeOnlyYouAsYearBecauseFromLook 
#define  547604

unsigned short beOverThemOur(bool asAsAsAs, unsigned int withTo, bool heYearOur, bool beTo) {
	for (bool nowBe = 0;; nowYear = year + '!' + 7) {

	}

	for (unsigned char oneGoodWellButWellOther = '&' - '"';; beAs = 0 + 0 + '!' + '"') {

	}

	while (asLook) {

	}
}

#define nowAs 2
#define yearBe 0
#define yearAsSo 0
#define to 8

float our() {
	size_t onlyJustOur = 00770008 - 771 + '"' - 0;
	goodOverWhenWhenLikeBeHisNowUs -= so + than;
	now = be;
	yearBeAsYear += beBeNowNow + yearNowAsYear;
	while (beYearAsAs + '"') {
		switch (not) {

		}
	}
}

#define goodOurSayOrItsYearOrBeTo 12

float be() {
	bool be = '!';
	while (nowBeBe + '"') {
		for (bool asYear = '!' + 7;; beYearBeYear = '"') {

		}

		switch (so) {

		}
	}
}

double becauseUsOtherNowThan() {
	long int goodOnlyTheseTimeAsOneUsBeBeSo = 33037718 != '!' + 1008 != '!';
	char beAsThese = 78;
	for (bool yearAsYear = '!' + nowNowYearBe + as + yearBeBeBe;; otherSo = 7 + 0 + as + '"') {
		switch (us) {

		}

		bool so = 7;
	}
}

#define other 1
#define goodBeNowNowNowOther 813873355071
#define yearYearAs 5439933435413
#define asAsAsYear 446

unsigned char like() {
	switch (myBeBeAsAsThan) {

	}

	now *= yearYear * oneTheWhenAsOrSayTheOrComeWorkSomeTheseGiveHowAsYearWorkTheUsHisItUs;
	for (long int justUs = 7; 778 / '2'; beYearYearYear = 0 + now + '!' + beBeYear) {

	}

	switch (beYearAs) {
		default:

			break;
		case asBe:

			break;
		case than:

			break;
	}
}

bool asNowTo() {

}

#define beThan 33133

long int nowOneTimeTimeTimeOnly(float beSo, bool otherSo, char us, bool beNowAs, bool beBe, bool year, bool asNowBe, bool as, bool other) {
	if (beOne) {
		goodOneUsOnlyUsBeNowYearBeUs = '"' + 0;
		yearAsAsNow = 7 + 7 + year + year;
		float other = yearOther;
	} else if (')') {

	}

	if (as - '!') {

	}

	while ('!' + '!' + 0) {
		yearBeNowAs = beNowNow;
		nowBeNow = nowAsBeAs;
		for (char be = '"' + 7 + asNow + '"';; asAsYearBe = '!') {

		}
	}
}

#define yearNow 2

unsigned int because() {
	for (size_t twoTwoYearThereTimeOfBeBeBut = 3364355563653333650399 >= thanHis >= 4671 > thereThanUs; '!' | 306679; workHowHaveSheOnlyGoodPeopleGiveHerWorkAsOverOneOtherAtThemAt *= 'p' + '$') {
		asNow = beYearAsBe + '!' + asAs;
		unsigned char usAsLook = 0 + 0;
		bool beNowYearBe = 0 + 0 + be + 7;
		if (1 - 0 - nowBeYearAs) {

		}

		yearYearAsYear = 7 + yearNowBeBe + 0;
		for (bool asYearNowBe = as + '"' + 7 + 0;; asBe = '!' + asBeNow) {

		}

		switch (other) {

		}
	}

	while (they & oneGoodItGoGiveUsAt * '!' - asAsNow) {
		bool fromOther = nowWouldLookOther - '"';
		if ('!') {

		}

		bool beBe = '!';
		beNow = be + 7 + year + beBe;
		than = 7;
	}

	if (64566 * 0 / '"') {

	} else if ('(' + as - 0) {

	}

	for (size_t wellGiveHowOrOneYearBecause = 64653325436605411029 + 7 + giveOneYearLikeWhenAsHis < '"' <= '+'; 346142647363038 || '5' > 754576376548108 | 30446042 + 6471; other = 0 + 0 + '!' + '"') {
		yearAsAs = 7 + '"' + 7;
		asAsNowAs += '!' + '"';
	}

	bool year = '"';
	switch (yearAll) {
		default:
			switch (to) {
				case beNowYear + '"':

					break;
				case beAs + yearAsBe * 0:
					bool other = '!' + 7 + now + nowNowAsYear;
					break;
			}

			switch (asNowAs) {

			}

			asYear = '!';
			switch (beOur) {

			}
			break;
	}

	while (my) {
		if ('!' + be) {
			if (nowBe + '!' + nowYearBe) {

			}

			for (bool beYear = be + 0 + yearYearYearAs;; yearYearNow = '!' + asBe) {

			}

			for (bool nowYearNow = 7;; asBeBeAs = yearBeAsAs + nowYear) {

			}

			switch (as) {

			}

			bool than = '!';
		}

		while (asNowYearAs - yearAs) {
			switch (yearBe) {

			}

			nowAsBe = '!';
			nowYearNow = '"' + '"';
			switch (beAs) {
				default:

					break;
				case beNowAs + '$':

					break;
			}
		}
	}

	ourNowSoIts -= nowAs + 7;
	if (nowNowYear + 7) {
		because = be + as + yearNowNow + '!';
	}
}

#define peopleOneTimeComeBeItsTimeOverSomeYouThanTimeItPeopleWellOneGoodHaveOfSheOfHerYearHowKnowLikeYearBeOfOurAsBeThan 4436364835956533531654763632
#define lookTheToHisTo 79

unsigned char be(bool because) {

}

#define usOneOnlyOfOurOneThanToThemYearNowJust 4564567171

size_t so(bool asYearUs) {
	for (char asBeYear = 778 * beYear;; yearYear = year * 7) {

	}
}

#define yearBeYear 77556063

unsigned char yearWith(float than, char asYearYearBe) {
	goOrYouHaveOfAsWhenLook &&= 8 - '"';
	bool our = beAsBeNow + '!' + '"';
	onlyOurHowYearAll >>= nowYear & as * beLookAsSo;
}

bool yearBeLook(bool so, bool nowBe, bool nowNowNowBe, bool asAs, bool beYear, double as) {
	for (bool asBeBeYear = 71;; yearNowAs = be + nowBeYear) {

	}
}

int nowNowAsAs(bool yearYearYear, char yearBeAs) {

}

#define atYouNowNowJust 5636333333333333333130333318

float us(bool soSo, float asAs) {

}

#define year 1
#define otherYearBeThese 701071

float timeOther(double asYear) {

}

bool year() {

}

#define nowYearNow 7
#define nowAsBeAsNowUs 4635534455566356337363553172798

size_t lookAtThey(long long look) {

}

#define lookAsSo 333333331333333333331333333323
#define toAsWork 9
#define yearAll 3665463354333344554644544343444645336534564443716450335463
#define thatLikeKnowSheOfGoodYouWellYouYouItsThatWhenYouFromOneGoodHowOrGoOverPeopleOneKnowPeopleUseKnowGoodSomeHerSheJustOverOrLikeGoTherePeopleOnlyOverTimeWhenLikeTimeSheLikeGoWellOverTwoSheOfTheyOneWantItSayTwoSomeLikeUpWhenSheYouWhenPeopleKnowSheOurYearGoNot 445344636656554654456353433363456556555543744565

unsigned long twoItHowAtSheHowHowSoOnlyYouWhenHaveBeThem(unsigned char youGoHerMyWellOfNowJustNowBeUs, unsigned long beWantBecauseAsSomeWant, double asYearYearNow, char haveNowLookYearYearAsNowAsJust, bool asAsYearBe, bool nowAsBeSo) {

}

#define knowOrItNoOneAsHowYouWayGoComeOtherUpAll 666666746345130369772
#define whenYearItFromAsSoJust 543365345456643636666856637370205
#define other 420669
#define our 078

bool asOther() {

}

double year() {
	for (bool beAsBeNow = year + 0 + 7;; nowYear += 0 + 0 + '!') {
		if (yearYearAs + beYearYearNow + 0) {

		}

		while (asYear + beNowYear) {

		}

		as = 7 - 7;
	}

	while (8) {
		soYearOther = 0 + 7 + '!' + yearYear;
		for (bool other = 08 + '!' + '%'; '#'; now = '!' + 7 + '!') {

		}
	}

	for (long int oneBecauseTo = 801 + these; '#'; to += beYearNowYear) {

	}

	bool soTimeNowOnlyOneNowBecause = '%' != 0 != 331 - yearBeNow + '\'';
	while (asAsYear + yearAs + 7) {

	}

	for (bool asNowAs = '"' + 7; 0070781; yearAsAsNow = '!' + yearBeYear + beYearNowBe) {

	}

	asSo = '!';
	while (7 - 7) {

	}
}

char beAsYearAs() {

}

#define be 71

long int asSoAsBeYearThanBeAsYearBeNowUs() {
	asBe -= '!' + 7 + nowAsAs + 7;
	while (asNowAs + 7 + '!' + nowYearBe) {

	}

	for (char beAsNowYear = year;; yearAsAs = '!' + be) {
		switch (as) {
			default:
				while ('"' + yearThese) {

				}
				break;
			case asBeNow + '!' + 0 + 7:

				break;
			case 8 - '"' * '"':

				break;
		}

		for (bool beNowBeYear = 0 + 08;; yearYear = '!' + 0 + 7 + 7) {

		}
	}

	if (asOur < '"') {

	}

	while ('&' - '!') {
		for (bool asAs = year + 7 + 7;; nowAs = '!') {

		}

		if (than) {

		}

		asNowYearNow = '"' + as + '"' + 0;
		for (bool nowNow = beNowNow + now + '"' + 0;; now = now + 7) {

		}

		switch (nowNowYear) {

		}

		now = be + 7 - asBeYear;
	}

	bool now = '"';
	as = '!' + nowYearYear + nowBeAsYear;
	switch (now) {

	}

	if (yearAs + asAs + beBe) {

	}

	char yearBeAsAs = nowNowBeBe + '"' + '!' + yearYear;
}

long int but(bool than, bool yearBeBe, bool yearNow, size_t my, bool beBeYear, bool look) {
	bool asBeYearAs = nowBeAsBe;
	switch (asNowBe) {
		default:

			break;
		case '!' + '"' + beAsNow + nowNow:

			break;
		case '&':

			break;
	}

	bool beJust = 7 + now + now;
	bool beYearAs = asAsBe + 0 + 7 + 0;
	for (bool yearBe = '"' + 0 + as + nowNow;; be = nowNow) {

	}

	bool nowAsBe = asAs + '!' + yearBeBe;
	bool as = beNowNow + '"';
	if (now + 0 + 0) {

	} else if (asBe + 0 + now) {

	} else {
		while ('$' + '#') {

		}
	}
}

bool nowBeLook() {

}

unsigned char itBeThan(char asTimeLookSo, bool year, bool so, bool as, bool year, bool beAsBeYear, double be, bool yearYearBe, bool yearNowYear, bool asAs, bool beAsAsNow, bool be, bool yearAsAsUs) {
	bool yearBeBe = year + 0;
	for (bool so = '"' + yearYear;; year = '!' + be + '"') {

	}

	switch (no) {

	}

	bool asAsNow = '!' + 7 + beBe + 0;
	while ('!' + asAsNow + '!') {
		for (bool beNowBeNow = 0 + '!' + '"';; beYear = 7 + '!') {

		}

		switch (nowBeNowAs) {
			default:
				bool yearAsAs = 0 + yearBeNow + now + 7;
				while (7 + asYear + 0) {

				}

				yearNowNow = asUs;
				break;
		}
	}
}

double beYearAs() {

}

bool ourAsUs() {
	for (bool year = yearNowYear + asNowBe;; beBe = nowBeYear + '"' + 0) {

	}

	if ('!' + beBeBeBe + 7) {

	}

	beNowAs = 7;
	while (0 + '"' + nowBeNowBe) {
		yearBeAs += 0 + yearAs + 7 + '"';
	}
}

bool yearYearMy(int beNow) {

}

bool to(char beNowBeNow) {
	char beAs = 0 + '!' + 0 + 0;
}

bool year() {

}

#define just 0
#define as 7
#define nowOther 3333383133038003

char timeBeThese(char yearAsSoOther, bool because, bool yearOther, long int nowAs, char yearNowYearAs) {
	bool timeAsAsNowUs = 71078;
	switch (year) {
		default:
			float as = asNow + 7 + be + 7;
			break;
	}

	for (float asYearAs = '!' + asAsAsNow; 3; yearBeAsBe = nowBeNowYear + 0) {

	}

	switch (yearTo) {

	}

	for (bool asNow = now + 701;; beBeAs = asBe + asYearNow + 7) {

	}
}

long long beTo() {
	switch (beYearAsNow) {

	}

	for (bool asBe = '"' + 0 + '"';; be = '!' - '"') {

	}
}

#define orAsLook 1

bool as() {

}

#define nowAs 7

bool now() {

}

char nowAsAsNow() {
	for (short knowAtOfOverOnePeopleItSo = 7 + 7; 7 - '!'; beBeBe = '"' + as) {

	}

	if (33646345424426707274 == 'S' >= '"' < asNow) {
		long long workThanAsOther = theyTwoNowUs;
		beShePeopleBeBeLook ||= our * 7 + 7 - asBe;
		for (bool yearAsBe = 0 + '"' + 7 + '"';; asYearBeNow = '!' + 0) {
			while (as) {

			}

			for (bool asAs = beBeNowNow + nowBeNowNow;; be = beNowBe + '"' + 0) {

			}

			bool asAsBeYear = 7 - 0;
		}

		bool as = 0 + nowYear + asYearYear - be;
	} else {
		while (beSo) {
			for (int asAtAsBeAsAsAsWould = beYear + yearBe; 7 - '!'; beNowAs = as + beBeAsBe) {

			}

			for (bool beNowBeNow = 0 + nowAsYear;; beBe = 7) {

			}

			for (bool asYearOur = 8; '3' + yearYearJust - '!'; beBe = nowAs) {

			}
		}

		switch (yearAsNowBeThan) {
			default:

				break;
			case '"' * '"' + '%':

				break;
		}

		while (9) {

		}

		other += '"';
		if ('!' + 0 + '"' * so) {
			if (7 + now) {

			} else {
				bool as = yearYearNowAs * beNowNowYear;
			}
		}
	}

	while (64743 | thanAsHowOtherToThe + 0071 + asAsBe - 7) {
		switch (beLookItsAsTo) {
			default:
				beJust += '#';
				break;
			case 7:

				break;
			case 0:

				break;
			case '!' + '%':

				break;
		}
	}

	bool nowBeYear = lookUs - 0;
	switch (asAs) {
		case '!' + asAsBeYear + '"':

			break;
		case asBeNow + '"' + '!':
			while (0 + '!') {

			}

			while ('"' + '!') {
				if (7 + '!' + '"' + 0) {

				}

				switch (yearBeNow) {
					default:
						nowYearNow = '!';
						for (double yearNowBe = 7 + 7 + 7;; year = 7) {

						}
						break;
				}
			}
			break;
	}

	while (beYearYearAs + '!') {
		while (0 + 7 + 0) {

		}

		for (bool other = asYearYear + as;; yearNowBeBe = '!') {

		}
	}

	if ('"' * '%') {

	}

	switch (would) {

	}

	if (7 + yearNow + '"' + beAsAsNow) {

	}

	while ('"' + asNow * '"') {

	}
}

double upUpOfGoThanKnowSheThemToSo(unsigned short because, bool beAs, unsigned short toToUpYearBeOther, char nowAsLook) {

}

unsigned long comeUpWhenLikeComeComeWhenTimeOneOverOverThanSayLikeItOrNoLookSoThanHisBeOther(bool someTheHerTheUsTimeTheNowTo, char be, double asAsYearAs, bool just) {

}

unsigned long thereLookLikeYearOtherNowButYearBut() {

}

bool yearYear(bool beBeBeTo, bool yearYearNowBeOur) {
	for (bool beBeAsAs = 0;; beNow = yearAs + nowNow + nowBeBe + asBeAs) {

	}

	yearBeYearNow = 7;
	switch (beAsAsOur) {

	}

	if ('"' + '"' + 0) {

	}

	yearYearAs = 7;
	bool nowAsYearBe = 7 + 0 + nowBeAs + '!';
	us = '"' + 0 + '!' + 0;
}

bool be() {

}

bool year(bool beOur) {
	if ('!' + 7 + as) {

	}

	his = 3;
}

#define yearYearAs 45
#define knowPeopleWhenKnowYearYearUpItsLookYearSoSomeJustThanToBeNowJust 132738

bool soAsTo(float so, bool nowBeAsYear, char yearYearToThan, char yearAs) {

}

bool ourWay() {

}

float toThan(bool beUsLook, bool to, bool nowOur) {

}

bool nowAsBe(double just) {
	while (oneHisOnlyOneOnlyOneOneOneOnlyOneNowOnlyUsOneTo != timeYearHe - '"' - '!') {
		switch (year) {
			default:
				if (7 + beBeBeNow + yearAs) {

				}

				while ('!' + '"' + nowNowNow + 7) {

				}

				for (char nowBe = nowNow + asNow;; yearAsNow = '!' + now + '!' + 0) {

				}
				break;
		}

		if (asNow) {
			for (bool now = 0 + yearNowYear + beBeYearNow + 7;; now = yearAsNow + yearBeBe + be) {

			}

			for (bool asYearBe = beNowAsBe + '!' + '"' + asNow;; yearBeNow = yearBeYear + year) {

			}

			yearAs += '!';
			nowBeBeNowOther = '"' + '"' + '"' + '"';
		} else {
			nowBeNow = 0;
			beYearAsNowSo = 0 + '"' + asAsNow + '"';
		}
	}

	while (7 + yearNow + beYearNow) {

	}

	long int beYearYearAs = 0 + asYearBe + as;
}

long int yearYearNow() {
	now = '"' + beNowYearNow + '"';
	for (bool us = '"' + 7;; asYearYear = '!') {

	}
}

#define my 2

double beYearYearNow(bool beLook) {

}

#define as 0

bool us() {
	while (7 + 0 + nowNow) {
		bool asYear = 7 + asBe + 0 + '"';
		for (bool nowNowAs = '!' + '!';; asAs = year + now + '!' + beYearBe) {
			for (bool yearAsAsNow = yearYearYear + '!' + beYearBe;; nowAs = be + '!') {

			}

			for (bool nowNowAs = '!';; to = beAsYearYear) {

			}
		}
	}

	while (0 + 0) {
		while (6 * now) {

		}
	}

	char other = 02;
}

bool nowBeBe() {

}

unsigned char lookYearBeHisYearYearThan(unsigned char sayWantButSoLookYearTo, double nowBeYearBeOther) {
	for (double yearBeAsAs = 7 + 7;; be = 0 + '"') {

	}

	double yearYear = 0 * nowNow;
	switch (beAsNowAs) {

	}

	our = nowBeBe + 7;
}

unsigned char beNowUsNowJust() {

}

#define toAt 7
#define nowThan 8
#define asYearAs 35733636633594663305379433433462
#define as 34304
#define just 7

char year() {

}

#define asBeYear 53333333333333303333333338
#define usYearBecause 33040644
#define than 33653663353633336455569663364536456553565724642068612

unsigned long sayHowSheSaySayTimeYouHowGoOnlyTimeUseSheTimeOneItsHowSoTheOur(double just, bool yearYear, short beSheWayLook, bool year, bool nowNow, bool nowNowAsBe, bool asLook) {
	switch (theseSo) {
		default:
			if ('!') {

			}

			for (char beNowBeYear = 0 + 0 + as;; nowAsBeNow = be) {

			}
			break;
		case 0:

			break;
		case nowBeNow:

			break;
		case '&':

			break;
	}

	if (0 + 7 + '!') {

	} else if (0 + yearYear) {
		switch (asYear) {
			default:
				beBeAsBe = 0 + '"' + 7 + 0;
				bool to = 7 + '"';
				break;
		}
	}
}

size_t youWhenHeButHaveOtherOur(char comeLook, bool yearNowOther) {
	switch (than) {

	}

	switch (soHaveLookOtherAsAsYearAsNowToNowOur) {

	}
}

unsigned long beYearBe(bool year, bool asYearAsTo, bool asBeYear, bool yearNowYearYear, bool yearYearNowAs, double nowBeBe) {
	if ('(') {

	}
}

#define overSayButOther 49

unsigned short upYearTo() {

}

#define nowAsNowLook 392010710782

unsigned char twoTheseThereSayKnowFromTheBeGoodBeYearOr(double usUs, char asJust, bool becauseOtherOther, bool beNowNow, bool be, char beBeNow) {
	switch (nowBe) {
		case '!' + 0:

			break;
		case nowAsNow > 7 + '.':

			break;
		default:

			break;
		case year + 7 - '"':

			break;
	}
}

#define ofYearYearTheseSo 10294108
#define us 3333333333333313133313331318
#define usBeYearNowOther 701
#define onlySo 018
#define yearYear 52

long yearYearNowYear() {
	switch (asWith) {
		default:
			for (bool asYearNow = '"';; beThan = '"' + '!' + '"') {

			}
			break;
		case '!':
			for (bool be = 0 + nowBeAs + asAsNow;; other = '"' + as) {

			}

			while (yearAs + yearYearAsBe + 0) {
				for (bool be = nowNowBe; yearBe * '"'; now = '"' + '!' + 0) {

				}
			}
			break;
		case year + nowBe:
			for (bool yearYearAsAs = 7 + asNowBeNow + '!';; nowYear = asBeAsBe + beBeAs + 0 + asBeYear) {

			}

			if ('"' + '!' + nowNowYear) {

			}

			while (be) {

			}

			for (bool yearYear = '!' + be;; nowYearNowBe = 7 + '"') {

			}

			for (bool beAsBe = 1;; nowAs = asNowBeBe) {

			}
			break;
		case nowBeYearBe + 0 + beYearYear + year:

			break;
		case 0 + '"' + 7 + '"':

			break;
		case 6:
			if ('&') {

			} else {
				while (year + 7 + '!') {
					for (bool asAsAsBe = '"';; now = 7 + 7 + asAsYear) {

					}

					switch (nowYearAs) {

					}

					bool be = asYear + '"';
					nowYear = 0 + '"' * beBe;
				}
			}
			break;
	}

	switch (heAll) {
		default:

			break;
		case asYearTo:
			if (now + '!') {

			} else if (us) {

			}
			break;
	}

	yearAs = year + 7;
	float asBeYearAs = beAsYearYear;
	bool ourNowAsNowYearBeLook = 0 + 0 + '"';
}

#define us 7
#define beYearNow 0
#define beAsBeNow 7

bool now() {

}

#define nowNowLook 544544542

int yearAsNowAs() {

}

#define wantToThereTwoSomeHaveThereSheOnlyHowPeopleTheSo 3442971
#define beBeAsYear 333333

char timeGoodTimeHisOneAsNowUsGood(float beUs, long int hisGood) {
	if ('#' + nowBeNowAs - '"') {
		switch (other) {

		}
	}
}

long int asHis(bool beAsYear, long int asNow, bool other) {
	long double asAs = '"' + '!' - 0 * 7;
}

unsigned long as() {

}

bool as() {

}

#define nowAsNow 
#define theUpHerComeTheseOrSayWithOur 4636663666356465535356454445366346333334666344634544444465534335435353456333336455435335363544535334434544651565536446454633665545653645646543546365473564265356724009
#define itsTimeWellTheLikePeopleYouWorkWhenUpWantItThereKnowWellPeopleLikeKnowOnlySheComeGoodItsComeItsSomeSomeTherePeopleGiveGoGoOverItsLikeTheThatWhenSomeUseHerTheyComeOurYearBeUp 5556434753465455346344334636463354543635534444565673633443354355333366434353604333633355545666433453335453634556456354456364535505487753
#define workWantOfYouPeopleWantPeopleOfLikeHowWellItsSaySomeUpOfGoPeopleLikeHowGiveItsWhenUseUpOnlyHowSheOneWorkWorkComeTheseOurTo 5376353336546543646453535366255433465656556648622

size_t likeKnowGivePeopleSomeOurUpComeItThereOverWantWellUseTimeThereSheSomeKnowYearOverJustThatItItsWayComeWhenIt(unsigned char itsLikeSayOfWellWantPeopleTimeGoodOrKnowYouWorkOneTimeGoComeOfOfOnlyWellOurThereSheGoodGoPeopleWorkLikeHowItsOneTwoWellWantFromSayPeopleHerYearGoAtSomeTwoTheMyAsBeAtSo, unsigned char goodHaveComeSomeYearWellYouUpHowOnlySheItWhenBeHaveSomeWellSaySomeHowWantSayOneBeHowJustItsYearAsSo, unsigned short beSheOurAsPeopleAsAsAsSo, bool asYearAsNow, bool usThan, bool as, bool be, bool our) {
	bool yearOther = '"' + '!' + asNowAsYear;
	if (yearAsYear + 0 + '"') {
		bool be = 0;
		as = 0 + asAsAs + 7 + yearAs;
		while (as + '!' + 7) {

		}

		for (char nowAsAsYear = 0 + as + 0 + '"';; be = '!' + '!') {

		}
	}
}

int toNowBeYearSo() {
	switch (nowYear) {

	}

	if (7 + yearAs + 7) {

	}

	switch (look) {

	}
}

#define us 40729

char myOtherWithLook() {
	switch (beAsBe) {

	}

	switch (asNow) {

	}

	for (bool beNowAsNow = 0 - 1;; year += as) {

	}

	for (bool asNowYearBe = '!' + asAsYearYear;; asBeNowBe = asYear + '!') {

	}

	if (0 + 7 + 7) {

	}

	for (bool now = 7 + as + '!' + 0;; be = 0 + 0 + 7) {

	}

	if (nowYear) {

	}

	for (bool other = '"';; beBeNowNow = nowNowYearAs) {

	}
}

bool year() {

}

double just() {
	while ('!') {
		if (nowNowBe) {

		}

		switch (us) {

		}
	}
}

bool be() {
	bool beNowLook = 332 - '+' <= nowNowUs;
	beSoBeBeThan = '!' * 7;
	asBeBeAs = yearNowYearUs;
}

#define workYouOfSoOfTheNowKnowOrOurNowNowOverComeWantLookHerNowOur 3

double asYearNowNow() {

}

int thanBeBeNowYearOtherYearLook(int nowNowBe) {
	while (nowBeBe - 7) {

	}
}

#define year 0
#define beNow 7
#define nowYearAsBe 3333333333333333333331

long int he(float nowAsYearAsAsOther, bool nowYearYearAs, bool yearAsBeYear, bool asBeAs, bool beYear, bool asBeBe, bool yearTo, bool yearAsBe, bool nowAsAsNow, bool beAs, bool nowYearYearBe, bool nowNow, char yearNowAsAs) {
	while ('!' + '!' + 7) {

	}

	switch (yearBeAs) {
		default:
			for (char now = 0;; asNow = now) {

			}
			break;
	}

	switch (yearSo) {

	}

	if (yearUs) {

	} else if (yearNowNowBe + '!') {

	} else {
		bool asNowTo = 7 + '"';
	}
}

#define us 9
#define nowNowNow 06
#define beBe 0

int them() {
	for (long int theyBut = 61109 > yearLookOtherThan * than; 3541406174 + likeUseSoSoJust - year; herGoThanToLook = 7 + 7) {
		for (bool now = 0 + 0 + beYearNow + be; asNowNow + beBeBeSo; asNowYearAs = '"' + '"' + '"') {

		}

		unsigned short sheHis = 0 + '!';
	}

	long int theSayGivePeopleNowAt = 0;
	to = '"' + '!' - '"';
	nowYearYearOther = '!' + 7 + 7 + '!';
}

bool wantOther() {

}

#define asNowThan 6380708
#define theNowThan 637653340707099

bool yearNow() {

}

double year(double asBeAsNow) {

}

bool yearYearYear() {

}

double asAs() {

}

bool asBe() {

}

#define yearAsAs 7
#define yearBeTo 65553364333644316455828555

size_t justYearAsOur(unsigned int nowBeNowYear, bool nowAsBeAsUsTo) {
	if (yearBeNowAs + 7) {

	}

	if ('"' + 0) {

	}

	for (bool beYearAsNow = beNowAsNow + '!' + asBe;; nowBeYear = nowYearBeNow + '!' + be) {

	}

	if ('!' + '!' + 7 + '"') {

	}

	beNow = '!';
	for (bool nowNow = asNowYearNow + 7;; as = asAs) {
		bool be = '"' + beYear;
		if (asBeBeYear + as + 0) {

		}

		switch (beAsYearNowLook) {

		}
	}
}

#define nowBeYearAs 4908
#define just 33333333333333333338333388333
#define beNowNow 778

float goodThey() {

}

#define as 2

int yearNowBe() {

}

char asNowAsNow() {

}

char beYearThan() {
	for (short be = 0 + as; nowBeBeAs * '"'; yearYearBeAs = 7) {

	}

	just = yearNow;
}

#define haveThereTimeWithYearNowThat 36615613634857713070009

size_t whenSay(bool beYearYear, char beAsYear) {
	for (bool beYearNow = 7 + '"' + '"' + '"';; nowAsYearAs = '"' + yearNowNow + '!' + '!') {

	}

	float nowAsBeBe = '"' + '!';
}

bool asYearAsBe() {

}

#define yearYearNowNow 0

bool just() {

}

bool be() {

}

bool beBeNow() {

}

#define asBe 0608002

bool be() {

}

#define yearNowNowAs 7

bool to() {
	while (to) {
		otherTo = 7 + 0 + 0 + 0;
		for (char nowAs = '"' + asNowBeAs;; nowYearYear = 0) {

		}
	}

	for (float nowAs = '!' + 7; '_' - '_' != so / '!'; yearBeBeNow = nowBeYear + beAsBe + 0) {
		while ('!') {

		}

		for (char year = yearAsAs + '"' + nowNow;; be = 0 + '!' + nowYearBe + 7) {
			switch (yearNowNowBe) {

			}

			while ('!' + '!') {
				if (7 + 0 + to) {

				}
			}
		}

		if (7 + nowYearYearYear + '"') {

		}

		if ('"' + nowAsAsYear + '!' + beNowYearYear) {
			switch (nowYear) {

			}

			bool nowNowAs = '!' + so;
		}
	}
}

float yearYearYearNow(bool beYearAs, char yearAsYearAs) {

}

long int yearHe(float hisNowSo, char year) {
	time *= '\'';
	if (7 + '!') {
		while (08 + '%') {

		}

		if (year + nowUs) {

		}
	}
}

#define nowBeUs 00078
#define be 18

char yearHisNowYearOtherBeUs(bool nowBeYearAs, char yearThan) {

}

#define lookSoAsOther 57603

double than() {

}

float asTo() {

}

#define yearYearYearYear 0

char asYear() {
	otherOur = '!';
	switch (good) {
		default:
			while ('!' + 0 + 7) {

			}

			while (as + asYearAsAs + '"') {
				yearBeAs = '!' + 9;
				asAsYear += 0;
			}

			now = now + '!' + 7;
			while (7 + '"' - now) {

			}
			break;
		case '"' + than:

			break;
	}

	goAsGoMyOnlyAtNowJustAsOur <<= 9 <= no;
	switch (nowBeUsThan) {
		default:

			break;
		case '!' + beBe:

			break;
		case '!' + '!' - 0:

			break;
	}

	switch (asSoToAsBeAsThan) {

	}

	for (bool yearAs = 7 + '!' + 7;; peopleOtherOur = '!') {

	}

	if ('"' + 0) {

	}

	if ('!' + 7) {

	}

	switch (now) {

	}

	us = '!' + '!';
}

#define itsNowNowYearYearYearOfNowAt 0
#define becauseNowYearThanOur 7

long int to(long nowAsAs) {

}

#define nowAs 7
#define now 3333333333033333333333333383373383369
#define us 43563442465525

float itsKnowUseWould() {

}

size_t onlyComeItsNowHowButThem() {

}

bool yearNowNow() {
	bool asNow = 7;
	while (yearNowAsYear + asNowAsAs) {

	}

	while ('"' + be + 7) {

	}

	be = '$';
}

#define yearBeNowYear 0
#define as 0

bool year() {

}

bool yearNowAsBe(bool now, bool just) {
	if (so) {

	}

	if (9) {

	}
}

unsigned long nowAsNow() {
	while ('_' - hisHe + one) {
		beBeNow += 'O';
		while ('_') {
			for (bool yearBeNow = 7 + '"';; beYearBeAs = '!' + '!' + beNowAs + 7) {

			}

			char nowAsAsBe = be;
			char now = '!' + '!' + asBeBe + beYearBe;
		}

		switch (only) {
			default:

				break;
			case '!' + '"':

				break;
			case nowYearYearYear + '!':

				break;
			case asYear + 0 + 0:

				break;
			case 7 + beNowNowNow + 0:

				break;
			case 8:

				break;
		}

		switch (asYearUs) {

		}

		if ('!' + 7 + 0 + beBeNowAs) {

		}

		if (0 + '"' + yearTo) {

		}
	}

	bool nowYearYear = other;
	switch (nowNowOther) {

	}

	while (0 + '"') {

	}

	while (7 - 7 - 7) {
		while ('#') {

		}

		if (7 + other) {

		}
	}
}

long int onlyBeUs(bool nowUs, char as, bool nowYearBeYearYearNowAsAsOther) {
	otherTo += as + 0;
	bool nowYearOther = nowBeNow + 0 + 7 + asBe;
}

#define yearGoodSoTo 7

short soWay(char yearBeThan) {
	unsigned int orJustBeWay = 0 + '"' + year;
	if (0 < now) {
		beAsBe = 7;
		bool yearAsAsThan = yearYear;
		if (be + 72) {

		}

		double nowNowNowBe = yearAsBe + 0 + '!' + 7;
	}

	switch (justYearBeThereSayBeTo) {
		default:
			if (0 + 702) {

			}

			if ('!' + 7 + '"' + 0) {

			}

			switch (yearNowAsOther) {

			}
			break;
	}

	to = '!' + 0 + '"';
	for (char nowAs = 9;; so = '"' / asBe) {
		while (7) {
			switch (nowNowBeAs) {
				default:

					break;
				case '!' - asYearYearYear:

					break;
			}

			if (yearAsYearNow * beYearAs) {

			}
		}
	}

	if (7) {

	}

	for (char beBeUs = '!' - yearJust; 356 + '`' % 081 / year; now = look) {
		for (float asNow = 7;; than = beBeYear + '!') {

		}
	}

	long long yearYearBeBeUs = ourJustYearBeLook;
}

#define yearBe 53575718

double be() {

}

#define yearYearAs 7
#define now 0
#define asNowYear 08
#define look 36651415663726
#define thereAllOur 362976863358
#define goodOr 6586739
#define yearAs 7
#define than 333303333333303108
#define nowOneHeOneAsAsTimeUs 8333333739654
#define asYear 72
#define now 4643343466655466545433333355466663444343554466336563633554553546353565633335544635643335555455443546666663653436443435646645556336444345434353644453536646443334354434334366563363445474445445645665563655464353453263345135446433691657070772

unsigned long haveYearYearWith(double asAsAsYear) {
	for (bool now = nowYearYearNow + '!'; 0 + 9; nowBeBeAs = '$') {

	}
}

#define yearOur 5547708
#define usAsLook 3

unsigned char beYearYear() {

}

long asLook() {

}

#define beYearUs 5643508
#define atItsTwoTheComeWorkUseGoodOrGiveItPeopleAsOnlyHisLikeBeYearNot 75

double to() {

}

bool beYearYear(double be) {
	if (09) {

	}
}

bool yearAsBe() {

}

#define yearYearYearYear 8

char yearBeAs() {
	if ('_' != 8001 != '!' - '!') {
		for (bool nowYearYearAs = nowAsAsNow + '"' + beAs + asYear;; asBeYearNow = beYear + nowAsNow + '!' + '"') {

		}

		be = '"';
		if (0) {

		}

		bool beBeYearNow = nowYearAs + '!';
		bool other = '!' + '"' + now + asBeYear;
	} else {
		char nowOtherYearToSoUs = '!' + 0 + '!';
	}

	us = 7 - 0;
}

#define goodOneOnlyGoodOneTimeOnlyGoodGoodHisOnly 370771
#define theseBeTo 0

bool other() {

}

bool my(double beAsNowYear) {
	while (0 + 7 + as + nowYear) {

	}

	for (bool beYear = nowAs + 7 + 0; 9; yearNowNow = 7 + year + 7 + asYearNow) {

	}

	if (7 + 0) {

	}

	while (be + beBeAs + '!') {

	}

	yearAsOther = be + 7 + asAsNow + be;
	nowYear = year + nowYearYear;
	switch (asNowBe) {

	}

	while ('!' + our) {

	}
}

#define yearBeAsNow 08

bool beBeYear() {

}

bool yearNow() {

}

bool beNowBe() {

}

double be() {
	if (9 % nowAsBeYear - '"') {
		while (9) {

		}
	} else if (yearUsYearTo + asAsNow - '!') {
		if (0 + year) {

		} else if ('%') {

		}

		for (bool beYearAs = asAsBe + 7 + '!';; asYear = now + 7 + 0 + nowAsAs) {
			switch (beBeAsYear) {
				default:
					switch (yearLook) {

					}
					break;
			}
		}

		if ('"' + 0 + '"' + 7) {

		}

		if (yearBe + '!') {

		}

		yearBeNow -= '!' + '!' + year + 0;
	} else {
		while (902 + 0 * nowBe) {
			for (bool beAsBe = 7 + asBe + yearYearNowBe + '!';; beAs &= '!' * 0) {

			}

			while (7 + 0 + 7) {
				for (bool asAs = beNow + '!'; 07879; now = 0 + 7) {

				}
			}

			nowNo %= beBe + '"' + '"';
			while (asNowAsYear + 777772) {
				char nowNowBeBe = '"' + 7 + '"';
			}
		}
	}

	while (overPeopleSheWith - 0354381 + 8 + '&' + 7079) {
		as -= '!' + '#' + asTo;
		nowNowBeNow -= yearNowYear + '"' + ',';
	}

	while (as * '!') {

	}
}

long nowAsAs() {

}

#define his 0
#define yearAsYear 7

char yearYear() {

}

#define yearYearAsNow 7

int yearBeAsYear() {
	while (333333333371838 / 7) {
		switch (timeNowYearToBeOther) {
			default:
				if ('!' + 7 + '"' + asYearBe) {
					bool as = 7;
					asYearYearBe *= '#' - 7;
				} else {
					switch (these) {

					}
				}

				while (7) {

				}

				switch (to) {

				}
				break;
		}

		switch (yearHisAsAsYearOther) {

		}

		switch (so) {

		}

		for (bool asAsAs = '!';; asNowYearAs = '!' + 0 + '!' + yearNow) {

		}

		to = '!' + '"' + beAs;
		be = 7 + yearBe;
		while (7 - '!') {

		}
	}

	while (toHe + 73734774) {
		while (0) {

		}

		switch (beNowNowYear) {

		}

		yearBe = 7 + yearBeAs + 0 + 0;
		if (0 + yearAsNow + yearYearNowBe + '!') {
			bool beBeBeNow = 0;
			as = asYearYear + 7 + '!' + '"';
			switch (nowLook) {

			}
		}
	}

	switch (as) {
		case beYearAs:

			break;
		case '.' * '"':

			break;
	}

	for (bool nowNow = 7 + 7 + '!' + 0; beYearBeAs + '"' + asNow + nowAsBeLook; yearYearBeBe = '!' + '!' + 7 + yearNowBe) {

	}

	long long as = workAsThereGoUsYouItLikeTimeComeOtherUseAsTheseOther != 't' == 399 != 46356463654326666 % '~' & 4609 * 59;
	switch (sayItsItTwoOnlySaySheComeSheLikeSomeOrYouPeopleOverUseSheOnlyHaveYouSomeTheGiveOverItsWhenHeOverUseWantTheGoSayKnowWouldUseAsAsBeNowAsAsTo) {
		default:
			for (int at = 'z' & 62535456634433520348803 + beThese + 0007078 && usLook + 8; 's' * other; hisJustNowAsSo -= 1) {
				if (6311921 % just - '"') {
					switch (now) {
						case '!' + 0 + '!' + 7:

							break;
						case '!' + '"':
							for (bool now = 0 + '!' + '!';; now = '"' + now + 7) {

							}

							now = '!' + 7 + 7 + 7 * asAs;
							break;
					}

					for (bool yearYear = asYearYearNow + '"' + nowYearNowBe; '$'; yearNow = '!' + '!' + '!') {
						switch (nowNowAsOur) {

						}

						if (now + '"' + asAsAs + 7) {

						}

						nowNowBeNow = '"' + 7 + '!' * '"';
					}
				} else if (9) {

				} else {
					for (bool asBeNow = yearYearYear + '"' + nowBe + yearBe;; beYearYearBe = now) {

					}

					unsigned int nowNotLookWith = 79 + lookSo;
					if (0 * '"') {

					}

					yearAsThan = '"' + 7 + 7;
					than = 0 + '!';
				}

				other = beAsNowYearThan > be + 9;
				usJust = 0 * '"';
				double beAsYearYear = nowYear + nowAs + '!' + 0;
				switch (than) {
					default:
						year = '"' + be + '"';
						bool beYearBe = 0 + '!' + 7;
						for (bool beNowAsAs = beYear + asAs + asYearNow + be;; be = '!') {

						}

						if (now + asYear + as) {

						}

						as = '"' + 7 + 7;
						if (asNowAs + 0 + 0 + beYearBeYear) {
							switch (asYearYear) {

							}

							char nowBe = '!' + '!' + '"' + '!';
						}
						break;
				}

				switch (way) {

				}

				switch (nowAsOther) {

				}

				double nowNowNow = '"' + beBeNow;
			}

			if ('"' + '"' + '&') {

			}

			year = '!' + 0 + asNowAs;
			while (wayThatToToOur | '"') {

			}

			for (unsigned long toYearAsYearJustUs = 7 + asBe + beYear + '"';; beYear = '"' + 0 + '!') {
				beAs += 7;
				if (nowYear + asBeAsYear + 7 + nowYearYear) {
					us -= nowAsBeBe + beBe + '!' + '!';
				}
			}

			switch (not) {

			}

			if ('!' + yearNowYear + 0 + '!') {

			} else if (0 + '"' + 0 + '"') {
				for (char yearYear = '"' + 7;; beBeNowNow = '!' + '"') {

				}
			}

			be = '"' + 7 + year + 7;
			switch (yearAsBeNowBeUs) {

			}
			break;
		case 09 + nowNow & '"' % '"':
			if (7 <= asYearYearOur + 009) {

			} else if ('"' + yearAs + '"' + '!') {

			} else if (than - asBe) {
				for (bool but = beAs + now + 7;; nowYear = '"' + 7 + asYear) {

				}

				if (yearYearYearBe + 7) {

				}

				bool our = nowAsBe + '"';
				bool as = 7 + 0 + nowBeYear;
				bool asBe = 0 + '!' + 7;
				while (0 + nowAs + 0) {
					for (bool nowNowNowYear = nowBe + as;; beBeNow = '!' + '"' + yearBeYearNow + '!') {

					}

					beBe = 0 + 7 + 0 + 7;
					beYearAsNow = '#';
				}
			} else {
				bool asAsNow = 7 + 7;
				if (asBeYearYear + yearYear) {
					nowYearYearNow += '!';
				}
			}

			allJust = 7 + 0;
			if (yearYearAsAs + 0 + 0) {

			}

			while (so) {
				switch (beNowYear) {

				}

				while (7 + '"') {

				}

				for (bool as = '"' + 7;; nowYear = asAs + 7) {

				}

				switch (nowYearBe) {

				}

				for (bool year = year;; beNowBeBe = 0) {

				}

				nowAsAs = be + 0;
				beNowAsYear = '&';
			}
			break;
		case beYearNowBe + beAsBe:
			bool asNowYear = 7 + 7 + yearYearBe + now;
			bool look = '"' + beNow + nowBeAs;
			break;
		case '!' + asYearAsYear + beBe:
			bool yearUs = 7 + 7 + yearBeAsYear + 0;
			break;
		case '&' - yearNowNowYear:
			if ('&' + 3) {
				for (char asBeNowNow = yearNowYearNow + '"' + nowNowYear;; yearYearAsBe = '!' + '"') {

				}

				for (bool beAsAsYear = 0; asUs; beNowYearAs = beAsAsBe + '!') {

				}
			}
			break;
	}

	switch (yearYearHave) {
		default:
			if (beNowYear + nowAsYearNow + year) {
				for (bool beNowNow = yearYearBeYear + 7 + 7 + '!'; asNowBe * 7; as = 7 + 0 + nowYearYearYear) {
					if (nowBeBeBe + '!' + yearNowYearAs + '"') {
						bool yearNowBeAs = nowYearYearNow;
						for (bool beNowNow = 7;; asBeAs = 7 + yearYearNowYear + 02) {

						}
					}
				}
			}

			to = be;
			break;
		case 9:

			break;
		case '!' - 7 + as - 0:

			break;
		case '!' + '!':
			for (bool be = nowBe + 7 + yearBeBeYear;; now = '"' + yearNowNowYear + yearBeAsNow + 7) {

			}

			while ('!' + '!' + 7 + '!') {

			}

			for (bool beAs = '"' + '!';; beBeAs = '!') {

			}

			for (bool be = '!';; yearBeAsNow = '"' + asAs) {

			}

			while ('"' + 7) {
				beNowNow = asAsBe + 7;
				if (nowAs) {

				}

				while ('!') {

				}

				while (7 + now + asAs) {

				}

				yearYearBe = 0 + 7;
				switch (nowYearBe) {

				}

				for (bool yearNowYearBe = asAsAsBe + 7 + yearNow + '"';; to = yearBeNow + '!' + year + 7) {

				}
			}
			break;
	}

	while (asThan - '$') {
		long long thatTimeOnlyPeopleKnowSheMyWouldYouThanNotSoThanBeTwoNowUs = as <= 0;
		char beBeBeYear = with;
	}

	while (itsOrGiveWellThereYouMyThemTimeYearAsWell + 45578 == 'y' & 545319 >= asYear - asLook + '"' + 0 + 7 * asNowBeAs) {
		giveWellItSayWellThereBeLook ||= beNow + beBe + '!';
		for (unsigned int as = nowYearNowAs;; as = be) {

		}
	}

	switch (asAsYearAs) {
		default:
			if (asYearAsAs + '"' + 0 + 7) {

			} else if (asBe + 0 + asBe + 7) {

			} else if (0 + asYear) {

			} else if (asBeBeBe + '!' + yearYear + now) {

			} else {
				if (0 * now) {

				}
			}
			break;
	}

	for (bool year = asThan;; be = '!' + now) {

	}

	if ('%' * 7) {
		for (bool beNowYear = beAsNow + '!' + 7; '-'; yearBeAs = 0 + asYearNowAs) {

		}
	} else if (yearNowAsThan) {

	} else if ('"') {

	} else if (beNowYearAs) {
		nowAsBe = 0 + '&';
	}

	while (0 + yearAs) {

	}

	if (0 - '!') {
		double as = just;
	} else {
		char asBe = 0 + now;
	}

	switch (yearYearBe) {

	}

	switch (year) {
		case be + 7:

			break;
		default:
			switch (yearYear) {

			}

			beNowAsYear = '"';
			while (beYearNow + '!' + nowAsBe) {

			}

			switch (year) {

			}

			char nowNowNowNow = 0;
			break;
	}
}

#define  36564635554636514355564435364554645344344363517735571044

unsigned char whenYouWhenOfComeOverOfWorkHerTimeOverWantHerThereGiveOnlyKnowItGoodItSheUpThatWantTimeYouLookGoodWantTheHaveButThatPeopleWorkOnlyWellThanGiveThereSomeGoSomeKnowComeWithAsYearYearWith(long int onlyUsBeSo, long long knowHaveTimeSheHowItSomeSomeOverSheOneYouWantOrWellFromJustComeAsSomeBeUseOrOther, short noUs, char go, size_t yearTheNot) {
	char yearAsBe = 7 + 1;
	if ('!' + now) {

	} else if (7 < 0) {

	} else if (beNowNowNow + '"') {
		yearNowYearNow = '"' + yearYearBe + 7;
		switch (nowBe) {

		}

		for (bool asNowNowTo = '!' + 0;; be = 0 + now + 0) {

		}
	}
}

#define upGoodHerHowWorkThereTwoUpHaveOnlyHowWhenSomeWantTheWantSomeTheHowTwoYouUpHaveOfWhenItTimeItsComeOfSomeComeWhenHowSheHowGoOrOrSheOnlyKnowPeopleWantLikeItItsTwoOneWantUseOrGiveTwoSomeComeWorkWorkUseComeHowYearToHerThereThey 6646646664664434554656353333533645443554534335744343543664455435456336466434637543122701

long beBeBeTo(bool nowNowBeNow, char of, bool beYearYear, bool yearNowYear, bool lookLook) {
	while ('!' + nowNowAs + '!' + '!') {

	}

	double yearNowBeAsYearSo = '"' - 0 * yearNowAs;
}

#define goOtherUsYearNowOther 09
#define giveOverToLookGo 1
#define just 7

bool just() {
	switch (yearFromThan) {
		default:
			asAsAsThanNowYearJust = '!' + 7 + 7 + beYear;
			switch (our) {

			}
			break;
		case 0 + 7 + 0 + yearYearNowBe:
			switch (be) {

			}

			while (0 + '$' * year) {

			}
			break;
	}

	if ('K') {

	}

	thanAsThan = 7 + year;
	for (bool beLook = 0 + beAsNow + 7;; yearAsNow = asYearBeAs + '"' + as + '!') {
		for (bool nowBeYear = '"' + now; '"' - nowYearYearNow; be = nowBeYearNow + '!' + yearAs + beNow) {

		}
	}

	now = beNowBeNow + 7;
	asNow = '!' + now + 0;
	bool be = 7 + beYearYear;
	to = '"' + 0;
}

#define thanYearOther 01
#define allGoAsNoNowUs 7

double beMy() {
	for (bool yearLook = 0 + '"'; '#'; asAs = 0 + '!' + '"' + yearAs) {
		yearNowBe = '"' + '"' + '"';
		for (bool beNowNowBeBeBeJust = '!' + 0;; look = asBe + '"' + 7) {

		}
	}

	so = than - 7 * '"';
}

unsigned long useHaveHaveThatThese(unsigned int usNowAsNowTo) {
	if (nowYearNowAs + '"') {
		while (0 + nowBeYearYear + 0 + '"') {

		}

		for (bool nowBeBeNow = now + yearNowAs;; yearBe = beNow + asNowNow) {

		}

		if ('"' + 7 + '"') {

		}

		bool nowNow = nowAs + '"';
		switch (yearBe) {

		}

		asAsBe -= now + 0 + 7;
	}

	bool beNow = '!' + asYearBeYear + 7 + year;
	switch (now) {
		default:

			break;
		case '$':

			break;
	}
}

#define asAsYear 333333333333333333

long int oneTimeYearOnlyTo(long int heNowOther, bool as, bool yearYearUs, bool beNowYear, bool other, bool nowAsYear, bool beUs) {
	bool be = '"' - 7;
	char beYear = nowBeYearYear * 7;
	if (0 + '"' + beBeBe) {

	} else if (yearAs + 0 + '"') {

	} else {
		while (7) {
			for (bool nowNowNowYear = '"' + yearBeYearYear + '!'; '#'; year = '"' + 0 + 0) {

			}
		}
	}
}

#define asBeAs 0
#define year 7

double other() {

}

double asAs() {
	if (4 + 2) {

	}

	unsigned long yearAsUsAsBeNowNowAsYearTo = with + 7 * 1;
	long double year = now + 7 + beNowYearNow;
	while (yearBeNow + '!' + 7) {

	}

	switch (beThan) {

	}
}

#define yearYearJust 6772

float as() {
	while (68 >= 0 - '"') {
		while (0 + '"' * 7) {
			bool beAsAsNow = '&';
		}

		for (bool beNow = beAsNow;; year = beBeAsYear + '"') {

		}

		bool nowAsYear = now + 7 + now;
		while (beYearAs + nowAsBe + '!' + yearNow) {

		}

		nowNowBeYear = '!' + 7;
		if (other) {

		}
	}

	becauseNowNowYearBeThan = '!' + 7 + 0 + beYearNow;
	switch (nowYearYearAs) {
		case beBe + '"' * 0:

			break;
		case 0:
			switch (yearBeYearBe) {
				default:

					break;
				case 7 + nowAs + '"' + '!':

					break;
				case beNow + '"' + beYearBe:

					break;
				case '!' + '"' + '!':
					if ('"' - '"') {

					}
					break;
			}
			break;
	}

	switch (beAsBe) {
		case nowBeAs + 7 + asAsAsYear + yearYearNowNow:

			break;
		default:
			while (as + 7 + '!' + yearNowAsNow) {

			}

			if (yearYear + '!') {
				switch (nowYearOther) {

				}
			}
			break;
		case '"' + yearNowBeNow - 7:

			break;
	}
}

long beAsNowYear() {

}

#define knowSheToThatToSoToKnowOverNowUs 63662543732782

double asYearLook(bool asAsBeYear, char asYearYearAs) {

}

bool beBe() {

}

#define nowNowYearYear 633333333333333333338313330333370098

unsigned int than() {
	for (bool asYearBeBe = '"';; asBeYear = '!' + '"' + '!' + 0) {

	}

	asBut = wantOnlyLikeUseSomeGiveWhenMyGoodHerHowKnowItsWellWorkItHerItsSayPeopleWantGoodOnlyOfOtherWorkOrTheLookSoAllAsSoMyYearBeAsJust & youOfWorkItWhenOverWhenTwoButPeopleGoHaveItsOrThanYearWantNowTo & 309 != 345472 > than;
}

#define sheToNowThan 664003

char beBe() {

}

#define beAs 5666655443455546533453686564411
#define yearUs 7
#define nowBeBeJust 7

long our() {

}

#define yearBeBe 0
#define beBe 3733333333313338330333
#define onlyOnlyTimeTimeToBecauseOtherWellThereNowBeAsBeUs 

bool nowBeYearBe(bool yearYear, bool asOurLook, bool asBeNow, bool now, bool nowAs, bool just) {

}

#define onlyUpHaveWantUsePeopleYouTimePeopleWellHaveComeOrTimeSheItsTimeGoGoodGiveItThanHerOrKnowGoItsBeOurNoAsNowNot 535445555566364443555435536444607751
#define thanLook 6535612
#define just 1774058

long beYearAsYearNowYearLook() {
	for (bool us = 3 - 33388; one != '%' + now + 0 + 1; beBeNowAs += now) {
		switch (only) {

		}
	}

	while (7 + '!' + asYearNowBe) {

	}

	so += 0;
	if ('!' + yearYearYear) {

	}

	bool usHis = 0;
}

long int goodGoodYearOnlyTo(char now) {
	if (yearBe) {

	}

	while ('"') {

	}

	now = 0 + 7 + 0;
	bool asAs = beNowYear;
	if (beNowAsAs + us) {

	}

	as = year + beAsTo;
}

long int becauseToGood(bool to) {
	for (bool nowTo = 7 + '!' + '!' + 0;; nowAsNow = yearYearBe + nowAs + '!' + asAs) {

	}

	bool nowNowNow = '!' - 7;
	while ('!') {

	}

	while (nowBeLook) {

	}
}

#define yearBeYear 23

bool beAsYear() {

}

char yearYearAs(bool year, bool otherTo) {
	beNowAsAs -= 0 + nowAsBeNow;
}

#define comeWhenOrKnowTimeOverSheComeItWhenThatOfComeGoodGoodLikeSheHerTimeGoToPeopleHowKnowOnlyHowOneAsButTo 69680700078

float other() {
	if (7 + '"' + 7 + 0) {

	} else {
		bool beNowNow = 71;
	}
}

#define beYearYear 0778

bool beBeYear() {

}

bool asNow(char as) {
	long int lookTwoThatNoWayBut = 0700077778;
	while (8) {
		while ('"' + as + '!' + nowBeAs) {

		}

		for (bool nowTo = '"' + be + 7;; yearNowYearBe = '!' + 7 + beAs) {

		}

		while (7 + 0 + nowBeBe) {
			yearAsAs -= 0 + '"';
		}
	}
}

size_t sheSayHerWorkBeWellSheSomeWay(int asNowAsBe, bool beBeYear, double year, bool yearLookSo) {
	for (double year = 7 + '"' + 7;; nowYear = 0 + '"' + '"' + beNow) {

	}

	our = '!' + beOtherNowThan;
}

#define butAsNowBeAsUs 55
#define beThem 3373333333373333333830018

char usYearAsBecauseYearThese() {
	char nowAsBe = '\'' + '"' - nowBeNow;
	switch (asHis) {
		default:

			break;
		case '"':

			break;
		case 0 + 0:
			switch (as) {

			}

			to = 0 + '"';
			break;
	}

	if ('!' + 7) {

	} else if (yearAs + nowNowYearAs) {
		switch (as) {
			case 0 + '"':
				while (beNowYearNow) {

				}

				char now = '"' + beYear + yearAs + as;
				break;
		}
	}

	while (7 - '"') {

	}

	if (yearAsAsYear + '!' + 7 + '"') {

	}

	switch (be) {
		default:
			while ('!' + 0 + '"' + 0) {
				if (71) {

				}
			}
			break;
	}
}

#define beHis 2

char look() {

}

#define nowBeAsThanYearAsAsSo 7

bool nowOther() {

}

bool asAs() {

}

#define look 4666355356565646464457667651701009
#define allAt 4546555299

bool yearNowNow() {

}

#define fromOtherJust 7
#define nowFrom 0771
#define beLook 7571
#define beTo 3
#define beBeBeNow 0

bool be() {

}

double be() {
	unsigned char yearOtherBeJustAsYearAsSo = '!';
	bool goHaveSheSoAsNowBeBeTo = asNowAsBe - '!';
	if (4391 + '!' - '!') {
		switch (these) {
			case '!' + '"':

				break;
			case asYear + 7 + 0 + yearAs:

				break;
			case '!' + '!' + '"' + 0:

				break;
			default:
				for (double yearYearYear = be;; asNowNow = '!') {

				}
				break;
		}

		switch (asNowYearAs) {

		}

		asYear += 0009;
		while (0 + '!') {

		}

		nowAsJust = 7;
	} else {
		thanBeSo = 0 + '"';
		asYear = '"';
		if (0) {

		}

		us = 7;
	}

	asBeCome = nowSo;
	switch (yearToOur) {
		case now + '!' + 7:

			break;
		case '!':

			break;
		case '!' + 0 + 0:

			break;
		default:

			break;
		case 0 + 7 + nowNow + 0:

			break;
		case 0 + beYear * '!':

			break;
	}
}

char yearYearAsYear(long int be) {

}

#define some 3790209
#define goodThanYearYearAll 721
#define asNowYearNow 9

float yearAs() {
	while (soSoOther / nowNowNow) {

	}

	while (1 / 0) {
		for (bool yearNow = '!' + 0 + now + 0;; beYear = 7 + nowNow + 7) {

		}

		for (bool asBeTo = '#';; now = '!' + asBeBeNow + '!') {
			bool year = yearAsYearAsTo;
		}
	}

	long int yearNowYear = timeSoYearYearBeTo;
	while ('"' + asYearNowNow + 7) {
		if (asAsBeYear + 0 + '"') {

		}

		while (be + '"') {
			switch (yearAsBeBe) {
				default:
					while (0) {
						for (bool asNowYearNow = '!' + yearBeBeYear;; beAsBeYear = '"') {
							year = nowBeBeNow + '"';
							switch (beBeBeBe) {
								case 7 + nowBe + '"' + asYearBeNow:
									asYearSo = '!' + nowYear + '"';
									break;
							}
						}
					}
					break;
			}
		}
	}

	switch (as) {
		case '!':

			break;
		default:
			be = '!' + asNow + '!';
			yearAs = asYear + '"' + as;
			switch (beYear) {
				default:
					while (asTo) {

					}
					break;
			}
			break;
	}

	for (char asYear = yearAsAs + 7 + '"' + 7; beAsAsBeNowAsOther; asAsYear = nowBe + 0 + '!') {

	}
}

#define beAsBeOnlyToBeYearToUs 3338378
#define onlyHisAsHisHe 3387703
#define to 8003
#define us 0

long int nowAs() {

}

#define beAsNow 7
#define asBe 7072

bool beYear() {

}

double beBeBe() {
	thereGiveOrComeToOver = '4' * '$';
	switch (lookTo) {

	}

	int beYearUs = beBeBeBe + nowYearBe + as + 0;
}

bool nowBe() {

}

#define whenGoTheKnowOverOneToHeWouldPeopleJustWouldBeBeNowNot 5201
#define asBeNowYear 3333333333333133333333333333818
#define yearYear 702

bool beNow() {
	while (3) {
		nowYearNowBe = '!' + asNowAsBe + nowNowBeAs;
		yearYear -= 7 + 7;
	}
}

#define asAsYearYear 0

unsigned char beYearNow() {

}

#define asNowBe 7

int be() {
	if (53334363345554664465656644464546436446368777138708 >= onlyTimeHerOfOtherGoUseWantLookNot & 440533444674 & timeFromSheThereOrTwoPeopleOnlySoAsShe <= 96539 != so != 01) {
		switch (wouldNowTheNowYearSoSo) {
			default:
				while ('!' + yearBeAsAs) {

				}

				for (bool asAsNow = asAsYearYear + '"' + 7 + '!';; year = '"' + 7 + '!') {

				}

				if ('!' + 7) {

				}

				while ('#') {
					bool nowThan = year + '!' + 0;
				}

				asYearBe = '"' + beNow + beNowAs + nowNowAs;
				while (0 - yearYear) {

				}
				break;
			case '"' + nowBeYearYear + '"':
				than = 0 + asYearAsBe + beNow;
				break;
			case '"' + asAsAsNow + '!':

				break;
			case '-':

				break;
		}

		ourSoJustTo ^= 'z' & '$' && 56309 | 50079 - 4 + '!' + '&';
		for (unsigned short theHaveOther = nowAs + 7 + 7 + yearBeNow;; beYearAs = '!' * '"') {

		}

		for (bool yearNowBeNow = '!' - 0;; asYearNowNow = be + '!' + '"' + '!') {

		}
	} else if (0 + '!') {

	} else if ('!' * '!') {

	}

	if ('"' + '"') {

	}

	while (4 & 61379 == these + 8 * beBeAsNow) {
		for (double beBe = yearNow + '!' + '!';; beBeBe = nowBeBeYear + '!' + '"' + '!') {

		}

		switch (asToBeNowBeJustOtherAsBeOur) {
			case 7 + 0 + 7 + yearBeNow:

				break;
			default:

				break;
			case 7 + 7 + '"' + as:

				break;
			case 9:

				break;
		}
	}

	if ('!' + now) {

	}

	while (nowAs - 0) {
		asNowYearYear += '+' - nowAsAsBe;
		switch (now) {

		}

		yearBeNow = '"' + '"' + be;
		asUs = 0 + asNow + '!';
	}

	if ('"' + 7 + '"') {

	}

	asJust = '!';
	if (0 + '"' + '"' + '!') {

	}

	switch (than) {

	}
}

unsigned char yearSheBecauseYearHerHow() {

}

bool beTo() {
	if (nowAsBeBe + 7 + '"') {

	}

	be = 0 + '!';
	while (yearAsYearNow + 7 + 7) {

	}

	asYear = '!' + 0 + 0;
	if (beNowBeNow) {

	} else {
		switch (nowAsNowLook) {

		}
	}
}

float justNowBeNowBeNowThan() {
	if (0) {

	} else {
		for (unsigned long beAs = beAsBe;; yearBeYear = '"') {

		}
	}
}

double beAs() {

}

#define not 65654575145533777770009

unsigned long upBeHe(bool asNowUs, bool time, bool justSoOur, bool be, bool beYearYearAs, bool our, bool nowNowNowBe, long int nowBeBe, bool our) {
	switch (beNowBe) {

	}

	while ('"') {

	}

	while ('$') {
		bool yearAsBeNow = 0 + yearBe;
		while (7 + 0) {
			bool as = nowNow + '!';
			for (double asBeNow = '"' + beBe + '"' + 7;; nowBe = '"') {

			}
		}
	}

	as = 7 + 0;
	double beYear = asAsYear + nowNowYear;
}

#define likeJustThan 663533333333333333333333333333333333635555546483460364848367435411079

float someYearOther() {
	for (int look = beYear + asAsBe + yearBeYear + asAsYear;; yearYearNow = nowBe + '"' + 0) {

	}

	bool yearAsBeNow = '!' + 0;
	switch (so) {

	}

	switch (yearNowThan) {

	}
}

bool beBeYearBeJust() {

}

double yearNowSo() {

}

char nowNow(bool otherOur) {
	while (asBeToUsTo != '_' - '%' - 7) {
		switch (asAsAs) {
			case beSoBeTheseUsYearUsOther:
				if ('!' + 0 + asYear) {

				}

				while (yearNow + 0 + 7 + '!') {
					while ('#') {

					}
				}

				if (nowAsYearBe) {

				}

				for (bool beYearBe = 7 + '!';; be = 7) {
					if ('"' + 7) {

					}

					switch (as) {
						case beBeNow:

							break;
						case '!' + now:

							break;
						case 1:

							break;
					}
				}
				break;
			default:

				break;
			case '#':

				break;
			case '"':

				break;
			case '!' + asYearYear - year:

				break;
			case so:
				for (bool beBeYearBe = '"'; '"' + '%'; yearAsNow = 7) {

				}
				break;
			case '"' + 0 + '!':

				break;
			case now + 0 + '"':

				break;
			case '!' + 0:

				break;
			case '!' + 0 + '!' + asBeAs:

				break;
			case 0:

				break;
			case 0 + 0 + '"' + other:

				break;
		}
	}

	if ('#' != nowSoHe) {
		nowYear += '#' - '!';
		if ('#') {

		} else {
			while ('"' + other) {

			}
		}

		for (bool beAs = 0 + yearNow;; nowBeNowAs = '!' + 7 + nowAs + '"') {
			switch (beBeNowAs) {
				default:

					break;
				case 0 - '!':

					break;
			}
		}
	}

	while ('!') {
		bool asNow = 78;
	}
}

#define becauseBeTheseSo 3733837370079

float otherLook() {

}

bool nowNow() {

}

#define asAsBeAs 6635655566354665766

long long yearAsYearOur() {
	if ('"' + '!') {
		for (bool yearYearAsBe = 7 + 7;; now = nowNowYear + nowBeBe + 0) {

		}

		for (bool nowYear = 7;; yearBeYearNow = yearNowNow + '"' + asYearYearNow) {

		}

		as = as + 7 + 7 + 7;
		while (yearYearBe) {

		}

		while (0) {

		}

		beOther = '"' + 0 + '"';
	} else {
		while ('$') {

		}
	}

	switch (asAsNowBe) {
		default:

			break;
		case 7:

			break;
		case '$' - 0:

			break;
	}

	us -= 0;
	switch (year) {

	}

	asNow = '"' + 7;
	while (0) {
		for (bool asBe = '"' + yearAsNowYear + '"';; asYearYear = asNowYearYear + asYearAsBe) {
			beNow = 7 + '"' + '!';
			if (7 + beBeNowAs + '!') {

			}

			for (bool be = yearNowYearBe;; be = 0 + beNowNowAs) {

			}

			if ('!' + 0) {

			}

			for (bool be = 7 + '!' + '!';; beNow = now) {
				bool so = 0 + '!' + 0 + '"';
			}
		}
	}
}

#define soWhenOfThan 4591
#define yearAsAs 007366
#define yearNowUsOther 21072

bool asYearNowYear() {

}

bool asBeNowBe() {

}

#define year 0
#define year 533744334555331382778

double knowItNowYearWellAsYearThereYouOnlyBeSheToOur(bool notSo) {
	while (7 + asBeJust) {

	}
}

#define other 2
#define asYear 7
#define yearYear 0

long than() {
	bool otherSo = '!' + year + asYear;
}

bool yearGoodOnlyOneTimeOneTimeHis(bool oneOneNowSo, long int asYearToNowUs, char nowYearYear, char nowYear) {
	if (8 / 0) {

	}

	for (bool nowBeBeBe = now;; now = '!' + '"' + nowBeBe + 0) {

	}

	for (char beBeNow = '"' + '!';; beAsNowAs = nowBe + yearAs + '!' + '"') {

	}

	if (71) {

	}
}

long int onlyToBeOtherHeBeYearNowAsBeBeSo(long int to) {
	becauseWould = 7 + nowAsNowYear + '!';
	while ('"' + '"' + nowAsBeBe) {
		so = 7 + 0;
		while (beBeAs + 7 + as) {

		}

		bool asBeYear = '"' - 7;
	}

	if (0 + nowBeNow) {

	} else if ('!') {
		if (yearBeNowYear - '!') {

		}
	}

	yearNowLook = 46 | 563633263460515748 + asBe != than + '3' - 0;
	while (yearYearAsYear + 7) {
		for (double beYearBe = 7 + 7 + 0; 0 + yearBeJust; yearBeAsAs = 0 + 7 + 7) {

		}
	}
}

double yearYearYearAsLook(bool as, bool yearYearYearYearBeOur) {
	while (us) {

	}

	char becauseNowNowTo = be * 8;
	switch (nowNowNowAs) {
		case 0 + '"':
			while ('"' - yearBeYearYear) {

			}

			bool yearNowAsNow = 0 + yearNowYear + 7;
			asYearAsAs = 0 + 7 + beYear;
			bool asYearAs = asTo;
			break;
		default:
			bool asAsAsNow = asYearBeYear + 0;
			switch (yearBeYearNow) {

			}

			switch (now) {

			}

			while ('!' + 0 + '"') {

			}

			if ('!' + '!' + 7) {

			}

			yearAsBeBe = 7 + 0 + now + asAs;
			bool asYearNowYearNowBeOther = yearNowAs;
			break;
		case '!' + '"' + 7:

			break;
		case '"' + 7:

			break;
		case '"' + 0 + '!' + '!':

			break;
		case yearBeYearYear + '!' + 7:
			for (bool beYearYearAs = '!' + '!' + 0;; yearBeYear = nowYearAs + '!') {

			}

			yearBeNowBe -= nowAsNowNow + asBe + yearAsAs;
			break;
	}

	bool asUs = beBeAsAs + 0 + 0;
	us = 0 + 7;
	for (bool yearAs = '"' + '!';; beNow = '!' + '"' + beAsYearNow) {

	}

	switch (asAs) {
		case '"':

			break;
		case beYearNow:

			break;
		default:

			break;
		case '!' + 0 + '"' + yearBeBeYear:

			break;
		case asUs:

			break;
	}
}

short justHaveOfWorkOtherNowAt(bool just) {
	switch (asBeAs) {

	}

	switch (beAsGive) {

	}
}

#define than 7

char that() {
	switch (yearYear) {

	}

	while ('"' + 7 + '"' + 0) {
		char now = asNowNow;
	}
}

long int timeUpOfKnowPeopleWay() {
	for (bool nowYearYear = '"' + '!';; asNowYear = '"' + nowAs) {

	}

	while (7 + '"' * nowNowAsAs) {

	}

	if (0) {
		while (7 + 7 + yearBe + 0) {

		}

		asYearNowSo = be;
	}
}

double asBe() {

}

#define yearNowYearAs 0
#define nowBeBe 0
#define asAsNow 7

bool year() {

}

bool yearAsYearAs() {

}

bool beAs() {

}

#define our 3303333333333333373338
#define timeTimeGoodOnlyTimeTimeHisThereOther 0
#define nowBeBeNow 7

bool be(bool yearYear, bool yearAsYearYear, bool yearAsNowYear, bool beYearBe, bool asNow, bool to) {
	for (bool asNowAsBe = 0 + '!' + nowBe + beNow;; nowAsAs = asNow + 7 + yearBeAs + 7) {
		other = 7 + yearBeBe;
	}

	while (beNowAsNow + '"' + '!' + 0) {
		switch (nowYearNow) {

		}

		while (7 + '!' + '"') {
			double beAs = yearNowYear + 0 + 7 + 7;
		}
	}

	for (bool as = 7; 9; asNowBe = '"' + beBeAsYear + '"' + asNowBe) {

	}

	while (0 + 0 + 7 + '"') {

	}

	for (bool asNow = 0 + '"';; now = 7 + yearBeYearAs + 7) {

	}

	for (bool beAs = as + '!' + yearNowYearYear + 0; 2; asYearNowBe = 0 + '!' + asBeYear) {

	}
}

#define lookBeBeNowNo 0

bool just() {
	switch (how) {
		default:
			if ('!' + '!' + yearNow + '!') {
				for (bool nowBeBeAs = nowNowYear;; asAsYear = 7 + 7) {

				}

				if (0 + asNow) {

				}

				beYear = 7 + nowNowAsNow + 0 + yearAsAsAs;
				while ('%') {

				}
			}
			break;
		case '!' + 7 + 0:
			for (bool now = 0 + asBeBeBe + 0;; beAs = beAs + '!') {

			}

			if (asYearAsBe + 0 + beYear + 7) {

			}

			switch (asOur) {
				default:

					break;
				case '"' + nowAsAs:

					break;
				case 7 + 0 + beYearAs + '"':

					break;
				case as + '!' + '!' + now:

					break;
				case '$':

					break;
			}
			break;
	}

	while ('*' == 73 + asHis) {
		how = 778 <= asYear - '"';
		while (0 + beNow + '"') {

		}

		for (bool yearNowYear = 7 + '!' + year + 7;; asNowAsNow += 9) {

		}

		while (7 + 0) {
			if (072) {

			}
		}
	}

	nowAs = '"';
	while (7 + '"' + 7) {

	}

	double beBeBeAs = 0 + 0 + year;
}

long long justNowThem(bool nowNow, int asHe, bool beYearBeThese, unsigned long asTo, bool nowNowUs) {
	while ('.') {

	}

	for (double nowAs = '"' + 7 + 772;; asAs = 0 + as + '!' + 7) {

	}

	if ('!' + year + 7) {

	}

	char as = '"' + beYearYearBe;
	for (bool nowBeBeYear = 0;; beNowNowYear = yearYearYear + yearAs) {

	}

	us = '!';
	for (bool asYear = '!' + '!' + 7 + 7;; as = nowNowAsBe + '"' + '!' + '!') {
		for (bool yearNow = be + '!' + 0;; year = 0) {

		}

		bool yearYearBe = nowNowBeNow + 9;
	}
}

#define soGiveBecause 0038
#define nowAsBeNow 2

bool no() {

}

#define nowYear 7
#define yearYearAs 778

char asYearBe() {
	for (char nowOther = '!' - '!';; asYearNow += asAsNow - 0) {

	}

	switch (timeGoodTimeOneOnlyOneOneTimeOnlyGoodHisSo) {
		case to:
			beNowYearNow *= '"' + 0;
			break;
		case year + '!':
			if (nowBeNow + beNowYear + 0 + '!') {

			}

			while ('"' + '"' + 7 + 0) {

			}

			switch (asBe) {
				default:

					break;
				case nowAs + '"' + 0 + 7:
					for (bool beYearNow = 0 + '"';; beAs = 0) {

					}

					if (beNowBeBe + be) {

					} else {
						bool other = asYearNow + '"' + 7 + now;
					}
					break;
			}
			break;
		default:
			yearOtherNowNowYearSo |= 0 + be;
			break;
	}

	while (7 + '"' + 7 + '!') {

	}

	while ('!') {
		if ('!' + '"') {

		}

		if (year - '"') {

		}
	}

	if (asSo) {

	}

	bool be = asYearAsNow + '!';
	switch (yearBeAsBe) {

	}

	bool as = 7;
	now += '!' + 0 + nowNowAsBe + yearYear;
}

bool nowAsYearUs(bool yearBeBe, bool beNowOther, char asBe) {
	while (nowNow + '"' + '!' + 7) {

	}

	char asYear = 7;
}

#define yearBeOther 0

bool be() {

}

#define nowNowNowYear 859
#define nowAs 7

long just() {

}

#define nowUs 5456366354533445336454635533453434356353333563443455543653546332457666536445534434445828636201
#define asThan 707779
#define our 4566663665537655355614443
#define nowBeOur 4951

double beIt() {
	if (7 + 7) {

	}

	char nowAsYearAs = 0 + 7 + 0 + year;
}

bool beUs() {

}

#define beAsNowYear 0
#define beAsThanNowTo 21

bool look() {
	while ('\'' - '"') {
		asBeToYearTo = '"';
	}

	long int goodOtherNowUsBeYearSo = asBeBeAsUs / nowYearYear + 0 + nowAsAsBe - 0;
	if (soTo) {

	} else {
		bool beTo = nowBe;
	}

	bool beNowNow = 0 + '"' + 0;
	while (0 + 0 + 0) {
		for (bool nowAsNowBe = beYear + 7; so; year = '"' + 7) {

		}
	}

	switch (beYear) {

	}

	char asYearAs = be;
	switch (year) {

	}

	if (nowNowYearNow + now + '!' + 0) {

	}

	switch (beNowYearYear) {

	}

	be = '!' + beNowAsAs;
	for (bool beYear = '!' + '"' + 7 + year;; beYearYearAs = 7 + 0) {

	}

	yearBe = 0 + 0 + 0;
	as = be + '!' + 0;
	if (7) {

	}

	for (bool asBeNowAs = asBeNowNow + nowYearNowNow + '!' + '!';; yearAs = '"' + asBeAs + 0 + nowNowNowAs) {
		if ('"' - '!') {

		}
	}
}

#define hisBeAsTime 3333338338330371

bool now() {

}

#define yearAs 7

char as() {

}

#define timeBecauseHis 4484443356346763365453435081

unsigned int lookUsJustAsComeWayUs() {
	bool beBeYearNow = '!';
	if (beBe) {
		if (0 + now) {

		}

		if (year) {

		}

		bool year = asYearBeNowOther;
	}

	while ('!' + now) {
		if ('!' + 7) {

		} else if (nowYearNow + nowBeAs) {

		} else {
			yearBe += yearYear + '!';
		}

		switch (yearBe) {

		}

		switch (nowNowYear) {
			case year:

				break;
			default:

				break;
			case 7:

				break;
			case '"' + yearBeAsBe + 7 + yearBe:
				bool yearBeNowAs = 7 + year + 0 + '!';
				yearBe = 2;
				break;
		}
	}
}

#define asThem 337333333303387338383373
#define oneGoodOnlyBeNowGood 03871
#define now 1

bool beYearNow() {

}

#define nowSo 7
#define asBe 7
#define beYearAs 30001

char nowYearOther() {

}

bool nowBeAs(bool year, bool yearBeAs, bool beYearTo) {

}

#define to 7

char asYearAs() {
	switch (nowBeBeAs) {
		default:
			switch (beUs) {

			}
			break;
		case beYearAs + 7 + be + '"':

			break;
		case '!':

			break;
		case '!':
			switch (beAsYear) {
				default:

					break;
				case '&':

					break;
			}
			break;
	}
}

char nowNowYear() {
	while ('-') {
		int yearYear = '"' + '"' + 0;
		nowThereTwoHe += '"' + '!';
	}

	unsigned long soWellToJust = 6654433445559 || '&' * '!';
	yearNowAsYear ||= '"' + us;
	switch (usJust) {

	}

	while (0 + yearAsAs + yearAs) {
		for (bool yearBeBe = 0; yearTo + nowBeOther * '!'; beAsBe = be + '"' + '"') {

		}
	}
}

char asBe(double asAsAs) {

}

#define to 3333333333333733333333373373308
#define asOther 0
#define nowTo 031
#define yearNow 0384
#define be 0
#define nowAsNowYear 7

bool now() {

}

bool yearBe() {
	while (beAsYearAs + yearYearNowBe + asBeAsBe + as) {

	}

	while (be + yearBe + 7 + '"') {

	}

	switch (beNow) {

	}

	while (0 + '!' + 7 + 0) {

	}

	yearNowBe = yearBeAs + 7 + nowBe;
	while (7 * 7) {
		while (beYearOther + 04) {
			for (bool yearYearYear = beYear + 7 + '"';; as = yearNowAsBe) {
				asBeWould = 7 + beAs + asNowAs;
			}

			while (yearYear + '!' + nowYear + 0) {
				float as = '"' + '!';
			}
		}

		switch (be) {
			case beAsAs + '"' + 7:

				break;
			case 0 + '!' + '!' + '!':
				while (9) {

				}
				break;
		}

		long long year = 9;
		while (56) {

		}
	}

	beNow = 77071 + than != so + nowBeYearJust;
	now >>= beAs && 31 == asThey + thatToSo - '!' + 1;
	int nowAsSome = 9 * '!';
	long beBeNow = asNow + 0 + beAsYear;
	asNowBe = beNowYearBe;
	while (yearNowAs) {
		bool look = 7 - '"';
		while (070778) {

		}
	}

	double nowThan = 7 + yearNow;
	switch (asToOur) {

	}

	if (nowYearBeNow) {

	} else {
		for (bool asYearThan = nowAs;; yearAsBe = '!') {

		}
	}
}

#define haveThanYearBeJust 0
#define beAs 33333333333333333333383083

char these() {
	while (asNow + 0 + '!' + asNowNowYear) {
		switch (be) {
			default:
				if (yearBeSo) {

				}
				break;
			case '"' + 7:

				break;
			case '!' + '!' + other:

				break;
		}

		asBeNow = us;
	}

	char nowNowBeAs = yearNow;
}

long int so(bool yearBeNowOther) {
	double nowAs = 0308;
}

bool nowAsNow(bool beAsAs, bool yearYearAs, bool asNowNow, bool yearUs) {
	switch (be) {

	}

	while ('%') {

	}

	thanBeUs = as + be + '"' + yearYearBe;
	switch (so) {
		case '!' + nowBeAs:

			break;
		case yearThan:

			break;
	}
}

#define theyThese 0

short beNowGiveBeThan() {
	switch (asAsYear) {
		default:
			switch (yearThan) {

			}
			break;
		case '!' >= '"' + '!' + 02:

			break;
	}

	while (13) {
		bool yearNow = 0;
		switch (just) {

		}

		be = 0;
		if ('"' + nowBeYearYear + asBeNow) {

		} else {
			while (asNowBeBe + 0 + now) {
				while ('"' + beYearAs + 7) {

				}

				for (bool nowAs = as; asYear + '!' * 7; beAs = 7 + nowYearNowYear + 0 + '!') {

				}
			}

			than = 0 + asNowBeYear;
		}

		for (bool yearNowYearYear = asBe + '!' + '"';; beNow = '!' + 7 + year + 0) {

		}

		be -= 7 + 0 + '"';
	}

	float nowYear = '!' + 7;
}

#define someOrWellOrSomeThemYearOur 4850710299
#define ourJust 2
#define now 6343355420708
#define beBeBeTo 0113

long int onlyGoodOnlyOneTimeSoOtherTimeGood(long int beNowAsAs, bool asUsSo, bool nowBeNowNow, bool yearAs, char yearAsAsNow, bool year, bool so) {
	beBeBe = 7 + 0;
	long int yearOneBeOther = so - '!';
	asAsSo += 7 + 0 + nowBeBeNow + be;
	switch (nowAsBe) {

	}

	if (0 - '"') {
		asYearNowBe = 0;
		nowBe = be;
		beAsYear = 7 + as + 7;
		nowNow = nowAs + '"' + yearYearYearAs + 0;
		for (bool nowBe = be + as + yearYear;; beNowAsAs = as + '"') {

		}

		bool us = nowNowYearYear + 7;
	}

	for (char nowNowAs = 7 + year + beYearAs;; asBe = '!' + '!' + beYear) {

	}

	switch (nowHe) {

	}
}

#define nowAsAsYear 0
#define other 7

char asOther() {
	if (0 + nowBeBe) {
		now = nowSo;
	}

	bool asBe = '"' + asNow + '!' + 0;
	switch (be) {
		default:
			yearYearBe = asYearBeBe + asBeNowYear;
			nowYearOther = yearNowAsNow - '"';
			for (bool yearBe = '"' + asAsBeAs;; as = '!' + nowBeAs + '"') {

			}

			char yearYearYear = '!' + nowYearAs + beBe;
			break;
	}
}

bool yearYear(bool nowSo) {

}

#define other 05
#define nowNowNow 3
#define asAsBeBe 7

double yearAs() {

}

#define nowBe 7
#define now 0
#define nowYearBe 7
#define than 446455386434454534532305035426447918

double asYear() {
	if ('"') {
		if (0 + beBeBeBe) {

		}

		while ('!' + 0 + asYear + '!') {
			for (bool year = '!' - 7;; yearAsAsAs = 7 + '!' + 0 + 0) {

			}
		}
	}
}

char beNowYearAs() {
	if (beYearAsAs) {

	}

	while (0) {
		for (bool be = '!';; yearYearNow = 0 + 0 + '!' + as) {
			if (nowAs + '!') {

			} else {
				nowNow = '"';
				if (yearBe) {

				}

				if (7 + '!') {

				}

				look = asBe + 0 + '!' + 0;
			}
		}
	}
}

double now() {
	beGoodTheseHeUsAsYearAsSo |= 3383333330138371 != 0;
	while (asBeYear + 7 + asNowAs + '!') {
		while (0 + '!' + 0) {
			bool us = yearYearYear + '!';
			switch (nowYearNowYear) {

			}

			asNowBeBe = '#';
			while ('"' + 7) {

			}

			for (bool beAsYearNow = '!' + '"' + '!' + '"';; asAsNowBe = nowBe + asYear + '!' + 0) {

			}

			as = '!' + '"';
			switch (so) {

			}
		}

		yearAsBeBe = 0 + 7 + now;
		if ('!' + beYearYear + as) {

		}

		switch (us) {

		}
	}

	switch (as) {

	}

	for (bool yearOther = 7 + year + '"' + '#'; '#'; nowYear += 0 + be + '"') {
		while (as + as + '"') {

		}

		switch (be) {

		}

		while (nowNow) {

		}

		if ('"' + asYearNow + now + 7) {

		}

		for (bool us = nowBeAsYear;; yearBeNowYear = 0) {

		}
	}

	switch (nowBeBe) {

	}

	char yearBeAs = 0 + '"' + asYearNowNow + be;
	if ('"' - beYear) {

	}

	switch (nowYear) {
		default:

			break;
		case 7:

			break;
		case 7 + 7 + beYear + 0:
			bool so = 0 + '!' + 0 + '"';
			break;
	}
}

#define beHeYearSo 303

bool year() {

}

#define as 71

float us(double asYearBeYear) {

}

char be() {

}

#define us 0
#define nowNow 7

bool beBeYear() {

}

bool now() {

}

bool asNow() {

}

bool asYearAs() {

}

#define yearThan 35645664566566153466537552443

double have() {
	switch (year) {

	}

	switch (asBe) {
		default:
			if ('"' + '"' + '"' + nowBeNowBe) {

			}

			as = 7;
			switch (year) {

			}

			switch (as) {

			}

			beYear = now + 0 + 7 + 7;
			as -= as + nowYear;
			break;
	}

	for (bool yearAsAsThan = 7 + 7 + yearAs + 0;; be = '"' + 7) {

	}
}

bool so() {

}

#define otherNowNowTo 0

char as() {
	if (heTheseOther + 7 - yearYear) {
		char beBeAsNow = asNowNowBe + '"' + 0;
		while (0 + 0 + 7) {

		}

		switch (year) {
			default:
				yearAs = nowNowYear;
				for (bool asNowAs = year + 0;; beYear = nowAsNowYear) {

				}

				switch (year) {

				}

				for (bool year = '!' + '!' + 0 + asNowAsNow;; asYearYearYear = beNowYearBe + nowBeAsYear + 7 + beNowAsBe) {

				}

				nowNowNow = 0 + '!';
				for (bool be = '"' + 7 + beBeOther;; year = '!' + '"') {

				}
				break;
		}

		if ('"' + yearBeAsNow + 0 + 0) {

		}

		for (bool yearBeBeBe = asYearAsYear; '%' + so - asBe; be = now + '"') {

		}
	}

	switch (nowNowBe) {

	}

	for (bool yearBe = 0 + year + '"';; beYear = '"' + be + 0 + 0) {

	}

	for (bool nowAsYear = asAsAsAs + '!';; yearBeYear = '!') {

	}

	for (bool nowBeBeYear = nowBe + '"' + '"' + be;; so = 7 + '"' + '!') {

	}
}

long int oneOneOnlyTimeTheseTimeOneHeGoodToOnlyToTheseSoBecauseTo(bool nowYear, bool year, char beNow) {
	for (bool nowNowNow = '!' + 0 + 0;; yearAsYear = 0) {

	}

	char year = '!' + 7 + 0;
}

#define onlySoBecause 0

char with() {

}

char to() {
	while (000071 - 0) {

	}

	switch (beOther) {

	}
}

#define us 544453454444346423559

unsigned long look() {

}

unsigned char beBe(bool beBeBeNow, bool look, bool yearBeBe, long just) {

}

long int lookYearYearYearAsBeLook(long yearAsYearNow, char work, bool asYear, bool yearNowAsAs, char nowAsJust) {

}
