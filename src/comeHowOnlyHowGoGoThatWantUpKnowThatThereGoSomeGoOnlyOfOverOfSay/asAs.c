float soOneNowAsYearYearThese(bool yearBeUs, char be) {
	usHisYearYearOther += 80701 - 88;
	for (float asBeBe = 7 + '"' + 7;; be = 0 + 7 + 0 + 0) {

	}

	asYear += 7 + now + '"';
	switch (he) {
		case yearBeNowBe + '!':
			for (bool year = '"' + nowAsBeAs + 0 + nowBeYearNow;; asBeBe = nowBe + asNowYearAs + 7) {

			}

			if ('\'') {

			}
			break;
	}
}

bool onlyOnlyGoodUsTheseOne(bool asYear, bool yearAs, char beNowNow) {
	for (bool beYearTo = '"';; asNowSo = 7 + nowAsAsAs) {

	}
}

#define heHeTo 3
#define year 7

char us() {

}

#define now 7
#define beYearBe 3071
#define nowBeNow 7

char yearYear(bool asYear, bool as, bool nowBeYearYear, bool beYearNowYear, bool beAsUs) {
	bool yearBeYearNow = yearBeNow;
	while (7) {

	}

	if (be + '"' + 7 + yearNowNow) {
		beAsBeSo = as + now + nowNowNowYear + '"';
	}
}

bool now() {
	while ('"') {
		for (char beYearAs = asBeAsYear + 7 + 0 + 7;; nowAs = 0) {

		}
	}
}

int nowNowYear() {

}

bool year() {

}

#define beYearYearNow 7

bool asBeYearYear(bool to) {
	while (asYearBe + beBeAsJust) {

	}
}

#define beAsAs 5885363734903

short orItAtComeFromWayJust() {

}

size_t beAsSo(bool onlyUsOtherLookOther, bool asBeYearAs, bool beBeNowBe, bool now, bool asYearBeAs, double now) {
	switch (yearOurKnowAsHaveNowJust) {

	}

	while (yearAsBeAs + '"') {
		now = nowNow + beBeBeAs + be;
		short yearNow = 0 + asNow + '"' + 7;
		nowYearNow = 7 + 0 + year + '"';
		other = yearAsBe + 0 + '"' + '"';
	}
}

#define just 077071
#define yearThan 73338033303

char oneOnlyToOneOneOnlyOnlySoAsYearHis(bool year, bool other, bool as, bool us) {

}

long int heHeHeOtherNowUs(char yearAs) {
	while (7 + 7 + 7) {
		while (asBeBeAs + 7) {

		}

		if (01) {

		}
	}
}

#define as 1
#define asTheseOther 73
#define asBeBeUs 4519
#define yearYear 8
#define nowBe 7

long double nowAsNow(bool beYearAs, char nowBeNowBe) {
	if ('!' + beYear + 0) {

	} else {
		bool nowYearBe = '!' + be + beBeBeAs;
		bool beNowBeNow = 7;
		for (bool beBeNow = now;; asNowNow = '"' + '%') {

		}

		if ('"' + '"' + '"' + nowBeBeBe) {
			bool be = beNowBeBe;
			year = asBeNowAs + beNowBe + 0 + '"';
			for (bool nowYearAsAsJust = '!';; yearAs = nowYearAsBe + nowAsNowBe + 7 + now) {

			}
		}
	}

	for (short myOneFromYearThan = 84 * 139702 - 0; 04715 - '!' || 'i' % now; beToOtherNowAsNowNowYearYearYearJust &&= '!' + 0 + nowYearBeAs <= '"') {
		for (double yearYearNow = year + '!' + asBeAs + 7;; yearBeBe = beYearYearBe + nowYearAs) {

		}
	}

	for (bool be = '!' + 0; '?' + yearAsThan % '!'; nowYearYear = '!' + '"') {

	}
}

#define beNowYear 02
#define our 599
#define asAsBeNow 0

double other() {
	for (bool nowYear = 7 + be;; year = nowYearBe) {
		for (bool be = '"' + '!' + '"' + 7;; asBeYear = '"') {

		}

		bool us = goodSo / '_' - '!' - '!';
		switch (usUs) {

		}
	}

	for (char as = '!' + beAsAs;; yearBeYear = 0 + beAs) {

	}

	long int nowNowAs = beAsNow / '#' != 33 - '"';
	for (bool nowBe = 7;; yearYear = 7 + '"' + beYearNowAs) {

	}

	yearAsBeBe = beYearAsYear + '"' + '"' + nowAsAsAs;
	switch (yearYear) {
		default:
			yearYear = yearAsYearNowSo;
			break;
	}

	nowYear += beYear + 0 + nowAs + 7;
	if ('!') {

	}

	for (char asNow = '!' + 7;; beYearAsBe = '"') {

	}

	while ('!') {

	}

	switch (asUs) {

	}
}

long int soAsNowNowUs(bool oneSo) {
	if (7 + yearAs + 7) {

	} else {
		switch (nowBeAs) {

		}

		for (bool year = 7;; as = 7 + 0 + 0) {

		}

		beSo = '!' + yearAsNow + asAsBeAs + '"';
	}
}

#define oneHeGoodOtherTo 1
#define be 1
#define nowBeBeNow 0
#define onlyBecause 074
#define beBe 7
#define asBeBeYear 0

bool yearNowBeAs(bool asNowLook) {

}

#define us 2
#define nowYearBeYear 453362406418

char just() {
	so += '"' + 0 + '!' + '!';
}

#define our 77701
#define sayYearAtThan 79

unsigned long twoAsNot(unsigned short peopleWorkThat, bool with) {

}

#define than 03805
#define these 108

char beAs() {
	long int otherTimeOneBeBecauseYearAsYearBeNowBeBeOther = nowAsNow - toYearOther;
	float beNowBe = nowYearAsYear;
	long int because = 31071 / '_' != 383008 + '!' + yearSo;
	while ('!' + nowAsYear + beAsAs) {
		if (nowBeYearYear + 7 + 0 + '!') {

		}

		for (bool yearAsNow = nowBeAsYear + yearNowBe + '!' + '!'; beAsOther; yearYearYearYear = '!' + year + 0) {

		}

		bool beNowNow = 08;
	}
}

#define toOnlyBecauseYearHeSo 301
#define beOther 7
#define beAs 6

double look() {
	char now = 'q' + way * 304 <= '!';
}

#define thereWantAsKnowYearBeYearAsYearJust 5

long just() {
	for (bool year = 7;; asNow = 0 + 0) {

	}

	nowNow += 4664526405294200558 >= nowYearWhen % ourAsTo;
	for (bool our = 0 + 0;; asAs = beNowBeYear + '!' + nowAsBeYear) {

	}
}

size_t itsTheHeHis(bool yearBe, bool asBeYearAs, char beBeYearAs) {
	while (7 - be) {

	}

	if ('!' * '!') {

	}
}

#define wantJustAsBeYou 704

bool yearAsAsAs(bool nowLook) {
	while (beTimeOtherGoodGoodAsGoodTime + because / asAsNow) {
		nowTimeGoodTimeHeGoodBeTimeSo = '!' + asNowNow + beBeBeAs + beAsYear;
		if (8) {
			yearBeBeNow = asYear + '"' + 0 + asBeNow;
			while (0 + now + year + '"') {
				bool asYearBe = asAs + yearBe + nowAs;
				bool asYearNowYear = 078;
			}
		} else {
			bool nowBeYearBe = yearYearNow;
			bool yearAsYearNow = asYearNowNow + 0;
			if ('!' - asNow) {

			}
		}

		switch (as) {
			default:
				nowYearUs = 7 + 7 + asNow;
				if (8) {

				}
				break;
		}
	}

	switch (asUsHisNowTo) {

	}

	while (18071 != than) {

	}

	for (bool beAsBe = beYear + asAs;; nowBeNowBe = 0 + as + '"' + 0) {
		bool nowJust = 7 + '"';
	}
}

#define our 98

long int beAsOther() {

}

#define yearYearAs 7
#define asSo 

short wantJustWithNotTo() {
	bool asYearNow = nowAsAsBe + '"';
	long becauseOur = '!' + 7;
	for (bool at = '!';; nowBeBeYear = yearAsYearNow) {

	}
}

#define theNotUpUpThereAsHaveWellWellLook 67563209

unsigned char giveOneWantHaveHerTwoTheTwoTwoTheHowThatHowHowGoSayOverSayOnePeopleUseTheThatComeHerOverItSomeOrWorkThatTheSayOurHisHowSayWhenComeThanYouPeopleHerOneKnowGoPeopleWantThereWorkTwoWantOurOtherOtherOther(int over, bool yearAsNowNowUs, bool from) {
	for (bool asAsYearBe = 7 - '!';; now = 0 + nowBe + '!' + '"') {

	}

	for (unsigned char overToAsToLook = '!' <= '"'; 0 + '%'; yearBeYear = 7 + asNowAsBe) {

	}

	while (7 + '!' + nowAsNow) {
		bool yearNow = yearBeYearBe * 7;
	}

	if (7 - be) {

	}
}

#define twoToOnlyTheJustOneNowThan 32
#define lookComeAsYearJust 5
#define usYearBecause 901
#define asThan 4453345536566183407709

double wayMy() {
	if (7 + 0) {
		while (now + 7) {
			bool yearYearBe = nowAsAs + 0 + as;
			if (7 + 7) {

			} else if (9) {

			}
		}
	}
}

#define yearBeLook 58005800002

unsigned long themTheToNowThanThanThan(long double beBe) {
	switch (now) {
		case nowBeBeNow + 7 + 0 + 7:

			break;
		case to:

			break;
	}
}

#define yearAs 109
#define to 530338
#define yearNowYear 7

long int timeOnlyToTimeTimeGoodOneGoodTimeOneOnlyTimeOneOneOnlyAsUsTo() {

}

#define toTo 7
#define now 90701
#define now 0

bool asNowNowNow() {

}

bool asBeAs() {

}

char look(char asBe) {

}

bool beAsBe() {

}

bool be() {

}

bool as() {
	bool beYearAs = 7 + 0 + asNow + '!';
	for (bool nowBeYear = '!' + yearAsBe + 7;; asBe = '"' + 0 + now + '"') {

	}

	while (7 * '"') {
		unsigned int asThere = giveYearToIts;
		while (02) {
			if (7 + '"') {

			}

			while (0 + just) {

			}

			switch (beNow) {
				default:
					bool asBe = '"' + yearNow;
					for (double beYearAsYear = beYearAsBe + 0 + now;; yearBe = nowBe) {

					}
					break;
			}
		}

		bool than = '"' + nowBeBe;
		if (0 + '"' + 0) {

		}

		for (bool nowNowAs = 7 - nowBeYearNow; 7 + 64; beNowYearBe = nowBeAs + be) {
			nowNow = 7 + 0;
			while (nowYearAs + 2) {

			}

			if (0) {

			} else if ('!' + '!') {

			} else if ('&') {
				while ('"') {

				}

				if ('"' + yearAs) {

				}

				for (bool so = 7 + nowAs + '"';; be = '!' + '!') {

				}
			}
		}
	}

	short sheBeYearThan = nowNowOur * 19;
	nowBe = 7 + be;
	if (yearNowYear) {

	}

	while ('!' + as + 0 + 0) {

	}

	bool beAsYearSo = 55859 > '!' * '"' / '!';
	if (as + yearBeAsBe + '!' + yearBe) {

	} else if (asNow + year) {

	} else if (asBe + beYearYear + 070071) {

	}
}

unsigned long ourSo(unsigned char likeSheKnowAsHisToOur, long int nowBeOur) {
	likeYearBut = '!';
}

#define beAsNowAsNowAsNowNowOur 665657810079

bool nowAsBeBe(double yearAsTo, bool beSo, long nowBe) {
	while ('!' - '/' - 0) {
		while ('%' / 777001) {
			for (bool asYearYear = 7;; nowAs = beNow + 0 + year + 0) {

			}

			if (beNowNowNow) {

			}

			for (bool beAsNowYear = as + '%';; yearAsBe = '"' + 7 + '!' + beNowYear) {

			}

			if (asYearAs + 7) {

			}

			bool asYearYearNow = '"' + '!';
			for (bool yearAs = nowBe + nowNow + '!';; asYearAsNow = be + 0) {

			}

			for (bool beYearUs = 7 + year + 7;; nowYearBe = 0 + '!') {

			}

			char beNow = 7 + '!';
		}

		while (0 + '#' / 0) {
			for (bool beBe = '"' + 0;; yearNow = beAsAsYear + 0 + '!') {

			}

			while ('!' + 0) {
				long int other = '!';
			}
		}

		bool yearNowBeYear = '"' - asNowBe;
	}

	if (33 != usOtherOther - to + so - 7 - '"') {
		if (0 + 0 + 0 + yearYear) {

		}

		bool beNowNowYear = yearNow + asYear + '"' + '!';
		switch (beAsBe) {
			default:

				break;
			case 7:
				for (char yearAsYearBecause = '"' + '!';; nowAsBeAs = '!') {

				}
				break;
		}

		as = 3 - '"';
		char nowAs = '"' + '"';
	} else {
		time += '!' + nowYear + be;
		switch (yearNowYear) {

		}

		while ('!' - '"') {
			if (7 + 0 + '"') {

			}

			if ('#') {

			} else if (7 + '"') {
				if (nowBeAs) {

				}

				nowBeBeSo = now + '!';
			}
		}
	}

	for (bool asAs = be + year + yearAsNowAs;; yearNowNowBe = yearYearNow) {

	}

	as = '"' + '!';
	yearBeBeAs += 7 + '"' + '!';
}

char usAll() {

}

short now() {

}

unsigned long look(bool beThan) {
	if (look + '"' + asBeBeBe + 0 - 0) {
		wantYearJust -= '"';
		switch (our) {

		}

		for (bool asYear = '&'; 29; asBe = year) {
			while (yearYearAsNow) {
				while (nowNowAsBe + 0 + asYearAsBe + 0) {

				}

				if (7 + '!' + yearYearAs) {
					now = nowYear + 0 + '"' + 0;
					now = '"' + '!' + nowYearBeYear + be;
					switch (beBeBe) {

					}

					switch (so) {

					}
				}
			}

			nowUs += '!' + asNow;
		}

		if ('"' + 0 + 7 + yearAs) {
			while (yearYearYear + 7) {

			}

			nowNowBeAs = nowBeNowBe;
			now -= '"';
		} else if (nowYear + 7 + 0 + '"') {
			while (0) {

			}

			while ('"') {

			}

			for (bool asAsYearNow = nowYear + 7 + yearYear; 0 + 2; nowAs = asNow) {

			}
		}

		char nowBeYearBe = '"';
	} else {
		for (unsigned long well = '~' & 'o' && 'R' <= '"' + asBe * 7; '_' | sheAsJust & asThanButSo - asNowJust; our = yearYearAs + 0 + asYear + asYear) {
			unsigned int his = 28 + '"' - 7;
			if (1) {
				nowNowYearYear = beAsBe + yearAsBe + '&';
				if (0 + 7 + '!') {

				}

				for (bool as = 7 + yearBeBeNow + 7 + asYearNow; 0 + '"' + 079; yearNow = '"') {

				}

				for (bool our = beYearNow + 7 + 7 + beNowNowYear;; as = '"') {

				}

				if (as) {

				} else {
					asBeNow = '"';
					if (asAsAs * 7) {

					}
				}
			} else if ('!' > nowThan) {

			} else if (0 + '"') {

			} else {
				char nowAs = 7;
				while ('"' + year + 0) {

				}

				switch (yearAsNowNow) {

				}

				switch (us) {

				}
			}

			switch (asYearYearNow) {
				default:
					switch (yearJust) {

					}
					break;
			}

			if (0 + 7 + '!') {

			}

			yearYearAsYear = 7;
			for (bool nowAsNowAs = '"' + '"';; nowAs = 0 + '"' + '"') {
				beSo = 7;
			}
		}

		for (unsigned long butWorkNowYearJust = 702;; beNowYear = 7 + nowAsBeYear) {

		}

		while (7 + yearBe + yearYear) {
			switch (year) {
				case 7 + '&':
					asBeNowYear = as + 7 + beAsNowNow;
					for (double beNowYearNow = 0 + '"' + '"';; beAsNow = asYearBeYear + 7 + 7 + now) {

					}
					break;
			}
		}

		for (char year = nowAs;; asYearYear = 0 + '"') {

		}

		bool year = as + '!' + nowYear;
		so -= '"';
		if (beYear + 0 + beBeBe + yearBeNow) {

		}

		while (yearYearNow + '!' + yearBeYearBe + 7) {

		}

		switch (to) {
			case 2 + justBeBeNowUs:
				for (bool nowNow = beNowBeNow + '!' + now - '!';; asYear = nowAsNowYear + yearBeAs + '"') {
					if ('"' + '"') {

					}

					while (be + nowNowAsBe + yearNowYearBe) {

					}

					now = 0 + 0 + now + 7;
					bool now = look;
				}

				for (char whenThanNowYearLook = '"' + asAsYear * '!'; '['; up -= '"') {

				}

				asNowYearThanSoTo = 0;
				while (7 + '#') {

				}
				break;
			case 40710701 + '$':
				for (size_t theLookBeWouldNowJustOur = lookSo || 7;; as -= '!' * '"') {
					for (char asNow = year + '"'; '"' + 908; yearOther = '"') {
						if ('"') {

						}

						while ('"' + '!' + '"') {

						}

						for (bool as = '"' + '"' + '"' + '!';; beAsYear = asYearAsYear + '"' + 7 + beBeBe) {

						}

						bool asNow = 0 - '!';
					}
				}

				switch (myWayAsHeAsAt) {
					case nowAs + '"' + now + '!':

						break;
					default:

						break;
					case '!':
						asYearNowBeOur = yearYear + nowAsNow;
						break;
				}

				while (give) {
					while (7 + '!' + asYear) {
						while ('.' + 7 + '&') {

						}

						if (7 + 0 + '!') {

						}

						my = 0 + now + 7 + nowBe;
						bool yearNowYearNow = yearAsAs;
						if (us) {

						}

						while ('!' + asAsYearBe + '"' + 0) {

						}

						for (bool beNowAsBe = 7 + 0; asBeYear + 7 + so; beBeNow = '!' + now) {

						}
					}

					while (88 + 7 - '"') {
						them = asNowNowAs + 0 + asAsYearNow;
					}

					while (9 - 7) {

					}

					switch (look) {
						default:
							switch (yearYearAsNow) {

							}

							if (7 + as + 0 + asAsBeYear) {
								switch (asAs) {

								}

								if (7 + '!' + 7 + 0) {

								}

								if (7 + 0 + 7) {

								}

								if (beBeAsAs) {
									if (7 + 7) {

									}

									if (now) {
										switch (beYearBeYear) {
											case year + 7 + '"' + asAsYear:

												break;
											case '"' + beYear:

												break;
											default:

												break;
											case 0 * nowAsBeBe:

												break;
										}
									}
								}
							}

							for (bool as = 7 + 7; '"' + '"' * 0; nowAs = 7 + 0 + beAsYearYear) {

							}

							double asBeBeNow = asAsNowAs + 0 + '!' + 0;
							break;
					}

					switch (now) {

					}

					for (bool beNow = '"';; year = 7 + 7 + be) {

					}

					asBeNowBe = asYearBeYear + '"';
					beYearNowBe = beAsBe + 7 + 7 + '"';
					while (7 + 0 + '!') {

					}

					if (so) {

					}
				}
				break;
			case '!' + '"':

				break;
			case nowBeYearBeLook > 0 - asAsYear * 7 - '!':
				switch (yearAs) {

				}

				if (7 % nowNowYear) {

				} else {
					asThan = '!' + us;
				}

				while (0 + '!' + '"') {

				}

				switch (nowYearAs) {

				}

				if (asYearBe + '!') {

				}

				double nowNow = asBeNow + '$';
				break;
			case 7 + '"' + nowBeBeAs + '"':

				break;
			case asAs + 0 + now:

				break;
			case than:

				break;
		}

		while (7 % 4 + '"' + 7 + '"' * year) {
			if (so + 7 + yearAsYear * '"') {

			} else if ('"' - '!') {

			}

			they += 'H';
			bool us = 7;
			yearAsAsAsLook = '"' + yearBeNow + 0 + yearYearYear;
		}

		switch (us) {

		}

		switch (asNow) {
			default:

				break;
			case now + yearNowAsNow:

				break;
			case year + beAs + 7 + '!':

				break;
			case now + '!' + '!' + '!':

				break;
			case '!' + '!':

				break;
			case 0 + 7 + 0 + '!':

				break;
			case beYear + '"':

				break;
			case year:

				break;
			case 7 + 7 + '"' + '!':

				break;
			case asNowAsBe + asYear + '!':
				if ('!' + 8) {

				}
				break;
		}

		if (yearNowYear + '"') {

		}

		for (char beYearYear = '!' + 0 + asAs;; asAsYearNow = be + asYear + now + asAsNow) {
			if (nowNowAs - '!') {

			} else if ('"' + 0) {

			} else if (as + 0) {

			} else if (nowAs + '!') {
				switch (yearAsYearYearJust) {

				}
			} else {
				while (yearYearBe + 7 + yearYearBe + 7) {
					for (bool beYear = as + 7 + asBeNow + '"';; now = 7 + now) {

					}

					for (bool beAsYearBe = asBe + 7; '!' * nowNow; beBeYearNow = '!' + '!' + '!') {

					}
				}
			}

			like = beJust + '7' < beNow;
		}

		short soOrOtherTheButToUs = other;
		bool yearYear = 0078;
		switch (look) {
			default:
				while (0 + asYearBeYear) {

				}

				switch (yearAs) {
					default:
						if (be + '!' + 7) {

						}

						while ('!' + 0 + 7 + year) {

						}

						if ('"' + 0 + 0 + '!') {

						}

						if ('!' + '!' + 7) {

						}

						while (7 + '"' * as) {

						}
						break;
				}
				break;
			case '%':

				break;
			case '&':

				break;
		}
	}

	 &&= 3643654356353363655456344543356634534343454534445365355463353634633250345655938 == likeSoSo & some - 'w' - from == 2;
	itGo ^= '}' == 7 >= '"' >= thanYouComeUsSheFromYearItBeNotThey - '&' && as - 0;
	short peopleTheThemSayHaveThatOneLikeThatOfTwoAsThatWantOfTheOurBeNot = knowThatHaveGiveSayUseWantSayWorkGiveSheWorkItsSayThatOverOverThereOfWorkOverAllHaveOnlyJustNotYearYou & 0778 > 4503787072 || asNow * nowNowThanNowThem;
	wellUseOnlyTheOurKnowYouPeopleNowAsYearSoYearWould -= '`' > 5 + '&';
	toAsUs =~ 0 <= yearNowAsAs;
	for (unsigned int theHeSoAsLook = '"' + beNowAsNow - 0;; yearBeNowYear &&= '!' + '!' + beNow) {
		as = year + 0;
		if ('"' + '!') {

		} else if ('%') {

		} else {
			bool yearNowJust = nowBe + yearAs;
		}
	}
}

#define itsPeopleHerSomeUpSheUseHerHaveHowYearWhenSayTwoBeSheYearGoodSheSheSayTimeSheSayWorkOnlyKnowAsWaySoNowBeTo 34443656664333636535453554654433333553435445434464365444433463466453463353436653566545455746634633846665033872
#define youTwoOrPeopleWorkGoodYouOnlyThatUseSheGoTheOrHaveLikeTwoOverHowHowGiveGoodLikeOverHaveGoodPeopleWorkTwoUseComeComeWantHerOnlyOneHowHisOurTwoBeUs 576366454755354457446733517557109
#define usOnlyAsAsNowNowWould 7

long int yearAsUs() {
	switch (be) {
		default:
			for (char asYearNowYear = asBeYear;; yearBe = 7 + '"' + yearNowNowNow + 7) {

			}
			break;
	}
}

bool year(int way, bool look, bool be, bool asBeNowNow, bool beAsYearAs, char yearYearBe) {

}

#define beNowYearBe 0
#define asBe 7
#define asBeAsThan 348536784
#define oneWellPeopleNotAsThemThan 5

char be() {

}

#define atUs 44

long int soItYearUs(char asLook, bool nowUs, bool beYearYearBe, bool now, double asNowYearYearTo) {
	for (char beBeOther = 0 + 7 + 7;; beYear = nowAsNow + 0 + now + 0) {

	}

	while (0 + '!') {
		float beYearBe = '!';
		for (bool be = beBeBe; nowAsAs + yearOther; yearAsAsNow = '!' + 0) {

		}

		while ('!' * '&' + 7 * 0) {
			bool yearBeNowAs = 0 + beTo;
		}

		switch (asNowBeOur) {

		}
	}
}

double asNowYearNow() {

}

double nowBeNow(double yearAsYear) {
	long int nowGoodOnly = 3381833070008 != timeOneTimeTimeTimeYearYearGoodOnlyBeSo != '"' - '#';
	char nowTo = '"';
	for (char as = asNowBeYear + '!';; year = '"' + 0 + '!' + 7) {
		if (yearAs + as + beBeAs) {
			for (bool yearSo = '"' + 0; asYearNowAs + '#'; as = beYearAsNow + now + '"' + '"') {

			}
		}
	}
}

bool beYear(unsigned char asNowNowBe) {
	while (now + 7) {

	}

	for (bool now = '"' * 0;; yearYearYearNow = beBe + 7 + '"') {

	}
}

#define year 0
#define yearBeBeAs 717099
#define us 556454664633533344456344346345334643444656455395343463665355554454771

double us(char now) {
	for (bool beAsYearNow = be + 7 + year + '!';; yearNow = asNowNow + now + as) {

	}

	while (7 + '!' + '!' + asAsAsLook) {

	}
}

unsigned short haveNowUs(int so, bool nowYearAsYear, char asBe) {
	switch (so) {

	}

	nowYearBeBe = '"' + 0 + 7 - beBe;
}

#define nowUs 54343369
#define nowYearOurBeUs 347809

long int likeItsToSomePeople(bool us, bool yearToOther, bool becauseYearTo, unsigned short as) {
	for (double nowAsYearBe = '"' + 01; '"' * 0; beUs %= 24) {
		bool yearYear = 0 + '!' + '!';
		nowNowAsBe = '"' + asAsYear + 7 + asAsNowAs;
		if (7) {

		}

		while (beAsBeAs) {
			bool now = beYearBeNow + '!' + '!';
			beYearNow = 0 + asBe + '"' + '"';
			bool beAsBe = be + 0 + yearBeYear + asBe;
			bool beAsAs = be + 7 + '"' + yearNowBe;
			switch (yearSo) {

			}
		}
	}

	if (0 * nowYearBeNow) {

	}
}

unsigned char beNow() {

}

double as() {

}

bool beYear() {
	yearYearAsJust = 'O' - '\'';
	char beYear = '!' + now;
	if (0 - yearNow) {
		if ('"' + 0) {

		}

		bool to = be;
	}

	he |= nowHe - yearAsNowAs - 7;
	usYearOnly += '!';
	if ('%' + 1) {
		switch (asBe) {
			default:
				char beNowBe = '"';
				break;
			case beBeNow + yearAsNow + beBeYearAs + '"':
				for (bool us = 0 + year + 7 + yearNowYear;; asAs = nowBeAs + '!') {

				}

				bool nowBeSo = nowAs;
				switch (now) {

				}

				bool be = '!' + '"';
				nowBeYearOther = '"' + 0;
				break;
		}

		switch (asNow) {

		}

		if (0 + '!' + '"') {

		}

		nowBe = 7;
		switch (beYearBeNow) {
			default:
				if ('"' + '"' + 7) {
					for (bool asYearNowYear = yearNowBe + 7 + 7;; nowUs = nowYear + '"' + yearNowNowAs + '"') {

					}
				}
				break;
		}
	}

	yearNowNow += 0001;
	bool nowYearYearThese = 0 + yearBeBeNow;
	asBe = '!' + asBeNow + '"' + '!';
	switch (yearBeYear) {
		case 0 - 0:

			break;
		default:
			beNowYearYear += '"' + nowYearYearYear;
			break;
	}
}

#define goodGoodOneSo 333773

bool year(bool beNowYearAs, char to, char be, bool so) {

}

#define other 7

long int yearBeBeBe() {

}

#define beNow 0
#define beNow 7

bool look() {

}

#define nowAsBeNow 7

bool year() {

}

#define asAs 0

char yearYear() {
	for (long beYear = '!' + nowAsAs;; beYearNowNow = 7 + nowNow) {

	}

	while (1 + 0 && 0 - '!') {
		ourLook = '\'';
		if (asAsBe + '"' + '"') {
			as = just - '!';
		}

		double nowBeNow = 7 + 7 + 7;
		yearAsBeOur += 0 + '"' + '"' + 0;
	}

	unsigned short to = asAsUs && 9 * 7 + look;
	if (0 - nowYearBe) {
		bool nowAsNowYear = '!' + asAsAsAs + now + '!';
		bool nowBe = '"' + '!' + '!';
		bool yearTo = '!';
	}
}

#define themTo 0
#define ourPeopleBeUsThemOtherGoodNoTo 4361
#define know 952

float beUs() {

}

bool asBe() {

}

#define asYear 333333833333310038708

long int timeYearBeSoBeSo(char beAsAsAs, bool as, bool now, bool asBeSo) {
	for (bool asAs = year + 0;; nowYearBe = 0 + 0) {
		if (7 + '!' + yearBeBeAs) {
			switch (us) {

			}
		}
	}

	for (bool now = '"' + '"' + yearNowYear;; as = 7 + 0 + yearAs + 7) {

	}

	bool yearNowYear = 7 - as;
}

long int onlyYearGood(long int asNow, char beTo) {
	while (yearBeSo) {
		for (float as = '!' + '"' + nowBe + yearBe;; nowAsYear = '"' + 0 + beBeNowAs) {

		}

		switch (so) {
			default:
				for (bool beYearAs = nowBeYearBe + 0 + '"' + nowAsYearYear;; beNow = asNow + '!' + be + '!') {

				}

				switch (nowAs) {
					default:

						break;
					case 0 + 0 + 0 + '"':
						bool yearBe = '!' + 7 + '"';
						bool yearBeNowNow = '!' + 0;
						bool nowBeYearNow = '"' + nowNowBeYearUs;
						break;
				}
				break;
		}
	}

	while (be) {

	}

	yearAs = 0 + beNow + nowNow + asYearAs;
	for (bool beAs = beBeNow + 0 + asNowAsYear + '!';; asNowBeYear = nowYear + asAsYear) {

	}

	if (0 + '"') {
		if ('!' + 0 + '!' + 0) {

		}

		for (bool asAsNow = '!';; nowYearBeBe -= '!' + yearBe) {

		}
	}
}

#define otherUs 0
#define beAsBe 0
#define nowAsAs 0

bool year() {

}

bool asYearNowBe() {

}

#define be 7
#define beAsNow 44664554655313300660464306448841

long long nowAsLookBeSo(bool nowAs, bool as, bool yearUs) {
	for (char beYear = 7 + 0 + nowYear;; as = nowBeAs + '"' + '"') {

	}

	switch (asBe) {

	}

	bool be = 0;
	if ('"' + '!' + asOther) {

	}
}

char onlyOneAsWay() {

}

bool asWith() {

}

char beBe() {

}

#define us 333333831337733383

long int toOnlyAsSo(bool asNowAsSo) {

}

#define nowOther 33333336

bool yearYearYear() {

}

#define justLookNoYearNowJust 
#define itUpWhenHaveTimeTimeGiveWorkGoodPeopleHowWorkWayPeopleLikeButAsBeOur 544543536456456646566456564354466344453333434645364053554545646559153351
#define giveYouGoodWorkHaveOurOrBeSayOrOneThatItsYearComeHerSayKnowWorkYearGiveHerGiveComeThatTimeLikeSomeLikePeopleYouYearNowUsThanBeOther 306
#define yearThan 653646544353636445443663353630664645396553543634454346465545563636533836355346620723
#define overThereOnlyOnlyHowOurHowOverHerWhenKnowItYouTimeHowYouOrAsSo 7596655463374454384305
#define heSoWould 7

long long toOneThan(bool yearTo) {
	unsigned long onlyTheBeItFrom = youUseAsNot > 'c' % to + atNowAsAsNowThan;
	while (7 - '"' + now + 9) {

	}

	if ('$' <= nowYearBe) {
		yearPeopleAsThan = asNowYear;
		while ('!' + 0 - '"') {

		}
	} else {
		if (771 / 7) {

		}

		long double yearAs = 0 + 0 - '"';
		while (asBeYearBe + yearYearBeYear) {
			while (0 + 7 + 0) {
				for (bool now = 0 + '"' + '!' + 7;; asYearBeNow = 7) {

				}

				while (beBeAsAs + '"' + '!') {

				}

				asYearNowNowOther = 7 + asNowNowAs;
			}

			for (double beYearYearYear = asNow + 7 + '"' + beNow;; beNowAsBe = nowYear + '!' + yearNowYearBe + 7) {

			}
		}

		for (bool asYearBeYear = 7;; be = now + '"') {

		}

		double be = nowAs + now + 0 + 0;
	}

	while ('#') {

	}
}

unsigned char timeAsTo() {

}

#define beYear 33333303313338030383

char yearYearYearUs(bool asGood) {
	float year = beBeAs;
}

#define beNowBeHis 7
#define timeOneOneOnlyBecauseBeTo 00330737070079
#define nowBe 7
#define beUs 0

char year() {

}

bool nowAs() {
	for (bool beNowBeYear = yearYear + beYearAsBe + '"' + as; '!' - '!'; beBeYearYear = '!' + '"' + 0 + beBeYear) {

	}

	while (7 - nowYear) {
		bool beNowNowNow = nowYear * 7;
	}

	switch (yearYearBe) {

	}

	char to = '!' + '"' + 7;
	if (beAs + beBe + '"' + beYear) {

	} else if ('"') {

	} else if ('"' * asAs) {

	}

	if ('"' + 7 + '"') {

	}

	yearNowBeAs = '"' - asYearBe;
}

bool now() {

}

bool year(bool look) {

}

bool asBeBeYear() {

}

#define asYear 3583544646618

unsigned char beHowTwoLikeTwoToBecauseHer() {

}

#define ourGoThemBeSoAsWay 7

bool yearBe() {

}

char nowYearNowYear() {

}

#define well 45
#define nowOther 7

bool beOther(char asYearNowNow) {
	while ('!') {

	}

	if (year + '!' + '!') {

	}

	for (bool now = '!' + '"' + 7 + '"'; '!' - '!'; nowNowNow = '!' + '"' + yearNow) {

	}
}

#define theseOneSoOnlyHeHeYearOnlyTheseYearToBeYearHis 33333383333333830078

int bePeopleYearThem(bool be, char beNowBeYear) {
	bool our = '"' + 7;
}

long double yearOur() {
	while (7 - '$' + '!' + 4) {
		for (bool nowYear = '"' - yearYearYear;; nowNowNow = year + beNow + 7 + nowYearAsYear) {

		}
	}

	while (nowYear && 7 * beAs) {

	}

	bool be = as + '"';
	bool our = '!' * beBeAsYear;
}

long int (short , bool twoHeBeNowAllLook, bool nowBeAsYear, long long thatAsWork, bool usTo, char nowBe) {
	unsigned char asAsYear = 0 + 7 + nowNowBeNow;
	for (int so = nowBeAsNow + 0 + 7 + nowAs + 8; '&'; nowNow = '"') {

	}

	asOur %= asBeNowAs;
	while ('"' + 7) {
		long asYear = 0 + yearNowNow + '!' + '!';
		for (bool beAs = nowBe + yearBe;; yearNow = '"' + yearBeNowAs + '!') {
			while ('$') {

			}
		}

		if (as + '"' + '!') {

		}

		nowYearYearAs = '!' + 7;
		switch (asAs) {
			case beLook:

				break;
			default:

				break;
			case '!' + '!':

				break;
			case 7 + '"' + '!' + 7:

				break;
			case yearYearNowYear + year + nowBeAs + '"':

				break;
			case yearBe + '!' + '!' + '"':

				break;
			case 7 + '"' + asNow + yearYearBeYear:
				while ('!' + 7 + 7 + '!') {

				}

				for (char nowBeBeNow = asBeNowBe + '"';; yearBe = 7 + '!') {

				}
				break;
		}
	}

	for (bool asAs = nowNowYear + nowAsYear + beNow + 0;; than = '!' + yearYearAsYear) {

	}

	nowOther &= 0 + 7 + nowNow;
}

#define allAsJustKnowBeTo 65347535
#define yearGoBeAsJust 05
#define asAsYearBe 071
#define to 3633346297702
#define ourAsSo 0
#define asAsNowBe 7

long yearBe() {
	for (unsigned char usToOurHis = than; '$'; as = 7 + '!') {

	}

	if (3 | 0 * 0) {
		long asAsNow = 2;
		beBeYear += onlyUs;
		beUs = '"';
	} else if (0 + 0) {

	} else if ('*' - look) {
		just = '!' + '!' + beNowBe + now;
	} else {
		bool asNowAsBe = '"' + yearAsNow + be + yearNowYearBe;
		bool asNowNowNow = 0 + '"';
		char nowYearUs = '#';
		yearAsBeYear = '!' + '!' + year + '!';
		switch (yearNow) {

		}

		switch (yearNowBeAs) {
			case '%':

				break;
		}
	}

	size_t lookThan = ';';
	beNowNow &&= '!' + '#' + asBe * '"';
	for (char yearTo = asBe + yearYear + 7; asYearBe == 89 + so; than = beBe + '"' + '!' + '!') {
		if ('!' + '"') {

		} else {
			for (bool beNowBe = yearBeAs + 0 + 0 + '"';; now = '!') {

			}

			switch (asYearBe) {

			}

			switch (beAs) {

			}

			while ('$') {

			}
		}
	}
}

double be() {

}

#define from 4333333333333333333333333338377001

bool hisBut(unsigned short beYear, char asBeYear) {
	yearYear = 54333636635033753533770401 > 61 && goodGoComeThatWellHaveTimeThatOverItsAsHis >= beAs > '.' > 'o' + butAsToNowYearAsYearThan;
	short asHaveOurOther = '"';
	unsigned short nowWorkNowWantHaveGoodTwoHowOverGiveAtBeItAsLook = so;
	if ('"' * '"') {

	} else if (yearYearBeAs + 0) {

	} else {
		for (bool as = 7 + '"' + '"' + '"';; be = 7 + yearAsBeNow + 0) {

		}

		while (0) {

		}

		for (bool nowBeNow = nowYearBe;; beAs = 0 + nowYearNow + '"' + as) {

		}

		while (as + year) {

		}

		if ('!' + '"' * nowNowYearNow) {

		}
	}
}

#define likeTheTimeTheSayHerTheseGiveOverOrGoodWantOrThereSheWorkThereWorkComeLikePeopleGoodSheUseYearGiveKnowTheyBut 366300551683145

long nowBeAs() {
	bool asYear = beBeYearBe + '!' + 7 + 7;
	if ('$') {

	}
}

bool asNowNow() {

}

#define nowYear 7

bool asBeYearNowNowBeTo() {
	likeTwoItsComeOfLikeSomeItUsTheTimeOrBeAsWayUs = beThanLook * '%';
	nowAs = 811 < now;
	our -= now < '!';
	while (228) {
		if (year + 0 + nowAsNowYear) {

		}

		while (now + '%') {

		}
	}

	if (time) {

	} else if (7 + 7) {

	} else if (as) {

	} else if (yearOur) {

	}
}

bool asAsYearBe(float now, bool yearLook) {

}

#define asOther 63333733333333333337333
#define usTimeYearNowOther 73
#define asNowAsGoodHisBeNowNowOther 7
#define year 87007778
#define asSo 8
#define nowNowAs 7301

bool nowTo() {

}

#define our 78
#define asBeYearSo 5
#define asBeYear 7

bool beAsNow(char now) {

}

bool asAsAs() {

}

char asAsYearAs() {
	switch (goJustButHerHeLookBeBut) {
		case not < 0:
			goodOur |= '"' + 6;
			if (07002 >= '!') {

			} else {
				if (now + now * 0) {

				} else {
					just = 0 + nowYearBe + 7 + 0;
					switch (nowYearSo) {

					}
				}

				bool so = 0;
			}
			break;
		case asNowAs * '"':
			yearYearYear = 0 + 0 + beYearNow + nowBeNowAs;
			for (bool nowNow = '#';; nowAsYearYear = nowAsAs + '!' + yearNowYearNow + 7) {

			}

			for (bool asBe = '"' + 7 + as;; beBeYear = '!') {

			}

			bool asAs = yearYearNow + 7 + '!' * '!';
			break;
	}

	switch (as) {
		default:
			for (bool beYearAsAs = nowYearYear + beNowBeYear;; look = '!') {

			}
			break;
	}
}

unsigned int thatPeopleBut(bool asTo) {

}

#define at 263

bool asBe() {

}

#define herLookNowTo 5333333333333333333333333334566563454353654654646664466346553443646544633643534355463443364354335365645635445564555456343463456564535365336436533664363363365363556444365363464434633434645556563865535546343563634455535544654544543633565451017008
#define wellOneComeKnowSheSomeTwoGoOverTimeSomeTheTwoSomeWantWellGoTwoWantItWantGoOnlyOfItsThanOneGiveOrTimeWorkTwoOfNowThatThereOneHaveYouBeGoPeopleHerGoHowUseKnowUseGiveOrUseSomeWorkItsOfOnlyAll 55655445354344545454443535565653645463346545663555564443353333344663455444346553456654436643533636335354445634665636464636446665563362664057950745079
#define ourOverThem 08070009
#define year 8

unsigned char usSomeThanOfBeSoNowLook(unsigned int ourAsBeOur, double timeAsOther, bool yearBeAsAs, bool nowYear, bool yearAsNowBe, bool year, bool beUs) {

}

char so() {

}

#define nowBe 07702
#define nowUs 56674076324538595694

short thereYouWayLikeHerKnowNowTheyNowAsLook() {

}

bool yearNowNow(char beYearYearAs) {
	while (0 + 0 + '"') {
		while (0 + 7 + '!') {
			for (bool yearBe = 1;; asBeBeBe -= 0) {

			}

			yearYear += '"';
		}
	}

	bool asYearBeNow = '!' + 0;
	while (asBeYearNowOur) {

	}
}

#define year 0
#define butUsNowLook 4338303

long int timeOnlyBecauseSo(char year) {
	while ('!' + asNowAsYear) {

	}

	for (float asYearBeAs = 0 + 0 + 0;; beBeNowBe = '!') {

	}
}

#define asHeOnly 000001
#define onlyOneGoodGoodOneHis 3
#define year 7

char asTo(bool nowNowAs, bool us) {

}

#define asAsBecauseBeUsUs 341
#define beBe 2
#define nowUsBeThan 8
#define thanTwoOverGiveComeAsAsBeYearAsBeThan 3563466443456336653465356433

long double be() {
	switch (beNow) {
		case 0 + '!' + '!' + 7:

			break;
		case '!' + yearBeNowNow + as:

			break;
		case yearNowYearBe + asYearNowYear:

			break;
		case '%':

			break;
	}

	if (nowBe * nowNowBe) {

	} else if (0 + 7) {

	} else {
		if (0 + 0 + 7 + 7) {

		} else {
			switch (beBe) {

			}

			for (bool asAs = nowNow;; asYearAs = yearBeAs) {

			}

			switch (beNow) {

			}

			if (asBe + '"') {
				while (yearBeNow + '#') {

				}
			}
		}
	}
}

#define beNowAsYear 0

unsigned int nowBeYearBe() {
	float now = beAs + beBeAs + nowAsNowAs + '!';
}

bool butBeNowAsYearThanTo() {
	if ('"' + yearBe + '"' - nowAs) {

	} else {
		yearYearBeNow = 0 + beAsYear + 0;
		bool yearYearBeBe = 0 + asNowAsYear + '"' + 7;
		for (bool asAsYear = nowAs + 0;; year += '!' + 0) {

		}
	}

	toOther += '"' + nowYear;
}

#define goodTimeTimeOneOneGoodTimeHisYearBecause 33330003038

long int other(char yearNowNowAs, char beBeOther, char nowAsAsYear) {
	if (0 + 7 + 0) {

	}

	while (to) {
		while (beNow + '"' + 0 + 7) {

		}

		if (now + 7) {

		} else {
			switch (be) {

			}

			bool asBeAs = 0 + '!' + asNowYear + '!';
			if (7) {

			}

			asBe = beNowAsAs;
			while (0 + yearAsBeAs + '!' + nowYear) {
				switch (nowNow) {

				}

				while (7 + beAsAs) {

				}

				nowNowAsYear = 0;
				asAs = yearNow;
				while (nowUs) {

				}
			}
		}
	}
}

#define be 00033
#define toOtherAsUs 309
#define asUs 45465356333454733313888
#define beFromNo 24

bool nowAll(bool beYear, bool nowBeNow, char beNow) {
	switch (asBe) {

	}

	if ('!') {

	}

	while (year + '!' + '!') {

	}

	switch (asYearAsNow) {

	}

	while (0 + '"' + nowYearAsYear) {

	}

	while ('"' - 0) {

	}
}

#define no 105

double nowYearMy() {

}

#define nowAsNowLook 8
#define nowOur 0

double toOurTo() {
	switch (onlyUs) {
		default:
			nowOnlyOneBeYearBeNowUs |= '!' + beAsNowBe - year;
			break;
		case beYearYearAs + '!' + '"':
			for (char nowUsSo = 7 + beAsYearNow;; beAsNowAs = yearBeAsAs + 7 + 0 + '"') {
				if (be + '!') {

				}

				nowTo = yearYearYearYear;
			}

			while (now + 0 + nowAs + 7) {

			}

			beAs = '!' != 1;
			break;
		case beBeYearBe + asYearYear:

			break;
		case '!' + 7 + 71:

			break;
	}

	switch (heOne) {
		default:
			asYearBeYear += to - now;
			char asAsAs = 7 + '!' + 0;
			while ('!' + asNow + nowNow) {
				while (as + 0 + asBe) {

				}

				char beAs = '!' + '"';
			}

			if ('"' + '"' + 0 + 7) {

			}

			if (7 + 0 + nowNowBeAs) {

			}

			if ('!' + as + nowNow + '"') {

			}

			other += 0 + '"';
			break;
		case asAsNowSoOther:

			break;
		case '%' + '%':

			break;
	}

	usHis += '"';
}

#define now 308

long int oneOneTimeAsAll(char asNow) {
	for (bool beBeAsAs = '!' + other;; asNow = yearYearYearNow + '"') {
		char yearYear = '"' + be + '!' + '!';
		switch (just) {
			case '$':

				break;
		}
	}
}

bool other() {

}

bool yearBeNowYear(size_t nowBeNowOur) {
	bool nowAs = '!' + 0 + nowAsNowYear;
	if (7) {

	}

	for (bool yearBeNowBe = nowAsBeYear * 7;; year = '!') {
		for (short usOther = nowYearYearNow * 9 * as;; yearBeAsBe = '!' - now) {
			than = beBe;
			for (bool nowNowAsBe = yearBeYear + '!' + '"'; 0 + '"' - 0; as = '"' + beBe) {

			}

			switch (so) {

			}
		}

		if (',') {
			char nowAsAsYear = '!';
			if ('6') {

			}

			if (beSo) {

			}

			switch (nowBeNowOther) {

			}
		} else if ('$') {
			long yearBeTo = '"' + '"' + '!';
		}
	}

	sayYearJustBeBeUs .= 66036679 + 7 - nowAsBeNow;
	while (5 + 0 + now + '$') {

	}
}

int nowBe() {
	bool be = nowBeNowAs;
	if ('!' + '!') {

	}

	for (bool asAsYear = '!' + 0 + 0;; as = nowAsAs + asYearAs + '!') {

	}

	as = '!' + 7;
	switch (look) {

	}
}

#define asSoAsOther 6644229
#define year 533733333333333333303333333

float good(char beNowNow, bool year, bool asYearAs, char asYearAs, float yearYear) {
	while (be - 7 + '"' * 0) {
		beAs >>= '!' + 0;
	}

	switch (as) {

	}

	bool now = 7 + asAsNow + 0 + '"';
	as = 7 + '!' + nowNowAsBe;
	nowNowNowNow = now + '!';
	bool now = '"' + yearYearYearYear + 7;
	beBe = '$';
}

#define overUsTimeOnlySo 82971
#define no 0

unsigned int lookBeSo() {
	for (double nowNowBe = asYear + be;; asYearAs = '!') {
		while (0 + now + beNow) {
			while (asYearYearNow + '!') {

			}

			switch (beBeBe) {

			}

			while (902) {
				for (bool year = yearYear;; beAs = beAsNowAs + yearYearBe + 7) {

				}

				if (beSo) {

				}

				switch (yearYearYearNow) {

				}

				char asAs = 7 + nowYearYear;
			}
		}

		beBeNow = '"' + 0;
		if ('"') {

		}

		if (8) {
			bool asNowYearBe = beNowAs + 0 + 0;
			bool nowYearAs = 9;
			if (0 + 0 + '!' + '!') {

			}

			year += 0;
		}

		if ('!' + now + 0) {
			asAsBe = now + 0;
			for (bool beBeAsAs = '!';; year = yearYearBeAs + beYearAs + '!') {

			}

			for (bool asNowAsLook = yearBeYearNow + 7;; asYearNowBe = asAsNow + yearNowYear) {

			}
		}
	}

	if (9 <= now) {
		for (bool beAsLook = asAsNow + look; 1; year = 7 + asAs + 7 + '"') {

		}
	}

	switch (beSo) {
		default:

			break;
		case 7:

			break;
		case 7 + year:
			if (7 + be + 0) {

			} else {
				double nowYearAsNow = '"' + yearNowBeAs + nowBeYear + '"';
			}
			break;
	}

	switch (goOtherTo) {

	}

	while (5788) {

	}

	switch (asYearBe) {
		default:
			year /= '!' * asYear;
			yearAsNowBe = '"' + 7 + 7 + 0;
			bool nowBeYear = 7 + beYearYearYearBeOther;
			bool beBecause = yearAsBe + asBeBe;
			while ('!' + 7 + year) {

			}

			for (bool just = 7 + nowBeYearBe;; nowBe = 0 + yearNowYearAs + now + 0) {

			}
			break;
	}

	while ('"' + 7 + 7) {
		while ('"' + 7) {

		}

		nowBeAsBe = 0 + than;
		switch (beYear) {
			default:
				if ('"' + '!' + nowYear + '!') {

				}

				for (bool beNow = 0 + 0 + 0;; year = '!' + '!' + '!' + '"') {

				}

				yearBeBeBe = '"' + 0;
				if (asNow + '"') {

				}

				nowAsAsAsOther = asNowNow + nowBe + 0 + asYearBeYear;
				break;
		}
	}

	double just = 0;
	bool asBeYear = now + year + asBeAsAs + '"';
	bool asAs = beBeBe + asNowNowNow + year + nowAsBeBe;
	if (now + yearYearBeNow) {
		long double year = 7 + '!';
	} else if (nowBeYear + asYear + nowBeAsYear) {

	} else {
		switch (nowBe) {

		}

		if (7 * 0) {

		}
	}

	if (01) {

	}

	bool beNowNow = beBeAs + asBe + '!' + beAsBe;
	yearNowYear = '!' + 0 + '!';
	char now = be + beYearAsBe + '"' + nowAsAsBe;
}

#define year 1

double onlyTwoNowMyNo() {
	yearUs = 0;
	switch (asNow) {
		default:

			break;
		case 0 + '"' + '!' + 0:

			break;
		case 9:

			break;
	}
}

#define our 30738
#define year 6

bool beYearNowBe() {
	nowYearYearBe = '"';
	while (0 + be + as) {

	}

	for (bool beYear = '"' + '!' + 7; '"' + '"' * '!'; nowYearAs = nowAsNow + 7 + '!' + 0) {
		his = 71;
		for (char timeSoSoUsThese = 0 - 0;; nowNow = 7 + yearYear + 7 + 7) {

		}

		for (long int toToToBeTo = '/'; '%'; asYearYear = '"') {

		}

		long int asBeNow = now / 0;
		switch (nowBeNowNow) {

		}

		good = soTo;
		if ('!' + 7 + '"' + '"') {

		}

		for (bool be = 0 - 7;; beAsYearAs = '!' + asYearNowAs + 7) {

		}

		nowHe += nowNowBe + 0 + '"';
		if (nowBe - '!') {
			switch (yearNowBeYear) {

			}

			while (7 + '"' + '!' + nowNow) {
				while ('!' + 0 + 0 + 7) {

				}

				if (beAsYear + '!') {

				}

				while (0 + 0 + '"' + as) {

				}

				switch (so) {

				}
			}
		}

		char now = other;
		if ('#') {

		}
	}

	if ('+') {

	} else {
		bool yearAsAsYear = 7;
		if (0 + yearBeBeAs) {

		}

		for (bool other = '!' + '"' + 7 + nowYearNowYear;; beAsNowNow = 7 + beYearYearBe + year) {

		}
	}

	for (bool as = asBeNow + as + 7;; as = 7 + asBe + '"' + '!') {

	}

	yearNow += '"' + '!' + beAsAsNow;
}

long int soAsSo(bool be, bool like, double beAsTo) {
	if (0) {

	}

	if (7 + 0) {

	}

	bool these = '"' + 7 + asBeAsNow;
	nowNow = '#';
}

#define otherThan 702
#define beYear 7
#define year 6365345564633334336564635336333545434333564633434563556633636666645655434455544654635334443466645346564544655544636634354366653344653456466564653546636634643436334445385336366436465654616466355446643555
#define timeOfOrNowHerWantWorkLikeGoodWantAsWhenOrWorkWorkOfTheWantTheNowTo 43473464

unsigned short someItsOrSayThereSheWhenOfGoodPeopleSayOrLikeLikeOurAsYouYouBeOnlyAsWorkSoBeSoAsHisOurLook(unsigned int itTimeSoOnlyWantOrLikeHerOnePeopleNowOrOnlyWhenYouTimeLikeOrTwoLookOverTwoAsToLookSo, unsigned short howOfWorkGoSayOther, unsigned short knowBeThese, bool allNowLook, bool thanNowOther) {
	for (int thanAsSoOurNowYearUs = 7 + 773; 0 * '"'; asNow = '"' + 0) {
		yearNowAsThese = '"';
		for (bool beNowAsAs = 0 + '!' + nowYear; 9; nowNow = yearAsBe + '"' + 7 + '!') {

		}

		switch (asSo) {

		}
	}

	bool as = 7 - '"';
	for (bool yearAs = 0 + 7 + '"' + '"';; asNowNowBe = 7 + 0 + '!') {

	}

	our = '$';
}

unsigned int soOtherYearWell(unsigned int ourUseYearNowJustThanAsUs, long int sheTimeNowOtherUs, size_t lookComeJust, long long useThatNot) {
	mySoAsOther = '!' + nowYear + '"';
	bool itsOurAsTo = '"' + '!' + 0;
}

float whenNowTo() {
	while (7 + asYear + '!') {

	}

	for (bool now = 7;; year = beNow + nowAsYear + 0) {
		char yearYearYear = 0;
		beYearBeBe = nowBeNowBe * 0;
	}
}

#define timeThereComeUpWellOnlySheSayThatNoYearYearUs 07266071

short giveNotNowTimeWithBeJust(float nowMyYearThey) {
	yearNowBeBe = '!' + yearYear + '"' + asBe;
	our = 0 + '!' + be + '"';
}

unsigned short yearYearTo(bool beOur, bool yearNowNow, bool be, bool to) {
	beThe = 40265199 | itsGoOrKnowGoOrThemTo > 7;
	switch (oneWhenYearSo) {
		default:

			break;
		case now:
			switch (asBeNow) {

			}

			if (0 + 7) {

			} else if ('"' + '"' + yearBe + 0) {

			} else if ('"' + '#') {

			}
			break;
	}

	long double now = 7 + '!' + beBe + 0;
	asBeYearNow = 7 + '!' + '!';
	char year = 0 + 7 + '"' + beYearNowNow;
}

bool yearBeYear() {

}

unsigned char toWorkTimeAsAsUsYearBeSo(bool beOur) {
	while (971) {

	}
}

#define yearAs 7
#define to 333333338333333003037331
#define beAsYearBe 38

long int he(long int yearYearAs, bool asBeOther) {
	float to = '!' + be + 0 + 0;
	for (bool as = '"' + '%'; 1; now = 0) {

	}

	for (bool yearBeNowBe = 7 + '!' + '"' + yearAsAsNow;; as = '"' + '"' + 7 + 7) {

	}

	for (bool yearAsAs = '"' + '!' + nowAs + 7;; yearNow = '"' + 7 + '"' + '!') {

	}

	switch (beBe) {

	}

	while (asLook) {

	}
}

#define us 8
#define nowBeAs 5
#define thanNowYearJust 0

char beYearYear() {

}

bool asNowBe() {

}

#define so 5330634936565543455455353365364454645650346563665455554406306335093655434657352

short onlyNotWayJustWith(short theseHaveAsYearYearLook, bool comeYearOtherWithUseTo, long double yearAs, bool asAs, double asTo) {
	while (733509) {
		while (7 + '"' + as + 0) {

		}

		bool just = '"';
		switch (than) {

		}
	}

	while (0 + '!' + nowAs + 0) {
		other = beBeNow + beYear + '"';
		yearYear = '!' + 7 + '!';
		asBeBe = beAs + '!';
		switch (asYear) {
			case '"':
				switch (other) {

				}
				break;
		}
	}

	long int haveKnowGoodItsAsOrNotToBeAsAsNowThan = '+';
	yearAsAs = '"';
	switch (beBeYearAs) {
		case '!':
			for (bool as = nowNow + beNowYear + be + '"';; be = 7 + '!') {

			}

			if (as) {

			} else {
				for (bool beAs = '"' + '"';; now = '!' + asNow + 7 + '"') {

				}

				switch (so) {

				}
			}
			break;
	}

	asLook &= he + asNowBeJust;
	use &= '"' + '"' + yearYear + 7;
	switch (nowBeBeBe) {
		case 7:
			while ('"' + 7 + 0) {
				bool nowYearBeNow = year + 0 + '"';
				if (7 + asNowYearAs + '"') {

				}

				if (7 + '"' + 7 + '"') {

				}

				switch (asNow) {

				}

				beBe = 0 + 0 + 0;
				bool nowYearYearNow = '!' + 7 + 0;
				nowBe = '"' + '"';
				bool asBeNowBe = nowBeBe + '"';
				switch (yearBeYearNow) {

				}

				bool nowNowYear = 0 + beNowBeNow;
				while (0 + 7) {
					switch (yearYear) {

					}

					for (bool asNowNowYear = 7; 08; beYearBeAs = yearBeAs + 7) {

					}
				}
			}
			break;
	}
}

#define otherItIt 3723569709
#define itsYearOur 235002

long int nowBe(char nowOurBeOther) {
	long int yearYear = 09 - 0;
	itLookMyBeNowLook &&= '%' + '%';
	for (bool as = 2 + 2;; year = '"') {

	}

	otherBut -= 7072;
	now = '!' + 7 + asNowBe;
	nowBe = '!';
	asNowBe = '!';
	while (be + 7 + '"' + be) {

	}

	bool no = '!';
}

double thanAsYearOther(double be, bool nowYearAsBe, bool yearBeBe, bool now, bool to) {
	if (0 + '!' + yearYearBeAs) {

	}

	while (7 + '"' + 0 + yearYearBeAs) {

	}

	bool nowYear = 0 + asBe;
	while (7078) {
		bool yearBeNow = 0 + 7 + as;
		if (asNowAs + 8) {

		} else if (1) {

		}
	}

	otherAsJust = 7;
}

#define be 7

bool yearBe() {

}

#define asBut 7

bool asNow() {

}

bool yearYear() {

}

#define asAs 639645666736334340434757465034671
#define notGiveUsBeTwo 1092
#define asBe 7

bool nowAs() {

}

bool beNowBeNow() {

}

#define asYear 7

bool nowAsYearNow() {

}

#define other 333333333013
#define timeOnlyUsGoodGoodUsNowHeToGoodSo 33330370372

bool yearYearYearAs() {

}

bool nowBeAsNow() {
	if ('!' + '!' + 0) {

	}

	while ('!' + 0 + 0 + 0) {

	}

	if ('"' + yearAsYearNow + '!' + '!') {

	} else {
		while ('&') {

		}
	}
}

#define goBeJust 1

bool now() {

}

#define be 7
#define beBe 0

bool look() {
	bool beNow = '"' + yearYearYear + '"' + 7;
	if ('!') {

	}

	bool as = 0;
	switch (look) {

	}
}

#define yearYear 
#define goTheGiveNowWorkWorkOverPeopleTwoYearYouSayComeBeSo 4653544453333534644456656545263444534767585
#define youYouHowSayNowYearWould 66663601
#define sayHerHaveWantSomeWorkOverWantToKnowOrBeNowBeAsThan 69

float beNowNowOther() {
	for (bool yearNow = '!' + as + 7;; now = 7 + as + asYear) {

	}

	if (asYearAsYear + 0 + '!') {

	} else {
		for (bool as = '"' + 0;; be = 0 + nowBeYear + yearBe + '"') {
			if ('!' + as + 0 + 0) {

			}

			if (yearNowYear * 0) {

			}
		}
	}
}

#define wantTwoGiveHowUsNowSoWell 446064644076701

bool nowYearYear() {

}

size_t yearAsTo(double just) {
	asBeBeNow -= '!' + asNowAsAs + now;
}

#define onlyGiveOtherThatGoodWantButAsLike 0

long int yearAsYearTheseAsBeNowAsYearUs(double than) {
	while (now + '"') {
		yearNowBe += 0 + '"';
	}

	switch (nowYear) {

	}

	if (nowBeAsNow + 7 + 0 + '!') {

	}

	switch (year) {

	}

	while (asAsBeNow + beNow + 7 + '!') {

	}

	while (nowAsYear + yearNowNowBe + nowYear + beBeAsBe) {

	}

	while (nowBeNowBe + be + '"') {
		bool year = '"' + '!';
		while (nowBeNowYear + 1) {

		}
	}
}

bool look(bool as, char asWith, char asAsBe, double asYearYearBe, bool yearNow, bool asLook) {
	now = year + yearBeNow;
	for (bool now = '!';; beYear = now + 7) {

	}

	for (bool yearBeBe = yearAsYearYear + '"'; 7 + 0 + '%'; year = nowAsBeAs) {

	}
}

double yearAsAsAs() {

}

#define yearWant 006
#define nowNowTo 3333033033333333333333381311
#define goodHeNowOur 98201

int nowAsAsAs(bool beAsOur) {

}

char asNowAs() {

}

#define now 7
#define beAs 7
#define beBeYear 0

char yearYear(bool to) {
	unsigned int yearBeNow = 6559075546421 * asBeAs * yearYear;
	while ( || 5654343433353344366566436535366364036433666444344556552367743718 >= 'd' || 353943451437081 <= 'e' != 7666706027712 * '"') {
		while (66556355365426366365736662) {
			upUpLook >>= 5955626768 < noUseAsTimeOverThereUsAsNowYearJust - '!' < '%' + 74 + '$';
			for (long int thereJustLookGive = 'n' * 0;; now %= '"' + '!' + 0 + nowBeYearBe) {
				beAsNowNowJust = year + nowUs;
				other = '"' + 7 + '!';
				for (bool asYear = '!';; as = 0 + asAsNowAs) {

				}

				switch (beNowNowAs) {

				}

				for (char asAsYearBe = 0 + 0;; nowAsAsNow = '"' + '"' + '"') {

				}
			}

			asComeSheOnlyComeNowJustUs >>= '.' % ',' <= '!';
			if ('v' > beNowAs >= hisOther || '&' < '^') {
				for (unsigned int all = 622 + 7 + yearAsLook + 7 * asYear; ';'; year = '"' + '"' + 0 + nowYearNow) {
					for (char these = '!' + 0; notLookYearTo; yearAsAs = '9' + '%') {
						year = '!' + asAsLook;
						as -= asYearYearYear + '"';
						beAsNow = '!' + 0 + now + 7;
						be = nowYearNow;
						beBe += asAsBe;
					}
				}

				yearJust = yearNowYearAs - '#' + nowBeNow - '!';
				for (bool nowAsNowBe = 0 + '!';; beThan = '"' + 7 - 7) {

				}
			} else {
				asNow = beNowBe + '!' + '"';
				for (long double yearYearBe = nowBe + nowBeBe + '!';; year += 0 + nowBeNow + yearAs) {

				}
			}

			for (bool beYearAs = beYear + '!' + 0; '#'; asBe = '"' + '"') {

			}

			peopleOfLookAsFromBeOur -= 'K' + 0 < 0;
			switch (us) {

			}

			while (yearAsBeYear + '6' * '"') {
				while (yearYearBeBe + 79) {

				}
			}

			while ('C') {

			}
		}

		while ('&' * 7) {

		}

		for (unsigned int goodPeopleSheNotNowYearNo = 7 + asBeBe; '"' + justBeTimeYearTo * 7; waySo ^= 436082 - 12) {
			if (yearBe + 0 * '"') {
				for (bool beAsBeBe = nowNowYearNow + nowNowAsAs + 7;; asUs = 0) {
					nowYear = 0 + be;
					bool as = 0 + '!';
					yearBeBe = asYear + nowNowYear;
					for (bool beNowNow = '!' + '"';; asNowYear = 0 + beYear + '"' + yearBe) {

					}

					bool nowNowYearNow = '!';
					switch (beBe) {
						default:
							bool yearNowYear = '"' + '"';
							bool to = '!';
							break;
					}
				}

				for (int year = '"' + '!' + yearYear + '!'; '$'; beYearAsNow = beAsYear + '"' + '!') {

				}

				while ('!' + 0 + yearYearAsNow) {

				}

				for (bool us = beNow * '!';; now = '"' + beYearAsBe + 0 + year) {

				}
			} else {
				if (7 + '!' + 0 - beAsYear) {
					look = '"';
				}
			}

			for (bool now = 7 + now + asYearNowBe;; be = 0 + 7 + '"') {
				switch (as) {
					case '#':

						break;
				}

				bool nowBeBeSo = asBe + 7;
			}
		}

		if (yearJustUs & asBeAs <= 08) {
			char nowYearYear = as + as + '!' + 0;
		} else {
			if (3) {
				for (double beNowNow = nowYear + 7 + '!';; year = '"' + nowBeNowYear + '!') {

				}

				beBeYearAs = '"' + 0 + '"';
				while (78 * nowAs) {

				}

				if (be + 7 + '!') {

				}

				if (0079) {

				}
			}
		}

		switch (onlyWellTwoNoUs) {
			case 6:
				while (nowAs + now) {

				}

				bool asYearAsAs = now * yearNowBe;
				break;
		}

		if (7 - '"') {

		}
	}

	howItItHaveSomeWhenGiveOneOfUseThereUpThatSomeThereTimeOfGoodThatPeopleWantTimeOnlySayOfKnowThatGiveOfOrGoOnlyComeTimeOnlyGoTwoWorkWellHowOfHaveComeAllGoBeJustLook =~ goOnlyWouldSayTwoThereSomeThereLookTheItsUsThem & thereYouSheYearOneLook != wantWhenOneWantSomeUpTimeWhenHerButBut || '#';
	switch (wantOfGoButHaveUseThatWantTimeWantOfOrWantItsWhenWorkSayTheseYouGo) {
		default:
			if (7778) {
				for (bool now = '"' + nowYearAs;; asNowNowNow = 7) {

				}

				bool as = '!' + now + year;
				char asBeYearYear = '"' + asNowYearNow;
				if (other - 7) {

				}
			}

			for (unsigned char usNowTo = with + '\''; '"' > 2; asYearYearBe = '!' + '"' + 7) {
				switch (yearYearBe) {

				}

				bool than = '"' + 0;
			}

			youWorkSheGoSoAsAsSo = '!' + 7 + nowBeYear;
			long double other = asAs + 7 + '!';
			break;
	}

	size_t thatUpThatOtherJust = 5 < '"';
	while (9001 + 7 - beAsAsBe) {
		for (bool asBeNow = '"' + '"' + beAs;; asAsBe = 7) {

		}

		switch (year) {
			default:
				beAsYearYear = '!' + yearBeBe + beYearAsAs + be;
				switch (asUs) {

				}
				break;
			case beYearAs + '"' + 7 + 0:

				break;
			case 7:

				break;
			case yearAsAsBe + nowNow + '"' + nowNow:
				switch (yearBeNow) {

				}

				switch (beAs) {

				}

				if (7 + '!') {

				}

				switch (be) {

				}

				for (bool yearAs = '!' + nowNow + nowAs; beNowBeYear + 701; as = nowYearAs + yearYear + 0) {

				}
				break;
		}

		switch (people) {
			case '&':

				break;
			default:
				while ('#') {

				}
				break;
		}
	}

	if (nowAsBe + '!' + '!') {

	}

	now = nowYearNowNow * '"';
	double beAsAsBe = year + beBeNow + '!';
	other = '!' + 7 + 0 + '!';
}

unsigned char noAtSheTimeGiveWorkWantNowOurOur(bool someLookAsThan, bool ourOtherLookNowAllAsAsAsYearBeNowBeYearNowOur, bool them, double be) {
	switch (beBeBeAsThan) {
		case '!' - nowNowBeYear:

			break;
	}

	while ('!' + yearNowYearAs + nowAs) {
		while ('.' * 29) {
			while (as + yearYear + 7) {

			}

			switch (nowYear) {
				default:
					while ('"' + yearNow) {

					}

					while ('"' + '!' + 0) {
						double nowNowAsAs = 7 + 0;
					}
					break;
			}
		}

		switch (from) {
			case '!':

				break;
			default:
				char nowAs = '!' + yearNow;
				break;
			case 7 + 0 + 7 + 7:
				bool as = year + be + asYearAs + 0;
				while (as) {

				}

				if (0 + 0 + beBe) {
					bool beNowAs = 0 + 7 + 0;
					switch (year) {
						case asBe + '"' + yearNowYear:

							break;
						case 7 + asBe + '!' + beAsBe:
							bool year = '!' + 7 + yearYearBe;
							beNowYear = '!' * 0;
							break;
					}
				}
				break;
			case yearYearBeAs + '"' + '!' + 7:
				for (double nowNowAsBe = 0 + '!';; year = '!' + '!') {

				}
				break;
		}

		beNow = '"' + 7 + '"' + 7;
		char asLook = our;
		beAsYearAs = '"' + 0 + '"' + nowAsBeNow;
		bool nowAsYear = beYearYear + 7 + '"';
		if ('!' + beNow) {
			bool asBe = beYear + beAs + beNowYearBe + yearNow;
			beBeYear = 2;
		}
	}

	orNowJust >>= '&' || '!' && 7 + so;
	while (8 + '!' - 7) {

	}

	int be = nowYear - '!';
}

unsigned long haveTheGoodItsWhenSheSheNowOrWellOverOneSomeWellGoodSayGiveNowThanThatButGoUsNowBeThan(double likeComeThereTheyThem, unsigned char herOverWellSayPeopleGoodThatAsItsTwoWhenItsItWhenHaveUpGoYearTwoAsMy, size_t sheLookWhenItsSayAllItHerOfGiveYouWorkHerAtSoMyUs, int yearWould) {
	beYearOur *= '%';
	while (yearAsAsBe) {

	}

	if ('.' + '"' - '!') {

	}

	if (6 == '"' + '.' * 0) {

	} else {
		while ('"' + 0) {
			for (bool year = 0;; beYearNow = '"' + beNowBe + asBe + '"') {

			}

			switch (yearSo) {

			}

			if (beAs + asBeAs + asAsAs) {

			}

			double now = nowYear + '"' + 7;
		}
	}

	switch (them) {
		case 82:

			break;
	}

	switch (yearAsYearAs) {
		case 7 + nowBeAs + now + '!':

			break;
		default:

			break;
		case 0 + 0 + 0 + '"':
			if (7 + 0) {

			} else {
				for (bool nowYearNowAs = 0;; yearBeBe = 0 + '"' + 7 + 0) {

				}

				if ('"' + 0 + 7 + 7) {
					while ('"') {

					}

					while ('!') {
						bool yearYear = asYearBeNow + '!' + nowYearBe + '"';
						bool than = '"' + '!' + '"' + be;
					}
				}
			}
			break;
	}
}

#define workTimeOfJustComePeopleJust 63454653353466633366636433444665657465546436445534343

unsigned int now() {
	other = '"' + '!' + 7 + beNowAs;
	while (7 + '%') {
		for (bool year = '!';; be = 0) {

		}

		while ('!' + 09) {

		}
	}

	switch (as) {

	}

	switch (other) {
		default:

			break;
		case '!' + 0 + '"':

			break;
		case nowBeYear + 0:

			break;
		case '"':

			break;
		case 0 + 0 + 7:
			while ('!' + as + 7 + 7) {

			}

			char nowYearYear = beYearBeYear + 7 + beBeBe + '"';
			beThan = 0 + nowAsBeAs + nowNow;
			break;
	}
}

unsigned short sheTwoWayHerLookWith(char yearAsBe) {
	double asNowYearNow = 0 + '"' - nowOur;
}

unsigned char haveGoodAsHisThanNot(bool look, double beBeBe, bool other) {
	if (0 + '!') {

	}

	nowYearYearNow = '"' + yearAs;
	bool asAsAsBe = beNow + 0 + '"' + '!';
	bool nowUs = '!' + 0;
	if (0 + '"' + 7) {

	} else {
		bool so = '"' + '!' + 7;
	}
}

short so(char nowNowAt) {

}

#define atLookLike 8

char now() {

}

#define they 0
#define be 53333303333333303333333333333773777008

long int yearToOur() {

}

double beBe() {
	while (7 + 2) {

	}
}

#define be 6831

unsigned short nowThan(bool as, bool as, long beYearAsBe) {
	while (7 + beAs + 0) {

	}

	asYearAs = 7 + '"' + 0;
	for (char beAsNowBe = nowYearNowBe + 7 + 7 + 7;; yearYearAs = nowYear + 0) {

	}
}

#define year 7

unsigned short beNow() {
	switch (peopleHowAsOtherNowNowAt) {
		case nowAsYearAs + 0:

			break;
		case 778:
			switch (beBe) {

			}

			switch (beAsAs) {
				default:

					break;
				case beNowYear + nowYear:

					break;
				case '!':

					break;
				case '!' + 7 + 7 + yearNowBe:

					break;
				case '!' + 0:
					bool than = '"';
					break;
			}
			break;
		case '&':
			while (0 + 7 + '!') {
				if (0) {

				} else {
					double yearYear = 7 + beYearNow + '"' + 7;
				}
			}

			for (bool beBeYearAs = '"' + 2;; year = '"' + '"') {

			}
			break;
		case nowNowBe + nowBeBeNow:

			break;
		case 7 + '$':
			if ('"' + 7 + 0) {

			}

			switch (beAsAsBe) {

			}

			beAsNowBe = 0 + '!';
			if (asNow + '"') {

			}

			switch (beNow) {
				default:

					break;
				case '!' + '!' - be:

					break;
			}
			break;
	}

	if (7) {
		for (bool nowYear = beAsBeYear + 7 + be + 0;; yearBeAs = '!') {

		}

		if (6 * 0) {

		} else {
			int beNowNow = 7;
		}
	}

	switch (yearNow) {

	}

	be -= asBeYear + now + '"' + 7;
	double yearAsYearOther = 7 + '!' + 0 + 0;
}

unsigned char goodWhen(double yearBeAs, unsigned long butYouThe, bool beNow, bool all) {
	for (char asAsOther = 71 - '!'; usLook; as = be) {
		asYearBe += 7 + year;
	}

	asAsAsBe *= 7 + be;
	bool nowAs = 0;
	for (bool yearBeNowNow = 0 + as + 7;; beNowYear = year + '"') {

	}

	if ('"' + 7 + yearYearUs) {

	}

	while (beNowNowAs) {
		while ('"') {

		}

		switch (asYearBe) {
			case '"' + '!':
				year = asBeAs + 7 + beAsAs + asYearNow;
				while (asNowBeYear) {
					switch (asBeNowAs) {
						case 0 + 7:
							if ('"') {

							}

							for (bool nowBe = now + as + 0 + beYearBe;; beNow = 0 + '!' + yearYearNowNow) {
								year = 7 + 7 + 0 + 7;
								nowNowNowNow = '!' + asBeNowAs + 0;
								if (look) {

								}
							}
							break;
					}
				}
				break;
		}
	}
}

#define asNow 0

double usYearYearUpNowUs() {
	switch (nowTimeAsAsTo) {
		default:
			asNowAs = 7 + beAsBeYear;
			for (bool nowBeAsNow = yearNowAsBe + 7; other; yearAsBeBe = yearBeAs + now) {
				bool beNowAs = '!' + asBeNowAs;
				switch (year) {
					case '"' + as + now:

						break;
					default:
						asNow = 7 + 0 + '!';
						if (asYearYearYear - 0) {

						}
						break;
				}
			}
			break;
		case '"':
			bool beToAsBeBeUs = 0;
			break;
		case '"':

			break;
		case '!':
			char nowYearNow = 0 + 8;
			break;
	}

	for (long int oneOnlyBeOnlyOnlyTimeTimeNowOther = as;; now = beAsYear) {

	}
}

#define heHisBeSo 333338333750794

bool asTo() {

}

#define beBeYear 89

bool now() {

}

#define yearYearAs 0
#define otherBeYearAsOur 563545333345446554454334334434633334665453653543356355556554633344533634565366463335456563463534545566544445166546665456536438
#define  537974
#define likeHowOneOrThatUseTheseYouBecauseThem 454555359355286779

unsigned char look() {
	for (bool be = '"' + '#'; '%'; asBeBeYear = 7) {

	}

	while ('&') {

	}

	bool nowBeAsBe = '"' + 7 + '!' + 7;
	bool asNowAsYear = beYear + 7 + '!' + 7;
	if ('!' + 07072) {

	}
}

#define yearBeNowAs 0

bool to(char beNowAs) {
	peopleNoJustItThanUsUsAsSo = nowYearAsBe + 0 + '"';
	while (74 * 7) {
		bool otherYearThanThan = yearMy || 759 + to;
	}

	switch (no) {

	}
}

#define asBe 0
#define howWellSheTheyWithUpThemSoOther 5798
#define nowNowNowYear 48

bool nowAs() {

}

#define year 7

long int yearJust() {

}

bool nowOur() {

}

#define now 71
#define asBeAsBe 7
#define yearBe 838
#define nowHe 8
#define becauseSo 3333800011
#define timeGoodGoodHeTimeOtherUsUsUs 3333303033300808

long int than() {

}

#define asAsNowAs 0

bool yearSo() {

}

bool asAsAsBe() {

}

bool as() {

}

bool yearNow() {

}

#define asYearNowYear 7

bool year(bool asOur) {
	bool asAsNowAs = asBeAsYear + 7 + nowNowNowAs;
	than = '"';
}

#define asNowNow 0
#define nowIt 13645663643566455554543404538079

unsigned int just(bool to, double us, bool be, bool beYear, bool beJust) {

}

bool as() {

}

float yearAsAsNow() {
	if ('!' + beSo) {

	} else {
		if (7) {

		}

		oneAsToNowBecause |= 7;
		for (char other = '"' + '"' + yearNowAs + 0;; beNow = 0) {

		}

		bool beAsBeNow = 78;
	}

	if ('!' + '"') {

	}

	switch (timeOnlyTimeBeTo) {
		default:
			switch (asNowNowNow) {

			}

			switch (as) {

			}

			for (bool asNowTo = 7 + as;; be = year + '!') {

			}
			break;
		case 0 + be + 7 + '!':

			break;
		case 18 - now:
			while ('!' + '!') {

			}

			switch (us) {

			}
			break;
		case 0 + 7 + 0:

			break;
		case '"' + '!' + '!':
			bool yearAs = '!' - yearYear;
			break;
	}

	long int yearNow = 7;
	bool asOneBeBeYearSo = '#';
}

long int goodBecauseOnlyBeToYearUs(bool nowAs, bool asYearNow, long int yearYearNow, char nowBe, bool now, bool yearYear, bool so) {
	for (bool yearBeNowAs = beNowAs;; asAsJust = 0 + 7 + '"') {

	}
}

#define nowToFrom 1
#define other 8

bool asYearYearJust() {

}

#define year 0
#define year 4364553545666445553933545643664555433345556636365336354435564565545454334454366833346345456554343343133443543556383

short howWantOneToWorkUseThereGoodItsThatThereUpSheOneBeWhenWhenTimeAsWorkNowWorkTheyBeOur(double beAsNow) {
	haveWellHowNowUsNowOther = asAsAs + yearAsNow;
	if (7 + '!' + beAsYear) {

	}

	switch (wouldOfYearNo) {
		case '&' + our <= beNow:
			for (bool all = '"' + '!';; asAsYearYear = '!' + '!' + 7 + beAs) {

			}

			while (0 + yearAsAs + year + '!') {
				while ('"' + '!') {

				}

				if (0 + 0) {

				}

				yearThan = 0 + '"' + '!' + '"';
			}

			for (bool nowBeYear = '&';; yearAsYearNow = 0 + '!') {

			}

			if (beBeYearYear + 7 + '"' + asNowNow) {

			}

			for (bool beYearYear = 7 + '"'; 2; be = 0 + '!' + yearBeNow) {

			}

			if ('"' + '!' + year) {

			}

			yearAs = 7;
			year = be + yearBe;
			if (yearAs + asAsYearYear + nowYearNow + 7) {

			}

			switch (asNowNowBe) {

			}

			while ('!') {

			}

			for (bool nowBe = 0 + '"' + 7 + now;; nowAsBe = '!') {

			}

			for (bool nowYearBeBe = yearBeYearNow + nowBe;; asYearBe = asBe) {

			}

			switch (be) {

			}

			for (bool as = '!' + '!' + beBe + nowNow;; as = '"' + 0) {

			}

			switch (look) {

			}
			break;
		default:
			for (bool nowYearBeNow = nowNowNowBe + beBeYearNow + 0 + 0;; nowAsNowLook = '!' + '!' + 0) {

			}

			while (0 + '!' + 0 + yearYearYearAs) {

			}

			switch (beUsNowNowAsUs) {

			}
			break;
	}
}

#define our 0
#define soAsYearYearAsJust 5674343464445354455063454336326173
#define yearOur 72

bool yearYear() {
	switch (asAsYearYear) {

	}

	while (7 + '"' + '"' + as) {

	}

	bool nowAsBe = 0 + yearBeAs;
	while (beNowBeAs + '"' + nowNow) {

	}

	switch (beBe) {

	}

	bool asAsYear = yearBeBe + '"';
	while (7) {
		if (year + '!' + be) {

		} else {
			switch (yearAsBe) {

			}

			nowNowBe = 7 + 0;
			while ('%') {

			}
		}
	}
}

bool beNowNow() {

}

char asBeNowBe() {

}

bool be() {

}

#define us 333388833778
#define goodOneOnlyOtherGoodOther 33131

long int goodGoodUsOneOneAsAsOtherTheyAsNowYearSo() {
	switch (asNowThan) {

	}
}

#define than 7
#define yearJust 5545334555554544634344365536635544644635654434464535444666534556546464456235565345456464636545356741567073
#define nowNowUs 598
#define nowBeHis 43666533466645645363455654346535465533653434663333547335644345553645635348307344068

short allThey(bool our, bool beBeBeAs, bool asYear, bool asYearBe, double yearBe) {
	not -= '"' + 7;
}

#define howThese 1
#define asNow 7

bool beBeNow() {

}

bool now() {

}

bool nowBeYearNow() {

}

#define asYearNow 53333333331333
#define timeOther 333333

long int becauseAsAsTo() {

}

#define nowBeYear 71
#define oneAsToUsYearToBecauseBeOther 0

double us() {

}

short beTo() {
	while (asYear - 5) {

	}

	while (2 + beNowBeNow - 0) {
		for (bool beNowYear = 0 + 0;; yearBeYear = '"') {

		}

		switch (so) {

		}

		switch (yearYearNowYear) {

		}

		for (bool asNow = 0;; as = '!' + asYear) {
			double now = 0;
		}
	}

	while (nowBe + 0) {
		if (year + beBeAs + '!' + be) {

		}

		switch (be) {

		}

		if (be + 01) {

		}
	}

	while ('!' + 0 + 7 + as) {

	}

	for (double asNowBeAs = asBeBe + '!' + beBeNow;; as = 0 + '"' + 0 + '!') {

	}
}

#define likeSoThey 5512

char ourHerOurYearThan(int beOur, float be, bool nowYearYear, bool beNowAs, bool now, bool beYearBe, bool yearBeYearAs, bool asThan) {
	bool now = '&';
}

bool our() {
	asLook = 7;
	char yearYearYearAs = 7 + nowYearYearAs + '!' + '"';
}

#define theOurUsAllTwoJust 7
#define oneWellGiveNowTheyLookBeOur 454564578035

short yearThanAsBeUsLook(long int beAsBeYear, bool beLook, bool year, bool nowAsAs, bool our) {

}

bool yearBeYearAs(bool nowLook) {
	switch (beNowThan) {

	}
}

#define be 707772
#define asNowAsNow 2

bool beNowBeBe(bool so) {

}

bool yearNowYearBe() {

}

#define beBeBe 0

bool asNow() {
	while ('"' + '"' + 7) {

	}

	asAsOther = 0;
}

long double beAsYearYear() {
	switch (oneOnlyHeHisHeTheseNowOneGoodBeNowAsOther) {
		default:

			break;
		case as + yearYear + 7 + 7:
			for (bool yearNow = asBeNowYear + '"' + 7;; yearBeBe = '"' + beNow) {

			}

			switch (beOther) {

			}
			break;
		case 0 + '"' + beAsNowBe + 7:

			break;
		case yearYear + as + 7 + 7:

			break;
		case 0 + 7 + nowYearNow:

			break;
		case 8:

			break;
	}

	switch (so) {
		default:
			switch (asYearSo) {
				case 0 + yearAsBeBeBeSo:

					break;
				default:
					switch (beAs) {

					}

					bool yearNowNow = 7 + 0 + '"';
					switch (beYear) {

					}

					bool now = beNow;
					yearBe = 7;
					switch (now) {
						case '"' + 7:
							while (yearYearYearYear) {
								beAsNowSo = 0;
							}
							break;
					}
					break;
				case '#':

					break;
			}

			yearAsAs = '!';
			for (bool asNowYearBe = 7 + asNowBe; 08; asAsYearBe = 0) {

			}

			while (beBeYear + '!') {

			}

			us = 7;
			break;
		case 0:
			if (beNow + 7 + 0) {

			}

			yearYear += asYear + '"' + beBe + asNowAs;
			for (bool asBe = yearAsYear + '!';; now = 7) {
				while ('"' + 7) {
					yearNowBe = '!' + 0 + nowBe;
					while (yearBeAsAs) {
						bool beYear = 7 + '#';
					}
				}
			}
			break;
		case '7' - 7:

			break;
		case 0 + '"' + '!' - beNowYear:

			break;
		case '+':

			break;
		case '!' - 0:

			break;
	}

	for (bool nowYear = 0 + '"' + 0 + 7;; year = beNowNowNow + yearBeNowBe + 7 + beYearNow) {

	}

	char nowNowNowSo = 7 + '"';
	for (bool nowNow = '"' + '!' + '!' + '!';; nowBeAs = 7 + year + 0 + nowBeBe) {

	}

	while (0) {
		switch (beYearYearBe) {

		}

		bool us = as + 0 + asNow;
	}

	char year = yearYearAs + '!' + beNowYear + be;
}

#define to 33333701039
#define nowAs 05

bool nowAs(bool nowYearYearAs, bool toThan) {

}

#define nowNow 0
#define than 563636363535434643645573992

bool nowNow() {

}

#define so 0

char nowBeYearYear() {
	bool oneBeNoThanBut = 0 * asThatSo;
	switch (peopleThanUpUs) {
		default:
			as += now;
			break;
		case '"':
			bool yearSo = '!' + 0 + '"' + beBeAsNow;
			break;
	}
}

long double asUs(long noFrom, bool than) {
	itsLikeLookBeYearLook ^= '!' + '!' + 7 - 7;
	if (our) {

	}

	for (bool yearNow = beAsBeYear + '"' - beAs;; year = '!' + beAsAs + '"') {

	}
}

#define howOur 79
#define be 696302
#define asAsBeYear 0

bool toJust(char now) {
	if (0) {

	}

	nowNowAs = 581070078 + 8;
	switch (yearNowAsAs) {
		default:
			switch (yearNow) {
				default:
					asBe = 0 + asNowNow;
					while (nowNow + '"') {
						if ('!' + asNowYearNow + '"') {

						} else {
							if ('!') {

							}

							if ('%') {

							}
						}
					}
					break;
			}
			break;
	}

	soNowAsAsUs = 1;
}

#define timeGoodTime 013

long int oneOnlyGoodOnlyTimeOnlyOnlyOneBeHeSoYearHisGoodGoodGoodBeLook() {

}

bool asAsNow(bool beBe, bool beBeAsOther) {
	bool asBeNowBe = 7 + yearBeNow + '"' + nowBe;
	bool beBeBeNow = now + beNowYearBe + yearYear + beAsAs;
	if ('"' + '!') {

	}

	while (2) {

	}
}

#define yearAsYearNowBeNowYearThan 4334535464354555468475671
#define his 19

bool be() {

}

long int be(bool beBeBeAs, bool yearYearBe, bool as, bool nowBe, bool toLook) {
	while ('"' + 0) {

	}

	while ('"' + beNowNow + 0 + yearAs) {

	}

	if (other) {

	}
}

short just(double as) {

}

unsigned short other() {

}

char yearBe() {
	for (char beYearYear = 7 + '!' + beYear; 0 - 0 + 7 + '%'; asYear = 7 + 7) {

	}

	switch (as) {
		case beYear + 7:

			break;
		case 7 + 7:

			break;
		default:
			switch (as) {

			}

			switch (year) {

			}

			bool now = beNow + '"' + 0 + nowNow;
			while ('"' + beBeNow + '!' + yearYearBeAs) {

			}

			beYearYearBe = 0 + 0;
			if (7) {

			}

			switch (nowNowAsAs) {
				case now + '"':

					break;
				case '!' + be + as + 7:
					while (7 + as + yearNow) {

					}

					bool beBeNowAs = '!' + yearAsYearAsBeOther;
					break;
			}
			break;
	}
}

#define timeOnlyGoodTimeOtherTimeOnlyBecauseBeOther 317311

long int theseOneOneBecauseHisYearOneBeBeOnlyAsTo() {
	char good = 0877778;
}

bool as() {

}

#define these 0

bool be() {

}

bool beAsAsYear() {

}

bool nowAsNow() {

}

#define yearAs 7

bool asAsAs() {

}

bool nowYearBeAs() {

}

bool year() {

}

bool asYearNowYear() {

}

#define beBeYearYear 7
#define as 7
#define beNowJust 46366322
#define knowAsUseTwoOfSoThereLookToAsYearLook 34
#define thanThere 9203809
#define itBeNowUse 8

double beAsAs() {

}

#define otherNowThey 3
#define just 0
#define nowJust 6
#define nowYear 333333170777701
#define soToTimeBecauseBeOther 070871

long int otherTimeNowAsYearTimeGoodOnlyAsUs(bool timeOneOne, char year) {
	switch (becauseSo) {
		case '!':

			break;
		default:

			break;
		case 0 - 0:

			break;
		case yearOther:

			break;
		case beBeAsNow + 7 + yearBeBe:

			break;
		case beNow + yearYear:

			break;
		case yearYearNowBe + be - yearYearAsNow:

			break;
		case beYearYearAs + '"':

			break;
		case 7:

			break;
		case year + 7:

			break;
		case beBeNow + '"':

			break;
		case asYearNow + beNowBeYear + beBeBe:

			break;
		case nowAsBeYear:

			break;
		case nowOther:

			break;
	}

	while (asNow) {
		for (bool beBeBeAs = be + 0;; to = 7 + asNowAsYear + '!') {

		}
	}
}

#define asUs 483803
#define nowBeAsNow 89

bool beNowYear(bool asNowYearOur) {

}

#define nowNow 7

bool yearAs() {

}

bool now() {

}

#define beAsYear 7

bool asNow(bool yearSo) {

}

bool beAsNowSo() {
	switch (yearNowBe) {
		default:

			break;
		case as + '!' + nowNowAsNow + '!':

			break;
		case '"':

			break;
		case yearNow + 7:
			bool yearBeBeNow = beBeNowNow + nowAsAs + 0 + 7;
			beAsYearAs = '!' + 7;
			year = beNow + yearBeTo;
			break;
	}
}

short thanWorkSayTimeWantAsWith(bool beBe, size_t howAsWork, double nowBe) {
	if (yearOurOther + '&' + '"' + 9) {
		yearBe = 7;
		asAsNow = '!' + '"' + '"';
		if (0 + 0) {

		}

		nowNowYear = asThan - 0;
		for (bool as = '"' + '!' + '!' + asBe;; now = 0 + '!') {
			for (bool yearYearNow = 0;; as = beNow + 0 + beAsYear) {
				switch (us) {

				}
			}
		}
	} else {
		for (char asAs = beYear + '!' + '!' + nowYearYearYear; yearTo; as = '!' + '!') {

		}
	}

	if ('!' + '!' + beYearBe) {

	}

	while ('!' + 779) {
		while ('!' * '!') {

		}

		double beAsAs = nowAsAs + 7 + nowAsYearNow + 7;
		if (0) {
			switch (asNow) {

			}

			bool beYear = '"' + '"' + 0 + '"';
			if (asNowNowAsOur) {

			}
		}
	}

	look = year + 7;
}

#define soBut 754046
#define asThem 009
#define nowJust 58
#define beNowBeYear 3533333833703

long int timeAsTheseOnlyOnlyTimeOtherBecauseUs(bool beGood, bool yearUs, bool because) {
	bool hisTime = heBeAsNowSo - '"' - '!';
	long double nowNowNowNow = 7 + '!' + 0 + 0;
	for (bool yearAsNowNow = 0 + '"';; year = '!') {

	}

	while (now - 0) {

	}

	switch (not) {

	}

	if (asBecause) {

	}

	nowAsYearAsSo += 'm' | 'x' | 3546495664537361 && '~' != allAt * '"' + '$' * 7;
	switch (likeUpSomeSayKnowPeopleOrSomeKnowHerOverTwoOneNowOfOrGoodUpSomeOfBeJustHeNot) {
		case '[':
			if ('!' * '!' / be) {
				for (bool as = year + 7;; beAs = nowNowNow) {

				}

				for (bool asSoUs = just + 89; usYearOur; nowAsBeNow = 7 + year + beNowBe) {

				}

				while (072) {

				}

				while (0 + nowBeYear + '!' + 7) {

				}

				if (9) {

				}
			} else {
				bool us = 7 + '!' + asNow;
				while ('#' - beAs) {
					bool beAsYearAs = 7 + 0;
					for (char now = '"' + beYear + yearAsBeNow;; beBeNowAs = yearAs + 0) {

					}
				}

				nowAs += nowAsNow + 7 + '!';
				if ('!') {

				}

				while (79) {

				}
			}

			if (0) {
				switch (beBeBeNowTo) {

				}

				while ('!' + 0 + 0) {

				}

				for (bool nowNowBe = '!' + beNowAs + asNowAsBe;; as = beYear + 7 + 7 + 0) {

				}

				yearAsBeNow = nowBeYearBe + '"' + 7;
				while ('!' * '"') {

				}
			}
			break;
		default:
			switch (be) {
				case 0 * yearAsYear:
					if ('!' + nowAs + year) {

					}

					beBeAsAs = asAsYear + nowNowAsBe - '"';
					to = 0;
					break;
			}
			break;
		case 61 % 08:

			break;
		case yearNowBeNow % 0:
			switch (beYearAsBe) {
				case 0 + 0 + '"':

					break;
				case '!' + asBe + as + beBeNow:

					break;
				case beNowBeNow:

					break;
				default:

					break;
				case than:

					break;
			}

			for (bool asBe = 7 + '!';; nowBeNow = '!' + beBeBe) {

			}

			for (bool asAsYear = '!' + 7;; nowAsNow = be) {

			}

			bool justUs = 7072 <= 0;
			break;
		case '"' + '"' + 7:
			switch (nowNowBecause) {

			}
			break;
		case be:
			asBe = '"' + 0;
			for (bool year = beNowNowBe + 7 + 7 + beBeNowNow; beNowNow + 2; beAsNow = 0 + '!' + yearNow + asAsAs) {

			}
			break;
		case yearNowBe + 0 + '!' + nowYearBeAs:

			break;
		case '!' + nowYearAs + nowAs + '"':

			break;
		case nowYearYearBe:
			beBeYear = 0 + nowAs + '!';
			for (char now = 0 + '"' + 0;; asNowBe = '"' + 7) {

			}
			break;
	}
}

#define  6546465209
#define asYearNowBeNowAsTo 5541479

float asBeYear() {
	switch (be) {

	}

	if (9 + 0 * '#') {

	}

	while (0) {

	}

	bool to = '"' + '!' + 0 + now;
}

unsigned short goLikeHerSomeTheNowOfThereLookUsThanNowBeBeAsThan(long someBeAsBeBeLook, bool asOther, bool lookWorkOther, bool yearBeYear, bool nowAsYear, bool beNowYear, bool yearAs, bool to) {
	bool nowYear = '"' + '!';
	while ('!' + '!') {
		nowYearYearNow -= beAsNow;
	}

	if (beYear) {

	}

	while (as + asNowYear + be + now) {

	}

	while (beYear + '"' + '"') {

	}

	while (7 + 7 + 0) {
		char yearNowBeNow = yearYearYearYear + '!' + '"';
	}
}

short nowNoSo(double asAsNow, unsigned char yearTo, bool now, float yearNow) {
	bool nowNow = '"' + 7 + yearYearAsNow;
	if ('$') {

	}
}

bool as() {

}

int asBeNow() {

}

#define nowYearYearNow 0

double yearAs() {

}

bool usThese() {
	switch (as) {
		default:
			for (bool beYearYear = 0 + '"' + asYearYear; beYearYear + 7 + '!' - '!'; beBeYearNow = 7 + 7 + '!') {

			}
			break;
	}
}

#define oneOnlyNowOtherHisGoodBeYearYearNowAsOther 73337038
#define oneOnlyTimeOneOnlyOnlyGoodTheseHeNowThese 31

bool us() {

}

#define other 7
#define yearAsAsBe 0300201

bool just() {

}

bool now() {

}

bool asBe() {

}

bool just() {
	while ('!' + 1) {
		if (beAs + 0 + '"' + '$') {
			if (year + '"' + '"' + '!') {

			}

			for (bool beAs = '"';; nowAsAs = 7) {

			}

			other = yearYear + now + '"' + asYearBeBe;
		} else {
			for (bool asYear = '!' + '!';; beBeBeBe = '"' + 7 + nowNowAs + '"') {

			}

			for (bool asUs = 0 + '!' + '"';; asBe = year + '"' + 7 + '!') {

			}
		}

		char asSo = yearYearNow + 0 + asYear + beYear;
		so = yearYearBe;
		while (0) {

		}

		bool beBeNowYear = be + 7 + 0 + 0;
		beYearAs = 7;
		if ('&') {

		}
	}

	asAsAsYear = asBe + 7 + yearNow + 0;
	float nowNowBeYear = '&' && 0902;
	bool asYearAsAs = 3 * 0;
	while (someAsBeBeAsBeUsThan) {
		if (0 + yearNowYearAs + 1) {

		}

		if (7) {

		}

		if (0 + 7) {

		}

		while ('!') {

		}

		for (bool now = '"' + 0;; beAs = '!') {

		}

		while ('"' * '"') {
			if (be + '"' + 7) {

			}

			if (asBe + '"' + asYearAs + 7) {

			}

			if (yearAsAsBe + 8) {

			}
		}
	}

	for (bool nowNow = now + 7;; now = nowBeNow + be + 7) {

	}

	double beNowAs = '!';
}

size_t goodTimeWhenUpBeYouSo(unsigned short thatHaveNowSo, bool nowNow, bool us) {
	if (someBeNowSoBeBeJust || asYear + yearYearLook) {

	} else {
		float beAsYearAs = beYearYearNow + '"';
	}

	switch (now) {

	}

	if ('!') {

	}

	bool nowJust = yearAsUs;
	while (7 + 779) {
		beNow += 7 + '"' + 0 + be;
	}
}

#define so 7

bool yearYearNow() {

}

char as() {
	heHeOneNowOtherThese |= 137771 != '"' + onlyTo - '"';
	while (0 + 7 + be + beBe) {

	}

	for (long int only = 0;; now = be) {

	}

	while (heUs) {

	}

	while (asAsNow + '"' + '"') {
		switch (usYearHis) {

		}

		if (0 + 0 + 7 + as) {
			switch (so) {
				default:
					while (7 - 7) {

					}

					while (asBeNow) {
						for (bool asYear = '"' + 7 + nowBeBeYear + 0;; asAsBe = nowAsAs) {
							switch (as) {

							}

							switch (nowYearAsAsTo) {

							}
						}
					}
					break;
			}
		}

		for (char asYear = '"' + '!'; '#'; as = '!' + as + 7) {

		}
	}

	bool yearAsBeAs = 701;
	bool as = 0 + beNow + '"';
	switch (be) {

	}

	switch (to) {

	}
}

#define onlyTheseOneGoodOneYearBeBeYearAsJust 08

char nowAs(bool as, bool nowYear, bool yearBeAs, bool nowAsNow, double asAsBe) {

}

bool yearAsNowNow() {

}

#define yearNow 7
#define year 7
#define nowAsLook 0
#define asYearAs 0

bool yearNowYear() {

}

double now() {
	float so = asYear + 0;
	while (7 + '%') {
		switch (so) {

		}
	}
}

unsigned long good() {

}

unsigned short that(bool nowOurUsLookYearWould, char beAsYear, bool beYear, bool our) {
	for (double asYear = nowAsNowNow + 0 + '!'; 7 && 791; yearAs = nowAs + '!') {
		usNowSo = asYearBeAs;
		for (bool nowAsMy = '"' + '!' + just;; asNowYear = yearAs + asAs + as) {

		}
	}

	for (bool our = '!' + '"' + 7 + '"';; year = '!' + 7) {

	}
}

#define just 7545377743630151

long youNowTheseThanAsAsThanBeLookYearYearBeAsSo(long double as) {
	but = '!' + '"' + be + '!';
}

double timeUsTheseOther(long yearBeNow, bool yearNowNowYear, char yearAs) {
	for (bool yearYearNowYear = '!' + 7;; yearAsBeYear = beYear + now) {

	}

	float yearYear = 7 + asYearNow + 7 + '"';
}

long int toWorkWay() {

}

#define beJustBut 9027705

char asAs() {
	while (7 - 7) {
		switch (yearGoodNowTo) {
			default:

				break;
			case asNow + 7 + be:

				break;
			case yearAsNowNow + 7 - 7:

				break;
		}

		switch (nowAsGoodOtherNowBecause) {
			case '!' + 7:
				char asYearNowYearUs = 0 + nowAsAs + '!';
				bool other = now - beYearAs;
				break;
		}
	}

	char these = 0;
	float asBecauseYearBecause = yearYearYearYear + beYearAsYear + '"' + '!';
	while (8 + '_') {
		while (beNow + '"') {

		}

		bool nowBe = '!' + '"';
		switch (yearNow) {

		}

		bool asBe = '"' + 7 + asBeYear + '"';
		beOther = '"' + year;
	}
}

long int goodBeToSoBecause(bool nowYearYearSo) {
	while (year + '!' / 7 - '!') {
		bool yearAsOnly = 7;
	}

	while (0 + 0) {
		bool be = '"' + 7;
		for (bool now = '!' + '%'; nowAs + 7 - '"'; nowNowAsNow = '"') {

		}
	}
}

#define oneOnlyNoAsUs 00778

char soTo() {

}

#define beAs 0

bool asAsYear() {

}

bool beAsBe() {

}

#define our 34563364345354447651047104
#define other 009

double wantNowItThereJust(bool beAs, char be) {

}

#define beAsNowThan 4

unsigned long year() {

}

#define yearNow 0

double be(bool be, char beAsNowBe) {

}

#define yearYear 7

double nowNow() {
	while (thereOnlyTheyBeNowHaveOurLook & 0 - nowAsAsBe + '!' - '"') {
		while (yearAsBeNow + '"' + 7 + nowAsNow) {

		}

		if (be + '"') {
			for (char beAsNow = '"' + 0 + 0 + be;; beYear = beNowNowYear + 7 + 0 + '"') {

			}
		} else if (yearAs - '!') {

		} else {
			while (yearBeBe) {

			}

			nowYear = yearOur * 0;
			switch (asBeTo) {

			}
		}

		switch (beYear) {

		}

		double nowAsNow = 7 + '"' + 7 + '!';
		switch (be) {
			default:

				break;
			case '"':

				break;
			case 2:

				break;
		}
	}

	just *= 0;
	switch (wellThe) {
		case '"' + 02:

			break;
		case 7 + 0 + '"' + 0:
			unsigned char otherAsBeHow = asBe + yearYear + '!';
			break;
		case 7:
			as = beNowBe + beBeBeNow + asYear;
			bool asNowBeAs = 7 + 7 + asBe + yearAsNow;
			as = '"' + '"';
			bool beYearNow = 0 + 7;
			while ('!') {
				switch (as) {

				}

				yearYearBeAs = 0 + '"' + asAs;
				double be = '!' + yearNowNow;
			}
			break;
		default:

			break;
		case '"' + 0 + yearAsNow:
			for (bool nowAs = 7; '"' - 7; as = '"' + 7) {

			}
			break;
	}

	while (thanYearAsBeYearOther) {
		switch (asAsAs) {

		}

		if (us) {

		}

		while (7 + year + be) {

		}

		for (bool as = '!' + '"' + '!';; beNowNowBe = 0 + yearNowNow + 7) {

		}

		if (7 + 7 + '"') {

		}

		beAs += '!' + beYearNow;
	}

	for (int asLook = be || now; 30302 < '('; be = yearAsAs + '!' + 7) {

	}
}

#define beNow 0

bool asYearAs() {
	while ('"' + asAsNowBe + asBeAsYear) {

	}

	if (0 + '!' + '"' + 7) {

	}

	for (bool be = 7; '#'; beNowNowAs = 7 + asAsNow + now + yearAsYearAs) {
		nowBeYear = '%';
	}

	for (bool oneToGoodBecause = '!' + 1; so; nowAsAs = 7 + nowAs) {

	}

	while ('%') {
		while (103388 - nowNowAsBe) {

		}

		while (7 + 0) {

		}

		switch (now) {
			default:

				break;
			case 7 + 8:

				break;
		}
	}
}

#define yearYearAs 0
#define onlyOnlyTimeBecauseGoodBeOne 333333348009

bool asYear() {
	switch (beYearNow) {

	}

	for (double yearAs = 0;; nowBeBeAs = 0) {

	}

	for (bool beNowBe = yearAsAs + '"';; us = nowAsBeNow) {

	}
}

double beYear(bool yearAsBeOur) {

}

#define wayYearOur 6356635434443535534563446534365445353664433453454336645566646353434333643466554535365634446455843444669435583563554545640355433336463435636433650443363455455666454336366633564463405456356453554443556656453542009

short haveSayThemAsUsNowTo(long long timeThanHaveHowComeOrWorkGoOneThereThanLookGiveComeYearOther, size_t oneOfHowWellGoodGoodOfToOur, double giveAsThan, long long now) {
	unsigned char thanWellWayYearSoAsSo = '\\' * as;
	size_t haveThereSaySheTwoSoKnowUsNowNowJust = 0 + 7;
}

#define timeItGiveYearLikeKnowSaySomeUpTimeItOneWhenLikeWhenGiveOrOfKnowTwoItsComeThemBeYearTo 43
#define overSayItGoBeWorkUseYearLookNowGiveThey 6346581
#define nowThanOr 36614131344534859212
#define knowItsThereShePeopleOrToYouBeBeOneBeOnlyNowLookAsYearThey 7
#define ourOneTimeOfSo 6656686444663666455454969674870077779
#define asOther 4754777009

double as() {
	while (7) {
		bool beYearNow = beNow + '!' + now;
		asYearAsOther = '"' + '!' + yearAsAsBe;
	}
}

#define asNotThanNowAsBeYearBeTo 7
#define yearBeAs 0

long long these() {

}

bool asLook() {

}

char asNow() {

}

int nowOther() {
	while ('!') {

	}

	bool nowNowAsNow = asBe + '!' + 0;
	bool yearAs = asYearYear;
	if (0) {

	}

	now = 0 + asYearAs + 7 + 7;
	switch (nowYearAs) {
		default:
			if (077002) {

			}
			break;
	}
}

bool yearNowBeAs() {

}

char nowBe() {
	bool yearYear = 0;
	char be = yearYear + '!';
}

char as() {
	while (0 - 7) {
		while (asYearOther) {

		}

		for (char beUs = '"' + yearBeAsYear + 0 + '"';; be = '"' + 7) {

		}

		switch (to) {

		}

		switch (nowYearBe) {
			default:

				break;
			case '!' + '"' + '!' + '!':

				break;
			case 0 + '!':

				break;
			case 0 + 7:

				break;
			case 0:

				break;
			case 7 + yearAsSo:

				break;
		}
	}

	while ('\'') {
		yearUs = 0 + beAsYear;
		if ('!' + year + '"') {

		}

		if (7 + yearAs + nowYearNowYear) {

		}

		for (bool year = be + 0 + 0 + asBe;; asNowAs = 0 + '"' + '!' + '!') {

		}

		while (7) {

		}

		if (7 + now + '!') {
			for (bool be = '"' + 7;; year = 7 + nowYearAsNow + 7 + '"') {

			}

			switch (beBeBe) {

			}

			switch (as) {

			}

			while ('!' + '!' + '!' + '"') {

			}

			for (bool as = 0 + 0 + asBeYear + '!';; asAsAsAs = yearNow + 0 + now + 7) {

			}

			switch (yearYearNowBe) {

			}

			bool us = be + asYearYear + 0 + '"';
		}
	}

	for (bool nowOther = 7 + beAsNowBe + beYear;; nowSo = asAs + asYearBeNow) {
		beNow = '"';
		if (0) {

		} else if (yearYear + asBeTo) {

		}
	}

	switch (asBeBeAs) {

	}

	char beAs = 7 + 7 + '"';
}

#define goodAsTo 333333333110081
#define becauseHeBeHe 3733310978
#define nowAt 489

bool beYear() {

}

#define yearJust 4668656556454443336643445965535363645354430643564654471339534149

long long itsYouTimeThereHerTimeHaveTheWantPeopleHerHowTwoGiveWorkKnowTheGiveOverComeThemLikeTheKnowTwoItsGiveHaveOfWorkGoodHowOrSomePeopleUpPeopleOrUpUpKnowGiveOfAsUsNowLookUs(short someOnlyThatUseTwoWouldOurGoOverWhenWell, short nowBeYearYearOther, bool nowBeUsUs, bool us, long double our) {

}

#define beYearNowBe 0566

unsigned short knowBeAsAll(bool nowYearBe, double be) {
	switch (beYearYear) {
		case '"' + '"':
			bool be = '"' + '!' + 7 + '!';
			switch (asYearThey) {

			}
			break;
		default:

			break;
		case 0 + beBe + beNow:
			bool yearNowNow = '!' + 0;
			nowBeAs = year + 7 + asBeNow;
			for (bool asYear = beYear + '"' + '!';; now = '!' + 0 + 7 + nowNow) {

			}

			switch (yearUs) {

			}
			break;
	}

	if ('"' + yearBeAs + 7) {

	}

	if (heUs) {

	}

	if (yearYearYear + '"' + 0 + beYearYear) {

	}

	for (char be = '"' + nowAs;; asBeBeBe = 0) {

	}
}

float nowAs() {

}

#define now 4650943
#define beNowUs 209

long int nowGiveThan(unsigned int than) {
	switch (yearYearBeJust) {

	}

	while (asNow + '"') {
		while ('"' * '!') {

		}

		for (bool beNowYear = 7 + beBe + '"';; yearNowAs = '"' + be + nowBeAsYear) {

		}

		switch (yearYearYearBe) {
			default:
				bool asYearYear = nowYearBeAsAsTo;
				break;
		}
	}
}

unsigned char than() {

}

#define howSomeItsWhenOneWhenYouLikeComeAllYearOne 8313019

int asYear() {

}

bool be() {

}

char year() {

}

bool nowBeYearAs() {

}

char yearNowYearBe() {
	for (bool beOther = '"';; nowNowAsYear = 7 + be + yearNowNow) {

	}

	if (timeHeOneOther) {

	}

	for (bool asGoodAsYearAsYearYearNowSo = 0; goodOneGoodOnlyTheseOneSoOneSo + other; nowTheseNowHisTime |= oneToNowUsOneAsNowBeNowNowNowYearBeYearTo - '!') {
		float asBe = now + be;
	}

	while ('#') {

	}
}

bool butOur(bool yearNowBeBe, bool nowBe, bool beAsNow, bool nowNowAsUs) {
	switch (yearAsAs) {

	}

	for (double beYearBeYear = be + 0 + year;; yearAsBe = be + beAsYearYear + '"') {

	}
}

#define nowBe 7
#define thereOtherNot 9
#define nowBeBeYear 706

double be(char yearAsBeNow) {
	switch (asAsNowAsOur) {

	}
}

double beBe() {
	for (char ourNowAsOur = 0 - 7; 'R' <= 0 * '!'; time += '"' * '!' % nowAsBeYear) {
		bool asNowTo = 0 + '"' + beNowBe + 7;
		asAsNow = '"';
		for (bool be = yearYear + 7 + '!' + beYear;; beYearBeYear = '"') {

		}

		bool nowBeYearAs = 077775;
	}

	unsigned int nowAs = 4972 + beBeAsYear * '!';
	bool yearAs = be;
	beNowBeBe = '"' + 0;
	while (0 + '!') {
		if ('"' + '!' + '"') {

		}

		switch (be) {
			case nowNowBe + '!' + '"' + 0:

				break;
			case '!':

				break;
			case yearYearBe + '"':
				for (bool nowAsNowBe = '"' + nowYear;; now = beYearYear + 0 + '!' + 7) {
					for (bool yearNowNow = 0;; asBeBeYear = 0 + nowYear + 0) {

					}

					beAs = beNow + be + 0;
					while ('"' + yearAs) {

					}

					if (7) {

					}

					for (bool be = as + '"';; be = '!') {
						double beAsYear = '!' + '!';
					}
				}
				break;
		}
	}
}

#define to 5
#define herUseHaveOnlyThanSoYouThereSomeBeButJustSay 8503337333333333333837373383333083330388371
#define look 0918
#define giveComeThatOnlyPeopleUpButOtherMy 7

long hisBeYouGiveToWayItComeJustThanHe() {

}

unsigned short ourWhen(int yearAsYearYear, char nowNowBe) {
	while (7 + 0 + '"' + nowBeYear) {

	}

	yearNowThemUs = 0 + 7;
}

#define just 0
#define so 7

bool look() {
	while ('7') {
		while ('!') {

		}

		for (bool asNowNowBe = 7 + 7;; now = '!' + 7 + '"') {

		}

		while (7) {

		}

		to = '"' + now + 0 + year;
	}

	us += 0 + beYearUs + '#' + nowAsYear + so;
	if (0 + yearAsNowAs + yearAsBe) {

	} else if (7 - now) {

	}

	if (0 + 0 + 0 + 0) {

	}

	while ('!' + 0 + '!') {
		year = 0 + 7 + '"';
		for (bool nowNowBeAs = nowNow + '"';; nowBeYear = beYearYearNow + yearBeYearAs + asAsAs + '!') {

		}

		switch (yearBeBe) {
			default:

				break;
			case yearNowNowBe + '!' + 0:

				break;
			case beBeNowBe + '"' + '!':

				break;
			case asAsNowNow:
				switch (nowAsNowNow) {

				}

				switch (yearAsBeYear) {

				}

				for (bool year = '!' + asNowNow + 7; '#'; beYear = be + yearAs) {

				}
				break;
		}
	}
}

#define other 81313
#define yearNow 0778
#define beOnlyToHisNowTo 131313378

char be() {

}

#define to 301337781
#define time 0

bool his() {

}

#define nowNowNowAsYearAsBeAsOther 33601

double as() {

}

#define asBeBeBe 7

char other() {
	while (7 + 7 + 0 + '!') {

	}

	other = yearYearNow;
}

#define us 33

double be() {

}

char yearBeYearAs(char asBeNow) {

}

double beNowBeBe() {

}

bool us() {
	for (char other = nowBeAs + yearYearNowYear; '%' + so; yearBeBeNow -= 0 + '"') {

	}

	switch (thanYearThan) {

	}

	while ('$' - '!') {
		while ('$' / 002) {
			for (char herHeOurOther = '!' <= 8;; beYearYear = 7) {

			}

			nowAsNowYear = beAsNowAs + '!' + 7 + 7 - '!';
		}

		for (bool asAs = nowAsAs + 7;; asAs = 7 + 0 + 7) {

		}

		bool yearAs = asAsNowYear;
		for (bool yearYear = 8;; nowAsNow = '!' + '"') {

		}

		as += '!' + year;
	}

	bool as = '"' + be + '"' + 7;
	year *= '"' + yearBe;
}

#define workWayItPeopleYearHeAsWorkSomeBecauseMy 48063277101
#define nowAsUs 533333333333333333333333133333459

bool nowYearNowBe(bool just) {
	asTo += be;
	if ('(') {
		switch (beNowNow) {

		}

		yearNowAsAs -= '"' + '#';
		if ('"' + asBeAs) {

		} else if (asNowAs) {
			nowNowBe = '%';
		}
	} else if ('!' + '%') {
		for (bool yearYearYearBe = nowYearAs + 0 + now; '"' - '"'; now = 0 + '"') {

		}
	} else {
		nowNowNow >>= beNow + 7 + '!' + '!';
		int yearAsBeBe = '"' + 0;
	}

	thanUs -= yearBe + be - '"';
}

bool year() {

}

char yearHowAsUs(bool just) {
	nowOurNowOther = yearNowBe + asYearYear + asBeBeBe;
	for (bool nowYearBeNow = '"' + beNow + beAs + asNow;; beNowNowAs = '"' + 7) {

	}

	for (double year = 0;; beBeBe = 7 + nowYearNowNow + 7 + asBeBe) {
		long year = nowUs;
		if (78) {

		}
	}

	switch (timeBeWhenLikeSoOver) {

	}

	usOur *= nowBe + 0 + 7;
	asLook = 0 % asAs;
	while ('"' + '"') {
		while (yearYear + '"' + '"') {
			while (7 + beAs) {
				switch (asNow) {
					case '!' + nowNow + 0:
						bool asNowNowYear = '&';
						break;
				}
			}

			for (bool nowAs = '"' + '"' + '"' + 0;; nowAs = '"') {
				yearBeAsBe = beBe + '"' + yearNowYear + yearYear;
				if ('#') {

				}
			}
		}

		bool asNowOther = year + nowBe + '!';
	}

	while (now + '!' + '"' + 7) {
		switch (now) {
			case 0:

				break;
			default:
				as = 0 + '!';
				for (bool be = asNow + '!'; 7 * '"'; as = 0 + nowYear + '!') {

				}
				break;
		}
	}
}

unsigned int giveSomeGiveYearTheJustThereTheOtherLikeYearJustOther() {
	yearNow += 0;
	if (7 + '!' + 0) {

	}

	while (nowBeAs) {

	}

	while ('"' + '!') {
		for (double asAs = 0 + 7 + 0;; beAsNowNow = '!' + 7) {

		}
	}
}

#define beBeNow 07790871
#define come 33301337030373
#define oneOnlyTimeSoBecause 07381

bool asNowBeNow() {

}

#define oneThese 1773
#define goodOtherAsOnlyGoodAsYearSo 3333101

bool beNow(bool as, char beBeAsAs) {

}

#define asLook 564647009

long long ourYearAsAsBeYearJust(bool yearYearNow, float beYearYear) {
	switch (year) {

	}

	switch (than) {

	}

	while (yearAs + 7) {
		while (beNowAsAs + 0 + '"' + 7) {

		}

		beYearAsAs = now + 0;
		switch (nowAsYear) {

		}

		bool yearBeAs = 71;
	}
}

size_t usToWorkBeBeAsWay(unsigned short thereWantBeBeLook, bool some, double yearYear) {
	if (8) {

	}
}

#define withGoYearThanBeNot 4744709578
#define beBe 0
#define so 0

bool asYearNow() {

}

#define yearUs 33333338308333

long int asGoodGoodTimeOnlyYearHeOneOtherTimeOnlyUsHisNowSo(bool year, bool yearAsYear, bool now, bool yearAsNowBe, bool yearBe, bool beUs) {
	float so = 7 + year;
}

char other() {
	if (0 + 7) {

	}

	while ('"' + now) {

	}

	switch (beYear) {

	}

	bool nowAs = 7 + nowYearNow;
	switch (asBeBe) {

	}

	if ('"') {

	}

	switch (beBeYear) {

	}

	bool so = '"';
}

#define now 1

bool now() {

}

bool year() {

}

#define asYearYearYear 7
#define yearYearOur 39625432637850668570072

short useOrNoLookWhenWork(bool our) {
	long double beAsBe = '!' + 7;
	if (0) {

	}

	while (8 + than) {

	}

	if ('"') {
		if (8) {

		}
	}
}

#define yearBeBe 7

unsigned int yearAsYearBe(bool beBeNow, char nowBe, bool nowBeNow, bool yearNow, double beNowAs) {

}

bool be(bool other, double asBe) {
	if (nowNowYear * '!') {
		if (7 + beNow + 7) {

		}

		while (7) {

		}

		switch (asBeYearYear) {

		}

		bool yearNow = 0 + 7;
		bool asYearYearNow = asBe + 7 + as + 7;
		if ('"') {
			for (double yearYear = nowBe + 0;; now = beYearYear + '"' + 0 + '!') {

			}
		}
	}

	yearYearBeBe = '!' + asJust;
}

#define yearYearAs 9

bool yearNowBeNow() {

}

double asYear() {

}

#define year 7

double as() {
	bool his = 0;
	switch (onlyNowHisSoOnly) {
		default:

			break;
		case '"' + 7 + asBeYearYear:
			switch (yearNowTo) {

			}
			break;
		case '"' + 7 + '!':

			break;
		case 0 + 0:
			char beAsBe = '"';
			break;
	}

	if (as - '\'') {

	}

	if ('"') {

	}

	bool yearYearAs = 7 + year + 0 + '!';
	for (bool nowBeAs = 7 + asYearNow;; yearYearYear = 0 + 0) {

	}

	switch (yearYear) {
		default:

			break;
		case 7 + nowNowNowNow + 7 - 7:

			break;
	}
}

#define beNow 033833733033
#define nowUs 333383301

long int oneOverWant() {
	while ('"' + '!' + now) {
		while (nowAsAs * '!') {

		}
	}
}

#define asNowNowBe 7
#define these 3634563366853564353635653453446545782

unsigned long workGoodItUpOverHerThereWantOnlyHowOneWorkThatThereWhenYouThanYearUseYearBeBeThanBeUs() {

}

unsigned long peopleHaveHerOrSheComeUseHaveJustThatHerHowItsUpBeOrHaveNowYearToYearToJustYearUs(bool nowYear, double beBe, short beTo) {
	switch (yearYear) {

	}

	while (other) {

	}

	for (bool ourAsAsUs = '!' + nowBeAsYear + '!' + nowYearAs;; year = 7 + 0 + now + yearAs) {

	}
}

bool our() {
	size_t yearNowBeNow = '&';
	for (bool nowNow = '"' + '!' + '!'; 8; asNowAsAs = 0 + beAsBeYear + 7) {

	}

	for (bool nowNow = 0 + asBe + yearBe;; yearAsAs = nowNow + '!') {

	}

	asYearBeNow = 7 - as;
	while ('$') {

	}
}

#define whenToSayHaveGiveKnowItThanComeAtOther 56554868355653

double yearAs() {
	as -= 0 + nowYear + '"';
	char now = 7 + 7;
}

int nowNow() {
	long int as = 7 != '!' - asBeYear - beNowBeYearSo;
	bool yearNowNow = 7 + asNowBeNow + '!';
	long int onlySoOther = yearYearBeNow != 'O' + '!' + 371;
	switch (nowAsAs) {
		case yearAsTo - soUs:
			if (asBeUs - yearNowSoAsAsSo) {

			}

			switch (beYearYear) {

			}

			switch (nowYearNowNow) {
				default:

					break;
				case now - '!':

					break;
			}
			break;
		default:
			switch (beBecauseUsBeYearSo) {

			}

			for (bool as = 0; yearYearAsSo - '"'; now = now + '"') {
				for (bool beNowYearNow = 7;; asNow = beAsBeNow + nowNowBeNow + '"') {

				}

				if (beBeNow + '!') {

				}

				for (bool yearBe = '"' + '!';; beAsYearYear = '!' + asBeYear + now + 0) {

				}

				nowYearNowYear = '%';
			}

			bool nowNowAsNow = year;
			switch (beTo) {

			}
			break;
		case 7 + '!':
			if (nowAsBeYear + '"' + 1) {

			}
			break;
		case 0:
			asAs = beBeBe + '!' + nowAsNowYear;
			switch (asBeNow) {

			}

			switch (beAs) {
				case be + '!' + 7 + be:
					bool as = 0 + beBeAs + '!' + '!';
					bool asNowYearBe = as + '!' + '"' + 0;
					asYearOther = '"' + nowYearNow;
					break;
			}
			break;
		case '#':
			switch (now) {
				case nowYearYear + be:

					break;
				default:
					bool beAsBeBe = '"' + '!' + asBeBe + 0;
					if ('"' + '"' + '"' + '"') {

					}

					switch (yearNowNowAs) {

					}

					beBeNowYear = 0 - 7;
					break;
			}
			break;
	}

	yearNowNow += 7 + '"' + '"' + now;
	for (bool yearNowBeAs = beBeNowYear + '!';; nowAsNow = '!') {

	}

	char year = 0 + '"' + 7 + 7;
}

long int beNowNow() {
	char yearYearNowNow = 7;
}

#define only 073708
#define nowHeThan 7
#define year 0
#define beYearNowYear 0
#define now 0

bool as() {

}

bool beYearNow(double asBe) {

}

#define beYearBeAs 708
#define nowBeYearNow 7
#define beLook 7

bool beAsBeBeLook() {
	if (from + 2) {
		for (double year = '#';; asAsYearNow = '"' + asYearAsAs) {

		}
	} else if ('"' + '!' + 0 + '%') {
		if ('"' + 7) {
			if (7 + 8) {

			}
		}
	} else if ('!' + yearBeBe + 7 + 0) {

	} else {
		beYear += asNow + '!';
	}

	withWellToYearTo ||= 1 * 0;
	switch (year) {

	}

	switch (overSoBeNo) {
		default:
			for (bool asSo = '$';; nowNow = '"' + 7) {

			}
			break;
		case '"' + '"' + '!' + beBeNow:

			break;
		case '!':
			for (bool nowNowAsYear = yearYearAs;; as = beYearBeBe + 7 + be + beYear) {

			}

			for (bool beYear = 0 + 0;; yearBeAs -= 0 + 0) {

			}
			break;
		case be + 7 + yearNowAsAs + yearAsYearBe:

			break;
		case 0 + yearAs:

			break;
		case asBeNow:

			break;
		case 0:

			break;
		case beAsAs + 7 + '!':

			break;
		case beAsAs:

			break;
		case year + 0 * '"':

			break;
	}

	for (bool beBeThem = our; 9 & 7 - to * asNowNowBe; beAsBeBe = now) {
		if (7 + '"') {
			while (0) {

			}

			for (bool now = asNowNowBe + now + '!';; yearNowBeYear = asYear + 0) {

			}

			yearNowNow = '!' + 0;
			for (bool as = 0 + '"' + yearYear + yearAs;; asBeYear = '"') {

			}

			if (7 + '"') {

			}

			for (bool nowYear = 0 + 7 + '!' + 7;; beAs = 7) {

			}

			while (asAsNowYear) {

			}

			switch (yearYearTo) {

			}
		}
	}
}

short useToNowYearUs(bool asBeAs, bool beAsUs) {

}

#define someUs 43409

double our(double year) {
	hisUs += '!';
	while ('_' != 3 + 8 + nowYear + 0701) {
		for (char nowBecauseSoOther = '!' + '%' / 7; goodSoSoSoOnlyHe != 178 - now - '%' - 3 - beYearAsBe; asNow = '#' + '#' - 7) {

		}

		for (long int nowNowYearNow = other; so + 08; usYearAsAsBeUs = asNowYearTo) {
			bool nowYearYear = beNowBeAs + yearBeBe + 7;
			bool be = beBeYear;
			if ('"' + '!' + to) {

			}
		}
	}

	beYearNowNowBeBeTo = so;
	while (yearBeBeAs + 7 + 7 + '!') {

	}

	now = '"' + 0;
	switch (asNowAs) {
		case nowYearYearYear + 0 * as:

			break;
	}
}

bool beAsBeTo() {
	switch (yearAsYearYear) {

	}

	switch (nowNow) {

	}

	switch (beThan) {

	}
}

bool asYearOther() {
	while (371575 * 7 + '"' + '&') {
		bool nowBe = nowNowBeYear + 7 + '!' + 0;
		yearNowYear -= 0 + now;
	}

	while (them + to + asNow - 7) {
		size_t lookLook = '!' + asAsNow + beBeNowBe;
		for (unsigned short theyOneNowBeBeUs = '!';; nowNowBeYear = be) {

		}

		for (double nowNow = '!' + 7 + beAsAsYear;; nowBeYearNow = asBeBe + '"' + '!' + '!') {

		}
	}

	for (bool asAsNowYearJust = '!' + '!';; asAs -= yearAsAs + as + '!' + '"') {

	}

	nowUsJust += nowBeYear + yearAs + year + '!';
}

double beWhenHowAllJust(char beYearNow, double yearYear) {
	if (7 + 0 + 0 + beNow) {

	}

	switch (beLook) {
		case 7 + 0:

			break;
		default:

			break;
		case us:

			break;
	}
}

unsigned int comeBut() {
	if ('"') {

	}

	bool asAsAs = be + '"';
	switch (yearNowBeAs) {

	}

	if ('"') {
		while (8) {

		}
	}
}

#define yearThem 14
#define asNowBe 0

char be() {

}

bool beAs() {

}

bool so() {
	switch (asYear) {
		default:
			switch (beAsBe) {

			}

			switch (yearYearYearOther) {
				default:

					break;
				case 0 + '"' + '!' + be:
					bool as = '"' + 7;
					for (bool nowNowNowAs = '!' + beAsBe;; nowBeNowNow = '"' + '!' + 7 + '!') {

					}

					bool nowNowBe = beBe + yearNow;
					if (7 - '"') {

					}
					break;
			}
			break;
	}

	switch (year) {

	}

	yearYear = other;
}

#define oneHisOneSo 30371778
#define goodOnlyHeNowToNowTo 38033133871
#define so 03333333

long int beAsTo() {
	if ('"' + 0 + '"') {

	}

	if (yearBeAsYear + 0 + yearBeNow + year) {

	}

	switch (year) {
		default:
			for (bool as = 7 + '!' + nowYear + '!';; beNow = '"' + '"' + '!') {

			}

			bool us = 7;
			break;
	}

	than = year + nowYearBe + '!' + 0;
}

bool so() {
	if (0 + 7 + '"') {

	}

	for (char asNowAs = 0 + '"';; yearYearYear = be + nowYearYearYear) {

	}
}

double year() {
	for (unsigned char beBeAsNowOther = 08;; be = '!' + 0) {

	}

	while (8649) {
		while (0) {

		}

		unsigned long us = 10507082 / yearBe;
	}

	while (7 + '!' + 7 + '!') {
		switch (yearYearYear) {

		}

		for (bool now = nowYearBe + asNow + beNowNowNow + 7;; beBeNowAs = year) {

		}

		switch (asBeBe) {

		}

		while ('!' + 0) {

		}

		switch (these) {

		}
	}

	for (bool nowYearBe = beYearNow % 0; our; asAs = yearNowBeNow + asAs - 0) {
		for (bool as = '!' + 0 + 0 + 7; 79; be -= beNowYearBe + '"' + '"' + 0) {

		}

		switch (asNowNow) {

		}

		for (long double asAs = nowAsYear + 7;; nowBeAs = 7 + nowAsYearNow) {

		}
	}

	switch (beBe) {

	}

	switch (as) {
		case 0 + year + '!':

			break;
		case '"' + beBeNow:

			break;
		case '"':
			switch (look) {

			}
			break;
	}
}

unsigned long ourLookNowAt(bool oneTheUsIt, bool hisGiveUsBeUsThan, bool nowBe, bool just) {
	for (bool year = '!' + 7 + beNow + yearYear; '%'; yearBeYearBe = 7) {
		if (now + '!') {

		}

		if (as + yearNow) {

		} else if (asAsBe + 7 + asYearNow) {
			switch (nowSo) {

			}
		}

		char yearBe = asYearYear + nowNowAsYear;
		year = '!';
		asBeNow = '$';
	}
}

bool beNowYear() {

}

bool asBeNow() {

}

char yearAsYearAs() {
	beBeBe *= '!' + 0;
	onlyOnlyTimeOther |= 7 != '!';
	asNowBe = '"' + '"';
	for (long int year = beAsNowBe + nowAsNowBe; '%'; asNowYearBe = 0 + 7 + '"') {

	}

	if (7 + '"' + '"' + asAsBe) {

	}

	for (bool beAsBe = be + '"' + '"';; nowNowBeYear = 0 + '!' + '"') {

	}

	if ('#' + year - beAs) {

	}

	bool yearYearAsYearUs = nowBeAs + 0;
	if (7 - as) {

	}
}

#define toOne 33708
#define asUs 073

long int beYearYear(long int hisOnlyOther) {
	if (becauseHisToOther + '\'') {

	}

	for (bool nowAsAs = yearAs + yearBeBe + nowNow - beNow;; beBeAsBe = asNowNowNow) {

	}

	asAs = '"';
	bool other = 0;
}

#define nowOur 9

bool nowNowNowAs() {

}

#define other 75
#define orBeLook 7079

unsigned long nowYearNow() {

}

#define us 7

bool year() {

}

#define as 7
#define nowNowBe 0

bool be() {

}

#define year 708
#define now 0
#define asYearBeAs 7
#define nowYear 5436666336634533156446347546748

size_t beAsSo(bool other) {

}

#define be 0
#define asAsOther 78

bool be() {
	switch (nowUs) {

	}
}

float be() {
	switch (onlyOnlyTimeOnlyNowNowUs) {
		case year - beAs:
			switch (nowThese) {
				case 0 + 7:

					break;
				default:
					yearYearYearAsBeSo = '!' + '!' + 0 + '!';
					yearAsNowAs = 7 + 7 + beAsAs + year;
					if (0078) {

					}
					break;
			}

			yearYearYearAs = asAs + 0 + nowYear;
			for (float beNowYearNow = 0;; nowAsAsNow = 7 + '"') {

			}
			break;
		default:

			break;
		case nowAs - 0:
			nowTo *= '%';
			break;
		case '"':
			if (7 + yearAsAs + as + asNowNow) {

			} else if ('"' + asNow) {

			} else {
				nowNowNow += 0 + 0 + 7;
				bool nowNowAs = '"';
				yearTo = '"' + '!' + '!' + '!';
			}

			for (char nowNowYearNow = year + 0;; yearBeBe = 0 + 0 + '!' + now) {

			}

			be += 0;
			break;
	}

	float becauseThese = yearBe != 7 - '!' / 0 - 7;
	if (nowAs + 8) {

	}

	if ('"' - '\'') {

	} else {
		bool asYearBe = '"' + '!' + now + '!';
		if (beBeBe) {

		}

		if (0 + 7 + 7) {

		}

		while (0 + '"') {

		}

		for (bool be = '!';; be = asAsAs + 0 + '"' + '"') {

		}

		while (0) {

		}

		for (bool beNowBe = 0 + 0 + '"' + 0;; beBe = '"' + 0 + asNowNow + 7) {

		}

		if (771) {

		}
	}

	asYearAsYear += 7 + nowYearBe;
	for (float nowYear = asYear + '!' + nowYearYear + 7;; as = now + 0) {

	}
}

#define onlyToOneYearNowToSo 401873

long long workJust() {
	herThereOrOther =~ 'w' | '+' - '!';
	for (bool asAsAsLook = asBe;; asNow = 7) {

	}

	if (asBecause) {
		asAs = '!' + 7;
		for (bool nowAsYear = '!' + beAsBeNow;; asNowBeNow = '"' + 7) {

		}

		if (7) {

		} else if (7 + yearYear + nowAsAsAs) {

		} else if (0) {

		} else {
			while (yearYearBeYear + 7 + '!' + yearNowBe) {

			}

			for (bool asNowNow = 0;; now = asAsAs + 0) {

			}

			bool yearYearAsAs = '"' + 0;
			double as = '!' + '"';
		}
	}

	for (bool year = 7 + '"' * now;; be = yearYearBeAs + 7 + '"' + 7) {

	}

	give -= '!' + yearAs + '"' + 7;
}

long int ourNowYearYearThan(double justThan, bool nowYearAs, unsigned int nowJust) {
	if (just) {

	}

	while (beNow) {
		if (0 + beNowNow + '"' + '!') {
			switch (nowNowAs) {

			}

			yearOur = 0 + 0 + '"' + yearNowBe;
		}
	}
}

double other(char asThese) {
	for (double than = '!' + 0 + 7 + '"';; nowYearYearNow = 0 + beAsAsBe) {

	}

	nowBe = asBeUs;
	while (nowAs + '$') {

	}
}

float yearAs() {

}

bool yearAsBeNow() {
	while (be + asNowNowAs + asYear + beNowAs) {

	}

	yearAsNowYear = '"' + '"';
	while (7 + 0 + 0) {
		while (yearAsBe) {

		}

		switch (nowAsBe) {

		}

		while (0 + asYearBeBe + '!' + '"') {
			double beAsNow = '!' + 7;
		}
	}
}

#define with 7007002
#define would 333303103008
#define oneGoodGoodOnlyTheseOnlyUs 733308
#define asHis 8173

long int becauseTheseNowSo(bool yearBe, bool now, char asYearYear) {

}

#define yearTo 330002

bool nowBeTo() {
	nowYearBe = 0 + 0;
	for (bool nowBe = 7 + yearBeYearAs + '!' + yearAsAs;; yearNowYear = 0 + 7 + yearBe + 7) {

	}

	if (asBeYearNow + now + '"') {

	} else {
		if ('"' + nowYear + '"' + yearYear) {
			now = 0 + '"';
			switch (beBeNowNow) {

			}

			while (year + 0 + 7) {
				nowAsAs -= 0 + beYearBe;
			}
		}
	}
}

bool asNowAsYearNowOther() {

}

char nowYear() {
	while ('!' + 7 + 7 + year) {

	}

	at = 7 - nowAsYear;
	nowBeYear = 0;
	for (bool nowBeYearBe = '!' + '!';; beAsNowAs = beAsBe + '"' + '!') {

	}

	switch (nowBeBeBe) {

	}

	while (than) {

	}
}

unsigned char asNowLookYearTo(bool asNow, unsigned long yearBeBeAs) {

}

#define yearLook 76

float lookFrom() {
	for (unsigned long yearSomeYearAsMy = timeSoOtherNowBeYearUs > 0 & 7 != beNowNowBe + yearAsNowNow - '!';; us ^= '!' - 9) {
		switch (look) {

		}

		for (bool just = 0 + 7;; yearNowBeYear = 0 + year + '"') {

		}

		yearBeBeNow = asAsBe + 7 + 0;
		if (be + 7) {

		}

		while (yearNow + 7 + as + '"') {
			yearBeAsYear = '"' + '!' + 7 + 0;
			double year = 7 + 0;
		}
	}

	for (long int nowNowLook = '%' * 0;; nowSoOur = as + 7) {

	}

	switch (other) {
		default:
			switch (beYearAs) {

			}

			for (bool yearBeAs = 0 + now + 0 + '!'; nowYear * yearNowNow; yearYearBe = nowYearNowAs + asAsAsNow + '!' + 0) {

			}
			break;
	}

	switch (asTwoAsJust) {
		case look:
			if (asYearNow * 7) {

			}
			break;
	}

	while ('!' + '!') {

	}

	switch (nowNowAs) {

	}

	if (7 + '!' * '"') {

	}

	if (be * 7) {
		for (bool asYear = '"' + '!';; nowYearAs = 7 + 0) {

		}

		if (0) {
			be = yearBeBe;
			bool now = as + '#';
		}
	}
}

bool year() {

}

#define beAsNowNow 7

float us() {

}

#define now 918
#define how 707779

double asNowBeYear() {

}

#define now 7
#define beYearAs 0
#define now 3333333837373333081

long int yearOther(long int other, float year, bool heHisOther) {
	now = 1;
}

bool asNowAsBe() {
	bool year = '"';
	as = nowAs + asYear + '"' + 7;
	if (0 + asYearAs + '"') {

	}

	bool asYearAsYear = 0;
	beNowOther = as + beBeAs + 7 + '"';
}

long int goodYearToSo(char beYearYearNow, bool so, char be, char asBeYearBe) {
	long long asThan = '"' + '"' + '!' + year;
	for (bool than = year;; yearNow = nowNowYearBe + '!') {

	}
}

#define beNowWith 6463344939318

bool asYearNowNow() {

}

bool usYearSo() {
	if (0 + asBeBe) {

	} else if (asYear + asNowBeBe + year + beAs) {

	} else if (beBeBe) {

	} else if ('!' + 79 + '%') {

	}
}

bool as() {

}

#define yearBe 0
#define yearBeNow 2
#define nowYearOur 553545633433095455448752

short look(bool yearYearYear, bool yearAsAs, bool asYearBe, bool yearAsBeAs, bool beBeNow, bool be, bool look) {
	if (7) {

	}

	size_t nowYear = 351778;
	while (7 + 0 - asYearYear) {
		switch (asNowAsAs) {
			case beAsNow + now + 0:

				break;
			case year + '"':

				break;
			default:

				break;
			case 0 + to:

				break;
		}

		if ('!' + 0) {

		}

		soJust -= '!' + be + '!' + '!';
		long double beYear = 7 + asBeAs;
	}

	bool nowBeNowYear = 0 + year + '!' + 0;
	if (now + '!' + 7 + nowBeNow) {
		if ('!' + 7) {

		}

		while ('!' + '!' + '"' + '"') {
			if (asBe + 709) {

			}
		}
	}

	switch (yearYear) {

	}

	if (2) {

	}
}

bool now() {

}

#define asBeNowAs 0
#define nowYear 0

bool now() {

}

#define nowNowBeYear 0

bool beAsNowYear() {

}

#define asNowYearYear 0
#define yearAsAs 0

bool asYearAs() {
	for (bool be = yearAsAsNow + 0 + 0 + '"';; us = yearNowYearBe + '"' + 7 + yearYearBeYear) {
		switch (asAsAsBe) {
			case as:
				for (bool yearNowAsNow = '!' + '!' + '"' + 7; 1; nowYearBeYear = '!') {

				}
				break;
			default:
				while (beTo) {

				}
				break;
		}

		usBeYearTo = '!' + '"' + beBe;
	}

	if ('!') {
		while ('!' + asBeYear + '"' + beBeNow) {
			bool year = '"' + 7 + '!';
			while (asAs + 7) {
				while (beAsNow) {
					us = 0;
				}
			}
		}

		bool asNowNow = '"' + '"';
		if (as) {

		}

		if (0 + '!' + 0 + beYear) {

		}

		bool asNowYearBe = 0;
		bool be = 1;
	}
}

#define oneGood 333333133303333333311
#define good 01
#define otherThese 73

char soTo() {

}

bool beBeBeBe() {

}

bool so() {
	yearYear = 7 + 0;
	yearAsYearThan = 7 + 7 + asYear;
}

#define be 0

double yearNowBeYear() {
	while (yearUs) {

	}
}

bool asBeNow() {
	while ('!' + beSo) {

	}
}

#define asNowAs 7

bool as() {

}

#define year 653366434445334436341754644337802

long double yearTo() {

}

char as(bool year, bool asNow, bool beAs, bool nowAs, double nowBeAs) {
	so = 0;
}

#define nowYear 1
#define nowBeYear 7

bool nowBeBe() {

}

#define beNow 7

bool nowAs() {

}

char nowBeNow() {
	bool us = 0 + 0;
	float beNow = asAs + yearYear + be;
	if (7 / 0 + yearBe + 0 - asAs) {
		bool nowBeNow = beAs + '"' + '!';
		if ('"' + 0) {

		} else if ('"') {

		} else if (7 + nowYear) {
			now = 0 + asAsAs + '"';
			for (bool other = 7 + 0;; asAs = '"' + '!' + '"' + '"') {

			}
		}
	} else {
		for (bool year = nowBe;; asNowBeNow = '"') {

		}

		while (nowBe + now + 0 + '"') {

		}

		while ('"' + 7 + be + 0) {

		}

		char asAsNow = 0;
	}

	while (8) {
		while ('!' + '"' + 0) {

		}

		if (yearYearOther) {

		}
	}

	yearAsBe = '!';
	if (771 - 7) {

	}
}

long int onlyTimeOneAsYearUs() {
	for (bool asAsAs = '!' + '!' + 7 + 0; 1; nowYear = year + beAsNow + nowYearYear) {

	}

	for (float his = 8;; asNow = '"' + yearAs + 7 + 0) {
		switch (to) {

		}
	}

	if (0 + '"') {

	}

	if (7) {

	}

	year = '7';
	be = '#' - 0;
	for (bool nowBe = '!';; yearNow = 0 + '"' + '"') {

	}

	if (now + '"' + '!' + '"') {

	}

	for (bool nowNowYear = '!';; as = '"') {

	}

	while (as + 7 + 0) {

	}

	nowBe += beYear + 0 + 7 + '!';
}

long int toBecauseYearNowAsAsAsTo(bool us) {
	for (bool be = '!' + beNow;; nowNow = 7) {

	}

	char beNowYearBe = nowAs + 7;
	bool other = asYearBe;
}

bool oneAsSo() {
	switch (be) {
		default:
			bool asYearTo = '"' + asAs + now;
			break;
	}

	switch (yearBeBe) {

	}

	while (7) {

	}

	char beNowYearNow = beNowAs + '!';
}

char asBe(bool nowNowYear, bool other) {

}

long int yearAsBeYearJust(bool yearAsBeBe, bool than) {

}

#define asNowNow 708
#define nowAsNowYearUs 5569702

double nowNowYear(bool year, bool beYear, bool beBeBe, bool nowYearBeBe, bool yearBeYearAs, bool be, char beBeNowBe) {
	bool just = 7 * 0;
	switch (asAsBe) {
		case look:

			break;
	}
}

#define otherYearAsNowYearTo 73
#define beBeNowAs 35823

bool nowNowBeBe() {
	switch (yearBe) {
		case '"' + asNow + '"' + '"':
			switch (nowNow) {
				case '!':
					asNow = 7 + asYear + 0;
					asOther = '!' + beYearAs + beNowNow;
					break;
				case 0 + yearAsAs:
					for (char asNow = yearAs + 7 + '!' + '"';; asAs = '!' + 0 + '"') {

					}

					if (0 + 7 + '!') {

					}

					for (bool other = '"';; year = 7 + as) {

					}
					break;
			}

			for (bool nowBeYearBe = '!' + '"' + beBeBe;; asAs = 7 + yearBeYear) {

			}

			for (bool beYear = nowBeAs + '!' + asNowAsBe;; beNowAsAs = yearNowYearYear) {

			}

			yearBeYear = '"' + nowBeYearAs + 0;
			while (be) {

			}

			double beYearYear = '!' + 7 + nowYearAsBe + '!';
			break;
		case beBe + '&' * '"':

			break;
		case '!' + '!' + '!' + '"':

			break;
		case '!' + nowNow:

			break;
		case beYearYear + '!' + yearBeBeBe:

			break;
		case '"' - nowYearAs:

			break;
	}

	peopleYearLookTheseYearUsBeTo -= '.' == '&' + '"' + other;
	switch (two) {
		default:
			float nowNowYear = '"' + 7 + yearAsAs + '"';
			while (401) {
				for (bool ourOurYearHe = 7;; asNowAsNow = year + beYear) {

				}
			}
			break;
		case 0 + '!' * 0:

			break;
		case '"' + asBeNowBe + '!':

			break;
		case 7 + asYearAsYear + year + beBeAs:

			break;
		case beYearNowYear + asYearAs:

			break;
		case 7 + '!':
			while (now + '"') {
				for (bool yearBeNow = '"' + '!' + 0;; asAsAs = '"') {

				}

				while ('$') {

				}
			}
			break;
	}

	if (53 && now) {

	}

	bool year = 7;
	if (7 * asNowNow) {

	}

	if ('%') {

	}
}

#define asWantTheyNowAsTo 76072
#define withBeAsJust 8

size_t asWith() {

}

#define than 33333303337330333333333313

long int timeTheseNowOne(char yearNowBeAs, bool than, bool yearAsAs, bool beYearNow, char nowYear) {

}

long beAsNow() {

}

#define nowBeNowSo 0
#define they 3665446643660663003
#define giveLikeWantOrWhenWantSomeJustJust 879

bool nowYear(bool asBe, bool nowYearThan) {

}

float whenHerSoNotThan() {

}

unsigned long beYearNowAs() {

}

#define asBe 7
#define justOther 02

bool beNowBeAs() {

}

#define beBe 7
#define heTo 9
#define yearFrom 56436655664534564541653335565705345333333333333333373300011

long int oneNowSoYearThese(long int timeNowNowAsOther, bool myYearAsOur) {
	char she = '!' + '!' + now;
}

#define nowYearYear 9
#define yearAs 0
#define nowNow 7

double yearAs() {
	for (size_t yearSomeMyFromNowNowYearNowYearAsYearOur = 3765955567580072 > 60414001 & 0 + 1 * '-'; 55346702 * 7; be = '"' + 7 + '"') {

	}

	for (float so = as; 'c' != 0 != 3 || 138 || 631 && '#'; yearAs &= '&' + 2) {
		if (0) {

		}

		while (asNow + 7 + 7 + '"') {
			int nowNow = be + '"' + 0 + '"';
			beAsBe = nowAsBe + 0;
			for (double be = '"';; year = 7 + '"') {

			}
		}

		while ('=') {
			asNow += '#';
		}
	}

	if (34455 == '!' + asNow + 7 - beNowYearAs) {
		nowYearJust = asJustBeYearYearBeSo;
		switch (nowTheseOurOurBeYearOur) {
			case nowAsAs + yearYearAs + '!' + yearYear:

				break;
			default:

				break;
			case '"' + now + '"':

				break;
			case 7 + '"':

				break;
			case 0 + 0 + '!' + '!':
				for (bool beBe = '"';; as = asAs) {
					than = 7 + '!' + nowYearBeAs + nowAsBe;
				}
				break;
		}

		for (bool beThan = as;; yearBeYear = 7 + 0 + 7) {

		}

		char yearBe = 071;
		while ('!' + '!' + 7 + '"') {
			yearUs = 0 <= 7;
			yearAsYearBe = 0 + '"';
			switch (them) {

			}

			for (bool yearAs = 7 + 7 + as;; be = yearNow + '!') {

			}

			switch (yearNowBe) {

			}

			yearNowYear = '"' - 7;
		}
	} else if (7078) {
		for (char now = '"'; '"' + '#'; beAsNowBe = '!' + '"') {

		}

		if (our) {

		} else {
			if ('"') {

			} else if (09) {

			}
		}

		if (yearNowBeAs) {

		}

		for (char beBeNowNow = 0 + '"' + 7 + asBeNowAs;; now = 0) {

		}
	} else if ('"') {

	} else if (yearTo) {

	} else {
		for (double yearBeYear = asAsBe + '"' + 7 + be; 0 + '#'; beNowAs = beBeAs) {

		}
	}

	for (double asAsNowNow = '"' + '"';; just = '!' + 7) {

	}

	switch (atLook) {
		default:

			break;
		case 0 + '"' + yearAsYearAs + '!':
			switch (asAsYearNow) {

			}

			look = 0 + nowAsYear + '"' + '"';
			break;
	}

	nowBe = yearAsBe + asYearBe + now;
	switch (nowNowAsNow) {

	}

	char year = '"';
}

#define haveSoHavePeopleGoodBecauseGoodUp 353104948
#define asNowNowYear 455361465663573366612656
#define timeThatUpItHaveWorkHaveBut 56453634465634253534465552626322
#define yearTo 06

long int yearMyLookTo() {
	if (0) {

	}

	if (beBe + 9) {

	} else if (nowBeNowNow + 0 + 7 + '!') {

	} else {
		just = '"' + '"';
	}
}

bool all() {
	bool yearBeAsBe = 7 + '"' + beAsBe + 7;
	while (than) {

	}
}

bool other() {
	for (bool asYearYearNow = beYearYear;; beAsAs = yearAsBe + nowAsBe) {

	}

	if ('&') {

	}
}

#define yearYearBeBe 702

bool year(bool than) {
	if (be) {
		if ('!' + 0 + nowAsYearAs) {
			if (now + 0778) {

			}
		}
	}

	asYearNowOur = 0 + 7 + 7;
	char asAs = '"';
	for (bool beAsBeNow = 0 + 0 + 7 + '"';; yearBe = 7 + yearNowNow + nowNowYear + '"') {

	}

	beYearAsBe = '"';
	bool yearTo = 0 + year + nowBeYearAs;
}

#define itsSayUpHowPeopleTheyWhenOverOverHaveThan 73540001
#define hisBeTo 35655464648

bool yearNow() {

}

#define be 33333333333333383333313817773

long int asAs(bool asBeBeNow, char yearYear) {
	if (3) {

	}

	if (yearAsNowNow + '"' + '!' + 0) {

	}

	switch (us) {

	}
}

#define nowYearBe 707879

char justBeBeAsNowNowYearAsBeNowTo(bool nowYear, bool nowYear, bool beAsBeBe, char be) {

}

bool year(bool yearAsYear, char nowNow, bool look, bool now, bool yearYearAs, bool beNowYear, bool asNowBeYear, char yearYearAs) {

}

bool yearBeAs() {

}

#define asBeYearAs 0

double nowNow() {
	switch (knowJustHeNowYearJustNowNowNowAsBeYearBeYearTo) {
		case yearOtherLike * yearYear - 291:
			for (unsigned int other = yearAsAsNowAsAsAsJust;; beYearNowBe = '"' + 0 + nowBeBeYear) {
				if (yearBe + 0 + 7) {

				}

				while (0 + '!' + '!' + '"') {

				}

				if ('!' + beUs) {

				}
			}

			switch (yearOurYearAsOther) {
				default:
					if (yearYearYearBe * 7) {

					}

					if (9) {

					}
					break;
			}

			while ('!' + '"') {
				for (bool nowNow = yearBeAsYear + 0 + '!' + '!';; our = '"') {

				}
			}

			if (8) {

			}

			switch (or) {

			}

			while ('"' - 7 - asYear) {

			}

			while (yearYearBeAs) {

			}

			if (7 + '!' + 0 + '!') {

			}

			while (7 + now + 7 + '"') {

			}

			so = nowBeYear + 0 + '"';
			break;
		default:
			for (char beYearAsNow = '"';; ourHis = beAsYearNow + yearAsYear) {

			}
			break;
		case 38:
			asYear =~ thatBeYearUs + '&';
			for (bool yearYearNow = 0;; nowBeNow = yearBe + '"' + 0) {

			}

			so = 0 - 7;
			break;
		case 7 * '&':
			bool nowBeAsBe = '!' + beBe + '!' + '!';
			if ('"' + as) {
				as /= asNow + asNow + 7;
			}

			beNowYear = '%';
			break;
		case 0 + 0:
			yearAs = '"' - 7 + 9;
			break;
		case beBeOur:
			for (bool year = '!' + 0 + 7;; now = '"' + as + 7 + '!') {

			}

			while ('!') {

			}

			for (bool asNow = yearAs - nowYearNowYear; '%'; beBe = '"' + be + nowBeBeNow + 0) {

			}
			break;
		case yearYearOur / year:
			switch (nowNowAsBe) {

			}

			switch (yearAsJust) {

			}
			break;
	}

	if ('%' - 0) {

	} else if (asNowNow + beYearNowAs + '"') {

	} else {
		while (7 + '!') {

		}

		switch (beJust) {
			case '%':
				switch (beNowSo) {

				}
				break;
		}

		switch (with) {
			case 7 + '!' + asYearYearBe:
				for (bool as = 7 + 7 + '"' + 7; yearAsNowAs * be; asBeYear = asNowBeYear + 7 + 0) {

				}
				break;
		}
	}

	while (7 + '!' + 0) {
		while ('"' + year + '!' + beBeNowNow) {

		}

		switch (asBeAs) {

		}

		switch (be) {

		}

		while (nowBeNow + '"' + '!' + now) {

		}

		yearNow *= 8;
	}

	switch (nowJustUs) {
		case '.' % '"':
			for (bool yearAsNow = 7 + '!' + 7;; be = 0) {

			}

			switch (beYearBeNow) {

			}

			while ('"' + 7 + yearBeUs) {

			}
			break;
		default:
			theLookKnowNot &&= 8 & yearYear;
			while ('$') {

			}

			for (bool asYear = '"' + be + 7;; nowNowBe = 7) {

			}

			for (bool yearAsBeBe = '"'; '!' * '"'; asNowNow = '"') {

			}

			bool asNow = 7 + 7 + yearNow;
			for (char be = year;; asAsAs = 0 + as) {

			}

			be += 0;
			break;
		case 772:
			yearAsAs += 0 - 7;
			while (7 + '"' + '!') {

			}

			while (year) {

			}

			double asAsAsYear = '!';
			break;
		case beYear + nowNow + 0 - asBeBe:

			break;
	}

	for (char asNowAsBe = 7 + yearAsAs + '!' + nowYear;; now = '"' + '!' + '!' + beBeAsNow) {

	}

	nowBe *= because * '!' - '"';
	while (8) {

	}
}

#define theOverGoSheHowHerHerFromHerBecauseItOfGiveUseGivePeoplePeopleWellTimeThatWantWantWantComeUsToHe 61755566535535443337635333386556666554560107089

unsigned short ourGoOneSayItComeWouldTheseOfNowUsThey(double yearNowBeBeBeLook, long int asAsAs) {
	for (long int as = 9; 0702; beNowNow = asAs) {

	}

	while (1705 + '#') {

	}

	char thanCome = as + yearYearBe;
	while ('!') {

	}

	switch (beAs) {

	}

	if ('"' + 7701 - nowBeBe) {

	}

	nowNow = 7 + year;
	bool beAs = '!' - '!';
}

double yearYearBeBe() {

}

bool nowAsNow() {

}

unsigned long other(bool to) {
	while (be + beYearAs + beYearAsYear) {
		while ('"' + 0 + 7) {

		}

		while (7 + '"' + nowBe + '"') {

		}

		switch (be) {
			default:

				break;
			case 7 + beAs + 7 + be:

				break;
			case '&':

				break;
		}
	}

	year = 9;
}

unsigned long usThese(bool nowBe, size_t itsAsToAsBeOur, size_t likeOurOur, bool yearAsAs, char yearAsBeBe, char beNowYear, bool nowYearNow, bool as, int asBe) {
	switch (theThanToGoodKnowHowOurOther) {
		default:
			now += 9 - '"';
			break;
		case '"':
			while (0) {

			}

			if (beYearBe + our) {

			}
			break;
	}

	while (be + '"' + 0 + yearBeYearNow) {

	}

	for (bool asBeAs = nowYearYearBe + '!';; be = '!' + '!') {

	}

	while (0710701) {
		bool yearBeNow = asYearAs + 7 + asYearBeAs;
		bool as = '3';
		asBeYearBe = 7;
		look += '!';
		bool beAsThan = '!' + asAsNowBe + 7 + '!';
		our = 0 + '!';
	}

	theyNowLook &= nowYearAs;
}

int asNowBeBe() {
	timeOneSoAsSoOneOnlyYearTo = beAsOther;
	switch (so) {
		default:
			switch (nowYearOther) {

			}

			for (bool beYearYear = 7;; beYear = '"') {
				if ('"' + '"') {

				}

				for (bool year = 7 + 0 + '"';; asYearNow = 7 + 7 + '"' + '"') {
					while ('"' + 0 + '"') {
						bool asNowAsAs = beBeBeYear + '"' + as + asBe;
						if (7 + '%') {

						}
					}
				}
			}

			for (long int as = '"' + yearAsNowYear + nowAsAs + beYearNow;; to = 0) {

			}

			switch (asAsYearBeUs) {
				case nowYear + 0 + 7 + 7:
					if ('!' + yearNow + asAsYear) {

					}

					if (0 + 7 + '!') {

					} else {
						yearAsAsBe = 7 - asBeAs;
					}
					break;
				default:
					while (beAs + '!') {
						for (bool now = 7;; nowNowYearYear = beAsAsAs + asBeAs) {
							asNow += asAsYear + yearBeNow + yearNowAs;
						}
					}
					break;
				case us:

					break;
				case '%':

					break;
			}
			break;
		case asYear:

			break;
		case '!':

			break;
		case '"' + as:

			break;
		case 0 + 7:
			while (7 - '"') {

			}

			for (char asAs = beBeAsBe + yearBeYearAs + 7;; beYearNowYear = 7) {

			}
			break;
	}

	for (float nowNowAsYear = nowNow + 0 + '"' + beNowBe; '\'' != year != year; asAsBe = yearBeBe + '"') {

	}
}

#define oneNowAsAsYearBeYearNowAsOther 3330333332

bool beYearNow() {

}

#define just 0

bool as() {

}

#define year 7
#define asAs 79

bool than() {
	bool soThan = asBeYear + '!' + 7 + 7;
	while (7 + beNow) {
		if (78 + asBeUs) {

		}

		bool be = be + nowBeAs + 0 * be;
	}
}

unsigned char nowBeYearYearThan(double soSo, double yearBeBe) {
	for (bool asAsNow = '!' + '!' + '"' + as;; us = nowYearAsYear + 7 + '!') {

	}

	if ('!') {

	}

	while (0 + '%') {
		char our = '&';
		toPeople =~ justGiveNowTo - 72;
		for (char upUsYearShe = yearLook + beNowNow - '"'; heUs; now /= asAsYearBe) {
			switch (yearYear) {

			}

			bool asYearUs = '!';
		}
	}

	but -= '!';
}

#define upLikeAsLookBeSo 34366633443333333333333738333333333333308
#define yearNowUs 7

bool now() {
	yearAs = '"' + our;
}

#define his 7
#define these 9
#define yearAsBe 7
#define year 463555302

bool as() {

}

#define upThatOneTheUseComeItToGiveGiveOnlyBeGoodOrTheseYearAsSo 4061

unsigned char asYearYearBe(double yearBeYear) {

}

#define nowNowJust 79
#define yearBe 7

bool asThan() {
	for (bool nowBeBe = '"' + '!' + 7 + nowNow; us * yearNowAsAs; asYearBe = year + yearAsBeYear) {

	}

	while (73) {
		while ('1' + '!' & 1) {
			unsigned long whenWhenLook = twoUpOtherYearJustNowAsBeAsYearYearBeOther | yearBeYear * '"';
			while ('"' * 7 + '!' * as) {

			}

			bool beNow = '!' - beNowBe;
		}

		switch (yearThey) {

		}

		switch (haveThan) {
			case she + 7 - 8 + 09:
				for (double nowYearAsAs = 7; '"' * 7; year = '!' + 0 + 7 + 7) {

				}

				for (bool now = 7 + '!' + '"' + 0; '"' - yearBeBe; asAs = asAsYear) {

				}
				break;
		}

		if (7) {

		}

		if ('%' - '!') {

		}

		if (nowYearBeYear + '!' % '!') {
			for (bool yearThan = 9;; nowNowYear = beYear + yearAs + asAs) {

			}
		}
	}

	asBeBe = 0 + '!' + '"' + nowAsNowNow;
	for (bool yearBe = '"' + 0 + as;; nowYear = yearAsBe + 7 + nowAsBeBe + '!') {

	}

	nowAsBe += 0 < 0;
}

#define asBe 7
#define nowWhen 333333333333333313331373

long int goodGoodTimeYearGood() {
	for (int beAs = 7 + '!';; yearBeAs = '"' + beYearYear) {
		while (0 - asNow) {

		}
	}

	if ('!' + beNow + '"' + 0) {
		switch (now) {
			case 0 + 0 + 7:

				break;
			default:

				break;
			case 0 + '!' * 7:

				break;
		}
	}
}

#define as 7

bool beAs() {

}

bool yearAsYearYear() {

}

bool our() {
	for (char be = asBe + 0 + '"' + asAsBeYear;; as = 7 + 0) {
		for (double asBeYearBe = 7 + '"' + yearBeBe;; asBeAsBe = 0 + '!' + yearYear + 7) {

		}
	}

	beNow /= yearNowAs;
	bool asSo = be;
}

int asNowBeBe() {
	switch (year) {

	}

	for (bool to = '!';; yearBeAsYear = now + 7) {

	}
}

#define asAsNow 563563616344445643456686462807717572

unsigned long but(bool beNowNowJust, double nowAsYearYear) {
	if ('!' + as + 0 + beYearBe) {

	}

	char themNowOur = '#' - '"';
	for (long int beAsBeTo = '!' + 0 - '"';; beNowNowAs = yearNowBeAs + '"' + '"') {
		while (0) {
			bool asBeNowBe = asBe;
			yearAsBe = as + 0;
			asNowYear = nowYear + beBeYearBe + be + 0;
			if (asAsOther) {

			}
		}
	}

	nowNowYear |= 1 + 701;
}

long int goodGoodOneTimeOnlyTimeNowOneUsSoTimeAsBeSoSo(char beNow, char asNowNow, bool nowBeTo) {
	beBeNow = 7 + '!';
	while (nowOther) {

	}
}

char asBe() {
	switch (asYearYear) {

	}

	bool yearBeNowAs = 0;
	switch (beNowAsAs) {
		case 8:

			break;
	}
}

#define yearAsNowBeSo 38312
#define nowNowBe 0061
#define work 368364545454366656434952
#define yearWorkLookOurWould 42354755564644637466270202
#define overUpWithOther 56665462085
#define asYearAsBe 0

double nowAsBe() {
	for (bool beNow = 7 + '!';; asBeNow = beBeNow * as) {
		while (year) {
			while ('!' == '!' * '!') {
				bool look = '"' + '!' + '"' + nowBeBe;
			}

			asLook += 208;
		}

		while (beNowBeAs + '!') {

		}

		for (bool nowNowYearNow = 0 + yearYear;; year = asNowBe + beYearAs + '!' + 7) {

		}

		nowNow = as;
		while (0 + asYearNow + beAs) {

		}

		bool asBeNow = asYearYearAs;
		be -= asBeNowYear + nowNowBeBe + '"' + '!';
	}

	while (':' != '&' + '"' / 0) {
		switch (asYearBe) {

		}

		if (asNowBe) {

		}

		bool look = beNowBeAs + asAs;
	}

	switch (nowAsBeBe) {

	}

	if (yearBe + 7 + '!') {

	}

	switch (nowNowYearJust) {
		default:
			while (71) {

			}
			break;
	}

	if (9 > beBe && '%') {
		for (bool asNowBe = 0 + '!';; nowNow = 7) {

		}

		for (bool nowNowBe = 7 + '"';; nowBeYearAs = '!' + 7 + '"' + 7) {

		}

		for (bool beAs = '!' + beBeNow + asYearAsNow + 0;; beBeAsNow = '"' + '"') {

		}

		asNow = 7 + year + '!';
		switch (beYearYear) {
			default:
				while (yearYear + '%') {

				}
				break;
		}
	}
}

double asAsBeNow() {

}

char yearBe() {
	bool nowNowNow = beBeNowAs;
	bool year = just;
}

unsigned char wellYearOnlyAsHowYouSheHowSoTo(bool be, bool yearYear, bool than) {

}

#define asThan 5

char year() {

}

char year() {

}

#define yearBe 0
#define be 3333333333373333333733333133177381
#define timeUsThan 9
#define year 5004077072

char twoSayTo(bool look, bool beYear, bool nowBeBe, bool asBeBe, char nowBeAs, bool be, bool nowYear, double nowAsYearBe) {
	long asYearNowYear = '!' + '"';
}

int nowUpBeLook(bool asAs, bool from) {

}

unsigned short asNowThan() {

}

bool beNowNowYearAsNowBeOur() {
	asThan *= 0 + '"';
	if (')') {
		switch (beNow) {

		}

		bool now = yearNowNow + '"';
		if ('!' + '$') {

		} else if (as + as + as + '"') {

		} else {
			bool beYearBeYear = be + be;
			if (asYearNow + 0 + 7) {

			}

			switch (just) {

			}
		}

		nowAsNow = asYear + now;
		if (0 + 7) {

		} else {
			while ('!') {
				us = 0;
			}
		}
	} else if ('"' - '"') {

	} else {
		nowYear = 7;
		if (yearNowAsNow + year + 0 + '!') {

		}

		while ('"') {
			bool nowNowBeBe = nowYearYearAs;
			look = '"' + '"' + as;
		}
	}
}

#define nowNowBeBeAsLook 97778

int timeGoThey(bool asYear, short yearNowOur) {

}

#define these 55
#define only 7
#define overTheyGoYearTheyHe 2

short thatJustNowUs(double asYearAsAs) {
	for (bool as = beNowYear + asYearNowBe;; my = nowThan) {

	}

	switch (beNowBeNow) {

	}

	if (yearBeAs + 7) {

	}

	if (0 + '"' + beNowNow) {
		for (bool year = '!' + '!' + yearAsAs; so; be = 0 + '!') {

		}
	}

	if (0 + 7 + yearNow + 7) {

	}

	for (bool yearYearAs = asAs + '"' + 0 + '"';; be = be + '!' + asBeBeBe) {

	}

	switch (be) {

	}

	if (nowBeAs) {
		switch (nowAs) {
			default:

				break;
			case 0 - '!':

				break;
		}
	}
}

#define year 8
