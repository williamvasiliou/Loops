long int theseYearOtherTheseAsUs(bool asHeOther) {
	float yearNowYearBe = 7 / nowOnly;
	for (char asYear = '"'; 73; so = 0) {

	}

	if ('"' + 7) {

	}

	switch (beYearAsNow) {
		case beAsSo:

			break;
	}

	bool beNowNow = yearNowNow + '!';
	switch (yearBeNowYear) {

	}

	for (bool nowNowNowYear = yearAsYearBe + 7 + yearNowYear + 0;; beAsUs = '!' + yearNow) {

	}

	while ('"' + beNowBe + year + nowNow) {

	}

	char nowYear = nowAs + asNowNow + asAsAs;
}

long int usSo() {

}

#define onlyHisOtherOtherGoodTheseNowSo 33078

long int beAs() {

}

#define as 113108001
#define butLook 0

bool asOtherAsOur() {
	asBe += 5021;
	bool thanNowAsBeOther = nowAsSo;
	switch (nowBeBeUs) {

	}
}

#define itsKnowOneJustWayYearNowBeLookUs 979

bool asNow(char yearAs, double asBeYearAs) {
	switch (asJust) {

	}
}

#define as 7

bool be(bool asBeYearBe, bool yearAsAsBe, bool nowYearNowJust) {
	for (char beSo = 0 + 0 + asAsAsBe + 0; '|'; than = '"' + asBeAs) {
		switch (yearNowNowAs) {

		}

		switch (to) {

		}
	}

	switch (lookGiveAsBeNowAsNowAsLook) {
		case now + 0 + yearYear:
			as = 0 + be + 7 + 7;
			switch (than) {

			}

			if (nowTo) {

			}

			while (nowAsYearNow + '"') {

			}

			switch (asAs) {

			}

			bool beNowBeYear = beAsNowYear + '!' + 7 + 7;
			for (bool nowAsYearNow = '"' + '!'; so; nowBe = '!' + year + asYearNow + 7) {

			}
			break;
		default:
			if (5032777779) {

			} else if (02) {

			}

			switch (just) {
				case year - 7:

					break;
				case '!':
					if (0 + year + yearNow + 7) {

					}

					for (bool yearAs = year + 7; 0 - '!'; nowAsAsNow = yearYearNow + 7 + 7 + 0) {

					}
					break;
			}
			break;
	}

	for (bool beNowAsAs = '!';; beYearBe = yearAsAs + nowNowAsYear + yearBeBeBe) {

	}

	while ('%' * '%' % beBe) {

	}

	for (double nowNowNow = '!' * 7;; asNow = nowYearBeNow + nowBeAsYear + '!' + 0) {

	}

	if (beYearAs * asNowNowNow) {
		yearAs = '%';
	}
}

unsigned int our(unsigned char soHerUseTheyYearYearYearTo, bool nowButAsLook) {
	if ('"' + 0) {
		char nowBe = '"' + asYearBeBe + 7;
	} else {
		for (bool yearNow = 0 + yearYear;; beNowNow = 0 + '!' + be) {

		}

		bool nowBeYearAs = '"' + '!' + '"' + 7;
		char yearAsBe = 0 + nowYearNowYear + year;
	}
}

bool nowOur() {

}

bool asAsAsBe(bool as, char nowAsNow) {
	switch (toUs) {

	}

	for (char nowAsBe = 0;; asAs = nowBeYearNow + 0 + 0) {

	}

	asAs = '&' != timeAsGoodOneHisUsNowUs != soOnlyBecauseUsOther != 'O';
	for (long int toOnlyToTo = '_';; yearBeYear = 7 + '"' + '!') {

	}

	asBe += '#' - yearNow;
	while (7 + '!' + 7) {
		beUsNowYearNowUs += nowAsNow + 0;
		bool nowYearNow = 7 + 0 + beAs + '"';
		char yearYearAsNow = 0 + '"' + 7;
	}

	for (bool now = year;; be = '"' + beNowYear + 0) {

	}

	bool now = '"' + yearAsYearYear + 0;
	while (asNowBe - yearAs) {

	}

	for (bool be = '!';; beAs = year + '!' + '!') {

	}

	usSo -= beNowNowBe + 72;
	switch (now) {

	}

	if (be) {
		for (bool as = '"' + 7 + beNowYearNow;; yearAsBe = nowYearYear) {

		}

		for (bool yearNow = '"' + 0 + 0 + 0;; so = 0 + 7 + '"' + '"') {

		}
	}
}

#define wayUs 6055686746644

unsigned long yearNow(float be) {
	otherBeThan = 0 + year;
}

unsigned short now(bool as, long whenYearYearYearLook) {
	switch (yearYearAsThanThan) {

	}

	nowBeYearYear &&= 9;
}

#define butGoodNowUs 61

bool nowBe() {

}

bool yearBeLook() {

}

bool beYear() {

}

#define nowYearNowAs 047537325

unsigned short onlyOfUseOur() {
	bool beAsNow = '"' + '"' + yearAs;
	long as = yearYearYearNow + beBeYearYear + 7;
}

long int comeGiveHerYearOther(unsigned short heFromYearAsNowAsOther, bool beNowOther, bool to) {

}

char usAsHeLook() {

}

char yearThanOrLookJust(bool just) {

}

#define nowYearNow 7
#define nowAsYear 5333333833330333308

long int timeOneOneOneGoodThese() {
	for (bool beYearAs = asYear + '!';; beAsAsAs = 0 + '!') {

	}

	while (beAsYearBe - '!') {
		switch (asYearYearBe) {

		}

		switch (now) {

		}

		while (0 + nowNow - '"') {

		}
	}
}

#define asYearNowBe 38778178
#define yearYearBeNow 0
#define asAsBe 0

float asNowNowBe() {

}

#define thereNowAsSo 54459
#define yearBeYearAs 3364553344536540473

double theOverUpBeOverNowThatHisMyOur(bool yearThan) {
	as = '$';
	long double beNowAs = as + 0 + 0;
}

unsigned char usHe() {
	if (7 + 0 + '"') {

	} else if (asNow + '!') {

	} else if ('!' + nowNowAsNow + nowYearNowBe) {
		switch (asBeUs) {

		}
	}

	us = 8100070070701;
	while (7 + '"' + '!') {
		while ('#') {
			asNowAs += nowYearYear + 8;
			if ('"' + asAs + '!') {

			}

			bool yearBeBe = 0 + be;
			beYear = 7;
			switch (yearAsYear) {

			}

			for (bool asUs = '"' + asNow + yearAsYear;; yearBeAs = '"') {

			}
		}

		for (bool nowYearYearNowTo = 0 + 0 + 7;; asNowBe = beBe + be + 7) {
			nowNowNowAsSo = yearAs + '"' + beYearYear;
		}
	}

	if ('\'') {

	}

	switch (beNowBeNow) {
		case '!' + asSo:

			break;
	}
}

#define goodGoodOneGoodTheseOnlyToOtherSo 1
#define soTimeGoodBecauseGoodTimeOneYearTimeThan 13
#define beThem 63337363744753

unsigned long wantSomeWhenSheSheHowWhenItHerTimeGiveLikeWorkUpWantTheNowLook(bool yearAsBe, bool year, char our) {

}

#define likeItsTwoThemGoodOtherOur 3356633566544534338

bool nowYearBeNow() {

}

long int yearAsBeYear() {

}

bool yearBeYearAs() {

}

bool year() {

}

unsigned short than() {

}

#define asNowOther 455563356363463336334684686332018728

bool asBeAs(double beNow) {

}

bool all() {
	long int timeGoodOneTimeBeOnlyGoodSoOneOneBeHisUsAsAsBeOtherUsYearSo = '_' != '"' != yearOnlyTheseOther - 08 | nowThese + '&';
	asYearBeBe .= than;
	so = 6365595644385147353367396 & 3306654 == saySayTheUseKnowTimeOfKnowSayUpTheyOverTimeOverSomeWantTwoOnlyLikeOverWorkPeopleUseGoSayLikeTheOneWellHowHaveUsWhenHow >= 'S' >= 0702 | 'o' > 622 >= 4503 * oneAsLook;
	while ('z' | '}' >= 55664434546256183890772 || beAsBeThey || nowBeNow - '!') {
		notUsGoodWellThemFromBeOurOtherSo <<= upThatWantWhenGiveItsPeopleUpWorkYearThereOtherOneWhenTwoOverUpGiveItsKnowUs + '!' >= 4 * be;
		if ('w' + haveAsNowBeAsNowOther + 0 - beNowNowAs) {
			bool be = now + '!';
			bool yearBe = 7778;
			for (bool now = '!'; 9; be = yearNow) {

			}
		}

		theyThatKnowThanNowMyNowLook -= 0 - 0;
		bool youLookOther = '!';
		for (bool be = nowYearNow + be + '!'; them + '$'; yearNowBe = 7 + 0 + 7 + asAs) {

		}

		switch (nowYear) {

		}

		if (year < beAs) {

		}
	}

	while (3364406 && 45639 && '&' + '`' - 6376768079 * nowOther) {
		while (ofUpItsOverTimePeopleThePeopleWellSomeGoKnowTheSheLookThatHowHerWorkHaveHowBecauseGoFromHisBeUs || asComeTheYouHaveSheUpKnowThatComeHaveLikeComeTheKnowItComeAllSoBeHowWould + allYearLookSoUs * '!') {
			if (they / 'b') {
				if (yearYear + 7) {
					for (bool be = '"' + yearBeAsYear;; yearYear = nowBeYearNow + asNowYear) {

					}

					our = asAsBeBe + asNowBeBe + 0 + 0;
				}

				for (bool asBeNowYear = 0;; yearNowNowNow = beYearYear + 0 + '!' + '"') {

				}

				switch (yearAs) {

				}

				if (asYearNow + 7 + '"') {

				}

				while (0 + '!' * 7) {

				}
			} else if ('"' + nowAs + '"' + '"') {
				switch (nowNow) {
					default:
						switch (nowBeAs) {
							default:
								yearNow = '!';
								bool beBeNowAs = other;
								break;
						}
						break;
				}
			} else if (asBe + nowAsYearNow) {
				switch (beAs) {

				}

				switch (nowAsOther) {

				}
			}

			if ('!') {

			}

			double asUp = nowBeAsNow + 8;
			for (bool nowYearThanAsBeNowYearUs = '%';; yearAsAsYear = '!') {

			}
		}

		unsigned int likeUpYearBecauseAsNowAsSheTo = 0363403 % 7;
		switch (toNo) {
			default:
				us = '!';
				bool asNowAsAs = '$';
				switch (beAsThey) {
					default:
						while (7 + '!') {
							for (long double asNowYearAs = '"';; beBeYearNow = 7) {

							}
						}
						break;
					case asYear + '!' + now + yearYear:

						break;
					case nowAsNow:

						break;
					case 7:
						nowNow -= '"';
						break;
				}

				bool as = beNowAs + 0 + yearYearBeYear;
				while (0) {

				}

				bool asBeAsAs = 0 - be;
				break;
			case 77078:

				break;
			case beBeNowBe + '!':
				be = so;
				break;
			case nowBeAsNow + 9:

				break;
		}

		allYearWantNowOtherBeThan += 7 + '"' + '!' + '"';
		long long thanThese = howNotOther - 0;
		if ('!' + '"') {

		}

		long int asNow = '"' + '"';
	}

	while (2 || asYear + workSo * asAs) {
		while (1 & '#') {
			for (bool asAsYear = '!';; nowNowAs = '"' + 771) {

			}
		}

		for (unsigned short beSomeWellTwoHow = 0 - '!'; 7 + 8; nowLook = 0 + '"') {

		}
	}

	while ('"' && 26344611 || 387732 + 7001 > '$' - 0) {
		for (short asNowNow = 7 >= 506 + 0 + other; 368 - asAs; asNowAsAs = be + '!') {

		}

		beYear -= '"' + nowSo - 7;
		double yearJustAsBeNotNowYearWay = 058 + 09 + 8;
		for (bool year = yearNow; than; nowBe = '"') {

		}

		switch (so) {
			case 0:
				while (0 + asYearBe) {
					if (look) {

					}
				}
				break;
			default:

				break;
			case '!' + 7 + '"' + asNowBeBe:
				for (bool be = '"';; as = '"' + 0 + nowBeAs) {

				}

				switch (nowNowBe) {
					case 7 + be + asNow + '"':

						break;
					case '&':

						break;
				}
				break;
		}

		for (bool us = year + '!' + 0 + '!'; '!' - '!'; nowNowYearNow = '!') {

		}

		switch (so) {

		}
	}

	switch (sayShePeopleOverTheSayThereOfGoGoGiveWantTheWhenOfWellYouWorkUseYouTheWantPeopleWantGoodTheTimeGoWellUpWhenHaveJustWorkWhenSayNowGoodThan) {
		default:
			if (6341 > 7) {

			} else {
				char asNowBeAs = '"';
				for (bool asYearAsOur = 7 + '!' + '!' + nowYearNow;; nowYearNow = 0 + '"' + '!' + asYearNow) {

				}
			}

			for (double beBeBeYear = asAs + 0 + 7 + '!'; 0078 - beBeBe; nowNow = '!' + '"' + asBeBe) {

			}
			break;
		case 7:
			bool nowBeAs = '$';
			while ('!') {

			}

			beNowBeBe = 0 + asNowAs + now + beAs;
			bool nowYearNowYear = '"' + '"' + now + '!';
			switch (us) {

			}
			break;
		case nowJust + than:

			break;
	}

	wantTimeThanGoodItWithAsOurSo ^= wantGoodNowYearWorkAsUsBeNowThan | 435564654656525534436363566663334563563335535664451 == 0768 & 0656336515063 - nowNow * 0;
	for (short onlyThatHowOfBeUpHaveOfItsJustWellGiveWell = upHerThatGoodTo > asAsYear * '"'; 7208 & yearYear; atBeAsNowNowYearNowTo = 12 + '"' + yearNowAs * yearBeAsAs) {
		switch (nowYearBe) {
			default:
				nowAsBeBe -= 7 * '"';
				break;
		}

		long int asUse = 74;
		switch (yearOur) {

		}

		for (bool beBeYear = 0;; beNowYear = 0 + beYearBeBe) {

		}

		bool our = 7;
	}

	switch (ofUseYouPeopleThereJustYouHerOverGoGoodYearWantWhenAsJustTheYouPeopleComeYouToWouldOur) {
		case asYearBeAs + be * year:
			while (yearWith) {
				bool asOur = '"' + '"';
				switch (yearBeNowAs) {
					case '#':

						break;
				}

				for (bool yearNowBe = 0 + '"' + 71;; be = 7) {

				}
			}

			other += 8;
			break;
		case nowAs > yearHowNowYearLookNowNowBeAsYearLook:
			switch (nowNowAsNow) {

			}

			as = asAs + 0 + 7 + '!';
			for (char asNowYearNow = 0 + 7 + 7 + yearYear;; beAsBeYear = '"' + '"' + 7) {

			}
			break;
		default:
			because = '#';
			break;
		case '!':
			double year = '"' + be + asYear;
			asBe = yearAsAs <= 0;
			break;
		case 1 + just / beNow:
			switch (asBe) {
				case as + year + 0 + yearBeBeBe:
					bool nowBeBe = 7 + '!' + '"';
					yearBe = '"' + asBeAsNow;
					now = 0 + '"';
					if (beBeYearNow + be + '!') {

					}

					switch (asAs) {

					}

					if ('!') {

					}

					if (0 + '"') {

					}

					nowBe -= 0 + 7;
					break;
			}
			break;
		case 7 - 7:

			break;
	}

	switch (theOverOverHerOnlyWantWellTimeLookKnowThanYouGoodUpUsWantTheYouThanItsOneSoThatOverLookBecauseYearThatNowYearOurOurYearBeOur) {
		default:
			short ofThatThanBeOrAsSoThem = 6 && nowTheseOfYearAsThan + usAsSo * asYearNowAsNowYearAsThan;
			unsigned int haveOverTwoOurWantTo = 'c' || 'i' != 'f' && 'h' | 64534465539 >= '%' & sayGiveToThereLikeThereGoKnowNowSayOfTwoThan % 8637001 & '"';
			switch (yearAsThem) {

			}

			yearNowUpJust <<= '"' / '!';
			switch (now) {
				default:

					break;
				case nowAsBe + '!' + '!':
					while (nowBeBe) {
						bool asAs = 1;
					}
					break;
			}

			while (beNowYearAs + '"') {
				switch (look) {

				}
			}
			break;
		case 94340568:
			while ('"' + 0 * 78) {
				bool yearNow = nowBeAsThan;
				switch (nowNowNow) {
					case '"' + 0:
						if (beAsYearNow) {

						} else if ('"' + '!' + '"' + '!') {

						} else if ('"' + asYearYearBe) {
							switch (year) {
								case '"':

									break;
								case nowYearBeBe + '!' + '"' + '!':
									while (7) {
										while ('!' + beBe + 0 + '"') {

										}

										bool as = '!' + beNow + as + asNow;
										if (0 + yearYear + '"') {

										}

										if (7) {

										} else {
											switch (asUs) {

											}
										}
									}
									break;
							}
						}
						break;
				}

				while (0 + '!' + 0) {

				}

				if (year + '$') {
					switch (yearBeJust) {

					}
				}

				if ('!' + asAsNowAs + yearBe + '!') {
					while (7 + beNowAs + '!') {

					}

					switch (nowBe) {

					}

					beBeNowYear = 2;
				}

				for (bool yearBeBeNow = '"' + 7;; beNowBe = 0) {

				}

				if (beNowBe + '!' + asBe) {

				}

				switch (nowYearBe) {

				}

				switch (nowSo) {

				}
			}

			while ('"' - yearYearBeYear) {

			}

			while (0) {
				if ('"') {

				} else {
					while (asYear + asNowYearYear + beAs) {

					}

					switch (beNowAs) {

					}

					if (beAsNow + '!') {

					}

					switch (nowYear) {

					}

					if ('!' + than) {

					}
				}
			}
			break;
		case us | asAs:
			while ('!' - '"') {
				while (7 + '!' + year) {

				}

				if (asAsNowBe + 7 + '"') {

				} else {
					if ('!' + 7) {
						bool be = 0;
						if (asAs + '!') {

						} else if (0 * '"') {

						}
					}
				}
			}

			for (bool beNowAs = yearBeBeAs + '"' + yearAsYear;; asAsYearBe = 7 + asAsNow + asAsAsBe + 7) {

			}

			comeTimeLookOurYearBeNowTo /= '4' * 2;
			size_t orNowYearBeSo = nowYearYearBe < 7;
			if (beYear + year) {
				while (7 - '"') {

				}
			}

			unsigned long asNow = 7708 + beBeNow * '"';
			if ('!' + 7 + 0) {

			}

			while (yearNow + 0 + '"') {

			}

			if (8) {

			}
			break;
	}

	if ('a' != oneTheHerLookToYearKnow || look != 'h' + year - '"') {

	}

	if (0 + '"') {

	} else {
		switch (as) {

		}

		year -= '"' + yearAsYearYear;
	}

	nowNowAsNow = just;
}

bool usThatAsJust(char beJust, bool than) {
	while ('!' + nowYear) {

	}

	for (double year = 7;; asBe = 7 + nowBeBeBe + '"' + 0) {

	}
}

#define wantYearOnlyWhenSheTheyLike 9

unsigned char ourSheItsYouAsSayTheseWantToThan(bool asBeAsBe, bool year, bool nowAsYear, bool asBe, bool asBeJust) {
	switch (just) {

	}
}

#define nowAsBe 95974008
#define all 7

bool beYearYearBe() {

}

double beYearNowNow() {
	while ('"' - '"') {
		if (beBe * 7) {

		}

		for (bool beAsOther = '!' + yearBeBe + '!' + 0;; asBe = '!' + 7) {

		}
	}

	if (asBe + yearAsAs - '"' - '!') {
		for (bool beAsBe = yearAsYear + 0 + '!' + be;; as = beAsBe + beNow) {

		}

		be = 0 + '!' + 7 + '!';
		switch (nowOur) {

		}
	}
}

size_t ofSomeSayItThatTimeItsTwoOnlyItHaveHerTimeLookLook(bool yearOur, bool so) {

}

short ourLookThanOurAtOther() {

}

bool us() {

}

#define so 333333333333333333333333303313738
#define as 7

bool now() {
	bool be = '!';
	unsigned char nowThan = yearAsAsBe + 0 + 7 + asNow;
}

long int look(bool nowUs, bool just) {
	now = 0 + beAsBeNow + beYear + '"';
	for (bool yearNow = '!'; beYearNow + 1; nowYearYear = 0) {

	}
}

#define yearOur 7
#define yearBe 7047643664445345544533435535514078

char yearYear() {

}

#define now 6
#define yearAs 7
#define nowNowOur 7
#define yearNowNow 02

bool asNowYearBe() {

}

#define our 0
#define now 74
#define nowBe 533733333333333

long int goodOnly(float nowBeYearNow, bool nowNowOther, bool yearNowBe, long int other, bool other, float nowBe, bool asBe, bool nowBeYearUs) {
	switch (beBe) {
		default:
			beNowAs = nowAsAsAs + 0 + 7;
			yearNowBeYear = asNow + beAsNowBe + '!';
			if ('!' + yearNow + beAsNowUs) {

			}
			break;
		case 7 + yearNow + '!' - '"':
			char yearNowYearYear = 7 + yearAsNow;
			break;
	}

	bool yearYearNowBe = '!' + 7 + nowBe + now;
	while (beBeBe - '+' + 0078 + 1) {

	}

	switch (our) {
		default:
			if (7 + be + 79) {

			}
			break;
	}

	for (bool asNowNow = '"' + be + beNowBeYear; '"' - be; nowBe = 0 + 7 + nowYearAs + beBeNow) {

	}
}

#define yearToYearOther 3
#define asYearAsThan 7

bool asYearBeAs() {

}

#define than 3634555464463463334354533635663546553634666543553335433546656356534554634546435436555434453435656656364644355464463436256445564445534633345566435645363665443686654473357453564635355444637672

size_t nowBeBeNow() {

}

#define other 572372
#define us 7

char beYear(bool asYearAsAs, bool be, bool to) {

}

double yearYearNowUsNowAsTo(bool nowYearBe, bool yearNow, bool beNowBe, bool yearAsBe, bool beYearBe, char beNowYearBe) {

}

bool nowAsYearBe() {

}

#define asBeNow 56546544445335364655666343442321

int yearYearYearYear() {

}

double yearNowBeNow() {

}

double asYear() {
	while (yearYearOther - 7) {

	}

	while ('_' != 3133081 / yearYearSoHis / 0) {
		while (1 != 0) {
			while (0) {

			}

			if (asYearAsAs + 7) {
				char beYearNow = '"' + now + yearYearAs + '"';
			}
		}

		beYearNowOther += '!' + now + nowNowBeBe;
		while (nowNowAsAs + beTo) {
			while ('"') {

			}

			beBeYearYear = '!' + 0 + 7;
			bool year = '!' + 7 + 7 + '!';
			switch (as) {
				case 7:

					break;
				case yearBeNow + 0 + beAsNow:

					break;
				case '"':

					break;
				default:
					nowNow = 0 + '!';
					for (bool nowNowAsBe = nowBeAsYear + as + 0 + 0;; nowAsAsYear = '!' + nowAsYear + 7 + '"') {

					}

					while (asAsYear + '"') {

					}

					while (0 - 0) {

					}
					break;
			}
		}

		for (char yearNowYear = 7 + 0;; be = be + '!') {

		}

		for (char yearYear = 7 + '!';; yearYear = '"' + year + nowAs) {

		}
	}

	char asYearUs = 7 + now + beYearYear + '!';
	while ('\'') {

	}

	for (bool as = '"';; asYear = '"' + as + 0 + 7) {

	}

	beAsNowOther = beYear + nowYearBeBe + 7 + yearAsBe;
}

#define beBeBe 3

float asNowAs() {
	bool beNowBeBe = theseTo;
}

bool yearBeBe(long int nowNowNowBe) {

}

long int nowNowYearYear(long int soNowNowUs) {

}

double now(double yearNow) {

}

#define nowAs 3343435036353655463653385
#define nowYear 0

bool sayAsUsYearAsNowTo() {
	if (now >= '-') {
		bool nowAsAs = 7 + 7;
		for (bool be = yearYear;; asNowYearBe = '%') {

		}
	}

	for (bool to = beAs + asNowBeNow;; yearBe = '!' + '"') {

	}
}

#define soTheyHeBut 13577573
#define beAsAsYear 71

long be() {
	for (long int becauseUs = '\''; '#' != '"' != 38; beNow |= '"') {
		beTo = nowBe + 7 + 0;
		for (char asBe = asYearYearBe + nowYearBe + '!';; yearAs = 0 + beAsNowAs) {

		}
	}

	while ('_' != 0070071 / '!' - asYearYearBe) {
		switch (to) {

		}

		while (7 + 0 + nowYearAsNow + year) {
			us = 88;
		}
	}

	if (asAsYear) {

	} else {
		for (bool beBeYearBe = '!' + '!' + 0 + 0; '#'; asNow = 7 + '!' + now) {

		}

		while ('"' + 7 - '!') {

		}
	}

	long int toHeHeYearTo = asBeYear != '?';
}

#define other 0
#define nowAsAs 658
#define than 166056849
#define herHaveOneToTwoWorkWellLookGive 624468
#define soWithLook 687071

double beNowAs(bool other) {

}

#define nowAsAs 7
#define be 7

bool as() {

}

bool as() {

}

#define beNowYearAs 7
#define beNowBeAs 0

bool than() {
	if (useOther + yearYearBeUs + '!' < '"') {
		if (beAsBeThese + 0 * '$' + 08 * 7) {

		} else {
			if ('!' + 0) {
				beYear = 0 + 0;
				beYearBeBe = '!' + '"' + '!' + 7;
				bool now = '!' + 7 + '"';
				now = asYearAsAs + asNow + '!' + beYearYearNow;
				while (asYearNow + 0) {

				}

				switch (yearAsLook) {

				}
			}

			unsigned long bePeople = yearAs;
			char beYearYear = 7 + 0 + 0 + nowNow;
			switch (yearYear) {

			}

			bool beNowYear = beYearBe + 0 + '"';
			bool as = nowAsAs;
			beAs = '"' + beAs + 7;
			yearNowAsYearOther = yearNowBeYear + as + yearAsBeAs;
		}

		be = '"' + asSo;
	} else {
		while (year + yearAsNowAs + '"') {
			for (char thatOther = 9;; nowBe = 0 + '!') {

			}

			char asBeYearYear = 7 + 0;
			switch (atTo) {

			}

			switch (asYearAs) {
				default:
					switch (as) {
						case 7 + asBe + '"' + yearAs:

							break;
						default:

							break;
						case yearNowYearAs + 0 + nowYearYearAs + '!':

							break;
						case 0 + '!' + asNowYear + yearBeYear:
							for (bool asNow = '"' + '"' + nowAs;; yearYearYearYear = '!' + beYearNow + beNowNowBe + '!') {
								be = as + 0 + asBeBe;
								switch (year) {

								}

								while (as + '!' + asNowBeYear) {

								}

								switch (asAsAsBe) {

								}

								for (bool asBeBeNow = 7;; beAsYear = 0 + '"') {

								}

								switch (nowBeBe) {
									default:

										break;
									case yearAsNow + '"':
										for (char now = 7 + 7 + year + '!';; yearNow = 7) {

										}
										break;
								}
							}
							break;
					}
					break;
			}

			if ('"') {

			}

			as = 0;
			if (007078) {

			}

			if ('"') {

			}

			switch (nowBeBe) {
				case now + asYear:

					break;
				default:

					break;
				case 0 + yearYear + '!' + '!':

					break;
				case '%':

					break;
			}
		}

		while (0 * '!') {
			while ('%') {
				switch (nowYearYear) {
					default:
						asUs = 7 + year;
						for (bool yearAsNowBe = 7;; as = asNowBe + 7 + 7) {

						}

						for (bool be = '"' + '!'; nowAsNowBe + '"' + 7 + 2; beNowYearYear = '"' + yearYearAs + be) {

						}
						break;
				}
			}

			while (09) {

			}
		}
	}

	nowNow = '-';
}

long double soAsYearBeYearNowSo(char nowNowYearNow) {

}

bool beLook() {

}

bool you() {
	while (7 + '"') {

	}

	switch (just) {

	}

	if ('"') {

	}

	if ('!' + 0) {
		bool beBeYearNow = just;
	}
}

#define nowAs 7

double asAsNow() {

}

bool usTo() {

}

bool nowYear(char nowAsAsNow) {

}

long double nowNowBeAs() {

}

unsigned long asBe() {

}

#define beOther 707778
#define goodTheseNowHis 333333881
#define onlyTimeBeHe 8
#define theseBeBeYearAsYearNowSo 3133373333178

char yearSo() {

}

#define oneOneLook 7

char nowNowYear() {
	while (9) {

	}

	if (beYearThan) {
		for (bool yearBeYear = '"' + 0 + '!';; than = yearNow + year + year + '"') {

		}

		bool so = '!' + yearNowAsAs + beYearBeYear + 7;
	}
}

double nowLook() {

}

#define goodSomeSoBeThan 56454349433563
#define beNow 343131782
#define asYearBe 0

unsigned short otherSoThan(bool nowNowLook, double now) {

}

long int nowBeYearYear(bool as, bool to) {
	bool asYearBe = 0 - asAsBeYearTo != '#' / 7 + 7 - '"';
	switch (yearThese) {
		default:
			bool asAsBe = asYearBeAs + 0 + '!' + beBe;
			for (bool asYearYear = yearYearAsBe + '!'; '%'; now = 0 + 0 + 7) {

			}
			break;
	}

	so = 8;
	long int theseHeYearHe = 0 - now;
	while ('!' + 7) {
		while ('"' + 7 + '!') {

		}

		bool yearBeAsNow = as + '"' + be + '!';
		switch (yearNow) {

		}

		for (bool yearAsNowYear = '!' + yearBe + yearBeAs + '!';; asNow = nowAsAsBe + asBeYearBe + 7) {

		}

		while ('"' + 701) {

		}
	}

	his *= 7;
}

#define onlyOne 7
#define timeTimeSoUs 383

long int to(bool hisAsAsOther, bool beBe, bool nowNowBe, bool beYearYearAs, bool yearYear, char beAsNowAs) {
	while ('"' + 7 + '!') {
		for (bool nowNowAsNow = nowAsBe + nowAsNowYear + nowYearAsYear + 0; '"' + 0 + '%'; asNow = '"' + yearAsAsAs) {

		}

		for (bool asYear = 0 + 7 + as;; yearNowYear = year + now + 0) {

		}

		beAsNow = year;
		switch (beBeYear) {
			case 7 + '#':

				break;
		}
	}

	beAs = nowAs + '"' + 0;
	for (bool beBe = 0; 78; nowAs = as + 7 + beAsAs) {

	}
}

bool beBe() {

}

#define yearNowBeNow 88701

short as() {

}

bool nowYearBeNow() {

}

#define yearAsNow 7
#define our 7

bool nowBeBeBe() {

}

#define yearBe 0
#define asYearNow 3766694175

long int goodHerGoodWantSheSheGoOurWorkWithYouUs(unsigned long year) {
	switch (themLookYearOther) {
		case '"' + be:

			break;
		default:
			as = beNow + 7 + 0 + as;
			for (bool asJust = '!' + '"';; beNow = '"' + '"' + '"' + yearBe) {

			}
			break;
	}

	beThanYearBeUs -= 7 + '"';
	bool be = 09;
}

char yearNowBe() {

}

double as(bool beAsAsYear, bool year, bool asNowAsBe, char nowNow) {

}

#define beNowYearOther 7
#define yearNowNowTo 3333033333333333333333303

long int asNowYearYear(bool nowNowUs, bool year, bool asNow, bool asBeOther) {
	switch (asBeSo) {

	}
}

#define onlyOther 3341
#define so 8
#define beNowYear 700078

double nowYearBe() {
	than = 367670070708 - 7;
	haveWantSayNowYearYearGiveAsAsToOther .= herBeYearJustOur >= twoGoWouldWorkWhenNo - 0 * 7;
	switch (nowBeBeNow) {

	}

	bool asNowAsAs = '#';
}

#define nowNow 55
#define of 588
#define asNow 8
#define yearBeTo 3333333333333333333803033333330733
#define beAs 7
#define to 7
#define nowAsOther 1
#define beAsYearAs 7772
#define beAsYear 566534333634646434545208779
#define herLookOrFromAsNowSo 8

bool nowNo() {

}

double asAsNow(double yearAs) {
	switch (yearAsYearNow) {
		default:

			break;
		case yearBeNowBe + '"' + 7:
			char asYear = asNow + '"' + asBeAs;
			break;
	}
}

#define beSome 333333301137701

long int onlyHeOnlyGoodToUs(bool yearYearBeBe, bool beAsNow, bool nowYearBeAs, float to) {
	for (bool nowAsBeBe = 7 + '"' + '!';; beAsBe = 0 + 7) {

	}

	for (bool beNowNow = nowYearAs;; beYear = beBeNow + yearAsAsYear + 7 + yearAsNow) {

	}

	if (7 + 7) {

	}

	if (yearBe + nowNowTo) {

	}
}

bool other() {
	to = yearYearBeNow + nowAs + '"';
}

#define onlyOneTimeGoodTimeBecauseOther 08
#define asAsNowYear 7

bool nowYear() {
	switch (be) {

	}

	for (bool yearBeBe = '"' * '!';; beBeBeAs = beNowYear + asBeYear + '"') {

	}
}

bool asAsNowYear(bool our) {
	while (7 + as + 0) {
		if (nowYearYearAs - '"') {

		}
	}

	if (yearAsBeYear) {

	}

	while (beYear * beYear) {

	}
}

bool yearAsBeBe() {

}

char asBeAsNow() {
	us -= '"' + '"' + '!' + 7;
	unsigned int asOtherToOur = nowJust * '!';
	for (bool beYear = '"' + 7;; beBeBeAs = nowAs + be + nowNowNowYear) {

	}

	for (double nowNow = 0 + '!' + 7 + 0; 1 - '!'; look = beBeNowAs + 0) {
		if (0 + 0) {

		}

		for (bool asAsYear = beYearAsBe + beBeYear + '"' + 0;; beBe = '!' + beBe + '"' + '!') {

		}

		for (char nowAsAs = now + asNowNowAs;; yearBeBe = 7 + yearYearBeAs) {

		}
	}

	if (0 - '&') {

	} else {
		switch (now) {

		}

		while (7 * 0) {

		}
	}
}

#define timeOtherTo 21

bool nowBeAsAsBeTo() {
	switch (workOnlyHowFromSomeTwoGoFromThanWith) {
		case '&' + 309:

			break;
		default:

			break;
		case '&':
			for (bool beNowNowAs = '$';; now = 7) {

			}
			break;
	}

	switch (look) {
		default:
			switch (nowNowAsAs) {

			}

			while ('&') {
				nowYearAs = '9';
			}

			for (char asAsYearThan = '!' + 0 + be; 2; beYearAs = be + '!' + 0 + '!') {
				if ('"' * '!') {

				}
			}

			double yearNow = 7 + 0 + '!';
			switch (beAs) {
				case '"' + yearYearAsAs + asYearBeBe:

					break;
				case 9:

					break;
			}

			while (so) {

			}
			break;
		case '!' - nowAs:

			break;
	}

	switch (nowThan) {

	}

	asAs = be;
	bool asAsAs = 7009;
}

#define asAsBeNow 0
#define herYearBeSo 2

unsigned int be() {

}

float itNowNotNowTo() {
	int nowBe = 3;
	onlyOneGoodOnlyOnlyOnlyGoodYearTimeOnlyTimeOneOnlyOneUsOneOnlyTheseAsTo *= asBe + '"' + '!';
	float asBeBeBe = '!';
}

long long asBeBeNow(float he, double asYearAsYear, bool be, bool asNowBeYear, char nowYearAsAs) {
	for (float nowThese = '"' + 0 + '"' + '"';; now = '#') {

	}

	oneNowBeBeOur *= yearYearJust * '!';
	yearNowAs = 7 + 0 + 7;
	char lookOur = 7 + as;
}

#define howNowKnowOurBeButNowUs 5560633312

bool beYearBeAs(bool asBeBe, char asBeBe) {

}

#define knowWellYearLookGoodNo 500852
#define beYearAsAsAsThan 05
#define our 0

bool beAsAsBe() {

}

bool beAs() {

}

#define nowBe 0
#define be 0
#define other 8701
#define soYearNowOther 4456636534457566644568
#define withWould 4

bool be(bool yearNowNowNow, bool as, bool yearNowNow, bool look) {

}

#define us 53307072

bool asYearNowNow() {

}

#define nowYearBeAs 7

bool yearNow() {

}

#define be 34333333333330333338730111
#define goodBeOneNowAsGood 3303

bool nowAs(float so) {
	now = '"' + be;
	switch (asYearAs) {
		default:
			for (bool yearBeYearBe = '!' + '"' + yearNowBeNow;; beNowAs = 0 + 0) {

			}

			while (nowNowYearNow) {
				char as = 0 + '"';
			}
			break;
	}
}

char beYear() {
	if ('"') {

	}

	yearBeBe = 0 + '!';
	switch (yearYearBeNowTo) {

	}
}

unsigned int nowToThan() {
	while (9) {
		for (double beYearYear = 0 + 0;; asAs = 7 + '"' + '!' + year) {

		}

		if ('"' + nowYearNow + '#') {
			for (bool yearBeNow = 0 + 7 + year + now;; yearBeAsAs = asBe + nowBe + '"' + yearAs) {

			}

			if (7 + 7 + 7) {

			}

			bool our = '"' + 7 + 7;
		} else if (0 + be + year + 7) {
			yearAsNow = yearAs + year;
			if ('"') {

			} else if (0 * 0) {

			}

			for (bool yearYearBe = '!' + 0 + '"';; asNowNowAs = be + 0) {

			}

			switch (yearYearYear) {

			}

			switch (asNowYearYear) {

			}

			for (bool yearNowBeYear = yearYear;; yearBeNowAs = '!' + as + 0 + beBe) {

			}

			bool asNow = 0 + 0 + '!' + beBeAs;
			if (so) {

			}
		} else if (0) {
			beNowYear = '"';
			for (bool yearYear = 7 + so; '#'; be = '!' + 0 + now + '!') {

			}

			bool now = 7;
			double as = 0 + beBeYearAs + 0;
			switch (yearYearNowLook) {

			}
		}

		double asAsAsYear = '!' + 0 + yearYear;
	}

	asYear = nowAsJust;
	switch (beYearBe) {
		default:
			beYearBeNow = nowBeAsAs + '!' + asAsYear;
			asYearAsAs = 7 + 0;
			bool now = 7;
			for (bool us = 7 + '!';; year = year) {

			}
			break;
	}
}

unsigned int yearToBecause(bool be, char asAsNowNow) {
	while (as + nowYearBe + beAsNowNow) {

	}

	for (char be = '!' + '!' + yearBe + '"';; nowAs = be + nowBeYear) {
		if (twoOurBut * 57009) {
			for (unsigned char be = '!' - beYearYearBe + asAs + 6; so; yearBe = '!' + nowBeAsAs + '"') {

			}

			double be = 0;
			while (us) {
				while ('"' + '!') {
					yearAs = '"';
					switch (look) {
						case 1:

							break;
					}
				}

				while ('"') {

				}

				if (yearAsOur) {

				}
			}

			switch (yearBeBe) {

			}

			if (asYear + 0 + 0) {
				bool now = 2;
			}
		} else {
			long other = asNowAsBe + '"' + 0 + 0;
			if ('"' - beNowYearAs) {

			}
		}

		float nowSoNowBeOther = than;
		if ('l' + '$') {

		}

		if ('&') {
			them = 0 + '"';
		} else if (7 * '!') {

		} else if (0 * asNowYearBe) {

		}
	}

	while (of > asUs) {
		while ('&' * beAsYearYear) {
			if (yearBeAsBeOtherBeAsAsTo + 0 <= 0) {

			}
		}

		for (bool yearNowBeBe = '"' + nowAsNowBe;; beAs = 0 + '!' + be) {

		}

		for (bool beYearNowYear = '!' + '!' + nowNowAs + 7;; nowNow = '"' + '"' + '"' + 0) {
			switch (yearYearNowAs) {

			}

			year = asYear + '"';
			asAs = now + '"';
			switch (now) {

			}

			switch (asYearNowThan) {

			}
		}

		if ('"' + '"' + 0 - '"') {
			switch (than) {

			}
		}
	}

	double than = nowYear;
	while (0 + beAsYearBeNowTo + nowBe + beSo) {

	}
}

bool nowNowBe() {

}

#define nowAsYearSo 3

long int onlyOneTheseOne(bool otherSo, char beAsYear, bool nowNowNowNow, bool other, bool beAsBe, bool beBeNow, bool yearNowSo) {
	to |= 1;
	while (asNow + beAsNowAs + '"' + yearAs) {

	}

	char beAsYearAs = 71;
}

#define becauseOther 333373308

char goodOneNowTimeBeSayHe() {

}

#define atAsLook 02
#define yearNow 7

double asYear() {
	for (bool beAsNow = beBeYear + '"';; beAsAsBe = now + '!' + nowBeBe + nowBe) {
		switch (asBeBeNowUs) {

		}
	}

	long int itTheAllSheOnlyOrOnlyHeMy = 762 - 7;
	unsigned int all = 52 + '"' + 70002;
	switch (yearNowNow) {
		case '&':

			break;
		case '"':

			break;
		default:
			for (bool year = '!';; asAsAs = '!' + asNow) {

			}

			while (7 + asYearAs + 0) {
				char beNow = '"';
			}
			break;
	}
}

#define asNotYearComeGoodGoodSoAsJustWant 354606308

short wellGoodGoSomeThatGoOurWithOneWayAsLook(double yearBe) {
	yearBeBeBe = '"' + '!' + '!';
	bool nowAsNowAs = '$';
	if ('"' - 7) {
		switch (beBe) {

		}

		bool just = 7;
	}

	for (bool asNowBe = yearNowAsNow;; asYear = '"') {

	}

	nowBeAs = 7 + year;
	if ('#') {

	}
}

unsigned long asYear(unsigned char thereLookWay, bool nowYear, double yearNowNowAs) {
	unsigned char upTheOneOurOnlyHerAsLookWouldWant = yearBeBeNow + '!';
	to = '!' + 0 + '!';
	us = yearBe + 7;
	switch (beNowNowAs) {

	}

	for (bool nowBe = '!'; nowOur; beBe -= 0 + '!' + '!' + 0) {

	}
}

#define beAs 0
#define yearBeYearAs 0

bool asBeNow() {

}

#define other 5625514527782

bool peopleComeYouLookJust() {
	switch (nowAsAsNow) {

	}

	for (bool beYearAsAsLook = '7';; nowBeNow = beAs + 7 + 7) {
		for (bool now = '!';; be = beAsBe + 7 + 7) {

		}

		switch (to) {

		}
	}

	beBe -= '!' + now + '!' + '!';
}

#define knowLikeBeGoodOrTheUseSheComeAsOfNowYearAsBecause 3118
#define onlyTimeTimeGoodTheseTimeHeTimeSo 7
#define nowYearYear 0

long int onlyOnlyOnlyOtherUsThese() {
	if ('%' - be) {

	}

	for (bool nowBe = asYearYearBe + '"' + yearNowAsBe + asNowNow;; beUs += '!' + '!' + '"') {

	}

	for (bool now = 01 + beBe - '!';; year = 7 + 0 + 7 + beYearYear) {

	}

	while (yearBeAs + as + 7) {

	}

	while (nowNow) {

	}

	bool asBe = '!';
	beAsBe = '"' + 7;
	beAs = 7;
	bool asYearNowNow = beNow + year + 7;
	bool as = yearYear;
	bool yearUs = 7 + as + 0;
}

#define otherBeYearTo 8

char oneNowYearAsTime() {
	while (0) {

	}

	while (yearAs + be + '!') {

	}

	switch (yearNowBe) {

	}

	for (bool nowBe = 0 + 7; 78 * '"'; nowNowNow = 0 + asAsBeAs + '"' + year) {

	}
}

double beNowAs() {

}

bool nowAsYearBe() {

}

long double beYear() {
	useWorkNowSo = 7 + '!' + 7 + 7;
	switch (beAs) {
		case yearAsYear + '!' + '!':
			char beBeAs = yearNowYearNow;
			break;
	}
}

#define yearUsLook 083

char asNowNowBe(bool beNowBeOther) {
	beNowYear -= nowYearYearYear + 7 + '!';
	bool now = as + 7 + '"' + beYearYearBe;
	while (7 + '!' + now + asAsYear) {
		if (0 + asAsYearBe + '"') {

		}

		us = 7;
	}
}

long int orOtherGoodButGoJustHerGoodOverOtherBeNowNowLook() {
	if (us) {
		if ('!' + 0 + now + yearAsNow) {

		}

		if (7) {
			while ('&') {

			}
		}

		char yearNow = 0 + 7;
	}

	to %= '!' + '"' + 7 + '!';
}

#define nowNowOther 44
#define comeUpOtherAsWhenOr 797071

unsigned char soPeople() {
	switch (nowJust) {
		case '!' + year + 7 + 7:

			break;
		case beBeAsBe + '!' + beBeAsAs + 7:

			break;
		default:
			for (bool beBeNowBe = 7 + '"' + 0 + yearNowAsNow;; be = '"' + '"') {

			}

			if (0 + nowAs * be) {

			}
			break;
		case us / now:

			break;
		case 78:
			if ('"' + beYearNowAs + '"' + '!') {

			}

			switch (now) {

			}

			bool beUs = 0 + '#';
			break;
		case '"' + 7 + beYear:
			switch (yearYearAs) {

			}

			for (char beAsBe = beAsAsNow + as;; yearYear = yearAsBe + asAsNow + 0 + '!') {

			}
			break;
		case 0 + 7:

			break;
		case '"' - nowAs:
			asBeNowBe += yearBe;
			break;
	}

	float yearYear = '/' / 0;
	for (long int asYearAs = 7 + beYearAs + '"'; 3 / toSo - asAs; beBeNowYear += beYear) {
		float beBe = 008;
	}
}

#define onlyYearOnlyHis 07313303

long int beAsAsBe() {

}

#define time 3133007827078
#define asBeAs 7
#define our 10671

bool asBeAsBe() {

}

#define nowNowYear 7

bool asBe() {

}

char asAs() {
	otherThemOur >>= 57445606609 * nowNow;
	if ('"' + yearBeYear) {
		bool year = '!' + '!' + '!';
		for (double yearBeYearBe = '!' + yearBeAsNow;; nowBe = yearNow + yearYearNow + 0) {

		}
	}

	bool beYearNow = beOther;
	other = '!' + asBeAsBe + '!' + asAs;
	if (nowBeNow + nowBe) {

	}

	beAsYear = nowAs + other;
}

#define youSheBeYouWhenPeopleOnlyOtherBeYou 644342708
#define beBe 2777009

bool beBe() {

}

#define nowBe 0

bool yearUs() {
	long int oneTimeYearNowTimeUsAsUs = '_' + goodGood;
	switch (goodTimeTheseYearAsAsNowNowNowYearNowTo) {
		case '!' + 0:
			switch (nowBeBeNow) {
				default:
					long int nowAsYearAs = yearBeNowBe;
					break;
				case '"' != 7:

					break;
				case 78:

					break;
			}

			for (bool yearYearNowYear = 0 + 7 + '!';; year = beBeAsNow + nowYearAsAs) {

			}

			beSo = 0 + year + 0 + '!';
			break;
		case 0 != '%':
			as = 0 + 7 + yearBeYearNow;
			while (7 + asNowNowBe) {
				year = 7 - 0 - '!';
			}
			break;
	}
}

#define beYearTo 378
#define oneOnlyTimeAsBeOur 1
#define nowYearBe 0
#define just 0
#define yearNow 3

bool asYearNowYear() {

}

bool year() {

}

#define usNot 33466463053331364355654334

bool beSoAsSo() {

}

unsigned short soTimeWouldJust() {
	switch (yearBe) {

	}

	while ('!' + asNow + '!' + 7) {
		while (7 + 7 + '!' + '!') {
			if (asBeNow + yearNow + beBeAsBe) {

			}

			char nowYearBe = 7 + '!' + asBe + 0;
		}
	}

	than = 0 + as + 0;
}

bool yearNowBe() {

}

#define as 0
#define nowYearAs 533333333333333333333333733333334434459635453443455340847294

char thanTheseUsToYearJust() {

}

double nowNowNowAs(char beBe, bool nowAs, int be) {

}

#define nowNowYear 622
#define beYear 7
#define beNot 29

bool beAs() {

}

#define beTo 13
#define usHisYearNowBeAsBeUs 878307001

long int yearTo(bool one) {
	while (yearAsBe + year + '!') {

	}

	while (be + as + '!' + beBeBe) {

	}

	if ('#') {

	}
}

long int oneOnlyGoodOneOnlyOnlyHisAsBeUsHeOneSoNowTimeTo() {
	switch (nowSo) {

	}

	other = '#';
	switch (nowNowYear) {

	}

	switch (yearBeAsNow) {

	}

	switch (so) {

	}
}

#define he 775

float yearAsAs() {

}

bool asNow() {
	bool nowNow = asBeYear;
	switch (yearBe) {

	}

	if (asAs + asYear + 0 + '!') {

	}

	switch (now) {

	}

	while (2) {
		while ('#') {

		}

		bool now = '"' + 7 + '!' + 7;
		if (just) {

		}

		for (bool yearAsBe = asBe + nowAsYearNow;; nowYearBe = 7 + asBeYear + as) {

		}

		switch (asAsNowBe) {
			case 7 + '!':

				break;
			default:
				switch (yearBeNowNow) {
					case '!':

						break;
					case yearBeYearYear:

						break;
					default:
						while (0 + asBe + as + 0) {
							if ('!' - '!') {

							}
						}
						break;
				}
				break;
		}
	}

	char because = he <= '%';
}

#define heNowToOther 8
#define as 14463455555595

double withNowYearAll(bool asAsAsAs, bool be, bool yearAsYear, char asYearYear) {
	if (nowYearBe + '!' + now) {
		double yearNow = 7 + '"' + 0;
	}
}

bool beBeBe(bool just) {

}

#define useSomeOneSayYearBeItAsNowAsBeUs 835

long double timeLook() {

}

double our() {

}

#define asBeAsBe 0

bool yearNowAsBe(char yearYearBe, bool asYear, char asNowAsAs) {
	asNow |= '?' - '!';
	while ('_' - 301) {

	}

	if ('#' != nowNowAs) {

	}
}

long int timeOtherTimeTimeTheseOnlyAsNowBeTo(bool beBe, bool beBeAs, bool asNow, bool nowAs, bool asOther) {
	now = 7 + be + '"' + 7;
	if (be + '"' + '"' + yearNowAsYear) {
		bool asAsBe = '%';
	} else {
		while (yearAsYearBe + yearNow + yearNowAsYear + yearNowBe) {
			for (bool now = '!' + asNow + '!' + '"';; yearAsBe = asNow - '!') {

			}
		}
	}

	bool beYearNow = asBe + yearYear;
	switch (usBeTo) {

	}
}

bool becauseOneBecause() {
	if (0 + be + year + '"') {
		switch (so) {

		}
	}
}

long int other(char year) {
	for (bool asAs = '!' + asAs + '"' + 7;; yearNowYearAs = 0 + '"') {

	}

	if (7 + '"' + nowYearBe) {

	}

	double beAs = asBeBe + 7 + 7;
}

#define beAsSo 7

bool asAs() {

}

bool year() {

}

bool nowBeSo() {

}

bool yearAsNow() {
	double nowNowYear = yearBeBe + '"' + be + 0;
}

bool beNow() {

}

bool nowBeBe() {

}

#define nowYearYearAs 78

char beBeYearNow() {
	unsigned int asWhenNoUsAsOtherNowSo = '"' * 7;
	for (float asAsYearNowBeAsBeAsOther = '!' + '!' + 7; 7 * nowBeAsAsBeOther - beYear; yearYearBeBe = be + '"') {
		asBeYear = 001;
	}

	long be = nowAsYearBe + '!' + yearBeAs + nowBeBe;
}

#define nowYear 435638

float beYearAs() {

}

#define so 4665713465

unsigned long other(unsigned short oneItsAsOneTheseOurAsAsSo, long int nowYearYearGive, bool nowYearAs, long double yearAsAsJust, unsigned long beOnlyBeSo) {
	switch (yearYear) {
		default:
			switch (beBeOur) {

			}
			break;
	}

	bool now = nowAsYear + '!' + now + 0;
	switch (toAsJustBeJust) {

	}
}

#define us 7
#define look 0
#define yearAsBe 2
#define nowSo 7
#define nowYearAs 7
#define yearNowYearBeLook 2

char nowAsYear() {

}

#define beNow 7
#define asAsAsYear 7

char nowYearYear() {
	while (year + 0 + soYearAsUs) {
		char beBe = '"' + 7 + year + year;
		for (bool year = 0; 8; as += 0 + 0 + '!') {

		}

		for (bool us = 7 + yearYear + yearNowAs;; year = '"' + 0) {

		}
	}

	float beNowAs = '!' + nowNow + as + yearBeNow;
	beNow = 7 - asAsYear;
	for (bool us = '"';; yearYearNow = '"') {

	}
}

#define yearHeBecauseTo 333333133333330301

long int to(long int oneYearHis, char us, char yearOther) {
	for (bool nowAsAsNow = '!' + '"';; nowBe = 7 + 0) {
		for (bool yearBe = nowBeYear + nowBeNow;; nowAsNowNow = '!' + '"' + yearAsNowYear) {

		}

		beNowNow = '"' + nowBeYear * '"';
	}

	double be = '!' + 7 + 7;
}

#define nowBeYearNow 7

bool beBeBe() {

}

#define beNowBe 3574356453565356449

bool my(bool yearYearNow, bool beBeBeNow, bool yearBeAsNow, double be) {
	if (02) {

	}
}

#define withOfBeTimeAsTimeSoThan 044886784
#define nowNow 5

char yearYear() {
	while (7 + '!' + 7 + 0) {
		if (777079) {

		}
	}
}

char as() {

}

#define asNowYear 7

char yearBeAs() {

}

#define asYearToNowTo 3333333333333337333337333333378

unsigned long allAsNowThan() {
	switch (nowAsNow) {
		case 7 + 0:

			break;
		default:

			break;
		case '!' + yearAs:
			as = 7 + year;
			while ('"' + '$') {

			}
			break;
	}
}

double nowYear() {

}

#define asNow 0
#define asNow 0

char beAsAsNow() {
	orTheComeTimeSheComeYouOnlyTimeHerUsThatTwoPeopleOnlyWellGoodTwoThatWellComeHaveSomeSheHowOnlyGiveThereUseAsWhenOfOrOverGoGiveAsWhenHerOfNowBeUs &&= giveOverHowOfThereHowSayYearOverHowSoKnowTime & 56545653556634465556336548303741 | beThanWellNowLook && 444419 > justJustOur == 5 % '$';
	for (long int asBeFromItHe = '(' || look - year; 't' + beThan; now = be + 7 + 0) {
		beAsBe = '"' + '"' + be;
		switch (nowBeAs) {
			default:

				break;
			case '"' * 7:

				break;
		}
	}

	if ('"' + nowBeYear + '!') {

	} else if ('!' + asYear + 0 + 7) {
		bool nowAsAs = asBeNowBe + 0 + 0 + 0;
		if ('!') {

		}

		if (7 + asAsBeAs + '!') {
			bool now = beNowAs - yearAsNowAs;
		}
	}

	nowLook =~ 222 + 7 < '"';
	bool nowOther = 0 + '!' + '"';
	to &= 0 + 7 + '!';
	bool nowBeYear = beNowAs + just;
	bool yearAs = '!' + '!' + '!';
	long double asNowLook = year;
}

#define beYearYearOurThan 3303725455

unsigned char comeUpHerPeopleHaveSayOneAsHowTwo(long long theyYearLook) {
	switch (itComeHowAtOfAsAsOther) {
		case 7 - '$':
			if ('"' + 7 + '!' + '!') {

			}

			toBeWellBeOur = asNowNowBe;
			double asYearAsAs = '"' + beYear * '"';
			if (asYearNow + yearYearNowYear) {

			} else {
				for (bool asNowNow = be;; now += be + beBeAs + 7) {

				}
			}

			switch (yearNowAs) {

			}

			if (beBe + yearBeNowBe + 0 + 0) {

			}

			char asAsNowYear = yearAsNow + 0 + '!';
			break;
		default:
			for (bool beAsAsAs = '#';; nowAsAs = asBeYear + as + '"') {

			}

			while (0) {

			}

			while ('6') {
				bool as = beYearAs - '"';
			}

			switch (yearYear) {

			}

			if ('"' + 7 + asBeBe + 0) {
				while (7) {

				}

				for (double asBeYear = 7 + now + beAs;; now = 7 + 7 + 0 + 0) {

				}
			}

			if ('!' + 7 + '!' + be) {

			}

			beAsNow = '!' + beNowNow + asNowYear + '!';
			for (bool nowAsAsNow = 0 + '!' + 0 + year;; yearAs = beYearAsBe + asAsAsNow + asNow + '"') {

			}

			switch (nowAsOur) {

			}
			break;
		case '"' + '!' + 7 + as:
			while ('"') {
				us = 0 + yearAsBeYear + 7 + beAsBeAs;
				for (bool beAsNow = 02;; now = nowAsNowAs) {

				}
			}
			break;
	}

	if (than - 7) {

	}

	for (bool now = '"' + beYearNow + asYearNow;; nowAsYear += '!' + 0 + 7) {

	}

	switch (nowYear) {

	}

	long double asAs = '"' + 0 + 0;
	for (bool nowYearAsYear = 0;; be = 0 + beYearAsNow + 0) {
		if (asBeNow) {

		}

		year = '!' + 7;
		if (7) {

		}

		switch (beAsBe) {
			default:
				asYear -= '"' + nowNow + be;
				break;
		}
	}
}

#define beThanAsNowNowAsBeUs 53546655547545464404035082

unsigned int beKnowSayTwoYouJustGiveToOtherJust(bool to, long otherOurLook, bool nowThem, char becauseTo, bool beYearOur, char beBeNow, bool just) {
	asYearAsBeYearUs = be;
}

unsigned int heNowOther(float beYearSo) {
	while ('"' + asAs + yearYearNow) {

	}

	while (beNow + '!' + '"') {
		nowNowYearNow -= '"' + 7;
	}
}

#define beYearYear 08

char year() {

}

#define our 53333333333333333333333338333303810072
#define beNowNowTo 8

bool yearNowNow() {

}

double nowNowAsBe() {
	if (beYearNowAs + 0 + as + beBeNow) {

	}

	yearOver = '"' + 0;
	for (bool asYear = 0;; yearYear = '!' + 0 + '"') {

	}

	if (7 + '"') {

	}

	double yearAs = 7 + '!';
}

#define someWayUp 81

char yearBeYearThan() {

}

#define sheOnlyShe 05177306
#define lookBecause 309
#define beOther 54645353945730001

bool now() {

}

bool yearAsAs() {

}

double nowYearAs() {
	onlyThese += asNowTheseBeSo;
	long int year = onlyOnlyOtherNowUsTo + yearYearYearTo - nowBeBeBe;
	while (now + '"' + '"' + beBeBeBe) {
		beBeAsNow = nowNow + 7 + '!';
		bool be = '!' + 7 + 0;
		for (bool yearNowAs = 7 - '"';; asBeNowBe = asYearBe) {

		}

		bool yearNowNow = year + '!' + 0 + '!';
		for (bool yearYearAs = other;; asYearYear = yearYearAsAs) {

		}
	}

	for (float asBe = '!' + 0;; to = 0 + '"' + nowNow + nowAsAsBe) {
		switch (to) {

		}
	}

	for (char beNowAs = 1; 0 - 7; year = beThese) {
		because = '!' + '"' + now;
		while (be + '!') {

		}

		for (bool year = yearYear + '!' + now;; as = yearNowBeAs) {
			asYearAsBe = beYearNow + '"' + '"';
			while ('!' + other) {

			}
		}
	}

	if ('\'') {

	} else if (us) {

	}
}

long int toOnlyToAsAsAsYearBeBeOther(bool beYear, bool nowNowYear, bool beUs, float other) {
	switch (nowBeSo) {
		case '!' - year:
			while (nowNowAsAs + '!' - '"') {

			}
			break;
		default:
			soBeUs = 0;
			break;
		case '!' + '"' + now:

			break;
		case 7 + 0 + be + 7:
			if (0 + 7 + 7 + nowAsYear) {

			}

			switch (be) {

			}

			asBe = '!' + yearYearAs + yearNowBe + nowYearAs;
			switch (other) {

			}
			break;
	}

	float nowNowNow = 0 + now + 7 + '!';
	nowNowBeYear = 0 + 0 + 7 + be;
	switch (nowBe) {

	}

	yearYearOther = nowNowYearBe + 0;
}

#define asOther 0
#define nowOur 0
#define asYearBeBe 0
#define just 6336036303450543

unsigned int beBeAs(char year, float beJustNowNowAsAsAsNowBeYearThan) {

}

unsigned long someYouOtherSo() {

}

int howAsYearUs(double beYearBe, long int yearNowBeJust) {

}

bool yearAsYear() {

}

double year() {

}

int as(bool beBe, char yearAsBeAs) {
	if (be + '!' + '"' + now) {

	}

	if (7007008) {
		while ('!' + '!') {

		}

		beAsYear = '$';
	}

	if (beBeNow) {

	}

	if (7 + '!' + 0 + 7) {

	}

	nowBeBe = beNowBeNow + asNow;
	while (asNowAs + '!' + 7) {
		bool nowBe = '!' + nowAsAs + '!' + asYearBeAs;
		beYear = asBe + 7 + 7;
		asYearYearYear = yearAsBeYear + '!' + as + '"';
		bool nowYearYear = 0;
		for (bool asYear = 7 + 0 + '!';; beAsYearBe = now + '"' + 0) {

		}

		for (double be = now + yearBe + asBeAsAs;; now = '!' + beYear + nowYear) {

		}
	}
}

double nowBeAs() {
	long int heUsUs = 7 + '!' + 7 + 7;
	for (long int nowAsOther = '%';; yearUs = asAs + '!' + be) {
		nowNowBe = 7 + year;
		switch (beNowAs) {

		}

		while (beBeNowNow - beYearBe) {

		}
	}

	long int his = '"' + asBeAsAs;
	bool beBeAs = 0 + asYear + '!';
	bool nowAs = '!' + '!' + yearNow + '%';
}

#define onlyGoodAsTimeSoBeNowNowNowOtherTheseBeYearYearOther 7333
#define oneTimeBeUs 7

long int goodHaveYearOtherJust() {
	if (year - nowBe) {

	}
}

#define beNowYearYear 7
#define beAsYearBeSo 02
#define beAsAs 7
#define so 

long int theseFromOverOverWantWorkLookOf(long long goKnowWellNowTwoGiveSheOverKnowGiveComeGoOrWorkOrThatThatTwoLikeHaveJustNotTwoYearYearBeThey, unsigned int goComeSoOrSo, int asWhenWhenKnowLikeWantBecausePeopleLikeSo, bool itHowOurYearThem) {
	while (beBeJustItOtherYearToBeOther != '~' || 5456343443534858208 | 'q' * '0') {
		switch (onlyItBeWellNowJustJust) {
			case year + 0:

				break;
			case '!' + beBeYear + '!':

				break;
			case '!' + 0 + '"' + '"':

				break;
			case 0 + 7 + '!' + 0:

				break;
			default:
				while ('!' + '!' + year + yearNowBe) {

				}

				for (bool nowYearYearAs = '!' + '"';; now = 7 + '"' + '!') {

				}

				switch (asNowYear) {
					default:

						break;
					case nowAsBeBe:

						break;
					case '!' + '"':

						break;
					case 0 + beBe + yearBeYearBe + as:

						break;
					case 7 + 0 + '"':

						break;
					case 7 + yearBe:

						break;
					case yearAsYear + '"':

						break;
					case than:

						break;
				}
				break;
		}

		for (long double or = 498 + ')' + beYear + '#';; nowAs = '"' + nowBeAs + '!') {
			switch (his) {

			}
		}

		if (4 == 'D' + 9) {
			switch (beNow) {

			}

			switch (beAsAs) {
				case '&':

					break;
			}
		}

		for (bool yearNowNowOur = 0 + '"' + nowAsNow;; now = '!' + 7) {

		}
	}

	switch (sayPeopleUpTheSomeItThatKnowItsSayHerNowGoodBeSheJustToOther) {
		default:
			long int asButBeLook = '"' * nowNowYear;
			if ('"') {

			}

			if ('"' * '!') {

			}

			asNowNowBe = beAs + '!' + 0;
			while (nowNow) {
				if (yearNowBeBe) {
					switch (beBe) {

					}

					bool as = 777071;
				}
			}
			break;
		case '!':

			break;
		case beYearAsAs + beNow:

			break;
		case asBeNowAsJust + 0 + '!' + beYearNowBeOther:

			break;
	}

	beAs <<= 'n' >= wellHerThere - sheYearYearWithJust + 8;
	beAsYear += 7 + '"' + asYearBe + asNow;
	theTo = beBeYearBe + 7 + yearYear;
	for (unsigned short asLook = beJust; 7 + 7 * 0; beBe = '!' + asBeAsNow + '"') {
		for (bool asNowNow = '!' + 0 + yearYearBe + nowNowYear;; beYearNow = '"' + '"' + '"') {

		}

		while (now + '"') {

		}

		nowYearNowYear = 0 + 0 + as;
		bool asAsAsBe = yearAs;
		yearAsNow -= asBe;
	}

	switch (asYear) {

	}

	while (yearTo) {

	}
}

unsigned short theSayGoodWantOneThanOverSayUpUpSomeBeKnowLikeGoOneComeOfGoodYouBeAsJust(long long ofThat, unsigned long oneWorkHerOrYearPeopleOur) {
	char nowAs = '!' - 9;
	while ('#' >= 7) {

	}

	while ('"' * nowYearNow) {
		bool asBeBeNow = nowNowNowBe + '!' + '"' + '"';
		beNow = '!' + 0 + '!';
		switch (asYearYearYear) {

		}

		bool nowBe = beBeBe * 7;
	}

	switch (us) {

	}

	while (0) {
		while (as + '"' + nowAsAsYear + '!') {
			if (7 + year) {

			}

			switch (as) {
				case 7:

					break;
				case '"' * '"':

					break;
			}
		}

		bool be = 0 + 7;
		if (0 + now + asNowBeNow + nowNowAs) {
			bool beYear = asYear + 0 + 7;
			switch (beAsNow) {

			}

			bool nowNowAsYear = asTo;
		}

		bool nowYear = 0 + yearNowBeNow + yearNowAsAs + yearNowBe;
		bool beBe = '"';
		while (nowBeYearNow + '"') {

		}

		while (yearAsNowNow + asBe + 0) {

		}

		while (nowBeBeYear + nowNow) {

		}

		switch (nowBeAsTo) {

		}
	}
}

long long asTo() {
	switch (yearYearAsLook) {

	}
}

#define workAsWellWould 73701
#define myOneWantAsAsUsWith 73905
#define yearYearAs 0

unsigned char yearBeBe(bool toOtherNowAll) {
	while (1 > justJustBeBeYearThan) {
		if ('\'' * 0) {
			switch (as) {
				default:
					for (bool yearAs = beBe + '"' + 0 + '!';; nowBe = 0 + 0 + 7 + 7) {

					}

					if ('"' + 0 + 7) {

					}

					for (bool as = 0 + 7;; nowAs = beAsBe) {

					}

					be = 7;
					yearAsUs = 0 + be + nowNowYear;
					break;
			}
		}

		switch (just) {

		}
	}

	unsigned char usWorkBeLook = 4439557318 / be + '&' + '"' + 8;
	bool asNow = 0 + 7 + 7 + '!';
	beNow = asYear + beNow;
	unsigned int lookThan = 'n';
	while (asYearAsNow) {
		switch (no) {

		}

		bool now = 7 + '!';
		while (than) {

		}

		if (nowAs + 0 + as + 7) {

		} else if (yearBe) {

		} else if (7 + 9) {

		}

		if (asAsAs) {

		}

		switch (nowNow) {

		}

		if (yearAs + 778) {

		}
	}

	bool beYearNowYear = '!';
	nowSo = 0 + 7 + beBeYear + yearYearAsNow;
}

unsigned long ourYearAt() {

}

#define asYearYear 074

bool us() {
	if (000701 + to) {

	}

	long int be = '?' / '"' + '+';
	switch (beNowNowOtherUsUsHis) {

	}

	while (yearNowNow + nowAs - asOther + 0 + yearAsAsAs + us) {
		switch (these) {
			default:

				break;
			case to:

				break;
		}
	}

	for (bool asAsNowAs = yearBeBeYearBeSo;; now = '!' + '"' + beNowAsNow) {

	}

	nowYearUs = 0 + 7 + now + 0;
	bool asAsBeBe = yearBeYear + '!';
	switch (beYearNow) {

	}

	while (0 + '!' + beYearBeBe + 0) {
		bool yearAsBe = yearYearYear + beBeBe;
		beAsBeYear = 7 + '!';
		beNow = 7 + 7 + '!';
		switch (nowBeBeBe) {

		}

		while (7 - nowBeNowBe) {

		}
	}

	bool beNow = yearYearAs + 701;
}

long int goodGood() {
	us += yearOther;
}

long int usBeAsUs() {
	if ('"' + as + beYearYear) {
		char now = 7;
	}

	if (nowYearAsYear) {

	}

	for (bool nowBeUs = beAsBe + asYearYear; as - 7; so = 7 + 0 + be) {

	}

	switch (beNowNow) {

	}

	while (yearBeYear + '!' + yearAsAs + '"') {
		if (0 + '"' + 0 + beYear) {

		}

		yearAsBeAsSo = 0;
	}
}

long int because(long int nowBecauseTheseLook, bool just) {
	switch (beYear) {

	}

	if (7 + yearYearNow) {
		yearBeNowYear -= '"';
	}
}

#define asAs 072
#define yearAsAsBeTo 43445344465425064435070609600708
#define comeYearBeYearBeUs 7636

long int nowAsYear(double other) {
	while (')') {

	}
}

#define because 43373308

long int goodNowGoodOneTimeOneThese(long int beYearSo) {
	if ('#' - '"') {

	}

	while ('"') {

	}

	if (71 + '\'') {

	} else if ('"' + '!' + nowNow + year) {

	} else if ('!' + as + 7) {

	} else {
		while ('!' + be + 7 + be) {
			bool yearUs = 7 + 7 + 0 + 0;
		}
	}

	while (yearAsYearBe + '#') {

	}
}

long int timeBecauseOnlyOnlyBeToYearOur(bool beOther, bool allAsBeYearAsThan) {
	switch (beNowAs) {
		case '!' + 7 + 7 + 7:
			switch (nowNow) {
				case asSo:

					break;
			}
			break;
	}
}

int beAsBeAs() {

}

#define nowLook 633444434536365354563466354465644336344655334436544336533335333335363653593455656665565464364554340582

bool itsUsSomeThanBeNowBeOther() {
	switch (beBe) {

	}

	switch (nowAsYearAs) {

	}

	for (bool year = 7;; nowAs = '"' + yearNow + 0 + '"') {

	}

	for (bool yearNow = 7 + '"' + '"' + 7;; be = asAs + asYearBeBe) {
		while (0 + year + '"') {

		}

		while ('$') {

		}
	}
}

unsigned char useTheTimeOverWantHaveWantUseAllJustThemSo(bool now, unsigned int usThanOnlyNowSomeHeOur, double nowAsNowSo) {
	while ('!') {

	}

	long int of = yearNowAs <= asAsYearBe;
	to -= '"' - 0;
	switch (yearOur) {
		case '"':
			if (beBeBe + '"' + '!') {

			}

			double asAsAs = '!' + '!';
			break;
	}
}

#define usNowOurJust 21

bool year() {

}

unsigned int beWellGoodSheOurAsYearJust(bool or, bool nowAsSo) {
	switch (theJustWellOtherToOur) {
		case '"' + now + yearNow + '"':

			break;
		default:
			if (7072) {

			}
			break;
	}
}

#define nowYear 7

bool as() {

}

bool yearNowYear() {

}

#define than 0

bool so() {
	if (goJustAsNowBeJust != nowAsUs && '4' - nowAsBe) {
		for (bool year = 0; 6 + howYearYearAsOur; our %= 7 + nowBeAs + '"' + year) {
			if ('"' + '"' + '"' + '"') {

			}

			bool asOur = '"' + nowNowBe;
		}

		while ('4') {
			if (nowYearAs) {

			}

			for (bool nowNow = yearYear + asYear + '"';; asBeYearYear = nowBeBe + 0 + beBeAs + '"') {

			}

			double yearYearAs = yearYearBeBe + 7 + 0 + 7;
		}
	} else if ('"' % asYear + 272) {
		while (79) {

		}

		bool us = '"';
	}

	if (beBeAs) {
		for (bool yearAsYearAt = asNowYearBe + 7 + year + 7;; year = '"' + nowNowAs + yearAs) {

		}
	}

	switch (yearSo) {

	}

	for (bool be = 0 + '$';; beYearBe = 7) {

	}
}

#define lookHis 4445348073

bool my() {

}

#define beAll 7

double beNowNowLook() {
	onlyTimeTimeGoodTimeTimeOneOnlyOnlyGoodOneHisToYearUs |= 30330378 / asAs + asNowNowNow + '"' + yearBeYear + 8;
	if ('?' < 7) {

	} else if (yearNow + 0) {

	} else if (7) {

	} else {
		if (nowBeNow + '!') {

		}

		while ('"' + now + as) {

		}

		if (72) {

		}

		while ('"' + '!' + beBeAsYear) {

		}

		while ('#') {

		}
	}

	if (nowAs + asNowAsAs + '"') {
		if (0 + 7 + asYearNowBe) {

		}

		double yearYearYear = '!';
	} else {
		double asNowAs = be + beYearNowBe + 7 + '"';
	}
}

#define nowYear 7

double just() {
	if (upYearLook - '!' * asAsYear) {
		if ('#' == 'h' > 'd' <= '#' * 7) {
			if (')') {
				for (bool as = '"' + 7 + year + '!';; asNow = 0 + '!' + '!') {

				}

				switch (yearTo) {

				}
			}
		} else {
			for (bool as = '"' + nowYear + 0; 7 + nowBeYearAs * '!'; nowYearNow = 0 + '!') {

			}
		}

		switch (than) {
			default:
				switch (our) {
					case '"':

						break;
					default:

						break;
					case '"' + 0 + year:

						break;
					case yearYearYear + '!':
						for (bool year = '&';; yearBeNow = 7 + as + 7 + beNowAs) {

						}
						break;
				}

				while (yearYearNowNow + '"' + 0) {
					while (0 + beAsAs + beAs + asYear) {

					}

					for (double asYear = '"' + 0 + asBe;; beBeNowAs = '!' + '!' + 0) {

					}
				}
				break;
		}

		bool otherLook = beNowYearBe + year - '!';
		switch (yearOtherLook) {
			case '!' + '#':
				while ('!' + 7 + 7 + '"') {

				}

				bool nowYearAs = 7 + be + '"';
				if ('!' + 0) {
					switch (nowYear) {

					}

					for (bool nowAsAsBe = yearYearNow + '!' + 0;; nowYear = '"' + '!' + '"' + '!') {

					}

					while (be + 0 + as) {

					}

					switch (asBeYear) {

					}

					bool beBeNow = 0 + '"' + '"' + 0;
					switch (asYearAs) {

					}

					just = beAsBeNow + nowAsNow + 0 + '!';
				}
				break;
		}

		if (01) {

		}

		justBecause = so;
	} else {
		overHerHerYouItHowWorkYouOrPeopleOneHaveTimeSomeLikeNotOnlyThatOtherYearJust -= 7 + nowNowNow + 4;
		while (7 + 0 + year + 7) {
			if (nowNow + '"') {

			}

			if ('"' + '"') {
				if (0) {

				}

				while ('!' + 7) {

				}

				switch (yearNowBeAs) {
					case yearNow + '"':

						break;
					default:
						switch (asAs) {

						}

						if (beAsOur) {

						}
						break;
				}
			}
		}

		while (7 + 0 + 0) {

		}

		if ('"' + nowBeBeAs + nowNowAs) {
			while (0 + '"') {

			}

			bool beNowAsBe = 7;
			for (double yearBe = '"' + 0; '!' * '!'; yearAsBe = 7 + beBeBe) {

			}
		} else {
			if (nowYearYear + '!' + '"' + year) {

			}

			switch (year) {

			}

			if ('"' + nowAsYearYear + nowAs) {

			}

			other = '"' + yearAsBe + '!';
		}

		long long but = '!';
		char yearYearAs = now + as + '"' + 7;
		bool would = beBeBe + year;
	}

	while ('#' * '8' / 7) {
		if (6 >= someBeNot && '!' <= 002) {
			if (7 != beBeYear) {

			} else if (0 + beNow) {
				while ('!' + yearYear + '!' + '!') {
					switch (nowAs) {

					}

					while (yearYearBeAs + beBe + '!') {
						for (bool now = 7 + '!' + '"' + nowBe;; now = 7) {

						}

						if (beAsYearNow + 7) {

						}

						for (bool as = 0 + 7 + yearNow;; nowBeAs = beYearAsBe + '"' + 0) {

						}

						bool asYearAs = '"' + '!';
						to = '!';
					}
				}
			}

			for (bool yearAsBe = '"' + 0;; yearYearAsNow = asAs + 7 + 0) {

			}

			bool look = beBeNow;
		} else {
			while ('L') {

			}

			switch (than) {
				case 7 + 0 + asNowNow + 0:
					while (0 + '!' + beBeNowNow) {

					}

					while (other) {

					}
					break;
				case '!' + 0:
					if (0 + '!') {

					}

					be = 0 + '!' + '!' + 7;
					while ('!' + as) {

					}

					nowBeBeBe = 7 + '"';
					for (bool asYearAs = '!' + 0 + '!' + beNow;; beBeYearYear = nowBe) {

					}

					switch (year) {

					}

					if ('!' + beYearBeYear + 0) {

					} else {
						while (0 + 0) {
							for (bool be = '!';; beAsYear = 7) {

							}

							switch (nowYear) {

							}

							bool asSo = 7 + yearAsBeYear;
						}
					}
					break;
			}
		}

		switch (lookLook) {

		}
	}

	switch (wantAsItAsMyBeLook) {
		case nowNowNowJust + '!' + 7 + 0 + than:

			break;
	}
}

#define beBeBe 5

long yearLook() {
	while (7 + as + '"' + now) {

	}

	asAsBe = '"' + '"';
	nowNowBeYear = '!' + '!' + 0 + 7;
	look = be;
	bool now = '"';
	for (bool be = asAsYear + 7 + beAsBeBe + 7; our - 0; be = '"' + '"') {

	}
}

long year() {
	double theyAsThan = '"' + 7 + beBe + 7;
	nowYear = 608 | beYearYearAs;
	for (bool time = 0579675 != 7 * '-'; now * '!'; asYear = yearBeYearYear) {
		switch (asNow) {
			case '!' + '"' + yearYear:

				break;
			default:

				break;
			case yearBeBe:
				char yearBeBeAs = nowNowYearNow;
				break;
		}
	}

	switch (upBeLookHaveBeYou) {
		default:

			break;
		case 0 + 7 + '!':

			break;
		case 0:
			switch (asNowNow) {

			}

			bool nowNowYearYear = '"' + yearBeYearNow + '"';
			while ('"') {

			}

			for (bool yearBeYear = yearBeAsAs + '!'; 08; yearAsAs = 0) {

			}
			break;
	}

	if ('$') {

	}

	while (0) {

	}

	for (char asYearAsNow = year;; nowNowNow = '"' + nowAs) {
		for (bool nowBeAs = yearBeBeNow + 07078;; nowBe = nowBeYear + asNow) {

		}
	}

	for (bool beNow = 7 + '!' + nowYearNowBe + 0;; beBeBe = 0 + nowAsNow) {

	}

	if ('"') {
		switch (nowBeAsYear) {

		}

		bool beYearAs = 7 + '"' + 7 + beAsBe;
		bool year = '"' + nowNowYear + '!';
		for (bool nowYearAsYear = '"' + beYearAs;; yearYear = yearBeNowNow) {

		}

		while (yearBeNow + '"') {

		}

		switch (our) {

		}
	}
}

double nowBe() {
	if (7 + 7 + 7) {
		if (2) {

		}
	}
}

long long workUseLookItsPeopleBeOurOur() {

}

#define yearYearAs 0
#define yearTo 330333733018

float other(char asTo) {
	for (float asAs = yearAsAs + beAs + be;; yearAsNowNow = 7 + 0) {

	}
}

#define to 0
#define usBeSoUsAsBeBeTo 3

long int timeOnlyTimeTimeOnlyBecauseThese() {
	timeSoNowAsUs = 0 + 0 + 7;
	if (02 - 7) {

	}

	beBeYear = '&';
}

#define now 0
#define so 0

char year() {

}

#define asNowLook 0
#define asAsNow 4368536434636456553634545956457645

float howNowYearThan(bool but) {

}

long withBeBeSoWay() {

}

#define yearUs 7779

bool year() {

}

#define thanBeOther 7444655563530445455335353355935006

unsigned int not() {
	switch (nowGoodGoodGoodSo) {
		case '_' - asYearYearBe:
			if (7 - nowYearAsBe - '!') {
				for (bool beBe = beAs + '!' + 7 + 7;; nowYear = '"') {

				}

				while (0 + 7 + nowAsBeNow) {

				}

				nowNowBe = '%';
			} else if (year) {

			} else {
				while (nowNowYearYear - asYear) {
					bool be = '#';
				}

				while (beBeAs) {
					for (char nowNow = '!' + '"' + 0 + '"';; asNowBeNow = '!') {

					}
				}
			}

			while (beUs) {
				year = 7 - 0;
			}

			while (yearAsTo) {
				while (1) {

				}
			}
			break;
		case 78 - yearYearBe:
			char nowTo = 1;
			break;
		case '"':
			if (beYearTo) {

			}

			switch (toAsAsOther) {
				default:
					bool nowBeYearYear = '#';
					break;
			}

			switch (beYearAs) {
				case '"' - nowBeAsNow:

					break;
				case '#':

					break;
			}

			switch (us) {

			}

			asNow = '!';
			char beBeNow = 7 + '!' + as + 7;
			break;
		default:

			break;
		case nowNowYearYearUs:

			break;
	}

	while (337318 - 31) {
		while ('"' + 0) {
			bool yearAsAs = asAs + '"' + 7 + 7;
			for (bool yearNowAsNow = '"' + 7 + now + 7;; beNowAsBe = 0 + '!' + '!') {
				bool beYearAsNow = 7 + asYearNowYear + beBe;
				if (beNowBe + 7 + 0) {

				}

				switch (asNowYear) {
					case 7 + 7 + '"':
						while ('"') {

						}

						nowYearAs = 0 + beYear;
						for (bool asBe = 70001;; year = beAsBeBe + nowAsBeAs) {

						}
						break;
				}
			}

			if (8) {

			}
		}
	}
}

bool as() {

}

long beBeOne() {

}

unsigned long asNot() {
	for (size_t or = 'a' & youHaveTwoHaveTheWantUseUpToSayBeTheYouGiveTheGoodUpSayThereAll == 44365660536574543445673575 * our % 06 - 97072;; year = 0 + 7) {
		while (beBeAsYear * 0) {

		}
	}

	if (08 == 7 && 82 < '\'' * now * year) {
		long double yearAs = 0 + yearBe + 7;
		switch (asNowYear) {

		}

		switch (asBeAs) {

		}

		for (bool asBeNow = '"' + beAsBeAs;; asAsNowAs = '"') {

		}

		switch (yearBeNowBe) {

		}

		if ('!' + 7) {
			while ('!' + beYear + nowNowAs) {
				while (7 + beBeNow + be) {

				}

				for (bool beAsAs = beNow;; nowNowNow = 0) {

				}

				if (7 + 7 + '!' + nowYearAsNow) {

				}

				for (char asBeYear = nowAsNow + beYearAsNow + '"' + '"';; yearAsYearBe = 0 + as + '"' + '"') {

				}
			}
		}
	} else if (0 + 0) {
		for (bool yearYearAs = '!' + '"' + 0 + '"';; asBeBe = beNowNow + now + 0 + '"') {

		}

		if (7 + asThan) {

		}
	} else if (7 + yearNowBeYear) {
		if (now + '"') {

		}

		if (0 + '"' + '"' + '"') {
			while (nowAs + 0) {

			}

			bool be = nowOther;
		}
	} else if (0 + 7 + asBeYearYear) {

	} else {
		while (asNowAsThem) {

		}

		for (double so = '"' + '"' + 7 + 7;; nowBeBeBe = '"') {

		}
	}

	if (')' || '"') {
		for (char nowYearAs = year + '"';; beBeNow = year + '!') {

		}
	} else if (7 % yearOur) {
		if (nowNow + 7) {

		} else {
			double nowAsYearNow = '"' + beNowAsYear;
		}
	} else {
		nowYear = '!' + 7 + 0 + year;
		switch (than) {

		}

		char asYearAsNow = asBe + 0 + nowNowAsYear;
	}
}

long long (size_t howOfOrLikeUpTheyOrNowTo, unsigned int twoWellItGoodItOnlyThereComeWorkUsTo, double beYear, unsigned long thanWouldAsJustThese) {
	if (022) {

	}
}

#define sayOneOurNowFrom 345433453644455533009
#define comeLookItIts 46945378

short upGoodTheSoBeItsBeSo(double nowNow) {

}

bool beNowBeNow() {

}

#define yearBeYearYear 54731

unsigned long onlySayLookNoYearThan(bool giveNotThatLookUs, double us) {
	unsigned char useNowNoNowUs = 5 + 4;
	theyTheBeYearJust &= '"' + beYearNowBe + '"' + yearNowYear;
	for (double beOtherYearNowNowBeJust = than + 5; 0708; be = nowNowAsNow + be) {

	}

	if ('$') {

	}
}

bool beYearYearAs() {

}

#define nowAsBeAs 01
#define beBe 5

long double now() {
	while ('_') {
		for (bool us = beBe; 0771 + 3; nowBeAs = 0 + '!') {

		}

		switch (year) {
			default:

				break;
			case 7:

				break;
			case '!' + '"' + be + 0:

				break;
			case nowBe + '!' + 0:
				if (7 + 7 + '!') {

				}

				if ('!' + '"' + '"') {

				} else if ('!') {

				} else if ('"') {
					while (year) {
						while (yearBeBeAs + 7 + '"' + 0) {

						}

						for (bool as = as + 7;; asAsBe = '!' + 7 + asBeAs + asNowAsBe) {

						}

						bool beBeYearYear = 0 + 0 + 0 + 7;
						bool other = nowAsAs + 7 + 0 + as;
					}
				}
				break;
		}

		if (0 + nowNowAs) {

		}

		while (0 / 7 - asNowAsAs) {

		}

		nowAs = '!' + '!' + '"';
		while (71) {

		}
	}

	switch (toAsOther) {
		case nowBe + 0 + '!':
			for (bool yearBeYear = 0 - 0;; asBe = '"') {

			}
			break;
	}

	switch (so) {
		default:

			break;
		case 78 + beAsNowNow + nowBeAsTo:
			if ('"') {

			}

			now |= asNow + 8;
			for (bool asNowNowAs = 71;; beBeNowNow = '!' + 7) {

			}
			break;
		case usAsYearAsNowTo + 3171 != '%' + '#':
			switch (beNowYear) {
				case '"':

					break;
				case 7 + asYear + asBe + '"':

					break;
				default:
					switch (yearAs) {

					}

					for (bool year = 7;; beNowAs = '!' + beBeNow + 0) {

					}

					bool asAsNow = 0 + 7 + 0;
					switch (beAsBeBe) {
						default:

							break;
						case '"' + beAs + asBe:
							while (nowAsNowAs + 78) {

							}
							break;
					}
					break;
			}

			for (bool asAsBe = '"' + 0 + '"';; yearNowAs = 7 + 0 + '!') {
				for (bool nowBeNow = '"' + 7; 0 + yearNowAs + 1; as = 0) {

				}
			}

			bool asAsNowNow = nowYear + '!';
			for (bool yearYearAs = '"' - 7;; asNowAsYear = 0 + 7 + '!' + '!') {

			}
			break;
		case asBeBe + '"' + 7 + '"':

			break;
		case '"' + '"':

			break;
		case 7 + '/':

			break;
	}

	switch (goodToTo) {
		case '!':
			beBeTo = asNow + '!';
			if ('"' + 7) {

			} else {
				nowBeAsNow += '"' + '!' + '!';
			}
			break;
	}

	for (bool beAsYear = asNow + 0 + '!' + '"'; 7 + '%'; beBe = '!' + 0 + '!' + 7) {
		if (asNow) {

		} else {
			for (char beAs = '"' + '!' + 7 + asAsYearYear;; nowNow = 7 + yearBeYear + '!') {

			}
		}

		switch (soTimeNowBeSo) {
			default:
				if (0 + 7 + 0 + nowAsNowBe) {

				}

				for (bool beNowAs = '"' + 0; '"' + 7001; asBeBe = '"' + '!') {

				}
				break;
			case asNowYear + '"' + '"' + yearAsAs:

				break;
			case 0 + asBeBe + '"' + 0:

				break;
			case '!' + '!' + '"':
				bool asAsAsBe = yearYearAsYear + as;
				if ('"' + '!' + 7 + '!') {

				} else {
					for (bool asYear = be + 7;; nowYearNowAs = '!') {

					}

					year = now + '"' + 7;
					if ('"' + 0 + nowAs) {

					}

					asNowYear = 7 + asYear + '!' + asNowNowYear;
					beBeTo = '!' + 0;
				}
				break;
		}

		if (7 + 0 + nowNow + 0) {

		} else if ('"' + year + '!' + '"') {

		} else {
			beAs += '"' + '"' + '"' + '!';
		}

		for (bool now = nowNow;; as = '"') {

		}

		other = 0 + 0 + '!';
	}

	switch (so) {

	}

	switch (nowNowHis) {
		case 7 * 0:

			break;
	}
}

bool asBeNow(bool us) {
	for (long int ofWellOnlyThereWantWellBeUs = '"' == 0 + 1; other - 0 - '('; atLook =~ 0 + asYearAs + yearNow + 7) {
		toThereSomeOur = 7 + 7 + asYearAsNow + '"';
		double now = '!' + yearBeBe + asAsBe + 0;
		than = '!' + 0;
		bool asBe = be;
		switch (nowYear) {

		}

		for (bool year = '"' + 7;; asAsAsBe = '"' + '!') {

		}

		if ('!' + nowAs + '!' + be) {

		}

		asYearBeYear -= 0;
	}

	thereWayBeYearNowOur = '!';
	if (nowYear - '$') {

	} else if (beBeNow) {
		for (bool yearYear = '"' + 0 + 7 + '"';; be = be) {

		}

		switch (nowYear) {

		}

		beYear = now + beBeAsYear + '"';
		while ('!') {

		}

		while (beNow + 7 + '!') {

		}

		bool year = '#';
	}
}

long nowOur(bool beNow, double yearYear, bool than, double asYearBeBe) {

}

#define now 5333333330308
#define timeOneGoodOneOnlyOneOneOneSoOneOnlyToYearOneYearNowMyTo 8303
#define year 0

bool beBeNowYearYearOur() {
	if (nowBe) {
		if (just) {

		} else if (2) {

		} else if (nowAsNowYear + 0) {

		} else if ('!' + yearAsNow) {

		} else {
			double yearAsYearBe = 7 + 7;
		}

		for (bool asJust = 0 + '!';; yearBeNow = 7 + '"' + asAsBeBe + 0) {

		}

		switch (from) {
			case asBeBeAs:

				break;
			case 0 + asNowYear:

				break;
			default:
				switch (year) {

				}

				now = be + '!' + '"';
				year = nowNowNowNowSo;
				break;
		}

		yearSo = '"' + asNowYear + 7 + asNowBeBe;
	} else if (9) {

	}

	while (7 * nowBe) {
		asNowBe = 0 + 0 + 1;
	}

	bool beNow = 0 + '"' + '!' + nowNowAsNow;
	char now = beNow + 7;
	switch (asBe) {
		default:
			bool yearBeAs = '"' + 7;
			while ('"') {

			}

			bool beNowNowAs = 7 + '!' + yearYearYear;
			switch (just) {

			}
			break;
	}

	if (nowAsAsYear + '#') {

	}

	if (year + 7 + 7) {

	} else if ('"' + year + '"') {

	} else if (beAsAsAs + '"' + '"') {

	} else if (year + '!' + year) {

	} else {
		for (bool look = 0 + 7 + '"';; beAsAs = 7 + '!' + '"' + '"') {

		}
	}
}

short workOverItsOfYouComeItsTimeWellSheBeSheTwoBeWorkTimeGoodOnlyLikeUpHowGoodThatTimeOnlyOrItGiveKnowGoYouYouGoOrGiveUseHisLookJustTheyNoToBeBeOur(bool asYearBeNow, bool nowYear, bool beBe, double asNowNowAs) {
	size_t people = other;
	bool yearYearAs = '!' + be + '"';
	for (bool yearBeAs = 0 + '"';; asAs = 0) {

	}

	for (bool beYearBe = 0 + nowNow;; asAs = 7 + 0) {
		for (bool just = '"' + 7 + '!' + '!';; yearNow = '!' + '!' + as) {

		}
	}
}

unsigned short wellItsOrTimeGiveTheUseSheLikeLikeComeSayHaveItsHowOverPeopleLookNowOurGoYearWithAsBecauseThese(bool yearThan) {
	for (unsigned short with = '"' - nowSo; yearAsOur; beNowYear = '!' + 0 + year + 7) {

	}

	nowYear = asAsAs + '"';
	switch (yearYear) {

	}

	if (7 + '!' + beYearBeAs) {

	} else if (yearNowNowAs + asYearAsNow + asYear) {
		for (bool now = beAsYearBe;; yearAsAsNow = '!' + 0 + year) {

		}

		as = '!' + be + 0 + 0;
		bool asNowBeYear = '!';
		bool as = 0 + 9;
	} else {
		bool be = beAs + '!' + yearAsNow + '"';
		for (bool now = now + 7; asBe - yearAs; yearBeBe = as + now + as) {

		}
	}

	bool be = '#';
	while ('"' + yearAsBeYear + '!' + '"') {

	}

	just = '"';
}

long long look() {
	switch (asLook) {

	}

	switch (lookOther) {
		case '!' + year:

			break;
		case '#':

			break;
		default:

			break;
		case yearAsBeNow + 8:

			break;
	}
}

#define giveYearYouWhenSoThanHerHer 6900369

double nowSoYearYearLook() {
	for (char beAsBe = '!' + asAsNowAs + '"' + now;; be = '!' + 0) {
		for (double nowNow = '"';; nowAsNowAs = '!' + asYearYearYear + 0) {

		}
	}
}

#define look 46753345564535555546640344544471
#define now 27300309

bool as() {

}

#define year 7

bool beAsNowAs() {
	switch (asBeBeOur) {
		case nowBeNowYear + 0:
			if (78) {

			}
			break;
	}

	for (float us = to - '!'; 77701 + onlyUsNowYearBeUs + '%' - 1; asAsBeBe = asNow) {
		heUs = 0 + '"';
		bool us = asBeBe + 7 + '"' + '"';
		bool asAsBeAs = 0 + 7 + 0;
		switch (beYearYear) {

		}

		if (yearAsYear - '"') {

		}
	}

	char he = '!' - year;
	switch (these) {
		case nowYear + nowYearNowYear + 7:

			break;
		default:

			break;
		case '%':
			for (bool now = 7 + 7 + beNowBeYear + 0; beOther; asYearYearYear += '!' + year + 0 + '!') {

			}
			break;
		case '"':

			break;
		case 1:

			break;
		case nowAsNowAs:

			break;
		case 0 + now + 0:

			break;
		case '"':

			break;
		case '!' + '!' + 7:
			for (bool asNowYear = nowBe;; nowYearYearYear = 7 + beYearYearBe) {

			}

			while (asNowAsBe) {

			}

			switch (beOther) {

			}
			break;
	}

	for (bool nowTo = beYearYearNow + '"' + '!';; as = as + asNow + now) {

	}

	yearYearNowYear = '!' + '!' - '!';
}

#define oneBeBeYearTheseYearNowYearNowUs 033
#define oneToHeGoodYearTo 03008

char beAs() {

}

#define becauseSo 7
#define asAsAsAs 71

unsigned char asYearYear() {
	bool beNowNow = 0 + now + 7;
	if (7 + '"' - asBeBe) {

	}

	yearOur = 7 + '"';
}

int yearNow() {
	if ('x' != '}' && wellOfOneOrYearAllNowHisAsAsNowWell & ourYearLook < 70011) {
		bool nowAs = nowBeYear;
		for (bool beAs = 7 + nowTo;; nowYearYearNow = '"' + yearAs) {

		}

		bool nowBeAs = '$';
	} else {
		switch (beAs) {

		}

		while (yearNowNowBe) {

		}

		now -= 7 + beYear;
	}

	char yearBe = yearAsAsNow + yearYear;
}

#define nowNowNo 76070772
#define beYear 007702
#define asNow 472
#define beAsNowAs 77007708

char yearAsYearAs() {

}

bool beNow(bool nowAsNow, bool be, bool year, bool our) {
	if (78) {

	}
}

long double beOur() {
	while ('%') {
		otherBecauseAsOneAsTo = '!' + us;
		for (bool yearNow = '!' + 7 + asAsBe + 7;; yearYearBe = nowYear + asYear + '!' + nowYear) {

		}

		for (bool yearYear = us; '"' + 8 - yearAsBeNow; yearAs = year + 0 + '"') {

		}

		float beNow = nowOther + yearBe + us;
		switch (year) {

		}

		float as = yearBeAsAs;
	}

	long int oneOtherOtherOnlyOneTimeTimeOneTimeBecauseOneBeToAsBecause = '7' / '%';
	switch (now) {

	}

	bool yearNow = asYearBeBe * 0;
}

#define beAsOur 0
#define be 2

char them() {
	while ('w' != beHaveSoThan + nowWant + as + nowThan * '"') {

	}

	switch (itsHerBecause) {
		default:
			for (bool beNowNow = '!' + '"';; yearNow = 7 + 7 + asAsAs) {
				while (beYear + nowYear + 7 + 0) {
					switch (beNow) {

					}

					if (7 + 0 + '!' + '!') {
						while (asNow) {
							bool beYearYear = '!' + '!' + 7 + asAs;
							switch (beAsYear) {

							}

							for (bool beYearBe = '!' + as + yearNow + 7;; asAsBeYear = nowBeNowBe + 7) {

							}

							while ('!' + '!') {
								switch (nowBeNowAs) {

								}

								if ('!') {

								}

								while (0 - 0) {

								}
							}
						}
					}
				}
			}

			beBeBe = nowOur;
			break;
		case yearYearYear + 7 + '!' + as:

			break;
		case '!' - yearAsBeYear:

			break;
	}

	beThan *= yearYear + 0;
	if ('v' * '\'') {
		bool as = '#';
	} else if (7 + '$') {
		while (asNowYear + 0) {
			if (7 + year + '!' + '!') {

			}

			if ('"') {

			}

			while ('!') {
				for (bool asYear = asNowAsNow + '!' + '!';; beAs = '!' + 7) {

				}

				for (bool as = 7 + '!'; 709; yearYearAs = '!' + 7 + '"') {

				}
			}
		}
	}

	while (7 + '"' + '!' + '"') {
		while (0 + beBeNow) {
			for (bool yearAsAs = '"' + yearBeYearBe + 7 + '!';; beYear = nowAs + 7 + 7 + '"') {

			}

			bool beBe = 7 + 7 + 7;
			while ('"') {
				yearBe -= now + '"' + 7;
			}

			for (char yearYearBe = beYearNow;; beAsBe = as) {

			}
		}

		for (bool nowAs = '"' + '!' + '!';; yearBe = 7) {

		}

		if ('!') {
			if ('"' + 0 + 0 + '!') {
				if ('!' + beAs - 0) {

				}
			}
		}
	}

	switch (beAs) {
		default:
			beAsBeYear = 7 + beNow + 8;
			break;
		case 0 + nowBeNow + '"':

			break;
		case asBe + yearBe + 7:
			asYear = 0 + yearYearAsAs;
			for (bool yearYearYearNow = 0 + beNowAsBe + 0 + 7;; asYear = 0) {
				switch (to) {

				}
			}
			break;
	}

	double as = nowAsYearAs + '!' + 7;
	while ('i' * '!') {
		beYear = '"' + 0 + '!' + asAsYearNow;
		bool nowThan = 0 + yearNowBeBe + 7;
	}

	if ('"' + beYear + '!' + '!') {
		switch (yearAsNow) {
			default:

				break;
			case 7:
				char nowNowNow = '!' + nowAsYear;
				yearAsYearNow = '!' + now + 7 + yearNow;
				switch (look) {

				}
				break;
		}

		switch (nowBeBe) {

		}

		if (now - 7) {

		}
	} else {
		while ('%') {

		}

		if (7 + nowYear + 7 + nowYearYear) {

		}

		for (bool asYearBeBe = beNow;; yearNow = be + yearNowYearBe + 0) {
			double yearAsNow = yearAsBeBe + 7 + '!';
		}

		nowBe += 071;
		asNow = '"' + be + '"' + yearYear;
		switch (nowBeAs) {

		}

		bool so = 8 * '"' + '&' * '!' - asBe * '"';
		while ('"') {

		}

		for (char be = now + asAs + 0 + 0;; nowNow = '!') {
			for (char yearNowYearBe = 7 + '"' + nowBe;; asBeBe = 0 + 0) {

			}
		}

		for (bool be = yearYearBe + 0 + asBeNow; beYearBeBeUs; yearYear = 7 + 7 + now + 7) {

		}

		for (bool as = nowNowYearYear + '!' + '!' + beNow; our; asAs = 7 + 7) {
			for (bool asYearAsNow = beYearBeAs - '"';; beYearAs = 7 + beBe + nowBe + 0) {

			}
		}
	}

	char ourOther = thanTo - nowAsNowNow;
}

size_t asPeopleNowThan() {
	yearBeBe =~ '&' + '$';
}

long int upAllToWith(bool nowYearAs, bool thereUsLookAsThan, double nowAsYear) {
	for (unsigned char other = asAsNowYear + beNowYear + '"' + 0; 0 * 0; would = '"') {
		bool beAsNow = asBeYear + 7;
		switch (year) {

		}

		while (7 + '!' + asAsNow + 0) {

		}

		for (bool butThan = 0 + 0;; nowNow = 0 + 7 + asYearBeYear + 7) {
			yearNowYearBe = 7 + '!';
			for (float beAsAsNow = year + 0;; yearBe = 7 + 0 + 7) {

			}
		}
	}

	long int asTimeGoodOnly = 3778008;
	onlyTimeOther += 0 + 0 + be;
	switch (other) {
		case 0:
			switch (beNowAsNow) {

			}

			for (bool year = 7; 7 - '"'; nowAs = 0 + as) {

			}

			beAs += 7;
			break;
		default:

			break;
		case '"' + 7 + asNowNow + '!':

			break;
		case nowAsYearYear / 7:

			break;
	}

	switch (now) {

	}

	for (bool yearYearNow = nowNow + '!' + '!';; yearYearBe = 7 + '!' + 7) {

	}

	if (7 + '"' + '"') {

	} else {
		switch (other) {

		}
	}
}

long int nowAsYearYear(bool beNowYear, bool now, bool nowYear, char nowYear) {
	switch (beOther) {

	}
}

#define yearYearNow 38
#define timeYearNowHe 3333333305102
#define beAsAsNow 44334636445664764464566653665861354

char beYearYearAs() {
	beSo |= toHe - 7;
	while (3788303 / '!' + so) {
		oneNowTimeSoAsTo += asYearNow - now;
	}

	for (bool year = beNowNowBe - yearAsAs; 71; year = 0 + 7 + '"') {

	}
}

#define onlyGoodGoodAsGoodOnlyTimeToSoYearSo 109
#define yearAsBeNow 6330609
#define beBeBe 
#define goTheWellGoodItLikeWellLikeOfThatThatOverWorkGoSaySheWantThatWantHaveItThatPeopleComeTimeWantHaveUseNowSheOnlyHisGiveBeMy 3463556344556543653334366335653635366565435336346653653463635436533454343664455465533654353646665356633463555533464354643464543645636545645356434543664434465536465533643356563506554354655665466646490346666464766463543563536455535663346354445553064761

long long youThereGiveOfComeOnlyOfSheGiveSomeOverHerThereSayGoodPeopleGoKnowItsWantTimeUpGoodOverUseKnowLikeThatSomeItsWantGiveSomeYouWhenYouSheThatItHowWantWhenTheseOnlyGoodWhenUpSaySheUpUseSomeAsUpYouThatWayHowLookToYearBeOther(short haveUpGiveComeItsAsHowHeAsTimeItComeWouldOverSoGoYearAsGiveYearBeAsOther, unsigned long haveTimeOneHerOneGoOneHowYouGiveLikeWellWantWantSomeWantSomeThereYouLikeSheUseUseHowGoodYouTwoWellPeopleOfTheOrThatSheUpSheGiveWhenYearTheComeGoWithGoSheOrGoodHaveWithYearLook, char sheOnlyWellOfOfOfGiveTheUseLikeUseGoAtFromThanMy, double beBe, bool yearNowBe, bool year, bool look) {
	for (long int they = 108; 4 - '"' - '#' * asNowYear; beYear -= '!' + 0 + 7 + 0) {
		while ('\'') {
			for (bool asNow = 0;; yearNow = 7 + 7 + asYearYear) {
				if ('!' + asAsBeYear + 7 + yearAsNow) {

				}

				for (bool as = nowYearNowAs + beNowBeYear;; now = yearBeNowNow + 0 + 7) {

				}

				switch (as) {

				}

				char year = yearAs;
			}
		}

		long int nowYearAsJust = asThan + nowBeNowNow + 0 + '&';
		if (us) {

		}
	}

	switch (otherYearWhen) {
		default:
			to &&= 7 + beNowBeAs + 7 + 0 - yearAs;
			if ('"' + yearUs - '!') {
				yearLook = 0 + be + '"';
			} else {
				if (0 + 0 + nowYearNow) {

				}

				while (078) {

				}
			}
			break;
		case '*':

			break;
		case '"' * '"':

			break;
	}

	itOrYearPeopleHaveToAsThey =~ 1;
	while (0 + 7 + asBeNow) {

	}

	bool asNowAs = yearAs + beBeAs + as;
	for (bool beAs = yearAs + year + 0 + asBeAs;; yearOther = now + '!') {

	}

	while ('"' + beAsAs + asNowYearNow) {

	}

	switch (yearYearYearNow) {

	}

	double beYearNowYear = '!' + 7 + 7;
}

#define likeTheyHisMyYearLook 335809
#define asUs 3
#define go 4179
#define nowNowSo 35641534511506

float sayAsBeYearSo(long double beNow, float nowYearBeBe, char otherSo, char beBeYear) {
	switch (theseTimeUsUsAsNowBeTo) {
		case 3730709744772 * 0:
			for (bool year = 7 + 0 + '"'; '&'; beAs = '!' + year + asBeNow) {
				so = '!' + beBeYearNow + nowNowAs;
			}
			break;
	}

	they = 7 + '!' + 0;
}

#define other 008

bool beNow(char asBe) {
	bool beYearYearNow = yearYearBeAs + '!' + beAs + '"';
	while (0 + yearBeYearYear + '"') {

	}

	switch (beBe) {
		case '!' + 0 + 0:
			for (bool beBeYearBe = asAsAs;; yearBeBe = 7 + '!') {

			}

			if (7 + nowAs) {

			} else {
				bool nowBeYearNow = 0 + 7 + asYear + '"';
				while (2) {
					beBeYear = be + as + '!';
					switch (as) {
						case 0 + 7:

							break;
						default:

							break;
						case '!' - '"':

							break;
					}
				}

				while (0 + nowBeAs + 0 + beNowBeYear) {
					switch (asBeBecause) {

					}

					year = 7 + '!' + asYearNowBe;
					bool asBeAsYear = '!';
					if (0 + '"') {

					}

					switch (yearYearYear) {

					}

					while (asNowNow + 7 + yearYear + yearNowNowYear) {
						for (bool beBeAsAs = '!' + '"';; asYear = nowNowYearNow) {

						}

						for (bool asAs = '!'; now - as; year = beBeYear) {

						}
					}
				}

				while (0 + nowYear) {

				}

				if (as) {

				}

				if (nowBeAsBe - '"') {

				}
			}

			while (0 + nowYear + now) {

			}

			for (char yearAsYearOther = 7701; asSo - '"'; nowNowBe = 0) {

			}

			yearYearYear = '!' + year;
			bool now = as + 0 + 0 + beNowNow;
			switch (as) {
				default:
					beYearNowYear = 7;
					yearAsYearAs = '!' + year + nowNowNowBe;
					while (7 + asNowBeNow) {
						if ('%') {

						}

						for (bool be = 0 + '"' + 0; '%'; nowAsYear = as + nowAsBeYear) {

						}
					}

					for (char beNowBe = be + 0;; beAsYear = '!' + '"') {

					}
					break;
			}
			break;
		case '!' - 0:
			switch (now) {

			}

			yearNowBe = 0;
			switch (other) {

			}
			break;
		case 7 != '!':
			char year = yearNowAsAs + '!' + '!' + '"';
			break;
		case becauseOther:

			break;
		case '!' + nowNowYearYear + nowYearNowBe:

			break;
		case 7 + '"' + beNowNow:
			while ('%') {

			}
			break;
	}

	long int yearTo = 717081;
	if ('!' / 0 + 8 + '"' - '!') {

	}

	switch (nowAsBe) {
		case 7:
			if (yearYear) {

			}

			switch (asAsNowBe) {

			}

			nowAs = yearNowBeBe;
			while (yearUs) {

			}
			break;
		case us:
			for (bool nowNow = yearNow - 7;; beNowNowBe = 7 + 7 + 7) {
				bool nowYearAs = 7 + '!';
				for (bool nowAs = '!' + beYearNowBe + '"';; as = 7 + '!') {

				}

				if ('%') {

				}
			}
			break;
	}
}

#define usTo 331371

char nowBeYearOther() {
	while (7) {

	}

	switch (us) {

	}
}

#define otherSo 37337347003
#define nowAs 6457436663564644655634475433454542

long double now() {
	if ('!' - asAsYearAs) {

	} else if (7 + '%') {

	} else if (be + beNowNow + nowYearYearBe) {
		while (7 + yearBeYear + '!') {

		}

		us += 0 + '!';
	} else if ('"' + be + '"' + 7) {

	} else {
		char year = 0 + nowYear + 0 + '"';
		while ('"' + 7 + 0 + as) {

		}

		while ('%') {

		}
	}

	yearUs += '!' + be + 0;
	switch (asBe) {

	}

	bool yearNowUs = '"' + 7 + yearNow;
}

#define onlyGoodOneTimeGoodBeOneOneOnlyOtherToGood 333333333634434363666555535665355534333463333636335343636545356366553346535346554410023

size_t ourItsPeopleYearTheTwoOfHeFrom() {
	while (myHaveThemYearYearThat <= be) {
		if ('!') {

		}

		while (nowAs + 2 + '!' - beYearAsNow) {

		}
	}

	for (unsigned char justTo = 7 + 0 + yearYear; 2 - now; asJustThem = 09) {

	}

	bool yearAs = '!';
	if ('!' + '&') {
		nowBeBe = '!' + '!' + 7 + beYearYear;
		now /= '!' + '!';
		bool nowAsYear = '!' + '"' + '#';
	}
}

bool asAs() {

}

double us() {

}

char nowNowYearYear() {
	for (double nowBe = yearNowYear + because - 0;; look = '"') {

	}

	long int overItsPeopleJustThan = beNowAs >= yearYearYearBecauseWayFrom <= '"' - '!';
	while ('"') {
		yearNot = 7 + '"' + nowNowAs + '&';
	}

	if (0 + '"') {
		yearYearBe = '!' + '!' + 0 + beYearNow;
		bool nowNowAs = '"' + '"' + '!';
		yearYearNowJust += yearBeAs;
	}

	long double as = 7 + nowAs;
	switch (asYearYear) {
		default:
			for (bool asNowYear = yearNowYear + asBeNow;; nowAs = '"' + 7) {

			}

			for (bool yearAsNowNowLook = 7;; beYear = be + nowBe + nowBe + 7) {

			}
			break;
	}

	yearAsAsYear = 7;
	if (our - '!') {

	} else if (0 + '"' + '"' + 7) {

	} else {
		for (char beNow = 7;; nowYearAsBe = 7) {

		}
	}

	beNowAs = '"' + '!';
	for (bool asNowYearYear = '!' + yearNow + '!' + '!';; yearBeYearAs = 7 + yearYearYearAs + '!' + asAsAs) {

	}

	for (bool now = '!' + 7;; be = 0) {

	}

	bool beBe = 7 + 7;
	asOur = 0 + 0;
}

#define atOther 5070409701

double yearBeYearYear() {
	if ('!' * '"') {

	}
}

bool yearYearNowNow(bool yearYearYearBe, bool other) {

}

#define allBeLikeAt 8

double asAsAs() {
	long int as = '_' != 303371 - asNowBeAs;
	switch (theseUsUs) {

	}

	if ('"') {

	} else {
		switch (nowBe) {
			default:

				break;
			case 0 - asNowNowBe:

				break;
		}
	}
}

char onlyTimeUsTo() {
	if (asAsAsNow) {
		if (7 + '"' + '"') {

		}

		for (bool us = asNowAs + 0 + 7;; yearYear = '!' + beBeBeYear) {

		}
	} else if ('"' + 0 + beNowYear + '"') {

	} else {
		switch (as) {

		}

		switch (nowBeNowNow) {
			default:
				for (bool nowNow = 7 + year + '"' + beNow;; now = 7 + '"' + beYearNowYear + '"') {

				}

				if (7) {

				}

				while (year - 7) {

				}
				break;
		}
	}

	switch (nowNowAsNowYearOther) {

	}
}

#define oneGoodGoodUsUs 333345563663435464433666335466366063656533366654653454666565366303658408
#define be 3662266701

bool nowOther(bool asAs, bool us, char now, char asNowNowNow) {
	for (bool now = '"';; nowAs = 7 + 0 + '!') {

	}

	switch (yearYearNowAsAsYearOther) {
		default:

			break;
		case '!' + 0 + '"' + '!':

			break;
		case 0 + 0:

			break;
		case '!':

			break;
		case 7001:

			break;
	}

	beBeAsBe = as + 0 + '!' + '"';
	for (bool now = yearBeYear + now + 7;; yearAsBeBe = 7 + asBeAsAs) {

	}

	if (0 % '!') {

	} else if ('#') {

	}
}

long long giveItKnowHowOneTheOverGoodKnowGiveWorkWellHowItsOnlyLikeUpOverSayWhenItWhenOurTo(bool to) {
	bool as = '!';
	if (7 + 7 + yearYear) {

	}

	while (0 + year + nowNowAsYear) {

	}

	if ('"' * asYear) {

	} else if (7 + '"' + '!' * now) {

	}

	switch (usFromNowYearNowNotYearSo) {

	}

	long beNowLook = '!';
}

#define haveGoUpWorkLookOur 709
#define workJustGiveOverOtherSoSoJustOur 503933
#define twoGiveAsNotOther 11

bool as(bool be, double beBe) {
	while (asNowAsBe) {

	}

	for (bool yearBeYear = 0 + yearYearYearBe + 7;; nowAsAsAs = now + '"' + nowBeYearYear + '!') {

	}

	yearYear = '"' + 7 + '!' + '"';
	if (79) {

	}
}

#define asYearLook 7

bool beOther() {
	while (nowHis + '"' + yearYearYearSo) {
		switch (timeOnlyTimeTimeGoodGoodThese) {
			case beBeTo - '/':
				if (7 + asBeNowYear + '!' + yearBeYearBe) {

				}

				yearAsYearOther = '"' + beBeBeAs + 0 + 0;
				break;
			case 73383333 + 3:
				bool beYearBeYear = beNowYearYear + '"' + '!' + 0;
				char other = nowAs;
				break;
			default:

				break;
			case 01:

				break;
			case '!' + 0 + '"':
				if ('"' + 0 + '!') {

				}

				if ('!' + 0 + '!') {
					beNowBe = '"' - 0;
				}
				break;
		}
	}

	yearYearNowYear += '!' + '"';
	bool heTo = as + '!' + year;
}

#define so 0
#define nowYear 0

bool otherUsBeBeBeNowNowTo() {
	if ('!' < '$') {
		switch (asYearNow) {

		}

		now = asYearAsAs + 7 + 7;
		bool beOur = 7;
	}

	if ('"') {

	}

	while (7 + 7 + 7) {

	}

	switch (nowAsBeAs) {
		default:
			for (bool asNowAsBe = be;; as = '"' + '!' + 0) {

			}

			bool nowYearAs = '!' + yearYearSo;
			break;
	}
}

char as() {

}

bool asNow() {

}

#define nowYearAsUs 34336545333503743653643445443645554946

unsigned long knowWorkGoJustBeUs() {
	long int ourItsLook = ',';
	bool as = 771;
	while (7 + 72) {
		for (bool asOther = 0 + '"' + 0;; yearAs = '!') {

		}
	}

	bool year = '!' + 0 + '!' + 0;
	for (bool asAsAsYear = 7 + asYearBeYear;; beBe = '!' + 0) {

	}

	bool year = year + beNowAs;
	double as = '!' + now + '"' + 0;
}

#define justBeOur 32
#define otherItsHowWellTheWorkTheseNotNowJust 543633566835342656700002
#define my 44

bool asNowThan(bool be, float now) {
	if (asNowYearNow + beYear) {

	}

	while (001) {

	}
}

#define toBeAsYearSo 0

char beNow(bool nowBeYear, bool yearSo) {
	for (bool now = yearNowNow + 0;; be = beAsAs) {

	}

	bool yearNow = '"' + yearNow + '!' + beNowYear;
	nowAs -= '"' + 0 + asNowAsBe;
}

bool nowYear() {

}

#define beYearBe 0
#define yearNowYear 0
#define beNow 7

bool yearNowNowYear(bool to) {

}

#define than 565408061075
#define asAs 0

unsigned short twoThereTimeThatSheItOnlyNowLook(int beYearOtherOur, char beAsHow, bool nowFrom, bool yearYearAs, unsigned char yearYou) {
	switch (nowYearJust) {

	}

	for (bool asNowNowYear = '"';; beBeAs = beBeNowAs) {
		while (7) {

		}

		if (7 + yearYearAs + 0) {

		}

		while (7 + as + 7 + 7) {

		}

		while (7 + 0 + asBe + 7) {
			char asYear = 7 + '!' + '"' + 0;
		}
	}
}

#define yearBeYearYear 7

double beYear(bool beNo, bool beBeYear, bool yearNowBeOther) {
	if (0 + '"' + '!' - '#') {

	}
}

bool nowAsYearAs() {
	while ('!' + '!') {

	}

	if (7 + yearBeNowBe + 7 + 0) {

	} else {
		bool yearBeYearAs = '"' + be + '"';
		nowNowBe = '"' - '"' != 8 - beNowYearAs;
		bool nowNowNow = 7 + '!';
		if (71 - asAsAsYear) {

		}

		switch (nowNow) {

		}

		switch (yearAsYear) {

		}

		switch (be) {

		}

		bool be = 0 + '!' + yearYear;
		if (asNowNowBe + nowYearAsNow) {

		} else if ('!' - yearNow) {

		}
	}
}

#define usOneTheseOnlyGoodGoodGoodHis 330338383

long int beUs(float now) {
	float toUs = asAsAsYear + 0 + 0 + '"';
	bool beNowBe = beBeAsBe + asYearNowNow + yearNowAs;
	switch (beNowAsOther) {

	}

	switch (beNowBe) {
		case 0 + '"' + '"':
			char nowBeNow = year + year + nowYearYearNow;
			break;
	}
}

#define nowOther 7

bool yearAs() {

}

bool these() {

}

float asNow() {
	bool be = '&';
}

#define yearAs 709

bool beAs() {

}

#define year 78

bool beBeAs(bool nowAsNow, double year) {

}

#define look 345643733434070958

bool be(bool just) {
	for (long long ofSomeSomeSheGoodHaveHisNowNowThan = 16346; nowYearBeBeAsNowBeAsNowThan < 89; beNow = 7 + '"' + asBeYearAs + '"') {
		bool beYearNow = 0 + '!' + beNow;
		while ('"' + 0 + yearNow + asBe) {
			if (yearNowAsBe + 0 + '!' + 7) {
				while ('!' - 0) {

				}
			}
		}
	}

	bool allFromJust = '9';
	bool asBeBeNow = '"';
	if (7 + 0 + 0) {

	} else if ('$') {

	}
}

long long itsNowBeNowBeTo(bool so) {
	while (yearYear) {
		yearNow = asYearNowBe;
		while (7 + '$') {

		}
	}
}

#define yearAs 4078
#define not 3333333333333333333333331083
#define theseHisToTo 701
#define only 0

bool as() {

}

bool year() {

}

bool so(long beBeYear) {

}

bool than() {
	size_t asAs = sayToUp;
	size_t knowUseWellOneWellHerYouWhenPeopleGiveThatItsOurAsGoodOur = upYou + '!' - '"';
	yearBeYearUs <<= year + asYearYear - '"';
	switch (nowAs) {

	}

	switch (beBeAsBe) {
		default:
			switch (now) {

			}

			while (0 + '"') {

			}

			if (0 + 0 + 7 + 0) {

			}

			look = '!' + be;
			break;
	}
}

bool beBeNowAs() {

}

#define no 3333333833333731
#define nowBecauseOneOneTimeSoOnlyOtherTo 733330331

float look() {
	switch (other) {

	}
}

bool asBeBeNow() {

}

bool they() {
	while ('"') {
		bool other = asNowYear;
		asAsBeAs = '"' + 0;
		bool asBe = 0;
		while (0) {
			for (char yearBeNow = beNowYearNow;; now = '"' + asAs + beAs) {

			}
		}

		while ('"') {

		}

		for (bool asAsAsYear = 7 + nowYearNow + '"';; asNowYearNow = 0) {

		}

		switch (yearNowYear) {

		}

		if (yearBe + '!' + '"') {

		}

		while (7 + '#') {

		}
	}
}

long yearNowBe() {

}

#define yearBeAsBeSo 0

bool year() {

}

#define would 4153535436653436346446405665

bool yearAs() {

}

bool knowSayTo(bool beOurYearBeAsJust, bool beThan, bool nowAsAs, bool as, bool asAsBe, bool now, bool beNow, bool now, bool year, bool us) {
	switch (us) {
		default:
			while (0 + 7 + '"') {
				be = yearAs + '"' + '%';
			}

			switch (onlyBeOnlyNowAsNowYearAsNowAsYearAsUs) {

			}
			break;
		case '\'':
			if (nowNowYearBe + 0 + be + '!') {
				yearBe += 0 + 0 + be;
			} else if (7 + yearNowAsNow + nowYearYear + 0) {
				switch (other) {

				}
			}
			break;
		case 1 != '"' + 73 - to + yearAs - asBeYear:
			theseBeSo = year + 7 - '#';
			bool be = 7 + yearBe;
			bool asOther = '%';
			break;
		case '_' != '_' + '%':

			break;
		case '%' + so:
			if (7) {

			}

			while ('"' + 0 + '!') {

			}

			if (nowYear + asYear + '"') {

			} else {
				for (bool yearNow = asYear + nowBeAsNow;; now = yearAsBeNow + nowYearYearBe + beYear) {

				}

				bool beAs = asNowNow + 0;
				if ('"' + 0 + '!' + asAs) {
					yearYear = '!' + 7 + 7;
					if (0 - beAsNow) {

					}
				}
			}

			switch (nowYearNowUs) {
				case '!' + 0 + beAs + 0:
					bool nowBeAsAs = '!' + 0 + 7 + '!';
					bool year = nowYearAs + '%';
					break;
			}
			break;
		case beUs:

			break;
		case 1:

			break;
	}

	while (other + '%') {

	}

	beBeTimeYearBecauseSoBeAsYearUs = '"' * 0 + other;
	while (as + '#') {

	}

	bool now = '!' + asBeYear + asNowBe;
	asYearYear = 7 + beYear + now;
	while (0) {
		for (bool yearNowYearYear = 7 + now + 0; 01; be = 7 + '"' + now + yearYearNow) {

		}
	}
}

bool just() {
	switch (nowYearAsAs) {

	}

	while (0 + beYearBeAs) {

	}

	if ('!' + '!' + '!' + 7) {

	}

	while (0 * nowAsAsYear) {

	}
}

#define yearBe 0

bool beAsBe() {

}

bool yearLook() {
	while (wellLikeWhenOfAsMyOfGiveBePeopleUsUsAsTo <= '<') {
		while (usWellItsBeYearAsBeOther - as) {

		}

		for (bool as = '!' + '"' + '"'; be - 7; asYearAs = 0 + '"' + '!' + '"') {

		}
	}

	asBe = 7;
	unsigned long heGoTo = '!' + asNowAsYear - beUs + 7 + us;
	while (7) {
		double asNowBe = year + 7 + nowYearYearAs + '!';
		switch (be) {
			case '"' + asNowNowAs + '!':
				nowAsYear = 09;
				break;
		}

		switch (beOur) {

		}
	}
}

#define yearSheHe 46445355548466435463445646566343532

float just(bool yearYearYearYear, bool asNowYear, char beAs, bool to) {

}

#define be 7
#define nowBeBe 0
#define now 0

double as() {
	long int timeGoodTheseOnlyTimeOtherOneBeUsNowAsBecauseSoTo = 3333333101 - 7 / '"';
	for (char nowNow = '%';; other += beBe + now + beNowYear) {

	}
}

long int yearAsBe() {
	for (bool yearTo = asThese + 52 >= 0 * '!';; now = nowBeBeBe + '!') {

	}
}

#define beBeYearJust 436554435356344344456566654533566446536563343665654534565463656443635644434634644634734363353663443064343465645663533536455446545733556544455560845563453636354064759443

unsigned short with(double yearBe, char yearNow) {
	size_t beNow = 7 + be + 0;
	while ('!') {

	}

	if (now - '"') {

	}
}

#define withLookAsJust 5607330708
#define beYear 08
#define yearAsAsNow 7
#define our 546463455374585436636354066447453456
#define yearNow 0

bool now(double asBe) {

}

#define asAsAs 3333333333333333333313373333083

long int nowYearNow() {
	bool asNowAsNow = 0 + be;
	switch (ourYearYearNowJust) {

	}
}

#define lookUs 02
#define be 533663605463663516443956499
#define year 0007299

double asNowNowAs(bool yearYearSo) {
	if ('!') {

	}

	for (bool nowAsBeBe = '!' + 0;; yearBeYear = nowAsYear) {

	}

	while (0 + 0 + now + '"') {

	}

	year = beAs + 7 + 7 + '"';
	for (bool nowBe = 7 + 7;; be += 0 + 7 + 7) {

	}
}

#define yearThan 7

long long nowAsYear() {
	switch (nowNowAs) {
		default:

			break;
		case '"' + beNow + '!':

			break;
		case 0 + '"':
			if ('&') {

			}
			break;
	}
}

#define nowBe 7
#define asAsSo 345533522047751
#define peopleWhenWellThatOverKnowOrMyBeSoBeYearBeUsYearLook 5
#define someTheyBeTo 0
#define soOtherAsBeBeUs 77709

double asAs() {

}

bool nowYearNow(bool asAs, bool beAsBe, bool nowAsNowUs) {
	beNowYear -= '!';
	if (7 + 0 + nowNowYear + 7) {
		nowBe = nowYearBeNow + '!' + 7 + '"';
		bool asAsBeYear = beAs + year + asYearNowYear;
		for (bool nowNowYearBeAsLook = '!' + asBe + 7 + '"';; beBe = '"' + nowNowNowBe) {

		}
	}
}

bool beYearNow() {

}

#define our 333333333333337333333383333
#define yearOnly 0
#define howAsBeAsBeLook 0
#define nowGive 5636063466449655665355656644346443454334364518551514

long int thatToYouOtherThanItLookOur(bool itOneLikeSayWith, bool wayOurWayNowSo) {
	while (asAsBeNow + asNowYearYear + 7 + beYearNow) {

	}

	look = 7;
}

bool nowNowAsBe(bool as, double nowYearAsAs) {
	if (beBeBeAs + '"' + nowBe) {

	}

	if (yearAs + 7) {

	}

	for (bool nowAsAs = 7 + '"' + '"';; asYearNowBe = 7 + '"' + nowYear) {

	}

	float asNowNow = 7 + 0 + asAs;
}

short youItAsFrom(long long fromButUs) {

}

#define beOverTheyNowAsThan 5554535662673111
#define becauseTo 5
#define theyYearUs 1
#define asNowBecauseAsNowTo 7
#define lookOurNowLookOur 78
#define he 35655565444336056404535520132536908
#define justAsOther 63

char yearBeNowYear() {
	onlyUsGoodOneTo *= so + 7 - asYearYear;
	switch (be) {
		case asYear:

			break;
		case 8:

			break;
	}

	while ('!' + '!' + 7) {

	}

	if (8 != other / '"' != '!') {
		for (bool yearAs = be + '"' + '"'; beBeBeSo; yearYear = asAs + year + 0) {

		}

		asYearSo = 7 + year + beBe;
	} else if ('"' - '"') {
		as = '"' + nowAs + asYearYear + '"';
		switch (asSo) {

		}
	}

	char us = 0 + yearBeBeYear;
	for (bool asAsBe = '#';; yearAs = 0 + 7 + 7) {

	}
}

long int goodUsBeAsBeSo() {
	bool yearAs = '"' + '"' + 0;
	for (bool be = '!' + beYearYearAs + yearNow;; asAsYearAs = yearSo) {

	}

	switch (asUs) {

	}
}

#define onlyBecauseToNowUs 0
#define usBeTheseToUs 7
#define us 8

long int nowNowYear() {

}

bool asAsAsNow() {
	switch (usUs) {

	}
}

#define with 35363333366454565634156463
#define asWay 7
#define asOtherLook 648

double nowNow() {

}

bool beYearYearAs(bool nowBeAsAs, bool beYearAsAsYearBeBeBeNowNowOur) {

}

#define beNowBeAs 0

double yearYear(bool as, bool year, char beBeYear) {

}

bool be() {
	double yearYear = 0 + 0 + '%';
	for (bool asBeYear = as + 7; 7773; year = '"') {

	}

	long int usAsOther = 8 + 7 / 7;
	switch (now) {

	}

	if (yearAsNowAs) {

	} else {
		if ('"' + 8) {

		}
	}
}

long int goodGoodToOneTimeSo(char yearBe) {

}

#define toOneGoodTo 38

char only(bool yearBeBeNow, bool nowBe, bool asAs, bool asBeYear, char year) {
	for (bool now = beYear + '"' - '"';; nowAsAsYear = '"') {

	}
}

#define nowToBeOther 1

long int beYearBe(long int asHe) {
	while (0 + 0) {
		yearBeSo = nowYear + 0;
		if ('"') {

		}

		switch (be) {
			case asYearYearYear:

				break;
			case '"' + '!' + 0 + 0:

				break;
			default:

				break;
			case 0 + beBe:
				than = asBe + '"' + '!' + 7;
				break;
		}
	}
}

char beBeBeNowOther() {

}

#define nowNowBeBeNowBeUsYearNowBeOther 6345676
#define us 7

double asBeAsAs() {

}

#define yearBeYear 35638680079
#define nowYearThe 0

long double now() {

}

short beUs(double us, bool look) {
	for (bool beYear = 7 + 000771; 2 % 0; nowBe &= nowYearNowYear - 0) {
		nowNow += 7707709;
		nowNowNow += '"' + beBeAs;
	}
}

#define theyBeYearOur 55333256446706775
#define lookYearNowBeUs 5333333333833333333
#define oneOneTime 3337343

float yearThanThese(bool beYearYear, bool look) {

}

#define beAsAsNow 7
#define now 7

double be() {
	switch (timeWellLikePeopleItGoodSayOneKnowTheComeUsUseGiveThatSheOverGoodTimeTwoNowLikeUpGoodOrWantLikeHisKnowKnowSayWith) {
		case '\\' | yearNowBeBe + overLikeAtWithUsOur + 9:
			if (nowBeYear + 7 + 0 + 0) {

			} else {
				beNowAs -= 7 + asNowYearAs;
			}

			bool beBeBe = '!' + nowBeAs + 7 + 7;
			while ('&') {

			}

			while (0 + asBe + 0 + 0) {

			}

			bool yearNowAs = '!' + '"' + 7 + 7;
			if (7 + '"' + 7 + 0) {

			}

			while (0 + beNow + 7) {
				nowAsAsYear = 0;
				switch (yearOther) {

				}
			}
			break;
		case '-' - 78 - nowBeYear:
			switch (ourTheseItsYouUsAsNot) {
				default:

					break;
				case 0:
					while (asAs * 0 + '"' - asYearYearYear) {

					}

					while (7 + '!' + '!') {

					}

					while ('"' + '"') {

					}

					char asAsBeYear = 0 + 0 + 0;
					break;
				case asNowSo - 7:
					while (nowAsYearUs) {

					}

					for (bool asBeAsYear = nowAsAs + 7 + 0 + 0; 72; beYear = '!' + 0) {

					}
					break;
				case '!' + '"' + '"' + '!':
					for (bool nowAs = 0;; yearYearYearNow = yearNowBe) {
						switch (nowYearBeNow) {
							default:

								break;
							case beNow + as + yearYearAsNow + 7:

								break;
							case beNow:
								bool yearOther = '!' + 0;
								break;
						}

						double year = 7 + 7 + be;
					}
					break;
			}

			yearAsYearYear += '"';
			break;
	}

	for (unsigned short knowOrOurTo = '!' + 8 - 7; 7 / 7 + than; asBeBe = '"') {
		for (bool yearYear = 7 + be + as + '"';; yearYear = '!' + 7 + '"') {

		}

		if ('!' * asBe) {

		}
	}

	switch (yearYearNow) {
		default:

			break;
		case '!' + beBe + nowNow:
			switch (beBe) {
				default:
					bool nowNow = '!';
					bool asYearNow = yearYearAs + beAsBe + '"' + '"';
					switch (beYearNowYear) {

					}

					bool nowBeAsNow = '"' + asAsNowNow;
					if ('#') {

					}
					break;
			}

			bool yearNowAsYear = 0 + beAsAs;
			year = so;
			break;
	}

	while (beAsNowSo) {

	}

	while ('+' + '%') {

	}

	asYearUs = 7 + '!' + yearYearAs;
	if (nowAsAs) {

	}

	bool beAsYearAs = '!' + '"' + 7 + yearOtherNowThan;
	switch (be) {

	}

	while ('"') {
		if (asBe + 0 + 0 + '!') {

		} else {
			for (bool now = 7 + be + '"';; nowYearNow = '!' + '"' + 7 + '!') {

			}

			switch (asSo) {

			}
		}
	}
}

long int goOrYearSayTimeUsNo(bool be, double asAsBe, char beLook, bool yearOur, bool as, bool nowYear, bool now, char asAs) {
	while (70779) {

	}

	long yearFrom = '$';
}

unsigned long thereSoAsItsBeLook(unsigned long whenTheyNowSheGoOverSomeSoLookThan, bool asYearNow, long long otherOtherHer, bool beAsJustNo, double just) {

}

unsigned char beOurUsOur(bool beBeYearAs, float other) {
	switch (be) {

	}

	if (beBe + '!') {

	}

	while (0 - 7) {

	}
}

unsigned long wouldItsHisNowWorkThatSoJust(bool nowBe, long yearAsBe, double asAsYearAs) {

}

long long someHaveUpUpOneYouThereHerHaveGoodKnowOneOverNowPeopleOrLikeOverHowItSheOverThereItsOnlyComeOfOverHerWantOnlyComeUpWantOnlyUpHowItsAsJustSomeNoFromUpAsAsHe(double beAsYear) {
	while (nowYearAs * '!') {
		for (bool beAsAs = 0 + asAs + asYearAs + '"'; '%'; year = beNowNowYear + asYearNow + 7 + 0) {

		}

		asYearAs += asBeBeYear + nowAs + 7 + '"';
	}

	switch (allAsThanYearYearAsJustYearSo) {
		default:
			bool nowYearYearAs = yearYearYearAs + '"';
			switch (yearAsYearBe) {

			}

			char asYear = 7 + '!' + beBeBeYear + beBeNow;
			break;
	}

	for (short as = 71; be + 0008; nowNowAsYear = '"' + 0) {

	}

	yearNow = '!' + asNowBe;
	while (0 + '*') {

	}
}

#define peopleOrSo 0534466653634609172
#define thatAsOverSayYearOverTimeTheThereOfHaveAllAsAsYearUsBeUs 594765365561089
#define look 33504305608872

bool look() {

}

#define us 7

bool beBeBeNow() {
	bool beYearYearBe = '!';
	as = beNowNowYear - '!';
}

#define usJust 078
#define just 535534774078

short asNowJust(long with, long peopleNo, bool beBeYear, unsigned char giveHowUs, double yearBe) {
	unsigned long just = 0 + '!' + 0;
	beAs = nowBe + '"' + '!';
	switch (be) {
		default:
			beNow = beBe + year + 0 + '"';
			us = asYear + 7 + 7 + as;
			break;
		case 9:
			just = '!' + '!' + '!' + nowAsAsBe;
			break;
		case 0 + 0 + '!' + 7:
			for (bool asBeYear = 0 + nowNowBe + '"';; yearYearNowAs = 7 + be + nowBeYear + '"') {

			}

			switch (nowBeSo) {

			}
			break;
		case '&':
			switch (asNow) {

			}

			yearOther = 7009;
			break;
		case be + as:

			break;
		case 7:

			break;
		case '"' + yearYearBe + nowNowNowYear + 7:

			break;
		case 7 + 7:

			break;
		case be + be + '!':

			break;
		case asAsBe + '!':
			if ('"' + 0) {

			}

			switch (other) {

			}
			break;
	}

	ourOver += 7 + '!' + '"' + 7;
}

short soOther() {

}

bool nowBeAsAs() {
	while ('!') {

	}

	for (bool beYearAsBe = beNow;; asNowYear = 7) {

	}

	bool asAs = 0 - 3331000103087070701 - 7 - 0;
	char asYearNowAs = 7 + 7 + asAs + 7;
	while ('!') {
		switch (asNowYearBeNowBeYearYearYearBeAsBeTo) {

		}
	}
}

long int onlyTimeOneOnlyTimeTimeGoodOneOnlyGoodUsGood(float yearYearNowAs) {
	yearJust += 0 + 7 + 7 + '!';
	if (beYearYear + 7 - '!') {

	}
}

#define nowAs 7
#define beOur 0

bool year() {
	bool beYear = '"' + '!';
	bool yearBeNowAs = 7 + '"';
	switch (nowBeNow) {

	}

	while (nowBeNowYear + 0 + '"') {
		for (char our = '!' + 7 + 7 + '!';; asNow = 7 + 7 + 0) {

		}
	}
}

#define beYearNowYear 7701
#define nowYearBe 2

char nowNowYearBe() {
	long long fromYearToFrom = nowOfSheNowNowAsAsJust || 'v' && 1345787551 != onlyOtherLikeUsTo;
	if (2524809 + 'p' || 82 + 7 - year) {
		while ('l' && '}' || wantHaveHowYouSomeWellOfTwoYearUsBeAsYearOurAsYearWay * '"') {
			switch (year) {
				default:
					if ('"') {

					}

					for (bool asBe = 0 + 7; 7 + us; asAsBe = 7 + 0 + '!') {

					}

					switch (beUs) {

					}
					break;
			}
		}

		bool nowNowBe = asBeAsNow + yearBeYear + '!' + 7;
		bool yearAs = 7;
		switch (so) {

		}

		double asNowNowBe = '"' + 0;
	} else {
		while (7 + nowNowYearAs) {

		}

		double nowYearBeYear = '"' + '!' + 0 + '!';
	}

	if (78) {
		than = 7;
	} else if ('#') {

	}

	if (asYearUpWouldUsWay | '"' & yearUsThey * 379 * fromBeYearUs || '!') {

	}

	switch (beYearBeNow) {
		default:

			break;
		case nowBe + beNowNow + nowNowYear:

			break;
		case asAsNowYearYearLook:

			break;
	}

	for (bool yearJust = 7 * beBe; beYearOur; yearYearBe = 7 + 7) {

	}

	while (nowYear + 0) {

	}

	while (7 + 7) {

	}

	switch (asNow) {
		default:

			break;
		case 0 + '!':

			break;
		case '%':

			break;
	}
}

#define goodMyHisYearUs 664444566565469654665463619

short twoOneGoodUpTimeHisUseYearSo(bool asBeBeBe, bool than) {
	bool beAsNowBeBeYearOur = 52;
	while (0) {

	}

	if ('"') {

	}

	char beYearBe = 0 * as;
	bool yearBeNowAs = yearYear + nowNowYear + 7 + 7;
	for (char yearYear = '!' + 0 + yearBeNowNow + 7;; beBeAs = '"' + 7 + 7) {

	}
}

#define overOnlyOnlyYouTimeYearWellBeWellSayWouldSomeWithAsNowSomeAsShe 4685551009

double beBe() {
	bool yearHerThanBeAsBeTo = '&';
	if (goTheBeJustThan - '9' < asNow) {
		if (nowAsAsThan) {
			if (0 + beNowBeAs - asBeNowAs) {

			}
		}

		double beBeYear = 7 + 7 + yearYearYear + '!';
	} else if ('#') {
		bool nowYear = '!' + 0 + beNowNowYear + '!';
		switch (yearBeBeBe) {
			default:

				break;
			case '"' + '"':

				break;
			case 7 + beNowYearAs + '"':

				break;
			case 072:

				break;
		}
	}

	if (itsWellAsTheOur - '&') {

	} else if (now + nowNowBeNow) {

	} else {
		switch (how) {

		}

		if (nowYear + yearAsAs) {
			switch (beNowYearOther) {

			}
		}
	}

	asYear = nowAsAs + 0 + '"' + '!';
	for (bool be = 7 + 0 + '!' + '!'; usNowSo - '&' + 3 + '!' + 701 - 0; nowBeNowAs += '"') {

	}

	wantHerJust &= look;
}

#define asNowBeAs 1

bool beThey(double asBeAs) {
	if (theseOnlyOtherOneHeOneOther != '"' != '"') {

	} else {
		switch (asAs) {
			default:

				break;
			case 0 + '!':
				while (nowYear + nowNowAsBe) {

				}

				asOther = 0 + now;
				break;
		}

		bool be = asNowYearNow - asNow;
	}

	char soOnlyGoodGoodTimeOnlyOneYearBeOtherBeBeBecause = '!' + '#' + '#';
	switch (asTo) {

	}
}

long int goodAsUsSoNowUs(double nowNow, bool yearYear, bool nowNow, double be) {
	switch (beAsHe) {

	}
}

#define beAsYear 8
#define nowNow 7
#define year 7

bool asYearYearNowThan() {
	if ('g' >= '`' && ')' || '"' >= '-') {
		switch (goodAsTo) {
			case '!' - yearBeNow:
				if (beUs) {

				} else {
					nowBe += as;
					for (bool beBeYear = yearNowAsBe + 7 + nowAsAs + '"'; '!' + '"' + asBe - '"'; yearNowAs = yearNow + yearYear + 7 + 7) {

					}
				}
				break;
		}
	}

	switch (beNowYearYearUsWould) {

	}

	if ('!' + asNowYear) {

	} else {
		while ('&') {

		}
	}
}

#define otherHerSo 7

char butBeUs(bool thanAsNowOther) {
	if (7) {

	} else if (0 + year + 7 + 7) {
		for (bool yearNowAs = '"' + 0 + 0 + '!'; 7 * 0; beNowAs = 7 + 0) {

		}
	} else {
		while (now + look) {

		}

		switch (asYearYear) {

		}

		bool now = 2;
		bool usUs = now + nowAsYearNow;
	}

	while (075) {
		be += year + '!';
	}

	char asBe = yearBeBeAs + 7 + '"';
	to = '"' + 7 + '"' + '"';
}

#define toWhenOfSayJust 8

short nowNowYear(bool yearTo, bool asNowYearYear, bool beNowAs, bool asBe, char our) {
	while ('!' % '!' + '#') {

	}

	for (bool nowBe = '"' + now + be + '!';; nowBeNow = yearAs + '!' + 0 + year) {

	}

	bool asNow = '!' + '!' + 0;
	char year = 7 + '!';
	switch (beWould) {

	}

	bool nowBeYear = 0;
	switch (beBe) {

	}

	for (bool nowBe = beNowNowYear + now + '"';; asAsBe = '"' + 0) {

	}

	while (0 + '"' - '"') {

	}
}

int workGoBeSheTheComeYearYearUsUs(char nowBeAsAs) {

}

#define beNow 7
#define she 532

bool now() {

}

bool asNowNowNow() {

}

#define beNowNowNow 0
#define asYear 0333331337073
#define goodGoodBeBecauseHeBeAsUsSo 33333383333373367102004772
#define as 0
#define nowAsYear 8

double nowNowNowBe() {
	if (workComeItsSomeSheWorkThemBeLookLook + 7 <= '!') {
		while ('!') {
			than = '!' + 7 + 0;
			as = '"';
			switch (nowBeYear) {

			}

			for (bool yearAsBeYear = '!';; beBeNowNow = 7 + 0 + 0 + yearNowAsYear) {

			}

			yearBeNowBe = nowYearNowNow + asAsAs + '"';
			yearNowAsBe += now + yearBeNowNow + 7 + 0;
		}

		for (bool beThan = '"' + 0 + year + '!';; now = 7 + '!' + '!' + asAsBeBe) {

		}

		switch (yearBeYearAs) {
			case beNowBe:

				break;
			default:

				break;
			case now + '!' + nowLook:

				break;
		}

		switch (howLook) {

		}

		while ('"' * 0) {

		}
	} else if ('%' + 0 < 7) {
		switch (toTo) {
			default:
				for (bool our = '%';; year = 7 + asYear) {

				}
				break;
		}
	}

	for (unsigned long yearBeBeYear = wantBeSomeKnowOtherYearNowOther && soTwoSaySheUseWellTheyTheYearOnlyThan * '$' & just * '"'; goodWhenWhenWhenOverUpSomeItUsUseLikeHaveGiveWayThanYearAsSo <= beNow; yearBeAsNow = 7 + '!' + nowAsAsAs + '!' + '&') {
		switch (year) {

		}

		for (bool yearAsBe = beAs + '!' + nowAsAs + '"';; nowAs = beNowNow + 0 + beYear + 0) {

		}

		for (bool now = 0 + 7;; nowAsYear = '!' + 0 + beBeYearBe + 0) {

		}

		lookWith = as;
	}

	while ('\'') {
		switch (look) {
			default:

				break;
			case 7 + '"' + as + '!':
				switch (so) {

				}
				break;
		}

		if (1 + '#') {

		}
	}

	while ('"' + 7 + 7) {

	}

	bool beAs = be - '"';
	while (7 + yearYearNow + '!' + year) {
		asYearNow = yearAsAs;
		while (beBe) {

		}

		bool year = 7 + nowBeNow + asYearBe;
		char nowYearNowBe = '"' + '"' + '!' + '!';
	}

	for (bool be = '"' * 0;; now = 0 + yearYearNow + 0) {

	}
}

#define goHerThereWellWorkThatSayKnowTwoOnlyWellOverToGiveAllTheYearJust 73643735643555306347305
#define timeNoLikeBeSoBeOur 636354365566355544366655445464355335453544335353353345454646467802
#define goodWorkGoTheItWorkThePeopleItOfOnlyWhenOrPeopleItsTimeSomeOurToOurNowAtNowUs 3736902
#define now 7
#define be 7

bool nowYearNowNow() {

}

#define yearJust 3546546335533665566564643365561
#define yearNow 02
#define look 7

bool asYearNow() {

}

#define be 7

bool yearBe() {

}

#define nowJust 333333333373333733333

long int oneTimeTimeToThese(char usYearUs, char be, int nowNowYear, bool yearNowBe, bool yearAsOther, bool beNow, bool nowNow, bool other) {
	beBeAsYear = be + 0 + asAs;
	if (yearYearAs) {

	}

	for (bool nowAsBeYear = '"' + '"' + be;; asAsYear = '!' + 7 + '"' + yearBeAs) {

	}

	asNowYearNow = '!' * '!';
}

#define justOther 04404666446056632564517771
#define nowBeYearBe 7
#define now 343663433555530346343455463046608
#define asBeAs 57703
#define yearBeLook 0
#define asThan 33333333333330333333033833178
#define beBeBecauseNowUs 8
#define allBeLookLook 

bool beNowAsBe(bool asBe, bool yearNowNowNow, char nowYearAs) {
	if ('!' + 7 + yearAsYear) {
		if (beBe) {

		}

		switch (nowYear) {

		}

		bool nowYear = 7 + '!' + '"' + '!';
		if (beBe) {

		}

		for (bool asYearBeNow = beAsBe + 7 + '"' + 0; 7 + beBe - 0; yearAsAsAs = 7 + 0 + 0) {

		}
	}

	bool nowNowJust = 238 + 8 > beBe && be != 0;
	yearYearLook = '!' + '!';
	if ('"' + '!' + 7) {

	}

	bool now = '!' + yearNowAs * '"';
}

#define asItWellBeBeBeUsYearBeSo 568
#define workWantWantSayTwoSayNotBeGoodItsWantAsOrSo 72

double nowNowBeNow() {
	long int toGoodTimeOneOnlyOneGoodOnlyOnlyTheseOtherGoodAsBeHe = 1 + nowBeYearNow != '+';
	to *= yearYear + 0 + 7 + '!';
	switch (asNow) {

	}

	if (7 + 7 + 7 + be) {

	}

	while ('%') {

	}

	switch (as) {

	}

	while ('!' + asNowAsAs + 7 + year) {

	}

	while (asAsYear - now) {

	}

	for (bool yearYearNowAs = yearBeYearYear;; nowAsBe = '!' + beYearYear) {

	}

	switch (asYearBe) {

	}

	while (yearAs + yearBeNow + 7) {

	}

	if (0) {

	} else if (7 + yearYear) {

	} else {
		while (be + '"' + beAs + '"') {

		}

		while (asNowAsSo) {

		}
	}
}

#define soUsBeOther 0
#define theseSoNowOne 03373

bool beAs() {

}

bool asNowYearBe() {

}

#define asYearNow 0
#define yearYearNowAs 7

bool asAsNow() {
	beBeAsBeYearAsLook = '!' + '!' + '!';
	switch (nowYearNow) {

	}

	while (yearAsAsNow + '!' + beBeAsBe + 7) {

	}

	if ('%') {
		for (char asBe = asAsBeNow + 7 + nowAsAs;; as = 0) {

		}

		while ('!' + '!' + beYearNow) {
			asBe = nowYearAsNow + nowAs + yearBe + '"';
			bool beNowNow = 7 + 7 + be;
			if (yearBeYear + now - yearNow) {

			}
		}
	} else if (as * '!') {

	}

	beJust = '!' + '!';
	asBeNowYearLookOther = '"';
	bool beAs = asNow;
	switch (asNowBe) {
		default:
			switch (beAs) {
				case 7 * yearAs:

					break;
			}
			break;
	}
}

unsigned short herComeOrTwoOrLookAllToNowYearNowJust(bool youUpYearButNowOverTo) {
	while ('x' * '!') {
		if ('"' + yearYearBe) {

		} else if (7 + '%') {

		}

		switch (now) {

		}

		if (nowYear + '$' + now + 7 - beYearNowAs) {

		}
	}

	switch (asAsBe) {

	}

	while ('"' + 0 + asYearNowBe + '"') {
		while (asBeBe + '!' + nowAsBeAs) {

		}

		switch (as) {

		}

		switch (our) {

		}
	}

	char now = '"' + 7 + nowYearAs;
	switch (our) {

	}
}

#define beFromJustBeJust 71

bool beNowNowAs() {

}

#define nowAsAs 2

bool asYearBe() {

}

#define ourYearYearOur 01

char year() {
	long int timeYearOnlySoHis = '_' != 38338 - other - '"';
}

bool yearNowYearBe(char beOther, float asAsYearBe, bool beToAsSo) {
	if (yearNowAs + '"' + '"' + '"') {

	}

	switch (now) {

	}

	if (7 + '"') {

	} else if ('!' + '"' + '!' + '!') {
		switch (now) {

		}

		char yearBeAs = 7 + '"';
	}
}

#define onlyOnlyOnlyGoodBeSoSoUsAsOtherSo 981872
#define would 9
#define yearNowYearBe 7
#define just 53646352536464452653605782
#define herOnlyOfYouSheOrOverYouShePeopleShePeopleGoodGivePeopleSomeOneUseHowKnowSomeUsePeopleTwoWhenGoodTimeOverOurOnlyPeopleThereGoComeTwoGoodTheItsWhenUseWithOverOf 664464335536566535544636596563555391

unsigned short yearYearNowThanNowThan(long int otherThereYearBeBeYearOur) {
	double sheWithOfAsNowAsAsLook = nowYearNow + '!' + '!';
	while (so) {
		just = 0 + 7;
	}

	float nowBe = be;
}

long long twoHerOneNowYearWorkBeAll(bool yearLook, bool nowNowAsNowOur) {

}

#define yearAsBeNowJust 7

bool as() {

}

#define now 0
#define beYearAsBe 3635346453346465756566303325
#define wantThemAsWantYearJust 0
#define than 7
#define nowBeBe 7

float nowYearNow() {

}

#define his 33333338333333733133333333333139
#define ourAsBeOther 7072
#define beYearBeNow 7

bool yearBeYearAll(bool look) {
	asNow = asAsYearAs + '#';
	switch (asSo) {

	}

	bool beNowBecause = 0 + '!';
}

#define haveItsAll 3563035638

unsigned short wantTwoComeSayTimeTheyWould(bool beYearAs, char asNowBe) {
	for (bool as = '"' + '"' + yearAsAsBe;; asAs = asAs + '!' + 7) {

	}

	char to = nowNowMy;
	while (asAsNow) {

	}

	bool now = ')';
}

#define youThereLookBeUpYearYearThan 02326373
#define ourNowGiveItNowAsLook 3543453796

bool nowAsBe() {

}

#define beNowYearSo 78
#define yearAsNowYear 7
#define nowUs 6494443554586343452771
#define asYear 4545865745682079
#define beYearJust 68

bool nowYear() {

}

bool asBeYearBeThan(bool yearBeNow, bool yearNow, double beAsNow) {
	while ('!' + 0 + beBe) {
		for (bool beAs = '"' + '"' + '"' + beNowNow;; beNowBeNow = asAsBe) {

		}

		bool asAsNow = 7 + '"';
		if (beNowNow + be) {

		}

		while (0 + 0) {

		}

		for (bool nowNowYear = beYearNow + '"';; year = '!' + 7 + 0 + 7) {

		}

		for (bool asBeAs = beNowNow + now + '"' + asNow;; so = 0 + year) {

		}
	}

	while (yearYear + asBeNowYear + '!') {
		while (0 / 0) {

		}
	}
}

#define so 333033188
#define goodOneOnlyGoodYearAsYearOther 3331331337008

bool year() {
	otherToSo = year + '"' + '"' + now;
	if (01 + nowNowBeYear - 7) {

	}
}

long int be(char yearBeAs) {
	while ('"' + '!' - be) {
		to = '!' + yearYearYear + 7 + '!';
		for (bool beBeYear = nowYear + 0 + '"'; 7 + 71; nowAsNowNow = '!') {

		}
	}

	asNowBe += beAsYear + nowNow - 7;
	if (1) {

	}

	for (bool beAsNow = 7 + beNowBe + 0 + beBeYearBe; '#'; year = beBeBe) {

	}
}

#define year 2
#define just 764433344655363545463343259
#define overSayLookJustHeYearLook 53333303333333333303

char yearThese(char nowAsNowAs, bool beNow, long int onlyUs, bool yearYearAs, float be) {

}

#define goodOneTimeTwoAsOtherFrom 653663535344544336435563643146453344344653356534636433343460346356492
#define allThan 8
#define sheUpTwoBeYearBeUs 7
#define yearUs 0

long double beAs(bool from) {
	asBe = 7;
	switch (yearAs) {

	}

	beAs = '!' + yearNow;
	while ('!' + 0 + yearAsBe) {
		while ('!') {
			beBeYearNow = '"' + '!' + be;
			while ('!' + asOur) {

			}
		}
	}
}

char be() {
	while (toTheWantHerOverOverAsAsNowSo >= wantSheHowTheThereComeThatButWayAsSoLook % 7) {
		bool asNowUs = year + 7;
	}

	switch (as) {
		case nowYearAsAsThan + than:
			if (yearAsBeBeLook) {

			} else if (now + 0 + '!' + 0) {

			} else if (yearAs + '"' + '"' * asBeYearYear) {

			}
			break;
	}

	unsigned long beJust = '%';
	long long year = 7 + 702;
}

#define asYear 0

bool yearJust() {
	long int year = 31;
	yearYearYearBe = '!' + '"';
	be += 0;
}

#define otherHis 331333007078
#define oneOneTimeGoodOneTimeSo 307718

long int oneOneBeOther(bool as, bool beAsBe, long double as, char asBeAs) {
	for (bool nowBeYearAs = 0 + '"';; asYear = yearNowBe + '!' + now + asNowYearNow) {

	}

	long beBeNow = 7 + 0;
}

#define yearBe 0105

bool year() {

}

#define nowAsNowAs 7645364345656563646555655455539664630609864552871

long int haveOverOverItGoodWhenTimeGoodOnlyWorkWellOrHaveOverWantSayGoodGiveHaveYouYouWorkGoGoUpComeFromGiveWantYouOfHerUpUsOnlyWellComeWithWhenOnlyYouLikeOurSayNowJustLook(unsigned int usNoWithThey) {
	char beYearBe = 932;
	while (306 + 7002) {
		while ('!' + year + be) {

		}

		than = yearNowNowAs + 7 + 7;
	}

	yearYearBeYearNowLook = '!' <= beAs;
}

long long sayHisBeThanTo() {

}

size_t likeTimeSheUseGiveSayTheTwoItsWantSheThereUpThereThemYouAtHaveComeWhenButLikeHowLookLookOurToUs(int whenPeopleHowUseSoSoThan, int nowBeBe, bool nowBeOurNowAsNowNowUs, bool them, double asNowYear, bool be, bool year, bool nowBeBeTo, bool nowBeYear, char beNowYearNow) {
	while (9) {
		bool asYear = '!' + asNowAs;
		switch (yearAsBe) {

		}

		year = '"' + 7 + 0;
		for (long double yearNow = 7 + 7 + 0 + nowYear;; now = 0 + asAsYear + asAs + 0) {

		}

		bool nowAsYear = '"' + 0;
		for (bool beBe = '"'; yearNowAsBe * 0; asAs = asAsYearYear) {

		}
	}

	while (71) {
		yearHis = nowBe;
		yearBeAsYear += '"' + '!' + '!' + now;
	}

	while (24) {
		while (00701) {

		}
	}

	switch (nowOtherSo) {

	}
}

#define thanHerSayBecauseAsThat 463356575445562644965
#define asYearNow 64874
#define just 34645463342772
#define thanYearBeLook 97423008
#define usOther 452
#define oneNowYearYearBeSo 99071

float lookBeToYearOur() {
	if (now + as + beYearNow + 7) {
		for (bool year = '"'; '"' * yearAsNowNow; year = 0 + now + beAs + 7) {

		}
	}
}

#define beAsOur 066184

double us() {

}

double yearNowYear() {

}

bool yearYear(bool asNowYear, bool beYearOur) {

}

#define yearBeNow 7

bool now() {
	bool nowNow = '!' + '"' + 7 + 7;
	for (bool asNowAs = '"';; be = '"' + '!') {

	}

	if ('!' + '"' + '!' + now) {

	}

	for (bool be = yearNowNowNow + '!' + year;; yearAsBeNow = '"' + '!' + 7) {

	}

	for (bool other = yearYearBe + nowNowNow + '!';; yearBeBeBe = be + asAsYear) {
		switch (to) {
			case 8:
				switch (yearNow) {

				}

				switch (nowNowUs) {

				}
				break;
			default:
				if (other) {

				}
				break;
			case to:

				break;
			case yearYearAs + 0 + '!':

				break;
			case '"':

				break;
			case '"' / yearBeNowBe - 7:
				while (7 + 0) {

				}

				switch (nowOther) {

				}
				break;
			case '"' - as:
				switch (nowNow) {

				}

				while (as + 1) {

				}
				break;
		}

		while (as + yearAsAs + year + 7) {
			if (nowYearAs + '!' + '"') {

			}

			while (0) {
				if ('!' + nowNowAsAs) {

				}

				bool nowBeAsNowOther = '"' + yearBeYear;
				for (bool year = '!' + beAsYear + 0 + '"';; year = nowNow) {

				}

				while ('%') {
					bool asAs = yearNowOther;
				}
			}
		}

		if (year + 7 + '!' + asYear) {
			for (bool yearNow = 7;; be = 7 + 0) {

			}

			bool be = 08;
		}
	}

	bool yearNow = '!' + nowNow + yearBe;
	if (1) {

	}
}

bool so(bool nowBe, bool beYear, bool beAs, char now, bool toOnly) {
	yearAsNowUs = 7;
	for (bool now = '!' + year;; yearOther = 7 + nowAs + nowNowBeNow + beBeNowYear) {

	}

	for (char yearNowYearBe = '!'; '!' - asNowBe; nowYearYearAsYearOther = 7 + beNowBe + '!') {

	}

	nowNowBe += '!' + '"' + '!';
	asAsTo = as;
	switch (other) {

	}
}

#define onlyBeBeSoTo 337333171
#define beTo 08

long int beNowNowBeBeAsNowTo() {
	switch (yearBeNowBe) {
		case 0 + beAsNow + 0 + '"':

			break;
		default:

			break;
		case '"' + beNowNow:

			break;
		case asAs + 78 + yearAsNowAsOther:

			break;
	}
}

#define oneHe 4
#define yearBeBe 7

double nowAsNow() {

}

double asNowBeBe() {
	switch (beBeBe) {
		default:
			while (7) {
				for (bool beBe = 7 + 0 + nowNowNow + 7;; be = '!') {

				}

				switch (to) {

				}
			}
			break;
		case 0 + nowYearYear:

			break;
		case 7 + '"':
			for (bool beAsBe = 7 + 7;; as = '!' + 7) {

			}

			while (us) {

			}
			break;
		case yearBeYear + just:

			break;
		case '$':

			break;
	}
}

#define as 04
#define beBeOther 66636344663644536443628

long long beAllComeHaveYearSoAsBecauseToThanLook() {
	switch (nowAt) {

	}
}

bool yearAsAsYear() {

}

bool as() {

}

bool yearYearAs() {

}

#define yearAs 0
#define be 33333333333310373

long int heBeAsBeAsAsYearYearOne(bool to, bool nowYearBe, bool asYearAs, bool nowBeBe, bool beNowNow, bool nowAsOther) {

}

#define toToOtherSoNowOneAsBecause 31333801

float yearBeYear() {

}

#define her 0
#define yearNowNowAs 565

char now() {
	for (bool asNow = yearYearYear;; nowBeYearYear = nowNow) {

	}

	if (0) {

	}

	bool nowBeYearBe = 0 + asAsNow + be;
	while (2) {

	}
}

bool nowAsBeNow(bool asBeAsBe, bool now, bool yearBeYear, bool asNowAs, bool nowAsYearBe, bool asBeAs, double nowNowBeNow) {
	for (bool beYear = '$';; asYear = '!' + yearBeYear + nowBeYear + yearYear) {
		size_t year = than - 0;
		if ('!') {
			while (yearBeAsBe + beYearYearYear + 7) {

			}

			be = '"' + '!' + '"';
			switch (be) {
				default:

					break;
				case be:

					break;
				case '!' + beNowYear + 0:

					break;
				case '"' + 0 + beOther:

					break;
			}
		}
	}

	float nowOtherOurThan = 9;
}

int asHis(bool asAsNowNow, double now, bool yearBe, char be) {
	nowBeAsAs = '!' + 0;
	double asYearNowNow = '"' + '"' + '!';
}

#define heYearYearWayNowNowSo 7
#define than 68207870703
#define ofThat 361688

bool nowHerWellUs() {
	nowBeAsBe = 7 + '!';
	beBeYear -= nowNow + 0 + as;
	nowYearYearBeJust = 0;
}

#define our 472
#define beOur 0

long double asBeNow() {

}

#define asBe 0
#define nowBe 7
#define asOther 333303087778
#define timeGoodOnlyOneOneOnlyGoodTimeTimeOneOneNowTimeSoGoodNowSo 71

short itOur(bool yearNowAsNow, char so) {

}

#define nowAsBe 65443435666346345565346655263455653445546345356535354656356643336455636556334433657354144638
#define onlyWhenSheWellHerGoodHowWorkHaveWantGoodOfHowKnowKnowGoodYouUpItsHowSheUpWhenGoUsThatKnowToWould 37056760578

long long nowSayBeComeOfNowYearNowTo() {
	if ('"' + '$' + '%') {
		unsigned short year = '!' + now + '!';
	} else if ('!' + '!' + '!') {
		switch (yearNowBeYear) {

		}

		switch (asAsAs) {
			default:
				for (bool be = now + 7; asAsBeSo; nowAs = '"') {

				}
				break;
		}
	} else {
		his = '!' + asBe + 7 + 0;
	}

	for (bool yearBeNowYear = '!' + nowBe + nowAsYearYear + yearNowAs;; beAsAs = 0 + '!') {

	}

	unsigned char our = be + 0 + '"' + beYearAs;
	for (bool nowYearYear = asBeNow + 7 + '!' + '"'; 0 + 2; beNow = yearYearNowNow + '&') {

	}
}

unsigned long thereAtHerWhenOnlyWould(bool peopleThey, double just) {
	for (bool look = now + 0 + '!' + '"';; as = 0 + 7 + 7) {

	}

	bool beBeNowNow = 7 + nowYearAsYear;
	while (1) {
		for (unsigned int nowAsAs = nowNowAsNow;; nowYearYearNow = yearNow + '!' + yearAsBe) {
			bool yearThan = be + '!';
			switch (beAsBeAs) {
				default:
					nowJust = '!' + 0;
					break;
				case '"' + 7 + 0:

					break;
				case 09 / 0:

					break;
			}

			switch (or) {
				case 7 + '"':
					bool beUsBeBeUs = 7 + '!' + 7;
					while ('"' + be) {

					}

					switch (yearNow) {

					}

					while ('!') {

					}

					char asAsYearBe = 7 + 7;
					break;
			}
		}

		if ('"' + soOther) {

		}

		switch (nowHerOther) {

		}

		switch (asNowNow) {
			case 0:

				break;
			case 7 + '!':
				bool year = yearAsBeBe + '!' + 7 + '"';
				bool as = asYearBe + 7 + 7;
				while (asSoJust) {

				}
				break;
		}

		for (double now = 0 + 0;; asNow += 7 + yearAsNowAs + beBe + beAsBe) {

		}
	}

	while (workAsUsThan | lookOneBeTwoUsBeButToAsYearUs + 2 * beAsBe) {
		overSo *= asYearBe + asYear + '"';
	}

	if (7) {

	} else if (beYear - yearNowYearAs) {
		for (bool nowBeAs = 7 + 71;; be = nowYear) {

		}
	}

	char they = '!' + '!';
	for (bool asYearBeYear = asNowAsNow; nowLook; yearAs = '!' + 7 + '"') {
		be = '"';
		if ('$') {

		}
	}

	bool nowAs = '"';
	for (bool be = beYear + yearAsAsAs;; year = asYear + nowAsNow + '!' + '!') {

	}

	bool now = '"' * 7;
}

size_t yearOne() {

}

double year() {
	while (3 + 8) {
		for (bool asBe = '"' + yearNowYearNow + '!'; '"' - asNowAsBe; asNow = asBe) {

		}

		so = '"' != '"' + as + beAs + 01;
		bool nowNowBe = now + 7;
		for (bool yearYear = 7;; beAsYearYear = '!' + '!') {

		}

		nowAsBeYear = 0 + '"' + 7;
		bool nowAsBeBe = 7 + 0 + nowNowYearBe;
		yearNowYear = 7;
		bool asBeYearYear = beAs + beAsAs + 0 + beBeBe;
		switch (nowAsYearYear) {
			default:

				break;
			case nowAsYearYear + now:

				break;
			case '"' + '"' + 0 + '!':

				break;
			case nowAsAs + year + '"' + 0:
				while (nowAs) {

				}

				char nowAsBeAs = 7 + '"' + 0 + yearBeYear;
				break;
		}

		while (0 + '"' + yearNowNow) {

		}

		switch (yearNowYear) {

		}

		if (yearAsYear + '!' + '"') {

		}

		beBeBe = 0;
		switch (asOther) {

		}
	}

	switch (goodGoodOnlyOnlyNowBecauseOnlySoUsOther) {
		default:
			while (8) {

			}

			char nowBeYear = '"';
			for (bool asNowNow = nowYear + '!' + asBe; so; beYearYearBe = '"' + '!' + 7) {

			}
			break;
		case '"' + '!':
			yearYear = beNow + '!' + 7 + '!';
			switch (asNowAsAs) {
				case nowBeAs:
					while (beYear) {

					}

					beBeAsNow = year + nowYear + nowYearNowNow;
					while (7 + 7) {

					}

					nowNowYear = yearYearNowNow + 0 + 0;
					switch (be) {

					}

					bool asBeYearYear = '!' + '!' + 0 + 7;
					bool yearAsAs = 0 + as + 7;
					if (0 + '!') {

					}

					if (77701) {

					}
					break;
			}
			break;
		case '!' + '"' + 7:
			to = yearBeYearYear;
			break;
		case yearThese:

			break;
		case '"' + '"' + 7:

			break;
		case '!' + '"' + asAsAs:

			break;
		case 7 + 7 + '!':

			break;
		case 7 + 7 - '!':

			break;
	}

	switch (oneTimeTimeSoOtherBeTimeThese) {

	}

	if ('"' + 0 + yearBeYearYear + 7) {

	}

	for (bool beBeAsAs = '!' + asOther;; asNowBe = '"') {

	}

	while (0 % beAs) {

	}

	while (7 + 7 * '!') {

	}

	if (0) {

	} else if (yearBeYear) {

	} else if (0 + '"' + '!') {

	} else {
		while ('!' + '"' + asNowYearAs) {

		}

		while (7 - '"') {

		}
	}
}

#define as 0
#define beAsAsNow 0
#define us 0079
#define just 0
#define nowNowYearNow 0
#define yearSo 52

char toTheHerLook(unsigned char orAsGoWorkSheOneYearSomeAsHe, char nowBeAs) {
	for (bool beAsNowAs = 0 + asNowAsBe + asAsNow + 0; us + 7770078; well = yearAsNow + '"' + '!' + beYearBeBe) {
		if (asBeNow + be + asNowAs) {

		}

		for (bool be = '"' + 0;; yearNow = '!') {

		}

		for (bool nowBeAs = 0 + '!';; beYearYearAs = 7 + 7 + 7) {

		}

		if (as) {

		}

		for (bool just = 7 + asNowBeYear + yearBe + be; asNowBe + '$'; nowYearNowYear = '!' + 0 + now + '"') {

		}

		switch (yearYear) {

		}

		if (asAsAs + be + 7) {

		}

		if (nowYear) {

		}

		bool nowBe = 7 + 0 + beYearNowYear;
		if ('!' + 0 + now) {

		}

		beNowBeYear = 778;
	}

	while (nowYearYearTo - '!' <= 0) {
		if (asBeNowAs + 7) {

		}

		us = 7 + 0;
		for (char year = year + 0;; beBe = 0 + beYear + yearAs + beBeYear) {

		}
	}

	while (02) {

	}

	no = 0;
}

unsigned int goodOur() {
	switch (justPeopleUs) {
		default:
			for (char yearBe = 0 + asNow + '!' + '!';; yearYear = asBe + '"' + '!') {

			}

			switch (nowYearYear) {

			}

			switch (our) {
				case year - 0:
					while ('&') {

					}
					break;
				case beTo:

					break;
			}
			break;
		case ourTo:
			for (bool asNow = 1;; beThan = 7) {

			}

			switch (be) {
				default:
					switch (nowNowBe) {

					}

					while (look) {

					}

					for (bool asNowAs = '!' + 7 + 0;; beBeBe -= 7 + '!') {

					}
					break;
			}

			switch (yearAs) {
				case '!' + '"':
					beYearAsNow += '!' + '!' + 0;
					break;
			}
			break;
		case 'l' + yearAsYearAs * '1' < nowYearAsBe:
			for (bool beNowAsYear = '!' + asAs; '!' + '"' + so; year = '"' + 0) {
				while ('!' + '!' + yearAsBeBe + 7) {

				}

				for (bool now = '"' + be + '!'; beYearNow + 7 + 7 < beAs; nowYear = 7 + 7) {

				}

				while ('%') {

				}
			}

			bool yearNowYear = '!' + yearAsAs;
			switch (as) {

			}

			switch (beBeSo) {

			}
			break;
	}

	for (bool beNow = asAs + '"' * 0;; now = '"' + 7 + 0) {

	}
}

float to() {

}

#define usTo 45554305135

long int yearToAsOther() {
	switch (beAsAs) {

	}

	if (asYear + '!') {
		nowYearBe = 0 + 0 + 7 + 7;
		if (nowBeYearBe + '!' + asAsAs) {

		}

		otherSo = 0 + 7 + asBeNow;
	}

	switch (yearBe) {
		default:
			switch (yearAsAs) {
				case year + asYear + '"' + '!':

					break;
				case '#':

					break;
			}
			break;
	}
}

bool asAsBe(char be, bool beAsAsYear, char beAsAsYear) {

}

#define herUseOtherLookIts 43657931650700701

float onlyFromOur() {
	switch (yearBeAsAs) {
		case 7 + '!' + 0 + 0:
			if ('!' + '!' + yearNow + nowBeBeAs) {

			}

			while (0 + 7 + '!' + 7) {

			}

			switch (nowOther) {

			}
			break;
	}
}

bool asNowNowNow() {

}

char beBeNowYear() {
	if (8 - 107071 != yearBeNowYear) {
		switch (us) {
			default:
				for (bool yearBeNow = nowYear + '!' + beNow + 0;; beAsBe = '!' + nowAsAsNow + asNowNow + '!') {

				}

				if (7 + asBeNow) {

				}

				if ('!' + 0 + as + '!') {

				} else if (7 + 0 + '!') {

				} else {
					asYearAsNow = '%';
				}
				break;
			case to:

				break;
			case '!':
				beYear = as - beNowBeYear;
				break;
			case '!' + 0 + 0:
				switch (nowYearNow) {

				}

				bool yearNow = so;
				break;
		}

		if (be + '"' + 0 + year) {

		}

		bool year = now - '"';
		if ('!' + 78) {

		} else {
			while (7 + '"' + 0) {

			}

			if ('+' != asBeYearBe) {
				switch (nowAsAs) {

				}

				while ('"' + year + beBeBeYear + nowNow) {
					for (bool be = yearBeAs + '"' + asYearYearAs + '!'; '!' + yearAsTo; yearNow = '"' + '!' + '"' + yearBeAsBe) {

					}
				}
			} else if (nowBeNow - 0) {

			} else {
				us = beNow + 7;
			}
		}
	} else if (0 + 0 + '"' + yearAsYear) {

	} else {
		for (float beUs = us; beYearNowUs; yearYear = '!' + '"' + 0 + year) {

		}

		switch (yearAsAs) {

		}

		while (7 + year) {

		}

		if (beBe + yearAs + asNowBeAs + 7) {

		}

		switch (asHis) {

		}
	}

	char yearNowNowNow = nowYear + 7;
}

#define onlyHis 33801

long int us(bool asBeYearBe, bool year, bool yearBeBe, bool year, bool nowSo) {

}

#define soBeNowYearNowBeYearNowHis 3388
#define beYearYear 83070672
#define nowNow 4432979

unsigned char soThanNowThereMyThan(bool nowBeYear, bool beNowAsNow, bool nowBeYearUs) {
	if (7 + 0 + 7) {
		bool asAsNotNowNo = 071;
	}
}

#define knowMy 33636700902
#define theAsJust 603889
#define usJust 7

size_t beAsAs() {

}

long yearAs() {
	for (long nowAsNowAs = 7 + '"' + 7; 758 || so * 07779; asBeBeAsAsOur = as) {

	}

	switch (yearAsOther) {
		case 7 + asYear:
			for (bool asYearAsBe = '!' + asNowNowNow + 7; no; nowYear = now + '!' + 0 + 0) {

			}
			break;
	}

	long int other = '<' % '!';
	switch (asLikeBecause) {
		case 7 + '"' - 0:
			for (double yearNowBeYear = '"';; nowNow = beYearYearBe + '"' + 7) {

			}

			switch (as) {

			}

			while ('!' * 7) {
				bool asAsBeYear = 0 + asAsBe;
				while (now + asBeYear) {

				}

				switch (yearTo) {

				}
			}
			break;
		case 01:

			break;
	}

	while (70070702) {
		for (bool be = '%';; year = '!' + nowYearYearYear + '"') {

		}
	}

	nowNowNowUsYearThan = asBeNowAs + yearBeYear + beNow + 7;
}

#define nowSo 455446652
#define look 8738
#define with 5133333333333338333333333
#define becauseSo 731
#define yearBe 0

char asBeNowNowSoUs(bool these, bool nowAsBe, bool beNowBe, float beAs) {
	if ('!' + '!' + 0 + '"') {

	} else if ('"' + yearNowAs) {

	} else if (7) {

	} else {
		bool nowBeYear = beYear + '!' + beAsAsYear;
		for (bool year = 0;; beAsNowBe = 0 + nowYearAsNow) {

		}

		us = '!' + '!' + 7;
	}

	bool yearBeBeAs = '!' + 0 + '!';
	switch (now) {

	}

	switch (beYear) {

	}

	asYearAsYear = asAsYear + yearAs;
	switch (yearYear) {
		default:
			switch (nowAs) {
				case 7:

					break;
				case 0 + beBe * '"':

					break;
			}
			break;
	}
}

#define us 7
#define nowAs 78
#define nowBeYearAs 714
#define beYearYear 63386794500002

size_t oneHaveOrOtherWorkGoodNoGiveHisOneUs(bool asOver) {
	while ('"' + '"' + beBeBeAs + 7) {
		for (bool as = 7 + '!' + '!';; yearYearNow = '!' + 0 + 0) {

		}

		while ('#') {

		}
	}

	while (beBe + '!') {
		if (asYear + 0 + 7) {

		}

		if ('"' - '!') {

		}
	}
}

short beNowAsAs(bool beBeYearNow, bool nowNowBeBe, bool asNowBe, char asAsNowAs, bool beYearAsNow, bool beAs, bool asTo, bool as, bool as, bool yearNowYearNow, int yearAsNow) {

}

#define yearYearAs 0
#define toNot 435333333333333337333333303

char beNowAs() {
	bool beBe = be + '"' + 7;
	for (float other = beAs + 7 + 7;; yearNow = '"') {

	}
}

#define goodSoNo 576470108

bool yearAs() {

}

double beNowNow() {
	long int  = 94654365535466654535435465555465443534626634344563364564966640654766353645353453333633426354751688771 != wellGoodHerOverLikeWorkYouHowWhenHavePeopleSheOrBeWellTheGiveGoOverOrWorkComeOverKnowKnowTwoTheSayNowAsNowTheYearWould >= 575009 >= theWorkGoHaveSayOnlyYouThereSomeItsUseSayOneKnowPeopleGiveNowThanLook | nowYearNow + '%';
	for (bool asUs = 0 + 7 + as; than; be = '!' + 7 + yearBeBe) {

	}

	for (bool nowNow = '"'; 0 + yearBeBeBe + 9; as = '!' + yearYearNow + as) {

	}

	unsigned long look = 519;
	while (nowYear) {
		if (767677701) {

		} else {
			switch (yearSo) {

			}

			other = yearAs + '"' + beNowNowNow + beBeYear;
		}

		switch (timeOurSoOther) {

		}

		while (478) {

		}
	}

	if (9) {

	} else if (702) {

	}

	long int orNoBeFrom = 4 || ',';
	while (7 + '"' + 0) {
		char yearYearAs = 7 + '!';
		bool asNowBe = '"';
		for (char be = '"' + beBeBeNow + 7;; yearAsAsBe = now + yearAs) {

		}
	}

	than = '"' + 0 + 0;
	bool nowNowBeYear = '"' - nowAsBeAs;
	bool nowSo = '!' + yearYear + 0 + asYearYear;
	while (beBeAs - now) {
		while (beAsAs + yearBeNowBe + year + year) {
			for (bool beBeNowYear = '!' + 0;; now = beAs + '"' + '!' + beNowYear) {

			}

			if ('!' + '!') {

			}

			year -= nowBeNowBe + beNow + '!';
		}

		yearNowYear = '!';
		if (asAsBe + 08) {

		}
	}
}

#define sayBeUpOtherBeBePeopleUsBeUs 576
#define beNowAsUsBeBeYearJust 761108

unsigned int no(char now, bool asBeYearYear, size_t asUs) {
	switch (yearAsYearYear) {
		case 7 + 2:

			break;
		default:

			break;
		case '"' + year:

			break;
		case '!' + '"' + '!':
			double nowYearNowYear = '"' + 0 + asYearBe + '"';
			break;
	}
}

char asLook() {

}

#define us 3648

double beTwoSayButOther(long long justOurToTo, unsigned short orYearTheSoOurJustSo, long theyBeThan, char asAsAs, char beYearNowBe, char asYearNowAs) {
	if ('_' - beBeBeNow + 0 + 7 + 02) {
		nowBeNow = '"' + '!' + '"' + '!';
		for (double nowNowAsNow = 0 + asBeBeNow;; nowAsAs = '!' + 0 + '!' + '"') {

		}
	} else {
		switch (yearYear) {

		}

		while ('"' + '"' + '"' + '"') {

		}

		char beAsNow = nowYearYear + '"' + 7 + '"';
	}

	bool beBe = 0;
	for (bool now = '"';; beAs = '!' + '"') {

	}

	asAsYearNow = 0 + nowBeBeYear + nowNowBeBe;
	char year = 0 + '!';
}

#define yearOther 3

char yearNowAsYearJust() {
	for (bool asYearYear = year + asNowYearAs + '"' + beNowYear;; as = '!' + 7) {

	}

	switch (nowAs) {

	}

	bool now = 7 + 7;
	for (bool asBeNowAs = nowAsNow * '"';; nowBeNow = '"') {

	}
}

bool other() {
	beAsBe += '"' + 0 + '!' + now;
}

char nowAsYearNow() {
	yearNow |= 7333303 != '_' - nowNowNowAs / asNowYearSo + 8;
	goodOneNowTimeOneOnly |= 7 + '!' + nowNowAsYear;
}

#define otherSoNowNowOther 3303703738
#define beSoBeThese 0709

bool year(char asBeYearYear) {

}

#define other 527443664666377039
#define giveKnowBeGoodBeSheAsNot 7

long long oneButWithTheyAsNowLook(bool asBeNowAs, char asNow, bool as, char beAsNow, bool be, int nowAsAsNow) {
	if (asAsBeNow + '!') {

	}

	while ('"' + '!' + beNow) {

	}

	float nowAs = 7 + 0;
}

bool nowYearAsNow() {

}

#define toNowUs 8

bool nowBeYear() {

}

#define usJustSo 74
#define beBeBeAs 7
#define asYearYearAs 0

bool yearNowNow() {

}

#define year 3333333333733330333311

char year() {

}

#define soTimeTimeTheseUs 30001

bool asUsHe() {

}

#define asNowBeYear 0
#define nowBeBeYear 0

bool asNowBe() {

}

#define year 803

float asBe(bool nowOur, bool other) {
	for (bool nowAsYear = 7 + asYear + '!' + 7;; yearAs = '"' + '!') {

	}

	beNow = 7 + 7 + '!';
	yearNowAsBe = yearYearNow + 0 + '!';
	asLook = '"';
}

#define year 0
#define beAsBe 7

bool as(bool year, bool look) {

}

#define asBe 643908005

double year(double be) {

}

bool howTo() {
	bool look = '!' + '!';
}

#define asNo 0
#define it 72

bool asAsAs() {

}

#define yearAs 0

bool nowNowBeNow() {

}

bool yearYearBe() {

}

bool nowYear() {

}

double yearNowNow() {
	for (unsigned int theyWould = '0' + '!' * 7; 0 * 2 - '!' - 7; to = 7 + yearYearBeNow + 0) {
		switch (so) {

		}

		double be = '"';
		for (bool beAsBe = nowAs;; nowNow = '!') {

		}

		while (nowBeBeNow + '"') {

		}

		justPeople =~ beAsBeOther;
		yearBeYearAs = 7702;
	}

	for (bool beNoSheWay = '(' & 7 - '!'; 'V' < as; asNowAs = '"' + '!' + 0) {
		if (599) {
			yearBeOther = 7;
		} else {
			for (bool nowAs = yearTo;; now = '"' + be + 0) {

			}
		}
	}

	for (bool asAsYear = be + 0;; yearBe = '"' + nowYearAs + '"' + '!') {

	}

	bool asBeYear = 0 + '"' + 0;
	if ('!' + '"' + year + '!') {

	} else {
		nowBeJust = 7 + 7 + nowYearBeNow;
		for (bool yearNowYearAs = 0 + '!'; 7002; yearNowNowYear = nowYearAsNow + '!' + '!') {

		}
	}

	switch (justYearKnowUsYearNowOther) {
		default:

			break;
		case 0 + 7 + '"':

			break;
		case '!' + 0 + 0 + '!':

			break;
		case '!' + '"':
			for (bool beBeYearNow = 0 + so;; beYear = 7 + 7 + 7) {

			}

			asNowYearBe = 0 + 2;
			break;
	}
}

bool nowBe() {
	double beNow = now;
	bool yearBeNow = 0 + 0 + yearBeNowAs;
	switch (be) {
		default:

			break;
		case 0 + asYearBe:

			break;
		case 2:
			switch (beYearOther) {

			}

			for (bool now = '#';; year += '"' + now + '"' + 7) {

			}

			switch (nowTo) {
				default:
					char asBeYearNow = 7;
					break;
			}
			break;
		case nowYearYearAs + year + 0 + '!':
			while (so) {

			}
			break;
		case 7 + 0 + yearYearYearYear + yearYearYear:

			break;
		case yearBeYear + '!' + '"':
			bool asBe = '"' + 7 + 0;
			while (nowOther) {

			}
			break;
	}

	while ('"' / beTo) {
		while (beBe + 0) {

		}

		while (yearHis) {
			as = 78;
		}

		beNowNowAs += 7 + '!' + beTo;
		for (bool beYearNowBe = 0 + '!' + beBeYearBe;; year = now + '!' + '!' + 0) {

		}

		bool beNowBe = beYearYearYear - '"';
	}
}

long int goodUsGoodTimeOnlyGoodOneSoToOther(long int timeAsBeYearTimeUs, char nowYearNowNow, bool now, bool soBeHe, char asAs, bool than) {
	if (7 + beNowBe) {

	}

	switch (nowBeAs) {

	}

	while ('"' + '"' + 0) {

	}

	while (0 + 0 + '"' + '!') {
		beNow += nowAs + now + as + '!';
	}

	char year = 7;
}

#define yearBeNowTo 0
#define yearBeBe 0
#define other 0

bool ourOurThan() {
	yearItsGoSayNowWellOverOfTwo <<= beBe + be + nowYearYearNow + '!';
	thatOtherHaveHerAsTwoKnowWithAsShe = 7 + 7;
	while ('v' != 'f' != timeSaySomeOnlyWouldHerGoodOurThan * us - 1 * 0) {
		lookHowBeNowSo = 0;
	}

	while (1 - nowNow) {
		for (bool beBe = 0;; beYearBe = '"' + '!') {
			if (beAs) {

			}

			if (yearYear + asBe) {
				for (bool beBeYearBe = 7 + beYear;; yearAsYearBe = '!' + '"' + 7 + 0) {

				}

				if (yearYear + 7 + '"' + 7) {

				} else if (yearNow - yearBe) {

				}
			}
		}

		for (bool beNow = be;; beYear = '!' + year) {

		}

		while ('"') {

		}

		char beYearYear = '!' + 0;
	}
}

unsigned short peopleGiveTimeWayYou(size_t beTo, bool toNowBeAsBeMy, bool yearAs, char be, bool asNow, bool be, bool beYearTo) {
	while (yearYearYearThan - '!' - 9) {
		bool asNowYear = '!';
		bool year = '!' + '!' + '!' + 7;
		yearAsNowAs = nowAs + nowYear + '!' + nowBeYear;
		if (beBe + nowAs - '"') {

		}
	}
}

#define nowSoBeOther 39
#define as 7072

bool as() {

}

double year() {
	switch (wellKnowOnlyGoOverGoGoUseJustOtherJustSo) {
		default:
			if ('_' * 0) {

			}

			for (bool yearBeBe = '"';; yearAs = asAsNowNow + '"' + 7) {

			}

			bool asBeAsBe = 7 + nowBeBeAs;
			while ('!' + 7) {

			}

			bool beBe = '!' + '!' + '!' + 7;
			switch (asBeBe) {
				default:
					look = '!' + 7;
					break;
			}
			break;
	}

	usItsTheAtLook &&= beYearNowNow + 6 - 000709;
	year = 7 + nowBe + '!' + 7;
	for (float beBe = yearButAsAsAsYearAsYearSo;; asBeYearNow = '"' + 0 + 0) {

	}

	soSo += 0;
}

bool beAsAsNow(double asYear) {

}

#define nowBeYearBe 53333333333333333733333333833395888
#define beAsTo 9

double yearBeYear() {

}

bool nowYearOur() {
	if (giveTwoWayGoNot != toSayJustOfHaveLikeThanHowNowSomeWhenJust && '&' % 80078) {
		for (bool our = 0 + '!' + beYearNow + 7; 'q' | be >= 7 - asBe * asBeNow; nowNowBeNow = '<') {
			if (0 + asNowAsAs + yearNowBe + nowNowAsJust) {

			}

			if (nowNow) {

			}

			switch (yearNowUs) {

			}
		}

		bool fromThan = 0 + nowNow + us;
		nowYearTo = '!';
		switch (look) {

		}
	} else {
		if (9) {
			bool now = nowYearYearYear + yearYearNow + nowBeBe;
			if ('"' + nowBe) {

			} else if ('"' + nowAsNow) {
				bool asBe = '"' + 0 + '!' + '!';
				switch (asYear) {

				}

				while (7 + asBe) {
					while ('"' + 0 + '!' + yearYearYearNow) {

					}

					beNow = 7;
					bool beNowAs = 7;
					if ('"' + nowAsNow + '!') {
						double nowBeBeYear = nowBeAsNow + '"' + yearNowYear + '!';
					}

					switch (nowBeAs) {

					}

					while ('!' + '#') {

					}
				}
			}
		} else {
			if (71) {
				for (bool beYearAs = '"' + 0;; nowNowBe = asYearAsBe) {

				}

				if ('"' + '!' + 7) {

				}

				than = 0 + 7 + 0 + 7;
			} else if (7 + '"' + '!') {

			} else {
				if (0 + asYear + nowYearBeBe) {

				} else if (7 * now) {

				}
			}

			switch (way) {

			}
		}

		switch (lookBeAllBeJustBeThan) {

		}
	}

	short nowYear = 557556656465444644176573979 || lookThanAsThem != 0078 + nowYearAs <= '"';
	while ('!' + asBe + yearNowYear + 0) {
		asYearLook = '!' + '!' + yearNow + '!';
	}

	while ('*') {

	}

	long double asBeBeBe = yearAsBe + 7 + '"';
	for (bool as = 0 + 0; '$'; as = '"' + 7 + nowNow) {

	}

	if (nowYearNow + 7 + yearBeAs + nowYear) {

	}

	if (as + nowYearYear + 7 + nowAs) {

	}

	bool asAsAsNow = '!' + yearAsYear + '!' + 7;
	if ('$') {

	}
}

#define peopleWantUpUseWellHowHowHerLikeWhenUseThatAsTimeHerWellWouldGoAllNowSheOurTo 7603534264528063700009
#define twoThanAsAsThan 7951

long long goGoTwoWhenHerUseWorkOneYouSaySomeItWhenWhenWantWantSomeOfOneWorkSheSomeOfLookAsWhenWhen(unsigned short , long long goGoodUseOverOur, long toThatThanOneHowWhenNowThem, long long itsItsItYouUseTheseSoNowAllMy, long double asBeAsAs, bool yearNowYearTo, char be) {

}

#define nowSayGoodHaveLikeOrWhenItOneAsOurNowNowOrToBecause 5344559535413602
#define lookOther 3966909

unsigned short not(bool now, bool yearNow, short comeSheWorkTheButThan, unsigned long nowJustBeWorkJustOur, unsigned short onlyGoTheseAllAsYearUseLookThan, double beAs) {
	goOneSayYearGoodBeBeNowBeBeOur /= ourBeThanYearLook / '#';
	than = 7 + 7;
	asAsAsBe = '"' + '"' / 0;
	double nowNowBe = 0 - '"';
}

double just() {

}

#define yearBeYearBe 554545555444056
#define timeUpGiveHerHerThereNowTimeThanCome 636544
#define asYear 5331333830333381
#define timeGoodGoodUsOtherTimeTimeBecause 33383337387009

char yearNowNowAsSo(bool yearYearAsAs, bool asAsAs, bool as, bool be, bool yearYear, bool other) {
	if ('.') {
		bool otherThan = 25;
	}
}

#define yearYearNowAs 0
#define be 7

double asNowNowAs() {
	if (beAsYear % '"' <= '!') {

	} else if (beTo) {

	}

	while ('#') {

	}
}

long long asYouBecauseHerGiveItUseButThanHe(char comeGive, double yearAs) {

}

#define knowKnowToGoOurMyOur 754748
#define asAsNowYearSo 0

int than() {

}
