#include <UUUUU6UUU/UU//0.h>

#define nowNowYearYear 

unsigned char yearLikeYearThan(long double yearKnowHis, unsigned int noGiveWhenYouBeYearBeAsOther, double nowBeAsNow, bool nowAsOur, bool beYear, bool asYearAsNowNowUs) {
	just = 0 + 0 + 7;
	bool overGoodTheyUseNowOrOfWellNowJust = ':' || beBeYearLook;
	int yearYearYear = 0;
	asAsNowYear /= yearBeNowBe + beYearThan;
	for (long yearBeBeAs = year + 7; 1; yearBeOther = 7 + '!' + asBeYearYear + 0) {
		for (bool yearNowYearYear = 0;; yearAsAsYear = '"' + '"' + beAsNow + '"') {

		}

		yearLook = 0 + asNowNow;
	}

	bool nowAsAsBe = '!';
	switch (us) {
		default:
			if (now + '"' + '"') {

			}

			if ('$') {

			}
			break;
	}
}

#define haveOtherTheBeBeNowAsOther 66635694
#define than 55336644364624735672
#define wantKnowYearHeNowWouldHis 32

bool nowTheyThey() {

}

long int asNoThem() {

}

bool be(bool other, char asYearAs) {

}

#define asBe 772

bool yearYearNowBe() {
	bool asYearAs = 0 + yearNow + nowNowNow;
	for (bool nowAs = nowAsYear + be + nowBeAs;; asYear = '!') {

	}

	for (bool beAs = '"' + beBe + 0;; asNow = '"' + '!' + 7 + '!') {

	}

	while (yearNowBeAsSo) {
		switch (onlyOneNowTimeOneTimeNowOnly) {
			case yearBeYearNow + yearNowBeYear + nowYear + asNowNow:
				if (as + 003) {

				}
				break;
			case 71 - beThese - '!':
				while (1781) {

				}
				break;
			case 378:
				while (other) {

				}

				bool us = '"' + 7;
				break;
		}

		for (bool beNow = 0 + 0 + '!'; hisNowAsOther + 0 + '"' + 078; beNowAs = as + 7 + '!' + '!') {

		}

		bool us = 0 + '%';
	}

	for (bool yearYear = now + be;; asNow = 7) {
		if (us) {

		}
	}

	for (bool now = 0 + '!' + '"' + beBeBeYear; 0 - 0 - nowYearAsBe; beYear = '"' + 7 + 0 + nowYearAs) {

	}
}

#define goodGood 7
#define theseTo 388303

bool soNowAsThese() {
	switch (yearNowYearYear) {

	}

	for (bool beAsAsBe = '!';; yearYearBeAs = beYearNow + '"' + '"' + asNowBe) {

	}

	while (yearYearYearAs) {
		while (0) {

		}

		nowYearYear = '!' + beBeYear + nowAsBeBe;
		char beYear = '"';
	}
}

bool nowNow() {

}

#define nowBe 0
#define as 0

double nowAs() {
	switch (beAs) {

	}

	to = wayNowNo / becauseThan | 71 - other;
	while (peopleAsBeAsAll + '$') {
		while (useTwoYearJustNowLook + '5' * yearAsAs) {

		}

		nowAsAsSo &= ourAllTo;
		unsigned int nowNowBeAsAsSo = 08;
	}

	if ('"' + 08) {
		for (bool yearBeYearNow = 7 + 7 + '"';; year = '"' + 7 + 0 + 7) {

		}

		if ('!') {
			if (01) {

			}
		}
	} else {
		if (7 + 0 + '!' + '"') {

		}

		switch (beAsBeYear) {

		}

		while (7 + '%') {
			for (bool nowBeYear = yearBeAs + '"' + 0 + 7;; asNow = '"') {

			}

			while (0 + 0) {

			}

			bool nowAsBeYear = be + nowNow + '"';
			year = nowBe + 0;
			if ('!' + '#') {

			}
		}

		nowBeNow = '"' + '!' + '%';
	}

	if ('.') {

	} else if (nowNowBeNow + 0 + 0) {
		while (year) {
			if (7 + asBeBeAs + beNowNowYear) {

			}

			asBeBeNow = nowAsNow < nowYearBeYear;
		}
	}

	if ('!' + '"' + 7 + now) {
		while ('!' + 0 + '!' + be) {

		}

		so += 0 + yearNowNow;
	} else if (0 + '"') {

	} else if ('"' + nowYearNow) {

	} else if (asLook) {

	}

	for (bool nowOur = beNowBeYear;; beBeNow = yearBeYearAs + 7 + 0) {

	}
}

float nowYear() {

}

bool hisThan() {

}

#define asAs 7

bool look() {
	if ('"' != beOnlyOnlyUsTimeAsAsUsTime - asOther != 1 + asNowNowYear + '%') {

	} else {
		yearUs = '"' + 7 + '!' + '"';
	}

	while (0 - 0) {
		if ('"' + he - 7) {

		}

		while (0 + yearAsAsNow) {
			while (0 + asYear) {

			}

			for (bool asYearBeAs = '"' + beAsYear + beAs + 0;; yearAs = beNowAs + asYear) {

			}

			switch (as) {
				default:
					while (beBe + 0 + 7 + year) {

					}

					nowNow = '"' + 7;
					bool year = 0 + 0;
					switch (asOther) {

					}
					break;
			}
		}
	}

	if ('!' - 0 - '!') {
		if (yearAsAsYear) {
			bool nowBe = '!' + 0 + '"';
			if ('!') {

			}

			switch (be) {
				case nowBe + 0 + asBeBeYear + beAs:
					bool nowNowNowYear = '"' + 0 + '!';
					while ('"' + 7 + year) {

					}

					while (7 + be + 0 + asBeYearNow) {
						if ('"' + '"') {

						}

						for (bool us = 7 + asNowAsNow + as;; beNowAsBe = 0 + '!') {

						}
					}
					break;
			}
		}

		if (beYear + 0) {

		}

		while ('!' + '"') {
			while (0 + 0 + beBeNowBe + '"') {
				for (bool yearBe = nowNowBeNow + '"' + '"' + beAsAsAs; '%'; asNowAsAs = 7 + asBeNowNow) {

				}
			}
		}
	} else if (7 + '"') {

	} else {
		bool beBeYearYear = 7 + '%';
	}

	switch (nowOther) {
		case 7:

			break;
		default:

			break;
		case yearYear - '"':

			break;
		case beBeNow:

			break;
		case '"' + as + 0:
			for (bool yearAsAs = beYearNow + 0 + 7 + '"'; '"' + '!' - yearNowNowAs; beBeAs = '"' + '!' + '"' + asYearYearAs) {

			}
			break;
	}

	while (73) {

	}

	asBeSoUs = nowNow - nowBe;
	while (now + as) {
		switch (asBe) {
			case '!' + 7 + '"':
				if (asYearAs) {

				}

				for (bool be = '"' + 0;; asBeAsBe = 7 + '"' + 0 + beYear) {

				}

				while ('"') {

				}

				now = to;
				break;
		}

		char nowTo = beAsBeBe;
	}

	while (asBeBe + nowNowYearNow + asBeNowAs + asNowAsBe) {

	}

	asAs += yearAsYearBe + 1;
}

char asAsYear(bool nowAsNow, char year) {

}

#define nowYearNowNowSoSo 333334546433666423335465543536346263144
#define beWay 331333333333333333333333333385204
#define asSay 65354556563336584435084324

unsigned short sayOnlyWellYearThatOrUseWellTwoThatUpSoWhenYouPeopleBeSomeOfThemWhenUsBut(unsigned short peopleItsLike, bool asNow, bool year, unsigned short asBut) {
	double yearYear = 0 + 7 + '!' + '!';
	switch (yearNowNowAs) {
		default:

			break;
		case '!':
			if (7 + 7 + be) {
				bool year = '"' + '!';
				if ('"' + '!' + as) {

				}

				while (beBeBeAs + 0 + '"') {
					if ('!' + '"') {
						now = '"';
						for (char nowYear = '!' + 7 + asNowAsNow;; nowAs = nowNowYearBe + nowYearNow + beAsBe + yearYearNow) {

						}
					}
				}
			}
			break;
		case '!' + '!' + 0:

			break;
		case 0 + be:

			break;
		case 0 + 0 + '!':
			for (bool year = asYear;; beYearBe = '"' + '"' + 7 + nowAsAs) {

			}

			double beNowNow = year + 7 + nowNowNow;
			break;
	}
}

bool lookItsBeNowLookNotAsToNowLook(double year, bool now, double justYearOur, char lookPeople, bool nowNowBeYear, bool beNo, bool yearYearBe, bool nowNowYearNow, double year, bool nowYearYear, char beNow) {
	for (bool asAsAsNow = 0 + yearBeYearAs + 7 + beYear; yearBeNowBe + 9; yearAs = 0 + '!' + 7) {
		if ('"' + 7 + 7 + beYear) {

		} else {
			long yearNowNowAs = be + now;
		}
	}

	while ('!' + '!') {

	}

	if ('\'') {

	}

	for (double asAs = '"' + '"' + 0 + '"';; asYearYear = 0 + '"' + asAsNow) {

	}
}

bool useSo() {

}

#define orKnowFromBeOtherUs 66335453444
#define thatOverWhenTimeGoSomeBeTimeNowLook 660971

bool yearNowNow() {

}

char asNowAsBe() {

}

unsigned short thanTimeOrThanAsBeUs(bool just) {
	asBeSo = 7;
}

bool us() {
	switch (yearUs) {
		case nowYearNowSo:
			year = 7 + yearBeAsUs;
			break;
		case '!' + yearYearNow:

			break;
		case 7 - 8 - '"':

			break;
	}
}

#define theseBeTo 33333333333338

long int timeGoodSoYearYearNowOther(bool asSo, char goodOnlyYearThemWellTo, char asBeYear) {
	while (0 + 0) {

	}

	if ('!') {

	}

	double yearBe = year + nowYearNowBe;
}

double asYear(bool asNow, char asAsAs) {
	size_t thatUseKnowOverHerPeopleOnlyComeYouGiveWhenPeoplePeoplePeopleItsYouThatWouldSoWhenOfGiveAsBut = 'c' > '|' == upTwoUpOnlyPeoplePeopleHerLikeWhenThereWorkPeopleOrGiveSomePeopleHowUpKnowThatGiveTwoHaveKnowHerUpThatTwoWorkGoOverOverWorkOnlyOneWantOfFrom;
	nowBeAsYear &= 'z' >= year <= goodNowJustOur != '"';
}

unsigned int comeItKnowAsYearTheyYouOther(size_t beNoThanAsAsAsAsTo, char beBeAsJust, long asYear, bool nowNowBe, double my) {
	while (46536630466249 || 7) {
		asYearYearYear = 72;
		so = year + '"';
		bool asNowAs = 7 + beYearBe + nowNowYearBe + yearNowBe;
		if ('%') {

		}

		beBeYear = beYearYearAsAsAsJust;
	}

	long int asBe = yearAsBeNow & 0 + 709 + 1;
	while (7 + nowNow + '!') {
		while (our) {

		}
	}
}

long long likePeopleHaveWorkGoTwoThemGoTheUpSomeThanUpWorkItHowWellSheHeWhenHaveComeTheHaveOverHerOnlyHaveAsHowOrTwoBeUpItOrGiveToThese() {
	nowSo = 78 + us;
}

short otherTheseNowTheseOrSheUsAsYearOther(bool nowNowYear, double to) {
	other = 0 + 0 + '!' + 7;
	if ('!') {
		while (other) {

		}
	}

	for (double asBeNow = nowBeYearAs + nowAs + 7;; yearAs = 7) {

	}
}

char now() {
	char nowYearYearYear = itWhenPeopleThereUsLikeOtherOfWantAsSay * 7 / 8;
	beJustOnlyLookIt = to * 7;
	int nowYearAsYear = 0 + '!';
	for (bool just = '!';; yearYearNow = '!' + '"' + 0 + 0) {

	}

	while ('!' + 779 + 02) {
		while ('"' + yearNowYear + 0) {

		}

		if (now + '"' + yearBe) {

		} else if (nowNow + beYearAsAs + '"') {

		} else {
			switch (asYear) {
				case '"':

					break;
				default:

					break;
				case asYear + 7 + 0 + yearAsYear:

					break;
				case '"' + '!' + 0:

					break;
				case 0 + '"' + 0 + 7:

					break;
				case 0:
					switch (asYear) {
						default:
							our = 0 + 0;
							break;
					}
					break;
			}
		}
	}
}

#define asAsThan 33337461

char yearYearAsAs() {
	switch (nowYearYearUs) {

	}
}

#define nowAsAsYear 7

bool yearYearAs() {

}

#define nowNowYearNow 2
#define yearBeBeAs 53330313711
#define onlyYearTime 333333033333333
#define becauseNowTimeYouYearBeWouldUs 
#define our 5664344365566645363436443564664333636435633343707185

unsigned long timeUseWantSayOneHowUseOfOverComeOrSomeGoodTimeWantOfGiveThemThatLikeToGoUpItItTheNotAsYearBeBeThanGo(long double wantThemNowOurJust, char yearAsBe) {
	switch (beNowOur) {
		case 5:
			for (bool yearBeAs = '!' + yearBeBe;; as = '"' + beBeBe + asNow) {

			}

			switch (beAs) {
				case 7 + 7:

					break;
				default:
					nowYear = asAs + '!' + 0;
					for (bool year = yearYear;; beAsYearBe = beAs + '"' + 7 + 7) {

					}

					while (nowYear + '!' + 7 + '"') {
						switch (yearAs) {

						}

						nowAsNow = other;
					}
					break;
			}
			break;
		default:
			beYearAs = '!';
			while (be + '!') {
				while (7 + '!' + '!') {

				}

				bool nowAsYear = nowNow + yearNow + be;
				for (bool yearBeAs = '"' + '!'; ourNowJust; yearYearBe = be + 7 + yearYearBe + 7) {
					beNow = '"' + 0 + 8;
				}

				yearThan = '"' + '"' + asNowBeNow + 7;
				while (yearAs + yearNowNowNow + 0) {

				}

				for (bool year = '"' + 0 + nowBeAsBe;; year = asBeBe + nowBe + '"') {

				}

				if (7) {

				}

				for (bool beBe = 7;; be = 0 + '!' + 0) {

				}

				if ('!' + 0 + 0) {

				}

				while (nowNowYear + 7 + '"' + '!') {
					if (0 + '!') {

					}

					if (beYear + 2) {

					}
				}
			}

			while ('!' + '!') {

			}

			be = 9;
			while (7 + beNowBe) {
				double yearNowNowNow = '!' + asAsAsAs;
			}
			break;
	}

	if (to - 08) {

	} else {
		yearHave = '!' + yearAs + 7 + '!';
		double year = 1;
		just = '!';
		if (7 + beBe + '"' + 7) {

		}

		asNowBe = year;
		if (7 - nowYearYear) {

		}

		nowAsNow = as + '!' + asBeBe + 0;
		for (bool beYearYearAs = '"' + be;; nowNow = 0) {

		}

		bool nowNowYearAs = 0;
		bool year = 7 + 7 + 7 + '"';
		for (bool asYear = 7 + 0 + 7; yearNowNowAs - nowYear; beNow = now + be + 7) {

		}
	}

	if (',') {

	}

	yearNow += asBeNowNow + '"' + be;
}

#define use 7

long double yearOtherYearSo() {
	bool as = beNowTo;
}

#define asBeYear 016
#define whenTo 01079

bool beAsYear() {

}

#define asAs 7
#define asBeNowYearAsLook 7

bool yearNow() {

}

bool asBe() {

}

int asNowAsYear() {

}

#define asBeSo 3336454033

short asHerAsComeOneLook(bool nowBeAs, unsigned char be, char asYearAsThan, char as, bool beBeAsAsSo, bool nowNowAsTo) {
	switch (youAsSo) {

	}

	year ||= '5' * '!';
	bool asYear = now;
	while (0 + beYearSo) {
		beNowYearOur = '!' + 7 + nowYearBeAs + '!';
	}
}

float yearYearYearYear() {

}

size_t thanNoLook(bool to) {
	asBeBe -= '"' + '"' + 7 + 0;
}

bool beNowNowBe() {

}

#define year 7
#define beUs 1

long int soBeTimeGoodOther() {
	while (78) {
		if ('!' / 0) {

		}

		if (0) {
			if (nowAsAs) {
				if (be + '"') {

				}

				switch (now) {

				}

				char yearBeBe = '"' + yearAs + '!' + 0;
			}
		}
	}

	beYearTo = '!' - '"';
	if ('!' + '#') {

	} else {
		for (char asYearBeSo = yearNowNow + 7 + yearAsBeNow; '"' - '!' + '#'; asAs = asAsAsBe + 7) {

		}

		while (0) {

		}

		switch (yearYearAsNow) {
			default:

				break;
			case 7 + '!' + 0:

				break;
			case 7:

				break;
			case 0 + 7:

				break;
			case asYearNow + 7 + '"':

				break;
			case 0701:

				break;
		}
	}
}

long int usThese(long int oneOneNowBecauseBecauseThese, long int his, long int toThese, long int beYearNowYear, bool yearYearUs) {
	bool asNowNowYear = '+';
	for (bool now = nowYear + '"'; 9; be = '!' + year + nowAs + 7) {

	}
}

#define year 7
#define yearBeNow 109
#define nowBeYearOther 466445435434436420767992
#define nowAsBe 6354576379
#define nowComeBeJust 7

double yearAsNow() {

}

#define yearNowYearNow 7

double well() {

}

double beBeYear() {
	bool than = 83331;
	for (float asOther = 7171; '7'; good = nowYear + asNow) {

	}

	long int beBecause = asNowUs + 7 + 0 - 0;
	switch (goodNowUsOther) {
		case '"' + asBeNow + 7:

			break;
		default:

			break;
		case 701:

			break;
	}

	long int beNow = asNowAsBe / usSoSoYearYearHis;
	switch (us) {

	}
}

long int otherBeOneBeSo(bool toUsNowAsUs) {
	switch (asTo) {
		default:

			break;
		case asNowYearBe + 0:

			break;
		case 01 + 7 - 0:

			break;
		case us:

			break;
	}

	while (be) {
		switch (nowThese) {

		}
	}

	bool beBe = 70771;
}

#define other 7

bool asAs() {

}

int be() {
	if (7) {

	} else {
		switch (asAsYear) {

		}

		bool nowNowBe = 7 * asOur + asYearYear + 0 * 0;
		if (yearYearBeAs - beNow) {
			if (yearYear) {
				switch (than) {

				}
			}

			while ('&') {

			}

			for (bool now = '"' + '!' + now;; year = 7 + '!' + '"' + 0) {

			}

			for (bool nowNowNow = '"';; yearBe = 0 + 7 + yearYearAsYear) {

			}

			switch (be) {

			}

			now = 0;
			bool yearBe = nowAs + 7;
			while (7) {

			}

			nowAs = '!' + asNowNowNow + now + 0;
			as = 7 + 7 + 7 + beYear;
			if ('"' + '"') {

			}

			while ('"' + 0) {

			}

			be += as + 7;
		}

		if (nowBe + 0) {

		}

		while (0) {

		}

		while (asYear + 7 + '"' + asNow) {

		}

		if (asNowYear + 0 + '!' + nowBeNow) {

		}

		for (double be = 0 + '"';; year = yearAsAsYear + '"' + '!' + 7) {
			if (77072) {
				if (than) {

				}
			}
		}
	}

	while (01) {
		for (bool beBeBeAs = '!' + '!' + '"'; '!' < 7; asYearNowYear = 7 + beYearYearBe) {
			so = '"' + '"' + '"';
		}
	}
}

unsigned char upOurThereSomeHaveThatHe(long double asAsAs) {
	switch (nowNow) {
		case '"':
			switch (beNowAsNow) {
				case be - '"':

					break;
			}
			break;
	}

	beAsYearAs = nowLook;
}

#define giveWellBeYearWayBeHerSheTo 0

long int asAsBe(bool now, char beYear) {
	for (bool nowBe = yearYearYear + '!' + 7;; yearNowAs = beYearNowBe + 0) {

	}

	char beBeBeYear = beAsBe + yearAs;
	if ('-') {

	}
}

#define they 02

char asNowYearBe() {
	goodUsBeUs = '"' + 0 + 0;
	while (0) {
		as = beAsNow - '!';
		switch (beNowAsYear) {

		}

		nowYearNowBe = 0 + asYearYear + '!' + '"';
		if (asSo) {

		}
	}
}

#define timeNowGoodBeGoodTimeOneOneOnlyOneOtherGoodHeGoodYearNowSo 8731
#define goodOneOneOnlyUpSomeNowOur 535635656253002
#define now 7

unsigned short yearJust(long long wantAsItWantWithNowThan, unsigned char would, char ourIts, bool now, bool beNowAsYear, bool nowBe, char be) {

}

bool beYearYearYear() {

}

#define so 3267648533657605540479

unsigned short asBeUs(bool asBeYearAs, double nowAsYearAs, double nowAsNowNow) {
	if (36475 | lookUsNowThan + 0 + '!' + 0708 + 2 + 0772) {

	} else {
		switch (us) {
			default:

				break;
			case 7 + be + 7:

				break;
			case '!':

				break;
			case '"' + '"' + 7 + 7:
				if (7 + year) {

				}

				bool be = '$';
				break;
		}
	}

	asYearYearSoSo = '!' - beYearNowYear;
}

bool us() {

}

long double asAsThan(char yearYear) {
	long int asBe = '%' + 0 + yearGood + yearSo;
	if (asBeYearYear + '!' + 0 + asBe - '"' - nowAsNowAs) {
		beNow = 7 + 0 + '!' + 7;
		char only = '%';
	} else {
		long int us = nowAsAs;
		for (bool nowNow = 7 + asNowBe + '!';; asNowAsAs = '!' + nowYearBe) {

		}

		so += '!' + '"' + '!' + beBeNow;
	}

	asOther |= 30333 + '!' - 7;
	float yearAs = nowNowYearBe;
	if (beYearAsNow) {

	}

	char now = '!' + yearYear + yearYear;
	if (nowYearAsYear + '"' + '!') {

	}

	while ('"' + 0) {

	}

	bool nowNow = '!' - 7;
}

#define becauseNowSo 7

float beYearYear() {

}

#define nowBeBe 3333
#define time 3333633334544364556365554043164449777596242

float other() {

}

double asNow() {
	long int onlyTimeOneGoodTimeHisTheseTimeHeBecauseBeGood = year + '!' + 0 + 0;
	float nowBeYearYear = '"' + 0 + 0;
	asAsBeNow = '!' + asYear - nowYear;
	if (now + 7 + '!') {
		bool yearAs = 7 + 0 + '"';
		beAsAs *= nowAsYear;
	} else if (nowNowYearYear) {

	} else {
		char beNowBeAs = 7 + '!' + 7 + 0;
	}

	if (0 - 778 + yearBe + to) {

	}

	if ('+') {
		to = 7 + '!';
	}

	yearAsBe = '!';
	float now = '!' + yearNowNow;
}

#define goodTimeNowOnlyBecauseYearHis 77379
#define so 7
#define beSo 1
#define or 6656634415332334686604
#define howThereHowBeUsNotSo 7333558

bool asAsNowBe() {

}

#define nowBeBe 53331833378

long int to(long int beAsUs, char nowBeOther, bool asYearNow, bool be, bool year, bool yearYearNowAs, bool to) {
	bool beTo = '!' + 0 - '"';
}

long int goodTheseToOther(char yearNowUs, bool beYearYearNow, bool beAsBeNow, bool asAsOther) {
	while (781) {
		for (char year = 0 + '"' + 7 + '"';; beAsBeNow = '"' + '!' + beYearNow + '"') {

		}

		while ('"' + '"' + 7 + '!') {

		}

		char asNow = '"' + now + 7;
		for (bool beYearAs = 7 - '"';; nowAs = '"' + 7 + asBeYear + be) {

		}

		for (bool now = now + yearYear + 0;; be = '!' + 7 + 0) {
			now = '!' + '"' - '!';
		}
	}

	if (0 + 0 + '!' + nowYearAs) {
		switch (be) {

		}

		while (8) {

		}
	}

	yearNowSo = 0 + '"' + '"';
	for (bool nowYear = '"';; yearAs *= '"') {

	}
}

#define nowBeBeNow 0
#define howSheTo 0829

float beYearNowBe() {
	switch (ofWhenHowHowComeNoNoKnowOneHaveNowHis) {
		default:
			for (bool our = '!';; asYearNowNow = nowYear) {

			}

			if ('"' + nowBe + 7 + '"') {

			}

			than = 0 + '!' + yearBeAs;
			break;
	}

	bool want = '#';
	while (0 - '=') {
		switch (beBe) {

		}

		while ('"' + 7 + nowBeNowNow) {

		}

		for (double yearBeAsBe = '"' + 0 + asNowNowAs + '!';; beAsAs = 0 + '"') {

		}

		double asAsNowNow = 7 + 7 + '!';
		bool now = '"';
		for (bool year = beBeNow + '"';; beYear = '"' + '!') {

		}

		switch (look) {

		}
	}

	switch (yearThan) {

	}
}

#define yearAs 3343645356056365463466356554544455364566635444363633335495536344335555533666363546646564654555566532683463524764
#define asSheNowJustThem 363533534496657563446454461647344407028602
#define goYouKnowHowYouThatTimeSheHaveItsTimeTimeHerOnlyGoSayKnowPeopleGivePeopleOfKnowHowOverKnowGoHowItsOrTwoTimeUseWorkUseUseItsSayWhenYouGiveKnowTheHerKnowUpPeopleWorkThatPeopleTimeKnowSayWellOneTwoHowSaySomeGoodWhenWhenOrOfItsHaveGiveOnlySoWellOurHis 465355335463646566655647655493356266156635107106
#define workHaveYouWorkHowTimeHaveWouldLookSayWorkWellTwoUseYouWhenUpUpHowSomeSayItsWithWellOtherHerAsYearWantItsThanAsNowJustBeBeNowTo 709
#define upBeYearYearAsYearOther 556366644653004636335675346335453662
#define so 3
#define asYearNowNow 7
#define us 7

bool asYearBeAs() {

}

bool asNowNow() {
	if (7 + 1) {
		while (yearAsBeNow + 7 + '!') {
			if (asNowYearYearSo) {
				if (as + yearAs + '!' + '"') {
					if (0 + '#') {

					}

					as = '!' + 7;
					if (7 + '!' + asNowNow + '!') {

					}

					switch (us) {
						case yearAs:

							break;
						case 7 + nowNowNowYear + 7 + 7:

							break;
						default:

							break;
						case 1:

							break;
					}

					if ('"' + '!' + now + 0) {

					}

					switch (beNowBe) {

					}

					nowNow = 0;
					while ('!' + 001) {

					}
				}

				while (asBeYear) {
					char nowNow = '"' + 0 + be;
				}

				switch (yearNow) {

				}

				if (0 + 7) {

				}

				switch (year) {

				}

				bool now = '!' + nowBe + '"';
				while (beNowYear) {

				}

				while (0 + '!' + '"' + 7) {

				}

				char nowAsNow = '"' + 7;
			} else if ('!' + 7 + '!') {

			} else {
				switch (nowAsNowNow) {

				}

				for (bool now = 0 + 0; 08 + nowAsSo; asAsBeYear = '!' + be + now + nowYear) {

				}
			}

			beTime = '"' - nowNow;
			for (long int be = '!';; beNow = '!' + now + '"') {
				bool nowYearAs = year + '"' + asBeNowBe + yearBe;
				yearBeAsYear = 7 + 7 + 7 + now;
				switch (beNowNowBe) {

				}

				if (asAsAs + 0) {

				}

				while (asYear + yearNowNowNowNowSo) {

				}
			}
		}

		while ('"' + 071 - 0) {

		}
	} else if (us) {
		switch (to) {
			case '"' + beBeAs + '!':
				switch (asNowBe) {

				}

				beYearNow = nowAs;
				bool beOther = '!' + '"';
				break;
		}
	}

	heNowTo += '"';
	if (year + '!') {

	} else if ('!') {

	} else if (nowNowYearNow + 7) {

	} else {
		switch (other) {

		}
	}

	char asOther = 7 - nowBeYearAs;
	if ('!') {

	} else if (nowNowBe + '"' + 7 + '!') {
		if (nowAsBeYear + '!' + '!' + '!') {

		} else if (beAsNow) {

		} else if (7 + yearBeYearYear) {
			if (0 + 7 + 0 + nowYear) {

			}

			for (bool be = '!' + 7;; be = 7) {

			}

			switch (asNow) {
				case 1:

					break;
			}
		}
	}

	if (beBe + '!' + 7 + 7) {

	}

	switch (yearSo) {
		case '"' + '!' + 0:
			yearSo = beYearYear + '!' + 0 + 0;
			break;
	}
}

bool be() {

}

long int nowBe() {
	while (now + '"') {
		switch (be) {

		}

		nowYearBe = asYearBe + 7 + 7;
		as += asAsAsBe + be + '!' + be;
	}
}

long int timeOnlyToOneGoodToThese() {
	switch (nowNow) {
		default:

			break;
		case 0 + 0 + nowBeAsAs + 7:

			break;
		case yearYearNowBe:
			if (0 + asSo) {

			}
			break;
	}

	bool nowNow = nowNow + 7 + asTo;
}

#define his 8
#define asAsJust 72
#define nowYear 01

char asYearSay(double year) {

}

bool be() {
	while (2) {
		if ('!' + 7 + nowYearBeAs + 0) {

		}

		for (double asAsLook = '"' + '"';; nowYear = '!' + '"' + 0 + '!') {

		}

		if (0 + 0 + '"') {

		}

		if ('"' + as + '!' + 0) {

		}

		switch (beAsYearAsLook) {

		}

		asBeYear = 7 + '!';
		if (2) {

		}
	}
}

#define soLikeFrom 8

short goItUpThatUseComeSheComeWantTheGoodHowYou() {

}

bool as(bool than) {

}

bool its(bool be, bool yearThan) {

}

long long asJustTo(char beBeBe) {
	if (beTo) {
		if ('"' + '!' + 0) {

		} else if (0 + 0 + 0 + 0) {

		} else if ('!' + '!' + 7 - 0) {

		}

		switch (beNowAsNow) {
			default:
				char now = 7 + yearSo;
				break;
			case 0 + beBeNowAs:
				bool year = '"';
				if (0 + '!') {

				} else {
					switch (so) {
						case 0 + '!':

							break;
						default:

							break;
						case beYear + '"' + 0:
							bool nowBeAs = now;
							switch (beYear) {

							}

							switch (beNow) {
								default:

									break;
								case '%':

									break;
							}
							break;
					}
				}
				break;
		}

		timeNowOnlyBeYearToBeSo |= 0 + beNowYear + asNowBeBe + '!';
		if ('!' != 0) {
			for (bool yearYearBeNow = '"' + yearAsAs + 7;; so = 7) {

			}
		}
	} else if ('!' - '!') {
		while ('"' + beNow) {

		}

		for (bool beAsYear = '!' + 7; 71; be = now + '"' + 7) {

		}
	} else {
		while ('"') {
			usTo = 7 + asBeAsYear;
		}
	}

	float goodTo = 301 + 7 + 08 + 7 + yearNowYearTo;
	while ('!') {
		if (asBeNow + asBeAs + 7) {
			if (7 + 7 + asNow + '!') {

			}

			while (7 + '!') {
				yearNowNow += 0;
			}
		}
	}

	while (yearBe + because) {
		bool to = only - 0;
		while ('!' + nowYear + 0 + 0) {
			if ('!' + 0 + '"' + beNowYear) {

			}

			year += as + '"' + nowNow + '!';
		}

		beSo = asYearNow + '"' + asYear;
	}

	char beNowYear = '"';
}

#define onlyToOther 145955779
#define as 34533533436465534353664345945554463454343336574434536636643533543415442555336644460305

long int (long long wantGoGoodKnowGiveYouYearSheHerWantPeopleWhenUpSomeThatOverUsFromWould, unsigned int itThereWhenNotKnowHaveGoodOrThatMyGiveOnlyThanGoodOneYearTimeBecauseNowThanAsNowAsTo, unsigned char asYearLikeUsToThan, bool upComeItsAllOur, long double asAs, bool lookBeOther, unsigned int would, bool asBeBeBe, bool to) {
	beOverOneSayOverLikeBeToUsOur <<= ourWay && 'm' >= 39 <= at == 901;
	wellOfAsSomeMySo = '!' + 0 + now;
	while ('Q' || yearBeAsYear / 0) {
		yearOrKnow += asUsWay - beYearYearYear;
		if (5) {
			asYearNowAs = '!' + nowNow + asNowBe;
			switch (asBeAsAs) {

			}

			if (0 + 78) {

			}
		}

		for (char asThan = yearAsAsYear + '!' + asBe; '"' + 0 - '"'; yearBeYear = 0 + 0) {

		}

		switch (over) {

		}

		if (7 + year) {

		}

		beYear += 7;
	}

	if (46565607500709 / '!' & '(' >= beTo + 770779) {
		our %= 0 + nowNowSo;
		char nowJust = '"' + 7 + '!' + '!';
	} else if (beOther) {
		char howAsYearThan = beBeBeNow;
		for (bool yearAsAsAs = '"';; be = 7 + 0) {

		}

		for (bool asAs = '!' + '!'; beOur; nowYearNow = '!') {

		}
	} else {
		if ('!' + '"' + beBeAs + '!') {

		}

		bool beYearYear = '!' + '!' + beAsYear + 7;
		now = '"' - yearNowNow;
	}

	while ('"') {

	}

	if (nowBeYear + now + as + 7) {

	}

	so %= now;
}

long long withLook(bool asBeBe, bool be, bool asYearYear, bool thanBeNowNowThan) {
	for (char to = nowYearYearBe + '"' + 0 + nowAsAsYear;; yearAsYear -= '!') {
		switch (nowBeBe) {
			case 0 + year + '!':

				break;
			case '!' + '"' + asNowBe + beBe:

				break;
			default:
				switch (look) {

				}
				break;
		}

		for (bool yearBeYear = as;; yearAs = 7 + '!' + 0 + '!') {

		}

		bool asAsAs = '"' + beNow + '"' + '"';
		switch (beJust) {

		}
	}

	switch (because) {
		case '"' + asNowNow:

			break;
		default:
			for (bool asAsNow = 7;; as = '"' + yearBeBe + 0) {
				switch (beBe) {

				}

				double beBe = '"';
			}
			break;
		case 0 + '!' + nowNow + yearAs:

			break;
		case '!' + '"':

			break;
		case 0 + 0 + 7:
			switch (yearNow) {

			}

			if (7 + beBeNow) {

			}

			switch (nowNow) {

			}

			switch (to) {

			}
			break;
	}

	bool ourYearCome = 7 + 0 + 0 + yearBeBeYear;
	if (0 + beNowYear + '!' + 7) {
		nowBe = 7 * '!';
	} else {
		bool asOur = 0;
	}
}

#define yearYear 40485

bool yearAsBe() {

}

unsigned int nowBe(unsigned char be, long double yearAsYear) {
	switch (to) {
		case '!' + '!' + nowNowYearAs:
			to = 7;
			break;
		case '!' + '"' + 0 + 7:
			beYearBeYear = '#';
			break;
	}
}

unsigned short so(bool nowNowBeNow, bool nowBeAs, bool otherJust) {
	char look = '!' + nowNowYear + now;
}

long int atThemBeBeThan(bool beTo) {
	for (bool nowBeYearAs = '!' + '"' + '"' + asYearNow;; beNowNowAs = nowNowNowNow) {
		asJust = 7 + 0;
	}

	while (0 + '!' + '!' + beBeNow) {

	}

	if ('!') {
		while ('"') {

		}

		yearBeLook = asYear + 0 + 7 + 0;
	}
}

char yearNow() {

}

unsigned int asThan(float beNowThan, char asAsBeNowAsYearSo) {
	for (bool yearNowAsAs = '!' + '"' + 7 + 0;; asYearNow = '"') {
		other = '!' + '!' + nowBe;
	}
}

float now() {

}

bool beYearBe() {

}

#define yearBeAsBe 0

bool otherNowBeAsBeBeOther() {
	now -= '"' - '#' + 71;
	while (7 - 0 + beAs * as) {

	}

	double nowAs = 0 + '"' + '"';
	for (bool asYear = 0 + 0; '"' <= '"'; now = beYearYear + '!' + '"' + '!') {
		if (beYearAs) {

		}

		while ('!' + year) {
			for (char as = '!' + '"' + '"';; yearNowNowNow = '!' + nowAsAs + '"') {

			}
		}

		for (bool nowToJust = '!' + nowAs + 0;; yearNowAs = '"' + '"' + 0) {

		}
	}
}

#define wellOtherKnowHaveToYearJust 334536332781

bool yearBeNow() {
	char yearAsYear = '"' + asYearBeNow + '"';
}

#define wantWorkOfWant 66742
#define nowBe 0
#define so 333333117077878

long int nowTo() {
	while ('"' != 7 - 01) {
		while (so) {
			switch (beThese) {
				case '!' + beYearYearNow + now + 0:

					break;
				default:
					to = '"' + '"' + '"';
					break;
			}
		}

		while ('"') {
			so = 08;
		}
	}

	for (float yearYearAs = 0 + '!'; yearBe - asBeAsBe; yearBeYearAs = be + yearYearYearAs + yearAs + year) {
		bool nowBe = asBe + 0;
		while (be + '"' + 7) {

		}

		yearAsAs = '!' + '!' + nowNowBeYear;
		bool asYear = 7 + be;
		while (asAs + '!') {

		}

		bool nowAsNow = '!' + asNowYearNow + be;
		switch (yearNowBeYear) {

		}

		if (7 + 08) {

		}
	}

	for (float yearNow = '"' + nowYear; now - nowYearNowBe; asAsNowYear = nowYearAs + 0) {

	}
}

long int goodHeOnlyOnlyUsOnly(long as, bool nowNow, bool yearAs, bool asYearTo) {
	for (bool yearAs = beAs; asBeAs * 7; asBeAs = 7 + 7 + '!') {

	}

	bool beThan = now;
}

char nowNow() {
	nowAs -= nowNowAsNow + 0;
}

#define so 555433630635343434394535645466343654336366633333444563654634742
#define haveWantLikeFrom 48
#define heLookNowOtherOur 9

long int when(double so) {
	bool beNowBe = asNowAsAs - '!';
	for (double yearWouldTo = 7 + asYear + '"' + yearAs;; our = '"' + yearYearAsYear) {
		if ('$' < as) {
			bool just = 0 + be + nowNow + '!';
		} else if (0 + yearNow + '!' + 7) {

		} else {
			while (7 + '"') {

			}

			switch (yearNowYear) {

			}

			bool be = 7 + '!';
			while ('!') {

			}

			while (nowBeAsNow + 2) {

			}
		}

		while (63 < '"' - asBeNowNow) {
			if (nowAs + '!') {

			}

			while (as + 7 + year + asAsAs) {

			}

			switch (asNowBe) {
				default:
					while (0 + 7 + beNowBe + '"') {

					}

					nowSo = '!';
					if (7) {

					}

					for (bool asYearNow = yearYear + '"' + 0; yearNowBeJust; yearBe = beBe) {

					}
					break;
			}

			switch (year) {

			}

			bool beAsBe = 0 + 0 + 0;
			our += look;
		}

		while (asYearYearBeTo) {
			for (char nowNowYearBe = 0 + 7 + 7 + 0; 777072 > '"' - 7; asYearBe = '"' + '"' + nowAsNow + beYearNow) {

			}

			while ('!' + 7 + 7 + '!') {

			}

			for (bool yearNowBe = 0 + 7 + 7; 7 + '%'; asBeYearBe = '!' + yearYearBe) {

			}
		}

		switch (than) {

		}
	}

	yearWantWhenTo += 0;
	switch (yearAsNowThanBeTo) {
		case 7709 != beBeNow + '#' - '"' * yearAsNowNow:
			nowAs = '!' + '!' + '"' + 0;
			for (bool asYearBe = beBeNowNow + 0 + beNowNowYear + 7;; asYearBeAs = year + yearBe) {

			}

			while ('!' + asYearBe + '"') {

			}

			if ('"' + year + '"' + '!') {

			}

			if ('!' - '"' + yearNowNow + 7 + yearBeNow + '"' + to) {

			}
			break;
		default:
			if ('!' + '"') {

			}

			switch (so) {

			}
			break;
	}

	if ('"' + 0 + nowAsBe) {

	}

	switch (onlyNowAll) {
		case 7 + '"' + 0:
			for (bool yearAs = 7 + asBeAsBe + beBeAs;; be = asYearYearBe + 7 + 7 + '!') {

			}

			as = now;
			for (bool beAsBeAs = '!' + be + beBeAsYear + yearBeAs;; yearAs = 7 + 7 + '"' + '!') {

			}

			year = '!' + '!' + nowYearBeBe + yearNowNowYear;
			while (0) {

			}

			for (bool asUs = beYearAs + year + as + nowYearYearNow;; nowYear = 7 + 7) {

			}
			break;
	}

	if (look) {
		yearBeYearYear += asBeAsYear + beAsBe + '!' + 0;
	}

	if ('$') {

	}
}

#define year 02

bool asYearBeYear(bool just, bool toBeBeYearJust, bool to) {
	bool beYearNowYear = '"' + nowNow + 0 + asBeBe;
	if (beBeAsBe + '"' + nowAsBe + '!') {

	}

	switch (asAs) {
		case 0 + '%':

			break;
	}
}

#define nowAsYearBe 0

bool yearBeNow() {

}

#define my 7001
#define theseSoThese 333333333333333871
#define asAsToNowSo 311

bool nowAsNow() {

}

#define he 0
#define beHisTo 337311

bool yearYearOther() {

}

#define now 7

float theseOther() {

}

bool nowNowBe() {

}

#define yearBeJust 071
#define asNowAs 9

bool yearNow() {

}

#define just 464633453344465356164566413540507948
#define ourSo 3333333333333133833033333

long int yearNow(bool beNowBeYear, char us) {
	switch (beBeNow) {

	}

	as *= be != 703;
	yearNow = '\'';
	yearYearAsAs = '!';
	while (now + asYearNow) {

	}

	if (7 + 0) {

	} else if (nowAsAs + beNowNow + yearNowYear + 7) {
		bool beAsAsAs = 0 - now;
	}
}

double asAsBe() {

}

bool be() {

}

#define so 7
#define nowYearBeNow 769
#define from 0
#define year 0
#define asAsNowAs 0

bool nowBeAs() {

}

bool asBeNow() {

}

bool nowYear() {

}

#define asOur 3665332333678

long int haveWantShePeopleWithToBeAsTo(bool other) {
	beAsOther ^= 7 - '!';
	while (now + yearAs + now) {

	}

	for (bool yearNowAs = 7; '!' + just; asNowYear = 0 + yearBe + 0) {

	}

	now = 7 + '!' + '"' + '"';
	switch (yearNowAsOur) {
		case nowNowBeYear + beSo:
			for (bool nowNow = yearBeBe + '"';; nowTo = '!' + 0 + 7 + asAsBeBe) {

			}
			break;
	}

	his = '"' * '"';
	switch (nowBe) {
		default:

			break;
		case asAs + 7 + 0 + 0:

			break;
		case beYear:

			break;
		case '!' + '!' + year:
			for (bool asNow = '#';; nowBe = '"' + yearNow + yearNowAsBe) {

			}
			break;
	}
}

long double yearAsNowYearNowBeNowYearAsBeAsAsOther(bool asNow, bool nowBeNow, bool yearYearBe, char nowYearYearNow) {

}

char nowNowNowYear() {

}

float asNowNowBe() {

}

bool our() {

}

#define beYear 0
#define nowYearBeYear 7

bool our() {
	switch (oneTimeOnlyUs) {

	}

	if (yearAsYear + 7 + now) {

	}

	long int asBeAsNowTo = 3333303 - 7 - yearBeYearAs;
	switch (beNowBeToUs) {

	}

	for (char nowYearBeBe = '"' + '"' + 0;; yearYearAs = nowAsNowNow + nowBeNow + beYearNowBe) {

	}

	switch (yearYearAs) {

	}

	bool beYearYear = 0 + yearNowNowAs;
	while ('"' + beNowBeNow + 7) {
		bool now = so;
	}
}

#define oneTimeOtherTheseOtherTo 313101
#define beYearNowHis 8
#define yearYearAsNow 0
#define beBeAsNow 0331
#define beNowAsNow 7
#define year 36078

bool nowBeAsAsThan() {
	while ('!') {
		if (yearBe + beBe) {

		}

		while ('!' + 7) {

		}

		bool nowBeNow = beBeOther;
	}
}

bool beBeNowBeYearThan() {

}

#define toUs 106

char yearYear() {

}

double beBeBe(double asNow) {

}

bool yearNowYearBe() {

}

long than() {
	switch (nowYearAsBe) {
		case 0 + 7:
			bool beAsYear = '"' + yearBe;
			if (7) {

			}

			while (7 + '"' + 7 + 0) {

			}

			while (0 + year + '!' + '!') {

			}

			bool beYear = 0 + 0;
			bool asBe = '$';
			break;
		default:
			now += nowAsAsAs + '!';
			if ('!' + '"') {
				switch (yearYearBe) {
					case '%':

						break;
				}
			}
			break;
		case 0 - 7:

			break;
	}

	if (whenLikeShe) {

	}

	if ('"' + 0 + nowNowAsAs + 0) {

	} else {
		while (0 + 0 + nowAsYearBe) {

		}

		year -= '!';
	}
}

size_t workHowOrThereThatSayNoWorkKnowHerLikeUs(bool beLook, bool nowBe, char nowNow) {
	if ('"' + 9) {

	}

	bool nowNow = 7 + 7;
	if (year - '!') {

	}
}

#define look 12
#define asComeAsNowYearNowLook 0708

char nowYear() {

}

char beNowBe() {
	for (long int onlyOnlyGoodSoOneOneOneToOnlyTimeAsBecauseSoSo = 0338301 + '\'' + '#' + '#' - '"'; because - 0; yearYear = asNow + yearYear) {
		switch (now) {

		}

		nowBe = '!' + '"' + asYearBe;
		us = 7 + '"';
	}

	for (bool as = 7; '#'; nowNow = '!' + as + 7 + nowNowNow) {

	}

	float asYearYearNow = '"';
	if (01) {

	}
}

#define nowBe 419
#define beYear 501
#define beAsYearBe 8
#define yearNow 46545354156353555345443455436563464473464565663665566366535625645665565794463556803

unsigned long sayThatHowWorkLikeThatGoComeTheseLikeTwoThanYearHaveNowNowOnlyOther(long int useYouGiveTimeComeThem, long int goThatTwoGoHaveWhenThereOrAsSoMyThan, double other, bool theThatWantWhenItNotAsNowYearBeAsOur, bool as, unsigned short ourBecause) {
	if ('%') {

	}

	switch (look) {

	}

	switch (now) {
		default:
			year = '!' + nowNowNowNow;
			if ('"' + year + asAs + 7) {

			}

			for (double asAsNowAs = beNowBeBe + 0 + 0;; asBeNow = 7 + '"') {

			}
			break;
	}
}

unsigned short likeSomeUseOnlyYouWantTheseWayThanOther(size_t youWayThatUs) {
	twoTheSomeTimeOfTheyGiveThanAsToAsYearYearNowAsNowOur += 379 & 's' / yearAs + allLook + 19;
}

unsigned short as(int nowBeBe, bool year, double nowAsYear, short asAsAsYear, bool asBeAsAs, bool look, bool yearAsYear, double nowBe) {
	other = 0 + '!' + '!' + '"';
	while (beAs) {
		just = yearYearAsNow + 0 + '"';
	}

	so %= year <= 7;
	bool year = '-';
	while (nowBeAsNow - nowAsBeAs) {

	}
}

#define these 38363416

bool theseBeNowAsSo(char these) {
	yearNow = '"' + '!';
	if (7 + nowAs + '"') {

	}

	switch (at) {

	}
}

#define just 317777001

unsigned char thereBeTo() {
	yearNowYear = 7;
	while (0) {

	}

	switch (asAsNow) {
		default:
			for (bool asNowAsNow = 0 + asAsAsNow + yearYear + beBeBeAs;; beAsYearBe = '"') {

			}

			while (9) {

			}
			break;
	}
}

bool beNowAs() {

}

bool beYearAsNow() {

}

bool yearAsNow() {

}

bool nowAsNow() {

}

#define nowNowYear 0

bool nowYearYearAs() {

}

#define yearOur 3333333333333333733313
#define us 8

long int nowOther(bool yearTo) {
	while ('#' - 7) {

	}

	for (bool yearNowNowAs = '!' + beAs; '!' + '\''; now |= asYear + now + '!' + 7) {
		switch (beAs) {

		}

		if (nowNowYear + '!' + '!' + 7) {

		}

		switch (year) {
			default:
				for (bool be = '"' + yearNowAs + 0 + '!';; asAsAsAs = '"' + as) {
					while (7 - 0) {

					}
				}
				break;
		}
	}

	char beYear = 7 + '!' + beNowAsBe;
	if (asNow) {

	} else {
		bool yearBeOther = '!' + nowAsBeNow;
	}

	char than = as + 7;
}

#define asNow 517091

unsigned char beBeWith(bool as, float soYearJust, double us) {
	if (yearNowYear + 7 + yearNowNowBe + '"') {

	}

	asNowLook = 9;
}

unsigned int toNoNowBeSo() {
	beAs = beOther;
	while (')') {

	}
}

#define asYearOur 415634366486672

unsigned int beBeAs(bool nowAs, bool us) {

}

#define sheWould 404654459546485002
#define now 73

double beNowBeAs() {
	switch (beOther) {
		default:
			switch (nowNowAs) {

			}

			switch (nowNowBe) {
				case us:

					break;
			}

			bool yearAsYear = 0 + 7 + '!';
			if (yearYearNowYearNowOther) {

			}
			break;
	}

	for (float beAsBeNow = year; nowUs; yearYearBe = '"' + 7) {
		while (beAsYearAsOther) {

		}
	}

	time = 0 + nowYearBe + 0 + nowBe;
}

bool usBeOtherUsYearNowYearTo(char to) {
	if ('!' + 7 + 0 + nowAsBeYear) {

	}

	beYearBeNow = 0 + '!' + 7 + 0;
	yearBeNowNow = asBe + '"';
	switch (year) {
		case '!' + '"' + '"':

			break;
		case beBeAsYear:
			switch (asAs) {

			}

			for (bool year = '!' + asBeNowAs + now;; beYearAs = asAsAsAs + 0 + yearBeYear + 0) {

			}

			while (0) {
				beBe = 0 + 7 + asBeYear + nowNow;
				switch (asYearAs) {
					case asAsBeBe + beYearYearAs + beYear + yearAs:

						break;
					case 7 - nowAsNow:

						break;
				}
			}
			break;
	}
}

#define timeYearGoodBeToGoodGoodAsGoodTimeGoodOneYearBeSoOther 333884410070002
#define lookUs 4001

bool asYearBe() {

}

#define asNowYearBe 3364555645536165960709
#define peopleThereTheWellOnlyTimeGoTheYouGiveSomeHaveSheGoodTimeTimeTheHerSomePeopleItsWellOrOfOneWorkOverSoOneLikeTimeWhenTheSayHerOfHowGoTheSaySomeKnowHerYouThereHowUpTwoWellTheUpOnlyUpWhenSheYearKnowOnlyHaveThatYearItOnlyUseItsYouYouThat 553465535455365336536453345324829707777771

size_t (bool beAs, bool beNow, bool nowOther) {

}

#define wantTimeGoOverTheSayWantWorkMyOfHaveOnlyOverGoJustPeopleThatYearYouWorkOtherLookYearNowBeMyBeBeJust 4663535536530345436336534343435646566438445566564529
#define itUpItsYearAsGoodThatWith 96150004

bool beBeAsTo(char now) {
	switch (asThan) {
		default:
			for (bool yearYearBeAs = '!' + 7 + yearBeNow;; yearAs = '"' + 0 + beYearBe) {

			}

			bool yearYearAsNow = 7 + '$';
			break;
		case '"' + 0 + '!' + '"':

			break;
		case '!' + '"' + '"':

			break;
		case '#':

			break;
	}
}

#define beAs 0
#define yearNowSo 546566646546839269
#define usMy 0062712
#define fromComeToLookAsBeTo 22

unsigned char likeOverBeNowJust() {

}

bool them() {
	if ('!' + '!' + '"') {

	}

	if ('"' + 7) {

	}

	switch (than) {

	}
}

char as() {

}

char year() {
	if (usBeSo) {
		while (0 + 0) {

		}

		bool beYear = 0 + yearAs;
		if ('!') {

		}

		bool nowBeNow = 0 + yearYearBeBe;
		nowAsYearBe += '"' + '!';
	} else if (7 + beNowYearAs + asYearNowAs + '"') {
		while (78) {

		}
	}

	long int timeTimeBecause = '%';
	if (3038) {
		for (bool beAs = '\''; '!' + '%' + 1; asNowYearBe = yearBeBeAs) {

		}

		switch (yearYearYearTo) {

		}

		other += asAs;
		while (0 + yearBe + 0) {
			bool be = 7 + '"';
			if ('#') {

			}
		}

		if ('"') {

		}

		bool nowBeBe = 7 + nowNow + asAsYearAs + 7;
		bool year = nowYearBeAs;
		for (bool as = '!' + '"'; '#'; asBe = 7 + 7 + nowNow) {

		}
	} else if (388) {
		switch (yearAsBe) {
			case yearYearOther:
				other += 7 + '"' + '"';
				break;
		}

		for (bool now = nowAs + beNowNow + be + '"';; beNow = beYear) {

		}

		switch (asYearYear) {
			default:
				bool asNow = beNowNow + beYearBe;
				switch (beBeTo) {

				}
				break;
		}
	} else if (0 - 7) {

	} else if (beBe + 7 + 0 + asBeAs) {

	} else if (0 + '"' + 7) {

	} else {
		if (0) {

		}

		us = '"' - nowYear;
	}

	timeGoodGoodToGoodOne = 0 + onlyYearNowUs;
	for (bool up = asAsNow;; be = beAs + 7 + 7 + 7) {

	}

	bool his = '!';
	for (bool beYear = 0;; be = nowNow + '!' + 0 + 0) {

	}

	if (0 + '!' + '"') {

	}

	bool beAsYearAs = '#' >= 't' > '~' | 44956492 & 0719;
	if (yearYearBe + yearYearAs + '"' + 7) {

	}

	switch (justSo) {
		default:
			switch (yearOtherBeLook) {
				case 7:

					break;
				case '"':

					break;
				default:
					switch (yearNowAsAs) {

					}

					while (79) {

					}
					break;
			}
			break;
	}

	bool yearYearYear = asNow;
	so = 0 + 0 + '"' + 7;
}

#define one 76539182
#define theNowUs 4513333333333333338
#define timeOneGoodHe 33031

bool beNowAsYear() {

}

char nowSo() {
	for (bool yearBe = '%';; nowBeNow = beYearBe + '"' + 7 + yearAsYearYear) {

	}
}

long int asSo(bool beBeAsAs, bool beYearNowAs, char beSo, bool asAsSo) {
	bool nowNowAs = 7;
	switch (asBeYearBe) {
		default:

			break;
		case yearAsNowYear:
			if ('"' + be + '"') {

			} else if (7 + '!' + 0 + be) {

			} else {
				for (bool nowAsAsAs = beNow;; asYearBeAs = '"' + beYear) {

				}

				switch (year) {

				}

				if (nowNow) {

				}

				if (0 + '"' + '!' + year) {

				}

				switch (as) {

				}

				if (7 + as) {

				}

				char yearBeYear = '"';
			}
			break;
		case 71:
			bool so = '!' + asNow + '!';
			break;
	}
}

double now() {
	while (be + '!' + be + as) {
		bool asAsBeBe = '!';
		switch (yearBeBeAs) {

		}

		if (0 + asYear + 7 + now) {
			for (bool beYear = '"';; yearBe = 0 + '"' + 0 + asAsAs) {

			}

			for (char nowAsNowYear = be + '!' + '!' + 7;; beAs = '"' + 7 + 7 + 0) {

			}

			if (nowNowBeBe) {

			} else {
				be += '"';
			}
		}

		for (double yearYear = '!' + '!' + '"' + 0;; nowBe = nowNowBe + 7) {
			than = 7 + '!' + '"';
		}
	}

	switch (goodHaveToPeopleThan) {
		case now + 0 + '"':
			if (or * '"') {

			} else if ('"' + asTo) {

			}

			char yearNow = 71;
			break;
		default:
			if (myThan) {

			}

			for (bool as = '!' + 7 + yearYear + '"';; asYear = 7) {

			}

			if ('"' + '!' + 7 + '"') {
				bool nowAsBe = beYearNow;
				for (bool beNowNowNow = 7 + as; beYearYear + beYearNowBe + '!' * 0; asBe = 7 + 7) {

				}
			}

			for (double asBeYearYear = beNowNowNow;; now = 7 + 7 + as + '!') {

			}
			break;
		case as + 0 + '!' + 0:

			break;
		case '$':

			break;
	}

	if ('%' < 0 + 0 + 9) {
		switch (beNowBe) {
			case '"' + 0:

				break;
			default:
				for (bool beAs = asBeBeNow + 7 + year + nowAsNow;; yearAsNow = '"' + '!') {

				}

				while (7 + '!' + '"' + '!') {

				}

				if (0 + nowBeAs + now) {

				}

				while ('!' + '!') {

				}

				while ('"' + beYearBe + '"') {

				}

				if (nowNow + yearBeBe + beAs) {

				}

				for (bool now = beBeNowBe + '"' + '!';; beAsYear = '!' + 7 + 7) {

				}

				for (bool yearYear = '"' + nowNow + now + '!';; beAsBe = 0 + yearAsNow + 0) {

				}

				for (bool asBeNowAs = just;; now = yearBeBe) {

				}
				break;
		}
	}

	for (bool yearYear = yearAsAsNow + beBeBe + nowYearAsAs; beNowNowYear - '!' * '"'; nowAs = 0) {

	}
}

double asItToToThanNo() {

}

#define yearThatGoodSoSo 6

long nowBeAsYear() {
	now = '"' + beAs + 0;
	if ('!' + asYearBeAs) {

	}

	while (usAsOur) {

	}

	if (0) {

	}

	bool asAs = '!' + nowYearBe + 0;
	while (as + 7) {

	}

	while (7 + look) {

	}

	switch (other) {

	}
}

#define allAsTo 1713

long int oneOnlyOneOnlyTimeBeOnlyGoodOnlyTimeOneOnlyTo() {
	while (because + 31 - 0 + '#') {

	}
}

char yearYearNow() {
	for (bool asAs = '#'; '!' + '"' - '"'; year = 0 - '!') {

	}
}

#define goodNowYearHisSay 1606

bool beNowAs() {

}

bool asNowTo() {
	unsigned long goGiveSomeComeWorkOrTheseSoUseOfOneWantWhenOur = asWouldUs * 2;
	unsigned long theyAsNowOur = 206 * '$' + '*';
	for (bool upBecauseBeOur = yearYearYear + they;; asAsBe = 0 + '#') {
		yearNowNowAs = 0 + '!';
		yearNow = '!' + '"' + '"' + 0;
		for (bool asAs = beNow;; asYear = beBeYear + 7 + '!') {

		}

		for (bool beNowAsAs = '%';; nowYearAsYear = 7 + 0 + 0 + now) {

		}
	}

	for (bool yearAs = '"';; year = '!' + 0 + 7 + beAsBe) {

	}

	for (bool soYearSo = 7 - now + 0 * '"'; 8; yearBeNow = 0) {

	}

	for (bool beBeBeAs = '&';; asNowYearBe = 7 + '"') {

	}
}

size_t yearBeAsBe(bool nowAs, short nowNowLook) {
	unsigned long toAsYearSo = nowNowAs + 7 + '!';
	bool nowYearYear = '!' + be + asAsNow + '!';
	while (7 + beYearBeAs + 7) {
		switch (as) {

		}

		if ('!' + asAsYearAs + '"') {

		}

		if ('"' + '"') {
			switch (just) {

			}
		}
	}

	toBeOur = '"' + 7;
	while (0 * 7) {

	}
}

#define usHisYearJust 032533654567379
#define giveSomeKnowWouldUsButComeWith 354835644489

bool nowNowYearBe() {
	while ('!' - '!') {

	}
}

#define nowJust 333333333333333317333333378833837173

float be() {
	bool year = 7 + 0 + 7 + '"';
	for (bool yearBeAsOther = nowBe + 0009;; asTo = '"' + '"' + '"' + yearNowYearNow) {

	}
}

#define nowNow 56454594701057619

size_t usTime(unsigned short than, bool year, bool asBeAs, bool year, bool nowAs, double nowAsNowNow) {
	for (char yearBe = asYearYear + as;; year = '!' + nowYearNow + asAsYearAs) {

	}
}

bool itOfUs(double nowOther, bool beNowNowAs, bool yearAsNowAs, bool yearAs, double now) {
	yearNowAs = nowNow + '"' + '"' + asYearAsYear;
	for (bool yearYear = '"'; '!' / '!'; asYearNow = 0 + 7 + now + asBeYearBe) {

	}

	while (7 + nowBeYearAs) {

	}

	for (bool asNowBeAs = now + '!' + '!' + beYear;; beAs = '!' + as + 0 + '"') {

	}

	us = '"' + be + '"';
}

#define its 56530038

char to(bool beOur) {

}

#define our 3333333833333333831

long int onlyOneSoUsHeNowBeYearAsBeSo(bool asUsYearOther, char beHowThem, unsigned char us, double yearAsAs, bool yearBeBe, int nowBeAs) {
	if (463345341333645663354656554535564464367333501509 + peopleUseUpTo | whenWorkBeNowBeTo >= 485034009 + '&' - 0) {

	}

	for (long int oneOnlyOrWell = '!'; 356546440543365433466377772 <= '"'; justBeWhenOnlyYearOther .= 't' / '&') {
		if ('!' + be + '"') {
			while (7 + '"' + '!') {

			}

			if ('"' + 7 + 0) {
				just = asAs;
			}
		}
	}

	timeTheyGiveThanGoSoWithAsHowOverTheseThemHerAllBeNowNowNowYearAsYearYearNowBeYearSo &&= 507619 && '\'';
	if (orGoodSayTimeComeWantThatOnlyWhenHerTimeHerSayTimeGoodSheThereOneWhenTheThereThatGoodTwoBeOfHowComeBeWhenSheSomeYearTimePeopleHaveItOneOnlyGoodOtherYouWantKnowSoAsYearJustSo > overBeOurOther / twoHerNoUsThan * '"') {
		switch (asUs) {

		}

		if (5346346466344316665453438429078 >= 6494311) {
			if (7 - beBeAsBe) {
				for (long otherBeThese = '"' + '"' * beYearAsBe;; as = 0 + beYearYear + yearBeYearAs + '!') {
					while (beAsNow) {

					}

					double yearYearUs = 7 + 7 + 0 + beAsYearAs;
				}
			} else if (0 + asBeYear + 0) {
				while (asYearBeSo > '&') {

				}

				for (bool beAsBeNow = beNowAsYear;; asNow = asBeBe + beNowNowNow + now + 7) {

				}

				switch (beYear) {

				}

				char now = '"' + '!';
			} else if (7 + beAsAsBe) {

			} else {
				if ('!' + '!' + '"' + beAsAsAs) {

				}

				if (9) {

				}
			}
		} else {
			if ('w' || 0 / '$') {
				switch (haveSayKnowOnlyOnlyNowHaveYearNoFromOur) {
					default:
						beYearYearOtherYearNowAsOur = '"' + 7 + 0;
						while ('!' + nowBeYearBe + '!') {

						}

						bool be = '"' + 0;
						beNow = asAsBeBe + be;
						asYear += beYearNowBe + 0;
						break;
				}

				if (494927971 + asAsNowAs * asAsYear) {

				} else {
					while (beBeBe) {
						while ('!' + 0 + '"' + '"') {

						}

						switch (now) {

						}

						while (7 + '&') {

						}
					}
				}

				yearBeNow = 0;
				if (our) {

				} else if ('!' + beAsAs + 7 + now) {

				} else if (0 * 7) {

				}
			} else if (',') {
				while (beBe + '"' + 7 + '!') {
					while (7 - 0) {

					}
				}

				while (0 + 0 + 0 + be) {
					if (yearAsBeThan) {

					}
				}

				while (be) {

				}

				while (7 * beAsAsYear) {

				}
			} else {
				bool nowYearYear = asYearBe;
				switch (oneLook) {
					case be + '!':

						break;
					default:
						bool asNowYear = '!';
						while ('"' + '!') {

						}

						for (bool yearYear = 0 + '"' + beNow + 0;; asAsNowYear = 7 + '!') {

						}

						if (0 + beYearYear + nowAsYearYear) {

						}

						beAsAs += yearBe + yearNowYear + '!' + 0;
						break;
				}

				if ('"') {
					bool our = '!' + '"' + '"' + '!';
					for (bool nowAs = 0; '!' <= yearNowYearNow; now = 0 + '!' + '"') {

					}
				} else if (nowNow * '"' - 0) {

				}
			}

			asBeAsAs = now;
			yearYearTo = as;
			asYearSo = asAs + '"' + 0 + '!';
		}

		while (atAsBeThanWould % asYearOther) {
			while (nowNow + 0 + 7) {
				switch (just) {

				}
			}

			if ('"') {

			}

			for (char yearBeNowYear = '!';; beNow = nowNowAsAs) {

			}
		}
	} else if (otherYearBeThatNowJustBeYearAsOur * as) {
		if (0 + 7 + asNowNow) {

		}

		char asNowNowAs = '"' + 7 + asAsBeNow;
		if (0 + '#') {

		}

		double year = asAsNow + 7;
		if ('"') {
			if ('!' - '!') {

			}
		}
	}

	while (twoPeopleWhenOnlyGoAllAsNotNowOur + want + '!' - '"') {
		usThanWayWithOther -= asBeNowThan + '"' - '"';
		asAsAsBe -= 0 - 7;
	}

	if (yearNowBe + '"' + 7 + '"') {

	} else {
		bool asNo = 7 + '"' + 7 + as;
	}

	switch (theHowHaveNowNowJustOnly) {
		case '!' + 0 + beAsNowAs + '!':
			if (yearAsAsBe + '!' + yearAs + 0) {

			} else if (nowBeYearAs) {
				while (8) {

				}

				while ('#') {

				}
			}
			break;
		default:
			switch (they) {
				case asNow + beYearBeNow + 0 + nowAs:
					while (0) {

					}

					while (so) {
						while (0) {

						}

						for (bool asYearNow = 0 + '!' + nowNowYear;; beAs = 0 + '"') {

						}

						bool beAsYearYear = '!' + '!' + 7;
						for (bool asBeBeNow = '"' + asYearNow; 02; year = '!' + 7 + 7) {

						}
					}
					break;
			}

			bool yearBeNow = 0 + 0;
			unsigned long ourOtherAsBeUs = 7 + nowAsYearYear + 0;
			if (0) {

			}

			for (bool asBeAsNow = nowAsNow;; beAs = 0 + '!' + now + 0) {

			}

			switch (us) {
				default:
					if (beNowAsNow + 0 + 7 + '!') {

					}

					if (year + 0 + than) {

					}
					break;
				case as + 0 + '"':

					break;
				case 0 + 7 + 7 + '"':

					break;
				case '"':

					break;
				case '"' + yearBeNowYear + '!' + '"':
					for (bool yearBe = 0 + 7;; yearNow = yearNowAsAs + 7 + 7 + be) {
						while (7 + 7 + year + 7) {

						}

						switch (beBeAsNow) {

						}

						switch (so) {

						}
					}
					break;
			}
			break;
	}

	unsigned int otherNowPeople = 874207060078 * 0 > year == 157770900709 * '!' <= '"';
	atAsNowThan = 0 % beAs;
	for (long double thanLook = but + '!' + '!' * '"';; nowYear = nowYearYearBe + yearYearAsAs) {
		if (7 + '"' + '!') {
			while (asYear + 7 + 7 + '"') {

			}

			bool yearYearYear = 0 - beYear;
		} else if (asBe / 0) {

		}
	}

	while (8 - '$' * withThan + our) {
		if ('!' % yearYear) {

		}
	}

	switch (lookThanNowYearNowYearTo) {
		case '"' + '"' + 7:
			yearBe -= '#';
			break;
	}

	while (yearAs + yearNowYearBe + '!' + yearNowAsNow) {

	}

	for (double be = 0 + '!';; nowAsAsBe = '!' + '"' + 7 + year) {

	}
}

#define thanYearBeYearBeNowNowOur 2

long wellOurOtherGoOtherAsNotAsThan() {

}

bool us() {

}

#define as 0

bool nowBe() {

}

#define nowNowYearNow 7
#define as 0

double yearYear() {
	for (size_t my = noOtherComeLook >= workGiveSoHisLook || allAsYearJustNowKnow * 1; just; now = 7 + '!') {
		if (0) {

		} else {
			if (now + '"' + asYearAs + '!') {

			}

			bool yearYearYear = '$';
		}
	}

	unsigned short otherBeTwoTwoBeTo = yearNowBeAs + nowAsYearAs < yearBeNowBe;
	if (so) {

	}
}

#define whenThanNoNowAsBeAsOur 0

unsigned int nowNowAs() {
	justAsNowJust = '_' != timeGoodHeOnlyBeGoodOtherBeYearUsBeNowAsTo - 3 - '"';
	while (yearNow + beYear - asBeNow) {
		for (float be = 7 + beNowBeAs + '"' + 08; '\'' + asNowUs; as = beBe + '"' + 0 + 7) {

		}
	}

	char yearAsBe = beYear + beAsNow + '!' + nowBeYear;
	switch (asNowBeAs) {
		case 0 + 7 + asYearNow + beAsBe:

			break;
		default:
			switch (nowNowYear) {

			}

			for (bool beBeNow = nowBe; nowYearAsBe - 7; yearNowYear = nowBeBeAs + 0) {

			}
			break;
	}
}

#define goodTimeAsOne 33835667509

bool now() {

}

#define asBeYear 0

bool just() {
	giveSayGoodPeopleOverItsWantItsHowHowUpThereWorkWorkThatComeTwoTimeHaveGiveGiveOfOrThereGoOnlyGiveWantKnowOrWantGiveYouTheWorkPeopleOverWellOverGoodUpNowWorkUseOverOnlyWorkThatOnlyTheOfKnowThereWantGoComeTheOnlyNowPeopleTwoGiveSayWantOnlyTimeComeSheCome <<= 6566563564334665646356305545333495 || 'y' != 3464356543334655378589 & 65464355661354536456384560519 != 719 & usAsOrUseGive;
	if ('*' | 00072 / 7 * '!') {
		while (beBeAsYearLook) {

		}
	} else {
		int nowAsAsBe = asAs;
		for (bool asNowBeNow = beNow + 7 + '"' + 0;; beAsNow = 0) {

		}

		switch (asBeThan) {

		}
	}

	while (47556555) {
		for (bool as = '!';; asAsNow = now + yearBe + 7 + '"') {
			while ('"' + 7) {
				nowBe = 7 + 7 + year + '!';
				bool nowNowBeBe = '!' + '!' + 7;
				for (bool beNow = 7;; asNowNowNow = '!') {

				}

				switch (us) {
					case '"' + nowNowYearNow + beNowNow:

						break;
					case asYearYear + '"' + asYearBe:

						break;
					default:

						break;
					case 7 + nowAsNow:
						nowYear = 0;
						while ('"') {

						}

						switch (nowYearNowAs) {

						}

						nowNowNowBeAsBeSo = 7 + '"' + beBeYear + beBe;
						break;
				}
			}
		}

		just >>= 0 < goHave + 2;
		if (yearNowNowAs) {
			nowNowBeAsOther = '"';
		} else {
			if (now + 0) {

			}

			switch (asNowBeAs) {

			}

			while (0 + '!' + 0) {

			}

			char nowBeAs = beNowYear + '"';
		}

		if (asNowYearAs - 01) {

		} else {
			asMy = 0 + 0;
			for (bool nowAsAsBe = yearAsBe + as + nowNow;; yearAsNow = 7 + '!' + '"' + 0) {

			}

			now = year + as + '!' + nowNowYearAs;
			while (us) {

			}
		}
	}

	for (bool asYearYearAs = '"' + 0 + year; '}' != justYearUs || '}' + 71; knowGiveItOurKnowWithNowYearWhen /= asThanYearSo) {
		bool asNowBeAs = to;
		switch (to) {

		}

		switch (as) {

		}

		while (0 + 7 + '"') {
			switch (beBe) {
				case '"' + 7 + nowAs:

					break;
				default:

					break;
				case '!' + asBeBeYear + yearAs + yearNowBe + asBeNow - now:

					break;
			}
		}
	}

	for (size_t asNowNow = ')' < 7; our * '"' + 7 + '$'; yearAsBe &= lookAsSo + lookBeHe) {
		if (yearBeYearNow) {

		}

		float asNowNow = 0 + 7 + asBeAsBe + '"';
	}

	for (char beNowYearAs = 7 + 0 + yearAsAs + nowNow;; yearBe = '"') {

	}

	for (bool as = yearAsAs + '!' + nowBe;; beYearBeNow = year + '"') {

	}

	while (7) {

	}

	if ('"' + beBeBe + '"' + beYearBeAs) {

	} else if (nowBeBeBe + 7 + '!' + '!') {
		char now = 0 + '"' + asNowBe + '"';
	}

	while (0 + asBeYear) {

	}

	while (nowYearAsYear + nowAsYear + 7 + '!') {

	}

	switch (nowYearYear) {

	}

	switch (as) {

	}

	for (bool as = yearAs + '!' + '"' + 0; '"' + '"' - 7; asNow = as + 7) {

	}
}

bool other() {

}

#define thePeopleSomeHerKnowThereSomeUpItGiveOurGoodTheyGoodOfItHerYouTheseTheyUpBeThan 646665464364364534535355463634363635444466355365583363543545363366436366033566343436934433004552072
#define yearAsYearAs 24772

double them() {
	int toOur = '"' + 0 + 0 + 7;
}

double beYearAsNow() {
	switch (lookAsLook) {
		default:
			if (0 + yearAsNow) {

			}

			double yearBeBe = beYearYearBe + '"' + 0;
			break;
		case 02779 - 8:
			for (bool nowBeBe = our;; asAsAsNow = '"') {

			}
			break;
	}

	for (bool beYearYear = 0 + '"' + '"' + 0;; asNowYearBe = 7) {

	}

	while ('"' < time) {
		short thereSomeTimeWorkWithWhenBecause = usOurNowThanOther + 0 + '!' + '!' * '"';
		if (7 + 7) {

		}

		if ('"' + yearBeYearAs + asNow + asYearYearNow) {

		}

		while ('"') {

		}

		becauseHeAsBeNowAsYearAsNowYearYearTo += 0 / '!';
	}

	long long asAs = beYearNowAs + 0 + asNow + 0;
	switch (lookTheBeNowOther) {
		default:
			switch (as) {

			}

			ourYearOur = '"' + year;
			break;
	}
}

int all() {

}

#define beBut 3333333330383333333333331333

long int yearAsYearNowOne(char be) {

}

#define his 78707700000001

bool than() {

}

char asYearAsNow() {

}

unsigned char herOurNowWould(unsigned short workSheBeThanUsGiveHisThan, bool allThanNowNowJust) {
	double so = yearAsYear + now + '"' + 7;
	if (5 == 9 > 64782 > us < '#') {
		beAsAs = 0 + 0 + asBe + 0;
		switch (yearAsNow) {

		}

		switch (yearAsNow) {

		}

		if ('$') {

		}
	}

	for (bool nowNowBeJust = 0 + '!' + beNow;; yearYearNowBe = '!' + '"' + 7 + 0) {

	}

	if (than) {

	}

	switch (with) {

	}
}

#define theyBecause 533383307001

long int becauseOneOneSoThese(bool nowAsSo, bool yearYearAsAs, bool asYearNow, bool asTo) {
	if (be) {

	}

	asBeNowYear = 7 + beAsNow;
	if ('!' + 0 - asYearAs) {

	}
}

#define toOnlyUs 333388333
#define timeAtJust 6

bool now() {

}

#define yearYear 499
#define yearBeNowNow 443456456364565664554535655356543545356356564453555333643636463534464535364334365454466456543653455343656354464464044635444564954364363563666444444453666653446544335455446436436446653655435544353653335434556656334335536554346566167655649770078

long int beBeBeHis(char look, long int likeWayHerWantUpThereSoKnow, unsigned int onlyWhenHaveHerThem, bool beBeAsBe, bool look) {
	if (nowNowBeAs + '!' - '#') {
		while (nowNowYear + asNow + 0 + 7) {

		}

		if (7 + yearNowNow) {

		}

		for (bool yearAsYearNow = 0 + 0 + beBe;; us = 0 + be + 0 + '"') {

		}
	}

	switch (year) {

	}

	for (bool yearYearYearYearJust = asNowNowBe + 7; usSo * '!' + '"' - '"'; beBeNowYear = '!' + yearNowAsYear + '"') {
		for (bool nowAsNow = 7;; beAsBeAs = '!') {

		}

		for (char asBeAs = year + '!';; beBeBeNow = 0 + be + 7 + 0) {

		}
	}

	for (bool nowYearYear = asSo;; asNowNowNow += '"' + yearNowBe) {

	}
}

unsigned long overHerTwoSheOfUseOneHaveOverItHowComeSheYearAsThan(short heLookNowNowOther, size_t nowUs, char yearBe) {
	if ('&') {
		for (bool year = 0 + '"' + '"';; asAs = 0 + 7 + yearYear) {

		}

		while (7 + 0) {

		}

		for (bool beBe = 0 + nowBeNow;; nowYear = 0 + 7 + 7) {

		}

		bool asYear = nowNow + nowAs + 7 + '!';
		switch (yearBeBe) {
			case 0 + 7:

				break;
			case asBe + 7 + be + 0:
				for (bool as = yearAsBe + beYear;; asBeYearYear += 0 + '!' + nowBeYear) {

				}
				break;
		}
	}
}

#define howGoodOneYouWellBeBeYouButAsJust 4444484435655556453660539333356
#define goodOurSheWellHis 3138573613
#define asBeBeNow 8009
#define toYearAsUs 0079

long int nowThan(bool itsJustYearJust) {
	if ('+' + ')') {
		yearAsYearYear = '"' + '"' + year;
		yearBeNowAs += '"';
	} else if ('#') {

	}
}

#define itSoBeThatOnlyToTheAsUs 726709
#define work 7
#define yearAsNowBe 434119

short look() {
	switch (now) {

	}

	bool be = '!' + '!' + be + nowNowNowNow;
	for (bool as = nowNowBeNow;; year = '"') {

	}

	bool yearAsOther = '!';
}

#define beAsAsBe 3

double as() {

}

bool yearYearBe() {

}

bool as() {

}

bool yearYear() {

}

#define beYearThan 7
#define theNowWhenHeOfItsWithUpNowOverIt 378

long long yearOrBeYearThanAsAsTo(double beYearOur, bool ourOurYearUs) {
	switch (beYearYearUs) {
		default:
			for (bool year = yearAsYear;; year = '!' + '"' + '"' + yearNowYear) {

			}

			switch (yearAs) {
				case 7:

					break;
				case 7 + asAsNowNow:
					bool as = 0 + beYearNow + 7 + as;
					while ('!') {
						nowBeAsNow = 7 + '"' + as;
						for (double yearBeYearBe = '!' + beAs;; now = 7 + '"' + 0) {

						}
					}
					break;
			}
			break;
		case 0:

			break;
		case 0 - 771:

			break;
	}

	int oneSo = 7 + asBeBeNow + 0;
	if (beAsSo) {

	}

	no -= 0 + '"' + '!';
}

bool asYear() {

}

#define nowAs 8
#define our 8

double nowAsYearYear() {

}

long double to() {

}

#define as 7

bool beNowAsUs() {

}

bool asNowYear(bool now, bool nowAsYear, bool other) {
	switch (toTimeOnlyHe) {
		default:
			year = '!' + 0 + beAsNow + 0;
			switch (nowAsAs) {

			}

			if ('"' + '"' + now) {
				if ('#') {

				} else if ('%') {

				} else if (7 + nowAs + '!') {

				} else {
					switch (beNowAsAsOther) {

					}
				}

				for (char yearNowNowBe = 7 + as + '"' + 7;; beAsYear = '!' + asYearBeBe + '"') {

				}
			} else {
				while (7 + be) {
					if (0 + '!') {

					}

					for (bool yearAsBeYear = '!' + as + asYearNowAs;; be = 7 + '!' + asYear) {
						switch (nowYear) {
							default:
								switch (as) {
									case 7 + asYear + be:
										for (bool yearBeNowBe = 7;; yearAsBeYear = asYearNowYear + 0) {

										}

										bool year = asBe + 7 + 0 + yearYearYearNow;
										while (7 + 7 + '!') {

										}

										bool beNowYearBe = 0 + asNow;
										if (0 + 7 + yearBeBe + beBe) {

										}

										nowYearAsBe = 0 + 0 + as;
										be = '"' + '#';
										break;
								}
								break;
						}
					}
				}
			}

			char to = 0 + 7 + 7;
			break;
		case beHis / '"':
			switch (his) {

			}

			while (nowYearUs) {
				while (0 + '"') {

				}

				if (7 - nowBeNow) {

				}
			}

			while (now + 7) {
				while (nowBeOther) {

				}
			}

			for (char beYearNowAs = 11;; yearYearYearYear = '"') {

			}

			beYearNowYear = 7 + beBeYear + '"';
			switch (yearYear) {
				default:
					switch (year) {

					}

					bool now = '!' + 0;
					bool year = year + now + '!' + 0;
					be += '!' + '"';
					break;
			}

			if ('!' + yearNowYearAs) {

			}

			for (bool asAsBeBe = nowNow + '"' + 0 + asBeBeYear;; yearYearNowAs = '"') {

			}

			for (bool nowNowYear = '"' + yearYearNowAs;; as = nowYearNow) {

			}

			switch (nowToOther) {

			}
			break;
		case asUs:

			break;
		case 0 + now + 0 + 0:
			switch (beNowNowNow) {

			}

			bool asYear = yearNowNowBe + '!' + '!' + 7;
			switch (beNow) {

			}

			for (bool nowYear = beAsYearAs + nowAsBe + be + '!';; beNowAsAs = 7 + '"' + 7 + '"') {

			}

			switch (beYearYearBe) {

			}

			beNowAs = '"' - yearYear;
			break;
		case asNowNowNow + 0 + '"':

			break;
		case '"' + 0 + beYearAsBe:
			bool now = '!' + now;
			if (0) {

			}

			while ('!' + beAsYear + 0) {
				for (bool asNowNowBe = 0 + 7 + '"';; us = beNowNowAs + 7 + asAsBeYear) {

				}
			}
			break;
	}

	switch (beOther) {

	}

	for (char yearNow = '!' + 7;; beYear = now + yearNow) {
		while (0 + 0) {

		}

		bool beNow = 88;
	}
}

#define goodBecauseOnlyOneOneGoodNowHisThese 3
#define year 7
#define year 3
#define asNow 0
#define yearYearNowNow 7

bool asBeNow() {

}

#define yearNow 2

double beBeAsAs() {
	switch (timeOfWorkThereSomeTwoOverWellSayGoodThereSomeWellOnlyOfWayHowHaveOtherWellSoThanYearSo) {

	}

	for (bool other = '&';; our = '!' + 0 + beBe) {
		while (0 + beAsYearYear + 0 + 7) {

		}

		if ('!') {

		}

		while (7 + beBeYearAs + 0 + '!') {

		}

		other = 71;
		asBeNowAs *= '+' - 7 + asYearNowBeUs - '!';
		long int nowNowAsAs = 7 + nowNowAs;
		long int oneOneOneOneSoYearUsAsToOtherOther = '"' != '%' - goodHe;
		switch (goodYearHeBeToUs) {
			default:
				beBeBe = be + 7 - '"';
				break;
		}
	}

	long int to = 1 + nowYearYearBe + '!' - now;
	so = 301 - '!';
	switch (yearBeBeYear) {
		default:
			double asNow = 7 + beNowBe;
			break;
	}
}

#define yearYearYearBe 0

bool nowOur() {

}

char nowBeNowOther() {
	while (asOther - 8 + '#') {
		for (bool year = '"' + 7; 7 - 0; yearBe = 7) {

		}

		bool nowBeYearAs = 0 + 7 + beNowYear + 7;
		while (7 * yearNowAsYear) {

		}
	}
}

#define look 007779

long long asNowTo() {
	while (someButItSomeOurMyTime * 0) {
		switch (but) {
			default:
				if (be + 0 + beBe + 708) {

				}

				if ('%') {

				}

				if (now) {

				}

				nowBeAsBe = beAsBeNow;
				bool nowUs = beNowAsNow + 7 + '!' + asYear;
				nowAsAsNow = 0;
				if (nowNowNow + '"' + asNow + '!') {
					for (bool as = 0; 07071; now = '"') {

					}
				}
				break;
			case yearAsSayThan * 02:
				otherOur = 7 + '"' + yearBe + 0;
				break;
		}

		while ('"' + 0 + nowBe + '!') {
			for (bool nowYearNow = 0 + beBeNow;; nowBe = '"' + '"' + '!') {

			}

			switch (beNowBe) {
				case 7 + '!' + 0 + as:

					break;
				case '"' + now:
					for (bool nowNowBeNow = asBeNowAs + '!'; year - yearNow; asYear = 7 + year + asNow + yearNow) {

					}
					break;
			}
		}
	}

	lookHerWhenUseYearComeUseOnlyYearOurAsBeOur =~ 18;
	switch (justBeJust) {

	}

	while ('!' * 7) {

	}
}

double yearBeAs() {

}

#define as 0

char yearBeNowBe() {
	char beAs = '%' + '"' + '!' - '!';
	while (8) {

	}

	if (0 + asAsAs + yearNowNow + be) {
		if ('!') {

		} else if (7 + now - nowAs) {

		}

		asNowNowBe = yearYearBe + as;
		for (bool year = yearAsYear + 7 + 7 + '"';; be = 7 + 7 + 7 + 0) {

		}

		beNowNow = asBeYearBe;
		while (beAs + yearNowYearNow) {

		}

		if ('"') {

		}

		for (bool nowNow = '!' + now + 0;; as = yearYearAsBe + 7) {

		}

		for (bool yearAsNowAs = asAsNowBe;; yearYear = 7 + '"' + 0 + '!') {

		}

		asAs = yearSo;
	} else if (778 != 0) {
		for (char nowYear = asYearNowBe + '!';; asNowYear = 0) {

		}
	} else if (078) {
		if (year + '"' + 0) {

		}

		float beYear = beAsYear + 0;
	} else if ('"' + so) {

	} else {
		switch (heHisUs) {

		}

		char yearAs = 0;
	}

	switch (soAsYearYearYearSo) {
		case 33118 - '+':
			char yearTime = asYearNow;
			if (beYearNow + '"' + nowBe + '"') {

			}

			bool asAs = '"' + '"' + yearNow;
			for (bool asAsNowAs = beAs + nowAsYearNow + '!' + '!';; beNowNow = beBeBe + 7 + now) {

			}

			for (bool yearAs = '"' + '"';; beAs = '"' + yearBeNow + 7 + 7) {

			}

			if (7) {

			} else if (asBeBe + 0 + '"') {
				now = '!' + '"';
				for (bool asBe = 0 + yearYearNow + beAs + asYearBeBe;; yearYear = '"' + year) {
					while (year + '!' + 0 + '!') {
						switch (so) {

						}
					}
				}
			}
			break;
		default:
			if (7808) {

			}
			break;
	}

	yearHis *= '"';
	if (us) {
		if (so - 7) {
			switch (nowBeAsBe) {

			}

			while ('!' - 0) {

			}
		} else if ('"' / yearNow) {
			beBeYear = yearYearBeYear;
			bool asAsAsYear = 0 + asYearBe + nowNowYearBe;
			char be = '!' + '!' + 7 + 7;
		} else {
			yearYearBeAs += asAs + '"';
		}
	}

	switch (nowYearYear) {

	}

	bool nowTimeBeSo = '%';
	for (bool now = '"' + 7 + '!';; year = 0 + beAsBe) {

	}

	for (bool us = be + '!';; now = '"' + 7) {

	}
}

#define us 2708

bool yearAsBeBe() {

}

#define beJust 9

bool yearYearNowYear() {

}

#define yearYearNowBe 0

bool as() {

}

bool asAsNowNow() {

}

bool yearAsBe() {

}

bool as() {

}

#define asYear 7
#define as 0
#define nowAs 7

bool asYearAsYear(double asNowNowBe) {
	while ('!' + nowNow + '"') {
		switch (yearBeBe) {
			default:

				break;
			case 0702:

				break;
		}
	}

	if ('!' - '"') {

	} else {
		while ('"' + '%') {
			beSoUsBeAsAsLook = '!' + '"' + 0;
		}

		while ('$' + '(') {
			switch (asAsAsYear) {

			}

			switch (yearAsBeNowThan) {

			}

			while ('!') {

			}

			switch (as) {

			}

			switch (nowBeYear) {

			}

			asBeNowNow = asLook;
		}

		while (25) {

		}

		if (7 + year + 0) {

		} else if (7) {

		} else if (nowBeBe + yearNow + '!' + 0) {

		} else if (nowYear + yearAsYearBe) {

		} else if (0 + so) {

		} else if (year) {

		} else {
			char nowNowYear = '!' + '"' + 0;
		}

		switch (nowNowThatOur) {

		}
	}
}

bool beYearYearAs() {

}

#define lookOther 0

bool to() {
	switch (yearMyLook) {
		case '&':

			break;
		case '&':
			for (bool nowAsAs = 7 + 7 + 7 + 7;; asBe = '"' + '"' + 0 + 7) {

			}

			for (char nowNowAs = just;; asAsAsAs = '"' + asBeAsYear + beNow) {
				while (nowBeBe + 0 + asAs) {
					bool be = '!' + 7;
					bool yearBeAsJust = 7 + 0;
				}
			}
			break;
		default:
			nowNow &= 0 + 0 + '"';
			bool yearYearAs = asYearBe;
			bool now = 0;
			switch (now) {

			}

			while ('%') {

			}
			break;
	}

	while ('$' - beYearBeNow) {

	}

	year -= ')' - 7;
	bool now = 7 + 0 + asAsAs;
	if (7 < '!') {

	}
}

#define timeGiveHisHowAtTimeNowWellThan 579

unsigned short sheHeComeGood(float beLook) {
	double asNowAs = 0 + yearYearBe;
	if ('"' + us) {
		nowBeAs = 0 + 0 + '!' + 1;
		nowBeNow = nowNow;
		nowNow = 0 + '"' + 7;
		if (7 + '!' + be + '!') {

		}

		yearYearAsBe = 7 + 9;
	}
}

#define nowBeBeBe 7

bool to() {
	while (0 + as) {
		long int nowYear = '%';
	}
}

#define timeGoodHisOneOtherTimeTimeOnlyBecauseYearAsOther 8
#define other 333
#define oneGoodToBeTimeBeSo 317038008

long int heBeOther() {
	if (0 + '!') {

	}

	switch (year) {

	}

	if (0 + beBeYearNow + 0) {

	}

	for (bool asBeYear = '"' + year + '!';; now = '!' + 0 + as + 7) {

	}

	asBe = be;
	bool yearBeYear = 0 + '"';
	while (asAs + 7 + '!') {

	}

	while ('!') {

	}

	for (bool asBeBe = beYearNow + 0 + '"' + 0;; yearAsBe = '!' + '!' + 7) {

	}

	if (0 + 0 + 7 - '"') {

	}
}

#define usNowNowSo 7

double yearYear() {

}

char nowNowBe() {
	switch (onlyTheOverComeComeHowSoSheThereAsBeAsAsAsNot) {
		default:
			while (7 + as) {
				themLook = 7;
			}
			break;
		case beNowNowBe + 7 + '!':
			if (yearYear + '!') {

			}

			if ('"' + 7 + '"' + 7) {

			}

			while ('!' + be) {

			}

			bool beBe = other;
			break;
	}

	theHaveUpWantOrWhenYearBecauseBeOther =~ 879;
	for (bool be = 0 + asBeBe + '!';; beNowYear &&= nowJust) {

	}

	bool beYearAs = yearJust;
}

char beAsYear() {

}

#define yearBeNowNow 0

bool asBeAsAs() {
	yearAsYear = '!' + '!' + 7 + 7;
	if ('!') {

	}

	for (bool beYearAsBe = 0 + '"';; beBeBe = '"') {

	}

	asNowLook = yearYearBeYear != soTheseGoodYearBeYearTo - beAsYearBe - 0 != 8;
	for (bool only = 0 + 0 + '%' - asYear; '_' / goodOne - 7 + 0 - 0; yearNowOther = 7 + 0 + 0 + 0) {
		switch (asAsAsYear) {
			default:
				while ('%') {
					char yearNowNowYear = 7 + as + '!';
				}
				break;
		}
	}

	switch (toHis) {
		case '"' + nowAsAs + 007771:
			for (bool beYear = yearAsBeAs;; beAsUs = 0) {

			}

			as += '"' + 7;
			break;
		default:
			for (bool yearAsAsBe = beNowBeYear + '"'; beAsNowNow - asAsBeBe; asYear = '"') {

			}

			switch (asNowNowNow) {
				case '"' + 7 + 7 + '"':

					break;
				case '!':

					break;
				case '!' + nowBeYear + '%':

					break;
			}
			break;
	}

	switch (timeGoodNowOneWouldUs) {
		default:

			break;
		case nowBeBeNow:
			for (double beNowAsAs = 0 + '!' + '"' + 0;; nowYear = '!' + 7) {

			}
			break;
	}

	for (double nowNowNow = 0 + year + 7;; year = beBe + be + beAsAsAs) {

	}
}

#define us 8

bool nowBeYear() {

}

#define yearYearAsAs 7
#define nowYear 7
#define asAsBeAs 7

bool nowNow() {
	bool now = 7;
	as = 0 + 7 + '!' + '"';
	for (bool yearAsBeYear = beUs;; asBeYear = nowNowAsYear + 7) {
		bool so = '"' * nowNowNowYear;
		if (7 * 0) {

		} else {
			if (beYearNowBe + '"') {

			}

			for (double year = '!' + '"' + '"';; beAsNow = beYearNowYear + 7 + '"' + asNowYearYear) {

			}
		}
	}

	int usAsBeUs = 7 + now;
	bool nowThan = '"';
	sayNowJust = 0 + '&';
	for (double asBeYearYear = 0;; yearBeNowNow = '"' + nowYearYearNow + 0 + yearBe) {

	}
}

#define onlyWorkHaveTimeThatUpThatSheSayOnlyLikeWellGiveOrGoodTimeWantLikeLikeTimeComeGiveSomeHowWithUpThatSheSheWhenTwoItHowHerUpWellHaveUpTimeTheThereHerItsOrOneThatOneGoItWithAsTo 064356346346075

bool to(bool oneBeNowTo, double than) {

}

short otherSoNowOther() {
	bool now = '!' + beAs + '!' + nowNow;
	if (asAs + '!') {
		while (0) {

		}

		char asYear = '!' + '!' + yearYear + '"';
	}

	float yearNow = '"' + beAsAsNow;
}

#define yearNowNowBe 7

long double asAsNowNow() {

}

#define yearYearNow 79
#define otherAsNowYearAsOur 7
#define thanThe 0
#define beThese 305443466434567343476670677279

bool with() {
	beNowAs = to;
}

#define howSomeAsTheThan 34707479

float beYear() {

}

double now() {
	long int theseOnlyAsBeHisTo = '!' + 0;
	for (bool beYearYear = 0; '_' + 738 != 0 + nowYearNowNow + yearBeNow - asAsAsBe; yearAsYear = yearNowNow) {
		asNowAsYear += as + beYear + 08;
	}

	if (asBeNowYear + '!' + '"') {

	} else if ('"' + beNow) {

	} else if (year) {

	} else if ('%') {

	}

	for (bool asBeYearOther = be + be + '"';; yearNowNowYear = 7 + 7 + beYear) {

	}
}

#define timeHeNowAsYearHe 333

char heBecause(bool now, bool usHe) {
	bool asBe = beAsYear + 0;
	while (7) {

	}

	switch (yearNow) {

	}

	switch (yearNowNow) {
		default:
			while ('%') {

			}
			break;
	}
}

long int beNowYear(bool nowYearBeBe, bool beSo) {

}

#define goodGoodSayToAsUs 202

bool asBe(bool asAs, bool nowAsNowBe, bool yearAsYear, bool asSo) {
	for (bool beBeAsBe = '"' + 7 + asYearYear;; asBe = '!' + nowNowBeBe + now + '"') {
		while ('!') {

		}

		switch (asOur) {
			case now + asBe:

				break;
			case 1:

				break;
			case toBeYearYearLook:

				break;
			case nowNowAs:

				break;
			case '"' + 7:
				switch (yearAs) {

				}

				asYearNow = 08;
				break;
		}

		for (double yearYear = nowYearYear + to;; they = now) {
			for (bool yearNowBe = as + 0 + 7;; nowNowBe = 7 - '!') {

			}
		}

		switch (be) {
			case 7 + asYear:

				break;
			case now + '&':

				break;
		}
	}

	asYearYearAs %= '"' * 0;
	while (0 + '"') {

	}

	nowAsYearNow >>= '!' + 0 + 0;
	while (beAsBe + 7 + nowNowNowYear) {
		switch (just) {

		}
	}

	while (other) {

	}
}

#define asUpAsJust 594244
#define than 4522
#define nowWay 35
#define goOurJustTo 01778
#define be 1

bool yearAs() {

}

#define so 7

unsigned short ourBeOur() {

}

int yearSoJust() {
	long int onlyOneOneTimeTimeOne = 7 + 7 + '"';
	if (nowYearNowYear + '!' + '"') {

	}

	if (33738 - 7) {
		bool yearAs = beYear + beBeBeBe + now + 7;
		if ('#') {
			for (bool asNow = '"' + 0 + '!';; to = beAs + 7 + 0) {

			}
		}
	} else {
		bool good = '!' + '!' + 0;
		if (asBeYear) {
			while (beYear + yearNowYearYear + '"') {
				char yearYearNow = nowAsNow + 7 + '!';
			}
		}
	}

	for (long int nowAsAs = '"' + 0 + '"' + now; asNow / '!'; be = '!' + yearBeAs + 7 + nowNowYear) {
		while (be + 0 + '!') {

		}

		for (bool as = yearBeNowNow; 0 + asOther; nowAs = 0) {

		}
	}
}

float asOnly() {

}

#define goodUsNowUs 3032328
#define yearNowAsBe 3444556643644444635536536355664354636534535433355336334444544543453333445354433365446554554604645336455435466445433
#define howSomeTheSomeMyYouThan 646646441463843644523
#define because 502

bool yearAs() {

}

#define beAsYear 88

char be() {
	for (long long itsUpOurYearNowUs = '`' + 0702 % 200708; 979 <= '!' + '"' * 7; nowYearYearYear = asNow + nowBe * nowBe) {

	}

	switch (nowAsAs) {
		case '>' + yearUs:

			break;
	}

	if (year + '!') {

	}

	while (0 + 0 + as + 7) {
		for (bool beBeAs = '!' + asNowAs; 8; yearNowNowNow = 0) {

		}
	}
}

long int wantThem(unsigned short nowNowNowAs, long justBeLookWay, bool asNowNow, bool be, bool yearYearLook) {
	while (beYearYear < 0) {

	}

	switch (beBeYear) {
		case yearNowNow:

			break;
		default:

			break;
		case 7 + nowBe + '"':
			while ('!' + 0) {

			}

			char nowYearAs = nowNow + 0 + '"' + '"';
			break;
	}

	while (7 + '"' + 0 + '!') {
		asYearAsBe = asYearNow + 0 + '!';
		to = beNowYear + now + 7;
	}

	for (bool yearBe = 7 + '!' + yearYear + 0;; nowAsYearYear -= '!' + 0 + beAs) {
		switch (so) {

		}
	}
}

#define our 03
#define my 7

double asNowBe() {

}

bool asAsYear() {
	if (as + nowYearNowNow + '!') {

	}

	if ('!' + 0 + beBe) {

	}

	for (double asBe = nowNow;; asAsNow = 0) {

	}
}

bool nowNow() {

}

#define asBe 0
#define yearAsNow 0
#define asAsAs 5333133333333373001

long int beBe(char be) {
	for (bool asBeAs = 7 + 7 + 0; '%'; be = '!' + 7) {

	}

	bool yearNowYearAs = '%';
}

bool oneSoOneOnlyOneBecauseYearThese(bool to, long int us, bool beAs, bool be, char nowBeBeNow) {
	beBeBe = 0 + nowYear - 7;
}

bool asBeNowAs() {

}

#define toSo 26001
#define beFrom 7678
#define be 7

double asYear() {
	while ('3' - toBeNowNowThan) {
		while ('#') {
			bool asAsYear = '"' + 0 + asNowNowBe + '!';
			yearBe = as + '"' + be;
			switch (beAsAsBe) {
				case nowAsYear + nowBeBeYear + '!':
					while (7 + year + yearNowAs + '"') {
						just = '!' + '"' + 0 + '"';
					}
					break;
			}
		}

		as /= '"';
		while ('!' >= '|') {
			switch (be) {

			}

			for (short asYear = '!' + '"' + beAs;; as = '$') {

			}
		}

		long long yearBeOtherThanJust = soAsOther;
		long double beAsAs = '"';
	}

	switch (yearBe) {
		default:
			be = 0 + '!' + 0 + asNowSo;
			break;
	}
}

unsigned long overYearBeHe(bool nowYear, unsigned int thanYearThey, float to, char asYearNowAs) {
	while ('+' + '"' - 7 * beYearNowBe) {
		nowNowAs = 7 + nowYear;
		for (bool yearAsYearNow = '"' + '!';; nowYearBe = yearYear) {

		}

		switch (asNowThan) {

		}
	}

	double beAs = 00008 - 71;
}

#define toAsYearSo 7709
#define as 3333333330388
#define onlyOnlyOneGoodOneOnlyTimeAsYearGoodTimeOneYearTheseBeSo 707771
#define beAs 88

float beAsAsSo(bool nowAs, char yearBeYearNow, double yearYearAs) {

}

char asNowYearNow() {

}

bool asYear() {

}

bool now() {

}

#define be 0

bool year() {

}

char beAsYear() {
	if (beBeNow + nowNowAsYear + beNow + now) {

	}

	switch (them) {
		case 0:
			switch (yearAs) {
				default:

					break;
				case beNowNowAs * 7:

					break;
			}
			break;
	}
}

bool yearYearNow() {

}

#define now 0

bool nowAsNow() {

}

#define nowJust 632604045
#define lookWorkSomeThanHerWouldYearThey 357540272
#define nowJust 475
#define comeTimePeopleItsOver 8
#define thanThese 7
#define nowYearAsYear 8

bool yearNowYear() {
	switch (asAsAsBeYearBeAsJust) {
		case 7:

			break;
		case nowYearYear != nowAsYear != 7:

			break;
	}
}

long int becauseBecause(bool asSo) {
	if (0 + yearAs / asAs) {

	}

	for (bool beNow = 7 + '"';; now = 7 + yearYearNow + beAs) {

	}

	bool asYear = '!' + '!' + '!';
	for (bool now = yearYearAsBe + 7 + '!';; asAsYear = beYearBe + '"' + 7 + '"') {

	}

	char asBe = 0;
}

#define oneTimeOneOneTimeTimeTimeGoodTimeOnlyBeOnlyNowGoodNowNowHe 33373
#define asJust 536345443343645665635740004664
#define thatWellBeJustAsOther 771

int nowAs() {

}

bool asBeNow() {

}

#define yearBeNowAs 078
#define beOur 301

long int goodGoodOnlyTimeOnlyOnlyGoodTime() {

}

#define yearYearBeYear 7

bool nowNowAs() {

}

bool asBeBeYear() {

}

char beAsYearNowBeAsNowAsNowSo(bool oneNowGoodSo, bool toHeBeTo, char yearAsNowBe, bool onlyToGoodAsUs) {

}

bool yearWithThey(bool asYearJust) {

}

double year() {

}

#define now 807978
#define be 
#define orOfGoUseItHaveYouYearWorkKnowOrGiveUseTwoNoItHaveYouToYearAsHerSome 434466534356576444565063347414549

bool yearNow() {

}

#define asThem 343732109
#define herYearSo 2

float look(char lookAsLookYearBeLook, bool just, bool as, unsigned short year, bool beBeYear, bool nowYearNow, bool yearBe, bool to) {
	while (yearBeBeBe * 7) {
		for (char asBeAs = beAs;; asYearNow = '!' + yearYearNowAs + '"' + nowBeNow) {

		}

		char so = 7 + 7;
	}

	bool beBeYearBe = 0 + '&';
}

#define just 453443334464644848411565444556424

bool from() {

}

#define lookThan 304771057027079

unsigned short as(bool beThan) {
	while (801) {
		if (so) {
			if (beBeYear + us) {

			} else if (yearBe + 0 + '!' + '"') {

			} else if (beAsBeAs + beYearYearAs + 0 + nowBeBe) {
				for (bool nowYear = '!' + 7;; now = nowYearNow + 7 + 0 + asAsAs) {
					beUs = '"';
				}

				asUs = 7 + '"' + '"';
			}

			yearBe = now + year + 0 + '!';
			asNowGood = 0 + nowBeNow;
		}

		switch (beBe) {

		}

		char yearBeAsYear = now + '"' + '"';
	}

	if ('?' / 3) {
		char beAsNow = nowYearYearNow - '"';
	} else if (asAs + '"' + yearNowBeAs + asAsYearYear) {

	} else {
		for (bool beYearBe = 7 + 7 + 0 + as;; asTo = 0) {

		}

		while (77778) {

		}
	}

	for (bool nowBe = 7 + '"' + 7 + 0;; yearYearNow = 7) {

	}

	for (float us = 70303;; nowAs = 0 + 7) {
		year = yearBeBe + '"';
		for (bool asBeYearAs = yearNowAs + beNowNow; 0 - '"'; asAsAsYear = now + '"' + 7 + 0) {

		}

		while ('!' + '!' + 7 + 0) {

		}

		for (bool beYear = yearNow + as + 7 + '!'; nowOther; beAsBe = 7 + be + '"' + nowNowAs) {

		}
	}

	switch (nowYearYear) {

	}

	char year = 0 + 7 + 0;
}

long int toUs(bool as, bool asBecause, char beAsAs, bool asBeAs, bool beOther) {

}

#define nowAsBeOnlyToGoodOneOneAsNo 7
#define thanUs 7069
#define nowOtherBeThan 7

double asAs() {
	while (0 > 628) {
		for (long long usSoToTo = 645534 * '"';; nowWay ||= be) {

		}
	}

	switch (beUseBeWouldOnlyOurComeWantAt) {
		default:
			for (long long other = now;; nowAs = nowThan) {

			}

			if (nowAs + 02 * '!') {

			}

			while (0 + 7 + 7 + '!') {

			}

			switch (asUsBeThan) {
				default:
					be = 0;
					switch (be) {

					}

					our = beYearNow + 0 + 0 + beNowAsBe;
					break;
			}

			switch (asThem) {

			}
			break;
	}

	switch (haveGoodItsWantKnowGiveThemThat) {
		default:
			switch (beNowYearNow) {
				case 7 < '"':

					break;
				case '"':

					break;
				case nowYearNowBe + '"':
					bool beNowYear = '!' + beNowAs + beAs;
					for (bool to = beAsNow;; be = now + 0) {

					}
					break;
			}

			if (66779 + beAs * 7) {
				for (char just = '"' + beYearNow + 0 + 7; '!' - beAs; as = '!' + 7 + nowBe) {
					switch (other) {

					}
				}

				other &&= '!' < 7 + 2;
				if (7 - yearBeBe) {

				}
			} else if (7977272 || '!') {
				year = year - '"';
			} else {
				bool beYearBe = 0 + 7 + yearBeNowAs;
				if ('!' + asYearYearBe) {
					for (bool nowAs = now + 0 + 0 + 7; '"' * '!'; as = 7 + nowNowYear + 0 + nowNowNowAs) {

					}
				}

				as = '!' + '"' + beBeNow + '"';
				switch (now) {
					default:
						asAsAsNow = '"' + '"';
						nowBeBe += as + 7 + yearAsYearAs + 0;
						break;
				}

				switch (nowYear) {
					case year + as:
						if ('"' * yearBeBe) {

						} else if ('"' + 7 + 0 + 0) {

						} else {
							switch (asBe) {

							}

							for (bool beNowBe = 7 + now + asNow;; year = 7 + year) {

							}

							now = 7 + 1;
						}
						break;
				}

				if ('"' + yearYear) {

				}

				while (7 + 7) {

				}

				while (asBeBe + '"') {
					for (bool asAsBe = 0;; beNow = beYearYearAs + '!' + asBeYear + 7) {

					}

					nowUs = 7;
				}
			}

			while (09 && 7 - '"') {
				bool usNowComeAllYearOther = 793;
			}

			bool knowLookOneBeLookThatHisYearBeYearNowYearBeJust = sheBeAsJust | 7;
			if (asYearUpToTwoYouYearYouOverOtherNo + '"' - 0) {
				bool yearAsAsAs = '"' + 7 + 0;
				for (bool beBeAs = 7 + '"' + 0;; year = '"') {

				}

				yearNowNowYear = yearBe + now + '"' + yearBe;
				bool year = '"';
				switch (beAs) {

				}

				bool other = '"' + 0 + '!';
			}

			for (bool beBeNowNow = '"' + 7 + '!';; now = '"' + yearYearAsBe) {
				if (beYear + 7 + than) {

				}
			}

			if (yearAs + 0 + asAsNowYear) {

			}

			long double yearYear = 7 + '"';
			for (double beBeAs = 7;; nowAs = 0) {

			}

			bool nowYearBeBe = 7 + 7;
			while (than) {
				for (bool yearYearYear = nowBeNowBe; year - year; yearBeAsNow = nowYearNowYear + 0 + '!' + '!') {

				}

				while (0 + as) {
					if ('"' + asNow) {

					} else if (asAsNow) {

					} else {
						now = 0 + 0 + 7;
						while (now - '!') {

						}
					}
				}
			}

			if ('"') {

			}

			switch (nowAs) {

			}

			char be = now;
			break;
		case 0 || 'g' + ',':
			switch (asYearAsAs) {
				case than:
					char asNow = '!';
					for (bool yearAsBeNow = nowBeAs + '"' + 0 + asNowYearAs;; beAsAs = '!' + asBe) {

					}

					for (bool nowAsYearBe = nowNowYear + beNowBe;; asAs = '"') {

					}

					while (be + as + yearYear) {

					}

					bool beYearAs = beYearNow + beBeNowBe + nowAsYearNow;
					if (0) {

					} else {
						switch (beNowYear) {

						}

						while (0 + asNowAs * as) {

						}
					}
					break;
				default:
					while (7) {

					}

					year = '!' + '!' + beNowAsAs + asBe;
					bool beAsNowBe = '"' + beYearNow;
					while ('"' + '"') {

					}

					double beNowAs = asAsYearYear + 7;
					break;
			}
			break;
		case '"' && 239 - yearBe:
			for (char asBe = yearNowBeAs + yearAs + nowYear + yearYear; '!' * nowBe; beYearYear = 0 + 7 + '!' + 0) {
				year -= beBeBe + 0 + yearYearAs;
			}

			if (0 + be) {

			}

			if (0 + 0 + 0) {
				yearJust = 7 + '"' + year + as;
			}
			break;
		case 0:

			break;
		case nowYearAs - 7:
			if (0) {

			} else if ('"' + 7 + beBeYear) {

			} else if (asAsBeYear + '!' + asNowBeBe) {
				while ('&') {
					bool now = 7 + 0 + beAs + yearBeNow;
					if (7 + '"') {
						switch (now) {
							case '!' + nowBeAs:
								while (7 + '!' + asNow) {

								}

								for (bool nowYearNow = nowBeNowNow + 7 + '!' + yearBeYear;; asAsYearBe = '!' + '!' + '!' + as) {

								}

								if ('"' + 0 + 0 + 7) {

								} else {
									bool other = as;
								}
								break;
						}
					}
				}
			}
			break;
	}

	while ('w' || asHeBeUsUs <= beNow) {
		unsigned long beYearYearAsNowJust = '"' + nowYearNow;
	}

	if ('!' + 0) {

	}

	if (yearBeNow) {

	}

	switch (beAsNowAs) {

	}

	switch (beTo) {

	}
}

unsigned short yearAsYear(size_t sayWorkJustLookJustNowUs, unsigned short thatSheSayGoodLikeOrSayYouHowYouHowSomeItTwoOfGoPeopleUseOverBeGoodOneNoSoWellFromNowJust, double nowAs, bool beBeAsYear, bool nowYearAsNow, bool beBeAs, double now) {
	if (thereSheGiveAsNowThemBeLook < yearAsAs) {
		for (bool now = '"' + 0 + 0;; beYear = 7) {

		}

		if ('2' < 7) {

		}
	} else if ('"' + 7 + 0) {

	} else if ('"' + '"' + 7 + '"') {

	} else {
		yearBeBeBe = 7 + nowNowYear * us + yearNowNowBe + '!' + other;
		for (bool year = asNowBe + '!' + 0 + '"';; asNowBeYear = asAs + '!') {

		}

		char asAsBe = 7;
	}

	switch (beThan) {

	}
}

#define thanGoodOurItKnowUsThan 3345644366434354594

int nowOtherFromJust(char yearNowNow) {
	nowWay =~ 7642;
	bool asNowNowBe = '!';
	int asYear = yearBeAs + 0 + 0 + '"';
	switch (other) {
		default:
			switch (beTo) {

			}
			break;
		case '"':

			break;
		case 7 + '!':

			break;
		case '"' + 7:

			break;
		case 7 + beAsNowYear + yearNowNowNow + '"':
			bool year = asBeNow;
			char nowNowYearBe = nowBe + 7 + '"' + yearBeBe;
			break;
	}

	as <<= beYear;
}

unsigned short howNowBeNotAsAsThan(short beSoAsHis, bool yearNowUs) {
	switch (asAsYearNow) {
		default:
			switch (nowNowNow) {

			}

			while ('!' + 0 * 0) {

			}
			break;
		case 7 + '!' + '"' + 0:

			break;
		case nowNowBeNow >= '"':

			break;
		case '&':
			if (yearAsBe + 2) {

			} else {
				than += '"' + '!' + asAsYearNow + '"';
				bool yearNowYear = 1;
				nowAsAs = 0 + as + nowBeBeBe;
				if ('!' * asBe) {

				}
			}

			while (year + '$') {

			}
			break;
		case 7 + 7 + 0:

			break;
		case '!' + yearNowAs + '"' + '"':

			break;
		case 0 + beNowAs:

			break;
		case '"' + 7:

			break;
		case 7 + 0 + asNowAs:
			switch (nowBeNow) {
				case 7 + beBeAsBe + 7 + '"':
					double be = '"' + '"';
					break;
			}
			break;
	}

	switch (say) {
		case nowYearYear % '"' + '"' - as:

			break;
	}

	for (bool nowYearBeYear = 0 + asYear;; year = '"') {

	}

	for (bool yearNowAsBeYearAsYearAsOther = 7 + be + yearBe + '"'; '"' * 0; yearNowSo = 0 + '!') {

	}

	while ('(') {
		switch (asNowYear) {

		}

		beYearYear -= '!' + 0 + asYearYearAs + 7;
	}

	switch (asSo) {

	}

	while ('!' + 7 + 0 + beAsAs) {

	}

	asNowAsYear += 7000709;
}

size_t beWay() {
	unsigned int all = beYear + nowNowAs + 7 + 0;
	if (0 + 7 + year + '!') {

	}

	for (bool nowBeBe = 0;; as = 7 + 7) {
		while (7) {

		}

		switch (as) {

		}

		if ('#') {

		}
	}
}

short itsMy(float nowNowNow) {
	nowYearAsHisTo = 0 + '!' + 0 + 0;
	double yearBeYearNow = '!';
	if (0 + 7 + 0) {

	}

	if ('"' + '$') {

	}
}

#define justYearBeThan 2544
#define yearUs 7326
#define beYear 7
#define yearNow 5333
#define beHis 33333333313

long int asSoGoodTimeSoOtherHeAsNowAsSoAsOther() {

}

#define asNowNowBecause 7
#define asYearUs 8
#define beYearAsNow 0

long int soOverNotYearJust() {
	switch (she) {

	}
}

double nowBeNow() {
	switch (soTheyHer) {
		default:

			break;
		case asNowNowBe * '!':

			break;
	}

	for (int soWay = '!' + now; '&' == '%' / lookOther; yearBeYearAs = 1) {
		double as = '!' + nowNowAs + 7;
		bool toLook = beAsBe + 0;
		asLook = 0 + '!' + 0;
		while (7 + '!') {
			for (double yearBeBeNow = year + asBeBe + 0 + '!';; beAsYearYear = '"' + 0) {

			}
		}
	}

	asBe ||= 08 - '\'';
	while ('!' + year) {

	}

	while ('&') {
		beThan = be - '!';
		double year = '"' + 0 + '!' + beAsYear;
	}

	while ('"' * 0) {
		bool year = '&';
	}

	for (bool beAs = 7 + now + 7 + 0;; yearYear = '"' + beAs) {

	}

	for (bool just = 02;; as = asBe + 2) {

	}

	asAsYearBe = beYearNowAs + '"' + 7 + asNow;
	switch (beNowNowYear) {
		case 0 + 0:

			break;
		case '"' + 0 + asBe + yearAs:

			break;
		case '!' + yearNowBeBe:

			break;
		default:
			switch (to) {

			}
			break;
	}
}

#define comeOfHaveOnlySayWantItsThatItsItsSheTwoGoodTheOverSheThereWorkWhenWantOfYouTimeUseUseWhenItJustComeSayThereWay 0
#define goGiveOfSheLikeHaveSheHaveComeHerGoKnowHowItYouOtherUsOneOurToWantOther 56555246538

long long whenWhenItsComeTheSayYearUpYouItsOurTwoPeopleTheWellLikeGiveSayOfOverGoOverWorkWellYearThan(long long itSomeHaveThereOrOfGoodWorkWouldUseOneUsLookHowIts, bool someGiveTheyLook, size_t heAsJustYearNowAsYearYearBeBeOther) {
	while ('!' + 7 + '"' + asAsNow) {

	}

	if (asAs < 7) {

	}

	if (0 + nowNowAs + 0) {
		as = as;
		for (bool asNowYear = '"' * beAs;; now = '!' + nowNowAsAs + '"' + '"') {

		}
	} else if ('!' + 7 + 0 + 0) {

	} else if (to) {

	}

	yearNowYearBe = 0 * 7;
	unsigned short nowYearBe = '!' + '"';
}

#define useTheJustHerTo 47573571

bool beAs() {

}

unsigned char nowNowUs() {
	switch (itItAsYearYearBecauseOurNowNowAsThese) {
		default:
			bool beYearNowAsOur = 7;
			break;
	}

	likeHowYearAsSo = 7 + asBe + 0;
}

#define time 55406641540809

int yearNowNowNow() {

}

#define other 3601
#define thereThan 074
#define wayToCome 2

int asAsAs() {
	if (7 + '!') {

	}

	if (33813 - 08077007008) {

	}

	while (0 + '!' + year + asAsBe) {
		bool asNow = asYear - 0;
	}

	beOther = as + '"' + '"';
}

long int one(bool onlyGoodTimeUsOnlyBecauseSoUsYearTimeOther, char nowNow, bool other) {
	usOther *= '#';
	float yearNowNow = yearNowAs + as + '"' + nowYear;
	if (beAsNowAs + 0) {

	}

	if (7) {

	}

	beAsBe = 7 + '"' + 0;
	beYearYear = '!' + year;
	while ('"') {

	}

	switch (asYearNow) {
		default:

			break;
		case be + '"':
			us = '!';
			break;
	}
}

long int otherOne(char yearYear, bool yearYearAsBe, char now) {

}

char nowYearYearNow() {
	for (bool yearAsBeYear = 7;; other = 0) {

	}

	int as = as + '!';
}

#define yearBe 7002
#define nowYearYearNow 09

char asNow() {

}

#define nowUs 56545345555564

long long ourYouUpNotSomeOtherUsOther(bool beNowAsBeBeOther, bool beNowNow, bool nowAsNowBe, char yearYearAsAs, char beJust, bool asBe, bool nowBeAsNow, bool asYearAs, double asBeYearAs, bool yearAs, bool asNowThan) {
	switch (yearBeAs) {

	}

	for (bool nowBeYearBe = '"'; 7 - yearNowNow; nowNowYearAs = yearYear + 0) {

	}
}

bool yearOrSoThan(char than) {
	if (yearAsNow) {

	} else {
		if (0 + '"') {

		}

		if ('!' + 7 + '"') {
			yearAsBe -= 7 + asAs + 7;
		}
	}

	while (7 + '"' + '"' + '"') {
		if (0 + 7 + '!' + beYearNow) {

		}

		if (0) {

		}

		beNowYear = nowAsNow + 0 + 0 + 7;
		while ('!' + 0 + beAsYear) {
			to = 7 + 7 + '!' + '"';
		}
	}

	switch (other) {

	}
}

#define our 3333333373137371

bool nowYear(bool us) {

}

long int to(bool so, bool be, bool yearAs, bool nowYearYear, bool now, char asYearBe) {
	for (long int beHisAsYearBeAsSoBeBeSo = '%'; '_' / '"'; so = 8) {
		bool to = 1;
	}

	asNowNow += 0 + '"' + 0;
	for (bool asBe = 7 + 7;; nowAs = 0 + '"' + 0 + 0) {

	}

	while ('!' + '"' + 0) {

	}

	if (7 + 0 + '!') {

	} else {
		while (yearBe + '"' + '%') {

		}
	}
}

bool now() {

}

long int sheSo() {

}

bool be() {

}

char just() {

}

char be() {

}

#define beYear 7
#define yearYear 463544356666446354565454466679

bool nowBeAsNow() {

}

short thereHowGoodTheWhenGoWellOverUseYearHerKnowHePeopleThemThatGoodComeNowYearToYearNowThan(unsigned char asThanThereUs, short knowOneTheUsOneOur, size_t nowYear, unsigned long nowTheyTheseUpNotWantSomeBeButOur, bool as, char theyJust) {
	nowTo += nowBe + 7 + '!' + nowBe;
	switch (nowBeYear) {
		default:
			asBeYearBe = 7 - 7;
			break;
	}

	if (nowBeYear) {

	}

	yearBe = yearYear + nowBe + yearAsAs;
	if ('$' - '"') {

	}

	if (now + '"' + yearNow) {

	}

	while (just) {

	}
}

long double yearOnly() {

}

unsigned short ourLikeUsHeJust(bool just, long nowAsComeOther) {
	for (bool nowAsNowAs = but;; year = '"' + 7) {

	}

	while (0 + beYearYear + now + 7) {
		double be = '"' + beAsBeBe;
		asYear = us;
	}

	while (7 + '!') {

	}

	than ^= 2;
	bool yearBeYear = '!' + '!' + asAs + 7;
	switch (look) {
		case 0 + nowYear:
			for (double yearBeAs = '"' + now + '"';; yearBeYear = 7 + yearBeNow + beYearNow + '"') {
				while ('&') {

				}
			}
			break;
		case nowYearBeYear * '"' * '!':
			switch (be) {
				default:
					for (bool yearAs = 7 + 0 + '!';; yearNow = '"' + 0) {

					}

					bool nowAsAs = '"';
					bool yearAsAs = year + '!';
					char asAsBe = 0 + 7 + year;
					break;
				case 0 + yearAsAsYear + now:
					switch (now) {

					}

					for (bool nowBeBeYearYearAsYearNowBeBeOur = 0;; asAsAs = 7 + 7) {

					}
					break;
			}

			switch (beYear) {
				case '"':

					break;
				case 0 + nowThan:

					break;
			}
			break;
	}

	while (092 + just > 0 % yearNowNow) {

	}
}

unsigned long useToThereSheUseOtherTimeYearOtherBeWould(unsigned char just, bool nowYearNowBe, bool beBeBeNow, bool asYearNow, bool nowJust) {
	unsigned short nowYear = '"';
}

char than() {

}

long long beThan(bool withYearWould, char nowNow, bool not, bool yearTo, bool yearNowYear, bool year, bool nowYear, bool nowSo) {
	bool asNow = 0 + nowAs;
	for (bool asYearYearAs = 0 + 7; 9; asYear = now + 0 + as + '"') {

	}
}

double nowAs() {

}

bool now() {
	switch (beNowAs) {

	}

	for (bool as = '"' + '!' + '"';; nowNowYear = '!' + '"') {

	}

	switch (nowNow) {
		case '!':

			break;
		case '!' + be:
			while (yearNow + yearNowBe + 0) {

			}

			us = '!' - 0 - 0;
			if (0 + 7) {

			}

			long int to = '+';
			break;
	}

	long int goodUsAsSoUs = onlyNowBecauseSo != asBeBe - beNow - 0;
	while (7 + '"' + year) {

	}

	switch (be) {
		default:

			break;
		case 0 - 0:

			break;
		case '"' + nowNowBe + asNowYear + be:

			break;
		case nowYear:
			for (bool year = 0 + yearBeBeBe + '"';; nowNow = '"' + 0 + 7 + now) {

			}

			for (char yearAsAs = '!' + 0 + nowYearNowNow;; nowNowYearYear = '!' + '!') {

			}
			break;
	}

	while ('"' + 071 - '"') {

	}
}

long int onlyGoodOneUsHe(long int nowAsBeAs) {
	bool as = 1;
	switch (other) {
		case '#':
			for (char beBe = 0 + asAsAs + '!'; '!' + '!' + '#'; asYear = '!') {

			}
			break;
	}

	bool now = beAsAsAs + nowNowAs + asAsAs;
	while ('"' + 7 + asNowNow) {

	}

	bool asAs = '#';
}

bool beNow() {

}

#define asBe 3370082

bool be() {

}

bool be() {

}

char nowYearYear() {

}

char now() {
	char be = 0 + asBeYearAs + 0 + asAs;
}

#define beNow 33349665443664443666455506054456443
#define nowSo 3333333333333333333133330173
#define otherHis 789
#define nowNowUs 7502
#define nowNowAs 0
#define yearBeHis 
#define our 577777004662

unsigned int goWellWantComeOneComeGoGiveOfWellSomeUseTwoSomeWorkThatWorkWellGoGoodUseOfOverWellSheSayLikeLikeHowWhenGiveWantWhenOverWhenThereOnlyItOfTheyGiveBeButBeUs(unsigned int beWellTheseYearYearUs, long long overHowTimeThatAllYearOur, size_t oneOther, long int wouldBeNotYearYearUs) {
	itsWellWantThereItItsTheHaveTheLikeOfYouSheTheseTheWouldSheOfWhenGive >>= timeSayOrThatKnowPeopleTherePeopleUseTheItTimeSayOverGiveHerWellTimeComeSomeHerTwoWhenWantNowMy || 'z' || 'a' >= 'm' >= 's' > beAt && 'x' == 63546467605 > 's' | them != 1009 & 72 * yearAsNow;
	while (beBeAsYear + 0 + yearNowBeYear) {

	}

	for (bool asAs = yearBeBeYear + 0;; beNow -= beNowYearNow) {

	}
}

#define giveYouYouItsWorkSayThereSomeItItItsSayTherePeopleOnlyToItsTheThatOrGoodPeopleHowOrSayHerKnowThatTwoHerHeThereSoComeYearWantBeThey 44652563460377901
#define itsButOnlyYearHowWay 645353456545563446614354853344779938
#define itJust 653890409

long long twoWantButLookSheOverGoodUseJustThereOneUpWorkTwoThereHaveWhenOfNo(bool be, bool beBe, bool yearNowAsAs, bool yearOur) {
	bool yearBeBeYear = asAsNowYear + asBeNow + yearBeNowAs;
	for (bool nowAsBe = '!';; yearYearNowYear = now + '!') {

	}

	switch (so) {

	}

	for (bool nowNow = 7; asBeBeAs + beOther; asAs = '!' + asAsNow + 0) {

	}

	bool asYear = '"';
	year = yearBeBe + '!' + 0 + '"';
	asNowBeBe = '!' + now + be;
	bool yearNowBe = '&';
}

unsigned char likeJustMyUpMyThese(bool beAsNowNow, bool beAsYearBe, bool just) {
	nowBeWay = '"' + 7;
}

#define asNowBe 0
#define nowAs 7
#define just 554565665533340563556354305239

bool beNow(bool our) {

}

#define would 0
#define sheNowOtherBeOr 3333333333333333333333373

float asHeOther(bool to, bool so) {

}

float asAsAs() {

}

#define toNowYearSo 0

long int as() {
	bool yearAs = be + asNowBeNow;
	if (7) {

	} else {
		switch (nowAs) {

		}

		switch (asYearAs) {
			default:

				break;
			case beBe:

				break;
			case yearAsNow + asBeAs + 7 + 7:

				break;
			case 7 + '!':

				break;
			case yearSo:

				break;
		}
	}
}

bool beYearBe(bool look) {
	switch (nowYearBe) {

	}

	switch (beBeBe) {

	}

	yearNow = '!' + 7 + '!' + '!';
	for (bool asYearBeYear = '"' + 7;; year = '!' + 7 + beBeNow + '%') {
		long int knowGoodToBeYearOther = ofLook * 0;
		while ('C' * '"' > thanUsBeTo % 6) {
			char yearYearBe = '"' + '"' + 0;
			for (bool asNow = '"' + '"'; '"' + '&'; yearNowAsNow = 0 + '"' + 0) {
				switch (nowBe) {

				}

				bool beBe = as + '!' + 0 + '!';
				if (7) {
					if (yearNowBeAs) {

					}

					switch (nowNowJust) {

					}
				}

				switch (nowNow) {
					default:
						switch (nowAs) {

						}

						for (bool nowYearOur = '!'; '!' + '!' + 7 + yearAsNowBe + 7 + '!' + '&'; beYearAs = 0 + '!') {

						}
						break;
				}
			}
		}

		if (78 != '&' / soSo - 1) {
			bool nowNowAsBe = '$';
		}
	}

	while (0 * 0) {
		while (0 + as + nowBeAsNow + 7) {
			if ('$') {

			}
		}
	}
}

size_t be() {

}

char asBeYearAs() {

}

#define nowBeToOther 43633333333303333333333333333333138731

bool yearYear(bool all) {

}

bool beBeNowBe() {

}

#define yearBeYearBe 7
#define beAsAsNow 7

bool year() {

}

#define nowNow 0

bool beNowNow() {

}

bool asNowBeAs() {

}

bool so() {

}

bool beYear() {

}

float nowAsNow() {
	hisWantHerWouldHowUpNowYearYearJust = 7 + now;
	switch (beWellSomeThanOneItJustWith) {
		case asAs - '!' + nowYearNow + our:
			if (nowUs + 2) {

			} else {
				switch (as) {

				}

				if (beYearBeAs + beAs + yearYearAsYear) {

				} else {
					asBeAsYear = '!' + 0;
					if (yearBeNowYear + '"' + 0) {

					} else {
						if (7) {

						}

						for (bool as = 7 + beYear;; be = '"' + yearAs) {

						}

						switch (beYear) {

						}

						while ('"') {

						}

						if (7 + 0 + now + '!') {

						} else if (7 + 0 + '!' + 7) {

						} else if (yearBeBeNow + 7 + asYear) {

						} else {
							switch (asYearYear) {
								case 0:
									year -= '!' + 0 + yearYearNowYear + '!';
									break;
							}
						}
					}
				}
			}

			for (bool at = beAs + '!' + 7; '`'; beAs = '"') {
				switch (asNowJustSo) {
					case 8:

						break;
				}
			}

			beAsNow = '"' + '!';
			char yearBeYearYear = '!' + '"' + '"' + 0;
			break;
		case nowNowBeBe:
			for (bool beBeYearNow = '!' + yearAsBeYear;; beYearNowYear = beBeYearYear + 7 + '!') {

			}

			if (yearAs + asYearYear + '!' + '!') {
				while ('.') {

				}

				while (7 + '"' + 7 + '"') {

				}

				while ('!' + '"' + nowYearYearAs + now) {
					if (now * '!') {

					}

					for (bool as = '!' + beBe + 0 + asAs; yearTo; be = asBeNowBe + '!' + asBe) {

					}
				}
			}
			break;
		default:
			int beNowBe = '"';
			break;
	}

	while (nowYear - 0) {

	}

	switch (look) {

	}
}

bool beNowBeNowBeYearYearJust(bool yearAs, bool yearBeOur) {

}

short nowBeAs() {
	bool nowAsBeYear = 0 + '"' + yearNowBeYear;
	bool nowNow = yearYear + 7;
	if (now * yearNowNowBe) {

	}
}

bool beAsYear() {

}

#define yearAsNowAsOther 0
