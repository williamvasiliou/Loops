#include <UUUUUUUUUU/0/662.h>

#define yearOther 0708

char yearBeAs() {

}

#define justYearAsAsAsNowYearSo 9

bool nowAsBe() {

}

bool nowBe() {

}

bool our() {
	asLook = now <= '!';
}

long int peopleItsWantOfBeAsUsYearYearYearYearJust(char whenToYearNowBecauseHaveOther, bool yearYearYearNow, bool now, bool be, bool yearBe, double asBeBeBe) {
	bool butAsWantYearOnly = 0 + asAsNowNow + 7 + yearAsNow;
	beAsNow ||= 0 - beBeAsBe + yearNow + 8;
	double itSoJust = '&' <= '$';
	switch (asYearBe) {
		case '"' + '!' + 0 + '"':

			break;
		case now + '!' + 0 + nowAsBe:

			break;
		case 7 + beAsAsNow:
			if ('!' + '!') {

			}

			nowBeBeAs = 7 + 7 + as + beAsNowNow;
			switch (beNowAsBe) {

			}

			asNow = '"' + '"';
			nowNowYearAs = asAs + asBe + 0 + '"';
			bool nowYear = nowNowNowYear + yearYearNow + '"' + 7;
			bool nowYearYear = 7;
			while (asAsAsAs + '"') {
				bool be = 0 + 0 + 7 + nowBe;
				be = 7 + '!' + 0;
				for (bool nowNow = '"';; beBeNow = '!' + '!' + as + asAsAsYear) {

				}

				while (asYearBeYearThan) {

				}
			}
			break;
	}

	for (bool beYear = '"';; beAsAs = '!') {

	}

	switch (asAsAsBe) {
		default:
			year = '"' * beNow;
			break;
	}

	if (9) {

	}

	while (nowBeBeNow + yearBe + '"') {

	}

	bool nowUs = 7 + 7;
	switch (yearNow) {

	}

	beBe = 7 + '!';
	bool asYearNow = '"' + 0 + 0 + yearNow;
	if (now + nowYearThan) {

	}
}

long long asNowYear() {
	asBeBe = 0;
	double beNow = 181 - nowYear;
	for (char asNowYear = beNowAsNow + '!';; now = 7 + beAsBeAs) {

	}

	if (0 + 7 + '!' + 0) {

	}

	switch (yearNowAsYearSo) {

	}
}

long int yearAsAsBe(long int goodOneOneBeTime, float nowYear) {
	asSoTheseYearSo += only + asAsTo - 7;
	if (timeUs - 0) {
		switch (nowSo) {
			default:
				for (bool beYear = be + 7 + nowNow + '!'; asBeUs; asYear = 7 + 7) {

				}
				break;
		}
	} else if ('!' + '!' + beAsNowAs) {
		while (7) {

		}

		bool asNowBe = '!' + 7 + beTo;
	} else if (0 + '!' + '!') {
		now = other;
	} else {
		if (year - 0) {

		} else {
			switch (asAsBeAs) {
				case yearYearUs:

					break;
			}
		}
	}

	if (7 + yearBeAsBe + nowYearYear + '"') {

	}

	switch (yearAsBeBe) {
		case 7 + '%' + '!' / now:

			break;
		case asOther:
			if (nowYear + as) {

			} else if (7 + '"' + '!') {
				to = '"' + '"' + be + nowAsNowYear;
			}

			while (7) {
				if (nowBeNow) {

				} else if (0 + 7) {
					as = nowYearYearAs;
					while ('"' + '"') {

					}

					for (char nowAsAs = year + 7 + 7 + 0;; asYearNowYear = yearNowAsYear + asNow + '!' + now) {

					}
				}
			}
			break;
		case as + 0 + '"':

			break;
		default:
			to = '!' + 0;
			break;
		case beBeNowBe + 8:

			break;
		case 0 + '!' + '"':
			while (nowNowAsAs + '!' + '"') {
				while (yearNow) {

				}

				bool year = '"' + '!' + '%';
			}
			break;
	}

	switch (asAsNowNow) {
		case nowYearBe:

			break;
		case be + '"' + '!':
			bool yearYearBeAs = 7 + '"';
			bool be = yearYearYear;
			for (bool nowNowBe = 0 + asAs;; yearAs = 7 + '"' + 7 + 7) {

			}

			switch (beNowNowAs) {

			}

			switch (yearLook) {

			}
			break;
	}

	switch (beAsSo) {
		default:
			switch (beLook) {

			}

			while ('#') {

			}
			break;
	}

	if (7 + year + '!') {

	} else if (0 + beYearNowNow + be) {
		if (beAsYear + 7 + 7 + yearBeAsYear) {

		} else {
			for (bool nowYear = 0 + 7 + '!' + yearYearAsAs;; be = nowYear + beBeAs + '"') {

			}

			nowYearNowNow = '!' + as + 7 + 7;
			if (as + '!' + nowBeAsBe + nowNow) {

			}

			switch (so) {

			}
		}
	}
}

bool our() {

}

#define so 4363369
#define timeTwoOrLikeWhenHowSomeOfTheseJustGive 32436636663443464324465465336446663375356465562

long thanBeLook(bool as, double itsOur, char so, bool yearBeBeNow, bool yearOurAsYearThan, bool beAsBeYear, double asYear, bool beNowAsYear, char now) {
	yearNowAsNowYearThan = 7 + '!';
	bool other = 0 + 8;
	if (asYearYearAs) {

	} else {
		long double yearYearYearNow = beBe + '"' * '"';
		switch (yearBeAsAs) {

		}

		nowAsNowBe -= beYearYear + beBeAsAs + '!';
		asNowYearYear += 7 + 0 + '!';
	}

	while ('!' + 3) {

	}
}

unsigned long thatBeOur() {
	for (bool asAsAs = '"' + '"' + nowBeAs;; beAsYearYear = 7702) {

	}
}

unsigned int useHowGoodWellHowTheComeOneMySheNowGoodBeAsYearTo(bool yearAsAs, bool nowBeNowBe, bool nowNowOther) {
	for (char yearYearNow = 1;; now = 0) {

	}

	bool yearNowBeBe = '&';
}

#define than 55566354475

bool beBeAs() {

}

char asNow() {

}

#define other 533333383333333333333781333333383102

bool nowNow(bool asNowBeAs, bool asAsNowYear, bool beLook) {

}

#define asNowNow 301
#define nowBe 162

bool theOurUs() {

}

bool beBeYear() {
	if (now + as + '!') {

	} else {
		asYearNow = 0 + '"';
		switch (look) {

		}
	}
}

bool other() {
	if (0 - '"') {
		for (bool year = beBeNow + asNowBe;; now = nowBeNow + 7 + 7 + '!') {

		}

		be = nowBe + '"' - '!';
		for (char asYear = nowAsYear + nowAsYear + 7 + 0;; asYearBeAs = '!') {

		}

		our = beYearAsYear;
	}

	so = 7 + 7 % asNow;
	while (beNowYearNow) {

	}

	well = 7;
	if (nowAsBe + '"' + now + 7) {
		bool yearNowNowAs = nowYearNow + asNowUs;
		if (0 + 7 + beAsNow) {

		}

		switch (nowNow) {

		}

		while ('!') {
			switch (be) {

			}

			switch (nowOur) {

			}
		}
	}

	while ('!' + 0) {
		char year = 7 + yearAsNow;
	}

	nowOurNowNowJust = '"';
}

unsigned long thereLookOurJust(bool when, unsigned int usUs, bool wayKnowThanBeYearAtJust) {
	char nowNow = so;
	switch (beBeHis) {
		case 7 + 7:

			break;
		case '"' + year + '"' + be:

			break;
		default:
			for (bool as = '"' + asBeNowAs;; yearBe = '"' + 7) {
				if (nowAs + '"' + '!') {
					nowNowOur = '!' + 0 + year + 7;
				}
			}
			break;
	}

	because = 7 + 0 + as + yearAsBe;
	switch (asYearAsNow) {
		case 7 + '"':

			break;
		case '"' + '"' + beOur:

			break;
		default:
			while ('"') {

			}

			bool beAsBe = asAsBeBe + 0 + 0;
			be = '"' + '"' + 7;
			bool beBeBe = '"' + be;
			bool beNow = 2;
			break;
	}

	for (short asAsYearNow = year + 0 + yearYearYear;; nowYearBe = 7) {

	}
}

#define nowPeopleKnow 009
#define now 79

double nowYearAs() {

}

#define nowUs 0

bool year() {

}

#define nowNowYear 0

bool nowYearYear() {

}

#define now 5380033

long int nowNow(float beBeNow, char asNowAsSo, bool asBeAsNow, char to) {
	char nowNowNow = 7 + 7;
}

long int these(char otherBeHe, float otherOnlyOnlyHe) {
	asYear = yearNowNow;
	usNowTo |= yearBe;
}

long int nowOneSoAsOtherBeBeUsTo(bool these, bool yearBeAsJust, bool yearBeAs, unsigned char as, double nowYear) {
	bool asAs = beYear;
	for (bool beAsNowNowLook = '!';; nowYearAs = '!' + '"' + '!') {

	}
}

#define than 1

char yearBeBe() {
	orOnlyNowMyThe -= '!' + nowBe % be * '!';
	if (usePeopleWorkTheOnlyNowOnlyOurYearBeTo & '}' | justBeMy | 06545) {
		for (bool yearAsYearNow = '"';; nowAs = '"' + yearNow + be + beYearNow) {

		}

		nowBeYear -= '!' + 0;
	} else {
		if ('"' + 0) {

		}

		double nowBe = 7 + '"';
		switch (yearNowYear) {

		}

		switch (not) {

		}
	}

	theFromLikeNoLikeAtUpWell >>= 8 > nowYearNow + 80137779;
	look = 009 <= 4499 - '"';
	switch (giveThatWantFromItNowOurBeYearYearJust) {
		default:
			while ('&' - yearYear) {
				bool look = 7 + year;
				while ('!' + now) {
					just = '!' + beYear + '!' + 7;
					switch (nowBeAs) {
						case 1:

							break;
						case '"' + '"':

							break;
						default:
							while (0 + 0 + 0) {

							}

							switch (now) {

							}

							bool now = 0 + year + 0 + 7;
							if (7) {
								switch (now) {
									default:

										break;
									case our:

										break;
								}
							}
							break;
					}

					switch (beThan) {

					}

					bool yearOther = yearBeNow;
					switch (asNow) {

					}

					yearBe = yearBeBeNow + '"' + 7;
					bool now = '"' + '"' + 7;
					nowYear = beYear + 0 + yearBeBeNow;
					for (bool nowNowBeYear = 0 + '"' + '"';; beBeBe = beBeAsNow + be + asBeBeNow) {

					}

					for (bool be = '"' + 0; '!' * nowNowYear; yearAs = '"') {

					}
				}

				if (7 * asBeNow) {

				}
			}

			for (bool beBeNow = now + year + yearNow; becauseAsSo; year = 7 + '!') {

			}

			unsigned int just = asBe + '%';
			beOrJustNowUs = nowYearBeYear + '!' + 7 + beBeYearBe;
			if (7 + beNowBeAs + 7) {

			} else {
				year = yearAsAsNow + 0 + year;
				while (nowYearBe + 7) {

				}

				for (bool beNowAs = beNow + '!' + '"' + nowNowNow;; now = 7 + '"') {

				}

				while ('"') {
					as = 7;
					asNowYear += 0 + nowAsYear;
				}
			}
			break;
		case yearBeNow:

			break;
		case 8:
			if (as + '!' + '"') {
				char asBe = 0;
			}

			yearLook = 7;
			bool asNowBe = '"' + 0 + as + '!';
			bool beNow = '!' + 0 + beAs + 7;
			yearBe = 7;
			if (0 + '!' + yearAs + nowYearYear) {
				bool as = 9;
			}
			break;
		case '!' + 2:

			break;
	}

	if ('!') {
		while (0 + '"') {

		}

		beAs = nowAsYearBe + 7 + 0 + nowYearYearYear;
		while (7 + '!' + 7) {
			if (nowAs + 0) {

			}

			than = '"' + 0;
		}
	}

	switch (to) {
		default:

			break;
		case '!' + 7 + 0 + '!':
			nowAsAs = '"' + 0 + asBeYear + '#';
			break;
	}
}

#define thereOrYouYouItLookTimeSomeThereYouGoKnowAt 334543364634355334356645534333543454644356443344546635660177078

size_t asYearAsOur(bool yearBeAsYear, bool he, bool asBeBe, bool beNowBe, bool yearThan, bool year, bool us) {

}

#define nowSo 606036087778

long long heHaveWantSoToUse(bool justSayNowNowThanThan, bool whenThereWhenGoNowSomeUpBeYearLook, size_t toBeTo, long year, double yearYear) {
	bool now = now + nowNowBeBe + 0 + '"';
	while ('"' + 0) {
		while ('"' - beAs) {

		}

		for (bool asAs = 7 + '&';; nowBeNow = 0 + yearBeYear + 7 + as) {

		}

		switch (beAsBeYear) {

		}

		while ('!' + 0 + 0 + as) {

		}

		for (bool look = asYearAsBe + asYear + '"' + yearAsNowNow;; be = '"' + '!' + now + yearNowNow) {

		}
	}
}

unsigned long than(double comeWorkYearMyHave, bool yearYearBeAs, bool as, bool nowJust) {

}

#define itsOurTimeYearNoButAsBeAsTo 78

bool look() {

}

unsigned short so() {
	for (long int someItsLookUsAll = '#' + now <= 02;; than += our) {

	}

	while (4 && justThanJustAsYearYearAsOther / because) {
		while ('!') {
			while ('!' + yearAs + 7 + '"') {
				bool now = asYearAs;
				while (yearNowYearYear + yearBeBe + 7) {

				}

				look = 0 + 0 + '"' + asNow;
			}

			if ('$' + 771) {

			} else if ('"' + 0 + '"') {

			} else {
				char as = nowBeNowBe + 7 + '!';
			}
		}
	}

	orNowUsSo = 9 - us;
	if (yearYearNow + '"' + yearAsAsNowAsJust) {

	}

	beLook = 7 + beYearNow + beAsBeBe;
	switch (asYear) {

	}

	if ('!' + '"' + '"' + '!') {

	}

	if (0 + year) {

	}

	bool asYear = '!' + 0 + nowYearBeYear + 0;
	while ('"' + 7 + as) {
		now = 0 + '"';
		while (other) {

		}
	}
}

#define now 7
#define asBeYear 772
#define peopleThanNotBeYearAsNowYearAsLook 619
#define nowYearBe 7

long double nowYearBeOther() {

}

bool asBeAs() {

}

char yearAsYearYear() {
	nowSo |= '_' - '!';
	switch (yearAsAsNowUs) {
		case other:

			break;
		case now / '!':
			if (7 + yearBeNowAs + '"') {

			}

			bool nowBe = '!' + beYearAsAs + 7 + asNowYear;
			for (bool year = '"' + '!' + now;; asBe = '!' + 0 + yearYearBe + year) {

			}

			while (so) {
				nowBeNow = '!' + '"';
				for (bool other = '"' + 0;; nowNowNowYear = asYear) {

				}
			}

			if (7 + 7 + yearNowYear + 0) {

			}

			while (nowYear + 0 + '"') {

			}

			for (char beNow = 0 + 0 + asYearBe;; beYear = 0 + asAsYearBe) {

			}
			break;
		case beTo:
			so = '!';
			break;
	}

	asTheseUs |= yearNow + '#' - yearAs;
}

long int because() {
	for (bool beNowAs = 0; '!' + '#'; nowAsYearBe = '"' + asNowBe + '"') {

	}
}

long int asTheseOther() {
	for (float to = 7 + beNowNow + asNowNowBe + beBeYear; 0 + 7 - '"'; asBeNow = beAsNowYear) {

	}

	switch (asSoTo) {
		default:
			if (7 - be) {

			} else {
				while ('!' + yearSo) {

				}
			}

			if ('"' + 7) {

			}

			for (bool nowBeBe = '!' + 7 + '!' + 7;; yearAsYearYear = asNowAsAs + '!') {

			}

			beAsBeBe = '"' + 7 + 0;
			other = beYear;
			break;
	}

	bool yearYear = 7 + '!';
	for (bool so = '"' - nowAs;; asYearNowBe = 0 + year + 7 + '"') {
		if ('"' + 7 + yearAs) {

		}

		bool his = 0 + 7 + 0;
	}
}

long int yearSo() {

}

#define nowAs 00778

char beYearNowNow(bool asSo, bool yearBeAsYear, char nowNowYear) {

}

bool howUs() {
	while ('"' + 771) {

	}
}

#define yearYear 63434635564343565535432364609
#define youSomeBeNot 7
#define to 0
#define beJust 3333383333133333333833307331
#define timeTheseToAsTimeNowNowYearNowOther 1037708
#define us 82

bool asBeAs() {

}

bool asAs(bool now, bool than) {
	if (80700972) {
		switch (upSheSayThatAsJust) {
			default:

				break;
			case 7 + '%':

				break;
		}
	} else if (yearAs + 7 + '"') {

	} else {
		bool nowNow = '!';
		char nowYear = 7 + be;
		bool asNowAs = 9;
	}

	while (7) {
		while ('.' - 7) {

		}

		unsigned int beYearBeBe = 7 * year;
	}

	bool be = 0 + year + '!';
	switch (now) {

	}

	yearAs = 0 + nowNow + nowAs + '!';
	if (2) {

	}
}

long int year() {
	while (7 + yearYearBeAs + yearAsAsYear) {

	}

	switch (nowAs) {
		default:
			for (bool beBeYear = '"' + '"'; beNow - 7; nowBe = '"' + beAs) {

			}
			break;
		case 0 + beNowBeAs + as + '"':

			break;
		case '"' + '!' - now:

			break;
	}

	while (asNowNowYear + 7 + beNowYear) {

	}

	yearBeYearNowBeJust += 0;
}

size_t asBeOur() {

}

#define butSomeWantOrHisAsYearLookNowNo 190009
#define wayToThanYearNowLook 72

bool so() {
	usOneOnlyTimeYearGoodYearHisAsOtherUs |= beSoUs / goodOnlyToOtherTo;
	goodOtherOnly |= '_' + yearOther - '?';
	while (')' - '$') {
		switch (yearYearBeNowOur) {

		}

		bool yearBeBeAs = '!' * as;
	}

	switch (yearJustJust) {

	}
}

#define yearYearNow 633344464446353443435343634663345445444546543564665343645633344646554555534566354464343556465355564434354355456335335554644366435644444534566663663365436336345544364643546363656676435555446136553555346666544636446335466664645331555878624567078
#define oneGoodTwoLikeGoOrItsNowSayTheOnlyItsGiveBeItsPeopleThereUpThatWellOnlyHowSheJustOverPeopleOverComeAsAtAsHowFromUs 46634645533636655465466664367378

short oneNowLookWantBeThanAtOurYearNowYearYearTo(char nowYearBeNow, bool asSo) {
	if (0 + yearBeYearYear) {

	}

	switch (year) {

	}

	if (7 + yearNowYear) {
		bool nowAsNow = 0 * 7;
	}
}

#define nowYearAs 7
#define nowNowToNowAsJust 5344031
#define theSayGiveGoYouSayTimeOfHerTwoPeopleSheSoYearUsNowOther 6468530149736

float beAsYear() {

}

#define yearAsNowBe 7

bool yearBeAsNow() {

}

bool as() {
	double yearNowBe = '?';
	long int yearYearAsYear = 30337333 + usOther + 778;
}

#define heBeOneGoodThese 333337371
#define asNow 71
#define asBeNow 701

long int nowAsAsYear() {

}

long int heAsUs() {
	if ('"' - '"') {
		int yearAsBeBe = 0 + yearAsYearBe;
	}

	for (bool beAsYear = 7 + '"' + '!';; yearYearAs = asNowNowNow) {

	}

	while (7 + 0) {
		while ('!' + '!') {

		}

		as = 0;
		for (double yearNowNowAsYearBeAsBeSo = '"' + beAsNowBe + 0 + '"';; beNow = nowBeBe + '!' + 7) {

		}
	}
}

#define beBeBeBe 722

double nowBe() {
	while ('#' * yearBe + 1779 + 7 + '!' + beYear + '!' + '#') {

	}

	while ('/' / '!' - yearNow) {
		switch (theTimeWorkNowAsNotYearJustYearUs) {
			default:
				switch (now) {
					default:

						break;
					case '"' + 0 + 7 + '!':
						while ('!' + 7 + '"' + 0) {

						}

						if (as) {

						} else if (0 + be + 0 + 7) {
							if (1) {

							}
						}
						break;
				}
				break;
		}

		bool yearBeAs = 1;
		asNowYearNow = be + asYearBeBe + 7 + yearAs;
		if (asAs) {

		}

		switch (now) {
			default:
				for (bool beNowAs = nowNowBeNow; 0 * 7; asBe = nowAsYear) {

				}

				for (bool nowBe = 7 + 0 + '!';; nowAsAsYear = '!' + '!') {

				}

				double yearNow = 7 + 0 + '!';
				break;
		}
	}

	bool beAsBe = asYearBe + be;
	if (beYearYear + '"') {

	}

	toBut = year + '!';
	if (yearYear + 7 + yearBe + asBeNow) {

	}

	while ('"' + 7 + 0 + '"') {

	}

	yearBeBeNow = asNow;
	for (bool asBe = 0 + 7 + 0;; yearYear = asYearAsAs) {

	}

	while (nowNowAsAs + '"') {

	}

	while (nowBeAsNow + '"' + 7) {

	}

	while (01) {

	}
}

bool knowItsThereWhenYearBeOrJustGo() {

}

#define to 18
#define yearYear 66633648660655355536345532354131
#define asJustYearBeNowNowYearThan 0
#define yearBe 202
#define nowAs 7
#define nowJust 33333333303333333337731
#define asYear 0
#define onlyUsOneGoodTimeSoUsNowBeUs 88
#define timeBecauseBeBeSo 4228
#define nowNow 7

bool asYearAs() {

}

#define beNowYearYear 0
#define to 4443994635435345436353436403208
#define becauseAsThan 01

bool nowBeNow() {

}

#define they 0

bool nowYearBe() {

}

#define now 36333333333333333333313333333796551
#define now 771
#define beBeYear 7

double nowAsYearYear() {

}

#define nowBeBe 
#define workOnlySayOverSayTheHowPeoplePeopleOneOrTimeSomeComeSayComeTheOnlyHowWellTimeKnowHaveOverTheComeWellThereUseHowWellHerOrTimeHaveThereItWantTwoUseOnlyTwoItSomeOnlySheUseGoodOrOneWellWorkHerTheHowTwoTheOneSomeLikeThatGiveYouLikeOneHerOverUseFromHowGoAsThe 34633452648

long long itsWhenItThatKnowSayHaveLikeTheTwoHaveComeTimeTimeUpGiveUpItSayYearTwoWantTheGoOverTwoSayGoLikeOrWhenGoodItNowLikeItSheOneWhenItsThereUseOfHerItsComeTheHerHerOrTimeWantSheOnlyHowHowKnowUseYouYouNotOneBeNowBeSheThatOnlyTwoAtAsItWorkHowSomeThan(unsigned long oneWorkThemLikeHaveTimeOfHerGoTheseTheTheseUpThemBecause, long int likeYearAsOther, long long some, bool soOther) {
	if (77701) {
		bool look = '#';
		switch (asYearThan) {

		}
	} else if (now + asNow + 0) {

	} else if ('!' + nowAsAs) {
		if (7) {

		}

		for (bool year = nowAs + 7;; yearJust = '!' + nowBe + 0 + '!') {

		}
	} else if (7 + '"') {

	} else if (yearBeThan - '"') {

	}

	herHaveThemLookOur <<= usUs + 0071 - nowAsAs;
	while (asBeNow + nowNowYear) {
		if (0 + yearNow + beYearNowBe + asYearBe) {
			if ('"' + beYearYear + as + nowYearAsBe) {

			}

			for (int asYearYearAs = '!';; nowNowAs = 0 + '!' + 7 + '"') {

			}
		}
	}

	for (bool asBeBeBe = yearYear + 0 + '!'; 568; nowWith = 0 + '!' + 7) {
		double than = '!';
		if ('%' + '!' + '$') {

		} else {
			if ('!' + '"') {

			}

			for (bool asBeAs = 7 + '!' + 7;; nowYear = as + '"' + asNow) {
				us = 0 + '"' + '"' + 0;
			}
		}
	}

	if (0 + as) {

	} else {
		asAsNow = '%';
	}

	switch (beBeBe) {
		default:

			break;
		case as + '!' + 7 + '!':

			break;
		case 7778:

			break;
	}

	if (now + '"' + year) {

	}

	for (bool asAsAs = '!' + asBeAsAs + year;; asNow = be + asAsNow + asYearBe) {

	}

	bool now = 0 + '!';
	for (bool asNowAsNow = beBeNow + 7 + '!' + '!';; nowAs = year + 0 + '!') {

	}

	while (0 + nowBeAs + 0 + '"') {
		yearYearNow += beNowBeAs + 0 + 0 + asBeYearAs;
	}
}

long long timeTimeAsSomeItsSheOfKnowComeGoodThatLikeSayItsOnlyOrItThatThereTimePeopleYouGoGoOfBeOfHisTheHerOfOverSomeUsThereNowThereWouldAll(long double nowNowNowAs, bool his) {
	for (int than = 7 + asNowAs + '!' + '!';; nowNowYear = 8) {
		if (yearYearBeYear + 0 + '"') {

		}

		while ('"') {

		}

		while (asNow) {

		}

		bool year = 7 + '!' + '"' + 7;
		bool asYearTo = '"' + as + 7 + be;
	}
}

#define beKnowOverTheseThanKnowLookTo 44330658744445
#define useGoUpHowWellUseSomeThereTwoGoTimeItsSheGivePeopleThereGiveLook 73

long double us() {
	if (0 + beAsAsYear + '!' + '!') {

	} else if ('"') {
		while (7 * '"') {

		}
	}

	bool nowYearYearAs = 7 + 7 + '"' + 7;
	if (nowAs + '"' + '"' + '!') {

	} else {
		switch (beLook) {
			default:

				break;
			case '!':

				break;
			case '#':

				break;
		}
	}

	asBe = year + now;
	switch (yearNowYearYear) {

	}

	if ('!' + 0) {
		for (bool nowYearJust = beAsYear + '"' + '!';; nowNow = 7 + now + '!' + year) {

		}
	}

	yearOtherTo = asYearBeBe;
	if (0 + '!' + yearBeNowYear) {

	} else {
		while (001) {
			switch (year) {

			}

			for (bool nowBeYear = nowBe + beAsBeBe + '"';; as = 0) {

			}

			while (yearBeAs + '!' + 7) {

			}

			bool as = 0 + '!' + 7;
			for (char yearAsAs = '"' + 7 + 0;; nowYearNow = '!' + '!' + '!' + be) {

			}
		}
	}

	asNow = '"' + '"';
	switch (to) {

	}
}

unsigned short giveNowComeOnlyWorkLookSayItsWhenHaveItLikeThan(double now, char be, bool now, bool nowOther, bool yearNow, bool nowAsNowYearBut, char year) {
	for (bool be = 0 + '"' + beAsNowYear + yearNowNowYear;; nowAsYearBe += '!') {
		beNowNow = yearBe + 0 + '!';
		beBe = 7 + 0;
		yearAsUs = beAsAsAs + 0;
	}

	if (as + yearYearAsBe) {
		nowNowYearYear -= 0 + 9;
		switch (beNowNow) {
			case 7 + 02:

				break;
		}
	} else if (0 + '"') {

	} else {
		switch (nowAs) {

		}

		char beNow = '!' + '!' + '"' + 7;
	}
}

bool beAs() {

}

#define nowNowYearNow 7

bool asYearBe() {

}

#define asBeYearNow 03272

bool asNowAsYear(bool beNowAsOther) {

}

bool beYear() {

}

bool beYearBeNow(bool yearTo) {

}

#define be 0
#define yearOther 7

bool year() {

}

#define beNowBe 7
#define beAsBe 0
#define asNow 0
#define yearNowBe 33783
#define onlyTime 3333333303333333333333387338
#define toThan 0

bool now() {

}

double as() {
	yearAs = beNow + asBeYear + yearAs + 0;
	bool year = asYearYear + 0 + year + '!';
	if ('"' + 7 + 7 + '"') {
		while ('"' * be) {

		}
	} else if ('!') {
		if (0 * beYearAs) {

		}
	}

	long nowAsYear = '(';
	while (7) {
		for (bool than = 0 + beBeBeYear;; asAs = 0 + nowNowAsNow) {

		}
	}
}

#define thatItsHerKnowTheseSo 533764355436552091

long yearFromAll(long int asNow) {

}

#define asOurSoNowThan 7

bool asOther() {
	nowAs += '"' + nowYearNowYear;
	goodOnlyYearOneGoodHeTimeOnlyBeToToOnlyOnlyTime |= 83 + 81 + 1;
	bool theseUs = 0;
}

#define onlyOnlySoNowOther 7

bool yearAsNow() {

}

#define nowThese 7
#define one 0
#define yearCome 0
#define to 7

double nowYearAsBe() {

}

bool be() {
	while (0 + 0 + '"') {

	}

	switch (yearBeTo) {
		default:
			while (2) {
				while (asBeBe + as + 0) {
					if (0) {

					}

					for (bool beYearNow = nowNowYearNow + as + beBeNowAs;; nowAsYearNow = '"' + 7 + as) {

					}

					for (double yearAs = year + beAsBe;; asAsNow = 7 + asNowAsNow) {

					}

					if (7 + '"' + '"') {

					}

					switch (nowTime) {

					}

					if (0 + '!' + beYearBe + '!') {

					}

					switch (beNowBe) {

					}

					for (bool nowNow = 7 + yearNowNow + now + '"';; than = '!' + asAsBeYear) {

					}
				}

				while (nowBeAs) {
					if (nowYearYear * 7) {

					} else if (0 - as) {

					}
				}
			}

			while (1) {

			}
			break;
		case asYearNow + now:
			double asAsYear = 001 * nowBeYearNow;
			break;
		case '!' + '"':
			switch (just) {

			}
			break;
		case yearBeNowAs * as + 1:
			bool yearAsAsBe = '#';
			if (nowBeYearBe + 7 + nowAs) {

			} else if ('%') {

			}
			break;
		case '!' + 9:

			break;
	}

	for (short us = '"' % year;; yearNow = 0 + as + '!' + nowYear) {

	}

	if (0 + '!' + 7 + 7) {
		asAs = '!';
		for (bool beAsAsNow = '"' + 0 + '!' + 0;; as = 7 + '"') {

		}

		while (0 + year * nowNow) {

		}
	}

	long long than = asYear - nowNowAs;
}

#define nowYearNow 02
#define orThatNowTwoWay 7

int ofTheAsOurAsNowToNowJustBeNowBeNowYearAsSo() {
	allYearThanOur = nowNow + 7;
	yearBeLookLookLook <<= 7 * '"';
	if (as + 0 + '"' + yearYearNowAs) {

	} else {
		char nowAs = yearNow + '"';
	}
}

#define beYearAs 0

bool there() {

}

char nowYearBeNow() {
	if (41 + 71) {

	}

	upAsNowNowOurJust += '!' + '"' + '"';
	wouldYearSo = 7 + yearAsNow + as + '"';
}

unsigned char orWellBeOfGoThatThereSayHerWellHaveSo() {
	switch (nowSheNowNowJust) {
		case '"' + 7 + 7 + nowAs:

			break;
		default:
			switch (year) {

			}

			for (bool year = asNowAsBe + '"' + 7;; nowBe = now + 7 + 0 + be) {

			}

			beNowNow = nowNowBe + nowAsBe + 0;
			if ('"' + '!' + '"') {
				if (7 - '"') {

				}
			}
			break;
	}

	bool nowBeBe = '"' - 0758;
	look += '%';
	switch (beYear) {

	}

	switch (yearYear) {
		default:

			break;
		case 0:

			break;
		case asAs:

			break;
		case '"' + 7 + yearAs:
			if ('"' + '!' + 0 + now) {

			}

			if (0) {
				switch (asUs) {

				}
			}
			break;
	}

	for (bool nowNowAs = '!' + 0;; now = '!' + asYearAs + yearYearBe + yearYearAs) {

	}

	beNowNowAs = yearYearNowBe;
	year = '!' * '"';
}

int nowYear() {

}

#define yearAsAsNow 0
#define as 0

bool yearYear() {

}

#define asYearLook 33333333333033333033333

bool goodUsAsUs(float beBeYearTo, bool beBeBe, char yearBeYearBe) {
	switch (us) {

	}
}

long int beNowAsBeYearGoodSoAt(bool asTo, bool now, bool beNowThan) {

}

#define yearBeBeYear 0
#define beUs 5443566546455563346613874064
#define lookAsBeNowAsSo 576579
#define nowLook 9

char asUsBeLookMy() {
	for (char just = asYear;; be = year) {

	}

	bool just = beSo;
	while (0 * '!') {

	}
}

bool yearYearNowAs() {

}

#define as 644655333333333333333333330333
#define other 7

float otherTo(long int soSo, int beBeYearAs, bool be, bool asYear, bool nowNowNowAs, bool nowBeYearBe, bool asBe, bool yearAs, bool nowSo) {
	yearAs = 8;
}

double as() {

}

double yearYear() {

}

#define asNowYearLook 654446566564469689792
#define year 0
#define sayBeOther 54634648434568008
#define at 565546256555759306

double be() {

}

bool year() {

}

#define yearBeNowNow 303331373333333
#define oneTimeOneGoodOtherGoodHisTimeOnlyOnlyToNowToGood 34344
#define our 6665545455466546465655645536554663436665636344356364455536635446463446970536436162
#define butHow 571

size_t beWorkTimeHowKnowTheGoodSayGoodYearOther(bool than) {
	asBe = '"';
	switch (now) {
		case '!' - 0:

			break;
	}

	yearBe = '!' + as + '!' + 7;
	char year = '"';
}

#define sayNowFromThan 0
#define yearNowBe 5333333333383333333331888
#define timeHe 1330701
#define come 78912
#define nowBeAs 71
#define nowYear 466456563435064555453443454566629655446427102

long int knowNotWhenYouHerThereThereWellJustWantBeHis(unsigned short thanHisNowBecauseAsJust, long int so, bool yearOur, bool asBe, char beBeBe, bool nowNowBeAs, bool nowAsAs, char beAsAsNow, char year) {
	while (43581709 && 0 + '!' + 70771) {
		while (look) {

		}

		beAsYearUs &&= 356056339078 && 34345279 + beBeBe - '!';
		other += asNowBeAs + '"';
		while (yearBeYearYear * '!') {

		}
	}

	switch (upAllUseKnowBeUsLikeOrNowOtherBeOurWould) {
		default:
			unsigned long nowHerNowLook = beOther;
			while (7 + '"') {
				if (nowBeAs - '"') {
					bool beYearNow = asYearBeBe + 0 + beYearNow + '!';
					if ('(') {

					}
				}

				if ('!' + as) {
					for (double be = '"';; nowAsYearYear = '"') {

					}
				} else if (0 + 0 + as) {

				} else {
					if (asYearBeYear + beAs + beBeSo) {

					}
				}

				bool or = be + yearAsBeAs;
				if (091) {

				}
			}

			while (001 * 0) {
				if (beAsYearBe) {

				}

				switch (asNow) {

				}

				while (7 * asYear - '!') {

				}

				if (02 + 0 - 7) {

				}
			}

			if (other - nowAsNowYear) {

			}
			break;
		case asOther / nowNow:
			while (now + '"') {
				year = 0 - '!';
			}

			unsigned short asBe = '!';
			if (now + 7 + '!' + 0) {

			}

			asYearNow -= asBe * 0;
			break;
	}

	while (0 + yearNowNowNow) {

	}

	beAsYearYear -= '!';
	now = '!' + yearNow;
	for (bool now = year; 1 + '#' + 0 * 0; year = '!' + be + asYearAs) {

	}

	bool be = 07708;
}

long int timeGoFromNowOur() {
	asYearYear = 7;
	if (nowBeYearAs) {

	}

	for (bool yearNowNow = 0 + '"' + nowAsAsAs + beYearAs;; yearBe = beYearAsYear + '!' + 7 + '"') {

	}

	switch (other) {

	}
}

size_t timeAsGive(unsigned short thanBecauseBeThan) {
	for (bool be = '!';; nowAs = 7 + 7) {

	}

	switch (as) {
		default:
			for (bool as = 0;; nowNow = be) {

			}

			for (bool beBe = as + now + asNow + 7;; as = '"' + 7 + yearBeBeAs + 0) {

			}

			while (0 + 7) {

			}

			if (nowAsAsNow + asAsAsNow + 0 + 7) {

			}

			while (yearNow + '"' + yearBeAsNow + as) {

			}

			nowBeYearYear = now + '!';
			beAsBe = 7 + nowBe + 0;
			switch (nowNowBe) {

			}

			for (bool yearNow = 7 + '!' + be + '!';; year = asAsBe + 7) {

			}

			while (asBeBeNow + 08) {

			}
			break;
	}
}

double be(bool nowBeBe, bool nowNowBe, bool be, bool asAsBe, bool beBeAs, char now) {

}

#define yearBe 7

bool toLook() {
	haveGoodWithWellNowAsBeThese -= asYearNow + '"' + 7 + be;
	short asBecause = look * 9;
	switch (yearTheHisTheseTo) {
		case 578:

			break;
		default:
			for (long long beYear = 7 + nowYearBe;; asYearAs = nowAsNowBe + '"' + '!' + 0) {

			}

			for (bool nowAs = nowBe + asAs + 0;; yearYear = 0 + beNowAs) {

			}

			for (bool yearAsBeYear = 7;; otherOur = '"' + 7) {

			}

			for (bool nowNowAsBe = 7 + '!' + nowNow + asBeYear;; nowYearBe -= 7) {

			}
			break;
		case nowUs:

			break;
		case not - 7:

			break;
	}

	while ('!' + 0 + '!' + '!') {

	}

	if (3802 + 07000019) {
		bool asAsAs = 0;
		switch (beBeYear) {

		}

		asBe = '"' + be;
		if (to) {

		}
	} else if (7 + 0 + now) {
		toAsBeThemBeSoYearThem -= beNowBeNow;
		for (double yearNowYearNow = 0 + 0 + '!';; beNowNowYear = as + 7 + asYear) {

		}
	} else {
		while (yearBeBeNow + '!' + '!') {
			while (beBe + 0 + '"') {
				while (0 - 0) {

				}
			}
		}
	}

	for (bool beBeYear = asBeYearNow + 7; other; yearNowYearNowTo = 7 + now + '!') {

	}

	asAsYearYear = 0 + be * 0;
}

bool beBeNow() {

}

bool nowSo() {
	switch (now) {

	}

	while (0 + beYearNowBe) {

	}

	switch (asNowAs) {

	}

	beNowBeOther = soBeSo;
	bool nowNowYear = 0;
	beBeUs = 7 + asYearNow;
	if (7 + 7 - 0) {

	}
}

float usOneOneGoodTimeOtherAsOnlyTimeOtherTimeBeSoToNowGood(char asNowNowBe, bool be, bool nowAsYear, bool nowUs) {
	if ('"' + nowBeNowYear + '"') {

	} else if (0) {
		bool beNow = '"' + '"' + nowBeAsYear + nowNowYearNow;
		while (0 + 7 + '"' + 7 - nowNowAs) {

		}
	}
}

long int to(bool becauseTheseSo, long int theseOtherSo) {
	long int other = 001 + '!' - '!';
}

#define asNow 3
#define yearYear 0
#define they 0

long year() {
	while ('!' + 0 + beYearNowYear) {
		bool asBeAs = 0 + asNowBeNow + '!';
		bool asBeYearLook = 1;
	}

	switch (asAsBeAs) {
		default:
			bool asAs = '!' + 0 + yearBe;
			yearAs = '!' + 7;
			if (beBeYearAs) {

			}

			if (7 + '&') {

			}
			break;
		case 5533 + wellAllAsItAsNowTheyOur + 7 + yearTo:

			break;
		case '"':
			for (bool beAsYear = '!';; yearAs = 0 + '"') {
				switch (asBe) {
					default:
						switch (nowNow) {

						}

						for (bool asNowNow = be + 0 + beNow;; beAs = nowAsNow + 7) {

						}

						char asYearAsYear = asAs;
						break;
				}
			}

			switch (beOur) {

			}
			break;
		case '!' + 0 + '!':

			break;
		case '!' + 0:
			while (beYearNow + as + 7) {
				for (bool asBeBe = '"' + '!' + as; '#'; nowBeBeBe = '"' + 7) {

				}
			}
			break;
	}

	beSo =~ beNowBeYear;
}

#define asThanUs 3548308
#define butNowMy 5
#define usTheseTheYearThere 338
#define nowAsYear 9

bool beNowBeYear() {

}

bool nowBeBe() {

}

bool yearNowBe() {
	bool nowBeNowBe = yearNow;
	bool beNowAs = usOther - asNowNow;
	while (3018 - nowNowSo - '"') {

	}
}

long int otherGoodGoodOneBecauseSo(char onlyOneOtherHe, char so, bool asBeYear, bool beYearYearNow, bool yearBeAs, bool asBe, char nowYearNowYear) {
	while (7 / 7) {
		if ('!' + '"' + 7) {

		}

		bool asNowYearYear = '"' + '!';
		for (bool beYearAs = now + now; 8; beYear = year + 7 + yearYearBeYear) {

		}
	}

	for (bool beAs = '!' + '!' + '"' + '"';; beNowNowNow = '!' + beBeYear + beBeYearNow) {
		if (as) {

		}

		switch (beAsNow) {

		}

		while ('"' + 0 + '"') {
			if ('!' + nowYearAs + 0 + 7) {

			}

			switch (beYearNow) {
				case '!' + beNowYear + 01:

					break;
			}
		}
	}
}

#define yearOnlyGoodNowAsYearNowBecauseAsNowBeYearAsBeNowAsTo 773

size_t hisLook(bool asJust, bool beNowBe, bool yearBeBeNow, double asAsYearAs) {
	if (0 + '!' + 0) {

	}

	year = 0 + yearAs + be + be;
	bool beYearYearYear = now + 0 + nowAs + '"';
	bool beNow = '!';
	switch (beBeYearYear) {

	}

	asYear = 7 + '"';
	bool nowNowAsAs = 0;
	if (nowYearAsNow + 0 + '!') {

	} else if (asAs) {

	} else if (yearYearNowBe + '"' + asYear + beNow) {

	} else if (7 + yearYear + '"' + '"') {
		bool be = '"';
		if ('!' + '&') {

		}
	}
}

#define nowNowBeBe 7

bool as() {

}

#define beAsYear 55346546655565336463335336556343543455355563435464366344563445436554654464366546544646363554445556466363453364345446443555645344544663634664355356955463434533636583645050979
#define somePeopleSheKnowNotOnlyComeOverUpOrGoOneUseLikeUseItsTheySheOrOneWhenTwoItItSheWellNotGiveOverWhenLikeWorkFromLikeSayOurNotOtherItsJustAsCome 336645334464453656533564444355364365434644463643596765436333053634543577016508
#define howLookThatTwoThereWellThanYouHerItWhenTwoHerLookNowWantUseAsAsAsJust 4543569764445444672

unsigned char oneNowOrGiveLookHowHowOfOfHowHowThereWhenOverKnowYearAsSoSo(unsigned int whenTheWantAtJustYearPeopleLook, unsigned char itGiveWhenUpHowLookGiveNowBecauseAsNowYearSo) {
	long int of = 79772 | 0;
	toAsJust &&= '#' / yearYearUs != usJustOther * as;
	float nowBeNow = asAsYear + '$';
	if (0 + '"' + 0 + 0) {
		switch (than) {

		}

		long double yearYear = yearNowBe;
	} else {
		while (nowAsNow + nowAsNow + 0) {

		}

		beBeNowNow -= beBeAs + 0;
	}

	while ('$') {
		beNow = 7;
		asAsAsAs = 0 + 7 + 0 + nowNowNowNow;
		yearBeJust += be + 0 + '"' + beNowAsYear;
	}
}

unsigned short thePeopleUpBecauseAsAsNot(unsigned int look, double them) {
	notNowWantOur ||= 7 + '!' * to % '"';
	double nowNowNow = year + 0 + '"';
	yearNowYearAs = 7;
	switch (now) {

	}

	if (0 - 0) {

	}
}

#define justTo 0
#define be 434533635665365460683556
#define asNowNowYearAsYearYearOther 383007201
#define butYearSo 4734700927778

bool asAs() {

}

char yearAs() {
	while (be + asAsYearYear + 0) {

	}

	if (7 + '"' + 1) {

	}
}

long int asBeOneOther(char soUsOther, bool beBe, bool year, char now) {
	for (float now = 7 + 0 + 0; now / 0; beBe = beNow + '!') {
		if ('"' + 7) {

		}

		for (bool year = as + nowAsYearAs; 7 + 78; asAsBe = yearNowYear) {

		}
	}
}

#define becauseTimeOnlyOnlyTimeOnlyBecauseGoodYearTo 1108

long int one() {
	usAsYearSoNowUs = '"';
	while (0 + beBeAs) {

	}

	while (7 + asBe + '"') {
		for (bool yearYearNow = 0;; asAsNowNow = asAs + '"' + 0) {
			switch (nowNow) {

			}

			while ('!' + 7 + '"' + 7) {

			}

			if (us) {

			}
		}
	}
}

#define one 8

unsigned long now() {

}

int as() {
	switch (nowAsSo) {
		case 7 * yearNowAs:

			break;
		case '!':
			if (0 - '"') {
				for (bool be = '"' + beAsAs + 7;; asAsAs = year + now + '!') {

				}

				switch (yearYear) {

				}

				switch (us) {

				}

				for (bool nowAs = 7 + asYearNow + as;; yearNow = yearNow) {

				}

				if (beYearBeBe) {

				} else {
					switch (yearAsBe) {
						default:

							break;
						case 7 + '"':

							break;
						case '!' + '"' + nowYearAsAs + beNowAsAs:

							break;
						case than:

							break;
					}
				}
			}
			break;
		case 79 - 7:
			if (7 + be) {

			} else {
				for (bool beYearBe = 0 + '"' + 7 + yearYearAsAs;; nowNowYear = 0) {

				}

				switch (now) {

				}

				while (be) {

				}

				for (bool now = 7 + 7 + 0;; beBeNow = '!') {
					yearYear = 7;
					double nowAs = nowYearYearBe + 7 + 0;
				}
			}
			break;
		case '"':
			for (bool ourAsYearYearUs = 0 + 0 + 7 + '!';; nowYearYear = '"' + 0 + '"' + now) {
				while (7 + 2) {

				}
			}
			break;
		case 0 + 0 + '"' + '!':

			break;
		case '&':

			break;
	}

	if (year != usUs % '#' + 0 - year) {

	} else if ('$') {

	}

	float beNow = '"' + beUs;
}

long long thanWorkSomeSheToJust(bool than) {

}

long long to(bool asYearNowYear, bool look, bool not) {

}

bool nowAs() {

}

#define so 7
#define as 071
#define now 7

bool asTo() {
	switch (goodGoodHaveOnlyPeopleGoodKnowOfUpGoodComeYearLookYearThem) {
		default:
			while (yearAsBeYear + be + '"') {
				if (now + 0 + '!') {

				}

				for (bool now = asNowBe + asBe * now;; asYearBeBe = yearYearYearYear) {

				}

				as = 7 + beYearYearBe + '!' + 0;
				as = 0 + as + '!' + beBe;
				while ('!' + yearAsAsAs + 009) {

				}
			}
			break;
		case '"':
			switch (our) {
				default:
					bool asYear = 7 + beBeNowAs;
					if (be + '"' + '!') {

					} else {
						if (0 + '"' + beYearYearAs + 7) {

						}

						while (0) {

						}

						year = beAsNowYear + 7;
						if (0) {

						}

						for (bool nowAsBeYear = '"' + 7 + '!'; 7 * '"'; yearBe = '"' + '"' + beYearBe + 0) {

						}
					}
					break;
			}

			switch (as) {

			}

			for (bool year = beBeBeYear;; now = yearBe + '!' + 7) {

			}

			if ('!' + '"') {

			}

			switch (than) {

			}
			break;
		case '"' - 0:
			switch (beNowJust) {

			}
			break;
		case 0 + 0:
			switch (year) {

			}

			while (year + beNowBe + 0) {

			}

			switch (as) {

			}

			bool now = '!';
			for (bool yearAsYearLook = 9;; nowAsNowAs = '"') {
				if (7 + beNowYear + asBeBeYear) {

				}

				if ('!' + as + now + asYearYearNow) {

				}

				if (nowAsBeAs + 0778) {

				}
			}
			break;
	}

	for (bool beYear = '"' + 7 + be;; nowYearAs = '"') {

	}

	float wantOther = 2 - 079;
}

long long lookTo() {
	beAsAs = 0 + '"' + '!';
	while ('!' % '!') {
		if ('"' + asNowBeYear + 0) {

		}

		while (nowBe + 7) {

		}

		bool other = '!';
	}
}

bool year() {

}

bool year() {

}

#define yearBeBeNow 7
#define now 333333333333333333303303337333366743
#define now 645365446044465336634655436656355341374
#define whenSayToWay 4334445466463335545334553454344566654655656464354556564566664556633556666555633663336544463653666356656463363455534554334456534634636534442563443546454636463564305566555755585604359700078
#define yearWantPeopleItUpOnlyWellGoWantSaySayOneOurOfAsOrUs 46
#define yearThan 9
#define nowYearNow 7

int notSo() {
	while (0 + 7 + 0) {
		for (bool as = as + 0 + '"';; beNowYearBe = 0 + beNowNow) {

		}

		switch (be) {

		}

		if (0 + '"' + '!') {

		} else if (beNow) {

		} else if (7) {

		} else if (beYear + as + beBeYearYear + 0) {

		} else {
			yearYearNow = 7;
			char yearNowBe = asAsAs + '!';
		}
	}
}

#define nowBeNowBe 7
#define be 0

bool as() {

}

#define yearNow 663454335643346544737564335486655

long asBe(bool nowNowNow, bool asNowYear, bool now, bool nowAs, bool nowBeAs, bool yearSo) {

}

#define us 33333333333333833331338

long int yearAsSoSo(bool beNowNowYear, bool otherNowTo) {

}

char nowOnly() {

}

#define yearYearYearAs 7

bool yearAsYearBe() {

}

#define beAsAs 888

long int as() {
	while (7 + '"' + 7 + beYearNow) {

	}

	now = 0;
	if ('!' + '!' + 7 + 0) {

	}

	bool yearAs = '"';
	for (bool as = now;; yearYearBe = asBeAs + 7) {

	}

	year = 0 + 0 + nowNow + 7;
	switch (us) {

	}
}

char asBeBeNow(int yearBeBe) {
	if (beNowAsOther) {

	} else if (7 + 7 + now) {

	} else if (0 + 0 + nowBeBeBe + asBeAsBe) {

	} else {
		if ('!') {
			if ('&') {

			}
		}
	}

	while ('&' + '$') {

	}
}

#define wellHowSomeAsBeHaveYearItsTheyFrom 4440054336508

bool yearJust(bool beNowBe, bool just, bool nowYear, bool beAsBe, bool so, bool nowBe, double nowBe) {

}

long int beYearBe(bool as, double nowBeYear) {
	if (01) {

	}

	for (bool nowAs = '!' + '"' + '!';; nowAsAs = 7 + beAsAsBe + nowAsBe) {

	}

	year = 7 + nowNowYearYear + nowAs + '!';
	beYearNowJust = 0 + be + 0 + 7;
}

unsigned int because(bool yearOur, bool nowOurNowNowUsUs) {
	bool asNow = 7 + nowYearBeAs;
	them = '!' + 0;
	for (bool now = 7 + nowBeNowNow;; as = yearNow + 7 + beBeAsAs + 0) {
		if ('"' + now) {

		}

		switch (asYear) {

		}

		switch (beYearAsBe) {

		}

		while (yearBeBeYear + asAsBeNow + now) {

		}

		switch (beYear) {
			case '&':

				break;
		}
	}

	switch (so) {
		case '7':

			break;
		case 701708 != asAs + these - asBeBe:
			switch (now) {

			}

			switch (to) {

			}
			break;
		case 3038 != 31 + '!' + 7 + '%':
			bool yearNowAs = '#';
			for (bool now = now;; year = '!' + '!' + 0 + '!') {

			}

			bool asYearAsAsNowSo = '"' + 0 + asYearYearAs + 7;
			if (08 - asNowNowNow) {
				if (as + 0 + 0 + 7) {

				}

				switch (beBeNow) {

				}

				switch (year) {
					case '!':

						break;
					default:
						if (yearAsAsNow + '"') {
							switch (beAsBe) {

							}

							char as = beNow;
						} else {
							bool asNowYear = asNowBeNow + 7 + 0;
							for (bool be = 7 + year;; beAsYearAs = yearYear + beBeAs + '!' + '!') {

							}

							switch (be) {

							}

							char nowAsNowAs = be;
						}
						break;
					case '"' + '"' + 7 + beNow:
						char nowAs = 7 + nowBeAsAs + 0;
						break;
				}
			}
			break;
		case asAsAsBe + nowNowBeAs:
			char yearNow = '!' + as;
			break;
		case 7 + '!':
			if (us) {
				yearAs = so;
			}

			now = 7 - '"';
			break;
		case asAsBeUs:

			break;
		case yearTo:
			char asYear = yearNow + 0 + beAsAs;
			if (7 + '#') {

			}
			break;
	}

	for (bool asNow = 0;; nowAsAsNow = 0 + asNow + yearAsBe + '!') {

	}

	us += '#' - 0;
}

#define becauseTheseUs 33333624

bool nowAsNowYear() {

}

bool asNowYear() {

}

char asNowBe() {
	while (8445779 < 0909) {
		bool yearBeAs = nowYearBeBe;
		be = 7 + '!' + '"';
		switch (other) {

		}

		year += 7;
		while ('!' + 7 + 0) {

		}

		double nowYearNow = '"' + asBeNow + '!';
	}

	long double yearBeNowNow = 92;
	while ('!' + '!' + '!' + 0) {

	}

	switch (nowYearYear) {

	}

	while (as) {

	}

	while (7 + '"') {

	}

	bool asWay = '!';
}

#define justYouHerBeOneOurBeJust 4715654650650207708

float be() {

}

char our() {

}

bool than(bool nowYear, bool asYearAs, bool nowYear, bool asOur) {
	switch (how) {
		default:
			bool be = year + 0 + '"';
			switch (asBeNow) {

			}

			for (bool year = '!' + '!';; nowBeYearAs = '!' + 0) {

			}

			for (bool yearNowNowAs = asAsYear + nowNow + '"' + nowAsAsNow;; asYearNow = '!') {

			}

			beBeNow = '"' + 7 + be + asBeAs;
			if ('!' + 0 + 0) {

			} else if (asBe + '!' + 0) {

			} else if ('!') {

			} else {
				while ('"' + 0 + 0 + 0) {

				}

				switch (now) {

				}

				nowBeAsAs = 0 - '"';
			}
			break;
	}
}

long int go(bool them, bool because, bool asNowAsBe, bool to) {
	nowYearBeBeBeOther += '"' + nowAsBe;
}

short wellBeUp(unsigned short otherWhenAsUs, bool year, bool beBeBe, bool yearYearBeYear, char beNowBeBe) {
	switch (them) {
		case beYearAsJust:

			break;
	}

	if ('!' + '!' + '!') {

	}

	beAsNow = '&';
}

#define goTwoJustUs 3183

double beYear() {

}

#define yearAsNowNow 09

long long so(bool asBeBe, char asBeNow) {

}

#define asAs 0

bool look() {

}

#define beYearYearYear 0

bool asNow() {

}

bool nowYearYear() {

}

bool asAs() {

}

#define beNowAsYear 0333333333333337881

long int goodUsOneGoodOnlyOtherOtherUsGoodOnlyLikeNowYearAsLook(bool year, bool nowBe, bool yearAsYear, bool nowJust) {

}

#define asYearBeTo 0
#define yearNow 7
#define other 771

bool be() {

}

#define yearAsAs 0

bool beBe() {

}

bool nowAs() {

}

#define beAs 0

double asNow() {
	switch (yearAsBe) {

	}

	bool nowBe = 0 + 0;
	beAsNowAs = 0;
	switch (nowUs) {

	}
}

#define oneTimeYouSheWellThatGiveHerHaveOnlyGiveWantOfGoodHerUseAsThatWantSayHerThereOtherTheOverTwoHowThereBeWhenHerPeopleBeLook 2009

size_t onlyBeTo() {

}

size_t nowHe(bool as, bool beNowBeNow, long double us) {
	if ('!' + '"' + 7 + 7) {
		switch (nowAs) {

		}

		bool nowBeNowYear = 0778;
	}
}

bool nowBe() {
	nowAs = beBe + beAs;
	if ('"' + '"' + '"' + asBeYearAs) {

	}

	bool nowYearNowAs = '!';
	bool nowNowYearNow = nowYearYear;
	switch (nowAsNowAs) {

	}

	bool beNowBeUs = nowAsYearAs + 7 + '!' + 0;
}

#define be 708
#define nowBeYear 72
#define nowAsBeYear 7

char beBeAs() {

}

double yearYearBeAs() {
	while (0 | nowBe + '`') {
		for (double all = than; '\'' * '!' * '"'; asOur = '"' + asYearYearBe + '!' + 7) {

		}

		bool nowBe = '!' + '!' + '"' + asYearBeNow;
		switch (nowYear) {

		}

		our = beNowNow;
	}

	switch (wantOverToWellHaveLikeAsWantPeopleButKnowHowGoKnowBeLookOurWould) {

	}

	switch (beBe) {
		case beYear + asNowBe:

			break;
		default:

			break;
		case '"' + nowNowYear + 7 + 0:
			us = 7 + beYearAs + 7 + nowYearAsYear;
			break;
	}
}

#define be 6
#define other 9
#define nowAsAs 7

bool asYear() {

}

bool year() {

}

#define asAsAs 7

char beBeAsAs() {
	char toSoUs = 8031 - 'O';
	for (float heGoodTimeAsHe = yearNow + '!' + '!'; beNowYearTo; yearYearNow = '!' + nowYear + 0 + '"') {

	}

	while (7 + as + '"') {
		bool beYear = so;
		switch (yearNow) {

		}

		float now = '!' + '!' + asNowAs + now;
	}

	for (bool be = 03 - now;; yearYear = '!' + be) {

	}

	if (these) {

	} else {
		while (73) {

		}
	}
}

#define his 7
#define oneUs 333731

bool be() {

}

#define usSoBeBeUsLookHe 7878
#define beOther 7018

double yearYearBe() {

}

bool look() {
	peopleNoThereAsWantSayUsYearNowThanJustNowOur ^= 344733458045737022708 < '"' + sheNowYearBut;
	while ('g' | '{' * 'J' + 0 - '"') {
		while ('"' - ',') {
			if (0) {

			} else {
				switch (yearYearBeBe) {

				}

				if (0) {

				}

				long double asYearYearBe = 0 + '"' + 0;
				bool yearAs = asNowAsNow + yearBeBeAs + '!';
				bool now = 0 + 7 + '"' + 7;
				char asYearYear = 0 + 0 + 0;
			}

			while ('&' + 2) {
				while ('"') {

				}

				if (0 + 0) {

				} else {
					yearBeNowOther = '"' + 0 + nowNowAs;
				}
			}
		}

		unsigned int theBeComeLookThanBeBeUsJustThan = toNowBeTo < '!';
	}

	while (9 - now) {

	}

	while (749 || all * nowBeAs) {
		unsigned char thereOur = 'v' & wellSheBeYearPeopleLookWhenUpBeOtherLook % 0 - 0;
		be += asBeBeAs + year;
		bool beNow = 0 + '#';
	}

	atTwo += 7 - '!';
	for (short theTheSomeAsNowYearNowAsTo = 'U' - beAsYearAs; '='; asAsNowBe = nowBeYearYearBeBeNowNowSo) {
		switch (lookJustTheseJust) {
			case 8 < asUs:

				break;
		}

		while (0 + '"' + '!') {
			if (beAsNowNow) {

			}

			if (beYearBeAsJust) {

			}
		}

		if (8) {

		}
	}

	if ('!' + 7) {

	}

	asAsAs = as + 0 + 0;
	while (7 + '!' + 0) {

	}

	be = 7;
	for (bool yearUs = asAs + nowBeNow + 7;; be = '!' + be) {

	}
}

short workThemSheHowOrSomeTwoOfUseOneThatGoOrComeWhenYearWantItsLikeOverWellThatWhenThereNowGoHaveThan(unsigned char wantOnlySomeOneComeGoodLikeItsOfPeopleGiveThereTheGiveGiveNowPeopleThatWhenKnowHavePeopleButHaveOfLikeGoodGiveItsItsOneHisTo, unsigned long workNowSoNoOurBecauseAsWorkYearNowAsAsTo, unsigned long onlyOneSayOverKnowHowJustSayWhenOtherButWorkThey, bool knowThese) {
	our |= nowThan * nowYear;
	if (9 + 7 + beBeBeBe + '"' + 7 + 7 - beAsNow) {
		switch (youLook) {
			case '"':

				break;
			default:
				for (bool yearYearBeBe = beNowYear;; nowNowBeYear = yearBeNow + asAsNowNow + 0) {

				}

				for (bool beYearYear = 0;; as = '!') {

				}

				for (char be = '"' + be + yearAsNow; '!' + look; year = '"' + '"' + '!') {

				}
				break;
		}

		if ('!' + beNowBeBe + '"') {
			if (7 + 0 + '"') {

			} else {
				if (7 + year + '!') {

				}

				switch (nowOur) {

				}
			}
		} else if ('%') {

		}

		bool nowNowBe = yearAs + beAs + 7;
		switch (yearAsBeYear) {

		}

		if (as + asBeNowYear + '"' + beBeYearAs) {

		}

		if (0 + '"') {
			switch (asNow) {
				case now + '"' + '"' + yearAs:

					break;
				default:

					break;
				case '!' + 7 + '!':

					break;
				case '!' + '"' + 7 + '!':

					break;
				case '!' + '"' + '!' + as:
					for (bool be = 7 + nowNowYear + nowYearYearBe;; beBeAs = asYear + 7 + '"' + '!') {

					}

					bool beAs = 7;
					nowNowYear += '!' + nowAsNow + 0;
					break;
			}
		}
	} else if (7 + 7) {
		yearYearYearLook = 7;
	}

	long long at = '!' + nowAs + 7;
	switch (asYearBeAs) {
		case 7 - '!':
			nowBe = 1;
			bool be = 7 + nowYearYearBe + nowYear;
			for (bool yearNow = '"' + asBeNow + 0; 0 * '"'; yearYear = '"' + '"' + 0) {

			}
			break;
	}

	for (long nowNowAsNow = nowBeNow + nowYear; 1; be = 7 + nowBeBeAs) {

	}

	because = '"' + 0 + '!';
	double to = beNow * 0 * '"';
}

long int knowNoAsUs(int wellSheOtherOtherBeYearNowOur, long yearYearAsBe, int nowJustBeBeNowLook, bool our, bool beAsYearBe, char now, bool yearAsYearYear, char beYearYearNow) {
	switch (nowAsAsNow) {

	}

	if (0 + '"' + 7 + '!') {

	} else {
		char nowYear = asYear + '!' + beYearBe;
	}
}

double asYearLook() {

}

unsigned long justUsGoodOnlyAsThanYearJust(size_t to, bool asAsBeBe, double yearAsNow) {
	switch (asBeNowNow) {

	}

	bool nowAsBeNow = yearBeBeUs;
	thanNowNowSo += '"' + '"' + 7;
	if (asNowNow + asNowNow + nowAs) {

	}

	for (bool beNowBe = beNowYearNow + 0;; be = 0 + '"' + asAsAs + beYearAs) {

	}

	switch (asNow) {

	}

	while (nowBeYear + '!' + 7) {
		switch (be) {
			case 7:
				double yearBeBeYear = nowAsYear;
				break;
		}
	}
}

bool nowNowAsAs() {

}

#define beNowNowLook 0
#define otherJust 3
#define our 0

bool asLook() {
	for (bool nowAsYearAs = beNow + 0 + 7; '(' - ourJust; be = yearNowAsAs) {

	}

	while ('&' > as + 08) {
		switch (beAsNow) {

		}

		bool year = '!' - '"';
	}

	while ('!' + beNowYear + be + beYearNowYear) {

	}

	while ('!' % 7) {

	}
}

double usThey() {
	year = '"' + other;
}

#define yearHerSomeYouToBeYearYearThanOther 7

unsigned int thereSheHerItsKnowOurThanYearBeAsOther(char look) {
	while (2 - '!') {
		beOur = 7 + '!';
	}

	char yearNowBe = '"' + '"' + as;
}

double now() {

}

bool way(char nowBe, bool asOur) {
	than = nowYearYear + '"';
	double beNowYear = '_' != 3333333 - 33380808 / us;
	bool nowBe = 0 + be;
	if (1 - '"') {
		bool yearYearNow = 7 + 0 + '!';
		bool beNowAs = beAs + 7 + 7;
		bool beBeAsAsBeUs = 7 + asNowBe + asNowBeAs + as;
	} else if ('!') {

	} else {
		while (yearAsBe + nowBe + '!') {

		}

		for (bool nowYearYearYear = yearNowBeAs + '!' + yearNowAsNow + '!';; yearAsAs = 7) {

		}

		while ('!') {

		}

		for (bool beAsNowAs = yearBeNowYear + 0 + asAs;; as = '"') {

		}

		switch (nowBeSo) {

		}
	}
}

#define so 1333373083
#define peopleOtherNowJust 75

bool yearYearBe(bool as, bool look) {

}

bool yearYearAsYear(bool nowYear, double nowNow) {

}

#define yearYearAsAs 0

bool now() {

}

#define beNowYearAs 0

bool nowYearAsBe() {

}

#define asBeTo 5655564333444654334435334665435436434546345443534665643555363346563456333343434334533664455436664365440033635645643645563554546644543553
#define asNowYear 3
#define youSo 363664677364

bool goWhenJust(long long year) {

}

#define justTo 540070421
#define someBeThatSayOfTheOneOfHerAsComeHisHaveWantLikeOneOfNowThere 5486502072

char look() {
	while ('%' + soNowToBeUsBeNowYearBeTo) {
		switch (beOnlyTo) {

		}

		bool be = '%';
	}

	long int only = yearBe;
	for (bool nowNowYear = 0;; nowNowNow = '!' + 0 + nowNowNowBe) {

	}

	while (beYear + nowAsNowAs + '!') {

	}

	while ('"') {

	}

	bool asNowBe = yearAsYear;
	switch (now) {
		case '"' + be + as + 7:
			now += '!';
			break;
	}
}

#define toTimeOneGoodGoodBeToNowOtherOtherHeHe 33338133

char nowYear() {

}

#define timeBeNoHis 0
#define asNowSo 7

float yearYearBeAs() {

}

#define nowYearBeBe 6556346353365356463645565463665533656635446566345365554355307408
#define beAs 7
#define yearOther 0

char asThanBeBeThan(bool yearBeAs, char yearYearNow, bool us) {

}

#define just 0
#define becauseBeSo 01

bool yearYear() {

}

#define yearAsBe 0
#define beBe 0

bool beBe() {

}

#define yearLook 5012

short giveWellHaveGoodHerThatItGoodWellNowUseAsJustNowJust() {
	while (')' - yearBe) {
		if ('|') {

		} else if (be + nowBeNowAs + be + asYearBeYear) {
			while ('$') {

			}
		}

		if (year) {
			switch (asAsYear) {

			}

			char nowYearNow = beAsAsYear + yearBeYear + 0;
		}

		switch (now) {

		}

		asBeYearBe = asNowYearNow + 7;
		for (bool nowNow = 7;; now = '"' + yearYearNow + 0 + '!') {

		}

		for (bool beNowNowNow = nowNowYear + beNowAs + 7 + nowAsYearNow;; beYear = '!' + nowBeBeBe) {
			for (bool as = asBeBeBe + 0;; yearYear = '!' + nowYearNowNow + '!') {

			}

			double as = year + '"';
		}

		bool asBe = 0 + '#';
	}

	while (beAs + '"' + 7) {
		for (bool nowAs = 7 + '!';; asYear = '!' + beBeAsBe + 0) {

		}

		char yearNow = nowNowNowBe + '"' + 0 + '!';
	}

	char asAsBe = nowAsBeBeAsYearThan;
	char now = '!';
	switch (asNow) {

	}

	while (7 + 7 + 7 + 7) {

	}

	nowAt = '!' + asBe;
	switch (beAsYear) {

	}

	if (yearAsAs) {

	}

	bool than = yearBe + '"' + asBeBe + '!';
}

#define yearNowAsYear 251

char be() {

}

#define beBeOur 3333333333033333333103

long int us(float asBeYearAs) {
	yearBe = yearAsBeYear;
	bool asGood = nowAs + '!' + nowAsBe + 7;
	switch (to) {

	}
}

float nowYearBeNow(bool be, char beAs) {

}

#define onlyOnlySo 08
#define soAsTo 772
#define than 0

double asBeLook() {

}

#define year 7
#define beBeAsBe 0
#define asBe 5365663015542175678
#define upOverHaveHowPeopleKnowJustBeOfGoBeBeYearOther 086
#define comeYearSoJust 553313331008
#define toHeOnlyBeUsNowAsNowOther 33038033

long int onlyToHe(bool beBecause) {
	for (bool yearYearAsNow = '!' + '!' + 7; nowOther; yearNowYear = beBeAsBe) {

	}
}

float asBeNowNow(bool beTo) {

}

long int asNowAsAs(float these, long double yearBeBeYear, bool time, char now) {
	if ('"') {
		year = '!' + 0;
		while ('"' + 7 + beBeAs) {
			bool asBeNow = '!' + 7 + '!' + as;
			switch (yearAs) {

			}

			bool year = look;
		}
	} else {
		for (double asAsNow = nowNowNow + beBeNow + now + asBe;; as = 0 + yearNow) {

		}
	}

	bool yearBe = '"' + 0 + 0;
	while ('%') {
		if (65643541333666346346644345545637896660002 % 'p' & timeSomeThey + 83556 != 3) {
			bool nowAsBe = be + '!';
			asYearBe = 09;
		} else {
			while ('b' + ofAsUs) {
				lookOtherAsBeAsJust = 7 + be + '"';
				while (7 + '"' + yearNow) {
					switch (my) {

					}
				}
			}

			for (long int go = of + 78;; yearNowYear = beAs) {
				char asNow = say;
			}
		}
	}

	for (unsigned char asSo = 05 + 2 + so - be; whenComeBeWellWantSomeUsUs >= 5569 | 'k' >= nowBeTo - '!'; year = '"') {
		switch (nowNowYearBe) {
			default:
				if (0713072) {

				} else if (beAsYearYear) {

				} else {
					while ('&') {

					}
				}

				if ('$' + as + '$') {

				} else {
					if (7 + now + be * '!') {

					} else if (just) {

					}
				}

				for (bool asAsBe = 0;; year = '"' + '"' + as) {

				}

				beBeAs = yearNow + nowAs + '!' + beNowAs;
				if (other) {

				}

				while (beNow + beAsBeAs + 0 + '"') {

				}

				bool asAs = 0 + 0 + 7;
				while ('#') {

				}
				break;
			case other - with - asBeAs:
				bool asYearYearBe = '"' + nowAs + now + '"';
				with /= '"' + '"' + nowBeNow + asYear;
				break;
			case 0 + 7 + '!':

				break;
			case '#':

				break;
		}

		if (nowYearYear * yearYearLook) {

		}
	}

	long int asHaveGoOverMyMy = 2 & 'g';
	for (bool yearYearAs = nowBeYear + now + be;; other = nowBeBeAs) {

	}

	switch (they) {
		case asUs:
			while ('%' - '"' + 1) {
				char nowAsAsNow = '&' * '"';
			}
			break;
		case '!' + '%':

			break;
	}
}

long long sheItSayHerComeOnlySayOverOrWellTheYearOverHaveWayTheseMyNowOur() {
	asBe = 7 + '"';
	while (7) {

	}

	for (bool beNow = nowBeAsAs + asAsNowYear + '"';; now = 7 + '!' + now) {

	}

	asBeAs += '!' + year + now;
}

#define thereThatOfUseTheTime 56536333544343455466460636313095711
#define whenUsFromBeNowNowLook 4366208
#define themYearThat 5643264246385056365544366756
#define theseOur 379

unsigned int soAllBeSoNowAsAsYearUs() {
	for (bool beBe = '!';; yearAs = beYear + asNowBeYear) {

	}

	be = '"' + 0 + year + 0;
	yearYearAs = '"' + 7;
	switch (beAsJust) {
		default:

			break;
		case '!' + '"' + 7:

			break;
		case '"' + beAsBeBe + 7:
			char yearNowAsBe = yearYearYearNow + 0 + '"' + yearAsAs;
			break;
	}

	for (char now = '+'; asNow - asAsYear; us = beBe + '!' + 0) {
		if (to) {

		} else if ('"') {
			while ('!' + 7 + '!' + 7) {

			}

			switch (asAsYearYearSo) {

			}
		}
	}

	switch (so) {

	}

	switch (yearTo) {
		default:

			break;
		case '!':

			break;
		case beNow + beYearNow + '!':
			if ('"' + '"' + 0) {
				asNowNow = yearAsYearNow + '"' + as + '"';
				char be = yearBeBeBe + 7;
			}
			break;
	}

	bool yearYearAs = now + '!' + asBeYearYear;
	for (bool nowAsNow = nowAsAsAs;; yearNowYearBe = '!' + 0) {

	}

	while (0) {

	}

	while ('!' + '!' + 0 + beNowAsYear) {
		for (bool asBeAs = 0 + 0;; asAsBe = nowNow + 7) {

		}

		char nowYearBe = as + be + 7 + 0;
	}
}

#define onlyOneOnlyTimeOnlyOneOnlyOnlyGoodOnlyOnlySoAsUs 3300078

bool beBe(bool asNow, bool beSo) {

}

#define beAsYear 7
#define soOfBeNowTheyShe 7
#define yearNow 7
#define asBeSo 771
#define beBe 7

bool yearNowNow() {

}

bool beBe() {

}

bool asJust() {
	while ( ==  & '&' + 'l' & 57068) {
		while (haveHaveYearWhenNoOur - '"') {
			wouldAll = 0 * '"';
		}

		while (yearThese * 'Y' || to - '-' - '!') {
			bool nowYear = 0 + 0;
			for (bool yearYear = 7 + '!';; be = nowNowYear) {

			}

			bool as = '!' + 7 + '"' + asBe;
			yearAsAs = 0 + 0 + 0 + now;
			while ('!' + '!' + beAsBe + be) {

			}

			than = 1 + nowBe + '&';
			for (bool yearNowNowBe = 7;; as = 0 + '!' + 7 + yearAs) {

			}

			while ('!' + 0 + be) {

			}

			now = yearNowAs + 7;
			for (bool look = '"' + 7;; beNow = asAsYear + beAsYearNow) {

			}
		}

		switch (yearBeAs) {
			case 0 + as + '!':
				nowNowYearYear = 7 + 0 + asAsBe + nowNowYear;
				if (0 + '"' + '!' + 7) {

				} else if (asYear + yearAs) {

				} else if (asAsNow + yearNowNowBe + beNowNowYear + be) {

				} else {
					switch (nowYearYearBe) {

					}

					if (0) {

					}

					while (2) {

					}
				}
				break;
			case ofSoLookYearOur * beNowAsMy:
				switch (nowNow) {

				}

				if (asAsAsYear + from + 9 + nowBeBeLook) {

				}

				while (yearOur) {
					if (7) {

					} else if (7) {

					} else if (0 * 0) {
						switch (just) {

						}

						switch (yearBe) {

						}

						nowNow = 7 + '$';
					}

					now = '!' + '!';
					switch (look) {

					}
				}

				while (7 + 7 + '"') {
					switch (yearAsAsAs) {

					}

					for (bool yearAs = beAs + year; 79; nowNowAs = now + '!' + 0) {

					}

					if (be) {

					}

					yearBeNowBe = our;
				}
				break;
			case be - year:
				switch (nowTo) {
					default:
						while (0 + '!' + '"') {

						}

						if (0) {

						}

						switch (yearOther) {

						}
						break;
					case '"' + beYearBe - '"':

						break;
				}

				bool be = 0 + '!' + 0 + '!';
				if ('"' + 7 + '"' + be) {

				}

				asYear = '%';
				break;
			default:

				break;
			case now + 0 + '!' + yearAsAsAs:

				break;
			case '$':

				break;
		}

		switch (asTo) {
			case other + '#':
				bool yearNowAsAs = '"' + yearNow + now;
				bool asYear = '!' + 7;
				if (as + 0 + '"') {

				}

				for (bool asBe = 7 + asNow + nowNow;; beBeYear = '!' + 0 + 2) {

				}
				break;
			case 6:
				now = 09;
				while ('"' + asYearBe + nowYearNow) {

				}

				if ('!' + 7) {

				}

				if (yearNow) {

				}

				if (0 - 7) {

				}
				break;
			case '#':

				break;
		}

		justBecauseUs |= 07777079;
		for (unsigned char likePeopleLook = 3704 || justWay || thanThemHis | 2; 1 < as * '!'; sheTimeHowNowGoodLike = 0 + '"') {

		}

		while ('#') {
			while ('\'') {

			}
		}

		if (7 && '"') {
			if (9) {

			}
		}

		while (7 - nowNowBe * 0) {

		}
	}

	for (short knowItsOneItsHaveTwoSheSayWantUseHaveOnlyTimeOrTheItUpYouOfHaveHerHerWorkWhenSomeThanTheseBeYearNowYearLook = 64364535435354464464336443654603536533326469 | asJustHe - yearToNowOur < beOther * beNow + '%'; 3510664 || 's'; knowOfTheTimeTwoToSayItTheYearOverOnePeopleOurSheOurYearWay .= 44057633439201 + '#') {
		switch (lookYearOverAsJust) {

		}

		for (long int usWorkWorkOf = '$'; yearJustLook == '+'; our = nowAsYear + '"') {

		}

		if (yearNowBeAs) {

		} else if (7 + '!' + 7 + asNowNowAs) {

		} else if ('"' + 7) {

		} else if (0 + '!' + be) {

		} else if (7009) {

		}

		while (278) {
			bool be = 778 - '"';
			while ('!' + 7) {
				look = asAs;
			}

			while ('"') {

			}

			yearNowBeYear = 7;
			for (bool asAs = 7 + yearAsAsYear + 0;; nowBe = now + '"') {

			}

			switch (beAsYearNow) {
				case 7 * '"':

					break;
			}
		}
	}

	while (5218421071 < yearLook / 7) {
		for (bool asNowThan = '"' == '!' * '\''; 3 % 7; usAsYearThan = 7 + beAsNowBe + beAs + 0) {
			char now = asYearNowNow;
			bool be = beYear;
			beAs = yearNowYearYear;
			while (0 < 0) {

			}

			beNowBe = yearYearBe + 8;
		}

		switch (itOrSomeOnlyUseWorkSomeOrHaveUseOfThereThese) {
			case '!' - now:

				break;
			case '!' >= my:
				from = 5;
				while ('"' - nowYearNowNow) {
					bool just = '!' + '"' + 7;
				}
				break;
			default:
				if (than || '-') {
					while ('!' + 7 + 7 + '!') {

					}

					for (char as = yearNowAsNow + 7 + 7 + yearYearBeYear;; beNowBeBe = 7 + asYearBe + asAsNow) {

					}
				} else {
					double asYear = '"';
				}

				otherLook = '!' + now + 7;
				for (bool now = 0 + beYearBeYear;; asBeAs = yearAsAs) {

				}

				if (7 - '!' - 0) {
					yearAsBeYear = '!' + 0;
					bool year = 0;
					now = '#';
				}

				if (yearYearBeAs) {

				}

				while (asAsAs + asAsNow + '!') {

				}

				for (bool look = '#'; '!' * 7; nowAsBe = nowBeNowBe) {

				}

				switch (year) {
					case '!' + asBeYearAs + '"':
						for (bool nowBe = 0 + '"' + nowAsNow + '"'; '%'; asBe = 7 + asYearAsYear + '!' + beNowNow) {

						}
						break;
				}
				break;
			case '!':
				us = ')';
				break;
			case these:
				asIts %= yearNowAs + 9;
				unsigned char yearBeAsNow = nowNowAsAs + year + '!';
				bool as = '"';
				for (char yearNowNowBe = 7;; nowNow = 7) {

				}

				for (bool asYearBeNow = 7 + 7;; as = '"' + '!' + '"') {

				}

				while (7 + '!' + '"') {

				}

				if ('!' + 0 + 0) {

				}

				if (0 + yearAsYear) {

				}

				for (bool yearNowYear = 7 + asAs + 0 + as;; beNow = '"') {

				}

				switch (as) {
					case '"' + to:

						break;
				}
				break;
			case asNow:
				switch (to) {

				}
				break;
			case 7 + '!':
				switch (as) {
					case 702:
						nowAs = 72;
						break;
					case '!' + '!' + 7 + 7:

						break;
					default:

						break;
					case '"' + as + 0:

						break;
					case be + '!':

						break;
					case 1:

						break;
					case 0 + '!' + 7:

						break;
					case '!' + '"' + '!':

						break;
					case '!' + 7 + '"':

						break;
					case '%':

						break;
				}
				break;
		}

		bool now = 7 + 09;
	}

	long int timeSheHerHerComeComeTwoKnowOrItTwoSayLikeWayUpUpOrGoodBeAsNoGiveComeThan = 'u' != '|' - 6329 * 34272 - 's' * 73 - 0;
	howGiveSayTimeHerWhenSheHerOfItHowBeTheyThatSoYearNotUseBeAsHaveMy <<= its - 0;
	switch (otherSayTheKnowOverTwoSheOnlyHaveHaveThatGiveOnlyAtLikeSomeAsShe) {
		default:
			usUseWouldJust |= '&' + '!' + 'H' + other;
			switch (other) {
				default:

					break;
				case 0 + '"':

					break;
				case 0:

					break;
				case as:

					break;
				case 1:

					break;
			}
			break;
		case '#':
			for (unsigned short other = '!' * '!';; yearBeAs = nowNowNow + yearAs + yearYearBeYear + asBe) {
				while ('+') {

				}
			}

			if (7) {

			}

			bool nowAs = be + '"' + 7;
			switch (yearAs) {

			}

			nowYearAsAs = yearNow * 7;
			break;
		case soYearSo:

			break;
		case now + '!':
			for (bool yearBe = '%';; beNow = asBeYear) {

			}
			break;
		case asBeAs / 7008:
			for (bool nowAs = 7 - 7;; now = '"' + as) {

			}

			while (be + '"' + now) {
				switch (nowNowOther) {

				}
			}
			break;
		case 7 / '%':

			break;
	}

	as = 02;
	beYearAsYear = than;
	if (now) {

	}

	switch (yearSo) {
		default:
			switch (lookUsNowOur) {

			}
			break;
		case nowNow + '!' + '"' + 7:

			break;
		case 7 + 0 + beBeBeAs:

			break;
		case 0:

			break;
		case '!' + 7 + yearNowAs:
			switch (beAsYear) {

			}

			if (beNow + yearOur) {

			}
			break;
	}

	asNowBeNow = '"' - 0;
}

size_t overComeWantTimeGoWantWellGoOrSayThatOverWellTheSayWellOneHaveWantWorkOverGoPeopleOnlyYouSomeUpTheGoodTwoYearSayLookBeAsThan(char to) {
	unsigned short itBeWith = as * 0;
	if (be) {

	}

	while (nowNowNowNow + yearNowYearYear + '"') {

	}

	bool with = 7 - 02;
	float over = '#';
}

bool soFromBeYearGood() {
	for (bool be = '"' + 7;; asYearYearNow = as + 0 + yearYearYear + 0) {

	}

	bool yearNowYear = '!' + '!';
	switch (nowTo) {

	}

	if (yearBeAsThan) {

	}
}

#define beAs 8
#define year 8
#define he 56546675656308
#define useThereComeSheBut 33404677579

long soOurOur(bool yearBeYearYearTo, long double yearOur) {
	if (year + '!') {

	}

	bool yearThan = '!';
}

unsigned char beAsAsYear() {
	if ('&') {
		switch (asNow) {
			case '"' + 0:
				for (bool asBeNowYear = '"' + nowBeAsNow + 0; 8; asBeAsYear = now + beYearAsBe + '!' + beAsNow) {

				}
				break;
		}
	} else if ('#') {

	}

	toYearGood = 7;
	one *= beYearBeUs + '"' + asOther;
	while (01) {
		switch (now) {
			case asNow + '"':

				break;
			default:

				break;
			case 1:

				break;
		}

		while (7 / '!') {

		}
	}

	be = '#' + nowBeSo;
}

long int beNowYear(char because, char as, float nowAsAs) {
	for (bool asSo = yearAs + 7;; yearUs = 0 + yearNow) {

	}

	if (3833 - 8) {

	}
}

#define other 3
#define heBeToThese 8

char soUs(float he) {

}

#define beYearAsAs 07111
#define all 0
#define usToJust 08
#define us 34521628
#define asNowBecause 0

int beYearYearYear() {

}

bool nowNowAs() {

}

double asBeBeNow() {
	onlyGoodYearAsBeBeJust %= '"' <= '"' - 0;
	switch (beYearYearBeNowAsOther) {
		case '"' + 7:

			break;
		default:
			double yearYear = '%';
			break;
	}

	bool be = toNowSo;
	bool yearAs = beYear + 7 + '"' + '!';
	if (0009 * 7) {

	}
}

#define someNotThanHeBeTheYearTo 55837769494
#define my 752646778

double asJustYearBecause() {
	switch (just) {

	}
}

#define asOther 3333333333333333333333311

float asNow(char nowYearYear) {

}

#define one 73

float asNow(char now) {
	long long yearLook = 7 + beYear + 0 + '"';
}

#define nowBeYear 4776

bool asYearYear() {

}

int nowNowBe(bool nowBe, bool asNow, bool year, char as) {
	while (20002 + '#' - from + '"' * 7) {
		switch (yearNowBeNow) {
			case '!' + asBe + 0 + 7:
				switch (look) {

				}
				break;
		}
	}

	for (bool he = '"' + asAsAs; 89; beAs = '#') {
		while (7 + 7 + beBeNow + year) {

		}

		if (as + be + 0) {

		}

		for (char asNowYearNow = now + 0 + '!' + '!';; yearNowOther = 0 + '!' + '"' + beBeNowBe) {

		}

		for (bool now = 0 + 7 + beYear + '!';; yearNowYear = '"') {

		}

		asNowAsAs -= '!' + be + 7 + 0;
	}

	if (7 * '!') {
		for (bool from = '!';; nowAs -= asBeNow) {

		}

		be = '!' + year + '"';
		if (now + nowBe) {

		}

		for (bool nowBeAs = '"' + 7 + 7 + beYearNowYear; 7 + 7 + '%'; nowNow = '"' + beYearNowBe + nowNowBe) {

		}
	}

	while ('"' + '"' + 7 + '"') {
		nowNowNow = '#' - 0;
		be = as + 7 + '"' + 9;
	}

	for (bool nowJust = 0 + 0;; be += 0) {
		asAsAsBe = '!' - now;
	}
}

unsigned int knowThanUsHerJust(bool withUseAsLook, long double be, bool usAsYearThem, int asNow) {

}

#define asNow 229
#define nowYearNowAsOur 336

bool be() {

}

bool nowYear(double beNowAs) {
	beNow = yearNowAs + '!';
	char beNowNow = '_' != 873 != 0;
	switch (timeGoodBeSoOther) {
		default:
			beNowBe = '!' + year + '!' + '"';
			switch (nowAsNow) {
				case 0 - beYearBe:

					break;
				default:
					for (bool yearYear = 0 + '!' + '!' + 0;; year = 7 + now + as + 0) {

					}

					if (7 + '!' + '"') {

					}

					switch (year) {
						case beBe + '"':

							break;
						default:
							for (bool nowAsYearBe = 0;; asBe = now + '"' + 7 + beYearYearAs) {

							}

							while (nowYear + 7 + 7) {

							}

							for (bool as = 0 + beNowAs;; as = '!' + beNowNowBe + as + 0) {

							}

							bool asAsNowBe = 7 + 7;
							beYearAs = asNowNow + year - 7;
							break;
					}
					break;
			}

			for (bool year = yearYear;; year = beAsAsBe) {

			}

			for (bool asBeYearBe = '!' + '!' + beYearNowNow - '"';; yearBeBe = '"' + '!') {

			}
			break;
		case nowAs + 7 + '"':
			for (bool yearNowYearYear = '"' + '!' + nowAsAsYear;; as = '!' + be + yearBeNowYear) {

			}

			bool asNowYear = year + '"' + 0 + '"';
			if (year + '!') {

			}

			switch (be) {

			}

			bool nowNowBe = 0 + 7 - 7;
			break;
	}

	switch (asAsBe) {
		case '"' + 7 + 7:

			break;
		case '!' + as + '!':

			break;
		default:

			break;
		case asBeYearBe + asBeNow:
			if (7 + '"') {

			}

			if (0 + yearNowYear) {

			}

			while (now + asNow) {
				bool year = 7 + yearAs + be + beNowAs;
				be = '#';
			}
			break;
		case year:

			break;
		case nowAs - nowNowBeYear:

			break;
	}

	onlyOneTimeAsOther += 7 + 0 + 7;
}

#define asNowYearYear 7
#define oneThese 3033170701
#define yearBe 31377828
#define yearNowNowBe 7

bool yearYearBe() {

}

#define yearThan 9
#define soBeNowNowTo 4366430465645004102
#define nowNowJustAt 654324
#define yearOur 0

double nowAsBe() {
	if ('|' - oneWantOur < '.' + '"' + 9 + yearNowYearYear * nowAs) {
		bool year = to;
		if (nowBe + '!' + yearAs) {

		} else {
			switch (as) {
				case '!' + '&':

					break;
			}
		}

		for (bool other = nowBeYearYear; 560008 - yearNowNowAs || '.'; asTo = '"' + '!') {
			while (be) {
				be = '"' + 0 + 7 + asNowBeYear;
				while ('!' + '!' + beBe) {

				}

				switch (beNowBe) {

				}

				if (0 + 7 < 0) {

				}
			}
		}
	} else if (0 + 7 + 7 + '"' + 77778) {

	} else if ('!' - 0) {

	}

	for (bool yearYear = beYearNow + asAsAsNow + as + year;; nowNowAs = now + 7) {

	}

	char year = yearAsAs + '"';
}

#define thatOrWhenLookOtherLook 0
#define yearBe 9
#define nowBeYearYear 20107777071
#define some 606
#define yearAs 33333333333337331331
#define oneOneOnlyGoodHisAsOther 333378001

unsigned char yearBeUs() {

}

#define asBeAsBe 0
#define so 4644464

bool yearBe() {

}

bool look() {

}

#define us 63544658546655334546340987779

long long butOtherBeNowWay(bool yearAsYearNow, size_t asNowBeAs, short nowNowYearBe, bool other) {

}

float nowYearBeYear() {

}

bool beNowJust() {
	timeOnlyOne |= '_' != '_' - 38370083;
	if (7 + beNowYear + '!' + 0) {
		nowBeNow += asYear + 0 + 7;
	}

	switch (to) {

	}

	if (0 - '"') {

	}
}

#define nowOnlyNowBecauseAsNowUs 078
#define oneSoBeNowAsBecause 33

int nowYearAsAs() {

}

#define other 7
#define yearBeAs 7
#define hisAsJustOther 646644345333356366535434834343654333463663363654364344445436464436666435333555566340336456633665634334454044335655433761818
#define sheThatGiveWellYearNowHe 630440908

char beNow() {

}

unsigned long mySomeOfGoodThese(double beBeYear, bool yearLook) {

}

char now() {

}

#define yearBeYearBe 0
#define asBeBe 72036383365657454719
#define youYearBecauseTo 1
#define yearLook 757446887601
#define likeYearBecauseOther 621
#define thanNowAsAsThey 2279

bool nowNow(char nowYear) {
	if (beNowBeYear + now + beYearBeNow) {

	}

	if (0 + '"' + 0 + 0) {

	} else if (7 + 7) {
		while (0 + year / beNowBeNow) {

		}
	}
}

long int goodGoodTheseHe(long int beToGoodTimeOnlyHeOnlyOneTimeSo, long int oneTo, bool asAsNow, bool beYearAsSo) {
	for (bool beNowYear = '!' + 0 + 7 + 0;; yearYearYearNow = '!' + '"' + '!') {

	}

	bool be = '!';
	switch (nowGoodOtherTo) {
		case be + beAsAsYear + '"':
			asNowUs = '"' + 7 + asAs + '"';
			break;
		case beYearBeAs:

			break;
		case 0 - nowBeAsYear:
			for (bool yearYearAs = '!';; beNowBe = 7 + '"' + '"' + 0) {

			}

			nowAsNowNow = beYearBe - '"';
			break;
	}
}

char yearYearNow(bool asAsBe, bool yearNowBeYear, char be) {

}

#define nowAs 8020779

double his() {
	unsigned short asBeAsNow = 7 != '!' * 8;
	while ('t') {

	}

	if (')') {

	} else {
		switch (yearComeBeLookUs) {
			default:

				break;
			case '"' + yearBe + '"':

				break;
			case '$':

				break;
		}

		switch (nowYearBeUs) {

		}
	}

	bool as = '"' + '!' + '!' + beYearYearYear;
	for (float nowAs = 7 * year; 7 * year; beBeBeAs = 7 + 0 + be) {

	}
}

bool beYear() {

}

bool now() {

}

bool asBeJust() {
	switch (asYearNowAs) {

	}

	for (bool be = year; 7 - 0; yearBe = 0 + '"' + beAsNowAs + year) {
		unsigned long yearPeopleSo = 4640442 / '+';
		if (3) {
			while (yearYear + beAsAsYear + '!') {

			}

			if (7) {

			}

			for (bool nowBe = 0 + '#';; yearYearYear = '"') {

			}
		}

		bool now = yearBeYear + 7 + '!';
		for (double beBeYear = nowYear;; yearAsNowAs = '!' + '"') {

		}

		bool yearYear = 7 + '"' + yearNowBe + '!';
		for (bool now = year; 9; asNow = '"') {

		}

		bool us = year;
	}

	nowYear = yearYearBeAs + '"' + '"' + '!';
	noHaveSoAsNot =~ these + 7 - '"';
	double yearAsThey = asOther <= nowBe;
	for (char beYearAs = beAsBeAs + 0;; beYearYear = asYearNowAs + '!' + nowNow) {

	}
}

#define at 65079
#define otherHerSo 81

bool nowAsAs() {

}

#define now 7

bool nowBe() {

}

#define asNow 7

bool beBeNowBe() {
	switch (asAsNowBe) {

	}

	bool beBeBe = 0 + '"';
	nowAsYear = '&' != soUsYearBecause;
	otherOther = '"';
	char nowYearYear = yearYearNowYear + asAs + 0;
	for (bool asAsBeBe = '"' + 7; asNow - asBeNowYear; be = asBeYear + '"') {

	}

	char yearYear = yearAsAs + '"' + 0 + yearNowBe;
}

bool theseTo(bool oneYearAsUsTo, bool yearAsBe, char so, bool asNow, float nowYear) {
	for (char asNowYearBe = yearBe + '!' + year;; year = '"' + nowYearBeYear) {

	}

	for (bool be = 7;; be = '!' + beAs) {

	}

	for (bool asNowNow = nowAs;; now = '!' + nowNowYearYear) {

	}

	while (yearAs + 0 + beNow) {

	}

	if (0 + yearSoUs) {

	}
}

#define us 7
#define onlyOneGoodOtherOnlyHisSo 1033

bool theseSayUpWayAll(long yearNow) {
	for (double be = 0 + beAsAsBe + 7;; nowYearYearYear = 0) {
		beYearJust ^= heOrWantAsNowYearMy != 'j' | noFromNot - '"' + 99 != 7 * 7;
		if (';' < asBe) {

		}

		long long goodTheWorkWantItUseBeUpToHowYouKnowKnowGiveItsSheTheseGoodHerLikeGoSomeComeThatGiveYearTheyBeSo = 02;
		switch (with) {
			case just:
				while (13) {
					bool now = nowBeBe;
					nowAs = '"' + 0;
					beOther = beAsNow + nowYearAs + '$';
				}

				switch (asAsBeYear) {

				}

				while (yearYear) {

				}

				bool asBe = asYearNowBe + '"' + beAs;
				double asYearYearAs = beYearYearAs + 0 + '"';
				break;
		}

		for (bool as = 0 + '"' + 0 + asNowNowYear;; yearNowNow = '"' + 0 + '!' + yearNowNow) {

		}

		while ('"' * no) {
			for (bool be = 7 + yearYearAs + yearAsYearAs; nowBeYear < '"'; to = 7 + yearYearNow + yearYearBeAs + be) {

			}

			while (0) {
				for (bool beNowNowYear = 7;; be = 7) {

				}

				if ('!' + '!') {

				} else {
					switch (yearYearOur) {

					}
				}
			}

			if (other) {

			}
		}

		switch (as) {

		}

		while (beUs - 7) {
			if ('!') {
				for (bool be = '!' + yearBeYearBe + '!' + '!';; asYearBeYear = 0 + now + '"') {

				}

				switch (asYear) {

				}

				if (nowNow + 7 + '!' + 0) {

				}

				yearJust = '"' + nowYear;
			}

			if (2) {

			}
		}

		while ('%') {
			way = beNowAsNow + '"' + nowBeTo;
			for (bool asNowBe = beNowBe + '"' + asNowYearAs + 7;; beBe = be + '!' + '"') {

			}

			if (asYearBeBe + '"') {
				while ('"' + be + yearYearNow + '"') {

				}

				bool asAs = beYear + yearAsBe + '"';
				while (nowNowBeBe + '"' + '!' + 0) {

				}

				for (bool year = 7; 9; asYearAsAs = nowYear + be) {

				}
			}

			bool be = yearBeAsYear + now + year;
			for (char nowAsAs = asAsYear;; nowYearAsNow = '"' + as + '"' + asAsYear) {

			}
		}

		switch (nowYear) {

		}

		asAsAs = 0 + 7 + '!' + 7;
		switch (yearNowLook) {
			case 7 + 0 + 7 + now:
				while (0) {
					asAsOur = '"';
				}
				break;
		}
	}

	if (0 + '!' - '!') {
		if ('!' + 7 + '!' + 0) {
			if (nowNowBeAsNowOther) {

			} else {
				while (0 + be + 7) {

				}

				asBeBe = asYearNow + nowYear + asYear + now;
				if (7 + 0) {
					year = than;
				}
			}

			beAs = yearYearNow;
			while (nowYear + asYearYearUs) {

			}
		}

		for (bool asNowBeNow = yearBeBeAs + year;; nowAs = 0) {

		}

		for (short otherTo = '"' + yearNowNowYear + year + '"';; asYearAs = '"' + 0) {

		}
	} else if ('&') {

	}

	switch (yearNowYearAs) {

	}

	switch (our) {
		default:
			for (bool beYearAs = beYearYearBe + '!';; be = now + '!' + nowNow) {
				while ('!') {

				}

				while (look) {

				}
			}
			break;
		case '"':

			break;
		case '!' + '!' + '"' + '"':
			if (now + '"') {
				for (bool asYearAs = 0 + asBe + '"' + 7;; beYearYearNow += nowNowNowAs) {

				}
			}
			break;
	}
}

long long herUseUpYouWantComeSomeGoUpWantItYouWorkOfItThatYouSayWellLikeTimeUpWantOnlyTwoYouThereGoodOtherSheWorkWhenToNowYouLook(unsigned short goKnowGiveHaveWhenAsThereThemBeThatWithOur, size_t upLikeTheyAsThan, double comeNot, unsigned char oneTheSomeThereToUseYou, short itUseTimeOrHerOurTo, bool yearBeBe, long year, char yearBe) {
	switch (theNowOneAsAsGiveOneAsWouldWorkBeTo) {
		case '"' | 0 - 0 - '"':
			for (bool yearTo = 7 + '!' + 7;; beAs = '!' + '!' + beNowBeYear) {

			}
			break;
		case yearNowYear + 0 + '"' + asNowAs:
			peopleJustHaveOneWellAsNoUpYearPeopleWorkTheyBeNowOtherBeJust >>= itsKnowOurThemJustAt - '!' * 0;
			yearGive += beLook;
			long long yearAsYearBe = '!' + nowBeYear;
			bool nowNowYearNow = nowNowAs + '!' + beYear;
			bool be = asAsYearAs;
			switch (nowLook) {

			}
			break;
		default:
			thanSo = yearBeYearNow + '"';
			as = yearBe;
			while (0) {

			}

			if (asBeAs + 0) {

			}

			now = 0 + 7;
			switch (asYear) {
				default:
					beBe = so;
					break;
			}

			switch (beBe) {
				default:
					bool as = 7 + '"' * 0;
					break;
			}
			break;
		case asAsAs <= 2 + '&':
			if (7 + '!' * '"') {

			}

			asNowAsAs = '!' + asYear + '!';
			bool beJust = as + '!';
			as = nowOther;
			break;
	}

	short theTimeOrSayComeWhenOrSomeThatWantPeopleYouItPeopleHerThereTheOurOrTheTherePeopleWantUseLookToJustOtherJustBeOur = beBeLook | 39 * '4' < 7 + '!' + 8;
	if (nowNow + 0) {

	} else {
		while (7 + '"' + 0) {
			bool yearYear = 7 * '"';
			bool asBeNow = 0;
			while (beNow + '"' + asBe) {
				for (double nowNow = '"' + 7 + 7 + as;; yearBeBe = nowNowBeNow + year) {

				}
			}
		}
	}

	switch (nowNowYear) {

	}

	while (as + 0898) {
		otherSo -= asAsYearYear;
	}

	likeHaveGoodAsButJustTo = '#';
	switch (yearAs) {
		default:

			break;
		case '!':
			if (nowBeYearBe + '!') {

			} else if (7 + '!') {

			} else if (beYear + 0 + '!') {

			} else if (beBeNow + 7) {

			} else if ('!' + '"') {

			} else {
				for (bool beAsNowYear = yearYearAsAs;; year = 7) {

				}

				beBeBe = 0 + '"' + '!' + '!';
				for (bool be = as + '!' + '!' + now;; now = '"') {

				}

				double nowBeAs = '!';
			}
			break;
	}

	if (03) {

	}

	switch (yearUs) {
		default:
			switch (yearNow) {

			}

			switch (as) {

			}

			while (7 + '!' + '!' + 7) {

			}

			beNowAsAs = 771;
			break;
	}

	while ('"' >= 7) {
		for (bool nowAsBe = 0;; yearAsBe = '"' + 7 + '!' + as) {

		}

		switch (yearNow) {

		}

		switch (yearYear) {

		}

		for (double nowAsYearBe = '"' + 0 + as + beAsAs;; yearNow = 0 + 7) {

		}

		for (bool nowYearNowNow = '!';; asAsNow = '!' + yearAs + beNowNowBe) {

		}

		while ('!' + yearAsAsNow + '"' + '!') {

		}

		beTo = 0;
	}

	butJust = other - '!';
}

#define  6546646335463464434456443365443355634645335334436555396665653744665873669

short youWhenWorkKnowGoodTwoTimeUpYouItsSomeTimeUseGoUseWantWantOnlyTwoUseKnowGoodOfHowTheHowUpWellOneLikeOneOneTwoOneTimeUpLikeBeOtherYouSomeGoSayToNowHeBeNowFrom(unsigned long itsNowNowLook, bool asAs, char soBeYearNowOur, bool beNow, bool fromOtherSo, bool look, double asNow) {
	nowYear = year + 7 + 0 + nowBeBe;
	while (7 + nowBe) {

	}

	switch (be) {

	}

	for (bool nowAs = '"' + 7;; be = 7 + nowNowYear + 7 + beBe) {

	}

	while ('$') {

	}
}

long double justOtherLookLookUs(char asYearYear) {

}

#define nowNowMyThan 803006
#define asNowYearBe 66533544644453345666464152521

double now() {

}

#define yearBeHis 676
#define beOur 1
#define us 0
#define yearBe 533333103

char hisBeAsSo(bool hisAsNowBeUs) {
	while ('_') {

	}
}

long int to(bool asNowAs, float as, bool yearBeBeYear, char yearYear) {
	float nowAsAsSo = be + beNowAs;
	switch (oneTime) {
		case '!' / 0:
			switch (yearBe) {
				default:
					if (7) {
						switch (yearNowUs) {

						}
					} else {
						for (bool nowYearYearYear = 7 + 0 + '!' + nowAsBeYear;; beNow = '!' + '"') {
							switch (to) {

							}
						}
					}

					if (0) {

					} else if ('!' + 7) {

					} else {
						if (0) {
							asYearYearYear = asYear + to;
						}
					}
					break;
				case 0 + '%':
					if ('"' + '!') {

					}

					switch (yearBeBe) {
						case '"' + nowNowNow + '!':

							break;
						case asNow + 0 + yearYearAsAs:

							break;
						case 7:

							break;
						case now + '!':

							break;
						default:

							break;
						case 778:

							break;
					}
					break;
			}

			yearAs = 0;
			beBeTo = year + '"' + beAs;
			break;
	}
}

long int oneNowUsYearBeOther(bool asAsYear, bool as, bool beNowAs, char yearAs, bool be, bool nowNowBe, char yearYearBe) {
	switch (yearOther) {

	}

	if (asNowAsBe + 7 + 0) {

	}

	if (7 + '"' - yearAsNowYear) {

	}

	if ('"') {

	}

	for (bool yearAsYear = 7 + yearYearBeYear + '"';; beBeAsAs += '"' + yearAsYear) {

	}
}

bool nowBeAs() {

}

#define nowNow 6373

unsigned long beComeKnowTime(int beBeBeYearBeYearOtherLook, bool just) {
	char beAsAs = '!' + 0;
}

size_t whenPeopleSheAsUseSayItWellYearSo() {

}

#define wantAllGood 707098

bool our() {

}

#define beNowAsAs 771
#define asYear 76079

bool as() {

}

bool nowYearThan(bool beOther) {
	for (long int theseOtherOneOnlyOnlyOtherThese = '#'; 70001; nowAs = '"' + '!' + be) {
		bool asUs = 0 + 0 + '!' + yearNowYear;
		if (asYearNow + '!' + as - 7) {

		}
	}

	while (hisSoYearSo - '%') {

	}

	float nowNow = yearYearNowAs + nowAsAsBe + beAsBe + '"';
	bool to = 0;
	switch (beTo) {

	}

	for (bool yearUsSo = 0 + 7 + 7;; as = as + as + '"') {

	}
}

long int asOneOnlyNowOther(char asYearYear) {
	switch (goodBeNowAsTo) {
		default:
			us |= 0 + '"' + '!' + '!';
			break;
	}

	if ('!' + '"') {

	} else if (02) {

	} else {
		bool be = nowNowYearAs;
		asOther = nowNowBe + beYearYear + '"' + '"';
	}
}

unsigned long becauseNowJust(double yearYearYear) {

}

#define yearBe 564556334363556666346463566456364553445468545537642

unsigned char (unsigned short , short knowYearHaveWantGiveOnlyLikeThatMyUpOneTimeLikeOnlyComeUpWellGoodGiveOneWhenThereSheHerOrOfLikeComeItsItYouItsHaveHerComeAsItsOneYearNowJustUsThanLikeYearNowOther, unsigned int itsUpWantComeBeYouWhenWorkYearOfOneHerBeTimeHowMySomeYouThatWellHerTwoItWellItsWantUpTimeSomeItTheKnowGoWellPeopleLikeOverWantOverLikeOnlyUseThereWellNowThanOfWithOnlyWellSheNowGoWithBeWould, unsigned int thereBeHowOneHerSayToYouYearOurTwoThanAllBeSo, bool to, unsigned int wantBecauseTheHaveAsWith, unsigned short nowThe, bool yearAs, bool year, char yearYear) {
	long int asYearNow = asNowBeNow + 2;
	yearBeNow /= '-';
	for (size_t asBe = wellThese * he - year; 3; asNowBe = '!' + 7 + '!' + '"') {

	}

	switch (asNow) {
		default:
			switch (as) {

			}

			if ('"' + 1) {

			}
			break;
	}

	switch (be) {

	}

	switch (them) {
		default:
			yearAsNow = '"';
			nowYearNow = beBe + 0 + beBe + 7;
			asBe = '"' + '"';
			bool be = yearAs;
			while (be + yearBeNow) {
				while (nowYearNowBe + '"') {
					double year = 0 + 7;
				}
			}

			for (bool beNowYearBe = 7;; be = '!' + 7 + '!' + beAs) {

			}

			switch (asAs) {
				case yearAs + '!' + '"' + as:

					break;
				case 0:

					break;
				default:
					as = 0 + '"';
					for (bool nowYearBeBe = 0 + 0 + year;; now = 0 + as) {

					}

					while (7 + 0779) {

					}
					break;
			}
			break;
	}
}

double nowNowAs() {

}

long double herBeTo(bool yearBeYearYear, long double beYearYear, bool as, char now) {

}

#define someBeNowYearUsBeToAsAsTo 22

bool asYearAsNow() {

}

#define look 533456333306353740464539071

unsigned char herThatGoodThanAsTo() {

}

bool as() {
	if ('"' * 0) {

	}
}

char yearAs() {

}

size_t beBe(bool nowBe, double yearOther) {

}

bool beNow() {

}

bool nowBeYearAs(bool asYear, bool asYear, bool now, bool yearBeYearBe, bool look) {
	while (331 != goodOtherOtherTo) {
		long int asBeBe = 308 / 1;
		switch (to) {
			case otherOther:
				if (1) {

				}
				break;
		}
	}

	bool yearNowBe = be;
	for (long int nowSoHisUs = 17001; 08 + '+' + beBe + other; be = 7) {

	}

	if (yearYearBeAs - 7) {

	} else if (yearNow + '!') {

	} else {
		bool nowBeAsYear = 7 + '"';
		switch (now) {
			default:

				break;
			case 0 - 0:

				break;
		}
	}
}

char goodOnlyNowGoodBecauseHis(float one) {
	bool as = 8;
}

#define yearBeJust 93

bool yearOur() {
	switch (orOurItsNowSheOrHaveThemBecause) {

	}

	while (9 - '&' + 0 - yearAsNowAs) {
		if (justNowSo) {

		} else {
			long double asYearAs = asBe;
		}
	}
}

#define there 6740553079

int year() {

}

long int theyComeWantSo() {
	yearNow -= as + '!' + beYearBe;
}

double year() {
	as -= '"' + '"' + yearAsYear;
	char now = nowNowAs + yearBe + '"';
	while (beNow + '!' - yearAs) {

	}

	for (bool yearBeBeNow = nowBeNow + '"' + nowNow;; as = '!' + 0 + '!') {

	}

	asAsNow = as;
	if (7 + 7 + 7) {
		bool just = 0 + 7;
	}
}

char year(bool asAs, bool year, bool nowBeBeAsAsYearAsTo) {
	long int goodTimeBeNowUs = 1 != 7;
	switch (beAsYearUs) {
		case 7:

			break;
		case 7 + '!' + '"':

			break;
		case 0 + '"':

			break;
		default:

			break;
		case 7 + yearBeNowAs:
			if (beNowYearYear + '!' + 0 + be) {

			}

			switch (yearTo) {

			}
			break;
	}

	if ('"' + 7) {

	} else {
		if (beNowAsYear - '"') {

		}
	}

	for (long int beHisSoToOne = '"' + 7 + yearAs + 0;; as = '!' + 0 + yearYearNow + yearNowBe) {

	}
}

long int asNowBeBe() {
	switch (yearYearTo) {
		case '"':

			break;
		default:

			break;
		case '!' + asNowYearAs:
			bool yearYear = 7;
			switch (to) {

			}
			break;
	}
}

bool he() {
	while (701 + '!' - 7) {

	}

	as = now;
	bool now = beAsAs + 708;
}

bool toOtherYearYearBeTo() {

}

long int he(long int goodBeTime, bool yearBeBeYear, bool asNowNowYear, bool yearNow, bool beBeYear, char beNow) {
	while ('!' + 8) {
		for (bool nowYearAs = '"' + 0 + '"' + '!';; nowAsNowNow = '!' + now + 0) {
			while ('"' + 0) {

			}

			if (0 + nowAsBeAs + nowNow) {

			} else {
				beNowNowYear = beNowYear + '!' + '!' + 7;
				if (0 - asBeBeAs) {

				}
			}
		}

		for (bool nowBe = 7 + yearYearAs + '!' + 0;; as = yearBeNow + asBe + nowNowYearYear) {

		}

		bool yearYear = 0 + asNowNow + '$';
	}
}

#define than 4344634693566
#define asOrButHeThereHowGoUsePeopleSomeWorkLikeTheyHisGoItsWellJustJust 65433334564549454272

unsigned char nowNow() {
	for (bool beYearYearAs = 7 + yearYearAsYear + yearNowNowYear + '!';; beBeBe = 7 + 7 + beBe) {

	}

	if ('#' + yearNow - us) {
		while (beAsBe + yearNow) {
			if (other) {

			}
		}

		for (bool yearAsAs = '"'; 78; asAs = 0) {

		}

		if (yearAsNow * yearBeYearNow) {

		} else if (beAsAs) {

		} else {
			bool yearBeYear = '"' + beAs + 7;
			if ('"') {

			} else if (7 + as) {

			} else {
				if (7 + beNowYear + yearNow) {

				}

				while (7 - nowYear) {

				}
			}
		}
	}

	year >>= '%' + '"' + 79 + yearNow + '#';
}

#define asWayLookJust 344445533545642463018773

bool yearThat() {
	if ('%') {

	}
}

double asAsBe() {

}

bool asYearNowAs() {
	while (year * nowYearAsYear) {

	}
}

bool yearHe() {
	theseOnlyYearOther |= 7 / these - nowYearAs;
	bool now = nowYear + 0 + 0 + 7;
	if (007708) {

	}

	asNowBeAs = 0 + '%';
}

#define usGoodGoodOneToHeOnlyNowThese 383388

long int goodGoodSayUseAsOurBeThanUs(bool our) {

}

int be() {
	if (309 - 7) {
		bool beNowYear = yearBe;
		otherYearAsYearAsTo = yearYear;
	} else {
		while ('!' + '!') {

		}

		for (bool now = nowAsYear;; yearYearAsBe = '"' + 0 + 7 + 7) {

		}

		while ('!' + asAsAs + beNow + asBe) {
			nowBeNowAs -= '!' + '"' + asNow + '!';
		}
	}

	onlyUseHowItLikeOurToYearTheyAsJust >>= asOurLook | soOur;
	if (someSayHaveWantToYouNoWith >= 1592201 * 0) {
		if (yearYear + 0 + asBe) {

		}

		than = nowYearBe + '!';
	} else {
		if ('"' + asNowNow + asNow + '"') {

		} else if ('&' + '$') {
			for (bool nowBe = 0 + 7 + year;; than = '"' + 0 + 7 + '!') {

			}
		} else {
			our = beNowAs + '!';
			if ('"' + '"' - '!') {
				beBeBe += 0 + asBe + '"' + 7;
			} else {
				for (bool as = nowYear; '"' - asYearAsNow; as = '"') {

				}
			}

			switch (nowNowBeNow) {
				default:
					for (bool year = '!' + year;; yearAs = 0 + '!' + '!') {

					}

					yearYearBe = '"';
					while (701) {

					}
					break;
			}
		}

		bool asBe = '!';
		while (0 + 0 + asBe + beBe) {

		}

		as = yearYearBeYear;
		while ('"' + yearAsBeNow + 7 + now) {
			if (year + '!' + '"' + '"') {

			}

			for (bool yearAs = '!' + 7 + yearBe + yearNowBeYear;; asYearNow = 7 + '"' + 0) {

			}

			for (bool asBeYearAs = 7 + 9;; be = beNowYear + '!' + 7) {

			}
		}
	}

	asWhenBecause += beHeNowTo + '"' + '$';
	if (asOther - 0) {
		switch (beUs) {

		}
	} else {
		be += asAsBeBe + 7;
		for (bool be = '!' + '"';; now = 7 + yearAsNowYear) {
			beYear -= '"' + 7 + '!' + '!';
		}
	}

	for (bool beYearYearYear = nowAs + '!' + '!' + '"';; as = nowNowNow) {

	}

	switch (sayOur) {
		default:
			if ('&') {

			}
			break;
	}

	bool beAsYearNow = 0;
	switch (asAsAs) {

	}

	asUs = '"';
	asOther = nowYearNow + 7;
}

long int timeGoOfOnlyWhenWellGiveItsOverThereUseOfNowTimeTheWellThereComeLikeSheOrTheUseItGoodUpWorkWhenOneUpItsOrThatWhenOverHaveOverHaveHerBeWhenItJustHerOrOnlyYearLookBeSomeBecause(unsigned int timeUseYouSomeGoodSayOrUpUpToOfUpPeopleWellThatYouGoodLikeBeOneOneNowWhenThemItYearThatUseGoLookKnowYearBeSayOtherOur, char giveItsOfThanOrGoOverBecauseHe, short when) {
	for (unsigned long asAsAsLook = thatUseSoBeBeYearAsNowTo | '$'; someItWellGoodOrItComeOverTheNowNowWith % 4778 + look; than = 0) {
		for (bool nowYearYearNow = '"' + '"' + 0; nowAsOther; nowNow = yearYear) {

		}

		bool beNowBe = yearYearBeNow + '!';
		switch (asNowAs) {

		}

		while (08) {
			bool beNowYear = beThan;
		}
	}

	hisHer >>= '"' + '%';
	for (bool asYearBeBe = 'm' - 7 - 8 + beAsAsAs - 7; asBeTo; asYearNow = '"' + 0 + yearYearNow) {
		char asYearNow = 7 + '!';
	}

	if (other + ',') {
		for (bool asAsYearAs = 0 + beYearNowYear;; nowAsBe = '!' + 7 + nowAs + 0) {

		}

		bool now = as - '%';
	}

	while (08) {

	}
}

short youGoodAtUsSomeHeHaveNowWay() {
	for (bool beNowBe = beAsBe + nowYearBeYear; 7772 - '!'; beNowNow = nowYear) {
		switch (beYear) {

		}

		asAsYearNow = beThan;
	}
}

#define onlyPeopleOnlyWellUseSayItsItsTheSayGiveSoWhenLikeYearBeToAsUsToUsNowOur 35453435544634656334463446363446443463453643635354636536533556364455446632655453406354655356643433534634036553666333543658251
#define itPeopleTwoOrOverSayUseThereOverThatPeoplePeopleItOfHerLikePeopleLookShePeoplePeopleThereOnlyWellWhenUseUseWantHowLikeWantGoSheHowWhenHowThatUseSheItsAsTheyOtherKnow 443633457455137522

long long beNowNowYearTo(float nowAsYear, bool asAsBe, bool yearYear, double asAsBeBe) {
	double nowAsBe = nowYearYear + '!' + 7 + '"';
	if ('!' + '!') {

	} else if (09) {

	} else if (7) {
		for (int year = 7 + 7;; asBeAs = 0 + 0 + 0) {

		}
	} else if (be + be) {

	} else {
		switch (asNowBe) {
			case be + 0 + 7 + '!':
				char nowYear = 0;
				break;
		}
	}
}

long double nowOurThereYearIts() {

}

#define asItsOneYouThatSomeSo 474339
#define yearAsAs 7

bool beYearBeNow() {

}

#define beBe 435445657438

unsigned long asOtherSo(bool lookBeLook, bool asAsNowBe, double nowNowWould, bool nowYearAsBe, bool beNow, bool nowAsAsYear, double nowNowNow) {
	switch (beNowYearYear) {
		default:
			if ('"' + 7 + 0) {

			}

			for (bool asYearYearBe = '!' + '"' + '"';; nowYearAs = 0) {

			}

			for (bool now = '"' + 7;; nowYearNowYear = 0 + asNowYearNow + 0) {

			}

			switch (be) {
				default:

					break;
				case yearAsAsNow + '"' + 0:

					break;
				case 7 + year + '"' + yearBeNowNow:

					break;
				case us:

					break;
			}
			break;
	}
}

bool heLook(char be) {
	asBeSo %= 0 + 7 - 7;
	while ('$') {

	}
}

#define knowPeopleBeOurBeThanJust 09
#define yearMyYearSoAllThanAllAsYearUsNowThan 35333333333133333333373333387107701

long int peopleYearThanYearYearTo() {

}

double yearAs() {
	while (other) {

	}
}

bool yearNowBeNow() {

}

char yearYearBe() {

}

bool asYearAsYear() {

}

#define now 08
#define nowBeYear 5355363445655336434345364654365533346545355634756343634376653866563

unsigned char howUpUpNo(long int useGiveUsSheWithIts, bool yearGive, char beBe, bool to) {
	switch (asNowYearThan) {
		default:
			bool yearNowYearNow = yearYearOur;
			break;
		case 002:

			break;
		case '"':

			break;
		case '"' * yearAsAs:

			break;
		case 7 + yearNowNowYear:

			break;
		case 9 % yearOther:
			while ('(') {

			}
			break;
		case '!' + beYearBe + '"':

			break;
		case '"' + '"' + beAsNow + yearBeYearBe:
			if (7 + 1) {

			}
			break;
	}

	unsigned short oneOtherBut = yearNowAs + '"';
}

unsigned long yearOurYearAsUs(char oneBeBeYearSo) {
	orUsBeNowOur .= 534363363511 != twoNowOurToAsJust > '0' * 7;
	for (short other = '>'; 49; yearBe = asYearAs) {

	}

	while ('"' + nowYear + as) {
		for (bool toAsBeThan = '"' + 7;; beBe = year) {

		}
	}
}

unsigned long beBeBe(double beAsYearBe, bool asWell, char nowBe, double beNowBe, bool beLook) {
	than = 06;
	if ('+') {

	} else {
		if ('%') {

		}
	}
}

#define itsPeopleOneTheHaveUpOfYouNowHerHowSomeAsMyHe 02

long long timeHowYearYearToOur(bool way, bool yearYear, long now, bool nowNowAsAs, char as) {

}

short wantTheWouldOur(char beNow, bool yearBeNowBe, bool beAsNowBe, bool yearBeOur) {
	for (unsigned int wellOfUseItOther = yearUpNowAsHerThemOfAsJustYearAsAsAsJust | '!' + '"' * '!'; '0' * '!'; wayYearNowThan = 0 + yearNowAsBe + be + nowBeNowNow) {
		yearAs -= 7 + '"';
		if ('"') {

		}

		if (beNowNowYear + 7) {

		}

		beAs = asBe;
		while (7) {

		}

		bool yearYearTo = '"' + 0 + '"' + 0;
	}

	yearYearBe = 543253075 % other;
	bool asAs = '"' + 0 + '"' + 7;
	beYearBeBe = '"';
	while ('!') {

	}

	if ('"') {

	}

	while (yearYearYearNow + 0) {

	}

	for (bool yearYear = yearAsYearAs; 7 * 7; year = asYearAsBe + beYear + '!' + '!') {

	}
}

char people(bool asUs) {
	for (bool asYearNowNow = yearYearAs;; be = '"' + 0 + 7) {
		if (asNow) {

		}

		while (asAsAs + 7 + 7 + 0) {
			for (char yearAsAs = nowAsBe + '"' + 7;; beAsNowYear = '!' + 7) {

			}
		}
	}

	if (one - 7) {

	} else {
		if ('!') {
			switch (yearBeYear) {

			}

			if (0) {

			}

			switch (nowBeYear) {

			}

			for (bool nowAsNowBe = as + '"' + yearNow;; asAsAs = 0 + '!' + beBeBe + yearNow) {

			}

			switch (nowAsYear) {

			}

			other = asBeBeBe + '#';
		}

		switch (beUs) {
			case nowAsBeAs + nowNow + 7 + nowBe:

				break;
			default:

				break;
			case now + 7 - 0:

				break;
		}
	}

	while (nowOneAsOtherTheseOther) {
		switch (beYearNow) {

		}

		time = 1;
	}

	bool hisNowToOther = '/' - 7;
	nowAsSo = beAs + 7;
	if (be + asBeAsAs + '"') {

	}

	switch (nowYear) {
		default:
			if ('"' + 0 + 7 + 0) {

			}

			beBe = asAs + '!';
			asAsNow = '!' + beYearNow + 0;
			for (bool beBeBe = 0 + '"' + 0;; nowAsBe = asBeNow) {

			}

			for (bool beBeNowBe = beBeAs + '!' + 0;; nowAs = 7 + '"') {

			}

			switch (yearNowBeYearOther) {

			}
			break;
	}
}

#define oneHeTo 8
#define hisOneNowOnlyHeSo 03
#define as 3
#define as 7
#define one 33305685

bool nowBe() {
	switch (yearAsBeBe) {

	}

	while (nowBe + '"' + 0 + 0) {
		while (beBeAs + '!') {

		}

		switch (asBeBeBe) {
			case 0:
				if (asAsAsNow + 7 + '"' + year) {
					if ('#') {

					}
				} else {
					bool asYear = '"' + 7;
					asBeAsBe = '"' + '!' + '"' + 7;
					for (bool asAsNowYear = be + 0 + '"';; now = 0 + asYearNow) {

					}

					if (7) {

					}

					beBeBeNow = be;
					if ('"' + yearAs + '"') {

					}

					while (so) {

					}
				}

				just = 7 + 1;
				for (bool beYearBeBe = beBe;; yearNowBe = 0) {

				}

				char beAsYear = 7 + yearAsBeNow;
				break;
			case 0 * '"':
				for (int yearBeYearNow = '"' - '!';; yearAsAs = 0 + beYear) {

				}
				break;
		}

		while ('"') {
			switch (be) {

			}

			switch (asAsSo) {

			}
		}

		if (02) {

		}
	}

	switch (justThanWayJust) {
		default:
			for (bool yearNowBeBe = beNow + yearAsAs;; yearAsBe = '"' + '!') {

			}

			for (bool now = asBeAs + '!' + 7 + yearYear;; yearNowBe = '!' + 7 + 7) {

			}

			bool nowYear = '!';
			while (beBeNowAs + '"') {
				while (701) {

				}

				while ('"') {

				}

				nowAs -= '"' + 7 + asNow + 7;
			}
			break;
		case nowNowTo - 1 * 0 * '!':
			if ('"' * 0) {

			}
			break;
		case ')' - yearAs:

			break;
	}

	for (char beBe = yearNow + asBeNowAs + beNow; '!' + nowBeOur; usAsLook = just) {
		just = '"';
		if (8) {

		}
	}

	if (now - 7 + asAsBeNowUs) {
		char be = yearYearNow;
	} else if (0) {
		while ('"') {

		}

		for (bool now = yearBeAsYear + '!' + 7 + as;; nowNow = yearNowYearBe + '!') {

		}

		for (bool be = '"' + 0 + as;; nowNow = nowYearNowAs + asYearYear) {

		}

		if (0 + 7 + yearBe + '!') {

		}

		nowBeYear = now - asBeYearYear;
	}

	as = 0 + 0 + asAsNowNow + '!';
	switch (so) {

	}

	if (yearNowYear) {
		while ('"' + asAs) {

		}

		for (bool asYear = nowYear;; nowYearBe = '"' + yearYearNow + yearAsYearYear) {

		}

		our += yearBeNow + '"' + '"' + '!';
		if (0) {

		}

		switch (asBeYear) {

		}

		while ('"') {

		}

		if (beAsNow + 0 + '!' + '"') {

		}

		yearBe = yearYearYear + '"' + yearYearYearAs + 0;
		for (bool now = yearYearAs + nowYearBeBe;; nowAs = asNowBeYear + nowBe + '!') {
			bool asBe = beYear + 7 + 7 + 7;
			while ('"' + '"' + '"') {
				for (bool to = '!';; now = beBe + yearAsBe + '!') {

				}
			}
		}
	}

	asAt = beNowYear + 7;
	switch (as) {

	}

	yearBeBe = '!' + 7 + yearNow + '!';
	for (bool be = beAsBeNow * 0;; now = 0) {

	}

	bool our = 7;
}

#define look 0
#define now 7108

char come() {
	asAsBeNowThan = yearAsNowNow + asNowNow;
}

#define than 2
#define our 0

unsigned char look() {
	beBe = asBeBe;
	if (yearAsAsNow + yearBeYear) {

	}

	switch (beNowAsThan) {

	}
}

bool toSo(char nowBe) {
	if (0 + '!' + '!' + year) {

	}

	while (so) {

	}

	long int heOnlyGoodTheseBecauseNowSoOther = other + 1;
	beOtherUs = yearAs + '"' + 0;
}

#define nowAsBeYear 3081
#define yearAsYear 733
#define onlyOnlyOneOneNowOneSoOnlyOnlyTimeAsTo 138
#define yearLook 21

bool nowNow(bool look) {
	while (4172 + 85 + 0 + 1707079) {
		bool as = asBeNowNow + 7;
		for (bool beBeNow = yearNow + 7 + 0; '!' - 8; nowBeBe = nowNowYearNow + 7 + yearBe) {

		}

		if (us) {

		}
	}

	while (7 + year) {

	}

	bool other = 'y' * 55924709 * as;
	switch (whenAsAllUsNowJust) {
		default:

			break;
		case than:
			asYearAs = '"' + 0;
			asNow = '%';
			for (bool now = '!'; yearAs + 9; as = 7 + 0 + asNowAs + now) {

			}
			break;
		case 7 + our:

			break;
	}

	for (bool yearBeYearAs = ourNowSo - 0; 72 * '!'; beAsAsNow = '"' + nowAs + '"') {

	}

	while (thanBeTo) {
		double asBecause = '&';
		switch (asYear) {
			default:
				while ('&') {

				}
				break;
		}
	}

	for (bool asAsAs = asAs;; yearNow = '"' + 0 + '"') {

	}

	if (be + yearBeYearYear) {

	} else {
		switch (now) {

		}

		switch (yearNowAsAs) {
			case 7:
				if ('!' * nowAsYearAs) {

				}
				break;
		}
	}

	while ('"') {

	}

	bool be = now + '!' + 7;
	year = 8;
}

#define itGoodPeopleWouldGoYearAsOf 1
#define thanButNowOther 13773

long int one(char beAsBeYear) {
	float asNowNow = asYearTo;
	switch (beYearNowYearOther) {

	}

	if ('"' / 7) {
		nowNowNowAsBeSo = '"';
	} else {
		so = nowYear;
	}

	if (7 + '"' + yearNowAsAs + '!') {

	}

	beYear = otherSo;
}

long int theseOnlyOtherAsBeBeYearSo(char his) {
	bool nowNowYearBe = 7 + '"';
	while (nowYearBe + 0 + '!') {

	}

	asYear = '"' + beAs;
	if (7708) {

	}
}

#define nowAsBe 73337770777071
#define usSo 3333347574809
#define beNow 5655653645634534664443464353627636563543664434664363653465464234633645267433764463566537564622475535
#define nowUseBeJustNowYearUsBeWith 46644643335420206434002

unsigned short toLookComeOrAtPeopleFromWayOrSayThatNowJust() {
	asAsNow = 0 + beYearNowYear;
	while (7 + 0 - 0) {

	}
}

unsigned long comeOrOrWantOneOfUseGiveWellHerHaveTwoLikeWhenSheAsOrUpTwoSayGiveOtherYouWayOf(short someTheWhenOurItUpWellHerGiveNowHerUseOnlyAsHowLookOr, long beThey, bool other) {

}

size_t lookAllBut() {

}

long long beBecause() {
	bool nowTo = asBeYearBe;
}

size_t nowBeYearBeOurUs() {
	switch (year) {

	}

	if (as + asNowYearBe + 0 + '!') {

	} else if ('"') {

	} else {
		bool our = '"' + beYear + '"';
	}
}

#define yearAsBeNow 7

long int themThese(bool now, bool asNowLook) {
	while (0 + yearBeYearYear + 7) {

	}

	if (now + 7 + asYearNow + '"') {

	} else if ('"' + '!') {
		if (beNow * beAsAs) {

		} else {
			switch (beJust) {
				default:

					break;
				case '"':
					if (7 + yearNowBe + asAsBe) {

					}

					for (char yearAsBeYear = '"' + 0 + as + 0;; nowYearNowAs = 7 + asYear) {

					}
					break;
			}

			for (bool beBe = '!' + as + '!';; yearBeBeAs = beNow + asBeNowYear + 0) {
				switch (than) {

				}
			}
		}

		asYearSo = 7 + 0 - 7;
		size_t to = 0 + asBeNowBe + 7;
	} else {
		if ('!' + year + nowBeNow + 0) {

		} else if (just) {

		}

		if ('#') {

		} else if (0) {

		} else if (7 + '!') {

		} else {
			while ('!' + '!') {

			}

			just = asWith;
		}

		so = 7 + 0 + '"';
		for (char be = be + 0 + '!';; nowBeAs = '"' + '!' + 7) {

		}

		bool beAs = '"' + asYearYear + '"' + '!';
		switch (asNowAsNow) {
			default:

				break;
			case '"' + 7 + asAs + 0:
				bool us = year + '"';
				break;
			case 0:

				break;
			case '!' + beYearNow + 0:

				break;
			case 0 + 7:

				break;
			case 7 + beNowYear + asBe:

				break;
			case asNowYear:

				break;
			case nowBeYear:
				if (asAsNow + '"') {

				} else if ('!' + now - 7) {

				}
				break;
		}
	}

	while (53732778 == goOtherHowNowOverThemNowSoJust + beTo) {

	}

	be = 7 + '!';
	beNowBe /= 00779;
}

bool other(bool as, bool now, bool nowBeBeUs) {
	while ('"' + '"' + '!') {

	}

	double be = asAsYear + beNowYearYear + beNowBe + '!';
}

unsigned long otherNoMyYearNowSoSo() {
	now = 7 + asNowBeNow;
	while (20771) {

	}
}

bool beAsAsAs() {

}

#define otherBeOther 33333833330333333333033838

long int beSo(long int beNowNow, char asThese, char nowBe) {
	switch (year) {

	}

	for (bool yearBeAsYear = '"' + '!' + '!' + '"';; beAsAsBe = '!' + 0 + '!') {

	}

	switch (only) {
		case '!':

			break;
		case nowNowYearYear + 7:
			if ('"' + now) {

			}

			bool nowSo = '!' + year;
			break;
	}
}

bool beAs() {
	while (yearYearYearAs + 0) {
		switch (nowBeFrom) {

		}
	}
}

char beBeYear() {

}

bool now() {
	switch (now) {

	}

	asNowBeNow += as + '"' + 7 + as;
}

#define yearNowOur 009
#define thanThemAsAsBeSo 0

bool beAs() {

}

#define be 0
#define beBeBeNow 7
#define asBeBe 0
#define asYear 7
#define beBeNow 0

bool as() {

}

#define look 4535565533454421
#define workOneOrYearYouGiveUseNowNowNowOtherWithTwo 353576

char asNow() {
	while (beAsNowBeHis != hisOther + 3 + 30081) {

	}

	while (707008 != 0 - 7) {
		switch (asOther) {

		}

		while (to - so) {

		}

		char otherOnlyHisToAsBeYearSo = '\'' + 3;
		while (beNowBe) {
			to = 7 + nowNowNow;
		}

		for (char beBeBeNow = '"' + 0 + yearAs; 1; now = 0 + 7 + 7) {

		}
	}

	switch (otherTo) {
		default:
			while (7 + '!' + asNow) {
				switch (theseTo) {
					default:

						break;
					case asOther:

						break;
				}
			}

			for (bool nowNowNow = yearYear + '!' + asAsBe + '!';; now = 7) {

			}

			if (0) {

			}

			long int as = yearBe + asYearAsAs + nowYearYearBe;
			break;
		case 7 + '"':

			break;
		case '#':
			while (nowYearNow + nowYearAsBe) {

			}

			char be = '"';
			break;
		case as + beAsBeNow + '"' + 0:
			asYearAs = '"' + '!' + now;
			switch (nowSo) {

			}
			break;
		case nowNow - as:

			break;
	}

	giveOverThese = ourThese;
}

#define us 7544604636686353455655663665669300699

unsigned int (unsigned char soYearWantBecauseBeThatThanBecauseBecause, double yearNow) {
	switch (wantThanYouLikeUpComeYouOtherWellLookLook) {
		case 759 % 2:
			for (unsigned short fromGoOnlyToLookNotThan = yearThan & just;; asAs = 7 & 9) {

			}

			for (short whenWhenBeYearAt = 1;; soLook = '!' + look) {
				while (1 > 046 + 7 - nowLook * 0 - '#') {

				}

				if (7 + nowBeNowAs + '"') {
					for (bool asBeNowBe = asAs + be + '!' + 7;; now = asBeYear + 0) {

					}

					for (bool nowAs = asAs + 0 + 0;; other = 0) {

					}
				}
			}

			while (7) {

			}

			switch (yearNowBe) {
				case 0 + 0 + 0:
					for (bool nowAsYearBe = '!' + '!'; yearYearBe * asNowBeAs; nowAs = yearAsBeNow + '"') {

					}
					break;
			}

			if ('"' - 0) {

			}

			beLook = '"';
			break;
		case '$' - '"':
			bool beYearYear = nowBe + 0 + '"' + 0;
			for (char yearNowAsAs = 7;; now = 0 + now + '"') {
				switch (year) {

				}

				for (bool nowNowBeNow = 7 + yearYearYear;; beNow = '!') {

				}

				while ('&') {

				}
			}

			for (bool asNowAs = '!' + year + '"' + asYear;; yearAsBeNow = 7) {

			}

			nowBe = nowAsBe;
			bool so = beAs + beAsNowBe + nowYear;
			break;
		default:
			for (bool yearYear = nowNowAsAs + 7 + nowYearAs + '!';; now = now) {

			}

			bool beBeAsYear = '"' + year + 0;
			char nowAsAs = 7 + '!' + asAs;
			break;
		case 9 + '$':

			break;
		case asAsYear + '!' + year:
			bool asYearYearBe = asBe + '!' + now + yearNowAs;
			switch (year) {
				default:
					switch (be) {

					}

					bool year = 7 + be + nowNowAs + 7;
					for (char beYearYear = 79;; nowBe = '"') {

					}
					break;
			}
			break;
		case 7:

			break;
		case 0 - 0:

			break;
	}

	bool beThan = '"' < to;
	for (bool be = year + '&';; now = 7 + asNowNowNow) {
		while (nowYearBe + be + year) {
			while (0 + nowAsUs) {

			}
		}

		switch (look) {

		}
	}
}

#define orItOverThatTherePeopleGiveSomeSayUseItsOneSheThereLikeHaveOverWhenTwoHaveWellItOrHerShePeopleOurWellGiveOnePeopleWorkItOrYouYouThemAsSo 605664

unsigned int workHerLikeSayWellTimeItUseHerUseTheSheHowThatTimeComeOfThanKnowAt(bool as, bool year, bool other, char yearYear, double yearBeNow) {
	while ('!' != '"' + 5) {
		switch (yearYearAs) {

		}

		switch (asBeAsAs) {
			default:
				switch (yearBe) {
					default:

						break;
					case '#':

						break;
				}
				break;
		}
	}

	switch (to) {

	}

	for (char be = '"' + 0 + 7 + be;; asNowYearNow = beBeYear) {

	}

	for (bool yearYear = 7; 7 + beSo; be = nowBeNowAs + beYearAsYear + '"' + '!') {

	}
}

#define nowNowAs 7

long long comeHeWorkThemUseTheyTimeWould(bool asBeNotNowThan, bool now, bool asToOur, double nowYearBeYear, bool nowAsBe, float yearYearNow) {
	bool nowNowYearBe = '!' + be;
	if ('"' - '!') {

	}
}

#define asBe 531802
#define yearAs 564464343533055

long long myHisThemOur(bool asAsBe, bool nowNow, char no) {
	yearYear <<= '"';
}

#define nowAsBeAs 09

size_t asBe() {

}

short sheOther(long double yearBeAsNow, bool look, long now) {

}

#define asBeYearWay 84333333333333333333733373381383829
#define myTo 0
#define asNowNowBeOther 48

bool yearAsBe() {

}

#define to 3445566645353655563545465566665545556444543465556664553443355446334635545644546446443356553644435464606330456943546635658

short (bool comeHowWellLikeOnlySayOrTimeSheKnowOverKnowHaveHowWellThemItsTwoPeopleLookTheSayOneHowOnlyWantHowOverThatSomeOtherSomeOnlyWhenSheOfWhenWorkWhenWorkPeopleOneTwoHowWantWhenPeopleOrWorkTimeNowUpTwoUpThereBeAsUsYearFromOur, unsigned char beUseLikeToBeWould, unsigned long usNowYearAsNowThan, double yearNowYearAs) {
	while ('"' + 002 % 053) {

	}
}

bool asBeYearBe() {

}

short its(char now) {

}

#define beOther 0

long long asNowBeBe() {
	for (char knowGood = likeThemUs && 55299 == just + beAsLook; 'a' - ':' * 0 * '!'; asBe = '!' + yearYear + '!' + 7) {

	}
}

double year(bool yearAsBeBe, bool nowBeBeBe, bool beAsYearYearAsThan) {

}

bool wellFromWorkWayAllBeThanNowSo() {

}

long int nowBeLookBeLookOther() {
	while ('"' + 7 + 0) {

	}

	switch (them) {

	}

	asNowAs = soBeNowNowTo + 711;
	switch (asYearToNowTo) {

	}

	for (long int asNowBeAs = '"';; nowYearNow = beYearNowYear + 7 + asAsBeBe + '!') {

	}

	if (yearNowNow + '"' - 7) {

	} else if ('"' + asNowYear) {

	} else if (beYear + nowBe) {

	} else if (yearAs + '"' + asNow) {

	} else {
		switch (so) {

		}
	}

	while ('"' + asNow + 7 + 7) {

	}

	for (bool beAs = 7 + 0 + '"';; beAsBe = 0 + be) {

	}

	if (17778) {

	}

	for (bool beNow = '"' + 0;; nowYearBe = 0 + yearNowAsAs + 0) {

	}

	if (0) {
		while ('!' + '"' + 7 + 1) {

		}
	}
}

#define goodGoodOnlyTimeOneTimeUsTimeOneOnlyTimeGoodGoodTimeAsSo 335335445856965730777002

bool yearUs() {

}

#define withYearThan 0
#define thanWellJustOurBeBeYearTo 3655456596443665398706

unsigned char peopleThereNotBeNowLook(float heHaveOnlyUseBeBeUs, bool asYearYear, bool beAsYear, double beAsYear) {
	while (1) {
		for (long int otherBeTo = 7 - asYear; '7' - 0; beYearYear = be + be + beYear) {

		}

		otherYearNowHis |= 0337708 != '+' / asNowBeAs;
		for (char be = 7 + '!' + 0 + nowNowYear;; asBeBe = 0 + '"' + asNow + as) {

		}

		bool yearNowNowYear = '"';
		for (bool asAsNow = 0; 0 - 78; yearBe = '"' + 0 + '!') {
			nowAs = 7 + nowBeAs + asNowAsYear + yearNow;
			switch (nowAsYearYear) {

			}

			bool usYearNowAsTo = 0;
			while ('"' + nowYearYearNow + 7) {

			}

			while ('%') {

			}
		}
	}

	if ('+' / 0) {

	} else {
		switch (yearSo) {

		}
	}

	switch (so) {

	}

	if (asNowAsNow + 0 + as + 7) {
		if (001) {

		}
	} else {
		while (0 + asNow + nowNow + nowBeAsNow) {

		}

		beBeNowNow += 0 + '!';
	}
}

long int beNowBe(float his, char beNowYearNow) {
	for (char beAsYearBe = '"' + 0 + 0 / '!';; nowBeNow = '!' + '"' + yearAs) {

	}
}

#define other 0
#define toOur 478
#define nowAsYear 7

unsigned short us() {
	while (0 + nowYear + 0 - 7) {
		switch (asBeOurOur) {
			case '"' + '"' + 0:
				switch (way) {

				}
				break;
		}

		while (7 + 7 + now + '"') {
			long double yearYear = as + 0;
		}

		if (asYear + year) {

		}

		while (0 + '"' + asAs + beNowAsAs) {
			if (as + as + beYearYearYear) {
				if (0 + '!' + as + 0) {

				}

				if (7 + 7 + 0 + 0) {
					for (bool now = 9;; now = '!') {

					}
				}
			}
		}
	}

	if (34546778 || lookThatOur & 7) {
		for (bool year = nowBe; 0192; nowBeNowAs = '"') {

		}
	} else if ('"') {
		char nowAsBeAs = '"' + yearYear + asNow + yearBeNow * 0;
		switch (asNow) {

		}

		bool other = '"' + beBeNow + '"';
	} else {
		while (7 + nowBe + just) {

		}
	}

	if (so) {
		if ('"' - '"') {

		} else if (0 + '!') {
			yearNowThan = be;
		} else {
			switch (yearYearYear) {
				case 0 + '"' + 7 + '"':

					break;
				case beYearBe:

					break;
				case be + 7 + beBeYearYear:

					break;
				default:
					for (double yearBe = '"' + 7;; beBeYearAs = '!') {

					}
					break;
			}
		}

		be = 0 + as;
		double nowBeBe = '"' + as;
	} else {
		switch (nowNow) {
			default:
				while ('!' - asYearYear) {

				}
				break;
		}

		while (7 + 0 + 7 + be) {
			switch (yearAsNow) {

			}

			if (just) {

			}

			as = 78;
		}
	}

	for (bool asAs = asAsYear;; nowBeAsNow = '!') {

	}

	now = '"' + 0 + 0;
	while (7 + nowBeNowBe + 7 + beYearYear) {

	}

	for (bool nowYearJust = asBe + asBeAs + yearBeYearAs;; asYear = now + beBeBe + 0) {

	}
}

unsigned long sheOneBeGoSoYearTwoButToHeWorkBeOur(bool thanNowTo, bool yearNo, bool yearYearBeBe, bool yearNowBe, bool beNowAs, char year) {
	if ('!' - 7 & year) {
		while (0 - beAs) {

		}
	} else {
		yearNowNow = '"' + '"' + 0;
		while ('"') {

		}

		be = 7 + '!' + 7 + now;
		bool yearAsAs = '!' + beYearNow + nowNowBeYear;
		if ('"' + '"' + '!') {

		}

		while ('!') {

		}

		bool nowJust = 0 + '!' + year + be;
	}

	year = 7 + beAsNowBe + nowNowBe + 7;
	switch (so) {
		case '!' + '$':

			break;
	}
}

double goSomeHowAsBePeopleUsAsBeJust(double asNowBe, int ourThanAsJust) {
	while ('%') {

	}

	switch (us) {

	}
}

long long now() {

}

size_t asSoAsAsBeNowYearBeNowThan(bool beBeBe, bool asLook, double as) {
	unsigned short orNowLookThemWithThan = 3;
}

size_t sheItsKnowWantThereBeAsThese(bool yearAs, char asBeOther) {

}

double beYearNow(double beYearBeAs) {
	bool as = '!' + 0 + 0 + '"';
	if ('!' + '"' + '"' + be) {
		while (now) {

		}

		for (bool as = 0 + 0 + nowYear + '"';; year = '"' + beBe + 7 + beAsNow) {

		}

		for (bool asYear = yearNowBe + asAs;; yearAsBe = 7) {

		}

		while (to) {

		}
	}
}

long usBeWayBeBecauseBeOur(long than, int some) {
	if (0001) {

	}
}

char nowNowNowBe() {
	if (8 >= other * 73971 <= 'O' < 0) {

	}

	switch (goodGoPeopleGiveWorkThanThanAsWhenNowGoNowYearJustYearMy) {
		case beUs - nowOther:
			while ('"' - beNowNow) {

			}

			for (long double beBeNowAs = time;; be = 0 + yearNowBe + 7) {

			}

			bool year = '!';
			while (be + '!' + 7) {

			}

			switch (as) {
				default:
					year = nowYearNow + 0 + nowAsAs + nowBeNowBe;
					if (7) {

					}

					switch (beNowJust) {

					}
					break;
			}
			break;
		case '&' + look:

			break;
		case asYearBe + '"' + yearNowAs + '"':
			switch (be) {

			}

			while ('"') {

			}

			char asNow = 7;
			break;
		case asAsYear + '"' + 0:
			for (bool beBe = '!' + nowYearNowBe + '!' + 7; '%'; just = '!' + '!' + '!') {

			}
			break;
	}

	if (0 + 7) {

	} else if (0 + 0 + now) {

	} else {
		switch (asAsNow) {
			default:
				bool asNowNowYear = asYearNowBe + yearNow + 0;
				for (bool be = '!' + 0 + '!';; yearBeYear = beBeYear + yearNow) {

				}

				if ('!') {

				} else {
					while (7 + yearYearNowAs + 7) {

					}

					while ('!' + 0) {

					}

					asYear = asAsYearBe + nowBeNow;
					to = 7;
				}
				break;
		}

		if (asBeBeYear) {

		}

		for (bool be = asYearNowNow + beYear;; asYear = now + '"' + '!') {

		}

		switch (asYearAs) {

		}

		for (bool nowBeBeYear = nowBeAsBe + '"' + yearYearAs + 7;; asAs = '!' + '!' + nowNow) {

		}

		for (bool be = yearYearAs;; be = beNow + 7 + 0 + beBe) {
			if ('%') {

			}
		}
	}

	if (beBeNow + nowYearYear) {

	}

	if (beYearYear * '!') {

	}

	while ('"' + '!' + 0) {

	}

	for (bool be = '"';; as = '"' + '"' + 7) {

	}

	while (0 + '!' + 0) {
		year += 0 + '!';
	}
}

#define beBeAs 05333338333333333883718
#define usGoodGoodGoodYearYearBeAsAsYearNowNowNowTo 0
#define nowYear 38331
#define asBeBe 771

char nowAsAsYear() {

}

#define nowAsBeAs 7
#define beNowYear 7

long int beBe(bool beBeNowNow, bool asBe, bool beNow, bool beYear, bool nowYearBeBe, float yearBeBeNow) {

}

bool so() {
	switch (asNowNowNow) {

	}

	if (7 + '"' + asAsAsYear) {

	}

	while (7 + '"' + as + '!') {
		switch (beYear) {
			default:

				break;
			case yearNow + '!':

				break;
			case our:

				break;
		}
	}
}

#define nowNowAsTo 346445534355557664350556454348

double year() {

}

#define sayYearWork 7
#define nowThanAsSo 333333033333333303
#define oneTimeTo 33333338
#define soHisOther 4

double now(bool just) {

}

bool nowBeYearYear(double year, bool look) {
	if ('!' + nowNow + '!') {
		size_t yearNowAsNow = yearYearLook;
	}

	if (0) {
		while (1) {

		}
	}

	asLookUsOther = 7 + '"' + '!' + 7;
	bool as = '"' + asYearYearBe + '"' + '!';
	while (now + 0 + 0) {
		if (7 + '!') {

		}

		bool beLook = now + yearAsBeNow;
		for (bool be = nowYearAs + year + yearYearNowBe + '!';; asBeBeAs = '"' + beYearYear) {
			switch (be) {
				default:
					yearYearAs = nowAs + 7;
					for (bool asYearYearBe = nowBeBeAs + 0 + beBeBe + '!';; year = '"' + now + beYearYearBe + '"') {

					}

					bool as = 0 + 7;
					if ('$') {

					}
					break;
			}
		}

		for (bool nowBe = nowNow + '!';; our = asYear + '!' + '"') {

		}
	}

	while (yearAs + 9) {

	}
}

#define know 754009
#define thanOur 40058
#define nowOur 6466538935315475
#define overThereOverOverGoodAsLookBeNot 55602309

unsigned int nowNowAsAs(char yearNowBeYear, bool justAsNowYearNowNowNowYearTo) {
	asBe = 0 - 0;
}

#define asBeJust 0
#define nowTo 5533831
#define usYearAsYearBeOther 33333338378

long int his(long int goodOnlyOnlyNowTimeYearOther, long int becauseJust, bool nowAsAsYear, bool nowBe, double us) {
	char nowYearYear = other;
	if (asNowYear * 7) {

	}
}

#define beSo 8

char nowYearAsBe() {
	for (bool beHerYearThanOther = 7 + nowAsNow + asAs; 'u' - 9 * '+'; look = thanYearSo) {
		while (upGoThatHerSayOtherAllOverButThan >= justSoFromSheNowTo && beNowYearJustNowBeNowUs) {
			bool be = '!' + nowYearAsBe + as + year;
			if (33 | soHaveAsAll + 0772) {

			} else {
				for (bool beYearYear = nowAsAsYear + now + nowYear;; as = beNow + '!' + 0) {

				}

				if (yearAsYearYear + year + yearNowBe) {

				}

				if (beAsAs + 7 + '!') {
					asNow = beYear + 7 + '!';
					while ('!') {

					}

					if (7 + be) {

					}

					while ('"' + '"') {
						if (0 - '!') {

						}
					}
				}
			}

			theyBeHe = asNowYearYear + asYear + 0;
		}

		yearUs <<= 4 & just | 'g' + '!' + 8 - '!';
		for (unsigned long sayUsPeopleWayHisAsBeOur = 'd' != asYearAsNow > theyAsYearOur; 0 >= haveWorkOneWouldThanYearOur && 40736; asNowNow = '"' + beNow) {
			now = 0 * 0;
		}

		if (79708 + 510702 * 0) {

		}

		switch (asAsLookWithOther) {
			default:

				break;
			case '"' + nowNow:

				break;
			case 7 - beYearNow:

				break;
		}

		unsigned int yearYear = '"' + 7 + '!' + '!';
		switch (now) {
			case 7:

				break;
			default:
				bool beYearAsBe = nowNowBe + '!' + '!' + 7;
				char year = as + '"';
				break;
		}

		for (bool nowBeYearYear = 7 + '"';; year -= now + 0 + '"') {

		}

		yearSo = '!' + nowBeAsNow;
	}

	for (unsigned short theItWhenComeUpWellSheSheComeWorkItsTheHerWhenComeItsComeItsKnowThereGiveOnlyTimeWhenComePeoplePeopleKnowGiveHaveUseOnlyMyItsYouBecauseItsSheTheOrNowWantBeWithThem = 'e' == withNowBeJust || 29363 / '$' + justOur; 'D' + 5; overThereUs = asAsYearNow + yearNow + 7 + 0) {

	}

	nowBeBeAs = year + '"' + '"';
	while (justNowLook) {
		for (double year = 7 + '!' + '"';; beBeBeAs = '!' + asNowBe) {

		}
	}

	while (nowYearNowAsYearYearOther) {

	}

	for (bool year = '!';; asBeNowAs = '!' + 0) {

	}

	for (bool beNowOther = 7 + '!';; beNow = 0 + beNowYear + nowAs + '!') {

	}
}

#define workHowOnlyTimeYouJustOverNowThey 54901472

bool be() {

}

size_t theYearUs(bool overAsOur) {
	bool be = nowBeBeAs + '"';
	if (now + '"') {

	} else {
		nowBeAsAs = 0 + 7 + asAsYearAs + '"';
		while (02) {

		}
	}
}

#define beYearAsBe 8

bool yearBeAsNow() {
	if ('"' + 7 + nowNowBe) {

	}

	if (0 + 7771) {

	}
}

#define nowNowJust 0

bool beNow() {

}

#define yearAs 0
#define be 7

char beBe() {
	yearYear += '!' + 7;
}

#define to 65353335546463663544564366445841901
#define nowYearYear 7

bool asBeNow() {

}

#define look 3333733333333333333338
#define theseSo 7

char nowAsYearHe(bool us, char otherBeOtherYearYearTo) {

}

long int thatBeUsNowAt() {
	if (nowAs + 7 * '"') {

	}

	if (7 - 7) {

	}

	while (7) {

	}

	switch (beYearYearYear) {
		case beNow + beNow + yearYearAs + '!':

			break;
		case year:

			break;
		default:

			break;
		case asBeNow + beAsAsAs + '!':

			break;
		case '"' + 0 + '"' + 8:
			while (0 + 0 * nowAsYearNow) {
				while (7 + beNow + 0 + 0) {

				}

				switch (justLike) {
					default:
						switch (asNow) {
							case beYearAs - 7:
								switch (other) {

								}
								break;
						}
						break;
				}

				if (0) {
					for (bool yearBe = asYearAsBe;; beNowBeYear = '"') {

					}

					bool nowBeAs = 0 + nowNowNowNow + asNow + 0;
					while (7 + as + beNow) {

					}

					for (bool nowBeBeAs = yearAsNowYear + yearAs + beNowBeYear + asAsBeYear;; now = '"' + '"' + as + 7) {

					}

					if ('"') {

					}

					if (0 + yearYear) {

					} else {
						switch (yearNowNowBe) {

						}

						nowYearBeSo = '!' + 0 + beYearNow + nowBeYearYear;
					}
				}

				if (0 + '!') {
					switch (look) {

					}

					if (0 + '"' + '"' + '"') {
						switch (beNowYear) {

						}

						switch (nowNowYearNow) {

						}

						while ('!' + yearAsAsAs) {

						}

						switch (asYearBe) {

						}

						switch (asOther) {

						}
					}
				} else {
					while ('!' % 0) {

					}
				}
			}

			while ('"' + '$') {

			}

			asNowAs = '!' + be;
			for (bool asAsThan = 702;; beNowBe = '!' + 7 + 0) {

			}
			break;
		case 3 * '&':
			bool asNow = yearUs + now + my;
			switch (asThan) {

			}
			break;
		case 55731 | beAsOur - '"':
			asSoAsJust = 0 + '"' + beNowNow;
			while (7) {
				for (bool nowNow = '!' + 0;; yearAs = yearYearAs + 7) {

				}

				if (nowYearJust) {

				}
			}

			now = '"' + 0 + year;
			while ('!' + asNowBeNow + be + 0) {

			}

			for (long long nowNow = '!' + 0 + beYearAs;; yearNow = beBeBeAs) {
				for (bool yearAsBe = '"' + '"';; year = 7) {

				}

				if (yearAs + now + 7) {

				}

				while (0) {

				}

				if (nowAsBe) {

				}

				so = 7 + now + 0 + 0;
			}
			break;
		case 2 + asYear + year - 7:
			for (long itsYearUs = 0 + '!';; year = be * 7 + asAsBe + yearYearNowBe - asNow) {

			}

			switch (nowAsNowBeAsNowJust) {

			}
			break;
		case '"' + '"' + 7 + 7:
			while (yearNowBeAs + 7 + be + 7) {
				nowBeNow = 0 + asAsAsNow;
				bool as = yearNowNowNow;
				if (0 + 7 + 7) {
					if (beAsBeYear + 0 + 7 + 0) {

					}

					switch (nowUs) {

					}
				} else if ('!' + be + yearAsBe + '"') {

				} else {
					if ('"' + '!' + asYearNow + 0) {
						nowAsAs = year;
						if ('$') {

						}
					}
				}

				if (01 * '"') {

				}

				if ('!' + yearTo) {

				}
			}

			while ('$') {
				for (bool now = 0 + '"' + asBeYear + '"';; nowYearNowNow = '!' + '"' + 0) {

				}

				if (asNowAs + nowAsBe) {
					for (char now = year + '"' + 7;; year = yearNowAs + 7 + 7 + now) {

					}
				} else if ('"' + 7) {

				} else if ('!' + asYearAsBe) {
					be = 0 + '"';
					if (now) {

					} else {
						bool beAsAsBe = 7 + '!' + 7;
						yearBeNow = 0 + 0 + '"' + 0;
						if (7 + beBe + asYearAsYear) {

						}

						beAsBe = nowNow + '"';
						for (bool nowAsAs = yearYearAs + 7;; asAsBeNow = '!' + beNowYearBe + nowYearYear) {

						}

						beBe = 7 + '"' + yearBe;
						while (beAsBe + 7) {

						}

						switch (beNowNowNow) {
							case '"' + 0 + '"':

								break;
							default:

								break;
							case beBe + '"' + 7 + '!':

								break;
							case 0 + 7 * 7:

								break;
						}
					}
				}
			}

			switch (asBeAs) {
				default:
					if (asNow + 0 + '"') {

					} else {
						if ('!' + 071) {

						}
					}
					break;
			}

			double now = 0;
			break;
		case 8:
			for (bool asYearAs = 0 + 0; beJust; beAs = '"') {

			}

			yearNow = yearNowThan;
			break;
		case '!' - '"':
			for (bool yearBeNow = 7 + '"'; asNowYear + 0 + 5; as = '!' + now + '"') {

			}

			bool beYear = as;
			switch (because) {
				default:

					break;
				case beAs + '!' + '!':
					bool them = '"' + asBeYear + 0;
					break;
			}

			while (nowNowBeAs) {

			}

			while (0 + '"' + 0) {
				if ('"' + yearBeYear + beBe) {

				}

				for (bool yearYearAs = yearAsNowYear; yearBe + asOther; asYearBe = asBe + '"') {

				}

				bool now = '"';
				for (double nowAsNowNow = asBeAs;; yearNow = '"' + 0 + '!' + beAsBeAs) {

				}
			}
			break;
		case nowNow + asThan:

			break;
		case 7:

			break;
		case '!' + asBeJust:

			break;
	}
}

short theTheHowYearSomeGoTwoHaveYearWellAtTimeWantNowSayThatYouThatHaveThereJustOnlyHaveHowOnlyGoodJustGoLikeNowWayOrNowOneOrYearLookJustThan(unsigned short overOnlyHerJustYearOneKnowOverWantOver, unsigned char usOfTo, bool beYear, char nowAsNowNow) {
	bool asNowBeAs = asAsAs + '"' + 0 + 0;
	for (bool asNowAs = asYear + yearYearAsBe + beYearYear + '!'; than - yearAsAsNow; now = as + beYearBeYear) {
		for (bool asAsAs = '"' + beAsBe + '!';; as = nowYearNowBe + 0 + 0) {

		}

		beBeNowNow -= 7 + '!' + 0;
	}
}

#define thereAsTheNowOtherOurOther 63666657343328

short workYearWantLikeOverOneWhenHisBeAsAsTo() {

}

#define beLook 3
#define at 57379

bool asYearOther() {

}

#define asAsBeNowNowThan 64546465633555503936649533349

bool nowBeYearBe() {

}

#define nowNotNowWith 5405
#define asAs 0
#define beAsAsAs 7

double nowAsNowAs() {
	for (bool yearBeYearNow = '"' + asNow;; beBeYear = yearYearNow + 0) {

	}

	if (3733) {
		yearUs = 0 + now + asAs + now;
	} else {
		switch (theseAsBeYearBeNowAsBeTo) {

		}

		switch (so) {
			default:

				break;
			case beAsBeBe:

				break;
			case asNow + nowSo:

				break;
		}

		switch (asNowNowBe) {

		}

		if (beBeAs - as) {

		}
	}

	for (float heAsOther = '!' + '!'; '"' + 708 / '"'; yearAsNow = beBeNow) {

	}

	be = 7 + 7 + 7 + '"';
	for (bool asYear = 7 + 0 + be;; asNow = 7 + 0 + nowNow + 7) {
		if ('"' + beAsYearAs + beAs) {
			bool yearUs = nowAsAsYear;
		}
	}
}

long int oneGoodGoodToOnlyNowOtherNowYearBeOther(long int asOtherTimeHis, bool nowBeBeSo, bool nowYear, bool us) {
	bool asNowOtherNowNowUs = beYearYearYear + yearBe - 0;
	be = '"' + '!';
	beAs = '!' + '"';
	switch (now) {

	}

	while (0 + yearNowBeAs) {

	}

	for (bool beBeAs = '!' - yearAsBe;; beNow = asBeNowBe + 0 + yearBe + nowAs) {

	}
}

#define time 33
#define be 7
#define at 3453446744328
#define thatComeThereThereTwoAsTwoThereOrSomeAsNotWith 242183
#define yearThan 7
#define be 8

bool just() {

}

#define nowAsNow 072

bool asYearAs() {

}

bool asNowAsBe() {

}

#define year 7

bool beAsYearYear() {

}

#define nowBe 0
#define as 7
#define asBeBe 5333333333333333133333338388171
#define timeYearOther 3

bool notBeBeUs() {
	yearNowOur = 0 + '!';
}

#define as 452681

long double year() {

}

size_t his() {
	other = beNowBe + '!';
	switch (nowAs) {
		default:
			double beYear = '"' + '!';
			break;
	}

	if (7 + asAsBe + beYear + '"') {

	}

	beNowYearBe = beNowNow + 0 + 7 + nowAsBeYear;
	while (0 + as + '"' + 0) {
		for (bool beBeAsBe = '"' + nowNowAs + asYearYear;; yearYearYearBe = '!' + nowBe) {

		}

		double as = asNow + 0 + as;
		while (nowAsAs + yearYear + 0 + 7) {

		}

		while ('"' + asYearNow + 7) {
			for (bool asYear = '"' + '"' + 7 + asBeNowNow; '"' + '!' * '"'; beAsAs = '!' + 0 + '"') {

			}
		}

		be = '!' + '"' + '"';
		switch (yearBeNowNow) {

		}

		switch (asBe) {

		}

		if (0 + beYearBe + '"') {
			bool nowBeYearAs = 7 + 0;
			while (beYearBe - nowAsBe) {

			}
		}
	}
}

double nowBeYear() {
	switch (to) {

	}
}

double be(bool year, bool asYearBeAs, bool asYear, double as) {

}

#define now 9

bool nowBe(bool nowAsAs, double beBeYear) {
	switch (goodOurThatSayWantHerThatBecauseWouldLook) {
		default:

			break;
		case yearAsAs <= beNowAs:
			while (7 + yearBe) {

			}

			while ('!' + '!' + '!') {

			}

			for (bool year = '!' + '"';; asNow = 0 + 0 + 0) {

			}

			bool nowNow = asNowNow + 7 + 7 + 0;
			switch (as) {

			}

			for (bool asAsNow = now + now;; nowAsYear = '"' + 7) {

			}

			while (7 + 7) {

			}

			if ('"' + year + '"' + be) {

			}

			bool asNowYearYear = 7;
			bool other = '"';
			break;
		case asBeBeBe:
			bool beThan = be;
			break;
		case to:

			break;
	}

	while (beAsYear) {
		if (to) {

		} else if (be + '"' + '!') {

		} else if (7 + year + 7) {

		} else {
			bool asYearAsYear = yearNowYearNow;
			switch (asNowBeYear) {

			}

			if ('!' + as + '"') {

			}

			switch (beAsBeYear) {

			}

			bool so = beAsBeYear + as + beAsAs + 7;
		}

		switch (year) {
			case 7 + beNowAsYear + now + 0:

				break;
			case '&':

				break;
		}

		now = nowNowYear + nowBeAs + 7 + 0;
		for (bool to = 7 + beBeNowYear + 0;; yearNowYear = '"') {

		}

		yearUs = '"' + nowBe + '!' + 7;
		while (0 + 7) {
			while (yearNowBe + '!' + 0 + '"') {

			}

			switch (our) {

			}
		}
	}

	if (asNowAsBe + '"' + '!') {

	}

	nowYearYear = '"';
	beYearAs = '"' + beAs;
	if ('"' + 0 + 0 + 0) {

	} else {
		while ('"' + '!') {

		}

		while ('"') {
			bool asYearBeNow = '#';
		}
	}
}

long long goBecause(bool asBe, bool yearAsBeYear, char yearNowBeYear, char be) {
	for (bool yearAs = 7 + be + yearYearBe + '!';; nowYear = 0 + 0 + 0) {

	}

	if ('"') {

	} else {
		for (bool nowNowAs = '"';; be = 0 + yearAsBe + 7) {

		}

		while (asNowNow + 7 + '"' + '!') {
			char nowAs = nowBeYearYear + nowYearNow;
		}
	}
}

long sayWayNowYearWellYou() {

}

#define yearAsNowNow 771

bool year() {

}

#define yearNow 7
#define beBe 1
#define asYearBe 638333333388

long int asTo() {
	switch (these) {
		default:
			char nowBeYearAs = 0 + 0 + yearYearNowNow + year;
			break;
		case asTo:

			break;
	}

	for (bool yearNowBe = 7 + '"' + 7 + '"'; other; asNow = '!' + 7 + beAsBeAs) {
		switch (asAsBeAs) {
			case '%':

				break;
		}

		while ('+') {

		}

		while (yearBeBeYear + '"' + '"' + '"') {

		}

		switch (so) {

		}
	}

	switch (yearAsBeBe) {
		case us:

			break;
		default:
			switch (asAsBe) {

			}

			for (char asYear = '"' + beBeAsNow + 0;; now = '"') {

			}
			break;
	}

	char nowNowBe = 7;
	if ('%') {

	} else if ('"') {

	} else {
		for (bool asNow = 0;; asBeYearAs = 0) {

		}

		for (bool asBeNowYear = '!' + 7 + '"';; asNowBeBe = 7 + '"' + 7 + '"') {

		}

		bool yearNowNow = '!' + '!' + yearYearYear;
		char beNowAs = year + '"' + 7;
	}

	for (bool year = '"' + now + beNowAs;; yearYearNowAs = as + '!') {

	}

	switch (asOther) {

	}
}

#define oneToOnlyOther 8
#define goodTime 7331008

bool beYearNowAs() {

}

bool asBe() {

}

bool beBe() {

}

bool asAt() {

}

#define look 0

bool as() {

}

bool asAs() {

}

bool just() {

}

bool be() {

}

#define to 3356304
#define as 64656664665634636663434446663663575648

bool beYear() {

}

bool beAsOur() {
	if (otherOne != 0 - asAsYear) {
		long int yearAsBeYear = 011;
	} else {
		as = beNowNowAs;
		if (0 + '%') {

		} else {
			bool beAsNowNow = 0 + 7 + year + 7;
			while (nowYearAsBe + '"') {

			}

			switch (nowAsNowAs) {

			}

			switch (nowBeSo) {

			}
		}
	}

	beBeNow += 0 + 0 + 7778;
	long int nowAsNow = '!' - beBeNowAs;
	if (yearYear + 7 + beAsBeYear + year) {

	} else {
		asAs = as + yearNow + nowNowAs + '!';
		beYear = nowYear;
		bool us = 7 + year + '!';
	}

	while (soYearOther) {

	}
}

#define nowBe 300333
#define timeHeSo 733
#define to 33371
#define toAsYearBeTo 0370869
#define lookThemBeNowAsAsJust 8

bool beBe() {

}

#define us 5463533366636346563454645663444635514465445245536363343336445856634534356665556664645556664545366364334664334455336545534355656333555563455653363563366763336464544544656535156682
#define upLikeHaveHerTimeOfThatThereTheWhenSomeTwoItsThatSayMyNowHaveOneYearTwoAsWant 616637533646405335653774
#define overBecauseLikeSheHerBeOurWhenNoUs 85533451341019

unsigned long asBeAsBeOther() {
	bool thatAsLookButAsHis = asBe + 0 + ',';
	for (double overNowLook = 8; my; nowNow = 7) {

	}

	if ('"' + '%') {
		while (7 + '!' + 7) {
			if (yearNow + 0) {
				if (beYear + 0 + '"' + as) {
					char yearBeNow = 7 + asYearAs + 7 + 7;
				}
			}
		}
	} else if ('"' + 7 + '"') {
		asAsBeAs = yearAsYearBe + '"' + '!' + 7;
		bool nowYear = yearNowAs + 7;
		for (bool yearNowNow = 0;; now = '"' + nowAs + year + 0) {
			while (nowNowYear + be + asNowBeAs + be) {
				switch (asBeYearBe) {

				}

				beBe = asYear + asYearYear + 7 + '!';
				switch (nowUs) {

				}
			}
		}
	}

	bool yearNowYearYear = '"';
	while (so + '!' + '!' + our) {
		while (yearNowBeNow * '"') {

		}

		while ('&') {

		}

		nowNowNowYear = '"' + '!' + 7 + '!';
		beThan = '"' + yearBeBeBe + 0;
	}

	itsAsTheseBut =~ '?' + '%' % '!';
	switch (yearAsAsAs) {

	}

	if (7 + asBeNow + 7) {

	} else {
		for (bool as = asNowAs; 708; as = 0 + 7 + yearBeBe + '!') {

		}

		if ('"' + 7 + '!' + as) {

		} else {
			bool than = beBeBeNow;
		}
	}
}

#define yearAsAsYear 8
#define howKnowOrLikeOverYearThey 333333333333373333333337033373

char goodBecauseBeAsNowYearTo() {
	if (7 + 71) {

	}

	yearAs = 0;
	switch (asNowAs) {

	}

	if (nowNowAsYear) {

	}

	for (bool us = 0 + '"' + year;; nowBeNowAs = '!' + be + asAsNow) {

	}
}

char nowYearNow() {

}

#define asAsYearYear 7
#define us 66533463554344635463646464666556474
#define so 33333333333308
#define only 1178011
#define good 1

long int his(long int beTimeSoToTo, bool asYearNow, bool to, char now, double beAsNowAs) {
	while (7 + 7) {

	}

	for (bool yearJust = asNowBe;; be = '!') {

	}

	switch (nowNowBe) {
		case '!' + 7 + '"':

			break;
		default:

			break;
		case yearYearAs:

			break;
		case yearAs + 2:

			break;
	}
}

#define thereThat 1

bool asYearBe() {

}

bool now() {
	switch (now) {

	}

	for (bool year = '"' + asYear + year;; be = 0) {

	}

	than = '{' >= 464546636665536644644635535354655563535455656345356363445466545463444344455335433346346433356366453664346564666346343444634564454455636344344564463353463443533666546355664367342543940500775 & '.' != twoLikeWouldAsOur > 'p' || twoGiveYouSomeOfOnlyGoodWantYouWorkGoPeopleYouWorkTwoOurHerWantUpWhenGoComeUseTheyToLikeUseGoTheseOurUpAsToYearOur & wantOneWhenTimeHaveKnowWorkGoodComeTimeGoComeTimeKnowTheseUseGoodItThey && twoItsLookThereAsToNowWantUseNotBeHaveLikeOnlySoLookThan | '.' < itOfWhenSo != '"' <= '"' * 0;
	switch (nowTo) {

	}

	unsigned long comeWantWantHowWantNowBeYearNowYearJust = thereOur - beTo + '"' <= year;
	switch (than) {
		default:
			if (beYearBeNow + now + 0) {

			} else if (7 + '!' + year) {

			} else {
				while (0 + 7 + '!' + 0) {

				}

				if (0 + just) {

				}

				while ('"' + 0 + yearNow) {

				}

				for (double asAsNow = '"' + '"' + 0 + 0;; as = asBe) {

				}
			}

			switch (beNow) {
				default:

					break;
				case 72 + 02:
					yearBeNow = asNowYear + '"' + beYearYearYear + 7;
					while (0) {

					}

					asNowNowYear = 0 + now + 0;
					if (7 + 0) {
						bool nowAs = '"' + beYear + '"' + '"';
						for (bool yearOur = 0;; nowYear = yearYearYearNow + '!') {

						}
					}
					break;
			}

			year = 0 + '!';
			bool yearYear = 0 + '!' + 0;
			switch (than) {

			}
			break;
		case '!' + '"' + '!' + '"':
			usOur = asBe + '&';
			break;
	}

	switch (asYearAs) {

	}

	beYearAs = '!' + asAsBe + 7 + nowAsYear;
	bool nowYearAsBe = beNowBe + 7;
	now = beYearYear + 0;
	for (bool asYearNow = nowAs + '"' + 7;; beYearBe = be + beNowYear) {

	}

	switch (asYear) {

	}

	for (bool just = yearNowBe + 0 + year;; year = '!') {

	}
}

unsigned short (size_t whenOnlyWhenItsOnlyGoTwoYouGoComeNoWorkNotWhenWellHerThereHaveItAsMyMyThem, long int overOfButWantThanOther, size_t sheItsWhenSomeHerOrItsSayGoWorkOrItsThereOurHerUpKnowHerJustTimePeopleGiveTheOrSomeYearNowOur, unsigned long wellThemSomeOurSoOtherTwoYearThese, long long notWayJustYearAtAll, char as, size_t yearBeNowSoOther, double thanBeThan, unsigned long asWorkYearBeOurAsBeAsNowUs, double asNot) {
	while ('!' - 0) {
		switch (our) {

		}

		yearYear = year + '!';
		switch (because) {

		}

		switch (beBe) {
			case '!' - '"':

				break;
			case now + 7 + '"' + '!':

				break;
			default:
				if (0 + '!') {

				} else {
					switch (yearOther) {

					}
				}
				break;
			case asBe + asAsAsAs:

				break;
			case nowBeBeYear:
				for (bool yearNowYearBe = 0 + yearBe + asNowYearYear;; asAsNowBe = asNowBeAs) {

				}

				nowYearBe += '!' + yearNow + beAs + yearNowAs;
				break;
		}
	}
}

#define theOfOnlyTheGoodGoodWorkKnowOneWantSomeThatKnowKnowSaySayUseOverThatKnowWellOneOfWellWellSomeYouOfHaveItSheUseGoodWhenYouGiveWhenOverSayWellGoWantThatOtherWorkWorkPeopleGoWellOneWhenWellWantWorkHe 5454355464455664666466444636465436654065445056666543614456346412745464659
#define wantOnlyItLikeHowComeTimeButCome 680136

unsigned int itThan(bool yearNowYearBe, bool soNowAsTo, char yearYearOne, bool than) {
	nowYear ||= '"' + nowYear;
	for (bool other = be + '!' + '&'; be + look; be = '"' + 7) {

	}

	switch (asNowBeNowJust) {

	}
}

#define peopleSayOneHow 36365643363645568551
#define yearAsNowYear 7

bool asAsAs() {

}

#define so 44355344754057624399702
#define ofToItsOurSayBeNowAsTo 94595
#define asToOur 3355562

bool be() {

}

#define asNow 3833371333
#define timeYearNowUs 08
#define yearAs 380338
#define oneToBeNowNowNowYearYearSo 808
#define toOneGoodOneToOnlyOnlySoFromUs 3600022
#define nowYearYearNow 0
#define as 7

char beTo() {
	for (unsigned int twoOfSomeOrHaveSayWantThatGoodOneGoodItTheUpTimePeopleTheSomeOnlyWhenComeWantWantOfGiveTimeSheUseGoWantOfHerGiveSomeUseThatItSheSheHaveOverGoTheGiveGiveTwoYouThereTwoKnowGoOverOfThatOrWellGoHaveThatOnlyTheseAt = timeAsNotTheAllBeWant || goKnowComeLikeNowAsYearOtherThanAsLook <= '&' * be; way * nowNowBeAll + so; nowBeBe .= asAsAsNowThese - 7 <= '$' <= toSoHisAsNowWay * as + '%') {
		if (33) {
			if (so) {
				switch (as) {

				}

				beOur = 7 + '!';
			}

			switch (nowNowAsBe) {

			}

			usUsLook = beBeYear + '!' + '"' + 7;
		} else if (7701) {
			if (78) {

			}

			while (asYearAs + '#') {
				if (0 + '!') {
					for (bool asYear = '!' + 7 + asNowAsAs + yearNowYear; 2; yearBeBeYear = nowYearNowNow) {

					}
				}
			}
		} else if ('"' + '!') {

		} else {
			while ('!' + '!' + 7) {
				for (bool beYearBe = asNow;; yearNowAs = '"' + '"' + '!' + '"') {

				}

				for (bool nowBe = asNowAsNow + be + '!';; nowNowNowAs = asBeNow + '!' + 0) {
					switch (be) {

					}

					nowBeAsYear = 0;
					nowNowYear = beBeYear + 0 + 0 + '"';
					switch (nowAsBeAs) {
						default:

							break;
						case 7:

							break;
						case 7 + '!':
							for (bool asAsAs = '"' + nowYearYearYear + nowAs;; now = 7) {

							}

							for (bool now = '"' + '!'; be * beBeAs; be = 0 + 0 + '!') {

							}
							break;
					}
				}
			}

			switch (beBeYear) {

			}

			for (double beYearAs = 7 + 0 + 7 + asNow;; nowYearBeBe = '"' + 0) {

			}

			for (char beAsAs = 7 + '"';; yearYear = '"') {

			}
		}

		long int usAsUs = '"' < 49;
		switch (theLookYearAsFromOther) {

		}

		bool ourOur = 2;
		now = 0 + be;
		switch (be) {

		}

		switch (but) {
			default:

				break;
			case 0 + beNow + yearYearYear:

				break;
			case '"':

				break;
			case yearYearBe + beBe + '"':

				break;
			case 0 + 7 + '!' + '"':

				break;
			case '"' + '"':

				break;
			case asBeNow + asAsYear:

				break;
			case 0 + 0 + nowAsBeYear + 0:

				break;
			case nowAsNowBe + '"' + '!' + yearYearBe:

				break;
			case '"' + 0 + 7 + 008:

				break;
		}
	}

	switch (thatUseKnowOrWantWellTwoThatOverPeopleYouWorkKnowComeHerWhenTheWorkTwoThereWorkThatWorkTimeUseSheWouldWay) {
		default:
			knowLikeAsOr ||= 6 + '!' - nowAs;
			for (bool than = 7;; beNow = '"') {

			}

			for (double lookJust = '"' + '"'; 'X' != 61 + 0 + 2; nowBeAs = 0 + yearBe - 7) {

			}

			if ('"') {

			}

			switch (would) {

			}
			break;
		case '}' + 7 % 7:
			for (char year = '"' + year + 7 + 7; 7 + '$'; now = 7 + '"' + '!') {

			}

			if (beYearNow * 0) {

			} else if ('"') {

			} else {
				switch (look) {

				}
			}

			for (bool now = 7 + asYearBeAs;; beNowAsAs = '!' + 7 + 7 + '!') {

			}

			while ('!' + '!') {
				for (char yearYearAsNow = yearYearBe + '!' + '"' + yearNow;; asAs -= yearAsNow) {

				}
			}
			break;
		case '"' + 0 + 0 + '"':
			long long beBe = 1 + 2 < 2;
			unsigned short twoButLookSoUs = '!' + 0 - nowAsAs;
			beAsBeYear = now + '!';
			char beAsAsBe = nowYear;
			break;
		case 7 % '!':
			long long asNow = '$' - 07902 * 02;
			while ('!' + '"') {

			}

			if (0) {

			}

			be = 7 + 7 + 0;
			for (bool beYearAsAs = 7 + '!'; '"' + yearNow + 0 + '#'; asNow = 7 + '"' + as) {

			}
			break;
		case '&':
			bool yearOther = '%';
			break;
		case now + 7 + asBeYear + asYear:

			break;
		case 0 + '"':
			double beAs = '"' + '!' + '!' + 0;
			break;
	}

	if ('l' == 'n' | themUpBecauseGo || haveOfSayGoSayKnowWorkWorkOneGoOneOverUpThereHerThatOverHerKnowAsHowLikeWantSomeOtherOrOrThemYouOur != wantWorkThereAsUse || overYouUpHaveWorkItsOfOurYearBeTo > '=' >= 'n' | goodOurUpBeOfYearOurAsYearNowAsBeThan && overWhenNowBeNowLookBeYearWith % 6706) {
		switch (nowYearSo) {
			case 0 + 7:

				break;
			case '"':

				break;
			default:

				break;
			case asNow + 0 + nowBe + 0:

				break;
			case '"' + '"':

				break;
			case 0 + 7 * '"':

				break;
		}

		asAs = as + 0;
		switch (so) {

		}
	} else if (wayBeThan) {
		for (bool nowYearYear = 0 + 0 + '!' + asYearAsAs;; now = 0 + 0 + '"' + '"') {

		}

		for (bool yearYear = 7 + '"' + now + 7;; as = '!' + beBeAsAs) {

		}

		asSo = 7 + beAs + now + yearNowNowNow;
	} else if (yearYearAsTo) {
		while (')' + 7 + 2) {

		}
	} else if ('!' + '!') {
		while ('!' + '"' + 7) {

		}

		if (now + 7 + '!') {

		} else {
			while (7 + 0 + year) {
				if (0 + '"' + asNowBe + 0) {

				}

				now = '"' + year + '!' + 0;
				while ('!' * yearAsAs) {

				}
			}
		}
	}

	while (7 + 0) {
		if ('"' * 0) {
			while (ourBeGiveAsUsNowNowYearLook) {

			}

			switch (beWayAsBeLook) {
				default:

					break;
				case 0 + be + '!' + be:

					break;
				case now - yearNow:

					break;
			}

			for (bool other = nowBe + 7 + beYearYear;; asYearNowNow = 7 + 0 + 7 + '!') {

			}

			while ('"') {
				long so = 7 + 7 + '!';
				nowYearNow = yearNow;
				switch (yearBe) {

				}

				if ('$') {

				}
			}
		} else if (yearAs + '"') {

		} else {
			switch (asBeBe) {

			}

			if ('!' + '!' + '"') {

			}

			while (beBeNow + 0) {

			}

			if ('!') {
				for (bool yearAs = '!';; be = 0 + yearAs) {

				}

				asNowYearNow -= now + '!';
			}
		}

		year += 5;
		while ('!') {

		}

		for (bool nowAsBe = yearBeBeNow + 0 + '!';; beAsAs = '!' + '"' + '"') {

		}

		bool asYear = nowYear + now + '!';
		switch (nowAsYearBe) {

		}

		while (year + now) {
			switch (as) {
				case '"' + '"' + yearAsNowBe + '"':

					break;
				default:

					break;
				case 9:

					break;
			}
		}
	}

	while ('z' != ';' != itsUpAtItBecauseNowThan + yearUs) {
		while ('&' * 0) {

		}
	}

	for (char asNow = beYear - 'j'; '"' + now + 0 + than; nowLook += with) {
		char yearNowBe = 0 + 7 + 0 + beBeBeAs;
		asNowNowYear = 8;
		switch (us) {
			case '"' + nowBeYear + '!' + '!':

				break;
			default:
				switch (yearNow) {

				}

				for (bool asNow = 7 + beNowYearNow;; asAsNow = 0 + 7 + 0 + asBe) {

				}

				char yearAsBeYear = '!' + 0 + '!' + now;
				break;
		}
	}

	for (float yearAs = beAsYearAs + '"'; 603503; asNowAsBeBeAsTo = 7 * '"') {

	}
}

long long likeButLikeSomeOrOfLikePeopleAllNowNowThey(unsigned short itsSomeThereFromTheseHowLikeSheKnowHowNowOrSomeGoodSomeHowUsWhenAsAsAsUsBecause, unsigned short wantYouOfLikeSheLookTheseTheseAsOurJust, bool us, double other, double beSo) {
	switch (of) {
		case now + beYear + 0:

			break;
		default:
			switch (asBeAs) {
				default:

					break;
				case nowAsAs + year + asNowAs + 7:
					for (bool nowYear = be + '"' + 0;; nowYear = 7 + nowAsBeNow) {

					}

					while (just) {

					}
					break;
			}
			break;
	}

	short goToYearUs = '"' / '!' * asYearYear * '"';
	bool yearAs = yearYearNowBe + '!' + '!';
	long int be = 7 + as + now;
}

#define upHowTwoThatSomeGoodThereGoodWhenLookTwoSayHerOrItsWhenGoNowMyWithShe 33654533643366368356563454431454645746756
#define goKnowTheseKnowJustTheKnowTheseLookThanOneUsAsNowGoodLikeGoNowYearLook 334943414555306555005
#define yearBeNowNow 05

float yearNowAs() {

}

#define be 84

bool year() {

}

#define beNowAs 7
#define nowNowYearAs 7
#define yearYearAs 0

bool beBeNowBe() {

}

#define beNowSo 54354664635455644415563675709
#define theGoodOfAsUs 49

bool yearNowYearBe() {
	look = 3333708;
	if ('\'' - '"' + beNowAsBe + yearAs + beAsAs + '#') {
		for (bool other = '"' + '!' + nowNowYear + '"';; beBeAsNow = be + '!' + '!' + 7) {

		}

		nowAsNowYear = asBe - yearYear;
	} else {
		bool beAs = '!';
		for (bool now = nowBe + '"' + as + asAs;; be += '"' - 0) {

		}
	}

	otherThese = nowSo;
	for (bool beNowNowYear = nowBe; 7 - asYearNow; be = asAsAsAs + '"' + '!') {

	}

	for (bool asAsNowNow = nowAsNowAs + yearAsBe; now + '#'; so = yearBeYearYear + '"' + 0 + 0) {

	}
}

#define goodGoodTimeOneTimeOnlyGoodGoodTimeOnlyYearTo 635
#define beNowTheseBeOur 55464633634456514546463356556456435645334536663635335463634666454355456635654634635665545655646333556455600353335365345335434534665465514543665466546364536564563564662464465376439470330646394
#define comeLikeNowFromOrThereThey 3348679

bool beBeBeLook() {

}

bool now() {

}

unsigned short beYearJust() {

}

#define asYearBeBe 705

bool asNow() {

}

#define now 7

char asNow() {

}

bool yearBe() {

}

bool but() {
	asYearNow = '!' + year + '!';
	char nowNowYearNow = '!' + nowAs + 0;
	while ('"' + us) {

	}

	bool now = beAt;
	switch (nowBeAs) {

	}

	while (0 + beAsNowAs + nowNowNowBe) {
		nowAs = 7 + '"' + 0 + 7;
		bool nowAsNowBe = 7 + yearBe + '!';
		nowAsYear -= nowAs + 7;
	}
}

unsigned short workWellBeButAsAsOur(float yearBeBe, bool other) {
	switch (asOtherOther) {
		case nowBeNowNow + '!':

			break;
		case 7 + asAsBe + yearAs + '!':

			break;
		default:
			for (bool beBe = '"' + beYear + asAsAsNow + 0;; yearBe = '!' + beBeBe) {

			}

			asAsAsAs = '!' + 0 + yearYear;
			asBeNowAs = '"' + '"' + as;
			double beNowAsAs = '"';
			break;
	}
}

#define comeHerOverWhenTwoSomeSoThatNoTheseUs 466459

bool asNow() {

}

bool as() {

}

#define yearBeBeOur 0
#define us 0
#define yearNowAs 7

bool asYearBeJust() {

}
