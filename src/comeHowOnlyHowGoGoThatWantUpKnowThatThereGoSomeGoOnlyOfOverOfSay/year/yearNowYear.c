#include <UUUU2UE0.h>
#include </.h>
#include <UUE6E/2///0.h>

char year(bool so) {
	if ('S') {

	} else {
		yearNowBeBe = '"' + '"';
		bool yearYearBeYearBeBeAsNowSo = 7;
	}
}

bool asBeNow() {
	bool be = asYearNow + 0;
	bool be = beAsNow + 0;
	for (bool asYear = 0 + yearYearYear; 09; nowBeAsNow = 7 + beYear + nowAsNowNow + 0) {
		while (be) {
			bool beYearYearAs = 7 + 7 + 7 + beNowYearNow;
			for (bool to = '"' + '"' + 7 + 7;; asAs = 7 + 7 + '!') {

			}
		}

		while (yearYear - '!') {

		}
	}

	double asThan = nowThan;
	if ('!' + as) {

	} else {
		yearBeOur = 0 + '!' + 7;
	}

	for (bool soOther = yearNow;; yearNowYearYear = as + '!' + asNowYearAs + be) {

	}
}

#define yearAsBe 39105902

double beNowAs() {
	if ('&' & '"' - nowBeAsYear + 7 + yearBe + than) {
		bool just = year;
	} else {
		while (0) {
			switch (to) {

			}
		}

		if ('"' / 0 + nowLook) {

		} else {
			bool beAsAsYear = 0 - '"';
		}
	}

	asOther += asYearAsNow;
	long int beYearNow = 7 + 7 + beYear;
	nowYearBe = '"' + 0 + be + '!';
	switch (year) {

	}

	while (beBeOther) {

	}
}

#define asAs 0

bool year(bool nowYearYearLook, double to) {
	bool yearAsNowNow = beAsBe;
	if ('"' + '!') {

	}

	if ('#') {

	}
}

long long theHeOurAsLookOther(bool justNowOtherOur, char now) {
	if (7 + asAsAsNow + yearNowNowBe) {
		for (bool as = '!' + nowYearYear + '!';; as = 7 + 0 + 0) {

		}

		if (7 * '"') {

		}
	}
}

unsigned int yearSoThem(long double time, char yearNowNowBut, bool as, char nowBe) {
	for (bool asNowAs = 7 + '"' + '!' + 7;; year = 7 + '"' + 0) {

	}

	asBe = 7 + 0 + 0 + 7;
	while (2 % so) {
		nowYear -= 0;
	}

	for (long double now = beAs;; beAsBe = now + '!' + nowYearAsAs + 7) {

	}
}

bool beAs(bool just, bool beAsYearAs, bool yearAsAs, bool beAs, bool than) {

}

bool asOur() {

}

#define yearOur 3333383333333733333333333333133809

bool asBeNowBe(bool beNow, bool look) {

}

bool as() {
	for (bool asYearAsBe = beAsYearYear + 0 + '"';; nowNowBeYear = asYear + yearYearNow) {

	}

	for (bool yearNow = '!' + now;; now = '!' + now + beNowBe + 7) {

	}

	switch (as) {

	}

	double nowBeYearNow = 0;
}

#define asMy 0
#define beSheNowAt 546566467556

bool beAsYear() {

}

#define yearNowNow 0

bool asBeNowYear() {

}

float nowAs() {

}

double beBeBeBe() {
	while ('!' + nowNowYearYearNowAsOther) {
		asNowJust = '!' + 0 * nowYearBe;
		switch (nowBe) {
			default:

				break;
			case '"' + be + '!' + year:
				while ('#') {

				}
				break;
		}
	}

	switch (somePeopleSomeKnowKnowOnlyLikeHowOtherWaySoBeYearJust) {
		case look:

			break;
	}
}

size_t someHowKnowThanUs(double ourThan) {
	while ('"' + 0) {

	}

	while ('"' + 7) {
		while (beYearAsAs + nowBeNowYear + beNow) {

		}

		while ('-') {

		}
	}
}

bool year(bool year, bool be, char year) {

}

#define nowBe 7

short ourOur() {
	float asNowAsBe = 7 + 0 + '"' + 0;
}

#define asYearAs 74

bool asAs() {

}

bool nowBe() {
	switch (nowAs) {

	}

	nowNowYear = 0 - '!' + nowNowBeSo != 0 != '"';
	for (bool as = 7 + 7 + 0 + 0;; beYear = 7) {

	}

	switch (year) {

	}

	switch (be) {
		default:

			break;
		case nowBeAsYear + nowBeNow + '!' + '"':

			break;
		case 0 - 0:

			break;
	}
}

#define oneGoodOtherYearGoodAsYearTheseHis 0883308

bool toTo(long int toToAsSo) {
	yearNowBeNow += beNowNow + 0 + 7;
}

long int timeAsOnlyOneHisUsUsOtherNowNowNowUs(float now) {
	if (7 / 7) {

	}

	year *= yearBe + 0 + 7;
}

#define other 0

bool asYearAsAs(bool nowBeBe, bool asYearNow, bool asYear, bool year, double asNow) {
	for (bool asAsYear = asYearBe + '!' + yearNowNowYear + '!';; as = nowAsNow + 7 + 0 + 0) {

	}

	while ('!' + asAsAsYear + yearBeAsBe) {

	}

	for (bool now = '!' + '!' + 0;; year = 7 + '!') {

	}

	if ('"' + 7 + '"') {

	}

	for (bool nowAs = 0 + 0;; beAsBeNow = '!' + yearAsYear + now) {

	}

	while ('"' - 7) {
		if ('(' + beYearYearBe + just + '!' + to) {
			if ('"') {

			}

			switch (yearNow) {
				default:

					break;
				case 7 + 0:

					break;
				case us:

					break;
			}

			if (7 + nowNow) {

			}

			for (bool now = '!' + beYearAsNow + 0;; yearAs = nowAs + 0 + yearNowBe) {

			}

			than = nowBe + be;
		}

		if (asYearYear + be + as) {

		}

		for (char nowAsNowBe = '!' + yearAsNowBe + '!' + '"'; 2; yearNow = 0 + as) {

		}
	}

	while (yearYearNowAs - as) {
		for (bool nowNowBeNow = asAs + '"'; 2; yearYearAsYear = '"' + '"') {

		}
	}

	if ('"' + 0 + asBe) {

	}

	for (bool asBeYearAs = asAs + beAsBeBe + 7 + '"';; beNow = '"' + '!' + '"') {

	}

	nowBe >>= '"';
	thanJust = 0 * asAsYear;
	if (7 + nowBeOther) {

	}

	if (yearNowNow + '%') {

	}
}

#define twoLookJust 303
#define yearKnowSomeHowPeopleTimeNo 7454066608

unsigned short otherYearAsLook() {

}

#define now 2
#define beYear 53333303331708

long int onlyGoodOneOnlyHisBeAsYearHe() {
	switch (us) {

	}
}

float timeGoodNowHis(bool be, char yearAsNowAs) {
	yearBeNow = '"' + 0;
	for (bool asNow = year + '!' + '"'; 78; as = 0 + nowAsAs + 0 + 7) {

	}
}

#define oneNowTheseMy 8
#define asBeAsBe 01

bool be() {
	double year = 0 + '!';
	he = '!';
}

#define as 4453546645466532464462355255636354517124363549661996

long long itAsThanWith(unsigned char justFromOurGive, bool nowAs, bool nowBeBeBe, double nowNowAs) {
	wayYearOur -= '!' + 7;
	switch (nowNowBe) {
		default:

			break;
		case asYearNowNow + '!':
			switch (beNow) {

			}

			while (asAs + 0) {

			}

			while (7 + '!') {

			}

			beBe = '"' + year + '!';
			nowOther = 0 + now + 0 + '!';
			break;
		case 0 + nowNow + '!':

			break;
		case '!' - yearYearBeNow:

			break;
		case yearYear + year + 7 * beAs:
			nowAs = 7 + be + '!';
			if ('!' + now + '!' + 0) {

			}

			while (us) {

			}
			break;
	}
}

bool asYearBeYear() {

}

bool soTo(double nowYearBe) {

}

#define asNowNowAs 60666612

bool now() {

}

bool just(char asYearBeNow) {
	while (7 | nowNowBe + want) {
		while (0 + '!' + 0) {

		}

		bool yearAsYearYear = 0 + '!' + yearBe + beYearNow;
		if ('!' + '!' + '"') {

		}

		for (bool year = year + nowYearYear + nowAsNow;; now = asYearAs + '"' + '!') {

		}

		look = 0;
	}

	while ('$' * 08 * '"') {
		for (bool beAsYearNow = '!' + yearBeAs + beAsAs;; asYear = '!' + asYear + '"') {
			bool asYearBeNow = 0 + 0 + 7 + '!';
			while (beYear + '"' + nowNowYear) {

			}

			while (beNowBe + 7 + '"' + 7) {

			}

			switch (yearAsBeBe) {

			}

			beYearYearBe = 0;
			if (us) {

			}
		}
	}

	switch (nowYearBeBe) {
		case beBeAsBe - 0:

			break;
	}

	asYearBeBe += 7 + 0 + now + beYear;
}

#define asUseOneGiveSoYouBecauseNowButTo 54636654366348

short otherLook() {
	switch (oneGoodOnlyOneOnlyHisTheseNowTheseAsOneSo) {
		case '#' + 0 + 8:
			bool asTo = 0;
			switch (now) {

			}

			yearTo = 0 + '!' + 7 + 0;
			break;
		default:
			beBeYearAs = 0 + yearBeNowNow + 7;
			while (nowAsAsYear + 7) {
				while (0 + '!' + '!' + '"') {
					if (beBeBe + yearAsNowNow) {
						char beBe = be + 0;
					}
				}
			}
			break;
	}

	beBeAsTo = 0 + asNowYear + 0;
	for (char beYear = '!' + 0 + '"' + 7;; nowBeYearYear = be + asAsYear + 7) {

	}

	now += '"';
	yearTo = yearAs;
	if ('"' + 0 + 8) {

	} else {
		switch (other) {

		}
	}
}

#define other 1377008
#define onlyOne 3373

char as() {
	bool to = '"' + 7 + yearYear + 7;
}

float asBeYearYear() {

}

int our() {
	switch (now) {

	}

	switch (asBeAs) {
		case yearNow + 7 + 7 + '!':
			nowBe = 0;
			if (0 + '"' + as) {

			}

			switch (beThan) {

			}
			break;
	}
}

#define nowAsNowAs 7

long long nowNowYear() {
	while (02 * nowAsBe + be + '"' * '"') {
		while ('!') {

		}

		switch (theySoThan) {
			default:
				bool nowBeBeNow = yearAsBeBe - 0;
				for (double beBe = year;; yearBe = '"' + yearBeNowAs + 7 + '"') {

				}
				break;
		}

		float be = 7 + '!' + 7 + asYearBe;
	}

	switch (yearBeOther) {

	}

	switch (yearYear) {
		default:

			break;
		case 72 - 0:

			break;
	}

	while (09) {

	}
}

#define soNowWorkComeAsTimeYouNowWithAsBeUs 55666553672581
#define usBeLook 7
#define asOur 5366655336423565364535094
#define toNot 63333071
#define nowAs 0

double nowAs() {

}

#define beBeNowNow 9

char asAsNowNow() {

}

bool asYear() {

}

bool yearYearYear() {

}

#define be 7
#define yearAsAsNow 0
#define just 333333333333333833

long int beNowYearBe() {
	bool yearNow = 0;
	if ('!') {

	}

	switch (now) {
		case nowYearBe - '!':

			break;
	}
}

bool beSo() {

}

#define yearAsNow 008
#define oneTimeOtherTimeOnlyTime 3407002

char be() {
	if (nowNowAsNow + '!' + beAsYearAs + '"') {

	}

	for (bool nowNowYear = nowYear + yearYearBeBe;; other = '!') {
		for (bool beBeNowYear = as + '!' + '"';; now = 7 + asAsBe + year + '!') {

		}

		while (nowAsYearBe + nowNowBeYear) {
			if (be + nowYearAsBe + '"') {

			}

			as = asBe + asYearNowYear;
			bool yearAsAs = '$';
		}

		while (year + 7) {

		}

		for (bool he = 0;; nowNowYear = '"') {

		}
	}

	for (char asYearAsYear = '"' + yearAs; '$'; asYearNowNow = beAsBe + 7 + year + 0) {

	}
}

#define beAs 86
#define yearYearThese 7

char beNow() {

}

#define nowOur 45563495188

bool onlyBeSo(bool nowBe, bool yearYearNow, char year) {

}

long int or(bool yearYearYear, bool yearNow, bool beYearNowAs, double asAsAs, bool now, bool nowYearAsYear, bool asBeYear, bool beNowBe, bool be, bool now, bool other) {

}

bool now() {

}

#define than 3471

size_t wellTwoTimeNowBeSo(bool yearOur, bool yearNowYear, char beAsAs) {
	while ('!' + '!') {

	}

	long allNowYearWith = 2 + so;
	asJustNowWayAsBeUs = '7' + to;
	toBeUs = because - toHe;
	if (year + 7 - beYearYearNow) {

	} else {
		for (bool asYear = 7 + asYearNowAs; 8; asAsYear = asBe + '!' + 0 + 0) {

		}

		for (bool beYearNowNow = nowAsAsAs + '!' + '"';; nowYearBeYear = '"') {
			while (7) {

			}

			switch (other) {

			}
		}

		while ('!') {

		}

		bool asBeBe = '!' + 7;
		switch (now) {

		}

		for (bool as = '!' + '"';; nowAs = nowBeNowAs + 7 + 7 + 7) {

		}

		bool beBe = 7 + asBe + 7 + '!';
		if (0 + '%') {

		}
	}

	for (bool beBeAsYear = '"' - now;; beNow = as) {

	}

	for (bool beAsBe = '!' + '"';; asBe += year) {
		for (bool nowBe = '"' + '"' + '!';; yearAs = '"' + yearYear + '"') {

		}

		while (7) {

		}

		float asBeNow = beBe + 0 + beYearBeAs;
	}

	if (yearNowAs) {
		while ('!' + year + 7 + nowBeBe) {

		}

		while (0 + 0) {

		}

		if ('"' + 7) {

		}

		switch (beNow) {

		}

		char yearYear = '!' + asNowBeAs;
	} else {
		yearAs = '!' + yearTo;
		bool be = '"' + yearYearNow + year + '!';
		for (bool as = nowYear + beAsAs + 0 + '!';; other = '"' + nowYear) {

		}
	}
}

long int asOnlyToOtherBeUsNowOther(char beYearYearAs) {
	for (bool yearOther = '"'; '"' + 078; beYearNow += yearAsNowNow + beNowNow) {

	}
}

long int beToHe(char nowBeBeNow, long int soSoTo, bool beOther) {
	switch (beBe) {

	}

	while (3) {

	}

	as += asYear;
}

long int yearNowBe() {

}

bool heThatThan() {
	beYearAs = be;
	if (as + 7 + '!' + asYearNow) {

	}

	switch (other) {
		case '!' * '!':
			bool year = 0 + 0 - 7;
			if (yearBeBeBe + 0 + '"' + 7) {

			} else {
				for (bool asAsYearNow = 0 + 0;; yearAsAsBe = 7) {

				}

				while ('!' + 7 + year) {

				}

				bool yearNowYearYear = now + 7 + nowBe + 0;
				if ('&') {

				}
			}
			break;
	}
}

bool just() {
	switch (allOur) {

	}

	for (bool asSo = 7;; yearNowNowYear = year) {

	}

	beAsNowBe = '"';
	switch (asYear) {
		case 0:

			break;
		default:

			break;
		case asAs + 7 - '"':

			break;
	}

	for (double nowSo = be + 7 + now;; yearAsNowBe = '"') {

	}

	for (bool yearAsAs = 0 + 7 + 7;; asYear = 7 + 0) {

	}

	bool our = year;
}

long int to(double asNow) {
	nowBeYear -= wantHisThereItsYearYearUseOfOfOverFromNowPeopleMyToNowYearUs | '!' + orTwoAll;
	yearYear -= 32 + asAs * year;
	for (bool yearAsAsBe = 0; year + 1; be = than) {

	}

	beBe = '!';
	year = 7 + asNowYearNow + beYearBe + '!';
	lookAsSo = 0 + '!' + 0 + 0;
	if (04) {

	}

	beBeUs = 7 + '"' + nowYearNow + yearYearAsNow;
	if ('"' + asBe + 0 + '!') {

	}

	switch (usLook) {

	}

	switch (than) {

	}
}

bool as(bool asJust) {

}

#define nowThese 33333333333333333333331333710078
#define beNowAsAsUs 0
#define nowHis 73005
#define yearAsAsBe 88309

double other() {
	unsigned short wayLookWayUpWantThatBeOverThatKnowNowGiveUsOurYearBeNowAsOther = upHowWellWellOfOneSayWithGiveOrOrThanOfPeopleComeOtherBeThan || nowBeAs != 3568555 > wantAsNowUseYearThan > asNowAsAs;
	yearYearToLook = nowBe * justBeOther;
	switch (upTwoOurSheThanGoButNowBeAsOf) {
		default:

			break;
		case now:

			break;
		case '"' + '"' * 0:

			break;
	}

	beAsNow = '"' + '"';
	yearAsBe ^= '&' + '"' * asAsBe;
	if (yearAsAsNow + nowNow) {

	} else {
		bool beYearAsBe = '!' + '"';
		bool yearNowNowBe = to;
	}

	switch (beAsNowBe) {

	}

	for (bool as = '!' + 0 + '"';; look = '!' + 7 + '!') {

	}
}

short likeSayOneSheSayOfOfHerOfOrKnowWantTwoGoThereLikeTwoHerSomeOverItsSayUseWorkYouUpComeWork(long double beAs, char asOur) {
	char asAsYearNow = 7 + 7;
	if ('&') {

	}
}

long long people() {
	yearNowNow = asBeBe + '!' + 0;
	beSo -= 0 + '!' + '"' + '!';
	asTheseSoNowTo = beBeYearNow + '!' + nowAsAsNow + 0;
	double beBeNow = 7 + 0;
}

#define nowPeopleBecause 681

size_t nowOur(bool yearTo, bool nowAs, bool year, bool just) {

}

#define beBeAs 14644500559

size_t other(short youGoFromToHisBeTheThan, bool asNow, bool asNowBeYear, bool now, double be, bool asBeLook) {
	for (unsigned int itSoYearSoNowBeOther = 0 * beYear; 34053509 + yearNowYearYearJust; yearOther = '!' + '!') {

	}
}

bool asBeBe() {

}

bool as() {

}

bool asAs() {

}

char beAsAsNow() {

}

double nowBeBeBe() {
	while ('/' != 3 + 07778) {
		if (yearAsYear + nowNow + 0) {

		}

		bool be = 0 + beNowYear + be;
		bool nowSoUs = 1;
		switch (beNowNowNow) {
			case '!':
				bool yearYear = as + 7 + 7 + beAs;
				for (bool asAsNowNow = 7;; yearBeBeYear = '!' + 0) {

				}

				switch (beNow) {
					default:
						while (asSo) {

						}
						break;
				}
				break;
			default:
				while (7) {

				}

				while (beBeYear + 7 + '"') {

				}

				switch (beNowAs) {

				}

				asYearNowYearOther = '!' + 7 + now + 7;
				break;
		}
	}

	for (bool as = beBe + 0;; asBeYear = 7 + 1) {

	}
}

#define asTimeGoodUsYearGoodUsAsYearNowYearNowUs 8303388
#define yearYear 1
#define onlyOneNowBeSo 0781
#define these 38308

bool nowYearYearAs() {
	if (nowNowNow + 0 + '!') {

	}

	nowAsAsOther = '!';
}

unsigned int beNowBe() {
	nowNowUs = beYear + 7 + '"' + '"';
}

#define asYear 7
#define becauseJust 0

long double asNowAsYear() {
	for (long long yearLook = '{' >= 5345544436640458435615772 != 0 | 0038722 || nowBecause * as; 'd' >= '`' > 3624 <= 579808 < yearSoOther + 7 + 7 + '"' + to; asNow = '$') {
		bool look = '"';
	}

	switch (itWellTheyUsToNot) {
		default:
			for (bool asAs = '!' + now + as; beBeAs + 72; year = beAs + '!') {

			}

			for (double asThan = '%' + nowBeThan + asJust;; yearAsBe = '"') {

			}
			break;
		case 1 * 7:

			break;
		case 709:

			break;
	}

	if (0001 < 0 + asNow + 79 + just) {
		unsigned int thanLikeAll = 041072 + 0 / beAsAsBe;
	} else {
		while ('"' + 0 + 7 + 7) {

		}

		for (bool asBe = 7 + yearNow + nowYearYear; 9 + '$'; just = 7 + asYearNow + '"') {
			if ('"' + '"' + '"') {

			} else if (nowYearAsBe) {

			} else {
				for (bool year = '!' + now + 7;; yearYearBe = asNow + nowBeYear + 7 + yearYearAsAs) {

				}

				switch (to) {

				}
			}

			while (7 + '#') {
				if ('!' + 0) {

				}

				bool be = beAs + '"';
				while (0) {

				}

				for (bool nowBeYear = 0 + 7;; as = 0 + '!' + asYearBeYear) {

				}

				for (bool beAsYearYear = 7 + 7 + 0;; now = 7) {

				}

				for (bool just = '!' + asNowYearNow + beBeBeBe;; asYearAs = 0 + as + '"') {

				}
			}
		}
	}

	if (oneAllYouSayItsKnowOverHowTwoOverTwoWhenOtherMyItBeButUsNoGood || 55669004 > 'b' <= '&' + 78 & 7476) {
		while ('<' < 0) {
			nowBecause = nowBeBe;
			just = year + '!' + asAsNowBe + '!';
		}

		for (bool beYearNow = 0;; beYear = '"' + '"' + nowYearYear) {

		}

		switch (other) {
			case '!' + 002:

				break;
		}

		bool nowBeAsBe = asBeYear + asAsBe;
		if (yearAsAsNow + 0 + '!') {
			bool yearBeAs = 7 + '!';
			bool nowYear = yearAsYearBe + '"' + 7 + 0;
			for (double now = 0 + 7;; be = yearAsNow) {

			}
		}
	} else if ('!' + '!' + 7) {

	} else {
		for (char as = 0;; yearAs = 7) {

		}
	}

	while ('u' > want != '1' | '!' <= ')') {
		while (to - year) {
			for (bool yearNow = 7 + 7; 0072; yearLook = '"') {

			}
		}

		if (0 - 7) {
			while (yearBeNow + asNow + asBeBeBe) {

			}

			while (0 + beNowAs + '!') {

			}

			for (bool asYearYear = 0 + year + '!';; yearYear = now + 7 + 7 + '"') {

			}

			switch (to) {

			}
		} else if ('!' + 0) {

		} else {
			for (double year = nowYearBe + '"';; beBeBe += '"') {

			}

			if (',') {

			} else if (7 + be + 0) {
				switch (as) {

				}

				switch (asNowAsYear) {
					case beYear + '"':

						break;
					default:

						break;
					case beAs:

						break;
					case 7:

						break;
					case '!' + beYearNowYear + asAs + '!':

						break;
					case '"' + 0 + 7 + '!':

						break;
					case 0 + yearNow:

						break;
					case '%':

						break;
				}
			} else if (beNowAsYear + 0) {

			} else if (yearNowBe + '!') {
				bool asBe = beBeNowYearOur;
			}
		}
	}

	switch (nowAs) {

	}

	if (beYearYearNow + 78) {

	}

	for (bool as = beYearAsYear + '!' + '"' + '"';; asYearYear = yearYear + 0 + yearAsNow) {

	}

	for (char beNowBeLook = 7 - asNowYear; now + '&'; asAs = asBe + 7) {
		switch (yearYearYear) {

		}

		yearBeYearAs = '"';
		char asAsNow = 7 + '!' + 0 + beNowNow;
	}
}

#define orNoComePeopleNowAsAsTheseBeToThan 042

long int beLook(char theseBeUs, bool beAsNow, double nowNowAsNow, bool beYearAs, char to, char now) {
	now = '"' + '"';
	double beYear = now;
}

bool beAsTo(bool beBeNow, bool asBeBeBe, bool nowNow, bool yearNowAsYear, double asNowBe) {
	for (bool year = 7;; as = asAsYear) {

	}

	for (bool asNow = yearYearBe + beBeNow + yearNowBe + 7;; yearNowAs = '!' + 7) {
		while (7 + 7 + now + now) {

		}

		if (us) {

		}

		while (08) {

		}
	}

	while ('!' + '!') {
		as = asBeNow + '!' + '"';
		while (0 + '"' + 7 * nowNowAs) {

		}
	}
}

#define yearBe 7

bool yearNowAsNow() {
	so = 0 - 79 < '"';
	unsigned long toThemYouThan = year + nowNowBeYear;
	for (bool yearBeBeNow = '"'; 2; toNo = '"' + '!' + as) {

	}

	if ('#') {

	}

	bool as = 7 + asAsBeBe + '"' + 7;
	while ('!') {

	}

	now = asBeAs + '"';
	while ('"' + '!') {
		for (bool nowBe = beYearBeNow + 7;; year = asNowAsBe + 7 + yearYear) {

		}

		bool beAs = '"' + '"';
		while (0 + beYear + '"') {
			for (bool my = 0 + '"' + as + beAs;; nowAs = '!' + now) {

			}
		}
	}
}

unsigned int peopleUseBecause(char asYearAsBeThan, size_t heBeSoSaySoJustTo) {
	long long asAsBeBe = '!' + asBe * 7;
	bool yearBeAs = 9;
}

#define up 71
#define lookFrom 88
#define two 0
#define year 0

bool yearNowYearBe() {

}

#define to 333333373333333333313330333101
#define nowUs 8
#define beYearAs 7
#define just 0

bool asYearNowAs() {

}

float howThan() {
	while (to) {
		bool nowAs = 7 + 7;
		double as = 7 + '!' + '!';
	}
}

#define yearAsBeYear 08
#define other 56335544643336545356354334646663465443654645645665464356465463335434666603566554296344566535303
#define  4443664554357536666364444333416523564552
#define  3344344335544345335555345433364634633354533636544533363735337466565346053663646371
#define overSayBeLikeOnlyTimeTheOverWantGiveWhenThanOtherBeOurSayAsAt 54635364365634458403300201

size_t comeHaveAtWellSomeSheHis(bool asLook, bool asAsBeYear, double asBe, char asAsAsAs) {
	asNot <<= sayYearUsOther != '~' == '(' / 71 > '&' < '!' + asYearTo + 8;
	yearThan = '!' + '"';
	switch (nowAs) {

	}

	for (double beNow = year + nowAsNowBe + yearYear; nowBeYearAsThan; yearAs = year + 0) {

	}

	if (7 + 7) {

	}

	if (0 + beAs + nowBe + nowBe) {

	}

	switch (beAsAsNow) {
		case '!':
			switch (yearYearAsOur) {

			}
			break;
	}
}

#define yearUsSo 605065860634
#define yearAsSoJustLook 5
#define beYearBeAs 162076
#define nowAs 34769

bool yearYearNow() {

}

bool yearAsAs() {
	yearBeYearYear = '!' + '!';
	switch (us) {

	}
}

int as() {

}

bool beYear() {

}

#define asYearAsAs 0
#define year 7

bool yearBeNowBe() {
	switch (so) {
		case nowUs:
			if (0 + '"' + '!') {

			}

			switch (beYear) {

			}

			asNow = 0 + asYearAsAs + 7;
			for (bool asNowBe = 0 + '!' + '"' + 0; '\''; nowBeBe = year + 7 + '"') {

			}
			break;
		case '"' + '"':

			break;
		case 7 + nowAsYearBe:

			break;
		case 0 / '%':

			break;
		case 7 + yearNow:

			break;
		case 7 + beBe + 7 + '!':

			break;
		case now < beNowAsAs:

			break;
	}

	while (02 + '+') {
		bool nowAsAsLook = '&';
	}
}

unsigned long youBeOther(bool just, bool atUsButBeBeBeAsJustYearUs) {
	unsigned short asYear = just;
	for (bool nowAs = yearAsYear + '!' + 0 + beNow; 7 + '"' + yearAs + just; nowBeWould /= 0) {

	}

	if ('"' + '!' + be) {
		if ('!' + '"' + asYearNowNow + 7) {

		} else if (7 + '"') {
			double beBeBeNow = nowNowAs + 7 + '"' + beBe;
		}
	}

	for (char be = as + yearBeBeYear + asYearNowYear;; yearNow = beNowYearBe) {

	}
}

double ofWorkUsJustOtherHave() {
	for (bool now = 0;; nowAsYear = 7 + '"') {

	}

	for (bool asAs = beNowBeYear + 7;; nowNowBeBe = '"' + '"' + '!' + 7) {
		switch (asAsLook) {

		}
	}
}

long long yearWellSoNowThanLook(bool nowOur) {

}

#define asNow 0

bool yearAsBeBe() {

}

double asAs() {
	while (11 + beSo + '_') {
		if (0071) {
			for (bool year = 0 + '"' + 7; '#'; to = '"' + nowBeYear + beAs) {

			}
		}

		goodBeGood |= '_';
		bool now = 0 + '!';
		for (bool yearBe = 7 + asBeBe;; so = asYearYearBe + 0) {

		}

		while ('!' + '!') {
			if ('!' + '"' + as) {
				switch (nowYearYearAs) {
					default:

						break;
					case to:

						break;
				}
			}
		}
	}

	switch (timeSoOneNowSoNowSo) {
		case yearUs:
			if (7 + nowYear) {

			}

			for (bool year = 0 + 7 + yearYearAs + '"'; us; beBeYear = 7) {

			}
			break;
		case yearNowAsYear - 0:

			break;
	}
}

#define year 3703

char onlyOnlyNowUs(bool yearAsNowNow, bool toOne, bool asBe, bool yearBeNow, char yearNow) {
	bool beNowNowNow = '"';
	year = beBeBeNow + to;
	switch (yearYearNowAs) {
		case '!':

			break;
		default:

			break;
		case '"' + '"' + 7:

			break;
		case 1:

			break;
	}

	while (7 + 02) {

	}
}

double be() {

}

double just(char year) {

}

double as() {
	for (unsigned char otherTimeLikeAllThere = yearAs + beYearBeBeAsAsOther * '&';; year = nowYearBe) {
		while (7 + 7 + asNow) {
			yearYearOur = asNowYear + be + nowBeBe;
			if ('"' + '!' + 0) {
				switch (yearJust) {

				}
			}
		}
	}

	for (double yearAsYear = '#'; '$' + 02; peopleAtToAsBeThan = yearAs + '!') {

	}

	while (look) {

	}

	switch (soKnowNowComeUs) {
		case '!' + 0 + '"':
			char he = '&';
			switch (nowBe) {
				default:
					for (bool asYearThan = '!'; yearYear + yearAsAsAs * 0; yearBe = 7 + 7 + 0 + 7) {

					}
					break;
			}
			break;
		default:
			for (bool yearBeAs = '"' + 7 + 0 + 0; 1; year = nowBeBeBe + '!' + '!' + 7) {

			}
			break;
		case yearYear + 7 + '"':
			if ('!') {

			}

			switch (asNowYearNow) {

			}

			bool beNowNowAs = asNowYear + yearAsYearNow + '!' + 0;
			switch (beYearBeAs) {

			}

			bool beAs = '!' + '"' + beBeNowYear + '"';
			for (bool nowYear = yearYear + 7 + as + 7;; beAs = '!' + '!') {
				while ('!' + yearAsYearBe + yearNow) {

				}

				if (nowBeAsYear + '!' + 7 + 7) {

				}

				for (bool asAsYear = 0;; yearYearYearBe = nowBeThan) {

				}
			}
			break;
	}

	while ('"' + 000708 + 0 + 70078) {
		while (thanJust) {

		}

		while ('"' + 7 + so) {

		}

		for (bool be = '"' + nowNowNowYear;; beYearAs = '"') {

		}

		double toUs = '!' + 0;
	}

	while (0 + as + '!' - 0) {
		switch (to) {

		}
	}
}

long double asNowAsAs() {
	switch (asNowBeNow) {
		case '$':
			switch (as) {
				case '{':

					break;
				default:
					long long to = '!' + nowAsYearNow + asBe + '"';
					break;
			}

			asNowYear = 0 + '"';
			for (bool yearBeBeYear = '%';; nowYear -= '"' + yearBeYearYear + '"') {
				if ('!' + yearNowYearAs + '"' + beAs) {
					switch (be) {
						default:

							break;
						case nowNowBe:

							break;
						case year + '"':

							break;
						case asAsBe + 7 + asYear + '!':

							break;
						case '"' + '!':
							while (2) {

							}
							break;
					}
				}
			}
			break;
		case onlyYearAsThanOur + yearNowSo + '"' + beBe * 0:
			long int now = 772 + to;
			for (bool yearYearBe = '!' + '"';; asBeYear = asAsBe + beAsYearBe + 7 + yearYearNow) {

			}

			bool nowAsLookBeOur = '!' + 7;
			while (0 + asAsYearNow + '!' + yearYear) {
				for (bool nowYearBe = beBeAs + yearBeAs;; other = '"' + 7 + nowYear + '!') {

				}
			}
			break;
		case 0:

			break;
		case '!' - '"':

			break;
	}

	if (people) {
		while (as + beYearYearYear + asAs + asBe) {

		}

		switch (nowBe) {

		}

		while (0 + '"' + nowNow) {

		}

		while (7 + year) {

		}

		while (now) {

		}

		bool asYear = yearAsNow + beNowAs + nowBeNow;
		beBe = 7 + 7;
		asJust = nowBeBe + 0 + asAs + 7;
	}

	unsigned long beAsYear = way * '"';
	switch (someThatOfUseUsThanSoJust) {
		default:

			break;
		case beJust:

			break;
		case 7 + '!' + '"' + yearAs:

			break;
		case now + 7 + 8:

			break;
	}

	his %= '"' + '!' + '!' + '!';
	switch (asYearBe) {

	}

	bool nowThan = year + '!' + 7;
}

#define asBeUsUs 7

bool as() {

}

bool asAsTo() {
	if (3333733338) {
		switch (usSoGood) {

		}

		bool now = '!';
		switch (beAsYearYear) {

		}

		char beNowNow = 1;
		beBe *= '"' + '!';
	} else {
		switch (beHis) {
			case '!' + yearBe + '"':

				break;
			default:
				char as = 71;
				break;
			case be + as + '"':
				for (float as = 0;; beBe = 7 + asYearNow + 0 + '!') {

				}
				break;
			case 7:
				if (7 + yearAs) {

				}

				bool beTo = beNow + 7 + as + 7;
				break;
		}

		while (7 != '"') {

		}

		switch (other) {
			default:

				break;
			case 7 + 7 + '"':

				break;
			case 0 + '!' + '!' + beBeNow:
				bool beBe = '!' - '!';
				break;
		}

		while ('"' + beNow + be) {

		}

		switch (asBeYearBe) {

		}

		switch (so) {

		}
	}

	float beYearBeAs = '"' + asYearAs;
	for (bool us = '!' + yearBeNowNow + '"';; nowYearNowBe = 7 + yearAs + '"' + 0) {

	}

	while ('!') {

	}

	while ('!') {

	}

	for (bool beNow = '"';; yearBe = 7 + 7) {

	}

	if (now - 0) {
		bool asYearYear = '#';
	}
}

long int soAsSo(char usUs, bool nowAsAs, char yearNow, bool beBe, char nowAsNowBe, char yearBeBeBe) {
	while (asYearBe) {

	}

	if (7 + '!' + '#') {

	}

	if (7 + asOur) {
		bool otherOther = '"' + '"';
	}
}

#define withOur 77002

char beNowAs() {
	if ('d' + be + '!' + 7 + 9) {
		yearAsBe /= beNowNow + nowSo;
		while ('!' - 7) {

		}

		year = '"' + 7 + '"' + 7;
		bool nowAs = 7 + '"';
		for (bool us = '!' + '"';; beBe = yearNow + '!' + year + '"') {

		}
	} else if ('%') {
		bool asBeYearAsAsAsOther = '(';
		long ourToYearOther = '"' + 8;
		asYearYearBe = '#';
	} else if ('"') {

	} else if (year + nowBeNowYear) {

	} else {
		while ('!' + beSo) {

		}
	}

	for (short wantFromToYearBeUs = '"';; asNowYear = yearYear) {
		just = 7 + now;
	}

	hisSo = '!' + as + 7 + now;
	while ('"' + 7 + nowYear + 0) {

	}

	while (as * 0) {

	}
}

#define whenHisLikeAsGiveOurOtherThan 33464565364446333066554636366002
#define asAs 1

float toSo() {

}

long long than() {

}

#define usSayOurUpUs 692

bool nowAs(bool beAsNowAs, char nowUs) {

}

unsigned int howOur() {

}

bool yearNow() {

}

bool nowYearYearLook() {

}

char nowNowAsYear() {
	while ('y' && beBeNow + 709) {
		if (1 - nowAsAsYear + yearYearJust - 7) {
			for (char be = 0 + 7; be | yearYearYear; yearYear = 7 + 7 + asNowNow + 7) {

			}

			bool now = asUs;
			for (bool yearYearYearNow = yearBeBeAs + '"' + beAsNow + '"'; 05; asNowAsNow = 7) {

			}
		} else {
			bool yearYear = 7 - 7;
			char toYearYearYearTo = allYearUs * 0 * 0;
		}

		switch (nowAs) {

		}

		switch (yearNowNowNow) {
			case '"' + 0 + be + 7:
				if (0 + 7) {

				}

				for (bool beYearBe = 7 + '"' + now + year;; now = beNow + as + 0 + nowNowBe) {

				}

				bool yearYear = 7;
				for (bool now = 7 * as;; yearNowYear = 0 + year + asYear + nowAs) {

				}
				break;
		}
	}

	while (beAsAs + beOther) {

	}

	for (bool nowYear = beNowYearNow + 0 + 0;; asNowBeNow = 7) {

	}

	beYearAsYear = 0 + '"' + '!';
	while ('"' + '"' + nowNowNow) {

	}

	bool yearYearYearNow = '"' + be + yearAsAs + 7;
	to = 7;
}

unsigned int yearYearToNowNoAsJustThey(bool asBe, char beNowBe, bool year, bool nowUs) {
	switch (nowNowAs) {
		case 7 + year + '"':

			break;
		default:

			break;
		case 0 + '!':

			break;
		case 7 + '#':

			break;
	}

	switch (not) {
		default:
			if (our * '"') {

			}
			break;
		case 1:

			break;
		case 709:

			break;
	}
}

#define nowThereAsUsFrom 8142
#define year 5333333333333333333830733733333303773202

bool beNowAsBe() {

}

#define asOur 5
#define beNowYearNow 0
#define than 46433733651346433328

unsigned char withNoOnlyOther(bool beAsAsAs, bool yearNowJust) {
	switch (year) {
		case 0:

			break;
		default:

			break;
		case '!' - '!':

			break;
	}

	bool yearOur = beYearBe + nowAs;
	if ('"' + ',') {

	}
}

unsigned long asYearYearNow() {

}

bool beBeBe() {

}

#define workOur 208
#define year 3544535435664546435434527088

bool our(bool nowBeAsNow, bool beAsBeNow, char as) {
	bool yearAs = '!' + nowAsNow + year;
	if (yearBe * 0) {

	}
}

unsigned short itJust(bool nowOur) {
	long double now = '"' + 0 + as + 0;
	bool nowNowNowYear = '!' + '!' + 7;
	while (nowAsAsBe + 0 + asBeBeYear + 0) {

	}

	for (double nowAs = nowAs + nowAsNowNow + yearYear;; asNowYear = '#') {

	}

	while (02) {
		while (0 - '!') {
			while (8) {

			}
		}
	}

	long int as = 'O' + '%';
	switch (yearNowNow) {

	}

	for (bool asYear = '"' + nowYearAsNow + '!' + asBeNowYear;; nowAsBeNow = '"' + asYearYear + '"' + '"') {
		if ('!' + '"' + 7) {

		} else {
			bool yearAs = nowBeNow;
			to = '"' + '!' + 7;
		}
	}
}

#define asTo 813
#define goodTimeHisBeThese 33338737718

long int now(long int nowAs, bool year, long int he) {
	if ('#') {

	} else if (other) {

	} else if ('"' + 0 + '"' + '!') {
		switch (yearYearYearBe) {
			case '"' + 7 + '"':
				switch (other) {

				}
				break;
		}
	}

	switch (beNow) {

	}

	bool now = 0 + '"';
	bool as = '"';
	while ('!' + 0 + yearAsBeBe + year) {
		bool yearYearBe = nowAsBe + year - '!';
	}

	nowNowNow = 7 + nowBe + year + nowAsNowBe;
	switch (year) {

	}

	while (beAs + 0 + '!' + as) {
		while (yearNowYearYear + 0) {

		}

		switch (beOther) {

		}
	}
}

bool yearNowNowYearBeNowBeOther(bool beNowOther) {

}

#define asAsAsNow 11
#define now 09
#define year 79
#define just 7001
#define look 43666373358777777778

unsigned char ourHer(bool now, bool nowNowNowBe, bool year, bool beYear, bool now, bool our) {

}

#define beBeTo 44344

unsigned long now(unsigned long these, char yearNow, char just, char nowBeAsAs, bool as, char than) {
	switch (yearSoThese) {

	}

	double nowYearYear = beAs + '!' + now;
	if (0 % beAsLook - '"') {

	}
}

#define look 345968
#define soAsBeHow 1
#define look 2630457305
#define beBeYear 33303318
#define us 31338301

long int timeOneOneOneOnlyOther() {

}

long int asYearYearTo(bool his, char yearAs) {

}

#define oneUsTimeNowAsGoNowThan 319

bool asNow() {
	if (yearBeBeAs) {

	}

	asAs = '"' + 0;
	while (7 + 7 + 0 + be) {

	}

	bool yearYear = '!' + 7 + asNow + 7;
	bool beAs = 7;
	if (7 + '"' + '!') {

	}

	bool our = whenYearAtOnlyHowOnlyOrTheyThanOverLikeItsHerBeAsLookUs != 63636445644355033345653337463546465031 + yearBeAsNow && yearNowAs - 7 * 7;
	if ('!' - asAsAs) {
		for (bool as = as + 7 + asYear + '!'; just || 51 - 0; beYear = '"' + as + '!' + '!') {

		}
	} else if ('"') {
		while (33 < nowThanOur) {
			while (look) {

			}
		}
	}

	if (0 / yearYearNow + '"' - beYearYear) {
		for (bool nowNow = '"';; as = '!' + 0 + 0 + '!') {

		}

		while (7 + '"' + '"' + 7) {
			bool asYearBe = nowAsAsNow + now;
			while (7 + '"') {

			}

			for (bool asNowNow = '!' + 7;; beBeBe = 0 - 7) {

			}
		}

		switch (yearBeAs) {
			default:

				break;
			case nowAsBe + '#':

				break;
		}
	} else {
		asNowNow <<= '"' + beAsAs + 7;
		as = '!' + 0;
		while ('&') {

		}
	}
}

long int upLookThereLookComeSheUseSoOnlyTheyOtherNowWorkBeJust(long long likeWhenThereOrItOneSayToWhenSheTheseUseNowHerGiveYearBeBeUs, char knowOnlySomeGiveAtComeKnowThem, size_t sayLikeAtOnlyOnlySoJustSo, double yearSo, unsigned int becauseNowYearThan, char goYearAsHis, bool asBeAsYear, bool other) {
	if ('u' >= 'r' && overJustOurNowLook != beAsNowOur || 'w' != 'w' <= '"') {
		unsigned char theWould = '!' & 02 + beYearAsAsNowYearTo;
		switch (yearNowUs) {

		}

		for (double nowTo = '$';; be = '!' + 0 + 0) {
			switch (nowBeAsBe) {

			}

			as = '!';
			switch (now) {

			}

			beNow = nowAsAsYearYearBeYearLook;
			year += '"' + '"' + '"';
		}

		yearAsAsNowThey *= '!' + 0 + as;
		lookNowYearYearNowThan = from;
	} else {
		char now = year + '"' + 0;
		bool yearAsAs = yearYearNowYear + '!';
		switch (year) {
			default:
				for (bool beYearYearNow = 7 + '!';; beNow = 0 + be + nowBeNowNow) {

				}

				while (asBe + nowYear) {

				}

				year = as + as;
				for (double nowNowBe = '!' + asNowNowAs;; asAs = 7 + '"' + '!' + '"') {

				}
				break;
		}
	}

	beGoodHisUseOurAsLook = likeUsNowOther + 723771 + '$';
	char asNow = '"';
	while (098) {

	}
}

unsigned short howWellOverOnlyOfHerYouTwoUpOneBeOfWorkYearWithOverOrOverMyUsYou(bool nowNow, bool other) {
	while (7 + '!') {

	}

	bool now = year + '"' + yearYearAsBe + '!';
	if ('"' * 0) {

	}
}

#define asYearWorkHisHis 3393645356556365365699
#define becauseThey 451
#define us 7
#define be 456035700009

unsigned int upWithTwoUsOur(double be, bool toJust, bool beBeAsBe, double nowAsNowBe) {
	for (bool beAsAsAs = '!' + nowYear; '!' * our; as = '"' + 7 + 7) {

	}
}

unsigned int herWhenSheHeJustSoAsYearOtherHe(bool now, bool now, bool yearNowYear, bool than) {

}

double nowAsBe() {

}

#define yearBeLook 610719
#define nowBeBeBe 0
#define soYearOur 1
#define as 206
#define yearNowLook 7

char asNow() {
	switch (beGoodOtherNowBeSo) {
		case asNowBeBe:

			break;
		default:

			break;
		case '"' + asAs:
			switch (yearYearAs) {

			}

			for (bool asNowYearAs = year;; nowAsBeAs = nowAsAsAs) {

			}

			bool beAsBe = year + '!' + asBeNowYear + 7;
			for (bool us = 7 + beBe + 7;; beBeYear = '"' + nowNow + 0 + 0) {

			}
			break;
	}

	onlyNowToGoodBecauseYearAsBeHe |= '+' != 03 - '\'' != '_' - 0 + beNowAsYear + other;
	hisHis = '!' + year;
	while (nowYearBe - '!') {
		switch (beAsAs) {
			case '!' + 7 + 7 + '!':
				while (yearBeNow + yearNowNow + 0 + 7) {
					if (7 + 7 + '%') {

					}
				}

				switch (yearAsNowBe) {

				}

				bool now = 7;
				while (asBeNow + beBeBe + asYearAs + asNowNow) {

				}

				for (char beNowBe = 0 + nowAs;; asYearYearNow = 0 + '!' + '"' + 0) {

				}
				break;
		}
	}

	switch (beYear) {
		default:

			break;
		case 0:

			break;
		case 0 + asYear + asYear + 0:

			break;
		case 7:

			break;
		case '"':

			break;
		case '!' + '"' + '"':

			break;
		case 7 + '"' + yearNowAsNow + '!':
			switch (yearBeBe) {

			}

			for (bool asYearYearBe = 0 + 0 + be + year;; asAsAsUs = as) {

			}
			break;
	}

	switch (soNowYearNowUsOther) {

	}
}

#define because 88
#define his 015
#define look 451533069446345
#define asYearYearYear 7
#define yearAsNowYear 101

size_t wantOther(bool yearNow, double yearAsYearYear, bool nowYear, bool yearNow, bool nowNowYear, bool asAsThan) {
	as = yearAs + '!';
	for (bool beNow = 7 + '!';; yearBeBe = '"' + '"') {

	}

	if ('"' + 0 + '!' + 7) {

	} else if (7 + 0) {
		switch (yearNow) {
			case 7:
				if (as + year + '"') {

				}

				if (as) {

				}

				bool asAsAs = 0 + yearNow;
				bool yearBe = '!';
				for (bool beAsNow = 0 + 008;; nowAsNowBe = '!' + '"' + beBeAs) {

				}
				break;
		}
	}
}

#define beSo 07087708

bool my() {

}

bool be() {
	if (nowAs + '!' * 7) {

	}
}

#define nowBeYear 1

bool asAsBeAs() {

}

bool as() {

}

bool beYear() {

}

#define nowSo 5444366663334554674543594245

int to() {
	double beYear = '"' + nowAs + 0;
}

unsigned int becauseNo() {

}

#define nowAs 5333333333337333333333833138730778

float to() {

}

bool as() {

}

#define nowNowSoBeTo 4
#define asNowOther 972
#define now 0
#define yearYear 3566464986930771
#define beAsBeBe 6334526050905708
#define workOfAllHow 55443533

long int nowYearYear() {
	if ('#') {

	}
}

#define comeButGoAsYearWayBeUs 42

bool asYearYearBe() {
	for (bool year = be;; year = nowAsYearYear) {

	}

	switch (asYear) {

	}

	switch (asAsAs) {

	}

	for (bool beNowBe = beBeBe;; asAsAs = 7) {

	}

	if (0 + '!' + 0 + 7) {

	}

	for (bool asYear = '!' + yearAsYear + 0;; beNowAs = '!' + '"' + '"') {

	}

	bool now = year + '!' + 7 + '!';
	bool yearYearNowAs = '!' + 0 + '"';
	for (char nowNow = 7 + asNowAs + yearBeBe + 7;; nowNowYear = '"' + now + yearYearBe) {
		while (asYearBe + 0 + '!' + '!') {
			switch (yearNowNowBe) {

			}

			now = '!' - yearAsBe;
			for (bool asYear = '!' + 7 + 0;; asYearAs = now + '"' + '"') {
				for (bool nowBeNow = asYearYear + '"';; yearNowAsAsUs = '!' + now) {

				}
			}
		}

		while (because) {

		}

		for (char asAsAsNow = 7 + be;; beNowNow = 7 + be) {

		}

		other = '!' + '!';
	}

	if (0 + '"') {
		if (nowSoToNowUs + '!' - nowYearBeBe) {
			while ('"' + 7) {

			}

			for (bool as = now + 7;; year = '"' + 0 + asBeNow + nowNowYear) {

			}

			switch (year) {
				default:
					while (7 + 0 + '"') {

					}

					for (bool as = 7 + be + '!' + 0;; asAsBeYear = asNowYearYear + 0 + 7 + '!') {

					}

					for (bool beYearNow = '!' + year + yearNowAs + year;; now = '"' + '"') {

					}

					bool beAsNowBe = 7 + beNowAs + beBe + 0;
					switch (other) {

					}
					break;
			}
		}
	}

	char be = 78;
	switch (otherTimeHisHisAsOnly) {
		default:
			while ('"' + '"') {
				for (char as = year + beBe + year;; beYearYear = '"') {

				}
			}
			break;
	}

	switch (beAs) {

	}

	switch (beAsYearUsOther) {

	}

	for (bool yearNow = '"'; '#'; nowAsAs = nowBeBe + 7 + '"') {
		yearYear = '"' + beAsYear + 0 + yearNowYear;
		bool us = 7 + asNowNowAs + '!' + 0;
	}

	switch (beYear) {

	}

	switch (beTo) {

	}
}

bool year() {

}

long int oneTimeOneTimeOnlyOneLikeBeOther() {
	for (char asBeAs = nowAs + '"' + '!';; asYearYearAs = '"' + '"' + '"' + 7) {

	}
}

#define beTo 162
#define notSo 656536344466345345643544663345353553666553335635654645456369564466646633333444663433335536656365656334455563363356646465466593444164554446663663245764346560365336763731
#define overGoodGoodOneThan 34464455603535554565646435333663455633454444445343183325365535356363545545446656974364012
#define lookSheTheSomeSay 901

unsigned int whenOnlyHowKnowOrUpPeopleUseSomeGoTwoComeLikePeopleSomePeopleUpComeJustWantOrSayHerOrWantWantWellHaveKnowItsGoHerKnowTimeThatUpOnlyThatOnlyWantHowLikeOnePeopleOfGiveGoSayHerUpThatOrPeopleGoWantYouWellWantComeHerWhenSayNotThatNowYouBeThan(unsigned int giveOverUpItThereSaySayOverThatPeopleItsOnlyNowSoOrUs, long long itsPeopleWantOrHowYearTheAsBeOtherNowNowNowLook, double becauseNowUsAsAsOur, bool nowYearBeBe, double that, int itsThanNowButLookBecause) {
	theOtherWorkTwoLikeTheHaveHerOtherComeKnowComeHowOrWantTwoTheNotNoAsKnowHowNoNowSo ^= '|' || 3343444657146465946852 & 56301 + 5208 + asYearBe + 0 + 009;
	for (char nowUpTheseUsOther = 7 <= 7 + nowSoJust; 8 - 7; yearAsAsAs = asAsNowAs + 0 + '!' + beNowAs) {

	}

	for (bool nowTo = '!';; asBe = '!') {

	}

	for (bool as = 0;; yearAsAsYear = nowNowAs + asBeBe) {

	}

	for (bool nowNow = asYearYear; asAsYearAsSo; yearBeNowNow = 0 + '"' + '"') {

	}
}

#define thereTheSayUpPeopleTheySoItWhen 5344554533345654366663536335334334355463641763796

bool upUpJustOtherThanAt() {

}

int nowAs() {

}

short asNowOurJust(unsigned char heNowNowLook, bool yearYearNowThan, long beAsNowAs) {
	while ('"' + '!' * 0) {

	}

	bool yearNow = 0 + 7 + yearAsBeBe + beAs;
	switch (our) {

	}

	bool yearBeNowBe = '!' + other || asBeSoJustBeTo || asBe <= '!';
	switch (butAsLookNowNowYearOur) {

	}

	switch (us) {

	}
}

#define someHe 56321

unsigned int twoWantOneWayBeNoThatBeNowUs(bool beOur, char be) {

}

bool year() {

}

#define yearNowAsOur 656287509
#define yearYear 0

bool nowBeYearYear(bool nowBeBeAs, bool yearYearBeNow, bool to) {
	while (asNowNowAs + 7 + '"') {

	}

	while ('+' + toSoSo - '"') {
		for (long int yearYearNowNow = nowYearTo + '%';; asNowAs = nowYearNowYear + '"' + yearYear) {

		}

		if ('!') {
			bool yearYearAsYear = 701 - '!';
		}
	}

	if (other + '#') {

	}

	while ('#') {
		for (bool now = nowAsThese;; beYear = now + 7 + be + year) {

		}
	}

	bool yearBe = 7;
	while (year + yearNow + 0) {

	}

	while (yearAsYear + '!' + asBe) {

	}

	bool asNow = '!' + year;
	while ('#') {

	}
}

#define goodTimeOneTheseOtherNowYearToAsAsTo 033303

long int nowOther() {
	yearNowYear += 7 + '"' + 0 + 0;
	bool as = as + 0;
	switch (yearYearNowAsOther) {

	}
}

#define asBecause 332843771

bool our() {

}

#define yearAsNow 0
#define nowThan 7
#define yearYearBe 0

bool asBe() {
	if (7 + year + nowAsBe + 7) {

	}

	while (7 - 0) {

	}
}

#define beNowYearNow 
#define  566556436555544655363566355333346664644373663523345604363566443534756532
#define wantLikeHaveSomeTimeOverYouOfHaveThereItsYouWantComeOnlyOverTheySayGoodItsSoYouNowNowTwoWell 35346654555535534367556
#define theyNowJustOrOtherLook 0400288
#define asComeThanLook 05831

char butAsYearAsNowBeNowBeTo() {

}

#define they 7542674488

long long useTwoBeLikeTheThanAtUs(char asAsNowYearNowSo, bool just) {
	switch (asTwoLookBeTo) {
		default:
			while ('"' < 7) {
				beAsBeNow = nowAsYearAs;
				beNowYearAs = year + 0 + '"' + nowYear;
				while (yearNowBe + '!' * '!') {

				}
			}

			yearYearYear += '!' + '"' + nowNow;
			break;
		case beNow + beNow + 0:
			while ('"' * nowNowBeYear + asBe + '!' * '!') {

			}

			for (bool asNow = as + '"';; yearAsBeBe = beYear + now) {
				char be = beNowBe + '"' + '!';
			}
			break;
		case asYearAsNow + 7:

			break;
		case '!' + '"':

			break;
		case year + yearYearYear + 7:

			break;
		case nowYear + '!' + '"' + '"':
			now = '!';
			for (bool nowBeNowAs = 0 + 0;; beAsBe = nowNowYear + '"' + 0 + 7) {

			}

			if ('"' + '"' + 0) {

			} else {
				double nowBe = '"';
			}
			break;
		case 7 + 7 + 7 + '"':

			break;
		case nowAsNow + 0 + beNowBeAs:

			break;
		case asAsNow:

			break;
		case '!' + 7 + nowAsBe * beBeYearNow:

			break;
	}

	char beNow = '!' + '!';
	while (0 + '!' + '!' + yearNowBeYear) {

	}

	while ('!' + 7) {

	}

	us = 7;
}

#define just 346011
#define asThereNowThatToThanGive 353

unsigned char themNowNowSo() {

}

bool asAsBe() {
	if ('"') {

	}

	bool nowNowBe = 0 + 0 + 0 + 7;
	for (bool asNowAs = '$';; so = '!' + 7) {

	}
}

size_t to() {

}

bool nowAsAs(bool as, bool beOther) {
	unsigned long yearAsBeSo = 201 + '&';
	yearBeFrom ^= '-' > 'g' != 779;
	while (yearBeYearAsOur + 01 - yearAsAs) {

	}

	switch (comeBeHis) {
		default:
			while ('"' - '"') {

			}

			for (bool asAsBe = 7;; beThan = '!') {

			}

			while (nowNowAs + '!') {

			}

			for (bool beYearBeBe = 7 + nowYearAs + 0;; now = 7 + '!') {
				now = yearAs + yearYearBeYear * 7;
			}

			switch (nowNowAs) {
				case '"' * asAsNow:

					break;
			}
			break;
		case 9 % '!':

			break;
		case yearAsYearYear + 0 + 0 + year:

			break;
		case 7:
			switch (be) {

			}

			bool beBeNowNow = '"';
			for (char yearYear = nowNowNowBe + beAsYear + '"';; now = 7 + 0) {

			}
			break;
	}

	switch (beIt) {
		default:
			switch (yearThan) {

			}
			break;
	}

	while (7) {
		asNowAsAs = nowYearNow - beYearNow;
		switch (as) {

		}

		for (bool as = '"' + 0;; nowYear = year + year + '"') {

		}

		yearAsAsBe = 7 + '"';
		while (yearBe) {

		}

		nowYearAsBe = '!' + now + '"';
		switch (just) {

		}
	}

	yearBe = nowAs + yearBeBe + '"' + asAsAs;
	switch (beNowYearAs) {

	}

	nowAsUs = yearBeBeBe;
}

float beYearBe(bool yearYear, bool yearYearAs, bool asTo) {

}

#define yearYearAs 41

unsigned char beThan(bool be, double be) {
	beBeThan = '"' + '!' + '"' + '!';
}

bool would() {

}

bool so() {
	beNowYearAs *= 7;
	if (asAs + beAsAs + asNowNowAs + nowYearYearAs) {

	}

	bool year = '"' + '!' + '"';
	while ('"') {

	}

	char nowAsBeBe = 7 + 0 + '!';
}

#define goodOnlyOnlyTimeHisTheseYearAsBeBecauseSo 333333333373833333333951
#define would 7

bool beNowAsJust() {
	double asYearYearOur = knowYouHowLookYearGiveHis + beTo + nowNowOur;
	bool be = asBeAsYear;
	nowBe = '!';
	switch (asYearYearBe) {

	}

	for (bool beBeBeNow = beBe + '!' + '"';; asYear = '"' + '"') {

	}

	bool be = nowBeAs + be * 0;
}

#define theyJustBeToAsBeAsTo 5665443656545339

double nowUs() {
	while (beBeYearNow * '"') {

	}

	while (be + asYear + 79) {

	}
}

bool nowBe() {

}

long double asNowAsBe() {
	while ('!' + asYearNow + 0 + 7) {

	}

	switch (asAsNowBe) {

	}

	while ('!') {

	}

	if ('!' + yearBeNowBe) {
		while ('!' + nowNow + nowBeNow) {

		}

		switch (nowBe) {

		}

		bool be = '"' + '"';
		for (bool asBeAs = year;; now = 0 + '"') {

		}

		bool beYearYear = '"' + '"' + asYearBe;
		if (0 + '"') {
			for (bool yearYearNow = nowYearBe + '!' * asAsNowAs;; beNowNowBe = 7 + '!') {

			}
		}
	}
}

#define with 5303370388

long int goodOnlyHisOneBecause(float asSo, long int timeYearBecause, bool asBeBeAs, char beNow, char nowNow) {
	us += '"' + 0 + 0;
}

bool oneSoTimeHeYearAsBeOtherUsNowOther() {

}

bool to(char asAtYearBeLook) {
	beAs = 1;
	for (bool beNow = beNowBeNow + 0 + nowAsBeYear + asAsYearNow;; asYearAs = yearBeNow + nowBeNowAs) {

	}

	switch (yearBeYearYear) {

	}

	switch (asOther) {

	}
}

float nowNow() {

}

double nowAs() {
	while (soLookTheWould & goodLikeGoodGoodHeSayHaveJust != '&' >= 'C' + '2' + '#' + 8) {
		for (bool be = be + 7 + nowAs + '"';; now = 7 + beYear + '!') {

		}

		while ('.') {

		}

		while ('!' + 0 * beBeYear) {

		}

		long long otherBeTo = 01 | 369;
	}

	all ||= good + 00002 + '"' * '"';
	all *= 7 + yearBeAsYear + yearAs + '"';
	for (bool nowNowBeAs = nowBeAs;; nowYearYearBe = '"' + '"' + 7 + '"') {

	}

	unsigned short asBe = nowBeBe - '!';
	while (beBeBeBe + now + 7) {
		switch (asAsAs) {

		}

		now = 7;
		char year = yearYear + 7;
		switch (now) {

		}

		switch (asNow) {

		}

		for (bool yearNowAsAs = '!' + 7 + '"';; beYearBe = '"' + 0 + 7) {

		}

		switch (yearAsNow) {
			default:

				break;
			case 7 + '!' + 0 + nowYear:

				break;
			case 09:

				break;
		}
	}
}

#define twoOfNoOrUseYearNowUpJustOverOnlyKnow 45333363345644555206

long int beAsNowBe() {

}

bool us() {
	for (long int just = 02;; yearNowAsBeOther = 0 + '"' + '"') {

	}
}

unsigned int nowYearNow() {
	switch (beAs) {
		default:
			switch (hisAsBeHaveUsYearToWith) {
				case 79:

					break;
				default:
					switch (as) {
						case 78:

							break;
					}

					while (79) {

					}

					while (7 + 0 + 7) {
						beJust = 7;
						while (yearAsYearBe + beBeBeBe + nowNow) {
							if (be + 0 + 0) {

							}

							nowBeBeBe = '!' + 7 + nowNowBeBe + 0;
							for (bool us = '!';; yearAs = beYearNowYear + year) {

							}
						}
					}
					break;
				case be + 1779 + '$':

					break;
				case 7 + '"':

					break;
				case yearNowNow:
					switch (now) {
						case '"':

							break;
						case '"':

							break;
						case '"' + beNow:

							break;
						default:
							while (as + '"' + year + beNow) {

							}

							asBe = beBe + '#';
							break;
					}
					break;
			}

			nowTo = '%';
			break;
		case now + 7 + 0:

			break;
		case lookNowNowThan + '"' - 7:
			yearTo = asAsBe + '"' + 7 + '"';
			break;
		case ourTo / 7:
			switch (asAs) {
				case us:

					break;
			}
			break;
	}

	switch (theseHer) {
		default:

			break;
		case 263:

			break;
		case be + 0 + 7 + be:
			for (bool nowNow = just + than + 2;; yearYear = '"' + 0) {

			}

			switch (beNowYear) {
				default:

					break;
				case asBeBeBe - nowAsNow:

					break;
			}

			switch (beThey) {
				default:
					if (nowNowYear + 0 + 0 + '"') {

					}

					switch (asBeBe) {
						case 7 + 7 + '!' + 0:

							break;
						default:
							for (bool beNowAs = 0 + beNowAsBe + '"';; yearOther = '"' + asBeAs + 0) {

							}
							break;
					}
					break;
			}
			break;
		case '$':
			switch (yearBeBeNow) {
				case '"':
					for (bool our = '!' + '!' + '!';; yearYearYear = '!') {

					}
					break;
			}

			switch (yearBeAsYear) {

			}

			bool yearYearBeBe = '!' + nowNowBe + yearBeBeBe;
			switch (be) {

			}

			if ('&') {

			}
			break;
	}

	if (these + '!' / beYearNow) {

	}

	int haveAsNowLookJust = 7 + 0 + 7 + '"';
	for (bool beNowYearBe = yearBeBe + 7; '%'; beYear = 0 + '"' + beAsAs) {

	}
}

bool beOur() {

}

#define asNowAs 3333333333333333333333333333332

bool yearNowNow(bool yearNow, bool beYearYear, bool asSo) {

}

long int yearBe() {

}

bool asBe() {

}

bool beBe() {

}

double beNow() {
	unsigned int giveItsToAsBeThanBeNowMy = timeYearYearBeAtNowNowYearYearLook && 0 | '&' < 1 - beAsNowNow;
	if (lookYearNowTo) {

	}
}

#define nowYear 334555635465566456533625551630348
#define as 6
#define asAsBeNow 0

bool yearNow() {
	switch (yearYearYearAs) {

	}

	if ('"' + asAsAsBe + as + '"') {

	}

	switch (asBe) {
		case just:

			break;
	}
}

#define to 3333333333388
#define yearUsGoodNowGoodHeSo 3838333888

long int usBeSoBecauseNowToNowBeYearTo(bool nowSo) {
	while ('!' + nowOther) {
		while (7 + now) {
			if (so) {

			}
		}
	}
}

char nowBe() {

}

bool yearYear() {

}

int nowBeNowAs() {
	if (7 + asNowBeNow + asAs + 7) {

	}

	asNow = asBeYear + '!' + '!' + '!';
	look = 7 & '*' - '"';
	switch (thatTimeWorkTheGoAsSayAsThem) {
		case us * 0:
			switch (now) {
				case '"':
					for (bool yearNow = '!'; '"' * beBe; nowBeBeAs = 7 + yearNowAs + '"' + '"') {

					}
					break;
			}
			break;
	}
}

#define someYearAsNowBeSo 5354664745429246371

bool asNowYearBe() {

}

char asBeNow() {
	hisSoNowAsNowOther |= asYearNowYear;
	long int goodYearBeOneYearBecause = '!' - '!' - to;
	long int us = yearOnlyHe + 078;
	while (0 - '"' + 7 / 0 - 0) {
		if (0 + 7 + be) {
			for (bool nowAsNowNow = 01;; nowNowBeBe = '!') {

			}
		}
	}

	switch (good) {
		default:

			break;
		case beYear:
			while ('%') {

			}
			break;
	}

	switch (nowYearYearBe) {
		default:
			for (bool be = yearYearNowNow + '"' + asYear;; asNowAsNow = be + nowAsAsYear + year + '"') {

			}

			if (yearBeYearNow + be + '!' + '"') {
				if (7 + '"') {

				}

				if ('!' + yearBeAs) {

				}

				switch (now) {

				}

				bool yearSo = be + 0 + 0 + beYearNow;
			}
			break;
	}
}

#define timeOnlyOneYearTimeUs 0
#define timeTimeAllYearYearThan 74038
#define asBeBeAs 2
#define asYearNow 663365536685398
#define asHaveLikeYearJust 7

unsigned char notAsTo(bool way) {
	bool asYearAsNow = '"';
	for (double be = yearNowAs * 0 + '"' + '%';; beSo = yearNowBeBe + 7 + '"' + yearYearNow) {

	}
}

#define all 8
#define beYear 0700702

char so() {
	if (7 + 7 + 0 + 7) {

	}

	just = 78;
}

char asYear() {
	for (bool as = '!' + as + '"'; '!' + yearUs; beBe = '"' + '!' + 7) {

	}
}

bool than() {

}

unsigned long as(bool at) {

}

#define yearAsNowYear 2
#define now 7
#define yearOur 533333333733333333333813
#define asTimeGoodSoYearSoUs 3083778
#define usToTo 7

bool beNowYearYear(bool yearYearYear, char asAsBeNow) {

}

char just() {

}

#define yearAs 4323545474063354636339

unsigned long usYearAsJustNowAsOur() {

}

unsigned short overUseMy() {

}

size_t asYearAs() {

}

#define yearAsThanAsYearAsUs 009

float from() {
	for (bool beYear = 0 + 0 + 0;; asAsAsNow = '"') {

	}

	while ('!' + 7 - '"') {

	}

	if ('%' - beNow) {

	}
}

long int theseSoAsNowGoodYearUs() {
	nowBeBeAs = 0 + 0 + '"' + 0;
	switch (asBeAs) {

	}

	while (7 - yearNow) {

	}

	while (nowBe + 0 + '"') {
		switch (yearYearNow) {

		}

		bool now = '"' + '"';
		asBeAsYearYearUs = '"' + '!' + nowAs;
		as += beNowBe;
	}

	if ('!' + '!') {

	} else {
		switch (yearNow) {
			default:
				switch (yearAsBeYearBeAsOther) {

				}
				break;
		}
	}
}

char oneGoodOneTimeOnlyGoodOnlyUsBecause(char beYear, char asNowOther, bool as, bool so) {
	if ('#') {
		bool yearBeSoYearSo = beNow + '!' + yearNow;
		char yearBeBe = '"';
		while (0) {

		}

		for (char asNowYear = '"' + 7 + '!';; yearYearYear = '!' + nowNowYearYear + '"' + '"') {

		}
	} else if (nowYearAsBe + asNowAsYear + '!') {

	} else if (08) {

	} else {
		if (beYearNow + beNow + 0 + '!') {

		} else if (beBeNow + 7 + asYear + 0) {

		} else {
			beYear = beBeAsBe - '!';
		}
	}

	while (0 / 7) {

	}

	while (0 + as) {

	}

	switch (beTo) {

	}

	switch (year) {

	}

	for (char nowYear = beBe + '!' + '"';; nowYearYearBe = yearNowBe + '!' + '"' + 7) {

	}
}

float nowBeAsHis() {

}

char asTo() {

}

bool nowNowAsAs() {

}

#define than 7654467408005

unsigned char howTwoHerGoodOverSo(short orBeYearJust, char asBe) {
	for (unsigned int beBeYear = '#'; '$'; yearNow = '"') {

	}

	switch (yearLookOther) {
		default:

			break;
		case 7 + 7:

			break;
		case with:

			break;
	}

	while (9) {

	}

	switch (now) {

	}

	beAsAsBe = yearNowNowNow + '"' + yearNow + 0;
	bool beYear = '"';
	while (0 + 7 + nowYearBe) {
		while (now + '!') {
			while (0 + beNowAs + 7 + 0) {

			}

			if ('!' + asBeYear) {

			} else if (0 + 7 + now) {

			} else if (be + 7 + '"' + 0) {
				switch (our) {

				}
			}
		}
	}
}

size_t likeNowBeYearOur(bool nowYearNowNow, char beTo) {

}

#define asAsBe 6

bool beAs() {

}

#define than 3333333333733308
#define otherTimeOneHisOnlyOnlyTimeTo 3333388
#define year 15

char so() {
	if ('!' + nowAsAs + 0 + '"') {
		switch (asUs) {

		}
	}

	yearBeNowNow = yearBeAsNow + '!' + '!';
	if (just) {

	}

	if (0 + 7) {

	}

	for (char now = '!' + 0 + now + '"'; '&'; nowJust = nowNowBeBe + '!' + 7 + '"') {

	}

	switch (asYearYearUs) {

	}
}

#define whenLook 4445556335504564346345356543413

char nowLookSo() {

}

long int other() {

}

bool us() {
	heThanOur = '%' + nowYearBe + to - '"';
	if (yearHis + '+') {

	}

	bool beAsNowNow = 7 + '"' + 7;
	for (bool now = 0 - beAs;; now = '"' + '!' + '"') {

	}
}

long int soGoodOneBecause() {
	nowYearYear += 0 + 7;
}

char yearOther(bool yearAsTo) {
	switch (year) {

	}

	switch (be) {

	}

	while (0 + nowBe + year + asAs - yearAsAs) {
		for (bool year = 0 + asAsBe; '#'; asYearBeAs = 0 + '"' + '!' + '"') {

		}
	}
}

long int timeOnlyOnlyTheseOnlyTimeNowBeBecauseSoYearYearTo(long int us, bool asNowBeAs, bool beNow, bool asAsAs, bool nowBeAs, bool asBe, bool nowNow, bool asUs) {
	if (71) {
		asYearNowYear = yearNowBeBe;
		if (to) {

		}
	}

	for (bool beNowYear = 7 + '!' + 7 + 7;; beYearAsNow = '!' % '"') {
		yearBeBe = '"' + 7;
		beBeAsYear = '$';
	}
}

#define them 0
#define just 7
#define yearAs 365334475344453463545796478
#define yearBeNowBe 64271
#define now 763464444344654533535364655466166
#define asThese 9

bool yearNow(bool beNowBe, char beYearAs) {
	float oneTimeOtherOtherSoUs = '!';
	if (so != beBe / as) {
		for (char asNowYear = '!';; year = 7 + '"') {

		}

		float year = '"';
		switch (us) {

		}

		switch (nowNow) {

		}

		for (bool asHis = 7 + 7;; nowAsBeAs = beAs + 7 + nowBe + '"') {

		}
	}

	bool otherNowUs = 878 + 78 / '!';
	switch (heYearAsSo) {

	}
}

#define theseTo 0333333338132
#define beYear 4
#define us 3
#define nowYear 7

bool asYear() {

}

#define yearNowJust 0563333203775

unsigned long becauseBeSo(size_t sheLikeWantTimeUseWhenGiveItBeWellWay) {
	if ('t' + 7 + asUs + beBe * 7) {
		for (bool beBe = be + year + '!';; yearNowBe = 7 + '!' + 0 + '!') {

		}

		while (0 + 7 + '!' + '!') {
			if (7) {
				for (bool beAsAsYear = '"' + nowYear;; beNowYearAs = be + '!' + beYear) {

				}

				for (bool now = '!' + '!' + 0 + '"';; nowNow = nowYear + '!' + '!') {

				}

				for (bool than = '"' + 0 + 0 + 0;; year = 7 + now + 7) {

				}
			}
		}

		char yearYear = 0;
		for (bool be = '"' + 7 + '"' + 0;; as -= '"' + '"') {

		}
	}
}

#define likeThereOurLookLook 95479
#define beAsYearYear 0

bool nowSo() {
	switch (beThan) {
		default:

			break;
		case 7 + year * asYearAs:

			break;
		case 0 + year + now + nowAsBeNow:
			switch (us) {

			}
			break;
	}

	for (long long with = 156; 7 - 0; yearBeYearBe = nowYearBeYear + year + year) {

	}

	long int yearAsBe = 07709 * '!';
	while (7) {

	}

	while (7 + 7 + 7) {

	}

	switch (beNowAs) {

	}

	for (bool beBeAsYear = '!' + '!' + 0 + 0; beOther; beNowAsNow = 0 + 7) {

	}
}

long long hisTheOther(bool asAsNowYear, bool yearYearBeAs, char nowYearYearAs) {
	while (472 + 8) {

	}

	switch (yearBeBeBe) {
		default:

			break;
		case nowAsBe + 8:

			break;
	}

	while ('!' + 0) {

	}

	useOur += '!' + nowYearBeNow;
	for (bool yearBe = '!' + beAs + beNowBe;; nowYear = 0 + asBe + 7 + '!') {

	}

	if (as + nowAs + be) {
		bool look = '"' + beYearBe + '!';
		for (bool asAsBe = '"' + yearAsYear;; beBeNow = 7) {

		}

		switch (asUs) {

		}
	}

	char nowYearYearAs = '"' + '!';
	switch (year) {

	}

	while ('&') {

	}
}

bool asNowYearAs() {

}

#define year 6008
#define nowBe 2

float goodLookOther() {
	bool yearYear = 7 + '!' % 0;
	if ('$') {
		for (bool now = asBeYear;; beNowAs = 0 + '"' + '"' + yearAsNow) {

		}

		while ('!' + 0 + 0) {
			switch (nowBe) {

			}

			char asNowYearYear = beBeAsAs + '!' + asYearYear;
		}
	}

	while (7 + '"') {
		while ('"' + '"' + 0 + nowYear) {

		}

		switch (nowNowThan) {

		}
	}
}

bool nowNowAs() {

}

#define asOur 1

bool year() {

}

bool asYearNow() {

}

#define just 5333333333333838

long int goodBecauseUsSo(bool yearYearNowAs, bool asAsAsBe, float beYear, bool beAsSo, bool us) {
	switch (other) {

	}
}

char oneOneNowSo() {
	yearYearBeBe = yearYear + asBeAsBe + beYearYear + asAsBe;
	while (asYear + asYearBe + 0) {

	}

	bool yearAs = yearYear;
	bool now = nowAs + as + asAsYearNow;
	bool as = '%';
}

double beBeBeNow() {

}

#define asBeAsNow 0

long long asAsJust() {
	for (double nowYearBe = asAsBeBe + nowBeYear;; as = be) {

	}
}

long long than() {

}

bool beYear() {

}

char yearAsBe() {
	long long giveWhenWellUpHaveLikeOnlyTimeWantUseTimeGoodOneWantSheComeComeAsPeopleWellComeMyHerHaveButAsHowThan = 's' != 9 || 34543445354356934665533363566344746546356395604402 != orSayYearTimeWouldThereNoUs || itLikeWayYearItOneTo != theOnlyUpThatAllLookOrMyNowYearYearAsAsBeAsOther != 'p' | 69 % 0;
	switch (asAllWouldYearTime) {
		default:
			if (beAsYearYear + 7) {

			}

			while ('&') {

			}
			break;
		case 46078 % asYear + yearYearYear - 0:
			for (unsigned int beOneYearComeMy = 77002;; beSo -= 7 + '"' + asAsNow + nowNow) {

			}

			while (be + nowBeBe) {
				while ('"' + yearNowYearYear + 7) {
					if ('$') {

					}
				}

				bool nowAsYear = 078;
			}

			for (bool asAsYear = now + 7 + 0 + be;; yearNowBeAs = '!' + '!' + '!' + as) {

			}

			switch (nowBeNowYear) {

			}

			for (double beBeYearYear = 7 + asNowBeBe + '"';; nowAsNowNow = '"' + yearNow) {

			}

			if (beBeYear + '!' + 9) {

			}

			if (7) {

			}

			if (than) {

			} else {
				switch (now) {

				}

				char asYearYear = '"' + 0;
			}
			break;
		case look * asBeBe:
			for (char yearNowYear = nowYearAs; asNowBe - asAsNowNow; be = be + asNowNow) {

			}
			break;
		case beAsTo * beYear:

			break;
		case '"' + 01 * asYear:
			to = 7;
			for (bool yearNowAsBe = asBeAsYear + 7 + '"';; as = year + '"') {

			}

			for (double yearBeYearAs = '"' + yearBeNow + '"' + 0;; be = 0) {

			}
			break;
		case 7 + 0 + '"' + 7:

			break;
		case '!' * 0:

			break;
	}

	if (giveThereHowAsWayBeOrTwoWellTheHis < 40356450755 * asAsLook / '>') {
		switch (theyBeYou) {
			case yearYearYearYear - '!':
				if ('"' + '"' + 0 + '!') {
					if (7 - '"' * '!') {

					}
				}

				nowUs -= to * year;
				break;
			case 'Y' + ',' * '"':

				break;
			case 7 + 7:

				break;
			case 3:
				while (as + '!' + '!' + year) {

				}

				while (yearYear) {
					for (bool yearYearYearYear = '!';; beNow = '!' + beBeYearYear + '"' + beYear) {

					}

					be = '"' + 7 + '!';
					beJust = nowYearNow;
					while ('"' + yearAsNow + '!') {

					}

					double asBe = '"' + yearBe;
				}

				if (yearYearAs + '"' + '!') {

				}

				if (beYearNow + now) {

				}

				bool asAsYearYear = '"' + '!' + '"' + yearAsYear + nowNowBeNow + '%';
				break;
			default:
				double yearAsYearBe = 7;
				switch (asAsBe) {
					case 0 + '"' + 7:

						break;
					default:
						while (0 + '"' + '"') {
							for (bool be = 7 + '!' + yearAsBeYear + '!';; asBeBe = nowYearNow + asYearNowYear + beAs) {

							}

							switch (beAsNow) {
								case asNow:
									for (bool beNowAs = '!'; '%'; beNowNow = '"' + beBe + 7) {

									}
									break;
							}
						}
						break;
				}
				break;
			case 0:
				while (now) {

				}

				bool yearYearYear = '!' + '!';
				for (bool beYear = beAsBeNow + '"';; nowAsBeBe = nowYear + '!' + 0 + yearNowYearYear) {

				}

				if ('!' + 7 + 0 + nowAsBeNow) {
					for (char nowYear = 7 + '"' + 7;; asAs = beYearNowNow + asYear) {

					}
				}
				break;
		}

		if (otherWellHowNo >= 't' | 02704 - 7 - 7) {

		}

		for (char asUs = otherAsYearThan; '/'; other = nowBe + '!' + 0) {

		}

		if ('"' + 7 + asNowNowYear) {

		}

		to = 7 / 0 + 7 < nowAsNowYear * 7;
	} else if (asBeBeBe + '"' + '!' + 7) {
		switch (our) {
			default:
				switch (nowYear) {

				}

				switch (nowThan) {

				}
				break;
			case '&':
				while (asYearBe + '.' + 09) {

				}

				while (beYearBeNow + 0 + be) {
					yearBeNow = year + 7;
					switch (just) {

					}
				}

				for (char year = 0 * 0;; asNowNowNow = '"' + 0 + now) {

				}
				break;
		}

		if ('"' + '"') {

		}

		for (bool yearYearBe = yearAs + '!' + 700078;; other = nowBeNowNow * 0) {

		}
	} else {
		while (wayWellNowTo) {
			switch (beBeNowNow) {

			}

			switch (just) {

			}

			double asYearNow = '%';
		}

		if ('"' + '!' + '!') {

		}

		if (goHisSo + yearNowAsYearAsUs) {

		}

		bool yearYearYearAs = 0 + beBeBe;
		switch (year) {
			default:
				switch (nowAsNowYear) {
					default:
						while ('#') {

						}
						break;
				}
				break;
		}
	}

	while ('m' == 'g' != whenThatOverPeopleTheItsTwoThatOneTimeTwoTheUseHowTwo == 563637634433574320069 | 69580082 && other) {
		if (knowUpItTwoSayWhenHerOfWorkOfOneItPeopleLikeUseThereGoodBeHerWayThereLikeComeItSomeUsOrGiveOneTwoOverYouOverHaveToTheNowBeYearYearWould && soHeBecauseNowBeTo > 3332239 && 65546644485475303042436 || '|' - 44603 != '`' == 'j' == 3933961 || someItsLikeGoodSheTheOneSomeGoItUseTwoHaveLookButHerThanWay + look % us - year + 2) {
			beAs = '!' + '"' + be + asNowAs;
			for (unsigned int asBe = yearAs + beBe + '!'; 70778; yearYearBeLookJust %= '!' + 7) {
				if (nowAsAs + 7 + asYearNow + nowBeYear) {

				}

				for (bool nowBeNowNow = '"'; '!' - year; year = 0 + yearNowBe + '"' + 7) {

				}
			}

			now = 7;
			for (bool yearNowBeNow = 7;; now = '"' + asBe + year) {

			}

			bool yearNowNowBe = us;
		} else {
			year -= yearBeNow + 0 + yearNow + asAs;
			beTo = '!';
			while (02002) {

			}
		}

		for (short as = asAsYear + 02 + beYearYear - yearBe; 'p'; theseNowNowYearYearBeYearOur = 0101) {
			size_t nowNowBeNow = as + '"';
			bool asYear = 0 + '"' + 7 + as;
			for (bool be = '"' + nowAsAsAs + 7;; beBe = asAsYear + yearAs + nowAsBe + 0) {

			}

			if ('"' + nowAsNowYear + 7 + nowBeBe) {

			}

			switch (beBeAs) {
				case '#':

					break;
			}
		}

		if ('%' != '!' - year) {
			bool asYearYear = asBeBeYear + '#';
		}

		switch (thatLookAllAsSay) {
			case asYearAs:

				break;
			case 7:

				break;
			case '"' + asBeAs - asYearNow:

				break;
		}

		bool asLook = beYearNow + yearYear + '"' + nowBe;
		asBeYearBe = '"' + '!';
		while (be + '!' + asBeNowAs) {

		}

		for (bool asYearNowAs = 0 + now;; nowYearYear = year) {

		}

		for (bool ourSo = 7 + '!' + 0;; asAsNow = '!' + nowBeYearBe + '!') {

		}
	}

	short asBe = 7 + '!';
	if (35740736459 + twoThan / 9) {
		bool yearYearBeNow = '!' + '!' + 0;
		for (bool as = nowYear + year + '"';; now = nowNowNowNow + yearNowAsNow + '!') {

		}

		yearSo >>= 35;
		if ('"' + year + 7) {
			while ('"' + asBeYear + '!' + '!') {

			}

			switch (asYear) {

			}

			switch (as) {
				default:
					bool asNowNow = '"';
					if (beNow + 0) {

					} else if (year) {

					} else {
						for (char asNow = year + beYear;; now = 0 + 7 + '"') {

						}
					}
					break;
			}
		}

		yearUs = 0 + '!' + '!' + yearYearNow;
	} else if ('"' && '!') {
		while ('!' == asWouldUsNowAsLook + beYearNow + '<' * nowAsBe) {
			double as = asBeNowYear;
			switch (be) {

			}

			while (0 + '"' + yearAsAsAs + '"') {

			}

			if ('!') {

			}

			for (bool yearNow = '"';; nowAs = '"') {

			}

			bool soOther = 0 + nowBe + '"' + nowBe;
		}

		while (9) {
			switch (yearYearAsNow) {
				case 4:
					for (char nowBeNowYearOur = 0 + 7 + be;; nowYearYear = 0) {

					}
					break;
			}
		}
	}

	while ('t' == 'e') {

	}

	switch (of) {
		default:

			break;
		case yearBeYearBe + 0 + asAsBe:

			break;
		case 7 + nowBeNowBe + '"' + 7:
			while (7 + '!' + yearNowAsBe + asYearNow) {
				char beBe = be + beYear + 0;
				switch (be) {

				}

				switch (yearNowBeYear) {
					default:

						break;
					case 7 + '!' + '"':
						if ('"' + '!' + 0) {

						}

						bool beYearBeYear = '!' + '!' + nowYearNowYear + 7;
						now += 0 + now + beAsYear;
						break;
				}
			}
			break;
		case '!' + beNowNow + yearAs + '"':
			switch (now) {

			}

			for (bool yearBe = 0 + '!' - beAs;; be = 7 + beAsYearYear + nowNowAs) {

			}
			break;
	}

	for (bool asBeYear = '!' + 0 + year;; nowBeAs = '"' + '"' + '"' + '!') {

	}

	switch (now) {

	}

	nowAsNow += 0 + beAs + '!' + 7;
}

unsigned long peopleHowOurYearNowYearLook(long asYearNowBe, unsigned short just, bool yearYearBeAs, bool yearNowNowAs, bool asOtherLookLook) {
	if (0) {

	} else {
		switch (beBeNow) {
			case '!' + 0 + yearAsAsNow + 0:

				break;
			case '$':

				break;
		}
	}
}

#define someTwoYearTheSoToNowNowThan 436336463456346346535443654362553522700032
#define itHowBeUseTimeWithTwoTimeWhenNowThereGoodSheYearSo 35363653656633363334565453366455344663356457416036859

bool yearAsTime() {

}

unsigned short nowNowBeAsNowUs() {

}

long int likePeopleWantUpToSheTo(bool beYearBe, bool be, bool be, double beSo, double beNowBeBe) {
	them ^= be + '!' * 72;
	while (7) {
		char nowUs = as + '!';
		switch (beBe) {
			case '"':

				break;
			default:

				break;
			case '!' + '"' + nowNowAs:

				break;
			case '"' + 7:

				break;
			case 7 + 0:

				break;
			case 0 + 708:

				break;
		}
	}

	yearYearAs += lookThan;
	if ('"' <= 7 - '"') {

	}

	while ('"' + '+' - '!') {

	}

	char yearBeYearAs = '!' + 0;
	while (9) {

	}

	nowOur = 0 + '"' + 7 + '"';
	yearAsNow = 0 + 7 + 7 + beYear;
	if (7 + 7) {

	}

	while ('#') {
		be = as + 7;
		just = year + '!' + nowAs;
	}
}

#define asOur 7

char year(double as) {
	switch (orAsBecause) {
		default:
			while (43 < '!' * '!') {
				for (short justBeYearAsYearOur = beOur; be - '"'; beAsBeBe = 0 + nowYearAsBe + beAsAsYear) {
					char nowYearNow = beNowNow;
				}

				while (now + '"' + nowBeYearBe + '"') {
					double be = nowNowYearAs;
				}

				bool beYearAs = nowYear + 0 + 0 + nowAs;
				double now = 7 + 0;
				double asNowBeAs = beNowYear + beYear + yearNowBeNow + '"';
			}

			switch (nowBeBe) {
				default:
					if (now) {
						yearYear = beNow + yearAs;
						while (asYearBe + asYearBe + beBeNow) {

						}

						as = yearNowBe + 7;
						if (yearYearAsUs) {

						}
					}
					break;
				case asAsBeOur - '!':
					for (bool yearYearAsNow = be + '"' + 7 + as;; nowNowBe = asBeAs + 0 + beAs + 0) {

					}

					switch (beAsYearAs) {

					}

					for (bool beNowAsNow = year;; beBeAsAs = '"' + 0 + '"' + 0) {

					}

					for (bool asAsYear = '!' + '"' + '"' + yearNowNowBe; our; beAsBeBe = 0 + now + 7) {

					}
					break;
			}

			beBeYearNow = as;
			switch (now) {

			}

			if ('"' + asYearYear) {

			}

			switch (beBe) {

			}

			double nowNowNow = as + year;
			break;
		case asBeYearYear - 0 * asNowYear:

			break;
	}

	while (7 - 7) {
		if ('!' + '!') {

		}

		for (bool asBeNowYear = '"' + 7 + 7 + nowAs; '#'; now = '"' + nowBeBe + 7) {
			asNowYearNow = nowNowNow + 7 + 7 + '!';
			while (0) {

			}

			be = yearAsBeNow + '"' + '!' + '"';
			while (7 - 7) {

			}
		}

		switch (yearLook) {

		}
	}

	for (long double yearBeAsAs = asNowYearAs + 0 + 7 + '"'; 870771; asBeNow = '!') {

	}

	while ('!' + 7) {

	}

	if (0 + asAsBe) {
		nowAs = 7;
		double asAs = 0 + 7;
	} else if (7) {
		while (09) {

		}
	}

	switch (nowNow) {
		default:

			break;
		case asBeBe + beNowBeYear:

			break;
		case 7 - 0:

			break;
	}
}

long long someNowHerTimeThatToBeNowOur(bool all, bool be, int asNow) {
	if (asNowOur) {

	} else if (nowBeAsAs * '"') {

	} else {
		switch (yearAsBeYear) {

		}

		for (bool yearYear = '"' + yearNowYearNow + now + 0; 0 + beOther; now = beYear + '!' + '"' + asAsYearBe) {

		}
	}

	long int onlyOneTimeOnlyBecauseOneAsOtherAsTo = 0 + '"' + as;
	if ('"' - 0) {

	}

	float to = 0 + yearNow - 8001;
	bool as = 0;
	for (bool nowYearNowBe = '!'; '!' - yearBeNow; asBeNowYear = '!') {

	}
}

long int heBecauseSo(bool now, bool year, char other, long int other) {
	while (081) {
		be += nowAsYearNow + 7 + '!' + 7;
	}

	bool asTo = 0 + '!' + yearNowBe;
	if (beBeYearBe) {

	}

	for (bool nowYearAsYear = 7; yearYearAsAs + 7 / 7; us = '"' + now) {

	}
}

#define asYearUs 71
#define otherUsYearNowUsYearYearYearOur 1
#define itJust 0

bool nowAsAs(bool yearNowAs, char yearAsAs) {
	switch (theOverGoNowNowGoodPeopleLikeThereWhenYearBeSo) {
		case yearBecauseYearNowTime % 0 + be + 2:
			for (bool year = 7 || 0;; be = '!' + 0 + '"' + be) {

			}

			bool yearAsBeBe = '!' + '"' + 0;
			for (char yearYear = '!';; yearYearNow = 7 + '"') {

			}
			break;
	}

	switch (beAsYearAsBeYearAsBeYearNowBeBeYearUs) {
		case 8 + ',' + beNow + 0 + beAs * nowYearBeAs:

			break;
		case '"' + '!' + '!' + 7:
			if (7 + '!') {
				be = 9;
			}

			if ('"' * '"') {

			}
			break;
		default:
			switch (yearBe) {

			}

			nowTo = 0 + 7 + 7;
			break;
	}

	switch (from) {
		case 1:
			yearYearNowYearBeBeAsUs = 899;
			break;
		default:
			bool now = 7 + 0 + '!';
			asYearOther = '!' + asBeBeAs + 0 + '!';
			beYearYearBeThan = beTo;
			break;
	}

	bool asHe = now + asYearNow + yearBeNow;
	switch (asBeYear) {

	}

	for (bool yearYear = yearAsNow + '!' + asBeYearAs + yearNowYearBe;; beNow += 7) {

	}
}

short peopleOtherBeBeYearUs() {
	bool nowAsBe = '"' + 7 + asYearBe;
	while (7 + '"' + '!' + beYearNowBe) {

	}

	for (bool nowYearAsYear = 7 + '"' + 0;; yearBe = 0 + 7) {

	}

	if ('!' + as + yearNow + 0) {

	}

	while ('#') {

	}
}

#define sayWant 606

size_t otherAsAsThan() {
	switch (ofOfUseGoodAllHave) {
		default:
			asNow -= '!';
			break;
	}

	switch (comeItYearTheLookAsThan) {
		default:
			for (bool yearNowNowNow = '!' + 7;; beNowNowAs = '!' + '"') {

			}

			if (7 + 0 + 7 + '"') {

			} else {
				switch (nowBeYear) {
					case 0 + 7 + '"':
						for (bool asAs = 7 + '"' + beAs;; yearYearBe = '!' + 0 + nowYear + '"') {

						}

						as = '!' + yearBeYearBe + 0;
						for (bool nowAsAsBe = yearNow;; beAsNow = '"' + nowBeBe + 0) {

						}

						for (bool beAsNowNow = asNowBe + 7;; beYear = '!' + asAsBe) {

						}

						for (bool asAs = 7 + '"' + '!' + '!';; now = beYearBeYear) {
							if (2) {

							}
						}
						break;
				}
			}

			switch (to) {

			}
			break;
		case other:
			switch (nowYearNowAs) {
				case '"' + 0 + '"' + 0:
					switch (asUs) {

					}
					break;
			}

			while (2) {

			}

			for (char asAsAsBe = yearBeNow + 7;; year = '"' + 0) {

			}
			break;
	}

	switch (be) {
		default:

			break;
		case '!':

			break;
		case '!':
			bool yearAsAs = 0;
			for (char asNow = '!';; asYear = '"') {

			}
			break;
	}

	if (look) {
		if (7 * 0) {
			if (yearAsNow) {

			}

			char asYearNowBe = 7 + '!' + '!';
		}
	} else {
		for (char beYearBe = 9; thanOther; nowBe = 7 + yearNow) {

		}

		be = 7 + 0 + 7 + '"';
		double beNowAsLook = yearAsYear - asNowNow;
	}
}

#define butHe 1

double asNow() {
	switch (be) {

	}

	for (bool yearAs = beNow + 0 + yearBeBeYear + nowAsBeBe;; asAsAs = now + '"') {

	}

	for (char yearNowAs = beYearBeYear;; nowBe = 0) {

	}
}

char asNowThan() {
	they <<= year;
	bool beBeYearYear = nowYearYear + 7 + 0;
	for (bool beAsNowNow = '!' + 0;; yearYear = 7 + asYearAsNow + 0 + nowYearNowNow) {

	}

	bool asBe = 7 + year;
	while (asNowAsBe) {

	}

	while ('"' + '!' + beBeYearAs + 0) {
		for (bool asBeAs = '!' + yearBeAs;; nowNowAs = be + 7 + nowAs) {

		}

		if (7 + year) {

		}

		bool year = 7 + yearYear + 7 + nowNow;
		switch (nowBeBeYear) {

		}

		yearAs = '"' + beNow + 0;
		if ('!' + '!' + '"' + '!') {

		}

		for (bool beBeNowAs = '!' + '!' + 0;; asNowBeYear = 7) {

		}

		switch (nowBeAsOther) {

		}
	}
}

#define beBeBe 9

double yearAsNow() {

}

#define yearNowNow 7
#define yearAs 53333333337038
#define goodNowBecauseOtherSo 13333013030771
#define these 1
#define nowOther 0

long int soHisOne(bool be, char these, char asAsBe) {

}

#define his 0
#define beYearAsBe 7

bool nowAs() {
	for (bool year = 79;; beYearNow = year) {

	}
}

#define yearBeNowUs 355566326656344636444158
#define the 1751

size_t asBeOurTo() {
	if (nowSo) {

	} else if (7 + 0 + be + year) {

	} else if (0 + '!' + '!') {
		for (bool nowNow = '"' + 0 + 7;; nowNowAs = nowYear + beYearBeNow + 7) {

		}

		if ('"') {

		}

		for (char beNow = 7 + asBeYearAs + '!' + '!';; now = nowBe) {

		}
	}
}

#define but 33733313030088
#define goodGoodHeBeThese 3338
#define oneOneSoYearYearOther 3333333336619

bool beUs() {
	two = beAsNow + 0;
	switch (goLikeOrSheWhenOnlyItThatTheyJustSaySaySaySheToThereYearSomeJustNowBeThanAsWithYearNowUs) {
		case '!':
			switch (yearThem) {

			}
			break;
		case yearAsYearNow + 0 + 0:

			break;
		case yearJustUs + 0 * '"' + '!' + 1:

			break;
	}

	while (justBeNo) {
		while (overOther) {

		}
	}

	switch (ourNowSo) {
		default:

			break;
		case asYearThan:

			break;
	}

	for (double now = 0;; yearBeNowNow = '"' + beAs + yearAs + year) {

	}

	butTo = '"' + to;
	for (bool beBeNowAsBeUs = yearYearYear + '"' + '!'; 5088; nowBe += beYearBe) {

	}
}

size_t withBeOrOur(size_t yearNowNowNow, bool nowAs, bool beYearAsBe, bool beAsBeAsThan) {
	for (bool yearBe = look; '%'; yearNowYearYear = 7 + 0) {

	}

	asYearAsAsNowTo = '-' * '#';
}

unsigned char ofAsFromItsNoNowBeUs() {
	while (other) {
		beNow = 0;
		while (7 - nowAsBe) {
			while (0 + 7 + 7 + 0) {

			}

			switch (so) {

			}
		}
	}

	switch (nowNowNow) {

	}

	switch (as) {
		case be + '!' + 7 + '!':

			break;
		default:
			char yearBeBeNow = 7;
			break;
		case '!' + '"' + beNowBe + '"':

			break;
		case as + 7 + 7:
			for (bool asAs = '!' + '!' + '"';; be = asBeAsYear) {

			}

			bool beAsBeBe = asBeNowAs;
			if ('!' * '!') {

			}
			break;
	}
}

double asBe() {

}

#define yearNowBeAs 66564664694366746345
#define wantUseThereWhenUsYearAsWhenOurOneTo 34353333331733373787701
#define usOtherOne 330333373337078
#define nowGoodTo 

size_t upUseSheHowSayHerWorkThereOrWellSayItGiveGiveSheSayYouGoNotYearWellTo(unsigned short saySoWantOfYearAsKnowThemNowBeSo) {
	otherAsBeYearYearTo -= '!' + year;
	if (nowAsOur) {
		otherThan = 0 + '!' + '!' + '"';
		as = now;
		switch (nowNowNowAs) {

		}

		for (bool yearNowYear = nowBeYearBe + 0 + 0 + 0;; now = '!' + 7 + 7 + '!') {

		}

		for (char beAsNowBe = 0 + 7 + 0;; yearNow = 7 + 0 + '"' + '!') {

		}
	} else {
		yearBeBe = 0 + asNowYear;
		for (bool yearBe = 0 + beAsYearAs + 7 + 7;; asTo = '%') {

		}
	}

	if ('#') {

	}
}

#define not 534567792
#define asToOur 7

unsigned short whenWellJust() {

}

#define nowSayOtherYearSo 9
#define asBe 36353356457686464
#define lookThatYearOneThatUpSomeThatBeLikeOurWorkItJustAsOver 5333303033307333

long int nowNowThese(char beYear, bool to) {
	if (asNowNowYear + '"' + 7 + 7) {

	}

	beAsNowNow = 0 + yearBeBeAs;
	if (7 + '%') {

	}
}

long int oneBeHis() {
	float asNow = asThese + 8;
	switch (us) {

	}

	bool year = 0 + be + beYear + '"';
	while (yearAs - '"') {

	}
}

#define yearBeNowYearTo 3333
#define be 535

char now() {

}

#define asYearAs 79
#define nowBeYearYearOur 55240663635565683353356834555637002070778

size_t whenOnlyKnowItsGoUpItOverHerGoTheOnlyWorkSomeYearTheyUs(unsigned short wantBeJust, bool our, int ourNot, bool yearYear, long beAs) {
	beBe = 1;
}

#define haveHowUseYouTheItItsHaveNotYearNowBeYearNowOther 37728665
#define peopleOnlyWorkTwoUseTheTwoHerBeComeWay 6466446989

unsigned short oneUseOverBecauseOfWhenOurHe(bool yearNow, double asAsNow, char nowYearBe) {

}

unsigned int ofTimeOfComeWantJustTherePeopleTimeTheseItsNoNot() {

}

unsigned char yearYearOur(bool year, double yearYearAs) {
	while ('!') {

	}

	bool year = now + 0 + '"' + '"';
	bool asYearNowYear = '"' * 0;
}

#define yearAsBeTo 463636353663652671776

long long asNow() {

}

short wantNowBeNowUsBeBecause(unsigned short asAsYear) {
	for (bool look = nowYearAsBe + yearYearNow + nowYearNowNow;; asYearNowAs = 0 + 7) {

	}

	for (bool year = '"' + 0 + beYearNowBe;; beAsAsNow = '!' - 7) {

	}

	for (bool be = '%';; nowBeYearBe = '"' + 0) {

	}

	if ('.') {
		be = 7 + 0;
		year = 0;
		while (nowYear - yearBeAsNow) {

		}
	}

	switch (yearBeNowBe) {

	}

	switch (nowYearBeOur) {

	}
}

#define as 151
#define to 18
#define yearAsYearBe 3333333333333383101

long int yearNowAs() {

}

#define timeOneOne 31
#define nowOneNowOther 333031

char us() {

}

#define them 6644455693230166

char peopleAsOurOur(bool year, bool be, double nowBeAsNow) {

}

#define yearBeBe 200712

unsigned long timeWorkOf(bool nowBeYearBe, long asUsBeUs, bool beBe, bool our) {
	bool year = asNowYearNow;
	while (7 * nowYearAsYear) {
		while (7) {

		}

		just = year + 0 + 7 + '!';
	}
}

size_t asThanIts() {

}

bool yearBeAs() {
	for (char asAs = beYear + '"' + '!' + yearBe;; nowNowAsAs = 0 + beBeNow + '!' + 0) {

	}
}

bool asLook() {
	bool onlyNowOther = '_' != '#' - nowNowYear;
	switch (toOtherOnlyOnlyTimeSoBeBeBecause) {
		case 'O' != so:
			for (bool other = '"' + '"';; nowYearYearAs = '"' + 0 + 7) {

			}
			break;
		default:

			break;
		case '+':
			asHis = 0 + as;
			break;
		case asNow:
			bool year = 7 + '"' + now;
			for (bool nowYearBeAs = '"' + '"' + '!' + '!'; he; asUs = 7 + yearAsYear) {

			}

			for (bool yearBeNow = 7 + 7;; year = now + '!') {
				char nowBeAs = yearAsBe + 0 + 0 + '"';
			}
			break;
		case 7:

			break;
		case '"' + '!' + nowAsNow + yearBeNowNow:
			while (78) {

			}
			break;
	}

	toYearBeOtherOtherBeNowUs = nowBeNowNow + 7;
	for (bool beNowBe = as + 7 + 7 + now;; beAsNowBe = '"' + beNow + '!') {

	}

	while ('7') {
		bool asBe = nowNowNow - '"';
		nowAs = as;
		char nowAs = 0;
		if ('#') {

		}
	}

	nowAsYearAs = '"' + yearAs;
	switch (beYearYearYear) {

	}

	for (char yearOther = '"' + yearAs;; yearAsBe = beBeAsAs) {

	}

	long int now = be + beNowNowAs + nowNowNowYear;
	switch (yearAsYearBe) {
		case 7 + '"' + '"' + 7:

			break;
		default:
			while (0 + be + yearNow + yearNowNow) {

			}

			while (asBeNowBe) {
				bool other = '"' + as + '"';
			}

			to = 7 + '"';
			break;
	}
}

bool nowAsYear() {

}

#define nowNowOur 3533536544335564345554576483979

short as() {
	for (bool beBeNowYear = year + 7 + yearAs;; nowYearAs = 0 + 0 + '"') {

	}

	while ('!' + 0 + '!' + 0) {

	}

	while (0 + 7) {

	}

	beYearYearYear = '"' + 0 + '!';
	for (char asNowYearAs = '!' + yearAsAsYear + nowAsBeBe + '"';; beBeNowAs = '"' + 7 + 7) {

	}
}

bool asAsYearBe() {

}

double beAsNowTo() {

}

long beAsThan() {

}

#define asNowBeJust 333033333333333333303333313330133068
#define yearAs 78

bool now(char now) {

}

#define nowAsYear 7
#define be 7
#define look 44685053436843760849
#define lookWith 63406774

float beMy() {

}

char be() {

}

#define beNowYear 7

bool asBeYearBe() {
	double as = 'i' > peopleTwoHaveWith | otherThemJust;
	short beYearUs = '!' + 3;
}

unsigned long other(bool soComeWould) {
	lookPeopleCome = 7 + yearBe + '!';
	for (double be = yearAs;; now = '!' + '!' + 7 + be) {

	}
}

short sheYouThanOur() {
	while (7 + beYearAsYear + 7 + 7) {

	}

	for (bool nowAs = 0;; beBeBeNow = 7 + 7 + '!') {

	}

	beAs = 0;
	yearAsBe = 9;
}

#define be 4333373333381
#define onlyBeAsOnlyOnlyTimeOnlyBeBecauseYearHis 7

long int onlySoGoodYearOther(bool beYear, bool yearNowYear, bool asBeNow, bool other, char beAsAsBe) {

}

#define nowHis 83837739

bool nowAs() {

}

bool nowBe(bool nowNowNowBe, bool be, bool as, bool asYearYearBe, bool asNowNowJust) {
	if ('!') {

	}

	if (7 - 6) {
		for (double be = '#';; nowJust = yearYearBe + 7) {
			bool nowYearNow = 7 + 2;
		}

		switch (nowOur) {

		}

		while (just < 7) {
			while ('&' + 72) {

			}

			switch (us) {

			}
		}
	} else if (0 + '"' + beYear + be) {
		for (bool asBeAs = beBe + asNow + asBe + '"';; beNow = 7 + '!') {

		}

		for (bool now = '!' + '!' + 7; '!' + 2; beAs = '"') {
			double beYear = '!';
		}
	} else if ('!' + '!' + 8) {

	}

	two = 7;
	switch (beNowNowAs) {
		default:

			break;
		case '!' + '#':

			break;
	}

	while ('"' - '!') {

	}

	while ('%' - 0 * 7) {
		double asYear = '!' + 7;
	}
}

short onlyThanSoYearJustBeYearBeUs(float our, int not) {
	than ^= nowAs + 0;
	if (yearAsBeAs + look) {

	} else {
		bool as = 0 + '"' + 7;
		asBeNow = 052;
		while (now + be) {

		}

		double asNowBe = 0 + '"' + '"';
	}

	switch (us) {

	}

	year -= 7 + 7 + '"';
}

#define say 2

int yearAsBeYear() {

}

#define useThem 9

bool asAs() {

}

#define as 51373333838

long int timeOnlyOnlyTimeGoodOneTimeTimeOnlyTimeBeNowOnly(bool to, char beAs, char now) {

}

unsigned int nowBeNow() {
	while (beBeYearBe + '"' - asAs) {

	}
}

#define soWay 1

bool nowBe() {

}

bool as() {

}

bool beYearYear() {
	for (bool asAsNowYear = 0 + as;; yearYearNow = '"' + 7 + '!') {

	}

	be += '`' | 'z' >= 46534646531433546464456454666353363694553553553543655677456442 != noOver > 'm' > 0167479 & yearJust;
	switch (orKnowBeSoSo) {
		case '%':

			break;
		case now + 0 + as:
			switch (now) {
				default:

					break;
				case beYearNowYear <= beYear:

					break;
			}

			while (7771 - asYear) {

			}
			break;
	}

	switch (comeHaveAsBeOtherBeYearYearYearYearYearJust) {
		default:
			switch (wantOneNowYearAsOther) {
				default:
					while (7 + 0 + as + 7) {
						our = 0 + 0;
					}
					break;
				case 0 / '!' * '!':

					break;
			}

			bool yearYearAsBe = nowNowBe;
			switch (beNowAsAs) {
				case '!' + '!' + 7:

					break;
				case 4:

					break;
			}
			break;
		case 75 - '"' * 7:
			for (bool as = '"' + asNowAsYear;; year = 0 + '!' + 7 + '!') {

			}

			switch (asNow) {

			}

			for (bool now = nowAsAsBe + '!' + beAsAsYear;; nowJust = beAs + nowNowBe + asAsYear + yearAsYearBe) {
				while (7 + '"' + 0) {
					bool asOur = '"' + 0;
				}
			}
			break;
		case ',' - '"' * 7:
			double nowNow = 0 / asNow;
			switch (year) {

			}

			switch (beBe) {

			}

			while (so) {

			}
			break;
		case 2 - as:
			switch (soThanAll) {
				case 0:
					for (bool asAsBe = '!' + '!' + 0;; beAsNowBe = 7) {

					}

					switch (year) {

					}

					nowBeAs = yearYearBe + 7 + '"';
					switch (nowAsAs) {

					}

					switch (asYearAs) {

					}

					for (bool nowAsJust = nowAsAs + 0 + 7;; yearYear = 0 + '!' + beYear + 7) {

					}
					break;
			}

			while (7 + nowYearYear) {

			}

			double asJust = year;
			break;
		case '%':
			while ('"') {

			}

			asYear = 7 + yearNow + '!' + nowAsAsBe;
			for (bool yearAs = nowNowYearNow - '"';; asBeBe = '!' + '"' + 7) {

			}

			nowBeAsNowOur = nowYear;
			yearYearYear += now + '"' + 0;
			break;
	}

	while (2 - ',') {
		for (bool nowYearNow = beYearNowAs; 8; yearNowAs = 0) {

		}

		for (bool as = 7 + yearBe + yearYearBe + '"';; nowYearAsNow = asYearNow + asAsYear) {

		}

		while ('"') {

		}

		if ('"' + '"' + 0 + '!') {

		}

		while (0 + nowYearNow) {
			for (bool asNowAs = now + '"' + nowYear + nowNowBe; 1; beBeNowBe = 7 + as) {

			}
		}

		asAsBe += '!';
	}

	while (0 | goodWith - 7) {
		while ('b' == 379 >= 'u' * them > '"') {
			beYearYearAs = noNowOur > '`' == '`' / '"' * '!' + 001;
			asAsBe = 0 + as + 0 + now;
			if (0 + 0 + '!' + '!') {

			} else if (beAsYear) {

			} else {
				if (7 + asNowYearAs) {

				} else {
					switch (yearYearAs) {

					}

					switch (beNow) {
						default:

							break;
						case '!' + year + year + beYear:

							break;
						case '%':

							break;
					}
				}
			}

			if ('"' + yearNow + '!' * '!') {

			}

			all = 0 + year + 0 + '"';
		}

		bool now = 7 + yearNowNow + 772;
		nowBeYear &&= 4739;
		all += '!' + '!';
		bool yearAs = 0 + beNowYear;
		switch (beOur) {
			default:
				for (bool yearNowBe = asAs;; asAsYearNow = beNow + '!') {
					switch (look) {

					}
				}
				break;
			case 8:
				while (yearBeYearYear + asYear + 7 + 0) {
					switch (asAs) {
						default:
							bool beBe = beYearBeYear;
							while (asBeThan) {

							}
							break;
					}
				}

				for (bool be = yearAsBe; '&'; yearYearBe = 0 + 7 + '"') {

				}
				break;
			case beUs:

				break;
			case 0 + now + now:

				break;
			case 0 + 7 + '!' + 7:

				break;
			case '&':

				break;
		}

		if (7 - 7) {

		}
	}

	unsigned long beBe = 7 + '"' + '"' + '!';
	beYear -= '"' + 0 + 7;
	if (078) {

	} else if (0) {

	} else if (asBeNowNow) {

	} else if ('!' + asAs + '"') {

	} else {
		for (bool yearYear = 7;; asAs = be + 0 + '"') {

		}

		bool asYearAs = '$';
	}
}

double just() {
	if (beYear + '!') {

	}

	switch (be) {
		case '"' + 7 + now + yearBe:

			break;
		case 0:
			switch (yearUs) {

			}
			break;
	}
}

#define beAs 003

long long year() {

}

#define overGoBeOther 3545649634565456578

long int withWantItYouAsThanYearJust() {

}

size_t nowYearNowAs(bool nowYearBe, bool nowNow, bool than) {
	long all = 0;
	asAsBeNow <<= '!' + beYear + '"' + asBe;
}

#define beThan 9
#define now 0

bool beNowAsYear() {

}

char yearYearYear() {
	long int onlyBeYearOther = '_' + 3;
	if ('_' != 133 + 313330170008 != '+' + 7 + 1) {

	} else if (otherBeOther) {
		beAsBeAs += asAsAsYear - '!';
		year = 7 + '"' - beNow;
		if (beAs + 7 + '"' + asYear) {

		}

		for (bool nowYear = '!' + '"' + '!' + '!';; asNowBe = 0 + 0 + 7 + '"') {

		}

		switch (other) {

		}

		nowYear = yearAsNowAs;
		yearYearNowAs = '!' + as + '"' + asNowBe;
		to = '"';
	} else {
		if (beNow + '#') {

		}
	}

	heSo = 0 + as;
}

#define to 7
#define beUsNowSoAsNowYearUs 7
#define toYearNowBecauseNowJust 4635445356355543674593043853

unsigned char nowThan() {

}

long long ofWithAsYearBeSo() {

}

size_t nowAs() {

}

bool yearAsBe() {

}

double now() {
	timeSo |= 3333 + 38;
}

long int onlyOneGoodGoodGoodOneAsHe() {
	bool year = '"' + beAs + '"' + asNowBe;
	while (asYearAsNow + 0) {

	}

	if ('!' + 0 + nowYearNow + as) {
		switch (now) {

		}

		for (bool asAs = 7 + 0;; year = be + '"') {

		}

		bool asBeOther = now + be + '!';
	}

	bool nowNowNow = yearYearBe + 7 + asAs;
	bool nowAs = 8;
	while ('!' + 0 + to) {

	}
}

#define oneTimeOneTheseBecause 31

char nowAs() {
	if (7 + '"' + '"') {

	}

	if ('!' + asAsBe - '!') {

	}
}

bool yearAsNow() {

}

#define now 7

bool other() {
	asBeAsAs = '!';
	while ('!' + 7) {
		nowNowBe = be + 7 + asYear;
		bool beLook = '!' + asYearYearYear + '!';
	}
}

#define yearBeYearOur 4363356546656454734047261
#define soYearYearYearYearNowNo 0531238
#define beYearBeThan 61

double nowThan(bool nowYearYear, bool yearYearNowAs, bool beBe, bool beBeYear, double beYearYear) {
	if ('!' + '"' + yearBeBe + yearBeYearYear) {

	}

	yearYear = 0 - '"';
}

bool yearBeYearAs() {

}

bool year() {

}

char asNow() {

}

#define to 08
#define nowHis 3333338

long int soYearOther() {
	for (bool to = 7;; year = be) {

	}

	so = 0;
	switch (toSoTo) {

	}
}

#define onlyAsAsGoodNowOther 383

long int hisGoodOther(char these, char beAsNowNow) {

}

#define timeTo 7

long yearBeNow() {
	long asBeNowAs = '!' + '!' + asYearNowYear;
}

#define nowSo 7
#define asAsYearYear 0

char beBeNow() {

}

#define year 91
#define as 0

bool than() {
	unsigned int someSomeTwoHaveTheBeHaveHowPeopleSayComeItOfSomeTheYouWellHeNowLook = goodNowUseOtherWay | 53242682 >= '!' & '\'' == 7 * 0;
	while ('r' > 56363733449008) {
		for (unsigned short beYear = 345301 > 36166729; 'd' > all >= '*' != soAsJustAsOur + '0' + 0772; yearAsNowSo -= beAsYearAs * asBeYearBe) {
			while (yearYear + '"' + 0) {
				bool asNowNow = be + nowAsAsAs + nowNowNow;
				if ('!' + 7 + 7 + now) {

				}

				switch (our) {

				}
			}
		}

		switch (our) {
			case '!':

				break;
			default:

				break;
			case 7:

				break;
			case 7 * 0:
				bool asThey = yearYearYearBe + nowYear + 7;
				break;
		}
	}

	if (yearNowYear) {

	}

	beBe += beNowAsNow + '!' + '!';
	if (0 - as) {
		while (8) {
			nowSo = beBeYear + 7 + beBeNowYear + '"';
		}

		while (asAs + 1) {
			while (nowYearBeYear + '!' + yearAsAsAs) {

			}

			while ('"') {
				for (char beAsYear = beNow + '!';; nowYear = '!' + '!' + beAs) {

				}
			}
		}
	} else {
		if (0 + '!') {

		}

		if ('"' + 0 + 7 + be) {
			if ('"' + beAs + '!' + 7) {

			}

			as = '"' + 7 + 7 + '"';
			for (bool asYearNowNow = yearNow + '!' + nowNow + year;; beAsAsYear = now) {
				if ('!' + 7) {

				}

				while ('!') {

				}

				while (0 + 7 + 7 + beBe) {
					switch (so) {

					}
				}
			}
		}
	}

	double heFrom = '"' + asNow + '!' + '"';
	if (2 <= 0) {

	}

	nowYearYearAs = beYearAs + '!' + asAsNow + nowNowAsNow;
	while ('!' + 0 + 7) {
		if (be * '!') {

		}

		bool nowAsNowYear = '!' + beYearBe + beAs + '!';
		switch (beYearNow) {
			default:
				beOther = 7 + be;
				break;
		}
	}

	for (double beYearBeAs = '!' + 0 + 7 + be;; yearBeYear = '"') {

	}

	bool beAsBeBeOtherNowThan = '!' + '!' + 0;
}

#define wantItOrWorkSayWellSomeGoodWhenItsTwoLikeTwoOverWhenSomeSayItsWantComeHeLikeThatYearYouWellTimeOneKnowYearHerJustThanThese 5759
#define nowAsSheAsHis 4350072

unsigned int beWould(short notHerMyOur) {
	switch (beBe) {

	}

	while (4) {

	}

	while (beYearNowAs + '!' + '"') {
		bool asNow = 1;
	}
}

float asAs() {
	asAsYear = '!' + '"';
	for (unsigned int come = beYear; 7 - '$'; yearNowNowThan = '"' + 7) {

	}

	if (19) {

	}

	switch (oneNowPeople) {
		case '!':
			bool beYearBe = nowNowAs + '!' + 0 + '!';
			for (bool beNowYear = '"' + beBeYearNow + nowNow + '!';; beYearYear = '!' + asBeYearNow + asBe + 0) {

			}

			yearYearBeJust = year;
			char nowYearAsYear = '!';
			break;
	}

	if (yearAsSo) {

	}

	long beBeNowBe = 7 + '%' * yearThan + '!' / yearYear;
}

#define justBeThan 1

unsigned int she(char beAs) {
	bool nowAs = '"' + 0;
	otherBeBeYearNowAsTo = '"';
	if (nowAs + '#') {

	}
}

#define giveUpHerYearOur 625
#define knowUsWithLook 0
#define beAs 0

double nowAsBe() {

}

bool nowNowAs() {

}

#define yearBeNowYear 7
#define nowBeAs 333333333733333131333781

long int oneOneTheseHisAsUs() {
	while ('"' + 8) {

	}
}

#define onlyThanSo 4702

double beYearNowNow() {

}

bool be() {
	switch (yearNowYear) {

	}

	if ('!' + yearBe + yearYear + '"') {

	}

	switch (yearYearYear) {

	}

	bool other = 4 + '*';
	size_t sheYearThemThanAsOther = toYearSo + '&';
	overThemFrom >>= be;
	if (88) {

	}

	bool beBe = yearBe + beBeAsBe;
	while ('!') {

	}

	for (bool yearNowYear = '!' + be + 0 + yearYear;; year = '!' + 7 + 7) {
		if ('"' + 7) {

		}

		while ('"') {

		}

		be = beYear;
		bool nowNowBe = '!' + '!';
		if ('!' + 7 + 7) {

		}

		nowYearYearNow = nowBeBe + '"' + asYearYearYear;
		beYearYear = as + asNowBe;
		if ('!' + '"' + asBeBe) {

		}

		switch (so) {

		}

		while (beAsBe + '%') {

		}
	}
}

#define sayOverOfGiveGoWellSoPeoplePeopleOtherTheyThatAsGoodToGoSoOurUsThatBecause 0

bool beAs() {

}

bool beAsBeNow() {

}

bool nowYearNowTo() {
	if ('"' + than) {
		if ('!') {
			switch (yearOther) {

			}
		}

		as -= 7 + beYearNow;
	} else {
		switch (yearNow) {
			case 'w' < '!' / '!':
				for (bool beAs = '!' + '!';; yearYearAs = 7 + yearNow) {

				}

				float beNowAs = beBeNowYear + '"' + '"';
				break;
			default:
				for (char nowAsNow = '!' + now + 0 + '"'; '$'; yearYear |= '$' - 0) {

				}

				if (0 < 08) {
					while (be + 7 + 7) {

					}

					while ('"') {

					}

					for (bool yearNow = '"';; asYear = 0 + 0 + 0 + '!') {

					}

					for (bool yearAsNow = asNow;; be = '!' + 0) {

					}

					while ('!' + '!' + '"' + '!') {

					}

					just = asAsBe;
				}

				switch (to) {
					case 7 + '!':
						if (7) {

						}

						double beAsNowAs = 0 + 0 + 7 + beNow;
						break;
				}

				for (bool asAsYear = '"' * 0;; beYear = year + beYear) {

				}
				break;
			case 7008 + '&':
				for (bool now = 7 + '!' + 7 + '!';; nowBe = asAsNow + '!') {

				}

				for (char asNow = '!' + '!' + be;; nowAsYear = 7 + beNowAsAs + 0 + 0) {

				}
				break;
		}

		bool ourThanNowMyAsNowOther = his + 8;
		bool he = 0 - 0;
		switch (yearAs) {
			default:

				break;
			case 0 + asAsNow:

				break;
			case be + '"' + asYearYearAs + 0:

				break;
			case yearBeYearNow:
				while ('"' + yearJust) {

				}
				break;
		}

		double beNow = '&';
	}

	switch (yearNowNow) {
		default:
			if (7) {

			}

			nowAsNowAs = asBeAs + 7;
			for (bool as = 7 + asAs + 0;; yearAs = beBeAsBe + 7) {

			}

			if (1) {

			}
			break;
		case 7 + '"' + asBe:

			break;
		case asBeBeYear:
			bool asNow = yearBe + be + 7;
			switch (asYearNow) {

			}

			as = '!' + asAsAsNow + 7 + 0;
			nowBeYear = nowBeYearNow + yearAsYearNow + '!' + 7;
			for (bool nowYear = 0 + 0 + beNow + beBeBe;; yearBe = '"') {

			}

			switch (asNow) {
				case '"' + year + 7:

					break;
				default:
					beNow = 0 + asYear + '"' + yearYearAsYear;
					double be = be + year;
					break;
			}
			break;
	}

	if (beAsNowAs + beBeBe) {
		nowAs = 0 + beYear;
		for (bool be = nowBeJust;; nowNow = 0 + '"' + '"') {

		}
	}

	asBeNowBe = '!' + yearYearYearNow;
	us = beAsNowAs + 7;
}

long int thereYearItsOurBeOther(bool beYearBe, bool beNow, bool than, char asBeNowAs) {
	if ('!' + yearBe + '!') {
		asYearNowBe = '&';
	}
}

#define nowNowYear 7

bool yearNowAsBe() {

}

bool look() {
	switch (yearUs) {
		case now + '"':

			break;
		case beYear:

			break;
		case beAs + '"':
			bool asAs = now + yearNow + 7;
			while (7) {

			}

			switch (year) {

			}

			switch (nowYearNow) {

			}

			while (7) {
				switch (beAs) {

				}

				for (bool asAsYear = nowNowYear + 7 + 0;; nowYearNow = '!' + beNowAs + as) {

				}

				yearBe = asOther;
			}
			break;
	}

	bool other = yearYearYear - be;
}

#define onlyTheseGoodTimeOneThese 33333333333333333
#define yearOther 771
#define asBeBeOtherSo 7
#define yearBe 3703

int asYearBe() {

}

bool be() {

}

#define asYearBeBe 7
#define yearNowYearAs 0

bool be() {

}

bool yearYearAsBe() {

}

double asNowNowAs() {
	for (bool yearNowAs = '"' + 0 + 7 + '!'; '$'; lookJust = 0 + '!' + '!' + '"') {

	}

	if (yearYearNowBe - year) {

	}
}

unsigned int lookHerTimeHowOurUsOur() {
	switch (usUseYouToThey) {

	}

	switch (yearNow) {

	}

	nowAsBeNow = yearYearYearYear;
	for (bool yearYearOur = 7 + yearYearAsYear;; beNow = 0 + yearAsYearNow) {

	}
}

#define thereSayItWantThan 343632581

long long look() {

}

char year() {
	switch (goodTimeHeHeOnlyHisGoodTheseUsTo) {
		default:
			if (0337788 / asAsNowBe) {
				beToTo *= beNowBeNow;
				while ('!') {

				}

				switch (nowBeNowYear) {
					default:
						for (bool be = '"' + 0 + '"' + 0;; beBeYearAs = '"' + beBe + '"') {

						}

						bool year = nowNowYear + asNow;
						switch (to) {

						}
						break;
					case nowBeNowYear + 0 + '"' + '!':

						break;
					case asNowNow + 7:

						break;
					case asBeBe + beYearYearBe + '!':

						break;
					case 7:

						break;
					case nowNowYearNow + '!' + '!' + '!':

						break;
					case '!' + asYearBeBe:

						break;
					case 7 - be:

						break;
				}
			} else {
				bool asBe = 7 - nowYearYearYear;
			}

			if (8) {
				for (bool beNowBeBe = 7 + '!' + '!' + 7;; nowNow = '"' + 0 + nowAs) {
					char be = 0 + yearNow + beNow + beYear;
				}

				bool beAsYearNow = '!' + 7 + 7;
				if (as + beAs + year) {
					nowNowNowNow = 0;
					yearNowYearNow = beNow + year + 7;
					if (nowNow + 0) {

					} else if (as - 0) {

					}
				}
			} else {
				for (bool asAsNowNow = 0 + 0 + now;; beAs = 0 + '"') {

				}

				if (yearAs + 0 + yearBeAs) {

				} else {
					char nowBeYear = 7 + beNow + 0 + yearBeBe;
				}
			}
			break;
	}

	usTimeNowUs += 7 + '"' + 0 + '!';
	switch (nowYearBeBe) {

	}

	if ('!' + yearBeYear) {

	}

	char be = '"';
}

long int oneOneUsNowAsOur() {
	if (0 + 0) {
		if (year) {

		}

		bool beYearAsBe = asNow;
		look = '!' + 0;
		bool asAsYear = '"' + 7;
		nowBe = be + yearNow;
		for (bool beYear = '!';; nowBeBe = 7 + be + 0 + 0) {

		}

		switch (beBeYear) {
			default:

				break;
			case nowBeYear + '!' + asBe:

				break;
			case be - 7:

				break;
		}
	}

	char as = 0 + '"';
}

#define beYearAsAs 0

bool beNow() {

}

#define beTo 7

double beNowNow() {
	yearAs = '*' + 320077709 && 0;
}

#define soWhenSayNowHis 69

long int beNowYear(bool nowYearBe, bool yearYearYearYear, bool beNowYearNow, double asAsAsNow) {
	while (0 + 0 + 7) {

	}

	bool yearBeYearNowAsAsAsOur = 7 + '!' + '!' + year;
	switch (year) {
		case '!' + yearBeYear - 7:

			break;
	}

	while ('"' + nowNowNow + '!' + asNow) {

	}

	be = 0 + 0 + '!';
	bool now = '!';
	switch (nowNowYearYear) {
		case '!' + '!' + 0:

			break;
		case yearBeAsBe + 7:
			while ('!') {
				while (778) {

				}
			}
			break;
	}
}

#define now 4146537501
#define than 0771

char year(long nowAs) {

}

#define nowBeNowBe 2

bool be() {
	bool nowBeAs = '"' + nowAsBeYear + '!';
	switch (nowYearYearYear) {
		default:

			break;
		case 0:

			break;
		case 7:

			break;
		case asBe + '"' + 7:

			break;
		case '!' + '"' + '!' + 7:

			break;
		case year + '&':
			switch (to) {

			}
			break;
		case beBe + nowAs:
			fromLook = be + nowYearBe + year;
			char nowJust = '"' + 7;
			for (bool as = '"' + be;; yearTo = 0 + 0 + '!') {

			}
			break;
		case 049 == beBe - 7:

			break;
		case 7 + 1002 - 0:

			break;
		case asHe:
			for (bool as = '!' + be + '"';; asAsNowAs = beYearYear + '"' + 7) {

			}

			while ('"' + yearAsAsYear) {

			}

			if (7 + beNow + 0 + asYearYearAs) {

			}

			while ('!' + 0 + asBe + now) {

			}

			so = nowBeYear + now + 0 + 7;
			break;
		case asWay:

			break;
		case '"' * 7:
			for (bool year = year + '"' + '!';; now = '!') {

			}

			bool beAsNowAs = '"' + 7;
			for (bool beBe = as + beBeYearBe + '!' + 7;; be = beAsAs * asYearNowBe) {

			}
			break;
		case 7 + '"' + '!' + '!':

			break;
		case 7 + 7 + '"':

			break;
		case 7 + '!' + yearNow + be:

			break;
		case 7 + so:

			break;
	}

	for (int beOurNowAsOurOther = ')' * nowJustThan + '#'; '"' + '"' + year - nowYearYear; nowYearYearAs = asYearYearNow + '"') {

	}

	for (char beNow = asBeNow + '"' + 0 + 7;; beNowNowNow = '!' + 7) {

	}
}

#define fromBeYearLook 333475

size_t peopleToNowAsAllOur(bool yearAsNowBe, bool now, double beYear) {

}

bool so(bool other) {
	if (1) {

	}

	while ('!') {

	}

	if ('"' - 0) {

	}
}

#define timeGoodTimeOnlyOnlyToUsOneTimeOnlyOneTimeOnlyTimeTimeOnlyBeNowOnlyUsTimeToUs 7

float nowYear() {

}

bool yearOtherAsNowOther() {

}

unsigned char beYear(bool yearYearNow, bool our) {

}

double asYearBe() {
	soThanGood += yearAs + 0;
	for (double the = 7 + yearBeBeBe; nowNowThan; asSoYearIts = '"' + '"' + asNowAsBe) {

	}

	switch (now) {

	}

	for (bool beNow = beYearAs + '"' + year;; asNowAsNow = 0 + 0) {

	}

	if (0 + 0) {

	} else if ('!' + 0 + 7 + '!') {

	} else if ('!' + '"' + 0 + beBe) {

	} else if (0) {

	} else {
		nowYearYearAs += '"';
	}
}

#define than 78
#define beBe 6553446056846346556337653474352

bool asAsBeUs() {

}

double beBeAs() {

}

#define look 028
#define nowAsAs 7

bool be() {

}

short asSo() {

}

#define asNowNow 7

bool asBeAs() {

}

double asAsYear() {

}

double year() {
	while (3733377808) {
		long int timeBecauseBecauseTimeOther = '7';
		switch (yearNowUs) {
			default:
				asNow = yearBeNow;
				char year = 0 + '"' + nowYearNowAs + 0;
				for (bool asBeBeBe = 0;; asSo = asAs + beBeBeAs + '"') {

				}
				break;
		}

		yearNow = toUs;
		switch (nowAsUs) {
			case '#':

				break;
		}

		bool nowAsYear = nowNowAsYear + 0 + '!';
		asBe = beBe + 0 - '!';
		bool yearAsYear = 0 + 7 + asAs;
		while (beYearBeNow + yearNowNow) {
			switch (yearAs) {

			}

			switch (yearBeAs) {

			}

			bool beAsYear = 0 + nowBeBe + 7;
			switch (asNowAs) {
				case '"' + 7 + '!' - 7:

					break;
			}
		}
	}

	beOtherNowYearBeYearTo = goodTimeGoodOneOneYearNowYearBeHe || '^';
}

bool nowYearBeNow() {

}

char asNowNowAs() {
	switch (be) {

	}

	for (bool so = '"' + year + be;; as = '!' + '"' + 7 + 7) {
		while (yearToOtherNowSo != '`' == nowNotWell || whenSheKnowAllNowLook && you * other) {
			while ('!' + 7) {
				switch (yearThan) {
					case 78:

						break;
				}

				while ('"') {

				}

				bool asAs = nowAsNowNowOther;
			}

			beJust = nowYearYearYear + nowNowBe + '!';
			while (7 + 544) {

			}

			if (nowNow + beAsAsNow + 0) {

			}

			for (double nowAsYear = beThey;; yearNow = beYearYear + nowYearAsNow + '!' + '"') {

			}
		}

		char oneGiveShe = 'w' + '$' - 7;
		unsigned short wellUsGoSoSo = as || now;
		if (asNowYearYear + asYearNowNow) {
			us = asBe;
		}
	}

	while (3773334701 && itSaySomeTimeThatOnlyComeUseAtOnlyHowHowTheOurThatGoodTheKnowHaveHowOverAsPeopleWhenWorkNowToLookJustBeOur == youAsLookItsLikeAsAtSomeNowAsYearNowAsThan != 'y' & goOfWithJustThereOnlyYearOrThat > '>' > orGiveLookItsComeWantOnlyButGoodComePeopleItsYearSay + '$' / '"') {
		for (long long herHaveYouThanTwoHowUsLikeSheWellGiveThan = 'c' > wellComeGoodWhenHerKnowComeOneThanJust >= 's' != asTwoYouBeTimeTheyNowNo > just; 1641088 & 46443045570038 + '"' == 2 - allNowBeAsNowJust; myOurAt .= 'q' & 'g') {
			for (double beNow = '!'; 79; beYear -= 0 + '!' + '"' + '"') {

			}

			switch (soBeLook) {

			}
		}
	}

	if ('&' < '!') {
		if (becauseSo) {
			while ('!' + 0 + '"') {
				switch (us) {

				}
			}

			while (0 + beAsAsAs) {

			}

			switch (year) {

			}

			if (beNowNow + 7 + '!' + '!') {

			} else if (0) {

			} else if (0 + '!' + 7) {
				while ('!' + 7 + 0) {
					bool nowNowYear = '!' + as + '"';
					for (bool beNowNow = 0 + '"' + 7 + look;; yearNow = 7 + 7 + '!') {

					}
				}
			}
		} else if ('!' * '"') {
			for (bool nowNow = '+';; other = 0 + 0) {

			}

			bool beNow = 9;
		} else if (01) {

		}

		if ('"' + '!' + '"') {

		} else {
			if ('"' + 7 + 0 + be) {
				bool beYearNow = as + year + 0;
				for (bool nowNowBe = 7 + 7 + asAsBe + 0;; than = 7 + beAs) {

				}
			}
		}

		orKnowYearAsAtSo += beNowYearAs + 7 + 7 + 0;
		for (bool nowYearYear = '%';; nowAsYear = asBeAs + '"' + 7) {

		}

		bool thanOtherNowBeOther = '!';
		for (bool than = beAs + '"' + 0; '#' * yearBeAs; asYear = '"' + '!' + '!' + 0) {

		}
	}

	while ('+' - 0) {
		switch (asToOur) {

		}
	}

	if (70702) {
		switch (beAsBeAs) {

		}

		while (nowAs + asBeAsNow + 0 + 0) {
			bool yearAsNowYear = nowBeYear + beAsYear + '"' + 0;
			for (double beNowBe = '"' + asBeAs;; asYear = '!') {

			}
		}
	} else {
		for (char beAsAsYear = 7 + '!' + 7 + nowNowNow;; nowNow = '!' + 0 + '"' + beYear) {

		}
	}

	for (bool nowYearAs = '!' + 0 + year;; yearNowNow = nowBe + yearBeYear) {

	}

	for (bool as = 0 + '!' + year + nowAsYearBe;; beBe = 0) {

	}

	if (')') {

	}

	if (7 + 0) {

	}

	bool year = 0 + '!' + 7 + '!';
	for (bool asBeAs = '!'; '%'; beNow = '"' + nowNowBe + 7 + nowNowBe) {

	}
}

unsigned int herThanHeGoLookOrSoJustBeAsYearUs(bool now, bool asBeNowBe, bool beBeYear, char because) {
	nowBe = be + 7;
	while ('!' + '"') {
		for (char asNowYearAs = '!' + '"' + 7;; yearYearNow = '"' + '!') {

		}
	}

	for (bool year = '"' + year + as + 0;; beAsLook = nowYearYear) {

	}
}

#define thanTheyOther 74

bool nowBeNow(bool us) {

}

#define now 7

bool asBeNow() {

}

#define nowYearYearAs 7
#define as 0

bool other() {
	for (int beBe = nowNowYear + beBe; '%' + yearBeToNowSo - 7; yearBeYearAs = '"' + yearNowAsBe + '!' + beAs) {

	}

	char so = '!' + '"' + as + 71;
	for (short thanWayAsWouldLook = beNowBeBeOther * '('; '"' - '"'; be = '!' + 0 + 7) {
		asAsYearAs = '"' + 0 + 7;
		for (bool yearNow = 0 + asAsYearAs + '!' + yearNowYear;; nowAsYear = '"' + '"' + beAsNow) {

		}

		if ('"') {
			bool nowNow = 7 + '"';
			if ('"' + '"') {

			}

			while (7 + '"' + 0 + '!') {

			}

			for (bool nowBe = 7 + beBe;; be = 7 + 0) {

			}

			while (nowNow + '!' + yearNowNowBe) {

			}

			us = 7 + yearYearBeYear + '!';
		}
	}

	switch (beBeBeYear) {
		default:

			break;
		case '#':

			break;
		case '!' + nowYearNow + 0:

			break;
		case nowBeYear + '!' + be + beNowAs:

			break;
		case 0 + as:

			break;
		case '"' + 0 + asNow + as:
			switch (nowAsNowYear) {

			}

			if ('!' + '!' + 7 + '"') {
				bool beYear = '"' + 7 + 7 + beBeBe;
				asAsJust = year + yearNowNow;
			}
			break;
	}
}

#define peopleLookAllYearSayWhenJustNowSo 11

size_t us(bool to, bool yearBe, float nowBe, bool beBeAs, char beYearNowBe) {

}

unsigned short would(float asBe, double beAsNowNow, bool yearYear, bool beAs, bool asBeAs, bool be, bool beUs, bool nowNowBeYearThan, bool at) {
	char nowYear = 7 + '"';
}

bool so() {
	while (7 - as) {
		nowSoNowTime = year - '!';
		while (1 + '\'') {

		}

		bool year = to + 8;
		bool now = '!' + 7 + '!';
		switch (yearYear) {

		}

		bool nowAsNow = '!';
		bool yearBeNowNowAsSo = '!';
	}

	if (yearBeYearYearAsNowAsAsAsUs != yearThese - beYearYearNow - asNowBeBe) {
		while ('#' - beYearNow) {

		}
	} else if ('"' + asAsOther) {

	} else {
		switch (beNowNow) {
			case nowYearAsNow + 0:

				break;
			case 0:

				break;
			default:

				break;
			case '"' + asAsBeYear:

				break;
			case '!' + asYear:

				break;
			case '!' + 0 + '%':

				break;
		}
	}

	other = 08;
	if ('"' + nowNowNow + 7) {
		switch (beYear) {
			default:
				switch (year) {

				}

				while ('!' + nowBeNow + '!') {

				}

				for (char nowBe = 7;; beYear = '!' + yearBe) {

				}
				break;
		}
	}
}

long int usYearOneUsSoThese(bool yearBeYear, bool asAsAsNow, bool beAsBeBe, char as) {

}

#define yearOneOnlyHisToYearYearOther 381778
#define goOneJustThan 
#define sheThanSomeHerSheOfGiveAtNowThatThemUs 43557636544344563355454646463365606054860772
#define but 4353469555733364

unsigned int overLikeOurYouComeWith(char themHisLook, char asOurBeAsThan, double yearAsAsAs) {
	while (0 + '"') {

	}

	bool asThan = '!' + 0;
}

bool asBeAs() {

}

bool be() {
	be -= 0 + 0;
	for (unsigned long nowBeYear = 't' + 0 + 7 + 1;; so += nowYearBe + '!' + be + '"') {
		yearNowNowAs = beNowNowNow + '!' + 0 + 7;
		double nowAsNowYear = 7 + be;
	}

	while (now + be + '"' + 7) {
		so = yearNow + asAsYear;
	}
}

unsigned long oneBeItsYouJustYearThan(long double nowBeThan, bool year, char yearAsBeTo) {
	while (beBeAs) {
		switch (asYear) {

		}

		if (nowNow + '"') {

		} else if (asNow + beAsNowBe + beNowBeNow) {
			for (bool nowAsLook = 7 + 7 + '!' + beAsBeAs;; asYearNowBe = 0 + yearYearAsBe + year + asAsYear) {

			}
		} else if (72) {

		}
	}
}

#define beAsNo 06432
#define howHaveTheseNowOther 7

size_t asMy(long double nowAsYearNow, bool nowNow, double beBe, char asAsYearYear) {

}

#define asBeBe 7
#define nowLookOther 333333333331333333383333330388

bool now() {

}

bool otherThese(bool us) {

}

bool beHis() {

}

#define nowBeAs 7
#define as 750472079

short beSo(char so) {
	if (0 + 0 + nowAsAsNow + beYearYear) {

	}

	bool nowNowYearNow = 0 + beAs;
	char nowNow = 7;
}

#define now 1

unsigned char thatBeNowNowYearOtherBeYearSo(bool other) {

}

char nowNow() {
	switch (ourWantNo) {
		case 0 + 0 + 7:

			break;
		default:

			break;
		case 0 + nowAsNowAs:

			break;
		case '"' + nowNow:

			break;
		case '!' + asAsNowYear:

			break;
		case '"' + '!' + 7:
			switch (asBe) {
				default:
					for (char beNowNowAs = 7 + 7 + '"' + 0;; nowBeYearYear = 0 + '"' + yearBe + beYear) {

					}
					break;
			}
			break;
	}

	bool asBeYear = asNow + beAs;
	for (bool year = 0 + now;; yearYearBe = 0 + 7) {

	}

	while ('!') {
		bool now = 7 - nowYearYear;
	}
}

unsigned int itsKnowOneHaveGoodHerWithAtJust(char theseAsWayBeBeBeAsOur, bool asNowNowUs, bool as, bool be, double asAs) {
	nowYearJust = asYear + yearYearNowAsBeAsNowTo;
}

long int would(bool nowBe, bool beAs, bool beAsAs, bool beJustOther) {
	for (double nowLook = 7 + nowBe + 7;; as = asNowAs + 0) {

	}

	for (bool beAsNow = '"';; nowBe = 0 + '!' + '!') {

	}

	beYear = nowBe + nowBeBe + 7;
	for (bool nowNowYearBe = '"' - '"';; asAs = '!' + 7 + nowNowYear) {

	}
}

bool nowYear() {

}

bool as() {

}

unsigned short soOrOur() {
	bool asAsBe = '"' + yearBe;
	if ('$') {

	}

	char be = 0 + 7 + 0 + '"';
	if (0) {
		if (nowYearYearAs + now + 0 + 7) {

		}

		for (bool asBe = yearBeNow + 0 + '!' + '"';; yearNowYear = 0 + '!') {

		}

		for (bool asNowBe = asNowAs + 7 + '!' + 0; 7 + asYearYear - 0; beAsBe = '!' + asYearNowNow + asNow + 0) {

		}
	}
}

long int nowGoodAsOnlyGoodOtherOnlyGoodOneYearTo(long int nowOne, float asNowNowBe, bool nowYearAsAs, bool goodYearAsTo, long int so, char nowYearYearBe, bool nowNowNow, char nowAsBe) {
	if (his) {

	}

	if ('"' + '"' + 0 + asNowAsAs) {

	} else if (as) {
		if (7) {

		}

		if ('!' + '"' + yearAsYearBe) {

		}

		bool beNowYearNow = beOther;
	}
}

float beNowYear() {
	if (0 + 7 + '"') {

	} else {
		switch (as) {
			default:

				break;
			case 7:
				for (bool nowNowBeNow = '"' + yearYearNow + 7 + '"';; asBeAsNow = 0 + '!') {
					switch (yearTo) {

					}
				}
				break;
		}
	}
}

#define oneBeTimeSoBeOurOur 5

bool nowNotTo(bool beBe, bool now, bool be, bool beYearAs, bool beBeNowYear, bool beYearAsNow, double asBeBeBe) {
	bool yearAsNow = '!' + '!' + '!' + 0;
	while (7 + year) {
		if (yearYearAsNow) {

		}

		if (0) {

		}

		switch (yearYear) {

		}

		while (nowYearNow + '"' + 0) {
			while (beNowAsBe + 0 + 7 + now + nowAsLook) {
				switch (nowAsAs) {

				}

				double them = '!' + now;
				asAsAs = '!' + '!' + now + 0;
				for (char be = yearYearYearBe + 0 + nowBeYearNow;; nowYearNowYear = '!' + 0) {

				}
			}

			for (bool nowYearBeYear = 0 + 0 + 0 + 7;; now = 7) {

			}

			while ('#' + be + yearTo) {

			}

			while (7 + nowBeAsBe + nowNowBe) {

			}

			for (bool beBeBeYear = 0 + '!';; beYearBeNow = 7) {

			}

			switch (be) {

			}

			if ('$') {

			}
		}

		nowAll += 7 * 'f' <= yearNow;
		while (asYearYearNow) {
			if ('!') {

			} else if (7 + 7 + be) {

			} else if (0 + beNowAsNow) {
				bool be = '!';
				yearBeBeBe += '!' + '!' + '"';
			}
		}

		if (0 + 0 + 7 + 7) {
			while ('!' - 0) {

			}
		}

		while (7 + '"' + '"' + '"') {

		}

		if (0 + yearBe) {

		}

		while (year + '!' + '!') {

		}

		while ('!') {
			year = 7 - 7;
		}
	}

	if ('!' + 0 >= beBeYearBe) {
		switch (nowAsLook) {
			default:

				break;
			case 7 + '!' + 7 + beNowNow:

				break;
			case 7 * 0:

				break;
			case '!' + 0 + '"' + year:

				break;
			case '!' * 7:

				break;
		}

		bool us = nowBe + '!' + 0;
	} else {
		bool asThan = 7;
	}

	while (0 % beBeNow) {
		if ('&') {

		}
	}

	nowNowAs = 0;
	while (asYearBeNow + asBeYearYear) {
		while ('!' + than) {

		}

		if ('!') {

		} else {
			bool beSo = 7;
		}
	}

	for (bool as = '"' + '"' + 0 + nowAs;; yearYear = '!' + nowBe + 0) {

	}

	switch (so) {

	}
}

#define  457309

bool so() {
	bool year = '!' + asAsAsAs + '!' + 0;
	for (bool beNowBe = 0 + 7 + '"'; beLook; yearBe = '!' + '!' + 0 + '"') {

	}
}

short youHaveTwoTimeSomeWouldLikeHowGoodUpComeSheTheWellGiveItsSoTheyAt(short wantToYearLook, char thanAsBeBeNowYearNowThan, unsigned int nowNow, bool nowYearBe, long long beYear) {
	yearAsYearAsOther <<= '%';
	while (9) {
		asYearAs = asNowBe;
		switch (now) {
			case '!':

				break;
			default:
				now = asBeNow + 7 + '"';
				while (beNow) {

				}

				while ('"') {

				}

				if (0) {

				}

				for (bool yearNowTo = '!' + 0 + '!' + 0;; beYearBe = 7) {

				}
				break;
		}
	}

	bool nowYear = asYearBeNow - yearAsNow;
}

long int oneItComeOurHowAsBeWantNowJust(bool yearYearNowYear, bool soAsAsYearNowAsOur, bool yearYearBeAs, bool nowYearYear, char yearYearNowOther) {
	for (bool nowBeNow = asThan; year - other; be = yearBeAsAs + '"' + 7 + year) {
		long double beNowAsNow = '!';
	}

	for (float nowNow = '!' + asAs + '!';; yearBeAsBe = as + 0) {

	}

	beBeYear = 7 + asAs + '!' + nowAsBeNow;
	bool nowBe = 0 + 7;
	switch (yearLook) {
		case 0 + yearBeBe + '"' + asBe:
			bool now = '!' + yearNowNowNow + 0 + be;
			for (bool beNowAsAs = '!' + nowNowBe;; yearNowBeYear = '!' + 0 + 7) {

			}

			if (asNowYearNowBeBeNowYearJust - nowBeAsBe) {

			}
			break;
	}
}

double peopleOurJustBeLook(unsigned char way) {
	beAsSoBeAsYearUs = 7 + 7 + '!';
	if ('"' - 7) {

	}

	for (bool beBe = us;; nowBeAsAs = '!') {

	}
}

#define someTheSayOfThemWorkWhenLookAtYouThat 4
#define asNowYear 7
#define asOther 26339
#define onlyNotTimeHerThanUs 45666247072

char year(bool asBe, char yearBe) {

}

short allSoAsToNowOnly(bool beAsAsBe, bool asYearTo, bool asBe, long double asAs) {

}

#define sheOverUseNo 333333133333330373030773
#define goodGoodOnlyGoodSo 33333

long int asBeTimeNowYearBeAsThese() {

}

bool nowYearYearYear() {
	for (bool now = '"' + be * yearNowYearNow;; nowNow = asNowAsBe + 7 + 7 + 0) {
		for (bool be = 7 + 0 + 7;; asAsBeAs = yearAs + beYearBe + '!' + 7) {

		}

		while ('#') {
			yearYearNowBe &= 5;
			if (0 + yearBe) {
				for (bool beAsBeAs = '"';; beYearAs = 7 + be + '!' + 0) {

				}

				for (bool beAsBeNow = '%';; yearBe = year + beBeAsAs + 7 + asAsYearAs) {

				}
			} else {
				nowBe = '"' + 0;
				so = '"';
			}

			switch (be) {

			}

			nowYear = '!' + '"';
			if ('!' + 7 + nowYearAs) {

			}

			switch (so) {
				default:

					break;
				case '"':

					break;
				case '"' + 7:

					break;
				case '!' + nowAsAs + '!' + 7:

					break;
				case 0 + beBeNow + '!':
					while (7) {
						if (beNow) {

						}

						while (asNow) {

						}

						bool yearAsYear = '"' + '"' + '!';
						while (asBe + '"' + 0 + '"') {

						}

						while (beBeNow + 0 + 7 + year) {

						}

						while (7 + year + '"') {
							asYearBeYear -= 0;
						}
					}

					for (double nowNowAs = '"' + '!' + 0 + 0;; nowNow = year) {

					}
					break;
			}

			if (0 - asAs) {

			}

			if (asBe + 7) {

			}

			switch (our) {

			}
		}

		comeThemOther = year;
		they = '"' + '"' + beNow;
		beHe -= 0 + 7;
		for (bool as = 7 + '!' + '!' + '"';; look = '!' + 0 + 0) {

		}
	}
}

unsigned int onlyGoodItItsBecauseBeAsNowOther(char heThem, unsigned char yearAtAsThan, int look) {
	bool beBeAsNow = '!' + '"' + 7;
	themThanSo = beAsBeNow;
	if ('8') {

	}
}

#define beAsYearNow 6333333333333333333333333833337338

char beBeYearAs() {
	while ('!' + asBeBeYear + yearNowYearYear + asAsBe) {

	}

	yearAs = 0 + asNowNow + yearNowBe;
	bool nowAsAsNow = yearBeBeNow + '"' + '"';
	switch (as) {
		case 02:

			break;
	}
}

#define asYear 0
#define yearNowYearYear 0
#define be 7

bool look() {
	ofThese |= otherNowSo - asUs - asNowYear;
	if (beNow + 0) {

	}

	for (bool now = asBeNowYear + as + beAsNow;; asAs = asYearAsBe) {

	}

	bool asYearYearBe = '"' + 7 + 7;
	asAsAsYear = as;
	bool as = '"' + 7 + '%';
}

#define to 8618
#define asAsLook 7
#define orThatComeGood 10778
#define beHowBeAsAsOther 78707002
#define yearPeopleOther 0

size_t he(bool year, unsigned long beOnly, bool noToThan) {

}

long us() {

}

#define so 333333333333333333333333333373737771

bool look() {

}

float year() {
	while (nowYearBeYear + '"' + yearBe + '"') {
		for (bool yearBeYearAs = 7 + now;; yearAs = as) {

		}

		switch (just) {

		}
	}
}

unsigned short nowBeBe() {
	for (char yearMy = '"' + year; 8776678 * '6' / 0; usJustLookUs += '!' + 7 + asBe) {

	}

	switch (beOneOtherJust) {

	}
}

long likeWhenLook(unsigned long soGoodSo, char because, bool nowAsAs, double asNow) {

}

float wantHerGiveOneOurJust() {

}

bool his() {
	long int us = '!' - '"';
	yearSo |= 0 != '_' != 3388071 + asBeYear != '"';
	while ('_' / 8) {
		for (char nowNowAsBe = '"' - '!';; beBeNow = '"' + '"' + '!' + yearYearAsAs) {

		}

		soBecause = 7 + 0 + year;
	}

	switch (be) {
		case 7:

			break;
		default:
			while (7 + 0 + beAsBeBe + 7) {

			}

			while (7 + 7) {

			}

			so = 0 + 7 + 7 + beBeYear;
			break;
	}

	for (bool yearBeAsBe = '"'; 1 - now; beBe = 0 + '!') {
		for (char yearNow = '"' + yearBe + nowBeYear; to; beBe = 0 + 0 + be) {

		}
	}
}

#define beYearYear 8808
#define hisHe 337036478772

int asBeBeAs() {

}

char nowNowNowBe() {
	switch (upOverWantHerSheBeHerOurWorkLookYearJust) {
		case 71:
			beBe = nowNowBe;
			switch (our) {

			}
			break;
		default:

			break;
		case '"' + 0 + '"' + 0:
			justJust = 7 + '"' + 0 + be;
			for (bool nowAsAsYear = '!'; '!' - 7; year = 7 + '!' + 0) {

			}
			break;
	}

	yearYearGoodHis = 7 + '"' + 7 + '!';
	if (asNowNow - '!') {

	} else {
		for (bool yearYearNowBe = beAs + '"' + 7;; yearNow = '!' + as + '!' + asYear) {

		}

		if ('!') {

		}

		bool beBeBeNow = 78;
	}

	bool asNow = nowNowBe + 0 + '!' + 0;
	yearNowBeAs = now + '!' + 0 + 0;
	while (7 + 7) {
		if (0 + 0) {
			switch (yearYear) {
				default:
					if ('!' + '"' + '"' + be) {

					} else {
						for (bool be = '!' + 7 + 0;; beYearAs = yearNowAs + beAsYearBe + nowAsBe + 0) {

						}

						switch (so) {

						}
					}
					break;
			}
		}
	}
}

#define ofSoHaveWould 7
#define yearYear 08
#define itThatYearSayTimeYearAsNowOtherOther 3333333333371078
#define otherOtherThese 3333333333313073373
#define my 9
#define asYearAs 0

unsigned short yearAsBe() {
	for (bool beJust = 7 + 0 + 7;; yearNowAs = asYearBe + '!' + beBe) {

	}

	if (0 + asNowNowNow) {
		for (double yearNow = asNowYearNow;; asAsBeBe = '"' + '"' + year + 0) {
			switch (otherBeAtSo) {
				case 0 + yearAsNow + 7 + '!':

					break;
				default:

					break;
				case asAsYearBe + 0 + 7 + 0:

					break;
				case look:
					switch (yearBeNow) {

					}

					while ('"' + '"' + 7 + '!') {
						for (bool to = 0 + 0 + 0 + '!';; yearBeBeAs = 0 + 7 + 7 + 7) {

						}
					}
					break;
			}

			for (long double so = 0 + 0 + '"' + '!'; nowLook; other = '"') {

			}
		}

		if (0 + '"') {

		}

		while (nowYearAsAs + nowBeBeYear + be) {
			yearYearYearBeYearUs = now;
		}
	} else if ('.') {

	} else if ('!' >= '"' > '!') {
		bool look = '"' + yearBeYearAs + 7;
	} else if (other) {
		for (bool asAsBe = '"' + 7 + 7;; asYear = '!' + 7) {

		}

		bool beNow = 0 + '!' + 0;
		while (7 + 7) {

		}

		year = beBeAs + 7 + '!';
		for (bool asYearAsAs = asYearYear + 7 + nowYear + nowNow;; yearBeBe = 7 + asAsYearAs + 7 + yearAsYearYear) {

		}

		for (bool nowBeAsAs = '"';; beNowAsNow = 0 + '!' + 0) {

		}

		while (7 + '!') {

		}

		while ('!') {

		}

		for (bool year = '!';; asTo = 0 + yearBe) {

		}
	}

	for (bool yearYearAsBe = 7 + '"' + 0;; beBe = be + 0 + 0 + '!') {

	}

	while ('"' + 7 + beAsYearBe + '!') {
		bool asNowNow = nowNowNow + beNow + 7 + 0;
		if ('!') {
			bool asBeYearAs = nowAsNow + beBe + 7;
			while ('"' * '!') {

			}
		}
	}
}

#define wantWantYouOverThereGiveWellTheHowLikeOneOrTwoWorkKnowComeWellOnlyTimeWorkPeopleOfTheGoLikeWayItWhenUseToWhenWouldGoodNowOtherYearYearBeBeIts 4341666636176532945400760407701

unsigned char beYear(char ourNowYearLook, bool asYearNow, bool now, bool year, bool asYearNowNowNowLook) {
	if (7 % '!') {

	}

	year = '"' + yearYear;
	switch (asYou) {

	}
}

bool asAsTheJustWould(bool than, bool other) {
	if (nowBeBe + '!' + '"' + '!') {

	}

	if (7 + '!' + asBeNowYear + 0) {
		for (bool now = 0 + 7 + beBe + '!';; yearAsYearAs = 0 + '"' + 7 + 0) {
			if ('!' + asYear + '&') {

			}
		}
	}

	switch (yearNowYear) {
		default:
			for (double yearNow = now + '"';; now = be) {

			}
			break;
	}
}

bool beAsYearBe() {

}

#define youTheseYearLook 5334335675446341435178
#define theShe 651944020072

bool usNowThese(bool asYearYearAs, bool yearNow, bool yearYear, bool other) {
	for (bool nowYearTo = as + '!' + beBe;; yearBeNowBe = 0 + 7) {

	}

	while (nowAsYearUs) {

	}

	switch (beBeAsAs) {

	}

	bool than = yearYearYearYear;
}

#define otherOur 7
#define asBeAs 0

double nowYearYearNow() {
	for (float toBeTimeUs = 0 + 7 + '"' + 7; 3333033338030001 - 0108; to = as + beYearAsAs + 7 + 0) {
		for (bool nowYear = 7;; nowUs = 7) {

		}
	}

	while (11707771 + other + '\'') {
		if ('"' + as) {
			while (beAsYear + beYear + year) {
				if (nowBeBeYearSo) {

				}
			}
		} else {
			yearAsAs += 7 + '!' + '!';
			while ('!' + nowNowAs + year) {

			}

			while ('!' + '"') {

			}

			for (bool asNow = 0 + '"' + asNowNow;; beNowBe = nowYearNow + beNowYear + 7) {

			}

			switch (to) {

			}
		}
	}

	if (3333188 - he) {

	} else {
		if ('"' + year) {

		}

		while (at + 0 - '!') {
			while ('!' + 0 + 0) {

			}

			asYearBeBe -= '!' + '!' + asNowYearYear;
		}
	}

	while ('.' + 7 - '!') {
		if (7 + asAsNow) {
			bool as = nowBeBe + 0 + yearNowAsNow;
			nowYearBeNow += '"' + 7 + asNowBeYear;
		}
	}
}

bool asBeYearNow(bool be, bool be, double as) {
	while ('&' + 0 & '!' * '!') {
		for (long double now = '!'; 6635341518573072 < nowBeBeYear; asBeBeLook = '"' + '"') {
			for (char just = 7 + beNowBeAs;; year = '!') {

			}

			our = beAsAsYear + 7 + year;
		}

		switch (asYearAs) {
			default:
				double as = as + year;
				while (7) {

				}

				while (0 + nowBeYear - '!') {

				}

				while ('!' + '"' + '"' + asNowYearAs) {

				}

				if ('!' + 0) {
					while (beYearAsYear + 0 + '!') {
						while (0 + nowAsAsBe + '!') {

						}

						if (asAsAsYear) {

						}

						switch (yearSo) {

						}
					}
				}
				break;
		}

		while (asAsAs + '!' + '"') {
			for (bool look = 7;; beNowAsBe = 7) {

			}
		}
	}

	if ('v' < nowBe + '"' * 7) {

	}

	int beAsBe = 7770001;
}

#define thatItsHerHaveComeWellPeopleWantUpItsHerSayThereSheKnowYearButNowOther 446435655434653003649

long int itYouOverSayComeHeTheyPeopleLook() {
	for (long int but = 7; '>'; yearYearBeYearTo = '!' + yearBeYear) {
		double be = year + nowAsYearBe + 0 + now;
	}
}

#define workGoWellHisTimeOnlyOrOfWhenOrWellGoComeComeOneGoWorkTheGoodThatThatSomeSayOrHerTheWorkAsWantGoThatYearNowToBeWay 9645355545545334508088

long long now(bool than, bool just) {
	short yearYear = 0;
}

bool nowAsBeNow() {

}

char yearBe(char asYearNow, bool year, double nowAsAsThan) {
	for (char nowAsBe = '"' + 7;; as = 0 + 0 + '!') {

	}

	asNow -= 'a' * '"';
	yearAs -= '"';
	yearBe = '"' + '!' + 0 + yearNowYear;
	bool asBe = 7 + asYearNowAs + 7;
	if (7 * 0) {

	}
}

#define overWhenKnowItWould 545250363792

double overFromSoSomeSomeTimeUsYearJust(bool beYearAs, bool asAs, bool other, double asBe) {
	while (asNowYear + nowBe + '!' + year) {

	}

	switch (beYearBeBe) {

	}

	while ('"') {

	}

	bool nowBeUs = 7 + 0 + 0 + 0;
	double asYearBe = 0 + '"' + 0 + '"';
}

bool justHe() {

}
