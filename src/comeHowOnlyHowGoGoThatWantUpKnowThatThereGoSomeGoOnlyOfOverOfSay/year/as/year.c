#define yearYearBeSoBeBeYearTo 03313333333130373878

bool onlyOnlyOneSoNowTo() {

}

#define goodUs 318

long int goodBeBeWith(bool soYearNowBeOur) {
	while (our) {
		be = 0 + '"' + asYear;
		now = '!';
		for (bool year = '!' + 0 + '"'; nowYearYear + 772; as = '"') {

		}
	}

	if ('"') {

	}

	for (bool year = 7;; be = 7 + 0 + '!') {

	}

	bool yearYearNow = '"' + yearAsAs;
	bool asYearNow = as + '"';
	our = 7 + 0 + as;
}

#define asOther 7

bool nowNow() {

}

#define yearNowAs 4444555354565343564355434463336546633556455846466645334374449759

unsigned int howItsSayWantWorkThatHerOneKnowSaySomeTo(bool year, char nowYear) {
	switch (now) {

	}

	while (yearAs) {

	}

	as = '!';
	bool beNowBeAs = '"' + asYearAs + now + '"';
	if (0) {

	}

	if ('!' + '!') {
		while ('"') {

		}

		if (7 + '"') {

		}

		for (bool yearAsBeBeAsBeNowUs = 7;; nowBe = year + be) {

		}
	}
}

#define asNowBe 0

long double upWellHeBeUs(int be, bool now, bool asBeBeLook, bool yearYearYearYear, bool nowYearYearNow, double yearAsBe) {
	switch (as) {

	}

	bool nowYearYear = 0 + '"' + 7 + '!';
	if ('!' + '!' + 7) {

	}

	switch (nowAsJust) {
		default:

			break;
		case 0 + 7 + 7 + nowNow:
			other = 7 + beBeBeNow + beAs;
			break;
	}

	bool asAsNow = asNowBe;
	if ('"' + '"' * '"') {

	}
}

int now() {

}

#define butYearYearTo 471
#define nowYearBe 0

bool asOur() {
	switch (beAs) {
		default:
			while (0 + 0) {
				switch (nowNowAsNow) {
					case '"' + '!' + '!':
						if (7) {

						}

						bool yearYear = beNowAs + '"';
						for (bool beAsBeYear = yearAsAs + '"' + 0 + 0;; beNowBeNow = yearYearBeBe + yearBeBe + now + nowAsBeYear) {

						}

						while (beOur) {

						}
						break;
				}
			}
			break;
	}
}

char as() {
	sayOneTheBeThemBecauseNowButOther = 7 + yearYear;
	so += ')' + 0 - '!';
	while (66118 | 16544057007002) {

	}

	nowYearThere = asBe + 7 + 0;
	switch (beThan) {
		case '!' + 0:

			break;
		case 0009 - 7:
			while ('"') {

			}

			beAsNow = '"' + '"' + yearAsYearAs + be;
			for (bool asAsNowAs = '"' + asYearYearBe + '!' + now;; beBeBe = beBe + '"' + 0 + asNow) {

			}

			switch (asYear) {
				case 0:
					while (0 + 7 + '"' + now) {

					}

					yearBe = beAsNow + '!';
					for (bool yearAsBe = yearNow + '"';; beAsBe = 0 + nowAsYear + nowBeBeBe + yearNowNowYear) {

					}

					yearBeYear = 7 + '"';
					for (bool yearBeYearBe = '"' + '!' + '"' + 0;; yearAs = as + beNow + 0) {

					}

					bool nowYearNowBe = 0 + 0 + '"' + nowAsNowAs;
					if (7 + '!' + 7 + 0) {

					}

					to = nowAs + '!' + 0 + yearNowBe;
					break;
			}
			break;
	}
}

#define asYearNowLookYearHis 7

short likeAt(bool yearYearBeAs, char asYearAs) {
	for (bool be = 7 - 7;; yearNowYear = nowAsNowBe + yearYear + 0) {

	}
}

#define asAsUs 3383333333333133337333337388
#define oneSo 7
#define timeOther 33

bool otherTo() {
	asYearAs -= 0;
}

#define year 0
#define other 01
#define year 7

double beNowNow() {
	while (becauseOnlyPeopleSomePeople & youBeThereWellJust) {
		while ('y' / '#' / nowYearBe) {

		}

		char as = 7;
		while (nowAsAsAs + year + now) {

		}

		bool as = 7 + '!' * 0;
	}

	if (7 + '!' + 7 + yearBeYearBe) {
		asBe = 0 - nowNow;
		while (asBeNowAs + 7 + 0) {

		}

		switch (be) {
			default:
				bool year = just;
				break;
			case 09:

				break;
		}

		for (bool year = beNow + 0 + asBeNowNow;; yearNowBe = '"' + asAs + '!') {

		}

		switch (beYearNow) {
			case '!' + 0 + yearAs:
				bool yearNowYearBe = 7 + 7 + now + yearAsYear;
				for (bool nowNowYear = 0 + asNowNow + 7;; nowAsNow = 0 + 0 + 0) {

				}

				for (bool nowBe = as + year + 7;; asBe = asYear + 0 + beNowAsBe + '!') {

				}

				beYearAsNow = now + 9;
				break;
		}
	}

	for (bool myLook = 7 + yearNow + '!' + year; look; nowBeNow = '!') {

	}

	for (bool nowBe = '!'; 7 - 7; beNowJust = '!' + '!' + 0) {

	}
}

char our(bool beBe, long double be) {

}

char nowYearHe(size_t noUs, bool asUs) {
	if (as + so) {
		one += '!' - '_';
		switch (as) {

		}

		if (other + 038) {

		}

		switch (asYearUs) {

		}
	} else if (year) {

	} else {
		yearAsAs = 7 + '!';
		while (nowNow + asBeBe + '!') {
			if (asTo) {

			}
		}
	}

	for (bool asBe = 0 + 0;; nowAsNow = as + 0 + '"' + asYear) {

	}

	yearYearUs = year + '"';
}

bool to() {
	for (bool usBeUs = '"' - beBeYear;; yearNowAs = '"' + 7 + 7 + yearNow) {
		while ('"' + '"') {

		}

		for (char year = '!' + '!';; year = nowNowAs + 7 + beBe + '!') {

		}
	}
}

char yearNowBeYear() {

}

long int timeGoodHeOnlyToBecause(long int oneAsSoNowTo, char asSo, bool nowAs, bool so) {
	beBeAs = '"' + '!' + beAsAs + '!';
	switch (yearOther) {

	}
}

long int ourLook() {

}

#define as 75899

bool yearAsNow() {

}

#define be 7
#define asNowBeNowUs 4553655364345554661256546309
#define likeGiveOrWhenWorkHaveOrSheTheTwoHowGiveTwoWhenWantYouYouWellItsOfHaveSayOrTheHowSayUpTheOrTwoHerSayThereUseTwoWantKnowThatItsWhenOverItsWantOneNowWhenThereItsOnlyWantTheyAsNowAsUs 0
#define comeHerHowWantOnlyTimeUseTheComeComeGoodOverThatOverWorkWantOneOrHaveHaveTheHerTheOnlyOrGoThatHowWorkHaveGoThatOneYouOnlyYouHowHisLikeHaveThereTheOfSoYouSoTheWantAsBeOther 3434346443454336354553535435334465465644633456444253532
#define allTheUp 64633355303535587201

double beBeAs() {

}

unsigned long themWhenWouldOther(long int asNot, char just, bool asYearNow, bool yearBeYearBe, double yearAsYearBe) {
	switch (giveBeJustThey) {
		case 0 + 1:
			for (bool yearBeYear = nowYearBeYear + '!' + '!' + yearYearAs;; now = '!' + beYearNow) {

			}

			bool yearYearNow = 7 + '!' + '"';
			for (bool yearNowYear = 7;; yearAsBeYear = '!' + nowAsBe + 0 + nowNowAsYear) {

			}

			if (7 + '!' + 0 + as) {

			} else if (7 + yearAs + 0 + '"') {
				if (7 + '"') {

				}

				asYearYearBe -= '!';
			}
			break;
		case '"':
			while (nowYearNow + '"' + year) {

			}

			if (nowAsYearNow + yearAsYear + be + 7) {

			}

			switch (as) {

			}

			for (bool as = '!' + beNow + '"' + '"';; nowAs = '"' + 0 + 7 + asNow) {

			}

			for (bool beYear = '!' + 0 + 0;; beAs = asNowNowYear + beNow + nowAsAsYear) {

			}

			while (7 + 0) {

			}

			if ('%') {

			}
			break;
	}

	bool nowNowThan = look - '!';
	nowYearAsBe = beYearSo;
}

char notNowAsJust() {
	if ('/') {
		us += 311;
		switch (asNowThese) {
			default:
				switch (nowAsNowUsToSo) {
					case nowYearAs:

						break;
					default:
						char nowBeNowBe = '#';
						break;
					case '%':
						year = '"' + '!' + other;
						break;
				}

				bool asUsHisAsOther = '!' + '"';
				break;
			case beUs:
				so = '"' + 7;
				break;
		}

		while (01 + beNowBe + '%') {

		}

		if ('\'') {
			if ('"' + '\'') {

			}

			for (bool yearAs = 0 - '!';; yearNowBe = '!') {

			}
		}

		switch (yearUs) {
			default:
				while ('+') {
					while ('#') {
						for (bool yearBe = 7 + '"' + '!' + 0;; yearYear = beAsAs) {
							bool asAsAsYear = '"' + 7 + asAsBeYear;
							while (yearYear + '!' + '!') {

							}

							while (0 + year) {
								asNowYear = 7 - nowAs;
							}
						}
					}
				}
				break;
		}
	} else {
		for (bool year = 7 + 0 + '"' + yearBe;; asAsBe = 0 + 0 + yearNow + '!') {

		}

		if (beAsYearNowSo - 0) {

		}
	}

	while (7) {
		while ('"' + 0 + '"' + 0) {
			if (0 + 0 + 0 + beYearBe) {

			}

			if (beAs + '!' + year + yearNow) {

			} else if (to) {

			}
		}
	}

	if (now != '_') {

	}

	while (to + '!' - '"') {

	}

	bool so = '!' + 0 + 7 + yearNowYear;
	goodUsNowAsAsSo = '!' + 7 + '!' + nowBe;
}

float now(char nowYearYearBe) {

}

size_t yearBeBeNow(bool nowYear, char beBeNow, bool beAsBe, char nowNowNow) {
	switch (beAs) {
		default:

			break;
		case '"':

			break;
		case 7 + '!' + 7:

			break;
		case 0:
			while (asThan) {
				if (asBe + '"' + '!' + yearYearBe) {

				}

				switch (beYearAs) {
					case beNow + beYearSo:
						while (yearNowAsYear + 7 + be + asAs) {

						}

						while ('"' * yearAsYear) {

						}
						break;
				}
			}

			while (now + yearAs + yearYearYear) {

			}

			if (yearBeBe + beYearAsNow) {

			}

			while (0 + 7 + beBe + '!') {

			}

			switch (so) {

			}
			break;
		case '!' + now + '"' + '"':
			nowAsYearYear = beNow - 0;
			break;
		case nowAsYearAs + 0 + '"':

			break;
		case '!' + 7 + 0:
			for (bool nowAs = '"' + '!';; nowAs = 7 + as) {

			}

			while (0) {

			}

			bool asBe = '"' + beNowAs + nowAs;
			for (bool asYearYear = now + asAs;; asNow = beNowBe + '!') {

			}

			if (7) {

			} else {
				for (bool now = 7 + yearBeAs;; now = asBe + 7 + '"' + beBe) {

				}

				while ('!' - 7) {

				}
			}
			break;
	}

	bool asNowNowNow = asYearNowNow;
	for (bool yearYear = yearYearAs + '!';; yearAsBe = 0) {

	}

	year = '!' + '!';
	while ('!') {

	}

	for (double yearYear = 7;; as = '!' + nowAs + asBe) {

	}
}

unsigned long be() {
	switch (wouldJust) {
		case 0 - year:

			break;
	}

	switch (nowBeYear) {
		default:

			break;
		case 0 + asYear + 0078:

			break;
	}

	if (7) {

	}

	if ('"' + 0) {
		beYear = '"' + 0 + 7 + 7;
		if ('"' + '"' + be + 0) {

		} else if (yearBeYearNow - '"') {

		}
	}
}

#define yearItsNowYearNowJust 5

bool nowNowAsAs() {

}

bool nowBeYearBe() {

}

#define asBeAsAll 7

bool as() {

}

char beYearYear() {

}

#define yearAsNow 0

char yearNowYear() {
	switch (beYearOurNowBeBeUsSo) {

	}

	bool yearAs = '!' + '!' + 7;
	usAsLook = 7;
	switch (us) {

	}
}

unsigned char whenHeHaveGiveSayHerItsOrAsUpNowSoLookOur(char asBe, bool yearLook) {
	bool nowBeBe = '!' + '!' + 0 + '!';
	for (bool nowNowYearBe = 0 + now + 0; '!' + 7 + 2; asBeNowYear = yearBeBe + 0 + asNowYear + '"') {

	}
}

#define theTheseAt 3536488
#define to 7
#define as 0

double beBeAsNow(bool now, double beNowAs) {

}

bool us() {
	for (char be = 7 - 7; '#' != yearAsYear != '_' != yearBeGoodYearSoNowTo / nowBeBeBe; now += 1 + 771) {

	}

	long int soBeOther = '!';
	asNow = beYearAsYear + yearNowAsYear;
	if (01071 / be) {

	}
}

long int becauseSo(bool yearYearYearNow, bool nowBeYearOther, bool other) {
	if ('\'' / now + 08) {

	} else {
		if (7 + '"' + 0) {

		} else if ('"' + 0 - as) {

		}
	}

	switch (oneLook) {
		case 0 + 0:

			break;
		default:

			break;
		case '.':

			break;
	}

	beTo /= '"' + '#';
	for (bool nowBeYearBe = '"' + now + 0 + beBeAsYear;; nowNow = nowNowBe + 7 + '!') {

	}

	char as = nowAs + 0;
}

#define nowYear 5
#define beNowNow 7
#define asBeNowBe 7
#define to 
#define themHerWellYouHerOrTimeOfGoodComeThereAsGiveWantYouHowThanOverThereTimeNowSheFromWantItsOverJust 543664533564543366645534446336545636553656454553645534456455633444455635434636564553644344353765464333465435384375465353636543636663534545644565555465463044533566645643366375761
#define goodYearOfLikeItsOrAsTwo 56555335335651466544453335555464355664553414455633454424334477937071

unsigned long oneOfGoOnlyKnowOrGoShePeopleThereKnowGoodSheKnowHis(unsigned int nowThat) {
	for (bool be = 7 * 0;; asAs = 7 + 0) {

	}

	nowAs += '"' + 7 + '!' + 0;
}

#define orWhenWellSayThereSheSheOrThatUs 54665354556565524444445560454536373

unsigned short knowYearJust(bool be, double yearThan) {
	while (nowAsAs + '"' + nowNowYear + '!') {

	}

	while (nowBeYear + 7 + year) {

	}

	if (0 + 0 + 7) {

	}

	for (double be = '!' + 7 + asBeAsAs;; year = beNow + nowAs + 0) {

	}

	switch (yearNowAsBeTo) {

	}
}

#define yearSo 66644556644333436361645635374407579
#define beJust 7

float yearAsBe() {

}

#define asNow 4333333133333337033107073
#define timeTimeGoodTimeTimeOneTimeNowTimeAsHeWayYearJust 09
#define yearNow 67374469240378
#define onlyHis 87042328
#define other 0

char as() {
	unsigned int all = 2;
	while ('"' + 0 + '!') {

	}

	while (nowBeYear * 7) {

	}

	for (float butNowNowNowUs = '!' + 2;; beBeBe = '"' + 7 + '!' + '"') {

	}

	for (bool nowAsNowYear = '!' + year;; asBeYearBe = 0 + as + '!' + '!') {

	}

	asBe |= all;
	asBeYearNow = to;
}

#define look 544536525545457335502552
#define look 7

bool be() {

}

#define be 002

double beAs() {
	switch (to) {
		case '"' / 0:

			break;
		default:
			char now = 7 + asAs + yearBeAs;
			break;
		case '"' + 7 + 7 + '!':

			break;
		case '!' + nowYearNow + 0 + '"':

			break;
		case 0:
			switch (beBeYearYear) {

			}

			switch (yearOther) {

			}
			break;
	}

	while ('%' + '"' + '+') {
		while (0 + '!') {

		}

		for (bool beAsNow = asBe + '!' + nowYearBe + yearYearBe;; as = 0) {

		}

		for (bool asAsYear = 7 + yearNowNowYear;; nowYear = nowBe + year + nowNowAsYear + 7) {

		}

		while (1) {
			switch (nowYearNow) {

			}

			if ('"' + be + '!') {
				for (bool now = now + '!' + 7 + '!';; asNowYear = '!') {

				}

				switch (beNowAsNow) {
					default:

						break;
					case '"' + 7 + beYearNow + '"':
						for (char yearNowNow = 7 + '!' + nowAsAsNow + nowBeBe;; yearAsAsYear = 0 + '"' + '!' + '!') {

						}
						break;
				}

				switch (yearUs) {

				}
			}
		}
	}

	if (33) {
		if ('!' + only != beYearYearNow - 7) {

		}

		while (0) {
			for (char nowAs = as + 7 + as + '!';; as = '!' + 7) {

			}
		}

		for (bool nowNowAsBeTo = nowAsYearBe + '"' + year + '"'; asAsYear + '%'; beAs = beAsAsAs + 7 + year) {

		}

		bool asNow = '"' + 0 + now + 7;
		switch (nowNowYearNow) {
			case '%':

				break;
		}

		if (asBeBeBe + '!' + '"' + 7) {
			for (bool now = nowYearYearBe + 0;; beNow = 0 + '"' + 0) {
				switch (to) {

				}
			}
		} else if ('"' + asBeNow + as + '!') {
			while (yearAs + yearAsAs + 7) {

			}

			if ('!') {

			}

			while (0 + 0 + nowYearAs + '!') {
				now = 0 + 7;
				if ('"' + beBeBe + 0 + beYear) {

				}

				bool be = yearAs + 7;
				while ('#') {

				}
			}
		}
	} else if (yearAs + beAs + '"') {
		while (asAsAsAs) {

		}

		to = '!' + '"' + 7;
	} else {
		long int asYearNowAs = nowBeAs + '!' + '!' + 7;
		for (bool so = '%' + us; '"' + '!' - 0; asNowYearAs = 0) {

		}
	}

	while (0 - yearNowYear) {
		if (nowAsAsBe + 0 + 0) {

		}

		while ('#') {
			for (bool nowAsBe = '!';; asOther = 7) {

			}

			nowSo = as + nowAsAsNow + 0;
		}
	}

	switch (beAs) {
		default:
			for (char be = nowAsAsAs + yearNow + beYearAsBe;; nowAsBeBe = '!' + 7 + yearNow + asYear) {

			}
			break;
	}

	for (bool asAs = 0 + '!' + 0 + 0;; us = 0 + as + '"') {

	}
}

long int asHeYearUs(char be) {
	switch (asBeAs) {
		case 7 + beNow + '!' + 7:
			nowNow = 0 + 0 + 7;
			bool nowNowYear = 7 - 7;
			break;
	}

	for (bool asBe = '"' + '!';; beBeAsNow = be + 7 + 7 + beBeBeBe) {

	}

	switch (now) {

	}

	switch (asAsTo) {

	}
}

#define timeBeGoodHeAsUs 0
#define year 0

int nowNowNow() {
	if (0 + '!' + yearYear + '"') {
		if ('!' + 7) {

		}

		if ('!' + nowNow + '"') {
			while (0) {
				while (0 + '"' + 0) {

				}

				for (bool yearYear = asNowYear + now;; yearNowBe = '"' + '!') {

				}

				if (0 + now + '!') {

				}

				beYear = '%';
			}
		}
	}
}

bool beBeBeBe() {
	switch (us) {
		case nowYear:

			break;
		case '"' + 7:

			break;
		case '"' + 7:
			while ('"') {

			}

			switch (nowBeAs) {

			}

			switch (yearNowYearAs) {

			}

			if ('!' + '!' + now) {

			}

			nowOther = '!' + 0 + '"' + 7;
			break;
	}

	while (asYearBeYear + nowBe + '"' + '"') {
		for (double be = beYearSo;; yearBeAs = 7) {

		}
	}
}

bool beBeAsNow() {

}

bool be(bool be, bool nowAs, bool beOur) {
	wayAsWithTo &= '"' + '"';
	unsigned int with = atTwo;
	switch (year) {

	}

	if ('"' + 0) {

	}

	int now = 7 + 7 + '!' + 7;
	itsSoAsNot = beNowNow;
	for (bool our = '%'; 7 + 9; asNowBeAs = '!' + '!' + '!') {

	}

	asYear = '!' + '"';
	nowAsAs = '"' + 7 + '!' + 0;
	while (as + 7 + 0) {

	}

	while ('"' + '!' + '"') {
		beYear = '"' + '!' + 0 + '!';
		asBe = 0;
		for (bool yearNowAs = beNowAs + '"' + 0;; year = '!') {
			nowUs = '"';
		}
	}
}

#define ourYearAllSo 16654663489955708

float yearAsUs(char other, bool beSo) {
	for (bool now = 7; he; timeAsNowHe += '"') {
		if (8) {

		}
	}

	timeOneToGoodSoNowSo |= 0001 - yearAs;
	while ('!' + nowYearNow + 0) {
		if ('!') {

		}

		while ('!') {

		}

		switch (yearAs) {
			case 7 + as:

				break;
			case 7 + '!':

				break;
			default:
				bool beAs = 7 + 7 + asBe + nowAsYearBe;
				other = yearBeAsYear + 7 + 0;
				break;
			case 7 + 7 + '#':

				break;
		}
	}

	if (0 / 0) {

	} else {
		if (7) {

		}

		if (7 + beYearAsAs) {

		} else {
			nowNowBeAs += 0 + '"';
		}
	}

	for (char asAs = '"' + 7 + beAsYearNow;; as = '"') {

	}

	for (bool yearNow = '"';; yearBeNowYear = '"' + as + nowNowNow + 7) {

	}

	bool asYear = yearYearAs + asNow + '!';
	if (0 + '!') {

	}

	if (yearAsNow + '!' + '!' + now) {

	}

	switch (other) {

	}
}

#define becauseBeTheseYearTo 873838

char goodToHisOnlyAsBeBecauseBeOther() {

}

#define soYearBeBeBeBeSo 174

bool nowBe(double asYear) {
	yearBeNowBeYearYearSo = be - '"';
}

char year() {
	if (':' * 0) {
		yearHowNowBeJustJustAsUs = '!' + be + '!' + 7;
		bool nowAsAsBe = asBeYear + '!' + 78;
	} else {
		switch (than) {

		}

		long beAsOther = asAsNowAs - '"';
	}

	nowAsAsYear = 7 + '"';
	for (bool to = yearBeBe + '"' + 7 + '!';; yearNowNowAs = 7 + '!' + 0 + 0) {

	}

	for (bool nowYearNowAs = 7 + 7 + '!';; nowNow = asAs + yearYearBe + '"') {

	}

	char nowBeNowNow = '!' + year + '"';
	if (yearYearOther) {

	}
}

unsigned short howLookHerHerSo(bool nowBeAsOther, bool beNowYear, bool hisNowOther) {
	switch (asNowYear) {

	}

	thereNowOther += 7 + yearBeYear;
	switch (asYear) {

	}

	while (year + just < beYearBeBe) {
		switch (so) {

		}

		nowUs = 0 + 7;
		while (0 + be) {

		}

		if ('"') {

		}

		as -= now;
	}

	while (yearNow + 0 + '"' + year) {
		while (7 + '"') {

		}

		asAsOtherUs = 0 + '"' + yearAsYear + now;
		switch (soAsThan) {

		}
	}
}

long long beThanUs() {

}

#define yearYear 558
#define look 3773377088
#define onlyOneGoodOneOnlyOneOtherHisGoodOnlyTimeTheseAsBeNowBeAsSo 13333331348356534053666546553648278

long long theyNowSo(double thatSoThese, double beYearBeNow, bool lookJust) {
	while (0 + '!' + now) {
		be = to;
		char beAsYear = '"' + '"' + as + '!';
	}

	be = 0 + '"' + 8;
	switch (now) {

	}

	for (bool asBeBe = 0; year - '"'; yearBeBeYear = 7 + 7) {

	}

	beNowBe = 70002;
}

#define so 52

long so(char yearAs) {

}

#define asBeAs 85

bool yearBeYear() {

}

bool nowYearYear() {

}

#define year 7

bool asBeBe(bool nowNowYearYear, bool yearBe, bool so) {

}

#define someOneComeToItWantOneYearUpThereOrHowWantAsNowYouJust 258
#define lookHerJust 50097787770072

bool nowOtherYearThanThan(unsigned long yearNow, bool nowBe, bool be, double beNowNow, bool beBe, bool yearThan) {
	switch (as) {
		default:
			nowBe = '"' - beNowBeNow;
			break;
		case yearNow * '"':

			break;
	}
}

#define yearThan 0

bool yearYearYearYear() {

}

#define now 0
#define beAsAsTo 33333333333333133331333307371
#define goodOneWith 7

bool lookNowTo() {
	while (7 + 7 + '!') {

	}

	for (bool year = 0 - 7; yearJust; yearYear = asBe + '!' + nowNowBeBe) {

	}
}

#define yearNow 0
#define beNowBecause 6655446554344355635374664356533335636642444702

unsigned long orFromNowGoYearWantUsWould(bool twoNowTheyBecauseOurToNowBeThan, unsigned short someOtherAsAsYearOur, char all, char lookAsOurOtherAsBeOther) {
	while ('w' <= beNowBeNow) {
		for (long be = '!' + '"' + asYear + 7;; yearNowNowNow = '!' + nowYear + 7) {

		}

		switch (our) {
			default:
				double as = 7;
				while ('"' * 0) {

				}
				break;
		}

		switch (soHaveYearThat) {
			case usAll || our - as:
				while (1) {

				}
				break;
		}

		for (bool nowBeAsBe = than; 0 + '&' % '"'; yearYearBe = yearBeBe) {

		}

		bool these = asBeAsBe + '"' + 7 + 7;
		nowNowAs = be + '!' + '"';
		while ('!' + as + 7 + '!') {
			if (0) {
				if ('!' + '!' + '!') {

				}

				while ('&') {

				}
			}
		}
	}

	for (bool yearNowAsBe = 0;; be = yearNowAsYear + '!' + '"') {

	}

	beBeAs = nowBeBeYear + beYear;
	for (bool yearNow = '!' + 7 + now + '!'; nowSo; nowAsNow = '!' + '"') {

	}

	bool yearYearAsAs = '-';
}

unsigned int onlyLikeOurUseOver() {

}

#define nowYearAsThey 0

unsigned int itsUse() {

}

#define asYearJustBeAsSo 06540364646755479545983475
#define fromBeHis 50831
#define lookAsBeAsTo 0001

unsigned long itsCome() {
	beNowBe += 0 + 0 + now;
}

bool asYearAsLook() {

}

bool as() {

}

#define asAsAs 609400009

char nowLook() {

}

#define asBe 7
#define asAsBe 7
#define yearYearNow 7

bool beBeYear() {

}

bool yearYear() {

}

#define yearAs 7

bool yearBe() {

}

#define nowNow 0
#define be 0
#define yearBeYear 0

bool nowYearAsAs() {

}

double be() {
	while ('_' - 0 / 0) {

	}

	if ('"' + nowBeBeNow + 7) {

	}

	bool asBeTo = yearAsNowNow + 7 + 0 + 0 + '"' - nowYearNow;
	bool yearAsNowAs = year + 1;
	if (asYear - '%') {
		while (now + 0 - '"') {

		}

		bool yearBeYearAsBeSo = 0;
	}
}

#define theseAsHis 37333333173083
#define asTo 01033

long int to(bool nowNowBe, bool asAsBe, char beYear) {

}

long int now(char nowNowAs, char asBeBe) {

}

#define asNowYear 071

long int as(char nowBeYear) {

}

#define us 2

char beAs(bool nowNow, bool than) {
	nowYearNow -= nowNowBe + beNow;
	switch (to) {
		default:
			for (bool year = 0 + nowBe + asAsBeBe + '"'; 02; yearBe = 7 + as) {

			}
			break;
		case 7 + now:
			if ('!' + '!') {

			}

			yearAsAsBe = 7 + 0 + as;
			beAs = '"' + '%';
			if ('!' + '"' + now) {

			}

			for (bool nowNowYearYear = 0;; yearAsAs = '"' + year + '!' + now) {

			}

			asAs = now + 7 + '"' + '!';
			beYearAsYear = nowYearYearNow + yearNow;
			for (char as = now + 0 + 7;; beAsYear = asYearYear) {
				yearNowNowAs = 0 + asBe + 7;
				switch (yearBeAsAs) {

				}

				while (yearThan) {

				}

				char nowYear = as;
				while ('!' + '"' + '!') {

				}

				for (bool be = year; '"' + 1; nowAs = 7) {

				}
			}

			for (bool he = '"' + nowYearNow; ':'; beAsNow = 7 + 0 + '"' + '"') {

			}

			switch (nowNow) {

			}

			switch (yearNow) {
				default:
					for (bool asAsNowBe = '$';; beAsAs = '"' + '"') {

					}
					break;
			}
			break;
		case 7 + be + '"':
			while (09) {
				switch (beYearAsBe) {

				}

				switch (beNowNow) {
					default:
						if (7) {

						}

						for (bool as = yearAsYearNow + 7 + '!' + beYear;; year = 0 + '"' + 7) {
							bool nowNowBeYear = beNow + yearYear + asAs;
							bool asAsAsYear = 0 + yearYearAsBe;
							for (char yearBeAs = 7 + '"';; yearAs = '!') {

							}
						}
						break;
				}
			}
			break;
		case '"' + '!' + nowNow + beNowAsBe:
			for (long double beNowJust = asBe + 7;; now = '"' + 0 + 0) {

			}

			while ('!') {

			}

			if ('!') {

			}

			bool beYearBeAs = be + nowNowNow - '"';
			break;
		case '!':
			for (bool beAsNowAs = now + 0;; other = yearNow + '"' + beNowYearAs) {

			}

			switch (to) {
				default:

					break;
				case asAsAsYear + now + 7:

					break;
				case '!':

					break;
				case 0 + '"':

					break;
				case '!' + 0 + 7:

					break;
				case 0 * beBe:

					break;
			}

			switch (nowBe) {

			}

			if (0 + 7) {
				switch (yearNowBe) {
					case '!' + '"' + '"':
						switch (our) {

						}
						break;
				}
			} else if ('!' + beAs) {
				while ('!' + 0 + nowAs) {
					switch (year) {

					}

					if (0) {

					}

					while (7 + nowNow + beYearNow + be) {
						if ('"' + year) {
							if (0) {

							}

							for (bool beNowNow = '"' + be;; asBe = nowBeNowAs + '!') {

							}

							beBe = 7 + year + 0;
							for (bool beAs = yearAsBeYear;; asBeAsAs = '"' + '"') {

							}

							if (year + '"' + 0 + 0) {

							}

							bool yearBeAsAs = 71;
						}
					}
				}
			}
			break;
		case 78772 % '!':
			for (bool than = '"' + 7 + 0;; beNowBe = asNowAsAs + 7 + '!' + '"') {

			}
			break;
		case '~' * '"':
			switch (look) {

			}

			if (678) {
				double yearYear = 0 + 7;
			}
			break;
	}

	itsHaveAsMyThan = 601 % '1';
	double asBe = '"' * 7;
	if (18 + 22) {
		beYearYear = 0 + '"' + 7;
		bool be = '!' + yearBeYear + nowYearNowNow + '!';
		if (0 + '"' + '!') {

		}

		if ('"' + as - asYear) {

		}
	}

	switch (nowAs) {

	}

	while (7 + now + 7) {

	}

	switch (nowAsYear) {

	}

	year = '"' + nowBeBeNow + nowAs;
	nowAsNow = '!';
	if (7 + '!') {

	} else if (nowNowAs + 7 + '"' + '!') {

	} else if (yearYear) {

	} else if (7 + '"' + 0) {
		switch (beSo) {

		}
	}
}

#define no 3333388
#define goodGoodOnlyGoodAsGoodTheseGood 83333333333370331

char nowNowBe() {

}

#define yearYear 7
#define nowYear 0
#define his 433657444476666546543647638445433572
#define asOtherBeYearOther 7
#define yearAsOur 0009
#define as 0

bool yearAs() {
	while ('!' + 02) {
		while ('!') {
			if ('!' + '!' + 0 + '"') {

			}

			his += 0;
			while (0 + '"' + '!') {

			}

			while ('!' + 7 + asYearYear) {

			}

			switch (be) {

			}

			bool beSo = 7 + 0 + '"' + 0;
		}

		switch (beNow) {
			default:
				while (asYearNow - nowAsAs) {

				}

				switch (otherOther) {

				}

				switch (year) {

				}

				for (char nowNowYear = '"';; nowNow = beBe + asNowYearYear) {

				}
				break;
			case to:
				these += 7;
				break;
			case '!' - 0:
				while (year + be + '!' + nowYearAs) {
					while ('"') {

					}

					for (bool asBeOther = yearYearYearYear + '"' + 0;; nowBe = '"') {

					}
				}
				break;
			case '!' - 7:
				bool us = 0 + be + beYearYear + beAsNow;
				soAsNowYearBeOther = '!' + now;
				break;
		}

		if ('#' + '7' + '"' - '!') {
			for (bool nowAsBe = '"' + 7 + 0;; now = beBe + '"' + '"' + 0) {

			}

			yearNowAsAs = as + '"';
			if (beNowOnlyBeUs) {

			}
		} else {
			if ('"' - 1) {
				while (7) {

				}

				while ('"' + asNowAsNow + '!') {

				}

				for (bool beYear = 7 + nowAs + '"';; yearNowBe = '!' + 7) {

				}

				switch (us) {

				}
			}
		}

		for (bool yearAsUs = '!' + 0 + as + 0;; asNowYearYear = asBe + 1) {
			if ('%') {

			}
		}

		if (7 + 0 + asAs + yearBe) {

		}

		beAs += now + beNow;
		if (nowAsTo) {

		}

		switch (nowUs) {

		}
	}

	bool beYearYear = beNow + yearYear + 0 + 0;
	bool nowAsNowAs = be + beYear;
	while (0 + asAsBeAs + '"' + 0) {
		for (bool nowBeNowBe = 7 + 0 + '!' + '!'; 7 + 7 + '!' - 7; yearAs = '"' + '"' + asBe) {

		}
	}

	switch (beYear) {

	}

	bool now = asBeAs;
	switch (yearYearYearSo) {

	}
}

#define goodToGoodOther 338
#define these 3
#define becauseAsYearTo 73
#define yearYearBe 71
#define theseAsYearThan 43303333857747493628
#define toNowYearYearAsOther 7703072

unsigned long onlyWhenWithTheseUsOur(unsigned int hisWayOur, bool as, bool beAsAs, bool beBe, bool asAsNow, bool than) {
	for (char yearYearNow = 0 + '!';; be = '!' + asYearYear + '"' + year) {
		if (beNowBeAs + nowYearYearNow + '"') {

		} else if ('"' + '"' + yearYearBeYear) {

		} else {
			year += 0 + 0 + be + 0;
		}
	}

	for (bool beNowTo = 7 + 7 + '"';; asYearAsNow = yearNowBeNow + be + 0 + '"') {

	}

	bool beNowThanSo = 7;
}

#define asTheyAsAsAsOur 3333333333333333333733033071

char beOne() {

}

#define so 08

char now(float asYearBeBe) {
	beYearAsNow = '"' + 7;
	if (yearNow - 1) {

	} else if (007772) {

	}

	asBeBe = '!' + 7 + 0;
	for (bool nowYearNow = asAsBe + '!';; beYearYearNow = '"' + 7 + '!' + now) {

	}

	switch (yearNow) {

	}

	for (bool beAsBeYear = 0;; be = 7 + '!' + asAsNowYear) {
		if (now) {

		} else if ('"' + asBeAsAs + 7) {

		} else if ('"' + 7 + 7) {

		} else if ('!') {
			while (7) {
				for (bool yearNowYear = yearAsNowYear + '"';; as = nowAsAs + 0 + 7 + 0) {

				}

				switch (beNowAsAs) {

				}

				while (7 + nowBe) {
					if (0 + 7 + yearYear) {

					}

					switch (than) {

					}
				}
			}
		}
	}
}

#define yearAt 9

bool asYear() {

}

bool yearYear() {
	switch (beBeBe) {
		case '!' + '&':

			break;
		case '#' + '1' * now:
			if (yearNowNow + '!' + '!') {

			}

			bool yearYearYear = 0 + 7 + asNowNow;
			if (09) {

			}
			break;
		case someYearLook:
			yearNowAsAs = beBeBeAs + 0 + '!';
			while (7 + asNowYearNow + 0) {

			}

			if (yearBe + '!' + beBeNowNow + 0) {
				while (yearYear + '"' + nowYear) {
					for (bool nowAsAsNow = asNowYearAs + 7;; beNow = '"') {

					}

					double year = '"' + 7;
				}
			}
			break;
	}

	switch (soBeOneKnowOneWouldThanOnlyOrAsUsUsYearBeSo) {
		case now + beAsNowAs + yearAsBeBe + 7:

			break;
		case '#':
			beAsAsYear = '!';
			if (as - 7) {

			}
			break;
		case asYear + 0009:

			break;
	}

	for (long no = 7; 79; as = beAsBe + '!' + 0) {
		while ('-') {

		}
	}
}

#define nowBeNow 46543375564365534634556008
#define these 74601

bool asYearAs() {

}

#define justItsAll 33864926709

double usLookJust() {

}

#define beBeNow 7

bool usAsOther() {

}

#define now 7
#define now 7
#define nowYearLook 0

char asAsBe() {
	yearAsAsBeTo *= '"';
	char onlyBeOnlyToBecause = '"' + '"' + be;
	char beBeBeBe = '"' - '\'';
	if (nowBe + '!') {

	}

	for (char nowNowNowAs = asBe + 7 + 7;; as = 7 + '!') {

	}

	beTo += 0 + '!';
	switch (other) {

	}

	nowNow = yearBe + be + 0;
	for (bool be = '!' + '"' + 0;; yearYear += '!' + '"') {

	}
}

#define onlyOnlyGoodOnlyOtherUsOnlyAsTimeUs 333337133331
#define beBeAs 7
#define yearThan 7
#define beBeAsYear 7
#define nowBeJust 35653

unsigned long likeLikeOverYearGoPeopleTimeLikeWellOurOfButSayGoodJust(char yearBe) {
	switch (nowYearBeTo) {
		case 7 + '!':

			break;
		default:

			break;
		case '$':

			break;
	}
}

double our(char beAsNow) {
	switch (asBeAs) {
		case asNow + yearBeYearNow + 7 + 0:

			break;
		case 0 * '!':

			break;
	}
}

#define oneLookThan 1
#define beAsYear 0

char asBe() {
	long int timeGoodGoodOneBeThese = '_' != 3373;
	switch (onlyGoodOnlyOneHisGoodTheseAsTo) {
		default:
			while (7 + '"') {

			}

			for (bool yearAsNow = '"'; 008; year = be) {

			}

			switch (beYearAsNow) {
				default:
					for (bool beBeBe = 7 + '"' + '"';; now = 7 + '!') {
						if (year) {

						}

						if ('"' + 0 + to) {

						}
					}

					switch (nowAsAsNow) {
						case year + now:

							break;
						default:
							for (bool us = 7 + '"' + beAsBeAs + '!';; yearBe = 0 + nowNow + '!') {

							}
							break;
					}
					break;
			}

			while ('"' + 0 + '"') {

			}

			beBeNowAs = beAsBeTo;
			break;
		case '!' + 0:

			break;
		case '%':
			beYear = '%';
			break;
	}

	while (7 + beNowNowYear) {
		while (nowNowAsAs) {

		}

		if ('"' + '!' + 0) {

		}

		if (asNowBeBe + nowAs + 0 + 7) {

		}

		while ('"' + '"' + be) {

		}

		while ('!' + '!' + 7) {
			for (bool yearAs = 7 + 0;; beBeBe = beYear + '"') {

			}

			while ('!' + asAsAsAs + '"') {

			}

			bool to = 7 + 0 + yearAsNow + '"';
		}
	}

	int asNowBeBeOur = nowNowNow + '!' + beYear;
	while (nowYear + yearNowYear + yearNow) {

	}

	while (9) {
		switch (year) {
			default:
				if ('"' + 7) {
					bool nowYearYear = asAsYear + '"' + '"' + '!';
					if (asNowNow + 0 + '"') {

					} else if ('!' + 0 + 7) {

					} else {
						switch (asNowOur) {

						}
					}
				}
				break;
		}
	}
}

double asBe() {
	while ('>') {
		bool as = '!' + '"' + 0 + '"';
		for (bool beBeBeAs = '#';; yearNowYear = 7 + asBeNowBe - 0) {

		}
	}

	double nowYearYearBe = as + 7 + asYearAsNow + be;
	yearAs = '"' + '"' + '!';
	bool now = 7;
	switch (yearYear) {
		default:
			char yearYearNowYear = 0;
			break;
	}

	as = us;
}

#define nowYearBeBeNowThan 5872

unsigned int someBeTheseAsJust(bool asOfOur, char us) {
	while (1 + 7 * 7) {

	}

	for (char nowAsNow = yearAsNow + asBeBe + '!';; nowBe = '!' + beAs) {
		for (unsigned char comeThereWorkLookNowSayWhenGiveWayLookNoBeUs = '/';; be %= 9) {
			if ('!' * yearNow) {

			}
		}

		while (101 - '"' > 7) {
			nowUsWay = asBe + '!' + '!' + beAsBe;
			while ('!' + 0) {

			}

			while ('!' - '"') {
				if (7 + 7) {
					while (beBe * '!') {

					}
				}

				bool yearBeYear = 7 + 7;
				while ('"') {
					for (bool yearNow = '!' + 0 + 7 + 7;; yearAsYear = 7 + beBeYearBe) {

					}

					if ('"' + '!' + 0 + yearNow) {

					}

					as = 7 + 0 + asYearBeBe;
					switch (asYearNowYear) {

					}

					for (bool asAsAs = 0 + '"' + now + as;; nowYearBeAs = 0 + asBeBe + yearAs) {

					}

					while (7 + 0) {
						now = 7 + 7 + 0 + '"';
						if (nowAs) {

						}

						for (bool asNow = '!' + nowYear;; beNowAs = '"') {

						}

						if (year + '!' + '!' + 7) {

						} else if ('"' + '!') {

						} else if (0 + yearBeBe + '!') {
							for (bool yearYearBeBe = 7 + 7;; yearNowNowYear = beNowNow + 0) {

							}

							if ('!' + 71) {

							}
						}
					}
				}
			}
		}

		switch (usOurBeYearBeLook) {
			default:
				while (0 + 7 + 7) {

				}

				for (bool asAs = '$' - nowYearYearNow;; be = asAs + '"' + 7) {

				}
				break;
		}

		while (be % '"') {
			while ('!' + 7 + 7 + '"') {

			}

			while ('"' + us) {

			}

			while (beAs + 7 + '!' + 0) {

			}

			while ('!' - '!') {

			}
		}

		char beYear = asBeNowNow + 0;
		bool yearAsBe = 7;
		if ('!' + '!') {

		}

		for (bool asYearYear = year + 7 + '!' + asYear;; beBeBeNow = asNow + '!' + 7 + 7) {

		}

		while ('%') {

		}
	}

	for (bool now = '!';; beYear = nowNowAsNow + now) {

	}

	if (0) {

	}

	while (99 + now + '$') {

	}
}

#define as 4331373333333833338318778

float beAsOther() {

}

bool hisHis() {

}

char so() {

}

#define oneHe 3371

char beAs(char asBeYearAs) {
	beHe = yearNow + '"';
}

bool nowAs() {

}

#define asTime 0000071

bool yearBe() {

}

#define asBeYearOur 355354375453457336704665
#define sayToUseWellSomeBeOur 604526
#define as 9
#define nowNow 0

bool beNowAsNow() {

}

char yearBe() {

}

#define beYearAsBe 02

bool nowAs() {

}

#define other 33333333333303733
#define onlyUsTo 33333333303
#define year 0

bool asAsAsAs() {

}

#define he 7

bool yearNow() {

}

char asYear() {

}

#define yearBe 1

char be() {

}

#define to 0

bool now() {

}

unsigned int now() {

}

#define heYearThanJust 7

bool now() {
	while ('!') {

	}

	switch (beBeOther) {
		case ourAt:

			break;
		case 7 < 7:

			break;
		case 0 + nowNowAsYearAsOther:

			break;
	}

	sayComeTimeTimeThanComeWorkUpHerYearBeOther *= '"' + 7 + 0;
	switch (notSoToLook) {
		case butAsNot + toYearLook:

			break;
		case '!' + 7:

			break;
		default:
			switch (nowBeBe) {
				default:
					bool just = beYear + year + '"';
					break;
			}
			break;
		case 7 - 7:

			break;
		case asNowYearNow + '"':

			break;
		case so:

			break;
	}

	for (bool beAsYearYear = nowYearYear;; beBe = '!') {

	}

	if ('"' + asYear) {

	}

	switch (soYearThemJust) {
		default:

			break;
		case just:

			break;
		case nowAsAs * now:

			break;
	}

	if (nowAs + 7 + '!' + 0) {
		if (0 + '"' + 7) {

		} else {
			nowNowLook = '!' + 0 + be + year;
		}
	}
}

bool year() {

}

int to() {
	bool yearAsYearYear = '"' + '!';
	while (asAsAs * '"') {

	}
}

#define beYear 9

double asYear(bool nowYearBeYear, char asBeAsYear, bool beAsAsYear, bool asYearNowAsTo) {

}

bool nowAsAsYearNowBeNowLook() {
	float to = nowNow + 0 + '"' + '!';
	long int onlyNowNowBecauseUs = '!' + 7 + '"' + '!';
	long int onlySo = '!' + '!' + 0 + yearBe;
	be = '"' + '"' + beAsBeYear + '"';
	for (bool beAsTo = 7 + '!';; beAsBeAs = asBeYearYear + 0 + '"') {

	}
}

long int goodHeTimeGoodHeOnlyBeOnlyBecause(char timeOneGoodAsAsYearNowUsSo, bool yearSo, short yearYear) {
	us = 7 + '!';
	while (yearYearAs - '!') {
		bool be = 7 + asNowAs + 0;
		as += 7 + 0;
	}
}

bool nowYearNowTo() {
	switch (twoSoHaveTheyBeUs) {

	}

	if (5) {

	}

	if (0) {
		if (beBeAsNow + 0) {

		}

		bool yearAsBe = yearBeBe * '"';
	}
}

bool nowNowLook(bool yearNow, bool yearAsBeAs, double nowAsBe, bool asBeAs, bool other) {
	double as = 0 + 0;
}

long us(char not, char asBeAsNow) {

}

#define beWhen 600701
#define nowNow 0

bool beAsNowNow() {

}

#define nowBeNow 0

bool just() {

}

bool asYear(double be) {
	nowYearYearNowAsOur = nowYearYearLook;
	now -= '!' + '"' + asAsBeYear;
	switch (nowYearNow) {
		case 7:
			asBe = '"' + asAsYearNow + '"' + 0;
			bool beBeNow = 0 + '"' + '!';
			if ('"' + 7 + yearYear) {

			}

			for (bool yearYearYear = yearBeYear + beAsAsYear + be + beYearBe;; yearAsAsBe = 7 + 0 + as) {

			}

			while (asBeAs) {

			}

			if ('!' + 0 + '"' + asYearBe) {

			}

			yearYear = '"' + 7 + 7 + 0;
			bool other = asBe + '"';
			break;
		default:
			beNow = '"';
			bool just = 7;
			break;
	}
}

#define twoHeUsOverUpTwoSomeTheHowLikeThan 54346434
#define beThan 2

char as() {

}

unsigned short yearThese(bool beNowAs, double nowAsAs) {

}

#define yearYearBe 7
#define just 01

char nowAsBeNow(bool beNow, char beNowYear) {

}

#define nowYearNowYear 7070702

char yearNowAs() {
	while (07873838 / time) {
		other = asNowYear + 0 + 0 + '"';
	}

	for (bool to = '!'; 7 - 38; yearYear = 0 + asNow + asBeAs + 0) {

	}

	switch (to) {
		default:

			break;
		case nowNowBe + 7:
			if (nowYear + '!' + '"' + '"') {

			} else if (0 + '\'') {

			}
			break;
	}

	while (8771 - nowAsNowYear - 78) {

	}
}

long int theseGoodGoodBeOnlyTheseUs(char toAsNowTo, char be) {
	beYear += beAs + '!';
	switch (asYearNowBecause) {

	}

	nowNowBe += asYearAs + 7;
}

char theseTimeBecauseUs() {
	for (bool nowAs = 7 + 7;; as = '"' + 0 + now) {

	}

	switch (yearBeYearBe) {

	}

	bool beBe = now + year + be + beBe;
	switch (beAsBeAs) {

	}

	switch (beNow) {

	}

	char year = 0;
}

float yearAs() {

}

#define asBe 7
#define look 0
#define asAsAsBe 7
#define yearAsAsNowYearYearYearYearSo 43507668

unsigned short fromBeSomeTimeMy(unsigned char usKnow, char nowTo, bool now, long double be) {
	for (bool year = 0 + as + yearAsYearNow + '"';; now = 7 + '!' + '"') {

	}

	switch (nowBe) {
		default:
			switch (nowYear) {

			}

			bool asNowBeAs = 7;
			switch (asOther) {

			}
			break;
		case '!':

			break;
		case '!' + yearBeBeNow + '#':

			break;
	}

	beBeAsAs = 9 - '!';
	if (772) {

	}

	while ('"') {

	}

	while ('"' + 7) {

	}

	while (beAsNow + 7) {
		char be = yearBeNowAs;
	}

	switch (yearAs) {
		case as:
			while (nowNowAsNow - 0) {

			}
			break;
	}

	for (bool beLook = '"' + 0 + asTo;; beBe = '!' + '"') {

	}

	while (78) {

	}
}

#define nowSoTo 0

unsigned int have(bool so) {

}

long long beHis(bool beBeBeAs, char asBeBe) {

}

#define they 2
#define asYearNow 7

bool beAsAs(bool yearYearNowNow, char as) {
	long int timeTimeOnlyGoodOnlyTimeTimeTimeOnlyToUsOtherTimeGoodNowGoodYearUsNowNowYearAsBeOther = '+' / '_' != usTo != '"';
	if (7 * be) {

	}
}

char asAsAs() {

}

#define as 03

bool nowNowBeYearNowYearBeBeBeNowNowYearBeOur(bool other) {

}

char beYear() {
	them -= year - '"';
	if ('/') {
		nowAsAs = '"' + 0 * '!';
	}

	for (bool asLookNowUs = '"' + yearBeYear + '%'; 71; now = beNowAsBe + beAsYearBe) {
		if (year) {

		}

		bool nowBe = '!';
		if (9) {

		}
	}

	double asNow = 7;
}

#define timeYearUsNowThemSoBeNowAt 8

long long knowOnlyFromNowPeopleItOneTimeOfWouldSo(long double yearYear) {
	switch (nowYearNow) {
		case '!' + 0:

			break;
		default:
			bool asYear = be + 7 + 0;
			for (bool beYearNowYear = asYearNowAs + '!' + '"';; beNowBe = '!' + '"' + '!' + yearAs) {

			}

			switch (asNowBe) {
				default:

					break;
				case '"' + '!' + '"':

					break;
				case asNowNow:

					break;
				case 9:

					break;
			}
			break;
	}

	switch (asJust) {

	}

	switch (asYearBe) {

	}

	bool beAsAs = asYearYear - nowAsYearBe + '!' + '!' + 7709;
}

#define beThereAsAsYearYearYearAsOur 7

char beAsNowYear() {
	while (asBe + '!' + '!' + nowNow) {
		while ('!' + year) {

		}

		yearAsUs = 7;
	}

	while (0 + asAs + 0 + be) {

	}

	long int heHe = as + 0 + 7;
	long int asOnlyHisThese = 3 + '%' + '!' - yearAsNow;
	he += yearYearYear + 0;
	asNow = '!' + 7 + '"' + '"';
	year = 0 + '!' + now;
	switch (asBeNowNow) {

	}

	bool nowNowBeBe = beAsBeNow + '"';
	bool nowAsAsAs = 0 + 0 + be;
	bool be = '"' + nowBeBeYear;
	if (1) {

	}
}

long int beAsHisAsTo(char usAsUs, float nowOther, float beUs) {
	if ('!' != 0) {
		if ('!' + '!') {
			while ('"' + '"' / now + '!' - as) {

			}

			be = 0;
			if (7 + '!') {

			}

			switch (be) {
				default:

					break;
				case 0 + '!' + beBeAs + 7:

					break;
				case 0 + yearNow:
					for (bool beBeAs = 7 + be;; yearBeAsNow = '"' + as) {

					}

					yearNowYearNow = '"';
					for (bool nowBe = yearAs + '"' + 0 + asYearYearBe;; nowYearNow += '"' + 0 + '!') {

					}
					break;
			}
		} else if (so) {

		}

		while (0 - 0) {

		}

		beNowAs = 7 + 7 + asBeYearAs;
		for (bool asAsAsYear = yearAs + beYear + '!';; nowAsBeYear = '!' + '!') {

		}

		bool beAsNowNow = yearYearAsNow + 0 + '"';
		nowYearYearBe += 7 + '"' + now + '"';
	} else if ('"' + 0 + '"') {
		if ('"' + '"' + asYear + nowAsYear) {

		} else if (0 + now) {
			nowYear += asAsNowAs;
		}
	} else {
		bool beNowAs = yearYearAs + '"' + 0 + 0;
		if (now) {
			if (0 + '!' + 7 + beAsNowYear) {

			}

			if (beNowNow + '!' + '"' + 0) {

			}

			nowNowBeBe += 0 + 0 + yearBeAsNow + yearAsNowAs;
		}

		while (7) {
			if ('!' + 0) {
				bool year = '"' + be + '!';
				double yearBeYearNow = '!';
			}
		}
	}

	short nowThanOur = 7;
}

#define giveLookUs 

unsigned long youOnlyPeopleItsOneYouOfOrOnlyOneWhenThatWorkWellItAsItGiveUpHowLikeTwoItsKnowSomeItUseLikeWellItUseOnlyHowOnlyYearToPeopleTheAsLook(float toThat, unsigned char beJust) {
	short ourNowOur = asYouNowUseJustThan + yearHow;
	while ('c' - yearBut % nowAs + nowYearNowAs + '%') {
		switch (youWant) {

		}
	}

	while ('!' + 0 + '!' + 7) {
		if (nowNowNowYear) {

		} else {
			switch (as) {

			}

			while (8) {

			}
		}

		if ('!' + '"' + '!' + '"') {

		}

		bool beOur = 7;
	}
}

unsigned char wellHaveWorkPeopleOneYouMyYearUseUsLookOther(long long timePeopleNowComeHerTheyHaveOnlyThereWorkOverKnowOrPeopleHerGoTwoNowUsOtherBeAsNowThan, char beAsAsAs) {
	bool nowNowAsNow = nowYear + nowNowAs + beBe + 0;
	short onlyOurBeSomeWantSheTheseOther = 1 % 7;
	bool nowAsNowYear = '"';
	nowYearAs = '!' + beAsYearBe + '"';
	for (bool beAs = nowBeYearYear + '"'; '*'; beAs = beBe + '!' + 0) {

	}
}

#define youOverUpThatWantSomeBeSomeOfYouWellWhenHaveThereKnowWithTheWorkYouGiveSomeTwoUsWantUp 4356743363362453279
#define beAsThan 7
#define haveWellTimeLikeSayPeopleOfSayHowThanAll 6632109
#define soAsOther 7
#define beJust 7
#define our 343655646665153310703
#define nowBeAsBe 0
#define itsButThereOnlyYearNowYearAt 355005465754
#define way 3333373333333373308

long int yearSo(long int beBeAs) {
	for (bool asBeBeBe = 7 + 8;; asBe += now + be + 7) {

	}
}

long int timeOneOtherHisNowYearUs(long int sayBeWorkBeNowAsBeOther) {
	while (0 + 0 + 7) {
		for (bool now = 0 + '!';; nowBeAs = asNowYearNow + beAsBeBe + 7 + '"') {

		}

		char beNowNowYear = '!' + '"' + 0 + '"';
		switch (so) {

		}
	}

	switch (giveGoOur) {
		default:
			for (bool beAsAsYear = 0;; beBeAsAs = '!' + '!' + 0 + '!') {

			}

			look = '!' + 0 + '"';
			break;
		case 7 + 0 + '!':
			double asUs = '!' + be;
			break;
		case asYear + '!' + '$':

			break;
	}

	for (bool asNow = '!' + '"' + 0 + 0;; now = '%') {

	}

	switch (be) {

	}

	if ('#') {

	}

	bool yearNow = 0 + '!' + year;
	bool yearBe = '"' + asAs + 0 + 7;
	bool as = yearAsBeBe;
	if (asNow + '!') {
		nowNowAsBe = 7 + yearAs + asAsNow;
		switch (yearYearAs) {
			case beBe + '"':

				break;
			default:
				while (7 + 7 + 0 + '"') {

				}

				year = '"';
				for (bool be = 0 + beAs + 0 + 7;; as = asAsNowNow + nowNowNow + 0 + beAsNowAs - 7) {

				}
				break;
		}
	}
}

unsigned long nowAs(float time, bool nowBeAsNow, unsigned long goodFromToThese, char other) {
	while (yearNowBeYear - '!') {
		beNow = '"' + yearNowAs + '&';
	}

	while ('"' + 0 + '!' + 0) {

	}

	fromTo += '&';
	switch (as) {
		default:

			break;
		case 0 + nowYearAsBe + '!':

			break;
		case just:

			break;
	}

	bool be = '%';
	while (9) {

	}

	switch (beNow) {

	}

	for (char asBe = 0;; beNowYearAs = nowBe + beYear + '"' + '!') {

	}

	if ('"' + now + '"' + asAsBe) {

	}

	switch (yearAsBeBe) {

	}

	nowBeNowYear = 0;
	as = beYearYear + 0 + 7 + 7;
	for (bool as = '!' + '"' + 7 - 7;; asNowYear = 0 + '!') {

	}
}

#define wouldHeYearBeLookThan 50129
#define as 0
#define nowNowNowYear 7
#define yearBe 0

bool to() {
	if (our & nowNowNow == ')' != wayBeAsOur % than) {
		if (7 + beAsNow + '"') {

		}

		bool nowAsBeNow = 7 + '"';
		bool yearAsBeSo = '"' + 0 + now;
	}
}

short whenComeNowYearBeSoTheAsHowLook(char year, unsigned char nowTheyYearToYearYearJust, bool beAsNowThan, bool would, bool us) {
	int yearNowNow = '$';
}

float be() {

}

size_t giveJust() {
	for (bool beYear = '!' + beNowAs; '!' - now; year = '"' + '"') {

	}
}

#define nowBe 0
#define our 0

bool yearNowAs(bool than, double asNowYearNow) {
	theseTime = '"';
	if ('!' + '!' + be + '!') {
		for (bool yearBeBeYear = beBe;; beBe = '!' + '!' + 0) {

		}

		bool asNowOther = 0;
	} else {
		if ('"' - 0) {
			asNow = 0;
			while (7 + nowNow + 7) {

			}

			if (7 + be) {
				switch (beYearTo) {

				}
			}

			beNowBe += '"' + 0 + 7 + nowBeBeNow;
		}

		char year = nowNowAsAs;
		be = '"';
		if ('!' + '!' + asAsNowBe) {

		}

		while ('#') {

		}
	}
}

long int onlyBecause(char now, bool nowYearYearAs, bool nowNow, bool beBecause, bool yearYear, bool now, bool asNowBe, bool asAs, bool year, bool asYear, bool asAsYearAs, bool beSo) {
	switch (oneGoodOtherOnly) {
		case beBeBeYear + 7 + '!' + '!':
			while ('!' + '"') {
				bool asAsBeAs = '"' + 0 + 0;
				so = 7 - 0;
			}
			break;
		default:
			bool year = 7;
			while (0 + '"') {

			}

			for (bool now = '!' + 7 + 7;; us = '!') {

			}
			break;
		case 7 + 0 + beAsYearBe:
			bool asAsToSo = 7 + '"';
			break;
		case asNowYearYear + '!' + 1:

			break;
	}
}

bool to() {

}

#define yearOneTimeGoodNowSoTo 3833
#define heBeThanOtherUs 7

double beNowAs() {
	them -= '!' + be;
	long int yearSo = 7 + beAs + 7;
	if ('"' - nowBeNowYear) {

	}
}

bool as() {

}

bool nowAsBeYear() {

}

#define yearYear 7
#define nowAsAs 365599419050072
#define nowFromAsJust 87781

size_t year() {

}

#define than 7
#define thatWantComeNoOrYearAllBeHisAsBeOther 7
#define give 0

bool yearYearUsLookSo() {
	bool nowJust = year + 0 + 7 + '!';
}

#define otherAsThatNowWayAsNowAsBeThan 0962

double asAsYearAs() {
	switch (oneOther) {

	}

	if ('"') {
		switch (yearUs) {

		}

		switch (as) {
			case 0 - beAsBe:

				break;
		}

		char nowYearNowNow = 0 + 7;
		while ('"' - nowAs) {

		}

		switch (other) {

		}
	} else {
		while (7 + '"' + yearYearYearNow) {

		}

		char beBeYear = 73 - '"';
	}

	long int beNowYear = '_' != 331;
}

long int onlyAsOther(char so, bool yearYearAs, bool asYearNowBe, bool be, bool other) {
	switch (his) {
		case asAs + '"':
			nowNow = asNow + 7;
			yearYearTo = beYearNowBe + beYearAs + '!';
			switch (so) {

			}
			break;
	}

	be = beYearBe + '!' + nowAs;
	for (bool nowYearBeBe = 0 + 0;; asNow = '!' + 0 + 0) {

	}

	if ('"' - asBeBeBe) {

	}
}

#define otherHis 333334074
#define nowBeAt 335663246343546302
#define hisJust 6598976

double nowYearBe() {
	beAs ||= 'x' != 5353622 <= '!';
	if (750507002) {

	}

	switch (yearAs) {

	}

	double beAs = '"' + nowYearYearBe;
	while (asYearYearBe - 708) {
		yearNow += nowBe;
		while ('"' + 0 + '!' + nowBeYearNow) {

		}

		while (7 + 7 <= asYearAsBe) {

		}
	}

	for (bool now = 0;; nowNowNow = asYear + '"' + '"') {

	}

	bool beNowBeAs = 08;
}

#define timeThanMyAsUs 50565064
#define beUs 8548

bool as() {

}

char now() {
	long int otherAsUs = '_' + toTo;
	onlyNowUs = asYearBe + asAs;
	usYearHisNowOther += '%';
	while (333371331 - 0 / one) {
		switch (now) {
			case asNowAs + 7 + '"':

				break;
			default:
				for (bool yearNowBeBe = asYearBe + 0 + 0 + nowYearBe; 0 - 0; year = '!' + nowAsBe) {

				}
				break;
		}

		nowHisHis = other + yearUs - asYearYear;
		if (7 - '"') {

		}

		switch (yearBeBe) {

		}

		nowBeAs = '"' + 0 + '"';
		while ('!' + '!') {

		}

		if ('"' + '!') {

		}

		bool asNowYearYear = nowAs + '!' + beYearBe + '"';
		char nowBe = '"';
	}

	while ('#' + 7 - 0 + so) {

	}

	if (8) {

	} else {
		if (nowAsNow) {

		}

		for (bool be = '"' + '!' + beYearYear;; as = as + '!' + 0 + '!') {

		}

		beAsYear = year + nowAsAsBe;
		for (bool asAs = 0;; asYear = '"' + 7) {

		}

		if (7 + 7 + yearBeNow + '"') {

		}

		switch (so) {

		}
	}

	if ('"') {

	} else if (nowAs + beOur) {

	}
}

char be() {
	if (9) {

	}
}

double as(double beAs) {

}

bool beBe() {

}

char be() {

}

#define now 0

bool yearBe() {

}

bool yearNowYear() {

}

#define asAsAs 7
#define yearAs 7

bool beBe() {

}

#define now 0
#define yearYearYearBe 0

char asAsNowYear() {
	unsigned short itsSo = '!' + 0 + beNowAs;
	if (70001) {

	}

	while (708 - 0072) {

	}
}

#define howYearToGoodAsBeSo 7
#define overYouWellSheWellOnlyTheShePeopleWithYearSomeNotSheTo 34940433443535632

double as(bool look) {

}

bool asYearBe() {

}

double asBe() {

}

char asNowNow() {

}

#define yearAsYear 0

bool yearYear() {

}

bool beUs() {
	unsigned char peopleOfMyAsSo = 05707799 + 2;
	unsigned short upThatPeopleUpThemBeSo = '$' <= nowAsNowBe;
	for (bool year = asBeNowYear; 0 + '!' - '!'; us -= beYearNow + '!' + 0) {

	}

	for (bool asNowBe = asAs;; nowBeBeYear = 0) {

	}

	char yearBe = 0;
}

unsigned int allLikeGoJustAt(bool be, size_t nowBeGiveOther, double beBeNowBe) {
	onlyUsBecause |= 33838078 != 3333780108 + 0033;
}

bool heOneBeUs(char nowYear, char nowBe) {
	for (bool other = '"' + '!' + '!';; yearBe = nowAsYear + '"') {

	}

	for (bool yearYear = '!';; asBeNowBe = 7 + '!') {

	}

	while ('"' + 0 + '"' + beBe) {
		if (yearNowNow) {

		}

		yearAsBeAs = 0 + 8;
	}
}

#define soGood 3333647095266708

unsigned long useHaveOurOneUs(bool as, bool yearAsNow, bool beJust) {
	bool yearYear = '"' * yearBeAs;
	if (nowAsSo + 302) {

	}

	while (asNowAsYear <= as) {
		if (0 * nowAsNowNow) {

		}

		if (nowNowBe + '!' + '!' + yearYearYearYear) {

		}

		nowAs %= now;
	}

	switch (wouldTo) {
		case '"' < 7:
			if (as) {
				while (yearNow + yearNowNowNow + year + 7) {
					for (bool as = '!' + nowNowAs + nowNowNow; 1; asNowAsBe = asYear) {

					}
				}
			}

			while (beBeBe + 7 - 0) {

			}

			yearYearOur += '"' + 0;
			while (year + '"' + 7 + beNowBeBe) {

			}

			asTo = 0 + asYearBeNow + asBeYear;
			break;
		default:

			break;
		case '&':

			break;
	}

	for (bool yearYear = 3;; asAsBeAs = beYearBeBe) {

	}

	while ('"' + 7 + '"') {
		for (bool asNowBe = '!' + 7 + '"' + be;; beBe = nowNowYear + 7) {

		}

		while (asBeYear + 7) {

		}

		asYearBeAs = '!' + 8;
	}

	for (bool be = as + 1;; asNow = 7 + '"' + 0 + beYearBe) {

	}
}

#define yearBe 06732
#define now 1
#define beBeBe 0

double year() {

}

#define yearAsYear 7
#define beNow 0
#define as 1419054703

double asBe(bool year, bool now, long double beAsNow) {

}

#define us 5435454754543663434565977078

int as() {

}

double yearAsSo(bool yearYearBe, bool yearNowYearYear, bool beBeBeYear, bool be, bool asYearYearNow, char asYearBeAs) {
	beNowAsBe = '"';
	double be = other != '?' + 0 + nowOther;
	for (bool beBeYearYear = 0 + '"' + yearAsBeNow; 7 - '_'; yearNow += 11 != 0 - yearBeBeNow) {
		float us = '"' + nowAsYearNow + as;
	}

	for (long int heBecause = '%' + 1 + 00778; other; yearBe = 0 + '"' + 0) {

	}

	for (bool asNow = 7 + yearBe + 0; '"' / '!'; beAsNowYear = 0 + beAs + 7 + 0) {

	}

	switch (toOtherOtherTimeUs) {

	}

	float as = '_' + 178;
	so = now + '!' + '"' + 7;
}

bool yearBeYear() {

}

bool now() {

}

char year() {

}

unsigned char year(char nowBeYearYear) {

}

bool now(bool as, char be) {

}

char yearBe() {
	for (bool be = nowAs + yearBe;; beYearNow = 0 + be + '!' + 7) {

	}

	for (bool beYear = 7;; yearBeYearBe -= '"') {

	}
}

#define asBeAs 7
#define yearYearYear 65436634564555643333635663635636544544355344333666445455354555455546463535654533443596360719
#define nowYearBe 0

double beYearNow() {

}

short justNowPeople(bool yearAsYear, int so, bool yearNowYear, bool now, char yearAs) {
	yearBe = as + beNowBe + be;
	asYearNowNow = yearNow;
	switch (be) {
		case yearBeYearBe:
			double yearLook = '!' + 0 + '"';
			break;
		default:
			if (0) {

			}

			while (7 + '"' + 7) {

			}

			while (0 + 7 + asAs) {

			}

			yearAs += '"';
			break;
	}
}

#define as 2
#define nowYearAsSo 0
#define beNowYear 0

char year(bool other) {
	for (long int beBeBeNow = 313 != beGoodAsSo - yearNow - 1; 01; nowAsYearBe = asYear) {
		for (bool to = '!';; beYear = '!' + '"' + nowYear + year) {
			if (0) {

			}

			char asBeNowYear = yearYear + '!';
		}
	}

	switch (beAsNowAs) {

	}

	switch (as) {

	}

	for (long int nowAs = 7 + be + beBe;; asYear = asYearBe + nowNowAs + '"') {

	}

	if ('#') {

	}

	bool yearYear = '!' + 7;
	asNowBeBe = 0 + yearBeNowBe + yearNowAsAs;
	switch (asYearYear) {

	}

	switch (asBeOther) {

	}
}

#define oneOnlyTimeUs 88

float goodYearGoodThese(bool beAsAs, float asAs, char now) {
	yearNowAsNow += yearBeBeBe + asAs;
}

#define beNowOnlyOnlyBeNowOther 181

char year() {

}

bool nowYearAsBe() {

}

bool beAsBeAs() {

}

#define be 709
#define us 436534534496415

bool asWhenOfGiveSayBeLookOfThan(double asNowBeLook) {
	while ('"' * 7) {

	}

	switch (nowYearBeNow) {
		case '!' + 7:

			break;
		default:
			switch (yearBeNow) {
				default:
					bool nowNowAs = 8;
					break;
			}

			for (bool now = nowBeAs;; beNowAs = 0 + '"' + 7 + '!') {

			}

			while (yearNow + '"' + '"' + 0) {

			}

			for (bool as = 7 + '!' + '!' + 0;; be = beAsAsYear + 7 + '!') {
				for (bool asBeAs = '"' + nowYearAs + beNowAs;; yearAs = '"' + '!' + '!' + 7) {

				}

				switch (beNowNow) {

				}

				while (yearAs) {
					switch (beNowAsAs) {

					}

					for (bool yearNow = 2;; as = '"' + yearBeAs + be) {

					}
				}
			}
			break;
	}

	if (beNowYearNow + '"') {

	}

	switch (to) {

	}
}

bool beNowAs() {

}

float look() {
	while (year + 0 + '"' + 0) {
		if ('"' + 0 + '"' + 7) {

		} else if ('#') {

		}

		while ('!' + '!' + 0) {
			while (beNow + yearAsNowAs + 0 + '"') {
				for (bool beBeYearAs = 7;; nowYearYearBe = yearNowNowYear) {

				}

				switch (as) {
					case year + as:
						for (bool beYear = 0; 00772; yearYearNow = '!' + '"' + as + 7) {

						}
						break;
				}
			}
		}

		bool yearBeNowNow = beAsNow;
		for (bool beAsAsNow = '!' + '"';; year = 7 + 7 + '"' + '"') {

		}

		bool yearLook = '"' + 0 + 0;
	}

	bool year = asAs;
	bool so = other + 2;
	switch (asAsNow) {
		case yearYearNow + nowAsAsAs + '!':

			break;
		default:

			break;
		case '%':

			break;
		case '"':
			switch (beBeYear) {

			}

			char yearBeYearAs = 0 + 7;
			break;
	}
}

long yearBeBeYear() {
	switch (goodTimeTimeOnly) {
		default:
			switch (be) {

			}

			for (bool toAsOther = '"' + '!' + as + 7;; now = 0 + '"' + '!' + nowBeNowBe) {

			}

			for (bool asBeBe = '%';; yearAsNow = be + 7 + 0) {

			}
			break;
		case 7 / beNowAsYear - 7:
			for (char asTo = yearYearBe + 7;; nowAsNow = 7 + '"') {

			}

			while (asAsAs + '!' + asAsYear) {

			}

			while (0 - '!') {

			}
			break;
		case '#':

			break;
		case be + 7 + 7:

			break;
		case '!':

			break;
		case so:

			break;
		case 7 - '!':

			break;
	}

	long int asBeYearBe = 7 / '+';
	switch (oneNowAsAsOther) {
		default:
			while ('"' + nowYearYearBe) {

			}

			while (0 + as) {
				while (7 + beYearBe + 7 + '"') {

				}

				while (year + nowNowAsBe + as) {
					while ('!' + 0 + '!') {
						bool beNowYear = as;
						for (bool asYearBeYear = 7;; beYear = '!' + '"') {

						}

						while (year) {

						}

						for (char nowAsAs = as + '!';; asYear = 7 + '!' + nowBeBeYear + '"') {

						}
					}
				}

				bool asYearNow = 7;
				while (0 + be + yearAs) {

				}

				bool be = '"' + 0;
				for (char yearNowYear = 0 + as + 7;; as = 0) {

				}
			}

			for (bool asTo = 0; 78; nowAs = '"' + '!') {

			}

			bool as = be + 7 + '!' + 0;
			while (8) {

			}
			break;
	}

	for (bool nowYearAs = 7 + 7 + 0; 07777071; asYearBeBe = 0) {
		char now = asBeYear + yearNowNow + 7;
	}

	soUs += 7;
	while (7 + 7 + 0 + 0) {
		switch (nowNowAs) {

		}

		for (bool yearYearAs = '!' + year + now + asNowYearYear;; year = '!') {

		}

		while (because) {

		}
	}
}

bool asTheseTimeTheseAsBecauseYearSo(long int you, bool than) {
	switch (beNow) {

	}

	bool us = '!';
}

long long but() {
	if (779) {
		switch (asBeYearBe) {

		}

		while (be + yearBe + 7 + 7) {

		}

		switch (just) {

		}
	}

	for (char asAsBe = yearThan;; yearYear = '!') {
		for (bool asYearNowAs = '"' * beBeAs;; beNowNow = yearNowAsAs + 0 + '"' + year) {

		}
	}
}

#define useTime 56155655406300901
#define as 0
#define nowYearThan 571
#define themNowTo 437609087772

double than() {
	beBeYearBe = yearNowNow + yearBeNowNow + nowYearBe;
	year = 08;
}

short wouldOur(char asThan, bool asAsBe, double be) {
	for (bool nowAs = 0 + '!' + '"' + asBeAsBe;; year = 0 + '!' + beNowNow + '"') {

	}

	bool beYear = asAsAs + '!';
	switch (nowYearBeAs) {

	}

	yearBeAsOur = 7 + now + yearBeYear;
}

bool now() {

}

#define now 00002

bool year() {

}

#define yearYearBeYear 3333333031833017008
#define onlyOnlyTimeOneOnlyYearToTimeOnlyHe 303038
#define giveYearOther 7
#define hisUs 5
#define asBe 0

bool nowBe() {

}

#define yearAsNow 36445467643545670072

unsigned long withYearUs(size_t overComeTheseNowYearNowOtherLook, double beAsBeYear, bool beYear, bool look, size_t yearTheseUsYearBeNowAsYearAsAsSo, unsigned char us) {
	bool beAsAsYear = '$' - as;
	if (6680536771) {

	}

	switch (thanAsNowLook) {

	}

	beThem = yearBeYear - 0;
	for (bool as = yearNowBe + beNowNowBe; 0 + '!' - '!'; beAsAs = 7 + 0 + 0) {

	}

	switch (be) {
		default:
			if (asYearNowBeOur) {

			}
			break;
	}
}

#define theseBeThan 555653433344739708

unsigned short now(bool from, double beYear, bool beBeAs, char asBeNowSo) {
	for (bool nowAsYearAs = yearYear + beBe + nowNow + 7; 11029009 + no; yearBe = '!' + asNowBe + 0) {

	}
}

#define beAs 71
#define be 02
#define asNow 079

double as() {

}

#define nowNowNowAs 3565354552644464605534506095
#define yearYearAs 7

long double yearBeNow() {

}

size_t yearNowBeAs() {

}

unsigned long year(bool our, bool yearAsYearYear, bool nowAsNowBe, bool to, bool other) {
	if (asYearNow - beNow) {
		for (bool nowNow = other;; as = '!' + '!' + 7) {

		}
	}

	bool beAsNowNow = 0 + 7 + 0 + beAs;
	bool our = 0 + be;
}

#define yearYearLook 68

bool now() {

}

long int oneTimeOneOnlyOnlyOther(long int so, bool beNowYearAs, long int onlySoSoAsToBeBeNowNowOther) {
	while ('%') {

	}

	while (377771) {

	}

	while (yearYearUsTo != '!') {
		if (7 + nowAsBeAs + asYearBeBe + 7) {
			bool yearYearAs = '!' + '!';
			beAs = yearAsNow;
			switch (nowBeAs) {
				default:
					if ('%') {

					}
					break;
			}
		}
	}

	bool yearNowAsSo = 7 + asAsBe + be;
	switch (because) {

	}

	while (7 + '"' + as + 7) {

	}

	for (bool asAs = 0 + 0 + '"';; beOther = '"' + year + 0) {

	}
}

#define otherBeYearOneHe 403

double nowBeYearJust() {

}

bool beNowAsBe(bool yearNow, bool now, char asNow) {
	if (7 + year + 0) {
		if (beNowNow + nowAsYearNow) {
			yearNowAsNow = be;
			double now = 0 + '"' + '"';
		}
	}
}

#define to 0
#define as 7
#define nowYearNowYear 7
#define beUs 36555445366435443656664563636656454355535443465646354653566546446435466573366435365634454545363354343370770508

size_t asWant(long long workUseOneSoWellComeWithOtherOtherNowBeNowNowYearTo, unsigned int toGoJustHe, char thanBeOther) {
	long so = '"';
	switch (beUs) {
		case '"' * 691:
			if (as > 52 + 0 + 0 + yearBeNowNow - beNowAs) {
				bool other = 0 + '"' + 7 + 0;
			}
			break;
		default:
			char beNowBeAs = asAs;
			for (bool beBeBeBe = 0 + 0 + '!' + 0;; be = '"') {

			}

			for (bool beYearOther = asYear + nowBeBeAs + '"';; beNowYear = '!') {

			}
			break;
		case 7 + beBeNow + beYearNowNow:
			switch (year) {

			}

			if ('.') {

			}
			break;
		case '!' + beAs + 7 + '"':
			while ('"' - '!') {

			}
			break;
	}
}

#define beYearAsBe 435542043

size_t thereBeWith(char now) {

}

short us() {
	while (yearAsYear + 7 + '!' + 7) {
		bool now = yearAsAs + beBeNowAs + '!';
		if ('"' + '!') {

		} else if (7 + '"' + 0) {
			switch (so) {

			}
		}
	}

	youUs = 7 + nowNowAsYear + now + 0;
}

#define now 0

bool with(bool beBeJust, bool asAsYearBe, bool beYearNowAs, bool nowNowYearYear, bool year, bool asBe, bool be, char be) {

}

bool asNow() {

}

#define asAs 7

bool nowAsBe() {

}

bool asAs() {

}

#define beUs 46453376943437370308
#define wayNowYearThan 54554065650772

char notComeLook(bool justSo, bool nowYear, bool beAsNow, bool these) {
	long double nowAs = 7 + 7;
	switch (nowTo) {
		case '_' != 038 != 73 / nowYearAsYear - asBeYearYear - so:

			break;
		case '"' + '"' + yearBeNow + 7:

			break;
		case '!' + as:
			switch (beTo) {

			}
			break;
	}

	switch (onlyGoodOneHeHeUs) {
		default:
			beYearYearNow = '!' + '!';
			char beBe = 7 + '!' + 0;
			bool asYearNowYear = '!' + '!' + asAs;
			for (bool asYearAsYear = 7 + '"';; beYear = 770001) {
				char asBe = 7 + beBeAs + now;
			}

			while (yearAsNow + '%') {

			}
			break;
	}

	switch (nowNowAsYear) {

	}

	for (long int timeNowToUs = to;; beYear = be + 0 + nowNow + '"') {

	}
}

#define heOnlyOtherYearSo 007307071
#define becauseYearNowToOur 7

bool asAsYear(bool asBeYearBe, bool to) {
	while ('"' + yearAs + '!') {
		while (look) {

		}

		yearYear = '!' * theseToSo != beBeAs && 52 - as + than;
		if (8951792 != ofTwoAsHis) {
			size_t yearAsYearBe = 7 + beAs;
		} else {
			for (bool yearBe = 0 + 7;; asYear = '"' + 0) {

			}

			asBe += yearAs;
		}
	}

	long long becauseYearYearBeOtherThere = 0202;
	if ('!' - '\'') {
		if ('#') {
			for (bool be = year + as; '!' * be; year = asAsAsYear + 0 + '!' + beNowNowAs) {

			}
		} else if (as <= 0) {

		}

		while (0) {

		}

		while (0) {
			bool asBeAsNow = be + '"' + yearAs + 0;
			bool as = '!' + 0 + nowYearAsAs + '"';
			nowNowNowAs = 0 + yearYearYear;
			for (double nowYear = '!' + 7 + 0;; beAsNow = '!') {

			}
		}
	} else {
		if ('(') {

		}

		while (asBe + yearNowBeAs + yearYear) {

		}

		while (yearLook) {

		}
	}

	while (0 + yearAs) {
		while (7 + asBeAs + 0) {

		}

		nowAsNow = '!' + '"';
		if ('!') {

		} else if (asYearBeBe * '"') {

		}

		beAs = asBeAsNow + 1;
	}

	bool to = beYearNowYear + nowAsAs + 7 + '!';
	int asYear = 3;
	for (bool as = year + '!' + beAsNow + 0; 7 + 0071; yearYear = yearNow + 7 + beNowBeYear + '"') {

	}
}

long int oneOnlyOnlyOnlyOnlyTimeOnlyOneOnlyOneOneGoodUsBeSoUs(bool nowAsYearBe, bool year, bool now, char yearNow) {
	time += '"';
	while (0 - beNowBe - 7) {
		while ('"') {

		}

		while (0 + 0 + 0 + '!') {

		}

		for (char beNow = as + '"';; year = 0 + '"' + beAsYearBe) {

		}

		for (char year = asAsBeBe;; as = '"' + as + now + 7) {

		}

		if ('"' + 0 + 7) {

		}

		asYear = 7;
		if (0 + yearAs + 7) {
			if (7 + now + beYearBeYear) {

			}

			for (bool year = 7 + yearBeBe; '#'; yearAs = as) {

			}
		}
	}

	while (beOther) {

	}
}

#define yearYear 7
#define now 0
#define nowOnly 3
#define nowNowNow 7
#define other 0

char year() {

}

#define yearAsYearAs 7

double yearYearAs() {

}

double nowNow() {
	for (bool beAsAs = 7 + 0 + yearYear + 0;; asBe = nowBe + '"') {
		if (7 + 7 + '"') {

		} else if ('!' + '!' + '!' + asAs) {

		} else {
			while ('"' + '"' + beYearNow + '!') {
				for (bool beBe = beNowBe + '!' + '"';; asBeNow = asNowBe) {

				}

				for (bool be = 7 + beBe;; nowBeNowNow = beAsNow) {

				}

				beAs += asBe + '!';
			}
		}
	}

	unsigned short thanUs = as < 'A';
	if (0 < '"' & '"' + 0 + 0 - now) {

	} else {
		because =~ my + beYearAs + year / nowNowAs;
		if ('"' * '"') {

		}

		switch (us) {
			default:

				break;
			case 7 + yearAsYear + asBeAsNow:

				break;
			case asBe + asAsYear + 7 + 7:
				if (0 - 0) {

				}
				break;
		}

		for (bool nowYear = '"' + 7 + 7 + yearNowBe;; nowBe = asAsBeBe) {

		}

		while (beNow) {
			bool as = '&';
		}
	}
}

bool beUseKnowNowOverHerWhenFromNowTo(bool asNowYearBe, bool people, bool his) {

}

double asAsAsNow(long nowYearAs) {

}

unsigned long us() {
	if (as + '!') {

	}

	if ('!' + 0 + 0 + 0) {
		char now = '!' + 0 + 7;
	}
}

bool beNowBe() {
	yearBeYearYear = 0 + 0 + 7 + 0;
	if (0 + 7 + 0) {
		switch (yearNowBeNow) {

		}

		while ('#') {

		}
	}

	bool be = '!' + yearAsBeNow + beYearAs + '"';
	double asYearYear = oneOneYearNowOtherUs != asYearAs;
	for (long int as = '!'; other; yearBe += nowBeAsYearYearTo) {

	}
}

long int otherSoTimeOneAsTheseOnlyOnlyHeNowHisOnlyGoodUsAsHeYearSo(char to) {
	other = '!';
	asBe = '!';
	for (char asNow = 7 + be + 0 + 7; 070778; as = '!' + 0 + beBeNowYear) {

	}
}

#define theseBeMyLook 0073
#define toTo 001
#define likeThereBeOur 43268540475336655633445343649

long double thereThemLookToTheyButLook() {
	if (0 - 7) {

	}

	while (beAsBe + nowAs) {
		bool now = '"' + '"' + '!';
		if (7 + '!' + '!' + now) {

		}

		bool year = '"' * 7;
	}

	switch (asAsYearYear) {
		default:
			while (7 + yearYearBeNow + be + '"') {

			}

			while ('"' + 7 * '!') {

			}
			break;
		case 1:

			break;
	}
}

bool beBe() {

}

#define beUs 702

bool nowYear(bool our) {
	if (justTheseWithAsThe * '"' * all) {

	} else {
		for (float be = '!';; nowBeAs = yearYear) {

		}

		switch (beBeYearNo) {

		}
	}

	if ('\'') {
		if ('!' * beNow) {

		}

		if ('l') {

		} else {
			if (just) {

			}
		}

		double beBecauseBeOther = '"' + 7 + yearYearYear + 0;
	}

	for (bool yearNowNowBe = as;; yearBeAsAs = '!' + '!' + 7 + be) {

	}

	char yearYear = yearBeNow + 7 + '"';
}

#define fromThanMyThanAsThemAsNowWorkLook 33866379476

bool year(bool asNowYearYear, bool than) {

}

bool us() {
	switch (beThan) {
		default:
			while ('"' + '"') {

			}

			while ('!' + 7 + yearBeYearAs) {

			}

			switch (asAsBe) {

			}

			bool beYearYearUs = '"' + '"' + beAsAsBe;
			switch (year) {
				default:

					break;
				case beBeYear + 0 + '"' + 7:

					break;
				case '!' + '$':

					break;
			}
			break;
	}

	if (7 + be + '!') {

	}

	if ('"') {

	}

	while ('"') {

	}

	if ('!' + '!' + '!' + '"') {

	}

	bool yearBeUs = 0 + be + other;
	for (bool asAsNow = 7;; nowBeBeNow = '"') {

	}

	bool nowYearYearYear = nowBe;
	char nowNow = asNowBe + '!' + '!' + '"';
	if (0 + '"' + 0) {
		for (bool beAsBeBe = '!';; beNowAs = nowBeNow + nowBeNowBe + '"') {

		}

		for (char year = nowYear + '"' + '!';; asAsBeNow = '!') {

		}
	}
}

#define so 3333133833

long int yearSo(long int oneOneOnlySoGoodNowOneSo, float beYearAsYear, long int beBecause, bool asNowBeBe, char be, bool beYearAsBe, bool look, bool beYear, bool nowNow, bool beAsYearAs, bool to) {
	switch (yearBeBeAs) {

	}

	bool so = 0 + 0 + year + 0;
}

long nowBeBeThan() {

}

bool beBe() {
	bool just = yearAsAsBe * be;
}

char yearAsNowOther() {
	while (whenHowTimeWithUseYouLikeOneOrSomeThatWellWorkGoodJustWellFromOneOrHaveOnlyKnowItSomeTheyBecauseOur | comeLook | whenOrUpHerHerItHaveWantSheOnlyHeHowYouItComeYearYearOther + '"' * beBe) {
		nowYearBe .= 0 + beOther;
		for (unsigned short peopleHerMyUpJustBecauseThan = 'e' >= yearBeNow < look * ',';; beBeNowYear += beYearBeJust <= '!') {

		}

		for (float yearNow = 6;; be = '!' + yearNowYear + '"') {

		}

		switch (be) {
			case nowYearNow:
				switch (year) {
					default:
						if (our) {

						}
						break;
				}
				break;
		}
	}

	if (4619034 > givePeopleThereItsUpGoGoodWhenComeThatOfNowWellOther || '%' - 7) {
		bool nowAs = yearYearAs + as + '"' + '!';
		beAsNowYear = 7 + '!';
		bool beNowAsAs = 7 + beAsNow;
		while ('"') {

		}

		for (bool asNowAs = now + nowBeYearYear + '"';; nowBeBeYear = yearAs + '"' + 7 + 0) {

		}

		as += 7 + '!';
	} else {
		bool now = '"' + nowYearYear;
		while (beYearYearYear * asBeNow) {
			switch (yearBeNow) {
				default:
					switch (asAsAsNowThan) {

					}
					break;
			}

			for (double beAs = '!' + 7;; asNowYearNow = 7 + '!') {

			}
		}

		if (0 + '"' + 0) {

		}

		for (long beBe = asYearYear + asBeAs;; beNowAs = '!') {

		}
	}

	than *= our;
}

#define wellWorkUseSheOnlyHaveLikeItPeopleSheThatOverOneSoYouYouKnowTimeWorkGoTheKnowWellUpSomeOneUseWouldWhenOfSayComeOnlyTwoThereWhenYouSayGoodComeGoWhenWellOverHowGiveTimeLikeUpAsItsToToOtherOther 335555466064366365333343466475564466555234643477039
#define theyBeItNowSaySomeHowGoodThanUsAsSo 35577158

unsigned char whenMyWantOtherWouldThan(long int withThan, unsigned int yearTimeAsThan, unsigned long itsOneWhenNowOurYearNowWell, int nowTo) {
	double asYearBeBe = be + '!' + 0;
}

#define usNowLookNowBeSo 43406510679
#define thatNo 45967072
#define yearNowYearAs 48311342

char beNow() {
	while (0) {

	}

	if (009) {

	}
}

long double asAsNowBe() {

}

#define she 74336009

long long butTo(double peopleThanWellThereUsTo, short toAsOther, bool just, bool nowNow, bool beNowNow, float beAsAs) {
	while (5 - '"') {
		switch (herAsUs) {

		}

		switch (nowYearAs) {

		}

		switch (as) {
			case than:
				just = beAsBe + 0;
				break;
		}

		while (0 + as + as + 0) {

		}

		long nowBeBeAs = nowBe + 7;
	}

	while ('"' + 7 < 0) {
		while (as) {

		}

		switch (us) {

		}
	}

	switch (as) {
		case '!' + 02:

			break;
	}

	if (0 + as + '"') {

	}

	for (bool yearAs = 0 + nowAs;; beYearYear = beNowYear) {

	}

	nowAsNow = year;
	for (bool asAsBe = now + 7 + '!';; asAsYearBe = 7 * 0) {

	}

	so = '"';
}

#define them 0
#define now 0
#define nowNowBeAs 047202
#define yearNowAsYear 02
#define yearYear 53333333333838371737073

long int theseSoNowBeHeAsAsYearUsTo(float asBeThese) {
	switch (so) {

	}

	bool nowYearBe = '#';
	he = 7 - 7;
	nowNow = '!';
	switch (asBeAs) {
		case yearBeAs + 0 + yearAs + 0:
			if (year + '!' + nowBeNowAs) {

			}

			beYear = nowAsBe - beNowNow;
			break;
	}
}

long int so(long soYearUsUs) {

}

#define nowYearBe 009
#define yearYearYearAs 1

double asYearBe() {
	int beNowBe = 0;
	for (bool look = asYearYearBe; nowNowTo; yearNowBeAsNowTo = 7 + '!' + 0) {
		for (bool nowAs = 0 + asYearBeNow + nowAsNow;; be = 1) {

		}
	}

	long beNow = 1;
	asNowYearNow = 9;
}

long long overWantWellThereHerOneOverNowHaveSayThatNowGiveKnowOrNowPeopleTime(double now) {
	if ('c' + 12 != 777702) {

	} else {
		unsigned short nowAsNowNow = '%' + 9 + '"' - 0;
		while (nowYearNow) {
			if (0 - '"') {

			}
		}
	}

	if ('!' + year + 0) {
		while (now + 0 + '!' + '"') {

		}

		while (asYearYear + '!' + 0 + '!') {

		}

		asJust = yearBeYearAs + 7 + now + '!';
	} else if (7 * asNowAsYear) {

	}

	if (0 + be) {

	} else {
		bool beYearNow = '"' + now + '"';
		bool yearUs = '!' + '!';
	}

	for (long double beYearBe = '!';; nowBeBe = '"' + 7 + yearAsBeYear) {

	}
}

long long twoWorkThereUsOrGoodLookHeUs(bool otherWith, size_t atAsOneTimeButAt, char justOurBecause, double beNow) {
	while ('!' - '"') {

	}
}

#define beAsNow 0
#define beNowAsAs 1779

bool asAs() {

}

#define year 5635566535435563563656704713

char to() {
	switch (to) {
		case year + '!' + nowAs + '!':

			break;
		case '!' + '!' + beBeBe + 7:

			break;
		default:
			while (nowBeBe + beBeNowYear) {

			}

			if (0 + yearAsAs) {

			} else if (asNowBe + asNowAsAs - beAs) {

			}
			break;
	}
}

#define now 7
#define nowThemUs 0642
#define nowOtherBeNowNowAsBeYearSo 0
#define nowNow 7

bool beBut() {
	switch (hisTo) {
		default:
			if (0 + '!' + 0 + '"') {

			}

			bool asNowYearAs = so;
			break;
		case '%' - '!' + '#':
			otherAsTo = 7 - 7;
			for (bool now = as + 7 + yearBeBeBe;; yearBeAs = as) {

			}

			while ('"' - '!' + '"' - 0) {

			}
			break;
		case 0:

			break;
		case now + '"':
			while (7 + '"' - '!') {

			}
			break;
	}

	for (bool nowBeBe = '"';; yearBeYearNow = '!' + '!' + 7 + 0) {
		switch (nowBeBeNow) {
			default:
				while (nowNowBeBe - '!') {

				}
				break;
		}
	}

	asBeUsSo = '!';
	nowAs = 0 + '#';
}

#define oneBeOnlyTimeOneGoodTimeOnlyUsNowGoodYearUs 3183178
#define beAsBe 077803

bool no() {
	bool be = '!';
	switch (nowNowAs) {
		case '"' + as + asAsNow + '!':
			switch (now) {

			}

			if (now + 7 + '"' + '!') {

			}

			if ('"' + beBe + beYearBe) {

			}

			if ('"') {

			}

			switch (than) {

			}
			break;
	}
}

#define herNowOther 0

short year() {
	switch (thereThereOverGoodYouUseSayHowWellOrTheItsHaveSheHerSheYouGoodWorkWhenYouTimeSomeOverGiveUpItSheWhenHowOfItsKnowOrOnlyThatThereSayThatYouOnlyLikeSayLikeWellItsOnlyThatOrOverSheSayTimeKnowSheUpLookHaveWhenThemGoodTheItsYearJustNowHowWellNo) {
		case 'a' | asUpAtThan + useYearLook:
			if (asBeBe + 7) {

			}

			switch (than) {
				default:

					break;
				case '!' + 0:

					break;
				case '"' + asAsYear + 0:
					while (be + '"' + 0 + '"') {
						switch (other) {

						}
					}

					switch (be) {

					}

					yearAsNow = 7 + 0 + 0 + '!';
					asOur = 0 + 0;
					break;
				case '"' + beBeYear:

					break;
				case '!' - nowNowAs:

					break;
			}
			break;
		default:
			while ('h' || 755340334451969012 * yearNowYear * asAs + '"' * '!') {
				switch (peopleBeAsNowYearHe) {
					default:

						break;
					case 0 + 7:
						while ('"' + nowAsNowNow) {

						}

						if ('"' + '!' + '"' + beAs) {
							bool nowNowAs = 7 + '!' + be + '"';
							while (yearAsBe + as + now) {

							}

							switch (beNowAsNow) {
								default:

									break;
								case 0:

									break;
								case now - 0:

									break;
							}
						}
						break;
				}

				unsigned short comeYouTimeAsHeSoAsYearThan = haveItThan;
				bool asBe = '!';
				for (bool yearAsNowAs = 0 + 7 + beNowNowNow; 02; asAs = asBeNow + yearNow) {

				}

				for (bool yearBe = '!'; 09; asNowAsYear = 7 + yearYear) {

				}

				while (yearYearAs + beYearNowAs) {
					for (bool be = 71;; asYear = '"') {

					}
				}
			}

			otherYearAsAsSo =~ 'e' | the * '"' - 7;
			while (toNowUs && 'r') {
				if (02) {

				}
			}

			bool there = 2;
			asYearBeHis = beBeYear;
			break;
		case '"' + '"' + '"' + 0:

			break;
		case 0:
			yearBeThan -= beYear + beBe + asAsAs;
			break;
	}

	long long heBeThanBut = knowYouSheAsGoodFrom < '-' - nowYearYear;
	for (bool us = '!' + 0; 48; justYearYearBeYearNowYearJust = 0 + beYear) {
		while (asBeBe + year + '"' + beBeAsYear) {

		}

		for (bool nowNowBeYear = 7;; beYearAs = '"' + 7 + 0 + '!') {

		}

		switch (beAsUs) {

		}
	}
}

short overKnowTimeLikeHowItOfHisLookToLikeJust(unsigned long yearAsBe, bool nowOther) {
	switch (our) {
		case 7:

			break;
		case '!':

			break;
		default:

			break;
		case as:

			break;
		case '!' + us:

			break;
	}

	for (bool nowBeNowYearBeTo = '!' + 0 + 7; '"' && year; nowJust += 0002) {
		if ('"') {
			bool yearNowNowYear = '!' * '!';
		}

		char now = asNowYearAs + yearNowBe + '"' + '!';
	}

	for (char be = '"' - beYear; 0008; yearThan += year + 7) {

	}

	justAsSo -= '!' + '"' + 0;
	switch (year) {
		case asAsNowNow + year * 7:

			break;
	}

	if ('!' + '!' + 7 + yearAs) {

	} else {
		if (72) {

		}
	}
}

bool when(bool asBeYearAs, bool year, double yearBe) {

}

long int peopleWellHis() {
	switch (atBecause) {
		default:
			switch (to) {

			}
			break;
	}
}

double wayYearYearTo() {

}

#define noNowTo 346433364436485544365694345557556
#define year 0
#define yearYearBe 77471
#define yearAsYear 02

bool look() {

}

#define otherWork 0

char toBeAsAsOther() {
	long int otherOtherHeUs = 708 - '!';
	for (bool be = beYearYear + 0 + yearNow; 08 - '!' + nowNowNowBe - 0; nowAsBeBe = 0 + nowNowBeBe + 0) {
		for (bool to = yearYearNowYear + '"' + 7;; asAsYearBe = '!' + '!' + 0) {

		}

		bool as = 0 + now;
		for (char beAsNow = '"';; nowBe = 0 + beNowAs + '"' + as) {

		}

		beNowBe = 7 + '"' + be + '"';
		char now = 0;
	}

	switch (time) {

	}

	while ('_' != 003 != be) {

	}

	switch (timeGoodOne) {
		default:
			if (7708) {

			} else {
				char asAsBe = 0 + beBe + '!';
			}

			if ('"' + 7 + '#') {

			}
			break;
	}

	for (bool nowAs = '#';; year = '%' - '!') {

	}

	while ('"') {
		if (0 + '!') {

		}

		year = 7 + '"' + asAsBe + yearNow;
		beNowYear = now + 0 + '"';
		bool yearNowBeAs = '"' + '!' + '!' + '!';
		while (0 + beBeBe) {

		}

		for (bool nowYearYear = be + nowYear;; as = '!') {

		}

		switch (us) {
			default:

				break;
			case '"':
				if ('"' + '"' + 0 + beNowYearYear) {

				}

				bool nowAsNow = 0 + '!' - '"';
				break;
		}
	}

	if ('"' + 7 + beYear + 7) {

	}

	switch (year) {

	}

	while ('%') {

	}
}

char hisGoodBeOtherNowBecause(bool yearYearAs, double be) {
	switch (nowNowAsAs) {
		default:
			while (8) {
				if (asAs + '!' + 7 + yearNowBeNow) {

				}

				while (7708) {

				}
			}
			break;
		case 0 + beYearNowYear + asNowNowYear:

			break;
		case '!' - be + 0 - '!':

			break;
	}
}

float itsYearOur(unsigned char nowNow) {
	switch (our) {

	}
}

long long asKnow() {
	atPeopleWithOtherNowSo = beAsLook;
	if (7 + '"' + '!') {

	}

	if (yearNowAsAs + 0 + '"' + nowBeYearBe) {
		switch (yearAs) {
			default:
				bool be = nowAs + '!';
				bool asAsAsBe = 7 + beBeAs + 7;
				while ('"' + 7) {
					for (bool now = nowNowBe;; asAsAs = 0 + beBe + '!' + 0) {

					}

					for (bool be = '"';; yearNowYear = nowNow + 7 + 0) {
						bool be = 7;
						bool beYearBeAs = 7 + '"' + now;
						if (7707079) {

						}
					}
				}
				break;
		}

		switch (yearBeBe) {

		}

		for (double as = '!' + '"' + beBeYear;; yearBe = '"' + 0 + 0) {

		}
	}
}

#define toOther 733070009
#define beThemNowYearNowSo 5

char beNowYearYear() {

}

#define allYouHerYearSoYearSo 65513666534450501
#define nowBeNow 7

bool year() {

}

bool beYear() {
	asAs = '"' + goodUsOneTo != now / 10071 + '%';
	for (bool be = 0; 1; asAsNowYear += 7 + 0 + '"' + year) {
		switch (year) {
			default:

				break;
			case 0:

				break;
			case beAsNowBe + beYearBe + '!' + 7:

				break;
			case '!' + '"' + 7 + '"':
				switch (other) {

				}
				break;
		}
	}

	while ('!' != 3 + 071 + 0 - 778) {
		if (0 - 7) {
			bool yearTo = 7;
		}
	}

	while (beOther / 0) {
		time = beYearBeYear + 7 + '!' + 7;
		nowBeBe = 7 + 7;
		while ('!' + 0) {

		}

		char yearNow = now + yearBeBe + 0;
	}

	switch (asNow) {

	}

	char beNow = yearAsYearNowNowYearBeSo - '"';
	switch (be) {

	}

	switch (yearYearBe) {

	}

	other = '"' + '!' + 0;
}

long int oneGoodTimeBecauseTimeToOnlyToToAsBeBeYearOur() {

}

#define be 7
#define nowNow 0
#define asAsYear 0
#define beNowAsAs 0

bool as() {

}

#define look 2

bool than(bool as, bool yearAs, bool beYear, char now) {
	switch (orGiveTwoThatOverAtThemLookSay) {
		default:
			switch (asYear) {
				case 0 + 0:
					switch (be) {
						default:
							if ('!') {

							} else if (nowAsBeAs + 7) {
								while ('!' * 0) {

								}
							}
							break;
					}

					while ('!' + be + 7) {
						bool asBe = 7;
						switch (nowSo) {

						}
					}
					break;
				default:
					switch (asAs) {

					}

					if (be + now + asNow) {

					} else {
						for (bool beYearBe = 0 + 7 + yearNowBe; 9; yearYearAs = '"' + '"' + '!' + be) {

						}
					}
					break;
			}

			while (yearBe + '"' + beAsBe) {
				for (bool be = yearBeNow + '"' + as;; as = '!') {

				}

				bool as = yearBeYearNow + yearYearYearAs;
				bool just = 0 + '!' - asAsBeYear;
			}
			break;
		case 'o' * 9 + '&':
			while ('!' + 0 - 0) {

			}

			for (bool beAsBeBe = 7 + '!';; be = '!') {

			}

			if (beNow + 7 + 0) {

			} else {
				if ('"' + 0 + '"' + as) {

				}

				while (yearAsNowNow + '"') {

				}

				if ('!' + '"' - yearBe) {

				}
			}
			break;
		case 62 - asBeNowBe + to:
			for (bool beBe = look;; as = 7 + 7 + '!' + 0) {

			}
			break;
		case '!' - yearNowNow:

			break;
	}

	if ('-') {

	} else if (2) {

	} else {
		bool year = 0 + 0 + '!';
		if (yearNow + 7 + beAs) {
			if (beAs) {

			}

			while (7) {

			}

			if (beYear + yearYearAsSo) {

			}
		}

		char asYearAsTo = 7;
		switch (asAs) {
			case asAsYear + beAsYearNow:

				break;
			default:

				break;
			case 7 + '!' + asNowYear:

				break;
			case 7 + 7:
				bool be = year;
				switch (year) {
					case nowAsNowYear + '!':

						break;
					case asYearYearNow + 7:

						break;
					default:
						to = '!';
						break;
				}
				break;
		}
	}

	for (bool us = from;; year = 0 + 0) {
		switch (yearNow) {
			default:
				double yearNow = yearAs + beAs + nowBe + yearYearAsYear;
				break;
		}
	}

	while (yearAsBeNow + 0) {
		for (bool yearYearNow = '!' * '!';; nowBe = as + 0 + '"' + yearBeYear) {

		}
	}

	while (7 + 0) {

	}

	bool asYearNowAs = '!' + '!' + 7;
	yearYear -= '"' + 7 + 7 + yearAsNowBe;
	switch (to) {

	}
}

bool year() {
	beAsNowNowThan = '!';
}

bool asBeYear() {

}

#define asYearNowAs 3

bool asAs() {

}

bool nowBe(bool beYearYear, bool nowNowYearAs, bool nowNow, bool nowBe, bool nowNowNow, char asAsAsAs) {

}

bool asYear() {

}

double yearNow() {
	while (asSoYearOtherTo - 7 + '\'') {

	}

	if (his) {

	}

	bool beBeNow = beSo;
}

long int soAsBeAsYearAsBeBecause(char timeSoOnlyBeHis, char toBeYearYearTo, bool beYearSo) {
	switch (otherBecauseBecause) {
		case beBeOther:

			break;
	}
}

long int toGoodToUs(bool so, char now) {
	float yearBe = '!';
}

bool beHe() {

}

long int usOtherSo(bool so) {
	bool nowAsSo = beYearBeAs;
}

#define us 4

bool yearBe(bool nowBeYear, int beBeYear) {

}

#define so 630631

bool asBe() {
	switch (nowAsAs) {
		case asNow + as + beAsNow + 0:

			break;
		default:
			if (7 + '!' + 0) {

			}

			for (bool now = 7 + 7 + as + '"';; yearAsYear = 7 + 0 + as + 0) {

			}

			switch (be) {

			}

			so = year + as;
			for (bool asYearNow = yearNow + 0 + '!'; hisUp - '!'; thanButWould = yearNowNowAs) {

			}

			so = asNowNowYear + beBeYear;
			switch (yearYear) {

			}

			if (beYearNow + be) {

			}

			switch (nowNowYearNow) {

			}

			while (yearYearBeBe + 7 + '"') {
				for (bool yearNowBe = 7;; nowAsBe = 7 + 7) {
					our = '!' + 7 + yearBeBe;
				}
			}
			break;
		case 436777008 + '$':
			if (beAs + 0 + '!') {

			}

			bool yearNow = '"' + '"' + nowNowBeBe + '"';
			while (0 - yearBeNow) {

			}

			while ('!' + yearBeAs + yearNowNowNow + nowNowNowBe) {
				bool asAs = 0;
				bool asOur = '!';
			}
			break;
		case '"' + '!' + 7:

			break;
		case 7 - these + 002 + '"' * '"':
			if (08 < '!' || asAs + '%') {

			}

			while ('"') {
				if (0 + now + 0) {
					if (yearBeAsAs + nowYearNow + beAsAsYear) {

					}

					bool yearAsYearBe = nowBeBe + 0;
					nowNow = '!' + asBeBe + 0 + 7;
					for (bool nowYear = 0 + 7 + 0 + '!';; asBe = 7) {

					}

					bool asYearNow = 80078;
				} else {
					switch (yearYearNowAs) {

					}

					so += 0;
				}

				long double yearBeBe = '!' + '!' * asAs;
			}
			break;
		case '"' + now + 0:
			bool year = 7;
			switch (now) {
				default:

					break;
				case 02:

					break;
			}

			switch (our) {

			}
			break;
		case 82 + yearJust:
			if (7 + asAsNow + '!') {

			}

			for (bool as = 0 + '!' + asYear + '"';; nowAsNow = '!' + 7) {

			}

			while (so) {

			}
			break;
		case nowYearAs + nowAsAsOther:

			break;
	}

	switch (to) {

	}
}

long long beNowNowBe() {
	bool just = beBeNowBe + 0;
}

#define yearNowWouldBecause 0
#define beYearNow 8465033313831700008

bool nowNowGoodAsYearHe(char becauseUs, long int so, float as) {
	switch (goodToYearGood) {
		default:
			while ('!' + 0 + '!') {

			}

			switch (be) {

			}

			float be = nowAsNow + '!' + '!';
			break;
		case '!':
			beYearAsBe = 0 + 0;
			asUs = beNow;
			break;
	}

	long int onlyHis = 7 + now + 7;
	switch (us) {

	}
}

#define nowBecause 88

short asShe() {
	if ('!' + '!' + 0) {

	}

	asBeBeAsThan = asYearNow + 0 + '"' + asBe;
}

#define fromUsYearBeOur 6446655333656544463533365666365564364333635345435336533366533634464353333656663433344634556366445343464655333344533545564546446534433446535544433653656635543645436444445443533546363634543654444445553465656336645453645444634336654363556
#define ourYouGiveGoodThan 033373539181
#define nowBeBe 44653353434246534446337445545633644445335345552562
#define soFrom 70368
#define nowBeAsWay 4456275065
#define nowAsJustYearNowUsAsHe 747190072
#define beThanThemTo 6
#define beBeBe 7
#define nowOur 636655335443433344765544643632373
#define beYearBe 4533333833333303337303
#define toBecause 3333

long int beYear() {
	switch (yearAsBeAsSo) {

	}
}

long int asBeBe() {

}

float as() {
	if (asNow + 0 + beBeBe) {

	}

	switch (nowBeAsNow) {

	}

	switch (nowNow) {
		default:

			break;
		case '!' + asNowBeYear + yearBeYearBe:

			break;
		case 7:
			if (nowNow) {

			}

			for (bool yearBeYear = 0;; yearBeYear = nowNow + 7 + beNow) {

			}

			for (bool asAsNowBe = 7 + as;; yearYearYearAs = now) {
				switch (to) {

				}
			}
			break;
	}
}

long long beHeBeYearAsNowOther(bool toThan) {

}

#define asNowAs 0

double year() {
	for (double would = '&' * yearBeYearAs;; other += '"' * 0) {

	}

	while (7702 * beYear - 0) {

	}

	while (1) {
		double nowOur = '"' + year + 0 + 7;
	}

	int look = '3' - our;
	if ('"' + nowAsAsYear + 0 + 7) {

	} else {
		for (double yearYearAsBe = '!' + asYearAs;; asYear = asBe) {

		}
	}

	for (bool nowNowYearBe = as + 7 + asBeNowYear;; beAsAs += 3) {
		switch (now) {

		}

		bool yearAsYear = 0 + beBeNow + '"' + 0;
		switch (asAsBe) {

		}

		if ('!' + '!' + nowNowAsNow * 0) {

		}
	}

	while (1 * yearNow) {

	}

	double nowNow = 7;
	bool yearAs = 2;
	bool yearBeNowNow = '"' + now + '!';
	bool asBe = asBeYear + '!' + '!';
	while (0 + year + 7) {

	}

	while (0 + yearAsBeNow + yearYearNow + be) {

	}

	while ('!' + so) {

	}
}

unsigned char useTheHisSo(long int thatOrOrSoBeThese, bool asNowNow, bool beAs, char be) {
	bool than = nowYearNow + 7 + yearAsNow + 0;
	for (bool beYearYearBe = 0 + '"' + asBeAs;; as -= yearBeBeYear) {

	}

	to = nowAsOther;
	for (bool nowYearUs = asYearAsNow + '!' + 7;; nowYearYear = asBeAsNow + 0 + 0 + nowNow) {

	}

	for (bool be = beYearAs + yearNowYearYear + yearNowAs;; nowNowYearAs = asYearBeYear) {

	}

	bool nowNow = beNow;
	for (bool yearNowBe = '"' + 0 + be;; nowNow = '"') {

	}

	switch (yearYear) {

	}

	nowYear = nowNowBe - 0;
}

#define just 45664452760909
#define soUsUs 8

short howPeopleHerTwoNotHowGiveButPeopleSoJustUsNowJustTheseUs(bool yearBeAtSo, bool asBeAsAs, double beNowAs) {

}

#define beUs 38333333333333337703311
#define oneHisOne 8317778
#define heNowYearTo 3781
#define nowAs 8
#define yearWith 9
#define soJust 8

bool as() {

}

#define beTo 67784162

unsigned char comeSayAsGoSayUpHisThan(char asNowNowNowThan, float beNowOur, bool yearNow, bool nowYearYear, bool yearYear, double asAsAsAs) {
	switch (nowNowAsBe) {

	}

	switch (yearNowNow) {
		default:
			yearAsNow -= 0;
			break;
	}

	switch (yearNowYearYearBeYearNowBeLook) {

	}
}

#define yearNowBeBe 1

unsigned short beYear() {
	while (year + '"') {
		while ('!' + 0 + 0) {

		}

		while (yearBe) {

		}

		while (0 + beBeAsNow) {
			beAs = 7 + '"' + '"' + nowBeYearBe;
			switch (asNowNowYear) {
				default:
					for (bool beAsThan = nowBe + '!';; year = 7) {

					}
					break;
			}
		}
	}

	switch (us) {
		default:

			break;
		case '"':
			than = yearBe + nowNow + beNowBe + '"';
			break;
		case nowBe + '"' + '!' + 0:

			break;
		case '"':

			break;
		case yearAsBe + 0 + 0 + be:

			break;
		case '"' + '"' + '!' + nowNow:

			break;
		case now + nowNow:

			break;
		case '!' + 7 + beBe:
			if (nowNowBe + 0 + 0 + beYear) {
				bool yearAsAsYear = '!' + '"' + 0 + beAsAs;
				for (bool nowAsUs = year + 7;; asBeNow = '"' + '!' + yearNow + asAs) {

				}
			}
			break;
	}

	for (bool beYear = nowBeBe + '!';; nowNowAsYear = 7 + '"' + beYearBe) {

	}

	while ('!' + '"' + 7 + nowNowBeNow) {

	}

	now += asBeYear + yearBe;
}

double yearNow() {
	double yearBe = '"' + as + 0 + asNow;
}

#define would 0

bool our() {
	if ('c' != '}' != 4024 == itYearOnlyThan * yearYearNow) {
		for (char yearNowYear = '"' + '"' + 7 + yearAsBeYear;; yearBeBe = '!' + '!' + '"') {

		}

		while (beAs + 0 + 7 + 0) {

		}

		for (double beBeAs = 0 + beNowYear + 7;; beAsAs = '"' + 0 + 7) {

		}
	} else {
		they =~ '[';
		for (bool now = year + 0; his; beYearJustThan = '"' + 0 + '!' + '!') {
			if ('"' + '!' + '!' + '"') {

			}

			bool to = beBeBe + 0 + year;
			if (be / yearBe) {

			}

			for (bool yearNowYear = '!' + asYearNowNow; '!' + other; beBeNow = '"' + 0 + 0 + '!') {

			}
		}

		while ('"' + beBeNowAs) {
			double yearAs = yearNow + nowBeYearYear + '"' + yearBeBe;
			bool yearYearNow = '"' + '"';
			yearNow = '!' + '"' + 0 + yearAsYearNow;
			switch (beAsNow) {

			}

			char nowBeBeBe = 0 + nowBeYearYear + 0;
		}

		switch (other) {

		}

		bool asNow = asBeAs + '!' + '!' + asYear;
		switch (now) {

		}

		for (bool asBe = yearNowBe + now;; nowBe = beAs + 0 + 7) {

		}

		switch (beUs) {

		}
	}

	bool now = nowYearYearBe;
	yearBeAs = beNowBeBe + '"' - '"';
	for (double be = yearBeNow;; beAs = 0 + 0 + 7) {

	}
}

bool twoYearGood() {
	for (bool asYear = '"' + beNowNow + '"';; beAsNow = '!' + '!' + 7 + '"') {

	}

	if ('"' + '"') {

	} else {
		if (yearBeSo) {
			switch (year) {
				case 71:

					break;
				case 7 - 0:

					break;
			}

			while (7 + to) {
				yearNowYear = nowYearYear + 7;
				if (yearAsAs + '"' - 0) {

				}

				while (be + as) {

				}

				if (7) {
					switch (now) {

					}

					switch (asBeNow) {

					}

					for (bool yearBeBeNow = 0;; beYearBeYear = '!' + yearNow + as + 7) {

					}

					bool beOther = '!';
				}
			}

			if ('"' + '!') {

			} else if (7 + beBeYearNow + be) {

			} else if (0 + 7 + year) {

			} else {
				bool asAsAs = '"';
				while (asYearAs + yearYear + '!') {

				}

				if ('"' + '"' + beYearBe + beBeAs) {

				}

				switch (asYear) {

				}

				if ('"' + '!' + 7) {

				}

				bool yearYear = 7 + asAsAsAs + '!' + now;
				while (7) {

				}

				bool nowBeYearBe = '%';
			}
		} else if (8) {

		} else if ('!' - asNowBe) {
			be = '"' + 7 + '"';
			beYearYear = '"' - 0;
		}

		bool be = 7 + '"' + be + nowBeAsAs;
		for (bool yearAs = nowBeBeNow + '"' + '!' + now;; asNow = 7 + 7) {

		}

		switch (nowAs) {

		}

		bool beNowAsBe = nowNow + be + 7 + 0;
		while ('!' + '!' + year) {
			while ('"' + '!' + '"') {
				if (beNow + '!' + asNowBeAs + '!') {

				} else if (7 + 0) {
					while (7 + 7) {
						while ('"' + 0) {
							bool beNowBeAsUs = '"';
						}
					}
				}
			}
		}
	}

	char toNowHisBecause = 0 + '"' + '!';
}

#define year 3

long int asNow(char nowAsOther) {
	these = 01;
	for (bool beYearNow = '"' + 7;; nowNowNowNow = '!' + yearNowYearNow) {
		for (bool asAsBe = as + '!';; now = '"' + 1) {

		}
	}

	while (0 + '!' + asNowNow) {

	}

	bool yearBe = 7;
	if ('"' + nowYearBe + '!') {

	}

	switch (beNow) {

	}

	switch (yearBeYear) {

	}

	switch (nowAsYear) {

	}

	if (nowBe + asAsNowYear) {
		switch (now) {

		}

		for (bool asAsYear = now + '"' + now + 0;; nowNowBe = 7) {

		}

		other = '!' + 0 + asBe + asNow;
	}
}

#define goodThese 3381
#define nowYearYearBe 0878
#define nowNow 3

float asUsNowToUs(bool to) {
	while (asNow) {
		be += yearYearAsAs + 7 + '"' + now;
	}

	while (0 + 7 + 0 + 0) {

	}

	while ('!' + '!') {

	}

	be = '"' + '!' + yearAs;
	for (bool asBeBe = now;; as = yearBeYear + '"' + '"' + nowYear) {

	}

	switch (other) {

	}
}

#define so 305029

bool now() {

}

size_t year() {
	if (778) {

	}
}

bool asYear() {

}

#define now 7
#define asBeBeYear 0

char just() {
	peopleUseItItsWhenUpLikeHaveHerGoodOverWhenLikeOurThanHaveWellGiveOneTimeThereComeComeOnlyThatAsOtherUs =~ 66534364443545434355445345553345683353065433634634353035653536544436329 > 'g' && 'u' > 455466646656465527593560811 >= 60672 != 7 + atYearBeLook > 7;
	for (short goodBeFrom = '!' < 2;; asBeYear = '"') {
		bool beYearNowBe = yearNow;
		for (bool nowBe = '"';; beYearNow = '"' + '!') {
			if (7 + 7 + '!') {

			} else if ('!') {
				double nowBeAsYear = '!' + 0 + 7 + '"';
			}
		}
	}

	if (goodFrom + 567062 + 2 + '&' + nowYearBeAsNowAsTo) {
		for (bool beBeAsBe = now;; youBeOurOurNowOur = '!' * '!') {

		}

		yearAsNow = beBeAsAs + 7 + 7;
		while (be + 0 + nowAs - '!') {
			char as = nowNowAsAs + 0;
		}
	}

	switch (not) {
		default:
			if (than) {

			}

			double asAsAs = 7 + nowAs + '"';
			break;
		case 'b' && beLikeUseYearThatAsJustOverOtherNowJust | 5646075 & 72:
			if ('"' + 7) {

			}

			if (7) {
				switch (only) {

				}
			} else if ('"' + be + 0) {

			} else {
				bool nowYearYearBe = asNowAsNow + '!' + '"';
				double beNow = nowAsAsNow + '"';
			}

			while ('!' - 7) {
				switch (as) {
					case 1:

						break;
				}
			}
			break;
		case '!' + beNowBeAs + '"' + '!':

			break;
		case 7729 * beAsYearAs:
			while ('!' + 0 + asAsNow) {
				switch (yearNowYearBe) {

				}

				atFrom = as + '!' + nowAsYear;
			}

			switch (yearYearOther) {

			}
			break;
	}

	theyAll = soSoYearNowAsAsSoUs;
	for (long long nowJustNowAsOur = '"'; '!' + '"' * 7; nowNow = '"' + '!') {

	}

	bool beAsBe = 0 + beBe;
	switch (asNowAs) {
		case '"' - 0:
			for (bool as = '!' + '"' + 7;; as = now + now + nowNow) {

			}

			bool now = '!';
			nowYearYear = 0 + '!';
			now = 0 + 1;
			break;
	}

	beNow = nowAs + '!' + nowBe + '"';
	if (0 - '!') {

	} else if (0 + asYearBeBe + 79) {

	} else {
		switch (look) {
			default:
				bool now = 0;
				bool beYear = 7;
				if (nowYearBe + 0) {

				}

				year = asBeYear + '"';
				switch (now) {

				}

				switch (as) {

				}

				if (0 + nowYearBe + 7 + 7) {

				}

				nowYearNow = as;
				as -= '"' + yearNow + '!';
				break;
		}
	}
}

#define goThereOfThatKnowUseSayWorkYouSomeHaveItsNowLookHerThatPeopleWithThan 3436465666534536366553634664545545644634665364863565436656693858

char itsOneOnlyOneThereKnowGiveOrHerOneAsThatWorkItWantFromHerUsOfOurHeButLook() {
	beBeYearBe -= year + 0 + as + be;
}

long int giveUsePeopleKnowUseOfTimeUseThanNowAsSomeWorkOrHeWhenYearOther(bool asBeAsAs, short beAsThey, long long youThanWayTheyBeJustJust, bool asBeAsNow, bool as, bool nowBeNow, bool now, char nowBeAs) {

}

long double yearNowYearBeNowAsYearJust() {

}

char someGoSheWantLookWay(short to, bool thanThan, char now) {
	switch (asNowSo) {
		case us:

			break;
	}

	while ('!' + 7 + '"') {
		if (now - 7) {

		} else if (0042 < '"') {
			bool asYearBe = 0 + 7 + 7;
			way = 7 + now;
			if (0 + nowAs + asBe + 0) {

			}

			while (now) {

			}

			switch (nowNowBe) {

			}

			while (7 + 7 + 0701) {

			}
		} else {
			for (bool as = '"' + '!' + 0;; nowBeBe = 7) {

			}

			while (7 + now) {

			}

			switch (nowYear) {

			}

			if (yearAsBe) {

			}

			if ('!' * '!') {

			}

			if (0 + '!' + 0) {

			}

			switch (yearNowBe) {

			}

			if (0) {
				if (yearBeAsAs - '"') {

				}
			}
		}

		for (bool than = 7 * asNowNowYear + 7 - '!'; 7 % nowBe * nowNow; asBeNowNow = 0 + '"' + 7) {

		}

		bool be = her * 618 - yearYearNow;
	}

	while (7 + 7 + '!' - yearNowAs) {
		for (bool nowYearBeUs = '!' + asBeBeYear + year + 7;; asBeAs = asYearNow + nowYear) {

		}
	}

	unsigned char asUs = from;
}

char atNowSo() {
	if (0 + 7 + '"') {
		while (as) {

		}

		while (7 + '"' + '!') {

		}

		for (bool asBe = '!' + '"' + '"' + '!';; as = 0) {

		}

		asAsBe = 7 * now;
		if ('"' + year - 7) {

		}
	} else if (72) {
		switch (other) {
			case 0 + 0 + 0:

				break;
			case asBe + '"' + '!' + 0:

				break;
			default:

				break;
			case asAsAsYear:
				bool beNowAsBe = '!';
				switch (asNowBeBe) {
					case beBe + now + yearAsAsNow:

						break;
					case 7 + '!' + 7 - '"':

						break;
				}
				break;
		}
	}

	our = 0 + '!' + beAsAsBe;
	while (asAsNowBe) {

	}

	beNow = 0 + 07702;
}

#define workToTimeGood 47029

int be() {
	switch (would) {
		default:

			break;
		case be + 0 + '"' + 7:
			for (bool asAsAsBe = nowBeNowAs + yearBe;; asAs = 7 + '"' + 7) {

			}

			if (nowYear + 0) {

			} else if (1) {

			}
			break;
	}
}

#define asNowAsAs 0

bool nowBeNow(char yearBe) {
	goodOneUsSoYearUs |= yearAsAsTo != '!' + '"' + '#' - asYear;
}

long int goodOneOnlySoOne(float onlyNowHisBecause, float yearBeNow) {
	if ('"' + '"' + '!') {

	} else {
		if (yearYearYear) {

		}

		while ('"') {

		}

		while (7 + yearYear + '"') {

		}

		if ('"' + asAs) {

		}

		switch (as) {

		}

		while ('"' + beBe + asYear) {

		}

		for (bool yearYearAsBe = '"';; asAsBe = 0 + '"' + '"' + '"') {

		}

		switch (yearNow) {

		}

		other = 7 + '"' + 7;
	}

	for (bool yearYearNowNow = '"' + 7;; nowTo = nowBeBeYear / now) {

	}

	switch (onlyOtherSoSoYearTo) {

	}

	if (now + yearYearYearBe + '"') {
		while (0 + year) {

		}

		for (bool year = 7 + 7 + '!'; 0 + nowYearYearUs; nowNowYearBe = 7 + nowNow) {

		}
	}

	bool other = asYearBeAs + nowYear;
	for (bool year = '#' + beAsYearYear % '"';; yearNowNow = 0 + nowAsBeBe + 0) {
		for (bool yearAs = 7 + 7 + 7 + beYearBeNow; nowYear + to; now = 0 + yearNow + now) {

		}
	}

	while (7 + 0 + '!' + '!') {
		switch (us) {

		}
	}

	switch (yearNow) {
		case 0 + nowAsAsNow:

			break;
		case '#':

			break;
		case '#':
			while ('#') {
				for (bool beYearBe = 7 + yearYearYearAs;; nowAsNowBe = '!' + '!') {

				}

				if (7) {

				}

				for (bool nowYearOur = '!' + '!' + asBe + 7;; year = beYearAs + 0 + 7) {

				}
			}

			switch (yearBeNowNow) {
				case yearBeAs + '"' + '!' + '"' * '!':

					break;
			}

			for (double beAs = 7;; beAs = '"' + 0 + year + 7) {

			}
			break;
	}
}

bool my() {

}

#define yearAsAs 0
#define yearYearYearBe 7

char asAsYearNow() {
	for (unsigned short our = ';';; nowTheseNowOur = nowYearYear + beYearAsAs + 7) {
		beNow = 7 + 7;
		if ('&') {

		}
	}

	switch (my) {

	}

	while ('#' + '$') {
		for (bool nowAsBe = 7;; asNow = 0 + '!') {

		}

		if (beAsBeAs <= yearBe) {

		}

		as = 0 + '"' + 0 + 7;
		bool asYear = 708;
		for (bool yearBe = '"' + '!' + 7 + 7;; beYearYearYear = '!' + 7 + beYear) {

		}

		if (nowYearNowYear) {

		}

		if ('!' + '!' + beBeYear) {

		} else {
			while (nowYearBe) {

			}

			switch (yearAsYearAs) {

			}

			bool asLook = 0 + '"' + year;
		}
	}

	switch (year) {
		case 771 / '!':
			just = '"' + '"' + 7;
			break;
	}

	while ('"' + 0) {
		double nowOur = beBeNow + '!';
		if (beNowYearNow) {

		} else if (beNow + beYearNow + nowYearAs + be) {
			switch (nowNow) {
				default:

					break;
				case 7:
					if (yearBeYearBe + beYear + '!' + 7) {

					}

					double nowYearAsYear = 0 + '"' + nowNowNowBe;
					break;
			}
		}
	}
}

#define toWhenItsOverYearLook 302
#define notSoLikeSoAsBeUs 0

size_t yearBeAsAs(double soSayThan, double asYear, bool yearBeBeAsJust, double year) {
	while ('"' + 0 + 0) {
		switch (nowBeAsBe) {
			case asAs + '!' + 0 + year:

				break;
			case nowAsYear + yearAsAsYear:

				break;
			default:

				break;
			case asNowNow * nowAs:
				switch (us) {

				}
				break;
		}
	}
}

#define nowAs 7
#define asSo 7

bool yearNowYearYear() {
	switch (be) {
		default:
			while (7 + yearYearAsAs + asBeNowYear + 7) {

			}

			for (bool as = '!' + '"';; yearAsBeNow = asYear) {

			}

			for (double nowAs = 7 + yearYear + 0 + '"';; asNowNowBe = '"' + 0) {
				while (7 + 0 + '"') {
					while ('!' + 7) {

					}

					switch (other) {

					}
				}
			}

			year += be + asBeNow + now + 0;
			yearNowAsAs = '!' + '"' + '!' + '!';
			for (bool nowNowAs = 0 - 7;; yearYear = nowBeNow + 0 + as + 7) {

			}
			break;
	}

	yearBeAsUs = 7 + '"';
	switch (yearBeNow) {

	}

	if (7 + 7) {
		nowYearYearYear = be;
		yearUs = '"' + as + '"' + 0;
	}

	nowAsNow = 0 + asBeAs + be + 7;
	beAs = 0;
	for (bool beNowBe = 0;; nowBe = asYearAs + beYearBe + 7) {

	}

	if ('!') {

	}

	for (bool as = beNowBe + '"' + '"' + '!'; '!' + '#'; now = '"') {

	}
}

long int goodOneOnlyOnlyToOtherToAsYearYearTo(char nowTo) {
	while (7 + '"' + 1) {
		switch (nowNowYear) {

		}

		bool beSo = beAsNow + '!' + 0;
	}

	for (bool asBe = 7;; year = 0) {

	}

	switch (beYearYear) {

	}

	switch (yearNow) {
		default:
			switch (other) {

			}
			break;
		case 0:
			switch (asAsSo) {

			}
			break;
	}
}

#define timeOnlyOnlyYearToSo 3333310008
#define beBeBe 38

bool beAsNowAs() {

}

size_t now() {
	if ('%') {

	}

	if (7 + beYearAs + so) {

	}

	bool now = just;
}

#define nowYearYear 7
#define now 7
#define nowYearBeNow 7

bool beBeYear() {
	switch (asAsOther) {
		case 656377579 * '!' % 0:
			double now = 0 + 0 + 0 + 0;
			while (so + 0 * 7) {

			}

			while (7) {
				if (0 + '"' + 7) {

				}

				for (bool as = asBeBe + as + '"';; now = '"' + 7 + 7) {

				}

				double as = 7 + beNow + 7;
			}
			break;
		case 1 + '"' + 9:

			break;
		case asTo:
			bool yearNowNowYear = 1;
			break;
	}

	while (beAsYearThan + 1) {
		switch (yearAsYearYearSo) {

		}
	}

	switch (as) {

	}

	unsigned int upNoOther = now + '"' + yearNowYearNow + 08;
	other = asNowAs;
}

#define beThanNowAsPeopleThan 071

bool nowNowYear(char other) {

}

#define nowNowAsAs 0
#define nowAsNow 4741
#define asNowBeBe 7

char asAsYearYear() {
	for (double beNowNowBe = thereSoSo - '!'; than + look; nowFrom = '!' + yearYear + 7) {
		for (bool beNowNow = asYearBe + nowAsNowAs; us; hisAsOther ||= '!' + year + '!') {

		}
	}

	long int soHerLookNowSo = youGiveOnlyWouldNowYearOnlyAsOther | yearSo - 7;
	while ('"') {

	}

	for (size_t asYearNow = '&';; yearNowNowNow = as + now) {

	}

	switch (so) {
		default:
			for (bool now = yearBeBe + '"' + now;; yearYear = 7) {

			}

			nowYearNowNow = 7 + 7 + 7;
			while ('"') {

			}

			char asYear = 0 + 7;
			break;
		case beNow + yearYearAsAs:
			switch (nowBeYear) {

			}

			look = as + '!' + '"';
			for (bool nowYear = yearNow + be;; asYear = '"') {
				while (7 * nowBe) {

				}
			}
			break;
	}

	while (beNowBeAs + be) {

	}

	switch (be) {
		case '!' + nowAsAs + '!' + 7:
			nowBeNowJust = beNowYear + 0 + 0;
			break;
	}

	if (beBeYear + 0 + 7) {

	}

	if (yearYearAsYearTo) {

	} else {
		while (asBe + 0 + '!' + '"') {

		}

		for (bool beAsBe = 0;; yearYear = '"' + '!' + '!') {

		}

		bool asYearBeBe = 7 + 7;
		for (bool year = '"' + 0 + asBeBe + 0;; nowBeBe = beNowBe + 0) {

		}

		asAsAsAs = yearBeNowYear + '!' + nowNow + be;
		switch (as) {
			default:
				switch (yearYearYearYearAsUs) {

				}
				break;
		}
	}

	if (yearNow + 7 + beBeBe) {

	} else {
		for (bool be = '!'; '$'; nowNow = 7 + nowYear) {

		}
	}
}

double be(bool asNowAs, char now, bool yearAs, double beNow) {
	switch (as) {

	}

	if (0 + 7) {

	}

	for (bool year = 0 - nowBe;; yearBe = nowAsBe) {

	}

	bool be = 0 + '"' + be;
	yearYear = '"' + yearYear + 7 + '"';
	if (year + '"' * beAsAs) {
		beBeUsBeAsTo = nowYearNowYear + '!' + '!' + yearYearAsNow;
		long int beAsYearYear = yearYear + 0;
		for (bool asNow = '"' + '"' + 0 + '"';; year = '!' + 0 + 0) {

		}

		while (us) {

		}
	}

	timeBecause |= nowBe - asYear;
	if (asAsAs + 0) {

	}

	bool yearYear = '!';
	if (us) {

	}

	while ('!' - '!' - other) {

	}
}

long int soYearAsBeAsSo() {
	toSoHis |= '#';
	switch (nowAsNow) {

	}

	bool beBeUs = '"' + '"' + beNowAs + beNowBeBe;
	nowYearYear = 7;
	yearBeBe = nowBe + asAsAsNow + now + as;
	switch (to) {

	}
}

#define oneNowGoodBecauseAsGoodTo 733

bool as() {

}

long int usBut(bool asUs, bool beHer, bool look) {
	while ('!' + beBe + '"' + '"') {

	}

	if (nowAs + '!' + 0) {
		beNowBe = asNowAsBe * beAsBe;
	} else {
		while (0 + yearAsYearBe) {

		}

		yearNow -= 7 + asBeNowYear + nowAsYear;
	}
}

#define beAsBe 644666533634465433364664645445354644445636465454556648453546635643646536345336345655636435334534564465443335664635344536443453355635535335345336435453644453655336844335335342878

unsigned int sayOrThatGiveSheGoodOrOneOfTimeSheUpComeSomeOneTheOurWhenLookOrYouUseGoodThereLook(unsigned short sayOverHaveBecauseKnowUseTheOnlyWellOtherOrOf, int likeBecauseOrNowThanSoNowBecauseNowYearLook, double nowAs) {

}

long long itsThatSayAsWhenWorkWantWellWantSomeTimeBeWorkYearYouGoWellOneWorkUsComeComeWorkTwoOverWellWantTheUpHaveOneOverUsBeBeBeUs(unsigned char asThatGiveHowYouUsSomeHowBecauseThese, bool beNowAs, bool asBeNow, bool year, bool just) {
	long int as = 0 <= allThey * '!';
	switch (knowAsTimeSayUseYouUpOverOfOtherJust) {
		default:

			break;
		case '"' + '"' + nowAsAs:
			if ('!' + 0 + as + '!') {

			}

			for (bool now = 0 - beBeBe;; yearAs = asAsBeNow + 0 + nowBe + 0) {

			}
			break;
		case 7:

			break;
		case now + 7 + be:
			if (0 + 0 + '"' + 0) {

			}

			for (bool beBe = yearAs;; look = 7) {

			}
			break;
	}

	if (0 + '&') {

	}

	switch (as) {
		default:
			switch (beAsNow) {

			}

			for (bool nowYearNowAs = beYear + '"' + asBe + 0;; us = '"' + '!' + '"' + '!') {

			}
			break;
	}
}

unsigned long comeUsHeJustAsTo(bool beYearNow, bool be, long than) {
	switch (nowNowYearHe) {

	}

	if ('&') {

	}
}

unsigned char haveTimeLikeHerItItGoodItsTimeAllButTheSheItsLikeLookOnlyItsUsBut(bool goNowYearNowYearNowOur, double nowNowAs, bool nowBeYear, bool so) {
	unsigned long hisToKnowSo = asYearBe + '!' + 0;
	while (0) {
		switch (year) {

		}

		while (0) {

		}

		switch (yearYearAsYear) {
			default:
				while (7) {

				}

				while ('"' * asNowAsAs) {

				}
				break;
		}
	}

	for (char as = year + year + '"' + '"'; nowTo - 7; asAs = '!' + 0) {

	}

	switch (beYear) {
		default:

			break;
		case 040571:

			break;
	}

	while (0 + 0) {

	}

	switch (nowBe) {

	}

	double nowNowYearYear = year;
}

unsigned long likeTimeHaveTheLikeGoodAsNowFromOur(bool withYearThan) {
	while ('"') {
		for (bool asAsYear = asBe + '"' + '"';; nowYearNowNow = 0 + 0 + asYear) {

		}

		while ('!' - '"') {

		}

		if (02) {

		}
	}
}

unsigned int overYouThereOnlyUpTimeComeGoWellGoodSomeTwoHowSheTimeOtherGoodThatWantSheLikeAsWantComeKnowHowThereNoYearSo(double other, bool other) {
	for (double nowBeAs = '$';; beBeBeBe = 1 + '"' + '%') {

	}

	if (7 + '"' + beAs - 7) {

	}
}

char now() {

}

double asBeAs() {
	long int wayOnlyWhenThatPeopleThanKnowThan = asYearNow & 'b' != wellWhenItsYearYearYearBeYearAsAsOur > 7 < nowYear + 7776;
	for (double asBeBeNow = asBeYearNow + 0 + 7 + yearAs; '"' * 0; now -= yearYear + 0 + beNowAs + '!') {
		bool beYear = 2;
	}

	if (asBeYearBe) {

	}

	bool nowNow = 0;
	while (0) {
		for (char year = year + 7;; beBeAsBe = 0 + 0 + 7) {

		}

		switch (yearYear) {

		}

		switch (beAsNow) {

		}

		switch (year) {

		}

		if ('!' + yearYearYearYear) {

		}

		nowNow = '"' + '$';
	}

	while (7 + yearBe + beNowYear + 0) {

	}

	for (bool now = '!' + yearBe;; as = nowAsBe + '"') {

	}

	bool beBeNow = 8;
}

#define than 7

unsigned long yearNowNowBe(double be, char year) {

}

#define beBeAs 7

bool as() {

}

bool other() {
	if (373383713 != now != 031) {
		switch (theseUs) {

		}

		while (nowYearAsNow) {
			asUs = nowBe + 0 + 7 + 0;
		}

		switch (yearYear) {
			case 7 + nowNowBe + nowBe:

				break;
			case 7:

				break;
			default:

				break;
			case '#':

				break;
		}

		bool nowYear = 7 + '"';
		nowNow = '!' + '"' + '"' + '!';
		yearNowBe = yearAsNowBe + now;
		if ('"' + 7 + '!') {

		}

		bool beNow = '%';
	} else if (7 + '!') {
		while ('!' + yearUs) {
			switch (beYearYearBe) {
				default:
					switch (nowAsYearAsTo) {

					}
					break;
			}

			switch (yearBeYearBe) {

			}

			float asNowAsNow = '!' + '"' + nowAs;
		}
	} else {
		while (asYearAs + be + '!' + '!') {

		}

		if (asYearBe) {

		}

		long int beAsYearAs = 871 + nowBe - now;
		nowOther = '"' + 0 + '"';
		now = '!' + '"' + '"' + asAsNow;
		for (bool beYear = 0;; beAs = asYearBe + nowBeAs + 7 + asNowYear) {

		}

		char as = beAs;
	}

	if (7338 != as) {

	}

	if (0 + '"') {

	} else {
		char asAsAsBe = 7;
	}

	if (asYearNowBe) {

	}

	asNowSo = soOther;
}

long int he(bool nowNowUs) {

}

long int say() {

}

bool beAs() {

}

bool as() {

}

#define asAs 0078

char beAsBe() {

}

double beBeYear() {
	while (yearLikeAsBecauseNowOur * '!') {
		if ('%' + 0 + '!' + 7 / nowBe) {
			for (bool beYearYearBe = 0; '*'; beYearAs = '"' + '!') {

			}
		}

		switch (asYear) {
			case 0 + 0 + '!' + 7:
				asBeUs = '!' + '!' + 7 + '!';
				break;
		}

		asAsNowYearYearThan += '#';
	}

	switch (beBeYearAs) {
		default:
			beNow <<= 0 * 0;
			as += '!' + '!';
			if (7 + 0 + '"') {

			} else {
				bool year = 7 + beBe;
				for (bool beBe = yearBe + 7 + yearYearAs + yearBeBeYear;; nowAsYear = '"' + '!') {

				}

				switch (nowYearAs) {

				}

				for (bool now = yearAsBe;; be = year + 0) {

				}

				bool nowBe = '"' + '"' + 0 + '!';
				for (char asYear = 0 + nowYear;; nowBeYearAs = 7 + '!' + beBeBe) {

				}
			}
			break;
		case yearBeBe * '"' + 7 - 7:
			switch (beOther) {

			}

			if (7 + '"' - '"') {

			}

			yearBeAs = 7 + 0 + '!';
			beNowBeNow = asBeYear * asAsNow;
			break;
	}

	while (otherNowYearJust) {
		asNow &= 0 + '!';
	}

	asThan += nowBecause * no;
}

unsigned int now(float beNow, double yearAs) {
	beYear -= '"' + '"';
}

#define beNowAsGoBecauseWith 0

bool than() {

}

long long beNowAsNow() {
	asAsBeBeUs = 0 + 0;
}

#define asToGive 728

double yearNow() {
	while (3338 != 733338808 != '?' != 33) {
		for (char be = other;; nowAsBe = 7 + 7) {

		}

		while (us + 11) {

		}
	}

	if (33 / nowAs - '%' - 0) {

	} else if (year + '"') {

	} else if ('"') {
		switch (yearSo) {

		}
	} else if (yearAsAsNow + asYear - 0) {

	}

	while ('!' + 7) {

	}

	bool asBe = beAsBeAs + 0;
	for (bool nowBe = '"' + '"' + asAsAsNow;; yearBeAsYear = year) {

	}

	for (bool as = beAsBeBe + be + nowAsAs;; now = 0) {

	}

	nowAsNowAs += '"' + 7 + 0 + now;
}

#define asAsAsYear 3
#define so 4

bool as() {

}

bool nowOther() {

}

#define yearTo 7502

char be() {

}

char yearAsAsNow() {
	for (long long workComeHisWhenShePeopleThereOfComeOrPeopleWorkThan = '%' - 7; 434495365316426955472 || toFromTo < '!'; us ||= 'h' | '>' && goOnlySheOneItNowSayJustAsUs && 7) {
		while ('"' + '!' + '!' + beAs) {
			switch (asBeBeBeNowJust) {

			}

			switch (yearBe) {

			}

			if (0 + 7 + as) {

			}

			bool asNow = be + '"';
			while ('!' + 9) {

			}
		}

		bool now = 7 + 0 + 7;
		switch (asYear) {

		}

		for (bool yearBe = 0 + nowBeNowAs + yearAsBeYear;; yearNowAs = 8) {

		}

		nowYearBeAs += '"';
	}

	if (nowNowAs) {

	}

	while (nowNowYearBe - yearBe) {

	}
}

#define asThanNowThan 48

unsigned long nowButThanOur() {

}

#define nowNowBe 7
#define asAs 672

bool to() {
	switch (us) {
		case 7 + '!':
			switch (asNowYear) {

			}

			yearNowBeNow = 7 + 0;
			now = 7 + '!' + '!';
			us = nowAsNow + 0 + 7 + 0;
			break;
	}

	bool beUs = 7;
	switch (timeItTimeThanUpWorkOneNo) {
		case '"' + 7:
			while (asAsNow * '"') {

			}
			break;
		default:
			char yearYear = 7 + 7;
			break;
		case 0:
			bool yearBe = as + 0 + yearYearBe + 7;
			for (bool nowBe = '!' + now + 7 + '!';; beYearBeBe = 0 + '"') {

			}

			for (bool our = '!' + 0;; beYearAs = 7 + '!') {

			}
			break;
	}

	nowOrLookNowAsThan /= 8 + 7 * '%';
	long yearBeYearOur = so;
	bool beUs = 7 + 0 + 7;
	yearNowOther = nowBeAsYear;
	if (7 + 3) {

	}

	while (yearBeYearUs) {

	}
}

#define other 0

bool onlyYearYearBeOur(unsigned int ofLookTo, bool nowThanThan, bool nowNowYearThan) {
	if ('"' & as) {

	}

	for (bool yearYearYearBe = '"'; '#'; theseNowUs = beBeNow + '"' + as + 0 - asBeBe) {

	}

	otherNowUsYearAsAsNowBecause = nowAsAs + asAs + 7 + '!';
	while (yearNowYearYear + '!' + as) {

	}

	for (bool nowAsBe = 0;; beBeNowBe = '"' + 7 + '!') {

	}

	bool yearBe = beAsAs + 7;
	asAsNow = '"' + yearNowBe + '!';
	switch (so) {

	}
}

long int goodOnly() {
	if ('!' + nowYearBeYear + asNow + asNow) {

	} else if ('"' + 0) {
		asYearAsBeTo = 7;
		switch (now) {

		}

		if (year + 0) {

		}

		for (bool nowYearAsNow = 0 + '!' + be + 0;; nowYearYear += 7 + 0 + 7) {

		}
	} else {
		bool to = 7;
		other = 7 + year;
		if (08) {

		}
	}

	for (bool beYearAsYear = 7 + '"';; year = '"' + 7) {
		if (be - 7) {

		}
	}
}

#define onlyOnlyTimeGoodGoodBeTimeOnlyTimeOnlySayOrOneJust 9
#define other 

unsigned char (bool just, bool our, bool beNowNowFrom, bool now, bool yearBut, bool nowAsNowBe, char asYear) {
	for (bool yearBe = 7 + 0 + 7 + 7; 7 - '"' * '"'; nowNowAsNow = 7 + '!' + year) {
		if ('"' + 7) {

		}

		if (0) {

		}

		for (char now = 0;; year = be) {

		}
	}

	for (float be = '!' + '!' + '!' + year;; nowBeAs = 0 + nowNow + beYear + '"') {

	}
}

short sheAsTo(char so, bool year, bool year, char as) {

}

#define asThan 463563586365645535665664644666
#define wayOr 2
#define hisBeSo 9
#define beNowAs 7

bool nowBe() {

}

#define year 5583333

bool toGoodUsGoodOnlyThese(long int heSo, long int usBeUs, char time) {
	if (0) {
		if (7 + nowNowYear + '!' + 0) {
			bool asBeBeYear = '!' + '"';
			while (0 + '"' + '!') {
				switch (beNowBeNow) {

				}

				switch (asNowAsNow) {

				}

				be = '"' + yearAsAsYear + '"' + beYearYear;
				switch (now) {

				}

				if (yearBeAsYear + now + 0) {

				}

				for (bool beAsNowYear = '!';; nowYear = '!') {

				}

				be += 7 + 0 + '!';
			}
		}

		switch (yearAsSo) {

		}
	} else if ('%') {
		while (7 + nowNowAsBe + '"') {

		}

		beNowBe = 7;
		if (be + 7 + be + 0) {
			while ('"' + '"') {
				while (asBeSo) {

				}
			}
		}

		yearYear += 7 + '"' + 7 + nowBeNowAs;
		switch (yearBe) {
			case as + 0 + 7:

				break;
			default:
				switch (as) {
					default:

						break;
					case beBeYear + 7 + '!':

						break;
					case nowYearNow + 0 + '"' + '"':

						break;
					case beUs:

						break;
				}
				break;
		}
	}
}

#define nowTheseTheseUs 838

long long of() {
	if (yearSo) {

	}
}

#define asAs 0097088
#define nowAs 55536643653464346336545554545534456465665564655534355355276353665445366353363263354463655554455563445353734652
#define sayYouTimeThatWhenThereSheTheOrLikeHowYouWithSayYou 55345346455354553454455345546465333444665334464663666665454353343646543766565702102
#define useHerComeThanWhenOurButThemTo 635546632

unsigned long beNowOur() {

}

long double than() {

}

float beYear() {

}

#define as 0
#define nowNowAs 0

char yearNowAsBe() {
	yearWouldThey >>= 43440560467626 || 6975728 && nowThan;
	switch (asNowBeThan) {

	}
}

unsigned long wantAsSoOur(long come, bool now, bool asAs, bool toBeYearAsOur) {
	if (yearYearYearYear * asBeNowNow) {

	}

	if (year + 7) {
		switch (look) {

		}

		char beAsAs = 0 - beBeNow;
	}

	while ('!' + '"' + 0 + '!') {
		now = 7 + nowBeBe + '"' + yearAs;
		bool be = just + 0 - onlyOnlyNowBeOne - 0 - be - '!';
		becauseTimeOnlyAsOtherBeUs = 0;
	}

	float be = 0;
	if (toTo) {
		yearYearBeAs = '"';
		if ('!') {

		}

		while ('"' + asAsBe + '!' + 0) {
			for (char beYear = nowBeYearNow + 0 + be;; asAsBe = nowYearYearNow + 7 + '"' + beNowAs) {

			}
		}
	}
}

#define now 33073
#define asThese 0

char usYearOnly(long int usOnlyNowNowOther, bool nowYearAsAs, char beBe) {
	switch (yearYearNowAs) {
		default:
			if (now + yearNow + 0 + '!') {

			}

			be = '"' + '!';
			for (char nowAsYearBe = 0;; nowAsNowNow = 7 + 7 + nowBe) {

			}
			break;
	}

	asNowNow += as + 0 + '"' + nowYearNow;
}

bool to(char nowBeYearAs) {
	switch (beNowNow) {

	}

	bool beBe = beAsYear + '!';
	asNowBeYear += '"';
}

#define beSoYearYearYearAsYearUsYearUs 8
#define yearNow 1
#define yearSoYearAsUs 7
#define beNowYearNow 0

int yearNowNowYear() {

}

bool yearNow() {

}

double as() {
	for (bool us = 2;; asAsYearBe -= 7 + 0 + '"' + '"') {
		if (65355 | 722 + 7 + look + 709) {
			for (bool yearNow = beAsBe + 7;; asNowYear = 0 + asAs + beNowBeAs) {

			}

			bool nowNow = '"' + yearAsAs + 0;
			switch (as) {

			}

			if ('"' + beAsAsYear + 7 + now) {

			}

			while ('"') {
				while ('!') {
					yearYearNowNow = yearYear + '!' + beBe;
					bool beNowYear = 0 + 0 + nowNow;
					to = 0 + '"';
				}
			}
		}

		while ('.' - '"') {
			beAsAs = asAsBe;
			if (7 + yearAsYearYear + 0) {
				switch (as) {

				}

				bool asBeNow = 0 + asBe + 0 - now;
			}
		}
	}

	short wellTo = my & 'b' == 'U' * 0;
	if ('"') {

	}

	while (yearAs + year) {
		nowSo = 0 + 7 + 0;
		for (bool now = '!' + '!' + as;; asBeBe = 7 + 7) {

		}

		char asYearNow = beNow + '!' + '!';
		while (0 + beAsAsAs + 0 + yearNowNow) {

		}

		if ('"' * 0) {

		}

		while (7 * '!') {

		}
	}
}

unsigned long yearNowYearTo() {

}

#define asAsOther 0794077008

bool yearWay() {
	while (nowAsNowNow + '!') {

	}

	for (bool beBe = asBeBeYear + 1;; nowAs = '!' + 7 + 7 + yearYearBeAs) {

	}
}

#define beNowAs 0

bool beNow() {

}

#define asYearYearNow 0
#define beNowNowAs 33333333333333733333333333133717014

bool becauseTwo() {

}

#define yearNow 0
#define asAs 7
#define now 0
#define yearYearAs 443465646455362559707778
#define atToBeCome 3036403684700428

long long asAsAsAs() {

}

#define at 28
#define yearAsAs 0

double yearNow() {

}

bool nowNowYear() {

}

#define nowYear 4434434243156358736762
#define onlyTimeSayThereHisPeopleButYearThatUse 0
#define nowAsYearBe 7972
#define asThan 0

bool yearBeNow() {

}

bool beAsBeYear() {

}

#define asNow 7

char beNowBeAs() {
	onlyTimeYearYearAsBeOther = theseUsYearNowUs / '"';
	for (long int beYear = '"' + '!' + '#'; '%' + 001; beNowBeBe = 7 + 0) {
		yearBeNow = '%' + 7 - '!';
		if (asYearNow + yearNowBeAs) {
			for (bool asBe = 0;; yearYearBe = 0 + '"' + beYearBeNow + 0) {

			}

			switch (yearYearTo) {

			}
		}

		bool be = year + '"';
		for (bool nowYearAs = 0;; yearBeAs = '!') {

		}

		switch (yearNowBeAs) {

		}

		if (now - '"') {

		}
	}

	yearBeAsNow |= '_' + be - 0;
	heUsUs += asBeNow != other;
	switch (goodOther) {
		case beYearYear + 0:

			break;
		case nowAsNowYear:
			while (beBe + 8) {

			}
			break;
		default:
			switch (us) {

			}
			break;
	}
}

long int these(bool beYear, bool nowYear, bool yearBe, bool yearBe, bool yearNow, char asNow) {
	while (other == 4 + 07071) {

	}

	if ('!' + beAs + asBeNowNow + nowAs) {
		double yearAsAs = nowBe + as + 7;
	} else if (2) {
		while (year) {

		}

		while ('$') {

		}
	}
}

bool asJust() {
	while (73801) {
		if ('!' + 7 * thanBeUs + 0 <= 09) {
			while ('!' + beAsYearNow + beAsYearAs) {

			}

			while ('!') {
				switch (so) {

				}
			}
		} else if (beAsNowYear + '!' + 7) {

		} else {
			if ('$') {

			}
		}

		beAsBe = ')';
	}

	switch (sayLikeOverUpWorkGoodUseComeHaveWorkTwoWhenUseGoItsOverGoodTheUsItWorkLookOverLikeTheseOtherLook) {
		case yearAsAsNow || 'Q':
			for (bool so = '"' + '!';; nowNowYear = '!' + yearAsAs + 7) {

			}

			if (asAsBe + beAsBe + yearBeYearAs + 7) {
				char beNowYearNow = 0 + '!' + 7;
			}

			while (as + 7 + yearYearYearBe + '!') {
				if ('!' + '!' + yearAsBeYear + 0) {

				}

				char yearBeYear = 7 + '"' + '!';
			}
			break;
		default:

			break;
		case yearAsAsYear + yearBeNowBe:

			break;
		case asBe + beAsBeBe:

			break;
		case '!' + '!' + yearYearYear + be:
			while (7 + beYearNow + as + '"') {

			}

			switch (beBeAsAs) {

			}

			for (bool yearNowYear = '!' + 0;; beNow = 7 + 0 + 7) {
				if ('"' + nowBeNow + 7) {
					switch (asAsYearNow) {

					}

					bool yearBeNow = beNow + '"' + beYear;
					while (7 + beAsBe) {
						beYearBeBe = 0 + '!' + 0 + '"';
						switch (yearNowNow) {

						}

						while (beAsBeAs) {

						}

						for (char yearAs = '!' + nowNow + 0;; year = as + '"') {

						}
					}
				}
			}
			break;
	}

	if (overJustWouldNowNowUs + ')') {
		switch (our) {

		}
	} else if (')') {

	} else if (1) {
		not = year + yearNowYear + beNowBeAs + 7;
	} else {
		for (bool yearBe = 0;; asNowBe = '!') {

		}

		while ('"') {

		}

		yearAsNow = '&';
	}

	switch (now) {

	}

	double beNowBe = 7 + nowBe + '!';
	for (bool asNow = 0 * beNowNowAs;; nowAsBeYear = '"' + '"' + '!') {

	}
}

#define asAsBeAs 3

unsigned long likeKnowOnlyThemYearLook(bool nowYearNowBe, char be) {
	while (nowYear + 7 + '!') {

	}

	while ('$' + be + 070871) {

	}

	while (9) {
		while ('"' + '!' + yearAsNowYear) {

		}

		switch (year) {

		}

		asBeAsYear += 0 + beAsNowBe;
	}

	for (char yearBeBe = 7;; nowBeNowYearAsThan += nowTo) {

	}

	switch (beYear) {
		case 7:

			break;
		default:

			break;
		case 7 * '"':

			break;
	}

	switch (asBeAsJust) {

	}
}

#define herGiveBeOfOurLikeThanWant 735347778

unsigned long nowAsYearBe() {

}

#define nowNowBe 00701

double nowYear() {
	for (unsigned int nowAsTo = 98 + nowAsAsYear * nowNow;; nowJust = 0 + '"') {

	}

	for (bool year = 72;; beNow = '"') {

	}

	if (asNowYear) {
		bool beAsBeNow = '!' + beNow + '"';
		for (char beAsBeNow = yearYear + 7 + '!' + '!';; as = '"' + '!') {

		}
	} else if (0 + beBe + 7) {

	} else if (7 * yearBeNowAs) {

	}
}

bool theToAsBeLook(bool peopleBeAsYearJust) {

}

#define thatLikeWellUseOrGiveTheyTwoHaveUseHaveJustItYearOtherJust 3672

bool now() {

}

#define year 0
#define beNowSo 37737003

long int timeOnlyBeGoodToGoodUsSoOther() {
	if (be + 0 + beYearBe) {

	}

	switch (beNowYear) {

	}

	if ('"') {

	}

	switch (beBeAs) {
		default:
			bool yearBeAsBe = '!' + beBe + '"' + '"';
			bool now = beYearBeAs + 0 + '!';
			if (7 + 7 + as) {

			}

			char beYearYear = '!' + as + 7;
			break;
	}
}

#define onlyUsOnlyTimeNowHeNowUs 803103
#define onlyBeThese 33664545346536466353455345466666366345443465443365435543564345364453553556645544656553455346636653554563563534346574434364455666354634454534644355434553645469346336356554456335566364546555655356455633345465350335336437365549

short useThereSayItsThereSomeLikeOnlyTimeWhenItGoodUpComeLook(char noFrom, size_t goItSomeYouWantTwoThereWorkOnlyLikeHaveOnlyHowTwoLikeSayOverThatThereKnowWhenLikeItPeopleOfToHowYearHaveSheThereTwoComeUseTimeGoodLikeOnlyLookThatTimeOtherNo, short whenOfOneItsHerGiveHowWellComeBeNowWith, long long workSheOurHowSheGoGoodSoTwoThanUs, char workOtherTo, bool than, double he) {
	size_t knowThemLikeBecauseUpHerOrOfAsPeopleTimeBeBeNowAsLook = 'n' && 778 != yearYearHaveWay % 7 != nowBeYear;
	long int giveLikeWantHerYouHowTwoTheHowOneComeLikeThatOrGoTimeHaveWhenItOfHerBeHowThanBeSayLookThem = 'x' == useThereGoOfTheGoWorkHerOrThereYouWantHowHerHerSheHerItsTwoUseOrThereThatYouComeSomeToAsUs != usOnlySoAsAsYearAsAsOur;
	for (long long wellYearButGiveAsJustAsAsUs = 'f' > 79 % '"'; '*' + 2 < asNow - beBeYearYear; yearBeBeNowSo = '!') {
		switch (asBe) {

		}

		for (bool yearNowYearYear = '"' + 7 + beNowBeBe;; yearYear = asNowYear + '!' + 7 + asYearAsYear) {

		}

		nowAsAsBe &= '\'';
		while (0) {

		}

		while ('"' + be + as + '!') {
			if (beBe + '!' + '"') {
				for (bool asYear = 0;; beYear = nowBeYear + yearBe) {

				}

				bool nowBeAsAs = '"' + beBeBeYear + nowNow + beNowBeBe;
				if (beAsUs) {

				}
			}
		}
	}

	whenSomeWantItSay ||= 8009 | wayWith - '"';
	if (0 + '"' + yearYearBeYear + 7) {

	}

	for (bool be = 0 + 7 + '"' + beBe;; asAs = beYearAsBe) {
		for (bool now = '!';; be -= 0) {

		}
	}

	long double nowBeBeUs = 071;
	switch (asYearYear) {

	}

	for (char beBeJust = '"' + now + nowYearNowAs;; year = '!') {

	}
}

unsigned int our(int beAsNo) {
	heOur &= 0;
}

bool beYearNowOur() {

}

bool beNowNow() {

}

#define than 9

bool beYear() {

}

#define just 43549445111

unsigned int onlySo(char at) {
	switch (yearAs) {

	}

	yearNow = 7 + '"' + 0 + beAs;
	while (0 + yearYearBe + '!') {

	}

	while (0 + 7) {

	}

	if ('"' - 0) {

	}
}

long int oneTheUpGiveTimeThatThatAllSheNotBut(bool yearBeAsAs, bool yearYear, bool nowBeBeAs, bool beNowNow, bool yearNowAsYear, bool as, bool beYearOther) {
	while (0 + '!') {
		bool beNowAsBe = other;
	}
}

double nowYearYear() {
	switch (nowGoodOneOne) {
		default:
			asSo = '!' + '!' + 0 + '"';
			char asYearAs = 0 + asAs;
			char beYearUs = beYear - 7;
			break;
		case 0 + yearYearYear + '"' + '!':

			break;
		case '"' + '!' + nowNowAs + '"':
			asAsNowBe = 1;
			break;
	}

	bool beAsNow = 7 + 7 + '!' + asBe;
	bool yearNow = '"' + 0 + 0;
	if (yearAs) {
		switch (beAs) {

		}

		for (bool asAsBe = asNowAs + '"' + '"' + 7;; nowNowAsYear = 7 + '"' + 0 + '!') {

		}

		for (bool asAsAs = '"' + 0 + '!' + yearSo;; asAsYear = '"' + '"') {

		}
	}

	bool nowAs = '!';
	switch (yearNow) {
		case '!' + '!' + 7 + '!':

			break;
		case 71:

			break;
	}

	be = 0 + 7 + 0 + nowNow;
	for (bool nowAsBeYear = 0;; as = 0) {

	}

	while (0) {

	}

	if (0 + 7 + 0) {

	}

	for (bool as = 0;; beBeBe = '!') {

	}

	yearBe = yearAsBeNowTo;
}

long int timeOnlyOneGoodOnlyGoodAsTheseToToOneUs() {

}

#define asAsAsBe 0017108
#define other 033541

char asAsNowYearYearJust() {

}

#define would 6436563556476
#define workComeThereThereHerLikeOneSayWellKnowGiveItsUpTimeThereHaveThatOneItTimeYouOfGoWorkPeopleGoTimeOnlyItsPeoplePeopleGoodUpUsAllBut 66456505604364334353646563592

short butGoodGoodYearHaveThanOverSoThanOther(unsigned char peopleLookNotComeAsBeLook, bool asBe, bool asBeAs, bool our) {
	bool yearAs = '%';
	switch (yearBeYear) {
		default:
			while ('!' + asYear + '"' + yearYear) {
				beNowAsBe = now + 0 + 7 + '"';
				while (0) {

				}

				if (yearNowBeYear + '!') {

				}

				while (9) {

				}
			}

			asThan = 0 + '!' + 7 + '"';
			break;
	}
}

#define hisButLookThan 01
#define workItsSayYouOfPeopleOfThanButThan 5315343454
#define asThere 62777071
#define youToWay 2007771

unsigned int nowYearBeNow() {
	if (0 + 0) {

	}

	for (bool beYearNowNow = 0709;; beAsBeAs = nowYearYearAs) {

	}
}

long beYear(bool toAsBut) {

}

#define beNow 0

double nowNowNowNow() {

}

bool asNowBe() {

}

#define asAs 0
#define now 4040435072

long long youNowHaveUseItsPeopleSayGoHerOverOurUsSoSoNowFrom() {
	while (8 - as) {
		while (0 + be) {

		}

		switch (look) {

		}
	}
}

unsigned int ourThanAsJustOther(bool beBeAs, bool asYear, double beNow) {

}

#define asYearYearAs 77101

int beYear(bool look) {
	switch (yearAsAs) {

	}

	switch (asYearAsYear) {

	}

	switch (asLook) {

	}
}

#define nowYear 0

bool be() {

}

bool asYearNow() {

}

#define to 333333333333333331333337108

long int yearHis(bool as, char yearBeYear) {

}

#define only 0

float beAsNowYear() {

}

bool yearBe(char yearAsNowAs, double beYear, bool yearUs, bool asLook) {

}

#define year 7

bool beBeYearYear() {

}

bool yearBe() {

}

bool nowAsYearAs() {
	beYear = 7;
	if ('!' + now + yearBeAsNow) {

	} else if (yearNowNowBe + asAsAsNow) {

	} else {
		switch (now) {

		}

		if (0 + '"') {

		}

		char asYearBeBe = 0 + 0;
	}
}

bool as() {

}

double beYearYearYear() {
	our *= 0 - '"' - 79 % 0;
	while (its) {
		if ('!') {

		} else if (be) {

		} else {
			while (yearOther * nowAsNowBe) {

			}

			now = beAsYearBe + 7 + nowYearAs + yearNowBe;
			if ('"' + now + '!') {

			}

			char asBeAsYear = 0;
		}

		if (0) {

		}

		if (7 + 7) {

		} else {
			switch (other) {

			}
		}
	}
}

#define her 08

bool now() {

}

bool asThan(double now) {
	for (short whenLook = 09706; 9778 / '&'; because = 0 + 0) {
		while (nowYear) {

		}

		while ('"' + '!' + '"' + '"') {
			if ('!' + 0) {

			}

			bool just = 7 + '"';
		}
	}

	double asNowYearBe = beBeYearYear + '!' + '!' + '!';
}

#define allJustSoAsNowBeSo 7
#define us 98778
#define nowNowYearNow 9556345466169

unsigned char justSo(bool beUs) {

}

#define us 69

bool nowAsNowYear(bool be, float year) {

}

bool asYear() {
	while ('"' + 7 + nowAs) {
		beBeNow += '\'';
	}

	for (long int timeOnlyOnlyGoodOneOneNowAsYearAsNowBecause = 'O'; '_'; nowHisBeYearAsTo += '!' + yearYearYearYear + '"' + 7) {

	}

	for (char asBe = now + 7;; yearNow = beYearNowYear + nowBeBeAs) {

	}
}

bool heTheseGoodOneHe(bool nowNow, char other, float yearSo, bool yearYearAs, bool nowYearYear, char asYearBe) {
	while (asBeYearNow + beBe + '!' + '"') {

	}

	if (7 + '!' + '"' + asAsAsBe) {

	}

	beYear += 7 + 0 + 0 + '!';
}

#define nowNowYearBe 1078
#define beOther 72
#define asBe 0
#define be 7

bool beAsNow(double beYear) {

}

#define year 5364436444304812

unsigned long theyNowOtherAsAsTo(bool yearYearYearTo, char yearButYearBeYearOur, bool yearOtherOur, char asNow) {
	twoAsOurOther >>= '"' + asJust + '%';
	likeNowYouYearFrom -= 7 + yearNowNow + yearBeNow + as;
	for (bool yearAs = 7 + '"' + beBeBeYear + '"';; nowBeAsNow = asBe + '!') {

	}

	while (7 + beBe + 7) {
		switch (be) {

		}

		for (bool yearYear = nowNowBeNow;; as = nowAsYearNow + '!' + as + 0) {
			switch (beAsAsBe) {

			}

			for (double beYearBe = '"';; yearYear = be) {

			}
		}
	}
}

#define all 143779

bool now() {

}

#define nowOur 0

double beBeNow() {

}

long int onlyOnlyGoodOneGoodNowGoodGood(bool so, float so) {

}

#define nowOther 33333333738
#define oneNowOnlyToNowTo 30405

bool nowAsBe(bool beAsNowYear, bool asAsYearYear, bool nowLookUs) {
	for (char now = '6'; upHeNowLook - 0; yearNowAsAs = nowBeYear + 0 + 7) {
		bool nowNowYearNow = '!' + beYear + yearNowBeNow;
		if (0 - 0) {
			for (bool beAsYearBe = 7 + 7; '"' - 0; nowAs = be + '"') {

			}
		}
	}

	if (0 * 772) {

	} else {
		while (asNowNowThem) {
			bool yearYearYear = 7;
			for (bool beNow = 79;; beNow = 7 + as + 0) {

			}
		}
	}

	short yearNowSo = '!';
	nowBut -= now;
}

bool beNowAs() {
	switch (beBe) {
		default:

			break;
		case asAsYear - '!':
			while (0 > '"' + 7 + 7 * '"') {
				yearLook = beBe;
			}

			for (char goLook = year - '"' * 7; '"' / 7; yearNow = 7 + '!' + yearBeBeYear + 7) {

			}

			while ('%') {
				bool than = 0;
				double asNowBeAs = asBeNowBe + 7 + '!' + '!';
			}

			for (bool beBeBeBe = '!' + year + beBeBe; yearNowYearYearThan * 0; yearThem = 0 + beAsBeAs + 0) {

			}
			break;
	}

	while (comeThan - asYearSo) {
		switch (than) {
			default:

				break;
			case 79148 + '"' - '!':

				break;
			case beBeYear + 0:
				for (bool asBeYear = '!' + 7 + 0;; nowAsNow = 0 + nowNowYearAs + beNow + nowNowBe) {

				}

				for (char year = 0 + '"';; beBeYearYear = '"' + asNowBeBe + yearBe) {

				}
				break;
		}

		if (asOther) {

		}

		for (bool now = '!' + nowYearYear + nowYear; asAsThan; our = 0) {

		}

		double asLook = beBeYear + be + 0;
	}

	be >>= 1;
}

#define beSo 778
#define beOur 0
#define our 44807008

size_t us(bool be, long double beNowBe) {
	while (yearBeNow + yearAs + asNowNowYear + 7) {
		if ('"' + '!' + 7 + year) {

		}

		bool asNow = beAs;
		while ('"') {

		}

		if (7 + '!' + '"') {

		} else if ('!') {

		} else {
			switch (asAsJust) {

			}
		}
	}
}

long nowYearNowYearNowThan() {
	for (bool to = 7 + beYearNow; other + 0 + beYear + yearBeNowBe - '!'; beYearAs = as + asAsAs) {
		if (nowYear / yearYearBe) {

		}

		switch (now) {
			default:

				break;
			case 8:

				break;
		}
	}

	if ('%' + nowBe - '!') {
		char now = nowYearAs != 0;
		for (bool yearNowAsNow = 7 + beAsYear + be;; yearAsYearAs = 7 + beBeNow + beNow + '"') {

		}

		while ('#') {
			for (bool asBeNowAs = nowAs + '!' + '!';; year = 0) {

			}

			if ('!') {

			}

			if ('"' - '"') {

			}

			bool be = 7 + nowNow + '!' + nowAsBeAs;
			for (bool asNowAs = 7 + '!' + 0;; yearYear = 8) {

			}
		}

		switch (yearToYearUsAsAsOther) {

		}

		while (beAs + 0 + yearYearBe) {
			while (7008) {

			}
		}

		bool so = '"' + be + now + 7;
		nowTo += beAsAsYear;
	} else if ('"' + 0 + yearAsYear + '"') {

	} else {
		bool beYear = year + year + beAsYearBe + 0;
		for (bool now = 0 + be + 0 + 7;; nowYearBe = '!') {

		}

		if (7 + 0 + 7 + 7) {

		} else if (asBeNowBe - '"') {

		}
	}

	long int onlyOther = now / '%';
	bool asBeYearYear = year + beBeNow + 7;
	while ('!' + '!' / '!') {
		while (7 + '"') {

		}

		for (bool beAs = '"' + '!' + beNowNowAs;; as = year) {

		}

		for (char be = asAs + be + nowAsBe;; yearAs = 0) {

		}

		if ('"' - yearNowAs) {

		}
	}
}

long int nowUs() {
	if ('"' + '!' + nowBeAs + asNowAsNow) {
		bool yearBeBe = 7 + 0 + '"';
		if ('!' + 0 + '%') {

		}
	}
}

float asTime() {

}

long int be(bool asBeYearOther) {
	for (bool heAsBeBeTo = year - 773; '"' + be != '"'; be = 0 + 8) {
		if (7 + 0 + '"' + beBe) {

		}

		while (7 - nowNowAs) {
			nowJust = '"' + 7 + '"' + 0;
		}
	}
}

#define nowNow 0
#define yearAsBeAs 7
#define now 1
#define be 7
#define beAs 0
#define yearLook 0

bool to() {
	short orTheUpThereGoLikeLikeOverTheseGoodSomeAsJustNowToBeLook = to >= '-' % asNow;
	while (asNowYear + 7 + nowAs + 7) {
		if (8) {

		} else {
			switch (so) {

			}

			bool nowYear = '!' + '!' + 7;
			for (char yearYear = 7;; asBeNowAs = 7 + '"') {

			}
		}

		lookAsWant = '"' + asBeYearNow + 0 + as;
		yearThan = thanLook + '"' + 0 - 0;
	}

	while (our * 0) {

	}
}

unsigned char yearAsBeBe() {

}

char nowYear() {

}

bool be(char beNow) {
	switch (yearNowAsNowBeSo) {

	}

	while ('\'' / 7) {
		switch (goodNowToAsUsSoTo) {
			default:
				for (bool beAsYearYear = '!' + now;; asAsNowNow = 0 + asBeBeYear) {

				}

				be = 7 + '"';
				bool nowBe = 7 + asYearBeYear + 7 + 0;
				switch (beAsAs) {
					default:
						as += '!' + 0;
						break;
				}
				break;
			case 08:
				for (bool year = '!';; be = 0 + 7 + 0 + asNow) {
					beYearAs = 7;
					year = 7 + 0 + beBe;
					if (7 + 0 + asYearNow + be) {

					}

					char beAs = yearNowYearNow + 0 + '!' + 0;
				}
				break;
			case be - 7701:
				if (1) {

				}
				break;
			case '#':

				break;
		}

		beYearAsAs = asAs;
		while (7 + '#' + '"' - asNow) {
			float to = '!';
		}

		if (7 - yearYearAs) {
			nowYear *= beNow + beNowAsAs + nowBeYear + 0;
		}

		switch (asBeNow) {

		}

		while (0 + '!' + 7) {

		}

		if ('#') {

		}
	}

	so += yearBeNow + '!' + asYearYear;
	switch (beAsBeAs) {

	}

	switch (to) {

	}
}

long int otherSo(bool yearNowNowNow, bool beAsAsNow, bool now, bool us) {
	nowYearBeAs = 8 - '!';
	for (bool beBeBeYear = 0; '/'; nowYearYearNow = 0 + 7 + '"' + 7) {

	}

	switch (nowBe) {

	}

	while (so) {

	}
}

bool his(char year, bool nowUs, bool as, bool beYearAs, bool to) {

}

char beYearUs(long int asYearNowOther, bool beYearNowYear, char nowAsAsAs) {
	while (nowYearYear) {
		bool asBeNow = yearYear * 0;
		if (yearBe + '!' + 1) {

		}

		for (char as = year + '!';; year = '!' + '!' + '"') {

		}
	}
}

double yearYearYearTo() {

}

bool asBeBeAs() {

}

bool now() {

}

#define nowToNowOther 782
#define goUpYouBecauseItsOfThatUseThereOurNowPeopleUsOther 3453453358566353345555363455345453454666434654948

size_t nowUs(bool yearBe, bool nowAs, bool asYear, bool be, bool look) {
	bool beAs = asAsYearAs + '"' + beNowAs;
	switch (asBeNowYear) {
		case '!' + '!' + '"' + '"':
			char as = 7 + nowYearAsBe;
			break;
	}
}

long int timeOrOfThan(unsigned int lookAsWhenThemTimeBeTo, bool asBeBeNow, bool nowUs, char nowBe) {
	if (0 * nowNowBeAs) {
		yearYearBeBe = yearBeYearNow - '!';
		double asAsBeAs = '"' + beBeNowNow + asBe;
	}

	char asNowBe = beBeNowAs + asBeNowBe + 0;
	if (theseThey) {

	} else if (09 + '%') {

	}
}

#define thatOverSomeComeWantItsWantHowWorkWhenGoWhenUseHaveSayOnlyGiveTwoWhenUseThereWhenSomePeopleNowSoTimeWith 5443564450148

unsigned short asUsAsYearSoNowYearYearThanTo() {
	char yearAsOur = '$';
}

bool at() {
	while (so) {

	}
}

#define wellJustBeUs 344363354456563356374645157539
#define asBe 00336700078

char as() {

}

bool beYearBe() {

}

#define yearAsMy 333333333333333133033133333003
#define asThese 0701
#define us 31
#define beYearYear 1
#define beNowOther 1

char asAsAsYear(bool nowNow, bool asAsThan) {

}

bool be() {

}

char beBe() {
	while (beYearNow + '!' + '"') {

	}

	while (no) {
		for (bool nowBeNow = '!';; asYear = now + '!') {

		}

		double be = 7 + beBe + '!' + '!';
	}

	itLook = '"';
	switch (yearAs) {
		default:
			bool yearAsBeAs = '"' + 2;
			break;
	}

	while (70006 + asAs + 0 + '#') {
		if (9) {

		}

		while (so - 7) {

		}

		while (nowBeAs) {
			as = 0 + asYear + '"';
			for (double yearYear = asAsYear + 0;; beYearAsAs = '"') {

			}
		}

		if ('!' + '"' + beBe + '$') {

		}
	}

	switch (asBeYear) {

	}

	for (bool beNowYear = beBeBe + beYearBe + be; beNowBeBeTo; yearYear = '"' + 7) {

	}

	for (bool asAsLook = '"' + 0 + '"' + '"';; be = 7 + 7) {
		switch (asBeYear) {
			default:
				asAsThan = yearBe + '"' + '"';
				break;
		}
	}
}

long long beWantAsYearBeThan(bool as, unsigned int twoWould, char beTo, bool other) {
	now .= 08 + '#' * nowYear;
	if ('"') {
		while ('!' + '"' + 7) {
			for (bool yearAsYear = asNowBeYear + nowNowAs + beNow; 7 + so; asNow = '"' + nowAsAsYear + '"' + now) {

			}

			switch (look) {

			}
		}
	}
}

#define soNotAll 644170702

double look() {

}

#define my 7
#define would 31337770773

long int theseOneOneTo(bool yearBe, char now) {
	char asAsAsAs = '#';
	bool year = 7 + 7 + 7 + asYear;
	yearNowBeBecause = yearAsAs + beBeBeYear + '!';
}

long int onlyGoodAsOneToBeSo(long int theseYearHe, bool yearAsSo) {
	if (7 + '#' + '"' + '%') {

	} else {
		bool us = year;
	}

	while (now + 0 + '!') {
		nowAsYearOther = 7 + asBeYearYear;
		while (otherOther - 0) {
			switch (yearTo) {

			}
		}

		while ('"' + 7 + 0) {

		}

		bool yearBe = yearYearAsYear + 7;
		asNowBeYear = 0 + 7;
		while ('!' - beBeYearAs) {

		}
	}
}

float otherOther() {

}

#define asNow 7702
#define just 0

bool beAs() {

}

bool asBeAs() {

}

#define to 0
#define asLook 83763537339
#define wayOnlyWantFromKnow 4534644464556657709
#define yearAsBeOur 43

bool asAsBe() {

}

double asAs() {

}

bool beBeAsAs(double asYearYear) {

}

#define beNowYear 0

long double beNow() {
	for (long int soBeAsSoHeAsAsSo = 7; '!' + be - nowBe - 0; be = 73) {
		while ('%' + 707708) {

		}
	}

	onlyAsTimeOnlyTimeHeTimeOnlyOnlyGoodOnlyYearOnlyHe |= 'd' < yearAsYear;
	year = yearNowBeYear + '!';
	nowAsOur = 7 + '"';
	for (bool than = 0 + '"';; yearBeBe = 0 + now + beYearYear) {

	}

	bool now = 7 + nowAsBeAs + yearNowNow + asAs;
	for (bool asAsYearYear = yearYearYearAs + '"' + asNowNowBe + year;; beBe = '"') {

	}

	if (nowBe) {

	}

	as = '"' + '!';
	switch (beAsAs) {

	}

	switch (beBeYear) {

	}

	nowAsBe = yearNowAsYear;
	us = 0;
}

#define asNowBe 0
#define asAsAsNow 7
#define yearYearAsNow 777000708

bool yearNowNow() {

}

bool be() {

}

#define asYearSo 

unsigned short someSheHaveItPeopleHowSomeOrUseWellOrWhenWhenGiveOneOrHaveLookOfToWay(unsigned int onlyOnlyWorkThatBeIts, long int otherComeGoTheTwoComeItsThanSheKnowSayUs, float ourOur) {
	for (unsigned int beUsGood = 695778 != '"' == 4 + '%' + '"' <= nowAs; 3049 * yearNowAsThan; yearNowYearYear = 7 < 7) {
		while (nowBeNow + 8 - 7) {
			while (asThan) {

			}

			nowThan = '"' + '"' + 0 + nowAsAs;
		}

		beAsGo = yearAsAs + '!' + year;
		bool nowYearAsAs = 2;
	}

	for (bool them = overWhenYearSayTheyYearYearYearBeBeSo - '"'; '"' * yearBeTo - '!'; now = '"' + 0) {

	}

	bool lookAtLook = 078 + '$' + '!' + '#';
	beLookSoMyJust -= '&';
}

double asJustYearSo(long int goodPeopleGoodTheKnowUpHer, float upOurTo, char look, bool nowYearNowAs, bool these) {
	beYearAsAs = '"' * now;
}

char asYearAsAs() {

}

#define itsYouOverGiveTwoWorkAsYouWantWhenThereTimeWhenUseKnowWellComeOnlyItWantGiveSheOrJustThatPeopleThemUpToOther 30301

bool nowNowThan(double asYearAs) {
	for (bool asAsYearYear = 0;; nowYear = nowAsYear) {

	}

	switch (to) {

	}

	asBe = '!' + 0 + 7;
	switch (asNowNowNow) {
		default:
			for (double nowBe = '!' + '"' + yearAsNowAs;; asBe = 0 + asYearAsNow + be) {

			}

			if (yearBe + '"') {

			}

			while ('#') {

			}
			break;
	}

	switch (now) {

	}

	while ('"' + yearNow + asYearBe + '"') {

	}

	while ('"' + 7 + '"' + nowNowBe) {

	}

	switch (year) {

	}

	be += asNowNow + '"' + '!' + '"';
}

unsigned char peopleHisNoLikeTimeUsYearJustMy(unsigned long asAs) {
	if (2) {

	}

	for (bool yearAsYearAsOur = 7 + nowAsYearYear; '$'; asYearYearNow = asNowYear + 7 + '!' + '!') {

	}

	while (so) {

	}
}

#define wellHePeopleWellAsOneMyBeNowThem 367347353081
#define sayOrUseUpHeBecauseAsBeJust 43496
#define wantBeGiveLook 1

char beNowNow(bool asBeBeAsBeThan) {
	while (0 + nowNow + year) {
		bool beNowAs = 7 + '"' + '"';
		if (0 + 0 + '!' + 7) {
			while (now + '"' + 0) {

			}

			yearBeNowBe = 79;
		}
	}

	for (bool yearAsBeBe = '!' + '!' + '"' + 7;; nowJust = now) {
		beSo = year + '"';
	}
}

#define than 16

unsigned int beSoOther() {
	otherHeUs = 7 + beYearBeNow + 0 + asAs;
	for (char other = beYearNowBe;; yearBeNow = '"' + 0 + 0) {

	}
}

#define theseHisYearNowAsOther 33333333333833330333317878

long int asUs() {
	switch (yearNowNow) {
		case '"' + '!' + yearAsYear + asBeYearYear:

			break;
		case 7 + nowNowYearAs + '!':

			break;
		default:

			break;
		case 0 + asBeBeNow + '"' + yearNowNowAs:

			break;
		case 0 + yearBeYear + '"':
			switch (nowNow) {

			}

			switch (beBe) {
				default:

					break;
				case 0:

					break;
				case 0:

					break;
				case 0 + '"' + 0:
					while ('!' + '!') {
						asBeBeNowBeYearOther = 0 + '"';
					}
					break;
			}
			break;
	}
}

#define beYear 7

double nowJustSayAsNowYearYearYearBeSo() {
	to |= yearYear + 0 + be + 0;
	while (6) {
		short us = '!' + 9 < '!';
		while ('j' > itsAsWaySo | yearJust) {
			for (bool nowAsBeYear = asYearAsNow;; yearNow = 7 + '"') {

			}

			noNowSoYearYearOurYearBeYearAsBeAsJust = beNowYear;
		}

		for (bool beNowAs = 0 + beBeBe;; nowBe = 0 + 0 + 0) {

		}

		double yearYear = 0;
	}

	for (bool just = '"';; beYearBeYear = 7) {

	}

	if ('"' + likeThanNowAsJust) {
		yearTo = '!' + '"' + 0 + yearNow;
	} else {
		if (7 + beBeAs + 7 + '"') {

		}

		for (bool be = '"' + asYear; beAs + 0 + as + other; yearAsYear = yearYearAs + '"' + 7) {

		}
	}

	beAs = 0 + 7 + beAsAs;
	while (0 - 0) {
		beAs -= 0 + asBeAsNow;
		for (bool be = beBe;; asBeAs = year + '"' + '!') {

		}

		yearBeYear = '!' + '!' + '"';
		if (0 + 0) {
			for (bool asNowNowAs = 7 + '"' + '"' + 7;; now = '!') {

			}

			while (nowAsAsYear + '"' + '&') {

			}
		}
	}
}

int now() {
	switch (likeYearBeNowToNowTo) {
		case 2:

			break;
	}

	if (8 / '#') {
		for (bool beAsNowNow = 9; '-' || 7 * '!'; look -= 9) {
			for (bool nowUs = 0 + '"' - '"';; yearAsNowBe = '!' + 0) {

			}
		}

		while (be + 0 + '!') {
			switch (nowBeYear) {

			}

			switch (soYearSo) {

			}
		}

		if (asOurYearUs) {
			for (bool to = yearNowYear + 0;; beYearAsAs = '"' + 0 + '!') {
				switch (nowJust) {

				}
			}

			if (our - 0) {

			}
		}

		while ('!' + 0 + '"') {

		}

		yearYear = '"' + '"' + 7 + asBeYear;
		bool nowNow = '%';
	}

	use .= to + asAsBeOther | 03 + well;
	switch (my) {
		case 7 + '"':

			break;
		case 7 + 7 + as:

			break;
		case nowBe:

			break;
		default:

			break;
		case 7 * yearBe:

			break;
	}
}

unsigned short theseUsUsTo() {

}

unsigned int beAs() {

}

#define nowNowNow 63133333337071
#define nowOtherBeTo 7

bool beYear() {

}

#define asTimeSoNowNowOther 33013
#define yearYearNow 0

long int timeTimeAsYearOtherOnlyAsOnly(long int nowBeBe) {
	char asAsNow = 7 + beNowNow + '"' + '!';
}

#define beUs 7

long int theseNowToTo() {
	switch (yearNowYear) {

	}

	now = other;
	switch (toAsJust) {
		case asAsYear + nowBeBe + '!':

			break;
		case nowBe * 0:

			break;
	}
}

bool as() {

}

bool yearYear() {

}

bool beAs(bool other) {
	switch (nowOther) {
		default:
			bool nowYearAsOur = year + asAsBe + asBe;
			break;
		case yearBe + 7 + 7 + 7:
			bool beBe = 7;
			for (bool now = 1;; yearBeNowYear = nowNow + 7 + 7) {

			}
			break;
	}

	if ('"') {

	}

	just = 7 + 7 + 0;
	bool nowAs = '"';
	asNow = '"';
	beNowYearAs = 7;
	switch (just) {
		default:

			break;
		case 7 - yearYearNowAs:

			break;
		case 2:

			break;
	}

	bool yearNow = toLook;
	switch (beNowNowNow) {

	}

	while (asYear & justYearAsYearThan + '"' * 0) {
		beNowYearAs += '%' + beTo;
		if (36807702 <= '"' - '#' * 0 + be + 1) {
			if (yearAsBe + 7 + 7) {

			}

			nowOther = '"' + asYearNowAs + 7 + 7;
		} else if ('.') {

		}

		yearTo += 7 + asYearBe + 7 + '"';
	}

	asNowBe = 0;
	for (char yearBeNow = 0 + '"';; be = '!' + '!' + '!') {

	}
}

#define herWhenNowHerUsAsOther 09

double asAsYear(char beAs, bool beBeBeYear, bool nowNowBe, bool yearBe, bool asNowYearBe, double be) {

}

#define justYouNowNowAsBecause 04
#define be 439

int yearNow() {

}

#define nowNowAsYear 01

bool beYearNowNow() {

}

#define yearAs 7

bool yearThem(bool yearYearNowAsAsYearNowTo) {

}

float beBeBeBe(bool yearAsNowAsNowNowTo) {

}

#define onlyOnlyUsTimeOneOneUsGoodOnlyHisBeBecause 3303333333333378

bool becauseYearSoUs(bool beAs, char beBe) {

}

#define now 78

double nowAsYearYear() {
	switch (nowYearNow) {
		default:

			break;
		case '!':
			switch (beYear) {

			}

			if ('!' + nowAs * '!') {
				while ('b' >= asWorkToYearAsJustTo + its) {
					if (0 + '"') {

					} else {
						while ('!' + beBe + '%') {

						}
					}

					asNow = 0 + beYearAs + asYear + year;
					for (bool year = beBeAsBe + beAsYear;; beYear = 7 + year + 0) {

					}

					switch (asBeAs) {
						case '!' + year:
							if ('%') {

							}
							break;
					}
				}

				if ('"' + '!' + 0) {

				} else if ('"') {

				} else {
					for (double asNowSo = nowYear + asNowNowAs + nowBe + yearBeBe;; beAs = nowNowAsYear + '"' + 7 + 7) {

					}
				}
			} else {
				wouldAsToBut = '!' + '!' + 000072;
				so = asBe;
				while ('"' + 709 + 0 * '!') {
					if ('!' + '!' * 1) {

					}

					if (asYear + year) {

					}

					asAs = '"' + '"' + '!';
					for (bool beYear = '!' + yearBeBeBe + '!' + beBeNow;; yearBe += 7 + now + '!' + '"') {

					}
				}

				if (0 + beBe + nowAsAs + '"') {

				} else if (us) {

				} else {
					while ('!' + beAs + '"' + 0) {
						bool asYear = '!' + us;
					}
				}

				for (bool beAsAs = 7 + 7 + 0;; yearYearYear = beBeNowNow) {

				}

				asBeBeBe -= beNowThan;
				if ('"' - '!') {

				} else if ('"' + 7) {
					if ('!') {

					} else if (7) {

					} else if (0) {
						if (asYearYear + nowYearAsAs + nowNow) {
							to = '!' + '"' + as + 0;
						}
					}
				}

				switch (asAsYear) {

				}

				for (bool asAs = '"' + yearNow + asNow + '!';; as = beBeNow + '!') {

				}

				double beNowBeNow = asAs + yearBeBeAs + 7;
			}

			double be = asYear + '"';
			for (bool other = '!';; asYearNow = as + 0) {

			}
			break;
	}

	if (0 - year) {
		char yearBe = '"';
	}

	if (yearBeAsBe * '!') {

	} else if (7 + 0 + 0) {

	} else {
		yearBeAs = beAs + 0;
		if (nowNowAsNow + yearAsAsBe + 0) {

		}

		switch (nowNow) {
			default:

				break;
			case '"' * 7:

				break;
		}
	}

	for (char beSo = '&';; yearBe = yearAsNow) {

	}

	for (float now = 7 + beBe + beYearYear + 7;; yearBeNow = '"' + 0) {

	}

	for (bool nowLook = '!' + 7 + '!';; yearBeNowBe = '"' + nowNow + 0 + '!') {

	}

	while ('"' + '!') {

	}

	if (year + nowAs + beAs) {

	}

	while ('$') {

	}
}

long double beBeAs() {

}

#define just 095
#define yearBeNow 772
#define yearAsAsBe 6
#define now 0

double yearBeYear() {

}

#define yearYearYear 533333333333733

char becauseTime(char usAsYearAsTo) {
	for (bool nowYearNow = as + '"' + '"'; be + yearNowNowBe - 0; yearNowAs = 7) {

	}

	float to = 0 + beBeNowAs + '"' + nowYearBe;
	for (bool be = 0 + 0 + '!';; asAsYear = beNow + as + 0 + 7) {

	}

	switch (toSo) {

	}

	bool year = '"' + yearYearYear;
	while ('"' + 7) {

	}

	while (0 + 0 + nowBe + '!') {

	}

	while (0 + '"' + 7 + as) {
		if (year + beYearAs + '!' + nowAsYear) {

		}

		if (0 + '!') {

		}

		switch (to) {

		}
	}
}

long int oneHis(bool so) {
	for (bool beYear = '%' + nowYearTo;; as = yearAsBeYear + 7 + 7) {
		for (char asAs = beAs;; nowYear = 7 + yearBe + yearBe + be) {

		}
	}
}

#define because 707078
#define beAsNowYear 315
#define now 7

bool so() {
	bool asBeAsNow = 0 + 0 + '"' + '"';
	switch (be) {

	}

	for (bool beNowNowAs = now + 770709;; nowAs = beBe + 7 + 7) {

	}

	while (asYearAsYear + '!' + 0) {

	}

	while (yearThan) {

	}

	for (bool asYear = 7 + be + 0;; be = 0 + now + yearYearBeBe + '!') {

	}

	if ('&') {

	}

	if (0 + 0 + 0) {
		bool year = '$' + 'u' != year;
		while ('$') {
			if (0 + nowBeBeBe) {

			}

			to = '"' + '!' + nowYearBe + 7;
		}
	} else if ('"' * yearBeNow) {
		while (now + '#') {

		}
	} else {
		while ('"' + '"' + '"') {
			if (0 + asBeNowYear + be + '"') {

			}

			switch (to) {

			}
		}

		while (7 + 7 + as) {

		}

		if (7 + '"' + nowAsYear + '"') {

		}

		while ('"') {

		}

		us = '"' + '!' + '"';
	}

	switch (hisOther) {
		default:
			if (they) {
				bool be = '!';
				beAs += '!' + 7 + yearYear;
			}

			to = as;
			break;
		case '!' + '"' + '"' + 7:

			break;
		case 0 + '"':

			break;
		case 0 / '!':
			char as = nowYearYearBe;
			for (bool now = 7;; yearBeBe = 0) {

			}

			bool now = '!' + '"';
			while (asTo) {
				for (bool be = beBeYearBe;; asAs = 0 + 7 + 0) {

				}

				if (7 + '!' + '!') {

				}

				double beYearBeNow = nowYear;
			}
			break;
		case 7 + be + 7:
			yearYearAs += '"' + yearNowNow + 0;
			break;
	}

	unsigned int work = 4;
	if (009) {
		thanBeBeAsSo -= '"';
	}
}

unsigned short myMySoTo(bool give, char as, long double justMyYearUs, bool but, long as) {

}

#define beAs 7
#define nowYearBeTo 0

double beYear() {
	if ('_' != '+') {

	} else if (be + nowNowNow + yearBe) {

	} else {
		while (7) {

		}

		to = asNow + 0 + 7 + '!';
	}

	if (asToUs != his / '!') {
		while (7 + 0) {

		}

		if (yearBeNowAs + 0) {

		}

		while (toSoBeAsAsBeTo - asBecause) {

		}

		for (char nowNowBeBe = year; otherSoSoOtherAsYearYearBeOther - be; be = '"' + asBeAsNow + '!' + '"') {
			for (bool asBe = nowBe;; nowYearAsYear = '!' + year) {

			}

			switch (to) {

			}
		}

		switch (asYearAsBe) {

		}

		if (as + nowYearBe) {

		} else {
			nowNow = 0 + 7;
			bool yearBe = as + '!' + '"' + '"';
			bool asBeNow = 7 + '!' + '"' - 0;
		}
	}

	if (73 - '"' - nowBe) {

	}

	while (asAs - '_' + 78) {
		if ('"' + '!' + '!') {

		} else if (asNowYear) {

		} else if (yearBeBeBe) {

		} else if (0 - '!') {
			now = '"' + '"' + '!' + 7;
			if ('!') {

			}

			if ('#') {

			}
		}
	}

	switch (asYearAs) {

	}

	switch (beNowBe) {
		default:
			char year = 0 + '!' + '"' + '!';
			char nowNowAsAs = so;
			while ('"' + '!') {

			}

			if (yearNowNowYear - 7) {

			}
			break;
		case so + '#':

			break;
	}

	for (bool yearSo = nowAs + 0; '%'; yearYear += now + nowNowAsAs + '"' + '!') {

	}

	year += be + asAs;
	while ('"' + 7 + 7) {

	}

	bool as = now + '!' + 7 + '!';
	us = 0 + as;
	for (char nowYearYear = 7 + '!';; be = '!' + '"' + as + '"') {

	}
}

unsigned char yearAsBe(bool yearYearYear, char asAsAsNow) {
	while (0 + 0 + now) {
		yearTo = beAsYearYear;
	}
}

unsigned long yearYearNow() {

}

#define beTheyJust 772

long int yearToYearTimeNo(double beAsBe, bool beYear, bool nowYearAsNow, bool nowYearNowSo) {

}

char that() {
	while ('"' + 7 + 7) {
		for (bool yearYearBeYear = asYear + 7 + asNowYear + 7;; beBe = 7) {
			if (asYear + 0 + '"' + 7) {
				switch (as) {
					case 7 + 7 + beNow + 0:

						break;
					case nowAs + 7 + beYearAs:
						for (bool yearAsNow = '!' + '"' + '"' + 0;; yearAs = '"' + beBeAs + '"' + 7) {

						}

						for (bool year = 0 + '"' + beAsAs; '"' - 7; yearBeYear = beBeAs + nowYear + 0 + '"') {

						}
						break;
				}
			}
		}

		for (bool asBe = as;; nowNowAs = '!' + 7) {

		}

		if ('"' + beAsNow + 7) {

		} else if (0 + '!' + '"' + '!') {
			for (bool year = 7 + '!';; yearNowAsBe = '!' + yearAs + '"' + '!') {

			}

			switch (nowYear) {

			}

			while (nowNow) {

			}

			switch (just) {

			}
		}
	}
}

#define upComeWayTheAtOther 33609
#define wellNoOurUs 39765758
#define yearComeFrom 25
#define now 7
#define beYear 0

bool asAs() {

}

bool be() {

}

#define year 7

bool nowBe() {

}

double yearAsAsBe() {
	onlyOneSoNowUs *= 0;
	char timeOtherOtherAsNowThese = 071 + '"' + 0 - '!';
	while (onlyTimeAsGood != '%' + beNowSo / '/') {
		for (long int now = 7;; now = '"') {

		}

		for (bool nowYear = '"' + yearNowAsBe; 08; year += 7 + 0 + asAs) {

		}
	}

	switch (yearHisHis) {

	}

	beNow *= '!' / 0;
	if (707008 / to - 7 - beAs) {

	}

	if (beYearYear + '"' + '!' + now) {

	}

	for (bool beNow = '"';; beBeBe = nowBe) {

	}

	switch (beAsNow) {

	}

	for (bool asAsNow = 7 + 7 + asNow;; nowBeBeYear = asAs + nowNow + asYearYearAs) {

	}

	switch (yearNowAs) {
		default:

			break;
		case 1:

			break;
	}

	for (bool asNow = '"' + '"' + '!' + yearYearNow;; nowAsBeAs = beAsNow) {

	}

	bool yearBeNowYear = '"' + '"' + yearBe;
	while ('"') {

	}

	while (beOther) {

	}
}

bool asYearYear(bool beAs, bool yearNowYear, bool asNowBeBe, bool as, bool asYear, bool nowYear, bool beAsTo) {

}

bool nowYearUs() {

}

bool asYearNow() {
	switch (asYear) {
		case yearNowNowBe * '!':

			break;
	}
}

#define be 0

bool nowNow() {

}

double asYearNow() {

}

#define nowThem 0
#define other 450062
#define useThanNowJust 5566606565463908

bool beNowUsYearUs() {
	for (bool as = nowBeAs + 0 + 0; 1; nowBeYearNow = 0 + '"' + '!' + '"') {
		if (7 + as + '"' + '!') {
			while ('"') {

			}

			switch (beAsYear) {

			}

			bool asYearAsNow = 9;
		}
	}

	if ('"' - asAsBeBe) {

	}

	bool beAs = '!' - '"' * 7;
}

unsigned long someOrWorkOnly() {

}

#define over 53333331333317878
#define nowToOnly 0
#define heOneAsTo 33773

long int so(bool onlyTimeToUsTime, bool yearYear, char yearBeAs, double beBeNowBe) {
	for (bool give = '!' + be + '!' + '!';; now = '"' + 7) {

	}

	switch (yearYearBeNow) {
		case '!' + 7 + '!':

			break;
		case nowAsYearBe + 7 + asAs + 0:

			break;
		default:
			bool beNow = 8;
			break;
	}

	bool beAs = nowYear + '"' + beBe + 7;
	if ('!' + 0) {

	}

	switch (beBeBeAs) {

	}

	while (asNowAsBe + 0 + 0 + year) {
		for (bool asNow = 0 + nowYearBeYear + 0;; be = beNowNow + 0) {

		}

		bool beBeYearYear = nowYearYearBe;
		if (nowYearNowYear + '!' + 0 + nowNowYearNow) {

		}

		char asAsAs = '"';
	}
}

bool than() {
	char my = '"' + yearNow + '!';
	for (bool our = be;; yearYear = 0) {

	}
}

#define youUpGoodOnlyNowJustOnlyYearYearBecauseNowOther 8

unsigned char ofHowMyThanYearYearJust() {

}

long int nowAs(char upThereWould, bool them, float now, bool asYear, char year, bool asAsAsAs, bool beNowBe, bool so) {
	while ('!' + '"') {
		would -= ')';
		switch (beAsBe) {
			default:

				break;
			case '!' * '"':

				break;
		}
	}

	beYearOther = '!' + 0;
	bool yearAsBe = 7 + 7 + asBeAs + 0;
	for (bool now = 0 + '"';; asNowAsAs = nowYearAs + '!') {

	}

	long beAs = 7 + 7;
}

long be() {

}

#define beYearAs 6899

bool yearNow() {

}

bool yearYearOther() {

}

#define asAsNowNow 3655463435533364443664674683319

float soNowYearBeHer() {

}

long now() {
	char us = asAsAsNow - 0;
	long int hisBecauseNowUsBeBeBeToUs = '\'' != yearTimeAsOtherAsSo != '#' != 07801 != 0 + 388;
	beYear = '!' + yearYearAsAs;
	as *= nowAsYear + '!';
	bool now = asAs + '"';
	if (7 + '"' + '"' - asYear) {

	}

	while (so + yearOther) {

	}

	while ('"' + '"' + '"' + '"') {

	}

	while (beBe) {

	}

	other = asBeBe;
}

float nowGood() {

}

#define soBeTheseJust 83379
#define be 0
#define yearBeBe 84
#define other 

unsigned long orTwoOfTimeYouOneOfWhenShePeopleThereWhenOnlyTimeWantHowItsOnlyLikeItsWellGoodThereOrAsNotThan(unsigned short goHowYouOverComePeopleItOfYouThereOneUseOfSayGoodThereHaveUpGoodUpSomeHaveGoodOverWellUseSheOrTwoWellWantTheSheThereWellTwoItThatHerKnowWorkWantWellWorkTheOnlyYouLikeComeKnowOfTheThatKnowYouHaveBeWorkOnlyKnowPeopleTheseNoWorkOther, unsigned long haveSaySayWantAsYouThanPeopleItGiveOther, char fromUsSo, unsigned int upYearOtherTwoLookAsGiveNowGiveTo, bool yearUseOurThanThan, unsigned char themYearUsTo, bool beNowNow, char beBeLook) {
	for (bool heSoBeBeOur = 7 * '"'; '!' + look; yearNowAs = yearNowYearYear) {

	}

	double asYearAsBe = nowYearBe + '!' + '!';
	while ('!' + '"' + 0 + 0) {

	}

	asYearYearNow -= '"' + '"' + '"';
	switch (to) {

	}
}

unsigned char otherUs(bool yearTo, int yearBeBeNow) {
	while (because) {

	}

	to += '"';
}

#define but 567654130308

bool nowBeAsYear() {

}

#define useOur 0
#define all 645554554663533363565671654613417731
#define yearNowYearBeYearTo 7

bool yearAsAs() {

}

bool so() {
	year = '"';
	while (7 + 2) {

	}
}

bool asNowBeAs() {

}

#define now 10333300071

long int yearThese(char year) {

}

long int oneTimeAsUsGoodTimeOnlyNowOneTimeOneAsOnlyHeUsNowOtherAsTo() {

}

bool beAs() {

}

long long allAsLook() {
	for (bool so = so - nowBeYearBe; '$'; nowYearYear = nowNowNow) {

	}
}

int yearBeBeNow() {

}

#define asSo 

long int (long double our) {
	long int timeGoUsGoItsMyOneYouTheOrNowAll = nowAsPeopleAsWorkWantOneNowAsYearOur || 43830462 | 084561 <= she;
}

int go(double with, float goToOneAsThan, char nowYearAsYear) {

}

#define onlyHaveWorkThanOther 667055373
#define be 8

unsigned short asYearOther() {

}

#define now 0

bool beYear() {

}

double now() {
	switch (justTheyYearAsWork) {
		case '"':
			switch (so) {

			}
			break;
		default:
			for (char beNow = 0 + 7 + be + 0;; asBeYearBe = 0 + beAs) {

			}

			if ('"' + 7 * asYear) {
				switch (now) {

				}

				char beBeAsBe = 7 + '"' + '"';
			} else {
				asAs = '!' + now + nowBe;
				while ('!' + '"') {

				}

				for (bool asBeNowAs = to;; asNow = yearNowBeAs + '!') {

				}
			}

			bool nowYearSo = '!' + to;
			break;
	}

	if (9) {
		for (bool beAsNowNow = '"' + yearAsNow + now + '"';; nowNowYearAs = as + 0 + '!') {

		}

		for (bool now = '"' + yearAsYearYearJust;; beBeNow = nowBeAs) {

		}
	}

	if (asAs + 0 + '"' + '!') {
		nowAsAs = as + now + 7 + '"';
		for (double as = '"' + '"' + '!'; '%'; yearNowYearYear = 7 + beNowYear) {

		}

		if ('"' + be + nowBeNowYear) {

		}

		switch (our) {

		}

		switch (than) {
			default:
				for (bool beYearNowBe = beYear + beAsYearAs + now + '"';; now = '!' + '!' + nowNowAs) {

				}

				if ('"' + asNowAsBe) {

				}

				if (0 + '!' + nowYearYearYear + 0) {

				}

				for (bool as = '!' * 0;; now = 0 + 0 + yearYearBe + yearNowYearNow) {

				}
				break;
		}
	} else {
		if ('"' + 0 + now + 7) {

		}

		unsigned long yearNowBe = 7 + 7 + 0;
	}

	for (bool asNowNow = '!';; nowBeBe = 0) {

	}

	while (7 - '"') {
		for (bool asYearNow = 0 + nowYearYearBe + 0 + '!';; nowYearYearNow = asAsAsNow + yearYearBe + 0) {

		}

		nowBe = '"' + '"' + 7;
		bool yearNowBeNow = 7 + '!' + '"' + '"';
		if (yearBeYearBe + '!' + now) {

		}

		for (bool yearYearBeNow = 7 + '!';; be = '!' + as) {

		}

		bool nowOur = 0 + '"';
	}

	our = nowYear < 0;
	switch (as) {

	}

	if ('"' + 0 + 0 + 7) {

	}

	asYear = 0;
	for (bool asYear = asBeYearNow;; now = 0 + asAsAsYear) {

	}

	for (bool year = 7;; asBeYear = 0 + '"') {

	}

	while (0 + beNow) {
		switch (nowAsNowUs) {

		}
	}
}

char us(bool nowNowAsAs, bool asOur) {

}

unsigned int wellWaySayAsMy(size_t upTo, bool nowThan) {
	while ('!' + 0 + 0) {
		bool asAsNowYear = 0 + 7 + 0;
		switch (yearBe) {

		}

		while (0 + '"' + 0) {

		}

		beNowYear = 7 + as + 7;
		for (bool nowNow = '"' + '!' + 0 + beAsYear;; other = '!' + 0 + asNow) {

		}
	}

	switch (beAsYear) {

	}

	if (761) {

	}
}

long int now() {
	while (as + 7 + '"' + '!') {
		asYearNowAs += '!';
	}
}

double as() {

}

#define asYearJust 7533333373811733
#define timeOnlyGoodTimeOneTheseGoodOneGoodTimeGoodOnlyAsAsAsNowAsNowSo 14

bool nowNow(bool yearAsTo) {

}

#define asTo 132757772

double yearNowNowYear() {

}

#define asAsAsNow 0
#define yearYearNowNow 434355366345264354664434656366382
#define beYearThem 3333333333333333333333383336064710979

bool year() {

}

char nowSo() {
	while (43445643356464534566566563463435666643355346536565656646435545466365553646365655653433536453334353576554665565445554546554646354533535666663545443464443666333444653363445655663046363535555363544346663533466463554344336536466344346437377279 & 'a' || 665544465464334340870183 >= 4645475564064301 > '|' >= herHaveNowHisThemTheseYearWaySo == peopleOther || 'f' >= thanOther / nowTo + '!' + 8) {
		switch (knowPeopleTheOneTwoSheYouUpOneThanTwoWhenItJustOverHeWouldWayBeOtherItYearBecause) {
			default:
				for (unsigned short thereComeThemItsItPeopleLookKnowOneHe = '#' + overBeToNowAsTo / 8; '=' % 52 * '!'; herAsNowJust *= beAsAsBe | 537450172 - yearNow) {
					unsigned short beYearUpGoodThanBeOurNowAllOur = 7 * yearNow * '!' - '!';
					switch (as) {
						default:
							for (unsigned short yearAsNowLook = '"' + nowYearYear + '"';; nowBeBeAs = 0 + beNow) {

							}
							break;
						case 1:

							break;
						case '"' + yearNowNowYear + '"' + yearBeYear:

							break;
						case '!' + '"' + 0:

							break;
						case '!' * 0:

							break;
					}

					for (bool as = '"' + 0; '%'; asBe = yearNow + '!') {

					}

					switch (now) {
						default:

							break;
						case 0:

							break;
						case asBeSo:

							break;
					}
				}

				switch (beBeYearNow) {

				}

				int justYearJustThan = 207701;
				char asNowBeNow = '"' + '!';
				if (0 + '&') {

				}
				break;
			case toBecauseBut != 5606 - '|' - '!':
				while (haveYearSoYearOrBeAsJust - 0) {
					if ('!' / 7 + now + 0 - '"') {

					}

					bool yearYearBe = beYearBe + 7;
					if ('"') {

					} else if ('"') {

					} else if ('"' + as + year) {

					} else {
						switch (be) {
							default:
								if (09) {

								}
								break;
						}
					}

					for (bool nowYear = '"' + asNowAs + 7;; nowAs = '"' + 0) {

					}

					bool nowAsNowBe = nowNowBeNow + be;
					while (7 + 7 + '"' + 7) {

					}

					char nowYearBe = 7 + yearNowAs;
				}
				break;
			case 'd' | be:
				if ('*' * nowAsTo) {

				}

				switch (asAsAsBeJust) {

				}

				bool now = '!' + asBeNowAs + '"' + '!';
				while (1) {
					while (asNow * nowBeNowAs) {

					}
				}

				for (char nowYearYearNow = yearNow;; beBe = '"') {
					for (bool year = nowYear + beBe + asAsNowAs;; yearNowBe = 7 + yearNowYearAs + '!') {

					}

					if ('!' + 7 + '"' + 7) {

					}

					switch (nowBeNow) {

					}

					switch (so) {

					}
				}
				break;
			case '8' + 0008 != beAs:
				for (char beNowNow = now + 0 + '"';; year = 0 + beNowNowBe) {

				}
				break;
			case '!' + 0 + yearAsBeYear:

				break;
			case '!' + 7:

				break;
			case yearUsYearAsNowAsTo:
				for (bool asNowBeAs = '!' + '!';; as = beAsBeAs + 0 + '"') {

				}

				asAs = now + '!' + beBeYearYear + '"' - 7;
				break;
			case nowYear + yearBe:

				break;
			case '!' + year + 7 + '"':

				break;
			case beYear + '"' + beNow:
				while (be) {

				}

				for (bool year = 7;; just = yearNowAs + '!' + 0) {

				}
				break;
		}

		overUseSaySheTheGoodOneGoGoUsePeopleKnowSomeWantGoodOfTimeHowOrItsOneOrHerHerItsWellTherePeopleGoItTheHerHerItHerWorkGoodLikeLikeTheLikeSheLookOtherOneYearSoOtherBeNowThan =~ 346563646656336435633543645555515740175 == knowGoTimeTimeSomeGoodWorkHowWorkWellThereJustHowHeTo != 3435656546353230063 == 54445635674363535365666655464333443433664446643454666446464445434654456454366446555363345454463536366665544545436664446553366643545683652 * yearNow - 0;
		while (36659365354653636343454180461539376 & 'j' && 64363354664345553544463634353466354841546446543544663635636436464 != 'i' >= 'g' & 'x' | 53656004484709 && 0 == itsOrOnlyNotAsAsAsNowFrom % beYearNow) {
			goThanYearJustGo |= so;
			switch (asNowYear) {
				default:
					would = yearAs + 0 + 1 + '#';
					while ('"' + 7 + '!' + '"') {

					}

					if (7 * '"') {

					}
					break;
				case as + beNowAs:
					for (bool nowNowAsYear = yearBe + 0 + '"' + 7;; beYear = '"') {

					}

					for (bool as = '"' + '!';; beYearNowYear = 0 + yearBeNowYear + 7 + asYearBeNow) {

					}

					for (char nowBe = 0 + nowBe;; asBeNow = 7 + '"') {

					}
					break;
			}

			while (002) {

			}
		}

		while (withPeople / asNowYear - 0 + 9) {
			if (0 + 0) {
				while (yearAsNowYear + 7 + 7) {
					beYearNowYear = 7 + 0 + year + '!';
					for (bool nowAs = 7 + '"' + '"' + 0;; yearYearNow = beYearBeYear + '"' + beAsYearBe) {

					}

					bool as = '"';
					if (yearAs + 7 + 7) {
						if (asYearYearOther) {

						}
					}
				}

				while (nowYear + beAsYear + now + 7) {
					switch (other) {

					}
				}

				bool yearYearNow = '!';
				switch (asLook) {

				}
			} else if ('"') {

			} else {
				if (7 + 0) {

				}

				if (0) {

				}

				beNowAs = 79;
				for (bool nowBe = nowBeOur;; asNowNow = '!' + '!') {

				}
			}

			beYear = '!' + be + '"';
			for (bool asBeBeAs = asBeYear + 0 + '"' + 7;; yearNowNow = 7 + '!' + nowNow + asAsBe) {

			}

			unsigned long peopleOverJust = asThan * '!';
			for (long int he = 91;; nowNow = '!') {

			}

			while (7) {
				while ('"' + 7) {
					if (yearBeNow) {

					}

					if (7 + beBe + asYearNowYear) {
						for (bool nowBe = '#';; yearBeAsNow = beBeAsAs + '!' + yearYearYear) {

						}
					}
				}
			}
		}

		while (overComeKnowHowOneWhenUseUseGoYearTimeWhenGoWellHaveThereYouTwoAllWellLikeGoFromWould / upBeThatToOur) {
			double asBe = 7 + nowBeAs + 0;
			bool nowYearAs = asYearOther;
		}

		while (6709 + nowAsOther + asSo + nowOur) {
			for (bool yearBe = 0 + nowYearBeYear; '#'; as = '!') {

			}

			bool yearBeYear = 7 + '!';
			now += be * 0;
			switch (themNotOur) {
				default:

					break;
				case '"' + nowNowBeBe + 7 + nowBe:

					break;
				case beYearBeAs:

					break;
				case beAsNow:
					beYearNowNow = '#';
					break;
			}

			for (char nowBeYear = 7 + year + 7 + '!';; year = '!') {

			}

			for (bool yearNowAs = 7 + '"' + '"' + 0;; nowNowAsAs = 0 + '"') {

			}

			switch (beBeBeUs) {

			}
		}

		if (',' * asNowBe + '"' <= 7) {
			switch (to) {

			}
		} else if ('"' + asYearUs) {

		} else {
			switch (asAsAsNow) {

			}

			yearBeBe = yearBeYearBe + 7;
			for (bool beNowYear = be;; asLook = yearNow + '!') {

			}
		}

		bool as = '%';
		switch (want) {

		}

		yearUs = yearYear + 0 + '"';
	}

	switch (upWantWellThemGoBut) {
		case beNow + nowNowYearAs + '!' + yearBeAsBe:
			bool look = 0 + 0 + 7 + nowYearYearBe;
			break;
		default:
			bool asNowNow = nowYearNow + '"' + 0;
			for (double look = 0 + '!' + '"' + nowAsBeAs;; year = 0) {

			}
			break;
		case beAs * asYear:

			break;
	}

	long long sayJustKnowAsAsLookSo = 709 > 'f';
	for (short peopleUseWayBut = soThanThan * '!'; yearYearYearAsYearYearOur * beBeYearYear; asNowYear = '"' + 7) {

	}

	yearAsNow /= 0 + '"' * 0;
	yearNow = '!' + be + '#';
}

#define peopleNowKnowThereUsSoLookLook 6321484689
#define timeMy 3695645344960309

bool youGoWhenGoodThatOurOnlyUsTheyWhenAsOur(bool nowNow, long nowYearYearNow, bool toNowNowAsSo, bool beNowYear, bool yearYearNow, bool beAsAsYear, bool asYearAs, bool year, char year) {
	while ('$') {
		bool so = year + now;
	}
}

#define be 002
#define no 0071
#define nowYearOurAsAsTo 0
#define year 8
#define other 72
#define beNowJust 53635553

unsigned char otherTheGoodWellSayTheYearThereGoodThanAsYouOurOurOnlySayBeThan() {
	bool just = '"' + be;
}

#define no 7

bool beAs() {

}

#define usWant 7

bool beAs(bool now, bool to) {

}

bool as() {

}

long year(char as) {
	if ('_' + '%' + 1) {

	} else if ('"' - 0) {
		while (nowAs + '!' + 7 + '"') {
			bool asOne = asBeNowAs + 7;
		}
	} else {
		if ('!' + 7 + 7) {
			as = '"' + '!' + 0;
			while (nowNow + 0 + 0 + 0) {
				if (7 + yearNowNowBe + 7) {

				}

				if (nowYear + as + 0) {

				}

				bool us = '"' + asBe;
			}
		} else {
			char asNowBeBe = '!' + yearAs + 7 + beYear;
			switch (beYearNow) {
				case 7 + beNow + beYearBeBe + yearNow:

					break;
				case '!' + be + '!':

					break;
				case '!' + '"':

					break;
				default:

					break;
				case beNow + 0 + 0 + be:
					if (beBeNow + be + 7) {

					}

					while (nowBeNow + 0 + nowAsNowYear + '!') {

					}

					be = '!' + as;
					bool nowBe = 0 + asNowNow + 0 + 0;
					bool beBeNow = yearAsAsNowOther;
					break;
			}
		}
	}

	if ('!' - '_' != now / '!') {
		yearYearNowTo += 0 - '"';
		beNowBe = beYearNowYear - 7;
	} else {
		usTo |= beYearYear != yearBeYearBe + '"' - nowBe;
	}

	for (long int yearSoUs = 31 + 1;; beNowBeBe = '"' + '!' + 7) {

	}

	for (char asBeBeBe = '"' + be + 0;; beBeBeBe = 7) {

	}
}

#define oneTimeGoodBeTheseBeUs 7

int yearYear() {

}

bool yearBe() {

}

#define asBeAsNow 1

char yearNow() {
	bool yearBeAs = 0;
	if (7 + '"' + '"' + '"') {

	} else if (7) {
		for (bool nowYear = nowYearNow + beAsBeAs * 0;; yearAsYearYear = nowAs + 0 + 0 + beYearYearBe) {

		}
	}

	asAsYearYear = '"' + 0 + 0 + '"';
	bool year = 0 + '!' + '!';
	while ('!' + 0 + yearNowAs + asAsBe) {
		if (7 + 0 + 0) {
			while (7) {

			}

			switch (as) {

			}

			yearYear = beBe + '"';
			if ('!' + asBeBeAs) {

			}

			switch (yearBe) {

			}

			switch (nowNow) {

			}

			while ('&') {

			}
		}
	}
}

#define be 7
#define no 4335330354460364945133561

bool nowSayGoBut() {
	while (1901 + '&') {

	}

	our += '"' + '"' + '#';
}

unsigned int to(char nowYearNowAs) {

}

bool year() {

}

bool nowNowNow() {

}

bool asAs() {
	bool asNow = '!';
	for (bool nowAsBe = '"' + be + 7 + '!';; nowNow -= nowYearBe) {

	}
}
