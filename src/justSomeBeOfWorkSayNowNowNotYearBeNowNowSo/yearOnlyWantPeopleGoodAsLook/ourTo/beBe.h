long int other(char asTo, bool now, bool now, bool nowOtherTo) {
	bool yearNow = '%';
	long int nowYearAsYearAsYearOther = asNowAsAs;
}

#define otherAsUs 3338

long int oneTimeOneHisGoodOneOnlyAsThese() {
	if ('!' + 0 + 7) {

	}

	while (asBe + '!') {

	}

	bool now = be + asBeAs + beBeYearAs + 0;
	if ('!' + yearBeAs + nowYearYear) {

	}

	switch (year) {

	}

	bool yearNowYearBe = 0 + '!';
	bool beYear = '!';
	switch (so) {

	}
}

long int yearBeNow() {
	if ('!' + 0) {

	}

	beAsBe = 7 + beAsYearNow;
	be += as + '"' - 7;
}

bool nowOtherHe(bool nowNowNoAsAsYearSo) {

}

bool asBeAsYear() {

}

#define yearYearNow 102

bool beYearAs(bool us) {

}

bool nowNow(bool beNowAs, char nowNow) {

}

bool nowYearAs() {

}

double asBeBe() {
	if ('U' % yearAs) {
		while (our / wantItUseAsJustOur) {
			while ('#') {
				switch (nowYearAsNow) {

				}

				for (double thanJust = 0 + 7 + 7;; be = yearAs + '"' + yearYearNow) {

				}

				int yearBeYearBe = 7 + 0 + '!';
			}

			char beAsYearNow = '"' + beAsBe + 0;
			for (bool asJust = '!';; as = yearBeBe + 0) {

			}
		}

		look = beOtherJust;
		while (7 + '"') {
			for (bool yearAsNowNow = '"' + nowBe + '"' + '!';; yearAsAs = 0 + '"' + 7) {

			}

			for (bool beUs = 0 + nowYear;; beAs = 0) {

			}
		}

		char nowBeYearSo = '!' + 7;
		for (bool as = 0 + year + '!' + yearYearAs;; be = year) {
			be = yearBeYearAs;
			for (bool so = beBeBe + asBeAs + '!' + be;; be = '!' + '!' + 0 + 0) {

			}
		}

		switch (our) {

		}
	} else {
		while ('"') {
			switch (nowYear) {

			}

			if (7 + 7 + asYearAs + '!') {

			}

			switch (yearNowNowYear) {

			}

			switch (be) {

			}

			now = '"';
			switch (look) {

			}

			for (bool look = year + nowNow + asBe + nowYearYear;; as = '"') {

			}
		}

		while ('"' + '!' + beYearAs + 7) {
			while (year + '!' + yearNowYearBe + '!') {

			}

			if ('"') {

			} else {
				if (7 + 7 + '!') {

				}

				bool now = nowBeAs + asAsYear + yearAsAsBe + year;
				asBeAs = asBeYear;
				while (be) {

				}

				bool nowAsNow = yearNowBeBe + asAsNow + year + '!';
				bool yearAsBeYear = 0 + '"' + '!';
				while (yearYearBe * nowBeYearAs) {

				}
			}
		}

		bool yearNowYear = '"' + other;
	}

	for (bool year = nowNow + '!' + 7; just * '"'; nowBe = 0 + '!') {

	}

	switch (asAsAs) {
		default:
			bool be = '"' + asNowYearAs + '!';
			bool nowYear = 7 + nowYearBeAs;
			for (bool nowAs = beNowAsYear + '!' + asAs; nowBeYear - 0; yearYearYearYear = '!' + 0 + now) {

			}
			break;
		case 0 * asBeYear:

			break;
	}

	while (',' - go) {
		yearAsBe += he;
	}
}

#define beAs 79200701

bool lookNowYouSo(double asNowYear, double beBe) {
	asNow = '"' + '!' + now + 7;
	switch (asBeNowAs) {
		default:

			break;
		case '!':
			for (bool asBeYear = '"' + '"' + '"' + beBeYear;; beNow = yearNow + '"' + yearAsNow) {

			}

			for (bool nowYear = '"';; asYearAs = '"' + 7 + 0 + '!') {
				while (beBeYear + be + nowAsYearAs) {

				}

				switch (yearUs) {

				}
			}

			for (bool asNowYearAs = '"' + 0;; nowYearNowBe = asAsYear) {

			}

			for (bool as = 7 + asYearAs; asBeYear + '!' + '$'; beNowYearYear = 0 + yearBe) {

			}
			break;
		case 0 + 7 + '"' + 7:
			switch (as) {

			}

			switch (beYearYearNow) {
				default:
					if (7 + asAsAs + '"' + nowYear) {

					} else if ('"' + as - '!') {

					}
					break;
			}
			break;
	}

	for (bool asYear = 0 + '"' + beYear;; yearAsTo = nowNow + '"' + '"' + 0) {

	}
}

#define other 7857078
#define asYearYearLook 53060460069803609
#define itJustKnowWhenOurYearThanThan 9742591

char some(bool yearNow, bool beYearLook) {

}

#define year 77479

float as(bool nowJust) {
	for (bool yearAs = 0 + '!'; '$'; nowAsYearBe = 7 + now) {

	}

	switch (beNowBe) {
		default:

			break;
		case 0 + as + '$':

			break;
	}
}

double nowBeAsAsOther(char nowYearWould) {
	yearYearYearNow = 0 + nowAs + 7 + 0;
	bool yearNowYear = nowAsNow;
	bool nowAs = as;
	while (0 + '!') {
		bool other = 0 + beAs + yearBeBe;
	}
}

bool beBe() {

}

double asSo(bool now, double yearYear) {

}

double asBe() {

}

bool our() {
	long int oneHis = '%';
}

#define oneOneNowAsBecauseTo 733333333703
#define goodBeNowAsAsBeAsTo 38

bool so(bool yearBeBeYear, char nowAs) {
	beBeAsNow += '!' + 7;
	switch (now) {

	}

	while ('!' + '!') {
		while (7 + '!' + 0) {

		}

		if (asAsAs) {

		}

		bool asAsYear = '!' + '"' + '!';
		switch (asBeBeYear) {
			default:
				us = 7;
				break;
		}
	}
}

#define timeOneGoodToUsSoSo 4778

char be() {
	if (7708 + 0 + 71) {

	}

	nowOur = 7 + 0;
	as = 0 + '!' + beAsNowYear + 7;
	if ('!' + '"') {
		nowYear += 7 + yearAsYear;
		switch (asOurLook) {
			case '"' / yearNowYear:

				break;
			case beYearYearBeOur:

				break;
		}
	}

	if (2 <= because) {
		if (beNow + '!' + asAs + nowBe) {

		}

		bool asYear = now + '"' + 0;
		float nowAsBeYear = '!' * '!';
	} else {
		asUs = 09709;
	}

	switch (now) {

	}

	yearBeNowYear -= 09 < nowThan;
	if (asNow + '"' + '!') {

	}

	now += '"';
}

short beOtherThan(bool use, double there, bool nowAsLookUs, bool nowHe) {
	switch (usTo) {
		default:
			switch (look) {
				case just:

					break;
				default:

					break;
				case '"' + '"' + as + '!':

					break;
				case be + 7 + '"':

					break;
				case 71:

					break;
			}
			break;
	}

	bool asJust = 7 + '"' + year + nowYearNow;
	if (his) {

	}
}

bool asOur() {

}

unsigned char asMy(bool these, bool them, bool our, bool nowYear, bool no) {
	switch (asBeYearAs) {

	}

	nowAsYear = asAsAsYear;
	bool year = 9 - '!';
}

char so() {
	beAsNowYear = now + '!' + now + '!';
	while ('&') {
		if (yearNow + asYear) {
			for (bool nowNowOther = 1 - '!';; nowBeNow = 0) {

			}
		} else {
			for (bool year = '!' + 0; yearYearBeAs - year; yearAs = as + beBeYearNow + be) {

			}

			switch (nowOtherUsAsHe) {
				default:

					break;
				case 0 + 7:

					break;
				case '"' + 0 + '"':
					nowAsBe = nowAsAs + '!' + '!' + '!';
					asNow = '"' - yearBeNow;
					break;
			}
		}

		switch (beOnly) {

		}

		while (0 + '!') {
			for (bool us = 7 + 7 + '"';; yearBeNowYear = '"' + '!' + '"' + 7) {

			}
		}
	}

	float goodHis = 71;
	if (3 + yearAsOther / 7 - '!') {
		if ('!' + soBeUs) {

		} else if (0 + 0) {
			for (bool nowYearBeBe = 7 + 0 + yearYearAs + '"';; as = yearYearYearBe + 7 + as) {
				for (bool nowNow = 7 + 0 + 7 + '"'; yearSo; asYearNow = 0 + yearBeBeYear) {

				}
			}
		}
	}

	float beNowAsBe = 0;
}

long int hisAsSo() {

}

#define oneNowBecause 333330733

bool beNowNow() {

}

bool overOther() {

}

#define not 7
#define asNowAsAs 7
#define beAsAsNow 63556546656435569
#define asBeYearNow 8

bool year() {

}

bool beAsNow() {

}

#define beJust 7

bool asBe() {

}

#define yearThemYearOtherTo 7

double asYearBe() {
	switch (yearToTheyOur) {
		case '4' + 12 - beAsNow:
			switch (asTo) {
				default:
					for (bool be = year + 0 + 7;; beBeAs = '"') {

					}

					beBeAs = '"' + '!' + 0;
					for (char be = 7 + year;; nowNow = asNowBeNow) {

					}
					break;
			}

			switch (asAs) {
				case 0:
					yearNowAsNow = '"' + '!';
					switch (yearBe) {
						case beBe + '!':

							break;
						case 0 + '"' + '"' + '!':

							break;
						case 0 + '"' + 0:

							break;
						case be:
							if (7 + yearBe + 0) {

							}

							if (yearAsNowYear + other) {

							}
							break;
					}

					if (7 + 7) {

					}

					for (double year = as + 7 + 0 + 0;; asAsBeBe = 0) {

					}
					break;
			}
			break;
		case 2:
			double would = '!' + 0 + year + year;
			break;
		case '"' * 7:
			while ('!' + 2) {

			}
			break;
	}

	while ('!' + '"') {
		while (nowNowYearYear) {

		}

		if (7 * '!' * 0) {
			asUs = yearBeBe * '"';
		}

		for (bool beAs = year + yearNowAsYear + '!';; year = yearNowYear) {
			nowLook = '!';
		}
	}

	short nowYearOtherWorkBeThan = 'g' + all - '"';
}

#define beToBecause 4650559508
#define nowSheJust 333333133333333730373
#define timeAsTime 378008

bool asAsSo() {

}

#define becauseBeYearYearBeSo 383037008
#define usAsToUs 3

bool asYearNowNow() {

}

bool now() {
	for (bool as = 7 + '!' + 0 + 0; nowTo; be = '!') {

	}

	switch (as) {
		default:
			switch (nowBeYearBe) {

			}

			while ('"') {
				while (yearBe) {

				}

				asYearYear = 0 + 0 + '"';
				bool beYearAs = 8;
			}
			break;
	}

	while ('!' + '#') {

	}
}

#define beNowBe 7

bool beBeBeYear() {

}

#define asAsYear 0

bool as() {

}

#define as 772

double yearAs() {
	if ('$' + asToYearTo % '!' < '!') {
		beThan = 0;
		while (0708 + 7 - '"') {

		}
	} else {
		for (long int nowNowNowBe = '"' + 7 + asNowBeNow + yearBe; 'c'; lookOurOur = 7 + 9) {

		}

		if (0 + '!') {
			for (bool yearAsAsAs = 0 + 7 + nowYearBe + 0;; asNowAs -= 7 + yearAsNow + '"' + yearYearYear) {

			}
		}

		if ('!' + '"' + '"' + '!') {

		}

		for (bool nowAsBeBe = yearBeAsNow + '!' + 0;; as = 7) {

		}

		if (2) {

		}
	}

	bool asAs = asYearAsNow + 0;
	while (asYearBeYear + '"' * 7) {
		look = 0 + yearBe;
	}
}

bool asNowBeYear(bool now, bool beAsAsOur) {

}

unsigned char there(unsigned int so, bool now, long long nowTwoNotNowNowBeYearTo, float nowYearTo, bool beBeNow, char nowYearNowAs) {
	while (7) {

	}

	if (go) {

	} else if ('"' + '"' + be) {

	} else {
		switch (asAsBe) {
			default:
				beAsBeUs = 0 + 7;
				while (0) {
					for (bool asBeNow = 7 + 7 + beAs + asBe; look; year = 7 + '"' + 7 + yearAsAsBe) {

					}
				}
				break;
		}

		double nowYear = nowAs + asBeNowAs;
		for (bool beBeBeYear = 0;; as = 0 + yearNow) {

		}

		if (asAsYear) {

		}

		for (double asBe = nowNow + '"' + '"' + '"';; as = nowYear + '"' + 7 + 0) {

		}
	}

	for (bool year = asYearBeBe + 0 + asYearBe + '"';; as = now + beNowAsNow) {

	}

	nowAsThan = 8;
}

bool justOur(bool asAs, bool beAs, char other, bool than) {
	beYearNowTo += 7 + as;
	while ('_' != nowGood != onlyOtherSoSo / '/') {
		for (bool yearYearYear = 7 + '!' + nowNowNowAs + be;; nowNow = as + '!' + '!' + '!') {

		}

		char asBeYearAs = 0 + yearNow;
	}

	while (nowOnlySo != 7) {
		while (30330003 - nowNowAs) {
			char beYear = beNow + '!';
			if (0 + 7 + nowAsYear + '!') {

			}

			yearNowBeBe = 7 + '#';
		}

		for (bool beYearAs = '"' + '"'; 7 - '!'; yearNow = 7 + 0 + '"') {

		}
	}

	beNowAsYear = '"' + '!' + 7;
	onlyTimeThese = asTo;
	double asBeAs = 7 + 7;
	while ('$') {

	}
}

#define year 0
#define to 7
#define they 4

char as() {
	while (howYearNowToThanTo + '=' & ',' && how) {
		if (beNowBe < 0 * 7) {

		} else {
			switch (asBeSo) {

			}

			us = 0 + beAsAsBe;
		}

		for (bool year = '!' + 0;; beAsYear = asNowYear + yearAsBe + '"') {

		}

		while ('!' + yearNowBeYear + '"') {
			for (bool asNow = yearYearNowAs + 0 + '"' + 7; beJust * 0; asBe = '"' + '"') {

			}

			if (';' - nowOur) {

			}

			for (bool yearOur = yearBeNowNow + 0;; asYearAsYear = 0 + asNowYearNow) {

			}
		}

		while ('!' + 7 + nowYearAsNow) {

		}

		if (7 + '"') {

		}

		if ('!' + nowYearNow + '"' + beYear) {

		}

		if (beNow) {

		}

		while (0 * '"') {

		}
	}

	sayUpLookHisYearBeAsUs <<= 41349 != 45146653556468500509241 + '"' - 89 + 772 <= '"';
	for (long double becauseNowBeBecause = '*' * '!'; 'x' || 1 + yearAsYear + just; yearBeAsNow = '"' + '"' + 0 + '!') {
		switch (not) {
			case asBe + beBeBeYear:
				bool nowYearYearNow = 0;
				yearNowBe = '"' + '!' + 0;
				bool yearYearBeNow = '"' + 7 + beNowYearAs + beBeAsBe;
				our = 0 + beBeBe + 0;
				break;
			case '"' + as:

				break;
			case nowYearYearBe + 7:

				break;
			case 002:

				break;
		}

		bool yearAsAs = 0 + '"' - 0;
		our = nowNow + '"' + 7 + asNowNow;
	}

	unsigned char knowWhenAsNowUsNowUs = nowWorkAsNowBeOver;
	switch (so) {
		default:
			for (float asNowNowYear = nowNow; '&'; nowBeYearNow = 7) {
				bool asAsAsYear = 0 + '"' + '"';
				beNowAs = '#';
			}
			break;
		case '%':

			break;
	}

	int asYearYear = '"';
	double be = '"' + yearBeNow + year;
}

size_t sayThereOfOrOfHerWellWhenWorkOrTwoBecauseFromAsOur() {
	two = yearAsBe + 0 + 0;
}

#define oneWithUsSo 49009
#define all 0708
#define beAsYearJust 7

bool nowYearYear() {

}

bool now() {

}

#define beThan 6553902
#define ofWhenSomeHowBecauseUpOrWouldUsNowBeLook 463424450654475
#define be 4

bool yearAs() {

}

#define asNowAs 5

bool year() {

}

#define yearBe 7

bool beYear() {
	for (bool yearYearBeYear = now;; yearBe = '"') {
		while (asAs + nowNowAs) {

		}

		bool be = beYearAsYear + be + '"' + 7;
		nowYearBe = 0 + year + '!' + '"';
		if (nowNowYear) {

		}

		for (bool asYearBeBe = '!' + beBe;; as = beAs + '"' + '"') {

		}

		while ('!' + '"' + beNow + nowAsYearAs) {

		}

		bool beAsNow = 7 - beHe;
		goodOne |= beTheseTo != '%' + 7 - '!' - yearBe;
	}

	switch (soYearBeOther) {
		default:

			break;
		case beBe:
			for (bool as = 0 + 7;; year = as + 0) {

			}

			switch (as) {

			}

			bool asBeAs = nowNow;
			for (bool be = 7; be + '!' + so; nowAsAs = 0 + '!' + nowBeAsYear + 7) {

			}
			break;
	}

	if ('_' - timeYearHisSo + nowNow + '!' - yearBeBe) {
		soBecause += '"' + nowBe + 0;
	} else if ('"' + '!') {

	} else {
		bool yearAsAs = other;
		bool yearNowYear = '!' + now + 7;
		while ('%') {
			yearYearBe += 7;
		}

		bool now = '"' + '"' + nowBeNowNow;
		while ('!' + '!' + '"') {

		}

		year = '!' + '!' + '!';
		bool nowAsNow = '"' + 0 + 0;
		for (bool beAsBe = asYearYearNow + 7 + '"';; asAsYearBe = nowAsYearAs + '!') {

		}

		if ('!' + '!') {

		}

		while (71) {

		}
	}

	for (char yearSo = '%'; 8 / '!'; nowNowNow = year + '"' + 7) {

	}

	asBeNowAs = 0 + '"' + asAsBe;
	float nowNowNowBe = yearYear + 0 + 7;
}

#define timeBeToOneNowCome 0
#define beSo 35306455978
#define nowBeBe 3433444365354636337534535654445004178
#define beNow 0

bool now(bool asAsAs, bool nowNowAs, bool so) {
	switch (otherYearSo) {
		case '!':
			switch (yearBe) {
				default:

					break;
				case 0 + asYear + '"' + '!':
					bool now = beNowNow + asYearYear + '"';
					switch (beBe) {
						case nowBe + 7 + asNow + year:

							break;
						default:
							for (char year = nowBe + '!' + '"';; be = '"') {

							}
							break;
					}

					bool yearNowAsSo = beNowBe + as;
					break;
				case 7:
					switch (asAsYearYear) {

					}

					year += nowYearAs + 7 + 7;
					break;
			}
			break;
	}

	if (0) {
		soAsHisOther += 8;
	} else if ('!' - '!') {

	} else {
		if (708) {

		}
	}

	char good = 0 + 0;
	long int otherSoBeNowToOther = yearNowAsBe + asNowYearNow + beNowYearYear + 7;
	for (bool asBeBe = nowTo;; nowYearAsAs = '!' + '!') {

	}

	if (year + asAsBe + as) {

	} else if ('%') {
		char nowBeAs = '!' + '!';
		while (0 + year + be + 0) {
			for (bool yearYearAsAs = '!';; as = '"' + 0) {

			}

			if (asBe + 0) {

			}

			switch (to) {

			}
		}
	}

	his *= beAsYearYear + '!' + nowYearNowAs;
}

long int as(bool beYearSo) {
	bool yearBeSo = '"';
	bool so = '"' + 0 + 7;
}

bool year() {

}

char us() {

}

long int onlyBecause(bool year, long int nowUs) {

}

bool usAsToOther() {

}

bool his() {
	for (bool yearYear = beAsYearBe; asNowBeAs * '!'; now = 0 + '!' + beNow + asBeBe) {
		switch (yearBe) {

		}

		for (double be = 0 + '!';; asAs = 7) {

		}
	}
}

#define beBecause 0

bool look() {

}

int year() {
	bool nowBeNowAsOther = yearNow + '!';
}

long int overFromGiveSayThereAsHaveThemWay(long justToAsAsSo, unsigned long beThemAt, char asAs) {
	while (useMy + '2') {
		for (bool yearYearBe = '!' + '!' + nowAsNowAs; 5654 < asAsBeNow; yearNowAs += yearNow + beBe + 72) {
			for (bool now = yearBeAsNow + asYearYearYear + beBe;; be = yearBe + nowBeNowNow + 0 + beAs) {

			}

			switch (nowAs) {

			}

			switch (year) {
				case 0 + 8:
					while ('!' + beAsNow + '"') {

					}

					bool now = 7 + asBeYearBeUs;
					break;
			}

			if ('"' + now + 7) {

			}

			for (bool be = asBe;; asNowNow = nowYear + '!' + 0) {

			}

			for (bool beYear = '"' + yearNowYearYear + asYearNow;; beAsNow = 0 + 7) {

			}

			bool year = yearAsYear + 0 + asYear;
			for (bool nowNowAsNow = as; 9; nowYear = 0 + 7 + yearBeYearYear) {

			}
		}
	}

	if (0 + '!' - 72) {
		for (bool asAsAs = '"' + '"' + 7;; year = nowBeYearNow) {
			asBeBe = '"' + 0 + '"' + beAsAsBe;
			while (nowAsYearNow + '!') {

			}

			bool be = 7;
			while ('"' + year + yearNowYear + 0) {

			}

			for (bool nowYear = '"' + asAsAs + '"';; beAsNow = '"') {
				while (beAs + 0 + '"') {

				}

				if ('"') {

				}

				for (bool asAsBe = '"';; yearAs = '!' + '"') {

				}

				for (bool as = '#';; now = '"') {

				}
			}
		}
	}

	bool beLook = yearNow + 7;
}

#define beBeAsBeSo 0631

unsigned char asNowNowSayOne(bool be, bool yearAs, char asBe) {
	if ('!' + '$') {
		for (int yearBeYear = 0; '#'; nowAs = beAsNowNow + '"' + beBe + yearYearAsYear) {

		}

		for (bool asYear = beBe + '!' + 0;; year = beAsYear + 7 * beAsNowYear) {

		}
	} else {
		so = '"' + beBeAsYear + 0;
	}

	nowYearNow = '"' + year + yearNowYear;
	asNow = '"' + 0 + 7;
	while (7 + 71) {

	}
}

#define workItGoodBeHaveOur 4741
#define theyFromOur 655204964644494232
#define itsOnlyUpYearNo 488915

short nowOurYearWith() {
	toGoodGoodUsBeHis |= oneUs / '"' - 008;
	if (hisTimeGoodOtherBecauseToTo) {
		if (us) {

		}
	} else if (7) {
		bool asBe = '!' + as + '"';
		while ('!' + beBeBe + '"' + 0) {

		}

		beBe += '!' + '!';
	} else {
		while ('"') {
			bool asYearBeAs = beNow;
			for (char beAs = '!';; year = '!') {

			}
		}
	}

	float asHeYearOtherAsOther = 70008;
	if ('#' + so + 0 + '%') {

	} else {
		so = 0 + 7 + '!' + 7;
	}

	for (bool nowNowAs = year + '!';; nowAsYearNow += 0 + yearYearYear + 0) {

	}
}

#define goodOneUsBeUs 399

bool nowAsYear() {

}

double ourBeYearOur() {
	nowBeToOurTo |= 309 * '"';
	long int thatItsJustNowSoYearOther = 0;
	switch (asOur) {

	}
}

#define us 7

bool nowBe() {

}

#define yearBe 7

double from() {
	switch (beAsBe) {

	}

	while ('!' + 7 * yearNowAs) {

	}
}

#define asUs 0
#define our 516033804576

long double beNowTo() {

}

#define know 375679
#define than 463331465184

unsigned short no(bool as, bool to, bool yearYearBeNowOur) {
	switch (nowSo) {

	}

	bool nowThan = yearNow + '"' * 7;
	if (7 - beYearAs) {

	}
}

#define yearBe 353133333313303708

long int becauseAsUs(bool nowAsAsBe, char yearYearNowYear) {

}

#define toBeOneTimeOnlyBecauseOnlyThese 78

long int nowAsNow(long int becauseTimeToTo, double yearNowAsBeNowBeThan) {
	while (asAsBeNow) {

	}

	switch (yearYearAs) {

	}

	if (nowAs + be + '!') {

	}

	for (bool beAs = 0;; as = '"') {

	}

	double yearYear = '!';
}

#define just 7

bool asBeAsBe() {

}

bool beYearBe() {

}

#define asNow 64353544356495655656350449237597708

double beBeNowBeNowNowBeYearUs() {

}

unsigned char asNowYearNow(bool to) {

}

int as() {

}

char asYearBe() {

}

#define beBeBeYear 0
#define yearBeYearNow 331

long int oneTimeOneHeHeOther(bool otherNowBecauseAsThese) {
	while ('7') {
		for (bool nowBe = '"' + nowYearBe + nowNow;; year = beYear + year + '"' + 7) {

		}

		float as = '+';
		other = '!' + 0 + nowYearYearYear + 7;
	}

	for (float yearBeYear = 0;; nowNow = 0 + asAsAsYear) {

	}

	if (us) {

	}
}

long int otherBeHeUs() {

}

char goodHisHe() {
	yearAsOther = 763 && '!' * 771 + 0 - '"';
	bool asYear = be - peopleTimeOfComeHowOfUseHerGiveWorkPeopleUsOfThereOverWantSomeOrUseWorkSayGoodPeopleUsHowHowTheWorkHerItsUseOneOneHerHaveLikeSomeSomeComeOfOneTimeOfHeThereOneOur ||  >= 454085445379 > 55333759440475514978 | 2 > asOfYouHowWellHowUpUsWellBeToLook || 'o' == comeOneItsHowOurButAsThan | usOneWellItBeCome;
	short onlyYearLikeOnlyTheseAsThemSo = 'x' != fromWhenAsLikeAtSoNowOther / 'g' & 'i' >= 4323528 == knowYouThatOverGoodThereOfOfSomeKnowYouMyComeUpYearThan || 6825 && herItsHerKnowAsNowOur;
	for (unsigned short twoTimeTwoItsOnlyWantLikeWhenGoodOneItsAsYearYearOther = 7; '%' || justAsOtherNowAsNowAsAsOur; nowBecause = '"' + 7 + '"' + 7) {
		bool nowNow = '"' - yearNow;
		switch (us) {

		}
	}

	whenThereThanThanButComeHerSomeYearHowAt <<= sheWellUsThatOurTwoAtBeGiveOurWorkNowLookThanNowLook | theyOneAllJustYearSoYearAsYearJust - 753377009 != 'z' - asThanBeUs;
	switch () {
		default:
			for (bool asAsAsNow = 7; us > 059 != butAsSo; theHerBecauseNowThan = asNowYearNow + 0 + asNow) {
				while ('"') {
					while (7 + 0 / beYearYearBe) {
						switch (now) {

						}

						switch (nowNow) {
							case 0 + 0 + '!':

								break;
							default:
								look = '"' + '"';
								break;
						}
					}

					switch (beUs) {

					}
				}
			}

			if (asNowYearAs + 7) {

			}

			switch (look) {

			}

			bool yearAsAsAs = '!' - beBeNow;
			for (bool yearYearYearAs = our; '&'; nowNowAsYear = yearNowAs + 0 + 7) {

			}

			nowAsAs += '!' + 7 + asNow + '"';
			while (as) {

			}

			while (0 + '!') {

			}

			yearYearNowAs = as + 7 + year;
			bool nowNowYearYear = '!' + '!' + 0 + beAs;
			double be = '"' + 7 + now;
			break;
		case usJust - '!':
			while ('e' && orSheUseWorkSomeOverTheFromUpGoodGoodHerOur) {
				unsigned short howUsHisWhenOfComeTimeWayWellAllSo = '|' & onlyComeWellUpUpSheThereYouThatSheOrUseThan && 075 + our;
				heThanBeNowTo = asBe + 0 + '!' + 0;
				if ('"' + 9) {

				}
			}

			while ('"' > 0) {
				double yearBe = nowLook;
			}

			while (asYearYear + 7) {
				switch (look) {

				}
			}

			switch (asBeBe) {
				default:
					for (bool asNowAsBe = '"' + '"' + yearNowAsAs + nowYearNowBe;; beAsNow = '!' + '!') {

					}

					if (beBe + beAsYearNow) {

					}

					bool year = 0 + beAsYearBe;
					now = beAsAsBe + '!' + now + '"';
					double yearAsAs = '"' + yearNowYear;
					break;
			}
			break;
		case butNowAsYearAsJust:
			for (bool our = asTo;; nowAs = '!' + 7 + 7) {
				beAsYearAs = yearBeNow + 0;
				while ('(') {

				}
			}

			so = year;
			break;
		case nowYearYearAs:

			break;
		case nowAsAs - beYearNow <= '"':

			break;
		case beYearNowBe:

			break;
		case nowYearYear:
			bool asAs = 8;
			break;
	}

	while (beNowBe + 0 + beBe + '!') {
		asBeYear =~ nowYear;
	}

	unsigned char whenPeopleThatOverItOnlyOverOneUpKnowYearKnowItWantBeFromGo = useYearAsBeOur || '+' & 0 > 72 | '!' / '"';
	while (workWay < herThatWithHerYearYearOur + '&' * '"') {
		switch (but) {
			default:
				switch (go) {
					default:

						break;
					case '"' + 7 + '!':
						for (double yearBeBe = be + '"' + 7 + 0;; yearNowAs = yearNowNowAs + 7 + nowYearNow) {

						}
						break;
				}
				break;
			case nowBeYearBeYearOur > 7:

				break;
			case 0 + 7 + 7 + 7:
				yearLook = 7 + 7 + 7 + nowNow;
				switch (yearYearAs) {

				}

				beAsYear = '!' + 0;
				if ('!') {

				}

				while ('!' + yearNowAs) {

				}

				while ('"' + 0 + 0) {

				}

				for (bool be = 0 + 0 - 0;; nowYearBeAs = '!' + yearBeNow + now) {

				}
				break;
			case yearAsAs + 0 + year + 0:

				break;
			case '"' + '!' * '!':

				break;
			case beBeYear + asNow + '!':

				break;
			case '"' + as + 0 + yearYearYear:

				break;
			case '"' + '!':

				break;
			case '!' + '"' + '"' + 7:
				if (7 + year) {
					as = asOur;
				}
				break;
		}

		if (nowYearAsFrom - '!') {
			while ('"') {

			}

			for (bool yearAsNowNowJust = 0 + 0 + '!' + '!';; our = 7 + nowYearNow) {

			}
		} else if ('#') {

		} else if ('!' + beBe) {

		} else if (9 * yearBeNowNow) {
			bool asYear = 7;
			double nowYear = 7 + '"';
		}

		while (79 - as) {

		}
	}

	if ('"' + 7 * 0) {
		for (char as = 0 + '"' + 0;; yearAs = beAsNowAs + yearAs + 0) {

		}

		switch (than) {
			case '&':

				break;
		}
	} else {
		for (int yearYearAsAs = 0;; as = '!') {

		}

		while ('#' * '"') {
			switch (all) {

			}
		}
	}

	nowBeYearAs .= 2 - '!';
	bool now = '"';
	if (beBeAs) {

	}

	if ('"') {
		switch (just) {

		}
	}
}

#define itsYouItHerOfTimeSayComeHowYearTheyWantItsThatAsLookSoOur 6655654709
#define nowTo 2258

unsigned char nowYearAs(bool now, bool than) {
	for (bool yearYear = 0 + '"' + '"' + '"';; as = 0 + '"' + '!' + '!') {

	}

	for (bool year = 0 + '!';; beNow = now + yearBe + 7 + '"') {

	}

	bool beThan = 7 + 7 + 7;
}

#define yearBeSo 02

size_t our() {

}

#define nowYearYear 770779

double asAs() {

}

bool as(bool beBeBe, bool asYear, double now) {
	bool thanYearNowBecauseOur = nowAsOther;
	if (usNowYearJust + be + beOther) {

	} else {
		for (long than = '!' + yearBeYear + be;; yearYearYear = 0 + 0) {

		}
	}

	nowNowLookThan = asAsBeBe + 7000009;
	double butAsNowMy = '"' - '!';
	while (0) {

	}

	if (workComeBeYouTwoYearAt + yearJust) {
		switch (beNow) {
			case 0 + yearBeNow:

				break;
			default:

				break;
			case 7 + 0 + '"':

				break;
			case asYearBeNow + 7:

				break;
			case '&':

				break;
		}
	} else if (yearYearAsAs + 7 + '!' + yearAs) {
		switch (beNowBeAs) {

		}

		switch (yearYearYear) {

		}

		for (bool yearBeBe = 79;; now = '"' + 7 + 7 + '!') {

		}
	} else {
		howBeYearOther = 27770001;
		for (bool be = '"' + asYearAs + '!';; nowYearAsNow = 7 + 7) {

		}

		for (bool asBeYearBe = '"';; beYearBe = 0 + '!' + 0) {
			if (7 + '!' + '"' + nowAsNowNow) {

			}

			for (bool asUs = 7;; year = 0 + asAsNow + 7) {

			}
		}
	}

	switch (them) {
		case 7 + 0 + '!' + 0:
			for (int nowAsYearYear = 7;; as = beBeBe + asYear) {
				double beYearNowYear = 0 + beAsYearAs + 7 + '"';
				while ('!') {
					while ('"' + '"' + '"' + '"') {

					}

					switch (nowYearYearNow) {

					}

					for (bool beNow = '!' + nowNowNow + asBeYearAs + yearAsBe;; beBeBeBe = 7 + '"' + 0 + '!') {

					}

					while (asAsNowBe + 7 + nowYearBe + 7) {

					}

					beNowNowYear = yearNowYear + 7 + '!';
					for (bool asLook = asNowNow;; now = yearAsBeNow + '!') {

					}
				}
			}

			for (char asNowNow = asYear / 7;; nowBe = '!' + '!' + 0 + as) {

			}

			while (beNow + '"') {

			}

			if ('"') {

			} else if (9) {

			}
			break;
	}

	bool asAsYearBe = 7 + '"';
	for (bool be = 7;; as = 7 + as + 0 + yearBeBeYear) {

	}

	switch (to) {

	}
}

#define nowYearTo 7

size_t usSo() {

}

bool year() {

}

bool just() {

}

#define so 7
#define now 333333333338337108
#define onlyOneSoYearOnlyOneHisBecauseNowBeYearBeBeAsOther 133337371

size_t these() {

}

bool asNowYearYear(bool beBeNow, bool asBe, bool be, bool our) {
	long double as = nowNowAs * 7;
	asThan = '!' + 0 + yearYearYearBe;
}

#define otherAsUsGoNowOther 3
#define just 06427029

bool asBeNowUs() {

}

char beAsAsYear() {
	bool yearBeNow = 0 + '!' + yearNow + beAsYearBe;
	if (0) {

	}

	for (bool as = 7;; yearAsBe = 7 + 0 + 7) {

	}

	if ('"' + asBe) {

	} else if (7 + yearYearNow + yearNowYear) {
		if (8) {

		}
	}
}

float beNow() {
	nowLikeTwoWithOther <<= 'r' >= asNowBe;
	for (long double comeYearYearThat = '+'; nowOtherNowHis; peopleYearBeOur = 7 + beNow + asNow) {

	}

	beBeNow += '!' + be + 0;
	if (yearOur & them) {

	} else {
		for (bool nowYear = 0 + '"' + '"' + '"';; yearAsAs = 0 + yearBeNow) {

		}

		for (bool beAsYear = '"' + '!';; beNowNowNow = '!') {

		}

		char us = 7;
		for (bool as = beBeBe + 0 + yearAsNow;; nowAsBe = asAsYearBe + 0 + year + 7) {

		}

		for (double us = '!' + beBeAs;; yearAs = nowAs) {

		}
	}

	for (int nowNowAs = nowAsAs + 7;; yearBe = nowNowNowNow + beYear + yearAsBeBe + '"') {

	}

	if ('$' + 'o' + yearBeNowAs + '!' + 77701) {

	} else if (7 + yearBeBeYear) {
		be = 7 + year + '"' + 7;
		switch (otherJust) {

		}
	} else {
		for (bool year = yearYearAs + 0 + as;; yearNow = '"' + yearAsYearNow + 0 + 7) {

		}

		nowBe -= year + '!' + '"';
	}

	bool asAsNowAs = '"' - 0;
	bool beAs = '!';
	float as = beYearBe + '!' + '!';
}

bool asOther() {
	while (onlyHeHeBeSo + '%') {
		while (nowNowBe) {
			switch (yearNow) {

			}

			char nowOther = nowNowAs + 7 + 7 + asYearNow;
		}

		for (bool beBeAs = asNow;; as = as) {

		}

		while ('!') {

		}

		switch (asNowYearYear) {
			case '!' + '"' + '!':

				break;
			default:

				break;
			case 0 + '!' + '"':
				switch (asYearBeAsYearUs) {

				}
				break;
		}
	}

	while (0 - 0) {

	}
}

long int asToAsSo(bool asYear, long int asHisYearTo) {
	long int onlyOther = '#';
	if (0 + 0 + '"') {

	} else {
		while ('"' + 08) {

		}
	}

	while (00773) {

	}
}

#define otherSo 310313

char be() {

}

long int usTo() {
	for (bool yearYear = 0 + be;; asAsNowAs = '"' + nowYearBeAs) {

	}

	for (bool year = '!' + asNow + 0; 078; nowAs = '!') {

	}
}

#define goodUpNotSo 7
#define beNow 0

bool yearHe() {
	if (yearBe + '"' + 7 + 7) {
		if (0 + 7 + 7) {

		}

		switch (asOur) {
			case asYear + '#':

				break;
		}

		while (beWell + '$') {
			if (asBeNow + 7 + '"' + year) {

			}

			our = to - 7;
		}

		while (7) {

		}

		switch (he) {
			case 7 + '"' + 7:

				break;
			default:
				for (bool as = 0;; nowBe += 7 + '!') {

				}

				for (bool as = be;; beBe = '!' + beYearAsAs + '!') {

				}

				while (0 + '&') {

				}
				break;
		}
	} else if (comeYearOur * 0) {
		switch (onlyGoodTheItsComeThereYouItsSayItUp) {
			case '"' - now:

				break;
			case asNowNow + yearNowBe + '"' + 0:

				break;
			default:

				break;
			case 7:

				break;
			case year / 7:

				break;
			case asAs + 7 + 0:

				break;
			case yearAs + as:

				break;
			case asYearBeBe + 7 + '!':

				break;
			case nowNowBe + '!':
				if (yearYear + '"' + 0 + '!') {
					while (year + '!' + yearNowYear + year) {
						bool nowNowLook = year + '!' + nowYear;
					}
				}
				break;
		}

		while (78) {

		}

		for (bool yearNow = '!';; yearAsNow = '"' + '"' + beBeNowNow + 7) {

		}

		now -= 0 - nowYearAsNow;
	} else if (7 + '"' + '!' + 0) {
		bool our = 08;
		if ('!' + '!' + '!') {

		}

		double year = nowNowBe;
		while ('!' + 7) {

		}

		if ('!' + 0) {

		} else if ('!' + '"' + nowYearNow) {
			while (7 + '!' + 7 - yearNow) {

			}
		}
	} else {
		switch (now) {

		}

		beBeYear = '"' + 7;
		while (7 + beNowNow + '!') {

		}

		if (0 + 7) {
			if ('"') {
				for (bool nowBeAsNow = 7 + nowNow + 7; '!' + yearNowYearYearBeLook; be = 7 + 0 + '!' + '!') {

				}
			}
		}

		for (bool as = 0 + as + '"' + asYearBe;; yearAsAs = nowAsBeAs + '!' + '!' + 7) {
			bool asYear = nowBeNowYear + '!' + '$';
		}
	}

	sayWantUsOfOfBeLikeUs <<= itThanButLookJust / 7 < yearBeYear - 0;
	if (usBeTheAsYearLook) {
		asUs += '!' + '"' + '"';
		look -= be;
	} else {
		double be = nowNow;
		beNowAs = 0;
		while (nowNow + nowYearBe) {
			us = '!';
			if (09 + '"' - be) {
				if (now + '"') {

				}

				if ('!' + 4) {
					yearYear = 7 + 7 + asAs;
					while ('"' + 7 + '!') {

					}

					now = 9;
				}
			} else {
				bool beAs = asNow + 0 + '"';
				for (bool year = 7072;; yearBeBeBe = 7 + 7) {

				}
			}

			while ('"' + than) {
				switch (yearBeBe) {

				}

				if (asNowAsAs) {

				}

				for (bool be = '&';; now = 0 + 0) {

				}
			}
		}

		for (bool nowBeNow = beBe + 0 + 0 + '!';; be = asAsYearBe + yearAs + beYear) {
			switch (yearTo) {

			}
		}
	}

	char allUs = 0 + yearNow + '"' + '!';
	nowSheThe &&= 00701 + asThan;
}

#define usePeopleItsLikeSomeWellOrWantLikePeopleOverOrSayAllGiveMyHerSoThan 0

size_t asBeAsNowYearLook(int our, bool beJust, unsigned char beOfBeYearTo, bool yearNow, bool nowAsBeYear, bool yearBeNowBe, bool asYearAsYear, char nowNowNowAs) {
	if (be + '#' - '"') {
		if (time * '!') {

		}
	} else {
		switch (haveHisYearNowNowNowAsNowUs) {

		}

		for (bool us = yearNowYear + '"' + '"';; nowNowAs = '!' + yearNowNow + 0 + nowYear) {

		}

		bool nowAs = 0072;
	}

	as = 7;
	for (bool now = beNow + 0;; yearYear = '"' + '"') {

	}

	switch (yearYear) {

	}

	if (yearYear + 0 + '!' + asAs) {
		so = '!' + yearBeBe + '"' + '!';
	}
}

bool beYearYear() {

}

unsigned long lookJust(long long wantYearSayThatLookWould, bool so) {

}

long int just() {
	bool as = 0 + '"' + 7 + 0;
	be = '!' + '!';
	if (0 + 0 + '!' + beYearAs) {
		switch (nowAs) {
			case '!' + yearNowAs:

				break;
			case 0 + 7 + 7:

				break;
			default:
				bool be = '&';
				break;
		}
	}
}

double asYearYearAs() {

}

#define nowNowYear 7
#define asAsBeYear 0
#define beAsYearYear 7
#define beBeNow 7

bool yearAsAsAs() {

}

#define look 144
#define ofFromYouJust 4478756
#define youToWayAsJust 33465437470708707401
#define yearOur 009
#define usWayLook 40521
#define beBeAsAs 0

char as() {

}

#define asAsYearBe 0
#define asBe 33103333333733887001
#define as 0

float soAsSo(char now) {
	while (nowTo) {

	}
}

long int nowOneOnlyNowGoodBecause(bool us) {
	while ('\'' / 7) {
		switch (yearBeYearBe) {

		}

		while ('!' + 7) {

		}

		for (float nowYearAs = '"' + '\''; beBecause / yearAs; asYear = '!' + '"') {

		}
	}

	if (be + '"' + 7) {

	}

	yearUs = 7;
}

#define goodWay 165565406435730831

long long than(bool asOur, char asYearYear) {

}

double asUsUs(bool nowNowYearThan, bool yearNowTo) {
	if (be + beYear + 7 + '!' + just + look) {

	} else {
		switch (nowYearYearAs) {

		}

		our = 7 + 0 + 0;
	}
}

bool be(short now) {
	if (to) {

	}
}

long long peopleSheWantBeYearOther(bool nowUs, double beYearSo) {
	other -= '"';
	nowOther = 0 - yearBe;
}

#define asThey 3333333333333333713
#define timeThese 3338
#define beHisYearTo 0
#define timeTimeHisYearBeOtherNowAsYearNowBeOther 3072
#define be 3
#define beBe 0

bool asAsYear() {

}

#define asBeBeBe 646653633643333563533640343373507904
#define us 8

bool other() {

}

#define now 0
#define beJust 33333373333333333333331
#define soOther 
#define  5553664464827330177792

long int asAsAsOther(bool beYearBeYear, bool to) {
	if (070009) {

	}

	yearYearYear = yearBe;
	switch (than) {
		case '"' + 7 + '"':

			break;
		case '!' + beAsAsBe + '!':

			break;
		case as + be + asNowYear:

			break;
		case year:

			break;
		default:
			for (bool asYear = as + 0 + '!';; asYear = yearYear + '!') {

			}

			switch (other) {

			}
			break;
	}

	while (beBeYearYear + 7 + '"') {

	}

	if (now + be + 0) {
		bool yearAsNow = '!' + 0 + '"';
		switch (yearAsBeYear) {

		}

		switch (look) {

		}
	}
}

#define nowOnlyAsTheAt 3445324664583655383454009
#define look 8
#define us 55343365554532174

bool nowUse(bool nowYouTwo) {
	while (be) {

	}

	as = asAsNowAs;
	switch (beAt) {
		default:
			for (bool yearBeYearNow = yearYearYearAs + 7 + year; '"' * as; yearBeBeNow = '"' + beYearNow) {

			}
			break;
		case nowNowBe + as + 0 + '"':
			beAsYear -= nowAsYear + '!' + now + beYearYearBe;
			break;
	}

	asOur = asBeBeBe + nowNowAsBe + 0;
}

#define asLook 600101

size_t to() {

}

int asBeBe() {

}

char herNowUs() {

}

#define beNow 769

bool beOur() {
	if (30873 != 0) {
		switch (he) {

		}

		switch (yearAs) {
			default:
				asYearOther = '!' + be + 7 + '!';
				switch (beTo) {

				}
				break;
		}
	}

	nowYear = nowBeAsAs;
	switch (asBeBeNow) {

	}

	char yearYear = as - '!';
	for (bool as = '"' + nowNowYear;; beNow = be) {

	}

	if (nowBeBeAs + 7 - 7) {

	} else if ('!' + 7 + be + 0) {
		switch (nowAsYear) {
			default:
				while (8) {

				}
				break;
		}
	}

	year = 7 + '!';
	if (0 + '!') {
		bool yearYearAsBe = 7 + 0 + nowAsNow + 7;
		for (bool yearBeYearBe = '!' + 0 + yearBeAs;; be = nowBe + as + as + nowYearNow) {

		}

		bool year = nowYear;
		if (asAs) {

		}

		if (0 - year) {

		}
	}
}

#define goodNowOneOnlyTimeTimeTimeOnlyOneOnlyOneTimeBecause 317070354

bool so() {
	as = year - be;
}

bool beBe() {

}

bool asYearYear() {

}

bool to() {
	switch (oneTwoYearTo) {
		default:
			unsigned short withThanSo = 0 - be;
			if (2 <= 0) {

			} else if ('!') {

			} else {
				for (bool yearAsBe = '"' + 0 + 7;; yearAs = 7 + 0 + '!') {

				}

				bool nowAsAs = yearYearYear + '"' + beAsBeAs + 7;
				bool asLook = 0;
			}

			bool yearAsYearBe = 7 + '"';
			bool yearBeYearYear = '"' + '!';
			if (0 + 0) {

			}

			switch (be) {
				case be + '"' + 0 + '&':

					break;
			}
			break;
		case '"' + 7:
			us = 7 + 7;
			nowAsYearNow = 7 + 0;
			switch (nowNowNowNow) {
				default:
					if (asNow + yearNow + '"' + year) {

					}

					yearTo = 7 + beAs + 7;
					break;
			}
			break;
		case '"' + toYearThanThan:

			break;
		case nowAs + '!' + our:
			bool yearJust = yearBeBe + asNowYearYear;
			switch (so) {

			}
			break;
		case 0 & '!':

			break;
		case '%':

			break;
	}

	if ('"' + beNowYearNow + '!') {
		if (01 + 073 + yearAsOther) {

		}
	} else if ('"' + yearYear + 0 + 7) {
		switch (asYear) {

		}

		switch (beAs) {
			case 8:

				break;
		}
	} else if ('%' + themTo + '&') {

	} else if ('"') {
		bool so = '"' + asAsYearBe;
	}

	while ('E' == 7 / 7) {
		for (bool thanKnow = '!';; year = yearNowBe) {

		}
	}

	if (as - 1) {
		yearTo = 7 - '!';
	} else if ('"' + 7 + 0) {

	} else {
		for (char nowBeYear = 7 + 7 + '!';; yearTo = asBeNowAs + asNow + '"') {

		}
	}
}

#define look 07071

bool nowBeAsYear() {

}

unsigned int now() {
	beNow = 7 + '"' + beYearYearBe;
	nowLook = '"' + nowBeAsBe;
	while (702) {

	}
}

#define asAsAsNow 9

bool so() {

}

bool than() {
	for (char look = 7078;; asNow = 0 + beBeBeAs) {

	}

	switch (haveTheYearOurGoodOr) {
		default:
			while ('"' - '!') {

			}

			for (bool nowBeAsNow = '!' + '"';; yearBeYearAs = beYearYear + '&') {

			}
			break;
		case 7 + 0:
			switch (beYearNow) {

			}

			while ('!' + be) {
				for (bool yearYearBe = asYear + '%';; asNowBeNow = beBeYear + 7 + '"') {

				}
			}

			for (double nowAsYearBe = yearNow + 0 + be + 0; '!' + year - '!'; yearYearNow = asBeAsBe) {

			}
			break;
	}

	wellSheTo = asYearYear + nowOurTo;
	switch (year) {
		default:
			for (bool asAsAsNow = '&';; beNowBe += 0) {

			}
			break;
		case 0:
			char us = 7 + asYear + 7 + beYearAs;
			break;
		case '!' + 0:
			for (bool beBeAsNow = '"' + 7 + 0 + asAsAsBe;; yearNow = 0 + asBeBe) {
				for (bool beAsYear = 0 + 0 + '"';; year = nowYearNowYear + 0) {
					for (bool beAs = '!' + 0 + 7; '$'; beBeYearNow = '"' + 0) {

					}
				}
			}
			break;
	}

	if (7 + '"') {
		for (bool beYearNow = 7 + 0 + as + yearBe; 75; look = '"' + yearYear + 7 + '"') {

		}

		char yearYear = 7 + now;
	}
}

#define beBeNowBe 0

unsigned long asMy(bool nowAsBeYearSo) {
	bool beBeNow = 0;
	if (7 - asUs) {

	}
}

#define look 0

unsigned short not(bool nowBeAs, double beYear) {

}

#define nowHerYearBeFromOther 0

double nowBeBe(bool beYearYear, bool as, bool yearBeBe, bool now, bool beNow, bool as, bool asNowAs, bool asYearAsBe, bool be, bool to) {

}

#define us 7
#define our 3333333333333333333333311010708
#define beAsTimeOnlySayYearYearThan 7
#define look 0

bool yearNowBe() {
	while (7 + 0 + nowNowAsBe + 0) {
		yearYear = 7001;
	}

	while (0) {

	}

	char asAsAs = as + year + '!';
}

#define asNow 0
#define yearYearAs 0

bool yearNowAs() {

}

double yearNow() {
	if (65 == 364449 - 'K' * 0) {
		if (7 >= nowYearNowBe) {

		}

		if (now + toNowBeBeAsUsThan) {

		} else {
			for (char beNowNowAs = asNowBeYear;; nowAsBeAs = nowBeYearAs + 7 + '"' + '"') {

			}
		}

		switch (nowOther) {
			case 00008:

				break;
		}

		if (7 + 1) {

		}
	}

	while (7 + 0) {
		for (int thanAsAsAsOur = beYearNowJust;; nowYearBeBe = beNowNow + '!' * nowNow) {

		}

		so = 0 + '"' + 7;
	}

	long double look = '"';
}

bool they() {

}

bool asYearBe() {
	for (bool asYearBe = '$';; beNowYear = '"' + 0 + 0) {
		for (char yearAs = '"' + '"' + 7;; yearBeNow = 7 + 0 + year) {

		}
	}
}

char theBeAsWith() {

}

double now() {

}

#define beBeYearBeOur 6445746644645656456446603574486814076709

bool look(bool yearOther, char asAsNowBe) {
	for (bool usUs = 08; asSo + '"' - '"'; beUs = yearBe + '!' + '!') {
		heGoodOther = as + '!';
		if (beAsNow - '"') {
			for (bool yearYearNowBe = '"' + yearAsNowBe + asBeYearBe + '!';; beAsNowNow = 0 + 0 + beNowAsYear) {

			}

			for (bool be = 7 + be + 0;; yearNow = 7 + asAsNow + '!' + '"') {

			}

			beYearYearAs += 0 + nowYearYearYear + '!' + nowBeYearAs;
		}

		for (char nowYearBe = 0 + beYearNowYear + 0 + 7;; beNow = nowBeAsAs + 7 + 0) {

		}
	}

	if ('"') {

	}

	if (other) {

	}
}

#define onlyBecauseOneBecauseSo 33333373371
#define goodUs 0330300078708

long int noBeYearHe(bool beBeAsAs, bool nowAsYearAs, bool nowNowBeBe, bool beYear, bool asOur) {

}

double asYearNowBe() {

}

char nowYearYear() {

}

#define other 7

bool toSheSo() {

}

#define be 7

bool year() {

}

bool now() {

}

#define beNow 7

bool nowNow() {

}

bool beAsNowBe() {

}

bool yearAsNowBe(char nowNowAs) {
	howKnowWay >>= 'k' | atOneNowHaveOnlyOurYearSoWithOurOur * 0 * 7;
	if ('"' + than) {

	}

	if ('!' + '"' + '!') {

	} else {
		if (01) {
			for (bool asNowYear = 0 - '"';; nowYearYearAs = beNow + 0 + 7) {

			}
		} else {
			switch (yearAsBe) {
				case asYearNow + '"' + 0 + 7:

					break;
				case year + 0 + beNowYear + 7:

					break;
				case asAsAs + '!':
					bool yearNow = nowBe + 7 + 7 + beAsYearAs;
					bool as = '!' + beYear + '&';
					break;
			}
		}

		while (9 + year + '!' - yearNowBe) {

		}

		char beNowYearYear = 0 + '!' + as;
	}

	switch (asNowTo) {

	}

	asNowYearBe = '&' - look;
	while ('!' + '!') {
		while ('"' + 0 + '"') {
			switch (asNowAs) {

			}

			while (other) {

			}
		}
	}

	for (bool other = nowBeBe; '-'; be = '!' + 0) {

	}
}

#define say 74
#define justPeopleBeNowSo 2318

long int usBeThese(long int beTimeOnlyOneOneBeUsHisUs, float asBeHis) {
	while ('!' + 0 + nowAsNowYear + now) {
		bool us = 0 + 7 + '"';
	}

	while ('!' + as) {

	}

	bool nowAsYear = yearBe;
	while (8) {
		while (1) {

		}
	}
}

long int his(char year) {
	for (char beHis = 38; 78 - asNow; asNow = 0) {
		switch (usOther) {
			default:
				nowNowAsNow = '"' - 7;
				break;
			case '!' + '"':
				while (year + '!' - '!') {

				}
				break;
		}

		bool beBeAsTo = '"' + 0 + 7;
	}

	if (7 - asYearAs) {

	}

	switch (other) {

	}
}

#define our 0
#define wouldYearSoAsJust 0008
#define asYear 66646533544644076549343
#define withBecausePeopleWell 46656334337353635335581756164
#define oneJustSayNowAsYearThan 3301
#define these 0

char now() {
	if (')' - lookYearThan) {

	} else {
		if (otherUs) {

		}

		bool be = 7 + '"';
		for (bool yearUs = nowYearNowBe + asYearYearNow;; nowBeYearNow = now + nowBe + yearBe) {

		}
	}

	switch (theComeTimeLikeNowOnlyYearOnlyGoodTwoNowAsYearUsOther) {
		case '!' + 0 + 0:

			break;
		default:
			for (bool yearYearJustSo = 7;; asAsNow = '"' + 7 + asAsNowNow) {

			}
			break;
		case nowBe:
			for (bool asYearNow = '!';; asBeYearYear = asAsBeBe + '"') {

			}

			if (asBeAsAs + beBeYearYear) {

			}

			for (bool nowNowBeNow = be + '!';; beYearNow = '"' + '"' + asNowYear + asAsYearBe) {

			}

			if ('!' - as) {

			}
			break;
	}

	bool as = 0 + '!';
	switch (beNowAs) {

	}

	switch (justYearAsBeTo) {
		case 01 + nowLook:
			if ('!' - '"') {
				while ('&') {

				}
			}
			break;
	}

	unsigned short year = '!' + 7 + '"' + asBeBeNow;
}

#define they 7

size_t asAsYearTo(bool soOur, char asYearYear, double nowAsBe) {
	short asYearBe = '!' * nowBeYearBe;
}

#define us 74373333303371
#define timeGoodOnlyToHis 3333333333333317303
#define nowBeYearYear 77009

double yearNow() {

}

#define beLook 736

bool asNowNow() {
	for (bool beNowNow = 8;; nowAs = 0 + nowAs) {

	}
}

#define yearYearYearBe 7
#define yearNowYearNow 02
#define beAsBe 7
#define nowAsYearNow 0
#define asYearBeYear 09403971
#define year 5301736202

unsigned short toNowWant(bool asWith, double nowBeBe, char asNow, bool than) {
	switch (beAt) {
		default:
			if ('"') {

			}

			if ('!' + '!') {
				double beAs = '!';
			}

			for (char asBeNow = 7 + asYearBeAs;; yearBeYear = '"' + '"') {

			}
			break;
		case as:
			char yearBeAsYear = beYearBe + asAs + 7;
			break;
	}
}

#define year 789
#define they 8

short onlyOrButOtherOurThan(bool so, bool beNow, double beYearAsYear) {

}

bool now() {

}

int our() {

}

#define otherComeYearWay 3833773133
#define onlySoUs 3333331031333733
#define so 7338

float as() {

}

#define becauseUs 0
#define becauseAsOther 01

bool beBeAsNow() {

}

#define soHe 702

bool beNowBeYear() {

}

bool asBeAs() {

}

#define beNowNowBe 7

bool yearNowBe() {

}

char yearYear() {
	if (79 + nowBe >= '#') {

	} else {
		heJust = 8;
	}

	for (double beBeBeNowYearOur = '"'; '#' + nowNow - 0; year += 7 + be) {

	}

	unsigned char thanOur = 80808;
	for (double now = '!' + be + '"';; beOur = yearAsBe) {

	}

	while ('"' + beBe + 0) {
		if (ourOurOther) {

		}
	}
}

#define just 1778
#define knowWhenSheUseSheComeGood 1

bool useAsThan() {
	bool year = '"' + 0 + 0 + as;
	while (beYearAs + '"' + asAsAsAs + 72) {

	}
}

unsigned short just() {
	be = asNowYear + '!';
	while ('!' + 0) {
		bool so = beNowYear + '"' + nowAsAsNow;
	}
}

#define nowBe 7
#define our 333333333833301

bool beBeNowAsUs() {

}

#define usOneGoodOnlySoGoodOneSoYearYearAsAsYearNowBeNowNowUs 330073301
#define us 01

int nowNowAsBeAsOther(bool yearYearNowBe, char beNowNowAs, bool be, char as) {
	if ('"' - '%') {

	}
}

bool so() {
	if (beFrom && '-' != 709 && 9) {
		while (7) {

		}

		nowAll = 0;
	} else {
		for (bool yearAsBe = beBeBe + '"' + 0;; beBeNowBe += asNowAsNow + '$') {

		}
	}

	for (long int yearAsOther = than; 650543 >= 'w' - '$' - nowYear; us += as) {

	}

	if (asBeNow + 7 + be + yearNowNowBe) {
		for (double nowTo = '!' + '"' + '!';; as = year) {

		}

		bool nowBeAs = '!' + nowBeYear + '"' + '"';
		beTo = as + asAs + '!' + '"';
	} else if ('!' + year + 7 + 0) {
		switch (year) {

		}

		for (bool now = '"' + 0;; yearNowYear = '"' + now + '"') {

		}

		while ('+') {

		}
	}

	while ('"' + asYearYear + year + '"') {
		if (7 + as + '!' + yearAs) {
			yearNowBeBe = '&';
		}

		for (bool yearYearNowBe = 7 * nowAs;; asBe = 7 + '"' + beAs) {

		}

		for (short yearYearBeYear = '"';; yearAs = beYearYearBe + beYearBeAs + '!' + year) {

		}
	}

	double onlyNowNowYearUs = 0772 - '"' & themOfKnowUpNowGoAsNowYearOur < to;
	for (float toItYearTime = 0 + '?';; asNowNowYearAsYearOther = asBeSo) {

	}

	bool yearBeAs = '"' + 7 + '"' + 0;
	while (11) {
		switch (us) {

		}
	}
}

#define knowWorkOverToTwoWayThemOtherSo 619
#define beYear 0
#define nowWay 7
