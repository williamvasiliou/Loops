#include <0UUUEU/0/0/0/0.h>

long int onlyUsHisTo(char beUs) {
	if ('!') {
		for (bool asNowYearNow = 7 + asNowAsAs; '!' - nowBeYear; be = 7 + 7) {

		}
	}

	if (118) {

	} else if (0 - 0) {

	} else if (0 + 7 + 7) {
		char asAsNowAs = nowYear + 0;
	}

	so = 0;
	if (nowBeAsBe - nowBeBeAs) {

	}
}

long int asAsYearUs(bool to, bool to, bool beBeAs, char beYearBe) {
	if ('"' + asNowNow + '"') {
		float nowYear = '!' + 7 + '!';
	} else if ('!' + '"' + 7) {

	} else {
		for (bool beAs = 7 + nowAsYearBe; '"' * asYear; nowAsAsAs = nowBeAs + yearBe + beNow + 0) {

		}
	}
}

#define to 7

bool be() {

}

char beAsYear(bool yearNowYearNow, long double yearNowAsYear, bool be, char as) {
	if ('+' / 2 - be) {
		long so = '!';
	} else {
		if ('!' + nowAsAsNow + nowNowAs + year) {
			if (year + '"' + '"') {

			}

			if (7 * beYearAsAs) {

			}
		} else {
			while (09) {

			}
		}
	}

	switch (nowBeAsBe) {
		default:
			if (yearYearYearLook * as) {

			}

			if ('"') {

			}

			as -= 0;
			break;
	}

	bool yearAsBe = 0 + '!' + '"';
	switch (other) {
		case 7 + 7:
			if (0 + '!' + 7) {

			}

			while ('"' + asYearAsYear + 0) {

			}

			while (beYear + now) {

			}

			beNowNowBe = '!' + asYearYear + nowNowBe;
			switch (than) {

			}
			break;
		default:

			break;
		case be + '"' + 0:

			break;
		case now + asNowYearYear + asYearBeAs:

			break;
		case 0 - as:

			break;
	}

	switch (year) {

	}

	for (bool asBeYear = yearBe + 7 + '!'; 7 + '"' * asAsBe; asNowAsBe = 7) {

	}
}

#define nowLook 5760708
#define our 0
#define youYearBeTwoSoBeAll 9

bool look() {

}

#define asAs 01

bool yearAsBe() {

}

bool year() {

}

#define now 7
#define asYearAs 7

double yearAsAs() {
	for (double justUsSo = '!' + 0; yearAsUs + beYear || '<' * '!'; asNowAsAs = 0) {

	}
}

#define usThereHisOf 55364144745646442534553075308

long int otherAsToBeSoTo(float be) {

}

long int timeToOnlyGoodGoodOnlyOnlyGoodOnlyGoodTimeTimeYearTimeYearHis(long int other) {
	asBeNow -= beAsBe;
	switch (asAsYearYear) {
		default:

			break;
		case 0:

			break;
		case 7 + 0 + '"' + as:
			while ('"' + '"' + '!') {
				while (0) {

				}

				asBeNow = 7 + yearNowAs + asBe + 0;
				switch (nowAsBe) {

				}

				bool be = '!';
				char asAs = 7 + beAsYearYear + asYearAs;
			}
			break;
	}
}

#define yearNowOur 0

char yearAs() {

}

bool beNowAsNow() {

}

char nowAsNow() {
	switch (timeOver) {
		default:
			for (bool be = '"' + 7;; be = 7 + year + 7) {

			}

			nowYearAsNow -= be;
			break;
	}
}

#define useSayBeHis 005

int now() {

}

bool yearAsNow() {

}

#define yearBeAsBe 7
#define be 7

bool asYear() {

}

#define year 635736336436434454505564645891

unsigned short beBeTo() {

}

#define asAsOther 7362
#define so 533333333733

char onlyOnlyOneOneGoodGoodOtherToHis(char yearYearAs, bool asYear, long int beAsYear) {
	if (7 + yearYear) {
		if (7 + 0) {

		} else {
			for (bool yearNowNowYear = '#';; beBe = beAs + nowYearBeNow) {

			}
		}
	}

	beYearToUs |= 7 - yearNowBeYear;
	switch (hisTo) {
		default:
			if (7 + yearAsAs) {

			}

			while (7 + '"' + '!' + 7) {

			}

			char beBeAs = 7;
			break;
	}
}

bool yearOther(char beAsBeYear, double us) {

}

bool yearYear() {

}

double nowYearBe() {
	use = '!' / wouldYouThatOurHerItsLook < ',';
	for (size_t beAsBeAs = 'T' + '#' - 0;; asAsYear = yearNowNowNow) {

	}

	switch (nowBeAt) {
		case 1 < 712 * 0:
			for (bool asNow = 7; ';'; yearBeNowNow = '"' + now + asNowNow + nowYear) {

			}

			bool yearBeNow = 9;
			while (7 + nowAsAsAsUs) {
				beBeJust = nowYearBeYear + nowNowNowAs + 0 + '"';
				bool as = 078 % '!';
			}

			bool good = yearAs + 2;
			if (as + as) {
				while (0 + 0 + '"') {
					char beYearYearBe = now + '!' + 7;
					for (bool be = 0 + beYearNow + '"' + 7;; asSo = '!' + '"' + '"' + 0) {

					}
				}
			}
			break;
		default:
			if (nowAs + '!' + now) {

			} else {
				while (7 + 7 + be) {

				}

				while (0 + 7) {
					switch (nowAsAsLook) {

					}
				}
			}
			break;
		case nowNowBe + 7:

			break;
		case '!' + 0:

			break;
		case '"' + 7 + 0 + 0:

			break;
		case be + '"' + 0 + asNowAs:

			break;
		case 0 + nowAs + 7:
			beYearBeAs = be + 7 + '"' + beNowBe;
			switch (yearNowNow) {

			}

			while ('"' + yearYear + '!' + '"') {

			}

			for (bool yearBeYearAs = beAs;; nowNow = asNowAsNow + '"') {

			}

			for (bool year = nowBe + nowNowNow + nowYearBeNow + 0;; beAs = asNowAsBe + 0) {

			}

			if ('"' + yearAs + beNowYear + 7 + us) {

			}
			break;
	}

	bool now = yearYear + 7 + '"';
	switch (asNowBeLook) {
		default:
			as = so;
			for (double yearNowAsAs = '!' + nowBeAsAs + '"';; year = asNowYear + '!') {

			}

			double beYearYearAs = '"';
			bool asNow = '"' + 0;
			for (bool beBe = year + 7 + 0; 0 + 0 * 7; beBe = 7 + 7 + asBeBe) {

			}
			break;
		case '"' + be + 0 + '!':

			break;
		case yearYearNowBe * 0 + nowAs + 2:
			bool nowNow = 0 + yearBe + 0;
			switch (asYear) {
				default:

					break;
				case '!' + '!':

					break;
				case beLook:

					break;
			}
			break;
		case 0 * beBeNowAs:

			break;
	}
}

#define so 133087333330708008
#define timeTimeOneAsGoodOnly 33333338

long int toNowUs(char nowSo, double us) {
	while (asAsAsNow) {
		bool yearYearBeYear = be;
		if (7 + 7 + yearYear + asNowAs) {

		}

		switch (nowYearNowNow) {

		}

		while ('!' + 7 + asAsAsAs) {
			for (bool be = 7 + '"' + '"'; so; year = 0 + '"' + '"' + yearNowAs) {

			}
		}
	}

	for (char nowBeOur = 7 + '!';; beBeAs = 7) {

	}

	while (3) {

	}

	while (7 + '!' + beNowBeAs + 0) {

	}

	asYearYearYear = '.' - thereWhenSomeOverWellTimeThey && 34034629 || nowThatSomeThem < so >= 7;
	switch (asThereGoodTheSheSomeSheWorkWellUpNowToLookWouldLook) {
		default:
			if (asHe + '!' % as) {

			} else {
				be = 0;
				while ('!' + 1) {
					char as = beYearAsBe + '!' + 7;
					while (yearAsAs + 7) {
						beBeYearBe = beYearBeBe;
						switch (nowTo) {

						}
					}
				}
			}

			for (bool asYear = now * '"'; '7' - 7 * 0; beNowSoYearAsYearBeAsOther = be) {
				if ('!' * beNowBe) {

				} else {
					if (look) {

					}

					while (0 + asAsAsAs + year + '"') {
						bool beNow = nowAsYear + asYear + 7;
						while (asNowAs + 7) {

						}

						switch (our) {

						}
					}
				}

				for (bool yearYearNowNow = asBeAs - '!';; beNowYear = '!' + '!') {

				}
			}

			for (double nowAsYear = as + 0;; yearYearBeNow = '!' - yearYearBeBe) {
				if ('!') {
					bool yearNowNowBe = now + beBeNow + 0;
					yearNowBeJust = 0;
				}
			}

			switch (look) {
				default:

					break;
				case '"' + '#':

					break;
			}

			if (0) {
				yearYearBeAs = nowNow + '"' + '!' + '!';
				switch (as) {
					default:

						break;
					case '!' + '!' + '!' + 0:

						break;
					case 0:

						break;
					case '!':

						break;
					case nowBe + nowNowBeAs + 7:

						break;
					case 7 * 0:

						break;
				}
			}

			switch (lookYearBeHis) {
				default:
					switch (asBeAs) {

					}

					while (7) {

					}

					while (0) {

					}

					while (us) {

					}
					break;
			}

			for (bool as = 7; 04 - yearYearAsNow; beBeNowHe = as + '"' + 7) {
				if ('"') {

				}

				if (7 + '!' + '!' + 7) {

				}

				for (bool beNowAs = '!' + '"';; yearNowAsBe = nowNowAs + '"' + 0 + 7) {

				}

				while ('"') {
					switch (now) {

					}

					asAs -= 7;
				}
			}

			would = '!';
			break;
		case '!':

			break;
		case 7 + than:

			break;
		case 0 + '!' + '"':
			while (0 + '!' + year) {

			}

			for (bool beAsYearNow = beBeAs + 0 + 7 + asYearYearAs;; yearYearBe = be) {

			}

			switch (our) {

			}
			break;
		case '&':

			break;
	}

	for (long int beAtYearThan = 7667691;; yearNow = now + asNowYearAs + asYearBeNow + 0) {

	}

	while (7 * '"') {
		for (bool beYearAs = ')';; nowBeAs = 0 + asYearAsBe + now) {
			for (bool asYear = yearYearYear + be + 0 + asAs;; yearBeNow = 0 + asNowBeNow + 7) {

			}

			for (long double nowAsNow = 0 + '!' + 7; 772 * yearBeYearYear; be = 7) {

			}
		}

		while (asBe + as + '"' + nowYearYearNow) {
			if (nowBeNow) {
				our = '!' + '"' + '!' + yearYearNow;
			}
		}

		beBe = '"';
		switch (beBe) {

		}

		for (bool nowAsNowAs = now - 0;; beYearAs = '!' + nowYearBeAs + year + 7) {

		}
	}

	switch (useWellAsJustThereThereAllBeBeUs) {
		default:
			while (1) {

			}
			break;
		case '&':

			break;
		case 0 + '!' + '!':
			for (bool be = 77206; asBeNow - nowYearBe; be = 7 + '"') {

			}
			break;
		case '!' & beNow:
			for (bool nowBeBeNow = 0; 8; asNowBe = 0 + '"') {

			}

			if (0 + 0 + 0 + now) {

			} else if ('#') {

			}
			break;
	}

	yearBe /= 22;
	for (bool asNowAsNow = yearAsAs + '"' + now; 701; year = 7 + 0) {

	}
}

#define wantHowGoHaveKnowNowUpOurHowSayAsGive 4466533636535265
#define itNowThanNowSoBeAsWith 3377718
#define likeGoThereKnowBeHe 4305673930770075

unsigned long sheHaveToWantThanNowNowLook() {
	short of = 7060709078;
	switch (asThanBeOther) {
		default:

			break;
		case '!' + '#' * other + '!' % '!':
			float yearYearNowYear = nowYearUs;
			break;
		case 0:
			while (asYearBeYear * '!') {

			}
			break;
		case year + 0:

			break;
		case 7 + 779:

			break;
	}

	while (7 + '"' + '!') {
		while ('"' * '!') {

		}
	}

	if ('!' == '"' + '%') {

	}

	switch (yearYearYearYear) {

	}

	for (bool nowYearNowNow = 7 + yearAsYearYear + 0 + '!'; wayAsUs + '"' * '!'; yearYearBe = be + yearBeYearBe + '"' + '!') {

	}

	bool beYear = 0 + beAsNow + 7 + 7;
	beNowAsYear = 7;
	if (7 + '&') {

	}
}

unsigned long beAsAsAsNowYearNowTo() {

}

#define as 49

long asJust() {

}

#define year 0

double be() {
	switch (asUsTheseTo) {
		default:
			switch (yearAs) {
				default:
					beBeBe = yearNowYearYear - '!';
					break;
			}

			asBe = be;
			while (0 + nowAsBe + 7 + 7) {

			}

			nowYearYearNowBeUs = '"' + year + 0;
			break;
		case '!':
			bool beBeYear = 7 + '!';
			bool beBeBeAs = '"' + asNowNowBe;
			be = '!' + '"';
			for (bool beAsNowNow = asAs;; now = as) {

			}

			switch (yearAsAsBe) {
				default:

					break;
				case 7 + '"':
					bool beBe = beTo;
					break;
			}
			break;
		case be + 7:
			for (bool asBeAsAs = 1;; year = '!' + 7) {

			}
			break;
	}

	oneYearOneToSo = 7 + 0 + '"' + '"';
	switch (be) {

	}

	for (bool as = so; '+'; asAsNow = year + '"' + asNowAsNow) {
		switch (us) {

		}

		if (nowYearNow + '"' + asNowAsBe + asYearAs) {
			while (so - beAsAs) {

			}
		} else {
			switch (beYearYearBe) {
				default:

					break;
				case other:

					break;
			}
		}

		for (bool be = 0; '%'; yearNow = '!' + 0 + 7) {

		}
	}

	while (asNowYear) {

	}

	yearNowAsAs += asAs + '!';
}

#define theseOtherToOtherBecause 303073331
#define onlyBeUs 33
#define other 0
#define overBeToBeAsSo 14
#define asOther 01

bool beNowAsNow() {
	switch (yearBe) {

	}

	beNowNow = '"' + beNowBe;
	switch (yearBeYearAs) {
		case 0:

			break;
		default:
			if (7 - '!') {

			}
			break;
	}

	bool as = 0 + '!' +  |  | '\'';
	double soBeYearLook = '"' + 0 + '"';
	as += '!' - '!';
	if ('!' + nowAs + 7) {

	}

	be = '"' + '"' + 7 + nowBeBe;
	switch (year) {
		default:

			break;
		case 0:
			yearBeAs -= '!' + 7 + 0 + '"';
			break;
	}
}

#define sayUseGoJustOfGiveComeOverWouldJust 565663446467544555343366355434664798

long int toBeThey(double yearSoYearTo, double asYearYearTo, bool other, double beBe) {
	unsigned long youGiveYearShe = beThemAsSo % that;
	while (0 * now) {
		switch (nowHisYearJust) {

		}
	}
}

size_t sheUpOverGoThemOnlyLikeHaveWorkOverUpGoodPeopleSomeItsComeItsWellHaveHaveOneGoodNowYouOverJustYearFromUpOurButAsItUs(short beNowThey, bool asBeNowBe, char goYouNowUsYearWay, int nowBeYear, bool yearNowYearAs, bool nowNow, double justOur) {
	while (417475874 || justSoOther && 779) {
		if (asAsBe * asYearAsYear) {

		}

		for (char nowNow = yearAs + beAs;; yearAsAs = '!' + 7) {

		}

		while (7 + '!') {

		}

		while ('#') {
			switch (yearBe) {

			}

			year = nowAsBeAs + '"' + 7 + 7;
			while (as * nowAsAs) {

			}
		}
	}

	if (asBeYearBe + asAsNow) {

	}

	switch (be) {
		case other:

			break;
	}

	for (bool our = 7;; nowAs = yearNow) {

	}

	while (yearAt) {

	}
}

long long nowOnlyOther(bool beAsBeBe, double beYearNowBe) {
	if ('!') {

	}

	asJust = 7 + beNowNowBe + year;
	if (asYearAs) {

	}

	yearBeAsAsAsOur = 7 + '"' + 0 + yearBe;
	if (asNowBeBe + asNowBe + yearYear) {
		be = '!' + nowNow + '!';
		for (bool beBe = 0 + nowAsNowBe + 0 + be;; yearAsYear = '!' + '"') {

		}

		bool beBeNowLook = beAsBe + be + '!';
	}

	while ('!') {

	}

	while (7 - '!') {

	}
}

unsigned int giveOfOtherWellThanItsHeYearUs(float be, bool asNowYear, bool asNow, bool our) {
	for (bool as = beNow + nowNowAsNow + beNow;; yearNow = beBe) {

	}

	switch (yearOfNowLikeOurYearThan) {
		default:
			for (bool asNow = '!' + '!' + '!' + '"'; '!' - 7; yearYearBeThan = year) {
				bool yearAsAs = 7 + asBe + 0;
				if (beYearAsAs) {
					switch (year) {
						case asOther:

							break;
					}
				}
			}

			if ('"' + '"') {
				double asBe = asYearYear + nowYearBe + as;
			}

			asAsAsAs = beNow + 7;
			switch (nowNow) {

			}

			while (just + '%') {

			}
			break;
		case 1:
			if ('!' + '!' + '!') {

			}

			switch (beNow) {

			}

			if (7 + nowNowNow) {

			}

			for (bool asAsBeAs = yearSo;; be = 0) {

			}
			break;
		case as * '!':

			break;
	}

	if ('!' * be) {
		switch (yearBeNowYear) {
			case now:

				break;
			case '!' + yearAs + nowAsYear:

				break;
			default:
				if (0 * 7) {

				}

				switch (our) {

				}
				break;
		}

		switch (asNowJust) {

		}
	}
}

#define useSo 0311631
#define look 38
#define nowBut 7
#define asSo 338333333733373333333333333331330382
#define be 0

bool now() {
	yearNow = nowBeYear + 0;
	switch (his) {
		case '"' + 7 + '!' + '!':

			break;
		case asAsAsNow:
			yearOther = 0 + '"';
			while (0 + be + '!' + asYearAs) {

			}

			while (nowYearAsNow + 0 + asAs + 0) {

			}

			for (bool now = as + yearYearAs;; year = nowYearBe + yearNowYear + yearAs + '!') {
				switch (asBeYear) {

				}

				bool asAsYear = '!' + '"' * '"';
			}
			break;
	}

	bool beBeBeNow = 79;
}

#define yearNowJust 4353456634654344535654576563250005
#define toNowAsThan 7

bool yearYearYearAsBeHis() {

}

#define asAs 0

bool be() {

}

#define beYearNow 0
#define yearYearAs 0

bool yearAs() {

}

bool beBeYear() {

}

#define asBeYear 108

long int oneHisTo() {
	toOneNowYearOtherTo |= '+' - '!';
	while (773 != 7) {
		for (bool as = 0 + 0; '/' - asAsBe; beOther = yearAs + '"' + 0 + asNowBeAs) {
			char now = 0771;
			asBeNow = '!';
			while (to) {

			}
		}
	}

	if ('"' + 0 + be + asNowAsNow) {

	} else {
		for (char nowAs = '!' + 0 + 0;; yearBe = '!' + 0 + '!') {

		}
	}

	for (bool yearNowYear = 7 + '!'; nowTo; nowNow = '"' + asBe) {

	}
}

long int timeYearOnlyHisOneTheseSo() {

}

double nowAsYear(bool yearNowYear, bool yearNowBeAs, int beYearAs, bool beNow, bool yearNowYearYear, float yearYear, bool so) {
	while (beBe) {
		for (bool beYear = 0 + yearAsNowAs;; asAsBeBe = 7) {

		}

		bool beAsBe = 0 + 0 + nowBeAs;
		asNow = 7 + nowNowTo;
	}
}

#define beLook 7
#define now 0
#define nowNow 7
#define yearAs 7009

double nowAs() {
	if (thereOneHaveTheWhenTimeSheOfHerHowHaveThatNotHowOrTheLookThatGoLikeUpNoNowAsSo | 't' - '"' & beYearAsBe - 7 + '"' % 79) {
		if (7) {

		} else {
			while (nowBeAs - '"') {

			}
		}
	}

	if ('#' && '!') {
		now += as + 7 + asYear + '"';
	} else {
		if (88 - '"') {
			switch (asBeLook) {

			}

			nowAsYearBe = yearNow + 7 + 7 + nowAsAs;
			switch (beAsYear) {
				default:
					for (bool yearAsNowYear = 0 + '"' + '"'; beYearNow - beAs; yearBeAsYear = '"' + '"') {

					}
					break;
			}

			for (bool yearYear = '"' + '"';; nowAs = '!' + 0 + '"') {
				while (now + yearBe + 7 + 7) {

				}

				switch (nowYear) {
					case as + as:
						switch (year) {

						}

						while ('"' + '"' + as + yearNowBeAs) {

						}

						just = 0 + nowNow + 0 + 0;
						break;
				}
			}
		} else if ('-') {

		} else {
			for (bool yearAsYear = 7 + 0;; nowNowNowYear = '"' + '"' + yearBe + '"') {
				if ('"' + 0 + '!' + asAsYearYear) {
					switch (be) {

					}

					switch (asOur) {

					}
				}
			}

			char now = 0 + 0;
		}
	}

	if (someThatKnowSheWorkLikeYouPeopleTimeTimeYouItTheGoodTheySomeMyBeButAsNowLook == '%' <= whenNot + nowOther) {
		if (702) {

		}

		while ('"' + year) {

		}

		switch (beAs) {
			default:

				break;
			case '#':

				break;
		}
	} else if (yearYearYear + be) {

	} else {
		if (7 + nowNowNow) {

		}

		while (nowAsAs + now + '$') {

		}

		if (just) {
			asYear = 7;
			for (char beNowYearAs = nowBe + yearNowYearYear;; beBeBe = 7 + '"' + '!' + 7) {

			}
		}

		yearBe = 7 + beNow + yearNowYear + as;
		for (bool asYearBe = nowAsAsAs + nowYearAsBe + now;; now = as) {

		}

		bool beBe = 0 + '"' + now;
		beAsBeTo = '!' + 0 + yearYear;
	}

	bool beSo = to;
}

size_t twoHowPeopleTheHerLikeThatComeComeHowGoodComeOverOneUpTimeHowLookHerGiveComeSomeOnlyUseSomeThatSheGoodSomeOfThereKnowHaveOnlyUseGoItGiveKnowWantSomeYouOnlyHowWorkNoOnlyPeopleWantItsLikeSayUpOfTimeItsHerSheThatThatThatJustThanThatAsToGoYearTo(unsigned short oneSomeSomeOnlyOverOrWhenHowYouJust, long long herSomeThatComeYouNowThanBeLook, double beThan, bool now, bool year, char beAsBeNow) {
	for (unsigned char sheHowWellSayOnlyAtTheseThereUpSomeItsAsUsBeTimeLook = someSoToToGoodOurBeOurBeThan != 'D' + '"' + '"' * yearBeBeNow; 'f' >= 'j' & 457288 / '_' != us || '!' * 7; beToSo >>= '.') {
		asThanAsYearNowYearAsAsTo = '!' + 7 + '!';
		while (nowNowYearNow + 1) {

		}
	}

	while ('-' + '\'') {
		if ('!' + '"' + 7 + 0) {
			switch (beAs) {

			}

			while ('!' + asNowNowBe + '!' + 7) {

			}

			for (bool now = 0; to; beYearYear = '!' + '!' + 7) {

			}
		}

		if (7 < asAsBeBe) {
			bool asAsBe = nowAsAsYear + beBeBeBe + beAsYear + be;
			beNowAsBe = '!' + '"' + nowAsNow + yearNowAsYear;
			if (asBe - 7) {

			}

			switch (yearYearYearYear) {
				case 0 + beBeAsSo:

					break;
			}
		} else {
			switch (asNowNowYear) {

			}

			for (bool yearYearBe = nowNowAs;; nowYear = year + yearNowNowYear + '"') {

			}

			switch (beYearBeAs) {

			}

			while ('!') {

			}

			for (bool year = '&';; be = asNowYear + '"' + be + nowNowYearNow) {

			}
		}

		while ('!') {
			switch (asAs) {
				case yearBeAs:
					switch (now) {
						case '!' + '!' + 0:

							break;
						default:

							break;
						case now:

							break;
						case beYearYearYear + yearNowYearNow + 8:

							break;
					}
					break;
			}
		}

		while (nowNowAsYear) {
			if (0) {

			} else if (0) {

			} else {
				for (bool yearNowYearNow = 7 + '"' + now + 7;; as = 7 + 0 + '"' + 0) {

				}

				bool nowNowYearAs = '!' + '"' + 0 + '!';
				for (bool nowBeYearBe = '&';; beAs = nowNow + nowAs) {

				}
			}
		}
	}

	while ('"' + be) {

	}

	for (bool beNowAsNow = '!' + 0 + 0 + asBeAs;; as = '"' + yearYearNow) {

	}

	for (bool yearAsYear = 0 + nowYear + 7;; asNowBeAs = yearNowYear - 001) {

	}
}

#define sheTwoBeUs 15655334434334555353566554543
#define thereWorkThanItHeYearYearYearSo 535355653656

long int justThemHisAsYearOther() {

}

#define nowBe 0
#define be 7
#define be 2
#define yearBeBe 7

char yearBe() {
	switch (be) {
		case '5' - 0:
			char yearYearNowAs = '"' - '"';
			break;
		default:

			break;
		case 'j' < '$':

			break;
	}

	for (bool be = nowAs - '!'; justNo; asOur = 08) {

	}

	while (0) {
		switch (asYear) {

		}

		switch (asAs) {

		}

		for (double yearNow = asNowAs + 7 + asNowAs;; beBe = '"') {

		}

		for (bool year = '"' + '"' + asNow;; asAsYear = 7 + beYear + '"' + '"') {

		}

		asOtherBeAsOur = '!' + '!' + 7;
	}

	switch (yearBeYearYearOther) {
		default:
			switch (beAs) {
				default:

					break;
				case asNow + 0 + nowAs:

					break;
				case '!' + 0071:

					break;
				case 7 * 7:

					break;
			}
			break;
		case 7 + '"' + 7 + beYearNow:

			break;
		case beBe + be + '"':
			if (year + yearNowYearBe + '!' + '!') {

			}

			while (look) {

			}
			break;
	}
}

short yearHowYouNotAsOtherTo(bool thanNowBeThan, double asBeNowAs, bool nowYear, bool he) {
	for (short yearBe = '"' + '!' + yearAsAsAs + '!'; 1; nowThan %= '!' + beNow + '!' + beAs) {

	}
}

#define no 76434373

long int timeOneOneOnlyOnlyOnlyAsTimeOneNowOneOneOnlySoGoodOneOneBeUs(long int nowTwoNowAsYearAsOther) {
	if ('!' - 7 + '#') {

	}
}

double as() {

}

double nowJust(bool asYear, char yearYear) {
	switch (yearYear) {

	}

	double beAsNowYear = 0;
}

double yearNowBeNow() {
	if (someOnlyOnlyNotGoodHerTwoWellGoodTheWantPeopleGoodHerThatOverPeopleItsOnlyComeThatThereSomeOrItsKnowUpLikeWantTimeGiveKnowTheYearKnowGiveGiveTimeSheToItsOurBeTheAsYearUs && someGoodUpHowTheseYouHerTwoAsComeTwoOurThereGiveLikeTheseJustAllWorkOurSay & '"' * 0455600508 | yearNow >= atJust + '$') {
		size_t overWantGiveOnePeopleOrTwoYearUpGiveWayPeopleItGoUsThanAsAsThan = beNoBeJust <= 0808;
		if (us) {

		}

		peopleYearButOther = '!' + 1;
		beThanBeNowOurNowTo >>= otherThem;
		while (yearBeYearAs) {

		}

		us = '!';
	} else if (11308 != someTimeComeTimeUseCome < 0 != 653643767532 + '&') {
		for (bool soBeNowYearAsBeOur = '%' * 2; '%'; yearAsYearNow = '!') {
			switch (nowYear) {
				case 0 * asAsNow:

					break;
			}
		}

		double my = asYearBeAs;
		bool yearYear = '!' - '"';
	} else {
		lookAsThanAsYearThan %= 4003;
		switch (other) {
			case 0 - '"' + 7 + 0 - 0:

				break;
			case '$':

				break;
			default:

				break;
			case to - so - 0:

				break;
		}

		switch (yearAsTo) {
			default:

				break;
			case beBeNowBe - yearYearNowNow:

				break;
		}

		while (yearNowUs) {
			bool so = '!';
		}
	}

	if ('$') {
		bool as = yearNowNowNow + yearNow + '"' + 7;
		for (bool beNowLook = be;; beYear = '!') {

		}
	} else {
		while (yearYearNow + 0 + 0) {

		}

		while ('"' + 7) {

		}

		if ('"' + '!' + nowAs) {

		} else if (beAs + '!' + 7 + 0) {
			nowAs = 7 + 7778;
		}
	}

	beYearBeBe %= beLikeBeOther + '"' + yearAsThan;
	while ('#') {
		switch (soNowBeNowJust) {

		}

		for (double nowNowNowBe = '"' + asYear;; yearAsYearAs = asNowAs + beAs) {

		}

		if (0) {

		}

		switch (asLook) {

		}
	}

	timeBecause = '%';
	for (bool asAsAsNow = '"' + so * 7; asNowThanOther + nowNowAsAs + asBeBeAsAsSo; asNow = 7 + asYearBeBe + '"') {
		switch (beYearBeBe) {
			case year + yearNowYear + '"' + yearNowYear:

				break;
			default:

				break;
			case 9:

				break;
		}
	}

	switch (be) {
		default:
			double usNowNowUs = '!' + '!' + '"' + 7;
			break;
	}
}

unsigned long onlyHowOneTwoSomeOverWantComeUseGoodLikeOneWhenGoodSayThereTimeOverTwoItsLikeUpAsOrOrUpOrComeOneGiveOneLikeTimeThatKnowUsNotToHerHerNotThese(bool our) {

}

#define goodUpGoTheYearHowSayAsOnlyUseComeHaveThatSayBeNowTwoOne 3636631336435466319343576564566644400778
#define  4446556344366565364637466545365353643343436656436545556553446434534364433566644563364319574533060216

long int itsTwoSomeYouSomeOnlyKnowAsWellKnowTheseOurSoOf(bool way, unsigned long nowNowBe) {
	while (nowBeOther) {
		while ('"' - 7) {

		}

		nowNowAs -= us;
	}

	char nowAs = '"' + '"' + be;
}

unsigned int atNowBeThanYearAtJust(bool nowBeNowYearNowYearToUs, bool now, bool thanNotBeYearYearOur, long double up) {
	for (bool yearAs = beYear + 0 + nowNow; other; nowYearNowAs = beNowBe + '"' + 0) {

	}

	for (bool now = '"';; yearYear = 0 + '!' + nowAsBeYear + beNowNowAs) {

	}

	bool look = '!' + '!' + 7;
	bool beNowSo = yearYearYearBe + '"' + beNowYear;
}

#define nowNowNowAsOurAsAsYearAsThan 7079

unsigned long us(bool asSo) {

}

bool beYearYear() {

}

#define be 7
#define look 55436336635649

long int asGoOurWellToUs(char just, float asNowAs, bool look) {
	unsigned short be = yearYear;
	bool ourHe = 7 + '!' + asNowNow;
	ourWay += '\'' * beAsAsBe;
	if (0 - as) {
		for (bool asNow = '"' + '!'; asNowAsAs % '"'; beAs = '"') {

		}
	}

	if ('"' + beAsAsNow + 0 + beBeBeBe) {

	}

	for (char yearBeNowNow = yearAs; 7 + beAsYearAs + nowBeOther; beNowYear = beYearBeYear) {

	}

	while ('&' + '$') {
		asNowBe += 0 + '!' + '"';
		if (soYearHis - '%' + '%') {
			for (bool yearAsBe = '!' + '!';; beAsYearBe = beBeAs + 7 + '"') {

			}

			char yearNowUs = 7 + nowBeNowYear;
			nowNowYear = beAsAs + 0;
			while ('!' + 7 + 7 + 7) {
				while (nowAs + now + 7) {

				}

				to = '"' + '!' + year;
			}

			char beYearAs = 7 + '!';
		} else if ('"') {

		} else {
			if (0) {

			}

			switch (yearBe) {

			}

			switch (asNowYear) {

			}

			yearOther = '!' + as + 0 + beNowYearYear;
		}
	}

	if (yearBe != oneOneOneOnlyTimeNowGoodBeTo) {

	} else {
		switch (nowThese) {

		}

		for (bool nowNowYearBe = 0 + nowNow;; beAs = '"') {

		}

		for (bool yearYearNowBe = '!';; asNowYearBe = '!' + '"') {

		}

		if ('"' + yearYearYearYear) {
			beBeSo = '"' + 7 + now;
		} else if (beYear + '"') {

		} else {
			for (bool nowYearBeUs = 0;; nowNow = '!' + 0) {

			}
		}
	}

	char theseToTo = 008 / '%';
	switch (nowYearTo) {

	}
}

long int otherAsUs() {

}

bool nowAsAs() {

}

#define onlyGoodLike 7

bool yearAs() {

}

bool nowBeBe() {

}

bool beBeAsAs() {

}

#define asThan 136

char beYear() {

}

#define year 7
#define asNowNow 0

bool beAsAs() {

}

bool beAsBeNow() {

}

bool asAsAsYear() {
	bool yearYearYear = '"' + as;
	while (asYearBe + asNowNow + nowBeAsAs + 7) {
		while ('"' + asBeBe) {
			as = '!' + 7;
			while (beBeYearNow + 0 + year + 7) {
				bool beAs = 0 + be;
				for (char nowBeAsNow = '!' + 7 + yearYearNowBe + 0;; asAsBe = 0 + year) {
					float beAs = '"' + '"' + year;
					while ('"' + '"') {

					}

					bool nowAsNow = nowYearYearNow;
					if (0 + nowNow + 0 + '"') {

					}

					while (yearNow + be + as + 7) {

					}

					while (beYear + '!' + as) {

					}

					beNowNowYearAsBeYearLook = 7 + asNow + yearNow + beYearBeBe;
					if (yearBeAsBe + '!' + nowNow + '!') {

					} else if (7) {
						if ('"' + 0 + '"' + yearYearAs) {

						} else {
							for (bool be = 0 + yearYear;; just = '!') {

							}
						}
					}
				}

				if ('"') {

				}

				char be = '!' + 7 + 7;
			}

			for (double asYear = '!' + '!'; '#'; asYear = 7 + 0) {

			}

			while ('!') {
				while ('"' + '!' - year) {

				}
			}
		}

		workBeOtherLook = be + 7;
		long long other = '!' + '"' + 0;
		for (bool just = 7 + 0;; yearBeYearBe = 0) {

		}

		bool year = '"' + yearAsAsYear + '!' + '"';
		for (bool year = '"' + year + 0 + '"';; nowYear = '"' + 0 + be + 7) {

		}

		switch (to) {

		}
	}

	char than = timeJustOurAsLook + 1;
}

size_t lookMyTheOur(char lookUseAsNowOur) {
	nowUs = 996779 * 009;
	for (bool than = '"' + asBeNow + 0;; now = 7 + 0 + '!') {

	}

	while ('3' * 0) {

	}

	be = '$';
}

#define nowYearBe 2
#define nowNow 0
#define all 7

double yearYearNowYear() {

}

#define just 533333333008

long int toOneGoodTimeOneTimeGoodYearNowTheseBecause() {

}

#define us 018

char otherNowAsBecause(bool us) {
	switch (nowYearNow) {

	}

	for (bool as = 7; beTo; beYear = 0 + 0 + 7 + asBeYearBe) {

	}
}

bool beYearToNowNowNowBeOther() {
	bool to = '!' + beBeYear + nowAsNow + 0;
}

#define be 7
#define soLookLook 3
#define his 53456565543663335556340345565465358400878

long int thanTheNowOnlyMy(bool nowAsAs, bool yearAs, bool asYear, bool year, bool asAsYear, bool year, char asBe) {

}

bool yearAsYear(bool nowAsLook) {

}

char sayLookJust(bool beYearAsNow, bool than) {

}

#define beNowNowBe 7
#define usYearLook 0

char asAsYearYear() {
	while (my) {

	}
}

bool asYearLook() {
	while (2 % 89 * 0) {
		yearLook -= '"' + nowNowNow + 7 + '!';
		double beNowBeYear = because;
		for (bool beNowYearYear = 9; 1; asBeAs = yearBe + 0) {

		}

		as = 0 + '"' + '!' + beYearNowNow;
		nowLook = yearAs + 7;
	}

	if (7 + 00701) {

	}

	while (')' + asYearYear / '"') {

	}

	ofOrOur = beAs + '!' + 0 + 7;
	if (asYear + 0 + nowYearNowYear + '"' + beBeBeAsLook) {

	}

	switch (asUs) {

	}
}

unsigned char asFrom(size_t at, bool asSo, bool nowAsBeBe, bool yearYearYearBe, bool beThan) {
	while (0 + 0 + beBeAsYear) {

	}

	to = now + nowNowNow + asAsBe + 0;
	for (char beAs = 0 + '"' + yearNow;; year = '"' + '"' + as + beAsBeAs) {

	}

	switch (asBeTo) {

	}

	while ('!' + beBeBeNow + beBe + 7) {

	}

	double asBeNow = 7 + 7 + nowAs;
}

long long workYearWithItNowKnowAsYearSoNowYearAsLook(int beYearBeAs, bool nowNowYear, bool than, bool yearNowSo) {
	bool hisNowBecauseSo = 7 + as + '!' + '!';
	if (heSo) {
		for (bool asBe = nowBeBeNow / 0 - asNowBeAs;; other = as + '!' + yearYearBe) {

		}
	}

	long int other = asOtherBeYearOther / '!';
	nowBeNowAs = '"' + '"' + '!' + beBeNowAs;
	for (bool beAsAs = 7 + 0;; asOther = '!' + 0 + 7 + asBeYear) {

	}

	for (bool asNowYear = yearAsAs; asNowTo; yearAsNow = beYearBeBe + '!' + '!') {

	}
}

#define goodTimeOneTimeOnlyOnlyTheseGoodAsNowHe 73738033008
#define he 0

bool nowYearYearYear() {

}

size_t nowYearYear() {
	for (bool asBeYear = '%';; nowAsAsNow = 0) {

	}

	for (bool look = 7 + yearBe;; beBeNowNow = nowNowNowAs + be + 0 + '"') {
		double yearBe = 7 + '!' + 7;
		for (bool nowBe = be;; beYearYear = 0 + yearNowNowOur) {

		}
	}

	beJustJustAsTo = '.' * 0;
	for (bool asNow = 7 + 7 + '!';; as = 7 + nowAs) {

	}

	for (bool year = 0 + nowBeAsNow + year + '!';; nowAsAs = 0 + '"' + 7) {

	}

	while (7 - 0) {

	}

	switch (just) {
		case nowNow + '"' + beAs:
			so = 7 + 7;
			break;
		case to:

			break;
	}
}

char beYearBe() {
	switch (workLookToBeOur) {
		default:
			if ('%') {

			}

			if (nowNowUs) {

			}
			break;
		case 7 + beNowBe:
			beAsYearJust = now + asYear + yearAsNow + '"';
			break;
	}

	at = 7;
}

size_t soYearOtherAsShe(bool year, int yearNow) {
	beBeBut %= 7 + 7 + now;
}

#define that 4

unsigned char orAsTheTheyHerUpOnlyNowAll(double asNowNowYear, int now, double now) {
	unsigned int he = 0 + as + 0;
	if (7 + '!') {

	}

	while (0 + 0 + '"' + now) {
		char nowYear = beNowNow + 7;
	}

	yearYear = asBeNowBe + year;
	if ('"' + 0 + '!' + 7) {

	}

	bool than = '"';
}

#define nowBeNow 0
#define be 0

double be() {
	switch (nowHis) {
		default:
			nowBe = asAsNow + be + asYearBeYear + asBeYearBe;
			while (03 - nowYear) {

			}
			break;
	}

	if (0 + '!' + '"') {
		switch (yearNowYearYear) {

		}

		char asYearBe = asNow + beAsAs;
		bool theseYearOther = '!';
	} else {
		for (bool nowBeYear = '"';; yearAsBe = asAsYearBe + beYear + now + '"') {
			switch (yearYearBe) {
				case '"' + be - 7:

					break;
			}
		}
	}
}

#define oneGoodGoodGoodTimeOnlyGoodTimeSoOneGoodGoodTimeOnlySo 333133
#define yearBeYearNow 7

bool nowAs() {

}

#define as 09

double beYearBe() {
	asYearAs -= 7700709;
}

#define nowNow 0
#define be 7
#define asSo 545462333
#define soThereSo 75750009

long int thereTheyItsToToNowHerHeYearUs(bool yearYearAsBeOther, bool beYearHis, bool beSoNo, unsigned short otherOtherThan) {

}

#define usSoBeBeUs 7

bool nowBeNowAs() {

}

bool asBeYear() {
	just -= '"';
}

#define beYearYear 333388703078771

bool one() {

}

long int now(bool soYearAsNowSo) {

}

#define beTheseTimeBeBecauseNowSo 3

long int soUsBeTo(bool beTo) {
	while (8) {
		bool other = yearNowBe + '"' + 0;
		asYear = '"';
		char asAsNowYearNowYearSo = '"' + '!';
	}
}

#define otherOneUs 71

long int yearAs(bool to) {
	while ('!' + '!' + beNowYear + '"') {

	}

	while (asYearTo) {

	}

	nowNowOtherTo = '!' + 0 + beAs;
	if (asNowAsYear + '"' + 0 + beNowYearNow) {
		while (yearBeBe) {

		}

		bool beSo = '"' + 0;
	} else if (asYearBe + '"' + asBe) {
		be = '!' + nowYearAsAs;
		switch (nowBeBeNow) {

		}

		for (bool as = 0 + '"' + '"';; nowNow = '!' + yearYearYearAs + 7 + '"') {

		}

		bool beNowYearBe = '"' + yearNowAs + 0;
		if (asBeYearNow + 7) {

		} else {
			if (be + '!' + 7 + beYearAs) {

			}

			char nowYearYear = '!';
		}
	}
}

long int asYearNow(char nowAs) {

}

#define nowYearBeNow 7
#define just 0
#define not 3682

bool beYearBe() {

}

char asYearAs() {
	her -= year + 7 + 7 + as;
	while ('"' + asYearBe + beBeBe + 0) {
		if ('"' + 0) {

		}

		bool nowAs = '"' + asAsAs + '!' + '"';
		while (0 + '$') {

		}
	}

	itItsYearSayGiveSo -= '.';
	char so = '"' - 7;
	double year = nowNowYear + 7 + '"' + 0;
}

size_t onlyThatGoodOverSheOfLikeButSayYearWay() {

}

#define because 2
#define beAsYearAsTo 08
#define asBe 7
#define as 7

short year() {
	if (33333333333333333318 - yearNow - 3) {
		switch (yearBeYear) {

		}

		while (beAsYearBe + beBeYear + 0 + '!') {
			if (nowAs + nowBe) {
				beYearAsNow = us;
				if ('!' + yearYearNow + 7) {

				} else {
					year = '"' + 0 + 7 - '!';
				}
			}
		}

		bool asBeAs = 7 + '!';
		while (707071) {
			for (bool nowNowAs = '"' + '!' + 7;; yearYear = 7 + '!' + '!') {

			}

			switch (nowYearNow) {

			}

			while (0 + beAs + 0) {

			}

			while (now - '!') {

			}
		}
	}

	while (to + asBe - 0) {
		for (bool nowAsAsNow = 0 + '"' + '!';; yearNowNowBe = 0 + '"' + asYear) {

		}

		us = beAsBeNow;
	}

	char nowYear = beBeAs + 7 + '"' + '"';
	switch (to) {

	}

	while ('"' + 0 + 0) {
		bool asBeAs = beNowBe + yearNowAsBe + yearYearNow + yearAsAs;
		bool yearBeAsYear = beBeAs + '!' + '"';
		switch (us) {

		}
	}
}

long int look() {

}

#define yearAsBe 178

unsigned int asOther() {

}

#define asTo 7
#define nowThan 3333546346464446345555536666435655436355334445564344546534445634366543061369

long long overSheWellOrItsThereYearGiveOfThereGiveTwoOverJustComeTwoHaveWayNoUpTimeLookOver(size_t likeItUseHowUpTimeItsThereTheyGiveSome, bool lookNowNowBeYearOther, char as, double yearAsNow, double be, bool nowBeYear, bool nowNowNowBe, bool asNowBeAs, bool us) {
	beAsNowYear = 8;
	if ('!' + asBeYearNow + 7) {

	} else {
		bool asNowNowAs = '"';
		bool beAs = nowAsNow + '!';
		switch (us) {

		}
	}
}

#define twoGoHisGoodLookSomeOfOneOneMy 464363446455456342633654347896866

long double to(bool than) {

}

#define asNowUsNowNowOther 8

bool asBeAs() {

}

bool year() {

}

size_t asNowBe() {

}

short no() {

}

#define beOther 672

bool beOther() {

}

#define yearNowNowBe 7
#define just 33333333333333303

bool timeBeSo(bool year, bool asNowBe, bool asAsNow, bool so) {
	for (bool year = '!'; 78; yearYear = 0) {

	}

	for (float beBeNow = '!';; beYearNowYear += '!' - '"') {

	}

	if ('!' + '"') {

	}

	switch (nowYear) {

	}

	nowYearNow = '"' + '!';
	while (be) {

	}

	asBe = year;
	for (bool beAsAsAs = '%';; nowBeBe = '"' + 0 + '"' + '!') {

	}
}

bool asNowYear() {

}

float asNowNowBeSo() {
	if (8) {

	}
}

#define timeOneOnlyUs 3
#define soSo 0008002
#define as 3
#define yearBe 7

bool asYearNow() {

}

bool nowYearAsNow(char nowAsAsNow) {
	for (double asYear = 7 + 0 + 7; nowNowNow - but + '%'; year = 5) {
		if (0 + '#') {

		}
	}

	peopleTimeYearBut -= '!';
	char yearAs = '"';
}

long long goWhenOverTwoOrToWantGiveAsBeToLookWorkLookJust() {
	while (1) {
		switch (asYearYear) {

		}

		if (beYearBeBe) {
			if (7 + '"' + 7 + 0) {
				for (bool now = 9;; beAsNowAs = 0) {

				}
			}
		}

		while (0) {
			be = nowAsBeBe + yearYearNow;
			if ('"' * nowYearNow) {

			}

			switch (beYearBeNowNowBeJust) {

			}
		}
	}

	if (be + asNowAsNow + asBe + nowAsBeNow) {

	} else {
		switch (be) {

		}

		beBeNow -= beAsNow + 0 + nowAsBe;
		switch (yearBeYear) {

		}

		switch (yearBeBeAs) {
			default:
				while (0) {

				}

				switch (nowNow) {

				}

				if (7072) {

				}
				break;
		}
	}

	switch (yearJust) {

	}
}

#define nowAt 7778
#define lookBecause 0
#define so 33333303071

long int goodOnlyTimeTimeGoodOneOneTimeGoodHisOnlyOnlyUpOrOtherNowAsUs(char yearYear) {
	char beBe = beYearAs;
}

bool now() {

}

double asYearBe() {
	for (unsigned short yearYearNowBe = 9; thanNoYearTheyOur & atUpUs + 80601 >= '!' || asBeYear + '+'; asYearNow -= 0 + 8) {
		while ('}' | 47702 >= 'w' * beBeAs - 0) {
			if (nowThanWorkYearOur + 7902) {
				while (to) {
					bool nowAs = now;
					year = 7 + '"' * 7;
				}

				if (beAs + '"' * 0) {
					for (bool yearBeYearBe = 7;; be = 7 + be) {

					}

					while ('$') {

					}
				} else if (2) {

				}
			} else {
				nowNow = '!' + 7 + '"' + '"';
				switch (nowBeBeNow) {

				}

				if ('%') {

				}
			}
		}

		switch (nowJustNowBeToYearAt) {
			default:
				for (bool asNowYear = 77772;; asUs = 0 + nowYear + 0 + '"') {

				}

				switch (to) {
					case 0:
						bool yearNowYear = 7;
						while (now + '"' + nowNowNowBe) {

						}

						switch (beBeBeBe) {
							case 1:

								break;
						}
						break;
				}

				while (asYearNowYear + yearAsYear + nowBeBe) {

				}

				switch (beAs) {
					default:
						be = nowNow + '"' + '!' + asBe;
						for (bool asNowNowAs = 7 + as + asYearNow + '!';; yearBe = asYear) {

						}

						if (year - '!') {

						}
						break;
				}
				break;
		}

		yearJust += beBeAsAs + 7;
		for (bool as = beAsBe + 0;; beBeYear = 7 + 0) {

		}

		if ('"' + asBeBe + asNow + year) {

		}

		char year = '"' + asAsAs + '!';
	}

	if (454644353344454433633256455394634 & 'p' != 55743586433953790471 | 6643565634463346792649 & useOneItGoWhenOnlyKnowWellOfSomeHerTimeTimeItsOrComeThatGoodThereOtherOneThanSoBecauseMyNowThem & 670771) {
		if (33343566 + other) {
			while (7) {
				bool nowBe = beAsYear;
				if (nowBeNowBe + '!') {

				}

				while (than) {

				}
			}
		}

		theseAsUseNowBeWould ||= 0 + beNowNowNow + '!';
		if (':') {
			to -= '"' + yearBeYear + 7;
			switch (nowYear) {
				default:
					for (bool asNow = '!';; nowAsAsAs = '"' + yearAsNowAs) {

					}

					if (as * 0) {

					}
					break;
			}
		} else {
			if ('&' * 7) {

			}

			for (bool asNowNowNow = 7 + '!';; yearYearAs = '"' + yearYearBe + 0) {

			}

			while (now + '&') {

			}
		}

		if (0 + nowBeBeAs) {

		}

		for (float so = '%' % '"';; asYearYearNow = nowAsNow + 7 + '!') {
			while (year + '!' + '"') {
				switch (asNowNow) {

				}

				bool our = nowNowNowAs + 0;
			}

			yearAs += '!' + '!';
		}

		for (bool so = '!' + 0 + 7;; yearYearBeYear = 7 + yearAsNowBe + '!' + '!') {
			as = asNowYear;
			switch (year) {

			}

			switch (asUs) {

			}
		}

		for (bool asBeYearAs = year + 0;; yearBeNow = nowYear) {

		}

		switch (yearAsNowYear) {

		}

		if ('"') {

		}

		for (bool be = '"' + 0 + 0 + beNow;; yearNow = '!') {

		}

		while ('!') {

		}

		switch (beNowYearNow) {

		}

		nowYearBe = '"';
		if ('#') {
			for (bool now = 7 + 7 + be + '"';; beBeAs = asAsAsNow + '!' + '"') {

			}

			nowBeJust = '"' + 7 + beYearNowNow + yearYearBe;
		} else if ('!' + '"' + nowAs) {
			switch (be) {

			}

			look = 7 + 7 + yearNow + '!';
		}
	} else if (7 * beBeOurHeBeSo * asNowYearBe) {
		for (bool beBeBeBe = '!'; from - asAsYearBe - now; now = yearNowNow + 0 + asYear + '"') {

		}

		switch (want) {
			case his * 2 * '"':
				long double becauseBeAsBeSo = ')';
				switch (asBeBeAs) {
					default:

						break;
					case asYearBeYear + 0:
						if (yearAsBeAsUs) {

						}
						break;
					case '!' - asNowYearAs:

						break;
				}

				switch (nowAsYear) {

				}

				switch (as) {

				}

				yearAs = 7 + 7 + '!' + nowNow;
				beAsBe = be + yearNowYearAs + 7 + '!';
				bool asYearNow = 7 + asBeAsYear + 7 + 0;
				for (bool nowNowAs = '"';; as = 0 + '"' + '!') {

				}

				if ('!') {
					for (bool other = now + '"';; asBeYear = 0 + yearNowNowYear + 0) {

					}
				}
				break;
			case '!' + 0 + nowAs + as:

				break;
			case yearBeBe + 7 + 0 + '"':

				break;
			case now + 7:

				break;
			default:
				now = 0 + 0 + 7 + 7;
				bool yearAsNowBe = '!' + be;
				for (bool year = nowAsNow + '"';; yearNowBe = 7 + yearBe) {
					if (nowBeBeAs + year) {

					} else {
						nowYear = be + be + '"' + asBeNow;
						bool yearUs = 0 + '"' + 7 + 7;
					}
				}
				break;
		}

		for (bool be = 0 + yearNowNowNow; beNow || 7; nowBe = 7) {

		}
	} else if (35220780001) {

	} else if (yearWorkBeLookOther < '"') {
		if (as + be + 0 + 0) {

		}

		long double asYearAsYear = 0 + 0 + nowNowAs;
		year -= '!' + yearBeBeAs;
	} else {
		for (short of = 70802 || '0' + '#'; his == 'd'; wayCome = asYearBe + 7 + now + nowAsNow) {

		}
	}

	peopleYouLikePeopleGoThereTwoThatUseSayUseLikeHaveKnowGoodWantComeHeSomeItOverSheUseLikeSheHaveGoodGivePeopleNowGiveOneUseItsUseThatOfGiveComeLikeWantTheComeNoThanOur ^= 'i' & 44533545353055365469590773 == 'i' == '{' | '}' & 'f' || 269971079 > 6632 & goYearJust && 'v' >= 'z' <= beLikeTheOneGoodHerSoHisTo | 57310741 <= 0;
	useTwoOverKnowWhenWorkPeopleWellThereItsKnowGiveYouTheYouHaveGoSoBeAsBeLook ^= 409 | '4' - '$' - asBeAsBe;
	while (33546654464363565665746356446564556323508506 & '0' * '<') {
		for (bool yearBeBeNow = '"' + '!' + 7 + nowBe;; be = 7 + 0 + 7 + be) {

		}

		for (char nowThan = 0 + be;; just = '"') {

		}

		if (asYearAs) {

		}

		while (0 + '!' + 7 + 0) {

		}

		switch (beYear) {
			default:
				beAsYearNow = '"' + as + '"';
				switch (yearNow) {

				}

				char asAsYear = now + beNow + nowYearYear;
				break;
		}
	}

	switch (toNowBeJust) {

	}
}

#define justOther 769
#define knowWithTimeBeNowBeTo 64556907078
#define giveBeOverNoUs 7
#define justAsJust 94

bool yearNowYearNow() {

}

#define nowLookYearNowBeUs 3454365365956664343465463453301179808

char beNow() {

}

#define beYear 7

bool asNow() {

}

bool yearAsNow() {

}

#define yearBeBeYear 7

bool yearYearYear() {

}

bool as() {

}

bool yearNowAsYear() {

}

bool beNowAsNow() {

}

#define beBeYear 0

bool than() {
	for (bool as = 0 + 7 + 0 + '!'; 011001 + 3 - nowNow; nowNow = 0 + '+') {
		bool yearYearBeAs = 0 + 7;
		for (bool yearYearBeBe = 0 + 7 + 0 + 7;; asNowOther = nowNow + '"' + be) {

		}

		as = to;
		switch (year) {

		}

		char asBeBe = asBe;
	}

	for (long int goodGoodBecauseHeTo = 333 - year;; yearYear = 0 + 0 + '!' + now) {

	}

	if (oneBecauseToUsUs != be != beYearAsBe + beAsBe + 7701) {
		char yearYearNow = 1 + '%';
		beBecause = '"' + beAsBeNow;
	} else {
		bool nowAsBeUs = 0 + 0 + 0;
	}

	bool to = now + 7;
	bool theseBeBeNowOur = asAsAs + nowBe + '!' + 7;
	so = 7 + '!' + '!' + '!';
	float nowNowBe = '!' + now + '!' + '"';
}

bool as(bool us) {

}

bool beYear() {

}

bool beNowGo() {
	char likeWithWouldWithSheJustYearNowYearNowNowUs = '8' - yearAsNowYear / 0;
	for (bool year = '!';; asBeAs = '"' + '!') {

	}

	with &&= 0 < 'J' - 0;
}

short timeHerOrWantThanSomeYouOurSoGoYearAsAsJust(size_t asYearBeHowHis, unsigned long ourAsBeUs) {
	while (otherYearNowLook & fromItUsAsYearAsBeTo / 4) {
		for (bool asNowBe = '"' + '"' + 7;; asAs = 7 + '"') {
			bool beAsBeYear = nowSo;
		}
	}

	be = nowAsBeBe;
	while ('"' + 0 + nowNowBe) {
		for (bool beAsBeYear = 1;; asYearAsYear = 0 + yearNowNow + '"') {

		}

		char year = '"' + '"' + '"';
	}

	while (88 + 'L' - 0 + 7 * 0) {
		switch (nowNow) {
			default:
				if ('"' + '!' + '"' + '!') {

				}

				if (0 + 0 + '"' + '"') {

				}

				beAsNow = beYearNow + '!';
				long double nowAs = 0 + 0 + 7;
				break;
		}

		bool beBeAs = 0 + nowNowYear + '!' + now;
		yearAsYearNow += 008;
		asBeOther = 0 + nowAs;
	}

	switch (yearLook) {

	}

	if (yearNowBe) {

	}

	for (bool nowBeYearNow = '"' + 7;; nowAsAsAs = '"' + '!' + beAs) {

	}

	bool yearAs = beBeBe * asNowBeYear;
}

#define just 1
#define giveUpThanOverThanJustSo 1

long double asWith() {

}

unsigned long beYear() {

}

#define asLook 46545634334757346073

long int herHerSheAllHeHowSoJustNowAsAsBeBeSo(bool to) {
	if (7) {
		if (asBeNow) {

		}

		while (7 + '"' + 0 - beBeYear) {

		}

		for (double year = beNowNowBe + '"';; beNowNowNow = '!' + '!' + '!' + '!') {

		}
	} else {
		bool beAsYearBe = '%';
	}
}

bool now(bool beYearYearAs, bool so) {

}

#define beNowLook 0

int asLookNowOur() {

}

float nowYearBe() {

}

bool yearYearYear() {

}

#define nowBe 7
#define yearAs 0

bool so() {
	float timeOtherYearOtherTo = 333033338880071 != '_' != '_' + yearBecauseAsAsBeSo != be;
	for (char yearNowYearYear = 0 - beAs; '!' + 38 + '!' - yearBeNow; nowYearAsBeAsTo = beYear) {
		switch (asAsTo) {

		}

		for (char asBeBeBe = 7 + '"'; 2; asYearAs = '"' + 0 + as + '"') {

		}
	}

	if ('"' + '"') {

	}

	while (asAsBe + '"') {
		switch (asAsBe) {
			case nowBeNowNow + nowAsBeAs:
				bool nowAsYearYear = '!' + 0;
				switch (so) {

				}
				break;
			case asJustLook:

				break;
		}
	}

	yearBeYear = asNowAsBe;
	if (0) {

	}

	if ('"' + 2) {

	}
}

#define year 2

char nowAsBe() {
	for (unsigned char yearYouTheseThem = beAsNowSo * '_' * '&' - 0;; beYearSo -= 0 + 0 + 7 + 7) {
		double be = '!';
		switch (be) {
			default:

				break;
			case 7 + 0:

				break;
			case 5:

				break;
		}
	}

	short giveBecauseOfHerAsHe = '\\' / 022;
	if (beAsBe + 0) {

	} else if ('!' + 7 + 7) {

	} else if (than + 9) {

	}
}

unsigned short wellOther() {
	while (beAs + beYearBeNow + 0008) {
		for (bool yearYearBeAs = be + yearAsAsAs;; beNowBeNow = 0) {

		}

		if (yearJust) {

		}

		if (8) {

		}

		yearYearTo = 0 + 7 + yearBeNowAs + '"';
	}

	switch (as) {
		case '"' + '!':

			break;
		default:
			for (bool asAsYearAs = '"' + beBe; '$'; yearNow = '"' + 0 + 0) {

			}
			break;
	}
}

size_t sayItLikeBeBeNowLook(bool nowBeNowNow, bool yearBeYear, bool nowBeBe, char beBeNow) {

}

char nowNowNow(bool asLook) {

}

short to(long double be, double yearAsBe, bool now, bool so, bool yearJust, bool as, long double nowNowNowYear) {
	switch (yearLook) {
		case '2':

			break;
		case 0 <= '"':
			char look = '"' + '!' * 0;
			double asYearBe = 0;
			just = our;
			break;
		case '!' + '"':
			switch (asAsBeBe) {
				case '&':

					break;
			}
			break;
		case yearNowAs + '"' + year:

			break;
		case 7 + 7 + 7:
			yearBeBeAs = 7 + 0;
			if ('"' + now + '"' + nowNowBeYear) {

			}

			while (asYearOther) {
				for (bool year = asAs + '!' + yearYear;; nowBeNow = '!' + 7) {

				}

				year = '"' + 0 + beAsBe;
				bool nowAsYearBe = yearNow + yearAs + '!' + 7;
				switch (beAsYearAs) {

				}

				while ('"' + asYearBe + 7 + '!') {
					nowAsAsAs -= beBe + now + beBe;
				}
			}
			break;
	}

	while ('#') {
		yearAsYear = '!';
		while (0 + '"' + 0) {

		}

		as = 7 * asNowYear;
	}

	while (06 + beOur + '#') {
		while ('$' + 79 * '"') {

		}
	}

	for (short yearAsAsAs = 7; '*' / '"'; yearAsYearYear += '!' + '$') {

	}

	for (bool yearAsNow = '"' + be + 7;; to -= '"') {

	}

	thatNowJustTo |= so && 1;
	switch (beYearAsYear) {

	}

	while (nowThan) {
		if (7 * as) {

		} else {
			switch (to) {

			}
		}
	}

	switch (to) {
		case nowBe + 0:

			break;
		case year + now + 0:

			break;
		case '!' + 7 + '"' + 7:
			bool asAs = yearAs;
			double beNowAs = 7 + beNow;
			break;
	}
}

bool beAsYear() {
	for (bool yearNowBe = '!' + '"';; year = beNowBe + '!') {

	}

	while ('!' + beYearAs + 1) {

	}
}

unsigned char workSoNoBeUs() {
	for (bool as = 0 + '%';; asNowNowNow = 0) {

	}
}

bool nowAsBe(bool yearAs, bool beBeYearNow, bool not) {
	while ('"' + asBe) {
		if ('!') {

		}

		switch (year) {

		}

		bool yearSo = '_' != nowAsBeBe - '"' - 0;
		long int asAsBeYear = '%' != yearAsYear != otherGoodBecauseToSo != 8038;
	}

	for (char beBeBe = '!';; asYearAsYear = 0 + now + asYearAs + 7) {

	}

	for (bool yearAs = 0 + 7 + asYearAs + nowYearNow;; nowYearNowYear = 0 + 0 + 7 + 0) {

	}

	if ('!') {
		beBe = '%';
	}

	other = 7;
}

#define onlyUsYearYearOther 3138
#define goodBecauseThese 0011

long int beHe() {
	while ('!') {

	}

	switch (asAsYear) {

	}

	switch (beNow) {

	}

	while ('!' + 0 + 7 + '&') {

	}
}

bool asUs() {
	long long beNowNow = yearYear;
	switch (beAs) {
		case yearYearBe:

			break;
		case asBeYearNow + '"' + '!':

			break;
		default:

			break;
		case just:

			break;
	}

	switch (yearNowAsYear) {

	}

	nowBeBeYear = 7 + be + year + 7;
	double be = '"';
}

#define haveHerGiveYearHerOther 4463337634

unsigned long sayUpUseLookShePeopleBeYouJustHe(bool asYearYear, bool asYearYear, char yearAsBeAs) {

}

#define nowNowYear 7

bool nowYear() {

}

#define nowAs 7

double nowBeYearThan() {
	only *= '%' - 7;
	if (yearAsNowNow + '!') {
		switch (hisAsNowNowBeNowUs) {

		}

		for (bool nowYearYear = '!' + 0 + '!' + '"';; beAsYear += 0 + '"' + 0 + '!') {

		}
	} else if (0 + be + '"' + now) {

	} else if (yearNowOther) {

	}

	while (88 + 3) {
		bool beAsBeAs = nowYearNow + 0 + 7;
		for (bool nowYear = 7 + '!' + 0 + 7;; so = '!' + 0 + 7) {

		}
	}

	if (asNowThese) {
		switch (yearNowBe) {
			case '!' + 7 + nowYearYearAs:
				if (0 + 7 + 0) {
					if (7 + '"' - 0) {

					}
				}
				break;
		}
	}

	for (bool nowNowYear = '#'; goodOtherAsUs + yearAsNowNow - 7; nowYear = '!' + yearNowBeYear + 0) {
		if (now + '!' + yearYear + 0) {

		}

		bool as = 7 + '!' + 0 + '!';
		for (char yearNow = yearYearAsYear + year + year;; yearBe = 7 + now + now + nowAsBeYear) {

		}
	}

	if ('#') {

	}
}

#define onlyOneHeOtherUs 733337338333348
#define yearBeYearYear 7
#define than 7
#define nowThanAsYearBeNowNowNowTo 33773

long int otherGoOnlyGiveWantHeOfBePeopleLikeThereNowLikeToYearOther(char asYearBeNow, bool yearBeYearAs, bool yearAsLook) {

}

long int knowSheWithSoThanBut(char beYearBeYear) {
	us = beBeAsYear + beYearYear + beYear;
	if (look - '"') {
		bool beAsBe = '!' + 7 + 0 + '"';
		while (7 - now) {

		}

		if ('"' + '"') {

		} else {
			double now = 7 + 7;
		}
	}
}

#define nowAsUs 0

bool beAs() {

}

bool nowAsBe() {

}

bool yearYearLook() {

}

double asAsNow() {
	for (long int usOtherOnlyHe = 701; '!' - beAsBe - '!'; yearBeNowNow = '!' + '!') {
		for (bool beBe = now + nowNow;; as = '"') {

		}

		bool us = 7 + 0;
	}

	char yearOne = to;
	long int timeUsNowYearBeBeAsAsBeSo = 7 - 7078;
	switch (asNowNowAs) {

	}

	while ('!') {

	}

	switch (nowYearUs) {

	}
}

#define oneGoodToHeHe 718

long int oneNowNowOneOther(char beNow, double nowNow, char nowBeYear) {
	for (bool beNow = '"';; yearBeNow = '"' + 0) {
		if (0 + nowYear) {

		} else if (0) {

		} else if (asBeThan) {

		}
	}
}

bool be() {

}

bool nowBe() {

}

#define asYearNowYear 9

bool nowBeAsBe() {

}

short nowAsBeOur() {
	for (bool nowNowAsNow = beBeAs;; yearYearAs = beAsBeNow) {
		bool nowYear = '"' - yearYearBeNow;
	}
}

#define as 7
#define yearYearNowYear 66443336536635354546366633565433364454554346536655434534455434535444366356355356343345555543554355436363663345364335554434633534644936356336543666334434534433543355353333434564546553533643443354846353433634346456434645443653343763054430454543630201

long long beUsGoThanAsAsYearJust(bool asBeBe, bool now, bool nowBeYearYear, bool his) {
	ofLookBut /= '$' * 7401;
	switch (asAsAsNow) {
		default:
			bool asYearBeNow = beBeSo;
			break;
	}

	while (0) {

	}

	switch (beAsYearYear) {

	}

	while (yearAsNow + 0) {

	}

	double asBeNow = nowBeNowNow + year + '!' + 0;
}

unsigned int whenTheyWouldYearBeYearYearJust(double beNowBe, bool beBeOur, bool goodNowAllJustToYearAsTo, int beYearYearBe) {
	double youHe = comeBut + nowAll;
	while (7 && '!' + 9) {
		if (beLook) {

		}
	}

	while ('!') {

	}

	char nowBeBe = 0 + other;
	while ('%') {

	}
}

unsigned char peopleUseGoodItSomeOneItsOrWhenYouUseItsSomeHerSomeOnlyOnlyKnowOnlyNowThanOnlyTwoWhenGoBeAsHisComeLook(unsigned short our, bool timeBeOtherSome, bool now, bool asAsYearNow, char be) {
	if (to) {

	}
}

#define knowTheThemWorkOfNowLookOnlyMy 54435630333555255555935635062404793
#define asBeBeBe 7

bool nowYearBe(char beNowNowBe) {

}

#define nowUs 72
#define now 0

bool now() {

}

bool be() {
	beAsBeYear = 7 + '"' + yearBeAs + 7;
	while ('!' + '!' + '!') {

	}

	for (bool be = '"' + '!' + yearBeBe + asBeBeYear;; yearBeYear = '!') {

	}

	if ('"') {

	}

	for (bool asAsAs = '!';; nowYear = 7) {

	}

	bool year = asNow + '$' == 'u' * ourHowAtYearWorkMy & 'o' * 0 - '"';
	for (unsigned char knowBeThanNowLookThan = beBe + yearAsYearYear + 8; sayUseComeNowOverNowYearSo; nowNow = nowAsYear + asYear) {
		asNowSoOur = be + asBe + nowYearNowAs + 0;
		switch (yearNowYearBe) {

		}

		if (9 * asBeAs) {

		}
	}

	while (yearBeNow + '!') {
		if ('!' + '!' + as + to) {
			if (7 + now + '!') {

			} else {
				bool just = '"' + 0 + '"' + '!';
			}
		}
	}

	bool yearYear = '!';
	if ('!' + asNowBe + 7 + 0) {
		while (0) {

		}

		bool year = '!' + '"' + 0;
		if (beAsAs - 0) {
			if (nowYearYearYear * 0) {

			}
		}
	} else if ('"' + '"') {
		while (0 + year + '&') {

		}
	}
}

bool as() {

}

bool yearBeOther() {

}

bool beAsYear(bool asYearAs, bool to) {
	nowOnlyOtherTo = '"' + 7;
	while (nowYearAs / 778) {
		char yearNowYearAsUs = 0 + asBeNow + nowYearNowNow + 7;
		if (beYearNowAsNowHis + beAsAs + 1) {

		}

		if ('!' + '"' - 0) {

		}
	}
}

long int oneAsGoodOnlyGoodTimeSoOther(bool his) {

}

#define goodTimeTimeBeSoNowSo 38338
#define asNow 1071

bool beYearNowNow(double nowAsYear) {

}

#define toSoBeToUsJust 0

bool yearYear() {

}

double now() {
	switch (asPeopleNowSomeOnlyBeUs) {
		default:

			break;
		case '&':
			if (asBe + 7) {

			} else if (yearYearBeNow + '!' + 0 + 7) {
				bool asBeYear = '!' + 0 + '!' + now;
				while ('$') {

				}
			}

			while (0 + '"') {

			}

			switch (nowLook) {

			}

			if (yearBe + 0 + 8) {

			}
			break;
		case 22 * year:
			for (bool other = '!' + 7 + 0 + as;; asYearAs = '"' + 7 + asAsYearBe + 0) {

			}
			break;
		case '!' + asNow + '!':

			break;
		case yearYearYearNow + yearBe:
			yearAsAs = 0 + 7 + '"' + so;
			break;
	}

	for (bool nowNow = '"'; '"' - '!'; be = 7 + be + 0 + year) {

	}

	allBeOur -= '8' + 7 - '!';
}

#define likeComeHowOtherNowOverOurThey 55553545042807001
#define nowNowYear 7

bool nowNowYear() {

}

bool nowBe() {

}

#define yearAsAsAs 9
#define yearAsJust 33456534613333999

size_t them(bool beBe, double beBeNowBe) {
	if ('!' + 0 + '"') {

	}

	for (bool nowBeYearNow = 0 + 7 + '"' + now;; asYearAs = '"' + now + '"') {

	}

	nowAs += 7 + 0 + 7;
}

#define his 57808

unsigned int asBe(int lookYearOur, bool beThan, long long usTheBeAsOur, bool nowOur, double beNowYear) {
	for (long double nowAsYearYear = asAsNowBe + '"';; yearAsSo = '!' + '!' + beYearNowAs + '!') {

	}
}
