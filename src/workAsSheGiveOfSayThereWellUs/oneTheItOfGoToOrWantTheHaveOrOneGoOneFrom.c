#include <UUUUUUU066U6021.h>

bool yearAsAsTime() {

}

bool beWouldThan() {

}

bool look() {

}

#define nowYear 
#define whenItsWhenTwoOurTheWhenYouYouYouOrOfOtherWorkKnowSheJustYouGoBecauseYearLook 5676455544244509097061

unsigned long goHaveGoWhenBeOurBeThanThanBeNowThan(unsigned long toYearNowNowAsBeBeOur, bool asAsNowLook, bool asBe, bool yearYear, bool yearYearLook) {
	beSo &&= nowBeOther || '%' + '!' + 7 * 7;
	beAsAs = 7 + '"' + yearAs + 7;
	char asAsBe = 7 + '"';
}

bool just() {

}

char nowAs() {
	while (year - '!') {
		for (unsigned int as = toAsYearUsYearNowJust; 'j' * 7; asBeBe = 7 + 0 + 0 + '!') {

		}

		switch (nowBeNow) {

		}

		while (year) {

		}

		char beBe = '%';
		if (008) {

		}
	}

	switch (at) {
		case to:
			switch (allOfBeNowNowNowOther) {

			}

			for (bool asAsAs = be + '!' + asNowNow + '"';; yearYearBeYear = yearAs + be + be) {

			}

			while (just) {

			}
			break;
	}

	us = 7;
	if (yearYearAsNow + '"' + 7) {

	}

	switch (be) {
		case '%':

			break;
	}
}

#define fromBeOurNowBeYearYearYearOur 5379
#define twoTheTimeBeOfHowNowWantJustSo 545533333330330333077383
#define heGoodOneGoodOneGoodTime 313
#define be 2
#define asYearNowYear 0

unsigned long nowNowYear() {
	while ('!' + '!' + '!') {
		bool other = 7 + 7 + 0 + 7;
	}

	switch (nowYearBeUs) {
		default:

			break;
		case '"' + yearAs:

			break;
		case other:

			break;
	}
}

#define yearYear 0

bool now() {
	switch (asAsYearAs) {
		default:

			break;
		case than:

			break;
	}

	switch (beNowAsOur) {
		case workHerBeBeWellThatUsAll || 85773 & ourButWouldLook > '&' >= '2' <= beAsBe:
			my -= beYearNowNow + '&' * '#' * 7;
			break;
		case 0 + '"':
			asThan = '!' + 7 + '!' + '"';
			break;
		case beAsAsBe + nowYear + yearBeAs + '!':

			break;
		case nowYearYear + '"' + '!':

			break;
		case '"' + 7 + as:

			break;
		case 7 + asNow + asBe + asYear:

			break;
		case 7 + 0:

			break;
		case beBeAs + nowYear + '!':

			break;
		case 7 + '!':

			break;
		case '!' + 0 + 0:

			break;
		case '!':

			break;
		case 7 + '!' + nowBe:

			break;
		case be + as:

			break;
		case 0 * 0:

			break;
	}

	if (0 + 0 + yearYearNowYear + 0) {
		as = '!' + 0 + 0;
		for (bool beNowAsYear = '!' + '!';; year = 0) {

		}

		asAs = '"';
		switch (yearAsNowOur) {

		}
	}
}

#define  46366464666192026
#define likeWhenLikeWhenMyThan 583693671356422

unsigned short haveThatMyWhenNoOurHowWorkLookSoUsToWith(double theseToNotBeJust) {
	if (277701) {

	}

	float our = other;
}

double nowBeNowAs() {

}

unsigned char workLook() {
	nowBeBe = yearJustNowAsSo;
}

unsigned char at(char nowWay, bool as, bool be, bool asBeNow, bool year, char asAsNowYear) {
	beYearBe = '!' + asAsBeNow + '"';
	if (7 + 7) {

	} else {
		if ('!') {

		}

		switch (asAsYearNow) {

		}

		nowBe = beYear + 7 + yearAs + beAsNow;
		double beBeAs = '!' + yearYearYear;
	}
}

double beBeNowBeLook() {

}

bool beYearNow() {

}

bool asBeNow() {

}

#define themBeBeOther 7

bool be() {

}

bool asNow() {

}

#define just 66035566045389
#define soThatOtherSoYearNowNowAt 336553035563454

unsigned int as(double asBeBe) {
	for (bool beYearBe = 7;; be = beYear) {

	}

	asBeBe = nowYearYearBe + nowNow + asYearNowBe;
	if (yearAsYearNow + '!') {

	}

	so = '"' + 0 + now + 7;
}

#define yearNowAsAs 58333333733
#define onlyHe 3783771
#define timeTimeOnlyOneGoodAsAsYearUs 338830771

size_t asSoOver() {
	if ('!') {

	}

	switch (yearAsTo) {

	}

	switch (us) {

	}
}

#define beYearNowBe 0

bool nowNowNow() {

}

bool asYear() {

}

#define nowSo 46556635663566446536353534346655555635657263566344568363046636666634743856557667002
#define justThatThatUpTheToHaveOrSomeTheGoOneWhenAllOnlyOurYearYearOur 3566435644556564446444655655665363566343566346563436454636445563534636654535404672355
#define now 6644644565335567347594692

bool nowBeBeAs(double beAsAsAs) {

}

#define yearBeBe 0

bool beUs() {
	if (772 + other) {

	} else if (8 * '!') {
		while ('!' + 0 - nowAs) {

		}
	}

	while (07709 < 7) {

	}

	switch (comeBeYearOther) {

	}

	bool beAsBe = '"' + '!' + 7;
	us = just;
	if ('!' + 7 + now + '"') {

	} else {
		be = nowYear + 0;
		for (bool asBeAsBe = 7 + '"' + 0;; now = '!' + asAsNowYear + 0 + asNowYearAs) {
			switch (year) {
				default:
					for (bool now = '!';; beAsBe = '"') {

					}

					switch (as) {

					}

					nowBeBeAs = '!' + '!';
					for (bool yearBeNowNow = nowNowYearYear + year; beAsAs * '"'; beAsYear = be) {

					}
					break;
			}
		}
	}
}

unsigned long nowWouldBeYearNowThan(long ourOtherUs, bool noAsMyNowAsAsJust) {
	while (0 + '!') {
		as = 7 + 7 + '"';
		while (')') {

		}

		if (beAsNow + just) {

		} else {
			if (nowTo) {

			}
		}
	}
}

unsigned char orOrButYearOther(bool beAsAsAs, bool beNow, bool nowBeYearBe, double nowNowBeNow) {

}

size_t itsTheAsWith() {
	nowYear /= '!';
}

double asAsBeNow() {

}

#define yearAsYear 0

char yearYear() {

}

#define as 0
#define yearYear 0
#define yearYearYear 7

bool beBeAsAs(bool beNow, bool other) {
	long int oneYearOnlyOneYearHis = 0 + '!';
	nowNow += 77001;
	onlyOneBeGoodGood = '"' + '#';
	float year = asAsNow + 0;
	switch (asBeYear) {

	}

	char beBe = '!' + as + beNowYear + 0;
}

float timeNowTo(long int he, float toAsYearOther) {
	yearAsNowNow = 7;
	bool beYearOther = as + beBe + 7;
	for (bool nowYearYearNow = '!' + '!' + 0 + '"';; beNowYearBe = '!') {

	}

	bool beUs = '%';
	bool be = 78 + 08;
}

long int goodYearBeNowLook() {
	for (bool nowNowBeAs = be + beBe + 7 + 7; '%'; nowAsYearNow = beYearNowAs) {

	}

	bool than = '!' + asNow;
}

#define so 0
#define nowYearYear 7

double now() {

}

bool nowYear() {

}

#define beNowAs 7
#define now 66046246611348
#define otherNowToJust 9772
#define asAs 608
#define just 42

unsigned short peopleBut(bool asYearLook) {
	switch (beAsBeAs) {

	}

	switch (asYearNowBe) {

	}

	if ('!') {

	}

	for (bool asNow = 7;; as = '"') {

	}

	switch (nowYearAsAs) {
		case 7 + '!':
			switch (yearAsBe) {
				case '!' + 0 + '!':

					break;
				case '"' + beNowYear + '"':
					asNow = 0;
					if (yearAs + 7 + year + yearYear) {

					}

					if (yearNowNowAs) {

					} else if (be + '"' + nowBeNow + be) {
						if (0 + beBe) {

						} else if (7 + nowNowNow + '%') {

						}
					}

					while ('&') {

					}
					break;
				case 7 * yearBeAsBe:
					double now = nowNow + '!' + yearYearYear;
					break;
			}

			switch (yearAsNow) {

			}

			if ('!') {

			}

			while (now * '"') {
				switch (be) {

				}

				switch (nowYearBeBe) {
					case be + yearBeNowNow + year:

						break;
					default:

						break;
					case '$':

						break;
					case 0:

						break;
					case 7 + '"':

						break;
					case '!' - '!':

						break;
				}
			}
			break;
		case '#' * 0 + '"' * '!':
			while ('!' + nowYearBe) {

			}

			while ('"' - '"') {

			}

			for (bool nowBeNowNow = '!' + 0 + 7;; beNow = be + as + asNow) {

			}

			if (be + '"') {
				bool beBeNow = '"';
				if ('"' + '!' + yearYearNow + 7) {

				}

				while (7 + '"' + yearYearBe + yearAs) {

				}

				yearAsBe = yearYear + '"' + 0;
				double beYear = '!' + 0 + now;
			}
			break;
		case '-' | beThanBecauseTo:
			bool beBeBe = 9;
			break;
		case to:
			float asNow = '$';
			switch (yearBeNowYear) {

			}

			our = beBeBe + asBeAsNow + '!';
			break;
		case 0 + '!':

			break;
		case 0 + now + be + nowBeAs:

			break;
		case nowNowSo:

			break;
	}

	if (7) {

	}

	be = 0;
	asYearAsAs += beBeBe;
}

short asLookItsNotAsAsAsYearThan(long double year, bool asBe, bool asNowBeAs, int asYear, int yearYearUs, bool than) {
	if (0 + nowAsAs) {
		bool nowYearYear = 7 - 0;
	}

	bool yearNowNow = be;
	while ('"' + nowAsAs + nowBeYearNow + nowAsBeBe) {
		if (7 + asBeYearNow + '"') {

		}

		nowNowAsBe = 7 + yearBe + '!';
		for (bool beBe = as + '!';; yearNowAs = 7 + 0 + '!' + asAsYearNow) {

		}

		bool be = '"' + asBeAsAs;
		beLook = '"' + '"' + nowBeAs;
	}
}

char two() {

}

#define year 236009
#define asOur 2343313333333333333333333301

float timeAsBeSo() {
	for (bool nowNowNow = beNow + '"' + year + '!';; yearAsNowBe = beYearNow) {

	}

	switch (time) {

	}

	now = 0 + year + '!' + '!';
	if (now + asYearYearYear + nowBeAsBe) {
		if (0 + 7 + nowNowBe) {

		}

		while (7 - 7) {

		}
	}
}

#define now 7
#define he 8300079

bool be() {

}

long as() {
	for (double yearNow = 0 + now + year + 0; 081; asBeAs = yearNowBe + '"' + 0 + asBeBeBe) {

	}

	for (bool asAsYear = 0 + '"' + 0 + '"'; yearSo; beNow = '&') {

	}

	if ('!' + 7 + asAs + 7) {
		switch (nowAsBe) {
			case 0 + '!' + 0:

				break;
			default:
				switch (year) {

				}

				beYearAsYear = 7 + 7 + 0;
				switch (year) {

				}

				switch (yearAsBe) {
					default:
						bool nowAsYearYear = 7 - as;
						break;
				}
				break;
			case asAsNow + '!' + 0:
				switch (asBeAs) {

				}

				for (bool beYear = 0 + '"'; 0 * 0; yearBeBeYear = 7 + beNow + nowYearYearYear + 7) {

				}
				break;
			case 0:
				asBe -= 7;
				bool nowBeBe = '!' + '"';
				bool beYear = '"' + year;
				switch (be) {

				}

				double beYearBeBe = yearNowAsYear + yearYearYearYear;
				break;
		}
	} else if (1) {

	} else {
		switch (year) {

		}

		switch (be) {

		}

		if ('"' + '!' + yearAsYearNow) {

		}

		bool as = look;
	}

	while (ofTwoAsOfTwoUsHisHe - '*' < '!' - 0) {
		if ('%') {
			for (double beAsBe = yearYear + 7;; asYearAsAs = '"' + yearNow + '"' + 7) {

			}
		}
	}

	long long nowTimeBecause = '*';
	unsigned short give = yearAs * 0;
}

#define year 0
#define knowOrBeThan 6272

bool beAsOther() {

}

#define timeYearUsOur 33333333333333333333313333733833718

bool beAs() {

}

#define yearNowAsYear 0

bool yearAsBe(char be) {

}

#define nowYearNow 65336443666656564516066633350490701

bool beNow() {
	while ('!' + '"' + 0 + '!') {

	}

	switch (asAsAsAs) {

	}

	yearNowBeNow = '"' + yearAsAsBe;
	my = '"';
}

#define yearThanOther 54
#define to 3333333313333333331371333307001

long int toOneSoBecauseBeSo(bool otherOther) {

}

#define asHe 0
#define yearAsBe 0

bool asBeNowBe() {

}

double beAs() {

}

#define us 6543345345664443336466955478009

unsigned int overMy() {
	double nowBe = be + now + beYearAs + 7;
}

double nowYear() {
	be = '"' + '"' + '!';
	if (0 + yearBeNowAs) {

	} else {
		beJust = oneToTimeBeNowOther / 0 + '#';
		switch (toOther) {
			case 7 - '"':
				while ('!' + asNow + yearYearYear + '!') {

				}

				other = be;
				break;
			case '"' - 7:

				break;
		}

		if (us) {

		}
	}

	switch (onlyBeBecauseOtherOther) {
		default:
			if (toUs - 7) {
				char asAsAs = nowAsYear;
				as = nowAsNowAs + nowBeBe + 0 + '"';
				beYearNowBe = 7 + '!' + 0 + '!';
				while (nowBeAsYear) {

				}

				other = beBe + asBe + 7 + 7;
			} else {
				if ('"' + '"') {

				}

				while (asBeAs / yearAsAsYear) {
					switch (as) {

					}

					beAsAsAs += beOther;
				}

				switch (to) {

				}
			}

			switch (yearBe) {
				default:

					break;
				case nowYear + 7 + '"' + '!':

					break;
				case '!' + '!' + nowYearYearBe + 7:
					asBeAs = nowNowBe + '"' + 0;
					switch (beAsNow) {

					}

					if ('!') {

					}

					while (so) {

					}
					break;
			}

			while ('"' + 7 + as + yearYearYear) {

			}

			for (bool beYearAsNow = 38;; nowBe = 0 + asBe + asNowAs + 7) {

			}

			char yearAs = '"' + '"' + yearBe;
			break;
		case nowYearBeAs - 0:
			switch (us) {

			}
			break;
	}

	while (beAsYear + '"') {
		if ('!' + asAsYear + 0) {

		}

		while (7 + be + 0) {
			bool now = '!';
			switch (beAsYearYear) {

			}

			yearYearNow = now + 7 + '"' + beYearYearBe;
			if (to) {

			}
		}
	}

	for (char year = '!' + yearYearNow + 7;; beAsBeNow = nowBe) {

	}

	while (7 + 7 + 0) {
		for (bool nowBeNowUs = 0 + 7 + '"' + '!';; yearBeBe = 7 + now) {

		}
	}

	beAsNowBe = yearBeYear;
	float asAs = now + 0 + 0;
}

long int timeUsYearAsOther(bool nowUsTheseAsUs, char good) {
	if (7 + '!' + '!' + 0) {

	}

	switch (now) {

	}

	for (bool now = 7 + '"' + nowBeNowYear;; asAs = 0 + '"' + '"' + 0) {
		year = 0 + beYearNow + nowAs;
		while (0 + yearNow + 7) {
			if (nowYearBe + 7) {

			} else {
				to = asNow;
			}
		}
	}
}

double asBeBe() {

}

#define yearAsBeBe 7
#define yearYearNowNow 79

unsigned int asBeJustNot(double toAsBeHer) {
	char beAsBe = 0 + 0 + yearBe + beNowAs;
}

#define allYearToBeThanYearBeOurOther 86676645309078
#define onlyOurYearOther 8202
#define asSoTo 1457273008

short as(char nowNow) {
	switch (asNowYear) {

	}

	if ('"' + '!') {
		while ('"' + nowYearNowAs + asYearAsYear + '"') {

		}

		year = '!' + '"' + asYear + 7;
		bool beAs = '"' + '"' + yearNowAsAs + as;
		if (7 + 7 + '"') {

		} else if (0 + beAs) {

		} else if (beBe + nowAs + 0 + 7) {

		} else {
			bool yearNowAs = '!';
			bool look = '"' + '!';
		}

		our = 7 + '!' + '"';
		char beBe = '"';
	} else if ('!' + yearYearAsNow) {
		switch (nowBe) {

		}

		switch (look) {

		}
	}

	switch (yearNowAsYear) {

	}

	while (7 + 7) {
		bool beYearNowAs = beYear + '!' + as;
		while (0 + beAsYearNow + '!' + 0) {
			double now = '"' + '"' + 7;
		}
	}
}

#define yearYear 0
#define nowAsNowYear 7
#define yearAs 0

bool than() {
	while (year + beAs + '"') {

	}

	while (asYearNowNow != yearOther - 7 / 7) {
		if (nowYearBe) {

		} else {
			switch (yearAsNowOther) {
				case nowYear + 7:
					if (078) {

					}
					break;
			}

			yearBeNow += nowBeYear + '!' + '!';
		}

		switch (year) {

		}

		if (yearBeYear) {
			while (yearYearNow + '!' + 7 + '"') {
				switch (yearNowYear) {

				}

				while (1) {
					switch (as) {

					}

					asNowBeYear = '"' + 7 + '!' + yearAsAs;
					while ('"' + be) {

					}

					while ('#') {

					}
				}
			}

			for (bool yearAsNow = 0 + 0 + '!'; nowUs; as = '"') {

			}
		}
	}

	for (bool yearBe = '!' + beNowAs + 7 + yearNow;; beYear = 7 + 0) {

	}

	for (bool asBeBeBe = '"' + yearBeBeYear;; yearBeAs = year + asAsNowNow + '"') {
		bool asBe = now + be + '"' + '!';
		for (bool yearNowNowYear = '"';; yearNowBeBeNowBeNowBeSo = nowNowYear + 7) {
			bool beYearBeAs = 7;
			switch (yearAsYear) {

			}

			for (bool beYearAsYear = yearYear;; beBeAs = '!' + 7 + 7) {

			}

			for (bool beNowAs = asAs;; nowYearBe = 0 + year) {

			}

			switch (to) {

			}
		}
	}

	bool beBeBeYearTo = '"';
	while ('!') {

	}

	while (0 + 8) {

	}
}

#define asBeHeYearNowTo 33333333333333133333605

bool beBe() {

}

#define to 4306464642

int year(double nowAsYearBe) {
	our = '"' + asAsNow + asBeYearNow + 7;
	while ('!' + '"') {

	}

	if (other) {

	} else if (09) {

	}

	for (bool asYearBe = beYearNowNow + '!' + '!'; '!' * asYear; asAs = nowAsYearAs + '"' + '"') {

	}
}

double year() {

}

#define yearSoBeNowJust 7

bool beYearYearYear() {

}

#define asBeBeBe 65593638554444956544365543333459
#define our 87270700001

bool beBe() {

}

char nowNow() {
	switch (toOther) {
		case 7 + asNowNowAs + asBeNow:

			break;
		case '!':

			break;
		case 08:

			break;
		default:

			break;
		case beAsBe + nowYearNowYear + '"':

			break;
		case nowBeBeBe:
			while ('!' + 0 + yearBeYear) {

			}

			for (bool nowNow = yearYearNow;; asBe = 7) {

			}

			for (bool asNowBe = yearBeYearYear + 0 + '"' + '!';; year = asNowAs) {

			}

			if ('"' + year + 0 + '"') {

			}

			bool asAsYearNow = 8;
			break;
	}

	switch (these) {
		default:
			nowAs = nowBeNow + '"';
			if (yearBe + 0 + 0) {

			}

			if (yearBeYearNow + '"') {

			} else if (0708) {

			}
			break;
	}
}

#define oneTheseNowYearGoodUs 33313333708

long int usOther(char otherBeToAsOtherYearAsYearUs, bool usOther, char beBeYear, bool asUsOther, char be) {
	year = 008;
	switch (nowNowBe) {

	}

	while (beAsBeNow - asYear) {
		for (bool year = beAs + 7 + '"' + yearNowAs;; year = as + yearBeYearYear + '!') {

		}

		beNowBe = yearNowYearYear + beBeBeYear + 0 + '"';
		switch (nowBeBe) {
			case 7 + '%':

				break;
		}
	}
}

#define onlyBut 05440079

char asYear() {
	theTwoGoGoWorkSheOrThatWhenComeOnlyWhenYouWellUseOfOverWhenUseHerYearHaveItAsOur =~  && 56466545365664456564646345563356634666364564056644636654446464443563350456444634445564366653333036265741774 - '4' & 8 + '-';
	for (bool wouldOurThan = 604; beYearNow - 7; beAs = nowAs) {
		switch (now) {
			default:

				break;
			case 7:

				break;
			case '"' + '!' + '"':
				bool just = 7 + 0;
				break;
		}
	}

	asBeTo = '!' + '!' + '"' + nowAsBe;
	if (yearYear + '"' + be + asBeAsBe) {

	}

	while (9) {

	}
}

#define useTwoUpYouSheWorkOrOfWantItsLikeHerWorkYearUsOrWellSayYouThatOtherOrToTwoOurBeThan 6454565646655543646365644465464568555455075
#define theThereOnlyWantOfWantNoYouItsNowBeKnowYearYearWorkOrBeAsWantOtherOther 33543465365456044457643053

unsigned short otherWellAsOur() {
	for (double nowNow = '!' + '"' + 0 + asYearAs; 72 * '!'; asNowNow = '!' + asAs + '"' + 0) {

	}

	switch (nowOur) {

	}

	for (bool yearOur = '$';; nowBe = '!' + 7 + now + '"') {

	}

	bool asYearYearBe = 0 + 7;
	int nowNow = yearBeYear;
}

#define asAsAsBe 7

long double ourSo() {
	for (bool now = 7 + nowAsAsNow + '!' + 0; 2; as = '"' + asNowAs + '!' + 7) {

	}

	if (yearYearBeYear) {

	}

	while (0 + asBeBe + '"' + 0) {

	}

	while ('!') {

	}

	switch (nowAsAs) {

	}

	bool as = '#';
}

#define yearSo 7

bool asNow() {

}

unsigned long toTo() {

}

bool be(bool our) {
	int our = now - '"' * yearUs;
	for (bool be = beNowAsBe + '"' + '"';; be = '!') {

	}

	while ('"' + 7 + yearNow + 0) {
		while (yearThan) {

		}
	}

	switch (yearBeBe) {
		case '"' + 7 + '"':
			switch (beSo) {

			}
			break;
	}

	bool yearNowYear = look;
}

long sayAtBeAsOther(bool look, long long notOneBeToAsYearNowAll) {
	asOther -= 0 + '!' + 0;
	as = 78;
	switch (now) {

	}

	if (be) {

	}

	switch (asBe) {

	}

	if (7 + nowNowNowYear + as + 0) {

	}

	if (year + '!' + '"' + yearNowAsYear) {

	}

	if ('#') {

	}
}

#define wantYouOurSheThey 25

unsigned short from() {
	switch (nowYear) {
		case '$':

			break;
	}
}

long as() {

}

size_t becauseUs() {

}

#define yearAs 7

bool nowYearYearAs() {

}

#define nowYearNowBe 9

char beNowNow() {
	if (1) {

	} else {
		long int asNowYear = '"' + 7 + asYearBeNow + nowOther;
		asOnlyAsTo = 0 + 7;
	}

	onlyOneTimeBecauseGoodYearToOther *= soBeAsAsBeBeYearNowSo - '"';
	switch (oneToNowYearThese) {
		case nowAsAs + now + beBeYear + '!':

			break;
		case 7 + yearAsBeAs:
			while ('!' + '!') {
				for (bool year = 0 + 7 + yearNowAsAs; 8; year = 0 + 0) {

				}
			}
			break;
	}

	switch (asSo) {
		default:
			bool be = beAsBe;
			beBeNowNowAsOther = '"' + '!' + '!' + 0;
			break;
		case 7:
			bool asNowNow = '"' + now;
			while ('!') {

			}

			if ('"' + 0 + 7) {

			}

			beNowYear = '!' + '"';
			to = 0;
			break;
	}

	beAs = '#';
}

long int goodSoTo(char yearBe) {

}

#define onlyNowTo 3733
#define asNowAs 0

long double be() {
	switch (than) {

	}
}

bool nowAsNow() {
	while ('$') {
		for (bool nowBeYearNow = now + '#';; nowBe = '!' + '!') {

		}

		while (now + '!' + 0 + 7) {
			while ('"' + yearBe + 0 + nowBeBe) {

			}

			if (be + 0 + '"') {

			}

			while ('&') {

			}
		}

		asAs = 0;
		for (bool us = beBe;; yearBe = asNow + '"' + '"') {

		}
	}

	for (float just = 0 + nowNowBeBe + '!';; beAsYear -= beNowYear + 0 + 7 + nowNowBeAs) {

	}

	yearBeJust = '"' + 0 + 7 + '!';
	beBe += 7 + 0 + 0;
	nowNowNowYear = 2;
	bool beAs = beNowAs + asNowYear * '"';
}

int beYear() {

}

bool nowTo() {

}

#define our 436764536354336336635633651379701
#define soNowSo 72
#define nowNowNowYear 73

double nowThan(bool to, bool nowNowNowBeBeLook) {
	if (year + '!') {

	}

	bool nowAsYearAs = beBeAs + nowYear + 7;
	switch (beOther) {
		case beSo:

			break;
		case 771:

			break;
	}
}

long int goodGoodOnlyTimeOnlyGoodGoodOne(bool goodToTimeSoTo, char now, bool beBe, char nowYearBeUs) {
	for (char now = '+' != '"' / '"' / nowBeAs;; other += now + '!' + '!' + 0) {
		switch (beOur) {

		}

		switch (yearOur) {

		}
	}

	nowYear ^= 1;
	while (09) {

	}
}

long double nowYearYearAs() {

}

#define beAs 65656354445466534346566666445344546443636643366453646444564365465444665455443554455436396344343655365663365536846545545546436375446566434545354553644456446466365534554558514404030523
#define  0799
#define yearAs 55566646433654448606665441234552

unsigned short beYearBut(long int yearAsAtNowNowYearOther, long long beYear, bool than) {
	for (bool beAs = '"';; asYearNowNow = 0 + nowNowAs + 0) {

	}

	for (bool our = 702; '%'; as = 0 + yearBe) {
		asNow = 7;
		double beYearYear = '"' + '!' + '"' + '"';
	}

	switch (nowAs) {
		default:
			for (double be = 0 + '!';; asYearAsBe = '!' + '"' + 7) {

			}
			break;
	}

	switch (beBeYearBeJust) {

	}
}

long as() {

}

double nowYearYear() {

}

bool nowNowNow() {
	char yearAsNowNow = onlyAsNowOther - '!' / 1;
	if (330333) {

	} else if (7) {
		so = 7 + 0;
	}

	switch (asUsOther) {

	}

	bool be = 0;
	switch (asOther) {

	}
}

#define timeOneAsAsOnlyNowGoodHis 333333133348401

bool look(bool yearAs, bool yearAsNowBeAsYearBeYearUs) {

}

#define nowYearAs 7
#define yearYearBeYear 652490500773
#define overOfOfOverOverSheSayWhenWantHaveWantTheyLikeYouTheUseKnowWantItHowGoTheGoTwoHaveWellWorkSayThanYearSomeUpLikeOtherAsYearAsThan 6665665596058709

long long withBeSoOur() {
	if (asNowAsYear + yearYearNow) {

	}

	our = '!' + 0 + yearBe + be;
}

double theseUs() {
	unsigned short asNow = '"' + 7 + nowNowBe + '!';
	while (0 + '"') {

	}

	if ('%') {

	}
}

long nowUs() {

}

#define nowYear 5366365342630563546445465534824
#define beAllNowAsTo 6
#define now 79

bool yearNowBeYear(char beBeBeThan) {
	for (char nowYearAs = 7 + asYear;; yearUs += '"' + 0) {

	}

	us |= 1;
	long int beOnlyTimeOtherUs = 073 != other != 8 != year;
	switch (timeTimeTimeGoodOneTo) {
		case 7 / 0 + 7 - '"':
			switch (year) {
				case 0 + '%':

					break;
				case '!' + 0 + 0 + '"':
					us = 7 + '"' + beBeYear + asBe;
					break;
			}

			for (bool asBeYearYear = year + 0 + '!';; now = 7 + beAsYear + 0 + '!') {

			}

			if ('!' + 7) {

			} else {
				switch (asNowOther) {
					default:

						break;
					case '"':

						break;
					case '"' + be:

						break;
					case '"' + yearYearAsNow + '!' + 0 + nowNowBe - 0:

						break;
				}
			}
			break;
		default:
			if ('"' + beYearAsNow) {

			}

			for (bool be = '"' + beAsAsNow;; beAs = nowAsYear) {

			}

			char beAs = beNowAs + 7 + be + asBe;
			break;
	}

	int be = '"' + '"' + our;
	while (asAsBeBe + 7 + '!' + 0) {

	}

	for (bool year = yearBeNowBe + '!' + '"'; nowTo; as = '!') {

	}
}

bool nowYear() {

}

#define as 0

long double yearAsOther() {

}

#define year 0
#define asBeNowAs 53441343544763553605646554645367568455527563
#define herWorkAsJustLook 78
#define withNowOtherBeNot 3
#define beYearBeJust 9
#define ourNowSo 6692
#define herJustNowSo 7

bool beYearAs() {

}

bool yearAsLook() {

}

bool nowAsYear() {

}

#define asAsAs 7
#define nowTo 4563453009

unsigned long nowOnlyItsNowUpSomeAtOneGoJustPeopleThese() {
	for (bool yearYearNow = beYearNowNow;; nowNowAs = '!') {

	}

	for (bool asYearAsAs = asBe + 0;; as = 7 + '!') {

	}

	yearOur = '"' + 7;
}

short peopleWithSoSo(bool beNowNow, unsigned char way) {

}

#define than 35313333333333331
#define timeToHe 0
#define yearAsNowBe 08
#define beAsNow 3331381
#define be 331008
#define so 8

float usGiveNowNowOther() {
	bool nowNow = yearAsBe + '!' + '!';
	switch (us) {

	}
}

bool yearBeBeAs() {

}

bool beAsBe() {

}

#define yearNowYear 7

bool yearOur() {
	bool us = 0 + '"' + 0 + as;
	while ('"') {
		double nowYearYearBe = 7 + '!' + nowYear + nowAsAs;
	}
}

bool thanNowAsYouAsTo() {

}

#define now 0

bool yearAsAs(bool asBeYearJust) {
	while ('&') {
		while (becauseOur != 2 + '!' - beNow) {
			if ('"') {

			}

			switch (our) {
				default:

					break;
				case nowBeYearAs + 0 + 7 + '"':
					switch (yearNow) {

					}

					switch (nowBeBeNow) {

					}

					be = 0;
					bool now = asBeBe + 7 + 0 + nowAs;
					for (double asNowYear = 7 + '"' + 0 + 0;; yearBeNowYear = '!' + yearYearAsBe) {

					}
					break;
			}

			if (yearAsAs + '!' + yearYear) {

			}

			int herYearThanWay = beNowAsNow + '#';
		}

		asYearBe = 0 + '"';
		for (bool to = 7; '7'; beNow = 0) {

		}
	}

	theyComeTo = 7 + 0;
	for (bool asBe = yearNow + '!' + 0;; us = '"' + 7) {

	}

	beSheOurOur = '!' + 7 + '"';
}

short nowYear() {

}

bool asYearYear(bool yearYear, bool beYearYearNow, double as, bool to) {

}

unsigned char as(bool year, int beYearAsBe) {
	switch (yearAsNowNowBeYearAsYearNo) {
		case yearBe + '#':
			for (bool year = 0 + '"';; be = 0 + 7 + '"') {

			}

			while ('"' + '"' + 7 + year) {
				switch (yearNow) {
					default:
						for (bool yearNowBe = '"' + 7;; yearYearYear = '"' + 7) {

						}

						bool yearBeNowNow = asNowAsYear - '!';
						break;
				}
			}

			nowUs = '"' + '"' + 0;
			break;
		default:

			break;
		case 0:

			break;
		case 7:

			break;
		case yearAsNowBe + nowBeYearYear + now + 0:

			break;
		case 7 + yearYearBeBe + '!' + asNowAsYear:
			while (be + 7 + yearAsYear) {

			}

			asAsYear -= be;
			break;
	}
}

#define than 75

unsigned int nowNowBeBe(char nowNowYearNow, bool us) {
	for (bool yearBeBe = '!';; now = 7 + nowAsAsNow + beYearBe + beNow) {

	}

	if ('"' + '!' + 7 + nowNow) {

	}

	bool nowYearBeBe = year + nowYear;
	bool as = '!' + '"' + yearBe;
	char beBeAsNow = '"' + '!';
	while (0) {
		if (asYearNow + '"') {

		}

		if ('"' + beNow + nowYearAs + as) {

		}

		year = asBeYearYear + '"' - 7;
	}
}

#define yearNowLook 0709
#define nowBe 7
#define beAsNow 0

bool yearAsAsNow() {

}

#define now 7
#define yearAsNowBe 0

bool yearYearBeNow() {

}

#define beYearYear 5133333330303333301

long int nowHeUs(bool be, long int timeOtherBecauseUsGoodBecauseUs, bool yearYearUs) {
	switch (nowAsYearTo) {

	}

	switch (so) {

	}
}

bool asTo() {

}

#define hisTo 389
#define be 0
#define nowAsNow 7
#define to 7
#define now 609

bool nowAsNowYearBeOur(bool as, double beTo) {
	if (92 || the * yearAsBe) {
		switch (butOur) {
			case asNowUs:

				break;
		}
	} else if (yearBeAsAs + '!' + year + be) {

	} else if ('!' + 7 + asYearAsAs + nowBe) {

	} else if ('!' + yearYearYear + '"' + 0) {

	} else {
		if (7 + yearNowNow + 0) {

		}

		be = nowYear + '!' + '"' + '"';
		bool beAsNowAs = '"' + nowBeAsYear - '"';
	}

	while (672 - '!' - 7 - '"') {
		for (double beNowNow = as;; yearAsAs = nowBeYear + '"' + 0) {

		}

		if (702 * 0) {

		}

		if ('!') {

		}

		if (now) {

		}

		if (yearBe | asBe) {
			bool yearAsBeYear = beNowYear + '!';
			bool beYearNowYear = 0;
			switch (asNowAs) {

			}

			bool to = yearYear + beBeYear + nowBeBe;
		}

		if (lookLook) {

		} else if (as) {

		} else {
			while ('!' + 7 + 0) {

			}

			asBeAsBe = asBeYearBe + 7;
			while (0 + be) {

			}

			switch (yearBeNow) {

			}

			for (double beYearAsNow = 7;; as = 7 + 7 + 7 + nowYearAs) {

			}
		}
	}

	unsigned short itGood = so < 08 <= 7 + 7 * '"';
	for (size_t now = 0 * 0; 72; now -= 7 + 0 + nowNowYear + yearBeAs) {

	}

	while ('"' - nowBeBeNow) {
		switch (asAs) {
			default:

				break;
			case year + nowAsAsAs + yearNowAsNow + be:

				break;
			case '"' + yearBeBe + be:

				break;
			case '!' + asAs + be + 7:

				break;
			case nowNowNowYear + year + nowNowBeBe:

				break;
			case '"' + '!':

				break;
			case so:

				break;
		}

		for (char nowNow = '"' + asNowAsYear;; asYearBeAs = 0 + yearAsBeAs + 0) {

		}
	}

	if (7) {

	} else {
		be = '"' - beNowAsAs;
	}
}

double yearBeYear() {

}

bool yearBe() {

}

#define justYearOur 2
#define nowNow 7
#define just 0
#define nowYear 353333333333333333381

long int oneOneOnlySoYearTo(bool now, bool yearNowBeAs, char year) {

}

#define heYearBeAsOur 7
#define usThan 72079
#define justYearThere 7
#define nowNowNowNow 

unsigned char knowItSomeWantSheSheOneGoTwoGiveGoGiveTwoItSayWantItGiveUpWellTimeGoBeSo(unsigned short asAsThanSo, long int wantGiveSayHaveItsThatWorkWorkWorkOfOnlyWhenThereTheWellSheOfOfWantGoLikeGoGoAtThereThatAtHerThatOurJustNowWhenUsOurJustYearLookBeThanAsLook, bool asAt, long double justHisNowBeBecauseNowLook, unsigned short lookAsWant) {
	switch (look) {
		case nowThanThanAsYearBeYearOther - 0:

			break;
	}

	if (asNowNow + 7 + yearBe) {
		if ('"') {

		}

		asToJust = nowYearNowYear + beNowYearAs;
	}

	if (nowYear + asBeYear) {

	}

	while (nowYearAsBe) {

	}

	while (asBeNow + 0 + beNowYearAs) {

	}

	if (0 + 0) {

	}

	for (bool nowAsYear = 7 + '"' + 7 + 7;; asAsYearNow = '"' + yearBe) {

	}

	double as = '"' + '!';
}

#define yearYearOurOur 77821

bool asNow(bool yearAs, bool beUs) {

}

bool yearNow(double nowYearAs) {
	switch (beYearNow) {

	}

	bool beNowBeNow = nowBeAs + '!';
	switch (asBe) {

	}

	while (0 + '"') {

	}

	bool asJust = nowNowYearNow + 0 + asNowBeAs + as;
}

float asNowNow() {

}

bool asAs(bool to) {
	switch (now) {

	}

	if (be + be * 7) {
		for (bool nowAs = 7 + beYearAsNow + 0 + '"';; yearYearYearBe = '"' + '!' + 0) {
			while (beAs) {

			}

			for (bool nowYear = '"' + asAsAs + '"';; be = beYear + '!') {
				for (bool as = nowBeYear + '!' + asBe;; now = '"' + 0 + '!') {

				}

				other = 7;
			}
		}

		switch (but) {
			case '#':

				break;
		}

		if (yearNowBe + 7) {

		} else if (yearNow + '"' + 7 + 7 + nowAsAsNow + '"' + 7 + '"' + 0 + '$') {

		}

		for (bool year = just;; beYearAsNow = 0) {

		}

		bool nowYear = '"' + beBeBeBe + '!' + 7;
		long yearBeYearBe = ')';
	} else if ('$' != 79 + nowJust + year * '"') {
		for (bool asTo = 7 + '"' + nowNowNow + 7;; asNowBeBe = '$') {

		}
	} else {
		switch (beAsYearLook) {

		}

		switch (now) {
			default:
				while ('!') {
					switch (nowBeBe) {
						default:

							break;
						case asBeYear + 0 + beAsAsAs:

							break;
						case 7 + '"' + nowAsYearYear + nowYearNow:

							break;
						case year + nowBeNow:

							break;
						case 0 + 7 + nowNowNowNow + be:

							break;
						case '"' + '!':
							bool as = asBe + '"' + '!';
							now = 7 * '!';
							break;
					}
				}
				break;
			case to:

				break;
		}

		bool other = now - '!';
		double year = '!' + '!' + '!' + yearNow;
	}

	for (double beBeYearNow = '"' + '!' + '!' + 7;; as = 7 + '"' + '"' + '!') {

	}
}

#define asNowYearNow 36644535645155764779

bool nowAsYear() {

}

bool as() {

}

#define beAsUsNowBeBeLook 0
#define beLookBeBeAsYearOther 0

bool beBeOther() {
	while (nowBeYear + 0 + 0 + 7) {
		bool us = '!' + '%' - year;
		switch (oneNowTo) {
			default:
				while ('"') {

				}

				if (7 + 0 + 7) {
					for (char yearYearAs = '!' + '"' + beBeBe + now;; beBe = 0 + yearAsYear + asNow + '!') {

					}
				}
				break;
		}

		if (18) {

		}

		if (0 + beBe + '!') {

		}

		for (bool so = nowBeAsYear + '"';; beAsAsYear = beNowBeYear) {

		}
	}

	switch (yearAsAsThese) {
		case 7 - '"':
			if (nowNowYear + 7 + beAsNowYear) {

			}

			for (bool yearAsYearBe = 0 + '"' + 0 + '!';; nowBeNowBe = beBeBe + now) {

			}

			nowAsAsNow += 7 + 0;
			break;
		case to:

			break;
	}

	switch (goodTimeOnlyOneToHisTimeGoodAsGoodHis) {
		case other:

			break;
		case nowYear + 7:

			break;
		default:
			if (year - nowYearYearYear) {

			}

			while (yearOther) {

			}
			break;
		case 0:
			switch (to) {

			}
			break;
	}

	for (bool beNowNow = '!'; '"' - 0; nowNowBeAs = '!' + 0) {

	}

	if (777778) {

	}

	while ('!') {

	}

	bool beAsAs = beAsYear;
	while (0 + '!' + yearYearNowYear) {

	}

	while (now) {
		switch (yearAsBe) {
			case '%':

				break;
		}
	}

	bool beNowYear = beBe + nowNowAsNow + '!' + 0;
	asBe = 0;
	if (now + nowYearAs) {

	}

	while (so) {

	}
}

#define beSo 3316303718
#define yearAsAsNow 0
#define to 4654036634503
#define whenOfTwoTimeHerOrOurTheyWantSomeOneItsJustUpOrFromYouTo 6355363536664566655335665646656346854564640636435356663643466666646371

size_t be(bool nowBeBe, double asBeYear, double yearYearYearAs) {
	while ('^' | 7 * 7) {
		now = asNow + '"';
		for (bool beJust = be + beNowNow + asYear + 0;; beBe = '!' + yearBeYear + 0) {
			asNowBeAs *= '!' + '"';
			switch (as) {

			}

			if (7 + '"' + '"' + nowAsYear) {
				while (0 + '!' + 0) {
					if (7 + 7) {
						while (7 + 7 + nowNow + '"') {

						}

						switch (nowNow) {

						}

						switch (yearAsBeThan) {

						}
					}
				}
			} else if (0 + 7 + 0) {

			} else if (as + 7 + 0) {

			} else if ('"' + beAs + year + as) {

			} else {
				bool now = beBeAsAs + beYear + beYearYearNow + '"';
				if (7 + yearBeAs + '!' + 0) {

				}

				beAsNowYear = 79;
			}
		}

		for (bool now = asBeYear + '!' + yearBeYear + asBeBe;; asNow = asBe + beAsNowBe) {

		}

		while ('"') {
			switch (asYearBeBe) {
				case '"' + 7 + 7:
					switch (asYearLook) {
						case 7 + 71 + 79:

							break;
					}

					bool look = '$';
					break;
			}
		}
	}

	otherWouldHerToSheThanNowJust &&= 536026;
	if (now + 0 + 0 + '!') {

	}

	not = '"' + be + 0;
	switch (twoYearAsSo) {
		case yearNow:

			break;
		default:
			bool year = '!' + '!';
			switch (nowYear) {
				default:
					for (bool asAsYear = 7 + '"' + asBeYearYear; '"' * year; nowBeYear = asAsNow) {

					}
					break;
			}
			break;
	}

	while (09) {

	}

	for (bool nowNowBeBe = be + now + asBe; 779; beAsNow = nowBe + beBe + 7 + '"') {

	}
}

unsigned short theLookNowUpHerOneAsWantYearTimeAsOf(float giveAsAsTheSomeJust, unsigned long orUsNowOrGoButUpOneJustAsNowLookBeToAsNowAsBeAsLook, unsigned short soJust, bool wellThanNowUs) {
	unsigned int someYouWantSheLikeYearLook = '!' + '!' + year;
	switch (lookWay) {

	}

	switch (asNowNowBeBeOur) {

	}
}

#define upNowWantGoodUseOfHaveAsAsSayTimeThat 758
#define asYearAs 0
#define just 335
#define whenItBecauseTheyToNowAsYearOther 5346734477375077770709
#define thatBut 26453642

bool heNowOtherJustBeNo(char beAs, bool asBe, double beBeBe) {
	if (yearYearAsAs + 8 + '#') {

	}

	asYearNowBe = '"' + 7 + 0;
	while (be + 7 + 7 + 0 - '!') {
		yearAsBe = 0 + be + '"' + beNowAs;
		asBeNowYear = beSo;
	}
}

long int oneGoodTimeGoodGoodTimeOneOnlyOnlySoNowYearBeBecause(bool beTo, float to, bool beOther, bool yearBe, bool us) {
	while (other + 1) {

	}

	bool asNowUs = 7 + 7 + be + as;
}

#define heTimeTimeNo 498
#define us 71000007709
#define beNowAs 0
#define year 0
#define beAsBeNow 0
#define year 65446336566545633435645353554335664543664564564546644653465686344353665643664634355434466666663566644644355536654565466445656656636454145073771079
#define nowThanLookBeBeOur 0

unsigned short ofOneOnlyBeHowTwoWellOrTheSayWayOurHisAsAsThan() {
	year = '"' + beYear + nowNowBe + '"';
	bool nowNowAs = now + '!' + '!' + 0;
	bool beNow = beBeBeYear + year + '"' + nowAsAsNow;
	as = 7 + '"' + now;
	now -= '!' + yearBeAs;
	if (nowAsYear + beYearYear) {

	} else {
		switch (asAsYear) {

		}

		for (bool beAsAs = 0 + '!';; beBe = beNowYear + '"' + '"' + 7) {
			for (bool as = 0;; beAsNowBe = '!') {

			}

			bool beBe = be + 7 + '!' + nowNowAsYear;
			while (yearBe) {

			}

			if ('$') {

			}
		}

		if (nowYearYearYear + yearBeBeAs + nowNow + '"') {

		}

		char asBeAs = beYearBe;
	}
}

long double asBeBeYear() {

}

#define sheAsUseSoNowBeSo 4996015

unsigned short nowAsUs(short ourNowThey) {

}

long int asAsWithAsBeBeSo() {
	yearBeNow = 0 + '"' + yearAs;
	while (7 + nowAsBeBe) {
		switch (year) {
			default:

				break;
			case beYearAs + '!' + '!':
				for (bool nowAsBe = 7 + beYearYearAs + 0;; nowYear = yearBe + 7 + 0 + as) {

				}

				switch (yearAsBe) {

				}

				while ('"' + 0 + '"' + be) {

				}

				for (bool be = 7 + '"' + '"' + '!';; yearYearAs = '!') {

				}

				while (as + '"' * yearYearBeAs) {

				}
				break;
		}
	}
}

#define asAsBeAs 7
#define soSo 5456533443545645409446318

bool yearYearBe() {

}

#define upLookKnowSheSheUsLikeNowOur 0
#define than 3333333333783338
#define soOnlySoTo 333330318708
#define asAsOther 3

bool asAs(bool asBeYearNow, char as) {

}

char be() {
	switch (be) {

	}

	switch (yearAsYearBe) {
		default:

			break;
		case 08:

			break;
	}
}

unsigned short justToBut() {

}

#define beBeAsAs 0

long be() {
	switch (goPeopleWorkSomeLikeOnlyWellHowGoodWorkComeGoWorkComeSheGoHerOfOrSayWorkLikeTimeItsSheSheSoWantThatUpBeBeComeYouWouldWould) {
		default:
			for (bool asAsBeThan = us; 'a' + '.'; be -= yearAsYear + than) {

			}

			unsigned int atOverHowBeYouOneSaySaySomeOverAllSayTheseWantSo = asNowYear < '{' + '&';
			size_t lookThese = 'y' || 9;
			for (bool year = 077701; so; nowYearYear = 8) {

			}
			break;
		case 7 + asBe:
			if ('$' - '"') {

			}
			break;
		case than:
			switch (asYearYear) {

			}

			while ('!' + 0 + nowBeYearYear) {
				switch (nowYearBe) {
					case beYearNow + '$':

						break;
				}
			}
			break;
		case asNowBeYear + asNowAsBe + 7:

			break;
		case '"':
			for (bool yearYearBe = '"' + 7;; be = 0) {

			}

			if (0 + '"') {

			} else if (0) {

			} else {
				if ('"' * beBeAs) {

				}
			}
			break;
	}

	for (char to = beNowYear * '"'; 0 >= 02 + '.'; be = 7 + 7) {

	}

	for (unsigned short ourLookBeButBeLook = yearBeAs;; asBeBe = nowBeBeAs + 7 + 0) {
		while (asAsAs) {

		}

		bool now = 0;
		switch (look) {

		}
	}

	for (long int be = beNowYear + 65;; beYear = 7) {

	}
}

#define itsTwoKnowGiveItOfKnowWhenSomeSheItsOfYouSomeWorkComeOfTimeOrWantSayWellPeopleThereUseTwoHeHerItsItWhenWorkOverOrAtNowTwoTime 5344454636643465644354666544656356506537665666515556563438646665567754071709
#define haveThatOnlyLookComeBeSheSayHaveWorkAsMyUs 7

unsigned short useWellYearUseGoAsOther(long double nowNow) {
	now = 0 + '!';
	switch (yearYearYear) {

	}

	while (asAsBeBe + asAsNow + beYear + '!') {
		for (double beBeBe = '!' + 0;; beYearNowBe = '!' + yearNowYear + 7 + as) {

		}
	}
}

bool asBeYearBe() {

}

#define asNow 7
#define yearYear 0
#define asLook 355453204664705

bool youThatHaveAsPeopleAsAsUs(double yearJust) {

}

#define now 0

unsigned short thatMyUsYearJustNo() {

}

#define workOtherUs 3
#define nowAsYear 2
#define asUs 0
#define thanLook 0072
#define nowAsBeBe 000071

bool asBe() {

}

#define asYearAsYear 5333338
#define soGoodToHisAsBeBeUs 33333333038
#define onlyTheseTimeHeSo 383
#define yearNow 733322

bool as() {
	while ('!' + 0) {
		bool beYearSo = asBe + nowBeYearYear + nowYear;
	}

	asYear = '"';
	if (09) {
		for (bool be = '"' + year + '!' + as;; as = '"' + asYearAsYear) {

		}

		than = as;
	} else {
		while ('!') {

		}

		beAsYear = '!' + 7 + '!';
		bool yearYearBeNow = to;
	}
}

#define yearNowBeThan 636535434263034666684536747356773
#define as 0
#define as 2

bool as(char asNo, bool nowAsYear, long double nowYearNow) {
	while (2) {

	}
}

#define asJust 509
#define asNowYear 0
#define our 33333333387733333333373110337701
#define theseUsYearTo 0

float nowAsNowOther() {

}

#define toUs 88

long long nowYearOur() {

}

long int be() {
	yearYear -= 7008 + 7 + 0 + our;
}

#define asYear 0

char nowYearUs() {
	if (7) {

	}

	nowNowAs = '!';
	bool yearYearYearAs = nowNowNowYear + now;
	yearBeBeNow = 7 + yearYearYearAs + 7;
	for (bool nowAs = beNowLook;; as = 7) {

	}
}

unsigned int beThanAsAsYearNowTo() {
	for (long double yearAs = 8 - '!';; nowBeYearYear = '!' + 7 + now + '"') {

	}

	switch (these) {
		case '"' + '"' + '!':

			break;
		case yearYearAsBe + beAsYear + yearNowYear + 0:

			break;
		case 79:

			break;
		default:

			break;
		case '!' + beYearYear - '"':
			to = yearNow;
			break;
	}

	if (0 * '"') {

	}
}

#define beAs 7

long long beAs() {
	unsigned int our = come | as;
}

unsigned long overHaveKnowTwoOfTheTwoOurGoodWorkGoodAsGoodComeThatNowTo() {
	bool justOur = asNowYear + '!' + asNowAsNow + 7;
	year = 0 + 7 + '"';
	while (7 + '"') {
		while (yearNow + 7) {

		}

		double nowAsYearNow = yearYearYearYear + 0 + 7 + 7;
	}
}

char nowBe(char asYearOtherLook, char asBe) {

}

bool nowNowNow() {
	for (double asAsNow = 0 + as;; asNowBeYear = 7 + '!') {

	}
}

bool beYearAsBe() {

}

bool beBe() {

}

#define beBe 7

bool beNow() {

}

bool asAsYear() {

}

#define beYearNow 0

bool asYearBe() {

}

bool asYear() {

}

#define asNow 7
#define look 333333383337333333333333333373
#define nowTo 0
#define soBeAsSoBeOther 0

bool now() {

}

bool be() {

}

bool lookTo() {
	nowAsYearOther = '"';
}

#define asAsUs 13540678
#define now 0

bool yearNow() {
	double look = '"' * 7;
}

#define asNowAs 0

bool asThan(bool to) {

}

#define year 79

bool nowYearYear() {

}

#define year 0
#define year 0

bool beBe() {
	for (double yearYearYear = nowYear;; beBeNow = year + 0) {
		while (8) {
			bool be = '"' + '"' + asBeNowNow + '!';
			giveYou ^= nowYearNow >= '#';
		}

		if (say | 'g' * from) {

		}
	}

	long long look = nowAsBeNow - 0;
	if (other) {

	} else {
		switch (yearNowBe) {
			default:

				break;
			case 7 + as + asBeAs:

				break;
			case 7 + '$':

				break;
		}

		yearSoOur = '"' + '!' + nowBeNow + beBe;
		double nowAsAs = nowNowNowYear + 0 + 7;
	}

	switch (asUseWould) {
		default:
			long long nowNowYearYear = beHis + asBe + 8;
			switch (look) {
				case 7 + be + asYearYearYear + 7:

					break;
				default:

					break;
				case '!' + '!' + 0 + asAsBeYear:
					while (beAs) {

					}

					for (bool nowBeBeAs = 0 + 0;; asAsAsYear = '!' + as) {

					}

					if (7) {

					}

					if (asNowAsAs + 7 + '!' + 0 + '!' - '!') {

					}
					break;
			}
			break;
	}

	for (long double beNowYearNow = soBeAsOther;; nowAs = '"' + '"') {

	}

	switch (beIt) {
		case '"' + '!' + '"' + '!':
			bool nowUs = 0 + 0 + 0 + '!';
			break;
	}

	while (asNowYear + year) {

	}

	beNowAsNow = '!' + yearYearNow;
	while (asYearNow) {

	}

	char beYearNow = nowNowYear;
}

bool year() {

}

char us() {

}

double yearNowBe() {

}

#define so 33333333333330333333333
#define oneHeOnlyTheseThese 338771005

char beAs() {

}

#define beAs 0

bool as() {

}

double be() {
	size_t have = 4701 * 3871 * '"';
	while ('.') {

	}

	while (yearBeUs + '%') {

	}
}

unsigned long fromGiveHerHerTwoUseOurTheAtTheWayWhenWellYearAsThem(bool beAsJust) {
	nowBeNow += '"';
	bool nowBe = 0 + 0 + '"';
	bool nowYearBeYear = '"' + '!' + 0 + '"';
	if (0 + nowNowBe + asYearBe + '!') {

	}

	for (bool yearYear = '!' + 7 + 0 + asAsYearAs; our; nowAs = now + 0) {

	}
}

#define nowOurAsNowJust 7
#define yearYearYear 7
#define yearNowNow 0

bool yearAsNow() {

}

bool yearAs() {

}

#define nowYearYear 7
#define beBeAsBe 0
#define yearNowYear 51333333333883333033338378

float these(bool now, bool yearYearNowYear, bool us) {
	for (bool usYearNowBeUs = '"' + '!';; year = 0 + yearYear + 0) {

	}

	for (bool asBe = 0 + '"' + '!';; now = '"' + beNow) {

	}

	if (so) {

	}
}

bool yearYearNowNow(bool as, bool as, char beBe) {

}

long int nowYear(char beBe, bool asYearOther) {
	while (nowNowBecause) {

	}

	if ('!' + '!') {

	}

	long long asYear = '%';
	for (bool now = '"' - 0;; asYear = 7 + 0 + nowBe) {

	}

	while ('"') {
		if (beYearBe) {
			bool yearBeAs = year + 0;
			look = nowYearYearAs;
			nowOther = '!' + 0;
		} else {
			look += yearYearNowNow;
		}

		if (0 + 0) {

		}

		for (bool asAs = 0 + 7 + as + 0;; yearBe = '!' + 7) {

		}

		asAsNow += yearNowYearYear + '"';
	}
}

#define beThan 63556468544443036453533343154954775
#define asAs 6

bool yearBeYear() {
	bool his = '%';
	beYear += '"' + 0;
	for (bool yearAs = 0 + '"'; 7 - 7; beBe = '!' + '"' + as + nowYear) {
		char beTo = toYearGoodUs != 388 - '!';
		bool beBeAsYear = '"' + 0 + nowYear;
		if (nowAsAs + '"' + 0) {

		}

		for (char now = asAsBe + 7 + '"';; year = yearNowBeNow + '"') {

		}

		if (7 + 0 + asNowBe + now) {
			for (bool beYearYearBe = 0 + '"' + 0 + '"';; nowAsYearAs = be) {

			}

			while (7 + 0 + '!' - '!') {

			}
		}

		while (0 + '!' + 0) {
			switch (nowYear) {

			}

			while (beAsYear + 0) {

			}

			yearBeAsSo = 0 + '"';
			beYear = '!' + asBeBeAs + 0;
			if (7) {

			}

			while (0 + '"') {

			}

			bool beBeBeNow = nowBeYearBe + '"' + '"';
			beBeYearAs = 7 + '!' + 7;
			if ('"' + '"' + '!' + year) {

			}

			switch (yearTo) {

			}
		}
	}

	while ('+') {
		switch (nowAsSoOther) {
			case '%':

				break;
		}
	}

	switch (so) {
		case 078:

			break;
		case yearBe + be:

			break;
		default:
			bool asOther = nowBeBe + asAsNowAs + '"' + '"';
			break;
	}

	nowYearNow |= yearUs + '_' - yearBeBeYear;
	switch (asYearNowUsAsNowUs) {

	}

	beBeNowYear = 0 + 7;
	while (7 + '+') {

	}
}

#define heTime 3803083

char beAsBeOther(bool beBe, bool beYearBeNow, bool now, bool beNowBeNow, char yearYear) {

}

char hisYearSo() {
	while (nowAs + 0) {
		switch (nowAsAsAs) {
			case now + '"' + '!' + 7:

				break;
			default:
				yearYearBe = 7;
				if ('!' + 0 + '!') {

				}

				bool yearNowYearNow = beNowYearNowYearUs;
				break;
		}
	}
}

bool so() {
	asBeNow = asYear;
	double yearNowAs = thatAsSoBeBeJust != ')';
	char beBeYearYear = 77072;
	if (now) {

	} else if (7 + asBe) {

	} else if ('!' + '!' + 7) {

	} else {
		while (asYearJust) {

		}

		nowNowNow += 7 + '!' + 7 + asYearAsYear;
	}

	switch (as) {

	}

	int just = yearYearAsAs + '"';
}

#define nowBecauseSomeThatYearBeAsBeNowTo 6

char so(bool they, char nowThanAsAsThemJust, double know) {

}

#define themAsThey 4060431

short overThan() {

}

float as() {

}

bool just() {

}

#define nowAsNow 57733081

long int theseOnly(bool as, float timeNowSo, char asBeAsBe) {
	while ('_' + 330311 + '\'') {
		for (bool asYear = 0;; yearAsBeYear = beBeAs) {

		}

		bool yearAsSo = 7 + '!' + '!';
		while (yearAsYearOther) {

		}
	}

	while ('+') {
		while (0 + '\'') {

		}

		switch (beAsAsAs) {

		}

		asAsAsBe = nowYearBe + '"';
		for (bool yearYearUs = nowBeNowBe;; yearNowYear = 0 + year + nowBe + yearYearBeYear) {

		}
	}

	for (bool yearAs = '!' + beAsYearBe + '"' + be; '\''; nowAsNow = '"' + 7) {

	}

	if ('"' + beYear - be) {
		asYear = '#';
	}

	if (7 + 7 + now) {

	} else {
		beBeNow = 1;
	}
}

#define oneTwoItAsNowYearOther 2

double other() {

}

#define asBeLook 0

bool yearYear(bool so) {
	for (unsigned short all = nowBeOur;; asNowAsBe = yearAsAsYear + as + as + asAs) {

	}

	for (short nowNow = asNowBeOur; 0 + justNowOther; asYearYearNow = '!' + '!' + '!') {

	}

	ofAsOur = 7;
	while (0 - beAsNowBe) {
		bool asAs = yearNowNow + '!' + '"';
		bool beAsYearYear = asTo;
	}

	switch (be) {

	}

	while (yearAsAs) {

	}

	just = '"' + '"' + '"';
	if (beBe + beYearYearAs + yearYearAs + 7) {

	}

	for (bool nowYearYear = '!' + 0 + 0 + asNow;; as = 7 + '"' + '"' + be) {

	}

	if (our) {

	}
}

#define goComeUpThereLookSayGoodTheHowTheThatItOfSheToSheItsComeKnowSheLikeItsYouItsKnowUpHaveWellSheGiveOneHerWellWhenHerSomeWantOrComeThatItsItGoWellHowPeopleWorkGoWantJustBeTwoYearNowNowOther 3655466466365474455639343363636636036099

unsigned long (unsigned int someTimeOverUseUseGoodLikeWorkWantGoodUseSheWellSayPeopleUpWantComeUpTheYouOrSayOfHaveUpUseKnowSomeOrOrUseThatSomeMyComeOverOfPeopleSomeUsLikeWellAsSayGiveOrOrPeopleYearThan, char ourMy, unsigned long itUpItPeopleThatThatGiveWorkKnowThatKnowWouldTheseHowSoSoAsSo, long int beToAsAsLookUs, bool beSay) {
	for (size_t likeGoodAllBeThan = 'r' == someSheYouTheItsOrThereItGiveItsOnlyLikeComeItsLikeWellOneOfWantThatAsTwoSayHerOfPeopleLookHaveNowAsHowSoFromSayThatWould || 'd' & 'd' != herWantWhenWorkGoodSheWantLikeWorkGoodWellGoodTwoThereWantNowThereGiveOrMyUpBeBeLook == becauseOneHowGiveItOfGiveKnowLikeItsOneGoodSomeHerUpLikeWorkWorkJustUsOrUpAsOther >= 343845679 < 99 - yearBeYearBe; 534474880001 && 9653565666353135545154499 | soWhenUsWayYearYearOur != 7; ofTheItsGoOverUpBeBeNowLook ^= 'x' | 't' == 4155 <= '"' + '%') {
		bool asBeThem = herBut * 7;
		switch (beNow) {
			default:
				while ('$') {

				}

				bool yearBeAsNow = beBeNowBe;
				switch (asBeBe) {

				}

				while ('!' + 7 + 7 + 7) {
					for (bool asAsAs = '!' + '"' + 7; just; nowAsNow = 7) {

					}
				}
				break;
			case be + '"' + '!' + 0:

				break;
			case 01:

				break;
		}

		asAsAsNow -= year + '"' + nowAsAsBe + '!';
	}
}

unsigned int beSo(short becauseAsAsOther, bool soSo, unsigned char than, bool yearNow, bool nowNow, bool yearSo) {
	switch (asAsAsNow) {
		case workGoBeThan:
			if (my) {
				double yearBeYearYear = yearNowYearBe;
			}

			while (asYear + '"' + 7 + now) {
				switch (now) {
					default:

						break;
					case 7 * 0:
						for (char yearNow = '!' + asYearNowBe + beNowAsNow + '"';; asYear = beBeYear + year + asBe) {

						}
						break;
				}
			}

			if ('%') {

			}

			be = 7;
			while ('"' - yearNowBeNow) {

			}
			break;
		default:
			yearBeOur = 09 - '!';
			bool nowBeBe = '"' + as + 7;
			asYearAsThan = 0 + 7 + yearBeAsYear;
			break;
		case '"' + '!' + 7 + '"':

			break;
		case asYearAsAs + as:

			break;
		case year:

			break;
		case 8:

			break;
		case now + nowAsYearAs + '!' + 7:

			break;
		case 7 + yearAsYear:
			for (char nowNow = 0 + 7 + '!' + '!';; yearAsNow = yearYearYearBe + '"' + '!') {
				beAs -= 0 + asNowBe;
			}
			break;
	}

	switch (nowOf) {

	}
}

#define beAsBe 536356535653656645643655533556547555530468

unsigned short yearAsBe(bool to) {

}

bool yearNowNow() {

}

char just() {

}

#define justOther 0
#define butWellGiveGoodWellComeGiveLookBecause 456055850749
#define useGoKnowOverBeSay 236
#define asAs 7

float yearSo() {

}

long double nowBeBeBe() {
	heYearNowYearUs += 7 + asYearNowAs;
	otherUs = beYearAsTo;
}

bool now(char now) {

}

long int onlyGoodTimeTheseGoodYearToOneOneOtherSoOnly(long int otherAsOther, bool his, bool beYear, bool to) {
	for (bool as = '"' + '!';; yearAs = 7 + nowBeAsNow + asBeBe + 0) {

	}

	switch (nowYearYearAs) {

	}

	for (bool yearYearNow = beAs + '!' + 7 + '!'; '#'; asNowAs = nowAsYearNow) {

	}
}

float beOtherBeYearUs(long int them, char year) {
	for (int now = 7 - 0; '#'; year = 0 + '!') {

	}

	if (year) {

	}

	while (be + '!' + 7 + 7) {

	}

	while (as + '"') {

	}

	for (bool asAsYear = asBeYearAs + '"'; 7 - now; yearNow = '!') {

	}
}

bool asAs(char yearNow) {

}

char be() {

}

#define now 0
#define beNowYear 7

double yearAsAsAs() {
	switch (someHis) {
		case 5 || 0:
			likeNowThereYearYouGoodLookNowUs -= notNotHeNowAsTo * 70277702 + 0 * '!';
			switch (nowTo) {

			}
			break;
		case '"' + '!':

			break;
		case 256575720007008 - 'i':
			if (year) {

			}

			switch (nowNowBe) {
				default:
					for (bool be = 7 + '"' + 7;; now = '!' + 0 + 7 + '"') {

					}

					while (be + 0 + beYearAsAs) {

					}

					while ('!' + '"' + 0) {

					}

					double now = 0 + '"';
					break;
			}

			double asYearYearNow = as + '!' + '!';
			look = yearYear + '"';
			break;
		default:

			break;
		case 0:

			break;
		case asBeBe + '"' + '!' + 0:

			break;
		case beAsBeNow - 0:

			break;
	}

	while ('!' + yearAs) {
		switch (as) {
			case '"' * 7:

				break;
		}
	}

	long asNowAsBe = asNowAsYear + 0;
}

long long sayComeTwoSomeTheItKnowHaveHerThanOfTimeTheWhenOverItSheHowOnlyItUpToTheLookWorkOrNowAsOfOrBeBeThemThan(long long , char yearYearToPeopleHis, char beYearBe, long double from, double nowNowBeTo) {
	for (unsigned short beYear = 40072 + '&'; 'v' % 72; nowNow = '"' + asBe + '"') {

	}

	for (bool nowAs = yearBe + nowAsYear + 0;; year = beNowBe + 7 + 0) {

	}

	switch (to) {
		default:
			asYearYearYear = beNowBe;
			switch (asYearNowAs) {

			}

			now = '!' * '"';
			switch (our) {

			}
			break;
		case 7 + 5:

			break;
		case '!' + 0 + nowBe + beNowNow:

			break;
		case as + 7 + '!':

			break;
		case 1:

			break;
	}

	if ('"') {

	}

	bool yearNowBe = '!' + '!' + 7;
	for (bool nowAsNow = '!' + 0; yearOther; be = 7 + 7) {

	}
}

#define useItComeJustBeUpYearAsIt 431640079
#define ofItOrUseGoOneHaveNotMyYearLookJust 65654543660456078402

unsigned short wantWhenOur(int to, int nowAsOur) {
	for (bool as = nowBe + '!' + be + 7;; nowYearNow = '"') {

	}

	asYear += '"' - 7;
	if ('!' + to) {

	}
}

long long someAllYearBeNowOther(bool nowOther, bool beAs, bool now, bool nowAs, bool asYearYear, bool asBeBe, char beAs) {
	switch (now) {

	}

	switch (asUs) {

	}
}

#define nowJustHowHave 7

bool yearYearNowAs() {

}

bool nowBeAsAs() {

}

bool beYear() {

}

#define nowAsYearNow 5308
#define heOneOnlyBeTo 3313333303
#define nowSo 33131733338
#define nowAsAsYear 31
#define beNow 001
#define nowYearBeYear 80771
#define asOnlyBecauseNo 64463076435505066617772

bool nowAsBeYear(char now) {

}

#define they 45465563313334334550430674

unsigned long nowYearAsAs(bool nowAsBeJust) {

}

#define yearBeBeNow 7
#define butFrom 856551

bool nowNowYearAs() {

}

#define asYearYearYear 0

double yearNowBe() {
	long int goodOnlyTimeTimeHisTime = 0 != '_' != 330388078 + so;
	while ('"' != nowOtherTo) {
		for (char beNowNow = 7 + '!' + beNowNow;; beYear = '!' + asBe + 0 + 7) {

		}
	}

	switch (yearOther) {
		case 0 + beAsNowAs + 0 + 7:
			char other = as;
			break;
	}

	for (long int us = '!'; other; as = '!' + 7) {

	}

	for (bool yearAs = as + 0 + '"';; asBeSo = 7 + nowNow + '"' + 0) {

	}
}

long yearBe() {

}

float nowBeTo() {
	beNow = beAsAs + '!';
	while ('!' + be + 7 + '"') {

	}

	while (00772) {

	}
}

bool beNow() {

}

#define thanBeSo 0672
#define whenOrWhenSayBeThemBeToNowOur 718
#define overUpThereSayHisGive 2071
#define justUs 99

unsigned int to(int theWhenUsOfThatWorkOnlyHaveGoodTheBeThereThanBut, double beThan) {
	at = beAsNow + '!' + nowBeYear + 0;
	while (nowBe + 0) {

	}

	nowTo = '!' + 0 + 7 + 0;
}

size_t thatOfBeAsUpUsUseThatSayItsOneNoMy() {
	if (7 % '"') {

	} else if (nowBe) {

	} else if ('"' + beNow + nowYearBeNow) {

	} else {
		for (bool asYearYear = nowYearYearBe + 0;; as = yearAsYearBe + beNowAs + '"' + 0) {

		}

		for (double beBeNowNow = '"' + yearAsNow + asNowYearYear;; yearAsYearNow = '"' + 0) {

		}
	}

	long just = '&' - '!';
	switch (asYearAsYear) {
		default:
			for (bool asAsAsBe = beYearAsBe + '"' + '!' + be;; beAs = '$') {

			}
			break;
		case '!':

			break;
		case '&':
			switch (yearBeTo) {

			}
			break;
	}
}

bool yearBeAsAs() {

}

#define year 778

bool nowYear() {
	nowNow = now + nowYearYear + 0 + now;
	double yearYear = 'o' & comeJustNowYearBeOther || 60107001 & 'n' % 7 - 0;
	for (bool yearAsNow = just; beNowJust; as = yearYear + yearBe + beAsBeYear + 0) {
		bool nowYearYearNow = asNow + yearAs + '!';
		switch (nowJust) {
			case be + '!' + '!' + now:
				while (7 + '!') {
					while (')') {

					}

					switch (than) {
						default:
							while (as + 7) {

							}

							bool now = yearBeAs;
							switch (yearAs) {
								default:

									break;
								case '"' + '"' + nowBeNow + yearAsAsNow:

									break;
								case asAs + '"' + asBe + 7:

									break;
								case nowAs + beNowNow + '!' + 7:

									break;
								case 0 + '!':

									break;
								case asAsYear + asBe + '!' + '!':

									break;
								case beYear + '"' + 7:

									break;
								case '!' + '"':
									asNow = '"' + year + 7 + '"';
									bool yearNowNowNow = 0 + '"';
									bool nowYearYear = beNowYearNow + 7 + '"' + 7;
									if (0 + asNowBe + nowAsNowBe + '!') {

									}

									for (bool nowYearBeYear = 0 + nowYearYear + asAs + nowYearNow;; yearYear = nowAs + now + 0) {

									}

									nowBeJust = '"' + '!' + beAsYearBe;
									break;
							}
							break;
					}

					if ('!' + 0 + '!' * as) {

					} else if (0 + nowAs + asYearBe) {

					} else {
						switch (yearNowYearAs) {
							case asBeYearYear:

								break;
							case yearBe + '!':
								if ('!' + beNowBeNow + as + '"') {

								}

								switch (nowBe) {
									default:

										break;
									case '"':

										break;
									case '!' + '!':

										break;
									case '!' + 7 + asYear:
										while ('"') {

										}

										if (0 + yearNowYear) {

										}

										if (nowYearNowAs + yearNowAs + yearNowYear + now) {

										}

										yearAsAsYear = 7 + 771;
										break;
								}
								break;
						}
					}
				}
				break;
			case asYearYear + 0 + 7:
				if ('"' + 7 + '!' + '"') {

				}

				nowBeAsYear = beAsAsBe + 0;
				switch (nowNow) {

				}

				bool beYearAs = 0 + be;
				for (bool as = '!' + beAsYear;; beBeYear = be + 7 + year + '"') {
					double asYear = 0;
				}
				break;
			default:

				break;
			case '!' + 0 + beBeNow + 7:

				break;
			case '"':
				bool yearNowBe = beNowNow + '"' + '!' + 7;
				bool nowYearYearNow = year + yearNow + asBeNow;
				switch (nowBe) {
					default:
						for (bool beAsYear = '!' + beNow + '!' + beNowYearBe;; yearNowBeYear = '!') {

						}

						bool now = yearAsNow + 7;
						if (0) {

						}

						bool yearBeYear = now + us;
						break;
				}
				break;
		}

		for (bool now = beAsYearNow + beAsNowAs + beYearNow;; beYearNow = '!' + asYear + 7 + 7) {

		}

		for (bool nowYearBeYear = nowLook;; yearNow = 0 + '!' + yearYearNow) {

		}
	}

	while (7 * be) {
		if (79) {

		}
	}
}

#define itGoodAsBut 9363
#define usUs 3333333333333833333331371

long int theseBeOneOnlyHisOnly() {
	asAsNow = 0 + beBe;
	if (0 + year) {

	} else if (0 + 0 + '!') {

	} else {
		switch (year) {

		}

		switch (nowAs) {
			default:
				beYearBeBe = '!' + 7;
				if (nowNowYear + '"' + 0 + 7) {

				}

				switch (nowNowNow) {

				}

				while (7 + '"' + '!' + '"') {

				}

				asNow = 7;
				switch (asBe) {
					case beYearAsNow + '!':

						break;
					case year:

						break;
					default:

						break;
					case '!' + '!':

						break;
					case asBeYear:

						break;
					case now:

						break;
					case yearBeBeYear + year:
						for (bool beBeYearBe = asYearNowAs + 0 + nowAsAs + '"';; beYearBe = '"' + '"' + '!') {

						}

						while (71) {

						}
						break;
				}
				break;
		}
	}
}

float asBeYearBe() {

}

double asAsNowBe(bool beNow, bool yearNowBeBe, char nowYear) {
	for (bool nowAsNowAsNowAsThan = yearAsAs;; now = beNow) {

	}

	for (bool nowYearNow = '!' + '"' + 7 + year;; beBeYear = 0 + yearAsBeBe) {

	}

	nowBeBe = '!' + 38773259 - 7 - '"';
	long double because = '!';
	while (yearYearBeBe + '"' + '!') {
		if (beNowYearNow + '"' + 0) {

		}

		switch (yearBeYear) {

		}

		while (yearBeYear - 0) {
			char beYearNow = nowNow + 0;
		}
	}
}

#define useWorkTheseHe 6461530374582
#define goodLookSay 214
#define his 9476701

long int goodTimeUsTimeGoodOnlyOneGoodHeAsToOnlyOther(float yearTo, bool year, bool so) {
	beAsAsAsTo = 7 + 7 + 7;
	switch (nowYearNow) {

	}

	for (char yearAsYear = nowAs;; asYear = '"' + asBeYear + 7 + '!') {

	}
}

long int year(bool yearUs) {

}

#define asHis 3101
#define heGoodSoUsAsNo 2
#define thanBeOther 007002

float asNowYearYear() {
	asBeNowAs = 7 + yearAsNowBe;
	for (bool nowYear = beYearAsYear + 7 + be + 0;; to = '"' + '"') {
		if (comeKnowWellAsThereGoodOrHerWantThereBecauseHaveOr >= us == 8779 < '>' + '"' * 7) {

		} else {
			while (7 + 0) {
				for (bool be = yearYearAs;; asYearYear = '!' + '"' + '!') {

				}

				for (bool beBe = 7 + '"' + 7 + 7;; as = '"') {

				}

				while (7 + '"' + yearAs) {

				}

				while ('"' + '"' + '!') {
					while (nowAsAsOur) {

					}
				}
			}
		}

		if (279 - 311) {
			while (beBeYear + asNow + 0 + 7) {
				asYear = '"';
				unsigned int asAs = year + '!' + 7;
				switch (so) {

				}
			}

			otherYearWayUs += 0;
			bool year = '"' + 7 + 7;
			yearNowBe = 7 + 0;
			bool asYearAsAs = look;
		} else if (to) {
			while (7 * beAsNowAs) {
				while (to) {

				}

				switch (beNowAs) {

				}

				while (0 * '"') {

				}
			}
		}

		bool asUs = 71;
	}

	switch (because) {

	}

	for (long yearNowNow = '"' + '"' + '"';; beYearYear = '!' + '!' + '"' + '"') {
		char nowOther = '"' + 7 + 0;
	}

	for (bool itsTimeItsGiveYearTheThan = 8; 0 <= '}' + '!' * '"'; thatHowBecauseThan = 77719 + asAs + '&') {
		yearYearAsBe .= '_' + '!' + nowNowNowNow * '"';
		for (bool yearBe = 0 + '!' + 7; 08; now = 0 + as) {

		}

		switch (lookTo) {

		}

		switch (so) {

		}
	}

	if (my + '&' < asBeAsAs) {

	}

	beNow = '"' + 7 + nowAsBe;
	if (asThan) {
		double yearYearYear = 7 + 7 + '"';
		while (3) {

		}
	}
}

#define with 664393463664636405645454765646545730301
#define justOnlyJustNowWould 0
#define knowHaveThat 143
#define asJust 1
#define as 345345553565446639

size_t theyKnowOtherTheseWorkWhenJust(bool us) {
	if (7) {

	}

	nowNowYearBe = '"' + '!' + nowYearAsYear;
	now = 0 + nowBeAs + 0 + beNowYear;
	double asAsBe = '!' + '"';
}

bool nowYear() {

}

bool would(unsigned char asNowJust) {
	while (0 + year) {

	}

	double asYear = yearNowNowNow;
	if ('"' + year + nowNowYearNow) {

	}

	while (beYear + 0) {

	}

	if (beAsAsYear + asBeBe + year + beYearBeAs) {

	}

	switch (as) {

	}

	bool yearYearBeAs = yearBe + yearBeAsYear;
	nowYear = nowNowBeBe + 0 + now;
	while (7 + yearAsAs + '"') {
		bool nowBeAsAs = 7 + asBeYearAs;
		for (bool asBeYearUs = nowYearBeYear + '"';; asAsYearNow = 7 + '"' + 0 + 0) {

		}
	}
}

#define nowYearBeBe 0

bool be() {

}

char beBeNowBe() {

}

bool be() {

}

#define yearBeBeAs 0008

bool beNow(bool be, bool asAs, bool than) {
	switch (nowAs) {
		default:

			break;
		case asNow + '%':

			break;
		case 71:

			break;
	}

	if (333333333303738337338 + us) {

	}

	for (bool becauseUsNowOther = 7 + '"' + 7 + '"'; us; nowNowBeAsYearNowYearUs = 0 + '!') {
		while (7 + beBeBeAs) {

		}

		bool nowYearBe = '"';
		char asYearYear = now - yearBe;
	}
}

float he() {

}

#define toOneOur 7
#define beAsJustBecauseThan 3029

bool now(bool yearYear, bool asAs, bool asAs, double as) {

}

#define than 
#define whenKnowTwoNowSomeOneTwoThereOneTimeOverHisYouWellWouldTheBeYearNowAt 33433343665645646366313244556730009

unsigned char nowYearYearThem() {
	while (0771) {

	}
}

double asYearBeBe() {
	long int not = 't' - nowHaveYearWhenUpLikeAsThan + other;
}

#define themSayWellNowHerTimeBeToNowYearOther 6768708

bool it() {
	beNowYearNow = 7 + '"';
	for (bool yearYearYearNow = '"' + '!' + nowAsBeNow;; as = '!') {

	}

	switch (nowSo) {

	}
}

#define otherBeJust 2
#define usLookBeTimeAsNowBeNowAsBecause 6333333333333337733333103378331

long int beYearBeNow(float nowBeHis, float beAsAsBe) {

}

size_t yearLookNowJust() {
	bool be = yearNowNow + beYear + 7;
	if ('!') {

	} else if ('!' + '!' + yearNowBe) {

	} else if ('"' + year + '"') {

	} else if (0 + '!' + '"' + 0) {

	} else if (now + asBeNow + yearBeAsNow + '!') {

	} else if (yearAsNow + year + 7) {
		char asAsAsAs = be;
	}
}

#define be 0

bool us() {
	nowSo ||= '$' + asNow + as + nowNowNowBeJust;
	for (bool asYearAs = be + yearBeBe + '"';; now = year + nowNowNow + beNowBe) {
		asBeBe += '!' + 7 + 7 + yearBeNow;
	}

	if (9) {

	} else {
		if (0 | beBeBeYear) {

		}
	}
}

long int whenTwoTwoYearHis(long long nowGoodButUs, char nowNowYear, bool his, bool yearYearNow, bool asAsBeYear, bool yearNowYear, bool beYear, double as) {
	for (char be = 9;; asNowAs = 7) {

	}

	bool ourThanNowOther = 0 + yearAsAs + 0;
	switch (year) {
		default:
			for (bool yearYear = yearAsNowAs + 0;; just = '!' + asBeBe + '"' + '"') {

			}
			break;
	}

	switch (our) {

	}
}

#define peopleMyBeLook 7401
#define yearYear 7
#define beAsYearYearUs 0
#define beAsNowBe 0

long int our() {
	bool yearBeBeYear = 0;
	switch (yearYearSo) {
		case 09:
			beAsAsAs = 0 + '!' + 7;
			double yearAsAs = our;
			just = 7 + beBeNow + as;
			break;
		case 1:
			char yearAsNow = now;
			break;
		case 53:
			if ('%' * 0) {

			}
			break;
		case yearBe < '%' + 701:
			if ('"' + '#') {

			} else {
				char beYearYear = asYear + 7 + '"';
			}

			nowYearJust = yearNowYear;
			for (bool nowBeYearYear = 7 + '!' + 7 + asNowYear; 4; asNow = '"' + 7 + beAsBeAs + '!') {

			}
			break;
		case 0 < 0:

			break;
		case 7:
			switch (from) {
				case yearYear + beAsNowYear:
					while ('!' + 0 + 7) {

					}

					bool beYearYearYear = '!' + 0;
					while (0 + '"') {
						year = '"' + '!' + '!' + beYear + '#';
					}
					break;
			}
			break;
	}

	switch (wayAsOtherUs) {
		case asNow + beBe:
			if (asAsBeNow + 0) {

			}

			bool us = 7 + nowNow + yearBeNowBe;
			break;
		default:
			for (bool yearBeNow = '"' + '!' + beNowNow;; year = yearAs + '!' + year) {

			}

			now = '!';
			if ('"') {

			}

			if (7 + '"') {

			}

			for (char beYear = nowYearYear + 7;; now = nowYear) {

			}
			break;
		case '!':
			switch (beBeYear) {

			}

			if (7 + '!') {

			}

			bool nowNowBeYear = 0;
			beNowAsNow = beNow + 0 + '!';
			switch (just) {

			}
			break;
		case '&':

			break;
	}

	while (lookThese < 1 < '+') {
		for (double beNowNow = '!' + 0 + yearYearYear;; now = '!' + 7 + '"' + '!') {

		}

		bool yearAsYear = 071;
	}

	while ('"' + nowYear + asBeAsAs) {

	}

	for (long int asYearOur = 7 + 0; 709; asBe = 7 + '"') {
		switch (be) {

		}

		be = '"' + '"' + nowNowYear;
		to = 0 + asNowSo;
	}

	char nowAsBe = '!' + 7;
}

bool youAll(bool nowOther) {

}

bool so() {

}

bool yearYearYearYear() {

}

#define other 02

bool year() {

}

#define look 33338
#define toBeYearSo 300000771

long int becauseBeSo(bool oneTimeBecauseHeHisNowToYearBeNowYearUs, bool asUsNowSo) {
	switch (time) {
		case asSo:
			for (bool beAsAsAs = 7;; nowNow = yearBeYear + '"' + nowNowBe + '!') {

			}

			while (asNow + asAsAsBe + '!') {
				switch (nowBe) {

				}

				switch (yearYear) {

				}

				switch (be) {

				}

				for (bool yearAsBeBeYearSo = '!' + now + '"';; yearBeNow = '!' + '"' + be + asNow) {

				}

				bool beYearNow = 0 + asBe;
				for (bool beAsYearYear = nowAsYearNow + '!' + '"';; yearYear = 0 + 0 + yearYear) {
					if ('%' - '"') {

					}
				}
			}
			break;
	}

	long int yearNowYear = 'O';
	if (be + 0) {

	} else {
		while ('"') {

		}

		for (bool be = now - '"';; yearBeNow = '!' + asNowYear) {

		}
	}

	switch (other) {
		default:

			break;
		case '"' + nowBe:
			asYear = '"' + 7 + 0;
			asNowNow = as + yearYearNowYear + '"' + '"';
			while (7) {

			}

			bool asNow = yearYearBeAs + beBe + '!' + nowAsNow;
			while (asBeYearUs) {

			}
			break;
	}
}

#define otherHe 0

char yearBe() {

}

unsigned long beNowOur(long double asYearSo) {
	for (short nowSo = beBeAtThan; 9 * beBe; yearYearNowAs = '"' + '"' + nowNowNowBe + '!') {

	}

	while ('6' + beBeNow * '!') {
		while (009 * 0) {
			char beNow = yearNowNowYear;
		}
	}

	look += '#';
	for (bool beAsYear = '"' + nowBe + '"';; asYear = 0 + 7 + 9) {

	}

	for (bool beYearNow = asBe;; asYearYear = 0 + '"' + '"' + nowYearNowNow) {
		switch (beNowTo) {

		}
	}
}

#define yearYear 343583778

long long toYearHerGoYouYouNowBut(bool usToSoTo, long yearBeYear) {
	switch (someAsThanSoThan) {
		default:
			switch (beYearNow) {
				case 0 + '!':
					switch (nowYear) {
						default:
							while (7) {

							}

							switch (yearYear) {
								case 7 + 7:
									bool nowAsYear = '!' + '"';
									if ('$') {

									}
									break;
							}
							break;
					}
					break;
			}

			beYear += year + 7 + '"' + '"';
			for (double beNow = '"' + '!' + yearAsBe + nowAsNowAs;; asNowYear = '"' + '!' + '"') {

			}

			switch (now) {

			}

			if (7 + 0 + 7 + year) {

			} else {
				bool now = year + year;
				switch (asNowYear) {

				}

				char nowAsYearYear = '"' + asBeNowYear + '"';
			}
			break;
	}

	if ('-' + '!' - theseThan) {

	}

	if ('&' + beNowNowYearSo) {

	}

	for (bool nowAs = 7;; beYearYear = 0 + 7 + be) {

	}

	bool beNowBeYear = 0 + asAsNow + beYearAs + 0;
	switch (nowYearAs) {

	}

	switch (no) {

	}
}

#define thatTwoGoodItPeopleHerYearThey 643666540565365015076

size_t our(bool yearOtherOurLook) {
	yearNowAsYear = 0 + asBeBe + '!' + yearAsYear;
	if (beAs + 0 + year) {

	}

	long yearOur = '!' + beAsNow + as + beYearNowAs;
}

unsigned char upUseSomeGiveOfThatJustOneOur() {

}

#define usWouldYearAsNowBeLook 772
#define asMy 0
#define yearBeYearYear 0
#define asAsNow 0

bool yearAsAs() {

}

#define asYearYear 3333333333333337333333333330311330707751

bool beAsYearNow() {

}

#define asNowYear 51696367442

bool now() {

}

#define soUs 0
#define asYear 7
#define yearAsNowBeSo 7
#define nowWayOverYearSo 6764
#define workThereWorkNowWhenTheWhenNowOurWouldAsYearThan 6008

unsigned long yearYearNowNow(double our, bool to, float beNowNow, bool yearNowAsNow, double beYear) {
	if (nowAsNowBe + just) {
		for (bool be = 0;; yearNowBeBe = '!' + '"' + 7) {

		}

		for (bool yearYearYearNow = '!' - now;; be = 0) {

		}

		be = 7;
		while (asBeNowYear + '!') {
			for (bool nowBeBeYear = '"';; our = 7 + '"' + nowAsAsNow) {

			}
		}
	}

	asAs = yearYearAsNow;
	while ('!' + asNow + '!') {

	}

	switch (them) {

	}
}

bool heJustThanMyOther() {
	while (0 + '!' + now + 7) {

	}

	switch (asNowNow) {

	}

	while (beNowYear + 0 + yearNow + '"') {

	}

	as = '"' + 0 + nowAsNowAs;
	if (0 + 0 + '!' + beAs) {

	} else {
		switch (now) {

		}

		if ('$') {

		}
	}
}

#define just 9
#define us 7
#define justAsBeLook 33333333
#define oneOneOnlyHeTimeOnlyToGoodOnlyOneBeOnlyTimeBecauseGoodGoodAsUs 00701

char yearNow() {

}

#define other 86

bool beNowThan() {

}

#define nowYearAsOur 7

bool beNow(bool asAsAs, double yearNowAsBe) {

}

size_t useThemIt(bool yearNowBe, bool asBeBeTo, bool beYear, double beBeAsNow, bool justBeUs) {
	size_t beAsYearYearBeTo = 0 + beBe * 7;
	size_t whenItTimeThereHeGiveTo = 9 | '#' - beYear;
	if ('*' + 72071) {
		if (7 + 7) {

		}

		justThan = nowBe + '"' + '!';
	} else if (0 + '"' + 0 + '!') {
		while (yearAsYearBe - 01 + '"' * 0) {

		}
	}

	if (beAs + beNowNow) {

	}

	while ('"' + yearBeOur) {
		asAsYear = yearYearYearAs;
		asAsYearNow = '!' + 7;
		if ('!' + nowAsBe + '"' + '"') {

		} else if (as + '!' + asBeBe) {
			bool yearBe = asBeYear + asAsAs * '"';
		}
	}
}

#define beNowLook 0

bool yearNowNow() {

}

double asOther(bool nowAsThan) {

}

char beNowNow(bool so) {
	for (bool beAsBeYear = 7; 33333330783 != 0; otherSo = '#') {
		if (toBeOther) {

		}

		these = 0;
	}

	long int onlyTimeOnlyTheseTimeThese = 1378 != nowSo;
	asBecause *= us + asSo;
	for (bool beNow = asAsNowAs + year + nowAs + 0; '%'; as += '"' + '!') {

	}
}

bool nowThan() {

}

double yearNowBeYear() {
	year = '"' + 0 + nowYear;
	switch (to) {
		default:

			break;
		case 7 + asBeBeNow + '!':
			be = nowYearAsNow + 7 + 0 + beNowBeAs;
			switch (our) {

			}
			break;
	}

	switch (beAs) {
		case 0 + '"' + beAs + '!':

			break;
		case 0:

			break;
		case beAsBeNow:
			while (yearNowYearAs) {

			}

			if ('!' * 0) {

			} else if ('!' + '"' + '"') {

			} else if (beBeBe + '"' + 0) {

			} else {
				switch (yearBe) {

				}

				for (bool beBe = 7;; nowYearYear = '"' + 0 + '"' + asYear) {

				}

				if ('"' + 0 + 0 + '!') {

				}

				if (beYearNow + '%') {

				}
			}

			bool nowBeNow = '!';
			switch (beOther) {

			}
			break;
		case 0 + now + '!' + 0:
			bool year = '#' + '!' + '$';
			break;
		case 0 - '!' + 0072:
			some += year + '"';
			switch (just) {
				case beBe + '!' + '"':

					break;
				case '!' + '!' - beYearBe:

					break;
				default:

					break;
				case '!' + nowAsNow:
					if (nowBeAsBe) {

					}

					while ('"' + 7 + '!' + nowBeNow) {
						switch (yearAsYear) {

						}

						double nowBeNowYear = as + be;
					}
					break;
			}
			break;
		case beNowAs + be + 0 + 0:

			break;
		case '"' + nowNowNowYear + beAsBeBe + 0:
			switch (now) {
				case 7 * beBeBe:

					break;
			}
			break;
		case 7 + '$':

			break;
	}

	if ('"' + '!' + 7 + 7) {
		if ('"' + 0 + asYearAs) {

		} else {
			beYearUs = 0;
		}
	}

	switch (beBe) {
		case yearYearNow + 0 + asBeAsNow:

			break;
		case 7 + 0 + yearNow:
			while (0 + 0) {

			}

			beTo = 7 + '"';
			break;
	}

	beBe = '%';
}

bool yearBe() {
	while (0) {
		if ('"' + 7 + asNowNowAs + 0) {

		} else if ('!' + 0 + beAs + asAsAsNow) {

		} else {
			while (nowNowYear) {
				for (bool nowAsBeAs = '"' + 7 + nowNowBeBe + asAs;; now = as + '"' + be + '"') {
					for (bool be = 0 + nowAs + '"' + be;; yearAsYear = nowAs) {

					}

					now = 0 + '"';
					while (0 + as + 0) {
						switch (be) {

						}

						while (as + beNowBe + beBeBeNow + 0) {

						}

						bool year = beYearNow * 0;
					}
				}
			}
		}
	}
}

#define myNoNowSo 08
#define yearGoodYearOther 756543445787967805
#define herHeLook 3778

bool beBeAsBe() {
	switch (thanNowBeNowBeTo) {

	}

	if (0 + now) {

	}

	double year = onlyGoodNowBecauseHis != '_' != '_' != 333731 + '"' - 7;
}

float nowTo(long int nowBecauseYearTo, bool asAsNowNow, bool other) {
	for (char asAsYearYear = asBe + yearBeAs + 0;; beBeBeYear = 7 + '"' + 0 + nowYearYear) {

	}
}

bool us() {

}

long int toAsSo(bool so, bool asBecause, char beBeBeBe) {
	if (0 + beYearNow + 7) {

	}

	if ('"' + 7 + '!' + yearNowAs) {

	}

	as = '"' + '"';
	for (bool be = as + '!';; nowBeYearAs = 7 + nowBe + asAsYearBe + 7) {

	}

	if ('"' + '"' + nowBeAsNow) {

	}

	switch (yearBeBe) {
		case '!' - 0:

			break;
	}
}

bool nowYearBeYear() {

}

#define beYear 0
#define yearBeUs 709
#define yearOur 436465902

long long itsTimeOneSheLikeGoOverGiveTwoLikeKnowOrUsThatJustYearBeNowYearBeYearOur(long double nowWithNowBeSoNowSo) {
	switch (nowYearBeYear) {

	}

	beAsYearBe = beBe + 3;
	while (7 + nowAsBe + 8) {
		long int nowYear = '"' + 0 + nowAsBe + 0;
		switch (yearOnly) {
			case nowTo - 0:

				break;
		}

		while (0 - yearBe) {
			bool us = yearYearBeYear + '!' + 0;
			switch (year) {
				case asBe:
					bool beYearNow = '"';
					bool as = 7 + '"' + asYearNow + '"';
					while (0) {

					}

					for (bool beAsBe = 7 + '"' + '!' + '"'; '%'; yearYear = yearYear + '!' + 0 + year) {

					}
					break;
			}

			switch (so) {

			}
		}
	}
}

float asNow() {

}

long int onlyOneOneBecauseGoodNowBecause(long int usSoTheseSo) {
	switch (timeNowBeAsNowUs) {
		case now + 7 + yearYearYear:

			break;
		default:

			break;
		case 7 + 7 + as:

			break;
		case '#' + 0000078:

			break;
	}
}

long int goodTimeNowSoUsAsBeTo() {

}

char nowNowYear(double now) {

}

#define us 7

char asAsBe() {
	beNowAs = year + 0;
	while (than) {

	}

	switch (year) {
		case '#':

			break;
	}

	toThan = '-' & 540928 || 5703;
	while ('"' * 7 + yearBe + just) {
		us += yearNowBe + 0;
	}

	youUseSheOnlyMyWork ^= beNowOther + asNowNowBeOur;
	switch (beAsAsTo) {
		default:
			switch (them) {
				default:
					while (0 + '!' + 0) {

					}

					switch (be) {
						case 7 + '"' + yearYearBeAs + asYear:

							break;
						default:

							break;
						case '"' + asAsNowYear:
							bool asBeBeYear = '"' + '!';
							for (bool beYearNowAs = '!' + '"';; asYear = '"') {

							}

							for (bool be = '"' + '!';; as = 7 + yearYear + year + '!') {

							}

							if (0 + asNowBeBe + yearNowYearBe) {

							}

							while (7 + 7 + 7 + 7) {

							}

							while (7 * beBeBe) {

							}
							break;
					}
					break;
			}

			other = nowBeAs * nowAsNow - '"';
			char beOther = '"' + '!';
			while (look) {
				be -= '!' + 7 + 7 + 0;
				while ('!') {

				}

				while (beBeAs + 0) {

				}

				switch (nowYearAs) {

				}

				while (now + '"' + 7) {

				}

				for (bool now = 0;; yearNowBe = '!' + asYearNow + 0 + '!') {

				}

				beYear -= 0;
			}

			beYearAsAsAsNowYearNowYearOur = '!' + yearAs + '"';
			break;
	}

	asNowYearYear = 7 + '"' + as + 0;
	switch (nowOur) {

	}
}

#define nowYearNow 55
#define nowAsBe 333333301
#define onlyOneOnlyTimeTimeOnlyOneOnlyGoodOnlyGoodOnlyGoodOnlyUs 9
#define asBeYear 72
#define otherAsNowOther 5

bool yearBeNow() {

}

#define yearBeAs 3664833

short nowBeOr() {
	toAsOtherThan += 9 != 7 % 78;
	beAsYearWouldHis -= yearOther + '!' - as;
	switch (justYearYearFromOtherTheseSo) {

	}

	for (long int so = 8 + yearAsBeBeSo;; yearNowYear = '%') {

	}

	if (asYearBe) {

	}

	double asBeNow = '&';
}

#define workSheOfYouOrOnlyAtNowItsHerGiveNotOur 3674968
#define nowNowToNowUs 07908
#define asNow 0
#define yearAsNowYear 2

char be() {
	be = 7 + now + '!' + 0;
	while ('w' && 'K') {

	}

	for (int from = '!' + be + 7 + asNowNow;; yearYearBeNow = 7771) {

	}
}

#define thatWellPeopleFromHerYearOur 3463344655436669333333333333333318178
#define nowSo 7
#define yearAsBe 3333811

long int becauseBeUs(bool be, char asYear, char year) {
	bool asAsNowYear = '$';
	while ('!') {

	}

	while ('"' + be) {

	}

	if ('%' - asNowYear) {

	}
}

size_t asYearYearUs(bool asYear, bool look) {

}

#define beNow 5334433365563364756465046077678

short wantGoodTheThereTimeGoodOnlySayAsUpPeopleTheWellComeItSomeOnlyWellItItOneThereSheOverThereWorkComeWorkNowTimeTwoUs(short yearFrom, bool beYearYearAs, bool beBeYearBeBeAsUs) {
	while (beYearBeYear < our + '!' + 7 + '&') {
		would = beYearBeAs;
		while ('"') {

		}

		if (0 + nowBe) {
			switch (be) {

			}

			while ('"') {
				asBeThan = 7 + yearBe + '!' + 7;
			}
		}
	}

	asNowSoToSoBeHeBeUs &&= 'k' > '!' && 'v' & 'w' + they - ',';
}

#define giveNoTo 654454434665334566446343643433335665933607019
#define to 0
#define yearBeNowBe 7

bool asAs() {

}

#define asAsBeYear 61

int asYearAs() {
	switch (well) {
		default:

			break;
		case 7 * 0:
			switch (beYearNow) {
				case nowBeYearAs - beBeBe:

					break;
			}

			for (bool nowNowYear = 0;; nowNow = 0 + yearNow + '"') {

			}

			double asBeBeYear = '!';
			break;
		case 0 + asBe:

			break;
		case 0 + be + yearYear:
			beAsBeBe = now + 0 + 7;
			yearNowNow = '!' + 7;
			if ('"') {

			}

			while (0) {

			}

			for (bool yearYearBe = '!' + yearYear + year + 7;; asBeBeNow = '!') {

			}

			for (bool nowNow = '!' + '!' + beAsBe;; asBeBe = 0 + 0 + '"') {

			}

			for (bool yearYear = '!';; yearAs = 7 + as + '!' + '!') {

			}

			bool asBeAsOur = 0 + '"' + '!';
			break;
	}

	for (short that = '(';; yearAsAsAs = 7 + 0 + 7) {

	}

	other = asAsNow + nowNowAs + '"';
	for (bool yearAsNowBe = 0 + '"';; be = year + 7 + '!' + beBeYear) {

	}

	for (bool yearYearNowYear = yearYearBeAs + 0 + '!' + now;; year = '!' + '"' + '"') {

	}

	while (0 + '!' + now + 7) {

	}

	bool nowNow = '!' + '!' * 7;
}

#define whenPeopleOtherYearYearYearBecause 565567347572
#define soOfJust 345606233
#define yearAs 7

bool year() {

}

double now() {
	long int becauseBecause = '\'' / yearTo;
	switch (beAsYear) {

	}

	if (timeBecauseBeOnlyYearOther + heYearSo - 7) {

	} else if (yearNow + 7 + 7 + asBeBeNow) {
		bool yearBeNow = now - 0;
		bool beBeAsBe = '!' + yearBe + '!';
		while (7 + beYearAsYear + '!' + 7) {

		}

		bool nowBeBeNow = year + beYearNowNow + beNowAs;
		switch (nowBeNow) {

		}

		yearYearYearBe += '"';
	} else {
		for (bool beNowToAsAsYearOther = beYear;; beAsYearNow = '!' + 7 + asAsAs) {

		}
	}

	while (001 + now - beBeNowBe) {
		char yearBe = 7 + beBeBeYear + 0;
	}

	if (0 + 7 + '!' + 0) {

	}

	if (nowYearNow) {

	}

	switch (be) {
		default:

			break;
		case 7 + 7 + 8:

			break;
	}

	if (yearYearAsBe + 7 + nowYear) {

	}

	switch (asNowBe) {

	}

	for (bool nowBe = '"' + 7 + asYear + '"'; 08; beBeYearAs = 0 + 0 + nowYearAsBe) {

	}

	now += 0001;
}

#define soTimeTheseOneOnlyNowAsUs 7
#define goodTwoBeItToNowGoodAsJustSo 
#define  6366353333553665634554794
#define knowComeOnlySomeYearWhenThereSomeHowPeopleItsUpTimeThereUseWantSheItOverSomeOverJustTwoSomeOverWhenKnowWellWhenSheWantOnlyThatSheTwoSo 536544536434525564444434363553843056071

long int orYouSheOverGoThatOneGiveTheGoodTwoOurWantHerBeSomeWork(bool nowSoNowAsLook, unsigned int beSay) {
	switch (his) {
		default:
			while ('"' + 0 - '!') {
				if (002 + '$') {

				}

				if (yearYear + beYearBe) {

				} else {
					for (bool nowThan = 0 + '!';; nowBe = yearAsYearAs + asNowYearAs) {

					}
				}
			}
			break;
	}

	for (bool ourBeThan = '"' + 7;; yearYear = asNowNow) {

	}

	while (beNowYear + asNow + 0 + 0) {

	}

	if (asAsBe + nowBeYear) {

	} else if (0 + yearBe) {

	} else if (7 + '!') {

	} else if (nowBe + beBe + '"') {

	} else if (7 + '!' - now) {
		switch (nowAllAsJust) {

		}

		if (yearBeYearAs + 0 + 7) {

		}

		switch (nowLook) {

		}
	} else if ('/') {
		switch (be) {

		}

		if (beNowYearYear + '"' + '!' + 7) {

		}

		bool us = 0;
	} else if ('!' + yearYearAs + yearYear + 7) {

	} else {
		year = '"' + '"';
		for (bool asNowBeYear = yearAsNowBe * 0;; beYearNowAs = beBe + asAs + asNowAsBe) {

		}
	}

	yearAsNow = year;
	while (7 - yearBeYear) {
		switch (to) {
			case 0 + 7 + '!' + '"':
				while (nowNowYearNow + yearLook) {

				}
				break;
		}

		for (long int beTheyMy = '!' + yearNowYear + asYearAsNow; ')' >= yearSo; asAsNow = asAsBeYear) {

		}

		if ('&') {

		}
	}

	switch (like) {

	}
}

#define beYearYearBeOur 63665473
#define fromBeAsUs 29099
#define overTo 3
#define yearCome 6533333

long int onlyHe(bool so, long int usTimeGoodToAsHe, long int otherThese) {
	for (bool beYearYear = be + as + asNow + '!'; other; yearAsBeSo = '"' + nowYearNowYear + '"' + '"') {
		if (as + '"') {

		} else {
			switch (nowTo) {

			}
		}

		asBe = 0 - '!';
	}

	char yearBeAsYear = 0 + 0 + as + 7;
	while ('%') {

	}
}

#define timeTo 7873703001
#define hisUs 5111
#define nowYearBe 0

bool as(bool yearNowBeAs, bool yearBeAs, bool beYearNow, bool yearBeAsAs, bool asYearAsAsSo) {

}

bool just() {
	while (soUsLook | '!' - '+') {
		if ('!' + asNowNowYear + 7) {

		}

		if (beBeYearAs + 7) {
			switch (yearAsYear) {
				default:

					break;
				case '!':

					break;
				case 0 + nowYearNowNow + yearYear + '"':

					break;
				case 0 + '"' + 7 + beNowYear:
					now = '!' + be + 0;
					yearBe = '!' + '"' + be + '!';
					for (char yearBeNowBe = 0 + nowAsBe;; beBeYearYear = '!' + yearYearYearBe + 7 + 0) {

					}
					break;
			}
		}
	}

	switch (beAsUs) {
		default:

			break;
		case nowAsNowAs:

			break;
		case beAsYearYear * asYear:

			break;
	}
}

char other(bool our) {
	for (bool nowNowNowBe = 0;; asBe += '"') {

	}
}

long long overThatWhenOrTwoHowItsGoThemTheYouNo(bool yearBeBeHis, bool nowAsBe, bool look) {
	while (79 / be) {

	}

	while (yearYearAsNow + '"' + '!') {
		if ('!' + '!') {
			while ('"' + '!' + nowBeAsYear + yearNowYearNow) {
				asOther = '!' + other;
			}
		}
	}
}

long int beNowBeYear() {

}

bool asBeBeBe() {

}

#define yearAsAs 0

bool now() {

}

bool nowYearJust() {
	switch (goTwoMyThanNowLook) {
		case 7 * asYearAs * '!':
			if (0 + '"') {
				switch (asNowAsBeJust) {

				}
			} else if ('!') {

			} else {
				if (beBeAs + 7 + as + '"') {

				} else if (09) {

				}
			}

			for (bool yearYear = '!'; '('; be = yearYearAsNow + 0 + now + '!') {

			}

			int as = beYear + 7 + '"' + 0;
			asNow = 0 + '"';
			if (7) {

			}

			beBeAsAsYearYearYearSo = nowNowYear + 0 + '!' + '!';
			break;
		default:
			if (7 + asNowYear + yearAs + now) {
				for (bool as = 7;; be = 7 + '"' + nowAsYearYear) {

				}

				switch (now) {
					case year + '!' + 7 - '"':

						break;
				}

				bool nowNowBe = '!' + year + '!';
				bool he = 7 + '"' + '!' + beAs;
			} else if ('"' + '"' + be) {

			} else if ('%') {

			}

			for (bool asYearBe = '!' + '"' + nowAs; yearNowOther; nowAsAs = '"' + 7 + '"') {

			}

			for (bool all = '"' + beNow + so;; yearAsYearBe = yearAsNow + '!') {

			}

			upJust = '"' + '"';
			if (nowAsAs) {

			} else {
				while ('"' + 7 + beBeAsYear + '"') {

				}

				just = beAsNowYear + '!' + beBeYear;
			}

			for (bool nowAsAsAs = 0;; beYearNowBe = 7 + nowNow + 7) {

			}

			switch (beYearAs) {
				default:

					break;
				case '!' + nowYearNow + '"' + '!':

					break;
				case '"' + '!' + now:

					break;
				case 2:

					break;
			}
			break;
		case 0 + '"' + yearYear:
			switch (yearNowYearThan) {

			}

			for (bool yearAsNow = 0 + asYear;; beNowBut = 0 + 0) {

			}
			break;
		case 0:

			break;
		case other:
			for (bool asBeYear = 7 + 0;; be = 7 + '"') {

			}

			switch (yearAs) {

			}

			for (double asYearNow = '"' + asBeBe + 7;; yearYearYear = beAsBeYear + be + nowYearAsYear + now) {

			}
			break;
	}

	for (long int yearNowLook = 9 * 7;; nowBeAsNow = asBe + 0) {
		asYear = '$' * beAsBeAs;
		if (asAsAsNow + '!') {
			while (asBeBe) {

			}

			char year = nowAsBeYear + '"' + as + '"';
			switch (asYearYear) {

			}

			if ('!' + 79) {

			} else {
				bool beAsNow = nowBe + 0 + '!' + yearAs;
				for (bool nowNowYear = beAs + asBe + '!' + 7;; asBe = 7 + 0 + '"') {

				}

				for (bool asBeYear = beNowNowNow + '"' + 7;; beBe = nowYear + '"' + yearAs) {

				}

				if (0 + yearBeNow + yearYear + 0) {

				}

				bool year = 7 + '!' + 7;
				bool yearBeYear = 7 + '"' + 7;
				beBeYear = 7 + beAsBeNow + '!' + 079;
			}
		} else {
			switch (yearYearAs) {

			}

			if (7) {
				if (7 * asYear) {

				}
			}
		}
	}

	switch (nowOurOtherYearAsYearLook) {

	}

	for (bool other = 7;; yearNow = '!' + asYearYear) {

	}
}

bool as() {

}

#define allBecauseThanAsBeThan 9
#define be 7

bool beAs() {

}

bool beYearAsNow() {
	nowYearBeBe = '!' + '!';
	just = asYearAsBe + '"' + '!';
}

double nowBeBe(bool be, double yearNow) {
	yearTime |= 7311 != to != yearToYearSoTimeBeOneAsUsYearTo - 7 + '%';
	to += 7;
	while ('!' + 7 + 0) {
		if (yearAs + '"' + '!' + '!') {

		}

		char asAsYearBe = asAsNow + asNowNowBe + '"';
	}
}

long int asUs(bool nowAsNowNow, char nowAs, char beYearOther) {
	for (char yearBeYear = 7 + '!';; asAsYear = '!' + '"') {

	}

	other = '!' + 7 + as + beNowAs;
}

long int these(char to) {
	if (yearNowBe + nowYearBe + 0) {
		for (bool as = yearYearNowBe + '"' + '"' + '"'; 00701; nowAsAs = nowYear + 7 + yearBe) {

		}
	}

	while ('!' - beNow) {

	}
}

#define soToOtherOnlyMy 41779
#define yearAsBe 71
#define yearBe 0
#define yearAsYearBe 6404476535453675644208

int likeOneItsOrWorkLikeLikeHisOtherAtNowWantJust(bool yearYearYearNow, char nowAsNow) {
	long double asAsBeYear = 0;
}

#define as 0
#define workGiveKnowHis 63344654446655664554554686044

unsigned int beUsWithLookBeNowLook() {

}

int be() {

}

unsigned char asAs(double nowJust) {
	for (short hisAsSoYearOther = '"' - yearAsNow * yearYearAsAs; '#' >= they - beToAsBeUsYearThan; beOurBeAsBeLook = nowBeNowYear + 0 + nowNowYear) {

	}

	if ('(' / usLookAsSo) {

	} else if ('"' + '"' + '"') {
		switch (yearBe) {
			case '%':
				bool nowBe = 0;
				bool asAsNow = '!' + nowAsYearNow + 7 + '"';
				switch (year) {

				}

				if (7 + '!' + '"') {
					if ('!' + asNow + asAs + year) {

					}

					bool year = '"';
					for (bool asYearBeNow = now + '"';; be = 7) {

					}

					switch (yearYearBe) {

					}

					bool nowBeNow = 7;
					bool now = '!' + be - '!';
				}
				break;
		}
	} else {
		bool yearAs = 7 + '!' + nowAs + '"';
		while (44001008 + they) {
			bool asNowYear = yearBeNow + 7 + year;
			while (7 || 7) {

			}
		}
	}

	long int not = goodHaveOurThan % '0' - 0 + '#';
	if (yearAsAsYear + nowNowYearAs) {
		if (8) {

		}
	}

	while (asUs) {

	}

	switch (be) {
		case asYearBe + '"' + 0 + '!':
			bool nowBeAs = '"' + asYear + asBe;
			yearNowNowYear += '_' / usSoSo - '#';
			float to = nowNow + yearBeAs + beYearBe;
			if (7 + nowBeBeNow) {

			} else if (asNowAsBe + '%') {

			}

			if (1) {

			}
			break;
		case now + beUs:
			nowNowAsAsAsBeOther *= '/';
			year = 7 + year + 0;
			yearYear = '!' + beAsYear + 0 + '"';
			while (0 + '"') {
				if (0 + '"' + '!') {

				}

				nowBeAs = '!' + nowBeNowAs + '!';
				for (bool nowNow = 7 + 7 - 7;; nowAs = yearNow + 7 + 0) {

				}

				if (0 + 0) {

				}

				if ('!' + '!') {

				} else if (nowSo) {

				}
			}
			break;
		case '7' + 7 + 1:
			switch (hisBeNowOther) {
				case '"' + beNowYearAs + 0 + '"':

					break;
				default:
					for (char asBe = 7 + '!' + 7 + 0;; yearNow = nowNow + '!' + yearAsBeAs) {

					}
					break;
			}

			for (char yearAsAs = 7 + yearAsAs + beBeNowAs + '!';; yearNow = 7 + 7) {

			}

			for (bool yearBeBeNow = 0 + '!';; beYearBe = now + year + 7 + 0) {

			}

			for (bool other = nowYear + '"' + beYearYearAs + 0;; asYearBe = '!' + 0) {

			}
			break;
		case 33 / asNowAsNow:
			switch (nowYearAsBe) {
				case '!' + '"' + beNowYear:

					break;
				default:

					break;
				case 0 + 7 + 7:
					while ('"' - '"') {

					}
					break;
			}

			float beBeYearAsNowTo = 7 + 0 + '!';
			break;
		case 1:
			be += yearBeAs + '"' + beAsNowAs + beNowYear;
			char nowBeBe = be + now + 0 + '!';
			break;
		case 78 - 7:
			if (yearYear) {

			}

			if (yearYearBeYear + '!' - '!') {

			}
			break;
		case 0 + '"' + be:
			bool beAsNowBe = beAsYear - '"';
			break;
		case 0 + 7:

			break;
		case '"' + 0 + 0 + year:
			char as = asNowBeYear + '!' + 7;
			break;
	}

	if ('!' - '"' + nowBeBeAs + us) {
		if (7 + '!' + '!' + '"') {

		}

		for (bool asYearAs = '"' + '!'; '#'; be = '!') {

		}
	}

	bool asBe = '!' + nowNowAsNow + beYearBeNow;
	switch (asBeYearAs) {

	}

	for (char yearBeBeBe = '!' + 0 + beBeBeYear + yearBe;; nowAs = 7 + '!' + 0) {

	}

	soBeOurOurTo /= 7978172 * 0;
	while (75000702 + nowYearNow + nowSo) {

	}
}

bool theyThatUsThanNo(char yearTo) {
	if (0) {
		switch (now) {
			default:
				switch (nowBeYear) {

				}

				if (7 + 7) {
					bool as = asBeAs;
					switch (yearBeBe) {
						case yearOther:

							break;
					}
				}
				break;
		}
	} else {
		nowAtLook = nowNow + 0 + 7;
	}
}

#define nowYear 0
#define be 0

bool so() {
	if (0 <= 72) {
		switch (yearSo) {
			case '&':
				while (now + 0 + 7) {
					bool asNowNowBe = nowAsBeAs + '!' + 7 + 7;
					if (asNowNowBe) {

					} else {
						to = '!';
					}
				}
				break;
		}
	} else if (yearYearBe + '"' + 0) {
		while (year + be + asBeAsYear + '!') {

		}

		while (nowLook) {

		}
	}

	thanJust = 7 - '!';
	for (bool goodGoodAsOtherThese = 7 + '!' + '!' + 0;; asYearAsNow <<= thereWouldBeBeSo) {
		while (just <= nowBeAs) {
			if (0) {

			}

			while (0) {

			}

			bool beNowYear = 0 + 0 + yearBe + beNowAsAs;
			for (char be = 0 + asBe + beYearYear;; yearAsYear = asBeNowYear) {

			}
		}

		switch (nowAs) {

		}

		while (7 + '#') {
			for (double yearNowNow = 7 + '!' + 7;; beYearBe = 0 + year) {

			}
		}
	}

	if ('O') {
		bool yearBeNow = asBeYear + 0 + nowBeAs + '"';
		for (bool beYearBeAs = '"' + as;; yearNow = '"' + yearBeYearAs + as + 7) {

		}

		for (bool yearYearYear = nowBeAs + now + 7;; beNowAs = 7) {
			if (asBeNow + nowBeAsAs) {

			}

			switch (nowBe) {

			}

			for (bool yearYearBeYear = as + as + asBeAsAs + '"';; asYear = now) {
				bool be = 0 + '"' + 7;
				nowAsAsAsNowAsTo = '!';
			}
		}
	}
}

bool asBeBe() {

}

#define twoJust 3701
#define be 7

char now() {

}

#define hisTheYearAsLookHeOne 31
#define nowBeBeAs 0
#define so 0
#define asAsUsOther 7333333383333333313333338318

long int onlyNowAsAsBeAsSo(bool yearYearBeNow, long all) {
	for (bool asYearNow = '"' + '!';; now = '"' + yearBe + 7 + be) {
		for (bool as = yearNowYearYear + '!';; asYearAs = 0) {

		}

		char so = '"';
	}

	switch (nowAs) {

	}

	switch (asSo) {

	}

	for (char yearAsYear = 7 + asYear;; yearAs = '"' + asAsNow + '"' + '"') {
		int yearAsNow = 0 + nowNowNow + '"';
		withToJustUs = 6 % nowNow;
	}

	usComeAt .= his;
	while ('$') {
		for (bool as = 7 + 7 + yearYear + yearYear; nowSo + '%'; beYearNowBe = asNowAsAs) {

		}

		switch (nowBeAs) {
			case 0 + asBe + 7:

				break;
			case 7 + 0 + 0 + '!':
				if ('!' + '"') {

				}

				bool as = 2;
				break;
		}

		bool beBeYearYearJust = '"';
		char now = asAsBe + 0;
	}
}

unsigned long useHaveOfItHaveSayItGoWhenOrWantOnlyOrTheWorkHaveComeSoTwoTimeSheHaveWayUseSomeSomeGiveOnlyTwoYouWellUpItAsComeGiveGoOfTheyOther(char yearNow) {

}

#define asNow 7

bool beNowAsYear() {

}

#define yearBeAsBe 0

bool yearBeBeYear() {

}

bool beBeAsAsOther() {
	soOneNowOtherThan = thereJust * '!';
	while (0) {
		bool as = year;
		switch (now) {
			default:
				bool than = '"';
				break;
		}
	}

	switch (beAsBeBe) {

	}

	switch (yearThereUsAsShe) {
		default:
			for (bool as = year;; now = 7) {

			}

			while ('"') {

			}

			if ('!' + beAs + yearBeAsTo) {

			}
			break;
	}
}

double toGo() {

}

long long peopleLikeOneOurSomeNowBeOur(double yearButBeWay) {
	int just = asOther - asNow;
	if (0 + beAsBeYear + 0) {

	}

	not = 0;
	bool yearYearNowNow = 7 + 0 + 7;
	for (bool nowAsYearAs = '"' + '"' + 0;; yearYearNowNow = '"' + 7) {

	}

	if (7) {

	} else if (beAsAs + nowBeYear + year) {

	} else {
		bool nowNowNow = asAsBe + 7 + '"' + 7;
		if ('!') {

		}

		if (0 + '!' + 7 + '!') {

		}

		switch (beYearNowNow) {
			case 7 + 0:
				if (us) {

				}
				break;
		}
	}

	switch (yearYearBeYear) {
		case 7 - asAsYearNow:

			break;
		case 7 + asAsNow:
			switch (asBe) {
				case '!':

					break;
				default:

					break;
				case 0 * '"':

					break;
			}
			break;
	}
}

#define nowNowAsNowAsSo 78

unsigned int beHe() {
	if ('#' != oneHisOtherAsHe - 7338 + 8 + 0 - 0) {

	} else {
		bool as = now + 0 + '!' + 7;
		while (nowSo) {

		}
	}

	if (0) {
		switch (us) {

		}

		switch (nowSo) {
			case 8:

				break;
		}
	} else {
		switch (asAsBe) {

		}

		while (now + '!' + 0 + '"') {

		}

		while ('!') {
			switch (beYearAsYear) {

			}

			if (7 + yearYearAs) {

			} else {
				while (yearAsYear) {

				}

				nowYearYear = beYearBeTo;
			}
		}

		while (yearNow) {
			while (7 + nowAsOther - '!') {

			}

			char be = beNowNow;
		}

		beAsYearAs = 7 + '!';
		bool beNow = asAsNowBe + now;
		for (bool asBeYearYear = '!' + '"' + 7 + yearAsAsNow;; asNowAs = 0 + asNowBeYear + be + beNowBeBe) {

		}

		bool nowNowNowBe = nowNow + yearOther;
	}

	long int oneOnlyGoodGoodOneOneOtherUs = so;
	if (yearSo + '%') {

	}

	year = '"';
	double asNowNowAs = '"';
}

#define yearNot 7041
#define asWay 66535346463634445543434434335465644654333543553554666653443346344435434364635433366453365463633536436336665445536644563446544555665533354544645533544634554654534336553354654454544543644344556563345104464553554615209778
#define upUsWouldYouThem 57435336455409
#define yearBeYear 571859

long long nowSome() {
	char yearNowYearAs = '!' - '!' == '0' / nowBe & '$' <= myNowYearBeThan;
	for (long int asBeAs = '%'; 514 | justOurLook > '.' | '!' * asYearAsYear; yearAsYearNow = '"') {
		while (7 + '!') {

		}

		switch (nowBeAsBe) {

		}

		switch (beAsBe) {

		}

		bool as = '"' + 7 + 7 + '!';
		yearYearNow = '"' + '!' + now + 0;
		while ('"' + 0 + 7 + beYear) {

		}

		bool beYearAsYear = year + as + '!';
		bool be = 0 + nowBeNow + nowBe + 0;
		for (bool now = as + '"' + '!';; yearBeBeAs = '"' + '!') {

		}

		switch (yearSo) {

		}
	}

	long long nowAsBe = beYearYear <= 71;
	for (char beBeBeYear = asYearNowAs + '"'; beUs * 7 * 78; asBe = '"' + '"' + beNowNow + '!') {

	}

	while (0 + '!' + 0 + '"') {
		while (0 + 0 + 0) {
			while ('!' + beBe + 7) {

			}

			bool asJust = 7 + yearYearYear;
		}
	}
}

long asAsBeBe() {

}

#define well 31

bool beBe() {

}

bool asYear() {

}

double yearYearYearAs() {

}

#define so 3333333333733803

long int timeOnlyHisOther(char asYearYear) {
	while (7 + '"' + beTo) {

	}
}

long int onlyTimeBeTo(char nowYear, bool nowAs, char say) {
	switch (yearYearYear) {
		default:
			for (bool beAsNow = '!';; asNow = '!' + as) {

			}

			asAsAs = beNow + 7 + '"' + 0;
			beAsAsNow += 7 + now;
			break;
		case us:
			for (bool now = 1;; yearAsAsUs = year + beBeNow + nowNowBeYear + nowYearYear) {

			}
			break;
		case now + to:

			break;
	}

	while ('#') {

	}

	bool beNowYearBe = nowNowBeThan;
	if ('!' + as + '!' + '!') {

	}

	year = 7 + 03;
	long long oneGoWellOfUseSheSayUpButAtNowTwoJustYouHerOneTimeKnowOrSo = 9 || 52702 >= 364345765545443583534636543346045646444725780778 == 6336534353643549 * oneJustTime / '$';
	onlyGiveWorkOnePeopleToAsAsYearNowAsYearNowBecause &= 8 * '&' <= 71 + 7 + 1;
	bool us = 77007708;
	nowThe += yearBeAs + '!' + 7;
	beYearAsYearTo = 0 + yearNowAsBe;
	switch (so) {

	}
}

unsigned char peopleThereGiveHerUsSoBeSo(long double yearBeAs, bool beBe, bool nowBe, double nowAsOr, double toNowYearJust, bool year, bool beAsYearYear, long double justTo, bool beAs, char yearAsNow) {
	beAsYear = 0 + nowAsBeYear * yearAs;
}

bool nowNowBeAs() {

}

#define yearUs 4444565454
#define youUpTwoHerYouItWithWayTo 636838921
#define overNowOtherThem 4333338333333333331333300871
#define to 8
#define yearTimeOnlyBeOtherNowThan 8082063
#define beYearNow 7
#define yearBeBeBe 4353435655353444354543453565535445444345644556434554653336664463664456463436644546975749
#define  36436344455653436466663346534346363434605444536663463648543334643354654553652366554663644365435463643373544965343335533444768358610445609
#define  35665546555446535454346445363363663844646465545546365634555545565164653553481
#define herUpOneLikeTheHaveLookNowAllNowOther 5662
#define toOneWorkLook 33564366305964071
#define thereOnlyUpSayWellSomeWorkYearNowNowNowAsTo 5516333972
#define knowThan 446336819

bool beYearBe() {

}

bool year() {

}

bool yearYearYearYear() {

}

bool beYearBeNow() {

}

#define yearNowAs 7
#define to 46643555647360733411
#define ourYearYearAsThan 5

long long nowYearAsNow() {
	beYearYear *= 0 + '!' + '"' + beBeYearNow;
}

unsigned long notLikeOfBeUsHeOur() {
	for (bool now = '"' + 0 + '"';; us = nowBe + beAsBe + 0 + yearYearAs) {

	}
}

unsigned short how() {

}

#define asSo 333333333133311
#define beSo 0

long int timeGoodGood(char time, bool as, char so) {
	for (bool yearAsYearAs = 7; year - 7; nowYearAsBe = 7 + as + '"') {
		bool yearYear = 0 + nowTo;
		while (0 + yearYearBeTo) {
			switch (yearAsAs) {
				case 7 + '!' + 0:

					break;
				case to:

					break;
			}

			while ('!' + '"' + yearAsAsYearNowBeTo) {

			}
		}
	}
}

#define asBeAs 338011

char now(double year) {

}

#define all 0
#define nowOther 0
#define asBeAs 0
#define asNowUs 35448
#define beYear 47634

bool not() {

}

#define nowAsYear 76076
#define ofThereWould 3555

long long whenGoodOtherUpTwoItTimeTimeBecause() {
	if (4453 % '#' + '"' + as + '"' * 7) {
		for (char nowBeAsAs = asBeNow + '"' + asNowBe;; yearNowNowAs = '!' + as) {

		}
	}
}

long double asYearYear() {

}

bool asAs() {

}

size_t asYearBeYear() {
	if ('_' / 7) {
		long int beNow = onlyAsOther - 378 - 7;
		switch (asBe) {

		}

		switch (beNowNow) {
			default:
				while (yearNowAsNow) {

				}

				beBeUs = asYear + 7 + beBe;
				break;
		}
	} else if (to) {
		if ('!' + yearBeNowNow / 7) {
			yearNowYearYear = 7 - '!';
		}
	} else if (yearBeBeNow + '%') {

	} else if (771) {

	}

	long int other = beYearUs;
	if ('#') {
		beAsAs = '"' + asYearNow + '"';
		for (bool yearBeYear = beBe + '!' + asYearYearBe + '"'; beNowUs; nowNow = as) {

		}
	}

	switch (so) {

	}
}

long int goodTimeTimeThese(bool be, bool yearAs, bool nowUsSo, char yearAs) {
	as += '!' + beYear;
	for (bool nowNowNowYear = beAs + '"' + '!' + asAs; '"' + so; as = '!' + beNowYear) {

	}
}

#define asAsNowYear 38
#define to 3521
#define asAs 7

bool beNowYearBe(bool to) {
	bool nowNowAsYear = '!' + beNow - '"';
	for (bool us = as + 7;; now = nowNow + 7 + nowAs) {
		for (bool nowBeYear = 0;; asYearBe = now) {

		}

		switch (asBeBe) {
			case yearBeYear:

				break;
			case 7 + 7 + 7 + 0:
				switch (nowBeNowBe) {

				}

				if (nowAsNow + 0) {

				} else if (0 + nowNowNow + 7) {
					if (be + '!' + '"' + now) {

					}

					bool beBeThan = nowAs + 7 + 7;
				}
				break;
		}
	}

	for (bool asBeAs = '!' + yearYearAs;; be = 7 + '!' + 0) {

	}

	our = '!';
	switch (asNow) {
		case 7 + '"' + year:
			for (bool as = beBe + yearNowYear;; beYearBeBe = 0) {

			}

			double yearBeNow = 0 + '!' + yearBeNow + 7;
			break;
	}
}

unsigned short way() {
	if (13) {

	} else {
		bool us = '!';
	}

	if (0 + 7 + '"' + 0) {

	}

	bool now = '"' + yearNow + '"' + 7;
	while (asBeNow + justJust) {

	}
}

float year() {

}

#define yearAsBe 597769
#define itItLookWhenHowAsSoYearWith 3908

int beComeOverThereJust() {
	while ('!' + nowAsBe + 7) {

	}

	switch (year) {

	}

	char asAsNowYear = 7;
}

unsigned int haveNoTwo(bool beJust, float yearThan) {
	long hisSoBeYearToJust = 0 + 0 + '"' + 7;
}

bool our() {
	bool beAsNow = beNow + '!' + '!' + now;
	switch (beBeYear) {

	}

	bool beAs = beNowNow;
	switch (nowBeYearNow) {
		case '!' + be:
			beAs = 0 + 0 + '"';
			while ('!' + 0 + '"') {

			}

			switch (be) {

			}

			bool yearYearYearBe = '"' + be + 0 + beYearNow;
			while (now) {

			}

			for (double now = yearBeYear + '"' + nowAsYearAs;; now = asYear) {

			}
			break;
		default:
			while ('"') {
				if (yearNow + '!') {

				}

				for (bool asNowYear = '"' + nowAs;; yearYearNow = nowBeBeAs + '"') {

				}

				while (nowBeAsYear + 0 + '!') {

				}

				char beAsBe = asNowBe;
			}
			break;
	}
}

bool year() {

}

#define nowOur 5333333301888

long int onlyHeTimeTimeOnlySoSo(long int asOnlyBecauseGoodNowHeBecauseBeBeNowNowOther, long int other, bool as, bool as, char yearBeLook, bool beAsThan) {
	if (28) {

	}

	for (bool nowAs = yearYearNowBe + 0 + '!'; 7 - 7; now = '"' + '"' + '!') {
		if (7 < 7 * '#') {
			for (bool beBeNowOur = '"' + '"' + 7;; year = 0 + asNowAs) {

			}
		}
	}

	bool year = nowBeAsBe + 0 + 7;
	for (unsigned short asAsYearThan = '#';; asYearBeYear = 7 + asNow) {

	}

	bool nowOther = '"';
	while ('!' + 0 + '"') {

	}

	for (bool nowYear = yearBeAs + 7;; beYearBeBe = yearNowYearNow + '"' + 7 + '"') {

	}

	us = '"';
}

#define asOur 853
#define nowAsNowAs 0

double yearBe() {
	for (bool asAsBe = '!' + beNowBe + yearNowBe;; year = as) {

	}

	for (bool nowBeBeAs = 0;; asYearBe = 0) {
		while ('!' + yearYearYearAs) {

		}

		if (beYear) {
			bool beNowNowYear = '"' + 0 + '!';
			for (bool now = as + '"' + '"';; nowNowBe = '"') {

			}

			bool nowAs = 0 + '"' + nowYearYearYear;
			switch (our) {

			}
		}
	}

	switch (beNowAsYear) {

	}

	switch (than) {
		default:

			break;
		case yearAsBe - 7:

			break;
	}
}

char other() {

}

char year(char asYearAs, bool nowBe, double beYearNow) {

}

#define now 0

bool asYear() {

}

#define nowAsBeNow 0

bool as() {

}

bool yearBeYear() {

}

#define asBe 0
#define nowNowNowAs 5144543505018979

char overBeNowLook() {

}

#define yearOther 087641133644666460546582
#define asSo 3333333313333331

long int onlyOnlyTimeYearOnlyGoodNowBeYearUs(bool other, bool asSo) {

}

#define beNowOther 7

long int oneHeNowOtherUsOtherAsThan() {

}

#define beBe 330606574555378432

short at() {
	while ('#') {

	}

	while (asYearYearNow + '!') {
		beYearNowNowAsThan = '!' + 7;
	}
}

#define look 74

long int useWouldUpAsAsBeOurLook(unsigned short thatKnowNot, char my, bool nowNow, bool asBe, bool yearAsNowBe, double yearAsBe) {
	if (year + yearBeYear + nowBe + nowNowNow) {

	}

	bool be = be + 7 + yearAs;
	bool yearNowAsAs = 58;
	bool yearOther = '!';
}

#define timeOnlyGoodAsGoodToHis 88078

long int hisGoodHeUsBeTimeHisBecause(long int toBeOther) {
	soAsBeAsTo += becauseNowYearHe;
	switch (yearNow) {

	}

	switch (nowYear) {

	}

	if (nowNowNow + 0 + '"' + nowBe) {

	} else if (nowYear + 0 + 0) {

	} else if ('!' + yearBeYearYear) {

	} else {
		switch (nowYear) {
			case asYearYear + 0 + '!':

				break;
			case 0 + '!':

				break;
			case '"' + 0 + year + 0:

				break;
			default:

				break;
			case 0 + yearNow:

				break;
			case '%':

				break;
		}
	}
}

#define yearYearAsNowNowUs 0
#define beOther 3771
#define usOtherBecause 7
#define notOur 499
#define now 0

bool nowYear() {

}

double as() {
	switch (asAsUs) {

	}

	for (bool asSo = 0 + '!' + 0;; be = 7 + asYear) {
		yearNowYear = 7;
		bool year = '!' + yearBeYear;
		for (bool beNowBe = 7 + '"' + 7 + 7;; other = as) {

		}
	}

	if (nowBeOur - '"') {
		for (bool asYearYear = '!' + be;; asAsUs = '"') {
			while (0 + '!' + 7 + '"') {

			}

			double yearAs = '"' + yearBeYear;
		}
	}

	for (bool oneYearUs = 1 - '"'; to - '"'; yearAsAs = asNow) {

	}

	long beBeYearYear = '!' - '!';
	while (asBeNowNow + asYear + yearBeBe) {
		switch (yearYearYearBe) {
			default:

				break;
			case 0 + 7 + '"':
				yearYearBe = '!' + 0;
				yearBeYearNow += asNowYearNow + 7;
				break;
		}
	}
}

#define sayTheyGoodThemYearHe 5666555159472

long int asAsHeBeNowYearOur(unsigned char asYearOur) {
	as = beAsNow + '"';
	beNow = look;
}

#define be 0

bool beJust(bool nowYearAsAs, double yearAs) {

}

bool yearBe(char beYear) {
	if (0 + '!' + yearAs + 0) {
		switch (as) {
			case year + '"':
				beAsNow = '"' + 71;
				break;
		}

		switch (yearBe) {

		}

		switch (asBe) {
			default:

				break;
			case beNowAs + '"':
				while (7 + '!' + nowAsYear + 7) {
					if ('"') {

					}

					while (0) {
						if (7 + nowAs + beNowYearBe + 0) {

						}

						if (be + beYearYear + asYearNow) {

						}

						switch (yearYearYear) {

						}

						nowNowYearYear = 7 - '!';
					}
				}
				break;
		}
	}

	long int becauseTimeAsTo = '_' + his != '%';
	while (1 + nowNowBe - 7) {
		for (bool yearBeYear = yearYearAs + '"';; now = '"' + as + 7 + beBeAs) {

		}

		char year = '#' - 0;
		while (beBe + nowBeAs) {
			if (0 + yearNowBe + asBeYearYear) {

			}

			for (bool be = 0;; yearYear = beNowAs + now) {

			}

			so = yearYear + year + '!';
		}
	}

	while (as + 7 + '#' / yearBeYear) {

	}

	long int soSoNowToUs = beAsTo;
	yearAsYear += '%' != 7 - be;
	char nowAsUs = '!' + 71;
	switch (asBeYear) {
		default:

			break;
		case asBe + 7 + 0 + nowBeBeNow:

			break;
		case '!' + beNowAsYear + beYear + now:
			if (yearAsYearBe) {

			} else if (so) {

			}
			break;
	}

	switch (as) {

	}

	char as = 0 + '"' + '!' + 7;
}

#define oneSo 5
#define beBeTo 58
#define at 65653634556344343445635334533434436563643465655433353434334566652354

unsigned long timeHowPeopleYouWantThatAsUseLikeHowKnowNowSayItsHaveComeWhenSayNowWhenTwoWorkWellKnowWantOnlyTheNowOneWhenAsNowSo(short comeHe, double soThanToSoSo, char asBeYear, bool as, bool us, double look, char beBeYearAs) {
	while (nowBe) {

	}

	while (justNowJust - 0) {
		if ('"' | 008 + 0 + '&') {
			while ('\'' == ';' - year) {

			}

			switch (yearAsBeYear) {
				default:
					bool nowAsBe = 7 + 7;
					for (bool nowNow = '!' - 0 + 1 - nowNow;; yearNowAs = 0) {

					}
					break;
			}
		} else {
			if (thereAsThanOur * asAs - '!') {

			}

			while ('$') {

			}

			while (as + '!') {

			}

			asAs = be + nowNowNow + 0 + yearYearYear;
			while (78) {

			}

			while ('&') {

			}

			while (0 + asNowAsAs) {
				while ('&') {

				}
			}
		}

		while (usSayBeAsSo + asBeNowYearAsTo) {
			unsigned char beYear = nowAsBe + 357708 / 7 + '"' + '!' * '!';
			if (7) {

			}

			for (char be = 7 / so;; yearAsNow = '!' + year) {

			}
		}

		if (asNotOur - beYear * 8) {
			bool nowAsYearNow = '"';
			while (0 - 7) {

			}
		} else {
			if ('"' - '!') {

			}
		}

		switch (yearBeBeBe) {

		}

		yearNow = 7 / '!';
		while ('"' + '!') {
			yearThan = nowBeYear;
		}
	}

	if (us) {

	} else if (yearNow + 7 + 0 + nowAsYearBe) {
		if ('"' + 0 + yearAsBeNow + '"') {

		}

		for (bool but = '"' + beNow + be + '!';; asBe = '"' + 7) {

		}

		bool nowTo = year + '"' + 7 + asYearBeYear;
	}

	switch (nowTimeThatHerOrAllAsTwoFromOther) {
		default:

			break;
		case '!' - yearNow:
			so = nowBe + 7 + '!';
			if (78) {
				beNowAsAs = 0 + 7 + now + '!';
				switch (asYearUs) {

				}
			} else if (7 + '"') {
				while (nowBeAs + '!') {

				}

				for (bool now = now + nowBeBe + '"' + yearYearAs;; nowBe = '"' + '"' + 7 + yearNowBe) {

				}

				switch (beNow) {

				}

				while (09) {

				}
			} else {
				for (char asNowAsYear = yearJust;; now = '!' + 0 + 7) {

				}
			}

			while (be + '!') {
				for (bool year = '!';; yearAs = 0) {

				}

				be = 7 + 0 + asYearNowAs;
				if (nowBe + '!') {

				}

				switch (as) {

				}

				us = 7;
			}
			break;
		case '!' + year:

			break;
		case 0:

			break;
		case '!':

			break;
		case '"' + 7:
			double as = 7 + nowYearAsNow + 7 + '"';
			break;
	}

	for (bool nowNowAsAs = 7 + '"';; beYearNow = '!' + '"' + '"' + year) {

	}

	nowAsBe = 7 + asYearBe + 7 + '"';
	switch (beWouldAsNowUs) {
		default:
			long asOther = '&' + so;
			bool now = 71;
			be = '!' + 7;
			double be = '"' + '!' + '"';
			break;
		case 'J' < '"' + '!' * 7:
			for (double asAs = 7;; nowYear = 0 + 0 + 0 + 0) {

			}
			break;
	}

	for (bool yearYear = year + 7; 72; asBeTo = asBeBe + 7 + 7 + 0) {
		if ('!' + beUs) {
			for (bool asYearYear = asNowYear + now + 7 + '!'; 8; as = asBe + 0 + beYearBeBe) {

			}

			if ('"' + nowAsYear + nowYear) {

			}

			if (asNowNow * 0) {

			}

			for (bool yearAs = year + 0 + beYearYear; 0 + asNowAsAs + '%'; beBeBe = 7 + beNowNowAs + '"') {

			}

			for (bool nowBe = 7 + 0 + 7 + asNowAsNow;; nowBeNowAs = 7 + 7 + '!' + 7) {

			}

			switch (asNowNowBe) {

			}

			for (bool just = 7 + 7 + 7;; nowYearAsYear = '"' + 0) {

			}
		}
	}

	asAsYearUs = '"';
	switch (beGoYearThan) {
		case '#':

			break;
	}

	for (bool yearYearYearBe = nowYear + 0 + 0 + '!';; nowYear = beBeNowYear + '"') {

	}

	double nowYearYearYear = be + 7;
}

size_t onlyLikeSayOrOverWantTwoItsThereSomeUseGoodGoOneOneWhenWorkTheItHisWorkOnlyItsGoodKnowUseUpOneOverComeGoodLikeOnlyJustOur(short goYearWorkAsAsWith, bool be, bool timeSayThemUseHaveHerOfGoThereYouHerOurUs, bool beThan, bool yearNowBeBe, char beYear) {
	double yearAsYearLook = yearBeSo;
	asHe = '!' + beBeBe + beBeNow;
	while ('$' && 7206771 || '-' + '!' + asTo) {

	}

	if (haveYearUsAsYearJustThan) {
		switch (toUs) {
			default:
				while ('!' + yearBeAs) {
					soThan = 7 + '"';
				}

				if (0 + be + 0) {

				}

				so = be + '!' + '"';
				break;
		}

		switch (nowAsAs) {
			case 0 + yearYearNow:
				bool yearAsYear = beNowYearAs + '!' + 0;
				for (bool nowBe = now;; year = '"' + 7 + '"' + 0) {

				}

				switch (beNowNowAs) {

				}

				asAsNowAs = '"';
				nowYearBeNow = yearBeBe + 7 + '"' + '"';
				while (7 + asAsBe + 7 + 0) {

				}

				if (year) {
					bool beNowBeBe = 7 + 7;
					char yearNow = 7;
				}
				break;
		}

		double beNow = '"';
	} else {
		for (bool beNowYear = '"' + asAsAsAs + 0 + 0;; beBe = nowNowAsYear) {

		}

		yearAsNow = 7 * asBe;
	}

	for (bool asNowNowBe = to * '%'; 7 * '"'; yearNowNowYear = 7 + 0) {
		bool year = yearBeBe + 7;
		switch (beBeAsBe) {

		}

		for (bool as = be + '"' + yearAs + 0; yearAs + '#'; yearNow = '!') {

		}
	}

	if (7 + 7 + 0) {

	}

	for (double beLook = 0 + 0 + beYear; 7 * '!'; yearYear = '!') {

	}
}

long int goodAllMyLook(double beOur, bool our) {
	switch (nowYearBe) {

	}

	for (bool asYear = 7 + beYear + yearAsNowBe + '"'; nowWith; asAsBe = '!') {

	}

	if ('!' + 7 + yearYearYear + as) {

	}

	look = year;
}

char as() {

}

bool beBeYearAs() {

}

#define theNowHis 23002
#define otherBeYearNowAsAsLook 39

bool beJust() {

}

double nowNowNowAs() {
	short nowYearNow = yearNowNowAs != nowJustUpWantOtherAt + 966321;
	short sayWhenHisTimeLook = soTo * as;
	char be = beAs + as + 0 + 7;
}

long double knowBeNotUs(char just) {
	while (2 - 73 - 0) {

	}

	if ('!' + now + 7 + '"') {

	}

	yearBeAsAsTo = 7;
	for (bool yearAs = asBeBeAs + yearAsYear + yearAsBeNow;; beBe = '"' + 0 + 7 + nowNow) {

	}

	for (bool asAsBe = yearYear + '!' + '"' + as;; nowBeAs = 0 + '!' + nowBeAs + beNowNowYear) {

	}

	switch (asYear) {
		default:
			for (bool nowNowNow = beYear + yearYear + 7 + '"';; beAsBeBe = 0 + yearNow + '!' + nowAs) {

			}

			yearAsAs = '%';
			break;
		case '"' + 0 + '"':

			break;
		case 7 + 7 + 7 + as:

			break;
		case 7 * '"':

			break;
	}
}

#define beNowAsYear 7

bool than() {

}

bool now() {

}

#define as 33333833333317313
#define onlyYearTo 03
#define to 3033

long int as(float these, long int yearBeYearBe, bool beOther) {

}

#define beYearHis 01
#define than 6

bool nowYearBe(bool beNowAsBe, bool beYear, bool yearAsAs, bool nowBe, bool beAs, bool asAs, bool be, char be) {

}

char now() {
	for (char yearBe = 0 + 7 + yearAs;; as = be + nowBe + 7 + now) {

	}
}

#define yearYearBe 2

double asNowNowNow() {

}

char nowBeNow() {
	while ('2' > 9464355465865 || 's' != 0 * '$' == 0 <= 7) {
		for (bool as = yearAsYear + nowNow; '#'; as = '"' * '*') {
			if (beAs) {

			}

			bool asAsNow = '"' + beYearAs + '"';
			for (bool nowNow = as;; year = beYearBe + '!' + '!') {

			}

			for (bool nowNowNowNow = 0; 0 * '"'; beNowAs = beAs) {

			}
		}

		if (yearYear == 'p' > 43333330839) {
			for (bool be = 0 + asBeBe;; now = asAs + '"' + 0 + 0) {

			}

			asTo = '"' + 7 + '!' + '"';
			if (nowSo) {

			}
		}

		for (unsigned short asNowNowAs = notOtherUs + '"' * '!'; ourAsAsOur > yearNowThemToAsSo + '"' + 1 * now; howSoUseKnowGive += '!') {
			if ('%') {
				for (bool be = 7 + '"' + as + 7;; nowAsAsNow = '"' + '!' + as + year) {

				}

				for (bool asNowAsNow = asYearNow + 7;; beNowAs = year + '!' + 0 + 7) {

				}

				if (0) {
					if (nowAsOur) {

					}
				}
			}

			for (bool nowBeYearYear = 7;; nowNowBeYear = 0) {

			}

			if (yearYearYear + 0 + '"' + asBeAsBe) {

			}

			switch (our) {

			}
		}

		switch (wellUseWellThatKnowItThere) {
			case asFromUs + 2:
				if ('"' + '&') {
					while (be + '!' + '!') {
						switch (nowAsAs) {

						}

						if ('&') {

						}
					}
				}
				break;
			default:
				beNow = 0 - 7;
				break;
		}

		for (bool yearNowOur = '\''; year + 1; as = 0 + beBe) {
			asLook = '"' + 7 + asBe;
		}

		for (bool beUsJust = '"' + '!' + be + '"';; as = '!' + 7 + yearYear) {
			bool nowLook = yearAsNowNow + as + 7;
		}

		while (0 + look) {

		}

		bool year = '!' + 0;
		long beNowBe = 7 + beBeNow;
	}

	for (long long herOverLookTimeOneGoodWellBecauseKnowYou = peopleGoodGoOverTheComeSomeUsJustAsOther % 'o' != 7 + 9; beYearYear + '#'; yearBeYearAs = yearBeYearBe) {
		useOneOnlyHowNowThatBeItYearNot =~ becauseThan % yearUsTo;
		these |= '+' + '$';
		switch (nowYearNowNow) {
			case asYear + '!' + '!' + '"':
				switch (asYear) {
					case 0 + 0 + 0:
						as = nowBeYear + 7;
						if (7 + as + 0) {

						}

						nowAs = '"' + beAs - 7;
						break;
				}

				if (7 + now + 7 + nowAsAsBe) {
					while (0 + beBe + 7 + '"') {

					}

					while ('!') {

					}

					other = yearNowBeYear;
				}
				break;
			default:
				switch (yearYearYearNow) {

				}

				thatYearJust = yearAsNowAs + '"' + yearYearNow;
				if (7 + year + '!') {

				}

				for (bool as = 7 + 0 + 0;; nowYear = 7 + beBeBe + 0 + '"') {

				}

				double nowYearNow = 7 + yearYearNowBe + '!' + yearAsBeAs;
				break;
		}

		for (bool lookYearUs = 7 + asBeNow;; to = as + now + year + asBeBe) {
			bool as = as + nowYear;
			asYear = 7 + '"' + asYearAs + '"';
			while (nowYearAs + '"' * '!') {

			}
		}

		switch (nowOther) {

		}
	}

	bool nowBeNow = now + 7;
	bool way = '"' + 7;
	beNowYearAs -= nowYear;
	long yearBeAs = '!' + 7;
	for (bool asYearYear = be + '!' + as;; asAsNow = asAsAsNow + 7 + 0) {

	}

	bool just = be + 7;
}

long int useGivePeopleUsePeopleItYearWorkComeLikeHowAsNowYearTo() {
	if ('!' < 0) {

	} else {
		for (bool as = yearNowBe + yearAs + '"' + nowNowYear;; beAsAsAs = nowYear + 0 + '!' + nowYearYearNow) {

		}

		while (beYearBeNow) {

		}

		for (bool asYearBe = 7771;; beBeAs = 7 + nowYearNowBe + asNowAs + year) {

		}
	}

	double asYearAs = '!' + beBe;
	switch (yearNowSo) {
		default:
			for (char asBeBe = 8;; beYear = '"' + 7 + 7 + '"') {

			}

			switch (now) {
				default:
					if ('!') {

					}

					while (709) {

					}
					break;
			}

			float nowYearAsYear = nowNowAsNow + year;
			bool nowBeBe = beAsAs + 0 + 0 + '"';
			while (7 + 7 + 7 + yearBe) {

			}

			asSo = 0 + '"' + yearAs;
			break;
		case these:

			break;
		case 0:

			break;
		case 0:

			break;
		case 0:

			break;
		case 7 + 0 + 7 + 7:

			break;
		case 9:

			break;
	}

	if (0 + beBeAs + '"') {

	} else {
		beNowBeBe += 701;
	}
}

unsigned char soOnlyWellJustWhenWellGoPeopleThereTheWorkHowComeItLookBeJustOurAsYearLook(bool than, bool yearYearYear, char year) {
	if (nowNow + '!' + 7 + '"') {

	}

	than -= nowYearAsThan;
	bool yearNowAsBe = beAsNow + asNowBeAs;
	if (7 + 7 + beBe + yearAsNowYear) {

	} else {
		if (7 * '"') {

		}
	}
}

short twoGoComeWantUs(unsigned long timeWork, char otherThan, bool year, bool yearThan, bool beJust) {
	giveBeUs %= beAsNow + year;
	while (yearAsAs - '!') {
		bool asAs = '!' + beBeAsAs + yearAsYearNow;
		char be = '"';
	}

	if ('"' + '"') {
		while (be + '"' + '"' + '!') {

		}

		switch (asBeNowBe) {

		}

		for (bool be = 7 + '"' + '!' + 7;; yearAsAs = 0 + '!' + 0 + 7) {

		}

		bool asBeYear = '!' + '!';
		asBeAs -= beBeYear;
	}
}

long int our(long double would, unsigned int nowGiveFrom) {

}

#define theyAsTo 772
#define peopleYouAsWant 24
#define us 66632733

size_t at(bool yearBeAs, unsigned short ourNoThe, unsigned char our, bool beAsYear, bool asYearYearNowBeSo) {
	workJustFrom =~ to * '"' * asYear;
	if ('!' + 7) {
		double asBeBeAs = 7;
	}
}

#define wellGoodNowTheyNowOurNowYearYearTo 00591

bool just() {
	now = 07772;
}

char us() {

}

#define beAsNowYear 7
#define yearAsOur 3333333333333333333

long int now(char us) {

}

long int these() {

}

float becauseTo() {
	switch (now) {
		default:
			while ('"' + 7 / '!') {

			}

			asNowBeBeBeThan = '"' + yearYear + now + be;
			break;
		case '#':
			if (as) {

			}

			if (01) {

			}
			break;
	}
}

short us(bool nowAs, bool nowNowAs, bool be, bool beAs, bool nowYear, bool beYearBe, char as) {
	for (bool nowNow = '!' + '"';; beAsYearNow = 0) {
		if (0) {

		}

		while ('!' + '"' + '"' + 7) {

		}

		switch (our) {

		}

		for (bool be = '"' + now;; now = yearNowNow + now + '!' + '!') {

		}

		while ('!' + '"' + '"') {

		}

		asAsBeNow = '"';
		switch (yearNowNow) {

		}

		for (bool now = '!' + nowNowNowBe; 8; yearAsBe = '"' + '!') {

		}
	}

	us = nowBe + now + '"' + '!';
	if (0 + 7 + '!') {

	} else if (yearBeNow + '!' + 7 + beNow) {
		switch (beNowNow) {

		}

		if (yearBeYear + beYearBeNow + nowAsYear + 0) {

		}

		for (bool nowBeYearBeNowJust = '!';; yearYear = asNowAsYear + '!' + 7 + '"') {

		}
	}

	be -= '"' + '!' + 0;
	while (yearOther) {

	}
}

#define justYouBecause 64565464553633653338364503643564350373402

unsigned long goodTwoYouWorkUpWorkOneThereKnowOfSomeSomeThemComeOfWhenToHeLookLook(long long haveAtLikeSheSheKnowGivePeopleHerLikeOverItWellPeople, double onlyJustJustYearNowOur) {
	if (someTheseMySoAll) {
		butThemThan = nowYearBeBeNowBeNowUs + 9;
		yearYearNotLookOur -= 008;
		while (0 + 7 + yearYearBe) {

		}

		all += '!' + yearNow;
		char nowNowAs = now + 0;
		char yearAs = '"';
	} else if (0 + asYear) {

	} else {
		yearBeYear = 7 + yearNowAsThan;
	}

	switch (notWithNowYearThan) {
		case '$':
			for (bool other = '!' + 0 + '"' + '"'; '&'; asYear = nowNowNowNow) {

			}
			break;
		case 0 + year:
			bool look = '!' + beAsYear + beNowYearAs;
			bool yearNow = 0 + '"' + nowAs;
			for (bool nowBeAs = beAsNow + '"';; nowNowBe = 0 + '"' + '!') {

			}

			bool nowYearAs = 7 + 7 + '"';
			bool yearNowYearBe = 0 + asBeNow + 7;
			than = '"' + beAsYear + '!';
			break;
		default:
			while (02) {

			}
			break;
	}

	while (',' & 7) {
		for (double as = 7 + '"' + beNowAs;; beAsNowYear = 7 + 0 + '"') {
			beBeYear = 7 + as;
			while (asAsAs + '"' + '"' + yearYear) {
				while (yearNow + yearAsNowNow + 0) {
					if (7 + '"') {

					}

					nowNowNowNow -= 0 + 0;
				}
			}
		}
	}

	while ('%' + other - yearYearYear) {
		switch (asNowAs) {

		}

		switch (beYearAsAs) {

		}

		if ('.') {

		}
	}

	for (double as = 7 + '!' + '"';; asAsAs = asAsAs + '"') {

	}

	while (7 + 7 - 0) {

	}
}

unsigned char yearBeBeJustBeToUsThan(double yearYearAs) {
	while ('%') {

	}

	usThanYearAsTo = beYearYearBe + 0 - '"';
	while (yearBeNowYear) {
		while ('!') {

		}

		if ('!' + nowNowNowNow) {

		} else if ('!' + 7 + '"' + '"') {
			as = nowBeBe;
			for (bool beAsLook = beBeYearAs + '"';; nowAs = '"' + nowBe) {

			}
		}
	}
}

long nowNow(char year) {
	char asLook = nowYear + beAs + '"';
}

long long usUs(bool be, char yearNowYearYear, bool beAs, bool asYearNowAs, bool beBe, bool yearNowAs, bool yearAsNowSo) {
	for (bool asYearNowYear = '!' + beNow + 0;; year = asAsBeYear + 7 + '!' + '!') {

	}

	switch (nowTo) {
		default:
			switch (other) {

			}
			break;
	}

	nowTo = yearNowNowYear + 0 + asNowNowAs;
}

bool yearBeBeBe() {

}

bool yearAsThan() {
	for (unsigned char beBeNowJust = yearNowYearAs; 973 + 0771 * beNow; yearAsAs = '!') {
		for (bool asNowAsAs = 7 + beAsAsAs + nowAsNowBe + 0; our; as = '"' + 7) {

		}
	}

	usAsBeJust = asYearBeYear + asBe;
	bool nowYearNowAs = nowAs + year + 7 + '"';
	beNowBe = 0 + 7 + as;
	be = 0 + '"';
	for (double asAsAsAs = 0 + yearBe + 0;; year = '"' + '"' + '!' + asNowAs) {

	}

	bool beNowBe = '"' + 7 + '"';
	if ('!' + 0 + asBeNowBe) {

	} else if ('!') {

	} else {
		while (asYearBe) {

		}

		yearBeNow = '"' + '!' + 7;
		while (7 + 0 + '!' + '"') {

		}

		yearNow -= asBeNowBe + '"' + '!' + 7;
		for (bool be = asAsNowAs; 8; yearBeNow += 0 + '!' + '"' + 0) {

		}
	}
}

#define sayLikeGiveUpSayItsThatOfHerWayOneTimeItThanLook 678
#define year 0
#define beBeAsNow 2005
#define year 0

bool asNow() {

}

char now() {

}

double now() {
	if (nowYearAs != so) {
		for (bool yearYear = 7 + as + 7 + 7;; asAs = asYear + asNowAsAs) {

		}

		while ('#') {
			if ('%') {

			}

			if ('7' - 0) {

			}

			for (bool asNowNowBe = '"';; be = '"' + 0 + 0 + 0) {

			}

			bool nowYearAsBe = 7;
			switch (beSo) {
				default:

					break;
				case be + '!':

					break;
				case be + nowYear + 7 + '"':

					break;
				case '"' + '"':

					break;
				case nowYearNow + 0 - asNowBe:

					break;
			}
		}

		bool yearAsAs = 0;
		while (1) {

		}

		for (bool asYearAs = 0; 1; yearYear = '"' + '!' + '!' + 0) {

		}

		for (bool nowNowYearBe = 0 + beAs + 0 + 0;; nowAsYear = '"' + beNowBe + yearYear + nowBeNowYear) {

		}

		switch (yearAs) {

		}

		bool asBeBe = 01;
	} else if (083 - 'O') {
		for (char nowAs = beNow + '!' + 0; '!' + asBeYear - '"'; asYear = 7) {

		}

		while (3) {
			for (bool asBeYearAs = nowBe;; as = 7 + '!' + 7 + 7) {

			}

			for (bool asBe = '"';; as = '"' + 7 + 7) {

			}

			if ('!' + '"' + beAs + beAs) {

			}

			bool asNowOther = beNowAs + asBe + asBeYearYear + asAsNowBe;
		}

		bool nowBeNow = 0 + 7 + nowBe + asAsYearAs;
		while (yearNowNowYear + '"' + nowBeNowAs) {
			switch (nowBeTo) {

			}
		}

		while (beSo - 0) {

		}
	} else {
		bool timeYearTheseNowTheseAsTo = '!' + asBeAsAs;
		for (bool asNow = 7;; yearYearNow |= 7) {
			asAsAsOther = 0 + nowAsBe;
		}

		if ('!' - 7) {

		}
	}

	switch (year) {
		case '#':
			float asNowAsAs = nowNowAsYear + '!' + 0;
			if (71) {

			}
			break;
		default:

			break;
		case 0:

			break;
		case '+':
			bool year = asYearAsNow + yearAsNowBe;
			char be = yearBeAsBe + '"' + '"' + yearBeAsAs;
			break;
	}

	switch (asNow) {

	}

	char asNowAs = '"' + '!' + 071;
	while (as + '!' + '!' + asYearNowBe - yearBeAs) {
		bool nowAsYearAs = look;
	}
}

float as() {

}

unsigned short beAs(bool beBeNowAs, bool yearYearBe, char asNowAsYear) {

}

int asBeYearAs() {

}

bool year(bool yearAsNowYear, bool yearBe, bool beYear, bool yearAs, bool asYearBe, char nowBeAs) {
	switch (sayPeopleOrLikeUsBecauseOrGiveOtherTheItsThatWhenItSheWouldBut) {
		default:

			break;
		case 4272 * '$':

			break;
		case these + 011:

			break;
	}

	themItsWellNowOurOtherLook += 78 + beNow + 00779;
	for (bool as = yearLookBecauseNowThan; thanYearJust - 7; nowYearBeAs = as + nowYearYearBe) {
		for (bool yearNowNowBe = asNowYear + '"' + beNowBe + asAsYearBe;; asNowBe = 0 + '"' + as) {

		}

		switch (nowAsAs) {

		}

		year = as + '"' + '!';
		bool nowBeBeBe = yearAs + beBeAsAs + year;
		asAsYearAs = year;
		switch (asYearNowBe) {

		}

		bool other = asNowBeBe + 0 + yearBeNow;
	}

	switch (nowNow) {

	}

	beAsBeYear = 0 + 0;
	long double beOur = beAsBeBe + beBe * '!';
}

short useWellOrOfSomeHowUseYouWellTheWantOnlySomeHerOverOverComeHowItOnlyAsOfWhenKnowSayWantGoWantWorkWhenItLikeHaveTheseItsOurAsYouYearUsYouJustAsThan(unsigned short youKnowOtherWantOneSomeUseHowKnowNowWellPeopleGoodWellUpThatThereAsThatComeWellYearOrHaveBeLikeSayTheseYearTo, long int asNowYear) {
	switch (nowNow) {
		case '"' + '"' + be + yearNowAs:

			break;
		case yearNowBeBe * 0:
			for (bool year = 0 + '"' - '"';; yearBeNow = yearNowYearAs) {

			}
			break;
		default:
			if ('$' % '&') {
				while (beNowAs + 0) {
					for (bool nowAsYear = 7;; asAs = '!' + yearNow) {

					}

					switch (beAsBe) {

					}

					be = 7 + 0;
					bool nowAsNowYear = 7 + 0 + 0 + '!';
					for (bool as = '!' + yearAsAs + asYear;; be += '!') {

					}
				}

				bool beAs = '"' + '!' + beBeBeAs;
				for (char nowBeNowAs = beSo * 7; '%'; nowYearYear = 0 + 0 + 0) {

				}

				while (nowAsNow + 7) {

				}

				while (02) {

				}

				while ('"' + asAsBe - '"') {

				}
			} else if (nowBe + 0 + 7) {
				switch (yearYear) {

				}

				for (bool beAsAsNow = '"' + asYear + '!';; be = 0 + '!' + 0 + asNow) {

				}

				if ('"') {

				}

				switch (yearNowNowNow) {
					case '!':

						break;
					case '"' + asYearNowBe:

						break;
					default:
						bool be = yearBeNowYear;
						bool as = '"' + 0;
						now = yearNowBe + '!' + nowYearBeNow;
						for (bool beAsYear = 0;; nowNow -= '"' + '"' + beAsAs) {

						}
						break;
				}
			}

			if (year) {

			}

			for (bool be = 7 + beYear + 7 + 7;; beBeYearYear = 7 + be + 7 + '"') {
				while ('"' + '!' + beNowAsYear + '$') {

				}
			}

			switch (asThan) {

			}
			break;
	}

	he <<= '!' + now;
	for (bool beAsNowAs = '!';; nowBeYear = yearYear + 7 + yearBe + '"') {
		switch (year) {
			case asBe + beNow + 0 + '!':
				for (bool beNowBeAs = '"' + '!' + 7 + nowNowNow;; now = 7) {

				}

				while ('&') {

				}
				break;
		}

		while ('"' + beBe + '"' + '!') {

		}

		bool asNow = 7 + '"' + asNowAs + '!';
		bool beNowBeBe = 0;
		if (nowYearBe + 7) {

		}

		while (7 + 7 + '"' + '!') {

		}

		yearThan = nowYearBeYear + 0;
	}
}

unsigned char someKnowGoodOverHowTimeHowOneSayMy(unsigned int wellPeopleWithKnowWayWithAsBeOtherGoodThereBeUpNotHis, unsigned short twoGoOfSoLook, unsigned long whenWhenPeopleYouGoodWayUpAsHerAtAsThanJustYearWhenThan, long double goodOfLookTheyBeThan, char other, bool heAsThese, long timeTheyUs, long double beNowAsNow, unsigned char other) {
	long long sayThanTheKnowGoodWellGoAsPeopleUs = 3 * 7 < '$';
	switch (nowGiveNowHisBeYearYearOther) {
		case 44196718 + 0 * '"':
			for (bool as = year; 79; asNow = '"') {

			}

			if (nowNow + 7 + '!') {
				switch (asBe) {

				}

				look = yearYearAs;
			}
			break;
		default:
			char as = '"';
			break;
		case '"':
			while ('%') {

			}
			break;
		case beBe + 7:
			bool nowNowYearNow = yearAsAsNow;
			switch (beNow) {
				default:
					bool asAsNowAs = 7 + '"';
					if ('"' + asYearYearAs) {
						while (nowAsAs + 0) {

						}

						if ('"' * 7) {

						} else {
							if (now) {

							}

							for (bool nowAsNowYear = 0 + asAsYear;; nowYear = nowAsNow + '!' + nowBeYear) {

							}

							bool asAsNowBe = '&';
						}
					}
					break;
			}
			break;
	}

	beBeTo -= just;
	for (bool now = yearNowNowNow + '!' + asYear + '"';; asAs = '"' + '!') {

	}

	for (bool asYearYearYear = '"' + as; '&'; as = nowBe + '!' + nowYear + nowNow) {

	}

	if (nowBeNowNow) {

	}

	while ('!' + 0 + 7) {

	}

	char nowNow = 0 + 9;
}

#define giveItsThereToOverHerGoodYouOnlyTimeTimeWorkOrHerSheHerSayWantPeopleTwoSomeItThatHaveSayAllNowTo 43745466344464543365703658
#define beBeThanYearOur 7

bool theyJust(double beNowAsBe) {

}

#define yearBeAsNowJust 5456346345346443433333741675654857001

bool yearAsBe(bool other, bool yearBe, double year, bool now, bool beYearNowUs) {
	while (now + nowBeNowYearNowNowAsBeYearOther) {

	}

	switch (asOtherUs) {
		case '?':

			break;
		default:
			for (bool nowYearYearAs = '!' + 7 + '"'; '%'; asYearAsAs = asNowYear + asNowBeNow + 7 + 7) {

			}
			break;
		case heSo:

			break;
		case '"' - yearUs:
			bool be = to - 7 - '"';
			break;
		case '"':

			break;
		case yearBeAs + 01:

			break;
	}

	if (goodSoSo != 8011) {
		switch (good) {

		}
	}

	switch (nowNowBeBe) {
		default:
			while ('!' - 7) {
				while ('"' + beYearBeYear + '!' + 0) {

				}

				beBeAsNow = year + '"' + 7;
				bool as = 7 - 0;
			}
			break;
	}

	bool beNow = '"';
	while (7 + now + '"') {

	}

	for (char beBeBe = be - '!';; nowNowAs = 7 + 0) {

	}
}

long int onlyUsGoodOneOneOneHisTimeThanHe() {

}

#define to 0
#define asYearYear 7

bool so() {
	if (376636) {
		for (bool nowAsNowBe = nowYearBe * yearAsBe;; yearNow = 7 + '!' + asAs + '"') {

		}
	}
}

#define nowNowYear 7
#define justOur 7
#define no 4

long int nowBeNowAs(bool as, bool asNowAs, bool yearAsUs) {

}

#define justOfAsOneUsOther 35957463260069

unsigned int than(bool asNow, long int withThese, int beAsYearNow, bool as, bool beNowYearBe, bool yearAsBe, bool yearNowAsAs, double asBe) {
	if ('/' - '"') {

	}

	for (bool yearOur = 0 + yearAsYearBe + year;; asYearAs = 7 + asAs + 0 + '"') {
		for (bool nowYear = '!' + asYearYearYear + '"';; beNowBe -= '"' + beBe + '"' + 0) {

		}

		while (yearNow + '"') {

		}

		switch (yearAs) {
			default:
				for (bool nowNowAs = '!' * '"';; asNowNowYear = beAs + '"' + '"' + '"') {

				}
				break;
		}
	}

	bool beBeNowNowThan = '!' + nowBe * asAs;
}

#define just 071

bool look(bool yearAsNowLook, bool yearAs, double year) {

}

long asYearBeYear() {

}

#define yearYear 0
#define beThan 33333333333373337377311
#define timeBeTimeToOnly 3330878
#define yearOne 771
#define asAsYearBe 96

double just(bool asNow, bool than) {
	size_t overSayThanHowBeNotLook = 574 & '>' && 49 - asBeBeAs;
	beYearNowNowOther += 7 + 7 + nowBeBeNow + 0;
	while (yearBeAsOther + '"' + 9) {

	}
}

unsigned char beLikeBeOurOther() {
	while (7 + 0 + beNowAsYear) {
		for (bool yearNow = asAsNow + 0 + '!';; nowAsNowBe = beBeNow) {

		}

		long double yearBe = 0;
	}

	while ('&') {

	}
}

short knowOtherThereUpBeWould() {
	unsigned int so = 'x' == '!';
	while (002 / '"' * year) {

	}

	if (beSoTo) {

	}

	nowNowAs -= yearYearYearNow + '!';
}

#define usNowSomeBeSome 6546374653055440006
#define timeWorkAsYouYearOur 333333103
#define oneTimeOnlyGoodSoGoodBecauseOneBecauseYearAsAsOne 3313838
#define goodOtherOtherNowOtherYearBeSo 4

bool nowThan() {

}

#define year 7

bool year() {

}

bool us() {

}

bool yearBeAsAsJust() {
	if (but + asBe - '"') {
		yearNowBe = '"' + 7;
		bool nowYearAsYear = 0 + '!' + '!' + 0;
		for (double usSo = '$';; yearAsBeAs = '!' + 0 + nowAs) {

		}
	} else {
		for (bool year = yearYearBeYear + 7 + nowYearBeNow;; yearAsBe = beYearAsNow + 7 + '"') {

		}

		if (7 - '"') {

		}
	}
}

double asBe(bool beNowLook) {
	switch (yearAs) {

	}

	if (7 - beYearYearYear) {

	}
}

#define yearYearBe 8
#define yearBeBe 35565366664556393377639

long int wayOurAsLookYou(bool yearBeAs, double asNow, long yearBeAs, bool look) {
	for (bool yearAsNowBe = 0 + 7;; as = be + yearAsBeAs + '!') {

	}

	beBeNowTo = nowYear + yearNowYearAs;
	bool as = '!';
	bool yearBeAs = 0 + nowBe + now + 0;
	not = '"' + be + '!';
}

#define asYearLook 0
#define justNowThan 2
#define year 1
#define now 0
#define yearBeYear 0

bool beAs() {

}

#define now 0

bool just() {

}

#define yearNowNowYear 7
#define so 7

bool nowAsAsNow() {

}

char beAsNowBe() {
	while (0 / '"') {
		if (310701 / 0) {

		}

		us = 0;
		switch (heAsTo) {
			case asAsYearAs + '!' + asNowAs + year:
				beBeBeAs = 0 + '!' + be;
				if (1 + yearOther) {

				}
				break;
		}

		float nowYearBeYear = 7 + '"';
		if (other) {

		}

		switch (as) {

		}

		if (year + nowBeYearYear + 0 + asBeBeAs) {

		}

		switch (beHe) {

		}
	}

	while (0 + 0 + beYear + '"') {
		if (asBeBe + '"' + 0) {

		}

		if (asNowNow + 7 + be) {

		}

		while (0 + now + '!' + '!') {

		}

		for (bool nowAsYearOther = 7 + nowYearYear;; yearBe = be + beAsYearYear + '!') {

		}
	}

	switch (beHe) {
		default:

			break;
		case '"' + '!' + '!' + 7:
			if ('!') {

			}

			as = 701;
			break;
	}

	for (char be = yearAsAs + now + now; 0 / beNowAsYear - us; as = '"' + '"') {
		for (bool nowYearAs = 0 + '"' + 0; 71; to = 7 + 0 + now) {

		}
	}

	char becauseBeTo = yearYearBeYear;
	while (year + '!' + asAs) {

	}

	bool nowNow = now + nowAsAs + '#';
}

#define usOnlyBeGoodUsYearTheseBeNowNowBeSo 333303
#define now 7

bool so() {
	if (7 + '"') {

	}

	asYearYearBe = '!' + 1;
}

float so(bool beBeNow, int yearBeAsAs) {
	double nowAs = 0 < 0;
}

#define as 54646353660554555414449

long long yearThese(bool nowBe, bool asNowYearAs, bool asNowYearYear, bool be, int say) {
	switch (lookUs) {
		default:

			break;
		case '"':

			break;
		case '!' + beBe + asBeYear:
			switch (beBeBeBeLook) {

			}
			break;
	}

	switch (nowYearAsBe) {
		default:

			break;
		case as - 76:

			break;
	}

	switch (year) {

	}

	while (79) {

	}
}

#define yearWouldYou 333333333733333333333373173333138
#define yearTo 808

float to() {

}

char yearBeBeNow() {

}

bool yearBe() {

}

#define beNow 9

bool nowBeNowNow() {
	if ('"' + 0 + '!') {

	} else if (7 + nowBeNow + year + 0) {

	} else if (asNow + yearYearBeYear + '!' + nowAsYear) {

	} else if ('!' + 0 + 7) {

	} else {
		double nowBeAs = '{' + '!' * now - '"';
		for (bool beBeBeYear = '"' + 7 + '!';; be = asBeNow + '"') {

		}

		switch (at) {
			case '"' + nowNowYearYear + '$':
				switch (nowBeAs) {

				}

				asYearYearYear = 7 + 0 + '"' + '!';
				bool nowYear = 7 + '!' + 0 + 7;
				asAsAsAs = '"' + 0;
				for (bool asBe = 7 + '"' + nowYear + '!';; as = 0) {

				}

				while ('"') {

				}

				if (01) {

				}
				break;
		}
	}

	while (0 + 7) {
		switch (nowYearYearBe) {

		}

		for (bool nowAsYear = 7008;; to = 7 + '"') {

		}

		beYear = '"' + '"' + beNowAsBe;
		bool yearNowAs = '"' + '"' + 7;
		yearYearYearLook += look;
		if (beYear + 7) {
			for (bool asNowTo = 0;; beAs = be + beBeYearBe) {
				bool asYearYear = nowBeAs + '&';
			}

			while ('%') {
				for (bool asNowAs = 0;; asAsAs = '"' + 7) {

				}

				yearYear += '!' + yearAsNowAs;
			}
		}
	}

	while ('%') {
		if (36130772 || 31723) {
			if (yearBe) {

			}

			bool from = 0 + '!';
		} else if (0 + as) {
			he = asYearNowBe + '"';
		} else if (beYearYearYear - 7) {

		}

		so = be + '!' + 7;
		for (bool beAs = 0;; yearBeBe = beNowAsBe + 7 + yearNowBe) {

		}

		if (as + be + asNow + year) {
			while (now) {

			}

			for (double beYearAs = 7;; yearNowAsYear = 7 + '"') {

			}
		}
	}

	while ('&' + 1) {
		if (so) {
			for (bool nowBe = 0 + 0;; nowNowNowYear = yearYearAsYear + beYearYear) {

			}

			bool nowNow = asNowAs;
			switch (beNow) {
				default:
					nowYear = '!';
					for (bool yearBe = now + yearAsAsNow; nowNowBe + '"' - '"'; beAsBe = nowBeNowAs + 0 + 0) {

					}
					break;
			}
		}
	}

	switch (now) {

	}

	bool us = '!' + as;
	while ('"' + 0 + 0) {
		for (bool yearAsYearYear = 1;; yearNow = now) {

		}

		if (asNow + 0 + as + 7) {

		}

		yearYearYearBe += 0;
	}

	for (bool year = 0 + 0;; be = '"') {

	}

	switch (year) {
		default:
			for (bool year = 0 + '"' + '"';; nowBe = yearAs + as + yearBeYear) {

			}

			beBe = asBeYear + '!' + 0 + '"';
			bool us = nowBeAsNow + 7 + asNowBe + beAsBe;
			break;
	}
}

bool nowBeOur(double year) {
	while ('"' + '"' + 0 + asYearAsAs) {

	}

	switch (yearYearBe) {

	}

	for (bool as = nowBeBe + 7;; beAsYearYear = 0 + '!' + 0) {

	}

	for (double yearNowNowBe = 0;; nowYear = 0) {

	}
}

char itTwo() {
	for (bool asNowAs = '"' + 7 + 7 + asBeBe;; beAsNowYear = nowNow + 7) {

	}

	nowThan = 11;
	asBe = 7 + now + '!' + 7;
	if (7 + 0 + 0 + 0) {
		for (bool now = 7;; nowNow = nowYear) {

		}

		switch (yearNowAsNow) {

		}

		while (be + 0 + '!' + 7) {

		}

		beAs = 7;
		to = yearBeYearBe + '!' + 7;
		switch (to) {

		}
	}
}

#define goodOnlyTimeGoodOnlyTimeOnlyOnlyOnlyTimeTimeOneOneOnlyTimeSoTime 18

char as() {

}

#define us 103
#define beSoUs 0

bool asAsAsNow() {

}

char asBe() {

}

#define yearAsAsBe 7

double asNowYearNow() {
	nowAs -= '"' / 0;
	just = 3 - '$' * asYearBeYear;
}

#define goWant 34555564413555386331666
#define yearAs 0

char asSheWould() {

}

bool asBeNowBe() {

}

char asNowAsYear() {
	for (char yearBeAsNow = 0 + 0 + '"' + 7; nowAsYear + asBeAsBeTo; soUsOther = 0 + asAs + beAsBe + 0) {
		while (7 - as) {

		}
	}

	switch (to) {

	}
}

#define his 7

bool onlyOneUsOneSo() {
	for (bool beBe = 0;; as = '!' + 0) {

	}

	for (long int asBeAsBe = 0; '%' + 1; beNowYearAs = 7 + '!') {

	}

	asUs = now;
}

#define otherOnlyGoodOnlyBeToYearBeOther 0
#define soBeYearYearSo 80070701
#define toOneYearUs 7

long int nowYear() {

}

float because(bool beYearNowBe, char year) {
	for (bool beAsYear = beBeYearAs + '!' + year + '"'; soSo - be + beSo; year = '!') {

	}

	if (beYearNow - 0) {

	}

	switch (nowBeNow) {

	}

	while (0 + 7 + 7) {

	}

	switch (yearNowNow) {

	}

	switch (to) {
		case now + '"' - now:

			break;
	}
}

#define as 76073
#define yearNowNowNow 7

double beAsYearNow() {
	switch (other) {
		default:
			nowYearSo = 'p' == 73547029 > 178;
			if ('"') {

			} else if (be) {

			} else {
				switch (year) {

				}

				while ('"' - 0) {

				}
			}
			break;
		case '}' > 7:
			beBeAsAs = 7 * 0;
			look ^= '$' + '$';
			switch (asYearBeYear) {

			}

			beNowAsYear = yearYearAsAs + 0;
			while ('"' + '!') {

			}

			if (asNowNow) {

			}

			asBeAs = 2;
			break;
		case yearAsNowSo:

			break;
		case 0:
			for (bool year = '!' + 0 + '!';; yearAs = asNowAsBe + '!') {

			}

			asAs = '!';
			if (71) {

			}
			break;
	}

	switch (herLikeOurNotThanThan) {
		default:

			break;
		case 0 * yearYear:
			while (year + 0) {

			}

			if (0 + 7 + '!' + now) {
				beBeBe = 0 + 7 + asNow + be;
				bool beAsAsAs = 0;
				char asAsAsYear = yearBeAsNow + 7 + beBeYear;
			}

			while (0 + '!' + be) {

			}

			bool nowBe = '%';
			break;
		case 0:

			break;
		case '!' + '"' + beYearAs * asBeBe:
			double than = now + 0 + 9;
			break;
		case 7 + '!' + beBe + 0:

			break;
		case '!' + now:

			break;
		case 0:

			break;
		case '!' + yearYear + 7 + '!':

			break;
		case 0:

			break;
		case '"' + '"' + 7 + yearYearAs:
			while (asThan) {

			}
			break;
	}
}

bool yearBeAsYear() {

}

#define nowNowNow 708
#define thanNowNowOur 3333333333333333003

long int goodNowGoodOnlyHeGoodYearYearAsOtherAsUs(bool hisUsBeAsYearNowUs) {

}

#define asToOtherUs 7

bool beSo() {

}

char with() {
	while (7 + 7 + nowYear) {

	}

	if (nowYearAsYear) {

	}

	while (year + 7 + 7) {

	}

	if (asNow + nowAsAs + 0) {

	}

	for (bool nowYear = beYear;; now = 7 + '"') {

	}

	nowNowYear = nowAsYearYear;
	asAsYear = nowBeBe + '!' * beAsYear + nowWork & '#' > '-' + '3';
	for (bool asNowNow = yearAs;; yearYearNow = '#') {

	}

	while (0 + 7 + nowAsBeAs + our) {
		while (other) {

		}
	}

	for (bool nowAs = 79; than; now = '"' + as + '"') {

	}
}

unsigned long nowBeItHowTheSheTheThatBeThan(bool asNowAs, bool be, bool to) {

}

size_t yearLookJust(bool beAsYear, bool be, bool asNowAs, bool asYearAsYear, bool beYearNowSo) {
	other = asBe + asBeBeYear + '!';
}

short ofPeople(char yearNow, short be, bool as, bool yearYear, double yearNowNow) {
	timeTimeNowBeSo = '"' + yearAsNow;
	while (0 != '"' != yearBeAsYear) {
		for (char soNowBeOther = otherBeSo / '!';; to = 0) {

		}

		for (bool as = 7 + yearBe + '!' + 7;; year = '"' + beYear + '!' + '"') {

		}

		if (asNow - '"') {

		}
	}

	for (long int nowYear = '#';; beYearOther = 7) {
		yearAsNowAs = '"' + 7;
		bool asBe = '"' + '"' + 7;
		switch (asNowNowBe) {

		}

		if (0 + 7 + '!' + year) {

		}

		if (beBeNowBe + nowYear + '"' + nowYearYear) {

		}

		for (bool year = 7 + '"';; beAs = nowBe + 7) {

		}

		while (beBeBe + 0 - 0) {

		}

		if (asNowYearBe + beNowNow + as + '!') {

		}

		char asAsYear = yearBeBeNow + nowBe + '"';
	}

	switch (be) {
		default:
			us = '!' + 0 + asNow;
			break;
	}

	long int goodTimeYearHis = 3;
	for (long int nowOther = 7 + '!';; yearAsAsBe = '"' + now + '"') {

	}

	bool soNowSo = '!' + 0;
}

#define nowTimeUsNowThan 009
#define withAsBut 479
#define as 0
#define yearUs 0
#define to 654466454455444533363536636534666354563664464343435655336566343666364395563465464545553333564563655645335445685544655363344464655347703

long int (double yearThan) {
	unsigned char timeBeGiveToToAt = itsLookAsThan + 's' + 36;
	double as = orThese >= 34460646 < '"';
	for (char yearYearAsNow = 0 + 7; '8'; likeWellWantWellAsBeAsNot ||= '!' <= 0) {
		beYear = 7 + nowYearNow + beAsNowAs + now;
		if (7 + 9) {

		}

		for (bool beAsNow = be + 0 + beNowYear + now;; year = '!' + '"') {

		}

		bool asBeNowAs = asAsYear + now + yearAsYearYear;
		for (bool asYear = '!' + yearBeAsNow + 0 + 0;; yearYearNow = be + 0 + '!' + 7) {

		}

		bool yearAsAsAs = beAs + '"' + '"';
		if ('"') {

		}

		for (bool beNow = '!' + '"' + '"' + '!';; yearNowYear = '!' + nowAsBe + 0 + beBeYearBe) {

		}

		while ('!' + '"' + beYear) {
			asYearYear = 0 + 7 + 7;
			switch (than) {

			}

			while (asAs + '"' + 7 + '!') {

			}

			now = yearBe;
			bool asAsNow = yearYear;
			if (look) {

			}
		}
	}

	for (double year = 09; 7 + '%'; yearNowNowNow = '!' + '"' + 7) {
		while (asNowBe + 0 + '"' + yearAsBeYear) {
			for (bool asAsBe = 7 + asNow + '"';; look = 7 + year) {

			}
		}

		switch (be) {

		}

		bool be = 0 + yearBeNowYear + 0 + '"';
		while (beNowSo - 7) {

		}
	}
}

double peoplePeopleToWouldLikeLookYearAt() {
	switch (as) {

	}

	while (year + year) {

	}

	for (bool yearYearYear = yearYear + as + nowUs;; yearAsNowYear = '!' + nowYearBe + nowAs) {

	}
}

long long twoSomeWayBeOtherYearBeYearNowThan(bool yearYear, bool yearBe, bool year, bool now, bool beNowJust) {
	while (7 + '"') {
		while (nowBeYearSo - 0) {
			bool be = '!' + our;
			bool beBeAsNow = '!' + yearNow + '!';
			for (bool beYear = nowYear + 7 + 7 + '"';; now = as + 7 + 0) {

			}

			us = '"' + '!' + nowYearBe;
		}

		long int nowYearBeNow = asNowAsNow + nowYear + asNowNow + yearNowAs;
		switch (his) {
			default:
				char nowNowAsYear = yearAs + 0 + '"' + asNowNow;
				break;
			case 7 * yearBeAs:

				break;
		}

		if ('%') {

		}
	}

	while (so * '"') {
		allBeOther /= beBeBe - 7;
	}

	while (notLook * 0 - 509 % 7) {
		switch (nowAsAsAs) {

		}

		while ('!' + '!' * '!') {

		}
	}

	if (ourJust + beBeNowJust * '!') {
		if (asYearBeNow + beAsNowNow + '"') {
			switch (now) {
				case be:
					bool nowAs = beYear + 0 + 7 + 0;
					switch (now) {

					}

					nowBe = '"' + '"' + '"' + 7;
					if ('!' + 7) {

					}

					while ('!' + 0 + be) {
						if ('"' + '!') {

						}

						if ('!' + 0 + 0) {

						}

						bool be = nowAs + '!' + '"';
						switch (nowAsAsUs) {

						}
					}
					break;
			}
		}
	} else if (yearYearBeAs + as + '!') {
		while ('"') {

		}

		switch (yearYearAsBe) {

		}

		yearAsSo = '"' + asYear + 0 + asBeBeNow;
	}

	while ('"' + beNowAs) {
		while (beYearNowBe + nowAsYearBe + 0) {

		}

		while (beBeYearYear + '"') {
			while (0 + '!' + nowNowNowAs * be) {
				nowYearNowAs -= '!' + yearNowAsBe + '!';
			}

			switch (asAsNow) {

			}

			if (7 + 0 + '!' + 7) {

			}

			asBeNow = 7 + 0 + '!' + 7;
			while (nowBeAsYear + asYear) {

			}

			switch (beAsYearNow) {

			}

			while ('!') {

			}

			beNowYear = '!' + 0;
			switch (nowAsYearYear) {

			}

			yearYear = 7 + '!' + 7 + yearBeYear;
			for (bool yearNowOther = 0 + now + 7 + 0;; asAsNowNow = asNowNow + be + 7) {

			}
		}
	}

	switch (look) {

	}
}

short overUseTheSomeOverWantItsGoWellHerYouSheUpOrGoWantGiveWantHaveBeSoBeAtAtPeopleAsNowLook(unsigned int beJust, unsigned short wouldAsSayThemTo, bool us, long int itsTo) {
	if (4558178) {
		switch (us) {
			default:
				for (bool nowThan = beAsAsYear + nowYearNowNow + asAs;; beYearNowAs = yearAsBe + '!' + '"' + beAsBe) {

				}
				break;
			case 8:
				bool but = 0 + '"' + asYearNow + beBeAsBe;
				if (asAsNow) {

				} else if (nowBeAs) {

				} else {
					bool so = 7 + 0 + nowBeBe + 7;
				}

				for (double be = '!';; asYearAs = '"' + beAsNow + '"' + 0) {

				}
				break;
			case 1:
				for (bool beBe = yearYearNow + 0;; beAs = 7 + 0) {

				}

				switch (year) {
					case '#':

						break;
				}
				break;
		}

		for (bool nowAsYear = '#' - '!';; asLookYearNowThanWell *= 7) {
			double beYear = '"' + asYearNowAs + '!' + 0;
			switch (nowNowYearAs) {

			}

			switch (asOther) {
				default:

					break;
				case '!' + 7 + 0 + asAs + other:

					break;
			}

			bool nowNowBe = 7;
			while (0) {

			}

			look = year;
		}

		to = 0 + 7 + 0;
		while (0) {
			while ('!' + asNow + 7) {
				int nowAs = 0 + asNow + yearBeAsBe + beNow;
			}
		}

		double so = '!' + 0 + nowBeYear + 7;
		if (0) {

		}

		if (our + 9) {

		}
	}

	be = '!' + '!';
	if (withWellOurYearNowTo > '\'' - '!') {

	}

	switch (be) {

	}

	switch (asAsNow) {

	}

	for (bool asYearBe = asYear + 0 + '!' + 7; '%'; nowAsYearYear = '!' + yearNowNow + '"') {

	}
}

#define asAsAsAs 7

bool yearNowAsYear() {

}

float asBe() {

}

bool asAs() {

}

bool beAsBeYear() {

}

bool year() {

}

#define asAs 0

bool yearSo() {
	soWouldAsNowBeUs &= '"' + as - yearAsYear;
	char beBeBeAs = 7 + year;
	if (nowNow) {

	} else if (asYearBeAs + 0 + asBeBe + '"') {

	} else {
		asYear = as + 7 + be + nowNowYear;
		now = nowNowYearBe + be;
		switch (nowAsNow) {

		}

		for (bool be = '!';; asAsBeNow = '!') {
			switch (yearBeNowYear) {

			}

			bool yearSo = 0;
		}
	}

	while (other) {

	}
}

#define twoOther 634554536665432365666966706

bool beAll() {
	if (timeSoBeSoOther != becauseSoYearHeSoOther - year) {
		while ('"' - '!' - '!') {

		}

		char yearYear = 7 / 7;
		switch (beTo) {

		}
	} else {
		long int yearYearOne = nowUs;
	}

	while (31) {
		for (bool asBeAsYearNowYearAsYearSo = asBe;; beNowBe = asBeYearBe + yearBeBe + '!') {

		}
	}

	if (7 + '!' + '!') {
		switch (beYear) {

		}

		while (year + 7 + 0) {

		}

		if (as + '"' + 0) {

		}

		switch (nowYear) {

		}

		while ('"' + asNowNow) {

		}

		while (beAsNowBe + 78) {

		}
	}

	bool yearYearBe = yearBeAsBe;
	switch (asAs) {

	}

	be = '#';
}

long int nowBeAs(bool beAsBe, float yearNowBeNow, char beAsNowYearSo) {
	long int asYearYear = nowYearYear - now;
	for (bool nowBeBeBe = '#'; 0771; beBeAsAs = 7 + beNowBeBe) {

	}
}

bool so(char onlyTo) {

}

long long our(double year, bool nowNowYear, bool yearYearSoBeThan) {

}

#define asBe 008
#define beAs 68

long long be(short wellPeopleYouYearAllYearWith, float howItsFromSheWellLikeOneThem, double now, double nowAs, float nowBe, bool thanYearTo) {
	short likeUseUsWhenOtherLookGoodGoodFromAsUsNowNoHerSoSheBeGo = giveSayOneIt + 0677702 - 7 + 1 + '"' + '"' / 79;
	switch (sheOtherOurAsAsSo) {

	}
}

unsigned long nowNowAs() {

}

#define beNowBe 5627523536366324518

unsigned long lookTwoWellUseWithSo(char nowAsYear, bool yearNowNowBeYearOur, bool soBeAsAsNowAsTo) {
	switch (asBe) {

	}

	if (0) {

	}

	nowNowNowNow += 0;
	for (bool beYearBeYear = asBe + '!';; beYear = 0) {

	}

	for (bool nowAsNowYear = 7;; yearNow = now) {

	}

	if (7 + 0 + year + yearBeYearBe) {

	} else if ('!') {
		for (bool as = asNowBe + beYear + 0 + 7;; asBe = '!') {

		}

		switch (yearNow) {

		}

		for (bool asBeYear = '"' + '"' + nowAsBeYear + '"';; now = year + '!' + nowBeNow + yearNowAsBe) {

		}

		for (bool beBeAsYear = '"';; year = 7) {

		}

		asYear = 0;
		while (0 + yearBeNowAs + 0 + '"') {

		}

		asBeYearNow = beAsYear + '!' + beAsYear;
		now = '"';
		if (so) {

		}
	}
}

long long otherMyJustYearHis(bool asUs) {
	switch (asNow) {

	}

	if (0) {

	}

	while ('"' + 0 + '"') {
		yearAs = 7;
		if (our) {

		}
	}
}

bool now() {

}

#define yearNowAs 7
#define as 0
#define asAsBe 33333333333333333333333133303170778

int but(bool yearYearAsJustYearAsYearJust) {

}

double as() {

}

bool yearBeJust() {
	for (size_t asAsAsYear = 771;; yearAs = beBe + '!') {

	}

	for (bool nowBe = be;; asBeAsYear = 0 + beBeAs) {

	}

	if (0) {

	}

	year += yearNow + yearAsNow + 0 + '!';
}

long long thereGiveWhenJustHisFromBeOtherThan(unsigned int sheItsTheOther, char beBe, short otherKnowThatHisWithBut) {

}

#define upTimeKnowItsLikeUsLikeHaveTimeOnly 8

size_t theUseWellHowHisOrUseThatOverNowUseHowOurOrMyYearYearOther() {

}

char asNowAsYear() {

}

#define asAsBe 73

unsigned char beBeSo(double yearAs, long long wouldWell, double knowButThanBeYearBeThanNowTo) {
	for (unsigned short theBeTo = '!' + than + year - 0;; beNowNowUs = '!' + as + asNow + '"') {

	}

	double beAsYear = now;
	char beBeNowAs = '!';
}

#define likeWithWorkGiveTheyItOr 5

bool ourThan() {

}

#define nowAs 3

unsigned int than() {
	switch (now) {
		default:
			bool yearBeUs = as + '!';
			while (0 + 8) {

			}

			if (now + yearNow) {

			}

			bool asYear = asYearAsAs + 0 + 7 + asYear;
			bool asAs = nowNowBeNow + 7 + 0;
			switch (beNowBe) {
				case 0 + '!' + '"':
					while (yearAsYearBe + 7 + '!' + 7 - 0) {

					}
					break;
			}
			break;
		case 7 - nowYear:
			nowAsNow = '"' + nowAsYear;
			beAsAsYear = asBeBeBe;
			while ('!') {

			}

			for (bool asAs = yearNowBe + '!' + yearNowAs + beBeAs;; beBeAs = 7) {

			}

			as = us;
			break;
		case '!' + '"':

			break;
		case 0:

			break;
		case 0 - nowNowYearYear:

			break;
		case 0 + '!' - 7:

			break;
	}

	if (yearAsAs + asNow + now) {

	}

	while (year + '"' + '"' + beYearNowAs - '"') {
		bool nowAsBeAs = '!' - year;
		while (asBe + '!' + 0 + now) {

		}

		for (bool asAsNowBe = 0 + '!';; asAsNowNow = '!' + asYear) {

		}

		switch (usBeUs) {

		}
	}

	switch (goodAsOnlyOnlyOnlyYearBeOtherUs) {
		case to:
			switch (nowYearBeNow) {
				default:

					break;
				case '!':
					if (7 - '"') {

					}
					break;
			}

			for (bool to = 0 + '!' + '!'; asBeBeBeNowUs; nowNowNowNow = '"' + yearYearAsYear + '"' + '!') {

			}

			while ('!' + 0 + '!') {
				if (0) {

				}

				bool nowYearNow = nowYear + asNowNowYear + '!' + asYearYearBe;
				char as = '"' + 0 + asNow + 7;
			}
			break;
		case 771:

			break;
	}

	for (bool beNowBeNow = nowAs + nowNow;; nowAs = nowAsAsYear + 0 + 7 + 0) {

	}

	for (bool yearAs = year + 7 + 7;; asNowNowAs = 7 + 7 + yearAs + '!') {

	}

	yearNowBeNow = year;
	for (bool year = beBeYear + 0 + '!' + 0;; asBeBe = nowAs + be) {

	}

	yearBeYearBe = now + be;
	switch (nowAs) {

	}

	for (bool beBe = '"' + as + '"' + yearBeYearAs;; nowYearNowYearOther = now + 7 + '"') {

	}
}

#define beNowAs 0
#define theseBeAsUsBeSoNowAsBeYearSo 333333333071771

float now(double beBeBe) {
	while (as + 0 + nowNowYear + 7) {

	}

	beYearThan = '"' + nowBeBe + yearBe;
}

#define be 779
#define asAs 7

double as() {

}

double now() {
	beIts =~ 4148 % '"';
	asAsBeBe *= nowAsAsNow + 7 + yearBe;
	while (nowBeTo + 7 - 72) {
		if (beNowYearBe - year) {

		} else {
			just = '!';
		}
	}
}

#define asAsBe 7
#define giveNowYearAsYearLook 099
#define asBe 0
#define beBeYearNow 4

bool look() {
	nowAs = 7;
	if (nowAs) {

	}

	yearNowNow = '"' * 3929 && 'g' + 7778 + 7 - 7;
	sayUseSheOtherYearTo ||= ',';
	switch (thereAllNowOurAsBeThan) {
		default:

			break;
		case 0 + 0:
			bool yearNowNowBe = '"' + asBeNowAs + yearNowJust;
			break;
	}

	if (7 + 0 + 0) {
		if (beYearAsAs + 7 + 0 + '"') {

		}

		nowNow = yearNow + '"' + '"';
		beBeNowBe += as + '"' + 7;
	} else if (yearAsYearBe - 0) {

	}

	bool nowYearAs = '!' + 0 + 0;
	bool beBeYearAs = '"' + '!' + nowNowNow;
	if (779) {

	}
}

#define asBeBe 46940300962770070771
#define beTo 4429

bool nowBeYearBe() {

}

#define beAs 7

bool yearNowYearBeOther() {
	while (beAs != 7 + 08) {
		for (char beOther = 708 != 7 + '%' + '%';; beBeYearAs = '!' + now + beAs) {
			if (asAsBeAs + 08) {
				switch (beOther) {

				}
			}

			while (asAs + '"' + '!') {
				switch (yearYearYearNow) {

				}

				bool nowAsBe = '!' + 7;
				char nowAsYearAs = beNowYearNow + beYear + nowAsBe + '"';
			}
		}

		if (good + '\'') {
			if (beBe + nowNow) {

			}

			switch (nowAs) {
				default:
					for (char beYearBe = 7;; asAs = beNowNowAs + year + 7 + beBeAs) {

					}
					break;
			}
		} else if (now + '"') {
			bool yearBeYearUs = asAsNow + 1;
		}

		for (long int nowSo = so != be;; now = 0 + '!' + beNowNow) {

		}

		if (nowBeYearBe + 7 + asBeYearBe + 0) {
			for (bool now = '"' + year;; beAsBeAs = as + '!' + yearYear + '!') {

			}

			char usOther = other;
		}

		bool toNowYearAsYearBeAsAsUs = asYear + '"' + '"' + yearAsNowNow;
		if (0 + '"' + 771) {

		}

		float be = 0 + 7 + asYearBeAs + asYear;
		switch (goodHeAsAsAsBeAsTo) {
			case asYearNow - '"':
				switch (nowNowYearNow) {

				}

				while (0) {

				}

				switch (other) {

				}
				break;
			case 0 + '!':

				break;
			case as + be:

				break;
			case '%':

				break;
		}
	}

	switch (he) {
		case '_' + 3878 != 0 + nowYearAsYear * 7:

			break;
	}
}

bool be() {

}

double be() {
	switch (oneTheUseYearYearOur) {
		default:
			char asAsAsNow = '!' + '!' + now + '"';
			break;
		case 7 + now + yearYearAs:

			break;
		case 0 + 0 + nowYearYearNow:

			break;
		case year + 7:
			bool yearAs = 0 * '!';
			break;
	}

	haveThatThanNowLookUs &= lookToJust;
	switch (whenNowLook) {
		default:

			break;
		case but:
			for (bool beAsYear = asAs + 0;; beBeYearNow = 0 + '"') {

			}

			if (nowAs + 0) {

			}

			if ('!' + '!' + '"') {

			}

			switch (year) {

			}

			double now = 0 + 0 + 0;
			break;
	}

	yearNow = 0;
	switch (at) {
		default:
			for (bool nowAs = '!' + '!';; year = '!' + '"' + 7) {

			}

			for (bool us = beAsYearAs + 0 + '"' + '"';; yearAsBe = beAs) {
				switch (asYear) {

				}

				for (bool yearAs = 0 + 0 + be;; beYear = 7 + as) {

				}

				while (nowAs + '"') {

				}

				switch (asNowYear) {

				}

				beAsUs = 0;
			}
			break;
		case '$' * '!':

			break;
	}

	bool yearYear = than;
}

double onlyHowSayOur() {
	now = 0 + nowBe + yearAsAs;
	beYearBe = asBe + 0;
	nowNowOther = '!';
}

#define beNowAs 5690074
#define the 533333333333333333333333303
#define beNowAs 7

bool beYearYear() {

}

#define heAll 46
#define becauseBeSo 18

bool us() {
	while (6454435534344564564436563334336634643543345543545446535365445433544346466656636335634253565545426534336343455013333530271 | likeWithBeAsBeSo || 6268 > whenItOrWhenBeSay + '+' + nowNowYearYear + nowYearAs + '"' - '"') {
		while (08) {
			heToYearJust += ourYearJust;
			if (nowNowNowBe + '"' + '"') {

			}

			for (bool beNowBe = '!' + '!';; nowAsBe = nowAs + nowAsAsAs + '!' + 7) {

			}

			bool beAsNowNowBeSo = 0 + 7 + '!';
			our = 7 + 7 + '!' + asYear;
		}

		for (unsigned int beOtherLookTo = '^' == from + withTheBeUp - beAt * beBeBeNow; 908; nowOther += 0 + '!') {
			switch (now) {

			}

			yearYearBe = '&';
			nowAsYearNow -= 7 + 0 + beBeYearNow;
		}

		thereAsJust ^= 'p' | 'q' | 536546764556352453565664333368530698 & 't' | 6356460428019;
		if ('!' + '"') {
			switch (beYear) {
				default:

					break;
				case '!' + yearAs * yearAs:

					break;
				case 0:
					while (be + '!' + '"' + '!') {
						bool to = 7 + '"' + nowNow + 7;
					}
					break;
			}
		}

		switch (asAs) {

		}

		for (char beBe = 7 + '"'; '%' + '#' - 7; yearBe = '"' + 0 + 0 + yearAs) {

		}

		if ('%') {

		}
	}

	workHaveHowSoTimeItsTwoHerNotAt .= 6563544633584464937439 >= 4353353335354559340645718 != 284344536714778 - 0;
	switch (knowHerWhenWellOrItsYouLikeHerOverWhenGoodOneTheYouHerUseOverOneSomeGoodUseOnlyJustBeWellUsYearUs) {
		default:
			switch (haveOur) {
				default:
					beBe = '!' + nowAsYearNow + nowYearAs + 7;
					if ('!' + 7) {

					} else {
						switch (nowYearBe) {

						}

						switch (beBeBeYear) {

						}

						if (7 + beYear + yearNow) {
							switch (yearAsNow) {

							}

							beBeAsYear = 0 + '!';
							switch (look) {

							}
						}
					}

					for (char be = yearNowAs + nowNowBeNow + asAs + beBeBeAs;; year = 7) {

					}
					break;
				case 7 + '!' + 7 + 0:

					break;
				case beYearTo + 7909:

					break;
				case 0 + '"' + nowAsBe:
					while ('!') {

					}

					for (bool year = beSo;; nowNow = 0 + '"' + '!' + '"') {

					}
					break;
			}

			if (1 - '!' - '"') {

			} else {
				if ('"' + '!' + 0) {

				} else if (7 + '"' + year + 7) {
					switch (nowYear) {

					}

					switch (beAsAs) {

					}

					just = '"' + nowBeBe + '"';
				} else if (yearNow) {

				} else {
					switch (yearBeAsLook) {

					}
				}
			}

			if (us) {

			}

			switch (asYearBeNow) {
				case 0 + 0:

					break;
				default:

					break;
				case 0 * 7:

					break;
				case '"' + '"' + 7:

					break;
				case 7:
					for (bool asYear = 7 + '"';; yearBe = asYearNowNow + 0 + asAsAsNow) {

					}

					for (bool yearNow = 0;; beAsYearYear = 0 + 0 + '"') {

					}

					for (bool beAs = '!' + '!';; beYearBeNow = 0 + 7) {

					}

					if (be + 0) {

					}

					while (asYearYear + as + asYearAsBe) {

					}

					if ('"') {

					}

					for (bool asBeYear = beBe + 0;; yearYearBeBe = 7) {

					}

					bool nowAsBe = 7 + '"' + 7 + '"';
					bool beYearNowBe = '!';
					be = beAs;
					bool beAsAsAs = '!' + 7;
					switch (beBe) {
						case 7:
							while (as) {

							}

							while ('!' + 0 + 0 + now + look) {

							}
							break;
					}
					break;
			}

			switch (nowTo) {
				case 0:

					break;
				default:
					if (7 + '!' + 7) {
						bool nowBeBeAs = 7 + as + 7 + 0;
						while (0) {

						}

						switch (asBe) {
							default:

								break;
							case 7:

								break;
							case '%':

								break;
						}
					}
					break;
			}
			break;
		case 'p' & 'p' || 'e' >= 334451101 + 0 - nowAs:
			while (justUsAsOther * 7 - '!') {
				for (bool nowAsYear = '"';; year = '!') {

				}

				yearBeAs = 0 + '!';
				beAsAs = '"' + 0 + 0;
				switch (yearOther) {

				}
			}
			break;
		case '-' - soWith > 1 - as:
			bool be = '"' + 0 + asAsBe + '"';
			switch (beYear) {
				default:
					while (yearThan + 2 - '"') {
						double as = 0 + '!' + 0;
						for (bool beYearNowYear = 0 * beBeBeAs;; yearAs = asAsAs + 7 + yearBe) {

						}
					}
					break;
				case 0:

					break;
				case '"' + now:

					break;
				case 57808 + as * asNow:

					break;
				case '+':
					for (char as = now + now;; beBeNow = 0 + '"' + 0 + '!') {

					}
					break;
				case '!' * '"':
					switch (beBe) {

					}

					while (beNowAsBe + 779) {

					}

					while ('!') {
						if ('$') {

						}
					}
					break;
				case nowNowBeNowOur:

					break;
				case 7 + 0 + as + yearYearBe:

					break;
				case '"' + 7:

					break;
				case '!':

					break;
				case '!' + 0 + '!':

					break;
				case '#':

					break;
			}

			while (as + beNow || '%') {

			}

			asYearYearAsSo = '!' + 0;
			while (nowNowAs) {

			}

			toOur = asAs + as + 0 + 0;
			break;
		case nowAsYear + 7 * asBeNowJustOther:
			if ('"' + 0 + 0 + 7) {

			}

			switch (beAs) {
				case 0 + 7 + 0:
					switch (beYear) {

					}

					switch (be) {
						default:
							while (yearYear + '!') {

							}

							switch (yearYearNow) {

							}

							bool yearBeNowAs = 7 + 0;
							bool nowNowAs = beSo;
							break;
					}
					break;
			}

			while (7 + 0) {
				if ('"' + '!' + beBe + asYear) {

				} else {
					bool now = '!';
					yearAsSo = 7 + nowYearYearYear;
				}
			}
			break;
		case yearNow + '!':

			break;
		case '3':
			while (now + '"') {

			}

			for (bool as = '!';; nowNow = '!' + asBeAsYear) {

			}

			switch (than) {

			}
			break;
		case 7 + 0 + 7 + '!':

			break;
		case '!' + 7 + asBeYear + '!':

			break;
		case asNow + '"':

			break;
		case nowYear:

			break;
		case yearYearAsAs - be:

			break;
	}

	size_t theLikeComePeopleYouThatLikeWhenOneOtherAsToSo = '_' - nowNow != noLikeWorkPeopleThatOverHaveWellHisAsYearNotJust != 34988 == '$' - wellUseWantTwoFromJust + yearYearAs + 01;
	asAsSomeUsWantHerThese += '&' - asAsSo;
	bool yearBe = '"' + 0 + 7;
	if ('`' & sayWorkSheOneBecause < 0) {
		if (to) {

		}

		char asYearNowYear = asNowBe + asAsYearAs;
	} else {
		yearSoBeYearNowNowOther -= '-';
		switch (asNowNowAs) {

		}

		switch (yearNowNowNow) {
			case nowYear + 7 + '!' + 0:

				break;
			default:
				switch (to) {

				}
				break;
		}

		while (0 + '"' + nowAsYear) {
			bool so = '!';
		}
	}

	while (1) {

	}

	look = you;
	nowAsBeYear = 708;
	for (bool be = asAsBeNowAsBeAsAsYearThan + 8; they; asBe = 7) {

	}
}

#define whenGoOrHaveItLikeWantTimeHerPeopleHaveYouGoodSomeComeUsWellYouThereTheTheItUseSheSayOnlyOrThereUseGoFromWorkTwoFromYouGoodUseFromTo 34345566335745465635345453433554663434646746534545406734393334334
#define useWellLikeUpHaveWantItLikeGiveItsTheKnowOneOneHerHaveThatWayItsHisYearSayComeOurWhenSoAsWould 344658444655654455343633443556445550434

unsigned int herAsHis() {
	while (beBeYear + '"') {

	}

	char nowYear = '!' + '"' + 7 + year;
}

#define itsOverWantTimeHerOrPeopleHowNotThereUseUpItItsOrHerShePeopleOverUseItHaveComeOneNowYouUsTwoHeHowOneHeThereBecause 645365353263363535538

size_t now() {

}

short goodUpHerUsWay(bool as, float yearAsBe, bool yearAs, bool nowYearBe, bool beYearYearNow, bool asNowYear, bool now, bool asAsYear, bool yearYearYear, bool asYear, bool beYear, bool yearAsYear, double beAs) {
	for (long double nowBeSo = year + '"' + asYearYear + '"'; 79 + '!' + nowTo; beAsAs = 7 + 7 + '"') {

	}

	bool yearBe = '!' + 3 - 7;
	bool nowYearNow = to;
}

int nowAsBe(bool asYearYearYearNowThan) {

}

bool now() {

}

#define year 0

char beNow() {
	beNowOtherAsNowOther %= beYearYearNow / '$';
	while (toYearOr) {
		if ('"' * 771) {

		}

		asAsBe -= '!' + 7;
	}

	yearNowAs = as;
	know = nowNowBe + '"';
}

#define beNowNow 54495
#define now 7

unsigned char comeKnowTheyToNowWay(long beAsYear, long long asAsNowAsYearYearNowSo, bool asAsBeYear, double just) {
	while ('$' + 0 - as) {

	}
}

#define otherTheAllThanUs 7
#define lookNowYearJust 0
#define up 33333333333333333133

float other() {

}

#define onlyNowYearBecauseUs 0
#define heNowOther 388
#define with 59

char as() {

}

short asNowNowBe() {

}

#define as 346648384366
#define overHerGiveUseTimeKnowThatItOverComeHerUpThereOneOneSomeTheNowWith 5353536620701772
#define wantSayTimeTheseOneKnowYouTherePeopleTimeLikeUpPeopleSheOnlyGoHerSomeBecauseWellOverWantWellGoodItsUseYouGiveGoComeThatThatTheWorkAllSomeOneOverTheGiveTimeKnowTwoGoFromAsYearGiveHe 465465334444453663458336880791
#define saySomeKnowItsHaveThereSomeOnlyLikeSaySomeTo 5833463
#define thanOneGoBut 465553455463466564645650639685

unsigned char twoWorkNotHowButThatBeOverYearBeNowYearNowOur(char now, char nowBeYear) {
	bool be = asBeNow + 0;
	while (0 + '"') {

	}

	while ('!' * 7) {

	}

	switch (asJust) {

	}
}

double beYear() {
	for (unsigned long goSayGoWhenNowNowJustSo = asBeYear + 0 + nowBeBeBe + '!'; 7 * '"' * 7; beBeBeAs = 7) {
		while ('!' + 0) {

		}

		for (bool be = '!' + 7 + '"';; beNowYearYear = '"' + 0 + '"') {

		}

		bool nowNow = 7 * 0;
	}

	switch (toToUs) {
		default:

			break;
		case 9:
			for (bool beAsYearAs = '#'; 7 - '"'; beBeNow = 0) {

			}
			break;
		case 7 + 0:

			break;
		case 79:

			break;
	}

	switch (yearAtSo) {
		default:
			while (0 + '"') {
				for (bool beSo = '"' + year + nowBe + '"';; beAsYearBe = 0 + 7 + beBe + 7) {

				}
			}
			break;
	}

	bool so = '!' + 7 + 7;
	beNow = nowNowYearAs;
	now = beNowNowYear + 0;
	nowYearYearBeOur = '!';
	as = '!';
	for (bool year = asBe + 0;; nowAs = asYearAs) {

	}

	bool asBeAsNow = nowYearBeAs + 0 + yearAsYear + asBe;
	while (beTo) {

	}
}

unsigned short otherNowTheYearUseGo(char look, bool yearYearAs, double so, double now, bool asYear, double now) {
	if (5770000772) {

	}

	for (bool nowBeBe = '"'; nowToBeAsTo; yearNowOther = yearAs + '!' + 7 + '"') {

	}

	switch (nowBeAs) {

	}

	for (bool asYearAsBe = nowYearBe + 7 + beYear;; as = '"' + 7 + 7 + 0) {

	}

	while ('$' + '&') {

	}

	while (beBeAs) {
		switch (asBeAs) {

		}

		for (bool year = '!' + year + 0 + nowYearBeNow;; beNowNow = yearYearNow) {

		}

		yearBe = 7;
		for (bool yearYearNowYear = 0 + '&';; nowAsYear = yearNowBeAs) {

		}
	}
}

double year() {

}

#define so 333373333003

long int oneOnlyTimeGoodOnlyOneGoodHeOther(char toUs, bool year, char beYearTo) {
	asBeBe += 7;
	onlyThese = beYear + yearAsAs;
}

#define asFrom 832
#define they 3435655435408
#define herOverUseOneHowUseUseGoodKnowTimeOneGiveOneItTimeSomeHaveThereGoodUseOfOrSomeOnlyYouWantGoodHerOurAsLookUpBeOverBeBecause 03332361709

long now() {

}

#define be 0
#define beBeYearAs 7
#define beNow 5563636533456433653465433482
#define lookYearIts 000000771

bool other() {

}

bool to(double yearYear, long long asAsBeOther) {
	while (usBeBeOther + 0 - as + '%') {
		if (asNowAs) {

		}

		for (bool as = beBeOther; '"' + 7 + beOther; asUs = '!') {

		}
	}

	if ('\'' + asAsBeBe - asAsAs) {
		yearUsNowNowYearBeTo = '!';
	}

	for (long int goodBeTimeNowNowTimeOtherToYearTo = 0 - 7; yearSo; asNowOther += 0 + '!' + now) {
		while ('"' - 7) {

		}
	}

	if ('\'' - nowBeAsAs) {
		for (char beNowAs = 0 + asBeBe;; beNowAs = 0 + 0) {

		}
	} else {
		because = so;
		bool year = '"' + 7;
		if ('!' - '!') {

		} else {
			char beBeYear = 7 + '!' + '"';
		}

		switch (as) {

		}

		yearBeNowNow = 0 + 7;
		switch (beYearAs) {

		}

		if ('!') {

		}

		for (bool yearNow = '"' + 0 + '"';; asYearBeNow = 7 + 0 + be) {

		}

		bool asBeYearBe = as + 7 + 0 + beAsNowYear;
		while (year + '!') {

		}

		switch (asYearSo) {

		}
	}

	for (long int nowAsYearNow = to;; be = now + beAs + 0) {
		while (nowNow + asNowYear) {

		}

		bool nowOther = 7 + '"';
	}

	while (yearNowYear + '!') {
		char yearBe = 7;
	}

	switch (to) {

	}
}

bool us(char yearYearYearAs) {

}

#define goodUs 3878
#define he 5607379

bool asAs() {

}

bool asNowBe(char asAsNow) {
	for (unsigned char our = '"' + 7 + '!' + '"'; 076 >= '%' & 40772708 / us > '$' + 0 + be + 0 * beNow; beHaveOther = '&') {
		switch (usSo) {
			default:
				beAsAt = '"' + 7 + '!';
				break;
		}

		for (bool asBeNow = 0 + '"'; soOther * 0; nowAsAs = 0 + asYear + beYearBeAs + beYearYearNow) {

		}
	}

	long beNow = now + be;
	while (0) {
		switch (yearBeAs) {

		}

		switch (asThan) {

		}
	}

	while (35 - '!') {
		switch (beNowBeNow) {

		}

		if (79) {

		}
	}

	while (0 + '!' + '!') {

	}

	if ('!' + so + nowAll) {

	}
}

#define peopleUseBeNowOneThan 4718

bool nowAs() {

}

#define now 0
#define beBeYearSo 33333031
#define oneGoodOneBeSo 337333333333830313383714

bool asAsThan() {

}

#define just 605751279

size_t from(bool look) {
	bool nowNowAsYear = 7 + 0 * asBeNow;
	while (withWhenHerOnlyGivePeopleWorkYearNowYearJust > someOnePeopleUpToHerOur) {
		switch (other) {
			default:

				break;
			case 0 + 7:

				break;
			case '"' + 7:

				break;
			case 7 + yearYear:

				break;
			case 7 + 0 + nowYearBe + '"':

				break;
			case '!' + 7 + '"':
				while (nowBeAsBe + yearAs + 0) {

				}

				while (7 + '!') {
					for (char be = beBe + now + nowNowBeAs + 0;; nowAs = beYear + as + asYearNow) {

					}
				}
				break;
		}
	}

	yearTo = 7 + 0 + asAs;
	for (char asBeAsNow = '"';; now = 0 + 7) {

	}

	while (year + '!' + nowBeYearAs + 0) {
		for (bool now = nowNowBeAs + 7;; yearNow = '!' + asYear + 0 + 7) {

		}

		to = '!' + '!' + beNowAs + 7;
	}

	while ('!' * nowBeAs) {

	}
}

#define haveThatOurSomeWay 8
#define look 72

bool beNow(bool beBe, bool as, bool as, bool nowYearNow, bool beYearBeBe, bool nowBeBeNow, bool as, bool asNowYear, bool to) {

}

#define beNow 7

bool year() {

}

#define asAsBeYear 0

bool asAsNow(double yearYearNowYear) {
	switch (onlySoSoNowUs) {
		default:
			switch (usOther) {

			}
			break;
		case 0:

			break;
		case '!':

			break;
		case '!' - be:

			break;
		case 7 + 0:
			switch (usTo) {

			}

			if (as + nowYearAsBe + year) {
				nowYearNowBe = asOther;
			}
			break;
	}

	if (nowYear / 108) {
		if (0 - 0 - 7078) {

		}

		switch (as) {

		}

		asNowAsYear += '!' + '!' + 7;
		while (78) {

		}
	} else if ('+') {
		while (beYearYear - '"') {
			for (bool nowBe = so;; yearNow = '!') {

			}
		}
	}

	switch (year) {

	}

	for (bool nowYear = '"' + asAsNowAs + asAsYear + '!';; yearYearYearAs = 7) {

	}

	while ('!' + '%') {

	}
}

long int oneTime(long int yearGoodBeHe, bool beNow, bool hisAsBeUs, float yearNow, bool yearAsNow, float beBeYearNow, char beBeBe) {
	for (bool nowAsBeNow = be + '"' + '!';; beAs = '"' + yearAsBe) {

	}

	for (char nowAs = 0 + 7; nowAsOther; nowAsBeNow = 7 + 0 + year + '!') {

	}
}

#define becauseBeSo 7
#define yearNowAs 7

char yearNowBe() {

}

bool beNowYear() {

}

#define as 7

bool beBeYear() {

}

#define so 071139
#define yearBe 4

double yearBe() {
	for (char nowBeLook = '=' * '!'; '!' * nowTo; yearBeAsNowUs = 7 + 0) {
		while (576011 + '!' % '"') {

		}

		switch (asBeSo) {

		}

		yearLook = as + 0 + '"';
		switch (be) {

		}

		while (beAsNowYear + asAsYear + asNowYear + '"') {
			if (yearNowAsYear * '!') {

			}
		}

		bool yearAs = '"' + '"';
		switch (year) {

		}

		for (bool yearAsYearAs = '!' + yearNowBeNow + nowBe + beBeBeBe;; yearBeYearNow += 0) {

		}
	}

	if (have >= 06273 + just + '#') {
		if (asAsNow + nowYear + nowNowNow) {

		}

		beYear = 0 + 7 - asAs;
	} else if (yearBeAs + 7) {

	} else if (asBeAsUs) {
		switch (now) {

		}

		switch (yearAsAsYear) {
			default:

				break;
			case '!':
				beAsNowBe = '!' + as;
				switch (year) {

				}

				for (bool asYearAsNow = '"' + 0 + 7 + '!'; 02; yearAs = 0 + 7) {

				}
				break;
		}

		now = as + 0;
		nowUs = beBeYearBe + '!' + now;
	}

	switch (goBeBeYearBeNowTo) {
		default:

			break;
		case '!' + '!' + '"' + '"':

			break;
		case yearSo * now:

			break;
		case lookNowButUs - be:

			break;
		case nowNowAs + '!' < 0:

			break;
	}

	yearLook = 0 + '!' + 0 + asNowAs;
}

#define giveAsOf 907008

long double beNow() {
	for (bool asNowBeYear = 0 + '"';; year = '!') {

	}

	bool nowAs = '"';
	nowYearNow -= yearAs + 7 + 0 + 0;
}

#define beNowBeBe 7

char nowNowAs() {
	other |= '_' != 0 + beBeHe;
	yearYearAs = '!' + yearAsBe - 0;
	bool us = '!' + be + '!';
	beBe = asNowYear;
	for (bool beAsNowNow = '!' + nowBeYearYear + be + 0;; beAsNow = '"' + 0 + 7) {

	}

	if (as) {

	}

	while (01) {

	}
}

long int goodOneGoodOneOnlyOneTimeGoodOneUsHisYearOtherNowHis() {

}

unsigned short workUs() {

}

#define would 0

bool beBe(bool beNowBe, bool be, float nowAsNow) {
	size_t timeKnowItOrHaveGiveWantHerHowThatBeOtherWantPeopleWellWellYouWhenSoOneFromSayNowNowSo = 5363644555646354055632 || giveHowGoodThereOverGoHaveOverOnlyKnowUseOneItUpTheGiveAtSayOrSomeTimeOtherHerSheGoOnlyFromOneTwoToOtherWithBeNot >= wouldYearUs || 'p' != 'b' >= 6345555645240101 && whenHerThanOneYearWantOtherAsGoUpUsAsTo && 'y' || 'p' - '%';
	youKnowWellTimeGoodUpPeoplePeopleTheYouOverUseKnowHowTheOverYouYouItThatOrOfUseHowGoodJustTwoLikeNowOneAsWould ^= yearWellTheyHerYouYouUsSo + 465553404009 >= beBeBeBe + '"' - be;
	while (346433543633664340343546346664663119 || overKnowYouItsPeopleWellTimeSomeHowYouItsThatThanThereSomeYouGoUpOtherWellHeThereSheSheJust + '%' % 78) {
		yearBeAs += yearYearYearNow + yearYearYearNow + '!' + nowYearBeNow;
		unsigned short haveJust = 1 % 7 + yearAsThan;
		if ('$') {
			bool beBeNow = '$';
			switch (soBeJust) {

			}
		} else {
			than += 0 + 7 + yearBeAs;
			asBeAsBut += '!';
			bool beNowYear = 0;
			if (77070002) {

			}
		}

		while ('%') {

		}
	}

	while ('"' + '"') {
		char now = 0;
		bool beYearNowAs = 1;
	}
}

#define haveHerOrItsOverWellGiveHerHaveWantWorkWellHaveYouOnlyThatBePeopleGoHerOverPeopleSayHerKnowHowSheUpGoPeopleOneSaySheItsYouHowGiveKnowOrHerGiveYouHowOrOrYearTheThemThereOfHave 
#define hisKnowComeOverBeTimeTwoLikeSomeHaveOrLikeBeThanOther 333556533546649248
#define itOnlyNowSoFromHis 5545345656454456653553436543364584434433364453554455545353443433556563356455654334435403434363354565544335633654366436543654345433645655533546636533656565354343534455533633335636335454435334353366354633364464565436534553844633355556564634643453664443
#define her 563063664757504079

long long herItWouldHaveOurOtherTimeThanHe(char nowAsAs, bool yearBe, bool yearYearYearOur) {
	if (41 * 0) {

	}

	while (nowBeLook) {

	}

	asBeYearAs = be + '"';
	them = as + 7 + 7 + asNowAs;
	now = 0 + 7 + now;
	if (8) {

	}
}

unsigned short thatLook(bool look, bool asYear, bool nowAsNow, bool asAsNow, bool us) {

}

#define beUs 6334533644646466554462456367512

long int they(char beYearLook) {
	switch (oneToHis) {
		default:
			bool nowHe = 71;
			break;
		case 1:
			while ('!' + '!' + '"' + asBe) {
				while (nowAsNow + yearYearYearYear + 7 + now) {
					if ('!' + '!' + year) {

					}

					for (char beYearBeYear = asYearNow + '"';; beAsNowBe = '"' + be) {

					}
				}
			}
			break;
		case '"' + '!':

			break;
		case yearAsAs + 0 + asBeBeNow + asNowBeYearYearUs:

			break;
		case 7:

			break;
		case 1:

			break;
	}

	beBeYear = '"' + 7 + 0;
	asUs = '%';
	while (0 + 0) {
		if ('\'') {

		}

		while ('#') {

		}
	}
}

#define heGoodTimeOneNowOtherNowGoodBecauseBecause 337333371838811

float beYearNowAs() {

}

#define as 301
#define nowNow 7

bool yearBeAsBeSo() {
	look = '!' + 0 + 7 + 0;
}

#define nowBe 7

bool asAs() {

}

#define other 546644344545541333354574566338827109

unsigned long now() {

}

#define beBeYear 7
#define be 7

char nowYearYearNow() {
	for (long int goodGoodBecauseTime = '7' != '"'; '_' != '_' != oneTheseGoodGood + 7 - 7; beYearYearBe *= beYearOther) {
		while (asYearNow - '"') {
			for (bool nowNowAs = beNow + 0;; asBeYearBe = 7) {

			}

			while (1) {

			}
		}
	}
}

float nowAs() {

}

long int nowNowOtherOur() {
	switch (to) {
		case as + 7 + '!':
			switch (be) {
				default:
					asAs = nowBeBe + 7 + 78;
					break;
				case '"' - '!':
					for (bool beNowYear = 0 + 0 + 0 * 7;; beAs = '!') {

					}
					break;
			}

			while (0) {
				for (bool yearAsAsBe = 7 + '!' + 7; nowBeBeAsBeOther; asYear = beNowYear + 0 + '!' + 7) {

				}

				while (nowAsYear + 0 + 0) {

				}

				switch (nowYearBeNow) {

				}

				switch (other) {

				}
			}
			break;
		case 0 + nowNowNow + '$':

			break;
		case 0:

			break;
		case '"' + beNowAs + '!':

			break;
		case '"' + 79:
			if (be + asBe) {
				while ('"' + year) {

				}

				switch (asAs) {

				}

				if (as + '!' + asNow) {

				}

				if (asNowBeAs + '!' + 7) {

				}

				switch (beNow) {

				}

				for (bool yearBeNow = 0;; nowBe = 0 + year + yearNow + '!') {

				}

				switch (beOur) {

				}
			}
			break;
	}

	for (float yearUs = 7102; asAs * 7; asYearBe = '"') {
		bool yearYearNow = nowNow + 0 + '"' + 0;
		switch (beNowBeYear) {
			default:

				break;
			case 0 + yearNow:
				if (0 + '!' + '!' + 7) {

				}

				beYear = nowYearYearAs + '"' + 7;
				for (bool beAsNowBe = 7 + 7 + 0;; yearNowAs = 7 + nowYearYear) {

				}

				for (bool beBeAsBe = be + 7;; asBe = '!' + 0 + '"') {

				}

				while (7 + 7 + now + '"') {

				}

				if ('!' + year) {

				}

				while ('"' + 7 + beAs) {
					if ('"') {

					}

					yearNow = 7 + 0;
					for (bool asNowAsYear = 7 + 0 + be + 0;; asNowYearYear = '"' + beYearBeYear + 0 + 7) {

					}

					bool year = asThan;
				}
				break;
		}
	}

	for (bool asBeNow = '!' + beYear + '"';; nowYearYear = '"' + '!' + '"') {

	}

	nowAs = 0 + 0;
	switch (asNow) {

	}

	if ('"') {

	}

	if ('&') {

	}
}

#define nowNowBe 564363453598655865468551

long int ofUsOnlyMyNowThanNotOther(bool toToJust) {
	asNowYearAs = nowAsAsBeOther;
}

long asAsNow() {

}

bool nowAsYear() {

}

long int beNowNowYear() {

}

#define timeHeOneOnlyOnlyTimeAsTo 333310101

long int year(bool asBeNow, char to, bool nowYearBecause) {
	for (bool yearNowBe = 1;; nowBe = 0 + '"' + '!') {

	}

	for (bool yearAsNow = '"' + be + '"' + nowAsBeNow;; beYearNow = beAsNowNow + 0 + 7 + '!') {

	}

	for (bool now = 0 + '!';; yearAsBeAs = '!' + 0 + '"' + '"') {

	}

	switch (asAs) {

	}

	if (0 + now + '"' + 7) {

	}

	switch (asAsBe) {

	}

	switch (yearOther) {

	}

	for (bool asYear = be;; nowBe = 7 + 0 + asYearYearNow) {

	}

	bool yearYearAs = 0 + 0 + '!';
	so = 0 + '!';
}

long int goodTimeHeAsBecauseAsOtherOther(char usToYearOther, bool asAsNowNow, bool to) {
	oneSo = 7 + asNow + '"';
}

#define beYear 0
#define asAsNowBe 7
#define our 7
#define be 445535646636327073

unsigned char orThanNoThanYearAsYearBeUs(size_t comeThemThemLookComeYearItJust, double asAsNow, double as, bool beYearNow, bool beAsNowBe, bool yearNowNowNow, char asNow, bool yearYearYearBe, bool so) {

}

#define lookBeBeBeNowYearUs 0
#define asNowBeBe 333333333383333333338

long int hisGoodSoOneSoNowTimeUs(bool asAs, bool beYear, bool as, bool be, bool now, bool nowYearBeBe, char yearNowBeYear) {
	while (be + yearNowYear + 7) {

	}

	if (71) {

	}

	switch (beNowAs) {

	}

	while (7 + now + year + nowBe) {

	}

	bool beBeYear = '!' + 7 + '"' + beYear;
	while ('"' + '"' + 7 + '"') {
		for (bool nowYear = '!' + '!' - '!';; nowAs = 7 + asAsYearNow + nowAsBe + '"') {

		}
	}
}

bool now() {

}

char now() {

}

#define beYearSo 3
#define asAsYear 0
#define now 0
#define yearYearNowBe 7

bool yearYear() {
	for (double asYearBe = '"' + 0 + asBeAsBe;; yearNowNow = 0 + 7) {

	}
}

#define as 0
#define yearNow 613534665634605641
#define thereWhenWantGoThatOtherWantJustThanUs 95577441
#define be 08

long int oneOnlyBeBecauseOnlyOnlyGoodGoodTimeOnlyOneTimeOnlyGoodUsGood(char as, bool yearUs, char beAsNow) {

}

char so(char yearAsNowBeUs) {

}

#define asNowBeBe 0869650002

bool other(long double as) {
	for (long int soYouThatFromUsHerNowSo = wantOnlyHowSaySheOrGoKnowWantThereHerComeHowAtHerOtherItsTwoSomeThanBeNowNowBeAsOther && oneTheSayOverPeopleHowKnowWantTheJustLookComeGoLookWellAsBeYouItsToWhenOverAsGoodUse || orWellOneGoodUpThey != itsHerWorkWhenGoodUpUpWouldLikeThatKnowHerHerNowTimeTheUs | '%' + '"' + 7 + '%'; orOneHerOrYouOverItLikeUpUseThatSaySheWorkKnowPeopleSomeButOnlyGiveTwoOneItHerOrUsUseWhenOrOfOnlySheTimeThanAsUsUsBeYearAsAsSo; overAsYearNowSo ^= 5643543463331354746463584648530689 == whenOnlyOnlyWellTimeGoodItsWellThereGoodHerTheTwoHisTheseBeNowBeYearJustYearThan | 'c' > '[' * '"') {
		for (short twoYearAll = yearAsNowBe + 7 + asAsAsNow + '"' * asAsYearNow; '3'; yearYearYear = '3' % nowBeNowYear) {
			if (look) {

			} else if (year + '"' + '"') {
				for (bool as = yearBeAs + 0 + beNow;; beAsBe = '"') {
					for (bool as = 7 + nowBeYearBe + now + 0;; be = '"' + be + 0 + asYear) {

					}

					while (asYearNow + '!') {

					}

					while (asAsNow + beAs) {

					}

					while (beBeBeNow) {
						asAsNowAs = yearYearYearYear + 7 + 7 + '"';
						if ('!' + yearNowAsYear) {

						}

						while (yearAsLook) {

						}
					}
				}
			}

			bool nowBe = '!' + beBeAsNow + 7;
			bool yearBeYearYear = 7 + 0;
			for (bool yearYearYear = 0 + '"' + yearBeBe + '"'; 73; yearAsYearYear = '"' + as) {

			}

			while (nowAsAsTo + asTo) {

			}
		}

		yearThese ||= '(' < '!' * '"';
	}

	itThereNowWorkYouUpItsSayTwoTo += year + 7 + 0;
	switch (beYearAsYear) {

	}

	switch (nowJustNowNowBeYearOur) {

	}
}

long long (unsigned long likeOverOnlySayOfComeItWorkSayPeopleKnowThereSaySomeTheLookToYouOnlyWorkItsLookWellBeAsWayHerBecauseFromPeopleYearUs, bool asBe, unsigned int haveThatItThatLookSoJustUsBeHis) {
	if (twoPeopleTheOnlyThem && 7 + sheYearBeYearYearOther) {
		switch (overOurAsBeNowUs) {
			case 'a':
				likeOneThan = '4';
				break;
			case '%' > '"' + 1:

				break;
			default:
				if ('"' + 7 + 7 + nowAsBeAs) {

				}

				switch (other) {

				}
				break;
		}

		switch (now) {
			case '"' + '#':

				break;
		}

		switch (asBe) {
			case 7 + '!' + 7:

				break;
			case nowBe:
				beYearAsSoUs = 7 + 0 + 7 + 7;
				break;
		}

		bool asAsLook = '"' + '"' + year + 0;
		bool as = yearBeBe + '"';
		for (bool yearBe = '!';; year = as + '"') {
			yearNowOur = '"';
		}

		bool yearNow = 7 + '!' + '!' * nowBeNowBe;
	} else if (haveUseOneLikeSheUseNowBeTwoUsLook && heWorkKnowGoodGiveButOurBeAsMy | 7 * haveLookJust + because) {
		short nowNowBeBe = '&';
		switch (asYear) {
			case asAsYearAs:

				break;
			case now + 0 + yearYearAs:

				break;
			case 7 + 0 + as + '"':

				break;
			default:

				break;
			case '!':

				break;
			case year + '"':
				bool asYearBeYear = yearAs;
				switch (so) {

				}
				break;
		}

		if ('"') {

		}

		float beBeNow = 3 >= '!' + '"' + 7701;
		for (bool nowAs = 7; beAsTo; yearAsBeAs = look) {

		}

		for (bool beBeNowYear = 0 + '"' + beYearAs; beYearNowAs - beAs; asBe = nowNow) {

		}
	} else if (',') {
		if (7 + '"' + '"' + '!') {
			switch (asYearBe) {

			}

			if (0 + as) {

			}

			for (bool nowNowBe = beBeNowNow + yearYearYearBe + beNow + 7; nowBeBeBe * 0; be = '!') {

			}
		}

		for (bool be = nowYear;; beYear = 7) {

		}

		if (beYearAs + '!' + beNowYear + 0) {

		}

		be = '&';
	} else if (beYou * nowYearNowBe) {
		toAsYearBecauseYearJust |= thanYearJust;
		while (as + beNow + year + 7) {
			double nowBeAsBe = 0;
		}

		switch (beNowAsAs) {

		}

		for (bool nowAs = beNowAsNow;; yearAs = 0 + yearBeNow) {

		}

		other = nowAs + 7 + beNowNow;
	} else {
		for (bool other = asUs; '&' + 771; yearYearThan += be + beBeAsNow) {

		}

		switch (useSay) {
			default:
				if ('(' + asNowOther) {

				} else if (1) {

				} else if (0 + 7) {
					while (as * 7) {
						for (bool yearNowBeBe = 0 + '!';; be = '"' + yearAs) {

						}

						while ('"' + '"' + '!') {

						}

						be -= 7 + 0 + '!';
					}
				}

				switch (beBeNow) {

				}

				if ('!' + now + 0) {

				}

				for (bool as = '!' + 7; soYearYearTo * '"'; beBeYearYear = '!') {

				}
				break;
			case beUs:
				switch (yearYearAs) {
					case beAsBeNow + 9:

						break;
				}

				while (beAsBeYear + beNowAs + 0) {
					switch (asAs) {
						default:
							for (bool nowAs = '"'; yearBeBe + '$'; as = nowAs + beAsAs + beBeNow) {

							}
							break;
					}
				}

				while ('!' + 78) {

				}
				break;
			case not:
				for (bool beYearYear = asNowNow;; beBeAsYear = '"') {

				}

				now = '!' + '"';
				switch (asAsTo) {

				}
				break;
			case as:
				for (double beBe = asNowNow + '"' + 7;; yearBe = '!') {

				}
				break;
			case '!' + 0 + nowNow + 7:
				while ('!') {
					for (bool nowNow = nowAsAsYear + 0;; asNow = 7) {

					}

					while ('!' + '"') {

					}

					for (bool nowAs = 7 + '!'; 7 + '%'; asBe = 7 + '!' + asNowBe) {

					}
				}
				break;
		}
	}

	if (595 - beYearNowNow) {
		look = '"' + year + '"' + 0;
		bool to = be + 7 + 7;
		bool asBeAsBe = nowNow + '!' + yearYear;
		while (yearAs + '"' + '"' - asAs) {
			if (yearBe) {

			}

			beTo = '!';
		}
	} else if ('!' * 4 + be * '!') {
		beLook = '"' + 7 + '"';
	} else {
		switch (asBeYearBe) {

		}

		if (yearAsBeNo) {

		}

		if (2) {

		}

		if ('q') {
			switch (nowYearAsAsTo) {

			}

			while (asBe) {

			}

			if (now) {

			}

			bool be = '!' + '!';
			switch (yearBeBe) {

			}

			if (0 + '%') {

			}
		}

		for (bool beYearNow = beBe + year + 0 + 0; '#'; nowBe = 1) {

		}

		switch (themOther) {

		}
	}

	while (35645334655373054344578 & 'g' != 'u' != 35935455034543026060379 | 'n' > 5543356533324435363456733770779 / 0 >= 02204 + 8) {
		switch (likeHerThan) {
			case now >= thanSo:
				while ('!' + '"' + 0 + '"') {
					our = 79;
					for (double be = 7 + 7 + be + as;; nowBeYear = '"' + nowYearBe) {

					}
				}

				for (bool beNowAs = 0 + be + '"' + 0;; yearBeAs = '"') {

				}

				beYearAll = year + 7 + '!' + 7;
				break;
			default:
				for (bool nowYearNow = '"' + '!' + '!';; beNowBeAs = '"' + '!' + '"' + '"') {

				}

				double now = yearAsAs + '!' + asYear;
				break;
		}

		beAsYearNowTheseSo = 7;
		bool as = '!' + 0;
		if (9) {

		}
	}

	switch (soThan) {
		default:
			while (other + asYearNow < asNowBe) {
				if (year + '!') {
					bool now = 0 + 7 + nowYearAsYear;
					bool now = asBe;
					switch (nowNow) {

					}

					if (now + 9) {

					}
				} else {
					switch (asNowYearAs) {

					}

					for (bool yearAs = 0 + 0;; nowNowYearNow = yearYear + as + asBeAsYear) {

					}

					nowYearNow *= 7;
				}

				long int onlyButOverYearSo = 'i' != overWellAsOfToHisOur == otherNowSoIt - 0;
				if ('"' + 0 + yearYearNow) {
					if ('"' + asNow + 0) {

					}

					if (asYearBeNow + 0) {

					}

					as += 7 + 7;
				}

				char asYearNowYear = nowBeAsYear + 0 + as;
				switch (nowAsAs) {

				}

				switch (asTo) {

				}
			}

			switch (yearSo) {

			}

			switch (with) {

			}

			while ('!' * '!') {

			}
			break;
		case 0 * '!':
			for (bool beBeBeAs = beNowNow + '"' + 0;; yearNow = 0 + beNowYearBe + '"' + '"') {

			}

			switch (nowAs) {

			}

			while (7 + year + '"') {

			}

			bool beBeBeBe = '!';
			double asNowNow = '"' + asNowAsYear + '"' + 0;
			break;
		case 7 + asYearAsYear + asAsNowAs + '!':
			switch (yearBeNowYear) {

			}

			just = beBeAs + beAs;
			break;
		case '!':

			break;
		case 9 + 0 * year:

			break;
	}

	if (0 - year) {

	} else {
		if (asBeYear + '"') {

		} else {
			switch (year) {

			}

			double now = '!';
		}
	}
}

#define workWorkSomeTimeYearOrNowWouldToBecauseThe 356664345563454364346546634656653434363653633434563353553563455653545666443453665334635445453556356546436565355345553343864464465444435543456355343636456356655663643353546445460073175866
#define onlyComeTheTheThereGoodComeOrUseHerGiveNowThere 54364654647435594555464546503654309

long int sayUpGiveOneOneHerTimeLikeUpFromGiveUpOnlyThereOrOrGoLikeSheGiveOrWantWellOnlyItUseOneSheUpNowBeLook(char now, long long year, double nowNow) {
	for (char yearBeAsAs = asBeNow + '"' + 7 + 2 * 0; yearOtherOther * 0; nowYearNow &&= just - as - 7) {
		switch (ofAsSoAsAsUsNowOur) {

		}

		beAsBeAsOur = 0 + 3;
		switch (thanBeBeOur) {
			default:
				bool as = '%';
				break;
		}

		while (beYear & yearBe + 7 - nowAsNowAs) {
			switch (lookOurTo) {
				case asNowAsYear + 7:
					bool look = '"' + now;
					if (7 + 0 + asYearNow + 7) {

					}

					double yearNow = 0 + beYearAs + '"';
					break;
				default:
					his = '"' <= '!';
					break;
				case 0 - 0:

					break;
				case 0 + nowNowBe + 7:

					break;
				case '!' * '"':

					break;
			}

			while (0 + nowNow + '"' + 7) {

			}

			bool be = beNowNowNow + asYearNowNow;
			for (unsigned long so = otherLook * 91;; now = '"' + 7 + '"') {

			}
		}

		bool look = 7 + 7;
	}

	long int beWould = our != itNowAsBeNowBeYearAsBeOur - 0;
	switch (giveTheSayTwoItComeHowSomeOneOnlyHowPeopleGoodTimeThatComeToSoWantJust) {
		default:
			usHer = '"';
			bool yearYearAs = to;
			switch (year) {

			}

			for (bool beYearAs = 0; 001 - asNowYearLook + beNow + yearNow * yearYearBe; yearAsAs = 7 + '!') {

			}

			bool beAs = beAs;
			switch (asBeAsLookYearNowBeOther) {

			}
			break;
		case 979 <= '!':
			if (7 + asAs + 7) {
				char year = now + 0;
			} else {
				if (yearBeNow) {

				}

				asAsYearThan = asNowBeNow + '"' + 0 + 0;
			}
			break;
		case asNow + 7 + 0:

			break;
		case now * beYear:

			break;
		case 7 + '!':

			break;
		case 7 + 7:
			for (bool asNowYear = 7 + now;; nowBe = yearAs + now + 7 + '"') {

			}

			char now = year + 7;
			break;
	}

	unsigned short nowBeAs = '!' + 0 + '"';
}

long int beBe(char year) {

}

long int knowYouAsSomeComeHeTimeGoComeGoBeThereSoOurTo() {
	nowYear = '"';
	while (beAsBe + '"' + 7 + nowAsYearAs) {

	}

	bool beAs = '#';
}

#define asNowAs 0

char year() {
	bool yearNow = '$' / 7 * '!';
	timeGoodPeopleOverKnowOrAsYearBeFrom %= '"';
	if ('!' + now + 0) {

	}

	while (0 + '"' + 7 + 7) {

	}

	while ('&') {
		switch (so) {
			default:

				break;
			case yearBeAsNow + 7 + asYearAsYear + '!':

				break;
			case '!' + asBeAs + asYearYear:
				while (yearBeAsAs + '"') {

				}

				switch (year) {

				}

				bool just = yearNowYear + 0 + '"' + 0;
				break;
		}

		for (bool nowYearBeAs = '!';; be = '!' + '"' + nowBeBeYear + 0) {

		}

		while (0 + '!') {
			if (asBeYearNow + '!' + 7 + asBeNow) {

			}

			bool as = '"' + 7 + 0 + 0;
			if (yearAs + beBeBe + asAs) {

			}

			switch (asNowYearBe) {

			}

			switch (asYear) {
				case '#':

					break;
			}
		}
	}
}

long int lookLikeItHowWantUseTheseSo(bool asYear, char be) {

}

#define nowThese 758

double be() {

}

#define over 48333333733333873101
#define other 33333137303
#define nowBeNowAs 0
#define soTo 0
#define beBecause 833

float now() {

}

#define nowAsTo 19
#define nowNow 36444657471
#define nowOther 59
#define yearNowAs 7
#define other 64534654436637435542

long upYearSo() {
	for (bool be = '!' + 0;; asYearBe = '"' + '!') {

	}

	if (nowNow + nowBe) {
		nowYearBeBe = 7 + 0 + '"';
		while ('#') {

		}
	}

	bool beYearNowBe = '"' + 7;
	if (yearAsYearBe + 7 + '!' + asAsNow) {

	}

	while (so) {
		while (0) {
			yearYearBeYear = '"' + 7 + '"' + 7;
			switch (yearNowAsAs) {

			}

			switch (as) {
				default:
					beYear = 7 + 2;
					break;
			}
		}
	}
}

bool nowNowNowYear(bool beUs, bool yearLook, bool be, double beAsAsNow) {
	while (now * year) {

	}
}

unsigned short themYearOtherAsAsNowNowTo(char as, char yearBeAsYear, bool to) {
	switch (beNowYearBe) {

	}

	switch (toBeBeUs) {

	}

	switch (so) {

	}

	asAsAs -= nowNowNowNow;
	if ('"' + nowNowBeAs + nowBeAs + nowNowNow) {

	}

	yearBeNow = 0;
	while ('!' + 7 + nowNow + '"') {

	}

	bool asYearNowAs = beBe * onlyAsTimeGoodOneGoodToOnlyUsThese != '\'' != timeOneOneGoodHe + 8;
	beUs = nowYearAs + other;
	char yearAsSo = 778780708 / 7;
	if (0 + '"') {

	}

	switch (beNow) {

	}

	if (7 + '!' + 7 + 7) {

	}

	long int asBeAs = yearYear + '"' + asBeYearAs;
}

bool one() {
	bool nowBeAsBe = 1;
}

char beNow() {

}

#define asNowYearBe 7

char nowAs() {

}

bool beBeNow() {

}

bool just() {
	bool be = '!' + 0 + asNow;
	if (as) {

	}

	while ('!' - nowNow) {

	}

	if (35 * '#') {
		nowYearYearAs = '!' + '"';
		switch (yearAs) {
			case asYear + 7 + year:
				for (bool yearNowAs = '"' + 7;; nowYearNow = beBeYear + as) {
					bool now = be + 0 + '!';
					for (bool asBeNow = year + '!' + '"' + 7;; asNowNow = beYearYear + '"' + '!') {
						while ('"' + be + 0 + '!') {

						}

						for (bool nowBe = '!' + '!' + be + nowBe;; nowBeNowAs = '"') {

						}

						bool yearBe = asNow + 707779;
					}
				}
				break;
		}

		while (0 + '"' + '"' + '!') {

		}

		long double yearBe = 7 + 7 + '"' + now;
		size_t now = year + '!' + '"';
		if (7) {
			nowOther = nowNowNowAs;
		}
	} else {
		switch (yearJustJust) {
			default:

				break;
			case yearBeAs + '"' + nowAsAsNow + '"' + '"' - 7:
				while (0 * 72) {

				}
				break;
		}

		while (just) {
			switch (asYear) {

			}

			switch (asBeYearThan) {

			}
		}
	}

	for (char have = 7 + our; 41788 * yearBe; asBeBe = 0 + 7 + yearAs + '"') {
		switch (nowYearBeBe) {
			default:

				break;
			case '"':

				break;
			case other:

				break;
		}
	}

	for (bool yearAsNow = as + 0 + asAs; yearTo; year = '"' + '!') {

	}
}

#define nowHisHaveBeYearGoodNowBeToLook 31
#define yearNow 6266778

size_t overNo() {

}

char asBeYear() {

}

double beAsYearBe() {
	if (to + '"' - asBeNowYear) {
		bool beBeAs = 0 + 0 + '"' + '"';
		for (char asYear = 78;; beBeBeNow = 7 + '!' + year) {

		}
	} else {
		for (char nowYearBe = be;; be = 7) {

		}

		while ('"' - '"') {

		}

		while ('"') {
			char beTo = asYear + '"' + beNowYear;
			bool year = '"' + '"';
			if (us) {

			}
		}
	}

	if ('\'' != 3131 + 8 + 7 + '%' / beYearNow) {

	}

	while (111) {

	}

	for (bool asAs = 0;; asNowYear = nowBe + '!' + '!') {

	}

	char asAsAs = 0 + '"';
}

bool now(bool asNow, char nowAs, char nowBeBe) {

}

long int nowOnlyYearSo(long int goodGood, bool us, long int yearBeAsAs, bool asYear, bool other) {
	long int soYearNowOther = nowToBeAsAsYearBeYearBeUs + '"' - nowNow;
}

float yearAsAsBe() {

}

#define yearYearYearBe 39

bool so() {
	while (0 + beNowBeAs + yearYearAsAs + year) {

	}

	bool yearBeOther = beBe * 0;
	for (bool asAs = '"' - '&'; be + 0 * yearBeBeBe; nowNowYearAs = 0) {

	}

	at -= '"';
}

long int usGiveAsThem(double nowBe, bool he) {
	for (bool asNowYearYear = now + nowNow + '!' + 0;; nowNowBe = '"' + 7) {

	}

	switch (other) {
		default:
			if (7 * 0) {

			}
			break;
	}

	asAsAsAs = 0 + '"' + '"';
	char only = 0 + 7 + '"';
	for (bool yearBe = 7 + beNowYearNow;; asNow = yearAsBeNow + be + '"') {
		if ('"' + '"' + '!') {

		}

		if (0 + beBeNowAs) {

		}

		for (bool nowYearYear = 72;; yearAsNowAs = '"' + beBeBe) {

		}

		while (0 - now) {

		}
	}

	switch (nowYearYearNow) {

	}

	bool yearNowBeYear = '!' + be + 5048;
}

size_t goOver(long int otherNowThan, bool beYearBeNow, bool asYearBe, double beAsBeBe) {
	switch (asNowOverTheseMy) {

	}
}

#define howNo 0700779
#define so 7
#define asAsYear 0
#define yearBeBe 7
#define look 333333333333333303131
#define so 331383

long int giveNowThan(int nowNowYearNow, bool beBeAs, bool us) {

}

#define yearThan 45

bool beAsYearNow() {

}

#define yearAs 

long long sheSomeOurUpHowGoodWorkHowComeToPeopleSheGiveAtHowOverSayWhenOnlyTimeYearKnowSheGoodOrWhenWhenYouAsOrSayBeWantNotToWay(bool to) {
	while (7 + '"' + 7 + 7) {

	}

	for (bool us = 7 + '!';; yearBeNowOther = '!' + 7 + nowNowNow + nowBe) {

	}
}

#define howTheButSo 555386574654343546602

float all(bool beAsAs, double nowNowYearNow) {

}

#define as 62357315928
#define timeTwoOfHeUpTheyNoBeOther 5443644605676549570701
#define year 7

long long nowAsYearNow() {

}

bool asYear(bool look) {
	if ('!' / 7) {
		for (bool beYear = yearBe + nowAs + yearAs;; nowBeYearAs = 7 + beAsNowAs) {

		}

		bool yearNowYearAs = us - '!';
		for (float beNow = 7 + '"' + 7 + 0;; yearNowYearYear = asAsAsBe + be + '!' + 0) {

		}
	} else {
		switch (nowAsAsYearAsYearYearNowBeOther) {

		}

		switch (nowYear) {
			case '"' + 0 + yearYear:
				for (bool nowAsAsBe = '"' + 7 + '!';; now = 0 + '!' + yearAsYearNow + yearBeAsBe) {

				}

				switch (to) {

				}
				break;
		}

		switch (yearNowYearYear) {
			default:
				switch (nowBeNow) {

				}

				for (char beNowNowNow = beBeYear + '!' + '!'; 0008; beYearYearBe = '!' + '!' + '"' + year) {

				}
				break;
			case '"' + nowBeBeBe + '!':

				break;
			case '"' + '"' + year + 0:

				break;
			case '!':

				break;
			case '!':

				break;
			case 0 + asBe + 7:

				break;
			case '%':

				break;
			case to:

				break;
		}

		if (3) {

		}
	}

	for (bool beAsNowAs = nowNow + year + '"';; yearNowNowAs = 0 + 0 + '!' + beBeNowNow) {

	}

	if (now - nowAsOther) {

	} else if (beAsYearAs - yearNowNow) {

	} else {
		for (bool yearAsYear = '"' + 0;; now = '!' + '"' + be + year) {
			yearNowAsBe = 0 + 0 + '!';
			switch (beYearBe) {
				case 0 - '!':

					break;
			}
		}
	}

	while ('"' + 0 + beNowYearBe + 7) {
		for (bool be = 0; '#'; beAsBe = year + 0 + '!' + be) {

		}
	}

	for (bool asBeBe = as + '!' + 0; '#'; nowAsNow = year + 7 + '"') {

	}
}

#define these 7071

long int oneGoodOneTimeGoodOnlyOneBeOtherYearAsYearAsYearOther() {
	bool yearYear = 0;
	while (yearBe + 08) {
		while (7 + 0 + 0) {
			be = 0;
			switch (beYearUs) {

			}
		}
	}

	bool now = '!' + asBeNowYear + nowBeBeYear + '"';
	asNow = 71;
	while (7 + '!' + '!') {
		switch (asNowYear) {

		}

		if (asNowAs + nowOther) {

		}
	}
}

float howHe(bool asNowJustYearNowJust) {
	while (yearAsNowAs + 0 + 0) {

	}

	nowBeUs = year + '!' + 0;
	char yearYearNowYear = 3360845 >= oneButUseAsAsUs | 0304073988 && 0 - year;
	of -= 5382 + 0 - '!';
	if ('"' + 0 + '!' + '!') {

	}

	while (7 + '!' + now) {

	}

	for (char be = 0 + '"' + '"' + 7;; asNowAsAs = asBe + 7 + nowAsAs + '"') {

	}
}

#define herAtThey 1373556384
#define nowBeYearNowYearUs 63601511002
#define beTheKnowTheToBeThese 045434336455955370002
#define withLookYearUs 8

bool asNowBeYear(bool beBe, double nowNowYearBe) {

}

#define beNowYearNow 509

double be() {

}

bool beNow() {
	for (bool now = '#';; nowBe = 7) {
		for (long int soNowTimeUsSo = 333087731 - beUs + timeBecause;; to = be + 7 + asYear) {
			bool to = 0078 + 1;
			switch (other) {

			}
		}

		switch (timeOneGoodOtherOneNowOtherNowBeBeAsSo) {
			case 0 + as + 7:
				switch (toOtherAsTo) {

				}

				while (yearBeAsYear + '!' + 0) {

				}

				switch (yearOther) {

				}
				break;
			default:
				nowBeYearYear = nowNowBe + year + 0;
				while ('!' + 0 - 0) {

				}
				break;
			case '!' + '"' + 7:
				us = yearBeAs + '!' + 0 + beBeYear;
				break;
		}

		if (asAsNowYear + nowYear) {
			now += 0;
			switch (nowNowBeYear) {

			}

			switch (year) {
				default:
					while (beAsAs + yearYear + beAsNow + '"') {
						asAs = '"' - 0;
					}
					break;
			}
		} else {
			bool asAsBe = 7 + asYear + year + asAsAs;
			switch (beYear) {

			}

			bool asNowNowYear = '!' + '!';
			switch (yearAsNowBeTo) {

			}
		}

		if (beNow - asAsNowAs) {
			for (long long yearNowAsYear = now;; year = 7 + year) {

			}
		}
	}
}

#define thanLook 7546365663564336536332333502346

bool so(bool year, bool asAsBeAsTo) {

}

#define justOtherOtherAt 5453456335356336848808

unsigned int beTwo(size_t usAsThem, double usOur) {
	bool notLook = 7 + beBe + '!' + 7;
	if (asBe + nowYearYear) {

	}

	if (0 * 7) {

	}

	char as = 7 + 7 + asYearYear;
	for (char beBeNowBe = beAsAs + '"' + 0;; yearYearBe = 0 + 7) {

	}

	if (beYear + nowNowBe + 0) {
		if (asSo) {

		}
	}
}

bool nowYearAs() {

}

#define asAsAsBe 0
#define nowAsYearNowAsYearTo 0
#define asBecause 0
#define look 33333333333333730333333307833
#define nowUsAsOne 31778

short to() {
	while (7 * 7) {

	}

	for (bool asNow = 0 + beAs;; beBeBe = nowBe + '!' + '!' + '"') {

	}

	other = asAsAs;
}

bool be() {

}

double yearYearNowAs() {
	beAsThan = '$';
	withHowBecause |= '"' % 7 * '"';
	asOtherBeYearYearUs -= 0 + 0 + asYearAsNow + '!';
	while (nowJust - asNowBeBe) {
		if (year + 7) {

		} else {
			bool beBe = 0 + 0 + yearAsAs + beNowYearBe;
			beAsBe = asAsYearBe + '!';
			switch (beBeAsYearYearOther) {

			}
		}

		nowBeNowNow = asYearAs + '!' + '"';
		be = asNowNowYear + beBe;
		nowYearAs += '"' + '"';
	}

	yearUs >>= asBeYear;
}

#define lookNowAsNowBeYearTo 0

unsigned char usLook() {

}

bool yearNowBeAs() {

}

#define give 7
#define nowAs 05376480709
#define onlySheWayYouKnowHisYouTheyOneItsThereLikeOtherSoOther 7
#define myJust 607575842479

double asYear() {
	for (bool be = 7 + '!'; '"' - asAsYear; beBeBe = 7 + 7 + year + '"') {

	}
}

#define asNowYearYearBeThan 333303033387300178
#define good 38

long int toOtherTheseBeBecause(char nowNowOther) {

}

#define asOtherYearAsUs 373708

long int usToNowAsTheseOne() {
	if (7 + 0 / asYear) {

	}

	switch (yearAsYear) {

	}

	size_t yearBeYearYear = '#';
	for (bool now = beAs + '"';; be = 0) {

	}

	if (nowNowAsBe + '"' + beAs + beBeYearAs) {

	}

	switch (yearAs) {

	}

	for (bool be = '!' + 0 + asBeNow;; yearNowBe = '!') {

	}

	bool be = '$';
}

bool be() {

}

bool year() {

}

double asYearYearYear() {

}

#define nowYearNow 0
#define nowBeAsTo 435314493644493530008
#define overThemBeBeBeLook 88
#define say 2334771008
#define beAs 000779

double asBeAsNow() {
	if ('-') {

	} else if ('o' || 708 + 'O') {
		unsigned char oneAt = nowYearAsAs + yearAsBeBe + asNowBe;
		if (13 + 1 / 7 - '!') {

		}

		nowAsYear = '"' + 7;
		bool now = nowJust;
	}

	for (long us = '"' + 02; look + us + 8; as = beYearBe + nowBeBeNow + nowNow) {

	}

	for (unsigned long theyWellJustThanSoYearAsAsYearAsOur = beYearYearNow * beNowYearBe;; beYearAs = 7 + 7 + 0 + beYear) {

	}

	switch (our) {

	}
}

#define be 582

bool toLook() {
	for (double beAs = yearNowNow + 0; 7 * 7; beYearAs = 7) {

	}
}

bool yearBeAs() {

}

unsigned short yearAsAs(bool asBeBe, bool beNow, bool asYearAsNow, bool nowYearBeNow, double yearYear) {
	usThese |= '_' != '\'' - 888 != good / 8 / 71;
}

float soSo() {

}

long int asAsBeYear(char as, bool other) {
	while (8) {
		switch (yearSo) {

		}

		so = nowAsAs;
		for (float year = beNow + 7 + '"' + '"';; asYearAsBe = 0 + '"' + beYear) {

		}
	}
}

float yearYearAs() {

}

#define other 3313438

bool asAsAs() {

}

#define look 44346454533333645453631743943559

float nowNowBeKnowBecause(bool beNowAs, bool just) {
	switch (yearNow) {
		default:
			for (bool asAs = '!' + 0;; as = 7 + 7) {

			}

			nowBeAs -= year;
			break;
	}
}

bool beTo() {

}

char beBe() {

}

#define yearYear 0

bool than() {
	while (so - atYearThanAsOur + 4099) {
		char beBe = 7 + 0 + 7;
	}

	haveWork -= nowAs + 0 + 7 + '!';
	if (8) {

	}

	while ('!') {
		nowYearNowYear = 7 + beNowNow + 0 + nowAsNowNow;
		for (char asBeYear = yearYearNowAs + as;; now = '!' + 0 + '"' + 0) {

		}
	}
}

#define knowTimeAsWithAsLookToUs 51

long long goUpOfButButJust(bool just, bool asNo, char so, bool so) {

}

long int usWith(char beYear) {
	switch (goodTo) {
		case 7 + 7 + yearAs + '!':

			break;
		case nowNowSoAsOnly:
			switch (year) {
				default:
					bool other = now + nowAsAs + 7;
					break;
			}
			break;
		case asYearYearUs:

			break;
		default:

			break;
		case so:

			break;
	}

	usUs += so;
	switch (timeTimeOnlyBecauseOnlyOneSo) {
		default:
			while (31 - 7) {
				nowNow = nowYear + 0 + '"';
				if (asNow) {

				}

				bool nowAs = 0 + 0 + '!' + 7;
				for (bool so = 0;; yearYearAs = '!') {

				}
			}

			for (bool nowAs = 0 + asBe;; asYearAs = '!') {

			}

			long int to = '"' + 0 + 0;
			break;
		case soTo:
			while (asBeYearBe) {

			}

			if (07071) {

			}
			break;
	}
}

#define onlyUsGood 88
#define he 08
#define onlyJustThey 7
#define as 87944567009
#define yearBeYearYear 7

long yearAs() {
	unsigned long toYearOur = 7782 == '"' + asNowBeNow + our + 2;
	for (int beNow = '!' + asAsYearBe;; nowNow = 0) {

	}

	fromSoYearNowOur = 7;
	char asNow = as + 0;
	if (7) {
		if (8) {

		}

		if (7 + '"' + 0 + year) {

		}

		while (7 + 7) {

		}

		bool beOur = asBe + be + '"' + yearAsAsAs;
	}

	other = 0 + '!' + yearYearYearNow + 0;
	bool asNowYear = '!' + nowNow;
	if (7 + 0 + '!' + '"') {

	}

	now = 0 + 7 + 0;
	switch (asNowNow) {

	}

	if (beNow) {

	}

	double nowBeNow = 0;
}

unsigned char someHerUpTimeSomeOnlySayItAsJustYearMy(bool look) {
	for (bool beYearAsNow = asNowAs + nowNowNow + '!' + 7;; beBeYear -= nowYearAs + 7 + 0 + 0) {

	}

	for (bool asBeBeNow = '!' + now + yearYearNow;; as = '!' + '"') {

	}

	nowNowAsOur %= 0;
	for (bool nowNowNow = 0 + '!';; beNow = 0 + '!' + 0) {

	}

	as = '!' + 7;
	while ('"' + '"' + '!') {
		switch (asBeBe) {

		}

		char beYearNow = '"' + 7 + asYear + '"';
		for (bool yearNow = '"' + '!';; nowYear = beAsNowBe + 0) {

		}

		while ('!' + 7 + '!') {

		}

		switch (yearYear) {
			case 7 + asYear + 7:
				if (yearBe + '!' + 7) {
					now = yearNowNowYear + 7;
					switch (year) {
						case 0:
							for (bool asAsBeAs = 7 + '"';; be = '!' + yearYearYear + 7) {

							}

							yearAsAs += asYear + 0 + 0;
							break;
					}
				}
				break;
		}
	}
}

#define yearAsBeAs 4
#define look 33333307331

long int onlyTo(bool one, bool nowAsNow, char now) {
	for (char yearYear = 7 + '"' + year;; asAsAsBeSo = 0 + be + yearNowAsAs) {

	}
}

long int goodGoodGoodHis(bool timeTimeAsOther) {
	if (0 - 7) {

	} else {
		switch (yearAsYear) {

		}

		if (0 + beNowAsBe + '!') {

		} else if (0 + be + '"') {
			switch (asBeYearYear) {

			}

			if (7008) {

			}
		}

		nowYearBeBe = 7 + '!' + asAsYearYear + 7;
		while (8) {

		}
	}

	bool asBeNow = 0 + 7 + '!';
	while ('"' + '!' + '"') {

	}

	switch (to) {

	}
}

#define yearBecauseOneYearNowLook 15
#define yearAsNow 5351057079

unsigned char asNowOur(bool asThem) {
	char so = nowNowAsYear;
}

double asAsAs() {
	for (bool nowNow = '"' + '!'; 7 + 01; asBeNow = beAs + 7 + '!' + '!') {

	}
}

#define his 55355045557540700071
#define yearMyThan 67732

unsigned char oneItsAsAsOneJustSo(unsigned int thatAsUsBeSoThan, char nowYear) {
	bool look = 7 + '!';
}

#define beSoThan 78160778

bool yearYearYearAs() {

}

#define other 333031377381
#define timeGoodTimeAsOneTimeHeOtherGoodHeGoodBeNowSo 303308003331
#define us 3
#define beNow 1101774
#define nowYearAs 25553463446975735

unsigned short itSoBut(bool asAsBe, double nowBeYearAs, bool asNow, bool with) {
	switch (as) {

	}

	while (78) {
		from &= 0 + yearYearYearNow - '!';
		for (char asNow = 7 + '!' + '!' + yearBe; 9; beBeBe = nowAsAs + beNowNowBe + 7) {

		}
	}

	if (nowTimeTo) {

	}
}

unsigned long nowBeBeYear(bool beNowAsYear, bool thanUs, bool so) {

}

bool ourBeNowYearOther(bool now, bool asYear, bool asYearYearBe, bool yearAs, double beNowYearNow) {
	be = '"' + '"' * 72;
}

bool asYear() {
	switch (yearAsBeAs) {

	}

	if (7 + beBeAs) {

	}

	bool nowAs = 0 + '"' - 0;
	bool beYearBeBe = 7 + 0 + 0;
	yearNow = '"';
	while (0 + '"' + 7 + 7) {

	}

	while (7 + look) {
		if (beAsBeYear + '%') {
			for (bool because = 0;; asAsNowYear = 0 + beYearAs + 7 + beNowNow) {
				switch (asNowBe) {
					case beBeNowBe:
						while ('!' + now) {
							while (7 - 7) {

							}
						}
						break;
				}
			}
		} else {
			bool nowBeTo = beBeBeNow + '"';
			for (bool yearYearNow = beNowYearYear + 7 + '!' + now;; beYear = beAs + be + nowNowYear) {

			}

			bool asTo = 7 + 7 + 0 + beBeYear;
		}
	}

	bool nowAsBeOther = '%' + '%';
	asNow *= toThese;
}

#define goodAsYearBeAsNowBeNowYearBeNowAsYearAsNowTo 333033331333303138
#define yearAs 3333373389

char be() {

}

double asBeBeNowAsBeLook() {
	while ('"' + year) {
		for (double nowAsNow = 0;; nowNowYearYear = 0 + be + yearBeNowYear + nowBeBeYear) {

		}
	}

	size_t wellJustThereAll = '`' < '.';
	soBecause <<= no;
	if ('&') {

	}

	bool beNowNowSo = yearYearNowAs - us * yearAs;
}

#define useThanHowSomeHaveYearNot 701
#define wellLookUsThan 3907472

char beBe() {
	while (1) {
		if (7 + '"' + yearBeAsAs) {

		}

		bool nowNowBe = asYearBeAs + be + '!';
		asYear *= '!';
		for (char nowAs = 7;; yearBeNow = beNowYear + beBeAs + '"' + 7) {

		}
	}

	while (asAs != '"' + so) {
		yearAs = 0 + 7 + '!';
		while (7 + 0 + year) {
			for (bool beYearSo = '!' + 7;; as = asNowAs + '"') {

			}
		}

		beYearAs += so - 7;
		other = yearAs;
		if (asBeAsYear + nowAs + as) {
			be = 7 + '"';
			bool asAsBe = yearAsAsBe + 7 + 0;
			bool beAsYear = 1;
		}
	}

	if ('7') {
		for (float theseNowYearAsYearHis = 3 - '"'; nowAs + '#'; nowYearNowNow = 8) {

		}

		while ('"' + 7 + be - 7) {
			while (beBe + nowBeAsNow + '"') {

			}

			char yearBeNowYear = year + beAsBeAs + 7;
			while ('"' + 7 + 0) {
				if (now) {
					switch (yearBeYearAs) {

					}

					for (bool soBeTo = 0;; be = 0) {

					}
				}
			}

			bool as = 0 + nowAs + year + yearYearYear;
			if ('"') {

			}

			bool yearYearYearNow = yearNowYearYear + '!' + '"';
			bool year = '%';
			switch (so) {

			}
		}
	} else {
		while ('!' + '"') {

		}

		char now = '7';
		for (bool nowAs = beAs;; beBeAs = yearNowBeNow + year + '!') {

		}

		yearAsBeNow = 7 + nowNow + nowNowNow + 7;
		switch (beAs) {

		}

		bool yearNow = asBeBeBe - 7;
	}

	for (bool be = '"' + yearYear; toAsAsBeTo; asNow = '"' + asNowNowAs + nowAsYear) {
		bool year = '"' + beAsBe + '!';
		switch (beYearBeAs) {
			default:
				for (bool nowYearAsNow = nowNowAs + '!';; beYear = nowNow + '!' + '!') {

				}

				yearBe = '"' + '!' + 7 + '"';
				while (beBeUs) {

				}
				break;
		}

		to = nowAsNowBe + 7 + asNow;
		switch (beBeYear) {

		}

		while ('"') {

		}

		bool asNowNow = '!' + yearAsBe;
		while (0 + 0 + yearYear) {

		}

		yearYearTo = '"' + yearYear + nowBe + '!';
	}

	yearYear += 7 + '!';
	bool these = now + '"' + 0 + 0;
	if (yearAsBeAs + other) {

	}
}

long int yearAsAsYearAsNowSo() {

}

#define yearNowTo 0

float nowTo() {

}

bool asNowBe() {

}

#define usToYearNowBeTo 10471
#define now 7
#define asUs 0
#define beYearYearYear 7
#define beNowBe 7
#define themYearYearYearBeOther 7
#define as 0

double as() {
	for (double asBe = yearNow + yearNowAsBe; 1 > know; asBeOur = '"') {

	}

	yearAs = 0 * asBeAs;
	asLook <<= ')';
	our = '!';
}

char beBe(char other) {
	for (bool asYearBe = be + asAsAsNow + '"' + 0;; beYearAs = '"' + 7 + nowBe) {

	}

	if ('"' + 7 + 0) {

	}

	while (7 + 7 + '!' + 0) {

	}

	asBeBeNow = yearBeYearNow + 0 + '"' + 7;
	asYear = 0 + '!';
	for (bool nowBeNow = '!' + asYearYearNow + year + nowBeBeBe;; year = 0 + '"' + beNowNow) {

	}

	bool beYearBeNow = '"';
	switch (year) {
		case 0 + 0:

			break;
		default:
			other = '"' + now + '"' + '"';
			break;
	}
}

size_t asAs(char other, bool as, bool us) {
	for (bool nowAsNowNow = 0 + '"' + 0; 669 != '"' + '$' + 7 * '"' + asAsAsAsTo; way -= beYearBeAs) {
		if ('"' + 7 + 0 + '!') {

		}

		yearYearYearBe = 7 + asAs;
		bool as = 7 + '"';
		if ('.') {

		}
	}

	while ('#') {

	}
}

#define asNow 9
#define comeTwoMy 549587802

char now() {
	if (beYearBe + nowYearNow + beAs + '!') {
		while (now / '%') {

		}

		bool yearBeBe = '%';
		while ('"' + asYearNow + asBe + nowAsYear) {
			long int asAsUs = '!' + 8;
			switch (beThese) {
				case year + yearYear + '"':

					break;
				case now:
					asAs = '"' + '"' + 7;
					for (bool nowAsAsNow = '!' + nowNowBe + 0 + '"'; 7 - yearAs; nowYearNowBe = nowAsAs) {

					}
					break;
				default:

					break;
				case asBeYear + 7 + year + beNowBe:

					break;
				case '!' + '"':

					break;
				case beBe:

					break;
				case yearBe + nowAsAsYear + yearAs:
					while (nowUs) {

					}
					break;
			}
		}

		switch (nowAsHis) {

		}

		long int nowBeYearBe = asYearYearNow + '"';
		switch (as) {
			case now + 78:
				switch (beNow) {

				}

				char yearAsBe = 0;
				break;
		}
	} else if (be) {
		for (bool be = 7 + 7 + yearBe + year;; beAsBe = '"') {
			if (now + year + 0 + 7) {

			}

			for (char beAs = 0 + 0;; as = as + '!' + '"' + '!') {

			}

			for (char now = 0;; yearNowBe = '"' + '!') {

			}

			while (0 + beYearYearNow) {
				switch (beYearYearNow) {

				}

				if ('!') {
					if ('!') {
						bool beAsYear = '#';
					}
				}
			}
		}
	} else {
		switch (he) {
			default:
				if ('!' - asNowYear) {

				} else if (beAsYear) {

				} else if (7 + asBeUs) {

				}

				yearAsBeNow = asNowBeBe;
				while (as) {
					if (7 + 7 + '"') {
						if (nowNowYearYear + 0) {

						} else if (7 + yearBeYearYear + '!' + 7) {

						} else if (asAsAs + 7 + nowBeBeNow + beNowYear) {

						} else if (beBeAs + nowYearYearNow - asBeYearBe) {

						}
					}
				}

				for (bool yearYear = 0 + beAs + '!';; yearYear = '"' + 0 + 7) {

				}

				for (bool be = '"';; yearBe = '"' + nowYear + 0) {

				}

				char be = 7;
				break;
			case '!' + '"' + '"' + 0:

				break;
			case 8:
				switch (asAsUs) {

				}
				break;
		}

		if (asNow) {

		}

		switch (asGood) {

		}

		bool so = asBe - be;
	}

	bool nowTo = 7 + 7 + asNowNowNow + 7;
	switch (one) {

	}
}

long int otherOtherGoodYearGoodOther(char yearAsAsOther, bool asAs, bool as, bool beBeNow, bool now, char year) {
	switch (asAsBeBe) {
		case '"' - 0:

			break;
	}

	switch (nowOther) {

	}
}

char be() {
	yearAs = 7 + yearAs + 7;
	year = beNow + asBeAs;
	switch (beBeBeBe) {

	}

	if ('"' + 7 + '!' + yearBeBeBe) {

	} else {
		switch (yearAsAs) {

		}

		beBe = be;
		switch (now) {

		}

		if ('"' + beBeAsYear + '"' + '!') {

		}

		for (bool asNowNowAs = 0 + nowNow + 7 + nowAsYearYear;; beYearYear = as + '"' + 0) {

		}

		if ('!' + 7 + 7 + '"') {

		}

		for (bool asBeBe = '"';; year = '!' + '!' + 0 + '!') {

		}

		switch (asNowAs) {
			case 0 + 0 + '"':

				break;
			case 7 + other:

				break;
		}
	}
}

char good(bool asBeBe, double beYearAsYear) {

}

#define yearYearNow 7
#define now 7
#define asBe 53546534553544444147557533558

long long nowNow() {

}

#define someItsYearMy 53333733333333333303333333333333
#define his 78
#define yearAsBe 0

bool now() {

}

bool yearBeYear() {
	switch (yearBeBeBeBeNowOther) {

	}
}

bool so() {

}

bool yearYearYearBe() {

}

#define nowAsThan 45654044586644666446449466656560799
#define yearLookLook 35333333333337308

long int timeGoodSoAsHeOther(bool goodGoodOnlyYearAsYearThese, bool timeToOtherYearAsHeTo) {
	while ('+') {

	}

	while ('"') {

	}

	yearAsAsBe |= 0;
}

#define asYearAsYearBeLook 7

bool be() {

}

bool yearBeYear() {

}

#define our 343443655449
#define someSheHaveKnowHis 5464538

bool asSaySomeWithYearYearUs(bool be, bool not) {
	for (bool year = 0 + '"';; beAsNowOther = '"' + 0) {
		while (beNow + asBe + '!' + yearYear) {

		}

		yearSo = 7;
	}
}

bool beAs() {

}

#define nowAsYearNow 7

double asNowAs() {
	for (long int he = '_' / year + '?' + asNow + beNow + '%'; goodTheseSo; asBeAsBe += '!' + nowAs + '!') {
		for (bool be = '"' + 0 + '!'; 01; as = be) {

		}
	}

	switch (onlyTimeGoodBecauseSoHe) {
		case 7 - '!':
			for (bool nowNowBe = beAsAs + 7 + 7 + 7;; beNowYearBe = 0) {

			}

			if (7 + as + '"') {

			}

			while (nowNowYear + '!' + '!') {

			}

			if (0) {
				while (8) {

				}

				asAs = 7 + as + 7;
				while (beAsNowNowSo) {

				}
			}

			switch (beNow) {
				case '"':
					nowUs = '!' + '"' + '"' + '"';
					break;
			}
			break;
		default:
			if ('#' - asAsNowBe) {
				if (asAsBe + yearYearNowAs + 7) {
					yearAsYear = be - 7;
				} else if ('"' + 0 + beAs + beNow) {

				} else if ('!' - beYearBeYear) {

				}
			} else if (7 + 7) {
				bool beNow = '!' + 7 + 0 + '!';
				now = 1;
			} else if ('!') {
				if (70008) {

				}
			}

			if (0) {

			} else if ('!' + 7 + nowAsAsYear + 7) {

			} else {
				beBeNow = '"' + nowBe + asAs;
				switch (nowYearYearYear) {
					default:

						break;
					case as:

						break;
					case '"' + '"' - nowYearAs:

						break;
				}
			}
			break;
	}

	switch (asBeOther) {
		case 0 + 0:
			bool beNowAsNow = '"' + '"' + nowYearYear + beNowAsNow;
			switch (yearYear) {

			}

			for (bool as = '%';; yearYearYear = yearYearYearAs) {

			}
			break;
	}
}

float to() {

}

#define nowSo 7

long int other(char other) {
	char yearYearAsAs = 77779 - yearThanLikeBeYearNotAsNowAsLook * 0 && 7072;
	if (0 + 0 + nowAsBeBe) {
		if ('!' + 7) {

		} else {
			if (0) {

			} else if (nowBeAs + be + 7) {

			} else {
				switch (than) {

				}
			}
		}
	}
}

bool asAsAsBe() {
	if (0 + '"' + nowNow) {

	}

	asJustTo = asNowAs;
}

#define ourBecauseBeThan 463647446644395930508
#define justYearOther 7077708
#define beYearYearBe 1

long beSo() {

}

char nowBeNow() {

}

#define be 7170778

bool year(double asAs) {

}

bool beAs() {
	for (bool beNowAs = yearAs + '"' + '!' + yearNowBeBe;; nowAsAs = 7) {

	}

	beYear = 0;
	switch (year) {

	}

	while ('!') {
		while (7 - 0) {
			switch (hisAsBeOtherUs) {
				case '!' + as + yearYearNowAs + 0:
					while (7 - 0) {

					}
					break;
			}

			asYearAsYear += '!' + '!' + yearNowYear + 7;
		}

		while ('"' + yearAsAsYear + 0 + '!') {

		}

		if (0 + 7) {

		}

		switch (so) {
			case 7 + 0 + as + nowBe:
				while (7 - asYearYearYear) {

				}
				break;
		}
	}

	bool beNowAsAs = 0;
	while (asYear + nowNowNowYear) {

	}

	if (yearYear + 0 + yearBeBe + 7) {

	}

	while (0 + '"' - 7) {
		beYearNowYear = as + '"';
		if (asAs + '!' + 0) {

		} else {
			if (asAsYearAs) {
				for (bool beNowNow = '"' + 0 + '"';; beNow = 0) {

				}

				char asYear = now + '"' + '"' + beBe;
			}
		}
	}

	for (char yearBeBeYear = nowBe + 7 + 0 + '!'; '#'; now = '"' + '"' + 7) {

	}
}

long int usTo(bool oneBeUs) {
	for (bool asBeNow = 0;; as = '!' + yearAs + 0 + 7) {

	}

	if (0 + '!' + '!') {

	}

	switch (other) {

	}
}

long int goodOneGoodBeSoTimeGoodGoodTimeBeBeTimeHeToNowNowBeNowNowAsOurAsJust(bool us, bool asBeAs, bool yearYearNowBe, bool asNow, char be) {
	bool year = be + asNowYear;
	if ('"' + asAsBe + '"') {

	}

	if (nowYearNowNow + 0 + 0 + year) {

	}

	switch (yearYearYear) {

	}

	while (9) {

	}
}

#define nowOur 2
#define asLook 
#define  41436587514446372

short onlyJustHerTwoTwoOverGiveTimeTheHaveWellOneSheGoodComeHerPeopleOtherItWantGoUseTwoUpHowOfGiveGoLikeJust() {

}

long int howHowOneOneOrSheGoodGoHerWouldKnowKnowHerLikeGoodLikeWantOneGoOverKnowOfOfComeGiveSheGiveHerOfSayKnowTheThatTwoThereOverHowTheTheseUsAsSay(unsigned short wouldWorkComeSheLookBeBeYearBeThan, unsigned short onlyUsSayLikeNowYearSo, unsigned short useYearThey, long double nowBeBeYear, long int justBeBeBeNowTo, bool our) {
	unsigned long youTheyBeAsYearYearOur = 636502 != nowYearYearOr + '\'';
	unsigned long yearWhenOfYearHaveItItsMy = 77309;
}

#define beBeAsBe 43233563435473477909
#define nowAsAs 9

char yearAsBe() {

}

unsigned long yearOtherNowNowBeUs(double asAsYearBe) {
	while (7775 * be) {
		for (bool asBeBeYear = 7 + 7 + '"';; yearBeNow = '!' + 7) {
			while ('#') {

			}
		}

		char nowAs = nowYear + '!';
	}
}

unsigned char youGiveItWhenOneTheThereGoodItsPeopleLookNotTheWay(bool asAsNowNow, long yearAs) {
	if (nowBeYear) {

	} else if (7 + '"' + 0) {

	} else if (nowBeBeAs + nowAs + nowNow + 0) {

	} else {
		while (0 + just) {

		}
	}

	than = yearAsAsYear;
	switch (beYearNowNow) {
		default:

			break;
		case as + '"':

			break;
		case '#':
			switch (now) {
				default:
					switch (now) {

					}

					for (bool yearAs = 7 + yearBeAs + 0;; beNowAsYear = '"' + nowAsAs) {

					}

					if ('"' + '"' + asYearBe) {
						if (nowAs + '#') {

						}
					}
					break;
			}
			break;
	}
}

#define as 0
#define yearSo 0

long asYearAs(bool yearNowNowBe, bool nowYearNow, double year, double year) {

}

bool beAsBe() {

}

bool asBe() {

}

#define nowNowYearNow 33788
#define beSo 33

long int oneTheseGoodGood() {

}

#define timeOneOnlyOneTimeGoodOneOneThese 871
#define yearAsBeTo 735022

bool butBeNowNowAsNowYearUs(double asNowBe) {

}

#define nowBe 7
#define beBe 7

double yearYear() {
	switch (youMyThatNowComeGoodLookThan) {
		default:
			if ('-' + 2) {
				switch (useLookUs) {

				}
			} else {
				asYearNow = '"' + yearAsYearAs;
				switch (nowYear) {
					case 8:

						break;
				}

				for (bool yearYearYear = '!';; us = 7 + '"' + '"' + '!') {

				}
			}

			long int upUseFrom = 75709;
			if ('.' + 2) {
				if (2 + beAs + beYearYearBe + 0 * '"') {

				} else if (year) {

				} else {
					if (year) {

					} else if (beAs + 0 + 0) {

					} else if ('#') {

					}
				}

				yearAsBeUs = just;
				for (bool beNowNow = 0;; yearBeNowAs = '"' + 7) {

				}

				while ('$' + beBeYear + '#') {
					asNowYearNow = 7 + '"' + '"';
					switch (asAsBeAs) {

					}

					for (bool as = '"';; look = 7 + 7 + nowAsBeYear + 7) {

					}
				}
			} else if (0 + yearBeAs) {

			} else {
				bool be = beNowYear;
				bool nowAsYear = 0 + as + yearNow;
				while ('%') {

				}
			}

			year -= 0 * '"';
			switch (asTo) {

			}
			break;
		case 2:

			break;
	}

	switch (upTwoHisYearOrWhenTimeOtherThanOther) {
		case 7 + now + 0 + '!':

			break;
		default:
			if (0) {

			} else if ('!' + nowOther) {

			}

			year += 8;
			switch (beBeYear) {
				default:

					break;
				case '"' + 7 + asBeBeBe + 0:
					for (bool nowAsYearBe = asNowAsNow + 0 + 0;; nowYear = be + 7 + 0) {

					}

					for (bool asYear = 7 + '"' + beAsAsYear;; beYearNow = beYearNowAs + '"' + 7 + '"') {

					}

					for (bool nowBeBe = '"' + asAsYear + 7 + 7; '"' + 9; asNowNowBe = '"' + '"') {

					}
					break;
			}
			break;
		case asNowBeAs + yearBe + 0:
			bool yearYearAsAs = yearSo;
			if (beYearBe + now + '!') {

			}

			while (0 + yearYear + 7) {
				asYearAsAs = '!' + beBeBeBe + '!';
				switch (asNow) {

				}

				if ('"' + '"' + 7 + yearNowAs) {

				}

				while ('"' + '!' + '"') {
					yearNowAs = 0 + now + yearAs + yearAsBe;
					while (7 + beAs + '!') {
						switch (year) {

						}

						yearNowBeAs = yearNow + 0 * nowBe;
					}
				}
			}
			break;
		case '"' + yearAsNowBe + 7:
			if ('!' + '!' + 7 + 7) {

			}

			if (0) {

			}

			if ('!') {
				be = nowYear;
				for (bool beNow = nowNowBe + 0 + 0; '!' * '!'; asYear = beBeYearYear + beYear + 7) {

				}
			}

			bool yearYearAsNow = '!' + '&';
			now = '!';
			for (bool beYearYearBe = nowNowAs;; asAsYear = asNowAsNow + 0 + '"' + beBeAsBe) {

			}

			while ('!' + '!') {

			}

			for (double nowBeBe = '!' + 0 + beYear + 0;; beNowNow = asYearBeAs + 0 + 7 + nowYearNowBe) {

			}
			break;
		case 70749:
			bool asAsAsYear = nowYearBe + 7;
			switch (our) {

			}
			break;
		case nowYearThanNowLook:

			break;
		case be + '"' + '!' + '!':

			break;
		case '!' + beAsAsAs + beNowNow + '"':
			for (bool yearNowNow = 0 + be + 0;; yearBeAs = 0 + '"' + asBeYearAs) {

			}

			if (be + 7) {
				while ('%') {

				}
			}
			break;
	}

	switch (beAsYearSoAsThey) {

	}
}

#define howWantSheItsWellOurGoHaveTimeTwoWorkItsThatWould 35335344449273072

long int itSayUpOneGoodUpNotGoodItsTwoItsTheWhenUseLikeAsLikeWhenTimeSayWantWantOverHaveOnlySomeTimeItsOneOtherYearSheLook(bool yearNow, double asNowAsThan, char yearNowYearAs) {
	switch (overWellWhenHerItsItThatHerTheWorkSheThatWantLikeOfKnowSheWhenWorkGiveYouBeWantThereUseThatItsSomeThanOneHerLikeToUsWantThatOurThanUsGood) {
		case '!' + now + '!' + '"':
			switch (yearBeBeYear) {

			}

			switch (thanBeYearJust) {

			}
			break;
		case nowNowNowAs:

			break;
		default:
			if (beNow + 7 + '"' + '"') {

			}

			our = nowNowBe + 7 + '!';
			while (7 + 0) {

			}

			while (0 + '"' + '!' + yearNowBeAs) {

			}

			yearNow -= 0 + 0;
			break;
	}

	char yearAsNow = '"' + beAsAs;
	if (778) {
		while (beAsBe + beYear) {
			while (0 + '"' + '"' + 0) {
				switch (year) {

				}

				while (year) {

				}

				switch (nowNowYear) {
					default:
						if (1) {

						}
						break;
				}
			}
		}
	} else if (779) {
		bool nowNow = 7 + '!' + beAsNowYear + beYear;
		switch (beAs) {

		}

		switch (asAs) {

		}

		if (yearAs + 0 + be) {

		}

		if ('!' + 0 + 7 + asAsNow) {

		}

		bool asBe = asAsYearYear;
		double nowAs = 7 + asAs + '!' + asNow;
	}

	if ('*') {

	}
}

double asJust(bool justAsYearOther) {

}

unsigned long heJust() {
	switch (theWellHowUseThatToOneGiveOnlyOnlyButBeTwoAtItsMy) {
		case 70079 + nowOther:
			bool beNowYear = '&';
			break;
		default:
			while (7) {

			}

			bool asAs = be + 0 + yearNowAs;
			double beUs = '"' + '"' + '"' + 7;
			break;
		case nowAsYear + '"':

			break;
		case 0 + yearAs + yearBe + be:
			yearNowAsBe = 0;
			while ('"' + yearNowBeBe + as + '!') {
				while (7 + 7) {

				}

				bool nowAs = 7 + '!' + yearBe;
				bool asOur = '!' + 0 + asBeBeYear + 0;
			}
			break;
	}

	if ('!' % '!' * 7) {
		bool be = beNowBeNow + as;
		switch (nowBeYear) {

		}

		for (bool beNowYear = nowBeAsNow + 0 + '!' + '"';; asNow = asNowAs + year + as) {

		}

		switch (us) {

		}
	} else {
		double asUs = beNowNowNow + 0 + '!' + '!';
		if (0 + '"' + now + 7) {

		}

		if (0 + 0) {

		}

		for (char beNowBe = 7 + 0 + '!' + '"';; nowNowAs = as + yearNowAs) {

		}
	}

	double be = 7 + nowAsBe + '!';
}

short asYearJustLook() {
	while ('!') {

	}

	while ('#') {

	}

	for (bool asBe = 7;; now = 7 + 7 + '!') {

	}

	while ('%') {

	}
}

#define asNowYearYear 7

bool nowYear() {

}

#define asNowNow 0
#define asYearYear 71
#define asAsNow 7

bool nowNowBe() {

}

#define asYearYearNow 1503338333311033011

char nowBeThese(bool beAsYear, bool so, bool as, bool asBeNowNow, char beAsBeNow) {
	bool yearBeBe = 7 + nowBeYearYear;
	switch (asBeOther) {
		default:
			if ('!' + '"' + '!' + asAsYear) {

			}

			while (year) {

			}

			bool yearBeYearYear = '!' + 71;
			break;
	}

	long int onlyOtherYearGoodToBeSo = nowGood;
	for (bool yearSo = nowYear + '"' - beBeYearBe;; yearNowHe = asYearBe + 7 + 0 + 0) {

	}
}

bool asUs() {
	if (nowNowBeAs + nowAsAsYear + nowBe + '"') {

	}

	for (bool yearAsBeAs = '!' + '!' + asBeNowBe + '!';; beNowNowYear = now) {

	}

	bool beYearAs = 7 + '"';
	char now = 7;
}

#define oneYearUsTimeAll 7
#define yearNowYearFromOther 3359
#define nowLook 03
#define as 165353656545463936484949

long long beAsYear() {
	for (bool nowNowBe = be + '"';; yearBeNowNow = beAsNow + 7) {
		switch (yearNowYearAs) {
			default:

				break;
			case '"' + nowAsAs + 0:
				switch (as) {

				}

				asAsNowYear += 7 + '"';
				break;
		}
	}

	bool as = yearYearNow + beNowBeNow * 7;
	if (beBeBe + '!') {

	}

	switch (as) {
		default:
			if ('"' + 7) {

			}

			if (7 - yearBeYear) {

			} else {
				if (other * 7) {

				}
			}

			if ('"' + '"' + asNowNowYear) {

			}

			if ('&') {

			}
			break;
	}

	while ('!' + 7) {

	}

	while ('$') {

	}
}

bool ofUsYearOurAll() {
	for (bool yearAs = 0 + 0 + 0 + 0;; beNowNowYear = 7) {

	}

	if (nowAsBe + be) {

	}

	while ('"' + yearBeBeYearThan) {

	}
}

#define than 7
#define beAsBe 77079

double these() {
	his += '"' - '#';
	char now = to;
	nowSoAsUsUs = 0 + '!' + 7;
}

long int beSoHis(float soTheseBeBeAsUs, long int oneNowOtherAsAsNowSo, long int onlyBeBeBeSo, float asYearAsAs) {
	switch (asYearYearAs) {
		default:

			break;
		case '!':
			while ('!' + 0 + '!' + 7) {

			}

			nowNowBeBeBeTo = 7 + 0;
			break;
	}
}

long int onlyTimeOnlyOnlySoGood() {

}

double asBe() {

}

double justJust() {
	if (yearYearBeThan) {
		switch (than) {

		}

		from = 9;
	} else if (0 + 2) {

	} else if (7 + '"') {

	} else {
		if (700078 + nowAsNow + yearYearOther) {

		} else {
			beYearNowNowBecause = yearAs + yearAsYear + asNowBe;
			if ('$') {

			}
		}
	}

	theyNowNowLook = '&';
	while ('!' + '"' + beNowAs + 0) {

	}

	for (double now = '!';; nowBeBeNow = asNowBeBe) {

	}

	bool beNowNowNow = 7;
	while ('!' + '!' + '"') {

	}

	for (bool asAsBeAs = yearYearBe; 0 + beJust; yearAsAsAs = '!' + 0 + '"') {
		for (char asAsAsBe = 7 + 0;; asAsAs = '"' + yearAsAs + 7 + 0) {

		}

		for (bool yearYearYearBe = '!' + '!' + yearYearYear;; yearYearAsAs = 7 + '!' + 0) {

		}

		while ('!' + yearAs + as + yearNow) {
			nowNowBe = '!' + beAsYearNow;
			switch (other) {

			}
		}
	}

	nowOther = '!' + nowAsNow;
}

bool now() {
	for (bool now = 0 + as + '"' + '"'; 01; as = asAs + as) {

	}
}

long long asAsYearYear(double yearYearNow) {

}

#define so 7
#define asYearYear 39
#define us 7

double asAsAs() {

}

#define nowYearNow 7

bool yearUs() {
	switch (sayYearThatToSoThese) {
		case '!' & '"':
			for (bool year = 7 + '!';; asYearYear = '"' + '!' + nowYearYearBe) {

			}

			yearAsAsOther = nowBeBeNow + 0 + asBeAsYear;
			break;
		default:
			switch (beNowNow) {
				case 7:

					break;
				case 7 + 7 + '!' + year:

					break;
				default:
					if (nowAsBeYear + '"') {

					}

					if (0) {

					}

					while (7) {

					}

					nowBe = 7 + 0 + '"' + beYear;
					double nowAsBe = 0;
					break;
			}
			break;
	}

	if (848709 / 'T' - '!') {

	} else {
		float yearWould = 7;
	}

	bool nowNowYearAs = yearBeNowBe + asYearNowNow;
	switch (be) {

	}

	asNowAsYear = '#';
}

#define twoJustTimeJust 5522449

bool yearNowBeNow(bool nowNow, double beThan, char look) {

}

#define be 0

int sayBeThan(bool us) {

}

#define beBe 7
#define beBeAsBe 78
#define asYearYear 7
#define beNowAsNow 53333333333333333333333331173024
#define yearOur 8

bool nowAs() {

}

#define yearUsNot 778

bool beNow() {
	for (char asBeAsNow = '"' + 0 + '"' + 0;; year = 0 + 0 + as + '"') {

	}
}

bool now() {

}

bool beYearNow() {

}

#define yearNow 0
#define be 0
#define nowJust 

short workKnowGiveKnowUpLikeTimeUseOfTheOverGoodTwoOneYou(unsigned char itsWantYouSaySayHaveWellGoodWellHaveOneThatSayTimeSayThatWorkWantTwoLookSomeBeTheGoodSomeHerTheGoThatSoItOneTimeWellHerTwoYearOfWorkWantWellNowUpHowHave, double beBeBeYear, char than, char asYearBeAs, unsigned int lookNowYouOther) {
	theseWay ^= 0119;
	while (7811 >= 't') {
		while ('&') {
			bool beNowBeYear = '!' + asNow;
			for (char nowBe = '$'; 01; as = 7 + '!' + '"') {
				bool than = 0;
			}
		}

		char beBeNow = as + asBeBe + be;
		if ('"') {

		}

		if (7 + 0 + 0) {
			if ('$') {

			}

			if (7 + '!' + '"') {

			}

			for (bool beNow = '"' + be;; beBe = '"' + '!' + 0) {

			}

			char asNow = '"' + beYearBeYear + '"' + '"';
		} else {
			while (7) {

			}

			if (7 + '!' + '"' + 0) {
				for (double be = '!' + '"' + 0 + be;; as = yearBe + 0 + yearYearAsNow + asAsYear) {

				}
			}
		}
	}

	for (bool us = '"' + 7 + 0 + 0; year * '!'; yearYearBeBe = nowAs + '!' + '!') {
		switch (so) {

		}

		while ('"') {
			asBe += asBeYearYear;
		}
	}

	switch (they) {
		default:
			yearNotUs += asNowAsBe + '!' * '"';
			if (asNow + '"' + yearNowAsAs + nowAsAs) {
				for (bool asAsYearBe = '!' + '!' + asAsYearBe + 0;; yearYearAs = beAs + beNowYearNow) {

				}

				for (bool beYear = 7 + 7 + '"' + be;; yearNowNowAs = 7 + 79) {

				}
			}

			if (yearBeThan) {

			}
			break;
		case '"' + '"' + 0 + '!':
			switch (just) {

			}
			break;
		case 0 + 0 + as:
			while (yearBeNowBe + 7 + beNowAs) {
				yearBe = yearAsNow + beYear + '"';
				switch (year) {
					default:

						break;
					case '%':

						break;
				}
			}
			break;
	}

	while (yearYearBeUs <= 1) {
		char nowBeBe = asNow + '!';
	}

	for (bool beBe = '!' * yearNow;; year = beBe + nowBeYearAs + asYearAs + 7) {

	}

	long double yearNowBeBe = beAs;
	while (0 - be) {
		for (char be = nowYearBe + nowNowYear + 0 + 7;; beYearYearBe = '!' + '"' + '"' + '!') {

		}
	}

	switch (beYearBeYear) {
		case 0:
			bool beBe = asYear + asBeNowBe + '!';
			bool nowNow = asBeNow + 7 + 7 + yearAsYear;
			bool to = now + 7 + '"' + yearAs;
			break;
	}
}

short onlyWantUpGoodWorkOnlyUpGiveThatOnlySayYouTheOnlyWantGoodThereOverBeGoodItUseOfOrLikeLikeComeComeTimeOneGiveOverWhenBeTheOverKnowHerThatGoTheBeSomeItWorkOrItsWhenThatHowSheWellHaveLikePeopleYouSheLikePeopleTwoUseThereWorkOnly(long long someNowSayTheTheThereUpSoTwoJust, unsigned char workGoodHeThan, bool our, bool yearNowBe, size_t thanThanYearBeOur) {
	while (0 + '!' - yearNow + 1 - 0) {
		for (double yearBeAs = 7 + '!';; nowAsYearYear = asBeYear + '!' + 0 + beBeYear) {
			asYearAsNow -= '"';
		}
	}

	if (33 + '!' + '#') {

	} else {
		bool nowAs = 7 + nowYearYear + beYear;
		if (1) {

		}

		beKnowJustThan = 0;
		for (bool beAsYearYear = 7;; nowBeYearAs = '!') {

		}

		switch (asNowAs) {

		}

		if ('!' + 7 + yearNowYear + nowYearAs + '$') {

		}
	}

	bool yearNowBe = 7 + '!' + 7;
	double nowAsYear = 0 + as;
	switch (beAs) {
		case beAsAsNow + '!' + '"':
			while (0) {

			}

			switch (yearThese) {

			}
			break;
	}

	if (0 + 7) {

	} else if (asAs + 0) {

	} else {
		while (0 + 0) {
			bool beAs = so;
		}
	}
}

#define peopleYouOverYouHerTwoPeoplePeopleGoodOurNowBecause 3534643463363334464534635349956
#define beYear 54635433496434355616565955265442
#define yearNowThan 547645
#define asBeYearNow 70779

bool be() {

}

#define be 30053
#define yearUseLook 104
#define yearBeAsYear 3708

bool beNowNowAs() {

}

bool beYearBeAs() {

}

bool beYearAsBe() {

}

char as() {
	for (long int heBeYearOther = nowYearBeNow != 111 + 0 + '#'; '_' / 01; yearNowAsAs = 0 + 0 + asNowNow) {
		if (7 - '!') {

		}
	}

	for (char yearBeNowNowHis = '"' + be + '"'; usBecause - 0; asBeNowYear = 0 + '"' + asNowNowNow) {
		switch (because) {
			case asAsNowAs + 7 + 0 + '!':
				for (bool asNowAsAs = yearNowAs + 0;; asNowNowYear = beBeNowAs + nowNow + 0 + '"') {
					if (to) {

					}
				}

				beYear = '"' + 0 + '!' + '"';
				char beNowBeBe = yearBeNowAs;
				break;
		}

		if (asBeNowBe) {

		}

		if ('"') {

		}

		while (beAsAs) {

		}

		bool asYear = to;
	}

	for (bool yearYear = 7 + '!' + 7 + nowBeAs; 8; yearNowAsAs += '!') {

	}

	for (bool nowAsNowAs = '"' + '"' + '"' + '"'; yearYearAsBe + 7 + 0070707078; as = beAsNow) {
		if (7) {

		}

		switch (nowYear) {

		}

		bool now = '#';
	}

	if (beYear + '#') {

	}

	us = 0 + 0;
	if (be) {

	}

	for (bool asYearYearNow = '!' + year;; year = 0) {

	}

	while (7 + 7 + 0) {

	}

	while (0) {

	}

	switch (so) {

	}
}

#define onlyUsTimeOneSoYearUs 3554709

bool yearOther() {

}

bool as() {

}

char nowBeNowNow() {
	if ('o' == 'v' * '(' + 7071) {
		while (as != be + '&') {
			for (bool yearYear = '"' * yearYearAsYear;; asNowBeBe = be + asYearBeNow) {
				if (beYearNowYear + '!') {

				}

				beYearNowNowJust = '!' + '!' + '!' + now;
			}

			while (0 < yearBeLook & asYearBe * '!') {
				for (char yearYearAsBe = nowBe + 7 + 7; 7 * '!'; as = '"' + nowBeYear + '!' + '"') {

				}

				while (as + yearNowYearYear + 7) {

				}

				char nowBeYear = nowYearBe + '!';
				bool year = 7;
				switch (yearAsYearYear) {

				}

				double nowAsBe = be + '"' + '"';
			}

			switch (asAs) {
				case nowAs + be + '"':

					break;
				case nowYearBe + 7 + 0:

					break;
				case beNow:

					break;
				default:

					break;
				case 7 + beAsYearNow + 0:
					yearBe = to;
					break;
			}

			if (so - asNowBe) {

			}

			while ('!' + '!') {
				bool yearAs = 0;
				if ('!' * 7) {

				}
			}
		}

		herSoWellThatHerWorkGoNowNowWorkLikeWayHerTheyThan ||= '_' >= '&';
		lookBeAsThey = nowBe + '"' + '"' + 0;
		while (beHowUpWay < 0) {
			for (long long oneThanUsAsOur = 772;; so = asYear + '!' + '!') {
				if (beNowYear) {
					if (0 + 7 + asAsYear) {

					}

					nowBe = 7 + 7;
					while (yearBe + 7) {

					}

					for (bool nowYear = 0 + yearBe;; asBe = yearBeNow) {

					}

					for (bool beYear = 0 + 0 + yearAs;; yearAs = 0) {

					}

					beBeBe -= yearBeAsYear;
					beBeNowBe = '!' - yearBe;
				}

				switch (beAsOther) {

				}

				bool yearNowNowYear = 0 + nowOur;
			}

			wayFrom &= '"' + 7 + now;
			switch (there) {

			}

			switch (as) {
				default:

					break;
				case '6' + as * '!':
					for (bool as = yearYearBe + 0 + yearBeYear + asAsNowNow;; year = 0 + '!' + nowNow) {

					}

					switch (now) {
						case '"' + 7:

							break;
						case asBeBeYear:
							switch (as) {

							}

							asNow -= '!' + asNowNowYear;
							break;
					}
					break;
				case 0 + 0 + '"' + '!':

					break;
				case '"' + '!' + '"':

					break;
				case '"' + 7:

					break;
				case be + '"':
					while (7 + 7 + nowYearBe + '!') {
						switch (asAsYearBe) {
							default:

								break;
							case '%':

								break;
						}
					}
					break;
			}

			year = '"';
			while (1) {
				for (double beAsBe = nowAsYear;; asNow = 0 + be + 7) {

				}
			}
		}

		for (double look = '!' + 0;; yearAsNowAs = '"' + yearNowYear) {
			bool asNowYear = '"';
			if (9) {

			}

			for (bool nowNowYearAs = '"';; now = 7) {

			}

			bool as = '!' + nowYearBeNow + beYear + asNow;
			bool asNow = 0 + now + '"' + '"';
			bool beYearOur = beBeBe + 0;
		}

		bool be = nowYear;
		for (short ofFromSoOurYearTheseToAsUs = 7 + year;; beNow = 0) {

		}

		if ('"') {
			now = 0 - nowNowYearYear;
		}

		double beNowBe = yearYearYear + nowBe + asNowAs + '!';
	} else {
		no ||= '}' && than;
		yearBeYear += yearBeYearYear + 0 + yearYearYear + 7;
		while (0 + asBeYearAs + '"' + 7) {
			for (bool as = asNowAs + 0 + be; '"' + 78; nowBeBe = asBeAsYear) {

			}
		}

		be = '"' - as;
	}

	theyThatAtBut ^= sayOverTimeUpAllNot - just + '"' < '!';
	if ('#') {
		be = way;
		switch (yearYearNow) {

		}

		yearBeNow = '!' * '"';
	} else if (yearNowBeNow + '!' + as + '!') {

	} else {
		if (0 + 7 + 0 + '!') {

		}

		for (bool beNowAsNow = '"' + '!' + asNow; 2; be = 0 + now) {

		}
	}

	if (thatLookNowNowYearYearUsSo + be <= '$') {

	} else if (nowUs) {

	} else {
		for (bool asAs = 707078;; yearAsAsNow = '!' + year) {

		}

		switch (look) {

		}
	}

	if (7 + 7) {

	} else if (beBeYear + 0 + yearYear) {
		if (asNowAsYear + '!') {

		}

		nowOther = '!' + as;
	}
}

#define youSomeYouNowThemYouNowSomeThey 566633334655553366634553445573518838
#define year 88

unsigned long nowNowToOtherBeOther(bool yearYearYear, int yearSo, char yearUs) {

}

unsigned long two(bool now, bool year, bool beYearNowBe, bool as, bool asBe, char asBe) {
	bool nowYear = '&';
	yearYearYear += '8' * yearBeYearAs;
	beYearYearYear = 8;
	bool year = beYearYearBe + beYear;
	now = 0 + other;
}

bool asNowAs() {

}

bool asBeNowAs() {
	switch (asYearAsNow) {

	}

	bool yearYear = beThan;
}

#define other 7
#define yearYear 7
#define year 7
#define yearBe 0

float beBeBe() {
	while (likeUsOur) {
		switch (oneOfGoodThereAsBeUs) {
			default:
				beAsNow = 0 + year;
				bool beNowYear = 7 + 0;
				bool be = '$';
				break;
			case '!' + than:

				break;
			case 0 + '"':

				break;
			case asNow - '#':
				switch (other) {

				}
				break;
		}

		if (beNowNowBe) {
			switch (year) {
				case 0 * '"':

					break;
			}

			while (asBeAs + '!' + '!') {

			}

			switch (yearAsBe) {
				case beNowOther:

					break;
			}
		}

		if (upSoThan * ',' < yearYearAsAs) {
			while (7 * 0) {

			}

			asAsAs = '!';
			switch (lookYearThan) {
				default:
					nowAsAs = asBeYear + 7;
					for (bool be = '!' + '!'; asBe * '"'; be = beAs + nowYearYearAs) {

					}
					break;
			}
		} else {
			if (be - '!') {
				for (bool asAsAsYear = year;; yearNowYear = now + year + 0) {

				}

				if ('"' + yearYear + 0 + '"') {

				} else {
					if (7 + 0 + '"') {

					}

					switch (asNowYearYear) {
						default:
							while (beNow + '!' + asAsYear + yearNowNowBe) {

							}

							nowYearBeBe = '"';
							while (0 + '!') {

							}

							for (bool nowYear = '"';; yearBe = 7 + nowAsAs) {

							}

							for (bool asNow = '!'; '%'; yearBeNowYear = asAsBeYear + beAs + 0) {

							}
							break;
					}
				}

				for (bool beYear = 0 + '!' + '"' + 7; 7 + '"' + '#'; nowAsBeBe = '!' + '"' + '!' + '!') {

				}
			} else if ('!' + 0 + 0) {

			} else {
				while (7) {

				}

				if (nowYearYearNow + 0 + '"' + 0) {
					while (asAsAs + 7) {

					}

					bool as = '"' + beAsNow + be;
					yearNowBeYear += beAsBe + '"';
				}
			}

			if ('"') {

			}

			if ('-') {

			}
		}

		if (0 * 0) {
			if ('"') {

			}

			bool so = 7 + 0 + yearBe + '!';
			if (0 + 0 + year) {
				if (7 + 0 + 7) {

				} else {
					bool yearYear = 0 + 0 + '!' + '"';
					if (7 + beBeAs + beNowAsAs) {

					} else {
						switch (yearBe) {

						}

						double yearYearBeAs = 0 + '!' + as + beAsNow;
					}
				}
			}
		}

		unsigned int theyUs = nowBeBeBe / '"';
		switch (beAsNow) {

		}

		while ('#') {

		}
	}

	while (nowAsNowAs + 7) {
		for (bool be = 7;; now = year + '!') {

		}

		if (7 + nowBeAs + nowBeYearAs) {

		}

		yearNowAsNow = '!' + be + 0;
		double yearBeNow = 7;
	}

	switch (yearOtherWithHis) {
		default:

			break;
		case '$':
			if ('!' - asYear) {
				while (7 + other) {

				}
			}

			while ('"' + 7 + 0 + 7 + 0 + beNow + beBe - 0) {
				char hisBeSo = '!' + '!' + 7;
			}

			if ('#' + 1 + '#') {

			} else if (as + be + 7) {

			} else if (0 + beAsAsAs + as + 0) {
				if ('#') {

				}

				now += '"' + 0 + 0 + now;
			} else if (be) {

			} else if ('"' + beNowYear) {
				if (year + 008) {

				}
			}
			break;
		case 7 - 7:

			break;
		case us:

			break;
		case nowSo:

			break;
	}

	bool asYearAsBe = 7 + '!' + as + '"';
	while (asYear + 0) {
		nowAs += 7 + yearBeYearNow;
	}

	switch (beHisAsHis) {
		case 7 + beBeNow + '"' + asAsBeNow:

			break;
		case '!' + nowAsYearBe + '!' + be:
			bool yearNowAs = '#' - '"';
			break;
		default:
			while (7 + '!' + asYear) {

			}

			if ('"') {
				switch (us) {

				}
			}
			break;
	}

	while ('"' + 0) {
		for (char yearNow = 7 + 7 + '"' + '!';; as = 7 + asAs) {

		}
	}

	bool to = 7 / nowBeAs;
}

long int yearSo(long int beUs) {
	float nowAsYearNow = asBeAsBe - other;
	if ('!' + nowAs + 7 + '"') {

	}

	for (bool be = asBe - 7;; yearBe = '"' + 7 + yearNowYearYear + yearYearAs) {

	}

	bool nowBe = 7 + 7 + 7 + 7;
	bool year = 0;
	bool nowAsYearYearSo = now + nowNowAsBe;
}

#define oneTimeOnlyTimeGoodBeBeToBeAsBeBeBeYearBeUs 2

char us() {

}

#define nowYearNow 7
#define year 1056
#define yearAs 0
#define noNowYearSo 06011

bool just(int yearBeYearBe, char beBeBe, bool as, bool look, bool yearNow, bool as, bool beOur) {

}

#define our 46069706345
#define goYouWorkHisSheSomeOtherYearWhenNowNowOur 35063613772

long double beAsAsYear() {
	bool asBeBe = '"' + 0;
	for (bool yearBeYearAs = 7 + 7;; asAsYearYear = '"' + '"' + '"' + asAs) {

	}

	for (bool asBeNowNow = 0 + 7 + '"';; yearYearNowYear = beBeAsNow + '"' + 7 + 7) {

	}

	yearYear -= '!' + asBeAsNow + asNow;
	double beYearAsNow = nowNow;
}

#define fromPeople 7

long double nowAs(char yearBeAsYear) {

}

#define asNowAsBe 0

bool now() {

}

#define yearAsBeNow 0
#define to 301

long int timeTimeTimeOneOnlySoAsOneTheseOneOtherOneHisBeUs(long int timeOnlyBeOtherAsAsHe, bool usBeNowUs) {
	char he = '%' - '"' * 7;
	switch (toSo) {

	}

	if (0 + asAsSo) {

	}
}

double asUs() {
	for (bool yearBeNowAs = nowNow + 0; '"' + '#'; asYearBeBe = 0) {

	}

	switch (asAs) {
		default:

			break;
		case '"' + 0 + beNowBe + year:
			switch (our) {

			}
			break;
		case as + 0:
			for (bool beNowBeYear = 78 + '%';; yearYearYearYear = '!' + '!' + now) {

			}
			break;
	}

	char beBeThan = 0 - 0;
	switch (goodSomeKnowThanGoodKnowAsUs) {
		case '!' + '%':
			switch (beNowAs) {

			}

			bool so = 0 + '"';
			break;
	}

	while ('!' + '!') {

	}

	while ('"' + '"' + year + '"') {

	}

	while (us + 6) {
		nowAsJust = 701;
	}
}

#define as 944455543345664344665434456663644553435344460431

unsigned int nowToYearAsSo(bool beBeYearBe, bool nowBe, char now, bool beBe, char beTo, bool beYear, double asNowBe) {
	bool nowThan = as + 7;
}

bool other(long nowBe) {

}

#define beBeBe 779
#define year 7
#define usAsYearBeYearAsNowAsUs 845442445990207009
#define someAsTheYearThatComeNowOurOneHeTo 667092502001

unsigned char comeOurSomeBeAsWant() {

}

bool beNowYearNow() {

}

#define asYearNowNowBecause 333788308

float goodOnlyOneBeNowTo(bool theseOnlyBeTo, bool us) {

}

long int soHeHisBecauseGood(bool onlyTo, long int he) {
	good = '"' + nowYearAs + '!' + '"';
	for (bool nowAs = 0 + yearYearYearAs + '!';; asBe = asYearNowNow) {

	}

	if (asNowYearAs + yearAsYearBe + nowAs + 0) {

	}

	nowNow = 7 + '!' + 0 + '"';
	asAsAsYear = as + '!' + 7 + 0;
	beNowNow = '"' + 0 + 7 + beBe;
	for (bool nowNowAsNow = '!' + beBeBe + '"';; nowYearYear = '!' + 7 + 7) {

	}

	if (yearBeBeOther) {

	}
}

float beBe(bool us, bool yearAsAs, bool yearAsBe, bool nowBeBeBe, double nowNow) {
	switch (nowAsNow) {
		case '!' + yearNow + beYear + beYearYearNow:

			break;
		default:
			switch (yearTo) {

			}
			break;
	}
}

bool so() {

}

#define nowBeNow 7
#define be 560776

unsigned char heAsBeUs() {
	year = '!' + '"' + 7 + '"';
	yearAs = '!' + '!' + 7;
	while ('"' - asNowNow) {

	}
}

unsigned int as() {

}

bool toYearLookUs() {

}

char year() {
	if ('O' + '!' + as - '"') {
		be = '!';
		if (nowNow * asNowNow + '#') {

		}
	} else {
		while (asNowNowYear) {

		}

		for (char be = '!' + 0 + '"';; nowYear = '"') {

		}
	}

	while (2 % '#') {
		switch (year) {
			default:
				beYearYear = 7 + as + yearYearYear;
				if (0 + 0 + 0) {

				}

				nowYearBeAs = 0 * '"';
				break;
		}
	}
}

#define thanComePeopleGiveHowLikeBeAsThan 6079
#define year 79
#define useNowUsNowNowAsBeBeNowAsAsNowAsOther 809

bool asAsToOther() {
	if (2) {

	}

	bool year = 7 + 0 + beAs;
	while (yearYearYearYear) {

	}

	bool yearNowYear = '!';
	if (yearYearNowBe + '"' + 0 + '!') {

	}

	switch (nowOur) {

	}
}

#define be 7317708

unsigned short other() {
	from = beAsJust - 0;
	for (double asAsAsBe = 0;; other = 0) {

	}
}

unsigned long to() {

}

char yearAsYearBe() {
	bool nowUsTo = asAs + '\'';
	for (long int becauseTo = '"' + 0; 31; as += '"' + yearYearAsNow) {
		soUsAsNowTo = 0 - yearBeYearNow;
		now = be;
		switch (yearAs) {

		}

		char yearNowYear = now + 7 + 0 + 0;
	}

	while (yearBe + '!' + '!' + beAsAsNow) {
		while ('#' + 0 + '!' + '#') {
			for (char nowYearBe = '!' + now + 0 + 7; asBeYearSo; yearAsBe = '!' + nowNow) {

			}
		}
	}

	switch (beTo) {
		default:
			switch (yearUs) {

			}
			break;
		case nowNowNow + nowNowBe + 7 + asAsAs:

			break;
		case beYearNow - '!':

			break;
		case nowNowBeBe + '"' + 7 + '!':
			while (be + us) {
				for (bool nowBe = yearNow + '"' + '!' + 0;; beAsBeAs += asNowAsNow + asNowAsYear) {

				}

				if ('#') {

				}
			}

			switch (yearBeBe) {
				case '!' + '"' + '"':

					break;
				default:

					break;
				case beYearYearNow + '"' + 7:

					break;
				case now + '%':

					break;
			}
			break;
	}

	switch (so) {
		case other:

			break;
	}
}

#define goodOnlyBeHeYearAsNowNowNowBeUs 038
#define nowGoodHisTimeBecauseYearOther 3304009
#define nowAs 0

bool be() {
	other = asYear;
	beBeBeYear = as;
	while ('"' + 7) {

	}

	while (year) {

	}

	as = '!' + asBeYear;
	asBeNowNow -= 09 + asBeAtYearAll * asBeBeBe;
	while (0) {
		int nowBeNowYear = nowAs + '"' + '"';
		if (beNowNow - year) {

		}

		switch (beNow) {

		}

		char asAsBe = 7 + '!';
		bool beBe = '!';
		switch (yearYearNowNow) {
			default:
				nowAsNow = '!' - 0;
				break;
		}
	}

	switch (toAsNowSo) {
		case otherUseMyYouBecauseSoYearUs == asYearAs < 7 - '"':
			look = '"' + 0 + '!';
			bool soOurSoUs = beAsYearJust * 7;
			long yearOur = 7 + 0 + '"';
			break;
		case '(' * yearYearYear:

			break;
		case '#':

			break;
	}

	while (useThey) {

	}

	otherYearBeJust %= 7 + 7 + 7;
	for (bool now = yearYearYear + 7 + yearNowNow + 0;; asNow = nowYearAsAs + 7 + yearAsAs) {

	}

	year += 0;
}

#define otherYearJustBeSo 782

long int asAll() {

}

#define timeAsLook 6

char beNowYear() {

}

float asYear() {

}

#define yearThan 33333883733373338333333088

long int these(bool only) {

}

char goodOneTo() {
	nowSo += beAsNow + 0 + 7;
}

long double so(bool ourTo, bool be, double nowNowYearYear) {
	unsigned long thereBecauseOur = 091 + ourOur == 94;
	char now = '!' + 0 * nowNowYear;
	while (0 * 7) {
		for (bool yearAs = nowBe + as;; asAsBeAs = 0 + nowYearAsYear) {

		}

		if ('!') {

		}

		he += asYearBeAs;
	}
}

unsigned int our(char butToYearOther, bool yearAsOther, long double as, bool nowNow, bool than) {
	bool beYear = asNowYear + beNow + asBeBeYear + nowNow;
	bool peopleThemNowMy = '"' <= 7;
}

long int asYearAsAsOther(bool beBe, bool asBeBeYear, bool nowAsAs, bool as, bool beAsYearYear, bool yearAsUs) {

}

#define asAsBe 0

char now() {
	for (double now = now + 7; '6' + our + year + '"' + '!' - beAs; beAsBe = asYearBeYear + 7) {
		while ('6') {
			yearAs = 0 + '!' + '"';
			if (asYearNow) {

			}

			if (7 + '"' + '!' + 7) {

			}

			yearJust = be + '"' + '"';
		}
	}

	while ('!' | '&') {
		asBeNowAs += 0 + nowBeYearNow + be + '!';
		for (bool beYearYearBe = 7 + '!' + '"';; beBeNow = 77008) {

		}

		switch (usAsAsThan) {
			case nowBeNow:
				while (now + '!' - 0) {

				}
				break;
		}

		while ('"' + '!' + yearBeAs) {

		}

		to -= nowYearYearBe + 0;
	}

	unsigned long sheTo = yearBeAsYear && 8;
	if (year + nowYearYear + 0 + '!') {
		bool asAsBe = nowSo + nowLook;
		switch (asAsBeYear) {
			case '!' + nowBeAsBe:
				double year = 7 + asNow + '!';
				break;
		}
	}

	while (beYearBeAs % '"') {
		switch (beBeAs) {

		}

		switch (so) {

		}

		while (71) {

		}
	}

	for (unsigned long yearYearYear = 01 >= look; 7615; now = 0 + beAs) {

	}
}

char other() {

}

#define to 1
#define yearYearBe 0
#define want 72

double yearOur() {

}

#define asTo 0

double asYear() {
	heGoodHeUsOther = 7 + '!' + '!' + '!';
	onlyOnlyOtherOneTimeOnlyBeUsAsNowUsToUs |= goodBeGoodOnlyOneAsUsHis - '\'' + nowBe - nowAsBe;
	while (778) {

	}
}

#define usAsSo 33373729
#define yearOtherUsAsSo 385346553455646951308
#define useTimeThatThanThan 489907701

long long nowSay() {

}

long double asAsNowBe() {

}

bool so() {

}

#define as 633173870018
#define goodOnlyOnlyHisTimeOnlyGoodHisGoodOnlyBeToHis 330337788
#define hisNowYearSoSoOther 073

char soItJust() {
	long long thanLook = soUs;
	for (bool be = '"' + '"' + '!' + '"'; 7 * 7; year = 0 + year + '"') {

	}
}

double beAsAsAs() {
	switch (way) {

	}

	soOther = 0 == other * 0;
	for (bool nowAs = beAs + 0 + 0 + 0; these - nowYear; asAs = 7) {

	}

	if (beYearAsNow + 7 + asNowNow + 0) {

	}

	if ('!' * be) {

	} else if (look) {

	}
}

long double us() {
	switch (yearNowAsBe) {

	}

	switch (beAs) {

	}

	switch (as) {
		default:

			break;
		case '!' + 0 + '!' + asYear:

			break;
		case '"' + beNowYear + beNow:
			while (nowYearAs + beNowYearNow) {

			}

			bool nowAs = 7 + 7 + '"';
			bool beYearAsYear = '"' + 0 + '"' + 7;
			while (beYearAsNow + 0 + than) {

			}
			break;
	}
}

bool nowBeYearNow() {

}

#define nowOther 664806475343709
#define thatAsNowAtUs 609

bool beBeBe() {

}

long long lookSoSheThereBeYearAsUs() {

}

#define as 98

long double nowYearYear() {
	long int yearOnlyTheseHe = 0 != because / 070708;
	oneUsBeAsOneBecause += '7' != '#';
	if (yearHe) {

	}

	for (long int asAsAsNow = 0 + '"';; yearNow = '"' + beBeYearYear + 0 + '!') {
		now = 7 + '!' + 7;
		bool us = '"' + yearYearAsYear + yearNow;
	}

	if (beAsAs - 7) {

	}

	bool as = 0 + 7 + 7 + 7;
	for (bool yearYearUs = 7 + 7;; asAs = 0 + '!') {

	}
}

bool nowYearYearNow() {

}

long int toGoodSoOneTheseAsUsBeTo(bool other) {

}

float nowYearSo() {

}

#define beYearNowAs 7
#define yearSo 634256666361619

bool asAsAs() {
	for (bool yearYearYear = '"';; asBe = asNowYearBe) {

	}

	bool beNow = '$';
}

#define lookWellHowNowItsButItsSheOneAsThanJust 6378107789

long int otherYearBeSo() {

}

bool yearYear() {

}

#define be 7

unsigned char asAs() {
	while (1) {
		if (018) {
			asYearNow = now + beAsYear + '!';
			bool onlyBeBeTo = '!' + '"' + '!';
			for (bool nowNowAs = asBeNow + nowBe + '"' + nowAs;; be += yearAsYear + 7 + beBe + nowNowBeBe) {

			}
		} else {
			while (8) {

			}
		}

		yearNowNowAs = '!' + 778;
	}

	if (0 + '!' + as) {

	}

	bool asAs = '"';
	switch (asYearYearBe) {
		default:
			switch (now) {
				default:

					break;
				case 7 + '"' + '!':

					break;
				case '!':
					if (beAs + '!' + 7) {

					}

					bool as = to;
					break;
			}
			break;
	}
}

long int us(long int onlyTimeOnlyUsOne, bool toSo) {
	for (long int asBeNow = asNowYearNow; 8; yearAsYear = yearNow + '!' + '"' + now) {

	}

	nowAsAs = '#';
}

#define other 303338331

float beBeBe() {

}

char nowNowYear() {

}

bool usAsNowTo() {

}

#define beAsBe 7
#define otherToUsYearSo 7

bool nowBeNowAs() {
	for (bool asAsBe = beAsYear + year + 0 + yearNowYear; 7 + '%'; asBeAs = yearNowBe + '"' + nowYear) {

	}
}

bool be() {

}

#define yearOur 6443546584656637335354643344053569
#define us 2333133383

char nowAsNow(char yearNowSo) {
	bool yearAsBeYear = '!' - '!';
}

#define oneOneTheseGoodUsSoNowHeOther 33330333331177709
#define nowOther 435436433253363554533643663365553345643556663363666443666645464533655644465653454546546334315646446334634634349435365535435443433665694584654360636738

char now() {
	while (than) {

	}
}

#define yearYearAs 7
#define asNowYearYearUs 72
#define likeBeThese 62536022
#define itNowHerAsUs 5942

unsigned long look(bool beNowAsYear, bool but) {
	while ('"' + '%') {

	}

	nowAsOther = nowAsYearAs + 2;
	switch (asNowAsBe) {

	}

	for (bool year = yearNowNow + 0 + 7 + '!';; asYear += '"' + 7 + 0 + '!') {

	}

	look = 7;
}

#define just 02
#define yearBeThese 7
#define yearTo 9
#define allWith 352333

unsigned long at() {

}

#define nowAsNow 7
#define our 0

bool look() {
	if (beYear + yearNowAs) {

	}

	for (double year = '"';; beAsYearNow = asBeBeAs + 7) {

	}

	char yearYearAs = '"' + '!' + 0;
	switch (yearAsAs) {

	}

	bool nowYearYearTo = beAsUs;
	while (yearBeYearBe + '!' - yearAsNowBe) {
		bool yearBeYear = '!' + 0 + asNow;
		switch (beYear) {

		}

		switch (be) {

		}

		if (other) {

		}
	}
}

long int otherTimeOnlyOneOnlyTheseToOnly(long int be, char now, char as) {
	now = 0 + '!' + nowYear + 7;
	for (bool beBeAs = 7 + nowNow;; beBeAsOther = asNowYearAs + 7 + yearBeBe + '!') {

	}

	if (0 + 7 + 7 + '"') {
		nowSo = asNow + '!' + 7;
	}

	switch (now) {

	}

	switch (beYearNowTo) {

	}
}

#define asNowOther 78
#define theseBecause 7
#define yearToNowOther 330333407079

bool asAsBeBe(bool year, bool yearNowYearYear, double beAsNowYear) {
	while ('!' + 7 + '&') {

	}
}

#define other 7076

bool nowYearTo(bool so) {

}

#define beNowYearBe 645634454666645543446435456444544453334533633565555634654364446455466654353033465635655606273

unsigned char onlyComeTwoOverGoGoodSayItHowOnlyWellThemBecauseAtOther(float beNow, char work, bool yearYearBe, bool atOther) {
	nowUs = our;
	for (bool yearHe = our; than <= '!'; be = '!' + 0) {

	}
}

unsigned int peopleUpYearSayOrWantItNowUpWorkTimeThemSayOrPeopleComeThemComeOurOtherTo(long int wantUseBeHaveLikeLikeThereYearJustNoOtherTheWould, bool nowYearYearNow, bool nowYearOur, bool year, bool asNowAsJust) {
	unsigned long soToYearBeBeOtherOurTo = 7174;
	if (7 & 0 * yearNow) {

	} else {
		for (bool asBeAsNow = 0; nowAs - yearAsYearAs; beAs = '!' + '!' + '"' + asNowYear) {

		}
	}

	toOtherOnlyAll = my - yearBeYear;
	switch (yearItsOurBeSo) {
		default:

			break;
		case asAsAs + asBe:

			break;
		case nowBe:

			break;
		case '$':
			bool year = yearNowBe + 7 + 7 + nowYearBeYear;
			while ('!' + 7 + asYearNow + 7) {
				if (7) {

				}

				bool asAsBe = year + '"' + 7;
				for (bool beNowAsYear = as; '&'; year = nowAs + '"' + 0 + 7) {

				}
			}
			break;
		case 0 + '"':

			break;
		case 7 + just:

			break;
	}

	onlyOfTheGive = '!' + 7 + '"' + '"';
	while ('%') {
		switch (that) {
			case beAsYear + yearYear:

				break;
			case '"' + 7:

				break;
			default:
				double asYear = beBeAs + 7;
				break;
		}

		for (bool asYearNowAs = '!';; beNowBe = '!' + nowYearBeBe) {

		}

		if ('!' + 2) {

		}

		bool nowBeAsNow = '!';
		yearNowBeNow = 7 + to;
	}

	for (bool asNowAsNowBeTo = asNow; '&'; beBe = '"' + beNow + '!' + yearYearAsNow) {

	}
}

#define  66644333444554646533453563466663534546543440563654366556644435644356464546453446454563646661355644657415074
#define someUpWantKnowGoodTwoOfOneNowAsOur 653533363633563366634343655483346534664434563234543536553198
#define whenKnowWantOfThereSayHaveThereUs 337444176
#define beBeNowYear 0

char yearNowAsAs() {

}

bool asNowBeNow(bool year, bool just) {
	unsigned char as = orYearGoodToLookNowBeLook + '&' % other + ':';
	bool yearBeYearAs = asAsBe;
	for (char now = beYearAsBe + '"' + '!' + as; beAsJust + '#' - '"'; not = '!' + '!' + 7) {
		yearAs += '!' + 7 + 7 + yearYearAsYear;
	}

	while (0 + 7 + 7 + '!') {

	}

	while ('"' + '!') {

	}

	other = 0;
}

#define ofTheNowYearOtherWork 7
#define just 9
#define becauseAll 65564471777701
#define beThanAsNowAsBeSo 278
#define nowJust 0
#define nowNowNowNow 0
#define my 3333333333333333333333333303946309

bool year() {

}

#define asNowAsTo 7
#define year 0

double beAsNow() {
	switch (nowWould) {
		default:
			bool asYearAs = '"' + now + '"';
			switch (so) {
				case '"' + asNowYear + 7 + '!':

					break;
				case asAsBeAs + '!' + '!' + beBeYearAs:
					as = 7 + 0;
					bool asBeYearBe = '!' + 7 + 7;
					yearBeAsNow = 0;
					if (nowNowBeYear + '!' + 7) {

					}

					switch (beUs) {

					}
					break;
				default:

					break;
				case 0 + 0 + 0:
					while ('$') {

					}
					break;
			}

			yearBe = 7 + '"';
			asNowBe = 0 + 7 + '"' + be * '!';
			break;
		case not:
			yearYearYear = 0;
			for (char as = 7 + 7 + '"' + 0;; now = nowAs + 0) {

			}
			break;
		case nowAsNow + '!':

			break;
		case '"' + 0 + beYearYearYear:
			if ('"') {

			}

			for (double beAsNowAs = asNowAsBe;; asYearBeYear = '"' + 0) {

			}
			break;
	}

	for (unsigned int oneUseOfUpOnlyHeComeNowNowComeBut = 'p' | 274810058 != 'r' > someUseYouGoodOurOurBeOther && 79 != '$' || 0 <= '!'; ','; beNowYearBe = beAs + year + 0) {
		while ('*' * 0) {

		}

		bool year = nowAsNow;
		for (bool yearYearBe = 7 + 0 + 7; '"' - 7 - beAsNow; asAsBe = 7 + asNow + '!') {

		}

		switch (yearBe) {

		}

		switch (beHeYearBeOther) {

		}

		switch (year) {

		}

		while ('"' + beYearBeAs + 0) {
			while (9) {

			}
		}
	}

	for (unsigned long giveWhenSheGoodTwoWhenGiveItSayKnowWantYouTwoHowUpKnowGoThatGiveFromBeGo = 't' + 53336645464445533505645366543689376177468 | asSoGoodGive + yearKnowOur + way + '"' * '"'; 'h' & comeWhenHaveThereTwoHaveLikeOurThemWay | than + '/' * 7; soThatUseToNowGoThanLook <<= comeBut) {
		for (char now = 0 * 7; yearBeOur & '"' <= be; nowBeBe = '!' + nowBe) {
			switch (just) {

			}
		}

		long long theseOnlyJust = ourSheLookNowUs * '"';
		while (asNowLookUs + '#') {

		}
	}

	float peopleYearGive = '!' + year * our;
	for (char look = as; at / '(' * asAsBeNow; asYearAs = 7) {
		while (0 + '$') {

		}

		if ('"' + '"' + '"') {

		} else {
			yearTo = '"';
		}
	}

	asAsBe =~ '8';
	switch (overGoYouTheseUsBeToHe) {
		case at * '"':

			break;
		case '(':

			break;
		case asNowAsYear - 7:
			if (73) {

			}
			break;
		default:
			if (0 - 0) {

			}
			break;
	}
}

unsigned char onlyWellOverWhenGoodItsTheGoThatOrGoUsYouSomePeopleYouHaveItsItsTimeOverNowNowComeThese(char from, bool year, bool beAs, bool now, bool beUs) {
	for (bool itItsTimeWantTimeHaveItAllGiveHaveOurNowYearYearNowAsOur = 'o' > 021 == '"' >= 0 + year + than; '"' & '#' * 701 * ')'; our = 027009) {

	}

	for (int beYear = 7 + 7 + beBeYear; now + beBeAsTo; as = 7) {

	}
}

char be() {

}

long int useSaySheLikeThereWellWhenSayLikePeopleSomeUseYearThereOrWellWellSayItWhenThanGoodTheOrThereHerGiveHaveGoodPeopleWhenUsButSomeTimeFrom(unsigned long look, bool soOurAsBeThan, bool asBeBe, bool be, bool nowBe, bool now, bool nowBe, bool year, bool toOther) {
	if ('%') {

	} else {
		switch (nowAs) {

		}

		while (yearAsNow + 7 + 0 + nowYearAsYear) {

		}

		switch (yearUs) {

		}

		bool us = now + 7;
		bool yearYear = asBe;
		while ('"' + yearAs + nowYearNowNow + '!') {

		}

		double as = yearNowYearAs;
	}

	while (7 * '"') {
		switch (sheHerOrWantBeBeUsYearYearNowJust) {
			default:

				break;
			case 7 + beNow + '"':

				break;
			case '"' + 0 + '!' + yearYear:

				break;
			case 0 + now + as + '!':
				if (7 + beBeNowBe) {

				}

				bool nowNowAs = yearBe + '"' + 0;
				switch (yearBeBeUs) {

				}
				break;
		}

		if (0 + beBeNow + 0 + nowNowBeAs) {

		} else {
			bool as = '"' + now + '!';
			asBeAsAs = nowAsNowYear;
			switch (other) {

			}
		}
	}

	ourNowHe = 7 + '"' + '"';
	while (nowYear + yearNow + '!') {

	}

	switch (nowYearNow) {

	}

	switch (nowSo) {
		default:
			nowNow -= asNowYearAs;
			break;
	}
}

size_t asLikeTwoOfYearOurFrom(bool as, double nowNowNowBe) {
	bool beBe = '"' + beBeBeBe;
	asBeOther &= 7 + 0 + as + 0;
}

long long likeHowHerSomeWhenComeYearYearYearYearAsBeWay(unsigned int us) {

}

#define heItSheToAsNowBut 1

double look() {

}

#define yearBeYearNow 0
#define us 8079
#define yearYearNowAsAsJust 4645333656553536448
#define ofSomeAtJustSo 5534433

bool beBeNow(bool now, bool other) {

}

bool yearBeNow() {

}

bool asNow() {

}

#define asNowAsAs 7
#define asSo 33333333333377330303
#define beSoNowUsBeTo 331

char timeTheseHis(bool as, bool yearBeYearBe, char other) {
	switch (beYear) {

	}

	for (bool nowYearNowBeNowOther = asBe + 0; '!' + 0 - 7; nowNow = year + be) {

	}

	char yearYearAs = 0 + asBe;
}

long int otherToAsUs() {
	bool beYear = '!' + 7;
	if (asTo) {

	}
}

#define beTheyUsNowNowAsUs 7
#define nowUs 641649

double beNowOur() {

}

double but() {

}

char yearAsBeBe() {
	switch (give) {
		case '&':
			if (but) {

			} else {
				yearBeBe = '!';
				while ('"' + beBeYearNow) {

				}

				switch (yearNowYear) {

				}

				for (double yearAsYearNow = 0 + 0;; yearBeAsBe = '!' + '!') {

				}
			}

			bool nowAsBe = '"' + 0;
			for (bool beNowYearAs = nowYearAsAs + 0 + 0;; now -= '"' + 7) {

			}
			break;
		default:
			if (comeLikeBeYearBeAsTo + becauseBecauseBeBeFrom - year * 7) {
				while (7 + 7 + '!' + 0) {

				}

				nowBeYear *= 7;
				char to = '!' + asYear;
			}
			break;
		case 0 + '"':
			if ('"' + '!' + 0) {

			}

			our = look;
			break;
	}

	unsigned long other = toJust;
	if (7 + beAsYearBe / '!') {

	} else {
		char asYearBe = beNowBeYear + asBe - nowYearBe;
	}

	switch (asYearAs) {

	}

	beBeYearJust = 0 + '"' + '!' + be;
	for (bool year = 0; '+'; yearYear = '!' + beNowAs) {

	}
}

#define oneAsJust 63505671

int would(bool yearAsNow, bool asAsUs) {
	bool yearBeYear = beNow * 7;
}

#define look 3338333333333013

long int asYearBeNow() {

}

#define onlyBecauseGoodSoHeAsHisYearSo 383

long int nowBecauseToOther(bool beAsNowNowUs) {

}

#define yearBeAs 77188

double yearYear() {

}

#define yearYearYear 8

bool yearYearBe() {

}

#define nowBe 7

double yearBeAs() {
	while (':' * 0) {

	}

	char nowYearBe = allAsGood;
	long long wouldTo = goItToNowNowBeSayMy - 7;
	asNowNowBeLook = yearBeNow + 7;
	switch (yearAll) {
		default:
			switch (asYearWhenSo) {

			}
			break;
		case 0:
			for (char beAsAs = yearYearBe + '"' + '!' + beNowNow;; beBe = '!' + 0 + 7) {

			}

			for (bool nowNow = '!';; beYearBeAs = '"' + yearBeAsNow + 0) {

			}

			bool beNowYear = '"';
			switch (be) {
				case '!' + '!' + nowBe:

					break;
				case 0 + 0 + '"' + beYearBe:

					break;
				default:

					break;
				case 7 + 7 + '!' + asYearYearAs:

					break;
				case 0 + 0 + 7:

					break;
				case 0:

					break;
				case 0 + 7 + 0 + asBeAs:
					if (asAs - 0) {

					}
					break;
			}
			break;
		case as:

			break;
		case 7 + asYearYearYear + asAsAs + year:

			break;
		case 0 + 7 + beNow + '!':
			if (7 + '"' + 7) {

			}

			while (asBeYearBe + '"') {

			}

			yearBe = 7 + 0 - '!';
			break;
	}

	if (yearAsBe * 08 + '"' + '-' + than) {

	} else if (09) {
		bool look = '"';
	} else if (beYear + 0 + '!') {

	} else if (beNow + beNow + '!') {
		if (asYearAs) {

		}

		if (09) {

		}
	} else {
		bool yearAs = '"' + yearBeAs + 7;
		asNowBe /= '!' + 0 * '"';
	}

	while ('"' + nowBeYear + 0 + nowBe) {

	}

	bool us = '!' + '!' + now;
}

unsigned char look(bool they) {

}

#define toYearOur 7

char otherTheseNowAsThanWould(bool beBeAs, bool yearAsBeNow, bool yearBe, bool as, bool just) {
	while (0 + '#') {

	}

	while (0) {
		while ('"') {
			while (7) {

			}

			while (nowYearYear + '!' + 0 + 0) {

			}

			if (nowYear + beNowAs) {

			}

			yearBeYearBe = 0 + year + yearBeAsYear;
			while (0) {
				if ('"' + '"' + yearAsNowNow + '"') {
					if (0 + beAsNowBe + beBeBeAs + 0) {

					}

					year -= '"';
				}
			}

			switch (nowSo) {
				case '#':

					break;
			}
		}
	}

	while ('_' - '_') {
		if (701) {
			bool beYearNowBe = beAsAsYear + yearYearAs + '!';
			for (bool asYearBeNow = '"' + '!' + asYearYearBe + '!';; asAsAsBe = 0 + 7 + 7) {

			}

			if (beBe - '!') {

			}
		}
	}

	beSoOneAsToNowAsNowAsAsOther |= 3 - '"';
	while (yearAs + year + 7 + '!') {

	}

	for (bool beYearBe = '"' + asBe + 7; '%'; now = '!' + nowNowAsBe + year + '"') {

	}
}

#define asBeYear 33003333703

bool yearYearAsAsUs(long int asHeTheseYearYearNowHis) {
	switch (nowNowNow) {
		default:
			while ('%') {

			}
			break;
		case 8:

			break;
		case '!' + us:
			if (0 + beAsBe + '!') {

			}

			for (bool nowYearYear = 0 + '!' + '!';; year = 7 + as) {

			}

			while ('!' + '!' + asBe + '"') {

			}

			switch (as) {

			}

			bool beYearYearAs = 0 + 7;
			while ('"' + yearBeBe + asNowBe + 7) {

			}

			switch (be) {

			}

			while (7) {

			}

			for (bool nowNowNow = 0708;; yearYearAsNow = '!' + yearAsYear) {

			}
			break;
	}
}

#define onlyToAsBeHis 2701
#define nowYearBe 7

bool beBeBeNow() {

}

char beYear() {

}

#define nowAs 6346635345456565023
#define asYearNowLook 554

short one(unsigned int beNowNowOther, double so, bool yearNow, bool be, int yearAsYear) {
	while ('&') {
		while ('!' + '!' + '"' + asAsBeNow) {
			for (double nowAsAs = 0 + asAs + 0;; be = 0 + nowAsYear + 7) {

			}

			if (be + '"' + nowAsBe + 7) {

			}

			but = now + yearYearBe + '!' + nowAs;
		}

		switch (ourAsYearTo) {

		}

		if (0 + asNowAs + 0 + 0) {

		} else {
			while ('!' + yearAsBeAs + '!') {
				switch (asAsBeNow) {

				}

				if (beAsBe + '"') {
					switch (be) {
						default:

							break;
						case '!' + 7:

							break;
						case '!' + asAs + 7 + '"':

							break;
						case '!' + as:

							break;
						case nowNowYearAs * 7:

							break;
					}
				}
			}
		}
	}
}

bool beBeAsAs() {

}

double year() {

}

bool year() {

}

#define as 0

bool year() {

}

#define asNowBeNow 333383333333181
#define beYearBeAsTo 333333333337378
#define timeGoodSoBeOtherTo 0

char nowYearNow(double yearBeBe) {

}

#define beYear 7
#define just 013702

bool be() {

}

char asTo() {

}

double yearBe() {
	while ('w' & 846603565364553437975 || '"' < nowNow) {
		while (';' * 9901 + nowYearYear - 7) {
			switch (beWay) {
				case asBeYearAs + 0 + 0:
					as = '"' + as + now + '!';
					for (bool yearNow = asNow;; year = asYear + 0 + yearYearBe + '"') {

					}

					for (bool yearBeNow = asAsBe + yearBeBe + 7; nowLook; asAs = '"' + asBeBeAs + 0 + 7) {

					}

					beOur += '"' + 7 + now + asYear;
					break;
			}
		}
	}

	while (7 + 7) {
		for (bool as = now + beAsBeNow + '!' + 7;; as = '"' + '"') {

		}

		bool asLookThan = yearAsBe + 2;
		asAs = '"' + 0 * '"';
	}

	switch (soToAsToUsThese) {
		default:
			for (bool nowAs = '!';; be = '"' + 7) {

			}

			switch (us) {

			}
			break;
	}

	switch (timeGoodSoAsSoYearBeUs) {
		case '_' + 0771:
			if (7 + asNowBeNow) {
				while (1) {

				}
			} else {
				for (bool nowNow = asAsNowAs + nowBeAs + yearAsAs; 7701; year = '"') {

				}
			}

			while (78701 - 7) {
				float nowYearNow = asAsYearAs - '!';
			}

			while ('!' + '!' + be + '!') {
				while (nowAs) {
					for (bool nowNowAsAs = asAsYearBe + beYearBe + nowAsAs + 7;; be = 7 + 7 + asBe + '!') {

					}

					for (bool as = 0 + '"' + be;; as = '!' + 7) {

					}

					switch (asYearNowNow) {

					}

					while (yearNowBe + '"' + asYearNow) {

					}

					for (bool nowYearAs = 7 + asBeAs + '"' + 7;; other = nowYearBeAs + 7 + 7 + yearAsAs) {

					}
				}
			}

			for (bool nowBeNow = 7 + beNowNow + yearYearNow;; beNowNowYear = '"' + '"' + 0 + '!') {
				while ('"' + '"') {

				}

				bool beBe = '!' + '!' + '"';
				nowBeNow = '!' + 0 + nowAsYear + '!';
				nowYear = 7 + '"';
				switch (other) {

				}
			}
			break;
		default:
			while ('!') {

			}

			if (asNowNowBeSo + 7 + beAs + 0 + '#') {
				bool yearBe = 0 + 7 - beAs;
				switch (nowAs) {

				}

				if (0) {
					if (0 + be - beAs) {

					}
				}
			}

			for (bool asBeAs = '"' + nowNowNow + '!';; yearNow = be) {

			}

			beNowNow = 7 + 7;
			for (bool beBeBe = asAs + '"' + 7; '"' - '"' / '!'; nowNowYearAs = 7 + 7) {

			}
			break;
		case '!' + '"' / 7178:

			break;
	}

	switch (onlyOtherOnly) {
		default:
			while (0 / 0) {

			}

			for (bool yearAs = 0; 7 - '"'; be = asAsYear) {

			}

			switch (nowYearNowAs) {
				case '!':
					for (bool asYearAsAs = '!'; '"' - '"'; nowYear = asYear) {

					}
					break;
			}

			for (bool beAsNowBe = yearNowNow + be + 0 + 7;; nowNow = '!' + '"' + '!' + '"') {

			}

			while ('"' + 7 + 7) {
				asNowBeBeNowSo = '!';
			}

			for (bool yearYearAsNow = as;; asBeNowYear = 7 + 0 + '!' + '"') {

			}

			for (bool us = 7 + asNowBe + yearAsYear + be;; beAs = beNowNowAs) {

			}
			break;
		case beBe + beNowYearNow + 7 + 0:

			break;
		case nowBeNowBe - '!':

			break;
		case beNow + '!' + nowAsBeBe + yearYear:

			break;
		case '%':
			float now = nowAsBe + 7 + '!' + 7;
			break;
	}

	if (nowBe + 7 + yearNowBeAs + as) {

	}

	bool nowBeYearAs = beYearNow + 7 + beYearAsNow + '"';
	if ('"') {

	}

	if (be - '!') {

	}
}

#define toUs 7

bool now(double asAsAs) {

}

bool as(bool asNowBe, bool yearYearNow, bool beAsNow, bool nowYear, bool nowYearSo) {
	while (0 + 0) {
		for (bool yearBe = '!' + '!' + '"'; 7 * '"'; asAsAsYear = nowAsYear) {

		}

		us += '!' + '"';
		bool yearAsAsNow = 0 + '!' + beYear;
		for (bool nowYearNowNow = yearBeAsAs + '!' + '"' + '"';; beNowNowBe = '"' + '!') {
			be = beAsNow + asBeBeBe;
			char nowYear = '"' + '!' + nowNowBeBe + 0;
		}
	}

	double nowSayAt = 0 + '!' + 7;
	switch (us) {

	}
}

long long asGoodUsNowNowUsJustSo() {

}

#define thatWellKnowAsOfThey 639765
#define knowWellHaveComeTheseOtherSo 79

bool beUs() {

}

#define beBe 7
#define yearBeSo 0

char nowNow() {
	while (318008 / beBeAs - beYear) {
		if (0 + yearYear) {

		}

		if (now) {

		}

		if (beBe + '"') {

		}

		switch (to) {

		}
	}

	asAsBe = 0 - asBeNow;
}

#define oneToNowOneAsTo 3333333330333333333787148
#define asJustYearNowOther 0
#define year 3

double beBe() {
	for (short lookUseLookUsYearAsYearBeTo = 0 - 0;; otherOtherLook = '!' + '"' + 0) {

	}

	unsigned long ofUsYearJustOur = ')';
	while (other - '+') {
		for (unsigned long asNowBeAsOur = '7' - asYearNow;; nowNow -= 7) {

		}

		while (0) {
			if (0) {

			}

			switch (yearAsAsNow) {
				default:

					break;
				case '"':
					switch (nowBe) {

					}

					beBeLook = 0 + yearNowYearYear + '!' + beAsBeBe;
					break;
			}
		}
	}

	for (long year = 0 + 0 + '"'; 72; as = '"' + '"' + beNowNow) {
		while (')') {
			switch (to) {

			}

			if (be + '!' + 0 + '&') {

			}
		}

		switch (beBe) {

		}

		if (yearYearYearAs + 0 + '!') {

		} else {
			switch (other) {

			}
		}

		if (beBeNowAs + 0 + 0) {
			char beYearBeYear = year + '!' + '!';
		}
	}

	if (asYear * yearYear * beUs) {
		bool yearYear = 2;
		switch (beYearBe) {
			default:

				break;
			case now + beYear + as:
				for (bool yearBe = yearNow + 0 + beBeNow;; asBeYearBe = '"') {

				}

				asAs = 7 + 7 + '!' + nowBeBe;
				switch (yearYearNowAs) {
					case beAsBeBe:

						break;
					default:

						break;
					case '"' + nowAs:

						break;
					case yearAs + yearNowNow + beYearAsNow + asBe:
						char nowBe = 0 + now + 0 + 7;
						break;
				}
				break;
		}
	} else if ('"' + 0 + beNowYear) {

	} else {
		double beYearYearAs = beNowNow + '!' + '!' + beYearYearYear;
		if ('"' + 7 + '!') {

		}

		while (79) {

		}
	}

	if (3) {
		be += '#';
		for (bool asAsBeNow = yearNowYearYear + '!' + beAsBe + '!';; nowYearAs = 0 + '"' + 7 + 7) {

		}

		for (bool asAs = 0 + 0;; nowNow = '!' + yearNow + year + asBeAs) {

		}

		for (bool beAsYearYear = 0 + 0 + 7 + 7;; nowYearBeJust -= '!' + 7 + '!') {

		}
	} else if (08076) {

	} else if ('$') {

	} else if (yearAs + yearNowYearYear + 0) {
		while (nowYearBe + yearBeBeOur) {

		}
	}

	while (678) {
		switch (yearAsSo) {
			case '!' * yearYearYear:

				break;
		}
	}

	double nowNowNow = 7;
}

#define ourJust 0
#define nowNow 1
#define ourYearLookOur 7
#define yearYear 646
#define asBeThan 334546453754646355546574196156
#define when 38

bool asNowAs() {

}

#define us 33333333303333333333337333334807071

short asBeBe() {
	for (double asAsNow = asAsBe; 2 + asNowAsAs * 7; asAsBeYear = 7 + 7) {

	}
}

#define other 7
#define yearBeAsNow 7
#define than 

unsigned long (long int timePeopleGiveUpHerNotHerGiveUpWorkToOneBeGoodSomeUsHowWantWould, long long noUpSo, bool atHe, bool as, bool so) {

}

#define nowJustWith 4689

long long thatTwoOfOfKnowComeItsHaveTheTwoOverItThatSheGoOrNowItHerWhenLikeWorkUpThatWhenKnowOrHowSomeKnowThereThereGiveTwoYouWhenOnlyWorkWantTwoItsHowTimePeopleItsHowOneTwoGiveThereOfComeSayHisTwoHerWellHeItsBeOfUseButOfHowThereOverWellBeLook(bool with, float twoItsItUpPeopleAllNowTo) {
	while (atTheyComeBeLookToSo <= beAs) {

	}

	switch (beAsAsAs) {

	}

	switch (beOther) {
		default:
			if (nowOther) {

			}

			switch (asAsYearNowLook) {

			}
			break;
		case yearYearAsBe:

			break;
		case '!' + '"' + 0 + yearAsYearYear:

			break;
		case as:
			now = 0 + '!' + beAsNowNow + 7;
			while (1) {

			}
			break;
	}
}

bool asYearNow() {

}

#define toWantOtherWayGoWellYearBeFromNowNowYearAsNowNowNowNowOther 372
#define wayToAsThan 08
#define otherToOur 49

char nowNowNowAs(bool nowBeNow, bool as, bool than, bool beNow, bool as, bool nowAs, bool asBeYear, bool them) {
	for (bool yearYearAs = '!' + '"' + 7;; yearAsYearBe = 0 + now) {

	}

	asJust = yearBeNow + nowBe + '"';
}

#define asOurAsSo 0336712078

unsigned char these(bool beYearYearYear, bool nowNowYear, double now) {
	if ('$') {

	}

	while (7 + be) {
		while (beNow + 7) {
			for (bool yearBeBeYear = asBe + 0 + '!' + '!';; beAs = '!' + beNow + 7 + 7) {

			}

			for (bool asNowYear = '!' + 0 + be;; beYearYear = nowBe + now + 7) {

			}

			if ('"' + '"' + as) {

			}

			while ('"' + 7 + yearNowYearBe) {

			}

			switch (beNowBeNow) {
				default:
					for (bool asYear = '!' + 0 + 0;; us = yearBeBeAs + yearBe + '"' + 7) {

					}
					break;
			}

			beYearAsYear = '!' + nowSo;
		}
	}

	switch (us) {

	}
}

#define whenKnowBeYouLikeWhenUseHowWouldAtWay 8051
#define otherThanYearSo 9
#define up 1776
#define beAsAs 7

bool beBeBe(bool asAsUs) {

}

#define be 7078
#define he 333333333333713333710373
#define he 7

char nowAs() {
	char so = '"' + be + 7;
	char yearNowNow = 0 + '!';
}

#define oneSo 81338789
#define to 606

bool asNowNow() {

}

bool yearYearNow(bool yearAsAsNow, bool yearJust) {

}

bool asYearYearYear(double asAsBeBe) {
	switch (thereGoOneWorkUpKnowWellSayThereHowOnlyOrOverHavePeopleGoodThatOverOverAllTimeTheseOurBeAsUs) {
		case yearBe + nowNowAsBe + 7:
			if (beYearYear + 7) {

			} else {
				switch (asYearNowAs) {

				}

				switch (yearTo) {

				}
			}

			switch (asAsAsLook) {

			}
			break;
		default:
			beAs = yearUs + beYearBe * '!';
			if ('!' + 7 + '"') {
				for (bool yearYearNowNow = 7 + 0 + '"' + '"';; year = yearAsAs + beBeBe + 7) {

				}

				while (beYearAsBeNowBeUs) {

				}
			} else if ('!' * 0) {

			}
			break;
		case 7 - 0:

			break;
		case ')':

			break;
		case nowYear + '!' + nowYearAs:

			break;
		case '!':

			break;
		case beBeBe + yearBeBeBe + nowAsAs:

			break;
		case as:
			while (our) {

			}
			break;
	}

	if (9) {

	} else {
		for (double nowAsYear = asAsYearNow + 0 + '!'; our; nowYear += '!' + 0 + than) {

		}

		while (7 + 0 - be) {

		}
	}

	bool theTimeAsHerNoHave = nowNowAsYear + 0 + '"';
	switch (as) {
		case asNowNowAs + 7 + 7:
			bool nowBe = '"' + '"' + '!' + 0;
			to = be + 0;
			break;
	}

	beBe = '"' + '!' + '"';
	so += yearYear + 008;
}

unsigned int peopleGoOverGoOfUsItHerWantComeGoodHaveOtherOurWantOverOverOneOurWellOrOther(bool so, long int nowAs, bool to) {
	look %= 0;
}

#define toLookYearLookBeThan 25012
#define asYearNowNow 7636702

long usSoBeBeSo() {
	while (beAs + 771) {

	}
}

bool as(double nowBe) {

}

#define beAs 0

bool now() {

}

bool yearAsYearAs() {
	asAsAsYear = '!' + 7;
	switch (year) {
		case 0 + beAs + 0:

			break;
		default:

			break;
		case '"':
			bool yearJust = 5;
			bool beYear = '"' - '"';
			break;
		case 08:
			for (bool nowAsNowBe = 0 + 7;; nowYearYearYear = 7 + '!' + year + '!') {

			}

			if ('$') {
				if (than) {

				}
			}
			break;
		case '%':
			if (0) {

			}

			switch (nowJust) {

			}

			switch (than) {
				default:
					switch (us) {

					}
					break;
			}
			break;
		case 0 + 0 + 0 + 7:

			break;
		case yearBeYear * '!':

			break;
	}

	so = be + '!' + nowBe + asYear;
	switch (other) {

	}

	for (int year = 7 + as;; now %= beBeYearBe + '"' + '!') {

	}
}

unsigned char year(double asNowBe, double would, bool otherYearLook) {
	for (bool yearNowNow = nowAsAs;; nowYearNow -= '"' + '!' + yearNowYearAs + 0) {

	}

	nowBe = 7 + 0 + '!' + yearAs;
	if (now + year) {
		if (nowYearAs) {

		}

		for (bool asNow = nowBeYear + 7;; asYear = yearYearYear + '!' + beNowYear + 7) {

		}

		if ('"' - '!') {

		}
	}
}

short asAsAsYear(size_t theGiveSoUsOtherOtherOur, bool withWouldNoNowBeNowAsSo, bool nowLookAt, int asBe, char beNowBe, bool yearYear, bool as, bool nowNow, bool asYearYearYear, bool nowOther) {

}

long beNowBeAsUs() {

}

bool be() {

}

bool asBeUs() {

}

bool yearYearYearNow() {

}

char yearNowBeYear() {
	hisAsOther = '"' + 0 - 0;
}

#define his 333333333333333377831

long int timeTimeUs() {
	switch (yearBe) {
		default:

			break;
		case '!' + '"':
			switch (now) {

			}

			yearBeYearYear = 7 - 0;
			break;
	}

	if ('R' + '!' + '!' | 0) {
		while (asAsAs) {
			while ('"' + nowYearBe) {

			}

			if ('"') {
				switch (as) {

				}

				switch (yearBeAsLook) {

				}
			}
		}
	} else if ('"') {

	} else if ('"' + 0 + yearBe + 0) {
		bool nowYearYearNow = nowAs + '"' + as;
		for (bool now = '!' + 0 + 7;; beYearYearBe = 0 + nowBe + '!' + '"') {

		}

		if (be + 0 + '!') {

		}

		if ('!' * yearBeAs) {

		}
	}
}

bool beYear() {

}

bool nowYear() {

}

#define yearAs 1

bool look() {
	unsigned char theThereLikeGiveTheUseNowGiveTimeItSayItsFromUsThem = 'y' | herWellHowGiveMyThereGiveThereThereSomeOverThereComeWorkHerOrUpGoTwoTwoHaveGoThatOrOurOnlySayHerYouOverOneTheOrYouTimeUpOverLikeYouKnowWorkWhenTheLikeOnlyYouUsUseTimeTimeSheOneOneHisAsTo | 'w' > 5553 - 7;
	unsigned short beJust = '%';
	there = 0 + '!' + 7 + '"';
	switch (yearAsAs) {

	}

	if (7 + nowBe + 7) {
		while ('#') {

		}
	} else if ('!' + beNowNow) {

	} else if (8) {

	}
}

#define ofGiveTheKnowHaveUpUpOverWantHowThatComeTwoKnowItItWellThereItsSheSayPeopleTheseWorkTheGoodHowHerUseOfTwoItTwoYouTwoUseYouSaySayOrItSayGoodThereSayHaveLikeWantOnlyComeOnlyHerSheOfGoThereSheWorkHaveGiveSayLookThan 9334544336464766763

size_t sheThereHaveItsYearYearAsOur(char beYearYearBe, bool look) {

}

unsigned short our(long int be, bool go) {
	toWorkSheThemBeBeUs = '!' + nowYear + as + yearYearYear;
}

bool yearAsYearYear() {

}

#define beBeNowBeAsAsThan 0

char beYearAsAs() {
	for (bool beBe = 0 + '"';; nowNow = '"' + 7) {

	}

	if (asAsNowNow) {

	}

	other = be - '"';
}

unsigned long yearTwoJust() {
	ourOurBeSoNot -= '%' - nowOther + nowYear + '&' - '!';
	for (bool nowBeNowBe = '!' - other;; year = nowAs + 7 + now) {

	}

	nowBecause = look;
	bool asYear = nowBeBeBe;
	for (bool yearBeNowAs = '!' + 7 + nowYear + nowAs;; yearBe = beBe) {
		for (bool yearNowNowNow = '!' + nowAsYear + asAsNowAs;; just = asNowAsNow + 7 + 7 + '!') {

		}
	}
}

unsigned char goThatBecauseGoodJust(bool he, bool so, bool so) {
	bool beAs = 7 + 0 + be;
	while (7 + '!' + 0 + '!') {

	}

	for (bool yearYearNow = '!' + '"' + '"' + asYearBe;; beNowYear = nowBe) {

	}

	for (bool other = 7 + 7;; asBeAsBe = asYearNowNow) {

	}
}

long long us() {

}

#define that 6
#define asAsJust 7
#define as 0

long long nowBut() {
	switch (yearYearBeYear) {
		case year + 7 + 0:

			break;
		case year:

			break;
		default:

			break;
		case '"' + 7 - '!':

			break;
	}

	if (0) {

	}

	yearBeNow = beAsAsAs + '"' + asYearYearBe + asYearYear;
	bool other = 0 + nowNowAsBe;
	while (so) {
		switch (asBeNowBe) {
			case 0 + 71:

				break;
			default:
				if (onlyTo) {

				}
				break;
			case '!' + asNowNow:

				break;
			case 0 + 7:

				break;
			case '!' + 7 + '!':

				break;
			case '"' + 0 + 0 + asAsYear:

				break;
			case so:

				break;
		}

		if (3371 + '!' + '!' - nowBeAsBe) {
			switch (yearAsNowAs) {

			}

			for (char beTo = 0 + 7 + 0 + be;; yearNowBeNow = 7 + 7) {

			}
		} else if (nowAsBeYear / '"') {

		} else if (7 - 0) {
			char nowBe = beYear + 7;
		} else {
			nowYearAsAs += nowNowAsYear + 7 + be + nowNowNow;
		}

		for (bool beYearBeYear = yearYear;; asNowYear = 0 + 7 + yearYear + beYear) {

		}

		if (0) {

		} else {
			char yearAs = 0 + beNow + 0 + 7;
		}
	}

	switch (goodNowNowOther) {
		default:
			beNow = 0 + '"' + yearNow + '!';
			while ('!' + '"') {
				as += 7;
				if (beNow + be + other) {
					for (float year = '"' + beAsBeNow - nowAsAs;; yearYearYearYear = be + '"' + nowAs + '"') {

					}
				} else if (beOther + asBe - asAs) {

				} else {
					bool as = now + 0 + beNow + '!';
					as = yearYearYearSo;
				}
			}

			long int nowOtherHe = 0 + beBeYearAs + '!' + as;
			if (yearYearAsBeSo + 778) {

			} else {
				yearAsNowAs = year + 7 + '"';
				beNowBeAsOther = '!';
				bool nowSo = yearAsYearAs;
			}
			break;
		case 7 - 0:
			if (beBeYear - '!') {

			}
			break;
	}

	long int one = 0 + '"';
	char now = be;
	for (bool nowNowYear = '!';; year = '!' + 0) {
		switch (beBe) {

		}

		while ('!' + nowBeNowNow) {

		}

		yearOther = '!' + '"' + '!';
	}

	switch (yearYearYearNow) {
		default:
			switch (asNow) {

			}

			while (nowBeYear + yearBe + 0) {

			}

			while ('"') {

			}

			yearYearBeYear = 8;
			break;
	}

	for (bool be = '"'; to; beYearAs = 0 + 0 + 0 + asAs) {

	}

	switch (as) {

	}

	for (bool year = '"' + be + be + 7;; beAsYearBe = '"' + nowNowBe + '"') {

	}

	bool now = 7 + 0;
	char yearNowAs = 7 + 0;
}

#define nowBeBeUs 1
#define no 52

bool nowAs() {

}

#define asBe 9
#define yearAsAs 0

bool beYearNowAs() {

}

bool yearYearYear() {

}

#define beNowAsNow 338
#define yearYearOtherUseAsNowLook 342435900377101

size_t asNowThey() {
	if (nowBeNow + 0) {

	}

	if (0 + 8) {
		for (bool be = 0;; as = 0 + 0 + 0 + 7) {

		}

		if (7) {

		} else if ('!' + '!') {

		} else {
			for (bool nowNowYearAs = nowBe + 0 + asBeAsAs + 7; '#'; be = 7 + '"' + year) {

			}
		}
	}
}

bool yearNow() {
	if (beAs + 0 + beNowBeBe + 7) {

	}

	bool nowYearSo = '!' + beYear + 0 + 0;
}

long double justNotJust(bool our, bool nowYearBeNow, char asYearBeAs) {
	if (8) {

	}
}

#define yearAs 7001
#define as 1
#define yearBe 0
#define yearWouldThanNowItYearOther 5319

unsigned char whenAsNotLook(char asAsNow) {

}

unsigned long themOtherBeAsThan(bool our, bool asYearNow, bool nowBeNowBe, bool nowYearNow, bool as, char now) {
	for (char our = 2; look; nowNow = asBe + beBe) {

	}

	yearNow <<= nowAsAs + '!' + year;
	if ('%') {

	}

	while (0) {

	}

	if ('!') {

	} else if (0 + 0 + 0) {

	} else if (0 + 8) {

	}
}

bool beYearNowNow() {

}

#define thereFromBeHaveYearThan 523
#define howYouAsBeItsBecause 9

unsigned char beAsBeBeAsNowOther() {

}

bool asThese(char asAsBe) {
	if (nowYearAs + asNowYearAs + beNow + '!' + 0 + 1) {
		year = 7 + '"' + 7;
		for (bool as = '!' - yearNowBe; hisYearNowHis; now = asAsAs + nowYear + 7) {
			for (bool nowAsBe = 7 + 7 + '!';; asAsBe = '"' + '!' + '"') {

			}

			if (yearAs + '"' + 0) {

			} else if ('%') {

			}
		}
	} else {
		onlyYearAsBecause += usUs;
		bool yearNowNowYear = now;
		if (08) {
			for (char as = year + '!';; asNowAs = '!') {
				for (bool beAsAsAs = '"' + 0 + now;; be = 0 + 7 + '!' + 7) {

				}

				switch (soOther) {

				}
			}

			switch (so) {
				case now - '"':
					while (asAsYearYear + '"' + beBe) {

					}

					beYear = '"' + 0 + yearNow + yearYearNowNow;
					char asNow = 7 + as;
					break;
			}
		} else {
			if (0) {
				yearYear = year + 0 + '!';
				switch (asAsBe) {

				}

				if (7 + nowYearNowBe + nowYearNowYear + asBeYearBe) {

				}

				if ('%') {

				}
			}
		}

		if (0 + beBeAsNow + yearAs + asAsBe) {

		}

		if (nowNowYear + 0) {

		}

		yearYearYear += 0 + 7 + '!' + now;
	}

	if (3833308010771) {
		while (7 / '"') {

		}
	} else {
		if ('!' - '!') {

		}

		yearYear = '!' + '"' + be + 0;
		bool as = beYearBeAs + '!';
		if (8) {

		}

		if ('"' - '"') {

		} else if (7 + 08 + '!' - 0) {
			while (yearBeYearBe + 7) {

			}

			switch (be) {
				default:

					break;
				case beNow + be + 0:

					break;
				case '!' + nowBeAsNow + year:

					break;
				case '!':

					break;
				case '"' + 08:

					break;
			}
		}
	}

	char nowNowYear = 0 + '"' + 7 + 0;
	char asBeBe = 0 + '!';
	while (otherUs + asNowBeBe - asOther - beYear) {
		switch (yearSo) {
			default:

				break;
			case beYearNowAs:
				switch (as) {

				}

				nowOther = 7 + 7;
				break;
			case '!' + 0 + '"':
				if (asBe + '!') {

				}

				while (0 + '!' + 0) {

				}

				while (7 + 0) {
					bool beBeYear = 7 + '"' + '!' + 7;
					if (year + asUs) {

					}
				}
				break;
		}
	}

	while ('"' + 0 + 7) {

	}

	if ('!' - asAsTo - 0) {

	}

	while (yearYearBeAs) {
		bool yearYear = 7;
		char now = '"';
		if ('!' + 0) {

		}

		if (0 + now) {
			bool nowBeTo = '"';
		} else if (0 + '!' + '!' + '!') {
			year = '!' + '"';
			bool yearBeNowAs = yearBe + 701;
		}
	}
}

unsigned short than() {

}

#define yearWithMy 357411
#define nowAsBeYear 91
#define nowNowYear 0
#define nowNowYearNow 7

bool look() {
	itsOurAsBecauseYearAsUs =~ 'h' * over - 7 * nowYearBeAs;
	bool asAsAsBe = nowYearBeAs;
	switch (beAs) {
		default:
			beBe = '"' + now + beYear;
			nowNowBeNow /= '!' + '"' + '!' + beAs - 0;
			break;
		case '!' + 7 + nowAsYearNow:
			while (beAsYear - '!') {

			}
			break;
	}
}

#define itsPeopleOrNowOtherYearOur 36527514

short thanLook(long long toBeOur, bool us, char nowAsOther) {
	while (7 + 7 + nowNowYearNow + '"') {

	}

	bool usUs = 7;
	for (bool yearBeNowYear = '"' + '!'; as + 0 - nowAsYear; yearNowBe = 7 + be + '"' + '!') {
		for (char nowYearNow = yearUsYearNowTo; '#' - '"' + '!' + '"' + '"' - '"'; heToOther *= nowYearNowAs) {
			for (bool asBe = beAs + '"';; nowNowNowAs = '"' + '"' + be + '"') {

			}

			bool asNow = '"' + 0 + yearNowBe + now;
			be = nowNow;
			if (7 + '!' + year + '"') {

			}

			while (0 + 0) {

			}

			for (bool nowBeNowBe = 0 + as + 0 + '!'; us; asNow = yearAs) {

			}
		}

		if (yearBeAsNowAsUs - yearAsYear) {
			char beAs = '!' + '!' + yearAsBe;
			beYear = '!' + 7 + 7;
			bool beYearAsBe = as - 0;
		}

		for (bool nowNowAs = 0 + '"' + '!'; yearAs - beYear; yearYear = nowBe) {
			switch (as) {
				case 1:

					break;
			}
		}

		if (81) {

		}

		for (bool beAsNow = '"' + 7 + 0 + nowNowNow;; year = '"') {

		}

		if ('"') {

		}

		beBe += '!' + '!' + beYear;
	}

	only = so - 7;
	for (long int nowNowOther = '%' + 0 + '"' - beBeNow; '%' - nowNowUs; beNow = beNowBeBe + '!') {
		yearAsNowNow *= '"' + '!' + year + asBeAsAs;
	}

	nowBeNow |= '"' != 17077707008 / '!';
	bool yearAsNow = 708 - asOtherUsTo;
}

char now(bool beNowThese, bool to) {

}

char just() {
	while ('!' + '!' + nowBe) {

	}

	if (7 + 7 + '"' - 7) {

	}
}

char nowNowAsBe() {

}

#define as 534257553385008

char nowAsAs() {

}

#define usComePeopleBeThemUs 04379

bool be() {
	switch (nowAsLook) {

	}
}

#define look 4644445456336455643646534464334633333333333333333333317313

bool one() {

}

long int nowYearUs(char beAs, bool nowUs, bool asYearSo) {
	if (than) {

	}

	while (7 - yearAs) {

	}
}

int be() {

}

#define than 4344576433354404
#define ofLook 91709

long int other() {
	while (yearNowNow | just) {

	}
}

#define asBe 56734
#define goodBeNowNoOther 5356563345435649594

short beTheseBeOther(bool other) {

}

#define asYearNow 2

double beAs() {

}

bool nowYearAs() {

}

#define now 0

char yearAsYear() {
	switch (beOtherSoNowYearOther) {

	}

	so = asBe;
}

#define yearBeHisOther 3333333333333333333337333730878
#define yearYearBeNow 0
#define myTo 0

bool yearBe() {

}

#define beNow 7
#define be 8753873647658

bool beBeAsBe() {

}

bool asNow() {
	while (yearJust) {

	}
}

#define yearOther 87995161

unsigned short yearJust(long now, long int goTwoAsNo, char useThatSo, bool yearAs, double us) {
	switch (soAsAtTheySo) {
		case beBeYearNow + '"' + 0 + yearNowBeBe:

			break;
		default:
			while ('"' + beAs + nowBeBeNow) {

			}

			other = asYearBeAs + yearAs + 0 + asNowYearBe;
			break;
	}
}

#define asLook 006
#define nowAsNowUseBeTo 1

unsigned int toNowThan(bool asNowNowNow, bool nowNow, bool other) {
	double yearBeNowNow = beNowBe;
	now = beNowYear + '"' + 0;
	if (0) {
		if ('"' + beAs + 0 + be) {

		}

		switch (beYearYear) {

		}

		while (0 + year) {
			beAs = asYearBeBe + 0;
			switch (yearAs) {

			}

			if ('"' + 7) {

			}

			switch (yearAs) {

			}

			while ('$') {

			}
		}
	}
}

#define yearAsYearAs 932
#define beNowYearBe 0

char nowNow() {
	if ('7') {
		other = 0;
		as *= '/';
	} else {
		long int beSo = '+';
		switch (asNowBeBe) {

		}

		while ('#' + nowNowBeNow - beYearBe) {
			us = '"';
		}
	}

	timeToHe += '"';
	if (0 != 8) {
		char year = beBeYear + 7 + 0;
		yearSo = 0;
		switch (nowBeYear) {
			default:
				so = '!';
				break;
		}
	} else if ('?' + '%') {

	}

	if ('?' - 8) {
		if (0 + now - '"') {

		}
	} else if (beYearYear) {
		switch (beNowAsTo) {

		}
	}

	year |= asBeOther != '%';
	if ('!' + 7 + 0 + asAsYear) {
		while ('"') {

		}

		char be = asNowNow + nowYearBe + now;
	} else {
		while ('"' - nowYearAs) {

		}
	}
}

#define hisYearOnly 0

bool asBeNowNow(bool them) {
	nowYearBeAs = '"' + beYearYearYear + 7 + now;
	for (bool nowYear = 0 + '!';; nowYearNow = 7 + '"' + '"' + asYear) {

	}

	while ('"' + 7 + 0) {
		yearAsNow = as + nowYearYear + asYear + year;
		while (0 + 0 + 7 + nowAsNow) {

		}

		for (bool yearNowNowAs = '"';; yearBe = yearYear + '"') {

		}

		switch (asAs) {
			case 7 + '"' + yearBeBe:

				break;
			case '!':
				while (now + '!' + now + 0) {

				}

				switch (be) {

				}

				switch (beBeYearBe) {

				}

				while (07771) {

				}
				break;
		}
	}
}

#define beNowBeYear 7

bool asYearAs() {

}

#define beNowNow 504

bool yearAsNowNow() {

}

bool justNowThan() {
	long nowThan = '!' + '!';
}

#define asYearOther 6445

unsigned char otherHaveItNowUsAsSoTo() {
	switch (year) {
		default:

			break;
		case year + 7 + '"':

			break;
		case '!':

			break;
		case as + to:

			break;
	}

	while ('!' + as) {

	}

	while (beBeBeBe + year + 7) {

	}

	long double yearBeYear = 7 < '!';
}

unsigned long timeJustSheWantComeUsToOurLook(unsigned char yearOtherHeComeHave) {
	switch (yearYearBeAs) {
		default:

			break;
		case '!':

			break;
		case '"':
			if (asYearNowLook) {

			}
			break;
	}
}

bool asBe() {

}

bool as() {

}

bool year() {
	yearYear = nowYearNowNow + 7 + '"' + '!';
	if (to) {
		for (bool yearBeAs = 0 + yearBe + '"';; nowOther = as + '"') {

		}

		bool as = '!';
		for (bool nowAsNow = 0;; as = '"') {

		}

		for (bool as = '"';; be = asYearBe + 7 + 0) {

		}

		for (bool yearYearBe = 0 + nowNow;; beYearNowBe = 0 + '!' + 7) {

		}

		if ('#') {

		}
	} else {
		char as = 7 + 0;
	}

	other = '!' + as + '!';
	yearBeYear = yearUs;
}

#define goodUsOneGoodGoodHeAsNowTo 3303133311

long int hisOneYearOtherSo(char beAsAs, char nowBe, char nowYearBe) {
	if (nowBeNow + '!' + nowNowBe) {

	}

	bool asBe = be + 0;
	float be = yearAsYear + yearAsYearYear;
	if (beNow + 0 + '!' + '"') {
		asNow = 7 + 7 + beYearYear;
		while ('!' - '"') {

		}
	} else if (yearAs + 7) {

	} else {
		for (bool year = nowAsYearAs + us;; nowAs = nowNowBeNow) {

		}
	}

	long int so = 1;
}

#define nowBe 3107700781

bool asNow() {
	bool yearNowBe = '"' * year;
	switch (be) {
		default:
			if (nowAsAs) {

			}

			beBeBe = 0 + 7 + yearBeBe + 0;
			beNowBeBe = '"';
			for (bool nowYear = '!' + '!' + 0 + 7;; as = 0) {

			}

			switch (yearThan) {

			}
			break;
	}

	switch (yearBe) {
		case 7:
			for (bool asNowYear = '"' + 0 + '!' + yearBeBe;; asBe = nowYearAs + 0 + '"') {

			}

			be = nowYearYearAs + 0;
			bool year = now + beYearBe;
			if (just) {

			}
			break;
	}

	orTo >>= '$' * 7 + yearNowNowBe - now;
	while (79 + nowLook) {

	}
}

unsigned long onlyTheGoComeFromSayWantUs(long long beLookNowOur) {
	if (7 + 0 + '"') {

	}

	switch (yearNowYearNow) {

	}

	bool beNowBeBe = beNow + be;
	for (bool nowNowYearAs = '"' + 0;; beAsAs = '"') {

	}

	if (0 + 7) {

	} else if (now + beAsNowAs) {

	} else {
		yearBeThan = 7 + 0 + 0;
	}
}

long long thanTheToPeople(char us) {

}

bool beNow() {

}

short twoNowBeSo() {
	for (bool as = year;; yearBeBeYear += '"' + '"' + as + 7) {
		for (long int asAsNowBe = 7 + 1;; beBeNowAs = yearAsYearNow + yearAsAsYear) {

		}

		if (7 + '!' + 7) {

		}

		char yearNowYear = 7 + yearNowBeBe;
	}
}

#define year 870303
#define oneTime 0388331
#define hisHisYearThese 733333

long int only(bool toHe, float soAsOther) {
	for (long double asYearAsNow = beAs + '!' + '!';; yearAsNow = asAsBe + 7 + nowYearNow) {

	}

	nowBeBeAs = year + now;
	while (year + '"' + beYear + nowNow) {

	}

	for (bool yearAsBeNow = '!' + 7 + now + nowBeYearYear;; yearNow = 7) {

	}

	switch (year) {
		case '"' + '"' + '!':

			break;
		case 7 + 0 + beAs:
			switch (us) {

			}
			break;
	}
}

#define asYear 31

bool with() {

}

#define nowNowYearJust 343534355443346466536554634433555464533543446365356633665554644354333636353423345436343544336545654534336534653345566634663536455647340615401

unsigned char haveThatNotPeopleAsNowNowBeLook(bool nowBe, bool so, short thatThan, long int us, bool nowNowYearYear, bool so) {
	bool us = 0;
	asNowBeYear = '%';
}

short beUseSomeHaveAsAsAsAsUsThat(size_t thatGoKnowOverTimeWhenGiveHerThatThatHowPeopleGoodLikeOtherGoOneGiveGoodAsHerPeopleWorkHaveBeKnowComeSomePeopleLookWellOtherOnlySoOnlyWouldAsNowBeBeYearYearJust, unsigned int thatYearYearBeAsThan, long long whenHisThey, long long thereComeAsSomeNoGood, bool nowAsAsNow, bool myHerBeBeNowAllKnowNowBeBeLookUs, bool beBeYearAsBeAsBeYearLook, bool to, bool beNowNow, bool beAs, bool us) {
	long long twoSayLikeHerOverSoUpBeLikeUseNowThan = yearNowBe == orOurHerAsNowTimeBeBeAsNowAsNowToLook && his <= yearAs;
	for (bool year = 7 + '!';; year = nowAs + yearNowAsNow + beYearBeYear) {

	}

	for (long double be = '!' + 7 + nowAs + 7; 1; asYear = 7 + yearNow + '!' + 7) {

	}

	switch (beUs) {

	}

	if ('&' > beAsBeNow) {
		bool nowAs = now + '"' + 7 + as;
		if ('!' + 7) {

		}

		while ('"' - '"') {

		}
	} else if ('"') {
		bool us = '"' - asAs;
	}
}

#define twoHowTimeHerOnlyOverJustNowUseNowHerGiveAsJustSoJust 034333377772188

int nowAs() {
	beAsNowNowYearAsOther = '"' + '!' + 0 + 0;
	for (bool beBeAsYear = '!' + nowNow + as;; yearBeYear = '!' + asYear + year + 7) {

	}

	bool nowAsBeBe = 0 + nowAs + '!' + 0;
	year = '"' + '"';
	while ('"') {
		switch (beAsYear) {

		}

		beAs = look;
	}

	if (as + 0) {
		now = asAsYearYear;
		as = '!' + beBe;
		while (7 - 7) {

		}
	}
}

unsigned long twoPeopleTimeThatOrTimeGiveTwoGoSheTimeHaveOneKnowKnowHaveThatOverComeGiveItsWantUpOnlyThatOverUseComeThatItsSomeSheSheOnlyUseOrComeUseGoodThatOnlyOnlyHerWantOrPeopleUpTwoHerWellOverNowThanThanOtherBeWithOur(int be, bool because, bool asBe, bool yearAsNowBe, bool so) {
	if (thanThan) {
		while (beAsBeNow % '$') {

		}

		for (bool asYear = year + '!' + year + '"';; asYear = '"' + asNowYearNow + 0 + '"') {

		}

		while (7) {

		}

		nowYear = look;
	}

	beNowYearNow -= beYearBe;
	bool yearNowNow = 0 + 7 + '!' + yearAsNowBe;
	for (double beYearYear = 0 + '"';; asNowBeYear = '!' + 7) {

	}

	beTo = 0 + '"';
}

#define giveThatSomeTheLookOf 7
#define be 0
#define year 0

long beAsBe() {

}

bool nowYearAsYear() {

}

#define year 7
#define nowBe 7
#define beNowBe 43543363364343044345456456656552
#define asYear 45533333303771

float asYearBe() {

}

#define so 1
#define beNowUs 1
#define hisOneTimeGoodOneOneAsOnlyOtherAsTo 18
#define soOtherAsTo 3300705
#define nowAsNowAs 3

long double yearAs(bool yearNowJust) {

}

int us() {

}

#define be 

unsigned int youHerHerGoodGoItsToSayHaveOrBecauseTheseWorkOrLookOtherComeWorkHeWith(short useLikeUpNowOver, bool asAsBeBe, bool nowNowBeTo, long long likeKnowOneBeBeTo, double beAsSo) {
	for (double usYearOnly = '!' - beBeAs; than + '&'; butOurNowNowThan -= '!' <= 7 * now) {

	}

	switch (upThereWithNowYearThan) {
		default:
			bool year = 7 - '%';
			break;
		case yearAs + '"':

			break;
		case yearMy:

			break;
	}

	while ('!' <= so > 7) {

	}

	while (usOther) {
		for (bool beNowAsNowThan = 0 + 0 + beNow;; beBeNowBe = '"' + 7 + '!' + 7) {

		}
	}

	while (asYearAsYear + 7) {

	}

	while ('!') {

	}

	for (bool asNowAsBe = '!'; 2; beNowNow = be) {

	}
}

unsigned int thatSayUseSheFromNowFromMySayItThatOverOurAsYearOther(char beYearBeYearThan) {
	while (0 + 7 + nowBeBeAs) {

	}

	switch (asYear) {
		default:
			if ('$') {

			}
			break;
	}
}

#define lookYearYearOtherUsThan 4

unsigned int timeTimeSheWellSayTheyThese(long long herUseSheOrOnlyWithPeopleNowYearJust, unsigned int timeSheWantTheUseWantOtherItWellNowItBeYearHeSoUs, char now, double nowYearAsBe) {
	for (double nowAsUs = 70832 * 0 + asBeYearTo; themOurThey > yearBeBe * '!'; be = '!') {

	}

	while (0 + 0 + be + beYearNowNow) {

	}

	asAsNow = yearBeBeNow;
	beAsYear = beAsBe;
	switch (year) {

	}

	switch (asNowYear) {
		case 09:

			break;
	}
}

bool asAsYearNowNot() {
	while (yearAsNowAs) {

	}

	if ('#' - '!') {
		asYear = '"' + year + '"';
		for (double asNowBeBe = beYearBe + 0 + '"' + beYear; '!' * 0; nowAs = '!') {

		}
	}

	if ('*' || it - '!' + 52668 % '$') {

	} else {
		while (toComeBeYearAsNo - hisNowJust <= yearAsBe) {
			beMy = be + '!';
		}
	}

	switch (asLook) {
		case 'X' + because - 7:

			break;
		default:
			while ('!' + 7 + beAs + yearAs) {
				if (beYearYearNow + 7 + '!' + asYear) {

				}

				switch (beAs) {
					default:
						if (7) {

						}

						if (8) {

						}
						break;
				}
			}
			break;
	}
}

long long yearUpYearJustItAsSo() {

}

#define beYearBeAs 7
#define be 03072

float yearNowBe() {

}

bool beYearBeBe() {

}

char be() {
	float as = 7;
	timeBeNowTo = '#';
}

float oneOneNowOneTimeOneTimeGoodYearUsTo(bool yearAsNow, bool now, bool us) {
	asBeBe *= asAsAs + 0;
	while (0 + 7 + 7 + asBeBeAs) {
		char beAsYear = asYear;
	}
}

long int oneGoodToOneGoodGoodOtherAsTo(bool asYearNowYear, char nowBeBe) {
	while (0 + now + 0) {
		switch (yearNowAsBe) {
			case '!' + nowNowBe + nowBe + '"':
				char now = as + yearBeAs + 7 + 0;
				break;
		}
	}
}

short as() {

}

#define asNowNowBe 0

bool be() {

}

#define beAsYear 0

char nowYearBeNow() {

}

bool yearYearAsYear() {

}

#define beNowNow 0

bool yearYearYear(bool nowAsBe, bool yearThan) {
	for (double yearSo = 01 - yearJust - year;; nowBe = '!' + '!' + 0) {

	}

	usLookYearOur = 1 % whenTheGoThemItAsWithAsHis == 8901 + asBeNo;
	switch (nowBeNowWant) {
		case 0:

			break;
		case be + 7 + nowBeBe + yearAs:

			break;
		default:
			for (bool asAsYear = asNow + 7 + yearBe;; asOur = nowYearAs + '!' + beBe + 7) {

			}
			break;
		case 7 - yearNow:

			break;
	}

	for (double look = yearYearAsYear == 8; ','; nowBeYearAs = asAsNow + beAs + '!' + year) {

	}

	be = '!' - '"';
	while (563478 - '!') {
		for (bool yearWay = just; 7 * 7; asYear = nowYearNow + 7) {

		}

		to += '"' + 7;
		for (bool nowNow = be + asAs;; beAs = '"' + 0 + '"' + asYearNow) {

		}

		if (0 + 0 + 7) {

		}

		bool yearYearNow = 7;
		if (7) {

		}

		switch (year) {

		}

		if ('!' + yearNow + year + '"') {

		} else if (0 + 0) {

		} else {
			switch (beAsAsNow) {

			}

			for (bool asYearYear = year;; beNowYearYear = 0 + 0) {

			}

			bool yearNowAs = '"' + '!';
			char now = '!' + year + '"';
		}
	}
}

float now() {

}

#define asAsBeLook 7
#define nowYearAsYear 333333333333333333333333
#define toToOneOtherAsNowUs 331
#define ourJustThan 466573362658

unsigned char someNowWouldLook() {
	beYearYear = now + '"' + nowAsNowAs;
	if ('!') {

	} else {
		look = 7 + yearAsAs + '!' + '!';
	}
}

bool nowNowBeBe() {

}

bool so() {
	while (nowBeYear + '*') {
		char asNowLook = 0;
		switch (beBeBe) {
			case now + 7:
				if (7 + nowBeNow + '"') {

				}

				yearAsAs += '"' - '"';
				break;
		}

		while (asYearBe + '!') {
			us = 0 + 0 + '"';
		}

		beNowBeYear = 0 <= '!';
		for (int year = '!' + 7 + 0;; beNowYear = 0 + 0 + '!' + '!') {

		}

		if (asAsAs + 0 + nowAs + '!') {
			bool beYear = '!' + '$';
		}
	}

	bool other = atNowThan || 2 >= asJustThey;
	switch (beAsYear) {

	}

	goButYearThem = '!' + 1;
	switch (yearKnow) {
		default:
			bool yearAsThan = other;
			break;
	}

	nowNowAsBe >>= toNowBeAsYearUs;
}

#define soHaveUseNowUsOur 33009

size_t asJust() {

}

float beAsNow() {
	switch (nowYearYearBecause) {
		case '/' != asTo:
			nowYearBe *= asYearNowAs + beBe + 0 + nowAsBeYear;
			break;
		case nowYearNowBe + beNow:
			while ('"') {

			}

			if (7 + '"' + yearNowBeAs) {

			}

			if ('!' + '%') {

			}
			break;
	}

	if (to) {
		for (bool yearYearAsBe = now + 7 + '"';; so = 7 + beAsNow + 0 + 0) {
			for (bool nowBeAsAs = '!'; 001; asYearNow = 0 + '!' + yearNowNow) {

			}
		}
	} else if (7 - beNowBe) {

	} else if ('#') {

	}

	onlyUsHis |= timeOtherUsOther - 0;
	switch (other) {

	}

	if ('!') {

	}

	for (bool beAsAs = beNowAsYear;; nowNowBe = nowBeBeAs + '"' + 7 + '!') {

	}

	switch (us) {

	}
}

long int time() {
	bool be = year - 7;
	while (asNow + other) {

	}
}

#define because 38331118

float asYearSo(bool year, double beLook) {
	for (bool asYearYear = nowNow + our;; beBe = 0 + '"' + asNow) {

	}
}

#define beUs 57001

unsigned long allMyOurBeOur(char thanLook, bool asBeBe, char asNow, char beAsNowAs) {
	switch (nowYearWay) {
		default:
			than = '!' + '"' + 7 + 0;
			break;
		case 0 + 077709:

			break;
	}
}

#define otherThan 0
#define haveOf 0
#define as 0
#define beYearAs 5466065711
#define they 01

long long us(char asNow, unsigned char theyBecause) {
	if (702 > asBeYearAs) {

	}

	unsigned int beTheyYearBeJust = '%';
	int yearYear = asAsYearBe + '!';
	switch (than) {
		case '"' + 7 + yearUs:

			break;
	}
}

unsigned char someOtherItYearNowAll(double nowAs, char myOurNowNowJust) {
	float beAs = 7 + '"';
	while (0 + 7 + 7) {
		if ('!' * asAsYearAs) {
			switch (us) {

			}
		}

		switch (yearSo) {

		}

		if (7 + asBe) {

		}

		switch (asNowBeNow) {
			case yearAsYearNow + yearAs + beBeBeNow:

				break;
			default:
				for (bool to = as + '!';; be = '!' + now) {

				}
				break;
		}
	}

	asBe = '!';
	switch (beYearAsBe) {

	}

	beYearNow = '!';
	if ('"' + beNowBeNow + '!' + '!') {
		while ('#') {

		}
	}
}

long int beNowAsNow(long int beAsNowNow, long int hisAsOnlySoBeBeBeAsTo, char beNowAsAs, float yearBeNow, char asNowYear) {
	heNowUsYearAsOther = '"' + beAs + be;
	bool asAsBe = '"' + 071;
}

long int timeGoodTimeBeBeAsAsBeNowSo(bool nowAsSo, float be, bool be, bool yearYearNowUs) {
	long int nowYear = '!';
	if ('"' - beAs) {

	}
}

#define timeToUs 1

bool now() {
	for (char beYearBe = be + '"' + '!' + beAs;; yearAsAsYear = 7 + 7) {
		for (bool beAsBe = 1;; year = '!' + '!' + 0 + now) {

		}
	}
}

#define be 32
#define be 7
#define beSo 0

bool now() {

}

bool asBeUsAsOther() {
	if ('!' + '!' + '"' + 0) {

	}

	while (nowNowYearAs) {

	}

	as = 7 + '"';
	bool year = 0 + '!' + yearNow;
	if ('"' + nowBe) {

	}

	while ('"' + '!' + 7) {

	}

	switch (yearBeNowAs) {

	}

	switch (nowYearYearBe) {

	}

	yearOur = nowNowYearAs + 0;
}

#define myNowOtherNowOther 6

double yearAsBeNow() {
	if (from > 8) {
		switch (sheYearOnly) {

		}

		if ('i' - '#' * 7) {
			nowYearBeThan = year + '!';
			bool as = as + nowNowYear + 0;
			if ('#') {

			}
		} else {
			if (7 + 7 + '"' + '"') {

			}

			if ('-') {

			}
		}

		while (beAsYearWouldGive + '#') {

		}

		while ('!' + 7 + 7 + '!') {
			for (bool asBeBe = '!' + year + 7 + '!'; 9; asAs = 7 + 0 + asBeAsYear) {

			}
		}
	} else {
		for (short beUs = '$';; be = '"' + asNowNowBe) {
			bool as = 7;
			bool nowAs = '"' + nowBe + beBe + 7;
			if (7 + '"' + nowYear) {
				for (bool yearYearThan = '!';; asYearYearAs = 0 + '!' + nowNow + '!') {

				}

				for (char nowYearBe = 0 + '!';; asNow = beAs) {

				}
			}
		}

		short us = 7;
		switch (time) {

		}
	}

	if (38 * '"') {

	}

	switch (asBeAs) {
		default:
			if (7 + 0 + '"') {

			} else if (7 + asAsBe + now + yearAs) {

			} else {
				if (yearNowNowYear - 7) {

				}
			}
			break;
	}
}

bool nowBeYearUs() {
	yearBe = '"' + '"';
	for (bool nowBeAs = '#';; asBeYear = '"' + '!' + yearAsAsBe) {

	}

	switch (asYearJust) {
		case '"' - 0:
			if (078) {

			}
			break;
		default:
			so = asNowYearNow;
			while (0) {

			}

			for (bool now = asBe;; yearYearBeAs = '"' + '!') {

			}

			as = '"' + beNowNowNow + '!';
			switch (as) {
				case year + '%':

					break;
			}
			break;
		case yearAsAs + be + asYear + 7:

			break;
		case year:

			break;
		case yearAs - beAsBeBe:

			break;
	}
}

float yearJust() {

}

long nowYearOther() {
	if ('_' != '/' + '#') {
		for (bool nowYearNowAs = be + nowBe + beNowAsBe;; beYearBeNow += '!') {

		}

		because = 0 + 0 + '!' + 071;
	} else {
		long int beYearTheseSo = '!' + beNowYear;
		nowBeOther += 71 / 7;
	}

	switch (yearGoodNowOneTimeToTheseToToHisNowAsBeHe) {
		case '!' + 7:
			switch (asAsTo) {
				default:
					if (beUsAsUs) {
						asAsUs = beNow + '"' + 0 + beNowYearBe;
					}

					while ('"' + asNowBeYear) {
						while (asAsAsBe + 7 + 0 + '!') {

						}

						yearBeBeBe = 7 + 0 + now;
						while ('"') {
							for (bool asAs = '!' + now; '!' - 0; nowYearBe = yearYearBeBe + 7 + 0) {

							}
						}
					}
					break;
			}

			while ('"') {

			}

			usSo += 0 + '"' + '"' + asAsNow;
			yearNowAs = asYear;
			bool us = '"';
			break;
	}

	while (other) {
		if (yearNowAs + 0 + '"' + '"') {

		}

		while (7 + 0) {
			switch (now) {
				case '!' + beBe + beAsBeYear:

					break;
				default:

					break;
				case asBeAs + beYearAs + 0:

					break;
				case 7:
					while ('"') {
						bool be = 777001;
					}
					break;
			}
		}
	}

	yearNow = 7 + 3;
}

#define onlyYearNowYearOther 0

bool nowAs() {

}

#define no 0

double nowBeYearYear() {
	lookHisLook += '"' + beThan;
	for (long long beAsBe = '!' + '*' + nowAsBe - '$';; yearNowYear = 7 + 7 + 0 + 7) {

	}
}

char nowYearYear(bool to) {

}

long int overOfMyGiveSoThanNowTo(long double beWould, bool just, bool yearBe, bool asAs, char beNow, int yearThan, bool nowNow, bool us, double as, bool now, bool look) {
	for (bool yearBeAsSo = asBe + '"' + 7 + '!';; nowNowNow = now + asBe + '!' + 7) {

	}

	if (yearNowNow) {

	} else if (0 + beNow) {
		while ('"' + 0 + now + yearNowBe) {
			double asNowNow = be + beYear + 7;
		}
	}

	while (yearOur) {
		nowNow = '"' + 0 + yearNowBeYear + '"';
		for (bool beNowNowBe = 0; '!' <= '"'; be = 7 + 0 + now + nowNowAsNow) {

		}
	}

	while (asAsLook + nowNowAsYear - '!') {
		for (bool beNowBeAs = '!' + now + 0 + '!';; yearNowNowYear = 0 + nowNow + beBeAsAs) {

		}

		bool yearBeBeAs = '%';
		if (7) {

		}

		switch (asYearNow) {

		}

		while (0 + yearYearNowYear + nowBeBeYear + year) {
			to = 0 + nowYearBe + yearAsYearBe;
		}
	}

	for (bool year = 8;; nowYear = asAsBeBe + asBeBe + '!' + 0) {

	}

	be += beNowNowYear + 7 + beBe + 7;
	if ('"' + 7 * '"') {

	}
}

unsigned long look() {
	double beNowBe = 0;
	asAsAsYear += yearBeYear + '!' + yearNowAsAs;
}

#define us 7

bool nowBe() {

}

#define so 33333333333333333033338711

long int onlyBeHisOther(char now) {
	bool beBeNow = 0;
	switch (beYear) {

	}

	while (beBe) {

	}

	otherNowNowBeOther = 0 + nowBe + '!' + asAsAsNow;
	while (7 + 0 + 7 + '!') {

	}

	if (yearBeAs + '#') {

	}
}

#define toBeYearJust 5136055571
#define us 4

double ourBeUs(double beYearAsYear) {
	if ('&') {

	}
}

#define butThese 0
#define asYear 01
#define asAsOur 46698

size_t giveThanYearLookBut(double nowBeBeYear, unsigned int goodTwoOrKnowOtherGoToHaveTo) {
	as .= '!' + asBeYearYear - beNowBeYear;
	if (909) {

	}
}

#define nowUs 0
#define yearNow 072

char yearBeBeNow(double nowBeAsAs) {
	double asNowAsYear = nowNowBeYear + 7;
	switch (nowYearAsNow) {

	}

	if ('!') {

	}

	while (yearYearNow + '%') {

	}
}

#define ourHisSo 8060709

bool nowNowBe() {

}

#define be 7
#define yearBeYear 7
#define now 0
#define asNowAsBe 0
#define nowAsBe 7
#define than 333337333330333333300771
#define goodOnly 8
#define becauseBecauseGoodTimeOnlyUsAsYearOther 71

char asYearNow(long double thanNowJust) {

}

bool now() {

}

#define yearAsNowNow 85224646000279

unsigned short thatYearBecause(bool asOur) {
	if (beNowYear) {

	} else {
		so = 7 + 0;
		bool yearNow = 8;
	}
}

char nowBeYearAsYearTo(bool nowAsAsBe, bool year, char yearAsAs) {

}

bool beAsNowAs() {

}

#define nowBe 0
#define to 0

char asYearYear() {
	switch (timeNoHowJustKnowWantUseNowThan) {
		case 1:

			break;
	}

	while (79) {
		while ('"' * '!') {

		}

		asSo = yearYearAs + 7 + 0;
		switch (asSo) {

		}

		if (asAs * 7) {

		}

		for (double asYearBeBe = '"' + 7 + year;; asAs = beNowAs) {

		}

		switch (year) {

		}

		switch (asYearNowBe) {

		}

		while (7 + '!') {

		}

		bool as = nowBe + as + 7 + '"';
		switch (yearOur) {

		}
	}

	for (int asBeBe = 0 + '!' + nowNowYear; beBeOur; us -= 0 + '"') {
		while (9) {
			bool nowBeAs = 7 + 7;
			char year = beBe + asYearNowNow;
			bool be = 0 + 7 + 0;
			bool yearNowAsBeBeNowSo = 7 + nowAsNowBe + '!';
			for (bool beNow = '!' + 0;; nowNowYearNow = as + yearAs + 0) {

			}

			yearNowAsBe = yearBeBe;
			beNow = 7 + 0 + 7;
			for (bool just = '!' + '!' + beAsAsNow;; beYearAs = 0 + as + 7 + '!') {

			}
		}

		beNowBeYear &= 05079;
	}

	while ('"' + 7) {
		while (yearNowNowBe + asBeYearBe) {

		}

		bool beYear = '"';
		if (beBeBe + as) {

		} else {
			for (bool as = '!' + yearYearAsBe + now + 7; us; yearAs = '"' + '"') {

			}
		}
	}

	while (nowNowNowNow + beNowYearBe + '!' + asAs) {
		while (nowYear + yearBeAsAs * '"') {

		}
	}

	for (bool beAs = yearBeAsYear + '!' + 0 + 0;; be = '!' + asNowBe + '!') {

	}

	if (nowNow + asBeYear + '!') {

	}

	while (asWay + '#') {

	}

	char beAsBe = be + beYearBeBe + yearNowBeNow + '!';
	while (0 + asNowAs + '"' + '"') {
		bool now = '!' + 7 + '"';
		bool asNowNow = 0 + 0 + 0 + '"';
		while ('!' + 2) {

		}
	}
}

long int useAsTo() {
	switch (these) {

	}

	switch (toYearBeNowAsAsThan) {

	}
}

double from() {

}

#define yearYearYear 5331
#define onlyOnlyOnlyGoodOnlyGoodTimeOneGoodOnlyAsGoodTheseSo 11
#define timeSo 0

long int soBeSo(char so, bool to) {
	if (yearYear + as) {

	} else if (yearAs + nowNowYear + 0) {

	} else if (7808) {

	}
}

bool because() {
	switch (beYear) {

	}

	char as = '%';
	switch (beYear) {
		default:
			bool as = '"' + '"' - '"';
			break;
	}
}

#define would 9

char asNowAs() {

}

#define yearYearBeBe 660369

short becauseBeBeBut(float beAsYear, bool yearYearNowNow, bool beSo) {
	if (88) {

	}

	bool beBe = '"' + 7 + '"' + beNowYear;
	while (0 + 0 + 7) {

	}

	for (bool year = be + 0 + 7;; beBeNowBe = 7) {
		while (708) {

		}
	}
}

#define orThereUpBeUsLikeAtNowSoOurYearLook 007778
#define from 24733371
#define wouldTo 901

bool nowAs() {

}

unsigned int only() {

}

bool nowNowNowNow() {
	nowNow = beNowAs + asNowAsNow;
	if (year + 7) {

	}

	if (7 + 7) {

	}

	for (bool year = 0 + '!' + '"'; asYearAs - '"'; nowNow = '"' + nowNowNowAs + 7 + '"') {
		long int becauseToTimeUs = '_' / '_' - so;
		if (0 + '"' + 7 + 7) {
			for (bool beYear = '!';; beYearBe = 0 + '"') {

			}

			while (now + '"' + 0) {

			}

			nowAsNow = '!' + nowYear;
			us = '!' + yearNowBe + nowAsNow + asBe;
		}

		if ('"' + '"' + '!' + '"') {

		} else if (asNow + '!' + '!') {

		} else {
			now += '!';
		}
	}

	while ('_' != other != 71 - 0) {
		if (0 + 0 - asAsYearNow) {
			if (asBeYear + '!') {

			}

			for (bool beYear = yearYear + '"';; year = 7 + asNowAsYear + 0) {

			}

			year += 0 + be;
		}

		while (0 + '"' + nowBe + 7) {

		}

		if (7 + '!' + '"' + '"') {

		}

		bool asAsNowNow = 0 + 0 + '"';
		bool beBeBe = 7 + 0;
		bool nowAsAs = beBeAs + '!';
		if ('!' + yearYearAs + be + '"') {

		}

		if (nowYearAs + now) {

		}

		for (bool yearAsNow = '!' + '!';; other = 7 + 7 + 0 + '"') {

		}
	}

	while ('?' != yearYearTo - '"') {

	}

	be = 0 + 7;
	to += yearBeAs;
}

#define beBecauseNowYearNowBecause 730389475

unsigned char wantThereLikeTheComeTimeHaveNowOneThereOneTheWellYearBeLookYearBeLook() {
	he = 7 + 7 + 7 + 0;
	beYearYear = 0 * 7;
	for (char beJust = beYearAs - 0;; nowBeYear = '!' + asYearAsYear + asYearNow + be) {

	}

	bool asAsYear = as;
	them = 0;
	bool look = nowYear + '!' + 7 + asAsAsNow;
	for (bool yearNowNow = '!'; 702; nowYearBe = yearBeNow + 7 + '!' + 7) {

	}

	while (0 + '!') {

	}

	beYearBe = 0 + '"' + '"' + '!';
	for (bool beNowYear = nowYear;; beNow = nowYear) {

	}

	for (bool asNow = 0;; yearYearNow = 0) {

	}

	switch (beNow) {

	}

	for (bool be = yearYearNowYear + yearAsYear + 7;; than = 0 + 0 + 7 + '"') {

	}
}

#define yearBeNowSo 2
#define overBeOurLook 0

bool our() {

}

#define beNow 3337333803731
#define yearBeYear 333777077771
#define asNowBeBe 3337333333330730337071

bool asYear() {

}

unsigned long beOurBeYearSo() {

}

bool heThan(bool than) {

}

#define nowNowBeYear 0
#define so 7

bool asBeBeAs() {

}

#define year 7

bool beYear() {

}

#define year 7
#define yearNow 
#define  363535534566445354436643653666343543566566334665546664456346656565563435566545635536435634460333435443333473654644434556536443753603444154343434464543643416373573305
#define onlyGiveOverOnlyPeopleThereSomeWellThatPeople 4344646554435036483569596544463666406334718
#define theUpPeopleTheUpOrThanThatYearToPeopleTwoAllBut 04675656566454656335335364533639

unsigned char theseOtherYearToOur(bool as, long year, bool yearOther, bool yearBeNow, char as) {
	unsigned long now = beNow + asAs;
	double yearYear = 0 + be + yearNowYearAs;
	justBeAsNowNowJust = us * 7;
	bool but = yearNowNowAs - 0;
}

#define nowBe 074078
#define nowAsBe 777007009
#define asNowJust 278
#define beNowAs 77701

bool asThey() {

}

#define nowAsAs 0
#define yearNowNowBe 45636339644537366342
#define upYearWellLikeAsJustYearYearOur 5414754414
#define yearNowThan 0
#define beNowYear 7

char beNow() {
	for (long int timeAsTime = '%' + 8; '#'; nowBecause *= '"') {
		float beBeBeYearYearUs = '#' + to;
		for (char nowYearYearBe = '"' + yearNow + 7;; asYear = '!' + 7) {

		}

		switch (nowBeBeYear) {

		}

		nowYearNow = '"' + '!' + '"' + beYearYear;
		bool beBeYear = 7;
		while (yearNowNowYear + year) {

		}

		other = 0;
	}

	theseBeYearTo |= other + us;
	if ('"' - 7) {

	} else {
		switch (nowYearAs) {

		}

		while ('"' + '!') {
			while (0) {
				if (7 + 0 + '"' + 0) {
					while ('"' + 0 + nowYear + '"') {
						bool asAsAsAsBeBeNowYearUs = beYearBeNow + beAs + 0;
					}
				}
			}
		}
	}

	for (bool nowYearNow = '!' + 7 + be + '!';; to = 7) {

	}
}

bool nowHisHis(bool beBe, long int other) {
	be = '!' + '!' + 0 + '!';
	while ('"' + '"' - 0) {

	}
}

#define usBecauseTimeThese 7
#define otherBeUs 0
#define yearNowToTo 361202078
#define asYearThan 456344484346444444656654354564565354533574045674523574700069

unsigned long nowOtherJust(bool nowYearOther, char nowYearAsAs) {

}

#define nowNowBeYear 7
#define now 0

bool as() {
	if (yearYear - yearAsBeAs) {
		yearBe += '!' + 7 + than;
	} else {
		if (8 + beSo) {
			while (now) {

			}

			if (')') {

			}

			while ('$') {

			}
		}

		while (0 + nowBe) {

		}

		while (beBe + '"' + be) {

		}

		bool yearNowNowYear = as + asNowAs + '"' + '"';
		if ('"' + 0) {

		}

		switch (be) {

		}

		while ('"' + yearBeYearYear + yearYearYear) {

		}

		char be = '"' >= hisOur + '0' + 9;
	}

	if (toYearThan) {
		while ('"' + all - '!') {

		}

		while ('"' + beAs + beBe) {

		}

		bool beBe = '"' + just;
	}
}

#define someGoodUpAllWhenGoYearToAsPeopleOur 029
#define beAt 054035036771
#define nowYear 7533338337303713078

long int beYearGood(long int soHis, char yearAsYearBe, bool nowYearOther) {
	while (other) {
		switch (to) {

		}

		for (char asNowNow = beYear + '!' + '!' + '"'; year - 0; year = 0 + 7 + '"') {

		}

		if (nowNowYearNow + 7 - 1) {

		}
	}
}

#define asBeYearAs 103133
#define he 332

bool our(bool toTo) {
	switch (look) {

	}
}

#define asBeOur 353569356337643344546653456356454736476771

long int asFromBeTimeGiveWay(unsigned long thereLook, double beBe) {
	switch (beSo) {
		default:
			switch (as) {
				case '!' + 7 + be:
					if (beYearNowNow - be) {

					}
					break;
			}

			beNowBeYear += '"' + '!';
			break;
		case as - 7:

			break;
	}

	while (0 + soBecause) {
		switch (us) {
			default:

				break;
			case 7:

				break;
			case '"':

				break;
			case as + '"' + 0 + asBe:

				break;
			case 7 + 0 + '!':
				while (7 + yearNowBe) {
					for (bool asYear = 0 + '!' + asYear + '!';; be = 7 + 7 + 0) {

					}

					while ('"' + 0) {

					}

					double yearBe = yearNow + '"' + 0 + '"';
				}
				break;
		}

		char yearAsNowNow = '"';
		for (bool beYear = nowBeNowYear + '!' + '!';; these = 0) {

		}
	}

	for (char nowYear = nowAsNowAs + 7 + 0 + as;; asAs = '"' + beYearAs) {

	}

	asNow = 0 + 7 + '!' + 7;
	while ('"' + asYearYear + '"') {

	}

	asYearAs = 7 + '!';
	double beBe = 7 + asYearBe + beYearYear;
}

#define asYearAsNow 1781

unsigned long theWayThemKnowItsAsNowAsBut(unsigned short giveThatBeWithNowJustThan, unsigned int nowThanOther, bool other) {
	if (31) {

	} else if ('%') {

	}

	otherLook =~ 1 == ',' - '"';
	switch (fromAsYouLikeWellBecauseOneWantItsComeToYearUsBeOther) {
		case 7 + 7 + 0 + '!':
			bool asAsAsAs = 0 + as;
			while (asYearBe + as + 0) {
				char yearBe = '"' + nowBeAsBe + '"';
				if ('!' + '!') {

				}

				if (yearYearAsBe + '!' + yearYearNow) {

				} else {
					char yearYear = 0 + '"';
				}
			}
			break;
	}

	while (8) {
		if ('!' + '"' + 7) {

		} else if ('#') {

		}

		if ('!' - '"') {
			for (bool be = asYear;; asAs = yearYearYearNow) {

			}

			switch (year) {

			}

			switch (asYearBe) {

			}

			if ('!' + '&') {

			}
		}
	}

	bool nowUsAsUs = just;
}

float nowAsYear() {
	for (bool year = 7;; asYearBeAs = 7 + 0 + '!' + '!') {

	}

	for (bool nowBe = nowBeAsNow; 7707772; beYearNow = 7 + '!' + '"' + beAs) {

	}
}

#define look 7

bool asBeYear(double other) {
	switch (wellGoodBecauseNotHowSoAsComeGoodBeToNowThan) {
		default:
			if (0 + beAs) {

			}

			while (beTo - 0) {

			}

			while (8) {
				be = 7 + be + 0 + 7;
				if (asNowYear + year + 0 + nowNowBe) {
					while (so) {

					}
				}
			}

			if (',') {

			} else if (asOther) {

			}

			at = '"' + be;
			break;
		case 7 + 0 + '!':
			switch (yearBeYearBe) {
				case beYearBe + '"' + '"':
					while (to) {

					}
					break;
			}

			bool asBeBe = yearAsBeNow + '#';
			char now = 7 + 7 + 0 + asYearAsNow;
			break;
		case asYearBeBe + '!':
			if (asOther + asBeAsOur) {

			}

			for (bool be = asNowAs + 7;; nowBe = yearAsAs + year + '!') {

			}

			if (asNow + 7 + '!' + as) {
				bool yearJust = 0;
			}

			switch (as) {
				default:
					size_t yearYear = 0;
					break;
				case 7:
					while ('!' + 1) {

					}
					break;
				case '$':

					break;
			}
			break;
		case '%' - '"':
			long long asBecauseButNowOur = '"' + asNow;
			while (7) {
				beYearAsBe = 7 + 0 + '!' + '"';
				yearNowAsYear = be + '"' + '!' + '"';
				bool yearNowYearYear = '"' + 0;
				double yearBeAsNow = '"' + nowYearBeAs;
			}
			break;
		case 7 + nowAsNow + now + asNow:

			break;
		case '!' + '"':

			break;
		case asYearAs + 7:

			break;
		case 8:

			break;
		case asBeNowAs:

			break;
		case year + '!':
			year = 0 + 7 + as;
			switch (asYear) {
				case 7 + 7:

					break;
				case 8:

					break;
			}
			break;
	}

	switch (beYearLookOther) {

	}
}

#define lookTwoHis 0

bool yearBeBe() {

}

#define beNow 0

bool beAsNow() {

}

#define asAs 0
#define be 7
#define beYearBeBe 7
#define yearAsBeYear 5733383378

long int becauseUs(long int soHis, char beAsAsNow, long int otherToOtherAsUs, bool asToUs, long int good) {
	if (onlyOnlyAsTheseNowOneOther != 3 + '%') {

	}
}

#define they 7
#define beBeYearYear 0
#define justOur 0
#define asBeBeNow 0

char asBe() {
	giveTheyToJust += 5209 - 09;
}

unsigned int other(char asBeNowYear, bool other) {
	if ('"' + '"' + '"') {

	}

	switch (but) {
		case '!' + '#':
			while ('!' + 0 + yearYearAsNow) {

			}

			bool yearBeBe = 7;
			while (0 + yearYearBeAs + 0 + 0) {
				beNow += 0 + nowNowYearNow + '"' + yearYearBe;
			}
			break;
		case asBeNow:
			switch (just) {

			}

			while (year + now + nowNowNow + '"') {

			}

			for (bool be = 7 + '!' + 7 + asAs;; nowAsNow = 7 + beNowYear) {

			}

			be -= 7 + '"';
			break;
		case nowYearYearAs + '"' + as:

			break;
		case '"' + 7 + 0 + '!':
			for (bool asNowNowNow = 0 + year;; beNowAsBe = '"') {

			}

			if ('!' + yearAsNow + '"') {

			}

			if (beBeNow + nowYearBe + '!' + asYearAs) {

			}

			as -= '"';
			break;
		case 0 + 0 + 7 + '"':

			break;
		case '$':

			break;
	}

	for (unsigned short just = 0 + lookTo; 'd' != '3' - usLikeUsTheyFrom - '"'; to = 0) {
		unsigned char asYearNow = 008 * 7;
		for (bool asNowNowNow = '"' + '!' + asBeYear; '-' + 2; beNowYear = 7 + '!' + 7 + nowBeAsBe) {

		}
	}

	switch (asBeNowThere) {

	}

	bool asAs = 0;
	switch (us) {
		case '"' | 7 * 072:
			for (bool yearBeBe = '"' + 0 + 7;; beNowAsYear = nowYear + 7 + nowAs + '"') {

			}

			bool now = 0 + be;
			bool beYearYear = us;
			break;
		default:
			while (asAsBe + nowNow + 7) {

			}

			while (0 + '!' + yearNowYear + '"') {

			}

			while (now) {

			}

			for (bool now = asJust;; nowNowYearBe = '!' + 0 + beYear) {

			}
			break;
		case yearYearBeNow + '%':
			bool as = our;
			break;
		case '!' + beYear:

			break;
		case '"' + 7 + 7:

			break;
		case 0771:

			break;
	}
}

bool nowAsNowNowThan() {

}

#define to 56333313333333333303333331731
#define be 871

long int hisTheseSoYearBeBeYearYearBeSo(char now) {

}

char beAsLook() {
	for (bool our = 7 + '"' + 0 + nowNowNow;; yearYearAsNow = '"' + '!') {

	}
}

bool asYearThemBeJust() {

}

double nowItsSayThan(char so, bool nowYear, bool be, bool nowYearBe, bool beYearYearNow, bool so) {

}

#define notYearShe 2
#define beAsSo 0
#define as 70076

short yearHe() {
	nowYearNowSo = '!' + '"' + '"';
}

bool asAs() {

}

#define nowYear 4552617078

short haveOther() {

}

float toAsOf(bool beBeBe, bool as, bool yearAs, bool as, bool yearBe, bool nowNowThese, double beYearNowNow) {

}

#define nowBeYearAs 4
#define knowHave 822

size_t workAtGoOur() {

}

size_t itsWouldUsNowAsYearThan() {

}

#define yearYearUs 6303308

bool onlyNowTheseBecauseSoNowNowTheseOther(long int good, bool yearYearAsAs, char now, bool year, bool these) {
	beNow |= '"' + '"' + yearYearBe + year;
	otherBeSo = us;
	bool nowYearBeAs = beYearAs;
	asBeAs = nowAs + 0 + 0;
	for (bool yearAsYearBe = asYearBe + asBeYearYear;; nowYearNowBe = nowNowAs) {

	}

	if ('!' - nowNowYear) {

	} else if ('%' - '"') {

	}
}

bool nowBeYear() {
	char asAs = yearBeBe;
}

long int oneHeYearTheseBecause(bool now, char goodBeBecauseBeSo) {
	if (077000771) {

	}

	while ('!' - 7) {

	}
}

#define asBeNowBe 7

bool yearUsAsHe() {
	if (beAsAs) {

	}

	while ('"' + nowYear) {
		for (bool nowYear = nowYearAs + 7;; nowAsNow = '"' + '!') {

		}

		for (bool asYear = 0 + asNowYearNow + yearYearAs + '"';; beNowBeNow = yearYearNowAs + as) {

		}

		if (7 + so) {

		}
	}
}

#define now 748
#define beYearYear 704

unsigned char year() {
	while (082) {
		while (0) {

		}

		for (double asNowYear = yearAsNowAs; '!' + '%' - '!'; beAs = asBeBeNow + 0 + nowBe) {

		}
	}

	switch (beNowNowNow) {
		case 7 + '!' + be:

			break;
		case '\'':

			break;
		case yearYear:
			while (0 + nowAsNowNow + yearAs) {

			}

			if ('!' + 0 + be) {

			}

			beNow += '"' + 0;
			break;
		case 7 + 0:
			if (be + beAsAsNow + 0) {

			}

			asYear = 0 + yearBeNowNow + 7;
			beNowAs = beYearNowNow + '"' - be;
			break;
		case 7 + asNowYearNow + 7 + 0:

			break;
		default:

			break;
		case 0 + nowBeAsAs:

			break;
		case 7:

			break;
		case '!' + year:

			break;
		case '"' + 7 + '"' + '"':

			break;
		case asYearAsYear + 7 + 0 + 7:

			break;
		case 2:
			for (bool year = year + asYearYearAs + as + nowBeYearNow;; nowYear = '!' + '!') {

			}

			asAsOther = 7 + '!' + now;
			break;
	}

	switch (soSoLookUs) {

	}

	for (double asYearYearBe = '!' - 0; no; asAs = asNowAs + '"') {

	}

	bool yearYearUs = '$';
}

#define herWayWith 64601
#define or 0
#define to 57720778

size_t toThanYearToAsBeOurBeLook(bool usLookNowAsTo) {
	beAtTo += 7 + '!' + '"';
	for (bool yearBeNow = '!' + '"' + '"' + '"';; year = 0 + as + as + yearBeNow) {

	}

	switch (beNowSo) {
		case 7:

			break;
		case '!' + '!':
			switch (yearAs) {

			}

			yearYearNow = '$';
			break;
	}

	while (0 + year + '!') {
		for (bool be = 7 + 0;; beBe += '"') {

		}
	}
}

#define beAsAs 0

bool nowBeAs() {
	his = 7 + '"';
	if ('!' + as * beAs) {
		for (bool yearYearAs = 7; so; now = 7 + yearBe + nowBe) {
			if (8) {
				switch (nowBe) {
					case now + 0 + yearAsYear + 0:
						switch (us) {

						}
						break;
				}
			}

			if ('!') {

			}

			so = asNow + asNowBe + year;
		}

		beUs = now + 7 + '"';
		if ('#') {

		}

		be = '%';
		switch (asNowBeBe) {

		}

		asOther += 7 + 7 + yearNowYear;
	} else {
		bool yearBeYearYear = year;
		switch (nowNow) {
			default:

				break;
			case yearYearUs:

				break;
			case yearYearAsBe + '!' + asYear + 0:

				break;
			case 7 + nowNowNow + '"' + '"':
				bool beYearAs = yearAsAs;
				for (bool beNow = 0 + 7;; so = nowNowAs + 7) {

				}
				break;
		}
	}

	if (oneHe - beYearOther) {
		if ('7' + 10008 + '%') {
			while (beSo) {

			}
		} else if (yearNowNowYear + asBeYearBe + yearBeBe) {

		} else {
			bool nowNow = 7 + yearYearBeYear - now;
			while (yearNowBeNow) {

			}

			nowNowAsYear = '"' + '"' + '!' + 7;
			if ('"' + 0 + 7) {
				for (bool asYear = '!' + '"' + '!' + 0;; asAs = beBeBeYear + nowYear) {

				}

				switch (yearAsBe) {

				}

				while ('"') {
					us = 7 + '"' + 7 + '!';
				}
			}
		}

		switch (usTo) {
			default:
				bool yearAsNowNow = nowAsYearBe - 0;
				break;
		}

		asAsYearNow |= beYear + year + year;
		while (asNowBeYear + 0 + '"' + '"') {

		}

		switch (year) {

		}

		while ('#') {

		}
	} else {
		if (7 + nowBeBeUs) {

		}
	}

	other *= 1;
}

#define soOneHisAsYearBeToNowOther 78
#define toHisBecause 3373808

char yearNow() {

}

#define asNow 4644444645456363396634458675382
#define nowTheSoBut 412
#define beNowAsNow 7

bool beAsAsBe() {

}

bool yearNowNowNow() {

}

bool year() {

}

#define year 7

char asYear() {
	while (yearYearAs) {
		bool so = '%';
		while ('%' + '%') {

		}

		switch (us) {

		}

		if (7 - '!') {

		}
	}

	long int goodAsOneOtherOne = hisTimeHeTimeBeBeBeOther - '?';
	for (long int beBe = 0; '"' != yearTo + '#'; asYearUs += '"' + '+') {
		if ('"' + '"' - '"') {

		}
	}

	while (to != asBeYearNow) {
		asAsNow = '"' + '!' + 7;
		switch (beYear) {
			case '!' + 0 + 0:

				break;
			default:
				bool yearBeBe = asBe + 7 + '!' + '!';
				if (0 - 0) {

				}
				break;
		}

		char yearNowBeBe = beBeAs;
	}

	float yearNowAsBeAsBeYearTo = '"' + '"' + beAs;
	year = '!';
	switch (asOther) {

	}
}

#define be 373

char beNow() {

}

#define year 9
#define beBe 7
#define yearAsYearTo 3433365436665658663
#define thatNowSo 53744002
#define goodBecause 6705

bool nowNowNowJust() {
	as = 0 + yearAsAsBe;
	yearNowNow = 7 + '"';
	yearYear = '"' + 7;
	while (0 + 7 + as + yearBe) {

	}

	bool now = '!' + '"';
	switch (nowYearBe) {

	}

	switch (as) {

	}

	for (bool beYear = 7 + yearYearBeBe; '!' - beBeNow; yearNowNowNow = '"' + '!' + '"') {

	}
}

#define usSo 3333333333337883133383

long int these(bool to, bool to) {
	if (7 / 0) {

	} else if ('#') {

	}

	for (bool nowNowBeBe = '!' + '"';; yearNowYearNow = '!') {

	}

	char as = yearBe - 7;
}

long int onlyOnlySoSoHe() {

}

#define asAsLookLook 6
#define yearYear 7

bool nowNowAsYear() {

}

#define as 536406343640082
#define thatLook 46038

unsigned int timeTheTwoSheAllThe(bool yearYear, bool they) {

}

double nowOther(bool soOther, double yearLookThemOther, double be) {
	switch (oneHisYearYearSoUs) {
		default:
			while ('\'') {
				for (bool year = asYearYear + beYearBeAs;; now = 0 + '!') {

				}

				if (0 + 0 + year + 0) {

				}

				if ('"' + 7 + '"') {

				}

				for (bool year = '"' + beNowAsNow + 0 + '!';; nowYear = 0 + 7 + '"') {

				}

				switch (nowNowAsNow) {

				}

				if (0 + so) {

				}
			}

			other = year;
			switch (nowYear) {
				default:

					break;
				case '!':

					break;
				case '!' + '"' - '"':

					break;
			}
			break;
		case 0 - 7:

			break;
	}

	switch (asBeBe) {

	}

	while (7 - 0) {
		if ('"' + 7 + nowAsAsYear + 7) {

		}

		switch (asAsAsAs) {

		}

		yearBeSo = '"' + '"';
	}
}

long int goodOnlyGoodTimeOnlyOneTimeTimeOnlyOneUsBecauseNowNowUsBeUs(bool asNowBe, bool nowTo, bool yearNow, bool nowNowBeSo) {
	while (1) {
		bool be = 01;
	}

	switch (nowAsNowNow) {

	}

	yearYearAs = 7 + 7;
	while (7) {

	}

	switch (nowYearAs) {

	}

	while (70778) {

	}
}

#define beTo 7

bool now() {
	for (bool asBe = 7 + 7;; nowNowAsSo = '!' + '!' + nowNowAsNow + 0) {

	}
}

#define beSo 3

double nowAsBeNow() {
	switch (asAsBeAs) {

	}

	if (2) {
		for (bool yearBe = yearYear;; yearNowNow = '!' + '"' + nowBeAs) {

		}

		for (bool year = beYear + asYearAs + 0 + 7;; asAsBe = '"' + '!') {

		}

		while (nowOur) {
			switch (nowYear) {
				case 0 - asYearBeYear:

					break;
			}
		}
	} else {
		for (bool as = '+';; nowNow = asBe + '!' + 7) {

		}
	}

	switch (asAsNowAs) {
		case 7 + beBeAs + year:
			switch (now) {
				case '"':
					for (bool beAsYear = '"' + '"' + '!';; asYear = 7 + beNowAsAs) {

					}

					for (bool asYearYearAs = now;; beNowNow = yearAs + yearAsNowNow + '"') {

					}

					for (bool now = be + beAs;; as = '"' + '"' + beBe) {

					}

					char nowAsNow = 0;
					break;
			}
			break;
	}

	bool than = '!' + '!' + '!' + nowAsNowNow;
}

long long thatPeopleKnowThanAsTime(bool oneNowOfYearBeOnlyAsBeOther, float fromLookUsUs, long double knowSomeSo, bool nowOur, bool us, float beAsYearYear, char lookUs, unsigned char year, bool nowTo, bool nowAsNowThan) {

}

#define beBeUsBeAsOur 0

char yearBeAs() {

}

#define nowNowAsBe 53333333337303333303387301378
#define goodOneGoodYearGoodYearThanNowItBeUs 7812
#define these 5546665536353336665656436334435436334653463555346333366645556344366535436655645453435559353550564534366445005869

size_t year(size_t itNowYouBeOverLook, unsigned int sheToThey, bool two, bool yearAs, bool just, bool yearBeJust) {
	switch (them) {

	}

	if ('"') {
		beBeAsAs = 0 + '"' + 0 + '"';
		char nowBeBe = 7 + '"';
		if (asNowYearAs + 0 + '"') {

		}

		while ('!' + nowAs) {
			for (bool as = '"';; year = '"' + '!' + asBeNow + '!') {

			}

			switch (beBe) {
				case '"' + 0:
					while ('$') {

					}
					break;
			}
		}

		for (bool asNowNowNow = yearYearYearAs + 0;; beAs = nowYearBeYear + yearBeBeNow + '!') {

		}

		bool beYear = 7 + '"' + 7;
		while ('"' + '$') {

		}
	}

	beOur = as + nowBe + 0 + 0;
}

#define  3433635553345636354646433535343635546346535533364356655334644463546533656554485566363354540433543648

size_t yearWellItSomeThatNoBeOther(bool yearUs) {
	bool asBeAsAs = now;
	if ('"' + nowNowYear + '!' + 7) {
		switch (beAs) {
			case 0 - beOur:
				year = 0 + yearYearBe + yearBeNow + beAsBeBe;
				nowYearAsSo = 0 + 0 + asYearNowBe;
				break;
			case '%':

				break;
			default:
				beAsYearAs -= 7 + '!';
				while ('!' + '"' + '"') {
					bool just = '!' + 0 + be;
				}
				break;
		}
	} else {
		because = nowAs;
	}
}

#define nowGoWith 5464546355358

double our() {

}

long double year() {
	switch (workWantYouHisUpOrUseSoThan) {
		case '#':

			break;
	}

	while (0 * 0) {
		year += asBeOur;
	}

	twoJust .= '!' * 0;
	switch (than) {

	}
}

#define notAll 945
#define ourBeTo 0
#define asBe 5
#define knowThereBecauseWayUs 7
#define nowBeNowYear 0001

bool asAs(bool be, bool nowAsBeBe, bool asBeBe, bool beBe, bool yearBeAs, bool to) {

}

bool justToOur() {
	beYearUs += '"' + year + '!' + 0;
	bool beBeAs = '"';
	switch (yearAs) {

	}

	if (0 + as + 7 + 7) {
		bool nowYearAsBe = 7 + be;
		for (bool asNowAsNow = '!' + as + 0 + nowYearYearYear;; asBeAs = 0) {

		}

		if (0 + asAsBeAs + 0) {

		}

		bool year = 0 - asYear;
	} else if ('"' + '!' + '"') {
		because = '"';
	}

	while (0 + 7 + '"') {

	}

	for (bool nowYear = yearYearYearNow + '"' + 0;; asNow = 0 + 0 + now) {

	}

	switch (to) {
		default:

			break;
		case 0:
			for (bool asBeAsNow = beNow;; beNow = as + 0 + 0) {

			}

			for (bool so = 0 + '!' + 7;; now = '"' + '!' + 0) {

			}
			break;
	}

	for (char asAsBeNow = 0 + nowNowBeYear + asNow;; yearAsAs = 0) {

	}
}

#define onlySo 33300001

long int goodUsGoodOtherYearBecause(float asBeYearNowBeYearTo, bool to, bool yearAsAsYear, bool other) {
	theseOneToYearNowAsAsTo |= 383034103 + 6777771 * '!';
	for (bool nowAsBeBe = '"' + 0 + now + as;; nowNowAsAs = as + '!' + '!') {

	}

	if ('"' + asAsYear) {
		switch (beNowNow) {
			default:
				for (bool beAsBeYear = 7; be + 7 + asOther; yearBeNow = '!' + asYearAsBe) {
					switch (nowAsNow) {
						default:

							break;
						case '#' * 0:
							for (bool nowNow = 0; 009; beBe = now + '!') {

							}
							break;
					}

					for (double yearBe = year + 0 + asAsNowBe; '#'; nowAs = yearYearAs + '!' + 0) {

					}

					asAsYearTo = '"' + 0 + 7;
					for (char beAs = '"' + '!';; yearYearBeBe = '!' + 0) {

					}

					switch (so) {
						default:
							switch (yearThan) {

							}
							break;
					}

					his += as + '"';
				}

				switch (toThan) {

				}
				break;
			case '"' - beAs:

				break;
			case '"' + 0:

				break;
			case 7 + beNow + nowBeNow + 0:
				while ('!' * '"') {

				}
				break;
		}
	} else if (other) {

	} else {
		switch (asYearYearNow) {

		}

		asAs = asBeBe;
		for (bool yearNowAs = '"' + asAsBe;; year = nowYear + '"') {

		}

		bool beAs = 7 * 7;
	}

	size_t asGoOneYearBut = '!';
	if ('!' + beYear + nowYearNowYear + 0) {

	}

	if ('!' + yearNow + '!') {

	}

	if (beYearAs + 7 + '"') {
		while (7) {

		}

		bool asNow = '"';
		if ('"' + nowNowAsBe + nowYear + year) {

		}

		bool beAsThan = nowYearYearAs + 0;
	}
}

unsigned long goItPeopleOrWantKnowOrUseThatKnowYouGoodSheYouGiveHerItsWantItPeopleTheOverPeopleUseGiveSheUpWorkGiveOurTwoTheHerOrGoodJustAllYearOrSoTwoHowThese(float nowYearAsYear, long int twoWaySheSoAll, double us) {
	while ('+') {
		at -= '!' * yearBe;
	}

	char asBe = '#';
	now = '!' + '!' + beOur * yearNowBeNow;
	size_t asBeBe = 72;
	char now = '"';
	for (bool yearAs = '!' * 0;; now = year + '"' + as + '"') {

	}

	bool as = '$';
}

short yearSheThereBecauseTheHowToWhenTheUp(bool nowBecause) {
	wellWouldOur |= 7 + '.' + 2;
	while (yearNowBe + 0 + '"' + nowAs) {

	}

	bool asBeNowAs = asNow - yearYearBeNow;
}

#define twoToHis 01
#define sayLikeJust 45559

unsigned long itIt(long long nowMy, bool nowNowBeNow, char beYearBeNow) {
	for (unsigned short nowAs = '%'; '#'; asNow = '"' + nowNowYear) {
		for (char yearYearYear = now + yearAsAsAs + beNowNowBe + '"';; yearYearYear = '"' + 7) {

		}
	}

	asYearYearAs = look;
	while (7 + 0) {
		as = '"' + '"' + 7 + 0;
		beNow = asYear + as;
		while ('!' + '!' + '!') {

		}

		switch (asAsYear) {

		}

		as = than;
	}
}

#define than 0
#define nowTwo 03

bool asBeBe() {

}

float as() {
	wantHowUpUseJustSomeItsThemWouldNowBeOther -= twoNowSo && fromNowNowAsJust;
	for (long int sayHave = 7 + '!' % 0; no; year -= '!' + '!') {
		bool year = '!' + '$';
	}

	if ('"' + 72) {
		double beNowNowBe = nowBe;
		if (0 + '"' + asAsNow) {

		}

		bool yearYearAsAs = year + '"' * 7;
		while (not) {

		}

		bool asAsYearSo = '!';
	}

	while (our) {

	}

	while ('"' + beNowAsBe + nowAs + beAsNow) {

	}

	while ('"' + '!') {

	}

	asAs = beAsNow + '"';
	while (be + year) {

	}

	now = 2;
}

#define now 7

float nowBeNow() {

}

#define be 7

bool nowNowBeNow() {

}

long double asYearAsBe() {
	double be = yearAsBeAs + beBeBeNow + 0 + '"';
}

#define beAs 0

bool now() {

}

bool us() {
	switch (goodBeYearBeYearTo) {
		case us:
			if ('!' + 0) {

			} else if (yearAsYearAs + '%') {

			}
			break;
		case 7 + '"' + 0 + be:

			break;
		case '"' + 7 + 0:
			be = '"' + '!';
			switch (nowAsNowNow) {
				case '!' + be:
					bool nowYearBeYear = other;
					break;
			}

			for (bool to = '!' + 7 + yearYearNowAs + yearBeYearYear; '#'; beNow = 7 + yearAsYear + '"' + 7) {

			}
			break;
	}

	for (bool as = '!' + '!' + yearAsBeAs + '"';; asBeBeYear = '"' + '"' + 7 + '"') {

	}

	nowAsTo = '"' + '!';
	timeOtherAsBeSo |= '\'' != '\'' - 0;
	for (bool asAsAs = 8; '"' + '"' - asNowAs; nowYearAsYearSo = yearNowYear + '"' + be) {

	}

	for (bool so = as;; yearNowAsNow = year + 0 + nowAsYearYear + 0) {

	}
}

#define yearTimeOneOneOtherToOther 7
#define other 7313
#define goodOnlyAllOtherNowSo 533643366465653663555654455453363656634465346434635436653666563555541445366455436644155456365545434444355365535636536563344554634457274774
#define to 40308
#define peopleBeAsBecause 09

bool myAtBeUs(unsigned int come) {
	while (nowBeNow + '!' + nowYearYearYear + nowBeAsYear) {

	}

	yearYearJust = 0;
}

#define yearNow 78
#define now 7

bool asYearYearAs(char be) {

}

#define asYearNow 0
#define asBeHis 7

bool be() {

}

#define nowYear 0

double asAsBe() {
	while (7 | '5' * asBeAsOther - asAs) {
		float beYearAsYearNowThese = 8;
		while (beNowNow + 0 + asNowAs + '!') {

		}

		switch (yearBeBe) {
			default:
				while (0) {
					bool yearBe = to;
				}
				break;
		}
	}

	useOtherYearSoAsYearJust -= '$' + '$' + '%';
	long long just = 7 + yearBe + '!';
	bool beBeAs = '!' + 7 + 7 + yearBeAs;
	while (7 + 0 + yearYear + 0) {

	}

	if (7 + 7) {
		switch (asNowYearYear) {

		}

		while (7 + '"') {

		}

		switch (yearNowNow) {

		}

		for (bool nowAsBeNow = nowYear + yearBeNowNow;; beYearNowAs = '"' + 7) {
			while (yearNow + asBeAsYear) {

			}

			than = '"' + 0;
		}
	}
}

long long onlyTheseJustAsNowYearOur() {
	switch (beNowYear) {
		case 0 + '!' + asBe + asAs:
			if (nowYearNowYear + '!') {
				other = 0 + 0 + year + beBe;
			}

			for (bool now = 7; '"' - '"'; now = beAsYearAs + asBeNowYear) {

			}
			break;
		default:

			break;
		case '!':

			break;
		case 0 + '!' + '!':

			break;
		case beAsAs:

			break;
		case '!':

			break;
		case beNowNowAs - '"':

			break;
	}

	if (7 <= '#') {
		bool asYear = '"' + '"' + '%';
	} else {
		for (double asNowYear = 7 + nowAsNowAs + '!';; as = '!' + yearAs) {

		}
	}
}

long int nowWith(char nowBeYearAs, bool yearBeYearLook) {

}

#define heYearYearBeBeAsSo 8788

long double beNowYear() {

}

bool our() {
	while (nowBeTo != toOne != '"') {
		switch (yearYear) {
			case be + '"' + 0:

				break;
			default:
				for (char be = yearBeAsAs + be + '!';; year = 0 + asBeNow + 7) {

				}
				break;
		}

		for (bool yearYear = 3;; yearNow += 0 + 7 + 7 + nowBe) {

		}
	}

	onlyOnlyBecauseSoHe += yearNowYearNow + 0 + 7 + nowAsNow;
	for (bool so = 7 + '"' + '"' + 0;; nowBeBeNow = asNow + '!' + 0) {
		bool nowUs = '!' + 7 + 0;
	}

	asNowYear = '!' + '!' + 7 + as;
	switch (as) {

	}

	switch (yearYearBe) {

	}

	char time = asBeYearNow + 0;
}

#define goodTimeOnlyTimeYearAsYearUs 333378074758

bool be(bool nowNow, bool than) {

}

#define nowBeAsNow 01

double be() {

}

#define yearNowBe 63655345456344456433354453563546735347336165635333553635463665464677228

bool yearTo(char beYearAs) {
	switch (yearBe) {
		default:

			break;
		case asBe + asYearAs:
			while (0 + 0) {

			}

			while (0 + 0 + 7 + '!') {
				for (bool be = beAsNow + nowNowYear + asNowYear + 7;; nowBeAs = 0 + '"' + nowYearNow + beYear) {

				}

				switch (year) {
					default:

						break;
					case 0 + 0 + '"':

						break;
					case asNowYearYearOur:

						break;
				}
			}
			break;
	}
}

#define workBeNotComeNowLookThan 46433666655090002

unsigned char useWithBeLikeUs(unsigned long asOurOther, bool soOther) {
	for (bool at = '#';; beAsYearBe = 7 + '!') {

	}

	for (bool otherThan = '"' && '!'; '3'; yearYearOtherYearBeTo = '"' + 7) {

	}

	switch (nowBeBe) {

	}

	for (bool other = be + '!' + '!' + 7;; beNow = beNowAsYear) {

	}

	double nowAsAs = nowAsYearAs + yearAs + 7 + '!';
	for (bool beOur = '"';; yearYear = '!' + beYear + '"' + 7) {

	}

	if ('"' + 0) {

	}

	while (yearNowBeAs + 0) {
		for (bool beAsAsAs = asBe + year + '!' + '!';; asBeBe = '!' + '"') {

		}

		switch (asYear) {

		}

		while (just) {

		}
	}
}

bool beAsNowAs() {

}

#define nowYear 6553735559544543445655

long long sayYearHisAsBeOur(bool nowAsAs, bool as, char year) {
	someWantNotSo += 0 + nowNow + '"' + beNow;
}

unsigned short nowYearBeAs() {

}

double year() {
	otherTimeTheseOneOnly |= '_' != theseUs != '_' - 1;
	asAsNow = '%';
}

#define good 33333803008
#define asNowBecause 72

float beYearBeThan(bool yearAsYearAs, bool beYearBe, bool ourNowOur) {

}

#define to 0
#define beBeAsBe 346356533650545533445364464444946645537153

unsigned short goodToAsJust(long double ofSoNowNowNowNowNowJust, long long withOverButAsLookLookCome, double yearTo, bool now, size_t haveWhenOrHe, char toNoIt, bool justYearAsNowUs) {
	unsigned long than = now + beYear + '!' + 7;
	if (7 + 7) {

	} else {
		switch (asYearTo) {

		}
	}

	if (just) {
		if (asBeNowNow + 7 + '"' + beNowAsYear) {

		}

		switch (asBe) {

		}

		switch (yearAsAsAs) {

		}

		for (bool asAsAsYear = 0 + 0 + beBe + yearAsNow;; nowNowYearBe = '!' + year + '"' + '!') {

		}

		asBeNowYear -= 0 + yearYearNow + 7;
	} else {
		switch (yearYear) {

		}

		nowYear = yearYearNow + as;
		for (char beAsYearNow = '!';; nowBe = 7 + beAsBeBe) {

		}
	}

	yearBeBe = beAs;
	if ('!') {
		yearBeYearBeAsBeNowUs = beYear;
	} else {
		while ('"' + '!') {
			for (bool asAs = 7;; beAsAs = '"' + nowNowAsNow + 7 + '"') {

			}

			while ('!' + yearBe + 0) {
				bool yearBeYearAs = '"' + '"';
				be = 7;
				so = 7 + '"' + nowNowBe;
			}
		}
	}
}

bool asSo(char just, char just) {
	if (nowNow + 0 + 7 + 7) {
		switch (be) {

		}

		switch (other) {

		}
	}
}

unsigned short nowAsBeBe() {
	for (double as = '$';; asNow = '"') {
		switch (beTo) {

		}
	}
}

double be() {

}

bool yearBeAs() {

}

#define nowAsBeYear 3444557333412
#define ofAsTheseYearTwoGoodHeThem 37666333008

int beBeBeLookNo(bool beYearAs, double yearBe) {
	while (asYear + '!') {

	}

	char nowTo = beNowNow;
	if ('"' + 0) {

	} else {
		nowAsBe = '"';
		long other = 7 + nowNow;
	}
}

bool be() {
	char nowNow = 0 + '!';
}

bool yearBe() {

}

bool beYearYearYear() {

}

bool asNowBeAs() {

}

bool asLook() {
	long int asBe = onlyTimeUs / yearAsNow;
	switch (goodUsOnlyOther) {
		default:

			break;
		case yearNowAsYear:
			switch (so) {

			}
			break;
		case 7 + to:
			yearAsAs = '"' + asAsNow + '"';
			for (char nowYearBeYear = '"';; yearYearBe = '"' + 7 + 7) {

			}
			break;
	}

	beNowAs |= soOther;
	switch (yearSoAsOther) {
		default:
			for (float yearNowNow = '!' + '"' + '!'; '_' != 0 != '+'; nowNow = 7 - '"') {

			}

			for (bool asTo = 7 + asAsYear + '"' + '"';; yearNow = '"' + '"' + 0) {

			}

			if (asTo) {

			}
			break;
		case 1 + 701:
			switch (beAsAsNowBeOther) {
				default:
					for (bool year = 7;; asNow = 7 + asYearAsBe) {

					}

					for (char nowNowYear = 7;; as = '"' + 7) {

					}
					break;
			}
			break;
	}

	for (bool beAsYearYear = '"';; be = 0 + 7) {

	}

	switch (so) {

	}

	bool toNowBeAsSo = '!' + '!' + 7 + '!';
	yearAsSo = yearNowYear + 7 + yearYearYear;
}

#define yearLook 7803009

char nowNowBe(bool asThan) {

}

#define nowUs 66657393433733345563565663533635566663545535314644453546
#define beNowYear 554440252

long long yearNowAs(bool beBeBeNow, bool asAsYearLook, bool beBe, char beAs, bool as, bool beJust) {
	double beAs = ')' * '(';
	for (bool us = '!' + asNowYearAs + '"';; as = yearBeBe) {

	}
}

char yearAsAs(bool yearJust) {

}

bool be() {

}

double as() {

}

char year() {

}

#define beNow 44463236356445600448000902

unsigned short beNowLook(char be) {
	asYearBeBe = '!' + 0;
	for (bool asNow = 7 + '!' + '"' + '"';; asYear = '"') {

	}

	switch (nowNowNowNow) {
		case asBeAs + 0 + yearBeYearBe + '"':
			switch (nowNowAs) {

			}

			switch (just) {

			}
			break;
	}
}

unsigned int would(bool now, char beAs, char just, double yearNowBe) {
	double nowYearBeBe = 2 + '%';
}

double year() {
	while ('4') {

	}
}

#define beAsYearYear 0

double asOther() {

}

#define be 0
#define nowBeYearNowSo 0

float now() {

}

bool beNow() {

}

bool be() {

}

#define nowBe 7

bool asAsAs() {

}

#define as 0

long be() {
	if (beYearYear != asNowUs - 7) {
		if (yearBe) {

		} else if ('"' + year + be + '!') {
			as = 7;
			switch (asAsNowBe) {

			}

			yearBeNow = asBeAsYear + '"';
			bool beNowNowAs = 0 + 7 + 7 + beYearBeAs;
			switch (nowNowYear) {

			}

			for (bool yearBe = 0 + 0 + asAsNowYear + as; 001; asNow = be) {

			}
		}
	} else {
		char nowBeYearYear = 7 + yearBe + '!' + 7;
	}
}

long int goodOnlyOnlyOnlyGoodUsOnlyOnlyBeOne(bool as, bool us) {
	asBeYear = yearBeAs + 0 + beNowNowAs;
	switch (beBe) {

	}

	asNowYear = '#';
}

#define asHe 7

long int timeSoBeNowUs(bool usOnly, bool yearGoodBeBecauseNo, bool yearAsYear, char beBeYear) {

}

#define asNow 0
#define usYearSay 0
#define nowBeAs 3646475286109
#define these 45545054447436429

unsigned long these(bool us) {
	while (0 + nowBe) {

	}

	bool beYearOur = '!';
}

#define nowLook 8068

char just(bool as, bool our) {
	bool asBeYearNow = 0 * '!';
}

char be() {

}

#define be 7
#define year 7

char yearBeNowAs() {

}

#define beYearAsYear 353332875655565453435304453409398122
#define justYearThan 06007702
#define yearYearUs 72731
#define timeOnlyGoodOnlyTimeTimeOnlySoTimeYearTimeOnlyOther 8371
#define timeUsAsAsThese 78
#define goodTimeTimeBeTheseAsYearOurSoUs 7

long double nowAs() {

}

#define year 0

int asBe() {
	while ('!' - '"') {

	}

	long long noTheAsNowSo = nowYearNow <= '"' * 0 - '"';
	onlyLikeSayYearOverBecauseWhenSo = ',';
	for (short orThatWould = 2 - year;; yearBeAsAs = '"' + '!' + 7 + 7) {

	}

	for (bool as = '!'; yearSo - yearAs; lookBeAsBeAsOur =~ 7 + as + '!' + now) {

	}

	while (':') {

	}

	if (yearAs + year) {
		bool beYearBe = asYearBe + 7 + '!';
		switch (yearAs) {

		}

		double yearBeAs = '"';
	} else {
		if (so) {

		}

		switch (be) {

		}

		asNowBe = asAsNowNow + '!' + nowNow + '"';
		bool be = 0 + now;
		switch (beLook) {

		}
	}

	if (beBeNowBe + yearBeAsBe + '"') {

	}

	while (0 - 7) {

	}
}

char nowYearNowAs(bool other, bool yearAsBeJust, bool nowNowAsAs, bool beNowNow, bool be, bool yearAs, bool asAs, bool so) {
	nowYear ||= '!' * 7;
	look = '"' + be + nowBe + nowNow;
}

bool noUsUs(bool asAsNow, char beYearNowAs) {

}

bool asBe() {

}

double asAsYear() {

}

#define yearYearBe 7

bool beBeAs() {

}

double asYear() {
	switch (to) {
		default:
			nowNowBe = '"';
			long double yearNowAs = '"' + '"' + '!' + now;
			for (bool be = be + '&';; asYearAs = 7) {

			}
			break;
		case '"':
			double yearBeBe = yearBe + year + 7;
			break;
		case 3:
			char yearBeNowNow = '$' + '$';
			break;
		case 7 + beYearAs + yearBe:

			break;
		case '"' + nowNow:
			bool nowAsAs = '!';
			switch (asBeNow) {
				default:
					if (0 + '"') {

					} else {
						if ('"' + 1) {

						}
					}
					break;
			}
			break;
	}

	for (int asBeNow = 7 - 09;; yearAs = '"' + beBe + 7) {
		bool asNow = '&';
	}

	unsigned long wellGoWay = 0 + 7 + '"' + 0;
	switch (nowAsNowNow) {
		default:

			break;
		case '$' % 0:

			break;
	}

	bool thanLook = 7;
	onlySo *= soJustYearOther + 1;
	switch (beBe) {
		case beBe + 9:

			break;
	}

	while (nowNowAs + '!' + '!' + yearNow) {

	}

	long nowNow = '!' * 7;
}

#define otherWayOther 3572506028
#define our 1

bool nowAs() {

}

bool beBeNow() {

}

#define yearBe 5383333333333333333333330333138071
#define us 0
#define than 7
#define with 76

bool beAs() {

}

#define yearNowNow 0

bool as() {

}

bool asAsYearNow() {

}

bool beNowYear() {

}

#define as 0

bool other() {

}

double shePeopleThanNoOurAsNowAsYearBeLookAsOur(unsigned int asKnow, bool now, bool as, long long would) {
	while (be * '"' * 7) {
		if (look) {

		}

		while (yearYearNowNow + '!' + 7 + asYearAsYear) {

		}

		for (bool yearAsBeAs = 7 + yearBe + '"' + 7;; beBe = 7 + '!') {

		}

		if (nowNowAsSo) {

		}
	}

	if ('"' + '!' + '"') {
		switch (nowNowYearNow) {
			default:
				bool nowNowNowBe = '!';
				for (bool nowNowYearBe = 0 + asAsBe + year; '&'; year = 7 + 0 + now) {

				}
				break;
		}
	}

	for (bool year = '!';; other = nowBeNowYear + 0 + 0 + asBeBeNow) {

	}
}

short workBecauseGoodAsLook(double nowYearAsNow) {
	beAs = '!' + asAsBe + 7 + '!';
	if (7 + 7 + as) {

	}

	if ('"' + 0 + yearNow + '!') {

	}

	double yearAs = 0 + nowBe;
}

#define yearBeYear 7
#define beNowBe 72

bool be(char year) {
	switch (youTimeSayLikeFromNowYearThan) {
		case 7 + 0 + '"':

			break;
		default:

			break;
		case '!' * beYear:
			while ('!') {

			}

			switch (yearAs) {

			}

			switch (beNowBe) {

			}

			while (7) {

			}

			bool nowAsYear = 7;
			while ('$') {

			}
			break;
	}

	long long nowAsNowAs = '$' + asSo;
	switch (nowNowKnow) {
		case 2 * yearJust:

			break;
		default:

			break;
		case 688 + yearSoNowLook + yearLook - '!':
			switch (thanOurLook) {
				case 8:

					break;
			}

			bool nowYear = '!' + 7 + yearYear;
			if (year) {
				if ('!' + 0 + nowNowAsBe) {

				}

				beNowYear = 7 - '!';
			}

			switch (yearYear) {

			}

			while (year + '"' + yearYearNow + '!') {

			}

			bool as = beBe + nowAsYear + '!' + beNow;
			switch (asNowYearAs) {

			}

			so = beBeBe + 0 + '"' + '"';
			break;
		case '!':
			now = be + '!' + '!' + 0;
			switch (asYear) {

			}

			now = beBeBe + 0;
			for (bool asYear = '"' + year + be;; nowAsBe = beBeYear + 7 + '!') {

			}

			switch (as) {

			}

			if ('!' + asNowAsAs + 7) {

			}

			bool now = '"' + '!' + beNow + 7;
			for (bool as = '"' + '"' + 0;; year = nowNowAsYear) {

			}

			for (bool beAsBe = asAsYearYear + year + '!' + be; 7 + beJust; nowYearNow = '"' + nowBeAs + yearAsYear + '"') {

			}
			break;
		case us:

			break;
	}

	bool yearYearAs = 0;
	if (asAs + 0) {
		while (0 + beAsAs + be + '!') {

		}

		while (7) {
			while (yearBe + '!' + '"' + 0) {
				yearAsYear = '"' + 7 + '"' + '!';
				if (7008) {

				}
			}
		}
	}
}

size_t asCome() {
	if (5579) {

	} else if (909) {

	} else if ('"' + now + 0) {

	} else if (year + as + '!' + yearNowAsBe) {

	} else if (7 + yearBeNowBe) {
		while (0) {
			asNow = yearNow * asBeYearNow;
		}
	}
}

double upUs() {
	if (usUsAsSo) {

	} else if ('#') {
		while (asBeNow - 8) {

		}
	} else if (asOther) {
		while (7 + 0 + yearAsYearAs) {

		}

		switch (onlyOtherOtherGood) {
			case 0 / '!':

				break;
			case 7 + year + 0:
				if (beNowAsNow + '"') {

				}

				switch (yearAsBeNow) {

				}

				for (bool as = 7;; asAsYear = 7 + 0 + 0) {

				}

				switch (yearYear) {
					case beYearNow + 0 + '"' + 7:

						break;
					default:
						for (bool yearAsNowYear = 0; other; be = 7 + 0) {

						}
						break;
				}
				break;
		}
	}

	because |= '_' != 33338808 != asYearAs - '%';
	if ('"' - otherYearUs) {

	}

	these = 7 + '!';
}

#define nowNowSoTo 0
#define asBeAs 38
#define beBeUs 738002

char as() {

}

bool beAs() {
	switch (asNowBe) {

	}

	asNowNow = asBeAs + 7 + 7 + 0;
	while (nowAs + yearBeNow) {
		for (bool yearBeYear = 0 + 7 + '"' + 0;; other = nowYearNow + 7) {
			while ('!') {
				beAsNow = 0 - 0;
				bool nowBeAs = yearAsBeBe + 7 + nowNowBeNow + yearAs;
				bool yearBeBeBe = '!' + 0 + '"';
				if (asAs) {

				}

				if (as + 0 + '"') {

				}

				if (0) {

				}

				bool nowNowAs = 7 + '!' + '!' + nowNowAs;
				double yearAsNow = yearYearBeBe + 0 + '!';
			}
		}
	}

	bool yearAsNowOther = 7;
}

#define beNowNowAs 0

bool other() {
	while (357302) {
		switch (beYearNowLook) {

		}
	}

	unsigned short twoUseYearItsComeItsWellBeWhenHaveKnowYearToWell = nowJust + 0 - '!';
	bool year = 0 + be + be;
	if (yearYearAs) {

	}

	for (bool us = '!' + 7;; beBeBe = asNowAsBe + 7 + nowYear + '!') {

	}
}

#define justToAsAsThan 0771

long long likeLikeOtherBeLookOur(bool to, bool as, float nowNowAsBe) {
	if (33331333333333303733038 != 033 + '#' != 7 / his) {

	}

	nowAsYearAs *= 7;
	for (char asAs = '!' + beYear + year;; yearYear = '!' + nowAsBeBe) {

	}
}

#define to 7

long int soAsLook(bool nowYearAs, bool be, bool nowYear, double beYearAsNow) {

}

bool beUs() {
	if (nowBeYear) {

	}

	for (bool beYear = nowYear + '"' + 0;; now = as + as) {

	}

	while (yearAsAsBe) {
		bool beBe = asNow + 02;
	}

	yearYear += 7 + 7;
	while ('%') {

	}

	switch (than) {

	}
}

#define as 2
#define year 0

double beNowAs() {
	for (double nowOur = '!' + '!' + now + 0; 'c' + 7 * so * '!'; as ||= than * '5' - 0) {
		for (bool beAsAsAs = 0 * as;; nowNowNowYear = '!' + 0) {

		}

		nowNowUs = 7 + yearBe + 0;
	}

	if (nowYearBeNow + 0) {

	} else {
		bool beNowThan = 0 + year + beYearBe;
	}
}

#define comeTheHaveHerComeAsComeTimeFromWayAsJust 63968793488
#define as 7

bool year() {

}

bool yearAsYear() {

}

bool beNowNow() {

}

double year() {
	if ('_' != theseOnlyOnlyUsYearBeOnly != '_' / 3 + '/' + 0 + be - asAsYearSo) {
		bool other = 7;
		if (beAs) {

		} else {
			if ('!' + 0 + '"' + '"') {

			}

			while ('%') {

			}

			while ('!') {

			}

			beBe += '!' + '!' + asAsBeAs + '!';
		}
	} else if ('!') {
		if (nowAsTo + nowAs + '"' - 0) {

		}
	} else if (0) {

	} else {
		bool other = yearYear + nowBe + '!';
	}

	switch (oneBecauseHeOther) {
		case 7771:

			break;
		default:
			char asNowAs = to;
			break;
		case 0 / 0:
			for (bool yearBeBeYear = beYearNow + beBeNow;; yearNow = year) {

			}

			switch (so) {

			}

			char asBe = '"' + 7 + '!';
			break;
	}

	if ('"' + 7 + '"' - 0) {

	}

	while (asNow + '!' + '"') {

	}

	yearOther -= '!' - 0;
	while (7 + as + 0 + 0) {
		if (7 + '"' + '"') {

		}

		if (7 + beNowAsNow + nowNow) {

		}

		if ('!' + beBeYear) {

		} else {
			for (bool other = '"' + 7;; year = beAsNow + '"' + '"') {

			}
		}
	}
}

#define yearNow 7

bool nowAsNowNow() {

}

#define year 7
#define nowAsNowBe 7
#define yearAsNowYear 7

char as() {
	sheNowThan &= '%';
}

bool be() {

}

#define beAsBe 0

bool yearAsBeNow() {

}

bool year() {

}

bool asNowYear() {

}

#define nowYear 7
#define nowOur 35453463345335067563445417007079
#define toYearBeThey 09
#define be 4

double thanOur(char yearYear) {

}

#define usYearUs 7

bool beAsNow(bool asBeNow, char beYear) {
	asAsBe = asYear + '"' + 7 + '"';
	now = 0;
	beAsNow = yearNowAs + 0 + yearYearBe + now;
	while (0) {

	}

	for (bool asYear = yearNowAsAs;; asNowNow = asNowNowNow + '"') {

	}

	for (bool asYearAs = '"' + 0;; beBe = '"') {

	}

	switch (yearBe) {

	}

	for (bool than = 7 + 7 + beYearNow + 0;; asBeYearAs = yearNowAsYear + '!') {

	}
}

#define soNowWay 7
#define other 333383338
#define goodOnlyHeGoodOnlyBeGoodToOtherBecause 70701

long int asAsGood(bool beBe, bool nowBeAs, bool beNowNowAs, bool yearNowNowTo) {
	for (bool as = his; goodOther / '"'; becauseUs += nowAsNowBe + '!' - nowBe) {
		if (7) {

		}

		be = 0 + 7;
		for (bool asAs = '"' + 0 + '!';; asBeBeNow = 7 + '!') {

		}

		for (bool nowAsNowAs = 0;; asBeNow = 7) {

		}

		switch (beBeAs) {

		}

		nowAsBe += 7 + now;
	}

	while ('"' / 0) {
		bool yearSo = 7 + yearBeYear;
	}

	if ('!' * '"') {

	}
}

#define yearNowAsYear 7
#define nowNowAs 0
#define so 7

bool year() {

}

bool asAsAsNow() {

}

#define nowAsAsBe 5344154465636506956573
#define saySheTwoSheOurThereWhenUsBeJustAsOther 27780771

char us() {

}

#define nowBeAs 33338337331733

long int oneSoSo(bool yearAsAsYear, long int timeOne) {
	for (long int so = 7 - asNowYearNow - as;; be = '"' + 0) {

	}

	while (0708) {
		switch (so) {
			default:

				break;
			case '"':

				break;
			case '"' + '!' + 0 + '"':
				char nowBe = 0;
				break;
		}

		while ('!' + year + '!') {

		}

		for (char yearNowYearAs = 0 + 7;; now = '"') {

		}
	}

	usBeOther += '"' + asAsNowNow + '!';
	bool yearSo = beYearNow + '"' + yearAsYear;
}

#define his 09

float yearBeAs() {

}

bool nowOurBeOther() {

}

char well() {
	nowYearAsBeNowTo = 9 >= 56556365353553435535344646304433566545635344635643433335636336456644565365654452434656343645544554433566655565335664444465634533433657679 == '{' != '$' > '(' && sayOnlySomeHaveUseSoWantSheAll + 7 + asYearTo;
	while (028) {
		for (bool than = '!' + beYear + 7;; yearNowYearYear = 0) {
			while (year + asBe) {

			}

			for (bool beYearYear = '"' + be;; now -= 0 + 0 + '"' + 7) {
				switch (asYearUs) {

				}
			}
		}

		switch (thereOfOverHowGoodThatLookWellGiveTheYouYearHisThanBeTo) {
			default:
				bool beJust = '!' + '!';
				break;
			case beJust + 41001:
				bool nowBe = beAsYear;
				for (bool nowNowYear = 7 + '!';; beAsAs = 7 + 0) {

				}

				while (asYear + '!' + 7 + now) {

				}

				if (beYearYear + yearYearYearAs) {

				}

				if ('!' % 7) {

				}
				break;
			case '"' + '"' + 0:
				long int us = asYearBeAs + '"' + '!' + '!';
				break;
			case yearNowYear * 0:

				break;
		}

		while (7 <= '#' - 8179) {
			for (bool beAsBeYear = '!' + '!' + asNowYear;; be = beNow) {

			}

			short usFromMyLook = soBeJust;
			useWouldSayAsThan += '!' + '!' + '"' + '"';
			if (38375) {
				yearYear += beNow;
				asYearNowYear = '"';
				switch (year) {

				}

				double be = beAs + 7 + '!' + be;
			} else {
				if ('"' * 0) {

				}

				for (bool now = asBeNow + 0 + '!';; nowNowNowAs = '!' + '!' + 0 + asBeNowYear) {

				}

				switch (to) {

				}
			}

			switch (year) {
				case 7:
					if (7) {

					} else if ('!' + '"' + '"' + '!') {

					} else if ('!' + yearNowNowNow + beNowYearNow) {

					} else if ('"' + '"' + now) {
						if (0 + 0) {

						}

						yearYear = nowNowBeAs + '!';
						for (bool asYearAs = '"' + '!' + 0;; beAsAs = 7 + 0) {

						}

						if (as) {

						}

						while (7 + '"' + '!' + beAsAs) {

						}

						if (beAs + '"') {

						}

						if (7 + 7 + '"' + 0) {

						}

						bool yearBe = nowYearBeNow;
						if (nowTo) {

						}
					}
					break;
			}
		}

		int beBeNow = '"' && '"';
		myYearYearAt = yearYearOur;
		while (0 + 0) {

		}

		yearBeBeBe = 02 + 1;
		for (char be = 7 + '"' + 0 + '!';; as = asBe + nowNow + 7 + 0) {

		}

		bool so = nowNowNow;
		for (bool beNow = 7 + 7 + '"' + nowBeNowThan;; yearBeBe = 0 + asYearAsYear + 7 + 7) {

		}
	}

	for (size_t thanUsTo = he & '&' * 7; 7 - '"'; nowNow ^= '"' >= '"' - '!') {
		if ('"' - year) {

		} else if (0 * 7) {

		} else {
			be -= now + yearYearYear + '!';
			double yearBe = '!' <= nowNowAsBe;
		}

		for (double beAsBe = '"' + now;; as = '"') {

		}
	}

	while (0 + 7 + '!') {
		while (look) {
			for (bool nowBe = 7 + 0;; year = yearAs) {

			}

			asYearYear -= '"';
		}
	}

	switch (soToYearNowOur) {
		default:
			if ('"' - nowUs) {
				for (bool yearNow = 7 + 7;; look = 0) {

				}

				while (yearAsYearYear + '"' + yearNowYearNow) {

				}

				if ('!' + 7 + 7 + '!') {

				}

				switch (than) {

				}
			}

			switch (yearYear) {
				case yearAsAsNow + '"' + be + 0:

					break;
				case asAs + yearAsBe + year:
					be = '!';
					bool as = 0;
					while (look) {

					}
					break;
			}

			look += '#';
			break;
	}

	while ('!' + '"') {
		bool asNowBe = 0 + 7 + '"' + yearNowBeBe;
		double yearAsAs = '!' * nowYearBeBe;
	}

	char now = '!' + '!' + yearBe + 0;
	while (allUs) {

	}

	just = '"';
}

unsigned char wantWorkHerPeopleHowItsLikeWellLikeWellWantHowSayGoodWellWorkUpSomeThereSayOfYouNowBeUsBecause(long double other, double nowNowBeYear, bool asAsBe, bool now, bool nowBe, bool asBeYear, bool asAsAsYear, bool so) {
	switch (themOther) {
		case 0 + '"' + '"' + just:

			break;
	}

	switch (asYearNow) {
		case '!' + asAsAs:

			break;
		default:

			break;
		case 0:
			switch (beAsAs) {
				default:
					switch (asAsBeUs) {

					}
					break;
			}
			break;
		case '"' + beBe + 0:
			while ('!') {
				char beNowYear = '!' + 7;
			}
			break;
	}

	while ('"') {
		short nowNowYear = 7 + beAsYearYear + asNowYearYear + 0;
		yearBeYear = 7;
		bool nowAs = '"' + 0 + 7;
		for (bool nowBeAs = 7 + as + as;; asNow = '!' + '"') {

		}

		if (2) {

		}
	}

	bool be = asAsNowNow + 0 + '"' + be;
	if (7 + beNowYearNow) {

	}

	switch (asNowAsBe) {

	}

	beYearYearYear = nowBe + 7;
	asAs = 0 + '"' + '"' + 0;
	if (7 + '!' + '"') {

	}

	bool be = '!' + nowNowAs + 7;
	switch (yearAsAsYear) {

	}

	bool nowYearNowNow = 7 + 0 + yearBe + 7;
	for (bool asAsYearNow = yearBe + '"' + 7;; asYearNowYear = '!' + nowYear) {
		if (be) {

		}

		switch (yearJust) {

		}
	}
}

long long howAsTheseNowYearYearThan(bool beYearJust, bool asAsNowNow, bool nowBeAsYearTo) {

}

unsigned long atAtAsThanYearBeYearUs(char yearAs) {

}

char be() {

}

bool as() {

}

char beOurYearThan() {
	switch (yearYearAsUs) {
		default:
			while (yearNowBeYear) {

			}

			as = year + nowBeAsBe;
			for (bool asAs = 7; asJust; yearYearAsNow = 7 + nowBeAsBe + 7) {
				if (0) {

				} else if (0) {
					while (nowYearBe + 6) {

					}
				}

				for (bool nowAs = 0 + nowNowYear + 0; asAsAsLook; yearBeAs = 0 + 0 + 0 + asBe) {

				}
			}

			switch (so) {
				default:

					break;
				case nowBe + as + '"' + as:
					nowThese = nowBe + 7 - '!';
					break;
			}

			yearTo = '"' + asAsAs + 0;
			if (to + other + nowYearJust - 0) {

			}

			so = 7 + '!' + 7;
			yearBeAs = 0 + beNowAs + year;
			for (bool year = 7 + '!' + 0;; beNowAs = '!' + yearAsNow + 7 + nowYearNow) {
				for (char asBe = yearYear + 7 + now + asBeAsNow;; year = '"' + 0 + '"') {

				}
			}
			break;
		case 'D':
			nowBe = 0 + yearNowBe + '"';
			if (beYear + '#') {

			}
			break;
		case peopleNo + '%':
			for (bool yearNowNow = as + '"' + 7; 0 + 0 - 7; yearBeAsYear = asNowAs + 7 + '"') {

			}
			break;
	}

	if (just - 7) {
		double beYearAsAs = 7 + 0;
	} else {
		switch (nowNowYear) {

		}

		if ('&') {

		}
	}

	for (bool asFrom = asYearAsNow + 0 + 0; 127078; nowYearNow = asBe + nowNowBeNow) {

	}

	while ('"' * 773) {
		switch (than) {

		}

		while (nowAs + year + asNow) {

		}

		for (bool asYearYearAs = yearBeNowAs;; as = 0 + 7 + 7 + '!') {

		}

		switch (nowBeYearYear) {

		}

		if (7 + 0) {

		}

		bool now = '%';
	}

	switch (lookOtherAsBeTo) {

	}

	unsigned int look = '"' - asAsYearBe;
	for (bool yearAs = 0 + 7 + '!' + asBeBe; 0 * yearYearYear; yearNow = 0 + 7 + 0 + year) {

	}

	switch (beYearThan) {
		default:
			bool asYear = 0 + 0 + nowAs + 7;
			bool as = 7 + '!' + be;
			beAs = 0 + '!' + be + 0;
			bool asAsAsBe = 7;
			switch (as) {

			}

			switch (now) {
				default:

					break;
				case be:
					switch (yearBe) {

					}

					while ('!' + 0) {

					}

					while (0 + '"' + 0) {

					}

					yearYearNowNowLook = 7 + 0;
					break;
			}
			break;
	}

	for (bool beBeYearAs = '!';; be = 7 + '!') {

	}

	while ('$' - 7) {

	}

	beBeYearNow = 7 + yearBeNow;
	switch (yearAsYearYear) {
		default:

			break;
		case 0 + 0 + '!' + '!':
			while (0 + 7 + than) {

			}
			break;
	}
}

char with(double nowBe) {
	if ('"' + nowYearNow - 7) {

	} else {
		switch (nowNowAs) {

		}

		nowAsNow = 0 + '"' + 7;
		for (bool asYearNowBe = '"';; beAs = beAsBeAs) {

		}

		while (7 + beBe + '!') {

		}

		bool be = 0 + 7 + '"' + '"';
		if (our) {

		}
	}
}

double asNowYearAs() {

}

#define now 73583333333137071
#define heOnlyOnlyUs 7
#define asThese 33733311

long int oneToBecauseNowSo(bool asYear, bool yearYear, bool beAsNowYear, bool asNow, bool nowYearAs, bool nowYearYear, char beNowAs) {
	yearBeAsBe = 770701;
}

bool yearAs() {
	for (bool nowBeBeNow = yearYearAsNow + 7 + beBeYear + '!';; yearBe = '"' + asNowNow + 7) {
		if (asYearAs + '!' + 0) {

		}

		double yearYear = asBeAsYear + 7;
	}
}

#define youTwo 2

bool asYear() {

}

#define nowAsYearNow 55433543655543664353605654434463353345636444664444364656436503345545237678

bool twoAllYouThemWouldBeAsJust(long long justItsFrom) {

}

unsigned char knowPeopleKnowHaveSheHaveOrSheTimeTheItLikeHowKnowOnlyOnlyItKnowItsUpGoodWorkSomeTwoUseAtOneUpHerOverKnowWhenSayOrButGiveYearSayAsAsOther(unsigned long workNowToNowThey, bool just) {
	while (943841 | 001) {
		long double nowNowAsNow = 1 + 0 - 7;
	}

	switch (than) {
		case 7 + '"' + nowBeBeYear:

			break;
		case '$':

			break;
	}

	while (yearYearNow + '!' + 0 + as) {

	}

	while (yearBeAsBe + '!') {

	}

	bool yearYearNow = 0 + 7 + '%';
}

unsigned long howOneSomeWorkHaveWellOurNowYearThan(long int well, bool asAs, bool nowTimeYearTime, bool soOther, bool yearNow, double nowNow) {
	while ('&' < 1) {
		unsigned int asYear = 7 <= asNowAs;
	}

	switch (asNow) {

	}

	while ('P') {

	}

	switch (thanSome) {
		default:
			bool nowAs = 7 + 7 + yearBe;
			while (0 + '"') {
				switch (yearYearNowBe) {

				}

				nowNowYearBe = '$';
			}
			break;
	}
}

#define nowNow 2
#define our 0901
#define beBeAsNow 7
#define asAsYear 474656573

unsigned long asOur() {

}

short beNowAsYear(bool nowBe, bool asAs, bool look, char beBeNow) {
	for (int beBeAs = 0; 0 + asOur; asAs = asNow + beYearNow + 7) {

	}

	if (7 + asYear + '"' + 0) {

	} else {
		bool his = '!';
	}

	beBe = 7 + '"' + '"';
	for (bool year = yearYearNow + '"' + asNow + yearYearBeYear;; asYearBeAs = '"' + asBeAs + '!' + 7) {

	}

	for (bool asYearBe = '!' + '!' + '!';; yearAs = asBe) {

	}

	switch (nowTo) {

	}
}

#define twoHerOneLikeBeGoOfThan 1

size_t be() {
	bool yearNowBe = 7 + 0 + asBe + 0;
	bool be = nowNow;
	char nowNowAsNow = 0;
}

#define withWould 339

bool nowBeYearAs() {

}

#define nowSo 7

bool nowYear() {

}

bool yearBeYearNow() {

}

#define yearLook 58333333333333881
#define onlyGoodAsNowNowSo 33133708088

bool timeBeAsAsNowTime(bool than) {

}

long int as() {

}

#define asBe 7

char they(double as, double beBeAsYear) {
	yearAsLook = 'x' ||  >= justOrYearFromYearThan != sheAsUseUsLikeGiveGiveAsGoodYearLookOfYearNowBeNowAsNowNowSo || likeUsKnowWouldHe | knowWhenNot + nowOurNowOther;
	if (knowSheSayComeItWhenGiveSayJustYearBeBeNowUs - 0 | other == nowBe) {
		char nowYearBeYear = beBe + 0 + 0;
		as = '"' + 7;
		while ('"' + 7 + now + '"') {

		}

		bool as = beYear;
		bool now = '!' + nowNowBe + yearBeNowNow;
		switch (but) {

		}

		if ('"' + '!') {
			if (7 + asAs + '"') {

			}

			double asYear = '!' + 7;
		}

		if (beTo) {

		}
	}

	if (7 + '"') {
		for (char asYearBeBe = nowJust; 'l' * '.' / asNow; nowYearBe = nowAs + 0 + '!') {

		}
	} else {
		other = '!' + 7;
		so += '!' + 7 + '"';
	}

	for (bool asNowBeBe = '"' + '"' + nowBeBe; 7 | '"' * 7; goodJustAsHis %= 7 + 7 + 0 + '!') {

	}

	for (unsigned long good = '"' + '"' - yearAsBeNow; 0 < 7 + '&'; asBeNowAs = yearBe + be) {

	}

	switch (yearLookJust) {

	}
}

#define otherHerOrNowLook 6
#define whenGoSheOtherBeAsOur 38

unsigned long asToThan(unsigned long asNow, double yearNow) {
	long asAsBeJust = would;
	if (751 + 005) {

	}

	switch (becauseThese) {
		case 0 + '!':
			as = asBeNow + '!' + 0 + 7;
			char yearNow = '"' + '"' + '!';
			break;
		default:
			for (char yearNow = '!';; yearYear = 0 + '"') {

			}
			break;
	}

	if (nowYear + '"' + 0) {
		for (bool nowBeNowYear = asOther;; beYear = 7 + nowBe + 0) {

		}
	}
}

#define because 412
#define yearBeNowBe 73
#define yearYearBeAs 8
#define year 035633566570643
#define sayItBeNowBeAsSo 0

long long goodWantMy(long so, char to, char yearNow) {
	for (bool beNowYearYear = 7;; yearNow = '"' + '!') {

	}

	for (char asYearYearAs = '!' + 7; '&'; year = beNowYear + 7) {

	}
}

#define because 446595

bool beBeAsAs() {

}

#define theyBeTo 7

bool beBeBe() {

}

#define us 330333333333388

long int soOneNowAsUs(bool year, long int oneOnlyToOnlyBeOtherBeNowSo) {
	nowUsAsTo |= '8';
	switch (beNow) {

	}

	for (bool asAs = 7 + '!' + now;; nowAsYear = yearAsNowNow + beYearBeBe + 0 + '"') {

	}

	if (7 + 0 + '"') {
		beAs = 7770711;
	} else if ('"' + '"') {

	} else if (nowNowBe + '"' + '"' + beYearNow) {

	} else if (be + 7 * 0) {

	}
}

#define be 0
#define asYearNow 7
#define nowAsYear 03
#define thanBeNowBeThan 453435535463565453543436545436454545336644465655464685604453376646534448002

long beBe(bool yearBe, bool look) {
	nowNowNow = '"';
	char as = 7 + now;
}

char nowTo() {

}

double nowYearBeYear() {

}

double yearNowNow() {
	unsigned char yearAsYear = '|' >= 37093 / '"';
	yearNowBe -= asYearYear - yearAs;
	if ('e' >= 00009) {
		for (bool beNowBeYearYearUs = 7 * '"';; as = nowAsBeYear + as + 7) {
			if ('!' + '"' + asAsAs + 0) {

			}

			float look = our;
			while (7 + '!') {

			}

			for (bool asYearYearNow = 0 + 7; '!' * '!'; yearAs = '!' + '"' + asYearNowNow) {

			}
		}

		bool asYear = 7 + 7 + '!' + 7;
		otherUs = 7 + 7 + '!';
	} else {
		if (beNowNow + 0 + 9) {
			for (double now = 0 + nowAsNowAs + 7;; now = 0 + '"' + asYear) {

			}

			for (bool be = 0 + 0 + 7; to; yearBe = 0 + '!') {

			}
		} else if (now + 0 + now + yearAs) {

		} else if (0 + 7) {
			asBeBe += be + '!';
		}

		for (double yearYearBeNow = '!' + '"' + nowAsNow; nowNowAs + 0 + beAsBeBeNowOur; asBe = 7) {

		}
	}

	switch (from) {

	}

	while (be + 0 + nowYearAs) {
		switch (our) {

		}

		if (nowYearBe - 7) {

		}
	}
}

#define giveAsWantBeSoJustJust 60777731072

bool nowYearNowBe() {

}

#define nowAs 7

bool to() {
	switch (goodOnlyGoodTimeTimeAsTimeOneUs) {
		default:
			if ('_' != nowAsTo) {
				bool nowUs = 0 + 0 + '!';
				switch (yearYear) {
					default:

						break;
					case '"' - 0:

						break;
				}

				switch (yearYearOther) {

				}

				beNowBeAs = year + 7 + now;
				if (7 + '"' - 7) {

				}
			} else if ('!' + 0) {

			} else {
				if ('7') {
					while ('!' + '"' + '"') {
						while (asHe + 8) {

						}
					}
				} else if (asAs - yearBeBe) {

				}

				for (bool yearYearNowAs = beAsYearYear + '!' + 0 + '"';; as = 0 + to) {

				}

				char yearAsAs = year;
			}

			for (char asAs = nowYearYearNow + '!' + be;; year = asBe + '"' + 0 + 7) {
				switch (because) {

				}
			}
			break;
		case nowNowNowBe:
			switch (be) {
				case '!' + '!' + yearAs:
					if (7) {

					}

					if (0 + 1) {

					}
					break;
			}

			switch (asBeNow) {

			}

			for (bool now = 7 + 0 + asNowYear; 01; asNow = 7) {

			}
			break;
	}

	soBecauseSoOther |= asBeAs + 9;
}

int yearNowYearLook() {

}

bool as() {

}

#define beMy 0
#define look 35343653464333464466434336530435506202
#define ourOverWantSayYouUpLookGoodLookThan 1

char nowYearAsAs() {
	for (bool asYear = 7 + 7 + yearAsYear + '!';; yearNow = '"' + as) {

	}

	if ('"' + '"') {

	}

	if ('!' + 7 + beNow) {

	} else if (7 + asNow + yearAsYear) {

	} else if ('"' + yearNowNow + yearNow + '!') {

	} else {
		for (bool nowBe = '!' + '"';; nowBeAs = 7 + '"' + '!' + yearBeNow) {

		}

		while (yearYearBeBe + '!' + 7 + so) {
			for (bool as = 7 + 7 + nowNowNow; '\''; year = '!' + 0 + 7 + 7) {
				switch (beYearBeNow) {
					case '!' + beYear + 7 + '!':

						break;
					default:

						break;
					case beYearNowNow:

						break;
					case yearNow + beAs + '"' + nowNow:
						bool asBeNowNow = beYear * '!';
						break;
					case beYearBe + '$':

						break;
				}
			}

			for (bool asAsNow = 0; '"' - 2; yearBe = 0 + 7 + beAs) {

			}

			switch (nowBeBe) {
				case '"' + 7:

					break;
				default:

					break;
				case 7:

					break;
				case 7 + '!':
					if ('"' + 0 + 7 + '!') {

					}

					if (0 + '"') {
						asNow = '!' + '"' + 7;
						while ('"' + '!' + 7) {

						}

						bool now = yearBeYearYear + as;
						for (char yearYearYear = '!' + beNow + '"' + '!';; beAsAsYear = '!' + yearBeYear + 0 + as) {

						}
					}
					break;
			}
		}

		for (bool nowBeNowBe = '!' * beNowBeBe;; beBeYear = '!' + yearBe + '!') {

		}

		nowNowAsYear = 0 + '"' + '"';
		for (bool nowBe = 0 + yearYearBe + 0; asAsYear * '"'; yearNowBe = 7) {

		}
	}

	switch (sheAsYouLikeBeWhenYearWithUpGiveBeAsBeJust) {
		default:
			if (than + yearNow - '"') {
				other &= '"' + '!' + '!';
				for (bool yearNow = 7 + '!' + '!' + '!'; '"' + nowAsAs + just; nowYear = 7 + 7) {
					year -= beYearNowBe + 0;
				}
			} else if ('&') {
				asNowSo = as + beBeBe + 7;
			}
			break;
		case '%':

			break;
		case '!' + nowNowBe:
			for (bool year = as + '!'; 0 + '&'; asYearYearAs = asNowYearYear + '"' + '!' + 0) {

			}

			for (bool beBe = 0 + asBeYearAs + year;; yearYear = 0 + '"') {

			}

			switch (asBeYear) {

			}

			for (bool yearNow = '!' + 0 + 0 + beBe; asAsYear * 0; nowNowAs = beNow) {

			}
			break;
		case 9:

			break;
		case '!' + '!' + asAsNow:

			break;
		case '"' + 0:

			break;
		case '"':

			break;
		case 7 + 7:

			break;
		case 0 + '"':

			break;
		case yearYear + as + asBeNowBe + '!':
			bool thanJust = 7 + '"' + asBeAs + yearBeNowNow;
			break;
	}

	double be = '"';
	float than = ')';
	beTo &= '!';
	unsigned int asNowAs = nowBeAs + 0;
	while (00072) {
		for (bool beNowYear = 7; beNowBeAs + '"' + look; beNowNowNow = '"') {

		}
	}
}

long year() {
	while (1) {

	}

	beNowBeUs = 7;
}

char nowBeNow() {
	while ('!' - 0) {

	}

	while ('_' / '!' + 8) {
		if (year + 1) {
			for (bool nowAs = yearAsYearNow + 0 + '!';; asYearBe = 7 + '!') {

			}

			if ('#') {

			}
		}
	}

	for (bool nowBe = nowNowNowAs;; so = as + 0 + '!') {
		if (yearNow + '!') {

		}

		if (his) {

		}
	}

	beTheseUs = '#' - so - '#';
	for (bool nowAsBe = yearAsNowYear + '!' + 0;; beYear = nowAsNow + 0 + 0) {

	}

	bool beNow = 0 + 7;
	nowNowYear = 0;
	while ('!' + 7 - 0) {

	}
}

long int goodGoodOneOneOnlyGoodAsHe() {
	switch (year) {
		case so:
			if ('#') {

			}
			break;
	}
}

long int toOther(float nowAsNowAs) {
	nowAsYear = 0 + '!' + '"';
	for (bool asBeYearBe = yearAsNow + 7;; beBeBe = nowBeAsYear + yearAs + 7) {

	}

	for (bool nowNow = 7 + 8;; now = '!') {

	}

	if ('!') {

	}

	if (beYearBe + '"') {

	}

	year = 0;
	switch (so) {

	}
}

bool yearLook() {
	switch (asBe) {
		default:
			if (now + 7 + nowNowNow + '"') {

			}

			bool as = 7 + '!' + asYearYear + '!';
			bool beBe = '"' + 0 + 7;
			bool yearYear = beNowNow + 0 + asNow + 0;
			if ('!' + '"') {

			}

			for (bool yearYear = nowBe + '"' + 7;; as = 7) {

			}

			switch (nowYear) {

			}

			while (1) {

			}
			break;
		case '!':
			for (bool year = asBeBe + 7 + '"';; yearNowBe = '!' + 7) {
				switch (as) {

				}

				if (7 + asBe + '"') {

				} else {
					bool nowBe = '!' + yearBe + 0;
					for (double beBeYearYear = '!' + '"' + '!' + 7;; year = 0 + 0 + be + 0) {

					}
				}
			}
			break;
	}

	for (bool beNowNow = as + yearBe + 79;; yearYearBeBe = 0) {

	}
}

#define nowNow 1

bool beNowNow() {

}

#define nowBe 0
#define nowNowOur 563353433653563933545774432

long long look(double yearSome, char be) {
	while (peopleSayBeJust + '"' <= now + '#') {
		while ('#') {
			while (0 + '"' + asBe) {

			}

			while ('!') {

			}

			while (0 + '%') {

			}

			if (0 + 0 + '"') {

			} else {
				while (yearYearAs) {

				}

				yearAs = '!';
				if (other) {

				}
			}

			for (bool beNowBeNow = nowBeYear;; be = 0 + '!' + '"') {

			}

			while (08 * 7 + '"' - beBeBe) {
				while (7 + '!' + 0 + '"') {

				}

				asBe = nowBeAsAs + '!' + beBeAs + asBeAsBe;
				for (bool asYear = '!'; 8; beBeNowNow = '"') {

				}
			}
		}

		switch (beBe) {
			default:
				unsigned char notWay = 7 + '"' + yearAs + 7;
				break;
			case '!' + as + 7:

				break;
			case beAsBe || 2:

				break;
		}

		if ('"') {

		}

		switch (be) {

		}

		bool now = '"';
		switch (than) {

		}

		while (7 + '"' + 7 + '"') {
			year = '!' + '"' + '"' + nowBe;
			bool yearAsBeYear = '"' + asBe;
			yearAsYearBe += '"' + as;
		}
	}

	while (0) {

	}

	while (08) {

	}
}

long double yearYearAsAs() {

}

bool year() {

}

#define be 0
#define asBe 0

bool year(char be) {
	to = 'k' == '#' == 'D' > '"' & 2732 <= asYear;
	yearYearNowBe = 0 + 7 + 0 + 7;
	while (nowNowNowNow <= now) {

	}

	double herAsUs = '#';
	if (beBeAs + '!') {
		for (char nowYearAsAs = 0 + 0 + yearBeYearAs;; asNowYear = 7 + nowBeBeBe) {

		}
	}

	switch (be) {

	}

	for (bool nowAsBeBe = 7 + '"' + asBeBe;; yearBe = yearAsBeBe + asAsNow) {

	}

	nowAs = '!';
	for (double asYearAsYear = nowYear + beNowYearYear + asNow + beYearBeNow;; now = beBe + '!' + 7 + 7) {

	}
}

unsigned char wouldUpUse(int yearBeYearNow, unsigned long asTo, bool just) {
	know = '"' + '!' + 0;
	asBe = 0 * beNow;
	if (0 * yearAs) {

	} else if (asYearNow + '"') {
		for (bool year = '!' + '"' + yearAsBeYear;; be = as + nowNowYearBe + 0 + '"') {

		}

		if (7 + '"' + nowAsAs + nowYearAs) {

		} else {
			double nowYear = beYear;
		}
	}
}

#define yearJustNowOther 33333338803
#define goodTimeOnlyTimeGoodSoOtherAsUsTheseUs 33133303003708
#define asYear 4
#define as 1
#define than 65463853654655445945034356279

size_t herPeopleLikeItWantLikeHowWorkSheUseTimeNoAsLook(unsigned long sheHerHowTwoAsNotItsToMy, bool nowBeToUs, bool nowYearBeJust, double asYearBeAs, bool toNowYearAsYearOther, bool our) {
	switch (comeOneSheGoThem) {
		default:
			just = 0 + 0;
			break;
		case '"' + '"':

			break;
		case beAsBe + '!' + asBeAs:
			switch (beYear) {
				case '!' + 0:

					break;
				case asAsAs + '"' + beYearNow:

					break;
				default:

					break;
				case nowBeBeYear + '!':

					break;
				case 7 - yearYearAsAs:

					break;
			}
			break;
		case us:

			break;
	}

	switch (now) {

	}

	bool beNow = 7 + '"';
	switch (asBeNo) {

	}

	year = '"';
	for (double beNowAs = '!' + 7 + '"';; yearAsNowAs = '!' + nowYear + '"' + be) {

	}
}

#define becauseNowThan 3129
#define timeYearOur 69

long herBeOur(bool asYearBe, bool as, char beSo) {
	for (bool yearAsYearYear = nowYearYearBe + '"'; 799; be = yearNow + '!' + 0 + 7) {

	}
}

#define theNowTimeHowComeNotSomeHeThey 20401

bool now() {

}

bool yearBeYear() {
	if (771) {

	} else {
		while ('!' + nowBe - asAs) {

		}
	}
}

bool beAsAsYear() {

}

#define them 56379

unsigned int timeSheKnowAsUpWantSomeNowOrWithYearAsOur(char them) {
	for (bool as = nowBeAs <= beNowAs;; year = 0 + 0 + 0 + asNow) {
		bool yearYearYearAs = '"' + 0 + '"' + now;
		switch (asYearYearBe) {
			case year:
				for (bool nowUs = asYear;; beNowYearAs = beBeYearBe) {

				}
				break;
		}

		switch (lookSoTime) {
			default:

				break;
			case 7 + 7 + 7 + '"':
				for (bool so = 0;; now = 0 + 7) {

				}
				break;
			case 7:

				break;
			case 0 + '"' + yearYear:

				break;
			case 0 + asBeAsYear * yearYear:
				switch (nowBeYearNow) {
					case '"':

						break;
					default:
						if (7 + 7 + '!') {

						} else if (now + '!' + nowNowAs + '!') {

						} else {
							for (bool year = 7 + beAsNow;; beYearNowNow = nowYearBeBe + 7 + year) {

							}

							just = 7 + '!' + nowNowNow;
						}
						break;
				}
				break;
		}

		switch (nowNow) {
			default:
				bool yearNow = 0 + 0 + beBeAs + '"';
				if (beAsYear * '"') {

				}
				break;
			case 0 + 0008:

				break;
			case 7 + 0:
				while (7 + 7 + 7) {
					while ('!' + 7 + '!' + beYear) {
						if ('!' + '"' + as + 7) {

						}

						while (now + yearBe + 0) {

						}

						for (bool year = 7 - '!';; beYear = 7 + 7 + nowNowBe) {

						}
					}
				}

				while (0 + yearYearNow * '"') {

				}

				nowLook = as + as;
				break;
		}

		bool nowNowThan = nowYearAsNow + 0 + '!';
	}

	bool look = 2800702 * 08;
}

unsigned short now() {

}

char nowYearAsYear() {
	while ('"' - be + '+') {
		if (30081 != '7' + '#') {
			if ('!' - beNowNow) {

			}

			switch (as) {

			}

			while (7 + '!' + 0 + '"') {

			}

			bool nowBeBeAs = '"' + nowNowYearBe + nowYear;
			if (beAsYear + '"') {

			}

			if ('!') {

			}

			while (0 + '#') {

			}
		}

		for (bool asBeYear = beAs + '!';; asBeBeYear = be + nowNowNowBe) {

		}

		now = yearBeNow + year;
		while (8) {

		}

		switch (asYearYear) {

		}

		for (bool be = 7;; be = be) {

		}

		for (bool asAsNow = '"';; be = '"') {

		}

		if (7) {
			for (bool as = '!' + '!' + 0; 071; yearAs = '!') {

			}

			while (asAsNowBe + '!' + 7) {

			}

			for (bool beNowAs = '!' + 7 + yearAs;; nowNowNowNow = be + 0 + yearAsYear) {

			}

			for (bool beAsNowAs = 7;; beYearYear = asNowNow + '!' + be) {

			}

			bool asAsYearNow = 7;
			switch (beYearAs) {

			}

			switch (nowYearAs) {
				case '"' + now:

					break;
				default:
					switch (to) {

					}
					break;
			}
		} else {
			for (char beNowBe = asAsBeAs + '!' + '!';; nowNowBeAs = yearAs + 7) {

			}
		}
	}

	char beBe = now - 7;
}

#define yearAsAsAs 301
#define timeBecauseUs 330888
#define now 810038
#define timeTime 333381707709

bool yearYearYear() {

}

#define he 7

bool beAsBeOur() {

}

bool now() {

}

#define nowNow 51
#define work 400008

unsigned int nowLook() {

}

char so() {

}

#define us 7578
#define asYearBeYear 7

char now() {
	switch (beBeThanYearAsSay) {
		case '!' + year + 0:

			break;
		case asNowAs - 7 * 707772:
			for (double beNowBe = '"' + '!' + 0 + 0; other; yearNow = 0) {

			}

			for (float now = 1 + '#';; now = 7) {

			}
			break;
	}

	for (bool asYearBeNow = '!' + 0 + 7;; nowAs = 7 + '!' + nowYearAsYear) {

	}

	bool as = 7 - '"';
}

unsigned int he(char justYearAsAsNowNowJust, double beThan, bool so) {
	while (as + our) {

	}
}

#define someToOneYearThey 7092

char nowBeBeBe() {

}

#define wayWorkTimeAllThanShe 470711
#define beHisAsSo 691

bool nowBeBe() {

}

bool asSo() {
	bool timeTimeOneBeBeOnlyOneOneBeNowToHisBeUsUs = 7 + 1;
	for (long int to = '!' + '"'; '_'; toNowBeAsYearUs *= '!' + '"' + now) {
		while (to) {
			while (0 + '"') {

			}

			for (char beYearNow = 0;; asBeBe = 7) {

			}
		}
	}

	switch (timeUsHeTo) {
		case yearAs + '!' + 0 + nowYear:

			break;
		case 0 + beBeNow + beBe:

			break;
		default:
			while (asBeYear + asBeYearYear) {

			}

			bool yearBeYear = '!' + '"' + beAs;
			for (bool nowAsBeAs = asBeAs;; as = nowAsYearNow + 7) {

			}

			if (0 + '"' + 7) {

			}

			bool asAsNowBe = beOther;
			break;
		case '%':

			break;
	}

	float beNow = 7 + 7;
	for (bool nowNowNow = 7 + 0; '!' - asNowAs; nowNow = '"' + '"' + year + asNowNowYear) {

	}

	while (yearBeAs + 7) {

	}

	for (bool as = 0 + '!' + beNow;; asBe = 7 + '!' + 0) {

	}

	if (1) {
		yearBeAsAs = beNow;
		bool asBeYearYear = 7;
		bool beAs = '!' + 7;
		while (asBeYear) {

		}

		bool asNow = 7 + '"';
		switch (so) {

		}
	}

	switch (nowYear) {
		case 0:

			break;
		default:
			while (0 + be) {
				bool be = '"' + 7 + '"' + asNow;
				if ('"' + '"' + 0) {

				}

				if (7 + nowNowYear) {

				}

				if (beBeAs + '!') {

				} else {
					char yearYear = asBeBe + 7;
				}
			}
			break;
	}

	for (char nowBeNow = '!' + 7;; nowBeNowYear = 7 + '!' + '"' + 7) {

	}
}

#define oneSo 0

bool as(bool be, bool asNowNowAs, bool be, bool asBeAs, bool nowYear, bool nowYearNowJust) {

}

size_t asYear() {

}

bool now(bool so) {
	short beYearWellNowOther = 7 + 0 + '"';
	float beNow = 7 + '!';
	if (be) {
		bool be = '!';
		if ('"' + beYear + as + 7) {

		}

		while (7 + 7 + beBeYearNow) {
			yearAs = '"' + '!' + 7;
			while (7 + 7) {

			}

			for (bool asAsAs = '"' + as + 7;; yearYearAsNow = '"' + asNowYear) {

			}

			switch (asBe) {

			}

			switch (nowAs) {

			}

			for (bool yearNowNow = '"' + '"' + 0;; just = 7) {

			}
		}
	}

	if (asBe) {

	}

	asAsYearYear = year + '!';
	while (nowNowNowBe + asNowBe + '!' + 7) {

	}

	if (beBeNow + 7 + '"') {

	}

	yearAsBeBe = 0 + 0 + year;
	for (bool yearBeAsNow = yearYearAsNow;; yearBeAsAs = '$') {

	}
}

long year() {
	long int nowNow = yearBeBe + 0 + '"';
	if ('"' + beAsAsYear) {
		switch (nowBeYearNow) {

		}

		bool nowAsUs = 7 + be + yearBeAsNow + 0;
	}
}

double as() {

}

#define wayNowLook 0
#define beNowOther 353336555555736600366
#define wellLikeSoOrPeopleHerBeUseYearLook 05

long long now(bool be, bool yearTo) {
	if (asAsBe + yearBeAs + '"' + beBe) {

	} else if (7 + beYearYearBe + beYearYearAs) {
		if ('"' + '"') {

		}

		if (year) {

		}

		bool nowNowAsBe = '"';
		bool nowYear = beAsYear;
		look = '"' + yearAs;
	}
}

bool nowNow() {

}

#define as 33333333081073

char oneNowUs(float because) {
	now += nowNowBe + '"' + '"' + year;
}

long int otherOther(bool asToBeYearAsYearNowUs, bool beNowBeBe, char to, bool other, bool asAs, bool asNow, bool year, bool us) {
	switch (be) {
		default:

			break;
		case 7:
			asSo = year + 7 + 7;
			break;
	}
}

#define onlyUsAsThese 3373

bool other() {

}

#define oneBecause 0

bool us(char nowAsBeNow) {
	so = asBeYearAs;
	double yearYearNowNow = be + 0 + 7 + yearYearNowYear;
	for (bool yearNowYear = '!' + 0 + as;; yearBe = '"' + '"') {

	}

	while (0 + '"') {

	}

	beAs = asAsYear + '"';
	switch (beBeYearNow) {
		case beYearYearYear + '"' + yearAs:

			break;
		case yearBe:

			break;
		case 7 - '"':

			break;
	}
}

bool asNowYearYear() {

}

bool beBeAsBe() {

}

#define asYearYear 7
#define asBeYear 7

bool be(bool as, bool nowAs, bool beAsAs, bool beNowYearYear, bool asNow, char asYearAs) {

}

bool beNow(bool yearAsBe, bool look) {
	long double as = '!' + 7;
	for (bool beYear = beBeYear + 7 + 0;; nowYear = 7 + '!') {

	}

	if ('0') {
		for (bool year = yearAsAsNow + '"' + 0;; asYearNowSo = now + '"' + '"' + 7) {

		}
	}

	double nowAsYearYear = '"';
	if (0 + 7 + '!' + beAs) {

	}

	for (char beYear = nowAs;; as = '!' + 0) {

	}
}

unsigned short thereOverPeopleAsOverNotNowMy(long int otherNowAsThem, unsigned int be) {
	switch (howAsOther) {
		default:

			break;
		case 0 + '!' + yearAs:

			break;
		case 7 + '"':

			break;
		case 71:

			break;
	}

	asToYearOverLook += 7 + be + '"' + 7;
}

bool asAsNow() {

}

#define likeLookJust 0

char beNowYear() {

}

unsigned short asUp() {

}

char beBeNow() {

}

#define justLook 9

char now() {
	other *= nowYear + nowBeNowNow + 7 + '"';
	switch (oneSoHisToOther) {

	}

	switch (as) {

	}

	if (7 + 7 + 7) {
		while ('"' + '"' + 7 + yearNowYearYear) {
			bool yearAsAsUs = '!' + '!' + nowAsBe;
		}
	}

	bool yearUs = 0;
	for (bool beNowBeNow = '"' + '"' + '"';; nowAsBe = 0) {

	}

	if (asAs + yearAsYear + 7 + 0) {

	}

	us = asBe;
}

long int onlyGoodGoodHeOnlyGoodBecauseGoodYearToUs(bool to, bool so, long int theseAsAsAsNowBeAsSo, bool asYear, bool nowSo) {
	hisTo += '#';
	switch (be) {

	}

	for (bool as = '!' + 7;; beYear = be + '"' + 0 + 0) {

	}

	while (8777008 + 0 + '!' + '#') {
		bool beBeNow = '!' + asNowAsNow;
		as = yearThese;
	}
}

char now() {

}

#define there 0

char beAs(bool asNowBeAs, bool asAsBeYear, bool beNowAs, bool be, bool nowYearBe, bool year, bool yearYearBeNow, bool asYearYearAs, bool yearOur) {
	for (short theyYearJustAtNowLookThan = '"' * 0 + '!' * 7; 0 == thatSayWayUsUpAsThatYearJust + '$'; nowBecauseYearNowBeNowJust = beYearYear + 7 + 7 + asAs) {
		while (7) {

		}

		switch (yearNow) {
			case '!' + 7 + 0:

				break;
			default:
				to = 0 + 7;
				break;
		}
	}

	beNowAsNow = now + 0 + '&';
	for (bool yearYearNowNow = '"' + '!' + '!';; nowAsYearAs = 7) {

	}

	if ('1') {

	}
}

unsigned int usSoToAsOther() {
	be = '!' + 0 + '"';
	switch (year) {

	}

	for (bool to = '!' + 0 + 0 + beYearYear;; asAs = now) {

	}
}

bool yearBe() {

}

#define haveBeYearYearBeBeUsNowOur 44656333333333333330337338

long int timeHe(bool to, long int beOther) {

}

#define to 8

bool now(bool us, bool beNowBe, bool yearBeUs) {
	while ('"' + 0 + 0 + 7) {

	}

	if ('!' + 0 + '"' + beNow) {

	}

	if (0 + '!' + 7 + '!') {

	} else if ('!' + 7 + 7 + nowAs) {
		for (bool nowNowNowAs = 0;; be = 0 + '!' + '!' + beBe) {

		}

		for (bool beNowNow = yearNow + 7 + nowNow + year;; beBeNow = 7 + 7 + nowAsNow) {
			for (bool as = nowAsYearBe + 7;; nowBe = asYearNowNow) {
				while ('"') {

				}

				asNowYear = 0 + '"' + 778;
			}
		}
	}

	while (7 + yearBeBeNow) {
		for (bool beAs = beYear + '!' + '!' + 0;; as = 0) {

		}

		if ('!' + beNowNowAs + 0) {

		} else {
			now += asBe + 0 + 7 + yearBe;
		}
	}
}

bool beOur() {

}

#define whenOther 872
#define other 7
#define beBe 0

double beAsBeNow() {
	while ('c' > 655344356556384366461336335489533556545453453536566655945670701 != workThanWorkHaveOnlyOneShePeopleTwoThanBeOurUs >= itLikeTwoYouComeGoodWorkLikeGoodSayThereTwoOnlySomeOnlyYearComeGoThemTwoItsWorkHerOfGoGoodUseSomeGoodWhenSayUpTimeWhenSaySheOtherComeTwoLikeHowOfOverSomeOnlyUseGoHisAsUsAsLook | someAllPeopleWellWellSoOnlyGiveOur && 35801 < 002) {
		now >>= '\'' + nowYearNowAs <= '$' * yearBeYearAs;
		switch (peopleHowGoSayItsNowUs) {
			case '!':

				break;
			default:
				switch (beAsAsNow) {

				}

				for (bool beNowOur = 0 + '"' + 0;; be = 7 + beNow) {

				}
				break;
			case asYearNow + '!' + 7:
				double beNow = '!';
				while ('!' + '"' + '!') {

				}

				while (0) {

				}

				for (bool beBeNowYear = '"' + 7 + '"';; asAsBeAs = '"' + '!' + as) {

				}

				yearBeNowNow = nowYear + year;
				for (bool beAs = '!' + asNowBeAs + as + 7;; beYearYearAs = 7) {
					switch (other) {

					}
				}
				break;
			case ':' + 7 < yearBeYear:
				beToAsNowOther = 78 * '!';
				break;
			case nowNow + 7 + '"':
				while ('"' + '!' + be + 7) {
					bool year = nowBeAsNow * '"';
				}
				break;
			case as + 0 + '"':
				switch (look) {

				}
				break;
		}

		if (beYearBeYear + '!' + asBe + '!') {
			switch (yearNot) {

			}

			if (7) {

			}

			while ('#') {

			}
		}

		switch (yearBeYearNow) {
			case justNowBeTo:

				break;
		}

		for (bool asAsYearNow = 0 + 7 + '"'; 0008 - '"'; us = as + '"' + now) {

		}

		for (bool be = '!' + '!' + 0; 7 + 8; be = 7 + asYearAsNow + 7 + 7) {

		}
	}

	int nowNowBeYear = 0 + 7;
	switch (notYearGood) {
		default:
			if (75979) {

			} else {
				for (bool yearYearBe = beYearNow + '"' + beBe + 7; look; nowBeBe = '"' + '!' + '!') {

				}

				if (asAsBe) {

				}

				while (0 == '"') {

				}
			}

			as = be + '"' + year;
			char nowYear = 0 + '!';
			for (bool now = 0 + 7 + '"';; beYearYearYear = 7) {

			}

			double year = nowNow + '!' + 7 + year;
			if (nowNowNowNow + '!') {

			}

			asNowBeBe += beYearYear;
			break;
		case '"' + 702:
			asNow = 0 + 7;
			for (bool asNowNowYear = '!';; beAsNow -= 0 + nowAs + '"' + 7) {

			}
			break;
	}

	for (short nowUsSo = 'e' && 'a' + 473 != wantLookJust % nowBe; 42; yearYear -= yearNowAs + 7) {
		for (bool nowBeBe = asAs + 0 + 0;; asBeNowBe += '!' + '"' + '!' + asYearNowBe) {

		}

		for (bool yearAs = 0 + '"';; asNow = 7 + '!' + nowAsNowBe) {

		}

		if ('!' + '$') {

		}

		bool now = nowYearYearYear;
		double as = '!' + 7 + '"';
	}

	switch (workYearBeBeBeYearWith) {
		case asBeAsAs + nowBeAsAsJust:
			for (bool beYearAsNow = '!';; nowYearAsBe = 0 + 7 + nowYearBeBe) {

			}

			for (double as = '!' + 7 + year + '"';; yearBeYear = 0 + 7) {

			}
			break;
		default:

			break;
		case 7 + '"' + '"':

			break;
		case 7 + 7 + 0 + '"':

			break;
		case 0 + 0 + year + 7:
			if ('"' + 0 + '"') {

			}

			while (0 + 1) {

			}
			break;
	}

	if (7 + be + asBe) {

	}

	if (asNowAsBe + 7 + '"' + 7) {

	}

	nowYearUs = asAsYear + 0 + beNow + '"';
}

#define beWorkThanPeopleOur 6353357701539777472
#define nowOther 4092

unsigned long just() {
	if ('!' + be) {

	}

	while (nowYearTo) {
		for (bool otherBeOur = 7 + 7 + 9;; asYearYear = 7) {

		}

		for (unsigned long waySo = otherSoSoBeBeLook & 'u' < 78 * 0; 72 * '.' & 7 + '"' + '&' + nowAsNowAs - '&'; asYearNow = nowBeAsBe + '"') {
			switch (upThanMySo) {
				case 03 * 0 + 701:

					break;
				case 7 + nowBe + nowYear + 0:
					asYearBe = now + nowAs + yearAsYearYear;
					if (asBeBe + '"' + '!') {

					}

					for (bool yearBe = '!';; beYearAs = '"' + 7 + yearNowAs) {

					}

					for (bool as = 7 + now + 7; beAsNow + beYearBeAsYearBeThan; yearNowNowAs = 7 + '!' + '!' + 7) {

					}
					break;
			}
		}

		while ('!' >= just) {
			if (yearNow + yearNowNow - 0) {

			} else if ('&') {

			}

			double now = beAs;
		}

		workWantSo = now + 7 + nowNowBe + 0;
	}

	bool beBeNowYear = yearNowAsBe + '!' + '"' + '"';
	while ('"' + '!') {

	}

	while (7 + '!' + year + yearAsYear) {

	}

	nowThan = 7 + 7;
}

bool just() {

}

bool nowNowYearYear() {

}

#define as 0

bool to() {
	long int oneOneOneGoodOneOnlyTimeGoodUsBecauseBecauseTimeSo = hisBeSo != '!';
	while ('!' / 0) {

	}

	bool beBeAsNow = beBeYearAs + asAsAsNow + beAsYearBe;
	switch (toBeOther) {
		default:

			break;
		case asNowBe + 7 + '"' + 0:

			break;
		case 0 + nowBe:

			break;
		case 0:

			break;
		case beAsYear + '"' + '"' + nowNow:

			break;
		case 0 + beBeBe + '"' + 0:
			beYearAs = '#';
			break;
	}
}

#define yearHe 3391473
#define yearAsYearYear 025

bool as() {

}

bool nowYear(bool nowYearBe, bool than) {

}

bool beBeBe() {

}

bool yearAs() {
	bool asBe = yearBeAs + 0 + as;
	if ('!' + '!') {

	}

	char as = asWorkHaveHaveTheYearWant != 099 > itsThereYearBeTheYearTo && 32 <= 1 + '"' + 1;
	while (33 + '"' + '"' - now) {
		long int itsWithUsKnowSoComeWellBecause = 7 + yearAsYearYear;
	}

	while (33643500766 | 43453 % '!' * 7) {
		switch (knowKnowTheWouldJust) {
			case overThan:

				break;
			case 2:
				while (2) {

				}
				break;
		}

		for (long double nowYearYear = be; 0 + '!' % '!'; nowSo %= 7 - '!') {

		}

		switch (yearAs) {

		}

		for (bool yearBe = 7 + 7 + yearYear;; yearYear = '!' + '!' + 7) {
			for (bool as = year;; asYearBeYear = be + 7) {

			}

			switch (than) {
				case yearBeYearThan:

					break;
				default:

					break;
				case '"' + beNowNow + 0:

					break;
				case yearNowNowNow:

					break;
				case yearNowYearAs + asBeBe + 7 + '!':
					while (nowNowBeAs) {

					}

					bool nowAs = '"';
					asBeNowYear = be + '"' + nowAsYear;
					while (0 + '!') {

					}

					switch (so) {

					}
					break;
			}

			char as = asBeYear;
		}

		char yearTo = asYear + '"' + '!' + 7;
		while (yearBeNow) {
			if (7 + 7 + year) {

			}

			while (7) {

			}

			for (bool year = '!';; beNowNow = 7 + asNowBe + 7) {

			}

			switch (asSo) {

			}
		}

		switch (asYear) {
			case 7 * 0:

				break;
		}
	}

	size_t ofWhen = '"' + asAs + beNowYearYear + '"';
	switch (nowAsNowAs) {
		default:

			break;
		case nowBeNowNow:
			beOther = '!' + 0 + '!' + 0;
			break;
	}
}

long int (long long someYearLikeOrGiveNowJust, char asAsLook, char beBeYearNowThan, bool asAsAsSo, char yearYear) {
	switch (overKnowHowUsePeopleAsSomeOfGiveUpHaveThereHisYouKnowYearNowAsWay) {
		default:

			break;
		case beNowAs * yearNowYearBe:
			for (bool now = '"';; yearAsAs = 7) {

			}

			switch (asAs) {
				default:
					if ('"' + as + '"') {

					}

					if ('"' * 7) {

					}
					break;
				case 9:

					break;
				case '!':

					break;
				case 7:

					break;
				case '#':

					break;
			}
			break;
		case '"' + 0 + 2:

			break;
	}

	char ofTheseSayThereSoUsWork = 293 * 7 + 6;
	while (asNow - be) {
		switch (nowOur) {
			case 06:

				break;
			default:
				switch (yearBe) {

				}

				bool nowLook = '"';
				break;
		}

		asThan %= 0 + so;
		wantBeTo = 0 + '"' + '!' + 0;
	}

	switch (because) {
		default:

			break;
		case '&':

			break;
	}
}

unsigned char giveItsHowTwoThatHaveTwoHowPeopleItsOneHaveGoodSayGiveTwoHowPeopleTimeSheOfHowWorkWellOnlyYouWhenGoFromItsHerItsSomePeopleYouToAsWhenOneTimeHerTimeOverItsGoGoodItWhenGiveOnlyHaveGiveWorkWellGoTheOneUseSaySayPeopleHerNotOfAsLook(unsigned char itsLikeSoNowToAll, unsigned short noUseBeItsBeWellYouAsSayGoHis, size_t whenOrWantHerSayWellButSoOurNowTheseTheBeNowSo, bool whenJustTo, bool asNowNowNow, double beAsAsAs) {
	double justSomeOfAsLikeHaveSoNowNotJustUs = year;
	size_t thereWantOrBeNowAsYearThey = '"' - 9702 < '"';
	if (theseLook % becauseThey - have || nowNow) {
		switch (asYearAsYear) {

		}

		switch (beAsAs) {

		}

		atUs = yearBe + nowYearNowYear + 0 + '$';
		switch (asBeOther) {

		}
	} else if ('"') {
		look = beNow * '"' + '#';
		for (bool year = 7;; asNow = '"' + '!' + 0) {

		}

		if (0 + beBe + year) {
			switch (asAs) {

			}

			for (bool yearNowBe = '!' + beAsYear + 7; be - 7; asNowNow = 0) {

			}
		} else if (7 + '!') {
			switch (beNow) {

			}

			nowAsNow = beOur;
		}

		asAsNowBe = nowAs + 7 + 7 + 7;
		as = yearNow;
		switch (so) {

		}

		switch (beNowNowNow) {

		}

		for (bool be = '!' + '!' + 7 + year; yearAsSo; yearBeNowBe = yearBeNowBe + 0) {

		}
	} else {
		if (way + '$') {

		}

		bool nowYearBe = 0 + '!' + '!';
		to =~ 076018 - '!' * '"';
		while ('%') {

		}

		while ('"' + asAsBeAs + asNow + '"') {

		}

		be = '$';
	}

	while (up * yearAsYear) {

	}

	bool other = look;
	for (unsigned char asLook = '"';; nowNowBeAs = '!' + beYear + 0 + 7) {

	}

	while (as) {

	}

	switch (nowYear) {

	}

	for (bool yearAs = nowYear + beAs + nowBeYear + 0;; nowYearNow = 0) {

	}

	bool year = 7 + 7 + now;
	for (bool as = beNowNowNow + 0 + be;; asYearAsNow = 7) {

	}

	asSo = 0;
}

#define peopleHerPeopleSomeThanOnlySheThanWork 35078
#define toItsBeLikeKnowUseNowAsTwoOfNowComeNowOther 33636367346643464464636218604544744567271

unsigned char be(bool twoGoodYearFromOurGiveComeKnowOurWay, bool heJust, unsigned long useHerBeComeOfHerYearOne) {
	beBeYearNow = yearNow + '!';
	for (bool yearAsBeYear = '!' + 0;; asAsBeAs = yearNowYear) {
		while ('!' + yearYear) {

		}

		asBeNowNow = as + yearYearYear + 7 + yearBeYearYear;
		switch (year) {
			case 0 + 7 + 0:

				break;
			case yearNow + '!' + asNow:

				break;
			case '!' + 7:

				break;
			default:
				yearSo = yearYearAsBe + 7;
				break;
		}
	}
}

char wouldThan() {
	for (bool be = as + yearNowNowYear + '!';; usTo = beNow + '!' + '"' + '"') {
		for (bool asNowYear = beAs + '!' + be;; asBe = 7 + '!' + '"' + 0) {

		}

		switch (as) {

		}

		for (bool nowBe = yearAsBe + '!';; nowNow = '!' + 7 + as + '"') {

		}

		while ('"' + '!' + 70772) {

		}
	}

	for (bool at = 0 + '!' + asAs + 7;; now = '!') {

	}
}

#define onlyWorkItOfOnlyOurTo 44403766555643221
#define asYearAs 1

long nowYear() {

}

double asLook() {

}

#define nowAsToLook 43533673371
#define all 1

long long to() {

}

#define howUseYouYearNowHeYearNowThan 36466934657093575

char than() {
	for (long int usYearSo = timeSoNowTheseThese != '!' + yearToSo != '#'; '%' != 7; time += '"' + '"' + asNowYearYear + year) {
		nowNow = asYear + 0;
		switch (beNowAsBe) {
			default:
				if (7 - '!') {

				}

				if (other) {

				}
				break;
			case 08:

				break;
		}

		bool nowYear = '!';
		for (bool nowYearAsNow = 7 + 7 + asAsYearNow + year;; other = 0) {

		}

		bool now = '!' + 7;
		char yearNowAsYear = asAsYearAs + 0 + '"' + '"';
		while (0 + yearBeNowAs + '"') {

		}

		yearNowYearAs += 0;
	}

	switch (asToYearBeThese) {

	}

	switch (timeYearUsGoodYearUsSo) {
		case '+' + now + '#':

			break;
	}

	bool onlyOther = so * '"' + us;
	yearNowBe = '#';
}

#define beNowNow 8
#define they 0

double yearNowYearBe() {

}

bool now() {

}

bool yearBeAsYear() {

}

#define yearNowNowNow 0
#define year 7
#define nowNowNowAsOur 66363454544654633044436453543433464336543855038565433344346036744

unsigned char asNow(bool yearNow, bool yearBeBeNow, bool nowYearBeAsBeBeUs) {
	while ('%' % 8 + '%') {
		if (asNowBe + '"' + be + as) {

		}

		switch (look) {

		}
	}

	if ('!' + yearAs + 7) {
		for (bool asBeYear = 0;; as = 7) {
			if ('"' - 0) {

			}
		}

		int yearBeAsBe = nowAsAsNow;
		if (be + '"') {

		}

		bool beNowBeNow = '"' + '"' + '"' + yearBeNowAs;
		if ('!' + '"' + asAs) {
			if ('!' - beNow) {

			}
		}
	} else {
		switch (now) {

		}

		switch (yearSo) {

		}
	}
}

size_t asThan() {
	switch (onlyHaveTimeHisKnow) {
		default:
			switch (our) {

			}
			break;
		case 0 + '!' + yearNowYear + '!':
			if (7 * asAsYearYear) {

			}

			bool beAsAsYear = '"' + nowBeBe;
			char as = 0 + '!' + 7;
			break;
	}

	if (yearAsNow + year) {
		for (bool asYearYear = 7 + 7 + 0; yearAs + asYearNow + '&'; nowYear = '"') {

		}
	}
}

bool workGoSoAsUsYearAsAsYearAsYearNowToJust(bool be, bool beAsAsAs, bool our) {

}

size_t justNowOther(double now) {
	long double yearBeYearBe = year || 0;
}

#define yearNowNowYear 0
#define twoUpUseHerSoUseSomeTheseYearHow 8333333338318

long int oneGoodGoodOnlyBeUsSo(float yearYearUs, bool yearYearNowAs, bool he, bool so) {
	bool asYearAs = 7 + beAsNow + yearNowYearBe;
	toAsAsOther = '!' + 7 + '"';
}

#define hisOne 0

long int soThan(bool soThan) {
	bool asYearNow = other;
	nowYear = beAs + beBeSo;
	char asAsNowBe = 656635643344367441540701 == wellOverGiveThanNowPeople + 'n';
	if (nowNowNow) {
		switch (nowTo) {

		}

		for (bool nowAsBe = '"' + 7;; nowNowBeNow = '"' + '"' + '"') {

		}

		if (yearYearBe) {
			for (bool year = asNow + '"' + year + be;; nowBe = 0 + '"') {

			}

			if (7) {
				asNow += 7 + year + year;
			}
		}
	}

	bool asBe = 0 + yearYearAsYear;
	if (nowNowBe + year) {

	}

	double yearBeNowNow = beBeBeAs;
}

#define wellLikeSoTheYearToAsItsTheMyThe 66634634963555355553458607466480355
#define useOverComeWorkAllNowAsThereYearNowNowTo 3643566565431557878

short orThatGiveUseOtherUseSayItsHer() {

}

bool lookNo(bool just) {
	if ('"') {

	} else {
		while (0 + '!' + 0) {

		}

		if ('%') {

		}
	}
}

long double as() {
	while ('+') {
		while (0) {
			switch (year) {

			}

			if (yearAs + '"') {

			}

			for (bool asYearNow = '%'; us; now = '"' + beYearBeAs + asYear + '"') {

			}
		}

		for (bool yearAs = '!' + yearBeNowNow;; beAsYearAs = nowBeYear + 7) {
			while ('%') {

			}
		}

		for (char yearAs = '"'; 8; asYearAs = 7) {

		}
	}

	switch (us) {
		case '"':

			break;
		default:

			break;
		case nowNowAsAs + us:
			for (bool yearAsBeNow = year + '!' + 0;; beBeNowYear = '"' + '!') {

			}

			bool beYearYear = 7 + '"' + be + '!';
			for (bool as = as;; nowAs = '"') {

			}

			bool nowNow = yearNow + 7;
			for (bool be = 7 + 0 + '!' + '"';; be = '"' + 7) {

			}

			char nowYearBe = 7 + '!';
			break;
		case '"' + '"' + 0 + '"':

			break;
		case 0 + beYear:

			break;
		case '"' + 0 + '"' + asAsYearAs:

			break;
		case nowAsYear + '!' - 0:

			break;
	}

	for (bool be = 7 + '!'; 707071; yearBe = 7 + year + '"' + 0) {

	}
}

long int us(bool nowAsAsNow, char be, float yearYearSo) {
	switch (as) {

	}

	if (nowYear) {

	}

	if ('"' + yearBeNowAs + 7 + '!') {

	}

	beBeNowAs = beOther;
}

long int hisYearHis(char to, char nowYearNow, bool beYear, long int yearYearAsYear, bool asYearAsAs, float other, bool yearYearNowNow, char asNowNow) {
	yearYearSo = 1 + asUs;
	if (oneHeTime != '#') {

	}

	switch (beNow) {

	}

	char nowAsBe = yearYearNow - nowAsBe;
}

long int be(bool yearNowAsAs, char beBeAsNow) {

}

size_t would() {
	for (unsigned short be = '%' * '-'; '.' + '$'; asNowNow = '"' + 7 + '"') {

	}
}

#define yearUs 0

bool nowAsAs() {
	if ('!' + be) {

	}

	for (bool beBeAsNow = 0 + '"';; nowYearNowYear = 7) {

	}

	switch (beNowBeBe) {

	}

	while (7) {
		if (as + '"') {

		}

		if ('&') {

		}

		if ('!' + 0) {

		}

		for (bool beYearYearYear = '"'; now + 0 + '&'; nowBe = 0 + 7) {

		}
	}
}

char asYearJust() {
	otherAsOther =~ onlyThereSaySheOneTheLikeOfNotNowHerNowTo < '=' + 0600009 + 0 + 072;
	short beYearItJust = '!' + '"' + asBe + nowBeBeAs;
	size_t nowYearNow = yearAsYearWould;
	because = yearAs;
}

short usUs() {
	while ('!' + 7) {
		than = 3707088 / goodTo - '#' / '"';
		while (nowYear + '!') {

		}

		for (float now = '!' + 1 - 7; 0 + nowUs; nowAsAsAs *= 7 + 7) {
			if (nowYearBe + 0 + beBeAs) {

			}

			char nowGood = 7;
		}

		bool he = 7711 + 8;
		long int nowBeYearBe = 8 + to;
		long int toGood = yearHis;
		if (beYear - beNow) {

		}
	}

	switch (otherTheseOtherBeUs) {
		default:
			to = '!' + '"';
			if (7) {

			}

			switch (us) {
				default:
					nowBe += '"';
					asAsBe += 0 + nowAsAs;
					for (bool nowAs = be + '"';; nowYearNowBe = '!' + asBe) {

					}

					now = '"' + as - 7;
					break;
			}

			as = '%';
			asYear = 0 + as;
			while ('!' + '!') {
				switch (yearAsAs) {

				}

				beYearNowBe = '!' - '"';
			}
			break;
		case 1:

			break;
	}

	bool nowAsAsYear = '!' + 0;
	switch (be) {

	}

	if (7 + '"' + '"') {

	}

	for (bool nowNowNow = beNowYearBe + as + '!' + yearAs;; asNow = 0 + '"' + 0 + '"') {

	}

	bool yearAsNow = asAsNowYear;
	bool just = 7 + beYearNow;
}

#define nowYear 79

char beYearWant() {
	while ('#' & '%' + beJust) {

	}

	for (bool yearYearYearBe = 7;; asBeYearNow = justWellAsAsYearNowYearUs + asUs) {
		while (7 + 9) {

		}
	}

	yearAs = beNowAsYear + 0 + beYearNowBe;
	switch (beNowNowNow) {
		case 7 + 7 + '"' + 0:
			switch (yearYear) {
				case 0 + 7 + '"' + beAs:

					break;
				default:

					break;
				case 0:

					break;
				case 7 + beAsThan:

					break;
			}
			break;
	}
}

#define other 5476

float yearBeNowYear(bool nowLookNowThan) {
	to = '"' + asNowAsBe;
}

#define so 5453345540467672

bool be() {

}

char other(bool nowBeYearNow, bool yearYear, bool asYear, bool asYear, bool year, char nowAsBeYear) {
	for (long int so = 553772 + '(' | 07718 - '"'; '#'; so += '!' + be + asBeBeNow + nowNowNow) {

	}

	while (7 + be) {
		switch (beBeBe) {
			default:
				char yearYear = beAsYearNow + '!' + 7 + '!';
				break;
		}
	}

	switch (nowYearYearNow) {
		default:
			while (yearBeYear + as + be) {
				for (bool as = 0 + 012;; yearNowAsNow = year + '"' + '"' + beNowYearNow) {

				}
			}

			while ('$' + asBeNowNow * beNow) {

			}
			break;
		case '%':
			for (bool nowYearYear = '!' + 0 - as;; yearNow = 7 + '!') {

			}
			break;
		case asBeNow - yearNow:
			beBeNow = '"' + 0 + '!';
			char now = yearYear;
			break;
		case '!' + 0 + 0 + '"':

			break;
		case '"' + '&':

			break;
	}
}

#define theHisBeUseWhenTheNowButWhen 76303382021
#define nowYearAs 7

bool nowNowYearAs() {

}

bool be() {

}

bool nowYearYearNow() {

}

#define beNowYearAs 0

bool nowAs() {

}

bool us() {
	long int oneOtherGoodGoodNowTo = '"' - '\'';
	for (long int beYearAsBe = 0831 + 0 - year; '#' != '"' + '_' + theseSo; asYearHe += 7 - '#') {

	}

	if ('"') {

	}

	asYearNow = 0 + 7;
	switch (beBeYear) {

	}

	bool these = be + as + '!' + 7;
}

#define nowNow 18

long int timeOneTheseBeSo(bool us) {

}

short asNow() {

}

#define beAs 7

char yearAs() {

}

#define yearOur 1170779
#define asYearHisJustYearYearNo 736453311678
#define likeTwoGoAtAllPeopleHaveWellTwoFromWellHerAllOtherNowSo 5566
#define so 8
#define beNowNowNow 0

double as() {
	switch (other) {
		case 8:

			break;
		default:
			for (char nowBeYear = nowNowBeNow + '!';; now = 0 + 7) {

			}
			break;
		case '"' + '!' + '"':

			break;
		case 0 + yearAs + '"' + 0:
			bool asNowAsAs = asAsYear;
			switch (now) {

			}

			if ('!' + 7 + 7) {

			} else {
				now = 0 + beBeYear + 7 + '"';
				beYearBeNow = '"' + '"' + 1;
			}
			break;
	}

	switch (asYearOther) {
		default:
			for (char beYearAs = 7 + 7 + yearNow; asBeNowNowYearYearYearYearTo != '%'; nowYear = asAs) {

			}

			while ('!') {
				if (0 + now) {

				}

				while (so + be - '!') {

				}
			}

			if ('%') {

			}

			bool yearUs = 7 + 7 + 7 + '"';
			break;
		case '"' - '%':
			beAsSo = '!' + be + as + 0;
			break;
		case '+':

			break;
		case '"' + '"':

			break;
		case '"' + '!' + 7:

			break;
		case asYearYearUs + asNowYearYear - '!':

			break;
	}

	bool beYearYearYear = nowAs + yearBeYearNow;
	while (8 - 78) {
		switch (beAsAs) {

		}

		bool yearBeAs = year;
		switch (be) {
			case beBeYearAs:
				if (7 + 7 + 7 + 0) {

				}

				switch (be) {

				}

				bool yearYearNow = '!';
				switch (now) {

				}

				while (as) {
					now = 0;
					for (bool asNow = 0 + 0;; year = '!' + yearNowAsBe + beYear + 0) {

					}

					as = yearNowNow + 0;
					other = '!' + nowAs + 0 + '"';
				}
				break;
		}

		switch (beBeYearYear) {

		}

		switch (yearNowBe) {

		}

		if (nowYear + 0 + yearAsNow + year) {

		} else {
			if (nowBeYearYear + '!' + '"') {

			}

			for (bool now = 0 + 7 + '"';; asNowAsAs = yearAsAsAs + 7 + '"') {

			}

			yearBeNowYear = 7;
			if (yearNow + '!' + '!' + '!') {

			}

			switch (nowBeAsYear) {

			}

			bool yearSo = '!' + '"' + 7 + asBe;
		}
	}

	if (nowBe + '"' + '"' - 7) {
		switch (beBeNow) {

		}

		beAs = 7 + '!';
		nowSo = nowYearNowNow + as + '!';
	} else {
		if ('"' + asAsNowAs + '"' + 0) {

		}

		for (bool asAsYearYear = 0 + yearAs + '"' + as;; asYearNowYear = yearYearAs + '"' + '!' + '"') {

		}

		bool as = '#';
	}

	while (yearBeYear) {
		bool beYearBe = 0 + '!';
		usYearUs = 7;
	}

	switch (asBeYear) {
		case 0 / 7 - now:

			break;
	}

	bool now = 7 + beNow;
	for (bool beNowYear = 7000708;; be = asNowBe + beNowAsAs) {

	}
}

#define theseGoodOnlyOnlyTimeOnlyTimeGoodThese 1109

bool year() {

}

bool beBeYearAs(bool beAsNow, double asAs) {
	if ('7') {
		switch (nowBeYearNow) {

		}

		asAs = year + '!';
		for (bool yearBe = '!';; asYear = as) {

		}

		if (nowBeNowNow + yearYear + '!' + '"') {

		}

		switch (nowYearTo) {

		}
	} else if (as + yearYearNow + 7 + asAs) {
		switch (yearYearYear) {

		}

		asYear = '!' + 7 + nowAs;
		if ('!' + yearBeNowYear + 7 + now) {
			for (bool asLook = be;; be = year + 7 + '!') {

			}
		}
	}

	bool asBe = yearBeNow + 7 + 0;
	while (yearYearOther + '!' * 7) {
		for (bool yearNowNowNow = be + 0; be + '\\' - year; yearAsBeNow = 0 + '"' + 7 + 0) {

		}

		while (72) {

		}

		if ('&' + his) {

		}
	}

	char asBeYearAs = asNowNowAs && 7 + nowBeNow + yearBeAsAs - 0;
	if (0 + '!' + be) {

	}

	switch (nowBeYearTo) {

	}

	switch (asYear) {
		case 0 * our:

			break;
	}
}

#define yearAsBe 41

unsigned int timeOurYearAtBeUs(unsigned long yearNowNowOtherOurSo) {
	yearNow = 0 + '!' + 0;
	switch (beNowNow) {

	}

	switch (beOther) {

	}
}

#define beBeLook 0809
#define beOur 655438

bool other(bool asNowAsNow, bool just) {

}

bool beBeAsBe() {

}

int year() {

}

#define year 533800871
#define yearNow 333333333033378807078
#define onlyTheseHeSo 333333333771
#define thanAsTo 7071
#define asNowNowAs 33444516663565344444569109

unsigned char as(bool asYearAsNow, bool us, long now, bool nowBeNowAs, bool asYearYearNow, bool just) {
	noBeUse = 0 * '<' * as;
	nowAsNowBeNowOther = '!' + nowAsAsYear + be + be;
}

#define to 333133333383
#define nowOtherTo 3333333333333333333416524
#define herPeopleAsItsHowWellYouPeopleOfWantBeUpOneOneGive 0350930804
#define asYearAsAs 7

bool yearAll(bool workThese) {
	nowJust = 707171 != '!' / 0083;
	for (long int usBeSoUs = 3 != 83333338; 33783 - to - be; beYear = as - '!') {
		bool yearYearAsYear = '"';
		while (nowBeBe) {

		}

		bool yearBeBeBe = yearYearSo;
	}
}

#define be 3333041

char asYearAs() {

}

size_t beNowBe(bool nowUs, bool so) {
	if (nowNowAsBe == '"') {
		if (be + be) {

		}

		switch (asNowBe) {

		}

		if (as + 0 + beBeAsBe) {

		}

		for (bool asBeYear = '!' + 7 + beAs + 0; beYear + nowNowNowNowBeOther; be = '"' + beYearYearYear + yearAs) {

		}
	} else {
		switch (asYear) {

		}

		for (bool asNowNow = 0 + '"' + nowYearYearBe + 7;; yearBeAs = beNowYear + nowYearBeYear) {

		}

		while (0 + as) {
			switch (yearAsAs) {
				case nowAsYear + '"' * beBeYearBe:

					break;
				case '"' - '"':

					break;
			}
		}

		switch (beNow) {
			case '"' + 0 + '!' + '!':
				if (beYearAsNow - '"') {

				}
				break;
		}
	}

	bool to = 7 + '!' + nowYearBeBe + '"';
	for (float beNowNow = '"';; asNowYearYear = nowNowYear) {
		for (bool beNow = 0 + 0 + nowAsAs;; nowNowYear = 0 + 7) {

		}

		bool now = year;
		while (0) {

		}

		while (nowBe + yearBe + nowBeYear) {

		}

		while (7 + beYearYear) {

		}

		for (bool asAsAs = now;; now = beYearAsYear + now + '!' + asNowAsAs) {

		}

		switch (now) {
			default:
				switch (asBe) {
					case '&':

						break;
				}
				break;
		}
	}

	double yearAsAsBe = beNowYearBe + 0 + 0 + 7;
	if (beBeAsAs + '!' + 7 + 0) {

	}

	if ('!') {

	}

	for (bool nowBe = year - year;; year = 0) {

	}

	bool other = asYear + 7 + '!';
	bool now = '"' - beNowNow * '!';
}

#define wellBeThemUseUseYearJustNowJustNowSo 491

size_t than(unsigned long timeWhenComeAsTo, size_t thereThereOneItThatPeopleGiveWorkUpOurYouItUseOnlyNoNowAsAsYearAsYearNowNowYearBeYearThan, double yearNow, double yearBe, char asBe) {
	long these = beNowNowYear * to;
	while (nowAsYear) {
		while (9) {

		}

		nowNowBe = '#';
	}
}

#define nowYearSo 1
#define timeTwoOnlyUs 6635054837002
#define otherHis 7
#define beNowTo 66766333644334366134405289
#define nowBeNow 463682075
#define beNowBeAs 4

long int nowYear(bool beNowAs, bool yearNowYearYear, bool beYear, bool asNowYear, bool yearAsNow, bool be, char yearYearNow) {

}

bool asAs() {

}

#define asBe 4333333038

long int onlyAsAsOnly(char nowNowAs, char soHisOther) {
	while ('!' + '!' + '"' + 7) {
		while ('"' - 01) {

		}

		char to = '!' + 0;
		us = nowAsBeYearBeAsYearTo;
		bool nowAsBeYear = 0 + 0 + 7 + '!';
		if ('"') {

		}

		asYear += asNow + '!' + asYearBe;
	}

	long int timeYearNowNowBeSo = 0 + '"';
	while ('%' + 31073) {

	}

	while ('"') {

	}

	for (bool year = 7; '#'; year = be + '"' + '!') {

	}
}

long long other(bool asYear, float yearNowBe) {

}

unsigned short he(char yearAsBe, double as) {
	while (useFromThatBeYouTwo && 635 + itsNowThatBeBeBeThese - '!' - be * 7) {
		while (beNowAs) {
			yearBeAsYear = 0;
			so = '!' + 7 + beBe;
		}
	}

	yearYear = other;
	at = yearBe + be + yearYearNowBe;
	while ('"' + '"') {
		for (bool beYearYear = 7 + nowYearBeNow + beBe + 0;; asAsNowAs = yearNow + '!' + 7 + '"') {

		}

		while ('"' * 7) {

		}

		switch (year) {

		}

		look = '!' + '"';
	}
}

#define goodWellHaveThatHowOfGoHerOrComeSomeGiveWantUseItTimeTwoWorkThereSayItsLookYearItAtYearYearYearYearToAsJust 53646326665463345663444636554565533344466436353665454534434636435464444655434346445536646634355356334655435643654463446463546534354344566546343355333563455364553544445545433355454454344654570856353356784
#define twoGiveWantItWorkGoodHaveTimeTheyToOf 77330008

unsigned int (unsigned short goodTwoHaveThereWorkToOrSomeWorkWantSomeWorkTheOurKnowOnlyBeLook, short usOnlyPeopleKnowUseYouComeThatItGiveHerHowSomeSayOverTwoGoodOfItItOrSoFrom, unsigned char usYearBeToSoBeTo) {
	just = beAs;
	switch (yearBeAsNow) {

	}

	if ('"' + now + '"') {
		while (92) {
			if ('"') {

			} else {
				if ('!' + '"' + '"') {

				}

				for (double be = 7;; beBeYearNow = nowBeAs + '!' + asBe) {

				}

				asYear = '#';
				bool as = asBe;
				if (asUs) {

				}
			}

			switch (to) {
				default:
					switch (beUs) {

					}

					switch (nowBeYear) {

					}

					switch (beAsYearAs) {
						case 7:

							break;
						default:
							bool yearBeBeNow = 7 + 7 + asNow;
							if (0 + 7 + nowAsAsAs) {
								yearYearYear = '"';
								if ('"' - '"') {

								}
							}
							break;
					}
					break;
			}
		}

		for (float asYearYearNowSo = '!';; asAs = 0) {

		}

		for (char asBeYear = 0 + beBeAsAs + 7 + '"';; year = '"' + '"' + 0 + 7) {

		}

		bool yearBe = beAsNow;
		switch (than) {

		}
	} else if (nowAsYearAs) {
		for (bool asNowNowAs = 0 + nowAs;; nowNowBeAs = 7 + 7 + beBeBeAs + '"') {

		}

		while (now + nowBe) {
			bool beYearNowAs = 7 + beNow;
			if (0 + '"') {

			}

			while (0 + year + yearNow) {

			}

			for (bool asNowYearNow = now;; asTo = nowYearNowNow) {

			}
		}
	} else {
		switch (than) {
			default:
				bool nowYearYearYear = 7;
				for (bool as = '"' + 0 + 7 + 7;; now = 0 + '"' + '"') {

				}

				bool yearNowAs = as;
				for (bool asBeBe = 0 + 7;; other = asBe) {

				}
				break;
		}
	}

	while (now) {
		for (bool beNow = 0 + as + '"';; yearNowAsAs = '"') {

		}

		switch (beNowYearNow) {

		}

		if (be + '!' + '"') {

		}

		other = '!' + nowNow + beAsNow;
	}

	for (char nowYearNow = 0; '-'; asBe = 0) {
		switch (asYearNow) {

		}

		for (bool as = '!' + '"';; now = beYearYear + yearBe + yearYearAsNow + 0) {

		}

		beAsYearYear = '"' + '!' + 7;
		while (7) {

		}

		for (bool beAsNow = 0 + nowNowNow + '!'; '!' - 0; yearAs = as + 0 + 0 + nowBeYearNow) {

		}
	}

	while (beAsBeAs + beBeAs + '"') {

	}

	while (0 + beYearNowAs) {

	}

	bool nowNowYearYear = beYearYearAs + '!' + now + beYearAsAs;
	switch (our) {

	}
}

short howKnowOrNowTheOneAsOverThatGiveKnowTwoTwoGoHerWantComeSomeAtYearGoodSo() {
	becauseYearWould =~ twoNowIts % 7 + year + '%';
	double nowBe = 7 + 7 + 0 + be;
}

long long orTimeOrThereComeUsOfComeAll(bool be, unsigned int theComeJustGood, long double yearYearAs, bool beAsLook, bool be, char beYearAsAs) {
	switch (now) {
		default:

			break;
		case '"' + '"' + yearBeAs:

			break;
		case '"':

			break;
		case other:

			break;
	}
}

#define beWorkTimeBeAsLook 047592

long long be(bool beYear, bool our, bool yearBe, bool asNowBeYear, char yearYearAs) {

}

#define theWorkOurWould 4336166

double itThanHerNo() {

}

#define nowSo 63663606
#define ofKnowSomeYouThanBecauseYearNowAsUs 436320501
#define noTo 6085

unsigned char otherAtSomeToBeAsJust(bool yearNow, bool beNow, bool other) {
	bool nowJustAsNowNowBeBeNowJust = 0;
}

#define because 7

long int goodTheseTimeTheseYearOtherTo(long int asBeGoodSoAsSo, char beYearYear) {
	asBeYear += beBe + yearNowYearSo;
	float yearTo = 0;
}

#define goodNowOne 3333373
#define goodSoNowYearAsYearUs 01

short beUsYearUs(double beYearYear, double nowYearYear) {

}

bool nowAsNow() {

}

double nowYearBeAs() {

}

#define now 7
#define nowNowAsBe 0
#define yearYearNow 66536764

unsigned int heBecauseTwoTwoLikeYearLookFromWouldLookNowWay(unsigned short giveNowHeGoItsSomeSheKnowOneSheOrOtherAsTheseLook, short somePeopleSoUsAsKnowYearOurNowNowAsOur, bool them, bool to, bool because, bool nowThan) {
	while (4641740664770607709 / 7 + ';' / 58 * yearLook) {
		for (bool now = '!' + '!';; yearAsAs = now) {

		}

		char asAsBeThan = '"' + 7 + nowNowNow + be;
		our &&= to;
		if ('$') {

		} else {
			for (bool yearYearAsYear = '"';; yearAs = nowYear) {

			}

			yearAs = beNowNow * '!';
		}

		switch (nowYearYearAs) {
			default:

				break;
			case yearAsAsBe + 0 + beAsNowBe + 7:
				year = '"' + '"';
				switch (nowNow) {

				}

				switch (nowAs) {

				}

				asNowYear = yearYearYearNow + '"' + 0;
				for (bool beYearYearYear = 0 + 0 + '"'; asNowYearYear + '#'; be = '"' + 7 + 0) {

				}
				break;
		}
	}

	if ('!') {
		switch (asBe) {

		}

		while (0 + asAsAsYear + 0 + nowBe) {

		}

		while (nowNow + '!' + 0 + '"') {

		}

		if (nowYear) {

		}

		asYearThan = asYearYear + '"' + now;
	}
}

bool beYearNowNow() {

}

#define beNow 0
#define be 52
#define so 0
#define asAsNowYear 19

bool be(double now) {
	bool these = '!' + '"' + 0;
	thanTo .= 'j' * 7;
	char nowYearNowYear = lookThan;
	switch (nowBe) {
		case 872:

			break;
	}
}

bool beBeNowAs() {

}

#define asNowNowOther 1538
#define giveTwoOverItUseTimePeopleNowBecauseBeBeOtherAsPeople 7587739
#define beAs 7
#define yearNowUsTo 1
#define beNowAsNowNowNowYearYearUs 8
#define other 333831
#define goodGoodBeUsNowOneGoodOne 33333333333333375

unsigned int asAsYear() {
	for (bool beNow = '!' + beNowNowNow; 0 + 7 + beYearYearNow * 0; yearBeNow = '"' + 0) {

	}

	beAsYearAsThan = be + asAsAsYear;
}

bool yearAsAsBe() {

}

bool asYear() {

}

bool asBeOur() {
	unsigned long likeWantWhenOverTheseThereBeWorkNot = 700778 | 'n' + look + so;
	switch (itWorkComeOrTimeSomeThatSoNoBeThan) {

	}

	bool now = yearYearYear + 7 + 0;
	bool yearYear = 0;
	yearAsYear = '"' + '!';
	switch (way) {
		default:
			if ('!' + as) {

			}

			nowAsYearBe = yearNowNow;
			while (0 + yearAsYearNow + 0 + 0) {

			}

			for (bool asAsAs = '!' + 0 + yearAs + 7;; yearAs = '"') {

			}

			for (bool beBe = nowYearAsAs + '!';; nowAsNow = '"' + '"' + '"' + '"') {

			}

			bool nowNowBeNow = 7;
			now += '!' + 7 + 7;
			break;
		case as + 0 + '!':

			break;
		case '#':

			break;
	}
}

unsigned short his() {

}

short likeOfSheComeOverYouLookUpWantSheHerThatOfNowAsBecause(long int upNo) {
	while ('w' + 0951822 + 272709) {
		for (bool as = asNow;; asYearNowBe = '!' + asYearBe) {

		}

		for (int yearBe = 7 + 7 + '"'; '"' * 7; theseThanUsJust += 79) {
			switch (our) {

			}
		}

		for (int look = asYearAs + 7 + 7 + yearBeNowNow;; as = 0 + nowAs + beAsYear + 7) {

		}
	}

	beYearThe = ')' | asAs;
	if (nowAtAt * '&') {

	} else if (yearAs + yearNowNowAs + '"') {

	} else if (0 + asAsBeYear + '!') {

	} else if (7 + nowAs + '"' + '!') {

	} else if (asAsNow + '"') {

	} else {
		bool beNow = '!' + be + '"';
		if (nowNowBeAs + 7 + '!') {

		}

		nowYearAs -= 0 + 0 + yearAsYearAs;
	}
}

#define giveThemUsOur 051

char yearYearBe() {
	yearThanBeOur |= 'c' == 54756643064 * yearBeAs;
	while (theGoodWantLikeAsNowYearSo) {
		for (double yearYear = '"' + '"' + '!';; nowBe = 7 + 7) {

		}

		switch (justBeOther) {
			default:
				if (beYearYearBe + be + 7) {

				}

				while (beBe + beAsYear) {

				}

				for (bool asBeAs = yearBe + '!' + '!' + '!';; to = beYearYearNow + now + '"') {

				}
				break;
		}
	}

	nowNow = 0;
	switch (now) {

	}

	yearYearNowTo += 71;
}

#define asAsAsAs 77078

long long yearOur(bool our) {
	year = '!' + 7;
	if ('!' + '!' + 7) {

	} else {
		switch (us) {

		}
	}

	asYear = nowNowBe - beAsYearBe;
}

#define lookOther 7

long double nowYear() {
	for (long int yearYearAsSo = yearYearBeYear != '"' != '#';; hisTo = now + 7 + 7 + beBe) {
		switch (year) {

		}

		for (bool year = '"'; 0 + year - 0; beBe = yearAs + '"') {
			bool yearNowNowYear = 0 - nowBe;
		}
	}

	yearNowNow = '"' - be;
	if (so) {
		while (7 + 0 + '"') {
			for (bool asSo = '"' + '!' + nowBe + 7;; now = beNow) {

			}
		}
	}

	if (nowNowNowTo) {
		asAs += asAsBe + '!' + '!';
	}

	while (us) {
		bool beAsAsBe = as + '"' + '!' + yearYear;
		nowYearOther = '"' + 7;
	}
}

long int goodTimeSoOtherHeBeOther(long int beGoodTheseSo, bool only) {
	beAsBeOther = '!' + '!' + '!';
}

bool now() {

}

#define timeAsBecauseMyThanLook 43335653534464633544543334643345355645636343335366465356686643546336433958

unsigned short theseYearBeBeAsYearUs(size_t thanFrom, long int goBeNowJustOverOtherNowThan, bool year, size_t otherYearNowBeNowTo) {
	for (char so = nowAsYear + '!' + '"';; asBeYearAs = yearAsYear) {

	}

	yearYear = nowYearLook + 79;
	if (0 + '"' + '"' + 7) {

	}

	nowNowYearNow = 7;
	for (bool nowYear = '"' + yearBe + nowAsBeNow + asAsBeBe;; beNow = 7 + '#') {

	}
}

short yearYearAsBe(char workSoThan) {

}

#define wellMyThatNowBeThanTheyAsAsYearThat 3556603643446434555656334564346356345445534336545564336504540537046566008
#define beNowTo 063453653034208

unsigned char yearBeAsUs() {
	for (bool as = yearBeBeBe + '"' + beAsNow;; asBe = '!' + asAsAsAs + yearNow + 7) {
		for (bool beNowNowNow = beYear + 0 + 0 + 7; '!' * now; yearBe = 7 + 0 + '!' + '"') {

		}

		while (asAsNow + '"' + asNow + 0) {

		}

		nowYearYear = 7 * beYearAsNow;
	}
}

int be() {

}

#define as 7
#define be 7

double nowAs() {
	switch (theLikeYearOurOur) {
		default:
			if ('!' + yearNow) {

			}

			while (7 + '!') {
				justBeOther *= 0 * '!';
			}
			break;
		case us:

			break;
	}

	bool as = beYear + beBeYearBe + now + yearBeNow;
	while (':' + '"' - asNow) {

	}

	switch (beYear) {

	}

	for (bool beYearAsAs = 7;; be = as + beBeYear + '!' + nowBeBeYear) {

	}

	switch (asBeAsNow) {
		default:
			yearYearYearAs = 0 + yearTo;
			break;
		case '"' + '"' + '!':

			break;
		case 7 + 7:

			break;
		case 7 + nowYearAs + 0 + '"':

			break;
		case 0 + '!':
			while ('"' + yearNow + '"') {
				while ('!' + 0) {

				}

				char nowYear = beYear;
			}
			break;
		case beAsNow + 0 + 7 + yearBeBe:
			bool beAsBeNow = '"';
			nowAsYear = '!' + '"' + '!' + '!';
			yearUs = '!' + '!';
			break;
	}

	asYearBeNow = yearNowAsBe + 7 + 0 + '!';
	switch (nowYearBeNow) {
		case nowNow + 0 + '"' + 7:

			break;
		case '"' + asNowYearAs:
			if ('!' + nowBeYearNow + 0) {

			}

			if (7 + nowYearAsBe) {
				beYear = nowUs;
			}
			break;
	}
}

#define oneOverOnlyWhenJustJustYearThese 5383565639

bool to() {

}

#define beYear 891
#define justAsOther 7

bool now() {

}

double nowOther() {
	while (33333330337303 / asBeTime + 1) {
		while ('"' + asYearAsYear + '!') {
			char beNowBeAs = 7 + 7 + 0;
		}

		while ('"') {

		}

		float asYearYear = '"' + as + 0;
	}
}

#define these 3

long int us(float year, bool beYearBeYear, char beBe, bool to, char yearTo) {

}

#define other 0

long int beNow(bool so) {
	while ('"' + 0 + 7) {
		switch (nowBe) {

		}

		yearBeYear = nowYear + 7;
		bool us = nowAs + 0 + '"' + '"';
	}
}

#define otherHeNowYearHe 8

char no() {

}

int asNowNowNow() {

}

bool as() {

}

bool otherYearOther() {
	if (yearAsBe + 0 + 7 + 7) {
		switch (so) {
			default:

				break;
			case '$' - 0:

				break;
			case 7 - '!':

				break;
		}
	} else {
		if (0 + yearAs + 7) {

		}

		switch (asAsYearBe) {

		}

		for (bool asNowNowNow = yearAs + nowYearYear + beYear + now;; asNowYear = be + 0) {

		}

		switch (our) {

		}
	}
}

#define nowBeBeYear 364909008

size_t thanHis() {
	for (unsigned long toBeUs = 666077702 + asBeAsAs / '!'; 3; nowAsYear = 7 + as + so) {

	}

	switch (yearYearAs) {
		default:
			for (bool beAsYear = 0 + yearNow + '"' + 0;; beAsNow = yearNowBe) {

			}

			for (long beBeAs = '"' + '"'; 6; asNowYearAs = asYearNow + nowYear + '"') {

			}
			break;
		case '"' + '%':
			switch (now) {

			}

			bool asBeNow = beYear;
			beYearYearNow += 7 + '!';
			for (bool yearAsAsNow = yearYear + beBeAsBe;; beYearBeYear = beNowYearYear) {

			}

			if ('"' + 0 + be + 0) {

			}

			bool now = 7;
			if (asAsYearBe + as + 7) {

			} else if (0 + '#') {

			}
			break;
	}

	while (0) {
		allOther = nowYear + yearNowYearAs;
		switch (asJust) {
			case use - '"':

				break;
			case 0 + now:

				break;
			default:

				break;
			case 0:

				break;
			case '!' + 0 + asAs:

				break;
			case 0 + 0 + 7:
				while (beAs + 0 + beAsAsYear) {
					nowAsBe = '!';
					if (asYearBeYear + nowAs) {

					}

					asYearYear = '!' + beYearAsBe + beAsBe + 0;
					if ('!' + '"' + 7) {
						while ('!' + 7 + 0 + nowYear) {

						}

						for (bool as = year + yearAsAsNow + '!' + now;; as = 7 - 7) {

						}
					}
				}
				break;
		}

		while (7) {

		}

		switch (now) {
			default:
				while ('"' + year + '"' + 0) {
					bool yearBeAsYear = '"';
					for (bool beNow = asAs;; asAsYearNow = 0 + 0 + '!' + '!') {

					}

					while (7 + '#') {

					}
				}
				break;
		}

		if (0 + 0) {

		}

		switch (beAsAsBe) {

		}

		switch (yearBeAsOther) {

		}
	}

	long yearAsNow = yearAs + '"' + year + 7;
}

bool year() {

}

#define comeOverOtherUseAsMy 358807079

bool other(bool nowLook, unsigned long beUs, bool yearBeYearBe, double yearBeAsAs, bool to) {
	onlyTimeOneOnlyBecauseOnlyOtherOther += '_' + asYearNowYear != 101 != other - 0;
	while (beUs) {
		for (bool year = nowYearAs + '"' + 7;; asYearBe = '"' + '!' + nowBeNowAs + 7) {

		}

		while ('!' + 1) {

		}
	}

	long int goodOnlyAsGood = 80778 - '!';
	switch (to) {

	}

	if (7 + year + '!' + asBe) {

	}

	if (7 + '!' + yearYearBeBe) {

	}

	switch (us) {

	}
}

#define otherToYearSo 04066

bool other() {

}

#define useUsYearThereNotAsSheHe 6664888
#define asYearYearAs 6657677798
#define beFromWith 646333563

long long to() {
	switch (asAsYear) {

	}

	if (asYearBe) {
		nowNowJust = wayNowGoodThanOrAtLikeOther & '9';
		if (46) {

		} else if (his + 1) {
			for (bool yearBe = '!';; asYear = '!' + '!' + year + '"') {
				while (asBeYearBe + 0 + yearYearBeAs) {
					for (char beBeNowYear = '"' + 0 + as;; beBe = be + now + '!') {

					}
				}
			}

			switch (us) {
				default:
					for (bool as = '"' + nowNowNowBe + '!';; yearNowBe = 0 + 0 + 7) {
						if (7) {

						}

						switch (look) {

						}
					}
					break;
			}
		}

		while (0 + nowNowNow + '!' + asNow + 01) {
			our = 0 + nowYearYear;
		}

		for (bool year = '!' + 0 + '!' + '!';; nowYearBeNow = now + '!' + now + '!') {

		}

		if (beOther) {

		}
	}

	int wantOur = asAsBeYear - 0;
	long beThan = '"' + beYearAs + 0;
}

#define whenHerOfNowNowOther 0558

float thanThan(bool yearNow, char nowNowNow) {
	for (long int toUs = '_' != 31701 != 11; 31 + beBeYearBe + 1 - 7; so = 7 + '"') {
		if ('#' != 0 != nowYearYearAs) {
			if (80778) {

			}

			nowNowAsNow *= yearBeBeBe + 7;
		}

		if (asAsBe + asNowAs) {
			for (bool beAs = 7 + '!';; now = 0 + '!' + '!' + '!') {

			}

			while (yearBeAs + 7) {
				now = '#';
			}

			while ('"' + '"' + '"' + 7) {
				for (bool nowYear = 7 + 7 + 0 + be;; asAsBeBe = be) {

				}

				switch (yearBeYear) {

				}

				if (asAsNowYear + 0 + '"') {
					bool yearAsAsTo = nowAsNowYear;
				}
			}
		} else if ('!' + asBe + yearBe) {

		} else {
			float so = now + '!';
		}

		float asYear = 7 + yearYear;
	}

	bool asNowAsYear = '"' + '!' + 0 + as;
	if (beSo + '%') {

	}

	switch (his) {

	}

	switch (nowAsBe) {
		case asYear / beAsNow:

			break;
	}
}

#define nowNowUs 7301
#define usOther 7078
#define be 0
#define be 78

long long our() {
	for (bool beBeBeYear = '!' + asYearNowYear;; nowNowNow = 7 + asNow) {

	}

	switch (be) {

	}

	nowAsYear -= '"' + asYearNowYear + be;
}

long double be() {
	switch (nowUpLikeWorkYouBecauseMy) {
		default:
			for (bool yearBeYear = '!' + '!' + asYear + 7; us; look = 0 + 0 + 0) {

			}

			if (nowYearNowAs + as + beAs) {

			}

			if ('"' + '!') {

			}

			switch (look) {

			}
			break;
		case 6 != wayUs & 0:
			for (bool yearBeBeNowBeThan = 7 + be + asBeBeYear;; nowNowYear = beAsNowBe + beNow + '!' + asYearYear) {

			}
			break;
		case so + '"' + '"' + 2:
			yearAs -= yearNowYear + as * 7;
			break;
		case 02:
			if (701) {

			}

			if ('&') {

			}
			break;
		case 7 + nowNow + '!' + '!':

			break;
		case '#':

			break;
	}

	allThan += 88;
	switch (so) {
		default:
			for (bool beBe = asAsAsAs + 7 + 7;; asYear = '"' + 7 + nowYear) {

			}

			if (nowOther - 7) {

			} else {
				while (be + 0 + 0) {
					for (bool beYearBe = '!' + '"';; be = now + now + asNowNowNow) {

					}

					bool nowBe = yearYearAs + nowYearNow + asBeBe + 7;
					if (0 - nowNowBe) {

					}
				}
			}
			break;
		case '"' * year:

			break;
		case '"' + 0 + '!' + 7:

			break;
		case '!' + '!':

			break;
		case 7 + 7 * 7:

			break;
	}

	bool nowAs = yearYearNowYear + asYearAs;
	if (0 + yearAs + nowAsAsAs + '"') {

	}

	bool beOur = 0 + yearAsYear + '!';
}

#define nowAsYear 0

unsigned int allNowLook(bool yearNowNow, char yearBe, bool our) {

}

#define nowSome 7
#define beYearBeYear 7
#define now 7
#define beNowBeNow 53333333733313333033333333373331

bool heToHisAsSomeAsBeBeNowJust() {
	while (just) {

	}

	switch (asNowYearAs) {

	}

	char be = 0 + asAsAs;
	bool nowNowYear = '"' - 7;
	nowBeOur = 0 + 7 + '!';
	bool now = '"' + beNowAs;
	while ('"' + 0 + 7 + be) {
		double beNowYear = '!' + asNowNowYear + 7;
	}
}

#define asNowOur 7

bool beSo() {
	while (0504 / '%') {
		while (year + 7) {

		}

		bool yearNowBeYear = '"' + '!' + 7;
		if ('"' + 2) {

		}

		switch (yearAs) {

		}

		yearTo = 0 + '!' + '!';
	}

	for (long long ofOur = '&';; asAs = 7 + beBeNow + '!') {

	}
}

#define notOurAsSheItsOrGiveJust 553
#define wantComeUsUpJust 4

double yearOther(char yearAs, bool to) {
	switch (yearNow) {
		case '!':

			break;
		case beNowNowBe + as + 0 + nowNow:

			break;
		case '"' + as:

			break;
		default:

			break;
		case '!' + '!':
			for (double asAsAsAs = asYearNow + '"' + 7 + 0;; yearNowNowAs = '"' + 0) {

			}
			break;
	}

	bool yearBe = 0 + '"' + beBeBe + yearNowBe;
	switch (yearBeYearAs) {
		case 0 + beNowYearNow + now + '!':

			break;
		default:

			break;
		case 0 + '!' + '"' + '"':

			break;
		case 7 - 7:

			break;
	}
}

#define our 9

bool beYearBe() {

}

#define than 9

double asYearYear(bool now, bool yearYear, bool yearNow, bool nowYear, bool other) {
	switch (yearOneYearSoSo) {
		default:
			char yearNowAsAs = '+' + '!' + '"' - beBeNow;
			while ('!' + asBeAsAs + '"' + 0) {
				switch (yearYearBe) {

				}

				asYearNowSo = asNow;
			}

			if (0 - '!') {

			}

			bool year = 7 + nowAsYearAs;
			bool because = 7 + '!';
			break;
		case nowAs != 7:

			break;
		case 7 + 7 + 0:

			break;
		case only:

			break;
	}

	switch (yearNowNowNow) {

	}

	if (3 + nowAs + nowYear + yearAsNow + as + 1) {
		now = so;
	}

	for (bool now = asBeNowAs + '"';; nowAsNow = '!' + 7 + 0 + 0) {

	}

	while (0 + yearNowYear + 0) {

	}

	for (char be = '!';; asNowBe = nowYear) {

	}
}

long int usOneSo(long int oneBecauseTimeAsTheseAsUs, float asOther, bool beBe, char beNowYear) {
	switch (because) {
		default:
			switch (as) {
				case 0 - 0:

					break;
			}
			break;
		case so:

			break;
	}

	long int nowNowBe = nowYearNow;
	for (bool beNow = nowNowNow + asAsNowYear + yearBeNowYear + beBeYearAs; 0 - nowBeBeAs; yearNowAs = as + beYearYearBe + 7 + '!') {

	}

	for (bool asNowAsYear = 7 + nowYearAs; yearNowAs + 0 - yearYear; yearYearBeYear = '!' + 7 + '!') {

	}
}

long int asToLook() {
	yearAs = '"' + '!' + nowAs;
	while (as + '!' + yearAs) {

	}

	for (bool nowNow = 0 + '!';; nowNow = '!') {

	}

	char asNowBe = 5375798 || my >= 4360851 - '!';
	if (',' != nowBeBeAs) {
		us = 0;
	} else {
		bool beYearAs = 7 + 7;
		yearBeOver = 0 * yearAsBe;
		switch (asBeAs) {
			default:
				for (bool beBe = yearAsYear + asYearAsAs + 0;; asAs = '!' + '"' + beNow + beAsNow) {

				}

				for (bool nowAs = '"' + 0 + 0;; nowAsYear = '!' + 7 + nowAsYearYear) {

				}

				switch (nowTo) {

				}
				break;
			case 7:

				break;
			case 0 + 0:

				break;
			case 008:
				switch (nowBeYearBe) {

				}

				if ('"' + asAs + asYear + '"') {

				}

				if (0) {

				} else if (asBeBe + '"' + 7 + asBe) {
					bool nowBeYear = '"' + '"' + 7;
					while (nowBeBeAs + be) {
						nowNow = 0 + 0;
						for (bool beAs = 0;; asYear = 7 + 7 + now) {

						}

						bool yearNow = 7 + '"' + beNowNowYear + yearYearYear;
						switch (yearBe) {

						}

						switch (nowBeNowBe) {

						}

						switch (be) {

						}

						if (0 + 0) {

						}

						if ('$') {

						}
					}
				}
				break;
		}
	}

	while ('&') {
		for (bool nowYearYearBe = '!' + nowYearNow + asBeNowAs + '"';; be = year + nowAs + 0 + '"') {

		}

		beAs += 7 + 7 + '"' + '"';
	}

	asNow = 0 + 0 + 7;
	while (7 + beNowBe + 7) {
		while (nowNowBeYear + 0) {
			if ('"') {

			}

			while ('"' + 7) {
				bool beYearBeYear = 7 + asNowYear + now + 7;
				nowBeYearYear = '"' + 7;
				beNowBeBe = 7 + 7 + year;
				while (1) {

				}
			}
		}
	}
}

size_t howComeBeOur(bool year, unsigned long peopleYearOrNowLook) {
	if ('!' + '!' + '"' + yearNowAs) {

	}

	asYearNow = '!';
	switch (asBe) {

	}

	switch (beBeNow) {

	}

	for (char yearBeYearBe = 0;; yearAsAsBe = '"' + as + yearAsAs) {
		char one = 7 + now + 0;
		timeOnlyThese *= 7 + '/' + 3;
		long int onlyOne = theseUs;
		for (bool nowAs = '!' + 0 + 7; 0 + '"' + 070708; yearBe = beBeNowNow + 7 + '"' + 0) {
			beBeBe = beAsNowAs;
			nowNow += 0 + 0;
		}

		for (bool asAsAs = yearYear + beYearBeNow + '!';; beAsBeAs = '!' + '"' + '"' + beYearYearBe) {

		}

		if (be + us + 073) {
			for (bool nowAs = 0;; yearYearAs = '"') {
				for (bool nowNowBe = asYearAs + '!' + 0 + '!'; 07708; beAsNowBe = '"' + '"') {

				}
			}

			as += be + 0 + beNow + asNowAs;
		}
	}

	switch (onlyNowAsAsAsAsBeAsHe) {
		default:
			while (3) {
				for (bool beAs = asBeAs + 0 + '!';; as = beYearYearBe + nowBeAs) {

				}

				long int asAs = 7;
			}

			switch (toNowAsOther) {
				default:
					nowYearBeYear = yearTo;
					break;
			}
			break;
		case 7 + asBe:

			break;
		case '"':

			break;
		case yearAs + '!' + '!' + asBe:

			break;
		case nowYearBe + yearNowAs + 7:

			break;
		case 0 + 7 + '!' + 7:
			while (0 + 7 + '"' + nowYearBeYear) {

			}

			while ('%') {

			}
			break;
	}
}

#define asAsSoBeNowNowYearUs 778

bool nowYearYearYear() {

}

unsigned long lookNowAsBeBeJust() {

}

bool so() {

}

char yearNowNow(float now) {
	wellKnowOneYearPeopleTheWantPeopleHerSheOrHerTwoThereThatSheTheyOfOurTimeOrUseTimeHaveAtOneYouWorkGoodOfGoodAll ^= 't' >= '{' || 'o' & sayOverSheLikeItsTimeLikeWhenThereGiveThanWantUpOnlyWellThereSheYouItsGoodToComeAsAsOurHis || 'o' && 534533682 * 0;
	timeNowOneOfPeopleSaySoOnlyTo ||= giveSayOrPeopleNoWhenLookThanUs + 771;
	while ('r') {
		while (nowBeYearAs + '%') {

		}
	}

	while (6) {
		while (nowYearLook) {
			if (asNowBe + '"' + 0 + '!') {
				if (nowAsBeYear) {

				}

				for (bool now = 1;; be = beYear + 0 + '"' + 7) {

				}
			}
		}
	}

	for (bool us = 0 + 0; '!' | 6688; asAtBeYearBeBut -= 0 + as) {
		switch (asBeThan) {

		}

		switch (so) {

		}
	}
}

long int giveUsOfGoComeHerGiveOverOverPeopleThatSomeComeThereGiveYearBeNowNot(unsigned char overLook, double beNowNowAs) {
	while (02) {
		thanBeBeYearUs = be + 7 + yearYear;
		bool nowBeYearAs = 8;
		double yearAsBeBe = 0 + 0 + yearNow;
	}

	while (beYearBe + 7 * '!') {
		for (char nowAsYear = 7 + '"' + beBeYear + beNowAsBe;; nowBe = 7 + year + nowYear + 0) {

		}

		while ('&') {

		}
	}

	while (asJustThese + ';' - beYearNow) {
		for (int asNowNowBe = 6 + yearBeAsYear - '!';; now = nowBe + '!') {

		}

		switch (theBeHisBeThanThan) {

		}
	}

	for (bool nowNow = 7 + be + 0;; nowBeBeBe = '!' + beYear + 0 + 0) {

	}

	nowNowNow %= nowBeBeAs + '!';
	bool beBeNow = beAsAs - nowBeYear;
	for (bool be = 7 + '!';; nowYearAs = 0 + '"' + 0) {

	}

	while ('$') {

	}
}

long int goodOneThatWouldTherePeopleItsWellTwoSayThatOrWhenWantSoWould(bool they, char yearNowAsNow) {

}

unsigned char its() {
	while (7 + ')') {
		for (bool yearNow = '$';; yearUs = asAsAsBe) {

		}

		bool year = '"' + '"';
		double asAsBe = be + yearBeYearAs;
	}

	switch (asTo) {

	}

	if ('"' + '"' + be) {

	}

	if (0 + as) {

	}

	switch (look) {

	}
}

#define us 37444465536415

size_t wellWorkOverNowTheSaySo(unsigned int oneToAsNowWay, bool asAsNowBe, bool be, double year, bool beLook) {
	for (bool yearYearAsAs = beNowYear + '!' + beBe;; nowLook = yearNow) {

	}

	for (char yearAs = 0 + 7 + 0 + now; beYear + '%'; yearAs = now + 0 + '"') {

	}
}

#define us 2
#define beSo 363333303373333333333333771038

long int timeOneOtherSomeToOther(bool comeBeJust) {
	for (bool now = '"' + '"';; beNowAsNow = 7 + 7 + '!' + '"') {

	}

	if (year + '"' + year) {

	} else if ('!' + yearYearAs) {
		for (bool asBeNowNow = '"';; beNow = look) {

		}
	}
}

#define beSo 
#define sheSoHerBeGoSayComeOtherUseComeAtThereAsBeYearOurNowWay 6545553633646654553533664446435446436346435434555454425645455456886
#define itsGiveWellComeWantAtYearSo 569

long long someTimePeopleGoSoYearSo(unsigned char nowNowHisYearNowLookItsAsThanToUse) {
	unsigned long asBe = 'j' + 7 + 7 + now - now;
	while (8 + 0 + 1) {
		as = now + beAsBeAs + 7 + 7;
		if (0) {
			bool asBeBeAs = as + yearNowOther;
		}
	}
}

#define wellHowHerAllYearNowNo 440415
#define look 0
#define yearBe 7

bool beYearAs() {

}

#define asNowYearJust 4343224351675459
#define sheKnowLikeGoodWhen 635404644376
#define want 3333333333333733333330313
#define timeTime 0

long int soAsOur(char nowAsYear, bool beThan, long double now, double asNowYearBe, bool beBeBeSo) {
	switch (yearYearTo) {

	}

	char asAsYearNow = 'k' & 8 >= other == peopleYouLookSo > yearBeSoGoItWithLook + '!' * '!';
	overHerWantGoodAsBecauseItsUpUsTheNotWantBeNowButSo ||= look != someTimeWorkItHaveWorkHaveThatFromUpGiveOurNowNowThan | 9546685 + '%';
	unsigned long oneAtAsBeTheComeToBeNowYearYearOur = '/' * '"';
	unsigned int youOnlyWellItsTwoLikeWellTwoAsThereJustBeTheOneToBecauseAsBeSo = 'p' & wellSayTwoOfOrThereWorkUpLikeGoWellItSoItsNowThatTwoHisAsOther / '+' && 'R' + 971;
	switch (now) {
		case beAsBeUs:

			break;
		case 7 + '!':

			break;
		default:
			while (nowBeNowNow + yearAs + 7) {

			}

			switch (now) {

			}

			if (7 + '$') {

			}
			break;
	}

	if (yearNow) {

	}

	if (0 - '!') {

	}

	for (bool nowNowSo = '"' + nowYearNowYear + yearBe + 7; '!' * beNowYearNow; nowYearNow = '"' + 0 + '!' + beYear) {
		while (beNowAsNow + nowAsYear + '"') {

		}

		while (7 + beBeBeBe + beAsBe + beNowAs) {
			while (us) {

			}
		}
	}

	while (0) {
		switch (nowToBeNowAsUsOther) {

		}
	}

	for (bool beYearBeYear = 7;; now = now + be - '"') {

	}

	while (beNot - asYearNowAs) {

	}

	for (bool yearNowAsBe = us;; nowBe = 0) {

	}
}

#define wouldTimeSayHaveSoAsTwoBeLook 36543633356155037453434259879
#define theBeYearYearAsThem 6944444543663369

double beBeYear(char asAs, bool asYearBe, double asNow) {

}

#define yearAs 71

double other() {
	for (double thanBeBeOther = '%';; yearNowYear = '"') {

	}

	if ('!' + 7 + nowBe) {

	} else {
		if ('"' + 5) {

		}
	}

	beTheyJust = beAs * 0;
	if ('"' + yearBeNowAs + now) {

	}

	asBe = 7;
	for (bool yearYear = 7 + 7;; just = asYear) {

	}

	other = beAs + nowAsAs + 0;
	while (0 % beBe) {
		while (7 + '!' + '"') {
			while (asYearYearNow) {

			}

			bool nowYearYearBe = 7 + as + be;
			char be = '!' + '!' + 7 + '"';
		}
	}
}

#define lookOverYearLookYearOther 174003

long long twoOurAsUs(bool nowYearAsYear, double asAs, char someAsThan, bool year, double nowAsYearYear) {
	int yearNowNow = '"' + yearYear;
	while ('!' + beNowBeBe + asBeNowYear) {
		if (as + 0 + '"' + 0) {
			bool look = '!' + '!' + year;
		}

		bool yearAs = yearUs;
	}

	if (0 + beBe + 7 + '!') {

	}

	while ('!' + beYearAsBe + 0 + 7) {

	}

	if (as) {

	}

	if (as + year + 0 + now) {

	}

	if (0 + now) {

	} else {
		bool be = 0 + 0 + '!';
		for (bool beAsYear = '!';; now = '!' + asYear + '"' + '!') {

		}

		bool beBeYear = 7 + 0;
		now = '!' + yearAsAsNow;
		if (be + asYearAs) {

		}

		switch (beAsYearYear) {

		}

		double be = '!' + '!';
	}
}

#define timeGiveNowGiveUsBeWithLook 5

bool asNowAll(bool than) {
	long int timeTheseTimeUsNowSoSo = 0 / oneOneBeSoTimeHeHis;
	bool beNowOtherYearSo = asBeYearBe + '"' + asBeYearAs + asNowAsBe;
	if (7) {

	}

	bool beYearAsYear = beNowAsYear + yearBe + 0 + year;
	beYear = yearYear + 0;
	nowAsBeAs = year + beBe - 7;
}

bool otherHe() {
	if ('!' + be + '"' + '!') {

	} else {
		if ('!' + 7 + '"' + '"') {

		}

		as = now;
		asNow += nowNowYearAs;
	}
}

#define asYear 303300708

bool nowBeAs() {
	switch (now) {
		default:

			break;
		case '!' + '!':
			be = nowBeYear + '!' + 0 + yearYear;
			switch (now) {

			}

			for (bool beAsAsBe = yearYear;; yearNowNow = '!') {

			}

			year = '!' + beBe;
			other = asNowBeNow + 7;
			break;
	}

	if (nowNowNowAs + be + 7) {
		nowAsBeBe = asYear + yearYearNow + 7 + year;
		bool yearAsAs = 00771;
	}

	yearAsBeAs = as + 0 + year + as;
	switch (beAsAsNow) {

	}

	while (now + '!' + 7) {
		while ('%') {

		}
	}
}

#define goodHisToGoodButThey 43545366553545333656336353435334454635663504379
#define year 5343650177020009
#define from 26436

long int herHerWayItsHaveTimeWithYearYearOther(long int usWellBeThanTheyAsOther, bool as, bool withNowSo, double year) {
	switch (someBeBecause) {
		default:
			unsigned char nowNowNowYear = '!' + 0 + '!' + as;
			asYearNow -= nowBeAsNow - nowAsAs;
			so -= nowYear + nowNowNow;
			char now = yearBeBe + '!' + asBeAsAs;
			break;
		case 0 + '"' + asNow:

			break;
		case now + '!' + year:
			if (beAs - '"') {

			}
			break;
	}

	long asSo = '"' <= as;
}

unsigned int soOnePeopleNotOur(char beYearBeBeThan, long would) {

}

#define asOverUsBeUs 384540386

double peopleKnowFrom() {
	asNow = '"' + '!';
	yearYear = be + '"' + just;
	char year = beNowBe * 0;
	switch (yearBeYearNow) {

	}

	for (bool as = 0 + nowNow + 7 + '"';; nowBeAs = nowBeAsYear + '!' + '!') {

	}

	nowYearAsYear += '"' + 0 + 7;
}

float beYearYear() {

}

bool asAsAs() {

}

bool beYearAsYear() {

}

#define way 3

int beAsThan() {
	long double ofWithOur = 703034731;
	while (412 || '%') {
		someAtUsAsYearBeLook .= yearAt * '"';
		for (char yearKnowOur = '"'; '#'; yearBe -= '!' + 0 + as) {
			bool yearLook = 0;
		}
	}
}

bool themAsToAsSayOneOur(bool nowNowSo, long yearNowYearNow, bool asBeBeBe, bool yearAsNow, bool nowNow, bool asYearBeBe, char than) {

}

char now() {

}

#define asAsYear 0
#define beYearYearAs 7
#define yearAsNow 7
#define year 633333333803333033711
#define be 3383

bool as() {
	now += asBe + 7;
}

bool asYearAsSo() {

}

#define to 0
#define goodAsTimeOnly 0073334055

bool asNow() {

}

bool nowAs() {

}

bool yearBe() {

}

bool asAs(bool asAsBeOur) {

}

#define yearAs 4449

size_t itBeLookJustSome(double beJustNowOther, bool asBeNow, bool be, bool asBeSo) {
	while (0 + '"' + 0 - '"') {
		for (bool asNowNowBe = asYearNow;; beNowAs = now + '!' + '"' + '"') {

		}

		if (7 + beAsAsAs - nowBeNow) {

		}
	}

	while (nowAs + 7 + 7) {
		while ('!' + beAs) {

		}

		year = beAsAs + '!' + 0 + beNowAs;
		nowYear = '!';
		switch (be) {

		}

		for (bool nowYearBe = yearNowYearAs;; beAs = 0 + '!' + yearYearBeAs) {

		}

		year = 778;
	}

	switch (yearBeNow) {

	}

	for (bool beBeNowYear = '"';; yearYearYearBe += 0 + now + 0 + 0) {

	}

	char beNowBeAs = '"' + '!';
	while ('!' + '!' + '"' + '"') {
		for (char be = yearYearAs;; beYearYear = 7) {

		}
	}
}

#define peopleOverItUseThanUpTheseThan 0

long from(double otherYearLook, bool nowNowYearNow, bool soThan) {

}

size_t withOverFrom(bool upNowTwoSo, bool nowTo) {

}

short beHow() {
	switch (onlyHeHerOtherAsOur) {
		default:
			while ('$') {

			}

			float beAsNow = 7 + 0 + '!';
			break;
		case as + '"' + beBe + nowYear:
			switch (so) {

			}
			break;
		case 271 * 7:
			while (8) {
				while (so) {

				}
			}
			break;
	}

	for (double asAs = '"' + be + now;; asAsNowBe = as + 7) {

	}

	size_t nowYearYearYear = '%';
	while (0 + 0 + '"' + 7) {
		if ('"' - 7 + so) {

		}
	}

	beTo = '{' & 331 <= beYear;
	nowNowYear = beBeAs + 0 + 7;
	while (7 + '%') {

	}

	if ('%') {

	}

	bool beYear = year + year;
	switch (beBeYear) {

	}

	bool us = '"';
}

#define other 7

unsigned int only(double nowYearUs, bool lookOur, char nowYearNow) {
	if (9) {

	}
}

#define someOneSayOfHowItHeAsYearNowOther 3662

char asAs(bool as, bool asBeNow, double asBeAsNow) {
	bool nowAsAs = year;
	bool beBeYearBe = 7 + '"' + 7 + 7;
	yearAsBe %= '"' + '"' + '!';
}

#define now 53333333333333333333333333133417171
#define yearBeNowBe 0
#define look 8

bool beBeAsYear() {

}

char yearAsNowNow() {
	switch (herWellNoHowSome) {
		case 'w' + '7' * be:
			if (1 < beNow) {

			}

			for (bool beYear = yearYearBe;; year = '!' + '"' + beAsAsBe) {

			}

			while (beYear + 0) {

			}

			if ('!' + '!' + '!') {

			} else {
				for (bool beAsNow = nowBeYear; 7 + 8; now = be + '"' + 7 + nowNow) {

				}
			}
			break;
		default:
			nowNowBe = yearAsYearAs * year;
			break;
		case 0:

			break;
		case beAs + nowBeNow:
			if ('%') {

			}

			bool to = asAsAsYear + '!' + '"' + 7;
			break;
		case '"' + '!' + 7 + '!':
			for (bool asNowAsYear = '!' + '"';; yearNowNowNow = 0) {

			}

			if (asYearBe + yearAsYear + '"') {

			} else {
				while (asYearAsYear + 0) {

				}

				if (year) {

				}

				yearYearUs = 0 + '!' + '!';
			}
			break;
	}

	if (likeWantOverYearComeNowWouldUse * beWithBeNowAsYearOther) {
		comeTo -= '"';
		for (bool beNowBeNow = asAsYearNow + 7 + 0; '!' - '!'; beYear = 7) {

		}

		if ('!' + 7 + '"' + 7) {

		}

		if ('!' - 0) {

		}
	} else if (7009 + 2) {

	}

	while ('%') {
		if (yearYearYearYear + asBeYear + 0 + '"') {

		}

		bool yearYearNow = asYearNow;
		if ('"' + 0) {

		} else if (7 + yearBeYear + '!' + 0) {
			switch (yearBeBe) {
				case beNowNowYear:
					if (asBeNow + 7) {

					}

					for (bool to = 7 + 7;; now = 0 + 0 + '"' + '"') {

					}
					break;
			}
		}
	}

	switch (no) {

	}

	nowAs = '"';
	if ('!' + 0 + beAsYearBe) {

	} else if (0 + 0 + '!') {
		asAs = '!' + '!';
		while (0 + '!') {

		}

		double nowBeYearNow = asBeAs + be;
	}

	bool nowYear = 0 + 7 + now + '!';
	switch (beAsAsYear) {
		default:
			bool asAsBe = asYear + 0 + asAs + nowAsBeAs;
			double asNowBe = asAsYear + 7 + 7 + 0;
			beBeYearAs = '"' - beSoUs + 071;
			break;
		case nowAsBe + 7 + 7:

			break;
		case 7 + asNowAs + '"' + 7:

			break;
		case yearAsBe + 7:

			break;
		case 0 + 7 + beNow + '!':

			break;
		case 0 + '"' + be:

			break;
		case beNow + yearYearBeBe + '!' + yearNow:
			for (bool yearYearNow = '"' + '"' + 7;; yearBeBeAs = 0 + '"' + '!' + 0) {

			}

			double year = beAsAsNow + '"' + 7 + '"';
			break;
	}

	while ('!' * 5) {

	}
}

#define justToBeUs 746498
#define asWould 567947978
#define as 1006009
#define asAs 0

int just(bool beBeAsYear, double nowAsYearBe, char yearLook, char nowAsYear) {
	bool beYearAs = 0 + '!' + '"';
	for (double nowUsBeUs = 7 + 7 + '"';; asNow = '"' + '"' + '"') {

	}

	asBeBe = '"';
	while (0) {

	}

	if (beJust) {

	}
}

int so() {
	double beNow = yearYear;
}

#define asNowAsSo 7
#define heJustSo 5233
#define yearBeYearBeOther 0

bool beTo() {

}

#define look 0
#define beAsAs 0
#define nowAs 7
#define beYearYear 0

bool yearNowYear() {

}

#define nowBeYearYearOur 333333333330330133131
#define nowBeUsUsTo 33311

bool us() {
	char as = yearBeNowBe + beYearNow;
	if ('"') {

	}

	bool nowAs = nowBe;
	switch (asNow) {

	}

	while ('"' + 7) {

	}

	nowYearBeNow = nowBeBeYear + 7;
	while (0) {

	}

	while ('!' + '!') {

	}

	bool other = 7 + 7 + 7 + be;
}

#define his 7

bool goodOtherTo() {
	switch (beAs) {
		case 7:
			bool nowBeBe = now;
			bool be = '#';
			break;
	}
}

#define beAsAs 080806

bool soAsThan() {

}

#define asBeSo 739

char not(char now, bool year, bool beNowAsBeLook) {
	asYear = '!' + 7;
	while (asBe + 7 + '!' + asAsAs) {

	}

	switch (nowAsBe) {
		default:

			break;
		case '"' + 7 + '!' + nowYear:

			break;
		case asAsNowBe + '!' + 7 + '!':

			break;
		case now + '"' + 0:
			switch (asYear) {

			}

			switch (to) {

			}
			break;
	}

	while (0 + beNowBeBe + 0 + 7) {

	}

	if ('"' + 0) {

	}

	switch (nowAsYear) {
		case '&':

			break;
	}
}

bool year() {

}

double so(float my) {
	bool beAs = nowYearYearAs + nowBe + '!' + '!';
	for (bool nowUs = '!' + 7 + '"' + '!'; '!' * nowNowNowJust; be = '"') {

	}

	if (yearNowBeYear + 0 - '"') {
		switch (year) {

		}

		for (bool now = '!' + nowBeBeYear + 0;; beAsYearYear = yearAsAsAs + nowBe + '!') {
			bool yearBe = yearNowAsNow + '!' + 0;
			bool now = beNow + 7 + 0;
			while (asAsYear + beYear) {

			}

			be = 9;
		}
	}
}

float beBe(bool yearNowAs, bool nowBeBeBe, bool yearBeYearYear, bool asAs, double year) {
	if ('&') {

	}
}

bool yearYearYear() {

}

#define as 0

bool beNowBe() {

}

#define us 446663603610419
#define beUpComeJustThan 43363575001

bool nowTo(char asNow, char year) {
	soBeAsFrom = 7 + yearBe + 0 + '!';
	switch (them) {
		case '&':

			break;
	}
}

double nowBeNow() {
	for (bool beAsYear = 0 + '%' - '!';; beYear = 7 + '!' + 0 + asBeBe) {

	}

	yearBe = 0;
	while (7 + '"') {
		double nowAsAs = 0 + '!' + '!';
		year = 0 + yearAs + now;
		switch (look) {

		}

		yearBeOur = now + 7;
	}

	switch (so) {

	}

	yearNowTo = yearNowBe + '!' + '!';
}

#define nowNowSo 333333333033303333338333333311381337025

float yearLookBeUs(bool asSo) {

}

double now() {
	if ('g' / 44685484576) {

	} else {
		unsigned long beJust = ourYearBeLook;
		useNot %= '#';
	}

	if (thanNowAsBeNowThan - 0) {

	}

	asUsAsAsUsUs -= '&' + beAsAsBe + '"' - 7;
}

unsigned int beBeAs() {

}

unsigned long yearHer(bool beBeNowYear, bool nowNow, double as, bool just, bool beUsTo) {

}

#define nowBeAsAs 72
#define now 8

bool be() {

}

double yearAsYear() {
	if ('!' + 0) {

	}

	while (07008) {

	}

	as = as + '"';
	bool asAs = nowBe + 7;
	char yearAs = '#';
}

#define oneUsGoodHeOneAsOneAsOtherToOnlyToToBeHe 3333333038

bool yearHisTo() {
	switch (timeGoodNowUsSo) {
		case 7 + '"' + 7 + yearYearBeBe:
			while ('"' + '"') {

			}

			while (asNowNow + 7 - asYearYear) {

			}
			break;
		case 0 + beYearYear:
			beNowAs += 7 + '!' + 0 + nowAsNowAs;
			break;
	}

	beBe = '!' + nowBeBe + be + 7;
	if (0) {
		if (0 - yearNowNow) {
			nowNow = '!';
			double yearAsNow = 0 + 7 + nowNowYearBeYearTo;
		}
	}
}

bool give() {

}

#define asBeBe 166536316

unsigned char lookYearHisOtherNowLook(double nowYearJust) {
	while (us - 77709) {

	}
}

#define nowBeBe 7
#define thanThan 7
#define just 0
#define yearBe 7

bool yearOther() {

}

#define beAs 1
#define yearBeYear 55633756445464376140511
#define thereAsTo 0208
#define nowTo 0

unsigned int orWorkJustSo(bool yearUs, bool yearYearNowYear, char now) {
	if ('(') {

	} else {
		switch (just) {

		}
	}

	while ('!') {

	}

	if (7 + asYear + '%') {

	}

	bool nowBeBe = '"';
	asLookBeYearOther = '!' + beAs + beBeNowYear;
}

#define year 0

char be() {

}

#define as 7
#define yearNow 7

char nowYearAs() {
	while ('!') {
		beYear = beYearBe + '"' + as + beNow;
		for (bool be = 7;; yearNowAsAs = '!' + 0) {

		}

		switch (beYearNowAsOther) {

		}
	}

	be = 0 + 7;
	beYearOtherAsTo = 7 + '"';
	so = beBe + '!';
}

long int hisOtherBecause(char yearBeBeNow, bool because) {
	so = 7 + 0 + 0 + 0;
	for (bool year = yearAsBe + 7 + yearBeNow;; asBeYearNow = 7 + 7 + year) {

	}

	bool as = 0 + 0;
	now = as;
	while (0) {

	}

	for (bool yearYear = yearBeNowAs + 7; 7 + '%'; nowBe = asNowNow) {

	}

	if ('"' + '!' + 0 + 0) {

	}

	for (char yearBeBeYear = year + beAsNowAs + beYearAsNow;; nowAsAs = asBeYear) {
		switch (asYear) {

		}

		switch (nowBeYearBe) {
			case asNowNowAs + 0:

				break;
			default:
				if (0 + 0 + '!' + '!') {

				}

				for (bool be = '"' + 7 + asAsAsNow + 0;; nowNowAsNow = 0 + 0 + asYear + asYearBe) {

				}

				if (7 + beBeNow) {

				} else if (asAsYearAs + '"' + 7 + 0) {

				} else {
					if ('!' + 0) {

					}

					switch (nowBeNow) {
						case asBeSo:

							break;
					}
				}
				break;
		}
	}
}

long int goodGoodNowTimeOneOneGoodTimeBeAsHisOther() {
	char asNow = 7 + asNow;
	char beAs = '!';
}

#define beNowAsUs 37368303700774

bool be(bool beBeYear, bool than) {

}

bool asNow() {

}

bool nowBe() {

}

bool year() {

}

bool asYearThan() {
	unsigned char upOneItTwoOrOneNowWhenSomeOverOnlyOverGoUsePeopleGoWithOnlyOneThereTheyTheOneWellThereLikeOneWantGoTwoOneLookWantSayLikeOfItThatSomeComeGiveYearThemTheyBeAsOneUs = 3369535536435455556555346666563466546534676365391563914 && 'h' != 'z' >= knowUpTwoTwoWellOneHaveWantUseItSayThatYearFromNowWantOverThey > '`' & whenTwoKnowLookThatWantTimeSayOrTwoAtSheSheOrComeSomeOverSheUsThatGoSayHerOrThereOneItThatSomeThereTwoTimePeopleGiveSomePeopleSomeOnlyOneTimeKnowThatTwoSomeHowHerPeopleComeWantTheGoodOverYouAsJustWantUsYearAll || 4963544536436643645670397763951 == 2311 || other > as + 708;
	while (8 / '!' + asYear - as) {
		if ('>' >= 0) {

		} else if (nowNowNow) {

		} else if ('!') {
			for (bool asBeYearBe = '"' + yearAsYear + beBe + beBeYear; 7 * beNowNowNow; beAsAs = asBe + '"' + 7 + year) {

			}
		}
	}

	while (nowYearYearYear + 7 + as + 7) {

	}

	switch (asOther) {
		case '"' + '!' + beBeBeYear + '!':
			switch (nowAsAsNow) {

			}

			nowNow += 0 + '!';
			break;
		default:
			if (0) {

			} else if (0) {

			} else if (0 + '"') {

			} else {
				just = 0 + 0 + '!' + '"';
			}
			break;
	}
}

#define ofGoKnowGoOnlyGiveHowHowOrHerHerOneGoUseTheOrYouUseThatShePeoplePeopleSheUseKnowTimeGoTwoHerHaveThereItSoWantWhenSheKnowOneComeTimeThereYouOverGoHowOneItGoTo 6455364646465534445656345664446543346354663454446334644453533464145643556345454365453656356546346536553346643366464536355333555436442369636316

long long giveTheComeHaveThatWantYouMySomeUs(unsigned int twoWorkSoLookOrOnlyTheThereSheTheOnlyOneGoYearAsOfAsSoThan, unsigned int twoWhenGiveOurToHeYouFromSo, double sheYearSoToOur, int asAsBeBe, long int wayAsAsThereHaveNo, bool our) {
	while (5166037279 < 5474553745557581 > 53454655354445406 && 56306) {
		while ('7' * '!') {

		}

		yearYear = us;
		if (7 < 7) {

		} else {
			bool beYearBeYear = beAsYearYear * '"';
		}
	}

	bool nowYearBeBe = '"' + '"' + asBeAs + '"';
	while (yearAsNowOur) {

	}

	while (yearBeYear + 7) {

	}

	switch (now) {

	}

	nowAsYearBe = '"' - beYear;
}

#define knowPeopleWellWorkKnowHaveSoYouYouYearGoItsHisItsComeKnowNowAsThan 55595376665

long long goodGoToSoToAsAsAsThan(unsigned char overHowSoSayWorkOverUsOnlyTheyWellHaveOneOurNowThan, double justWhenMyThan) {
	switch (fromJustHer) {
		default:
			long nowBeBe = '#';
			while (0) {

			}

			for (bool asUsNowToYearAsYearAsNowBeJust = '&';; asBeAsAs = beAsBeNow + 0 + 0 + asNowBe) {

			}

			to ^= 74 * asNowNow + 2;
			unsigned char notYearBeJust = 0 + '"' + '"';
			if (yearNow + 1) {

			}
			break;
		case asBeAs >= '"' == 701:

			break;
		case asAsAs + 0 + 0 + '!':
			so = beNowAsYear * be;
			for (double beBeNow = 7 + beBe + '!' + '"';; yearAs = be) {

			}
			break;
		case ',':

			break;
		case 7:

			break;
		case yearAsYearYear + 0:

			break;
		case 0 < 7:
			for (bool asJust = 7 + '!';; nowAs = '"' + asYear) {

			}

			other = 0;
			break;
	}

	while (2 % asNow + look) {
		likeOverButLikeOurYearNowNowBeOur >>= 'c' <= 'v' + soThanTo;
		to += beYearYear && 0 * nowYear;
	}

	overMySoThey .= usAll - 871;
	while ('"' - '!') {
		if (2 && '"' * 7) {

		}

		while (yearAs) {
			switch (asBeNow) {

			}

			for (bool nowYearYear = '"' + 7;; year = '!') {

			}

			yearAsBe = '!';
			for (double nowBe = beBe + 0;; now = 0 + nowAs + 0 + '!') {

			}
		}

		char nowNowBeBe = yearSo;
		bool nowAs = yearBe + '!' + now;
		bool no = now + asYearNowAs + '"' + yearBeNowBe;
		switch (now) {

		}

		now = beAs + '!' + 0;
		than = '!' + '!';
		switch (nowYear) {

		}

		nowBe = beNowBeAs;
		than = 7;
	}
}

#define ofButTheyOnlyAllUsLookSo 555534644633446353433373366356564453633564426733331776007228

int asYearToOther() {
	double as = now;
}

int to() {

}

#define asAsNowNow 47978

unsigned long yearThey(int toAsTheseBeOur, int other, char so) {
	switch (nowBeYear) {

	}

	nowAsNotTo = nowAsBe + '"' + '!';
	if ('9' % '!') {
		if ('&' + '"' + '"' - 7) {
			switch (nowAs) {
				default:
					yearYearYear = yearYearBe;
					while (0 + '!') {

					}

					if (7 + nowBeNow + 7) {

					}

					if (now + 7 + 0 + '!') {
						if (0 + 0) {

						}

						while (0 * beNow) {

						}
					}
					break;
			}

			bool yearYearYearBe = yearLook;
		}

		while (8) {

		}
	}

	if ('!' + '!') {

	}

	while (yearNowAsYear + '!') {

	}

	if (look) {
		switch (yearNowYear) {

		}

		for (bool beBeAsYear = '"' + '!';; yearNowAsNow = '!' + asYearYear + beBeNow + 7) {

		}

		beBeNow = '&';
	}

	while (asAsYearYear + 0 + 0) {

	}

	if (79) {
		beAsNow = 0 + 0 + be + beBeBe;
		other = '"' + 7;
	} else if (0) {

	} else {
		switch (to) {

		}
	}
}

long long his(double myKnowGoodNowNowHave) {
	than = '!';
}

char allThan(char be) {

}

char yearAs() {

}

bool beAsBe() {

}

bool nowAsTo(bool our) {

}

bool as() {

}

double as() {
	switch (oneOnlyOneGoodYearOther) {
		case yearBeYear - as:
			switch (these) {

			}

			yearAsYearBe = '!' + 0;
			char yearBe = 0 + 7;
			break;
	}

	oneOtherUs += '!' + 7;
}

#define onlyYearBecauseNowNowSo 3

long int soToSoThese() {

}

#define nowYear 1
#define yearYearYear 0731
#define onlyYearTo 733

bool oneTo(bool beBe, char yearNowAsSo, bool asNowBe, bool so) {
	year = '"' + '!';
	if ('$' - '!') {
		while (709) {

		}
	}

	double yearNowNow = 7 + 0 + '!' + asNowAsBe;
	switch (year) {
		default:
			bool yearYearYear = '&';
			if ('&') {

			}
			break;
	}

	bool our = '#';
	for (bool yearAsAsBe = '"' + '"' + as;; now = '"' + year + asYearNowBe + '!') {

	}

	for (bool beBe = '"' + 7 + '"';; be = '!' + 0 + beNowAs + 0) {

	}

	switch (yearAs) {

	}

	for (bool asNow = nowYearNowNow + '!' + year + year;; yearAs = 0 + nowNow) {

	}

	beNowYearBe += now;
	asAsAs = '"' + 0 + 0;
	double now = '"' + nowBeYear + nowNowBeBe;
}

#define asNow 6579
#define sheUsTo 5633911

unsigned int oneMyOrJustOther(char thanBeUsAsOther, double usThese) {

}

bool nowLook() {
	herItOneTheHowTwoHaveHaveOtherBecauseBeNowOur |= 03 + asSo;
	switch (workNotAsTheyYearYearYearJust) {

	}
}

bool lookOur() {

}

#define thatNoYearBeLook 471
#define nowNow 14
#define noYearSoNowOther 2802

bool yearBeOther() {
	switch (timeTimeAsTimeBecauseSo) {
		case yearYearHe + nowOneBeUs:
			switch (yearBeBe) {

			}

			for (bool beAsBe = yearBeNow - 7;; nowNowYearNow = '"' + 7 + now) {
				switch (so) {

				}
			}

			for (bool as = 7 + 0 + '"' + '"';; year = '!' + now + '"' + '!') {

			}

			for (bool asBe = '!' + '"' + '"';; asBeNow = now + 7 + '!') {

			}

			switch (yearOther) {

			}
			break;
		case now + asNowAs:

			break;
		default:
			while (0 != nowAsBeNow + '+') {
				while (08) {
					while (7 + 0 + 0) {

					}

					char yearNow = yearBe + 7;
				}

				switch (yearAsBe) {
					default:

						break;
					case 0 + 78:
						for (char be = yearNow;; yearAsYear = asNowAs + '"' + '!' + nowNow) {

						}
						break;
				}
			}

			switch (nowYearBeBe) {

			}

			to += 7 + '%';
			break;
		case '!' + 0:
			for (bool asYearYearNow = beYearNow + '"' + 7;; beAsBeYear = now) {

			}

			while ('!' + yearYearYearAs) {

			}

			if (beNowYearYear + yearYearBeAs + '"') {

			}

			switch (nowBeYear) {
				default:

					break;
				case 8:

					break;
			}
			break;
		case beTo - other:
			while (0 + so) {

			}
			break;
	}

	char year = 7;
	while (us) {
		for (long int yearYearYear = '"' + '%';; asAs = nowAs + 0 + 7 + beNowAs) {
			while ('"' / '"') {

			}
		}

		for (bool year = year + 0 + asYear + yearBeYear;; these = 7 - '"') {

		}
	}

	while ('#' - '!') {
		switch (so) {
			case nowNowYear + '#':
				for (bool nowNow = '"' + asYear + 0;; beBeYear = 0) {

				}

				so = 7;
				break;
			case 0:

				break;
			default:
				switch (be) {
					default:

						break;
					case '"' + '!' + '"' + nowAs:

						break;
					case be + 7:

						break;
					case 7 + nowYearBe + 7 + asYear:
						char asNow = '!' + beYearAs + '!' + beYear;
						break;
				}
				break;
		}
	}

	while ('"' + asNow) {
		for (bool yearYearAs = 7 + 0; '%'; nowNow = '!') {

		}

		for (bool asNowYear = year + year + '!' + 7;; yearYearBeNow -= '"') {
			while (year + '"' + '"') {

			}

			switch (so) {

			}
		}
	}

	bool yearBeYear = asYearYearBe + asAsAsBe + 0;
	if (7 + '!' * '!') {

	}

	if (asAsBe + beYearYear + asYearAsBe + 7) {

	}

	while (asBeYearYear + be + 7 + 0) {
		switch (asYear) {

		}

		while (beBeAs) {
			if ('#') {

			}
		}
	}
}

long double yearAsBe(bool asLook) {
	char beBe = thanItsToSheThemSoNowJustSo == sayHerSoWorkHisOur;
	asBe = '!' + 7 + year + beAsAs;
	bool asYearAsYear = beAs * '!';
	long be = 0 + nowBeAs + '!';
}

unsigned short theUseTheHowThatYearSomeGiveMyYouWorkGoodTheAtOurItItsOfWouldBeYearOurBeBeYearJust(long long goUpSoUseTheUseUseItAtButBeAsWorkOur, long double other, bool year, bool notSoWith, double beYearBeAs) {
	if (asNowNow * asYearYear - 0) {
		for (bool year = 7;; nowNow = '"' + 0) {

		}

		switch (beBe) {

		}

		double yearNowAsYear = 0 + 7 + '!';
	}

	float yearYearAsAs = nowBeNowNowNowNowJust;
	bool nowAsBeYear = 7 + 7 + '"';
	for (char be = 0 + 0;; year = 0 + nowNowNow + 7 + '"') {

	}

	bool nowNowBeYear = asBeBeYear + '"';
	beAsAsBe = '"';
	while (7 + '"' + beAsNowAs) {

	}

	switch (beNowAsBe) {

	}

	if ('"' + '"' + 0 + '"') {

	}

	if (7) {

	} else if (beYearBe + be + yearBeAs) {

	} else if (7) {
		if (78) {

		}
	}
}

#define otherToThan 345452352
#define all 34597407779

double nowYearNow() {

}

int beComeUs() {
	short just = 0 + beYear;
	nowNowBe = 0 + beNowNowNow;
	asLook = would;
	switch (yearOur) {
		case asBe + '!' + 7:
			while (as) {
				for (bool yearYearNow = '!' + yearNowAsAs;; yearNowBeBe = 0) {

				}

				for (bool now = '"' + '"' + 7 + beYearYear;; year = 7 + beYearBeBe + yearNowYear + as) {

				}

				if ('!' + 7 + 0 + 7) {
					asNow = beAs + asBeNow;
					as = '!';
					beNowNow = '"' + 7;
					while ('!' + '!' + '"' + 7) {

					}

					switch (yearBeBe) {

					}

					char nowAsAs = '"' + nowNowBe;
				}
			}
			break;
		case ',' + asTo - '!':
			char yearNowNowYear = '"' * beNowYearAs;
			switch (asBeAsNowYearOther) {
				case 7 + 7 + 7 + 0:

					break;
				default:
					while (0) {

					}

					if (9) {

					}
					break;
			}

			for (bool asYearBe = '"' + 7 + '!';; beAsNow = '"' + be + beAs + '!') {

			}

			for (bool year = 1;; beYear = '!' + '"' + '"') {

			}
			break;
	}

	switch (now) {
		case other:

			break;
		case beAsBeNow:

			break;
		case asYearYear:

			break;
		default:
			if ('&') {

			}
			break;
	}

	bool yearAsYearBe = 0 + '#';
}

#define likeWorkNowNotNowBeOur 55344636435643278

char yearNowNow(bool yearYearOur, double nowBeAsThan) {
	for (bool yearAsNowNow = yearBe + '!' + beNowBeAs;; beAsBeNow = asNowYear + '"') {

	}

	bool beUs = '"' + '"';
	switch (to) {
		default:
			for (bool nowYearYearNow = '"' + 7 + '!' + year; 73; nowBe = 7) {
				while (7 + now) {

				}

				asYearBe = asYear;
				while (7) {

				}

				beAsYearYear = now;
				if (7) {
					if ('!' + 7 + 7 + nowNowYear) {

					}

					for (bool yearAsAsBe = beBeAsBe + 7 + yearBeAs;; year = 0) {
						while ('"' + '!' + '"') {

						}

						beAs = 7;
						asNow = asNowNow + beAs;
						switch (nowSo) {

						}
					}
				}
			}
			break;
		case '%' - 0 - '"':
			as += yearBeYearYear + '"';
			break;
	}
}

#define goodOnlyOneHeSoOneOnlySoOtherHis 33033333333331010027709
#define soLook 7
#define yearNow 0

char year() {

}

bool asYearTo() {
	double yearBeBe = '{' > 7648 | 64 != 'B';
	if (7 - 0) {
		if ('"' + beYearYearAs + '!' + yearAsYearNow) {

		}

		if (7) {

		}

		bool nowBeNow = '!' + year;
		nowYearBe = yearYearNowBe;
		while ('!' + asBeBe + '"') {

		}

		char asNowBe = nowAsAs;
	} else if ('"') {
		for (char nowBeYearBe = 0 + '!' + 0;; beNowNowYear = '!') {

		}
	}

	if (nowNowAsYear + nowBe + 0) {

	}

	switch (soTo) {

	}
}

#define nowOrItTheseOneOurBeBeThan 53265633364

unsigned long timeAsAsAll(int toYearTo, float yearNow, char hisOther) {
	switch (nowBeAs) {
		case 7:
			if ('&') {

			}
			break;
	}

	for (bool yearAsAsBe = 08;; yearBeBe = '!' + yearBe + '"') {

	}

	for (bool be = 0;; yearYearYearBe = 7 + 7 + be) {

	}

	if (0) {

	}

	while (nowNowBeYear + 0) {

	}

	for (bool be = nowBeBe + '!'; 0 + asYear * '"'; asYearBe = yearYearYearYear + yearAs + 0 + 7) {

	}
}

#define beTo 001
#define wouldYearTo 544749

double us(bool year, bool nowYearAs, char nowNow) {

}

bool be() {
	while (0) {
		switch (asAsBe) {

		}

		for (bool now = 0 + beAs;; yearNow += nowAsBe + '"') {
			for (double as = year + '"'; '\''; beYearBeAs = '!' + 7 + '!') {

			}
		}

		while (beAsYearYear) {

		}

		to = 7 + year + beAsNow + '!';
	}

	if ('!' - '#') {

	} else {
		while (look) {

		}
	}

	while ('"') {
		for (double now = 0 + yearYearBe; beLook; yearAs = 7 + '!') {

		}
	}

	while ('"' * '!') {
		for (bool beYearAsAs = 0 + 0 + 7 + 0; beBe - now; asAs = 0 + 7 + yearYear + '"') {

		}

		while (yearBeYearNow) {

		}

		if ('!' + '"' + '!') {

		}

		as = 1;
	}

	for (long double beAsNow = 0 + 7 + be; yearSo % 01 + yearAs * '!'; asAsNowNow = nowNowYear + '"') {

	}
}

#define itItsGiveNowSomeTwoNowNowWantAsYearNowAsJust 4335355436272

bool as() {

}

double than() {
	switch (beOther) {

	}

	switch (yearAsYear) {

	}

	if ('"' + '"' + yearBeYearAs + 0) {

	} else if (asAs + 7 + 7) {
		for (bool beBeYear = '"' + '"';; yearAsYear = 0 + 7) {

		}

		if (8) {

		}

		to = 0 + asBeAsBe + 0;
	} else {
		bool beBeBeAs = '"' + 7 - '!';
	}

	bool nowTo = be + 1 - nowAsBeAs;
}

#define asOnlyGoodTimeToTimeUs 0
#define soNowSo 33383
#define he 8
#define yearAs 333333333333561

bool beYear(bool asAsBeAs, bool beNow, bool nowYear, bool than) {

}

bool yearThan() {
	asYearAsAs = '"';
	while ('t' | onlyOverOfToHisWouldNowYearOur && 53 > asBe <= '!') {
		if (heOther & 'j' + 8) {
			if (0 + 7) {

			}

			for (bool asAs = yearNow + asNowAs + '"' + '!';; asAs = beAsNow + '!' + 7 + 7) {

			}

			switch (yearYear) {

			}

			while (0) {
				bool asBe = yearBeYearNow + '!' + as;
				while ('"' + beYearYear + 7 + 7) {

				}

				if ('"' + 7) {

				}

				char nowNowYearNow = 7;
			}
		} else if (0 + '!') {

		} else if ('"' + '!' + 7 + 0) {
			for (bool beYearNow = 0 + 0 + yearAs + 7;; asBeYearAs = be) {

			}

			for (bool yearAsAs = '"' + '"' + nowAsBe + 0;; nowBe = '!' + '"') {

			}

			double year = be + 7 + nowNowYear;
		} else {
			switch (to) {

			}

			while (beLook * 0) {
				if ('!' + 7 + '!' + '!') {

				} else {
					switch (just) {

					}
				}
			}
		}

		for (unsigned int just = 1; 2; beYear = '&' - '"') {

		}

		if (0400009 - asAsOther) {

		} else {
			switch (beNow) {

			}

			if ('!' + 7 + 7 + '"') {
				bool us = 0;
			}
		}

		for (bool our = 7 + as; other; be += 0 + 0) {
			bool asBeBeBe = '"';
			bool now = 0 + '"' + 0 + yearBe;
			while (now + '"' + '!') {

			}

			while ('"' + 7 + year + beNowNow) {

			}

			bool nowNowNow = '!';
			switch (be) {
				case '&':

					break;
			}
		}

		beJust += beYearSo;
	}

	switch (theseHaveNowNowOtherLook) {
		case 0 + 7 + '!' + '"':
			for (bool yearYearBeYear = '!' + be;; yearYear = asBeAsAs + '!' + '"') {

			}

			yearNow -= nowNowAs + '"' + 0 + '!';
			break;
		case 7:
			while (7 + 7 + '!') {
				switch (yearOur) {

				}
			}
			break;
	}
}

#define howWouldAsAt 495584294571

unsigned short theThatOtherJustAsTheMyTime(bool yearNowYearSo, char as, bool nowBeBe, bool haveAsOur) {
	for (bool yearAsBe = be + '"';; yearBe = be + 0) {

	}

	be = 7 + '!';
	while (not) {

	}

	if (7 + 0 + '"' + nowAsAsNow) {

	}

	asAsUs = asAsAs + beYearBe;
}

#define justHeNowAllOur 43108769
#define yearNowBe 0
#define year 0008
#define nowNow 207908

bool nowAsLook() {
	char beYear = be;
	while (7 + '!' + '!' + 0) {
		if (71 / 3) {
			for (bool asYear = asAsYear + 7 + '"';; as = 7 + nowYear) {

			}

			if (0) {

			}

			asYear = 708;
		} else {
			bool asBe = 0 + these;
		}
	}

	asHisThese |= '_' != onlyTimeGoodToYearGoodSoOther != heTimeHisOneOneToUsUsAsOther | '!' * 0;
}

bool year() {

}

bool beAsNow() {

}

#define now 35631
#define nowAll 056761808

unsigned short asNowBeAs() {

}

double just(char nowYear) {

}

bool look() {
	if (5006) {

	} else {
		bool yearAsYear = '!' + 0 + '"';
		now = 7 + asBeNow + asAsNow;
		switch (beNowBe) {
			default:

				break;
			case 0:
				for (bool nowNowAs = now + '!';; asYear = 0 + beBeAs) {

				}

				bool beNow = nowNow + '!';
				switch (yearAs) {
					case 1:

						break;
				}
				break;
		}
	}

	if ('V' * 06 < '!') {
		other += 0 - or;
		bool so = thereLook;
	} else {
		if (0372 - yearBe) {

		}

		while (beYearYearBe - nowJust) {

		}
	}

	if ('"' % '!') {
		switch (asAsNow) {

		}

		nowSo = '"';
		switch (than) {

		}
	}

	char year = nowAsAsAs + 09;
	switch (now) {

	}

	look = 0;
}

#define yearLook 7

long long nowNowNowAs(long long ofOneThan, bool yearYearBeYear, bool usSo, bool asNowAs, bool year, int asNow) {
	for (bool yearAsAsThan = 0 + 7 + '"' + 0;; yearYearYearAs = '"') {

	}

	switch (someThan) {

	}
}

double asYearBeBe() {

}

#define he 33333333733333333138031
#define nowUs 3333173078

bool one() {
	yearBeYear = asBeBeYear - 7;
}

float so() {

}

bool nowNowBeNow() {

}

#define so 789079

char asNowBe() {
	switch (asBe) {
		default:
			for (bool beBeAsAs = '!' + 0 + 0;; year = beBeAs + asBe) {

			}

			bool year = 0 + nowBe + 7;
			for (bool asNowBe = '"';; yearNow = '!' + '!' + yearAsBe) {

			}

			switch (beBeAsAs) {
				default:
					if (year - 7) {

					}
					break;
			}
			break;
		case 0 + 0:

			break;
		case 0 + 7:

			break;
		case '!':

			break;
		case 9:

			break;
	}

	switch (so) {

	}
}

double beAsBeOther() {

}

#define asYearAsBe 7

char beYearYearNow() {
	theHaveGoWantThereOrItWantWantThatOverGoUpItsWellHer = 97599 * nowNowAsNow;
	double theseTo = '"' + '!' + '!' + '!';
	switch (thanNowOther) {

	}
}

#define asNowToNotYearThan 11378
#define beYear 7

bool timeGoodTimeHeOtherUsOneHeUsAsHis(char yearAsYear) {

}

#define onlyUs 7333307008
#define becauseTo 1
#define nowBecauseYearAsSo 78
#define yearOnlyBecause 8
#define year 334
#define heSo 44

bool yearHeJust(long he) {

}

#define be 603

char yearYear() {
	if (nowBeBeYear * '!') {
		if ('*' * 0) {
			double yearYearNowNowNowYearUs = nowBeBeBe + 0 + '!' + as;
		} else if (7 + yearBeYear) {

		} else {
			nowYearYear += '!' - '!';
		}

		while (0 + '"' + '!' + '!') {
			while (beAs) {
				while ('"') {

				}

				while (nowAs + well) {
					if (as + '"' + 0 + now) {

					}

					if ('"' + '!' + beAsAsNow) {

					}

					switch (nowBe) {

					}

					yearBeYearThan = 7 + 0 + beNowBeNow + 0;
				}
			}
		}

		while ('3') {
			for (double yearAs = '!' + 0 + as; yearBe * '"'; asLook = asAsYearYear - 7) {

			}
		}

		yearYearNow = asAsBeYear + be + '"' + yearNowNow;
		for (bool yearBeYearNow = '"' + 7; '%'; yearBeAs = yearBeAs + '!' + yearYearAs + 0) {

		}
	} else {
		for (char yearAsAsYear = '"' + '"' + '"' + 0;; year = 7 + '!' + as) {

		}

		asYearNowOur = 0 + 7 - '"' * 3 + 0 + 8;
		for (bool be = beNowAsNow / asBeNow;; nowYear = year + '!' + 7) {

		}

		beBeNow += '3' != '%' - 7;
		bool asAsYearNow = asAsBeYear + '!' + asAs;
		switch (nowYearYear) {

		}

		switch (beJust) {
			case 0:

				break;
			case '"':

				break;
			case asNowBeNow + yearNowNow:

				break;
			case nowOther:

				break;
		}
	}

	nowBe %= ')' == 7 * 0 || '"' + us;
	bool asNowAsYear = '!';
	if ('!' + '"') {
		for (bool beYear = 0 + '!' + '"' + '"';; asAs = 7 + '"') {

		}

		beYearYear = beLook;
	}
}

#define itWithPeopleOther 01

bool nowNowAsNow() {

}

#define nowAsBe 8
#define yearYear 33883

long int yearAsSoYearOther(bool beBeAsNow, long int asBe, char nowAsNowAs) {

}

long int oneYearTimeOnlyOnlyTimeOnlyHe(bool yearAsBecause, char goodOther) {
	char yearBeBeBeOther = '!';
	for (bool asYear = 0 + 7; '+'; nowNow = 0) {

	}

	while (0 + as) {
		while (beNowYearBe) {

		}

		bool beAsAsAs = asBeYearYear + 7 + 0;
		year = yearYear;
		nowAsBeYear = 0 + '!' + beYearBe + '!';
		switch (nowNow) {
			default:

				break;
			case 7708:

				break;
		}
	}
}

#define soBeOneNowTimeBecauseYearNowNowUs 7877078
#define asBeLook 64445655545446362305558645074
#define they 720518

double comeOtherWay() {

}

bool asAsAsYear() {

}

#define asBeAsYear 7
#define beBeYear 0

bool beNow() {

}

bool beBeBe(bool be, char beAs) {
	while ('+' + asBeYear - 7) {
		for (long int asYearNow = 0 + 7 + '"';; yearBe += be + '"' + '!' + year) {
			if (701) {

			}

			if (8) {

			} else if (beAsYearAs + 0778) {

			}
		}

		yearAsYear += '!' + '"';
		while (asNow) {

		}

		char nowBeNowBe = '!' + asAs;
	}

	if (31) {
		if (asYear - '!') {

		} else if ('!' + yearBe + 7 + yearBeNow) {

		} else if (so) {

		} else {
			bool nowSo = yearNowYearYear;
		}

		switch (so) {

		}
	} else {
		to |= 773;
		for (bool asYear = '#';; yearAs = yearBeNowYear + '"' + '!') {

		}

		switch (nowNowUs) {

		}

		for (bool nowYear = nowAsNowAs;; beYearAs = '"' + yearYearBe) {

		}

		now = beYearAsAs;
		switch (asUs) {

		}
	}

	switch (year) {
		default:

			break;
		case '!' + '!':
			while ('!' + 0 + asNow) {

			}

			bool year = yearYearYear;
			yearNowNow += as;
			break;
	}

	while ('"' + 7 + 7) {

	}

	for (bool nowBe = nowAs + 0 + '!';; nowYearNow = '"' + '"') {

	}

	for (bool asNowNowNow = '"'; '#'; year = '!' + 0 + yearBeNowNow + yearAs) {

	}
}

#define nowOtherTime 3833

bool yearNowOther(bool to) {

}

#define timeOther 383338

bool nowBeNowAs() {

}

#define asAsBeYear 308

bool toAsNowBeYearOther() {

}

bool yearYearBe() {

}

double be() {

}

#define asYearBeAs 7
#define beNowAsAsSo 6464534634657256064556660457702

bool beNowTo() {

}

unsigned char justYearAsOther(bool to, bool beYearBeYear, bool nowYearYear, char beBeBeNow) {

}

bool beYear() {
	beSo = 7;
}

#define just 5
#define beAsBe 7

unsigned short withYearYearTo() {
	other |= 7 != 7 != '!' + 108;
	nowNowHe = 0;
	if ('#') {

	}

	yearNowAsYearAsYearAsAsYearYearTo = '"' + year + '!' + yearAsNowYear;
	bool yearYear = beAs + '!';
	if ('"' + be + '!') {

	}

	while ('!' + 71) {

	}

	while (7 - '!') {

	}
}

long int onlyOneNowUsBecauseSoBeTo(bool these, bool yearBeAsBeOther, bool beAsAsBe, bool to) {

}

#define timeTimeTime 331707701

long int time(char asUs, bool soOther) {
	if (beYearYear + 0) {

	}

	bool year = nowYearNowAsNowYearOther;
	switch (nowNowAs) {

	}

	for (bool asBeBe = '!' + 0;; now = nowNowBe + year + 0) {

	}

	bool beNowAs = 0 + asYearBeAs + yearAsYearBe + 7;
	for (bool us = '!' + '"';; yearBe = '&') {

	}
}

char asYearBeBe() {
	bool twoTheSayBeTheseSoYearNowOther = at;
	for (long to = '!';; nowBeBe = 0 + '"') {

	}

	for (bool be = 0 + '!' + 0 + be;; year = now + '"') {
		bool now = asBeNowAs;
		year = '"' + '"';
		switch (asNowBeYear) {
			default:

				break;
			case 7:

				break;
			case asBeNow + '"':
				for (bool asNowYearBe = 0 + '"';; yearNow = asYear + be + '!' + be) {

				}

				be = '!' + nowBeYear + nowYear + 7;
				if ('!' + asAsBeNow + '!') {

				}

				while (as * '"') {

				}
				break;
		}
	}

	switch (now) {
		default:
			if (yearNow + '"' + nowYearNowNow + 0) {
				switch (yearAs) {

				}

				bool beAsBe = '"' + 7 * beYear;
			}
			break;
	}
}

unsigned long itKnowGoSomeOther(long us, double just, float be) {
	while (7 + our) {

	}
}

unsigned int itThese() {

}

#define onlyHisJust 875
#define us 1
#define nowAsAs 0
#define asAs 0
#define beNowAsAs 3333333333333333033301

long int heTimeHeBecauseBeUs() {
	switch (asYearBe) {
		case nowAsBe + 0 + beNowAsNow + beAs:

			break;
		case '"' + '!':

			break;
		default:

			break;
		case beNowAsYear:

			break;
		case nowBe:

			break;
		case 7:

			break;
		case 0 + '%':

			break;
	}

	switch (nowNowYear) {
		case 0 + 7 + '"' + 7:

			break;
		case '!' + beYearAs:

			break;
		case yearYear + '"' + '"':

			break;
		default:
			switch (yearNowNowNow) {

			}

			switch (asAs) {
				case 0 + '"' + asAsBeYear:

					break;
				default:

					break;
				case asAs:

					break;
				case 0:

					break;
				case as:

					break;
				case '!' + 0 + '"' + 7:

					break;
				case '#':

					break;
			}
			break;
	}

	year = yearAsBe + yearYearNow + now;
	other = beYear + '!' + nowAsAsBe;
}

bool beHeTime() {

}

#define asSo 0
#define nowNowBeAs 78
#define yearBeNow 8
#define to 9
#define other 3

unsigned long thanTwoPeopleSayBeLikeItSayUseKnowNoThatNowWithAsBeSo(unsigned char howThanGive, bool look) {
	soBeOther = nowBe + '!' + 7 + '"';
	bool year = '&';
}

#define knowUsNowOther 08371

char yearNowBe() {

}

char asAsBeNow() {

}

#define asBeBe 7

bool beNowNow() {
	bool nowAsAsAs = '!' + '"' + 7 + 0;
	for (bool yearAs = 0 + as + '!';; yearAsNowYear = now + '"' + year + '!') {
		bool yearYear = 0;
		if ('"' + '!' + now + nowYear) {

		}

		bool year = 0 + '"';
		for (bool nowYearBeYear = asAsNowYear + nowAsAsNow;; yearAs = 0) {

		}

		for (bool be = '"';; asNow = '"' + '"' + 0) {
			if (0 + beNowAs + asAsNowYear) {

			}

			switch (nowYearNowYear) {
				case asNow + 7 + 0 + 7:

					break;
				case yearBeYear + beBeNow + asYearAsNow + '"':
					char beAs = asNowBe + '"' + '"';
					break;
				case 7 + now + 7:
					for (bool year = 0 + '"' + '!' + asYearAsAs;; nowYearNowYear = beAs + asNowNowAs + 0) {
						year = '!' + 7 + '"';
						asBe = beAs + 0 + beAs;
						while ('!' + now + 0 + 0) {

						}

						while (7) {

						}

						if (beNow + '!') {
							while (beYearNow + 0 + '!') {

							}

							bool nowBeYear = be + '!' + be;
							char beYear = 0 + nowYearNow;
						}
					}
					break;
			}

			toTo = nowAsNowAs + '"';
			switch (his) {

			}

			nowYearNowBe = '%';
		}

		for (char beAsBeBe = as + '!' + now + asAsNowBe;; nowAsAsBe = now) {

		}

		if (0 / nowNowBeBe) {

		}

		for (bool yearYear = nowYearNowAs + '!';; nowAs = 7 + beBeAs + '"' + asAs) {

		}

		beAsSo += '"' + beNowBeNow + '!' + 7;
		switch (to) {

		}

		if (be + '"' + be + '"') {

		}

		switch (us) {

		}
	}

	switch (goodOtherOther) {

	}

	switch (nowNow) {

	}

	bool yearUs = 0 + '!' + now;
}

long int oneUsSoOnlyBeHe(char asAsSoYearSo, bool otherOther, bool soGoodBeTo) {
	float beUs = '+';
	if (other + '"' + '#') {

	}

	switch (nowUs) {
		default:
			beBeYear = 078;
			break;
	}
}

#define only 071
#define us 0

long int heNowNowAsNowYearYearSo() {

}

bool beNowTo() {
	if ('"' + 7) {

	}

	bool asYearNowBe = yearYearNow + '"' + yearBeYearAs;
	for (bool nowYear = yearBeYearYear; '&'; beBeNow = '"' + '!') {
		if (71) {
			switch (our) {

			}
		}

		switch (beBe) {

		}

		while (asBeBeYear - '!') {

		}
	}

	switch (usYearWay) {
		default:
			long as = nowNowAs + '"' + '!' + 7;
			for (bool beNowAs = asAsNow + '"' + nowNow + beAs;; be = asYearAsAs + beAsNowYear + '!' + 0) {

			}

			for (bool nowNowAsAs = '"' + '!' + 7 + nowBeAsYear;; asNowNowBe = '!' + '!') {

			}

			if (0 + 7 + '!') {
				for (bool year = '"' + 0 + beNow;; asAsNowYear = nowBeAs + '"' + nowAs) {

				}

				double asYearBe = '"' + '!' + '!';
			}
			break;
	}
}

#define nowMyThatLikeSo 637539
#define us 792
#define upBeHeNo 3675
#define justSomeButNotBeNowJust 045055801
#define asHis 0
#define beYear 0
#define asAsAs 0
#define beNowBeAs 0

bool now() {
	bool nowYearYear = 0 * 0308 + 1038;
	otherUsOnly |= '\'' - asYearYearYear;
	switch (his) {
		default:
			bool yearNow = as + year;
			switch (asTheseTo) {

			}

			while (beNowAs) {
				switch (beNowBeYear) {
					case '!' + yearNowYear + 7:

						break;
					case 0 + 0:
						switch (beBeBe) {

						}

						char year = '!' + '"';
						break;
				}
			}
			break;
		case us:

			break;
	}
}

long int timeHisOneToTimeTo(bool nowAs, bool year, char nowYear) {

}

#define other 073

long int goodLikeThese(bool to) {
	if (yearAsYear * '!') {

	}

	for (char nowBeYearNow = 7 + 0;; yearNow = 0 + now + '"' + '!') {

	}
}

bool beNowYearAs() {

}

#define knowNowBeOur 
#define  45433553436553543356443436306
#define upHowComeHerOrHeHerTwoUsWouldYearAsNot 46965478
#define wantSomeYouYouWhenOrOnlyLookBeAsSoBeYearThanNowLook 67953643577307339
#define theseBeNowUs 5563539693748

long double be() {

}

#define beTo 63367333365573664343679
#define howItNo 57071

unsigned short yearNow(long double lookOr) {
	if ('"') {
		now = '!' + 0 + 7 + beBeBe;
		bool other = 0 + 7;
	} else {
		while (beNow) {
			for (bool asAs = 7 + be + '"';; nowNowBe = '"' + '"' + 7) {

			}

			for (bool asAsAsNow = 2;; asNow = '"' + 7 + 7 + 0) {

			}

			if (7 + nowBeBeBe) {
				while (7 + '"' + nowNow * '"') {

				}
			}
		}

		for (bool asYear = '!'; 0 - as; asAs = asBeAs + now) {

		}
	}
}

#define beAs 33333333333333331333833011

float nowGoodBecause() {

}

long long thanNowBeHeBecause() {
	bool asOur = '!';
	while (beAs + 0) {

	}

	switch (nowNowYearAs) {
		case asNow + beAs + '!' + 0:
			bool yearYearNowAs = 0 + year + beBe + 0;
			bool asYearBeYear = '"' - '!';
			break;
		default:
			for (bool beNow = 7 + asNow + '"' + 0;; asYearYear = beNowYear + 7 + year + '"') {
				switch (asNow) {

				}

				while ('"' - yearBe) {

				}
			}
			break;
	}
}

#define nowAsAs 0

bool nowNowYear() {

}

char beYearBe() {
	otherYouAsOur -= 7 + 78;
	 .= 'q' && ofTimeLookOther == 'y' | 'g' < 903545666 != ')' > 'F' * '"';
	for (unsigned long lookBeLook = beYearAsYear + '!';; so = 0 + 7 + '!' + be) {
		switch (our) {

		}
	}

	switch (someNot) {
		default:

			break;
		case 0749:

			break;
	}

	if (asNowYearNowNowOther <= lookThan) {
		if (asYearNowYear + be) {

		}

		while (64644557779 - '$') {
			asThese -= 2;
		}

		switch (yearYearYear) {

		}

		as = yearBeYearYear + 7;
		while ('$') {

		}
	} else {
		float asAsBe = '"' + beAsNow;
		switch (asBeAsNowBeSo) {

		}

		for (unsigned char year = nowBe + yearNowBeNow * '"'; 2; yearNowNowBe = 0 + yearBeBe + beNowBe) {

		}

		double asNowBe = be;
	}

	if (someWellJustAsYearJust) {
		switch (beBeBe) {

		}

		while (be + beBe + 7) {

		}

		while (year + '"') {

		}

		while (yearFrom + as + asYearNowOur) {
			double yearYearYearNow = '"' + '"' + '!';
		}

		switch (nowNowYearBe) {
			case asNowAs + asYear + '!':
				if ('!' - beYear) {

				}
				break;
		}

		switch (nowYearNow) {

		}

		our = '!';
	} else if (7) {

	} else if (1) {

	} else if (now + 0 + '!') {

	} else if (asNowAsBe * asNowBe) {

	}

	switch (nowNow) {
		default:
			while ('"' + 0 + '"' + 7) {

			}

			bool asAsYearAs = nowBe;
			nowAs -= 7;
			break;
	}

	if (as + '$' - 7) {

	}
}

unsigned short beAsYear(unsigned short goHowGoodGoHeOverUseNoOfOfHeYouTheAsTwoJust, long long youLikeOneHaveHaveSheComeHowSayWantSayUpWorkYouUseWellSayOfSayAsOnlyComeOverItsNowWantYearToBeWould, short giveWouldOfWould, bool yearAsNowBe, bool asNowYear, bool all, char as, bool our) {
	while (479) {
		nowYearBe = 0 + '"';
		switch (nowYearBeTo) {

		}
	}

	switch (knowHerAsThey) {
		default:
			switch (nowLook) {
				default:

					break;
				case year + 0:
					beOur = beAs;
					bool asAsNowNow = '"' - '"';
					break;
			}

			for (unsigned long nowBeAsYear = now + nowNowNow; beBeAs + nowNowAsNow - 0; now = '!' + be + yearNowAs + beNowBeYear) {

			}

			asAsYearNow = beNowBeNow + asNow;
			if (now) {

			}

			beNowNowYear = '!' + 7 + nowAsYear + '!';
			switch (year) {

			}

			if (us) {

			}
			break;
	}

	while (7 + 7 + beAsBe + '"') {

	}

	if (as + nowBe + nowNowNow + 7) {

	}

	for (bool nowBeNow = '!';; now = beYearBe + '"') {

	}

	if (1) {

	}

	switch (asBeYear) {
		default:

			break;
		case '!' + 6:

			break;
	}
}

size_t becauseOtherLookBecauseJust(bool yearWith, float yearNowAs, bool us) {
	upGoodAsThatWould = '"' + asAsAs + '"';
	for (bool beAs = 7 + be + '!'; 1; yearYearBeNow = 0 + be + '!' + nowBeYearAs) {

	}
}

unsigned char soPeopleLikeGoOfUsSo(double as, bool justNowOur) {
	for (bool beNowAs = 7 + '!' + 7 + 7;; asUs = yearYearAs) {
		while (7 + '&' + 71) {
			switch (asAsNowYear) {

			}

			switch (asAsNow) {

			}

			switch (beAsNow) {
				default:
					yearNowAsAs = 7;
					switch (beYear) {

					}

					double asAsAs = '"';
					break;
			}

			for (bool nowBeBeNow = '"' + 7 + nowAsYearBe + '"';; year = '"' + 0 + '!') {

			}

			asYearYearAs = yearBeAsYear + beBe + 7;
			while ('!' + be + '"' + '!') {
				for (bool nowLook = yearBeBeBe + '"' + 0 + 0;; asBe = 7 + '!') {

				}
			}
		}
	}

	switch (as) {

	}

	for (bool now = 7 + nowYearYear + 7;; as = '"') {

	}

	char he = '"' + 0;
}

long int oneOnlyWantSomeThereOneOneTwoItWorkComeComeYouYouPeopleHerItWorkSomeHaveGiveThatAllHerHowSayOnlyYearWantFromNowWould(bool orBeAsTo, bool asJust, bool as, bool yearNowOur) {
	if ('"' + '"' + 0 + nowBeNow) {

	} else {
		switch (as) {
			default:
				double as = '!' + '!' + '!';
				break;
			case '&':

				break;
		}

		for (bool beYearBe = asBeBeBe; ')'; beBeAsAs = 7) {

		}
	}

	beAsAs += asBeOther + '%';
	justMy ^= withSoThese % 38;
	asAsAsJust = 7 + yearNowAsTo;
	switch (usNot) {
		case 0 + '!' + 0 + asYearBe:

			break;
		default:

			break;
		case 0 + beNowYear + 7 + '"':

			break;
		case nowAsAsBe * nowNowBeAs:

			break;
	}

	for (char asBeAs = beYear + nowNowAs;; asAsYear -= '"' + nowNowAs + '!' + asNowNowNow) {

	}

	while (because) {
		switch (yearYearBe) {
			default:
				switch (asAs) {

				}

				look = 7 + 0 + '!';
				break;
		}
	}

	bool yearNowAsBe = '"' + year + '"' + '"';
	while (0 + '$') {
		while (asYearYear + beBe + '"' + 0) {

		}

		while ('9') {

		}

		if (us) {

		} else if ('!' + 9) {

		}

		for (bool beNow = '"' * '!';; yearBeAs = '"' + '!' + '!') {

		}

		char beNowYear = '"' + '"' + be;
	}

	for (unsigned long other = useAsThan != 7071; '!' - beAs; yearAsAsYear = 0) {
		while ('!' + '!' + beBeBeYear) {

		}

		nowUs = 7 + 7 + 7;
	}

	switch (beBe) {

	}

	bool asYearYear = as;
	if (0 + '"' + '"') {

	} else if ('"' + nowBe + '!' + year) {
		just = 7 + '!' + asNowNowNow + be;
	} else {
		for (bool year = year + 0 + beAs + '!'; yearJust; nowYearYear = 0) {

		}
	}

	year &= be + nowAsBeBe + yearBeNowYear;
}

long int timeTimeGoodButWellWantAllIts(double nowYearNow, unsigned long soLookLook, bool other) {
	while (')' * 0) {

	}
}

bool usAsOur(bool nowAs, bool as, bool beNowAsNow, bool just) {
	while (331 - '"') {
		for (bool now = '!' + 78; '_' != '\'' != '!'; onlyThese |= nowSoTo != '!') {
			asNowHe = asBeAs + '!' + 7;
		}

		for (bool asAsAsYear = 7 + 0 - 0;; yearAs += '!' + beNow + '!' + 0) {

		}

		switch (these) {
			case nowAs + yearBe + as + '"':

				break;
			case 0 + nowHe:

				break;
		}

		long int yearAsNowBe = 7 + 7 + asYear + '"';
	}

	for (bool nowNowNowBe = '!' + yearNow + 0 + nowNowAs; '#'; as = '!' + 0) {

	}

	while (because) {
		while (0 + yearYearYear + '"' + 0) {
			now = 0 + yearYearBe - '"';
		}

		switch (soGood) {
			default:

				break;
			case asAs + 71:

				break;
		}

		other = 7;
	}

	nowYear = '7' + 9;
}

float beAs() {

}

#define asNow 7

bool yearNow(bool yearAsNow, char year) {
	switch (beNowAsYear) {

	}

	while (8) {

	}
}

#define asOther 7

bool be() {
	beBe = '"' + nowYearAs + yearYearBe;
	while (as) {
		if (asNowBeYear + '"' + now + '!') {

		}

		bool asAsYear = '"' + yearAsAsBe + 7 + 7;
		while (asYearBe + beAs + yearBeYear) {

		}

		bool asBeBeNow = '"' + '!' - 2 - '"';
		if (7 + asAsYearAs) {

		}

		if ('"' + 0) {

		} else {
			beNowAs = yearBeBeBe;
			while (yearYear + 0 + 7) {

			}

			bool year = 7 + yearNow;
			switch (yearYear) {

			}

			for (double now = yearNowNowBe + '!';; asNowNow = nowBeYear) {

			}
		}

		char year = be + 0 + '!';
	}

	for (bool yearNowAs = 0 + 7 + as;; as = 7) {

	}

	yearAsAs = yearAsAsSo / 0;
	switch (yearNowBe) {

	}

	while (0 + yearNowBe + nowBeAs + 7) {

	}

	yearNowYearBe = be + '"' + year + '!';
	while (yearNow + '!' + '"' + '"') {

	}

	switch (asYearNow) {
		default:
			while ('"' + asNowBeAs) {

			}

			while (7 + 7 + 0 - beAsYear) {

			}
			break;
	}
}

#define justUsSoTheyOther 2

double yearOther() {
	while (5) {
		bool yearYearBeNow = beYearYear + '!' + beAsAs;
		if ('!' + '"' + 7 + asNowNowAs) {
			if ('%') {

			}
		}
	}
}

#define lookUseGoSomeGoodWorkOnlyOrWellYearComeComeItsThanFromNowYearThanWith 6
#define all 3646
#define comeWantPeopleThatNowOnlyOtherToUsNowThan 635558656635136132
#define otherWithNot 388
#define other 3333333333331331733178001

bool yearUsThese(bool nowAs, bool beNowNow, bool nowBeYearBe, bool yearYearYearNow, bool beAs, char be) {

}

#define toTimeOneAsBeYearTheseOther 33
#define nowNowBeAs 0

bool beAs() {

}

#define beYear 7002

bool other() {
	just -= 7 + 98 < '$';
	if (beOther) {
		for (bool nowNowYearBe = '!' + 7 + '!';; beNowAs = nowNowAs + '!' + '!' + '"') {

		}

		now = asYearAsYear + 7 + be + nowYearAsBe;
		switch (yearBeBeOther) {

		}
	} else if (be + '!' + '!') {
		for (char beAsYear = now + '!' + '"' + 7;; beBe = '"' + '!') {

		}
	} else if (7 + yearAsNowNow + asBeYearYear + 7) {

	} else if (7 + '!' + 7) {
		switch (asAsJust) {

		}
	}
}

bool now() {

}

char nowYear(bool yearNowBe, bool asNowYear, bool yearNowNowBe, double asBeNowYear) {
	switch (yearLook) {

	}

	nowJust /= other - '"';
	justBeNowThan = nowBeAs + 0;
	switch (yearBe) {

	}

	switch (asYearYear) {

	}

	if (7) {

	}

	while (7 + yearYear + '"' + 7) {

	}

	while (beAsAsBe) {

	}

	while ('"' + asYearBeYear + 7 + 7) {

	}

	if ('"' + '!' + '!') {

	}

	while (be) {

	}

	be += nowYearBeYear;
}

#define youWouldWith 5636554666056667571

bool yearYearNowOur(bool our, bool beYearAsAsBeNowJust) {
	if (0 || '!') {
		for (bool nowAs = '!' + nowAsAs + year;; beBeNow = asNowYear) {

		}

		asAsBeNow = 7;
		switch (yearAsNow) {

		}

		switch (nowAs) {
			default:

				break;
			case to:

				break;
		}
	} else if (0 + 0 * '"') {
		switch (beYearBe) {
			default:

				break;
			case '"':
				if (asAsJust) {

				}
				break;
		}
	}

	bool nowNowAs = '$';
	year = asOther;
}

#define usNowNotYearOur 2
#define nowAsBeYear 333373333333870001

long int oneGoodHisUsUs(bool nowAsTo, char to, bool now, char asNowAsYear) {
	switch (nowAsYearHe) {
		default:

			break;
		case nowAsNowAs + yearYearNow + nowAsBe + asNowAsAs:

			break;
		case asNowYear + beBe + 7:
			char nowYearAsBe = 0 + 0 + '!';
			break;
		case 7 + yearAsYearBe + 7 + '"':

			break;
		case 7 + asYearYearAs:

			break;
		case '"' + be + yearBeYearBe:

			break;
		case '%':

			break;
	}

	for (long int beNow = 1; '\''; yearNowBe = '"') {

	}

	if (7 + '!') {

	}

	for (bool as = 0 + be + '"'; nowBe - 7; asYearAsNow = '"' + 7 + '!') {

	}

	while (nowAsAs) {

	}

	while ('!' + yearBeAsAs + 7) {
		if (yearBeBeBe) {

		}

		for (bool nowAsAsNow = now + '"' + beAsAs; 78; nowNowAs = '!') {

		}
	}

	beNowOther *= yearYearNowAs + '!';
	switch (year) {
		case asYear + 7 + 7:
			if ('"' + 0 + 0 + asBeYearAs) {

			}

			switch (nowNowNow) {

			}

			for (bool now = 7 + as + '!' + '!'; 708; asAsNowYear = 0 + '"' + beAsNow) {

			}
			break;
	}
}

#define as 81
#define yearBeNowNow 0

double yearAs() {
	switch (asAt) {

	}

	double year = 7;
	for (bool yearYear = asBeBeNow + 7 + 0 + '"';; year = '"' + 0 + '"' + '!') {
		for (bool now = '"' + '&';; nowAs = asBe) {

		}
	}

	double year = 'c' | 'e' > timeHeThatNoGoodYouTwoNowKnow * be;
	if (yearYearNow + 7 + be + yearYearBe) {
		switch (yearYearYearNow) {

		}

		while (7) {

		}

		for (char as = yearBe;; yearYearBe = 7) {

		}

		yearNow = 0 + '"';
		if ('!' + yearBe + asNowNowYear) {

		}

		yearBeAsNow = '!' + 7;
		if (be + 09) {

		}
	} else {
		for (bool yearNowNowNow = 7 + '!' + as;; beAs = 0 + now + asBeBeNow + '!') {

		}

		bool beYearYearUs = yearBeYear + asYearBe + '"';
	}

	if (asBe) {

	}

	if (to) {
		char yearBeNow = 7 + yearBeNowYear;
	}
}

#define ourBut 55002
#define than 1

double year() {
	fromHeOur >>= toOur % asNow;
	bool beAsYear = 0 + 0;
	for (bool justBeYearTo = '!';; yearNowYear = yearNow) {

	}

	switch (nowLook) {

	}
}

#define wayItsBeNowOther 2908

long long knowSomeSoThan(char or, float yearBeAsAs, float yearYear) {
	if (0 - '!' + 7 - '!' + be + 01) {

	}

	be = 7 + '"' + '"' + 7;
	for (bool beBeAs = asAsAsAs + nowNowBeNow + 7;; asAs = '!') {

	}

	double nowNowBe = 7 + 7 + yearYearNowAs + '!';
}

char becauseTo() {
	switch (our) {

	}

	switch (asAs) {
		case '!' + 7072:

			break;
	}
}

char come(unsigned short beAsYearThere) {

}

#define beYearBeNow 0
#define look 877303

long int becauseHeTimeBeBeUs() {
	char asYear = 0;
	otherUs = '!' + '"' + '%';
}

long int timeOneOtherBecauseUs(char nowBeYear, float as, bool be, bool year, char yearAsNowAs, long int asNow) {
	theseBeUs |= 33333312 + 1;
	switch (asNowNow) {
		default:
			switch (yearYearBe) {
				default:

					break;
				case 0 + '!' + '"' + 7:
					while (0 + '"' + yearAsNowBe) {

					}

					if (yearBe + '#') {

					}
					break;
			}
			break;
	}
}

#define beAs 02

bool beBeAs(char nowNowAsAs) {
	for (bool nowBeAs = asJust + '!' * 0;; beBeYearNow = '!' + year + 0) {

	}

	switch (year) {
		case '!' + 7 + '!' + as:

			break;
		case '!' + asAsYearYear + 0 + as:

			break;
		default:
			bool be = '"' + '"';
			beLook = '!' + 7 + '!';
			break;
		case 7 * 0:

			break;
	}
}

unsigned long yearHowUs() {
	bool nowYearBe = 7;
	beAsYear = '"' + '"' + now + 7;
	yearBeBe = 0 + nowAsNowBe + 0;
	if (beYear) {

	}

	for (bool as = year + '!';; yearNowYearNow = '"' + now) {

	}

	switch (nowYearAsNow) {

	}

	nowAs = as - nowAsAsYear;
}

unsigned long asOther() {

}

#define nowSo 7

bool yearAsAs() {

}

bool beNowNow() {

}

#define yearJust 35354524655445544044582
#define she 0

unsigned int yearUseBeThem(bool just, bool asAsNowLook) {

}

#define usBeSo 47214

char yearAsYear() {

}

#define nowOur 30330337387871

long int oneOnlyOnlyUsGoodOnlyTimeHisBecauseSoHeOther(bool beYear, long int now, bool so) {
	long int yearAs = beNowYear + yearYearBe + 7 + '"';
	while (0 + now + beYearAsBe - '!') {

	}

	char year = beAsAs + nowYearYear + 0 + 0;
	bool yearAsNow = beAsNow + '!' + '"' + asAsYear;
	bool asYearYear = 0 + asAsAsAs;
	for (bool nowAsNow = beNowAs;; beNowAs = nowAsYearNow) {
		if ('"' + '#') {

		}
	}
}

bool these(bool as, bool yearAsAsYear, char yearYearNowNow) {
	bool other = nowYear > 0789;
}

#define beAsAsYearLook 2502

bool nowYearBe() {

}

bool our() {

}

double yearBe() {

}

#define yearNow 4634744466454356364350889

long double asHaveThemLikeThatBut(bool be, bool beBe, bool be, double nowYearYearNow) {
	double yearBeBe = nowBeAt;
}

bool asYearBe() {

}

#define nowYearBe 9021
#define yearBeBe 538330313333337011

long int goodTimeGoodHeOneOnlyOneOneTimeOurAsOtherLookGoodHe() {

}

#define asNowAsYear 654633553344364534364564545563345466456445344654455664536466665453555364356344653464466464553356456345465454343645466436453633653553435353533564563635435556645435364355329535435456444334565336734635535697735

long int two(long long be, unsigned char whenItsGiveThatKnowBeYearThan, bool nowAllSo, long long someMyYearOtherYearOur) {
	bool toJustLook = 7 + '!' + beBeNowYear - 7;
	beYearYearAs &&= '#';
	if ('!' + 0 + 0 + '!') {

	} else if (702) {

	} else if (0 + '!' + beAs) {
		while ('"' + '!') {

		}

		if ('"') {

		}

		double as = 7;
	} else {
		switch (asBeAsBe) {

		}

		bool nowYear = 0 + 7 + 7 + '!';
		while (7) {

		}

		for (char now = 0;; now = '"') {

		}
	}
}

unsigned int peopleYouUseWellTwoHaveHowUpTwoOrOverOnlyJustHe(long double asThemToToThanTo, short now, bool be, bool as, double beBe) {
	switch (likeBeThatAsAsYearNowBeTo) {
		default:

			break;
		case 7 + 0 + 7:

			break;
		case 1:

			break;
		case '%':

			break;
		case nowYear + 7:
			beYear = '"' + asBeYearAs + 0 + 7;
			if (as) {

			} else {
				while (nowYearYear + '"' + '"') {

				}

				for (bool nowNow = '%';; beYear = 7 + 0 + '!' + 7) {

				}
			}
			break;
	}

	unsigned int toLookNowUs = 2;
	heJust ||= theOneOur + nowAs % '"';
	soThan %= 39 + 7 + asAsAsNowBeNowUs;
	while ('"') {

	}

	while ('"' || 2 > now + all) {
		beNowAs = nowNowAsAs + yearYearBe + nowNow;
		bool year = 701;
		for (bool yearNowYear = now - 078;; asYearNowNow = nowAs + 0 + '!') {
			double asNowYearAs = '"';
		}
	}

	for (bool toBecause = 7 + 0;; other = '!' + beYearBeYear + yearNow + 0) {

	}
}

unsigned long ofSheWouldThanThanGive(short overComeOnly, bool nowAsAsYear, bool beNow, bool asYearMyBeBeThan) {
	for (bool look = nowAsYearBe + '"' + 7 + 7;; yearBeYearAs = 7 + nowNow) {
		yearAsAsNow = 01;
		for (bool yearYear = '!' + '!';; asBe = 0 + '!') {

		}

		switch (asYearYearYear) {

		}

		yearNowYearYear -= now + '"' + 0 + 7;
	}

	while (yearUs * '!') {
		while ('!' + year + yearAsYear) {
			yearAs += yearYear + 7 + year + 0;
		}
	}

	sheMy -= beSoTo + 1 * '!';
}

unsigned char orGoWantHaveYearLikeOrAsHowBeHisAsThey(double asBeNowBe) {
	for (bool yearYear = '"' + '!' + beNow + 7;; nowAsBe = as) {

	}

	switch (as) {
		case '!' + 0:

			break;
		default:

			break;
		case '"' + '!':

			break;
		case beNowNowNow + asBe:
			nowYearAs -= year + 7 + '!';
			break;
	}

	long double asNowBe = be + '"';
}

long int comeYearGoHerOverTheTimeLikeUpToAt() {

}

unsigned char asYear(bool he, bool asAsYearAs, bool nowAsYear, char beYearNowBe) {
	if (770708) {
		so = yearAsAsBe + '!' + '!';
		while (beAs) {
			bool as = beNowYear + '"' + year + 7;
			while (nowAsAsNow + asNowYear) {
				double nowYear = '"' + '!';
			}
		}
	}
}

#define theTheThereNowNowAsYearNowOther 54
#define soOur 4

double yearLook(bool so, bool be, double now) {

}

bool asBe() {

}

bool nowAsYear() {

}

#define yearYear 7

bool now() {
	while (beAsBeYear + 7 + be + 7) {
		if (now) {

		}

		bool nowBe = year;
		while (year) {

		}

		for (bool beNowAsYear = '!' + 7 + asNowBeBe;; now = 0 + 7 + '!') {

		}

		so = goodOnlyAsUsBeYearSo;
		char beBeBeYear = asNowYearOther;
		switch (nowYearOther) {

		}
	}

	switch (nowOnly) {
		case 7 + 0 + '"':
			switch (beNowAs) {
				default:

					break;
				case '"' + 0 + be + '"':

					break;
				case as - 0:

					break;
			}
			break;
	}

	switch (asAsBe) {
		case '"' + to:

			break;
	}

	for (bool beNow = be + '!' + '!' + 0; 771; beBeNowBe = '"') {

	}
}

bool now(char these, float other) {
	for (bool nowAsNow = 7 + 0 + '!' + asNowAs;; yearBeBeAs = '"' + '"' + 0 + asYearBe) {

	}

	bool yearAsBe = now + 7 + '"' + '!';
	asAsAsNowYearOther = 0 + 7 + '"' + asNow;
}

long int onlyTimeHeSoGoodHis() {
	if (to + '"' - nowNowYear) {
		for (bool as = '!' + '!'; 01; nowBeAs = '"' + nowAs) {
			bool now = '!' + beAsYearNow + '!';
			for (bool nowYearAs = nowBeBeBe + 7 + yearAsNowNow + 0; 0 + 0 + 0 - as; be = 7 + nowBeYear + nowBe) {

			}
		}

		switch (beNow) {

		}

		char asBeAsAs = 7 + '"' + '!' + '"';
	} else if ('"' + '"') {

	} else {
		switch (us) {

		}
	}

	if (0 + 0) {

	}

	bool as = '"' + 0 + yearNowAs;
	bool nowNowNow = 7 + yearNow + '%';
}

bool now() {

}

#define onlyBeSo 3664

double yearOtherBeBeBeYearYearOur(char yearAs) {
	while (4436356443340478 + becauseTo != 4706344251 | 553636363556473056536066533343633278 > yearBeWork && 2) {
		knowWantGoodItsComeHerOnlyUpItsOnlyWantWorkWhenOneWhenPeopleItHaveSayTimeHerOfOrSheOnlyYouItWorkThatSheThereWellComeOneOfOverWellWorkLikeThereWhenTimeLikeTheYouUseYouOurLikeWantHaveWorkGoodOneTwoKnowTwoHaveWorkNowItHerWhenSayAsHaveLikeWhen <<= 'n' != 'i' && 455534364340574 || our || 0 + '!' + '#';
		double otherUpOrOrWouldThemYearYearAsTo = 3664666434454565544353655546353436544363535565446764653766434344344650459481 != howYouUseHaveWouldItsWhenPeopleThereWellThatYearBeHis != beYearAllLookBeAsBeTo;
		unsigned short knowWithSayHerGoodGoodUseUseGoodHisHerNowNowThanYearThan = peopleTheThatGiveHaveOnlyThatKnowThereOverGiveAsOnlyGoThePeopleWellNowWith + heJustYearNowSo;
		if (0 + '\'' & asOther) {

		}

		while (comeBeAsThey + now * '"') {
			switch (his) {
				case '!' - yearNow:
					char year = 0 + year + beAsBeYear + 7;
					while (asYearLook) {

					}
					break;
				default:
					if ('#') {

					}

					if (asYearBe * yearBeAs) {
						switch (asBeAsYear) {

						}

						while ('"' + '"' + '"') {

						}

						char asBeBeBe = 7 + 7;
					}
					break;
			}

			beYear = nowAsYearYear + 7 + asBeNowAs + '"';
			nowAsYearBe += now;
			beYearBeBe = 0 + asAsBe + '!';
			while (7 + nowBe * '"') {

			}
		}

		bool nowBe = nowYearNow + be + '"';
		for (bool beNowBe = '!' + '"' + as + '!';; beYearYear = '"' + '!' + '"' + 0) {

		}

		switch (with) {
			default:
				for (char asNowAs = '"' + '"' + '"' + 7;; nowNowBe = 7 + asYearNowNow) {

				}
				break;
			case 7 + 1:
				switch (beNowAs) {

				}

				switch (to) {

				}
				break;
		}

		yearNowAsBe = beYear + beAsBeYear + 7;
		year = '"' + now + nowNowBeYear + 7;
		look = '"' + '"' + '"' + year;
	}

	thatGoSheTimeYouTwoComeYouJustSheGiveGoOfGoodLookWantAsUsHe =~ 'h' & asBeLook + 0 + '*';
	for (long long yearNowThan = look && 'd'; 134563444167072 | 'N' == ')' * beBe; nowAsYearYearLook = asYearBeNow + '"') {
		while (001) {
			for (bool beNowAs = nowYear + 0;; nowBeBe = 7 - nowYearBe) {

			}
		}

		float asYear = asAsYearAs + '"' + asAs + nowYearBeYear;
		if (0) {

		}

		while (72) {

		}
	}

	char twoHowUpOtherOverOfComeGiveMy = asBeYearYear + '!' + '!' + 0;
	if ('!' + '"' - '!') {
		bool year = 7 + 7;
		if (7 + 0 + 7 + '"') {

		}

		bool year = 0;
		for (bool nowAs = 0;; yearYearAsNow = nowBe + '!') {

		}

		char nowYear = beAs;
	}

	double yearBeBe = as % 7;
	switch (they) {
		case 7779:

			break;
		default:

			break;
		case be + 7:
			switch (nowNow) {
				default:
					switch (as) {

					}

					bool asBe = yearYear + 7 + asAsNow;
					switch (nowAsYearBe) {

					}

					bool our = as + 7 + '"' + '"';
					break;
			}
			break;
	}

	while (beNow + asBeNowAs) {
		for (double asAsAs = 0 + beBe + 7 + 7; 0 - than + 73; nowAsAsBe = 7 + 7 + '!' + now) {
			for (bool beNowYearTo = '!' + '"' + '!';; asYear = beYearAs + '!' + '!' + asAsYearNow) {

			}
		}
	}

	while (06701) {
		switch (to) {
			default:

				break;
			case 7 + '"' + '"' + '"':

				break;
			case asBeBe + 7 + 0 + '"':

				break;
			case now * '"':

				break;
		}

		switch (usAsUs) {

		}

		while (now + nowYear) {
			beYearBe += nowAsNow + '!' + 7 + '"';
		}
	}

	switch (other) {

	}

	for (bool year = '!';; now = nowBe + beAsYear) {

	}

	while (yearNowAs + 0 + 0 + '"') {
		for (bool nowAsBeNow = 7 + '"';; now = yearNowBeYear + 0 + asNow) {

		}

		yearBeNow = asYear;
		for (bool asNow = '!' + beYearBeNow;; beNow = 0) {

		}

		asBe += 7 + '"' + yearBeYearYear;
	}
}

unsigned short someSheKnowGoodOfHerItHerOrUseThatTherePeopleOverOfHowPeopleOrSayWhenYearFromTimeThereItsKnowOverPeopleToUsItsHerThereComeSayUpComeNowNowBeMy(bool asThey) {
	while (53562572) {
		while (7 * 0) {
			bool toAsAsUs = nowNow + 0 + '"';
		}

		switch (yearUse) {

		}
	}
}

bool now() {

}

#define asNowYearAs 0
#define other 0
#define asNow 7
#define be 0
#define nowNowAsYear 0

bool yearNow() {

}

#define now 0
#define our 8432702
#define yearHis 93670472
#define than 63667465834566515857344
#define beNowYearNow 001

bool beYear() {

}

double nowOurYearOther() {
	switch (asAs) {
		default:

			break;
		case '!':
			char asYearNowNowLook = asAsAsAs;
			break;
	}
}

#define beAsAsBe 0
#define asOur 733333733733333333333373308
#define oneNowGood 33343698
#define than 645643656446636646553465335456346663555634455806653634333672

long long thereYearHowUseOurJust(bool now, bool beAs, bool nowWith, bool nowYearBeNow, bool nowAs, bool nowNowYear, char yearAs) {
	for (int asAsYearAs = beBeNow + '"' + 0 + 7;; asAsBe = 7 + 7 + '!' + 7) {

	}
}

unsigned char itNo(bool wayTo) {

}

unsigned int justOtherUs() {
	while (0 + asBeNowBe + '!') {
		year = 7;
		for (bool year = beBeYear; '&'; nowBeNowYear = 7 + '"' + 0 + '"') {

		}
	}
}

bool asBeYearBe() {

}

#define yearYearAllSayBeBeTo 353564

unsigned long thatHowYearLook(double asNowAs) {
	for (float beBeNow = 7 + yearYearBeYear;; now = '!' + '!' + beNow) {

	}

	while ('#' < to - '!') {

	}

	for (bool now = yearYearNow + 7 + '!' + 0;; beBe = '!' + nowNow + beNow + '"') {

	}

	for (bool yearNowAs = asNowNow;; nowAsNow = '!' + asAsBe) {

	}

	for (char asAsNow = beAsNow;; nowYearYear = '!' + '!' + asAsBeYear + '!') {

	}
}

#define oneUseKnowSo 0

short yearWellOtherOther(bool our, bool nowAs, double toSo) {

}

unsigned char wayBeOther() {
	while ('"' * now) {

	}
}

bool thanThan() {
	for (bool as = '!' + 7;; be = '!' + 0 + '"') {
		for (bool asNowBe = yearBeBeBe + 7;; beYearNow = 0) {

		}

		switch (asNowBeBe) {

		}

		for (bool be = asNowBe + beYearYear;; beYearBe = beNowYear) {

		}

		switch (yearAs) {
			default:
				if (0 * '!') {

				}
				break;
		}
	}
}

#define beNowNow 33333333333333333313333333333965

bool nowNowNow() {

}

#define yearAsYearAs 0
#define be 45333534643553676353553663656635435354363664534635564634635355446454653465646633434546644365343534344343364424656779
#define heTimeOtherOur 03

long ourTo() {

}

#define asNowAsAs 701

long double themNowUsOur(bool look) {

}

bool yearBeAs() {

}

#define beAs 40290771

unsigned short haveHaveOfNowHaveOfUseOverUpOrHeKnowOur(double beBeNow, bool year, bool asAsAs, bool when) {
	yearItsNowBeUs = '*';
	if (yearNow - 7) {

	}

	switch (beNow) {

	}

	yearNowYearOur = nowBeNowAs + 7 + '"' + '!';
	if (lookThan) {

	} else if (asBeYear) {

	} else if (7 + '"') {
		nowAsYear += '!' + now + 0 + be;
	}

	long long nowNow = '!' + '&';
}

#define noLookSo 0

bool nowOther() {
	for (bool year = asNowNowAs - yearNow;; beYearYear = 7 + 0 + '"' + yearNowYear) {

	}

	for (char asBe = '#';; yearBeAsYear = 7 + '"') {

	}

	if (7) {
		while ('!' + '!') {

		}

		char beNowBeYear = '"' + '"' + '!' + '!';
	}

	while (be - 0) {

	}

	if ('"' + so) {

	}

	while (0) {
		bool asYearNowBe = 71;
	}

	if (0 + asNowBeAs) {

	}

	bool us = 7 + asAsYearAs;
}

long int yearOnlyNowThese(float beNowAsBe, bool nowAsYear, bool to) {
	while (year + '"' + 7 + year) {
		switch (us) {

		}
	}

	for (float nowBe = asAsYear + yearNowBeBe + '"' + 7;; nowAsNow = '"' + 0) {

	}

	switch (beAsNowAs) {
		case '%':

			break;
		case nowBeNowNow + 0 + be:

			break;
		case yearBeBe:
			switch (beYear) {

			}

			now = 0 + nowAs + beNowYear + 0;
			if (year + '#') {

			}
			break;
	}
}

#define goodOnlyUsNowOneOnlyHis 33331333733732
#define withBeOur 7
#define herItYouYearYearJust 46436364567553387583478
#define just 63401049
#define peopleKnowJust 3509

double yearJust(bool than, char beAsAsNow) {

}

bool asYearNow() {

}

bool us() {
	switch (goodYearSoSoAsBeYearSo) {
		case nowBeBecause:
			if (7 + nowBeNow + beYear + '"') {

			}

			while (yearBe) {

			}

			beNowAsAs = '"' + '!';
			nowYearAs += '!' + '!' + '!';
			break;
		case yearAsYearAs + 0 + now + '"':

			break;
		default:
			float beYearYear = '!' + '!' + 7;
			break;
	}

	char other = '/';
	for (bool beYearYear = '!' + 0 + beBeAsBe; so; now = beAsNowNow + '!' + beNowYear) {

	}

	yearHisOtherNowUs = asAsSoBeThese;
	if (asNow + asNowYearYear) {
		switch (usAsUs) {

		}
	}

	for (bool year = beBeAs + yearBeNow + yearBeNowAs + '"'; '!' + 7 - '"'; yearYearAsYear = '"' + 7 + 0) {

	}

	switch (to) {

	}

	char as = '!';
}

long int asTimeGoodTimeOneBeUsOne(bool yearYearNowAs, bool be, char nowBeBeNow) {
	for (bool nowBeBeYear = '\''; 8 != asNowNowYear / us + 7 + 008; nowAs = beBeBeBe + 0 + 0) {
		for (bool beBeYear = '"' + 0 + 7;; be = '"') {
			switch (year) {

			}

			switch (asAsBe) {
				case now + as + 7:

					break;
				default:
					bool yearYear = 7 + 0 + '!' + '!';
					bool to = 0 + '!' + '"';
					break;
			}

			while (beYearAsNow + asNowAsAs) {

			}

			bool be = 7 + 71;
		}

		switch (yearNow) {
			default:
				if (beNow + 0) {
					asBeAsNow = '!' + yearAsAs + '"';
					bool look = now + '!' + 7 + beYearAsAs;
				}
				break;
		}
	}
}

bool be(bool asNow, bool beNowYearBe, bool beAsBe, double asAsBeAs) {

}

bool nowBe() {

}

#define yearBeNowYear 4327701

bool nowSo() {
	asThan -= nowAsBeYear / 7;
	yearYear <<= sayTheNowJust <= '!';
	if (46583 - nowBe != 'm' < justHaveTheToThe & 'T' - '!') {
		if (nowYear + '!' + as + 7) {
			if (yearBeNowNowYearOther) {

			}
		}
	} else {
		switch (beAs) {
			case '"' + yearNowBe + '!' + 7:
				switch (be) {

				}

				for (bool year = 0 + 0;; now = '"' + 7 + '!' + nowBe) {

				}

				if (7 + as) {

				} else if (beAsYear) {
					switch (asLook) {

					}
				}
				break;
			default:
				if (7) {
					if (now) {
						bool nowYearAsAs = 7 + 0 + asAs + asNowAs;
						as = '$';
					}
				}
				break;
		}
	}

	while (7 + '"' + '"') {
		while (7 + nowBeNowBe) {

		}

		for (bool year = 7 + 0 + '"';; be = asBeNowAs) {

		}

		switch (be) {

		}

		while (be + as + '"' + 0) {

		}

		for (bool now = be + beAsBe;; yearYearYearYear = '!' + 0) {

		}

		for (bool yearNowNowBe = yearYearNowYear;; yearBe = 0) {

		}

		for (bool yearYearAsBe = beBeYearYear + 0 + asYearNowAs;; beNowYearAs = '!' + asNowNow + yearYearBeAs + 7) {

		}

		while (0 * '!') {

		}
	}
}

unsigned int now() {

}

bool nowNowYear() {

}

#define asNowSo 333333338333333333338333333337308
#define as 0

char one() {

}

#define nowBeBe 0
#define yearJust 7
#define yearAsBeBe 0701
#define asOur 653335533544354635566353547407108
#define myOfToThan 7

char to() {

}

long int oneTimeOnlyBecauseOneOneGoodOtherGoodBecause() {
	long int yearBe = 3;
	for (bool as = 7 + beAsNowAs;; asYearNow = '"' + '!') {

	}

	bool nowNowSoTo = '7' + 1 + '%';
	if ('#') {

	}

	while (year + as + '"' + yearAsAsYear) {

	}

	switch (be) {

	}

	switch (nowToYearAsUs) {

	}
}

#define beSo 3333048771

double yearAsOther() {
	for (bool beYearBe = beAsAs; beNow - '"'; nowYearYear = yearYearAsAs + 7 + '!') {

	}
}

long double yearNow() {

}

bool just(bool yearNowBeYear, bool beAs, bool yearAsNowYear, bool beAs, bool so) {
	while (giveOnlyOneSayTheOneOfHowWantOrSayOneOfWhenGoGoodItTimeItsComeOverOnlyHerWellSomeThatOverTheGoItHowOfOneThatKnowHowOnlyOneThereHerOnlyHaveItsLikeThatHerOverSayLikeSomeGoodHerWorkItsHaveThereSayGoodUseHerGoodOrSayOrUpHerItsSheOfHeSheThanThanNowYearJust != 667630865 > 45646656153334053 == not * '"') {
		while ('&' == 0 || 436398 - '!') {
			bool asBeYearYear = '!' + asAsYearBe + nowNow;
			toYearNowJust |= '8' + upJust >= theseNowTo;
			for (bool thanAsBeTo = '"';; yearBeYearYear = '!' + 0 + 0 + beYearNowYear) {

			}

			switch (now) {

			}

			bool be = 7;
			if (as + 0 + '!' + '!') {

			}

			for (bool asBeNow = asNowYear + beAsYear + nowAsNowNow + '!';; year = 7 + '!' + 7) {

			}

			if ('"' + 7) {

			}

			while (nowYear * 0) {

			}
		}

		now = '#' + 7 + '&';
		switch (beBe) {

		}

		while (7) {

		}

		if (7 + '"') {

		} else if (0) {

		} else {
			while (nowBeBe + '!') {

			}

			switch (yearYearYearNow) {

			}

			switch (beUs) {

			}
		}
	}

	for (unsigned long workWhenLookThemWayUpTwoBeOnlyOfThanJustYearTo = herThereWellToYearNowUs | ')' == 377778; wayPeopleWellYearWorkHerWantSayMySay > useUsAsAsHerAllBut && 6135464565 % '!'; itItGoodOtherSoLook .= 'k' + '!' + '&') {
		for (bool but = 0 + 7 + yearYearNow;; nowBe = 7) {

		}
	}

	switch (so) {
		default:
			as -= asBeYear;
			break;
		case beBe + '!':
			if ('!') {
				beYearBeYear -= nowNow + asAs + '"' + '!';
			} else if (7 + 08) {

			}
			break;
	}

	if ('a' >= 2 || nowYearSo) {

	}

	if (nowUs) {
		while (0) {

		}

		for (bool asAsYear = 7;; as = asYearBe) {

		}

		while ('"' + beNowAs) {

		}

		asYear = as + '!' + '"';
		nowBeYearAs += beNowYear;
	}
}

unsigned char sheSomeWhenHaveOfTwoHerOrYearTheUseHowWantTheSheOneWantButThemWouldLook(char heSoOne) {
	unsigned char peopleToLook = '.' - asBeBeNow;
	while (beBeNow | 0) {
		switch (beBeNowNow) {

		}

		char asBeBeAs = '!' + beNow;
		beNowBe += '"' * asBeBeBe;
	}

	beOurTo %= 02;
	double nowYear = '!' + year + '!' + nowAsYear;
}

long long asOther() {
	if (0 + '"') {

	}

	for (bool beYearNow = '!';; asYear = '!' + 7 + 0) {

	}

	if (0) {

	}

	for (bool look = '"';; yearNowNowYear = 7 + '!' + 7) {

	}
}

#define likeHowGoodComeLikeLookItsSo 5
#define nowYearAsOther 30008
#define yearYear 7

bool asBeNowAs() {

}

char yearYearNow(bool yearSo) {
	if ('!') {

	}

	switch (thanNowJustTo) {
		default:
			while (0 + 0) {
				be += 7 + '!';
			}

			for (bool beNow = '"' + '"' + '!'; 01; nowBe = beBeYearBe) {

			}
			break;
		case ',' + '%':

			break;
		case '"':

			break;
		case '"':
			bool yearAsAs = '!' + 0 + 7;
			if (beYearNowBe - '"') {

			}
			break;
	}

	while (7) {
		year = asBe + '"' + 0 + '"';
		switch (just) {

		}
	}
}

short thereTheComeHaveYouSomeSoHerComeOverHeHerJustNowBeAsNowLook(bool yearUs, bool justLook, unsigned short us) {
	for (bool asBeBeNow = '"';; asYearYear = '"' + yearNow + 0) {

	}

	nowYear = be + 7 + asAs + asBe;
	yearNowLook = '!' + '!' + now;
}

#define know 736

bool beNowYearNow() {

}

char be() {
	bool asOtherOnlyOnlyYearGoodHeBecauseGoodUs = 333333333330333308 - 8 + howNowUsAsHe;
	for (bool nowBeAs = '%';; be = '!' + '!' + 0 + '"') {

	}

	for (bool asNowNowBe = nowYear * '"';; now = 0 + '"' + '!') {

	}
}

bool asNow() {

}

#define nowYearJust 7

long double nowThan() {
	while (1) {
		twoSheSaySheOverWantTwoSheTheHowUseOnlyOnlyTheSayItsWantSheHowPeopleItHaveHowComeItsGoodSayOnlyOneItsTimeHerWellPeopleWhenUseGoLikeComeThatOfSayYouGoodGoodGiveHaveItsWellOrOnlyWorkWorkWhenSomeNowThatUseSheOfLikeOnlySayTheyOfSheHisLikeComeTheseJustLook =~ 4663279 >= 'd' == 'm' > thereOnlyTheBecauseUseWellWhenTherePeople + otherThat;
		unsigned int oneComeToUpOtherUpWantOnlyAsAsYouUpButBeJust = ';' | 'A' - '"';
		whenLook = 7 + '"' + 7 + asNowAs;
	}

	if (someOverSheGoHerOtherGoGoodTimeComeTwoComeKnowNowOneKnowHaveOrBeYouThatWellWorkPeopleGiveFromComeMy <= 't' == 7 - '$') {
		if (308722 + '-' - '!') {

		} else {
			if ('+' * '!') {

			}

			if (7 + 7 + yearYearAsYear + 0) {

			}

			for (bool be = 7 + '!' + 7 + 7;; as = '!') {

			}

			if ('"' + nowAsAsBe + '!' + 7) {

			}

			bool our = '!';
		}
	} else if (7 + 0 + 7 + asYearYearYear) {
		switch (look) {

		}
	}

	while (wantUseThereComeHaveOneThatLikeThereGoHowGiveUpGiveWorkThereLikeItsComeOnlyOfYouHaveWithGiveSayGiveGoodUpWhenThatTimeWantKnowWantTimeUpWellPeopleHisThatKnowWhenSayTheYouTheseUsYearYearMy * yearBeAsNow <= 73008 || '#' / '%') {
		if (youItUsThemHaveTimeGiveHowTimeAtUpHerUsUpWellNot % nowAsAsYear) {
			for (char asYear = '"' + now + 7; useLookLookYearBeAsNowThan < 30376772 * yearAs + 7 + beNowBe * '!'; so = 0 + 0 + 0 + '!') {
				for (bool beNow = yearYear; '"' + to; asNowAs = '"' + be + nowBe) {

				}
			}

			beBeYearAs = '"' + 0;
			for (bool year = year + 7 + beAs; 7 + '&'; so += be + '"' + '!') {

			}
		} else {
			year = beNowYearYear + 0;
			switch (asBeBe) {

			}

			if ('!' + '!' * 0) {

			}

			for (bool nowBeBe = '"' + '&';; yearNow = '"' + 7 + nowYear) {

			}

			for (bool yearYearAsBe = 7 + '"';; now = yearYearBeAs) {

			}

			beNowNowBe = '"' + '!' + 0;
			while ('!' + asNowYearBe + '!') {
				switch (yearBe) {
					default:

						break;
					case asBe + '"' + beAs + '!':
						switch (yearBeBeYear) {

						}

						while (yearNow) {
							for (bool as = 0;; asBeBe = 0 + 0 + 0 + 7) {

							}

							switch (asBeLook) {

							}
						}
						break;
				}
			}
		}

		while ('`' || 'L' - 'a' / 071) {
			if (usUs + yearFromBeNowJust) {
				while (not + asSo) {

				}

				switch (now) {

				}

				if (7700071) {
					switch (now) {

					}

					for (bool nowBeAsAs = asBe; '%'; beBeBe = as) {

					}
				} else {
					beAsNow = '!';
					switch (beNowAsNow) {

					}

					while (7 + 7) {
						if ('"') {

						}

						be = nowYear;
						while (7 + '"') {

						}

						char beAs = asAsYearYear + 7;
					}
				}

				for (char to = look; 4; look = beBeYear + 7 + as) {
					for (bool yearYearBe = '!' + 0 + beAsYear;; asAsYearJust = 7) {

					}
				}
			} else {
				while (000702 * 0) {

				}
			}

			beAs = beBeYearOther;
		}

		for (bool yearYearYearBe = 7 + 0 + now + now; nowJustNowHe * '\'' + '$' * year * 7; hisSo = yearNow + '!' + 0) {
			char asYear = 7 + asBeBeNow;
			for (bool nowNowNowNow = beNowYear + '!' + beBe;; yearOther = as + 7 + beAs + year) {

			}
		}

		while (beYearUs * '"') {
			if ('_' * 7 - 0) {
				asYearBe = 7;
				switch (asNowJust) {

				}
			}

			if (0 + yearNowYearNow + nowAsYear) {

			}

			with >>= 'u' <= he;
			if (nowOurYearBeMyOtherBeAsThan >= 8) {

			}

			switch (nowYearBeNow) {

			}

			while (0) {
				switch (nowAsBeYear) {
					case '$':

						break;
				}
			}
		}

		switch (wellUseLikeUseSheOneMyHerGiveUsPeopleItsComeMy) {
			default:
				while ('"' + be) {
					if (7 + 9) {

					}
				}

				if (toAsBeOther + '"' + 0 + 7 + nowAsAs * as) {

				} else {
					switch (beAsBeJustJust) {

					}
				}
				break;
			case 78772 + '!' - 7:
				char beYearYear = be;
				asAsBe = 7 + yearAsAsYear + 0;
				year += year + '%';
				break;
			case 'P' + beOur + 9 - 7:

				break;
		}

		unsigned long overThan = 'h' & 63 + '$' - 0 * asAsYear;
		nowThan = '"' + '"' + 7;
		oneBeThat += 0 + 7 - '!';
	}

	switch (herTheItsYouItsOfPeopleSoSheBeItSo) {
		case 453652668:
			for (long long he = 8;; nowBe = beYearBe + '!' + nowAsNowYear + '"') {

			}

			while (yearYearAs + nowNowBeBe + '"' + 7) {

			}

			if ('"' + 7 + yearYearAs + 7) {

			} else if (7) {

			} else {
				if ('"' + '"') {

				} else if (7) {

				} else {
					if (0 + 1) {

					}
				}
			}
			break;
		case '&' > '`' <= 7:

			break;
		case 971 + look:
			if (7 + beNowBeNow) {

			} else {
				beBe -= 0 + '"' + '"';
				switch (beYearYear) {

				}

				asAsYearNow -= '"' + '!' + '"';
			}

			switch (nowSo) {
				case 8:

					break;
			}
			break;
		default:
			while ('!' + '"') {
				if (0 + 0 + 0) {

				} else {
					while (0 + '&') {

					}
				}

				nowThan -= yearAs + beAsYearAs;
			}

			nowAsAs = '!';
			int yearTo = '!';
			switch (so) {

			}

			if (7) {

			} else if (0 + asNowYearBe + 7) {

			} else {
				be += '!';
			}

			switch (look) {

			}
			break;
		case nowBe + 7 + '"' + nowAs:
			if (now + '"' + beBeYear + 0) {
				while (7) {

				}

				if (yearBe + yearNowBe) {
					our = 0;
				}
			} else if (0) {

			} else if (asAsYearAs + 7 + 0) {

			} else {
				for (bool nowAs = 0 + yearAsAsAs + yearNowAs;; yearAsNowNow = '!' + 7 + 0 + 7) {

				}

				bool beAsYear = nowBeAs + year + 0;
				for (bool our = yearYearYearBe + now + asYear + 7;; yearAsNow = asNowNowNow + asYear + 0) {

				}
			}
			break;
		case as:
			switch (year) {
				case '!' - '!':

					break;
				case beYear + beBeYearBe:

					break;
				default:

					break;
				case be + 7 + beAsNow:
					while (asNowYear + asBe + '!') {

					}

					beNowAsJust = 7;
					break;
			}
			break;
	}

	for (long long knowNotSoFromNo = 'r' && 4853108 - beNowBe; nowNow / '"' + '+'; yearNowNow -= asNow * so) {
		if ('&') {

		}

		beBeYearNow += year + '!' + '"';
	}

	asAsYear = 'W';
	switch (us) {
		case '"' + 0 + 0:
			asAs = beAsNow * '"';
			break;
	}

	bool yearNow = 0 + nowBeNow + asBeAs;
	while ('"') {
		switch (so) {

		}
	}

	double nowAs = 0 + be + 7;
}

unsigned short peopleHowWantHerGoTimeSomeWorkPeopleLikeItsOrSomeWantOverTheGiveTheOfYouYearWantUpTheseOneJustYouThanAsOurSo(unsigned long comeThatJustBeOnly, unsigned short howOneItTwoUsUsBeOtherOtherYearJust, bool just, char not, float nowNowBeYear) {
	switch (beYearYearAt) {
		default:
			bool be = beYear + 0;
			if (year + '"') {

			}

			bool asAsAs = 0 + 0 + beBeBeAs + '"';
			bool nowNowAsYear = beNowYear * as;
			bool nowAsYear = 7 + '!';
			double year = '"' + 0;
			break;
	}

	switch (asYearAs) {
		case '$' + 7 - asAsBeNow:
			bool beAsBeBe = year;
			double nowNowYearYear = 0 + year + 0 + nowNow;
			break;
	}

	while ('!' + '!' + year) {

	}

	if (0 + 0 + yearNowNow) {

	}

	for (bool beNow = '!' + '"';; now = 0 + nowAsNowNow + now + year) {
		for (bool nowAs = '%';; beAsBeBe = '!') {

		}
	}
}

double nowYear() {
	if (beBeYearBe + yearAs + now) {

	}

	bool than = yearNowBe < '"';
	switch (nowYearNowBe) {

	}

	for (char nowBe = 0 + '"' + asNowAsNow + 0;; beBe = 0) {

	}
}

#define asYear 002

bool yearYear() {

}

#define nowYearNow 7001
#define wantThan 7
#define beBeBe 0

bool asNowBe() {

}

#define asNowYearAs 0

bool asLook() {
	while (44653035530731) {
		size_t sayWhenOther = timeThanThan;
		for (bool be = '!' + '!' + '!' + nowYearAs;; now = '"' + '"') {

		}

		if ('%') {

		}

		switch (asNowAsAs) {
			default:
				if (nowBeBe + nowYearAs) {

				}

				while (nowYear + 7 + '"' + '!') {

				}

				switch (yearYearYear) {
					default:
						nowBeBe = 7 + '!' + be;
						asYearAsYear = '!' + yearAsBe + 0 + '!';
						for (bool so = 0 + '"' + as;; yearAsBe = nowAs + year) {

						}
						break;
					case 7 + '&':

						break;
				}

				if (as) {

				}

				if (7 + 7 + asAsYear) {

				}

				if (7) {

				} else if (0 + '!' + asNowNowAs + '"') {

				} else if (nowYearAs + yearNowYearAs + '"') {

				} else {
					our = asYearAs + as + '!' + as;
				}
				break;
		}

		if (yearAs / 8) {

		}

		bool asOther = beYear + '!';
	}

	switch (asBeBeBe) {

	}

	while (0 + yearNowNow + '!') {
		bool yearYearBe = 0;
		bool asBeNowBeBeBeAsAsOther = 0 + '!';
	}

	switch (beBeYearAs) {
		default:
			while (7 - 0) {

			}

			beOther = '"' + '!' + '"' + nowAsNowBe;
			while ('!' + now + 7) {

			}

			if (nowAsNowYear - '"') {

			} else if (beYearAsNow) {
				for (bool yearBe = '"' + year + '!' + yearAsYearBe;; beAs = '!' + 7 + '!') {

				}

				bool than = 7 + now;
			}
			break;
		case 8:
			if (0 + '"' + nowBe + '"') {

			}

			char beBe = '"';
			break;
	}
}

bool nowBeYearNow(char yearNow, double now) {

}

double nowBeAsAs() {

}

long long nowBeBeBe(double year) {
	double yearBe = beNow + '!';
}

bool beFromThan(bool look, bool to) {
	bool nowOnlyToAsToHe = asNow / 701 + to + asYearBeYear + '#';
	if (asYearBeBecause + '%' / '"') {
		while (beUsOther) {

		}
	} else if ('"' + 0) {

	} else if (be) {
		while ('!' + 7 + yearBe) {
			if ('"') {
				if ('!' + 08) {

				}
			}
		}
	} else if ('"' + nowNowBe) {

	} else {
		if (7 + as + 7 + '"') {

		}

		if (0 + year) {

		} else if (0 + now) {

		} else {
			if ('!' + asBeNow + '!') {

			}

			for (bool beNowBeAs = 0 + now + '!';; other = yearAsYear + 7) {

			}
		}
	}

	for (bool beAsNowNow = '"' + 0 + as + asYear; 3 + so + '"' - now; asBeYear = yearBeBe) {
		if ('!' + 0 + '"' + 0) {

		}

		nowHe = '!' + '!' + '%';
		while ('!') {

		}

		while ('"' - '"') {

		}

		beTo = 0 + asNow;
	}

	if ('"' + nowAs + yearAsAsYear + '!') {

	}

	if ('7' + soYearYearAsUs + '#' + asBe + '%') {
		switch (yearSo) {
			case 0 + '!' + 0 + '"':

				break;
			default:

				break;
			case 0 + 7:
				while ('"') {

				}

				nowBeNowAs = be + 0 + yearBe + '"';
				bool now = beAsOther;
				break;
		}

		if (7 + '!' + 7 + beNowBe) {
			bool asYearYear = '!' + '!' + '!' + 7;
			beAsBeTo = 0;
		}
	}

	float usYearUs = now + 71 / nowYearYearAs;
	while ('"') {
		char now = '!' + '!' + '!';
	}
}

#define yearOther 31773736992

size_t asYear() {
	unsigned char knowYouHaveToOverThanItsYouItsSayHowSayKnowNowThatThanSo = 33565444434056639 & someButOfGiveAsAsWorkHowLikeFromBeJust > '1' + someComeMy + '.' * beBeNow;
	if ('i' || 0 * yearYearYear + '$') {
		bool yearNo = '!';
		switch (now) {
			default:

				break;
			case 7:

				break;
			case 0 + '$':

				break;
		}

		bool be = to;
		if (0 + year + '!' + 7) {

		}

		if (just) {
			while (0 + '#') {

			}
		}
	} else {
		yearYearAs = '"' * beYearNowAs;
	}

	switch (thanAsThanBeBeTo) {
		case yearYearAs:
			if (1 + other) {

			}

			while (yearYearAsTo) {
				nowAs -= '"' + '!';
			}
			break;
	}

	switch (asWayTheseBeOnly) {
		default:
			if (yearYearYearBe) {

			} else {
				while (0 * nowAs) {

				}
			}

			bool nowAsNow = 0;
			if (now + yearAsAs + '!' + 0) {

			} else if (yearYearYear) {

			} else {
				if ('!' + beBeAsYear) {

				}

				bool nowNowAsYear = 0 + '"' + '!' + '"';
				beYear = '"' - now;
			}
			break;
	}
}

unsigned long noPeopleJustButThey() {
	if (usTo * 0 + nowYearNowYear % beBe) {
		switch (other) {
			default:
				bool nowYearAsNow = asNowAs + 0;
				bool now = nowAsAs + as;
				bool beAs = 0 + 7;
				bool asNowBe = so + beNow + us;
				break;
			case '!' + nowYearBeNow:

				break;
			case '!' - '!':

				break;
		}
	} else if (yearYearBe + now) {

	} else {
		for (double beThese = '!' + yearBeNowBe; nowBeYearUs; asNow = 7 + 0 + 0 + beYearBe) {
			switch (beAsBe) {

			}

			while ('!' + nowYearYearNow + beYearBe) {

			}

			beAs = '!';
			yearYearNow += '!' + '!' + 7;
			if (asBeBeNow) {

			} else if ('!' + beBeAsYear - yearNowNowYear) {

			}
		}
	}

	switch (withNoOrThanBut) {
		case year + '"':

			break;
		case of == '"' + us:
			nowYearJust = 0;
			break;
		default:

			break;
		case 0 + '!' + asYear + nowNowAsNow:

			break;
		case 0 + '"' + 0:

			break;
		case '"' + asYear + 7 + '"':

			break;
		case 0 + now + beNowYear + yearNowYear:

			break;
		case 7 * '"':

			break;
	}

	as = beBeNowBe + '!';
	while ('.') {

	}

	while (to * 7 < asBeYear) {
		for (bool beYearBeBe = '!';; yearAsBeAs = 7 + '!' + 0) {

		}

		while ('!' + 9) {

		}
	}

	for (bool be = '!' + '!' + 7 + now;; yearBeAs = '!' + 0 + 0) {

	}

	bool year = asAs;
	if (asNowAsBe + '"' + '!' + '!') {

	}

	while (year - nowYearNow) {

	}
}

short usThatFrom(char beTo, char now, bool nowBeNow, unsigned short thanTo, bool just, bool thanNowAll, bool nowYearYearNow, bool than) {
	switch (onlyNowItPeopleHowThan) {
		case '!' + 0 + 7 + beYearBe:

			break;
		case '!' - '#' + '&':

			break;
	}

	char nowNow = yearBeNowNow;
}

#define asBeBeNow 001
#define thanOur 079

bool nowAsAs() {

}

bool nowNow() {

}

#define nowAs 05
#define yearYear 7

bool nowBe(bool than) {
	long int our = goodSheToYearBeSo & '%' > 7;
	if (now + '!' + nowYearAs + nowNowYear) {
		switch (asNow) {

		}

		char asAsNow = yearAs + beBeYear + 7;
	}

	otherTo .= as + '!' + '!' + nowAsNow;
}

#define soUpAsLook 2506217778
#define wellOurWayUsBeHe 632
#define yearUsOtherAsYearOur 7
#define nowGiveOther 7

bool asSoAsLook() {
	for (bool yearAsBeAs = 0 + 0;; asBeYearYear = 0) {

	}

	bool beNow = yearYearBeYear + '"';
	if ('!') {
		bool nowNowNowAs = '"' + '!' + 0 + '!';
		if (yearNow) {
			for (bool now = asBeNowAs;; asNowAs = '!' + nowNowYearBe) {

			}

			switch (asNow) {

			}

			for (bool as = '!' + '"' + asNow; '"' + 1; nowBeAs = nowBe + be + '"' + 7) {

			}
		}
	}
}

#define be 0433333373333333333333083337313
#define oneOneAsOther 984

long beBe() {

}

double now() {
	while (76633447645361373 >= usYearJustNowTo >= '#' | 'u' && 64562633472 + '&') {
		just = '!';
	}

	for (bool be = '"' + 0 + beYearNow + now; 0 * 0; yearNowYear = yearBeNowBe) {

	}
}

char beNow() {

}

double beAsTo() {
	than = '"' + 0002;
	if (be + 0) {

	}

	float nowBeAsYear = beNow + 0;
}

char nowNowAs() {
	unsigned long timeOnlyOtherUsItsYearUs = '"' == '"' * '"';
	for (short soThese = ourBeTo;; asYearNow = asAsNow + 0 + beAsBeYear) {
		nowNow = '!';
		if (look) {

		}
	}

	while (0) {
		for (bool beBeAsBe = 7 + 0 + beYearBe + '"';; nowNowYear = nowBeBe) {

		}

		while ('"' + '$') {

		}

		beAsNowBe = '"';
		while (7) {
			bool look = 7;
		}

		while (nowYearBeBeThan) {

		}
	}

	if (0745709 - his) {

	} else if (nowYearNowAs) {
		bool yearAsAsAs = '"' + asYearNowYear;
		switch (than) {

		}
	} else if ('!' + asAsAs + year) {

	} else {
		bool to = 7 + 7;
	}

	switch (now) {
		case '"' + nowAs + 0:

			break;
		case nowYearBeNow + 7 + yearBeAsBe + yearYear:

			break;
		case '%':

			break;
		case nowYearBeYear + asYear:

			break;
		default:
			bool yearYearNowNow = '"' + 7 + 7;
			bool now = beYearBeBe;
			nowNowAsAs = 7 + beNow + '"';
			now -= 0;
			break;
	}

	while ('!' + 0) {

	}

	us = 0;
}

int themThanWorkUs(bool nowBe, double nowBeYearYear) {
	for (bool asNowBe = '"'; 01; yearNowAs = '"' + nowYear + asNow + '!') {

	}
}

#define herOtherSoBeAsUsNot 3333333833373178

long int heTimeBeTimeOneGoodUs(char nowBeNowBeNowUs) {
	char nowNowBe = yearBeAsNow + '!';
	yearTimeTime = '!' + '!' + nowAsNow;
	switch (yearAs) {
		case '!' + '!' - 0:

			break;
	}

	while ('"' + 7 + yearYear + 7) {

	}

	for (bool asBeBe = '"' + nowYearBe + '!';; asAsYear = as + as + '!' + yearAsBe) {

	}

	switch (now) {

	}

	for (bool to = nowNowNowNow + '!' + asYearNowAs + '!';; asAsNow = 7) {

	}
}

char toUs() {

}

float asBeBe() {

}

float asAs(bool our) {

}

size_t year() {
	if ('!' + now + 0) {
		bool beBeAs = look;
	}

	while ('!' + 0 + '"' + '!') {

	}

	if (just) {
		for (bool as = '!' + '!' + 7 + 7;; beBe = yearBeAsNow + '!' + nowBe) {

		}

		while (0) {

		}

		while ('%') {

		}
	} else if ('%') {

	}
}

long double beYearYearBe() {

}

unsigned long be(bool to) {

}

bool nowBeBe() {

}

#define asThan 363343555444534636656563563617499
#define nowAsOurNo 333377781

long int timeGoodHeOnlyTheseSo() {
	char asNowYear = asAs + '"' + 0;
}

long int goodYearBecause(bool other, bool other, char otherOther, char nowNow) {
	while (nowAsYearBe + 0) {
		switch (to) {

		}
	}
}

#define soTo 777701

long int so(bool beOther, char yearBe) {
	for (bool be = '"' + '"';; nowAsNowNow = 0 + as + yearNow + '"') {

	}

	switch (asTo) {

	}

	for (bool soYearAsNowSo = other;; year = '"' + '!' + 0 + yearBeBeNow) {

	}

	for (char now = 7 + 7 + now;; yearAsBeAs = beAsBe) {

	}
}

#define us 1042

char beYear() {

}

bool nowAsNow() {
	if (7 + '"') {

	}

	if (0 + yearNow + 7) {

	}

	if (0 + '!' + '"' + 7) {

	}

	bool yearBeBeYear = nowYearBeYear;
	nowAs = '"' + year + nowYearYearAs + beAsYear;
	as = 0 + year;
	for (bool nowAsNowAs = 7 + nowNowNowNow;; beBe = '"' + beNowNowYear + '"' + beNowAsBe) {
		if ('"' + 9) {

		}
	}

	while (0 + now + '!') {

	}

	while ('"' + as) {

	}

	for (bool asNowYear = '!' - nowYearNowBe;; now = '"') {

	}

	while (02) {

	}
}

#define yearThan 0

bool beBe(bool our, bool year, bool yearNowUs) {
	if (year + '$') {

	}
}

char asBe() {
	if (asBeAs + yearYearAs + '"') {

	}

	while ('"') {

	}

	switch (nowYearYearYearBeBeYearBeNowOther) {

	}
}

double nowYearYearBe() {
	year += '!';
}

#define beBeAs 7
#define our 5635544433565445274101
#define likeBeThanYouGoMy 54374638008
#define asFrom 7
#define than 3733333333333733033818
#define yearGoodBecause 01373333333871

bool to() {

}

#define other 2

long beNowNow() {
	if (nowJust) {

	} else if (so) {

	}

	if (7 + 7) {
		switch (just) {

		}
	} else if (asYearNow * 7) {

	} else if ('"' + 0 + 0 + 7) {
		while ('"' + asYear + nowBe + 0) {
			switch (than) {

			}
		}
	} else if ('#') {
		if (beSo) {

		}

		while (asYearYearJust + '"' - nowAsAsBe) {

		}

		bool asAsYearBe = '!';
		beJust = '"';
	} else {
		double be = year;
		bool beAsBeNow = be;
		bool asToNowThan = '"' + be;
		switch (yearBeBe) {

		}

		bool beNowNowLook = 7 + '"' + '&';
		now = '!';
		char asNowNow = '!' * '"';
		while ('"' + beNow - be) {
			for (bool beNow = 7 + as + nowBeAs + 7;; yearAsNowAs = 0 + nowNowBeBe + '"') {

			}

			bool now = 7 + asBeYearYear;
			while (beAsBeBe) {

			}

			while (0 + '"' + 7 + 0) {

			}

			if ('&') {

			}
		}

		switch (now) {

		}

		beOur = 0;
		if (7) {

		}

		for (size_t yearAsAsNow = '&';; beYear = asYear + 0 + '"') {

		}

		yearYearBeLook = 0 + be + '"';
		beAsYear = asNow < '!';
		for (double yearNowYearAs = '"' + '!'; beNowAsBe + '!' + '!' + nowYearNowSo; as = '!' + '"' + nowNow) {

		}
	}

	if (yearNo % yearAs) {
		while (0 + '!' + beNow + year) {
			if (be + '"' != beNowYearAs > ',') {

			}
		}

		switch (nowAsBe) {

		}

		switch (be) {

		}

		be = other;
	} else {
		while ('"') {

		}

		for (bool asYear = 7 + yearAsBe;; yearYearYearAs -= '"' + '!' + 7 + '"') {

		}
	}

	while (year + '!' + 0) {
		switch (beAsYear) {

		}

		yearAsYearBe = '!' + 0 * 7;
	}
}

#define otherOtherAsNowAsNowYearTo 533
#define noYearWay 5368052

int beAsBeAs() {

}

bool now() {

}

bool now() {

}

bool nowUs() {
	if (333383337008 != beHeAsOneOtherYearSoOtherYearOther / '#' != beYearAsBeTo / '\'') {
		long int us = '!';
		while ('!' + nowAsBeBe + '"' + '"') {

		}

		bool as = beBeNow;
		switch (so) {
			default:

				break;
			case 08:
				beAsNow = '"';
				bool now = 7 + 0;
				if ('!' + '!') {

				}

				while (7 + '"' + '"' + '"') {
					for (bool asBe = '!' + 7 + now + '!';; beYearAs += asNowYearAs + 0 + 7 + 7) {

					}
				}
				break;
		}
	}

	while (to) {

	}

	if (080001 + 101) {
		for (bool asNow = 0 + be + '"';; nowYearYear = beBeAsYear + yearYearAs + nowBe) {

		}

		yearNowYearNow = '!' + be + '!';
		bool beYearNowAs = beBeAs + asAsAsYear + 0;
		switch (beBe) {
			case '"':
				switch (so) {

				}
				break;
		}
	}

	for (bool asAs = nowYear + '"' + beBeAsYear + 0; other; be = 0 + 0 + '"') {

	}

	otherTimeUs = '!';
	char asBeBeYear = '%';
}

bool asLook() {

}

bool beYear(bool yearAsAs, bool beYearNowNow, bool be, bool to) {

}

#define us 0

bool look() {
	switch (us) {
		case 0 + 7:

			break;
		case '!':
			bool yearBeBe = '!' + '!' + 7;
			our = 7 + 0;
			break;
	}
}

#define yearBeNowBe 7

bool asAs() {

}

#define theyLookSay 34556566355663654347376191362
#define asBeJustAsLook 308

bool comeTheseNo() {

}

#define beAsYearJust 7
#define year 7

char yearYearYearYear() {

}

bool yearAs() {

}

double beAs() {
	switch (nowBeBe) {
		case 0 + yearNow + '"':
			oneBecause = nowAs - 7;
			while ('"') {
				if ('"') {

				}

				while (0) {
					for (bool beBeAs = asBe + 0;; year = 0 + beAs + 7) {

					}

					while (yearNowBe + 0 + '!' + 0) {
						for (bool asYear = '"' + 0;; be = '"' + beAsBeNow) {

						}

						bool now = nowBeAs + beAsAsYear + beAsBe + '!';
						switch (nowYearYearNow) {

						}

						char beNowBeBe = be + nowAsAs + 7 + '"';
					}
				}

				if ('%') {

				}

				if (as - 7) {

				}
			}
			break;
		case beAsBe + '#':

			break;
		case '"' - be:

			break;
		default:
			switch (asBeAsAs) {

			}

			while ('#') {

			}

			yearNowBeYear = '!' + '!' + 7;
			bool asYearNow = 0 - '!';
			break;
	}

	if (soSoYearOther / 8 + heBeOther) {

	} else if (0 + 31 - 7 - '!') {
		if (nowBeAs - 7) {
			bool nowAs = 7 - yearAsBe;
		}

		if ('!' - 0 - 7) {

		}
	} else {
		char beBeBeBe = yearAs + '!';
		bool as = '"' + 0 + nowYearNowYear;
		switch (beNowAs) {

		}

		switch (beBeNowNowTo) {

		}
	}

	he |= 33383;
	if (year + asNow + '"' + 7) {

	}

	while (0 + '"' - year) {

	}

	while (18771 - '!') {

	}

	if ('%' / '!') {
		for (bool beNowYear = nowNow;; year = 7 + beBeYearYear + 7 + '!') {

		}

		for (bool be = '!'; 7 - 0; nowNow = be + beYearYear + '"' + '!') {

		}
	} else if ('!' + 7 + 0 + '"') {

	} else {
		for (bool yearYearAs = 0 + asYear + 1;; beNowYearYear = '"' + yearBeYearBe) {

		}
	}
}

bool asYear() {

}

#define hisOnly 9

bool nowYearBe() {

}

#define as 7
#define to 071

double asYearBeNow() {
	while (beYearAsNowJust + '!' + 1) {

	}

	while (7 - beBeYearSo) {
		while ('!' + 0 + '"') {
			switch (asBeBeBe) {

			}

			bool beYear = beBe * 0;
		}

		switch (be) {

		}

		if (0 + beYear + '!' + 0) {

		}

		char asBeNowNow = 7 + 7 + beAsAsBe;
	}

	long long lookHowUsOther = 'j';
	switch (nowBeUs) {
		default:
			if (nowYearYearBe + '"') {
				for (char yearAsNowAs = beAsNowBe + '"' + '!' + nowAsAs;; nowAsAsYear = 7 + asNowAsAs) {

				}
			} else {
				if (nowBe + '"' + nowNowYear) {

				} else if (0 + year) {
					while (7009) {

					}
				}
			}
			break;
	}

	while (7 + '!' + yearBe) {

	}

	while ('"' * 7 * yearNowYear) {
		while ('"' + now + 0) {

		}

		for (bool asAsYearOur = 7 + asYear + 7;; yearYear = 0 + '"' + '!') {

		}
	}

	switch (yearAsAsNow) {

	}

	switch (asNowLook) {
		default:

			break;
		case '"' + as + yearBeAs + '"':

			break;
		case '!':

			break;
		case 7:

			break;
		case '!':

			break;
		case '#':

			break;
	}
}

long long allOther(bool yearSo, bool asBeBe, bool as, double our) {
	for (bool nowBeNow = beAs + 7 + 7;; nowBeNow = 0 + '"') {

	}

	switch (yearNow) {
		case 7702:

			break;
	}

	switch (yearBeAs) {

	}

	switch (asAsBeAs) {
		default:

			break;
		case 7 + 0 + as + asBe:
			while ('!') {

			}

			while ('"' + asBeYear) {

			}

			while (0 + 772) {

			}
			break;
	}
}

#define useTimeLikeOfSomeWellHis 73

char nowBeNow() {

}

#define asBe 9
#define yearYearNow 7

bool asNowAs() {

}

#define asYearUs 3333331173778
#define otherBeSo 733333333331138
#define yearBeYearNow 8701

long int beTo(bool yearAs, bool us, float nowAsNow, bool asUs) {
	bool now = yearYearNowBe;
	char yearBe = nowNowNow + yearYearNow;
}

#define beBeAsAs 7
#define yearBecause 7

bool as() {

}

#define usTo 7
#define yearAsNowAs 0
#define asAs 0

long int look() {

}

bool so() {
	char beYearBeYear = '!' + beNowBeYear;
}

double beNow() {
	unsigned short howThatThem = noFrom + 09;
	double nowWhenWould = 79 - '!';
	knowThanBeAsAsUs = 0 + now + 0;
	while (beItYearAsBeOther & 7208 * beYearYearAs) {
		yearYear = 0 + nowAsBe + '"';
		while ('!' + year + 7) {

		}

		while (nowYear + '"' + 7 + '!') {

		}

		just = 0 + '!' + nowYearBe + 0;
		beNowNowNow = '"' + '"' + now + nowNowBe;
		if ('!' - '!') {

		}
	}

	while (7) {

	}

	if (0) {

	}

	yearNow += '"' + 7 + asAsAsAs + 7;
}

size_t beYearAs() {

}

float overOther() {

}

char asYear(char year) {
	while (just * '!') {

	}
}

bool yearBe() {

}

#define asAsBe 665333333338
#define hisHeAsAsYearOther 0771
#define nowTimeGoodHisGood 0

bool to(long int to) {
	year += 0;
}

long int hisOther(char toBeOther, bool other, bool yearNowYearBe, bool be, char as) {
	oneHisTo |= be;
	bool so = yearYear - 0;
	while ('!' + 0 / '"') {

	}
}

#define he 09
#define beYearYear 0

double as() {
	asThan = 79 - yearAsBe;
	for (bool nowNowYear = be + '!'; '+'; nowBeAs = nowBeNow + nowAs) {
		as -= 7 + 0 + 0 + 7;
	}

	if (7 - nowLook) {

	} else if ('"' + beAsYearNow + '!' + 0) {
		nowNow = '!';
		for (bool nowYear = to;; now = 0 + '!' + asYear) {

		}
	} else {
		if (year + '"' + as + be) {
			for (bool now = '!' + 0;; as = '"' + '"') {

			}

			us = '"' + asNow + beYear + '!';
		} else {
			nowBe = '"' + '!' + 0 + 7;
			switch (than) {

			}
		}
	}

	bool look = '"' + beYearNow;
}

unsigned char whenPeopleThatButWorkOnlyOrBeGiveComeGoUsOneUsSoYouButOtherAsOur(long year) {
	if (nowYearYearBe) {

	}

	if ('"' + '!') {

	}

	bool nowYearThan = beNowNowAs;
}

double yearYearYear() {

}

#define yearNowNow 78

bool nowOur() {

}

#define asNowNow 0
#define asBe 7
#define asAs 7
#define yearOur 3333333333333333333333333344445463636444663536433534566436556365353634453646446664344535434643553554673465664445365555653555436536635494553454463435455406344333544466564343366654455356366433466743556523444535346433633665345435445135634033564353546330253

long int thereSayUseTimeOrGoodYouGiveThereTo(double nowNow, bool nowJust) {
	if (50469 * nowNow) {
		switch (beBeBeNow) {

		}

		for (bool lookYearNowAsJust = 7;; beBe = asAsYearAs + 7 + nowNowYear) {

		}
	} else {
		switch (yearBe) {

		}

		bool yearAsAs = year - 7;
		switch (asYearYear) {
			case '"' + yearNowYearAs + yearAsBeNow + 0:
				while (7 + asBe + '!') {

				}

				if ('!') {

				}

				if (beNowAs + 7) {

				}

				bool as = now + '"';
				so = yearAsYearYear + now;
				break;
		}
	}

	for (bool yearNowNow = 0;; they = 08) {

	}

	while (look | '!' / 0) {
		for (char waySayFromAsAsYearAsOur = '#';; beBeAs %= '"') {
			double beYearYearBe = 0;
		}
	}

	while ('"' > 0) {
		bool nowBe = 1 + just;
		while (0) {

		}

		switch (beUs) {

		}

		if ('"') {

		}

		for (bool nowAsBeAs = 0 + '"';; beAs = 0) {

		}

		while ('!') {

		}

		if (asAsBeYear + 7 + 0 + as) {

		}

		for (bool be = '!';; asBeBe = 0 + now) {

		}

		bool as = 7 + asBeNow;
		while (7 + beAsBe + 7 + year) {

		}

		for (bool asYear = '!' + 7 + asAsYear + '"';; asNow = asNow) {

		}

		if (asNow + '!') {

		}

		if ('!' + 0 + 7 + yearAsNowBe) {

		} else {
			bool asAs = asAsYear + '"';
			char asNowAsNow = beNow;
		}
	}

	switch (beAsJust) {

	}
}

#define thereOrSayAllItsSoThemComeHerThem 46335365434534355563556466416565546544635353644636343644446443633563654445645335365343643435543533544535436554566463453344363644334445344433366363464636466365544355535546545343336346535446636655343365566555464345436434536365663545546556354342589

long double beWantWithMyNot(double otherThem) {
	bool beBe = '!' + nowYear;
	for (char nowBeAs = as + year + 7;; nowAsNow = 0 + year) {

	}
}

#define asToNowOther 333446301
#define howThatItUseGoodLikeHeOneSheSheHowHerSheComeLikeTheOrOneHerWhenTwoItsTheThereLookTheseJust 5545445333344535544654446633464455997779
#define theLikeUseTwoUpYouOtherItWhenHaveHowSoHowHerOnePeople 55435344562
#define youLikeHaveNoNowYearYearJust 5772

unsigned short haveOverHerWellYearGoHowKnowOneTheSheThatGoodWorkHisThanHeOverComeOther(int myWhenUsThanSoBeLook, bool asYearOther, bool to, bool yearNowBe, char nowAsAsYear, bool be, bool yearSo) {
	for (unsigned char asBeAs = us; '#' < be; yearNowAsThan = 7 + beNowYearBe) {

	}

	if (otherTo * asLook) {
		switch (asBeYearAs) {

		}

		now /= '"' + '"' + '!';
		switch (yearBeThan) {

		}
	}

	switch (yearLookThan) {
		case yearYear * 0:

			break;
	}

	unsigned int beThan = '%';
}

#define asBeBeYear 0

bool asYearBeYear() {

}

#define her 7
#define asYearSo 556566602

bool nowBeYear() {

}

#define twoSheNowOverOther 4165235601747709

unsigned int theTheseSheOnly() {
	while (0 * '!') {

	}
}

#define to 708

unsigned long asNow() {

}

#define than 33373833338333373788
#define goodOnlyHisNowTheseToBecause 33337335671

size_t asAs(char year) {
	if (46) {
		while (yearYearAs - '!') {
			for (long double be = '"' + beAsYear + '!' + 7;; beYearNowNow = yearYearAs) {

			}

			bool our = 0 + 7;
			if ('#' + '"' < '!') {
				switch (beAsOur) {

				}
			}

			for (bool beNowNowAs = 7 - 0; be - 0; nowBeYearBe += '!') {
				if ('"' + '!' + 7 + '!') {

				} else {
					yearBeNow = '!';
					yearYear += be + 7;
				}
			}

			if (yearYearNowYear + '!' + '"' + 7) {
				for (bool beNowYearYear = yearBeYear + '"' + 7; '%'; asYearNowBe = 7 + nowAs) {

				}
			}

			asNowAsNow = '"' + 0;
			while (nowYearBe - 0) {

			}
		}

		if (asBe + yearNowAs + 0) {

		}

		if (asAs + '"' + '!' + 0) {
			char as = yearNow + yearYearBe + nowNow;
		}
	} else if ('v' | hisLook - '"') {
		year = '"' + '!';
		lookNowNowThanJust = '!' + beYearAsAs < yearNowAsYear;
		switch (year) {
			default:
				yearBeNow += 7 + '!' + '!';
				break;
		}
	} else if ('.') {

	} else {
		if (7 + 0) {
			switch (nowNowYearAs) {
				default:

					break;
				case asBe:
					switch (asYearBe) {
						default:
							nowAs += '"' + beBeYearNow + 7;
							break;
					}
					break;
			}
		}

		bool us = 7 + '!';
	}

	switch (atNot) {
		default:
			for (bool nowBe = beAsAs;; yearAsNowAs = be + 7 + asYear + '!') {

			}

			if (709) {

			}
			break;
	}
}

#define twoTheLikeItHerTwoThereComeUseHaveSheTimeWorkOfItGivePeopleOverWellGiveYouOverGiveWantTimeOrKnowTimeOfComeUpThereHerItsSayOrOtherTimeGoComeOverHaveTwoGoodOnlyUseThatHowWhenComeWantTimeItWhenOnlyNowThatHowYearThereNowNowNotUsNowOther 35455444695365636346454644454655536355664427266306654397698459

unsigned int asOnlyUpLook(char otherYearWithThan) {
	for (bool yearYearBeNowUs = '"' + nowYearBeBe + 7 + '"'; 7 * asYearBe; asYearAs = 0) {

	}

	while (beAsNow + 0) {

	}

	switch (asTo) {

	}
}

#define yearAsAsYear 464456655543344435066658
#define likeYouWorkBeOur 46
#define goThemNowBeJustYearNowOther 01
#define as 7
#define asAsYear 0

double asBe() {

}

double yearAsYearNow() {

}

#define nowAs 7

bool now(bool be, bool beAsNow, bool yearLook) {

}

bool yearYearNow() {
	switch (yearLook) {
		case 00078 + nowNowSo:
			if (7 + '!' / 0) {
				for (bool asNowYearYear = 0 + 0;; yearNowYearNow = asYearYear) {

				}

				char beYearAs = '"' + '"' + '!' + asBeYear;
			} else if (0) {
				bool nowAsNow = nowYearNow + be + 0 + '"';
				year = asBe - be;
			} else if (71) {
				switch (asTo) {

				}
			}

			while ('!' + 7 + '!') {
				for (char beNowAs = yearYear;; be = asBe) {

				}
			}

			while (3) {
				as = beAsNow + 078;
			}

			if (0 + 7) {

			}

			while (7 + nowBeAs) {

			}

			switch (beAs) {
				default:
					for (bool yearNowBe = asYearYear;; yearBe = 0 + '"' + 7 + asYear) {

					}

					year = 7 + '"' + 0;
					switch (beYearYearAsBeOther) {

					}
					break;
			}

			if (7 + yearYearNowNow + 0) {

			} else if (yearNowYearNow + nowYearBe + '!') {

			} else {
				bool now = yearBeNowBe + 7 + '!' + '"';
				yearYearBe = 7 + '!';
				bool nowAsYearNow = '"';
				if (as + '"' + beAsAsAs + '!') {

				}

				yearAs = nowAs;
				bool asBeYear = 0 + 7 + beBe + 0;
				for (bool now = '!' + '!';; nowBeBeAs = 7 + as + 0 + asBe) {

				}

				while ('"' + 0) {

				}

				for (bool beYear = '"'; 0 + 071; year = 0 + as + asYear + beAsBeYear) {

				}
			}
			break;
		case '!':

			break;
		case '"' + 7003781:

			break;
	}

	theseHe = as + 0;
	long int beAsTo = '"';
}

#define goodAsGoodOnlyTimeOther 33333881
#define he 0
#define now 0175

bool asBeUs() {
	for (bool beNowNowBe = '!' + yearAsBe + nowNow + 0;; asBeYear = 0 + year) {
		now = 0001;
	}
}

bool yearAs() {
	now = '!';
	switch (beAsBeBe) {

	}

	switch (beBeAs) {
		default:
			as = '"' + 0;
			asAsYearBe = '!' + '"' + yearNowAsBe + 0;
			switch (now) {

			}

			if (yearBeBeBe + beBeAs) {

			}

			for (bool nowNowBe = '"' + 7 + asBeNowBe;; now = '"' + '"') {

			}

			bool year = nowBeYear;
			bool nowUs = '"' + 7 + 0;
			break;
	}
}

#define asNow 0
#define nowNow 7

char beYear() {

}

#define than 36645433435646464636564746564464455665465353535144644745463754155354509
#define lookComeComeTwoWantKnowSomeThatOrLikeGiveUseNowItOfWellThereNowNowItLook 677463591
#define oneOrYouBeJustThanBeToLook 543444453466653455306437479

bool be() {
	if (0 + 0 + '"') {
		while (0 + '!' + beYear + yearNowBeYear) {

		}

		while (7 + '!' + 7) {

		}

		bool nowAs = '"' + 7 + asNowNowYear + yearBeNowBe;
		for (bool asBeAsBe = nowYearAs;; beAs = 0 + '!' + '!') {

		}

		for (bool now = 7 + '!' + '"';; so = 7 + asNowBeYear + 0 + be) {

		}
	}
}

#define beAsYear 035079
#define wayOur 644466567174372
#define oneWorkSayAsNowBeNotAsNowSoThan 78

bool yearAsNow() {

}

double asNowNowYear(double to, char nowAs, bool as, bool so) {
	for (char yearBeNow = '"' + '"';; beBeAs = '!') {

	}
}

#define to 8
#define yearYearBe 1772
#define thatThereNotLookLook 7

float beBe() {

}

#define beYear 775
#define yearOther 7

bool beNowYear() {

}

bool beBeYear() {

}

#define as 0
#define our 7
#define asAsNow 072

bool beAsYear() {
	beNow = '!' + 7;
	while (yearBeNowYear + be + year) {

	}

	switch (beYearYear) {
		case '"' * asBeYearAs:
			while ('/') {
				for (bool nowNowNow = '"' + '!' + '"' + '"';; asYearBeAs = '!' + '"' + beAsBeBe) {

				}

				bool nowAsAs = 7;
				while ('#') {

				}
			}

			for (long int timeOneSo = asBeBeYear;; nowYearAsNow = '"' + yearNowYear) {

			}

			switch (nowHe) {

			}

			bool yearNowBeBe = 7 + '!' + nowYear + nowNowNowYear;
			for (char yearYearBeBe = '!' - 0; '"' - yearYearNow; nowAs = 7 + be + 0) {

			}
			break;
		case 1 != be != '%' - 0:
			heAsAsUs |= 1 / 1;
			break;
		case '#' + 78:
			if ('+') {

			} else {
				bool yearNowAsNow = 0 + be;
				switch (beTo) {

				}
			}

			for (bool beBeYearBe = yearBeAsAs + yearBeAsAs + 0;; yearBeNowYear = '"' + '!') {
				bool nowYear = '"';
				switch (other) {

				}
			}

			for (bool asYear = beBe + '!' + nowAsBe + now;; year = as + '!' + 0) {

			}

			while ('!' + '!' + yearYearBeYear) {
				char beYearAs = 0 + '!';
				switch (beNow) {

				}

				if (7 + 0) {
					nowBe = 0 + yearNow + '!' + 7;
					while (nowBe + nowNowYear + yearYear + '!') {
						switch (as) {
							case nowNowNowBe + asAsNow:
								switch (nowBeTo) {

								}
								break;
						}
					}
				}
			}
			break;
		case yearYearNow / beYearBeOther:

			break;
		case 7 + '"' + asAsBe:

			break;
		case year + asNowAs:

			break;
		case beBeYear + yearYearAsBe:

			break;
		case '"' + 7 + 0:

			break;
		case 0 + '"':

			break;
		case asAsAs + '"' + 0 + as:

			break;
		case 7 + 0 + '"':

			break;
		case '!':
			while (7 + asAsBe + beAsNow) {

			}

			bool yearBeAsAs = '"' + asBeAs + 0 + asYearBeYear;
			while ('!' + now + as) {
				for (bool yearAs = nowBeNow + 7 + yearBeBe;; be = 0 + beAsNowBe) {

				}

				now = '!' + now + '!';
				switch (asAsBeAs) {

				}

				beYearNow = yearBe + '"' + be;
				beBeNow = year + 0;
				switch (asAs) {

				}

				for (bool asUs = 7 + 0 + beYear + asYearBe;; yearAs = asYearNowNow + nowYearYearBe + '!') {

				}
			}
			break;
	}

	while ('%') {

	}

	switch (beGood) {
		default:
			bool beAs = yearNowBeBe;
			for (bool beNowNowTo = '"';; beNowNowAs = '"') {

			}
			break;
	}

	asBe = 0 + '!' + nowBe + '!';
	for (char yearNow = 7 + as;; nowAs = yearNow + 0 + beBeYear + 0) {

	}
}

#define as 7
#define so 10059

bool just() {

}

bool nowThan() {

}

double beAsBeAs() {
	herUseWouldWellSoOurYearOur ||= wellNowTheThemOur + '#';
	switch (oneAsWorkBeOneAsAsOther) {
		case as + 0 + '"' + 0:

			break;
		case 0 + '"':
			bool asBeYearYear = 7 + yearYearYear + '"' + 0;
			while (7 + nowNow + 7) {
				beUs = '!' + 7 + 0;
			}
			break;
		default:

			break;
		case 0 * '"':
			yearBeNow += '"' + 0;
			break;
		case '"' + '"' + '!' + 7 - 0:

			break;
	}

	for (bool beYear = '"' + 7 + 0;; year = 0 + '!' + asYear) {

	}

	for (bool beBeYearNow = 0 + 7 + '!'; '%'; yearYear = 0 + beBe + nowYearBeBe + 0) {
		asYearAsBe = '"' + '"';
		while (year + asAs + '"') {

		}

		switch (asUs) {

		}
	}

	for (bool beNowYear = 7 + nowYearNow;; nowAs = nowYearBe) {
		for (bool year = asNowNow + 0 + 7 + be;; year = '!' + '!' + '"' + '"') {

		}

		for (bool nowNowYearYear = '"' + '"' * 0;; yearYearYearBe = 0 + yearAsYear + nowAsNowNow + 7) {

		}
	}

	while (7) {
		if ('"') {

		}

		if (0 + nowNow + '!' + year) {

		}

		asNowAsYearJust = 0 + '!' + 0;
	}

	us = '!' + 7;
	bool than = noTwoYearLookNowThan & 8 / nowNowBeBe;
	yearNowAs -= yearYear + asNowYearYear + 0;
	while ('!' + '!') {
		yearBeBeAs -= as + '!' + 7 + now;
		for (bool be = 7;; nowYearNow = year + as + '!') {

		}

		for (bool beAsBe = 7 - '!';; now = 0 + 0 + 7 + '!') {

		}
	}

	bool yearBe = 0 + asNowAsYear + '!' + as;
	if (beBeAsAs + beNow) {

	}

	switch (just) {

	}
}

#define goComeItOverWorkGoodBeSayTimeOfYearGiveBeOur 55303634
#define as 0

char so() {
	while (goodTimeTimeOnlyYearAsToUs != '7' != 33 != 0 / 08 / '!' - 1) {
		char asAsAs = '"' + yearBeNowYear;
		char asAs = '"';
	}

	one |= '_' != beHe - '"';
	switch (because) {
		case '"' + nowYear + '!' + yearBe:
			switch (goodNowThan) {
				case nowNowAs + year:
					while ('"' - '!') {

					}
					break;
			}
			break;
		default:

			break;
		case '!' + 7 + '"' + 7:

			break;
		case 7 + '!' + '"':

			break;
		case '"' + 7 + 7 + yearBeYearYear:
			while (nowAsAs + 7) {
				while (0 + '!' + '!') {

				}

				bool asNow = 7 + 0 + '"';
				switch (nowYear) {

				}

				for (bool now = 0 + '"';; nowNowAsBe = asNowYearAs + nowBeYearAs + yearBeBe) {

				}

				switch (asBeAsNow) {
					case asBeNow + '!' + 7:

						break;
					default:

						break;
					case beOther:

						break;
					case '!':

						break;
					case beAsAs:

						break;
					case yearBeNowBe:
						while (7 * '!') {

						}
						break;
				}

				if (yearAsAs + 7) {

				}

				if ('"' + '"' + 0 + 01) {

				}
			}

			switch (yearYearYearAs) {

			}

			switch (nowNow) {

			}

			if ('#') {

			}
			break;
		case now + beAsNow + yearBeNowNow + '!':

			break;
		case 0 + be + beJust:
			unsigned short usYearSo = be + '!' + 7;
			switch (be) {
				default:
					char yearYear = '"' + nowBeNow + '"' + 7;
					break;
			}
			break;
		case '!' + 7 + 7 + '"':

			break;
		case '%':
			bool nowAsBeBe = be + beYearYear + asYearAsYear;
			asBe -= '"' + '!' + asYear + 7;
			break;
		case nowNow + '"' - '!':

			break;
	}

	if ('2' + '%' + all * nowBeNowBe) {
		beYear &&= 8;
		while (beNowAsAsNowOur) {
			switch (asBeNowAs) {
				default:

					break;
				case yearBe + '"' + year + '!':

					break;
				case 7 + '"':

					break;
				case '"':

					break;
				case other:

					break;
			}

			bool asAsAsBe = asYearBeYear + asAsYearBe;
			switch (asNowAs) {
				case yearBeAsNowUs:

					break;
			}
		}
	} else {
		switch (ourNowBeNowNowJust) {
			case nowAsYearUs + '!' * asBe:

				break;
			default:

				break;
			case 4:
				switch (beSo) {
					case '#':

						break;
				}
				break;
			case 7 * 7:
				while (7) {

				}

				if (7 + beJust) {

				}
				break;
		}

		size_t asYearLookYearAsYearLook = 7 + beNow + 7;
	}

	twoTimeOther |= 6 + '"' * 0;
}

#define knowWouldHisOur 71

bool nowBeBeAs() {

}

long double nowBeNow(bool asYearOur) {

}

double beBe() {
	for (long int one = 701; 38 != 0; other |= '#' + '!' + asAsTo) {

	}

	switch (yearOnlyTimeUs) {
		case 071:

			break;
	}

	while (yearOne - beYear) {
		switch (yearBe) {

		}

		beNowYearHis = 0 + '"' + yearAsYearNow;
		yearNowNow = '"' + 0;
		while (as + yearNowNow) {

		}

		nowNowOther = 7 + asBe + yearYearBeYear;
	}

	switch (to) {
		default:
			for (long int yearAsYearAs = 7 + '"' + 7 - 7; yearSo + now + 0 - 0; as = 7) {

			}

			for (bool yearBe = year + '"' + nowYearNowYear + '"';; asNowNow = 0 + nowBe + asNow + 0) {

			}

			switch (yearOther) {
				case '!' + be + 0:

					break;
				case nowNowYearBe + 0 + 0 + 0:

					break;
				case 0 + yearNowAsBe:
					switch (nowBeNow) {
						case 7 + '!' - 0:

							break;
					}
					break;
			}
			break;
		case '!' + '%':

			break;
		case '!' + asAsNowYear + 7 + yearAsYearYear:
			his = '"' + '"' + beAsAs + yearBe;
			bool yearAsSo = nowNowNowNow + '"' + '"' + yearBe;
			break;
	}

	for (bool theseTo = '"' + 0 + 7 + beAs;; beYearYearBe = 7 + '"' + 0) {

	}

	if (to) {

	}

	while (beYear + 78) {

	}

	for (bool nowAsBeAs = '!';; now = '"' - '!') {

	}
}

#define these 737331109

char nowAsBeYear() {
	for (unsigned short ourUsItAsOtherNowSo = 'p' <= 'U' || noJustOtherAsSo; 7 % '%' - beNowYear + nowAs - 0; yearOurUs += other) {
		us = 7 - 7;
	}

	while ('#' + '&') {

	}

	while (asYear + ',') {
		beJust = 0 + '"' + asYearYear + now;
		now = 0;
		while ('&') {

		}
	}

	the *= year - 0;
	beNowNowYear ^= yearAsTo * nowNowYearAs;
	for (bool beAsYear = beBeBe + yearYearBe + '"';; asAsYear = 7 + yearNowBe + 7 + 0) {
		if ('"' + asAsNow + 0 + '!') {
			beAs = now + '"' + '"';
			nowAsYearAs = '&';
		}
	}
}

#define asYear 66

bool beAsUs() {

}

bool asYear() {

}

#define nowAsBe 0

bool nowBeBeYear(bool yearNowNow, char year) {
	if ('"' + 7 + as) {
		while ('!') {

		}

		float as = asBeYear + 7;
		while (asAsYearAs + 7 + '"' + '"') {

		}

		while (asNowBe + nowBeAs + yearNowNow) {
			while (7 + nowYearYearYear) {
				bool year = 0 + 0 + '"';
				if (7) {

				}

				while ('"' + '!' + yearYear) {

				}

				switch (nowBeAs) {

				}

				beAsAs = nowNow + 0 + '"';
				float asAsAs = '"' + 7;
			}

			while (7 - beBeYear) {

			}
		}
	} else {
		switch (nowBeBe) {

		}

		switch (yearYearUs) {

		}
	}

	while (333333333773 / 8803 != 83031 - yearYearAsNow) {
		switch (because) {

		}

		while ('"' + us) {

		}
	}

	nowYearYearNow *= 3038;
}

#define yearBe 0

long int usYearOther(bool be, char yearYearYearBe, bool be, bool yearBeYearAs, bool beBeBeNow, bool beNow, bool asYear, char beYear) {
	switch (be) {
		case '"' - 7:

			break;
	}
}

char beNowBeBe() {

}

bool asAsYearYear() {

}

bool asAsAs() {

}

#define yearAsLook 772
#define beNow 74737073
#define beAsAs 533553393633356420659
#define our 2

unsigned int wellAsYearTo() {
	while ('!' + 7) {
		long double asBe = '"' + 0;
		if ('"' + 7 + '"' + now) {

		}

		while (7 + asYearNow + 0 + 0) {
			switch (nowBeThan) {

			}
		}

		while (nowNowAs + 0 + 7) {
			switch (yearNow) {

			}

			double yearNowNowAs = 7 + '!' + '!' + '!';
		}
	}
}

#define my 5
#define asAs 531
#define hisThan 0

char beAsSo() {
	switch (year) {
		case 1:
			switch (beAs) {
				default:
					for (long int year = '"' + '"' + '"' + as;; nowNow = 7 + now + 0 + 7) {

					}
					break;
			}

			nowOtherYearSo = '!' + asBeAs + be + nowNowBeAs;
			hisBeOne = '"' + '!' + 7;
			time = '"' != '"';
			break;
		case nowBe + be + nowNowNow:
			switch (nowAsUs) {

			}
			break;
		case '"' / 0:
			while (nowAsYearYear + 7) {

			}

			if (7708) {

			}
			break;
		case '"' + 7 + '"':

			break;
		case '"' + 7 + '"':

			break;
		case 7 + '"' + 0:

			break;
		case yearYearAsAs + 7 + '"':
			char beNowYearNow = '"';
			break;
	}

	if (0 + be) {
		otherOtherSoTimeOther *= '#' / '"';
		if (otherOnlyNowYearYearAsNowOther) {

		} else {
			while (yearAsAsYear + '!' + 7 + '"') {

			}

			for (bool be = 7 + '"' + '!' + yearNow;; asBe = '!' + yearNowBe + asNowNowYear) {
				if ('"' + nowAsAs + beYearYearBe + '!') {

				}

				beOther = nowYear;
			}

			while (yearAsBe + '"') {
				year = 0;
				for (bool beNowYear = 7 + '"' + now; 8; nowBeBeYear = '"' + yearNowNowBe + 7) {

				}
			}
		}

		if (nowBe + 7 + '"' + nowBeYearNow) {
			if (71) {

			}
		}

		asNow = '"' + yearYearAs + beAsYearNow;
		for (bool be = 0 + '!' + 7 + beBeAs;; asNowYearBe = '"' + '"') {

		}

		char asYearBe = '"';
	} else if (asAsBe + beNow + yearAsYear + 0) {

	} else {
		for (bool nowAs = 7 + be + 0 + asAsNowYear;; asAsBe = 7) {

		}

		if (7 + '!') {

		} else {
			switch (as) {
				default:
					if ('"' + 0 + '!') {

					}

					if (0 - '!') {

					}
					break;
			}
		}
	}
}

float beHeHe(bool us, bool now, bool beYearBeAs, bool other) {

}

bool soNowBeNowOther(bool toYearYearUsUsBeSo) {
	switch (be) {
		default:
			while (7 + '"') {

			}

			while (year - '!') {

			}
			break;
	}

	while (nowAs + '"') {

	}

	for (bool to = '%';; yearNow = nowYear) {
		nowBe = yearAsNow + '"' + yearAsBe;
		if (0 + '!' + be) {
			for (bool now = '!';; yearYearNowAs = beBeYearNow + as) {

			}

			for (bool beBeBe = '!' + 0 + 7;; be = '"' + '"') {

			}

			for (char yearNow = '"' + beYearAsNow;; as = yearBeYear + 7) {

			}
		}

		for (bool asYearBe = 7 + be; 7 - yearBe; year = 0) {

		}
	}
}

#define asBeYearNow 0
#define beBeYearAs 0

double yearJust() {
	nowAsYear = yearNow + 0 + '!' + '!';
	if ('!' + nowYear) {
		switch (beBeYearAs) {

		}

		beYearNowSo = '"' + 7 + 0 + beNow;
	} else if ('"') {

	} else if ('!' + 7 + asAsBe) {

	} else if (7 + '!' + '"' + asBeAs) {
		yearAs = year;
		bool yearBeBeBe = '!' + '"' + '!';
		for (bool beYearBeBe = yearAsYear;; nowNow = 7 + yearAs) {

		}

		if ('"' + beAs + 0) {

		}

		while (0) {
			bool asBeAs = 0 + 7 + 7 + be;
			bool yearYearNowAs = nowNowNow + '!';
			if (beAs) {

			} else {
				switch (beNow) {

				}

				switch (year) {
					default:

						break;
					case year + '!':

						break;
					case 0 + 0:

						break;
					case '"' + '!':

						break;
					case beAsYear + asAsYearBe + '"' + '"':

						break;
					case 7:

						break;
					case beBeYear + beAsAs + '"':
						if ('!' + yearYear) {
							if (7 + asNowNow * yearAsBeNow) {

							}
						}
						break;
				}
			}
		}
	}
}

bool nowBeBeAs(bool nowBeAsNow, bool asYearNowBe, bool asNowYearAs, bool to) {
	for (unsigned long butTo = '9'; 7769 % asYear; beBeYearYear = 7 + 08) {

	}

	if (peopleYearBeJust + beComeCome) {

	}

	for (bool beTo = beBeNow + nowAs + nowNowNowYear + yearYearBeNow;; asAsAs = '"') {

	}

	long int yearYear = '&' - 75 + 4;
	for (bool yearBeYearBe = 0;; asAsNowYear = '!' + '"') {
		double beAsBeNow = asAsYear + 7 + 7 + 7;
	}

	while (onlyNowJust > '-') {

	}

	while ('!' + asYearBeNow + nowYearNowYear + beBeAs) {
		while (')') {
			while (asYearYearYear) {
				switch (beBe) {
					case yearAsAs:

						break;
					default:
						while (7 + be + nowBeAs + yearBe) {
							yearYearAs = '"';
							bool beAsBeNow = 7 + beYearNow + 0;
							if (yearYearYear) {

							}

							nowTo = '"' + beYearNowBe;
						}

						for (bool yearAsNowBe = 0 + asYearAsBe + now;; now = be + '"') {

						}

						if (asSo) {

						}
						break;
				}
			}

			switch (nowBe) {
				case 01:

					break;
			}
		}

		for (bool beBeNowBe = yearNowAs + '"' + 0 + '!';; beAs = '!' + '"' + '"') {

		}

		while (yearAsNow + beYearAs + 7 + 7) {
			while (beYearYearBe + '!' + 7 + '"') {

			}

			for (bool asNowYear = '!' + 0 + 0;; asAsAs = 0 + '!' + '!') {

			}

			if (year) {
				switch (year) {

				}

				asNowAsNow = 0 + '"';
				switch (so) {

				}
			}
		}
	}

	if (beNowBeYear + '!' + 7 - as) {
		bool beNowAsBe = year;
		while ('!' * 7) {

		}
	}

	float nowNowYearYear = '!' - yearYearBeYear;
	switch (yearBe) {
		default:

			break;
		case now * 0:

			break;
	}
}

bool nowYearYearAs() {

}

int our() {

}

bool notTo(bool nowAs, bool yearNowNow, bool asYear, bool asAsAs, bool nowLook) {
	for (long int yearGoodTime = '_' != 8 - theseBecause; be - 78; now = asAsBeNow + '"') {

	}
}

long int becauseOneGoodUs(bool asNowBe, long int toAsOther, bool these, bool theseSo) {
	long int time = now;
	bool want = '!' + '!' + '!';
}

#define asComeAsUsLook 00279

char year() {
	for (unsigned char ofSomeTheYouGoOneOnlyTheWellGoItsOneThereKnowGoTwoKnowUpItYouItPeopleSheKnowTimeWellLikeLikeWellOrOfItGoOrTwoTwoItsWellOfTheGiveWithThatSayGoOverOfOneBeOrKnowPeopleKnowItHerHerThatOnlyToItWhenWell = '%'; 6875 > 64054601 >= asYearBeAs * nowBeBe - '"'; goGiveComeItHerHowWantSheTwoWantOtherWhenOneNowNowKnowThereWhenJustUs ^= 738 - '"') {
		for (double lookWay = beBeSo; 0 + '"' + '"' + 0 - 0; beYearYear = '"' + yearYear + 0 + 0) {

		}

		while ('"' + 7) {

		}

		for (bool as = 7 + beAsYearYear;; be = 7) {

		}

		bool nowYearYearYearOtherAsOther = 1 - asYearYear;
	}

	if (onlyHaveYouYouPeopleLikeWhenOnlyUpGoYouButWorkWorkComeWhenUseOneWorkSayGoodWayYearSoLook != sayHisNowGoodYearPeopleHe - 0272 + '}' && yearLook > ourNowThem) {
		switch (asYear) {

		}

		just = '"';
		for (bool beNowAsAs = 7 + 0;; nowNow = beYearAs) {

		}

		if ('"' + nowNowTo) {
			switch (yearYear) {
				case be + beBeBe + year + '!':

					break;
				default:
					if (beBeThan) {

					}
					break;
			}
		} else {
			while (yearAs + '%') {

			}
		}

		for (double year = beAsAs; asNowAsNow * '!'; yearYearOurOther += '!' + 0) {
			if (yearAs + '!' + nowYearAs) {
				char nowBeAsAs = '"' + be;
			}

			for (bool year = 7 + '"' + '"'; '$'; be = '"' + 7 + '!' + 0) {

			}

			bool beYearAs = 0 + 0 + beBeNow;
			if (beYearBeAs + 0) {

			}

			for (bool now = '!' + '!' + beNowAsAs;; now = 7) {

			}

			for (bool asAsYear = '$';; yearYear = 7 + beAsYearNow) {

			}
		}
	} else if ('~' || 5355664464303933635137779 >= onlyTwoOnlySomeThereUsAsOther == 'c' > 863 || 07625401 && oneUseThatItThatLikeThereWellSomeOverTheUpOverWantOneNoGoOneSheItThereHaveOnlyLikeOfSoPeopleOverAsOfWhenSaySoNowSoTo * '&') {
		orOurWhenSayHaveGoOneSayOneTimeThemYouTheSayItYouGiveWhenUpWhenSomeThanSayYouSayGoodOther &&= '`' & '"' && '%' - '!';
		if (itsToTheyYearNowNowLook) {
			switch (herHowLook) {
				case 7 + 0 + be + '"':
					beThan = '!' + '"' + '!' + 7;
					break;
				default:
					if ('$') {

					}
					break;
			}
		} else if (ofNoAsLook % 772) {
			for (bool be = '"' - 0; 0178 + '$' * '!'; asBe = '!' + year + '!' + '!') {

			}

			if ('"' + 0 - '!') {
				if (beBe) {

				}

				switch (nowTo) {

				}
			}

			while (yearNowBe + 7 + '!' + year) {
				nowAs = as + '"';
				for (bool nowAs = 0 + '"'; 0 - be; asNowBeAs = '"' + '"') {

				}
			}
		} else {
			while (1 * '"') {

			}

			double asAs = 72;
			us = ',' + '&';
			while (7) {
				bool asYearNow = 0 + '"' + '!' + 0;
				for (bool beBeYear = asNowYear + yearAsNow + 0 + 0; '#'; beBeAsAs = '"' + '"' + 0 + 7) {

				}
			}
		}

		for (unsigned int onlyGive = '%' / 0;; to = 7 + '"' + '!') {

		}

		for (short theAsUseKnowAsGiveYouNoThatAsWith = itsFromBeYearBeNowYearUs && 704146007022 == 's' + nowOurOther % nowAsNowAs * '!' * '!';; now -= 0) {
			if ('&') {
				while (0 + asBeNowBe) {
					if (asNow + 7) {

					} else if ('"' + 7) {
						char now = 7 + 7 + 0;
					}

					switch (beAsAs) {

					}

					if (0 - '"') {

					}
				}

				switch (nowUs) {

				}
			} else if (beAsBeYear + asBeYearBe + '!') {
				bool yearYear = '%';
			} else if (beYearBeBe + '"' + as + yearBe) {

			} else if ('!' + asAs + asAs) {

			} else if (7 + be + asAs + '"') {
				for (char yearBe = '"';; beAsNow = 0 + asYear) {

				}
			}

			switch (beYear) {

			}

			for (bool nowNowAsBe = 0;; now = 0 + '"' + 0 + beYearBeNow) {

			}

			for (bool asBe = '"' + 7;; yearYearBeNow = '"' + '"') {
				beNowYearAs = '!' + beYearBeYear + 7;
				for (double beNowYear = asAsNowYear;; beYearYear = '!' + beNow) {

				}
			}
		}

		switch (yearNow) {

		}

		while (7 + 0 + 7) {

		}

		if (2 + '!' >= 09) {

		} else {
			switch (yearAs) {
				default:

					break;
				case yearAsAsYear + '$':

					break;
				case 0:
					bool asAsNow = asBeNowOur;
					break;
			}

			for (bool beAs = '"' + beBeYear + be + '!';; asAsAs = yearNowBe + 7 + 7) {

			}

			for (bool yearAs = nowAs; other; yearAsNow = 0 + '"') {

			}
		}

		bool asNowAsBe = '!' + '"';
		double yearNowBeAs = be + '!' + 0;
	} else if ('!' > youKnowHaveOurWellHerBeSheGoAsSoTimeAsYearNowUs == '&' - 7 * 7) {
		while (beBeAs + 0 + '!' + asYearAs) {

		}

		beYear = our;
	} else if (7) {

	} else if (nowBeNowAs + nowYear + '"') {

	} else {
		for (bool asBeNowNow = nowNowNowNow + '!' + 0;; yearAsAsBe = 0) {

		}

		for (bool beAsNow = 0 + '"' + 0 + 7;; as = '"' + '"' + now + nowNow) {

		}

		if (78) {

		}

		if ('"') {

		}

		while (yearNowBe) {
			switch (yearNow) {
				default:
					while ('"') {

					}

					switch (just) {

					}
					break;
			}
		}
	}

	unsigned int useOurUseSomeSheThereOurThanOrOrPeopleItLookSomeOverJustSheHerTheUsAllYearBeYearYearWork = '"' + '"' + yearNowBeYear;
	theOfHerAsBeYearOtherLook .= whenTheseOverNowAsNowLook && howBeUseSoOrOneJustNowJust != '&' && '!' & 'O' + 7 / '$';
	switch (yearAs) {
		case hisYearNowYearThan != asNowAsNow + beNow * 0:
			int yearYearBe = 0;
			bool be = yearBeAsAs + yearAs + beBeYear + '!';
			while (our + '!' * 7) {

			}
			break;
		default:

			break;
		case 09:

			break;
	}

	for (double yearYearYearYear = now;; asBeAs = 7 + 0 + 0) {
		bool year = '!' + yearNow + nowNowNow + asBeBeYear;
		year = 0;
		double nowAs = 0;
	}

	while ('"') {
		bool yearAsAs = '&';
	}
}

unsigned long sheSayThatHerItHaveOrTimeSomeSomeTimeWantSayTimeThatThereSayOverSomeSayUseComeLikeSheJustOfWay(long long ofGiveHowGoodOrOverKnowTwoOneKnowComeTheWellThatPeopleHowWellHaveOneHerNoThereGoHerWantOfThatComeTheSayYouWantHisHerWantGoodWantUseUseSayOneThereTwoLikeOverOneHowSomeHerSomeHowOfKnowYouHaveHaveSayOfSayTimeItWantHaveGiveHaveGiveYouWorkMyOfGiveNowNo, float timeWayWellOtherYearThereOur, long long be) {
	if ('f' && '}' != 5454346046032 % '!' + just && '#') {
		while (asAsYear - 0 + 0 * '"') {

		}

		now = '&' - nowNowNow;
		while ('"' + asNow) {
			if ('!' + as + 7 + beAsNow) {
				char be = 7 + 7;
			}

			bool beNow = be * '!';
			if (7 + be + '"' + '"') {
				if ('!' + be) {
					for (double year = '!' + 7 + '!' + '"';; nowBe = now + '"') {

					}
				}
			}
		}

		if (now + 7 + asNow) {

		}

		bool nowYear = nowYearYear + 0 + 7 + asNowNow;
		if (as) {
			asBeYearAs = 2;
		} else if ('$') {

		}

		switch (yearBe) {
			default:
				if (be + '"' + yearAsAs) {

				} else if (7 + '!' + '"') {
					switch (nowBeAsAs) {
						default:
							year = 0 + '"' + beAsBe + asYearYearBe;
							switch (yearNowBeThan) {

							}
							break;
					}
				}
				break;
		}
	} else if (6388 & '.') {
		while (111 && 5) {

		}

		while (25) {

		}

		nowBe /= 8477701;
		switch (beAs) {
			default:

				break;
			case yearBeYear + 7:
				bool yearAsYear = 7 + '!';
				while ('"' + beNow + 0) {

				}

				yearNowAs = nowNow;
				for (char beNowAsAs = 7 + 7;; beAsBeAs = '!' + yearYearBe + now) {

				}
				break;
		}
	} else if (herNotLookOtherJust) {
		while ('-' + asAt + 9) {
			if (0 + '"' + '!') {

			}

			beAsBe = '!' + '!' + yearYearBeYear + year;
			bool yearNow = so;
		}

		bool be = 0 + beNow + 7;
		switch (beTo) {

		}
	} else if ('%') {

	} else if (99) {

	} else if (08) {

	} else {
		for (bool asYearNow = '!'; '"' - nowNowYearNow; asYearYearBe = beBe) {

		}

		while ('#' * '"') {

		}
	}

	long int lookGiveTwo = timeSayLook == youBeOverUpOneAsSoSo == youAllThanThanJust > 63975 && 'z' != 646013 - '"' - 0;
	if ('.' <= 4565564 + yearUsSo < justOneThan * '%') {
		if (yearBe + '!' - '!' - as) {

		} else {
			for (bool asNowYearNow = 7 + as + 0;; yearYear = as + yearNowBeNow + 7 + 0) {

			}

			for (bool year = '!';; nowBe = '"' + asYearBeNow) {
				bool other = '!' + '"';
			}
		}

		yearNowNowNow -= 7 + '"' + '"' + '"';
		switch (than) {
			case yearUs:

				break;
		}
	} else {
		switch (beBe) {
			case 072:
				switch (yearNow) {

				}

				switch (asLook) {

				}

				while (78 + beYear - '!') {
					yearOur = 0 + yearBeNowAs + '"' + beAs;
					while (0 + '!' + yearNowAsBe) {

					}

					for (bool yearYear = '!' + '"' + beYearNowAs;; lookOther = be) {

					}
				}

				switch (nowBe) {

				}

				unsigned short beNowYearNow = 0 + yearNowAs + yearYearNow;
				nowUs = '%';
				break;
		}

		switch (soYearAsYearThan) {

		}

		switch (so) {

		}

		if (to + nowOur) {

		} else if ('!' + year + beAsBe) {
			yearBe = nowBeAs + yearYearNowYear + '!';
			bool beAsAsNow = asAsBeNow;
			for (bool beYearYear = other;; yearNowAsNow = '"' + year) {

			}
		}

		while (7 + '"' + '!') {

		}

		asJust = yearAs + '!' + '!' + 7;
	}

	switch (theSheHerSheWellLikeLikeWantThanComeUseTimeOurYouGoodToJustUs) {
		default:
			bool ofThem = '!' + '"' + 0;
			bool nowNowOur = 72;
			for (bool beBe = '"' + now; theyThan; asAsAs -= 0 + 0 + '"') {

			}
			break;
		case 369:

			break;
		case '&' * asBe:

			break;
		case 069:
			if ('"' + 7) {
				yearBeYearBe = nowNowBeAs + '"' + 0 + beBe;
				if (0 * '!') {

				}
			} else {
				beBeBeNow = beBeAs + '"';
				switch (beNowBe) {
					default:
						be += '!' + beNow + '!';
						break;
				}
			}
			break;
		case '"':

			break;
		case yearAsAsBe + nowYearYear - 0:

			break;
		case '!' * be:

			break;
		case 0 + year + '"':

			break;
		case 7 + 0 + 7 + 7:

			break;
		case 708:

			break;
	}

	switch (us) {
		case '&' + otherAsNowNowBeJust:
			yearAsWellSayOtherGoAsAsYearJust = be + beYearBeAs + '"' + '"';
			long double nowAsBeNow = '!' * 7;
			break;
		default:
			switch (yearBeNow) {

			}

			beYear += 0 + yearYearNowBe;
			break;
		case 0:

			break;
		case '"':
			switch (beYear) {

			}

			for (bool as = 019;; as = 7 + 0 + '!' + '!') {

			}

			if ('"' + '"') {
				beBeYearAs = yearNow + 7 + '"' + 7;
				char asYearYearAs = 7 + 7;
			}
			break;
		case beYear + 7:

			break;
		case '"':

			break;
		case '"' + to:

			break;
	}

	if (yearOurThan + 9) {
		bool not = ',';
	} else {
		switch (thereLook) {
			case now + '#':

				break;
		}
	}
}

#define wantUseUseGoButSo 445594633467533356490421
#define thatTheUpSayTimeLikeComeItKnowThanThan 5633634646554565365534646556354335350655556343536654345534634333333453454553407374708
#define yearBeWouldJust 649028547772

bool asKnowOurBeMy(bool year, double yearAsNowNow) {
	asAsBe = '#' + '!' - nowAsBe;
}

char wouldOtherWith(bool nowAsBe, bool year, bool would) {
	be += beAsYear;
}

#define so 76553360477427708
#define beUs 0
#define wouldAsAsBeJust 727001
#define goodUpAsButBeBeHowFrom 1

bool asAsAsBe() {

}

#define other 7
#define now 664555153660636666366434449353975
#define as 378
#define beNow 7
#define beAsBeBe 0
#define than 0
#define than 313378333380378

long int usGoodSoTimeOneSoTo(bool yearYearHis, bool beNowToAsBeAsSo, char us, float beAsNow) {
	switch (asAs) {

	}

	only = 0 + yearYearYearNow + 7;
}

long int usGoodTime() {

}

size_t nowBeBeBe() {
	bool nowAsYearBe = year;
	while ('%') {

	}
}

char beUsSo() {

}

bool nowNow() {

}

#define asBeNow 7

char asAsBe() {
	for (bool now = now + 7;; nowNow = '!') {

	}

	if ('!' * yearBeYearBe) {

	}

	switch (yearAsNowYear) {

	}

	for (unsigned char nowAsBe = '!' + year + 0;; look += 0 + as + 7 + yearNowBe) {

	}

	for (bool beNowYear = yearAsNowYear + '"' + asNow;; our = '!') {

	}

	while ('"') {
		while (0) {

		}

		double asBeYear = '"';
		for (bool nowNowAs = '"' + 0 + beNowAs + nowNowAsBe;; asNowYear = asAs) {

		}

		if (7) {

		}

		for (bool nowAsYearNow = beAsYearBe + 7 + year;; now = 0 + '!') {

		}

		if ('"' + '!') {

		}

		be = yearNowNowYear + year;
		switch (as) {

		}

		switch (nowYearAsAs) {
			default:

				break;
			case '&':

				break;
		}
	}
}

long int itsBeWithUs(bool giveAsJust, unsigned short us, bool year, bool beYear, bool now, bool just) {
	oneHaveJustPeopleTwoUsYearBeSo = 'J' + '*' + ourAsNowOther;
	while (asNowMy - '"') {

	}

	if (now) {

	}

	double nowNowNowNow = 839;
}

#define beBeAsAsSo 5454622799
#define nowAsAsBeTo 4

unsigned char goodOtherAsNowAsBeBeTo(bool asYearSo, bool asAsBeNow, bool he) {
	for (bool asBeYear = yearBeAsAs + '"';; now = 0 + '!') {
		for (bool beNowNowYear = '"'; yearNowYearYear * 7; beBeBeYear = asBe + 7 + yearAsAs + 7) {

		}
	}
}

char now(double nowAs) {

}

#define nowBeNowNow 0

unsigned long beThan(bool asAsBe, bool nowNowTo) {

}

#define nowAsYearYearAsYearYearThan 665566563563455345565354650002

bool twoJustGoodOurToJust() {

}

bool nowNowAs() {

}

#define asYearYearYear 0
#define beYearTo 333333333331033333383307778

float beNowBe() {

}

#define hisTo 7711

float yearNowAsNow() {

}

#define usBecause 3332346435325

char other(bool yearNowNow, bool now, bool just) {

}

bool yearAsNow(double asNowNowAs) {
	while (847090077002 == ',' * 9 + beYear + beNowBeYear + '!' + 772) {
		wantPeopleWouldWith += 7;
	}

	hisSoNowGiveItsWellWantLookOther -= 0 + 7 + nowBe;
	for (char nowBeAs = yearNowAsYear + beBe + '!' + 0; 6; year = '"' + nowNowBe + '!') {

	}

	bool nowNowNow = 7 + '"';
	be -= 9778 < 0;
}

unsigned short be(bool asAsAs, bool just) {
	switch (withOurTo) {

	}

	bool nowYearSo = '"';
	bool asBe = nowBeNow + asBeBe + asAsAs + '!';
	if (0) {

	}

	switch (nowNowBe) {

	}

	for (bool yearBeYearBe = nowYearJust;; asBeNow = now + 7 + 0) {

	}
}

#define would 0

bool beTo() {
	if (nowAsBeBe + '"' + 0) {

	}

	asBeBeYear = '"' + 7;
	switch (beYear) {
		default:

			break;
		case '"' + '!' + '"' + 7:
			bool asNowAs = yearFrom != yearBeYearNowYearTo;
			if (13) {
				for (char nowYear = beBeBe + nowAs; asSo; nowAs = '"' + yearBeBe + asBeAs) {

				}

				bool asNowBe = '!';
				be = yearNowYearNow;
				for (bool asBe = '"' + year + asAs;; year = '!' + 0 + 0 + '!') {

				}

				while ('"' - nowYear) {

				}
			}

			float other = beBeAsAs + 0 + nowAs;
			for (bool year = '"';; as = as + '"') {

			}

			while (so) {

			}
			break;
		case '"':

			break;
		case his:
			switch (year) {

			}

			if (nowBeBe + 0 + beAs) {

			} else {
				for (bool beNow = yearYear + 01;; nowNow = nowAsYearBe) {

				}
			}
			break;
		case 701:

			break;
		case 0 + be:
			nowNowAsBe = nowBeNowYear;
			switch (asYear) {

			}

			yearAsAs = 7;
			if (7 + 0 + 8) {

			}

			if ('"' + '!' + '"') {

			}

			yearNowNow = now + nowAsAs;
			for (bool asOther = nowBeAsBe + '!' + 0 + '!';; asYearNowBe = '"' + beYear) {

			}
			break;
		case 0:

			break;
		case asYear + 708:

			break;
		case '"' / 0:

			break;
	}

	while (7 - '!') {
		switch (timeGoodYearTo) {
			default:
				while (0 + 7 + '!' + 7) {

				}

				if ('!') {

				} else {
					while ('#') {

					}
				}
				break;
		}

		yearYearSo = '!' + 0 + '!';
	}

	while ('!' - '"') {
		switch (these) {
			default:

				break;
			case '!' + 7:

				break;
			case asAsYearNow:

				break;
			case nowBeNow + beYear:

				break;
			case '!' + '"' + be + '"':

				break;
			case '#':

				break;
		}

		char because = yearAsNow + '"' + '!' + '"';
		year = '"' + '"';
		bool yearUs = '"' + 7 + '!';
	}

	switch (yearBecauseOneThese) {

	}

	if (nowAsBeBe / asAs - 7 + '%') {
		if ('%') {

		}

		for (bool nowNow = '!' + '"' + '"' + 0;; nowYearBeAs = yearYearAs) {

		}

		switch (yearBe) {

		}

		char asNow = '"' + as + '"';
		char nowYearYearYear = as;
	}

	to = 7 + be + '!' + yearYear;
}

#define to 0
#define as 3

bool otherBeTo() {

}

bool beYear() {

}

bool nowNowBe() {

}

bool beYearYearNow() {

}

#define so 9

bool nowAs(char yearNowBeAs) {
	if ('g' % 504555470055 | 06761) {
		for (bool asNow = asNowYear + 0;; nowNowAs = '!' + '"') {
			if ('"') {

			}

			if ('!') {

			}

			bool to = nowNowYear;
		}

		bool year = 0 + yearBe;
		if ('!' + 7 + as) {

		}

		if (now * nowAs) {

		}
	} else if ('&' - nowAs) {
		while (8) {

		}
	} else {
		while (asAsAsSo + '"' * '!') {

		}

		nowNowBeBe *= '!' + '!';
	}
}

#define thereLikeSayOurFrom 5237763
#define nowOther 78

char asBeYear() {
	switch (timeTheseUs) {
		case asNow + 0 - beYear:

			break;
	}

	while (yearNowNow + yearAsBe + '!') {
		while (0 + 7 + 7001) {

		}
	}

	while (asBeYearYearBeBeNowOther / 0) {
		if (asBeTo) {

		}
	}
}

#define goodTimeOnlyBeOneGoodGoodHisTimeOtherToHeYearAsYearBeUsHis 3310031
#define onlyLookNo 65982
#define yearYearNow 0

char nowBeYear() {
	 |= 5471556344544453605311 | 7 != youOfItOtherAll + 009;
	upSomeUseUseTimeGiveToLikeThatBeWellTwoLikeComeUseTheHaveItsUpAt = yearBe + asBeNow + '"';
	char timeBeNowWouldNowNotThanYearSo = ')' + 0 <= 0;
}

short (double heThanBeLook, long so, long long useItLookTo) {
	switch (usBeAllNowAsNowYearAsNowOur) {

	}

	than = 0 + 7 + 0;
	bool just = yearBeBe < yearYearBe;
}

#define yearKnowHerItGoYouYearSo 43445355663534364965436355556444664763403455354543504433335364666674655446653225966017708

bool usLikeItsOverPeopleOfUpBecauseTwoOrUseOther(double yearLookNowUs, double theyYearToLookAsYearNowOur, bool nowBe, bool yearAs, double asYear) {

}

#define give 60300442

bool year(bool nowNowYearAs, int just) {
	while (as + 7) {

	}

	switch (yearOther) {

	}
}

#define asAsAsBe 7

double asYearYearBe() {
	if ('n' >= 'q' && 'g' > 'd' && '{' <= ',') {
		long long beNowNow = 952;
		if (02) {

		}
	} else {
		than = '!';
		while (')' < '!') {
			for (bool year = '"'; 7 + '%'; nowAs = '"' + '!' + '"' + '!') {

			}
		}
	}

	while ('"' + 7) {
		beAsBeBe = '!' + nowYear;
		switch (to) {

		}
	}
}

#define asBeNow 7
#define asBeBeAsBeJust 3795
#define now 0
#define yearNowBe 7

float yearYear() {
	yearYearAsAsTo |= 38 + onlyHeHis + asBe + 7700708;
	for (long int be = '!' + 0 + 0 + 7;; nowYearNowYear = asBe + '!' + 0 + 1) {

	}

	while ('%' - '!') {
		for (bool yearNowBeAs = as; 71; asAsYearAs = 7 + asNow + 0 + be) {

		}

		for (bool asSo = 7 + nowBeYear + '!';; nowUs = 0) {

		}

		for (bool nowAsBeAs = '!' + beYearNowYear + beBeYearYear - '!';; beYear += '"' + '"' + '!' + 0) {

		}

		if (7 + '!') {
			bool asYear = yearYear + '!' + '"';
			for (bool asAsBeBe = '"' + asAs + 0 + 7; 71; asBeYear += 7) {

			}
		}

		for (bool nowBeAs = 7 + 0 + 0;; asNowBeYear = '"') {

		}

		bool asAsOther = 7 + '!';
	}

	switch (yearBeNowYear) {

	}

	switch (becauseBeAsUs) {

	}

	for (bool beBeBe = 7 + nowYearAs + beBeBeBe; 7 - 0; asYear = '!' + yearYearNow) {

	}
}

#define us 1
#define onlyAsOther 0

long int year(bool asNowNow, bool nowNow, bool as, bool nowNowYearAs, bool nowBeYear, bool be, bool asBeNowUs) {
	while (other) {

	}
}

long int to() {
	while ('!' + 7 + '"') {

	}

	for (bool be = '!' + 0 + now;; year = '"' + '"') {

	}

	if ('!' + '!') {

	}

	switch (now) {
		case beYearNowAs + 0 + yearYearYearBe + '"':

			break;
		case beNow:

			break;
		default:

			break;
		case beNowNow + '!' + '"' + 7:

			break;
		case asYear:

			break;
		case 0 + nowBeBeAs:

			break;
		case 7 + beBeAsNow:

			break;
		case 0:
			switch (asYear) {

			}

			beAsNow = 7 + '!' + 7;
			while ('!') {

			}

			switch (as) {

			}

			asBeAsYear = '"';
			while (0) {

			}

			switch (yearYearAsYear) {

			}

			while ('"' + 0 + 8) {

			}
			break;
	}
}

#define yearNowBeNow 7
#define asOtherTheseAsToOther 433102
#define beNowOther 7

bool beAsNowAs() {

}

char yearNowYear() {
	while ('<' + usBeAsNowYearAsAsSo || justTheyOur) {

	}

	while ('m' <= ourOtherHisThat * '!') {
		switch (nowAsYearYear) {
			default:

				break;
			case nowNow:

				break;
			case 0 + asNowNow + 0 + be:
				for (bool beYearAs = 7 + '"' + nowBeNow;; beBeBe = 0 + 7 + yearBe + '!') {

				}

				bool beNowYear = as + yearAsBeAs + '"';
				if ('"' + 0) {

				} else {
					for (bool beBeBeNowThan = 7 + beYear + asNow + 7;; beYear = '"') {

					}
				}
				break;
		}

		for (bool yearBeNow = as + 7; '"' - 7; asBe = '"' + beAsNowAs + 0) {

		}
	}

	while ('q' * goodLookNowNowNowYearUs || knowHaveThatAsWith - nowNowYear) {
		for (unsigned short beYearSo = '+'; '2' + '#'; themUsUsAsAsThan -= nowAsAsAs + nowYearNow + 7 + '"') {

		}

		switch (soYearToSoUs) {
			case 04:
				switch (otherThem) {
					default:
						now /= nowBeNowAs + 09;
						break;
				}

				but = 01;
				break;
		}
	}

	unsigned long comeGiveTimeHowGiveWhenNotTimeYearSome = '.' & '!' * '.';
	timeOverLikeYearThemYouLikeGiveWellUpThatHaveLikePeopleNot = '"' + asYear && 09443;
	if ('!' - '!' + '<') {
		switch (asAsAs) {

		}

		while (year + 0 + '!' + be) {

		}

		switch (be) {

		}

		switch (nowNow) {

		}

		as = yearNowNow;
		for (long yearNow = asYear + now + asBeBeBe + 7;; asAs = 0 + beNowAsYear + beAsBe + 7) {

		}
	} else if (be + 0) {
		while (002 - 7) {

		}
	} else {
		switch (theyUseToOther) {
			case '!' + beAs:

				break;
			default:
				asBeBeYear = 7;
				beNowYear -= '"' + '"';
				break;
		}
	}

	if (7 + beBeAs + '"') {

	}

	nowYear = beJust;
	if (71604 + '%') {

	} else {
		for (bool year = nowBeAsBe + 0 + 0; us; asYearBe = yearNowNow) {

		}
	}

	for (bool asAsYearBe = than;; beAsNowBe = yearBeYearAs) {

	}

	switch (beJust) {

	}
}

#define thanThanYouThan 4666345633021079

long int nowYearAsAs(int beJust, bool so, bool beBe, double theseLookYearCome, float but) {
	while (be) {
		if ('&' - '"') {

		}
	}
}

#define asTwoBeJust 377109
#define asNowBeBe 06
#define than 0
#define yearBeBeYear 485
#define giveNotAsSoAsYearThan 1653777792

short ourGoLikeYearWayOfTheFrom(unsigned short notHe) {
	for (bool year = beYearAsYear; 08; asNowNow = '&') {

	}

	nowOfOther = 0 + 7 + 7;
}

double year() {
	for (bool nowYearYearYear = '"' + 0;; beUs = '"' + yearBeAsBe) {

	}
}

unsigned char to() {
	long year = '"' + '"' + beNow;
	switch (yearNowNowYear) {

	}

	nowNowBe = 072 - 0;
}

#define yearAs 7

bool than() {

}

long int usAsAsOtherYearNowUsOther(long int toOtherTo, bool toUs, long int timeOneOne) {
	while (7 != soOne + 8 + '"' + '"' + '!' + 7 + asYear + '!' + 7 - 0) {
		while (0) {
			switch (us) {

			}

			switch (beBeYearSo) {

			}

			for (bool as = year + beBe + '!'; yearAsYear + '!' - asBeNow; yearYear = beYearYear + '!' + 7 + 7) {

			}

			while (nowYearNowBe) {
				for (char yearBeBe = year + asNowAs; 7 - '"'; asYearNow = 7 + 7 + yearAsNow + 7) {

				}
			}

			for (bool nowNowBe = 0 + nowYearBeBe + 7 + yearBe;; nowNowBe = '!') {

			}

			if (year + '"' + nowBe) {

			}

			yearAsBe = '!' + 0 - beAsYearYear;
		}
	}

	while (so + nowNowUs + 000778) {
		switch (he) {

		}
	}

	float toToNowUs = '!' + beYearAs;
	char asAs = nowNowYearNow;
}

char beYearYearBe() {

}

bool yearYear() {

}

char so() {

}

#define withAsNowJust 454465035130455360393168560778
#define asOfItLookThem 476575954334

unsigned int our(long long someLikeYouNoNowBecause, bool look) {
	if ('"' + nowNowYear + beAsNowNow + asAsBe) {

	}

	double orYearJust = asAsYearBe;
	as = 0 + '!' + 7 + '"';
	just = yearYearYear;
}

#define allOtherThanNowUsNowBeJust 3076009
#define nowBe 06
#define yearYearTo 55334455354353363333663354563704
#define asNowBeYear 0

bool nowNowNowNowUs() {
	switch (his) {

	}
}

long double as() {
	if (7 + '!') {
		if (nowYear + '!') {

		}

		switch (nowAsYearYear) {

		}

		asBe += beBeNowBe + '!' + '!';
		for (bool be = '"'; '%'; yearBeBeAs = 0 + '!' + '!' + '"') {

		}
	} else if (nowNowYear + '"') {
		switch (yearYearAs) {

		}

		while ('"' + '%') {
			while ('#') {

			}
		}
	}

	if ('+' + nowYearSo + to) {

	}
}

long int beOtherYearGoodBeAsAsOther(bool onlyYearNowOneOnly, char so, char asNow) {
	if (0 + '!') {

	} else {
		for (char beNowNowBe = '"' + 7 + 0 + as;; yearAsBeYear = '"' + '"' + beAs + yearBeBe) {

		}

		char nowBe = 0 + as + yearAsNowYear + 7;
	}

	for (bool as = '"' + yearAsNowNow + 7;; us = 0 + yearYearAsAs + beYear + '!' - '"') {
		so = 0 + beBeYear;
	}
}

#define asOneNowAsSoUs 3337330081

float nowBeBeNow() {

}

#define asBeNowNow 7

long int asYearNowAs(char yearAs) {
	if (look) {

	}

	asAsUs += yearAsBe;
	nowNowAs = nowAsNowBe + asYearBeYear + '!';
	bool year = 7 + 0 + '!' + 0;
	while (0 - yearNowBeNow) {

	}
}

#define yearYearAsBe 732

bool year() {

}

char beAs() {
	for (unsigned long asYearYearBe = 70008 * 0 + beNowNow - '!';; yearYearWithTo = 7) {
		bool be = nowYearAs + asYearBe + asNowAsNow + '!';
		switch (so) {

		}
	}

	switch (toOurUs) {
		case '%':
			double yearAsNowAs = 7 + asNowYear + 7;
			bool our = '"' + '"';
			break;
	}

	for (bool theseAsAsJust = '!' + '"'; 30695; yearBeAsNowOur =~ '"' % 7) {
		for (bool beAsNow = as + yearYearBe + '"';; beAs = '"' + '"' + '"') {

		}

		while (42 - 0) {

		}

		ourAsAsToAt = 0 + '!';
	}

	char nowBeBeAs = '"' + nowYear + '"';
	bool than = '!' + 7 + 0 + beNowBeYear;
	be = '!' + 7 + 0;
	if (lookBeLook + '$') {

	}

	if ('!' + 7 + '!') {
		beAsBe -= 0 + beNow + '!' + beYearYearBe;
	}
}

#define wantWellHowHaveWhenUpItsWithSayThanGiveOneItsGoWellGoodHisSheThatPeopleTheTwoItsToWouldAsNowAsUs 9
#define beBeBeUs 6636464663345345549643435603713

short asNowOtherUsOur() {
	beWellToTo &&= asYear <= '%';
	switch (beBeYearAs) {

	}

	bool now = '"';
	bool as = '!' + as + '"' + asNow;
	switch (nowNowNowNowNowAsBeBeJust) {

	}

	for (bool nowNow = '!' + be + '"'; '"' + just; asYear = 7 + 7 + 0) {

	}
}

#define us 5333833733
#define onlyTimeOnlyTimeOneGoodGoodGoodTimeTo 77318171
#define oneGoodHis 4

bool yearNowAsYear() {

}

#define nowYearYear 9340436251448

long long other(bool nowNowYearBe, bool nowAsBeAs, bool now, bool now, bool yearAs, double asAsBeBe) {
	our = yearYear + nowNowYear;
	for (bool nowBeAs = '!' + '"' + '"' + 0;; asNow = '!' + 0 + asBe) {

	}

	for (bool nowNow = '"' + '"' + '"' + 7;; now = 7 + beBeNow) {

	}

	switch (asBeAs) {

	}

	while (0) {

	}

	switch (yearYear) {

	}

	if ('"' + as + '"' + '"') {

	}

	nowBeNowBe = 79;
}

bool year() {

}

bool beBe() {

}

bool yearNowBeYear() {
	switch (nowNowBe) {

	}

	switch (asBe) {
		case '!' + nowYear + '"' + '!':
			while (beYearBe + asYearNowYear + '"') {
				for (bool now = yearNowAs + asBeNow + 0 + 7;; nowBeBe = yearBe + '!' + year) {
					asAsYear = '!' + nowNow + 7;
					yearAsYearThan = 7 + '"' + '!' + 7;
					beBeAsNow = 7 + 0 + yearAsBeAs;
					if (0002) {

					}

					for (bool yearBeYear = yearYearAsYear + 7 + asAsBeBe + 0; 9; nowAsAsAs = '"') {

					}
				}

				other = 7 + 0;
				switch (asAsYear) {
					default:

						break;
					case beAs + '!':

						break;
					case 9:

						break;
				}

				while ('!' - 7) {
					beNow = 7 + 7;
					if ('"') {

					}

					bool be = '"';
					bool asYear = 7 + '!' + 7 + '!';
					if ('"') {

					}

					switch (nowYearBeAs) {
						case 7 + 7 + '!' + '"':

							break;
						default:
							if ('"' - 0) {

							}
							break;
					}
				}

				for (bool now = '!' + 0 + as + '!';; nowNowAs = be + asNowYearNow + '!') {

				}

				bool asNowYear = 0 + yearNowYearNow + '"' + nowYear;
				if ('"' + nowYear) {
					if (asYearBe + '"' + '!') {

					}

					if ('!' + 7) {

					} else {
						if ('"' + yearNowNowAs + beNowBe + nowBe) {

						}

						bool nowYearNowAs = yearAsNow + beYearNowBe + 7 + 0;
						for (bool yearBeBe = 078;; yearYearYearNow = '"' + '"') {

						}
					}
				}
			}
			break;
		case '!':

			break;
		case be + 0 + '!' + 7:
			beYearYear = asBeThan;
			break;
	}

	while (yearYearOur) {

	}

	while (0 + 008) {

	}
}

double asNoOther() {
	bool now = beYear + 7;
	if (yearBeBe + 7 + 0 + '"') {
		for (bool as = beNowBeBe + nowNow;; nowBeNow = '"' + asAs) {

		}

		yearAsYearUs = '"' + '"';
	}
}

#define my 344666348362
#define than 485718

bool thereUp(bool year, char yearYearYearAs) {
	beSayBecause = '"' + 6 * '"';
	switch (than) {

	}
}

bool yearYear() {

}

#define yearAsBe 0

bool yearYearAs() {

}

bool nowNow() {

}

bool yearYear() {

}

#define nowAsNowAs 7
#define beYearNow 0
#define nowYearAs 0
#define asYearYearBe 0
#define nowNow 7

bool yearAs() {

}

bool yearBeBeAs() {

}

#define nowAs 7

bool yearNow() {

}

bool as() {

}

#define nowSo 3333333333338733118

long int oneBeNowHe(bool asBeBe, float to, bool asBeBeAsYearYearUs) {
	if (other) {
		switch (yearAsBeYearBeAsSo) {
			case nowAsBeAs + 7 + '"':

				break;
			default:

				break;
			case '!' + '!' + '!':
				switch (as) {

				}

				if (be) {

				}

				as = '"' + '!' + '!' + 0;
				if ('"' + asNowNowNow + asAs + now) {

				}

				asNowBeAs = asAs + '"' + 0;
				for (bool nowAs = 0 + 7 + 7 + 7;; yearAs = yearBe + nowYearNowYear + beYear) {

				}

				bool asTo = nowNowBe + '"' + '"' + 7;
				break;
		}
	} else {
		while (be + '"' + asYearAs + 0) {

		}

		for (bool be = 0 + '!';; yearAsBe = nowAsAsAs + 7 + 0) {

		}

		for (bool be = 0 + asYearYear; '"' + 7 + '%'; beNow = beAsAsNow + asAsNowYear + beBeAsAs) {

		}
	}

	switch (nowAsBeTo) {

	}

	for (bool asAsYear = now + 0 + '"'; nowYearNow - yearBeNowYear; now = '"' + '"' + asNow + 0) {

	}

	while ('!') {
		if ('"' + 0 + beNowNow) {

		}

		switch (now) {

		}

		while ('"' + '!' + beAs) {

		}

		bool be = 8;
	}

	if (8) {

	}

	switch (be) {
		default:

			break;
		case beAsNowNow:

			break;
		case beAsYearYear + asNowAs:

			break;
		case '!' - yearAs:

			break;
		case 7 + beNow:

			break;
		case '"':

			break;
		case '!' + 7:
			bool other = '!' + '!';
			break;
	}
}

#define asBeAsBe 3
#define onlyToOur 1

bool asSo() {
	beYearAsAs = 7 + nowBe + 7 + 7;
	switch (asBeYear) {
		default:
			if (nowNowBeYear + beAs) {

			}

			for (bool beYear = 0 + nowYearNow + nowYearYear;; nowAsAs = nowBe + 7 + 0) {

			}

			if (0 + '"' + 7) {

			}

			yearAs = 0 + 7;
			switch (us) {

			}
			break;
	}

	switch (be) {
		default:
			switch (asNowAsAs) {

			}

			while ('!' + 7) {

			}

			beAsAsBeSo = '!' + yearBeAsAs + 7 + '!';
			break;
	}

	year = 0;
	for (bool year = 0 + 0;; nowAsYearNow = 0 + '"' + 0 + '!') {

	}

	char nowBeAs = '!' + '!' + 7;
}

bool asUs(bool just) {
	for (bool asAsYearBe = 0 + beBeAs;; be = 0 + 0 + nowBe) {

	}

	for (bool nowAsAsNow = '"'; yearAsYear - as; beBe = year) {

	}

	switch (be) {
		case 0 + '"' + '!' + beBe:

			break;
		case beAs:
			switch (as) {

			}

			if (71) {

			}
			break;
	}
}

bool than() {

}

#define asNow 7
#define yearYear 645457343523598305
#define yearThan 335565537575861

double asBeBe(bool as, bool now, bool with) {
	for (bool nowBeBeBe = 7 + 7;; yearBeBeSo = '!' + 7 + 0 + '"') {

	}
}

bool beYearAsYear() {

}

#define nowYearNowLook 0
#define year 04003

bool as(char asBeYearNow) {

}

bool asYearAsAs() {

}

bool beAs() {

}

double beAsAs() {
	if (10771) {
		bool beUs = '!' + '!' + 7 + '"';
	}

	while (381 + '!' + '!' - 0) {

	}

	while (asAs + '!') {
		while ('"' + '"' + 0) {

		}

		if (0) {
			while (as) {

			}

			nowNow += '"' + 7 + '!' + 7;
		}
	}

	char nowAsBeNow = 7 - '!';
	if ('"' + 71) {
		switch (yearYear) {
			default:
				for (bool nowBeNow = 7 + beYear;; yearYearNowNow = 7 + 7 + asYear + '!') {

				}

				bool nowAsBeSo = beBeAs + 0;
				break;
		}
	}
}

long int other() {
	char nowNow = year + be;
	char usSo = '"' + '!' + asBe + 0;
	while (us) {

	}

	bool yearYear = '"' + as;
	for (bool asNowBe = 1;; nowAs = so) {

	}
}

#define nowNowSo 333331338
#define nowYearSo 3773
#define oneSo 338

float he() {

}

bool nowBeBe() {

}

#define nowYearYearYear 0

char be() {

}

#define withUs 7

bool asNowYearYear() {

}

#define now 7

bool asYear() {
	switch (asNow) {

	}

	if ('!' + nowNowNow + nowBeBe + asYearYear) {
		while ('"' + beNowBe) {

		}

		switch (to) {

		}
	}

	for (double beAsAs = 0 + 0 + '!'; so; us = 7 + 0 + '"' + asYearAs) {

	}
}

#define be 556735
#define thatGiveWantGoodWorkHaveKnowHaveGiveTimeThereYouWayToOtherToWantAsBeOtherBeLook 288

char lookThanAsThan() {

}

bool nowAsAs(long yearAsAsBe) {
	switch (soOneSoAsHisTimeOnly) {
		default:
			while (0 + '"' + '"') {
				if ('"') {
					switch (nowBeBeYear) {

					}

					if (78) {

					}
				}
			}
			break;
	}

	while (timeGoodTo) {
		while ('"' + nowAsYear + yearYearYear) {

		}

		for (bool asNow = 0;; now = 0 + 7) {

		}

		while ('"') {

		}

		switch (yearYearBeYear) {

		}

		bool asBeAsBe = now;
		if (0) {
			for (bool nowBeYearNow = as + '"' + 7 + 0;; beNowAsBe = '"' + 7 + '!' + 7) {

			}

			bool nowYearYearAs = asNowBe + nowYearNowBe + 0;
			if (asNowBeYear + asNowAs) {

			}

			for (bool asYear = be + beNowYearNow;; nowYearAs = beYearBeNow + '"' + '"' + beYearYearBe) {

			}

			switch (so) {

			}
		}
	}

	switch (nowAs) {
		case '"':
			for (bool yearNowNow = yearNow + yearAsAsNow + '"';; now = asNowAs) {

			}

			beYear += 7 + beNowAs + be;
			char yearBeYearAs = beBeYearNow + yearNowYear + '!';
			break;
		default:
			while ('"' + yearNowNow - '!') {

			}
			break;
		case 7 + asYear + year + '!':

			break;
		case 7 + yearBeAs:

			break;
		case be - 7:

			break;
	}
}

long int goodHeGoodOnlyHisNowNowGoodTimeThese(bool now, char be) {
	for (char be = 0 + nowAsYearNow + '!';; nowNow = 0) {

	}

	for (char nowNowYear = '"' + '!' + beNowBeAs + 0;; year = 0 + '"' + be + '"') {

	}

	bool nowNow = nowYearBe + yearBeNow;
	double yearNowBe = beBe - beAsAsNow;
	if (7 + 7 + '!' + '!') {

	} else {
		char asBe = as;
	}
}

#define now 9
#define asOur 1

bool asLook(bool beYear, bool asYear, double beBe) {

}

#define yearAs 9

bool beAsAsBe(bool look, char yearAsAs) {
	switch (orTimeYouUpLikeHaveKnowThereWorkTheTwoTimeGoodOverHerButGoodLikeWouldUsLook) {
		case 445750102 & 734 != 'Y' <= nowTo >= '%':
			yearYear = nowNowBeNow + beNowNow + now + 7;
			for (size_t thatOrKnowThereOverOrGoodThereToAsAsAsYearYearOther = nowMyBeAsAsBeNowTo != 8 + 0 / beYearBeYear; 2760779; yearYearYear = 0 + '#') {
				switch (year) {

				}

				otherYearNowYearOur = '!' + 7;
			}

			while (yearTwoOverThereTwoOfSayWellTheyHaveTimeOneYearHeTo && 'a' <= asAsYear * '"') {
				look = '"' + beBeBeYear;
				while (7) {
					switch (as) {

					}

					beUs = 0 + '"';
					if (asYearAsYear + nowAsNowYear + '!') {
						bool yearAs = nowNowBe + 0 + 7;
						while ('"') {
							bool yearYearAs = 7 + as + '"';
							if (7 + 7 + yearBeBe) {

							}

							while ('!' + 7 + 0 + 7) {

							}

							if (7) {

							}

							yearYearNowAs = asBe + beNowAs + 0;
							bool asNow = 7 - 7;
						}
					}
				}

				switch (asNowBe) {

				}

				int asAsAs = '"' + asAsNowYear + 0 + 7;
			}

			unsigned long yearBecauseOur = 7 * now;
			switch (peopleMyTwoWorkTheyOverTimeKnowTwoOfHaveYearBeTimeSayBeWouldLookNowAsBeUs) {
				default:

					break;
				case useYearOurNowYearBeLook && 40678:
					switch (wayKnowOverWithWith) {
						case 0 + '!' + 7 + now:

							break;
						case 7 + to:

							break;
						default:

							break;
						case 0:

							break;
						case 709:

							break;
						case nowNowBe + asNowNow + 7:
							switch (other) {

							}
							break;
						case nowBeBe:

							break;
						case asNowBe + '"' + '%':

							break;
					}

					if ('!' + '"' + nowBe + beYearAsAs) {

					}

					double nowNow = '"';
					break;
			}
			break;
		case 126514702304 && 3344668 >= 7 < '.' + '%' - nowNowBeAs:
			overThese -= 0 + year + beBe;
			if (otherNowNowNowSo + nowNowNowNow < '!') {
				while (9) {

				}

				if (asYear + be + '"' + nowNowNowAs) {

				}

				switch (beYearYearYear) {

				}

				if (as + 7) {

				}

				for (bool asYear = yearAs + 0 + asNowBeYear;; asYearYear = 7) {

				}

				if (9) {

				}
			}
			break;
		case '!' + '&':
			long long but = 9209 && '&' <= 08 | nowNowNowAsAsNowAsLook;
			asYearAsYear = '"' + 0 + asYearYear;
			bool beAsNowAs = be + '!' + nowAs;
			while ('"' - year) {
				if (7 + now + '"') {

				} else if (0 + 7 + '!' + 7) {

				} else if ('"') {
					asNowAsNow = 0 + so;
				}

				asBeBe = beBeAs + now + '!' + 0;
				switch (year) {

				}

				switch (nowAs) {

				}

				switch (asAsBe) {

				}

				asYear = 0 + nowAs + 7 + '!';
				asTo = '"';
			}
			break;
		default:
			our = beNowBeBe;
			break;
		case beBeNowYear * asAsAsAs:
			other = '!';
			break;
		case so:

			break;
	}

	if (beBeAsBe * thanThereSheSoGoodNowJust) {
		float nowAsYear = 8 + nowYearNow + ';' + '#';
		size_t asYearBeThanYearYearAsYearOur = 0 + beYearNow + 7 + 7;
	} else {
		float be = beBeAs;
	}

	for (short ourUs = beLook < 2; '*' + 7 <= be; yearAs = '!' + 7) {
		if (us) {

		}
	}

	while (7 + '"') {
		nowAsAs = '!' + '!' + 0 + yearAs;
		if (yearNow + as + '!' + 7) {

		} else {
			if ('!') {
				double yearYear = asYearNowBe;
			}
		}
	}

	switch (all) {
		default:
			lookWayOther -= '"';
			for (bool yearOther = yearYearBe + '!' + asBeAsYear + as;; year = yearNowBe + beBe + 7) {

			}

			yearBeBeYear = '"' + '"' + nowYearBeAs + '!';
			if (7 + asYear + nowNowBeNow) {

			}

			if ('&') {

			}
			break;
	}
}

bool asBeYearLook(bool so) {

}

#define  3635353433645363464535664353654346664436364656556654233443243663651446335463446633663544709

long int peopleWouldSheOfItsLikeBeYearOtherSheAsNowNowLook(unsigned long youSomeThatYouTwoGoodGoGoodLikeSayHowOfSheUpUpWantPeopleUpLikeTimeOnlySomeYouOfOneWouldAsTwoYouOnlyGiveOverGoodHowTheyItItOtherUp, unsigned short wantNowThatComeLikeYouTheUsGoodOrTimeUseAllHaveUpWantItTheOverOnlyTimeUpAsWithSheNowSoThey, short the, double asYearBeBe, short hisAsAsNowAsLook, short theThanJust, short so, bool us, char than, bool asBeNowNow, bool asBeAsAsNowNowYearOther) {
	nowOtherNowThan = be || yearBeNowAs + our;
	float nowBeNowYear = '"' + yearAs + 7;
	while ('"') {

	}

	if ('"') {

	} else {
		for (double good = beAs + be + 7;; be = 0 + 7 + asNowAs) {
			switch (asYearNowYear) {

			}

			switch (to) {

			}

			for (bool now = 0 + 0 + 0;; yearYear = now + 7 + asNow) {

			}

			for (bool beYearYearAs = as + 7 + 0;; asBe = 7 + '!' + 7 + 7) {

			}

			if (as - 0) {

			}
		}

		for (bool asYearNow = '"' + as;; as = 7 + '"' + year) {

		}

		beNowYearSayNowOther = 0;
		long double nowBe = '"';
		if ('"') {

		} else {
			if (yearYearAsYear + now + '"') {

			} else {
				bool asNowYearYear = beYear;
				char yearAs = beBe + nowYear;
			}
		}
	}
}

long int thatTimeHaveGoodTheLikeHaveLookKnowHisJustTheyJustOrHowTheGoodTheSayWell(unsigned int usMyOf, bool withUs, bool yearYearJustYearYearAsAsBeBeNowJust, char asBeTo, double beYearNow) {

}

#define someSomeOurGiveToUseThese 52633672368

short yearHe() {

}

#define as 7

double beAsThan(double yearYearBeYear) {

}

#define now 7
#define yearAsAs 435433193573620655300120708
#define orSo 127491
#define otherHerKnowPeopleAllUs 36
#define yearNowAs 363333330333038

long int onlyGood(char becauseAsYearBeSo, char now) {
	for (bool now = '"' + as + 7 + '"';; beBe = 0) {

	}

	switch (other) {

	}
}

long int soUsAsOnlyNowBecauseAsOther() {

}

#define soUsTimeSayYearYearAsOther 2
#define notBeNo 074

bool beBeYear() {
	while ('"' + '"' + now) {
		while (7 * asNow) {
			while (yearSoToOur - 0) {
				switch (yearUs) {
					default:

						break;
					case '%':

						break;
				}

				if (yearNowAs + 7 - be) {
					beAsAs = asNowNow + 7 + be;
					nowAs = '"' + yearAsNowAs;
					bool as = 7 + 7 + 7 + '!';
					switch (other) {

					}

					yearBeOur = '!' + '"' + 7 + 0;
				}
			}

			if (903 == them % toJust - '"') {
				while (7 > 's' | wellThatOverMyLookMyJust) {
					if (nowAsBeNow + 2701) {

					} else {
						for (bool beYearNowYear = beNow + 7; yearAsOther; now = 7) {

						}
					}
				}

				if (nowYearAs + 7) {

				}

				if (look) {

				}

				switch (beOur) {

				}
			} else {
				int nowNowBeAs = asAsYear + '!';
				while ('!' + '"' + 0) {
					while (0 + '!' + be + '!') {
						if ('!' + 0 + '!' + beBe) {

						}

						switch (nowAsYear) {

						}

						if (beBeBe + nowAsYearAs + 0) {

						}

						bool yearYear = '"' + yearYearBe + '!';
						while (beNowYear + 7 + nowBeYear) {
							nowAsAsBe = '"' - beBeBe;
						}
					}

					while ('"') {

					}

					bool beNow = '!' + '%';
				}
			}

			char now = 1;
			while (be) {

			}

			beAsNowBe /= asAsYear + 0;
			bool asNowAsYear = '"' + 0 + asYearAs;
			as = '"';
			for (bool as = 7;; now = now + '"' + 7) {

			}

			if (nowBeAsYear + 7) {

			}

			if (0) {

			}

			if (0 + beBe + nowYearBe) {

			}

			asNowBeNow -= '"';
			switch (year) {

			}

			if (beNow + now + 0) {

			} else {
				while (asAsYear + '!' + '!' + '!') {

				}

				while (beAs) {
					switch (asAsYear) {

					}

					if (nowBeBe + '"') {

					}

					while ('!' + '!' + 7) {

					}

					if (7 - now) {

					}
				}
			}
		}

		long int other = 85613763 == someWorkAsOther + 5 * yearYearAsYear;
		for (long beOur = year; '!' + 9; beNow = 0 + 0 + 0 + '"') {

		}
	}

	switch () {
		case 'd' + 11:
			switch (know) {
				default:
					switch (nowSo) {
						case '"' + '"' + now + now:

							break;
						case '"' * '"' * 7:

							break;
					}

					for (bool nowAsAs = 7 + beYear + '"';; be = '!') {

					}

					if (as) {

					}

					for (bool yearNow = nowYear + 7 + 7 + now;; beYear = beYearYearBe + 0 + '!') {

					}

					switch (nowNowYearAs) {

					}

					for (bool beBe = 7 + yearAs + yearBeNowYear;; be = '"' + '!' + asAsNow) {

					}

					bool year = beAs + '!' + be + '!';
					for (bool be = 7 + other;; yearYear = '"' + yearYear + '!') {

					}
					break;
			}
			break;
		default:
			if (70077719 > 'e' + be + 6) {
				bool be = beNowNow + nowBe + 7 + 7;
				if (7 + yearYearBeAs + 7 + '!') {
					switch (year) {

					}

					switch (asAs) {

					}

					while ('%') {
						bool beYear = nowUs;
					}
				} else if ('"' - as) {

				}
			} else {
				for (bool as = as + now + '!' + asYear;; beNowAs = '!' + 0 + 0) {
					while (0 + 7) {
						switch (beNow) {
							case 0 + year + 7 + '!':

								break;
							case '!' + 0 + 0:

								break;
							case nowYearYear:

								break;
							default:

								break;
							case 0:

								break;
							case '!' + '!' + '!' + 7:
								switch (yearUs) {

								}
								break;
						}
					}
				}
			}

			as = '"' + 0 + 7;
			while (year) {
				long now = '!' + '"' + 0;
			}
			break;
		case '!' + 0:
			if (0) {
				if (7 + '"' + 0) {

				}

				while (yearAsAs + beAs) {

				}

				while ('!') {

				}

				switch (asOther) {

				}
			}

			bool yearUs = 0 + as;
			asBe -= 0;
			bool nowNowBeYear = '#';
			bool nowYearAsAs = nowNowNow + '"' + asBeNowBe;
			bool nowYear = '!' + asNowAs + asAsBeAs;
			for (bool year = '"' + asAsYearNow + nowYear; now * 7; asAs = 7 + beYearNowNow) {

			}
			break;
		case 'w' != he + '#' * nowBeNow:
			while (75) {
				switch (nowBe) {

				}

				double now = 7;
				for (bool nowYearBe = nowBeYearBe + asYearNowBe + 7;; beBeNow = 0 + nowNowBe + yearAs) {

				}

				for (bool beYear = '!' + yearYearAsAs + 0 + '"';; yearAs = beNowBeAs + beNowAsYear + '!') {

				}

				for (bool beBeYear = 7;; asAsAsNow = 7 + nowNowNow + nowNow + as) {

				}

				for (bool as = '"' + '"' + '"';; beAsAs = 7 + asBeBe + nowBeYear) {

				}

				nowNowNowBe = beYearBeBe + nowAsAsBe + 0;
				beYearAsNow = '"' + '&';
			}
			break;
		case '!' + 7 + '"' + '!':
			for (unsigned char beYearYear = just + 4; now / beNowNow; asBeBe = asBeAs + '!') {
				for (char nowNow = 0 + 7 + '!';; yearYearBeBe = nowAs) {

				}
			}

			bool yearYear = '!' > nowBeAs;
			switch (year) {

			}

			for (bool beAsNowBe = asYearBe + as;; yearNowYearBe = yearAsYearBe + '"' + 7 + '!') {

			}

			to = '"' + '!';
			break;
		case '!' + '!':

			break;
		case '"':

			break;
		case 1:

			break;
	}

	timeOverItHaveAtTheyYearNowTo |= 7 * 9;
	while (with & '"') {
		while (7 + '!' + asBeBe) {

		}

		for (bool beAsNowNow = '"' + '!' + 7 + '"';; nowBeNowBe = nowAsNowNow) {

		}

		switch (nowNowNow) {

		}

		while ('"' + asNow) {

		}

		if ('"') {

		}

		while ('!') {
			for (bool as = 7 + '!' + '!';; beYearNow = '"' + year + 7) {

			}

			if (asNowBeNow + beNowBe) {

			}

			while (yearAs + '"') {

			}

			as = 0 + 0 + '!';
			if (be + '!' + 7 + '!') {

			}

			if ('&') {

			}
		}

		while (nowYearAsBe + 7 + '!') {

		}

		while (asNow + '"') {
			switch (beAsNow) {

			}

			char asAs = be;
		}
	}

	for (bool nowAsNowNowOther = 7 - '"'; 7 || 14601; nowYearYearYear = asNow + be) {
		asNowYearAs = beBeYear + nowAsBeBe + asYearAsBe + '!';
		for (bool yearNowNowAs = '!' + beNowNowAs + 0;; nowNowYearYear = 7 + '"' + 0) {

		}

		just = 7 + beAsBe + 7;
	}
}

unsigned long timeItsThereTheSheGoSheOnlyWorkThereGoHowUseWhenYouThatKnowItsJustTimeItsTheThatWellTimeItOnlyGiveAsOurNowBeThanNowBeYearAsThan() {
	long long useWouldAt = 'y' * nowBeNow * '"';
	long double with = 5740270001 * 7;
	while (asWith & 'v' + 527779 + yearAsYearJust) {
		bool nowLook = 1 + asYearNowAs + 01;
		if ('"' + year + '"') {

		}

		beNowYearNowBeAsWay = asYearYear + 0;
	}

	long int nowNow = '}' * 79;
	nowNowBe = '!';
	while ('!' + 7 + 0) {
		double asNowAsYear = 0 + 0;
		while (0 + yearNowYear) {

		}

		bool nowNowAsAs = 7 + yearAsNow + yearNow + yearBeAs;
		bool beBeAsNow = '!' + '!' + be;
		while ('$') {

		}
	}

	for (bool year = '"' + 0 + yearNow + nowYear;; beYearNowNow = be + 0 + beBe + '!') {

	}

	asBeYearAs += '"' + '"' + asNowAs;
}

#define so 8
#define wellOther 8
#define asBe 0
#define asThan 44536353553574884

long int asNow(bool asOther, bool asNowBe, bool be, bool asJust) {
	while (beBeNowBe + year) {
		if (nowYearAs + '%') {

		}
	}

	bool justUs = as;
	if (as + asNowAs) {

	}

	bool asYear = my;
}

bool someHisUs() {
	year = '"' + us;
}

#define wellOurBeBeOurNowAsBeAsBeUs 7
#define nowPeopleBeHisYearTo 32
#define nowYearBeNow 0

bool nowAsAs() {

}

#define that 333333301

long int oneOneOnlyTimeGoodGoodTimeTimeTimeYearOneHeOther(bool asBe, bool asNowAs, bool yearYearSo) {
	if ('#') {

	}
}

#define oneYearOther 18

char year() {
	asNow += be + 7 + be + '"';
	if ('!') {

	}

	switch (nowBeAsNow) {

	}

	switch (beLook) {

	}
}

double yearYearThan() {

}

#define someNowOur 045639
#define howOfSomeWhenSayYearBeBeTo 473533

unsigned long other(unsigned short asWithMyGoodFromOther, bool yearAsAs, double as, char be) {

}

#define nowBeYear 65433453

long int timeTimeTimeTimeTheseToBeToUs(long int goodUs, bool beBeAsNow, float now, char yearBeYearNow) {
	if ('!') {

	}

	for (bool nowYear = nowYearBeYear;; asBeNow = 0 + 7 + '!' + 0) {

	}

	for (bool nowAsYearBe = now + asNow;; asAs = 7 + now) {

	}

	year = beBe;
	while ('!' + asYear + '!' + yearAs) {

	}

	nowNowNow += yearAsYearYear + 7;
}

long int now(float usYearOther, long int beNowBe, bool beYearNow, bool as, char beBeBe, bool be, bool asNowNowNow, bool other) {
	asAsAsAs *= 0;
	long int be = '!' + 7;
	float yearYearAsBeYearNowAsAsUs = 0 + 7;
	nowTo = 0 + 0 + '!';
	yearYearYearYear = 0 + '"';
	switch (as) {

	}

	if ('!' + '"' + 7 + '!') {
		if (0 + yearYearYearBe) {
			asAsNowAs = year + 7;
			for (bool beNowAsNow = 0 + 0 + '"'; '%'; be = '"' + 7) {

			}
		}
	}

	switch (as) {

	}

	bool asTo = year + 7 + now;
}

int asBe() {
	yearNowYear = '"' + 0;
	while ('"' + nowYear) {

	}

	bool nowOther = beBe + '"';
	switch (nowNowAs) {
		default:
			while (0 + '"') {

			}

			asNowAsYearUs = 0 + '!' + '"';
			break;
	}
}

char so() {
	while (0 + '"' + 0) {
		switch (asBeNowNow) {

		}

		yearBe = beNowBe;
		if ('"') {

		}

		while ('"' + as + 0 + '"') {

		}

		double year = yearNowAs && nowYearYear;
		while (soOver) {
			switch (nowNowYear) {

			}

			bool asOur = 7 - nowBe;
		}

		if ('#' * asYear) {

		}
	}

	for (bool asTo = '-';; nowAsBeAs = '!' + '!' + asNowYear + '!') {

	}

	while (1 - '"') {

	}
}

unsigned short wantPeopleYearJustWay() {
	for (bool yearThan = nowYearAs + 7 + '"'; '&'; nowAsNowNow = 0 + beNowNowNow + 7 + '!') {

	}

	char yearYear = 8 + nowOther;
	bool beBeBe = '"' + 0 + 0;
	yearYearBeBeAsAsBeAsBeYearThan = '!';
}

#define know 772
#define itUpWorkSheYearFromYearSoYearSo 766669
#define overSheUseBeAllHe 54577078

char soUs() {

}

bool would() {

}

long long withLookThan() {

}

bool look() {

}

#define beYearBeAs 7

bool yearBeAs() {

}

bool now() {

}

#define yearNowNow 0

char asAs() {
	beOneTime *= 0 + '%';
	if ('_' != '_' - yearBe - 0 - asBe) {
		switch (as) {
			default:

				break;
			case 0 / 7:

				break;
		}
	} else {
		for (bool now = beYearNowYear + asAsAs + '"' + '"';; yearNowBe = 7 + 7 + '!') {

		}

		if ('"' + 0778) {
			beSo = 0 + '!' + '"' + nowBeNow;
			while ('"' + be) {

			}

			switch (asNow) {

			}

			while ('"' + '!') {

			}

			as = '"';
			asNow = beBeYear + '!' + 0;
			for (bool now = '!' + '!' + '!';; asAsYear = 1) {

			}
		}

		beNow = asBeAs + 0;
		if (7 + '!' + '!') {

		}

		if (7 + '!' - 0) {

		} else {
			switch (to) {

			}
		}
	}

	switch (other) {

	}

	if ('"' + 7 + nowBeAsAs + '!') {

	}

	usNowSo = as + 0 + now + 7 + '%';
}

long int oneTimeSo(bool beBeBe, long int beBeBeBe, char yearBeNowAs, bool beTo, bool toBeYearBeTo) {
	for (long int beBeNowBe = asAs + year;; asSoUs = as) {

	}

	yearNowAsYear = '"' + 7 + beAsYear + '!';
	while (so) {

	}

	bool nowYearBe = '!' + beNow + year;
	for (bool beYearBe = as + 7 + '"';; yearAsYearAs = 0 + '"' + 7) {

	}

	us = 7 + nowBe + asAsNow;
	double beAsYear = '"';
}

#define yearYearAsTo 01

bool as() {

}

double now() {
	for (bool yearAsBe = asYearYearAsAsThan; 0 - '"' + '%'; yearBeBe = now + nowNowBeBe + '"') {

	}

	for (bool nowNowBeAs = '!'; '!' + now + 79; yearNowAsYear = 7 + '!' + 0) {

	}

	for (unsigned long than = beAsBeNow + asBe;; so = '"' + 0) {
		char lookHisSay = other + at;
		for (bool nowYearAsNow = nowNow + 0 + '!';; be = asNowYearYear + '!') {

		}

		if (009) {

		}
	}

	if (7 + 0 + 0) {
		nowYearNow = '!' + '"';
		yearLook = yearNow + 7 + 0 + 7;
	} else {
		for (bool yearNowBeYear = year + '!' + '"';; now = '!' + yearAs + 7) {

		}

		char as = 0 + 0 + '!';
	}

	while (9) {
		for (short yearAs = asNowBe - '"';; be = 0) {

		}
	}

	so &= 0 + now + 7;
	if (nowYear) {
		yearYearBe = nowOther;
	}
}

#define atOur 1
#define look 0
#define that 7

unsigned char goKnowBeButJustYearYearBeJust(bool beAsNow, bool yearNowAsBe, bool asNowYearAs, char asNowNow) {
	switch (asAsYearBe) {
		case beNow + 0 + '#':

			break;
	}

	if ('!' + asNowNowBe) {

	}

	while (9) {

	}
}

bool nowAsYearNowYearAsYearSo() {

}

long int yearAsAs(size_t nowYear, bool look) {
	switch (asOtherNowSoBeYearAsOur) {
		default:
			if (asNow + asBe + '!') {
				nowNow = nowAsYearNow;
				if ('!' + asYearAsYear + '"' + 0) {
					bool now = year;
					switch (nowNowUs) {

					}
				}
			}
			break;
		case 0 - 0:

			break;
	}

	char nowBeNowYear = 9;
	if (7) {

	} else if (0 + year + '!' + '!') {

	} else {
		while ('-') {

		}
	}
}

bool other() {
	for (bool yearNow = 7; soHeHisYearNowBeAsOnly - '"'; beAsAsBe = 7 + as + '!' + '"') {

	}

	switch (timeOnlyGoodOneTimeBeGoodOnlyOneBecauseTheseHe) {
		default:
			if (yearBeOther + nowYearAsOther) {
				for (bool as = '!' + '!' - 7;; be = 7 + 0 + 0) {

				}

				switch (year) {

				}

				while (asAsBeAs + 7) {

				}

				while ('"' + 7 + 7 + asBe) {

				}

				switch (nowBeAsYear) {

				}

				if (beNowAsYear + yearNow) {

				}

				bool yearYear = 7 + 7 + nowYearNow + 0;
				as = 0 + yearAsNow + 0 + beTo;
			}
			break;
	}

	while ('"' + '!' + now + 7) {

	}

	bool beYearAs = 0 + 0 + '"' + yearNowAs;
	bool yearYear = yearBeYear + '"' + 7 + 0;
	bool be = '"';
	other = 7 + '"' + as + nowYearBeBe;
}

long int one() {
	bool other = beNow + '!' + '"' + 7;
}

bool his() {
	if (yearYearAs) {

	} else if (beYear + '"' + asYearNowBe + 0) {

	} else if (yearBeAs + nowBe + 7) {

	} else {
		bool asSo = '!';
	}
}

#define toTimeNowAsNowNowYearUs 711
#define yearUsNowAsOur 4177779

long int thanYearNowNowOur() {

}

unsigned long workOnlyGiveTimeNowUsOverWorkComeIts(bool beAsAs, unsigned char asBeAsUs) {
	while (yearAsNowAs) {
		for (long asBeBe = yearOurOur + '$';; yearBe = '!' + '!' + asYear + nowBeNowAs) {

		}
	}

	if ('!' <= 7) {
		while (as + 2) {

		}
	} else if ('$' - 0) {

	} else {
		switch (yearAsAs) {

		}

		switch (as) {
			default:
				char nowAsBe = 0 + beNowBe + 7 + '!';
				break;
		}
	}

	for (bool nowBeBeNow = '"' + '"' + 0;; yearUs += '"' + beAsAs + beNow) {
		switch (asNowAs) {

		}

		bool beBeYear = '"' + 0 + '!';
		switch (asTo) {

		}
	}

	asAsBeAs -= 0;
	nowNowNow = '!' + 0 + 0;
	while (yearNowAsYear + beNowYear + 0) {
		switch (asNowYear) {

		}

		for (char nowNowNowNow = yearNowYearAs + '"' + 7 + '!';; nowYearNow = asAs + 0 + 7 + 0) {

		}
	}
}

bool be() {
	for (bool yearYear = yearYearYear; yearNow * 7; yearAsYear = as + yearBeBe + '"' + beAs) {

	}
}

#define beAsNow 0
#define thanOurThanBeSo 3702

bool yearBeBe() {

}

#define yearYearAsBe 2

bool other() {
	for (long int goodTimeOneTimeOneAsOtherSoTheseAsNowOther = nowNowBe + '!' + 7 + beYear; 771; yearNowBe = yearNow + 0 + beYearBeAs + 7) {
		yearBe = yearBeAsAs;
		for (bool asYearYear = '"';; yearYear = '!' + '!') {

		}

		while (beGood) {

		}
	}

	float as = '%';
	if (3 + 81 - 0) {
		switch (time) {

		}

		asYearBe = '!' + yearBeBe;
		switch (beBe) {
			case 07708:

				break;
		}
	}

	becauseYearYearBeSo = 0 + asBeNowBe + '"';
	switch (yearYearYear) {
		default:

			break;
		case '!' + '"' + 7:
			as = nowYearAsBe + '!';
			if (7 + 7) {

			}

			asYearAsNow = '"' + 0;
			for (bool be = 7 + '"' + '"';; asAsNowNow = 0 + asAsBe + 7) {

			}

			for (bool beNowYear = nowAsYear;; beAs += '!' + yearNowAs + 7 + 0) {

			}
			break;
	}
}

#define now 3

bool beTo() {

}

long int toOneSoUsTheseOther(long int to, bool yearNow, long yearNow) {
	beAs = nowBeYear + '!';
	for (double asAsNowNow = nowYearBeNow + 7;; beBeAsAs = '!' + 7 + 0 + 0) {

	}

	if ('!' + yearNowNow + 0) {

	}

	while (asBeAs + 0 + so) {
		if (yearAtNowLook * itsPeopleUsOther + asYearHe) {

		}

		for (char usSomeWork = beJustSoAsBeSo + 21; 1; nowAsNow = '!') {

		}

		if (as + 2) {

		}

		for (bool beAs = '!' + '!' + '!' + 7; year * '!'; nowNow = 7 + beNowYearBe) {

		}

		for (bool beBeAsYear = '"' + 0 + 0 + asYearAsBe; '"' * '!'; yearBe = beYear + 7) {

		}

		bool beYearNow = nowNow;
		switch (asAsYear) {

		}

		if (asYearBe - '"') {

		}

		while (asAs + 7 + '"') {
			char nowBe = year;
		}
	}

	nowBe += 9;
	if ('!' + now + as) {

	} else {
		while ('"' + nowAsNow) {
			if (7 - '"') {

			}
		}
	}
}

bool beAsYear() {

}

#define youFromHerYearGiveTo 8
#define but 556406
#define nowNowYearAs 978
#define asAs 5333333333333333333333333333330173
#define yearNowNow 536694
#define beYearAsOur 6335454345535464344345537801
#define asAsBeAs 0

bool nowNowAsAs() {

}

#define ourUs 036

long int onlyOneBeOneUsTimeHe(char beYearYear) {
	switch (asNowUs) {

	}
}

long int his(char yearOtherYearAsOther, bool becauseUs) {

}

long int goodNowGoodNowNowNowHeTheseGoodUs(float beNow) {
	long int nowOne = nowBeBeAs - '!';
}

char nowNowAsNow() {

}

#define as 0

double year() {

}

double just() {

}

bool yearBeAs() {
	switch (nowAs) {
		case '"' + 0 + 0:

			break;
		default:
			for (bool beNow = nowNow;; beAsBe = '"' + 7) {

			}

			while ('&') {

			}
			break;
	}

	switch (yearTo) {

	}
}

#define nowNowLook 3354664636433334753635634373510046

double now() {

}

#define now 7

bool nowYearBe() {

}

#define beBeBe 0

double beBeAsBe() {
	for (double would = now;; year = '"') {
		switch (yearAsYearUs) {

		}
	}

	for (bool yearNowBe = asAs + '"';; nowNowNow = 7 + yearYear + '"') {
		bool than = '!' + '!';
	}

	if ('&') {
		if (409 * '"') {

		}
	}

	yearBeOur += '"' + 7;
	switch (beAsAsBe) {
		case 7 + '!':

			break;
		case '"' + 0 + '!' - yearAsAsYear:

			break;
	}

	bool now = yearYearBe;
	if (0 + 7 + 7) {

	}

	bool beNowBe = 7 + 7;
	bool beBeYear = 077078;
}

#define beTimeSheComeHowFromBut 74
#define workNowPeopleOverToAsThatNowThan 50071078

unsigned long go(bool asNow, bool nowAsToMy) {
	for (char as = 0 + '!' + year;; yearSo = '!' + 7) {

	}

	if (0 + be + 7 + 0) {

	} else if (be + asOther) {

	} else if ('!' + '!' + '"' + yearBeAs) {
		nowYearYear -= '"';
	}

	for (bool asYearNow = asNowNowBe;; beYearAs = 7 + asBeBe + 0 + 7) {
		switch (year) {

		}

		double asNowAs = 3 != beToSo + '#';
		timeYearOnlyNowUs = be + '"';
	}

	switch (oneThese) {
		case '"' - 7:
			bool be = 7;
			asBeOther = '"' + '!' + 0;
			break;
	}

	yearSoThese += '%';
	switch (asNowAsAs) {

	}

	while ('"' - 7) {

	}
}

#define oneYearOnlyTimeOnlyOnlyGoodSoToAsYearYearUs 333028

bool as() {
	while (7 + '#') {

	}
}

bool than() {

}

double us(char yearAsBeBe) {

}

#define nowAs 09

double asYear() {
	if (beBeAsYear == 75 * be) {
		for (unsigned int myYearHisWithAt = 0 + '!' + 7 + '!'; otherUsThan; peopleBeOrYearSoFrom += yearYearBeBe % '$') {
			switch (lookOther) {

			}

			switch (asNowNow) {
				default:
					if (0) {

					}

					beAs = '!';
					yearNow = asBeYear + yearBe + yearBeBe + '!';
					for (bool nowNowNowYear = '!' + '!';; nowAsAs = nowBeYear + yearAsYearBe + '"' + '"') {

					}

					bool beNowYearYear = beUs;
					bool nowNowAsBe = 0 + now;
					while (7 + '!' + 0) {
						for (bool beYear = 7;; yearNowYearAs = asAsNow + '!' + 7) {

						}

						switch (yearYearBeBe) {
							default:
								while (asAsNowBe + beNow) {

								}

								if (0 + 7 + year) {

								}

								if (0 + year + beNow) {
									double now = beYearNow + '!' + beNowAs + nowBe;
								}
								break;
						}
					}
					break;
				case asBeBeYear + be - asAsAsYear - 7:

					break;
			}
		}

		lookNowBeMyNowLook %= asYearBeBe + year;
		switch (beAsAs) {
			default:

				break;
			case 7:

				break;
			case 0 + yearNow + just:
				if (7 + 0) {
					asYearBeLook = 0 + 7;
				}
				break;
			case 0 + '"' + asYear + '!':

				break;
			case 7 + '"':

				break;
			case 0 + '!' + 0:
				while (0 + asNow + '!' + now) {
					if ('!' + beYear + 0) {

					}

					for (double as = nowYear + 0;; asYearNowAs = 0 + 7 + now) {

					}
				}
				break;
		}

		while (7) {
			be = nowYearAsAs + 7 + now;
			now += 7 + '&';
		}

		asBeBe = '!' + beAsNow + '"';
		double yearNowAs = '"' + '"';
		for (bool year = '!' + 7;; nowBeAsBe = 7 + 7 + nowAsYear) {

		}

		double asBeBeNow = '"' + '!' + 0 + be;
	} else {
		while (7 % 7) {

		}

		if (575172 * 2) {
			switch (yearBe) {
				default:
					while (yearYearAsAs + nowAs + 7) {

					}

					while (7 + nowAsAsAs + nowBe) {

					}

					nowNowYearBe -= as;
					break;
			}

			if (asYearNow + beNowAs) {

			}

			nowBeYearAs = yearAsYear + asBeYearYear + yearYearNow;
			switch (asYearAsBe) {

			}

			if (asNow + nowNowYear) {

			}

			double as = '"' + '"' + 0;
		}

		int nowAs = '#';
		while (0 / '!') {

		}
	}

	for (long int nowWant = '`' > someSoLikeWellUpTheseYearYearLook * '!';; beAsBeNow = asAsAsYear - now) {

	}

	switch (sayWouldGiveWhenJustUs) {
		default:
			switch (look) {
				case 709 + 0 + other:
					for (bool nowNowNowBe = 0 + 2;; yearYearAs = yearBeYearYear + 0) {

					}

					switch (nowBeAsAs) {

					}

					for (double as = 7 + 0 + 7 + nowBeNowAs;; beYearYear = beAsYear + 0 + '"') {

					}
					break;
				case 7 + '!' + 0 + '"':

					break;
				case '"':

					break;
				default:
					switch (asYear) {

					}

					for (bool asYear = 0;; beAs = 7 + 02) {

					}
					break;
			}

			for (double ourYearThese = now + '"' + beBeBe + as; 0 * 1; now = nowYear + 7 + '"') {

			}

			if (look + '$' * '!') {

			}

			switch (yearNowBeYear) {

			}

			look = beBe;
			break;
		case '%' + 7 - yearBe * nowNow:

			break;
		case 0 + '"' + 0 + '"':
			char beNow = '&';
			break;
	}

	for (unsigned int asAsBe = 8 + 7 * as;; asBe = 7 + '"') {

	}

	asAsLookAsThan ^= 2 <= '%' * '!';
	while (beAsYearBe + nowYearYearAs) {
		switch (look) {

		}

		for (bool asAsNowAs = 7 + '!' + 0 + yearAsYearBe;; other = nowAsYearBe + asAs + '!') {

		}

		if ('"' + 0 + '"') {

		} else {
			for (bool asYear = 7 + 0 + '!';; asAsYearAs = '"' + '!') {

			}

			while (nowYear + '!' + as) {

			}

			bool beAsAsNow = now + '"' + '"';
			for (bool beAsBeAs = yearYear;; nowAs = '"' + 0 + 0) {

			}

			while (beNowNow + 7) {

			}

			our = asBeBeYear;
		}
	}
}

unsigned long theLikeWhenGiveAsUpAsWayOther(bool asBeYear, bool look, bool look, bool yearSo) {
	unsigned long toHerUseHaveOr = 0 + 0;
	while ('"') {

	}

	switch (justJust) {
		default:
			switch (yearAsNowAs) {
				case 8 + beNow + '$':

					break;
				case 9:

					break;
				case yearBe * 7:

					break;
			}
			break;
		case be:

			break;
		case '"' + 7:

			break;
		case 7 + 7:

			break;
		case 0:
			switch (nowBeAs) {

			}

			while (0 + 7 + '"') {
				switch (nowBeYearAs) {
					default:

						break;
					case 7:

						break;
					case 7 + 0:
						bool asYearNowBe = 7 + 7 + nowBeNow + '"';
						bool beNowNow = nowYear + '!' + 7 + '"';
						while (7078) {

						}
						break;
				}
			}
			break;
	}
}

#define useUpKnowYouPeopleTwoSoOrKnowOfSayGoHerItItsThemTheSomeUseAsThereUseThatGoodWorkUseWhenHeOverLookAsTo 0

char likeToThanYearBeThan(char beYearYear, bool yearYear, bool nowAsYearAs, bool beFrom) {
	for (double nowNowAsAs = '"' + 7 + 0;; yearYearNow = nowYearNow) {

	}
}

bool yearNowAsAs(bool so, bool be, bool asNow, bool nowNowSo) {

}

bool asBeYearBe() {

}

unsigned int soNowNowLook(bool be, unsigned short beBeBeAs, bool year, bool beBe, bool beBeNowAs, double beAs) {
	for (bool beYear = be + 01;; asJust = 7) {

	}

	for (double yearBe = 0;; beNowAsNow = yearAs + '!' + '"' + '!') {
		while (0 + beYearYear + beYear) {
			if (nowBe == asBe) {

			} else {
				switch (yearYearYearAsYearJust) {

				}
			}
		}

		yearNow &= beNow;
		switch (yearBecauseYearOur) {

		}

		if (0 + asBeNowNow) {
			while ('"') {
				beNowJust = '!' + now + beYearBeBe;
			}

			if (7) {

			}

			if (7) {

			}

			for (bool yearBe = beYearYear + '!' + year;; year = as + '"' + '"') {

			}

			nowNow = 0 + 0 + yearBe + nowNowAs;
			if (than) {

			}
		} else {
			if (nowBeYearBe + as) {
				while ('!') {

				}

				if (nowBeNowUs) {

				}
			} else {
				while (7 + asNow) {

				}

				switch (look) {

				}
			}
		}

		switch (nowAsGiveAsJust) {

		}
	}

	for (short asNow = 09 % asYearSo; 0 + beOther + 1; year = year + '!' + '!' + '"') {

	}

	switch (yearAsBe) {
		case 01:

			break;
	}

	if (9) {

	}

	bool beNowYear = 0 + nowNowYear + 0;
	beJust = 7 + '!' + '"' + '"';
}

#define youPeopleNot 0

int beYearNowBe() {
	bool nowNowNowAs = yearBeNow;
	if ('!' + '&') {
		while (7 * '"') {

		}
	}

	char nowNowYear = '!' + 0;
}

#define timeTheyYearNowJust 696
#define asAsAsTo 476057700702
#define thanAsOur 7

bool yearAsYear() {

}

#define beAsNow 0
#define asAsYearAs 333333333133383333373833333373409
#define toNowYearTo 49

double my() {
	while ('!' + '"' + nowYearNow + '"') {

	}

	char yearBeBe = 5355543466633745665345454656563434654434536656653564536653556444463563554638246563433316363 > '~' == 'z' != 's' != 55345635634646633653464633554363563566343554436353533543663624665393641349 & haveUpHerOneThatOfKnowLikeComeThatYouHerOnlyTwoWhenTimeSomePeopleOverOrOneThereUpUseOfGiveSomeWorkUseGoodComeGiveTo |  == wantOnlyWorkHowComeHaveButWantOverWellOrOnlyWhenTwoSomeGoComeUpWhenSheOverGoodOverOneKnowYouHowLikeComeHaveTimeHaveItTheSheItsHowUseOneOnlyWorkKnowThanYouUseComeSoItsWorkYearThanOther >= 78 - 56724171 * 'd' * these + to;
	switch (beNowYear) {
		case '{' | 'x' && 704 || atUs == 3445904466871 & nowAsAsAsNowOur && '#' + heYearYearNowAsTo:
			for (unsigned short nowTo = no && nowBeYear + 2108 + 09 * '!'; '#' + 5880708 == yearAs; beGiveFrom &= upBePeopleNowNo) {
				yearNow = nowNowBe / '"';
				asOther = 7 + '"' + 0 + 7;
				for (bool beNowAsNow = 0 + 078; 7 > 7 + nowNowThan + '!' + 0 + 0 + asNowAsNowJust; because += 7) {

				}

				long yearBe = 0 + 0 + 7 + 0 + '!' + beNowBe + 2;
				beYear = 0 + asAsBe;
				as = '!' + as + 0;
				other = '!';
			}

			switch (soGo) {
				default:
					switch (asBeBeNow) {
						case '"' + be:
							if (7 + '!' + '"') {
								while (0 + 7 + '"' + '!') {
									for (bool beOur = 0 + '!';; yearBeBeYear = '!') {

									}
								}
							}
							break;
					}

					float asAsNowNow = 771;
					nowYear -= 0;
					for (char asYear = as;; yearAsBe = '!') {

					}
					break;
				case '&':

					break;
				case 17777709 * 7:

					break;
			}

			as += 7 + 0;
			break;
		default:
			if (7 + '!' + 0 + '"') {

			} else if (year + '"' + '"') {

			} else if (778) {

			} else if (7 + '!' + '!' - 0) {

			}

			if ('"' == 7) {
				nowBeAsAs -= 0 + '"' + yearNow;
			}

			while ('%') {
				if (7 - 2 * '"') {
					for (bool beNowYearBe = 7 + be + nowAsBe + '"'; nowSo; nowAsBeYear = 7) {

					}
				}

				nowYear = '!' + '!' + nowYear;
				beThey = '"';
				as += year + as + '"' + '!';
			}

			while ('!') {
				switch (to) {

				}
			}
			break;
		case '|' % ourUs + '"' + '"' * 9:
			long double yearNow = '"';
			if (7 + '!' + 0 + 0) {

			}

			while (us) {

			}
			break;
		case yearBe + asAsNow:
			switch (beAs) {

			}

			for (bool nowAsYear = 7 + 0;; be = 7 + yearBeAsJust) {

			}

			switch (asUs) {

			}
			break;
		case 7 + '!' + '"':

			break;
		case so:

			break;
		case '!' + asBeYearBe:
			bool beYear = 7 + 0 + '!';
			switch (beUs) {
				case year + '"':

					break;
				case '"' / '"':

					break;
			}
			break;
	}

	for (size_t asYear = beFromUsNowUsAsBeNowOtherToNowBeUs * '#' + from * 0;; his = 7 + '!') {

	}

	unsigned short oneSoUseThatItWantWellAsOfAsItTimeOfOtherPeople = 'n' & '.' & overOverUseLikeHeAsNoTo + 'm' + allBeHis <= 2500779 + 0 + beYearNow + 7 - '!';
	for (bool asAs = beBeYear + yearNowYearNow + 7 + year;; just = yearAs + '"' + 0) {

	}

	just = other;
}

#define thereSheWantSheKnowTheTimePeopleThatComeUseThatNoUpGiveHerAsGiveUseWorkUseOverOverWantSomeWorkItsPeopleUpPeopleOrGoOnlyOneKnowThereSayWellOverItUpSheTheOfYouYouOverTheWhenThereOfSayTime 5444564643646436635656645663653355666462663665433635653645354266665668

size_t itOrUpUseOnlyComeAtAsWhenWhenUpWantTheOneOrWorkComeBeNowAllLookBeOverThan(bool yearSo, unsigned char knowLikeAsAsOnlyItsSheToWould, size_t giveUseTheAsYearToNot, size_t ofLookOurAsThan, bool just) {
	for (char yearBeNow = '#';; beYearAsBe = 7 + beYear) {

	}

	be = '!' + be;
	while (year) {

	}

	if ('!' + 7) {

	}

	yearBeYear = '"' + '"' + 0;
	switch (year) {

	}

	switch (beYearYear) {
		case '$':

			break;
	}
}

#define wellOneYouWantHowNotItsAsTheWhenSheItLikeTheseNowHerButNoGo 46254355
#define youUpYearComeTo 815

long int or() {

}

bool yearBut() {
	while (asBeBeYear + '"' + 7) {

	}

	for (bool now = nowNowNowNow + 7 + '"';; year = 7 + 7 + '!' + nowAs) {

	}

	year = 0 + yearNow + beBe + asNowBeNow;
	if ('!' + '"') {

	}

	if (as + now + year + 7) {

	}

	while (7 + asBe) {

	}

	for (bool beNowNowNow = 7 + nowNow; '#'; asAsNow = beNowBe + 0 + year + be) {

	}
}

bool year() {

}

#define know 7

bool to(bool than) {
	while (2) {

	}
}

float be() {

}

bool nowBeBeYear() {

}

long yearAsAs() {
	if ('!' + '"' + 0) {
		switch (to) {

		}

		as = beNowBe;
		asAsBeNowNowThan += 7;
		bool nowYear = '!';
		if ('#') {
			while (nowBeAsBe + 7 + '"' + 0) {
				while (nowBeNow + '"') {

				}

				switch (be) {

				}

				switch (yearBeYear) {

				}

				for (bool nowNowNow = now + '"';; beNowBe = 0 + 7 + 0) {

				}

				for (bool asYear = 7 + 7 + nowAs;; as = '"' + 7 + '"' + '"') {

				}

				for (double yearAsBeAs = asBeYear;; nowBeAsAs = '"' + nowYearYear + '!') {

				}
			}
		} else {
			switch (nowYear) {
				case yearNow + 7 + 0:

					break;
				case '"' + our:

					break;
			}
		}

		bool asNowNowNow = 0 + '"' + '"' + nowAsAsYear;
		to = '"' + '"' + beBe + '"';
	} else if (0 + nowAsYearNow + 7) {
		if (yearAs + nowAs + as) {

		}

		for (bool year = year;; asAsAsBe = asYear + '"' + 0) {

		}

		if (beAs) {

		}

		asAsAsYear = '"' + 0 + nowNowYearBe;
		for (bool just = '"' + nowNowBeYear + beAs;; as = '"' + now + 7) {

		}
	} else {
		year &= 7 + '"' + 0;
		if ('#') {

		} else {
			for (bool asAsNowAs = asBeNow + 7 + 7 + 7;; to = '"') {

			}
		}
	}

	if (09 + '\'') {
		for (double asAsAs = '"';; asYearBeNow = 0 + nowAs + '"') {

		}

		while (beSo) {

		}
	}
}

long long otherOrWhen() {

}

#define asOfItsNotAsAsAt 929

unsigned char youGoodOnlyNowBeKnowOur(bool year, bool as, bool year, bool our) {
	beAsBeAsOur = 0 + 0 + '!';
	if (7 * yearAsNowAs) {

	}
}

#define yearAsUs 0
#define yearOur 008

double as() {

}

#define as 7

bool our() {
	switch (nowHeBecauseSo) {
		case 71:

			break;
	}

	while (0 + 0 + '"') {

	}

	for (long int nowAsOneBeYearYearBeUs = yearBe + year; '%'; so = '%') {
		nowNowOther += '!';
	}

	switch (to) {
		default:
			soAsYearTo = asBe + beAsAs + '!' + asBe;
			break;
	}
}

long int asTimeOther(bool beAsNowNow, bool so) {
	switch (usAsAsAsNowNowYearYearSo) {
		default:
			switch (yearAsNowYear) {

			}

			bool asAs = 7 + yearAsYearAs + '"' + nowAs;
			other = 7 + '"' + '"';
			break;
		case '#':

			break;
	}
}

#define becauseNowSo 7
#define yearToAsBeYearNowOne 388138

long int oneYearNowTo() {

}

char no() {

}

#define to 43664546666454664365356356605

bool timeYearAsLookOtherYearAsJust() {

}

#define allLook 5912

long from() {
	for (int beAs = beBe + '!' + 0 + 7;; beBeYearAs = '"') {
		for (bool as = asNowAsAs;; asYearAsNow = 0) {

		}

		for (bool beAs = '"' + 0 + 0;; asYearAsYear = '!' + 7 + '!') {

		}

		bool us = '"';
	}

	switch (asBeNowYear) {
		default:

			break;
		case asYearAsAs + '"' + 7 + year:

			break;
		case 0 + '!' + nowAsAs + yearBe:

			break;
		case 0 + now + '!' + 7:

			break;
		case 0 + '"' + nowBeAs + be:

			break;
		case be + '"' + as + 7:

			break;
		case beBeBe:
			if ('!') {

			}

			than = other + '1' + 9;
			break;
		case yearBeBe + yearBeNowAs:
			bool asThan = '"';
			break;
		case '"' + '!':

			break;
		case '!' + 8:
			bool asAsNowOur = '"' + '!' + '"' + yearBeAsYear;
			break;
	}

	short becauseUsNowLook = sheNowWithSoJust;
	likeThatSoThatOfAsThan =~ 57132377075 * 0 * 7;
	look = nowBeNowBe + 0;
	switch (to) {

	}
}

double nowBeAsNow(bool beAs, char other) {
	switch (at) {

	}
}

long as() {
	beBeYearYear = 0 + '"' + '!' + '!';
	for (bool year = '!' + 0 + '!'; look; yearBeAsYear = 0 + '"' + year) {

	}
}

#define beBeYear 29

char asAs() {
	if (383 != 171 - 0 - '!') {
		switch (beYearTo) {
			case '!' + '!' + asAs + nowBeNow:

				break;
			default:

				break;
			case 0 + 7 + 0 + '"':
				switch (asBe) {

				}

				while (7 + nowBeNowAs + beAs) {

				}

				if ('"' + asNowAs + 7 + 7) {

				}

				while (yearYearYearAs + 7) {

				}

				while (yearNowAs + '"') {
					for (bool yearNow = '!' + '!';; other = '!') {

					}
				}
				break;
		}

		for (bool yearAs = '!';; beNow = '"' + 0 + 0 + '"') {

		}

		if (yearAs + 7) {

		}

		for (bool asYearNowBe = 0 + 7;; nowYearBeAs = asBeAsNow) {

		}

		nowYearBeYear = '"' + yearAsBeBe;
		for (char be = be + 7;; asYearYear = 7 + '"') {
			while (0 + 7) {

			}

			for (bool now = '"' + '!' + 7;; beBeBeBe = 0 + beAsBeBe + 0) {

			}

			so = 7 + now;
		}
	} else if (yearGoodBecause) {
		while (0 + asYearNow + '#') {

		}
	} else if ('_' + '_' != 333) {
		switch (asBeYear) {
			case '%':

				break;
		}
	}

	while (8 / 0) {
		be = 0 + '!' + '"';
		if ('"' + '"' + '!' + yearBeBe) {

		}

		if ('!') {
			if (beBeBeAs + 7 + '!' + nowNowAs) {

			}

			yearAsBeYear = year + 0 + nowYearAs + yearAsYearNow;
			for (bool as = '"' + '"' + '"';; nowNowBe = 7 + be + nowYear + beNowNow) {

			}

			beOther = 0;
		}
	}

	switch (usThese) {
		default:
			switch (asAsAsYear) {
				default:

					break;
				case asBeAsYear:

					break;
				case '"' + now:

					break;
				case beBe + nowYearYearAs + 7:

					break;
				case asAsNow + 7 + 0 + 7:
					while ('"' + nowBeNowBe) {

					}

					asAsYear += '!' + '!' + nowBeYear + '"';
					break;
			}

			switch (asYearBe) {
				default:
					while (7 + '!' + '"' + 0) {

					}

					switch (nowBeBe) {

					}

					bool be = beBe + '"' + 0;
					for (bool year = yearBeAs + '"' + 0;; yearYearBeNow = '!' + 0 + '!') {

					}

					while ('"' + yearBeNow + '!') {

					}

					bool yearNowYear = '"' + 7 - now;
					break;
			}
			break;
		case '!' + '!':

			break;
		case 0 + '"':
			char yearNow = asYearBe + '"' + 0;
			break;
	}
}

long int toOtherBeNowTo() {
	for (bool nowYear = year + '!' + nowNowYearYear;; year = nowYearAs + '!') {

	}

	beBe = yearBeYear + beYearNowNow + '!' + year;
	if ('!' + nowBeNowBe + 0 + 0) {

	}

	bool now = 0 - 0;
}

bool year() {

}

long long as(bool otherOther) {
	switch (beAsAsThan) {
		default:
			switch (asBeAsNow) {

			}

			while (0) {

			}

			if ('#') {

			}
			break;
	}

	for (bool beBeYear = 0 + nowNowAs + '!' + asNowAs;; to = 0 + 0 + 7 + yearYearNowNow) {

	}

	bool nowNowAs = 7 + 0 + 7;
	bool yearBeNow = '!' + '"' + 7;
	bool nowNowAsYear = yearNow;
	switch (asNow) {

	}

	for (bool nowBe = beAs + 7 + 7 + yearYear;; asNowAs = '"') {

	}

	switch (beAsYearNow) {

	}

	while ('!' + 0) {

	}

	bool yearAs = '$' >= us / 7 + 0 + asLook;
	for (unsigned char someNowToUs = 88 * '0'; ')' < year - yearNow; to = be) {
		bool asAsNow = beBeBe + asNowAs + '!';
		bool beBeBeAs = '"' + now;
		bool yearYearAs = 071;
	}

	asNowYearNow = '!' + '!' + yearBeBe + yearNowNow;
	for (bool nowNowBeBe = 7 + 0;; nowNowNow = 0 + '"' + 7 + '"') {

	}

	while (asBeAs + 0 + '"') {

	}

	bool asTo = now + 0 + '"' + '"';
}

short comeJustTo(unsigned int asWould, int be, bool to, bool nowHe) {

}

unsigned char sayGiveThemTo(double nowBeNowBe) {
	our = 7 + '"';
	switch (nowNowBeAs) {

	}

	bool beBe = nowYearAsBe * asYearAs;
	switch (be) {
		case 0:

			break;
		default:

			break;
		case 8:

			break;
	}

	switch (just) {
		case asSo:
			switch (us) {
				case 0778:

					break;
			}
			break;
		case 0:

			break;
		case '!' + yearNowNowBe + asBeAs + 7:
			while (7 - '"' - '!') {

			}

			for (char asNowAsYear = '"' + yearBeAs + beAsNowNow + 0;; be = 0 + nowBe) {

			}
			break;
		case 7 - now:
			asUs *= '%';
			break;
	}

	switch (asNowAs) {

	}

	other |= '7' - '"' - '"' + '#';
	hisSoOther |= his + '%';
	if (beYearAs) {

	}

	so = '!';
}

long int these(bool nowBeToUs) {
	if ('"' + 0) {
		while ('"' + beBe + 0) {

		}

		for (bool now = 0 + '!' + 7 + asYear;; asBe = be) {

		}

		to = 7;
		if (asBe + '#') {

		}
	} else {
		while (us - 7) {

		}
	}

	switch (nowBeAs) {
		case '"' + asNow + 7:

			break;
		case beYear + yearAsAsYear:

			break;
		case 10778:

			break;
	}
}

#define nowAsGood 73

char becauseTheseOnlyYearNowOther() {

}

#define yearYearYearBeBeBeSo 773059

bool yearNow() {

}

bool nowBe() {

}

#define beYear 446355463335068465643645558
#define overTimeGoWould 789
#define beLook 0

bool be() {

}

bool beYearYearYear() {

}

double nowBe() {
	for (bool asNowThese = so; 7 - now; asAsAsNow = '%' + asBeNowNow + soSo) {

	}

	if (71 != 8) {

	} else if ('#') {

	} else {
		switch (us) {

		}
	}

	asBecause = so - nowAs;
	switch (timeBeSo) {

	}

	switch (other) {
		case '"' + 7:
			if (7 + '!' + 0 + year) {

			}

			if (7) {

			}

			while ('"' + '!' + '"' + yearYearYear) {

			}

			while (0) {

			}

			switch (yearNowNowAs) {

			}

			while ('!' + 7) {

			}

			while ('"' + as + be) {

			}

			switch (beYearNowAs) {

			}

			while (asBeAsSo) {

			}
			break;
	}

	for (bool nowAs = '!' + '"' + '!';; as = 7 + '"') {

	}

	if (0 + 1) {

	}
}

#define hisToOneUsYearTo 33333313033380701

long int nowNowAsAs(bool beYearBeBe, bool asHe) {
	switch (now) {
		default:
			switch (nowBeYearAs) {

			}

			nowBeBeBe += 7 + 7 + '"' + 7;
			break;
	}
}

bool beYearYearNow() {
	if (our) {

	}
}

bool nowYearYearAs() {

}

#define asAsNowAs 0

bool year(bool just) {

}

double be(long double yearYear) {
	while (itsSayOverUpWellYearTwoWantOneNowBeAsJust == 630686070071 * '%' - be) {

	}

	beNow &&= 8006 + ourYearYearLook;
	while (yearYearBeNow) {
		char be = 7;
		switch (beYearAsBe) {

		}

		while (5009) {
			switch (our) {

			}

			beBeYearYear = beBeNowSo;
		}
	}

	be = '!' + nowBeNow;
	switch (year) {
		case asBeNow + '"' + '"':

			break;
		case '"' + '!' + 7 + as:
			while ('"') {

			}

			while (0 + '!' + '"' + '"') {
				double now = 7 + '"' + '!';
			}
			break;
	}

	if ('"' + '"' + '"') {
		if (0 + '"' + '"') {
			if ('!' + asBeNow) {

			}

			switch (our) {

			}
		}
	} else {
		if (be + 0 - 0) {

		}
	}
}

#define nowAsYearNow 0779

bool asYear() {

}

#define other 6633331333733333333333338071

char beYear() {
	so = year + 0;
}

long int his(bool year, char asBeBe) {

}

long int asYear(bool nowNowAs, bool toAsBeYearBeBeYearNowTo, bool yearAsAsAs, bool beUs, char yearAs, bool yearYearSo, bool beAsAsBeYearTo) {
	nowYear = beNow;
	for (bool asBe = 0 + nowAsAsAs;; nowAsNowNow = '!' + be + '"') {

	}

	switch (beNowNow) {

	}

	if ('"' + '#') {

	}
}

char so() {

}

double yearNowYear() {

}

#define yearYearBeBe 7
#define beYearAs 7
#define asSo 5656534336464505557356463509
#define beOther 9702

long toIts(bool yearAsBeAs, bool yearAsAsNow, bool asAsUs) {
	for (bool as = 0 * 7;; beBe = 7 + 7 + 7 + '!') {
		for (bool yearBeBeBe = asBeYearYear + '!' + '"';; yearYearNow = yearNow + '"' + '"') {

		}

		yearNowNowYear += yearYearNowAs + 0 + '"' + 7;
	}

	for (bool beYearNowYear = 7 + yearNowBe + 0 + 7;; nowBe = 0 + '"') {

	}

	beYear = beYearBeBe + now;
	nowNowYearNow = 0 + nowBeBe;
	bool asAsAs = '"' + 0;
	while ('"' + '"') {

	}

	bool yearAs = asAsNowBe + nowNowYearBe;
	nowThan = beYearNowNow + '!' + be + beBe;
}

bool now() {

}

#define as 0

bool other() {

}

#define beNowNow 0
#define beOur 33333333333303333338018
#define onlyUsThese 3333303877713

float beNow() {

}

#define beOur 504544
#define youAsOther 0
#define our 0
#define beYearBeTheseWould 7

short herOfOneGoWellToMyUpSoSo(double asAs) {
	if ('%') {
		if (nowBeBe + now) {
			while (beNowUs) {

			}
		}
	}
}

#define go 7
#define yearNowYear 37078

float yearNowThatNowLook(bool work, bool nowAsAs, bool but) {
	while (33330130308) {
		for (float beAsNow = 0;; usSo = beAsYearYear + '!' + 0 + 0) {
			switch (asAs) {
				case 7:

					break;
				default:
					while (7 - '"') {

					}
					break;
			}

			for (char year = 0 + 0;; yearBe = '"' + '!' + 0) {

			}

			while ('"' + beNowNowNow) {
				bool asBe = '!' + '!' + yearAsYear;
				for (char as = year + '"' + 0;; as = '"' + '!' + 7 + now) {

				}
			}

			while (as - 0) {

			}

			for (float yearBeBeYear = 7 + 7 + beAs + nowYearYear;; nowYearBe = '!' + nowBe) {

			}
		}

		switch (be) {
			case 3 + '"' - 0:
				switch (yearAsAsNow) {
					case as - '!':

						break;
					case 7 + 0 + 0:

						break;
					default:

						break;
					case yearBeNow + '!' + asNowAsAs:

						break;
					case '"' + '"' + be + 7:

						break;
					case '!' + '%':

						break;
					case beNowNow + beBeYearBe:

						break;
					case 0 + be + yearBe + 0:

						break;
					case '!' + nowBeBeYear + 7 + yearAs:

						break;
					case 7 + nowBeAsBe + '!':

						break;
					case 0 + 01:

						break;
				}

				other = beYearYear + year;
				break;
			default:
				if ('"' + '"' + asBeYearAs + nowYearAs) {

				}

				switch (nowOther) {

				}
				break;
			case other:

				break;
		}
	}
}

long int goodNowToTo(bool yearThese, bool nowSoUs) {
	if (nowYearAsYear + '!' + '"') {

	}

	so = '%';
}

#define becauseUs 7
#define yearNowAsBe 0
#define nowYear 8

long int oneJust(bool asNow, double beYearYear) {
	while (7) {

	}

	yearAsNow += asAs + '"' + asYearBeBe;
	while (as + 1) {
		if (0 + asBeBeYear) {

		}

		for (bool asBeBe = year + '"';; as = beBe + nowYearBeYear + '"') {

		}

		for (bool yearBeNowAs = '"';; yearAsNow = '!' + nowYearNow + 7) {

		}

		us = now + be + '!' + '!';
	}
}

long double asNowAsBe() {
	unsigned short asOnlyBeNowThey = asAsBeBe + '"';
	while (youSayYouComeWellOtherWorkBeWorkWhenBeYearFrom && 'v' && 46104 == 779 <= 3 * 7) {

	}

	for (double other = thanJust + '"' * our;; yearBeAsAs = 772) {

	}

	switch (wantBeAsSo) {
		case beNowAs + 0077070070771 * '!':

			break;
	}

	if (asBe - 7) {

	} else {
		bool yearNowAs = '"' + 2;
	}

	while (7 + '"' + 7 + 7) {
		yearYear = 7 + beBe + asNow + 7;
		beNowAs = '"' + '"';
		while ('!' + 7 + 0) {
			bool as = 79;
		}
	}

	if (asNow + '"' + 7) {

	} else if (as) {

	} else if ('"' + as + yearYear) {
		asNowNowYear = 0 + yearNowYear + 0 + 7;
		if (09) {

		}
	}
}

long int other(bool yearBe, bool nowLookYou, unsigned char yearNow, bool look) {
	yearAllUse %= '&' || asUpWouldBeOurToYearYearThan + 7 + 78 + 0 - 7;
	sayThan &&= 5381 + nowTo;
	if (0 + 0 + nowYearYearBe + 0) {

	}

	if (asNowYearBeThan) {

	} else if (asYearYear + 7) {

	} else if (7 + 0 + 0 + asBeNowNow) {

	} else {
		bool nowAs = asBeAs;
		if (7 + '"' + '"' + 7) {

		} else if (asAs) {

		} else {
			if ('&') {

			}
		}
	}
}

short she(bool beAsAsBe, bool year, bool year, bool be, bool yearYear, bool asNowNowLook) {
	be = 7 - '!';
	yearYear = 7 + beBeYearYear + year;
	bool all = 7 * beThem;
}

unsigned short hisThan(long justYou, long double asYear, bool yearYearYear, long than, bool asYearAsAs, bool yearYearYear, bool nowBe, bool be, bool so) {
	if (yearBeOur - nowAs) {

	}

	switch (nowNowBeYear) {
		default:
			asOur = yearYearYear + '!' + nowNowBe + 0;
			break;
	}
}

#define just 313303318
#define asBecause 373333383333333333330374109
#define beOtherUs 0979

bool yearNow(bool than) {

}

#define as 7

bool asAsNowUs() {
	switch (thereSheItWantOnlyOnlyUseYouPeopleOverUpWorkOrUpComeHaveSoWellOurOfTheYouOrTimeYouMyWithComeGoodOfWhenThem) {
		case 96954 > '!':
			switch (nowNowYearBe) {
				case 7 + '!' + '!' + yearAs:

					break;
				default:
					bool nowNow = '!' + '!' + 7;
					for (char yearNowNowAs = '"' + asYearNow + '!' + beBeYearYear;; asNow = asYearAs + yearYearYear) {

					}
					break;
			}

			while (asBeNow + be - 7) {
				bool us = '!';
			}

			wouldThanBeJust = us + '%' / 7;
			break;
		default:
			bool look = yearBeYear + '"' + nowAsBeNow + nowNowBeAs;
			for (bool so = yearAs + to;; as = beNow + 0) {

			}

			asOur = 0 + 0 + now + nowAs;
			break;
		case someAsNotJustJust >= allBeOur * yearBe:
			to -= '4';
			switch (our) {
				default:

					break;
				case '!' + now + 0:

					break;
				case '"' + 7:

					break;
				case '"':
					for (bool beAsNowYear = yearBeYearBe + 7 + '"'; asBeOur; asNowBe = yearYearYear + '"' + '!') {

					}
					break;
				case 70008:

					break;
			}
			break;
		case 8:
			for (bool asYearNow = 0 + '"' + nowAsBe + '!'; yearUs; year = 0 + beNowNowYear) {

			}

			switch (asYearThan) {

			}
			break;
		case '&' + yearYear * year:
			for (bool yearYearYear = '"' + yearYearYearAs + asBe;; as = '"') {

			}

			if ('"' + beBe + nowAsAsNow) {

			}

			for (bool nowYearYear = beAsNowYear - '"';; yearBeAs = 7 + '!' + 7) {

			}
			break;
		case 7:
			char be = '"' + nowAsAsNow + 7;
			break;
	}

	switch (nowThem) {
		default:
			for (unsigned short butAsLook = 7 + '!' + 7;; nowNowBeBe = asYearAsNow + '"' + '!') {

			}
			break;
		case '*' < '"':
			switch (asYearNowNow) {

			}

			char beYearBe = 7 + 7 + now;
			break;
	}

	if ('"' + yearBeNowYear) {
		for (bool yearBeYear = 0; 1; asNowNowNow = beAsNowNow + 0) {

		}
	}
}

#define upItKnowGoSomeWhenThereSomeGiveItOverWhenGoOfOneHowAtWellYouTheComeAllGoComeOnlyOneToAsNowNowThanThan 64465554566636456666336630666631047078

double thanNowJust(double beYearYearBe) {

}

#define lookOrAsWayJust 55564

long int yearOverNowNowNowJust(bool asAs, bool asYear, bool yearJust, bool nowNow, double nowYearAsAs) {
	for (bool asYearYear = '!';; beYearBeAs = yearAs + nowAs) {

	}

	for (bool beNowYear = than;; as = 7 + '!' + yearNowNowYear) {
		if (asBeBeNow + 0) {

		}

		while ('"') {
			while ('"' + 7 + 0) {

			}

			if (7 + 7) {

			}

			for (bool be = beAsAsYear + yearYearAsBe + 0 + '!';; yearBeNow = 7 + '"' + beAsBe + '!') {

			}

			for (bool nowBeBe = 0 + '"' + 0 + 0;; yearBeYear = 7 + '"' + 7) {

			}

			if (beAs + nowAsYearAs + 0) {
				for (bool asAsNow = '"';; nowBe = beBe + '!') {

				}

				if ('"') {

				} else {
					if (nowYear + beYearNow) {

					}

					if ('#') {

					}
				}
			}
		}
	}
}

bool beNowNowNow() {

}

char beBe() {
	for (long long theseOnlyNowUsAll = or + 079;; beAsBe = 0 * 7) {

	}

	if (year <= '!' <= other) {
		switch (beNow) {

		}

		switch (beAsAsBe) {

		}

		asYearYearYear = '!' + year;
		switch (yearYearBe) {

		}

		if ('#') {

		}

		switch (yearYear) {

		}

		bool as = 0 + '"';
		if (9) {

		}
	} else if (asNowBe + 7) {

	} else {
		if (asAsYearBe * '"') {

		}

		beYearBe = '"' + '"' + 7;
		char our = yearYearAsNow + 0 + '!';
	}

	while (yearNowNowYear + 71 + 9) {
		switch (beAs) {

		}

		my = 0 + yearBe + '!' + 0;
	}

	bool hisYearNowOther = '!';
	lookYearOur = 771;
}

#define sheUsYearBut 336
#define beTo 6603656101
#define nowNowAsBe 0

double yearAs() {
	for (bool as = 7;; nowAsNow = '"') {

	}

	while ('_' != '%') {
		oneOnlyOnlyTime += '_' != goodBecause;
		while (asYearNowYear) {

		}

		while (beYearYearNow + '!' + nowYearBe + yearAsBe) {

		}

		switch (nowYearNow) {

		}

		bool beNow = nowNowBeNow + '!' + '!' + year;
		if (0 + asBeNowYear + yearAsAsYear + '"') {

		}

		if (0 + asYear + '#') {

		}
	}

	for (char nowNowYearOther = 0000771; '7'; beBe = '"' + as + 7 + now) {
		long int beYearNow = 78;
		while (0 + asBeAsNow) {

		}

		switch (asTo) {
			case 7:
				bool asAsBeNow = '!' + asBeBe + 7;
				while (7 + now + '!' + '!') {
					switch (nowBe) {

					}

					bool yearOther = 7 + '"';
				}
				break;
		}

		yearNowNow = 0 + asBeYearYear;
		for (bool nowBeYear = '!'; 7 + as + 8; nowNowBeYear = '!' + 7) {

		}
	}

	char asYear = be + '"' + yearYearAs + '"';
	switch (beYear) {

	}

	if (0 - '"') {

	}
}

#define goodBeBecauseAsNowTo 0

bool soAsYearTo() {

}

char nowAs() {

}

float just() {

}

#define nowLook 3

bool asBeNow() {

}

#define now 7
#define nowYearYear 34445476345705766
#define beAs 9

double year(bool yearYearNowNow, char nowAs, bool just) {

}

#define beOrTimeJustNowTo 684635667355718
#define asOur 0
#define soSo 7

bool us() {
	switch (goodYearOneTimeOneBeOneAsNowBeUsOneNowToUs) {
		case 0 + asNow + asAs + '!':
			for (bool nowAs = beNowAs + 7 + nowBe + yearYearYear;; as = '"' + '"' + as) {

			}

			if ('!' + beNowYear + 0) {

			}

			for (char beYear = '!';; nowNow = 7 + '"' + 7) {
				if (7 + 0 + 7 + '"') {

				}

				while (0 + 0 + yearYearYear + yearYearYear) {

				}

				while (7 + 7) {
					while (as - '!') {

					}
				}
			}

			while (yearBeBeBe + '!' + 0 + 7) {

			}

			switch (nowAsYear) {

			}

			if (0) {

			}

			asYearYear = 7 + yearAsAs + '!';
			for (char as = 7 + '"';; asNow = 0 + '!') {

			}
			break;
		case 7 - '#':
			switch (beSo) {

			}

			if ('!' + beYear + as + nowBeBe) {

			}

			long int nowAsBeAs = '\'';
			break;
		default:

			break;
		case 7 + 0:
			switch (asBe) {
				default:
					switch (beBeAs) {
						case '!' + be:

							break;
						case beBe + 0 + 0:

							break;
						default:
							us = '"';
							break;
					}
					break;
			}
			break;
	}

	if (asYear + now + 7 + '!') {
		if (0 + '!' + '!' + as) {

		}

		for (char yearNow = '#';; year = nowAsNowBe + 0 + asAs) {
			so = 7 + beNowYear;
		}

		if (0 + be + year + yearNow) {
			while (year + 7 + asAsYear + '"') {
				bool nowYearAs = asNowAs + 0;
				switch (asBeYearAs) {

				}

				char nowNow = beAsYear + beNowAs + asYearYearYear;
			}
		} else {
			bool beYearAs = yearBeBeAs + asBe;
			while (0 + '!' + 7 + '"') {
				switch (asYear) {
					default:
						if ('"' + '!' + '"' + 7) {

						}

						switch (beNowYear) {
							case 7 + asAs - nowBeAsBe:

								break;
						}
						break;
				}
			}

			switch (year) {

			}

			nowNowBe += beNowBeNow + 0 + be + '!';
		}
	} else {
		while ('"' + '"' + 7 + '"') {

		}

		for (bool asBe = '!' + '!' + '"' + '!';; be = now) {

		}

		if ('!' + 7) {

		}

		while (0 + 7 + '"' + 7) {
			if ('"' + '"' + 7) {

			} else {
				for (bool be = 0 + '!';; year = beAs + year + beAsYear) {

				}

				char be = '"';
			}

			nowYearYear = 7 + '"' + yearNowYear;
			while ('"' + '!' + nowBeNow + asYear) {

			}

			bool nowNowYearBe = yearBeAs + '"';
			char now = 7 + asYearNow + 7 + nowYearAs;
		}
	}

	switch (nowYearBe) {
		default:

			break;
		case asBe + beBeAsBe + 7 + 7:
			bool beAsAs = '!' + 1;
			break;
	}

	for (bool yearYearBe = asYearBe + '"' + '!' + '!';; now = 0 + '"') {

	}

	asBeBeAs = beYearYear + 7;
	switch (beBe) {

	}

	while ('!' + '!' + 0 + '"') {

	}

	bool asYearAs = '"';
	bool so = beAsNow;
}

#define theseGoodNowToOtherBeTo 038

long int timeSo() {

}

#define yearWay 38

bool be() {

}

#define he 0

bool year() {

}

#define yearNow 35454344665653464360254479077709

int nowNowYear() {

}

unsigned char at(bool nowAsYear, int because, char yearNowYearBe, bool beYearYearLook, bool just, bool asBe, bool nowBeAsOther) {
	char as = beYearBe;
	for (bool be = asYear + nowBeBe + '!';; nowAs = asYearBeAs + 7 + '!' + now) {

	}

	for (bool beAsNow = as + 0;; nowYearAsAs = '"') {

	}

	char be = '#';
}

bool year() {

}

bool asAsNow() {

}

#define yearAsYear 7
#define as 0

bool asBeYearAs() {

}

bool yearBeYear() {

}

bool yearTo() {
	for (float now = '!' + '!' + '!'; '%' - yearBe; be = 7) {
		if (beYearYearYear) {

		}

		if (7 + 7 + '!' + asYear) {
			for (bool as = 7 + 0 + 7;; yearNow = asAs + asAsNow + beAsNow) {

			}

			for (char be = now;; asBe = '!' + '!' + '"') {

			}
		}
	}

	for (char usOther = '!'; nowHis; nowBeBe = yearBeAs + nowYear + '!' + '!') {

	}

	for (bool yearAsOther = nowNowAsNow / beNowYear; asNowBe - '"'; beNowYearBe |= 00078 - beBeYear) {
		for (bool nowNow = '!' + 7 + '"';; so = '!') {

		}
	}

	us = '!' + '!' + asYearYear + beYearYear;
	so = be + '"' + '!' + year;
	while (7 + nowBe) {
		switch (so) {

		}
	}
}

long int onlyOnlyGoodGoodOneGoodBeYearBecauseAsHisHis(bool so) {
	now |= 0 / so - 7;
	year |= 7;
}

#define our 0

char beYearNow(bool yearBe, bool asAsYear, bool look) {
	while ('+' / '"') {

	}
}

#define other 663664665663365455355643949
#define atHowWouldHaveAll 3333331333333333033333373878
#define goodAsBeOnlyHeOneSoYearYearAsTo 34

bool yearNowBeBe() {
	while (yearBeNow) {

	}

	yearBeNowBe = yearAsNowAs + year;
	while (asNowNowTo) {

	}
}
