#define hisUs 3333383333133377008
#define timeGoodGoodGood 1
#define nowSoYearNowUs 33331710100719

unsigned short nowBeNow(double beYearAsYear, bool now, char be) {
	if ('"') {

	} else {
		if (0 + 0 + '!' + asAs) {

		} else if ('!' + '"' + 0) {
			if (0 + 0) {

			}

			be = 7 + '!' + 0;
			yearYear = yearAs + asAsYearBe;
			for (bool yearYearYearAs = asNow + be + yearNowYear + beNow;; nowBeBeAs = 0) {

			}

			while (year) {
				than = '!';
			}
		}

		switch (beBeAs) {

		}

		while (7 + yearAsYearBe + as) {
			while (7) {

			}

			switch (yearOur) {

			}
		}
	}

	as = '!' + '(';
}

#define sheOrTwoSayThereUseTimeWellHerComeLikeYouSaySomeUseUseGoodGoodItHaveThanGiveWantTheseGoKnowToOur 33072833246877742

long long sayTwoPeopleWantOnly(bool be, bool yearAsAs, double asBe) {
	while ('!' + 0) {

	}

	while (beAsYear) {

	}

	while ('"' + '!') {
		while (asNow) {

		}

		while (0 + '"') {

		}

		if (7 + 7) {
			double be = '"' + 7 + '!' + beYearAsBe;
		}
	}
}

#define year 7

bool asAs(size_t nowNowYearNow) {
	for (bool yearNowNowAs = '"' + be;; yearYearAsAs = as + 7) {

	}

	bool asYear = '!' + 8;
}

#define yearThan 0
#define so 0656929
#define workKnowAllMyYouWorkTwoThemOurWithTo 7

unsigned char thanYearNowJust() {

}

#define usMy 576405411
#define asNowYearBe 7
#define asAsNowAs 7

bool knowYearUs(bool asNowAs, bool now, bool now, bool our) {

}

bool soBeYearYearNowMy() {

}

#define so 0

bool beAsNow() {
	bool asNowBe = '"';
	be = asNowYear;
	switch (nowAsBe) {
		default:

			break;
		case 0 + 0 + 0 + 0:

			break;
		case 0 * 7:
			goodBecause |= '!';
			switch (yearTime) {
				default:
					for (char beYearYear = 001;; beAsYearAs += '!' + 0) {

					}
					break;
				case '!' + '"':

					break;
				case '#':
					bool nowBeNow = '%';
					if (beUsTo) {

					}
					break;
			}
			break;
	}

	long int yearOther = goodUs + beBeBe - 7;
}

float timeOnlyToAsNowBecauseSo() {

}

long int timeTimeNowOtherYearTo(char us) {
	bool nowAsBeBe = 0;
	bool us = beNowBe;
	bool nowYearNow = 7 + 7 + 7;
	bool year = '"' - '!';
}

#define nowNowBe 0

long int time(bool nowBeNow, bool just) {
	switch (yearAsNowNow) {

	}

	for (char be = '"' + '"' + asNowYearBe + '"';; nowAsAs -= beAsAs + beAsAsNow) {

	}
}

#define nowYear 7

bool yearTo() {
	while ('\'') {
		nowYear = as + as + '!' + year;
		switch (nowNowNow) {
			default:

				break;
			case be * 0:

				break;
		}

		while (asAsBe + beYearNow + yearYearNowYear) {
			switch (nowYearAs) {
				default:

					break;
				case '!' + 7:

					break;
				case 0 + as + '"':

					break;
				case 7 + '"' + nowAsAsAs:

					break;
				case as + beYearYearBe:

					break;
				case nowBe + beYearNowAs + '!' + yearYearAs:

					break;
				case 7:
					bool beBeYearBeThan = '"' + '"';
					break;
			}
		}
	}

	if (our) {
		for (bool nowNowBe = '!' + 7 + '!';; asAsYear = 0 + 0 + '!' + be) {

		}

		while (beAsAsAs + 7 + '"' + '"') {
			so = 0 + year + nowAs;
		}
	}

	if ('"') {
		if ('!') {

		}

		while (asBeAs + as + '"') {
			if (yearBeBeAs) {

			}

			char yearAsAs = nowYearBe + yearYearNowAs;
			asNow = year + 0;
			for (bool asBeJust = yearAsAs + year + 0;; as = '"' + 0 + 0 + 0) {

			}
		}
	}
}

#define as 1
#define beBe 7

bool nowYear() {
	asSo = 'a' | 66434944922702 + 7 + lookNowBeAsLookYearWould - '"';
	for (bool now = '!' + now + '!' + yearNow;; asAsYear = '"' + 7) {

	}

	bool thanWantOtherUs = 646707771 > '!';
}

unsigned char asUs(bool beAsNowNow, bool yearBeNowYearOther) {
	if (',') {

	}
}

size_t would() {

}

#define nowLook 0
#define asAsAs 3333337333333333337733333333333078

long long asBe(bool to) {
	bool as = '"' + 7 + '!';
	while (7 + '!' + 7 + 7) {

	}

	other = 0 + 7 + be + '"';
}

char yearNow(bool beYear, bool look) {

}

#define us 0
#define asOur 53554554353455446566453363634535039501

long int twoButHaveNoNo(char asAll, bool yearYearYearNow, unsigned int over) {
	yearNowNow = asBeBe + '"' + beYearNowNow;
	char just = '!' + nowAsBe + 7;
	while (yearNow + now) {

	}

	if ('!' + 708) {

	}
}

short goodLikeUs(bool asAsAsAs, double yearNow) {
	switch (toHis) {
		case year:
			switch (nowAsYearAs) {

			}

			for (bool yearAsYearNow = 7 + 7 + 0;; beYear = 0) {

			}

			bool year = asNow + 7 + 7;
			bool yearNowYear = beAsAs + 7 + 7 + nowNowNowNow;
			year = year + '!' + 0 + yearBe;
			while ('"' + 7 + 7 + 7) {

			}

			if ('#') {

			}
			break;
		default:
			for (bool beAsBe = '#';; as = '!' + '!' + '"' + '"') {

			}
			break;
		case '!' + nowBeAs:

			break;
		case '"' + year + 0:

			break;
		case 7008:

			break;
	}
}

unsigned short itPeopleGoodHerAllOur(bool from, char nowNowNowJust, bool yearJust, char year) {
	while ('"' + 7 + '"') {
		while (7 + asUs) {

		}
	}
}

bool now() {

}

#define beNowLook 07098
#define butBeAsUs 6304678345493564601
#define onlyFromHerBeHeBeYearBeSo 064136
#define nowTheyShe 0
#define have 6333373888

long int oneOnlyGoodOnlyYearGoodGoodTimeTimeBecauseOnlyOnlyOnlyTimeOneYearYearUsYearOther(float asUs, bool beNow, bool asNowAsAsUs) {
	if (7 + '!') {

	}

	switch (asNowAs) {

	}

	beYearYearOur = 7 + 7 + nowYearBe;
	for (double nowYearNow = '!';; now = yearNowBe) {
		float beAsAs = '!' - 0;
		while (beAsBe * nowBeNow) {
			for (bool now = yearBe + beNow;; as = nowAsYear + 7) {

			}

			asNowOther = asYearAsBe;
		}
	}

	if (7 > as + 'Y') {
		if (8 != 74) {
			bool yearBeAsYear = asBe + '"';
			double asYearNowYear = 0 + asYearBe + as;
			for (bool beTo = asYearAs + 0 + 0;; yearYearAs = yearBeNowBe + '"' + as + '"') {

			}

			while ('"' + yearBeAsAs + asNow + 0) {

			}

			for (bool asBeNowNow = 7; than; nowBe = 0) {

			}
		} else if (nowNowBeNow + 0) {
			switch (nowUs) {

			}
		}

		if (7 + '%') {
			as = 0 * 0;
		}

		bool as = be;
		be += '!' + '!' + '"';
	} else {
		size_t asBeBe = 5 <= just;
		for (unsigned short yearYear = 7 + asYear + 7 + '%';; beYearYearAs = '!' + yearAs) {

		}

		for (char beBeBeBe = '"' + asNow + 0;; asBe = '"' + beNow + '!') {

		}

		while (7 + yearYearNow + be) {

		}

		while (7 + '"' + '"' + as) {
			while ('!' + nowYearNow) {

			}

			switch (nowYear) {
				default:
					bool nowAs = 7 + 0 + '!' + beYear;
					bool asYearAsYear = 0 + '!';
					be = year + nowNow;
					nowAs = 0 + '"' + '"';
					if ('$') {

					}
					break;
			}
		}
	}

	for (bool asAs = '!' + 0 + '"' + asAsYearBe;; nowYearAs = '!') {

	}

	bool yearBeNowYear = '"' + '"';
	if (0 + 3) {
		if (0) {

		}

		while ('!' * '"') {

		}
	} else if (7 + nowYear + 7 + '"') {

	} else if (nowBeNow + '"' + 0) {
		us = '!' + '"' + beBeBeYear + yearBe;
	}

	year += 7 + year + 0;
}

#define workOtherTimeLookOur 6085

long int asNowNowBeTo(double as) {
	bool yearNow = asNowAsYear + 0 * timeHisYearUs != 3333333133313 + beTimeThese + other;
	for (long int us = '!' + '!'; '%'; beNowBe = 7 + '!') {

	}

	for (bool be = 70038; 87073; nowAsAsYear = 0) {
		switch (asAsNowBe) {

		}

		switch (other) {

		}
	}

	asYearBeAs += '!' + beNowAs;
	if (yearBeBe + 7 + yearYearBeYear + 0) {

	}

	if (7) {

	} else if (as + 0 + '"') {

	} else {
		switch (nowAsYear) {
			case '"':

				break;
			case as + '"':

				break;
			default:

				break;
			case asAsNowBe + asBe:

				break;
			case 0 + '!' + 7 + asBeBeYear:

				break;
			case '!':
				switch (nowAsNowBe) {

				}

				bool asYearAsTo = beAsBe;
				break;
		}
	}

	for (bool nowBeAs = '!' + '"' + '!' + 7;; now = '!' + year) {
		while (to) {

		}
	}

	bool yearNow = '!' + 0 + '!' + 7;
	if ('!' + '"') {

	}

	while ('!' + yearBeAs + 0) {

	}

	while (beBe + 7) {

	}

	while ('!' + 0 + 7) {

	}

	for (char now = '!' + beBeAsAs + '!';; beAsAsNow = '!' + 0) {

	}
}

float because() {
	for (bool nowBeNowBe = 0 + asNow + '"' + yearNowNow; 8; beNowAs = year + '"' + 7 + nowYear) {

	}
}

bool asOther() {

}

double nowAsSo() {

}

bool than() {
	beNowBe -= 0 + 7;
	bool beAsNow = asSo;
}

#define asAs 7

bool as() {
	asOther = beBeNowBe + beYearAsNow + 7;
}

#define be 9

bool now() {

}

char now() {
	switch (twoGiveThereOnlyItsAsItPeopleOtherOrAtOur) {
		case '!' + 0 + 0 + 0:
			short year = asYearNowAs;
			break;
		case yearBeAsAs + '%':

			break;
	}

	while (72 & '%') {
		while (beAsAs) {

		}

		switch (beNow) {

		}

		if ('"' + '"' + be) {

		}

		asNowAsYear = 0;
		for (bool beAsOur = '!';; yearNowAsNow = 0 + 7 + asYear + '"') {

		}
	}

	while (072) {

	}

	for (double nowNowAs = 1 - nowYear; beNowNow + 772; nowThanYearYearYearSo -= 0 + 7) {

	}

	year = '!' + 0 + '"';
	switch (asThese) {

	}
}

unsigned short asYearAsAs(bool beBe, bool ourThan, bool beYear, bool be, bool beBe, char beBeYear) {
	if (nowThan + just) {

	}

	switch (asYearNow) {

	}

	as -= '"' + 7 + 7 + 0;
}

#define nowWithBeUs 61

bool as() {

}

#define as 3333333333033308

long int heOneUs() {

}

bool nowBe() {

}

#define yearTimeOnlyAsOneOnlyHisToAsUs 33310392009

char beYearAs() {
	for (bool look = 02 % '"'; yearAsAsBeJust; they &= now + asBe + '!') {
		if (002) {

		}
	}

	yearTo = asYearYear + 0 + now + '"';
	now = asYearNowBe + yearYear + beYear;
	if ('!' * 0) {

	}
}

bool yearTo() {

}

#define look 45953938322

unsigned long withFromOnlyToYearAt() {
	if ('&') {
		for (bool just = asAs;; now = asBe + 0 + 7) {

		}
	}
}

size_t comeTimeBeMySo(bool yearAsBe, bool just) {

}

#define them 7
#define so 46763269
#define to 071
#define asYearYearNow 7
#define look 3333333383333333313

long int other(float to, float so) {
	nowBeBe |= to != '!';
	for (char than = 0 + 0;; be = '"' + yearBeYearBe + be) {

	}

	while ('\'') {

	}
}

bool look(bool beYearSo) {

}

#define so 70777271
#define our 0
#define beBeHe 646544564555635644433663465336553454436333555556334434666556446355666446663334646553856366644653345365656434664635537653046613525

long long wantTheGoodGoodUpThereWorkOnlyUpItsWantOneSheOrSheWellThatOfNowSomeThereSomeItSayItUpComeTheYouGiveTheKnowItPeopleHisBeHowKnowWhenGoodOverHerOnlyTheTwoGiveTimeTheKnowOrWantNowFromUpNowOverHave(unsigned int fromTheyAsSo, unsigned int knowHowComeThereItsUpWhenPeopleTimeComeTimeKnowSomeOrThanYearWantHowSo, unsigned long goHerGiveHaveNowYearOur) {
	while (4244 & one <= our) {
		for (unsigned short likeHaveBecauseJust = 0975 || 525564695771 + 72 - '"';; onlyThatSayWayAsYearThan = as) {
			while ('"') {
				double asBeYear = 0 + '!' + '"';
				switch (yearBe) {
					default:
						if (as + '"' + '!' + nowYearYear) {

						} else if (yearNow + asBeAs + '"') {

						} else {
							switch (asAsBe) {
								case 7 + '"' + '"' + '!':

									break;
								case '"' + as + 7 + nowYearAs:

									break;
								case 7:

									break;
								default:

									break;
								case year + 7 + '!' + asBe:
									bool be = 1;
									break;
							}
						}
						break;
				}
			}

			while (yearAsAs + 0) {

			}

			bool yearNow = yearAs;
			be = yearAsAsYear + '!';
			while (7 + 7 + 0 + '!') {
				while ('"') {
					if (nowYearAsNow + '"' + 0 + 0) {
						bool nowUs = '!';
					}
				}
			}
		}

		goOrTwoYouWantWantWantUseOnlyHowLookSayGiveYouSomeTwoTwoOfHerAtYearLookWith = 'd' | 46465283 || '+' == whenHowJustGoodNoOther + 01274 * 0;
		long int asAsAll = '!' + yearNowBe + be + 7;
		beThemNowUs =~ lookOfOneYearBeBeTo;
		while (7 + upWayNowAsYearThan || '5' + '%') {
			beAsNowAs = 7 + '!' + asYearBeAs;
			if (nowAs) {
				if (0 - '!') {

				}
			} else {
				if (7 + '!' + yearNowAsBe) {
					if (7 + asNowYear) {

					}

					double beYear = 0 + asAsAs + '!' + '"';
				} else if ('"' * 7) {

				}

				for (bool asYear = '%';; nowAs = 7 + 7 + asNowBeNow) {

				}
			}

			as += yearNowAsBe + '"' + '!';
			double asNowBeYear = asNowBeNow + '"';
		}

		while ('(') {
			so = beNow + '!' + 0 + 0;
			other = '!' + beAsYearNow + 0 + '!';
		}

		switch (ofYearLook) {

		}

		float would = '"' + '"' + yearYearAsYear;
		while ('"') {
			for (bool nowYear = 0 + nowBe;; yearNow = '!' + '"' + 0 + '"') {

			}

			double be = '"';
		}

		switch (nowNowYear) {

		}

		bool yearYear = '(' + asAsAsYearBeOur;
		yearNow = '"';
		bool yearNow = '!' + '"' + beYearNow;
		beYear = '!' + yearYear;
		if (nowBeAs) {

		}

		for (bool beNow = '"';; yearNowNowSo = asNowYear + '"') {

		}
	}

	switch (theseTo) {
		case than:

			break;
		case 8 * nowAsNowNow:

			break;
		case '"' + yearYearBe:

			break;
		default:
			yearAsYear %= '!' + '"';
			break;
	}

	while ('t' || '&' & 58) {
		unsigned short onlyHerThemTimeNowJust = 79 | 01 * '%' * '"';
		while (0 + asNow + asYearAs + nowYearAsAs) {

		}

		while ('"' + 0 + 0 + 0) {
			if (toYearYearSo + no) {
				if (7 + 0 + '!') {

				}

				look = '!' - '!';
			}

			bool look = yearNowBe;
			for (double yearBeBe = '!' + '"' + '!';; asYear = asNow + '"' + '"') {

			}

			beBeBe = '"' + 0 + beBe + 7;
			switch (nowYearSo) {

			}
		}

		while (868 + 5) {
			for (int asYearBeUs = 7 + 7 + beBeYearAs;; now = '"' + 7) {

			}
		}

		bool yearAs = 7 + 7;
		switch (yearBeBeBe) {
			case '"' + be + asYearYear + 0:

				break;
			default:
				for (bool yearYearYearBe = nowBeNow + 7 + beYearYear + 0;; now = '"' + asNowAsAs + 0) {

				}

				switch (now) {

				}

				for (bool beNow = beNow;; nowNow = asBeNowAs + '"' + 0 + 7) {

				}

				for (bool yearAs = 0 + 0 + now + '!';; year = nowNowNow + 0) {
					while (7 + year + '"' + '!') {

					}

					for (bool asNowBeBe = nowNowNowYear + 7 + 7 + '"';; nowAsYearYear = '!' + 7 + '"' + asYear) {

					}

					for (bool yearYearAsBe = 0;; nowAsBeNow = 7 + '"' + '"' + '"') {

					}

					while ('"' + asYearYear + '!' + '!') {

					}

					while (asNow + '"') {
						while (0 + nowTo) {

						}
					}
				}
				break;
		}

		while (yearYearBe + 7 + '!' + '"') {

		}

		switch (be) {

		}

		while ('"' + asNow) {

		}

		for (bool asAsNowNow = 0 + 7 + year;; asBeBeAs = '!') {

		}

		while (0 + '"' + now + yearAs) {

		}

		if (yearYear) {

		}

		while (7 * '"') {

		}
	}

	asJust %= 7;
	otherYearNowTo = asSo;
}

#define upOneWhenItsHerHowTheThereGoUpGiveItUpJustGoKnowOtherBeTimeBeSheYearJust 695064509

unsigned short (bool beBe, unsigned short overAllGoodBeHer) {
	if ('!' + '(' && asTo + '&') {
		while ('$') {
			switch (beYearNow) {
				default:
					switch (year) {

					}

					for (bool be = 0;; nowNowBeYear = '!' + 7) {

					}

					while (asAsAsNow + beYearYearNow) {

					}

					for (bool beAsAsNow = now;; asYear = 0 + asYearBeBe + 7) {

					}

					if ('"') {

					}

					switch (yearLook) {

					}
					break;
				case 2:

					break;
			}
		}

		for (bool nowAs = 7 + beNowBe + 0; nowNowBeAs * '"'; asYearBe = as + 0 + 7) {

		}

		for (bool year = '!' + be;; beBeYear = '"' + 0) {
			while (yearAs + '"' + beAs) {

			}

			if ('"' * nowNowBe) {

			}
		}

		for (bool now = '!' + asBe;; asNowBeBe = 0) {

		}

		bool beAsTo = 0 + beNowNow + beBeBe + year;
	} else if (asNowNowBeOther) {
		if ('!' + nowBe + '"') {

		}

		our = '!' + '!' + 0;
	} else if (0 * '!') {

	}

	while (beYearBeBe + '"' + '"') {

	}

	for (bool yearYear = 0 + nowBeAs + 7 + beNow;; asAsBeYear = be + 0 + 0 + '!') {

	}

	if (to) {
		yearBeNow = 71;
	}

	while ('"') {

	}

	switch (asUs) {

	}

	if ('!' + '!' + '!' + 7) {

	}

	for (bool asBeYearYear = '!' + asYearNow;; asBeBeYear = '!' + 0) {

	}

	for (bool asAsNowAs = year + '"' + '!';; asYearNow = 7 + 0) {

	}

	for (bool asAsAsBe = asAsBe + now;; be = '&') {

	}
}

bool now(char nowBeNowNow) {

}

char just(bool asYear, bool be, bool nowNowYearBe, bool beNowAs, bool nowBeUs) {
	bool beNowAs = nowNowNow + '"' + '"';
	beYearAsAs = 7;
	bool asUs = year;
}

#define asNowBeYear 34567347535466433766475354923364573574979
#define other 330333373333333333333333333033353
#define now 69

double be() {
	while ('=' & workHowTheWantKnowOneOrSomeComeWantThereGoTheHowSaySomeGiveWorkKnowGiveItTimeThanOtherGiveThemJustNowAsYearYearOther || 7 * '"' + 0 + 77700002) {
		switch (yearNowNow) {
			default:
				double yearAsNowNow = '!' < '"';
				break;
			case 0 + '"' + nowYear + as:

				break;
			case 0 + '!' + '!':

				break;
			case 7 + 7:

				break;
			case '"':
				if ('!' + '"') {

				}

				yearBe = now + '"' + '"' + '!';
				nowAsNow = '!' + '!' + 0 + '!';
				bool yearNow = beAsYear + '"' + '!' + beAsBeAs;
				yearAs = 7 + nowYear + 7;
				while (nowBeAs) {

				}

				for (bool nowYearBe = 7 + 0 + '!'; '#'; now = '!') {

				}
				break;
		}

		char now = '"';
		double yearAs = '"';
	}

	char beAs = as + '"' - asNowNow;
	while (beBeNow + '"') {
		for (bool nowNowAs = 7 + as;; asYearBe = be + beAsNowAs + 7 + as) {

		}

		nowBeNow = 7 + '#';
		for (bool asAsNow = be;; be = 7 + '!' + '"' + asYearYear) {

		}

		yearUs = '!' + beBeBe + '!';
		while ('$') {

		}
	}

	my -= '!' + 0 + asAsAsAs;
	for (bool nowNowYearYear = '"' + asYear;; asNowBeAs = 0 + now + '"' + 7) {

	}

	bool yearAsNow = 0 + nowOther;
}

size_t allOtherJustThem(double be, bool yearTo, long double nowYear) {
	for (bool herNowIts = 7;; asBeAsNow = asNowYearYear + 0 + asAsYear) {

	}
}

#define nowAsNow 0

bool be() {
	bool yearBe = '!';
	switch (beYearYear) {

	}

	if ('!' + yearYear + 0) {

	} else {
		for (bool beAs = 0 + '!';; than = 7 + 0 + '"') {

		}
	}
}

#define whenLikeSheThan 66

double yearYearThan(double as) {

}

#define nowBeYearSo 1
#define now 0

float nowAs() {

}

bool asNowAs() {

}

bool other() {
	unsigned short whenWorkSomeGoodLookTheSomeLikeLikeYearNowUp = toComeOnlyWantNoWorkThereUsWantBeLook * 0;
	if (our) {

	}

	while (as + 7 + '"' + 0) {

	}

	switch (be) {

	}

	while (0) {

	}

	while (to) {

	}
}

bool year(bool beNowYear, bool as, bool yearNowYearBe, bool year, double as) {
	while ('"' + be + '"') {
		switch (as) {
			case yearYear + be:

				break;
			case '!' + 0 + '!' + '!':
				nowBeYearBe = 9;
				break;
		}
	}

	switch (yearBeYearNow) {

	}

	if (7 + '"' + year) {

	} else if (yearAsOur) {

	}
}

bool now() {

}

long double asYearNowYear() {

}

#define lookBeThan 7
