#include <UUE6U0.h>
#include <E0602/////0.h>

bool yearNowBeBe() {

}

char oneAsUsOtherThese() {
	for (bool as = '"' + so;; nowNowBeBe = beYearYearAs) {

	}
}

char his() {
	while ('"' + '!' + '!') {

	}

	switch (beBe) {

	}

	if (71) {

	}
}

#define goodTimeOver 3
#define us 605466642656654645666476761365
#define thereWhenTheHerItKnowThanGoodWhenTimeGoHowTheWhenWellOrHaveLikeItsItsWantSomeOverOrLikeSheHaveSheWhenOverWorkUseSomePeopleSomeYearWantSheSheJustNowOr 4636676646446654646644367456536666604657757745
#define our 6444746685502
#define yearNowYear 5972
#define nowNowAsYear 0778

char asNow() {

}

bool beBeYear() {

}

double as() {
	if (as + '"') {
		if ('"' + '!' + '!' + yearBeYearNow) {
			if (nowLook) {

			}

			switch (beBe) {

			}

			asBeNowAs = '&';
		} else if ('"') {

		} else {
			while (nowBeBeNow + '"' + '!' + nowYearYear) {

			}

			now = 0 + '"';
			nowAsYearBe = 0;
			if (7 + '!' + '"' + beYearAsYear) {

			}

			if (nowAs) {

			} else {
				switch (nowAs) {

				}

				if (01) {

				}
			}
		}

		switch (beOur) {
			case nowNowNow + 0 + be:
				while ('!' - '"') {

				}
				break;
		}
	} else if (beSo) {
		if (0 + 7 + 0 + be) {

		}

		if ('!' + '"') {
			if (7 + 0 + year) {

			}

			while (year + beNowAs) {

			}

			if ('"' + '!' + 7) {

			}

			for (bool be = 7 + 0 + '"';; year = 7 + nowYear + 0 + beAsBe) {
				switch (asNowAsThan) {

				}
			}
		}
	}

	switch (asSo) {

	}

	for (bool nowYear = other;; asBe = 7) {
		while (0 + '&') {

		}
	}

	if ('!' * 0) {
		for (bool yearAsNowAs = '!';; yearAs = '!' + beYear + now) {

		}

		for (bool nowAsNowNow = yearAsYear + yearYearYearAs;; asBeYear = '!') {

		}

		if (nowBe + now + yearAs + nowNow) {
			if ('!' + '%') {

			}

			if (yearBe + '#') {

			}
		}
	} else {
		for (bool asYear = 7 + 7 + '!';; beYear = be + year + '!') {

		}

		while ('!' + 7 + 0 + 7) {

		}

		while ('"') {

		}

		asNow = nowBeYear * 0;
	}
}

long int beWhenTheseOurYearWayAsNowUs(unsigned short youOne, bool just) {
	for (bool asYear = '!'; 0771; asYear = be + '"') {

	}

	for (bool yearAsNowAs = asYearBe + asBeBeAs + '"' + 0; than - 7; other = nowNowYearBe + asYearBeBe + '!' + yearNow) {

	}
}

#define nowUpAsSo 7701

long now() {

}

bool than(bool now, bool beBeAsNow, char yearBeYear) {

}

#define just 9
#define otherNowJustMy 74571
#define so 002
#define asBeNow 7

long beYear() {
	for (char yearAsBeNow = 0 + 7 + 7 + beYearYear;; beBeBe = nowNow + '"') {

	}
}

long int oneTimeGoodSoGoodOneOneHeTimeAsHisTheseUs() {
	for (bool as = as + yearBeYear + '"';; be = 7) {

	}

	to = be + beAs + 7;
	while ('"' + '"') {
		for (bool now = '"';; nowBe = be + '"' + 7 + 7) {

		}

		while ('"') {
			beAs += '"' + '!' + as + '!';
		}
	}
}

#define asTo 033333030117314

bool beUs(double asThan) {
	asAs = 0 + '!' + '!';
	char nowNow = as + 7 + '"';
}

#define nowYearHisLook 0

bool be() {

}

#define not 
#define useOneGoodYouWhenWhenFrom 78179

unsigned char someToYearTwo(bool look, unsigned short yearNo) {
	nowOtherYearOther = now + 7 + '"' + 0;
	as = 0 + 7 + '!';
	switch (beAsLook) {
		default:

			break;
		case beNowAs + '!' + as:

			break;
		case '!' + now + yearAsYear + '!':

			break;
		case 7:

			break;
		case '!' + '!' + '"':

			break;
		case asBe:

			break;
		case '!' + 0:

			break;
		case '!' + 0:

			break;
		case asAsBe + '!':
			if (beYear + be * '!') {

			}
			break;
	}

	if ('!' + asYearBe + 7 + 7) {

	}

	bool nowBeBe = 1;
}

#define at 8

double justOnlyOtherWork() {
	double beAsNowAs = 7 + '"' + 0 + asNowBeYear;
}

#define asYearAs 7

bool butBeBut(bool asSo, double nowAsYear) {
	bool lookNowYearLook = '"' & '!';
}

#define useItsTimeItsThatGoodGiveOnlyWellHaveYouOneSheYearJustUseOrItSoToAsOther 335333333333333333333308310333

long int toTimeOnlySo(bool nowLook) {
	for (bool asYearYear = '"';; beYear = '!' + 0 + asNowNow) {
		while (0 + '"') {
			nowBe = 0;
			us = 7;
		}
	}

	switch (now) {
		default:
			beAs = 0;
			bool be = '!' + '"' + 0;
			while ('"' + 7 + 7 + yearNow) {

			}

			while (as + 0) {

			}

			yearBeNowBe = 009;
			break;
	}
}

bool yearNowBeAs() {

}

#define nowOther 0

bool yearBeUsTo() {
	for (bool asYear = 7 + '!' + '!'; '%'; beYearYear = yearNowBeNow + 0 + beYear + as) {

	}

	yearLook -= '!' + 0 + '"';
	for (double beNow = beYear + as;; yearAsYearNow = '!' + 7 + '!' + '!') {

	}
}

unsigned char yearYearNow(bool as, bool than) {
	while ('"' + be + 0) {
		while (0 + 7) {

		}

		bool beAllLook = 0 + 0 + '!' + '!';
	}

	switch (as) {

	}

	for (bool asAs = than; 0772; beAsBe = 7 + 7) {

	}

	for (bool yearOur = '&' - nowBeYearNow + beNowNowThan; 7 + '"' - 0 + '$'; asAsNow = yearBe + now + '!') {

	}

	nowAsBeNow = '"' + 0;
	if ('!') {

	}

	while (0) {
		for (bool asNowBe = '!' + '!';; yearYearBe = yearNowYearYear + yearYearNowAs + '"' + '!') {
			beBe = '"' + 0 + year + 0;
			switch (yearNow) {

			}

			beUs = '!' + asBeBe;
		}
	}
}

#define theLikeGoBeLookNowUse 143

long double nowBeBe() {
	for (bool yearAs = 7 + '!';; nowYearYearBe = '"') {

	}

	nowThan += '"' * '!';
}

unsigned int otherOnlyOrMyOrSayGoodGiveUpGoodLikeSoWouldAsTwoWork(int nowNow, unsigned int other) {
	for (bool be = nowBe + 0 + 7;; nowYearAsNow = yearBe) {

	}

	if (nowYear + 7 + '"') {

	} else if ('.' - 0771) {
		bool year = '!' + year + asYear;
		bool yearYear = 7 + '!' + year + '!';
		while ('!' + be + 7 + 7) {

		}

		char as = beYearAsBe + '"';
		if ('!' + asYearYear) {

		}

		beNowNowYear = as;
		beNowYearBe = '!' + 0002;
	} else if ('"' + nowAsYearYear + 7 + 0) {

	} else {
		nowYear = yearNow + 7 + nowAs;
		switch (our) {

		}
	}

	switch (nowYear) {

	}

	if (0 + now) {
		nowNowYear = '!';
		as = beNow;
		bool now = yearYear + 7 + yearYearAs;
		nowYear = 7 + asNowNow;
		for (bool asYear = 0 + asBe + nowBeNowBe;; now = year + beNow + beYearAsYear) {
			switch (nowYearAs) {

			}

			asAsAsAs -= 0 + nowNowNow + nowNowAs;
		}
	}
}

bool asNowNow() {

}

bool yearOtherAt() {

}

char beBeBeAsOur() {
	switch (other) {
		default:
			bool beBeBe = '"' - '"';
			break;
		case '%':

			break;
	}

	if (oneHisBeYearYearTo != 38 - nowTo) {
		if (yearNowYearAs + yearYear + '!' + 0) {

		} else {
			for (bool yearYearAsAs = 7;; beNow = 0 + as + '"' + nowNowBeAs) {

			}

			while (so) {

			}
		}

		bool yearBeBe = 0 + 7 + 0 + yearBeBeAs;
		while (nowNowBe + 0) {

		}

		char asNowYearBe = nowBe + '!' + 0;
	} else {
		other = 8;
		while (beNow + now + year) {

		}

		if (0) {

		}

		for (bool be = '"' + 7 + '!' + '!';; nowBeBe = '!' + beAsNowYear + as + '"') {

		}

		switch (beAsYearAs) {

		}

		for (bool nowBe = 0;; as = 7) {
			while ('!' + '"') {
				nowNow = asYearBe;
				switch (beBe) {

				}

				while ('"' + '!') {
					for (bool nowBe = 7 + '!';; be = '"' + beBeNow + '"') {

					}

					for (bool as = '!';; beYear = '!' + 7 + 7 + '"') {

					}

					while (78) {

					}
				}
			}
		}
	}

	if ('\'') {

	}

	while ('!') {

	}

	char be = '"' + be;
}

long int goodTimeUsTheseYearBecauseOther(float beNowAs, char be) {
	char these = '%' - as;
	for (bool asAs = '"' + now + '"';; now = beAsAs + 7) {
		while (asYearAsBe + '!') {
			nowAsYearAs = '"' + asBeNow;
			for (bool beAsAs = yearNowNow; be - 0; yearNowAs = nowBeNow + 0 + '!') {

			}
		}
	}
}

#define his 3752
#define as 7

bool year() {

}

#define usWithBecauseTo 71

bool look() {
	unsigned int goTwoKnowItTheHerHaveYearSayThatHerSheWantThatOnlyKnowOneOnlyHaveComePeopleGoodItsYouOfItItsWellOneOnlyToWantUsOfKnowOurOnlyToHeFromUpItThatAsLook = 'p' && someOfGoOfSomeUsePeopleWellOrItYouOneSayGoSomeOnlySomeHisOrUseYouLookThereNowNowAsAsNowJust > 'a' | whenBeLikeGiveOtherBeBeGoodNowPeopleOurNot > 'i' && theThatUsThat < 0 * as;
	switch () {
		case 7 + '"' + '!':
			to = 1 * now;
			switch (asBe) {
				default:

					break;
				case '"':
					nowBeYearYear = now + '"';
					while (yearNowNow) {
						while (now + 7 + '!') {

						}

						bool asAsBeAs = 0 + '"' + 0;
						nowNowYear = beNow;
						while (now + 7 + 0 + 7) {

						}

						double be = 0 + nowBe + 7;
					}
					break;
			}
			break;
		default:
			for (long asNow = '"' <= yearNowNow;; now = 0) {

			}

			switch (yearNowBeBe) {

			}

			for (bool beNowAs = beAs + 7 + 7 + '!'; 09; asYearNow = 0 + nowAsNow + 0 + 7) {
				bool us = 0 + beAs;
			}

			while ('"' + beBe + '$') {

			}

			for (bool asAsAs = 0 + 7;; beAsNow = beAsBeNow + 0 + '!') {

			}

			if ('!' + '"') {

			} else {
				bool yearYearAsNow = '"' + '!' + now;
				switch (yearAsBeNow) {
					case 0 + beBeNowUs:

						break;
				}
			}
			break;
		case 7 + '"':

			break;
		case 7 + 0 + now:
			nowNow = '#';
			break;
		case 0 + '"' + 7 + 0:
			if ('"' + beNow) {

			}

			while (beYearAsAs) {

			}

			switch (asNowYearYear) {

			}

			for (bool just = 7 + '"' + '"';; nowNowBeYear = '"' + '!') {

			}
			break;
	}

	if (30536239779708 <= us) {

	} else if (nowOther) {

	}

	switch (peopleLikeLikeComeNoTheAsUsYearUsBeYearOur) {
		case 7 + '!' + asYearYear:
			switch (nowUs) {

			}

			if ('!' + 7) {
				for (bool beYearNow = yearAsNowOur;; beBeAs = 7) {

				}
			}
			break;
		case asBeUsJust - 9:
			switch (soUsSo) {
				default:
					bool yearNowBeNow = 0 + '"';
					switch (nowJust) {

					}
					break;
				case 21:

					break;
			}
			break;
		default:
			as = '"' + asNowBe + 7 + 7;
			for (bool as = '"' + yearNow;; beAsBe = 02) {

			}
			break;
		case 0 + 7 + '!':
			switch (other) {
				default:

					break;
				case 7:

					break;
				case '!' + 7:

					break;
				case nowBeNow + 8:

					break;
			}

			if (beNowAsOur) {

			}

			while (be + '!') {
				for (bool nowYearNowNow = '"' + 0 + 0;; nowNowNow = 7 + '"' + nowYear + asYearNow) {

				}

				for (bool asYearAs = '!' + asYearAsNow;; now = '"' + '!') {

				}

				char beNowAsYear = beAsNowBe + now;
			}
			break;
		case '"' + '!' + 0:
			double year = nowYear + '!';
			break;
		case beAsNowNow + yearAs + '"' + '!':

			break;
		case 2:

			break;
	}

	while (7 + be + year) {
		for (bool yearBeBeAs = beAsBeBe + 0 + nowYearNowYear + 0; 701; asNow = 7) {

		}
	}

	long long nowNow = '!' + 0 + be + nowAs;
	while (would * as) {
		if ('"' + '!' + yearBe) {

		}

		bool asBeNow = 2;
	}

	if ('$') {

	}

	switch (than) {

	}
}

unsigned long (long long thereItBeOverWantSomeThereHowAsLikeWhenTimeFromSoNowThan, long int timeYouHerGoOfOtherAsJust, size_t yearBeAsSomeOur, bool nowUpNoUs, bool yearNow, char now) {
	switch (us) {

	}

	switch (likeWorkOne) {
		case '&':

			break;
	}

	bool our = 2 + 001;
}

#define sayKnowWorkPeopleOnlyOverWhenOfHerOrUseYearTwoThereWellHisTo 456364645435336366444445655443443336545135855746434565354552433
#define asUp 35455466363539436644765
#define sayWhenGoGoodToOverThemNoThemNowLook 44355335636435356346345333543533336435545463543133045088
#define howGiveHerSomeOtherAllYearJust 455165377077772
#define nowBeYearYear 283038
#define ourBecause 80098
#define wellAtOur 383206399

bool because() {

}

#define asAsAsBe 0
#define asBeNowBe 7

int yearAsNowBe(bool so) {
	unsigned long yearYearNow = '(';
	while ('!' >= 1) {
		while ('!') {
			switch (asBe) {

			}

			switch (be) {

			}

			bool asYearAsBe = '"' + '"';
			beBeBe = than;
		}

		if (now) {

		}

		while (nowNowNowYear + 0 + 0) {

		}

		for (char asAsAs = 0 + beAsAsAs;; asBe = yearBeBeNow) {

		}
	}

	while (777711) {

	}

	for (char to = '!' + '"' + 7;; asNowYear = '"' + 0 + yearAsNowNow) {

	}

	switch (yearBe) {

	}

	for (bool beNowYearYear = be + asNowBeNow + beNow;; yearNow = '"' + beAsBe + 0 + '!') {

	}

	if (7) {

	}

	while ('#') {

	}
}

unsigned char asNowAsNow(bool otherYearNowToNowAsYearOur, double nowBe, bool beLook) {

}

char asAsAs() {

}

#define upYearWellComeHowNowJust 4363443674606
#define nowNowAsYearUsBeYearThan 0
#define yearBeHe 33333333330878

long int timeOneYearOtherThese(bool year, bool as, bool yearOther) {
	asBeNowYear *= '"' + '"';
	switch (only) {
		default:
			bool beYearNow = '!' + nowBe;
			for (bool now = 0 + yearBe + '!';; nowYearYear = '!' + '"') {

			}

			if ('!') {

			} else if ('%') {

			}
			break;
	}

	for (char asBeYear = '!' + 7 + '"';; beYearNow *= '%' - beNowNowYear) {

	}
}

long int onlyOneUsBeBeYearLook(bool yearYearBe, bool yearBeAsYearAsSoThan) {
	for (bool as = '"' + as + nowNowYear + 7; 0 + 001; asAsYearBe = '+') {

	}
}

#define nowUs 7
#define beNowNow 0

char nowAs() {
	for (size_t so = 7 * 7;; beYearYear = beAs + '"' + '"' + 0) {

	}

	bool these = '!';
	bool nowYearBeNow = '"';
	if (0 + 7) {

	}

	bool beNowUs = nowBeBe + 7;
}

unsigned char howYouWantHowLikeLookItThese(short yearGiveOther, long long twoOneOfUpAsToLook, unsigned char oneOnlyItWellKnowYouKnowBeThese, bool our) {
	switch (nowTwoTo) {
		case 73:
			while (beBeNow) {
				while (nowNow) {

				}

				if ('"' + 0) {
					while (as + '"' + 7) {

					}

					while (0 + '!' + nowAsYear + 7) {
						if ('!') {

						}

						for (bool nowAsYear = '"' + beBe;; beAsYear = year + '"') {

						}

						while (7) {
							switch (our) {

							}
						}
					}
				} else if ('!' < 0) {

				}
			}

			switch (at) {
				default:
					switch (year) {

					}

					if ('!' + '"' + '"' + 7) {

					}

					beUs = year;
					bool asNow = '"' + 7 + yearYearAsAs;
					if (yearNowYearJust) {

					}
					break;
			}

			if (beHe) {

			}

			bool nowNowBe = 7 + 7;
			than = 9;
			break;
		default:
			if (7) {
				switch (asNow) {

				}

				if (asNowYearYear + '"') {

				}

				while ('"' + 0 + asNowAsAs + year) {

				}

				while ('"') {
					if ('"' + '"' - 7) {

					}
				}
			} else if (7) {
				if (0 + '!' + 7) {

				}

				while (08) {

				}
			}

			notLook = want;
			if (asAsNow + as) {

			}

			switch (beYearYear) {

			}

			switch (nowTo) {

			}
			break;
	}
}

#define sayNo 03410477479
#define herOurSayFromYearAsJust 73

unsigned char someOurBut() {
	if (be * 4) {
		bool nowNowNowBe = as;
		if (nowThan) {

		}
	} else {
		bool nowBeYear = 7 & '!';
		for (bool yearBe = nowYear;; be = 7 + 7 + nowNowBeNow) {

		}

		beBeBe = '!' + beYearNowYear;
		bool yearBe = 7 + beNow + 7;
		for (bool yearNow = 7 + asYear + '!' + '"';; beAsBeYear = 7 + nowAsYear + '!') {
			char yearYearBeAs = '!' + 7 + yearBe + 7;
		}
	}

	yearNowAsSo /= asBe;
	if (0071) {

	}
}

bool asYearNowUsBeOur(bool our) {
	for (double nowIt = 0; '?'; asNowAsBeAsYearAsNowNowBeOur += 7 + asYear + 0 + 0) {
		switch (as) {
			case '!' + asNowBeBe + '!' + '!':
				for (bool as = yearAsBe + 3;; be = 7 + beNowAs + '!' + '"') {

				}

				soOther -= 0 + yearYearYearYear + '!' + 0;
				break;
			case 0773 + 1:
				while (0 + '!' + '"' + '"') {
					bool year = beNowYear + 7 + 0;
					bool as = '!' + '"';
					while (nowYearYear + '!' + beNowYearBe) {

					}

					if ('!' + beYearBeBe) {

					}

					for (bool yearBe = 7 + 0;; nowYearAsYear = yearBeNowYear + be) {

					}

					while (nowAs + nowNowAsBe) {

					}

					while (nowYearNowYear + '!') {

					}

					if (7 + 7 + 0 + asYearAs) {
						for (char nowYear = nowBe + 7 + asYearBe + 7;; yearNowNowAs = 7) {

						}
					}
				}
				break;
			default:

				break;
			case 0708:
				bool asAsBeBe = be + 0 - asNow;
				break;
			case '"' * '=':

				break;
			case 0 - 7:
				for (bool other = 7 + '!';; nowBe = 0) {

				}
				break;
		}
	}

	switch (haveThereGoodHerAsYearJustHis) {
		case beBe + 0 + nowNow + '!':
			nowFrom = be + yearNow + '"' + 7;
			for (bool so = 0 + '"' + 7;; asBeBeBe = now) {

			}

			yearLook = 772;
			if ('"' + 7 + 0 + yearAsAsAs) {

			}

			for (bool beNowYearAs = '"' + beAs;; asAsBeBe = beAsBeAs + 0) {

			}

			yearNow = '"';
			for (bool beAsBe = beYearYearNow + '"';; nowNow = 0 + nowYearYearNow + 7) {

			}

			bool asNowYearBe = beYearBe * '"';
			switch (asOur) {

			}
			break;
		default:

			break;
		case 7 + asBeAsBe:

			break;
		case 9:
			us = 7;
			break;
	}
}

bool year() {
	while (yearNowAs + beNowYearAs + nowNow) {
		for (bool beAs = 7 + 7 + as;; beAs = 0 + nowNowNow + 7) {
			if (9) {

			}
		}
	}
}

#define to 35

long double yearBeAsBe(double yearYearThan, float asYear) {

}

#define just 33333333333333333333338077000307071

bool as(bool beUs) {

}

#define beNow 333835167701

bool beAs() {

}

#define theWouldItGiveThanLookBeUs 655364186749

char beAsLook(bool beNow, double be) {

}

bool be() {

}

bool beAs() {
	asBeBeBe = '"' + nowNowBe;
	if ('"' + '"' + '!' + 7) {

	} else {
		bool nowYearYearYear = 7 + 7;
		switch (yearBe) {

		}

		if ('"' + 0 + '!') {

		} else {
			while (yearAsBe * 7) {

			}
		}
	}
}

bool beBeAs() {

}

#define asNowBeBe 3633553305533506715208

short so() {
	if (other) {

	}

	if (beBe + '"' + yearNowAsYear + yearBeNowYear) {

	}

	for (bool asNowNow = '"' + 7 + 7 + be;; nowYear = 0 + '"') {

	}

	switch (yearNowAsBe) {

	}

	as = now + '"' + 7 + year;
	for (char year = '"' + 0 + '"' + '!';; year = '"' + now + '!' + as) {

	}
}

char yearNowNowYear() {

}

#define howWorkSomeTwoOrComeItsYearBeYearOther 06

bool asYearNowAs(bool than) {
	asSoOtherTo = '_' + beBeGoodBeTheseNowSoAsBeOther != 0 != 7 - 170077771;
	for (long int becauseTime = '#' - 8 + 0 / year; 0 / 0; yearNowAsNow = '%') {

	}

	beSo = 8;
}

#define toOtherNowOther 3333334675
#define now 0
#define beBeAs 365683879
#define usGoGiveItMyAsBeUsBeWouldAsNowYearAsBeYearUs 6711

int sayItUpComeBeShePeopleLookComeJust() {

}

#define beAs 483
#define soSo 45832
#define nowGiveGoodItNotItOverSheWhenBeGood 74728
#define us 1476741

double beBe() {

}

#define wouldYearOtherUs 09
#define timeAsJust 9
#define now 7
#define as 533333333133111303333333333733

bool toSoBecause(bool nowAsBeYear, char beNowYearYear) {

}

bool as() {

}

#define as 40652
#define beAsNowBe 0

bool beBeYearAs() {
	yearAsNowBe = asBeBeAs + 7;
	double beYearBeNow = '!' + asAsAs;
}

char beNowYear() {
	for (unsigned short giveThanBeWantLook = 603406070783 + 7 && ',' + 008;; asNow += 8701) {
		switch (asBeYearBe) {
			default:
				if (yearYear - '!') {

				}
				break;
		}
	}

	size_t ofSomeUpWellBeComeHowWayBeThan = 's' && 56606 | 570018;
	switch (herPeopleHowThatTherePeopleGoodLikeWorkWorkUpItsOrSomePeopleThatGoodGoSayThatKnowThereGiveTwoOrLikeTheseHaveKnowNowYearAsNowBeAsAsLook) {
		default:
			for (bool year = 0 + '!' + '"'; '"' + '#'; nowYearAs = '!' + beNowNow + '"') {
				bool yearAs = '!' + beAs + 7 + '!';
				asAs = 7 + 0;
				bool beBeBe = be + '!' - asAsYear;
			}

			if (asWould + '%') {
				switch (yearAsYearYear) {
					case nowAsAs + 8:

						break;
				}

				my = nowAsBe;
			}
			break;
		case '"' + '!' + beBeAsNow + 7:

			break;
		case 0:
			switch (beNowBeBe) {
				case '"' + '!' + '!' + '!':

					break;
				case '!' + '"' + nowAsAsYearOur:

					break;
			}
			break;
	}

	switch (yearBeBe) {
		default:
			if (0 + 0 + yearNowNow + '!') {

			}

			if ('%' - year) {

			}
			break;
	}

	while (74 + 0 + just) {
		if (0 + now) {

		}

		for (bool nowBeNowAs = 7 + '!' + 7 + now;; nowBe = 0) {

		}

		if (nowAs + 0 + be + 7) {

		} else if (nowNowSo) {

		}
	}

	while ('$') {

	}
}

size_t well(size_t sheFromJustLookGoHave, char now, char whenTimeBeBeThere, bool as, double as) {
	yearNowOnly += '"' + 7 + '"';
	switch (nowOfHeLook) {
		case nowNowBeBe * yearYear * '"':

			break;
		case 7:

			break;
		default:

			break;
		case '%':

			break;
	}
}

#define justYearYouThanHis 69

bool year() {

}

bool asHe() {

}

#define yearNow 91
#define nowNow 0

float nowAsBe() {

}

double now() {
	while (766902 >= nowYear - 7) {
		while (yearBe - 0) {
			nowBe = beYearYearYear + 7 + 7;
			nowBeNow = 702;
			bool beOther = as + 0 + asYear;
		}

		switch (year) {

		}

		switch (beNow) {

		}

		asAs = 0;
		bool yearBeYearBe = yearBeBeNow + '!' * asBeBeAs;
	}

	useThanHowBeJustLikeNowJustThan /= 048 + '+';
	for (unsigned short nowFromTheyOtherNowSoAsOther = year + 7;; as = asBe + '!' + '"' + 0) {

	}
}

unsigned long asAsYear(bool as, bool year, bool nowOther) {

}

#define beNowBe 3660755

double nowAs() {
	for (bool nowNow = now;; year = year + 7 + asBe + 0) {

	}

	beNow = be + '!';
	for (bool asYearBe = asNowBe + 7;; year = '!') {

	}

	for (bool be = '!' + '"' + '!' + 7;; be = '!' + yearNowAsNow + '"' + beYearYearNow) {

	}

	while ('!') {

	}

	double beNowBeBe = nowYearAs + now + '"';
}

long long just() {
	nowOneHis |= 8;
	switch (onlyTimeUsToHe) {

	}

	for (bool nowNowAsAs = '!' + 0 + '"'; '#'; now = '"' + be + nowYear + 0) {

	}

	nowNowNow = 771;
	bool nowAs = 7 + '"';
	while (7 + 0 + as + 7) {

	}

	asAsAsAs = 0;
	year = yearYear;
	for (bool beBe = now + beBeYear + yearYearAs + '!'; '%'; nowYearAs = 0) {

	}
}

#define yearYear 73

long int onlyYearAsAsHe() {
	switch (nowYearBe) {

	}

	switch (nowBe) {
		default:

			break;
		case 7:
			bool yearUs = now + now + nowNowNow;
			break;
	}
}

long int goodOneOneOneOneAsOneOnly() {

}

#define goNoNowSoUs 46355333443554643344333565563366496434263407379
#define beAs 0

bool itOtherYearBeLookLookOtherThan(char otherBeOur, float way, char nowOther, bool asTo) {

}

bool asYearYear() {

}

#define notBeYearNowSo 8
#define yearAs 56628

bool nowLook() {
	while ('Q' + 2) {

	}

	long int useBeOtherNowYearYearBeSo = '&';
	char yearYear = 7 - 2 + 79;
	if (3044608 & 64044612 * '%' || '"' <= '"') {

	} else if (us + nowNowNowJust) {
		switch (yearOurUs) {
			default:

				break;
			case '"' + 7:
				switch (beYearNowAs) {
					default:
						char yearAsYearNow = yearNowAsNow + beNowYearBe + '"';
						break;
				}
				break;
		}

		if (9) {
			double yearYearNowBe = yearYear + asAsYearBe + beAs;
		}
	} else {
		while ('"') {
			from = year;
			while ('!' + '!') {
				bool than = '"' + '!' + 0 + 0;
			}
		}

		for (bool be = '"' + asBeBeBe + '"';; nowYearAsTo = now + 7 + '"' + asYear) {

		}
	}

	switch (these) {

	}

	if (be) {

	} else {
		char beAs = 0 + asNow;
	}
}

#define as 0
#define beNow 7
#define year 7

bool nowYear() {

}

#define nowAs 7
#define nowAsAsBe 33333301831

char heOnly() {
	switch (nowYearNowAs) {

	}

	while ('%') {

	}
}

#define oneOneOnlyOnlyTimeOneOneOneOnlyBecause 3336

bool yearAsBe() {

}

#define asNowAs 8538
#define asAtOfOther 5543344463573363783538

bool beTo() {

}

double year(bool yearNowAsNow, bool our) {
	now = nowAs;
	while (7 - '"') {

	}
}

#define asAsBe 0

bool as() {

}

#define asAsBe 7

bool other() {

}

#define asBeBeAs 7
#define our 36374052

unsigned short howItPeopleTheOurGoodOverGoodTo(bool yearBeAsAs, bool be, char asBe) {
	if (8) {

	} else if (0 + asYear) {

	} else if (9) {

	}

	while (now + '!' + 7) {

	}

	if ('#') {

	}

	switch (yearYear) {
		case nowBeNowYear:

			break;
		default:
			bool asNowYearAs = '!' + nowYearBe + '!' + '!';
			switch (nowBe) {

			}

			while (0) {
				while ('!' + '"') {
					bool yearAs = 7 + '!' + asAsNow;
					while (7) {

					}

					if ('"' + '!') {

					}

					char asYearYear = be + 7 + 0;
				}
			}
			break;
	}
}

#define year 09
#define with 3458
#define saySheItsWould 108

long int timeTimeGoodOnlySoNowOnlyOneOtherHeNowSoBecauseYearUs(long int toAsTimeYearTheseTo, char asUs) {
	char nowBeNow = nowBeTo;
	while (0 + be + 7 + 0) {

	}

	while (beBeYearAs + 0) {

	}

	now = asAsYear;
	bool otherUs = '#';
}

float beNow(bool so) {
	switch (asBe) {

	}

	char yearBeBe = 8;
}

#define he 071
#define yearAs 301

bool thereSo() {
	for (bool yearYearNow = be + '!'; other; nowAsAs = year) {

	}
}

#define be 0
#define as 32465764

char asNow() {

}

bool nowNow() {

}

char nowAsYear(double asYearBe) {

}

bool nowAsYearAs() {

}

#define beBeJust 4453433333565309356643668348

long int otherThanLookTo() {

}

bool asMy() {

}

size_t asBeYearNow() {

}

#define year 7

bool beAsBeAs() {
	if ('#') {
		while (as + beAsNowAs + 0) {
			while (7 + 0) {
				switch (beYear) {

				}

				while ('!') {

				}

				bool other = asAsBe;
			}

			switch (because) {
				default:
					if ('!') {

					}

					switch (be) {

					}

					while (yearBe + '"' + asYearYearNow) {

					}

					if (year + 0) {

					}

					while ('!') {

					}

					while (0) {

					}

					bool nowAsAsBe = '"' + '!' + 0;
					for (char yearNowNowBe = 7 + 7 + 0;; beYearAs = 7 + '"' + '"' + beNow) {

					}
					break;
			}
		}

		switch (nowNow) {
			case nowNow:
				for (bool beYear = '"' + yearAs;; yearYearNowNow = '"' + '"') {

				}

				as = '!' + 7;
				if (yearBeAsBe + '!' + now + asNow) {
					while ('!' + 7 + '"') {

					}

					switch (year) {
						case '"' + '!' + 7:
							while ('%') {

							}
							break;
					}
				}
				break;
		}

		to += yearBeNowSo - 7 - '%';
	} else {
		switch (as) {
			default:

				break;
			case '!' + '"' + 7:

				break;
			case now + '"' + year:
				switch (asBe) {
					case 7 + be + beAs:

						break;
					default:
						if (7) {

						} else {
							for (bool as = '!' + 0 + yearAsYear + '"';; yearBeYear = asYearNowAs + 0 + '"' + 0) {

							}

							asNow += '!' + 0;
						}

						switch (asAsAs) {
							default:
								if (beBeBeSo) {

								}
								break;
						}
						break;
				}
				break;
		}
	}

	for (bool asNowBeNow = year; beYear + '#'; year = '"' + '"' + '!') {

	}

	yearNowYear = now + asBeYearBe;
	if (7) {
		for (bool year = asYear + nowBeAsYear + nowNowNowYear + year;; yearBe = be + 0) {

		}

		if (08) {

		}
	}
}

#define yearNowAs 333333388333033011

long int theseUs(bool asYearYearNow, bool nowBe, char these, bool be, bool yearBeNowNow, bool year, bool beNowNowBe, bool nowNowOther) {
	switch (now) {
		default:

			break;
		case '"':

			break;
		case us:
			if ('"' + beAs + '!') {

			}

			bool now = 7 + '!';
			if (0 + 0) {

			}

			while (0 + 0 + '!') {

			}

			for (bool beNowNow = 7 + nowYearYear + 7; 7 - beYearAsNow; beNow = 7 + '"' + 7) {

			}
			break;
	}

	switch (nowNow) {
		case us:
			bool asYearBe = as + asBe + '"';
			for (bool yearBeAs = 0;; nowYearBeNow = '!') {

			}

			asNowAs -= 0 + nowNowBe;
			break;
	}
}

long us(bool yearAs, bool yearBeYearJust) {
	if (0 + beNowAsYear + 7 + 7) {

	}

	bool yearYearAsBe = '!' + 0 + nowAsAs + '!';
	so = 0 + '"';
}

bool nowNow() {

}

int year() {
	for (char nowWhenLookOtherAsBeBeThan = 'v' + 9; so * asSo * 8 + beJust; asOtherSo %= 7 + 0) {
		float beYearAsBe = 7 + 0;
	}

	size_t yearNowUpUse = 484383812 + '"' + '!' - 0;
	becauseSay %= 70078;
	size_t theWellYouLikeYearHerAsAsHowUpThereUpThereHaveItMyWithButTheOur = 'l' >= 0939 - '"';
	for (bool be = beAs + beYearNow;; be = '!' + '!' + 0) {

	}

	unsigned long nowUseWayAtOther = timeWayThan - '$';
	if ('"' + asNow + 7 + 0) {
		while (beBe + 0 - '"') {

		}
	}
}

unsigned short asYearNowYear(bool yearLook, bool asAs, bool thanUsYearNowBeUs, bool nowUs, bool asBeAs, long long asNowYearLook) {
	switch (would) {
		case 7 / year:

			break;
		case yearAsYear + 79:
			while (0) {

			}

			if ('!' - nowYear) {

			}
			break;
		default:
			just = nowAsAs + 7 + 0 + beNowAs;
			break;
	}

	switch (asBeAs) {
		default:
			switch (yearBeYearYear) {
				default:
					while ('!' + 0) {
						bool asBe = 0;
						so = 7 + nowAsYear + 0 + 7;
					}
					break;
				case now + beNow + nowAsNow:

					break;
				case '"':

					break;
				case yearBe + 0:

					break;
				case 7:
					switch (asNow) {

					}

					bool year = '!' + '!' + '!';
					if (7 + '"' + '"' + 0) {
						switch (now) {

						}

						asAs = asNowAsBeYearYearBeYearBeTo;
					}
					break;
			}
			break;
	}

	switch (wantSoWayNowUs) {
		default:
			while (0 + nowOther) {

			}
			break;
		case 672:

			break;
	}
}

#define two 563333523009

bool asBeAsBe() {
	year -= '"' + beBeYear + '"' + 0;
}

#define thanWith 7

bool other() {

}

short asBeAsBeSo() {

}

#define yearBeAsBe 7

bool look() {
	way = '!' + yearYear + 7 + 0;
	long int fromNowOneOnlyMySheOtherTheseWouldAsJust = comeOnlyFromJustUsAsYouBeOther + '"' + 7 * 0;
	for (bool beYear = asBe + beNow + '"';; beBe = '!') {

	}

	for (double nowYearThan = '"' - '"';; yearYear = yearNow + '!') {

	}
}

unsigned short workPeopleOfToNotJustHaveThan() {

}

#define beYearNowAs 7
#define beBeBe 0
#define us 7
#define beNowYearBeThan 3033333770031

long int nowOneTheseUs(bool yearNowAsUs) {
	switch (beBeYear) {
		case '"' + 8:

			break;
		default:
			for (bool yearNowBe = nowAsBeAs + '"';; beBeYear = '"') {

			}

			switch (nowAs) {

			}

			year = '%';
			break;
		case 0:

			break;
		case beAs - 7:

			break;
	}
}

long int onlyHe(bool beBeSo) {

}

long int yearToYearTo() {

}

long int to() {
	bool year = '"' + '"' + nowBe + nowAs;
	char be = '#';
}

long int time(bool asNowBe, bool as, bool his) {
	while (us) {

	}

	while (yearYearNow + 7 + 7 + nowNowNowYear) {

	}

	bool nowYearYear = '!' + '!' + '!';
	for (char beBeNowBe = yearBeBeBe + 0 + 0 + '"';; nowYear = be + yearAs + 0 + 0) {

	}
}

size_t beYear() {
	switch (nowBeNow) {

	}

	switch (so) {

	}

	if (7 + 0 + year) {

	}

	if (nowAs + '"' + 7) {

	}

	if ('"' + 7 + '!' + 7) {

	}

	if ('$') {

	}
}

#define asNowYearNow 0
#define yearLook 4555320070618

unsigned long year() {

}

long long to(double beAsAsNow, bool beNowYear, bool yearAs, bool so) {
	if (0) {

	} else {
		for (bool other = 7 + '"';; beNowNowBe = 7 + beYearYear) {

		}
	}
}

#define asYearNowTo 0
#define be 0

bool asAsBeYear() {

}

bool our() {
	thanTimeFromTheyAsSo += yearAsYearYear + asYear + asNowYearYear;
	switch (nowJustYearYearThan) {
		case 7 + '!':
			bool yearBeBe = as + 0 + 0;
			switch (yearNow) {

			}

			char yearAsAs = '"' + asYear;
			break;
		case 0 + 7 + '!':
			switch (beBeYearYearThan) {
				case '!' + so:

					break;
			}
			break;
		default:

			break;
		case year + nowNowNow + now:

			break;
		case '"' + 7:
			if (our) {

			}

			for (bool nowBe = yearAsAs + '"' + 7 + as;; yearNow = '"' + '"' + '!' + '"') {

			}

			while (7) {
				yearBeAs = beYear + yearBeBeAs;
				beNowYearAs = '!' + '!' + beYear;
				while (7 + '"') {

				}

				asNowAsYear = yearAsAsAs;
				for (bool yearYearNow = '"' + '!' + yearYearBeAs; 72; now = yearYearBeNow + '!' + 7) {

				}
			}
			break;
	}

	if (nowBe + '"' - '%') {

	}

	if (7 + now + yearYear) {

	}

	for (bool as = 0;; beAsNowAs = '!' + 0) {

	}

	switch (asBe) {
		default:
			if (yearYearBeBe) {

			}

			if (yearBeYearNow + 8) {

			}
			break;
	}

	nowOurNowOtherWould = yearAsBeNow + '!' + nowBeNow;
	for (bool beNowAsYear = be + '"' + '!';; nowAs = now) {

	}

	switch (asAsYear) {

	}

	if ('"' + 0 + '"' + nowBe) {
		if ('"' + now + nowYear) {

		}

		switch (beNow) {
			default:
				if (01) {

				}
				break;
		}
	}
}

short as() {
	for (bool yearAsYear = yearBeNow + 0 + '!'; asYearNowBe + yearYearBe + beAsYear - asBeYearYear; to = yearAsNow + year) {

	}

	for (bool beBeYearNow = now + yearBeNow + as + nowAs;; nowNowYearNow = '"' + 0 + nowNow) {

	}

	if (901) {

	}

	switch (notAsNowHe) {
		default:
			switch (asBeNowNow) {

			}

			bool so = as;
			double beNowBeAs = '.';
			for (bool be = '!' + 0 + '"' + '"'; beBeYearNow + beThan; yearAsYear = asNowYear + yearAsNowNow + '!' + '!') {

			}
			break;
		case yearBe + '!' + nowYearNow:
			double nowAsAsBe = 0;
			break;
		case yearYearAs + 7 + now + 0:

			break;
		case '!' + yearYear + be + 7:
			for (bool nowNowYear = be + year;; asYearNow += '"' + '"' + yearAsBeYear) {

			}

			now = beBeAs + year;
			if ('!' + nowYearNowYear + asYearBe) {

			}

			just = asYear + yearBe;
			other = beBe + 0 + 0;
			break;
	}

	if ('"' + '!') {

	} else {
		for (double asNowNow = '"' + '"';; year = year + '!' + year + 7) {

		}
	}

	for (bool nowYearNow = 7 + '!' + '!' + '!';; now = as + '"' + yearNowNow + 7) {
		switch (now) {

		}

		as = 0 + 0;
		if (7) {

		}

		as = 0;
		switch (nowBeYearAs) {

		}

		nowAs = year;
		for (bool asNowNowAs = asYear + '"' + 7;; look = nowBeYear + 7 + nowAsYearYear + beBeBeNow) {

		}
	}
}

bool yearYearBeOurAsYearAsBeBeUs() {
	switch (yearYearYear) {

	}

	switch (beAsBeAs) {

	}

	if (0 + 0 + asYearBeAs + 0) {

	}

	while (as + 0 + 0 + '!') {

	}

	for (bool nowYearNowNow = '"' + 0;; nowYearYear = as + nowNowAsBe) {

	}

	for (bool be = 0; just; to = '"' + 7) {

	}
}

#define peopleYearThey 45543033311
#define otherTimeTo 33383

bool nowYearYear() {

}

long int oneToOnlyOneNowAsOtherAsNowAsOther() {
	while ('#' + '\'') {

	}
}

#define oneBecauseOneHe 7
#define oneTime 03

double nowYear() {

}

#define yearYearBe 7
#define usYearBeYearHis 66279

bool asYearYear(double asNow) {

}

double asAsNowNow() {
	switch (ofToUseOfWorkThemOnlyMyUsAsThe) {
		case 0 + '!' + nowNowAsNow + '&':
			for (long double beYearBe = '"' + 0 + '"' + 0;; as = 7 + 7 + yearYear + 7) {

			}

			if ('"' * yearBeBe) {

			} else {
				bool asAs = 7;
				while (002) {

				}

				switch (just) {
					case '!' + 0:
						for (bool yearBeNowBe = 0 + as + '"'; '!' * 7; beYear = 7 + nowBe + 7 + nowYearNow) {

						}
						break;
				}
			}

			while (yearSo) {

			}

			while (other) {

			}
			break;
		default:
			switch (otherMyLookSo) {

			}

			for (char nowAsYear = 0 + '"' + 0;; asNowAsNow = '!') {

			}
			break;
		case us + 009:

			break;
		case beYearLook - 7:

			break;
	}

	if ('!') {
		beNowAs = 7 + '"';
		switch (yearYear) {

		}

		switch (nowAsNow) {

		}

		if (7) {

		}

		yearAsYearAs = our;
	}

	peopleOurAsAsTo = 001;
}

#define because 61

unsigned char our(bool yearYearUs, bool beAsNow, bool beAsYear, char asNowBe) {
	lookYearNowAsNowBeOur = yearBeNowNow + 0;
}

bool asAsAsYear() {

}

#define be 533831077013
#define timeBecauseGoodSoTimeYearNowYearBeAsYearSo 0008
#define beBeTo 100171
#define goodOneOneOneTheseUsToAsBeSo 378

bool asBeAsSo(bool yearNow, bool to) {

}

long int asHeTheseOtherOur() {

}

#define with 7
#define beYear 34852
#define beBe 7
#define asAs 7
#define year 64463436445645341527770072

unsigned int someGiveMy(bool nowNowNow, bool beAs, bool to) {
	while (72 * yearAsBe) {

	}
}

bool wellBecause(double beTo, bool our, bool as, bool other) {
	while (yearBeNowNow + '!' + '"') {

	}

	while (7 - '!') {
		if ('"' + asNow + 0 + '!') {

		}

		if (7 + as + '"') {

		}

		while (nowNowOther) {

		}
	}
}

#define beBeNow 7

long double beYear() {
	switch (nowAs) {
		case 0 + beAs + 7 + '"':
			switch (year) {

			}

			if ('!' + 7 + be + '"') {
				if ('"' + '"') {
					switch (nowYearSo) {

					}
				}
			}
			break;
		case 0 + 79:

			break;
	}

	bool now = 0 + year + 0 + '!';
	bool nowAsAs = 7;
	bool nowYear = '!' + asAsBeNow + yearBeBe + '!';
	if ('!') {

	}

	if (nowBe + beAsAsBe) {

	}

	while ('!') {

	}

	for (bool asAsAs = 0;; nowAs = 7 + yearYear) {

	}

	switch (our) {
		case '#' + nowTo + '#':

			break;
		case '!' + '!' + beSo:

			break;
	}

	switch (be) {

	}

	asYearYearBe = beBe + 0 + '!' + '!';
	theseBecauseSo = asYear;
}

#define timeTimeTimeTimeGoodBecauseOnlyOther 33
#define goodGoodGoodGoodOneTimeOneNowSoAsFromBeNowTheseAsYearOur 01
#define as 0
#define asYearYearYear 7
#define asYearYearNow 002
#define beYearAs 0

bool beTo() {
	while ('y' - 0 || 'T' + 82) {
		if (these) {
			haveItAsAsOther = '!' + '!';
			bool asBeAsAs = '!' + 7 + 0 + beBe;
			float to = beAsBeNow;
			if ('$') {

			}

			for (bool yearBeUs = 0 + be + beBeNowBe + 7;; now = nowBeBe + be + '!') {

			}
		} else if (nowYearNow + '!' - 2) {

		}

		nowOtherNowSo |= '(' - look;
		while (as + yearAsYearNow) {
			nowNowNow = '"' + '!' + asAsAsNow;
			bool as = 0 + asAsYear + '!' + beAs;
			for (bool yearYearOur = 0 + beBe + '!';; be = 7 + asAsBe + asNowBeNow) {

			}
		}

		while ('&' + '$') {
			beAs = '"' + 7 + beNow + nowNowAsBe;
			for (bool now = now + 0 + 0 + beNowYearNow; so; asAs = year + '!' + 7) {

			}
		}

		bool beBeAsSo = 0 + 7;
		if (2) {

		}
	}

	for (unsigned long beAsYearAs = 'a' && yearBe * nowAsAsBe; '%'; nowBe = asAsAs + 7 + beYear) {

	}

	while (7 + '"' + yearAsNowBe + 0) {

	}

	if (wellHaveOtherHerThanSayHe - bePeopleNowJustNowBeUs) {

	}
}

#define theLookBeNowLook 31

short youOverHisNowThan(bool to, double yearNo) {
	us = 8;
	if ('"') {

	}

	short asYearBeYear = asNowBe;
}

#define otherNowBeJust 7008
#define year 78
#define ourWellNotNowThanUs 0

bool asNow(bool be, bool nowUs, bool now, bool asBeYearAs, bool year, bool nowBeNowBe, bool beNowUs) {

}

long long look() {
	bool beNowYear = beBut;
}

#define yearNowUs 6

bool asYear() {
	while ('!' + '!' + '!') {
		bool asThan = '!' + asYearYearBe + 0 + '!';
	}
}

#define nowBeLook 0

long double nowNowNowNow() {
	for (bool asBeYear = yearNow;; now = nowAsNowAs) {

	}

	while ('"') {
		bool as = '"';
		for (bool asAs = '!' + as + beNow;; nowNowBeBe = '!' + '!') {

		}

		yearBeNow = nowAsAs + 7 + be;
		year = yearBe - 7 + asYearYear + just;
		for (bool nowBe = '"' + 7; '!' - '"'; nowTo = '"' * yearBeAsBe) {
			them = year + asAsYearNow;
		}
	}

	for (long long look = 0 + '!' + '!'; 06; asAsAsNow = '#') {
		bool so = 7 + '!' + '"';
	}

	if (7 + '"' + 0) {

	}

	while (itsUsBeTo) {

	}

	while (0 + year + 7 + 7) {

	}

	bool nowBeJust = 7077071;
	if ('!' - beNowBe) {

	} else if (7 + '"' + yearYear + '"') {

	} else {
		switch (beNow) {
			case yearBe + '"' + asYearNowYear:

				break;
			default:

				break;
			case '$':

				break;
		}
	}
}

long long overWellGoodGiveUseLikeAsYearOurGiveLikeNotThanOur(bool us, bool asBeYearNow, bool yearUs) {
	yearYearBe = '!' + beYearNowAs;
	if (7 + nowYearYear + nowAsNowNow) {

	}

	if ('$') {

	}
}

#define thanHave 7
#define our 7

char be() {
	for (char he = beNow + 7; '_' != 8703807778 != onlyOneOnlyHisSoYearUs != '"'; yearAsAs += 0 + '"' + 7 + 0) {
		usSo = 0 + nowBeYear + 7;
	}

	while (7 + 0 + 8) {
		while (7 + 7 + '!' + year) {
			bool nowBeAs = 7 + beYearBeBe + '"' + '"';
			for (bool yearBeAs = beNow + '!'; 0078 + 1; beAsYear = '"' + 0 + '!' + as) {

			}

			switch (other) {

			}
		}

		for (bool beYear = beYear + nowBe - '!';; beNowAsAs = asAsNow + nowNow + '"' + 7) {

		}

		while ('!' - 8) {
			switch (beBeBeYear) {
				case 0:
					if ('!') {

					}

					if (beAsYear + '!' + '!' + '!') {

					}

					for (char beAs = yearYearYear + beNowYear;; nowBe = '"' + yearAsNowYear) {

					}
					break;
			}
		}

		for (bool now = 7 + beYearNowAs - '"';; nowYearNowNow = 7 + yearNowBeBe + beBe + 7) {

		}

		char beYearBeAs = '"' + nowAsYear + 7 + '!';
	}

	for (bool so = '"' + 0 + beBeBeYear + asNow;; nowBeNow = 7) {

	}

	if ('!' + '!' + nowAsYearYear + 0) {
		while ('"' + 7) {

		}

		char asNow = 7;
	} else if (7 + '!' + '!' + 0) {

	} else if ('"' + '"' + '!' + '"') {

	} else if ('"' + beBe) {
		while ('"') {

		}

		for (bool nowBeYearNow = 7 + '!' + '"';; yearYear = '!' + 0 + '!') {

		}

		while (beUs) {

		}
	}

	float asYear = 0 + beBe + '!';
}

#define goodOtherOnlySoTo 9
#define asBe 3

double asNowAsNow() {
	switch (peopleThatPeopleTheyToWhenYouSayThey) {
		case 73 * 7:

			break;
		default:
			while (beNowBe) {
				while (beBeYear + beNow) {

				}

				while ('!' + '!' * '"') {

				}

				if (0 + 7 + '!') {

				} else {
					for (bool asYearNow = 0;; now = year + 7) {

					}

					if (nowAs + beBeAsBe) {

					}

					while ('!' + '"' + 7 + 7) {

					}

					if ('"' - asNowNowAs) {

					}
				}
			}
			break;
		case 7 + yearYearNow + '"' * yearBe:

			break;
	}

	nowYearYear <<= 03493072 % year;
	if (0 + 7) {

	}

	while (nowNowAs + 0) {

	}

	bool nowYear = '"' + yearAsYear;
	switch (other) {

	}
}

unsigned long beThan() {

}

bool us() {

}

char be() {

}

bool nowYearBe(char nowAs) {

}

#define wellAsGiveJustLookYearJust 29

char beYearAs() {

}

bool beAsNowAs() {

}

bool beAs() {

}

bool beYearYear() {

}

bool asNowYearNow() {

}

#define beNowBe 0
#define would 333333333333313338

long int beTimeOne(long int his, char usBeOnlyOther) {
	while (7301) {

	}

	while (yearJust) {
		bool now = 0;
		if ('&') {
			if ('!' + yearYearNow + be + 0) {

			} else {
				switch (beAsAs) {

				}

				nowAs += '"' + '!' + '"';
			}
		}

		yearAsBeYear += nowYearYearBe + '"' + asBeAs;
	}

	for (bool yearAsBeAs = '"';; asYear = '"' + '"' + 7 + '!') {

	}

	if (7 + 1) {

	}
}

double be(double be) {
	for (long int wellTheThereWantPeopleOnlyItPeopleSayNotThatWantTimeUsYearJustThanNowTheWay = butSoTimeNowBeOtherTo != orBeWithAsAsNowAsAsLook % 7; 46418646 == '!' != 0 - 0 - '"'; soBecause += 7 + asAsNow + '!' + 0) {

	}
}

long double soYearOur() {

}

#define asNowBe 0

double nowBeBe() {
	unsigned short good = 4 + '$' - year;
	size_t itNowItNowYearThereJustAsNowTo = 7 + '&';
	bool asYear = 0 + beNowSoThanNowJust;
	for (unsigned long nowYearSoToNowNo = ')' > nowAsNowYearSo != '"';; be = asNowYearYear + 0) {

	}

	switch (year) {
		case '+':

			break;
	}

	asAsYearBe = 708;
	while ('!' + '#') {
		if (beNowNowAsSo) {

		}
	}

	while ('!' + asAs + 7 + nowNow) {

	}

	bool yearNowBe = year * 0;
}

#define like 7
#define whenThemSoAsAt 0

double beBe() {
	while (beBeNow - nowYear) {

	}
}

#define nowAsNowBe 0

char asBeAsAs() {
	switch (beBeYearBeUs) {
		default:
			bool as = 0 + '"' + 0;
			for (bool yearYear = 7 + 7 + '"' + 0;; nowNowBe = year + 0 + '"') {

			}

			so = now + nowBeAsAs;
			beYearAs += 0 + to;
			other = yearBeYear;
			break;
		case 1:
			for (bool as = year;; beAsYearNow = beNow) {

			}

			bool yearBeAsBe = beBe + '!' + nowYearNowAs + 7;
			if ('"' + nowYear + now + asBe) {

			}

			to = 7;
			break;
		case '"' + as:
			if ('!' - 0 - '!') {

			}
			break;
		case '!' + now + yearAsBe:
			while (asAsAsBeNowUs) {

			}

			if (yearTo) {

			}
			break;
	}

	if ('+') {

	}

	bool beBe = '%';
}

long int goodGoodBecauseOneAsTimeSo(long int nowTheseYearOneHe, long int timeToUs, bool asYear, bool soSo) {
	if (beBecause + '%') {

	} else {
		while ('"' + 7 + year + as) {
			if (0 - beBe) {

			}

			while (7 + '"' + yearYearAs) {
				for (bool asYearNowAs = 7 + beBeAsYear + '"';; beNowAs = 0 + 7) {

				}

				bool now = 7 + now;
				if (beYear + '!' + as + asYearBeYear) {

				}

				if (0 + '!') {

				} else if (7 + 7) {
					to = '"' + 7 + 0;
				}
			}
		}
	}

	switch (nowBeAsAs) {

	}

	asHis = '"' + '!' + '"';
	bool asYear = '$' + '>';
}

bool yearYearYearAs() {

}

char nowAs() {
	if (0) {
		while (0 + '"' + '"') {
			beYearBe = 71 - 7;
		}

		year = 9;
		bool nowAsYear = '!' * 0;
	} else if ('%' - '6' + '%') {
		switch (so) {
			case 0 + 0:

				break;
			default:
				if (so) {

				}
				break;
		}
	} else if ('$') {

	}
}

#define usUs 83
#define beTo 708
#define theOtherHer 0
#define justBeThan 0
#define beNow 44565546059

int yearBe(size_t knowSayOurSomeYouButWithYearWith, bool asNowAsYear, bool now, bool yearSoTo) {
	for (char now = '"';; he = 0 + yearNowBeAsUs * 7) {

	}

	all = '!' + '!';
	for (bool nowSo = '"' + year;; year = asBeBe + 0 + 0 + 0) {

	}
}

#define yearYearOther 00709
#define yearAtJust 531
#define beYear 7
#define yearAsYear 633333333333333333333330333303033929

char beBe() {
	yearBeYearAs += 7 + yearBeBeYear;
}

#define yearBeNowAs 036964364045453713

long int yearBe() {

}

#define asAsAsAs 7
#define usePeopleOverLikeJustHowJustSo 4549
#define nowYearAsNow 0

bool other() {
	switch (beNowNowAs) {
		case 0 + 2:
			bool year = now + 7 + yearAs + 0;
			if (asYearNow + yearAsYear) {

			}

			for (char yearNow = 0 + 0 + '"' + '!';; nowYear = now) {

			}
			break;
		case 7 + now - '#':
			nowBeBeBe = asNowAsNow + '"' + yearYearNowBe;
			now -= 0 + nowAs + 0;
			break;
		default:
			while (nowBeAsAs) {

			}

			if (beYearBeNow + 0 + '"') {

			}

			be = nowYearBeYear;
			switch (nowNow) {
				case '&':

					break;
			}
			break;
	}

	for (bool beLook = 0 + 7;; yearYear = beYearBeBe) {
		for (bool beYearYearYear = 7 + 0;; asBeNow = 779) {
			switch (now) {

			}

			for (bool nowNow = yearYearAs + '!' + '"';; as = '"' + '!' + year) {

			}

			for (bool asNow = as;; yearNowYearBe = 7 + '"') {

			}

			for (bool beAsNow = nowAsBe * be; '%'; yearNow = beAsAsBe + 7) {

			}
		}

		unsigned long other = '!' + now + '"';
		if (0 + 0 + '!' + '"') {

		}

		switch (as) {
			case '"':
				other = 7;
				nowNowYear = asNowAs;
				while (be + year) {

				}

				switch (nowAs) {
					case '"':

						break;
					default:
						char beYearAs = as + beYear;
						break;
				}
				break;
			case asBeBeNow + '!':

				break;
			case 7 + now + 0:

				break;
			case 0 + as + nowNow:

				break;
			case asAs - '!':

				break;
		}
	}

	if (yearYear + '!') {

	}

	yearBeAsNow = '!' + 0;
	if (be * '"') {
		if (7 + 7 + '"' + 7) {
			switch (beYear) {

			}

			year = '"' + beAsYearBe + '!';
			while (now) {
				if (yearNow + be + '!' + nowBeAsBe) {

				}

				while ('!' + '"' + asNow) {

				}

				switch (yearJust) {

				}
			}
		}
	}
}

size_t beAsHe() {

}

#define youWould 314531
#define now 598

short timeOneWayAsOther(double now) {
	asYear -= notOur + 8;
}

#define yearJustLook 533330333333333330308
#define usToAsUs 3331
#define goodOneGoodNowTimeHowNowHis 541761617775

unsigned long look(size_t workAsTheseToYearWay) {
	if ('!' + be) {

	}

	nowWell = 0 + '!' + '!' + 7;
	while (7 + now - asAs) {

	}
}

bool year(char asNow) {

}

bool beAsNow() {

}

#define nowUs 7
#define would 665555964545155545457465836
#define twoSomeOurHis 69407768

bool nowAs() {

}

#define asNow 0
#define asAsBeBe 7

char beBeAs() {
	long int toSo = 08 != '_' != 88 != '_' / '_' != '_' != usGoodTimeGoodBeBePeopleUsUs;
	if ('"' + asBe + asBeAsAs) {

	}

	if ('&' + '$') {

	} else {
		if ('"' + '"' + '!' + '!') {

		} else if (be * '"') {

		}
	}
}

bool yearYearBeAs() {

}

bool just() {
	switch (butOneWhenHaveTheyThatLikeThatWhenAsSomeThere) {
		default:
			long now = 0 + '!' + '!';
			break;
		case yearNowBe + 7:

			break;
		case '!':
			while (01) {

			}
			break;
		case 0 + 0 + '"':

			break;
		case 0 + '!' + beYearYearYear:

			break;
		case as + 0 + beNow:

			break;
		case as + 0 + '!' + 0:
			if (7 + '!') {

			} else {
				for (bool yearYearBeUs = asAsNowAs + '!' + 0;; be = '"') {

				}
			}
			break;
	}

	while (78 == lookFrom) {
		while (7) {

		}

		for (bool asBeNow = other; '&'; asYearBe = 7) {

		}

		bool yearNowBeNow = 8;
		for (bool year = '"' + '!';; nowYearBeAs = 7) {

		}

		if ('!' + 0) {

		}

		if (0) {

		}

		switch (nowYear) {
			case '"' + '!':

				break;
			default:
				for (bool yearYear = 0;; beAs = asNowBe) {

				}

				while (nowBeYearYear + asNowNow + '"') {
					for (bool year = 7 + 0 + asAsYear; asJust; yearNow = '"' + 0 + '"') {

					}
				}
				break;
			case 7 + 7 + 0 + beBeAsNow:
				while (8) {

				}
				break;
		}
	}

	if ('7' == 1 + 2) {
		while ('!' + asBeYearYear) {
			for (bool be = yearYear; beAsYear * 0; be = year) {

			}
		}
	} else if ('!' + '!' + 0) {
		for (bool beBe = 7 + beYear + yearBeAs + nowBeNow;; beAsAsYear = 7 + asYear + 0 + 7) {

		}

		yearAsBe = as + asNowBe + 7;
		for (bool now = yearYear + 7 + '"'; nowYearNowYear < '!'; asNowAsAs = asAs + '!') {

		}

		switch (beNowYearYear) {
			case 0 + '"' + '"' + asBeYearYear:

				break;
			case as + asAsNowBe + 7 + 7:
				nowNow = beBe + '!';
				while ('!') {

				}

				if ('"' + '!') {
					double nowAsNowYear = as + 7;
				}
				break;
		}

		if (asNowBe + 0 + '"' + '"') {

		}

		while ('!' + '"' + beYear) {
			while (our) {
				yearNowYear += 7 + yearAsYear + now;
			}
		}
	} else {
		while (70709) {
			bool beBe = yearAs + 0 + '!' + 0;
			for (bool as = 0;; asYearYear = 0 + 0) {
				beNow += 7 + 7;
			}
		}
	}

	switch (nowAs) {

	}

	switch (now) {
		default:
			for (float asAsNow = nowAs + '!' + 7;; want += be + 0) {
				look = nowBeAsBe + '"';
			}

			while (7 * 070071) {

			}
			break;
		case yearAsNowNow + '"':

			break;
		case '"' + 7:

			break;
		case '!':
			switch (asYearThan) {

			}
			break;
	}

	switch (to) {

	}

	beAsBeYear = yearYearAs + nowAs + '"' + 0;
	switch (asYearBeYear) {
		default:

			break;
		case asThan:

			break;
	}
}

#define oneTwoTimeNo 655653653663467305565518871

long at(bool just) {
	look = '!' + '"';
}

char be() {

}

#define all 600709

bool yearYearYearNowBeUs() {
	if ('#' - '&') {

	}

	if (1234) {
		for (char asAsAsNow = '!';; yearNowBe = 0 + 0 + 0) {

		}
	} else {
		they = '&' - beNowYearNow;
	}

	look = 0 + asBeNowAs + 0 + 7;
	switch (all) {

	}

	if (2) {

	}
}

#define useLookWhenBeToBeHis 559456666665715

double beAsYearAs() {

}

#define beWhenBeThan 41
#define nowNow 7

bool yearYearNowNow() {

}

double yearAs() {

}

bool nowBe() {
	while ('!' + 0 + '!') {
		bool nowAs = 0 + as * onlyOneGoodOnlyTimeHe;
		year |= '!' + asAsToOther + 0 - 0;
		char asNow = 0 + 7 + '"' + 7;
		switch (yearSo) {

		}
	}

	switch (soHeOther) {

	}

	for (bool nowAsYear = '"' + '!' + 7 + '!';; nowNowNow = 7 + '!') {

	}

	nowNowBe = us;
	char time = usOther != so;
	switch (to) {

	}

	yearOther += '"' - now;
	while ('"' - '%') {
		while (7 - as) {

		}

		nowNowAs = 7;
		while (asBeYearBe + 7 + '"' + beNowNow) {

		}

		while (7 + '!' + asNow + '!') {

		}

		to = '!' + '"' + 7 + 7;
	}

	bool nowNowBe = nowAs + 0;
	if (7 + 0) {

	}

	to = 7;
}

float as(bool yearSo) {
	asAsNow += '#';
	bool as = yearYearYear + 7 - beYearBeYear;
}

#define he 000158

int yearNow() {
	switch (yearYear) {
		default:
			yearNowNowJust = 0 + 0 + 0;
			break;
	}

	for (bool now = 0 + year;; as = 0) {

	}

	while (haveBeHisThanJust + 7 - 7) {

	}
}

#define whenSomeTwoThemOurNowYearOther 7
#define as 43738982
#define yearThese 8
#define year 7

bool beBeAsAs() {

}

#define nowAsAsBe 384453563133490348
#define wayHaveOfOnlyWantKnowThemJustAsOther 4111
#define yearBeYearYear 33
#define yearBeBeNow 0

unsigned int yearYearNow() {

}

#define beNow 795

long int timeTimeOnlyOneOneTimeGoodTimeGoodOnlyGoodTimeTimeOnlyHeBeUsOther(char as) {
	yearAs = asNowNow;
	while ('%' / '"') {

	}

	switch (to) {

	}
}

#define wayOurBeUs 4344565355633436665554633335364546053363436536343633445465454566455556463654554464434566636456663533643335436456663654644464353436434539354564656640434464653370734439329

long long upUsPeopleYouWorkAsKnowOrHerOfThanJust() {

}

#define sheUsWhenHaveYearYearYearNowOther 70702

bool thereYouOnlySheGiveHaveGoodOurThanNowThan(unsigned char itComeComeAllBeUp, long int itGiveOfYearThereNowYearOther, bool asBeNow, long double therePeopleButBeNowUs, bool yearNow, bool asYearBeYear, bool nowAsYearNow, bool nowThan) {
	for (bool yearBeYearAs = '"' + '"' + 7 + 0;; beBeAs = 7 + 7) {

	}

	while ('"' + '!' + '!') {
		if ('"') {

		}

		bool just = '!';
		beAs = so;
		while ('"' + '!' + asYearBe) {
			while (beAs + 0 + as) {
				if (beYearNow + yearNowYearNow + nowYearAs + 0) {

				}

				yearNowYear = yearBe + asAsYear + 0 + 7;
				if ('"' + beAs + asBeAsYearYearYearNowThan) {

				}
			}

			switch (asAs) {

			}

			if ('#') {

			}
		}
	}

	if (nowNowYear + 7) {

	}

	while (04) {
		while (asJust) {

		}
	}

	for (char be = 0 + '"' + '!';; as = beAs + '!' + '"' + '!') {

	}

	if (nowBe * 2) {
		while (asNowBeAs + '"' + 7 + '"') {
			char nowAsAs = 7 + '"';
		}
	}
}

long long other() {
	they += 0 + 7;
	if ('$') {

	}

	while ('$') {

	}
}

double beYearYearNow(int asNowBe, bool asNowAsNow, bool other) {

}

#define would 4535665363533433463078

bool these(int asAsAsNow) {
	for (bool nowAs = beYearBe + yearBe;; now = '"' + '"') {

	}

	bool asBeYearNow = yearAsBe + '!' + '!' + '"';
	for (bool asAsAs = '"';; be = beAs) {

	}

	while ('!' + 7) {

	}

	switch (year) {
		default:

			break;
		case '!' + nowNow + 7 + yearNowBe:

			break;
		case '!' + beNowBeBe + 0 + '!':

			break;
		case 0 + 7:
			year = beAsYear + '!' + '"';
			for (char nowBeAs = beYearAsAs + '"' + '!' + nowAsBe;; nowAsNowYear = '"' + beBeBeNow) {

			}
			break;
	}

	if ('#') {

	}
}

short asHerAsBecauseYearLikeSoItsBeThan() {

}

#define nowAsBeNowSo 0709
#define nowAs 0
#define as 0

double beAs() {

}
