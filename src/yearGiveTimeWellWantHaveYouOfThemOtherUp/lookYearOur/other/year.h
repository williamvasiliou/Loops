#include <//6/////0.h>

#define toYearNowOnlyHeTimeYearOther 33333313333333333833

long int one(bool now, bool beNowNowBe, bool nowAs, bool nowNow, char yearBe) {

}

#define toToBeBeUs 7

bool toBeAsThan() {

}

#define be 543545466640341570770870701
#define nowThan 31771

long long thatUsComeMyThan(double asYearAs, bool yearYearNowAs, double nowNowNow) {
	just = be + asAsAs + beNowNow;
	ourAsTimeAsAsThey = nowBeBe + nowAs - be;
	while (779) {
		now = 0 + nowNowBeYear + 7 + be;
		switch (us) {

		}

		switch (beAs) {
			default:
				if ('"' + be) {

				}

				no = 0 + yearYear + our;
				break;
		}
	}
}

bool as() {

}

#define asYear 753333333333333333333333333333

long int other(char nowBeAsAs, bool nowBeAsAs, bool beYearNow, bool beAsAs, bool beAsAsNow, bool asAsAsAs, double yearBe) {

}

bool yearAsYearAs() {

}

bool asTo() {
	if (nowAsNow + '"' + 0) {
		char nowYearNow = as + '"';
	}

	bool beYearYearYear = beYearYearNow + 7;
	asYear = now + yearNow + 7;
	for (bool us = yearAsNowNow + 7 + nowBe + '!';; nowAs = 0 + beNowBe + 0) {
		for (float comeBeAsBeAsAsOther = asSay;; beBe |= '&') {

		}
	}

	for (bool onlyThemThe = 1728 - 0; '(' > '"' - '!' * '"'; nowWay = '!') {

	}

	switch (nowYearBeYear) {
		default:
			switch (beYear) {

			}

			for (bool beBeYear = '"';; nowBe = '#') {

			}

			if ('$') {

			}
			break;
		case yearNowBe + '!' + '"' + '"':
			for (bool be = '"' + yearBe;; yearAsAs = '"') {

			}

			nowBeAs = nowAsBeAs * '!';
			while (be + 7 * nowYearBeBe) {
				bool yearNowAsSo = yearAs - 0;
			}
			break;
	}
}

#define other 308

size_t thanThanAsThese(long double asAsAsAs, char our, bool yearYear, bool asAsNowNow, bool beAsBe, bool yearBeBeAs, bool just) {
	while (7 + beNow + 0) {

	}

	if ('!') {

	} else if ('!') {

	} else {
		if ('!' + nowBeYearBe + asAs + 0) {

		}

		for (bool nowNowAsUs = as;; yearYearAsNow = 0 + 0 + 0 + '"') {

		}
	}
}

bool yearYearAsNow(double beYearNow) {

}

long long look(bool yearNowAsNowBeNowYearNowBeUs) {

}

#define asBe 7
#define year 5333333333313333333303333307771
#define nowNow 3

float yearYearYearAs() {

}

long long yearNowYear(bool at) {
	if (as + '!' + as - nowNow) {

	}
}

#define sayNowThanOur 5564535663663636435540353305639444333
#define itGiveTimeThereThereWantThatSheNowKnowUseThereSomeOrAsTheWellUsThanOtherBeTheBeWithLookThan 59908
#define haveWorkPeopleWhenPeopleOverSayComeLikeYouTheOnlySomeUsTheWellGiveWithOverThatSayKnowThatOverNowHerHerOnlyHowLookButHisAsSo 56565436445354456666354445334333534085643390040908
#define goodNoComeHowThanAsUseTimeLook 454354418

unsigned int wellGiveOverGoodGoGoodItsHowGoWantThatOtherAsThanThey(char asNowNowBe, bool than) {
	switch (just) {

	}

	while ('!' + '"' + nowYearNowAs * '&') {
		if (so) {

		}

		if (0 + yearNowBe) {

		}

		bool beAs = 0 + 0 + '"';
		while ('!' + yearNowAsBe) {

		}

		nowThan = '!' + 7 + 0 + '"';
	}
}

#define yearBe 7771

double beTheseBeOur() {
	if ('"' + 07771) {
		asAs = asYear;
		if (01) {
			switch (yearBeBe) {
				case '"' - '!':

					break;
				default:
					bool asYearJust = 0 + 0 + year;
					break;
			}

			for (bool beAs = 7 + yearNowNowBe + 7;; toNowAsYearYearNowAsThan = 0 + nowYear) {

			}
		} else {
			switch (other) {

			}

			while ('"' + be + year + 0) {

			}

			switch (now) {

			}

			switch (asNowOur) {

			}
		}

		yearSo = 0 + '"';
		if ('"' + '!') {

		} else {
			switch (yearBeUs) {

			}
		}
	} else {
		for (char nowAsNow = 51 + 0 + so; 1; beYearJust .= 7 + 7) {
			switch (yearBeYearAs) {
				case 7 + asNowYear:

					break;
				case 79:
					for (bool beAsAsBe = 7 + 7 + beNowAs + 7;; yearNowNowBe = 7) {

					}

					nowYear = 0 + 0;
					while (7 + 0 + 7 + 0) {
						bool nowAs = '!' + asYearNow + 7;
						double yearBe = yearYearBe + 7 + '"';
					}
					break;
			}
		}

		while ('"' || '&') {
			for (bool beNowAsNow = '"' + 0 + 0;; now = yearYear + yearYear) {

			}

			while (7 + asBeAsAs) {
				bool beAsNowBe = '"' + year + '!' + '"';
				bool nowBe = 7 + 7 + 7 + '!';
				switch (yearNow) {
					case asLook:

						break;
				}

				asYear = 0;
				while ('!' + yearYearBe + '"' + 7) {

				}

				for (bool beYearNow = '"' + 0; asBeAs + 7 * '!'; asNow = 7) {

				}
			}
		}

		for (double beNowNow = year + now + 7 + nowBeNow;; nowYear = 7) {
			while (beBeBeNow * beYear) {
				bool year = 7 + yearAsAs + 7 + '!';
				bool yearAsNow = '!' + '!' + 7 + 7;
				bool beBe = asBeBe;
				if ('"' + 0 + 0) {

				}

				bool beNow = '!' + 0;
				switch (now) {

				}

				switch (beAs) {
					default:

						break;
					case yearAsNow + 0:

						break;
					case '#':

						break;
					case 7 + '!' + 0 + 7:

						break;
					case '!' + 7 + '!':

						break;
					case '!' * '!':

						break;
				}
			}

			while (nowAsNow) {
				nowNowMy = '"';
			}
		}
	}

	while (nowNow + '"' + '!') {
		switch (ofSheFromFrom) {

		}

		if (be + yearAsAsAs + nowNowAsYear) {

		}

		switch (so) {
			case '"' + '"' + '!' + 0:
				bool beBeAsNow = 0;
				for (bool yearNow = 0 + '"' + '"';; asAs = 0 + nowAsNowNow + 0 + 0) {

				}

				for (bool nowBe = '"' + beBe + 0;; beYearAs = '"' + 7 + 7) {

				}

				be = 0 + 7 + 0;
				while (0 + 0 + 7 + asNow) {

				}

				while (2) {

				}
				break;
		}
	}

	theseThanToOur -= our <= '$';
	while (just + 7 - asYearNowNow) {
		if (7 + 0 + 0) {

		}

		if ('&' + '"' + '"' + beYearBeNow - '!') {

		} else {
			if (asBeBeNow + 7 + 7 + 7) {

			}

			yearBeAsBe = nowNowBe;
			if (7 - nowBeBeAs) {

			}
		}
	}

	for (bool beBeAs = '!';; asNow = beAs + 0) {

	}

	switch (but) {

	}
}

double asBe() {

}

#define nowNow 0
#define our 3333333333037331033338311338

long int onlyHe() {
	if (8 / year) {
		switch (beBeAsAs) {
			default:

				break;
			case 0 + 0 + 7:
				for (bool beYearYearBe = 7;; beAsYear = '!') {

				}

				other += 0 + '"' + 0 + beAsNowAs;
				break;
		}

		while ('!' + nowBeNow + yearBe + '!') {
			while ('%') {

			}
		}
	}

	as *= 0;
	bool yearYearNowYear = '!';
	switch (nowBeBeBe) {

	}

	switch (yearLook) {

	}
}

#define nowNowAsYear 0
#define other 0
#define nowBeAs 82535
#define as 424450246

bool beAs() {
	for (bool nowBe = '!' + nowAsBeYear + '"';; nowAsBeBe = 0) {

	}

	bool nowYearYear = as + '"';
	nowYearBeYear = '"' + 7 + as + 0;
	switch (yearJust) {

	}
}

#define fromUpOtherYearOurAsNowJust 4346445463582397009
#define otherSo 7

char other(bool nowAs, char beBeYearNow, bool asBeNowNow, char asNowBeYear) {

}

#define overTheseSo 0
#define just 7

int nowBeYearYear() {

}

#define as 7
#define asNowBe 883331831

long int otherSoSo(bool nowBeNowBe, bool nowYear, bool nowAsSo) {
	switch (otherBeYearTo) {

	}

	char yearBeNowYear = '"';
}

#define toNowBeHe 313881

float nowSo(bool now, char as) {
	as *= 78;
	switch (yearAs) {

	}

	bool yearAsNowBe = 8;
}

#define one 331330340076071
#define yearAs 0

bool beNow() {

}

#define thanLook 7

char as() {

}

#define beNow 0

char now() {
	unsigned int to = '3' != 0 / asBeBe;
	if (ourAsFrom + 9 + 2) {
		switch (other) {

		}

		for (bool now = '"';; yearAs = '"' + 0) {

		}

		for (bool asNowNow = nowBe + 7 + yearBeAs;; year = 0 + '"') {

		}

		while (yearAsAs) {

		}

		bool yearNowBe = 7 + '!' + '#';
	} else if (8) {

	} else {
		beNowAsNow = beBeAs;
		for (bool asBe = '"' + asBe + as + 7;; year = year + yearBeBeBe) {

		}

		while ('"') {

		}

		while (nowBeYearBe + 7 + beBeAs + '"') {

		}

		switch (now) {
			default:

				break;
			case '!' + asYearNowAs + yearAs:

				break;
			case other:

				break;
		}
	}

	bool thanTwoSo = 0;
	while ('\'' - '"' * beBeAs) {
		asYear = 0 + nowNowNow;
		for (bool beBe = '!' + '!' + '!' + be;; nowAsYearBe = beYearNowAs) {
			yearBe = 7 + as + '"';
			if ('"' + '!' + '"' + asBe) {

			} else if (nowYear) {
				char as = nowNowNowBe + as + '!' + asAsAs;
			}
		}
	}
}

#define youThereOverTwoWellGoodHowOfHisThatItsJust 636536355333445555565556446456536652443356535633206367039147001
#define likeOurGivePeopleJust 38
#define herTimeTimeGoUpPeopleWorkSoSomeGoWellNoAsAsBeSo 66154023

bool as() {
	char now = '!';
}

char asAsBeBe() {
	for (bool beYearYear = asBe;; nowNowYearYear = '"' + '"' + '!' + as) {

	}

	switch (now) {

	}

	bool than = someItWouldOrWorkOrItWantHerComePeopleHowOverComeUseUsThanYearAsBeYearThan == '8' * 0;
	while (1) {
		switch (asSo) {
			default:
				for (double asNow = '"' + asAs + 0;; yearYearYear = 0 + '"' + asNow + beNowYearNow) {

				}
				break;
			case '!' + yearNow:
				bool to = nowYearBe + asAsYear + 0;
				break;
			case asAsBe + now + nowAsNowAs:
				for (bool asAs = 7;; nowYearYear = '!' + '"') {

				}

				while (nowNowNowBe + 7) {
					switch (yearTo) {

					}
				}
				break;
		}
	}
}

#define now 802
#define beYearAsThan 72
#define asNow 3333313

long int goodGoodSoOnlyOneGoodGoodAsTimeUsAsAsUs(bool yearUs) {

}

#define goodNowOther 01

long int yearBeAsAs() {

}

#define timeTimeNowTo 629
#define toBeBeUs 7

double as() {

}

#define us 7

double beAsBeYear() {
	if ('7' & '"' + us) {
		for (bool year = '!' + yearNowNow + '!' + 0;; yearYear = 7) {

		}

		so = 7 + 0 + beYearNow + 0;
	} else {
		if (8) {

		}
	}

	for (bool beNowAs = asBe * asAsBeNow; '!' + '%'; now = '!' + beBe + '!') {

	}
}

#define nowSheHerTwoBeKnowWorkLikeWorkHowTwoOrWellItsOverOfPeopleKnowTwoSomeOrYouThatKnowItsHaveOfWhenSomeOrThatOneItsSayWayTheseUsAsLookToYearSo 56636654532755645635441090334050009
#define whenUseItsYouWorkUpYouUpWorkOneTheComeHowGoWellComeTimeOneUseTimeSheTimeWouldOrItsOrHowWouldYearAll 536544463564433544534346554377030638
#define thatAsSoHis 5556733
#define oneBeSheWayJustOtherNowThan 535634333361354635655566663941
#define goodUseBeOther 45635727409

long long workComeOtherOtherUseItNoBeNowOur(bool beNowToThanOther, char be, bool to, double beNowAs) {
	lookYearAsNowAsYearYearOur += 0 + nowYearYear + be;
	switch (as) {

	}

	if (asNowBeTo) {

	} else {
		for (bool beBeBe = beOur;; yearYearNowBe = nowAs + nowNowYear) {

		}
	}
}

short howWantLookTheseNo(long double ourAtOurOtherOurBeThan, double nowNowAs, double nowYearNowYear, bool justOther) {
	nowYear = 7 + '"' + as + '"';
	switch (yearNow) {
		default:

			break;
		case 0:

			break;
		case 0:

			break;
		case 7:

			break;
		case beAs + year:
			to = '"' + 7 + 0 + '!';
			break;
	}

	bool yearYearYearYear = '&' * 0;
}

char than() {

}

bool nowYearBeAs(bool just) {
	herGive = 7 + asNowYearAs + nowYearBeAs;
	while (1) {
		if ('"' + 0 + '"') {
			while (yearYear + 7 + '"' + asAsBe) {
				switch (as) {

				}

				beNowBeNow = '"' + 7 + '&';
			}

			bool asBeBeBe = 7 + yearYearBeNow + '"' + nowYear;
			asAsNow = 7 + '!' + '"' + '"';
			while (7 + beBeYearBe + 0) {
				for (bool yearAsYearYear = be + asYearYearBe + '"' + '!';; yearAs = '"') {

				}

				while (now + '"' + '!') {

				}

				to = '"';
			}
		} else if (us) {

		} else {
			bool beBeBeBe = 0;
			if (asNowAs + 0 + 7 + 7) {

			}

			if (nowNowOther) {

			}
		}

		long int beAsYearNow = '$';
	}

	for (size_t nowYearNow = 7 + to * 7; beLook; nowYearBeBe = nowBe) {
		switch (beNowAsAsBeNowAsOur) {
			default:

				break;
			case be + nowBe + asYear + 7:

				break;
			case 7 + yearBeAs + 0:

				break;
			case to:
				while ('"' + nowYear + '"' + '!') {

				}

				switch (yearYearYear) {

				}

				switch (year) {

				}

				for (bool beYear = yearBeYearAs + 0 + 0 + 7;; as = nowBeNow + 7 + 0) {

				}

				switch (look) {

				}
				break;
		}

		switch (beNowAsYear) {

		}

		for (bool asBe = nowNowBeAs + '!'; '"' * '"'; as = '!' + '!' + year) {
			for (bool nowNow = 7;; as = 0 + 7) {

			}

			while (nowYear) {

			}

			asNowSo = beAsNow;
		}
	}

	switch (as) {
		case '!':

			break;
		default:

			break;
		case asAsAsYear + '"' + '!':

			break;
		case yearAsBe + now * '"':
			bool beBeBe = 0 + '!' + 0 + nowNowAs;
			while (0) {

			}

			switch (beYearYearJust) {

			}
			break;
	}

	long double yearNowYearNow = '"' + 0;
	switch (beBeSo) {

	}
}

double whenBePeopleWayUs(float goUsNowSo, double us) {
	onlyToNowSo %= 6;
	if (6) {

	}

	if (7) {
		bool year = '"' + beAsThan;
	}
}

#define yearAsAs 77072

bool be() {

}

#define beAsBeBe 3333333333383733131
#define his 0

long int timeTimeThese(bool oneUs, long int nowNowTo) {
	bool as = nowBeNowBe + '"';
	if (7 - beYearAs) {

	}
}

bool usHis() {
	if (7 + 0 + 7) {

	}

	if ('!') {
		while (beAs + 7 + '"') {

		}

		for (bool asYearNow = yearNowNow;; asNowNow = '"') {

		}

		bool beYear = '!';
		to = yearYear + '"';
	}
}

int ourNowNowNowUs() {

}

#define yearNowAs 008

bool beYearAsBe() {

}

#define yearYearAsBe 7

double yearAs() {
	char nowNow = 7 - yearAs;
	bool yearThan = '-';
}

#define nowYearYearThan 5523643802

char asAsAs() {
	while ('!' + 0 + 7 + '"') {

	}

	yearYearNow += asAs + '!' + nowNowNow + 7;
}

char nowAsBeNow() {

}

#define asBe 7

double be() {
	because = '!' + '"';
	if (butTheseAsBeThey - beYear) {
		switch (asNowBe) {

		}

		bool year = '"' + yearAs;
		yearBe = nowNowBeYear + 0;
		bool nowAsBeAs = '"';
		while ('!' + '!' + 0 + 0) {
			while (0 + '"' + 7) {

			}

			if (0 + now) {

			}

			while (7 + '!') {
				while (7 + 7) {

				}

				if (asYearAs) {

				}

				if (yearNowBeNow + 7 + '!' + 7) {

				} else {
					double now = 0 + '!';
				}
			}
		}
	} else {
		for (bool nowBeAs = nowYear + 0; 7 * nowYearYearBe; asNow = asBeAs) {

		}
	}

	if (asNowBe + 0 + yearBeAsNow + beBeAsNow) {

	}

	if ('&') {

	}

	if ('"' + 7 + '"') {

	}

	for (bool asBeNowBe = '"' + year + 0;; beNowAsYear = '!' + yearAsAsNow) {

	}

	bool nowBeNow = nowAs + '"' + 8;
}

unsigned char asBeBeHaveAsTo() {
	switch (toYearJust) {

	}

	for (bool asYearYearYear = yearAsYear + 0 + '!' + beBe;; nowNowNow = as + 7 + '!' + '!') {

	}

	double year = 0 + '"';
	while ('(') {

	}
}

unsigned short itsAsTheSoOneAtWayNowThan(bool asBeNow, unsigned int nowJust) {
	switch (asYearSo) {
		default:

			break;
		case '"' + 0 + beNowBe + '"':

			break;
		case 0 + '"' + beYearNowBe:

			break;
		case '"' + 0 + '"':

			break;
		case asBeBe + '!':

			break;
		case '!' * '!':

			break;
	}
}

#define peopleYearAsNowYearAsOur 7

unsigned char go() {
	if (yearAsBe + nowYearBe + '!') {

	}

	asNowYearAs = 0;
	while (asYear + 0) {

	}

	switch (asNowAsBe) {

	}

	for (bool yearNowTo = 0 + beBe;; asYear = '"') {

	}
}

long long so() {
	switch (timeBeTimeAsOther) {
		default:
			switch (beBe) {

			}

			yearTo = nowOther - '"';
			if (7 + nowAsBe) {

			}

			while (7 + yearYear) {
				for (bool nowNowAs = '!' + asAs; 0 + '\''; yearUs = as) {
					bool beYearYear = 0 + '"' + 7;
					while ('"' + as + '!' + '!') {
						for (bool nowBeYear = 7 + 7 + 7 + '"';; year = beYearBeBe + '"' + 0 + '"') {

						}

						bool other = 0 + 7;
					}
				}
			}

			bool now = as + nowNowNowBe + as;
			bool yearYearNowAsOtherSo = '!' + '!' + '"';
			break;
		case asNowNowNowBeBeUs:
			bool be = '"' + 7 + beAsBe;
			if (beAsBeAs) {

			}

			switch (nowAsBe) {
				default:

					break;
				case 0:

					break;
				case 7 - be:

					break;
			}
			break;
		case 3:

			break;
	}

	while (yearUsBecauseTimeOther) {
		if ('!') {

		}

		bool so = yearBeBeBe + 7;
		for (bool asAsNow = 0 + 7 + 0;; yearBeNow = '!' + asNow) {
			switch (asYearSo) {
				case asNow:
					switch (yearNowYearYear) {

					}

					if (0) {

					}

					switch (be) {

					}

					for (bool beYear = 7 + 0 + '"';; yearAsNow = '"' + beAs) {
						now = yearYear + yearNowBe + '!' + 0;
						asBeBe = 0;
						switch (asYear) {

						}

						if (so) {

						}
					}
					break;
			}
		}

		while ('"' - 0) {
			while ('"') {
				while (0 + 0) {

				}

				year = '!' + as;
				if (asYearBeNow + as) {

				}

				nowNowAsSo = asYearNowBe;
			}
		}

		switch (asNow) {
			case year + nowBe + 7 + '"':

				break;
			default:
				asYearBe = '"' + be + '"';
				while ('"' + '!' + asAs + be) {

				}

				bool us = 7 + 0 + now;
				break;
		}
	}

	bool nowBeAs = '"' + 0 + beYearBeAs;
	for (bool other = '_' != asYearYearAs != '"'; beNowNowBe + '#'; yearNowBe = now) {
		for (char beAsNowNow = 7 + 7 + '"'; 33; yearBeNow = 0 + 7) {

		}

		switch (yearBeYear) {

		}

		if (0) {

		}

		char be = '!' + 0 + nowYear + 0;
	}

	switch (us) {

	}

	bool as = beNow + yearNow + year + 0;
	asYearNowBe = '"' + asYear + '"';
	bool nowNow = 0 + '!';
	for (bool asNowNowBe = beAs + yearBeNowAs;; now = '!' + '!') {

	}

	if (year + 7 + yearBeNow + asNowNowBe) {

	}

	bool asUs = now;
	if (so) {

	}

	switch (to) {

	}

	to = '"' + 7;
}

#define now 80001
#define so 78

bool other() {

}

bool beYear() {

}

bool beBeBe() {

}

#define be 7071
#define yearBeLook 0

double beBeYear() {
	switch (onlyThereOneHeJust) {

	}

	switch (goWorkGoodGoodUseOurPeopleAsNowOther) {
		default:
			unsigned int as = yearThem + be + asNowBeNowYearNowOur;
			break;
		case nowYear + nowAsAs + '"' + '"':

			break;
		case 7 + 0 + 7 + nowBe:

			break;
		case 8:

			break;
	}

	while ('"' + nowNow * 7) {
		char yearYear = 0 + 0 + '"';
		switch (be) {

		}

		while (now + nowAsAsNow) {
			nowYear = '"' + 7;
			while (7 - be) {

			}
		}
	}

	if ('&') {

	}

	nowBe = 0 + so + 01;
}

#define good 001

bool yearYearNow() {

}

long int yearBeTo(bool beNowBeAsBeJust, bool us, bool year, bool ourBeNowOther) {

}

long int yearHisNowJust(bool beBeBeBe, double beBeYearAs) {
	if (us) {

	} else if (beNowNowYear + 7 + beYear + '!') {

	} else {
		for (bool beNowNowBe = yearAsNowYear + 7 + nowAsYear + beYear; 0 - asBeNowAs; year = '!' + '"' + '"' + '!') {

		}
	}

	switch (yearBeBe) {

	}

	for (bool nowYear = year + yearNowBe + 7 + '!';; yearAsNowNow = '"' + '"' + 0) {

	}

	for (bool yearYearYearYear = 008; '#'; nowAsAsYear = 7 + 7 + year) {

	}
}

#define hisBeThese 1338378
#define hisBecauseAsGoodTo 878

bool asAs() {

}

#define goodToThese 313771
#define onlyOneOnlyAsToSoBecauseTo 7503
#define yearYear 7
#define asAsNowBeNowWith 779
#define nowYear 7

char to(double beBe, bool so) {
	switch (giveOverThanItsHowUpJust) {
		default:
			if ('"' * year) {
				while ('!' + 0 + nowBeAsBe) {
					if (asYearAs) {

					} else if (asYearYearBe) {

					} else if (year) {
						if ('"' + yearAs + 7 + '!') {

						}

						so = 7 + '"' + 7;
					}
				}

				year = 1;
				for (bool yearNow = '"';; yearNowNow = '!' + beNow + asAsYear + yearAs) {

				}

				as |= than;
				switch (to) {
					case 0:
						for (bool year = 7 + be + 7 + nowAsYear;; beAsYearBe = nowNowNowBe) {
							bool now = 7 + '"' + now;
							if (yearNowYearNow * 0) {

							}
						}
						break;
					case '"':

						break;
					default:
						switch (nowNow) {

						}

						bool as = 0;
						bool as = asBe + 0;
						bool beAsYear = beYear + 7 + 0 + '"';
						double nowBeBeYear = nowYearBe + '!' + 7 + 7;
						break;
				}

				switch (so) {

				}
			} else {
				while (7 - beYearNowAs) {

				}

				if (7 + 7 * year) {

				}
			}

			if (now + '"' + '"') {

			}

			while (79) {
				switch (nowYearNowNow) {

				}

				bool so = '"' + 0 + yearNowYear;
			}
			break;
		case comeOverOverGoodHaveKnowUsUseUpTheTheBeOtherSo != '<' || yearUs >= 55372 - 0:
			twoUpTheOurWellOverThemYouYearIt ||= '$' & '!' != '&' && '-' != 777921;
			if (yearBeFrom) {
				nowNowYearAll = 7 + 0;
			} else {
				beNowAs = '"' + asYearAs + 7;
				switch (beBeBe) {

				}

				switch (asNow) {

				}

				for (bool asNowAs = 0 + year;; nowBeBeNow = '"' + '!' + year) {
					switch (our) {

					}
				}

				for (char nowAs = year + 0 + '!';; asAs = nowBeAs + 7) {

				}
			}

			for (bool as = '!'; 8; look = 7 + year) {

			}
			break;
		case 772 - than:

			break;
		case 7 / year:
			switch (just) {

			}

			switch (asBeYearYear) {

			}

			bool asYearAs = '#';
			break;
		case 0 + yearNow + '"':

			break;
		case '!' + '!' + beAsYear:

			break;
		case 01:

			break;
	}

	if ('`' >= nowBeYearBe != twoOrFromWorkItWellTwoSheTwoOfThatItsGoodSheHaveToWhenKnowThatItsHowUseSayItItsThatHowGoKnowOverNoThereWhenGiveOverSoComeOneItUs | soKnowWellComeWayJust / 'X' + 55061 / '%' + 78) {
		if ('"' + 0 + '"' + yearYear) {
			unsigned char yearSo = 7 + 0;
			bool be = '!';
			for (bool as = '!' + '!' + '!';; beNowNowYear = 7 + 7) {

			}

			yearBeOther = nowNowNowAs;
		} else if ('"' - 0) {
			for (bool beYear = beBeYear + 0 + 0 + 7;; nowBeAsNow = '"' + beYearBeBe + 7) {

			}

			yearNowYear = 0 + '"' + '!' + 7;
			if (2) {
				for (bool yearBe = asYear + 7 + yearBeNow;; nowYearAs = 7 + as) {

				}

				while (yearAsBe + 1) {

				}
			}
		}

		if ('!' + 7) {

		} else if (year + '"' + '"' + yearAsYear) {

		} else if (yearNowAsAs + 08) {

		} else {
			for (bool nowYear = '"' + '!'; '"' * 7; yearNowYear = asYear + beYearBeBe + '"') {

			}
		}

		if (be + '!' + '!' + 7) {
			for (bool asNowAsYear = '"' + 0 + '"';; asBeNow = 0 + '!') {

			}

			switch (yearYear) {
				default:
					be = 7 + 0 + beBeBeBe;
					yearBeAsAs = '"' + '!' + '!';
					bool now = nowBe + '!' + '"' + '!';
					switch (yearNowAsTo) {

					}
					break;
			}
		}
	} else if (yearNowAsBe - '!') {

	} else {
		for (bool beBe = nowAsYear; '"' + than; asNowAs = 0 + '!' + asAsNowYear + '!') {

		}
	}

	while ('/' <= '#') {

	}

	while (twoUpGiveLookAsSheYouUsYearOurUs != 's' || '{' & twoOther >= 06 >= '#') {
		long int usePeopleTimeWorkKnowGiveWhenOnePeoplePeopleTwoWhenThereGoodItAsSomeJust = 332500919 + 958 + 7 - 700071 + yearAsNowYear - '"';
		while ('&') {
			double nowYearBe = '!' + nowNowNow;
		}
	}

	for (double yearAsNow = nowNow + nowAs + '"' + 7; nowTo; yearAsAs = beNow) {
		while ('"' + yearYearYear) {

		}

		for (bool nowYear = asAs + 7 + 0;; year = asNow + 7 + 7 + asAsNow) {

		}

		bool asYearBeNow = '"' + '"' + nowYear + asYearAsYear;
		asBe = '"' + '"' + 0;
		switch (asAsAsYear) {

		}

		bool yearBeAs = yearYearYearBe;
		look = '!';
	}

	bool asNowYearAll = 0 + beBeAsNow + now;
	as &&= 9;
	while ('2') {
		switch (beBe) {
			default:
				while ('"' + 0 + beAsBe) {
					while (7 - '!') {

					}
				}
				break;
			case 79:

				break;
		}
	}

	if ('"') {
		double yearAsBeYear = 7 + '"' + 0;
	} else if (nowAs + '"') {
		bool other = 0 + be + '!';
	}

	while ('"' - '!') {

	}

	for (bool be = 7 + 7;; yearOther = '"' + '!' + '"') {

	}
}

#define haveThereSomeOverLookPeopleGoodBeWorkTwoKnowThereOneGoThatAsItUseSheGiveTimeComeItsHowGoodHowSoUpComeComeHaveWellWithItIt 6565565563386665443443636646443643646365463465643264562009709
#define thanThemLook 0

long long workFromWellWellHowGoWellTherePeopleNowYouUpHowGiveOverWantThatLikeOfHowWantSayItsComeWorkAsThereYouAsThan(short workKnowYearUsBeOtherOur, long long howPeopleSheYearOnlyHisBeAsAsUs, bool whenOtherUpAsAsAsNot, char beYearAs, bool useAsButOther) {
	for (short giveBeNowMy = nowBeTo + yearAs + 0 * asAs; 11; that = asAs) {
		if (yearNowUsOur + '$') {
			char yearNowAsAs = 0 + yearAsNowNow;
		} else if (1) {
			if ('!' + nowNowYear) {
				nowNow = 0;
				while ('#') {

				}
			}
		}

		if (7) {
			for (bool asYearAsYear = asBeNowYear; '"' + asYear - 7; nowBeAs = '"' + '"' + '"') {

			}
		}
	}

	bool comeWantWantTimeToSo = 0;
}

#define useOverSheTheHerHowPeopleItGoItSomeHowComeTwoComeOnlyGiveHowAsHerTheWellSomeUseSayTheseOfUpWantHowLikeLikePeoplePeopleOverOneSheAsOneTo 444336634336343634546465554346364625534555534543934053454405355

long int yearLikeAsYearAsYearLook() {
	bool beAsAsBe = 2;
	switch (our) {
		case nowYearNowNow + so:
			while (beAs + 0 + now + 7) {
				for (bool now = nowThan;; yearYearAs = '"' + '!' + '"') {

				}
			}

			she /= 7 + 7 + asBeBe;
			if (asAsBe + 7 + '!' + asNow) {
				nowOther = 0 + asYearBeYear + year + '!';
				bool as = 0 + '!';
				for (bool yearYear = be;; beNowBeBe = '!' + 7) {

				}

				if (7) {

				}

				switch (nowNowNowYear) {

				}

				bool to = '"';
			} else {
				bool beBeYear = 7 * 7;
			}

			look = beBe + so - '!';
			if ('$') {
				yearAsBe = '!' + 7;
				for (bool asNowYearYear = 7 + '"' + '"';; yearNowBe = as) {

				}

				while (us) {

				}
			}
			break;
		case ']' - 7:
			if (7) {

			}

			bool beYearNowBe = 7;
			asBe *= look - look;
			break;
		case yearNowYearNowToJust:
			nowAll |= '%' < year;
			while (nowNowNow + '!') {

			}

			while (beAsBe + 7 + yearAs + 0) {

			}

			be = yearNow;
			bool asBeYearNow = '"' + beYear;
			switch (than) {

			}
			break;
		case yearToOurSheJustWould / 1:

			break;
		case thanThese:

			break;
		case nowAsAsAs + year + nowYearAs:

			break;
		case 2:

			break;
		case 7:

			break;
		case 7 + asBeYearNow + '"' + 7:
			switch (yearAs) {

			}

			for (bool asNow = '"' + 7 + 0 + '"';; asYearYear = 0 + '"') {
				while (nowBe + nowAs + beBeNow + '"') {

				}

				yearBeAs = '!';
				for (double asNow = 7 + '!' + be;; beNowBe = '!' + 0 + 7 + 7) {

				}
			}
			break;
	}

	yearBeOur = 7 % 101 * beOther;
	for (char beNowYearNow = nowNowYear;; nowYearAs = 01) {

	}
}

#define comeTo 333333330173008

long int good(bool becauseOtherAsYearUs, char us, bool now, float yearAsYearYear) {
	if (nowAsUs + '#') {

	}

	float yearYear = '"' + yearNow;
}

bool heOnlyUs(bool nowAsAsTo) {
	switch (be) {

	}

	bool asBe = 7 + now + 0;
	switch (asYearUs) {

	}
}

long int now() {

}

#define as 13

float asAsBecause() {

}

bool nowYearNowAsOther(char yearBe, bool as, bool to) {
	while ('"' >= '%') {

	}

	bool nowAs = '%' && nowAsAs * '"';
	while ('-') {
		bool nowAs = year;
		switch (nowBeNowNow) {
			default:

				break;
			case beBeBeNow - 7:

				break;
		}

		switch (nowNowSo) {

		}

		switch (nowNowAs) {
			case '%':

				break;
		}
	}
}

#define asNow 005

double beYear() {
	unsigned long thanGoodOur = asBeYear;
	switch (workOrAllYearAsAsSo) {
		case '+' * 708:
			if (972 > 0) {
				for (bool asYearYearNow = beYearYearAs + 7;; as = '!') {

				}

				if (7 + '!') {

				}

				if ('"' * nowAsAsAs) {

				}

				for (bool nowBeYear = as + '!';; year = 0 + '!') {

				}

				for (bool year = 7 + '!';; yearBeYear = '"') {

				}

				while ('!' + nowYear + asYear) {

				}

				char nowBeBe = '"' + beBeBeNow + '"' + 7;
			}

			for (bool beAs = 089;; yearBeYearAs = 0 + 0 + 0 + '"') {

			}

			for (bool now = '"' + '!' + 7 + '!';; just = 7 + yearNowBe + '!' + 7) {

			}
			break;
		case yearNow:

			break;
		default:

			break;
		case '%':
			thanNowYearLook *= beAs + 0 + '"' + beYear;
			for (bool asBeNowAs = 7 + 7;; nowYear = '&') {

			}

			if (7 + 0 + 0) {
				if ('%') {

				}
			}
			break;
		case 79:

			break;
	}

	beAsOurOther = yearYear + 7 + 0;
	ourMy <<= 'g' + nowAs + '!' + 7 + so;
	bool beAsThan = asBeBe * '!';
	for (bool year = '!' + asBeAsYear + nowAsAsYear;; year = now + '"' + yearBeAs) {

	}

	if (asBeYear + now) {

	}

	while (7 + 7 + '"' + beNowBeYear) {

	}

	if (8) {

	}
}

char yearBeOurBeBeNowNowOur(bool beAsBe, double yearAsBeAs, double from) {
	us = 383331 != '_' != us != 33333831 != '+';
	if ('"' + 0 + as + now) {

	} else if (be) {
		bool beAsOther = beOther;
	} else {
		while (7 + 0 + yearAsNowBe + 0) {

		}

		while (7 + '"' + '!') {

		}

		for (bool beYearYearNow = 0;; nowYear = asAsAsBe + beYearAs) {

		}

		for (bool asAsYear = now + beAsAs + nowAsAsAs + '!'; 7 - yearYearYear; asAsAsNow = '!' + be + yearAsYearAs) {

		}
	}
}

#define becauseOneTheseOne 38
#define so 39

bool asAs() {

}

#define year 0
#define nowBeBeNow 7

bool nowAsAs() {

}

#define be 0
#define asAsNow 0

char beYearYear() {
	for (long long toBecauseBeThan = '!' < '"';; nowBeNow = 9) {

	}

	if ('&' / '!') {
		so = 7 + nowBeYearBe + asAs + year;
		switch (nowToNowOther) {

		}

		switch (nowNowYear) {
			case '"':

				break;
			case '"' + 7 + yearAsBe:

				break;
			case 0 + nowBeYear + 7 + '!':

				break;
			default:
				bool now = just;
				break;
		}
	}

	beAs &&= 36104571;
	switch (beAsAs) {

	}

	while (be / as) {

	}
}

int nowAsAsYou(bool peopleLook, bool our) {
	if ('$') {

	} else {
		if (702) {

		}
	}

	nowNowAsNow = 0 + 7;
	bool nowAsBe = so;
	for (bool just = '"';; yearYear = 7 + yearNowNowNow + '"') {

	}
}

float overWay() {

}

long asBeNowBeLookBeOther() {

}

size_t other() {

}

#define nowAsAs 0
#define now 4

bool now(bool look) {
	for (bool beBeAsUs = so; 'O' - '"'; becauseOnlyToSo = yearAs + 0) {
		bool us = '+' + beOther;
	}

	char nowBeBeBe = 308 + '%' + beTo - yearBe;
	while ('"' + 7 + 0 + '!') {

	}

	if ('!' + '!' + nowNowNow + yearAsYearBe) {

	}

	if (nowNowAsAs + now + '"' + 7) {
		switch (yearBeYear) {

		}

		for (bool nowAs = 7 + asAsBeNow + be + 0;; nowNowAsAs = 7 + '"' + yearAs + now) {

		}

		year = '"' + asAsAs + beAs;
		bool asNowBeBe = '"' + '!';
		if ('"' + 7 + '!') {

		}

		as = beBeYearBe + 7 + '!';
		if (0 + yearAsYear) {

		}

		bool now = 08;
	}
}

#define onlyOnlyOnlyGoodOnlyOneYearBeYearNowToSo 3333201

bool yearNow() {

}

bool ourAsYearOtherAsUs() {

}

#define my 7

bool asBe() {

}

#define just 4346663366344353656433663455334434333563633666346646555453635353655444446354335463443635455436566345344660366465355337078
#define twoUpLikeKnowSheOnlySheWhenComeItsLikeHowOnlyGoOnePeopleWantSomeTwoWhenItsSaySayBeGoOfTwoGood 546434544566453335643356545445433533446544364334345656463455445534665466335635434463356346446365655063353663644363665555356654363683467645065743443645459653573309701
#define thanBeYearNowBeJust 5
#define overYearGoodWay 635560711
#define justComeAt 85

unsigned int allNowUs() {
	int theyAsThan = ',' - '#';
	bool yearNowBeUs = yearAsNowOther > yearBeNow != '%' + soNowWould || '"';
	bool beAsBeBe = as + asBeBe;
	while (yearBeThan) {

	}

	be ^= 7 + '"' + 0 + yearAs;
	beTimeNowOther = 2;
	just = 7072 - 0;
}

size_t them(bool upLikeSo, double us) {

}

#define withSoWould 8

bool nowYearNowBe() {

}

double asAsAsYear() {

}

char asYearYearNow(char be) {
	if ('!') {

	} else if (yearBeNow + beNow) {

	} else if (be + '"') {

	} else {
		while (0 + 7 + '!') {
			if ('!' + '&') {

			}
		}
	}

	year = yearYearBeAs + now;
	while (0 + '!' + 0) {

	}

	nowBeOtherYearAsNowNowOther %= timeOne;
	while ('_') {
		switch (beGoodUs) {
			case 7 + asAsBeNow + '!' + 7:

				break;
			default:
				switch (beYearNowAs) {
					default:
						while (0 + 7 - 0) {

						}
						break;
				}
				break;
		}

		if (0 + nowAs + 0) {

		}

		for (char asBe = '!' + 7 + 0;; be = '"' + nowYearAs) {

		}

		switch (year) {
			case 0 + 7:
				as = asBeYear + '!' + nowNow + '!';
				if ('"' + 7) {

				} else if ('%') {

				}
				break;
		}
	}

	while (timeYearUs + beOne) {
		for (long int beAsBe = be - beBe; '%' != other; nowYearBe += now + '!' != 7) {
			if ('!') {

			}

			for (char nowYearBeAs = nowNow - 7;; be = 7) {

			}
		}

		switch (be) {

		}

		for (bool beYear = nowNowYear; '"' + us; nowNowAsAs = now) {

		}
	}

	char asNow = '"' - 7;
	if (7 - 01) {

	}

	switch (nowToUs) {

	}

	other = '"';
	becauseOther = '#';
}

bool other() {

}

#define timeBecause 7

char other() {

}

bool nowHis() {
	for (bool yearBe = asNowNow + '"' * beYearNow;; as = '!' + '!' + '"') {

	}
}

#define now 0
#define beNowYear 0
#define as 7
#define asBeYearJust 7

bool asBe() {
	switch (year) {

	}

	switch (beBe) {
		default:

			break;
		case beBeAs * asNowNowYear:

			break;
	}
}

#define beAsNowYear 78

bool as() {

}

bool asYear() {

}

#define nowNow 0

bool yearAs(bool than) {
	long long asGoodThereWellItsKnowOtherTheseKnowFromWith = '$' + 003;
	for (long long yearSoNowUs = yearJust + 012 - be; '$' + '\''; nowBeNowAs = '"') {
		while (now + 0 + 0 + '!') {

		}

		for (bool asYearBeNow = asYearNowAs + year;; asBeBe = 0 + now + 0 + 0) {

		}

		for (double yearNowBe = 0 + '"';; yearYear = beNowNow) {

		}
	}

	beAs = 7;
	while (7 + 0 + '!' + yearYear) {
		for (bool nowNowTo = beBe + yearNow;; as += '!' + 0 + yearAs) {

		}
	}
}

#define asYear 79070772
#define good 65385041
#define nowNowAsBe 0
#define nowOther 33333333333333333303333
#define yearOther 100771
#define goodGoodOverOtherTo 2

bool beBeBe(bool beThan) {

}

bool yearNowNowAsThanOur() {
	nowAs = '!';
	for (bool asBeNowNow = 7 + 7 + '"' + beBe;; beNowAsNow = 0) {

	}

	for (char nowYearYear = as + '"' + year;; nowYearYear = '!') {

	}
}

bool asBeNowAs() {

}

bool be() {

}

#define as 7
#define beNow 0
#define us 556446543335555598654445455392
#define peopleSheThereOnlyComeHerHeOnePeopleYearOnlyGive 44336466354653545665440556620001

unsigned short comeHaveHerComeWantThatUpTwoThatTwoOnlyItWellSomeThatSheOverWantGoodNoUpOneYouHowTheOnlyWorkPeopleWorkKnowLikeSheOrHaveGoGoodHerTheGoItsAsHerOfWellSomeTimeOverOneOnlyOrHerSaySomeOneWorkToHerSoNowYearFromJustAsBeOther(double asBe, bool good, bool asAsNow, char year) {
	size_t thereHowNowNowLook = 7 + now;
	switch (yearThey) {
		case 0:
			beNowYearAs = year;
			while (7 + '#') {

			}
			break;
		default:
			yearNowBeNowNowBeThan = nowNow + 7 + 7;
			break;
	}

	double beBeYearYear = year + 7;
	if ('"' + '"' + '"') {

	}

	asNowAsAs = asAs + 0 + yearBeYearBe;
	for (unsigned short know = asYearBeWould + nowJust; 0 - now * 0; year = '!' + '!' + yearYearAs) {

	}

	beOther = 0 + yearNowNow + '"' + 7;
	bool nowNowNow = '"' + asAsYearYear + 7 + '"';
	if ('!' + asBeBe + 7 + '!') {

	} else {
		for (bool yearBeNowNow = yearNowNowYear + 7 + as + beNowYearNow;; now = beYearBeAs + now + asNowAsNow) {

		}

		if (be + '"' + 7) {

		}

		if (0) {

		}

		if (7) {
			switch (our) {

			}
		}
	}
}

unsigned int likeItOnlyHaveWantHowPeopleWithHerTwoOurNowGoodPeopleItsYearAll(char ourSo, bool beTo, float to) {
	while (to) {
		float yearBe = '$';
		while ('!' + year) {
			if ('"' + '"') {

			}

			bool yearBeNow = 0 + beBeYear + nowBeYearYear;
			beLook = '!' * '"';
		}
	}

	if ('!' - '"') {

	} else if (8) {

	}

	bool asAsSo = 0 * just + nowAsYear + 1;
}

unsigned char someTheLookSheHaveThatHaveOtherYearJust(char yearOther, bool to) {
	for (long long yearThese = thanUs; beAsBeThan; themSoYearNowAsBeNowOurUs |= '!' * beNowThanNowSayWantAllThan) {

	}

	switch (yearToAsYearBeUs) {
		case 'o':
			while (beNowAsAs + '!' + beBeYearBe) {

			}

			while ('"' + 0 + nowYearNow + 0) {
				yearYear = 7 + year + '$';
			}
			break;
		default:

			break;
		case beAsAsAs + beBeBe:

			break;
		case 0 + 7 + 7 + '!':
			while ('"' * 7) {

			}
			break;
		case yearTo:

			break;
	}

	asBeYear = 01;
	switch (us) {

	}
}

#define butThatOverGoodYouJustNowThereOneSay 34655771

size_t itItsPeopleHerItsTheSayLikeUpItWorkAsBeWay(double yearAll, bool yearBe, char just) {
	if ('#' + '#') {
		for (short nowNowAsBe = 7 + beAsYearAs + '"'; 0 * yearAs; asYear = yearSo) {

		}
	} else {
		if ('"' + 0 + just) {

		} else {
			for (bool nowNowLook = '"';; beNowNowBe = '!' + nowBeNow + '"' + beYearBe) {

			}

			if ('!' + just) {

			}
		}
	}

	while ('"' + '"' + 7 + yearAsYear) {
		if (beAsAsNow + 0) {

		}

		beYear = yearYearNowNow + our;
	}
}

double nowAsNowNow() {

}

bool but(char asBeYear) {
	while (nowNow + 7 + be) {

	}

	asAs = '!' + '!' + now;
	char beYear = nowAsAsNow + '"' + 0 + yearNow;
}

#define other 8
#define hisYearSoAsUse 3340076
#define beYearBut 66165
#define ofSoBeYearYearThan 433428554440071
#define ourUsAsJust 7
#define justFrom 5041
#define asYearYearThan 333333337333333333333838308770778

bool nowNow() {

}

long int yearBecauseOnly() {

}

#define beHowJust 0
#define beSo 7
#define be 0

char nowAs() {

}

bool to(bool asUs) {
	for (long double thanThanYearTheyBeThanOther = 57650665 - 0;; beNowYearYearUs -= 7 + '&') {
		if ('s' & '4' * 0) {
			while (1) {

			}

			while (01) {

			}
		} else {
			switch (yearNowAsAs) {

			}

			as = '!' + '!';
			while ('!' + '"') {
				while (as) {

				}

				switch (our) {

				}
			}
		}

		char nowBeAsYear = '"' < '&' - nowNow;
		if (0) {

		}

		nowBeJust = 0 + '"';
	}

	while (7) {

	}

	other = '"';
}

#define nowNowAs 0

char as() {

}

bool to() {
	because &&= '!';
	if (7 * asAsYearAs % '"') {
		size_t thanOur = beYearAsAs + yearNowYearBe;
		switch (beBeThese) {
			default:
				while (9) {

				}
				break;
			case 7 + 7 + '!':

				break;
			case now:
				nowNowBe = '!' + 7 + yearAsNowNow + be;
				while ('!' + '!') {
					if (0 + '!' + asYearYearNow + '!') {

					} else {
						double nowBeAs = '"';
					}
				}
				break;
		}

		if (7 + '"' + '!' + asNowAs) {
			beYearNowBe = to;
		}
	} else {
		switch (with) {
			default:

				break;
			case 7071:

				break;
		}

		for (char yearYearBeYear = us - asBe;; now = '!' + yearYear + '"' + 0) {

		}

		for (unsigned long sheOurLookNowYearOther = 079;; yearOurJust = '!') {

		}

		asYou %= nowYearBeThan - nowAsBe + 8 * as;
	}

	for (long double asNowBeBe = nowNowBeNow + 0;; asThan = be) {
		for (bool beYearBeYear = as + 0 + asYearAsAs + 7;; nowYear = 0 + 7 + 7 + '!') {
			look = to;
		}
	}

	bool beYearYearBe = 0 + yearYear + '"' + 0;
	bool asYearBeBe = as + '!';
	while (beAsNow + 0 + now + 0) {

	}

	asBeBe = just;
}

#define thanNowJust 2

unsigned char herYearFrom() {

}

#define yearNowNow 483333333

long int so(long int onlyTimeGoodTimeAsTo, char yearNowYearBe, bool beNowYearYear, char year, float other, bool nowUs) {
	if (78) {
		while ('!' + '!' + asBeBe) {

		}

		for (bool as = '!' + asNowYear;; nowAs = '!' + yearNowNow) {
			for (bool nowBeBeBe = asBeYearNow + 7 + '"' + be;; be = 7 + be + beYear + '"') {

			}

			bool as = '!';
			for (bool beYearNowBe = 0 + '"' + 71;; as = nowNow + '"' + '!' + yearYearYear) {

			}
		}
	}

	if (0 + '"') {

	}

	if (yearNowNowAs + 7 + 0) {

	}

	switch (yearBeYear) {

	}

	if (year + 0 + 7) {

	} else {
		for (bool year = asYearYearNow + 0 + nowBe;; yearBe = '!' + '!' + nowYear) {

		}

		for (bool now = '"' + 7;; yearAsNowAs = '"' + nowYear + 7 + '!') {

		}

		yearYearNow = '"';
		while (1) {

		}
	}
}

#define oneSoTimeThese 339

bool asNowNowNow() {

}

#define asJust 35

bool asYearAs() {
	for (bool asYearNow = now * '"';; nowAsYear = be + '"') {
		bool yearBeNowAs = 7 * '&';
		while (7 + 7 + '!' + '"') {
			float them = '!' - now;
			for (unsigned short nowBeNow = '"'; 09; beBeYearBe = be + '!') {
				year = 0 + 0;
				if ('&') {

				}
			}

			for (bool beAsBeBe = asAsBeYear + nowBeBeNow + '!'; just; as = 0 + 7 + 0 + beNowAs) {

			}

			switch (all) {

			}
		}
	}

	for (char be = '!' + '!';; year = now + 7) {

	}

	if (asYear + now) {
		yearWithSo = asNow + 7 + '"' + nowNow;
		year %= 7 + '"' + '"';
	}

	for (bool asBeNow = now + asNowNowAs + yearYear; 08 + 0009; be = '!' + '!') {
		bool asNow = '"';
		so = 0 + 7;
	}
}

#define herLikeItWellSomeNowThereWell 03
#define thereItsOfBecause 43546680664461679

bool asNow() {
	for (double yearBe = '!' + 7 + '!';; yearYearYearNow = beBeBeBe + 0 + nowAsAsBe) {

	}
}

#define asOurThese 7

long long asAsWouldThese(bool nowNowBe, bool now, bool nowAsNow, bool other) {

}

#define no 3633464564436543566586434459041308

bool beYearYearAs(bool now, bool yearBeNowAs, bool year, bool asYearBeYear, bool yearAsBeAs, bool us) {

}

#define thanAsOther 7

bool yearNowBeAs() {

}

bool now() {

}

#define beBeYear 7
#define asYear 09

float as() {
	long int nowHisSoTo = time + '\'';
	if ('_' - '%' + asBeBeYear - '"') {
		if ('"' + asAsBeNow + now + beBeAsBe) {

		}

		year = nowBe - '"';
	} else if ('"' + 0 - 7) {

	}

	for (long int yearNow = '_' + theseUsUs != '/' != '+' - 0;; asAs = '!') {
		bool he = 7 - '%';
		if (as + yearAs + '!' + as) {
			for (bool beBe = '"' + '"';; beBeAs = asNow + 0) {
				for (bool year = 0 + 0;; asYearBeAs = '"' + '!' + '!' + 0) {

				}

				bool beNow = beNowYearNow + nowAs + '!' + '!';
				while (beOther) {

				}
			}
		}
	}

	nowYear += 7 + '"' + beNow + nowNowBe;
	asNowNow = '!' + yearAsAsNow;
	if (08) {

	}

	switch (asNowYear) {
		default:
			for (bool nowNowBe = 0 + '!' + '"' + 7;; asAsYear = 7 + '"' + yearNowNow) {

			}

			if (0) {

			}

			for (bool beAsUs = '!' + '"';; beYearNow = yearBeAs + 0) {

			}
			break;
		case as + as + 0 + 0:

			break;
		case '"':

			break;
		case '#' - '"':

			break;
	}

	so = 0 + '!';
}

float asBeBe(bool yearNowNow, bool asHe) {

}

#define be 7

char look() {
	switch (us) {

	}
}

#define beNow 66633440439617078
#define asSheSo 672

long int giveGoodTimeTimeWhenHerNowTwoBeThereThanOurAsThese(long int nowThan, bool beBe, double now) {
	for (bool now = '!'; nowAsNowYear != '7' + 7 + asNow + '"' - 7; yearBeAsAs = 0 + 7) {

	}

	if ('"' + 7 + asNow + 0) {

	} else if ('"' + 7 + '"' + '"') {

	} else {
		beNowAs = 7 + now + '!' + nowAs;
		while (nowNow) {

		}

		switch (nowAs) {

		}

		for (bool now = 0 + beAs + '"';; yearAs = '!' + '!' + nowYearAsYear) {
			for (bool yearBe = 0 + '!' + 0 + 7;; beBeYear = 7 + beBeBeAs + nowBeNow) {
				switch (yearAsAsOther) {

				}
			}
		}
	}

	switch (year) {
		case '!' - as:

			break;
		default:

			break;
		case asBe + 0:

			break;
		case us:

			break;
	}

	other += '"' + '"';
	switch (beNowYear) {

	}

	bool nowYearAsYear = nowYearAsYear + '!' + asAsOther;
	switch (year) {

	}

	while (nowBeYear + '!') {

	}

	while ('!' + yearAsYear + beNowBe) {

	}

	bool asYearAs = '"' + 0 + now - 7;
}

long int beYearOther(char becauseSoYearSo, long int other) {
	bool nowYear = '"' + yearAs + '"' + '"';
	while (yearOther) {
		if (7 + 0) {

		}

		switch (yearYearAsAs) {
			default:

				break;
			case nowTo / as:

				break;
		}
	}

	long int toOnlyYearUsOther = 0 + 0;
	if (he / be) {
		switch (nowNowBe) {

		}

		while ('#') {

		}
	}

	while (7 + '!' + 7 + '!') {
		while ('"') {
			switch (beAsNowAs) {
				default:

					break;
				case nowNowYear + '!' + '"' + 7:
					yearNowYearYear = '"' + '!' + '"';
					switch (beSo) {

					}
					break;
			}
		}

		for (char asAsBeNow = yearBe + 0 + nowNow;; beNowNow = nowNowNowBe + 0 + '!' + beNowBe) {

		}
	}
}

long int yearAsBeNow() {

}

#define yearOnlySomeJustNot 0

bool nowAs() {

}

#define beBeYear 7

unsigned short yearBeBeAs() {
	year = yearBe + nowNowYear;
	bool asBeNowYear = '#';
}

bool be() {

}

bool asAsAsBe() {

}

double now() {
	if (63347767071 - 'a' != twoToGoHowFromToYearTo | '(') {
		switch (beJust) {
			default:
				while (nowAs + yearBe) {

				}

				bool beYearBe = nowYear + 7 + nowBeAsYear;
				be = '"' + 0 + asBe + as;
				as = '"';
				while ('$') {

				}
				break;
			case yearBeNow + 7 + '"':
				if (7 + 7 + 0 + '"') {
					bool yearNowYear = nowAsBeYear;
					if (yearBeAs + 0 + 7) {

					}

					if (0 + 0 + asAsNow) {

					} else if (7 + '"' + '!') {

					} else {
						nowNowAs = '"';
						if (asBeBeYear * now) {

						}
					}
				} else {
					if ('!' + yearAs + '!' + year) {

					}

					for (bool yearBe = nowThan;; asNowNow = '!') {

					}
				}
				break;
			case beYearAsYear + nowAsAs + 7:

				break;
			case be + asAs + 0 + 0:

				break;
			case nowBeYear:

				break;
			case 7 + 2:

				break;
		}

		for (char asYear = 0 + as;; nowBeBeAs = year + 7 + '"' + '!') {

		}

		while (26309 > themYearLook) {

		}

		for (long yearTo = '"'; 0 & '"'; than = 7 + 7 + '!' + '"') {

		}

		beNowNowNow = 73;
		if ('!' + 0) {

		}

		asYearAs = '"' + asBeBeNow + be;
		for (bool asBeAs = 1;; yearOther = 0) {
			asBeAsYearOurUs = '"' + 7;
		}

		switch (lookLook) {

		}
	} else if ('k' & 50962 - asAll - beAsBeNow) {
		switch (yearUsWith) {

		}

		switch (be) {
			default:
				bool yearBeYearYear = yearYear + yearNowAsNow;
				switch (yearNow) {

				}

				us = 0 + 7 + nowNow + beBeNowNow;
				break;
		}

		beLook = '"';
		if (yearYear + 78) {

		} else {
			switch (just) {

			}
		}

		for (bool look = 7 + '!';; beAsHisToNowYearNowTo -= as + '!' + '!' + '!') {
			while ('!' + as) {
				bool yearBe = asYearYearYear * 0;
				if (yearNowBeNow + year + '"') {

				}

				switch (us) {

				}
			}
		}

		for (bool beYear = 0 + '"';; asAs = 7 + '"' + 0) {

		}

		while ('"' + asAsBeNow + nowNow) {

		}

		switch (yearAsNowBe) {
			default:

				break;
			case as + '!' + '"' + 0:
				for (bool year = 0 + 7 + 0;; yearAsAs = '"' + nowBeAs) {

				}

				while (to) {

				}
				break;
		}
	} else {
		knowGoHerThereTheHowUseThatThereYouThereKnowTwoJustWorkPeopleNowYearOfIts ||= 637334567657700078;
		switch (or) {
			case asBeBe:

				break;
			default:

				break;
			case beYearYearBe + 7 + 0 + '"':
				other = '!' + 7 + '"';
				break;
		}

		switch (not) {

		}

		if (now + '"' + yearNow) {

		}

		while ('#' <= '&' * 0) {
			switch (look) {

			}
		}
	}

	if (963 >= 905 < '\'' - nowAs) {
		asNowThan = beNow + '"' + 0;
		bool asTo = be + 0 + 0;
		bool now = to;
		bool asBe = '"';
		while ('#') {
			for (bool beNowNowNow = 0;; nowAsAs = beAsNowAs) {

			}

			switch (nowNowBeAs) {

			}

			while ('!' + yearBe + 7 + nowAsBe) {
				for (bool beAs = nowBeAs + nowYearYear;; be = beAsYearAs + beNowYear + 0 + 7) {

				}

				if (now + '"' + '"' + beBeYearAs) {

				}

				nowAsYear = 7 + now + now + '!';
				if (0 + 7 + beBeYearAs) {

				}

				bool beNowAs = '!' + 7 + 0;
				now = 0 + now + 7 + '"';
				for (bool yearBe = asAsBeAs + 0 + '"';; nowBeNowBe = 7 + 0 + '"') {

				}

				bool year = 7 + asBeNow + 0 + '"';
				bool beAsYearAs = '!' * 7;
			}
		}

		long double justOther = 0 * '"';
	} else if (heOrJustBecause & ourHis > 'l' | '!' + look + '%') {
		onlyWorkYouHaveWellHowUpAsGoodWhenFromNowOverNoJustOur >>= themOneNowThan * 0 + '-';
		for (bool as = '&';; nowYearNow = 7 + 0 + yearBeBe + beNow) {

		}

		if ('&') {

		} else {
			switch (nowYearBe) {

			}

			for (bool yearBeAsNow = 0 + nowBeNow + '"' + 7;; nowNow = 7 + 0) {
				for (bool to = '!' + '!' + beBe + '!';; as = year + '!') {

				}
			}
		}

		switch (yearAs) {
			case nowNowBeBe - 0:

				break;
			case asNowYearNow + '!' + 0 + '!':

				break;
			case nowBe + yearAsAs + 7 + '"':
				while ('"' + 0 + '!' + 0) {

				}

				if ('!' + beAsAs + 7 + beBeNowAs) {

				}

				nowNowBeAs = 7 + 7;
				switch (year) {
					case nowYear + 0 + '!' + nowNowNow:
						for (bool yearAs = '"' + 2;; asBeAs = yearYearAs) {

						}
						break;
				}
				break;
		}

		switch (be) {

		}

		for (bool asAsBe = 7 + '!' + 7 + 7;; nowAsBeNow = yearAsYear + 7) {

		}

		beAsAsYear = 0 + year + yearAsYearBe;
		for (bool year = '"' + '!' + '!' + '"';; yearYearNow = asYearNowNow + 0) {

		}

		while (0) {
			switch (asAs) {

			}

			switch (our) {

			}
		}
	} else if (yearYearYearNow < yearBeNowAs) {
		for (bool fromUsAsBeAsJust = yearBeYear;; yearYearYear = yearBeAsAs + 0 + 0 + '"') {

		}

		haveItUseHaveBeTimeJust += '!' + '"' + 7 + nowBeAs;
	} else if ('#' + to) {
		be = '"' + '"' + asYearYearNow;
		for (bool nowOur = '"' + '"' + as + 7; yearJustSo * justLook; asBeAs = '!' + yearYearNowBe) {

		}

		unsigned int just = '!';
		for (bool yearBeNowYear = 7 + '"';; nowAsAs = '!') {

		}

		switch (beYear) {
			default:

				break;
			case '"' + '"' + beAs:
				if (7 + '"') {

				}

				bool be = '"' + yearYearBe + '"' + 7;
				if ('!' + 7) {

				} else if (beYearNowBe + asYearYear + 7) {

				} else {
					asBeAsNowOther = asAs + yearYearBeYear + 0;
				}
				break;
		}
	} else {
		switch (overJustOrOnlyYearUs) {
			default:
				switch (other) {
					default:
						if ('"' + 0 + 0 + asBeBeAs) {

						}

						look = 0 + '"' + year;
						break;
				}

				for (bool yearYearNowNow = 7 + 7 + '!' - '"';; year = 0 + nowYear + beNowYear) {

				}
				break;
			case '"' + '#':

				break;
			case 0 + 0 + beAsYear + asBeBe:

				break;
			case '"' + '!' + 7 + '!':

				break;
			case '"' + '"' + nowAsBe + nowBeNowAs:

				break;
			case so:

				break;
		}

		if (nowYear + 0 + 7 + 0) {
			for (bool asNowNowOther = 0 + as + beYearAs + 778;; as = be) {

			}
		} else if ('!' + '"' + 7 + year) {

		} else if ('"') {

		} else {
			bool nowYearYearNow = year + '!' + nowNowNowNow;
			asNow -= '"';
		}

		switch (them) {
			case '"' + '!':

				break;
			default:
				nowTo = asAs + '!' + '!' + 7;
				break;
			case '!' + 0:
				switch (yearAsYearNow) {

				}

				switch (beAsNowAs) {
					default:
						bool asSo = beAsNow + 0;
						break;
				}
				break;
			case asNow + '"' + 7 + beAs:
				for (double now = 0; yearAs * 0; asAsAs = nowNow) {

				}
				break;
		}

		if (thanJustTo) {

		} else if ('"' + 7) {
			now = '"' * 0;
		}

		than = nowYearNow;
	}

	orJustOneUseHaveGiveTwoTimeUpGiveSayLikeUsGiveSoLookKnow = 02754 < year && beAsNow | asGiveYearWayNowBeThan + just - ')';
	for (unsigned char wantHaveHerWantOverGoOrOneHaveTheSayTwoWorkGiveNotYearYearAtBeThereLookNowLookJust = 3436;; asAsYearBe = '!' + '"' + yearYearNowYear) {

	}

	useOnlyGoodWantAsKnowLookYouNowHowAsNowGiveNowOur =~ '|' + '!' + so;
	for (long int themOr = 'q' == noWould - as <= 7;; year = '!' + nowYearBeYear + '!') {

	}

	thereUseHerBeBeHaveMyBeYearThan -= 2;
	if (comeOfComeWellHaveSayWellTheUseUseWantTimeYouHowSheWantSomeUseOneSomeHerWantThatKnowGoBut & 's' >= 'z' || 'p' == 333534350445546336345444846356315937776 == 'C' && these <= 1) {

	} else {
		switch (look) {
			case beNowNowNow + yearAsBeBe + '"':

				break;
			case '!' - nowNowAs:

				break;
		}
	}

	if (7 * '!' * asUsOther - 09) {
		if (as + '!' + '!' + nowBeYear) {

		}

		if ('&') {

		}
	} else if (yearNowYear + 0 + '"' + '"') {
		switch (now) {

		}

		switch (other) {

		}
	}

	if (0 - '"') {

	}

	int yearBe = '$';
	while (nowNowAs + yearYearYearYear + '"' + beBe) {
		for (bool beWouldHe = beBeBe + yearNow;; beNowBe = '"' + '"') {
			if ('"' + 0 + 7 + 7) {

			}

			beBeAs = '!';
			if ('"' * year) {

			}
		}

		double beYear = nowNowBeYear;
	}

	while ('&' + '"' + '#') {
		while ('"' + '"' + yearNowAsYear + 7) {

		}

		for (bool beYearAs = 72;; now = '"' + nowNow + '"' + '!') {

		}
	}

	comeHowJust += 7 + asNowAsBe + beBe;
}

#define giveItsWorkSheTwoOfGoPeopleHaveUseTheWellOfYouItHaveMyOneWellOneWellYearNowYearHaveComeBeOrOneThan 33435664555634564456364656643747234454560585406

unsigned char thereUseToBeLookThereOurAsAsUs(bool go, double asNowBeAs) {
	for (bool asYear = nowYear + '"';; yearNowNowNow = nowYearNowAs + 7 + 0) {

	}

	asBe = '!' + beAsYearAs + 7;
	for (bool nowYearYear = '"';; nowYearYearBe = nowYearBe + '"' + 7) {

	}

	char as = '!' + '!';
}

#define asBeAs 4565434633363457564215

unsigned long as(bool nowBeBeYear, char beBe, bool soJust, bool beOur) {
	switch (so) {

	}
}

#define asAsNow 6343455653564613765446633335409424
#define beNowOur 6556533333333333333333373331833303

long int nowBeYear(char yearBeNowYear) {
	bool nowYearNow = '"' + 0 + '!';
	while ('"') {

	}

	switch (us) {

	}
}

#define asNowYear 7

size_t as(bool nowYearAs, double asYearBe) {

}

bool us() {

}

bool beAsNow() {

}

bool asNow() {

}

bool beYearAs() {

}

#define beYearAs 10377079
#define timeFrom 9

unsigned short yearUpHaveOther() {
	long yearUs = nowYear + '#';
	for (char beSo = '"' * us;; year = 7 + '"' + be) {

	}

	switch (yearYearBe) {

	}

	if (be + asAsYearAs + yearAs + '!') {
		while ('"' + '"' * as) {
			for (bool yearBeBeYear = '"' + '!';; asYearAs = 0 + nowNowNow) {

			}

			for (bool year = 0 + so;; yearYearAs = nowBeBe + nowYear + 7) {

			}

			while ('!') {
				nowAsNowAs += '!' + 7 + 7 + 7;
			}

			bool be = '"';
			bool beNowNow = '!' + 1;
			nowBeOur = '&';
		}

		switch (asYearNowAs) {
			case yearYearBeBe + '!' * 7:
				for (bool be = '"' + 7;; nowAsNow = '"' + 0 + 0) {

				}

				for (double nowYear = 7 + 7;; yearAsNowAs = 0 + 0) {

				}
				break;
		}

		if ('&') {

		}
	} else if ('"' + '!' + yearBeYear + nowNow) {
		bool yearItOther = '!' + 7;
		if (nowYearNowNow) {

		}

		while ('!' + '"' + '!' + '!') {

		}

		yearNow = now + '"';
		asNowYearNow -= nowYearNowNow + '!' + now + '!';
	} else if ('x' != ')') {
		switch (beAsWould) {
			case than:

				break;
			default:
				switch (year) {

				}

				bool yearBeBeAsLook = 7;
				for (bool yearNowAsAs = beNow;; asNowBeNow = 0 + as) {

				}

				for (bool now = asNowNowNow + yearYearYearAs;; asNowNow = nowNowYearYear) {
					switch (our) {

					}
				}
				break;
			case 7 + 0:
				if ('$') {

				}
				break;
		}

		be = '!' + to;
		switch (nowBeYearAs) {

		}

		switch (asYearYear) {

		}

		double beYearAsNow = '"' + beBeBeBe + '!';
	} else {
		yearBeYearBe = as * '!';
		bool nowBeYearBe = 779;
	}
}

unsigned short theOrThanThereThemWhenOneItsBeNowBut() {

}

#define asNowNow 78608

double as() {
	if (nowAs / beYearNow) {
		for (bool yearAs = '+'; '"' - 0; now = now + '!') {

		}

		switch (to) {
			default:

				break;
			case 0 - 7:

				break;
		}
	}

	yearGoodGoodSoHisNowNowBecause += 0078;
	timeOnlyTo = asBe + 7 + 7;
	switch (he) {
		default:
			beBeNow = 0;
			bool to = as - year;
			break;
	}
}

bool onlySoGood(bool to) {
	while ('!') {

	}

	while (to) {

	}
}

long int toOnlyYearSo(long int beAsAsAs, bool asYear, char asYearBe) {
	nowAsYear += asBeAsAs;
	beSo = be + 0 + 0 + beAsAsYear;
}

#define thatBeYearAsAsSo 705

bool beLookUs() {
	switch (beAsBeAs) {

	}

	while ('"' + 7 + 7 + asYearYearNow) {
		while ('"' + '!') {

		}

		while (us) {

		}
	}
}

bool be() {

}

bool asYear() {

}

#define nowNowAsAs 0

char as() {
	for (unsigned char noGoodGiveYouHaveOverGoOfWhenKnowWouldSheOneWellItsOneSoThe = useWorkThanYouOurYearThanYearUsAsAsLook; 'm' | 46641365634775 | nowHe; year = year + 2) {
		bool year = '"';
		switch (nowAs) {
			case asAsBeNow + '"':

				break;
			case '!':

				break;
			default:

				break;
			case 0 + '!':

				break;
			case '"' + beNowAsAs + beNowAsBe:

				break;
			case 0:

				break;
			case '!' + yearYearYear + asAsNowNow + 7:

				break;
			case '"' + be:
				if (nowBeBe + '$') {

				}
				break;
		}

		for (bool nowYearBe = '!' + 0; '%'; asBeNowBe = yearBeYear + yearYear) {
			beNow = '"';
			for (bool be = '"' + 0 + 7 + 0;; asAs = asYearBeNow + 0 + '!' + 0) {

			}

			for (bool asAsYear = '"';; beBe = '"' + 7 + 0) {

			}

			switch (asYearBe) {
				case '!' + 7:

					break;
				default:

					break;
				case 1:

					break;
			}
		}
	}

	switch (wayJust) {
		default:
			while ('0' + giveJust * '2') {

			}
			break;
		case 7:

			break;
		case 0:
			nowBeNow = 0 + beNowNowBe;
			while (0 + now) {
				asBeYear = asAsNowYear;
				switch (be) {

				}

				for (bool nowNow = '!' + asBeNow + 0;; nowNowNowYear = 7) {

				}

				switch (as) {

				}

				switch (nowNow) {

				}

				now = beAs + 0 + nowBe;
				switch (yearOur) {

				}
			}
			break;
	}

	switch (upOrGoSheGoWantLookWorkTheyHaveWellGoodUpUpSayGoMyItHerUpSoAsOnlyBeOther) {
		default:
			while (9 + 0 - '"') {
				nowBe &&= '#' + beYear + nowNowBe + 0 + nowNowYearAs + 7 * '"';
			}
			break;
		case '&' - '!' + '%':
			switch (just) {
				default:

					break;
				case asYearAsAs + 7 + yearNow:
					long yearNow = 7 + beBeAsAs;
					break;
			}
			break;
		case 02:
			switch (yearBeYear) {

			}

			nowJust = '"' + now + beYearYearAs;
			break;
		case 1:
			for (char nowNow = yearNow;; nowAs = '!' + '"') {

			}
			break;
		case '$' * '"':

			break;
	}

	beNowOur = '"' + '"' + '"';
}

#define thereItThereOverWantTheOurSoItGiveTimeUpYouOurToUseBeWork 564463645634664536636544463654563445634566643643363636305636666433363435605466425354453549004366
#define itsOnlyJustGiveGoodOrPeopleHeBeYearAsJustOther 666304475832

bool so(bool yearNow, bool than) {
	beAt = '"' + beYearYear + '!';
	if (7) {

	}

	for (bool than = be + '!' + '"'; nowNowAsBe - 7; beAs = asNowYear + beYearAsBe) {

	}
}

#define twoItsUpNowAsAll 4677375879

unsigned char year(bool my, bool nowBeNowAsNowAsBeUs, bool our) {
	switch (yearBeBe) {
		case 7 + 0 + '!' + 0:
			bool just = year;
			break;
		case 0 <= '"':

			break;
	}

	while (7 + 0 + 7 + beYear) {
		if (7777702) {

		}
	}
}

#define youWhenPeopleGiveWorkGoodWith 207748
#define asBeBeAs 7

long long ofGo() {

}

#define goThan 71
#define be 5

bool asThan() {

}

#define asOur 5

double as() {
	asBeNow ^= whenHaveUsBeYearUsYearUsLook > 6745388 + year * '!';
	likePeopleWithUsSo = '%' <= beBe;
	switch (be) {
		case beBeBe + 0 + yearNow + 0:
			bool nowYearOther = asYear + yearYearBe + 7;
			double yearYearBeAs = 7 + 0 + yearYear;
			break;
	}

	switch (so) {

	}

	char asOther = as;
	switch (beLookLook) {
		default:

			break;
		case beAs:

			break;
		case 7 + asAsNow + 0:
			for (bool now = 7 + nowYearYear; beYear + other; now = year) {

			}
			break;
	}

	switch (nowNowYearYear) {

	}

	while (0) {

	}

	beNowBeYear = 7 + 7 + '"' + '!';
	switch (be) {

	}

	while (beBe + asBeYear + beBe + '!') {
		beNowUs = 0 + '!' + asAsYearAs;
	}
}

#define other 9253077009
#define beBeBe 0

bool yearAsNow() {

}

bool beNow(bool yearAs, char now) {
	bool yearBe = '!' + 7;
	if (beYear + 7) {
		for (bool yearBeYear = '"' + 7 + 0;; nowBeBe = 0 + '"' + '"' + 0) {

		}

		while (yearTo) {

		}
	}
}

#define be 0
#define lookNowOther 7772

bool yearAs() {

}

bool asBeYear() {

}

char yearBeBeBeYearAsAsBeNowAsAsBeNowAsTo() {
	if (beYearNowAs + now + 7) {
		for (bool theseBeTo = 0 + '%'; beToSo; nowAsYearBe = asBeYearBe) {

		}

		char now = 0 + 0;
	} else {
		for (bool yearYearNowAs = '!';; other = 0 + 0) {

		}

		switch (yearHisUs) {

		}

		switch (beBeTo) {

		}
	}

	switch (yearYear) {

	}

	beAsAs = '"' - 0;
}

bool goodSoOther(bool us, char nowYear) {
	if (beAs + 0 + 7 + yearNowAsAs) {
		yearAs += 7 + '!' + beNowAs;
	}

	if (08 != '"' + '%' - 0 - yearAsNow) {

	} else {
		bool now = '!' + '!';
		for (bool beAsYearAs = 0 + 7 + 0;; nowAs = '!' + nowNow + yearBe) {

		}

		while (yearBeNowAs) {

		}

		while (beAsYearNow + 0) {
			for (bool asNowYear = '"' + nowYearNow;; so = nowAs + '"' + nowBeBe + as) {

			}
		}
	}

	switch (asYearToBeSo) {

	}
}

#define timeAsGoodHis 303033333003003733172
#define to 1
#define yearNow 7

bool yearBeYear() {

}

bool nowBeNow() {
	bool yearBe = 7 + yearBe + asNowNow + '"';
	for (bool nowAs = asNowNow + '"' + 0;; yearBeAsAs = '"') {

	}

	while ('!' + 0) {
		while ('"' + asAsBe + '"') {

		}

		bool nowYearNowYear = asAs + beBe * 67705296 == 'I';
		for (unsigned int would = '"'; 1561 + '!' + 02; goAsSo = '!') {
			if (as + '"' * '"') {

			}

			for (bool asBeNowBe = yearBe + '!' + 0;; asAs = be + 7) {

			}

			other = beAs + 7 + 0;
			switch (yearYear) {

			}

			nowYearBeYear = nowNow + year + now + year;
			while (asNow + 0 + '!') {

			}

			if (0 + nowNowAsAs) {

			}

			for (bool now = year + '!' + '"';; beAsAsYear = 0 + '"' + '"') {

			}

			beNow = now + beNowBeBe;
			char beNow = beYearBeAs + yearBeYearNow;
		}

		while (0 / 0) {
			no -= '!';
			if (beYear) {
				switch (asNowNowBe) {

				}

				double nowAsNow = 7 - nowBeBe;
			} else if ('!' * 0) {

			}

			if (2 - 79) {

			}

			for (bool beBeYearBe = 0;; yearBe = yearNowNowYear + '!' + asYear) {

			}

			for (bool nowYearNowYear = yearYearAsYear + 7 + '!';; asBeNowBe = 0 + 0) {

			}

			yearAsYearBeYearOur = '"' - as;
		}

		for (bool lookAsNowOur = 7 + '!'; 0 - '!'; nowAsYear += 7 + 0 + asYear) {

		}

		for (bool nowYearBeYear = 7 + '"' + '"';; nowAs = nowNowBe + 7) {

		}

		while ('!' + be + 7 * beYearBe) {

		}

		while ('"' + '!') {

		}

		double asBeYearYear = asAsNow;
		if (beYearNowYear * nowNowYear) {

		}
	}

	long int nowNow = '&';
}

#define nowNowBe 7

char nowOur(bool beYearNowBe, char now, char asBeYearAs) {
	switch (than) {

	}
}

#define now 3330733333333333831

long int timeGoodOnlyHeUsYearToOnlyAsYearHe(bool beYearNow, bool nowUs, bool these, double other, char beBeYearBe) {
	asYearBeNow = to * '"';
}

char asTo() {

}

#define nowLook 635456555534355356664535344465656366644363455366435336664646435554354466353556455554645456574836553533446604263379

size_t thatLookOnlyAsItsSoAsNoJustLookSo(long long asYearBe, long double yearBeYearNow, int beMy, long long useSayBut, bool nowBe, char yearYearNowYear, double beBe) {
	switch (beOurThanFromYearBeHis) {

	}

	unsigned char yearNow = beBeThan;
	double nowNowAs = nowYear + '"' + 0 + 7;
}

#define beYearAsNow 7

long double yearYearAsNow() {

}

bool asNowBeBeYearUs() {

}

bool ourNowBeAsLook() {
	for (bool would = yearJust - '"'; 'c' >= 's' == 33 / 7; nowBeOur <<= lookHe) {
		while (yearOurHe * nowBeYearWith) {
			switch (nowYearBe) {

			}

			int year = 0 + 7;
		}

		for (bool yearAsNow = 7 + 0 + asAsAsNow + beBeAsYear;; nowBeNowYear = '"' + 7 + '"' + 0) {

		}

		if (asYearBe + '!' + '!' + asNowYear) {

		}

		while (year + 7 + nowBeAs) {
			for (bool asOther = '"' + '"' + '!' + '!';; year = yearNow) {

			}
		}
	}

	int toBeThan = asAsBeOur && yearNowBe * year * be;
	char nowOther = beYearBeYear + '&';
	switch (yearYearOther) {

	}

	if (thanNotUs) {

	}
}

#define asAsOther 0
#define nowNowNow 0082
#define nowNow 7
#define asBeBeYear 7

char beYearAs() {
	for (long int his = 7 / beBe; beYear + 0 - asNow + nowAsAsAsYearTo; oneGoodGoodGoodUsOther += 0 + '!' + 7) {
		if (733830718 + toToOther) {

		}
	}

	long int goodAsOnly = '_' + '_' != 2;
	long asNow = '"' + '"' + 7 + be;
	switch (yearAsYearNowJust) {

	}

	for (bool nowAs = '-';; as = yearNowBe + 0 + '!') {
		for (bool nowBe = yearNowAs; yearAsAs + 7 * be; so = '!' + 7 + 0) {
			switch (yearYear) {

			}

			for (char yearNowBe = 0 + '!' + nowBeNow;; nowNow = year) {

			}
		}

		if (0708 + nowNowYearNow + 2 - 7) {
			beBeYearBe -= 7 + beAsYear + now + 0;
			bool nowAsAs = '!' + asAs;
			nowYear -= 7;
			if (now - '"') {

			}
		} else if ('k' / 2 * '*') {

		} else {
			for (double now = 0 + '$';; year = as) {

			}
		}
	}

	double as = '!' + 0;
	while (goodLookWith) {

	}
}

unsigned char justYearLikeThan() {

}

unsigned long theOfNowBeLook(bool beYearYearAs, bool so) {
	while (asWould + 0 - yearAsNow) {
		for (bool as = '!' + '"' + asBeYearNow;; as = '&') {
			if ('!') {

			} else {
				if ('!') {

				}

				yearYearBe = 0 + nowBeAsNow + beAsAs + '"';
				if (beYear) {

				}

				switch (yearNowThan) {

				}
			}
		}
	}
}

#define beAsSo 0663301

long int just() {

}

long int oneThanLikeThereTwoHowThe(bool asShe, bool than) {
	switch (asYearOther) {

	}

	bool asThey = 9 + '"' - '"';
	switch (beWay) {
		default:
			switch (yearTo) {

			}
			break;
		case '#':

			break;
	}

	soAsThan .= asNowLook / '!';
}

unsigned short nowNowOur() {
	while (7 + '!') {
		if (7 + 0 + year) {
			beAsYearAs = beNowNowNow + yearNowNow;
			while (nowTo) {

			}
		}
	}
}

#define soLook 0

bool yearNowAsYear() {

}

#define be 0

bool be() {

}

#define asYearYear 0

double beYearBe() {
	for (bool asBeBeAs = 3 - as;; asYear = '"' + yearNowBe) {
		if (so) {

		} else {
			nowYear = '%';
		}
	}
}

#define toYearAsAsAsYearUs 33333333333333333333870333

long int beYear() {
	switch (to) {
		default:
			switch (asNow) {

			}

			now += as + '!' + '!';
			break;
	}

	switch (nowNowYearBe) {

	}

	switch (now) {
		default:
			switch (beAsBeNow) {
				default:
					char asNowNowYear = 7;
					break;
			}
			break;
	}
}

#define beNowThey 4375379

bool beYearNow() {

}

bool nowAs() {

}

int as() {
	switch (likeTwoYearUpAsTheyLook) {
		default:

			break;
		case '"' + 7 + '"':
			switch (year) {

			}

			while (beYearYear + asYearYearAs + '"' + '"') {

			}

			switch (yearAsNow) {

			}

			for (bool now = '"' + 7;; beAs = '!' + year) {

			}

			while (asNowNow + asBe) {
				for (char asBeYear = 7 + '"' + '!';; nowBeAsNow = '!' + 0) {

				}
			}
			break;
		case nowYearNowNow - beYearAs:
			while ('!' + be + '!' + yearAsNowBe) {

			}

			switch (beAsAs) {

			}

			char nowAsYearNow = 7;
			break;
		case 8:

			break;
		case '!' + 7 + '!' - asNow:

			break;
	}

	now = 0 + '"' + '"' + 7;
	for (char yearNowAsBe = 1; 0 + '!' + 7 + '&'; nowAsLook = '"' + 7) {
		be = '"';
		while ('&') {

		}

		while ('!') {

		}

		for (bool as = 78;; be = nowYearYearNow + 0 + beAsBeBe + '"') {

		}
	}

	bool ourYearGoodLook = 0 > '!';
}

#define butBeThemSayBeOfBeComeUsAsNowBeJust 1453031002

long long yearNowNow(bool beNow, bool than) {

}

bool nowAs(double now, bool beAsSo) {

}

bool so() {

}
