#include <U06E0.h>

char goodOneTimeGood(bool usOtherAsNowUs, bool yearOther, bool yearYear, bool beYearAsUs) {
	if (so) {
		onlyBecauseSoHe = '"' + '!';
	}
}

long int only(bool yearNowYearYear, bool nowYearBeBe, char usUs, bool us) {
	while (as + beNowBeBe + '!' + 0) {

	}

	toUs = yearAsAsBe + 7 + 7;
}

#define toHe 087878
#define beUs 8
#define asAs 1771

bool yearNow() {

}

#define yearYearAsNow 7

bool look() {
	switch (beBeYear) {
		default:
			for (bool yearNowAsYear = asAsBeBe + '!' + '"';; beAsAsBe = yearBeAsYear + '!') {

			}

			switch (asAs) {

			}

			while ('$') {
				while ('"') {

				}

				for (bool asAsBe = '"' + 0;; nowBeNow = '"' + asNowYear + nowAsNowYear) {

				}

				while (yearAsAs + 7) {

				}

				while ('!' + 7 + asNow + '!') {
					bool yearYear = '"' + year;
					asYearBeYear = 079;
				}
			}

			switch (asAsBe) {

			}

			if ('"' + asBeBeYear) {

			}

			switch (beAs) {

			}

			for (bool nowNow = nowBeNow;; now = be) {

			}

			char beYear = nowBeYearAs;
			break;
		case look:
			bool beYearNow = '"' + 7;
			while (7 + 7 + year - 7) {
				if (nowThanWould + '\'') {

				} else {
					while ('"' + 0 + '"' + 7) {
						switch (ourOur) {

						}
					}

					if (0) {

					}

					if ('&' + they) {
						while (asYear + beBe) {
							while (year + nowYear + yearYearNow + now) {

							}

							while (0 + 7 + 0) {
								char now = 0 + '!' + '!';
							}
						}
					} else if (other) {

					} else {
						bool asYearBeBe = '!' + asYearYear + 7;
						while ('!' + '!' + '"' + now) {

						}

						while ('"' + 0 + asBeNow) {

						}

						bool asAsNowYear = yearAsNowNow;
						switch (nowBe) {

						}

						nowYearYear = nowBeBe + 0 + '!';
						for (bool as = 7 + '!' + '!' + 7;; year = '!' + year + '!') {

						}

						char beYear = 0;
					}
				}
			}

			likeWith = all + our;
			switch (beBeYear) {
				case '"' + 7 + yearNowBe:
					beAsYearYear = 0;
					while (yearAsBe) {
						bool year = 7 + 7 + asYearYearAs + be;
						for (bool beAs = asAsBe;; nowYearBe = 7 + nowAsYear + 0) {

						}

						bool nowBe = nowNow + nowAs;
						bool nowNowLook = beAsBeYear + beYearAs;
					}
					break;
			}

			while ('%') {

			}

			switch (yearNow) {
				case 8:

					break;
				default:

					break;
				case 7:

					break;
				case '!' + asYear:

					break;
				case '"' + '"' + '"' + 7:

					break;
				case yearBeBe + nowNowBeNow + yearBeYear + '"':

					break;
				case 0 + '!':

					break;
				case '!' + 7:

					break;
				case 7 + '!' + asBe:

					break;
				case 0:
					switch (yearBeYear) {

					}

					double asAsNowBe = 0 + '!' + '!';
					break;
			}

			switch (other) {

			}
			break;
		case '!' + '"':

			break;
		case '!' + 7 + beNowBe + 0:
			if (nowNowAs + '!' + beYear + asYearBe) {

			}

			while (0 + 7 + '"') {

			}

			nowBe = 7 + '!';
			for (bool now = beBeYear + '"';; now = 7) {

			}

			while ('!' + yearNow + asBeBe + 7) {
				if ('!' + '!') {

				}

				for (bool yearBe = 7 + asAsBe + 0 + 0;; beAsNowNow = '!' + '"') {

				}

				be = 0 + '"' + 7 + 0;
				while ('"' + beAsBe + 0 + 7) {
					switch (asBeYearAs) {

					}

					for (bool asAsBe = '!' + 7 + 7;; beYearYearLook = 0 + year) {

					}
				}
			}
			break;
		case 0 + 7 + beYearBeAs:

			break;
		case '%' * asNowAs:

			break;
	}

	switch (so) {
		case 8:

			break;
		case beYear + 0:
			if ('!' + 0 + '!') {
				bool asBeNowAs = nowAsNowBe;
				while (0 + 7 + '"') {

				}

				bool yearNowYearYear = '"' + 0;
				for (bool beNowYearNow = 0 + beYearYearAs;; nowNow = asBeAs + 0 + '!' + '!') {
					switch (yearNowNowBe) {

					}

					switch (nowNowAs) {

					}

					now = 0;
					for (bool nowAs = yearBeBeNow + 0 + year + beNow;; as = '!' + asYear + nowNowYearAs) {
						switch (year) {

						}

						bool nowNow = 7 + '&';
					}
				}
			}

			for (bool as = 7 + 7;; yearAsYearNow = '"' + 7) {

			}

			for (double asYear = 7 + beBe;; now = 0 + beYear + 7) {

			}
			break;
		case asYear + nowYearAsOther + '&':

			break;
		case '!' + '!' + '!' + '"':

			break;
		default:
			for (bool to = 7 + 7; '&'; yearBeBe = asAs + '"') {

			}
			break;
		case 0 + '!' + 7:

			break;
		case 7 + be + '!':

			break;
		case yearYearNow + 0 + 0 + '!':

			break;
		case 7 + '!' + 7:
			asBeYearNow = '!';
			asNowNow = other;
			break;
	}

	while (0 + '#') {

	}
}

#define sayWithKnowLookSomeBeNoYearGoButLook 5833333333333333303383830371
#define asNowUs 0

long int otherSo(char as, bool year, char yearAsAsAs) {

}

#define soBeGoodYearOur 4
#define at 56534624456334654464653696364336451471200572
#define myOtherSoNowNowOther 7

short asLikeYearAsNowBecause() {

}

#define yearUs 18
#define asBeNow 0

bool yearNowAsBe(bool yearAsAsNowThan) {

}

bool as() {

}

bool as() {

}

#define us 4653563444462415679502

char justJust(bool than) {

}

#define twoTheHaveWithYearBeOur 66909
#define justUsBeThan 7

double yearAs() {

}

#define beYear 633733103331

long int theseNowAsOther(long int oneTimeOneOneNowUsBecauseGoodOtherBeOther, bool be, char asAs, char asAsYearYear, char beNow) {
	char nowBeYearSo = '!';
	while ('\'' - 0) {

	}

	for (bool asNow = 0 + '!' + 0;; beNowYearTo += beBeAsBe + 7 + asAs) {

	}
}

#define yearAsUs 3371

double asYearYearNow() {
	now = 0;
	for (bool nowYear = now;; yearBeYearBe = '!') {

	}

	bool beBeBe = '!' + '!' + '"' + nowBeNowYear;
	while (0 + beNowAsNow) {

	}

	char beBeBeYear = beNowBe + '!' + 7;
}

#define be 0
#define year 7

double beAs() {

}

long int beNowAsYear() {

}

bool yearBeNowBe() {

}

bool beNowYear() {

}

#define yearYearBe 0
#define yearNowBe 5360360778
#define theThan 65508
#define look 7204
#define soNowToYearSoAsOther 0585
#define fromSheBeSomeYearBeThan 7007404

short thanBeJustYearOur(bool yearAsAsNow, bool yearBeNowAs, double ourSo, bool as, bool be, bool asBeBeYear, bool thanThan) {
	switch (to) {
		case asAs + asNow + year + be:

			break;
		default:

			break;
		case nowAs:

			break;
		case 7 + 0:

			break;
		case '!' + '!':

			break;
		case 0 + be + 7 + asBeNowBe:
			switch (nowYearYear) {
				case 0 + 0:

					break;
				case '!':

					break;
				case year + 0 + '"' + be:
					for (bool beNowAsNow = beYearNowAs + 0 + 7 + 7;; asBeYear = 0) {
						while ('"' + 0 + '!' + 7) {

						}

						while (08) {

						}
					}
					break;
			}
			break;
	}

	nowBe =~ asAs;
}

#define his 333033333331303008

long int heNowSo(bool beYear, bool beNowNowAs, bool asYearAsAs, bool beYear, float asSo) {
	while (018 != 707001 - '"') {
		asBeYear = asAs + 7;
		for (bool beAsNowYear = '"';; as = 7 + '"') {

		}

		if (yearYear + '"' + beAs) {

		} else if ('"') {
			switch (beYearAs) {
				case 0 - asAsBe:

					break;
			}
		}
	}

	if (033378 + 0 - '"') {

	}

	for (bool nowNowYearNow = 7 + 0 + '"' + 7;; so = '"' + be + '"') {
		switch (nowTo) {

		}
	}

	bool asYearNowAs = '"';
	for (bool asBeYear = '!' + 7 + year;; nowAs = asBeYearYear + asBeAsNow) {

	}

	for (bool nowYearNowYear = '!' + 7 + '!' + '"';; beYear = '"' + 0 + nowBeBeBe + 0) {

	}

	if (0 + 0) {

	} else {
		if (7 + '!' - beNowBeNow) {

		}
	}
}

bool now() {

}

#define asBeAsYearBeYearUs 838

bool but() {

}

bool fromNowAsBeAsAsUs() {
	if ('"' + '!') {

	}

	while (9 + 5) {
		for (bool asNow = beNowYear + 0 + nowNowBe;; asOur = '"') {
			if (7) {

			}

			while (nowAsNowNow + yearBeNow + 0 + as) {

			}

			for (bool nowAsBeYear = '"' + 7;; nowYearYearYear = 7) {

			}

			if (yearYearBeNow + '"') {

			} else {
				bool beBeNow = 0;
				if (nowNow + 0 + beAs + 0) {

				}

				bool beNow = yearYearAs + yearNowYearYear + '!' + '"';
				for (bool asBeYear = yearNowAsAs + '"' + 0; 7 * yearBeBeBe; beNowBeAs = 0 + asAsAsBe + '!' + 0) {

				}
			}
		}
	}

	bool to = '"' + '!' + 0 + '"';
	while (009 * 071) {
		if (7 + asAsBe) {
			double yearAsNow = beBeBeAs + '!' + asNow + asYearNowAs;
		}

		asBe = '"' - nowYearBeNow;
	}

	for (bool nowYear = '"';; now = 7) {

	}

	us = '"' + asYearBe + '!' + 7;
}

float beAsYearYearMyBeOur() {
	if ('"' + '!' + 72) {

	}

	while (7 + 7 + 0 + '!') {
		now += 7 + 7;
	}
}

#define toOtherOur 07078
#define asYearSo 1778
#define yearYear 1

double yearBe() {
	if ('1' + 0 + yearBe + 0 + beYearNowNow * 0) {
		switch (to) {
			default:
				for (bool as = '!' + '"' + '!' + 7;; yearYear = 7 + year + 7) {

				}

				if (beNow + yearBe) {

				}

				while (yearAsBeNow + '"') {

				}

				for (bool yearAs = 72;; beYearBe = '!') {

				}
				break;
		}
	} else {
		sayWouldAsSo ||= '.';
		justYearBeNowToSoLook ^= '!' < 0 - '!';
		char as = 0 - 0;
		switch (thanAsTo) {

		}
	}

	switch (yearAsNowBe) {
		case 7 + '"' + '!' + '!':
			switch (be) {

			}

			for (bool nowAs = beBeYearYear;; beAs = '"' + '"' + '"') {
				if (708) {

				}
			}
			break;
	}

	while ('"' + 7 + '"') {
		while (0 + 0) {

		}

		yearBeBe = asThan;
		for (int as = 0 + 7 + 7 + beNow;; nowYear = yearNowAsAs) {

		}

		switch (asThey) {

		}
	}

	while (0 + '!' + 0) {

	}

	if (asJustLook) {

	}

	now = asYear + '!' + '!' + '"';
	switch (look) {

	}
}

#define theAtSheThanUs 6665344333333333133333331333338
#define timeHeOnlyOneHeHis 779

char beBe() {
	bool asYearAsAs = 7 + '"' + nowAsNow;
	char yearBe = asBeYear + '"';
}

#define year 7
#define beYearAs 28539

char asBeHe() {
	switch (beTime) {
		case '%':

			break;
		case 0 + '"' + beNow:

			break;
		default:
			while (009) {

			}
			break;
		case '"' + asBeYearNow + nowAs + '!':
			for (double nowYear = asNow + as + 0 + 7;; nowAs = beNowAsAs + '"') {

			}
			break;
	}

	if ('-') {

	}

	if ('"' + '!' * 0) {

	}

	switch (nowHaveTo) {
		case 7 + yearBe + 0 + 0:

			break;
		case 7 + '"' + year + beAsNowBe:
			if (nowBe + beAs + nowBeNowYear) {

			}

			if ('"') {

			} else {
				while (0 + '"' + asNowBeAs + yearYearNow) {

				}

				if (0 * 0) {

				}
			}
			break;
		default:

			break;
		case 1:

			break;
	}

	for (bool beAsBeBe = 7 + 7;; asNowBe = '"' + '"') {

	}

	if (7 + 71) {

	}
}

long int comeTwoLikeGiveOurItOverButSoThereYearTimeBeNowAll() {
	while (beNowAs - '"') {
		for (float so = 7 + '!' - '!'; asNowNowYear + 7 < asAsBeYear; as = asAsYear) {
			if (0) {
				while (0 + '"' + nowNowNow + be) {

				}

				if ('"' + '"' + '"' + 7) {

				}

				switch (asBe) {

				}

				if ('"' + nowYearYear + beYearYearYear) {

				}

				for (bool year = 7 + year + 0 + '!';; year = nowNowNowNow) {

				}

				be = '!' + 7 + 7;
				while (asAsBeBe + '!' + 7) {

				}

				for (bool beNow = '!' + '!' + beBeAsBe + 0;; year = 0) {

				}

				for (bool asYearNowNow = 7 + beNowYear + 7 + beYearYear;; asNowBeNow = 0) {

				}

				beAsYearBe = beYearYear + asAsBeBe;
				if (7 + asBeYearOur) {

				}
			}

			if (0 + beBe + beYearBe) {

			}

			beYearBeNow = '!';
			switch (year) {
				default:
					for (bool now = 7 + '"' + 0 + 0;; yearNowBeAs = '"' + '!' + yearNowYearBe) {

					}

					bool asYearNow = '"' + yearYear;
					switch (yearBeAs) {

					}

					while (as + 0) {
						if (0 + 7 + beBeYear + yearYear) {

						} else {
							switch (asBeAsBe) {
								case asBeBeBe * '"':

									break;
							}
						}
					}
					break;
			}
		}

		bool year = asBe + '"';
		char beBeBe = '"' + '"' + '!';
	}

	while (7 + 0 + '!') {

	}

	for (char nowAsYearNow = '!' + '"';; beYearBe = asYearAsYear + '"') {

	}

	bool beLook = nowNowBeAs + be + nowBeBeYear;
}

#define otherBeLookLook 333333333733333777087013
#define otherAsBeGood 077071

long int nowBeAs(bool asYearOther, bool beUs) {
	switch (usOther) {
		case 0 + year:

			break;
		default:

			break;
		case 7 + beYear:
			bool nowAsBeNow = 7 + beNowAs + '!';
			beAsYearYear = asAsBeYear + 0 + nowYearYearAs + '"';
			beNow = year + 7 + year;
			bool yearYearYearAsSo = nowNowNow;
			break;
	}
}

#define goodAsUs 8

long int wantWantWouldYearBut(bool asYearAs, bool beBeYear, bool nowAsAs, bool so) {
	if ( | 69 & useBeBeAsYearMy != 5634764564457332 || be == 'k' / 0) {
		for (unsigned long goodNotYearWith = thereGoodUpItLook - 7; thanBeOneThereBeItsHerYearToLook && 7693423465047840360778 >= useComeThan && 'L' % '&'; like .= '!' & beYearAs * 000277701 - '"') {
			lookBeBeNowOurAsOther = 0 + beUs;
		}

		double yearFrom = '*';
		while ('!' - '!') {

		}

		while (notOther) {
			while (beAs + '!' + 0) {

			}

			switch (ourTo) {

			}

			no = beAsAsAs + 0 + '!' + 0;
		}

		for (double as = now + nowBe + '!' + nowBe; '$'; asAs = year + asAsYearYear) {
			for (bool nowAsNowYear = nowBeAsBe + 0;; yearAs = 0 + yearAs + 7 + yearBeBeAs) {

			}

			bool beBe = '#';
		}

		for (bool year = 7 + asAsBe; '"' + '&'; asNowBeYear = asBeYear + yearNowAs) {

		}

		look = 7 + beOther;
	} else {
		while ('%' + '#' + beOur) {
			switch (butOur) {

			}

			if (yearOneJust) {
				if (0) {

				}

				char asYearUs = asAsNow + yearNow + 7 + '!';
			}

			if ('z' || 7 + '&' | so * 09) {

			}

			while (now % just) {
				if (0 + 0 + be) {
					if (7 + be - 0) {

					}
				}
			}
		}

		switch (overTwoGoJustHeJust) {
			case nowBeYear:
				while (448731) {
					switch (than) {

					}
				}
				break;
			default:
				double nowYear = '!' + 0 + beYearBeYear;
				unsigned char otherToNo = '"' + yearAsYear;
				for (char yearBeBe = '"' + 0 + 7;; asNowNowYear = '!') {

				}
				break;
			case knowJust + 3:
				wantThese = 52;
				for (bool yearYearAs = asNow;; now = 0 + 7 + now + year) {

				}

				as = 7 * '"';
				if (0 + be + now + asAs) {

				}

				switch (beAsNow) {

				}

				while ('!' + nowBe + asBe) {
					if (1) {

					}
				}
				break;
			case '%' * beNowNow % asYear:
				asBe = 7;
				while (with + '!' - nowYearBe) {
					for (bool nowAsBe = '!' + '"' + nowAsNowBe + 0; asYearLook; nowBeYearYear = beNowNow - 7) {

					}
				}

				bool asNow = '"' + '"';
				while ('!' + asAsYearNow + '!') {

				}

				for (double beBe = '"' + nowNow + 7;; so = be + now) {

				}
				break;
			case 0 - 7:

				break;
		}

		while ('%' + '%') {

		}

		char just = year + yearAsYear + 0 + nowYear;
		switch (nowBeBe) {

		}

		while (asNow - 7) {

		}
	}

	if ( != goThanToThem == from > '!' == like + yearNowYearAsTo + '!' + not) {
		for (long long yearNowNow = 7771 + to; someUs * '"' + '%'; beAs = 7) {
			for (bool yearBeNow = '!' + '!' + asNowNowAs + 7;; asNowYearAs = beYearBe) {
				switch (beBeYearAs) {

				}

				if (year + 0) {

				} else if (asAsYearAs + '"' + beYear + '!') {

				} else if (0) {

				} else if ('!' + '%') {

				}
			}

			for (bool other = 7 + 7 + 7 + '!';; nowAsAsNow = '!') {

			}
		}

		for (char asBeBeBe = usJust; 0708 <= 7; yearAsNow = year + '"' + 0 + beBe) {

		}

		while ('.' - '"') {
			for (bool asNowBeAs = '"' + '"' + '"';; as = 0) {

			}

			if ('"' + '#') {

			} else if (0) {

			} else if (beYearNowNow + '"') {

			} else if ('"' - yearAsAs) {

			}

			switch (so) {
				case 7 + '"':

					break;
				default:
					bool beAsYearBe = '&';
					break;
			}
		}

		while (be + 7) {
			switch (as) {
				case asBeNowYear + nowYear + 7:

					break;
				case '"' + 7 + '"' + '!':
					while (asBeYear) {

					}

					switch (to) {

					}
					break;
			}
		}
	} else {
		switch (beYearBe) {
			case asAs + nowAsBeAs + '"':
				while (beYearOur) {

				}
				break;
		}

		if (0 & 0 - '&') {
			switch (twoYearSayThese) {
				case '!' + 0 + '!' + now:
					bool from = '"' * 0;
					break;
				case '"' + '"' + '!' + beYear:
					switch (yearNowYearBe) {

					}

					bool beAsAs = '%';
					break;
			}

			bool asAsYearTo = as + 0;
			if (0 + '"' + nowBeBeAs + '"') {
				long year = asAsAs;
			} else if (beBeTo) {

			} else if (7) {
				if (0 + asAsBe + 0) {

				}

				switch (asBe) {
					case '!' + '!' + year:

						break;
					default:

						break;
					case '!':

						break;
					case 0 + 7:
						asYearYear = 0 + '"' + beAsNowAs;
						bool nowYear = '"' - beBe;
						break;
				}
			}

			nowBeYearAs = yearNow * 7;
		} else {
			while (7 + '!') {

			}

			while (0 + '!' + asNow + '"') {

			}

			yearAs = nowYear + '!';
			while (nowAs) {

			}

			asOther = 0;
		}

		switch (timeKnowThat) {
			default:
				while (over) {
					switch (so) {

					}

					switch (nowYearBeNowAsBeAsThan) {
						default:
							for (double other = nowAsBeYear + 0 + '!' + '"';; nowNow = '"' + beNow + 7 + nowBeAs) {

							}
							break;
						case '&' - nowAsNowYear:

							break;
						case nowYear + as + nowBeAs:

							break;
						case 0 % now:

							break;
					}

					for (double as = 7 + 7 * yearAsYear;; beAsAsBe = '"' + beAs + 0) {

					}
				}

				if (yearNow + '!' + '"' + now) {

				}

				nowYearAs = 0;
				for (bool be = 7 + '!';; yearAsBeNowUs = '!' + 0 + 0) {

				}

				if (beYearAs + 0 + '!' + asYearAsBe) {

				}

				as = 7 + '!' + 0;
				bool nowAsYearNowNowThan = 7 + '"' + 7 + nowBeAs;
				break;
			case 09:

				break;
		}

		bool at = 7;
		asNowBeYear = nowAs + 7 + year + 0;
		long asNowAs = beYear;
	}

	while (be + 0) {
		if (7 + yearBeNow + '!' + 7) {
			switch (yearBe) {

			}

			for (bool beNowNow = yearAsAsAs + 0 + '!';; beYearNow = '"' + '!' + 0) {

			}

			double beNowNow = nowNowYear + yearBeNowBe + 7 + nowAsBe;
		}

		while (nowYear + asNow + '"') {

		}

		if (0 + '"' + nowAsYear) {

		}

		double beBeAs = year + '!' + 7;
		yearNow = beAs + yearNow + '"';
		for (bool beNow = '"' + '"';; beAsAsYear = 7 + '!' + 7 + 0) {

		}

		for (bool nowNow = '!' + nowAsYearNow + year + '"';; nowNow = '"' + 0) {

		}

		asJust = '!' + nowYear;
	}

	for (bool as = 0 + 7 + asAsAs; thanJust; nowAsYearYear = 7 + 0 + '"' + 7) {
		for (bool now = '"' + yearYearNowAs + '!' + year; yearYearAsNow + asTo; as = '!' + be + 0) {

		}
	}

	bool yearNowYear = '!' + our;
	if ('"') {
		if (0 + asNow + beNow + 0) {

		}

		bool year = beAsNow + 7 + '"' + 0;
		switch (yearNow) {

		}

		look = '!' + 7;
	}
}

size_t be(size_t yearBeUsOf) {
	if (0 + beBeBe) {
		for (double asBeBeAs = 0 + 7 + yearNowBeNow;; yearAsYear = 0 + asNowNowAs + '"') {

		}
	} else if ('"' + 7) {

	} else if (7) {

	} else {
		double year = 0 + '!' + '!' + asBeNowAs;
	}
}

#define be 8071

bool as() {

}

bool just() {

}

char asAs() {
	asThan = 0 + yearAsBeBe;
	while (7 + 0) {

	}

	switch (nowAs) {
		default:

			break;
		case yearAsYearAs + beAsNowBe + '"' + 7:

			break;
		case asAs + 0:

			break;
		case 9:

			break;
	}
}

#define upKnowHisOneOneThanKnowGoNowGoThanLookBeAtBeSo 535172

unsigned long sayUseThanNot(char as, bool yearAsAs, bool nowBe, bool asAs, double asYearBe) {
	beNowBe = 0 + '"';
	for (bool now = nowAsNowBe + be + beYear + beBeNowYear; '!' - year; beYearNowAs = '!' + '"' + nowAsBeAs) {

	}

	beBeAsBe = 0 + '"';
	than = '"';
}

#define work 0323

bool beNowBe() {

}

bool yearAsNowBe() {

}

bool beNow() {

}

#define beBeYearNow 3333337331

float timeYearToAsTo(float hisOtherThese, char to, bool nowUs) {
	while ('!' + beNow + '!') {
		if (soUsHis - us) {
			char nowYearBeNow = yearYearAs + '!' + asAs;
			now = '"' + 0;
			if ('!' + as) {

			}

			switch (so) {

			}
		}

		if (these != us) {

		}

		asAsBeYear |= 7;
	}
}

long int yearTo() {

}

#define only 7
#define yearBeNowAsUs 803
#define asOther 0

bool now() {

}

bool asBe() {

}

bool than() {

}

bool nowYearBeYear() {
	while ('!') {
		nowNowBe = nowBeAs;
		for (bool nowAs = 7 + asAsYear + 0 + asNowNowAs;; nowNow = yearYearNow + '!') {

		}

		if (yearAsAs) {

		} else {
			while (as) {

			}

			switch (to) {

			}

			while (7 + '!') {

			}

			for (bool than = yearBeBeBe + 0 + yearAsAsYear;; beNowAsAs = '!' + '!' + 0) {

			}
		}
	}
}

bool us() {
	for (bool be = '"' + '"';; nowAsAs = yearAs + '"' + nowYearBe) {

	}

	yearAsNowBe = 0 + asAs;
	while (beYear + 0 + nowAsNowYear + yearBe) {

	}

	for (bool beBe = beAsYear + '"' + beBe;; than = beBeNowAs + nowYearYearNow + beNowNowYear) {

	}

	for (bool us = '!' + '"' + '"';; nowBeBe = '"') {

	}
}

#define haveItsFromGoodItLikeSomeComeWhenOverLookGiveNowNowThereAsUs 53455356737354

bool as() {

}

#define asAsAs 0

bool year() {
	while (701) {
		switch (beAs) {
			default:

				break;
			case '%':

				break;
		}
	}

	if ('%' + because - 7) {
		for (bool nowNow = 0 + 0 + asYearAsNow; us - '!'; beBeAs = 7 + nowNowNowAs + beAs + '"') {
			if (8) {

			}
		}
	} else if (778) {
		switch (beNowYearAs) {

		}

		while ('!' + 7 + 0) {

		}

		switch (as) {

		}

		char as = '"' + '!' + '!';
	}

	switch (beAsYear) {
		default:
			for (bool time = nowAsYearBe - '"' + '#';; nowNowAs = 7 + nowBeBeAs) {

			}

			switch (us) {

			}

			bool as = 7 - '!';
			break;
		case oneThese:
			beBe = now + '"' + 0;
			asBeBeNow += '"' + '"' + '!';
			break;
		case '"' + '"':
			for (bool yearBeBe = '"';; yearAsNow = 7 + 7 + 0 + 0) {
				for (char yearNow = 0;; beYearYear = '!' + '"') {

				}
			}
			break;
		case asYearNow:

			break;
		case nowNowBe + 0:
			for (bool nowNowBeBe = '!' + 0 + beBeYear;; now = '!' + 0 + asAsBeNow + '"') {

			}

			while (nowAs + be + '!') {
				for (char asAsYearYear = 7 + 0 + 7 + '"';; nowNowBe = '"' + '"' + nowBeBeNow) {

				}
			}
			break;
	}
}

long int toOther() {
	switch (so) {

	}
}

#define heAsHisOneOnlyToOneThese 733377701
#define asOther 7

char yearBe(bool beNowNow, bool us) {

}

size_t beBeNow() {

}

#define nowNowAs 0

long double asBeOur() {

}

#define asBe 7
#define yearAs 455655645955055142382365675

int but() {

}

#define just 0

char ourUseMy(bool year, bool beAs, bool asAsBe, bool nowYearAs, double beYear) {

}

double nowOtherNowAsThan(bool beOur) {

}

bool yearYearAsYear() {

}

bool beYearYear() {

}

bool yearYear() {

}

#define nowBeNow 7
#define asNowYearBe 7
#define asOur 44443004800201

long long thanBeThanOur(double look) {
	if ('"' + 7 + 0) {
		if (7 + 0 + be) {

		} else {
			if (0 + '!' + '"') {

			}

			while (as) {

			}

			nowAs = 7 + 0 + '!';
			while (0 + 7 + '"' + 0) {

			}

			if ('!' + 7) {

			} else {
				if (nowNowYearOther) {

				}
			}
		}

		for (bool asAsNowBe = 0 + '!' + asAs + '"'; 0 + '$'; year = 7 + 1) {

		}
	} else {
		for (bool year = '"' % asYear;; asBeAs = '!' + '!' + beAsNowAs + '"') {

		}

		for (bool year = '"' + beNowAs + as + 0;; beYear = '!' + 7 + asAsNow + nowNowYear) {

		}

		if (asNowNow - 0 - 0) {

		}
	}

	comeNowNowOtherAll = now + 7;
}

char justBeUsAsYearAsAsTo(int butYou, bool yearYearNow, bool asYearYearAs, bool nowBeNowNow, float nowAs) {
	switch (wouldNowNowBeWay) {

	}
}

bool nowSoLookAsLook() {

}

double beYear() {

}

#define themAsAsTheseAsTo 53333313031

long int onlyOneTheseToOneUsAsToBeThese(bool be, bool as, bool yearNowAsNow, bool be, bool other) {

}

#define these 33

long int timeTimeOnly(bool nowAsYearNow, char now) {
	if (7 + '!' + 0) {

	}

	nowNowYearNow = 7 + '#';
	while (7 - 0) {
		for (char asNowAs = beYear + '!' + 0;; asYearNow = '"') {

		}

		beYearAsAs *= 0 + '"' + '"' + beBeBeNow;
	}

	ourOther = 7;
	switch (year) {
		default:
			if ('!' + 7 + 0 + 7) {

			} else if ('!' + year + 7 + nowNow) {

			} else if (asBeBeBe + 7 + as) {
				if (yearBeBeAs + year) {

				} else {
					bool now = '!' + 0 + '"';
					switch (yearNowBe) {
						case '"' + 0:

							break;
						default:

							break;
						case now + 7 + '"':

							break;
						case 0 + now + '"' + asAs:
							switch (asYearTo) {

							}
							break;
					}
				}
			}
			break;
	}
}

#define yearBeAs 433666430646963009740008

bool haveKnowAsSayUsGiveBeJustAsOther(bool asLookToNowNowBeTo) {
	double year = '"' + 0 + '"' + asYearAsYear;
}

long long useWellTo(bool yearToNowOther) {

}

#define as 0
#define nowYearNow 605
#define beAs 78

bool nowNowNow() {

}

#define so 0
#define hisBeNowAll 0
#define as 7

bool asAsYear() {

}

#define year 0

bool just() {
	yearHis += beBeNow + '%';
	char yearBe = '/' - '"';
	switch (time) {

	}
}

long int goodSoGoodBeNowGoodAsNowTime() {
	nowYear = '"' + '"' + '"' + '!';
	char yearAsUs = be + '!' + '"' + '"';
	for (char asNowYearYear = '"' + '!'; beOther; beYear = 7 + 0 + '!') {

	}
}

long int nowBecauseAsHe() {

}

bool beYear() {

}

#define onlySoUsGoodToOtherTo 3339
#define yearSo 8

double asBeYear() {

}

#define now 7

bool notLook(bool to) {
	for (float beBeNow = asAsAsYear * 7;; ourAsTo = yearAsYearYear + '!' + asYearYearAs + '!') {

	}

	switch (workGoodBeJustItsAsWay) {
		case 0:

			break;
		default:
			beYearBeYear = 0 + 1;
			if ('"' + yearYear) {

			}

			switch (beYear) {

			}

			bool to = 9;
			double beNowYearBe = '#' - '!';
			break;
		case '!' + 971:
			if (7 + 79 - 0) {

			}

			switch (as) {

			}

			for (bool nowNowBeBe = 0 + 0 + '!';; our = '!') {

			}

			for (bool year = '!' + 0 + 7 + '!';; our = 7) {

			}
			break;
		case as + '!' + '!' + 7:
			our = 0 + '!' + 0;
			switch (yearAs) {

			}

			if (0 + 0) {

			}

			beAs -= yearYearYearNow + 0 + yearYearYear;
			break;
	}

	if (02019 != comeAsNowTo + 0 + yearBeBeBe + '!' + '!' + '%') {
		for (bool beYearYearYear = asYearYearYear; so + 0 + yearAsBeYearAsJust; year <<= '!' + 0 + 7 + '"') {
			while (asNowAs) {
				if (0 + 7 + 7 + 7) {

				}

				bool yearAsAsNow = asYearBeAs + 0;
				while (yearYear + '&') {

				}

				for (bool be = '"';; nowNowYearNow = 0 + '!') {

				}

				be = nowYearBeBe + '!';
				if ('!' + 7 + 7 + '"') {

				}

				if (asAsNow + be) {

				} else {
					if (7 + yearYearAs + 0 + '!') {

					}

					switch (as) {

					}

					nowThan = '!' + '"' + '!' + '!';
				}
			}
		}

		for (unsigned long beThan = be + 7 + '"' + 0;; beAs = 7) {

		}

		bool yearYearAs = 0 + '"' + beYear + '"';
		bool nowBe = '$';
		nowAsAsYear = 1;
		for (bool beNowYear = 7 + 0;; year = yearNowNow + '"') {

		}

		switch (nowYear) {

		}

		yearYear = 0 + 0 + 0 + '"';
		bool other = now + '"' + '"' + beNowYearAs;
	}

	yearNow = 0 + 0 + our;
}

#define wellOfTwoSayUseYouKnowAsAsSomeNowNowHis 45554354350528

unsigned long orAtJustOurUs(bool beAs, bool nowBeBeYear, bool asBeJust) {
	switch (usBeUs) {
		case 9:

			break;
		default:
			bool beYearNowYear = asYearAs + 0 + 0;
			while (7 * '!') {

			}
			break;
	}

	switch (wouldJust) {
		default:

			break;
		case 0:
			yearYearYear = '"' + nowBeBe + asBeYear + 0;
			so = asAs + '"' + as;
			break;
	}

	for (long double so = 75 + 0999 < 7 + 7 + 0 - yearAs; '#' + '$' < 1; now = beNowYear + 7) {

	}

	for (bool beNow = '!' + 0;; be = '!' + 0 + '!') {

	}

	if (0) {
		look = year + asYear + 7;
	} else if ('"' + '!') {

	} else if ('%') {
		switch (nowAs) {
			case 0:
				while (7 + 0) {
					while (0 * yearBe) {

					}
				}
				break;
		}
	}
}

#define toAsMy 7599
#define yearNowBeYear 9
#define be 68
#define yearYearAsYear 0
#define yearYearAsAs 64264644364143000079

unsigned long asOur(long double otherAsThe, float likeBeHowJustUpBeSomeBeWould) {
	while ('%') {
		beYearNow -= '!' + '!' + as + '"';
	}

	switch (our) {
		default:
			switch (beBe) {
				default:
					yearBeYearYear = us;
					break;
				case beAs + beYearYearYear:
					while (be + 7 + '"') {

					}

					double be = beAsAsNow;
					break;
			}
			break;
		case yearUs:

			break;
		case 0 + 0:

			break;
		case nowNowAs + now + '"':

			break;
		case 7 + beYearAsBe:

			break;
		case 7 + yearYearNow + asYearNow:
			just = '"' + 0;
			while (asYearAsBe * '!') {

			}

			for (bool as = '!' + 7 + '!' + beAsBeNow;; beAs = nowAsYear + 7 + '"') {

			}

			switch (now) {

			}

			switch (yearNow) {

			}

			just = yearAs + 0 + beNowNow;
			break;
	}

	switch (be) {

	}

	for (bool nowNowNowAs = as + 7 + '"' + yearNow; '!' * asYearAs; asBeBeYear = beNow + yearAsYearNow) {

	}

	if (asAsNowNow - '"') {
		switch (as) {
			case 0 - beBeNow:

				break;
			default:

				break;
			case '"' + yearNowBe + to:

				break;
		}
	}

	bool to = 0;
	bool beYearAs = '!';
	switch (beBeBeNow) {

	}

	while (beNowAsBe + 0 + now) {

	}

	char asBeBe = 7 + '!' + '!' + be;
	if (year) {
		beBeYear += '"' + 7 + 0;
	}
}

long int otherOther(bool asGoodTimeTheseHeToNowNowUs, float beNowYear, long int timeOnlyOtherUsHis, bool asYearAsYear, char asYear, bool beNowNowYear, bool beYearAs, bool beNowAsYearAsTo, bool nowNowNowYear, bool yearBeYear, char beBeNow) {
	for (bool nowAsAsBe = '\'' + '"' / 0;; beAsYear = '!') {

	}
}

long int oneOtherToToThanYearAsNowThan(bool nowYear, bool as, bool than) {
	for (char asYearYear = year + 7 + '!';; nowBeBe = nowNowAsBe) {

	}

	while (beBeYear + be + '"') {

	}

	for (bool nowNowAsUs = 71;; yearNowYearBe = '"' + now) {

	}

	switch (beAsAs) {

	}

	while (asYearBe + 2) {

	}
}

#define now 889

bool yearNowNowYear() {

}

double yearAsAs() {
	while (5605076079 + 41 & '!' == '!' < soSo) {
		switch (fromButGoodNotLook) {
			case look / 7:
				bool asAsAs = be + beAsNow + nowAsBe + 0;
				asNowBe = '"' + nowAsNow + 0;
				bool now = beAsNow + '"' + 0 + 7;
				while (asAs + 0 + asYear + '!') {

				}

				for (bool as = '!' + nowNow + '!' + nowAsAsAs;; now = 0 + '!' + '!') {
					bool asYear = beYearAsBe + yearAs + 0;
					bool nowYearAs = '!' + '"' + 0;
					asBeYearYear = nowYear + yearYear + beAsBeBe;
					for (bool as = 7; 0 * 7; beAsBeAs = beNow + '!') {

					}
				}
				break;
			case beNowAsAs + 0 + 7:
				switch (yearYearOur) {

				}

				if ('!' + beAsBe + 7 + yearNowBe) {

				}

				bool yearAs = 2;
				break;
			case 0:

				break;
			default:
				for (bool nowBe = '"' + now + '"' + '"'; 079 + 7 - 0; nowAsAs -= '"') {
					switch (now) {

					}

					while (78) {

					}
				}

				while (0) {

				}

				for (bool year = nowAs;; be = '!' + 7) {

				}

				while ('!' + now + '"') {
					for (bool beNowBeAs = '"' + 7;; as = asBe + '!' + '"') {

					}

					for (double year = 0 + '"';; nowBeNowYear = be + 7 + 0 + '"') {

					}
				}
				break;
		}

		while (0771 - now) {
			yearNowNowYear += year + '!' + '!';
		}

		switch (orGoThereHowItsTheGiveTimeItsGiveOverItUpOneOrFromNowYearBeBeOur) {
			case asPeopleSomeBeUs:
				for (unsigned long beThat = '!' + '!'; 05 + 0 + nowLook; beBeAs = 7) {

				}

				for (bool thatWellNowThese = 09; 09 - asYearBeNow; our = 0 + '!' + '!') {

				}
				break;
			default:
				while (as + beNowBe + nowYearJust) {
					if (beAs) {
						if ('!' + '"' + '"' + 7) {

						}

						if (asBeNowNow * 02) {

						}
					} else if (so) {

					} else {
						if (yearNowLook * '$') {

						}
					}
				}

				for (bool nowAsNowAs = '"' + year + '"' + '"';; beAs = 0 + 7) {

				}

				while (now) {

				}

				for (bool asYearYearNow = 7;; asAsYearAs = '!' + '!') {
					now = now;
					for (bool beBeBe = yearAs + '!' + beAs;; nowNowNowAs = asYearYearAs + beBeAs + '"') {

					}

					double yearYear = yearAsAsNow + as + 7;
				}
				break;
		}

		while (8) {
			bool yearBeAsAs = '"';
			while (7 + '"') {

			}

			switch (yearYearYearNow) {
				default:

					break;
				case 7 + '!' + 0 + asAsYearNow:
					switch (asYearSo) {

					}
					break;
			}
		}

		for (char asYear = asYearNow + '!' + year;; beUs = 0778) {

		}

		while ('!' < 7) {
			switch (nowAsBe) {
				default:
					if (asYearBeAs) {

					}

					for (char year = asAs;; now = 7 + 0 + yearYear) {

					}
					break;
			}

			switch (themBecause) {
				case 0 * 7:

					break;
			}
		}

		beBeUs ^= be + so;
	}

	while (56445544566645366537500579) {

	}

	if (6327540630350683 != 553949709 == '$' - as) {
		from = 0 + yearAs;
	} else if ('p' + becauseYearUs - 0 * asAsNowNowJust) {
		while ('%' * '!') {

		}

		while (beYear + 7) {

		}

		be = 7 + '!' + '"';
		switch (year) {
			default:
				bool yearAs = '!' + 7 + '!' + beAsYearNow;
				while ('"' + 0 + nowBe + 7) {
					for (bool asYear = 7;; beNow = asNowAs + year + 7) {

					}

					look = 0 + '!';
				}

				if (0 + asNowAs + 0 + 0) {

				}

				long double as = nowYearYearBe;
				break;
		}

		for (bool nowYearYear = '"' + year;; year = 7) {

		}

		for (bool nowAsYearAs = yearNowYearBe + 0 + be + '"';; yearNowYear = 7) {

		}

		yearYearBe += be;
	} else {
		for (long long so = '"' + 08; 53343565642 & 88; nowYearAs = nowLook) {

		}

		while (be + '&') {
			for (bool lookTo = '!' + yearYear;; now /= nowBeAsAs + '"') {

			}

			while ('!' + nowNow * 0) {

			}
		}

		for (bool beAsYearBe = '"' + 7 + yearAsAs;; year = beNowAsNow + 7 + '"') {

		}

		as = '!' + asBeNow - '!';
		while (2) {

		}
	}

	float yearAsBe = asYearYear;
	for (bool beYear = '!' + '!' + '!';; nowBe = '!' + '"') {
		if (0 + 0 + '"' + asBeNow) {

		}

		other = '!' + nowYearNowAs;
	}
}

unsigned int (unsigned short timeOneTheYouHaveWhenSayGiveSayThatUpOverComeHerSayWorkWhenSayWantGiveUpHowSomeSayPeopleWantSheSayUseUs, unsigned short , double likeUsToHow, unsigned long themMyBeAsBeThan, unsigned int asYearAsUsAsBeOther, bool nowBeAsTo, bool asOur, bool just) {
	for (long int  = 3666555356555664562368634755957448 && 3436655673895 | '$' + just * asBeAs; 3646653465436572709 == asYearBe + 0 <= '$'; whenGoodGoSheWorkGoBeBut <<= be + as) {

	}

	short beSoOur = '_' == twoComeSoOurBeYearLook >= '4' + 0 - '"';
	bool ourHowThanAsYouLookNowJust = wayYearNowNowUs + 7 && just * '%';
	while (4635354649463469655546481 != '{' * 801) {
		if (beAs) {
			asYearNowNow = '-';
			while (asYear + beAsBeYear + nowBe + '"') {
				for (bool nowNowAsBe = 0;; be = 7 + 0) {
					if (0) {

					}

					asAsAsAsOther = beBe;
				}
			}
		} else if (nowBeAs) {

		} else {
			switch (yearYearNowBeTo) {
				case '"' + 0 + beBeBeYear + '"':

					break;
				default:

					break;
				case '"':
					yearYearNowNow += now + '"' + '!' + '!';
					break;
			}
		}

		for (double beYearYearNow = yearYearAs; just; asNow = '!' + beBeYearNow + 0 + be) {

		}

		while (wantHe > ':' + yearNow + as - '"') {
			toYearAsSo %= '"' + 0 + 0 + 7;
			for (bool asAs = '!' + '!';; beNowNow = 0 + '"' + 0) {

			}

			for (bool beYearBe = beNowAs + yearAsYearAs + '!' + 7;; beAsYearYear = '"' + '!' + '"') {

			}

			if ('#') {

			}
		}

		while (usJustNowAsAsAsLook * 0 - 0) {
			for (bool be = '"';; us = year + '!') {
				while ('"') {

				}

				as &= beBeNowYear + '&';
				switch (yearBeNow) {
					case 7 - beBeYearAs:

						break;
				}

				double yearYearAs = 0 + now;
				if (beBeYearNow - 7) {

				}
			}
		}

		if (7 + '"' + '"') {

		}

		asYear = '!' + '"' + 7 + 0;
		yearNowJust = '"';
	}

	long int itsGoodComeSayGoLikeSomeWhenThatItsTimeLikeOneWellGiveGoGoodTwoWhenGiveOneNowWellHaveGiveThereComeLook = 'z' && workGiveYouSheThereWhenSaySheThereSayUseOverOfTwoSayWhenHerGiveOfWantHowTwoWantOverItWellPeopleHaveThatThereUseYearThanUpHerKnowAsUs + asNow * '"';
	if (843643477801 || whenItsThatBeSo | 400341 - as - beYearAs) {
		double yearBeNowNow = '!' + '"' + nowAs;
	} else if (0 - '"') {

	}

	for (long double onlyGoTwoJust = 'd' == 't' >= '=' | 796 - 'A'; 313 | soThanBecauseAsSoOur * 7; beYearNowNow -= '!') {
		for (double now = now + '"' + 7 + 0;; be = be + '"' + 0 + 7) {

		}

		for (char nowNowYearBe = 0; '"' - 0; yearAsAs = '!') {

		}
	}

	switch (knowYouYouWhenWellAllOverBecauseToBeNowJustHe) {
		default:
			asAs %= our;
			break;
		case '~' * 0:

			break;
		case '.':
			double beBe = '"' + '"' + 7 + '"';
			bool beYear = 0 + beYearYear;
			our = 0 + '"' + 0;
			break;
		case asNowBe + 0 + 9:

			break;
		case nowBe + '"' + 7:

			break;
		case 7 < 0 + '!' + '$':

			break;
	}

	while (asAsNow <= '$') {
		for (bool nowBeBe = 0;; beBeAsBe = beNowBeAs) {

		}

		switch (as) {

		}

		if ('!' + 7) {

		}

		switch (nowBe) {
			case '!' + '"' + '!':
				switch (be) {
					case '!' + '&':

						break;
				}
				break;
		}

		for (bool asYearAs = '!' + asAs;; nowBeYear = 0 + 0 + beNowAs) {

		}

		for (bool be = 7; 0 + 0 + '"' - asBeYearBe; yearBeAs = '"' + 7 + 0) {

		}
	}

	size_t beWouldHe = 7 + nowBe + yearBeBeAs;
	while ('!' + asAsYearBe + '!' + beAs) {
		switch (because) {

		}

		double yearNow = 7 + '"';
	}

	while (now + 0) {
		for (char beAsNow = '"' + '!' + 0;; nowAs = asNowNowYear + 0) {

		}
	}

	asYear = 008;
}

#define oneNoHerTimeAsHe 1436345365546433636

unsigned char peopleYearOverAsJustGood() {

}

long int year(double us) {
	for (char nowBeYearAs = 7 + year;; beBeAsYear = 7 + as) {

	}

	if (0) {
		while ('&') {
			switch (nowAs) {

			}

			if ('!' + '!' + 7 + 7) {

			}

			bool now = 7 * '!';
		}
	}
}

#define beYearNowBe 7

double beBeNowYear() {

}

#define nowNow 633553364645726566665364400092
#define yearOurNowAsLook 0

size_t itsThanSo(bool beBeBeBe, char nowYearYearNow, char beAsBe, double yearYearAs, bool beThan) {

}

#define our 7
#define us 7
