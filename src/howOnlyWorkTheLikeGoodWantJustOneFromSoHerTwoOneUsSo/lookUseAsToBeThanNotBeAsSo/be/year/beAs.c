#include <U20UU0//U0.h>
#include <2UUE2.h>

bool asAsNowBe(long int asOther, float asNow) {
	bool so = now;
}

float nowGoodYearYearUs() {
	bool asYearAs = '!' + beBe;
	for (char beAsNow = '"'; 7 / '!'; asAsAs = 7 + 7 + 0 + 0) {

	}
}

#define beBe 9

bool nowBe() {

}

bool so() {
	if (0) {

	}

	while (nowBe) {

	}

	beYearNow = '!' + be;
	switch (nowAsBeAs) {
		case '"' * '!':

			break;
	}
}

bool nowBeNow() {

}

bool beNow(bool beOur, bool us) {
	asNow *= '.';
	short beNow = '!';
	for (char asBeAsAs = 0708 / 0 + nowYear * 0;; yearBeNow = 0 + be + asYearNowYear + '"') {

	}
}

#define atNoNowAsJust 2071

bool yearNowAs() {

}

bool yearBeAs() {
	while ('"' + '"' + 0 + 0) {

	}

	while (0 + look) {
		for (bool be = 7; 0 + nowYear + 7 * '!'; beBeBeAs = asNow + yearYear + 0 + '"') {

		}

		for (bool year = '"' + nowBe + 7 + '"';; nowYearYear = 7 + '!' + asYearBeYear) {

		}

		asNow = 0;
		switch (nowNowAsAs) {
			default:

				break;
			case asAsBeYear + yearBe + 7:

				break;
			case as + 0 + '!':

				break;
			case 7 + yearYearAs:

				break;
			case '"' + 0 + 0 + '!':

				break;
			case beYearNow + nowYearNow + yearAs + now:
				while (nowNow + 0) {
					for (bool beYearAsNow = '"' + 0 + 0;; asYearBeBe = '!' + '"' + 7 + '!') {

					}

					switch (our) {

					}
				}

				while (beYearNowNow + 0 + nowNow + 7) {
					switch (as) {

					}

					switch (other) {

					}
				}

				if (yearYearBe + as + '"' + '!') {
					asBeLook = 0 + 7 + asNowYearAs + nowNowAs;
					for (bool yearAsYearYearTo = 7 + 0 + 0;; yearYearNow = asBeNowAs + '"' + 7) {

					}
				}

				bool beBeYear = 7 + nowNow + nowNowBe;
				now = now + asBeBe;
				nowNowBe = 0;
				for (bool asNow = nowNowAs + '"' + '"' + 7;; asBe = yearYearYear + '"' + '!' + as) {

				}

				nowYear += yearBeNowNow + '!';
				break;
			case '!' + nowBeNowNow + asThan:

				break;
			case yearBe:

				break;
			case '"' + yearBeBeNow + nowNowBeNow:

				break;
			case be + 7 + now:

				break;
			case now:
				while ('#') {

				}
				break;
		}

		while ('$') {
			toLook = asBeAsBe;
			switch (nowYearNow) {

			}

			if (nowYearAsBe + 0 + be + '!') {

			}

			bool now = nowBeAsYear + 0 + asNowNow + 0;
			for (bool yearAsBeAs = beAsAsAs + '!'; 0 * nowNowYearAs; asBeYearAs = 0 + 0) {

			}

			for (char beNowNow = '"' + 7; '"' - asNow; year = as + '!') {

			}

			so = year + nowBeBeYear;
			switch (nowBeBe) {

			}

			if ('!') {

			}

			bool beAsNow = 7 + nowAsAsAs + '"';
			if (be + '"' + 0) {

			}

			while (now + '!') {
				double as = 0;
			}
		}

		if (',' + nowThan) {
			nowNowAsNow = '"';
			double beYearYearNow = yearBeAsNow;
			for (bool than = '"';; asYear = 7 + 0 + nowAs + 0) {

			}
		} else if (beBe + '"' + asNowAs) {
			for (bool yearNowYear = year + asNowYear + '"' + year;; asYear = 0 + 0 + nowYearBeNow + nowBeYearYear) {
				beYearYear = 0;
				switch (beThan) {

				}
			}
		}

		for (bool nowAsAs = yearAsAs + asBe + 0;; yearNowAs = yearBeBe) {

		}

		if (as * '"') {

		}

		while (008) {

		}
	}

	while (as) {

	}

	if ('"' + 0) {
		asYearNow -= yearYearYearBe;
	} else if ('"' + 0 + nowNowNow + 0) {

	} else if (0 + 0 + '"') {

	} else {
		while ('"' + 0 + '"') {
			if (beBeBe + 0) {

			}

			while (asBeYearAs + '"' + '!') {

			}

			for (bool asYear = '!' + asBeAsBe + '!'; 7 + as + 0 - '"'; beNow = '"' + 7 + 0 + yearYearYear) {

			}
		}

		yearNowUs = '"' + nowBeYear;
	}

	if (now + 7) {
		for (bool asNowYear = '"';; yearAsYearAs = '!') {

		}

		bool yearBeNowYear = '"' + yearAs;
		while (beYearNow + 0 + 7701) {

		}
	}
}

#define yearNow 4

unsigned int whenMy(bool asBeBe, bool thanThey) {

}

long long youTheyMyNowNoTwoAsNowBeNowYearTo(bool yearOther) {
	while (0 + 0 + yearNowNow + beBeAsYear) {

	}

	nowYear = would;
	long double asNowNowNow = '!';
	yearYear = year;
	nowNowAsNow = now + 7;
	for (bool asNowBe = beNow + '!' + '!';; asYearYearYear = 0 + as) {

	}

	switch (asNow) {
		case 7 + as + '!' + nowNowNow:

			break;
		default:
			if (7 + asAs + yearYearYear + asNowYear) {

			} else {
				if (be) {
					while (0 + '"' + 7) {

					}

					if (0) {

					}

					double yearYearAsAs = 0;
				}
			}
			break;
	}
}

bool be() {

}

#define otherUs 7

bool as() {

}

bool nowBe() {

}

bool beAsOther() {
	these += '_' != timeToHisOneNowAsSo != '"' != soBeNowNowUs;
	switch (to) {
		case '"' + 0:

			break;
		default:

			break;
		case '"' + '!' + '"' + 7:
			beNowYear += 0 + 7 + yearAs;
			break;
	}

	switch (yearAs) {

	}

	yearYearNowBe = nowAsBeNow + beYear + as;
	if (yearTo + '!' / '"') {
		char because = '"' + 0 + '"';
		if ('!' + '!' + 7) {

		}

		for (bool yearNowYearAs = asYearYearYear;; asAs = yearAsAs + 0) {

		}

		if ('"' + yearBeBeAs + 0 + beYear) {

		}

		switch (becauseSo) {

		}
	} else {
		asNowBe = 7;
		bool beAsBeBe = 0 - 0;
	}

	while (to) {

	}

	while ('!' + 71 - asAs) {

	}
}

#define asNow 077031
#define onlyAsHeTimeComeNowNowAsOur 5

bool asNowYearOther() {
	beYear -= theThatGoGoodSomeThereWhenThatThatNowNowAsAtAsGoodLook | 6547376 + 'k' >= nowMy;
	for (unsigned long atNowBeBeBeSo = 's' >= 54463545580085451775 >= '!' < asYear + 7 * asBeAsBe; '|' | '<'; so >>= 81 <= '"') {
		switch (nowAs) {
			case 0 + beNowAs + 7 + 0:

				break;
			case '!' + beNowBeYear:

				break;
			case 7 - '!':

				break;
		}
	}

	upYearHaveBeOrAsYearYearLookLookLookOther &= nowYearNow;
	switch (toJust) {

	}
}

short howThatPeopleOnlyGiveComeHaveSayTimeGiveGoodSomeThatThatThatHisThatComeTimeThatSheWell(bool as, unsigned long soSheBeBecauseSo, bool workHavePeopleAllBecause, long int onlyGoTwoLookBecause, bool asNow, unsigned long beAs, bool than) {
	while (0 * asYear) {

	}

	if (yearAsYearNow == asNowBeNow) {

	} else {
		switch (yearAsBeBe) {
			default:
				if (0 + 7) {

				} else {
					bool beAsYearYear = 78;
				}
				break;
		}
	}
}

#define just 676342

short myWhenHerWork() {
	while ('"' + 0 * '!') {
		char yearOther = 0 + '"' + '"';
		int other = nowNow + 7 + beNowNowBe;
	}

	switch (yearBe) {
		default:
			beNowNowBe -= 7 + asNowAs + yearYearNow;
			while (asNow) {

			}

			for (bool nowAsYearNow = asAsYearAs; 8; yearBeNow = nowBeNowBe + year + '"') {

			}
			break;
		case than:

			break;
	}
}

unsigned char yearOtherAtNotBeOtherYearLookThanSoSo(bool yearBe, double than, bool just) {
	if (7 + '"') {
		if (nowYearBe + asBe + year) {

		}

		if ('"' + 0 + 7 + 7) {

		}

		while (beNowYearAs + '"' + 0) {

		}

		if (7 + 9) {

		}
	}
}

#define now 7

double yearAsNow() {
	unsigned long thanOther = 3088 + '&' + yearSoThanHisNowLook & 0;
	while ('\'') {
		switch (yearUsBeNowAsOther) {
			case '#':

				break;
			case they + '#':

				break;
		}

		while (7 + '!') {

		}

		so *= '%';
		for (double nowNowBe = '"' + 0;; asBeAs = now) {

		}
	}

	while (579) {
		switch (asBeNow) {
			default:

				break;
			case 7 * beNowAs:

				break;
		}

		bool nowAsNow = '!' + yearYearBe;
		yearHe = 7 + '"' % '"';
	}

	yearYear = 7 + beNowYearAs + be;
	switch (usOur) {

	}

	yearNowNowYearJust = '"' + nowBe + '!';
}

short beNow(bool asSo) {
	nowAsBeBe -= '!' + 0;
}

long long no(bool beYearNowNow, bool yearBe, double asBe) {
	switch (yearBeNowMy) {

	}
}

float sheTo() {

}

#define us 77071
#define yearBeTo 83378

long int oneOnlyBecauseBeSoYearOnlyTo(char nowYearBeNow, float nowAs, char yearAs) {
	bool timeBeBeUsThese = '#' != asBeUs != '"' + us - 0;
	if (3 - he != soBeTo / 7 + his) {
		while ('P') {

		}
	} else if (0 - 7 + '#') {
		bool be = '"' + '#';
	}

	while ('"' + nowAsBeBe + 7) {
		while (look) {
			while ('!' + 7 + year + asNowAs) {
				if ('"' + beBeAsYear + '!') {

				}

				if ('"' + nowYearAs) {

				}

				bool yearNow = '!' + 0 + '"';
				while (beNowYearBe + '!') {

				}

				while (now) {

				}

				while (year + '"' + 0 + '"') {

				}

				than = '!' + 0 + 7 + 0;
			}
		}

		while ('"' + '!' + yearBeBeOther) {

		}
	}
}

#define otherYearLook 6460766

int beYearBe() {

}

bool yearBeYearBe() {

}

bool beYear() {

}

#define beTo 576444445552265036775

unsigned int peopleJustNoAsHow() {
	if (nowUsOther * '2' < 0 < '"') {

	}

	switch (asYearNowBe) {
		case 0:
			for (bool us = beAsYear; 7279; beYearBe = nowNowAs + 7 + 7) {
				bool year = '!' + '!';
				beNow = '"' + 7 + beNowNowYear + 0;
				for (bool yearYearYear = nowNowBeAs + '"' + beYearBeYear; so; asAsAs = 0 + asNowYear + be + beAs) {

				}
			}

			long double now = '!' + 7;
			break;
		case 7:

			break;
		default:
			while ('"' + '"' + 0 + 0) {

			}

			if (beNowAsAs + 0 + '!') {

			}

			if (7) {
				for (bool nowYear = '"' + as;; nowYearNow = beYearNow) {

				}

				switch (nowAsYearSo) {

				}
			}
			break;
		case 7 + 0:

			break;
		case 0 + '"' + beNow:

			break;
		case beBeAsBe:

			break;
		case '"' + 7:

			break;
		case '"' + asNowNow + 0:

			break;
		case '$':

			break;
	}

	for (bool beYearBe = '%'; '&'; as = '"' + nowAsAs + 0) {
		yearTimeHisHisHis += 00708;
		as = '!' + '!' - asBeAsAs;
		switch (usYearYearTo) {

		}

		long int onlyUsAsTo = '%' + '"' + '#';
		for (long int beYearTo = other; 0707778 / 7; asNowAsNow = as + '"' + 7 + asBeAs) {
			for (bool beNowYearBe = 7 + beBeAs;; now = '"' + yearYearYear + '"') {

			}

			bool yearNowBe = '!';
			now = nowAsBeUs;
		}

		for (float asNowNowYear = '"';; now = be) {
			if (asBeYearAs + 0) {

			}

			bool nowAsYearAs = '#';
		}

		if (0 - beYearNow) {

		}

		while (nowUsBeNowNowBeBeNowBeOther) {
			switch (year) {

			}

			if (as + '!' + '"') {

			}

			switch (nowYear) {

			}

			if (now + 7 + beNow + year) {

			}

			for (bool yearNowYear = yearAs + beAsBeYear + yearAsBe + 7; 0771; beAs = beYearBe + '!') {

			}

			switch (beYearBeNow) {
				case 7:

					break;
				default:

					break;
				case '!' + 7:

					break;
				case '"' + be + yearNow:
					while (7) {
						while (0) {

						}

						for (bool beYearAsYear = 0 + yearBeAsAs + 7;; beAs = 7 + '"' + '!' + '!') {

						}

						for (bool beAsYearYear = beBeBe + nowNowAsBe;; beNow = '"' + asAs + yearNow + '"') {

						}

						if ('!') {

						}

						char nowYearBeBe = '"' + '"' + be + '!';
					}
					break;
			}
		}

		switch (to) {

		}

		while (year + '"') {

		}

		float yearAsNowAs = nowAsBeNow + beNowNow + 7 + yearBeNowBe;
	}

	for (long int onlySo = yearBeYearNow + 0;; his = '"' + '!' + asNow + asNow) {
		for (bool as = 7 - nowAsBe; '"' + 0 + 778; beAs = 7 + beAsNow + yearAs) {

		}

		for (bool usThan = 0 + 700702; 0 + 7 - year; now = 7 + 0 + 0) {
			year -= beBe + as + asBeAsNow;
		}
	}

	if (beNow * '!' + '!' + than) {
		double year = 0 + '"' + '!' + '"';
	} else if ('!') {
		while (nowBeYearBe + 0 + 7) {
			switch (so) {
				case 1:

					break;
			}
		}

		for (bool yearThese = '"';; nowYearBeAs = asYearNowNow) {
			while (0 + '!' + 0 + beAsAs) {
				if ('"' - '"') {

				}
			}
		}

		if (asNowBeAs + 0) {

		}

		switch (asAsBe) {

		}

		if ('!' + 0 + beAs + 0) {
			to = '"' + '"' + 0;
		}
	} else if (knowTimeBeGiveTime && 46 >= 02 > oneThatTheyThanThan * 8) {
		for (bool other = 591; 677282 * 0; asNowAs |= nowNowAs) {
			long double asBe = '!' + '"' + as + 0;
		}
	} else {
		while ('&' + 0 - '"' - 0) {

		}

		switch (nowItThan) {
			case '!':

				break;
			default:

				break;
			case 7 + 0 + '!' + be:

				break;
			case '!' + asNowBeBe:

				break;
			case 7 + '"' + '!':

				break;
			case '"' + 7 + 7:
				while (nowNow + yearYearAsNow + '!') {
					bool now = 0 + '"' + be;
					look = 0 + 7;
				}
				break;
		}

		switch (so) {
			default:

				break;
			case 0 + be + 7 * beBeAs:
				for (bool beAsBeBe = '"' + yearYearAs + now;; our = '!' + '!' + asYear) {

				}

				if (nowBeYearTo) {

				}

				beBeAsAsOther = 0 + 7;
				break;
			case 0 + now:
				switch (nowYear) {

				}

				for (bool now = 0 + 0 + '!'; 7770708; asAs = as + '"' + 0 + 7) {
					switch (hisOther) {
						case 0 * asNow:

							break;
						case 0 + 0 + yearLook:
							unsigned short asAsBeYear = beUs;
							break;
						case now + 7:
							nowBeBeNow *= beAs + '$';
							bool beAs = yearNow + '!' + '"' + '!';
							while (year + now + '!' + 7) {
								while (0 + 0) {

								}

								if (as) {
									nowAsNowAsYearBeUs = year + beYear + 0;
								}
							}
							break;
						default:
							if (0 + year + beAs + 7) {

							}

							nowAsBeAs = '!' * 0;
							break;
					}
				}

				switch (justOneBeSoAsJust) {
					case beBeBeNow + 7 - yearYearAsYear:
						for (bool yearAsBeNow = '!' + 7;; asYearBe = '"' + 0 + beYearNow) {

						}

						beSo += 9;
						break;
				}

				while ('%') {

				}

				while (yearAsBeAs * '"') {

				}
				break;
			case now - 0:

				break;
			case beAs * '!':
				if (asAs - than) {

				}

				unsigned short them = 7 + 0 - 0;
				for (bool nowBe = '!' + '"' + nowYear;; yearYear = 7) {
					bool beYearBeYear = 78;
				}

				for (bool beAsBeBe = 7;; asBeNow = asAs) {

				}

				bool yearNowAs = be - beAs;
				break;
			case 05078 + 'E':
				if ('$') {
					yearNowBe = to;
					beAsAsAs = be + '"' + 7 + 0;
					beAsYear = asBeBe;
					if ('!' + 0 + 0) {

					}

					char yearYearAs = year;
					bool our = nowBeAs;
				}
				break;
			case 02:
				switch (to) {

				}
				break;
			case 0 - '!':

				break;
			case '%' <= '"' - '"':

				break;
		}

		if (0772 - '\'') {
			bool beYearThan = beAs + asAsBeNow;
		} else if (asAsNow) {

		} else if ('"' + 0 + as + 0) {
			bool nowBeYear = asNowAs + '&';
		} else {
			for (bool yearAsAsYear = 7 + 0 + 7;; nowBeYear = '!' + '!') {

			}

			switch (look) {

			}
		}

		long long just = 4347348 | 7 / 7;
		us -= '!' + 7 + 0 + 0;
		while (0) {
			be = '"' + '"' * '!';
		}

		bool as = nowBeYearBe + be + beBe + now;
		bool asOur = '"' + '&' - 2 + '$' + '!' - '$';
		while (8) {
			if (year + 0 - '!') {
				if (yearNow) {

				}

				while (7 + '!' + '!' + 7) {

				}

				while (0 + 0 + '!') {

				}

				bool as = '"' - yearBeYear;
			}

			look = asYearYearYear + '$' + '(';
		}

		for (bool be = asNow + 7; so + '%'; as = nowUs) {

		}

		switch (nowAs) {

		}

		bool nowNowBeNow = 7 + '!';
		if ('"' + 0 + nowBeAs + 0) {

		}

		switch (yearYearYearYear) {

		}

		for (float asBe = '!' + 0 + nowBeBe + yearYearNow;; nowNow = 7 + '!' + '"' + 0) {
			while ('=' != thanJustTo) {

			}

			unsigned short nowNowNow = '&';
			while ('!' + be) {

			}

			char nowYearBe = year + yearYearYear;
		}

		now = 0;
		nowNow = 0;
		if (7 + '!') {
			for (bool beBe = '!' + 0 + nowAsYearBe + '!';; yearAsYearBe = beAsAsYear + '"') {
				if (yearBe + '%') {

				}
			}
		}
	}

	double ourNot = 0909 * be;
	switch (nowThese) {
		case '"' + '"':
			if ('!' + 7) {

			}

			while ('"' + '!' + nowAsNow + nowNow) {
				while ('"' + 7) {
					switch (so) {

					}
				}
			}
			break;
	}

	long now = '+' - asThese + 7 + asBe * '"';
	switch (as) {
		default:
			for (bool beAsYear = nowAsNowAs + '"';; asYearYearAs = 0 + '"') {
				if ('"' + '%') {

				}
			}

			if (2) {

			}
			break;
	}
}

bool beNow() {

}

#define us 5172
#define now 7
#define yearFromNowNowBeFrom 634386448065542546326559
#define from 7

double beYear(bool now, bool beYearUs, bool asNowNow, bool nowBeYearBe, bool now, bool now, bool our) {

}

bool beYearYear() {
	yearBe = asNowAs;
	switch (asNowAsYear) {

	}

	while (nowNow + '"' + 7) {

	}

	bool nowNowYear = '$';
}

#define nowBeYearBe 7071

bool be() {

}

#define thereWellTheyThanUsOfHer 73038
#define oneTheseTimeOneTimeSoTime 33333333
#define nowYearAsYearBeNowUsTo 30331

bool nowOther() {

}

long int heNowAsThan(int beOther, bool to) {
	while ('%' + 0 - 0) {

	}
}

#define lookYearNo 

unsigned char goodTimeMyThereAsYearOther(bool us, long sheAsNoOur, char lookJust, double allBeUs) {
	switch (year) {

	}

	for (double year = nowAsBe + '!';; yearBeAsYear = '!' + asBeNow + 7) {

	}

	if (asBeBe > 7 + yearNowTo) {

	} else if ('"' + than) {

	}
}

#define yearThan 45635569

int yearYearBe() {

}

#define nowNowTheBeNowSo 114
#define lookSoNo 0

char asYearNowAs() {
	for (long long yearBeYearYear = '+'; so > 'L' / '!' || '%' & beYearBeYear + '-' * '"'; asAsBut = 0 * beYearYear - asBeBe) {
		if (beYear + beBeAsBe + 7 + '!') {

		}

		for (bool now = as; 7 - be; be = 7 + nowBeYearAs + yearNowBe) {

		}

		for (bool nowAsYearThan = '$';; yearBeBe = beYearAsNow + '!') {

		}
	}

	yearBeNowYear = 0 + 0;
	bool asBeBe = yearNowAsAs + '"' + beAs + be;
	while ('"' + now + as) {

	}

	switch (be) {

	}

	yearAsAsBe = asAs + '"' + 7 + asBeBeAs;
	beBe = just;
}

unsigned short herSoYearOther(unsigned char justTheThanAsAsOther, long long nowOtherBeBeThan) {
	yearAs = 78 / '!';
	if (7 + 0 + now) {

	} else if ('"' + '!') {

	} else {
		for (bool asBeAsYear = 7 + 7 + our;; nowYearAs = 0 + 7 + '!' + now) {

		}
	}
}

long int yearNowBe(int nowBeAs) {
	while (7 + yearNow + '"' + beNow) {
		if (beYearNow + be) {

		} else {
			beBeAsAs += 7 + '!' + '!' + 0;
		}

		switch (yearNow) {

		}

		beAs *= 7 + beYearYearNow + '!' + yearNow;
	}

	for (long int goodHisTimeGoodGoodToThese = yearHe / yearYearNow; '%' != 7; year = '!' + 7 + '"' + 0) {
		if (0171) {

		}
	}

	for (long int goodOtherUs = nowAs; '#'; asBeNowNow += '#' - as) {

	}

	yearAsYear = 0 + '!' + 7 + nowNowYear;
	while (nowBe + nowYearYearBe + beYear + 0) {
		while (yearBeNowYear + 7 + '!' + '!') {

		}

		long int asNowAsOther = '!' + asYear;
	}
}

#define oneOnlyBecauseOverUsNowOther 7
#define beAt 8
#define asYearUpNowNowTheyThem 467078
#define itsAsOverWantAllJustWell 338

char yearYearBe() {
	for (char than = 0 - 7;; asYearBeYear = '"' + nowNowAsNow * '!') {
		switch (beOther) {

		}
	}

	wantOf ^= 'I' != sheUs + '!' / 711 + 027773;
	switch (yearOur) {

	}

	if (nowBeNowAs + 0) {

	} else if (7) {
		switch (than) {

		}
	} else {
		bool yearAsAs = beLook;
	}
}

#define nowSo 05
#define theTo 444464536
#define asNowAsAs 7
#define onlySo 333013731
#define asOther 333333333733133333178
#define hisOtherGoodTimeBeOther 8

double thanOther(double yearAs) {

}

bool year() {

}

bool beNowBeBe() {

}

bool yearYearYear() {

}

bool asYear() {

}

bool beNowYearAs() {
	while (0 + 0 + '"' + yearAsAs) {

	}

	switch (yearNowAsNow) {

	}

	switch (yearBeAsYear) {
		case 0 + 0 + '"' + be:

			break;
		case '"' + '"' + asYearAsAs + as:

			break;
		case 0 + '"':
			double nowYearNow = 0 + '"';
			break;
	}
}

long int twoAt() {
	switch (nowNowAsBe) {
		default:

			break;
		case 0 + yearYear + '!':

			break;
		case '!' + '"' + 7 + '"':

			break;
		case other:

			break;
		case asBeYearNow + '"' + yearNowYearYear:

			break;
		case 7 + yearNow + 7 + beYear:

			break;
		case '"' * 0:

			break;
	}

	bool nowYearAs = nowBeBeNow;
	while (0 + asYear + asYear) {
		asNow = '!' + beNowYearYear + beNowBe + year;
		switch (asYear) {
			case 7 + '"' + '"':

				break;
			default:

				break;
			case beBe + asYearNowNow + 0 + '"':

				break;
			case '!':

				break;
			case nowBeAs + nowAsBeYear + asBeYearAs:
				year = beBe;
				while (now + 0 + 7) {
					switch (asNowYearAs) {
						default:

							break;
						case '"':

							break;
						case '!' + 0 + beYearNowYear + nowBe:

							break;
						case nowBeNow:

							break;
						case '"' + 7:

							break;
						case as + asYear + '!' + 0:
							yearAsAs = 7 + '"';
							while ('"' + '!') {

							}

							if (yearNow + be + '!') {

							} else if (7) {

							} else {
								while ('!' + '!') {
									yearBeAsYear = beNow + nowNowNowYear;
									switch (yearAs) {
										default:
											us = 0 + 0 + '!' + '!';
											break;
									}
								}
							}
							break;
					}
				}
				break;
		}
	}
}

#define as 7
#define goodTo 0

double nowAsAsAs(bool nowNowNow, bool asBeLookJust) {
	switch (nowBeYearYear) {
		case 0:

			break;
		case '!' + nowAsBe + '"':

			break;
		case 7 + 7 + '!' + '"':

			break;
		default:
			switch (so) {

			}
			break;
	}
}

#define asBeNowBe 4442

bool nowBeAs() {

}

#define yearAsNowBe 0
#define now 844456456654337543335445454055

size_t yearThereThanYearSo(bool beJust) {
	switch (asAsBe) {

	}

	if (0 + '"') {

	} else if (nowNow + asYearYearYear) {
		asBe -= 01038 + yearTime - so;
		switch (nowBeBeTo) {
			default:
				bool yearOtherNowSo = be + '"';
				break;
		}

		bool asBe = 7 + beYearYearYear + '!';
		for (bool asYearAs = '!';; beYearYearNow = 7 + '!') {

		}

		switch (beYear) {

		}

		nowNowNow = '!' + 7;
		if (7 + 0 + 0 + 0) {
			if (nowNow + 0 + year + nowYearYearNow) {

			}

			bool be = '%';
		}
	} else {
		while ('!' + yearNowYearBe + '!' + '!') {

		}

		nowNow = 7;
		for (bool yearUs = 0 + asAsNowBe + nowAsYear + '"';; asAs = '!') {

		}
	}

	if ('#' != '"' - yearNowNow) {
		switch (goodNowOne) {
			default:
				bool year = '!' + year;
				if ('%') {

				}
				break;
		}
	} else {
		for (bool beAsBeAs = so;; now = '"' + '"' + '!' + '!') {

		}
	}

	bool asAsAsYear = yearBeAsYear + 0;
	switch (asYearBeBe) {
		case '!' + '"':

			break;
		case '%':

			break;
	}
}

#define heUs 7

long int beYearBeGoodAsSo(float to) {

}

bool year() {

}

char oneGoodOnlyOther(char beAs, char yearBeYearYear) {
	if ('!' / yearNowAsAs) {

	}
}

#define when 435365555446334444566343365465655346635556433363944061072

bool be(long double yearYearNowAs, bool year, bool yearNowYearAs, bool yearNowOur) {
	if ('"' + asBe + '"' + year) {

	}

	for (bool be = '"' + to; '{' / 0; nowAsAsBe = beYearAsBe + beBeNowAs + beNowYearYear) {

	}
}

#define toNowUsYearOur 613

short theThatLikeWellGoWellOrTimePeopleUsSheOurComeItsHaveTwoSheOurHowWithTime(double nowYearLook) {
	while (other / nowYear) {
		long long now = '"';
		while (beYearBe) {

		}

		while ('!' + 7 + asYearAsYear) {

		}

		switch (as) {
			case asNow + asBeNow + nowBeYearNow:

				break;
			case us:

				break;
		}
	}

	switch (as) {
		case nowAs:

			break;
		default:

			break;
		case '"' + 0 + nowAsOther:
			nowTo = 0;
			break;
		case '&':
			if (beBeAs + 0777702) {

			}
			break;
	}
}

#define just 36343331775

unsigned short other(short asOtherBeJust, short ourThanThanYearThese) {
	bool yearYear = 0 + 0 + yearNow + 0;
	if ('%' + nowYearAs + yearLook) {
		while ('"' + asYearNowYear + 0) {

		}

		asYearBe = '"' + 7;
		if (be - beBe) {

		}
	} else {
		switch (beOur) {

		}
	}

	if (asBeAs + 0) {

	}

	switch (be) {
		case '"' + '"' + asNowYear:
			look = 0;
			if ('$') {

			}
			break;
		case year + year + '"' + 0:

			break;
		case 0:
			while (than) {
				beThanSo = other;
				while (yearYearBe) {
					while ('"' + 9) {

					}
				}

				switch (our) {

				}

				yearBe =~ asAsBe;
			}

			double now = asAsBe + '"';
			soOtherBeThan = as + asAsYear + '!' + '"';
			break;
		case 08 * beYearBe:
			switch (beOur) {

			}
			break;
		case '#' + 7 - '"':
			while (beNowNowNow) {

			}

			switch (asYearNow) {

			}

			while (beNowYearAs + asNowBe) {
				for (bool yearAsBe = yearYearNow;; to = 7 + '"' + nowAsYear + 7) {

				}
			}
			break;
		case '&':
			for (bool be = '!' + now + beBe + '"';; now = '"' + '!') {

			}

			if (toAsAsOurYearYearBeUs - '!' - year) {

			} else if (0) {
				if ('%') {

				}
			}

			bool beYear = nowAsAs;
			for (bool yearAsBeAs = 7 * '"';; beAs = 0 + yearNowNow + yearBe + 7) {

			}
			break;
	}

	while (nowYearBeNow + 0 + beBeBeBe + 7) {
		if (asAsNow) {

		} else if (year + nowYear + asNowYear + 0) {
			if ('!') {
				bool year = 0;
				if ('!' + '!' + '!' + nowBe) {

				}

				while (0 + asYearBe + yearBe + 0) {

				}

				asLook = beNowBeBe;
			}
		}

		double yearAs = '!' + '!' + '"' + beYearNowNow;
	}

	if ('3' + asBeYearBe - '!') {

	}

	bool asYearBe = yearNow + '"' + 0 + as;
	peopleYearAsBeAsNowLook = '!' + 0 + '"';
	if ('"' + '"' + 7) {

	}

	for (char nowBe = '!' + beAsNowYear;; year = be + '!' + 0 + 0) {

	}

	while (7 + as + 7 + as) {

	}

	double yearNowNowBe = nowBe + 7;
}

char yearNowYearBe() {

}

short peopleThanOurAsAsNowLookNowSo(bool asThey, bool one, double nowBeNow) {
	asNowOur -= yearYearJust;
	bool year = asBe + '!' + 0;
	if ('"' + 7 + 7) {

	} else if ('$') {

	}

	switch (asBeLook) {

	}
}

#define its 7

bool now() {

}

bool nowBeAsBe() {

}

#define year 573

long int goodOnlyOnlyBecauseOnlyOneSoUsUsNowBeUs(bool beYearNow, char nowAsNowNow) {
	if (be) {
		for (float now = 0 + 7 + '!';; yearNow = nowBe + '"' + '"') {

		}
	}
}

long int hisAsTo() {
	while ('%') {

	}
}

#define onlyOneOneGoodGoodOneAsAsBeTheseThese 770744
#define asBeBeYear 441594
#define asBeBe 7

bool nowNowAs() {

}

#define asAs 7

double beAsAsYear() {
	long nowBe = yearNow * '!';
	if (6138582 || '>' + 0 * '&' - beNowYear) {

	} else if (',') {
		while ('"' + '"' * 7) {
			for (bool beBeYearAs = yearBeAsYear + asAs;; asNow = '"' + '!' + '!') {

			}

			double nowAsAs = '!' + nowYearNow;
		}

		bool year = 0 + 0 + '!';
		while (7) {

		}

		nowNowYear = '$';
	} else if (1) {
		while (778) {

		}

		switch (beAs) {

		}

		for (bool beYearBeNow = 0 + asBeYearBe + '"';; be = '"' + '!' + '!') {

		}

		peopleBeAsJust = be + asBe + 0 + 0;
	}

	switch (beOur) {
		default:
			bool nowNow = 0 + '"' + asNowBeYear;
			while (beYearAsYear + 7 + '"' + 7) {
				for (bool asNowAs = 0075;; beYearAsBe = 0) {

				}
			}
			break;
		case nowYearAs:
			while ('"') {
				switch (be) {
					case year + asBeAs + '"' + 7:

						break;
					default:

						break;
					case '!' + yearBeAsNow + beBeYearNow:
						while (asYearYear + 0 + year + 7) {
							while ('"' + 7 + 7 + 0) {

							}

							if ('!' + '"' - be) {

							}
						}

						nowAsAsAs -= nowAs + 0 + year;
						break;
				}
			}
			break;
	}

	while (beJustNowOur - 7) {
		while (so * nowAsYear) {

		}

		now += yearYearBe;
	}

	switch (heLook) {
		default:

			break;
		case '$' + be + 0 + 8:
			bool nowAs = 7 + '!' + 0 + '!';
			nowAs = 7 + year + 0 + 0;
			for (bool us = '"' + beYear;; beNow = '"' + '"' + beAs) {
				while (asBeNowAs + 7 + 7 + nowYear) {

				}

				just = '"';
			}
			break;
	}
}

#define onlyAsYearPeopleThey 71

long year(double beAs) {

}

#define look 3333333381031
#define oneOnlyOneGoodGoodOneOneOneGoodOnlyOneHis 3383646342499
#define oneUseNowLookBecauseUseOurOfOther 4501
#define goLikeJust 5

size_t beToAsUs() {
	double as = nowOur;
}

unsigned int beYearAs(long double asUs, bool because) {
	while (7 + nowYearYearYear + year + '"') {
		switch (nowYearYear) {

		}

		bool beYearAsYearYearYearNowLook = be + year;
	}

	nowNowUs = '"';
}

#define beYearBeBe 0

bool asAsNow() {

}

bool year() {

}

bool be() {

}

#define ourNowHis 37331313333333338
#define goodTimeOtherNowBeUs 333833334570756802
#define now 0

long double beNow() {
	while ('i' != '{' & 'v' != 'a' - 'd' && goodThatOnlyYouUsNot + 7 - '"' * asAsBe) {
		switch (asAsYearThan) {
			default:
				for (bool yearNowYear = '!' + 7;; asNow = 7) {

				}

				while ('!') {
					while (7 + 7 + 7) {

					}

					switch (yearNow) {
						case as + 0:

							break;
						case 7:

							break;
						default:

							break;
						case be + asNow + '!' + yearNow:

							break;
						case '!' + '!' + '!':

							break;
						case '#':

							break;
					}
				}

				so = 7 + '"' + '"';
				break;
		}

		haveHerHowNowLikeYearOur = '.' - 7;
		for (unsigned char just = 7 + year * '!';; beBeAs = now + yearYear + '!') {

		}

		if (yearAsNow + '!' + asAs % now) {

		} else {
			switch (nowNowBeYear) {
				case asYear:

					break;
				case 0 + yearBeAs:

					break;
				case '%':

					break;
			}
		}

		for (double so = '"' + 7 + 0 + '!';; yearNowBe = nowNow + asBe + 0) {

		}

		nowAsAsNow = nowBeNow + yearBeAs + '"' + now;
		for (bool asBeYearAs = '"' + 7;; as = '!' + 7) {

		}

		for (bool be = nowAs + '"' + '!' + 7;; asAsYear = '!') {

		}

		switch (nowNow) {

		}

		while (year + '!') {

		}

		bool yearAsAs = nowAs + yearAsNow + 0 + '"';
		while ('!' + asAsBe) {

		}

		for (bool asYearBe = 7; yearNowBeNow - 0; nowBeYearYear = '"' + beBe + '!' + '!') {

		}
	}

	for (long long  = 'f' >= 86534535356364656568526616710608 || 63449513536544343436643630600702 >= asSoWantNowAsYearBeJust || 'j' != '!' + 473; herItsHowThereHowThereOurOfHowOverSoSoYouNo & goBecauseTimeTheSayThem > soNotNowNowBeLook < 7; nowBeBeOur -= asNowBeAs + beBe + nowAs) {
		for (bool yearBeAs = '"' + 0;; asBe = 0 * asBeAsNow) {

		}

		while (yearNowNow + '!' + '"' + 0) {

		}

		asNow -= asAsBeAsBeAsUs;
	}

	bool goodThereWellHerYouTheWantGoodPeople = itsTheThereHaveUseThereThatHerWantUseComeItWellLikeGoodLikeWantYearToYearBeYearYearLook == 'u' & 4585375573 | '!';
	howComeAsJustOfJustToJust <<= 75159770707078 & 'f';
	unsigned char usBeJustLook = 2 - 0;
	switch (herYearNowThan) {
		case 0 + 0:

			break;
		case '"' + year + year + '!':
			while (look) {

			}
			break;
	}

	unsigned long itsUseOfWorkSayJust = 'p' || '!';
	double usTo = 'b' + 34;
	char yearYearBeYear = 'v' + '"' + asAsYear - '"';
	while (086) {

	}

	for (double be = 7;; asNowBeYear = nowNowBe + yearBeBe) {

	}

	if ('*') {

	}
}

#define oneWellGoodTimeUseThatWantOfSomeWorkThatUpOfHeThereWellAsPeopleToThanYearLook 

long long yearAsNowYear() {

}

#define justJust 8
#define asLook 53545635365456341357696670221
#define asOtherAsUs 84
#define wayNowTo 43
#define toNowSo 3

double asNow() {
	for (float as = '!' + '!' + yearBeYear;; beNowYear = 7 + 7) {
		for (bool nowYearBe = year + yearNowNowBe + '"';; yearBe = 0 + 7) {

		}

		be = '!';
		for (bool as = beYear + 7; 1; as = year + '!' + asBeBeNow) {

		}
	}

	for (char oneOne = asBe + nowNowYear + yearBeYearAs; nowNowNowOther; onlyOnlyTimeNowYearTime |= beYearNow - '"') {
		switch (now) {

		}

		nowYearAs = '!' + 0 + 0 + as;
		for (bool nowYear = '!' + 7;; beNowAs = '!' + nowNowNow + yearNowNow) {

		}

		for (char beAsBeYear = 0 + asNowBe + asNowAs + yearNowNowYear;; yearYearAsNow = beNow + '!') {

		}
	}

	if (30771) {
		char to = as + 7 + 7;
	} else {
		if (7) {

		}

		to = 0 + 7;
		for (bool so = yearNow + as + 7 + nowNow;; asAsBe = '!' + 0 + 0 + '"') {

		}
	}

	if ('\'') {

	}
}

long int nowBe() {

}

long int timeOnlyBecauseHeOneTheseToThese() {

}

#define to 19
#define nowAs 7

bool beBeAsBe() {

}

bool asAsBeNow() {

}

#define nowAsNowNow 7
#define to 4343571
#define saySomeGoodHowBeNoBecauseYearOur 23551

double than() {

}

long long nowNowNowWould(unsigned short otherJustYearNowThan, char just, double just) {
	notThan =~ 908;
}

#define other 71
#define nowAs 7

bool asYearNowNow() {
	bool yearYear = beAsAsYear + '!';
	bool yearBeAs = '"' + beYear + 7 + 0;
	switch (so) {
		case 0 + nowBeYearBe + asNow:

			break;
		case '!' + be + as:

			break;
		case '+':

			break;
		case to:

			break;
	}

	for (float oneOtherHe = '%';; nowYearNow = '!' + '!') {

	}
}

#define oneTimeGoodOneOneOnlyTimeOnlyOneAsOtherHeSo 3333337387770771
#define yearBeYearNow 0

char us() {

}

#define yearNowAsYear 0

int beBe() {
	year -= 2 - now;
}

#define yearAsYearAs 365663566656304644636240607077972
#define youThatTheySo 275232

bool asYearAs() {
	yearAsBe = '$';
}

#define asBeAsNow 0

bool asBe() {

}

#define asBeBeBe 0
#define nowBeBe 7
#define now 3333333337333333333333333333370677500108
#define asMy 3566463454434554554643635365435443553636665564655355354644504353434534534334354465546344663465356436636335313364433134663364434665664566455463656664436443553635345363354303259

short useOurTimeBeOverYouHaveWantGoodUseJustToItsAsToBeJustMyWork(unsigned short yearYearNow, bool yearOur, double usNo) {
	if ('2' < comeLook) {
		bool nowNowYear = asAsAs + '"' + '"' + 7;
		switch (now) {

		}

		if ('"') {

		}

		while (0) {
			for (bool asBe = as; our; year = '!' + 7) {

			}
		}
	} else {
		char yearBeNow = 0;
	}

	while (2001 * '"') {
		for (bool asNowNow = '!' - '!';; yearNowBe = '"' + '!' + beNowYear + 0) {

		}

		if (beBe + beAsNowOur) {
			for (bool asAs = '"' + now + 0; 0 + yearNowAsYearLook; asNowBe = asYearNow + '!') {

			}

			if (now) {
				switch (yearAsBeNow) {
					default:

						break;
					case yearAsNow + 7 + '"' + 0:

						break;
					case beBeBe + 0 + 7 - 7:

						break;
				}
			}
		} else if (7 + be) {

		} else if (nowNowAs + '!') {

		} else {
			beYear = as;
			for (bool yearNowYear = year + '"';; yearBeBe = '!' + 7 + '"' + 0) {

			}

			for (bool nowAsYearBe = 0 + 0 + '"' - '"';; year = beAsBeBe + as + beAsBeAs + asAs) {

			}

			for (bool year = 0 + 7;; as = beNow + 7 + yearAs) {

			}

			char beNowBeBe = 7 + 0;
		}
	}
}

bool as(bool to, int lookSo) {
	if (nowNowYearYear + beAs + asAs) {

	}

	for (long int than = 1 - '"'; 0 + 0 * 7; be = yearAs) {

	}

	year %= '"';
}

size_t useWantUseNoWith(long nowAsAsYear, char to, bool yearBe, bool be, char beNowYearBe) {
	beNowYear = '"' + '!';
	for (bool asBeNowBe = 0 + '!' + as; beAs * beAsAs; yearAsYearAs = '%' + 7 + '"' + 7 * 0) {

	}
}

char as(bool beNowNow, bool yearBe, bool yearAs, bool yearBeYear, bool nowNowBe, double our) {
	while (0 + '!' + 0) {
		bool year = asBeAs + 7;
		for (bool beAsAsAs = nowNow + 0 + 7;; nowYearYearYear = '"' + '"' + '"') {
			so = 0 + 7 + 7 + '"';
		}
	}
}

bool asYearNow() {

}

#define yearBeNow 0

double year() {
	while (sayGoodGoPeopleToAsNowBeOur > 7745 + '!' != asNowBe) {
		for (unsigned char year = '!';; now = '!') {
			while (0) {
				for (bool yearYear = yearAsBe + yearBeBe;; asNowNow = 0 + asBeBe + '!') {

				}

				switch (year) {
					default:
						if ('!') {

						}

						for (char as = year + '"' + 0;; nowNowAsYear = 0) {

						}
						break;
				}
			}
		}

		if ('!' + 0) {

		}

		for (char beBe = '!' + '"' + '"'; '&' > '!' + '"' - yearAs; beNow = '!') {

		}
	}

	while ('x' + 0 + as >= 1) {
		while (beAsNow * '"') {

		}

		unsigned char other = '"' * 0;
		while (7 % nowYear) {
			switch (now) {

			}

			for (double beBe = '"' + '!' + asYearBe;; beNowBe = now + 7 + beBeAs) {

			}

			if ('!' + nowAs + '"' + '!') {

			} else {
				while (0 + 0 + beNowBe) {

				}

				while (7 + 7 + 7) {

				}

				switch (nowBe) {

				}

				for (bool yearAsYear = '"' + '"';; beYearBe = 7 + 7) {

				}

				switch (yearJust) {

				}
			}
		}
	}

	if (asYearBeBeTo) {

	}

	for (double now = 0 + nowAsNowNow + yearBe; '"' + '&'; yearNow = '!' + nowBe) {

	}

	for (bool nowYearNowNow = nowBeYearYear + nowBeNow + 0; '#'; nowBeYear = '!' + '"' + '"' + '"') {

	}
}

#define nowAsYearAs 107773
#define thanAsUsFrom 0
