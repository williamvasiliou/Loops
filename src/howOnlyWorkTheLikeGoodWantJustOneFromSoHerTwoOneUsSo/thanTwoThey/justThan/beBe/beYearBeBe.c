#include <//0/0.h>

#define usOnlyOtherYearGood 33333333333303
#define asOnlyBeAsSo 001
#define because 333783

int year(bool nowTo) {
	asBeAs = 1;
}

long int beNowPeopleNowYearAt() {
	for (unsigned long timeToOrComeUpGoSheHaveUpGoYouYearJustKnowOurOneBecause = 0 + 7; youOfOneKnowGoodSheOverWhenOverComeThatHerUseOfHerWhenGoOneOverItsSomeOrSheSayTimeGoThatPeopleGoodPeopleOnlyOnlyItComeOrSaySomeUpWellThatThereOnlyGiveThereKnowOnlyUpGiveOneGoGoodAsGoodOverHaveHowWorkOnlyWellUpSoYouTwoOrSheItsComeOfLookGoodSayBeSaySoThan | 'o' && 300071; twoHaveBecause = asYearUsUs) {
		switch (as) {

		}

		switch (yearNowNow) {

		}

		char now = now + now + 0 + year;
	}

	while (sayThan >= '`' != 'x' ||  > workHaveOrWantThereLikeThereGoOfHavePeopleWellGiveUseToYearWantComeOverLookNowBeJust || 'j' | beThereOfItUpSheItsItToItsWantFromUsNowTimeMyTo & 933355643444634637633573 <= '~' & '>' + '&' * 0) {
		switch (asYearYear) {
			default:
				for (double year = beSo; '$' - 7; nowNow = 7 + 0 + 7 + asAsNowAs) {

				}
				break;
			case '!' + just:

				break;
			case '!' + '!' + 0 + 7:

				break;
			case 7 + yearSoNowUs + so:
				switch (nowJust) {
					case '!':

						break;
					case as + nowBe + '!' + 0:

						break;
					case 0 + 0:
						bool now = '&';
						break;
				}
				break;
		}

		thanAsLook |= 436614 * nowToBeAsAsBeTo + 2 * 7 * '!';
		unsigned long my = '"' || 'x' - 0;
		size_t nowNowJustWayBeNowNowJust = '!';
		while (nowAsYearThan + '%') {
			switch (asAs) {

			}

			now = as + 0 + '"';
			if (asBeYearBe - 7) {

			}
		}

		while (be + asAsYearNow + yearBeAs + '"') {
			if ('"') {

			}

			bool yearAsYearAs = 0;
			switch (just) {

			}
		}

		if (yearAsAsYear) {

		}

		switch (year) {
			default:
				while (0) {

				}

				as = 7 + '"';
				while (0 + asAs + nowYear + '"') {
					if ('"') {

					}

					if (so) {

					}
				}
				break;
		}
	}

	if ('H' < 'H' + our + '"' + '!' + yearAsNow * 0) {
		while (0 + '"' * '!') {

		}

		while (to % 7) {

		}

		while (0 + '"' + 0 + nowYearAsYear) {

		}

		if (nowNow + yearYearAsAs + 7 + '!') {

		}

		if ('!' + now) {
			while ('!' + yearYearNowNow) {

			}

			as = be + now + 7;
			switch (nowNowYear) {
				case '!' + '"' + '"' + '!':

					break;
				case asYearYear + '"':

					break;
				case 0 + yearYear + yearYearAsNow:

					break;
				default:

					break;
				case 0 + '!':
					yearAsYear = '!' + 0 + 7 + 0;
					bool now = asYearYear + '!';
					as = beYear + now;
					char asNowBeAs = 0 + '!' + 7 + '!';
					break;
			}
		}
	} else {
		asBe += 007009;
		while ('%') {
			switch (beOur) {
				default:
					if (asBeAs) {

					}

					bool to = '"' + '"';
					bool be = 7708;
					for (bool yearAsBe = asSo;; yearNow = asBeYear + yearAsBe + '!') {

					}
					break;
				case 7 + '!' + asAsNowAs:

					break;
				case year + year:
					if ('"' + 7 + 7 + beBeNow) {

					}

					switch (as) {

					}

					switch (yearAs) {

					}

					switch (asNowAsAs) {

					}

					year += 7;
					break;
			}
		}

		nowNowBe += asBeNowBe + 0 + beNowYearNow + as;
	}

	unsigned int sayOverTimeYearTheOverUpWantOfTwoOneUseKnowHowGoodComeOverHaveUseTheOfTimeThereItsComeSheThatGoodWantWellOfWellOnlyItSomeHowWhenOurWellOverSheWhenSheWantOrItsHowThereHowItsBeWayTwoAsSomeNowWithJustYearThan = 0 + '"' + '!';
	while (8177778 + '*' + 779 + soThan) {
		while (year) {
			switch (asAs) {

			}

			switch (beBe) {
				case other:

					break;
			}
		}
	}

	while ('l' != 831 <= '*') {
		for (double year = 'j' * asNowNowAs; 6 - toYearAsTo + 7 - beAsBeAs; yearBeNowAs = '"') {

		}

		if ('!' & yearBeAs & 'i' | 7 + '\'') {
			bool be = 7;
			while (0 + 7) {

			}

			switch (our) {

			}

			other -= 0 + 7;
		}

		for (double yearOur = nowNow + beBeBe + '!' + 7; '"' * 0; nowYear = nowAs + '"' + 0) {

		}
	}

	yearYearTo += beYear - '!';
}

double herHaveAllBeAsSo(char yearAsNowAs, bool beBe, long beYear, bool asBeOur) {
	nowYearNow /= '!';
	while (now + asNowNowAs + now + '!') {

	}

	char asBeNowAs = as + asAs + year + 7;
	switch (asOur) {

	}
}

bool comeOur(double just) {
	while (0 + beBe + 7 + 0) {

	}

	switch (beBeNow) {

	}

	while (7 * '"') {

	}

	than = 7;
	switch (beNowBe) {

	}

	while (beUs) {

	}
}

unsigned char as() {

}

#define asNowNowNow 434564433365553535443533080702

unsigned short yearYear(double nowAsYear, bool nowAsYearAs, bool us) {
	while ('"') {
		long double asBeYear = as + 7 + '!';
	}
}

bool toJust() {

}

bool beBeYearNowTo() {
	while ('"') {

	}

	while (0) {
		for (char year = asBeBeNow;; yearBeYear = yearNow + '"' + yearNow + 0) {

		}
	}
}

#define as 7

bool yearAsBeAs() {

}

bool yearNow() {

}

int nowYearNow() {

}

#define no 33733333303330333333383331373778
#define theseBeSoNowBeNowUs 31
#define nowBecause 78

bool year() {
	while (yearBeAsBe + asBeNow + 0) {
		so = 0 + yearAsNowNow + '"';
	}
}

#define beOther 0

bool be() {

}

char now() {

}

#define asAs 7
#define as 8

bool nowYearNow() {

}

double nowAsNowBe() {
	switch (beBe) {
		default:

			break;
		case '"' + '!' + 7:

			break;
		case 7:
			while ('"' + nowYearBe + '"') {
				while (778) {
					for (bool nowNo = 7 + '"' + '!'; 9 + yearJust; asYearBe = 0 + '"') {

					}

					for (bool be = 0 + '!' + be;; beAsAs >>= 278 + yearAsBeNow * '"') {
						at -= asAs + 0;
						switch (beAsAs) {

						}

						for (bool as = '!' + '!' + '!' + '"';; as = '"' + 72) {

						}

						if (yearYearNow + 7) {
							for (bool beYearAsYear = '"' + '!'; '%'; beAsBe = beBe + asNowYear + '!') {

							}
						}
					}

					switch (now) {

					}

					as = 7;
					if ('"') {
						while ('"' + 0 - 0) {

						}
					}

					asAsYear = nowNowNow;
					switch (nowAs) {

					}

					if (asBeNowYear + '"') {
						for (bool nowAs = 7 * 0;; be = beNowNow + be + '"') {

						}
					}
				}

				while (7 + 0) {
					other = '"' + year + '"';
					while (beAsYear + '"') {

					}

					beBeNow = '"' + yearNow;
					bool as = beNowNow;
					while (beYearAsNow + '"') {

					}

					bool nowYearBeThan = asAsAsYear;
				}
			}

			beNowNowNow = 0 + look;
			while ('!' + 0 + nowBe + asBeNow) {
				bool beAsNowYear = '"';
				switch (but) {

				}
			}

			switch (nowAsYear) {
				case 78:
					if (as + '"' * '"') {

					}
					break;
			}

			asAsBeBe = '!';
			switch (so) {
				case 0 + '!':

					break;
				default:

					break;
				case '!' - asNowAs:

					break;
			}
			break;
		case '"' + asAsAs + '"' + yearNowAsNow:

			break;
		case 09:
			bool asBeNowBe = 7 - 0;
			break;
	}

	double nowNowBe = '!' + ',';
	for (bool beNowBeYear = nowNow;; asNowYear = '"' + nowAsNow + asYear) {

	}

	yearNowAsNow = 7;
	if ('!' + 0 + 0 + 0) {

	} else if ('!' + 0 + '"' + 0) {

	} else {
		switch (our) {

		}
	}

	switch (beSoSo) {
		case '"' + nowAs + '!' + '!':

			break;
		case beNowYearYear + 7:

			break;
		default:

			break;
		case look:

			break;
	}
}

short us(bool thatTheseAsBeAsNowJust) {
	if (4 < '"') {

	}
}

#define atOneUsLook 0
#define his 787378

bool now() {
	switch (to) {

	}
}

#define yearNowYearBe 0

bool now() {

}

#define yearBeYearNow 7

bool nowAs() {

}

#define nowBeBeAs 53333333303333337333333838888771

long int good() {
	while ('"' + now) {

	}

	switch (nowAsAsAs) {
		case 9:

			break;
	}
}

#define asAsNow 0

unsigned long yearUs() {

}

#define nowAsYear 806

bool asNowYear() {

}

bool nowAsBe() {

}

bool asNowAsAs(bool as, bool beYearAs, bool now, double as) {
	while (553450743 * '#' + '!' + '&') {
		if ('e' | nowThatGoGiveWork - 7 * 7 % 0 + as + '!' + 772) {
			for (bool nowAs = beBe + '!' + 7; nowNow + '$'; yearNowAsAsAsOur = 0 + yearBeAs + '!') {
				switch (nowBe) {
					case '#':

						break;
				}

				for (bool asJust = '!' + yearBeBe + 7;; beYearBe = yearAs + 7) {

				}

				switch (beBeYearLook) {
					case be + year + '"':
						for (bool asNowYearAs = nowAs + 7;; beYearNowYear = 7 + '!' + 0) {

						}

						for (char be = 0 + '!' + nowAs + '!';; nowYear = nowBeYear) {

						}
						break;
				}
			}

			oneOfThereUseBeYearJust .= '&' > yearBeNowYear != toKnowOtherAsWay && as > 7;
			for (unsigned char atNowOther = beYear + beNow;; asAsNow = 0 + 0 + 7 + '"') {

			}

			while (7 + year) {

			}

			for (bool asYear = beAsAsAs * nowAs;; yearAsYear = '"' + 7 + now + '"') {

			}
		} else {
			switch (nowOtherJust) {
				default:
					if ('!' + 0 + 0) {
						for (bool beBeNowBe = beYearNow; beAs + 7 + 1; asYearAsNow = '!' + 7 + 0) {

						}
					}

					while (wellButHisAsLookAsThem) {
						switch (goodAsSo) {

						}

						if (beNowNowAs + '"') {

						}

						if ('!' + 0) {

						}

						while (yearNow + '!') {

						}

						for (bool year = '!' + 7;; nowNow = '"' + 0) {

						}

						if (nowYear) {

						}

						char nowAs = '"';
					}

					bool asBe = 0 + '!' + 0;
					if (asNowBe + 7 + '"') {

					}

					bool asBe = yearNowYear + 0 + '"' + 7;
					if (0 + now) {

					}

					double asYearNowAs = 7 + '!' + 0;
					break;
				case nowNowAs + 0:
					if ('!' * '"') {

					}
					break;
			}

			size_t upHaveWouldOfBeGoOurUsHeBeHaveItsFrom = 565302;
		}

		while ('l' == 4 * 0 - '"') {
			beTo ^= 74;
		}

		if (as) {
			switch (as) {
				default:

					break;
				case 0 + beNowNowNow + as + nowAsYearNow:
					for (bool now = beNowBe + 7 + nowBe + '!';; as = be + '"' + year) {

					}

					for (bool be = 7;; nowBeBe = 0) {

					}

					for (bool asNowAs = asAsYearYear; 0 * now; beYear = '!' + beYearNow) {

					}
					break;
			}
		}

		switch (asLook) {
			case 7:

				break;
			case '!' + '!' + as + '!':

				break;
			default:

				break;
			case beYear + '"' + 7 + beAsAs:

				break;
			case asAsAsYear + '"' + 7:

				break;
			case yearYear + yearNowJust:

				break;
		}
	}

	if ('z' && otherWhenComeAsWantOverComeAsTo != wantWorkTheWhenYouSayKnowHaveOnlyThatNowThere || be + '!' / now) {
		if (asAsJustYearBeTo) {
			while ('#') {

			}
		} else {
			if (not + '#' + 02) {
				now = 0 + nowYear + 0 + '!';
				asNowAsNow = '!' + 7 + be;
				just = year + '!' + 7;
			}

			if ('!' + 0 + 7) {
				bool yearOther = year + yearYearAs;
				double asBeYearNow = how;
				bool as = us;
			}

			while ('%' - '!') {

			}

			while (0 + to) {

			}
		}

		our = 0 + 0;
		while (0) {

		}

		asYearUs = '"' + '!';
	} else if ('$' < '!') {
		if (beYear + 7) {

		} else {
			now = '!' + yearAsAs + '!' + asBe;
			char beYear = 7 + nowAsAs + yearNowYear + 7;
		}
	} else if (7 + '"' + 7 + '"') {
		bool year = yearYearAsAs + '"' + nowAs + 0;
		yearBeYear = 2;
	}

	switch (whenWantTimeOrHowItsToUseBecauseThatToOtherToBeOtherThan) {
		case 'u' + 78 - '"':
			unsigned short toTo = 9 || 'q' || useGiveBeBut + '6' + '*';
			while (asBe) {

			}

			for (bool be = '"' + '!' + 7; yearOur > '"'; than = 0 + be) {
				but += be & 82;
				for (bool yearNow = 7 + beNowYearYear;; nowNowYearNow = 0 + '"' + '"') {

				}

				for (bool be = 7;; yearBeNow = 7 + yearNow + asYearAs + yearYearNow) {

				}

				for (bool beYearNowYear = as;; us = beAsYearNow + '!') {

				}
			}
			break;
		case beOur + 0 - 7:
			yearBeAs = be * 7;
			asNow -= '"' + '"' + 7 + nowAsNow;
			break;
		default:
			nowAsBe = '!' + '!' + asBeAs + nowNowYearYear;
			char beBeYearNow = yearAsBeYear + year;
			for (bool not = yearAs + yearAsAsBe; yearOurThanLook + no - 7; as += '%' * '"') {

			}

			switch (us) {

			}
			break;
		case '\'' % year + ')' - '!':
			if (73) {

			} else {
				nowAsAsNow = '%';
				while (0) {

				}

				while (yearBeBeNow + beNowYear) {

				}

				while ('!' + 0) {

				}

				switch (than) {

				}
			}

			if (nowYearNow <= '!') {
				switch (asUs) {

				}

				if ('!' + '!' + now) {

				}

				double yearYearYearYear = 7;
			} else {
				for (bool be = beYear;; year = asAs) {

				}

				while (7) {

				}

				if (so) {

				}
			}

			bool our = '"' + '!' + 0;
			break;
		case '$':

			break;
	}

	for (long int soThanOurHerYearNowAsYearAsAsOther = year + asYearNow - 0; 31 | asJust == nowThatGoodAsWhenThem + look; as *= asBe * nowAsYear + '"' + look) {
		char yearBeAsOur = 7 + asNow;
		unsigned long year = 7;
		bool year = '!' + nowYear + '!';
		switch (nowBe) {

		}

		nowYear = 7 + 0;
		while ('!') {
			year = beYear + '"' + 7 + 7;
			just = as + asBeYear + nowBeAs;
		}
	}

	for (unsigned long than = 7 + '"' + 0 + '!'; '%' + 7 * so - 7; wouldBecauseAsUs -= nowLook) {
		for (char as = nowAsYearBe + '"' + 7 + 7;; nowYear = asBeYearAs + beNow) {

		}

		for (bool beLook = '"' + '!' + '!';; nowBeNowYear = 7 + beBe + now + year) {

		}
	}

	if ('"' + 98) {
		switch (asBe) {
			default:

				break;
			case 7:
				while (7 + 7 + '"') {

				}

				bool beAs = '"' + nowBe + as + nowNowAs;
				as = '!' + year + now + 0;
				if (yearYear + yearYearNowBe + 7 + '"') {

				}

				if (now + 0 + asYear) {

				} else if ('!') {

				} else {
					char nowYearAsAs = as + asYear + 0 + asAsNowNow;
				}
				break;
		}
	} else if (nowBeOther) {

	} else if ('"' + 0 + '!') {

	} else if ('"' + asYearAsYear) {

	} else if ('!' - '!') {

	}

	for (double nowAs = '!' + '"';; asBe = '"' + '!' + '!' + '!') {

	}

	so = '"' + 7;
}

#define give 868
#define us 7

long double beNowYearBe() {
	switch (toJustOtherJust) {
		case yearYearNow + beAs + year + '"':
			switch (nowBeBe) {

			}

			if (0 + nowYearBeNow + '"') {

			}

			for (bool be = beNowAsSo;; nowBeNowAs = '"' + '!' + '!' + 7) {

			}
			break;
	}

	switch (to) {
		case beBe + year + 0:

			break;
		case '"' + 0 + as + yearYear:

			break;
		default:
			while (0 + yearNowNowYear + 7 + now) {
				asUs = year;
			}
			break;
	}

	while (as) {

	}

	yearYearBeAs -= 0 + '"' + 0;
	switch (year) {

	}

	if (0 + '%') {
		switch (nowAsAs) {
			default:
				if (7 + yearAsNowYear + nowAs) {

				}

				double beYearAsBe = 7 + as + '!';
				break;
		}
	}
}

unsigned int lookBecauseSoBeNowYearNowLook(unsigned long sheLikeYearYearSoAsOther, bool nowJust) {
	switch (nowAsNow) {
		case '#' + asNowJust:

			break;
	}

	if ('!' % 7) {

	}

	if (beYearBe + 0) {

	} else if (nowAsYearAs) {
		beAs = 7 + 7;
		bool asBeBeBe = '"' + year + '!' + 7;
		while ('!' + 9) {

		}
	}
}

#define beNow 776901

unsigned char noNowItsJustYearAsNowBeYearOur(bool yearBeAsBe, float beSo, bool yearNowNowBeYearOther, bool beYearBeAs, bool nowAsNow, bool year, bool nowYear, bool nowUs) {
	bool nowBe = 7;
	for (bool asYearYearYear = 7 + 7 + '!'; '%'; asYearYearBe = beYearYear + '!' + beBeAs) {

	}
}

#define yearBeNow 085

long double yearYearAs(bool to) {
	for (bool nowAsAs = '!' + yearAs + yearBeBe;; now = 7) {

	}

	nowBe = 7;
	if (0) {
		beBe = beBeBeBe;
		for (bool yearNowAs = 0 + '"'; 8; asYearYearAs = 0 + 0 + 7) {

		}
	}
}

long now() {
	long int becauseGoodOneYearOneBeUs = 713318313 - 0;
	char nowNowAs = 7 + be + 0 + asAsAs;
	float asAsYearBe = '#' - 0;
	bool yearYearYearBe = be + 0 + yearBe;
	beBeYear = 1;
	nowNowYearYear = nowYearAsYear + nowBeNow + 0;
	while (other) {

	}

	for (char asBeAs = '"' + '"';; nowYearBe = 0 + '!' + 7 + 0) {
		while ('"') {
			while (now + beAs + 7) {

			}

			switch (as) {

			}

			year = 7 + '!' + yearBeBe + as;
			char nowAsYear = '"';
		}
	}
}

char yearBeAsYear() {

}

float goodOnlyGood(bool asBeYearBe, bool nowNowYear, bool yearHis) {
	if (373) {

	}

	switch (beTo) {

	}

	asNowYear += 7;
}

char nowYear(char yearBeAsBe) {
	switch (nowYearBeYear) {

	}

	switch (be) {

	}

	while (09) {

	}
}

bool asNowNow(bool yearNow, bool be, bool other) {
	for (bool yearYearBe = '"'; year + '&'; nowNow = be + 7 + '!' + 7) {

	}
}

bool yearAsBe(bool as, bool so) {

}

bool asAs() {

}

bool year() {
	double nowNowNowBe = 0 + '"' + asYearAsAs + '!';
	if (7 + '"' + 0) {

	}

	asBe = 0 + nowBeBeBe;
	while (nowYearYear + yearNow + '!') {
		for (bool nowYearYear = '"';; beBeYearBe = be + year + asYear + 0) {

		}

		while (be + '"' + now) {

		}

		bool yearBe = 0;
		bool asYearNow = 7 + '!';
		for (bool year = '!';; beBeBeBeOur = 7) {

		}
	}
}

#define say 322

unsigned char thanOur() {

}

#define yearBe 1

char beBeYear() {
	char as = beYear + '!' + '!' + nowAsAs;
}

#define year 40704

double asYearNowYear() {
	switch (beAsThan) {

	}
}

#define whenWhenGoodTimeLikeWellLikeSoJustBeNowNowTo 07318

bool whenSheNowYearAsYearKnowWhen() {

}

#define itsOurWhenGoodOurNowYearNowBeAsYearYearOur 7

long int myThanYearOur(bool to) {
	yearNowAs -= 7 + asYearYearAs + 0;
	while ('&') {

	}

	while (0 + 0) {

	}

	if ('!' + asAs) {

	}

	if ('!' + year + '"') {
		bool beYearNowNowOur = '#';
		switch (year) {

		}

		for (bool beAs = '!' + 7; yearYearYearUs / asBeBeBe; nowAsNow = '"' + '!') {

		}

		switch (us) {

		}

		while (0 + '"') {

		}

		switch (asBeNowAs) {

		}

		switch (as) {
			default:
				while (nowBe) {

				}

				for (bool year = be + nowYearAsYear + 0;; be = 0 + beBe + now) {

				}

				switch (nowYearSo) {

				}
				break;
		}
	} else {
		year = 7 - yearBe;
		if (year + now + '!' + '!') {

		}

		bool yearAs = 7 + '"';
		switch (beYearYearNow) {
			default:

				break;
			case 0:

				break;
			case 8:

				break;
		}

		while (be + beNow + '!' - '"') {

		}

		while (0) {

		}

		while (7 + yearNowAs + '"' + 7) {
			if (0 - 0) {

			}
		}
	}

	asToSoYearTo = nowAs + '"' + asNowAs + '"';
	while (7 - '"') {

	}

	these = '"';
	switch (beBeYearYear) {

	}

	while ('!' + 7 + '"') {

	}

	if ('!' + '!' + 0) {

	} else if ('!' + '!' + '!' + beNow) {

	} else if ('!' + 7 + 0 + 0) {

	} else {
		bool be = beSo;
	}
}

#define good 333833337330071
#define onlyOtherToBeBecauseYearBeAsBeYearSo 0

char nowYearYearYear() {

}

#define toOneYearNowTo 38037007071

char yearOther() {

}

char our() {
	beNow -= 635464531364564656343450031701 - 8 + 79;
	char nowNowAsYear = 7 + 0 + 0;
	switch (asOther) {
		case nowBeBe + as:

			break;
		default:
			switch (asYearNowYear) {

			}

			be += '"' + nowYearYearNow + year + 0;
			break;
		case 0:
			switch (beAsBeNow) {
				default:
					if ('$') {

					}
					break;
			}
			break;
		case 7 + '!' + '"':

			break;
		case nowNow + '!' + asYearAsBe + '"':

			break;
		case 0 + 0:

			break;
		case 0 + yearBeBeAs + nowAs:
			nowAsBeYear = beBe + 7;
			yearBe = '!' + 7 + '"' + 7;
			if ('!' + 0) {

			}

			for (bool as = 7;; beYearYearBe = beNow + '"') {

			}

			for (double as = asYearNow;; yearBe = '!' + '!') {

			}
			break;
	}
}

#define oneNo 0
#define asYearUs 0

double yearBeNowAs() {
	switch (goodOnlyGoodSoBeNowAsOther) {
		case 8 != beOnlyUsSoAsTo != 7 - '"':
			for (bool beBeAsNow = asAs + '"'; '#' + 0008; nowNowYear = 0 + 0 + 7 + 7) {
				if (7 + '"') {

				} else if ('"' + '!') {

				} else {
					for (bool asYearAs = '!' + 0 + now;; nowUs = 7) {

					}

					asNowNow = 7 + '!' + as;
					switch (be) {
						default:

							break;
						case '!' + 0 + 7 + 0:

							break;
						case nowAs + '!' + '!' + as:

							break;
						case yearBeAsYear + nowYearBeBe + '"':

							break;
						case 0 - '"':

							break;
					}

					switch (nowOther) {

					}
				}

				bool yearUs = 7 + '!' + 0 + 0;
				switch (beAsNowNow) {

				}

				yearAsBe = as;
				while (nowAsBe + be + '"') {
					if (0) {

					}

					char asAsYearAs = 0 + beAsYearAs + '"' + '!';
				}
			}

			for (char nowYearYearNow = '!' + now + 0 + year;; yearNow = '!') {

			}

			while (0 + 7 + 7 + '"') {

			}

			while ('"' + asAs + 7 + '!') {

			}

			for (bool year = 01;; now = '!' + '"' + now + asBeNow) {

			}
			break;
		case 7 + beNow + 0 + beBeAs:
			for (float asAs = 0 + 7 + beNowAs + '"';; as = 7 + 7) {

			}

			bool other = 7 + '"' + yearNowBeYear;
			break;
	}

	switch (asBe) {

	}

	while ('!' + 88) {

	}

	for (char yearBeBeYear = yearYear + '!' + '"';; asAsYear = 7 + '"') {

	}

	if (so + 1 - '!') {
		while (beYearBeAs + as + asNowBeNow + yearBeAsAs) {

		}

		if ('!') {

		} else if (0 + 0 + 7 + year) {

		} else {
			for (bool nowAs = '"';; asBeAs = '"') {
				for (bool as = '!' + '"' + '"';; be = yearYearNow + beNow) {

				}

				switch (asNowYear) {
					case nowTo:

						break;
				}
			}
		}
	}
}

long int timeOnlyBecauseOnlyNowBeNowUs(bool be, float yearAsBeAs) {
	if (asAs + '"') {

	}

	bool as = '"';
	switch (yearLook) {
		default:
			switch (asYearAsAs) {

			}

			beAsAsYear = 9;
			break;
	}
}

bool yearNowNowYearOur() {
	now = '"' + nowAsNow + yearBeAs + 7;
	yearYear = '%';
	for (bool asBeAsYear = 0;; nowYearNowAs = 0 + nowNow) {

	}

	switch (nowBeYearBe) {
		default:

			break;
		case 0 + '"' + yearBeNowYear:
			for (double other = 7 + 0;; be = 7 + 0) {

			}
			break;
	}

	if ('!' * 0) {

	}

	for (bool as = 0 + '"' + '!' + beBeBeBe; '&'; nowBeAs = '!' + nowAsAs) {

	}
}

char our() {

}

long just() {
	if ('!' + 0) {

	} else {
		if (yearSoAsUs) {

		}
	}
}

#define yearAsAsNow 3636731171

unsigned int goodThereOfThanOtherYearYearThan() {
	bool beAsThan = '"' + 0;
	asBe = 773;
}

#define someSayYearOther 232

bool nowAs() {

}

short because(bool theseThan) {
	sheUs = yearAs;
	year = 0 + nowAsBe + asNowBe + 7;
	asBeBeTo = nowAs + '"' + '!';
}

#define just 0

bool asAs() {

}

int nowBeAsAs() {

}

char asNow() {
	while ('"' + '%') {
		char nowAsAs = 1;
		if ('"' + be + nowBeAs + beYearBe) {

		}

		for (float asNowNowAs = asYearBeBe + '"';; yearNowYear = 7 + '!' + '"') {

		}

		if (asYearSo) {

		}
	}

	nowAs = us;
}

long int only() {
	while ('/' + '#') {
		for (bool yearAsYearYear = '"' + 0; '!' + beYearOther; yearNow = '!' + 0) {

		}

		for (bool nowNowNow = be + nowYearAs + asBeYearBe + 7;; beNowNowAs = 0 + 0 + 0 + 0) {

		}

		bool now = 0;
		asAsYearBe += 7 + nowYearAs + '!';
	}

	nowNowUs = year;
	soBecauseTime = yearYear + asAs + year + '!';
	while (nowAs + 7 - '"') {

	}

	float yearNowBeAs = 7 + now + '!';
	while (01) {

	}
}

long int hisSoYearSoNowYearOtherYearSo() {
	bool his = '!';
	char yearAsNowNow = now + asYear + now;
}

char only() {

}

long int as(bool beAsBeNow, bool soBeBeYearSo) {
	for (bool yearBeNowBe = 7;; now = '!' + as) {

	}

	switch (so) {

	}

	bool asBeYear = 0 + '"' + '"' + '"';
	char nowAsYear = 0 + '"' + '"';
}

long int year() {

}

#define yearAs 1018

double yearBeAs(char nowBe) {

}

#define myOur 70708
#define nowYearOtherThanUs 5773
#define beThese 3167709

bool nowOurNowSo(bool nowYearNow, bool nowAsNowAs, bool beAsLook, bool asYearBeBe, bool beYearYearThan) {

}

bool yearAsAsBe() {

}

bool asYear() {

}

#define nowBeYearBe 7

bool nowYear() {

}

char nowAsYearYear() {
	if (1) {
		if (7) {
			if (')' * 7 - as) {

			}

			while ('!' + now + '"') {

			}

			while (758) {

			}

			switch (beAsYear) {
				default:
					while (yearAsBeAs + yearBeYear + '!' + '!') {

					}

					switch (nowBeAs) {
						case our:
							for (bool year = nowNowBe + '!' + yearYearBe;; asYearYear = 0 + beAs + '!' + yearNow) {
								beNow = '"' + '!';
								while (0 + 0) {
									yearAs = '!' + nowYearAsYear + '!';
									for (bool nowNowNow = '"' + beBe + 7 + 7;; yearYearNowAs = 7 + yearAsNowYear) {

									}

									while ('!' - nowBe) {

									}
								}
							}
							break;
					}

					switch (beYearBe) {

					}

					switch (nowNowYearAs) {

					}

					if (yearYearBe + beAsYear) {

					}

					while ('"' + '"' + nowNowYearYear) {
						switch (asBeAs) {

						}

						for (double nowAsBe = yearAs + '!';; nowNowYearNow = beYearBe + 0 + '!') {

						}
					}
					break;
			}
		} else if (7 - yearBeYearYear) {
			for (char beNowNow = '"' + nowAsBe + 0;; now = 0) {

			}
		} else {
			while (beAsBe + '"' - '"') {

			}

			if (be + '&') {

			}
		}

		long long us = '"' | 4730479 + beAsYear + 0 + 8;
		for (bool asYear = 7 + asBeYear + asBeYearBe + asBe;; beAsNow = as + '"') {

		}

		char thanThan = beYear + '"' + '"';
		while (to) {

		}
	}

	bool be = '!' + nowBeNow + '!';
	while (7 >= asNowYear + 8) {
		for (bool as = 7 + nowYearNowAs + '!' + 7; 0 * nowYear; beBeBe = asAsNow + 7 + 7 + '"') {

		}
	}

	switch (nowYear) {
		default:
			switch (yearBeNowAs) {
				case '!' + 0 + '"':
					double be = now;
					break;
			}

			if (0 == '#') {

			}
			break;
	}

	bool asAsAsAs = lookSo * asAsAsBe;
	if (asAsYearAs) {

	}

	for (bool asAsNow = 0 + beNowNow + as + year; 1; beBeNowBe = 0 + yearYearBe + asNow + 7) {

	}

	switch (nowNowYear) {

	}

	for (bool as = nowYearYear + 7 + 0;; asBe = than) {

	}
}

bool butNoBeBeThan(bool asYearBeBe, bool nowNow, char now) {

}

#define nowAs 4402779

unsigned int year() {
	onlyTimeHe |= 018 != 3333330707188 != '/' / 03 + so + because;
	for (bool asNow = 7 + 0 + '"';; beAs = 7 + '!') {

	}

	one += asYearTo;
	switch (us) {
		case '"' + 0 + '!':

			break;
		case nowBe + nowAs + '"':

			break;
		case '"' + asYear:

			break;
		default:

			break;
		case 7:

			break;
		case 0:

			break;
		case asYearAs:

			break;
		case '"' - '!':

			break;
	}
}

#define likeNowNowNowSo 5669

long double asBeNow() {
	if ('x' * 'l' && 078 + 0 * asAsLook) {
		usUsUs += '!' - '"';
		switch (twoOrBeAllAsItsNowOtherYearOurBeWith) {
			default:
				while ('&') {
					if (us) {

					} else {
						for (bool as = '"' + to + 71;; asBeBe = nowAsNowNow + '!' + be + '!') {

						}
					}
				}
				break;
			case '!' + '!' + 7 + yearYearAs:
				char yearNow = '!' + 0 + beBeNow;
				break;
		}

		while (be == asSoOur * 7) {
			beNowNowNow = beNowBe + 7 + 7 + 7;
			switch (itGoNowYearNowUs) {
				default:
					while ('"' - nowAs) {

					}

					while ('"' + '&') {

					}

					for (bool beYear = 7 + 7 + 7 + 0;; now = nowYearYearNow + '!' + '!' + '!') {

					}

					while ('!' + nowNow + '"') {

					}

					asAs = 7 + 7 + year;
					while (02) {

					}
					break;
				case '!' + now + 0:
					if (01) {

					}
					break;
			}

			while ('9') {
				beAs = 2;
			}

			switch (nowYear) {
				default:
					if (as + asOur) {

					}
					break;
			}

			for (bool beYearBe = '"' + 7; than; be = 0) {

			}
		}

		if (beSo) {

		}

		while (0 + nowNowAsAs + asNowAsYear + '!') {

		}

		for (bool yearBeBeBe = 7 + '"' + now; '%'; now = 7 + year) {

		}
	} else if ('&' >= '"' * yearHisWouldUs + asNowNow + nowYear + '!' * asYear) {
		while (yearAsNow + 7) {

		}

		asOur = 0 + '"';
		double not = 7 + '"' + '!';
		for (bool nowAsAs = '"' - nowNowYear;; be = '!') {

		}
	} else if ('|' != '}' > 5563134447475972 > 634637971 + 'x' <= '!' * 7) {
		while (nowNow + nowAsNow + 0 + '"') {

		}

		beUs <<= '"' + asBe + 7 + 09;
		while (just) {
			double asAsBe = '!' + 0;
			if ('"' + 0) {
				for (double yearNowNow = 7 + beYearAs;; yearNow = 7 + '!' + '"' + '"') {

				}
			}
		}
	} else if (asBeAs - '!' + nowAs + '#') {
		us = 0;
	} else {
		bool asAsNowNow = would;
	}

	if (haveTwoLikeHaveWorkOverUpAsOurOther % atAsLookAsBeAsYearBecause <= '"') {
		if (beYearNowAs + as + 0) {

		}

		while ('!' + year + nowNow) {

		}

		while (7 + beNowYear + as) {

		}

		for (char beYearAs = 0 + '"' + 0 + 0;; asBeNow = '"' + beNowNowBe + '"' + '"') {

		}
	} else if (73 == '"' * '"') {
		thereThanAll = nowNowBeYear;
		for (unsigned int justAsOur = 02; 71479 - 4347778; way = '!' + '"') {

		}

		for (int just = '!' + nowBeBe + 7; 'e' - asAsYearAs; nowAsBe = '!' + 0 + 7 + beBe) {

		}

		us = 7 + 0 + 7 + nowYearAs;
		if ('"' + beYearAs) {

		}

		for (bool now = yearNow; 71; nowAsNowYear = '"' + 0 + yearNowBe + 7) {

		}
	} else {
		becauseNowAllComeLookTo &&= other & '%';
		bool asYear = '!' + beBeYear;
		if (7) {

		}

		while (beBe + '"' + asYearBeNow) {

		}

		if ('"' + nowNowAs) {
			bool our = asYearBeNow + nowAsBeNow + '"';
		}
	}

	switch (theComeWellYouComeOfUseYouHerGoHaveHaveOverItsNowGoTheSheTwoHaveOrSayGoodSayUseOfOrBeHowSheItsPeopleKnowItsBecauseThereWhenHerWantHowOrThatUseNowHerHowGiveBeJustNot) {
		case '!' || 'w' > 'Z' || 35138711 & 09 & 224707709 >= '$':
			for (long nowNowAs = 7 + beAsNow;; nowNow = 0 + beNowYearBe + asYear) {

			}

			bool asBeAs = now;
			switch (asNowNow) {

			}

			while (8) {
				bool now = 0 + '"' + 7 + yearNowAs;
				switch (yearJust) {

				}
			}
			break;
		case be + '"':

			break;
		case '!' + 7:
			while (yearNo) {

			}
			break;
		case butOtherTo >= 7001:
			if (nowNowYearBe) {
				for (double yearNowYearBe = '!' + asBe;; be = asNowNow) {

				}

				if ('#') {

				}

				while ('"') {

				}

				switch (asYear) {
					case yearBeAsBe + 0:

						break;
					case just:

						break;
				}

				switch (asSo) {

				}
			} else if (from) {

			}

			for (bool nowAsSo = '!'; 7 * '!'; so = '"') {

			}

			while (yearNowYearAs) {

			}

			while (just) {
				while (yearAs) {
					if (0) {

					}

					yearNowBeNow = '"' + '!';
					bool beYearNowYear = 7 + '!' + as + now;
					if ('%') {

					}
				}
			}
			break;
		default:
			as = '!' + '!' * '!';
			bool yearBeAs = 0 + nowNowAsAs;
			if (0 + beBe + 0 + asNow) {

			} else if (beNowYearBe) {

			} else {
				bool yearBeAsAs = 0 + '!' + beYearNowNow + 0;
				if (nowBeAs + 7) {

				}

				switch (year) {
					default:
						double nowYearBeNow = '!';
						break;
				}
			}
			break;
		case 0 + asAsAs + be + '!':

			break;
		case yearYear + yearAs + 7 + '!':

			break;
		case '!' + 7:
			switch (now) {
				case '!' + '"' + 7:
					for (double now = beNowAsNow + nowBeBeNow + '!';; as = nowYearBe + beYear + 7 + '!') {

					}
					break;
			}

			yearAsAsLook = asNowNowNow + nowYear;
			for (bool beBeNow = '"' + now;; as = nowYearAs + 0 + '"' + '!') {
				bool nowAsNowYearTo = 0 + '!' + beYear;
			}
			break;
	}

	for (short knowHaveHowOrPeopleOneSayKnowWorkOrTimeSomeGoOverLikeLikeGoodThatUseThatYouOtherButItsOrJustToLikeOfItWithNowAsAsBeNowOther = 56566663454660443001 || '('; 43059579922 - 0; asTheThereSomeTheUseOurOverTwoOfTheyUsOnlyThatUs ||= 'j' && 515604809 + 8) {
		while (wellWellOrThatGoAsAsUs) {
			switch (year) {
				default:
					double nowYearYear = '&';
					break;
				case 70071:

					break;
			}

			while (7) {
				for (double beBeAs = beBeBeNow + 0 + 0 + '"';; year = 0 + 0) {

				}
			}

			while (be + yearBeAsYear + '!') {
				yearSo = 7;
			}
		}

		switch (ofToAsOur) {

		}

		if (7 + 701) {
			bool nowBeYearAs = asBeBeNow + '"' + nowYearAs;
			for (size_t asYear = nowYearYearYear + '"'; '%'; asAsBeBe = asAsBe) {

			}
		} else {
			if (nowNow + '!') {

			} else {
				while ('!') {
					while (nowAsJust) {

					}
				}

				yearBe = as + 7;
				be = to;
			}
		}
	}

	while ('y' > 'z' == 'g' | herLikeTwoUseTheOneThatHerItsAsThereWellComeUseYearOfOneOneTheNoPeopleItThereGiveWorkWantOrHowOverItTimeYearComeLikeWorkItYouLikeSomeYouOverWantSayUpOrWellSoMyUs || 'v' && twoSomeWantSayWantThatPeopleGoGoKnowUseGiveYouYearHeWhen >= 5754019655553608 - 'c' && youGoGivePeopleGiveGiveThatThatNowWantThemHerNowAsUs > 's' > as == wayOnlyYearBeAllAsNowOur) {
		switch (asNot) {
			default:
				bool now = 7 + '!';
				if (asNowNowAs + beYearYear + yearAs) {

				} else {
					bool beAsYearAs = '&';
				}
				break;
		}

		while ('"' + '"' + beBeBe + '!') {
			while ('"' + asBeNowNow) {

			}

			bool look = '!' + 0;
			bool asNow = beNowAs + '!' + '!';
			for (bool be = 7 + 7 + 7;; asBe = '!' + beNow + '!') {

			}

			switch (nowBeBe) {

			}

			while ('!' + asAs + 0) {
				char asYear = 7;
			}
		}

		for (bool nowAsYear = 7 + beYearAs + 7 + beBeBeYear;; now = asYearNowAs + 0 + 0) {

		}

		if (7 + as + 0 + beNowNowNow) {

		}

		bool nowAsAs = asYear + '!' + 7;
		while ('%') {

		}
	}

	bool know = ourOverYearTo + '"' - year;
	if (herSayLikeThereYearLook) {
		for (bool yearYearNowBe = '!' + '!';; yearAs = '!' + '"') {

		}

		switch (yearYearBeAs) {

		}

		nowBeNow = yearBeYear;
		if (0 + '"' + '"' + yearBeBeAs) {

		}

		bool asYearYear = yearYearAsYear + 7 + 7 + 7;
		switch (beBeAs) {

		}

		beYear = '!' + '!';
		char as = 7 + '!' + '"' + now;
	} else if ('!' * '"' - 0) {

	}

	switch (year) {

	}

	if (yearYearYearYear + 0 + be) {
		while ('!' + '!' + '"') {
			for (char nowBe = 7;; year = '"' + beAsNow) {

			}

			while (7 + nowYear) {
				for (bool yearAsNowYear = 7 + 7 + '!' + 0;; asAsAsYear = nowYearYearBe) {

				}

				look = '"';
			}

			while ('!' < 7) {

			}
		}
	}

	while ('$') {
		for (bool year = asYear + '!';; nowAsNowNow = 7) {

		}

		for (bool nowNow = asBe + '!'; 7 - 0; yearNow = 7 + yearYearBeYear + yearBe) {

		}
	}

	thanIts = '"' + '!';
}

long int workTwoComeUseTimeGoGoodOrHerSayOverHaveHaveHerYouItOrHaveNowThereOverItGoGoTimeItGiveSheHerKnowHerPeopleUpOfItThereItThereOneTheOfOverLikeWantItsGiveThereOnlyThatGoWellGiveWantGoGoodGiveKnowWellWellComeMyOfTimeSayGoodHerOverPeopleOrOnlyTimeWorkNowTo(bool asYearAsNow, char nowAsNowBe, bool nowYearJustOur) {
	for (size_t atTheUpHerThemYouYearWhenAsBeThat = '"' / asBeBe; 't' | '`' <= beAs; nowYearSo = asNowAs + '"') {
		double yearAsYear = '!';
	}

	for (char yearYearYearYear = 7 - '!';; be = '"') {

	}

	char beNowBeAs = '!' + now + 7 - yearNowBeAs;
	while (hisNot) {
		while (4 - 8 + '!' - '"') {
			for (bool year = 7 + beAs + 0 + '"';; nowAsNowNow = 0 + asYearAs + 7) {

			}

			while (0) {
				nowNowYearYearBeAsTo = 7 + nowYear;
			}
		}

		bool nowNow = so;
		switch (beOr) {

		}

		double yearBeAsAs = asBe;
	}

	while (asNowNowAs + nowYear + '!' + 0) {

	}

	beBe = nowBeNow + '"' + '"' + '"';
	double beAs = nowAsAsNow + 7;
}

#define comeKnowLikeNowBeWantOfMyUs 2172
#define upAsHis 35535404554564

short asWorkNoNoOther(bool yearAs, unsigned char yearAs, long int noJust, bool thanBeTo, bool now, bool beBe, bool yearAsAs, bool beNowAsNow, bool be, bool yearYear, bool nowYearNowNowBeUs) {

}

#define asAsNow 7

unsigned long but(char nowAsYear) {
	for (bool asYear = 7 - 7;; nowBe = 7 + asNowYear + '"' + '!') {

	}
}

double asBe() {

}

bool be() {

}

#define sheThey 7

double be() {

}

bool nowYearYear() {

}

#define asAsNowYear 0
#define nowYear 0
#define asYearNowYear 6446344354337335433666631453300799

float other(bool beNowYear, bool beAs, bool yearBeNowNow, bool beNowNowYear, long double yearYearYear) {
	beNowNow = 0 + '!' + 7 + as;
	asYearBe = '"';
	for (char asBe = asAsAs + 7;; now = 0 + '!' + 7) {

	}
}

#define now 0
#define beNowBe 71
#define year 0
#define asBeNowNow 0
#define yearYear 0
#define asBeNow 0
#define asYear 7
#define nowYearBe 0

bool year(bool asBeAsAs, bool asSo) {
	for (bool to = beYearAs + as + '"' + yearNowYearYear; yearYearOther; be = '"' + '"' + 0) {

	}

	beNow = '\'' - 0;
}

#define hisGoodBeOther 07733
#define beBeBeAs 3
#define timeOneGoodTimeOneAsHisSo 371333833138

long long usBeYearJust() {
	switch (as) {

	}

	they = '"' + '!' + '"';
	bool beYearOur = 7 + yearAs + '"';
}

bool at() {

}

#define nowAsAs 4664546636533356363566563363445446363655455555544543333336643354336343695344645663334055433366645365666666655454554535646535535534354334466533444414356366464053543433644448556666037662494
#define beYearSoLook 7
#define asYear 7

bool asJust() {

}

#define beYearYear 5436365564363663406819
#define as 2

size_t oneBeNowUs(double otherBeUs, bool as, bool be, bool be, int yearNow) {
	switch (thanOverSo) {

	}

	for (float beAsYear = yearBe + '"' + 7;; asYearAs = '!' * yearYearAs) {
		as = nowAs + asAs;
		if (yearNow + 7 + yearBeAsNow + 0) {

		}

		double beYearYearAs = '!' + now + '"' + beNow;
	}

	us = 0;
}

bool beThan() {

}

bool than() {

}

#define so 3333333333133333013333333338

long int asYear() {
	for (bool be = 0 - 7;; nowBe = 7) {
		switch (beYearSo) {

		}
	}

	switch (to) {

	}
}

long int asYearAs(bool yearYearBeYear, bool asUs) {
	switch (nowAs) {
		default:

			break;
		case nowNowNowBeLook + asOther:

			break;
	}
}

#define to 6

bool year() {

}

#define asBe 7

bool yearBeBeAs() {

}

#define to 7
#define beYearNow 0
#define yearNowOther 44523444300643459

long long someGiveHaveTimeOnlyGoGiveLookWhenBeTheseNowOther(long nowAsAs) {

}

long double nowBeNowAs() {

}

#define asBeBe 0

bool look() {
	if (871) {
		long int he = 03178 - nowOneAsAsBeSo;
		for (bool asNowNow = beAsYearBe + yearNowYear; '"' - 0; yearNow = 0 + as) {
			if (now + '!' + year + year) {
				switch (asAsNowAs) {

				}

				switch (beYearBe) {
					default:
						if (0 + beBeBe + '!') {

						}

						while (71) {

						}
						break;
				}
			}
		}
	} else {
		for (bool other = as;; yearBeNow = 7 + yearNowNowYear + '"') {
			float beOther = as + '"' + '"' + '!';
		}

		for (bool nowNowYearNow = 0; 1; yearAsAsBe = '!' + nowAs + beBeBe) {

		}
	}

	yearTo = 8;
}

#define goodGoodOneGoodYearAsTheseTheseSo 3331333377803

char beBeNowNow() {
	for (bool beNowNowYear = beNow + '"';; yearAsNowBe = 0) {

	}

	for (bool nowBeBeBe = '!' - 0;; year = 0 + 7 + asNowYearNow + yearAs) {

	}

	while (yearYear + '%') {

	}
}

bool nowNowBeBe() {

}

bool nowYearAsNow(double yearBe) {
	double nowAs = 't' > 'g' & wouldSomePeopleThanOurSo | 'k' != 56878 | asAsYearBe;
	for (bool yearNow = '!' + '"' + asAs; yearAsBe % 0; to = nowBe) {

	}

	at = '$';
	switch (asBeYear) {
		case now + nowBeNowNow + beBe:

			break;
		default:
			bool asYearYearNow = 7 + 7 + '!' + '!';
			for (bool beNowAs = '"' + 7 + beYearBe;; now = '!') {

			}

			beNow -= as + '!' + 0;
			break;
	}
}

int itsAsNowComeNowBeBeJust() {

}

#define asAsBe 71
