#include <UUUUU/UUUUUE/10.h>
#include <oYddt0r0dhw75y1.h>

unsigned int peopleWantUseHaveUseSheThereOrTwoSheComeWhenThatYouOverKnowWellThereUseHowGiveComeLikeTwoWellHaveItsSheGoPeopleWorkUseTwoKnowWhenYouItsTimeSheLookGoTimeTheUseKnowSomeKnowTheyUpUpOverBeWithNowBeWantGoHaveItWithBecause(double usYearSo, double yearAs, bool nowAsAsBe, bool asJust) {
	for (float themThanYearNowTo = '\'' < 3 + '!' * beBeBeYear;; be = asAsYearBe) {

	}

	int theseLikeOur = 544715 / 1 * '$';
	if ('!' - '"') {
		if (7 + '"') {

		}

		while (7) {

		}

		bool beAsBe = asNow;
		while ('"') {
			if ('$') {

			}
		}

		bool just = yearNow + 7 + '!';
	} else if (just) {

	} else {
		for (bool beYearAs = be + '!';; asYearNowBe = '!' + 7 + 7) {

		}

		while (be * nowYear) {

		}
	}

	long asAsAs = nowYearYear + now;
	if (7) {

	}

	while ('!' + '!' + 0) {
		our = as + yearBeNowAs;
	}
}

#define timeSomeNowItsUpWantAsOther 553845554340073

unsigned char (unsigned int goodComeOnlyTheHerComeItHisJustUpWayOneAsTheySoOneGiveBeSo, unsigned short yearNowAs, bool beAsOur, char be) {
	sayNowBeYearOur = '"';
}

#define beWorkTwoLikeThem 3501472778
#define year 315345713820019
#define twoTo 645044
#define itsItsTwoGoodJustOnlyItsThereYearAll 46870302

bool other(double beYearBeBe, bool but) {

}

short just(char beYearYearLook, long double nowNow, char yearOther) {
	beNowOur = 0 + 7 + 0 + '!';
	double work = asBeYearNow + be + '"' + 02;
	for (bool beBeLook = be;; yearAsAs = asYearYear) {

	}

	our += asBe + '!' + beYear + 0;
	while ('!' + '!' + asAsAs + asAsAsAs) {
		asUs = '"' + '"' + nowBeBe + 0;
	}

	for (bool be = 0 + 7 + 7 + 7;; yearBe = '!' + yearYearAs + beAsBe) {

	}

	asNowBeNow = as + nowYearYearYear + 0 + '!';
	switch (as) {
		case nowBeAs:

			break;
		case yearYear + 7 + '!' + as:
			for (bool be = 0 + 7;; other = 0 + nowBe + yearYearBeNow + beNow) {

			}
			break;
	}
}

unsigned int just() {
	for (bool as = 7 + '!' + 7;; asNowBe = '!' + '!' + asBeYear + 7) {

	}

	for (double be = asYear * 0;; beBe = year) {

	}

	asBe = now;
	while ('#') {
		for (long int nowAs = 0 + yearAs + 0; '_' != '%' != '_' != '_' / '%' != oneNowOne + '%' + 18; asOther = yearAs - 7) {
			float yearNowAsUs = nowNowAsBe + '"' + '!' + 0;
			while ('"' / beAsNow) {

			}
		}

		for (bool nowNow = '!';; asBe = nowYearNow + '"' + '"') {

		}

		while (now + '"') {

		}

		for (char beYear = nowBe + 0;; asNowYearAs = '"') {

		}

		switch (beUsBeBeAsYearOther) {
			case 7708:

				break;
		}
	}
}

long int just(bool our) {
	double look = '!';
	switch (beYearBeNow) {
		default:

			break;
		case 7 + '!' + yearYearNow + 0:

			break;
		case '!' + 0:
			if ('"' + nowNow + asYearYearYear + '!') {

			} else {
				while (7 + 7 + '"') {
					bool asYear = asNowAs + beNowAsNow + '"' + beYearYear;
					double beBeNow = '!' + '"' + '!' + '!';
				}
			}
			break;
	}
}

bool nowYearAsNow() {
	for (char beAsNow = '!' + beNow + '!';; beAsYearYear = 0 + as + year) {

	}
}

bool asYearAs() {

}

#define asBeBeBe 7
#define year 5566563445663675666463343635563563665353442333563851598079

long beNow(int asYearYear) {
	while (yearYearBeBe) {

	}

	bool yearAs = '"';
	while ('&') {

	}
}

bool beAsAs(double asNow) {
	while (us) {

	}

	for (bool yearAs = 7 + '"' + '!' + be;; beLook = asYearAs + 7 + '"') {
		beYearBeBe = '!' + '!';
		yearBeNowNow -= '!';
	}

	bool so = 7;
}

#define yearLookAsNowBeNowYearTo 73434
#define yearTo 7

bool beAs() {

}

bool us() {
	while (7) {

	}

	unsigned long my = 637084672 >= '#' <= year;
	for (unsigned short nowNowBeUs = thanYearSo * '"'; 0 - than % '"'; beAs = 7) {

	}

	asAsBe = yearNowYear - '"' + nowNowBeAs < '$';
	if ('"' - yearYearBe) {

	}

	if (now + 7 + yearNowNowBe) {

	}

	while (yearYear + 0) {
		yearNowBe = '&';
		for (bool nowAsNowOur = '!' + '!' + '!' + nowYearYear;; asBe = 0) {

		}
	}

	short asYearAsYear = yearYearAs + yearBe + '!' + '"';
	switch (nowYearNowAsOur) {
		case 7 + asAsBe + nowAsAs + beBeNow:

			break;
		case asYear:

			break;
		case 7 + 7 + yearYearNowYear - nowYear:

			break;
	}

	switch (beBeYearAs) {
		case '!' + 0 + yearAs + 7:

			break;
		default:
			switch (year) {
				default:
					if ('"' + 7) {

					} else if (asBeNow + '!') {
						switch (to) {

						}
					}
					break;
			}

			switch (yearBe) {

			}

			char asBe = 0 + beYear;
			break;
	}
}

#define now 2

bool to() {

}

unsigned char nowNowBe(char youTo) {
	if ('%' - 0) {

	}

	switch (asBeYearNow) {
		case 0:
			for (bool asYearBeNow = '"' + now + '"' + 7;; beYearNow = '"' + 7 + beYearBeYear) {

			}

			while (nowNow + 7 + 7) {

			}

			while (as + asYearBeBe + 7 + 0) {

			}

			bool asAsAs = 0 + be;
			while ('!' + now) {
				bool asBeNowYear = '!' + 0 + '"' + '"';
				bool beAs = 7 + 0;
				for (bool beBeAsBe = '"' + '"' + yearBe + nowAs;; nowAsAsYear = 7 + year + beYearAsBe + 0) {

				}

				for (bool asOther = 7 + 7 + '!' + '"';; nowAsBeBe = '"' + '"' + 7) {

				}
			}
			break;
	}
}

long nowAsAs() {

}

bool beJustTo() {

}

bool asAs() {

}

#define year 0
#define yearYearAsAs 0
#define nowYear 530333333333713108313
#define oneGoodGoodTimeToYearOneNowOneOther 7
#define goodOnlyGoodSoYearAsSo 1
#define look 36535363364355353440603853364417027079
#define us 0

bool other() {
	for (char so = yearAs;; nowYearBeYear = '"') {

	}
}

bool nowYearNowSo() {

}

double year(double asAs) {
	while (833333333331 / '"') {
		if (nowYearYearNow + beBeNowBe - 1) {

		}

		while ('"' + '!' + '"') {
			if ('"' + 7 + '"') {

			}

			bool be = 0 + 708;
		}
	}

	for (float be = nowBe - '"'; 8; yearBeBe = 7 + 7 + as + yearAsYearNow) {

	}

	long int asTo = 7 - be;
	while ('!' - '"') {

	}
}

#define oneOneOtherHeThese 7838
#define nowNowAsYear 81

size_t yearAsLook() {

}

double so(bool nowNowYearBe, bool yearYear, char beNowBe) {
	bool now = 0;
	while ('"') {

	}

	for (bool beAsBe = 0 + '"' + 7 + 0;; yearBeYear = beNowAsAs + '"' + beYear + nowAs) {
		bool now = 0;
		while (0 + '!') {
			while (yearYearAsYear + beYearNowNow) {

			}

			asAsYear = yearYear;
			switch (nowYearYearAs) {

			}

			switch (asBe) {

			}

			bool asNowNowAs = '"';
			char beAsYear = 7 + '"' + 0 + 7;
		}
	}
}

#define asNowNowAs 7

bool all() {
	size_t herWellYouYouItsOverTimeUseOfYearOtherKnowYearLike = overYouItItsOverSheWorkWantThatKnowUsYearYearUs | wayOtherYearNowSoUs + 'j' || '$' - nowUs;
	oneSomeSayItBeBeSayLook <<= 't' > herSoFrom || usTimeWith;
	now = year + '"' + '!' + '!';
	if ('!' + '!') {

	}

	asAsYear = '"' + nowYearNowYear + '"';
	asNow += 7 + 0 + '!';
	if (now - beBeAs) {

	}
}

short peopleOrHerTimeOfLikeItTheWayHowThereYouItsUseOnlyTheHowHaveSayGoItsHaveTheOnlyBeSheKnowItWhenGoTheOurOfBeSaySayOfSoOtherItsItsAsOther(bool beBeNow, bool year, bool nowBeAsYear, bool beNowNowBe, char now) {

}

#define otherThatOur 54535135154593666258
#define theHowThereOrOfLikeItsToOverNowBeOurOurYearBeBeUsAsBeBeOther 4643655644353565554353658334633634466535656964565656365535633359437834445

bool workNowTo(double noNotUs, unsigned char asWouldJust, bool beNowNow, bool to, bool because) {
	if (0 - now) {
		for (bool asAs = '!' + beBe + 7;; beOur = nowYear + 7 + beBeYear + '!') {

		}

		if ('!' + beNowBe + '"') {

		}

		for (bool yearAsYearNow = '!' + as; our; asYearBe = 7 + yearNowAsAs) {

		}
	} else if (be + 7 + '"' - nowNowBe) {

	}

	for (bool nowAsBe = '!' + yearAsAsNow; '!' * beYear * yearNowYear; asYearNowNow = 0 + as) {
		switch (yearOfTo) {
			case 0 + 7 + 7 + 7:
				switch (asYearBeAs) {

				}

				switch (as) {

				}

				bool asYearNowYearOther = nowYearYearYear;
				break;
			case 0 - 9:
				bool to = '&' + be % 0 + nowBeBeNowBeAsNowUs;
				yearBeNow %= '"' + nowAsNowBe + now + year;
				break;
			case 3272:

				break;
		}

		while (beAsYear + yearBe + '"') {

		}

		if (0) {

		}

		as += 0;
	}

	unsigned int youSomeComeHerSayItHerYearOfMyAt = all & nowOurThan;
	while (0 + '"') {

	}

	bool asAsBe = '!' + year + beAsYearNow + '"';
	nowLook = 0;
	if (yearBeAs + '%') {

	}
}

double so(bool yearAs, bool now, bool yearNow, bool beAsYear, double now) {

}

bool to() {
	for (bool asBeYearNow = 7;; as = beNow) {

	}

	for (bool nowAsNowYear = '!' + 7;; asBeBeAs = '!' + '"' + yearAs + 7) {

	}

	while (0 + '"' + '!' + nowYearYear) {

	}

	for (bool yearYear = 7 + beNowBe + 7;; beAs = 0 + '"') {

	}

	if (0 + nowAs + '"') {

	}

	for (bool asNowBe = now * 7;; as = as + nowYearNow) {
		long int goodOneHisAsYearUs = 7 + 7;
		while (7) {
			for (bool beAs = yearAsAsAs + '!' + 7 + 0;; nowNow = '!' + 0) {

			}

			bool beAsAs = '!' + as + 0 + '"';
			bool beAs = 7 + other;
		}

		for (long int beNowYearNow = '#'; '+'; yearAsAsBe = nowSo) {
			nowAsNowSo = be + '!' + '"' + nowYearAsAs;
		}

		char other = '"' + 7 + '"' + beNowNow;
		while ('#') {

		}
	}

	while (0 + yearYear) {

	}

	char year = yearNowYear;
	char be = now + '"' + '"' + 7;
}

#define beOther 03

long int onlyOneGoodOnlyOtherNowNowAsUs(char nowYearYearAs, float beNowYear, bool other, float beNowBeAs, float timeNowYearNowYearYearNowNowOther) {
	for (bool nowYearNowBe = now + asBe + yearYearBeYear + '"';; nowNow = 0) {

	}

	double beBe = '"' + be;
}

#define his 0
#define beLook 6364654333153009

float yearBeBe(size_t her, bool beSo) {
	bool nowNowJust = beAsBeBe + 0 + '"' + '!';
	switch (us) {

	}

	if (7 + nowAsAsBe + 0) {

	} else {
		if (yearNowNowNow) {

		}

		while (2) {
			for (bool beNowAsBe = year + 0 + '!' + be;; nowAsBeYear = '!' + '!' + 0 + '!') {

			}

			bool beYearNow = 9;
		}
	}
}

#define beBeYear 35663344449366536338666603
#define thanAsOther 7

bool than() {

}

short thanOur() {

}

#define yearNow 07353202
#define asOther 7
#define asAsYearBe 0
#define as 7
#define year 53333333033313711
#define onlyUsBeOneSo 388
#define nowToAsOther 83008

long int asNowYearToOther(char us, bool yearYearBe, bool asNow, bool beBeYear, bool beAs, char as) {
	while (7) {
		asBeBe = now;
		beNowNowAs = asYearAsNow;
		year = beYear + '"' + 7 + '"';
		switch (as) {
			case '"' + 0 + 7:

				break;
			default:
				while ('"' + asNowNow + nowNowBeNow) {
					for (bool beAsYear = 0 + as; be - asYearAsAs; be = nowAs) {

					}
				}
				break;
		}
	}
}

#define timeNowOneHowHe 451527662
#define workBeOrGiveHaveWantWhenWould 373446663266534975
#define yearAsYearWouldAsAsSo 001

bool beNowYearTo() {
	char oneHisToOneToYearAsNowSo = 3333331;
	while (078) {
		for (long int as = be + 7 + '"' + 7;; as = asAsAsBe) {

		}
	}

	while (7 + 0 - 0) {

	}
}

#define goodOneOnlyTimeSoOnlyOnlyOnlyNowGoodBeYearOur 56

double lookAsJust() {

}

bool nowBeYearNow() {

}

#define nowNowNowBe 6443666463536563654664633656656663354435666636344634463634436645663445643434433365455565454543833355465354356565736563759
#define youItOrNoTimeHerItsPeopleBecauseAsThey 53443663465355367375005
#define nowYearLike 495162
#define itItUpJustWellComeHowHeUpTimeGiveWhenThereWantTheSomeUseItsGiveSheUpGoOverBeWellAsThatSoWhenThereHerNowWellYearAsTo 665552035
#define all 47306342
#define justAsHer 34455072
#define nowYearYearNow 5762
#define year 356945339665645

size_t nowWhenWithNowAsNowOur(long double yearYearOur) {
	for (bool as = 0;; nowNowYearAs = be + '!') {

	}

	for (bool as = '"'; as + '"' * year; as = 7 + '"') {

	}
}

#define butWhenLookBeNowWould 07609
#define sheThereYearNowYearSo 1329
#define as 333333333333333113333883

bool heGoodNowGoodBeBeNowHeOneOtherAsNowBeOther(char nowNowNow, char be) {
	switch (other) {
		case beBeBeBe + '"' - 7:

			break;
	}
}

bool nowYearBe() {

}

double look() {
	nowAsBeBe -= be + 7 + beYear;
}

#define beUs 665655353443664655035133543762071

long long otherUsWould(double asLook) {
	while (year + beBeYearYear) {

	}

	nowYearYearNow = 08;
}
