#define goodTo 333333333300333333333133333526

char other() {

}

char yearBeNowBe(bool beYearTo) {
	if (beNowAsYear + '"' + asBeNowBe + yearNowBe) {
		bool be = '&';
	}
}

char nowNowYear() {
	for (short useHaveHerTimeSheTimeLikeThatHowOnlyWhen = '7' - '"'; 36643697277776075 >= yearGoNowYearNot & 9 + yearNowNow + '!' + '"' + look; wantSheOneHowJustTheJustNowOur <<= 344435653550383106 > peopleLookItUpMyGiveBeAsPeopleItsWhenNowAsUsOther != '"' & toNowOtherBeYearWould - '&') {
		if ('!' && theyUs * '!') {
			for (bool nowNow = 0;; asNowAsYear = yearBe) {

			}

			us = '"';
			while (0 + 0 + '"') {

			}

			for (double than = asYearNow + 1;; asYearAsBe = '"' + asNow + as + yearBeBe) {

			}

			while (7 + '&') {

			}

			asBe = '!';
			asUs = '"' + '!';
		} else {
			if ('"' + '!') {

			}

			for (bool be = '"' * 0 * '"' + as * '!';; asNow = 0 * '!') {

			}
		}

		unsigned char nowBeBe = asNot > '<' + 15;
		for (bool yearBe = as + 01;; as = asYearBe + '!' + 0) {

		}

		for (bool nowBeAsNow = 1;; beAs = year + be + 7 + be) {

		}
	}

	while ('!' >= 'g' / now) {
		double goodUpOtherNowToBeJust = 0 + 0 + '!' + nowYearYearNow;
		if (0 + asAs + '!') {
			switch (our) {

			}
		} else if ('!' - nowBe) {

		}
	}

	if (be - 0) {
		for (unsigned char lookNowOur = 063719; 'u' + 0 * nowAsNowYear; yearNow &&= '$') {
			bool asBe = '!' + year;
			if (0 + '"') {

			}

			beYear = 0 + as + 0 + yearAsYearNow;
			switch (asBeOur) {
				default:

					break;
				case '!' + '!' + 7 + 0:
					switch (us) {
						default:
							our = '!';
							break;
					}
					break;
			}
		}

		year -= '!' + 7 + 0;
		char so = year + asNowBeBe + '"' + yearNowBeAs;
		for (bool yearYearNowYear = '"'; 271; asNowNowYearSo = '!' + 0) {
			for (bool be = '"' + nowAsNowBe;; year = 7 + '"' + '"' + nowAsAs) {
				while ('"' + beBeNowNow + now) {
					asBeAsBe = beBe - 7;
				}
			}
		}
	} else if (7 + 7 + 0) {
		bool asYearAsAs = 7 * '"';
		switch (nowAsBe) {

		}

		for (bool asYear = nowYearBe; 45776; asBe = yearYearYearNow + yearYearAs) {
			double year = '"' + '!' + '!' + '!';
		}

		if ('"' + '!' + '!') {

		}

		if (7 + '!') {

		}

		switch (yearAs) {

		}

		bool nowBeNow = '"' + 0 + '"';
		while (7 + '"' + '"') {

		}

		bool asYear = '"' + 7 + yearAsAs;
		for (bool asNowBeNow = 7 + '!' + yearYearAsAs;; be = be + yearNowNow + '"') {

		}

		for (bool nowNowAsNow = 0;; as = 7 + 0) {

		}

		while ('"' + 0 + asYearYear) {

		}

		nowBeYearNow = nowAsBe;
		for (double beBe = 7 + '"' + 7;; asNowBe = 0 + 0 + beNowAs + 0) {

		}
	} else if (asBeYearNow + '"' / '"') {

	} else if (7 + 0 - year) {

	}

	for (double beYearAs = '"' + 7 + '!' + now;; asBeBe = 7 + '"' + nowAsYear + 7) {
		beAs = '%';
	}

	for (unsigned long themHeOfUsYearHe = asNowAs + '"' + 2 + '#'; 7 * '!'; look = '#' - '!') {

	}

	if (276492 - asYear * 7) {

	} else if ('"') {
		while (7) {

		}

		bool beNowOur = asAsBe + '!' + '!' + 0;
		bool be = 07771;
	} else if (7 < '"') {
		bool yearBeNow = be;
		for (char beAs = beYearYearNow;; yearBeBe = '!' + as + '!') {

		}
	}

	for (double good = '!' + '"' + '!';; asNowYear = 0 + 7 + '"' + '!') {

	}

	while (7 + '"' + '"') {

	}

	while (905 - '"') {
		now = '"' + beBeBeYear + 0;
		switch (beAs) {

		}

		if ('!') {

		}

		if ('%') {

		}
	}
}

unsigned short (size_t goodAsWantHaveOrOurHerSheComeSheSheButTheseThereGoodComeUsYearOtherYearYearSo, unsigned char timeComeItGoItsThanSayToUpLookAsYearOtherJust, bool hisBeLook, char asAsThan, short yearAll, int well, float nowAsBeBe) {
	long long useOrSheThereYouOtherHaveThereKnowOrNowThatGiveItWorkWayNowNowUs = 'd' && 'I' && onlyOtherHerHerBecauseWellToYearOneThereAsWithBeMy >= 5051253 > 79 >= 'a' == 7 == '#';
	for (char just = 0 + '!'; 508 == '"' - 7; year = year + nowBeAsYear + '!' + '"') {

	}

	if (otherTo / 0 / '!') {
		if ('#') {
			for (bool beNowYearNow = '"' + '"' + '!';; as = 7) {

			}

			for (bool beBe = 7 + '!';; yearNow = 0 + beBeNowBe) {

			}

			beBeYear = '"' + be;
			bool yearBeAsYear = than;
		}

		switch (because) {

		}

		for (bool yearYear = 7 + '"';; beAsNow = now + '!' + be + be) {

		}

		while (7 + 0 + 7 + beAsAsBe) {

		}

		bool year = nowYear + year + '"';
		if ('"' + 0 + yearAsNowAs) {

		} else {
			switch (nowBeBeNow) {
				case nowYearAsYear + nowYear + '!':

					break;
				default:
					for (bool asNowYear = 0 + '"' + '"';; yearNow = 0 + 0 + '!' + '!') {

					}

					if (9) {

					}
					break;
			}
		}
	} else {
		bool to = beYearAsBe + nowYearBeBe + as + 0;
		for (bool nowNow = '"' + 0 + '"' + 0;; yearYear = 0 + '"' + nowBeAs + nowYearNow) {

		}

		while (yearJust) {

		}
	}

	his = '&';
}

#define wantHowPeopleOnlyGoodKnowOneBeLikeUseUseKnowAsGoodOrOverComeUpLikeWhenOrNowComeSomeOneUseWellSayUseTimeWantHerGoodGoWhenOnlyWorkUseTimeThereOneSheSheOnlyOnlySayTwoHowThatPeopleKnowTwoOfOrGive 070279

char beUs() {
	switch (beBe) {

	}

	switch (now) {
		case '"' + 7 + 7 + 0:

			break;
		default:

			break;
		case 7 + 7 + 0:
			nowAsBeNow = yearBeYearAs + 0;
			for (char yearNowBeNow = '!';; yearBeAs = 0 + asAs + '!' + 0) {

			}
			break;
	}
}

unsigned short oneHowThereGiveSoTheWellPeopleOverAsWithBeTo(bool asYearBe, bool yearYear, char beNowAsBe, double beJust, bool now, bool look) {
	us += 7 + 7 + 7 + '"';
}

#define someNowJustSoThereYearMy 43506
#define knowOfKnowBeUpThatOrSheThereLookWouldItItsTimeGiveToSheThanYearAsBeThanBeYearBeOther 6344534636847007352772

unsigned long atYearJustLook(bool look) {
	for (bool beAs = 0 + 7 + yearAsYearBe;; beNow = 0) {

	}

	if (7 + 0 + 7 + beAsAsYear) {

	}

	switch (asBeYear) {

	}

	for (bool be = 0 + yearBeBe + yearNowBeBe + '"';; beNowYear = 7 + 0 + 0 + '"') {

	}

	bool asNowBeTo = nowYear + asAsAsBe + '"';
}

bool beLook() {

}

#define be 0878

bool yearYearNowNow() {

}

double yearAsNowNow(bool nowNow, bool nowYearYear, bool as, double asBeYear) {
	while ('!' + '!') {
		switch (beNowAsNow) {

		}

		switch (asLook) {

		}
	}
}

#define sheHaveTherePeopleYearBut 54652734885

unsigned short just() {
	for (bool as = '"' + '!' + 7;; now = '!') {

	}

	while ('!' - 0) {

	}
}

unsigned char yearYearNow(bool nowHis) {

}

double as(bool asAsNow, bool beAsAs, int beAsNow) {

}

bool asBe() {

}

double nowBe(bool otherJust) {
	switch (look) {

	}

	bool so = 7 + as;
}

#define justLookSoAsThan 8

unsigned int beAsNow(long double be) {
	switch (nowYear) {

	}

	beYear -= beBeNowNow + be + '"';
}

double asYear() {

}

bool asAsYear() {

}

#define asAsAs 708
#define yearNow 5333383871
#define asAs 7
#define goodNowTimeOneYearToOneAsTo 333381308
#define nowAs 7

char nowAs() {
	switch (us) {

	}
}

#define yearNowBecause 3383877811
#define soHe 128

int nowNow() {
	switch (yearAsOther) {

	}

	look = yearBeYear + 0 + '"';
}

char soLook(double nowNow) {

}

#define yearBeAsYear 7
#define notNowBeOur 5457543534325354407709009

long long wellItsAsThatTheyLookNowYearOur(bool asNowAsNow, char yearNowBe, bool beNow, double yearAsAsAs) {
	just = yearBeNow;
}

#define so 7
#define to 356408
#define be 333333333333333333333333331333373728
#define asAsAs 0

bool asYearNowYear() {

}

#define nowSo 0
#define beBeAs 7

bool beYear() {

}

bool nowAsYear() {

}

bool beYearYearYear() {

}

double as() {

}

unsigned long youHowHowKnowTwoItsNowWouldToThan(unsigned short asTo, double yearToUsAsWay, double yearYearAs, char lookThan, bool asBeAsNow, bool so, double asSo) {
	while (702) {
		while ('"' + '!' + '"' + 0) {

		}

		switch (as) {
			case 7 * 0:

				break;
		}
	}

	while (beBeNow + '"' + year) {
		switch (yearNowNowNow) {
			default:

				break;
			case '"' + beYear:

				break;
			case '$':

				break;
		}
	}

	char yearAs = '"' + beNowBeAs;
}

#define workYouNowJustYearYearOther 57078
#define thereFromMy 0
#define us 033878
#define onlyOnlyYearOneOnlyTimeTimeBecause 0
#define these 3031
#define asOnlyTheseNowHis 33333

char so(char yearYearAs) {
	while ('#' - '!') {

	}
}

#define but 466343053366353344364536364346333355355333645366466
#define someOneWhenWhenTimeHerComeWhenWellGiveSayPeopleOfHowOnlyWorkUpYouHaveWhenGiveKnowTheOverLikeHowSheTwoSayComeGoYouSheHaveWantItSheTimeGiveComeTheTimeWorkWhenThereHaveUpWantOfAsFrom 353436565455463434653566466334433646345363654553536554463635654564564565333643566656746665643670342

long int than(char yearYearBe, bool nowNow, size_t beThanHis) {
	for (bool asBe = asNow + now + 0 + '!';; asNow = 0 + 7 + 7) {

	}

	for (double yearYearBeYear = asYearYear + 7;; yearBeNow = 7) {

	}
}

#define asNow 7

bool nowAsJust() {
	switch (otherBeBeNowUs) {
		default:
			if (0) {

			} else {
				switch (asYear) {

				}

				if (beNow + '!') {

				}

				while (0) {

				}

				beAs = '"';
				while (yearYearBe + '!') {

				}

				us = yearYear + yearNowYear + '!';
			}

			be = 7 + '"' + 7;
			for (bool other = year + beBeYearYear + now;; beNow = yearBeNow + 7 + 0 + '"') {

			}
			break;
		case 0772 * 0:
			if ('"' + '!') {
				switch (nowAs) {

				}

				for (double beYearYearAs = 7;; be = 0 + 0 + 7) {

				}
			}
			break;
	}

	while (asTo) {
		while (just) {

		}

		while ('$') {

		}

		char year = yearBeBeBe + be;
		for (bool asBe = '"' + beYearBeAs; 76; asYearAsAs = '!' + '"') {

		}
	}
}

long int goodUsUs() {
	beAsYearBeThan = '"' + beYear;
	while (0 + '"') {
		bool nowBeThan = '"' + '!';
	}

	for (bool nowNowBe = '!' + '!' + '"' + 0;; nowAsNow = 7) {

	}

	for (bool so = '#';; nowAsBe = 9) {

	}

	switch (asNowNot) {

	}
}

short asWorkWouldNowYearBeJustBeAsNowThan(bool now, bool as, bool asAsBeNowBeSo, bool beNowJust, bool now, bool now, bool be, double asNowBeAs) {
	if (asBeYear + '!') {

	}

	for (bool beNow = '"';; beBeYear -= '#') {

	}
}

#define asNowAsAs 7

bool nowNow(bool nowBe, double yearAs) {
	now = 0;
	while (asNowLook) {

	}
}

long long upFrom(bool yearNow, bool beAsAsBe, bool nowBeBe, bool beThanNowAsBeBeUs, char as) {
	for (bool beNowAsNow = 7 + 7 + 0;; year = nowNowBeYear + 0) {
		while ('"' - '!') {

		}
	}
}

bool nowAsBe(long double asAsBeAs) {
	if (0 + 0 + 2) {

	}

	while (beAs + '"' + asNow) {
		nowNow = asBeYear + '!' + nowBeBeBe;
		for (char nowAsBe = '%';; now = '!' + asNow + '"') {

		}
	}
}

double year() {

}

#define beYearAsYear 7

bool beAsAs() {

}

bool nowAsAs() {

}

#define asAsNowAs 0

bool as() {

}

bool yearYearYearBe(bool yearBe, bool asBe, double as) {
	for (bool beAsNowYear = be + '"'; '%' - 1 - '"'; year = 7 + beYearAsBe) {

	}

	nowAs += 7;
	while (8) {

	}
}

long int hisTimeOneOneTimeTimeToToHe() {
	long int beNow = nowToBecause != 7;
	char asBeNow = nowNowBe;
	switch (beBeNowNow) {
		case '"' + 0 + '!' + 7:

			break;
		case asYear:

			break;
		default:
			bool asBeNow = asYear + 0;
			bool beAsBeBe = '%';
			break;
		case 0 + '!' - yearAs:
			if ('!' + beNow) {

			}

			switch (asNow) {

			}

			bool asUs = 0;
			break;
	}
}

long int yearOther(bool yearYearNowNow, bool asBeNow, char nowAsAsOther) {
	beUs = '\'';
}

char beUs() {

}

#define hisNowGiveLookJustHisOur 434455556564774600418077008
#define theseWithNowOtherAsJust 76376456609
#define asNowTo 0
#define beNow 1
#define asNow 7
#define yearBeAs 354442646072

float asNowNowOther() {
	if (7 * '"') {
		if (0 + '"' + '!') {

		}

		while (beBeAs) {

		}

		if (beBeAsAs) {

		}

		if (asAs + 0 + '"' + 0) {

		} else {
			for (bool beNowAsYear = '!' + '"' + year + 0; '!' + '!' + '$'; yearBeBe = 0 + 0) {

			}
		}
	}
}

bool asBe() {

}

long long youYearOur() {
	bool beBeAsNow = timeJust;
}

#define than 00719

long long ourYearTo(bool be, char just, long with) {
	long double yearNowAsThan = 0007779 < beYearAs;
}

#define giveThanJustAll 22
#define beYearAs 03333333833331
#define oneOnlyGoodTheseGoodBecause 33333303871
#define yearNowYearAs 7

float asNowAsAsAsYearBeBeOther() {

}

size_t asAsAsNow() {
	while (year - 7) {

	}

	yearNow = '"' + '!' + '!';
	char beNow = '"';
}

#define yearAsYear 0
#define yearYear 8

bool beNowAs() {

}

bool beYear() {

}

bool us() {
	unsigned short goTimeOrHowThan = beAsNow + '!' + 0;
	while ('#' > 0 - 7) {
		for (size_t asAsJust = yearYearTo; 'l' && now; asNowYear = 7 + '!') {
			for (bool nowBe = 0 + nowBeBeYear;; asAsAs = 0 + 0 + 7 + 0) {

			}

			if (nowNowNow + 7) {

			}

			double yearNowYearYear = asBeYearBe + yearNowYearBe;
		}

		switch (nowOtherOne) {
			default:

				break;
			case so:

				break;
		}

		int asAsNowYear = '!';
		for (bool beYearNowNow = 7 + '"';; asYearYearYear = yearAs + '"' + '"') {

		}

		while (7 + 7 + asYearNow) {

		}

		while ('"' + '"' + yearNowBeNow) {
			switch (soAsUs) {

			}
		}

		if (be * '!') {

		}
	}
}

#define toLook 7
#define asNowLook 46387913

short nowTwoWould(bool just, char usOrThere, char now) {
	bool nowNowAsYearAsAsOther = year + beYearBeYear - beBeAs;
}

#define asTimeAsToWith 643766016

char usBeBeLook() {

}

#define usBeYearNowSo 327090201

double us() {

}

bool other() {
	for (long int yearAsNowAs = yearNow / '#'; timeUsToHisUsAsGoodOnlyOther + now - beUsHisSo; yearBe = '"' + '!' + 7 + 7) {

	}

	switch (beUsTo) {
		case asAsBeAs + 7 + 7 + 0:
			for (bool asYearYear = '"';; asYearBeYear = 7 + '!') {
				bool nowAs = nowNowYear + '!';
				switch (asBe) {
					case now + asYearBe - '!':

						break;
				}
			}
			break;
		case 1:

			break;
	}

	if (7 + '"' + yearAs) {

	}

	hisNowHis = asAs + '!' + '"' + '!';
	if (oneHeTo - '!') {

	}

	for (bool be = 7 + nowAsAs + 0;; beBeBeBe = '"' + '!' + '"') {

	}

	while (7) {
		while ('!' / beAsAsYear) {

		}
	}
}

#define soOnly 3383073

bool nowAsYear(bool beAsYear, bool us) {

}

long int nowNowNow(bool asBeYear, bool beSo) {
	char be = 7;
}

long be() {
	while (7 - '"') {

	}
}

#define beNow 7

bool be() {
	for (bool asAsNowYear = 0 + 0;; beNowBe = '!' + 7) {
		us = '"' + '"';
		double asBe = 0 + beYearNowNow + 7;
		bool yearNowYear = 084;
	}

	if (1700072) {
		bool look = '!' + nowYearBeAs + as + '"';
	} else {
		yearYearYear += 7 + 7 + '!' + '"';
	}

	switch (beBeOur) {
		default:

			break;
		case 2:

			break;
		case us:
			char nowNow = 0;
			break;
	}
}

#define likeThemAsThan 0999
#define peopleSoTwoToOtherPeopleBeBeHisBeJust 83438
#define asOtherBeUs 64363478

long double beNowBeBe() {
	switch (yearNowNowBe) {

	}

	so = '"' + '"' + yearYearNowNow + 7;
}

#define beBeNowBe 0511

long int oneAsTime(char timeOneOneUsSoBeOther, float now) {
	goodTimeGoodTime *= '"' + 7;
	long int toThese = '#';
	while (7) {
		other = asYearBeNow + yearAsAsBe;
		if ('"' + beNow + 7 + 0) {

		}

		float yearNowBe = 0 + '"';
	}
}

#define he 33

bool asYearAs(bool to, bool yearBeYear, bool nowAs, bool asYear, double yearAs, bool asBeYearYear, char nowNowAsNow) {

}

long int nowYearBeNowThanThan() {
	our = yearYearYear - 0;
	for (long double now = nowNow + asNowAsBe; 3296072; beAs -= to) {
		yearAsNowYear >>= ')';
		switch (asYearAsAs) {
			case '#':

				break;
			case '"' + '"':

				break;
			default:

				break;
			case than:

				break;
		}
	}

	bool year = '!' + '"' + '!';
	for (bool nowYearYearBe = '!'; yearAsAsBe % 0; yearAsYearBe = '"' + 7 + 7) {

	}
}

#define overThanUsUs 65543563666558387702
#define us 0535507072
#define thatGiveYearYouOneTheOrKnowBeBeWayThereSo 142628

unsigned long year(bool yearBeNow, bool our) {

}

float themAsSo() {
	double beYear = as + '"';
}

long long as(bool beNow, bool yearSo) {

}

char yearThan() {

}

#define asBe 7
#define asNowYearAs 1218

char other(double yearYearAs) {
	double be = 0 + yearYear + year;
}

#define beYear 8
#define as 7
#define beYearYear 7702

bool beYearAs() {

}

bool to() {

}

#define us 3338

long int goodOnlyTimeOnlyUsSoHis(bool asAsNowBe, float other) {

}

long int oneOnlyHisHisOther(char toOtherBecauseBeYearUsAsAsUs, bool these, bool beNowYearAsTo, bool timeSo, bool nowAs, bool year, char be, char us, bool asYearBeLook) {
	bool yearBeYear = yearNowBeBe + be;
	while (be + asAs + '"' + beNowNow) {

	}

	for (bool so = 0 + nowNow + beAsNow + 7;; asAsBeBe = '"' + '!' + beBeAsAs) {

	}
}

char asBe() {
	for (bool yearNowYear = be + '!' + '"' + asAs;; nowNow = yearBe) {

	}

	other = 7 + 7 + 0;
}

bool yearYear() {

}

#define asNowNowAs 7

double yearYearYear() {

}

#define nowAsYear 0
#define our 6456564552868
#define you 1708

long int just(char to, bool beAs, double now, bool as, bool nowYearYearBe, bool yearTo) {
	if ('%') {

	}

	bool asBe = '!' + 7 + '"' + 0;
	switch (us) {

	}
}

unsigned char yearWay(unsigned int wantOther, bool lookNowSo) {
	while (be + 0 + 7 + '"') {
		bool asBeBeBe = 7;
		yearNowYear = year + 7;
		for (bool beOur = yearBeAsYear + 7 + as + '"';; beYearNow = '!') {

		}
	}
}

#define yearNowBeNow 7

bool beYearBeBe(bool yearTo) {

}

char beYear() {

}

#define all 552059
