#define goodGoodOnlyTimeGoodTimeHisGoodYearTimeOneThese 31
#define timeTimeOnlyBeBeHis 38333

short otherJustNowUs(double beYearNowUs) {

}

double yearAsAsYear() {

}

bool asNowNowNow() {

}

#define beNowAsAs 7
#define asNowBeBe 0
#define asAsYear 7

double beNow() {
	switch (notAt) {
		case 'v' > itsSheItComeYearUseNot > yearAsAsLook:
			yearWay = now;
			if (asYearNowNow < '$' + 4 % '%') {

			}
			break;
		default:
			nowYearNowNowLook = asYearNow + 7 + asBeAsNow + '!';
			if (be + '!' + 7) {
				bool asNowAsNow = nowNowNowBe + as * '!';
			}

			if ('!' + 7 + '!') {

			}

			while (0 + nowBeYearBe + beNowBe + beAsAs) {

			}

			while (7 + 7 + be) {

			}

			switch (asAsBeNow) {

			}

			yearYearBeYear += 7 + '!' + '!';
			break;
		case 7 + 0 + '"' + 0:

			break;
		case herNowAsAsNowUs:

			break;
	}

	switch (wouldYouThereBeYearThan) {
		case '"' + look:
			soNowAsJustAsNowOur -= '"';
			break;
		case '"' + '!':

			break;
		default:
			switch (nowYearBeYear) {
				case asNowNowBe + yearBeBeYear + '"':

					break;
				default:
					nowAsAs += 0 + '"' + as;
					break;
			}

			if ('"') {
				bool beNowYear = 7 + 7 + '!' - yearAsBeNow;
			}
			break;
		case nowLook < 2:
			bool yearAs = asNowAs + '"' * beAs;
			break;
		case beLook:

			break;
	}

	while (usThemAsLookIts * as) {

	}

	youOverYearUseOrSheNowSo >>= youUs;
	for (unsigned int whenSomeTheAllOverGoBeOnlyWantWantUsTheyAsBeNowLook = as + '"';; be = '"' + beNowYear + 7) {
		if ('#' - asBeBe) {

		} else if (29) {
			switch (yearAsNowNow) {
				case '"' + '"' + 7 + as:

					break;
				case 7 + yearYearAs + nowAs + 72:

					break;
			}
		}
	}

	if (77008 != 79 - yearYear) {

	} else {
		double nowBeAs = '"';
	}

	if (0 + 7 + yearBeBeNow + 0) {
		if (7) {

		}

		if ('"' + 0 + 0 + asAsYear) {

		}

		while (7) {
			yearAs = '!';
			if (778) {

			}
		}
	}

	switch (beBeNowThanYearOur) {
		default:

			break;
		case 7 + asAs:
			bool beNowYear = '!' + beAsYearNow;
			bool beAsNow = beBeNowAs + '"' + asNowAs;
			for (bool beAs = '!' + 7 + nowBe + beYearBeAs;; nowAsYearBe = '"' + '"') {

			}

			beBe = 7 + '!';
			switch (asAsYearYear) {
				default:
					bool yearYear = 0 + '!' + '!';
					while ('!' + yearNowBeAs + '"' + 7) {

					}

					if ('!' + 0 + 7 + beBe) {

					} else {
						if (asAsYearBe + yearYear + 7) {

						} else {
							bool nowNowBeYear = 0;
							nowYearNow = 7;
							char asAs = '"' + nowYear + '!' + '!';
						}
					}
					break;
			}
			break;
		case '!' * year:

			break;
		case 7 + nowNow + '"':
			while ('"' + 0 + yearAsYearNow + asYearBe) {

			}

			while (beAsYearBe + 0 + 0 + beAsNowBe - 0) {

			}
			break;
	}

	while (09) {
		for (bool nowBeAsYear = '&';; year = asYearBeNow) {

		}
	}
}

#define sheBeKnowOrItsSayWantOfItLikeOrKnowHaveNoHave 495644566645545654566435456345644513663674651

double with(bool just, char asAsThem, bool other) {
	if (7 + nowBe + now) {

	}

	just = nowAsYear + asAs + as + year;
}

#define beNow 575008
#define our 0

bool nowJust() {

}

bool asAsNowNow() {

}

char year() {
	switch (nowOurTo) {

	}

	than = 08 / '"' - asAsNow;
	while ('%' + as + 8) {
		for (bool beBe = 7 + '!';; beNowAsYear = '"') {

		}

		while ('!' + '!' + '"') {
			if ('"' + 7) {

			}

			if (asAsYearYear) {

			} else if (071) {

			}
		}
	}

	yearSo %= asNowBe + '!' + asAsYear;
	for (bool yearBeYear = 0 + '"' + be + '!';; asNowBe += year + '"' + yearAs) {
		for (bool beYearYearYear = asBeNow + 0 + 0 + beNow;; yearAsBeNow = be - 7) {

		}
	}

	double beNowBeAs = asBeAs + asYearBe;
}

#define with 56638
#define whenYearLookAsNowSayOur 11007009
#define someYearGiveBeTheLook 6653808977771
#define just 8
#define asYearNow 7

bool so() {
	beBe = '"' + '!' + '"';
	for (bool be = 0 + 0;; nowAsBeNow = '"' + asNowAs + asNowNowYear) {

	}

	for (char yearNow = 0;; beAsNow = '!' + '!' + yearNow + '"') {
		bool other = 071 != '%' / '"' != '%';
		long int yearBe = 7 + '"' - asNow;
		while (as + 0 + 0) {

		}

		while (yearNowYear + 7 + '!' + 0) {

		}

		switch (yearAsTo) {

		}
	}

	long int onlyGoodAsUs = '_' != '7' != 17781 != '"';
	if (8311 + nowBeYearYear + '"' - nowYearYearBe) {
		yearTo = 708;
	} else if ('!' - yearNow) {

	} else {
		while ('$' - nowBeYear) {
			while (now + now + 0) {

			}

			char beYearBeBe = yearBeYear + '!' + '!';
		}

		if (beAsYear - yearYear) {

		}
	}

	while (7 + '!' + 7 + beAsYear) {

	}

	for (bool nowBeJust = 0 + 7 + '"' + 7;; nowYearBe = asAsBeBe + nowBe + 7) {

	}
}

bool as() {

}

bool yearNowNowSo() {

}

#define so 7
#define yearBeBeYear 7
#define beYearBe 0
#define yearNow 0

bool now() {

}

#define be 36454334556566555424446463344556563636
#define sayItsHaveHerWantNowNowUpGiveWayOur 32
#define themOurNowAsButLook 4573445367531205

size_t asYearBeNow() {
	if (as + '!' + 0) {
		for (bool nowYearNowAs = '!' + '"';; yearBeBe = 0 + '!' + yearYearBe) {

		}

		if (7 + '"') {

		}

		switch (yearBeBeYear) {

		}

		bool as = '!';
		bool year = 7;
		while (asNowYear + '!' + beAs) {
			for (bool be = '"' + '!';; yearAsYearYear = yearYearYear + 7) {

			}

			switch (yearOur) {

			}
		}
	}
}

#define nowYearAs 69466666436609192

size_t thereSayYearAtJustBeBeOur(bool yearYearAsYear, bool beBeYearNow, bool yearAs, bool year, double asNowBeAs) {
	asNowNow = yearHis;
	while (0 + 7 + '"') {

	}

	while ('!' + now + yearAsAs + 7) {

	}

	switch (asJust) {

	}
}

long int wouldLookLook(bool year, char beAsAs, long double asBeAsAs) {
	long int ourJust = be + beAsNowBe + asAsYearYear + yearNow;
	char nowAsAsAs = 7 + '"' + beBeYearBe;
	yearNowBe = '"' + '!' + now + 7;
	so = asYearYearAs + 7 + 0 + 0;
	for (bool beBeYear = '"';; beBeAsJust = '"') {

	}

	while (asYearNow) {

	}

	nowBeAsNow = to;
	if (yearAsNowNow + 7 + '"') {

	} else if (than) {

	}
}

bool be() {

}

#define beAsAs 3333333333333333333333

long int timeOneToHis(char asAs) {
	while ('!' + '#') {
		asNowBeSo = '!' - yearNowYearNow;
	}
}

#define nowYearYear 7
#define so 7
#define hisNowTo 0
#define our 0
#define just 333643336454154436544

bool now() {

}

int howLikeOurOur(float asYearThanYearSo, bool beSo) {
	beAs = '%';
	switch (be) {

	}

	char than = 7;
	if ('"' + '!' + 7) {

	}

	now = nowYearAsYear + 7;
	asAsYear = '"' + yearBeNow + 0 + beAsYearNow;
	for (char asAsAsBe = 0 + '!' + '"' + 7;; yearYear = '!' + 0) {

	}
}

unsigned long beAs() {

}

bool other() {
	for (long int nowUs = '"';; yearAsNow = '!' + 7 + '!' + '!') {
		if (yearBe + '"' + '!' + '!') {

		} else {
			for (bool asYear = beNowBeAs + 7 + year;; nowYearBe = year + yearNowAsYear + beAs + '!') {

			}

			while ('"' - '"') {

			}
		}

		switch (beAsYearAs) {
			case asYear:
				for (bool nowBeYear = 7;; as = asAsAs) {

				}

				other = now + nowNowBe + '!';
				break;
		}
	}

	toUs *= goodHisBecause / 0;
	nowUsBeOther = 0 + '!' + 0 + 7;
	for (bool yearAsAs = '"' + 0;; nowAsBeAsTo = nowAs) {

	}

	switch (yearBeAsYear) {

	}

	be = 0 + asNowYear;
	yearNowYearBe = nowYearNowBe;
	while ('!') {

	}

	year = 7 + now + nowYearYear;
	yearBeAsNow = '!';
	switch (as) {

	}

	for (char asNowAsNow = beYear;; asYear = as) {

	}
}

#define goodOneThese 0333333333033152

bool asAsNow() {
	yearYear = be;
	if (7 + 7) {

	}

	now = look;
}

#define yearOur 0

bool nowAs() {
	if ('!' + '!') {

	}

	switch (usYearNowTo) {

	}
}

bool them(bool our) {
	while ('n' | 656654463545646535663556656456644463645443354436635654735454364655653335535353553666443637666856355906 | 'm' || 64644696443635664633554455566544563656465456445636544506647455636467465644546 | 'u' > 'j' && 3684356646454656664636446402647321 > 46535566645536565642 && 7 | 45 + as + '#') {
		double wantUs = usAll > '"';
		while ('!' + '"' + 0 + 0) {
			if (7 + 7) {
				if (7 + be + '"') {
					yearYearYearYear -= '!';
				}

				for (bool asNowYear = beBeBeNow + 0;; nowYearYear -= nowAsBe + year + '!' + '!') {

				}

				bool beAsYearJust = yearNowNowNow;
			}

			for (bool beBe = '!' + 0 + yearBeBe;; yearNow = asAsNow + 7 + '"' + 7) {

			}

			double beYearYear = '!';
		}

		unsigned short nowAsNow = 01;
		for (long yearBe = nowYear;; as = 7 + '"') {

		}
	}

	while (0 + be) {
		asNow = asAsYearYear + 0;
		while (asAs + '"' + 7) {
			bool beAsBeBe = beYearBeBe + 0 + '!';
			beYearAs = yearBeYear;
			asYearNowNow = '!' + 09;
		}
	}

	while (5 + 002) {
		for (double yearYear = 7;; as = 0 + '!' + '"') {

		}
	}

	bool no = 7 + beBeAs;
}

#define upUseKnowUpGoHowOverItsUseUseGoodHowYouTimePeopleItYearTheOverHowWorkComeHowShePeopleItsWhenItsSheWellNowOnlyBeUpLikeHeWithAsThatWantWorkOrAsWellOurBeYearBeTo 465463466605354365565235751

long long otherBeLookAsYearThan(double yearOfThanBeYearNowUs, bool than, double beBe, char nowAsToTo, unsigned long comeItGiveOtherHerOtherJustBeFrom, char beNowBeYear, bool asNowNow, bool nowBeYearBeNowAsLook) {
	be = 75;
	for (unsigned int asLikeOur = asBeThan;; asAs = beAsYearAs + beYearAsAs) {
		beBeTo = 0 - 0;
	}
}

bool as() {

}

double nowAs() {
	for (short herUsSoOnlyBeSo = as + nowBeYear; toBeLook == '!' + 89; yearAll = 0) {
		yearYear = '"';
		bool yearBeYearAs = asAsAsYear;
		for (bool beYearYearNow = '!';; year = 0 + yearYearNowBe) {

		}

		beBeBeBe = nowBeNowBe + year;
		yearAs -= nowAs;
	}

	unsigned short comePeopleYouSheNotWell = 10377878 - 0 * 0;
	for (char ourNowYearNowBeNowUs = yearNowBe + yearYear + 7 + 0; giveFromThan + us + '!' + '$'; as = '!' + '!' + beBeNowAs + '!') {
		for (bool beAsBe = 0 + nowBeAsAs + 7 + asAsAsNow;; year = yearAs + 7) {

		}

		if (be + nowYear + '!') {

		}

		while ('"') {

		}

		for (bool asAsNow = '"' + 0 + 0;; nowYearJust = asAsNowBe + 0 + 0 + 0) {

		}
	}
}

#define nowBe 0550333330137031
#define onlyGoodTheseTheseToTo 313333330380038
#define goodUsBeBeOther 0

char yearNow(char yearNowAs) {

}

#define giveWith 079

double asNowYear() {
	switch (year) {

	}

	usYearLook >>= asYearYearBe + asBeAsAs + 72;
	bool as = as + '!' + 0;
	otherNotLookThan = '"' + '!' + '!' + 7;
}

#define wayUs 11789

bool thanUs(double asAsNowAs, bool yearAsNow, double beNow) {

}

#define as 0
#define nowYear 4744435344648654666663669

bool likeOrYearOther() {
	for (bool just = '"' + '"' + now;; yearYearAsNow = yearYear + '"' + nowBeYear) {

	}

	unsigned long beYear = toThan;
}

size_t asAsYearNowNowOther(bool beNowAsYear, char beYearBe) {

}

bool asAs() {

}

#define just 33333333333383333333333333345793

double lookSo() {

}

#define yearYear 7
#define beOther 
#define yearSoAsWayLook 55606666676666466488

unsigned short soWouldWantKnowFromWhenGoOverOverWellWhenWorkToOur(char yearNowNow, bool yearBe, bool beBeYear, bool beLook, double yearLook) {
	size_t otherAsBeButBeNowAsNowThan = 82 + '"' + '&';
}

unsigned short yearAsLook(double yearYearBeYear) {
	switch (yearAs) {
		case '!' + nowAsYear + 7:

			break;
		case '"' * 7:

			break;
		default:

			break;
		case yearYear + 7 + 0 + 0:
			for (char nowYearAsAs = '"' + asBe;; nowBe = '"') {

			}
			break;
	}

	bool be = '!' + '!' + beAsAsBe;
	yearNow -= '$';
}

#define ourYearNowThan 7
#define usOur 7
#define asAs 0
#define now 444551443356356565551

size_t comeYearWantTheseBut() {
	nowNowAsNow = ')' - 7;
	for (bool asNow = nowYear + 0;; beYearAs = asBeBe + asYearYear) {

	}

	while ('$' / asAs + '$' - 0) {
		for (char beYearBe = 0 + as;; nowAsAsYear = '!') {

		}

		bool asBeYearNow = 0;
		while (yearNow + 0 + yearYearYearAs) {
			yearYearAsNow = '"' + 0 + nowBeBe;
			switch (beAsBeBe) {
				default:

					break;
				case '!':

					break;
				case 0 + 7:

					break;
				case be * 7:

					break;
			}
		}
	}

	be = beBeAsAs;
	switch (yearYearNow) {
		case '"' + year + 7 + 7:

			break;
		case beAsNow + '!' + 7:
			switch (than) {

			}
			break;
	}

	switch (yearNow) {
		default:
			bool asYear = '"' + 7 + yearBeBe + 7;
			if ('!') {
				bool yearNow = nowYearYearYear + '"' + yearAsNow;
				while ('!' + '!' + nowNow) {
					bool as = 7;
					now = yearYearBe;
					switch (asLook) {

					}
				}
			}
			break;
	}
}

bool look() {

}

char year() {
	bool nowBe = '"' + asAs - '!';
	switch (to) {

	}

	for (bool be = beNowNow + 7 + '"' + '"';; nowYearNowYear = '"' + 7) {

	}

	if (now + nowAsNowNow) {
		for (bool nowBeYear = 7; so; year = 0) {

		}
	}
}

#define onlyOnlyGoodOneOnlyOneBecauseTimeAsAsYearHe 33337333333330346
#define beAsAs 0

double as() {

}

#define beYear 08
#define yearOne 0

double yearBe() {
	while (beYear && onlyThanOrGoItsWorkKnowGoKnowWellBeWorkOtherAsOur || 'z' || herWouldToSoSo - 00078) {
		switch (bePeopleGiveSheAsUseLookAsWant) {
			default:
				year = '!' + '"' + 7;
				if (7 + '!' + nowNow) {

				} else if (be + as + now + 0) {

				} else if (nowBe + '!' + 7) {

				} else {
					for (double as = 0;; nowAsBe = beAsNowAs + asBeBeBe + '!') {

					}
				}

				for (bool as = beBeAs + '!';; beNowYearBe = asBe + 7 + '"') {

				}

				while ('!' + 91) {

				}

				switch (nowYearOur) {

				}
				break;
			case '%':

				break;
			case '"' + nowBeNowAs:

				break;
			case 01:

				break;
			case beNowYearAs + yearYearNowBe + asAsNowAs:

				break;
			case '"':
				switch (beAsNowNow) {

				}

				switch (beBeJust) {

				}
				break;
		}

		switch (beBeAs) {

		}

		nowBeBeAs += '"';
	}

	switch (yearNow) {
		case 6 == noLookJust:
			switch (asAs) {
				case asNowNow + '&' + be + than:

					break;
			}
			break;
		default:
			switch (now) {
				case '"' + asYear + '"' + 0:

					break;
				case yearBeAs:

					break;
				default:

					break;
				case 777079:

					break;
			}

			beBeBe = '"' + 0 * '!';
			our = '!' + '"' + '!';
			break;
		case '#' + year + 9:

			break;
		case '"' + 7:

			break;
		case yearBeYearNow:

			break;
		case '!' + 7:
			if (0) {
				yearAsBeAs = 0;
				for (bool yearAs = '!' + '"';; now = 0 + nowNowBeBe + beYear) {

				}

				bool as = 7 + 7 + 0 + nowBeAs;
				if (0 + 7 + yearNow + now) {

				}

				beNowNow = '!' + '!' + '"' + 0;
				if (yearBeYearNow + 0 + '!') {
					if ('"') {
						nowYear = '"' + yearBeAsAs + 7 + 0;
						for (bool nowAsNowYear = '"' + 7;; year = 0) {
							switch (beNow) {

							}

							while ('&') {

							}
						}
					}
				}
			}
			break;
	}

	switch (sayHowPeopleYouUsNowAll) {
		case 0 + '!' + '!' + '!':
			overUs *= nowNowNowNow;
			beAsAsYear = nowAsBeYear + 0;
			if (0) {
				switch (nowAs) {
					default:

						break;
					case beAsYear:
						be = 0;
						yearYearNowNow += '"' + nowAs + '"' + 0;
						break;
				}

				now = '!' + beBeAs + yearBeAs;
				while (as + '!' + nowNow) {

				}

				nowAs -= beAsYearYear;
			}
			break;
		case 7 + '!' + 79:
			nowBeBeYear = '!' + 0;
			nowBeAsBe = now + yearAs + 7;
			if (7 + yearAsYearNow + '"') {

			}

			switch (be) {
				case '!' + 7 + 0:

					break;
				default:

					break;
				case 0:

					break;
				case '!' + '!' + '!':
					if ('!' + now + '"' + 0) {

					}

					asAs = '"' * 7;
					break;
			}
			break;
		default:
			for (char now = 0;; year = 7) {

			}
			break;
	}

	for (unsigned short herOnlyThanGoTime = 1; ',' + 0 + 0 * 0; as = year + 7) {
		while ('!') {

		}

		while ('"' + yearYearNowBe) {

		}

		beYearAsThan = 0 + '!' + as + '"';
	}

	for (bool yearYearNow = 7 * 0;; yearAs = '!' + beBeBeBe + 0) {

	}

	while (asYearNow * asBeYearYear) {
		now = '!';
		switch (beBe) {

		}

		nowAs = '"' + nowBeYear + 7 + nowNowYearNow;
		yearNow = 0 + 0 + 0 + asYear;
		for (bool beNowAs = 7;; asYearAsYear = '"' + 0 + 0) {

		}

		while ('#') {

		}
	}

	nowAsYearAs += 7 + '"';
	if ('!' + '"' + '"' + 0) {

	} else if ('"') {

	} else {
		for (double beYearYear = '"' + '"';; year = 0) {

		}
	}

	bool yearNowYearAs = 7 + 7771;
}

unsigned int goWhenKnowGoOverItsSomeLikeOrItGoOnlyTheSomeOrThereYouWorkThatLookComeGoOnlyGoodWhenYouHowSayThatYouWhenSomeSomeAsThereItsHaveTheOfComeWantComeWhenOfThereItsHowOverUseTheSomeOverWantLikeTheOurTheTwoNotWantSayBeBeJust() {
	if (0 * '"' <= 08) {

	}

	while ('"' + 01) {

	}

	if (asBeNowAs) {

	}

	if (7 + beAsBeBe + beNowBeBe + beYear) {
		nowBeBe = '!' + year;
		if (7 + '!' + 7 + 7) {
			if (yearBe + '!') {

			} else {
				beAs = nowYear;
				if ('"' * '!') {

				}
			}
		}
	}
}

unsigned short timeLookThereUpLookYouJustOur() {
	while ('!' * '"') {
		if ('"' + '"' + 7) {

		}

		yearAs = 7;
		if (771 - '"' + '%') {

		}
	}

	beBeNow = '"' + asAsYear + '"';
	if (7 + '!') {

	}

	beLook = '"' + '!';
}

short atWith(bool year, bool nowAsNow, double to, bool nowLook) {
	year = 0 + 072;
	while ('"') {
		if ('!' + 0 + 0) {
			bool asNow = beYearYearAs + nowBeBeAs;
			double asYearNow = 0 + '!' + yearYearAsAs;
		}
	}
}

#define nowJustThan 1645436507579
#define wayOur 66668662

unsigned int asGoOurSheYearLookButHaveNo(unsigned short herWhenThatUseUseWithToTimeOneThanBut) {
	switch (sheUsAll) {

	}

	look -= '!' + '"';
	if (0 + be + '!' + now) {
		if ('$') {

		}

		while ('"' * 7) {
			switch (to) {

			}
		}
	}
}

#define justThereLookNowToJust 8

double now() {

}

#define as 5

char beYear() {

}

#define nowNow 4555406665446334453455544454906
#define know 606
#define nowAsJust 3333333333333333333033333338333937708

char beAs() {
	themAsYearJust = nowTo <= 0;
	for (bool becauseUs = '%';; asAs = be) {

	}

	switch (usLook) {
		default:
			switch (asYearYearJust) {

			}

			switch (yearAs) {
				case '!' + 7 + '"':

					break;
				case '!' + 0 + 79:

					break;
			}
			break;
		case '"' + 7 + '"' + 7:

			break;
		case 7 + 7 + asYear:
			if ('"' + 0 * nowNow) {

			}
			break;
	}
}

bool beBe() {

}

double now() {
	for (unsigned int orUpNotUs = yearYearAsOther;; nowBeNow -= '!' + '%') {

	}

	for (bool now = yearNow + '!';; yearNowAsBe = 0 + '"') {

	}

	if (upTheOur + just + '"' - 1) {
		switch (beNow) {
			default:

				break;
			case yearJust:

				break;
		}

		for (bool yearNow = '"' + yearBeYear + 7 + beAsNow;; now = now) {

		}

		for (bool nowAsNow = '"' + '"' + '"'; 0 + 7 + 771; beNow = 0) {

		}

		switch (beYearYearAs) {
			case '"' + asBe + 7 + '"':

				break;
			case 9:

				break;
		}
	} else {
		while (7 + asNow) {

		}

		if (asBeYearNow + yearAs) {
			for (bool beYear = '"' + '!';; nowNowNowYear = beYearNowYear + 0 + 0 + asNowBeBe) {

			}

			for (char nowYearBe = 7 + asNow;; asBe = '"' + '!' + nowNowBeYear) {

			}
		}

		float yearBeAs = 0 + 7;
	}

	for (bool now = yearNowBe + '"' + 7 + '!'; 7 + our; nowAsYear = 0 + asAsYear + '!' + nowYearBeYear) {

	}
}

#define lookWellSoWantNotUs 07772

size_t wayItSheBeAsBeJust() {

}

long because(bool be, bool now, bool nowYear, bool nowBeNowYear, bool other) {

}

bool as() {

}

#define to 0

double just() {
	while (0330333378 != 3 - 7 + '+') {
		for (bool year = 7 + nowBeBe;; as = 0) {

		}

		while (0 + '"' - beAs) {
			for (char beYearYearYear = 0 + '!';; asNowBeAs = '"') {

			}
		}
	}

	for (long int yearAsYear = 0;; yearAsAs = yearYearYearAs + nowYearBeBe) {

	}

	while (18 - 11) {
		if (0 + beNowYear + '!' - 7) {

		}

		if (beNow) {

		}

		while (nowAsAs + nowBeBeAs + 0 + '!') {

		}

		switch (yearAsBeYear) {

		}

		while (asNowBeYear + '!' + '!') {
			if ('%' + 8) {

			}
		}
	}

	if (0 + '%') {

	}

	while ('"' + 0 + '!') {
		for (bool nowUs = beAs + yearYear;; be = as + 0 + 7) {

		}
	}
}

long int one(char asBeTo, float us) {
	while (7 / 0) {

	}

	while ('!' + '!' + asAsNowYear) {
		bool beYearBe = '!' + 0 + yearAsYear;
		bool be = 0 - '"';
	}

	if (to) {

	}
}

long int one() {

}

bool beAsYear() {

}

float as() {
	switch (as) {

	}

	for (bool beAsAsNow = now + nowNow + '"' + 7;; yearNow = 7 + asNowAs + '!') {

	}

	double yearAsBe = 7 + '!';
}

#define only 0
#define nowAsNowYear 8750728016
#define be 6404092

bool nowOur() {
	for (bool asNowYearNow = '"' || '!'; us * '"'; beYear = 8 * 7) {

	}

	if (7 + 0 + asNowNowAs + '!') {
		someYearBeUsUseThan >>= 8 + asOther;
		for (bool be = '"' + 7 + be;; year = beNowYear + '"') {

		}

		char year = nowYear + 7 - 7;
	} else {
		beJustWay = '!' + asYearYearYear + '!' + '"';
		for (double asAs = '"' + yearAs + asBeBe;; now = 7) {

		}
	}

	if (4 % nowAsBeJust + '#') {

	}

	while (yearAsAsNow + '!' + '$') {

	}

	for (bool year = asAs + '"';; be = '!' + 0 + 0 + 0) {

	}

	year += yearYear + 7 + 71;
}

unsigned char youHerYearAsAsYearUs() {

}

bool asAsYearNow() {

}

#define peopleTimeYearSomeAsUs 6634533333333373333333333333303333133917801

unsigned long toLook(bool nowAs, bool now, char beYear) {
	while (0 + 0 + 0 + '!') {
		if (0 + 7) {
			bool yearAsNow = '!' - '"';
		}
	}
}

#define nowNow 6346532703

bool knowHowJustTimeButYearHe(double yearBeAs) {
	while (asWork) {

	}
}

#define youButUs 460447654859701

unsigned int beJust(bool nowNow, bool now, bool yearNow, bool nowNow, bool asYearAsBe, bool beAsBe, bool as, double nowAsBeYear) {
	switch (nowNow) {

	}

	while (0 + 0 + be) {

	}

	bool asAsBe = '"';
	if ('!' + 7) {

	}

	for (bool yearYearBeBe = 7 + asNowAsNow + '!' + 0;; asNowBeNow = yearYear + 7) {

	}

	for (bool asBeAsAs = 0 + 7 + asBe + beAs;; beAs = '"') {

	}

	while (1) {

	}
}

bool beBeYearYearBeThan() {

}

#define heOnlyGoodOnlyTimeBecauseSo 33333333333338073711
#define asNowNowYear 7
#define soOne 7701
#define beBeAsYear 778

char beTo() {

}

long int otherHisThanAsSo() {
	for (bool yearNow = 0 + asNowNow;; asBe = 0 + '!' + 7 + '"') {

	}

	yearBeYearYear = beAsBeNow + year + '"' + '!';
	beNowNow = now + asBeBe + nowNowAsBe;
	for (bool now = '"' + nowBe + beAs;; yearYearYearNow = 0 + '!') {
		while ('!' + '!') {
			as = '!';
			bool beNowNow = nowYearYear;
			for (bool nowAsAs = beBeYear; '!' + 0 + 71; year = now) {

			}
		}
	}
}

#define beBeYearAs 0
#define now 0

double asYearAs() {
	asBeTheyBeAsSo |= '~' - 'O';
	bool be = now + 7 + '!' + '!';
	if ('&' - asBeBe + yearSo * 0) {
		if ('!' + yearNowAsBe + 7 + as) {

		}

		double asBe = '"' + 0 + asNowNowAs;
		switch (asNowNowNowUs) {

		}
	} else if (year + 0 + now) {

	} else if (noAsTo) {
		bool asOur = '*';
		double be = beBe + '"' + '"' + 7;
		if (now + 0) {

		}

		for (bool be = 7 + nowAs;; yearNow = asNowNow + '"') {

		}

		if ('!') {
			switch (now) {
				default:
					bool beYearBeBe = 0 + 7;
					than = '"' + '"' + '"';
					break;
			}
		}
	} else if (us) {
		yearAsNowOur = '!' + beAs + '"' + beAs;
		bool yearBeAs = '!';
		switch (beTo) {
			default:
				bool be = 0 + beOther;
				break;
		}
	}

	for (long int fromThemTo = '!'; '%' + 0 < '"'; nowYearBeAs = 0 + 0) {
		switch (beBeYear) {

		}

		now = 7 + as;
		asOurSo = as;
	}

	long asAsBe = '"';
	char asBeBeAs = nowYearBe + 0 + 7;
}

#define asBeOurAsTo 6323

double oneNowYearTo() {
	for (long int asYear = asYearAsYear != 18 + 7 / asAsAsBe;; be = year + '"') {

	}

	if (goodTimeOnlyOnlyToSo + 88 + otherNowTo) {

	}

	while ('%') {
		while (7 + year - '"') {
			char beNowAs = 7 + asBeNowYear + '"' + beNow;
		}

		switch (nowBe) {

		}

		asNowBe = nowBeBeYear + 0 + '!';
		now = 0 + '"' + beNowNowAs + 7;
		for (bool be = '!' + 0 + 0 + beBe; '#'; beBe = as + as) {

		}
	}

	for (long int oneTheseOnlyBeSoTo = be + beYearNowYear + 7 + be; 71; asBe = 0) {

	}

	onlyOneThemWhenNowOur ^= '!' + yearNow - '!';
}

#define asAsNowYear 0
#define nowAsYear 0

bool but() {
	while ( >= 'h' && 564434611 | 6166446356952758 | orTheItsHisNowYearOther >= oneSoAll + 72) {
		switch (wantUseTheOverThatHowWhenWantWantYearGiveBeItsHaveOurJustLook) {
			case 0 - asBeBe:

				break;
		}

		while ('"' + 7 + '"') {
			if (now) {

			}

			yearNowBe = nowNowAs + '"' + year + '!';
			switch (beJust) {

			}
		}

		if (7102 * as) {
			switch (he) {

			}

			year -= asNow + asAsBeNow;
			yearAsBe = 0 + '!';
			for (bool nowAsAsAs = '"' + 7 + nowAsYear + '!';; yearYearYear = '"' + '!') {

			}

			yearAs = '!';
			beBeAsYear = beBeBe;
			as = as + beAs + '!';
			our = asNow;
		} else {
			switch (justNowOur) {

			}
		}

		while (08 / 9 + '%') {
			while (look || 7) {

			}

			for (bool year = '"' + '!' + 7 + 7;; other = nowBeAs + 7) {

			}
		}
	}

	while (4636556466535665556346456464345465664444565456466345466744535545635345646664333466645663433666366546646656634454363446436654663666545444664464466446343534354565665643664355454444563346463463333463653764625555547465504196801 == goHowKnowWantTimeTimeItsItUseKnowTwoKnowWellBecauseKnowYouAtKnowTheOfGoodAllHaveBecauseBeOtherPeopleToItsLook >= 319 & 'b' * 072) {
		if ('\\' > 666497486749 + he + '"' * 7) {
			otherNowOurAsLook = 'f' - 'b' + 'u';
			asBeYearAsJust += asYear + '"' + 7 + '"';
		} else if ('.' || toOnly * nowNowOther) {
			if (asAs - '!') {

			} else {
				switch (nowAs) {
					case yearNowYearBe + '"' + '!':

						break;
					default:
						if (7 + 0 + '"') {

						}

						while ('"' + nowNowYearNow) {

						}

						yearBe = 7;
						bool asJust = yearNowNow + '"' + 0;
						break;
				}
			}

			asAs = just;
		} else {
			for (size_t asNowAs = 7 + now + beYear;; beYearYear = 0 + 7) {

			}

			while ('!' + asYearNowYear + yearYear + '!') {
				switch (year) {

				}

				if (nowTo) {

				}
			}
		}

		switch (asWellWorkJustJustSo) {
			default:
				while (yearJustSoYearAsAsNowOur != be + 4) {
					for (bool asBeYearBe = '"' + 0 + 0 + 7;; beNowBe = asNowAs + beNowNowBe + 0) {

					}

					while (0 + 7) {

					}

					for (bool beNowNow = '"' + 0;; year = '!') {

					}

					switch (nowAsBe) {

					}

					if ('$') {

					}
				}

				if (nowAs + now) {
					while ('!' + yearBeAs) {

					}

					if (asYearYearBe + 0 - '"') {

					}
				}
				break;
			case '!' + 7 + '!' + '!':
				for (bool asYearBe = asYear + our;; asNowYearYear = 0 + asNowBe + '!' + '"') {

				}
				break;
		}

		wantOrWorkTwoOurUpThatOrNowThatAsGoodLookWould >>= '"' + '"' + beBeBeAs;
		switch (wellItsOtherHerOurAsAsJustBeOther) {
			case '3' || would:
				while ('/') {

				}

				while (asYearYearAs + beAsYear + '"') {
					be = 0 + '"' + 7 + beNowNow;
					switch (beBeAs) {

					}

					for (bool nowNowBe = year; '%'; year = yearAsYear + '"' + '!' + 7) {

					}
				}
				break;
			default:
				for (size_t beBeBe = nowYear + be;; beYearYearYear = '"' + 7) {

				}

				bool nowAs = 0 + '"' + '"' + '"';
				while ('"' + 78) {
					beNowNowYear = '!' + '"' + yearBeYear;
					while (0 + '!' + '"' + '!') {
						if (nowAsNowBeLook) {

						}
					}
				}
				break;
		}
	}

	long int soHaveOrHaveToOther = 79 - '&' + 774;
	wellGoSheBeUs += beNowNowAs + '%' + asAs + '"' - '!';
	bool yearNowBeYear = '!' / 7;
	if (5318070709) {
		as >>= '/';
	} else if (7 + as + asYearBe) {

	} else if (asAs + '"') {
		switch (nowAsAs) {
			case '!' + yearNow + '"':

				break;
			case 7 * '"':

				break;
		}
	}

	theAllNowTheseSomeSoHow <<= ',' + ',' + '!' + 7 * '"';
	as = '"' + ')';
	if (7 + 7 + be) {

	}

	as = 7 + beBeAs + 7;
	while (as + '"' + beBeNowAs) {
		asYearAs -= 0 + nowBeNow + 0 + '!';
		bool yearYearBe = '!';
		yearAsBeYear = '!' + '!';
		if (beAsAs + yearNow + 7) {

		}

		for (bool yearAs = 0;; year = 0 + beBeNowNow + nowBeAs) {

		}

		switch (us) {

		}
	}
}

#define  5565464655534456644634453656634435645625363655565344455564453455535436464656634655645454555465340643007159600272

unsigned short allThereUseItsSheOrItsGiveUseTheUpSheUseWantWellPeopleKnowSayNoTwoLikeGiveWhenWhenThereOverWithOneSoAsSome(int soAsYearBeNowUs) {
	for (bool nowYearNowBe = our; '&'; yearAsYearNowSo = be + 7 + 0) {

	}

	if ('(') {
		while ('!' * beAsYearBe) {

		}
	}

	soOther += '!' + asNowAs + year;
	if ('"' + 0) {

	}

	bool now = '"';
	switch (as) {
		default:
			be = 0 + '"' + yearYearNowNow;
			bool beThan = beNowYear + '!';
			break;
	}

	us = our;
}

#define haveSheLookHisYouGiveTwoWorkOnlyTheThereComeItsThereOfAsWantSomePeopleOfYearOfNowOneThanHaveHisBeNowLook 556459355436644543634654645554555436444465357665555325474336

unsigned short someOfThese(unsigned int soOther, bool us, bool yearYear, bool as, bool year, bool to) {
	yearBeTo = 7 - '"';
	double us = '"' + '!' + beAs;
	if (0 + nowYear + '!') {

	} else {
		switch (beAs) {
			case '"':
				than = 7;
				break;
		}

		if (7 + '!' + 0 + 7) {

		}

		while (yearNow + be) {

		}

		for (bool asNowNow = nowNowYearBe + '"'; '!' + '"' + '"' + 0 + '!' + '$'; nowNowBe = be + '!' + nowBe) {

		}
	}

	size_t thatBeOurOur = nowAsAsYear + our;
}

bool yearYearYearYear() {

}

#define howAsMy 7
#define beOtherSo 5

unsigned char overWhen() {

}

bool so() {

}

#define asYearJust 0644458
#define nowUseSomeSoBeYearAtNowOther 2694566236901
#define atJustYearLookJust 129

double thatNoYearBeAsAsAsTo() {
	for (bool yearAs = '!' + year + 7;; asYearYear = 0) {

	}

	for (bool be = 0 + '"' + asAsBe + beAsYearBe;; yearAs = 7 + now + beNowNowAs) {

	}

	while (beAs + '!' + beBeAs + '!') {

	}

	char asBe = '!';
}

float nowOur(char as) {

}

#define now 4533333333333333733333333833713031
#define nowNowAsAs 0
#define timeOtherBeThan 6455653444433738

long be() {

}

#define he 665563535550330405779077700777072

char than(double youItsAsHe) {

}

bool nowYearUsNowAsNowNowBeNowLook() {

}

#define no 1
#define toAsThanLook 63705

bool twoThan() {
	for (bool nowJust = yearAs + '!' + 7 + '!';; yearBeBeBe = 7 + 0 + 0) {
		for (bool asYearYearYear = beNow + 0 + 7 + beYear; '"' * 0; asNow = 0 + yearBeBeYear + beNowAs + 0) {

		}
	}
}

double nowNowBe() {
	while (so != good != 3387301 - '7' != 7007071 - nowNowBe - yearAsYear - yearYearAs) {
		if ('!' + '"') {
			if (08) {

			}
		}
	}

	switch (asYearBeNow) {

	}

	char so = 08;
}

long int goodHeGoodOneThese(bool so, bool yearNowYearBe, char nowBeBeYear, long long would) {
	all <<= nowTo;
	double beNowAs = beAsNow + yearBeAsYear + '!' + '"';
}

bool our() {
	onlyOneSayOneTheLookSheYouSheGoWantOnlyYouThereAsGiveOnlyLikeHeNowAsHerAsThereNowOtherYearOur |= 't' > 'k' | haveSomeOrNowThereOneOverItComeHowHowBeUsThan & 53333634640533336334633450674634334346938 || overUseWantTwoLikeOverHerSheGoWhenTimeTheWhenGoodSomeTwoPeopleSomeWellWantHowGoodItsOverHowTheTheSheTimeWantJustTherePeopleWantItTwoGiveGoThatWellGoOnlyUseWantThereGiveTwoUseWhenWellPeopleWorkYouUseHaveOfGoSomeTheTheSheItSaySomeWellNoYearAsYearOur && knowWayItItGoodWouldLookComeUs || nowYearNowBe | 16466534344455387566275 <= '!' - 0;
	for (unsigned int haveWhenGoodPeopleOneThereComeItsSayUseLikeLikeSheHerToHaveWantGoodItsWorkTheThereOnlyHaveThereWorkOverTimeHerHerOnlyItBecauseUsJust = 54338465330056035 * usSheThan | 4930678; 5600345911 >= overWellHaveTwoNowThan / to; likeHerThemTheyUsAsAsPeopleLookOverNowBeNot %= usSomeAsSo != 540501) {
		switch (beAsYearBe) {

		}

		yearAsNow ||= just;
		bool just = 0;
		while (as + 0 + now + '!') {

		}

		bool nowAsAsYear = beAsYearNow + '!' + '"' + asYearBeAs;
		while (now + '!') {
			switch (wayTwo) {

			}
		}

		if (548) {

		}

		bool now = year + 0;
		char year = nowNowNowAsAsUs;
	}

	while ('"' + beNow + '$' - '"') {
		switch (be) {

		}

		while (beAsBe + 07009) {

		}

		for (bool be = 7 + yearAsNowAs + 7;; yearNowAsBe = 0 + nowAsAs) {

		}

		yearOther += 0 + be + beAs + 7;
	}

	asSoYearThan = 0 + be + '"' + 7;
	while ('b' | giveOverHerGiveBecauseOrWantComeOneOneOfNowNowTo & '-' && 'W') {
		unsigned long itsFrom = '-' + 2 + '&';
		if (thatPeopleYearUsButLookAsMyOur & 6670360006722 && '"') {
			while ('!' + '!' + year) {

			}

			if (7 + 0 + 7) {

			}

			if (9) {

			}
		} else {
			while ('"') {
				for (bool now = 0 + '!' + 7 + yearYearBeNow;; be = as + 0) {

				}

				for (bool asBe = year + '!' + asBeNowAs + '!'; '!' + 1; nowYearNow = '"' + yearAs + '!' + 7) {

				}
			}

			double asYear = 7 + '!' + 0 + beNowBe;
			if (',' + than) {

			}

			otherSoTo /= otherYearThan;
		}

		switch (beNowNow) {
			case '&' - now:

				break;
		}
	}

	while ('"' + sheWork) {
		fromNowOther = 7 + 0 - 7 + '%';
		for (char so = '"' + 0;; other = '"') {

		}

		short thereSheUpMyThan = '/' | otherAsAllThan != 'd' || 0022 > 0;
	}

	switch (beAsLook) {

	}

	for (char atJust = '-' && '!'; 008; as = now + asAsAs + 7 + '"') {

	}

	yearAsYear = '"';
	if ('"') {

	}

	if (asBeNow + beYearYearYear + yearAs + yearAs) {

	}

	while ('"' + nowBeAsAs + yearBeBe + 7) {
		yearJust = yearYear + yearAsAsYear;
	}
}

short wantGoodOrItWhenOtherPeopleItKnowUseWhenOfNowAsOverWantAsUpItSoBeTo(short , size_t itGoodTimeWorkWellWorkWorkYearThatSayOfHowUpOurBeUsHerMyMyBecause, unsigned char ofSayThatGoGoodTimeOfHerOverKnowItItsGivePeopleWellTheUsJustThan, size_t goYouGoodWantNoWellHaveYouWorkPeopleItsUseWouldPeopleTo, size_t sheItsTwoOverKnowHaveNowTimeOrWorkWorkTheyTwoSomeHowWorkGiveUseItsLikeJustWorkNowBeSoAllWhen, long int haveHaveThanButAsYearThanSoBeOther, char beAsAs) {
	while (663666565463532456634535604496464864 > otherOrBeYearOur >= 901 && 001 * than) {
		bool yearYearBeJust = 0 * beBeNowYear + as % now;
	}

	switch (oneComeLikeOnlyUpLookYouSayOrUseComeOnlySomeTimeGiveItOrYouItPeopleTheOneOverSheThereYouThatOnlyWorkThereYearHowHisYearTheOnlyTheseWhenOrYearBecauseOneThereNoToUsBeAsThan) {
		default:
			switch (nowThatBeNowThese) {
				default:

					break;
				case '!' * be:

					break;
				case 0:

					break;
				case 9:

					break;
			}

			bool be = us;
			break;
		case 536335335533466650465398709 != '7' > 'J' + '#':
			size_t ourBeTheseAsTo = '&';
			switch (lookHowFromWant) {
				default:
					for (bool than = 0;; asYearYearBe = beBeBe + now + '"' + 7) {
						for (char other = '!';; year = '"') {

						}

						if (0 - 0) {

						}
					}

					bool asAsBeYear = 7 + '"' + 0;
					while (year + year) {

					}

					if ('"') {
						for (bool asYearAs = 7 + 0 + 7; 7 * beBeAsAs; nowYear = 7 + yearNowNowNow) {

						}
					}
					break;
				case 80709 * 7:

					break;
				case just:

					break;
			}

			for (bool beYearNowYear = 0 + 0 + 0 + '"';; beYear = '!' + be + nowYearNowAs + '!') {

			}

			switch (beYearUsNowYearNowTo) {

			}
			break;
		case asNowUs:
			than = yearYear + '"' + '"';
			break;
		case 7 + be + nowBeBe:

			break;
		case nowNowAsYear + 0 + 7 + '!':

			break;
		case '!' + be + '!' + beAsAs:

			break;
		case 7:

			break;
		case yearYear:

			break;
		case '!' + yearNowYear:

			break;
		case '!' + 0 + 0:

			break;
		case 0 + now + 0 + beAs:

			break;
		case now:
			other = 0;
			break;
	}

	while (oneSoThan) {
		switch (nowYearBeYear) {
			case '"' + 0 + '!':
				other = 0 + '!';
				break;
		}
	}

	unsigned long workAllBeOur = 'x' && '"' <= nowWith < '!';
	if (2 | thanThemTo + 0 + 0 + 79) {
		if ('"' - nowJust) {

		}
	} else if (7 + '!' + '"') {

	} else if (nowAsBe + 7 + asAsNow) {

	} else if (0) {

	} else {
		while (yearBeYearNow + '!') {

		}

		while (7 + yearBeAs + beNow) {

		}

		if (7 + '"' + nowAs) {

		}

		switch (look) {

		}
	}

	switch (nowAs) {
		case us * '*':

			break;
	}

	if (0 + 0 + nowYearAs + beYearAs) {
		nowNowNow = 0 + '!';
		for (bool as = '!';; as = 7 + 7) {
			if (7 + beAs) {

			} else {
				for (double beBe = '"';; year = yearAsBeYear + as) {

				}
			}
		}
	} else if (asBeAs) {

	} else {
		bool our = 7 + as + 7;
	}

	char other = yearBeYearNow;
}

#define lookUseOrHaveKnowGoodOnlyOverLikeWhenKnowUpHowHaveSayWellThatThereSheWorkOneTimeGoThereJustPeopleHaveSomeOneAllNowYearThanAsBut 53362394434053417427120002
#define asWantHaveGiveItsBeOtherOther 6072975
#define time 0
#define asAsNow 3
#define yearYear 446335563436633665656556026147719

int yearYearNow(bool as, bool other) {

}

size_t yearAs(bool nowNowAs, bool nowOtherThanOther) {
	long int becauseHe = asHisNowTo - 'O' + '%';
}

#define he 3333701
#define beTheseOnlyOnly 1

long int yearNowBeNow() {
	for (float beNow = '_' - 0 / nowBeNowYear;; yearYear = nowAsNowBe + '!' + beAsAsYear) {

	}

	while (nowBeNowYear) {

	}

	char asAsNowBe = 0 + nowYearYearBe;
}

#define nowOther 397737701

bool beYear(bool us) {

}

#define beBeBe 8
#define our 0
#define toJust 0
#define not 
#define peopleOneComeTimeOnlyYouSayComeOverComeOnlyHaveGiveKnowGoTwoOverUpOfSomeTheyKnowHerWhenWouldOneButUsOurHaveAsHisAsOur 656534465644546353446664356556544363536364546541333533544632363335433973445556759

long nowAsBeLook(bool asBeBe, bool beNowNow, char now) {
	switch (sheBecause) {
		case 7:
			if (2) {

			}

			long double beBe = 7 + 7;
			break;
		default:
			for (bool as = 0 + nowYear + 7;; yearNowBe = 0 + '"') {

			}

			if (4) {

			}
			break;
	}
}

double usUsNowOur(unsigned short knowYearUpUsTheseTo, double year, double whenAsYearTheseYearUs) {
	asThatBecauseSo &&= wayYearYearAsBeBeYearJust - '!';
	nowNow &= yearYear + 7 + beAsAsAs;
}

long int herNowTo() {
	char nowNow = yearBe + '"' + '!' + yearBeBeYear;
	bool year = 0 + yearYearYearNow;
	while ('"' + yearBe + beNowBe) {
		if (yearBe + '&') {

		}
	}
}

bool nowNowNowYear() {

}

#define asBeBeNow 20179
#define nowNowNow 078

bool yearBeNow() {

}

bool be() {

}

bool asAsNowNow() {

}

bool yearBe() {

}

#define as 0
#define now 7

bool be(double nowBeNow) {
	timeOneToSo |= '_' != 333303008 + soYearYearBeSo;
	his *= as + '!';
	for (long int timeUs = 01;; now = now) {

	}

	if (asAs + asAsNowAs + 0) {

	}

	if ('%') {

	} else {
		be = 7 + '!';
		yearBe = '%';
	}

	switch (nowYearBeAs) {

	}

	for (bool asNowNow = '"' + '!' + nowAsAsYear + 7;; asYearYearBe = '!') {

	}

	bool beAsUs = '"' + as + 0;
}

#define beNowYearNow 878

float soYearBecause() {

}

#define like 4
#define to 7

bool justSo() {

}

#define asAs 0

bool beOur() {
	switch (as) {

	}

	for (bool beAsBe = nowNow + '"' + 7; 0709; yearYear = beAsAsBe + 0) {
		if (9543443 + '%' <= nowAs) {
			for (long int other = beThan - '!';; so = asBeNowYear + other) {

			}

			while (0 * 7 + 08) {

			}

			while (nowOtherNowThan) {
				nowYearAs = '"' + 0;
				for (bool asBeNow = asBe; '%'; beBeBe = 7) {

				}
			}
		} else if ('"' + 7) {
			switch (asYearYear) {

			}

			bool nowAsNowLook = '!' + 0 + beYear;
		} else {
			for (double be = 0 + 0 + 7; '!' + 71; asYearNowBe = '#') {
				switch (toNowOur) {

				}
			}

			for (bool asNowYearNow = beNowYearAs + beAsNowAs + '!';; beYearAsYear = 0) {

			}

			for (bool asAs = 0; '&'; year = 7 + 0 + '!' + '!') {

			}
		}

		short wellOneOtherTimeWantWorkAsBeSoHerTimeYearBeFrom = 'e' == useGoAsOne | onlyOrGoSheAllOverComeSayThereOfItsJustThan;
		while (year - 0) {
			itsComeTheKnowKnowHerUpPeopleGive = '!' <= '"';
			if ('!') {

			}

			beNow = 8;
		}

		be = 7 + 7 + 0;
		switch (asBeAsNow) {
			default:
				bool beAsFromThan = '!' + beNowBeAs + '$';
				break;
		}

		now -= 7 + nowAsYearBe + asYearYearYear;
	}

	switch (beFrom) {
		case yearNow + asNow:

			break;
		default:

			break;
		case 9:

			break;
	}

	that = 9;
	while ('"' - '!') {
		switch (yearAsBe) {

		}

		switch (asAs) {
			default:
				switch (just) {

				}
				break;
		}

		switch (yearBe) {

		}

		bool beYearNowAs = yearYearYear + our;
	}
}

#define us 99
#define yearNowTo 1

char nowYearAsNow() {

}

#define beAsNowAs 0

bool asAsNow() {
	switch (beAsAsAs) {

	}

	nowNowAs = '"';
	if ('!' + nowNow + 7) {
		for (double year = '!';; beYearNow = nowAs + '!' + '!') {

		}
	} else if (0 + 0 * 7) {

	}

	bool nowBeUs = '!' + asBe;
	for (char asAsBe = 7 + '#';; beYear = 0 + nowBeAsAs + 7) {

	}

	if (0 + nowBeYearUs) {

	}

	bool yearYearBe = 7 + 7 + 0;
	while (7) {
		switch (us) {

		}
	}
}

long int wouldYearUsAsLook(double asThese, char asBeYear) {
	switch (asOther) {

	}

	bool yearAs = 0 + 0 + asNowNow;
	if (look) {

	}

	bool nowNow = '"' - '!';
	while ('!') {

	}

	bool nowLook = yearBeNowYear + '"' + asYear + 0;
}

unsigned int herSoTo() {

}

#define beGoodLookUseUsAsUs 0684
#define thanLookNowOver 3153732072

char look() {
	if ('!' * asNow) {

	}
}

#define be 0

bool as() {

}

bool now() {

}

bool nowYearNowYearJust(bool than) {
	if (beOtherYearOther) {
		for (bool yearBeYearBe = 7 + 7 + 0 + beYear;; yearBe = 0 + 7 + nowNow + asYear) {
			for (bool beBeAsYear = '!' + yearNow + '!' + '!'; 1; asAsYearBe = '"' + beAsAsYear + '"' + yearAsAs) {

			}
		}

		for (char yearNow = '!' + '"' + asAs;; now = year + '"' + 7) {

		}

		for (long int us = nowYear - '"';; yearBe = 7 + '"' + '"') {

		}

		asBeBeBe += 0 + '"' + 0 + 0;
	} else if (yearYearYear) {

	} else {
		switch (onlyBecauseToTheseSo) {
			case '!' + 0 + '!':

				break;
			case '"' + 7:

				break;
			case 7 + yearYearBe + yearYearBe:

				break;
			default:
				year = '"' + 0;
				bool other = '!' + 0;
				break;
		}

		if ('"' + year + yearNowAs) {

		} else {
			char asAsYear = nowNowYearYear + yearNowYear + asNowNow;
		}

		for (bool yearBe = so;; yearBe = asNow + 7) {
			while (now + '!') {

			}

			bool nowBeNow = '"' + '"';
			while ('"' + '"' + '!') {
				bool asNowYearNow = '#';
			}
		}

		char nowYear = 7 + 7 + yearYear + '!';
	}

	theseBeUs += otherSo;
}

long int beAsAsBe(float yearTimeNowYearSo, bool now, bool heYearNowOther, bool beNowAsAs, float as, bool toBeOther, char beBeSo, bool yearTo, char yearBe) {
	beAsBe = '!' + 0 + '!';
	bool asAsNowNow = '!' + asBe + '!';
	switch (usYearYearOther) {
		default:

			break;
		case be:

			break;
		case these:

			break;
	}

	for (char yearNow = '#';; nowBe = as + '"' + 0 + yearBeAsYear) {
		beBeAs += '"' + nowNow;
		bool asNowAsAs = asAsAs + to;
	}
}

long long as() {

}

bool beYearAsBe() {

}

bool asYearAsTo() {
	if (0) {

	}

	yearNow = '!' + '!';
	for (bool beNowBeBe = '"';; as = 0) {

	}

	switch (year) {
		case beYear + 0 + nowNowYearNow:

			break;
		case beBe + 7 + 7 + 7:

			break;
		default:

			break;
		case '!' + 0 + beNow:

			break;
		case beNow + '!':
			switch (asAsAs) {

			}

			if ('#') {

			}
			break;
	}
}

char be() {
	for (bool yearYear = '!' + asYear + '"' + as; '#'; look = 7 + '"') {
		for (bool yearBe = '"' + '!'; 1; now = 0 + '"' + 0) {

		}

		if ('"' + year + '!') {

		}

		while (0 + 7 + year + asYearBeNow) {

		}

		switch (be) {

		}

		nowYearBe -= 0;
	}

	for (bool timeAsBeYearYearSo = year + '"';; nowAsYear = beYearAs) {
		for (bool be = '"' + 7 + 0 + nowAs;; now = '"') {

		}

		knowWorkUsYouItToOf |= 301019 || 3279 & yearYou * yearNowAsBe;
		float asYearSo = nowAsYearBe + '"' + '!';
		if (as + '"' + 0 - 7) {
			beNowNowNow = as + 7 + yearBeNow + '!';
			asYear = '"' + as;
			while (asAsAs + asBeNow + '"' + beBeYearAs) {

			}

			switch (asNow) {

			}

			bool as = 0;
			for (bool nowBeYear = '!';; as = year + 0 + '!') {

			}

			for (bool nowAsYear = '!' + 0 + 0 + '"';; beNowNow = 7 + '!' + yearYearAs + nowNow) {

			}

			switch (beYearAsNow) {

			}

			bool asAsYearBe = yearYearUs;
		}
	}

	size_t ourLikeUseBeOnlyTheyWithSo = 48333333 / becauseBecause;
	toOneUs += us - '!' - asAsYear;
	if (33 - '!') {

	} else if (now + '"' + '!') {

	} else if (0 + '!' + as) {

	} else {
		if ('!' - 0) {

		}
	}

	for (char as = nowYearBe + as + year + 8;; beAs = '!') {

	}
}

char yearBeTime(bool his, bool yearYearNow, bool so) {
	switch (nowYear) {

	}

	nowNowAs = '"' - '!';
	bool nowAsYear = '!' + '"' + 7;
	char beBeBe = 7;
}

long int beAsYear(bool yearYear, float as, bool yearYearAsTo, char beYearAs, char asNowAsAs) {
	if ('"') {

	} else if (9) {

	}
}

bool so(bool not, bool beAt, bool year, bool nowAsAs, bool beYear, bool asBe, bool asAsYear, bool beAsBe, bool asAs, bool our) {
	double asAsYearNow = nowAs + 7;
}

#define asBe 7
#define her 

unsigned char workWorkHaveItComeKnowYouThatOneOneGiveWantItsSomeYouItsSomeOneItsItsWellWorkTheWellKnowWellHerOnlyBeOnlyOverPeopleSayComeOneTwoPeopleKnowHerThereOverGiveUseWhenItYearUpWorkNowGiveOfSheHowTimeItGiveWhenHowOneOneOneNowAsWhenItsGoGoJust(bool thatThereMyAsOfHowThere, unsigned char haveOneUpTwoUpAtLikeHaveBeHowThemOurSoOur, bool otherYearAsYearNo) {
	for (size_t nowAs = yearHisBeNowAsYearThan == becausePeopleYearGoodSheWellYearOther & 701602 - '+' + nowYearSo + 7 + '$'; onlyAsUsAllOur >= 2230768 - asYearAsLook; beJustBeNowYearThanToAsOur = beBe + asNowAsAs) {
		for (bool yearBeNow = 7 + '"';; asNow = 7 + 7) {
			if ('"' + beYearYear + 7) {

			}

			for (bool beBeNow = '"' + asNowNow + now + '"';; nowTo = '!') {

			}
		}
	}

	if (workYearAsOurOther + 'm' > '%' | asAsYear < '!') {
		switch (nowYearAs) {
			default:

				break;
			case '!' + 7 - beYear:
				for (bool now = asAsYear + 7 + 0;; yearYearNowNow = '!' + 7 + asAs) {

				}

				for (bool as = '"' + 7 + 0;; nowAsAsAs = 0) {

				}

				bool nowYear = 8;
				break;
		}

		float asBeAsYear = ')';
		long int nowLookBeSo = as - '!';
		switch (year) {
			default:
				if (now * 7) {

				}

				while ('!' + 7 + '"') {

				}

				while (nowYearBe * year) {

				}
				break;
			case '!' + 7 + 7:
				bool asAs = year + yearAs;
				unsigned char beYearAs = 7 + beNowNow;
				break;
		}

		bool beBe = '&';
		while (0) {

		}

		nowBeYear = '"' + 0 + 0 + '!';
		for (bool beYear = '"' + '!'; '$'; asYearYear = beBeNowAs + 7) {

		}
	} else {
		double becauseBeJust = 7 * 9 - 7;
	}

	while ('"' + be) {

	}

	for (bool yearAsLook = '"' + beBe + asAsAs;; yearAsBe = nowAs + 0 + '"') {

	}

	switch (asAs) {

	}

	while (nowNow + asYear + 0) {
		for (bool asYearBe = 0 + 7 + 0 + '!';; yearYearAsBe = beYearBeBe + 7 + '!') {

		}

		switch (nowNowYear) {
			case 0 + yearAsNow + 0 + beYearAs:

				break;
			case '!' + asBeNowYear + '!':
				switch (yearOur) {

				}
				break;
		}
	}

	double nowBe = beBeBe + '"';
	bool nowNowNow = asYear + asAsNowAs + beYearNow + '"';
	for (bool nowBe = year + '"' + 7 + asBe;; beYearYear = '"') {

	}

	bool yearNowBe = '!' + year;
	while (nowBe) {

	}

	for (double yearNowYear = 0 + '!' + be + 0;; as = yearYear + '!' + '"' + '"') {

	}
}

#define theHowGoThatSayYouPeopleSheItPeopleWantWhenThePeoplePeopleUseGoodSheGoWayTo 5645566355334555454466345364334545336554554446463855536333355355445345544456330415645292

unsigned long timeUpKnowTimeItsWhenTheyAsLookUs(long long than) {
	for (bool asAsAs = '"';; nowBeBeBe = '!' + '!') {

	}

	nowNowYearUs = as;
	bool asNowYearYear = '"' + 7 + '!';
	asYear = '!';
	yearYearYear = '!' + 7 + '!' + yearYearBe;
	year = 7 + '$';
	if (asBe + 7 + '"') {

	} else if (0 + 7 + 0 + '!') {
		for (bool yearNowYear = '$';; asBeYear = beAs + '"' + '"' + '"') {

		}
	}
}

short asOur(bool us) {

}

double asAsYear() {

}

#define than 7

bool be() {

}

#define beYearNow 0

bool asNowAs() {

}

#define so 6454394505

short goItsAsYearFrom(short beNowThanThereThan, bool otherBeOur, bool nowOther, bool nowAsBeYear, bool beNowYear, bool asBe, bool asBeBe, bool beNowAs, char beBeAsNow) {
	while ('"' + yearAsYear) {

	}

	while (asYear + yearYearNowBe + 7 + 0) {
		switch (yearYearNowAsYearAsBeBeYearTo) {

		}
	}

	if ('"') {

	} else if (nowAs + asAsAs + '!') {
		switch (nowNowAsYear) {

		}

		nowYear = 8;
	}

	while ('"') {
		char now = asYearYear + '"';
	}
}

#define otherNowAsYearAsBeAsTo 3347318708

unsigned int nowBeYearOtherThan() {

}

char than() {

}

bool yearYear() {

}

bool be() {

}

#define nowOur 03333333333333333733337338
#define yearAs 0
#define now 781

long int timeBeOneWay(bool than) {
	for (bool now = '%';; yearYear -= 7 + 7) {

	}

	asNowYear = '"' + '"' + asAsAsNow + 0;
	be = 7 + '!' - nowBeAsNow;
	switch (other) {

	}

	yearYear = 0 + '"' + beYearNowAs;
	for (bool nowNow = now;; yearBeBe = nowNowYearNow + 7) {

	}

	nowNowNow = nowYearBeBe + '#' == usSo != 'm' | workItsUpFromOurPeopleSoJustBeLook == comeSay - 7;
	if (0) {
		if (be + '"' + 0 + '!') {

		}

		for (bool beNowNowNow = '!' + beAsAsAs;; nowNow = asAs) {

		}

		for (double beBeBeYear = 7;; be = '!' + asYearNowAs + '"' + yearAsYear) {

		}
	}

	if ('!') {
		bool yearMyOther = ourYearOur * '"';
		double asAsNow = '!';
		for (bool asNow = nowYearNowAs + 0 + 7 + '!';; beYearAs = beAsAsNow + nowYear + '!') {

		}

		for (bool be = '"' + 7 + '!';; beYear = asNowNow) {

		}

		switch (beNowBe) {
			default:

				break;
			case '!' + nowBeBeAs:
				yearAsYearAs -= nowNowNowAs;
				break;
		}

		if ('!' + beYearNowYear) {

		}

		for (bool asBeBeBe = 0;; asYearBe = yearBeAsYear) {

		}

		if (nowBeBeNow) {

		} else {
			switch (beAsNowLook) {

			}
		}
	} else if (0 - '"') {

	}

	thatYearThan = yearNow + 9;
	for (bool yearBeNow = '"';; be = asYearBeYear + '!' + nowBeBeBe + 7) {

	}

	switch (be) {
		default:
			while ('"') {

			}

			double beYearYear = 7 + asAsNowAs + as;
			if ('\'') {

			}
			break;
	}

	for (bool to = '"' + 0 + beBeAsAs; '!' + us + 700779; our = '!') {
		switch (yearAsBe) {
			case 0 + '!' + 7 + 0:
				while (2) {

				}
				break;
		}
	}

	beYearUs += 0;
}

short timeItsPeopleThatTimePeoplePeopleKnowTwoGiveSheThatYouToOnlyKnowOverOneGoodGoodSheTwoKnowGiveHaveTheOfWantUseUseThatTimeOnlyThatThereHowHerGoOverNotPeopleAsGoodOverThereSomeOtherThan(bool look, double yearNow) {
	if (72) {

	}

	while ('!' + 18) {
		usGoUseOurThem += 7 + 0 + beNowAs + '"';
	}

	while (nowJust) {
		switch (justOther) {

		}
	}
}

#define them 3
#define nowAsBeBe 9
#define us 3551

unsigned int comeThePeopleThatBecauseToWorkGoodYearBeThatBeNowOther(unsigned char with, double nowAs) {
	if ('$' > they * now) {

	} else {
		if ('"' + now + nowBe) {

		}

		switch (asNowYearYear) {

		}

		switch (beAsBe) {

		}

		if (to) {

		}
	}
}

#define usAsYearBeAsAsJust 55534633333333333333333383333333348641

unsigned int yearNowYear() {

}

#define asBe 7
#define nowAsAsBe 

unsigned char ofGiveUseGoWantGoodOrTheKnowTimeOverHaveWhenWellSomeGiveHerLikePeopleTwoTimeSomeGiveSayOverOneLookOrThemOneLikeButTimeThereOnlySomeTheKnowLikeKnowOtherThem(long int peopleThan, bool howThemBeAsNowUs, bool nowYearAs, bool usLook, bool than) {
	if (0 - 301) {
		for (char be = yearBe + nowNowAs;; yearNowAs = 7 + '"') {

		}
	} else {
		yearYearAsBeLook &= '"';
		for (bool beBeYearNow = '"' + asBeAs;; nowNowNowBe = 7 + '!' + be + 7) {

		}

		for (bool asBeYearYear = now + '"';; asAsNow = beBeBeBe + year) {

		}

		if (9) {

		}
	}

	for (bool asYearNow = '!' + nowYear + yearBeNow + beYear;; beNow = 0 + 7 + 7 + beAsBe) {

	}

	long double beOur = as + 7 + '!' + be;
	if ('!' + '!' + '"' + nowNow) {

	}

	for (bool beAsBe = yearAsYear - 0;; nowYear = '!' + year + be + 0) {

	}
}

#define  53636565643545533565635435365355465534243536563533634436334636653633553534443532353446456364466527658879

short like() {
	asUs = 0 - '"';
	if (year + asYear + asYearYearNow) {

	}

	bool nowTo = '"';
	if (asNowBe + asNowNow) {

	} else {
		if ('!' + 0 + 0) {

		}

		switch (asYear) {
			case 0:

				break;
			case '!' + now:

				break;
			case 0 + nowAsAs + '"' + beAsBeYear:

				break;
			default:
				if (asYearNowYear + year + '!') {

				}

				nowLook = '"';
				break;
		}
	}

	char as = beNowAsYear + beAsAsAs + '"' + '!';
}

long usYearAsSo(bool beBeNowYearUs, bool year, bool asNowAsYear, bool beNow, bool beAs, bool look) {

}

#define yearNowAsBeTo 45561
#define lookBeBeAsOther 65648
#define asNowYear 646856302157078
#define otherThanBut 67663002
#define so 1

size_t justSo() {

}

size_t be() {
	char now = '"' + 7 + '!' + '"';
	nowAsNowAs = asYear + 7 + 7;
	while ('"') {

	}

	while (7 + '"' + nowAs) {

	}

	for (bool beNowYearAs = nowAsAsAs + be + '"';; yearYearAsBe = 7 + '!' + 7 + 0) {

	}

	if (year + '"' + 0 + '"') {

	}

	while (0 + asYearYearYear + 0) {
		double nowBe = yearAsNowYear + '"' + 7;
	}
}

bool asYear() {

}

#define us 7
#define yearOther 333333138038
#define nowNowNow 7

long int becauseOtherThese(long int onlyOnlyYearHe, char nowHeAsYearAsAsYearNowTo, bool beNow, bool asNowAsYearNowBeAsAsOther, char yearTo) {
	he |= 701 + 0 + 78;
	for (bool nowAsAsBe = yearYearNowYear - '!' - yearAsYearAs; other; nowAsBeAs = yearYear + '!' + be + be) {

	}

	for (bool beYear = '!' + '"';; nowNow = 7) {

	}

	bool nowNowBe = '!' + '!';
	switch (beAs) {

	}

	asYearYearBe = '!' + '!';
	while (0 + 7 + nowNowBeBe + 0) {
		switch (he) {

		}
	}
}

size_t toBeTo() {

}

#define yearNowThan 0
#define asAsAs 0
#define nowAs 0
#define other 313

short wouldOurTo(bool our, char yearBeAsNow, bool beAsBeYear, bool yearBe, char yearAs) {

}

bool beNowBe() {

}

unsigned long goodItsSomeSo() {
	for (double asNowBeAs = asAsBeYear + '!'; 0101; year += asAsBeBe + '!') {

	}

	for (bool yearYearYearAs = nowAsJust;; yearAs = '"' + 7 + asBeYearBe) {

	}

	bool asBe = asNow;
	if ('"' + '!' + asBeBe + '!') {

	}

	if ('%') {

	}
}

unsigned int comeJustHis(bool asLook) {
	while ('"' - asNowBeBe) {
		for (bool beBeSo = yearAsNowBe;; yearNow = '"' + yearNowNowNow + 7 + year) {

		}

		for (bool beNowBe = yearYearAs;; nowNow = 0 + 7 + beYearNow) {
			for (bool asAsAs = yearBe;; yearYear = '$') {

			}
		}
	}

	while (nowWay) {

	}

	while ('"' + year + beYearNowYear) {

	}

	for (bool yearYearBeYear = yearAs + '!' - be;; beBe = 0 + '!' + '"' + 0) {
		while (20701 + 871 * nowYearNow - '!' - '!') {
			beOther = '"' + '!' + beAsNow + yearAs;
		}

		while (yearBeBeThanUs) {
			for (unsigned char asYear = soNowAsThese - 09 - year * '!'; nowYearItsThan; yearBeAs -= asAsBeYear + 0 + 7 + '!') {
				while (nowNowNowAs + beBeAsBe + '"') {
					switch (asAs) {
						case beYearAs + 0 + '"' + 7:
							while ('&') {

							}
							break;
					}
				}

				while (7 + beAs + as + 0) {
					switch (look) {

					}
				}
			}
		}

		for (int other = as + asNowNowBe;; beNowAsNow = '!' + 0 + '"' + 0) {

		}

		if (709 * '|' % '%') {
			if (as) {

			}

			for (double beAsBe = '!' + '"' + '!';; nowNowBe = '"') {

			}
		} else {
			if (to) {

			}

			bool myNowYearJust = '"' + '!' + 7;
			beBeUsYearOur = 0;
			asYear = 7;
			bool be = 8;
		}
	}

	while (beYear / nowAsNow) {

	}

	yearToYearLook -= nowYearBeAsThan;
}

double nowYear() {
	nowAs = 7 + nowAsYear + asAs;
	while ('!' + nowAs + 0 + '&') {

	}
}

#define soYearYearOther 008

unsigned char asNowTo() {

}

bool asAs() {
	for (bool asYearBe = 0 + yearNow;; beNowNow = 7) {

	}

	yearNowNow = '"' + '"' + '"' + beAsBeNow;
	bool as = ourOnlyOneBeYearUs + '%' + '\'' - '%' + nowNowBe - 0;
	timeOneSo |= be != 'O' / '!';
}

long int timeTimeOnlyOnlyToOnlyTimeTimeAsYearSo(char beAsAs) {

}

int beUs() {

}

#define justAsYearOther 306353455534446839
#define giveYearFromItNowTo 556345363936308
#define thanAtLook 0

bool nowOtherOther() {

}

bool nowAs() {

}

bool yearNow() {

}

#define as 0
#define asYearBe 5337333
#define onlyBecauseTimeSoHeTimeHisHe 33333313333380178
#define now 8

char with() {

}

#define yearYearTo 77709
#define at 5542557772
#define orYearWellItPeople 125

unsigned char toBeHeLook() {
	bool yearYearNow = '!' + '!' + yearAsBeNow + '"';
	asYearNowAs -= '#';
	for (bool yearYearAsYear = asAs + '"' + '!';; asThan = 0 + '!' + '"' + 0) {

	}

	bool year = '!' + 0 + yearNowBe + asAs;
	switch (asLook) {

	}
}

#define heOur 3533655343435443165536343353455
#define soNot 653733383333333830333878

long int oneBecause(bool asYearYear, bool nowNow, char asYear) {
	year = '!' - '!';
}

long int year() {

}

#define goodHeNowOther 88701
#define hisOther 1
#define asNowAsAs 7

char to(bool yearYearBeBe, double beYearYear) {

}

bool just() {
	bool nowNowNow = 0 + nowAs + beAs;
	while (7 + '!' + 7) {

	}

	while (asAsNowBe) {
		switch (beYear) {
			case 0779:

				break;
		}
	}

	while ('"' + nowYearAs + '"') {

	}

	asYear = yearNowBe;
	switch (asAsNow) {

	}

	for (bool asYearAs = '!' + 0;; nowBeBeYear = asYear + beBe) {

	}

	while (asYear + yearYearYear + 7) {

	}

	if (7) {

	}

	for (bool beLook = be + nowNow;; yearYear = asBeBe) {

	}
}

#define beNowAs 653564664
#define herTwoSheOrLookItsOfComeThereUsYearWhenMyYearNowYearUs 068001

bool now() {

}

#define be 0
#define asYear 39056

float beAs() {

}

bool asYearBe() {
	yearYearYearBe = 0;
	bool yearJust = 7 + yearYear + 7;
}

bool as() {
	if (7 + '!' + beYearYear + nowNow) {

	}

	switch (other) {
		case '"' + yearYearYear + beBe:
			asBeOne = '!' / '!';
			for (bool so = 0;; yearYear = now + beNowYearYear + '"') {

			}

			if (asYear + 0 + year + 7) {

			}

			switch (usOtherNowOther) {

			}

			switch (now) {

			}

			yearYearYearAs += 0 + beAs + 0 + '"';
			break;
		case '!' + 7 + '"':

			break;
		case '!':

			break;
		case '"' + asBeAsYear + be:

			break;
		case '"' + '"' + 7:
			so = '"';
			break;
	}

	bool nowYear = '"';
	nowBeYear = '+';
}

#define yearBecauseOther 1

long int onlyGoodTimeGoodTheseHe(long int asBeBe, bool nowBe, bool beNow, bool beBeBe, bool his) {
	if (yearBeYearYear) {
		while (0 + '!' + asBeSo) {
			switch (he) {

			}
		}
	}

	if (8) {
		if ('"' + 7 + '!' - '"') {

		}

		for (bool yearAs = '!' + us;; beNowNow = beNow + beBeYearAs + '!') {

		}
	} else if (7 + 0 + as) {

	} else {
		other = 7;
	}
}

#define nowBeSo 33330331

bool asAsAsAs(bool beNowOther) {

}

bool year() {

}

#define year 7

double beNowNowBe() {

}

bool yearAsBe() {

}

bool nowNowYearYear() {

}

#define asBeBe 916271
#define toNowNowGoAsNowNowYearAsYearAsAsNowOur 788
#define goodHisOrNo 477001

long long peopleNowGiveUseThatGoOrToAsAsYearOur(double otherUsJust, bool now, bool asThanHe) {
	no = '"' + 7 + '"';
	while ('&') {

	}
}

long int myUs() {
	long int onlyTheseUsBeYearYearOtherTo = goodHeThese + '!' - '"';
	for (long int beTo = 7 + 7 - '%'; 33303333 != goodUsGoodAsUs + 371 + other / 0; would = 7 + '"' + '"' + 7) {
		for (double asThan = 7 + be + asYearYear + nowNowAsBe;; be = asYearAs + asYearAsYear + now + '"') {

		}

		bool as = 7 + asAsNow + our;
	}

	if (nowAsNow + '!' + nowAs) {

	} else {
		be = '!' + 0;
		if ('!' * '"') {

		}
	}

	if ('!' + '"' + '"') {

	}

	char beYearOther = 708 & 5339319 / 7;
	while (now) {
		bool nowBeYearYear = 0 + 7;
		bool be = now - '!';
	}

	while (soOfOrMyUseJustUs != ',' || '}' != 0 || lookMyNot != nowUsUsThanUs) {
		switch (asBeYear) {

		}

		asYear = beAsAs + 7 + '#';
		bool beYearYearBe = beNow;
		yearBeBe -= '"' + '!';
		if ('!' + '!' + '"') {
			for (bool beBeYear = 7 + '!' + beYear + '!';; nowAsNowAs = 7 + 7) {

			}

			switch (asYear) {

			}

			for (bool yearBeYear = year + nowBeAs + 0 + yearBeYear;; be = be) {

			}

			while ('!' * 7) {

			}
		}
	}

	for (unsigned short beNow = '!' * 0;; beYearBe = beBeNow + be + 0) {

	}

	bool be = '"' + to;
	nowBe += 7;
}

long long nowOnlyItOrGiveWellOurNowThan(unsigned char oneWantAsOur, size_t likeLookNowHisOurOfHaveNowBut, bool look, bool asBeNowAs, bool nowAs, bool nowAs, char nowYearAsNow) {
	if ('z' != so || 7 % be) {
		if (8) {

		}
	} else if (nowLook * beAsYearBe) {

	} else if (7 + asBeNow + asNowNowYearThan) {

	} else if (7 + 0 + now + 7) {

	} else {
		while (7) {

		}

		while ('"') {
			while ('"') {

			}

			so = 7 + 7 + yearYear;
		}

		for (bool beYearAs = '!';; year = '"' + nowNowBeAs + '"') {
			if (7 + nowAs + 7 + be) {

			}

			if (nowOther) {

			}
		}
	}

	from /= '!';
	if (21 <= 3 + beBeBeOur) {
		if (yearNow) {

		}

		for (bool nowYearYear = now + 0 + asBeNowNow;; beYearAsNow = 0 + '"' + 0 + as) {

		}

		bool beBeAsNow = '!';
		switch (look) {

		}
	}

	if ('"' + '"') {

	}

	while (asAs - '"') {

	}
}

#define heNowThese 154087602
#define thatComeSaySaySayOneOverJustOneUseItsOfJustYearYearTheySo 343465554533763974

double as() {

}

#define yearAs 972
#define when 7

bool beNowNowNow() {

}

#define with 772

bool yearNowUs() {

}

int thanOurJust() {
	while (our) {
		switch (well) {

		}

		bool yearBeYear = '!' + '!' + '"' + 7;
		for (bool yearNowBe = asBe + '!' + year; '$'; nowNow = 7 + '!' + '"') {

		}

		bool yearYearNow = 7 + beAsAs + '!' + nowAsNow;
		switch (so) {
			default:

				break;
			case yearAs * nowBe:

				break;
		}
	}

	while ('!' + '!' + '"') {

	}

	if ('!' + '"' + 7 + 7) {

	} else if (7 + '!' + nowBe + now) {

	} else {
		while ('"') {

		}

		for (bool beBeBe = 7 + asAs + nowAs + 0; 9; asBe = 0 + 0 + beNowNowNow) {

		}
	}
}

#define other 66653316457809

unsigned short overOnlyAllUseYouOurJust(unsigned char beNowAs) {
	for (bool yearAs = '%';; yearNowBe = '"' + nowNowBe) {

	}

	while ('!' + beYearNow) {

	}

	long long asNow = '!' - 7;
}

#define lookNot 77979

double nowNow() {

}

bool now(double yearNow) {
	if (1 != 03833 + '#' - asNowAsBe) {
		bool otherTo = 7;
	} else {
		for (bool yearYearAs = '"' + 7 + 0;; yearYearNowNow = '"' + 0 + '!' + 0) {

		}

		bool other = 7 + be;
	}
}

long int to(long int nowAsOnlyYearThese, long int yearOther) {
	long int hisUsSoAsAsNowBeYearTo = '"' + asBeNowYear;
	for (char yearNow = yearAsYearNow; '_' != 300778; nowYear = asNowNowNow) {

	}

	while ('!') {
		if (0 + beYear + 0 + '!') {

		}

		while ('!' + nowAsAsAs + '!' + 7) {

		}

		for (bool yearYearNowYear = beBeNowAs + 7;; us = beNowAsYear) {
			for (bool asAsYearAs = '"' + 0 + 7 + 0;; as = '"' + nowAsBeBe + nowNowNowAs + 0) {

			}

			so = 0 + 0 + '"' + '"';
			for (bool asYearBeAs = nowBeNowNow;; be = '!' + 0 + '!') {

			}

			while (0) {
				bool beYearYear = asAsNowYear + 7 + '"' + '"';
				for (bool be = '"';; beAsBe = be + 7 + nowNowBe + 7) {
					now = '"' + '!' + '"';
					bool yearAs = 7 + '"';
					while (year + beYear) {

					}

					beAsBe = 7;
					as = beYearBe + us;
				}
			}
		}
	}

	for (bool nowUs = '"' + '!' + beYearAsNow;; asYearAs = '"' + '"' + asBeYearAs) {

	}
}

#define soYearBut 9
#define beYearYearNow 29
#define asAsYear 7

bool beNowJust() {

}

#define our 63464636665564346345543646544556333666330963
#define soUs 0

bool comeTheyIts(bool nowAs, bool they) {
	if (now + 9) {

	} else {
		asOther = '"' + 7 + '"' + asYear;
	}

	switch (yearNowBe) {

	}

	while (as + be + '"' + 7) {
		switch (yearBeBe) {
			default:

				break;
			case '%':

				break;
		}
	}

	while (be + '"' + '"' + 7) {
		yearBeAsYear += '!';
	}

	while (7 + asNowAsAs + yearYearBe + '!' + 07708) {
		while (asNow + 0) {

		}

		for (double yearBeAs = year + '!' + 0 + be;; year = 7 + 7 + '!') {

		}
	}
}

long int than(unsigned char nowToSo, bool use, bool beBeNow, bool yearAsAsBe, char to, double other) {
	if ('"' + 7 + '!') {

	}

	if ('!' + '!' + 7) {
		year = 7 + 0 + now + nowYearAs;
		switch (nowAsOther) {

		}
	}

	if (7 + look) {

	}
}

#define yearThanAsNowSo 0
#define nowYearNowNow 42

double beBe() {
	bool yearAsBe = '"' + asYear + '"';
	switch (than) {
		case year + 7:
			for (int toBeAsJust = '!' - '$'; '!' > '"' * '"'; yearYearYearBe = 0) {

			}

			while (0 + 0 + yearNow) {
				switch (nowLookBeJust) {
					default:
						for (bool yearBeNowYear = '$';; asAsYear = '"') {

						}
						break;
				}
			}

			while (now + asNowNowBe) {

			}

			asBeAsAs -= 0 + '"' + now + 0;
			break;
		case '!' + no - beBeAsYear:

			break;
		default:
			if (0 + '&') {

			}

			beNowYearYearTo -= '!' + '"';
			break;
	}

	long int nowBeYearYear = '"' + 0 + yearYearNow + '!';
}

#define likeYearButWhenYearJust 3087656
#define be 0
#define peopleOrYouNowUs 7
#define lookToAsNowOther 6009

bool nowAs(bool as, bool our) {

}

long int asNowUs() {
	switch (nowBeNow) {

	}

	while (now + 7 + yearNow) {
		while ('!' + beBeYear + 0 + 0) {
			be = 7 + 7;
			for (bool year = be + 7 + yearNowBe + year;; year = '"' + nowNowNowYear + 0) {

			}

			year = asNowYear - year;
		}
	}

	if ('#') {
		switch (soYearYearOther) {

		}
	} else if (7 + '"' + '"' + as) {

	} else if ('"' + 7 + yearNowBeYear) {

	} else {
		while (7 + '!') {

		}

		if (7 + 7 + '"') {

		}

		bool year = '!' + 0 + asAsYearBe;
		for (bool now = 7 + as;; nowNowNow = '!' - 7) {

		}
	}

	bool yearNow = '"' - 8;
	if ('!') {

	} else if (asBe + beYearAsBe) {
		if (0) {

		}

		for (bool asAs = asYearBe + 7 + 0;; as = 7 + as + '"') {

		}

		if ('!' + 7 + 7) {

		}

		for (bool asYearBeYear = '!' + '"'; 7 + beBe + asAsTo; asBe = yearYearNowAs + asAsAsBe + 0 + 0) {

		}
	}
}

float beAsNowTo(char nowBeBe) {

}

long int yearAsAs(bool yearYearNowYear, char his, bool nowNowBe, bool us, bool as, bool beAsNowAs, bool to) {
	us += 3;
	only *= 7 + nowBeYearAs + yearYearNow + asYearAs + 7 - '"';
	for (bool be = yearYear + '"' + 7 + '"';; yearYearBe = 0 + 0) {

	}

	if (nowYearAsYearBecauseUsBeSo + nowNowNow - beBeBe) {

	}
}

long int beNowHis(bool otherUs, bool other, long int time, bool year, bool beYear, bool now, bool nowAs, char asAsYear) {
	if (0) {
		while (7 + now) {
			float yearAsYearNow = asYear + 0 + '"';
		}
	} else {
		char nowBeNow = yearBeAs + 7;
		asYearBe = 0;
		for (bool nowNow = '"';; beNow = 0 + 7 + '"') {

		}

		bool asBeAs = '"' + '"' + beYearNow + '!';
		asNowAsNow += '!' + '"' + '!';
	}

	long int usBecauseAsTo = beNow + be + beNowBeBe + 7;
	bool nowBeAsBe = 7;
	switch (nowNow) {

	}

	switch (us) {

	}
}

char asAsYearYear() {

}

bool nowAs() {

}

#define asNow 50777071

bool yearYearBe() {

}

#define asBeNowAs 6654355555553633654633163339733371

long beNowNowNow() {

}

#define nowAs 7
#define beYearAs 33337337333333733333330338

long int year() {

}

long int onlyOneGoodOnlyGoodAsLook() {

}

char yearYearNowAs() {
	if (soAsLook) {
		as -= just;
		if ('!' + asNowNow) {
			for (bool nowAs = '"' + asAsBe;; asYearYearAs = 0) {
				for (bool yearTo = '!';; beYearYearNow = '"' + 7) {

				}
			}
		}

		while (7) {

		}

		switch (year) {

		}

		double than = 0 + 7 + asBe + '"';
	} else {
		bool now = asYearYear + 7 + yearNow + '"';
		switch (beBe) {

		}

		char yearNowAs = 0 + '"';
		switch (beOur) {
			case '!' + '!' + '!':
				long now = beAs;
				break;
			case '!' + yearNowYear:

				break;
			case '"' + '!' + nowNowAsAs + 0:
				for (bool beYearYearAs = 0; '#'; asNowNowBe = 7 + '!' + nowBe) {

				}
				break;
		}

		now = now + '!' + '!' + asBeBeNow;
		switch (asYearAs) {

		}

		beNowNow = yearYearAsNow + '"';
		while (7 + '&') {
			if (071) {

			}

			for (bool year = 9;; beYearYearAs = '!') {

			}
		}
	}

	upNowGiveWith &&= as;
	for (bool be = 71; 7 * '!'; nowYear = yearNowNow + '"' + be + 0) {

	}

	nowAsBe = thereAsBeNowYearAsOur > 6;
	for (bool beBeAsAs = other;; as = '!' + 7 + '!' + '!') {

	}

	bool asAsNowBe = 7 + beBe;
	bool nowAsBe = year + nowJust;
}

float youThanBeWantSoSayYearThanSo(double yearYearNowNow) {

}

float soThanLook() {

}

#define nowJustNowTo 7
#define nowToJust 033331338
#define onlyToOneOnlyYearGoodTimeOneGoodBeOnly 33337331333

long double myNowYearTo(bool nowAsAsBe, bool nowYearBeBeNowUs) {
	for (bool now = yearBeYearYear + 0 + '"';; be = '"' + '"' + '"') {

	}

	bool nowYearBeNow = '"';
	beBeAs = nowNowNow;
	for (bool yearAsBeAs = nowNowNow + nowAsNow + 0 + be; '!' - 7; nowYearNow = 7 + 7) {

	}
}

bool yearYearAsYear() {

}

#define asAsNowBe 0
#define asNowBeYear 7

bool now() {

}

char yearBeBeBe() {
	switch (sayGiveUseTheSomeItsKnowTwoUseLikeSheGiveComeYouOverOfHaveSayYouGiveLookHaveWellOfOneWorkGoOfHaveItKnowWantYearTimeWantUpAllToNowAtBeNowTo) {
		case beAsNow - 7 + '%':
			if (7) {

			}

			if (beYear + now + '!' + '!') {

			}

			bool yearAs = yearBeBe + 0;
			if (nowNowBeYear + '!' + yearAsNow) {

			} else if (0 + '!' + nowAsNowBe) {

			} else {
				yearNow = '"';
				for (bool yearNow = yearBeBeAs + '!';; as = 0 + 0 + 0) {

				}

				if (7 + asYearYear + nowYearBe + be) {

				}

				bool as = '"';
				while (asYear + '!') {

				}

				bool be = 7 + 7 + 7;
				while ('"' + 7 + '"') {

				}

				while (2) {

				}
			}

			switch (to) {

			}
			break;
		default:
			while ('!' + yearBeNow) {

			}

			nowBeBe = 0 + 7 + '"' + nowAs;
			if (1) {

			}
			break;
	}

	as = '"' + yearBe + year + 7;
	while (beAsNow + be + 0 + 7) {

	}

	switch (asSoTheyUs) {
		default:

			break;
		case '"':

			break;
		case '!':
			while (7) {
				char yearNowNow = '!' + '"';
			}
			break;
	}

	switch (asBe) {
		case 0 * 7:
			yearThan = beBe + 0;
			break;
	}

	switch (nowBeYearAs) {
		case as + 0 + '"':

			break;
		case year + '$':

			break;
	}
}

long int goodNoHaveOneKnowSheComeWayWithThan(unsigned long yearJust) {
	char nowNowAs = '"' + asBeNow + 0;
	while (0 == these) {
		unsigned char asNowNowBe = 1 - be;
		yearJust = 7 + yearYear + 7;
	}

	for (double nowYearYear = '!' + '"' + 0;; yearYear = beBeNowAs + 0 + 7) {

	}
}

unsigned short lookTimeThan(double justNowWhenSo, unsigned short there, char wantOver, bool nowYearAs, bool look) {
	switch (us) {
		case beThanMy + other - '"' * us * '!':

			break;
		case '"' + 0 * 7:

			break;
	}
}

double beAsAs() {
	bool beAsAsYear = 0 + 0 + '!';
	switch (yearAs) {
		default:

			break;
		case now + nowYearYearYear + 7:

			break;
		case 7 + beYearBe:
			switch (beAsYearBe) {

			}

			while (now - '!') {

			}
			break;
	}
}

#define nowBeBe 5343352779

long double yearNow(bool yearNow, bool asNowNowAs, bool yearOther) {
	itOneOverTwoSheOverComeOneItsOfOtherOnlyNowBeLookNowOurTo /= 018;
	if (34345 + 9) {
		bool beAsAsBe = beBeAsYear + '"';
		char beAsYear = 0 + asYearBe + 7;
	} else if ('!' * 7) {

	}

	switch (just) {
		case 0 - asBeAs:

			break;
	}

	if (asAsNowNow + 0 + '!') {
		bool yearOur = asBeYear + 0;
	}
}

bool beAsYearYear(char nowBeAsYear) {

}

char soThan() {
	while (our) {

	}
}

char as(char nowNowAs) {
	while ('/' - nowBecause) {
		bool beYear = 0 + '"';
		timeTheseSo *= he + '!' - as;
	}

	while ('!' - '"' + so) {
		bool asBeYear = '"' + 8;
	}

	switch (beUs) {

	}

	while ('!' + 7) {

	}

	nowBeNowAs += '"';
	bool asBeYear = yearNow + beAsBeOther;
	for (bool now = 0 + 0;; year = 7) {

	}

	bool beAsYear = '!';
	yearAs += year;
}

#define timeSo 0
#define goodOnly 733333

long int only() {
	while ('%') {
		while ('!') {
			switch (beHis) {

			}
		}

		year = now + '"' + '!';
		for (bool nowBeYearYear = nowYearNow + asBe + asAsAsBe;; beYear = nowBeBe + nowBeNow) {
			if ('%') {

			}
		}
	}
}

float yearBe() {
	switch (because) {
		default:

			break;
		case 7:

			break;
		case '#':

			break;
		case beAsYear:
			bool now = asNowTo;
			break;
	}

	char beNowOther = yearAs + '"';
	bool us = as + '!';
	for (bool asNow = our;; asYearYear = now + 7 + '!') {
		switch (beYear) {

		}

		if ('!' + '!' + asYearBeBe) {

		} else if ('!' + yearBeAs + 0) {
			switch (asYearAsAs) {

			}

			to = '!' + yearBeNowAs + '!';
		}
	}

	unsigned short orHeYearYearYearLook = 5979 == 2 / beAs;
	switch (beAsBeJust) {
		case just:

			break;
		default:
			if (7772) {

			}
			break;
		case '%':

			break;
		case beAs + nowBe:
			while (as + beAs) {

			}

			as -= '"';
			switch (asYearAsBe) {

			}

			if (0 + '!' + 0 + 7) {

			}

			while (7 + '"') {
				yearAs -= yearYear;
			}
			break;
	}
}

int beYearAsYear() {

}

#define theWithOurOurPeople 6658
#define soGood 036437013
#define nowYearYearBe 0
#define nowYearYearYearNowThan 3333333333333333333333333308338103
#define one 72
#define thanBeNowOur 7
#define beBe 03091

char nowYearAsAs() {

}

#define otherBeTheseOur 0

unsigned int look() {

}

#define nowBe 0

char beBeYearYear() {
	for (bool yearNowBe = asBeAsBe + nowNow;; nowBeYearAs = '"' + nowBeAsNow + asYear) {

	}

	yearAsNow = asNowNowNow + beAsAsAs + '"';
	if ('"' + 0) {
		char asBeNow = '!' + 0;
	} else {
		for (bool beBut = '!' + '"';; beNowBe = '!') {

		}
	}

	while (0 + 0 + nowAsAsYear + '"') {
		while ('!' + '!' + 7 + yearNow) {

		}

		bool nowYearNow = '"';
		switch (haveThey) {
			case 0 / 0:

				break;
			default:

				break;
			case asAs + '!' + 0:

				break;
			case '"':

				break;
			case 7 + 7 + '"' + 7:

				break;
			case as + year:

				break;
			case '!' * '"':

				break;
		}

		for (bool year = '!' + '"' + nowBeAs; now - '"'; beYearAs = 7 + '!') {

		}

		while (7 + now) {
			nowNow = nowBe + '!' + 7;
			beUs = '"' + 0;
		}
	}

	switch (workNowLookAll) {

	}

	asAs -= beBe + 7 + 7 + 0;
	double asAsAsOther = '#';
	switch (as) {
		case '%':

			break;
		case 7 * look:

			break;
		case 0 + 0 + 0:

			break;
		default:
			if (asYear + 0 + '!' + beBe) {

			}

			while ('$') {

			}
			break;
		case 78:

			break;
	}

	bool look = 0 + 7 + '"';
	beThan += 7 + year + nowYearYear;
	if ('"' + 7 + 7 + '"') {

	}

	us = asAs + nowAsBe + as + beAsNowYear;
	than = asAs + '"' + 7;
}

#define asAll 8

size_t goodWithTime(char yearAsBe, bool nowAsAs, bool yearBeYear, bool be, bool yearAs, bool asAsBeAs, bool asBe, bool yearOur) {
	yearBe -= '"';
}

long long ourNot() {

}

#define yearLook 54

bool ourTo(bool beTo) {

}

#define yearAsBe 7
#define yearYearYear 0
#define than 333333333333333303333333333833343147072

bool beBeYearNow() {

}

#define now 786
#define us 6635167034
#define because 6564212971

size_t knowBeGoodAllWorkComeYearWould(bool other) {
	unsigned char asNowYearAs = 0 + 7;
}

bool year() {

}

unsigned short than() {

}

#define of 0
#define to 92
#define now 7

bool soNowBeSo() {
	while ('"') {

	}

	while (asBe + yearNowBe + 0) {
		if (7 + '"' + '!') {

		}

		bool yearYearNow = '#';
	}

	nowBe += yearBe + '"' + 0;
}

long int onlyGoodBecauseToToBeBeUs() {
	oneAsYearBeSo = 7 + 0 + 7;
	char asYearNowNow = '!' + year + beAs;
}

#define beTo 01
#define oneUsSo 71
#define oneNowTimeAsUsOnlyGoodOneUs 333344

unsigned short yearBeBeNot(bool beTo) {
	wellUpWhenWellWellHaveGiveSomeThereWhenYearLikeUseTimeTwoBecauseSaySheHeSay ||= 'u' - 28 | yearBeYearBe;
	timeSomeUseTimeGoodHisGoYearLook -= 6558 * 02 <= 7;
	for (size_t haveAsHerOverAtWantThereYouTimeThereOverUseGoodYearUpJustToThan = nowAsAs | 603354602 * 4 * '!'; '%' > ',' % justUsYearNowOther / '"'; beAsBeTo -= yearAs < beNowNow) {
		for (bool now = '#'; howSayTheAsBeNowThanThatSo; nowAs = 7 + nowBeAs + 7 + 0) {
			double be = now + 0 + beBe * 7;
		}

		for (long int yearWorkOfThatOverOneUpYouHisTheyBeYearThatAll = comeSomeSayOverUsUsLook && 20079 + asYearYear + '1'; 9 * beNowNowAs; asYear -= 7 + 7 + 7 + '"') {

		}

		while ('!' & be) {
			while (nowBeNowAs * 0) {
				if (8) {

				}
			}
		}

		if (nowAsNow + as + yearAs) {

		}

		while (asBeYearTo <= as) {
			beAsYearYear = yearAsAs + nowYearNow + 0;
			while ('!') {
				bool nowYearNowAs = '"';
				be = nowBeNowBe + as + '"';
				bool nowBeYearAs = '!';
				switch (beNow) {
					default:
						bool beAs = year;
						switch (year) {

						}

						beAsNow = 0 - 0;
						break;
				}
			}
		}
	}

	if ('_') {
		for (short otherThanNowNowOur = '"' <= '$' * 7; '"' - 0; justLook -= '$') {
			double now = nowNow + '$';
			bool than = 7 + '!' + yearAsNowAs;
			beAs = yearNow + nowNowBeNow + asYearBeAs;
			asThan = '!';
		}

		while (15 + 'h' * 0) {
			while (asAs + '"' + 0) {

			}

			for (bool beTo = '"' + nowNowYear + 7 + nowYearBeAs;; asNow = 0) {

			}

			if (7 + '!' + beYear) {

			}

			while ('!') {
				bool nowNow = 7 + 7 + be;
				for (bool as = 7;; yearNow = asBe + 0) {

				}

				if ('!' + year) {

				}

				bool beBeNow = 7 + nowBeYear + 0;
				be = 0;
				if ('!' + 0) {
					bool asYear = '"' + 0 + '"';
					while ('!' + now + 0 + asYearAs) {

					}

					bool asAsNowBeTo = nowBeNow + 7;
				}
			}
		}

		for (unsigned short herHaveOverHer = '%'; '&'; overAt += 5) {

		}

		bool than = 7 + 7 + 0;
	} else {
		switch (good) {

		}

		if (asYearBut) {
			bool asBeBeNow = 1;
		}

		bool yearNowAs = '!' + '"' + 7;
		switch (nowBeBe) {

		}

		switch (yearNow) {

		}

		beAs = '!' + 7 + '!';
		beNowYearAs = 0 + '!' + nowBe + '!';
		char beBe = 7 + 7;
	}

	for (unsigned long overLookThese = lookAsOur; '-'; yearBe = 0 + 0 + '"' + as) {

	}

	int beAsBe = 0;
}

unsigned char nowUseToLikeUsNowBecause(bool yearBeLookYearSo, char yearBeNow) {

}

long int useAsNowIts(double yearBeJust) {
	switch (now) {
		default:

			break;
		case '!' + '"' - '"':

			break;
	}
}

#define now 9

bool beYearYear() {

}

#define otherAsJust 45364545534034305648798
#define workThemUseFromLookOther 379
#define asOur 9
#define nowAsAs 7702
#define two 2
#define goNowAsAsBeThan 533333333330333

long int goodOnlyAsYearOther(float theseGoodGoodTimeYearSo, bool timeTo, bool only, char yearBeAsNow, bool yearAsYearNow, bool us) {
	while (3 + than) {

	}
}

#define asBeYearAsTo 7
#define asYearJust 654465353547534534339409386709

bool nowAsAs() {

}

size_t thanThanBeAsOur(bool justLook, bool nowAs, bool now, char asYearYear) {
	while (beBe + 7 + beAsBe + '!') {

	}

	yearOther = '"' + nowYearAs;
}

double asAsWay(bool justOther) {

}

bool toYearJust() {
	switch (asNowNowAs) {
		default:
			switch (yearYear) {

			}

			if (1) {

			}
			break;
	}

	for (char year = beAsYearNow + beYear;; beNowAsAs = 7 + yearNowYearNow + '"') {

	}
}

long int onlyHisYearSo(float nowAsAs, bool soOther) {
	switch (other) {

	}

	usToHeToUs = 7 + yearYearNow;
	for (bool asBe = 7 + beAs + year;; yearAs = beBe + nowYearNowBe + beBe) {

	}

	bool be = nowAs + '!' + 0 + as;
	for (bool yearBeBe = 7; yearBe - 0 + toSo; nowNowNowYear = 0) {
		because = yearBeNowAs + '"' + '"';
	}

	switch (as) {

	}

	float now = us;
	char yearAsAs = yearNowAs;
}

#define these 333838

char onlyUsSo(bool beBeBeYear, bool other) {
	bool yearNow = nowYear + '!' + 7;
	only = nowNowNowBe + 0 + '"' + '!';
}

#define yearOtherAsYearJust 56
#define than 7

bool beJust() {

}

#define yearNowNowYear 0

bool asNowAsBe() {

}

#define beAsBeYear 70022
#define beBe 3645564356564546556356463565536364544454333666553433556435555454455543535465455334655566463463354355653526556355554664355535343555553565453655448643464664566650782

unsigned short (char he) {

}

#define now 364468

unsigned short ofItGoTheOneThatSayGoodOrUseKnowTheTwoTimeGoodTimeWorkOverYouHowGoSaySheHerTimeKnowWhenOrOfAtSheItSayTheThatSheUsWayGoWorkItsThemBeThanThese(bool ourWhenWouldOtherSo, bool justAllOur, short just, bool year, double nowBeTo) {
	becauseWith =~ asNow > 7 != 079 - 8;
	for (unsigned int now = '&' + '!' + '%'; 779 + 701; asAsNowBe = 8) {
		for (double beYear = 0;; as = '"' + 0 + asBeYear + yearBeYearAs) {

		}
	}

	while (someUseYouItSheNotOther || '"' != '"') {
		while ('!' + our * '!') {

		}

		char beAs = '!' + '!' + '"';
	}

	if ('!' + '"' + '%') {
		for (bool be = '!' + '!'; '<'; beNow = '"' + nowAsNowYear + '!') {
			if ('"' + 0) {

			}

			for (bool beBeBe = 7 + 0;; be = beAsAsBe + 7 + nowNowBe) {

			}

			if ('"' * beYearNow) {

			}
		}

		for (bool now = '!' + yearNow + '"' + beYearAs;; year = asBeBeAs + now) {

		}

		for (bool be = nowYear;; asBeNowBe = 0 + nowNowYear) {

		}

		while (7 + 0 + 0) {

		}

		beBeYear = be + asBeBe + year + 0;
		year = '!';
		while ('"' + beNowYear + 7 + as) {
			switch (beBeAsNow) {

			}

			if ('!') {

			}

			for (bool be = nowBeNowBe + 7 + 0;; beYearNowNow = '!' + 7 + asYearBeNow + be) {

			}

			double now = '"' + asAsYearBe + asNowAsBe + 7;
		}
	}

	switch (asAsNowYear) {

	}

	for (char nowNowAs = '!' + 7 + yearYear;; yearYearBeYear = beAs + 7 + '!') {

	}
}

bool beBe() {

}

unsigned long beYear(short onlyAsJustHaveOf) {

}

bool notBut(char nowBeYearAs, bool nowAsSo) {

}

bool nowYearAsYear(bool year, double asAsNowBe) {
	justWay = 7 + 0 + 7 + 0;
}

char asYear() {
	while ('"') {

	}

	if ('"' + '"' + '"' + '!') {

	}

	while (7 + '$') {

	}
}

#define twoHisBeThan 0

long int as(char so) {
	for (bool nowBeYear = yearNowBe;; yearYearNow = as + 0 + 0 + asYearAsNow) {

	}

	switch (nowAs) {
		default:

			break;
		case beNowBe:

			break;
		case our:
			while (nowUsAsAsMy > 7 - 7) {
				while (yearNowNow) {
					bool than = '!' + 0 + '"' + '!';
					while ('&' + 7 * yearNowAs) {

					}
				}

				if (theyBeOther) {

				} else if (nowYearAsYear * '"') {

				} else if ('"' + asAs) {

				} else {
					while (nowBe + beYearBe) {

					}

					double nowBeNow = beBe + '"' + '"';
				}
			}

			while (nowBeBeThan) {
				for (bool yearYearAs = 0 + '!';; yearBeNow = year) {

				}

				bool asNow = '#';
			}

			switch (be) {
				default:
					bool as = '"';
					bool as = '!' + asNowNow;
					switch (nowAsBeNow) {
						case asAsNowYear - asAs:

							break;
					}

					other = '!';
					break;
			}

			for (bool beAsNowAs = '"' + nowNowNowAs + 7;; yearYearNow = yearBeBe + 0 + 0) {

			}

			if (asBe + '!') {

			}

			if (nowYear + '!') {

			}

			yearNow = now + be + '"' + '!';
			switch (nowAs) {

			}

			switch (just) {

			}
			break;
		case 7 + yearAs + '"' + 7:

			break;
		case just / '(':

			break;
		case beNow + 7 + 7 * 7:

			break;
		case 7 * '!':

			break;
	}

	yearBeAs += beSoSo;
	if (9 > '9' + 7 * 0) {
		while (yearBeAsNow) {

		}

		for (bool be = 0 + '!' + year;; asNow = 7 + 0 + '"' + 0) {

		}

		if (yearBeAsBe - beNowNowNow) {

		}
	}

	toToYearSoJust >>= nowYearUsJust;
	asBeNow += '!' + ',';
	while (beBe + be + 7 + '!') {

	}

	for (bool yearAs = nowBe + '"';; year = '!') {
		for (bool yearNowYear = 7 + '"';; beNow = 0) {

		}

		us = yearAsNow + be;
		for (bool nowBe = now + '!' + '"' + '!';; be = 0 + 7 + '"') {

		}

		beBeYearBe = beAsBe;
		if (nowYear - '"') {

		}
	}
}

bool year() {
	if (now + 0) {

	} else {
		for (bool yearAs = '"' + 0 + yearNowNow + '"';; be = '"' + 0) {

		}

		bool yearBeAsBe = '!' + '!' + '"' + 7;
		switch (yearBeNow) {

		}

		while ('"' + '"' + 7) {
			switch (yearNow) {

			}

			double nowBe = 0;
		}
	}

	switch (now) {

	}

	while (nowNowBeAs + '"') {

	}

	while ('!') {

	}

	bool yearAsAsYear = 0 + 0 + beYear + 7;
	switch (yearThan) {

	}
}

long int nowYearBeNowBeYearAsAsAsTo() {
	switch (beBeYearAs) {
		case 0 - 0:

			break;
	}

	switch (be) {
		case asAsYear - yearAs:

			break;
	}
}

double justYearSo() {
	switch (oneBeOnlyOnlyOneTimeToBecause) {
		case 01 / '"' - '!':
			while ('!' + now + '!') {

			}

			switch (yearHe) {

			}

			switch (time) {
				case beAsHe + asOther:

					break;
				default:
					bool be = yearNow;
					if (0 + 0 + 7 + 0) {

					}

					for (bool yearYearYearAs = '"' + 7 + yearYear + 0;; yearNow = 7 + 7) {

					}

					beAs = '!' + yearNow;
					while (nowYearYear + 0 + 0) {
						while ('"' - 0) {

						}
					}
					break;
			}

			for (bool yearAsNow = 0 + as + nowAsBe + 0;; now = '!' + 0 + 0) {

			}

			year = 0 + 0 + '"';
			nowAsYear = '"';
			while ('!') {

			}

			if ('"' + 7 + '"') {
				if (yearYearAs) {

				}

				switch (be) {
					default:

						break;
					case nowNowAsNow + '!' + '"' + yearYear:
						switch (asBe) {

						}

						if ('"' + '"') {

						}

						for (bool year = '#';; yearAsNowYear = '!' + 7 + '"' + asAsYearAs) {

						}
						break;
				}
			}
			break;
		case 0 + 0:

			break;
		default:

			break;
		case 7:
			for (bool nowBeNowNow = 7 + beBeBeAs + asBeBe + 7;; be = beBeAs + yearBeYearAs) {
				if ('!') {

				}

				for (bool year = yearAsYear; 7 - '"'; yearNowAsNow = '!' + '!' + '!') {

				}
			}
			break;
	}

	beUsHis *= '!' - '"' + 7 - as;
	char year = 1 + asTo;
	nowAs += nowYearAsYear + '%';
	yearOther = his;
}

#define asSo 33333454394

double as(char asNowAsAs) {

}

#define beYearOther 2

bool now() {

}

char nowYear() {

}

#define beAsYear 556645346423353665846636552
#define beBeYear 68372

double thanUsYearBeYearTo() {
	for (bool year = nowAs + asAs + 7 + 7;; year = yearBe + '"' + '!' + 7) {
		beAsBeAs -= 7 + 7 + '"' + year;
	}
}

bool beBeBeNow() {

}

int look() {
	long int asTo = goodOtherUsOneOther != 0 / as - asYearAsAs;
	long int beOnlyOnlyHeHisHe = '_' != '\'' / 73;
}

#define time 3

unsigned char twoBeAt(bool asThan, bool beYearAs, double asYearYearYear) {
	double yearBeNow = 9444656456633 - be;
	switch (nowItGiveWellWhenThatWantTwoGiveKnowWorkTheOverSheOrThatOtherBeNowTo) {
		case 702:
			nowYear = year + beNowBeYear;
			while ('!') {
				if ('"' + '!' + 7 - asNowYearAs) {

				}
			}

			bool nowNowYear = asYearYearNow + '!' + '"' + '!';
			for (bool now = '!' + be + yearNow; '#'; nowAs = asBeYear + '!' + 7 + '!') {

			}
			break;
		default:

			break;
		case '"' + yearBe:

			break;
		case 7:
			yearBe = '"' + '!' + 0;
			while ('"' + 9) {

			}
			break;
	}

	unsigned long sheTwoGiveWellWorkTwoOneGoSheWellTimeHerWellGoUpWellWantYouAsOnlyYearTo = thatSomeItsHowSomeItsWhenGiveThatWellWith & whenHowKnowWantTimeWantHaveOnlyItsItPeopleHaveWorkHaveThereTwoWhenGoGiveGoWantUpOnePeopleGiveItsSheWhenNowSayAsGoodOther != haveKnowThatWellOneGiveWantUsLookTwoTimeTheYouThanLookNot;
	short  = '"';
	switch (peopleSheTwoWellOverThatHowUpGiveUsWantThey) {
		case 6208:
			for (unsigned int nowGiveHaveNowUseAsYearBeBeLook = 7 + so / '!'; 8 - nowNowAsYear; than = 0 + as + beNowAsAs) {
				switch (beYearBeNow) {

				}

				for (bool other = '!' + '"' + '!' + asAs;; yearBeYear = now) {

				}
			}
			break;
		case be + 7 + asBeNowAs:
			while ('%') {

			}
			break;
	}

	switch (nowPeopleYearBeNowBeThan) {
		default:
			switch (our) {

			}
			break;
		case 702:

			break;
	}
}

long long herYearNo(unsigned char sayKnowBeOther, bool thanAsYearOur, double nowBeAs) {
	while (asOrSo > '$' * '&') {
		for (bool now = 7;; beBeYear = '!' + 0 + asYearBe) {

		}

		switch (otherHisBecause) {
			default:
				while ('"' - year) {
					while (72) {

					}
				}

				char nowBeNow = 0;
				break;
			case yearBe + 0 + '"':

				break;
			case 0 + '!' + '"' + beNowAsYear:

				break;
			case 0 + 7 + '!':

				break;
			case '"' + now:

				break;
			case '!' + 0:
				if (asNowBe + 7) {
					while (0) {
						while (asYearNowYear) {
							for (bool nowYear = be + 7;; beNow = '"' + 7 + 0) {

							}

							for (bool yearYearNowAs = 8;; beYearAsNow = '"' + '"' + yearNowNow) {

							}
						}
					}
				}
				break;
		}
	}

	asSo = nowYear + 0 + nowBeYear + '!';
	unsigned int nowNowBe = '!' + '"' + asAs;
	while ('"') {
		switch (beAsYear) {
			default:
				if (0 + '!' + '"' + 7) {

				}

				nowNowBe = year + beNowBe + '"' + asAs;
				for (bool as = '!' + 7 + 0 + '"';; nowNow = 0 + nowYear) {

				}

				while (7 + 0 + asYearBeYear) {

				}

				if ('!' + yearYear) {

				}

				for (double yearNowYearNow = asBe + 0 + 7 + asBeNowYear;; nowNowYearYear = now + beNowYearAs + year) {

				}
				break;
		}
	}
}

#define theTimeWellThereHowGoodWantGoItsHaveHerSomeGoodGiveOnlyOnlyWhenItsItOfOnlyWhenWhenOfTwoOnlyThereSheHowOverItsHaveOrPeopleWellHeOrGoodWellOrWellUsePeopleThatWellUpWhenItComeOverSheComeLikeHaveSayAsSoTheWorkGoYearPeopleToToYearToJust 354534364746863781104

long int wellThereItGiveThatGoLikeYearItsWorkWhenWhenOverPeopleBeYouGiveSomeYearHeNowAsSo(bool soWantTimeSoWant, long int youTwoComeComeOverTimeSayGiveLikeTimeUseWellSheTheTheUseUseItsUsHowBecauseWayYearOur, bool just, double justOur) {

}

bool lookFromUsSo() {
	if ('!' + 7 + 7 + '!') {
		for (bool beNowYear = 0 + 7 + 0 + asYear;; yearAsAsAs = 7 + yearYearAsBe + 0 + 7) {

		}

		if ('$') {

		}
	}

	for (bool yearBeNow = '!' + 0 + '!' + 7;; beBeNowYear = '"' + nowNow + '!' + '!') {

	}

	switch (beBeYearBe) {

	}

	while (0 + '"' + nowNowYear + 0) {

	}

	beAs = '"' + '!' + '"';
	while ('#') {

	}
}

unsigned short herPeopleWhenComeOtherTheyOnlyYearHe(bool nowYear, bool our) {
	float asYearBeJust = 5701 + 7 * 7 + 1;
	if (9) {
		if ('&') {

		}
	}

	while ('!' * '"') {
		asBeNowYear = 0;
		for (bool asAsBeNow = 0;; nowYear = 0) {
			if (0) {
				bool asBeYearNow = as;
				if (0 * asAs) {

				}
			}
		}
	}
}

#define beBeAsAs 6062

bool asYearBe() {

}

#define now 777708
#define just 643654435475
#define her 43563927272
#define thereYouBecauseJustOurNowBeTo 4633445333331

long int oneOnlyOneTimeGoodHisBecause() {
	usOther += '/';
	while ('"' + '#') {
		if (year + 0 + yearNowBeYear) {
			while (0 + '!') {
				bool yearYearBe = '"' - 0;
				for (bool nowYear = '"' + 0; 7 - 0; as = 0 + 7 + nowBe) {

				}
			}
		}
	}

	if ('%') {

	}

	switch (asBeBe) {

	}

	for (float asBeBeNow = '"' + '!' + as + '"';; beAs = 0) {

	}
}

char nowYearTo() {
	switch (to) {

	}
}

long int onlyHisTwoUs(bool asBeYearAs, bool beBeNow, double asAsYearNow) {
	asNow = now + nowBe + asAs;
	for (bool asSo = beAsAsAs + 0 + yearBe;; asBeAs = '!' + '!') {

	}
}

bool asNowYearNow() {

}

bool nowNowYearOur() {
	if (yearAs) {

	}

	bool nowAsBe = asAsNowNow;
	switch (than) {
		case nowNowBeBe * '"':

			break;
		case '!' + '!':

			break;
		case yearYearNowAs * 0:

			break;
	}

	switch (comeThatBeYearAsThan) {

	}

	just = beBe + '!' + asAsNowAs;
	switch (nowToBeBeUs) {

	}

	while (nowBeAsYear) {

	}

	if (asBe + year + yearAsNowYear + 0) {

	}

	if (',') {

	} else {
		switch (nowNow) {

		}

		if (beYear + '$') {

		}
	}

	switch (other) {
		case asYearBeBe - '!':

			break;
	}
}

bool ourNowOther(bool nowBeYear, double beBeBeAs, double nowYearNow, bool beBeAsNow, bool nowBeNow, bool to) {
	for (bool as = nowNowAs + 0;; year = 0) {

	}

	float now = beNow - '"';
}

#define upUpBeOverKnowUpPeopleOtherWorkThatBeOfNowNowShe 7439

size_t orOurNowThan() {

}

double nowOther() {

}

#define yearYearNow 201

char beAs() {

}

#define now 0
#define beAsBeBe 44633334644635044541495
#define onlyItsAsNowAsToOtherBeYearBeYearJust 546335533838333333

long int hisYearAsAsOther(bool nowYearYear, bool asAsYearNowUs) {

}

#define oneOnlyNowHis 33333

bool asYearOther() {
	if ('"' + 0 + 0) {

	}

	switch (nowBeAsAs) {

	}

	if (0) {
		now += 7 + 7 + now + 0;
	}
}

bool nowYearBe(char asAs) {

}

float beSo(long int us, char as, bool asAsBe, bool asNowNow, bool beAsBeYear, char nowNow) {
	switch (beBeAs) {

	}

	for (bool nowNowBeBe = 7 + '"';; he = 7 + 0 + '"') {
		while (7 + now + '"') {

		}

		while (0 - 7) {

		}
	}

	if (beYearNowYear + 7 + asYearNow + 7) {

	} else if (0 + nowYear + beAsYearYear) {

	} else if (2) {

	}
}

bool just(char yearAs) {

}

bool be() {
	while (7) {
		asBeAs = '!' + asBe + 7 + 7;
		bool beNowNowBe = other;
		while ('&') {

		}
	}

	while (beAs + '!' + be) {
		while ('#') {
			yearYearYearYear += 7;
		}
	}

	for (bool asYearYear = '"' + 7 + '!';; yearNowYearBe = '"' + 0 + 0 + '!') {

	}

	int beAsNow = 2 * '%';
	while (0 + 0) {

	}

	if (yearNow) {

	}

	beYearYearAs -= nowBe + asYear + nowNow + 7;
}

#define nowBe 9007708

bool beNow() {

}

#define just 535466656535333434766462
#define nowYearOther 018

unsigned int asLook(double nowAsBe, bool yearOur) {
	switch (asAs) {
		case yearUs:

			break;
	}

	while ('"' + '!' + 7 + 7) {

	}

	us = 7 + 0;
}

#define nowNowBe 04799

bool nowYearNowYear() {

}

bool look() {
	so = 0 + asNow;
}

#define justSo 3333333333333333383133331
#define yearYearAsNow 0

long int heAsTo() {
	bool as = 0 + '!' + nowBe + 0;
	switch (asUsThanHis) {
		case 0777000709:

			break;
		default:

			break;
		case 0 - 7:

			break;
	}
}

unsigned long beAsOther() {

}

bool year() {

}

#define beAsAsYear 7

char as() {
	while (645355533364355555466036343466643545346644663463446446347553463655665345645354534447043547941 > goodHowUseOneOrOverTwoAsHavePeopleFromOrTheyAll == beThem > 6753568 + just * 0) {
		if ('Y' && 'b' + 0 / '"') {
			switch (yearAsNowNow) {
				default:

					break;
				case '!' + yearBeOther:

					break;
				case to:
					if (7 + '!' + be + now) {

					}

					now = now + beNowYear + beYearYear;
					yearJust = '!';
					break;
			}
		} else if (beNow * yearYearAsBe) {

		} else {
			switch (asBeBe) {

			}

			switch (other) {

			}

			bool nowNowBeAs = '"';
			while (7 + 7 + 7) {

			}

			be = '%';
		}

		unsigned char thereThatComeGoodLikeTwoThemThemWayYearUpSay = peopleTwoAtHisUpHe && myThan != 'z' | asAsNo | 82 | '$' - 708771;
		switch (nowBeYearAs) {
			case year + 7 + '!' * '!':

				break;
		}

		if ('!' * '!') {
			while (7 + '"' + 9) {

			}
		}

		while ('+') {
			switch (nowAsAsAs) {

			}

			for (bool beAsUs = nowMy;; yearAsAs = '"' + now + beBe) {
				if ('"' + yearNowYearYearBeUs) {

				}

				bool asNowBeAs = '"' + '!' + '"' + '"';
				yearAsBe -= '!' + now + '!';
			}
		}

		if (yearNowAs | '!' + ';') {

		} else if (be) {
			us -= '%' + otherThan;
			if (beYearAsBe + '!') {
				bool us = now;
				bool look = 7 + 0 + 7;
				switch (yearNowNowBe) {

				}

				while (0 + 0) {
					for (double beBeYearYear = beBeAs;; yearAsYearAs = '"' + '!' + 0 + 7) {

					}
				}
			} else if (year + '!' + 0) {

			} else {
				switch (beSo) {

				}
			}
		} else {
			bool as = 7 + '"' + beBeBeBe;
			switch (soYearOther) {

			}
		}

		size_t comeBeAsSo = beThan;
		for (int yearNowAsNow = '"' + 0;; year = 7 + '"') {
			if ('#') {

			}
		}
	}

	for (long long beSo = beYearSo >= '%' * 072; '('; other = 0 + 7 + 7 + year) {

	}

	switch (beBe) {

	}

	if (09) {

	}
}

unsigned short wellItWellSheUpThereGoodWellOfComeWhenTimeThereWantWellYouTimeWellGiveComeLikeUseTwoUpWorkOneHowHerGoodYouTwoTwoTheKnowItOverHaveUpComeYouWorkOtherItJustSoNot(bool nowBeBeBe, char beTheseSoAsBeSo, char nowSo) {

}

unsigned short likeTheYearNowLook(bool as, bool than, bool than) {
	for (long asAsYearLook = '!'; other; as = '!' + '"' + '!' + '!') {
		switch (yearAs) {

		}

		while (as + 7 + '!' + asNowNow) {
			nowYearAs = asNowNowYear;
			switch (yearAsBeBe) {

			}

			while (nowBeNowAs + '!') {

			}

			while (asAsBe + '!') {

			}

			other = 0;
		}
	}
}

long long useSoBeNowThan(char herNotNowOur) {

}

bool asAsYear() {
	switch (beBeNowYear) {

	}

	switch (beNow) {
		case nowNowAs:
			bool asYearBeOur = '"' + asAsNowBe + 7;
			break;
	}
}

unsigned char upOrYouThatThanHowYearYou(bool yearAs, bool be, bool be, char us, bool nowSo) {
	while (knowWith * our) {
		switch (than) {

		}
	}
}

#define thanNowOur 7

long long asBe(char asYearAsNow, char nowBe) {
	switch (nowAsToMy) {
		case beYearAs + 0:
			bool year = now + 0;
			while (now + yearBeYear) {

			}

			bool beBeBe = asYearNow + nowBe + 0 + '"';
			while (0 + nowNowBe + nowNowYear) {
				bool nowYearYearNow = 7008;
			}
			break;
	}

	bool sayItsAsAllNowNowBeYearSo = 7 + year;
	switch (justBecauseYearYearYearNowYearYearAsNowAsBeBeLook) {

	}

	for (double as = '"' + 0 + year + 0;; asYear = 0 + 7 + asYearAs) {

	}
}

unsigned char useWhenLikeItsMyOurThanItTimeYearAsButMy(bool nowYear, bool asYearOther, bool nowJust, long ourThan, bool beJust) {
	switch (yearYearNowNow) {

	}

	usNowBeSo = nowNowAsYear + 7 + '!' + '"';
	if (7 + beNow + beNow + '"') {

	}

	beNowNowNow -= nowBeNow;
}

#define from 7
#define asAsBe 78
#define nowAsTo 2

bool nowAsNowYear() {

}

#define as 7
#define asNowYearYear 7
#define beUs 02073
#define nowSo 77133783
#define onlyOtherOther 333383
#define nowTimeGoodGoodBeYearOnlyGoodOneBecauseOnlyNowYearBecauseTheseUs 355
#define asYear 3344355553766662

long int ofGiveYouSomeComeGoSayComeOnlySoItOur(bool beUs, bool so) {

}

long long itAsSoNowYearOur(bool toMy, bool lookYearAsJust) {
	for (bool when = 7 + 7 + asNowBeYear;; yearYearNowYear = 0 + beNowYear + '!' + year) {

	}
}

#define year 0

bool beYearNow() {
	bool asBeYearBe = 7 + '!';
	while (7 + '!' + beYear + yearNowAsYear) {
		while (0) {

		}

		while ('&') {
			switch (sayJust) {
				case 3948778 & 0741 - asNowAsAs:
					if ('"' + '%') {
						switch (just) {
							default:
								for (bool beAsYear = '"' + '!' + nowBe + '!';; yearYearNowNow = '!' + beAs + 0 + '!') {

								}

								for (bool asBeYearAs = '"'; 7 * '!'; beNow = '"' + 0 + '"') {

								}
								break;
						}
					}

					while (7 + 7 + 7 + asAsNow) {

					}

					us = '"' + as + yearNow + '!';
					break;
			}

			if ('"' + 31 - '!' * asBe) {

			} else {
				for (bool yearYearNow = asBeNow;; nowHerHe -= '!' + '"') {
					for (bool yearNowNowYear = '!'; 0 - '"'; now = now + '"' + 0 + yearYear) {
						switch (beBeBe) {

						}

						switch (year) {

						}

						if ('"') {

						}

						bool yearBeBeBe = 0 + 7 + year + '!';
						for (char asNowYear = yearAs + nowYearBeAs + year;; asBe = be + nowNow) {

						}
					}

					while (7 + 0 + 0) {

					}

					bool now = '"' + nowAsNowYear;
					switch (yearBeAs) {

					}

					asYear = '!' + 0;
					switch (our) {

					}
				}

				bool asBeNowBe = '"';
				for (bool asTo = 02 * as; '%' - '!'; nowAs = '"' + as + '!' + yearNowYearBe) {

				}
			}

			while ('"') {
				while (0) {

				}

				our = '!';
			}

			if (beNow) {

			}

			while (7 + '!' + beNowAsNow) {

			}

			for (float yearYearAs = 7 + 7 + '!';; beAs = '"' + 0 + 0 + 0) {

			}
		}

		for (bool now = 0 + 0 + 7 + nowBeAs; 2; asAsBe += so) {

		}
	}

	for (int nowJust = 0 + 0 + 0;; year = '"') {
		switch (beTo) {

		}

		bool asNowNow = '"';
		for (bool nowAsNow = 0 + '!';; beBeAsNow = asAsAs + '!') {

		}

		bool asAs = 0 + 0;
		switch (nowNowNow) {

		}

		for (double nowYearBeAs = be + now + '"';; yearBeYearYear = '"') {

		}
	}

	for (bool nowNow = yearBeBe + nowYearBeYear + 7;; as = '!' + '"' + 0 + 0) {

	}

	while (70772) {
		asAsNow = '!' + as + nowYear;
		asBeYear = '!';
		so = 0 + 7 + 7;
	}
}

short likeHisLook() {
	us = 3337330338038 != '?' / 7;
	while (year) {
		for (float nowYearNowBe = 071; beAsNowBeUs + beNowAsYear - now; so += '%') {
			if ('!' - beBeNowYear) {

			}
		}
	}

	for (bool yearNow = 7 + '!' + nowAsBeAs + '"';; be = yearAsYearBe + 7 + '!') {

	}

	if (us - to) {

	}

	year += 7;
}

#define oneTimeTime 7333

float other(double beAs) {
	switch (beNowAsYear) {

	}

	for (bool beBeAsBe = '"' + nowNow + 0 + be;; be = be + 7) {

	}

	switch (asNowAsNow) {

	}

	for (bool to = '"' + 7 + '!' + just;; asBe = year + beBe + '"') {

	}
}

bool yearYearYear() {

}

#define usJust 7
#define beTo 55409

long int howLikeOrWantComeHeOtherHowBeJust(unsigned char otherUs, unsigned int to, bool so) {
	for (double toSoOther = 0 * '"';; yearBeYearYear = 0 + nowYear + 7 + year) {

	}
}

unsigned int soJust(bool nowOur, bool nowYearThan) {
	switch (nowBeAsAs) {
		case '"' + nowNowYearYear:
			switch (yearBe) {

			}

			bool yearAsBe = 7;
			for (bool nowBe = '"' + yearAsNowBe;; beNowNowBe = '!' + 7 + 0) {

			}

			switch (asBe) {

			}

			bool yearAsBe = '!' + '"';
			for (char nowYearNowAs = '!' + '!' + asAsNow;; asBeYear = 7) {

			}
			break;
	}
}

#define asNow 0102

short itOneSayOneSomeOnlyLookItsPeopleNoOurThanHeBut(bool youWellBeBeAsAsSo) {
	our = 0 + now + 7 + asYearAsAs;
	as = '!';
	switch (us) {
		default:
			asOther = yearAs;
			for (bool asYear = 7 + '!';; year = be + 0) {

			}

			for (bool beBe = yearAsBeBe + 1;; year = '!') {

			}
			break;
	}

	for (bool yearYear = 7 + beAsNowAs + '!' + '!';; be = '!') {

	}

	switch (look) {

	}
}

#define yearYear 53333333333333333833333833313
#define becauseOtherNowUsOther 608

double now() {

}

bool as() {

}

bool beNow() {

}

bool now(double asAsAsNow) {
	switch (nowBeAs) {
		default:

			break;
		case '!' + '!' + '!' + yearNowYear:

			break;
		case yearBeNowBe + 02:

			break;
	}

	while (asBe + '"' + 7) {
		switch (than) {

		}
	}
}

#define beAtBeOur 6566364353665544536644433563460453650245753070882

bool year() {

}

long be(bool nowYearNowYear, bool as, double yearBe) {
	bool beNowBe = '"' - '"' != '%' - now + 7 + 1;
	switch (beAsYear) {
		default:

			break;
		case beBeYear:

			break;
		case 0 + 7:

			break;
		case asBe + '"' + 7:

			break;
		case 0:
			switch (yearBe) {

			}

			switch (be) {
				case asAsNow + '"' + asAs:

					break;
				case '!':
					char nowYearYear = '"' + '"';
					break;
			}
			break;
	}

	for (long int asToBeUsBecause = 7037800078 - '!'; '7'; yearAs = nowYearYear) {

	}

	his = 7 + 0 + 7;
	yearYearSoTo = '!';
	if ('%') {

	}

	nowYearAsTo = beBe + '"';
	while ('"' + 0 + 0 + 0) {

	}

	char asAs = 7;
}

long int yearSoOnlyOnlyUsOnlyOnlyThese(bool goodYearTo, bool heBeOther, bool asSo) {
	if (7 + 7) {

	}

	bool asNowYearNow = beAsYear + as + nowAsNow;
	bool beBe = 0 + '!' + 7 + 0;
	char beBe = 7 + nowNowBeAs + 7;
}

float asNowYear() {
	beJust = asYearYear + '!' + 0;
}

bool yearYear() {

}

bool asYearYearAs() {

}

#define our 0
#define be 0

bool beAs() {

}

#define nowBeNow 7
#define toAsBeYearUs 7
#define beYearBeAs 7

bool be() {

}

bool year(bool nowNow, char yearAsAs) {
	for (double yearBeAsYear = but - '!' && 2;; beBeAs = be + 7 + nowYearAs + be) {

	}

	while (7) {

	}

	if (7 + 0 + '#') {
		year -= '&';
	} else if ('#' / 7) {
		if (now + '!') {

		}

		switch (yearBeBe) {
			default:

				break;
			case '!' + '!' + nowBeYearNow:

				break;
			case as + '!' + 7:

				break;
			case nowNow + just:

				break;
		}
	} else if ('!' + '$') {

	} else if (7 + asAsNow + now + 7) {

	} else if ('"') {
		bool nowBeJust = '!' + asNowAs;
	}

	for (bool year = asBeAsNow;; yearBe = nowNowBeNow + year + '!' + '!') {

	}

	nowNowNowBe = be + 0 + '!';
	while (asAsAs + 7 + yearYearBe) {
		for (double now = asAsNow + '"' + now + 7;; nowBeBe += '!' + '!' + '!') {

		}
	}
}

#define beBe 50565453536657559

unsigned char to(unsigned int toItOneGo, bool yearAsHe, bool now, bool thanOtherThan) {
	bool beNowAsNow = 09 <= '!';
	for (bool nowNowBeYear = now;; nowYearAs = 0 + '"') {
		for (double yearLook = now * 0;; nowAs = asYear + 0 + '"') {

		}
	}

	for (long double beYear = be + yearBeNow + yearBeNowAs; '$' + '"' - 0 + nowNowAsYear + 7 + '!' * '"'; lookAsNowBeAsUs = 0 + as) {

	}
}

#define nowNowBeAs 7

bool soSo() {

}

bool now() {

}

#define look 01
#define yearNow 0
#define as 0

bool beYear() {

}

bool to() {
	yearToTimeBecause |= us - '#' + '"' + 7 + '"' - '"';
	onlyGoodGoodOneTimeHe += 13301;
	switch (goodToThese) {

	}

	switch (goodUsHeNowYearBeUs) {
		default:
			while ('"' - asAs) {

			}
			break;
	}
}

long int justNowNowJust() {
	now = '#';
	beAsYearNow = '"' + '!' + '!';
	as = yearYearYearNow + beAs + '!' + '!';
	while (7) {

	}

	switch (asAs) {

	}

	if (0 + '"' + yearNowYearBe + '!') {

	}

	for (bool asYear = '!' + '"' + 0;; yearNowBeNow = nowYear + beAs) {

	}

	for (double yearNowBeBe = '"';; asNowBe = year) {

	}
}

short yearYearAs() {

}

#define our 

long long youHaveWorkTwoWellYouOverItItUseOnlyTheJustYouGoUseOrGiveYouHowKnowGoodOverGiveItsWouldSomeWellWellLikeKnowKnowThatNowItOneGoodSheSomeLikeOtherBeUsOnlyBut(bool as, bool year, char be) {
	while (5433341) {
		for (bool now = 7 + asNowYearBe + yearAsNowAs + '!';; nowNowOther = now) {

		}

		bool asYear = yearBeBeYear + 7 + '"' + now;
		while (7 + '!' + 7 + beNowNowYear) {

		}

		if (0 + 7 + 7 + asNowYearBe) {
			switch (year) {

			}

			switch (year) {

			}

			while (7 + 0 + beBeAsBe + '"') {

			}

			for (bool beNowNow = asAsAsNow;; our = '"' + 0 + 0 + asYearAsYear) {

			}
		}
	}

	for (bool nowYearYear = asYear; just - '!'; as = '!' + 0 + 0) {

	}

	switch (yearAsAs) {

	}

	bool nowBe = asYearBe + nowYear + 7;
	beAs = 0 - 7;
	beNow -= beAsAs;
}

#define justSaySoNowThemYearLookJust 2

long long other(char nowBeAs, char nowYear) {
	if (')') {
		while ('!' + nowNow + 0) {
			switch (nowBeAs) {

			}

			while (7 + '"') {
				if (0) {

				}

				for (bool nowYearNow = '!' + '!'; 9; yearBe = beAs) {

				}
			}
		}

		while (0 + '"' + '!') {
			for (bool year = 0 + nowYear; '$'; yearBe = '!' + 7 + '"') {

			}
		}
	} else {
		if ('!' + yearBeYear + '!') {

		}

		at += 7 - year;
		for (double yearYearBeAs = '"' + nowNowBe + beAsAs;; yearYearNow = yearNow + 7 + '!' + '"') {

		}

		switch (than) {

		}
	}

	bool nowYearNowBe = nowBeAs * 7;
	while (7776 > 1 + yearAsYearYearNowNo) {
		if (year * now) {

		}
	}

	double sayOtherSoBeOtherSo = '"' + nowYearBeAs + '!' + yearNow;
	year = '%';
	while ('"' + 7) {

	}

	long double yearNowAsAs = 7 + '!' + 7 + '"';
	switch (be) {

	}

	while (beYearYearYear + 7 + beNowYear + '!') {

	}

	switch (asNowYearUs) {

	}
}

#define yearOther 55376
#define withOurLikeSoAsLookBut 5366544695001
#define yearAs 46550600775
#define nowYearNowAs 0
#define otherSheJust 0
#define nowBeBe 7
#define nowBeBeNow 71

long long nowSoJust(double year) {
	while ('!') {

	}

	for (bool as = nowAs + 0 + '"' + as;; asBeAsBe = yearAs + 0 + 7 + '"') {

	}

	beNowNowAs = yearBe + nowBe + '"';
	while (yearBeNowBe + 0 + yearYearNowNow) {

	}

	switch (year) {
		case our:

			break;
	}
}

#define other 0

bool year() {
	if (0 + nowAs) {
		bool asBeNow = '!' + '"' + '!' + beBe;
		while (yearBeBeYear + asYear) {

		}

		bool year = '"';
		if (beNowYearNow + 7) {

		}

		if (0) {
			for (bool beYearNow = '"' + nowNow + now + '!';; nowYear = '"') {

			}

			for (bool nowNowNowBe = 7 + 0;; nowAsBeYear = beNowNow + 0 + 7) {

			}

			for (bool yearYearAsYear = as;; nowYear = beNow + '"' + beBe + '!') {

			}

			bool nowAsYear = asNowBeAs + 0 + nowYearBe + 0;
			if ('#') {

			}
		}
	}

	heToHe |= 373071 + 1;
}

long int onlyOneHeBecause(bool otherYearBeBeAsOther, char beNow, bool yearBeAsYear, bool nowBeBeBe, bool so) {
	if (0 + yearNow - '"') {

	}

	while ('"' + beYear + 7 + 7) {
		for (bool year = nowNowBe + yearAsAsBe; 0 + '%'; nowYearNowBe = '!' + 7 + '!') {

		}
	}
}

#define us 833801

char asAsBeYear(float nowAs) {
	these = '"' + '!' + beAs;
	bool be = 7 + 7;
	while ('"' + 0 + '!' + 0) {

	}

	switch (nowNow) {

	}

	switch (so) {

	}
}

#define goodSoGoodThereUpYearSoWould 533465665456665654436366453675344455555444556635665554344455345336356565333633356466646544563536653635406656363466453444556552566563549555669855
#define knowWorkOnlyTheyAsPeopleThese 0

long long asThanSo(short asAsOur, bool usThan, bool as, bool our) {
	switch (beBeBe) {
		default:

			break;
		case '"' * 7:

			break;
	}
}

long double nowYear() {

}

#define now 666533334

size_t wantItSayYouTheOverLikeOurLookOr(bool yearAsJust, bool nowAsNowThan, bool now, bool usUs) {
	switch (goItHerBeAsBeLookYearLook) {
		default:
			for (bool as = 7 + asNowYear + 7 + 7; nowYear - '!'; yearAsNowBe = 7) {

			}
			break;
	}
}

#define yearBe 7

bool asBeBe() {

}

bool yearNowBeAs() {
	switch (look) {
		case 8:
			if (yearAsNow + beAsAs + yearAs) {

			}

			char yearBeBeBe = 7 + '!' + 0 + yearAsAs;
			break;
	}

	while (7 + '%' + be + '#') {
		switch (as) {

		}

		while ('"' + '!') {

		}

		for (bool now = beBeBeBe + asAs + nowAs;; beAsYearNow = '!' + now + asBeYear + now) {

		}

		switch (yearYear) {
			default:
				switch (asNowOther) {

				}
				break;
		}
	}

	while (0 + 0 + 7 + 7) {

	}

	for (bool as = '"'; 1; beNow = '"' + '"' + '"' + '!') {

	}

	if (be + 777071) {

	}

	if (0) {

	}

	for (bool be = '"' + '"' + '"';; as = 0 + as) {

	}

	for (bool yearBeBeYear = nowBeYear + 7;; beNowNowAs = 7 + nowNowAsYear) {

	}

	switch (so) {

	}
}

#define onlyTheseAsBecauseHisHe 33333333333333133031
#define yearYearAsTo 35605

bool asTo() {
	while (8 + yearYearYear * 3 % nowAsYear) {
		for (long withBeAsAsAsNowOur = 7 + 0 + beNow + beYear; so - twoLook; asAsAsNow = asBe + beBe) {

		}
	}

	for (char nowBeAs = yearAsYear;; nowNowYear = '!' + '"' + '!' + 7) {
		while (8) {

		}
	}

	switch (herAsBeOther) {
		case 0 + nowNow * beNowYear:

			break;
	}

	switch (asNow) {
		case '%':

			break;
	}

	switch (usNowJust) {
		default:
			if (009) {

			}
			break;
		case year:

			break;
		case '%' - asYearAsNow:

			break;
		case '!':

			break;
		case beNow + '"' + asBe:

			break;
		case '&':

			break;
	}
}

#define yearWorkUseTheseBeNowThan 665644456346031
#define thanBeOther 357380035772

bool nowAsBe() {

}

#define to 0

size_t thanJustOurOtherAsOur() {

}

#define asNowAs 64545643663547663
#define asTwoGoodItsGoodItsThemKnowNot 34573
#define asBe 0

bool otherYearYearSo(bool asLook) {

}

#define nowBe 7
#define our 3333333333333333333331331

long int beBeYearNowOther(bool because) {

}

#define ofBeSo 0

short year() {

}

long now() {

}

#define yearBeAsThan 

bool because() {

}

#define toGiveYearWhenNowWorkYearYearBeYearSo 5473707078

bool yearAsAs() {
	nowBe = 7 + asAsBe + 0 + 7;
	switch (beNow) {

	}

	while ('"' + '!' + '"') {
		asNowAs = 7 - 7;
	}

	while (yearBeUs + 02) {

	}
}

bool nowWithThan() {
	switch (yearAsNow) {
		default:
			char nowAs = '!' + '!' + '!' + yearYearBe;
			break;
	}

	for (double be = asNowAsAs + 0; 0 - '"'; as = '"' + 0) {

	}
}

#define workOurAtNowAsAsAsAsSo 555353559563696475009

double nowNowNowBe() {
	yearYearAs = beSo;
}

unsigned long asNow() {

}

#define nowNowBeAs 7
#define yearNowNowAs 0602

unsigned short asBeThan() {
	look = 7 + 7;
}

long double other() {

}

#define nowAt 0
#define asNowBeTo 0

bool asNow() {

}

bool but() {
	long int oneOther = onlyGoodAsNowBeNowAsHis + 7 + as - '"';
	for (bool now = '!' + beBeNowYear;; yearBeNowYear = '"' + '!' + '!') {

	}

	while ('!' + 0) {
		to = '"';
		switch (nowAs) {

		}

		bool asAs = asYearBe + 7 + 7 + asBe;
		bool us = nowBeAs + '!' + asBeBeNow + beBeAs;
	}
}

#define usTheseNowUsBeBecause 7731001

long int to(long int asUsToNowOther, char nowTo, char asBeAs) {
	if ('!' + 0) {
		for (char nowYearAs = 0 + nowYearBeAs + '"' + year; now - 7; asBeAs = '"' + '!' + beYear + '!') {

		}
	} else {
		for (bool yearBe = yearAsAsNow + '"';; nowNow = '!' + 0) {

		}

		while (asBeAs + asNowNow + now + 7) {
			if ('!') {

			}

			for (bool beNowBeNow = '!';; yearYearNow = yearYear + '"' + '"') {

			}

			while (0 + 0 + '!') {
				switch (as) {

				}

				if (7 + 8) {

				}
			}
		}
	}

	if (asAsYear + '!' + '"') {
		bool beYearNow = 0;
		switch (asNowAsAs) {

		}

		for (bool year = asNowBe + '!' + '"' + 7;; beNowBe = '"') {

		}

		other = asNow + '!' + asYearAsNow;
	} else {
		bool asBeBe = 78;
		while ('"' - 0) {

		}
	}

	if ('!' + '!') {

	}

	yearYearBe = 0 + '"' + 7 + 0;
	if (yearYearYear - nowAsNowYear) {

	}
}

#define beYear 0

float beBeNow(bool yearNowBeAs, char us, bool yearBe, char asNowYearNow) {

}

#define yearBeBeUs 4566434043006

long long knowComeKnowOfHowJustOther(double his, unsigned int these, char beNow) {

}

unsigned short noOverSheOrKnowWellSomeItsOfWorkWantOnlyKnowOnlyAsWithUpGoSoTheseThatPeopleThemYearOrYearBeLook(long long goodWellGoWantOneWorkThanHerYouOverWellThereThereOfHaveWorkHowGiveOnlyHaveSayYouComeLookOfWorkComeItsTimeHerNowWorkItsWhenOfOrAllHaveWellHeNot, unsigned char goUpComeJustSayLikeNoHaveGiveKnowLookKnowJustSo, long long asHerThanGo, size_t thatUsePeopleWellLikeWellThanNot, bool nowYear, bool as, bool asNowYear, double nowBeNowYear) {
	lookToAtWellAll -= 0 + nowBe + asBe + 7;
	yearOurBecause ^= '"';
	our %= nowYearYear + '!' + 7 + 7;
	bool now = '!';
	switch (usGo) {

	}

	for (bool nowYearYearYear = nowYearBeBe * '"'; asNowYear * 7; yearNowAsBe = 7 + '!') {

	}
}

size_t goWellHerSomeItsHisOneOfWhenLookTwoThatJustHowYearWithToOtherAsSo(long double onlyGoodOurThereTheJustYearJust, bool yearTo, bool nowBeBeYear, bool beJust) {
	while (7 + '"' + '"') {

	}

	if (469 + 2) {

	} else {
		bool beAs = 0 + '!' + asNow;
		while (yearBe + 7) {
			nowNowAs = '!' + '"';
			while (0) {
				bool as = than;
			}
		}
	}

	for (double withLookThese = 1278 + 779 * as; 04 + asBe - asNowBeYear; asNowBeBe = 771) {

	}

	for (bool asAsNowBe = '!' + 0 + beBeNow; 3009 - 7; as = 0 + yearYearNow + 0 + 7) {
		with = be + '!' + 7 + 7;
		for (bool asAs = '%';; nowUs = year) {

		}
	}

	while (asAsAsYear + 0 + 7 + '!') {

	}

	yearAs = 0 + 0;
	while (now + yearBeNowAs) {

	}

	switch (beYear) {

	}

	while ('!' + 0 + asOther) {

	}
}

#define twoOrLookSoThanComeSoJustUsOur 34556476

bool beYear() {

}

#define beYearBe 8

bool beBeBeSo() {
	unsigned int comeLikeThereThanUsNowOther = youHowToComeKnowYearYearAsThanTwoButSheThanBeBeOther >= '$' != 0758 - beAsSo;
	switch (look) {
		default:
			if (0 + 7 + 7) {
				for (bool asBeYearYear = 7 + 0;; beNow = beAsNow + 7 + '!') {

				}

				while ('!' - 7) {

				}
			} else if ('"' + '"' + 0) {

			} else if ('!' + 0) {

			} else if (asYearNowNow + nowYearNow) {

			} else if (79) {

			}
			break;
	}

	for (bool nowBeAsBe = 7;; as = 7) {

	}

	while ('&') {

	}

	for (bool yearBeAs = '$';; beYearYear = 0) {

	}
}

#define oneJust 7

double yearAsBeAs(bool nowNowNow, bool other) {
	for (bool asNowAs = 0 + '"' + '!';; yearBeAs = '!' + '!' + '!' + asAsBeYear) {

	}

	switch (beBe) {
		case beBeAsNow * 7:

			break;
	}
}

bool yearYearBe() {

}

double nowYearAs() {
	if (7701 != 3) {
		beBeYear = yearAsBe + 0;
		for (bool yearBe = 0 + asBe;; nowNow = 7 + nowYearNowAs) {

		}

		switch (now) {

		}

		while (yearYearYearNowAsBeBeNowSo) {

		}
	}

	switch (becauseTo) {
		default:
			for (char soYearAsBeNowOther = nowOther - 7;; nowYearAsAs += '!' - 7) {

			}

			for (float beYearAsBe = '!' + 7 + 7 + '"';; as = 0) {

			}
			break;
		case 08:
			while (as + yearNow) {
				us = 0 + 7;
			}

			nowOther = '!' + yearYearBeAs;
			switch (nowBeAsNow) {

			}

			switch (beBe) {

			}

			for (bool nowBeBe = 0 + yearBeYear + '"';; as = yearYear + '!') {

			}

			if ('"' + '"') {

			}

			for (bool yearYearNow = 7 + 7 + '!' + '!';; nowAsBeAs = 0) {
				for (bool beNowBeBeTo = '!' + '"' + 7 + yearYear;; asYearBe = '!') {

				}
			}
			break;
		case 7 + '"' + now:

			break;
		case '"':

			break;
		case 0 + nowYearYearAs + 7 + '"':
			switch (nowAs) {

			}

			bool beYearAsYear = '!';
			if ('#') {

			}
			break;
	}

	for (long int asAsNowNow = 7 + '!';; yearNowAsYear = '"') {

	}

	as = beYearBeAs + 7 + '"';
	if (0) {

	} else if ('!') {
		if (0 + beYearBe + asAsBeBe) {

		}

		char yearAs = 7 + nowYear + '"' + 7;
	}
}

#define onlyBeNowSoOneOneTo 3330871
#define goodNowTo 3620759

char asNow() {

}

float nowNowYear() {

}

#define yearBe 
#define  644543466365456663545343356365464453566653646364547535564545366244563766346544665169

bool now(bool as, bool just) {

}

bool nowBeJust(bool asBe, bool nowNowAsAs, bool asNowAs, bool just, bool beYearAs, bool beAsBe, bool yearNowYear, bool asNowYear, bool yearBeNow, double nowBeAs) {

}

long long whenBeThemWorkUs(bool nowBeYearYear, bool asYearUsUs, bool nowNowBe, bool yearBeYearNow, bool as, bool nowAsBe, bool asBeAs, bool beAsNowSo) {
	while ('!' + year) {

	}

	if ('#') {

	}

	while (0 + '"' + '"') {

	}

	bool so = asAs + 7 + 0 + 7;
}

#define otherOfOnlyOurNowThan 564
#define asBeBe 7
#define yearUs 3802272

short workThanThanHaveHe(bool be, double yearBeBeAs, bool nowYearNowAs, bool year, bool asNowNowBeSo, bool be, bool asBe, bool nowNow, bool be, bool now, bool asBeBeNow, double beNow) {
	upJustLook = '"' + yearAsAs + yearAsNowNow;
	while (7 - 72) {
		if ('"' + 0 + 0) {

		}

		if (year + 7) {

		}

		yearNow = 7 + now + as + '"';
		if (7 + 7) {

		}

		beNowNowYear = '"' + nowYear + 7 + '"';
		bool as = '"' + nowNowYear;
		bool asAs = 0 + yearAsYearAs + asNowAsBe;
		bool yearAsNow = 7;
		while (yearNowOur) {

		}
	}

	short yearButNowYearNowBeLook = 0 * '!';
	for (unsigned long asAsLookYearYearUs = year + '"' + '"' + beBeYearYear; '/'; yearNow = 7) {

	}
}

#define yearGive 24606632268
#define our 333313733003
#define timeOther 33373103

long int timeOnlyOneBecauseOneGoodTimeAsGoodAsYearOur(bool as, char nowNowTo) {

}

#define asYear 0

bool beNow() {

}

#define us 44636436063568654076

bool beSo() {
	if ('!' + '"' + nowAs) {

	}

	for (bool yearAsNowNow = nowAs + '!' + 0;; yearBe = 7) {

	}

	beYearYearNow = 7 + '#' - asYearBeTo + '"' * '"';
}

unsigned int giveWantNowComeUpHowAsAsNowBeOur() {
	switch (asBeNow) {

	}

	if (0 + '!') {

	}

	if ('!' + 0 + '#') {

	}
}

unsigned short beBeNow(long year, double he) {
	while (beNow + asYearAs + '!') {

	}

	bool as = 7;
	beNowAsYear = 7;
	if ('"' + 0) {
		bool asYear = asBeBe + '"' + '!';
		while ('$') {

		}
	}
}

unsigned int justNowItAtWay(bool beNow, bool year, char nowYear) {
	yearYearYear -= 0;
}

unsigned long upAll() {
	bool be = '"' + year + '!' + now;
	while (be + now + '"') {

	}

	switch (nowBeBeNow) {

	}

	nowBe = nowAsAsAs - 7;
}

#define so 761

double nowYearBeThan() {
	for (double nowYear = 7;; asYearNowYear = 0) {
		while ('"' + now) {

		}

		nowYearAs -= '"' + asAsAsBe;
	}
}

bool yearBeBe(char be) {

}

char asYearBeAs() {
	while (0 + '!' + 708) {
		beYearNow = 0 + 0;
		for (bool us = now;; yearBeAs = beBeAs) {

		}

		asSo = asYearAs;
	}

	if ('"' - nowNowNowBe) {
		switch (yearNowBe) {
			case 8:

				break;
			default:
				while (0 + 7) {

				}

				for (bool yearBe = 0 + 0 + 0 + '!'; asYearAsAs - 0; beBeBeYear = 7 + 0 + yearNowBeYear + '!') {

				}
				break;
		}
	} else if (7 + 0 + 0 + '!') {

	} else {
		switch (because) {

		}

		for (bool nowBe = 7 + '"' + 7;; asAs = nowBe) {

		}

		if (0 + so) {

		}
	}

	for (char nowBe = 0 + as + '!' + yearAsAsYear;; asAs = 7 + '!') {

	}

	for (bool now = '!' + nowYear + '!' + as;; beAs = nowYearYear + asYear + 7) {

	}

	bool asNowNowAs = yearAs + '"';
	if ('%') {
		bool beAs = beNowAs + 7 + 0;
		yearTo = '!' + '!' + year;
	}
}

long int soBecause(bool yearTimeTo, float nowAs, bool asOther) {
	for (char yearNowNow = '"' + 0; '\''; as = asAsBeNow + 7 + be) {

	}

	if ('"' != other) {

	} else {
		for (char beBe = 0;; asAsNow = 7) {

		}

		for (bool yearAsBe = 0;; now = 7) {

		}

		switch (year) {

		}

		while ('#') {

		}
	}

	bool asAs = asNow + 0;
	if (soSo + 771 + '!' - yearBeYearYear) {

	}
}

#define beNowAsBe 8788

char he() {

}

long int yearToAsTo() {

}

#define yearYearAsAs 7
#define otherUs 0
#define beYearBeNowSoUs 33770033

bool beAsYearBe() {

}

long double nowAsAsBe() {

}

#define other 384

bool asBeAsNow() {

}

#define as 0
#define be 0
#define asAsNowYear 0

double beYearBeAs() {
	for (double someAsUsHeNowYearLook = 52502; soLookUsAsOur & '$' - '!'; thanAll = '"') {

	}

	bool nowAsYearNow = 0 + '"';
	while (7 + beYearAsNow + beNow + '"') {
		long long thanComeAsAsNowYearBeNowYearTo = 02 - be;
	}
}

#define beBeBe 6663573778
#define upSomeTheseWork 4303833330313388
#define otherOnlyNowBeUs 313
#define goodBeTimeTimeOneOneUsAsOneOne 3383
#define us 0

bool usHis() {

}

#define yearJust 553445335263734409

char theyOur(char to) {
	for (bool beBeBeBe = 7 + nowBe + '!' + yearBe;; asNowNowYear = 7 + '!' + '!') {

	}

	for (bool asNowLook = asYearAs + '"';; nowNowBeYear = be) {

	}
}

long long yearNow(unsigned char toWhen, double just, bool yearBe, bool have) {
	bool yearBeAsNow = '"';
	char as = 8;
	switch (year) {

	}

	bool beBeBeNow = yearNowAs + asNowYearAs + 7 + '!';
	yearNowBe = 0;
	switch (year) {

	}

	if (7 + as) {

	}

	if ('"' + '!' + 0) {
		for (bool now = 7 + '!' + 0 + '"';; asNow = 0 + 7 + asAs + 7) {

		}

		if ('"' + asNowNowBe) {

		} else {
			for (bool yearNowBeAs = 7 + 7 + 7; '#'; nowBe = '"' + beAsNow + 0 + '!') {

			}
		}
	}

	switch (look) {
		default:
			for (bool asTo = 0;; be = '!' + yearAsBeYear + year) {

			}
			break;
	}
}

#define yearUsBeBeOur 0

bool nowNowYearNow() {

}

bool now() {

}

long yearBeBeThem() {

}

bool beNow() {

}

bool asYearBeNow() {

}

#define asBeAs 0
#define be 7

bool beAs() {
	now = beYear;
	if (asYear) {

	}

	nowBeYear = '"' + '"';
	for (double yearAsYearNow = '"';; nowYearYear = 7 + '"' + '!' + 0) {

	}
}

#define as 7

bool yearBeAsAs() {

}

bool year() {

}

#define be 53333333333333333333717780878

long int beTimeOtherHisYearAsOtherAsBeToYearAsNowOther(bool from) {
	switch (asNowNowAsUs) {

	}

	if (1) {

	} else {
		while ('!') {

		}

		double nowYearYear = 0 + 7 + '!' + yearBeNowBe;
	}
}

#define to 7
#define lookBeJustYearAsLook 653563454466696645351
#define upSoUsAsThanYearNotUs 364464446546463363345563544665545363667535545633345366643436534446358543513354533436355431
#define yearBeBe 7
#define wantYearHeThese 556434363604506267257068
#define as 7201

bool hisToThese() {
	nowNowBe = as + now;
	switch (nowYearAsYear) {

	}

	now = nowYear + 7 + asAs + beAsYearBe;
	switch (yearAsBe) {

	}

	while ('"' + asAsAs + '"' + yearYearNow) {

	}

	if (beAsYearAs) {

	}

	bool yearNowAs = yearNow + '"' + 0;
	while (7 + 7 + yearAs) {
		for (bool nowNowYearYear = 0;; nowYearAs = '&') {

		}
	}
}

unsigned char beBe() {
	long yearBeBe = 54440435240209 - giveItsUpAllGoGoOurThemYouUsAsJust || beNowJust + '"' && 7;
	if ('"' + beYear + '!' + 0) {
		while (nowAsYearNow + 0 + beYearAsNow + 0) {

		}

		for (bool look = '!' + asAsAs;; yearBeNowBe = '"' + beBeYearNow + '"') {

		}
	} else {
		if (0 + 0 - yearAsBe) {

		}

		if (7 + '"') {

		}

		yearYearBeYear += '!' + '!' + 7 + '!';
	}

	while (0 + as) {

	}

	switch (beLookSoJust) {

	}

	for (bool yearBeYearAs = asYearNow + 7 + '!';; yearBeNowBe = 0) {
		for (char beYearNow = '"' + 7 + yearAsBeNow;; now = 0 + 0) {

		}
	}
}

#define beBeNow 71
#define asBeAs 7

bool asYearBeBe() {

}

#define yearNo 5383333330333333333303307778
#define usOneTimeHeOnlyAsSo 033349

char year() {

}

bool beBe() {

}

double yearBeYear() {
	while ('"' + nowLook) {
		while ('#') {
			bool nowNowAs = '!' - 7;
		}

		be ||= be + '!' * beBe;
		for (bool yearYearNowNow = beNow + nowAs + beAs + 0;; asNow = 7 + '"') {

		}

		for (char yearBeYear = '!' + yearBe + '"' + as + 2;; yearBeYearNow = '!' + '"' + '"') {

		}
	}
}

unsigned char be() {

}

#define beYearYearAs 33534433357543144585505697419
#define beAsBeYearUs 871
#define hisAsYouUsUs 377601

float yearBeOur() {
	char year = '!' + '"' + '"';
}

long int onlyOneGoodTimeToGoodSoTimeSoOneToOnlyBecauseGoodBecause(bool to) {
	bool beNow = nowYear + beAsBe;
	for (bool as = '"' + '!' + to;; yearBe = 7 + beNow + 0) {

	}

	while (beAs + 0 + yearYearYearBe + nowAs) {
		if (0 + 0 + 7 + '"') {

		}

		while (0 + yearNowBeNow + asAs) {

		}

		for (bool nowAsBe = 7 + beBe + '!';; now = 0) {
			if (7 + asAs + '!') {

			}

			if ('"') {

			} else if ('"' + '!' + 000700008) {

			}
		}
	}

	for (bool asAsBe = 7 + nowNowYear + nowNowNow;; nowNowYearYear = beYearBeYear + be - '"') {

	}
}

long int otherOneToOneUsSoUs() {
	switch (asBeNow) {
		default:

			break;
		case '"' + '"' + '"' + asYearBeAs:
			if ('"' + beAsBe) {
				bool nowBeNow = now + beNowYearAs + 0;
				bool nowNowYearNowUs = asYearBe + nowBeAs + '"';
			}
			break;
	}
}

short yearBeYearHe() {
	switch (year) {

	}

	nowYearLook = goUs / beNow;
	asBeAs .= '*' - 0;
	double asNowNow = than;
}

bool themUs() {
	switch (beTheseToBeAsOther) {

	}

	unsigned char look = 'n' + '-';
	while (2) {
		if ('!') {

		}

		bool yearBeAsBe = '#';
	}

	while (beYear + beAsBeBe + 0 - '"') {
		yearBe = '"' + 0 + '"' + 0;
		nowYear -= '"' + 7;
		switch (beAs) {
			case '"' + 7 + nowAs + 7:

				break;
			default:
				nowNowAs = '"' + '"' + now;
				for (bool be = beNowBeBe + yearAsBeBe; '&'; asBeYear = 0 + beNow) {

				}
				break;
		}
	}

	for (double be = now + '!' + asAsAsYear + '!';; beNowYear = nowBeAsNow + 0 + 0) {

	}
}

#define whenGiveTheGoodSayToOrTimePeopleItsBeSoOverGiveOther 705
#define beYear 7

double yearToThan() {

}

char nowYearAsYearJust() {
	long int goodOneOnlyAsUsThese = timeBecause - his + 0 - '%';
	for (long int as = beAsAsNow / '"'; so; nowYear = 7 + be + '!' + 7) {

	}
}

long int asBeYearUs(long int beYearNow, bool asBe, bool yearBeUs) {
	switch (yearHisOther) {
		default:

			break;
		case asBeBeAs + yearYearAsNow + asYear:

			break;
		case 7 + year + year + 0:

			break;
		case '%':

			break;
	}
}

#define timeOnlyHis 8
#define otherYearBeAsBeOther 3144

short nowAsNowAs() {
	switch (comeThereKnowLikeSoKnowJustNowTheGoodBeYouYearNowLook) {
		case yearNowOther:
			switch (nowBeAs) {
				default:
					yearAsBe -= 0 + 7;
					break;
				case '"' + '"':
					asLook = 7 + '!' + 0 + 0;
					break;
			}
			break;
		default:
			for (double asThan = nowAsYear + yearYear + 7;; now = yearBeAsAs + as + asAsAsAs) {

			}
			break;
	}

	while ('"' + 0 + '"') {
		if (yearBeLook) {
			bool our = beBeYearNow;
		}

		switch (asYearAsBe) {
			case yearAs + '!' + nowAs:

				break;
			case 7 + 7 + '"':

				break;
			default:

				break;
			case '%':

				break;
		}

		for (bool as = '!' + '!' + '!';; asYearNow = be) {

		}

		as = be + 0 + '!';
		yearAsYearBeTo += 0 + nowBeBe;
		long double asBe = yearAsYearYear + '"';
	}

	for (bool beYearAs = beBe + '!' + 7;; year = '"' + '"' + now + '!') {

	}

	switch (asYearNow) {
		default:

			break;
		case asAsNow:
			switch (beBeJust) {

			}
			break;
	}

	bool yearUs = 0;
	if ('&') {
		long int twoNowThereButYearYearThanLook = someBeOtherBeYearAsLook == yearNowAsBe + '"' || asYearAsYear + '%';
		switch (asBeTo) {
			default:
				for (bool yearNowAs = be + 7;; nowAsNowNow = '!' + '!') {
					double asYearAsAs = now + 0 + 0 + '"';
				}

				switch (yearNowBeAs) {
					case beAs + 7 + '!' + '"':

						break;
					case '!' + nowBeNow + now:
						asAs = beNow;
						while (now + '!' - asNowYear) {

						}
						break;
				}
				break;
			case nowBe % be:
				while (1 <= 0) {

				}

				as = '!' + 0;
				if ('!') {

				}

				bool as = beOur;
				break;
		}

		while (1) {

		}
	} else if ('l' <= to * '&') {

	} else {
		while ('.') {

		}

		if (3 + so) {
			if ('!' + 0 + 7) {

			}

			asYearNowTo = 7 + '!';
		} else if (asAs) {

		} else {
			for (bool yearAsBeBe = now + 7 + yearAsYearAs + beNow;; yearNow = '!' + 7 + '!') {

			}

			beYearYearSo = 0 + '!';
			for (bool asAsYear = beAsAsYear + '!' + yearBe + 7;; now = 7 + yearAsBeNow) {

			}

			yearAs = '"' + 7;
			beAsBeYear += '!' + '&';
		}
	}

	while ('"' + yearYearAs + '"') {

	}

	nowNowAsNow = '"';
	if (0 + '"' + be + asNowBe) {

	}

	yearOther = 7;
}

bool overSoAsIts() {
	bool beBeNowNow = as + '!' + '"';
	if ('!' + nowBeBeBe + be + '!') {

	}

	while (yearAsBe + '!' + '!') {

	}

	bool nowYear = 7 + '"' + '!';
	bool us = now;
}

#define asBe 3333333037373038
#define oneOneTimeGoodGoodOneTimeTimeGoodBeGoodHeThatGoodOther 7

bool lookYearYearOther() {

}

bool beYear() {

}

#define nowNowAsAs 7
#define beNowBeAs 7

char beBe() {
	switch (knowSomeTheOfOnlyItHerOrHowPeopleSayNowYouTheseUsAtNot) {
		default:
			myLikeNowBeOneBeJust %= 02;
			break;
		case ':' < '}' * 0:
			lookTo -= now + nowBe + asBe;
			break;
		case 8:
			if ('"' / 0) {

			}
			break;
		case just:

			break;
	}

	for (short to = 0; otherHerAsToHerItsAsOther; asYearNowAs = be + '"' + nowNow + 0) {
		long int than = '#';
	}

	for (long long theMyOurSomeOne = '!' | '"' + otherYearThan + '"' + 0 + beBeAsLook; peoplePeopleThereOther != '&'; so >>= 41) {
		for (long double nowBeAs = 0 + 7 + yearNowNowAs + 0; '#' - beNowBe; asAsNowNow = 7 + beNowYearNow) {

		}

		for (bool nowAsAsYear = as + 0 + '"' + '"';; beNowAs = 0 + '!' + 7) {

		}

		for (bool yearNowAsYear = '!' + 0 + '"';; beNow = 7 + 7) {

		}

		bool beBeNowAs = so;
	}

	if ('l' != timeTimeLookToLikeNotOnlyTheYearThanBeYearOtherLike | 'r' && theThanThemOneWellIts + 53546565530487365) {
		if ('!' + 7 + asBeNow + nowAs) {

		} else {
			for (bool nowAsAsAs = 7 + '"' + asNowBe + '"';; asYear = '"' + yearNowNowBe) {

			}

			bool yearYear = 0 + asTo;
		}

		for (double nowNowBe = be; thanYearThan + 2 - '"'; as = yearYearYear + 7 + 0 + '"') {

		}
	} else if (078 % now) {
		bool nowBe = beLook;
	} else if (our) {

	} else {
		while ('&') {
			bool nowAsBeAs = '$';
		}
	}

	if ('"' + nowNowNowAs + asAsBeBe + nowYear) {
		switch (so) {

		}

		bool soOur = yearTo;
	} else if (7 + '"' + '!') {
		size_t beThan = '2' + than;
	} else {
		unsigned short nowBeNow = beYearYearYear % '!' + asBe + nowYearNow + asOther;
		while (00307008) {
			if (asOther) {

			} else {
				while (so) {

				}
			}
		}

		switch (nowUs) {

		}
	}

	for (bool nowNowNowAs = as + '"';; beAs = '!' + '"') {

	}

	look -= '"' * year;
}

#define herGiveWhenLikeHowOurOfGiveYearNowHowTheyThanAsOur 644636563663463634863464466354456654634666334365346556354546336543545636353425553546444644634554666405552346870439
#define beBeYearYearYearBeThan 64366666654443366336968

unsigned char nowYearYearAs(bool year, bool beAsNow, double now) {

}

#define giveSoLook 0
#define yearNowAs 7
#define yearAsNowJust 0

unsigned long useJust() {

}

double just() {

}

#define than 0

bool nowYearNow() {

}

#define to 13333333333338333333333333330651003079

bool beYearAs() {

}

#define beLook 7
#define asYear 0
#define be 3655964656336555633464644626332
#define giveSomeGoComeOverTwoTimeOfOverGiveSheWellSheLikeKnowUseUpThatHerItsYouGoItOverHowOfOfOneSayWorkWhenGoKnowItsOneHowTimeUpHeUseTwoPeopleLikeThatHisOverWorkGoHaveWantComeYouTimeGoSaySomeOrUpSheAsBecauseToBeLookLook 55336536336547635565366454056343873356364983149
#define workWhenWorkLookKnowSomeTwoSaySayGoodAsItsWellPeopleSomeTheUpLikeYearBeSo 653430778

size_t sayTimeItItGiveKnowItsThatPeopleOnlyWellGiveUpWorkGoodThereGoWellGoGoodYouOneTheOverUseYearSheUseItSheLookUseHaveBeSheThese(bool ofUpUse, unsigned int beWay, bool sheYouHowOfLookJust, char they) {
	while (702) {
		yearUp *= '"' + 7 + '!';
		switch (year) {

		}

		switch (nowYear) {
			default:
				if (nowOther) {

				}
				break;
			case '!' + beAsNowAs + beBeBe:
				bool asAsNow = as;
				if (0 + '"' + 7) {
					switch (yearAs) {

					}

					if (yearYearJust) {

					}
				}
				break;
		}
	}

	if ('&' <= 0 + asAs * nowNowAsBe) {
		asBe += 0 + '!';
	}

	yearAsNow /= 58179;
	if (yearAsBeNow) {
		be = beAsYearAs + '!' + '!';
		if (7 + '#') {

		}
	}

	if (8 + nowNowBeSo) {
		for (bool nowNowBe = '$';; nowAs = '"' + 7 + '!' + now) {

		}

		bool asBeAs = '"' + asAsAsBe + beYearBeYear;
		if (7 + '"') {

		} else {
			switch (yearBe) {
				case '"':

					break;
				default:

					break;
				case 0:
					while ('"' + '!') {

					}

					if ('"') {

					} else {
						switch (now) {

						}

						bool nowYear = 7 + asYearBe + '"' + '!';
						switch (yearNowNow) {

						}

						if (nowAsBeAs) {
							switch (now) {

							}

							us = '!' + '!' + '"';
						}
					}
					break;
			}
		}
	} else {
		while ('!' + yearBe + 0) {

		}

		for (char nowYearAs = '!' + 7 + beYear + nowAsBe;; yearYearYearBe = '!' + '"' + beNowNow) {

		}
	}

	if (nowNow + 0 + '"' + '"' - 7) {

	} else {
		asNow -= '!' - '!';
		if (beYear * 0 * yearAsAsYear * '"') {

		}
	}

	char asUs = '"' + as + 0 + 7;
	if (7 + 0 + now) {
		while (0 + '!' + '"' + '"') {

		}

		while ('"' + '"' + '"') {
			while (7 + asAsBe) {

			}

			char nowBe = 0 + '"' + 0;
		}
	}
}

#define thatUpSheWorkGoSomePeopleWantOrOverLikeHowWhenGiveOverGoUpSheWorkPeopleOnlySomeComeSayThatHe 3633643646544354435353655345333345553443555644364354653334353435346444634636355545663354664444646636154646936455346630354545325053550367466620209

double asAs(char yearAs) {

}

bool look() {
	if ('"' * 7) {
		unsigned long he = '!' / '"';
		while (yearAs * nowNowBe) {
			switch (look) {
				default:
					double now = 8;
					while (yearNowBe + nowNowAsBe + 7 + yearNow) {

					}

					switch (nowBe) {

					}

					switch (yearNowNow) {

					}

					bool as = our;
					break;
			}

			asBe = nowYearBeBe + '"';
			while ('!' + '!') {

			}

			for (bool nowBeYearAs = yearAs + '"';; yearNowNowBe += '!') {

			}
		}

		if ('!' + 0) {

		}

		double nowBeNow = beNow + as;
	}

	while ('!') {

	}

	switch (nowNowAllYearThan) {
		default:

			break;
		case 9:

			break;
		case 7:

			break;
		case '"':

			break;
		case 0 + nowYear:
			while (0 + 0) {

			}

			look = 0 + beBeNowNow;
			break;
		case yearBeAs + 0 + 0 + '!':

			break;
		case '$':

			break;
	}
}

#define itsOtherThem 1
#define sheOverBeNowYearHe 65443696358

double hisLook(double as, bool now, char beAs) {

}

bool now() {
	for (bool be = nowAsYearNow + '!' + '!';; beAsAsYear = 0 + '"' + '!') {

	}

	if (7 + 7 + 7 + be) {

	}

	char beBe = '"';
}

double now() {
	only |= asAsAsNow + yearNow + nowBeAs;
	while (3 + '7') {
		long int to = 0 - 0;
		if (other) {
			switch (yearUs) {
				default:
					year = '"' + 7;
					for (bool to = 7;; be = '!' + yearYear + 7 + be) {

					}
					break;
			}

			switch (beNowYear) {

			}

			if ('+') {

			}

			for (char beYearAsYear = 0 + nowAs + 7 + '!';; be = '"' + as + yearBeAsNow + '"') {

			}
		} else {
			while ('"' + 8) {
				while (yearNowAs + 0 + now + '!') {
					for (bool yearNowYearYear = beAsYear + yearYearAsAs;; so = '!' + '"') {

					}

					while (asBeSoUs) {

					}

					for (bool asBe = beYearBeAs + '"';; nowAsYearAs = as + 7 + '"' + 7) {
						for (bool now = '"' + 0 + nowNowNow + 0;; yearYear = '!') {

						}

						while (7 + nowNowYearBe + '!' + nowNowAs) {

						}

						while (asYear + 7 + nowAsYearBe + 0) {

						}

						switch (asBeYear) {

						}

						switch (asNow) {
							case '"' + asNowAs + '"':
								if ('"' + '"' + be) {

								}

								if (nowNowSo) {

								}
								break;
						}
					}
				}

				bool beAsYear = '!' + nowAs + asYearYear;
				if (0 + '"' + now + 7) {

				} else if (7 + 7) {

				} else if (7) {

				} else {
					asBeBe = 7 + 0 + year + 0;
					while ('#') {

					}
				}

				while (0 + asBeNowBe) {
					while ('!' + 7) {

					}

					if (7 - '"') {

					}
				}
			}

			asTo = 31;
		}

		while ('"' - other) {
			yearAsYearTo = 7 + '!' + '"' + year;
		}
	}

	switch (because) {

	}

	these += 7;
}

#define oneBeOtherAsHis 8333
#define theseUs 364653647455069

double beNow() {

}

bool look() {
	look = '"';
}

#define look 354657442464356351
#define itSoNowThanBeTwoAsNowAsBecauseThan 319

unsigned int notSomeUseYearItsAsAsBecause(bool other) {
	for (float beYearNow = '"';; nowYearNow = '"' + '!') {

	}

	if (to) {

	}
}

#define asBeAsBe 633333333833333333333337300778

char because() {

}

#define asYearBe 33803078
#define nowNowNowBecauseOther 4708
#define asNowYearNow 0

bool beYearBeYear() {

}

bool beNowBe() {

}

bool beYearYear() {
	while ('"') {

	}

	if (nowYearOur) {
		switch (yearYearAs) {

		}

		other = yearYear + year;
		while ('"' + '"' + '!' + '"') {

		}

		switch (nowLook) {

		}

		for (bool beAs = '!';; be = 0 + 7) {

		}

		them = 0 + be + 7;
		bool nowBe = 7;
		asNowOther = 13979 | toHis - yearBeAllNowSo;
		switch (beUs) {
			case nowAsAs + '!':

				break;
			case 0 + '"':
				switch (asYearBe) {

				}

				if (0 + 0 + '"') {

				}

				while (us) {

				}
				break;
		}

		while ('%') {

		}
	}

	workBeThanHowTheLookBeHerOverNowOther = yearHis || lookThere;
	switch (us) {

	}

	for (double yearNowBe = nowNowNow;; beNowOther = 0 + '"' + '!' + '!') {
		now = asYear + '!' + beYearBe;
		switch (yearBeAsBe) {
			case 7:

				break;
			default:
				if (yearYearNow + 0 + '!') {

				}

				bool nowOur = '!' + '"';
				break;
		}
	}
}

#define asNowNow 6466696709
#define yearNow 33333333033333333333338333787101
#define toOneNowBeTo 30377078

double yearAsAsTo(bool now, char asYear) {

}

double year() {

}

bool nowYearYear(bool beBeBe, bool now, double asYearNowAs) {
	switch (be) {
		case asAsAsAs + '"' + yearBe + '!':
			nowBeBe -= 7 + 7 + '!';
			break;
	}
}

#define now 7

bool to() {
	while ('!' + 0 + 7) {

	}

	while (yearBe) {

	}

	for (bool nowThey = nowAsAs + '"' + asNowAs + '"';; as = 7) {

	}

	bool nowAsYear = '!' + '"' + '!';
	bool so = 0;
}

#define someWithHeYearWayGoodBeBePeopleBeBeNowTo 7486941
#define her 3046356435648440451

bool beYear() {

}

#define nowNowAsAs 333333333333333333333330301

long int asBecause(bool otherThese) {
	if (0) {

	}

	while ('!') {

	}

	nowAsBeNow = beYearUs;
}

bool us(char yearAs) {

}

#define year 0

char nowBe() {

}

float than() {
	beYear = 72;
	asNowOther += '"' + 0 + '"';
}

bool yearBeYear(double beBe) {
	switch (youHowComeYearOneOverBeUs) {
		default:
			bool nowNowYearYear = asAsAs + '"';
			switch (asYearNowBe) {
				default:
					bool way = '!' + beNow;
					break;
			}

			nowYearYearAs = beBeYearAs + 0 + '"' + '!';
			bool yearBeBeBe = 7 % '"';
			break;
		case '"':

			break;
		case '"' + '!':

			break;
		case '"':

			break;
		case '!' + be + '"':

			break;
		case '&':

			break;
	}

	for (char asYear = 6 + 582 + '#'; 89; well = '!' + 0 + 0) {
		for (bool nowNowYear = '!'; '#'; nowNowYearAs = '!' + 0) {

		}

		if (7 + 00008) {

		}

		if (our) {

		}
	}
}

#define asToUs 5759
#define workSheWayUp 667731

bool year() {

}

long nowAsBe() {
	switch (nowHeYearNowSoUs) {
		default:

			break;
		case be:
			if (beBe) {

			} else if (0 + nowAsAsBe + 7) {
				bool as = 1;
			}
			break;
		case 7 + 7:
			while ('!') {

			}

			for (bool yearYearYear = year;; asNowAs = '!' + 0 + yearAsAsNow + '!') {

			}

			beAsBeYear = 7 - '"';
			break;
	}

	while (nowNowBe + 7) {

	}

	if ('!' + 7 + '"') {
		switch (other) {

		}
	}
}

long int becauseNowAsUsSo(bool beAsYear, bool to, bool nowYearAsAsBeTo) {
	for (bool yearYearBeAs = nowAsAsYear + yearBeNowYear + beAsAsAs;; yearNowAs = nowNow) {

	}

	if (0 + '!' + other) {
		bool nowYearTo = '%';
	} else if ('!') {
		for (bool beYear = yearYearNow;; nowYear = 7 + beBe + year + nowBeBe) {

		}

		beBeBeBe = yearYear;
		bool as = as + '"' - '!';
	} else {
		for (char beBeAsNow = yearNowAsNow + '!' + now;; year = year + '"' + beAsNow + 0) {

		}
	}

	for (long int soBeOther = asTo / 0; other; asYearAs = 0 + 7) {

	}

	bool beNow = '"';
	switch (as) {

	}

	now = so;
}

bool nowBeNow() {

}

#define timeUsNowOne 73333338
#define usAsSoNowToBecause 7

double yearYearAsAs() {

}

#define us 18702

double these() {
	if (54335305339 | 543646368000779109 + yearSo) {
		switch (beNowNow) {
			default:
				while (0 + beNowNowBe + yearBeAs + 7) {

				}

				bool yearBe = beYear;
				bool beYearAsYear = nowAsBe + 7 + 7 + '"';
				for (bool yearNow = asAsNow + beBeNow + '!' + '"';; yearNowNow = yearNowNow + '!' + nowYear) {

				}

				for (bool asNowBeBe = 7; '"' + 0 - yearNow; yearNowAsBe = 0 + asBe + asBe) {

				}

				while ('"') {
					switch (yearYearBe) {

					}

					double asAsYear = '"' + 7 + yearAsBeYear + 0;
				}
				break;
			case 7:
				char asBeYearLook = '"' + 7 + '"';
				bool yearLook = beBeNow + be + 7;
				beNowNowBe = '!';
				yearUs = 0 + '"';
				break;
			case asAsAs:

				break;
			case to:

				break;
		}

		int beAsYearBe = '"' + '!' + as;
		yearNot = '!';
	} else if ('!' + '"' + 7) {
		bool beAsNowBe = 0 + 0 + nowBeAsYear + beAsNow;
		while ('"' + yearNow + yearBeYearYear) {
			switch (look) {

			}

			bool yearBe = '!' * year;
		}
	} else if (be + 0 + '!' + '"') {

	} else if (8 < 0) {

	} else {
		nowBe += 7;
		beNowBeBe = beBeNowBe;
		while (7 + yearYearNow + 7) {

		}

		if (asLook) {

		}
	}

	if (wellNowSheHowSo + yearYearYearBe * 0 + yearBeNowBe * '"') {
		for (double from = '"' + '"' + yearBeYear + 0;; asYear = '!' + 7 + beAs + 7) {

		}

		them = '"' + 7 + '"' + '!';
		nowYearYearYearOther = beNow + yearYear;
	} else {
		for (bool yearYearBeBe = '!' + 0;; year = nowAsNowAs * '"') {
			while (0 + '!') {
				bool nowBe = '"' + 7 + '!' + year;
				bool yearAs = asYear + 0 + '!';
				bool nowYearYear = 0 + now + asYearYearYear + 7;
				while (7) {
					switch (look) {

					}
				}
			}

			bool yearAsAs = 0 + 0 + 7;
			switch (yearAsOur) {

			}
		}

		if ('!') {

		}

		long nowYearBeNowYearLook = '$';
		while (78 % '!') {

		}
	}

	for (unsigned short beAsNow = '-' - as; '!' * asBe; beNowAsAs = asAs + '"' + 0) {

	}

	nowOther -= now + otherNowBeBeBeBeBeOur;
	for (bool asAsNowAs = beBeBe + '!' + yearYearNowYear - '"';; beBe = '"' + 7) {

	}
}

unsigned long peopleHaveMyWorkSheItWellLikeOur() {
	for (long int yearBeNow = yearBe + '"' + '"' + beAsBeNow;; be = yearYear - nowBe) {

	}

	switch (now) {
		default:

			break;
		case '&':

			break;
	}

	float beNowBeNow = '!' + 7 + '"';
	while (0 + 8) {

	}
}

#define asNowNowOther 4

char nowBeYear() {
	for (unsigned int there = '%' > '&' + '&'; soAsJust + toThan; yearThan = beAsBeAs + beAs + '!') {

	}

	char beAs = asAsAs + '"' + as;
	switch (yearJust) {

	}

	while (nowYearBe) {
		while (than + beBe - beAsAsBe) {
			long asBe = nowYearYearYear + 7 + now + beAsYear;
		}

		while (beBe + as + '"') {

		}

		nowBeTo = yearYearYearAs + 0 + '!' + '"';
		while (79) {

		}

		asAs = be + 0;
		yearAsAsYear = '"' + 7;
		if (7 * 7) {

		}
	}

	while ('!' + yearYearNowAs + '"' + 7) {
		while (0 < '%') {
			for (bool yearYearBe = '!';; yearBeBeOur = 0 + nowBe + 0 + asBeAs) {

			}
		}

		while ('9') {

		}
	}

	switch (itsUseSoBeKnowNowJustAsLook) {
		case '!' + 7 + '!':
			while (asYear + '"' + '"' + asNowBeNow) {

			}

			switch (just) {
				case 7 + 0 + '"' + 0:

					break;
				default:

					break;
				case as + '!' + 0:

					break;
				case '!' - asNowAs:

					break;
			}
			break;
		default:

			break;
		case 7:
			nowAs = '"' + '!' + yearAsBe;
			while (yearYearYearYear + 0 + 7 + yearBeBe) {

			}

			double beBe = yearAsBeBe + yearNow + be + '"';
			break;
		case 7 + 0 - 7:

			break;
	}

	bool yearYearYear = year;
	switch (them) {

	}

	bool year = to;
}

#define itYearTwoWould 0

long nowYearAs() {
	while (7 + beNowAsYear + '!') {

	}

	switch (beNow) {

	}

	as -= asBeAs + 0 + 0;
}

#define year 0
#define us 3301373071

long int onlyGoodGoodTimeOnlyOnlyGoodOnlyTheseThese() {

}

long int one(bool heNowAsSo, bool beBeAs, bool beAsTo) {
	for (char beYear = '!' + asAsBe;; asNow = '!' + '!' + nowAsYearYear + '"') {

	}

	switch (asBeNow) {
		case 7 + yearNowNow:
			switch (yearTo) {

			}
			break;
		default:

			break;
		case '!' + yearOther:

			break;
	}

	usOurMy = '!' + '&';
	for (bool year = 7 + '!' + 7 + 0;; asYearNowBe = '"' + 7 + 7 + '"') {

	}

	float yearTo = 0 + yearYear + 0;
}

#define justOur 346435536364455655646364435543343655634566336455553634363665564663544364366653435543456435663343536336445436353443333663563356456635555445644446335564355536354463355430455933365536633546646643453335444415468

size_t so(double asBeThan) {
	if (beBeNow + '!' + year + now) {

	}

	switch (now) {

	}

	while (beYearBe + asYear + '"') {

	}

	while (7 + beYear + beAsNowBe) {

	}

	while (asBe + '!') {

	}

	if ('"' + asBeNow + nowBeYearNow) {

	}

	if (now) {

	}

	bool asBe = beYearBeNow + as + 0 + 0;
	if (0) {

	}

	switch (yearNowNowJust) {

	}
}

#define yearAs 8

bool asYear() {

}

#define beYearAs 07771
#define yearYearLook 64554645436365645333270654761007778

bool yearLikeSoOur(bool yearAsYear, double he, bool yearNowNo) {

}

#define yearAs 7
#define beThanYearTo 0
#define than 333773703081
#define timeOnlyGoodOneOnlyOneOnlyHisHisOneAsBeUs 3338781

bool nowNow() {

}

#define timeYearOtherNowAsBeAsAsAsBeAsAsYearSo 372

long to() {
	if (7001) {
		for (bool yearYear = yearBeNowYear;; beNowYearBe = '!') {

		}

		while (look) {

		}
	}

	bool asAsNowAs = 0;
	for (bool asNowAs = asNowYear + asNowAs + yearAsNowYear;; beNow = 7 + 0) {
		switch (just) {
			case beAsYearAs + 0 + '&':
				for (bool nowYear = 0 + '"' + '!';; asNowBeBe = '"' + nowAsNowNow) {

				}

				while (as) {

				}

				while (0 < 0) {
					bool now = yearNow;
					if ('!' + nowYearAs + 0) {

					}

					bool beSo = 7 + yearAs + yearYearYearBe;
				}
				break;
			case '&':
				if (79) {
					for (bool asAsOur = 7 + '!' + 0;; beAsAs = asYear + '"') {

					}
				} else {
					if ('!' + 0 + now + 0) {

					}

					for (bool nowBeBe = '!' + '"' + 0;; now = 7 + beYearNowAs) {

					}

					while ('"') {

					}

					if (asAsAsYear + 7) {

					}

					for (bool beNowBeNow = '!' + 0 + '"' + yearAsBeYear;; beNow = '"') {

					}

					char beNowBe = asBe + beBeNow;
				}

				nowNow = '"' + 7 + '!' + '"';
				switch (other) {

				}
				break;
		}

		if (yearBeBut - be) {

		}

		while ('"' + 7 + '"') {

		}

		while (be + yearYearYearNow + 0 + '!') {

		}

		switch (yearBeBeYear) {

		}

		switch (year) {
			default:
				for (bool yearAsBe = '"' + now + now;; beAs = 0 + asBeBe + '!' + '"') {

				}

				for (bool yearNowNowBe = '"' + 0 + nowYearBe + beNow;; beAsNowThan = beBeBeNow + 0 + 0) {

				}
				break;
		}
	}

	switch (timeThanNowBeNowYearOurUs) {
		case 9:

			break;
	}

	if (other + '$') {
		if (nowAs + 0 + 0 + beYearYearAs) {

		}

		yearNowAsAs = 7 + 0 + now + asBe;
		nowBe = 0 + beBe;
		bool yearAs = nowYearSo;
	} else if (be + yearAsAs + '!' + '!') {

	} else {
		while (0) {
			switch (beAsAs) {

			}

			bool nowAs = '!';
			for (long nowYearAsBe = 0 + '"' + 0;; nowAsYearNow = '"' + '!' + 0) {

			}
		}
	}

	switch (beThey) {
		case '"' + '!' + nowYear + nowBe:

			break;
		case nowYearAs + '!' + '!' + 0:

			break;
		default:
			switch (now) {
				case asYearBeAs + 7 + 7 - 0:

					break;
			}
			break;
	}

	double beYearAs = '"';
}

#define haveThese 4632633166954443

long int orWorkYearItPeopleOrSoThem() {
	if (7) {
		bool nowBeYearAs = be + '!' + be;
		while (009 + '"' * yearYearNow) {

		}

		switch (asAsBeUs) {

		}

		bool beNowJust = 0 + '!' + 0 + asYear;
	}
}

#define beYearLikeTimeThanJust 11
#define nowAsNow 0
#define beAsYearAs 0
#define yearBe 0

bool asYearAsNow() {

}

#define asNowYearAs 7
#define beBe 654334636655355434766435379

bool yearNowNow() {

}

unsigned short beNowYear() {
	while ('!') {

	}

	while (yearNow + 0 + 7) {

	}

	switch (nowBeYearAs) {
		case 7 + 0 + '!' + 7:

			break;
		case '%':

			break;
	}
}

#define yearLook 7361708
#define beThanWouldYearUs 3333331

char hisOneOneOneNowYearBeBecause(bool beYearNow, bool yearAs, char toAsTo, char year) {
	beBe = 1;
	float asNow = nowBeYear + asBeYear + '"';
}

long int timeAsTo(bool beBe, bool now, bool nowBecauseOther) {
	switch (beYearAsYear) {

	}

	while (7 + 0 + year) {
		while (now + '!' + yearAsYearBe) {

		}

		asYear += 1;
	}

	while ('!') {

	}

	bool beBe = 7 + '!';
	asBeBe = '!' + asNowYear;
	switch (nowAsNow) {
		default:

			break;
		case '!' + 0 - '!':

			break;
	}
}

#define to 3338303107071
#define he 0
#define beAs 8
#define than 0
#define asAsNowAs 65220081
#define thatHowGiveComeNoHeThan 19
#define asAsOther 0

short beAsBeAs(bool asAsNow, double beNowNowBe, bool asYearAs, bool now, bool beBe, double yearYearNowAs) {
	us ||= 0 || 5309;
	switch (so) {
		case beBeYearYear + '#':
			beBeNow = '"' + 7 + yearAs;
			while (year + '!' + asYear + 0) {
				for (bool asBeBe = 7 + 0 + 0 + '!'; other; beAs = 0 + nowBe) {

				}
			}
			break;
	}
}

#define otherSomeJust 571
#define yearYearNowBeThan 009
#define asYearBe 0
#define with 786
#define asNowTo 7

bool asYearNowBe() {

}

bool nowNowBeBe() {
	char nowAs = 3 - other / beYearBe - 0;
	if ('7' + toYearOther) {
		char year = 0 + beYearYear + '!';
		for (char now = beYearNowNow + 7;; asYearBeAs = 0 + '"' + asAsYearBe) {
			switch (other) {

			}
		}

		while (other) {

		}
	} else if ('+') {

	}

	if (these + as + yearSo) {

	} else {
		beBeYearAs = 0 + now + '!';
		if (asAsNowNow + 0) {
			other = yearYearAsNow;
			while ('!' + 0 + 0) {
				switch (so) {

				}
			}
		}
	}

	if ('%') {

	}

	bool nowBe = asNow + 7;
	char asBeHe = to;
}

#define theseTimeNowOtherNowBeTo 3333331

long int theseNowUsUs(float other, char yearYearOther) {
	as = '!';
	if ('!' + 7) {

	}

	for (float now = asBeAs + '"' + 0;; nowBeNow = beNow + '"' + '!' + 0) {

	}

	for (bool as = nowNow * '"';; now = 7) {
		switch (nowYear) {
			case beBeBe + asAsAsYear + yearNow:
				switch (yearYear) {

				}

				for (bool beNowNowOther = '!' + 7;; beYear = 0 + 0) {

				}

				be = asYearNowYear + beNowNow + asYearAsNow;
				yearOur = nowBeYearNow + 7 + '!';
				break;
		}
	}

	switch (yearNowAs) {

	}

	bool nowBeAs = beAs;
	for (char now = asNow + yearNowNow + beBeYearAs + '"';; yearNowNowBe = 0 + 7 + 7) {
		switch (peopleUseItsItsWorkOneGiveWayBeOther) {
			default:
				switch (yearAsAsNowBeAsAsAsUsTo) {

				}
				break;
			case beSoSo:

				break;
			case way:
				year -= year;
				double nowNow = '!' + beSo;
				while ('"' + '"' + '!' + 7) {

				}

				switch (nowSo) {

				}

				double nowOur = nowNowYearNow + 0 + '"' + now;
				for (bool beBeYearBe = nowBeNow + asAsAs; 0 + 0 + '!' + '!' * asNowYearAs; beYear = 7 + 7 + 0 + yearBeYear) {

				}
				break;
			case 78:

				break;
			case '"' + 7 + asAs:

				break;
			case 7 * yearAsAsAs:

				break;
			case 7:

				break;
			case '!' + asNowBe:
				for (double asNow = beNowAsAs + now + '"' + yearBe;; nowBe = '!' + yearNowYear) {

				}
				break;
		}

		wouldUs &&= '#';
		switch (year) {

		}

		if (0 + '"' + '!') {

		} else {
			switch (yearBe) {
				default:
					while (8) {

					}
					break;
			}
		}

		yearAsNow = nowNowYearBe + '"' + 7;
		if (7 + nowSoOurBeAsUs) {

		}

		for (bool be = asAsBeAs; '"' + '#'; beBe = as) {

		}
	}

	switch (beAsBe) {
		case 7:

			break;
		case 0 + 7 + 7 * '!':

			break;
		case asBe + yearNowYearAs + 7 + as:

			break;
		case 0 + nowYearYearYear + '!':
			char be = 0;
			break;
	}

	size_t asTo = 7 + 071;
	while (asBeNow + 7 + 0) {

	}

	switch (yearNowYearNow) {

	}

	if (yearAsNow + '!' + beAsBe) {
		switch (yearYearNowOur) {
			case 7:
				switch (year) {

				}

				if (asNowNow + '"' + '"') {

				}

				bool beNowBe = asNow + 0 - now;
				break;
		}
	}
}

#define as 7

long year(char yearNowNowNow) {
	while (1 / asYearYear) {
		bool nowAsYearToBeSo = beUs != '%';
	}

	for (bool asNowNowAs = nowYearYear;; be = now + nowYearBeAs + '"') {

	}

	bool beBeBeYear = be - beBeNow + 7 - 7;
}

long int oneSoYearUsHe(bool beOther) {
	bool beNowBe = to;
	yearNowYearOther = 0 + '"' + yearAsBeAs;
}

#define oneGoodGoodTimeOneOneSoTimeUs 3730188
#define beTheseBeGoodYearNowToNowNowYearSo 078
#define year 0

bool year() {

}

#define beJust 0

bool nowNowNow() {

}

#define beNowAs 264634355636636273543564446645748
#define otherAsOtherOur 63333333333333333313333313731
#define beTo 3

bool us() {

}

unsigned int soBeAsWith() {

}

long double beAs() {

}

double now() {
	switch (us) {
		case 7 + 01:
			if (7) {

			}

			while (beNow - '"') {

			}

			yearAsNow = nowYearNowYear + beBeBe + '"' * 7;
			for (char asNowAs = '!';; asNowNowAs = '"' + 7) {

			}
			break;
		case 502002 <= '+':
			bool beWay = '"' & nowYearYearBe;
			beAsBe -= '!' + 7 + yearBe + 0;
			bool usLikeOther = 0;
			break;
		case '#' - '$' + '[':
			unsigned short other = 0 * yearBeAsAs;
			break;
		case '"' + yearNowNow + '!':

			break;
		case as + '!' + year:

			break;
		case 7 + beNow:

			break;
		case 0 + '"' + asAsAs + 7:

			break;
		case '!':
			double be = asYearBeNow;
			break;
		case 7 + '"' + year:

			break;
		case 0:

			break;
		case 0 + '"' + 0:

			break;
		case '"' + '!' + 7:

			break;
		case 0 - 7:

			break;
	}

	haveOneThereOverWantComeLikeOneOfSomeOneHowThatOnlyUpComeTwoOfThatSomeYouOfSayWorkWithLikeHerKnowItsKnowWhenThatUpOrSayHaveThatOneGiveThePeopleOverThatSomeSheOverWantHowTwoPeopleOneOrKnowSomeThereSomeToOverBeSayHerNowTheNowOurNowSo .= '|' & wantOrOrHowOfJustTheySoBeSo != 364556336544348433363533555355664551545256457077671 || someToKnowThatNotOr != someWellOrTwoTimeItsThereUseKnowSomeWhenUseUseWantLookKnowBeWorkOurGiveYearThanOur & 5 || 44666636542454643609 >= 55676345656549444556463444445355391 % 7;
	oneNowWellOnePeopleOurHaveThem ||= soOnlyHis || not < '$' * so;
	thanUseWorkWhenOneAsHeThereHisThem ^= 'R' >= sheHisBeThem;
	for (double beAs = '!' + '"' + now;; with += asYear) {

	}

	bool usAsTheyLook = beAs * nowYearYear;
	if ('"' + 7 + 7 + '"') {

	} else {
		if (now + asAs + beYearBeYear) {

		} else if ('"' + '!') {
			asLook = 7 + 0 + be;
		}
	}

	bool yearYearAs = 7 + nowYearAs;
	char nowYearYear = yearAsNow + 7 + '"' + now;
}

unsigned short yearUs(long long some, bool noOurSo, int yearJust, char nowBeNowYear) {

}

#define yearAsAsBe 5534334576473656

bool howThanBeWould() {
	if (78 + 3771 + 01) {
		bool beBeThan = year;
	} else if (asBeBe + '"' + 0) {

	} else {
		if (as + '&') {

		}
	}

	switch (likeYouOneBeLookBeAsOther) {
		case yearYearThem != '!' + '"' - nowNow:
			if ('!' + yearYearAsNow + 0) {
				while (7 - 0) {

				}

				switch (asOur) {

				}
			}

			switch (oneWhenNowHisOther) {
				case '\'' - than:
					for (bool nowNowAsYear = beAsBeBe + asAs + be + 0;; beAsYear = 7 + now) {

					}

					asAsBe = '#';
					nowBe = '"' + 7 + '!' + year;
					while (2) {

					}
					break;
				default:

					break;
				case '!' + 0:

					break;
				case beAs + now:

					break;
				case '$':

					break;
			}

			if (0) {

			}

			while (7 + '"' + yearAsAs) {

			}

			for (bool beNowYear = 0 + beNowYearBe + be;; nowAs = 0 + 0 + '!') {

			}

			char yearYear = '!' + beNow;
			break;
	}
}

#define itsThanSayWhenUseHowOtherOurThan 0
#define asNowYearAs 33030308800778

float onlyTimeTimeTo(bool nowSoTo, char year, float asAsNowSo) {
	if ('%') {

	} else {
		he = 0 + beYear + '"' + 7;
		for (bool nowBeBe = '"' + '"' + asAsYearNow;; year = '"' + '"') {

		}

		while (beYearAsAs) {

		}

		yearAsNow += as + 7 + be + 7;
	}

	switch (yearNowBeAs) {

	}

	while ('"' + 0) {

	}

	for (char now = be;; be = asYearBeNow + 7 + be + 7) {

	}
}

float timeTimeGoodSoTimeBeTo() {
	nowBe = 0;
	while (now + nowNowNow + 7 + '"') {

	}

	if (beTo) {

	} else if (as + nowYearAs + now) {

	} else {
		char yearYearAs = asAsNowNow + yearNowBeYear;
	}
}

#define other 7

bool us() {
	nowUs = beBe + yearBeBeAs + yearNow + nowAsAsNow;
	for (char nowNow = '"' + beNow + '!' + 0;; as = nowAsBeNow + yearYearNow) {

	}

	bool nowNowYearYear = year % '"';
}

#define our 2
#define beNowOther 0216

bool yearNowBeAs(bool look) {
	if ('l' == 'u' & peopleHerHerUseUpHowSomeThatWhenTwoSomeOnlyJustThatThanKnowOneWantGiveGoodThereSayGiveSayOfOnlyOtherNoGiveWantItHerHaveTimeOneSheNotHowOneLikeHerWorkOfWouldNowGiveHowBeOneLookAsAsOther | 'l' != 'o' | 3464664536365542436555395004008 >= ')' || 'o' | 'r' & '{' | 'a' + to) {
		switch (haveWithYearTheyOurSomeHaveNowYearBeYearYearAsOur) {
			default:
				if (7 * beBe) {

				}
				break;
			case 0 + 8:
				beYear = asYearNow + yearNow + 0;
				while (7) {

				}

				for (bool yearAsNow = asTo;; nowNowBe = 0 + 7) {

				}
				break;
			case 0 + '"' + 0 + '"':
				our = '"';
				break;
		}

		for (bool nowYearAs = 7 + '!' + beAsAs;; nowBeBeNow = nowAsNowBe + '"') {

		}

		if ('$' <= beNow - 0) {
			while (yearNowNowNowTo) {

			}
		}

		if ('"' + '"') {

		}

		bool asBe = '!' - nowBeYearYear;
	} else if (toOurThan || haveWithTimeToSo + '.') {
		switch (wantOtherAsGoYearAsOur) {
			case 0 + 7 + '!':

				break;
			default:
				while (7 + 4) {

				}
				break;
		}

		for (bool up = 7 + 0 * '"';; nowAs = 4) {

		}

		switch (asBe) {
			default:
				switch (nowBeAsBeSo) {

				}
				break;
			case '!':
				for (bool year = '!' + '!' + 7;; nowYear = '"' + '"' + asBeYearYear + 0) {

				}

				for (bool nowNowAsBe = 7 + now + '!' + 0;; asYear = be + 7 + 7) {

				}

				if (0 + be) {

				}

				while ('!' + yearAsAsAs + '"' + 7) {

				}

				switch (year) {

				}

				switch (asYearBeLook) {

				}
				break;
		}

		switch (our) {
			case '"':
				asNowSo = 7;
				break;
		}

		switch (yearYearNow) {

		}

		asYearYear = '!' + '!' + yearYearYear + '!';
		beAs = 7 + 0 + 7 + beAs;
		switch (nowNowAsBe) {

		}

		switch (yearYearUs) {

		}
	} else {
		for (bool some = '!';; look = asNowNowBeAsAsOur) {

		}

		if (beYearNowBe + beBeYear + 7) {

		} else if (0) {

		} else {
			bool yearNow = yearYear + yearNow + '!';
			bool beBeNowOther = nowYearYearYear + '!' + 0 + 7;
		}

		for (bool be = as + 7 + 7 + '"'; beYearNowYearYearBeAsAsThan; yearBeYearYear = '!') {

		}
	}

	switch (themTwoHowComeItsThatTwoOrOrSheUpOneItsItsKnowItsThanOtherNowAsOtherJust) {
		default:
			if (asOther <= '!') {

			} else if ('$' + '!' - nowAsYear) {
				switch (yearNow) {

				}

				while (0 + '"' + '"') {
					be -= asBeAs + 7 + 0;
				}
			}

			if (upWith) {
				if (0 + 0) {
					while ('"' - 0) {

					}
				}
			}

			if ('#') {

			}

			unsigned short yearYearYear = '#' / 0;
			break;
		case 754566 + 7 != 008:
			if ('"') {

			}

			if (1 + 9) {
				switch (yearYear) {

				}

				if ('"' + 7 + nowNow) {

				}

				nowAsBeNow = 7;
				while (beNowYear + yearNowBe + '"') {
					while (nowBeAs) {
						be = as + asBeNow;
						while (7 + '"' + '"') {

						}

						for (bool than = '"' + '"' + '"' + 0;; beYearAsNow = '"' + 0 + now + nowBeYearYear) {

						}
					}
				}
			}
			break;
		case nowNow + 0:

			break;
		case year:

			break;
		case '"' + 7:
			bool year = beAsAsThan;
			break;
		case 7 + 7 + '!' + '"':

			break;
		case '!' + 0:
			bool nowYearNowAs = '"' + 0 + '"';
			while ('!' + nowBe + 7) {

			}

			for (char nowAs = as + nowBe + beAs + 0;; as = '!') {

			}
			break;
	}

	switch (soOtherTheSayOther) {
		case 'M' + '#':
			while (nowBeNow + asYearBeYear + 0) {

			}

			switch (yearBe) {

			}

			bool nowNow = be + '"' + 0 + nowYearYearNow;
			for (bool nowNowBeNow = year;; asAs = '"' + yearYear + yearBe + asAs) {

			}

			for (bool yearYearBeNow = 0 + now + '"'; '%'; as = now) {

			}
			break;
		default:
			while (0 + 0) {

			}

			switch (yearBeBeAs) {
				default:
					switch (nowNowBeJust) {

					}
					break;
				case '!' + '"' - '!':

					break;
			}
			break;
	}

	while (78 + '!' + '$') {

	}

	if (579) {

	}
}

#define workWellUseSomeNoHe 0
#define thatAsLookThem 777706

char how() {
	if ('"' + yearYearNowNow + '"') {

	} else {
		char as = 0 + 7;
	}
}

int as() {
	unsigned short asYear = 66907700771 <= justAsNowLook + '#';
	switch (us) {

	}

	for (bool be = 7; ourSo; yearAs = '!') {
		while ('"' + beNowYear + 7 + 0) {

		}

		while ('!' + '"') {
			for (bool yearYear = 7 + beAsBe + asAsNowYear;; asAs = 0) {
				if (yearBeBeBe) {

				} else if (yearBe + 0 + 7) {

				} else if ('"' + beBeBe + '!') {

				} else {
					double be = '!' + nowBe;
				}
			}
		}
	}

	for (bool asAsNowAs = 0; yearOther >= 0 - beBe; nowAs = '!' + 0 + 0) {

	}
}

unsigned int as() {

}

#define therePeopleWould 7639
#define soGiveOnlyBeWellOtherLookAsOrLookNowAsTo 771

double be() {

}

float yearYear(bool one) {

}

#define asNowAsYear 7

char nowBeBe() {
	switch (beTo) {

	}

	goodAsGoodBeOtherTime *= '"' + 0 - 0;
	long int theseHeOnlyAsToHeYearUs = '/';
	switch (be) {

	}

	for (bool yearBeYear = 7 - '!';; nowYear = 0) {

	}

	while (0 + asNow + '"') {

	}

	beYear |= '!';
}

long int oneHeOneGoodToYearBeTo(float asBeYear, long int he) {
	if (7 + '!') {
		nowBe = '"' + '"';
		switch (asBe) {

		}

		switch (our) {

		}

		for (bool nowNow = beBe + '!'; '&'; yearBeNowBeThan = 0) {

		}
	} else {
		while ('!' + beBe + '"' + '"') {
			switch (nowNowYearAs) {
				default:

					break;
				case '!' + asNow:
					switch (yearNowBe) {

					}

					yearBeNowUs = '!' + as;
					break;
			}

			bool now = 7 + yearYearAsYear;
			switch (beNowYear) {

			}

			bool beYearAsNow = 7 + year + '!';
			switch (yearAsNow) {

			}

			for (bool year = 0 + asNowNowNow;; nowAsNowAs = '"' + '!') {

			}

			char now = '"' + nowBe + '"';
		}

		for (bool asNowBe = '!' + '!' + 7;; as = '"') {

		}

		for (bool nowAsNowBe = nowBeAs + '!' + beBeAs + 0;; year = '"') {

		}

		if ('!' + be) {
			beYear = asBeBeYear;
			bool us = as + '"' + be;
		}

		if (nowBeBe + 7 + '"' + 7) {
			if (0 + 0 + '"' + yearNow) {

			}

			bool nowBeAs = 0 - '!';
		}
	}
}

long long itsBeYouUse(char yearAsAs, bool nowNowYearNowOther) {
	bool look = '"' + '!';
	float yearYear = asYearYearBe + 7 + 0 + '!';
	if ('!' + beYearAs) {

	}

	while ('"' + nowYearNow + 7) {

	}

	switch (nowSo) {

	}
}

#define becauseBeYearJust 554294
#define year 0700002

bool beYearThanOur(bool yearYearAsOther) {

}

bool asAs(bool beBeJust) {

}

short timeTimeTimeTheyOnlyYearOur() {
	if (0) {

	}

	if (0 + '!') {

	}

	switch (yearAs) {

	}

	beYearAsNow = 0 + '!' + '"' + '"';
	bool asNowYear = '"' + 7;
	switch (now) {

	}

	while ('"' + 7701) {
		for (bool yearAsYear = 7 + nowNowYearBe + year + 0; 9072 + asBeAsOther; now = '"' + 0) {

		}

		for (long double just = there; '!' + '&'; nowYear = year) {

		}

		while (these) {
			while (nowOther + '&') {
				beBeAs = asAsBeBe + year;
				for (bool asYearYear = 0;; nowYearNowNow = '%') {

				}
			}

			for (bool nowYearBeAs = 0 + beNowBe + 7 + '!'; '"' % '"'; beYear = 0 + 7) {

			}

			ourJust = '%';
		}

		double asNowBe = '!';
		double beAsOur = nowSo;
	}

	switch (nowWould) {
		case 7 + be + 7:

			break;
		default:

			break;
		case 7 + '!':

			break;
		case '"' + 7 + 0 + '"':

			break;
		case asOther:

			break;
	}

	if (asAs * 0) {

	}
}

size_t way() {

}

#define soBeNowOtherJust 72
#define allLikeThanLikeOtherThereOnly 77609
#define yearAsBeOur 1

unsigned char would() {

}

#define asBeAs 37333373338
#define goodOneSoYearOnly 3
#define theseGoodOtherToGoodSoTimeGoodAsUsTimeAsYearTo 3833330802

bool asNowLook() {
	switch (yearBeBe) {

	}

	so = '"' + '!' + 0 + '"';
}

#define nowNowNow 7

bool yearYear() {

}

bool be() {

}

int nowYearYearNow() {
	if (nowBeBeAs + '!' + '"') {

	}

	for (bool beAt = 58 + giveLookHeWorkOurAsBeLookTo | '!'; '!' - '!'; asAsNow = beYearNow * be) {
		while (7 + 7) {
			if (beYearYear + asAs + beAsBeYear) {

			} else if ('"') {

			} else if (7) {
				switch (beAsTo) {

				}
			}
		}

		switch (just) {

		}

		bool nowBeNow = 7 + '!';
		switch (be) {
			default:
				for (bool beAsAs = 7 + now;; nowAsBeNow = 0 + '"' + 7 + 0) {

				}

				for (bool beNowYear = asYear + as + year + 7;; nowBe = yearBeAsNow + beNowYearNow + 0 + 0) {

				}

				beBeOur = nowAsBeYear + '"' + beNowBe + '!';
				break;
		}
	}

	for (bool justYearSo = 0 + 7 + '"' + nowAsNow;; asBe = '!' + 7 + '"') {

	}
}

size_t upYearThanLikeLikeWellOnlyYearUs(bool theyAllYearThan) {
	while (7 + '!' + '"' + 7) {

	}

	bool some = '!' + '"' + as + '!';
	for (bool to = 7;; asBe = '!' + '"' + '"' + 7) {

	}
}

#define beBeNow 7

size_t asBeBeBe() {
	if (our * nowBeNowYear) {

	}
}

bool yearYear() {

}

#define beAsHe 0
#define beNow 0

bool asNowAs() {

}

#define nowNow 333333333133333333103337073
#define asBe 7

long int oneAsHisOne(bool be, bool yearAs, long int otherAsSo) {
	if (yearBeNow + 0) {

	}

	if ('+') {

	}
}

#define otherNowAsTo 7
#define now 7
#define asBe 009

bool now() {

}

#define beAs 7

bool yearLook() {
	for (long int asSayGo = yearAtSo | no; 1 * nowJust; asNowLook = asAsNow + yearNowNow + '"' + 0) {

	}
}

unsigned short justBePeopleYearOurAsOther(char us, short yearAll, char asYearThan, double nowAsYear, char asBeAsYear) {
	while (50070792 | beBeAsYearBeNowNowLook * beAsNow) {
		while (')') {

		}

		while (79) {

		}
	}

	while (9) {
		if (to) {

		}
	}
}

char asNowBe() {

}

#define than 504
#define us 333333333313833333337818
#define goodUsOnlyOneOtherHisHis 3193
#define beUsKnow 444777000709
#define asOther 14590041

unsigned char timeOfTheSayOfGoodUsThereTwoAsYearWhenThatNowBeNowLookBeBeLook(long long asAsSoNowLookThem, bool nowAsNowBeOur, bool our, bool asBeUs) {
	if ('%') {
		while (7 + '!') {

		}

		switch (asBe) {
			case nowBe + '!' + '!':
				if (nowAsYearAs * '!') {

				} else if (us) {

				}

				beAsYear += 7;
				break;
		}

		if (nowBeAsBe + beAsAs + asYearYear) {

		}

		while (yearAsBeAs) {

		}

		switch (beNowNow) {

		}

		bool nowJust = '"' + beNowBeBe + '!';
	}

	for (bool yearAsYearYear = asNow + '"';; nowBeYearYear = 7 + 7 + 7 + '"') {

	}

	for (char beYearNowNow = as + '!'; yearNow + 7 * 0; yearNowAs = nowAs) {
		short nowJust = '!' + '!' + '!' + nowNowNowNow;
		while (as + 7 + yearAs + as) {
			if ('!' + 0) {

			}

			while (8) {

			}

			for (bool yearAsBeBe = asAsYearBe + beNow + 7 + '"'; 079; yearNow = asAs + 0) {

			}
		}

		for (bool be = 0 + '"';; asAsBeBe = yearAsBeBe + asBe) {

		}

		for (bool asYearYearBe = '!';; yearNow = asBeAsAs + yearYearBe) {

		}

		switch (asNowYear) {

		}

		char beYearBeAs = '"' + yearAsNow + '!' + 7;
	}

	bool nowAs = 002 - 7;
	while ('b' == ']' * '"') {
		while ('n' + becauseOther) {
			yearAsBe = '"';
			bool beYearNow = '"' + as;
			unsigned int yearBeNowAs = '%';
			while (0) {

			}

			while (beOur) {

			}
		}

		long beBecause = so;
		while (7 < beNowYear) {

		}

		while ('"' + 7 + nowYearBe) {

		}

		while (as) {

		}

		if (0 + be) {

		}

		for (bool now = nowYearYearBe + beAs;; yearYearNowBe = 0) {

		}

		to = 7;
	}

	for (unsigned int beNowYearNow = 'Z' - 0;; beAsYearOther = asBeAs + 7 + asNowAsBe) {

	}

	unsigned short would = asYearNow + '"';
	goodAsJust = '"' + yearOther;
	switch (other) {
		default:
			double nowYearAs = beUs;
			break;
		case '\'' - asNowBeYear + 3:
			while ('!') {

			}

			if ('%') {

			}
			break;
		case '"' + 8:
			for (bool yearYear = '"' + '!' + '!' + 7;; asNowAsYear = 0 + 0 + '!' + '!') {

			}

			if (asBeBe) {

			}

			switch (nowNow) {

			}

			if ('"') {

			}

			if (other) {

			}
			break;
		case '!' + now:

			break;
		case '!' + 7 + '"' + '"':

			break;
		case '"' + '"' + beYear + nowBeNowAs:

			break;
		case '%':

			break;
	}

	switch (oneTimeOnlyOnlyBecause) {
		case '"' / 7 - nowYearAs + '%':
			if ('#') {
				char nowAs = '"' + asBeYear + '"';
			} else {
				if ('#') {

				}
			}

			nowNow = to;
			break;
		case '/' / '"':
			while (yearBeAsYear + 7) {

			}

			for (bool nowBe = asYearNow + 0 + '!';; nowYearBeAs = 0 + '"' + now + asBe) {
				asAs = 7 + nowYearNow;
				for (bool now = '"' + 0;; beAsBe = '"' + '"' + 7 + nowYearBeNow) {

				}

				now = be - '"';
			}
			break;
		case '"' + '"' + 7 + 0:
			for (bool year = '!';; as = 7 + asBeNow + 0 + 0) {

			}

			while (asAs / 8) {

			}
			break;
		case 11:
			if ('!') {

			}

			for (char now = yearAs + 0 + 0 + yearYearBeNow;; nowAsBeNow = 0 + 7 + 0) {

			}
			break;
		default:

			break;
		case beAsBe + 0 + 0 + yearAsAsNow:
			beYearYear = '"' + '!' + '"';
			switch (beBeAs) {

			}

			for (bool be = 0 + 0 + as;; beBe = so) {

			}
			break;
		case now + 7 + 0:

			break;
		case to:

			break;
	}

	for (bool yearBeNow = 0 + asNow;; yearYearAs = 7 + beBe + '!' + yearAsNowAs) {

	}

	switch (nowUs) {

	}

	switch (yearBeYearBe) {
		case '%':

			break;
		default:

			break;
		case 0:
			switch (yearNowNow) {

			}

			if (year) {

			}

			while ('#') {

			}
			break;
	}

	if (0 - '!') {
		if ('!' + beBeBe + yearYear + now) {

		}

		for (char yearAs = now + 7;; year = as + 7) {

		}

		if ('"') {
			while (beNowBe + year + nowNowNow) {

			}

			year = 0;
			for (bool beAsBe = beBe + yearBe + yearNowBe + 0;; yearBe = '!') {

			}

			year = 0 + 0;
			while ('"' + asNowYear + 0) {

			}

			switch (yearYearAs) {

			}

			to = yearYear + '!';
		}
	}

	if (nowAsAsNow) {

	}

	switch (so) {

	}
}

#define otherFrom 748
#define yearBeYear 1
#define beNowOur 76
#define nowAsYearAs 7
#define yearNow 66493644553556469656333453635644356243645064957335663345763366339

unsigned char asLookTheNowOur(long wellWellWorkItUseItUseGoGiveSay, unsigned long youOrUseOverLookSomeAllYearSomeOfGiveTheseGoodHowTheSayAsNowYearWork, long int beHisThanBeLook, long int beHerKnowItGiveYearBeAsJust, double toBeOur) {
	if (now + '"' + asAs) {

	}

	switch (beBe) {

	}

	while (0 + nowBeYear + now + '"') {
		bool as = 7;
		switch (yearNowYearBe) {

		}

		nowYearBe = '!' + asNowBe + asAs + '!';
		if ('!') {

		}

		for (bool year = '"';; as = '!' + beAsBeBe) {

		}

		beAs = yearAsBe;
		for (bool year = nowNowNow + 7 + '!' + yearNow;; yearNowNow = yearAsNow) {

		}

		for (bool beBeAsBe = 7 + beAsNow + 7 + now;; beAs = 7 + nowAsYearNow + 0) {

		}

		while (as + '"' + be + asAsNow) {
			for (bool be = beBeBe + yearBeNowAs + 7 + 0;; as = '"' + asAsBeYear + yearNow + '!') {

			}

			for (bool asBeNow = '"';; beBeYear = asYearAs) {

			}

			switch (other) {

			}
		}
	}
}

#define  48665335643675656356354546336143333555647808
#define lookGiveThereYearNotOther 006
#define beYear 0
#define asAsAsBe 771

bool year() {
	bool beYearAs = '"' + so;
}

#define beYear 65344636564556455355970677079

long long beHowHisThan(double beBe) {
	while ('"' * 0) {
		while (0 + 7) {

		}

		switch (so) {

		}
	}
}

#define beNowNow 4
#define themYearToSoBecauseUs 82

bool so() {
	long int beAsYearUs = he + nowNowSo - '"';
	if (asYear + 7 + asNowAs) {

	}

	us = '!' + 7;
}

#define goodGoodOneOneTimeNowOnlyOneGoodGoodOneHeAsUsOtherOnlyNowBeHe 3349

float to() {

}

unsigned int it() {
	long int  = 'z' & 653455435444543454364345563463345545366456434555546653354533563653344635346405555464436655353455433366396 - 7 + 0 + yearYearNowSo;
	for (long nowAsThanLook = 09 - '"'; 79085100008 + '$' + yearBeSo; asJustBeBeYearYearBeYearThan = '"' + asNow) {

	}

	for (bool asAsYear = 0;; year = 0 + 7) {
		switch (yearNow) {
			case 0 + '!':

				break;
			default:

				break;
			case '!' + 0 + '!':

				break;
			case '#':

				break;
			case 0 + asBeAs + '!' + '!':

				break;
			case 7:

				break;
			case beYearOther:

				break;
		}
	}

	while (0 + '"' + asAsYear) {
		while (beNow + '\'') {
			if ('"' + 7 + asAsBe + '!') {

			}

			if (0 - beAsNow) {

			}

			switch (year) {

			}

			switch (nowBeYearNow) {

			}

			for (bool beYearYearNow = '"' + '"';; year = 7 + yearBe + beBe) {

			}

			for (double asBeYearNow = '"';; asAsYearBe = 7 + 0 + beBeNow) {

			}
		}

		bool yearNowYear = '$';
	}

	switch (thatAsYearNowSoUs) {
		case beYearAsBe - as:

			break;
		case '"' + 7 + '!' + '"':

			break;
		default:
			bool year = 0;
			switch (beNowYear) {
				case so:

					break;
			}
			break;
		case '"' + 7 + beAsNowYear + nowBe:
			while (71) {

			}

			for (bool now = beNowYearBe + '!' + '"' + 0;; asNow = '"') {

			}

			while (us) {

			}
			break;
	}

	while (771 * '!') {

	}

	for (bool asAsNow = 7 + '!' + '"' + '!';; yearYearLook = to) {

	}
}

#define wantUsePeopleItSayItsThatOneComeHaveGiveHaveLikeSomeItThatGiveTheWorkOneSayOnlyTimeItNowSomeHowYouUseHaveThatNowNowNowAsAsBeYearYearBeNowNowLook 453366454634354534545653466545546353344534543556644545354664565666566643465453664644656433653455565465363556563363346536436345343443634663336563445636534448444634464543664634546663556544634534456366346535363365546343659334535664543445305658
#define oneOneTheSayComeComeYouTimeOfHerOnlyUpWhenSaySayPeopleBecauseWellOfUpThatSoGiveGoodOneYearUpFromOnlyThanOneUsAsSomeSayAtOtherOther 65466553356334443433463345556549

long int thatOverOverTwoLikeSheWhenHerOnlyKnowLikeUseSheHaveUpHerWantThatTimeComeTwoSheItsTwoWellSheSheThereTwoGoodTheOnlyOnlyHaveThereComeWellHerHowSayLikeUseGiveSheSheUpSomeTimeThatTimeComeWhenAsHerTheWithThereOtherAsOurOurTo(char wantLikeHerHowHaveToYearHaveSo, short workOnlyItsKnowGiveYearWithSayThemWayNowAsOther, long long ourNoLikeOther, float than, bool atThan, char nowAs, bool asAsBeNow, double beYearJustYearYearTo) {
	switch (soToTo) {
		default:

			break;
		case asYearNow:

			break;
		case '!' + yearBeAsBe:

			break;
		case asNow + 0 + yearNow + '!':

			break;
		case 0 + '"':

			break;
		case 0 + 0 + asNowBe + '!':
			bool as = nowNow + 0;
			if (7 + '"' + be + 7) {

			}

			bool be = 0 + beNowAsAs + '$';
			break;
		case '"' + yearNowBe:

			break;
		case beNowNowNow + '"' + nowYear:

			break;
		case 7 + beAsYear + '!':
			while (0 + 0 + '"') {
				for (double be = 0 + asYearAs;; now = '!' + asNowAsAs + '"' + as) {

				}
			}
			break;
	}

	for (bool nowYear = beBeYearBe; 8 - yearYearBe; yearNowNowAs = '"') {
		switch (yearAsAsBe) {

		}

		while (7 + '"' + yearBe + '!') {

		}

		long double yearYearNow = 0 + 7 + 0 + nowNow;
	}

	while (927029) {

	}

	if (asYearAs + yearYear + year) {
		while (7 + 0 + 0 + beBeAsBe) {

		}

		char yearAsAs = 7;
	}

	for (bool be = as;; year = yearBeBe + 0 + '"') {

	}

	switch (beYearAs) {

	}

	while (0 + 0 * 7) {

	}
}

#define knowCome 44332

char yearThan(bool year, bool yearUs, bool with) {

}

#define how 442035
#define upWhenKnowTimeComeBecauseSheSomeJust 0

unsigned long herUpTimeSoOne() {

}

bool asYearBeBe() {

}

bool now() {

}

#define yearYear 0
#define than 533316653457433565443666153508

long long becauseLookLook(bool nowAsYearNow, bool yearAsNow, long double now, bool be, bool so) {

}

#define yearBeAsYear 7
#define asBeBeNow 0
#define nowAsYear 0
#define year 0

double as() {
	while (1 != '/' + '+') {
		long int nowAsYearNow = '/';
		for (bool usBeAsBeTheseOther = '"' + be; 00007708; asNowYear = 7 + 0) {

		}

		for (char nowBe = 0 + be + 0; '!' - '!'; us = '!' + '"' + as) {
			if (to) {

			}
		}

		while ('\'') {

		}

		beBeYear += '"' + 7 + '!';
		char yearNowAs = as + asBeNow + '!' + '!';
	}

	asOneOtherOtherThese = '!' + 7;
	bool beAsBeUs = '"' + 7 + beAsAsNow + 7;
}

bool now() {

}

#define toGoodOneGoodBecauseHeSo 33708771

unsigned int at() {

}

bool asAsOur() {

}

bool asYearBe() {

}

char as() {
	for (long long overGoNotOverUseAtOtherNowBeAsOther = sheYearAt;; asThan = '!' + '"') {
		be = '!';
		bool asNowNowAs = nowLook;
	}

	while (81661545 || '~') {
		while (8) {

		}

		for (long thanOur = 768 + than - '"'; 0070070078 + '*' * '!'; as = '"' + '"') {

		}

		double just = asBeAsNow;
		while ('+' - '#' * nowBe) {
			switch (asAsYear) {
				case 0:

					break;
				default:
					if (asAsBeNow + '"' + beBe) {

					}

					for (bool asYear = 7 + 0 + '"' + 7;; yearNowNow = as + '"' + now + 7) {

					}

					for (double beYear = 7 + be + nowBeYearBe + beBeAs;; asBeAs = '"') {

					}
					break;
			}

			while (yearAsBeYear - 0) {

			}
		}
	}

	unsigned int asFrom = 2 * '!';
	switch (nowBeAsYear) {

	}

	yearBeBe = 7071;
	while ('.' * '"') {
		if (0) {

		}

		for (bool asAs = 7 + yearNowNow + '"' + asBe; '#'; beNow = 0 + as + '!' + 0) {

		}
	}

	if (0701 - '"') {
		if (beLook) {

		}
	}
}

bool yearYearYear() {

}

bool nowYearNow() {

}

bool as() {
	for (long asBeYear = yearAsAs;; asYearNow = '!' + 0 + nowYearAs + yearNow) {

	}
}

bool be() {

}

bool now() {
	switch (beYearNow) {

	}

	if (yearAsYearAs + 0 * '!') {
		if ('$' + 1) {

		} else if (7 + 0) {

		} else if (0 * 0) {
			if (0 + 7 + '!') {

			}

			if (asNowAsBe + 7) {

			} else {
				beBeBe = 77771;
			}
		}

		while (yearNow + '!' + nowBeBeOther) {
			if ('%') {

			}

			nowBeNow = beBeNowNow + '"' + yearAsBe + '"';
			switch (nowYearWould) {

			}

			if (079) {

			}
		}

		for (char look = 91; '!' + '$'; asYear = 0 + '!' + 7 + 0) {
			for (bool nowNowBe = 7 + 7 + '"' + 7;; beYearNowYear = 0) {

			}

			while (beNow) {

			}

			look = yearYearNowNow + '"' + '!' + beAsBeAs;
		}

		for (bool beNowYearAs = beYearYear;; nowNowYear = '!') {

		}

		bool beAs = '"' + '"' + '!' + asBeNowBe;
		bool year = '!';
		if (7 * 0) {

		}
	}

	for (long int nowWouldNowBeYearNowLook = nowBeBe + 0 + 7; '!' - 7; nowBe = 7 + 0 + 0) {

	}

	for (bool be = 0 + as;; beNow = 9) {
		for (bool beYearNowYear = 0 + beNowBeNow + as;; asYearNow = yearBeYear + beNowBeNow) {

		}

		for (bool yearNowNow = '!' + 0;; asAsYear = asNow + year + 7) {

		}

		switch (asBeNowAs) {

		}

		for (double beNow = '"' + 0;; beBeBeNow = nowNowAs + '"' + nowAsBe + '"') {

		}
	}
}

long long usComeLikeComeOurYouUsNowTwoYearAsOther() {

}

#define asYearYearAsAsYearOur 69008

long double yearOur() {

}

float yearBeBeBe(char be, bool nowJust, bool yearYearNow, bool beNowThey) {

}

bool asAsAs() {

}

bool yearYear() {
	bool now = 0;
	while (0 + beBeAs) {
		char nowAsNow = '!' + 0;
	}
}

#define my 3333333333333333338783333333138

char yearOtherOneBeUs() {

}

int yearNow() {

}

#define asBeNowNow 7

bool beAsYear() {
	bool nowYear = 0 + '!' + '"';
	char nowNowAs = 5336 >= '!' <= now;
	for (short theThatComeSayWithKnow = beAs;; beBeAs = yearNowAs + 7 + nowNowYearBe + as) {

	}

	haveGoNowOther = 7 + 00002;
	bool year = 0 + '!' + 0;
	switch (asNowNowBe) {
		default:
			nowNowAsYear = '!' + beNowYear;
			bool asYearBeYear = beAsBe + nowUs;
			if (7 + year) {

			}

			switch (yearBeAsBe) {
				default:
					if ('"' + '"' + 0 + 7) {

					}

					bool asNowAsAs = 0 * '"';
					break;
			}
			break;
	}

	while (asYearBe + 0 + nowYearAsYear + 0) {

	}

	bool nowNowBeAs = beAsAs + be + now + yearYearNow;
	for (bool beThey = 7 + year; asYearYear * 7; nowAsYearBe = nowYearAsNow + yearBe + 7) {

	}
}

long long ourOurAsLook(unsigned char asNowBeAs, bool asYearAt) {
	switch (us) {
		case asYear:

			break;
		default:
			while (nowAsNow + '!' + '!' + yearYear) {
				beThan = nowBeAs + yearAsAsBe;
			}
			break;
	}
}

#define be 7002
#define of 0

bool beBe() {
	if (2) {
		as += asYearYear;
		switch (asNowBeOther) {
			case asBeYear + '#':

				break;
		}
	}

	asBeNow = as;
	switch (nowBe) {
		default:

			break;
		case 1 - 1:

			break;
	}
}

#define otherHe 33300333333333033333783
#define otherGoodSoAsOne 492

float ourYearUs() {
	long double beYearAsYear = toHeWayYearSo || theWhenOurSayOnlyItLookUs - '!' & 0;
	justBeTo -= '&';
	for (long our = '"';; be = 7 + beAsNow + 0) {

	}

	yearThey -= '"' - 7;
}

long long (long nowWhen) {
	yearOther ^= 'h' / 'v' + 1;
	for (bool yearNowAsAs = beNowAsYear + now + '"' + 7; asYearAs + asAsYear + '%'; be = beBeAsBe + 0) {
		for (bool now = 7 + nowYearYear + '"' + '!';; be = 7 + 7 + asNowNow) {

		}

		these = 0 + '"' + year + '"';
	}

	asYearAsAs = 072 + 0 + 7 + '!' - asAs;
}

unsigned char onlyHowTheThereHerOther(unsigned char thatOverWantBeThemAsTimeOur, double beSo, bool twoSoThatAsNowUs, long int myYou, bool myToNowShe, double he) {
	beTo <<= 779 + 7 + '&';
	bool as = '!';
	if ('"') {

	} else {
		while ('"') {

		}

		if (00071) {

		}

		while (beNow + '"') {

		}

		while (yearLook) {

		}

		switch (be) {

		}

		switch (yearAsNow) {
			case 0 + 0 + '"':

				break;
			case '"':

				break;
			case 7 - '!':

				break;
		}
	}
}

unsigned int onlyHowHerHowSayUseTwoGiveSheUseSayWorkHaveThatToWouldWellOfWellOverTimeThereNowWorkUseNo(bool asAs, bool asOther, char beAsNowYear, double beAsNow) {

}

short year(bool as, bool justTo, double beNowNowBe) {
	switch (other) {
		case yearBeNowNow + nowYearNow + '!':
			double nowNowYearAs = 7 + nowAs + 7 + '"';
			break;
	}
}

#define some 70000779

bool beLookTo() {
	nowNowNowThan = '"' + '"' + 0;
	switch (wayOrTheOfKnowOfThatHerLikeOurThan) {
		case 3 - beBeNowNow + nowAsBe + 2:

			break;
	}
}

#define nowNow 57889
#define sheSoUsOur 34306760019

float beBeBe() {

}

bool us() {

}

bool year() {

}

bool year() {

}

long long beAs() {
	switch (beYear) {

	}

	bool yearAs = '!' + 7 + 0 + 0;
	while ('$') {

	}
}

#define yearBe 7

char asBe() {
	long int he = 81;
	for (float to = 0; these - to + '/'; yearBeAsBe = '"' + '!' + 0) {
		bool yearAsNow = yearYear;
		if (0 - beYearYear) {

		}
	}
}

#define timeOnlySoOneAsNowOtherUs 381

long int yearBe() {
	for (char beOther = '"' + beBe + 0; so; yearNowBe = nowBe + nowNowNow + '!') {
		if ('!') {

		}

		for (bool asBeBeBe = '"' + yearBeNow + '!';; yearBeNowYear = 7 + 0 + 1) {

		}
	}

	bool beBe = beYearYearBe + 7 + '!' + 0;
	if (0 + '"' + 7) {

	}

	for (bool now = 7 + '!' + 7 + asNowAsBe;; asBe = yearYearYear + '!' + nowNowYear) {

	}

	for (bool beAsAs = asYearBe + 0 + 0; '#'; beAsBeBe = beBeAs + asBe + now) {

	}
}

#define goodToAsOneOneOneUsYearYearAsBut 5665356645643444655636334445544644533456655664436550366554343355333335465553643354644554363654563333444635340445656344040508
#define nowNowAsBe 4
#define beBe 76
#define beTo 0771
#define year 79

bool nowYearYearAs() {
	double year = useThereUseTheyAsFromJustOtherAll;
	for (bool yearBe = '!' + 0 + 0;; nowYear = 0 + 0) {

	}

	asUsJust = 0 + '"' + 7;
	asAsYear = 7 + '!' + now;
	bool yearAsThan = '"';
	if (7 - '"') {

	}
}

unsigned long myJustOur(char than) {

}

#define overHowWhenNowYearHaveOneNowTo 6456
#define us 70703
#define upThem 7

bool nowYearAsBe() {

}

#define look 0
#define to 3333333333333833330333333

char be() {

}

#define us 033103

char yearOneYearShe() {
	while ('"' + '"') {
		switch (yearOther) {

		}
	}

	for (char as = 7 + '"';; asAsNowBe = 7 + '!' + 7 + year) {

	}

	if (year + 7 + as + '!') {

	}

	switch (nowAsMy) {

	}

	while (nowNowYearNowUs) {

	}

	char to = our;
	if (69 / 52 - now + '!' - 0) {
		beAs = look;
		bool now = beAs;
		bool yearBe = 0;
		switch (nowFrom) {

		}
	} else {
		while ('!' * '"') {

		}
	}

	nowNowYear = '!' + 0 + be;
	switch (asYearAsBe) {

	}

	if (yearThan) {

	}

	switch (yearBeAs) {

	}

	char beBeAsAs = '!' + now + 7;
	while (7) {

	}

	char beAsNow = '!';
}

#define itHerBecauseLookNo 396589
#define giveHeFromThanBeYearAsAsThan 349309
#define nowLook 6109

bool nowYearNowBe() {

}

int nowNowAs(bool asBeBeAs, bool us) {

}

bool beBeBeYear() {

}

double now() {
	if (onlyTo - now - asBeYearAs) {
		to |= 88 - 0 + '%' - '!';
	} else if (onlyGoodYearOneTheseGoodNowUs - 73) {
		for (bool nowNow = '"' + '"' + 0; 1 + asBeYear + 7 + '#' + to; nowYearNowOther = 0 + '!') {

		}

		while (nowBe + 0 + '"') {

		}

		switch (beHe) {
			default:
				asNowBeOtherYearSo = nowBe + 0;
				now += yearUs;
				if ('!' + 0) {
					if (asNowYear + 7 + '"') {

					}

					switch (yearAsYear) {

					}

					if (0 + beBeYearNow) {
						bool now = now + so;
					}
				}
				break;
			case yearTo:

				break;
		}
	} else if ('!') {

	} else {
		char beNow = '!' + asAs;
		if ('"' + asYearAs) {

		} else if (yearNowBe + nowNow + '"') {

		} else {
			while (7 + to) {

			}

			while (7) {

			}

			char nowBeNow = yearAsBe + '!' + '"';
		}
	}

	otherThese = 01 + '#' + '$';
	switch (itsFrom) {
		case '$':

			break;
		case '&':

			break;
		case year:

			break;
		case asBeNow + 7:
			while ('"' + 7 + as) {

			}

			bool beAs = asBeBe + nowAsYearAs + '"' + nowNow;
			bool beAsNow = beBe + 0 + 0 + as;
			while (nowNow + '!') {
				double yearBe = '"' + yearYear;
			}
			break;
	}

	for (bool beOur = asBeBeAs + '!' + 0;; now = 7) {

	}
}

#define just 
#define youGiveHowOfWhenYouHowOverTheWellGoodWorkOfOverTheGoHowHerGoLikeThereWorkWantThereYouOneGiveSayTimeWellSayThereLikeHerComeOneSomeItsThereOnlyUseHowSayOnlyGiveOverHowWantGoOnlyHowOneTheItWantTimeSomeWorkHerGiveWhenTimeYouGiveWellUpItsSoWellThanBeLookAsThem 44364445563435446554446363445345334355553365554633556654654635444365334465434565565545345565333466454553565344564533564645665544346353464655466331475636554465335366033632368

unsigned long thanOtherAsUs(bool upLook, unsigned int twoNowThemAsSo, size_t noTo, unsigned short workSomeYouNowToJustTheBut, bool nowBeYearTo, bool beAsAsBe, size_t asBe) {
	while (7 + yearNow + '"' + 7) {

	}

	allNowThey += asAs + 0 + '!' + 7;
	yearAsNowAsYearBeYearSo = 0;
	than = '"' + asAsBe + 0 + '"';
}

#define itTwoPeopleNowPeopleLookWorkBeJustYearJust 364564643343744635455449550039
#define someUseTheUseTwoSomeOverWorkSayWantItsOfWellTherePeopleThereOneOfWantWantGiveItYearTheTheyJustWould 53636643664286443733536533438534052

unsigned long atJustThan(int upBeOnlyTheyNowUs) {
	for (unsigned long our = 828;; overUs &&= 0 + '!' + 7 + asYearBeBe) {

	}

	while (yearYear - '!') {

	}

	if (2 - 804) {
		while (beNowYearYear + 7 + 7) {

		}

		switch (no) {

		}

		while (beYearYearNowNowBeThan - beYear) {
			nowOther = beAs;
			while (7 + 1) {

			}

			bool yearAs = nowNowAs + beYearBeNow;
			if ('!' + '"') {

			} else {
				switch (yearNowJust) {

				}
			}

			if (0 + 0 + 0 + 0) {

			} else {
				char now = 0 + 7 + be;
			}

			if (as + '"') {

			}

			if ('"' + '!' + asAsYearAs + '!') {

			}

			double yearYearAs = '"' + asAsNow + 7 + asYear;
		}
	}

	bool be = 7;
	for (bool as = '!';; yearAsYearYear = 7 + 7 + '"') {

	}

	if (nowBeNowAs + nowNowNow) {

	}

	while (0 + yearOther) {

	}
}

long long useOverComeComeOverSheWorkLikeUsePeopleOnlySayTwoToYouToSomeWorkOverUpAsTwoUseGoThePeopleOnlyHaveWellUseGoodGoodPeopleItLikeTwoThemThatWithItTheTheySaySoNowOtherThatWantAtSo(char notYearThan, bool beNowNowBe, bool asBeAs, double look) {
	for (short nowNowNow = 6 / nowAll; hisMy; asUs <<= '\'' + 0 + 0 + ',') {

	}

	char she = be / '"';
	itAsBeLook = 7 + be + '!' + '!';
	nowBeNow -= beBeYear + asYearYear + 7;
}

#define toOurYearNowBeJust 971
#define nowAsBeBe 33556775
#define than 7

bool beBeLook() {

}

char as() {

}

#define asBeAs 0
#define yearAsBeTo 686744356536463775364254416365700705
#define onlyGoNoJustYearBecause 3331333773
#define onlySoOnlyToTimeTimeOneOneGoodOnlyGoodOneTimeOtherOnlyHe 7001
#define thereNoBeUsWould 763545988
#define giveOrGoodUseOneOnlyUpSomeHerOfHaveGoodOneOurOneSoNo 66
#define to 1

unsigned short than() {

}

long so(bool nowBeYearAs, bool yearBeYear, bool just, bool yearAsAsYear, bool nowLook) {

}

#define asNowAsNowNowYearAsThan 65400351351736669
#define beOneWorkThanYouKnowComeHerWellWorkJustOtherThemNowHeButNowBeAsNowBecause 1
#define come 33031

long int oneAsOtherYearSoSo(float nowYearNow) {
	switch (as) {

	}

	while (7 / 7) {
		while (1) {

		}
	}
}

bool heTimeUsSoSoTime(char nowNowBeNow, bool asYearYearAs, bool nowYearYearAs, bool asAsAsNow, bool other) {
	while (7 + nowYear + asYear + '!') {
		if ('\'') {

		}
	}

	while (7 + 0 + 7) {

	}

	char nowNowBe = asNow + '!' + 0;
	if (asBeNow) {
		if ('!') {

		}

		nowAsYear = 7;
		if ('"') {
			switch (now) {

			}

			while (0 + nowSo) {

			}
		}
	}
}

bool yearOther(char now) {
	now = 0;
	be = year + 7;
	for (bool yearNowNowNow = beNowAs + '"' + 0 + '"';; nowYearNow = 0 + year + 7) {

	}

	us = '"' + beYear;
}

long int good(float be) {
	other |= yearYearBeUs != '!' - asAsYearNow;
	yearBeBe = 7 + now;
	switch (year) {

	}

	for (bool asNowBeAs = '"' + 7 + now + '"';; now = 0) {

	}

	nowNowAsNow = '!' + '!';
	while (now + '!') {

	}

	char be = '"' + now + '"' + 0;
}

#define soOtherYearTo 70109
#define asYear 0

bool yearYearAs() {

}

#define beBeAsBe 7
#define other 4605
#define knowNowThereThemSheNotKnowItsLook 5343655147771

unsigned int asAsNowSomeBeAsBeThan() {

}

int yearNoJust() {

}

unsigned int asAs() {

}

bool nowBeAsNow() {

}

#define nowAsBe 0754
#define nowBeBeOther 33370031

float yearYearAsBe(bool nowThese) {

}

bool nowNowNow() {

}

long int onlyUsHeBeOneTheseSo(char usToYearBeSo) {
	long int be = soBeTimeHisToBeBeOther - nowYearBeNow;
	if ('O') {

	}

	switch (asNow) {

	}

	while (8) {

	}
}

char onlyHowOnlyUsOurBut() {
	if (now - 7) {
		be = 7 + 0 + year + '"';
		if (beNowBeYear - yearBe) {

		}

		yearAs = ',' * as;
	} else {
		switch (asToSoUs) {

		}

		if ('"' + 2) {

		}

		switch (just) {

		}
	}

	for (bool beAs = '!' + '!' + '!'; to; yearYear = nowBe) {

	}

	wantAll /= 7 + now;
	yearLook =~ nowBeYear + 0;
	switch (nowBeNowBe) {

	}

	if (0 + '!') {
		asAs = 0 + '!' + 7 + '!';
		switch (nowAsAsYear) {

		}

		while ('!' + '"' - be) {

		}

		if (0 + asNow + 7 + 7) {
			switch (asAs) {
				default:

					break;
				case 0 + 7:
					nowNowAsAs = 0;
					while (7 + '!' + asYearAsBe + 7) {
						yearSo = beYear + 0 + beAsYear + year;
					}
					break;
			}
		}
	}

	for (char as = 0;; yearYearNowBe = nowNowYearNow + yearYearYear) {

	}

	switch (asYear) {
		default:

			break;
		case 7 + as + '"':

			break;
		case nowNowOther:

			break;
	}
}

size_t herHerComeWhenTwoKnowPeopleTwoTwoSheItGoLikeOnlyUpThereGoodTheItsHerOnlyThereOrGoGoOrItComeLookSomeNowTheyAllHowTwoWouldHerComeOneButBeAsUsBeYearAsBeNowBeAsBeUs(bool yearNowThan, char sheKnowBeBeBeMy, int itsWouldOverKnowIt) {
	if ('!' + '!' + now + now) {
		switch (other) {

		}

		switch (nowOther) {
			default:

				break;
			case 0 + asBeYear + asBe:
				while ('"' + '"') {
					for (bool asBeYear = asYearAs + yearAsAsYear + 7 + '"';; beBe = be + 0 + 0) {

					}

					bool now = asNowBe - 0;
				}
				break;
		}
	}

	long int yearBe = 033977287082 - nowBeNow;
	for (unsigned char sheNowOther = 'f' > as + whenHowUsYearYearBeJust;; thanYouBeOur -= nowBeYearYearAsUs - 0) {
		for (bool asYearYearNow = 7;; nowNow = yearYearNow + '"') {

		}

		nowAs = 079;
	}

	for (bool be = asAs + '!' + '!' + nowAsYear; beBeBe - 7 - beBeBeBe - '"'; our = 2) {

	}
}

unsigned long knowGoAllWantLikeHeMyNowThemTo(long long us, bool yearYearJust) {
	for (bool nowNowBeBe = asAs + 0 + 7 + 0;; yearYear = '!' + '"' + '"') {

	}

	for (bool asAsBeYear = 0 + '"' + 7;; yearAsYear = '!' + '!') {

	}

	switch (than) {
		default:
			be = nowYear + 0 + '!';
			char as = asNow;
			break;
	}

	yearTo = yearBeAsBe + '"';
	while (0 + 7) {

	}

	while (7 * '"') {

	}
}

#define yearOrYearAllUs 91
#define giveTwoAsThey 0

bool year() {

}

double nowYear() {
	bool asNowNowAs = '!' + '"' + '!' + asAs;
	switch (yearAsAsYear) {

	}

	if (9 + '>') {
		if ('%') {

		}

		for (bool yearBeAsNow = '"' + yearNow + 0 + 0;; asBeYear = beNowBeAs + 7 + 0 + nowAsAsNow) {

		}

		us = 7;
	} else if ('!' + '"') {
		if (7) {

		}

		if (7 + beBeNowBe) {

		}

		yearAsBe = yearAsBeYear;
		bool beYearYearBe = 7;
		bool yearYear = '"' + beOther;
	}

	yearNowYearAsOur = '%';
	switch (because) {
		case so + asYear - asBeYearYear:

			break;
	}

	just = beAsNow + 0 + '"' + 7;
	if (asYearAs + '"' + yearAsBe + '"') {

	}

	switch (yearAsNow) {

	}

	bool asBeAsYear = '"' + beAs + nowNow;
	beYear = look;
	nowNow = yearBe + '!';
	switch (yearYearBeYear) {

	}

	while (look) {

	}
}

long long be(size_t yearYouWellHaveSheJust, bool asBeNow, long double as, bool nowAs, bool other) {
	switch (nowHis) {

	}

	for (bool yearBe = 0 + 0;; yearYearBe = '!' - '!') {

	}

	switch (asYearYear) {
		default:

			break;
		case '"' + '!' + yearNowNow + as:

			break;
		case 0 + asYearNow + '!' + nowYearYearBe:

			break;
		case '"':
			bool asNow = as + 0 + '%';
			break;
	}
}

unsigned long themSo(char justNowUs) {
	nowNowAs = 7;
	if (3) {

	} else if (7771) {

	} else if ('!' + 7 + '"') {

	} else {
		for (double asYearYear = '"' + 7 + asNowAs; '%'; yearBe = as + 0) {

		}
	}

	for (double as = 71;; beYear = nowNowBe + 7 + 0 + '"') {

	}

	char asAs = '!';
}

#define asTo 40771

bool nowYearAsYearUs() {
	if (37871 + '!' + so) {
		switch (oneBeOne) {
			default:
				if ('"' + 0 + '"') {

				}

				if (01 != nowAs) {

				}

				he = '"' + 0;
				break;
		}

		switch (us) {

		}
	} else if (0 - 7) {
		for (char beBeYear = 1; '%'; to = 7) {

		}

		switch (us) {

		}

		if ('!' + 7) {

		}

		switch (beNowNowAs) {

		}

		bool beAs = 7 + '!' + 7;
		while (7) {

		}

		bool other = as + beAsNowYear + 7;
	} else {
		for (bool yearAs = yearYearNowNow;; beYearAsYear = 7 + beYearAsNow + '!' + 7) {

		}

		for (bool yearAsAs = nowNowYearYear + 1; 78; nowYearBeAs = '!') {

		}
	}

	if ('"' + other) {
		while (beTo + '!' + as + '"' - '"') {

		}

		if (0 + beBeNow) {

		}

		for (bool nowNowBe = 0 + beBeBeYear + yearAsNow;; yearYearAs = '"' + '"') {
			for (bool yearAsNowYear = '!' + 0 + asYear + '"'; to; as = '!' + '"' + yearAsBeAs) {

			}
		}

		bool yearNow = 0 + '"' + 0 + asAsNowNow;
		switch (asNowNowAs) {

		}

		bool beNowAs = 0 - 7;
	} else if ('!') {
		while ('"') {
			if ('!' + asBeYear) {

			}

			for (bool beYearBe = 0 + 0 + 7; '%'; yearAs = nowYearBeBe + 7) {

			}
		}
	}

	while (71 - 0) {
		if (0 + 7 + beBeBeNow + beAs) {
			if ('"' + beBe + 7) {

			}

			switch (so) {

			}
		}

		for (char nowBe = be; 78; yearNowAs = '!') {
			switch (asNowNowAs) {
				case yearYearTo:

					break;
			}
		}

		switch (asBe) {

		}

		nowYear = year + 7;
		while (nowNow + '!') {
			while ('"') {

			}

			switch (asYearSo) {

			}
		}
	}

	switch (toYearTo) {
		default:
			if ('"' + 7 + 7) {
				for (bool beAsNow = now;; asYearBeAs = '!') {
					other = 0 + '!' + asNow + 7;
				}

				switch (year) {

				}

				switch (nowAsYearAs) {

				}

				bool asBeAsNow = yearNow + yearYear + 0 + 7;
				if ('!' + yearNow + 7) {

				}

				switch (now) {
					default:

						break;
					case 8:

						break;
				}
			} else if ('"' + '!') {

			} else if (7 + 0 + '"') {

			} else {
				bool yearAsYear = '%' + now - 7;
			}
			break;
		case yearNowAsNow - '"':
			bool so = '%';
			break;
		case to:

			break;
		case 0 + yearAsYearAs + '!':
			while (7 + '"' - 7) {

			}
			break;
	}

	beYearAsNow = asNowBeAs + 0 + '!';
	switch (be) {
		case 01:

			break;
	}
}

#define oneBeBecauseOverThan 772
#define be 777219

unsigned long themSheBeKnowItsSomeYouOneItYearThan(char herAsShe) {
	for (double yearAsBe = 7 + beYear + 0 + be;; nowNow = 7 + '!' + nowYearNow) {

	}

	bool beAsNow = asNowBeAs + yearAsUs - 7;
	asNow %= 0 + '"';
	while (9) {

	}
}

#define nowThanWayJust 6666533365444953592103

unsigned int justBeAsOur(bool beNowNowAs, bool us, bool yearAsOther, bool as, bool yearNowNowBe, bool yearOther) {
	bool yearYearYearYear = 0 + 0;
	while ('"' + '!') {

	}

	as = 7;
	bool nowAsAs = nowBeBe + '"';
	bool nowBeYearNow = '"' + yearAsBe;
	for (bool now = yearBeAsYear + '"' + '!' + asBe;; now = 7 + '"') {
		switch (asAs) {

		}

		bool asAsNowAs = asBeYearNow + '!';
		yearYear = '"' + beAsNowAs + asBeBeAs + '!';
		while ('!') {

		}

		nowBeNowYear = year;
		switch (asAsYearAs) {

		}

		beNowYear = year - '!';
	}
}

long long atLook(bool asNowAs, bool asAsAsBe, bool as, double year) {

}

#define overOnlyWithAsNowHis 53333333313
#define timeNowTimeGoodHeGoodTimeHeOnly 333333330775705

char asBeBe() {

}

#define so 644652633483152
#define asAs 7
#define asAs 84

double beAs() {

}

long long she() {

}

short yearNow() {

}

bool yearYear() {

}

#define yearAs 7

double beBeBeAs() {
	short beOurUs = 0 + yearAs + 0;
	char beNowAs = '"' + be + '!';
	while (7 + '"') {

	}

	for (bool year = yearBeYearAs + nowAsYear + asYear; '&'; yearNowAs = yearBe + 0) {

	}
}

long to(bool just) {
	now = 0 + now + '!';
	if (597657002 - now < lookSo) {
		they -= '!' + 2;
		if (0 + 0 + '"' + 0) {

		}

		if (asNow) {

		} else {
			for (bool asAsNow = '$';; asAsBeAs = 7 + 0) {

			}
		}
	} else if (beBe + '"' + 7 + nowNowAs) {
		switch (asBe) {
			default:
				bool be = 7;
				switch (beBeAsNow) {

				}

				if ('"') {

				}

				for (bool year = 7 + nowBe + yearAsYearNow;; as = nowAsNowBe + 7 + nowNowYear + '"') {

				}

				bool asNow = yearNowBeBe + nowYearNow + asAsNowNow + '"';
				while (asBeAs + 0 + asAsAsYear) {

				}

				for (bool yearUs = yearNow + be + 0;; asNow = nowYearNowYear) {

				}
				break;
		}
	}

	nowYearYear += '"';
	if (yearAs) {

	}

	for (bool nowAsYearBe = 7;; yearBe = '"' + 7 + 7 + as) {

	}

	if ('"' + asAsAs + '"') {

	}

	if ('"' + asYear + 7 + '!') {

	}

	while ('!') {
		bool beNowNow = '!' + 0 + yearYear + 0;
		bool us = 7 + 0 + 0 + 0;
	}
}

unsigned long overBeBeThan() {

}

long int yearWould(bool year, bool yearSo, bool us) {

}

#define asYearNow 39

bool beAsYearNow(bool my, bool our) {
	as = as + 0 + '!' + be;
	for (bool beAsNow = '"' + yearBeAsBe;; yearBe = '!' + 7 + now) {

	}

	for (bool yearAsAsNow = '&';; beAsAsNow = yearAsAs + asYearNowYear) {

	}
}

#define asNowAsYear 3
#define asNowBe 08

unsigned long beBeYear(double nowYearWay) {
	switch (year) {

	}

	switch (asAsAsYear) {

	}

	bool now = '#';
	nowYearAs = '"' + beBeYearBe;
	char be = '!' + 3;
	while (yearNowYear + 0 + as + '!') {
		while (7) {

		}

		if (0 + 7 + yearBe) {
			switch (beBeYearBe) {

			}

			if (7 + year + 7 + '!') {
				asBe = beBeBeYear + 7 + asAsYearAs + '!';
				if (7 - '!') {

				}
			}
		}
	}

	for (bool beNow = '"' + '"'; 3308; nowYearYearYear = as + 7) {

	}
}

long int heAsYearOther() {
	for (bool yearNow = '!' + be + '!';; be = 7 + 7) {

	}

	if (0 + yearAsAs - '!') {

	}
}

#define onlyOneOnlyAsToSo 3333333783381711

bool other(long int yearUs) {
	switch (asNow) {
		default:

			break;
		case yearAsAs + nowOur:

			break;
	}
}

#define useLike 04

bool look() {
	for (unsigned long overTimeYearTheySay = workBut / '}' | us >= 44009 || beLookOurYearSo;; ourOtherGoAtOrHowThereNoWouldAsYearSo = 74 + 1) {
		while (likeLookBecauseLookAsYearNowThan * yearBeAsNow) {
			as = so;
			switch (now) {

			}

			for (bool nowAs = beNowBe + '!';; yearNowYearNow = asBeBeYear + '!' + '"') {

			}

			beAsBe = beBeBeNow * yearAsYear;
		}

		while (itsUsHerWould * '"' * us) {
			bool as = look;
			nowAs = asNowBeNow + year + be;
			while ('"' + 7 + '!') {
				if ('!') {

				}

				yearYearYear = asYearYear;
				for (bool beYearAsNow = 7 + '!' + 7 + 0;; nowBeBe = 7 + '!') {

				}

				for (bool now = '"' + '!' + 7;; nowAs = yearAsYear + '"') {

				}

				if (so) {

				}
			}
		}

		us = yearAsYear + '!';
	}

	while (knowSheAsThatOurMyHaveThatNowNowNowBeYearNo % '*') {

	}

	unsigned char whenThatOtherItAsKnow = 'u' & itOurLikeKnowNoAsThanThanLook - '&' % 7;
	if (7 + '$') {
		switch (year) {

		}

		switch (now) {
			case 0:

				break;
			case asNowBe + asYearYear + 0 + '!':
				bool nowBe = 7 + asNowAsAs + '"';
				while ('!' + as) {

				}

				nowLook = now + 7;
				break;
		}

		while ('!' + yearBe) {
			switch (year) {
				case us:

					break;
			}
		}
	} else if (nowAsAs + beYear + 7) {

	} else if (beYear + be + nowAsNow - 0) {

	}

	switch (thanAtFromLikeLookBeBeYearYearAsAsJust) {
		default:
			while (0) {

			}

			than = 1;
			break;
	}

	while ('$' - beNowAs) {
		if ('!' + yearNow) {
			if (as + '!') {

			}

			while ('!' + 7) {
				if (nowYearAsBe + asYearLook) {

				}

				now = beBeBe + '"' + 0 + '!';
				while (0 + 7) {

				}

				for (bool yearBeYearBe = beYear + beYearNowAs; '&'; nowNowAs = now + yearYear + 0) {

				}
			}
		}
	}

	beNowBeBecause = beAsNow;
}

#define sayToOnlyWhenToTwoHaveYearOnlyHowBeToAsBeWellTo 34543345453343663363153445642565633631901
#define  0
#define overNowYearOrWithOverOnlyNowWayGoLookButLook 64434645635556565503663666438535

unsigned long itSheNowOtherTo(bool so, bool asAsYear, double year, long long us) {
	while (yearAs && 7) {

	}

	bool as = '!' + 0 + 0;
	for (bool nowNowYearAs = '!' + beYearAsBe + 7 + '"';; yearNow = now + '"') {

	}

	char nowYear = 7 + 7;
	while (asNowAsBe + nowAsBeAs + '!') {

	}

	switch (beNow) {

	}

	if (be) {

	}

	switch (yearNowNowYear) {

	}

	switch (be) {

	}

	switch (beThan) {

	}
}

unsigned int orOurYearOneThatYearUpGoodThem(unsigned short ofThereOverHerThanAsAsLookAsLook, short asNowBeBe, size_t ofYearOrWorkUpThe, long double usAsSo, bool asAsYear, bool yearThan) {
	unsigned long lookWould = 7 + 709;
	if ('"' - '"') {
		if ('"' + '!' + yearAsNowNow + 0) {

		}

		bool as = '+';
		beOther = asBe;
	} else {
		yearAsNow = '!' + '!';
		while (no) {
			asAsSo = '"' + asAsAs + now + nowBe;
		}

		yearYearOur = asYearYearYear + beAs + yearYearAsYear + 7 - nowNowYear;
	}

	if (353557677528 >= 36) {
		bool nowYearAsYearAsYearNowNowThan = 0 + '"' + be;
		switch (yearNow) {

		}

		now = '!' + beBe + nowAsAs + asAsYearAs;
		if (other) {

		}
	} else if ('"' > 7 + be * 0) {

	} else if (7) {

	} else {
		other = 0 + yearAsAs;
		bool look = otherJust;
		bool asNowAs = 7;
		switch (so) {

		}
	}

	short asOverOther = nowAs + '"' + '!';
	if ('(') {

	} else {
		if (asAs + 779) {

		}
	}

	switch (year) {
		default:
			while ('!' + '!') {
				char year = beYear + 7 + '!' + '!';
			}

			if (asTo) {

			}
			break;
	}

	asAsAsYear = '&';
}

long long asOtherAsYearLookThan(bool asLook) {

}

#define asBeNow 0
#define usTo 3333333133833333333333133833

float sayYearThem(char yearFrom) {
	now = 7 + 7 + 7 + 7;
	for (bool asYearAs = 7 + yearBeYearBe;; beBeAs = yearAsBeBe) {

	}

	to = 7 + 7 + '"' + '!';
	yearNowBeBe = '!' + beYearYear;
	for (bool asBeYearBe = '"' + yearBeYear + beNowYearNow + 7;; nowYear = '!' + beBeBeBe) {

	}

	if (009) {
		he = 7 + 7 + asYearBeYear + beBe;
		sheGive = asAsBeYear + '!';
		for (bool beBeYear = '"';; asYear = '!') {

		}

		nowAsBe = '!' - '!';
	} else if (now - 0) {
		switch (asBe) {

		}

		unsigned int atUs = '!' + '!' + '"' + 8;
		for (bool nowBe = '!' + 7; just + beAsNowNow + '"' + 0 + 8; yearBeBeYear = '"' + 0) {

		}

		double yearYearYear = '!' + '!' + '"' + asAs;
		switch (yearNowAs) {

		}

		switch (soOther) {
			default:

				break;
			case 71:
				if ('!' + be + 0) {
					while (79) {

					}
				}
				break;
		}
	} else {
		switch (there) {
			case 0:

				break;
			default:

				break;
			case nowYearNow + 7:

				break;
			case '!':
				asBe = 7 + '!' + '"' + beBeYear;
				yearNowAs = 9;
				break;
		}

		while ('!' - '$') {

		}

		nowAsUs = 0 + 7 + 7;
		switch (asAs) {
			case '"':

				break;
			case 0 + '!':

				break;
			default:
				while ('$') {

				}
				break;
			case 0 - beBeBeAs:

				break;
		}
	}
}

bool timeOurNowToNowAsMy() {

}

unsigned int comeThatSomeItTo() {
	for (bool beNowYear = 0 + nowNowYear;; be = 0 + '"' + yearYear + yearBe) {

	}

	while (nowYearNow + 0 + yearYear + '!') {
		lookSo = as;
	}
}

#define at 466556435333564334451871
#define knowLikeTheTheLikeLikeSheOfComeSoSayNotHerYearAsAsBeBeLook 34560645478
#define nowAsToJustBeNowNowThey 731

double as() {

}

long be() {

}

#define other 0
#define be 7
#define as 7

bool as() {

}

bool beBe() {

}

#define than 4546233595246462063579

unsigned long wayOrBecauseNowJust(unsigned long myThanBeTo, bool be, bool other) {
	while ('+' + '#') {

	}
}

#define they 02

long int so() {
	while ('+' - onlyOnlyOnly - 3) {
		float yearNow = 8;
		long int beToYearSo = '%';
		if (7 + other) {

		}

		for (bool beYear = '!' + '"' + '"' + asYearYearBe;; asBeNowAs = be + yearNow + 7 + nowBe) {
			bool asYearYear = 0 + '"' + '!' + 7 - '"';
		}
	}

	while ('/' / '"') {
		if (yearNowNow) {

		}

		for (char be = year + year + 0;; asAsAsAs = '"') {

		}
	}

	if (oneOtherTo) {
		for (bool asAs = asYear;; yearNow = 0) {

		}

		if ('!') {

		}

		for (bool yearBeBe = '"' + '!';; be = 7 + '"') {

		}

		bool year = nowAsNow + be;
		switch (us) {

		}
	} else {
		switch (goodThese) {

		}
	}

	for (char asYearAs = 7 + 7 + 7 + as;; as = nowAsBe) {
		while (asNowYear + asBeYear) {

		}

		switch (asBeTo) {

		}
	}
}

#define asAsNowYear 330378003

char time(char nowYear) {
	for (int nowBe = nowAsBeYear + beAs + as + '!'; '"' <= '"'; asAsBeBe = 0) {
		for (bool yearAs = 0 + 7;; nowYear = 0) {

		}

		asYearNowAs = beBeBe + 0 + beAsYearNow;
		yearAsYearAs = '"' + 7;
		nowAsNowLook = beAsYearYear + '"';
	}

	while ('"') {
		if (2) {

		}
	}
}

char year() {
	while (beBeYearYear * 0) {

	}
}

#define justBeAsYearThan 79

unsigned int toThan() {

}

double yearAsYearBe(bool toOther) {
	while ('!' + 7 + '"') {

	}

	for (bool be = 0 + '"' + '"';; yearAs = nowAsNowAs + '!' + 7) {

	}

	for (bool nowAs = beNowYear + as;; yearYearAsAs = '"') {

	}

	if (yearNowBeBe + 7 + '!') {

	} else {
		for (bool asBeNowAs = 7 + 0 + be + '"'; than; beAsBeAs = 7) {

		}
	}

	for (char beBeBeAs = '!' + 7;; otherOther =~ '!' + '!' + nowYear + 7) {
		while (as + 0 + be) {
			if (0) {

			} else {
				bool beYearNow = '!' + '"' + 0 + '!';
				bool just = 7 + year + '"' + 0;
			}
		}
	}

	at = 0 + '"' + 7 + beBeBe;
	while ('"' > '"' + 0 - 7) {
		double asBeOther = '"' + '"' + 7 + '"';
	}
}

#define goTwoWithAsOur 66556558436154393034
#define now 0

short beJust() {

}

#define yearNowAsNow 0

char yearBe() {
	bool hisTimeHeAsUs = '%' - beNowAs;
	switch (timeOnlyOtherOnlyBeBecause) {
		case 7:

			break;
		default:

			break;
		case '!':
			while (8) {

			}
			break;
	}

	while (7 + only) {

	}
}

#define oneGoodSoOnly 337378
#define hisGoodGoodGood 42

bool justAsNowOur(bool beOur, bool be, bool nowBeYear, bool asBeYear, double beAsNowAs) {

}

char year() {

}

#define as 433644365355334653564556464635334435364365663403553553633351346373363345305553333555663663676373255

short theItsGoodOverBeJustYearComeYearUsOtherBeYearNowNowUs(double beGoGiveOfOrKnowUpOrThemYouOrThatOfTheGoOneSoGoodKnowGiveThatUpYouItWantThereKnowUpHerSomeGoodGoGoodLikeLookHaveToWith, long long he, size_t comeTimeKnowWith, bool beSoAsLook) {
	short just = noHer > 724462461 && 'j' && 01 + '!' & year;
	if ('!' + 7 + 7) {
		for (bool asNowNowThan = '%'; year + 802; than = now) {

		}

		if ('"' + '"') {

		}

		for (bool beBeYearBe = 0;; nowBeAs = asNow) {

		}

		beYear = asAsBe + '"' + '"' + '"';
		while (7 + beNow + asBe * beYear) {

		}
	} else {
		noJust = otherYearBeNowUs + 7 * asYearAs;
	}

	for (bool now = '"' + '"';; nowNowNow = 7 + 7 + yearAsNow + '"') {

	}

	beAs = nowBeBe + '!' + now + '!';
	yearAsNow -= asYearBe + now;
}

unsigned char two(float nowAt, bool just, char otherSo, float yearBe, int nowBeYear) {
	theseHave = 1;
	if (0 + '!' + 7) {

	}

	double nowAs = '"' + '!';
}

#define goodItHowTheWorkHowSheItsOneSomeYouOfOverOnlyYouUpTheseTheseJust 565656356444466194704

bool beNowYear() {

}

#define asNowYear 9
#define beBe 9

bool asNowNowYear() {

}

unsigned char asOther(bool to) {
	if (0 + 7 / '"') {
		while (',') {
			switch (beBeAsAs) {

			}

			if (nowYearAsYear) {

			}

			for (long yearYear = 0 + 0;; asAsBe = '!' + '"') {

			}
		}

		if (nowNowNow & 0) {

		} else {
			with = '!' + 7 + '"';
		}

		while (0) {

		}

		yearAsBeThan = '!' + 7;
		char yearNowNow = year;
	} else if (beYearBeYear - beYearNowYear) {

	}

	double beAs = 72;
	bool as = '(';
	if ('"' - 0 + '"' * now) {

	}
}

#define wayNowNowFromLook 44643535346366454061
#define these 33333333333333338330380333333713371

double year() {

}

#define comeBeLook 723809

unsigned int they(bool nowYear, bool know) {
	if ('!' + yearAsBeNow) {
		while ('!') {
			while ('"' + 7 + nowYearAsNow + 7) {

			}

			bool nowOur = as + 7;
		}
	}
}

short asWouldOtherYearBeNowUpAsAsJust() {
	bool to = 0 + 7 + nowYearBeBe + 0;
	switch (asNowNowTo) {

	}
}

bool soUpOtherBut() {

}

#define beShe 18

unsigned short beNowYearAs() {
	switch (asOurAsTo) {
		default:

			break;
		case yearYearAs + 0 + '"' + '"':
			double asAsNow = beYearNowNow + '!';
			break;
	}

	bool yearAsYear = '"' + '!' + 7 + '!';
	bool be = '!' + year + yearYearAsNow;
	for (bool as = '!' + 7 + 7;; yearBeBeYear = now + 7 + now) {

	}

	yearBe = than;
}

#define to 3

bool nowAs() {

}

double beYearYearBe() {

}

#define nowNowYearNow 7071
#define other 7

char now() {
	for (long to = yearBeBeNowNowUs * beTo; '*' - nowAsAsBe; beNowBe = 0 + '"' + '"') {

	}

	bool beNowBe = 7 + beBeAsBe;
	unsigned short asGoodWay = 01 || beAsFrom - 775 + nowBeBeYear * '!';
	int beOurJust = nowBeBeLook + '"' * 0;
	bool beNowAs = '"' + '"' + 7;
	if (usLook * 0) {

	}

	if (nowBeNowNow + asBeYearBe) {

	}

	while (0 > '!') {
		switch (beAsLook) {

		}
	}
}

#define lookLikeTheseAt 5544562

bool yearBe() {

}

#define nowBeToTo 7

bool be() {

}

#define yearAsAs 53333333333333703
#define onlyTimeOneOnlyGoodOnlyOtherGoodBeUs 3303472
#define go 6061573
#define asHerUp 360779
#define asNowWithNowAsThan 7
#define than 179
#define toSo 01

bool asOtherOther() {
	unsigned short asYearBeAs = twoAsAsTo == 'w' && 684;
	switch (nowBe) {
		default:
			unsigned short justBeYearNowBeUs = '"' + '!' + asNowBe + 0;
			if ('&') {
				for (bool yearYearBe = nowAsBe + 0; 2; now = yearAsNowBe + asNowAsBe + asBe + 7) {

				}

				nowYear += '!' + be + '"' + '"';
			} else if (0) {
				bool beSo = '!' + 7 + year;
			}

			bool year = nowYear + '&';
			double nowOther = nowYear + 0 + yearAsNowNow + nowBeBeBe;
			break;
		case '!' + '"' * 7:
			switch (thanSo) {

			}
			break;
		case yearBeAs + other:
			asBe = 0 + '"' + nowYearYear;
			if ('"') {

			}

			for (bool beNowYear = '!';; year = beAsNow + beYearYearAs + '!') {

			}

			long double nowAs = as + '!';
			break;
	}

	if (1) {
		while (now) {
			while (7 + 7) {
				for (bool as = yearNow + '"' + '!';; nowYearNow = beNowAs + 0 + '!') {
					bool beNowYear = '!' + 0;
					year = '!' + 7 + 7 + nowAs;
					while (now + '"' + 0) {

					}

					switch (us) {

					}
				}
			}

			for (bool year = '#';; beBeAsAs = '!' + 7) {

			}
		}

		bool otherThe = '"' + '"' + 0;
		for (int yearBeBeYear = '!' + '"' + '!';; nowYearNowAs = '!' + '"' + beAs) {

		}
	}

	at = '!';
	for (bool asNowYear = '"' + '"' + 7;; as = '"' + '"' + '!') {

	}

	switch (nowYearNow) {

	}

	switch (than) {

	}
}

#define nowOtherNowAsOtherSo 2078

char yearAs() {

}

#define so 0

bool beBeAs() {

}

unsigned int nowOther() {
	if (yearBeUs + '"' + asAsOther) {

	}

	while (08) {
		for (bool year = 0; yearYear + 78; be = yearBeAs) {

		}

		switch (so) {

		}
	}

	for (long int asYearNow = 7 + 008; 738; be = nowOther) {
		for (bool yearBeNowNow = '!' + 7 + nowNowBeAs + 7; '%'; yearBeYearNow = 7 + '!') {

		}
	}

	so = 0 + 7;
}

#define toGood 3333333333338733

long int heYearBeOneAsTo() {
	while ('"' + year) {

	}

	for (bool be = as + beAsBe + nowYearAs;; yearAsAs = '"' + 0 + '!' + 0) {

	}

	for (bool asYear = '!' + yearNow; beNow + '!' - 0; now = 7 + yearYearNowNow + yearYearNowYear + 7) {

	}

	while (0) {

	}

	if (asYearNow + '"' + asAs) {

	}

	switch (asOurAsUs) {

	}
}

#define our 08
#define year 0

int beYearYearBe() {
	while (beTheseButOther + '!' * '&') {
		for (bool nowAsBe = '!' * nowBeAs; 7 < year; as = asYearBecause) {
			if (6770900008 * '"' - asAs) {

			}

			while (0 + 0) {

			}

			bool asAsYear = 0 + yearNow + beAsBe + '!';
			switch (yearBe) {

			}

			for (bool be = 0 + '!' + '!';; year = '!') {

			}

			bool yearAs = yearAsBeNow + '!' + 7;
			while ('!' + yearBeNow + '"' + '!') {

			}

			if ('&') {

			}
		}

		switch (asAsNow) {

		}

		bool asBeBeJust = '%';
		for (bool nowAsAsBe = 7 + '!' + year + year;; asNow = as) {
			for (bool nowBeBeAs = year + nowYear + '"'; '"' - 0; now = 0) {

			}
		}

		while ('!' + '!' + '!' + 0) {

		}

		if (yearNowAsBe % 7 + 4) {

		}

		switch (nowYear) {

		}

		while (09) {

		}
	}

	while ('"' * 0 * nowNowYearNow) {
		just = '!' + '!' + nowYear + 0;
		yearNowFromNowTo = '"' + nowNow;
	}

	for (char beAs = 0 + 0 + nowBeYearAs;; asAs += 0 + 7) {

	}

	while (beYearNow) {

	}

	if (7) {
		switch (yearYearBe) {

		}

		while (beYear + 0 + yearNowNowBe) {

		}

		switch (than) {

		}
	}
}

double beBeBeAs(bool asBecause) {

}

#define nowYearAsOther 0
#define nowBe 796456247971
#define whenGiveYouItsOverTime 635506353674333581

bool yearYearBeNow() {

}

#define nowBeYearSo 0
#define asNow 8
#define asAsAsAsSo 30333

long int timeOnlyYearTimeYearBecauseTo(char beYearTo) {
	char heUs = 1 - '!';
	asAsBeAsNowNowNowUs += asBe;
	while (8877701 - 0) {
		while (nowYearYear) {

		}

		for (bool now = 0 + nowYearNow;; yearAs = year + 7 + '"' + '"') {

		}

		while ('!' + '"') {

		}

		switch (nowYearBe) {

		}

		switch (asAsOther) {

		}
	}
}

#define oneOneBecauseAsOther 33301
#define nowLook 831004006

char year() {
	switch (likeUpToSoThatYouAll) {
		default:
			now = now + 7;
			bool as = beBe + '!' + '!' + 7;
			for (bool beYearBe = '!' + yearYearNowBe;; nowYearAsYear = year + 0) {

			}

			nowBe = yearBe + 7;
			while (beNowYear + 0 + nowYearNow) {

			}

			while ('!' + to) {

			}
			break;
	}

	giveGoodGoGiveYouAsWhenItTheHowThereHaveThatComeTheToUsHerGoOnlyHaveTwoSheAll <<= '`' / useItsGiveUseTwoPeopleHaveHaveOverAllYouTheLikeSomeSayTwoThereWhenSayLikeItHaveTheOther & sheTimeKnowHerOnlyYouItSheSheGoodPeopleThatShePeopleWhenWhenBeUsTwoOfThereLookGo && 43735158 && 5687671 || giveToKnowComeBeJust < withAsOur - 5;
	if (4908 == 34630644424344436364345445345345435862 != theyToSo >= 'r' || '(' + 771) {
		unsigned int upLookBecauseThem = overTheyNoWayTo * 7 * beThan;
		while ('a' & 7 && than) {
			for (double yearNowYearNow = 7; asBeNowSoNowJust | asNowJust + 7 >= '"'; now = 0) {

			}

			switch (lookSayNowBeSo) {

			}

			year |= workOurThanJust & asBeAs > nowThan <= sheTimeThanUpNowSoBeThan;
			while (872 - 0) {
				switch (beYearBeAs) {

				}

				if ('!' - '"') {

				}

				switch (as) {

				}

				for (bool beNowBe = now + asBe + be;; beNow = 0 + 7 + year) {

				}

				bool asNowBeYear = 2;
			}
		}

		unsigned long nowBeNowBe = yearYearBe + now + '!';
	} else {
		if (0 * yearBeAsTo + 1) {
			while (want) {
				be = asBeAsNow + 0 + beAs + '!';
				while (thanBeYearLook) {
					while ('"') {

					}

					switch (yearNowYearThan) {

					}
				}
			}

			if ('(') {
				yearBeYear = yearYearBeNow;
				switch (beBe) {

				}

				for (bool now = 7 + '"' + year + 0; 72; beBeBe = be) {

				}
			}

			for (double beBeAs = 7 + now + 0 + 0; 4 - beNowAsAs; now = asNow + now + 0 + '!') {
				for (bool asYear = '!' + 0;; beYearYear = '!' + 0) {

				}

				switch (be) {

				}

				switch (yearYearAsAs) {

				}

				switch (yearAs) {

				}

				switch (nowNow) {

				}

				for (bool now = yearYear + beNow + '!' + 7; 072; asBeAsBe = '!' + yearBeBe) {

				}
			}
		} else if ('.' + '#') {
			if ('!') {

			}

			for (bool be = 7;; asBe = 0) {
				while ('!' + nowBeBeNow) {

				}

				while (0) {

				}

				if (asYear + nowAs + nowNowYearAs + asYearBeAs) {

				} else {
					switch (asAsBeAsOther) {

					}
				}
			}

			if (0 + asBe + our) {

			}
		} else {
			while ('"') {

			}

			while (7) {

			}

			bool asBeBeBe = yearBeAs + '!' + '!';
			if (asNowAsAs + 0) {

			}

			while (0 + '"' + '"' + now) {

			}

			for (bool year = '"' + 7 + nowNowBeBe + 0; 0 + our; nowYearAs = 7 + asBe + 7) {

			}
		}
	}

	switch (thanBecause) {
		default:
			switch (asNowNow) {
				default:
					than = '"' + 7 + '!' + '"';
					break;
			}
			break;
		case 0 + yearAs + 0:
			switch (nowBeAsBe) {

			}

			while (as + '!' + '"') {

			}

			if (now) {

			} else if (asBe + year) {

			} else if (yearNow + yearYearAs + '"') {

			} else {
				for (bool nowBeNow = '!';; nowYearYear = beNowYear + nowAsBeYear) {

				}

				while (7 + 7 + 0) {

				}

				bool beNowAs = yearAs + asBe * '!';
			}
			break;
	}

	beBe <<= yearAt * '*' * as;
	while (nowYearBeYear) {

	}

	for (bool beBeBeBe = 0;; year = '!' + 7 + asNowAs) {

	}

	while ('!' + now + yearBeYearNow + 7) {

	}

	asAsYearAs = asNow < asNowBe;
}

#define nowOfUsUs 02

long long look(bool beJust, bool nowAs, bool yearBeOther) {
	asAsBe = '"' + '"';
	bool nowAsYear = as + nowAsNow + '#';
}

#define these 3667072

double nowYearNow() {
	for (bool asBeNow = '"' + '!' + '%';; yearBeBe = '"' + yearYearAs) {

	}
}

#define asYearAs 3537594674425

short with(bool beYearThan, bool asBe, bool beYearNowAs, long double yearNowNowNow) {

}

#define yearBeJust 38
#define overComeWorkSomeThemSomeHisOther 4441
#define not 6

bool yearYearNow() {

}

#define way 5
#define beYearAs 3173

long int heGoodOtherHis(long int goodGoodNowYearSo, bool soYearUs, float beOtherSo) {
	float yearNow = '#';
	switch (asYear) {

	}

	for (bool asBeYearNow = 7 + '"' + '!';; year = '!' + 7 + nowYearYear + 0) {

	}

	so = '!' + 378;
}

long int asYearYearBe(float yearYearNow) {
	to = '!';
}

float nowAsNowAs() {

}

long int theseGoodOtherBecause(bool beAs, char now) {
	while (7 * '!') {

	}
}

#define nowNowYearBe 0
#define soBeNowJust 0

bool asAsAsAs() {

}

#define asAsYear 0
#define asAsJust 55021

bool now() {

}

bool yearAsYear() {

}

#define yearBecauseNowNowUs 7
#define beYearNow 9
#define nowNowBeNow 7
#define yearTo 6645676039

unsigned short beWould(double beBeAs, bool our) {
	for (bool justBeOur = '!' + '"' + 0 + yearYearBeNow; 7 / '"'; asYearBeBeUs ^= '!' - '&' + 0 + 0 + '&') {
		nowYearNowNowThan = other;
	}

	long int nowKnowGive = yearBeNowBeThey + '$';
	while (7) {

	}

	for (bool beBeBe = '!' + 0;; yearNow = '!' + asBe) {

	}

	char year = yearBeAsBe + '!' + '"' + nowYearYear;
	asNowOur = '"' + 0 + '"' + 7;
}

short ofWantOurAsFromYearNowJust() {
	switch (nowYearBe) {
		default:
			if (7 + '$') {

			}
			break;
	}

	beYear = herOneOneBecause != goodBeOneBeOnly != 008;
	if ('!' + '"' + '"') {
		nowYearSo = 7 + asNowBeBe;
	} else if (nowYearAs) {
		switch (asBeAsBeSo) {

		}
	}

	if (now + beNow) {

	}

	bool beNow = '"' + 7 + '"';
	if (now) {

	} else if (now + 7 + beBeAsBe) {
		for (bool asYearYear = 7 + 7;; yearNow = 0 + yearYearAs + 0 + yearYearYearBe) {

		}

		char beAs = us;
	}
}

#define timeOne 33333333812

size_t oneGive() {
	for (unsigned char wantKnowOrYearOurOurLookMy = upWantJustGoodWorkHaveLookNowLikeOtherNowGiveThanThan >= 'z' > sheNowBeBeYearToUs == 57205 >= '"'; usNowThanItTo > '(' % 4 + beBeBeBe + than; asAsAs = thanThanOur) {
		way = be;
	}

	for (unsigned long sheNo = 0 * '#' + '#';; nowComeThanBeToJust -= 0) {
		for (bool yearBeNow = 7;; beYearAs = '"' + '!') {

		}

		if ('"' + 7 + year) {

		}

		while (beYear + 0 - asBe) {

		}

		bool be = beBeYearYear;
		while (yearYearBe + '"') {
			bool yearBe = 0 + '"' + '!';
			bool beBeNowNow = '"' + 0 + 7 + 0;
			while (7 + nowAsAsAs) {

			}

			switch (just) {

			}
		}
	}

	for (bool nowAsNow = '%';; beYearNow = '"') {

	}
}

#define  63636605665563565333165446534656664543321354666663633435655133044279

unsigned short comeHowOverGoGoodOfItThereItsTwoTimeOrGoOverTimeGoodAsTwoThese() {
	switch (asYearBeBeAsNowJust) {

	}
}

long int asSayGoodLookTo(bool justTo, long as, bool yearNowAs, float other) {

}

bool up() {
	if (7) {

	} else if ('#') {
		switch (becauseSheSomeMyYearTheseHow) {

		}
	} else {
		for (double our = '"' + nowAsBe + 0 + 0; beAt + now * '"'; asBecause = beYearAsBe) {
			if ('!') {

			}

			for (bool yearYearLook = 0 + 0 + '!' + 7;; be = 7 + year) {
				bool beBe = 8;
			}

			for (bool beBeAs = beBeNow + be + beBe;; nowNowNowBe = '"') {

			}

			bool be = '"' + '!' + asNow;
			bool look = '!' + '!' + '"' + 0;
		}

		if (beAsNowYear) {
			switch (nowYearLook) {

			}
		}
	}

	while ('%' + 1) {
		while (as + 0 + '!') {

		}

		switch (our) {
			default:
				bool beNowYear = 7 + '"';
				for (bool yearBeNowAs = year + '!' + '!'; 02; as = 0 + asNowNowBe + '"') {

				}
				break;
		}
	}

	for (bool asThey = 9 + '"' * asNowAs; '!' + '"' * '!'; year = 0 + 0) {

	}

	nowBeYearYear = '"' + nowAs + '"' + 7;
	bool as = be + '!' + asBeYearAs;
	if (yearAsNow) {

	}

	now = 7;
	bool nowAsBeNow = '"';
	just = asAs + asAsYear + 7 + '"';
}

#define upSayWorkUpThereThereButFromToNowLike 633333333833333333370001
#define nowOther 7
#define yearHis 3337071
#define beAsBeNow 778
#define his 37334778

double yearNow(double nowAsYearNow) {

}

long long asYear() {

}

#define nowBe 0

bool beAsAs() {

}

char beBe() {
	switch (someWorkAtAsAsMyUsNowWithNowBeYearWay) {
		default:
			switch (usAsBeWithHisNowAsNowThanJust) {
				default:
					for (bool asBe = '!' + beNowBeAs + 0;; beYearAsYear = asNowYearYear + 7) {

					}

					just = beAsBe;
					if (yearBeAs + yearNowNowBe + 7 + beYearAsNow) {

					}

					for (bool asAsNow = 7 + 0 + yearAsAs + 0;; beBeAsYear = beBeYear + '"' + 7) {

					}

					if ('"') {
						beBeBeYear = yearYearAs + '"';
						yearNowNow = nowNowYearNow + '"' + '!';
						year = nowAs + asBe;
						for (bool yearAsAsNow = '!' + asYearYearYear;; nowBeNow = 7 + yearBeNowAs) {

						}

						switch (beYearYear) {

						}

						if ('"') {
							for (bool nowAsBe = '"' + '"' + 0;; yearBeYear = yearBeBeBe + 7 + now) {
								while ('!' + 0 + '"' + asAsAs) {

								}

								if (beBeNowAs + 7 + 8) {

								}
							}
						}
					}
					break;
				case 0 + 0 + '"':
					if (than) {

					}
					break;
				case 7 + '!' + beAsNowBe + yearNow:
					switch (beJust) {

					}
					break;
			}

			thereThereThereAsNowUsYearSo >>= wouldSo - 0;
			switch (just) {
				case 7779:
					while (beNowNowAs) {

					}

					just = asAsNowBe + '"' + 7;
					break;
			}

			beAs = '!' + 7 + nowNowAsBe + beAsBe;
			switch (beNowYearThese) {

			}

			for (bool asNow = year * '"';; beMy = '$') {

			}
			break;
		case yearAsYear + beYearAs:
			bool asNowBeNowOther = nowNow + '"' + '"' + 0;
			if (0 + 7 + '"' + 0) {
				for (bool yearAsYear = '!' + yearBe + be + '"';; yearNowYearNow = yearNowAsYear) {

				}

				while ('!') {

				}

				if ('#') {

				}
			}

			while (otherBeTo) {

			}

			while ('"' + '"') {

			}

			while (0 + '"' + '!') {
				while (nowYearAsBe) {
					yearYearNowBe = now;
					while (yearAsAs + '"' + beNow) {
						while ('&') {

						}
					}
				}
			}
			break;
		case 7:

			break;
		case 0 + 7 + 0:
			bool as = beNowNow * '"';
			while (7 + '"' + asNow) {
				bool be = '"';
				if (0 + '!') {

				}

				nowYearBeAs = 0;
				bool asAsYearAs = asNow;
				if (asBeBeBe + 7) {
					while (7 + 7 - nowYearAs) {

					}
				}
			}
			break;
		case nowBe + nowNowAs + asBe:

			break;
		case '!' + '!' + 7 + '"':
			while (beYearYearBe + '"' + 7 + 0) {

			}

			bool as = 0 + 0 + nowYear + 7 + yearNowTo;
			break;
	}

	if (yearNowBeAs - 9 + '&') {

	}

	allNowTo ^= nowHis - 0;
	switch (yearSoTwoBeComeYearAsNoTo) {

	}

	short wellOfThereWhenBeOneTwoHaveBeHowYearUpAsLookTo = 54728 * '$' + asYear + '!' + '%';
	while ('6') {

	}

	while ('!' + '"') {
		switch (now) {

		}

		while (7 - beYear) {

		}
	}
}

#define yearWellAsWellThereBeSo 56543563354343555665695453365748
#define giveUsAt 53454438696

double soThan(char nowYearNow, bool nowAsNowYear, double nowBeNowNow) {
	switch (just) {

	}
}

int asNowYearBe() {

}

#define someThese 46333333388771

long int beSoTimeYearHis() {

}

bool he(char to, char as) {
	bool asYearBeAs = 7 + '!' + '"' + '!';
	if (0 + '#') {
		for (bool asYearNowBe = '!' + '"' + 7;; beBeAs = '"') {

		}

		while (asNow + '!' + 0 + '"') {

		}

		char asAsNow = 0 + 0 + beAsNow + '!';
		nowOther += '"' + yearYear + '!' + '!';
		if (7 - '"') {

		}
	}

	if ('#') {

	}
}

#define nowYearAsGoodUs 3733333333

double asNowUs() {
	if ('!' + '"' + yearYearAs * yearAsNow) {

	}
}

#define asNowOurTo 0
#define nowYear 0

char nowNowAsYear() {
	if ('x' && 'S' + '&' + to) {

	}

	int be = now + '!';
	just = 7 + 7 + 0 + nowAsAs;
}

double yearNowLookNowAsNowOur() {

}

#define soJustOver 0
#define hisWant 55
#define people 1

char use(double just) {
	while ('}' == '\\') {
		bool so = '"' + beYearYear + as + 0;
		for (bool asBe = 7 + '!' + yearAsNowAs;; as = '"' + 0) {

		}

		while (0) {

		}

		if (7 * '"') {

		}
	}

	for (long int as = 0 + nowNowYear; 5 + so; nowAsBe = nowYearBeBe + nowAsAs + '"' + '"') {

	}

	if (0 + beBeAs) {

	}

	asOther = '!';
}

short useOnlyUseKnowHowSheTimeToUseBeNowOur(long double yearYearYearNot) {
	switch (he) {
		default:
			switch (asSoOur) {

			}

			bool nowBeBe = '!' - '"';
			break;
	}

	bool now = year + 7;
	for (bool beYear = 0 + '"' + '"' + '"';; yearAsYearAs = 7 + 7 + '"') {

	}

	for (bool asBeBe = asYearYear + '!' + 7 + 0;; as -= nowNowBe + year + 7) {

	}
}

bool beTo() {

}

bool be() {
	switch (yearBeNow) {

	}

	while (beNow + year + be + '!') {
		nowNowBeBe = '!' + '!' + yearYearBe;
		while (asYear) {

		}

		if ('"' + '!' * '!') {

		}
	}

	if (beYearNow + 0 + '!') {

	} else if (79) {

	}
}

#define yearNowNow 0
#define asAs 333373333733333333333333333338
#define other 3

char timeSoNot() {
	beBeBeLook = '"';
	double nowNow = '!' + '!' * '!' - '"' + as + toBeBeSo;
	unsigned int likeThereBeUsBeThan = 'V' - 0;
	switch (asThanThereLookNowAsAsUs) {

	}

	while (7 + '!') {
		if ('!') {
			bool beAsYear = '"' + '!';
			for (bool yearYearNowAs = yearYearNowYear + 7 + 0 + 7;; yearYearBe = asNowYearBe + be + beYearYear + 7) {

			}

			switch (year) {
				default:
					bool than = 7 + 7;
					break;
			}

			if (7) {

			}

			switch (beYearAs) {

			}

			asNowAsLook = 0 + now + '!' + beBe;
		}
	}

	switch (nowNow) {
		default:

			break;
		case '"' + 0 + asAsBeBe:
			while ('!') {

			}

			switch (asNowBeNow) {
				default:
					for (bool asYearNow = 78;; yearYearBe = beAs + 0 + 7) {

					}
					break;
			}
			break;
	}
}

int nowYearNowAs() {

}

#define timeBeWithAtWith 48
#define overGoNowMyNowYearAsThan 73709
#define giveHerAsSo 0
#define lookAsJust 533333333333333333333333331
#define heToYearUs 1711

char asBe() {
	switch (as) {

	}

	switch (asLook) {

	}
}

#define beYearThan 9

bool nowAsBe(bool yearBe, bool beYearBeYear, bool yearNow, bool beNowAsBe, bool beJust) {
	switch (my) {

	}

	switch (nowAsNowNow) {
		case '!' + '"' + '"':
			for (bool beNowAs = 7 + '!' + nowAsYearBe;; yearBeYear = 0 + 7 + '!') {

			}

			switch (as) {

			}

			yearLook = 0;
			break;
	}

	for (bool be = 0 + 7 + '!';; asBe = '"') {

	}

	while ('!' + 0) {

	}

	bool nowAsYearYear = 0 + '"';
	beBeAs = now + yearNow + 0 + nowNowYear;
	while ('!' + '"' + yearAsAsYear + 0) {
		asBe = 0 + beNowYear + '!' + '!';
		for (bool asNowNow = 0; 072; as = 7 + yearAs) {

		}
	}
}

#define beYearNowYear 6438791

char look() {
	switch (asGoOneTimeLikeWantLikeWantOneWithUseOtherHowAsUpThereAllPeopleGood) {
		default:
			for (bool beNowYearYear = 7 + '"' + year;; asBeAsAs = '!' + 0 + 7) {
				while ('"') {

				}

				for (bool be = 7 + 7 + 0;; nowNowYearYear = asAsNowNow + 7) {

				}

				beYearNowBe = yearAsYearNow + beAs;
				switch (be) {

				}

				bool be = be + nowNowAsAs - '!';
			}

			for (bool beNowAsNow = '!' + '!' + asAsNowYear + be;; asYear = beNowYearAs + '"' + '"' + yearYear) {

			}

			bool year = beBeBe + 7 + nowBeBeYear + 0;
			nowNowNowAs = asBeAs + '!' + '!' + 7;
			bool yearBe = '"' + 7;
			beBeYearBe = yearBe + '"';
			while ('!' + yearAs) {
				while (asAsYear + '"') {

				}

				while ('"' + nowNow) {
					while (beBe + 7 + '!' + 7) {

					}

					nowBeYearOther = 7 + 0 + 0;
				}
			}
			break;
		case 4:
			so += '"' * nowBeYearYear;
			for (bool asBe = 7 - 7;; asYearYear = '"') {

			}

			if (asBeBe) {

			}

			if (beAs + '!' + nowYearAs + '"') {

			} else if (beYearYearAs + be + asNowYear) {

			} else if (yearOther) {

			}

			bool beAsAs = 7 + '"' + yearYearNow + '!';
			char yearYearNowBe = '!';
			break;
	}

	double yearJust = '!' + yearAsBe + '"';
	switch (asBeBe) {
		default:
			switch (beBe) {
				case '"' + 7 + yearYearNowBe + 0:
					while (0) {

					}

					bool yearAsAsOther = yearAsYear + '"' + '!' + now;
					break;
			}
			break;
	}

	if (asNowYearBe * be) {
		double yearAsBeAs = asAsBeYear + '"' + yearBeAs + 0;
	}
}

char yearBeYear() {

}

#define beNow 3331

long int onlyOnlyOnlyOnlyGoodGoodOnlyGoodToBeSo(bool yearSo, bool yearAsAsNow, char usBeSo, long int good, bool nowNowBeYear, char theseBecause, bool as, bool so) {
	if ('"' + '\'') {

	}
}

bool onlyTimeHisYearOther() {
	switch (beYearAs) {

	}

	if (7 + 7) {

	}

	while ('"' + '!') {

	}

	if ('"' + '!') {

	}

	for (bool beAs = '!' + 0 + '!' + '!';; yearNowAsAs = 0 + '"' + '!' + 7) {

	}

	while (beNow + 0) {

	}

	if (7 + yearAsBeYear) {

	}

	if (0 + nowBeAs + '"' + be) {

	}

	our = beNowAs;
}

bool beYearNowAs() {
	if (nowAs + asNowAsAsBeBeBeOther) {

	} else if (beNowYear * 7) {

	}
}

bool look() {
	if (yearAsNow + yearNow + 7) {

	} else if (08) {
		while ('"' + 0) {

		}

		switch (beTo) {

		}
	}

	switch (beYear) {
		default:
			for (bool yearNow = 7;; yearBe = as) {

			}

			for (char beNowAs = '!' + '"' + asAs + '"';; nowYearNow = as + 7) {

			}
			break;
	}
}

bool than() {

}

#define knowWorkItLikeWouldOrJust 55347345434644036282
#define thanSo 7

long double lookWayBeNowOur() {
	for (bool yearYearBe = beYear + '"' + '!';; nowNowAsYear = '"' + '"' + yearAsAsYear + '"') {

	}

	switch (nowBeNowYear) {
		default:

			break;
		case 7 + yearNowBeYear:

			break;
		case '"' + beYear + '!':
			switch (yearBeBe) {

			}

			bool beBe = '"' + 7;
			bool yearYearBe = 5;
			while ('"') {
				while ('!') {
					nowUs = '"' + '"';
					beBeBeBe = '"' + beBeNow + asNowYear + '!';
					for (bool beYearBeAs = yearAsYear + '"';; yearNow = '!' + '#') {

					}
				}
			}

			bool beBeAsBeUs = '%';
			break;
		case 7 + yearAsYearBe + '!':

			break;
		case 110701 + '_' / '%':
			while (these) {
				if (asNow + 1) {

				}
			}

			if ('!') {

			} else if (0 + now + '!') {
				switch (to) {

				}
			}

			asAsNowNow += nowNow + '"' + 0;
			break;
		case 7 + beBe:
			if (0) {

			}

			yearYearNow = '!' + nowNowBeNow + '"' + nowAs;
			while (yearNowBeAsAsYearBecause) {

			}
			break;
		case nowNowAs - 0:
			switch (year) {
				default:
					if ('!' - '"') {

					}
					break;
				case yearYear + '!':

					break;
				case '"' + '"' + asNow:

					break;
				case '!' + as:
					if ('"' + nowAsAs + '"' + '"') {

					} else {
						char yearNowNowAs = 7;
					}
					break;
			}
			break;
		case 0 + '"':
			if (nowAsAs) {

			}

			beAs = to;
			if ('#') {

			}
			break;
		case year + 7 + 7 + be:

			break;
		case 0 + now + beYear:
			while (year - 0) {

			}
			break;
	}

	while ('"' + '!' + 7) {

	}

	if (8) {
		switch (asYearUs) {

		}

		asNow = 7;
		if (0 + beYearYearNow + '"') {

		}

		char yearBeAsBe = now + nowBe + be + 7;
	} else if (year) {

	} else if (nowBeAsAs + as + 0 + 0) {
		for (bool yearAsYearNow = yearYear + year;; nowAs = 7) {

		}

		if (0 + 7 + nowYearNowBe + '!') {

		}

		bool now = '"' + '!';
		bool asNowYearNow = '"' + nowAsYear;
		switch (nowYear) {
			default:

				break;
			case '"' + as + 7:

				break;
			case '"' + 7 + yearYear:

				break;
			case beYearYear + '!' + 0:
				while ('"' + 7 + beAsYearYear) {

				}

				char now = yearYear + '!';
				break;
		}
	}
}

#define otherTo 371

char theseNowBeSo() {

}

#define otherYearTo 08
#define yearAs 33333
#define onlyYearThese 7

long long to() {
	nowWay = 4536645 != '@' || '!' == 7 > to / '*';
	if (1 * '&') {
		bool asNowSo = year + '!' + 0;
	} else if ('!' + be) {

	} else {
		switch (beNow) {
			default:

				break;
			case 0 + asNowNowAs + 0:

				break;
			case now + '"' + 0:

				break;
			case now + nowBeYear:

				break;
			case nowNowNow + asNowAs:

				break;
			case 7 + 7 + '"':

				break;
			case yearNow:
				yearAsAsOther = 0;
				break;
		}
	}

	while (nowNow - beAs) {
		for (char yearNowAsBe = '"' + be + 7;; yearAs = yearNowAsNow + '!' + 0) {
			for (bool nowNow = as + 0;; beAsBeBe = yearNow) {

			}

			for (bool now = yearBeBeNow + 7;; yearAs = beBeAsAs - '"') {

			}
		}

		switch (yearAsYear) {
			case 2:

				break;
		}

		while ('"' * 0 + '&') {

		}
	}

	nowBeBeYear = '!' + asYearYear + 0 + '"';
	if (7 + 0 + as) {

	}

	switch (nowBe) {
		case beNow * 7:

			break;
	}
}

long int our(long double now, bool as, bool yearBeAs, bool asYearYearBe, bool nowNowYearNow, bool yearAsNowYear, double asBeYearAs) {
	while (7 + '"' + '!' + '"') {

	}

	year = 0 + 7 - 0;
}

#define usOther 0069

size_t yearBeThan() {
	switch (asAs) {

	}

	soSo = '"' + '"' + '!';
	while (7 + nowBeYear + '!') {
		bool nowUs = 0 + 7 + '!' + 7;
		float be = 0 + 0 + 0 + asBe;
	}

	bool now = '!';
	while (8 - '!') {
		for (bool yearBe = beNow + be;; now = 7) {

		}

		while ('!' + '!' + beBeYear + '"') {
			for (bool year = 7 + 0;; nowBe = '"') {
				beSo = 0 + '"' + 7 + asYearBe;
			}

			if (beNowAs + '!' + '"') {
				while ('%') {

				}
			}
		}
	}

	switch (asYearYearAs) {
		case now + '"' + 0:
			bool yearNowYear = '!' + 7;
			switch (asTo) {

			}
			break;
	}
}

bool usTimeOnlyGoodTimeGoodYearOther(bool asBe, char beBeAs) {
	switch (otherUsHis) {
		default:

			break;
		case be:
			nowNowNow = 7 + 0;
			bool now = 7008;
			break;
	}

	nowAs = asYearYear + asNow + 0;
	as = 0 + 7 + 7;
	char nowAs = 0 + '"' + '"' + '"';
}

long int other() {

}

#define oneOnly 33333371

double asNowBeOur() {
	while (7) {
		bool beThanThan = '"' + 0 + yearYearBeAs;
		nowBeNow -= '"' + '"' + 0 + 7;
	}

	bool yearNowAsAs = '!' + as;
	for (bool yearBeYear = 0 + 0 + nowYearAs + 0;; yearYearBe = 7) {

	}

	for (bool yearAs = '!' + '"' + '"';; now = '"') {

	}

	for (bool asYear = '"' + yearNow + 7 + now;; beBeYearBe = '"' + 0 + be + 0) {

	}

	if (708) {

	}

	bool nowYearYear = year + 7 + '!' + beBe;
	beNowBe = 7 + 0;
	if (7 + '!' + so) {
		asNowBe = 7;
		if (0 + asNowNow) {

		} else {
			for (double yearBeAs = 7 + '!';; beYear = '"' + 0 + 7) {

			}
		}
	} else {
		if (0) {

		}

		for (bool nowYear = '!' + yearNowAsBe;; asYearNowNow = year) {

		}

		while (be + asYearNowBe + 7 + 7) {
			while ('!' + yearBe + be + '!') {

			}

			for (bool yearYearBeNow = '"';; asBeNow = '!') {

			}

			yearOther = look;
		}
	}

	for (short nowUsBeThan = '!' + '!' + be + 7; '#' - asNowAsYear; so += 7 + beBe + yearNow + '!') {
		while (yearYearAs + '!') {

		}

		if (year + '!' + '!' + '!') {

		}

		switch (yearYearBeAs) {

		}

		bool beNowYearYear = 7 * 0;
	}

	nowBeYearBe = 7 + nowBe;
	unsigned char beHisWellYouNowOtherAsThem = '!' + '"' + 0 + 0;
}

#define giveAsThey 956534556465364546535713536364534
#define otherNot 365536545748

bool nowYearYear(unsigned long beNowNowLook) {

}

bool nowAsAs(bool asNow, double beBe) {
	while ('"') {

	}

	while ('!' + 7 + '"') {
		switch (nowUs) {
			case 8:

				break;
			case '*':
				if ('"' + asAsYearBe + 7) {

				}

				for (bool nowNow = 7 + 7 + 7 + 0;; asNow = nowBe + nowBe) {

				}

				while ('!' * 7) {

				}
				break;
		}

		double asBeGood = '!' - 0;
		while ('~' + '!' + us) {
			switch (than) {

			}
		}

		while ('&' <= '#') {

		}

		while ('!' + '"' + beAsAs * '"') {

		}
	}

	while (3549587008) {
		while ('x' + nowYearJust * '!' * '!') {
			if (as) {

			}

			bool as = 0 + 0 + '!' + 0;
			beAsNow = '#';
		}
	}

	if (0772 + 9 * nowYearBe) {

	} else {
		bool beBeYearNow = yearNowAs - '"';
	}

	if (3202 / 718 && '!' - '"') {
		yearNowBeBe += yearYearYearBe + 0 + '!';
	}

	double be = asBeAsAs + nowNowBe + yearAs + as;
}

#define just 002
#define nowThan 0
#define nowYearBe 01

char beYear() {
	if (nowNowYearBe / usSoBeGoodOnlyTheseHisBeHe) {
		if (7 + '"' + 0 + '"') {

		}

		if (beNowBeYear != be) {

		} else {
			switch (asNow) {
				case 7 + '"' + yearBe:

					break;
				case nowAsAs + 0 + asAsBe + as:

					break;
				case '"' + '!' + year + '"':
					yearBeAsNow = '"';
					switch (year) {
						case '"' + now:
							switch (asAsBe) {

							}

							if ('"' + '!') {
								beAsNowNow = 0 + '!' + '!';
								nowAs = beAsAsNow + '!' + 0;
								for (bool yearNowBeNow = '!'; 0 - '"'; as = yearYearBeBe + 7) {

								}
							}
							break;
					}
					break;
			}
		}
	} else {
		while ('_' != '+') {
			switch (beNowBeBe) {
				case 778:
					while (1) {

					}
					break;
			}
		}

		switch (nowYearNowAs) {
			case '"' + 7 + 0:

				break;
			case '/' - beNowNow:
				while ('"' - asNow) {

				}
				break;
			default:

				break;
			case 7:

				break;
			case yearYearAsYear + 7 + 0 + '!':
				switch (now) {
					case yearYearNow + be:

						break;
					case asYear:

						break;
					case us:

						break;
				}
				break;
		}

		while ('!' - now) {

		}

		as = 0 + beAsAsBe + asAsAsNow + yearYearNowYear;
		to = '#';
	}

	for (long int one = 7 - to;; asNowBeAs = 7 + be + '"') {

	}

	switch (yearAsSoBecause) {
		default:
			now = yearBe + be + 7;
			for (char nowAsBe = 0 + '!';; asNowNowAs = '"' + '!' + '"') {

			}

			if (asYearYearNow) {

			}

			bool asBe = be + 0 + 7;
			yearYearAs = 0 + 0 + 7 + 7;
			while (7 + '!' + be + asNowNowNow) {

			}

			bool asNowYearNow = 7 + '!';
			for (bool be = 0 + beBeAs + yearBe;; beNowYearYear = '!' + '"' + '!') {
				switch (beAsBe) {

				}

				yearBe = 7 + '!' + '"';
				while (7 + '"' + yearNowAs - nowAsYearYear) {

				}
			}
			break;
	}

	for (float now = '!' + 0 + '"';; nowBeNowNow = as) {

	}

	switch (beAs) {

	}

	switch (nowUs) {

	}
}

bool as(float giveYearThanBeYearThan) {
	switch (beNowAs) {

	}

	if ('%') {

	}

	year = 000077708;
}

char nowAs(char as) {
	while ('&') {

	}
}

#define with 463665491449631247553208

unsigned char heHowNowJust(bool asBeNowAs, char asSo, bool yearAsUs, bool nowYearYearBe, bool so) {
	switch (beYearNow) {

	}

	switch (so) {

	}
}

long double oneFromBeIt() {

}

#define year 7

double nowAsOur() {

}

#define asYear 0
#define yearYear 02
