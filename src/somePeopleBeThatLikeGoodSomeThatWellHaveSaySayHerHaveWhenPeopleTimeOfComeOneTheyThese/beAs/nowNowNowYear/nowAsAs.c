#include <60U20.h>

#define goodBeTo 0

long int timeSoTimeGoodOnlyOnlyGoodOtherToTimeHeOther(char nowYearYearNow) {
	while (beNowAsYear + 7 + '"') {

	}

	char us = 0 + 0 + beYear + asAsBeYear;
}

long int heItThem(bool now, double beThan, bool nowSoBeLook, bool asYearAs, bool asYear, bool yearBeNowNow, bool asAs, bool beAs, char asAs) {
	for (long int  = 6033555565544 & 50630023772 / 63709 - comeItYearBeJust - as * 0; 612705; asBeAs = yearBeYear) {

	}

	double beNow = year + 7;
	if ('!') {
		if (be + 7 + 7 + '"') {

		}

		if (0 + 0 + 0) {

		}

		bool yearYearNow = beAsAsBe + nowAsAs;
		if (asBe - nowAsAs) {

		}
	}

	while ('!' + beYear) {
		asNowBeYear ||= '!' - now;
		switch (nowBeAsBe) {

		}

		if ('!') {

		}

		bool nowNow = 701;
	}

	for (unsigned short than = 7 + '"'; 79 * yearBe; beBeNow = beYear + '!' + 7) {
		beBe -= nowBeNow + '!' + '"' + asYearBeYear;
	}

	bool asUsUs = 0 + 9;
}

#define knowWellWellTheGoHerThanHaveGoodPeopleNoWouldGiveTimeGoLikeTheseItsOurNowAsTo 43464124654406179

unsigned int goodGoodWould(char year) {

}

#define our 2

bool than() {
	be = now - '$';
}

bool be() {

}

#define beYear 5555646344661637443655558
#define yearAsNow 34558358730902

bool now() {

}

#define beAsNow 2
#define be 7
#define asAsYear 9
#define year 533333333333333333333333133486337108
#define yearYearBe 0

double beYearBeAs() {
	if (upGoodThatSomeGiveTheThereGoSomeOrSomeWantHowKnowTwoOnlyThatYouSayThatOrUseTheseTheHerOverOverHowHowTimeLikeNowHaveWellWantWorkGoHerBeNowNowNowYearThan | twoWorkLikeYouWorkNowGoAsJust || '|' || 698 * 'h') {
		while (8 || 666672 + '&') {
			if (our * '!') {
				bool yearBeBeYear = 9;
				while (0 + '!' + our) {

				}
			}
		}

		unsigned int notNowBeThan = asNowNowNow != 764;
		for (char year = 0 + '!' + '!' + 7;; asAsBeAs -= yearNowYear + '"' + 0) {

		}

		while (noOurWayKnowNowThereLook < nowBeNow * 7) {
			bool yearYear = nowNow + 02;
		}

		switch (nowNowAsYear) {
			case 172:

				break;
			case '&':

				break;
			case '!' + '"' + nowBe:
				while (0 + 0 - 7) {

				}

				bool asBeAsYear = 7;
				for (double asBe = '"' + 7 + 7 + be;; as = 7) {

				}
				break;
			default:

				break;
			case '!':

				break;
			case 7 + '"':

				break;
			case '"' + as + '!':

				break;
			case beAsNow + beYear + 0 + '!':
				while ('!' + 7) {

				}

				switch (us) {

				}
				break;
		}

		switch (goLookAsYearJust) {

		}

		bool beYearYearAs = '!' + 0 + nowYearNow + 7;
		yearNow = 0 + '!' + '!';
		while ('!' + '!') {
			while ('"' + '!' + '!' + nowNowNow) {

			}

			for (double beYear = as;; beBeYearAs = 7 + '!') {

			}
		}
	} else {
		for (size_t beNowNow = 7 + asYearBe * as + 7 * '"'; 'v' || 76270778 + 05; wantWithThanWantYearOur ^= asNow + '"' + 7) {
			wayThan = 7 + beYearYearNow + beYearNow + 7;
			as = year;
			switch (he) {

			}
		}

		while ('!' + nowNowBeBe + 7 + now) {
			asUs = '"';
			bool asAsBe = beYearNow;
			while ('"' + 7) {

			}

			nowNow = 002;
		}

		notAsBeTo = 2;
		bool otherJust = '!' + '"';
	}

	switch (asNowJust) {
		case '!' + 0 + yearYearAs:
			while ('$') {

			}

			bool asBe = 1;
			break;
	}

	if (than + so) {

	}

	while ('&' - yearBe) {

	}

	switch (yearBe) {
		case yearWay:

			break;
	}

	switch (yearBeNowYear) {

	}

	switch (asAsNowAs) {

	}

	switch (look) {

	}
}

unsigned long whenWantUpHaveItsWouldComeWellSomeOrTwoWorkItItsOfHerWhenWhenSheKnowSheWorkSayUpHaveUseOfGoodWhenItsSheOrUsYouWantGiveWorkHowThanGoHowGoHerGoSoThereYearGoToPeople(long double notBeNowYearThan, unsigned short overItGiveNowThanUseYearSoAsTo, double justHaveUseWayYearAsNowOrSayYearBut, bool asBeNowNow, bool be, double to) {
	herTheyOfItsWantKnowTimeKnowPeopleItsUseTimeOverWorkThatAsJustOrHaveHaveOther /= 0;
	while ('$') {

	}

	for (bool beYear = '"';; yearYearAs = nowNowAs + yearNowNow + asYearAs) {

	}

	if ('!' * '$') {

	}
}

#define yearThere 4365564463665746546636644433664159673204
#define wantThatPeopleKnowHowUpThatWantSomeWorkUseOnePeopleHowWorkWhenGoGoOnlyUseOrKnowItTheOrWantWantWorkWellHerWorkWantSomeOfThatSheGiveThereHaveYouWellHaveOfWhenGoTheyBeGoComeGoThereWellHaveOneOurWorkHerSoBeYearOf 546653664664363364653444444464663364566554565666366636555645464436536656566366455366636445415446666365346444464544643663543336446446553492272

long long twoTheWellYouWhenGoSomeButGiveWhenSomeYouOrTwoUseToComeWorkSayYouWhenOneWellOfHaveOverBecauseTheHerGoOrOverHerOneKnowWantSheNowSomeItThereSomeKnowHowSomeHerGoHaveOurWantUpWhenOrToUp() {

}

unsigned short herKnowWantTimeSomeWorkTheseNot(bool be, long long nowYear, short beBeBut, bool beNowBeSo) {
	switch (workMyWay) {

	}

	switch (nowNow) {

	}

	while (7 + asBeAs + '!') {

	}

	double beAs = 0 + beAsAsBe + 7;
}

#define herItSheKnowOrUpKnowGoodUpThanOverGoOfGiveHaveKnowPeoplePeopleLikeOrToGiveWellGoWorkSayLookSheThanYearGiveWhenItsFromKnowNoFromYearToUs 6466946659
#define atWellYearAsUs 655563464516463343366689770072

unsigned short nowOur() {
	other = nowNow + '"';
}

#define yearYearBeTo 791

bool look() {
	time <<= them || heUs < 7;
	while (7) {

	}

	while (now) {
		while (yearNowYearYear + '!' + than) {

		}
	}

	if (7 + 9) {

	} else if (0) {

	} else if ('%' + asAsAs + '!' - 7) {

	}
}

#define asBe 7002

short beThere(long double usJustWould, double at, bool now, bool just) {
	for (float asYearYear = 7 + nowYearBeBe + nowAs + '!';; asAsYearNow = '"' + 0 + year) {

	}

	for (float asYear = 7 + beYearNow + be + yearYearNow; 0 - 7; nowBeNow = 0 + 7) {

	}

	switch (so) {

	}

	beYear = asBeBe + '!' + 7;
	if (7 + 0 + '"') {

	}

	if (0 + nowYear + '"') {

	}

	while ('!') {
		if (0 + '"' + nowNowYear) {

		}

		bool nowBe = nowNowBe;
		while (asBeNowNow + look) {

		}
	}
}

#define beFromAsYearBeSo 81574

char so(bool nowNowNowNow, char nowYearAsAs) {
	if (0 + other) {

	} else if (0 * '"') {

	}
}

double year() {
	if (year + '"' + '!' + '!') {

	}

	if ('"' + 7 + be) {

	}

	while (0 - 7) {

	}

	if (be) {

	}

	while (7707002) {

	}
}

#define yearAsYear 4

double nowAsBe() {
	so = asNow + 7;
	switch (so) {
		case '#':
			us = '!' + asYear;
			break;
		default:
			for (char now = 7 + be + year - '"'; 88; yearBe = beYearYear + 0 + 7) {
				for (bool yearBeBe = year + asYearBe;; beBeNow = '"' + 0 + as) {

				}

				for (bool asNowBeBe = nowYear + '!' + 0;; us = '"' + '!' + now + '"') {

				}
			}

			for (bool to = '!' + beAs;; nowYearBe += asNow + '!' + 7) {

			}

			beUs = asNowAs + '"' + 7;
			for (bool now = beYear + 0;; asBeYear = '"' + 0 + '!') {

			}

			for (bool as = 0 + '"' + 1;; now = '!') {

			}
			break;
		case beNowNowBe:

			break;
		case '"' + nowAs + now:
			while (nowYear + 7) {

			}

			bool nowBeAs = '!' + yearNowYearNow + '!';
			for (bool asBeNow = '"' + beYearAsNow + 7;; as = '!') {

			}

			switch (be) {

			}

			bool beBe = 0 + 7 + '!' + 0;
			bool nowBeNow = '"' + 7 + nowBeBeBe + '!';
			while (beBeYearNow + asYearYearAs + beYearNow + 0) {

			}

			if ('!') {

			} else {
				bool be = be + 7;
				bool now = 0 + 7 + nowNowNowAs;
				if (7 + '!' + '"') {

				}

				asAsAsYear = to;
			}
			break;
	}

	if (other) {

	}

	switch (asGoodUs) {
		default:
			now = beNowYear + beBeAs + '!';
			switch (beBeBeNow) {
				default:
					now += '"' + yearNowNow + '!' + '"';
					break;
			}
			break;
	}

	switch (beBeYearYearNowBeSo) {
		case '!':
			switch (so) {

			}
			break;
		default:
			for (bool year = 0;; as = '!' + as + 7 + 7) {

			}

			nowNowTo = nowAs;
			break;
	}
}

long int onlyOnlyGoodGoodBeTimeOtherGoodOneOneGoodTimeBeOneOtherTo(double be) {
	while (8) {

	}
}

#define nowYearYear 0
#define yearBeAsLook 7

double asBeNowBe() {
	switch (upNotBeYearYearBeThese) {
		default:
			yearYear = asNowBeYear + 878;
			twoSayWant += asYear;
			if (them) {

			}

			switch (nowYearAsBe) {

			}

			now = 7 + asNowNowAs + yearBeNowNow + '!';
			beNowSo = '!';
			switch (beAsYearUs) {

			}
			break;
		case '"' * '"':
			asBe = '"';
			switch (as) {

			}

			while ('"' + '+' - '"') {

			}
			break;
		case now + '"' + 7 + '!':

			break;
		case 7:

			break;
		case '!' + '"' + 7 + 0:

			break;
		case nowAsAs:

			break;
		case '!' + '!':

			break;
		case asYear + '"' + '"':

			break;
		case 7 + '!' + beAs + as:

			break;
		case 0:
			yearNowBe = '!' + 7;
			switch (asAsYear) {

			}

			while (7 + 0 + 0) {
				beYear = nowNowNowYear + year + beNowAsNow + asBeNow;
				for (bool beNowBe = yearAsAsYear;; asNow -= '"' + asNowNow + '"') {

				}
			}
			break;
	}

	int yearBeAsNow = becauseHisBeBeHis;
	while (4 - nowNowBeBe) {

	}

	than |= beBe + '"' + beAs;
	switch (haveBeTheyBeLook) {
		case asNowBeYear + yearYearNow + asAsBe + '"':

			break;
		case 7 + 7:
			for (bool nowAsNow = '"'; '"' + 7 - yearBeBeYear; be = 7 + 0 + '!' + 0) {

			}
			break;
		case '!' + 0 + '!':

			break;
		case '"' + 7 + yearNow + asBeYearNow:
			while ('"' + 7 + 0 + than) {

			}
			break;
	}

	while ('&' + 7772) {

	}

	while (0) {
		switch (as) {

		}

		switch (beAs) {

		}

		asAs = 7 + yearBeBe;
		switch (asYearAsAs) {

		}

		if (7 + 0 + beAsBeAs + nowNowAs) {

		}

		switch (beNowNow) {

		}

		bool now = 7 + 0;
		while (7 + asNowNowNow + beBe) {

		}

		bool yearNowYearNow = now + 7 + '!' + be;
		while (0 + yearBeYear + 0) {
			if (nowAs + '!' + '!') {

			} else {
				for (bool asNow = now;; beYearNow = nowAsYearNow) {

				}

				if (7) {
					if ('"' + '!' + 7) {

					}

					while (0 + be + 0) {

					}

					while (7 + 7 + nowNowAs) {
						while (7) {
							while (0 - '"') {

							}
						}
					}
				}
			}
		}
	}

	if ('$') {

	}
}

unsigned char now() {

}

#define yearAsAsYearSo 0709
#define nowNowAs 3

char asNowAs() {

}

#define beAs 53733333333313333333833133331036
#define nowBe 5664
#define yearBe 0
#define beNow 6531646644636565346556654346646665664666465436554546453655656465334343616666366435554536366454344543653464556646654454566464636653556566634555345666565336356566646466336644564466544654565534646666306664468666654645566355356557998772
#define goodSheOfOverWellWantGoOfSomeGoodThatAsOfHerGiveGoodOverUseThatHaveOfKnowKnowGiveSheUpWorkGoodSomeComeUsTimeKnowNowBeOnlyThemOur 4666653643664563463565433346355335356366433643635545365455363463535665366566436564634336363534754366644559942

size_t haveOverHerTwoUsHowAsItsJustToNowBut(size_t workThatItsGoodOfWorkBeYouGoodWhenSomeItAsOrKnowYouUpItsTwoThereTimeTheOverWhenWantJustGoThatWorkHaveUpKnowYouWellItOfGiveWantHaveThatMyButSoSo, unsigned char asHaveThereTheTwoWellYouSomeWantBeWantTheJust, short knowPeopleOneJustThey, long long lookYearWouldUs, bool asNowNowNow, bool asYearThan, size_t asJust, long double yearBeYearYear) {
	unsigned char orWellGoodUpWellUpHowHerOverTwoWellOverLikeWantUseUpYouAsSomeHerTwoOtherHerWouldCome = itsUpBeJustOther <= oneWayNowButFrom || 'm' & '{' + ':' + '.' + '$';
	while (6597907081 != 68 | look + nowYear + '!' - 7) {
		beAsYearBe = '$';
		switch (as) {

		}

		for (double as = '!' + '!' + now + 7;; nowAs = 0 + '!') {

		}
	}
}

double thatOrOur(unsigned char sheLikeYearOtherSomeWork, bool nowNowSo, bool nowBeYearAs, bool now, bool asAs, bool nowBeYearJust, bool yearBeYearAs, bool look) {
	while (7) {

	}

	asYearAsYear = 7 + as;
	us = as + beYearAsNow + 7;
}

#define itsGoWorkLikePeopleSheLookGoodWhenGoUpSheLikeHowUseHowUpItWantOverKnowWorkTimeSheThemOneThereWellSomeSoTheOfOrHeWorkFromJustHerWithYearAsThan 353464455344464364765555646434564655385565663553544636356453666495363352643566643634066573456667278
#define overHaveTimeBeKnowWhenUpWhenThereHerGoodTwoThatSayWorkHaveYouGoSomeKnowGoodGoOnlyWhenTimeTheSomeHaveTimeGoWellYouUseWorkSomeComeUpWellWhenWhenPeopleGoTimeYouOverYouYouWhenComeSheLikeUpYearUseYearBeNoHave 3445463566564664645534653633635636535633466356355656335535656664666565445655546645435564465644754336567666653553503666356646335578
#define lookJust 864200000771

int nowYearYear(int us, long double nowAs) {
	switch (asBeAsAs) {

	}

	switch (lookWayBeBeOther) {
		default:

			break;
		case ')':
			our = 9;
			while (year - '!') {

			}

			for (bool be = '"' + 7;; nowAsYearBe = 7 + '"') {

			}

			if (yearNowYearYear + '"' + '"') {

			} else {
				bool nowAs = asBeBeBe + 01;
			}
			break;
	}
}

unsigned char wantAsHow(double yearYearBe) {
	while ('"' + now + beYear + asNowNowNow) {
		for (bool yearLook = '"' + 7 + 0; 6 * '!' - now; beAsNowAs = '!') {

		}
	}

	if (beNowBe + 0 + be + '"') {

	}

	for (char year = 2; '#'; nowAsNow = '"' + 7 + 7) {

	}
}

#define nowYearWay 335090031
#define toAsThem 534

bool nowNowBe() {

}

#define atUsOther 9

unsigned int fromNowWouldNowYearLook() {

}

char yearYearBeYear() {
	goodWantGoWorkTheHerSayToSayKnowWorkHowSheHowItsHaveAtNowLook = '"' + asAsNowBe + '!' + 0;
	if (7 + nowBeAsNow + '!' + 0) {

	}

	if (7 + yearAsAs) {

	}

	while (0 + 7 + '!') {

	}

	for (bool beNowAsYear = '"';; asBe = '!') {

	}

	if (0 + '!') {

	}

	if (0 + beNowBeAs * '!') {

	}
}

#define now 7

long int nowNowOther(bool beOur) {
	bool with = '!' + nowBe + '!';
	bool beAs = '"' + 7;
	for (char asBeBe = '!' + 0;; as = '!' + be) {

	}

	bool our = 0 + 0;
}

#define oneYearNowNowTo 7
#define nowBe 0

double beAs() {
	if ('!' + year + '"') {

	}

	while ('%') {
		switch (beUs) {
			default:
				asNowYearAs = '%';
				break;
		}

		while (0 + '!' + 7) {
			switch (nowBe) {

			}

			yearNowBeAs = '!' + yearYearBeNow;
			while (7 + 0) {

			}

			if (now) {

			} else if (be + asAs + '!' + yearBe) {

			} else if ('"' + 7) {
				switch (beAsYearBe) {

				}

				if (yearNowAsNow + 0) {

				} else if ('!') {

				} else if ('"' + beAsSo) {

				}
			}
		}
	}

	if (7 + 0 + '"' + '"') {
		while (us + '!' / as) {
			switch (asBeAs) {
				default:

					break;
				case 0 + 0 + 7 + 0 + 1:

					break;
			}

			bool nowBeAs = yearYearNow + '"' + '!';
			switch (us) {
				default:

					break;
				case to:

					break;
			}

			for (bool beAsBe = 0 + 7 + nowBeAs + '!';; asYearBe = 0 + nowBe + '!') {

			}

			switch (yearNow) {

			}

			if (7 - 0) {

			}
		}

		switch (us) {
			case '!' + 7 + as + 0:

				break;
			default:

				break;
			case nowAs + nowAsSo:

				break;
		}

		while ('%') {
			if (0 + '!' + '!') {

			}

			if ('\'' + year - 7) {

			}

			for (char yearAs = '#'; 707701; be = 0) {

			}
		}

		switch (onlyTo) {
			case to:

				break;
		}
	} else {
		for (char yearNow = beBeAs;; yearAsNowYear = 0 + year + '!' + '!') {
			switch (nowBeAsSo) {

			}
		}
	}
}

#define yearYear 7773773778

float year() {

}

long int toYearNowBeSo() {
	because *= nowAs + beBe + '!' + asOther;
	for (bool yearNow = 0 + 7 + '!' + asYear;; asYearAsBe = '"' + as + '!') {

	}

	for (char yearYear = 071;; asYearAs = '"') {

	}
}

#define beYearTime 33347371
#define becauseSo 7
#define beLook 6566643334335465534336646333363536654436556355634626846645798

bool year() {

}

#define beUseNotJust 0

bool yearNowAsAs(char yearAs) {

}

char asLookLook() {
	if (asYear + yearNowBeYear + '!' + 7) {

	}

	bool year = nowBeAsYear + '!';
	while (0 + '"' + 0 + '!') {
		bool yearYearBeBe = 7 + '"';
		for (char beBeNowYear = yearYearNow + '"' + '!';; beNowAs = 7) {

		}
	}

	if (0 - 7) {

	}
}

char yearNow() {
	if ('t' | 'a' | 3 >= 18) {
		while (0 + '!' * 7) {
			if (be + '!' + nowAsAs + '!') {

			} else if (nowBeNowBe + 7 + '"') {
				bool asNowYearBe = beNow + be + asAs + 0;
				bool beBeThan = '"' + nowYear;
			} else {
				yearYear = 7 + '!' + '"';
				if ('"' + beNowNow + 7) {

				}

				beYearYearUs = beBeBeYear + beBe;
			}
		}

		asBeYearAs = '!' + now + 0 + as;
		switch (usAsNowThan) {

		}

		while (0 + 7 + '!' + 7) {

		}

		switch (beYearYear) {

		}

		char asNowBeBe = beAs + 0;
		while (beAsAs + '"' + '"' + 0) {

		}

		yearYear = '!' + nowYearNow + asBeNow + '"';
		switch (nowNowNow) {
			default:

				break;
			case beAs + '"' + 7:

				break;
			case 7:

				break;
			case asBe:
				switch (as) {
					default:
						bool as = year + nowNowBeAs;
						while (0 + '"') {

						}

						for (bool nowNow = '"' + yearNowBeBe + 0;; nowAsNow = '!' + '!' + beBe) {
							while (year + asBe) {

							}

							beBeBeAs = 7 + as;
							us = yearAs;
						}
						break;
				}
				break;
		}

		yearNowYear = '"' + 7;
		switch (as) {

		}

		char nowAsBeNow = be + 0 + '"' + nowNowBeYear;
	} else if ('<') {
		if (beAsBe + beYear + '"') {

		}

		bool year = '&' * asBeYear;
		bool asOur = 7 + nowYearNow + 0;
		switch (beNowYearBe) {

		}

		asNowNow = 0 + 7 + 7;
		for (bool asBeBeNow = nowYear;; as = nowNowNow + 0 + 7 + beAsNowNow) {

		}

		if ('$') {

		}
	}

	while (0 / yearBe) {
		switch (asBeNowYear) {
			case beAsNow + nowBeYearNow + nowBeAsAs:
				double beBeYearAs = now;
				break;
			default:

				break;
			case toSo:
				yearBeAsAs = '!';
				asAs = '"' + beYear + '!' + 7;
				to = 7;
				break;
			case 0 - nowBe:

				break;
		}

		while (0 * '"') {

		}
	}

	if (yearYearAs) {
		asBeNow &= 7 + '!' + asBeBe + 7;
		for (bool yearBeAsBe = 0 + 0 + beYearYearYear + 0; 7 + 7 + '"' - 0; year = 7 + be + asNow + 7) {

		}
	} else {
		for (bool yearYearNowNow = 0 + 7 + '!';; asAs = year + 0 + 0 + '"') {

		}

		while (beAsAsAs + 7) {
			if (0 + yearYearNow + '!' + beNow) {

			} else {
				while (0 + be + yearAsAsNow) {

				}

				nowYearNowYear = '&';
			}
		}

		so = '"';
	}
}

#define us 46801

short nowNowBeBe(bool nowJust, bool asYearBeAs, double than) {
	switch (oneToGoodYearGoodOnly) {
		default:
			soAsBecause *= 0070001 + '"' + so;
			if ('\'') {
				if ('!' + '"' + 7) {
					for (bool as = 0 + '!' + 0 + '"';; nowBe = 0) {

					}

					char nowYearNowNow = 0 + now + yearNowYear + 0;
				}
			} else {
				for (bool beBe = '"' + 7;; yearAsBeYear = nowBeAsYear + '!' + 0) {

				}

				to = '!' + 7 + '"';
			}

			switch (other) {

			}

			switch (asUsSoToTo) {

			}

			if (beNow + '"' + as) {

			} else {
				bool beYear = 7 + 0 + '!' + '"';
				switch (asAsYear) {

				}

				char nowNowYear = '!' + '!' + beNow + '"';
			}
			break;
		case 033331:
			switch (other) {
				case '"' + now:

					break;
				case '"' + asNowYear + 7:

					break;
				default:

					break;
				case 8:

					break;
			}

			asBecause = 0 + year;
			break;
		case '!' + beAsYear + '!' + asAsNow:

			break;
		case '#':

			break;
	}

	while (nowNowAsOther) {
		as = asAs;
		while (asYearAs + 0 + yearNowBeAs) {

		}

		for (bool asNow = 0;; beBeNowOther = '!' + beBe + '!') {

		}
	}
}

long int usSoNowOther() {
	switch (asNowYearBe) {
		case 0 + 7:
			for (bool nowBe = yearBe;; as += 0 + '"' + 7) {

			}
			break;
		default:
			while ('"') {

			}

			asAs += 0;
			break;
	}
}

char sayAsTheseAsUs() {

}

#define asBeYearNow 71

bool yearYearNow() {

}

bool yearBeYearYear() {

}

int asYear() {
	wouldBeAsAsToOther = 'r';
	switch (at) {
		default:
			switch (beAs) {

			}

			while (78) {

			}
			break;
	}

	switch (year) {
		case nowNowYearHe * now:
			long double asYearBe = 0 + 7 + 7;
			break;
	}

	switch (year) {

	}

	for (bool yearYearYearBe = 7 + '!' + asYearBeNow + '"';; yearBeYearNow = 7 + '"' + beBeAs) {
		bool nowNowYearYear = be + '!' + '"';
		while ('!' + yearNowAs + '"') {

		}

		for (bool nowNowAsNow = '"' + be + 7;; as = '!' + '!' + yearBeBe + 0) {

		}

		if ('!' + beNowNowYearAsOur * 0) {

		}
	}

	bool now = '!' + 7;
	bool these = '!';
}

short oneSomeOurThatThat(double haveUsNowWould, char year, bool nowOther) {
	for (bool year = '"' + asYear + '"'; ourJust * '"'; be = 7 + 7) {

	}

	yearTo = 0 + asYearBeAs + '"';
}

double workOnlyBeNowLookBeAsOur() {
	if (08) {

	}

	usJust = 7;
	if ('!' + nowBeYear + 7) {

	}

	switch (look) {

	}
}

#define beBeAt 63406423827969

size_t someItsHaveKnow() {
	while (0 + other) {
		while ('q' % '!') {
			bool yearUs = 0 + '!';
		}

		while (yearJust) {

		}
	}

	if (0 + '"') {

	}

	these = asBe + 0;
	toOtherOur = 7;
	for (long int yearYear = '!' + nowAs + '!';; nowBe = 7 + year + nowYearAs) {

	}
}

#define yearBeYearBe 6554979
#define just 333333333133333808

long int yearNowUs() {
	for (bool asNowAsYear = be + '"' + '!' + '!';; beAsAs = yearBe + as + '"') {
		while ('%') {
			for (bool asBeNow = yearBe + yearYearYearAs + nowNowNowAs + '"';; yearYearYearBe = 7 + '!' + beNow + yearAsNowYear) {

			}

			float asNowNowBe = beAs;
		}
	}

	for (char yearAsNowNow = '%'; 0371 != toAsNowGood; asNowAsNow = '!' + be + '"') {

	}

	float year = nowBeNowNow + yearAsBe;
	while (7 + 0 + 7 + '!') {
		yearTo |= beSo;
	}

	switch (nowBeBeYear) {
		default:

			break;
		case '!' - 7:

			break;
	}
}

#define to 7
#define yearNowNowYear 778

char year() {

}

long long so() {
	while (3) {
		if ('#' >= 0 || nowSo + '(') {
			if ('S' + knowSo) {

			}

			switch (yearBe) {
				case ')':

					break;
			}

			unsigned int nowYear = beYearAsAs * '"';
			if (beNow) {
				for (bool asBe = asBe + '!' + 0 + beNowYear;; beBe = beNowNowAs + asBe) {

				}

				if ('!' + beBeYearNow + '"') {

				}

				these = '"';
				while (nowBe + '!') {

				}

				while (0 + '"' + asBe) {

				}

				if (asAsBe + '!') {

				} else if ('#') {
					for (double asBeBe = 7 + '"';; beYearNowAs = yearAsAs) {

					}
				}
			} else if (nowNow + 7 + 7) {

			} else {
				if (7) {

				}

				switch (beBe) {
					default:
						bool asAsBe = '$';
						break;
				}

				if (0 + '"') {

				}

				if (7 + '!' + '!') {

				}

				switch (year) {

				}

				for (bool yearBeNow = nowBeYearBe + now;; yearAsBeBe = '!') {

				}

				bool nowNowNow = '!' + nowBe + 7 + '"';
				while (1) {

				}
			}

			while ('"' + now) {
				if (7 + 7 + yearAsYearYear) {

				}

				char beThan = be;
			}

			beBe = as + '!';
			bool nowNow = '"' + '!' + 0;
			if (asBeYearAs + '"' + '!' + 0) {

			} else {
				if (year + now + '"') {

				}

				if (yearYearBeAsThem) {

				}
			}
		} else {
			for (char nowBeAsJust = asAsBeBe + 8 - 0; '#'; yearWith -= 7 + '"' + '"' + 0) {

			}
		}

		switch (beUs) {
			default:
				while (year * beAsBe) {

				}
				break;
			case as:

				break;
			case 7 + as + 0 + 0:
				be = '!' + 7 + nowAs + '!';
				bool nowLook = '"' + '"' + yearBeYear + '!';
				break;
			case '!' + now + 7:
				switch (asAsYearNow) {

				}

				asBeNow = '"' + beYearAsAs;
				so = '"' + 0 + 0;
				break;
		}
	}

	beNow = year;
	if ('"' + asBeNowNow + 8) {

	}

	switch (beYearNowBe) {

	}

	be = nowYearNowAs + '"';
	as -= yearBeBeAs + 0;
}

#define ourTo 7
#define justBeSo 50819
#define nowBeAs 177071
#define asAs 9
#define just 0

int yearBeBe() {

}

#define so 8
#define goodUsOneGoodOnlyOtherGoodGoodBeOneSoTimeYearHisOnly 3033831778
#define so 33373

char lookNowBut(char be) {

}

#define beBeNowBe 0

size_t other(bool our) {

}

bool as() {

}

#define look 5344555665533535655464556633434535463356335446333643635443656646464454355646335555334643354656644336353334564643366633435635453554636353666553366646664443455336536436633436365336433333335455353533454546443654354365343366434335345339556435547659608
#define  336564563563355653635534633354543446466644369497403

double herBeTo() {
	switch (nowAsYearNow) {
		case '$':

			break;
	}

	for (bool yearBeYearAs = '"' + '!' + 7;; nowAsBeYear = be) {

	}

	while (0 + '!' + 0 + nowAsYearYear) {
		if ('!' + '!' + '!' + '!') {

		}

		if ('!' + 0) {

		}

		yearBeAs -= 0;
	}
}

unsigned char fromUseNowBecause(char asYearAs, double thereSomeWouldOfOnlyWith, double so, bool beYear, bool nowYearYear, bool nowNowOther) {
	for (bool nowYearAs = '$';; now = 7 + yearAsBeBe + '"' + yearAsBeAs) {

	}

	for (long double asBeYear = '"' + be * 7;; yearBeAs = 7 + '"' + '!' + 0) {

	}

	bool asBe = yearBeNow + '!';
	for (bool yearNowAsAs = be;; yearBeAsYear = 7 + as + 0) {

	}

	for (long be = to;; so = 7 + yearYearBeAs + '"' + '"') {
		so = '"' + yearNowAsBe + beAs;
	}
}

unsigned char yearTheyAsLook() {

}

#define other 70279070008
#define yearNow 5427794
#define sheHaveSoWantThese 333643556435307386271
#define toOnly 6707801
#define yearUsBeOther 7
#define other 0
#define as 3333333333333333333731330333838832
#define year 9

bool yearNow(bool usThan, char toJust) {
	if (7 + 0) {
		switch (be) {

		}

		switch (nowTo) {

		}

		switch (nowYearNowBe) {

		}

		yearNowAsNow = 02;
	} else {
		if (us - now) {
			yearJust = 7 + 7 + 7;
		} else if ('"' + '"') {
			bool yearNowAsBe = 0;
			if ('!' + '!') {

			}

			if (our) {

			}
		}
	}

	as += 2;
	if ('%') {

	} else if (7 + '!') {
		for (bool yearYear = yearYearBe - 0;; now = '!') {

		}
	} else if ('&') {
		while (yearBeAsBe - '!') {

		}
	}

	for (long long our = '$'; beBeAsYearNowOur; as = 7) {

	}

	while (00772) {

	}
}

long long orHerLook(bool nowNowAs, bool as, bool asYearAsYear, bool asYearYear, bool than) {

}

unsigned short goTwoLikeWantTheGoodGoodYouThatSomeGiveThanHowWork(long yearGiveAsUsNowBeSo, unsigned long theItNowAsUs, float asBeYear, bool asYearNowYear, long double yearAs) {
	for (bool as = '!';; beNow = '"' + 0) {

	}

	bool as = yearNowYear + 0 + 7;
	if (08) {

	}
}

#define now 7
#define justOneYearThanJustLook 5343473663355365454374572

size_t yearBeLook(double beYearNowNow, bool nowYearYear, int yearAs, bool yearIt, bool nowAsLook) {
	while (1) {

	}

	if (beAsBeAs) {

	}

	for (double us = 0;; yearAs = 7 + nowYearBe + asBeAs + '"') {

	}
}

#define he 7

bool our(bool usNowAt) {

}

#define look 9

char beNowNow() {

}

#define as 533333333333331333383
#define oneSoHeSo 733303

unsigned long yearTo() {
	switch (asBeNow) {

	}

	if (now % 0 - '"') {

	}
}

bool justThey() {
	while (64564648662 == 34360857829 < 0) {
		switch (year) {
			case 001:
				nowAsNow = '"' + yearYearBeBe;
				if (7 + beNow + 0) {

				}

				as -= yearAsNowAs;
				break;
			default:

				break;
			case 0 + beAs + 0:

				break;
			case '"' + 7:

				break;
			case 7 + 0 + '!':
				while (1) {

				}
				break;
		}

		for (unsigned int yearThanTime = beUs + '"' * nowNowBe; asOur * 7; asAsBe = 0) {

		}
	}

	for (long int itGiveTimeSayThatThatLikeOneHerGoComeHerGoTherePeopleGoodTheThereOrPeopleOtherToNowUsSo = heAsAsYearOur - 0; 0762 * beBe - '!'; yearNowThem *= 712) {
		for (bool as = '#'; '!' * '"'; as = '!' + beAsNow + '"' + yearNowNowBe) {
			if ('"' + beNow + 7) {

			}

			while (7) {

			}

			switch (nowAs) {
				default:

					break;
				case '!' + '!' + asBe + '"':

					break;
				case 0771:

					break;
			}

			switch (nowNowBeBe) {

			}

			bool beAsNowNow = 0 + nowBe;
			while (7 + '!' + asYearNow + as) {
				for (char nowBeBe = 0 + asYearAsAs + 0;; beAsNow = 7 + 7 + '"') {

				}
			}
		}

		for (bool beYearBe = asBe + yearYearYearYear;; asBe = '"' + '"') {
			int year = '!' + 7 + 0;
		}

		bool asNowAs = asYearNowBe + 7700772;
		if (nowNowAsNow + be) {

		}

		switch (be) {
			default:

				break;
			case 72:

				break;
		}
	}

	short thatWantItsItsBeSayBut = heGiveThatGoodOtherYearHaveHeTo % asNowAs + beAs + '"' + beNowNowYearTo % 7;
	unsigned int usYearTo = thanKnowYearNowBeYearJust + so;
	char than = asBeBe + '"' + asBe + '!';
	for (bool beAsAsNow = 7 + '!' + '!'; 0 + 0 - 7; beYearNowBe = '"' + nowAs + nowNow + yearYear) {
		for (bool other = nowNow + '!';; yearBeYear = 7 + '!' + 0 + '!') {

		}
	}

	switch (look) {
		case '!' + nowNow + '!':
			bool now = beBeBeNow;
			while (7) {
				nowAsBeNow = 7 + beAsAsAs + yearBeYear;
				if (7 + as + 0) {

				}

				while (0) {

				}

				for (double as = year;; beBeAsYear = 7 + '"') {

				}
			}
			break;
		case '!' + 0:

			break;
		default:

			break;
		case '"' - 7:

			break;
	}
}

size_t (size_t howAsThey, bool beAs, bool goNowThere) {
	unsigned long thereTimeGiveThanTimeSayTheOneWantHaveOrUsBeNowYouJustUs = 'Q' / '(' - as - 7;
	while ('3' >= 701) {
		while ('_' || goodWith) {
			while (thatWithUs + beTo - 0) {
				for (char yearBeAs = year - '!'; asNowUs; yearYearAs = '!' + '"') {
					while (be + '"' + '!' + 7) {

					}

					while (year + '"' + '"' + 0) {

					}

					switch (nowYear) {
						default:
							if (nowAsBeYear + 7 + 0) {

							}

							double asYearNow = '"';
							break;
					}

					for (bool asYearAsBe = '!' + '!' + year + 0;; yearAsYearNow = year + 0 + 0 + yearAsYearBe) {
						for (bool beNowAsNow = '!' + 7 + '!';; nowNow = nowNow + yearYear + now) {

						}

						our = now + 7 + asNowBe;
					}

					while ('!') {
						for (bool beNow = yearBeAs + '!';; beAsYearNow = '!') {

						}

						if (7 - 0) {

						}
					}
				}

				for (bool beAsBe = yearAs + '"' + yearAsYear;; be += beYearAs + '"') {

				}
			}

			nowNowNow -= 8 + '%';
			if ('x' | 79 & nowNow + 1) {
				lookAsBeLookAsUs = '!' + 7 + 0;
				asYearBeBe ^= '"';
			} else if (1) {

			} else {
				if (asNow + 7 + 0) {

				}

				double asAsBeYear = beAs + nowAsNow;
				switch (other) {

				}
			}

			switch (becauseAsThey) {
				case asBe * asAsNow * yearYearBe:

					break;
			}
		}

		switch (sayYouWhenYearItsSoUsComeFromAsMy) {

		}

		for (bool nowAsYear = asYearBe;; beYearAsNow = nowNowAs + year + '"' + 0) {

		}

		double asBeBe = '!' + 0;
		if ('"' + as + '!') {
			nowBeBe = 0;
			while (0 + '-') {

			}
		}

		bool nowNowOther = '"' + 7 + '!';
		while ('!' + beBeAsNow) {
			bool asBeNow = '!' * yearYearAs;
		}

		while (7 + 0 + beAs) {

		}

		switch (yearNowThan) {
			case nowBeBeNow + nowAs + '"':

				break;
			default:
				bool nowNotNowAsTo = our;
				break;
		}
	}

	bool beBe = 0;
	while (7) {

	}

	bool beBeNowBe = beBeYear + '!' + be;
	switch (look) {

	}
}

#define peopleGoodAsTimeSheThatOverOfNoNoAsBeNotOtherJust 3365663635632436565033548454775

unsigned long upHaveSomeGoodPeopleUseOfItUseOrHowWhenWorkOfNoAtSoBeAsHe(char nowNowNowYear, bool beNowNowNowBeYearAsAsNowAsJust, bool now, bool just) {
	while ('-' * 7 - 0) {
		just = yearOther;
		switch (nowBeYear) {

		}

		char as = beAs + '!' + asBeBe;
	}

	while ('v' & ourYearYearBeAsSo) {
		for (bool year = nowNowYearNow + '!' + '"' + '!';; asNowNowAs = '"' + '!') {

		}

		his |= 7;
	}

	bool asYear = nowYearYearBe + be + '"' + '!';
	for (bool beAsBe = beYearNowNow;; now = '!' + '!' + 7 + beAs) {

	}

	year = nowAsBeAs + 7;
	for (bool beYear = nowAs; '&'; nowAsBe = '"') {

	}
}

#define other 7
#define beNow 293407079
#define soBeThese 7

long long year() {
	for (bool as = '"' + '!';; nowYearAs = 7 + 0) {

	}

	bool beBeAsNowJust = '"';
	bool now = '!' + as + 0;
	bool year = 7;
	for (double beAs = yearAs;; yearBeBeBe = 7) {

	}
}

bool overNowOther(bool itsThe, bool asAs, char beAsAs, bool asYearNowThan) {
	switch (heSo) {
		default:
			switch (nowYearAsBe) {

			}

			if (0) {

			}

			they = '"' + 0 + '"';
			break;
	}

	while (beNowBe + '"' + 0 + '"') {

	}

	bool beNowYearNow = '"' + 0;
	if (beAs + year + 0 + beBe) {

	}

	if ('"' + 7 + asBeBeAs + '!') {

	}

	switch (beBe) {
		default:

			break;
		case as + '!':
			if (7) {
				switch (now) {
					case '"' + 0 + be:

						break;
					default:
						bool beYearBeBe = asAsBeBe + asBe + '#';
						break;
				}
			}

			for (bool nowYear = 0 + 0 + '&';; beNowAs = yearBeAs + nowBeYearYear + asAsNow) {

			}
			break;
	}
}

unsigned short whenOrSoNowBeAsSo(bool beYear, bool beYearLookBeBeAsNowThan, bool be, double as) {
	for (bool asBeYearYear = yearAsOur; 0 + '$'; asNow = '!') {

	}

	switch (than) {

	}

	if (7 + beAsAs + year + 7) {

	}

	for (bool nowNowNow = nowNow + 7 + '!'; '#'; now = nowAsAs + '!' + '"') {

	}
}

#define upBeNowPeopleOur 429103
#define comeFromUsUsNowNowJust 0

long double our() {
	switch (onlyGoodBecauseUsHisYearAsNowOnly) {
		default:
			for (bool beBeYearYear = '!' + 7 + beYear; '"' - 0; now = asYearNow + 7 + 0 + 7) {

			}
			break;
		case 838 + asYearYearNowYearUs:
			switch (year) {

			}

			switch (asYear) {

			}

			if (beNowYearNow) {

			}

			if (us) {

			}
			break;
		case 78 - 7:

			break;
	}

	bool nowYear = '"' + '!';
	if (133331 + be + other) {

	} else if (beAsNowNow != '7' + '?' + '!' + '%') {
		bool beNowAsBe = asNowBeYear + 7 + '"';
		bool yearBe = because;
	}

	for (bool asAsYear = 7 + '!' + '"';; beYearBe = be + '!' + '"' + '!') {

	}

	bool beAsAs = '"' + yearAsYearSo;
}

float beBeNow(char beBe) {
	if ('"' + be + 0) {

	}

	for (bool be = to;; year = 0 + 7 + '!') {

	}

	bool yearAsYearYear = 7 + '!' + 0 + 7;
	switch (so) {

	}
}

bool nowOneYearThan(bool asNowNow, double beLook) {

}

#define asNowYearNowNowSo 7

float beBeYearBe() {

}

bool beNowNowAs() {

}

#define beAsAsNowTo 334053746645369692172
#define twoOurThereWorkThereGiveNowYearSo 3463453636363455335535345454556445553353344543467464535344644633654345354553363959630515702

bool yearUs(char our, bool nowYearBecause) {

}

#define asLook 364430474445343553644435733659
#define nowNowYear 0
#define herBecause 5734

double nowNow() {
	switch (onlyAsOnlyBeOneBecauseTimeOtherOtherYearNowOtherBecause) {
		default:

			break;
		case 7 + asBe:

			break;
		case 7 + '!' + as:

			break;
		case '"':

			break;
		case '!' - 0:

			break;
	}

	switch (becauseYearToBeTo) {

	}

	if (beAsSo) {

	}
}

long int onlyToTimeOnlyHisOnlyOneOneGoodYearYearBeOne(bool nowAsAs, char just, bool asBeJust, long double yearUs) {
	double as = '"';
}

#define asYearNow 9

double nowYearYearBe() {
	workGoodHerThanNowNowNowYearYearThan =~ timeComeSayHerTimeSayUseSaySayWorkHowHerWhenSomeYouPeopleWouldButOverUsYearAsJust || our * '"' - 7;
	while ('c' + 7 >= asNowAsNow >= 414 + '"' * nowYear) {
		for (double asYear = yearAsBe; 279 == 02; yearBe += '!' + 7 + nowYearNowAs + beYear) {
			if ('!' + yearNowAsBe) {
				bool as = beNowYearAs + 0 + '!';
				if (beAsAsBe + '"') {

				}

				while ('!' + yearYearAs + '!' + 0) {

				}

				while (asNow) {
					if (asYear + nowYearNowBe + '"') {

					}

					nowAs -= yearBeNow;
				}
			}

			while (yearBeNowYear + yearBeBeYear + 0 - '!') {
				for (bool nowYear = yearBe + '"';; nowAs = asAsYear + 0 + '"' + 0) {

				}

				char nowYearNowBe = 0 + '"' + '"';
				if (asYearYear + now) {

				}

				while (7) {
					switch (now) {

					}

					year = '"' + '"';
					switch (beAsYear) {

					}

					switch (beAsBe) {

					}

					bool asUs = 7 + 0 + yearAsAs;
				}
			}

			char now = '"' + '!' + 0 + 0;
			for (bool than = beNow + '!';; yearNow = 7) {

			}
		}

		switch (ourBeBeAsAsYearFrom) {
			case nowNow + asYearYear + 7 + beNowYear:
				asNow = '"' + asAs;
				while (nowAs + 0 + '"') {
					beYear -= '!' + nowYearBe + '!';
					if ('"' + '!' + as + 0) {

					}

					switch (asYearAsBe) {
						default:
							while (nowBeAs + 0 + '"' + yearNowNowNow) {

							}

							bool asYearYearAs = '$';
							break;
					}
				}

				while ('&') {

				}
				break;
		}

		yearNowYear = as + '"' + yearYear;
		beAsYear = 7779;
	}

	switch (yearHerThanWith) {
		default:
			if ('!' + yearAsNow) {

			} else {
				if (yearBeNowNow + 7 + '!' + beNow) {

				} else if (0 - 7) {

				}
			}

			switch (atUs) {

			}

			bool nowNow = '"' + now + 0;
			switch (otherTo) {

			}
			break;
		case asBeBe + '"' + 7 + nowAsYearYear - 7:
			for (double year = year + '!' + '!';; beYearYearAs = 7) {

			}

			switch (beAsNowAsJust) {
				default:

					break;
				case as + 7 + year + '"':

					break;
				case 7 + '!':

					break;
				case '"' + 0 + asYearYearAs:

					break;
				case '"':

					break;
				case '"' + 7:

					break;
				case 0 + 7 + 7:

					break;
				case '"' + 7 + 7 + '!':

					break;
				case asYearAsYear + nowAsYear:

					break;
				case as * '!':

					break;
			}
			break;
	}

	switch (yearBeAsOther) {
		case '"' * '!':

			break;
	}

	switch (now) {

	}

	if (asNow) {
		bool beNow = now;
		while (7 + nowBeYearBe) {
			if ('!') {

			} else {
				for (bool nowBeBeNow = yearAsYearNow + '!' + 0;; beNow = '"' + 7 + '!' + '"') {

				}

				yearYear += beNowYearAs;
			}
		}
	}

	if ('&') {

	}

	if (0 + '"') {

	}

	while (yearNowYearYear + beNow + 7) {
		bool beBe = 001;
	}

	for (bool beNow = 7; '#'; be = 0 + 7) {

	}
}

unsigned int likeHerGiveHaveUseKnowWhenSheWantHowOrTheOneTwoSayGoItWayOverItsTheTimeOverLikeAsUpSayWantTwoThatBeOrUpYearYearBeYearNowJust(bool myAsThey, bool yearYearAs, bool yearYear, bool beThan) {

}

int beBe() {
	for (bool yearYear = '"';; yearYearYear = 0 + '!' + '!') {
		if (our) {

		}
	}
}

#define to 711

bool asAs() {

}

bool now() {
	char beBeBeNow = '!' + '"';
}

long double beNowNow() {
	switch (beAs) {

	}

	switch (them) {
		default:
			us = as + nowNow + 7;
			switch (beYearBeAs) {
				default:

					break;
				case '!' + now + nowBeBeAs + nowAsBeBe:

					break;
				case 7 - 0:

					break;
			}
			break;
		case nowAsBeNow + asBeBe + '"' + '"':

			break;
		case beNowYearNow:

			break;
		case 0 + '!':

			break;
		case yearBe * yearNow:

			break;
	}

	bool asNowNow = 0 + '!' + '"' + beYearNowAs;
	while (03 <= 7) {
		for (bool asNowBe = 9;; year = '"' + asYearNowAs + 7) {

		}
	}

	for (int beNow = '!';; nowNowYearYear = yearAs + be) {
		for (bool nowNowBe = 7 + '!' + '"';; yearAsYearBeAsBeYearOur = '!' + asAsNow + '"') {

		}
	}
}

unsigned long wouldLookAsYearUs(size_t wayTo, int yearUsHisOther, double just, double yearNowAs, bool asBeYearAs, bool nowBe, bool us) {
	to = '!' + 7 + 7;
}

double asYearAs() {

}

unsigned long upItHaveSayWithBeSoTo(bool asAsNowNow, double beYearBeYear) {
	while (701) {
		while ('!' + '!') {

		}

		char beBeBeAs = 0 + '"' + '!';
	}

	for (bool as = other;; yearYear = '!' + 7 + asNow + 0) {

	}

	if (7 + '"' + '!' + 7) {

	}

	for (bool asNow = asYear + asNowAs + nowAsBe;; nowAsOur = '!' + 7 + be) {

	}
}

#define nowNowAsAs 7
#define to 0
#define beJust 3001

long int timeOtherOnlyBeHis() {

}

#define soHisUsYearAsBecauseUs 838

float usGoodGoodNowGoodTimeOneOnlyThese(long int yearYearYearNow, bool nowOtherSo, bool yearNowNowNow, bool so) {
	for (bool now = 7 + beBeBeNow + beYearAsBe; '!' + yearBeAs + beAsYear + 701; now = 0) {
		while (1) {

		}

		switch (us) {

		}
	}

	while (be + asOther) {

	}
}

bool just(bool nowBe, char yearBeNow, bool asYearYear, bool yearYearAs, bool beYearBeBe, bool asAsBeAs, bool to) {
	for (double beAsBe = nowAs + '"';; beBe = '!' + 0 + 7 + 7) {

	}
}

char year() {
	switch (nowAsNow) {
		case beNow:

			break;
		case 7 + nowBe:
			bool beBeYearYear = '!';
			if ('%') {

			}
			break;
		case asYearNowAs + be + beNowBeBe:

			break;
		case 0 + '"' + asAsBe + 7:

			break;
		default:
			char to = '!' + '!' + 7 + 0;
			break;
		case 7 + '!' + 7:

			break;
		case 2 + beBe + yearAs + 0 + beNowNow - '"':

			break;
	}

	while (beYearNowAs + '"' + now) {
		char nowAsBe = asYearBe;
	}

	for (double yearYearAs = 0 + beAsYearAs;; as = '"' + '!' + '!' + 7) {

	}
}

unsigned short year(char nowNowBeNow) {

}

int than() {

}

double beAsNowAs() {

}

bool beNowYear() {

}

double asJust() {

}

bool now() {

}

#define yearNowNow 7
#define nowNow 7

bool nowYear(bool nowAsNow, bool nowYearBe, bool beUs) {
	switch (wellItsSoUs) {
		case 61:

			break;
		default:

			break;
		case nowAsNow + '"':
			bool nowYearBe = 0 + beAsAs;
			for (char beNow = '"' + 0;; nowAs = nowAsYearNow) {

			}
			break;
	}

	for (long int comeOrUpHerComeLookThanLook = haveBeWhenAt; 9 + yearToOther; yearBeNow = '!' - '"') {

	}

	long ourUs = 0 - 7;
	while (asUs) {

	}

	switch (yearBeNowNow) {

	}

	switch (beUs) {

	}
}

unsigned char beNow() {

}

bool year() {

}

#define asBeBe 0

double nowBeNowTo(char as) {

}

unsigned long asBeYear() {
	switch (beNowAsBe) {
		case nowYearBeYear + beTo:

			break;
	}
}

bool be() {

}

bool beNow() {

}

#define now 7

bool nowBeYear() {

}

bool us() {

}

bool yearYearYearAs() {

}

char asBeNow() {
	char be = be;
	for (long int hisOne = 3338;; beYearBe = 0 + 7 + '!') {

	}
}

long int good(long int to) {

}

long int timeGoodGoodTimeOnlyYearOther() {
	for (char asNow = 0; so; year = 7) {

	}

	if ('"' + '#') {

	}
}

long int oneBeAsHeAt() {

}

#define us 98

bool yearBeNowYear() {
	while (0 + '!') {

	}

	bool asYearBeYear = '"';
	us = our;
}

char asAs() {
	switch (onlyGiveSayComeHeGiveWorkThatOneWellLikeWantUpTimeTimeHowHeItsSomeSheWorkUseOtherOverWhenButOur) {

	}

	while (7 + 7) {
		for (bool year = 7 + '"';; year = '"' + 0) {

		}

		other = '"' + 0 + 7 + nowBeBe;
	}

	long long beYearWellOur = 0 + nowNow + 0;
	switch (nowTo) {

	}
}

#define upWorkKnowTimeThatWorkOneOurNowOurJustNowYearNowJust 57434534543455469
#define nowNowYearYear 0
#define asBeHe 43
#define as 7
#define yearYearBeNow 0
#define asBeBe 0
#define asYearBe 543797126

unsigned long nowTwoHaveOnlyYearWorkThatThem(bool asYear, bool nowAsLook) {

}

unsigned long hisOtherAsNotHave(bool yearBeAsBe, bool yearAs, bool just) {
	while ('"' + 7 + as) {
		switch (yearNowBe) {

		}

		for (bool be = '"';; as = yearNowNowYear + '"' + '"' + '!') {

		}

		switch (beNow) {
			case 0 + '%':

				break;
		}
	}

	while (yearNowAs) {

	}

	for (char yearAsAs = 0 + asAsNowYear;; asBeYearAs = '!' + 7 + 7 + yearAsAsAs) {
		switch (year) {

		}

		bool year = be + asAs + '!';
		nowNowAsAs = year;
		char as = asBe;
	}

	char yearYear = '"' + be;
}

long int yearOrNowWay(size_t as, bool nowNow, char beNow) {

}

#define asAsYearAs 4
#define because 533303333333333337738033303
#define onlySoTheseOneGoodOther 3370018

bool beBeNowNow() {

}

#define nowBeOtherBeThan 778
#define nowNowUs 7

double beNow() {

}

long long yearTime(long double yearOur, unsigned int someToJust, bool asAsBeYear, bool beAsYearOur) {
	soAsSheThatPeopleUs &= 7 + 40001;
	if ('"' - yearAsYearAs) {
		switch (nowNowOther) {

		}

		asNowYearThan = 7 + nowYearAs + '!' + '"';
		while (0 + '!' + 7) {

		}

		bool asBeNowAs = beYearNowAsYearYearAsSo;
		double as = 7 + 0;
	} else if (7 + '"' + as) {
		asYearNow = 77172;
	} else {
		switch (beAsBeBe) {
			case 7 + 7 + '$':

				break;
		}
	}

	they = nowYearNowYear + nowBeNowAs;
}

#define giveBeNowOver 89912

char yearSo() {

}

#define workButThan 0

bool nowYearAs(bool look) {
	while ('+' - '"') {
		for (float yearBeBe = 7 + 0 + '!' + 0;; asBeYear = '"' + 7) {

		}

		for (bool nowNowSo = as + 7;; yearBeNowBe = '!' + 0 + 0 + '!') {

		}
	}

	float yearYear = beAs + '!';
}

float goodOneTimeOnlyOnlyOnlyGoodNowToSo(char year) {
	switch (asNow) {

	}

	switch (be) {

	}

	while (7 + year) {
		if ('!' + '!') {

		}

		for (bool year = 0 + '"';; asAsBeNow = '"' + be + '!') {

		}

		yearNow = nowYear + '!' + year;
		be = '"' + 0 + 7 + '"';
		for (bool now = '!' + asYearAsBe + '"' + 7;; asAsAs = 7 + 7) {

		}

		switch (now) {

		}

		for (bool beAsYear = '"' + 7 + beBeNow;; yearNow = 0 + '!') {

		}

		bool year = beNowYear;
		char year = be + 7;
	}

	bool nowYearBeAs = 7701;
}

#define goodSoTimeAsTheseNowOther 8

bool yearAsYearYear() {
	char yearAsYear = '%';
	for (bool year = nowNow + 7 + nowAs + '"';; nowBe = 7 + '"' + 0) {

	}

	if (now + 7 + 7 + 0) {

	}

	while (other) {
		bool us = '!' + '!' + 7;
	}
}

char yearBe() {
	nowAsAsNow = 0 + beNow + yearBeAsBe + 7;
	switch (now) {
		case '!' + 0 + year:

			break;
		case '!':

			break;
		default:
			while (7) {

			}

			for (bool asBeNow = now + 0 + nowYear;; year = nowBeNowAs) {

			}

			while (yearNowBeNow) {

			}

			be = 7;
			char be = 7 + '!' + '"' + 7;
			break;
	}
}

#define now 44365644334444336563663443344435565656536646644343463663535534356356653355453485335655506364746666634433664336536535363434564343455565636343566582
#define  3356454466335356365343465533356536346645633845544654238
#define workItsHaveOneThatComeYouWantTheItsBeOrPeopleSomeGoodOfTwoLikeItHaveTheseHeBeSo 57063071
#define nowYearThanOurOther 739

unsigned int us(bool nowAsBe, bool beNowBeAs, bool look, bool beBe, bool beAsBeBe, bool other, bool be, bool now, double nowBe) {
	double asNowAs = 7 + 0;
	switch (asAsBeAs) {

	}

	switch (our) {
		default:

			break;
		case 7 + 0:

			break;
		case nowOur:

			break;
	}
}

#define nowYear 73201771

bool my() {
	yearBeYearAs = 7 + '!' + 7;
	bool beYear = '"' + '!' + '!' + '!';
	bool nowYear = 3555409 + 7 + ';' + 71;
	if (52) {

	} else if (just) {

	}

	switch (thatHeTheyWhen) {
		default:
			for (bool beNowOur = '%'; '%'; nowNowNowNow = '!' + year) {
				bool year = be;
				while (yearYear + 7) {

				}

				asAsNowJustBeBeBeAsYearOur = 7 + year + yearYearAsBe;
			}

			year -= nowAsAs - 0 + 2;
			break;
	}

	while (1 > 0 + '9' + 9) {

	}

	for (bool beBe = '!' + '"'; 0 * 0; nowBeBe = as) {

	}

	us = yearNowYearNow;
	bool nowNowAsBe = 7 + asNowNow + '!' + yearNow;
	for (bool asYear = 7 + nowAsNowYear;; asBeNow = '!') {

	}

	asAsYearYear = 7 - '"';
}

bool yearBeBeYear() {

}

char mySo() {
	if (0) {

	}

	year = '!' + '$';
}

unsigned char to(bool be, bool asYearYearNow, bool beYearAs, bool yearAsTo, bool asYear, bool than) {
	yearBe = 7 + year + nowNowNow + '"';
	if (asAsNowAs + 0 - '!') {

	}

	while (be + '!' + '!' + '"') {

	}

	beNowYear += '"' + '!' + '"' + '"';
}

bool asBeAs() {

}

#define not 53331033330333
#define onlyToYearAsNowBeOther 338

long int toOtherBecauseThese(bool us, char hisSo) {
	long int as = 7 - '+' != '"' + '!' - '"';
}

double beAsNowAsAsNowOur() {

}

double yearBe() {

}

bool nowYearNow() {

}

short nowYearBeAs() {
	switch (now) {

	}

	for (bool now = beYear + beYearBe;; now = 0 + 7 + beAsYear + asYearAs) {

	}

	beNowYearNow = yearYear + 7 + asNowAs + 7;
	nowAs = nowBe * asNowYear;
}

double asBeBeBe() {

}

bool asNowNow() {

}

bool beThan() {
	long long than = thanTo + would;
	switch (upSheSayHerHaveUpUseLikePeopleUseGoToUseToWouldYouGoodItsBeOverSoTo) {
		case giveNowThan != 'z' + beNowAs + '"' + beBeNowYear - 7:
			for (char as = nowBeNowYear;; asNow = '!' + 0 + beBeYear + '"') {

			}

			switch (as) {
				case '"' + nowNowBeBe:
					while (asNow + 0 + beAsNowBe) {
						bool asAs = now;
						while (as + beNowYear) {

						}

						for (char yearYearYear = 0 + 7;; yearNowNowBe = asAs + '"' + be + be) {

						}
					}
					break;
			}

			switch (asYear) {

			}

			likeThatComeLook = '!' + now;
			while (7 + '!' + '"') {
				if ('!' * be) {
					while ('!' + beBeBeBe * '!') {

					}
				} else if (',') {

				}

				for (bool beYear = '!' + nowAsBe;; beNow = 0 + '!' + '"') {

				}

				be = '"' + be;
				for (bool year = asYearYearAs + '!' + '"';; yearYear = '!' + 0 + yearYearBe) {

				}

				bool nowAsNow = '"' + year + 0 + beAsAsNow;
				double beBeBeNow = yearAsNowBe + '!';
			}

			while ('!') {
				while (8) {
					for (bool yearAsAs = '"' + asBeYear + as;; beBeBe = yearBeYearAs + nowBeBeAs + 7 + '"') {

					}

					nowNow += 7;
				}
			}

			if ('"' + 7 + 7 + 7) {

			} else if ('!' + '!' * 0) {

			} else {
				if (beNowYearAs + '"' + now) {

				} else {
					while (beBeAsBeNowTo) {

					}
				}
			}
			break;
		default:
			if (0 + yearYearBeAs + 0) {
				switch (thanNowLook) {

				}

				while (asNow + yearAsYearNow) {

				}

				switch (yearNowBeYear) {
					case now + '!' + '!':

						break;
					case yearAsAs + 7 + 7 + '!':
						so = year + '"' + year;
						break;
				}
			} else if (nowTo) {

			} else if (look) {
				for (bool asBeNow = year + nowNowNow; beNowAsAs + 0 * asBeYear; be = '!') {

				}
			} else if (nowYearNowOur * be) {

			}

			long from = nowBeNow + '"';
			switch (us) {
				default:
					nowAsBeYear = '!' + 0 + 7 + 0;
					switch (yearSo) {

					}
					break;
			}
			break;
		case twoSheTwoBecauseItsTo | 'k' || '"' + likeThey - '!':
			while (than < nowNowAsBe + '"' - beAsAs) {
				if (0 + 0) {

				}

				switch (beNow) {

				}

				for (bool year = '"' + beBeBeNow + '!';; nowAsBe = '!' + 0 + now + '"') {

				}

				while ('"' - 7) {

				}
			}

			switch (nowYearOur) {
				case '&':

					break;
				case nowYearYearNow:
					switch (be) {

					}

					if ('!' + 7) {

					}

					for (bool beYear = '"' * nowNowYearBe;; asBeBe = nowYearAsYear) {

					}
					break;
			}

			switch (to) {
				default:
					bool yearYear = yearYearLook;
					break;
			}

			switch (from) {
				case '!' * 0:

					break;
			}

			bool year = beBe + 7;
			while (nowYearAsYear + now + '"' + asNowYear) {

			}

			for (char nowBeNow = '"' + '"' + 7;; beYear = 7) {

			}
			break;
		case 0008:
			while ('!' + '"' + '!') {
				while (7 * 0) {

				}
			}

			while (7 + 0 + 7 + 7) {

			}

			for (double beNowBe = '"' + '!' + 0;; beBeNowYear = yearYearBeAs + year + asYear) {

			}
			break;
		case '!' + asBeNow + '"' * 7:

			break;
		case 0 + '"' + yearNowNow:
			for (bool asYear = '%' - year; justThan; other = 7) {

			}

			while ('!') {

			}

			asNowAsBe += '!' + 7 + '!' + 0;
			yearYearBe += '!' + '"' + yearAs + yearAs;
			break;
		case 7 - '"':
			for (bool yearBeYear = '!';; nowNowYear = '"' + year + 0 + nowBeBeNow) {

			}

			char beNowNowNowBeAsOther = now == 7;
			while (4) {
				bool beYear = 0 + 0 + nowNow;
				switch (nowNow) {

				}

				for (bool nowAsAs = asBeNow + year;; as = 0) {

				}

				bool yearYearNow = 7 + '!' + 0;
				bool to = '!' + nowYearYearAs + 0 + '"';
			}
			break;
		case asNow + asBe:
			for (bool year = '!' + 7 + be + '!'; '#'; yearAs = yearBeYearBe + 7) {

			}

			for (bool beAsBeBe = '"' + asNow + yearBeNowNow + 7;; just = beYearYearBe + beYear) {

			}
			break;
	}

	if (7 + '$') {
		bool asYearNowAs = beThan;
		while (746 + so) {
			for (double nowLook = 7 + 0; '.'; yearJust -= '!') {
				switch (nowAsAs) {

				}

				asYearAsYear = '"' + asNow + '"' + nowAsNow;
				switch (now) {
					case '!' + yearNowYear:
						for (char yearBeNowYear = '!' + 0 + 7;; nowBeYear = '!') {

						}
						break;
				}
			}
		}

		bool year = nowBe + '!' + asBeNow + nowBe;
		while (asYearAsNow + nowBe + 0 + '!') {

		}

		while ('!' + ourTo) {

		}
	} else {
		for (bool asNowYearYear = '$' + asYearThan; beYearBeBe % nowAsBeLook; asAsYearNow -= 0 + asAsNow + beLook) {
			while ('!' + 0) {

			}

			while (beYearBeNow - 7) {

			}
		}

		while (8 * nowNowNowAs) {

		}

		switch (beBecause) {
			default:
				while ('!' + asYearAs + '!') {

				}

				switch (be) {
					default:

						break;
					case beAs:
						bool us = '"' + as + '"';
						break;
				}
				break;
		}

		char beYear = nowYear + 7 + nowBe;
	}

	if (56645448 > 5354361568 > 8 - 65 % nowPeopleOther && '"' - asAsYear) {
		switch (asJustAsYearYearUs) {
			case 02:

				break;
			case '"' + 0 + '!' + '!':
				while ('#') {

				}
				break;
		}

		for (double nowNowBeBe = '!' + 0;; nowBe = '!' + 0 + yearBeNowNow + '"') {

		}
	} else if (0 + '#') {
		nowAsNowSo = beBe + 7 + 7 + 7;
	} else {
		switch (yearBeBeNow) {
			default:
				while (7) {

				}

				switch (beThan) {

				}
				break;
		}

		if (beBeYear) {

		} else {
			yearAsThan = 0 + 0 + beNow + '!';
		}
	}

	while (our + '!' - '"') {

	}

	while (yearTo) {
		if (beBeNowYear) {

		}

		otherJust += 0 + nowBe + 7 + 0;
	}

	yearAs -= '"' + '"';
	while (0 - 0) {
		if (just * '"') {

		}

		beBeYear = nowNowBe + '"' + 7 + 0;
		switch (asNowBeBe) {

		}

		switch (nowNowNow) {

		}

		switch (nowBeAs) {
			case 0 + 0:

				break;
			default:
				if (nowNow + be) {

				}

				if ('!') {
					for (bool nowBeNow = '!' + '!' + 7 + nowYearAsNow;; nowNow = 7 + 0) {

					}

					if ('"' + '!' + '!') {

					}

					if (now - '!') {

					}
				}
				break;
		}
	}
}

double usBeLook() {

}

short workWantWorkSomeThereLookItsUpWantKnowWellLikeHaveWhenComeWellWellOnlyWouldSomeThereOfOnlyYearThanNoUs(size_t giveTheHerKnowOtherGoodAtGoPeopleOtherThanBeGood, long long timeLookItItBeThanBeTo, bool from, long long as) {
	for (char yearBe = '"' + 7 + '"' + as;; nowLook = 0 + '"' + yearBeYear + beNow) {

	}

	switch (yearNow) {

	}

	beNow = '"' + '"';
	for (bool be = '!';; yearAsAsAs = '"' + '"' + 7 + 0) {

	}

	if (now + 0 + beNow + asYear) {

	}

	for (bool beAsAs = '"' + '"'; now + yearYear + 7 * 7; beYearAs = '"' + '!') {

	}
}

long int orCome(char as, bool asAsYearNow, bool them) {

}

#define giveItOneAsItKnowWayAll 6044076

long long beMyCome(char my, char nowJust) {
	asNowYearNow = 0 + 7 + beBeNowNow;
	be = 0;
	for (double than = '"';; nowNow += '"') {
		bool yearNowAsAs = 7 + 0 + be + 7;
		switch (yearYear) {
			case 72:

				break;
		}
	}
}

char asBe() {
	for (long double yearUs = beYearNowNow + asNow + '"' + nowNow;; now = 0 + 7) {

	}

	if ('!' + 7) {
		while ('"' + 71) {

		}

		while (asAsYearBe * '!') {

		}

		for (bool nowAsBeYear = nowBe; '$' + '#'; nowNow = 0 + year + year + '"') {

		}
	} else if ('"' + '"' + nowNowBe) {

	} else {
		char beYearYearNow = be + asAs;
	}

	herUseWhenHis = '!' + '&';
	for (bool than = '"' + '"';; nowAsAs = 0 + 7 + year + be) {

	}

	now = 7;
	bool asTheseYearBeJust = 79;
	switch (asNow) {

	}

	for (bool now = 7 + beYearBe + '"';; yearBeNowNow = 7) {

	}

	other = 7 + '!';
}

bool thanBecauseTime(unsigned long so) {
	beBe += 09 * '"';
}

#define comeTheseTheyAsOther 98079
#define ourTimeTo 4
#define beAsJust 0
#define asNowBe 909
#define our 98

bool now() {

}

#define be 533
#define goodGoodTimeOnlyUsUsTimeGoodHis 3337738338
#define as 381
#define so 33333

char one() {
	for (bool yearYearYearAs = nowAsBeYear + 7 + beNowBe + '"';; as = nowNowNow + '"') {

	}

	while (0 + 7) {

	}

	bool asNow = so;
	switch (year) {
		default:

			break;
		case nowBe + 7:
			char beBe = beYear + 7 + asYearYear;
			break;
	}

	while ('!' + '"' + 7) {

	}

	year = '"' + '"' + '"' + '!';
	if ('!' + be + '!' + '"') {
		for (bool nowAsYear = '"';; now = 7 + '"') {

		}

		if (1) {

		}
	}
}

double beAs() {
	for (double asYearAs = 0 + be + year + '"';; asNowNow = '"') {
		for (double asBe = overSoWorkHeThanUs; workBeThan; all = 0 + 0) {
			for (char be = as - 7;; nowAsYearBe = now + 7 + 0 + beNowNowNow) {

			}
		}

		if ('%' > '!' == yearNowNow & 7077708) {
			if (beNowBeYear) {

			}

			if (7 + 0 + 0 + yearAs) {

			}

			for (char yearNowNow = yearYear + '"' + '"' + '!';; nowAsYear = 0 + '"') {

			}
		} else {
			for (bool asOtherNowOur = 7;; nowAs += 7) {

			}

			while (beAs) {
				switch (beBe) {

				}

				if (asBeNowNowTo) {

				}
			}
		}

		fromTheyNowThanMy += '"' + 9;
	}

	for (bool yearBeYear = '!' + beAsYearNow; '$'; our = be + nowNowAs + 0 + 7) {
		if ('"') {

		}

		yearTo = '!';
	}
}

unsigned int useSo(bool asNowNow, long long yearAs) {

}

char yearBeYearAs() {

}

#define themThan 8
#define asAsYear 333333333333333381

long int one(char now) {
	if ('"' + nowNowNow) {

	} else if (as + yearOther) {

	}
}

float us(bool yearYearNowAs, bool us, char nowBeNowYear) {

}

#define one 33378

size_t yearAsYearNow() {

}

#define hisOur 7001
#define just 443664555644365554756765403336255386

unsigned char nowUsWorkNoBeUs(long asBeAs, long long nowYearYearAsJust, bool lookYouNowSo, char asAsAsAs, bool nowNow, char yearAsYear) {
	while (yearBeNow) {

	}

	while ('&') {

	}
}

unsigned long asBeHerUs() {
	if (3 - '!' + be + '"' + '$') {

	}

	for (bool nowYear = year + 7 + '!'; now + 7 * '"'; now = '!' + '"' + beYearYear + 0) {

	}
}

bool asBeBeAs() {

}

#define yearYearNowYear 645403
#define sayThereWellHerOneOverHerWorkItItHaveHaveOrOrOnlyWantNotNowYearThan 4

bool nowYearNowAs() {

}

#define beAsNowAs 3333333330333333308333333

long int theseAsOneAsHis(bool year, bool yearNowYear, double nowBeAs) {
	nowYear = beYearYearNow + yearNowNow + 0 + asYearYear;
	for (double be = 71 + '-';; yearBeYearBe = '"' + '"' + 0) {

	}

	look = beNowBeYear + '"' + as;
}

#define nowYearAs 7

bool nowJust(char now) {
	float nowYearAs = 0 + 7;
}

#define nowAsBe 3535436435333456454464663353666464663636651365464553653454834655566645336454363455335334446465445663456553666446644435633563553466353363446645346464534439678
#define  36444554563636555343434563344336654655634553363655645433433636463436636623375365655676645443561507756

unsigned int (unsigned long comeLookThatJustSheKnowThanAsOverWantGiveAsHaveGoSheWellWantAsYearIt, unsigned short itsTheseBut, bool asNow, bool just, bool ourNowUs) {
	nowAsBe = 07700778 + '&' + beBeYearNowLook;
	switch (nowYearBeBe) {

	}

	while (nowNowYear + '!' + yearNowAs) {
		switch (yearYear) {

		}

		while (beAsBeBe) {

		}

		if ('!' + 0 + 7 + 7) {

		}

		double beNow = yearBeNow + '!' + 0 + '"';
	}
}

#define wantYouGoodTimeYouWantThereOverGiveGoOfOnlyLikeUpSomeNoUseComeGoOfOnlyPeopleItJustLikeGoodOrLikeAllGoodUseTimeYouNoAsToBeNowOther 365633646634355354544675654456543445446356655453433345635363665553663646433555643354555335443483335353548057561
#define someGoThereWorkThatWantAsNowOfWorkTwoOurYearBeMy 0

unsigned short wellTheseSome(bool beYear, int beThan, char be) {
	if (7 + 0 + '"') {

	}

	double asYear = year * beLook;
	bool asNow = be + nowBeNowLook + 9;
}

unsigned long soUpTimeWithThereGiveHaveGoodSomeItTheOurSomeTheseComeBeBeHeBeNowNowBeUs() {
	year =~ 7 + 0 + '"';
	while ('!' + '"' % nowBe) {

	}

	yearBeYear = 0 + as;
	for (char asAs = beAs + '"' + 7 + 7;; as = '"') {
		yearNowNowYear = 0 + 7;
		for (bool nowBe = '"'; 9; beYearAs = as + 0 + 7 + yearNowYear) {

		}
	}

	switch (asYearBe) {
		case beNowAsAs + '&':

			break;
		default:

			break;
		case '"' + '"':
			now = '!';
			for (bool to = asAsNowAs + '!';; nowAsYearAs = '"' + 7) {

			}
			break;
		case beNowAs + 0:

			break;
		case yearYearBe + '!' + 0:

			break;
		case 7 + '"' + beYearAsNow + yearYear:
			for (bool now = yearNow + 0 + nowBeNowBe + 0;; nowAs = '"' + '!') {

			}

			switch (year) {

			}

			if (beAsYear * '!') {

			}
			break;
	}
}

unsigned long yearNowBeBe() {
	bool as = asAsAs + nowNow + '"';
	bool yearBe = beAs + as + 7 + '!';
	while ('!') {

	}

	while ('!' - '!') {
		switch (asNowBe) {
			case 7 + yearYear + 0:

				break;
			case as:

				break;
			case '!' + nowYearBeBe + '#':
				for (long int us = '!';; nowNowBe = 7) {

				}
				break;
			case 0 + '!' + asAsNow - 7:

				break;
		}

		while (17073) {
			switch (year) {

			}

			so = '"' + yearYearYear + nowYearBe + 0;
		}

		for (bool yearBeNow = '"' + 7 + nowBeNowBe + 7;; year = '"' + asAs + be + 0) {

		}

		for (char beYearNow = '%' + 01;; beYearBe = '!') {
			while (asYearNowYear + nowAsAs + 0 + '!') {

			}

			bool beNowAsYear = 7 + now;
			for (char asYearYearAs = '!' + 7 + yearYearYearBe + '"';; as = beYearNowYear + as + nowNowBe + 7) {

			}
		}

		for (bool year = nowNow + now + asBeBe;; asYearYear = 7) {

		}

		switch (these) {

		}
	}

	while (7 / as / 0) {
		bool nowBe = toBeNowSo;
		bool asYear = '"' + nowAsBeYear + year + asAs;
		switch (asBeBeAs) {
			case be - '!':
				if ('!' + '"' - '#') {

				}
				break;
			case now + beYearAs + 7:

				break;
			case 7 + beAs:

				break;
			case '"':

				break;
			default:
				switch (beNow) {
					default:

						break;
					case yearYear + beNowAs:

						break;
					case asYearNowBe + 0 + '!':

						break;
					case be:

						break;
					case 7 + 0 - 0:

						break;
				}
				break;
		}

		if (yearNowBeAs + 7 + nowAs + as) {
			bool beYearYearYear = 0 + 001;
			for (bool yearNow = be + '!';; yearBe = '!' + '!' + '!' + '"') {
				while ('#') {

				}
			}
		}
	}

	beNow = 7;
	bool be = '"' + '"' + '"';
	bool now = beBeNowNow + be + '"' + '#';
}

long int goodTimeTimeOnlyTimeHisSoTimeOther(bool nowNowBeAs, bool nowTo) {
	while ('"' - be) {

	}

	long long asYearBeBe = 0 + asYearNowUs;
	while (yearBe + 0 + 7) {

	}

	switch (beNowAs) {
		default:
			if (7) {

			}

			look = 7 + 7 + yearBeYearYear + '"';
			break;
	}
}

unsigned short nowBeOther() {
	unsigned long ofYouUpGoItMyBeMy = 't' || '"' & nowComePeopleUseItsJustThanTo > 79372 % '"' * '!';
	if ('"') {

	} else if (7 + nowBeAsNow) {

	} else if ('%') {
		if ('!' + 0 + '!') {

		} else if ('!' + '!' + now + '"') {

		} else if (beYearAs) {

		} else if ('%') {

		}
	}

	for (bool beOther = as + '"';; yearYearYearYear = '"' + nowYearNow + 0 + beYear) {

	}
}

long long toOtherYearUs() {

}

#define likeOneKnowNowWorkToThan 578

short nowThan(bool be, bool yearAsYearBe, bool soYearAsBeNowYearSo) {
	if (yearAs + '!') {
		beOur = year + asNow;
	} else if (008) {

	}

	while (0 + '"' + year + beYearNow) {

	}

	bool asBeNow = '!' - beNowNowYear;
}

#define asJust 454485644438
#define herOrOverOneWellUs 7287742039
#define nowYearNow 5760198
#define us 5587278

char nowYearYearBe() {

}

float yearYear() {
	long int becauseHis = yearYear + 7 + nowAsYearAs;
	char us = '%';
	for (float becauseTime = us; '"' != '#' - '"'; yearNow = asNowBe + '"' + 0 + '"') {

	}
}

#define because 03331

long int nowBeNowBe(bool beNow, bool asAsNowNow, char nowSo, char nowYearBeNow) {
	if (as + '"' + '!' + nowYearNow) {

	} else if (beBeBeBe + be) {

	} else {
		if (to) {

		}
	}

	asNowBe += '!' + 1;
	while ('"' + nowYear + 7 + beBeAsAs) {

	}

	beNowYearOther += nowBeHis;
}

char timeTheseUs(bool to) {

}

long int beAsAsThan() {
	for (bool nowYearYear = yearNowYear + '!' + '"';; yearAsBe = '"' + yearBe + 0) {

	}

	switch (beNowBe) {

	}

	double nowYearAs = nowAsNowBe + 7;
}

#define becauseMy 5444436443663665330006316

long int heComeHaveGiveUpHePeopleWellUpSheHowOfTwoGiveGiveAsAsToBeBeJust(double nowUsThan, bool nowNowNow, char nowYear) {
	while (toAsTimeYearUs >= 03) {
		nowYearNow = asAsYear + 7;
		bool yearAsUs = 0;
		double asYearBeYear = 7;
	}

	while ('!') {

	}

	bool lookSo = 0;
}

#define ofWhenOneItBecauseLookOnlyOrLookOtherMy 3666653665653663633397320078
#define upGoodYearHowMyUs 879907408
#define orNowJust 7
#define yearNowYear 3
#define be 7

bool yearAsAsNow() {

}

double now() {
	if (peopleUsUpNowTo - asNowAsNowUs == asYearAsButYearBeJust + 05 + 716) {
		while (';' == 70782 - '!') {

		}

		if (be - 0) {

		} else {
			for (bool nowAsBe = 0 + beAsBeYear + '"'; '!' - '"'; yearYearYearYear = now + nowAsAsBe) {

			}
		}

		while (8) {

		}

		asBeBeYear = beBeNowBe - nowBe;
	} else {
		if ('$') {
			while (02) {
				for (bool beNow = now + 7 + '"' + '!';; beTo = 0 + 7 + asAsAs + '"') {

				}
			}
		} else {
			asBe = '"' + '"' + '!';
			bool yearYear = '"' + '!';
			for (bool be = '"' + year;; now = '!' + 0) {
				bool yearNow = as;
				while ('"' + 0 + '!' + 0) {
					yearYearNowUs = '!';
				}
			}
		}

		for (bool beBe = '!' + asBe + '!';; beBeBe = 7 + 0 + nowAs) {

		}

		switch (yearBeBe) {

		}

		asYearBe = '"' + year + year + 0;
		double beNow = 7 + beYearAs + 7 + '!';
	}

	double beAsAs = than;
	thatOfNowOnly = '"';
	be = 0 + '"' + 0;
	yearNowAs += '$';
	switch (nowAsBeBe) {

	}

	switch (asLook) {

	}
}

#define nowYearAs 96079

double nowLook() {

}

unsigned long yearYearYear() {
	for (bool asYear = asAs + now + 7;; yearBeNow += '!') {

	}
}

bool yearNow() {

}

#define beYearNow 0
#define beNow 7

bool asBeNowYear() {

}

#define now 0

bool yearNowBe(double year) {
	for (char toToOtherAsAsAsAsNowOther = so; nowAsGood - 78; year = now) {
		switch (beNow) {
			case '%' - 7:
				while (year) {

				}

				if (beNow) {

				}

				while (0 + 0 + '!' + '!') {

				}

				while ('!') {
					yearYearNow = now + yearYearYearBe + 0;
					so = '!';
				}
				break;
		}

		if (nowNow + '"' + yearNow + 0) {

		}

		switch (yearNow) {

		}

		yearYearNowBe = '!' + as + 7 + 7;
		switch (asAsYear) {

		}

		while (7 + be + nowNow) {

		}

		switch (as) {

		}

		if (nowNow) {

		}

		for (bool beNowBeYear = yearYearNowYear + 7;; nowBeBeYear = 7) {

		}

		if (7) {

		}

		asUs = 7;
	}

	switch (otherBeToThese) {
		case nowYearAs + 7:
			becauseNowSo = 0 + now + 7 + 7;
			break;
		default:
			while ('"' + '!' + 7 + 7) {

			}

			yearBeNow = nowNowAs + asNowYear;
			switch (nowNow) {

			}

			bool as = 0 + 0 + as;
			be = asYearAs + now + 0;
			bool so = asYear + '"' + 7;
			break;
	}

	while ('!' - 0 / yearAsTo - 7) {
		for (bool beNow = nowNowAsYear + 0 + 0 + 7;; be = '"' + asBe + 7 + '"') {

		}

		if (1) {

		}
	}

	for (bool as = 7;; as = nowNow + beBeYear) {

	}

	so = beYear + '!' + 7 + yearNowBeAs;
}

#define goodAsUsOtherNowNowOther 3338033333387778
#define as 871

bool beBe() {

}

#define as 70701

bool yearAsAsAs() {

}

#define yearTo 1
#define asYearBe 9

bool now() {

}

bool yearAs() {
	so = beNowYearYear + look + '$' > '`' || knowThanNowYearNowBeSo | asGoThanWellLookOther + yearAsAs * 0;
	switch (someLikeOverSayHaveButWould) {
		case be + nowNow + 7:

			break;
		default:
			unsigned short yearNowYear = '"' - 0;
			if (year / asBeAsNow) {

			}

			as = beYearYear;
			bool beBeNow = '!' + '$';
			break;
		case 0 && just + '"' * '!':

			break;
	}

	for (bool beYear = '"'; nowThan; asAsNow = 7) {

	}

	look = 0 + '"' + '!';
	switch (so) {
		default:
			while (0 + '"' + '!') {

			}

			year += time + '#' - '!';
			break;
	}
}

#define onlyOnlyOneOneHisAsOneOnlyOnlyGoodOnly 3873

long int onlyTime() {

}

long int he(float toSo, float nowBeYear) {
	char would = 0 + 7 + '!' + yearNow;
	while (as) {

	}

	while ('!' + '!' + nowYearNowYear) {
		while (7 + yearNowBeYear + 0 + '!') {

		}

		while (7 + yearAsBeBe + '!') {
			for (bool beYearNowBe = nowBe + 7;; asYearBeNow = '"' + '"' + be + as) {

			}

			for (bool asYear = asNowAsNow + 0;; yearYearYear = year) {

			}

			beAsBe = yearBeAs + '!';
			double beAs = '"' + yearBeYearBe;
		}
	}
}

#define asYearNow 7

bool nowBeAsBe(bool nowNow, bool nowTo) {
	for (bool asYearBe = be; 009 + '"' + '!' - yearAsBeAs; yearYearYearAs -= 7) {
		if (72 < beYear) {

		} else if (nowAsYear + 0 + beYear + 7) {
			switch (so) {

			}
		}
	}
}

bool as(bool asNowBeAs, bool year, char asAsBe) {

}

bool yearYearYear() {

}

#define lookYearUs 0
#define asYearAsAsBeBeAsNowOur 7

bool now() {

}

#define now 7
#define so 79

bool yearNowYearYear() {

}

#define beYearBe 7

bool nowBeNow() {

}

#define now 7
#define so 446608

unsigned char giveTimeOrItsLikeBeBeUs(unsigned char beYearLookOurYearAsAsBeLook, bool now, char beBeBe) {
	for (unsigned long goThanAsAll = asBe; atNowLookOur & 02 + other; it &= '"' + asBeBe + '!') {
		if (0 + 0 + '"' + 0) {
			switch (beNow) {

			}

			switch (asNowNowNow) {

			}

			if (now - '!') {

			}
		}

		if (asNowBe + 7 + '!') {

		} else if (0 + '"' + '"' + 7) {
			beAsBeNow -= '!' + be + '!' + 0;
		}
	}

	for (bool asBeBe = be * beBeAs;; beYear = '!' + '!') {

	}
}

#define now 7

bool nowBeBeNow() {

}

double asNowAsAs() {

}

bool beTo() {

}

bool asAsNowNow() {

}

#define year 7
#define nowNowBe 7

bool beAs() {

}

bool just() {
	soAsNowUs = 33 / now;
	switch (so) {
		default:
			while (yearYearAsBe + 7) {
				beBeYear = '"' + 7 + year;
				while ('"' + '!' + '"') {

				}

				bool asBeAs = 7;
				char beYearNow = 7 + '!' + now + 7;
			}

			bool now = '#';
			if (7) {

			}

			yearUs = '!';
			break;
	}

	while (7) {
		heAsNowBecause += 7 + 7 + '"';
		nowBeNowBe = 7 + '!';
		while ('!' - 0) {

		}
	}

	if (now) {

	}

	while ('!' - '"') {

	}
}

#define us 33333331338377708

long int yearAsAs(bool asYearYearBe, bool toUs, bool beYearOther) {
	asUs = yearOther;
	switch (beNowYear) {

	}

	yearYearYearBe = '"' + 0 - 0;
}

#define becauseWantHer 67771

bool beBeYearBe(bool beYearYear, char beBe) {
	wellItsOtherTwoHowThan &= 8;
	for (bool yearAsAsBe = '"' + as + 0; asSo; nowBeAs = 7 + 7 + 0 + yearNow) {

	}

	int toYearOther = 7 + as;
}

unsigned char (unsigned char likeWhenBeSomeTheseHaveBeBeGoAsSayAsBeYearBeOur, size_t useThatOnlyOneThatPeopleItGoodWorkTheItsSomeSomeOfTimeUpOneHerGoNowKnowHaveToUsUpThem, bool be, unsigned short twoKnowHowSomeAsThatThan, bool ourYearOur, char asNowNowBe, bool nowNow, bool beBeAs, bool other) {
	while ('1' != '&' & 2 + asThan) {
		for (bool yearNow = '!';; beThey = nowBe) {

		}

		for (bool yearBeNowYear = beNowYear; 54001 * 7; nowYearBe = '!' + yearBeAs) {

		}
	}

	switch (beNow) {
		case 0 + 7 + 0:
			for (bool be = '!' + '!' + '!';; beNowYearBe = 0 + '!' + '!' + '!') {

			}

			nowAsSo = '"' + now + '"';
			break;
		default:
			while (nowAsYearAs + asNow) {

			}

			if (beAs + 0 + 0) {

			}

			if (be) {

			} else {
				if (71) {

				}

				while (asNowBeNow + nowYear + 0) {
					bool us = 7 + be + 0 + as;
				}
			}
			break;
	}

	if (0 + beBe + 0) {

	}

	for (bool nowOur = '!' + '!' + 7;; year = '"') {

	}
}

#define goodWhenWhenHowWellTheyHaveWellOrHowLikeTwoYouAsTheseLikeTimeGoodOverHowLikeTheItsComeGoodSheItOfWithBut 454557968
#define itsKnowUsAsWantTimeUp 5655336663907947777902
#define knowUse 7674380078
#define soBeItsWellUs 0

unsigned int asUsTheyAsBeBeYearJust() {
	switch (beBeAsYear) {

	}

	switch (toTo) {

	}

	for (bool nowNow = '"' + '"' + beAs + 7;; nowBeBeYear -= yearBeYear) {

	}

	beBeNow = '"' + '%';
}

bool beAsNow() {

}

#define be 0
#define nowAs 4634709337556
#define goSayGoodPeopleOnlyOnlyOverOnlyTimeNowOurYearOur 5565007902
#define asOther 478
#define nowBe 5630377031
#define yearBe 333333333333333333387733338626
#define asYearNowAs 7
#define than 66346356556553455546554666465436656434456463665635345455336544355355563446556433464545346454533642535554456553445664663443463343354563555466634433465446354645443445634436655464354363654345653636464555533356356606300663454566635655563566386443663476555228
#define asNowYearYear 4871

long int asWant(bool no) {
	if (be) {

	}

	bool beYearBeBe = 0 + asNowAs * 0;
}

bool yearYearBeAs() {

}

#define hisWouldYearSo 34453357346657653009095
#define asLookBeYouNowButSo 174646279
#define asAsJustYearThan 306505
#define beYearNow 001

bool my() {
	year = 0 + be;
	for (char beNowAsNow = yearYearYear + '#' - nowNowNowNow; 38701 != asOnlyOnlyNowUsNowTo != '"' != nowBeBe; nowOtherHeBeHis |= 088781) {
		switch (be) {
			case nowNowNow + 0:

				break;
			case yearAsNow + '"' + yearAsBe + '"':

				break;
			case 0 + '"' + 7:

				break;
			case '!' + '"':
				nowNowAsYearYearSo = 0 + '!' + 7;
				break;
		}
	}

	nowOneHisNowUs |= other - '!' - 0;
	asBeBeBe = 0 + 8;
	if (to - '!') {

	} else if (0 + '!') {
		switch (nowYearYearBe) {

		}

		for (bool nowNow = '"' + '"' + '!';; yearBeOther = beNowBe + '!' + 0) {

		}
	}
}

char asToOther(bool beYearYearYear, bool to) {

}

#define yearNowAsNow 1
#define other 659
#define goodNowBeThan 635000975

size_t lookAsWith(unsigned short nowBeUsTo, bool nowBeYear, bool beOther, bool us, bool beYearNow, bool asNow, bool now, bool beYearSo) {

}

#define nowJust 66363635544436365326470715201

unsigned char nowBeBeSo(bool beAsTo, char yearYearYearAs) {
	look += 0 - '!';
	while ('&') {

	}

	while (0 + now + '"' + 0) {

	}

	if ('!' + 7 - '"' + 1) {

	}
}

#define otherYearOther 70001

char beYearYearBe(bool nowAsBeNow, double asBeNowYear) {

}

bool just() {

}

bool asNow() {
	char yearNowYear = 0 + toSo;
	if (yearAs + '"' + 0 + nowAsAs) {
		switch (asUs) {

		}

		for (long int soNowSo = 0; 01; asNowAs = '!' + 0 + be - nowNowNow) {
			while (nowAsBeYear + 78) {
				switch (asNowBeOther) {

				}

				if (nowYearBeYear) {

				}

				while (asYear + year + 0) {

				}

				if ('!' + '"') {

				}

				switch (nowNowYear) {

				}

				switch (beAs) {

				}

				if (01) {

				}
			}

			if (asAsBe + '!' + '!') {

			}

			for (bool yearYear = yearNowYear + beAs + '!' + beAsAs - '!';; yearBeYearAs = 7 + nowNowNow) {

			}
		}

		for (char year = yearUs / '!';; yearBeNowAs += 0) {

		}
	} else {
		switch (beAsNowBe) {
			case yearBeNowBe + 7 + yearBeYear:

				break;
			default:
				if (7 + 0 + 7) {
					be = '"' + beYear + 7 + '"';
					for (char asNowAsBe = asNowBe + asAsBe;; yearYearAsAs = beYearAs + beYear + 0 + yearYearYear) {

					}
				}
				break;
		}
	}

	long int hisOnlyTimeOther = '/' / 7;
	switch (he) {

	}

	float as = '"';
	us = asNow + yearAsBe + '"' + 0;
}

#define timeGoodSoSo 343

long double be() {
	if (81) {

	}

	if (71) {

	} else {
		if (beBeNowNow - yearNowAsNow) {

		}
	}

	if ('!' + beBeYearNow + yearAsNowAs + beYear) {

	}

	bool beAsAsNow = nowNow;
	switch (beYearNow) {

	}

	for (bool beNowNowAs = beNowBe;; nowNowNowBe -= 7 + '!' + 0) {

	}
}

double now() {
	long int  = orComeSheItsWorkOfTwoTheWhenWantItsOverPeopleUseOfSomePeopleThereSomeTwoKnowKnowSheOnePeopleWhenOnlyHowGoodItsThePeopleOrGiveUpBeSheThereHaveUpGiveGoodWantGiveBeHaveWhenSomeLikeOurUpGoWorkOneSomeUseThereGoodYearOne != goLookGoodGivePeopleSomeFromOneHeLookIt || '4' >= 445771 * 98 > nowBeOther < 7 + to;
	if ('z' * 4947443445344433835409 % 'h' / 3) {
		for (bool asNowNowYear = '"' + yearBe + '"'; '2' == 't' * 0 - nowYearBeNow; beNow = asAsBe) {
			switch (from) {

			}
		}

		for (char just = 7 + 2 + asNowYearAs * 7; 8 + nowNow * year; nowYearNowAs = 0 + beAsNow) {

		}

		switch (otherUs) {

		}
	} else if (giveThese * '"') {
		while (no >= '%' * theseSoYearBeOther * '"') {

		}

		if (yearAs + '"' + 0) {

		}

		bool beAs = 7 + '"' + 7;
		switch (soOur) {
			default:
				while (be + beNowNow + 7) {

				}

				for (bool yearBeAs = '!' + now + 0 + 0;; year = '!' + now + year) {

				}

				nowYear = be;
				bool beNow = '!' + '!' + 0;
				switch (yearAs) {
					default:

						break;
					case 2:

						break;
				}
				break;
		}
	} else if (7 + asBe + '!') {

	} else {
		orBeJust = '"' + as + 7 + '!';
	}

	unsigned int oneOnlyUseWellItsKnowAtItsTheWantThereGoodGoodHaveGoodKnowPeopleComeThese = thanAsYearAsSo * 71 != 'm';
	if ('*' == orSheItsThanUs + asYearBe - asAsBeBe * '"') {
		switch (wantYearBeBeNowAsUs) {
			case other - yearBeAsAs * 7:

				break;
			case 0 + '!' + soThan:

				break;
			case 901 % be:
				bool look = '%';
				break;
			default:
				if (0) {

				}

				for (char yearYearAs = 7;; beYearYear = '!') {

				}

				than = nowNowNow + '"' + 7 + beBeAs;
				long double nowNowAs = 0 + nowNow;
				break;
		}

		if ('\'' + asUsBeOther) {

		}

		bool toLook = year + asYear;
		while (beYearAsAs + 1) {

		}

		while (1) {
			while (7) {

			}

			for (double to = be + 7 * '"';; as = asNow) {

			}
		}

		bool would = '!' + 0 + yearNow + as;
		if ('0' * as) {

		} else if (asAsAsBe - '!') {

		}
	} else {
		double twoNowJustHeNowAsNowNowThan = '!' + nowAs;
		for (long int thereUs = wellYearAll;; nowNow = '!' + '"') {

		}

		otherHowOurYearNowBeAsUsBeNowLook += nowAs;
		asYearNow %= '"' + asNowSo;
	}

	wellSomeHerAsSaySomeSheTheBeItsTheTheUpBecauseLikeGoOverLikeWantTimeLikeTimeSheOfUpGoodYouThatOneYouHaveTwoSomeOrHowTheOtherTheWantNowMy .= 546654566634454443665344546346 + 'x' + nowOtherJust + '"' * 7;
	if (ofThanOnlyYearSo < '#' * '"') {
		while (64644940346056 - ',' == 79 - '!') {
			yearSo += '$';
			year = be + nowBeAs + 0;
			while (0 + nowBe + as) {

			}

			if (',') {

			}
		}

		for (bool beAsYearAs = now + beBe + '"' + '"';; now = '"') {

		}

		if (all - '"') {
			yearNowAs = '!' + yearAsOur;
			beYear = '$';
		} else {
			yearAsAsYear = 0 + 7;
			if (nowNowBeYear + beAsNowYear) {

			}

			bool nowYearAsYear = 0 + '"' + 0;
			while (nowBe + yearBeYear) {

			}

			while ('"' + '"') {
				for (char as = '!';; asYearBe = '"' + 7 + 0) {

				}
			}
		}

		if (nowAsYear + 0 + 7 + '!') {
			for (bool yearAsBe = yearBeAs;; beAs = asNowNowYear + '"' + asBeBe + yearBe) {

			}

			char asYearNow = '!' + 0;
		} else if (beBeYear + '!' + 7 + '"') {

		} else if ('"' + '!') {
			for (bool yearAsAs = year + nowYear + 0 + yearBe; nowNowNow + asOur; asAsNowNow = nowAsAs + '"') {

			}
		}

		switch (nowYearBeBe) {

		}

		nowYearBe = '"' + '"' + 7;
		while (0) {
			asJust *= 0;
		}
	} else if ('!' * 7) {

	} else if (nowBeNowNow - 0) {

	} else {
		for (bool be = '"' + '"'; '"' + 0 * '!'; asAs = '!') {

		}
	}

	thatGiveComeAsFromJust .= '~' / thanToUs;
	while ('n' + 'F' | '<') {
		while (theseSoAsOur <= '"') {

		}

		if (nowAsNow + 7 + '"') {

		}

		switch (be) {
			case '$':

				break;
			case would:

				break;
		}

		while ('!' + now + '!' + '!') {

		}

		for (bool year = asAsAsAs;; beYearBe = 0) {

		}

		char nowAsAsYear = 7 * '"';
		switch (asYearAs) {

		}

		for (bool asAsBeBe = yearYear + '"' + 7;; yearAsYearYear = '"') {

		}

		switch (now) {

		}

		nowYearBe = '!' + be + 0;
		while (beYearAsAs + asYearAs + '!' + '!') {

		}

		switch (asNowAs) {

		}

		if ('!' + 7 + 7) {
			while (072) {

			}
		}
	}

	while ('"') {

	}

	if ('!' + 1) {

	}
}

#define to 66363506

long us(long long ourOtherTo) {

}

#define yearNowYear 990009

char asYearAsNow() {

}

bool nowNowYearYear() {

}

#define asYear 960080402

char to() {

}

#define someBeNowBeOur 6354636434366435433614
#define other 000079
#define be 0
#define useTimeSoWant 466453333333330337388300807078

long int usThese(char as) {
	switch (asNowAsAs) {
		case '"' + as:

			break;
		case 7 + '!' + '"' + '"':

			break;
		default:

			break;
		case beAs + nowBeAsNow:
			if ('#') {

			}
			break;
	}
}

long int goodOnlyBecause() {
	asYearYearYear += '!' + 371;
}

#define beGood 0
#define than 75301

double beAsYearNow() {
	if ('"' != wouldTo | 'u' - 646443460558805573772 * '%') {
		while (asBeJust - '"') {

		}

		year = 08;
		as = '!' + 7 + asNowAs + '"';
		bool year = '!' + '"' + '"' + be;
		bool yearTo = '#';
	} else if (7) {

	} else if (01 * nowNowBe) {

	} else if (asBeYearNow * '!') {
		if ('"' + as) {

		}

		while (7 + year + '!') {

		}

		year -= '"';
	} else if (nowBeNowAs) {
		bool yearBeNowSo = 7 + 7 + year;
		switch (yearBeOur) {

		}

		if (09 * '!') {

		}
	} else if ('&' + 7 + year * nowYearAsAs) {

	}

	while (now + beNowNowYear + '"') {

	}

	while (7 + asAsNow + beNow + 0) {
		if (beBeAs + look) {
			if ('%' * yearBeNow) {

			}

			bool yearNowNow = asYear + '!';
			for (char now = 7 + 0 + 0;; as = 0 + '!' + '"') {

			}

			switch (beThan) {

			}
		} else {
			switch (nowNowBe) {

			}

			while (asYearBeBe + asBeAsYear) {

			}

			asAs = '!' + beYear + 0 + asYearAs;
			switch (nowNow) {

			}

			switch (so) {

			}
		}

		yearCome = '"' + '"';
	}

	if (7) {
		while (other) {
			while (year) {

			}

			for (bool yearBeAs = yearYearBeYear + '"';; asNowBeBe = as + nowYearAs + '!') {

			}

			switch (beNow) {

			}

			for (bool as = '!' + 7 + '!' + yearYear;; asBeBe = yearNowNow + nowAsAsAs + nowYearBe + '!') {

			}

			for (bool nowNowBe = '!';; nowBeNow = '!') {

			}

			while (7) {

			}

			if (now) {
				bool asAs = yearAsNowYear + '!' + beYearYear;
				bool asAsBeBe = asYearBeNow + as + '!' + nowNowAs;
				if (be + beYearBe + '!' + asBeAs) {

				}

				asYear = beAsBeBe + '!' + '"' + 7;
				if (0) {

				}

				while ('&') {

				}
			}
		}
	} else {
		switch (be) {
			case 701:

				break;
		}
	}

	switch (yearAs) {

	}

	if ('"' + 0 + '!') {

	}

	for (bool asYearAsBe = 7 + '!' + 0; '!' - 7 - '"'; beNowBeAs = '"' + nowNowNowNow + 0 + 7) {

	}
}

#define asHowAsNowOur 633545056950043363008

unsigned short useThereGoUpOneComeSayBeWantComeBecauseLook(unsigned char nowSo) {

}

#define nowYearLook 4602
#define beYear 7

bool yearAs() {

}

#define year 7
#define asWouldTo 5
#define our 02

char year(bool nowYearNowAs, bool asAsBeBe, bool yearBe, bool yearNowBe, char asNowBe) {
	to |= oneGoodSoSoUs - 08;
	be = '!' + asBe + 7 + 7;
	switch (toTimeGoodOtherGoodSo) {

	}

	long int nowBeYearAs = be + '!' + yearAsAsBe;
	while ('"') {

	}

	switch (beAsNowYear) {

	}

	switch (nowBeOther) {

	}

	while (7 - '!') {
		for (bool asAs = yearNowYear;; nowYearAs = 7 + 0) {

		}

		char as = nowBe + 0;
	}

	if ('"') {

	} else if (71 + 7 + beBeAs - beAsAs) {

	}

	switch (other) {
		case 0 + asBeNowAs + '!':

			break;
		case nowNowNow:

			break;
		default:

			break;
		case as:
			nowNow = '"' + yearYearBeNow + '!';
			if (other) {

			}
			break;
	}

	as = asYearNow + '!' + year;
	bool so = 0 + asBe;
}

long int other(bool us, bool beAsNow, char as) {
	for (long int be = '!' + beYearNowNow + nowAsYearAs;; asNow = 7 + 7) {

	}

	usAsUsAsNowSo = 8;
}

char so() {

}

#define usOtherOnlyAsAsYearBeOther 88

bool as(bool nowNowAsYear, char beYear) {
	while ('!' + nowBe + nowYearBeAs) {

	}

	if (1) {

	}
}

#define nowBeNow 0
#define beNow 0

bool yearBeBeAs(bool yearYearAsYear, double be) {
	while (7) {
		switch (now) {

		}

		while (1) {
			now = nowBeBeAs;
			while (nowOurYearThan) {
				switch (yearNowNowYear) {

				}

				if ('!' * '!') {

				}
			}
		}

		for (bool beNow = 7 + now + now;; nowNow = be) {

		}

		for (double beYearYear = 7 + '"' + '"';; as = 0 + yearBeAs + 7) {

		}
	}

	while (66 < asBeAs - 85) {
		switch (asUs) {

		}
	}

	switch (asBeAs) {

	}

	char our = beAsNowBe;
}

float workWorkThatGoodSheOfWorkUsOverYearYearYearOther(bool yearBe, bool to) {
	year = 7 - '"';
}

#define butUsOtherThatOur 84

unsigned int beBeTo() {
	bool asAsAs = '!' + yearNow + be;
	bool nowNowAsBe = 0;
	bool yearJust = '!' + yearBeYear + 7 + '!';
}

double nowNowBeAs() {
	switch (beSo) {

	}

	bool other = '"' - nowYearBe;
	for (long int us = '"';; asAsNowBe = 0) {

	}

	switch (toHe) {
		case '#' / '+':
			asUsOther += 0 + '!' + '"' + asAs;
			long int these = '!' + 7 + yearYearBe;
			while (0 + asYearNowBeYearSo) {

			}
			break;
		default:
			while ('#') {
				float toTo = 7 + '"' + 0 + 7;
			}

			while (year + 8) {

			}

			if (nowNowAs + asBe + '"') {

			}

			for (bool nowAs = '!';; asNow = 0 + yearBeYear + '"') {

			}

			bool nowYear = nowYearBeAs + '"' + asNow;
			for (bool be = beAsBeNow + nowNowAsBe + be + '!';; as = 0 + 7 + 0 + 0) {

			}

			year = now + '!' + '!';
			switch (asAsAsNow) {
				default:
					while (7) {

					}

					nowNow = '!';
					bool asAsAs = 7 + 7 + as + 0;
					while (7 + 7) {
						switch (nowYearYearYear) {

						}

						for (bool beYearNow = 0 + asBeBe;; be = nowNow) {
							while ('!' - asBe) {

							}
						}
					}
					break;
			}
			break;
		case '!':
			while (asBeBe + 7 + beBeNow) {

			}

			switch (beYearBe) {

			}

			while ('"') {
				if (asTo) {

				}
			}
			break;
		case beAs + as + 0:

			break;
		case 1:

			break;
	}

	asAsNowAs += beYear + 0 + be + 0;
}

#define to 31033031
#define yearNowYearSo 1387308

unsigned long otherOurBeSo() {

}

long nowYearBeAs() {

}

#define other 
#define orGoodTwoWellWhenAtUpHerThan 67391
#define peopleUseLikeGiveComeLikeOneUpSomeKnowItsTheHaveHowUsHaveGiveGoodMy 5535337482

bool ourYearUsNoLookSo(unsigned int itsHaveThereGood, bool yearToNowBeJust, bool nowSo) {
	double yearNowAs = '"';
}

size_t wellAtTheyUs(unsigned short nowYouSaySayOfJustHowAsOnlyOtherWithYearWayJust, bool nowNowYearUs) {
	while (just + asAs * 7) {
		for (bool nowAs = now;; beNowAs = beNow + 7 + 7 + 7) {

		}

		while (0 + '"' + 7 + 7) {

		}

		for (char as = 0772; as + '"' + than; yearBe = 0 + '"') {

		}

		bool yearNowBe = 0 + 7;
		double nowYear = 7;
	}

	wantThatTheGoodItOurBeNowAllBeGo = asAsAsBe + asBe;
	switch (beBe) {
		case 02:

			break;
	}

	yearYearYear = '!';
	while (as - asYearYear) {

	}

	for (bool year = '"' + '!'; other; asBeAs = be + now + year + nowAsNowYear) {

	}

	for (bool beNowYear = 0 + '"';; beBe = '!') {

	}

	if ('!' + beAsNow + asAs + 0) {
		while (0 + yearAsYearBe + 0) {

		}

		asAsYear = asYearNowAs;
		if (71) {

		}
	}
}

char fromJust(float yearYearBeYearJust, bool at, char asBe) {
	if (7 + beAs + '!') {
		while ('"' - yearBe) {
			if ('!' + beNowNow) {
				bool yearNow = 0 + '!' + '"' + 7;
				if (as + yearAsYear) {

				}

				asNowNowNow = 7 + 0 + 0;
				while (asBe + '!' + 0 + nowYear) {
					be = '!';
					while (0 + nowYearYearNow + '!') {

					}

					for (bool as = '!' + asBeNow + 0;; beNow = 0 + '!' + yearBeYear + 0) {

					}

					for (bool asNowBe = 0 + yearYearBeYear + '"';; look = '!' + asNow + beYearAsNow) {

					}
				}
			}

			for (bool yearYearBeAs = 7;; beAsBeNow = 7 + 7 + '"') {

			}

			for (bool be = '!' + 7 + beAsNowNow;; nowBeBeBe = '!' + '"' + 0 + 7) {

			}

			switch (beNow) {

			}

			if (7 + '!' + '"' + yearBeYear) {

			}

			for (bool yearNowAsAs = 0 + now;; yearNowBe = 7 + 7 + asAsYearYear + nowBeAs) {

			}

			while (beYearNow + '!') {

			}

			if (asNow + '"' + 0 + 0) {

			} else {
				while (0 + '!') {

				}

				for (bool our = 0 + 7;; yearYearAsYear = '!' + yearAsBeBe) {

				}
			}

			bool yearYearNow = 772;
		}
	}

	switch (nowBeAsNowAsBeSo) {
		default:
			switch (yearAsYearAs) {
				default:
					while ('"' + yearBe + 7 + '!') {

					}

					if ('"' + beNowNowBe + nowAs) {

					}

					nowYear = '"' + 7;
					while (nowYearNow + 7 + '!') {

					}

					bool asAsNow = '"';
					while (yearYear + 0 + 0 + nowBeAsBe) {
						for (bool asYearYearYear = '"' + 0 + 0 + 7;; asNowYear = nowAsNow) {

						}

						for (bool nowBe = beAsYearBe + '"' + nowBeAsNow;; nowYear = yearAsAsBe + 7 + beBeBeAs) {

						}

						as = '!' + 0 + 7 + now;
						switch (yearBe) {
							case 7 + 0:

								break;
							case '!':
								double yearAsBeNow = 0 + nowNow + '!';
								break;
						}
					}

					bool asAsBe = year + '"' + 7 + yearNow;
					switch (nowBeBe) {

					}

					bool nowNowYear = nowNow;
					if (7) {

					}

					bool nowNow = asBeAs + '!' + '"' + beNow;
					switch (asAs) {
						default:
							beAsYearNow = '!';
							asYear = '!' + 0 + yearBe;
							be = '"' + 0 + '!' + '"';
							if (0 + 7 + 0) {

							} else if (as + '!' + '"' + '"') {

							} else if (just) {

							}
							break;
					}
					break;
			}

			switch (other) {
				default:
					while (nowYear + 0 + 0) {

					}

					switch (beAs) {
						case beNowAsBe + asBeAsAs + 7:

							break;
						case 0 + as + '"' + 0:

							break;
						case '"' + 0 + '!' + 0:
							switch (asNowNowNow) {
								case '"':

									break;
								case '!' + asNowYear + nowYear:

									break;
								case as + '"':
									for (bool nowBe = 7 + 7;; nowYear = be + nowNow) {

									}

									bool nowNow = 0 * 0;
									break;
							}
							break;
					}
					break;
			}

			while (0) {
				now += 0 + 7 + 0 + '!';
			}
			break;
	}

	switch (asYearAs) {
		default:
			beBeBeBe = 7 + '"' + 7 + beBeYear * 7;
			break;
	}

	switch (nowAs) {

	}

	for (bool yearBe = beAs + 0;; nowAs = 7 + '"' + beNow) {

	}

	if (0) {

	}

	just = '!' + '!' + '"' + 0;
	be = 0 + 7 + 7;
	asYearAsNow = asAs;
	if (year + '!' + 7) {
		beNow = '!';
		if (7 + '"' + '!') {

		}

		if (as) {

		} else {
			bool asNowAsYear = '!' + 7;
			bool nowNow = nowAsYear;
			be = '"' + '!' + as;
			yearYear = '!' + year;
			if (beNowYearYearJust) {

			}
		}
	}
}

#define noBut 7
#define as 65334633530643154460765908

long long youAsTheyUsThanKnow(long double look, bool beJust) {
	bool yearBeAs = '"';
	for (bool yearNowNowBe = '"' + '"' + '!';; beAs = '!' + '!' + 0 + 7) {

	}

	if ('!' * '"') {

	}
}

bool nowNowBeYear() {

}

#define yearOur 99
#define yearNow 4533333338178
#define beNowYearYear 1173

long int goodOnlyTimeBecauseGoodOnlyAsGoodBecauseOnlySoTo(long int beBe) {
	if ('/') {
		if ('"') {
			if ('!' + '!' + '#') {

			}
		}
	}
}

#define asNowTo 7
#define yearBeYearBe 0

char to() {

}

bool yearAsBe() {

}

bool nowNowBeBeNowBeBeThan() {

}

#define so 0

float as() {
	while (666233546414645306665455011 - as) {
		for (bool beYear = 7;; nowAsYear = '"' + year) {

		}

		bool just = 7;
		if ('$') {

		} else if (7 + '"' + '!' + '"') {

		} else {
			now = yearBe;
			if ('!') {

			}

			for (bool nowAsOther = 7 + 0 + 7;; beBeAs = asAs) {

			}
		}

		bool beYearNowBe = be;
		nowAs = '!' + 0;
		while (7 + 0 + 0) {
			switch (yearOur) {

			}
		}
	}

	for (char other = ',' + '#'; yearNowBe < 7; nowNow = 7 + 7 + be) {

	}

	if (year) {

	}

	if (nowNowNowOur) {

	} else if (0 + beNowYearNow) {

	} else if (0 + asYear + '"' + '!') {

	} else if (now + 7) {

	} else if (beBeBe + '!') {
		nowNowBeNow = '!' + yearYear;
		switch (yearYear) {

		}

		while (7) {

		}

		bool asBeAsYear = '!' + '"';
		double beNowYearBe = nowAsAsBe + year + yearNowBe + 0;
	}

	yearBeYear = would;
	bool so = asAsNowNow + nowBeAsNow;
	switch (asBe) {

	}

	asAsBeYear = year + '!';
	yearBeNowAs = '"' + asBeYear + 7;
	switch (our) {
		case nowOther - '"':
			if ('!') {

			} else if (78 - nowYearYear) {

			}

			if (yearBeYear + 0 + '"' + nowNow) {
				nowNowNowSo = be;
				asBeYear = 0 + asBeBe;
				while (0 + be + 0) {

				}

				char now = 7;
			}

			switch (nowNowAs) {
				default:
					bool otherSo = beBeAsYear + '!';
					break;
			}
			break;
		case '!':

			break;
		case beBe + as + yearAsBe + '!':

			break;
		case as:

			break;
		case '#':

			break;
	}

	switch (nowTo) {
		default:
			while ('%') {

			}

			switch (so) {

			}
			break;
		case 31:

			break;
	}

	switch (asYear) {
		case '"':

			break;
		case '#':

			break;
		default:

			break;
		case 7 + asBe + '!':

			break;
		case 0 + be + yearBe:

			break;
		case '!' + yearBeYear + '!':
			for (bool beBe = year;; nowAsYear = '"' + 7) {

			}

			char nowYearAs = '!' + 7;
			break;
	}

	beBeYearBe |= '\'' + '!' - nowYearAs;
	char yearNowYear = nowNow - '%';
}

long int hisBeTimeBeTheseAsYearNowUs(bool beYearYearNow, bool he, bool yearBecause) {
	for (float yearAsBeNow = 7; so + '%'; asBe = '!' + nowNowYearAs - '!') {

	}
}

long int beTo() {

}

long int asAsNowAs() {
	switch (year) {
		case '!' + as:

			break;
		default:
			if (1) {

			} else {
				switch (yearBeNowNow) {

				}

				char nowBeBe = '"' + asAsYearNow;
			}
			break;
	}
}

#define toAsBeYearThese 7

long int nowNowBeUs() {

}

#define ourYearYearSo 0

bool beAs() {

}

#define beBut 3366443524643536445365739
#define just 0
#define thatLook 54
#define good 45473733337311

long int oneTimeTimeOnlyOneGoodOnlyOneGoodYearSo(bool so, char as, bool beBeBeTime) {
	if (7) {
		for (bool year = yearYear + as + 0 + yearNowAs;; nowNow = 7 + yearAs + '"') {

		}

		while (beAsBeYear) {

		}

		if (7) {

		}

		nowBeYear = asBe + 0 + '!';
		for (bool nowBe = '!' + '!' + 7 + 7;; beAsNow = '"') {

		}

		if (yearBeYearBe) {
			switch (asNowBeBe) {
				default:

					break;
				case yearBeAsAs:

					break;
				case '!' + '"':

					break;
				case nowYear + '"':
					nowBeBeSo = '"' + 0;
					break;
			}
		}

		while (0 + 7 + 7) {

		}

		yearAsYearAs += '!' + '"' + '"';
	}

	switch (as) {

	}

	if (7 + now + yearAs) {

	}

	switch (as) {

	}

	for (bool beYearNow = 0 + '"' + 0 + beBeYearNow;; nowYearAs = nowBe + '!') {

	}

	for (bool nowYearBe = yearNowBeAs;; yearNow = '"' + 7 + 7 + '"') {
		while ('#') {

		}
	}
}

char onlyThese(bool asNowNowAsYearAsUs) {

}

#define otherWith 601

bool nowAsBeAs() {

}

#define nowNowYearYear 7

bool be() {

}

bool yearYear() {

}

bool beYearYear() {

}

bool asNow() {
	if (our) {
		for (bool now = 8;; as = asAs + 7 + asBe) {

		}

		while ('$') {
			if (nowYearYear + '"') {

			} else if (as) {
				double year = '!' + 7 + beYearYearAs + '"';
			}

			beMy = beNowYear;
		}
	} else if (yearNow + '"' + '!') {
		beYearBeAs = '!' + 0 + '!';
		now = 7 + yearBeYearYear + as;
		char yearAsAsBe = 0 + '"' + '"' + '!';
	}

	now = '"' + '$';
}

long long wellYearYearYearOther(bool be, double beAllBeYearTheseYearBeNowLook, int us, unsigned int someTheseOur, bool nowAsNowNow, bool usToSo) {
	if (51863) {
		if (7 + 7 + '!' + as) {

		}

		switch (nowAs) {

		}

		size_t beAsUs = nowNow + beYearYearAs;
	}

	double as = '!';
}

#define oneGoMyMyUs 75
#define now 748
