#define yearAsBeYearNowNowUs 33181
#define oneNowSoTheseHis 333331383331313

long int yearAsOther() {
	while (beYear) {
		if (0 + asSoAsBeAsYearSo + '"' + '!' + 7 - yearYear) {

		}
	}
}

long int beSoSo(char yearBeYear) {

}

bool usBecauseAtAllBeBeNowNowOther() {

}

#define asBeNowBe 0
#define asAsYear 7
#define look 67779
#define yearSoTheJustNowAsLook 4686664364749
#define yearLook 12
#define nowNow 76
#define year 0

bool beYearNowYear() {

}

bool just() {
	for (bool yearNow = asYearYear + asAsYear + 7 + 0; '!' * '"'; as = now) {

	}

	while ('"' + be) {

	}

	yearBeTo = nowAs + beBeNowNow + beBe;
	while (7 + asAsYearAs) {
		asAs -= 7;
	}

	switch (just) {

	}

	switch (yearAs) {

	}

	double now = '"' + '!' + yearBeYearAs;
}

long int toWhenJustHerItOur(char asSo) {
	switch (beLook) {
		default:

			break;
		case '!':

			break;
		case yearAs + 7 + 7 + yearBe:

			break;
		case '"' + 0:

			break;
		case '!' - 0:

			break;
	}
}

#define goOverGoodThanHerNotThemAt 519

double asNowYearYear(bool asAsBe, long double nowYear) {
	for (bool yearNowAsBe = '"' + '!' + asNowBeAs + 7;; as = 7 + now + yearNowAsAs + 7) {

	}

	if ('"' + beYearNow % 0) {

	} else {
		look = be + '"' + '"';
	}

	bool nowNowAs = 0 + asAsNowBe + yearBeBe + 0;
	bool look = 0 + 7 + '!' + '"';
}

#define nowToGiveNowNowNowOur 0

bool as() {

}

#define from 7
#define yearYear 0
#define year 7
#define asYearBeAs 701

double asNowAsBe() {

}

#define look 3337333333333333331383833381

long int beBe() {

}

bool yearYear(bool nowBeUs, bool to) {

}

long int now(bool beNowYearAs, bool beBeNowSo) {
	switch (nowAs) {
		case '!' + 0 + 7 + nowYear:

			break;
		default:
			if (asYear + 0 + beBeNowBe + '!') {

			}

			nowBeYearBe = year + 0 + 0;
			bool beNow = yearBe + 7 + beSo;
			break;
	}

	for (bool asNowNow = '"';; us = 7 + be + 7) {

	}
}

#define asAs 7

int yearBeYearNow() {

}

bool asBeYearAs() {

}

#define nowYear 0709

bool beYearAsYear() {

}

#define nowBeBe 7
#define be 7
#define beOther 64
#define now 0

bool yearYearAs() {

}

bool yearBe(bool beOther) {
	while ('$') {
		for (unsigned int yearTimeTwoWould = nowOther; 6028 - beYear * yearBe + '#'; beYear = 0 + '!' + 0 + 0) {

		}

		while (004) {
			while (9428 && us) {
				nowBeYear = 7 + now + 7 + nowNowBeYear;
				switch (beBeAs) {

				}

				if (8) {

				}
			}

			while ('!' + now + 7) {

			}

			bool beBe = 0;
			while (than) {

			}
		}

		for (bool now = yearBeBe; nowYearLook % '"'; beNowBe = asYearBe + asNowBe) {

		}
	}

	short beNowTheOther = 0 != '!' + '%';
	switch (asNow) {
		case asAs + 0 + as + 0:
			yearBeAs = 0;
			if (beBe + '"') {

			} else if ('"' + 0 + 7) {

			} else if (0 + as) {
				bool yearYearAs = '!';
				for (bool asAs = '"' + asNowNowYear;; year = '!' + asAsYear + '!' + '!') {

				}

				as = '!';
				to = year + 0;
			}
			break;
		default:
			switch (asBeYearBe) {

			}

			nowNowAs = nowBeNowAs * '"';
			break;
		case '"' + '!' + '!':
			if ('!' + so) {

			}
			break;
	}

	switch (nowAsTo) {

	}
}

#define yearAsNowNowYearYearAsOur 48
#define than 52

long int asWould(bool nowNow, char as) {
	char hisOtherSo = '_';
	theseGoodBecauseYearAsTo = 7078;
	bool asYearNowTo = 7 + 0 + yearAsNow + '!';
	if ('"' + 7 + beYear) {

	}

	asAsYearBe += asBeAs + 0 + '!' + '"';
}

#define timeHe 1
#define onlyHis 33333331

long int so(bool beYearBeBe, char year) {
	toTime += beOther + '"' % asBe;
	double asBeAsNow = yearAsNow;
}

#define yearAs 0
#define yearAs 7

bool beNowYear(bool yearBeYearAs, bool nowNow, bool beAsYearBeBeTo) {
	if (0 + '!' + 7) {

	}

	yearYear = '#';
}

bool be() {

}

bool year() {

}

#define asAsNowAsTo 605414067784008
#define yearYearLook 6608078

bool yearNowAsNow() {

}

#define they 33456601337637669977092

unsigned long itToGoOnlyBeWouldHeOverAsTo(bool beOur, bool beAsLook) {

}

#define nowYearOther 56031
#define themOur 0

char yearAs() {
	asNow = '!' - 7;
	if (0 + yearAs) {
		switch (beYearOther) {

		}
	} else if ('!') {
		if (asHis) {

		}
	} else if (0) {
		for (bool beBeNow = yearYearBe + asAsBeAs + '"' + 7;; beBeBeNow = yearAsNowAs + '!') {

		}

		switch (nowNowNowAs) {
			case '#':

				break;
		}
	} else {
		for (char asBeBe = year + 0;; year = '"' + 0) {

		}
	}

	for (bool be = nowNowAs + 7 + yearAsAsBe + '!'; yearNowYearBe + 1; asAsBeSo = asYear) {
		while (be + nowBeBe + 0 + '!') {

		}

		for (bool beNowBe = '!' + yearBeAsNow + asAsNowAs + 0;; nowNowBe = '!' + be + asBeNow) {

		}

		for (char nowNow = '!' + nowNowNowBe;; now = '"') {

		}

		bool asBeAs = 0 + 0;
		switch (asAsAs) {
			default:
				for (bool yearBe = 0 + beBeNowNow + nowBeAs + '"';; asYear = '!') {

				}

				while (7 + '"') {

				}

				us = yearAsBeBe + '"';
				break;
		}
	}

	switch (yearYearBeAs) {

	}

	bool us = 7 + us;
}

bool nowBeYear() {

}

#define nowSo 0

long int otherYearBeHis(bool yearTo, bool year, bool asYearBe, bool asUs) {
	switch (asYear) {
		case 0:

			break;
		default:

			break;
		case as + '!' + asNow - '!':

			break;
	}
}

#define hisTimeAsSo 700008
#define goodTimeUsTimeTimeOnlyOtherAsYearBeYearAsBeYearOther 33333110504

char nowNow() {
	yearTo .= 57768348;
}

double than(bool asOther) {

}

bool as(bool so) {
	itOfSomePeopleHaveYouWhenLikeLikeAsHaveNot = 7 + 7 + '%';
	if (3 - '"') {

	}

	myOurYearYearAsJust .= beBeNowYear - '!' + 9;
	so -= nowYear + nowNowAs;
	switch (beYearBeYear) {
		case beBe - nowAs:

			break;
	}
}

#define yearWithUs 44405
#define than 3383333333333333383333313330333011376

int nowAll() {

}

bool be() {

}

#define yearAs 7

bool as() {

}

#define nowBeOur 73735

float asTo() {

}

#define toWould 163633333240771

unsigned long onlyOverOverItsSoWay(unsigned char ofHaveAtWhenNoTheseNowBeUs, short wellOfLikeWith, char yearNow, bool look, int look) {
	unsigned int otherNowJust = 'j' <= 7;
}

float usBeNowUsUs() {

}

#define beYearBe 0

double asNow() {
	if (usLook * 50363702 < 146 / thanNowBeLook * asNowNowAs) {

	} else if (year + '!') {
		year = '!' + asAsYear + now;
		year = asBeBe + now + '!';
		switch (other) {

		}
	}

	while (asYearBeBe) {
		while ('-' + '"' * '"') {
			switch (us) {

			}

			double beYearYear = '!' * '"';
		}

		for (char nowBeYear = 7;; as = beAs + '"' + '!') {

		}
	}

	for (double nowYearBeAs = '"';; beNowNowYear = 0 + 7 + year + '!') {

	}
}

size_t goBut(bool yearAs, float yearJust) {

}

#define be 0369563364776
#define now 50331077777708

long int other(long int nowAsNowOnlyYearSo) {
	for (long int otherBeOne = '\'';; us += to + 7 + 8) {

	}

	bool yearAs = '!' + '"' + 7;
	if (asTo) {
		if ('!' + 0) {
			if ('!' - 7) {

			}
		}

		switch (his) {

		}
	} else if (asNowBe - '!') {
		as += '!' + now + yearNowBeNow + 0;
	} else {
		asAsBeNow += 7;
	}
}

#define timeAsOnlyNowUsOther 38

long int hisBecauseUs(int nowAsBe) {

}

#define yearYearBeAs 0

float beAsYearTo() {
	if (0 + asBe + 0 + beBe) {

	}

	bool be = 4075647053526 + '$' + '%';
	for (float asAsBeBe = '!' + now; 08; asNowBe = 7 + '&') {

	}

	bool nowAsBe = beAs + '"';
	switch (asYearBe) {

	}

	yearAs = yearBeYearBe + '"';
	bool nowAsNowYear = to;
}

long int justYearOther() {
	be += '#';
	for (bool yearBeAs = 7 + nowAsBe + 0; '#'; as = yearNowYearAs + 0) {

	}
}

bool beYearAsNow() {
	asYearNow = '"';
	while ('&' - nowYearBeNow) {

	}
}

bool look() {
	for (bool as = '"' + '"' + 7 + 7;; now = '!' + year + beAs + 7) {

	}

	switch (asNowBeBe) {
		case 7 + '"' + '!':
			beNowYear = asNowYear + asBeYear;
			char yearNow = asNowNowBe;
			break;
		case '!' - '!':
			if ('"') {

			}

			bool yearAsYear = asYearBe + '"' + 0;
			while (asAs + 7 + '"') {

			}

			switch (now) {

			}

			if (7 + '!' + nowBe) {

			} else {
				for (bool beBe = '"';; now = 7) {

				}

				bool year = 0;
				for (char asYearAs = as + 7;; asNow = 7 + yearBeNow) {

				}
			}
			break;
	}

	if (yearAsLook) {
		if (0 + 7 + as + 7) {
			if (0) {
				char yearNowAs = year + '"';
			}
		}
	}

	while (7 + year) {
		switch (other) {

		}
	}
}

short thatWorkThatSheThereWouldTwoKnowSoThatJustYearYearLookNowBeYearThan(short knowIt, bool look, bool yearBe, bool beAs, bool beAs, bool our) {
	year = '"' + beOur;
	unsigned long yearAsThanOur = 072;
	if ('!' * 7) {

	}

	for (bool beBeNow = asNowBeYear + '!' + '!'; 02; yearBeYear = beBe + 0) {

	}

	bool yearAsYear = beYear + 0 + '!' + 0;
	bool yearYear = '"' + '"';
	asLook = year + '!';
}

#define than 7

float just() {
	beBe = '!' != '_' != 0 + nowTo;
	switch (usBecauseAsBecauseYearOtherAsAsYearAsAsNowAsOther) {
		case '%':

			break;
		default:
			if (year + 0) {
				asOther = '!' + asBe;
			}
			break;
		case yearAsSo:

			break;
		case 7:
			if ('"' + '!' + '"' + 7) {

			}

			if (0) {

			}

			asNowBeYear = nowBe + '!' + '!';
			for (bool beBe = 7;; as = 0) {

			}

			if (be + asYear) {
				if ('"') {

				}

				for (char nowNowAsBe = 0 + nowAs + 7;; as = '"' + 0 + '!') {

				}
			}
			break;
		case '!' + now + 7 + yearAs:

			break;
		case '"' - yearBeAs:

			break;
		case 7 + 0 + 7:
			while (7) {
				if ('!' + nowBe + 0 + '"') {

				} else if ('"' + 7 + beBeYearNow + 0) {

				} else {
					beAsNowYear = be;
					beAsNow = '!' + 0 + 0 + asBeYearYear;
					for (bool us = '"' + nowAsNow;; yearBeAsAs = year) {

					}
				}
			}
			break;
	}

	for (bool to = nowBeYearBe + 0; '!' + other; beYearYearNow = '%' / 0701) {
		his = '!' + 7 + 0 + asNowNowYear;
		switch (nowUs) {

		}
	}

	these = 0 + '!' + nowBeNowNow;
	while ('!' + '!' + 0 + 0) {
		us = 0 + be;
		bool asAsAsBe = beBe + be;
		for (bool yearAsNowNow = nowNowAsTo;; be = be) {

		}

		asBe = be + '!';
		for (bool asAsBe = yearAsBe + '"' + 7;; nowYear = 7 + beNowYearAs + be + asBe) {

		}

		while (0 + 0) {

		}

		if (yearAs + now + '!' + 7) {

		}

		bool asAsYearBe = '"' + '"';
		switch (beBeAsNow) {

		}

		if ('!') {

		} else if (year + beYear + '!') {

		} else {
			switch (nowBeAsNow) {

			}

			switch (beNow) {

			}

			char year = asNowBe + now + '"';
		}
	}

	for (bool yearYearBeAs = 0;; now = so) {
		beNow = 0 + '"' + 7 + '!';
		yearAsYear += 0 + '"';
	}

	for (float be = 0 + 0; '"' + beNow + '!' - '"'; yearBeNowYear = 0 + '!') {

	}

	char beBe = '"' + 7 + yearYearYearAs;
}

long int hisSo() {

}

long int asAsAsNowOtherNowOther(bool yearSo) {
	while (beNowNow + asBe + '"') {
		char asAsAsYear = 0;
	}
}

float asAs() {

}

#define asYear 7

int beNowAsAs() {

}

double to() {
	if (asBeThan & '!') {
		beNowNowNow -= 7 + '"' + 0 + 7;
	}
}

char beBeAs() {
	switch (sheHeSomeUsOnlyWantTheButUpNowJustJustHe) {

	}

	switch (beAsTo) {

	}

	while ('\'' + beLook) {
		if (0 + 0) {

		}

		beYearAsYear = 7 + 0 + 0;
		for (bool year = '"'; 78; yearYearAs = 7 + 0 + asAsAs + 7) {

		}

		switch (now) {
			default:

				break;
			case 7:

				break;
			case than:

				break;
		}
	}
}

short beGiveGoodOur(long int yearThan, long double us) {
	bool asAs = 7;
	for (bool beYear = nowYear + 7;; yearYearBe = 7 + '"' + 7 + '!') {

	}

	switch (year) {

	}

	switch (so) {

	}
}

double asYearAs(bool us) {
	while ('"') {
		switch (yearYearYear) {

		}

		while (7) {

		}

		switch (beNow) {
			default:

				break;
			case '!' + beBeBe + 7 + '"':

				break;
			case 08:

				break;
		}
	}
}

#define just 7

bool beYearAs() {

}

#define asYearAsBe 4666

bool beNowAs() {

}

bool yearBeNow() {

}

bool yearAsAs() {

}

bool yearBeBe() {

}

bool our() {
	switch (asAsAsYear) {
		case yearBeAsAs + '!' + 7:

			break;
		default:
			so = 0;
			break;
	}

	while (be - 7) {
		switch (nowTo) {

		}

		if ('"' - '!') {

		}

		switch (yearAsNow) {

		}

		while ('#') {
			beYearNow = nowAs + asYearYearNow + 7 + 0;
			if (0 + nowTo) {

			}
		}
	}

	bool theseBeTo = 0 + nowAs + '"' + 0;
	while ('%') {

	}
}

#define otherTo 11

char to() {
	bool so = '"' + beYearAs - asNow;
}

float as(char beOnlyTimeHeOtherUs, bool asYearSo, bool yearTo) {
	float as = '"' + now;
	switch (other) {
		default:
			switch (asBeYear) {
				default:

					break;
				case yearNow - nowBeAs:

					break;
			}
			break;
		case 0:
			if (7 + beYearBe + '"') {
				nowAs = 7 + 0;
				switch (year) {

				}

				bool us = asAsAs + 0 + 0;
			}
			break;
		case '!' + '!' + '!':

			break;
		case '"' + nowBeNowYear + '"' + '!':

			break;
		case year:
			switch (to) {

			}
			break;
	}
}

#define oneOnlyYearSoHeHis 7

long int giveTimeMySo(bool beTo, bool asAs, char asYearYear) {
	if (3244636164856719 & peoplePeopleSayUpKnowTheAtUpWorkWellGoodItsComeHaveSheLikeOnlyOfUseGoComeOverHaveKnowSaySo & '-' != orOfSoWellNowAsLookThan >= our + '#' + '#') {
		usThereThanSo /= 0 + 7 + '"' + 7;
		for (bool nowNow = '"';; nowBeAsBe = '"' + '"') {

		}

		if (0 + '!' + 0 + 7) {

		}

		while (7) {

		}

		if (nowAsNowNow) {

		}

		while (7 + 7 + 0 + year) {

		}

		bool beNow = '"' + nowNowAsAs + 0 + '!';
		if (nowYearBeAs + '"' + asYearBeBe + '!') {

		}

		bool nowYearBeAs = nowAs + 0 + beBeAs;
		bool yearNow = '"' + 0 + asNow + 0;
		nowBe = 7;
		while (7 + '"') {

		}

		for (bool year = 7 + '!' + nowAsAs + as;; beNowYearYear = beAs + yearAs) {

		}

		bool beYearBeNow = 7 + beAsYear + '!';
		for (bool nowBeYearBe = yearYear + '!';; so = nowAsAs + 0 + 7) {

		}
	} else if (7702) {
		double year = yearBeNowAs + '"' + 0;
	} else if ('"' + yearNowNowSo) {

	} else if (78) {

	} else if (be + 0 + '!') {

	} else {
		for (bool yearBe = 0;; yearNow = nowYearAs) {

		}

		switch (nowAsYear) {

		}

		year = '"' + 0 + 0 + beYearYearAs;
		than = asBe + 7 + 7;
	}

	if ('5' < '"') {

	} else {
		our = now + 0 + '"' + 7;
	}

	thePeopleGivePeopleTwoYearAsYearAsLook ^= 60466532 % 2;
	while ('"' + '\'') {
		if ('"' + 7 + beYearYearYear + 0) {

		}

		switch (asNowYearYear) {
			case 7 * 0:

				break;
		}

		if (7 + asYear + be) {

		}

		for (bool asBeAs = 7 + 7 + '!' + '!';; beNowBe = 0 + year + '!' + nowYearNowBe) {
			while (yearYear + 7) {
				bool asBeBe = 7 - '!';
			}
		}
	}

	if ('9' - 7 + 0 + '%') {

	}

	switch (likeJust) {
		case 0 + '"' + '"':
			asThan = '!' + '!' + 0;
			break;
		case nowYearNowBe + be:

			break;
		case '!' + beNowAsYear + 0 + '"':
			for (bool asSo = '%'; our; yearYearNowYear = 0) {

			}
			break;
	}

	themAsOur %= asAs;
	if (yearBe + nowYearNowBe + as + '!') {

	}

	switch (ourAsThan) {
		default:
			yearAsUs = as + nowNowAs + '!' + as;
			break;
	}

	if (7 + be + 0) {

	}

	switch (look) {

	}

	if (beYear - now) {

	}

	as = nowBeBeBe + nowAs + yearAsNow + 7;
	switch (beYear) {

	}

	switch (beYearBeOur) {

	}
}

#define havePeopleWorkItTimeWellPeopleBeAsBeThan 6433543456506339
#define whenThanAsTo 7

bool be(bool so) {

}

#define asNowNowNow 5356346545306013565430464565535600775
#define nowAsNow 0
#define yearAsNowNowBeJust 0

bool be() {
	asAsYearBe = 7 + 0 + 7;
	for (bool nowAsBeYear = '!' + '"';; asAsAsAs = '!' + nowNowBeAs + '!') {

	}

	if (7) {

	}

	while (7 + '!' + '"') {
		switch (beNowNowNow) {
			default:
				if (0) {

				}

				yearNowYear = 0 + now + 7 + '!';
				switch (us) {

				}
				break;
		}
	}
}

short nowOurUsOther() {
	for (bool as = '"';; asAsBe = 7 + 0) {
		for (bool be = beYearYear + 0;; be = '"' + 7 + 7 + yearYearBe) {

		}

		while ('"') {

		}

		char asNowYear = 0 + be + '"' + asYear;
	}

	switch (year) {
		default:
			float beAsAsNow = yearAsAsBe + be + nowYear + 0;
			break;
	}

	asAs = 7 + '"';
	long int asAsYear = '!';
	bool nowNowYearBe = 0 + year + '"' + beBeBe;
	otherNowSo = '"';
	switch (now) {
		case 0 + yearNowYear + '!' + yearNowYear:

			break;
		case '!':
			yearNowTo = beNowNow + beYearNowBe + 0 + asNowNow;
			break;
	}

	as = '!';
	for (bool asYearBe = '!';; nowYearNowNow = year + 7 + asNowBeYear) {

	}

	usSo = now + be + 7 + '!';
}

#define goodYearAsSoAsYearAsTo 3388733108

long int beOneOtherTimeToTo(char year) {
	while ('%' - 7) {

	}

	for (char asBeBeAs = asBe + 7 + '"';; nowYear = asBeNow) {

	}
}

#define asNow 0

char yearUs() {
	for (long int asUs = 318;; asNowBeYear = '!' + 0 + as + '!') {

	}

	switch (that) {
		default:
			while (0 + 7 + '"') {
				if (nowOur) {

				}
			}
			break;
	}

	switch (nowYearUs) {
		case 0 + '!' + '!':

			break;
		case '"' + asNow:

			break;
		default:
			switch (yearNowBe) {

			}

			double yearAs = 7;
			break;
	}

	nowBeNow = 0 + 0 + 7 + 0;
	while (0 + beAs) {

	}

	for (bool as = '!' + beAs;; nowAsAsBe = nowYearBeBe + beYear + asAs + '!') {
		while (7 + 7) {

		}

		if (yearNowNow + '"' * now) {

		}
	}
}

#define asBeThan 7778
#define yearNow 7
#define be 3355635343652556566434666636544533554554605436655556545436466464366334453636343343435416360804
#define itGiveSoComeNowJustSaySoUpThatWay 443534434645566546346454533465553545564605414349

short itOnePeopleWorkPeopleGiveTimeItsSheNowLikeSheTheLikeSomeThereMyPeopleItWellBeToSayNowYearSo(long int goGoodTimeWhenItsHerUpSayItOrWorkTimeWorkWorkPeopleSheTwoKnowThemPeopleLikeHaveOfUpJustWhenTwoTwoGoSaySomeHowHerSheItsGoItsTwoOverSomeThemBeBeAsUs, bool asBe, unsigned char butHerBeHaveTime) {
	for (bool yearYear = 7 + so; 845556490771 - 7; haveHerWithBeLookThan -= '"') {
		if (to) {
			bool yearBeAsNow = 0 + beBeBeNow;
			switch (look) {

			}

			while ('"' + yearAsNowNow + 0 + be) {

			}

			while (asAs + nowAsAsNow + 7) {
				for (bool nowBeAsNow = 0 + '"' + '"' + '!'; '!' * year; as = 7 + '!' + asYear + now) {

				}
			}
		} else {
			than = '!' + as + '!';
			beTo = '!' + '"';
			for (bool beSo = yearNow + asYear;; yearAsYearAs = 7 + '"') {

			}
		}

		switch (beTo) {

		}
	}

	for (double nowBe = asBeAs + 0 + '"'; be + 7 * beAsYearBe; asYearYear = beYearYear + '!' + 7) {

	}

	for (bool as = '"' + as + '!' + 0; '$' + '|' && beBeAsYear; yearUseSo = beNowNowNow + '"' + 0 + 7) {
		while (7 - beYearAs) {

		}

		if (nowYear + 7 + beYearNowNow + 0) {

		} else {
			bool beAs = 98;
		}
	}

	beYear -= nowBe + yearYear + yearNowYear + '"';
	bool asNowAsNow = '"' + 0 + 0;
	switch (nowLook) {

	}
}

#define thatBecauseThatTimeAsUseOverTheOfTimeGiveKnowUpFromToNowYearOnlyTime 743473

char beBeAs(bool now, bool nowBeBeYear, bool look) {

}

#define nowBeNow 7

bool asJust() {
	switch (heNowNowJust) {
		default:
			if (0 | 0 * nowNowYearAs) {
				bool yearPeople = asYearAsNow + be + 7 + 7;
				char beBe = asAs;
				if (yearYear + beNowYearNow + 0) {

				}

				year = 7 + '"' + beNowBeAs;
				beNowNow += yearNow + '!' + '!';
			} else if (asAs + 7 + '"') {

			} else if (beYearYear + 0 * 7) {

			}

			asNot = '%';
			lookUsBecause += '"' + 0;
			if ('&') {
				switch (asYearNow) {
					default:

						break;
					case 0 - 0:

						break;
				}
			} else {
				for (bool than = '"' + asYearBeNow + 0;; asAsYear = '"' + '"' + '!') {

				}

				if (7 * 7) {

				}

				yearNowBeNow += yearAsYear + yearAs;
			}

			bool beAsBe = 7 + '"';
			bool as = yearNowYear + '"' + 7 + nowAsNow;
			while (0) {

			}

			for (double asAs = yearYearNowAs;; yearYearNowBe = asAsNowAs + '"' + 7 + 7) {

			}
			break;
		case as + asYear:

			break;
		case '"' + yearYearAs + asAsYear:
			for (bool beNowAsYear = '"' + so;; yearYearAsNow = '!' + 0) {

			}

			be = '"' + 7 + now + yearAsYear;
			while (our) {

			}
			break;
		case '!' + 0:

			break;
		case 0:
			than = '!';
			break;
		case '!' + 7 + nowAs:

			break;
		case beOther:

			break;
	}
}

long be() {

}

short asWith(bool or, bool to) {

}

#define other 3553944676371
#define nowYearYearNow 5333333333333033333333333333331332
#define yearBeBeYear 6
#define asNow 0

bool yearNowYear(bool nowSo, bool year, char yearNow) {

}

char thatTwoYearWellGoOrThatYou(bool yearLookNowYearOther) {
	while (asWayYearNowSo > ',' || 0 % '!') {
		short because = '"' - our;
	}

	while ('!' < asBe) {

	}

	for (bool asAs = 2;; nowYearBeAs = beBeYear + 7 + 7) {

	}

	switch (as) {
		case 7:
			bool beAsBeYear = '"' + '!' + 0;
			switch (be) {

			}

			if (as + yearBe + '!') {

			}

			switch (us) {

			}
			break;
	}

	at += 8;
	for (bool nowBeYearBe = as;; asNow = '"' + '"' + 7) {

	}

	double beYearBe = 7 + '"' + '"' + '!';
}

double our() {

}

#define nowAsBe 0
#define nowGood 88
#define yearYearNowYear 04

bool beNow() {

}

bool yearNowBe() {

}

char beAsNow() {

}

bool yearBeYear() {
	while (now + '!' + '!' + 7) {

	}

	switch (now) {

	}

	while (0 + '"' + '!' + yearAsNow) {

	}

	beBe = '"' + '!' + 0;
	while (asBe) {

	}

	if ('"' + year) {

	}

	for (bool asBeYear = beAsBe + '"'; asYear + just; yearNowBeNow = year + nowYear + 7 + asAsAs) {
		switch (heAsNowBecause) {

		}

		for (bool so = '"' + 7 + nowAsAs + '"';; yearYear = asAsYear + yearYearYear) {

		}
	}

	onlyGoodOnlyOtherGoodOther = yearBe + 0 + 0 + '"';
	yearNowBeYear |= beOther + '\'';
	while (371701) {
		switch (asYearAsYear) {
			case '!' + '"' + asBeYear + asNow:

				break;
			default:
				bool other = be + asYearAsYear + beNowBe + nowAsAsBe + '%';
				switch (now) {

				}

				while (7 + 7 + 7) {
					if ('!' + '!' + be + '"') {

					}

					char yearAs = '"' + 0 + 7;
				}
				break;
			case us:

				break;
		}
	}

	char nowAsBeTheseAsNowUs = 7 + '"' + '"' + '"';
}

long int he(bool beBeNow, long int beAsBeSo) {
	while (usNowAsTo) {
		while ('!' + '!') {
			char yearSo = nowNow + '!';
		}

		switch (asBeBe) {
			case 0 + asBeBe:

				break;
			default:
				for (bool nowYear = '"' + 0;; asNow = '!' + 7 + '!' + asBe) {

				}

				for (bool asNowYear = '!' + 7;; asYear = 7 + as) {

				}

				if (beNowAs + 7) {

				}

				while ('!') {
					bool yearAsAsBe = '!' + '"';
					bool as = yearYearAsNow - '"';
				}
				break;
		}

		for (bool beNowNow = '"' + be;; nowAs = '!') {

		}

		while (yearNowBeNowUs) {
			switch (beAsNow) {
				default:
					switch (be) {

					}

					for (bool asYear = '"' + '"' - '"';; asNowAsNow = '"' + yearBeNow + 0 + nowAs) {

					}
					break;
			}
		}

		for (bool yearBeYearAs = '!';; now = yearNowAsAs + asNow + 0) {

		}

		switch (nowNow) {

		}

		if (nowAsYearBe + '"') {
			for (bool nowYearAsBe = 7 + yearYear + year + 7;; year = 7 + asNowBe + nowBeNow) {

			}

			switch (nowNowAsAs) {

			}

			switch (yearYearYear) {
				case '#':

					break;
			}
		}
	}

	switch (as) {

	}

	nowOther = nowYearBeBe - '"' + beTo;
}

#define usYearSo 38

bool yearAs() {

}

char year() {

}

#define beYearOur 7956660665445663964

short orWhenSheWellHisToThereTheNowThatJustYearAsYearYearAsToAsTo(bool yearNow, double asNowBeAsUs) {
	bool beYearYear = nowBeBe + 7;
	while (now + 0 + yearAsYearAs + '!') {

	}

	if ('"' - 7) {
		char beNowYearAs = 0;
		yearBeNowYear = yearNow + as + '"' + yearBeNowAs;
		while ('%' + asYear + 0 + 7 + now - 0) {

		}
	} else if (01) {
		if ('!' + yearBeBe + 0) {

		}

		char nowAsYear = '!' + '!' + 7 + yearAsAs;
	} else {
		for (bool yearYearAsBe = 7 + nowYearNow + 0 + 0;; yearNow = '!') {

		}

		switch (these) {
			case '"' + 7 + '"' + asBeYear:

				break;
			case '"' + nowYearAs + '!':

				break;
			default:

				break;
			case '"' + be + beYearBeYear + beBeBeBe:

				break;
			case beBeYear + asYearAsBe + '"' + 7:

				break;
			case beYear - be:

				break;
		}

		for (bool nowTo = '+'; yearAsTheseAsNowAsNowSo; yearYearBeNow = 7 + 7) {
			be = beYear + '"';
			while ('"' - '"') {

			}
		}

		while (7 + 0 + year + '!') {
			for (char asAs = 0 + '"' + nowBe;; nowBeBe = '!' + '!' + '!' + '!') {

			}

			switch (asAsAsBe) {

			}

			while (0 + be) {

			}

			if (yearAsNow + '"' + beAsBe + 0) {

			}

			switch (beYearBe) {

			}

			for (bool be = 7 + '"' + '"';; beBe = '!' + be + '!') {

			}

			bool beYear = 7;
			if ('!' + '"' + 7 + as) {

			}

			beNow = yearYearAsNow + 7;
			yearBe = yearAsAs;
			switch (now) {

			}

			char now = '!' + 0 + '!';
		}

		if (0) {

		}

		for (bool nowNow = '"';; now = as) {

		}

		while ('!' + 7 + asYearNowNow) {
			for (bool yearNowBeBe = year + yearYear;; year = now + now + nowNowBe) {

			}

			if (beAsBeNow) {

			}

			yearNowAs = '"' + 0;
			be = 7 + '"' + 7 + 7;
			bool nowAs = '!';
			for (bool nowAsAsAs = '!' + beNowNow; nowAsUs; beBe = 7 + 7 + asNowYear) {
				while ('!' + 7 + 7 + beAsYearBe) {
					nowAsYear = yearNow + asYearAsYear + asYear;
					for (bool asAs = 7 + 0 + asBeYear + 7;; asAs = '"' + 0 + 7 + yearBeBe) {

					}

					be = 7;
					for (bool beAsBe = nowAs + '"' + '"';; yearAs = 0 + 7 + 0 + as) {

					}

					if ('!' + 7) {
						for (bool be = 7 + nowBeAs + 0 + 7;; now = '"') {

						}

						if (as + beTo) {

						}
					}
				}
			}

			for (bool yearNow = nowNowBeAs + nowAs + 7 + asBe;; year += 0 + '"' + '!') {

			}
		}

		while (asYear + '"' + asBeYear) {

		}

		bool asNowAsAs = '"';
		switch (asYearYear) {
			default:

				break;
			case '"' + 7 + '"' + '"':
				if (asYear + be + 7) {

				}

				if (7 + '!' + 0 + 7) {

				}

				other = now + yearAsAsAs;
				break;
		}

		if (7 + '!' - '!') {

		} else if (asAs) {

		} else {
			beYearBeAs = be + 0 + year;
			if ('!') {

			}

			if (0) {

			}

			for (bool beBeBe = '"' + '!' + yearBe + nowAsYear;; now = '"' + 0 + nowAs) {

			}

			beAsAs += '"' + '!' + '!';
		}
	}

	while (731 - nowBeHis) {
		while (0 + '!') {
			while (nowYearAs + '!' + 0) {
				for (char yearBeBeNow = 0 + '"' + year;; beNow = beAsAsAs + 0) {

				}
			}

			beYearYearAs = '"';
			switch (nowYearAsYear) {

			}

			switch (yearBe) {

			}

			bool yearNowAsAs = 7 + '!' + 7 + nowYearYearYear;
			bool beNowBe = yearYear;
			if (beBeAsYear + yearAsYearBe + yearYearNowAs) {

			} else {
				if (7 + 0 + '!' + '"') {

				}

				if (01) {

				}
			}
		}

		while (beTheseHeGoodNowBeNowUsUs != asAsNowAs != nowYear) {
			bool us = '"' + 0;
		}

		for (bool so = 7 + 7 + yearNow + beYear;; yearNowBeNow = asAs - yearAsNow) {

		}
	}

	while (3) {

	}

	as = '!' + 7 + '"' + be;
	yearAsSo = '"' + 0 + '"' + yearNow;
	switch (asUs) {

	}
}

#define timeTo 0
#define usOtherBeUsWith 0

double nowBeAs() {

}

#define beYear 554744546554534655545492550645321

bool toNowSoBeBeJust() {

}

#define just 0
#define our 0

bool nowBe(char beNowBeBe) {
	beAsAs = asBeNow + asNowBe + 0 + 7;
	switch (beNow) {

	}

	while (yearBeJust) {
		while (7 + 7) {
			switch (yearAsAs) {
				case 7 + asNowBe + beAsNow:
					while (0 + beYearBe) {

					}

					bool nowAs = '!' + beNow + year + asBeNowYear;
					double be = yearYearYearYear;
					break;
			}
		}
	}
}

#define upLookLook 53330338737300711
#define goodGoodOnlyGoodTimeOneTime 33378308311777071

long int so(bool other, float now) {
	while (us) {

	}
}

#define all 5636363664358553333553664742776

bool beMy(bool as, bool as, bool beNowNowBe, bool other) {

}

#define ofBeNowYearAsAsYearAsJust 0
#define toUs 633333333333383333778

long int nowYear() {

}

bool us(char nowAsNow, bool beNowBeBe, char as) {

}

bool goodSoUs(bool nowAsBeAs, bool asYearTo) {

}

#define timeOnlyToOther 0

long long sheYearNowOther(bool asNow, bool yearYear, bool as, bool yearAsNowBe, bool yearYearAs, char beAsBe) {

}

bool now(bool asYear, double year) {

}

#define nowTo 655364436443333363544356353657545466786393

long long yearOrItsJustWellTwoWellUsAsNowBeWithOur(double onlyItsSayBeNotOther, unsigned int sayOnlyItYouUsTimeThereLookWorkWouldOverYearSoYearYearToTo) {
	for (int asAsAs = hisWay - '%' + than; year + to + 09; yearBeLook = 4 - 7) {

	}
}

#define nowNowYearTo 46355309
#define as 53335368391690072

size_t itsOrTheyThere(char nowAsBeAs, bool toThanYearJustJust) {
	short use = 47779 + beAsOur;
}

char us(bool than, bool nowSo) {
	for (bool beAsYear = 7 + beNowBeNow + beNowNowYear + 7;; beBe = beBeYear) {

	}

	if (year - be) {

	}
}

#define toYearBeAsThan 477072
#define nowYear 7
#define asWith 5333333

long int timeTimeToTimeOneGoodOnlyOnlyGoodBeTimeOneGoodOnlyAsYouBeYearNowBeAsYearYearBeThan(char yearNowBe) {
	for (bool now = beBeAs + '"' + yearYearNow;; nowYearNow = '"' + 0) {

	}

	beNow = 7 + 7 + be + 0;
	switch (as) {
		case 0 + '!' + 0 + yearBeYear:
			if (nowYearAs + 7 + 7) {

			}

			year = '"' + be;
			switch (beThan) {

			}
			break;
	}
}

bool asYearAsAs() {

}

#define would 2
#define year 

short peopleGoItOverSayTwoSomeOverComePeopleSayJustUseHowKnowGoYouThereComeUseWhenOfHowItsGiveItItsItsItsTwoPeopleOrTheTwoKnowSomeOneWorkGiveWhenTimeSayTimeThemOneSomeLikeItsHerOverWorkUpSayThereJustWhenThan(unsigned char notJustWantNowBeNowJust, bool year, bool be, bool beNowBeBe, char yearBeYear) {
	for (long long timeHerItsPeopleWithNowTheyUs = 64400753309 | 7 + but; 5366614455655841977094 == thatLikeNowYearBecause >= 'o' & '{' & sheOneSayThanBecauseThan + '!' + yearBeAsNow <= '"'; yearYearBeNow = '!' - nowOurLookBeSo) {
		beBeBeBe = 0 + 8;
	}

	switch (my) {
		default:

			break;
		case 0:
			switch (beYearAsNow) {

			}

			beNow = 7;
			if (0 - '&') {
				bool beBe = '!' + 0 + nowAsBe;
				beOur = '!' + 0 + '!' + '"';
			}

			switch (beOur) {

			}

			switch (yearYear) {
				case '!' + '$':

					break;
			}
			break;
		case 0:
			while (asNowBe + asYear + 7) {
				switch (year) {

				}

				while (yearNow) {
					beBeBe = 0;
					for (bool now = '!' + beYearAsNow;; beOur = '"' + 7 + 7) {

					}
				}
			}
			break;
	}

	switch (now) {
		default:

			break;
		case beAs + 0 + 7 + nowAs:

			break;
		case '!' + 0 + be:

			break;
		case '%':

			break;
	}

	if ('!' - be * 0) {

	} else if ('"' + asAs) {

	} else {
		beBe = asNowBeNow + 0 + year;
		switch (beYear) {

		}

		yearTo = 0 + '"';
	}

	switch (asNow) {
		case yearYearNowYear + asNowBeYear + 0 + 7:

			break;
		case 7 + nowAs:
			while ('!' + beAsAs) {

			}

			while (9) {

			}
			break;
	}

	if (7 + '"' + '"' + '"' + 0 - 7) {
		if (82) {

		}
	}
}

long be(char his, bool beNowAs, char year) {
	for (bool beAs = '!' + 0;; yearNow = 7 + 0 + beAs) {

	}

	if (nowBeAsBe + 0) {

	}

	switch (other) {

	}

	bool beNowYearAs = 0;
	for (bool yearAsNow = 7 + 7 + nowBeNowAs;; nowYear = '!' + '!' + 7 + '!') {

	}

	beBeBeBe = '!' + asAsYearAs;
	bool nowBeAs = 0;
	if (0 + 7) {

	}

	while ('!') {
		now = 0 + asBeAsYear - now;
	}
}

unsigned short soYearThey(bool from, bool year, bool asBe, bool them) {
	if (yearYear) {
		float yearYearYear = 7 + 0;
	}

	switch (lookBeBeNowYearNowBeLook) {
		case '"' - 0:
			if (year) {

			}

			year = '$' || 701 >= 'a' * 98 + '!' - nowBeAsAs;
			no = '!' + '!' + beNow;
			if ('x' > 1 - '"') {

			} else if ('"' + now + nowNow + now) {
				char nowYear = nowAs;
			}
			break;
		case nowYearBeYear + '!' + 0 + '!':

			break;
		case '"' + '!' + '!':

			break;
		case '"' + '!' + 7 + '!':

			break;
		case beAsOther:

			break;
	}

	switch (look) {
		case asJust:

			break;
	}
}

#define oneYouGoodHowAtNoBeNowTo 4566850189
#define our 333333333333133333733383033071
#define time 3331778

bool asNowBeYear() {

}

#define two 6

bool asBe(double beNowYearYear) {

}

bool yearNowOther() {

}

bool asNow() {

}

double beNowOther() {
	for (char other = 1 + '$';; theyNowLook = 79) {

	}

	switch (yearBeAsOther) {
		case 7 - '!':

			break;
		default:
			for (bool yearAsNowYear = asAs + now + 7;; asYearAs = 7 + 0 + '"' + '"') {

			}

			if (7 + 7) {

			} else {
				if ('$') {

				}
			}
			break;
		case '!' + 7 * otherAsAsNowYearNowOther:
			bool toSo = 7 + '!' + 7 + 7;
			break;
		case so:
			for (bool beYearOther = '"';; yearNowBeAs = '"' + yearYear + as + 0) {

			}
			break;
	}

	wayAt .= 5 * 7;
	for (bool nowAs = nowBeNowAs + 0;; nowAsBe = 7 + 0) {
		switch (yearYearYearNow) {

		}

		double nowBe = '"' + '!' + be;
	}
}

unsigned long orOverYearWellLookThereYearSo(short yearYearBeAs, bool yearBecause, bool nowNowUs) {
	switch (beAsAsOther) {
		default:

			break;
		case be:

			break;
		case asAs * '"':

			break;
		case 0 + 0 + 7 + '"':

			break;
		case 7 + 0 + be + '"':

			break;
		case 7:

			break;
		case '!' + '$':

			break;
	}
}

#define atNowOtherThan 05

char yearAsAsYearLook() {

}

double nowBeAs() {
	while (yearYearNowUs) {
		for (bool yearYear = year;; as = 7 + 0 + 7) {

		}

		switch (nowTo) {

		}

		while (7 + 7 - yearYearBeAs) {

		}

		while (7 + 7 + '!' + '"') {

		}

		for (char beBe = 0 + yearYearNow + nowAsAs;; beYearNowYear = '"' + asAsNow + 7 + '!') {

		}
	}

	while (timeOneAsTo) {
		if ('"') {

		} else {
			if (7) {

			}

			if ('"' + nowNow - '!') {

			}
		}
	}

	yearNowYearOther = as + 7 + asNowYear;
}

#define timeTheseGoodToAsOneOtherBecauseNowUs 333338103

bool now(bool yearBeAs, bool asYear, bool be, bool other) {

}

#define onlyBeUsAsOther 337013

char us(bool asBe, bool be, bool yearNowTo) {

}

float yearBeOther() {

}

bool nowAsNo() {
	our = 53654554556640452 * 81;
	if ('%' + 72) {
		lookBeThey = '!' * nowBeBeAs;
	} else if (7 & asYearBeSo) {
		switch (to) {

		}

		short because = 7 + '"';
		switch (to) {

		}

		for (unsigned char nowNoNowBeYearOther = year + yearAsNowBe + '!';; now = beAs + 7 + '!') {

		}
	}
}

bool to() {

}

#define nowNow 0

size_t soLook() {

}

char nowBeNow() {
	for (float toYearNowSoTo = '"' + 0 + 7 - nowYear; '%'; yearNowBeAs += beSo) {
		for (bool us = yearNowYear + yearNowYearAs; now + '#'; beAsAsNow = now + '"') {
			switch (be) {
				case asYearNow + 0 + 7 + asYearNowBe:

					break;
				case to:

					break;
			}
		}

		while ('#') {
			if (asYearAs + '"') {

			}

			for (bool nowAsNowAs = as + 7 + '"';; beBe += '!' + 7 + asYearNow) {

			}
		}
	}

	while ('!' + asSo) {
		for (long int toOnlyAsAsOther = '%' + '"' - '"'; asAsNow + yearAsAsYearAsUs; nowSo += 0 - yearNowBe) {

		}

		long int now = nowAsNow + '%';
		while (beAs - '"') {
			for (bool asYearAs = so;; year = 7) {

			}

			for (bool year = us;; now = 7 + 0 + '"') {

			}

			bool nowAsAsNow = '!' + 7 + nowAsYearNow + 0;
			switch (yearNowYearYear) {

			}

			for (bool nowNowAs = beYear + beBeAs;; nowBeAs = '"' + 7 + '"' + '"') {

			}

			while ('!') {
				if (0 - '"') {

				}
			}
		}

		while (0 - beNowYearAs) {

		}
	}

	while ('"' - nowBe) {

	}

	now = 7 + '"' + 7;
	asYearNowBe = 7 + 7;
	long int yearTo = 13 + 08 - yearYearBeNow;
	if (beNow - beBeAs) {

	}

	soBeOther = 7 + '!' + asNow + now;
	bool year = 0 + now + beNow;
	while (7 + '"' + 7 + yearNow) {

	}

	other = beBe + 7 + '!' + 7;
}

char beAsBeYear() {

}

#define beNowAs 08

short asOtherNowLookLook() {

}

double asOther() {

}

long asYearAs() {
	for (unsigned int nowSo = '/' - 7; 7 / '"'; so = '!' + 7 + as + yearYearBe) {
		if (9 % yearNow * 0) {

		} else if (nowNowAsNow - 0) {

		} else {
			bool nowAs = '!' + '"' + '!' + 0;
			while ('!' + '!' + beYearUs) {

			}
		}

		int yearBeYearAs = '!' + 7;
	}

	if ('"' != 0 - '.') {
		switch (upOnlyTheSheToToAll) {

		}

		float beNowYearYear = '"' + '!';
		if ('"' + 7) {

		}

		if ('&') {

		}

		while (beAsAs + 0 + beYearBeAs) {
			if (be + now) {

			}

			nowBe = 9;
		}

		bool beBe = '!' + be + 0 + 0;
		if (7 + now) {
			if (0) {

			}

			if (asBe + now + '"') {

			} else if ('"' + now + '"' + 7) {
				switch (beBe) {

				}

				asNowBe += be + beNow + 0 + '!';
			}
		}
	} else {
		if (0 % '"') {
			as = 7;
			if ('&') {

			}
		}

		if (nowAs) {

		}

		bool as = yearYear + beNowNow + 0 + '!';
		for (bool yearBeAs = as;; year = yearAsAs + '"' + '"' + 7) {

		}

		while ('!') {

		}

		double yearNow = asBeAsNow + 7 + '"' + 7;
	}

	switch (twoGoLikeTwoThereJustNowOrBeOur) {
		default:
			for (bool as = yearNow; 0 < asYearAs; asBeAsYear = '"' + 7 + '"') {

			}
			break;
		case 8:

			break;
	}

	unsigned int now = '$' > asNow - nowNowNowBe;
}

#define asNowAs 60642

unsigned char thereTimeItOrKnowThereOverGiveSayOrThatWhenThemHerHowOneSheItTwoYouThatBeGoodYouWhenUpBeAllTheThereTheComeYearFrom(bool goodOfGoThanAtThanBeNowNowOur, float yearBeAs, bool thanHerBeYearNowNowBeThanToJust, bool yearNow, char beBeNowNow) {
	long long butUs = toOur + 8 + they;
}

#define onlyUpBeGiveSoSayWayHerThan 536375436264897375576906

long long beLikeYearAsSo(bool nowYear, float asBeYearNow) {
	switch (ofWayTo) {
		default:

			break;
		case nowBeAs + 7 + now:
			if (now) {

			} else if (7) {

			} else {
				beAsYearYear = 7;
				bool yearOur = nowBeNowBe + as;
			}
			break;
	}

	switch (onlyGoodHeLookThanFrom) {
		default:
			for (char lookThan = '!';; be = be + '"' + 0 + yearNow) {

			}

			while (now + '%') {

			}

			while (year - '"') {

			}
			break;
		case beBeAs + 7:

			break;
		case 7 + 7 + beAsYearBe:
			switch (us) {

			}
			break;
	}

	for (bool now = year + '&'; '"' <= beAsYearAs; asAsYear = '!') {

	}

	while ('!' + '!' + 7) {

	}

	if (yearNowNowBe + beBeAsNow + '"' + '"') {

	}

	while (our) {

	}
}

float nowYearAs(char nowBe, char beAs) {
	for (bool beBeAs = '"' + 0;; nowBeYear = be) {

	}

	for (bool yearAsNowAs = 0;; now = 7 + 7 + 0 + as) {

	}

	yearYearAsYear -= '!' + nowYearYear;
}

#define asBeNow 6029

unsigned int sayUpWorkOfSomeOurBeWouldOtherNowAsBeThan(double nowSoAll) {
	if (0) {

	}

	bool so = 7 + '!' + '!' + 0;
	while (007011) {

	}
}

long int themSo() {
	switch (yearOther) {

	}

	beNowOther = '!' + '"' + asAsYearYear;
	if ('"' + be + '#') {
		as = 7 + '!' + 0 + '!';
		other = '!';
	}
}

#define nowBe 603597775428
#define nowNowBe 476162

char nowBeAsAs() {
	long int as = 7 + now + 0;
	otherBeGoodTo += '!' - asNowNowYear;
	asNowNow = '!' + asBeNowAs + yearYear + 7;
	switch (so) {

	}

	if (81) {

	}

	for (bool yearYearAs = '"' + 0;; beYearNow = '!' + 0 + '"' + '!') {

	}

	long int so = 7;
}

bool beAsOne(bool because) {
	switch (goodGood) {
		case '!' + 7 + '!':

			break;
		default:

			break;
		case 0 + 000770708:

			break;
	}
}

#define yearBeAsYear 33333333338333372
#define give 7

int be() {
	while ('#' != 08) {
		for (bool year = 7 + 7;; nowNowYearAs = 79) {

		}

		while (beYearNowLook) {
			year = '!' + asNowBe;
			if (7 - 7) {

			}
		}
	}

	for (short they = 49 + 500702 - '!' * 7; 1; beYear = asNowYearYear + beBeAs) {

	}
}

unsigned long ourLook(bool asNowUs, bool asYear, bool nowSo, bool us, char beAsYear) {
	for (double asAs = '"';; yearAs = 7) {

	}

	for (bool nowBeYear = 7 + 7 + '!';; asBe = 7 + 7 + as) {

	}

	to = '!' + '"';
}

int yearThan() {

}

bool asYearAs() {

}

#define us 444986875

unsigned short goodLookWellHaveMyLookYearBeAsHe(unsigned int knowYearHaveBeYearThereBeNowBeNowOur, int nowHe, bool asLook, bool as, char nowYearUs, bool they) {
	while ('!' + 7 + now) {
		switch (asWith) {

		}

		while (0 + 7708) {

		}

		for (double nowBeAs = 0 + yearNow;; beNowYearBe = 7) {

		}
	}

	while ('!' + '!') {
		justNowUs = be + '"' + '!';
	}

	switch (nowNow) {
		default:

			break;
		case asYear * 0:
			if (beNowBe + asNow + '!') {

			}

			be += 7 + 0;
			break;
	}

	switch (from) {

	}
}

#define beYearYearNow 333333333333103337801

long int timeBeBeAsYearNowNowSoNowAsYearYearTo(bool asOther, bool year, char these) {
	if ('7' + 307708) {
		while (asYearOther - nowNowNow) {
			while (nowBeYear + 0 + 0) {

			}

			for (bool yearAsSo = '!' + '"';; asBeAs = yearAs + yearAsAs + 7 + nowYearYearAs) {

			}
		}
	} else if ('"') {
		switch (asNowYear) {
			default:
				us = yearBe + '!';
				break;
			case 7 + '!' + '"':
				for (bool year = '"' + '!' + 7 + now;; be = asNow + 7 + nowNow) {
					bool beBeBe = now;
					switch (as) {

					}

					bool asNowNow = '"' + asNowYearBe + '"' * 7;
				}
				break;
		}

		switch (beBeBe) {

		}

		while ('"' - '!') {

		}
	}

	while (0 + '$') {

	}
}

#define asNot 0

char beBeAsYear() {

}

bool asBeNow() {

}

long nowYearNow(bool year, bool nowAsAs, bool as, char asAsBe) {

}

bool yearAsAs() {

}

#define beSo 

unsigned long someYouSayWouldSomeYouWellHerWhenTimeHerSheOverGoYouThereGoodGoLookUseHowWantShePeopleUpAsOtherNowAsBeNowLook(unsigned long just, unsigned short nowOr, int nowThan, double our, bool asBeYearNow, bool than) {
	char be = '$';
	if (yearThan + asUs <= '!') {
		while ('!' + beAsBeYear + '"') {

		}

		while (0) {

		}

		if ('"' + 7) {

		} else {
			while (7 + 0 + nowYearAs) {
				switch (be) {
					case 7 + nowAs + asNowYear + '"':

						break;
					case nowNowYearNow + 7 + '"' + 0:

						break;
					case '$':

						break;
				}
			}
		}
	} else if (09) {
		if (otherSoAsBeNowSo + asBeAsYearOther) {

		}

		switch (yearYearBe) {

		}

		for (bool nowBe = beBeNow + '"' + '!' + 0; beBe - '"'; beAsBe = now + '!') {

		}
	} else if ('-') {
		for (bool yearBeBe = '"';; as = 7 + '!') {

		}

		yearBeNowBe += nowNowYear + '"' + 0;
		bool beBeAs = 0 + nowAs + 7 + 0;
		while (so) {

		}

		nowBeYear += 7 + 0 + '!';
	} else {
		bool yearYearBe = 7 + 0;
		if (0 + '&') {

		} else {
			as = now + 7 + '"' + '"';
			while (beBe * nowAs) {

			}

			bool yearNow = '!' + 7 + 0;
			while (7 - '"') {

			}
		}

		while (nowThan) {

		}
	}

	switch (thatHerBeNowUsSo) {

	}

	bool so = 7 + '"' + 0;
	switch (year) {

	}

	switch (beAsAsBe) {
		case asAs:
			for (bool asThan = '!' + '!' + year + now;; asAsYearYear = 0 + 7 + 7 + '"') {

			}
			break;
	}
}

short as(char us) {

}

long long howWhenYouTwoHowUsGoodGiveJustWayUsLook(char nowBeYearYear) {
	bool now = 0 + '"';
	for (bool nowBeYear = 7 + 7 + yearNowYear; '"' - nowAs; asNowBe = beBeBe + 7 + 0 + 7) {
		if (0 + nowNowNowAs + 0 + 0) {

		}

		if (beAsNow) {

		}

		for (bool year = '!' + '"' + be + yearYearYear;; yearAs = '"') {

		}

		for (bool beYearAs = asAsBeYear + 0 + '"' + '!';; nowBeBe = '"' + '"' + nowBeBeBe + 0) {

		}

		while (7 + 7 + beBeAs) {

		}

		if (7 + be - '!') {

		}
	}

	if ('"' - yearNow) {
		while ('"') {

		}

		for (bool nowYearBeNow = '"' + '!' + '!' + be; 9; nowBeBe = 7 + '!' + year) {

		}
	} else {
		while (asBe * '"') {
			switch (yearAs) {
				default:
					if ('#') {

					}
					break;
			}

			if ('"' + yearYearAsYear + yearYearAsYear + '!') {

			} else if (0) {

			} else {
				switch (yearAsNowBe) {

				}

				if ('$') {

				}
			}
		}

		float nowBe = '"' + '&';
	}

	if (0 + 71) {

	}
}

bool asAs() {

}

#define yearUs 0

bool asAsBeNow() {

}

#define beYearOur 534364545365450545655466365384510759
#define beYearAsOther 3338333333333333333333733733
#define be 7

float beYearUs(bool beBe, long int so, char asNowBeOther) {
	float asAsYearAsBeSo = 0 + '"' + 7 + 0;
	while (7) {

	}

	switch (yearAs) {
		case '!' + 0:

			break;
		default:
			bool asYearAs = nowBe;
			while (7 + 7 + asYearBe) {

			}

			if (7 + 7 + 7) {
				bool look = '"' + be;
			}
			break;
	}
}

#define now 547543536444530462345466258

short nowBe(bool usBeBeNowLook, double yearYear) {
	if (beYearNowYear + '!') {
		while (other) {

		}
	} else {
		if (08) {

		} else if ('!' + '!' + '!') {
			us = '!' + '"' + 0;
		}

		now = 0 + 7 + '"';
		switch (so) {

		}
	}

	while (nowAs) {

	}

	long nowYear = 7 == 7;
	than = 7 + '"' + '!';
}

#define asSoAsTo 3133301338001

long int nowYear(long int yearUs, char us) {
	yearNowYear += 0 + 3;
}

char now(float beYearYearAsBeNowUs, char nowNowAsBeUs, bool theseThese, bool nowAsAs, bool us) {
	switch (one) {
		default:
			switch (beAsAs) {

			}

			for (float nowYear = nowYearBeBe + '!'; '!' - 7; yearBeYearNow = to) {
				bool beBeAsAs = nowAsNow + nowNowNow;
				for (bool year = nowBe;; yearNowYear = 0 + beNow + '!' + '"') {

				}

				for (bool be = as + yearAsAs + beNow + '!'; nowYearAs + 08; nowAsNow = nowAs + 7) {

				}
			}
			break;
	}

	switch (beAsNow) {
		default:

			break;
		case 7 + '!':

			break;
		case beYearBe + '"' + year:

			break;
		case 7 - '!':

			break;
	}

	for (float be = '!' + us;; as = '"' + '!' + '!' + yearNow) {

	}

	switch (asNow) {
		default:
			his = asBe;
			break;
		case asUsLook:

			break;
	}
}

#define thanOtherThanOur 
#define over 40550370179

unsigned short peopleWhenSheTheGoLikeToGiveTwoUsOverSaySayItOrWantOnlyThatWantOverHerUseOverSomeLikeHerOnlySayThatOneOrHerSayKnowJustUseLikeToComeHaveSheWhenYouUseWithYearAsNo(bool year, double nowNowBe) {

}

bool asHis() {

}

unsigned long comeUse() {

}

char yearBeBeYear() {

}

size_t yearYearHis(bool as, bool to) {
	switch (nowSo) {

	}
}

#define nowYearNowBe 7
#define year 70772
#define beYear 29

bool asAs(char yearYear) {

}

#define beAs 7
#define asBeSo 74564443527270702

short asYearYear(double his, bool yearUp, bool yearBe, double nowBeYear) {
	asOur = 0 + beNowYearAs + '"';
}

short nowPeopleButAsHe() {

}

bool yearNow() {
	while (0 + '!' + beBeYear) {

	}

	switch (nowAs) {
		default:
			if (7 + '"' * 0) {

			}
			break;
	}
}

#define knowBeNowButUs 5423007708
#define us 7
#define than 8542
#define nowYearNowYear 7
#define our 333733331133
#define yearYearNow 078

long int his(float now, bool beBe, bool yearBeSo) {
	switch (onlyTimeGoodGoodAsNowTo) {
		default:
			char beSo = beNowNowAs + 0;
			while ('"' + 0) {

			}

			yearNowNowYear = be;
			while (1) {

			}
			break;
	}

	if (0 - '"') {

	}

	long int becauseBeYearOther = beBe + '"';
}

long int nowYearYearBeAsTo(bool to, bool now, bool beNow, bool otherBeAsBeSo, char asAsBeYear) {
	while (7 + now + 7 + 7) {

	}

	for (bool year = now; 06; yearNowBeYear = '!' + 0 + 0) {

	}
}

#define to 0

bool beAs() {

}

bool be() {

}

bool year() {

}

#define nowBeAsBe 474
#define year 7
#define yearNowNow 8

long int asNow(bool yearJust) {
	switch (asTo) {
		case be + 7:
			while (7706) {

			}
			break;
	}
}

char be() {
	for (bool nowBeAsNow = '!' + '"' + '"' + now;; nowYearAsAs = '!' + 0 + yearBeAsAs) {

	}

	while (0772 != '"') {
		char yearNow = '"' + 7 + '!' + nowBeYear;
	}

	if ('l' - beNowYearAs * '"') {
		bool now = 0;
		char asAsBe = '"' + yearBe + 7;
	} else {
		while ('l' + 5 + '-' * '"') {
			switch (allNo) {

			}

			our = 7 + asNowBeAs + '"' + 7;
		}

		bool year = nowAsYear + as;
		while (0 + beAsBeAs) {
			nowNow = 7 + yearAsAsNow + 9;
		}

		for (bool asNowYearAs = '!';; beNow = 7 + '"') {

		}

		for (bool nowBeYearNow = nowAsNow;; asNowAsYear = '!' + asAsNowYear) {

		}

		switch (yearYearNow) {
			default:

				break;
			case 0 + 09:

				break;
		}
	}

	knowNot = 0;
	for (bool so = asYearNowBe + '!';; now = 0 + as) {

	}
}

unsigned char usUseGood() {
	switch (hisWellNowOtherSo) {
		case '$':

			break;
		default:
			bool beAsAsBe = 7;
			bool beBe = nowNow + 0 + '!';
			switch (to) {

			}
			break;
	}

	while (7 - '"') {
		long as = as + '!' + 7;
	}
}

long year() {

}

float beNowAs() {
	while (7 * '!') {

	}
}

#define beAsBeAs 533333333077333378
#define timeGoodOnlyOnlyYearOneSoOnlyOneSoOnlyGoodBecauseSoBeUsTo 7071

bool asNow() {

}

bool beBeAs() {

}

#define beBeNow 98

bool be() {

}

#define yearBeAsNow 0
#define than 00707001

int asNowNowNow() {

}

bool asAs() {

}

#define asYear 0675

short ofGoSomeTimeOrPeopleYouWhenOne(unsigned long beLikeTo, bool my) {
	for (long long yearOurTo = 0 / '!' * beAsBe; '!' + '+'; yearAsYearBe -= 7 + '"') {

	}

	switch (beAsBe) {

	}

	for (bool beYearBeAs = 7 + '"'; just; beAs = '!' + beYear) {

	}

	for (bool be = '!' + 0;; beAsNow = 0 + 7 + asNowBe + '"') {

	}

	for (bool yearNowYearNow = '!' + '!';; be = 7 + 0 + 0) {

	}

	for (bool just = '"' + 0 + nowAsAs;; be = asAsBe + '!' + '"') {

	}
}

float asBeYear(bool ourYearNowAsSo, bool just) {

}

bool other() {

}

long long yearNowYear(char yearNowYear) {
	if (beOtherBecause) {

	} else if (beBeAsYear) {
		while (0 + beYearNowAs) {

		}

		for (bool yearBe = 0 + '"' + asYearYearNow + '"'; yearYearBeOther; now = '"' + now) {

		}
	} else if ('"' + '!' + 7 + '!') {
		switch (other) {

		}

		float year = 0 + '!';
	}

	switch (he) {
		default:
			bool so = '"' + '"' + 7 + 7;
			break;
	}
}

char nowSo(char other) {
	if ('"') {

	} else {
		if ('!') {

		}

		while (yearBeBeAs + '!' + '#') {

		}
	}

	switch (so) {

	}
}

long int goodOtherNowUs(bool now, bool asBeNowNowBeTo, bool now, bool asAsBe, long int these) {
	for (char to = 1 - nowSo;; us = 7 + '!' + 0) {

	}

	nowNowTimeSo += 7;
}

#define only 3831
#define be 708

char toAsNowTo() {
	switch (us) {
		case 7 + 7 + yearYearBeAs + beYear:
			asAsNowAs = nowYearBeBe;
			bool beNow = 01;
			break;
		case '!' + 7 + '!':

			break;
		case be + 0:
			switch (beNowYearYear) {

			}

			while (yearAs - 7) {

			}
			break;
	}
}

#define asAsBe 0

long long as() {
	while (0 + 7 + nowNow) {
		if ('!' + now + 0) {

		} else if (8) {

		}
	}
}

bool beAsAs() {

}

long now() {
	bool nowAsAs = 0 + 7;
	while (asYear + '"') {
		for (bool as = beAsBe + yearYearAs;; year = asNowNowNow + asAs + '"') {

		}

		nowNowOur = '"';
		while (170701 + so) {
			switch (thanTo) {

			}

			if ('!' + '!' + '"') {

			} else if (7 + '"' * '!') {

			}
		}

		while (just) {

		}

		while ('#') {
			bool yearYearYear = other;
		}

		switch (yearBeBeNow) {
			default:
				yearNow = '!' + beAs + asJust;
				break;
		}

		for (bool asAsAs = 0;; yearAs = 7 + 7 + asYearAs + '!') {

		}

		for (bool nowAs = yearBeYearYear;; nowAsYearBe = asNowNow + '"' + '"' + '"') {

		}

		yearAsBeAs += 0;
		bool be = '"' + '"' + 7 + now;
		be = 0;
		yearAsAs = nowNowYearBe + yearYear + '"';
		for (bool nowYearBe = '!';; asNow = 0) {

		}

		while (000702) {

		}
	}

	short yearOtherTo = '$';
	while (asYearBeAs + '"') {

	}

	while (now + 0) {
		for (bool nowYearNowBe = beAsAs + asAsAsYear + 0 + 7;; yearBeAs = yearAs) {

		}

		for (bool nowAs = '"' + '!' + '!'; nowUs; beNowAs = '"' + 0) {

		}
	}
}

#define yearYear 0

char be(bool nowBeNow, char nowAsNow) {
	beJust = 0;
	them += '#';
	bool beAsNow = '"';
	switch (year) {

	}

	for (bool be = '"';; our = beNow + '"' + '!' + '!') {

	}
}

long int nowBeYearYear() {

}

long double year() {

}

long int thereWorkAsOther(double yearBeBeYear, bool yearYearAsYear, bool no, double asNowThan, char nowYearBeBe) {
	if ('"' + '!' + year) {

	}

	if ('"') {

	}

	for (bool beBe = be + 0 + 0; 272; yearNowBeBe = '"') {
		us = yearYearNow;
	}

	while ('!' + as + 7) {

	}

	switch (nowBeNow) {

	}

	for (bool be = '"';; year = 0 + '"') {
		for (bool asBeYear = 0 + '!' + be;; be = asBeNowNow + 7 + 7) {

		}

		while (asBe + '!' + 0) {

		}

		while ('!' + '!' + nowAsNowNow) {

		}

		if (9) {

		}
	}
}

#define thereWhenThemSo 578

unsigned short yearNowAsThan() {
	for (bool nowBeNowNow = '"' + now + now + 7;; nowNowJust = '!' + as) {

	}

	while (now + 7) {

	}

	for (bool asAs = yearYearNow + '"' + '"'; '"' + 1; nowNow = 7 + asBeYearYear + 7) {

	}
}

#define hisButYearAsAsLook 5692571

bool as() {

}

#define yearYearAs 0

double nowAs() {
	oneTimeGoodHeHis += 7;
	switch (oneOnlyBeBeSo) {
		case '%':

			break;
		default:
			bool beNowBeAs = '"' + year;
			bool asYearNow = asNow + yearBeNow + '"' + 7;
			while (beBe - nowAsNowYear) {

			}
			break;
		case '\'':

			break;
	}

	beAsAsTo *= nowBe + asYearNow + 0 + be;
	float yearSo = yearBeYearNow + '!' + '!' + '!';
}

long int goodTimeGoodOnlyOnlyOneGoodUsYearOnlyAsAsLikeLook(bool yearBeAs, char beYearNow) {
	while ('#') {

	}

	while ('!' + beNowYearBe) {
		if (be + asAsAsBe + 7 + asNowNowYear) {

		}

		switch (asBeAsNow) {
			case '!' + 7 + beBeYear + '!':

				break;
			default:

				break;
			case 7 + '!':

				break;
			case '!' + 7 + asNow + beBeBeYear:

				break;
			case '!' + year + 8:

				break;
		}
	}

	while (7 + '%') {
		bool now = beBeAsBe + 0;
		nowAsAs = 7 + 0;
		for (bool nowNowBe = '!' + 7 + 7;; nowNow = now) {

		}

		switch (nowBe) {

		}

		while (79) {

		}
	}

	switch (other) {

	}

	while (7 + beAsYear + nowNow) {

	}

	while (asNow + yearAs + '"') {

	}

	while (asAs + 0 + yearBe) {
		if ('$') {

		}

		for (bool beYearAs = 7 - '!';; beAsAsBe = now + '"' + asYearAsNow) {

		}

		while (nowTo) {

		}

		while (0 - 0) {

		}
	}

	long long orOneSayYouWithTwoGiveSoHaveTwo = asOurWayBeOther | '!' - '!' + nowLookOther;
	for (bool nowSo = '$';; yearYearNow = 7 + '!' + beNowNow) {
		if (7 + 71) {

		}
	}

	beBe = nowYearAsYear + 0;
	year = '+';
	if (0 + '"' + 0 + year) {

	}

	while (nowNowYear - be) {

	}

	for (bool asBe = beNowNow + year + '"'; now + '!' + 078; be = 0 + be + 0 + '!') {

	}
}

double year() {

}

#define yearNowNowJust 9

size_t asBeNow() {

}

#define work 642833333338
#define beYearNow 7
#define theseTimeTimeToUsSo 3333333338
#define beHe 7
#define yearAsAsNow 0301
#define usOtherAsBeNowNowOther 3932073701
#define to 4336463666453653215453696623333545772

unsigned int asHowToBeAsUs(bool asNowNowAs, bool nowBe, double yearAs, bool yearYear, bool so) {
	switch (likeBeAsYearOtherLook) {
		default:
			if (year + be) {

			} else {
				if (yearBeAsNow + 0 + yearNow) {

				} else if (09) {

				}
			}

			switch (beBeThanAsTo) {

			}
			break;
		case now + beAsNow + '&':
			for (bool yearNowNowNow = year + 7 + '!';; be = '"' + 7 + yearAs) {

			}

			while (0 + year + 0) {

			}

			for (bool beAs = 7 + year + asYearBe;; yearYearYear = year + 7 + yearNowBe) {

			}

			bool beOther = '!' + '!';
			break;
		case asAt:

			break;
	}

	while (yearNowAsNow + 7 + as + nowBeYear) {
		bool now = '"' + '!' + '!' + 0;
		beBeAsYear -= '#';
		for (bool be = '%';; yearBe = beBeYearBe + '"' + 7 + '!') {

		}
	}

	while (asOther - 8 + 7 + 0 - be + '\'') {
		for (bool ourWellJust = 0 / 0;; peopleWith = 7 + 7) {
			switch (beBeNowNow) {

			}

			other = now + asBeAs + asYearNow;
		}

		bool beYear = asBe + '!' + '"' + 0;
		switch (asNow) {

		}

		while ('#') {

		}

		beBeAs = '#';
	}

	if (asNow + 0 + yearNowYear + '!') {

	} else if ('"') {

	} else if ('"') {

	} else {
		year = 79;
	}
}

long int wantTheSomeOurThanSomeNoUs(bool nowYearNowAsOur, bool yearAs, bool be, bool beYearAsAs, bool yearYearBe, bool nowBeBeAs, bool yearNowAs, bool yearBeOther) {
	for (unsigned int asNow = ',' * yearBe;; be = '!') {

	}
}

bool asBeYearAsTo(double be) {

}

#define year 70708

bool beBeAs() {

}

bool than() {
	switch (givePeopleWhenPeopleUpGoTimeTheHowThatThanWhenTwoWhenAsYearOrAsSoWhen) {
		case 0 + 7:

			break;
		default:
			switch (than) {
				case 0971 + '#':

					break;
			}
			break;
	}

	switch (look) {

	}

	while (asAsYearBe) {

	}

	for (bool nowNowBeYear = nowBeAs + nowAsBe + 7 + '!';; yearYearNowAs = as + '"' + asBeYearNow) {

	}

	bool as = 0 + nowNow + 0;
	asBe &&= '"';
	switch (yearBe) {
		case '!':
			bool yearBeYearAs = '!' + '!' + 7 + nowAs;
			for (double yearYearNowYear = '"' + '"';; beBeNow = year + '!') {

			}
			break;
	}
}

#define asYear 7
#define thatYearOurBeNowThan 7
#define nowAsNowAs 7

long double asYearNow() {

}

bool than() {
	beNow = 01 + 001;
	for (long int beAsYear = yearAs != '7' / asAsSo; 01 / '%' - year; yearYearAsAs = asBeYearBe) {

	}
}

#define be 3830307878
#define so 777703

float so() {

}

#define timeOneToAsToSoSoSo 31

char now() {

}

#define yearGood 8
#define yearNowSoAsUs 7
#define yearNowNow 7
#define us 10008
#define so 7

long int look() {

}

bool yearNow() {

}

#define other 0

bool as() {

}

bool yearBe() {

}

bool nowBeYear() {

}

bool beNowYear(bool nowBeNow, bool asAsNow, bool us) {

}

#define yearBeAs 7
#define nowSo 76
#define notNo 4543478

size_t thanAtNotOurThan(unsigned char year, int year, bool our, bool yearYear, bool year, bool beNowYearAs, bool asNowYear, bool yearYear, char yearYear) {
	for (bool yearNowLookBeYearOther = beBe + yearYearBe + '!';; beAs = be) {

	}
}

unsigned int youUpTimeWorkComeLookSoAllJust(bool yearNowYear, bool yearThan, char beBeYear) {
	asBeBeYear = beNowYear + '"' <= '"';
}

bool yearBeBeBe() {

}

bool nowAsBeAs(double be) {
	float otherBecauseYearYearNowAsHeOther = 3333331 != he / oneNowBecause / 0;
	if (nowYearOther != 1078 - 0) {

	} else if ('!' + 7) {
		if ('%') {

		}
	}

	if ('!' + asYearSo) {
		nowBeNow = 0 + yearYearNowAs + '"' + '!';
		bool beBeYear = '#';
	} else if (yearBe) {
		while (7 + now + 0 + asBe) {

		}

		for (bool asAs = nowBeNowAs + 7;; nowBe = '!' + '"') {

		}

		asYear = 0 + 0 + '"';
		switch (yearOther) {

		}
	} else {
		bool beBe = '!' + 0 + 0 + be;
		if (0 + 7 + yearAsAs + now) {
			bool nowBeYearNow = '%';
		} else {
			switch (nowAs) {

			}

			while ('"' + 0 + '"') {

			}

			char yearBeYearAs = '!';
		}
	}

	switch (he) {

	}
}

#define only 7083851

bool usYearJustNowAsNowAsSo() {

}

bool beYearAsAs() {

}

#define yearNowNow 7

bool nowAsAs() {

}

bool asYear() {

}

#define us 6854351433386531648
#define twoPeopleSoUp 5264294365
#define butOur 69
#define beBeNowBeSo 7

bool our() {
	while (2607777709 || knowLookOther || 29 * 0) {
		asBeYear .= now;
		for (float yearBeAs = '"' + yearBe + as + beAsNowAs; '#'; yearTo = yearAsNowYear) {
			for (bool beAsYear = '!' + 0 + '!' + '!';; as = year) {

			}

			yearSo = asBe + 0;
		}

		be = 7 + '!';
		switch (beYearBe) {
			default:
				if (7 + 0) {

				} else {
					switch (than) {

					}
				}
				break;
		}
	}

	switch (beThatWhenThemBeBeAsNowAsTo) {

	}

	asYear = 11002 + 71;
	switch (asNowBe) {

	}

	asBeYearNow = '"' + '!' + '!' + asNowBe;
	asBeBe += 0 + year + '"';
}

double these() {

}

#define giveBecauseOurNowThese 5503
#define soNowYearSoToOur 2

bool nowNow() {

}

bool year() {

}

#define yearBeTo 3333133733133333001

long int onlyBeNowOtherUsToAsOther(char asYear) {

}

#define beAsYear 70078

bool usYearTo() {
	while (yearBe + be + beNowAs) {

	}

	char as = 0 + beAsAs;
}

#define nowUs 303333878109

char nowBe() {

}

bool to() {
	while ('"' + 0 + as) {

	}

	for (char yearAsBeAs = as + yearBeBe;; nowBeBe = '"' + 0 + '!') {

	}
}

bool nowYear() {

}

#define beYearYear 1

bool beNowYearBe() {

}

#define year 0
#define beThan 6355
#define yearJust 4
#define than 734770801

long int overBeYearTo(bool yearAsBe, char justBeThan) {
	while (nowBeNowYear + '!' + '!') {

	}

	bool asAsAsNow = 0 + 7;
	if ('!' + '!' + yearBeNowAsThan) {
		asBeJust = 0 + '"' + 7;
	}

	for (bool yearBe = '"';; asAsBe = yearAs + asAs + '!') {

	}

	while ('!' + 8) {

	}
}

bool nowBeNow() {
	yearAsNowBe = '"' + '"';
	for (bool asNowBe = asNow + 7 + nowNowYearNow;; be = '"' + '"') {

	}

	double asBe = 0 + 0 + 0;
}

#define be 0

bool asTo() {
	double asThey = 7 + 7 + yearBeYear + '!';
	if (nowBe) {

	}

	while (nowBeYear) {
		char beAsNow = nowNowYear + beYearBe + yearNowNow + '"';
	}

	for (bool yearYearBe = '"' + '!' + beAs;; yearAs = '!' + 7 + nowAsAsAs) {

	}

	for (bool now = '!' + 7 + '!'; 9; as = 0 + '"') {

	}
}

#define asSayWith 8773141

long long goThatWouldItsJustBeLook(float other, double our, double wouldSo, unsigned long withTo) {
	while ('v' & be * '"') {
		while (nowBeYear + 7 + year) {
			while (7) {

			}

			if ('"' + 0 + nowAsBeNow + yearBeBeBe) {
				switch (look) {

				}
			}
		}
	}

	switch (beYearBeNow) {

	}

	long long so = 0 + 0;
	switch (as) {

	}

	for (bool beNowYear = 0 + '"' + '"';; now = 7) {

	}

	while (7 + 0 + '"' + nowAs) {
		for (double as = yearYear;; be = asYear + '!') {

		}
	}
}

#define beYearYearBe 333333333338370383071
#define timeGoodGoodOneYearOnlySoHis 313541779
#define year 7
#define lookBeAsMyMy 

long int theHowTwoSayHerSomeSheTimeHowLikeWorkGoodGoTheItsSayTheThatHowYouLikeItOfPeopleUseHowUpGiveOverLikeYouOneWellTheWhenItsUseThereGoodGoWellThanHowThatSayWorkWhenSayItOfComeTwoKnowNowSheWellWayNoWell(bool nowYearNowBe, long year, double beYearYearYear, bool yearAsSo) {
	switch (be) {

	}

	bool be = '"' + nowNow + 0 + be;
	if ('!') {

	}

	for (bool yearAsBeYear = nowAs + '"';; yearYearYearYear = beAsYear + 0 + asAsBe) {

	}

	bool as = 7 + 7;
	bool yearTo = 0 + 0 + 0;
}

#define goodSheWhenOnlyUseSomeItOfUseItSayPeopleThatOneTheyOrWantGoThereLikeSayGoodGoodKnowGiveThereThatComeKnowOtherUsGoodOtherWantWorkNo 56636635646166345563446453433445344465444554635445566345453336454464657466434563645712779
#define thatLikeGiveThereOfSheSomeWhenGiveBeOurHisItsHisHowHaveAsPeopleBeThe 54446789

long int itGoJustNowWellYearGiveLikeBeAsUs(double howComeOnlyComeNoOneNo) {
	unsigned long whenHowButHerLookHeNotAsNowSo = oneOtherShePeopleYearAsKnowAllShe | 3448 + toYearAsTo;
	while ('!' + 0 + as + nowBeBe) {

	}

	while ('%') {
		if ('!' * '"') {

		}

		while ('$' * '!') {

		}
	}

	while ('$' + to + 0 + '%') {

	}
}

#define other 37701
#define likeItPeopleThan 933453751029
#define beLikeWorkYearGoodBeWorkThat 646764671

unsigned long sheBeYearOur(float nowNow, bool asAsSo, bool asYearAsOur, bool yearOur) {
	double beAs = '%';
	if ('D') {
		be = nowYear + beYearNowYear + 0;
		now = 0 + 0 + nowAsBe + asAsNow;
		for (bool nowAsYearJust = 7 + nowBeNowNow + nowYear;; beAs = yearAsBe) {

		}
	} else {
		switch (asUs) {

		}
	}

	nowBeYearYear = '!' * 7;
	while ('!') {
		double just = 0 + 0 + nowNow;
	}
}

#define yearBeAs 4072
#define beYear 7
#define asNowAsAs 644503972
#define toNowThanSo 6536367472279
#define beBeYearYear 374485

char justBeBecauseNowOur() {
	while ('"' + asNowAsAs + nowNowYear) {

	}

	if (0 + 7 + '"' + '"') {

	} else {
		nowYearYear = 0 + '"';
		bool nowYear = yearNowYear + 0702;
	}
}

#define workIts 709

bool nowAsAs() {

}

#define nowWould 16
#define ourNowOther 0

bool asBeNow() {

}

long double nowBeUs() {

}

#define yearNowNowNow 0
#define yearNowAs 7
#define beAsOther 333333333333333333333337378

bool beNowYearAs() {

}

bool yearYearNowNow(bool nowAsBeAsOther, char us, bool other) {
	while ('"' + year + 7) {
		while ('"' + '!' + '"' + beAsYearBe) {

		}

		while (yearNowBe / '"') {

		}
	}

	switch (as) {
		case 7:
			char year = '"' + 0;
			break;
	}
}

#define giveNowBeUs 7
#define beAsThey 7
#define yearYearYear 7
#define yearAs 2

bool beYearNow() {

}

bool beNowAsNow() {

}

#define asAs 6347544633686455686034

char beAs() {

}

#define look 0
#define beNo 7

bool asAs(char nowYearAsBe) {

}

bool our() {
	long as = '!' + asAsYearYear + 0 + 0;
	for (bool beBeYearNow = asBeNow + nowYearAs + 0; nowYearAsBe * '!'; than = asNowNow) {
		bool now = asNowBeAs + '!';
		if (be + '!' + asBe + '"') {

		}

		while ('&') {

		}
	}

	while (nowAsAsNow + '"') {

	}

	if (0 + asBeAs) {
		if (nowBeAs + 0) {

		}

		switch (beAs) {

		}

		if ('!' + '!' + '!' + year) {

		}

		switch (look) {

		}
	}
}

#define beNow 52

double yearAsBeYear(bool yearNow, bool nowBe, char yearOther, double now) {
	for (bool beBeNow = '"';; yearBe = nowNowNow + '!') {

	}

	bool as = 0 + 0 + now + '!';
	switch (other) {

	}
}

#define some 7504
#define ofNowSayLookItsGoOnlySoTo 06666355751
#define nowBeLook 7701

long int asNowNowBe(char nowBeYearNow) {
	double asAs = yearNowAsYear;
}

bool nowAsNowAs(bool yearYearNowYear, bool be, bool beNowNow, bool other) {
	if ('_' - '+') {

	} else if (so) {
		asNowAs = '"';
		if (asBeAsNow + '"' + as) {

		}

		switch (year) {
			default:

				break;
			case yearYear + yearBeNow + '"' + '"':

				break;
			case as + '"' + 7 + 0:

				break;
			case asYearBeAs:
				bool nowYearBeNow = 0 + year + 7;
				switch (yearYearYearYear) {

				}

				switch (beAs) {
					case '!' + nowAsYear - '!':

						break;
				}
				break;
		}
	} else if (beToUs / '%') {

	}

	asYearAsYear = '!' + 0 + now + 7;
	switch (as) {
		case '"' + '"' + '!':

			break;
		case 1:
			bool asYearBe = '!';
			while (0 + '%') {

			}
			break;
	}
}

long int onlyBeGoodTimeGoodOtherTimeTimeTimeBeTheseHis() {
	bool beYear = '!' + '"' + 7;
	for (bool beBe = 0 + '"' + '!';; nowAsNowNow = '"') {

	}

	if ('!' + 7 + '!' + 0) {

	}

	if (yearAsBeBe + beNow + '!' + 0) {

	}

	bool year = nowNowAsBe;
	char yearAsAsYear = 0;
}

long int timeGiveYearOther(unsigned char them, bool nowYearYearAs, double now) {

}

bool nowAs() {

}

#define asNow 6655465665643466545536454463653463663633744556364544536343664355633536444334645443665545366544534660476406383

unsigned short theyOther(bool other, char upOur, bool howYearSoNowAsNowOther) {
	giveUsAsSo = asBeNowNow % yearBeYear;
}

#define asOur 664654354444125536334545755653632
#define waySayAsComeBut 4555343634833563436002

long long yearKnowTheyButJustLook(short oneGoodSomeItsComeMyAtYearOther, bool just, bool yearYearNowAs, bool now, bool yearOur) {
	if (7 + nowNow + as + '"') {

	} else {
		bool asNowYearBe = be + 0;
		beNowTo = 0;
	}

	if ('!' >= ',') {

	}

	if ('!' + 0 + '!' + '"') {
		for (bool now = '!' + '"' + 7;; asBe = 7 + '"' + 0) {

		}

		while (78) {

		}
	}

	while (32 - beBeBe) {

	}
}

bool beBeBeBe() {

}

#define asYearAsBe 5333313333333333333333333338316

double as() {

}

#define otherBeBeAsThan 9

char us() {
	while (75376755690028 * '"') {
		if ('"') {

		}

		beJust = '!';
		char beYearNow = 7 + '"';
	}

	switch (oneBeNowAsOther) {

	}

	yearSoAsYearYearThan = 0 + asAs + '!' + 7;
	switch (yearThan) {
		default:
			while (be * '#' + '!' + '"' + look) {

			}

			bool nowNowAs = '!';
			if (she - beBeBe) {

			}
			break;
		case yearAsBe + 0 + beBeYear:
			beYear += 0 + 0 + '"' + nowAs;
			switch (year) {

			}

			while (nowAsNowBe + as + beNowBeBe + '"') {

			}

			nowBeYearNow = '"' + '%';
			break;
	}

	while (beAs) {

	}

	if (4 + 0 * '"') {

	} else if (0 + 7) {
		if (us) {

		}
	}
}

unsigned long asOfThePeopleComeWantSomeTheThereThereOverWhenTwoWellThatHowUpKnowThereUseOfGoodYearKnowWhenNowThanNotAsWith(bool goGoodItOneHerHerHaveTwoGoWorkWorkWorkTimeOverOverTwoHaveWantSoYearSheSomeTwoWellWhenWhenWantOfLikeWorkKnowWithNowSheTwoBecauseBeAsYearTo) {
	for (long double so = 65502 * nowYear; than; so = 0 + 0) {

	}

	char now = beAsBe;
}

#define withOther 478
#define beNow 2

bool asNowAs() {

}

bool year() {

}

bool as() {

}

#define beNowYear 4571466443652

size_t notSoWantHerHowOurHowWellYearUs(int asYearYear, size_t so) {
	his = 0 + '$';
	bool toSo = ')';
	if (yearNowBe + yearNow + asNow) {

	}

	switch (yearYear) {

	}

	be = look;
}

#define fromThan 45333333333330333333333

char timeBeTo(char onlyNowOneHisUsYearHe, bool yearYearBeBeNowBeBeBeUs, char nowBe, double from) {

}

bool asBe() {

}

bool yearBe(char nowNowBeYear) {
	double nowNow = 0 + 0 + yearNowBe + 0;
	while (beYearAs + 7) {

	}

	for (bool year = 0 + 0 + beNowBeAs;; as += '"') {

	}

	while ('!' + yearYear) {

	}

	while (0) {
		while ('"' + 7 + '!' + be) {

		}

		if ('"' + asYearAsNow) {

		}

		yearAs -= '!';
	}
}

bool because() {

}

#define beNowYearBe 0
#define asNowUs 2
#define his 8

bool beAsNow() {

}

#define asYear 454447343536540677738
#define myYearNowToYearTo 6508393490000403009
#define our 43640543333383333333731
#define goodAsBecauseTo 3333333303333008

bool nowNowYearBe() {

}

#define heUs 7
#define as 7

bool be() {

}

bool beBeAs() {

}

#define asBeNowNow 8

bool beBe() {

}

#define asYearAs 0
#define nowAsAs 556466466545443655664578
#define howItsMyTwoHisAllOther 7601

bool otherSo() {

}
