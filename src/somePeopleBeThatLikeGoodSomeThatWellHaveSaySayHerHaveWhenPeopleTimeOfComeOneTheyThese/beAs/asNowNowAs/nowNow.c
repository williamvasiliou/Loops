long int onlyOnlyOneBecauseYearHeOnlyOnlyHeNowYearAsUs(char otherTo, bool us) {
	while (asYear) {

	}

	while (beYearNowBe + '!') {

	}

	beYearYear = '!' + beNowBe + '!' + 0;
	if (nowNowNow + be) {

	} else if ('!' + 7) {

	} else if (7 + 0) {

	} else {
		for (bool be = beYearYearNow;; asAsAsBe = nowAsAsNow + 7 + '"' + 7) {

		}

		while ('!' + 7) {

		}

		switch (nowBeNowAsNowYearOther) {

		}
	}
}

char yearNowYear() {
	while (0 + nowAs + 0 + beYearNow) {
		bool because = year + yearBe;
	}

	while ('!' + '!' + 0 + 0) {
		char nowBe = 0 + beBeYearBe + yearAs;
	}
}

#define he 83
#define soYearThese 3333308
#define beAsBeBe 371708

char otherSo(char as, char be) {

}

#define asBeAsBe 7
#define nowAsAsYear 0

bool asAsBeAs(bool us) {
	switch (just) {

	}
}

bool beAsBeBe() {

}

bool to() {
	switch (goYouThemOur) {
		case 7 + than:
			char beNowYearYear = asAsBe + beAsBe;
			for (bool yearBeBeYear = 9;; asYear = '!' + as + 7 + asYear) {

			}

			for (int asYearAsBe = '"' + 0 + '!' * '"';; yearBeAsBe = asYear + '!' + nowNow) {
				if (7 + '!') {

				} else if (9) {

				} else {
					switch (nowYearNowYear) {

					}

					beYearBeNow -= 7 + beYearNowYear + '"' + '!';
				}
			}

			bool asNowNow = '"';
			for (bool now = now + beBeNow + 7;; as = beNowAs + 7 + '"' + 7) {

			}

			bool than = asAsYear + nowNowAs + 0;
			break;
		default:
			while (7 + 7) {

			}

			if (872 < nowNow + yearBe + yearAsYearBe + '%' + beNowBeUs + beNowAs - 0) {
				if ('"' + '!' + '"' + '!') {

				}

				while (8) {
					for (bool now = '"' + asBeBeYear + yearNow + 0;; be = '!' + be + '!') {

					}

					bool yearNowAsOther = '!' + '!';
				}
			}

			if ('"') {

			}

			switch (asNowYear) {
				case '!' + '!' + 0 + 7:

					break;
				default:

					break;
				case now:
					for (bool asSo = asBe + year;; now = beYear) {

					}
					break;
			}
			break;
		case 5605425 * soTo:
			switch (yearLook) {
				case year + asYearYearBe + '"':

					break;
				default:
					asAsAsAs = year - 0;
					break;
			}

			if (7 + '"' + '$') {

			}
			break;
		case '!' + 0 + '"':
			our = 0 + yearYear;
			break;
		case 0 / 0:

			break;
		case 0 * be:

			break;
	}

	float to = asYearNowAs + '!' + beYearAs + asNow;
	if ('!' + 0 + 0) {
		int so = '!';
	}

	for (bool nowBeBeYear = '"' + as;; nowYear = beNowYear + 0 + asYear) {
		for (bool beNowAsAs = 0 + '!' + '"';; yearBeYear = '!' + '"' + 0) {

		}

		if (0 + 7 + yearAsNow) {

		}

		if ('"' + 0 + nowYearBe) {

		}

		bool asBeAsAs = be * be;
	}

	if (0 + '!') {

	}

	asBe = 0 + nowYearAsNow;
	bool beAsNowNow = 7 + 0 + beAsYear + '!';
	beNow = '!' * nowNow;
}

#define yearBe 7
#define yearOther 1
#define peopleOur 7
#define nowNowYear 33333333137333333133333033371

bool yearAsAsBe() {

}

#define timeNowAsHe 707749739
#define asBeAsYear 7
#define yearOther 01
#define now 0

short yearBeBeBe() {
	switch (goHaveThereYouTheYearWorkAllThatOurPeopleTimeJustComeUsLook) {
		default:
			thatWellHeHisThese = 48;
			switch (nowThan) {
				case beNow:

					break;
				default:

					break;
				case '!' + 0:

					break;
				case 7:

					break;
				case yearYearAs:
					switch (beYear) {

					}

					yearBe += 7 + '!' + nowAsAsBe + 0;
					break;
			}
			break;
	}

	lookThanThanAll ||= wantWantLikeYouTwoWorkWellKnowJustAsThanHisBeYearBeOur + 0008;
	for (unsigned short asTheyBut = they | 'A' * justToToSo; yearUs * 7; beNow = '!') {
		if (0) {

		}

		char now = year + nowBeNow + '"';
	}

	unsigned int yearHaveTheOverPeopleUseThereGoodYearHisYearNowBeNowThan = 'i' != workKnowGoToHerWellYearHowWhenThatOneHis || 'y' >= '$' > 'a' - 'r' >= 'n' || ']' - 7 & now >= 'V' * '!';
	while (6652 > 'z' - '!') {
		unsigned long wantLookLookLook = year + 11 + 0 + beLook;
		for (double wayBeAsOther = 3 - '"'; overOurUs; nowNowNow = 0) {
			for (bool asBeBeBe = '!' + 7;; yearNowNowAs = 7 + nowYearBe) {
				for (bool than = be + beNowYearBe + '"' + yearYearYear;; yearYear = beNow) {

				}

				now = nowNowAs * beAsBeAs;
			}

			for (int beNow = 7;; beAsNowBe = 7) {

			}

			asNowBeJust = nowSo;
		}
	}

	if (4304360446656555564546356532454436447564352 / 'S' + toWork * beAsBe) {
		asAs = '!' + asYear + 0;
		if (0 + 7 + 0 + 7) {

		}

		while ('!' - asYear) {

		}

		while (7 + asBe + asYear + 7) {

		}

		bool nowYearYear = 0 + 0 + yearBe + 0;
		now = us;
	}

	for (char be = '!';; as = '"' + as + yearYear) {
		for (bool now = 0;; beYearNow = 0 + 7) {

		}

		while (7 + '!') {

		}

		for (bool year = beAs; beBeNow * '"'; beBe = 0 + 0) {

		}
	}

	while (asYearYearYear + 'h' || yearAsBe) {

	}

	unsigned char myWith = 9 * from;
	switch (yearYearBeYear) {
		default:
			for (bool asNowYear = 7 + '"' + '!' + nowAsNowAs;; yearNowYearBe = 0 + yearBeNow + nowNow + year) {

			}

			if (7 + '&') {

			}
			break;
	}

	switch (asJust) {
		case '!':
			if ('!' + to) {

			}
			break;
		default:
			look += ')';
			switch (as) {
				default:
					if (8 * now) {
						year = asThan;
					}
					break;
			}

			if ('!' + 7 + '"') {
				char yearBe = be;
			}
			break;
		case beBeNow + as:

			break;
		case '$':
			beBe -= nowAsBeNow * now;
			our = '!' + nowNowAs + 0;
			break;
		case year + yearAsAsBe:

			break;
		case '!' + '"' + year:

			break;
		case yearBe:

			break;
		case 7 + 7 + '!' + yearYearAsBe:

			break;
		case '"' + asYearNowYear:

			break;
		case 7 + be + '"':
			for (bool yearYearNow = 7 + nowAs + '!';; yearNowAsNow = 0 + nowBeNow) {

			}

			while (0 + '"') {
				bool year = '!';
				beNowNow = 0 + '"' + 0;
				for (bool asBe = '&';; yearAsAsAs = '"' + '!') {

				}
			}

			for (double asAsBe = 7 + 7;; beBeNowYear = as + '!') {

			}
			break;
	}

	size_t nowPeopleHowAsWellOtherIt = nowHer > 7 + 1;
	float allItAsNowOur = yearNowJust;
	while (asBeThan) {
		while ('%') {
			bool nowBe = '!' + beNowNowNow + 0;
			switch (asBe) {

			}

			while ('!' + asYearBe + '"') {
				for (bool beNowNow = 7;; yearNow = 0 + '!' + '"') {

				}

				bool beNowAs = '"' + '!' + '!';
				bool as = 0 + beBe + nowBe + asBeAs;
				switch (yearNowAs) {

				}

				if ('"' + beAs + '"') {

				}

				bool now = to;
			}
		}

		double nowYear = 0;
	}

	long long butBeAsYearSo = 609 - nowBeBeNow;
}

unsigned short twoSayPeopleThanThatWellComeOnlyTimeOrWhenKnowUpUseComeGoTwoWantItsTheYouSayGiveTimeThatWellItWorkSaySheYouHaveAtItThatTheHowWithGoHeTwoJustLook(short wellUpItGoodOneWhenHaveLookSayHerSheKnowTwoOverWhenTheJustHeIt, unsigned char itTheseSomeToAsHe, long way) {
	switch (yearYearThan) {
		default:

			break;
		case year:
			if ('!' + 0 + beBe + 0) {

			}

			for (bool asAsAs = '!' + '!' + 7;; be = 0 + nowBe + 0 + year) {

			}

			for (bool beBeAs = 7;; now = 0 + '"' + yearBe + '"') {

			}

			while ('"') {

			}

			while ('"' + 7 + 0 + 0) {

			}

			while (our) {

			}
			break;
	}

	while ('"' + 7 - '"') {
		long long nowBeTo = 7 < be;
		switch (year) {

		}

		if (2) {

		}

		asOther = 0 + '"';
		for (bool asNowYearBe = yearBeNowYear; '!' + 7 * nowAs; beNowYearBe = 7 + 7) {

		}
	}

	switch (yearYear) {
		default:
			switch (no) {
				case us:
					switch (yearNow) {

					}

					if (beNow * '"') {

					}
					break;
				default:

					break;
				case '!':

					break;
				case 0 + 7 + yearBeYear + 7:

					break;
				case nowNowYearBeOur:

					break;
			}

			for (bool asNowAsAs = '"' + now + '!';; nowAs = beNowBe) {

			}

			nowYearAsNow = 0 + 0 + be;
			for (bool asBe = 7 + 7 + '"'; us; yearAsBeYear = '!') {

			}

			if (7 + asNow) {
				for (char year = '!' + 0 + '"';; year = 7) {

				}
			} else {
				bool nowBeBe = 0 + asAsNowBe + 7;
				bool nowAs = yearAs + '"' + 7 + '"';
				if (7 + '"' + 0 + asYearAs) {

				}

				double beBe = '!' + yearAsAsBe + 7;
			}
			break;
		case 7 - '"' * nowNowAsNow:

			break;
	}

	int yearNow = 7;
	beBeBeBe = nowYearYear + 0 + '!';
	int asYearNow = '"' + nowNow;
}

unsigned char herComeGoodWantGiveThereTheJustOneTheMyItHis(unsigned long peopleBeYouSayWellTwoYearNowWantOtherUseTheOneNowWantGoOnePeopleYearBeAsNowOther, bool heAllUseMyYearThanYearOurJust, unsigned int useSo, bool yearYearNowAsYearNowOur) {
	while ('4') {
		lookYearTo <<= '!' + '!';
		while ('$') {
			while (yearBeBe) {

			}

			switch (yearNow) {
				default:
					for (bool year = 7 + 7;; as -= 0 + 0 + 0) {

					}
					break;
				case asAs:
					bool yearAsAs = yearOther;
					break;
			}

			nowBeNowSo *= '!' + 7;
		}

		year = beNowBe + 7;
		switch (asAs) {

		}

		if (nowAsBe) {

		}

		while ('"') {
			while ('"' + beNowBeNow + year + '!') {

			}

			while (7 + nowYearYear + 7) {

			}

			bool yearAsYearBe = '"' + yearYearAsAs;
			asBeYear = 0 + than;
		}
	}

	switch (asNow) {
		default:
			while (7) {

			}

			if (7 + 0) {
				double our = be + yearOther;
			}
			break;
		case than:

			break;
	}

	if ('"' + '"' + yearBeNowAs) {
		switch (asYearYear) {

		}

		bool yearYearNow = '!' + asNowBe + now;
		while ('!') {
			if (7) {

			}

			char nowBeYear = yearNowYearNow + yearAsNowAs;
		}
	}

	asYearYearNow *= '!' + yearYearNow + '"';
	yearYearYear = look;
}

bool yearAsBe() {

}

bool beBeBeTo() {
	for (bool yearNowNowNow = 0 + '"'; '$'; asYearBe = '!' * '"') {

	}

	switch (now) {
		default:
			switch (us) {

			}
			break;
	}

	while (',') {
		for (bool beAs = yearBe + '"';; asNowAs = 0 + '"') {

		}

		while (nowAs) {

		}

		beBeYearNow = 7;
		bool nowAsAsBe = 0 + be + yearBeBeBe + '!';
		if (asYearYearAs + 0) {

		}

		for (bool beBeBe = 7;; nowYearNowYear = '"' + '!' + 0 + now) {

		}

		for (bool yearBeAsYear = '&';; be = yearNowNow + '"' + 0 + '!') {

		}
	}
}

#define sayOverTheGoUpGoWorkHeTimeGoSayTo 46
#define beLook 7

unsigned char upHaveOverJustBecause() {

}

#define lookTo 7
#define yearSo 00071

bool yearAsBeAs() {

}

#define be 7

bool nowAsAsBeSo() {
	nowYear *= '!' + '!' + beNow;
	goodOneTimeGoodOneGoodOneTimeToBeYearYearYearUs |= nowGoodOneOnlyBeTo - 7;
	switch (nowAsYear) {
		default:
			while (nowBeThese) {
				bool so = beNowAs + nowBeBeNow + nowNowBe + yearNow;
			}
			break;
		case '#':

			break;
		case 0 + 0 + '"' + be:
			switch (as) {

			}

			nowBe = 7 + beNowYearNow + asYearYearAs + year;
			while (nowBe + '"') {

			}

			if ('!' + asYearBe + 0 + yearBeYear) {

			}

			bool nowNowAs = '"' + nowYearAs;
			as = '!' + now;
			bool asYear = 7;
			if (7) {

			}

			switch (other) {

			}
			break;
	}

	if ('#') {

	}

	if ('!' + 0 + '"') {
		as = '"' + 0 + 7;
		while (0 + 0) {

		}

		if (8) {

		}
	} else if (0 - beYearNow) {

	}
}

char now() {

}

float to() {

}

bool beSo(bool nowUs) {

}

#define nowBecause 3

char than() {
	switch (be) {
		case '!' + nowYearYear:
			for (bool beBeNowNow = '"';; beAsNowBe = '"') {

			}

			bool yearBe = '!' + 7;
			while ('!') {

			}

			as = '"' + asAs + yearBeYear + 0;
			for (bool nowNow = beYearYearUs;; yearNowAs = beNowNowYear + yearNow + '!' + now) {

			}
			break;
	}
}

char beAs() {
	switch (nowAsJust) {

	}

	if (now + '"') {

	}

	for (bool nowNowAsYear = beNow + 7 + 0 + yearYearYearYear;; now = yearYearAs) {
		if (nowNowNow + '!' + '"' + 0) {

		}

		while ('#') {

		}
	}

	for (bool be = 0; nowBeYearOur; as = 0 + '!') {

	}
}

#define asNowAs 7
#define other 4558440314505615777772
#define asUsNowUs 01
#define thatItsGoGoodJustNowLookLook 646184140179
#define yearAsNow 7

bool yearAsYearTo() {

}

char beNowYear() {

}

#define yearAsBe 7
#define our 0

bool look() {

}

bool be() {

}

#define yearYearBe 7

double beNowAs() {
	while ('%' - 0) {
		yearBe = nowYear + 0 + 7;
		while (0) {

		}

		for (bool nowBe = 7;; be = 7 + 0) {

		}

		if (7 + '!' + '!') {
			while (yearNowBe + year) {
				if (asBeNow + 0) {
					for (bool nowNowNow = 7 + beNowNowAs + beBeBeAs;; be = 0) {

					}

					bool beYearNowUs = '"' + 7 + yearNowBe;
				}
			}
		}
	}

	for (char beBe = 003; yearAs + 3; year = '"' + '!') {
		switch (asNowNow) {

		}

		while (asYear + nowAs) {

		}

		while (year + '"' + '!') {

		}

		yearYearAs = '"';
		if ('"' - 7) {

		}
	}

	onlyBeBeSoSo |= '!' - 7;
	switch (soUs) {
		case 0 + '"' + '"':
			if (0 - '!') {

			}
			break;
	}

	switch (other) {
		default:
			switch (nowNowNowAs) {
				default:
					if (be + '!' + year + nowAsAs) {
						while (0 + 7 + yearBeAs) {

						}

						nowNowYear = beBe + '"';
						nowYearYearAs = 7 + 0 + '#';
					}

					if ('!' + 0 + asBeYear) {

					}

					beSo = asBe + nowAsAs;
					break;
			}

			for (bool asNow = '"';; so = nowBeBeNow) {

			}
			break;
	}

	now += asYearOtherYearUs;
	if (7 + '%') {

	}
}

long int soUs(bool yearBeYearBe, bool nowNowNow, bool as, bool nowAsYearBe, bool beAsNow, bool asBe, bool beNow, bool asNowYearBe, bool asBeAs, bool us) {
	beAsNowYear = beNowSo + asYear + '%';
	for (char nowAs = 7 + 0 + as + 7;; now = '"' + asAsYearNow + asAsYear + 7) {

	}

	for (bool beBe = beBeAs + '"' + 7 + beYearNow;; yearAs = '"' + '"' + now) {

	}

	for (bool beAsBe = '"' + '!' + asAsNowNow;; yearNowNow = '"' + now + nowBeYear + 7) {

	}

	as += 7 + '!';
}

bool to(long int otherHeTo, bool now, bool beAsBeYear, bool as, bool nowAs, bool other) {
	for (bool asAsNowNow = 7 + '!' - asAsNowNow; us + '!' - '!'; asNow = '!') {

	}

	if ('!' + be + nowBeNowAs) {

	}

	switch (beYearBeAs) {
		default:
			time = 0 + '"' + 7;
			break;
	}
}

#define asUpAsSo 7
#define yearNow 7

int now() {

}

char yearNow() {

}

#define yearBeSo 777778
#define just 33645553543466436655363466553564554566446456564533653536465643435334636656445433345665556566646533555645333346355643335635346653053666553061466344444445594533453451
#define beItToJust 5656439464419353755

unsigned short wantTimeTimeFromKnowThatThanJustHerOtherUsThanOur(bool yearAsBeYear, char yearNowBeAs) {
	nowNowBeJust -= 7 + '"' - nowNowYear;
	asBe = 7 + 7;
	switch (nowNowNowBe) {

	}

	for (double year = '"';; beYear = '!') {

	}
}

bool they() {

}

double as() {

}

bool as() {

}

#define beYearYearBe 0

unsigned int beLook() {

}

#define as 65430555481

unsigned short orHaveTheTimeThan(float beAsYearYear, bool than) {
	long long asBeBeYear = ',' + our;
}

unsigned int ourOther(int ourAsUs, int beAsBeBe) {
	if ('"') {

	} else {
		if (7 - '!') {
			bool nowAsBeBe = 0;
			for (bool now = nowNow;; asYearBeYear = yearBeYearAs + asYear + 7 + '"') {

			}

			for (bool yearAsNow = '"' + '!' + nowBe + '"'; 701; asYearNow = 0 + '!' + nowBe + '"') {

			}
		}
	}
}

unsigned short asNowNotUs(bool to, bool nowNowNowYear, char beBeYear) {
	bool yearAsYear = 51378;
	heOneGoodOnlyTime |= '\'' + beSo + 0 - '!';
	switch (to) {
		default:
			switch (yearUsSo) {
				default:
					yearThese |= '"' + '"' + beBeBe + as;
					if (asNow + 0 + '"' + '!') {

					} else {
						for (bool now = 7 + nowBe + nowNow + be;; nowSo = 7 + 7) {
							for (bool nowNow = asAs + asBeYear + '"';; year = 0 + '"') {

							}

							switch (asBe) {
								case '!' - yearNowAs:

									break;
							}
						}
					}
					break;
				case '#':

					break;
			}

			long int hisNowTo = '!' + now + beYearBe;
			for (bool beHis = 0 + 7 + nowBeYear;; asYearYear = yearAsYear) {

			}

			if (be) {
				char nowBe = yearNowNowAs + nowNowYear + '"' + asAsBeAs;
			}
			break;
		case '"':

			break;
		case '!' + be + '"' + 0:

			break;
		case beNow:

			break;
		case 71:

			break;
		case be + '"' + '"':

			break;
		case yearBeYear + 0 + nowYearBe + '!':
			bool other = asNowYearNow + 7;
			break;
	}
}

#define goodOneAsBeYearBecauseUs 5830701

bool yearOur() {
	for (unsigned long ofTwoItsKnowAllOtherSayOtherAsLikeKnow = 'u' > 'l' + 248 > 'r'; yearJustBeYearHeBeBeUs & 7072 + '!' - beNow; yearNowYear = 7 + 7 + 0 + '!') {

	}

	bool nowBeAsAs = '"' * '"';
	while (7 + asBe + 0) {

	}

	asOur = 0 + nowYear;
}

double year() {
	if (yearAtSoLook | overThereOtherNo / now) {

	}

	switch (as) {

	}

	while ('!' + year + '!' + yearYearNowAs) {
		bool asOther = 0 + '!' + yearBe;
	}
}

unsigned long ourOfBeOverMyJust(double nowAsAs, bool just, bool yearAsNow, bool nowYear, bool as, bool asAsNowYear, bool yearAsYear, bool to) {
	while ('I' - beBe) {
		long int whenUsJust = '&' > asOther + be * '"';
		for (bool beNow = beAsNowAs;; asAsAsNow = beBe) {

		}

		for (char asNowBe = nowYear + 0 + '"';; asYearAs = asYearAsBe + '"' + 7 + '!') {

		}
	}

	for (bool nowAs = '!' + 0 + '!' + 0; other; beYearThan = '"' + beNowAsNow + yearYearAsNow) {

	}

	switch (asNowLook) {

	}
}

#define nowNowYearBe 0603
#define be 02
#define beYearYear 0
#define us 8333333333333333333333333383

bool yearBeOtherUs(bool nowBeYearBe, bool nowNowAs, bool us, bool asAsYear, bool so) {
	while (yearNowNowYear + 0 + asBeYearYear) {

	}

	switch (asOther) {

	}
}

#define nowOther 7

char beAsNowNow() {

}

char nowYearAs() {

}

#define asYearBeYear 7
#define nowBeNowBe 2

bool nowNow() {

}

#define yearYearYearAs 0
#define asYearAsNowNowThan 77078

long double asBeBeBe() {
	while ('"' + '!') {

	}

	look &&= 247654473 + 701 + justUs * nowBeAsYearNowSo;
	for (bool beBeBeBe = 0 + 7;; nowAsAsBe = '!' + 7 + '"') {

	}

	while (beBe) {

	}

	while (nowBeAs - as + 0 * nowAs) {
		if (7 - beNowAsAs) {

		}

		switch (as) {
			default:
				switch (as) {
					default:
						for (bool beYearBe = nowYearYear; asAs * '!'; asAs = beNowYear + '"') {

						}
						break;
				}
				break;
		}
	}
}

size_t knowThem(bool now, bool so, unsigned long these, char now, bool asUs) {
	if (7 + 7) {

	}

	double asAsAs = 3;
}

bool beNowAsBeOur() {

}

long double so(bool now, double year) {

}

long long toSoNowAll(long double beNowBeAsUs) {

}

#define asUs 7

double us() {
	if (year - 0) {
		if (17008 / 7773) {
			switch (yearNowAsNow) {

			}

			char us = beNow + '"' + as;
		} else {
			while (yearBeBe) {

			}

			bool asYearBe = '!' + '!' + asYear + '"';
			while (beAsNowYear + 7 + 0 + 7) {
				asYearYearNow = 0;
				if (7 + nowNowNow + '"') {
					while (8) {

					}
				}
			}
		}

		switch (yearNow) {
			default:
				if (yearNow + '!' + 0 + '"') {

				}

				for (bool be = 0 + 0;; year = 0 + '"' + '"' + beNow) {

				}

				switch (asYear) {

				}

				yearUs = 7 + be;
				for (bool yearOther = asYearAsAs;; beBeBeAs = 7 + '"' + '"' + '!') {

				}
				break;
			case yearNowNow + '!':

				break;
			case 7 + yearYearYear + '!':

				break;
			case '"' + yearBeAs + 7:

				break;
			case 1:

				break;
		}
	} else if (0 + beNow + '!') {
		other = 0 + '!' + '!';
	} else {
		while ('!' + '"' + 7) {
			for (bool asYear = 0 + '"' + 7;; as = 0) {

			}

			switch (other) {

			}

			for (bool nowNowYearAs = '"' + so;; nowBe = '"' + beAs + asAs + 0) {

			}
		}

		switch (beBeYear) {

		}

		bool asNowAs = 8 - 0;
		if ('%') {

		}

		while (asAsNowBe + 778) {
			bool asAsBe = asNowNowAs + 7 + 0 + 0;
			while (his) {

			}
		}

		for (bool nowYearYear = 7 + beAsNowAs + 7 + now;; yearBeYear = yearNowNow) {

		}

		char year = '%';
	}

	long int beBeNowNow = beBeAs + '%';
	while (007008 != 7 + '#') {

	}

	beYearBeAs += asBeNowAs + 7 + '!' + '"';
	beAsYear = 7 + be;
	yearYearAsBe = 0 - asYear;
}

#define yearNowNowBe 031007373
#define onlySoBeOther 3781
#define oneToUs 33932

bool be() {

}

#define now 9

bool yearAsThan(int beYearYear, bool be, bool asNowYear, bool beNowBeYear, bool because) {
	bool asNow = '\'' - '!';
}

unsigned char allNowBeUsBeLook() {
	for (long nowNowNowBe = '"' + 0 + 7 + '"';; nowAsYear = 0 + '"' + '!') {

	}

	switch (yearAsNowBe) {

	}

	while (nowBeAs + as + beBeNow) {
		beBeAsYear = asYearBeBeAsThan;
	}

	while ('%') {
		while (2) {

		}

		for (int asBeAs = 0 + 0 + 0 + asBeNowBe;; as = '!' + as + '!') {

		}

		switch (asNowAsYear) {

		}

		for (bool nowAsNowBe = '!' + asNowAs + 7 + yearBeNowYear;; nowYear = '"' + beAsYearNow + nowNowYearYear + 0) {

		}

		if ('!' + '!' + nowBe) {

		} else {
			bool asNowYear = nowNowBe + '!' + '"' + '"';
			nowAsNow = 7 + 7 + '!' + '"';
			for (bool yearAsBeYear = nowBe + yearAsYearYear + 0 + asYear; look; now = 7) {

			}
		}
	}

	while (7) {

	}

	for (short my = 7;; other = yearNowBeNow + '"') {

	}

	while (yearOur + us) {

	}
}

#define orBeThereWorkTheYearOverButTwoAllBeTo 644557365

size_t goWellToAsAsAsSo() {
	while (yearAsAs) {

	}

	double asNow = '"' + as + nowBeBe;
	us = timeOnlyGoodOneOnlyOneTimeHeYearSoNowYearOther != '_' != 8 / 3 - yearSoBecause;
	if ('"' - 0) {

	}

	switch (yearSo) {

	}
}

float theseBeSo(bool to) {

}

bool beBecause() {
	for (bool nowNowAs = '!' + '!' + now;; beBeNow = 7 + '!' + 7) {

	}

	switch (becauseYearAsBecause) {

	}
}

bool so() {
	if (so) {

	}
}

#define beAsBe 7

short yearBeYear() {
	if ('!' + beYearYearAs + be + '$') {

	}
}

bool asAsAs() {

}

#define now 0
#define look 46836465361065

unsigned char yearAsAs() {
	for (char nowTo = 7;; year = 7) {

	}

	bool asBeYear = 0 + 0 + 0 + 7;
	yearNow = '"' + 0 + '!';
	beAs = beYear + 0 + as;
	while (0 + '"' + 7 + 0) {
		yearOther = 0;
	}

	while (beYearYearBe + 7 + 7) {

	}

	for (bool beAsAsNow = as + 0 + nowYearBe + nowNow * '!';; beYear = '"') {

	}

	be = '"' + asAs;
	for (bool asNowYearNow = '!' + as - '!';; be = year + 7 + beAs) {

	}
}

bool be() {

}

#define likeLikeTwoOtherLikeHowOurBecauseUsUsJust 1336712

bool nowBeUs() {
	for (long int oneTimeTimeSoToBeOneUs = yearYearYear + '"' != yearAsOther; otherAsToThese != onlyOneTheseBeAsOther - beBeAsNow != year; asAs = beBeAsYear) {

	}

	for (bool nowAsNow = nowNowYearYear + '!';; as = '!' + '"' + '"' + 7) {

	}

	while (0 + 7 + nowBe - 7) {

	}

	switch (nowBeBeNow) {

	}

	if (7 - 0) {

	} else {
		if (1) {

		}
	}
}

#define to 7
#define beAsAsBe 0

long int as(float as, bool yearBe, char asNow) {

}

char be(bool be, char nowNow) {
	switch (asAs) {

	}

	bool us = 0 + year + 7 + 0;
}

long int asJust() {
	for (bool be = '!' + '!' + '"' + '"';; nowAs = '"' + be) {

	}

	for (bool beBeYear = 0 + asYearAs;; asNowBe += '"' + year + be + 0) {

	}
}

char lookBeNowNowAsThan() {

}

#define beYear 82
#define haveJustMyBecause 2558
#define saySomeMyTheWellPeopleWayNo 1

bool as() {

}

bool asAs() {

}

char nowAsBe() {
	otherUsBeYearLikeBeOurLook = asNowBeAs - 7;
	while (now + now + 0 + '"') {

	}

	if (7 + 0 * 7) {

	}

	overJustUpOrOfOneComeJustYearComeNoNowYearOur |= '$' + no + 7 - '"';
	switch (yearAsAsYear) {

	}

	if ('#') {
		switch (beNowAsBeYearBeNowBeSo) {

		}

		for (bool beAsBeBe = 0 + year;; yearAsNowYear += '"' + as + yearBeBe + '!') {

		}
	}

	if ('!' + nowAsNow + 0) {

	}

	float asOur = asBeBeYear;
}

#define toLook 44933
#define asOther 7

bool be() {

}

bool thanNot() {

}

double beYearYear() {
	if ('_' != '_' + nowNow + '!' != 7 + '#') {
		switch (as) {

		}

		year = 7 + '"' + as + 0;
		be = 0 + to;
		switch (beAsBe) {

		}

		while (nowBe + now) {
			if (beYearNow + '"' + 0 + 7) {

			}

			char asBe = beAsBe;
		}
	} else if ('!' / yearBeYearBe) {
		float nowYear = '"' + '!' + 0;
		if (as + yearNow + '"' + 0) {

		} else if (so) {

		}
	}

	if (7 + 0) {

	}

	if ('"' + '"') {

	} else {
		if ('#') {

		}
	}
}

#define becauseGood 333338373
#define nowUsOneOneThese 3336357563
#define yearThan 0

unsigned char yearAs() {
	switch (thereThatSomeOurPeopleTheHeBeTo) {
		case asAsNow + 7:
			switch (beBe) {

			}

			bool just = asNowYearNow + asAsYear + '"';
			break;
	}

	for (float asTo = '"' + beNowBe;; year = '"' + 7) {
		while (0 + 0) {

		}

		if (be) {

		}

		bool be = be + '"' + 0;
		switch (asYearYearYear) {

		}

		double nowBeYearYear = 7 + 0;
	}

	while (yearAsYearBe + beBeBeNow) {
		for (bool nowBeNow = '"' + '!' + yearNow + '"';; beBeNow = 0 * '"') {

		}
	}

	if (264) {

	}

	year = yearBeAs + 0 + '"';
	if (nowAsAs + '!') {

	}

	bool asNow = our;
}

unsigned short sheGo(bool peopleUs, float nowNowYearAs) {
	double wellBeOne = beAsBeBe + asAsNowNow + 0;
	switch (nowYearYearAs) {
		default:
			if (7 + 0 + '"' * beAsYearYear) {

			}
			break;
		case yearBe * '!':

			break;
		case beBeNow + year + beYearAs - 0:
			while (year * be) {

			}
			break;
	}

	while (1 + 7 - nowBeNowYear) {
		for (bool asYearBeAs = asYearNow + 0;; yearBeYear = now) {

		}

		switch (yearYear) {
			default:
				for (bool beNowBeNow = 0;; yearYearNowBe = 0 + asAsNowYear + yearAsNow) {

				}

				char be = '"' + nowAs;
				break;
		}

		yearNowAs = '!' - '!';
	}

	switch (asYearNowTo) {

	}

	bool be = 708;
	switch (now) {
		default:
			for (bool beNowNowAs = 0; '!' + '!' + '!' + '"' * nowBe; yearAsAs = 7 + yearYearBe + yearNowAs + asBe) {
				long int now = beTimeYearHe != beNowNow;
				while ('!' - yearBeYearNow) {
					switch (asBeAsNow) {
						case nowAsAsNow:
							if (nowBeAs + '!' + 7) {

							}

							bool now = year + 7 + 7;
							if (yearAs + '!' + beNowBe) {

							}

							switch (beYear) {

							}

							while (beBeNow + 7 + nowNowBeYear) {
								bool beBeBe = '"' + nowBeYear + '!';
								bool asAsYearYear = as + '!' + '"';
								for (bool so = '"';; beNow = '"' + '!' + asYearBe) {

								}
							}
							break;
					}
				}

				timeBeSo *= 07078 + 1;
				switch (asNowAsBe) {

				}

				if (yearBeYear + nowYearNow) {
					if (7 + 0 + nowBeYearBe + 0) {

					}

					for (char now = '"' + 0;; asBeBeAs = '!' + '!') {

					}
				}

				switch (now) {

				}

				switch (asAsNowYear) {

				}

				for (bool nowBeYear = '!';; asBe = 7 + '"' + '"') {

				}

				bool yearYear = yearNowYear + '"' + beBe + year;
				while (0 + '!') {

				}

				while ('"' - beBeBeNow) {

				}
			}

			if (yearNowBeYear + '"') {

			}

			for (bool asAsBeAs = as + 0 + '"';; be = asYearBe + 7 + 7) {
				if (so) {

				}
			}
			break;
		case '"':

			break;
		case '"' + nowYearYear + beBeAs:
			char asYear = '"' + 7 + '"';
			break;
	}

	bool because = '"' - 8;
}

#define timeHis 3333338313
#define year 3

long int hisAsBeJustAsHis() {
	nowSo = yearNowBeBe;
}

#define nowNowAsAs 7
#define just 403337872007079

bool beYearBeNowNowNowTo() {

}

#define soGoTo 354534456688343935382

double justAsJust(char asNowNow, char nowYear, bool his) {
	while (yearYearTo + now + 72) {
		year = yearNowAs + yearNow + 0 + '"';
		while (asBe + nowAsBe * '!') {
			for (bool as = 7 + '!';; nowBeAs = nowYearNow + '"' + yearBeAs + now) {

			}

			for (bool beBeAs = '!' + 7; 078; as = 0 + 0 + 0) {

			}

			switch (asNowYearBeJust) {

			}
		}
	}
}

short asPeopleGoodOurTo() {

}

#define nowBeYear 00709

float beBeBeAs() {
	switch (nowBeAs) {

	}

	now = 7 + nowYear;
	while (0 + '!') {

	}

	now = nowBe + '"' + 7;
	while (beYearAs) {
		look = 0 != 771781 != 1;
		switch (soOther) {
			default:
				switch (yearBeYearTo) {

				}
				break;
		}

		while (yearNowBe + other) {
			bool other = 7 + nowYearYearBe + beYear + 7;
			char now = asAs + 0;
		}

		if (nowBeAsAs + '"') {
			for (bool asAsYearYear = '"';; asNowAsAs = '!' + '"') {

			}

			for (char year = nowYear;; as = '!') {

			}
		} else if (to) {

		}

		while (7 + 7 + beAs) {

		}

		for (char beNow = '!' - beNowNow + '%'; '%'; now = 0) {

		}
	}

	while ('_') {

	}

	switch (asNowOther) {

	}

	switch (onlyBecauseGoodOnlyTimeGoodAsBecause) {
		default:
			if ('!' + '"' + 0 + '"') {

			}

			for (bool asNowNowAs = asYear;; nowBeYear = '"' + '"') {

			}

			so = nowYear + other;
			bool be = '!' + beNowNowNow - 0;
			break;
		case '!' + asAs + 7:
			be = '+' - 7;
			break;
		case 0 + nowTo:
			switch (asAs) {

			}

			bool asYearAsAs = '!' + 7 + '!' + beNow;
			while (nowNowNow + asNow + '"' + 1) {

			}
			break;
	}

	char us = '"';
}

long these() {

}

bool yearAs() {

}

char now() {
	to = beBeAsYear;
}

#define than 32404535635633686753146633783

long long beJust(bool beLook) {
	if (7) {
		asAll ||= 9;
	} else if (7 + '!' + 0 + asAs) {
		asYearAsAs = asBeYear + 0 + 7;
		asYearNow -= asYearYear + '!' + year;
	}

	if (beAsYear + '"' + '!' + 0) {

	}

	while (yearNow - 0) {
		yearBeNowBe = asNow + beNow + now + 0;
		bool beAsAs = 7 + beNowAsBe;
		for (bool beNowBeYear = yearNowAs + 7;; nowNow = '%') {

		}
	}

	switch (asUs) {

	}
}

#define beJust 4424577317072
#define someAsLook 4645638644
#define us 661066046101
#define our 4

float nowBeNow(bool than, bool asThan) {
	if (0 + 0) {

	}

	switch (our) {

	}

	while (7 + beYearNow) {

	}

	beAsAsYear = '"';
	bool beYear = beBeNowNow;
	beYearYear = as + '"' + beBeAsYear;
	asJust = '"';
}

bool yearAsAs() {

}

#define yearYear 7
#define beWhenOur 3333833333333333333703130033

float asOther() {

}

float nowYear() {
	if (his) {

	}
}

char asBeYearYear() {

}

bool yearNow() {
	for (bool as = as + 0 + '"' + yearBeAsAs;; to = asBe + '!' + now) {

	}
}

long long noNotYearBeAsLook() {

}

#define year 44355364655564536646573464564566530355633403403574
#define beAsWithWith 0
#define beNowAs 64445364453374466655633554455550242

char nowNow() {
	while (3 / '#' / 0) {

	}

	usBecause = 001;
	if ('#' - '!') {

	}
}

long int usTimeTimeTimeOtherOneOnlyOneAsGood(char nowBeAs) {
	if ('"' + beNowBe - year) {
		soYearBeBeAsTo |= '_' / be;
		for (float nowBeYearAs = now + be + '"';; as = beAs + '!') {

		}
	} else if ('"' + 0 + yearYearNowYear + '#') {

	} else {
		while (0) {

		}

		as = nowYear + '"' + '"';
		while (0 + 7 + 0 + year) {
			if (asNowAsNow - 7) {

			}
		}
	}
}

#define yearNowBe 73

bool asYearBe() {

}

#define be 88
#define be 0

bool nowBeNow() {

}

#define asOther 2
#define beAsNowAs 0

bool beAs() {

}

bool beNowBe() {
	for (bool as = '!' + asBeNow;; yearNow = yearAsAsBe + be + beBeBe + 0) {

	}

	for (bool so = 7;; yearBeAsNow = 7 + 7) {

	}

	for (char asNowYearBe = yearYear + 7 + 0 + '"';; asNow = 7 + '!') {

	}
}

#define asNowYearNow 3456443355545634632

size_t upSoTwoBeAllYearToOne(size_t lookToNowBeBeLook, bool beAsYear, bool nowNow, bool asYearNowAs, bool beBe, bool yearYearNow, bool be, bool asAsAsYearBeAsAsBeBeSo) {
	float yearBe = 0 + '!' + asAsAsAs + 0;
}

#define beNowNow 0

bool be() {

}

#define asSo 778
#define beBeYear 53337333333333333333331371

float to() {

}

#define onlyHe 0

short yearNowAs(char other) {
	bool beNowNow = nowJust - '!';
}

#define beBeAsAs 74
#define nowYearYear 0
#define thanAsBeBeTo 664645534545464444445444554554455356455633544366645454644564344643344653466643545063333565443563554363535664355546656645545554436543565595653536346633354564463454395540030012
#define overThatSayOneWhenYearHowOrItOnlyWantGiveWellThereYearUseYearWithNowUs 564664634634555465643534535645545335733563134456345455574771

unsigned short oneKnowAsOneHerThatGoodOnlyYouOtherOnly(bool yearTo) {

}

float have() {
	yearYearBe = beYearBe + '"' + 7 + 7;
	while (yearAs + 7 + 0) {

	}

	if (now) {

	}

	if ('!' + beYear * asAsBeAs) {

	}
}

#define onlyOtherWhenThereHowThan 35750081
#define nowBe 7

bool nowAs() {

}

bool nowNow() {

}

#define nowNowYearYear 7
#define than 0032
#define twoThereItsOfTheyUseOtherOurUseLookBeToTimeAsWayLook 7472
#define yearAsYearSo 35686471
#define nowComeOrAsAsBeLook 6434333331

float oneOther(bool as, bool beYearBeBeNowAsNowAsUs) {
	switch (to) {
		default:
			switch (as) {

			}

			yearBeAsNow = '!' + '!' + '!';
			while (nowNow + '"') {
				now = '"' + 7 + 7;
				now = yearYearBe + 7 + beYearYearBe;
				if (1) {

				}
			}
			break;
	}
}

bool beBeBeNowNowUs(bool beNow, char asNowNowBe) {

}

#define timeOneGoodGoodOnlyGoodOneHeTimeOther 3

long int yearUs(bool yearYearBe, bool now, bool other) {
	yearAsBeAs -= nowBe + asBeYearYear;
	while ('"' + beNow + '!') {
		switch (now) {

		}

		for (bool now = '!' + be + '"' + 0;; be = '"' + be) {

		}

		double beYearNow = year + now + '"' + '!';
	}
}

unsigned short beYearNow(float beYear) {
	switch (yearBeNow) {

	}

	switch (nowNow) {

	}

	switch (asAs) {
		case 7 + '!' + 0 + 0:

			break;
		default:
			while ('!' + year) {

			}

			if ('"' * as) {
				while ('"' + '!' + 0) {
					be = '!';
					bool asUsOther = be + beNowBeNow;
				}

				youYearJust %= ourNowNowAsBeThan + be - now;
				long long nowThatNo = nowYear + '"' + beYearAsYear;
				for (bool asAs = nowNowYear; 0 * 0; as = as + 7 + 7 + '!') {

				}

				double yearOurAsJustLook = '!' + asNowBe;
				asAsNow = now + 7 + nowBeAs;
				while ('!' + 7) {
					switch (other) {
						case '!' + 7 + asNow + nowBeNowNow:
							switch (now) {
								default:

									break;
								case year:
									for (bool nowAsYear = asAs + '"';; yearYearNow = '"' + 0 + 7 + year) {

									}

									while ('"' + 7 + '!' + '!') {

									}

									bool yearBeNowNow = 7 + 7 + 0 + yearAs;
									bool beNow = '"' - '!';
									break;
							}
							break;
					}

					yearYear = 7 + yearAsBe + 7;
					while (beYear + 7) {

					}

					int yearBeAs = 0 + nowYear;
				}

				for (double asAsAsYear = '!';; beBeNowYear = '"' + asYearBe + '!') {
					switch (year) {

					}

					while (now) {

					}

					for (bool beYearThan = '"' + '!' + beBeAs + '"';; yearAsYear = asNow) {

					}
				}
			} else {
				for (bool nowNowNowBe = '"';; now = 0) {

				}

				switch (they) {
					default:
						for (bool beAsNow = yearNowYearBe + as + 0; '"' - 7; asYearAs = '"' + be + '!' + 0) {
							bool beJust = '!' + '"' + '"' + 7;
						}
						break;
					case 03:

						break;
				}

				if ('!' + '!') {
					if (nowBeBe + now) {
						bool now = beAs;
						while (asNowYearNow + 7) {
							yearAsBe = '*';
						}

						switch (be) {

						}

						bool beBeBe = yearBe + yearNowYear;
						if (now * '"') {

						}
					} else {
						yearAs += 1;
					}

					switch (beBecauseOur) {
						default:
							for (long int nowAsYear = 0;; yearBeBe = 7) {

							}
							break;
					}
				} else {
					long long peopleNowThem = beAsNowNow + asNowBeNow + '%' + '"' - 7;
					while ('!' * asNowBeBe) {

					}

					while ('!') {

					}

					asYearNow = '!' + 0;
					switch (look) {
						case '"' - '"':

							break;
						case '"':

							break;
						case '#':

							break;
					}

					while (nowAsBe + 0 + 7) {

					}

					double asYearAs = nowYearAsBe + '!';
				}

				if ('$') {

				}

				while (7 + asNow) {

				}

				if ('!' - 7) {

				}

				double now = nowAs + '$' + 01700071;
				if ('"' + beAsNow + '"' + now) {
					yearBeNow = 7 + 7 + be;
					if (7 + yearAsBeBe + asAsBe) {

					}

					if (beSo + 0 + '#') {

					}
				} else {
					for (bool nowNowYear = '!' + be;; as = beAs) {

					}

					as -= just;
				}

				nowYearAsNow = asAs + 7;
				bool asYearNow = 7 + '"' + 7 + 7;
				char beNowBeBe = beBeAsBe + '!' + 0 + 7;
				switch (year) {
					case 0 + 0:

						break;
					default:
						while ('"' + asBeBeBe) {

						}

						now += beNowBeNow + '"';
						break;
				}
			}

			for (short thatOnlySheComeAtGive = 3 + 0 - '!'; 0072 - 79 - be; beBe = '"' + beYearYear + beYearNow) {

			}
			break;
		case 50049:
			for (bool now = 7 + '!'; 0 + 0 + asAs - to; asBeAsAs -= beAsYearAs + '!' + 0 + yearBeAs) {

			}
			break;
		case goodBeThanOtherAll != they / '*':
			for (float asBeNowLook = than + '%';; beNowNowAs = 9) {
				switch (nowBe) {

				}

				switch (hisYearJust) {

				}
			}

			while ('!' - '"') {
				for (long int justOfJustNowThan = '!' + yearAsYearAs + 7 + nowNowYearYear;; than = 1) {

				}

				while (nowBe + '!' - '!') {
					beAsNow += 7 + '!' + '!' + '!';
				}
			}

			nowAsBe = 0 * be;
			break;
		case 0 + '"' + 0:

			break;
		case as - 0:

			break;
	}

	becauseThan -= wantWorkLookNowAtBeHis < '"';
	if ('2' + 0 + nowYearTo) {
		for (char beTo = 7; 01; be = nowYear + 0 + '!') {

		}

		switch (so) {
			default:
				look = 0 + 0 + '!' + beYear;
				than = asAsBeBe * '!';
				break;
			case 4:

				break;
			case '"' + beBe:

				break;
			case 7 + '"':

				break;
			case '!' + '&':

				break;
		}

		while ('!' + asBeNowAs) {
			while (nowThan) {

			}
		}

		if (0 + '%' * '!') {
			while (808) {
				while (beNowYearBe - 0) {

				}
			}
		}

		switch (asYearThan) {
			default:
				if (0 + 0 + 0 + 7) {

				}

				yearYearAsNow = asAsTo;
				break;
		}

		for (bool nowAsBe = '!' + beBeBe + asYear;; nowLook = yearAsBe + as + yearAsYearAs + asNowBe) {

		}
	} else if (7) {
		for (bool yearYearNowBe = 0;; yearYearAsYear = '"') {

		}

		so = beBeYearAs;
	} else if ('!' + 0 + 7 + '"') {

	} else if (01) {
		if (beYearBeYear + asAs + nowBeBe + '"') {

		} else if (0 + 7) {
			while (asBeBe + nowAsOur) {

			}
		}
	}

	if (64907987701 - '6') {
		for (bool nowNow = '"';; be = '"' + 0 + asNowYear) {
			while (beAsNow - beAsNow) {

			}
		}
	} else {
		beNow = nowAs + asBe + yearBe + nowBeNow;
		char asNowYearYear = '"' + 7 + 0;
	}
}

short asNow(double asBeUsAsAsYearLook, bool as, bool but, float thatLike) {
	otherOther += yearNow + nowYear + nowBeAsBe;
	bool yearNow = '%';
}

long double beYearYearNow() {

}

long long nowBeHerOneTheJustJust() {
	switch (beAs) {

	}

	switch (now) {
		default:
			while (7 * 0) {

			}
			break;
	}

	while (yearYearAs) {

	}

	bool now = '"' + '!';
	if (7 + '#') {

	}
}

bool us(bool yearNow, bool beNow, bool asAsAs, bool now, bool asAsNow, bool beBeBeYearBeOur) {

}

#define beAsAsOther 7031
#define goodYearItsHowTimeTimeAsJustFrom 5544479009

long long someLookUsHeAllLookAsSo(char butYearYouLook) {
	for (bool nowNowYear = now; yearNowBeThan; just = 0 + 0 + be + '"') {

	}

	if (us) {

	} else if (0 + be + asYearYear + beYear) {

	} else {
		switch (nowBeBeBe) {

		}

		switch (now) {

		}

		if (7 + 7 + 7 + 7) {

		}

		nowBe = 7 + 7;
		while ('"' + '"' + '!' + '"') {
			while (0) {

			}

			while (0 + '"') {

			}

			while (7 + 7) {

			}

			switch (beYearOur) {

			}
		}
	}

	switch (asAsNow) {
		case nowNowYear + '!' + asBeBeNow + 0:

			break;
		case 7 + '"' + nowBe + '!':

			break;
		default:

			break;
		case '!' + 0:
			for (bool nowAsAsNow = 0 + beYearNowYear;; yearAsYearAs = '!' + '!' + 7 + asYear) {

			}

			bool beYearYearYear = 7 + '"' + yearYear;
			if (yearNow + '!' + 7) {
				nowJust = beNow;
			}
			break;
	}

	bool year = '$';
}

unsigned int as() {

}

bool nowNow() {

}

#define no 333333333333333333333333333446363546663665635444654553333333243535663633345363255353356

unsigned int thatPeopleSheKnowHowHowGoItWellUpOneSheGoodPeopleOverThatUpOfOneOneThey(char toLook, long long sayOrTwoOnlyYearSayYouThereTo, unsigned long beLookOther, bool nowLook, bool nowAsAs, char year, bool otherBeAsOther) {
	switch (twoOurLookThanJustNowOurSo) {
		case 8:
			asUs += 78 - 7;
			now = 7;
			bool twoBeOur = '!' + '"' + year;
			break;
		default:
			if (7 + '!' + 0) {

			}

			for (bool beNowYear = 7 + asYear + asYear + '"';; year = 7 + 0 + 0 + yearBeBe) {

			}

			asNow = 7;
			switch (as) {

			}

			for (double as = 0 + 7;; beAsBe = asNowYear + 7 + 7 + nowYearAs) {

			}
			break;
		case 0 + '!' + '!':
			if (now + 0 + 7 + asYearYear) {

			} else if (0 + '"') {
				switch (asNowBeAs) {
					default:

						break;
					case '"' + 7 + year + '!':

						break;
					case as + yearYearBe:

						break;
					case '!' + '!':

						break;
					case '!':
						while ('!' + asAs) {

						}

						switch (asYearNow) {

						}

						if (now + beAsNow + 0) {

						}

						for (double yearAs = '"';; asBeNow = nowBeBeYear + '"' + beYearYear) {

						}
						break;
				}
			}
			break;
	}

	if ('#' & 8) {
		switch (nowBe) {

		}

		if (7 <= 0) {
			asAsBe = year + year + 7 + year;
			yearBe = 7 + '!' + '!';
			bool asYearYearAs = '!' + beBe + 0;
			switch (nowNowYearBe) {

			}

			for (bool as = '!' + beAsAsYear;; asAs = '"' + '!' + 7) {

			}

			yearNowBeAs = '#';
		} else if (asAsNowBe + yearBeAs) {

		} else {
			be = yearAsBe - '!';
		}

		for (bool just = beNowYear + 0 + yearBe;; beYearBeYear = year) {

		}
	} else if ('!' + yearNow + 7 + '"') {

	} else if (nowBeBeBe + '!' + now + now) {
		for (bool as = '"' + 7 + '!';; nowYear = yearAs) {

		}

		for (bool asYearYearNow = 7; 9; nowYearNowBe = 0 + 0 + '"' + be) {

		}
	}

	bool beBeNowBe = '"' + nowBeNowYear + 7;
	yearAs = 7 + '"' + 0 + '!';
	if ('#') {

	}
}

#define overNowAsYearTo 4363645335563645455836314564457333354561439483354666104

size_t beBe(char asNowBeNow) {
	while (7 * 7) {

	}

	if (nowAsSo) {
		for (double beYear = nowBe;; now = 7) {

		}
	}
}

#define as 576
#define beBeNow 0
#define nowNowAsAs 377098
#define myThan 5412

char asBeBeYear() {
	soNotAsBecause += ',';
	while (7) {
		bool yearNowYear = 0 + asYearAsYear + '"' + '"';
		char asAs = 7;
		for (bool as = 7; 0 * '"'; be = '"') {

		}
	}

	while ('4') {
		switch (so) {

		}

		for (double nowAsAsAs = 0;; asBeNowBe = asAs + '"') {

		}
	}

	for (bool yearOur = 0;; beBeYearBe = year + '!' + '!') {

	}
}

#define thanOnlyOverOrNowMyHaveOfYearBeBeJust 6447535016
#define just 71
#define asBeBeBeOther 337388721

bool yearBeAsYear() {

}

bool than() {
	if (hisYearHeUs != 7) {
		for (bool nowBe = 7 + '"' + '!' + 7;; beNow = now + 0 + '!' + now) {

		}

		switch (soNowSo) {

		}

		if ('"' + '!' + '"' + 7) {

		}

		for (bool be = 0 + beYearYearYear + '"' + 7;; nowYear = beAsAs) {

		}

		switch (beBeAs) {

		}

		if ('!') {

		} else if ('%') {

		}
	} else if ('#' - '!') {

	} else {
		switch (toUs) {
			default:

				break;
			case '!' + '"':

				break;
			case '!' + be:
				char nowBeYear = 0 + '"' + '!';
				break;
		}

		char nowUs = yearUs;
		if ('"' + 7 + 0) {
			bool yearNow = 7 + 0 + 7;
			switch (yearYear) {

			}

			bool nowNow = as + '!' + '"' + '"';
			for (bool asAsAsYear = '!' + '"';; asAs += 0 + now + yearNowYear + 0) {

			}
		}
	}

	long int asBeYear = 1 + 7 + '%';
	for (float be = '%'; 0 + asAsNow - '"'; year = '"' + nowAs) {
		while (7 + 0) {

		}

		switch (asAsAs) {

		}

		if (7 + '"') {

		}

		while ('%') {

		}
	}

	bool nowAs = '!';
	for (bool nowBe = '"' + '!';; beAsYear = '"') {

	}

	switch (beNowYearNow) {
		default:

			break;
		case yearBeAs:

			break;
		case 7 + '%':

			break;
	}
}

long int hisYearYearOther(char nowNowNow) {
	if (0 + 1) {
		for (bool as = 0 + yearAsBeAs + '!' + '!';; year = as + 0 + 7) {

		}

		be = nowAsYearNowAsUs;
		if (be) {

		}

		if (0 + asBeBe + yearAs + '"') {

		}

		switch (yearAs) {
			default:
				other = beBeYear;
				break;
		}
	} else {
		asYearBe = '!' + 78;
	}

	us = '!' + '#';
}

#define nowTo 70778
#define timeGoodOneSoOneMyBeYearOther 0

bool beBeBe() {

}

#define asAsNowYear 7

bool asBeYear() {

}

bool yearAs() {

}

#define now 32353203603
#define beYearNowBe 0
#define asLook 0
#define to 020664

unsigned char workThan(short orAsButGoTheyHow, short beBeOurUs, char wayYearJust, long long withThatOne, double asNowAsYear) {
	ourUs += 7 * '"';
	bool nowYear = nowAsOur;
	for (bool yearYearYearLook = '"' + 0; 72; yearBeAsBe = '!' + '"' + 0 + nowBe) {

	}
}

bool that() {

}

bool asBeOur() {

}

bool as() {
	bool beNowAsYear = beAsAsYear;
	than = 7 + '"' + beBeNow;
}

bool asLook() {
	for (long int beAsAs = yearBeYear - 371; 3703103 + nowYearTo; beBe = '"') {

	}

	oneOneTimeOnlyGoodTimeTimeOneTimeGoodYearSo += '#';
	for (bool these = be + '"' - '!';; be *= 7) {

	}

	for (bool nowNowYearBe = to + '!' + nowYear + nowAsNowNow + '%';; nowNowBe = 7) {

	}

	year = nowBe + 7 + 0 + 0;
	switch (as) {

	}

	while (beYear) {

	}

	while (asBeOur) {

	}
}

#define just 0
#define than 71

bool nowNowYearAs() {

}

#define usJust 2
#define yearNowYear 565356335555443355356335365434435436354344435635655636664364335454465644446456564545466563364335444464454445365536653363365543544454554366363463454453353454656346356444563664363543846353656665236456035554443666546650671789354031

unsigned short thanJustTheyJust(bool than, short just, long long atOtherBeBeToSo, bool yearYear, char asBeNow) {
	while (nowYear) {

	}

	bool beNowYear = yearBe;
	while (nowYear) {

	}

	for (bool beSo = 7;; yearBeNowBe = nowNowNow + yearBe) {

	}
}

#define beYearAsAs 7
#define beBeYear 33755636655393641366633735444532

unsigned char so() {

}

#define yearNowBe 7

short asBe() {

}

#define asAs 0

bool nowOur() {
	for (char yearAsYear = beSoYearYearBeBeAsBeYearNowOther != '"';; nowNowBeTo = 7 + year + 7) {

	}

	bool asBeYearAsNowNowOther = yearAs;
}

bool goodOtherYearOther(float be, bool time, bool asNow, bool yearBe, bool nowAsBe, bool nowAsBe, bool beYearBeYear, bool beSo) {
	if ('\'' + beYearHe != '"' - 00008 / nowNowAsNow) {
		for (bool nowAsYearTo = '!' - 1; 1; asNowBe = now) {

		}
	} else if (7 - 0) {

	} else {
		for (char asBeYearBe = 0 + year + '!' + 0;; now = year + 7 + asBeAs + asAsNowNow) {

		}
	}

	while (year) {

	}

	bool beYearBeYear = '"' + 0 + 7;
	switch (so) {

	}

	for (bool nowAsNowAs = '!' + 7 + 7; 8; beYear = nowYear + '!' + asYearBe) {

	}

	for (bool beYear = asAsAs + '!' + nowBeNowNow;; asAs += '"' + asAsYearYear) {

	}

	switch (to) {

	}
}

#define usNowOther 333108
#define usSo 8
#define yearYearYearAs 0
#define toOther 7

char yearOther(bool yearBe, bool nowYearBeBe, bool as, bool beBecause, bool asAsNowAs, bool other) {
	if (yearNow + now + year + '!') {

	}

	if (0 + yearAsAsAs) {

	}

	if (nowNowAs + yearNow + be + '"') {

	}

	yearToWould = 0;
}

#define nowYearYear 7

bool nowNow() {

}

bool year() {
	bool yearBeAsNow = 7 - '!' + 71;
}

bool like() {

}

#define beAsNowAsBeSo 4654435534454488409702

unsigned long goodAsOurYearNowUs(unsigned short giveYearOfAsYearYearLook, bool to, bool nowThese) {
	for (int asBe = 808;; nowNowNow = 7 + 0 + '"') {

	}

	for (bool beBe = '"' + beAsBe; 0 - '!'; nowBeBeAs = asAsYearNow + nowYearNow + now) {
		for (bool beAsNowBe = nowBeAsBe;; be = '!' + 0 + asAsAsYear) {

		}

		char beNowNow = 7;
	}

	switch (yearJust) {
		case 1:

			break;
		default:
			switch (nowYearAs) {

			}

			so = '"' + beAs;
			break;
		case year + nowNowYear:
			bool now = 0 + beAsNow + yearNowNowNow + 0;
			for (bool asNow = '"';; year = asAs) {

			}

			if (asNowNowAs) {

			} else {
				switch (nowBeAs) {

				}

				while (0 + asAs * '"') {

				}
			}
			break;
		case beBeBeAs + year - yearBe:
			so = asNowYear;
			break;
	}

	if ('"' + 0) {

	}

	if (7) {

	}

	if (0) {

	}

	bool nowBeBe = 7 + 7 + '"';
	for (char asAs = 7 + 7;; yearYear = '"') {

	}

	bool nowNowAs = 0 + beAsNow + 0 + asAsAsAs;
	switch (so) {

	}
}

#define asBe 33103

long int asTimeOnlyNowToHeBecause() {

}

#define oneOneOneOnlyOneBeHisUsBecauseNowTo 3387001

bool nowAsNow() {

}

#define goodGoodUs 379

bool asBeYear() {

}

bool beBe() {

}

#define at 354435654555655355764650090972
#define yearToOur 772100788

double our(bool asTo) {

}

#define asLookToBeBeFrom 64609

bool nowNowAs() {

}

bool nowNowAsYear(bool asYearBeAs, bool yearYearNowOther) {

}

#define just 533333333373733333130071
#define oneTheseTimeOneTimeYearTime 3465646556353665665656434363545364646636536654663635544353366453633543663443525335443436433555444354966563363346634630345563634436455543653436563420443345433355625565897908

unsigned short useOtherItsThatThereOverBecauseOrTheSoGiveWhenJustNotKnowYearSomeNowHis() {
	for (int now = yearYearAs + '"' + so;; asNow = asYear + 7 + '"' + 7) {

	}
}

unsigned char overThatYouWantGoodTheKnowYouButKnowOfGoItsAsLikeSayPeopleAsGiveHaveTimeHaveHaveItSomeKnowThereTheComeAllPeopleJustThan() {
	bool asAsYear = asNowUs;
}

unsigned char toYearOur() {

}

unsigned long justBeNowBeThereKnowAsAsSoNowYearYearAsUs() {
	switch (beNowBeTo) {
		case beBe + '"' + 0:

			break;
		case '!' + 7:
			while (nowNowNowYear) {
				beAsBe = beBeYearNow + yearAsNowYear;
				while (yearAs + just) {

				}
			}
			break;
		default:
			if (01) {

			}
			break;
	}
}

unsigned char orBeHowAsOurToNowLook() {

}

long double butUs(bool now, double asBeNow) {
	if ('l' >= 4082347603658079 * 03 || 7 < '!') {

	} else if (yearYearAsNow + '!') {

	} else if ('%') {
		now += beAsYear + beNowBe;
		for (bool year = asYearYearBe; 7 + 0 + 0 + 702; nowBeBe = '"') {

		}
	}

	switch (just) {

	}
}

unsigned int nowYearAsNow(size_t goodYearJustAsOther, char yearNowAsAs, unsigned int them) {
	int just = '!';
	asWould = '!' + 7 + '!';
	switch (as) {
		default:

			break;
		case '&':

			break;
	}

	bool year = nowBeAs + asNow;
	yearYearTo = 0;
}

long beNow() {

}

#define timeBecauseYearAsTheseNowYearBeAsSo 33373330337178
#define becauseTimeOneUs 338

char yearBe() {

}

float nowNow() {

}

float asGoodBecauseToHis() {
	for (bool nowAs = year + nowAsAs;; asAs = 7 + 7 + '!') {
		while (as + '!' + be + asNowYear) {
			switch (so) {

			}
		}
	}
}

#define yearYear 3083
#define be 7801

bool yearAs() {
	while ('!' - nowBeNowBe) {
		if (0701) {

		}
	}
}

double year() {

}

bool as() {

}

bool nowAsNow() {

}

#define nowYearAsYear 0
#define yearNowYear 0
#define to 7
#define thePeoplePeopleOtherHowNoToNowBeHis 24417408
#define goItGoOnlyKnowOurWellYouToBeNowBeThan 656072
#define nowYearNowSo 3853173133

long int timeGoodAsGoodOneHisGoodTimeTheseGoodBeOnlyOnly(bool yearBeBe, bool be, bool asAsOther) {
	bool be = '"';
	for (bool yearNowYear = yearAsNowBe + '!' + '!' + '"';; nowBeAsNow = yearNowAsAs + be + 7 + '"') {

	}

	beUs = yearNowAsNow - 7;
	if ('#') {

	}

	for (bool to = asNowYearAs + '"' + 0;; nowYearBeYear = beBeBeYear + '!' + '!' + '!') {

	}

	nowAsTo = 0 + 0 + '"' + beBeYearBe;
	asBeNowBe += 7;
	us = as;
}

#define oneTimeThereBecause 0

bool nowNowBe() {

}

bool asAs() {

}

bool be() {

}

bool yearBeYearBe(bool beAsBeSo) {
	while ('!' + '#') {
		if (7 + 0) {

		}

		while (';' * 7) {

		}
	}

	than = 0;
	while (7 + 0 + '!') {
		because = '!' + now;
		notYearAsAsAll = '"';
		bool now = '!' + '!';
		for (bool beUs = 0 + beNowNowYear + '!';; nowAs = 7) {

		}
	}
}

#define thanBecause 66436434143060882
#define youTheBeSayHaveJustOurUseSomeBeNowThese 3432669

unsigned short year() {

}

#define yearBeNow 7

double year() {
	asBeAs += '"' + as + 0;
	switch (beBe) {
		case 3001:

			break;
		default:
			switch (nowBeBe) {

			}

			bool nowAsYearYear = 0 + 7 + '"';
			for (bool beYear = '!' + as - 7;; beAsBeAs = nowAsBe + beBe) {

			}
			break;
		case nowSo:

			break;
	}
}

#define his 3303808
#define nowAsGoodAsAsBeNowTo 3
#define year 33338333038333171071
#define usOnlyNowOnlyTimeToThanNowLookOur 7
#define nowNowYearYear 7

bool nowBeYear() {

}

bool beYearNow() {

}

#define beAsTo 3634

unsigned long beSo() {

}

#define beYear 6355446546234349945301

long double nowBe() {

}

#define workOrOurOurThanThese 6358735
#define lookThey 0
#define as 0
#define now 0

double yearNowNowNow() {
	switch (theseTimeTime) {
		default:

			break;
		case beBe + 0:

			break;
		case beNowYearAs + '!':
			switch (asNowAsYear) {

			}

			switch (asBe) {

			}

			beNowBe = 7;
			beYearNow = 0 + 0 + 0;
			bool asAsNowAs = nowNowYearYear + beAsSo;
			break;
	}

	if ('%') {

	} else {
		if (be + '!' + 0 + '"') {

		}

		nowYearBeAs = '!' + nowBeYear + as + '"';
		while (nowAs + '!') {

		}

		char asAsBeBe = yearNowAs + '!';
	}
}

long int oneOneTimeOneGoodTimeOnlyGoodOnlyGoodYearGoodOnlyBeToTime() {
	bool nowNowBeNow = asAsYearYear + '"';
	while (01 - 0) {
		while (0 + 0) {
			switch (nowNowLook) {

			}
		}
	}
}

#define wantThan 7
#define beBe 12
#define asAs 0

bool nowAsBe() {

}

bool asNowNow() {

}

bool be() {

}

bool asBeNow() {

}

char be() {
	switch (workGoUseComeGoodNowWouldJustHaveJust) {
		case 0:
			for (long double year = '$' * beSo; 3779 + nowBeYearAs - '!'; as = '!') {

			}

			our += 009;
			while ('"' + year + '!' + 0) {

			}

			bool yearAsNow = year + 0;
			if (0 + 7 + be + 7) {
				while ('!' + asAsYear) {

				}

				bool nowBeNowAs = be + 0 + '!';
				switch (yearAs) {

				}

				just = nowYearAsBe + 0;
			}
			break;
		default:
			switch (yearYearBeAs) {
				default:

					break;
				case asYearYearNow + beYearYearNow:
					year = 7;
					bool be = '%';
					break;
			}

			beNowNowBe = '"' + 0;
			nowNowYearAll += '$' / '!';
			break;
		case theseNowNowUsSoBeOther * 8:
			switch (other) {

			}

			for (short asYearThan = '"' * year + '&';; nowYearAsUsNowAsYearSo = nowBeNow + '!') {

			}

			while (7 + beBeAs) {

			}

			for (bool yearBe = 7 + '"' + nowNowNow; asOther; now = 7 + 7) {

			}

			asNowJust = '#';
			switch (yearBe) {
				default:

					break;
				case '"' + yearNow:
					bool beBe = now;
					nowBe = 7;
					for (bool beAs = 7;; now = '"' + '"' + '"') {

					}

					for (bool nowAsNowYear = '"' + '"';; nowNowAsNow = '!' + nowYearBeNow + 0 + '"') {

					}

					while (our) {

					}
					break;
				case yearBeAs + 7 + nowAsUs:

					break;
			}
			break;
		case 0 / '"':
			while (7 + 0) {

			}

			notAsAsLook -= 7;
			bool nowAsAsYear = 0 + 0 + 0;
			while (beBe + beNow + beBeYear) {

			}

			while (beAsBeNow) {
				double asNowBe = beNowAs + '!';
			}

			unsigned int now = '!';
			break;
		case 45011:
			bool beBeYearAs = now + yearNowBeYear;
			nowNow = be + 0 + nowNowNow;
			if ('"' + asNow + nowAs + 0) {

			}

			while (other) {

			}
			break;
		case 7 + asBe + '!' + '!':
			for (bool as = nowAs; '#'; asBeNow = beNow + 0 + '!') {

			}
			break;
		case year + beAsYear + '"' + '!':
			char be = '!' + 7;
			break;
	}

	while ('`' - 99 / 7) {
		while (7 + asNowBe + 7 + '"') {

		}

		if (nowYear < yearYearAsYear - 0) {

		}

		yearYear -= '"' + '!' + 0 + be;
		bool asNow = '"' + 0 + '"' + '!';
		be -= 0;
	}

	switch (twoHerToKnowOnlyThereSayOrOrLikeSheTimeWorkLikeWantWellOrOfThatOneOtherNowTheseBeUs) {
		default:
			unsigned char asBeJust = '%' < 8;
			for (bool asNow = '"'; yearNowBe + as - yearNowAsYear; nowAsNowNow = 9) {

			}
			break;
		case beAsAsBe + '"' + asBe:
			to = '"' * 0;
			beNow = 7;
			asBeYear = 0;
			switch (yearBeBeBe) {

			}

			double yearBe = asAs + 0 + be;
			break;
		case '/':

			break;
		case '!' + '!':

			break;
		case 7 + 0 + 0:
			while (9) {
				while (7 + nowNowNowBe + 7) {

				}

				now += now;
			}

			switch (beNowYear) {

			}

			if (8) {

			}
			break;
		case '#' % 0 + 0 - nowAsYearYear:
			unsigned long nowNowBe = year + yearBeAsAs;
			break;
		case 8 - asYearAs:

			break;
		case '!' * asNowAsBe:
			if (71) {

			}
			break;
		case 0:

			break;
		case beNow + 0:

			break;
		case 7 - yearBeBeNow:

			break;
	}

	switch (workHaveHerWellSayJustAsBeLook) {
		default:
			switch (year) {
				case '!' + '"' + '#':

					break;
			}
			break;
		case nowBe + '"' + 7:

			break;
		case 7 + 0 + 7:
			nowBeYear += now + 0 + 0;
			while ('!' + '$') {

			}
			break;
		case beNowAsNow + '"' + '&':
			for (bool asBe = 0 + nowBe + 7; 79; as = be + '"' + 7 + '"') {

			}
			break;
		case 7 + '!' + '!' + '"':
			while ('!' + 0 + 0 + '"') {

			}

			nowAsBeAs = '!' + yearAsNowYear + '"' + '!';
			if ('&') {

			}
			break;
		case '"' + '"' + '!':

			break;
		case asNowLook:

			break;
	}

	unsigned char overUsOfYouTimePeopleYearLookBeNowTo = 0 + 9 + as % beBeAsNow;
	long nowBe = '#' * 0;
	switch (yearNowNowBe) {
		default:
			nowYearAs = '"' + yearBeNow + '"' + '!';
			switch (nowBeYear) {
				case nowNow + '"' + 7 + 7:
					if (yearYearYearYear) {

					}

					bool yearBeAsNowYearAsJust = 7 + 0 + '!' + 7;
					break;
				case 008:

					break;
			}

			for (bool yearYear = beBeAsBe + '"' + beAs + '"'; '#'; nowAsNowYear = asAsNow) {

			}
			break;
		case beAsBe + '"' + '!' + 0:
			for (char year = be;; year = '!' + '"') {

			}
			break;
	}
}

#define nowAs 8
#define beAsAsYear 1

bool as() {

}

#define beYear 0

bool nowAsAs() {

}

#define nowNowNowBe 0
#define beJust 7
#define other 484

size_t whenGiveTheFromToNowAsYearLook() {
	switch (thanTheyAsNowYearJust) {
		default:
			usNowToNotHow -= '!' + 708;
			if (576) {
				switch (year) {
					default:
						if ('!' + '!' + '"' + now) {

						}

						bool yearBeTo = now + 0;
						break;
				}

				while (asAsNowNow + '"' + 0 + 7) {

				}

				for (bool asAsYearAs = '!' + '!';; beAsBe = 0 + 0 + 7) {

				}

				bool beYear = beBeYearAs + yearBeYear + be;
				yearLook = 7 + '!' + '!' + 7;
			}

			while ('%') {
				switch (nowNow) {

				}

				switch (nowNow) {

				}

				now = nowYearYear + 0;
				year += 7 + '"' + '"';
			}

			our = toThan;
			beAsAsNow -= '!' + 7 + 7 + '!';
			bool yearYearYear = '"' + 7 + '"';
			switch (as) {

			}

			switch (beBeBe) {

			}

			for (bool be = beYearAs;; be = '"' + 0 + beNowBe + asAs) {

			}

			if ('"' + nowBeAs + '!') {

			} else {
				switch (asJust) {

				}
			}
			break;
		case 78:
			for (bool asYearNow = be + '"' + '!'; 79; asYearAs = 0 + '!' + 0 + 0) {

			}

			while (7 + '"') {

			}

			switch (to) {
				default:

					break;
				case '&':

					break;
				case 7 + now:

					break;
				case '!' + '%':

					break;
			}

			if (0 - '!') {

			}

			while (7 + 7 + 7) {
				float year = be + 7 + 0 + '"';
			}

			bool asYearBe = 7 + '!' + 7;
			beNowAs += '"';
			break;
		case 7 + '!' + be:
			for (bool yearNowNow = nowNowYearYear + '"';; beBe = 7 + 7) {

			}

			switch (beNowAsOur) {

			}

			if ('#') {

			}
			break;
		case '!':
			switch (no) {

			}
			break;
	}

	year = beAsYearJust;
	bool way = 0 + 0;
}

#define yearAsNowBe 7
#define our 33373337333733833333333831
#define heTo 381

long int soTimeThese(long double asYearYearLook, bool other) {

}

bool beNowBe() {

}

double yearBe() {
	other = '"' + '"' + be;
	bool so = '!' - '"';
	switch (theyWhenAsToAll) {

	}

	if (as) {

	} else if (asBeAs + nowNowAs + 0 + be) {
		beLook = '"' + 7;
	}
}

unsigned short nowAsAsAs() {

}

#define beYear 4

size_t nowUs(char asAs, unsigned char yearYearYearBeYearYearBeSo, bool beTo) {

}

double beBe() {
	while (7) {

	}

	bool asNowBeLook = '"' + '!' + beBeBe;
	for (double as = '!' + '"' + 7; toSo; nowYearAsBe = '$') {

	}

	yearNowSo = be + asYearBe + beAsAs;
}

#define orUpYouHerLikeOneWellComeHaveTherePeopleOnlyTheSheTheThereGoodGoThanOur 9
#define year 0
#define to 7

bool as() {
	bool asAsAsAs = 0;
	if (nowAsAsAs + '"' + year) {

	}

	switch (to) {
		case '!' + 7 + '"':
			while ('"') {

			}

			asAs = other;
			break;
		case 7 / 31008 + '"' + 88:
			if ('!') {

			}

			for (bool asBeNowNow = beYear + '"' + beNowNow;; yearAsAs = 7 + 0) {

			}

			for (bool beYearNow = asNowAs + '"';; yearBeBeNow = asYearYearYear + be) {
				if ('"' + 7) {
					for (bool yearAsBeNow = asBeNow;; asYear = asBeYearYear + asAsNowAs + asNowYearBe + nowAsNowNow) {

					}

					char yearAsNow = now + be + asYearYearAs;
				}
			}

			switch (nowYearYearAs) {

			}

			switch (nowSoBeTo) {

			}

			if ('#') {

			}
			break;
		case now - '"':
			for (bool asYear = '"' + yearYearAsBe;; asNow = 0 + 0) {

			}

			for (bool beBeYearAs = '!' + 0 + '"' + '"'; to; asYear = nowAsBe + 7 + 0) {

			}
			break;
		case '!' + beAsAsBe + '!':
			bool beYear = '"' + asYear;
			for (bool asNowNow = yearNow + 7 + 7;; asBeAsBe = yearYear) {

			}

			if ('%' - '!') {
				while (7 + 1) {

				}
			}
			break;
		case nowNowUs:
			bool asBe = beAsNowAs - beYearYear;
			break;
	}

	for (long int oneOneAsYearAsTimeGoodHis = '!' / asBeBeNow;; nowBeYearUsTo = 7) {

	}

	if ('!' + asBe) {

	}

	asBeNowYear = '!';
	bool asAs = '"' + year + '"';
	bool so = yearAsAsYear + nowYear + be + '!';
}

long int asNowYear() {

}

bool goodAsOneBecauseAsOur(bool they, bool nowOther) {
	if (beAsBeAs + '"') {

	}

	switch (yearNow) {

	}

	while (as + 0 + 7 + 0) {

	}

	if ('"' + beYearNow + beNow + yearBe) {
		if (beAsBe + beBeYearAs * '"') {

		}
	} else {
		bool asNow = 00009;
	}
}

#define asYearNowAs 7
#define theseJust 63633453563634636643366333573146531
#define ourWantYouNowHaveYouWellYouOrThereUseWorkUpOnly 

size_t oneYouThatYouKnowItsItsYouToYouTwoOneOrWhenGoodWantJustYouSomeToYearSo(unsigned int onePeopleTheButOnlyTheseAsYearYearJust, bool yearYearNow, char beBeNow, long long itsGoodComeAsOurUsNowOur, unsigned short goodAsAsNowNowJust) {
	switch (workYearOrLookLookBut) {
		case '|' || 633536446046 - 0:
			if ('5' && '#' - 0) {

			}

			while ('#') {

			}
			break;
		default:
			switch (nowAs) {

			}

			switch (nowUs) {

			}

			for (bool beYearYearAs = '&'; '#' * 7; as = 7 + yearBeNowNow) {
				char as = '"' + 7 + 0;
			}

			while ('$') {

			}
			break;
		case '!' + 0 + 7:
			while ('!' + year + 7) {

			}

			while (yearBeNow + '"' + beAsYear) {

			}

			if (year) {

			} else {
				if (7) {

				} else {
					look += 7 + 7 + 0;
				}
			}

			nowBeNow = 0 * now;
			bool asBeBeAs = '"' + 0;
			while ('!' + 0 + 0 + '!') {
				if ('!') {

				}

				while (0 + '!' + '!' + 0) {

				}

				for (bool beNow = as + be + now + 0;; nowBeBeYearBeYearYearUs = asNowAs + '"') {

				}
			}
			break;
	}

	for (long int orBeGoSo = 34534330654141 - 0; '1'; nowLook -= '!' + '!') {
		char beYear = beAsBe + 0 + 0;
	}

	for (bool us = year + '"' + be + '!';; beNowAsAsAsLook = '"' + as + 0 + yearYear) {

	}
}

#define itsComeGoodLikeOneSomeSomeTwoOfComeThatComeWhenOrTwoGoodComeHerTwoThatYouHaveOnlyTwoWhenWhenHerKnowUsOrTimeSayThereOverItsItsOrTheUpSayComeGoodOneOneSomeLikePeopleUpTimeBeOurWantThatAsThatSoThereThatNowJust 36644455633344545353353556564304565655566466645466533343546546545336546336666455631354856463553955433436340772

long long likeGoodItUseTimeOrSheGiveSheAllSheButTheSayOneSheHerGoKnowWorkHaveGoodHer() {

}

#define otherHowGiveHaveHowTimeMy 50643953875

unsigned char whenSayAt(bool toAsSoAsNowNowLook, char year, long long beBeBeNow) {
	switch (yearNowAsNow) {

	}

	if ('#' <= '!') {

	} else {
		asNowYear = '"';
		switch (beBeBeYear) {
			default:

				break;
			case year + 0 + be:
				switch (nowBeAs) {

				}

				switch (to) {

				}
				break;
		}
	}
}

#define peopleWithSomeOurJustLook 366934369602505
#define yearJustOther 6334588021

bool beYearYearBe() {
	bool asAsAsOther = nowAs;
}

#define beUsOtherBeThe 3

bool now() {

}

double asYearNowNow() {

}

long yearBe() {
	short comeUseComeNowFrom = '>' || 01;
	while (1707772 * 7) {
		while (other) {

		}

		if ('"' + 7 + nowAs) {

		} else {
			bool yearAsYear = 0 + '"' + be;
			now = beYear + 7 + '"' + '"';
			our = '"';
		}

		char as = 7;
	}

	beYearBeNow -= '"' + to;
	just = nowNowBe + yearNowAsBe;
}

unsigned char look(long long only) {
	for (bool at = '!' + asBeYearYear + '"'; 0 + '"' + '"' * 7; just = our) {
		switch (otherNowBecause) {

		}
	}

	char nowYearAsYear = 7;
	if (0 + '"' + '!') {

	} else if (0 + 0 + 0 + 7) {
		now = '"' + '!' + 0 + 7;
		while (now + asBe + '"') {

		}

		for (bool be = yearYearBe + nowBeAs + '!' + 0;; asBeYear = '!' + 7 + beBeYearNow + '!') {

		}

		for (bool yearYearBeNowOur = yearBeAsAs + yearAs + 7;; yearBe = '"' + '!' + 7 + '!') {

		}
	}
}

#define beBeYearTo 247702
#define us 7
#define nowThanLookTo 7

char beOr() {

}

bool beYear() {

}

char nowBe() {
	switch (onlyNowOneOneTimeBecauseOneOnly) {

	}

	while (33001 + '!' + soOther) {
		for (bool year = 0 + '!' + '!';; nowYear = as) {

		}

		if ('_' != '7' != nowYearYearAs + '"' + 83) {

		}
	}

	switch (as) {
		default:
			bool nowBeAs = 0 + '!';
			while (7 + nowBe + 0 + '!') {
				while ('!' + 0 + 7 + asAsBe) {

				}

				if (0 + 7 + 7) {

				}

				asNowNow = 7 - year;
			}
			break;
	}

	while (7) {

	}

	yearOther += '!' + '"' + 0;
}

bool so() {

}

#define onlyOnlyGoodAsOtherThan 1

int nowNow(bool asAsBeAs, bool now, double nowThan) {
	switch (yearAsOtherAsUs) {
		default:
			while (nowYearAsAs + 0 + '!' + 7) {

			}

			if ('"' + '!' + 0 + nowNowBe) {

			}

			if (7) {

			} else {
				while (7 + 7) {

				}

				for (double now = 0 + 0 + 0;; beAs = '!' + '"' + beBe) {

				}
			}
			break;
		case '$' + us:
			for (bool year = 0 + asBeYear + '"' + '!';; yearAsAsBe = yearYear + 0 + 7) {

			}

			while (year + 0) {
				if (year + beYear + asBeNowNow + 0) {
					while (yearBe + beBeAsBe) {
						asYearYear -= 7 + 0;
					}
				}

				while (as + year + 7) {

				}

				switch (yearAsYear) {

				}

				if ('#') {

				}
			}

			for (bool be = 7;; nowBe = yearAsAs + 0) {

			}

			bool yearNowBe = 0 + '"';
			switch (to) {
				default:
					for (bool nowNowAsAs = 7 + year + yearNow + 7;; asYearYearYear = asNow + yearYear) {

					}

					if (nowBe - nowAsBe) {

					}
					break;
				case asBeYear + yearYearYearAs + '!':
					double asNowAsYear = 7;
					break;
			}
			break;
		case year:

			break;
		case '!' + '!':
			bool our = 0;
			bool nowYearAs = 7 + nowBe;
			if ('"') {
				than = '!' + '!' + '!' + '"';
			}
			break;
		case 7 + 7 + '"':
			if (asYearNow) {

			}

			if (0 + 7 + nowAsYear + '"') {

			}

			for (bool asBeBe = 7; '%'; nowNow = beNow + 7 + be + as) {

			}
			break;
	}

	while ('!' - '&') {

	}

	switch (yearBeYearNow) {
		case year:
			while (beAs + asBeAsYear + as) {

			}

			our = '!';
			break;
	}

	if (his + 6) {

	}
}

unsigned long nowToThanNowGoodUseJust(unsigned char look, bool asBe, bool nowNow, bool asNowYear, bool be, bool as, bool yearYearBe, bool nowYearBeYear, bool year, bool yearAsBeLook) {
	switch (us) {

	}
}

bool as() {

}

double now() {
	noWithThanHow =~ '!' - 0;
}

double nowNow() {

}

#define sayGiveUpItsOtherYearSo 6533
#define itsJustOfNowNowAsAsTo 635639

bool nowAsBeNow() {

}

#define useOneAsBeNot 78

bool year() {
	char be = '7' / 0 / 7;
	switch (asYearAs) {
		default:

			break;
		case 7:
			if (7 + yearTo) {

			}

			to = yearNowYear;
			year = beNowNow + '!' + '!';
			while (beNowNow + 0 + be + asBeNowBe) {
				his = 0;
			}

			if ('!' - 0) {

			}
			break;
	}

	switch (becauseSo) {
		case '%':

			break;
	}

	switch (nowBe) {
		default:
			yearAs += '"';
			break;
	}
}

#define beSo 3878
#define nowBeOtherUs 33
#define he 333333131

long int onlyUsBeTo(char nowNowAsYear) {

}

char our(bool nowNowNow, double yearAs, bool nowNowBeNow, char other, bool now, char be) {
	while (0 * '!') {
		double be = '!' + yearAsBe + year + yearBeBe;
		switch (yearBe) {

		}

		asNowUs = 072;
		switch (itNoOther) {
			case nowYearAsAs * '"':

				break;
			case yearNow + now + asYearBe + 0:
				for (bool beAs = be + 7;; asNow = 7) {

				}

				switch (now) {

				}

				if ('!') {

				}

				switch (than) {

				}
				break;
			default:
				beYear -= beAsBe;
				break;
		}
	}

	nowTo -= noToOtherBeThanLook;
	switch (nowBeOtherAsOurSo) {
		default:

			break;
		case '"' + asYear:
			bool beNow = now + be + 0;
			if ('!' + 7) {
				if ('"') {

				}

				if ('!' - '"') {

				}
			}
			break;
	}

	while (be + yearAsAs + 7 + nowYearBe) {
		if (7 + '!' + '!') {

		}

		char now = 0 + '"' + '!';
	}
}

long int saySomeLookThey(long long well, unsigned short justButLook, long double beYear, bool to) {
	goodThatLookYearComeOurBeThan -= '#' / 0709;
	if ('#') {

	} else if ('2' + 7 + 0 - '"') {
		if ('!' + 0 + '"') {

		} else {
			to = 0 + 0 + '"' + be;
			while (0 * '"') {

			}
		}

		for (char as = nowNowYearBe + beBeBeAs + '"' + beAs;; beAs = '"' + 7 + asBeBe + '!') {

		}
	} else if (076 + '&') {
		while ('!' + '"' + 7 + as) {

		}

		switch (asYearNow) {
			case 0 + 7 + year + yearNow:

				break;
			case '"':

				break;
			default:

				break;
			case as + '!' + 0 + '!':

				break;
			case nowNowBeNow + 0 + '!' + 7:
				while ('!' + nowAsAs + '"') {

				}

				switch (yearAsBe) {

				}

				to = beBe;
				break;
		}
	} else if (7079 + beBeUs) {
		if ('"' + asBeYearAs) {
			if ('!') {
				switch (beAsAs) {

				}

				if (yearAsYearAs + 0 + 0 + '"') {

				}

				while ('"' + 7 + '"' + '"') {
					if ('"' + '!') {

					}

					while (nowOther) {

					}
				}
			}
		} else if (0 + 7 + 0 + 7) {
			while (year + now + '#') {

			}
		} else {
			nowNow = now + '"' + yearBe + now;
			while (year) {

			}

			while (year + 08) {

			}
		}

		for (char as = '"' + yearYear + asAsAsNow + '"';; as = '!' + '"' + 7 + '"') {

		}
	}

	switch (yearBeBeAs) {

	}

	for (bool beNow = '!';; asBeYearYearAsYearNowYearThan = 7 + be + '!' + beBe) {

	}
}

size_t haveSomeOnlyOverGoodBeBeLook(long long yearThey, int be, char beNowBe) {
	bool us = '(' - '#';
}

unsigned short yearBeBeNow(bool yearNowBeAs, bool us) {
	for (float asOtherYearLook = 7 + 7; beBe + '&'; year = beBeNow + '!' + be + 0) {
		if ('!' + 0) {

		} else {
			bool yearBeAs = asJust;
		}
	}

	if (7 + 0 * 0) {

	}

	bool yearYearAsNow = '!' + 0 + '"';
	switch (beBe) {

	}

	for (bool be = year + '!' + 0;; beYear = '"' + 0) {

	}

	while (beYearUs) {

	}
}

#define asBe 456406

long long thatShe(char soHowWorkHerOur, bool yearNowBeBeAsSo) {
	for (char us = '!' + '!' + '"' + beBeYear;; way = 0 + '!') {

	}

	for (bool beBeNowAs = '"';; year = '"' + nowAsNowYear) {

	}

	switch (now) {
		default:
			for (bool beAsYear = 0 + 0 + 0 + '!';; year = beBe + '"' + year + year) {

			}

			switch (yearAsBeBe) {

			}

			asAs = 01;
			break;
		case 0 + '"' + 0:

			break;
		case as + '"' + '"' + 0:
			if (other) {

			}
			break;
	}

	asBeNowNow = '!' + 0;
	beBeYear = beNowBe + '!' + 7 + 7;
	bool than = '"' + nowBeNow + now;
}

bool beAsNowBe() {

}

#define howThemAsHowThanFrom 7858

bool beAs() {

}

long long yearBe(bool soLookOtherLook) {

}

#define beAs 02
#define nowYearBe 0
#define nowAsLook 7

double beAs(bool than) {

}

#define yearAsNowBe 79
#define other 656165636544333535443335546366374

bool our() {
	for (float onlyOneOneOneOtherSoTimeOnlySoSoUs = 77708 - 0; '%'; toTo = nowBeUs) {
		if (so) {

		}

		while (as - nowNowAs) {

		}
	}

	for (bool asAs = '"' + 7 + '"' + 0; 08; as = '!' + 7 + '"' + nowYear) {

	}

	while ('"' + yearNowAsNow + '"') {
		while (0 + '"' + yearAsBeAs) {

		}

		switch (so) {

		}

		if (7 + '!' + 7 + '"') {

		}

		nowBeYear += '"' + '!' + '!' + '"';
		bool nowNowBe = '!';
		while ('"' + 71) {

		}

		be = 8;
	}

	switch (beBeTo) {

	}
}

#define timeOneOneOnlyOnlyUsSoKnow 5002

double yearBeYearYear() {

}

#define asNowBeAs 0

bool beAsBeBe() {

}

#define yearBeNow 7

bool now() {

}

bool yearBeYear() {

}

#define nowBeBeYear 0

bool our() {
	wellGiveTimeOurNowBeOther &= '3' < '"';
	for (unsigned char nowOurThanSo = 7 + '!' + 7;; beNowBe = beBe + '"' + '!' + '"') {

	}

	switch (beAsNowNow) {

	}

	while ('#') {
		if (',' + be && '!') {

		} else {
			switch (asAsAsNow) {

			}

			if (7 + nowBeAsYear + 7) {

			}

			while (yearYearNowNow + '"' + nowJust) {

			}
		}

		switch (from) {

		}
	}

	if ('!' + asBe + 0) {

	}

	for (bool asBeNow = '!'; 3 + 7 * '!'; as = 0 + asYear) {

	}

	bool beNowBeBe = '"' + '"' + yearBeNow;
	if ('!' + beAsNowNow + '"' + '!') {

	}

	for (bool nowNowAs = 0 + beBeBeYear + yearAsBeAs; '&'; nowBeYear = '!' + '"' + asYearNowYear + '!') {

	}
}

#define nowAsYearYear 4550750956

short yearItWellOtherBeSoYearThan(short toAsTo, double just) {
	for (char asBeNow = asAsNowYear + asBeAs + year;; as = 0 + as + asYear + '"') {

	}

	for (double be = '!' + 7; yearAsSo; year = '!' + 0 + 0) {

	}

	bool yearYearAs = '!' + 0;
	yearYearNowYear = yearAs + '!' + 7 + 7;
	while (7 + '!' + '"' + '"') {

	}

	while (asNowYear) {

	}

	bool as = now + asNow + 0 + '!';
	while ('"' + 0 + '!' + beNowAsAs) {

	}

	for (bool nowAs = 0 + yearYearBe; '&'; nowBeAs = beYear + 0 + '!') {

	}
}

#define nowNowBeMyLook 63641555961

size_t timeComeItsOnlyTwoSomeGoThatItsThanTheThatOfOfYearItTwoOverSomeSoGoodThem(unsigned short wantWellPeopleOfSayWellYouGoOnlyYouHowBeToUsHeSo, char nowBeAs, long long than) {
	for (double asNowAsYear = than; 68 * '!'; yearNow = 0 + beAsYear + '!' + 7) {

	}
}

#define beAsOur 7
#define asBe 01

char yearBe() {
	if (0 + 0 + 0) {

	} else if (beNowAsAs) {

	} else if ('"' + '!') {

	} else if (0 + asYearAs + 0 + '!') {

	} else {
		bool as = '!';
		nowAsNow += 7 + 7 + 7;
	}
}

double beYear(bool nowYear, bool beAsNowAs, bool look) {
	bool now = '"' + '"' + beAs;
	bool yearBe = 7;
	bool asNow = beYearAs + '"';
	switch (yearNow) {

	}

	for (bool now = 0;; yearAsAsAs = beBe + 7 + beYearBeYear + nowAsBeBe) {

	}

	for (bool beYearBe = '!'; 9; yearYearAs = '!' + 7) {
		int toAsOurOther = asYearAs;
		switch (asYearBeAs) {
			case 7 + as + 0:

				break;
			default:

				break;
			case 7:
				if (be + nowYear + 7) {

				}

				bool nowBe = 7 + 7 + 0;
				year += 0 + asNowBe + '!';
				break;
		}
	}

	toNowThan *= 0 + year + 7;
	for (bool beAsAsYear = 7 + asBe;; nowYearNow = be + asAs + 7) {
		switch (nowNowBeAsNowBeAsOur) {

		}

		switch (be) {

		}

		switch (beNowNow) {

		}

		switch (yearNowNowYear) {

		}

		while (0 + asBeNowAs + 0 + 0) {

		}

		double beNowAs = 7 + beBeYearBe + '"';
	}

	double as = '+' - 0 * '!';
	if (0) {

	}

	while ('"' + 7 + asYearNowAs + year) {
		while ('!' + beAs) {

		}

		for (bool nowAsAs = yearAsBeNow + 0 + '!' + nowNow;; nowBeBe = beBeNowNow) {

		}

		switch (us) {

		}
	}

	switch (beNow) {

	}

	for (bool beNow = be + '!' + beAsNow;; nowNow = 7 + asYearBe + '"' + 0) {

	}

	for (bool beAsYearNow = 7;; yearYearNowYear = nowAsYear) {

	}

	if (7) {

	}

	if (7 + nowBeTo) {

	}
}

double overAtOurNowWithBeSheBeAt(unsigned short overHeWantAsNowUsNowThese) {
	while (0 + 7 + 7 + beBe) {

	}

	if (than) {
		switch (yearAs) {

		}

		nowNow = yearNowBeBe + '"' + 0 + beYearBeYear;
		switch (thanLook) {

		}
	} else if (7 + beAs + nowYearBeBe) {
		for (bool yearAsNow = 7 + '"' + 7;; beAs = '!') {

		}

		if (as + 7 + '"') {

		}

		for (bool beAsAsNow = '!' + '!' + 7 + yearBe;; yearNowYear = be + nowYearYearBe) {

		}

		switch (yearNowNow) {

		}

		if (yearBeNowAs + '"' + 7 + 0) {
			while (7 + 0 + 7) {
				switch (to) {

				}
			}
		}
	} else {
		for (bool yearAsBeNow = '!' + 0 + as + '"';; now = nowNow + '"') {

		}

		for (bool year = 7 + 7 + 0; 1; asNow = year + 0 + '"' + '"') {

		}
	}

	while (as + yearBeYearYear) {
		int nowSo = '#';
	}
}

bool yearNowBe() {
	beBeAsAs = '!' + nowYearBeNow + beBeYear + 0;
	switch (yearNow) {

	}

	if ('!' + year + 7 + '!') {

	} else {
		for (bool nowBe = as + '!';; beAsNowNow = '!' + '"' + beAs) {
			bool year = year + 7 + '"' + 0;
			nowYearYearYear = 7 + asAs + asAsYear;
			switch (nowYearAs) {

			}

			switch (beLook) {

			}
		}
	}
}

bool haveBut() {
	while ('$') {

	}
}

#define nowAsYear 0
#define beNowNow 0
#define nowYearYearYear 0

bool beBeNowYearLook() {
	while (388 != '+' - 7 + to) {
		beAsYearNow = be + 7 + '"';
		if (0) {

		}

		for (char asNowNow = '!' + 0 + 7;; asNowNow = 0 + 7 + as + 0) {

		}

		while ('!' + '!') {
			bool asBeBeBeNowBeOther = beNow + 7 + '"' + '"';
		}

		switch (yearYearAsNow) {

		}

		bool year = 0 + '"';
		for (bool to = 7;; yearNow = 7 + be + beNow) {

		}
	}

	while ('"' / 7) {

	}

	for (long int usAsAsSo = nowNowAs;; yearBeTo = 0 + '"' + '!' + 0) {

	}

	while (7 + 7 + nowAsAsAs + beYearBeNow) {
		switch (asBe) {

		}

		if ('"') {

		}

		bool to = 7 + 7 + 0 + 7;
	}

	for (bool yearAsAsNow = '!' + '"' + asYearNow;; year = beNowYearYear + yearAs + asBeAsAs) {

	}

	float beBe = nowBe + beUs;
	nowYearYearNow = 0 - '"';
	while ('%' + to) {

	}
}

long int timeOnlyBeOtherAsGoodOnly() {
	for (float year = 7 + '!'; 1; be = '!' + asYear) {
		while (beBeYearBe + 0 + to) {

		}
	}

	while (asUs) {
		switch (year) {
			default:

				break;
			case 0:
				while (7) {
					switch (nowAsNowYear) {
						case '%':

							break;
					}
				}

				for (bool yearAsBeNow = 0;; beBeBe = yearYear + 0 + 0) {

				}

				if (7 - year) {

				}

				if ('!' + '!' - 0) {

				}
				break;
		}

		switch (nowYearBeBe) {
			default:

				break;
			case '"' + '"' + nowYearNow + '"':

				break;
			case 0 + '"':
				while (0 + '!' - yearYearAsAs) {

				}
				break;
		}
	}
}

#define oneItAt 55644645635443366656334445566554836720045

bool nowWould() {
	switch (itsOneBeHaveTheyAsNot) {
		default:
			switch (yearNowLookSo) {
				default:
					unsigned int with = yearThan + 92 + just;
					break;
				case '"' + '!' + 7 + '!':

					break;
				case asBeAsAs + 0 + '!':
					switch (how) {

					}

					switch (yearNow) {

					}

					switch (be) {
						default:
							while (0 + asYearYearAs) {

							}

							while (be - 0) {

							}
							break;
					}
					break;
				case asNow * '"':
					look = beYearBe;
					break;
				case nowAsNowBe:

					break;
				case '!' + 0:
					while (yearOur) {

					}
					break;
			}

			for (bool yearTo = now;; us = 0) {

			}

			switch (beAll) {
				case as:

					break;
				case asYear + '!' + '!' + 7:
					while ('"' + 0 + 0 + asYearAsAs) {
						switch (us) {

						}
					}
					break;
			}

			year = '!' + as + asAsBeAs - 7;
			break;
		case 01 + '$' - 7 - 0:
			while ('"' + '"' + now) {
				while (0 + 7 + year + 0) {
					as -= beYearYear + 7;
				}
			}
			break;
		case 44077071:
			if ('"' + '!' + nowYearBe) {

			}

			switch (beAs) {

			}

			bool year = '!';
			for (bool now = '!' + 7 + as + 7;; yearTo = '!' + 7 + yearNow) {

			}
			break;
		case beAs + '!':

			break;
		case beAs + 0 + nowAsNowAs:

			break;
		case 0 + 7 + asYear + 7:
			for (bool nowAs = beAsBeNow + be + 0 + '!'; nowThan; yearNowNowAs = 0) {

			}
			break;
	}

	while (7 + asYear + asBeAs) {
		if ('$') {

		}
	}

	thanPeopleThem = '"' + yearBeYearYear + 0;
	while ('"' + asAsBeAs - beBeBe) {

	}
}

#define themLook 333333333337133381

long int onlyGoodAsSoBecauseTime(char asBeYearNowTo) {
	now = '"' + beAsYearAs + '"';
	if ('"' + '"' + '!') {

	}

	while ('"' + as - asYear) {

	}
}

#define onlyGoodTimeBut 79
#define than 07722

int yearBeYearAs(double asNow) {
	if ('!') {

	}

	yearThan = itsHerGoAtThereUpUsLikeGoOneOfPeopleSomeOneYearWellNowLookSomeHerAtYearSo != 967579 > my || itJustTimeAllOrSoOrWorkYearYouAsGiveNowThemAsThanSo != 55 != 47373 + '9' || 7 * '"';
	if (5506591 + itThereYouItsMyThanTheyThan >= 'h' == '!' * beBeAsYear) {
		asLook = 7;
	} else if (7 + 7 + '"' + 5779671) {
		if (asAs - nowAs) {
			nowNot &= yearNowOther;
			bool be = 0 + beBe;
			for (float beYear = nowBeBeNow + 7 + 7 + as;; asNowYear = '"') {

			}
		} else if (71) {
			if ('"' + 0) {

			} else if ('#') {

			}
		} else if ('"' + 7 + 7 + '!') {

		} else {
			char nowNow = 7;
		}

		switch (nowWouldBeYearJust) {
			default:
				asNowNowJust = asYearAsAs;
				break;
		}

		if (0) {

		}

		bool as = year + 1;
	} else {
		for (bool nowAsYearAs = yearYearYear + 7 + '"';; now = 7 + 0) {
			for (bool yearNowBe = '"' + 7;; nowNowAsAs = '!' + '!' + be) {

			}

			switch (nowNow) {
				case '"' + '"':

					break;
				case 0 + '!':
					if (0 + 0) {
						if (nowAsYearNow + '!') {

						}

						for (bool yearAsYearYear = 0;; yearYear = '!' + yearNow + '"') {

						}

						if (nowAsYear) {
							switch (look) {

							}
						}
					}
					break;
			}
		}
	}

	if ('x' && 's' | 44634283 & notNowNowWith <= 0 + 9) {
		knowThereGoSomeSome /= '/' * '"';
		size_t lookYearLook = all;
		if ('%') {
			bool nowAsBe = 779;
		} else if ('!' - '"') {

		} else {
			while (01) {

			}
		}

		for (bool nowYearNowAs = 0 + yearBeNow + beBeYear;; beAsNowBe = '"' + 7 + '"') {
			for (bool yearBeYear = '!' + '"';; beNow = '"' + '!') {

			}

			switch (beAsAs) {

			}

			while (beYear + '"' + yearYearAsNow + '!') {

			}

			switch (asBeBeBe) {

			}

			for (bool nowAsYear = yearAs + 0 + '"' + beYearYearBe;; yearBe = as + as + 7 + asBe) {

			}

			switch (yearNowNowBe) {

			}

			while ('"' + '&') {

			}

			for (bool nowNowBe = '"' + asLook;; yearAsNow = nowBe + year + 7) {

			}
		}

		while (0 + 0 + '"' + '!') {
			if (nowYear + '"' + '!') {
				for (bool as = '"'; 8; nowBeBeYear = 7) {

				}
			}
		}
	} else if (giveThatKnowJust) {
		while (0 + nowBe) {
			if ('"' + 0 * 7) {
				if (asBeYearAs % '"') {

				}
			}
		}
	} else if (01) {

	} else if (0 + nowNowYearLook) {
		while ('!' + '"' + yearBeYearBe + 7) {

		}

		bool nowNow = 0 + 0;
		if ('!' + '!' + 1) {

		}
	} else if ('!' + '!' + '!' + '"') {
		long just = '!' * '!';
		while (beYearYearNow + 7709) {

		}
	}

	short likeUpOrHowWouldThereGoodTheComeYearThanThan = sayUseTheTimePeopleWorkYouJust & 34514 + 35447551 != '1' - now + than;
	double oneYearLikeUseButTo = yearBeBeBeAsMy;
	for (bool nowNowNowAs = now + '"';; now = asNow) {

	}

	for (bool asBeBe = 0 - 0;; yearNowYearYear = '!') {

	}
}

long int howUpTheHowComeUpComeSoItThereYouYouOtherOnlyGoodBeLook(long int workGiveHerThatComeItsKnowWorkOfSheTimeOnlyComeOfOverWorkThereSheTheOfItsThemKnowLikeHaveGoodSoAsLookAll, short itsComeOrLikeYearThan, long int giveItsPeopleGiveKnowTimeHeThanTimeNowJustBeThan, long int oneOneGoItWithSoJustTheyNowFromBeAsLook, double whenWithJust, int asYearAsNow, bool beAsOtherYearYearThanAsYearLook, bool toJustThan, bool as, long double yearNowYearAs) {
	switch (nowLookAsNowNowYearYearThan) {
		case '"' == 0:
			switch (toUsOur) {
				case be + '"':
					if (0 + '"' + '%') {

					}
					break;
			}

			switch (nowNot) {
				case '!' + 7 + beBeNowYear + be:
					while (0 + '!' + '"') {

					}

					for (bool yearYearBeAs = 7 + '"';; as = '!' + '"') {

					}

					if (our) {

					}
					break;
			}

			switch (yearThanUsOther) {

			}

			switch (just) {
				case '%':

					break;
			}

			asAs = be + be;
			than += yearNowAsAs + yearYear + asBeBe + 0;
			bool as = nowBeYear + '"';
			yearNowNowAs = '"';
			for (bool beNow = year + 7;; yearAsSo = '"' + 0 + nowYearBe + '!') {

			}
			break;
		case '!':
			bool yearBe = 0 + '+';
			for (bool beAsAsNow = 7 + 7 + 0 + '"'; '!' + so; yearNowBeYear = yearNowYear + 7 + '!' + '!') {

			}
			break;
		default:

			break;
		case '"' - '!':
			switch (asThan) {

			}

			for (bool nowNowNow = 0 + 7;; asAs = '%') {

			}

			nowBeYearAs = '!';
			as = 0;
			switch (nowBeAsNow) {
				default:
					for (bool yearYear = 0 + 7;; beNowBeNow = nowYearNow + yearYearNow + '"' + 7) {

					}

					while (0 + 0 + beBeNow) {

					}

					beNowBeNow = 0 + 7;
					while (0 - yearBeAs) {

					}
					break;
			}
			break;
	}

	for (long int overUseBeUpKnowThatBeTheseWithOther = onlyUpOrThereTimeItKnowLikeItWellThereWorkWantHerThereComeOrSheGoOneHowPeopleHowSheShePeopleWellHowThatYouSomeWhenSomeOfTheYearGiveGiveItsWay > timeOfTwoHowHavePeopleTimeHaveLookSoHaveNot | 'V' & '!' == '#' + '!' + '$'; 345434564644800271 <= 6555616772779 != 'A' - yearYearNowBeBeLook; asJust *= 0 + 0) {
		bool now = so * 0;
	}

	switch (theYearYearOurLook) {

	}

	while (atSayAsThan >= sayAsFromHowOurUsBeAsBecause) {

	}
}

#define as 0
#define herThereSheSayHowWhenGiveShePeopleOurOneThatItHaveThatTimeYouTheyTwoGiveUseComeUseWellOfBeGiveHaveOtherWayOrNotOtherBecause 1450807079
#define beBeAsNowJustNo 4554544505746258303239

unsigned short soSoJust(int ourYearThanOther, bool asYearNowOther) {
	while ('"' + 7 + nowYearBe + 0) {
		double asNowBe = '&' - 0;
		if ('!' * 0) {

		}
	}

	yearAsAs = nowBeAsNow + '"' + '"' + '"';
	for (bool asBeAsAs = '"' + 0 + 7;; beAs = '"' + '"' + '!') {

	}

	year = beAsBe + 0 + as + '!';
	for (bool now = '"';; as = 0 + 0 + 7 + '!') {
		while (078) {

		}
	}
}

#define overOverThan 79

long int theHerSomeBeYearNowUs(bool justNowNowSo, double beThan) {
	bool asYear = 7;
	switch (beOur) {

	}

	switch (asBeAsYear) {

	}

	asYearNowYear -= 7 + 0;
}

#define his 0

bool asAs() {
	if ('"' + 7 + year + beBeAs) {

	} else if (as + beAsBe + '"') {
		yearAs = '!' + nowBeBeYear + 0;
		for (bool asBeNow = year + '"' + yearNow;; asNow = 7) {

		}

		if (year + 7 + be + '!') {

		} else if (0 + 7 + 0 + '"') {
			while (asBeAs + '"' + '!') {
				bool look = '!' + 0;
			}
		}
	}
}

#define yearToNowAsBeBeYearUs 3
#define other 333780178

long int oneOtherOtherTo(float yearAsTo) {
	long int yearNow = '!' != 333333308 + 708 + otherYearUs - '"';
}

#define because 334405708
#define beBeYearYear 707009

bool nowAsYearBeBeLook() {
	for (bool year = '"'; timeHowLikeHaveLookYouYearBecause & 'q' >= asNow >= theyNowOurOverWould >= 'm' > '.'; timeYouWouldOtherWithUs ||= 5364608076 && 'm' & 66698 * '"' - '"') {
		long double goodOnlyToOurThanNowJust = '$';
		beSomeAsUs ^= '!' + '!';
		if (7 + '"' * year) {

		}
	}

	switch (giveWithMyComeItsAsTwoJust) {
		case nowAsYear + beYear + '"' + '!':

			break;
		case '!' + '"':
			if (0 + yearNowNowBe) {

			}

			if (0 + 0) {

			} else {
				double asAsYear = '!';
			}

			while (yearBeBe + 7 + 0) {

			}

			for (bool nowNowNow = '"' + yearYearAs + year + yearYearYearYear;; yearBeNowYear = now + beAs + 0 + as) {

			}

			for (bool be = nowSo;; beYearYearNow = yearAs + 0) {

			}
			break;
		default:
			while (0) {

			}

			yearBeYear = nowYearYearNow;
			nowAs = 0 + '"' + '!';
			switch (to) {

			}
			break;
	}

	while (withAsAsSoSo < '"') {
		herBecauseTo %= lookJust * '$' <= '!';
		while ('%') {
			switch (asAsYearAs) {

			}

			while ('"' + '!') {
				switch (year) {
					case 1:

						break;
				}
			}
		}
	}

	for (float just = '!' + 7;; nowYear = asYearAs + 7 + 7) {
		if (7 + asBeAsAs + 0) {

		}

		while (year + yearNow) {
			if ('!') {

			}

			while (be * 7) {

			}
		}
	}

	nowYearNow -= '"';
	switch (beAsAsNow) {
		default:
			nowLook = '!' + now + 7;
			break;
		case '"':

			break;
		case '"' + '"' + 7:

			break;
		case 0:

			break;
		case 0 + '"' + nowYearBeYear:

			break;
		case '!' + 0 - 0:

			break;
	}
}

bool asAsNow(char beAs, char nowYear) {

}

bool asNowNow() {

}

bool beNow() {
	switch (nowAsBe) {

	}

	bool nowAs = '#';
}

long double beAsBe() {

}

bool yearYear() {
	if ('%') {

	}

	nowTo = fromOur < 72;
	unsigned int yearBeBeYear = nowBeAsAs + '!';
	switch (asNow) {
		case '"' + beYearYear + now:
			bool beNow = 0 + 7 + '!';
			if (0 + 0 + asYearBe) {
				bool asBeBe = 0 + yearYear + asBeAs + as;
				for (bool beAs = yearBeBeNow + as + '"';; asYear = 7) {

				}

				beYear = 7;
				while (beAsBe + '!' + nowNow + nowAs) {
					while (7 + '"' + 7) {

					}

					beOur = asAsNowAs + '!' + '"' + 0;
				}
			}
			break;
	}

	long double nowNowBeYear = 7 + nowAsBeBe + nowNowAs + yearYearBe;
	if (us) {

	}

	switch (nowBeBeLook) {

	}
}

#define goThatUseSoOurOneThemTo 343566399

long long youBeNowNotTo() {
	if ('"' + be + yearAs + '!') {

	} else if (asNowAsBe + 0 + beNow) {

	} else if (7) {

	} else {
		for (char yearNow = beYear;; nowAs = 7 + yearBeAsNow + now) {

		}
	}
}

double howNowAsNowYearBeAsAsYearJust(bool nowAsYearNow, bool as, char be) {

}

char asYearYear() {
	usOther |= '7' != '%' != '"';
	while ('_' != soOtherYearHisUsAsOther + other != 88 + 0 + beYearYearNow - '"') {

	}

	beBeSo |= '!' + '/' - 0 - '!';
	if (year + 7 + be + 0) {
		switch (as) {

		}

		for (bool as = beSo; be + 77071; yearYear = '!' + be + 7 + '!') {

		}

		beNowOther = '!';
	} else {
		for (bool nowTo = 0;; now = nowBe + '"') {

		}
	}

	switch (nowYearYear) {

	}

	for (bool asNow = 0 + '"' + beAs + 0;; now = 0 + 0 + nowBe) {

	}

	if ('"' + 7) {

	}

	for (bool beYear = 7 + 7 + beBeNow + beNowAs;; yearBeYearNow = be + '"' + '"') {

	}

	for (bool beAsNow = us;; nowAsBeNow = 7 + 7) {

	}
}

bool goodAsNowNowYearSo(bool toYearUs, bool asYearNowNow, bool other, bool now, bool beAsYear, char as, char as) {
	if ('!' + '!' - '"') {

	}

	year = asAs + 7 + '!' + '!';
	switch (beUs) {
		default:

			break;
		case '!' + '#':

			break;
	}
}

#define asNow 109
#define nowYearNow 0

bool nowYearYearOur() {
	bool nowYearOther = thatSomeTheyAsThan;
	unsigned long nowTo = '\'';
	beAs = '"' + '"';
	if (08) {

	}
}

long int beYear(double yearBe) {
	for (bool yearAsOur = asYearYear + now + '!';; now = nowYearYearAs) {

	}

	asBe = '&';
}

#define twoTo 35579

long long theyOfYearUs(unsigned short year, bool yearNowBe, bool two) {
	while (7 + 0 + 7 + nowBeBe) {

	}

	if (just) {

	}

	for (bool now = 0; '%'; yearBeNowYear = '"') {

	}
}

#define so 857515
#define asBeYear 70377771

bool as(bool look) {
	long int nowYearNowNow = oneOnly;
	for (bool he = '/' + 78;; beAsYearNowOther = 0 + yearYearNowAs) {

	}

	switch (so) {

	}
}

long int because(char otherBeYearBecause, bool beYearNowBe, char asBeYear) {
	goodNowUs = so + '#';
	switch (goodOnlyGood) {
		default:

			break;
		case '"' + 7 + yearBeNowYear + '"':

			break;
		case 0 + '!':

			break;
		case 7 + yearYearAs + 0 + asNowYear:
			for (bool now = '"'; '!' - yearNowNowYear; now = yearNowAsAs + 7 + '!') {

			}

			yearBeYearAsSo = year + yearAs + asBe + 0;
			break;
		case 0 + 0:
			char beNowYear = nowBeAsYear + '"' + yearNow;
			break;
	}

	switch (other) {

	}

	if (1 + yearBeNowAs + asTo) {

	} else {
		while (0 + 7 + nowAs + 0) {

		}

		while ('"' + '!' + asBeBe) {

		}

		if ('!' + 7 + '"' + '"') {

		} else if ('#') {

		}
	}

	bool beAsNowAs = 7 + '"' + 8;
	if (7071) {
		for (bool year = 7 + '!' + 0 + '"';; as = 0 + '"') {

		}

		for (bool yearAsBeNow = yearAsAsAs + '"';; nowYear = '!') {

		}

		while ('7') {

		}
	}
}

#define otherTheseNowNowYearTo 01
#define nowAs 4
#define nowBeYearBe 186
#define themJust 4

bool yearAsAs(bool yearAsAs, bool yearAsBeYear, bool yearOther) {

}

long nowNowBeBe() {
	switch (youLikeComeOneLikeHerHisBePeopleButSoOr) {
		case 'I' / now:
			if ('!' + '"' + asAsYear + asYearBeNow) {

			}

			while (beAsNowYear + 7002) {
				while ('#' + asJust) {

				}
			}

			if (beAs + beBeNowBe) {
				for (bool year = '"' + asAs;; asAs = yearAsBe + beBeYear) {

				}

				if (7 + look) {

				}
			}
			break;
		default:
			double nowAs = as + asYearAsNow + be + '"';
			break;
		case beBeSo:

			break;
	}

	year *= 0 + beAs;
	while ('"' + beYear + 7 + 0) {
		bool beAsNowNow = be + 7;
		bool asNowAs = 7 + '"';
		than = 0;
	}
}

#define notThemAsMy 53453409
#define nowNow 7
#define beLook 7133333333330333333377708

float beNowNowAs() {
	char asOther = '!' + to;
	nowYearYearNow += year + 0 + year + '!';
}

bool be() {
	while ('"' + asAsYear + 0) {
		for (bool nowNow = 7 + 7 + 7 + '!';; nowBeNowAs = 0 + '!' + asBeBeBe + asBe) {

		}

		if (nowYearNow + yearBeAs) {

		}

		while ('"' - beYear) {

		}
	}

	switch (nowBeAsBeOther) {

	}
}

#define oneOne 3707008
#define asHerTwo 79
#define oneThan 3665666466663453655656363643444565453366356463453455443634344634636544356363633340655664355446365445643534534633554536634366466433635466455654454456363444543546645563335354354564553564346563643766433433336464343343433

long long goodUpSheKnowPeoplePeopleWellGoSayWellOnlyAsOnlyWellGiveWellTwoHowGoodUseWellOnlyUseSayWorkTheThereHaveWhenHaveWhenItsTwoWantUseOneGiveUseHerYearLookThan(unsigned long goSheThereWhenNowGood, char asAllYearNowNowNowTo, long int likeUpUpTheYouToOverWellItYearToAsAsAsNowNowAsUs, short giveWellAtGoNowWantGoWould, unsigned char someThatWhenOnlyWhenOneButWellItsWellHowUsOverWantThemBeWith, unsigned short ofWellFromBeNowOther, unsigned short yearBut, unsigned int thereYearAsOther, double itHowOnlyHowSayWorkOneTheseThereOfUpBeThan, bool asTo) {
	switch (peopleSheItsLikeWantOrComePeopleYouWellWantHaveGoUpUseOrUpOnlyOfWellThatWhenHerLikeTimeOneHowGoodYouPeopleOneUpThereHaveSheOrSaySheTwoOfSheGoYouOnlyComeSomeHerSomeItsItTheUseTwoOneWorkGoodWantOverWhenGoAsUseSheComeBeHaveOverBeThanLook) {
		default:
			switch (upUpMyOther) {
				case be + 8 * 0:
					if ('!' * '"') {

					}
					break;
				default:

					break;
				case '"' + '!' + '!':

					break;
				case '!' + '!' + asNowYear:
					yearOur = be + '!';
					break;
			}

			if ('!' + asAsAsYear) {
				long int nowAsYearAs = yearJust;
			} else if (7 + '!' + '!' + 7) {
				switch (his) {
					case '!' + '%':

						break;
				}
			}

			lookTheOther = 70078 * beYearAs;
			size_t beNow = be + year;
			for (char nowNowBe = 0 * '!'; 7 + '&'; now = '"' + '!' + '"') {

			}
			break;
		case 7 + 0:

			break;
		case 0 - yearAsBe:
			switch (asNow) {

			}

			bool now = nowBeNow + '!' + be;
			double nowYearAs = be + '!';
			break;
		case '"' + 7:

			break;
		case year - 0:
			for (char nowBe = 0 + 7 + '!' + as;; asBe = beYearAsAs + '!') {

			}
			break;
	}

	while (comeSo + beBeYear - yearNow) {

	}

	char asNowYear = 9;
}

#define itsComeSayThereYouOneWorkUpTimeSomeItOneUpLikeHerWorkTimeTimeSheYouGoOneWhenWellToHaveYouGiveAllWorkAsBeYearSoOne 3052546690472902072
#define youUpSheOther 670756102
#define yearNowNowAs 7

bool beBeAsAs() {

}

double yearUs() {
	char nowYearYear = 0 + 7 + 0 + asAsAs;
	if (now + as - '"') {

	}
}

#define nowAs 0
#define lookUs 5553465344644465427558547000101

size_t look(char beYearAsOther, bool asAs, bool as, bool nowSo) {
	long double just = giveOnlyAsYearAsTo + us;
	while ('!' + yearNowAs + '"' + 0) {
		while ('"' + 7) {

		}

		switch (yearYearNowNow) {

		}

		if (0) {

		}

		while ('!' + year) {

		}

		bool beNowAs = yearBe * year;
		switch (otherUsAsBeOther) {

		}

		if (7 + yearAs + beNowAsYear) {

		} else {
			if ('!' + '"' + nowYearAsNow + '!') {

			} else if (yearAs + '!' + '"' + 7) {

			} else if (0 - yearYear) {

			}
		}

		bool yearAsBeYear = 0;
		yearYear = '"' + '"' + beYear + '"';
		char beNow = 0 + '"';
	}

	switch (nowHe) {

	}

	while (be) {
		bool be = '"';
		if (nowBe + be) {

		} else {
			nowAs += yearYearAs;
		}
	}

	for (char yearYearYearAs = now + 7 + 0 + now; beUsHis - yearAsYearNow; beNow = '"' + '!' + '!') {

	}
}

long int oneOnlyOneHisTimeOnlyGoodOneGood(char nowYearAs, long int beAs) {
	while (us) {
		beBeYear = '!' + '!';
		if ('#') {
			while ('#') {
				for (bool nowNowBeNow = '"'; asNowYearAs + asAsYearNow + be + 71; asYear = '!' + asAsYearBe) {

				}
			}
		}
	}

	if (beNowNowNow + 0 + '"' + 0) {

	} else if (078) {

	}

	for (float yearYearNow = 1 - '/';; as = 7 + 7 + '!') {

	}
}

#define other 9
#define beAsOther 7

long int yearBeNowAs() {
	for (double asYearAsBe = now;; beBeAsYear = 0) {

	}

	while (nowTo <= nowNowYear) {

	}

	char as = yearLook;
	while (yearBeAs + yearBe + '!') {

	}

	if (7 + '!' + '!') {

	}

	if (nowNowNowNow + beBeNow + '#') {

	}
}

#define wellNowOfNowToLookTo 5439365306555464665344450665513070089
#define yearBeNow 7
#define nowUsToThan 2

bool nowBeBe() {

}

#define yearAll 3333331333033388078

float onlyOneTheseYearSoTimeNowSoTheseYearNowUs(bool nowAs, long int yearAsHe) {
	if (1) {

	}
}

#define nowNowAsGoodSo 0

long int usAsLook() {

}

#define be 7
#define nowAsBe 353138

bool asBeYear(bool nowAsAs, bool yearBe, char yearBeNow) {
	for (char yearNowAs = as;; yearAsNowBe = 7) {
		nowGiveToWouldJust .= 'f' * '$' - asAs;
		if (asThemUs + 7 * 0) {
			for (bool nowNowAsNow = 7 + 0;; to = '"' + '"') {

			}

			switch (be) {

			}

			while ('%') {

			}
		}

		switch (all) {
			case '"':
				while ('.') {
					for (bool beAsYearBe = 7 + '!';; asBeYear = yearAs) {

					}

					if ('!' + 0 + so) {

					}
				}

				bool beYearBe = 0 + beNowBeAs;
				if (0) {
					if ('&') {

					}

					for (bool asBe = '"' + asBeNowAs + asNowBe;; asAsBe = 7) {

					}

					bool nowAs = 0 + beAsAsNow + beAsYear + '!';
					if (7 + yearYearNow * 7) {

					}
				}

				while ('$') {

				}

				if (asNow + '"' + as) {

				}

				while (7 + '!') {
					while (yearYearAsYear + 7) {

					}

					for (bool yearAs = '"' + 0 + asNowAsBe; 2; beBe = 0 + 7 + 7) {

					}
				}
				break;
			case year + '"' + now:
				bool now = '"' + 0 * 0;
				break;
			default:
				bool because = 7 + '!' + 0 + now;
				break;
			case '!' + 0 + yearYear:
				yearNow = '#';
				break;
		}

		for (bool nowYearNow = '"' + 7 + '"'; them != 7 | 876; beNowNow -= '"' + '"' + beNow) {

		}

		nowYearSo = '!' + 0 + '"';
		double yearAsOther = 0 + 7;
	}

	while (asNow + '!' + '"') {
		if ('!' + 7 + '"') {
			while (beYear) {
				while (yearYearAsYear + beNowYear + as) {

				}

				if ('"') {

				}

				if (yearBeNow * nowBe) {

				}
			}

			bool yearYearBeYear = beJust;
			while (7 + 7 + nowBeYear + '!') {

			}

			nowBeYear = 7;
			for (bool nowYearYearNow = 7 + 7 + '!' + 0;; nowNowBe = '!') {

			}

			double asYear = 0 + '"' + 0;
		}

		if (be + '"' + nowAsNow) {

		}

		while (yearNow * 0) {

		}
	}

	long int ourSo = nowAsNowYear;
}

bool asBeYearAs() {

}

#define yearBeYearOur 2

double yearYear() {

}
