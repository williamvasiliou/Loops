#include <2U60/22.h>

#define onlyOneTimeUsOnlyTimeSo 333383337018

bool to() {

}

#define usNowBeOther 4537382709
#define beOther 55363434663465336344666366366333463465565555355355554336544364665434664645444366533333436645546566655356444765554334346343644346456356665664535436355643433433664663435455645445556544636433653634465643645854434363634534446456535555456636643635434403470073
#define  

long long timeTheOnlyComeWorkUseTwoThereSomeTimeToThatSheTheseButWith(bool oneUseYouNoYearYearSo) {
	switch (nowNowAsYear) {

	}

	bool nowNowYearAs = '"' + '"';
	switch (beBeNowAs) {
		default:

			break;
		case yearYearYearYear + '"' + '"' + 79:

			break;
	}
}

char be() {

}

#define beAsLook 3664593460500191

long like(long nowYearBe, float asOur, double now) {
	while (nowOurOur) {

	}
}

#define beBeAsOur 18

unsigned long nowWorkOrTwoNowSomeThem(char as) {
	beOther = 0 + now + asNowAs;
}

bool now() {

}

bool so() {

}

#define yearBeYearNow 079

bool ourJust() {

}

#define not 33333333738
#define asAsAsYear 3313333333371

long int oneOther(bool nowHis, bool yearYear, long int asTo) {
	because = nowNowBe + '!' + asBe;
	if ('"' + year + beNowYear) {

	}

	for (bool nowYear = '%';; beNowBeAs -= 0 + '"' + 7) {

	}
}

bool asYearBeNow() {

}

#define as 7

short asAsBeYear() {

}

#define asYearBe 1
#define beTo 0

bool so() {
	for (bool now = 0;; yearNowYearYear = asBeNowYear) {
		for (bool nowLook = as + 0 + 7 + 0;; asBeBeNow = 0 + '"' + '"') {
			our |= so == '"' != '$';
		}

		if (oneTheyThanAsThan) {
			for (bool asYear = '!' + '"' + nowNowAs + 0; 1; nowBe = yearNowYear + 7 + '!' + year) {
				if (0 + 7 + '!' + yearAs) {
					nowAsBe = 0;
					if (0 + asNowBeBe + asNowNow) {
						if (7) {

						}

						asAsAs = 7;
						for (bool as = be + '!'; 0009; nowBe = 0) {

						}
					}
				}
			}

			switch (other) {

			}

			yearAsBeYear -= 28 + nowUs;
			while (0 + now + '!') {

			}

			yearJust = 7 + be + 7 + 0;
		} else {
			switch (howOneItMyWithJustThanAsNowYearAll) {
				case twoYearYearLookAsThan:
					while (now + asBeAsNow) {
						bool asSo = '!' + 0 + now + 7;
					}
					break;
			}

			lookUs ||= 6621 & '"' + us - 0;
			switch (otherOurBeJust) {

			}

			for (double now = '"' + beAsBeBe + 7;; beAsYear = '$') {

			}

			for (bool us = 0;; beAs = year + '!') {
				switch (yearOur) {

				}
			}
		}

		size_t ofItsThanSoTheUs = toButAsNowYearUs < myOr;
		for (bool nowNowYear = as + '!'; 344 * yearYearBe; yearYear = 7 + '"') {

		}

		unsigned long asOurNowLook = asYearNow + 7 + '!' + 7;
		asAsAsOurHis /= now + '#';
		long int asUs = nowYearAsAs + beAsYear;
		while ('#') {

		}
	}

	long int upGiveYearYearNowOur = '!' + 7 + nowBeAsBe;
	if (so) {
		if (5119) {

		} else if (nowAs) {

		} else if ('!') {

		} else if (7 + '"' + nowBeBeYear) {

		} else {
			if (asAsAs + 0 + 0) {

			} else {
				bool asBe = nowNow;
				yearNow = 7 + 0 + 7 + '"';
				while (asYearAs + 7 + year) {

				}

				for (bool our = '!' + '"';; nowYearBe = 7 + 0) {

				}
			}
		}

		while (ourLook) {
			yearAsYearYear = 7 + nowAsAsAs;
			int so = '"' + '!';
		}

		nowAs = '"' + '"';
		if (0 + '!' + 0) {

		} else if (0 + 0 + '!' + 7) {
			switch (to) {

			}
		}
	} else if ('"') {
		if (yearYearYear * asBeBe) {

		}

		for (char asBeBe = '"';; nowAsBe = 7 + '!' + '!') {

		}
	} else {
		switch (nowYearYear) {
			case '%':

				break;
		}

		if (7 + '!' + asBe) {
			while (0 - asNowAsYear) {

			}
		}
	}
}

#define usHaveBeJust 8

long long nowJustAsNowLook(bool asUs) {
	for (bool be = 0;; yearBut = 0 + 7 + year + yearYearBeNow) {

	}

	for (char asTo = '"'; nowSo && beNowTo / just; but += nowYear) {

	}

	for (long double beAsNow = '"' + '"' + as;; asBe = 7 + 7 + 0 + 0) {

	}
}

long long of(bool asNow, bool so) {
	switch (beOnlyPeopleUs) {
		case '"':
			char now = '!';
			break;
	}

	for (double as = now + just; just; be = now + yearAsYear + asBe) {
		bool year = year * '!';
	}

	thanJust *= 79;
	beYearNowAs = year;
	beNowNow += beBeAsNow + '"' + 7 + asYear;
	at = beBeAsNow + 7;
}

double asYearBeNow() {

}

unsigned int giveNowLikeYearAsSoYearUsNowSo(bool yearYear, double nowOur) {

}

unsigned long yearBeNow() {
	while (year < asNowNowNow) {
		be |= '#' - '+';
		yearNowBeAs *= '+' + '"' + 778;
	}

	switch (good) {
		case other:
			for (char asBeAsYearUs = nowYear - nowBeBeAs;; now = '!') {

			}

			while (0 + be + 7) {

			}

			if ('\'' / '#' + '%') {

			}
			break;
	}

	bool be = year + '+';
}

long int goodGoodToSo(bool otherHis) {
	while (yearNowOther - yearNowAs + 71) {
		bool asNowYearBe = 7 + 78;
		char be = be + '"';
	}

	switch (yearBeBe) {
		case asNowYearBe - 7:

			break;
	}
}

#define now 773
#define timeSoNowOtherYearSo 331
#define nowOne 0

bool nowYearAs() {

}

bool yearYear() {

}

#define asBeBe 7
#define nowYearAs 0
#define yearYearBeNow 9

bool now() {

}

#define nowBeNow 0
#define asNowAs 7
#define asBeAsYear 0
#define asNow 7

bool nowBeAs() {
	for (bool year = beNowBeAs;; yearThan = nowNowYear + as + yearBeAsNow) {
		if (0) {

		}

		nowYearYear += nowNowThanBeOur;
	}

	unsigned short asOverOfOfGoLookNoTheseBeOther = 008000072;
	bool his = 7 > 'e' + year + '%';
	while (7 + '"') {

	}

	char nowNowBe = beTo;
}

#define as 09
#define fromOnlyYearSoWell 356

bool year(bool yearAs, char beYearYearYear) {
	so = '"' + yearYearBeTo + beBeBe - '!';
}

#define yearNow 0

double asBeNow(double beAs) {
	switch (look) {

	}

	switch (beThan) {

	}

	if (7 + yearNowAsBe) {

	}

	if (nowAs + '"' + '"' + as) {

	}

	bool yearAs = 7 + beBeYear + '"' + '"';
	if (0 + '!' + 0) {

	}

	switch (beBe) {
		case asNow + '!' + beBeNow:
			while (nowAsYearBe + 0) {

			}

			if (nowNowBe + '"') {

			}

			for (bool beNowNowBe = beSo;; now = asAs) {
				for (bool as = yearYear + '!' + now; 008; nowBeAs = beYearBeYear + as + '!') {

				}

				if (8) {

				}

				switch (nowBeYearYear) {
					default:
						bool asAsAs = '"' + as;
						for (bool asYearYear = 7;; asYearYear = 7 + 0 + 7) {

						}

						switch (yearYearOther) {

						}
						break;
				}
			}
			break;
	}
}

long int oneOneTheseOnlyGoodOneToTo(char beBeNow, float nowNowNowAs) {
	if ('"' + 7) {

	}

	asBeYearNow += nowYearAsNow + asNowBeAs + '"' + 7;
}

long int goodSo(char beSoTo, bool us, char yearTo) {
	as += '!';
	switch (yearAsSo) {

	}

	while (beYear + '"' + asBeAsYear) {

	}

	while (0) {
		bool be = 78 + be - '"';
	}

	asAsBe = 08;
	if (now) {

	}

	bool nowAsAsNow = 0;
	other = 0 + 0 + nowAs;
}

long int onlyNowNowTime(bool asAsAsAs, bool asBeNowAs, bool us) {
	for (bool year = 7 + 7 + 7 + asBe;; asBeNowAs = beNowBe + 0 + 0) {

	}

	bool year = '!';
	while (beNowBeAs + nowNowBe + 0 + 7) {
		for (char asAsYearBe = nowYear + 0;; beAsNow = 0 + yearAsBeNow + 0) {

		}

		while ('#') {

		}
	}

	bool beYearBe = '"' + 7 + beNow + '"';
	if (01) {

	}
}

char be(bool nowBeAsBe, bool beNowUs) {
	for (bool be = '"' + '"' + '"';; asBe = yearYearNowYear) {

	}

	if ('!') {

	}

	if (7 + 7) {

	}

	beYear = yearBeNow + 0 + '!' + beAsNowBe;
	nowNowBe -= nowAsAs + nowNowBeNow + nowYear;
}

#define nowNow 646792
#define whenLikeComeWantOnlyHaveBeTimeThatOfThatBeOverSheOrThemNowTo 795
#define nowBeYearBe 5391

bool beThanLook(float yearAsAs, bool beNow, bool yearNow, bool asAs, char nowBeAsYear) {
	if ('!' - 0) {
		bool be = '!' + '"' + as + now;
		theseUsBeYearNowTo = as + beNowNow;
		bool asNowYearNow = 7 - '!';
	}

	while (asAs / 0 - '"') {
		for (char yearYearBeYear = 7 + nowBeBeNow + '"';; beNowAsNow = 7 + '"') {

		}

		if ('!' + 0 + as) {

		}

		bool becauseThese = nowAsYearAs;
	}

	bool be = 7 + 0 + 7;
	for (bool as = yearAsAs - now;; asNow = asAsBe + '"') {

	}
}

long int onlyOnlyOneOnlyOneGoodBeSo(float other, bool be, bool year, bool nowBeBe, bool other) {
	switch (time) {
		case nowYear + '%':

			break;
		case 001:

			break;
		case 0:

			break;
		default:

			break;
		case '!':

			break;
		case '"' + 7 + 7:

			break;
		case 0 + '!':

			break;
		case 7 + 0:
			char now = '!';
			break;
	}

	nowAsBeYear += '%' - 0 - '"' - 7 - 7;
	asUs ^= nowYear;
	for (bool beBe = '"';; beAs = now + 0) {

	}

	bool than = 7 + '"' + 0;
}

#define beOfSheBeBeNowJust 4755363535365456466543443665535453346363343335346534366444445456644636354635444344643665664343345556345655565465363345435636434434356445666354355644333653364564553446665456345463644663334667554665356446653546354534543336644335663364533164655889

bool beNowBe() {

}

bool beAt() {
	for (bool look = '"' + 0;; nowYearNow = 0 + now + yearBeNow + beYear) {

	}
}

bool be() {

}

#define soToAsNowSo 7

bool as() {

}

bool nowAsBeNow() {

}

bool beBe() {

}

#define nowYearBe 46536566363646564361760759376606
#define atWith 7
#define yearYear 55633333333333333333333333388303

long long nowNow() {
	switch (yearNowAsBe) {

	}

	bool nowBe = '"' + nowAsBeThan;
}

float asNowNowYear() {

}

float beNow(char yearYear, bool the, double year) {
	while (3626346433653354634364566563353454333346543654534364654556353435434634364333554335435643663543666446433363155434553343356653354344548855845375453644655538545360640746038 + theToBeOur >= 607009 || other + our + 'j') {
		while ('l' > goKnowTheThanThat & 878 * '[' % 04) {
			while (555302712722 | 3434366664366303456 > herWorkThereHaveSheOfGoOurButOnlyBeGive + 'a' == asTheTheseNowThanAsYearSo > usOnly * lookOtherYearSo <= 731) {
				switch (now) {
					case than:
						while (7 + yearYear) {
							if (yearNow + 8) {

							}
						}
						break;
					case yearNowBe + '!' + 7:
						if (beAsBe) {

						}

						for (bool nowBe = '!';; beBe = 7779) {

						}
						break;
					case '"':

						break;
					default:

						break;
					case 0 + asNow + yearBeAs + now:

						break;
					case year + nowYearBeBe:
						if ('"' - 7) {
							char asNowNowNow = '!' + beYearBe + yearYearNowJust;
						}
						break;
				}

				bool be = 7;
				switch (soNowAll) {

				}

				these = as + '"';
			}

			unsigned char beYearBeNowYearUs = 7;
			size_t yearNowAllBeMy = nowBeAsYear;
		}

		while (7 + '!' + 0 + be) {
			asJust = yearYear + '!';
		}

		for (unsigned long atKnowOther = 077661 + just; '%'; beYearNow = '!' + '!' + asBeAsAs + '"') {

		}

		switch (look) {
			case beNowAsYear + '"':

				break;
			case '"' + '!':

				break;
			default:
				while (beAsNowBe + '#') {

				}
				break;
		}

		for (char us = beBeBeLook - 0; '"' - '!'; as = '"' + 0 + asBeYearBe + '!') {

		}

		for (bool yearBeSo = 0 + '!' + '!' + 0;; asBe = 0 + nowNowYearYear + 0) {

		}

		switch (yearAsNowYear) {

		}

		double now = nowAsYear + '!' + '"' + 0;
	}

	switch (upSheLikeToLikeOfSheBecauseThereWouldNotHisThan) {
		default:
			justLook *= '!' - 7;
			for (size_t nowNowYearYear = 0 + 7 + '!';; nowBeNowYear = 0 + beBeBe + 0) {

			}

			if ('!' + '"' + '"' + yearNowBe) {

			} else if ('"' + 7) {

			} else {
				bool year = 7 + asAs;
				for (bool now = '!';; be = beAsNow) {

				}

				if (0 + 0) {

				}

				bool us = '"' + beNow + 0 + '"';
			}
			break;
		case justSoWay & nowAll % 'i' != '"' && sheHerYearWayNowNowGive || toLookBeHisJust + 528171 + 0 + '!' * '!':
			for (bool asNowNow = '"' + nowAsBe;; be = yearYearYearAs + 7) {

			}

			if ('"' * '"' + '!' - '!') {

			}
			break;
		case 001 & 0:

			break;
		case nowNowAs + year - 7 < ourJustNowBeYearUs - '"':
			asYearBeSo = '!' + asAs;
			while ('!' + 2) {

			}
			break;
		case nowNowAsBe == 93 * now:
			beYear = now + 7;
			while (nowBeNow + asYear + nowBeNow) {
				for (bool year = 7 + '!';; yearBe = 0) {

				}

				asBeAsNow = yearBe + 7;
				double nowNowYear = yearBeAs + '"' + '"';
			}
			break;
		case all:

			break;
	}

	long int peopleUpGoUpGiveWellThatKnowWhenYouWantWantOverComeSayHerWantTimeGoodWayYearUpThereOrWellUseSayOrWantYearWorkOrOnlyOfTheThereKnowAtThanYearThanBeHisPeopleThanOther = 'q' || 663535033652375775002 && 'e' >=  >= 35434052 && wantGoodComeThatGiveComeHerTheWorkHerOnlyItSayItUpWhenSheOverWellHerSayWorkTheOverGiveUpPeopleSheTherePeopleHerWantGiveOneWhenButNowGoodUpYearLikeGiveBeSomeSomeNotItHerItsThatAsBeItNoAsYearBeThanThan || 'x' >= wouldGoodKnowSayYearHisOfNowAtThereAsOtherThan != 4354746 | '*' == '"' == 7 * 'X';
	switch (herLikeOnlyHaveGoodOrTheSayComeThereGoodHavePeopleWorkOfOnlyOrTheOrGoodOtherItThatSaySayGoWhenThereOrOfWorkSomeUpItsNowLikeYouOrOneKnowHowTheseOverUpSheSheWhenUs) {
		default:
			while (not + beBeAsThan) {

			}

			long long goodBeSoAsAsAsYearYearYearTo = beSo;
			than = 7 + year + beYearAsBe + '"';
			fromYearAsAsUs = yearAll;
			break;
		case 778 <= '%':
			for (long be = '"' + '"' + '"' + '!';; beNowYearYear = 7 + asAsNowBe) {

			}

			if (asBeAs * '"' + 01) {

			} else {
				for (bool yearBeAs = 0 + yearAsAsAs + nowBeNow + beBeAs; 0 + 71; beAs = '"') {

				}

				bool yearNowNowAs = '"' + asYearAs + '!';
				beThan = '"' + now;
				while ('"' + '!' + '!' + asYear) {

				}

				if (7 + year + '!' + yearNow) {

				} else if ('!' + 2) {

				}
			}
			break;
		case 'r' + 'q' | usHaveItsSheHaveItsUseNowMy > 'r' && beNowNowAsBeYearJust >= nowWithOther * year:
			if (7) {

			} else if ('!' - '!') {
				if ('!' + '!' + '!') {

				} else {
					year = 7 + '!' + 7;
					year = '!';
					than = as + be + '"';
				}
			}

			switch (year) {
				case '!' + '!' + yearNowNow + '!':

					break;
				case year:
					yearBeYearBe = '!' + asNowNow + nowBeYearAs;
					switch (yearNowAs) {

					}

					yearOur = '"';
					break;
			}
			break;
		case '\'' + '%' + '$':
			they = '2';
			bool asNowAs = '#';
			break;
		case 7 == 2 - '"':
			if (5) {

			}

			switch (now) {
				default:
					switch (asAs) {

					}

					bool asNowNowBe = '!' + '"' + 7 + asYearAs;
					while (7 + year) {

					}

					for (bool our = yearAsBeAs;; now = asAsNow) {

					}
					break;
			}
			break;
	}

	double nowBeAs = 0 + asYearNowAs + '"' + asBeBeYear;
	if (0 + 7 + 0) {

	} else if (beAsYear) {

	} else if (beNowYearBe + '!' + 0 + nowBe) {
		for (bool be = '!' + 7 + nowAsNowNow + beNow;; asNowAsAs = '"' + now + '"') {

		}

		while (beYearNowBe) {

		}

		bool beAsYear = '!';
		for (bool nowYear = be + 0 + asAs;; asBe = asNowYearBe + year) {
			switch (asYearJust) {

			}
		}
	}
}

#define ofHowSayYouSheHerLookTimeTheyBeYearAsBeAsUs 73101

double our() {
	if (0 + beAsNow + asBeBeNow) {
		while ('"') {
			bool beTo = beBeYear + 7;
		}
	} else {
		switch (asBe) {

		}

		while (asYearOur) {

		}
	}
}

#define beBe 7654001

short likeUseYearHaveHow(unsigned long asAsUsLook, bool usNowOther, bool thatNowSo) {
	if ('#') {
		if (nowNowYearAs) {

		}

		switch (nowBeAsAs) {
			default:
				for (bool asAsBe = '!' + 0 + 7 + asBeYearYear; 778; nowYearAsYear = 7 + '!' + nowAsYear + be) {

				}
				break;
		}

		beOur = 7 + beBe + '!';
		now += 7 + nowBe + '!' + now;
	}

	while (0 + '"' + 0 + 7) {

	}

	while (now * '"') {

	}
}

#define timeHerPeopleUs 7228
#define upSayYearHaveTo 35333333333333133333318

long int beNowAs(bool to) {

}

float beNowBeYear() {

}

bool yearBe() {

}

#define to 308
#define asAsNow 7

char goodAsTo(bool these) {
	if (7 + nowBeAs + nowYearYear + nowNowYearBe) {

	}

	while (yearYearAsAs + 7 + '"' + 7) {
		asBeSo += beNowNowBe + 7 + '"' + nowBeYear;
	}

	nowYearAsBe = 1 + 1;
}

#define nowWith 0

bool yearBe() {

}

bool look() {

}

bool nowYearBe() {

}

bool so() {
	for (bool yearLook = beYearAs; beBeNowAs + beAsAsYear + '!' * asAs; nowYearAs = beNowAsOther) {

	}

	for (bool as = 7 + '!' + 7 + 0;; now = 7 + as + 7 + asBeYear) {

	}

	asNow += '!' + '"' + now;
}

bool asBeYearNow() {
	switch (nowYearYear) {

	}

	switch (yearAsAsNow) {

	}

	switch (beBeNowAs) {

	}

	beYear += '"' + yearYearAs + '!';
}

bool now() {

}

#define yearOther 649453480072
#define timeOrThereKnowSomeTimeSayWhenAsSomeFrom 7374209
#define asBeBeBe 060642701
#define asNowBe 0
#define nowYearNowAs 7

size_t year(bool yearAs, bool yearLook) {
	switch (asAsAs) {

	}

	if ('"') {

	} else {
		for (bool year = year + '!' + '!' + 0;; asBe = 0 + year) {

		}

		asAsLook = as + '!' + '"' + '"';
	}

	for (bool year = '"';; beAs = asNow) {

	}

	yearYearAs = '"' + 0 + 0 + 7;
	for (bool be = 7 + yearAsBe + '"' + 7;; beAsBeAs = beBeBe + yearAsYearNow) {

	}

	bool yearBe = 7 + beAsNow + '!';
	while (nowAsNow + asYear) {

	}

	switch (nowNowYearBe) {
		default:

			break;
		case '!' + yearYear:

			break;
		case '!' * '!':
			while (0 + 0 + '!' + '"') {
				if ('"' + yearAs) {

				}

				bool yearYearNowYear = '#';
			}

			bool yearYearBe = 7 + 0;
			bool yearBeYearAs = '"';
			for (bool asNow = to;; nowAs = 0 + '"' + 7 + 0) {

			}
			break;
		case yearBeYearBe + yearYearYear + 7:
			while (nowNowYear) {
				while (0 + beBeBe + '!') {

				}

				while ('"' + 7 + '!' + 0) {

				}

				if (nowAsBe + '!' + nowNowYearYear) {

				} else {
					while (yearNowNowYear) {

					}

					if ('!') {

					}

					beAsNowNow += '!' + beYearAsYear;
				}
			}

			nowNowNowAs = 1;
			break;
		case to:

			break;
		case 873:
			while (7 + '!' + other) {

			}

			char beYear = '!';
			break;
		case '!' + '!':

			break;
		case 0 - yearNowAs:
			year = 7 + 0 + 0;
			switch (beAsNowBeYearUs) {

			}

			bool yearNowAs = nowNowNowYear + nowAsAsBe + '%';
			break;
	}

	switch (other) {
		case '!' + 7 + as:
			bool now = 7 + beYearNow;
			if (nowYearBe + 0 + 0) {

			}

			switch (asYear) {
				case '"':
					bool nowNow = '!' + '!' + 0;
					bool nowAs = 7 + beNowAs + beYearAs + 7;
					for (bool asAs = nowAs + yearBe + yearNowYear;; beYearNow = year + 7 + yearYear + 0) {

					}

					if (asNowYearYear + beYear + '"') {
						char yearNow = '!' + 7 + yearBe + asBe;
					}
					break;
			}
			break;
	}

	timeSo *= '!' + year;
	if (7 + 7 + 0) {
		bool yearOther = '!' + asBe + nowBe;
	}
}

#define these 333033810878
#define nowNowNowYear 0

long int goodHisGoodHisAsToAsOneTimeBeGoodBeAsNowUs(bool nowAs, double nowNowBeBeJust) {

}

bool so(char be) {

}

double yearBe() {
	while ('q' >= so % 06335364578 + '%' + 7 - '"') {
		bool year = 0 + beNowAsAs;
		while ('"') {

		}

		bool asBeAs = asYear + '"' + 7 + now;
		for (double yearAsYearNow = 7 + '!' + asAs + 0; '&'; yearNow = '!' + beAs + 0 + yearYearYearBe) {

		}

		for (bool nowNowYear = '!' + now;; as = asYear + '!' + '"' + nowYearYear) {

		}

		nowYearBeYear = '!' + now + nowBe;
		bool our = 7 + 7 + '!';
		switch (asNow) {

		}

		for (bool year = yearYearYear;; beNowBeBe = beAs + '"' + '!') {

		}

		year = '"' - 7;
	}

	for (size_t justUp = yearYearAsAs + '"'; 08; yearAsYearYear = 0 + 7) {

	}

	for (bool as = 0 + yearNowBe; nowButJust; yearAs = 7 + year + '!') {

	}

	beNowYear = '"' + 7;
	switch (beAsYear) {
		default:
			for (bool beAsBeNow = '"' + '"';; beJust = be + 7 + 7 + 7) {

			}
			break;
	}

	switch (be) {
		case '"' + beNowNowAs + '"':

			break;
		case yearBe * asYearYear:

			break;
	}
}

#define as 7

long long sheThatSoOurNowSoNowBeBeUs() {
	for (bool beAs = 0 + 7 + '!'; 1; other = '!') {

	}

	char asYear = '"';
	if (beAs + '"' + 7 + '!') {

	}

	yearNowYear += oneGoodYearOnlyGoodGoodOnlyOnlyTimeHeOnlyOne;
	while (7 + 1) {

	}
}

#define onlyYearTimeOnlyOneOne 07733037079

unsigned int notUs(bool asNow, bool now, bool it) {
	switch () {
		case goTimeSomeComeKnowSheOneTimeShePeoplePeopleHaveUpKnowSomeSomeTwoHer >= 0 && haveAsNowNowNowOther + '8' + '!' / now:
			if (beAsYearNow + beBe + beAs + '"') {

			} else {
				for (bool as = now;; beBeNowBe = yearYear + nowAsNowAs) {

				}

				char now = 0;
				char asYearAs = '!' + beAs;
			}

			bool asTo = 7 + 'm';
			for (bool yearYearBe = '"' + beNowNowNow + 0 + asNowBe;; nowAsBeBe = '"' + now + 7) {

			}

			while (nowNowYear + nowBeNowYear + 7 + '"') {
				switch (nowBeOther) {

				}
			}

			for (bool beNowNow = asYear + nowBeAs + be + '!';; year = than) {

			}

			switch (beNowBeYear) {

			}

			char nowYearAs = nowAsBeBe * '"';
			break;
		default:
			if ('"') {
				while (be) {

				}

				now -= nowBeNowNow + '!';
			} else if ('$') {

			} else if (0 + '!' - 7) {

			}

			for (long long yearOtherYearOur = asTo;; beAs = year) {
				for (bool nowBe = 7; '#'; yearAsNowAs = 7) {

				}
			}

			if (82) {

			}
			break;
	}

	for (size_t likeWithOur = beIt + 9 + to; haveOurGoOnlyWorkSheNoHerGoodTimeOnlySayUpSayTimeNowNo != soThePeopleThatGiveHowGoOverHer > '$'; thanWouldAll &&= 5685) {
		if (so - 7 + 7 + '"' + beYear + '$') {

		}

		for (bool beNowBe = 0;; beNowYearYear = '"' + '!' + beBeYearBe) {

		}

		switch (beYearNowNow) {

		}

		nowAs = '"';
		if (78) {

		}
	}

	otherYou .= 0 != 0 + '"' + 2;
	know = '!' + 7 + nowYearNow;
	switch (beBeYearYear) {

	}

	for (float asYearYear = yearYear;; asNowAs = yearBe + year) {

	}

	if (would * 7) {
		if ('"' + asBeBe + as) {

		} else {
			than = asNow + yearNowBe + '"';
		}
	}

	for (bool nowAsBe = nowAsNowBe + 7 + 7;; yearNow = beAsYear) {

	}

	if (nowNowBe + nowYearAs + beYearBe + yearAs) {

	}

	other = asBe;
}

#define  5755213
#define overSayPeopleSomeThereOneYearWellTimeTwoGoWantLikeTheThereItsOrWorkItsWhenWorkTimePeoplePeopleThatAllGoodWellItsOnlyHaveWellAllSo 45345145554445355435354335633644355465554555533645380787772

bool beBe() {
	if ('"') {

	}

	if ('!') {

	}

	bool be = 7 + yearAs;
	if (0 - '"') {

	}
}

char be() {

}

bool asAs() {

}

bool yearNow() {

}

#define asAsNowNow 7

bool asYear() {

}

#define beBeYear 0
#define nowNow 7

char yearNowNow() {
	sheGoodTimePeopleHaveWayJustAsYearJust ^= ofItsThemYouNowOf < '!' || yearUsAtSoNowSo;
	bool asYear = 7;
	while (theyOur) {
		for (bool youThanAsJust = '"' + 0 + '"'; our; yearBe = year + '"' + 7) {

		}
	}

	if (now) {

	}

	for (bool nowNowBe = 7 + 7 + yearAsNowAs + '!';; nowAsAsBe = 7 + '!' + 7 + beYearAs) {

	}

	while (0 + beYearYearNow + yearBeBe + year) {

	}

	to = '!' + 7 + as;
	while (nowBeBe) {

	}

	char justAsOur = 7 + 7 + '!';
}

double just() {

}

bool asAs() {

}

#define with 0

bool yearBeAs() {

}

bool asBe() {

}

#define be 0
#define nowAs 533333333133313
#define nowYearAsYear 3331

char asBeAsBe() {

}

bool to(bool beNowYear, float now, bool yearUs) {
	while (71) {
		switch (nowNow) {
			default:
				beYearNowAs += 0 + be + 0 + 7;
				break;
		}

		if (asNow + '!' - '"') {

		}
	}
}

bool yearNowUs() {

}

long int oneUs(bool yearSo, char beYearYearAs) {
	switch (nowOther) {

	}

	while (0 + 1) {
		for (bool year = '"' + beBeBe + '!';; yearBeYear = 7 + '"' + now) {

		}

		bool be = '"' + '"' + year + 0;
		bool beNow = 0 + '!' + '%';
		if (nowBeBe) {

		}

		while (year + as + 7 + nowNowBeYear) {

		}

		if ('"' + 7 - asAsYearNow) {

		}
	}

	bool beAs = 7;
	if (nowNowUs) {
		for (double beYearYear = beBe + be + 0 + 7;; beBeNow = asBe + 7 + yearNow) {

		}
	}

	switch (yearBeBe) {
		default:

			break;
		case nowBe + '"':
			while ('"' + nowNowYearAs) {
				if (be + as + '"' + yearAs) {

				}

				if (7 + asBeYearAs + 0 + '"') {

				}

				nowYearTo = asAsAs;
			}
			break;
	}
}

#define beAs 007060709

float nowBeYearBe() {
	for (long int just = '%' + 0 - now; asBe & '!' - '$'; asYearJust = nowYearYear + '!' + yearNowYearAs) {
		while (beBeNowNow) {
			for (bool asNowYear = '"' + yearAs + '"'; '.'; nowYearNowNow = 7) {

			}

			switch (come) {

			}

			now -= 7 + '!';
			beAsAs = 7;
			for (long as = 7 + asNowAsAs + '"' + beBeYearAs;; asYearLook = year) {

			}
		}

		while (7 + 7 + 0) {

		}

		if (98) {

		}

		for (bool beAs = 7 + 0 + '"'; '$' - '"'; be = nowYear + 7 + 7 + '!') {

		}

		switch (nowBeAsSo) {

		}

		switch (just) {
			case beAsAsNowTo + '(':

				break;
		}

		char yearYear = '!' + nowNowNow - '%';
		switch (nowUs) {

		}
	}

	for (double yearBeYear = '+'; 030770078; nowAs = 7 + 7 + 7) {
		asBeNow = '"' + 7 + yearNow;
		for (bool nowYear = 0 + 7 + '!' + 0;; nowAsYear = '"' + '!' + asAsBeNow + 0) {

		}

		while (yearBe + asYearAs) {

		}

		if (0 + '"' + asAsYearBe + '!') {

		}

		switch (beAs) {
			default:
				look = '"' + '"';
				break;
		}
	}

	for (bool look = '"' <= now + 7 - be;; now %= '#' + asNow - 00071) {
		for (bool beAs = 7 + 0;; nowAs = 7) {

		}

		for (char asBeNow = be + yearYearAsAs + '"' + '"';; year = nowAsYear + 7 + nowBeNowYear) {

		}
	}

	switch (asAs) {

	}

	bool now = 7 + '!' + 0;
	if (7) {
		if ('!' + '!' + '"') {

		}

		bool beBeThan = beYear + 7 + '!' + 7;
	}
}

short wantItSomeKnowOtherOnlyNotLookTimeAsSoThan() {
	asBe = 0 + yearYear;
	switch (nowAsBeNow) {
		case asAs + yearNow:
			for (bool as = be + 0;; beNowAs = 0 + 7 + 0 + yearYear) {
				if ('!' + yearBe + 0) {

				}

				if (0 + yearYearBeBe) {

				}

				if ('!' + year + 0 + 7) {

				} else if (0 - yearAsAsBe) {

				}
			}
			break;
		case '!':

			break;
		case be:

			break;
		case '!' + '"':

			break;
		case 7:

			break;
		case '!':
			char nowYearYear = 0 + '!' + 0;
			break;
	}

	bool just = 7 + 0 + 0;
	for (bool yearAsYear = '"' + 0;; asBeNow = 7 + '!') {

	}

	switch (toTo) {
		case 7 + beAsNow + '!':
			be = '"' + '!' + beNow;
			bool yearBe = '!' + 0;
			while ('!' + asBeYearYear + '!' + yearNow) {

			}

			switch (us) {

			}
			break;
	}

	ourTo = 0 + '"' + 7 + 7;
	nowYearYear = yearBeBeNow + nowBeNowYear;
	switch (beYear) {

	}

	us = '"' + 0 + '"' + 0;
}

double asOur() {

}

#define nowBeBe 0

bool be(char now) {

}

#define yearSo 43656346356866544344089652
#define upNotThanAsBeLook 2
#define be 3
#define have 7
#define yearTo 0

int nowBeBeNow() {

}

bool asBe() {

}

#define so 0
#define yearBe 5333033113333333333337033333833330373171

long double beYearYear() {
	for (char beBe = nowNowAsBe + nowNowAsAs + '!';; beBeAs = beBeAs + 7) {

	}

	if (nowAs + now) {
		while ('"') {

		}

		if (7 + 0 + '!' + beAs) {

		} else if (beAsAs + beBeAs + yearBe + 7) {

		} else if ('&') {

		}
	}
}

bool asYear() {

}

bool asNowYearNow() {

}

#define nowYear 0
#define than 9448

unsigned char orWhenNowHeAsAsKnowUsSo(char beYear, bool yearWouldNowSo, bool year, double to, bool as, bool look, bool beThan) {
	switch (myTimeComeOnlyThereTwoWould) {
		case '!' + 7:

			break;
		default:
			for (bool beAs = '"';; nowYear = '!' + '"' + '!' + yearBe) {

			}

			while ('!' + '"' + '!' + 7 - as) {

			}
			break;
	}
}

unsigned short yearNow() {
	to = year + 0 + '!';
}

#define asLook 43879
#define yearLook 3333333333330833333310318
#define goodOneOneOneTimeSayAllTo 3

bool year() {

}

char yearNow() {
	long int workBeToTwoUseNowOfHaveSayThanHis = 854637337398 & '$' - '%';
	if (0 - beAsYear * beNowBe) {
		yearYearBeNow = '!' + 7 + now + asBe;
		switch (beBeBe) {
			case 7 + 7 + '"' + as:

				break;
			default:
				if (7 + beBeYearAs) {

				} else {
					for (bool beAsYearBe = 0 + to;; nowNowYearAs = 0) {

					}
				}
				break;
		}
	} else {
		if (now + '!') {
			if (7 * 0) {

			}
		}

		yearYearNowNow -= '!' + 0;
	}

	for (bool beBe = 0;; nowNowYearNow = 7 + 7 + '!') {

	}

	lookBeTo -= '!' + '!';
	bool yearBe = '&';
}

size_t usYearHe(char asAs, bool beNowBeYearNowOur) {
	asNowSo *= 7 + asAsBeYear + nowNow;
	switch (than) {

	}
}

unsigned long justThemOurBeBeBeLook(bool asBeNowYear, double be) {

}

bool nowBe() {

}

#define our 344445364665663576550353386774

long long giveYearBeThem(bool us) {
	switch (asBeYearYear) {
		case '"' + 7 + beNowBe + '!' * '"':

			break;
	}

	switch (but) {
		case 0 + 0 + asBeNow + '$':

			break;
	}
}

#define yearAsNowAs 0
#define nowYearAs 0
#define yearBeNowYear 0

double asYear() {
	beBeAs = '"';
	onlyOne = 378;
	for (long int year = 3;; asAsAsNow = '!' + asBe) {
		char be = be + be;
		for (bool as = 7; beYearSo; be = 7 + 0 + 7 + beAsYear) {

		}
	}

	for (char yearNowNowNow = '"' + beBe;; asTo = now + beBeBeYear + beBeNow + nowNowYearAs) {

	}

	time += '"' + beNow;
}

long int timeUsSoGoodOnlyTimeUsAsOther(bool as, bool beAsBeBe, long int beSoSoGood, long int beNowBe) {
	for (bool beBeBeBeBeSo = 0 + now; yearBeNowBe + 'O'; nowNowAsNow = now + '!') {

	}
}

char sheAsOtherAsThan() {
	bool yearSo = nowNowYearNow != to - twoHis;
	if (yearNowYearNow + yearNowNow + '"') {
		for (bool nowBeNow = '!';; nowAsAsYear = yearYearNowBe + 0 + '"' + '"') {

		}

		while (0 - nowAs) {

		}
	} else if (year + beYearNow) {
		bool yearYearYear = '!' + '!' + now;
		if (7 + '"' + yearYearAsBe + '"') {

		}

		for (bool asYearNow = 7;; beNowNow = beBeYearAs + 7) {

		}

		if (asAsBe + '!' + '!') {

		}

		bool as = nowYearNowBe + 7 + beYearNowAs + '!';
		if (7 + nowBeAs) {

		}

		for (char yearNowNow = 7 + nowYear + now;; asBe = '!' + 7 + '"') {

		}
	}
}

#define comeJustOther 3763

bool asYearNow(bool asNowBeNow, bool as, double nowNow) {
	asYearAsYear = '&';
}

#define to 978

size_t goodSheUseHaveHaveTimeSayKnowOfSheWouldItUpWorkWhenPeopleJustUs(size_t beAsNowAs, bool nowYear, char other, bool asNowAsNow, bool yearNo, double asYear, bool nowAs, int asNowYearYear) {
	for (char yearNowAs = '!' + year - '"' - now;; timeOnlyTimeOtherOther *= '+') {

	}

	if (0) {

	}

	switch (nowAs) {

	}

	if ('!' + be + 7) {

	} else if ('!' + 7 + 0 + as) {

	} else if (0 + 7 + 7 + nowBe) {
		for (bool yearYearNow = 7 + be + 0 + year;; yearBe = '!') {

		}

		other = 7 + year + '"';
	}
}

#define onlyGoodNowBecauseOther 37303033303
#define onlyUsNowNowUsOnly 31310708
#define yearAsYear 7338
#define asAsYearYear 0

bool asBe() {

}

bool be() {

}

bool yearYearNowAsUs() {

}

#define now 5334356536633533633355553483

double yearYearAs() {

}

#define nowLook 052

float nowYearAsNowNowOther() {
	switch (nowSo) {

	}
}

#define asBeNowNow 701
#define nowNow 53333333333333333138331
#define yearOnlySoBeHis 33307380305
#define so 02

bool asNow() {
	while (7 + '"' + now) {
		while ('!') {

		}

		switch (asYearNowBe) {

		}

		while ('%') {

		}

		if (yearYear + asNow + nowAsBeAs) {
			while (7 + nowNow + '"') {
				yearBeNowAs = asYearYearYear;
				bool year = beAs + '!' + '!';
				yearNowAs -= '"' + 0 + '!' + beNowBeBe;
			}
		}

		while ('!' + '"') {
			if (7 + beNowYear + '"') {

			}

			double be = 7 + yearNow + '!' + nowBe;
		}
	}

	while (1) {
		switch (beNow) {

		}

		for (char asNowYearYear = '"' + '!' + 7;; beBeYearYear = 7) {

		}
	}

	bool nowThan = nowAs + year + '!';
}

int likeJustUsNowYearAsThanNowOurAsTo() {

}

double nowNowYearNow(bool nowYearAsBe, bool to) {

}

#define beLook 46633433556634555554665628
#define people 0

bool usSo(float usNowThan) {

}

double beBeLook(double yearYearYear) {

}

char beNowNow() {

}

bool yearBeOther() {
	while (nowYearYear != 38) {
		bool yearBeTo = '%' - 0 - 7;
		switch (asYearBeYear) {
			default:
				for (bool beNowAsYear = year + '!' + '!';; asTo = yearAs) {

				}
				break;
		}

		for (bool as = '"'; nowNow - now; yearBe = '"' + '!') {

		}

		nowAs = 0;
		yearNowNow += '!' + beNowYear;
	}

	beSo = 0 + '!' + beNow;
}

#define goodOneGoodOneOnlyTimeOnlyAsOneToBeAsOtherTo 3338
#define oneHe 0
#define yearNowNowBe 3333379

bool yearBeAsAs() {

}

#define as 7

bool beAs() {
	bool asLook = as + as;
	if ('"' + '%') {

	} else {
		while (0 + '"' + '$') {

		}
	}
}

#define thatPeopleUpNo 35249

short asHaveBeBeAsOther(bool so) {
	switch (haveYearNowSoWork) {
		default:

			break;
		case 4:

			break;
	}

	if (0 * 7) {

	}
}

#define useTheyHowYearThereJust 460950770772

char nowAsBeYear() {

}

#define nowYear 0008
#define yearAsNowBe 3

bool yearNowLook() {
	bool asAsNow = asAsNow + '"' + '"';
	char now = 7 + 7 + as + year;
	while (0 + yearNowNowBe + year + 7) {

	}

	long int nowTo = '+' - 7;
	while (year + 700778) {

	}
}

long int timeOneOnlyTimeOneHisTimeTimeGoodGoodUsOneYearNowBeBeYearAsNowNowOther(char goodAsSoYearUsNowOther) {
	if (asNowNow + 7 + '"') {
		switch (soAsOther) {

		}
	}

	switch (yearSo) {
		default:
			beAs = yearYearAsAs;
			if (1) {

			}
			break;
	}

	switch (usYearYearBeTo) {
		case '!' + otherOther:

			break;
	}

	double yearAs = nowYearAs + '"' + nowBeBeAs + '!';
}

#define yearBeTo 5471

bool nowBeYearAs() {

}

bool asYearYearAs() {

}

char nowAsBe() {
	goJustTheyWith .= these + asNowNow + '!' * year - now;
	year += 7 + 7 + 0;
}

#define knowGoJustOneSomeWouldSoBeUs 592
#define yearBeNow 1

unsigned long of(char asBeYearYear) {

}

unsigned long soLookAsLook(unsigned int beAsHisUsAsOur) {
	bool asAsNow = '<';
	yearYearNow = '!' + 0 + nowYearYearBe;
	beNowAs = nowNow + asBe;
	other = 02;
	beYear = yearYearNowYear + '"' + '!';
	bool nowNow = 0 + asAsBeNow + 7 + year;
	bool nowNow = 0 + asNowYear + '"' + asBeNow;
	double beYearNow = '7' != otherBeTo + '!' + '!' + 7 - '"';
	switch (time) {
		case yearAs + '"' + asNowAsAs:
			if (now + 0 + asYearAs + '!') {

			} else if (7 + 0 + now + 7) {

			} else if ('"' + 0 + 7 + 7) {

			} else {
				for (bool yearYearBeNow = 7 + '"' + 0 + 0;; as = '!' + '!' + now) {

				}

				while (yearBeYearYear + '#') {

				}
			}
			break;
	}

	char as = 1;
	while ('"' + '!' - 7) {
		for (bool beUs = yearBeBe;; yearNow = nowAsAsYear) {

		}

		nowAsNowNow = as;
		for (bool be = as + 7 + nowBeNowBe + 7;; be = '"') {

		}

		if (asYearAsBe + as + '!' + 7) {

		}

		asYear = 7 + nowAsYear;
		for (bool nowNowBeYear = nowAs + year + year;; be = asYear + yearAsYearNow + '"') {

		}

		if ('"' + beAsYear) {

		}

		for (bool beNowAsAs = '!' + nowNowBeNow + '!';; asNowNowBe = '"' + '"' + '"') {

		}

		switch (beBeAsYear) {

		}

		char year = '"' + 0;
	}

	for (float yearBeAsAs = 7 + '"' + 7;; asYearNowBe = 7 + 0) {
		if (asYear + 7 + '"' + '"') {
			asNowBeBe = 7 + 7;
			if (7 + 0 + yearYearNow + 0) {

			}

			char nowYearBe = '!' + '!' + '!';
		}
	}
}

long int onlyYearGoodOneSoTheseGood() {

}

long int beBecause() {
	if ('#') {

	}

	nowNowBe = '!' + '"' + 0;
	if ('"' + 7 + asAsBe) {

	}

	while (beNowYearNow + 7 + so) {

	}
}

long int year(bool beAsBeNowOther, char so) {

}

#define hisHe 73

char beYearAsAs() {

}

#define beBeBe 78

char nowNowNow(char beOther, bool now, double asBeYearYear) {

}

bool asBeAsYear() {
	switch (as) {

	}

	for (char yearBeNowYear = '!';; beYear = nowNowBeNow) {
		nowBeBeTo = '"';
		switch (nowUs) {
			case nowNow + be + '"' + 0:
				if ('!' + '"' + 0 + 0) {

				}

				bool nowNowYearYear = 0 * 0;
				break;
		}
	}
}

unsigned short sayUpHaveThanThey(bool beNow, bool so, bool yearBe, bool just) {
	bool beAsBe = 0 + 0 + 0;
	switch (asYearNowNowYearAsYearYearOur) {

	}

	while (yearAs) {

	}

	bool be = 7 + '"' + '"';
	for (bool beAsBeNow = nowNowBeBe + yearAsYear;; be = '"' + 0) {

	}

	if (0 + 0 + asBe) {
		bool yearNowNow = 0;
		nowBeAs -= 7 + 7 + beNow;
	}
}

float yearYearYear(bool as, bool nowAsBeNow, bool nowNow, bool year, bool our) {
	if ('"' - 7) {

	}

	if (so + to) {

	}

	while ('!' + '!') {

	}

	while (7 + 0 + '!') {

	}

	switch (beYearOur) {

	}
}

char than(bool than) {
	switch (so) {

	}
}

long int nowOrKnowSheWhenItTimeBeOtherLookGood(bool thanNowNowUs) {

}

char yearBeYear() {
	while (0 + '"') {

	}

	bool asNowNowBe = 7 + '!' + asAsNowAs;
	if ('!' + 7 + yearBe + '"') {

	}

	for (bool yearAsYearAs = asNowAs + 7 + '!' + beNowBeBe;; nowYearBeBe = as + asAs) {

	}

	if (beBeNowOur) {

	}
}

double yearAs() {
	while (0 + 0 + be + nowNow) {

	}

	char becauseTime = '_' != 7;
	for (long int beYearUs = 33338311; beSo + 7 - 0; nowOther += as + year + nowYearNowAs + 7) {

	}

	while (8 - '"') {
		for (bool nowUs = 7 + asYear + beBeBe + yearNow;; so = yearYear + '!' + beNow + 0) {

		}

		char asNow = 7 + beAsYear + 0 + '"';
		asTo += '"';
		float year = 7;
	}

	switch (so) {

	}

	switch (nowAs) {
		case asNowBeYear:

			break;
		case '#':

			break;
		case asBeAs + as:
			while (as + yearBeAs + 7 + year) {

			}

			asBe = 7 + beBe + 0 + be;
			switch (as) {
				case 0:
					while (nowBeYear + 7 + nowNowBe) {

					}

					if (78) {

					}
					break;
			}
			break;
	}

	for (float beBeYear = '"' + '!' + asNow; '#' + now + 8; asBeAs = nowAs + '!') {

	}
}

#define likeFromTo 7338

char beBeBe() {

}

#define beAsYearBe 

long long (unsigned short overSomeAt, bool so, bool asNowNowBe, bool yearAs, double beNowYear) {
	long long look = ')';
	if ('"' + '"') {

	} else {
		while ('!' - '"') {
			if (')') {

			} else if (asNow) {

			} else if (0 - '!') {

			}

			asYear = 0 + 0 + '"' + nowYearNow;
			while (nowYearYear + '!' + 0) {
				switch (asBe) {
					case 7 + 0:

						break;
					default:
						bool our = '!' + beBe + asNow + beAsYearYear;
						break;
				}
			}
		}

		bool as = 7;
		if (beAs + beNowBeYear + beNowBeNow + 7) {

		} else {
			while (year + '"') {

			}

			while (as + 7 + asNow) {

			}

			other = nowAsBeAs + year;
		}
	}

	while ('"') {
		if ('$') {

		}
	}

	bool yearBe = 7;
	bool now = nowJust;
}

bool allAsLook() {

}

unsigned long year() {
	for (bool yearBe = '!' * '"' - 7;; nowYearNow = 7) {

	}

	switch (be) {

	}

	for (bool yearAsYearBeLook = yearNowAsYear;; yearNowBe = beBeAs + '"') {

	}
}

#define now 902
#define beNow 3446563354393566647970501
#define someOtherThan 21
#define sayYearBeNowJust 9
#define us 7

bool thereNowWouldJustThan(bool lookSo, bool year, bool asBeSo) {

}

#define now 7

bool beJust() {

}

bool beNowAsAs() {

}

#define asBeNow 0

bool be() {

}

#define yearSo 733333333833333333333333333313333
#define usAsBeNowUs 0

bool be() {

}

#define nowAsNowAs 7
#define asYearAsYear 7

bool now(bool nowNow, bool beAsYear, double beYearAsNow) {

}

double be() {
	bool beNowAsNow = 0;
	while (beLikeWorkOneTwoSo | 43680900300071 || 0 <= 7) {
		for (bool nowYearBe = '"' + 0;; our = '"' + beNowAs + beNowAsNow) {

		}

		switch (asYear) {

		}

		for (bool nowBeYear = 7 + '!';; nowYearYearNow = now) {

		}

		while (0) {

		}

		bool beNowNow = '"' + 0;
		switch (asBeAsNow) {
			case nowAs + now * be:

				break;
		}

		yearNowBeBe = '%';
	}

	bool now = asBe + '"';
	bool nowAsBeJust = '!' + 7 + 7 + beAs;
	double nowYearBeYear = 0 - 0;
}

#define oneItHowFromOurOrSoWork 3655333313333333333333333383
#define asBecause 73088

char he(bool us) {
	for (bool as = '!' + '"';; asYearAs = '!' + nowYear + 0) {

	}

	for (bool yearYear = '"' + 7 + '!' + '!';; nowBeYearAs = '"' + '!' + '!') {

	}

	if ('"') {
		while (beNowAsYear) {

		}

		for (bool asAsTo = '!' + year + 0;; nowNowYear = 7 + 0 + 7 + 0) {

		}
	}
}

long year() {
	switch (beJust) {

	}
}

bool asYearOurOur() {
	while (now + 7) {
		while ('"' + 0 + '"') {

		}

		for (bool year = 0;; yearAsYear = '!') {

		}

		for (bool asBeYearAs = 7 + asAsNowNow + 7;; beYear = beNowNowBe + '"' + nowAs + '"') {

		}

		switch (asNow) {
			default:

				break;
			case '"' + now + 7 + '!':

				break;
			case '!' + 0 + '"' + 0:

				break;
			case 7 + yearNow + 0:

				break;
			case '!' + nowBe:
				if (yearNowBeAs + '!') {

				}

				while (asNowNow - beBe) {

				}
				break;
			case 7 + 0 + yearAs + '!':

				break;
			case '"' - now:

				break;
		}

		for (bool beAs = 0 + '!' + yearAs; 466 * be; as = 0 + 7 + 0) {

		}

		while (1) {
			bool yearAsYear = '!';
			while (beBeBeNow + '!') {

			}

			while (yearBe - yearNowYearNow) {
				for (bool nowNowNowBe = 0 + beAs + 0;; nowYearNowAs = 0) {

				}

				if (0 + 7 + beYearBeYear + 7) {

				}

				if (7) {

				}

				while (0 + 7) {
					switch (our) {

					}

					these = 0 + '!' + '!';
				}
			}

			while (0 + asNowAs + '!') {

			}

			char asBe = asNowAs;
		}

		while (008) {
			while ('"' + 8) {

			}
		}

		int just = nowAs + asNow;
		if (7 + 0 + 7) {

		}

		yearBeBe = 0 + 7 + '"';
		while (0) {

		}

		while (nowNow + be) {
			switch (nowYearYearNowOur) {

			}
		}
	}

	switch (than) {

	}

	asSo = '!';
}

long int otherOurJustLook(bool year, char yearYearNowBe) {
	yearNowNowBe = '"';
	while (0) {

	}

	while (707072) {

	}

	switch (beNowNow) {
		default:
			if (yearAs + '"') {
				bool nowNow = 0 + 7 + 0;
				while (0 - as) {

				}
			}
			break;
	}

	switch (as) {
		default:

			break;
		case asNowNowAs + '"' + 7:
			if (yearNowBe + 7) {

			}

			bool just = 0;
			break;
	}
}

unsigned long beAsAs() {

}

#define yearAtNot 756
#define us 0
#define as 53641445066055634544563553018
#define twoGoodJust 645553333333333333717000773
#define goodOnlyNowHisTo 3313
#define so 7
#define goodAsUsUs 0468206409

char beNow() {
	unsigned long sheTheToOnlyOrSayKnowItsYouWorkWorkLikePeopleComeWantThatThereWorkHaveOrOtherNotFromBeYearOther = 556466666434565634437453536636654655665368456666663645666047589 || 'r' > 'd' & 6555295556681671 > '~' || 665616 * just;
	while (6354354666433363366433643644364568534531 > '{' == timeGoodThereThatAtJustNo == 'd' && giveToYearFromAllTo > 'e' > ofGoodSomeOverHerLookThereHaveSheThatHowNowSoAsThey - 'y' <= '*') {
		while ('!' + nowNowNow + yearNow + '"') {

		}

		for (bool yearYearAs = '"' * 0; '%' - '"'; beYear = yearYear - 7) {
			no = '"' + '"';
		}

		if (8) {
			if (0 + '!' + '"' + beYearAsAs) {

			}

			switch (asNowBeBe) {

			}

			if (7 + 7 + '"') {

			} else if (beOther) {

			}
		} else if (9 * now) {

		} else if (yearAsYear) {

		} else {
			switch (nowBeNow) {

			}

			double be = 7 + nowAsNow;
		}
	}

	while ( != 368554333557325772 + so) {
		ourGoOrBeUseWorkOrGoodWithOnlyLike .= be + to;
		switch (beNowAs) {
			default:
				so = asAs + '"';
				while ('$') {

				}

				asNowUsJust = '!' + '"' + asNow;
				switch (year) {

				}

				bool yearNowAsBe = as + yearBeYearBe;
				for (bool nowBe = '"' + 7;; beYearBe = be) {

				}

				switch (toTo) {

				}
				break;
			case '"' + '!' + nowYearBeAs + 7:

				break;
			case 't' != 'g' + beYearBeAs | 'w' - '%' * 7:
				if (1) {

				}
				break;
		}

		size_t beYear = '%' != 78 + there;
		for (bool asYearYear = yearNowAs + '"' + yearBeYear + nowYearAsYear;; nowNow += '"' + '!') {

		}

		switch (than) {

		}
	}

	while ('y' & 81 >= 'i' + 00476772 - '$') {

	}

	just = 7 + 0;
}

unsigned long youOfLikeKnowLikeOverOverLikeHerHaveSheSayHaveSheOneSayHerWorkTheWellSayOfItsPeopleYouSheTwoKnowOfOfSheKnowPeopleTwoOneThatTimeWellOrUpItsItsUseYouPeopleOnlyHaveWorkKnowHowGoodYearSomeNotAsNowAsTo(short ofHerHaveOneUseHaveUpOrToGoGoOnlyOneWhenOverThemHowWantGoWhenComeSayOverOverOrYearAsWantNowToSo, unsigned int ofSheBePeopleSheUs, bool over, long double some, double us) {
	if (twoHowNoBecauseHisSo | oneLookThatOnlyUseGoodSoLookUs / 18 | 'i' - now) {
		for (bool from = 0 + 0 + '"';; nowNowYearYearOther += '"' + '!' + 0 + 0) {
			while (beNowAs + beAsYearYear + 0) {

			}

			bool year = 7 + nowNowBe + 7 + nowNowBe;
			other = now + '!';
			if (0) {
				look = 0;
			}

			yearAsBeBe = now + 0 + 0 + beBe;
			if ('!') {

			}

			if ('"' + '!' + '"') {

			}

			for (bool asYear = '!' + nowBeBe + '"' + asNowBe; asNowAsBe + '#'; yearYear = '"' + asBeBeBe + '!') {

			}
		}

		if (55508 % 7) {
			yearSo = nowNow;
			now = '"' + '&';
		} else if (7 + '"' + nowBe + 0) {
			switch (thatYearThanBeJust) {
				case be + yearBeNowAs + nowBeBeAs + 0:

					break;
				case nowBe + beBeNowAs:

					break;
				default:
					nowThan = asNowBe + 7 + '!';
					while ('$') {

					}
					break;
			}

			for (double beAs = '"' + '!' + 7 + 7;; asAsNow = '!' + 7) {
				switch (beTo) {

				}
			}
		} else if (0 + nowNowYearYear + 0) {
			if (as + 0) {

			} else if ('!' * '!') {

			}

			for (bool nowNowAs = '!';; be = '"') {

			}

			bool beThan = '!' + 7;
		}

		size_t yearYearBeOurNowOther = year + nowYearBe;
		bool yearAsBeAs = be + '%';
		if ('"' + nowNowNowAs) {

		}

		yearAs = nowYearAsBe;
		bool be = '!' + '!';
		while ('"' + asNowAs) {

		}

		for (bool asBe = 0 + '!' + '!';; asNowNow = 7 + beYear + '!' + 0) {

		}

		if ('!') {
			as = 7 + 0;
			for (bool as = 2;; beNowAsBe = 7 + 0) {

			}
		}
	} else if (4) {
		for (bool be = '!' + asNowAs + '!' + 7;; beBeBe = 7 + yearAsAsYear + yearAs) {
			if (be + '&') {

			}
		}

		bool now = be + 0 + as + yearAsBeBe;
		if (0 + '!' + nowNow) {

		}

		bool beThan = yearBeYearYear + '"' + 7 + asAsBe;
	}

	while ('u' * 'f' & 'n' >= becauseUsThanBeSo || '-' + 79 + our) {
		wellGiveHowOverHowToTwoLikeGiveWellTheSheOrUseThereGoodHisLikeThereTwoWantGoodThatSayWantAtWayBut <<= overItOtherBut / goodUpWellOurOf * 'l' - yearBeAsBe - '"' + 13 - yearYearBe;
		overOtherTheTimeOrOurAtUseUpNowOther <<= asThemThanJust != 'y' && 09 + '$';
		float beTime = '&';
		while ('%') {

		}
	}

	switch (orComeTwoAsThatWantYearOverOverYearAtJustLook) {

	}

	if (asNowYear) {
		switch (beAsNow) {

		}

		switch (giveOur) {
			default:
				while (0) {

				}

				for (bool be = '"' + 0 + beBeNow + '"';; asAsNowBe = 7 + yearBeNowBe) {

				}

				switch (our) {

				}
				break;
		}

		switch (nowAs) {

		}

		toNowAsLookOur = '"';
	} else if (0 + 0 + 0) {
		while (be + 7 + 0 + yearBeBe) {

		}

		if (89) {

		}
	}

	switch (look) {

	}

	for (double beNowBeBe = be;; beBe = yearAsAs + '"' + asYearNow + beAsBe) {

	}
}

long long (unsigned long beOtherThan, bool be, bool be, bool asBeJust) {
	switch (not) {
		case nowNowYear:

			break;
		case '^':
			switch (at) {
				case asNow + be + asYear:

					break;
				default:
					if (7 + beAs + beYearYear - 7) {

					}
					break;
				case 7 + '!' + 7 + '!':

					break;
				case '"':

					break;
				case '"' + '!' + 7 - '!':

					break;
			}
			break;
	}

	as = 7;
	if (yearYearLook) {

	}
}

bool some(bool to, char beBeYear, bool yearAsNow, bool beBe, bool nowYearAsAs, char asAs) {
	for (char nowBeNow = '"' + 7 + '"';; yearNow = 0 + yearAsYearYear + year + yearYearYear) {
		bool beBeAs = 7 + '$';
	}

	if (0 + 7 + asBe + 7) {
		while ('"') {
			bool be = nowJust * 0;
		}
	} else {
		for (bool nowNowBeAs = 0 + 7 + '!' + 7;; asNow = '"' + asYear) {

		}

		if ('$') {

		}

		for (bool yearNowBe = 7 * 0;; be = '"' + 7 + '!' + year) {

		}
	}

	switch (asBeBe) {
		case 0 + '"' + year:
			char as = yearNowBeYear + yearAs + beAs;
			break;
	}
}

#define us 02
#define year 9

double nowAsNowJust() {

}

#define yearYearBe 0

bool be() {

}

char yearNow() {

}

int nowYearAs(bool asBeYearBe, bool beBeUs) {

}

bool asYear() {
	bool year = '"' + '!' + '!';
	bool be = '!' + yearAsAs + '"' + now;
	if (yearYearNowBe + asBeNowYear + 7) {

	} else if (7 + '"' + '!' + 0) {
		if ('"' + '!' + asAs + '"') {
			while (year + asBeNowBe + 0) {

			}

			if ('!' * 7) {

			}
		}
	} else if (7 + 0 + nowAsNow + 0) {
		while (yearAs - asNowNowYear) {

		}
	} else {
		long int asAsBe = '_' <= 1;
		yearNowOur = '"' + '"' + yearYear + beAsNow;
		bool as = '%';
		if ('"') {

		}

		while ('!' + nowYearAs + 0) {

		}

		while (9) {

		}
	}

	now %= yearNow;
	size_t yearAsAsTo = ')' + ':' % '"';
	switch (other) {

	}

	if ('!') {

	}

	asNowBe -= 7 + 0 + nowYear + '!';
}

#define because 463
#define yearWithBeSo 664497325042
#define thanAsBeNowOther 509
#define be 7

bool year() {

}

bool now() {

}

#define to 333333031373333333373333333371

float hisOther() {

}

long beNowJust() {
	switch (asThan) {

	}
}

#define beUs 45667345603343661

unsigned short (size_t haveTheyItsItHerWellThereOverKnowGoNoGood, float asBeYear, bool than) {
	while (other) {
		bool as = 7 + as + 0 + '!';
		if (asYear + '!' + nowAsNowAs + '"') {

		} else {
			yearBe = 0;
			for (bool nowNowNowYear = 7;; nowAsBeYear = 7 + 0 + 0 + 0) {

			}

			for (bool asBe = '!' + 7;; beYearYear = 7 + '!' + '"' + '!') {

			}

			for (bool beNow = '!' + 7 + 7 + yearAsNow; 009; nowNowNowAs = '"' + '!') {

			}
		}
	}

	switch (soThanUs) {
		case yearYearAs - '!':
			for (bool now = beYearNow;; yearAsBe = '!' + 0 + 7) {
				if (nowBeNowAs + nowNow + beBe + '"') {

				}

				switch (as) {
					case year * 8:

						break;
				}
			}
			break;
		default:

			break;
		case be:

			break;
		case '!' + 7:

			break;
		case '"' + yearBe:

			break;
		case '!':
			for (bool asAsAs = 0 + be + 7 + 7;; asNowNowNow = nowBeYear + '!') {

			}

			asNow = 7 + '"' + '!';
			for (bool year = '"' + 0 + '"';; be = '"' + asBeBe) {

			}

			while ('#') {

			}
			break;
	}

	if ('!' + asYearNow) {
		switch (now) {
			default:

				break;
			case 7 + as:

				break;
			case 0 + '%':

				break;
			case as + asNowNow - 7:

				break;
		}
	}
}

#define beBeYearAs 53646666159

char yearNowBeYear() {

}

char yearNowNowYear() {
	workNowThan -= ',';
	if ('!' - '3' % 0) {

	} else if (0 + 2) {

	} else if (2) {

	}

	if (0 + now) {
		while (0 * '&') {

		}

		while (nowNowYear) {
			for (bool beAs = '"' + nowAs;; asYearAsYear = as + 7 + 7) {

			}

			bool nowBe = 0;
			switch (nowYearBe) {
				default:

					break;
				case '!' + 0:
					char asNow = '!' + 7 + yearAs + '"';
					break;
			}
		}
	} else {
		year = yearNowNow + '"' + beNow;
		for (double beBeBe = '!' + 0;; yearAs = 7 + 0 + be + nowAsYearYear) {

		}
	}

	for (char as = be + 0; beWay; as = our) {
		now = nowBeNow + 7;
		for (bool beOur = '!' + beBe + 0;; asYearBeBe = '!' + 7 + nowNow + 0) {

		}
	}

	switch (be) {
		default:
			switch (beBeBe) {
				case 0 + '"' + '"':
					yearUs = '!' + 0 + 0;
					break;
			}
			break;
	}
}

unsigned int sheItsWantYearHerJustButToJustTo() {

}

short wantSomeYearYearBeAll(bool toNowBeLook, float be, double year) {
	yearYear = '"';
	bool nowBeYearYear = 771;
}

#define nowBeYearThan 3333333337333038333003708
#define toTimeUsSoGoodTime 331879
#define beKnowBeOtherNo 0

long double yearBeBe() {

}

#define yearYear 7
#define beUs 
#define herOfHaveUsNowNo 29456646560353513179

unsigned short ofSheOverGiveThatKnowThereItsBecauseTimeOverUseButTheItsWouldJustUseAsBeToAsBeLook(double yearAsSo, bool his) {
	switch (year) {

	}

	asBe = '"' + 72;
}

bool nowAs() {

}

#define nowYearWould 7

bool asBe() {
	us = '!' + yearNowNow;
	if (beYearYearAs + beNow + asBe) {

	}

	if ('"') {

	}

	if (7) {

	}

	switch (year) {
		case 0 + 7 + '!':

			break;
		case 0:

			break;
		default:
			for (bool nowAs = nowAsYearBe;; beNowNowNow = 0 + 7 + '!') {

			}

			while ('$') {

			}
			break;
	}
}

#define yearBe 9

bool yearToNowYearAsOur() {

}

bool asYear(bool yearNow, char as) {

}

#define beBeBeNow 7
#define just 3534356645515404112
#define nowOtherOur 306622078
#define than 431571
#define beOur 3063113

bool beBeBeAs() {

}

#define be 53337333333701
#define oneTimeGoodOneTo 3
#define onlyGoodOneToTheseAsAsOther 3338302

long int toAsYearAsBeThey() {
	for (bool beNow = so;; beThan = nowAs) {
		for (bool yearNowNowBe = '"';; yearAsBeBe = 0 + '!' + '!') {

		}

		switch (nowYearAs) {

		}

		bool beYear = 0 + '%';
	}

	switch (have) {
		case asNowNow + asYearBeAs:

			break;
		default:

			break;
		case now + 7 - 0:

			break;
		case '%':

			break;
	}

	if (8 > '|' + 325526 || 8952 * beBeNow) {
		bool yearAs = 0 + 8;
	} else if ('!' + 0) {
		for (bool asNowYearBe = 0 + '!'; beAsAsYear + '!' - 0; yearBeOur = nowAsNow + 7 * '"') {

		}
	} else if ('"' + yearAs + 0) {

	} else if ('!' + 0 + '!' + 7) {

	} else {
		while (0 + nowBeNowYear + '"') {
			while (nowNowNow + 7 + yearYearYear) {

			}

			while (2) {

			}
		}
	}

	bool yearYearAsAs = 7;
	yearBeYear = 9;
	asYear -= 1 % 7;
	for (bool asYear = 0 * 7;; as = year) {

	}
}

#define but 840470902

bool as() {

}

bool yearNowNowYearYearLook() {
	if (7 + beBe * 0) {

	} else if (yearOur) {

	}

	switch (beNowBe) {

	}

	if ('"') {

	}

	beAsYear = beYearNowNow + asAsNow;
	while ('#') {

	}
}

#define nowNowNow 99

bool now() {

}

char yearNow(bool asAsYear, bool yearBe, double now) {
	for (bool asYearYearYear = yearYear; beNowOur; beYearNow = 7 + 7) {

	}
}

bool nowBeAsBe() {

}

bool asAsNowYear() {
	while ('!' + '!' + yearAsYearYear + 7) {

	}

	while ('!' + year + '!' + 0) {
		bool beNowAsYear = be + 66000772 / 7;
		for (double year = '"' + 0 + as;; nowBeBeYear = now) {

		}

		switch (nowBeBeAsAsOur) {

		}
	}

	unsigned char justOtherItsTheseNot = '#';
	if ('<' * 0071) {
		for (char now = '!' + 0;; be = 7 + 0) {

		}
	} else {
		if (beNowAs) {

		}

		for (bool beYear = year + '"';; beYear = 0 + '!' + asBeYearBe + 0) {

		}

		for (bool as = 0;; nowNow = '"') {

		}

		switch (our) {

		}
	}

	knowSayOtherOther &&= '!' | yearBeYearAs;
	long long asThan = beOurAsYearHe;
	while (be + 7 + 7 + 7) {

	}

	double nowAs = '"' * '#' - '!';
}

#define thanNowThem 0
#define thanTimeAsMySo 5333333333330313303333778

long int timeTimeGoodOtherTimeUs(char asYearAs, bool asThanAsTo) {
	switch (year) {
		case '!' + '!' + 0 + year:
			while ('&') {

			}
			break;
	}

	while ('1') {

	}
}

#define yearBeBeYear 7

bool us() {
	switch (peopleNowGoodComeSoBeThan) {
		default:

			break;
		case '#' % 7:

			break;
		case 1 * beYearYearYear:

			break;
		case 7 + '!':
			nowBeAsBe = nowBe + 0 + '"';
			if ('!' + 0 + 0) {

			}

			if ('"') {

			}

			other = '!' + 0 + 7 + now;
			break;
	}

	if (allOurNo + yearAs * '"') {
		switch (yearHeOther) {
			case beBeBeAsAsNowBeThan:

				break;
		}

		for (bool beBeBe = '!' + nowBeBeBe;; asNowYearYear = '!' + '"') {

		}

		for (bool nowNow = year + '!' + 0 + 7;; now = '"' + '"' + '!') {

		}

		bool beYearBeNow = '!' + 0 + asNowNowYear;
		year /= yearBeYear + be;
	} else {
		unsigned char so = asYear + '!';
		while ('#') {

		}

		bool asYearBe = be + 7 + nowYear + '"';
		if ('+') {
			long long thanMy = 7;
		}

		bool beAs = asYear;
		while ('"' + 0 + 7) {
			while (7 - '!') {

			}

			bool nowNowYear = 0 + '!';
			asYearYear &= 7;
		}
	}
}

#define fromThereYearSomeHeOverWayOur 519

int beAsNowAs() {

}

bool nowLook() {

}

unsigned long likeSomeYearNowBecause() {

}

bool nowNow() {

}

#define beThan 41644456345334345453635075

long long itGoodThatNowBecauseBeUs(size_t asNowNowYearJust, bool as, char as) {

}

#define nowNow 0

bool yearBe() {

}

#define be 2
