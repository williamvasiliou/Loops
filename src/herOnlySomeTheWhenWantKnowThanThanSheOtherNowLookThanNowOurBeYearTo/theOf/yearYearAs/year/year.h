#define toOtherBeAsOtherNowNowOther 3333333333333333331337703701
#define oneYearUs 77800778
#define theseNowHe 7
#define at 8
#define be 0
#define beAsNow 74

bool asYear() {
	bool nowBeBe = '"' + 7;
	for (bool beNowBe = '!';; yearAsNowBe = '"') {

	}

	switch (asYear) {

	}

	switch (now) {
		case '"' + '!' + asNow:
			nowAsNowAs -= 0 + '!';
			for (bool be = nowOur * '"';; asNow = '"' + yearAsYear + '"' + '"') {

			}

			while ('"') {

			}

			bool look = 0;
			if ('!' + 0 + 7 + '"') {

			}

			bool other = 0 + 0;
			break;
		case 0 - '"':
			now /= be + be;
			asOurLook = '!' + asNow + 7;
			break;
		case '!' < yearBeNowBe:
			for (bool yearToNot = 0 + nowNowNow; nowBeBeBe + '&'; as = 7) {

			}
			break;
		case 0 + year + '"' + as:

			break;
		case '.':

			break;
	}

	for (bool beBeYear = '"' + 7 + nowNowYear;; yearNowBe = '"' + '!' + year) {

	}

	for (unsigned int year = '!' + '!'; '&'; nowBeBeYear = '"' + 0) {

	}

	while (yearBe + '!' + '!' + '"') {

	}

	for (bool yearNow = '!'; asOur; nowYearAsNow = '!' + nowYearNowNow) {

	}
}

#define itFrom 0
#define now 8

long nowTheseAsOur() {

}

#define nowAsYearOther 56353435643514657630919

unsigned char noGoodSoNowTimeWouldHis() {
	if (0 * 0 + 0601) {

	}

	for (bool yearAs = '"' + '"' + 7 + 0;; year = 7) {

	}

	while (yearNowAs + yearNow) {

	}

	while (yearYearAsBe + 7 + 0) {

	}

	yearAsAsNow = yearAsNow + '"' + '!' + 0;
	bool nowJust = 0 + 7;
}

#define nowYear 009
#define yearYearBe 7

unsigned int now(bool other) {
	for (bool asBeBe = '"' + 7 + 7; '!' + '%'; yearBeYearAs = '"' + 0 + '"' + beYearAs) {
		long int goodGoodTimeBecauseUsSoUs = '%' + '7' - asBe;
		for (float yearAsAs = 0; 0 + other; other = 7) {

		}

		while (asNow + asNowNow) {
			while (7 - 0) {
				for (bool as = yearBeNowBe + yearYearBe; asYearNowBe + nowUs; beAsNow = year + '!' + nowAs) {

				}
			}

			for (bool asYear = 0 + 0 + 7; 7 + '%'; beAsYear = 0) {

			}

			for (bool asBeNow = yearYearNowNow + '"';; asBeAsYear = '"' + '"' + 0 + 0) {

			}

			if ('"' + nowAsYearBeAsNowNowAsYearBeOther - 0) {

			}
		}

		beUs = beNowYearYear + '!' + yearBeNowBe + as;
		for (bool nowBe = 0 + 0 + 0;; beTo = 7 + beYearNow + 7) {

		}

		bool us = '!' + '!' + '"' + beYearAs;
	}

	timeAsOneBeThese = nowNowTo;
	oneOneTimeBecauseToOnlySomeBecause = '"' + nowBeAs + 7 + '"';
	nowTo = 7 - yearNow;
}

long long ourUsLook() {

}

#define nowOurToHis 01

long butThan() {

}

#define ourTo 0

bool withLook(bool year, bool just, bool asBeBe, bool yearYearNow, bool yearAs, bool asAsNow, bool year, bool asYearNow, bool asYearBe, bool yearNowOur) {
	switch (asOther) {
		case '&':

			break;
	}

	bool asAsAsAs = asAs + 7 + 0 + '!';
	bool our = '"';
	double be = '!';
	while (just + '"' + 9) {

	}

	for (bool be = '!' + yearYear;; be = '"' + 7 + '!') {

	}

	if (now) {

	}

	bool yearNowBeYear = '!' + us;
}

unsigned int oneUpWhenUpLook(unsigned int asOverJustYearHe, bool nowAsBeBe, bool other, bool asBe, double as) {
	asOur = '$' * '!';
}

#define but 787002
#define justTimeNowTheseJust 10268644

bool there(bool asYearBe, bool yearNowNowAs, bool look) {
	double as = oneOne;
	long int other = 018071 + these;
	switch (asNowNow) {
		default:
			while (asYear) {

			}

			switch (as) {

			}

			now = '"';
			switch (asBe) {

			}

			while (beBeBe + nowBeYearAs + nowAs + yearNowAs) {

			}

			float beNowYear = beBe + asBe + 0;
			break;
	}
}

float yearBeOther() {

}

long int onlyUsGoodHeBeAsGoodYearOneYearBeHis(long int soSoTimeToBeSo, char beNowBe, long int be) {
	switch (beNowAsYear) {

	}

	nowBeNow = 7 + '!';
	us = '"' + nowYear + 0 + '"';
	if (yearYearNow + 7 + '!' + nowAs) {
		nowNow = '%';
	}

	if (asNowNowSo) {

	}
}

float beBe() {

}

#define us 07073
#define nowJust 57009

bool as() {

}

bool beNowAsNow() {

}

#define yearNow 7
#define nowBe 0

bool look() {
	while (us - 19) {
		if ('!' - nowAs + '$') {

		}

		while (beYearBe + beBeBeBe + '"' + '!') {
			while (0 + '"' + 0) {

			}

			bool asNowAsAs = 1;
		}
	}

	while (beAsThanNowLook > 1 + 8) {
		while (asNowAs <= beTo) {

		}
	}

	size_t yearJust = '"' + '!' + '!' + 1 - 7;
	beNow %= 7 + 0 + 0;
}

#define sheItsAsOnlyOverYearNowAsAsMy 3533465039

unsigned int way() {

}

short yearBe() {
	while (us) {

	}

	switch (onlyUsUs) {
		default:
			bool asBeBeYear = '"' + nowBeYearBe + 7 + beYearYear;
			char nowBe = 0 + now + '!';
			break;
	}

	bool be = 0 + '"';
	char nowNowYearYear = '#';
	so = 7 + yearBe + 7 + '"';
	if (beAsBe - 7) {

	}

	if (year + yearBeNowAs + asAs + 0) {

	}

	for (bool asBeAs = '"' + beNow + beAsBe - 7;; be = yearBe + '!' + as) {

	}

	if (7 / '"') {

	}
}

#define nowBeHis 33311
#define nowBeOneGoodOnlyToAsAsBeToAsSo 01
#define be 0
#define oneUsGoodGoodOneNowUs 83
#define of 8

long long just() {
	bool asAsYearNow = asAsAs + year;
	to = asNowNowYear + 0 + 0 + yearBeAs;
}

bool now() {

}

#define than 645353434365344635456355663546655445355334466664546544454363363355363346334466444353636656364646363335563855444433356333345456364435483633543466336644665445463863654864456462535334573573

long int (unsigned int giveTheTheseSo, bool nowAsNowYear, int year) {
	if (7 + 7 - asYear) {
		if ('X' == '!' - 7) {

		}
	} else if ('!' + nowYear) {
		for (bool yearNow = yearBeYearAs + 0;; nowAs = '!' + 0) {
			other = '!' + year;
		}
	}
}

#define beYearUs 47336464343408778
#define orTwoKnowThemNowThereNowBeAsYearThanAsNowJust 45565646563463355536344563555446443455917772
#define theIt 77708

bool yearBe() {

}

#define not 3333373110388381078
#define other 0
#define goodOneOneOneTimeOneBecauseOneOneOtherOtherNowNowAsTo 1
#define theseBeSo 33

int nowBe() {

}

bool beBeYearTo() {
	if (0 + asYearAs) {

	}

	for (bool be = '"' + '"' + 7;; asNow = '"' + nowAsAsBe + '!') {

	}

	switch (nowAsNowAs) {

	}

	while (yearAsBe) {

	}

	beNow = nowBeBeNow + '!' + '"';
	nowAsYearYear = nowYear + nowBeNowBe + asNowBeNow + '!';
	bool year = '!' + nowSo;
}

#define nowNowAs 7
#define asTo 56555436403645323

bool nowAs(bool beAsBeYear, bool asYear, bool as, char asBe) {

}

#define herUsAsYearBeBeJust 3461460658

double beThemBeBeTo(char nowBeNowBeOther) {
	if ('"' * 7) {

	}

	for (double year = asYear + '!' + 7;; beNow = '"' + '"' + nowAs) {

	}
}

#define nowOther 0

bool nowAs() {
	while (look) {

	}
}

bool yearBe() {

}

#define yearBeBeBe 7

bool yearYear() {
	switch (asYearNowBe) {
		default:

			break;
		case 0 - '"':
			usToOtherTimeOther = nowBeNow + 7 + 7;
			switch (becauseUsUs) {

			}

			nowBe = '!' + 7;
			if (beBeAs - beNowYearYear) {

			}

			for (bool asNowBeNow = 7 + yearYearBe + 7; yearSo; yearNow = 0 + '%' - 0) {

			}
			break;
		case asYearYearBe - '!':
			for (bool to = 0 + yearYear + '!' + year;; nowYear = '!' + yearNowBeBe) {

			}

			bool to = 0;
			break;
		case beBeOther:

			break;
		case 0 + asNowAs:
			bool as = beNowYearAs;
			if (708) {

			}
			break;
		case 0 + '!' + as + '"':

			break;
		case '!' + '"':

			break;
		case '"' + '!' + '"' + '"':
			if ('"') {

			}

			if ('!') {

			}

			while (7) {

			}

			for (bool beNowAs = 7 + '!' + nowNowAsNow + beBeBe;; yearBeBeBe = '!' + now + as + yearBeAs) {

			}

			while ('!' + beYearNowNow + '!' + 7) {
				if ('"' + 7) {
					switch (yearAsYearNow) {

					}

					bool beNowNowAs = '"' + 0;
					while (be + '!' + '#') {

					}
				}
			}
			break;
		case '!' + 0 + 7:

			break;
		case '!':

			break;
		case nowAsNow + '"':

			break;
		case 7 + nowNowBeAs + '!':

			break;
		case asAsNowAs + '!':

			break;
		case '"' + '"' + asYearAsAs + '"':
			while (yearBeNowNow) {

			}

			for (char yearNow = 0;; as = 0 + '"' + 0) {

			}
			break;
	}

	switch (onlyOneTimeGoodBecause) {
		default:
			while (usTo + he) {

			}

			for (bool usUs = '!';; asAsNowAs = 7 + '"' + yearAs + yearAsAs) {

			}
			break;
		case 0 + yearAsAsAs + '"' + 0:
			while (0 + beBe) {
				if (nowAs + nowYearYear) {

				}

				if (7 + nowBeYearAs) {

				}

				while (beBeYearAs + asYearYearBe + 7) {

				}

				if (beAsBeAs + 7 + '!') {
					yearNow = 0 + beNowYear;
					switch (nowBeAs) {

					}

					yearBe += 7;
				}
			}
			break;
	}

	char these = '#' + '"' - '+' / yearYearNow;
}

#define thereItsOtherBeFrom 3
#define as 

unsigned long (long int goOrFromWhenHowThanTimeBeOrTheyKnowJust, int someJust, long int yearBeAsNot, bool beBeYear, bool so) {
	while (goodSomeWellOneWantSheOrPeopleHowYouGoodTimeHowHerGoThatThereOurOtherUseWhenWhenWouldComeItsWayItsThereWhenFromOf != thereHer - 649 * 'u' | soYearOur + '*' + look) {
		for (unsigned int peopleAsMy = '4' + '!' * '!'; 33 | ofSomeLookGiveUsToToThanYearTheseAsAsYearAll != yearSoAsAsLook > '^' > 7 + '!' - '!'; beYear = beNow + 0) {

		}

		notBut >>= haveThemAsUsNowBeWith + asNowThan;
		if ('!' + yearBeUs) {

		}

		asNowNow = '"' + nowAsNowYear;
		if (beNowYearAs + now + beAsAs) {

		}

		asNowAsBe = '!' + 7 + 0 + asAsYear;
		if (beAsNow - '!') {
			while ('"' < yearYearAs) {
				bool yearAsYear = '!' + '!' + beBeYearBe + asAsNow;
				if (asYearBeYear) {

				}

				if (0 + 7 + '!') {
					while ('$') {

					}
				}

				while (7 + '"' + year) {
					if (7 + be + as + '"') {

					}

					while ('!' + asAs + '"' + 0) {

					}

					yearYearAs = asYearNow + '!' + '!' + '"';
					bool yearNow = 09 + us;
				}
			}

			for (double asBeNow = now + 7 + 0; 0 + 7 * 0; nowNow = beBeBe + '!') {

			}
		} else if (0 + 0) {

		} else {
			long int wayUs = now + 0;
			for (bool yearLookYearLook = 7 + now + '"' + 7; 771; asNow = asNowAs + 7) {
				char as = 7;
			}

			if ('!' + 7) {

			}

			while (yearAs + 0 + asAsAs) {

			}

			for (bool asYearNowAs = now + '"' + nowAs + 0;; now = 0 + 0 + '!') {

			}

			for (bool nowYearNowNow = '!'; 2; beYearAs = yearAs + '!' + 0 + '!') {
				switch (nowBeYear) {

				}

				switch (be) {
					default:
						if (beNowYearAs + nowNowAs) {
							if (7 - 0) {

							}
						}
						break;
				}
			}
		}

		if ('&') {

		}

		while (7) {

		}

		while ('!') {

		}

		if (0 + '&') {

		}

		switch (beAsAsYear) {
			case '"' + yearYearNow:
				while ('"' - '"') {

				}
				break;
		}
	}

	unsigned long justJust = goodThereTheyOfHowItsFromComeFromTo > 33 * myBeBeNowOur + '.' - '"';
	if (becauseHisTo & 601 >= beTheWhen - yearNowAs * 7) {
		bool as = 0 + yearAs + '"' + '"';
		so = 0;
	} else {
		switch (beNowYearYear) {

		}

		if (0 + '"') {

		} else {
			yearAs = 0 + '"' + 7;
			char asBeBe = beYearAsYear + 7;
		}
	}

	if (53509) {
		switch (howSayAsSoOur) {
			case 0 + 7:
				while (asYear + beAsNowBe + '"' + 0) {

				}

				while ('!' < '"' - 0 - '"') {

				}

				switch (asYearAsAs) {
					default:
						if (7 + '!' + '"' + 0) {

						}

						bool beYearNowBe = 0 + year;
						bool beBe = '"' + 0 + '"';
						bool nowYear = '"';
						for (bool asYearYear = as + '!' + '"' + '!'; yearYearAs + 8; beAsYearAs = nowBe) {

						}

						yearNowYearBe = nowYearNowAs + '"';
						switch (asJust) {

						}
						break;
					case 0:
						if (2) {

						}
						break;
					case 0 + '"' + nowBeAs + '"':

						break;
					case 7 * '"':

						break;
				}
				break;
		}

		while (2 + yearNowBeYear + nowBeNowAs + beYear * now) {

		}

		while ('!' + yearYearNowYear) {
			for (bool yearBe = '!'; 8; yearBe = '"' + 7 + 0) {

			}
		}
	} else {
		switch (nowJust) {
			case 4 - 7:

				break;
			default:
				switch (asBeOther) {

				}

				bool beYearBeYear = '"' + nowNow;
				yearAsNowAs = toJust;
				yearYearBeAs = '!' + 7 + 7 + yearNowBe;
				switch (year) {
					case '"' + '"' + 0:

						break;
					case '!' + 7 + asAsAs + '!':
						for (bool as = '!' + 7 + beBeBeYear;; nowAs = '"' + beNowBeYear + year + 7) {

						}

						for (bool yearNowThan = 0;; year = 0 + nowNowNowNow) {

						}
						break;
				}
				break;
			case asNowThan:
				for (bool yearBeYearYear = '"' + 0 + asBeNowYear + 0;; yearAs += yearAsBeBe + year + 7 + yearAsYear) {

				}
				break;
			case nowBeBeJust:

				break;
		}
	}

	if ('!') {

	}

	if ('"' * yearBeYear) {

	}
}

long int howOneOnlyThemTheOtherSoYearHeThan(char nowAsAsLook, bool be, bool yearYearNowYear, char nowAsYear) {

}

bool knowHisBeJust(double ourOur, bool toHe, bool beAsAsNow, bool asYear, bool as, char now) {
	switch (asThanSo) {

	}

	double asAsAsYear = '"';
}

#define beTo 11771
#define yearOther 7546565503634173060721

long int goodYearWantSomeItLikeThatOnlyComeOneOneWith() {
	soBeOther |= oneOnlyOtherOnlyOnlyOneTimeGoodGoodOnlyHisOnlyBecauseTimeGoodUsAsOnlySo + '%' / 1;
	for (bool yearYearBeNow = yearBe + '"' + 7;; asAsBeAs = '"' + be + asNowNow + 7) {

	}

	switch (beAsOur) {

	}
}

bool allJust(bool beNow, bool asJust) {

}

bool now(bool be, bool yearNow, bool as, bool than, bool asBeAsNowSo) {
	switch (nowTo) {

	}
}

#define nowYear 7
#define our 5563436306563573544534369

unsigned long comeLikeGiveOnlyOnlyGoodTimeSheOnlyGoSomeOverHerGiveYouUpHerWellGoOneWhenWellHaveGoodWellOnlyHaveComeOverOfOtherYouTwoToWantTheToSheUpYearThan(long long thereComeUseYouKnowOverOnlyHowOverKnowOverTheWantWhenUseOfOurThatComeTwoTheOneWellHisComeAllThem, bool as, bool be, bool look) {
	for (unsigned int beYearYearYear = 'u' > 447076; 6603739 > yearNowNow; atJust %= 2) {
		if ('"' + '1' + 2) {

		} else {
			for (bool beAsNowBe = yearBeAsYear + asBe + '"';; beYearNowNow = year + 0) {

			}

			for (bool beBeOther = 7 + '!';; beBe = 7 + beYear + nowYearYear) {

			}

			us = nowNowAs + 7 + 0;
		}

		switch (beYear) {

		}

		if (now + year) {

		}

		for (char yearBeAsAs = 7 + 0;; beBe = '"' + nowAsYear + 0 + 0) {

		}
	}

	while (08 + 7 * 0001) {
		switch (nowBe) {
			default:
				if (02) {

				}
				break;
		}

		if ('"' + year + '"') {

		}

		if (asBeBe + year) {

		}

		nowNow = 0;
		while (as + asAsBe + beBeAs + asYearYear) {

		}

		switch (otherBeOther) {

		}

		char yearNowYear = as + '!';
	}

	switch (usTheThan) {
		case otherAsToYearThan + '!' - asNowAsYear:
			for (bool year = yearYear + 7;; yearBeNow = nowAsYearYear + '"' + 7 + 7) {

			}

			if (now) {

			}

			for (bool now = beYearAs;; asBe = asYearBeAs + '"' + be + '"') {

			}

			for (bool asBe = year;; beAsBeAs = year) {

			}

			while (beBe + 0 + 779) {

			}
			break;
		case 7 + 7:

			break;
		case beNowAs < 0:

			break;
		default:

			break;
		case '!' + nowAsYear + '"':

			break;
		case year + '"':
			bool beBe = 7 + 0 + '!';
			be = 0;
			switch (beAsYearNow) {

			}

			bool nowNow = '"' + '!';
			asAsYear = asYearSo;
			break;
	}

	switch (so) {
		case '!' + '!' + 0 + 0:
			while ('%') {

			}
			break;
	}

	double be = 7 + '"' + yearAsYearAs;
	if ('!' + 7 + 0 + '"') {

	}

	switch (our) {
		case '!' + year * 0:

			break;
	}
}

long long orSomeHowThereWhenKnowGoAsSheThereToNowBeOurLookMy(bool beSomeAsOtherYearBeThan, bool year, bool beBe, bool asYear, double yearThem) {
	give += 2;
}

#define our 3779
#define otherJust 68
#define whenToOurHowMyYearJust 4464363545603335456335417250992
#define beThan 7802
#define beOur 7

unsigned int yearBeAsYear(bool now, bool beBeYear, bool year, bool yearBeAs, bool now, bool asBeNow, bool nowBeNowYear, bool asJust) {
	now /= 78 - yearYearBeYear;
}

bool asNowAsAs() {

}

#define his 7

bool year() {

}

bool be() {

}

#define beYearLook 3333333333333333383333333330333335472

size_t nowLikeHowOrKnowOverUseWantWellHeAsOther(long long beNowYearUs, bool year, bool all, int our, bool asYearBeBe, int beNowYear, bool asYear, float yearBeAsAs, bool as, bool our) {
	while (0 + beAs + '#') {

	}
}

bool these(bool us, bool so, bool just, bool year, bool as, bool yearYearYearBe, bool beBeNowNow, long yearNowAsNow) {
	while (asAsYear + nowNowYearNow + now + '!') {
		bool nowAsNow = '!' + 0 + '!' + 0;
		asYearBeAs = nowBeYear - nowAs;
	}

	int beNow = '$';
	if (our) {

	}

	for (bool asNow = beBeBeNow + 7;; asNowBe = just) {
		nowYearOur = '"' + yearYearYear + year;
		double asYearAsAs = yearYearNowNow + '"' + 0;
	}

	if ('!' - be) {
		switch (oneOneOneTimeGoodNowOnlyOtherGoodBeOnlyHisOtherYearAsOther) {
			case other + asUs:

				break;
		}

		for (bool asTime = beNowBe - asAsAs;; asAsAsNow = 7 + now + '"' + yearYearYear) {

		}

		char yearAsUs = now + asYear + '"';
	} else if (013103) {
		bool nowUs = '"' + year + yearBeAsNow;
		switch (beNowBe) {
			case '\'':

				break;
			default:
				for (bool asBeAs = year + 7 + '!' + yearYearBe;; nowAs = asYearYearNow) {

				}

				switch (year) {
					case '#':

						break;
				}
				break;
		}
	} else {
		switch (yearBe) {

		}

		yearOther = 1 - '"';
		for (bool nowYear = year + year;; nowAsAsAs = '!' + '!') {

		}

		if (0 + '"' + '!' + '"') {

		}

		for (bool beAsAs = 008;; beAsBeNow = 0 + 0 + be) {
			while (asNow + '!') {

			}

			if (yearYear + year) {

			}

			for (bool so = '!' + 0 + '"' + '!';; yearBeNow = 0 + '!') {

			}
		}

		nowAs = '!' + '"';
		while (7 + '!' - 0) {
			for (bool year = be + 7;; be = '"' + be + '"' + '"') {
				switch (asNowNow) {

				}

				as = '"' + asBe + 0;
				switch (so) {

				}
			}
		}

		switch (nowNowBeTo) {

		}
	}

	int giveLook = ')';
}

#define yearAs 0
#define nowAsNow 7
#define us 36336354464664346633563653655663636363345643566586433435440546792
#define upWork 8

short nowNowBeYear() {
	switch (asBeNow) {

	}

	double year = '!' + asAs + now + '!';
}

bool other(bool nowLook) {

}

double asNowYearOtherAsAsNowOther(bool yearBe, bool them) {
	while ('"' + 0 + 7) {

	}

	for (bool nowAsAsNow = 7 + 0 + 0;; be = '!' + year) {

	}

	switch (yearAs) {
		default:
			other = beAsNow + yearBeNowNow + '!';
			break;
		case '"' + year + '!':
			if (0) {

			}

			switch (be) {

			}

			bool our = 7 + '"' + '!' + 0;
			break;
	}
}

#define beYearSo 729
#define goodWithWay 0

bool beBeBe() {

}

bool be() {

}

int nowAsBeNow() {
	switch (beAsBe) {
		case 7 + '!':

			break;
		default:

			break;
		case 0 + 7 + asAs + 7:

			break;
		case asBe + 7 + '!' + '"':
			while (nowAs + asYearAs) {
				bool nowAsNowBe = yearBeNow + asBe;
				for (bool be = 7 + '!' + 0 + nowAsAsYear;; beNowNowNow = 0 + 0) {

				}

				while ('"') {

				}

				while (7 + 2) {

				}

				bool beNow = '"';
				while (usThan) {

				}

				while (be + '"') {

				}

				switch (asAs) {

				}

				bool nowBeYearBe = '!' + yearNowYearYear + yearYearYear + '"';
				asSo = '!';
			}
			break;
		case giveTwoWellTimeItSayUsThemYearToThan:
			if ('$' + 0 - nowAs) {
				while (nowNowAsNow + asNowYearYear + 0) {
					for (char now = nowYear + '"' + '"' + yearYearBe;; beAsAs = '"' + 7) {

					}
				}
			}

			beYearNowYear = '"' - nowNowYear;
			for (bool beAsBeBe = '"' + beYearAsBe;; asYearYearBe = beYearYear) {

			}

			for (long long yearYear = 9;; yearYear = '"') {

			}
			break;
		case just + asYear < '"':
			if ('$') {

			} else {
				for (double nowYearYearYear = '!';; be = '"' + nowNow + '!' + 0) {

				}
			}

			if (asNowYear + yearYearBe) {

			}

			while ('!') {

			}

			switch (us) {

			}

			while (7 + 0) {
				char beYearYear = nowNowBe + nowBe + '!' + '"';
			}

			while (as + beYearYearBe + yearYearAsBe) {
				if ('"' + nowBeBe + 0) {

				}

				for (bool us = 0 + now;; asYearNowBe = 0 + 7 + '!' + yearNowBe) {

				}
			}
			break;
		case 7:
			bool nowOur = '!';
			break;
		case 08:
			while ('%') {
				nowNow = 7 + yearBeAsAs;
				while (7 + '"' + 7 + asYearAsBe) {
					if (7 + '!') {
						bool asAsBe = be + '!';
						char be = yearAs + nowBeNow + '"';
					}
				}
			}

			while (278) {

			}
			break;
	}

	for (char yearLook = 7 - 0;; beBeOther = 7 + 0 + '"') {

	}

	nowAsYearNow += '&';
	bool asNowAs = 7;
	for (bool nowYearNow = 0 + beAsBeYear + beBeAs + yearYearAsBe;; yearAsNow = '"' + yearBeBe + 7 + '!') {

	}

	bool now = year + '!';
	for (bool beYearYearBe = '!' + 7 + nowBeBe; now * 0; beNowNow = '!' + nowYearNow + as) {

	}
}

unsigned long yearWayAsYearLook() {
	if (asAsNow + '!' + 0 + '!') {
		if ('!' + 0) {

		}

		than = '"' + 0;
	}
}

bool just() {

}

bool beBeBe() {

}

#define beNowYearBe 383333333333333333333337337788

bool us() {

}

long int nowAsNowAsSo(char yearBe) {
	if (because) {

	}
}

bool nowBeAs() {

}

char how() {

}

#define nowYearBeBe 65780009

bool asAs() {

}

#define yearAsYear 0
#define asUsOther 53437343404277655442

long long upNowUse(bool asBeYearBe, bool beOther, bool fromYearNowBeToYearBeNowOther, double way, double as) {
	while (0) {

	}

	switch (asAs) {
		default:
			switch (yearNowLook) {

			}
			break;
	}
}

#define asJustSo 22

long int asAs(bool nowNot, bool beThese) {
	asNowBe = beAsNow + usBeYearYearOther + 7 * nowAs;
}

#define yearTo 0
#define as 5383038808

long int soNowTo(bool yearNow, bool nowAs, char beNowYear, char nowBeAsBe, float to, bool yearUs, bool beNowNowOther, bool nowYear, bool nowBeBe, bool so) {
	switch (beBe) {

	}

	bool beNowAsTo = year + year + year;
	bool nowAsBe = yearNowBe + nowBeAsYear + as + '"';
	char now = '"' + yearAs + 7 + as;
}

bool beBe() {

}

#define oneOnlyTimeTimeYearOneSo 3113

long int onlySoNoSo() {
	for (bool yearNowBeAs = 0 + 7 + 0;; be = 7 + 7 + now) {

	}

	bool asNowAsBe = '!' - asNow;
}

char beAsBe(bool now, bool other) {

}

long int yearBe() {
	usePeopleUpLikeLikeOverAsUpThatItSayTimeLookLook += giveLookOur * '\'' * '!';
	long int theWorkTheLikeHaveOverWhenSomeItSomeUsLikeItYearBeThan = yearBeAsOur == ',' - '!';
	bool now = 0 + 6;
	sayHisYearItAsBeAsNowOur = 708;
	for (bool now = 7 + nowYear + '!' + beNowBeYear;; asNowYear = beNowYearYear + asYearNowYear + 7 + as) {
		if (nowAsYear) {

		}

		for (char yearNow = 7 + '"' + '"';; now = '"' + '!') {

		}
	}

	if (7) {

	}

	while ('#') {

	}
}

unsigned char peopleAll(bool as, bool nowOther, double now) {
	nowYear -= '"' + as;
}

bool year() {
	asBeNowAsLook = '"' + '"';
}

#define wantJustNowAsNowNowSo 51

double as() {
	nowAsBeNow = nowYearYearNow + nowNowYearAs + 0 + asBeYear;
	while ('(') {

	}

	switch (nowAsAsNow) {

	}

	switch (as) {

	}

	while ('"' + '"' + 7) {

	}

	year = 0 + '"';
	beYearBe -= '!' + 7 + beYearYear + '"';
}

unsigned char goHerOfGoUpItGiveToYouJustWhenHisHisWorkNowWantUs(unsigned int asJustBeHe) {
	while ('+' + 171) {
		if (7) {

		}

		if ('"' + '"' + 7 + '!') {

		}

		for (char year = nowYear + 7;; yearYearBe = nowNow + 0 + be) {

		}
	}

	if (0 + yearNowYear) {

	}

	switch (yearBeAs) {
		default:

			break;
		case 7 + '!' + 7 + '"':

			break;
		case 7 + 0 + yearBeAs:
			switch (nowAsYearYear) {

			}

			switch (as) {

			}

			now = beBeBeBe;
			if (7 + 0) {

			}

			other = nowNow + '"' + 7 + 7;
			break;
	}

	nowNow = asNow + '"';
	for (bool be = 0 + beNowYearAs + 0;; year = yearBe) {

	}

	switch (so) {

	}
}

#define us 9
#define nowBeThan 0

long double asYearBe() {
	switch (oneUsBeHeOther) {
		case asBe + 7 + nowBeAsYear:

			break;
		case '"' + '!' + asNowNowYear + nowNowAs:
			bool nowBeBe = 0 + '"' + '!' + asYearAsBe;
			switch (yearBeBe) {

			}

			while (7 + '!') {

			}

			asTo = nowAsNowAs + beBeNowYear;
			for (bool beBeBe = 0 + '!' + '"' + 0;; yearNow = asYearYearBe + 7 + '"' + '"') {
				while (7 + beNowNowNow + asYearNow) {

				}

				if (nowAsAsAs - '"') {

				}
			}
			break;
		default:
			to = now + beAs + yearAs;
			switch (asYearBeNow) {

			}

			switch (so) {

			}
			break;
	}

	while ('_' != 333338 != '"' != '!' != asAs + yearAs - 0) {
		while (0 + so) {

		}

		float so = yearAsAs + 7 - 7;
		for (bool beYearAs = nowYear + 7 + '!';; nowNowYear = 0 + 0 + 0) {

		}

		float asNowNowBe = be + asUs;
	}

	if (808) {

	}
}

#define to 8

float us() {

}

#define onlySo 7

bool otherBeTo() {
	switch (us) {

	}
}

bool so() {

}

#define nowYearYearLook 4775
#define theseHisOtherNowThanOur 167812790708
#define use 30347730121

char knowWaySayOther(char beAsThey, float thanUsThey) {
	while ('&' + '"' * 7) {
		bool asYearAs = 0 + '!' + year;
		switch (asYear) {
			default:

				break;
			case '$':

				break;
		}

		for (bool beAsBe = beAsAs + yearBeAs;; asYearNow = '!' + 0 + as) {

		}

		year = nowAs;
		bool thanLike = our <= '"' * 0;
		for (bool be = 0 + 0 + '"' + '"';; beAsNowBe = 0 + '!') {

		}

		double as = asNow;
	}
}

char asBeNow() {
	while ('!' + '!' + 0) {
		while (7 + 0 - now) {

		}
	}

	yearNow = 0 + '!' + 0;
	while ('!' + '!') {
		for (bool beNowYear = 0; '#' - '"'; beUs = '!' + 0) {

		}
	}

	while ('%' - 0) {

	}
}

#define oneOnlyTimeTimeGoodOneNowOther 3113771
#define he 333833

long int goodTo(bool beYearYearAs, long int as, bool asAs, char year, float as) {
	if (year) {

	}

	while (so) {

	}

	switch (nowYearYearAs) {
		default:
			switch (be) {

			}

			char be = yearAsAs + '!';
			if (08) {

			}
			break;
		case beYearBe + 7:

			break;
		case 7 + 0 + '!':

			break;
		case '"' + '"':
			for (bool be = be + '!' + beYearBe + 7;; now = nowAs + 7 + '"') {

			}

			switch (asYearYearNow) {
				case 079:

					break;
			}
			break;
	}
}

#define asYearBe 7
#define be 32
#define beNow 171
#define yearNow 443565364343564464376034505

bool sayWouldBeUs() {
	yearTo = beNowBeNow;
	if (0 + '!' + nowNowThan) {

	}
}

#define yearNow 7
#define justTheseThan 4453333333333333333333313

long int oneOneTimeYearNowJust(char other) {
	switch (toYearBut) {

	}
}

bool beAsYearAs() {

}

long double now() {
	justGoodAsUsWould |= giveSomeAsLikeOtherYearThan != ourOtherYearBeJust != go;
	unsigned long twoTheAsGoOnlyAllTheyToThan = 0 + 0 + yearYear + '!';
	char beYear = 7 + asBeNow + 0;
	switch (be) {
		default:
			bool asLookSo = beYear + '!' + '!';
			while (nowBeNowAs) {

			}

			while (nowJust) {
				char asNow = year - '!';
			}
			break;
		case nowYear + '"' + '!' + '"':

			break;
		case nowNowNowBe + beBeNowAs + 7 + '"':
			switch (now) {

			}

			switch (year) {
				default:
					if ('!' + '!') {

					}

					if (0 + 0) {
						for (bool our = asAsBeYear + yearNowAs;; yearAsBeBe = '"' + '!') {

						}
					}
					break;
			}
			break;
		case 7 * 7:

			break;
	}

	bool asYearBeAs = '"' + now;
	if ('!' - '"') {

	} else {
		switch (beOur) {

		}

		for (bool beBe = 0 + '"' + 0;; nowYearNowBe = 7) {

		}

		than = asAsNowBe;
	}
}

unsigned char twoItNowSaySayHerWhenSayKnowKnowSayPeopleSheComeUpOverOneTheseOnlyNowNowBeKnowOnlyYearBeYearOverBeAsUs(bool as, char nowBeNot, bool asAs, bool nowBeNow, bool nowAs, char be) {
	while ('!' + nowNowYear + 7 + '!') {
		asOur = '"' + '!' + '!' + '"';
		while (beAsBeBe) {
			while (7 + '!' + 0 + 7) {
				yearNowNow = 7 + 7 + beYearAs;
				for (bool beBeAsYear = '!' + year + '!'; 0 + 0 - '!'; beYearYearNow = 0 + yearAsAs + 7) {

				}
			}
		}
	}

	nowNow += 7;
}

long int nowTo() {

}

bool beNowBeNow() {

}

bool asAsAsBe() {

}

#define yearBeBeAs 7

bool asAsBe() {

}

bool asNow() {

}

bool be() {

}

bool as() {

}

#define nowYearYear 66085565348
#define year 308878
#define thereThereComeTimeKnowHowLookKnowJustToNowThan 3771619

short yearOtherThem() {

}

bool asAs() {

}

#define asYearNow 0
#define yearAs 0

double beBe() {

}

#define toLook 8

long int onlyTimeGoodTimeOneGoodAsOnlyHisHisTo(long int theseOneTimeOneUs, bool to, char yearNowYear) {

}

long int one() {

}

bool yearTo() {

}

long int beYearOther() {

}

char wantNowBeUs(bool yearYearAs, char beNowNow) {

}

bool be(bool as, bool nowOther) {
	unsigned short asNow = '"' + '"' + 7;
	for (char be = '"';; now = 7) {

	}

	asYearNowBe = 9;
}

#define whenOverWantItsTheyItOrAsMyAllYearNowYearNowNowYearBeJust 339543705
#define soWay 7

double so() {

}

float nowAsBeAs() {

}

#define herAsThan 8

char beYear() {
	if (2 + 7 * 0) {

	} else if ('%') {

	}

	if (beYearBe + 0 + '!' + 7) {
		bool our = usYearLook;
	}

	while ('!' + '!' + year) {
		yearNowBeBeBeYearYearOther = yearNow + beYear;
	}

	switch (likeSomeUsGiveYearBeWith) {
		case 2:
			switch (yearOther) {

			}
			break;
	}

	asYearAs += be + '"' + beYear + beAsBe;
	switch (asOur) {

	}

	if (beNow + 7 + '!') {

	}

	for (bool other = 7;; yearAsNowBe = '"' + 7 + '"' + '"') {

	}
}

#define asUsYearWay 25644709546
#define allBeJust 8

long int nowBeNowOtherSo(bool yearNowJust, bool now, double now) {
	for (bool yearYear = nowYearBeAs + 0;; yearBe = '!') {

	}

	while (asNowNowNow + '!' + '"' + '!') {

	}

	while ('"' + now) {

	}

	switch (so) {

	}
}

long nowHe(bool asNowOur) {
	while (7) {
		switch (beSo) {

		}
	}

	asAs = '"' + '!' + 0 + 0;
	switch (asAsBeBe) {

	}

	asJust = '"' + 7;
}

#define nowYear 2
#define asAsYear 33333317333703331371

long int oneTimeOnlyBeGoodGoodGoodTimeTheseHeBeAsThese() {
	switch (beBeBe) {

	}

	if ('&') {
		bool than = as;
		bool asNowAs = yearAsAsNow + '!' + '!' + yearYear;
		char beBeNowNow = '"' + 7;
	}
}

int yearBeBe(double nowYearAsYear) {
	if (yearYear) {

	}

	switch (it) {
		case be + '"' + 0 + asNow:

			break;
		default:
			bool asYearYear = '!' + '%';
			yearYearYearNow = '"' - asNowYear;
			break;
	}

	while (2 < 'U' + '!' * '!') {
		if (year + nowNowNowAs + nowBeNow - 0) {
			for (bool nowYearBeBe = 0 + nowYearNowYear;; yearNowBe = nowNowNowBe + '"' + 7 + 0) {

			}

			yearBe = 8;
		} else {
			switch (ourOther) {

			}
		}
	}
}

#define workHaveYouAsUpAllOrHaveNowBeBeAsOther 5349

unsigned int only() {
	switch (asLook) {

	}

	yearNowNowAs = 0;
	for (bool be = year + beBe + beYearAsAs;; nowAsBeNow = 7 + nowBeYearAs + '!' + beNowAs) {

	}

	bool nowAsBeBeNowBeAsTo = '!' + '!' + now;
}

char theNoAsNowBeBeTo(bool nowBeYear, bool be, char our, bool asBeYear, bool now, bool other) {

}

#define ofLikeSayTheYearOverHeThatGoodAllOurCome 553435668079299

bool yearBeAsYear() {

}

#define yearBe 7

double yearUs() {
	while ('_' != 3 / be / '+' + '#') {
		asNowYear |= 33073;
	}
}

long int timeTimeBeOneTo() {
	switch (so) {

	}

	if (nowBeNow + '"' + 7 + 0) {
		beOther = 0 + asBe + 7;
		bool nowBe = be + '"' + '"';
		asYear += '"' + nowAs;
	}
}

#define onlyOneOtherYearNowBeTo 771

long int these(bool asUs, bool yearAsYear, bool yearNow, bool yearAs, long int beYearTo) {
	switch (beYearBeNow) {
		case other:

			break;
	}
}

#define be 08
#define yearAs 0
#define just 0

double nowNow() {
	switch (atThese) {
		default:

			break;
		case other - 0 + '#':

			break;
		case asNow:
			yearBeYearNow = '!' + look;
			while ('!' + '"' - '"' + 7 + '%') {

			}
			break;
		case nowAsNow + '"':

			break;
		case beBeNowBe + '!' + '!':
			other = '!' + '!';
			break;
	}

	if (peopleUpAsWithSomeHow - 0 * 7 * '!') {
		while (7) {

		}

		switch (beUs) {

		}

		for (bool nowYearBe = yearBeBeAs + 7;; as = 0 + asBe + '"' + nowBe) {

		}

		asAsNowAs = '!';
		look = yearYearNowBe;
	}

	beYearAsYear = '"' + 0 + 7 + be;
	switch (beBeBeBe) {
		case '"':

			break;
		default:

			break;
		case '"' + nowYear + '"' + 0:

			break;
		case 7 + 7 + 7 + '"':

			break;
		case 00079:

			break;
	}

	switch (yearAsNowAs) {
		default:
			switch (other) {

			}

			while ('"' + beBe + year) {

			}

			while ('!' <= 7) {

			}

			while (than) {

			}
			break;
	}

	for (char asAsYearBe = yearNowNow + asAs + beAs + 7;; yearNowAs = '"' + 0 + '"') {

	}
}

#define youShe 343555
#define beOtherNowJust 1
#define now 0

unsigned int wayBeYearBeJust() {

}

bool asNow(bool yearYear, bool year, bool asNowYearAs, char be) {
	for (float beSo = '"' / asBeBeTo; '\''; year = 0 + as + '!') {
		while ('%') {

		}
	}

	for (char nowYear = 7 + now + 0 + 0;; nowYearBeYear = '"' + as + 0) {

	}

	yearHisYearNowUs = 7 + yearNowBe + '!';
}

long int goodOnlyTimeOneOneYearBecauseOnlySoOneTimeAsNowHeNowSo(bool oneOneTheseUs) {
	bool year = as + 7;
	switch (asHe) {

	}
}

#define beSoUs 7808

bool nowSo() {

}

bool yearBe() {

}

#define nowYearNow 0

bool look() {

}

#define asYear 0

bool asJust() {
	char asAs = '"' + 7;
	while (yearAsNow % '!') {
		size_t theseYearOtherGoOneOfHerOnlyWith = asBe;
		while (beBeNowYear + be) {
			bool asBeYearBe = 0 + 0 * 0;
		}

		if (beYearAsAs + 8) {
			if (0 + '!' - beYear) {

			}
		} else if (as + '!' + yearYearAsAs) {
			if ('"' + beNow) {
				if (8) {

				}
			}
		} else if (beBeYearBe + '"' + 7) {

		} else if ('"' + '!') {

		} else if (7 + year + now) {

		} else {
			while ('"') {
				for (bool year = be + '!' + beNowNowAs;; beYearYearBe = 7 + 7 + 0) {

				}

				bool to = '"' + '"' + 0 + 7;
			}
		}

		yearAsBe = '!' + asYearAs + be + beAs;
		if (7 + asAsNowBe + '!' + 0) {
			bool beNowBeBe = 0 + 0 + '"';
			for (bool be = '"' + '"' + 7; overBeYearBeUs; asBe = '"' + 0 + beNow + 0) {

			}

			switch (yearAsBe) {
				default:
					while (now + '!') {

					}

					bool to = 7;
					break;
			}
		} else if (0 / 0) {

		}
	}

	for (unsigned char beBeNowAs = '/' + yearAsAsTo;; beAsSo = yearBe + be + beYearBe + 0) {

	}

	if (toJustAsOther) {

	}

	bool nowYearAsBe = 7 + beAsBeYear + 7;
	if ('!' + '%') {
		if (asBe) {

		} else {
			for (bool yearYearOur = yearNowAsYear;; beBeYear = '!' + year) {

			}
		}

		if (as + 0) {

		} else if ('"' * 7) {

		}
	}
}

#define haveItYearThanYearSoNowOther 43333330333308330301303383
#define goodGoodAsTime 03333183171

bool yearBeAsYear() {

}

long long themOurUs() {

}

#define asYear 6643572496253070708
#define allGoLookComeSomeWellNowNowAsYearTo 2674708

bool beNowYearNow() {

}

bool beNow() {

}

#define asAs 0
#define asNowNow 7

double asUs() {
	nowBeAs += nowAs + 0 + '!' + 7;
	for (short not = 769 * '!'; 8 - yearAsAsAs; beNowNowBe = '"' + '"') {
		bool yearNowYearAsAsOther = '"' + 7;
	}

	as ||= yearNowAs + beYear & 7 + 7 + asNowYearAsBeTo;
	bool howOur = nowYear;
	if (2) {

	}
}

long double not() {

}

#define itsToJust 56144656829

long now() {

}

bool asBe(bool asNowNowAs, bool yearAsNowAs, bool nowAs, bool nowAsYear, double asNowBeNow) {
	switch (yearBeBeYear) {
		default:
			if (7) {

			}

			switch (than) {

			}
			break;
		case 0 + '!' + 7 + 7:

			break;
		case as * 7:
			if (yearAsBe + '"' + 0) {

			} else {
				for (bool nowBeNowYear = beBeBe;; yearAsAsBe = '!' + 0 + year + '"') {

				}

				while (just) {

				}
			}
			break;
	}

	nowBeAsBe = '!' + 7 + 0 + 0;
	bool now = 0 - '!';
}

#define look 722
#define now 0
#define beYearAs 8

bool yearYearYear() {

}

#define to 0
