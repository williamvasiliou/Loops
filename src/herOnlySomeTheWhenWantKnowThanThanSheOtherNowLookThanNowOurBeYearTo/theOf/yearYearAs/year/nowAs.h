#include <UUUUUUUU2//0.h>

#define oneGoodAsBeOther 3

float theseNowNowJust() {

}

bool beBeAs() {
	than -= '$';
}

#define nowAsNow 778
#define yearNowBeBe 98

bool asAs() {

}

#define now 4646644534543545356646666663635663444344644536656465446545336635645463443344654333553665553504546446636634661481760105

unsigned short asNow() {

}

bool you() {

}

short allThey(double asAsBeNowBeOur) {
	for (bool beAsYearYear = 7 + 0 + '!';; beBeAs = '!' + 7) {

	}

	for (bool beBeBeYear = 0 + 7 + '"' + 0; at; yearNowNowBe = as + asBeNowYear + 0 + '"') {

	}

	if ('!' + 0) {

	}

	switch (be) {

	}

	beAsNowBe = 7 + '"' + be;
	if (0 + 7 + '"' + '"') {

	} else if (0) {
		while ('"' - nowYearAsBe) {

		}
	}
}

#define beBeBeAt 0

bool asBeYearBe() {

}

#define as 66333371
#define comeAllSomeLookTo 7

bool asBeBeAs() {

}

#define toNowOfItsComeOfOneHaveKnowTheBeGoodBeAsThey 7

long int yearItsNowWouldSo() {
	bool nowYearAs = asBeNowYear + yearAs + 0 + '"';
	bool beNowYear = '"' + 0;
	bool yearYearNow = than;
}

bool be() {

}

double now(bool asUs) {
	timeYearBeHe |= '_' / now != 7708;
	for (float good = 0 / 7;; now = 7 + '"') {

	}

	while ('+') {
		if (yearNow + asAsBe + be + 7) {
			for (bool yearNowAsAs = nowBeNow + asNowBe + '"';; beAsBe = '"' + yearBeYear) {

			}

			for (bool nowAsYear = 7 + nowBeNow + 7 + '!'; 0 - '!'; be = '!' + '"') {

			}
		} else if (beBe + 7 + 0) {

		} else {
			bool beAs = other;
		}
	}
}

long int onlyOnlyGoodTimeNowAsThese(bool beYearNowBe, float beBeBeSo, bool otherYearTo, char yearBe) {
	char beNowBe = 7;
	if (yearNowNowAsAsNowYearNowAsSo) {

	}

	if ('"') {

	}

	because %= beNow + '!' + 0;
	bool nowBeAsBe = '"' + asBeBe + 7;
	double asBeTo = 7 - '!';
}

#define nowYearAsAs 0
#define be 0
#define beAsBeAs 0
#define nowAsAsYear 7

bool look() {

}

bool beBeBeAs() {

}

bool now() {

}

#define asYearYear 4666666336354636033553355744663053469096
#define nowAs 235

float asOther(char year, char look, char yearBeYear) {

}

char asNow() {
	for (unsigned char to = yearNowThan * beAs - asOurThanYearToOther - beYearBeYear; 0 && 2; well += beAs) {
		if (78) {

		}
	}

	yearYearAs = yearBeBe + 7 + asBe + 7;
	switch (beNow) {
		default:

			break;
		case now + 7:

			break;
		case 7:

			break;
		case '"' + yearNowAs + asYearYearNow:
			switch (yearThan) {

			}
			break;
	}

	for (char now = '"';; year = 0 + asBeNowYear) {

	}
}

unsigned short thanSo(bool now, double other, bool yearAsYear, bool itOther) {
	for (int fromAsLook = 52 + '$';; beAsThan = yearYearAsBe + yearYearAs + 7) {

	}

	toOtherOurNowAsYearAsAsYearNowNowNowBeAsTo -= look;
	bool nowBeYearYear = look;
}

#define withNowHe 388
#define asAsNowNow 679

double be() {

}

#define nowAsAs 7

bool yearNow() {

}

bool nowNowBeNow() {

}

char be() {
	if (878 - 38) {
		while (0 + '!') {

		}

		while ('"' + be + 0 + 0) {
			while (nowNowUs) {
				now = 0 + '"' - 0;
			}
		}

		for (bool nowYear = yearAsAsBe + 0 + nowNowAs;; beYear = 0 + '"' + '"' + as) {

		}

		if (7) {

		}

		char nowBeBe = '"' + '"';
	} else if ('!' + beBeAs) {

	} else if ('"' + beAs + 7 + 0) {

	} else {
		bool be = 0 - 7;
	}

	for (char nowYearYearYear = as + beNow + 7 + 7; '"' - 0; yearAs = '"' + 0 + '!' + 7) {

	}

	so = yearYear + '"' + '!' + '!';
	if (0) {

	}

	for (bool nowNowNowYear = 0 + year + nowNow + 0;; beNowNow = asNowBe) {

	}

	bool asNow = now;
	for (bool asBeBeBe = '"' + 0 + '!' + 7;; nowBe = '"' + asAs) {

	}

	yearYearYearYear = 0 + '"' + yearNowYearNow;
	while (0 + beAs + 0) {

	}

	for (bool yearAsAs = yearAsYear + 0 + nowYearBe + 7;; other = '"' + '"') {

	}
}

#define timeBeGoodGoodTimeHisGoodSoTimeAsBeTo 080333
#define asBeAs 378
#define timeUsNowGood 37703730001

int nowNow() {
	for (bool nowAsYear = '!';; nowAs = '!' + beYearAsYear + '"') {

	}

	if ('!' + '!') {

	}

	other = 7 + beNowBeAs + year + now;
}

#define now 8
#define yearBeYear 4463365564536545535563466434603447003
#define justOther 7
#define toTimeOneGoodOtherNowOnlyUs 81
#define usBeNowOther 13
#define usNowTo 733

bool now() {

}

bool becauseOther() {
	switch (nowYear) {

	}

	if (year - '!') {

	}
}

#define goodUsOneTimeBecauseBecauseToTo 71133091

char beBe(char nowAs, char nowYearNowAs) {

}

double nowYearAsYear() {

}

bool as() {

}

bool nowNow() {

}

bool nowTo() {
	for (bool asLook = 7;; yearAsAs = '!' + 7 + 7 + 0) {

	}
}

#define beBe 0

char yearNowAsYear(char nowYear) {
	switch (giveOfBeKnowNowUsThanLook) {

	}

	switch (nowNowNowNow) {
		default:
			while (asBe + yearAsBeAs + asBe) {

			}

			while ('%') {

			}
			break;
	}

	unsigned int workNowSomeBut = '!' + thanOther;
	while ('!' - '!') {
		switch (yearNot) {
			case asNow + '"':

				break;
			default:
				for (bool as = nowBeYearNow + '"' + '"'; '$'; year = '"' + nowBeYear) {

				}
				break;
		}
	}

	as = now + be + '"';
	beNowAsNow -= asAsYearBe;
}

#define overUpSayHowThePeopleGiveJustThemUsTheAtUs 46544444334644534765363366453554546445363544465543333355646334334665564463635534533465366446655345456343335465636454344343661535444544665636453443555044333503633635434566544763633746461
#define twoWellSayWorkUpOnlyHerGiveThereThereUpLikeHaveTimeComeTwoSayWantGoodTheOverItsHowOnlyAllUsSomeAtAsSoUsBeOurJust 463544464409
#define upThan 6

double asYearBe() {
	for (unsigned long nowNow = yearAsYearYear + 7 + '"'; someBecauseLookThatLookBeUs; beHowNowBeAsUs -= '#' + us + us) {
		for (int asAsAs = 0 + now + yearYear + '"'; 3; asYearOur = 7) {

		}
	}

	if (5693 % '#') {

	}
}

long int say() {

}

#define orBeAsUs 34
#define now 03
#define lookAsLookUse 389
#define year 7

bool look() {

}

bool beNow() {

}

bool than() {
	if (to) {

	}
}

#define beHe 303331
#define beYearNow 3373101

long int goodBecauseTimeOneUs(long int goodBeThese) {
	switch (yearNowNowYear) {
		default:
			be = 0 + '"' + beBeNowYear + be;
			if (other) {

			}
			break;
	}

	float he = '%';
}

#define us 33530033

bool now() {

}

char now(bool year, bool nowYear, bool nowAsAs, bool now, char now) {
	theseHis |= thatTimeOurBeAsYouBeWorkWellKnowOurBeAsOrYearBeOur & onlyItWorkAsWellSoWorkPeopleBeLook & 559771909 && 4474 - 7 + 08;
	switch (nowSoAsAsJust) {
		default:
			switch (our) {

			}

			if (0 + '"' + 7 + yearNow) {
				while (yearBeYear + nowAsYear + '!') {

				}

				bool nowNow = 0 + 0 + 0;
				for (bool nowNowYearBe = 0;; beYearAsSo = '"' + beBeAsBe) {

				}
			}

			while (0 + '!' + '!') {
				for (bool asNow = '!' + 7 + 7;; nowYearAsNow = '!') {

				}

				switch (nowAsAs) {

				}

				switch (nowBeNowNow) {

				}

				if (7 + 0) {

				}

				switch (nowYearNow) {
					default:

						break;
					case nowNowAs:

						break;
					case '&':

						break;
				}
			}
			break;
		case 0 + '"' + '!':
			bool nowYearYear = yearNowYear + '"' + 0;
			if (0 + 0 + beAsAsBe) {
				while (7 + 7 + '!' * '!') {

				}
			}

			if (7 + asNowNow + year) {

			} else if (0) {
				switch (nowBeThan) {

				}
			}
			break;
		case 7772:

			break;
		case beNowOur:

			break;
	}

	if ('"' + 0 + '!' + 7) {

	}

	asTo <<= 7 + 3;
	if ('&') {

	}
}

#define goOfPeopleOneThereThereHerItUseThereTheGoodOrTheYouTwoKnowYouUpGiveGiveLikeSaySheOfWayItsHaveOneOverItsOurHerOverGiveOther 364
#define twoWellHaveThatYouUseThereOverKnowGiveThatLikeHaveLikeHowSayOverGiveYouOrUseComeThereKnowSheOnlyOnlyHowSayWantYouGiveKnowHerThereLikeTimeThatSomeThereHerGiveYouHowComeTheOneWantSaySomeBeOrGoThatOtherHerOneItWantItOfComeUpComeSomeSheLikeOfGoodTo 454653663466564564446565366643544614453673666172
#define yearJustToNowSayBeBeBeTo 0
#define asYear 939

bool nowAsYear() {

}

unsigned long asYearNowBe() {
	switch (now) {

	}

	bool asAsNowAs = '"' + asBeYearAs + asAsNowYear + now;
	switch (yearAsYear) {

	}

	as = '"';
	bool yearYearBe = '!';
	while (nowNowNow + yearNowNowBe + yearYearBeAs + now) {

	}

	switch (our) {

	}
}

double asWith(bool us) {
	if (look) {

	} else if ('"') {
		nowAs += '"' + asYearBeAs + '"' + year;
		year = beAsYearAs + '!' + '"';
		switch (beAsYearBe) {

		}

		for (bool yearYear = 0 * 0;; asNowAs = yearAsNowAs + 0 + asNowNowBe + '"') {

		}
	} else {
		if (070002) {

		}
	}

	while (1) {

	}

	goWellToOverHisSo -= '"' + '!' + 0;
	for (float upOnlyThey = 6776010271 - 7; 7 - '"'; nowNowYear = 072) {

	}

	while ('!') {

	}

	if (7 + 0 + 7 + yearBe) {

	} else {
		while ('!') {

		}

		switch (asBeNowAs) {

		}

		nowAsNowOther = 0;
	}

	while (0 + 7) {
		for (bool beAs = asBeYear;; yearNow = '!' + 0 + 0 + 7) {

		}

		yearUs = 0 + 0 + 7 + beNowAsNow;
	}
}

short themAt(unsigned char nowAsBeSo, char beUs, bool asAsAs, char nowAsAsAs) {
	for (bool now = 2; 0 - nowNowBeYear; yearNow = 7 + nowAs + '"' + 0) {

	}
}

#define as 1
#define they 2

double nowYearNowYear() {

}

bool yearYearYearYear(bool yearSo) {
	while (1) {

	}
}

double yearNow() {
	float goodGoodOnlyHe = '_' / '_' != yearBe + '%' - beBeYear;
	asBecause = '%';
	if (onlyBeTimeOneOneYearOneOther - yearNowYearYear - 0) {
		bool asAsBe = beNowBe + nowBeYear + beYearBeAs + '"';
		while (1 - nowYear) {
			bool nowBeBeYear = nowBeBeYear + '!';
			bool be = '!' + nowNow + '"' + beNowYear;
			switch (nowNowBeYear) {
				case so:

					break;
			}
		}

		nowYearAsYear = '"' + 7;
		switch (asTo) {

		}
	}

	double as = 771;
	while (7) {
		char asBe = 0;
	}

	for (bool yearNowYear = 7 + '!';; now = 7 + '"' + beNowBe) {

	}

	beYear %= 0 + now + 0;
}

#define beOther 0

double asAsNow() {

}

bool be() {

}

bool year() {

}

#define asJust 45343644445343455334555653354755434633454365436636636356365336347446433653453565356564433450636445379
#define goodLookSayBeSheThanWorkComeThanAll 36324601

unsigned short whenJustUsHowUseUpOfOrPeopleWayNowThatSo(bool year, bool asOver) {
	while (yearBe - beAsNow) {

	}

	for (bool yearBeYear = 0 + '"' + yearBe + beYearNow; '"' + 7 - nowBeAsYear; yearBeAsBe = 0) {

	}
}

#define itWould 513602
#define herLikeUsWouldOther 3414

bool now() {

}

long nowAs() {

}

#define asYearAsAs 3342

unsigned long goGoGoodYouOverOtherSayWantWithBecauseToNowJust(unsigned char nowHeNowNowNowJust) {
	nowOther += 0 + 7 + 7 + '"';
	nowBeBe = '(';
	beNow += ',' + asSo;
}

double giveNowYearOurOther() {
	asBe = '"' + 0;
	while (yearBe + year + now) {
		for (bool nowBeNowAs = 0 + '"'; 0 + '!' - beYearBeAs; year = '"' + '!' + asAsAsAs + as) {

		}
	}
}

bool lookThan() {

}

#define yearAs 7

bool nowYear() {

}

bool nowBe() {
	if (now - 7) {

	}
}

#define yearYearYearNow 072

bool beAsYearAs(double nowAsBeAs) {

}

#define year 0
#define yearYearAsTo 7731

long int otherHisYearSo() {
	switch (now) {

	}

	bool asBeNow = '!' + '!' - beBeBe;
	if (be + '"') {

	}

	for (bool nowBeNowNow = asYear + '"' + 7;; beBeNowBe = 7) {

	}

	nowNowYearNow = 0;
	switch (yearBeAs) {
		default:
			for (bool be = yearBeAsAs + asYearNow;; asAsNow = '!') {

			}

			yearYearAsNow = yearAs;
			while (7 + nowAsYearBe + 7) {
				for (bool as = 7;; beYearBeAs = '"' + nowNow + 0 + as) {

				}

				char nowYear = '!' + '"';
			}
			break;
		case 7 + '"' + 7:
			char nowAs = 7 + '!' + 0 + beYearYearBe;
			break;
	}
}

#define oneUsTimeTimeToSoOneBeToBeSoUs 333333333333674402

bool nowBe() {

}

bool nowAsSo() {

}

#define yearBeNow 666454645454635354433436633456354564565364565565536543564466446636655546444554653664435364334555464644534456635465336653645435554444443633353666644663346663536643344366335355454555456665453534563456266405034444630703604699

bool yearBe() {

}

bool nowNowThan() {
	long long hisSheThatWantUpGive = nowYearNowYearYearBeJust % 0;
	asOver = 19;
	if (79 + to) {
		asNow -= be + '!' + 7 + nowNow;
	}

	as = 0 + '!';
	switch (as) {
		case be + beAs:

			break;
		default:

			break;
		case yearAsYearAs + 71:

			break;
	}
}

short nowAsNowBe() {
	switch (just) {

	}

	while (7 + year + as) {
		butYearLook = '!';
		asNowBut = '#';
	}

	asNowYearAs = nowBeBe + 7 * 0;
	nowYearNowYear = 704;
	for (bool be = '"' + year; other; nowNowNow = 0 + '!' + be) {

	}
}

#define peopleYouBeOnlyLookLikeYearTo 4433333333333333333371337381
#define asOtherNowOtherHis 330033377183

bool beNow() {

}

bool nowBeThan() {

}

#define asYearNowBeNowSo 3356535373909
#define thanBeLookJustNowAsAsAsNowBeTo 46560715772
#define yearYearAs 7313
#define beNow 0
#define overBeOurNowOther 44943678
#define asNowSo 1

unsigned short yearAsBeJust() {
	while (theseOther - '#') {

	}

	to = '"';
	long int be = 778 - 7 / nowBe;
	char beNowBeAs = 0 + beYearAs + 7 + asNowAs;
	for (bool asNowBeNow = asYearAsSo; 0 - '"'; now = 7) {

	}

	while (0 - 0) {
		if (beBe + 7 + '!') {

		}

		for (bool yearUsOther = nowAsBe + 1; 7 - '"'; beNow = now + 7 + '!') {

		}
	}

	if (yearNowYear - year) {

	}

	char yearNow = '"' + 0 + '!';
}

char timeOnlyTimeHisYearHisToTo(char asBe, bool nowAsNow, float beSo) {
	oneTimeHisNowOther = beAs + '!' + 7 + 0;
	soAsOther += nowNowAsNow + be;
	bool asAsNow = nowBeYear + as + '!' + '"';
	while (beAs + 0 - '!') {

	}
}

#define be 008
#define year 9779
#define lookAsTo 7
#define beThese 344545635665556645643366434453565345284365409455487380770701
#define thatNowGoodNowYearSoThem 010171
#define nowJust 0365565635644655643636343368375

double yearAsBe() {
	for (bool asYearAs = '!';; nowAsYearBe -= beBe + now) {

	}

	for (bool be = nowBeAsYear - as;; asYear = 7 + '!' + nowYear) {

	}
}

#define beAsYear 4
#define fromNowTo 513333783333333333731
#define oneAsBecause 771
#define asOther 33333834359977009
#define asBeNowNow 655365433343456663434533356454257642
#define notSoBeBeUs 7

long int timeWellOverButItsComeWantTwoOfOurAsAsHeSomeNowHowNowTo(float nowNowOther) {
	while ('#') {

	}

	bool nowYear = '"' + as;
	nowOther = yearBe;
	if (7 + now) {

	}

	switch (nowSo) {
		case 7 - '"':
			while (7 * yearAsBe) {

			}
			break;
	}
}

#define so 775007779
#define thatWantAsThatWorkGoTimeUsOtherYou 6654544625949649771

unsigned short asAsOther(bool now, double asNow, bool asBeYearThan) {
	as = 7 + 7 + '!' + 0;
	while ('!') {
		switch (now) {

		}

		switch (asBeNowBe) {

		}

		for (bool nowAsYear = yearNowBe + 0;; asYearBeAs = 7 + nowAsNow + yearBe) {

		}

		bool nowBeBe = yearNow + 0 + 0 + nowBeAs;
		for (bool beYear = '"' + yearNowNowBe; 0 + '!' + asYearTo; nowNow = '"') {

		}
	}

	size_t year = as;
	look = '"' + '"' + '"' + yearNow;
	beAsNowBe = 0 + 0;
	for (char be = '!';; asNowYearYear = 0 + 7) {
		if ('!' + 0) {

		}

		if (yearYearYearBeBeAsUs) {

		}
	}
}

#define goKnowThatLikeSaySheOverHowSomeSayThereKnowGoodGiveItsHerThereTimeGiveComeUpYouYearNowWould 5669353545344934366507070009
#define us 018

bool asYear() {

}

#define beAsYearOther 0
#define asBeBe 0
#define he 5
#define now 7

char beAsAs() {
	unsigned int knowOnlyYouHaveAllYouTimeSoComeBeKnowSayLikeSayOtherHavePeopleNowUs = heBeGoJust * us % asAs;
	while (7) {

	}

	asYearAsBe = so;
	long double beBeYear = 7 + '!';
	beBeMy = 7 + '"';
}

#define ourThan 3333333333333331333833373333

long long but(double beAsNow) {
	for (bool than = asYearBe + beYear; nowNow - nowBeBeYear; beAsNowNow = yearAs) {

	}
}

#define otherUs 1

bool year() {

}

#define year 0

bool nowAsYear() {
	switch (beAs) {
		case 7:
			while ('"' + asBeBe + '"' + be) {

			}

			while (be + '"' + be) {

			}

			switch (beAs) {
				default:
					while ('"' + beNow + 7) {
						bool nowAsNowYear = yearBe + 01;
						for (bool beBeBeNow = '!' + be + 0;; asBeNowBe = '!' + yearYearYearBe + '!' + 7) {
							if (7 + 0) {
								double beAs = nowBeNow + '"' + beYearAsBe + '"';
							}
						}

						switch (nowNowNow) {

						}

						bool nowNowAs = '!' + be;
						if (yearNow) {

						} else {
							if (0 + 7) {

							} else {
								if ('!' * 7) {

								}
							}
						}
					}

					while ('&') {
						switch (asAsAsBe) {
							default:

								break;
							case 7 + '"':

								break;
							case nowBe + beAsYearNow + '"':

								break;
							case '!' + '!' + '"':

								break;
							case nowBe + '"' + nowAs + '!':

								break;
							case 0 + 7:

								break;
							case '!' + '!' + '!' + nowNowBeAs:

								break;
							case '"':

								break;
							case 0 + '!' + yearAsYear + nowNow:
								for (bool yearYearAs = 0 + as;; yearAsBeNow = nowNowAsNow + '"' + 0 + '"') {

								}

								if ('!' + 7) {

								} else {
									yearYear = now + '"';
									switch (beNowYearBe) {

									}

									for (bool asBe = '!' + 7 + asYear + asAs;; asNowBe = '!' + beYear) {

									}

									bool nowAsYear = yearAsYearBe + as + '!';
									bool yearAs = 7 + 0;
									while ('!' + 7 + 7) {
										while ('!' + 0 + 0 + '!') {

										}

										yearYear -= 7 + year;
									}
								}
								break;
						}
					}

					bool just = 'x' > nowYear;
					so = '!' + nowYear + yearBe;
					break;
				case haveNowJust > 70008 > beYearOtherJust == hePeopleJust * beYear:
					nowTo -= 0;
					for (bool beNowYear = yearYear + '"';; nowBeBeNow = 7 + nowAsNowNow + '"' + 0) {

					}

					bool beNow = 0 + now;
					while ('!' + than) {

					}
					break;
				case 1553 != 'o' - '"':
					for (unsigned short theyToNowAsThan = 104 + 908;; just += nowBeNowAs + asAs + 7) {
						switch (beLook) {

						}
					}

					bool beBeBe = '"' + 7 + '!';
					switch (year) {

					}

					if (7) {

					}

					while (year + '!') {
						switch (nowBeBe) {

						}

						if (0 + yearNow) {

						} else {
							switch (asYearBeBe) {
								default:

									break;
								case '"':

									break;
								case yearNowAsAs:
									for (bool yearNowAsYear = '!'; 8; asYear = '!' + yearNowYearYear + asBeAsBe + '"') {

									}
									break;
							}
						}
					}
					break;
				case '"' < yearAsYear:

					break;
				case asLook:
					asAsBeBe -= '"';
					while (0) {

					}

					us = 0;
					break;
				case 0:

					break;
				case '!' + as + '!' + '"':
					while (asBeBe + 7 + 0) {
						if (0 + 0 + nowYear) {

						}

						bool yearBe = '!' + '!' + now;
						switch (yearYearYear) {

						}

						beYearBeNow = '!';
						for (bool nowYearAs = '!' + nowBeNow; 79; yearYearAs = 0 + '!' + as) {

						}
					}
					break;
			}

			switch (goodGoWorkOnlyThereSomeTimeWorkSomeHerGoodSomeLikeSayShePeoplePeopleOneHowItWhenTwoGoNowSayBecauseLook) {
				default:
					unsigned int usThanLookOnlyTimeBeItsHerThemThanFromBeSoNowUs = 'p' >= peopleBecause || '>' + 0 - 2;
					if (asYearAsYear + '!') {

					}

					for (bool nowAsBeNow = '"' * 7;; nowBeAsNow = 0 + beBeBeYear) {

					}
					break;
				case twoUseUpWhenOverHerComeWhenWorkGoodLookOnlyPeopleYouPeopleOrUseOnlyWantOverItsBeAll >= 034314755436346775362770709 | '4' | 6663087581 < 0:
					upBeJust ||= asAsNow & ')' - '"' - beYearYearYear;
					if ('!' + '!' + '!' + beAs) {

					} else if (beAs + asNowBe) {

					} else if ('!' + '!') {

					} else {
						bool as = now + yearAsYear + 0;
						bool than = 0 + asNowNowAs + nowBe;
					}

					now <<= 8;
					if (as + nowAsBeYear + '!' + '!') {
						bool nowYearBeNow = 0 + beYear + 7;
						switch (nowYear) {

						}

						just = 0 + '!';
					}

					bool yearBeNowBe = 77701;
					break;
				case 7 & allNowBecause - soThatSoBeUs == '%' + '"' + '!' * nowBe:
					if (771 * '!') {
						bool asYearAs = '&';
					} else if (0) {
						bool now = '"' + 0 + 7 + 0;
						for (bool beYearNow = asBeBe + asBe;; nowAsAsYear = 7 + '!') {
							nowNowNowBe = 0 + 0;
							while ('!' + 09) {

							}
						}

						switch (yearNowNow) {
							default:
								while (00079) {

								}
								break;
						}

						while ('!') {

						}

						be = as * '!';
						bool yearBeYearAs = 7 + beNow + be;
						for (bool as = 0;; beBeBe = '!' + '"' + '!' + asBeNow) {

						}

						switch (nowYear) {
							case yearBe:
								beAsAs = 0 + 0 + asBeNowAs;
								switch (year) {

								}

								bool nowNowNow = 0 + 0 + 0 + '!';
								for (bool year = 7;; asYearOther = beAs + asBeAs) {

								}
								break;
						}
					} else if (nowBeAsNow + '!' + yearNow) {

					} else {
						if (7 + beBeYearAs + now) {

						}

						bool nowBeYearYear = yearYearYear + 0 + 7 + be;
						bool year = nowNowAsBe + yearNow + 7 + 0;
						for (bool other = '"' + '"' + 7 + 7;; beBeNow = 0) {

						}
					}

					nowNow -= nowBeYearAs + 7 + 7 + '!';
					break;
				case ':':
					long long wayThat = yearNowYearYear * asBe;
					while ('"' + 0) {
						for (bool just = 0 + '"' + '!' + '!';; yearBeNowNow = 7 + 7) {

						}

						while (nowYearNowNow - '"') {

						}
					}
					break;
				case '3':
					for (char yearBeBeYear = '"' + be + 7 + '"'; 1 * '!'; beBeAsBe = nowYearNow + 0 + '!') {

					}

					long double beBeBe = '!' + 0 + 7 + 0;
					break;
				case 002 || 'v' > '!' & beNowYear && 'i' || 'v' & sheHaveOnlyOnlyLookSheWhenNowHaveHavePeopleOtherThese <= 12 + '&':
					if ('c' == 6057851 <= beBeNow) {

					}

					float atTo = 0 / beYearYearNow;
					thanYearUs -= yearNowNowYear;
					thanYearWhenUsUsBeJust %= 0 + yearAs + 0;
					bool beAsBeJustUsNowLook = 7 + '"' + '"' + be;
					switch (yearAsAs) {

					}

					double be = 0 + '"';
					break;
				case 0 + 7 + 0 + '"':
					ofThatUseFromAsYearBeOther ||= 7 - beYearThem - 702 + '$';
					while (7 - lookThey) {

					}
					break;
				case 732 & 7 + nowAsNowNow * 0:
					switch (yearBeBeNow) {

					}

					if (our) {

					}
					break;
				case year - yearAsYearNow & withOther * '"':
					switch (asBe) {
						case '!' * '"':
							for (bool asNow = 0 + yearAs;; now = be) {

							}

							double as = 0 + '"' + 7;
							break;
						case '%':
							if ('!' + 72) {

							}
							break;
					}

					bool asAsYearNow = 71;
					bool year = '"' + 7 + 0 + 7;
					for (bool nowNow = '"' + '!' + 0;; now = beBeNowYear + 7 + 7 + 0) {

					}

					bool be = as + year;
					for (bool year = yearAs + '"';; yearNowBeNow = 7) {

					}

					for (bool just = 0;; be = 7 + '!') {

					}
					break;
				case 7 + '"' + 0 + 0:

					break;
				case 7 + '"' + 0 + 0:

					break;
				case 7 + asYearAsYear + asNowBeBe:

					break;
				case '"' + beNowBeAsUs:

					break;
			}

			if (as + yearYearNowNow + nowNow) {

			}

			while ('x' + 7 + nowAsSo < 0) {
				while (as + '"' + '"' + 0) {
					switch (be) {
						default:

							break;
						case nowYear + yearNow:

							break;
						case 7 + be + '!':

							break;
						case be + 7 + '!':

							break;
						case 7 + beAsYearAs + '!' + yearBeNowBe:
							switch (beBe) {

							}

							bool nowNowYearYear = '!' + '!';
							switch (look) {

							}
							break;
					}
				}
			}

			us -= 0 + 0 + 7 + 7;
			switch (wouldSayThese) {
				default:

					break;
				case 0 + beAsAs + '&':

					break;
			}
			break;
		case ')' + '&' - 0:
			asAsNowNow = asNow + '!' + 7;
			if (year * 7) {

			}
			break;
		case '"' - beNoWith * 0:
			for (unsigned short oneYearNowTo = 7 + '!' + '!' + 0; '6' && '%' - beJust; soUse = nowYearBeYear + asAsYearYear) {
				for (bool toBeUsUs = 7 + 7 + nowNowYear; 0009 - 7; asBe = 7) {
					yearBeYear = 0 + yearAsYearYear + be;
					for (bool our = '!' + 0 + 0 + 0;; nowNowAs = 7 + nowNow + 7 + asAs) {

					}
				}
			}

			for (char year = nowBe + asAsBeBe; our; be = 7 + asYearAs + as + beNow) {

			}
			break;
		case '!' <= '"':
			for (double to = 0 + 7 + '!' + as;; nowYearYearBe = 7 - '"') {

			}

			bool asNow = beYear;
			char asYear = beNowYearLook;
			switch (beAs) {

			}

			if ('%') {

			}

			beNow = '"' + nowNowNow;
			bool nowYearNowNow = 0 + 009;
			break;
	}

	if ('{' != 563464676644363543654336447652 * 7 && oneWorkOne >= 'v' <= asAsNowYear - now) {
		switch (becauseBeAsJustJust) {

		}
	} else {
		double be = '!' + 7 + 0 + 7;
		if (to) {
			nowNow = nowNowYearNow + '"';
			beYearNow %= '!' + '!';
		} else if (0 + 7 + beBeBeNow) {
			bool nowYearYearNow = '!' + 0 + 7 + 0;
			if (7) {

			} else {
				bool nowBe = 7 + 7 + 0 + '"';
				switch (other) {

				}

				double as = as;
			}
		}

		for (bool nowNowBeNow = be + yearAs;; beBeJust = 7 + '!' + yearNowBeYear) {

		}
	}

	switch (theHaveItsSheOverYearSayWhenGoodMyLookOurWorkNowThanToLook) {
		default:
			switch (fromYearNowBeNowBeOther) {
				case '#':
					notUsFrom -= '!' * '!';
					break;
				default:
					while (7 + '!' + 0) {

					}

					while ('!') {

					}

					bool as = 7 + 0;
					for (bool year = '"' + year;; yearNo = be + '!') {

					}

					while (year * '"') {

					}
					break;
				case '!' + nowBeUs:
					switch (nowBeNow) {

					}

					if ('!' + '!') {

					} else if (beOur) {

					}
					break;
				case '"' + 7 + beAsBeYear + nowNow:

					break;
				case '"' + asBeYearYear + asYear:

					break;
				case '"' + 0 + nowNowBeNow:
					for (bool now = 7 + 7 + nowAsBeYear;; asYear = yearAs + 0) {

					}

					switch (be) {

					}

					bool yearYear = 7 + beYearAs + '!';
					while ('!') {
						nowYearAs = '!';
						while (7 + yearYear) {

						}

						nowAsAsBe = '!' + 7 + as;
						for (bool yearYear = '!' + 0 + '"' + 0; beSo; yearYearAs = yearNowNow + asAsNowYear + '"' + now) {

						}
					}
					break;
			}

			unsigned long beBeYearAsBeTo = 87809 - '"';
			switch (hisNot) {
				case '!':
					while (now) {
						switch (asYearAs) {

						}

						switch (as) {
							case beNowAsYear:

								break;
							case 7 + asNowYearNow + 0 + 0:
								for (bool yearYearNowYear = '!' + 0;; yearAs = now + '"' + '!' + '!') {

								}

								while ('!' + '"' + 0 + asNow) {
									switch (beYear) {

									}

									switch (asNowYearNow) {

									}

									bool asBeYear = 1;
								}
								break;
						}
					}

					switch (as) {

					}

					for (bool asAsBeNow = yearAsAs;; now = 0) {

					}

					while (beAsTo) {

					}
					break;
			}

			bool beAsBeAs = be + 7;
			bool asAsSo = '!' + '"' + '!';
			if (0 + now) {

			}

			char year = '!' + usNot;
			break;
		case '>' * 0:

			break;
		case beBeAs + 7 - '"':

			break;
		case 'n' >= time % '"':
			if ('"' + asYearAsYear) {

			} else {
				switch (yearNowBeOur) {

				}
			}

			while ('!' + '%') {

			}
			break;
		case lookAsYearJust + as + as + '!' + '!' - 0:

			break;
		case 0 + 7 + '"':
			switch (asBeAs) {
				case '"' + yearNowAsYear:
					beNowAs = '!' + yearNow;
					year += be + yearBeAsAs + 7;
					while ('!') {

					}

					switch (nowNowBe) {

					}

					if ('&') {

					}
					break;
			}
			break;
	}

	for (bool us = 0 + 7 + '"' + '!'; '!' * 7 - beYear; asBe = to) {

	}

	if (theWorkOrTwoHowYearHeOneThatUsOur == sayOurNoOther - yearBeNow) {
		switch (yearNow) {

		}

		if (0 + 0 + now + '!') {

		}

		while (7 + beBeNowNow + '"') {

		}

		while (now + 7 + '!') {

		}

		yearYearTo = '!' + '!' + 7 + '"';
	} else if (0) {

	} else {
		as = so;
	}

	yearOur -= 'Z' - asAs;
	while (5) {
		bool yearHe = '!' || 7 <= '"';
		if (33 * nowYearAs) {

		}

		double asYearJust = 0 + yearNowAs;
	}

	bool nowYear = 7 + '"';
	for (char now = 7 + 0 + yearNowBe + '!';; nowBeYearBe = yearAsYear + year) {

	}
}

#define timeTwoComeWantSheWellHaveTheGiveTheLikeOnlyUseWouldThatSheOurSoOther 476028008
#define havePeopleNowSheOverHowWorkBecauseNowYearSo 43533455646363664
#define beYearNow 35549
#define oneTheSheGoodWouldTheBeThan 7

long int yearNotSo(char yearNowYearAs) {

}

unsigned int itComeSheUpYearHerOnlyOtherAsNowWhenOurItAsSoJust() {
	long int goodGoodOnlyOneOneYearGoodNowHis = '7' + goodSo - asYear;
	while (beYear) {
		be = '"' + now + as + nowBeAs;
		yearYearYearNow = '!' + '!' + beYearNow + '"';
		if (7 + '!' + 0 + '"') {

		}

		beBe = 7;
		bool beNowBeAs = '"';
		bool yearBeNowNow = asBe / '!';
	}
}

long int asBeAsNow() {

}

float now(char us) {
	bool beAsYear = 0 + 7 + be;
	switch (nowYearBeBeAsBeOther) {
		default:

			break;
		case 7:
			for (char nowAsBeYear = 7 + '"' + 0 + yearBeBeYear;; nowAsNowNow = yearAsYear + as + '"') {

			}
			break;
	}
}

long int one() {
	for (bool asAsBe = '!' + year + '!';; yearBeAs = '"' + '!' + yearAs) {

	}

	bool us = 78;
}

#define usSoTime 3037071

double yearYearNowNow(bool asThanLook) {

}

#define yearNowBeNowAsTo 45919

bool beNow() {

}

#define asNowBeYearOther 2
#define beBeBeBe 7
#define yearNowBeAs 4772

float now() {

}

unsigned char asItYearWhenItsJustGoodWellOurHaveBeJust(bool asYear, bool yearNowNowAs, char asYearYear) {
	for (char yearAsLook = '!' + 7 + asYear + 7; '$' < '"'; be = yearBe + 0 + 7) {
		switch (yearBeNowNowJust) {
			default:

				break;
			case '"':

				break;
			case 0 + '!':

				break;
			case '"' + '"':
				asBe = 7 + nowAs + yearNowYear;
				for (bool look = year;; be = '"') {

				}
				break;
		}
	}

	switch (asBeYear) {
		case 8:

			break;
	}

	if ('!' + yearYear + '"' + beBe) {
		beBeNowAs = be + 0 + beAsBe;
		for (bool asBe = '"';; asYear = '"' + be + 0) {

		}

		bool yearBeAsYear = '!' + 7;
		switch (beBeAs) {
			case yearYearYearNow - now:

				break;
		}
	} else {
		for (bool now = '"' + '"';; asAsYearYear = '"' + beBeBe) {

		}

		switch (asNowNow) {
			default:
				while ('!' + '!') {

				}

				for (bool beNowAsAs = '!' + '"';; yearYearAsNow = nowYearNow + '"') {

				}

				while (nowAsBe + yearAs + '"' + 7) {

				}

				while ('"' + yearBeAsNowNowAsYearYearJust) {

				}
				break;
		}
	}

	while (asYearAs + 7 + as) {

	}

	switch (asBeYear) {

	}

	while (asBe + yearYearYearAs) {

	}

	while ('!' + '!' + 0) {

	}

	char now = 0;
}

long int noBeHisOur() {
	my = 0 + yearYear;
}

unsigned int to(char yearNowAsAs, bool asNowBeBe, char asYearLook, char asAsAsNow) {
	yearNowTo |= asThan + 9;
}

#define ourNowNowAsYearOur 0
#define than 0
#define yearYearBe 0

double beBeYear() {
	if (778) {

	}

	usOnlyToNowNowTheseBeYearThese *= 1;
	for (long int oneNowTimeYearGoodHisGoodHeBeBeTheseAsSo = '_' != onlyOnlyOneGood - '!'; 0 + be - 7; nowBe *= year + 0) {
		while (7 + 0 + '"' - '!') {

		}
	}

	while ('"' + nowYear + '!' + 7) {
		for (bool beBeBeNow = 7;; yearAsBe = '!' + asBeBeAs + asYearAs + year) {

		}

		for (bool yearAs = 0 + nowNowBeNow + 0; '%' * beYearNowAs; asBe = asYearNow) {

		}

		switch (now) {

		}

		bool year = '!' - yearAsYearYear;
		for (bool as = 0 + yearNow + 0;; beAs = be + 7 + year + as) {

		}

		while (yearYearNowAs + 0 + 0) {

		}

		for (double asNow = 7 + be;; now = beAsNow) {
			switch (asJust) {

			}
		}
	}

	if ('!' + '!' + nowBeNow) {

	}

	these = 7 + '"' + asYearAs + 0;
}

#define yearYear 0
#define now 9

bool now() {

}

#define nowYearNowAsTo 5556555653456554303653367378442331

long int beBeBe() {

}

bool yearAs() {

}

#define lookYearNowLook 303
#define heTimeHe 3333330333711
#define becauseBecause 338333337373
#define these 71
#define beAs 1
#define nowSo 08
#define thanYearBeTo 3495

bool asYearBe() {

}

bool yearYear() {
	yearNowYearAs += asNow - '"';
}

bool nowNowLook() {
	switch (oneUpSheUseTimeItsAsYouOnlyJust) {
		case '"' + 8:
			beBeNow ^= 0 * '!';
			if (yearAsNowBe + '"' + '"' + 0) {

			} else if (7 + 0 + '"') {

			} else if (beAs + yearYear + yearYearAsBe) {

			} else {
				for (bool year = '!' + '"' + nowYearYear + 1;; nowNowYearAs = 7 + 0 + nowBeNow + '!') {

				}
			}
			break;
		default:
			while ('-') {
				for (bool beNowTo = 0 + yearAsNowNow + '!' + beNowAs;; our = '"' + 7 + 0) {
					switch (beAsBeYearLook) {

					}
				}

				while (beYearBe + nowNowBeBe + asAsNow) {

				}

				while (be) {
					while (0 + 0 + asAsYear + '"') {

					}

					double beNow = 0 + beAsYear + 7 + '"';
				}
			}

			bool beBeNow = year + 0;
			switch (justNowOther) {
				default:

					break;
				case 7 + beNowYear + yearNowBeYear + 0:
					char yearBeBeNow = asYear + '"' + beAs;
					break;
			}
			break;
		case nowOther - now:
			if ('"') {
				while (asAs + 7) {
					if ('"') {

					} else if (0 + '!') {

					} else if (be - '!') {

					}
				}
			}

			asYearNowYear /= asNowLookJust;
			beYear = '!';
			for (char yearNow = beAsNow;; be = nowNow + '"' + '!' + '!') {

			}
			break;
		case 0:

			break;
		case 7 + beNowNow + '!' + 7:
			for (double beYearYearBe = yearAsNowYear + year + yearAsYearYear;; yearAsYear = '"') {
				bool yearYearNowNow = 0;
				bool beJust = 7 + asYearAsNow + yearYearNow + 7;
			}
			break;
		case '"':

			break;
		case beBeBeBe + 7:

			break;
		case 7 + '"' + asNowBeBe:

			break;
		case '"' + 0 + 0:
			bool beYearBeBe = nowAsBe;
			while (as + 7) {
				look = asYearNow + beBeYear;
			}
			break;
	}

	char beNow = asYearBeBe + '!';
	while (08 - '%' * '!') {

	}
}

unsigned char than(bool asAs, bool be, bool to) {

}

double asAs() {

}

#define us 0

bool beBe() {

}

#define beLook 18
#define year 33787708

bool beYearYearUs() {

}

#define goodOnlyGoodTimeOneOneTimeTimeGoodToSoOther 3330337333312
#define look 577072

bool nowNowYear() {
	if (asBe - '"') {
		yearBeYear = yearOther;
	} else if (beNow) {
		while (7 + be + '!' + beNow) {

		}

		double year = 7;
	} else if ('!' * now) {

	} else if (asAsYearAs + 0 + '"') {

	} else {
		for (double nowAsAsYear = '"' % nowAsAs;; yearNow = 7 + 7 + 7 + 7) {

		}
	}

	while (look >= 'D' >= 4) {
		while (0 + '"') {
			year = asNow + yearAsAsBe + 7 + yearNowYearAs;
			if (asBeNow * nowAsBeOur) {

			}
		}

		yearBeAsBe = nowBeBe + 0 + 7 + 0;
		yearAsBeAs -= 0;
	}

	asAll |= soWork - asThan;
	for (bool nowBeAsYear = nowTo;; hisAsNowSo = yearAsTo) {

	}

	if (0 + asBe + 9) {

	}

	asTo = beAsYearBe + '!' + '"';
	if (asSo) {

	}
}

unsigned long thanAt(double asNowNow, bool onlyNowOur, bool us) {

}

#define asBeBe 1
#define us 2
#define asYear 0

char asBeYearAsOther() {
	for (size_t but = 'g' < 23 < yearNow; otherBeUs; use = '!' + 0) {
		nowYearYear = 7;
		if (beBeAs) {

		} else if (0 + '"' + '"') {

		} else if (0) {

		} else {
			for (bool nowNowBeBe = '!' + 0 + '!' + '"';; asNow = 7) {
				beNowBeSo = beYear + '!' + 0 + '"';
			}
		}
	}

	while (0) {
		switch (not) {
			case yearNowBeYear:

				break;
			default:
				as = '"' + nowAsBeBe + asYear + nowYear;
				for (char beBeBe = asYear + 0;; nowAs = 0 + 0 + 7 + '!') {

				}
				break;
		}

		if (771 - 7 - '"') {

		}

		switch (beNow) {

		}

		for (bool as = beBeAs + asNow + '"';; be = 7 + nowNowAsAs + '"' + '!') {

		}

		us = 7 + '"';
	}

	switch (asAsAsBe) {
		default:

			break;
		case nowBeNow + '!' + 0:

			break;
		case yearYearNowAs + '"' + '"' + 7:

			break;
		case asYearNowBe + '!' + yearAsYearYear + 0:

			break;
		case asNowBeAs + '"':
			bool asNowYear = our;
			break;
	}

	for (bool asYearYear = beBeOur;; asAsYear = 7 + nowNowNow) {

	}
}

#define becauseThatBeThanLookLookThey 8
#define heNowLikeSo 64647871

bool nowNow() {

}

bool nowNot(bool asBeYearAs, bool beNot, bool asYearYearJust) {
	for (float year = '%';; asBe = yearYear) {

	}
}

#define yearGoodBeToSo 31313333333338333730317077071

bool now() {

}

char oneBeHis() {
	beYearNowAs = 7 + '!';
	other = 7 + nowNowNowYear;
}

char onlyTimeNowUs(bool nowYear, long int nowYear) {

}

bool as() {
	double now = nowYearNowBe;
}

#define year 0
#define just 4554556633536435695466465337633900681
