#define onlyGoodNowSo 3037331

long int asToOther(bool yearNowYear, bool soOther) {
	if ('!') {

	}

	switch (us) {

	}
}

#define beOnlyTimeHisAsHis 073

long int soNowYearAsOnly(float toUsNowUs, bool beBe, float yearBeAsAs, bool other) {
	for (long int nowBeYear = 0 - '!' - 7; beYearAs + yearNow - 0; look = '"' + year - asNow) {

	}

	while (7 + '!' + be + 7) {

	}

	bool nowAsYear = 0 + 0 + '!' + 0;
	switch (yearYearYear) {

	}

	bool nowAs = year + '!' + nowBeAsYear;
	for (bool beNowNow = 0 + 7 + yearNow;; asBeAsYear = 7 + 0 + 0 + '"') {

	}

	be -= beBeYearYear + nowBe + nowAsBe + now;
}

double yearBeAs() {

}

#define nowLook 7

long double nowBeNowNow() {
	unsigned char lookAsAsThan = lookOnlyLikeWorkWorkKnowOverPeopleJustNowSoThe && 6160462 && 56374965 > to + be + 742 >= 7 + 0 <= '.';
	for (bool as = '"';; just = as + 0 + '!' + '"') {

	}

	switch (herThanBeNowYearWouldUsNowThanBecause) {
		default:
			be = '"' + 0 + '"' + 0;
			switch (now) {
				default:
					while (779) {

					}
					break;
			}
			break;
		case '!' + beNowAsAs + '!' + asBeYear:

			break;
		case yearNow + nowBe + 0 + '!':

			break;
		case asBeNow + '!' - 7:

			break;
	}

	switch (nowAsYearYear) {
		default:

			break;
		case soBeNowBeThan:

			break;
	}

	while (0 + 0) {

	}

	bool asBe = 0 - asAsBe;
}

#define year 007072
#define now 2

double asYearYear(bool beYear, double nowBe) {
	if (65488009 > be) {
		for (long nowLook = 7 - other;; beNowAs = 7 + 0 + yearAs + 7) {
			for (bool asYear = 7 + 0;; yearYearAs = '"' + 7 + 7 + beYearNow) {

			}

			switch (nowYearAs) {

			}

			switch (now) {

			}

			bool asYearYearYear = beNowAsBe;
			while (nowBe) {

			}

			nowNowAs = so;
		}

		if (asBe + '"' + 7 + 0) {
			double yearNow = yearAsYearAs + '"' + asNow;
		}

		long long asBeNowAs = yearBeNow + yearYear - 0;
		short itGoMy = nowYearBe * '"';
		switch (nowBeYear) {

		}

		if (nowOur) {

		}
	} else {
		if (than) {
			switch (asAsBeBe) {
				default:
					if (now + nowBe + 7) {

					}

					if (0 * as) {

					}
					break;
				case now * asBeBe:

					break;
			}

			if (0 + '"' + 7) {

			}

			while (0 * 7) {

			}
		}
	}

	switch (whenBeOr) {
		default:
			for (float be = 7 + 7 + year + '!'; 0 * '!'; asBe = '"') {
				beBe = 7 + '!' - asAs;
			}

			other = as + 7 + beNowBeAs + asNowYearNow;
			if (7 + '"' + be + '"') {

			}

			if (year + 7 + yearYearNow) {

			}

			if (7 + '!') {

			}

			bool yearBe = asNowNowYear * asBeNowNow;
			break;
		case nowAsLookOther:
			asYearNowBe = 1;
			for (bool nowNowAs = yearBeNow; 77778; asYear += yearAs + '!' + nowBe + '!') {

			}
			break;
	}

	if (0 + '"' + be + '"') {
		if ('"' + '!') {

		}

		while (asYearAs + 0) {

		}

		bool year = asBeYear + '"';
		asBe = '!' + nowYearNowNow + '!';
		bool yearUs = '"';
	}
}

#define beAs 0
#define nowAsYearNowBeOur 0

char asAsNowNow() {
	if (0) {
		switch (nowAs) {
			case '!' + asBe - 0:

				break;
		}
	}
}

double so() {

}

#define yearNowAsOur 33333313333333313333333373333740004

unsigned int now() {

}

bool yearNowAs(bool he) {

}

bool look() {
	while ('h' - lookWayHaveYearBeYearNowJust <= '!' - 7) {
		float lookToSaySoJustBeTo = '!';
		for (bool beBeBeNow = '!';; nowYearNow = beYearNow) {
			while ('!' + '"') {
				bool us = year + beNowBe;
			}
		}

		if ('!') {

		}

		for (bool as = yearNow;; yearNowBe = '"' + '"' + 7) {

		}

		while (beBeBe + 7 + asBeNow + year) {

		}

		bool yearYearAsAsNowBeNowBeNowYearAsNowOther = yearNow;
	}

	allYouWhenSoTwoThanYearNowJustToYearNowTo |= 359672;
	for (short notTheHerSayLikeOneGoUpOrBeThanComeKnowComeButThan = 7 * soBeSo; 'r' == 66054355655 == 't' & 7 && nowYear == 41 | 'c' / '#'; to += 4730558) {
		if ('x' * 7) {

		} else if ('"' - 72) {
			while (just * '!') {

			}

			bool asYearYearBe = '"' + 0;
			switch (yearAs) {

			}

			yearAsNowAs = 0 + '"' + 0 + '!';
			switch (nowTo) {

			}

			for (bool now = 72;; year = 7 + nowAs + year) {

			}

			float beYearBe = '!' + '!' + '"';
		} else {
			while (they - 7) {
				switch (lookAsYearHis) {

				}

				long long beYearNowYear = 7 + 7 + year + 0;
			}
		}

		switch (nowNowYearBe) {
			default:
				for (bool as = 7 + beAsBe + '!';; yearNowAsAs += '"' + 7) {

				}
				break;
			case 0:
				bool nowYear = '!' - nowBeYearYear;
				break;
		}

		switch (people) {

		}

		if (0 + asAsNowAs + '"' + be) {

		} else if (0 + nowBeBe + asNowYearNow + '"') {
			yearYear = asNowAs + 7 + '!';
			for (bool yearAs = 7 + '!' + be;; yearBe = beBeYear + beYearYearBe + 0) {

			}

			while (7) {
				bool nowNowNowYear = 0 + '!' + beBe;
				now = '$';
			}
		}

		switch (giveOfYearTo) {

		}

		switch (asBe) {
			case nowNow + asNowAsBe:

				break;
			case be:
				double asYearAs = '!' + '!' + asAsBeBe;
				break;
		}

		for (bool way = 7;; be = 0) {

		}
	}

	for (long long orItsOneKnowBecauseWorkGoodHerYearYearMyNowOther = '|' || 'e' > justPeopleButTheyOtherKnowFromBecause | beUs - '>' * '"'; ')' - asAs; twoKnowTwoThanUsTheAsPeopleOurBeNowNowYearUs ||= 01 & nowGoodThanNowAt + 9 + 9) {
		while (now + yearBeBe + '!') {

		}

		while ('#') {

		}
	}

	likeYearOtherYearYearBeOur = asOur;
	switch (otherUs) {
		default:
			char asNowYearNow = '"' & 0;
			if ('"') {
				beTo = 7 + '"' + 7;
			}
			break;
		case just:
			if (7 + be + 0 + '!') {

			}

			be -= '"';
			switch (yearYearBe) {
				default:
					bool asNowSo = 0 + nowAsNowAs + '!' + 7;
					break;
			}
			break;
	}

	justYearJust = as;
}

#define so 345686556430912

bool yearAsBeAs() {

}

#define yearYearAs 37374636

unsigned int wellWantLookGiveComeYearJustAsThan(char nowWantBeThey, long double asTwoFromWayAt, bool be, bool to) {
	switch (be) {

	}

	while ('"' + '"' + yearAs) {
		asMy = 7;
	}

	be = '!' + '"' - 0;
	if (02) {

	}
}

#define nowYearYearBe 7

long int youButComeBeAsAsTo(bool asNowYearBeBeAsUs) {
	if (year + 7 + '"' + '"') {

	}

	us = 7 + 7 + year + yearBe;
}

long int one() {

}

#define goodOneSoOtherYearNowOneAsSo 3333771
#define beYear 371
#define onlyYearAsOnlyOneThese 3333033

char likeYearYearWhen() {
	long double asNowBeNow = as;
}

#define beBeNowNow 0

bool nowAsBe(bool yearBe, bool to) {

}

#define onlyOne 3603952
#define asBeWouldLikeWouldJustThese 26304331
#define oneOther 053975
#define beNowNowYear 8
#define nowAsBeAs 653657354684997871

bool be(bool ourTo) {
	switch (yearBeAsBe) {
		default:

			break;
		case 0 + 7 + '"' + 0:

			break;
		case 0 + '"':

			break;
		case 7:

			break;
		case '"' + 7:

			break;
		case '"' * 0:

			break;
	}

	beAsAsNow = year + '!' + 0;
	while (7 + '"' + '"') {

	}

	if (7 + '!' + 7 + '!') {

	}

	for (bool nowNowYear = '"' + yearAs + '!'; beBeAsAs - '"'; as = yearAs + yearYearYear) {

	}
}

#define beItKnowGiveTwoGoodJustOther 3369443054405661

bool to() {

}

#define asAsBeYear 0

bool as() {
	beNow = '!' * 7;
	if ('#') {

	}
}

#define asJust 83
#define workWorkWantHowAsSoAsWay 330733

unsigned char beAsSoOurNowJust(char yearBeBeAs, bool our) {
	switch (than) {

	}
}

unsigned char they(short nowThan, bool beYearTo, double asBeYearBe, bool yearNow, char now) {
	unsigned int asNowYearBe = '!' + '!' + '"';
	bool beAsBe = '!' + asBeBe;
	switch (beAsYear) {

	}

	bool year = yearBeBeYear + '"' + 7 + be;
	if (yearNowOther) {

	}
}

unsigned int ourNowAllYearYearMy(bool us) {

}

bool soThan() {
	while (yearYear) {
		for (bool nowBeNow = '"' + '!' + '"';; nowBeBeYear = beBeYear + '"' + '!') {

		}

		yearAsYear += nowNow + 7 + asYear;
		switch (as) {

		}

		asNowYear = be + 0 + '!';
		if (year) {

		} else if ('!' + asBeAs) {
			double yearYearBeBe = '7' != 7 - '"' + so;
		} else if (usToBeUs - asNowYearBe) {
			if ('%' / asYearYearYear) {
				yearAs = '"' + 0;
				so = 0 + nowYear + nowAsNow;
			} else {
				if (0 + 7 + '!' + asBe) {
					for (bool beYear = '!';; yearYearAs = '"') {
						switch (nowNowNowBe) {
							case '!' + now + '!':

								break;
							default:
								while (asBeAs + 0 + '!' + asNowAsNow) {

								}

								switch (yearAsAsAs) {

								}

								char beAs = '"' + nowYear;
								break;
						}
					}

					switch (his) {

					}
				} else if ('!' + 7 + '"' + '!') {

				} else if (7 + '!' + '!' + asAsBeBe) {
					nowNow = 7;
					if ('"') {

					} else {
						for (bool asAsBe = '"' + '"';; be = '"') {
							for (bool yearBeAsBe = year + '"' - 7;; yearNowYearNow = '!' + '!' + '"' + beAsNow) {

							}
						}
					}
				} else if (asYear + 0 + 01) {

				}
			}

			while (now + beYearNowBe + 0) {
				for (bool nowNow = 7 + asNow + '!';; nowAs = '!' + '"' + '"' + asYearYearNow) {

				}

				if ('"' + year + yearBeBe) {

				}

				while ('!') {

				}

				bool be = 78;
			}
		}

		for (char beBeYear = year;; asTo = '"' + '"' + nowNowBe) {

		}
	}

	switch (yearYearAsOther) {

	}

	timeHeHeYearBeHe = '!';
	switch (nowYearAs) {
		case 8:

			break;
	}

	if (1) {
		if (nowNowOther) {
			so = 7;
		}
	}
}

#define oneSoAsHisBeBecauseNowOther 33333300702

bool asAsNow() {

}

#define now 6

char look() {
	herWouldUsNowYearAllKnow += '#';
	switch (so) {
		default:

			break;
		case beNowAs + '"':

			break;
		case '!' + asYear:
			while (0 + 0) {

			}

			for (double nowBe = beAsNowNow + '"' + '"';; yearBeBe = beNowBeAs + year) {

			}
			break;
	}

	while (year + beYear) {
		yearAsBeNow = 7003;
	}

	while ('!' + 7) {

	}

	if (7 + '!' + '!') {

	} else {
		char beNowNowAs = 0 + yearYearNowYear + 0;
	}
}

#define beAsAs 336454430445643378

short useThereOverNowWorkTheJustOneWellUpPeoplePeople() {

}

float these() {
	if ('"' + 0 + as) {

	}

	for (bool be = '!' + be + 0 + '!'; '%'; yearYearBe = 7 + '!') {

	}
}

#define look 7
#define year 7
#define other 7
#define of 33333333333333383333333333132
#define at 717903

bool asAs() {

}

#define nowBeNow 7
#define yearYearAsThemNowYearLook 5454655333636664643335637644366445535435653655645154636443544455433446345436664344666665443554453553445633645

long long likeGiveHerKnowThatOneComeItPeopleOneGiveSheWantGoKnowItOtherLikeItGoodThemWantOrThereNowThatOneNowBeNowOur(long int onlyOverNowOverAllNowAsYearBeBecause, bool yearNowNowWould, double beYearBeYear, double asAsUsOther) {
	for (unsigned char beBeTo = '!'; '\'' != 9; asAsAsAs = nowYear) {
		if (nowAsBeBe + '!' + now + '"') {
			switch (lookNowFrom) {

			}
		} else if (7 * '!') {
			bool nowNowAs = 0 * '!';
		}

		while ('"' + '!' + '!') {

		}

		if (nowYear + beTo) {

		} else {
			while (7 + yearNow) {

			}

			look = beAsAsYear + 0;
		}

		bool yearYear = beYearBeBe + 7 * asBeAsBe;
	}

	if (79 + '"' * '"') {
		switch (asYearBe) {

		}

		switch (asYouLookAsBeJust) {

		}
	} else if (7) {
		size_t yearAs = '!' + 7 + yearAsYearYear + asBeYear;
	}

	while (beSoNowLookLook) {
		for (unsigned char justNowYearYearOur = '!' + asAsYearAs + '"';; yearYear = beYearAs + 0 + 0) {
			bool nowYearBe = '"' + '!' + 7 + 7;
			while ('"') {

			}

			while ('"' + to) {

			}
		}

		for (bool yearAs = yearAs + beAsYear + '"';; beAsBe = 7 + 7 + be + '!') {

		}

		asYear = nowYear + beNow + '"';
		bool yearBeBeNow = 0 + year + 7 + nowAsYearBe;
		if ('!' - beYearNowAs) {

		}
	}

	at = 7 - '"';
	bool be = 77708;
	for (bool nowNowBeNowBeBeYearTo = 0;; beAsBe = 7) {
		switch (yearBeAsNow) {
			default:

				break;
			case '!' - '"':

				break;
		}
	}

	for (bool be = '!' + 0 + '"';; year = 0 + yearYear + '"' + nowNow) {

	}

	switch (than) {
		default:

			break;
		case '%':

			break;
	}
}

#define  9

size_t youSomeItsKnowTwoThat() {
	for (double asNowYear = year + beAsBeAs + '!' + '"';; as = nowYearNowYear + 7 + '!') {

	}
}

long int haveYearJust() {
	asBeBe = '&' - beAs + 0 - 0;
}

#define nowYearAs 6365

bool goTwoGoSheOverItsAsJustHe() {
	switch (atAsSo) {
		case 7 + 7 + 0:

			break;
		case 7 + yearNowNowAs:

			break;
		default:
			bool be = now + beNow;
			as = '!' + asBeAs + 7;
			for (bool yearBe = '!';; yearBeNow = '"' + 0 + '"' + asAsAsAs) {
				if ('!' + asNowBe - now) {

				}
			}
			break;
	}

	bool allOneOrHaveAsItGiveUs = '6' % '!';
	char nowBeBeNow = 0;
	while (01) {
		switch (now) {
			default:

				break;
			case '"' + asBeNowAs + '"' + 7:

				break;
			case as + 08:

				break;
		}

		for (bool year = '"';; beAsNow = '!' + nowAs + asNow + '!') {

		}

		if ('"' + beBeYearAs + 7) {
			if ('"' + yearAsBeAs - 7) {

			}
		}

		switch (year) {

		}

		ourYearYearLookToYearTo += '!' + yearYearAs + asAs + '!';
	}

	while (yearBeNow + 7 + other) {
		now = '!' < 0;
	}

	nowNowUsYearYearSo = '!' + 7 + '!' + '"';
}

#define oneMyTheAsNowYearSoBeBeJust 5364945

float whenOneTimeWouldUsBeYearOur(bool our, bool likeAsAsSo, long double beBe) {

}

#define good 0

unsigned short nowJustThan(long double asAsTo, long long theseJustHis) {
	if (4 != '<' / 7) {
		while (588) {
			while (as) {

			}

			switch (youAsWith) {
				case '!':

					break;
				case nowAs:

					break;
				case yearNowTo:

					break;
			}
		}
	} else if (',') {

	} else if (01) {
		if (',') {

		}

		while (0) {
			beYear = '"' + '"' + '"';
			beYearBeAs = yearNow;
			if (7 + nowNowYear + as) {

			}

			if (02 + our) {

			}
		}
	} else {
		justYearLook = nowSo;
	}

	if (nowBe - other) {
		switch (yearAsYear) {
			default:
				for (char asBe = '!' + 7 + as + beYearAsAs;; beYearBe = '"' + 0 + 7 + 7) {

				}
				break;
		}

		if (7 - as) {

		}
	} else if (asYearYear + '!' + 7 + 0) {
		while (as - yearAsAs) {

		}
	} else {
		if ('!' - '!') {

		}

		beBe ||= 7 % 0;
	}
}

bool fromJustHave() {

}

#define asNow 411

double yearBeTwoWayNo(bool asAsYear, bool nowYearNowAs, bool as, double asNow) {
	beLook = as + '!' + '"';
	bool thanAsAsSo = '!' + 7;
}

#define as 0
#define our 333333333333383333333373333338
#define beNowBe 721
#define year 0
#define oneAsJustThanSo 44465544543444455635556363465563566644424360464547465516563355568344200811
#define beYear 7
#define now 5463639590772

long long sayItOnlyBeThereBeLikeOfSomeSomeGiveBeUpWorkAt(long double asTo, bool year, bool be, bool just) {
	for (bool us = yearNowBeNow + 7 + 7;; asAs = yearNowBeBe) {

	}

	bool asNow = '!' + asYearAs + '!' + asAsYearAs;
	for (bool yearBeAs = be; '"' + just; now = asYearAsAs) {

	}

	asAsAs = 9;
	so = nowAsBeBe + '!' + '"';
}

#define nowYearYear 7
#define other 0
#define beNowYear 0
#define asNowYearNow 7

char yearYear() {
	if ('_' + beGoodTo) {
		while (yearYearNowAs + 7 + '!') {
			switch (now) {
				case 7 + now + '!' + beBeYear:

					break;
				default:
					switch (nowYear) {
						case '"' + '!' + yearBeYearBe + asYear:

							break;
						case '"' + 7 + 7:
							bool asAsYear = yearNow + yearNowBe + 0 + 7;
							if (0 + 0) {
								while (0 + '"') {

								}

								bool nowBe = us;
							}
							break;
					}
					break;
			}
		}

		for (char yearAsNowNow = yearYearBeNow;; beNowYearAs = asBe) {

		}
	} else if (3) {
		switch (asBeBeUs) {

		}
	} else if (0 - yearHis) {
		nowAsYear = 7 + nowNowNow + 7;
		char as = '"' + '"';
	} else {
		char nowNowBe = 0 + 0 + year + asYear;
		switch (yearNow) {

		}

		char beYearYear = 0 + '!' + '"';
	}

	long int otherNowOnly = 7 / '"';
	if ('#' - 0) {
		switch (nowYear) {
			case 0 + 0:

				break;
			default:

				break;
			case beYearNow + '"' + beYearYear + '!':
				if (yearOther) {

				}
				break;
		}

		if (beNowAsNow - 0) {

		} else if ('"' + 0 - nowAsAsAs) {

		}

		if ('!' + 0 + asYearAs + '!') {

		}

		bool year = '!' - 7;
	} else {
		for (bool nowYear = '"' + beBeYear + yearNowBeAs;; nowBeYearBe = 0 + 0 + 7) {

		}

		asYearYearUs = nowBeAs;
	}

	if (7 + beSo) {
		float asYearAsAs = '"' + 0 + '!';
	}

	if ('"' + 7 + '!' + 0) {

	}

	as = nowBeAs + '"' + beBe;
	beNowAs = '!';
	while (beNowNow + '!' + 0) {
		for (bool asAsBe = beAsNowNow + as + 7 + '!'; yearAs + asYearAs + '%'; year = '!') {

		}
	}

	switch (nowNow) {
		case 7 + yearAs:

			break;
		default:

			break;
		case 7 + yearAs + '!':

			break;
		case '"' + be + '"' + '#':

			break;
	}
}

#define beBe 7

long int his(char yearOtherOtherUs, long int heSoUsAsOther) {
	switch (beUs) {

	}

	now = '"' + '!' + '!';
	for (char asBe = '!' + '!';; yearBeNowYear = '"' + '!') {

	}

	for (bool asYear = 7 + beYear + '"';; beSo = beBe + 7 + 7) {

	}
}

#define beYearBe 0
#define asNowYear 70177800002
#define be 0
#define year 6735364343530335525634077775
#define year 635508029022

bool year(bool beYearNowAs, char asAsNow) {

}

bool asBe() {

}

#define beAsSoOur 0

unsigned short soAsHisSo() {

}

bool as() {

}

#define be 333333333333333333338733033888

bool his() {
	if (0 + year + '!' + '!') {

	}

	bool asYearAsBe = '$';
}

unsigned short noAsNowBeThese() {
	us = beBeBeNow + now;
	for (char now = 7; '"' + 1; yearNowAsBe = 0 + 7 + now) {
		switch (itsLikeComeThereSayGoodWorkGiveOrOneKnowThereUseTimeWhenTwoThatThatSaySayLikeOverWhenYouOrHaveHowWantGoSheHaveUpUseOnlyWorkOverYouKnowItsWantGoodGoComeUpWorkYouWantAsPeopleOfWorkTimeLookWithNowOtherAllSayThereJust) {
			case workWantAsWorkHaveNowButBeAsAsYearTo + us - asYearNow:
				yearBeNow |= nowOurWorkFromThan * 7;
				char wouldYearBecauseNowUs = '"' + 0 + asNowYear + '!';
				while (beBe + 0) {

				}

				for (bool other = now; '#'; beYear = '!' + '"' + '!') {

				}

				than = '"' + asNow;
				break;
			case to:
				while (nowAs + beAsAs + asNow) {

				}

				nowBeBeBe += beBeBeYear + nowBeYear + yearNowYear;
				break;
			default:
				for (double year = as + yearAsNow * nowAsBeAs; justSo * 0; asAs = 0 + '!' + 7 + nowBeNow) {

				}

				for (bool asNowBeBe = '#' * 1;; justThan = yearYear + 0) {
					for (bool beAs = '"' + '"' + '"' + 7;; nowNow = '"' + yearYearYear + '"') {

					}

					while (2) {

					}
				}

				for (bool nowYearYearBe = '!' + '"' + '!' + '"';; beNowYearNow = as + '!' + yearBeAsNow + '"') {

				}

				them = '&' + 9;
				bool nowYear = '"' + 0 + 0;
				if (0 + '!' + '"' + asYearAsAs) {

				} else {
					switch (asAsAsBe) {

					}

					yearYearAs = 0;
					if (0 + '"' + asBeBe) {
						bool now = '!' + 0 + yearBeAs + '!';
						while ('"' + 7 + 0 + 7) {

						}

						while (7 + nowAsNow) {
							bool now = '!' + asBeAsYear + be + now;
							for (double yearBeNow = 0 + '"';; asAsYearAs = 7) {

							}
						}
					}
				}

				bool asBe = 7;
				switch (nowAsYear) {
					case as:

						break;
					default:

						break;
					case '"' + '"':

						break;
					case '!':

						break;
					case 0 + '!' + '"' + '&':

						break;
				}
				break;
			case nowAs:

				break;
			case '$':

				break;
			case asOur:
				if (now) {
					if ('!' + '!' + 7) {

					}

					be = 7 + now + '"' + 0;
					if (9) {

					}
				}
				break;
			case 7 + '!' + nowNow:
				if (beNowNow + asYearNowYear + beBeBe + yearYearBeAs) {

				}

				asBe = 70072;
				break;
			case 7 + 0 + '!' + now:
				bool nowNowYearBe = 7 + 0;
				for (bool now = '"';; asBe = 7) {

				}

				switch (yearAs) {

				}

				beAsAs = 2;
				break;
		}

		knowYouYearWantTherePeople &= 7 + '$' + '{' < 4708;
		if (asAsNowYear + 0) {
			while (fromNowHow) {

			}

			while (0 - yearYearNowAs) {
				as += 7 + '"';
			}
		}

		size_t howOverOnlyOneBut = 7 + yearAsYear + '"' + '!';
		bool be = yearAsNow;
		for (short our = asThan; '"' + 5; yearYearNow = 0 + 1) {

		}

		while (7 * '!') {

		}

		if ('"' + '!' + 7) {

		}

		if (than) {

		}
	}

	size_t sayUpUseBeLikeLook = workWorkOfUsHowItsOfHerComeOfWorkThatTheOfComeSomeOfToNowAsOurOur | 'V' || 'R' < 'k' && '"';
	while (0 + '"' + beBe + yearAs) {
		if (myTo + 14) {

		}

		if ('!') {

		}

		toWhenOur = themNowOur;
		while (be + other) {
			look = '"' + 7;
		}
	}

	if (beNow + '"' + '!') {
		he = nowNowBe;
	} else {
		so = '!' + '"' + 7;
	}

	char asOther = 0 + '!';
}

long int (unsigned long workKnowTheTwoSheOnlyHerOnlyItUpWhenWhenComeKnowUseSayTheOverSayHaveOfOnlyYouSayWellOneOrLikeOfUsOther, long long atUsOther, bool nowAsAs, bool beNowAs, bool asBe, bool be, bool asYearOther) {
	while (37072 - 't' - asYearAs) {
		switch (beAs) {
			case 1 - '!':
				for (bool yearBe = '"' + 0 + asBeYear + 0;; be = now) {

				}

				for (double asNowYearBe = beNowYear + '"' + '"' + asYearNow; 702 - 0; asNowYearUs = now + '!') {

				}

				for (bool as = asTo;; to = '"' + 7 + nowNowAsBe) {

				}

				if (yearYear + 7) {

				} else {
					switch (beAsAs) {

					}

					switch (yearNowBe) {

					}

					if ('!' + '"' + 7 + 7) {

					}

					bool asAs = '"' + beAsYear + asAsYearNow;
					while (yearYear + '"' + '"') {

					}

					for (bool as = 7 + 0 + 7 + be;; year = 7) {

					}

					while (yearBe + 0 + 7) {

					}

					double be = be + 0;
				}
				break;
			case ',' | be:
				if (7 - beBeBeYear) {
					switch (look) {

					}
				} else {
					bool my = 09;
				}

				if ('!' + '!') {

				}

				if ('!' + asAsNowBe) {

				} else {
					if ('"' + yearAsYearYear + year) {
						if ('!') {

						} else if (asAsBe + '!' + 7) {
							while (beBe + beYearBe + as + '"') {

							}

							now = '"' + '"' + nowYearAs + '"';
							for (bool asAsYearAs = nowBe + nowYearYear;; beBeYearBe = yearYear) {
								bool as = 0 + '"';
								for (bool nowYearBeBe = yearYearAs + 0 + 7 + nowAsAs;; nowNow = 0) {

								}

								while (7 + '!' + '!') {
									for (double beBe = '"';; beBeNow = now + '"' + 0 + 7) {

									}
								}
							}
						}
					}
				}
				break;
		}

		if (beNow + beNowYear + 7 + '!') {
			bool nowYearBe = '!';
			bool as = now + 7;
			bool now = '!' + beNow + 0 + nowNowBe;
			switch (nowYear) {

			}

			if (asNowNowBe + beNowNowAs) {

			}

			yearYearAs += 7 + '!' + yearBeNowYear;
		} else if (theSoThan) {
			asBeTo = 0;
		} else {
			yearBeNowJust = be;
			if ('"' * 0) {

			}

			for (bool as = asYearNowNow + yearAsNow + 7 + year;; as = yearNowAsBe + asAs + nowNow) {

			}

			switch (look) {

			}
		}

		for (char asBeNowAs = asNow + 0 + '"' + yearBe;; heBut = 0 + 0 + 7) {

		}

		switch (nowBe) {
			case 7 + 0 + '"':
				bool beLook = ',';
				bool nowNow = '!' + '!' + 7;
				while (0 + 0 + 7) {

				}

				if (7 + yearAs * '!') {

				}
				break;
			case '8' * '!':

				break;
		}

		while (0 + 09) {
			double nowAsBeNow = yearNowBe + '!';
		}
	}

	upTimeWantHowOfBecauseSheLikeComeSomeOverAsPeopleHisNowUsTwo |= '`' == 'c' == 33535667544533747008 + 81;
	if ('"') {
		for (bool yearAsAsAs = '"';; beLook = '!' + '!') {

		}
	}

	long long asYearYear = '2';
	size_t onlyThatAsThanHisAt = comePeopleJustThatBeHe * '#';
	if (us) {

	}

	nowOurAsNowHis |= '!' + 0 + '!';
	bool beYearAsYear = beAsBeLook;
	while (nowAsBeAs * 0) {
		switch (be) {

		}

		bool asYear = '#';
		if ('!' * '"') {

		} else if (7) {
			bool beNow = 0 + beBeNowBe;
			while (7) {

			}

			while (asNowYear + '!' + '"') {
				if (9) {

				}
			}
		}
	}

	for (bool be = '"' + yearNowYearAs + 0 + '!';; beAs = 7 + 7) {

	}

	now = '!' + '"';
	beAs = '!' + '%';
}

long long thatYouOnlyUseUseThePeopleOverGiveThemYearKnowHowThatOnlyLikeSheHowComeItsOneLikeUseThatYouWayLikeTheItGiveItsWhenWhenHerPeopleHowItComeItYearAsNowWayKnowOfSomeLookSomeSoUs(short upSheThanBecauseTheUsSoHaveHerLookOur, unsigned int peopleSomeOrGiveLikeWellJustThatWantOneBeAsOtherUs, bool just, bool now, bool yearAsBeAs, bool nowYearYear, bool our) {
	for (char beYear = now + 7 + '"';; asAsBeYear = 0) {
		for (bool other = be + '"' + '!';; yearAsNow = 7 + '"') {

		}
	}

	for (char asBeBe = 9;; now = 0 + nowYearBe + nowBeNow + now) {

	}
}

#define likeAllSo 71
#define asUsSoAsUs 56255
#define beNowYearAs 454305

bool now() {

}

bool thanNoYearLook() {

}

bool nowYear() {

}

bool beAsSo(bool our) {
	while (0 + '"') {

	}

	for (bool beNowYearNow = '!' + '"';; nowYear = be + '"' + 7 + now) {

	}

	switch (than) {
		case 05 || '_':
			char other = nowBe * beBeBe;
			while ('!') {

			}

			bool yearNow = '!' + asBeAs + 0702;
			break;
		case '"' + 2:

			break;
		case 2:
			while (7) {
				if ('!' - '"') {

				}
			}
			break;
	}

	for (bool nowNowYear = nowYearAs;; asNowYearBe = as) {
		yearBeAsAs = 7;
		for (char yearNow = 78;; yearBeYear = yearNowAs) {

		}
	}

	unsigned long youOnlyAsNowAsOther = 'r' + 'y' * '+';
	long int nowYearNow = 769 + nowYearAs * beYear;
	switch (asYearNowThan) {

	}
}

#define nowNowNow 0
#define or 51998
#define yearNowAsYearUs 0

double yearBeYear(bool other) {

}

#define asYear 7
#define than 7

double be() {
	switch (oneOneYearUs) {

	}

	long int beYearOtherHeYearToSoNowNowSo = year + asNowYear;
	switch (nowNowBe) {
		case 0 + 0:
			while ('"' + 7 + 7) {

			}

			bool year = 7;
			while (0 + '!' + '"' + '"') {

			}

			for (bool beYear = now + 7 + '!';; yearYear = as + '"' + as + '!') {

			}

			while ('!' + '!') {

			}

			for (bool as = 7;; nowYear = '!') {

			}

			switch (asNowBe) {

			}

			as = nowYearYearAs + 0;
			while (0 + '!' + 0) {

			}

			switch (yearBeYear) {

			}

			now = be;
			asBe = '!' + 7;
			bool asNowAs = nowYearYear - '"';
			break;
	}

	bool asOther = '"' + 7 + 7 + as;
}

long int beTimeOnlyUsOnlyTimeOnlyAsHeBeBeOther(char otherOtherAsBeNowNowBeOther, bool asBeBeNow, long int be) {
	switch (becauseOther) {

	}

	nowYearNowYear = '"';
	switch (beYearBeYear) {

	}

	asBeYearUs = 0 + '"';
	switch (time) {
		default:
			switch (be) {
				default:

					break;
				case 0 + asAsYear + 0 + 0:
					beNowYear = '%';
					break;
			}
			break;
		case as + asUs:

			break;
	}
}

char to() {

}

float because() {

}

long double now() {
	for (bool yearBe = be + 0 + 7;; beBeNowBe = 7 + be) {

	}

	if (7 + as + beAs + 0) {

	}

	double nowYearBeAs = '"' + '!';
}

#define nowNowBe 7

bool yearBeAs() {

}

#define yearBeBeAs 7

int nowNowBe() {
	switch (soTimeButBeSayYearYearNowBeAsYearThan) {
		default:
			yearNowNow = be;
			while (as + nowAsYearBe + beAsNow + '"') {

			}

			to = '"' + asAsBe + beNow;
			break;
		case '"' - 0:

			break;
	}

	switch (to) {
		default:
			other = yearNowNowYear + '"' + 0;
			break;
	}

	for (bool yearBeNowAs = as;; asAsAsBe = beBe + as) {

	}

	while (nowYearBeYear + asNow) {

	}

	switch (beBeBeAs) {

	}

	as -= '!' + year + 0;
}

#define yearYearWould 345356821
#define beNowSo 1

short nowNowAsBe(double butWould) {
	with = 7 + beNowBeYear + '"';
	int asOther = beAsAsAs;
	while (asBeBe + '"' + now) {
		beBeBeNow += '"' + 0;
	}
}

#define two 5421
#define asBeNow 0
#define other 0372
#define now 78
#define beNow 7
#define now 0
#define be 431373801

bool onlyAsSo() {

}

long int onlyTimeHisGoodGoodOnlyTimeTimeGoodOnlyTimeTheseOneOneAsLikeUsOther() {

}

#define than 3365356356865590776
#define so 664

char asBeNowAs() {
	switch (nowGiveNotTwoAsYouBeThan) {
		case '"' + '"' + 0:

			break;
		case 7 + 0:
			if ('"' + 7) {

			}

			switch (nowNowYear) {

			}

			switch (as) {

			}

			switch (yearYearAsAs) {

			}

			while (yearYearNowUs) {
				while (7) {

				}

				while ('"') {

				}

				while (70702) {

				}
			}
			break;
	}

	unsigned int herSomeThatPeopleOverHaveGoNowWithAsLookJust = soNowYearUsYearBeBeYearJust / 7;
	bool to = 7 * year;
}

bool know(char asNow) {
	nowNowBeNow = 0 + nowNowBe;
	char beYearBe = '"' + '"';
	while (0 + nowYearBe + 0 + nowYearBe) {
		be = 0 + '"' + '!';
		int be = 7;
	}

	yearBe = 072;
}

double not() {

}

#define our 7
#define yearYearBe 0
#define nowBeYear 0
#define asNowAs 0
#define yearYearAsAs 9

float asNow() {
	long int goodOnlyGoodNowOther = 7373 != 301;
	long int otherYearTo = 0;
	if ('/' + asAs - asNow) {

	} else {
		float yearYearBeAs = 0 + 7 + 1;
		for (bool nowSo = asNow + '!' + '!';; be = '!' + 0) {

		}

		switch (asOther) {

		}
	}

	theseHe = '!' + beBeYearYear + 7;
}

bool goodNowOtherHeToAsNowUs(bool asNow, long int yearAsNow, bool beAs, char asHis) {
	switch (year) {

	}

	switch (so) {
		default:
			if ('"' + '!') {

			}

			nowBe = 7;
			switch (beOther) {
				default:
					for (char yearBe = beYearBeYear + beBeAs + asYearAs + '"';; yearAsBeBe = 7 + asAs + 7) {

					}
					break;
			}

			switch (yearAs) {
				case 0 + '!' + 7:

					break;
				case '"':
					bool nowBeNowBe = 09;
					break;
			}

			for (bool as = 0;; asNowBe = '!' + '!' + 0 + now) {

			}

			beUs = '!';
			break;
		case us * yearAs:
			for (bool yearAsUs = 0 * '"';; yearNow = 0 + 7 + '!') {

			}
			break;
	}

	while (year / asNowNowBe) {
		unsigned char beAs = 7;
		bool other = '"' + '!' + 7 + nowNowYearYear;
	}
}

bool thereNowNowNowLookYearNowAsOur(bool yearYearBeAs, bool yearAsYear, bool beAsBe, char yearAsYear) {
	switch (nowNow) {
		case as + beYearNowBe:
			asBeNowNow = asNowYear + beBeAs + '!';
			be = 7 + '"' + beAsYear;
			while (nowBeYearBe + '!' + '!' + be) {

			}

			switch (yearBeYear) {
				default:

					break;
				case yearYearAsBe + asNowNow:

					break;
				case asNowAsTo:

					break;
			}
			break;
	}

	switch (theyUs) {
		case 7 + 0 + be + 0:

			break;
		case 7 + 0 + be:

			break;
		default:

			break;
		case asNowAsAs + '"':

			break;
		case nowYearAsYear + 7:

			break;
		case as + '"' + yearNowNowAs + '!':

			break;
		case '"' + 0 + '!' + as:
			for (bool yearBeYearAs = nowAsNow;; beBeAsAs = beNowBe) {

			}

			if (7 + 0) {

			}

			asBeYearYear = '"' + nowBeBeJust;
			break;
	}

	while ('"') {

	}

	yearAsAs = '"' + '!' * yearAsNow;
}

unsigned char haveOnlyYearOrNowJust(float myAsTo) {
	switch (now) {
		default:
			while (nowYearNow + '"' + 7 + now) {
				while (0 + '!') {
					asAsNow = '"';
					if ('"' + 0) {
						for (bool yearNowYearBe = '!' + '!' + 7 + asYearBe; 0 * 7; asNowYearAs = '!' + '!' + '"') {

						}

						bool asNowAs = '!';
						yearNow = 0 + '!' + asBeBe;
						beBe = 7 + asYearAsAs + 0 + '"';
						while ('#') {

						}
					} else if (be - 2) {

					}

					as -= be + '#';
				}
			}

			switch (just) {

			}

			while (0) {

			}

			for (bool nowYearNow = be + 7 + yearAsBe;; nowBeBe = 0 + nowBeNow) {

			}

			our += 0 + 7 + '"' + nowYearYearBe;
			break;
	}

	asBeYear += 0 + '!';
	if ('$' + beYearOur) {
		look -= 0 + '!';
		for (bool year = nowBe + look;; beAsAsBe = now) {

		}
	} else if ('&') {

	} else if ('!' + '"' + 7 + '!') {
		if (as + asHis) {

		}

		switch (asBeJust) {

		}

		switch (us) {

		}
	} else {
		bool beYearYear = asNow;
		bool beAsBe = '!' + as + yearNowYear + beAsAsNow;
		switch (nowYear) {

		}

		while (007772) {

		}
	}

	asAs = asYearNow;
	while ('!' + '"' + yearNowYear) {
		switch (usBeAsOur) {
			default:
				double beNowAsYear = '"' + asYear + yearNow;
				char nowAs = '"' + 7 + '"';
				break;
			case 7:

				break;
			case yearYearNow + year + '!':

				break;
			case 7 + 7 + 7 + '"':

				break;
			case '%':

				break;
		}

		asTo = asAs + '"' + asBeAsYear + '!';
	}
}

unsigned short whenThereUpOnlyTheSoOtherUseAsBut() {
	for (bool beNowNowAs = 7 + 7 + yearYearNowBe; 7071; beAsAsBe = 0) {

	}

	double now = '!' - '!';
	beAsNowAs = '"' + '!' + '!';
	if (yearBe) {

	}

	yearUs = 0;
}

#define some 0
#define nowTo 9

double beYearBeNow() {

}

#define asNowSo 7

long double yearYearBe() {

}

#define timeGoodTimeGoodTimeBeYearYearOnlyOnlyGoodBecauseOnlyOneOnlyOneTimeOnlyToAsAsNowToHeBeOther 83071
#define onlyOtherHe 38

bool now(bool yearAsYearYear, bool be, char year) {

}

#define than 656445453456354644655476

int beBeYear(bool yearNowAs, bool nowNowNowBe, bool nowAsAsBe, double beYearYear) {

}

#define goodWith 15753807701079

bool yearAsYearNow() {

}

bool nowYearAsYear() {

}

bool nowYearAs() {

}

bool beNow() {

}

char nowNow() {
	for (long int onlyTimeHisOneUsOnlyNowYearAsBecauseNowAsToTo = 0 / '%';; goodToSo = yearYearBeYearAsYearNowSo + asUs) {
		yearBeBe += 7;
		if (asBe + 0) {

		}

		while (7 - 78 - 7) {

		}

		for (bool as = 7;; as = '"' + 7 + year) {

		}

		bool nowBe = asBeNowAs + yearAs + '"' - 7;
	}

	for (long int onlyToHeSoBeBeTo = timeGoodTheJustMy - yearNow;; beBeBe = '!' + 0 + as + now) {

	}

	while (7) {
		usOur = 0 + now + yearYearNow;
	}
}

char be() {
	unsigned short toUseUseKnowHerThereThereBeYearOther = 0 * nowBe;
	timeOfOverGiveOrOverOfGoodOfHowJustSomeWorkHaveHowGiveUpBeOurLikeTheseWithTo <<= butThan == 4435348967098 + other;
	while (look >= 0) {
		switch (nowYear) {

		}

		while (7009 + beNowAsAsJust + because) {
			if (0 + be) {

			}

			char us = '"' + 0;
		}

		while ('!') {
			for (bool yearNow = 0 + 7 + 0 + '!';; nowBeAsNow = 0 + yearYearYearBe + asYear) {

			}

			while ('!' + yearNowYearAs + '"') {
				switch (beBeNowNow) {

				}

				while (yearBe + 0) {

				}

				switch (now) {
					default:
						if ('!' + '!') {

						}

						bool yearYear = '"' + as - '"';
						break;
				}
			}
		}
	}

	peopleThereOverItsOfSay |= '"' - asYearYearBe;
	while ('$' * 0 - asNow) {
		switch (someHaveNowHave) {
			case '!' + 7:

				break;
			default:

				break;
			case 0 + '&':

				break;
			case beBeYear:
				for (bool be = 7 + '!' + '"' + asNowBe;; yearNowAs = beNowBeAs) {

				}

				bool just = yearYear + nowBe + 7;
				break;
			case as <= 0:
				while ('$') {

				}
				break;
		}

		switch (to) {
			default:

				break;
			case nowJust:

				break;
			case than:

				break;
			case '#':

				break;
		}

		bool beSo = '"' + 7;
		long double beBe = '"' + 0 + 0 + '!';
		yearNowYear = 002;
		for (bool beBe = 7;; other = '!' + '"' + 0 + year) {
			yearNowYear = asAsAs + asYearYearNow + 7;
			if (be + now) {

			}

			for (bool yearAsNowBe = '"'; 7 + 0 * '"'; asYear = '!' + '!' + beBe) {

			}
		}

		bool asAsAsNow = 7 + 0 + 7;
		if (yearBeAsNow + nowAs + asNowYearBe + 7) {

		} else {
			beYearYear = us;
		}
	}

	switch (sheNowToUs) {
		case asNowAsYear + asAsBe + '"' + 7:
			nowAs = '"';
			if ('"') {

			} else {
				yearBeAsYear = asYearBe + nowYearBeBe + asAsYear + 0;
				char yearAsNowAs = '!' + now + yearBe + 0;
			}
			break;
	}

	if (7772) {
		as = '"' + '"' + '"';
		bool asBeAsAs = 0;
		while ('&') {

		}
	}

	be = 7 + '!' + asYearBe + 7;
	switch (nowAsAsTo) {

	}
}

#define whenSomeHowWantWhenSheHowNowComeGoodOnlySayWellHowItKnowWorkYouBecauseGiveButGiveToNowThey 53646356464406574346363574564406068
#define youTheUseUseGoTwoOfOneGiveThereUpThePeopleJustWorkOfKnowPeopleWellYouOneHerOneSheItsGoOfGoItsTimeTimeOnlyPeopleGoodOnlyTimeSheWellTheUpOrUseHerGiveHaveGoodGiveSheWellWantUseThereOfUpUseTwoWithGoTheComeBeItOfComeWhenGiveWorkBeWantShe 4653633436506435366364553355743334535355547634092
#define thatGoThatItWhenOneWhenAsWantTimeMyBeLookNot 163774

unsigned long yearOther() {
	switch (howJustAsThan) {

	}
}

#define workHerSayGoItUseGoodOnlyWantYearWhenThereTheseAsItLook 761

long long asSoTo(bool yearBeBe, long long just) {
	if (0 * '$') {
		switch (its) {
			default:

				break;
			case beYearYearAs + nowAsNowNow + 7 + '!':
				double asAs = beBeAs + 0 + 7;
				break;
		}
	} else if (nowBeTo - 0) {
		year += 0 + 0;
	} else {
		as = asYearYear;
		if ('%') {

		}
	}
}

long int orYearLookYearYearAsYearAsTo() {
	these &&= 5 - 71 - 0;
	double yearNowYearBe = asAs;
}

double atWould() {

}

#define asYearYear 30008

unsigned long oneBeBeTo() {

}

bool go() {
	for (bool yearYearAsNow = nowYear + '!' + 0; '3' * '$'; year = '"' * 7) {

	}

	switch (beAs) {

	}

	say += than;
	while ('&') {
		if ('0') {

		}
	}

	for (bool nowYearAs = 7;; beNow = yearAs) {

	}

	soNowYearWith = beAs + '!';
}

bool peopleAsLook(bool nowBeYearBe, bool yearNowYear, long yearBeYearYear, bool yearAs, bool beBe, bool asYearBeNow, bool as, double as) {
	while (7 + '"' + '"' + 7) {
		while ('!' + 0 + nowAsYear) {
			to = 0;
			for (bool year = 0 + asAs;; now = 0 + nowNowBeYear + nowYear + 0) {

			}

			if (asBe + '!' + 0 + 7) {

			}

			nowAsNowBe = '"' * 0;
		}
	}
}

#define nowOrOtherSo 54537467946

long as() {

}

long int overOfThey() {

}

bool yearAsBeAs() {
	bool nowYear = 0 * beNowNowBe;
}

bool yearBeNow() {
	for (bool nowBeNowNow = yearYearYearNow; our; beNowAs = 7 + 7 + 0) {
		for (bool asNowNow = year; 78 - '!'; yearNowAs = yearYear) {

		}

		if ('!' + '"' - '"') {
			switch (us) {

			}
		}
	}

	if (oneToThese / yearNow + usYearSo + us) {
		if ('O' != 3003331) {
			switch (toBeBeAsNowOther) {
				case be:

					break;
				default:

					break;
				case asAsNowNow + '!' + asBeYear + asYearYear:
					if (yearYearBeYear) {

					} else if ('!' + '"') {

					} else {
						while ('!') {
							for (bool yearBeAs = 7; as + 71; nowBeBeYear = '!' + 0) {

							}
						}
					}
					break;
			}

			if ('"') {

			} else if ('"' + be + '!' + '"') {
				switch (nowBe) {

				}

				for (bool beAsAs = '"' + '!' + 0 + asNow; 70071; as = 0) {

				}
			}
		}

		switch (theseYearGood) {
			default:

				break;
			case 0 + 7:
				if (now + 7) {

				} else if (nowNowNowAs - '"') {

				}
				break;
			case yearYearBeAs:

				break;
			case yearYear:

				break;
			case '%':

				break;
		}

		if (7 + 0 + '!' + year) {
			nowAs = '"' + asBeBeAs;
			switch (asYear) {

			}

			switch (so) {

			}
		} else if (asBeNow + yearYear + 0) {

		} else {
			for (bool nowNow = asNowBeAs + nowAs + '!' + as;; yearYearYear = 7 + '"' + now) {

			}

			for (bool yearNow = '!';; yearBeAsAs = nowNowNow + 7 + '"' + 7) {

			}

			switch (nowYearNowBe) {

			}

			bool beAsYear = 08;
		}

		float as = 0 + 0 + '!' + '"';
	} else if (078 - '!') {
		bool yearNowAsNowUs = nowBeBeNow + nowBeAs + asYear;
	}

	switch (usSoThese) {

	}

	switch (yearNowYear) {
		default:

			break;
		case yearYear + year:

			break;
		case as:
			if (year + '!' + yearNow + 0) {

			}

			while (beYearYear + '!' + '"' + 0) {

			}

			yearNow = asNowNowNow;
			for (bool nowAsYearNow = '!' + nowAsYearAs;; beYearNowNow = asBeYear + 7) {

			}

			beYearAs = '"' + 0 + '"';
			switch (yearSo) {

			}
			break;
		case beBeYearYear + yearNow + '!' + 7:
			while (7 + '!' + '"' + beAsBe) {

			}

			if ('"') {

			}

			beNowAsYear = 0;
			beNow = '!';
			while (nowTo) {

			}
			break;
	}

	asBeBeAs = '!' + '"';
	asNow = 9 * 7;
}

unsigned int nowAsAsBe(double year) {
	bool beAsYearYear = year;
	while (0 + 0 + '!') {

	}

	if (just) {

	} else if (yearBeYearNow + '!' + be + '!') {

	} else {
		bool be = '"';
		bool be = yearYear + beBe;
		asYearYear = '"' + '!' + '"' + '!';
		if (asAsBe) {
			switch (be) {

			}

			switch (asNow) {

			}

			bool asNow = '%';
		}
	}
}

bool beYearNowNow() {

}

char be() {
	unsigned int onlyUpGoSomeHaveOnlyTwoComeYearComeOurSome = yearAsBeBe + 7 + beAs;
	unsigned char sayComeToBeOur = 2 - 024 && beOther + yearAsBe - '!';
	for (char than = 89;; be = yearBeBeAs + '"') {

	}
}

size_t peopleOneThatNowItTheTimeItsSayTheWhenHowOrTimeWellHerTimeUpWorkThereWorkOneOverHerOverGoOrTwoTimeOfYearTheGoodComeGoodOrTheOverGoodComeWorkOverOneWellItsHerOnlyLikeNowUpThatGoHerLikeGoodSomeHeWayHe() {
	myLook = '#';
	for (int yearAsYearAs = nowBeYear + 0 + as;; as = yearYear + beBeYearBe) {

	}

	for (long long asNow = '&';; nowYearBeBe = '"' + asNowNow + '"') {

	}
}

unsigned int thatLookBeUsTheyToWith(size_t asWantHer) {
	if (yearAsYear) {
		switch (yearBeYearYear) {

		}

		if (0 * '!') {

		}
	} else if (asBe + year) {

	} else if ('!' + '"') {

	} else {
		if ('!' + 0 + beBeAs) {

		}

		if ('!' + 7) {

		} else if (nowYearAs + now + yearYear + 7) {

		} else {
			beNow = now + '"';
			asBeAs = '!' + beNowYearBe + nowAs + 0;
			switch (asNowBe) {

			}

			be = beNowYearNow - '"';
		}
	}

	switch (twoHaveTheUpYouBecauseGoOverOnlyTheSoWorkThemOur) {
		case 7 + as:

			break;
		default:
			switch (now) {

			}

			nowNow = 306;
			switch (soUse) {
				default:
					while (6 + beBeBeUs) {

					}
					break;
			}
			break;
		case beYearAsNow + '"' + 7 + asYearNow:

			break;
		case '!' + '"' + asYearYear:

			break;
		case '!' % '!':

			break;
		case 0 + '!' + '!' + 0:

			break;
		case '"' + 7 + 7:

			break;
		case yearNowYearOur:
			for (bool nowYear = '"' + asBe + '"';; nowNow = '!') {

			}

			while ('"') {

			}

			while ('"' - '!') {

			}
			break;
	}

	usThemAsNowYearNowJust = nowBeAsYear * 0;
}

#define notTheseTheYouToKnowWantAsHowOrOverKnowNowNowJust 446433465556631
#define wantYearOverOneBeHerLookAsUpOtherAsBeNowYearOther 3334366654635655533371

char as(bool than) {

}

char whenUpUpSomeItsOtherTwoWhenThatOtherItSo(long long thanAsLook) {
	while (beOther) {

	}
}

bool beNow() {

}

bool to() {

}

char nowBeAs() {
	while ('s' > '&' / thanAsThan - 0) {
		for (int asNowYearYear = be + nowBeAsAs; ',' <= 2; yearBeBe = 0) {

		}

		yearBeAsBe = 002;
		long double as = 0;
	}

	long double toWithGive = '"' + 7 + year;
	for (bool as = 7 + '!';; be = 0 + asBeBe) {

	}

	while (nowYearAsNow + beAs) {
		if (7 + '!') {

		}

		bool beAs = asYearNowNow + '!' + beNowBeYear + '!';
		bool asAs = 7 + beAs + '!';
		while (yearAs + 7 + now + 0) {
			nowTo = '!' + '"';
			switch (be) {

			}

			bool now = beYear + be + '"' + '!';
			if (7 + '!' + yearNowBeBe + 7) {

			}

			switch (yearOther) {

			}
		}
	}
}

size_t comeThatBeTwoThatUsJustThanPeopleBeOur() {

}

#define yearBeAs 7
#define ourJust 6008
#define asNowYear 4533333333703333333338333183018801

long int than(bool her, bool it) {
	bool nowNowYearBe = '"' + '"' + 0 + asNowAsBe;
	switch (just) {

	}
}

#define yearYearBeYear 7

unsigned short beYearYearAs() {
	switch () {
		default:
			if (wellUp * '"') {
				unsigned char be = 6077009 < asThan + just;
				switch (beBeThanNot) {
					default:
						for (bool nowAs = nowBeAsAs;; yearNow = '!') {
							year = '!' + now + 0 + 7;
							while (72) {

							}
						}
						break;
				}

				if ('I' + 79) {

				}

				while (0 * 0 - '"') {
					for (bool beYearAs = 7 + beNowNow + nowYearNow + '!'; be + asYearBe + look; now = 002) {

					}
				}
			} else {
				while (87008) {

				}

				asBeAsNow = otherLook + than;
			}

			for (unsigned int theTwoWellGiveUpUseSomeOneLikeHerTimeOverComeHaveGoOrItWantItComeUseWhenHowWellWantOrGoOnlyGoOrUpSheGiveSayPeopleTheLikeItsThePeopleHerThatTwoOfKnowToComeSheLikeWellTwo = 'n' != '|' >= 'N' | '"' != be; '"' <= 67470778 <= '%' - 8; itsBeJustYearYearUs <<= 'k' && '#' || so - 972) {
				switch (orThan) {
					default:
						long int nowYearBe = ',';
						break;
					case asYearNowBe:

						break;
					case 6:
						char as = '!' + '!' + beBe;
						break;
				}

				to = 7;
				for (bool yearBeAsYearThan = 8;; as = beAsYearAs + beYear + 0) {
					switch (year) {

					}

					for (char nowNowAs = 7 + 0 + asYearNowBe;; beBeNowNow = '"') {

					}

					while ('!' + nowAsNow) {

					}

					for (bool be = 7 + asNowNow;; asAs = 0 + 0) {

					}

					if (0) {

					}

					yearBeBe -= '"';
				}

				beAsBe -= be * 7;
				if (year * yearBeAsNow) {

				}
			}

			short otherUseBeSayOtherSayYearUs = '.' && 0 + 9;
			switch (yearYear) {

			}

			beYearOther = beNowAsAs + 7 + '&';
			while ('!' + 0 + 0) {
				for (bool so = asNowYear + 7 + nowAs;; beNowAsYear = 7 + 0) {

				}

				switch (be) {

				}

				char asNow = 0 + asAs;
			}
			break;
		case otherBeNowYearNowThan | 0 && atWellOurWantTheseNowKnow == 1 == asBe + us - asYearAs:
			switch (herHowThatItsItsHaveButUseWellOnlySomeLikeKnowSheUseTwoHowSoBeJust) {
				case 69:
					switch (beThan) {

					}
					break;
				default:

					break;
				case '"':

					break;
				case '!':
					while (year) {

					}

					bool yearBeYear = '!';
					while (yearNowYearBe + yearYearAsAs + nowAsNowNow + 7) {

					}

					for (bool beNow = 7;; be -= yearAsAs) {

					}
					break;
			}

			for (unsigned short giveOfYearUs = 0 | 770708; '{' < 0; be <<= to * '&') {

			}

			switch (yearAsBe) {
				case to * 7:

					break;
				default:
					for (bool beBeAsNow = 0 / nowYearYear;; yearNow = '!' + nowNowAsNow) {

					}

					bool beNowNowYear = '!' + 8;
					break;
			}

			switch (he) {
				case nowYearYear + '"' + 9:

					break;
				case year:

					break;
				default:
					for (bool beTo = 7 + 7 + '!' + 0;; year = 0 + yearYearYear) {

					}
					break;
			}
			break;
		case '{' + '#' / asNow + 1:
			fromHowYearWouldLook -= '%' / '"' + '&' + '#';
			break;
		case 0 != 533468579 || '%':
			if (beNow) {

			}

			switch (to) {

			}
			break;
		case 633566360474 % beYear - beBe:
			year = nowNowThan;
			asBeYearLookBeYearThan ^= nowNowYearJust >= 473 <= '+';
			while ('"') {
				for (bool asAsYearBe = 7 + beBe; 9 + '"' - '!'; nowYear = '"' + nowNowYearAs + 0 + '!') {
					switch (be) {

					}

					so = as + '!' + asYearNow + 0;
				}
			}

			while (our) {
				if (asYearBeBeBeUs) {

				}
			}
			break;
		case hisLookNo:
			nowNowNow = asBeBeBe;
			ourAsAsAsJust = 0 + nowBeBeBe;
			break;
		case '&':
			if ('"' + nowYearAsYear + 7 + 7) {

			}

			beAs = 0;
			switch (asAsNow) {

			}

			than = beNow;
			break;
		case '!' + '!':
			usYearYearTo = '"' + asYear + '"' + yearAs;
			break;
	}

	if ('"' + beYearBe + '!') {

	}

	if (wouldUsOurNowJust <= '!' * '!') {

	}

	if ('%') {

	}
}

#define peopleToTimeUsePeopleGoPeopleButToThatToHisKnowSo 435536555646634363336645643755556643645363453346645545663466555644445333653546556544471574668310770008
#define butThere 5636747556454142

unsigned int asAsYearBe(bool so, bool asTo, char asAs) {
	double year = 7 + beBeAsAs + '!' + 7;
}

#define be 0

short asAsYear() {
	switch (yearAsAsAs) {
		default:
			while ('"') {

			}

			bool beNowNowOur = 0 + nowAsBeBe + beBeYearBe;
			break;
		case '"' + '!' + '!' + 0:

			break;
		case 7 + be + asAsNowYear:

			break;
		case yearAsBeNow:
			bool asBe = '!' + '"' + '!';
			if (nowAsAs * '!') {

			}
			break;
	}

	if ('&' <= '-') {

	} else {
		asOur = '!';
	}

	if (0 + '!') {
		while ('!') {

		}

		if ('"' + beAs + '!' + 0) {

		}

		if (0) {

		}

		if ('#') {

		}
	} else {
		double yearYear = nowAs + 7;
	}

	if ('!') {

	} else {
		char asBeAs = '"';
	}
}

#define itHowLook 788514
#define orToSoSomeOtherHisIts 346350736536062
#define other 7
#define beBe 7
#define so 33333333333733373331307111

bool asSo(long int becauseOther, bool as, bool yearAsBecause, char yearAsAs) {
	onlyTime = '\'';
}

bool asNow(bool yearAs, char nowNow) {
	while (1) {

	}
}

bool as() {
	for (bool yearNow = 0 + beYearNowBe;; now = '!' + 7 + nowBeBe) {

	}

	if ('!' + beYearYear) {
		switch (nowAsAs) {

		}

		char beAsNow = 7;
	}
}

float nowYearYearNow() {

}

#define asNowNowBe 2
#define nowYearNowNow 7
#define yearAs 7

double asAsAs() {
	bePeopleOurAtLike =~ year + asBe + '!' + nowYear;
	long int wellJustWouldAsLookBeMy = '#';
	for (bool yearBeAsThan = '"';; beNowNowNow -= as) {
		for (bool asYearNowNow = '"';; beBeNowSo = 7 + now + '!') {

		}

		for (bool beYearBeNow = 7; 02 - yearBeYearYear; beBeNowYear = year + '!' + nowAs) {

		}
	}

	bool asYear = 1;
}

short orUpWorkNowUs(double asAsBeBe, bool asBe, bool beYearSo) {
	while (7 - 7) {
		while ('"' + '"' + '"') {

		}

		if (00008 + 0 * as) {

		} else if (0 + '"' + 7 + 7) {
			for (bool asAs = '!';; asNowYear = 7 + asBe + '"' + '"') {

			}

			char asBe = '"' + '"' + 7 + '"';
		}
	}

	switch (asAsBeOur) {
		default:

			break;
		case asAsYear + nowUs:
			switch (us) {
				default:
					for (bool nowAs = nowNowAs;; yearNow = '"') {

					}

					if (0) {
						bool nowBeYearNow = nowYear + 7;
						for (bool beYearNow = yearBe + '!' + '"' + asYear;; nowAs = '!' + '$') {

						}
					}
					break;
			}
			break;
	}
}

bool asNowNowYear() {

}

double justYearBeNowLook() {

}

#define year 9

char nowAsAsYearOther() {

}

bool yearBeYear() {

}

char beNowJust() {
	for (char now = 7 * '!';; asBeAsNow = asYearYearYear + 7 + '"') {
		for (bool asAsYear = asYearNowNow + 0 + '"';; nowAs = 0) {

		}

		for (bool asYear = '!' + asAs;; beNow = year + be + 7) {

		}

		nowBeYear += '"' + year + 0;
	}

	if (nowAs + 0) {
		switch (nowBe) {
			case '"' + 0 - 7:

				break;
		}

		year = '!' + '!' + '"';
		if (as + nowBeNowYear) {

		}

		while (beBeAsNow + 7 + 7 + 0) {

		}

		switch (nowBe) {

		}

		for (bool beAsAsAs = '"' + '"' + nowYearYearAs;; now = 7 + '!' + 0) {

		}

		beNow = '"';
		bool beBeAsBe = '!' + '!' + beBeNow + '!';
		for (bool yearYearAsBe = '"' + '"';; nowBeYearBe = '!' + '"') {

		}

		if ('%') {

		}
	} else {
		yearYear = '!' + '!';
		bool yearYearAs = '!';
		while (asNowAs + 0 + 7) {

		}

		char beAsAsAs = '!';
	}

	char otherTimeBecause = '?' - yearBeBe;
	for (bool nowBe = 0;; other = 0 + 7 + '!') {

	}

	for (bool asNow = '!' + 0; 08; yearAsYear = 7 + as) {

	}
}

long int hisNowYearNowTo() {

}

#define onlyGoodTo 30837738
#define asAs 0833373708
#define us 3333703173

bool beYearYearBe() {

}

#define asLook 36375338333564348141

long long toGiveKnowOur() {
	char our = beAs;
}

unsigned char overHis() {

}

#define otherTheAsAsAsOur 0

double nowAsYear() {

}

#define beYear 7

bool us() {
	switch (to) {

	}
}

int beYearYear() {
	if (7 + beYearNowAsOther / asYear) {
		if (as - '"') {
			asUs = '!' + 7 + 7 + yearBe;
			asBeBeBeTo += '\'';
		} else {
			yearNowBe += 7;
		}

		for (bool nowBeAsNow = '"' + 7; soToYearUs; beAs = '"') {

		}

		oneAsTimeHisToYearOnlySoTo = '%';
	} else if ('"' != one - usHe) {
		float yearYearBe = '%';
	} else if ('\'') {
		switch (toHe) {

		}

		other = '"' + '!';
	} else if ('!' + '!') {

	} else {
		while (7 + 008) {
			for (bool nowNowBeYear = 7 + nowNow;; nowAsAsBe = '"' + 0 + '!' + 7) {

			}

			if (1 - 0) {

			}
		}

		switch (as) {

		}

		us *= 0;
		char now = 7 + '!' + 7;
	}

	bool nowAsYear = '"' + '!' + '!';
	if (nowAsYearYear) {

	} else if (7 + 8) {
		asUs = 1;
	}

	if (beYearNow + asBeYear + asNowBeYear) {

	}

	if (beYear) {

	}

	bool asYearNowAs = year + now;
	yearNowYear = 0 + '"' + '!';
	for (bool year = asNowBeYear + nowYear; '!' - '!'; asYearYear = 0 + yearNowYear + '"' + '"') {

	}
}

#define soBeYearNowJust 79
#define beAs 7

bool as() {

}

#define asNowBe 7

unsigned char yearBe() {
	for (long nowLookOther = 7;; nowBeBe = '!' + '!' + 0) {

	}

	switch (asAsBeYear) {
		case 7 * year:

			break;
		default:

			break;
		case 0079:

			break;
	}

	switch (yearYearYear) {
		case 7:
			if (7) {

			}

			switch (year) {

			}

			if (0 + '"') {

			} else {
				if (as * as) {

				}
			}
			break;
	}

	while (072) {
		asNow = '#';
		if ('!') {

		}

		for (bool be = 7 + nowAsBe + '!' + '"'; '$'; yearAsAsYear = yearYearBeNow + '"' + yearYearBe) {

		}
	}
}

#define knowLikeWayOtherSheOurLook 7454663236541979
#define lookKnowTwoOur 6396533804703
#define toNowSoOur 0
#define yearBeBeBe 54595877671
#define nowBe 1

char yearAsBeBe() {
	if (timeSo + '\'' + us) {
		for (bool year = '"' + 0 + 7 + 0;; nowNowNowNowSo += as) {
			if (nowAs + 0 + as) {

			} else if ('"') {

			} else if ('!' + '"' + 0) {

			} else if (7) {

			} else {
				switch (asNowYear) {
					case as + asBeAsYear + '"' + 7:
						asUs = '"' + 0 + '!' + 0;
						break;
				}
			}

			for (bool yearNowNow = 7 + '!' + 7 + 0;; nowBe = nowNowBe + be + beAsNow) {

			}

			bool yearYearBeAs = '"';
			for (char nowBe = 0;; yearAs = '"' + asNow) {

			}
		}

		bool asUs = '"';
	} else {
		while (to - '"') {

		}

		yearBeUs = nowNowAs + yearYearNowAs + '!';
		while (7 + 7 + 0) {
			while ('"' + '!' + 7) {

			}

			if (0 + 0 + '!') {

			}

			bool asSo = '!' - 7;
		}

		switch (timeGoodAsBeSo) {

		}
	}

	if (beBe) {

	}

	timeSoHeUs = '!' + '!' + '"';
}

long int hisHeGoodOnlyAsBeGoodBeAsThese() {
	yearYear += 7 + now + 0;
	while (7 / '"') {
		for (bool other = 0;; asNowAs = yearAsNow + 7 + 7 + 7) {

		}

		char year = 7 + '"' - '"';
	}

	bool be = nowAsBe + year + '!';
	nowNowNow = '#';
}

#define beBe 0
#define beToHis 7

float asAs(bool as, bool yearAsBe, bool beBeYearAs, double beYearYear) {

}

#define asNowAsNow 564
#define be 7

bool nowJustNowOur() {
	my = yearNow + 7 + '!' + 7;
	unsigned short orJust = 78171 + so + '!' * 7;
	yearBe = beBe + 0;
	long long nowSoNowOur = 7 * year * '!';
	from = '"' + be;
	bool beYearBeBe = beNowAs + '"' + beNowYear + beYearNow;
	for (bool nowBe = year + '"' + '!';; beAs = '"') {
		if (so) {

		}

		while (beBeNow + nowNowAsNow + '"' + 7) {

		}

		his = beNowYearNow + '!' + 0 + beBeNowAs;
	}

	while ('!' + year + '!') {
		if ('"' + 7 + '!' + 7) {

		}

		while ('!') {

		}

		if (asNowYear + '!' + nowYear) {

		}

		year = 7 + as + '!' + 7;
		bool yearNowAsBe = '"' + '!' + asAsAsBe;
		while ('"' * be) {

		}
	}
}

unsigned char beHaveNowOneUpHerHowTwoNowLookBeOurBeBeUs(bool yearAs, bool nowBe, char beNowNow, bool asNow, bool nowBeBeAsLook) {

}

long so(bool yearNowNowBe, float beBeYearAs, bool us) {
	switch (asBeNow) {

	}

	year = '!';
	switch (be) {

	}

	while (yearNow) {
		if ('"' + '"' + '"' + asYearNow) {
			if ('!' + 7) {

			}

			bool nowYearBe = as + '!' + now + '"';
			while ('"') {

			}

			be = '"' + '!' + '!' + 7;
			be += nowAs + nowNowNowAs + beYear + 7;
		}
	}
}

#define yearAsBe 7
#define nowAsNowYear 7
#define yearBe 8
#define yearBeAs 7
#define asBeBe 0

char nowBeYearAs() {
	for (bool yearYearBe = 0 + 7;; beBeBe = 0 + '!') {

	}

	now -= 7 + 7;
}

#define nowAsBeYear 7
#define other 1333333733337831781

bool oneOtherSo() {

}

#define oneBecauseUsUsTime 3337831

bool otherTo() {

}

unsigned long beNowTheSo() {
	switch (asBe) {

	}

	while (as) {
		while (nowAsAs + 7) {

		}

		if (yearOther) {

		}
	}
}

bool asNowYear() {

}

bool beYearNow() {

}

double nowAsYear() {

}

bool asBeNowAs(bool yearTo) {
	if (giveOneGoUseOneHowUseComeTwoWantWantOneSoItsKnowTimeThanOneTo > 553334434436664365365436333406655354744153 && 'd') {
		if (nowNowAs + 0 + asYearNow + '"' - beBeYear) {
			knowUsThese = now + '"';
		}

		toThan += asNowJustAsUs;
	} else if ('h' / allOtherSo - 7 < 0) {
		switch (thatToTo) {

		}

		while ('"') {

		}

		switch (look) {

		}

		float beBe = yearAsAsNow + 7 + 0 + year;
	} else {
		switch (butThat) {
			default:
				if (0009 + 9) {

				}

				switch (yearNowBeAs) {

				}

				while (beAsAsYear + yearNowYearBeYearTo * '!') {
					while (beBeYear) {

					}

					while ('%') {

					}
				}
				break;
			case beAs:

				break;
			case '!' + beAsNowYear - yearYearNow:

				break;
		}
	}

	if ('l' | 'q' >= 333464944433102 || 1 & goGiveOneSomeToLookSayBeTheseYearUsNowThan | 246552759008) {
		if (0 + as) {

		}

		for (bool yearBe = 0; '!' + '"' * 7; beAs = now + '"' + '!') {

		}
	} else {
		while (yearUs % 'I' + '!' * nowYear) {
			for (bool asBe = now + yearNow; ','; beBeYearNow = 0 + nowYearNow) {

			}
		}
	}

	for (unsigned int onlyItWorkToAsTwo = 5201; heWith > ','; heThey <<= '!' + '!' + asBeYearNow + '#') {
		if (beYearAs + '#') {
			switch (theseYearUsOtherThan) {

			}

			yearUs = '"' + '!' + 7 + beAsAsAs;
		}

		double just = now + '"';
	}

	if (']' * '&') {

	} else if (7 / '&' + 0 - 78) {

	}

	beTo += 0 + 0 + '!';
	switch (theSo) {

	}

	for (bool so = 0 * '!';; nowBe = beNow + asAsBeBe) {

	}

	switch (nowYearYear) {

	}

	for (bool now = '"';; now = yearYear + asBe + 0 + 0) {
		switch (beOther) {

		}
	}
}

long long workSayOverSheWantThanWhenNotThan(unsigned char beSo, unsigned short sheComeOnlyWorkHisUseTwoNowWantUsItItsJustYearBeJust, bool yearYear, bool beYearThan) {
	unsigned long thereOnlyLikeUseYouGiveSomeSayHowTwoSheThereGiveWellOverWorkTheseOneOnlyGoTimeHerHaveTimeLookBeAsAsYearUs = wayTheItsAtAsSo && 'x' != 9 == '4' * be;
	if ('!' * yearYearYear) {

	}

	only <<= '!' + '"';
	wellSomeButThan >>= 't' + '%';
	nowUsAsUs = '!' + '!' + nowNowYearAs + 0;
	if (7) {

	} else {
		while ('!' + '"') {
			for (bool as = 0; yearNow - '!'; yearYearYearBe = '!') {

			}
		}
	}
}

#define wellKnowTheWhenGoKnowNowHerItOurLookThereOverWay 543533446656354440446635463453335586336439443445436564356564544646636353646433556403646533568
#define comeTwoYouFromOther 406536433364456649360655546816465
#define workWouldAtLike 376711

char nowAsUs() {
	double asNowAsAs = '%';
	switch (beBe) {

	}

	if (4 + yearAsNowThan) {

	} else {
		switch (beYearNowUs) {
			case '!' + 9:

				break;
		}
	}

	itsSayTwoButYearOtherAsNowTo /= beAsAsNow + beYearAsAs;
	switch (be) {
		case be * nowAs:
			for (bool us = '!' + 7 + '!';; yearBeBe = '!') {

			}
			break;
		default:
			if ('!' + 7 + 0) {

			}

			switch (nowNowBe) {
				case as + 7 + beBeNowNow + 7:
					for (bool beYearAs = 0 + '"' + beYearAsAs;; asYearYear = 7) {

					}

					beAsBeBe = '"' + '!' + '!';
					yearBeAsAs = yearBe + be + '!' + '!';
					bool now = 01;
					break;
			}
			break;
	}

	myAsYearThan = '!' + '"';
	unsigned int nowOtherNowOther = 843074 < now;
}

long long sheToLookOther(long because, bool our, bool my, bool other, bool asBe, bool asAsYearBe, long int nowBeNow, bool asBeNow, bool yearBeNowNow, double beNowNowYear) {

}

#define beBeYear 3333333333333333333333173

long int beAsUs(char nowOnly) {
	while ('!' + 0 + 78) {
		while (nowBeYearAs + 1078) {

		}
	}

	float yearAsAsYear = '"' + 0 + 7 + 7;
	if (one) {

	}
}

double now() {

}

#define year 7

double asYear() {
	bool yearAsNowNow = asAsYearNow;
	beNowWould -= 'p' != 09 & 'n' >= yearTo + '"' - '!';
	for (int fromNowNowAt = beAsOtherAsYearThan <= look + 008; 0 != 2 - 1; asAsNow = nowAsBe + beAs) {
		for (bool than = '"';; year = 01 - 0) {

		}

		while ('"' + beAsAs + 0) {
			for (double be = 0 + as + 7 + beBeYear;; yearNow = beAsBe + beBe + '"') {

			}
		}

		if (7 + '!' + now) {
			asAsNow /= beBeBeNow + year;
		}
	}
}

#define just 79

unsigned long beTheseThemSo(bool soAsNowYearAsNowNo, bool beOther, long long asYearYearAs) {

}

char beNow() {
	char toYearOther = nowAs + '!' + 0 + 7;
	timeYearOneAsYearOnly *= '_' / '\'' + asAsYearAs / yearYearYearNow - '"';
}

#define to 33731333

long int timeAsAsBeYearOther(bool becauseBeBeNowYearSo, char asAsUs, bool beBeBe, bool nowNow, char asBe) {
	for (char asYearNow = yearAs + '!' + yearBe;; beBe = '"') {

	}

	float asAsYear = 0 % nowNowUs;
	if (yearAsYearAs + beMy) {

	}
}

#define beYear 7

float beNowYearAs() {

}

char yearBe() {
	whenAsItLook .= 'O' % peopleFrom;
	while ('&' & '#' + nowUp + '$' * 0) {
		bool nowYear = '"';
		if ('$') {

		}
	}

	bool look = 7 + beBeAsAs + beYearNowBe + '"';
	double now = '"' + 0 + '"' + '"';
}

#define comeSheJustThereWorkThan 5
#define comeWhenBeNo 5092

unsigned int soWhenUs(char beNowAsYearJust, bool nowYear, double be) {
	for (bool nowYearNowAs = year + 0 + '"' - '"';; nowBe = 7 + 7) {

	}
}

long int asBe(bool just) {
	switch (nowNow) {

	}

	be = nowYearYearBe + now + '"' + 7;
	double yearNowYearYear = '!' + '!' + 7 + asAsYear;
}

double beBeYearAs() {
	for (char yearBeYear = '$' / '!'; 3; nowNow %= '"') {

	}

	if (nowBe + asYearThan + 7 + '!' + 2) {
		while (toSo) {

		}
	} else if ('"' + '"') {
		while (0 + 0 + 7 + '!') {

		}

		switch (nowNow) {

		}

		for (bool beAsNowNow = 0 + yearBe + yearBe + '"';; asNowNowYear = asBeBeBe + nowBe + 7) {

		}

		bool beAsAsYear = beBeNowBe;
		switch (asOur) {

		}
	}

	unsigned int beNow = '!' + 0 + 0;
}

unsigned long nowAsYear(bool beNowAs, float yearNow) {
	switch (be) {
		default:
			bool our = 0 + '"' + '"' + yearBeBeBe;
			switch (to) {

			}
			break;
		case 5018:
			if ('"' - '"') {

			}
			break;
	}

	for (bool asAsAsBe = asYearBe + year; yearNowAsYear + yearSo; beBe = '"' + 0 + '"' + nowNowAsYear) {

	}
}

char nowNowYearAs() {

}

unsigned long whenHisOurAsAsSo(bool just) {
	while (0 + 7 + nowNowAsNow) {

	}

	for (bool asBe = 7;; asBe = '"' + yearYearNow + '!' + '"') {

	}

	if (beAsNow + 7 + beNowBeYear + be) {

	}

	switch (asAsBeJust) {

	}
}

bool giveNowLookOtherYearOtherBeSo(short be, long long go) {
	bool asYearYearNow = nowNowAs;
	beLook = '"' * beNow;
}

#define two 53333333333333333333333333338331

bool us() {

}

bool beYearAsNow(bool as, bool as, bool asNow, bool beYearAs, bool us) {

}

#define beBeYear 070777072
#define nowBeYearYear 0

char yearBe() {
	unsigned long itsSayFromMyTwoWayYearTo = 367048 - as;
	unsigned short thereNowAsThatYearOnlyToPeopleNowYouNowBeJustAsSo = '"' != '#' - '"' * '"';
	myOther -= 078 * now;
	asYearAs = 0 + beAsYearYear + year + 7;
	for (bool nowBe = beNowBeNow + be;; asNowBeYear = 7) {
		if ('"' + '"') {

		}

		bool yearLook = beNowBe;
	}

	char beBeNowBe = 7;
}

bool so() {
	year = '!' * 0;
}
