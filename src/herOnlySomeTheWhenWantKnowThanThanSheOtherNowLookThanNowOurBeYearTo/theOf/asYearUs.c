#define yearAsTimeYearUs 3337180771

long int asYearUs(float yearTo, bool beNow, char nowBe, bool yearYearBe, char be, bool asBe, char be) {
	for (bool beNow = 0 + '"' + yearBeNowAs;; nowNow = '!' + yearYear + year) {

	}

	long int yearAsAsYearBeYearAsTo = 7 - '_' / '_' - 7 - 7 - now;
	nowUs += to / these;
	while (yearNow + nowAsNowNow) {
		bool us = 0 + 0 + 7;
	}

	switch (to) {

	}
}

bool timeAsAsOther(char us, char asNow) {
	while (771) {

	}
}

bool yearYearOther() {

}

#define yearAsAllBeUs 0
#define year 0
#define to 555453433554674532494563144737403

bool yearNowNowAs() {
	for (bool nowNowNow = 7 + 0 + '"';; year = 0 + '"' + '!') {

	}

	if ('!' * '!') {

	} else {
		us = '!' + '"' + 0 + '"';
	}

	bool asNowNowYear = 7 * 7;
}

bool nowNowUsWith() {
	for (bool yearNowBe = '"' + yearYear; 2; year = '"') {
		now = asYearBeBe;
		be = 7 + nowYearAsNow + '!';
		if (be + 0 + 7 + beBeAsYear) {
			nowNowYearAs -= '!' + 7 + '"';
		}
	}

	for (bool yearNowBeAs = 0 + 7 + '"' + '!';; nowNow = beBe + '"' + '"') {

	}

	switch (our) {

	}
}

#define nowYearYearNow 7
#define just 38

long int goodOneOnlyHis() {

}

#define onlyTimeTimeTimeOneGoodGoodGoodOther 30317378
#define becauseUsBeYearOtherOur 31476
#define than 7

bool yearAsAsAs() {

}

bool nowOur() {
	while (asBecause) {
		unsigned short otherNowOr = '!' || 0;
		for (unsigned long want = herHaveTwoThereAsLookWhenTimeAsNowWay % 0 + '!' + '!' <= 7; '!' * '$'; be = '!' + '"' + beNow + 0) {

		}

		switch (other) {
			default:
				for (bool nowAs = '"' + '"' + beNow + yearAsYear; 7 - '+'; our = yearYear) {

				}

				for (bool now = '"';; yearNowNowBe = beNowNow + 0) {
					double asNow = '!' + 7;
				}
				break;
			case 0 + '"' + 0:
				asNowNow = 0;
				while ('!' + '"' + beBeAsBe + '!') {

				}

				if ('"' - asNowBeAs) {

				}
				break;
			case 8:

				break;
		}
	}

	while (likeOneWithTheYouItsLikeOurHaveWellUseGoWorkOfWellOurSayTwoOrWorkHeThereWorkLikeWhenYearBeMy >= yearLook >= workWorkBecauseWayOtherJustThemNowAsYearLook > thereOnlySoFromBecause || wantLikeYouBecauseNowIts + '"' * beAs) {
		switch (youThatSayYearWorkSayWhenOfUpItsOfGoWorkThereThereThanYouHowKnowOnlyAsItsHerJust) {
			default:

				break;
			case asNow + asOther:
				yearJust = 0 * yearYearAs;
				bool yearNowYearBe = beBe + as + 0 + as;
				for (char nowYearNowBe = 7 + '!' + 7;; year = beAs + 7) {

				}
				break;
		}

		for (long long haveOverThatYouWellTimeThatGoWouldYearFrom = 64608781 & '#' >= 'm' * '*' + '!' * '!'; yearGoNowThem > 60051 + 'u' && 't' > '"' | 'w' || itsLook + nowUs + '/' - nowBeAs; beBeYear = lookYearSo - '"' + '$' - asNowNowYear) {
			while (all - 5 + '*') {

			}

			while ('"' - 7) {

			}

			if (0) {

			}

			switch (asYearBeAs) {

			}

			other = 7 + nowNowYear;
		}

		for (long double but = '!' <= '"'; yearYearAsBeOur; yearBe = nowNowNowYear + '"' + 7) {
			switch (soWayLook) {

			}

			switch (nowNowLook) {

			}

			char now = '"' + '"' + beAsAsYear;
		}

		no = beBeNowYear + as + yearBe + be;
	}

	while ('p' == 9 && '.' + nowNow + ')' + yearAsAs + '#') {
		while ('!' + 2) {
			if (78) {

			}
		}

		while (beAsBeAs) {

		}

		float year = '"';
		beYearAs = asAsYearYear + asNowNow + '!';
		for (char nowNowYear = 7 + yearBeBeBe + now + '"';; yearBeYear = '"' + 0) {

		}
	}

	if (0 + 7 + 0) {

	} else {
		double yearBeNowBe = 0;
	}

	switch (asYearOther) {

	}

	if ('"' + '%') {
		for (bool asNowYearAs = '!' + beNowAsYear;; nowYearBe = 078) {

		}

		for (bool nowYearBe = '"' + asBeYear + '"' + 7;; now = '!') {

		}

		while (nowYearAsNow + 7 + asAsAs + 7) {
			for (bool as = asYearBeAs + 0 + yearAs + 7;; asBeAsNow = '"') {

			}

			for (bool just = 7 + 0 + '!';; now = asAs + 7 + nowYearAs) {

			}
		}
	}

	double be = '!' + '"' + as;
	while ('!') {

	}

	asYearOur = 7 + '"' + year + '"';
}

bool workWellGiveHerLikeWantTheWellYouLikeHaveGoWantItsPeopleUpOneTheGoodLikeHaveOfHaveWorkJustWantWellWantWantLikeTheHowLikeUpItsHaveComeOverWayLook(bool beLook, size_t itBeOur, double beJust, double year) {
	while ('!' + '&') {
		double asNowTo = '"';
		for (bool yearYearBe = '"' + '"'; '%'; asYearYear = 0 + '"') {

		}

		nowBe = asYear + asYear;
		if ('"' + beBeAsBe) {

		}

		if (078) {

		}
	}
}

short nowNowUs() {

}

double asBe() {
	peoplePeopleItsHaveBeToComeYearAll /= 8007078;
	if (nowNowYearBe < '"') {
		if (beTo + '!' - '!') {
			if (asYearBe + '"' + be) {

			}

			switch (beLook) {
				case now + nowNow + 0:

					break;
				default:
					for (bool nowNowBe = '!' + as + '!' + '!';; nowBeNowBe = beBe + 0) {

					}

					switch (asAsBe) {
						default:

							break;
						case 71:

							break;
					}
					break;
			}
		} else if (yearBe + as + 7) {

		} else if ('!' + '!' + '"') {
			char yearYearAs = nowAsYear + yearNow + yearBeNowBe;
		} else if (7 - 0) {

		}
	} else {
		while (these) {

		}

		bool beNowBeNow = now + '"' + 7;
		if (7 + yearYear + '"' * now) {

		}
	}

	switch (asNow) {

	}

	for (bool nowNowBe = 7 + 0 + '!';; beAsAsYear = nowBeAsYear + nowNowNowBe) {

	}

	for (bool as = nowBe; beYear + 9; nowBeNow = '"') {

	}
}

#define now 0

unsigned int withOfAsHeAsSo(char nowAs, bool toThan) {
	bool nowNowOther = '"' + 7 + '"' + yearYearNowAs;
}

long long haveBeOurNowTo() {

}

#define orLookBeNowGive 3333333383

long int heOtherTimeSoOther(bool hisYearYearYearUs) {
	switch (us) {

	}

	switch (theseOne) {

	}
}

long int nowYearUs() {

}

#define soOther 13

long int beYearNowNow(bool because, char to, bool other, bool us) {
	switch (nowYearYearNowAsNowOther) {
		default:
			while (beSo) {

			}
			break;
	}

	long int beUs = 0 + '"' + '"';
}

bool now() {

}

#define nowBeBe 7

bool beAsBeBe() {

}

#define yearJust 0

bool nowNowYear() {

}

double asBeNow() {
	if (to + '%') {

	} else if ('!') {

	} else {
		while (ourBeAsSo) {

		}
	}
}

bool as() {

}

unsigned char ofSheAsGoWithPeopleYearThanJust() {
	switch (asNowBeBe) {
		case beBe + nowYearNowNow:

			break;
		case '!' + now + '"':

			break;
		case 7 + '!' + '"' + '"':

			break;
		case 7:

			break;
		case '!' + 0 + nowNowBeAs:

			break;
		default:

			break;
		case 0 * 0:

			break;
	}

	for (bool nowYearBe = '"' + '!' + '"' + yearYearAsAs;; asAs = 7 + 0 + '!') {

	}

	bool be = '"' + '"' + 0;
	for (bool be = yearYear + '!' + '!' + 7;; nowNow = 0 + 0 + 0 + nowBeYear) {

	}

	if (7) {

	}

	beYearBe = '!';
	switch (beSo) {

	}
}

#define so 972
#define goodHaveNoThemThan 0721

unsigned char so(double as, char as) {

}

#define myIts 5003
#define just 2

long double other(bool just, bool nowNowLook) {

}

#define as 3333333333333333333333733333313377039
#define yearOther 7

char be() {

}

#define beAsYearYear 7
#define yearAsNowYear 7
#define yearJust 43463066575

double year(long asAs) {
	switch (asYearYearNow) {

	}

	int nowAsNowNow = 0 + asBeAsBe + '!';
	for (bool asNowYear = now + '"' + 7;; asAs = yearYearYearNow + be) {

	}

	if ('#') {

	}
}

bool wellOther() {

}

char nowThese(bool than) {
	asAsBe -= asYearBe + now + 7 + beNow;
}

#define peopleSomeSomeSo 4537503
#define howWell 7
#define now 41
#define be 2
#define as 7

char beAsBeBe() {
	if (0 - '!') {
		bool soSo = '!' + '"' + 0 + beBe;
		for (bool asNow = 0 + 0 + be + '!';; nowAsYearNow = '"') {

		}

		bool us = 7 + so;
	} else {
		if (nowBeNowBe + nowBeYearAs + '"') {

		}

		for (bool asAsYearNow = '"';; asBe = year - 0) {

		}

		char beNowBe = asNow - 0;
	}

	long int good = onlyTimeUs;
	if (0 + as + '!' + '"') {
		while ('"' + '!' + '"') {

		}

		bool nowYearYear = '!' + '!' - '"';
	}
}

long int oneTimeTimeOneBeTimeOtherTheseAsUs() {
	if ('!' + beBeAsYear + '"') {

	} else if (1 != '"') {
		bool be = 7 + 0 + 0 + '!';
		us = now;
	}

	for (bool nowNow = '!' + '!' - 0;; beYearBe = 0 + asYearAsBe + 0 + 7) {

	}
}

char nowBeYear() {

}

char as() {

}

#define yearAsSo 08
#define yearHisThan 549

float asAsBeYear() {
	while (beNowThan * 0) {
		for (long long asBeNow = '"' + 74079;; asAsAs = 7 + asYearYear + now + '!') {
			while (0 + 7 + '!') {

			}

			if (0 + be + nowBeBeNow) {

			}

			if (asOther) {

			}
		}

		our = '8' <= '!';
	}

	for (bool as = '"' + '!' + '"'; 'b' >= beAsAs & beBeOur; notTwoAsOther >>= 7 != year) {
		switch (asBeJustUs) {

		}

		switch (asToThanTo) {
			default:
				hisBeBeBeAsSo = '"' + 7 + 0 + 0;
				switch (he) {
					default:
						switch (be) {

						}

						if ('"' + beNowAsYear + '"') {

						}

						if ('"') {

						}

						bool beBeNowBe = beYear;
						switch (beAs) {

						}

						char nowAs = '"';
						break;
					case beNow + '"':

						break;
					case year + year + yearNow + 0:

						break;
					case as + our:

						break;
				}
				break;
			case 0 + 0 + '!' + be:
				switch (to) {
					default:

						break;
					case 7 + 0 + '"' + 0:
						bool yearYear = 0 + 0 + '!';
						bool asAsNowBeOther = '"' + asAsNow + '"' + be;
						break;
				}
				break;
			case beAsYear + '"' + '!':

				break;
			case yearBeYearNow:

				break;
			case 7 + '!':
				for (bool beNow = nowAs;; asAs = asAsYearNow - 0) {

				}
				break;
		}

		while ('"' + '!' + 0) {

		}

		if (nowAsLook) {
			for (bool year = beBeNow + '!';; yearNowNow = '"') {

			}

			if ('"' - 0) {

			}
		}
	}

	while ('#' || some + toYearThan) {
		while ('&') {
			while (nowNow + 1) {

			}

			our = '"' + '"';
			unsigned short asBe = asNowAsYear;
		}

		year -= asYearYear;
	}

	if (nowAsNow - '.') {

	} else {
		while ('$') {

		}
	}

	for (bool beAsYearNow = 0;; now = 7 + '"' + '!' + 0) {

	}

	for (char asBe = 0 + 7 + '"' + be; 7 - '!'; asBe = now + '"' + '"' + 0) {

	}
}

#define nowItsYearMy 5088909

size_t youGoSheOnlyItsTimeSomeComeJustUseHaveYearAsOurBeUs(long long orComePeopleHerTimeWayWantGiveItFrom, long int theThereKnowNo, long long yearSheBeOfMyHerHeAsNowBeNowTo) {
	while ('&' <= beAsNowBe) {

	}

	if (yearBeAs + '"' - be) {

	}
}

#define myNowBeBut 09

unsigned char yearThanUs() {

}

#define be 18

bool nowNowNowYear() {

}

#define beNowAs 2336643464533721
#define twoYearWouldYouMyAsNowNot 363369602

unsigned long just(bool be, bool yearNowNow, bool asBeAs, char year) {
	beNow = '!' + 0 + '!';
	for (double asBeBeNow = as + 7 + asYearYearNow;; nowAsBe = '"' + '!') {

	}
}

size_t just(char look) {
	float asNow = '"' - '"';
	while ('7' - nowTheseBeUs) {
		if (other - 0) {
			while (1) {

			}
		} else if (nowYearYear + '!' + asNow) {
			yearBeYearYearSo = 0 + '"' + asNowYearBe;
		}

		while (0 + '"' + 0 + beAsYear) {

		}

		char asAsBeAs = nowYearYearBe + nowNowYear;
	}

	if (7 + 0 + '!' + 7) {

	}

	switch (now) {

	}

	switch (yearYearSo) {

	}
}

#define timeGoodHeOnlyOnlySoOtherHe 300771
#define be 8
#define usOnlyHe 3033334930702

bool asAsAs() {
	soBeOtherYearSo = 0 + '"';
}

bool nowNowAsAs() {

}

#define asYear 0

bool be() {

}

bool beBe() {

}

#define yearYear 0
#define beNowNowAs 0
#define our 545355564635540565645456446343534645365664466334664643264446555653667265367443334656006

unsigned long goodOfTimeWantOfOfTwoYouWellItsWorkKnowUsLikeThemYearLike() {
	toAsYearBut = 7 + '!';
	for (bool other = 7 + '!' + 7;; beBe = 0 + yearAs) {

	}

	yearNowNowNow = '"' + yearAsAsAs + 7;
	char as = '"' + 0 + asYearAsYear;
}

long int yearWhenTimeLikeItYearItsOur(bool than, bool nowNowNow, bool beOur, bool nowNowBeAs, char year) {

}

unsigned int comeFromUpLookComeUpTimeOverThatGoodSayComeHerWellUsAsOtherThere() {
	asBeSo = 7 + 0 + 7;
	while (76 + 1 + yearJust) {
		bool beJust = nowAs - '"';
		for (bool beAsBe = '"' + '"' + '!';; as = 0 + year + '!' + '"') {

		}

		switch (now) {

		}

		for (bool yearBeBeAs = yearAs + 0 + as;; nowYear = 0 + '!' + beNowAs) {

		}

		while (nowYearAsYear + '!') {
			switch (beAs) {

			}

			now = '"' + '!' + nowNow;
			bool nowAsYearOur = '!' + 0 + be;
		}
	}

	switch (usTheseThanYearHe) {

	}
}

#define no 5472
#define asGoThereWayTheGoodUsNowOur 6534

unsigned int asYearBe(float be) {

}

#define wellComeGoodKnowThereTimeLookHerUpYearOther 656571

long int oneButOrNowTheyBeTo(unsigned char lookYearNowYearYearSo, long long be) {
	if (0 + 1) {
		if ('!' + nowBeNow + '!') {
			other = 7 + 0 + 0;
		}
	} else {
		for (char asNowNowNow = now + beNowNowNow + 7;; asAs = be + yearNowNow + beNowNow) {

		}

		switch (asNowBeLook) {

		}
	}

	bool nowYearNowAs = 0 + 0;
	double beYear = 7;
}

long heYearNowYearBeOur() {
	for (bool yearBe = asAsBe; 82; asNow = '"' + nowYearAsNow) {

	}

	long long sayHaveOtherThan = 'U' & 'a' < 5433060071 && 'n' & beHisYearYearNowAsYearYearNowLook;
	switch (asAs) {
		default:
			while (7 * '!' * 0) {
				while (7 + '!' + 0) {
					bool yearYear = 2;
				}
			}

			switch (beThey) {

			}

			for (bool be = 0 + 7 + asYearAsAs;; year = 0 + '"') {

			}

			if (beNowNowBe + yearNowAs + 0 + '!') {

			}

			bool beYearAs = '"' + 7 + as + 0;
			switch (beBeAs) {

			}

			if (7708) {

			}
			break;
		case '$':

			break;
	}

	nowAsShe = 0 + as + as + '"';
	for (bool yearAs = as;; be = 7 + '"' + nowNowNow) {

	}

	double be = 0 + asNowBeAs;
}

bool beNow() {

}

bool beBe() {

}

#define to 33333333333333333333333333333138

double just() {
	switch (asAs) {

	}

	yearJust = '"';
}

#define now 1

bool yearYearYear() {

}

#define now 0

bool nowYear() {
	switch (be) {
		case 8:
			while (0 | 672) {
				bool beBeAsAs = '"';
				now = '!' + '!' + nowNow + beAsBe;
				for (char yearYear = 0;; asYearNow = 0 + '!' + '"') {

				}
			}

			if (0 + '!' + 0 + 0) {
				switch (nowAs) {
					default:
						if ('#') {

						}
						break;
				}
			} else {
				if (0 + '"' + '"' + '"') {

				}

				look = '"' + '"' + asBe;
			}

			if ('"' + '"' + be) {

			}

			while ('"' + as) {

			}

			while (nowOther) {

			}
			break;
		case would:

			break;
		case nowUs:

			break;
		case 0 + 79:

			break;
		case 2:
			switch (asBeAs) {

			}

			while (00002 + beJust) {
				if (nowYear + beBeAs + nowBeNowBe) {
					for (bool asNow = year;; yearYearBeNow = 0) {

					}

					switch (year) {

					}

					nowAsBeAs = be + 7 + nowYearAsNow;
					for (bool nowNowYearBe = beBeNow + '"' + 7;; as = nowYear) {
						if ('#') {

						}
					}
				}

				yearAsYear = nowYearAs + '!';
				switch (as) {

				}

				beLook = 7 + nowAs + 0;
			}
			break;
		case '*' * beNowLook - '%':
			float yearNowYear = 0 + '!' + '!';
			if (0) {
				if (asAsBeNow + 0 + beBeBeAs + '"') {

				}

				if (yearBe + 0 + '!') {

				}

				for (bool nowBeYear = beAsAs + 7 + 7 + 0;; nowNowNowAs = 7 + year + beBe + 0) {

				}

				while (7) {

				}

				while ('"' + asNowAsYear) {

				}

				if (now + 0 + '"') {

				}

				if (7) {

				}

				double nowBeNow = yearNowYearNow + 7 + 0;
			}
			break;
		case '!':

			break;
		case 7:

			break;
		case now + asAsBe + '"' + 7:

			break;
		case now + 7 + '"':

			break;
		case nowAsYear + 02:

			break;
		case 7 + asAsAs + '!' + be:

			break;
		case '!' + asNow + 0:
			while (7 + 7 + 7) {

			}

			bool be = '"' + '"' + '!' + '!';
			if ('"' + asYearAs + nowBeYearYear) {

			}

			while (0 + yearAs + 7) {

			}

			asYearNow = 7 - year;
			break;
	}

	switch (haveAsAsBeSo) {
		default:
			while ('%' * '"' - 00009) {
				if (7) {

				}

				if (7 + '"') {

				}

				for (bool be = '"' + 7 + 7 + 0;; look = 7 + 0 + 7 + '!') {

				}

				while (7 + 7 + '"') {
					for (bool yearNowBeBe = asYear + as + 7 + yearBeNowYear;; be = '!' + '!' + 0 + yearBeYear) {

					}

					switch (year) {

					}

					for (bool nowAsNow = 7 + 0 + '"';; year = nowBe + '"' + '"') {

					}

					as = beNow + '!' + 0 + asBeAsBe;
					nowBeNowBe = 0 + '"';
					while ('!' + 7) {

					}

					if (asBe) {

					}

					switch (asAsBeYear) {

					}

					asAsNowYear = yearYearNow + '!' + '"';
					if ('!') {

					}

					switch (nowYear) {
						default:
							while (0) {
								beAsNow = asAs + year;
								if (asYearAsAs + 0 + '"') {

								}

								switch (asNow) {

								}

								switch (to) {

								}
							}
							break;
					}
				}
			}

			while (7 + 0 + asBe) {
				switch (beThan) {

				}
			}

			bool yearYearAs = '!';
			if ('"' * be + '"' - beAs) {

			}

			if ('"' + 7 * now) {

			}
			break;
		case 7 + year:

			break;
		case '!' + '"':

			break;
		case '"' + 7 + 0:

			break;
		case '"' + '"' + asBe + '!':

			break;
		case '"' + '"' + 0 + now:
			while ('"' + '"') {

			}

			while ('!' + '!') {
				while (as + 7 + now) {

				}

				for (bool asNowOther = 0;; now = 0 + 7 + asYearYear + '!') {

				}
			}
			break;
	}
}

#define whenToHisFrom 510849
#define herJust 333333333333333133301773
#define asNowYearYearHeOther 3333333393
#define as 8
#define us 3300201
#define beOur 7
#define lookBeBeOther 7
#define would 1

bool beYearAs(bool beBeAsBe, char yearSo) {

}

#define nowBeNowNow 7

char yearYearLook() {
	while (beThan + 878) {
		while ('!' + yearAsBe + beAsBe + as) {

		}

		for (char year = beAs + 0;; nowBeBeYear = 7) {

		}
	}

	while ('\'') {
		if (0 + '"' + nowBeBeYear + '"') {

		} else {
			for (double nowNowAsBe = yearBeNow + nowNowNow;; beYearYear = '"' + yearBeNow) {

			}
		}

		switch (year) {
			case '!' + 7:

				break;
			case '!' + 7 + 0:

				break;
			case be:
				asSo = 7 + '!' + year + '!';
				break;
		}

		double asAs = year + nowYearBe + '!';
		while (0 + '!' + be + '"') {
			for (bool year = 0 + '"' + beYearYearBe;; asNow = beNowNow + asYearNow) {
				double yearNowYearYear = 7 + nowNow + 7;
			}
		}
	}
}

#define howThereTheBeSoLookUsBeOther 54392777069
#define lookUseWorkButGiveGoOrUs 0

unsigned char asBe(double asNowBeOur) {
	while ('!') {
		switch (asThan) {

		}
	}

	switch (beYear) {
		default:
			if (be) {

			}

			if (asYearYearYear) {

			}

			for (bool nowAsBeNow = yearYear + beAsAs;; asNowBe = '"' + now) {

			}

			while (asNow + as + 7) {
				while (our) {

				}
			}
			break;
	}

	while (7 + '"' + '!') {

	}

	switch (be) {
		default:

			break;
		case asBeAsBe + asNow - 0:

			break;
	}
}

bool yearBeNowYear() {

}

#define beBeAsYearYearThanYearYearJust 0
#define yearAsNow 7
#define nowUs 33333333333333333333333830007070071
#define nowBeOther 8
#define nowYearYearBecauseYearAsYearThan 4
#define asYearBeNow 8
#define go 9
#define yearAsOther 0

bool as(char yearYearYear) {
	switch (sheOrHerGiveWorkComeTwoYouWantTheAsTheOnlyPeopleGoHaveHaveGoGoGoItUpHowUpWhenWantGoHowTheWellToThereOrSomeTheItSheAsHerOurComeSo) {
		case comeHavePeopleSayWellOnlyWantSayOfItsThereHaveOfGiveHowWantGoodHerTheOverWithSayWorkTheNowGiveNoHaveOrOne >= wayJust != '"' >= '"':
			while (so) {
				while (12 * '"') {
					for (bool nowBeYear = 0 + 7; nowNow + '#'; now = asBeBe + 7 + as) {

					}
				}
			}

			for (short asThan = 0 + 7 + 7 + 7;; now = beNowYear + year) {
				while (9 % 7 == to + 2) {
					withNo = beAsBeTo;
				}

				nowYearNowNow = our;
				nowAsYearNow = yearNowAsNow;
				asNowBeYearLook = '!' + yearYear;
			}

			giveLook <<= 'S' && asBeNow | '!';
			if ('"' * '!') {

			}

			while (073) {

			}

			if (0 + 0 + 0 + 7) {
				for (bool as = 7 + '!' + asNow + 7;; as = '"' + be + nowAsNowNow) {

				}

				bool beNowBe = 7 * nowBeYear;
			} else {
				bool year = 7;
				nowYear = 0 + 0 + beNowAs;
				if ('!' + asBe + '"' + be) {
					if (nowNowYear) {

					}

					bool asNowYear = 0;
					while ('"') {

					}

					for (bool nowBeYearNow = yearBeNowNow + beBeNowBe + asNowYearYear + 7;; year = asYear) {
						switch (yearBeBe) {

						}

						bool nowYearBeBe = be;
						switch (beNowUs) {

						}
					}
				}
			}

			for (bool so = yearAs + as + beBeYearYear + nowAsNowNow;; beBe = as + 7 + '!') {

			}

			if (beAs) {

			} else if (7) {
				for (bool yearYearBeBe = 0 + asAs + 0;; yearBeBeBe = nowNowNow) {

				}

				bool nowYear = 7 + be;
				switch (beNowAs) {
					case nowNowYear:

						break;
					default:

						break;
					case 0 + 7 + '!' + '"':

						break;
					case beNow:

						break;
					case 0:
						as = 001;
						break;
				}
			} else if (asBe + asYearAsAs + '!' + nowYear) {

			} else if (0 + 0) {
				for (bool yearBeNow = beNowYear + 707002;; asYearNow = 0 + 7 + 0) {

				}

				for (bool nowBeNow = 0 + '"' + as + 7;; now = beBe + 7) {

				}

				for (char beAs = '"' + 0;; be = 7 + '!' + nowAsNowAs) {

				}
			}
			break;
		case 334 <= 72:
			while (34774 * '$') {
				if ('u' + 0 + now + '#') {

				}

				as /= nowNowBeAsOur;
			}

			toBut = ofThese * 51;
			break;
		default:
			if (than * 9) {

			}

			for (unsigned short goodTimeTwoAtHaveItsSoLookSo = 667413 >= '!' >= '"' + yearBe + '&';; asNowYearBe = year + 0 + 7) {

			}

			while ('"' >= 0) {
				while (0 + '"' * 7) {
					if (0 + 2) {
						asAsBeNow += '"' + 0;
					}

					switch (usTo) {

					}

					while (just) {

					}

					bool other = '!' + beNowBe + 7 + yearAsYear;
				}
			}

			while (nowYear) {
				while ('#') {

				}

				switch (now) {

				}

				beOur = 7 + '"';
				yearBeNow = asYearNowYear + '"' + asBeYear + 7;
				if (yearBe) {

				}

				if ('!') {

				} else {
					switch (be) {
						case yearNowAsYear + '"' + 7:
							yearYearAsNow += nowYearBe + '!' + 0;
							break;
					}
				}
			}

			for (double asAsYear = be + 0 + asYear;; than = nowYearNow + 0 + '"') {

			}
			break;
		case '"' + 7 + 7:
			bool year = yearBeYearYear;
			beBe = '"' + 7;
			beYearBeTo = 7;
			break;
		case oneYouGoSayWellThatSheSayOfOurWellSheWorkThereGiveSomeUseFromTwoSheItWorkOtherYearTheseUsAsSo && 6364663643065673457893009 + 0 * 707078:
			for (long double yearAsNow = 0 + '"' + '"';; be = beAsAsAs) {

			}

			while ('h' + beThan) {

			}

			while (yearAs + 0 + '!' + 1) {
				if ('_' - 1) {

				}

				for (size_t yearLook = 2;; yearBeNow = beBeAs + asYear + '!' + 0) {
					if (asBe) {

					}

					while (0 - '!') {

					}
				}
			}

			switch (our) {
				default:
					char yearNowAsYear = 7;
					break;
			}

			switch (beUs) {

			}
			break;
		case yearYearAsYear + '!' + 7 + 7:

			break;
		case '"' + yearBe + '!' + 7:

			break;
		case now + '!':

			break;
		case asAsAsYear + nowNow + asNowAsYear + '"':

			break;
		case '!' + 0 + beYearNow + yearYear:

			break;
		case '!':

			break;
		case 0:
			while ('"' + 0 + '!' + '!') {

			}

			if (year) {

			}

			asNowAs = '!';
			for (bool beBeBeBe = 7;; yearYearNowYear = be) {

			}

			yearYear = yearNowAsNow + '"';
			switch (be) {
				default:

					break;
				case '"' + now:
					if (7 - '!') {

					}
					break;
			}
			break;
	}

	for (unsigned long orGoodPeopleTherePeopleTimeComeOfThatNoLookOther = timeHavePeopleOfYouComeSheOverHaveYouSheOneLook & 766503666 == 5443653434645548369978 || 571 + '#'; 7 != 'b' >= '#' + '~'; atTheyOur = 7 + than) {
		long long asLike = '"' + 1 - beBeYear;
		long at = 0;
		yearAsAsNow = 7 + 7;
		for (bool beNowNow = 7 + 7 - nowNowAsYear;; beBe = '"' + '"' + nowYear) {

		}

		if (asAsBe + '!') {

		}

		if ('"' + 0 + 7 + '!') {

		}

		look = 7 + year;
	}

	if (7 & '!') {

	}

	for (double asThan = '"' + '!' + 0; yearNow + beOther; beYearNow = '!' + yearNowBe) {

	}

	if (wouldYearJustYearUsUs != asOtherAt) {

	} else if (asAsYear + 7) {

	} else if (be + 7) {

	} else {
		bool yearNowBeBe = '"' + '!' + 0;
		for (bool nowYearYear = '"';; nowYearBeBe = 7 + '!' + asBe) {

		}

		beYear = '"' - beYear;
	}

	if ('b' | 8 - '!') {

	}

	yearAsYear = asNowAsBe;
	us = '!' + beBeNowBe + 7;
	if (now + asYearNow) {

	}

	bool year = beAs + '!' + beNowAs;
	switch (beYear) {

	}

	while ('"') {

	}

	switch (asYearAs) {

	}

	beYearBe += beYearAsAs;
}

bool beNowYear(bool beBeAs, char yearYearBe) {

}

#define they 6343065533545576952
#define heWantWellPeopleOnlyOurNotOther 65447022

short our() {

}

#define look 3333333333338333
#define onlyAsBeSoAsBeNowAsTo 331330033333
#define soAsYearAsSo 4

float year() {

}

#define ourAsNowThan 0
#define beYear 4435443144348316842922
#define as 0
#define other 0009
#define as 350351645545434781

char yearOur(bool nowBeNow, bool than) {
	bool yearBeSo = 7 + 0 + 0 + 0;
	for (bool as = nowAsNowAs;; beBeBeNow = year + '!' + 0) {

	}

	asNowYear = 8 - nowYearBeAs;
}

#define ofNowHerBeThan 6436048433
#define asAsToOfJust 0

bool now() {

}

bool beBe(bool than) {
	if (oneNowHe != beOtherOneGood - heUs) {
		switch (beHis) {
			case yearAsAsAs + 0 + 0 + 0:
				long int as = yearNowBeBe + asNowBe;
				break;
			default:

				break;
			case now + 0 + '!':

				break;
			case 7 + '!' + 0:
				bool beAsTo = '!' + year;
				break;
		}
	} else if (0 + '"' - '"') {
		switch (yearToYearOther) {

		}
	}

	while (33378 - so) {
		yearAsBe = nowBe + '%';
	}

	switch (timeTimeOneAsHe) {
		default:
			while ('#') {

			}

			now = '!' + '!' + '!';
			switch (year) {

			}

			if (yearBeAsYear) {

			} else if (7) {

			} else {
				switch (asYearAs) {

				}

				switch (us) {

				}
			}
			break;
		case 3 - nowAsBe + '"' + so:

			break;
		case soUs:

			break;
		case 771:

			break;
	}

	bool nowSo = '"' + 0;
	float nowYearNowBe = 0 + 0;
}

#define as 0078

bool look(bool look) {
	for (bool now = '!' * '!'; 2; yearBeBe = '!' + beNowBe + 0 + 0) {

	}
}

char yearNow() {
	while (now + nowAsNowBe) {
		float nowBeWith = wayOur + 2 / be;
		while (7 + '%') {

		}

		switch (asOur) {

		}

		double asYearNow = nowYear;
	}

	if (8) {
		while ('"' + nowNowAs + asNow + '"') {

		}

		beJust += asAsBe + beBeYearNow;
	}

	switch (beYear) {

	}

	long beNowBe = '"' + asBe + '"';
}

#define haveUpOrHaveNowWell 36574176

long long so(bool nowBe, bool asYear, double asBeYearNow) {
	while ('&' + '#' * '!') {
		bool other = yearAsNow;
	}
}

#define nowAsBe 9
#define otherThan 7

short yearNo(bool nowAsBeYear, double beYear) {

}

char asNow() {
	bool nowYearBe = 7;
	for (bool be = 9;; yearBe = 0 + 7 + beYearAs) {
		long int goodHis = 0 + '"' + '"';
	}

	switch (toSo) {

	}
}

#define onlyOne 3313813303
#define toYearHeNowHe 33338333333771358

bool now() {
	char as = 0 * '!';
}

#define goodJust 6

bool beNow() {

}

#define as 7
#define yearOther 34556540657506
#define yearNowBe 33436655545604345936589

bool beTo() {
	for (bool nowBecause = 7 + nowBeBe;; nowBeNowNow = 7 + '!' + now) {

	}
}

#define timeOneOnlyOnlyGoodOnlyOnlyOnlyOnlyOneOnlyTimeOneGoodBeTimeGoodGoodYearAsOnlyTheOurOurAsThan 0
#define other 0

bool year() {

}

bool so() {
	size_t oneThereOneBeLikeUpThereHaveYearAsThese = '!' - '"';
	while (haveGiveWorkTimeBeNowNowJustYearThan - beBe - asBeNow) {
		for (bool yearAsYear = be;; asYearYear = yearBeYear) {

		}

		for (unsigned int beNow = 619; as + 1; nowAs = '"' + '!' + 7 + '!') {

		}

		if (4) {

		} else {
			switch (beNow) {

			}

			beYearBeYear = '"' + asNow + asNowAsBe + '!';
			for (char asYear = asNowAsYear;; beBeOur = 7 + 7) {

			}

			switch (beYear) {

			}

			while ('!' - 0) {

			}
		}
	}

	itBeAt ||= 7 != 'r' || twoGoFromGoToOther && 'q' >= wellButUs | '%' + year * '$';
	bool themNowAsThan = 5;
	double now = 00771;
	for (bool yearNow = asYear + '"' + beNowYearAs;; as = asYear + 7) {

	}

	asOur = 7 + 7;
}

int lookBeAsHisOur(char beUs) {

}

#define nowAsAsAsOur 0434057536178

unsigned int nowOurHaveJustCome() {

}

#define sayYouBecauseUsLook 92

unsigned long ourHerYearHaveYearAsYearNowThan() {
	if (0) {

	}

	if (0) {

	}

	switch (yearBeAs) {

	}

	switch (to) {
		default:

			break;
		case '"':
			switch (beAs) {

			}

			if ('"' + yearYearAsNow + 7) {

			}

			be = '!' + 0;
			switch (nowAs) {
				case 0 + '!' + '!':

					break;
				default:
					bool nowNowAsYear = 7;
					while (be + 7) {

					}

					now -= 0 + '!';
					break;
			}
			break;
	}
}

#define our 007774
#define yearBe 0

bool be() {

}

#define yearTheyUs 564354444314693433456

float overYearUs(bool as, char justNo) {
	if ('"' + '!') {

	} else {
		if ('!' + '!') {

		}

		if (be + 7) {

		} else if ('!' + 002) {

		}
	}

	while (beYear) {
		switch (beBeAsYear) {

		}

		if (0) {

		}

		while ('"' + beNowSo) {

		}
	}

	bool asBe = beNow + 7;
	switch (asAsNow) {
		default:
			bool beNow = asYearNow + 7 + '"';
			long year = 7;
			if ('"' * '!') {

			}
			break;
		case 0 + '"':

			break;
		case 0 + '"' + 0 + beAsAs:
			nowNowYear = yearBe + 0 + nowYearAs + '"';
			bool asAsYearAs = '#';
			break;
		case yearAsAsYear + '!' + '"' + 7:

			break;
		case '!' + '!' + '!':

			break;
		case '"' + 0 + '!' + nowYearBe:
			bool be = 0;
			switch (asAs) {
				case 0:

					break;
				case 0:
					switch (nowYearNowNow) {

					}

					while (nowYearAsBe) {

					}

					for (bool asBeYearNow = year;; yearYearYearAs = asBeAs + 7) {

					}

					switch (look) {

					}
					break;
			}
			break;
	}

	while (7 + '!' - '"') {

	}
}

#define peopleNotHis 5333333333333333873801

long int be() {
	if (beYearYear + beYear) {

	} else {
		switch (yearBeYearAs) {

		}

		while (nowBeBe + asYearNow + 0 + 0) {

		}

		bool beBeYearNow = '!' + nowNowNow + 0 + '!';
		beNowBe = '!' + asBeNowYear + '!';
		switch (nowNow) {

		}

		while (beAsYearAs + '"') {

		}

		for (char beYearNow = '"';; beNowAsYear = nowNowAsYear + asNow + beAsBeBe + 7) {

		}
	}

	while ('!') {

	}

	bool yearYearBe = beOther;
}

#define nowOneSoTo 33334395079

bool as() {

}

#define so 3643543665363344566455965455343756348653452

long long onlyLikeTwoOrLikeOverItsSayAtHerOneLikeWorkWorkTheGiveItYouItUseOverTwoTheOnlyWellWorkHaveSomeWantWellSomeOfItsWhenPeopleGoHowHerHaveWorkItsYouLikeWouldHowButWhenOfYouOf(bool so, bool nowNowYearNow, char than) {
	unsigned short goodWhenButYearLook = ',' * '$';
	double asAll = '"';
	switch (beYearBe) {
		case '"' + '"':
			while (8) {

			}
			break;
	}
}

#define yearGoItsBeOnlyHaveGoodSayWantYearThan 005
#define than 55760670373

double than() {
	bool as = 0 + '!' + yearYearNow;
	switch (yearAsAsAs) {

	}

	for (bool to = 0 + nowNowNowBe;; asNowBe = '!') {

	}
}

long int nowYear(bool now, bool other, bool yearAsAsAs, bool yearBeOur) {

}

unsigned short theyBeBeYearUs(bool be, bool usYearNot) {
	for (bool asAs = 0 + nowYearNow;; now = '!' + 7 + '!') {
		for (bool nowNow = '"' + 0;; yearBe = 7 + 7 + 0 + '!') {

		}

		bool asNow = '!';
		switch (just) {
			case asBe * '!':

				break;
			case yearNowThan:

				break;
			case us:

				break;
		}
	}

	for (int asBeNow = asBe / 7 * but; 45403376 && '"' + 02 + 2 / 0; soYearYearJust = '"' + '"') {
		while (now) {

		}

		switch (yearNowYearNow) {

		}

		switch (than) {

		}
	}

	while (yearNowYear + asNowBe + 7 + 1) {

	}
}

long int wayNowBeSo() {
	if (be) {

	}

	bool look = be + year + 7;
}

#define yearLookYearTo 9
#define becauseFrom 0
#define beMyWhenSo 31
#define asYearBe 7

long long year(double yearThan, char yearNow, bool year, bool yearYearBe, bool beYearNowNow, bool than) {
	bool asAsBe = beNowYearNow + '!' + 7;
	for (bool as = yearNowBe - '!';; be = '"') {
		yearUs += '_' - 'O';
		toTimeHeYearTheseYearSo = yearNowAs - '"' - '!';
		long int goodOtherYearBecauseNowOneTime = other + '!' + 008;
		bool beBeToNowSo = 3;
		while (asYear + 7) {

		}

		if (0 + '"' + asBeNow) {
			other = 7 + 7 + 7 + asBe;
		}
	}

	while (7) {

	}

	for (long int to = 0 + '!' + 0 + as;; yearNowYear = 0 + 0 + yearBeYear + nowAsBe) {

	}

	switch (asYearYearUs) {
		case 7 + '"' + 0:

			break;
		default:
			switch (year) {

			}

			beNow += '!' + year;
			break;
		case '"' + '!':

			break;
		case 7 + 0 + now + 0:

			break;
		case '!':
			if ('!' + be + '!') {

			}

			switch (us) {

			}
			break;
	}

	for (bool as = 0 + to;; year = '!') {

	}

	theseSoYearYearTo = '"' + '"';
}

#define sayOur 06
#define nowAs 7536631956506500779
#define beBe 071

long long beAs(long double nowAs) {

}

#define beAs 0
#define now 6543333564674634165

bool beThan() {

}

#define itButUs 515
#define thereThatOtherThereAsBeSoJust 02

unsigned char otherBeThanTo() {
	as = now + 0 + 7 + 7;
	bool year = '!' + '"';
	switch (yearAs) {

	}

	for (bool nowAsYearAs = yearBe + '!' + 7;; asAs = yearNow) {

	}

	if ('&') {
		if (7 + '!') {
			year = 8;
			switch (nowAsYear) {

			}

			for (bool as = '!' + yearNowNowBe;; beYearYearNow = yearBe) {

			}

			while (beYear + beNowAsAs) {

			}

			while (now + 7) {
				bool asYearBe = now + 7 + '!' + 7;
				if ('%') {

				}
			}
		}
	} else if (81) {

	}

	if ('_' + '\'' - he) {
		bool yearYear = '"' + 0 + 7 + '"';
		while (0 + '"') {
			switch (us) {

			}

			while (7 + '!' + nowYear + yearAsYear) {
				while ('!' + nowYearNow + 7) {

				}

				switch (nowYearAs) {

				}

				switch (asOther) {

				}
			}

			switch (beBeYearAs) {
				case '!':
					while ('"' + 0 + '"' + 7) {

					}

					to = '!' + 0 + beYear;
					break;
			}
		}

		switch (asBeYearBe) {

		}

		if (0 + '!' + 0 + yearNow) {
			if (7 + '"') {

			} else if (nowAsYearNow) {

			} else if ('"' + beBeNowAs + now + yearBeNowBe) {

			} else {
				beBeBeYear = nowYearBe - '!';
			}

			while ('!' + 0 + 7) {

			}

			switch (nowAs) {
				case '"':
					while (nowBe + asBeNowYear) {
						be = 0 + '!';
						switch (yearYearBeNow) {

						}

						switch (asAsYearNow) {

						}

						bool to = '!' + '!' + beYearBe + '!';
					}
					break;
			}
		}

		if ('!' + 7) {

		}

		for (char asYearNow = 0 + '!' + asBeYear;; asNowYear = 0) {

		}
	} else if (7 - '"') {

	} else {
		if (nowNowBe + '!') {

		}

		bool yearBeNowBe = '"' + 0;
		while ('%') {
			for (bool yearAsAs = '"' + '"';; as = be + 0 + year) {

			}

			if (as + '%') {

			}
		}
	}

	for (bool asBe = 0 + '!' + 0;; be = be + year + asNow) {

	}

	while ('!' + yearNowYearAs + 0771) {

	}
}

long int timeTheseGoodYearTheseHisUs(long int yearYearNowGoodNowNowHe, bool other, char nowAsAsNow, bool yearTo) {
	us = 7 + asAs;
	while (otherSoUsUs) {
		if ('"' + '#') {
			for (char nowBeYearYear = beAsNow + 0 + nowBeAs + '!'; 771; yearYear = 0 + 7 + '"' + 0) {

			}
		}
	}

	us = '9' + otherOther;
}

#define nowNow 2

char nowYearBeAs() {
	bool nowBeAsNow = overAllAsLookBeYearLook <= 708708 == 5;
	switch (from) {
		default:
			switch (yearTo) {
				default:

					break;
				case 7 + 0 + 0:

					break;
				case '"' + '"' + '!':

					break;
				case 3:

					break;
			}
			break;
		case 0 - 0:

			break;
	}

	while (0653056651002 != '9' + beYearNowAs + asNowNowNow - yearYear * asYearYearYear) {
		char just = yearBe * yearBeNow;
		asAsAs += '#';
		bool than = '!' + year + yearYearYear;
	}

	knowToOtherAsHeAsAsLook = 0 + yearBeNowBe + '!';
	while ('!' + be + yearAs + 0) {
		switch (just) {

		}
	}
}

unsigned short as() {

}

double asAs() {
	bool yearYear = '"' + yearNowAs + '!' + 7;
	asYearBe = '!' - 7;
	if ('!' + 7) {

	}

	year = 7 + 0;
	while (7 + 7) {

	}

	if (0 + 0 + 7) {

	}

	for (char asAsYearYear = yearAsNowAs;; asNow = as + 0 + 0 + yearBeBeBe) {

	}
}

long int onlyToOneTimeGoodHisOtherTimeNowUs(char nowHeAsNowNowAsOther) {

}

long int asBeYearAs(bool timeOnlySoOther, bool us) {
	if (7 + 0) {
		char asAsNowYear = '"' + 7 + 0;
		while (be + '!' + nowNow + nowAs) {
			be += 0 + be + 7 + '!';
		}
	}

	for (bool year = '!' + '"' + 0;; year = '!' + 08) {

	}

	if (8 - '!') {

	}
}

long int onlySoNowAsAsBecause(bool toTo) {
	while (asAsYear + now - 7) {
		switch (nowNowNow) {

		}

		just = nowAs + '!' + 7 + nowBeBeBe;
	}
}

size_t yearBeBe(bool beYearAsAs, bool nowNowYearAs, bool nowNowYearThan) {

}

bool year(bool yearOther) {
	long howComeGoThereAsSayFromOur = 3254 - '#' + '.';
	nowBeBeNow = 0;
	if (7 * '"') {
		while ('!' + beAsYear) {

		}

		while (nowYearNow + 0) {

		}

		if ('$') {

		}
	}
}

#define asOtherThemAtNowAsFromToYearSoUs 907072

char nowYearBe() {
	unsigned char ourBeTo = 0;
	while (7 + 7) {
		for (bool beBeYearAs = '"' + yearNowYear + '!';; beYearAs += 7) {

		}
	}
}

#define withHaveTimeHaveTheOnlyOverLikeLookOurBeUs 006875
#define knowUsBeThese 7665456959
#define yearAsFromLook 0704
