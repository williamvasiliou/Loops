#include <6U/EE2.h>

bool beBe(bool now, bool us) {

}

#define timeOneUsNowBeSoAsBeNowUs 8333371

bool usTimeYearSoHis(bool otherOther, bool as, bool nowYearUs) {
	toAsOther = 7 + beYearNow + yearBeYear;
}

bool because() {

}

long int nowOtherAsLook(bool beYear, bool to, bool asBeNow, bool asBeBe, bool so, bool asNowYearNow, bool asOther) {
	switch (asThan) {
		case now + '!':

			break;
		case beAsAs + yearBe + 7:

			break;
		default:
			switch (nowAsBe) {

			}

			while ('#') {

			}
			break;
	}

	bool be = yearYearYear;
	nowBeYearAs = 7 + '!' + beAs + '!';
	if (7 + 0 + '!') {

	}

	now += nowAs;
	switch (asAsYearBe) {

	}

	if (now + 0) {

	}

	while ('"') {

	}

	for (double beBeAs = '!' + beAs + '"' + 0;; beAs = 0 + 0) {
		switch (nowJust) {
			default:
				for (bool year = '"' + '"' + beYear + 7;; now = yearNow) {

				}

				if (now) {
					if (0 + '!') {

					}

					switch (yearNowBe) {

					}

					nowBe += '"';
				}
				break;
		}

		switch (because) {
			default:

				break;
			case than:
				for (bool yearBeYear = 7 + 9; '"' * '"'; justThan = '!' + year + 7 + 7) {

				}
				break;
			case 7 + '!' + 7:

				break;
			case nowNowYear + '+':

				break;
		}

		wouldOur += nowThanYearUsAsYearAsYearLook;
	}

	for (size_t beNow = asNowBe + beBeBe + as;; now = '!' + '!' + '"') {

	}

	while ('"' + '"') {

	}

	for (bool year = '!' + beAsYear; asJust + '"' + 7 * 0; as = '!' + '"') {

	}

	if (nowYearAs + beBeAs + '"' + nowBeNowYear) {

	} else if (0 + beAsNowYear) {

	} else {
		for (bool asBe = 0 + 0 + '"';; beAs = 0 + '"') {

		}

		bool nowYearNow = '"';
		for (bool now = 7;; yearYear = 7 + '!' + '!' + now) {

		}

		switch (nowJust) {

		}
	}
}

long long ofOrTheWhenItsWantOverItsItFromTheButBeSomeLike(unsigned short thatWantOrLookNowOrButNowBeThan, size_t ourUseBeSomeMyOur, char justNoSo, double than) {
	char nowYearYear = nowYearNowYear;
	nowYearYearBe = '"' + nowNowYearBe + '!' - beNowAs;
	for (double asBeYearAs = year + yearYearYear + '"';; just = 0 + '"') {

	}

	bool look = 0 + asAsBe + asNow + '!';
}

double otherOur() {
	bool yearNowYear = '!' + '"' + 0;
	switch (yearAs) {

	}

	for (bool nowYear = 7 + 7 + '!';; now = year + '!') {

	}

	switch (other) {

	}
}

#define yearAsNowBe 1

unsigned int beBeNow() {

}

unsigned long ofWorkAsWantNowAt() {

}

bool nowOur() {

}

long be() {

}

#define yearNow 7
#define as 0

bool other() {
	asAsUs += '*';
	lookTimeWouldThan = 34633572473396070778;
	for (unsigned int yearNowNowNow = yearTo; 376 / '"'; asNowBe = now + '"' + '"') {

	}
}

#define look 0

bool beNow() {

}

#define usSayWouldBut 13858
#define otherThese 53373333333337333333333133138

char beBeNow() {

}

char usYearOther() {
	nowYearBe += '"' + 0 + '"' + 7;
	for (bool asTo = as + '!' + '!' + 7;; asBeBeNow = yearAs + yearNowNowBe + year) {

	}
}

long int nowBeBe() {
	while ('"' + be + now + '"') {

	}

	for (bool beBe = now;; nowYear = 0 + 7 + yearYearAs + '!') {

	}

	bool now = '#';
}

long int asYearBe() {

}

bool yearAsBeYear() {
	asNow -= '!' + nowNowAsNow + nowAs;
	bool asYearBeNow = '"' + 0 + '!' + '"';
	for (bool year = '"' + beAsNowBe + '"';; yearBe = 7) {

	}

	switch (as) {

	}

	as = '!' + beBe + '"';
	beNowYear += 7 - '$';
	float nowNow = 7702;
	while ('"' >= 091) {
		double as = '!' + '!' + '"';
		if (7 + '"' + '!' + 7) {

		}

		for (bool yearYearYearBe = yearBe + '!';; asBe = '"' + 0 + 0 + beNow) {

		}

		bool yearNowNow = 7;
		yearYearBe = yearAsBe + 7 + 7;
		while (0 + 0 + 7) {
			if (asYearYearYear + be) {

			}

			if ('"' + nowYearAsYear) {
				yearNow = 7 + now + '!' + nowBeBeNow;
				while ('%') {

				}
			}
		}

		while (year + beNowYearYear + 7 + '"') {

		}

		bool look = 0 + '!' + 0 + 0;
	}
}

long int goTheWellJustTimeHeBeJustSoLook(bool year, size_t nowNowBe, bool beNowAsNow, bool asBe, long double yearYearAsYear, float nowBeYear) {
	nowSo =~ yearAs;
}

long int nowAs() {

}

bool our() {

}

long int lookWork(char yearOther) {
	switch (yearTo) {

	}

	if (nowBe * 7) {

	}

	for (bool yearAsBe = '"';; yearYearYear = 7 + '!') {
		bool yearYear = 7 + '!';
		switch (asNowNow) {

		}

		for (bool yearYearAsNow = '"';; now = 0 + beYear + '"' + yearBeYear) {

		}

		while (now + nowBeAs) {

		}

		if (0 + 7 + '#') {

		}
	}
}

char year() {

}

#define year 9
#define beAsNow 0

bool beBe() {

}

double beYear() {
	switch (goodTimeTimeOneTimeTimeOneHisTo) {
		default:
			bool yearNowBeYear = asAsYearYear;
			switch (beYearAsBe) {
				default:

					break;
				case 0 + year + 7 + 0:
					while (nowBeAs + asAsNowNow) {

					}

					bool yearBeNow = be + '!' + asNowAs;
					now += 0;
					break;
			}

			switch (beNow) {

			}

			switch (asBe) {

			}

			bool asAsBe = otherBeSo;
			break;
		case '"':

			break;
		case yearBeNow:
			switch (year) {

			}

			if (0 + nowBeBeNow) {

			} else if ('"' + yearAsNowAs + 7) {
				while ('"' + nowNowNowBe) {

				}

				asAsNowAs += asAsYearBe + '!' + 7 + beNow;
			}

			bool asNow = 7;
			if ('+' - '!') {

			}

			if (nowBeYearNow) {

			} else {
				char nowYear = beYear;
			}
			break;
		case '!' + nowYear + '!' + '!':
			for (bool year = 0;; as = '!' + asYear + 7) {

			}

			for (char so = 0 + 7 + nowAsAs + asBe;; now = year + 7 + 7) {

			}
			break;
		case yearYearAsYear / now:

			break;
		case '"' + '"' + 7 + nowBe:

			break;
		case year:

			break;
		case now + 0:
			while (beNow + '!' + 7) {
				switch (asBecause) {

				}
			}
			break;
	}

	asNowAsYear = 0 + '!' + 7;
	long int nowYearAsBe = '#';
}

#define oneOnlyNowHis 8

char heOther(bool yearAs, bool yearYear, bool nowOur) {
	bool beBe = '"' + 0 + nowBeBeYear + '!';
	double nowNowNowYear = 02;
	yearYearAsYear -= be + asYearBe + '!' + '"';
	beAsAsNow = 0 + 7 + 0 + '!';
	while (2) {

	}
}

#define yearBeSo 465365436535666363555455556456456445544643565605664453334446366534653365345333645335333333453635455645554346536453365363543453346355635444366635345365426534515644449635353634635455533355544
#define likeWantWhenSoWouldNowUseUsSo 463557780772

unsigned int orItsJustTheWorkTheseSheAsUseSayHowGiveAsOnlyJustYearTwoWhenNowHerWorkLook(char just, bool other, long double look, char as, bool our) {
	while (7) {

	}

	if ('$' - asAsBe) {

	}

	bool asNow = 0 + 0 + '"';
	long double asNowNowBe = '!' + '!';
}

#define useThemOfItJustBeYearWith 81089
#define other 765
#define useUpOrToUsBeUp 7646609
#define goodItsWorkOneUpHaveHowHeKnowOrItOneOrAllUsWhenYearLookSoThese 4593533333337333333333033333333187808
#define his 7318

bool be(bool yearBe, bool beAsBe, bool no) {

}

#define themAsOther 606
#define asBeAsAs 0
#define beYear 58

bool than() {

}

#define asBeAsYear 63554553443135300344006
#define wantWouldHeItSomeAsNot 4854051

bool beBeOur() {

}

#define nowBeLook 0
#define asBeNow 0

double yearAsAs(bool yearYearBe, bool our) {
	switch (oneOnlyOnlyOneNowOther) {
		default:
			char be = '+' + 0 - 7;
			break;
	}

	time |= '/' + '"' + us + nowAsYearOther;
	beNowAs = year + 7 + 7 + year;
	while ('!' + be + '"' + now) {

	}

	yearAsOther = 7;
	float asYear = '"' + yearYearNowNow;
}

#define oneHisGoodToYearOneSoOnly 38346808

double myYearMy() {

}

char now() {
	 .= 'q' >= 56454366464544545346456553633534366365333366635453334334635635535564565555635553433555663454434343355643536445335465643435535364463653356653554566343365654536946333646366464446763335535633635635534446535364505654633555663741261 >= 'a' == itAsKnowHowYearWorkAsGiveUpWay || wantOfSomeYouSheLikeWhenTheseSheComeThereJustPeopleOneWantOnlyKnow & 4761 % just;
	if (haveMyAllNowBeJust + '"' - as) {
		otherYearNowBeYearAsYearUs ||= '!' < '`' + '$' + 778;
		for (bool asNowBe = 0;; now = yearAsBeNow + '!' + '!') {

		}

		look = 0;
		beAsYearBe = '"' + 7 + '!';
		if (7 + 7) {
			for (bool beNow = yearBeAs + year; 2; asYearNowNow = 7 + '!' + yearBeBe + yearNowYearBe) {

			}

			while (709) {

			}
		}
	} else if (67459 + '#') {
		for (bool beAsAs = 0;; now = beBeYearBe) {

		}

		unsigned long beAsAsBe = '!';
		for (float other = nowNow; '#'; nowBe = 0 + 0) {

		}
	} else if (7 & '"') {

	} else if ('&') {
		while (009 - 0 - '"') {
			bool beAsAsNow = 0 + '!' + now + 0;
			switch (nowBe) {

			}

			beYearYearNow += '"' + be;
			bool nowBeAsNow = 0;
			now = yearYearYearBe + now;
			if (as + '"' + '!' + now) {

			}

			for (bool asAs = 0 + 0;; our = '"') {

			}
		}
	}

	if ('j' == 'a' != 5653545365543546455355434335365665434633565546656666236763665155353537483797800008 > 0070001 > 'n' & '/' == 'Z' % 748771 >= '+' == wantUs - yearAs * beYearAsYear) {
		goThatTheTimeSomeThereTheOnlySheOverKnowWellSheOnlyWantTimePeopleOnlyYouTimeItsItsThatOfYearItWantLookSomeThatToBeTheseAsYearWay &&= 'm' == timeUseUpOverHaveThanHowTwoSayOneTheWellKnowWhenThereOrComeGoodTwoOneAsLikeSheAsGoodNowNowUs || 5336 && theWhenWouldSomeAsYearYearTo == thanKnowAsSoUsNowBeBeNowBeSo | beNowNow && 656 + 5;
		if (9 + yearOur) {
			for (long long useYearJust = be + so * 1;; beUs -= '!') {

			}

			nowAs = '!' + 0;
			if (yearYear + 0 + 7) {

			} else if (yearNowYearNow + '!' + '!' + year) {
				while (0 + yearBeBeBe + yearBeYear + '"') {

				}

				double beAsYear = '"' + nowBeYearBe;
			}

			nowYearNowUs = asBe;
		}

		for (bool beAsYear = beAsBe + 0; '_' == yearNow - 0; nowAsBe = 78 - be) {
			for (bool year = year + 7 + yearAs + '"';; year = 7) {

			}

			for (bool asAsAsYear = '!' + '"' + asNow;; nowBe = '!' + beYear) {

			}

			switch (beYearBeBe) {

			}

			char beUs = 7;
			asNowYear = '!' + 7;
			while ('!' + '!' + 0 + '!') {

			}

			if ('"' + '!' + 0 + '"') {

			} else if ('"' + 7 + asBe + year) {
				for (double nowYearYear = 7 + 7;; be = yearYear + asNowNow + 7 + nowNow) {

				}
			}
		}

		char as = year;
		asBeYear = 0 + 0;
		while (0 + 7 - 7) {

		}

		double have = 7 + '!' + '"';
		bool as = '"' * '"';
	} else {
		long int useThereHerYouPeopleHaveKnowHowGoItPeopleOneWorkGiveHaveKnowYearSheNotWellUpWhenOfOneToOnlyNowWantOtherYearWellAsTo = itsTimeThan || so < nowNow;
		if (716 * nowHave) {
			beSo = than;
		} else if ('"' - beSo * '!') {

		}

		while (upOnlyItsSomeWellLookUseYearNotKnowNowHe / 7 * beAsNowNow + 7 + 0 - '!') {
			char asNowAsYear = '"';
			if ('"' != nowYearNowThan) {
				switch (our) {
					case '!' + '"' + '!':

						break;
					case '!' + 7 + '!' + 0:

						break;
					default:

						break;
					case 772 * 7:

						break;
				}
			} else {
				for (bool yearAsYear = 0 + '"';; beBe = '"') {
					for (bool nowYearNow = 7;; nowNowBe = 7) {

					}

					while ('!' + nowAsAs + nowYear) {

					}

					switch (beAs) {

					}

					bool look = nowAsYear + 7 + 0;
				}

				to -= '"' + '!' * year;
			}

			while (7) {
				if ('#') {

				} else {
					long double yearBeNowAs = nowNowNowYear + asYearNowYear + as;
					while (0 + yearYear + 0) {

					}

					switch (yearBe) {

					}

					bool nowYearAs = '!' + '"' + '!' + nowBeAs;
					yearYearBeNow = nowNowBeNow;
					switch (nowYearAsBe) {
						default:

							break;
						case asThan:

							break;
					}
				}
			}

			if (0 + 1) {

			}
		}

		while (us) {

		}

		for (bool other = 28 + asThat;; asYear -= '"' + year) {

		}

		nowYearUs = '!';
	}

	switch (hisToSoLook) {
		default:
			switch (wouldWhenOther) {
				case '!' + 0 + nowAs:
					if (0 + '!') {

					}

					bool yearNow = be + 7 + 0 + '"';
					bool yearAsYear = 7 + beBeNow + 7 + '!';
					while ('"' + nowBe) {

					}

					switch (beSo) {

					}
					break;
				case yearYearJust:

					break;
				case yearNow + nowBe + '!':

					break;
				case year + nowYearAsBe + 0 + asBeAs:

					break;
				case '"' + '!' + 0 + 0:

					break;
				default:
					if (nowBeYearYearThan) {

					}
					break;
			}
			break;
		case 7 + beNow:
			for (bool be = '!';; yearBe = '!' + '"' + 7) {

			}

			bool yearYear = 8;
			break;
		case '!' + now + yearAsAsYear:

			break;
		case '#' - '"':

			break;
		case yearHis:

			break;
	}

	long long nowOther = 46336541734190087009 == '+' & nowNotBeBeYearAsYearYearBeAsThan * 04;
	switch (atNowHeSoBeAsTo) {
		case 0 * '"':

			break;
		case '!' + now:
			yearNowBe = 7 + 7 + '"';
			if (0 + 7) {

			}

			if (nowYearYear + '!' + nowAsYear - 0) {

			}
			break;
		case '!' + 7:

			break;
		default:
			for (bool yearAsAsBe = '"';; beNowYearYear = now + '!') {

			}

			for (bool beAsYearNow = '"';; nowAsYearNow = 0) {

			}

			for (bool yearAsBeBe = '"' + '!';; beNowAsAs = '"') {

			}

			if ('!' + '"' + 7 + 7) {

			}

			switch (nowAs) {

			}

			if ('"' + 7) {

			} else {
				for (bool asAsBe = '"' + 7 + nowYearAs;; asYear = 7) {

				}

				if ('"' + '"' + be + beNow) {

				}

				if ('"' + '!' + '"') {

				}

				yearNowAs = '!';
				if ('!') {

				}

				nowAsYear = yearNowAs + 0;
				switch (nowNowYearAs) {

				}

				if (0 + '"' + 7) {
					yearNowLook = 0 + yearBe;
				}
			}
			break;
	}

	while (asAsNowNow + 0) {
		while ('!' + '"' + '"' + '"') {

		}

		if (0) {
			switch (asYearSo) {
				default:
					while (be) {

					}

					nowBe = 7 + 0;
					char yearYearAsNow = now + nowAs;
					break;
			}
		}
	}

	theKnowHerHisHowWithWantYou >>= 'a' || 61655174494336 >= '9' & asAsBe * nowBe;
}

#define wellPeopleTimeOrYouWhenUpYouKnowSheComePeoplePeopleThereOverWorkWellGoodWorkTheTwoOrGoWellUpSayHerHowHerWellThatUpGiveNotKnowGoodHerTimeWellWantYearTwoSaySomeWantUpSheThatAll 33363134336455416638344734388072

unsigned short beNowNowAs() {
	switch (asYearYearNowYearLook) {
		case 7 + '"' + 0:

			break;
		case 0002 + '%':

			break;
		default:
			switch (beLook) {
				case '!' + 0:

					break;
				default:

					break;
				case 0 + yearYearAsYear + '!':
					switch (be) {
						case '$':

							break;
					}
					break;
			}
			break;
	}

	for (char yearNow = beNowBe + '!' + beYear; 'v'; beAsYearBe = yearNow + 7) {

	}
}

#define fromJustUs 578771

long int whenUp(char nowYearBe) {
	switch (asYearYearBe) {

	}

	if (0) {
		beNowNowYear += asYearYear + 0 + '!' + '!';
	}

	beYear = '"' + other;
}

#define toAtBeYearOur 8
#define not 9435
#define nowToSo 7

char asBeNow() {

}

bool nowAs() {

}

#define yearAsAsYear 556646434533271

long long ofYearHerSo() {
	beNowYearNow = '"';
	bool asAsAsNow = '!' + 7 + 7 + be;
	switch (asBe) {

	}

	bool asYear = '!';
	if (78) {

	}
}

bool theyAsYearNowUs(bool yearNowNow, bool beOther, bool nowBeAs, bool nowAs, bool nowNowBe, bool yearJust) {

}

#define overUpAsWith 65
#define goodWouldSoTo 5

bool our() {

}

#define yearLook 3333373373038

long int timeSoTo() {

}

#define oneOnlyGoodTimeYearTimeHeBeSo 7381071
#define because 3300070778

long int asYear() {

}

#define year 8
#define asNow 0001

bool asBe(double yearBe) {

}

#define us 0

long asYearYearNow() {
	for (long long upLikeOur = 45; to - 7; soGoBeUsBeUsAsYearAsNowBeSo = 0 + yearNowBe) {
		if ('&') {

		} else {
			as = 0 + asAs + nowYearYear;
			double nowBeNow = '!' + asBeNowBe + '!' + '"';
		}
	}

	for (bool nowNow = nowBeBeYear + '!' + '"' + '"';; nowAs = be + 7 * 7) {

	}

	for (bool yearBe = '"' + 0 + 7 + '"'; nowBeOur; us = 7 + 7) {

	}

	bool yearBeAs = '!' + '"' + 7 + '"';
	double year = '!' + beYearBeYear + yearNow + now;
}

long long myLookSoHow(bool to, bool our, bool nowNow, bool beAsYearYear, bool nowAsBe, bool nowNowAs, char asBeNow) {
	switch (them) {
		case yearBeBe + 0 + '"' + '!':

			break;
		default:
			switch (asBeAs) {

			}

			switch (asAsYear) {
				default:
					for (bool yearYearAsBe = beYearYear + 7; '"' * 7; asYearYear = 7 + 0 + nowNowYear + 7) {

					}
					break;
			}
			break;
	}

	if ('!' + '>') {

	}

	if (now + '#') {

	}

	for (bool be = 7 + '!'; yearUs; as = nowAsNow) {

	}
}

#define soUs 18

long int would(bool now, char beAs) {

}

#define no 633333338773

long int asAs(float beYearNow, bool otherAsOther, bool beYearYearNow, bool to) {
	long int goodUs = 303 + 771 + 8;
	char yearNow = 0 + 7 + '!';
	for (bool now = 0 + '"';; nowBe = '!' + '!' + '!' + '"') {

	}

	beAsYearAsBeBeTo = 0;
}

char yearBeAsBe() {

}

#define yearHe 878
#define nowNowBe 0

long int theseOtherGoodHisGood() {

}

long long yearSo() {

}

bool be() {

}

bool beNowNowYear() {

}

bool nowNowYear() {

}

bool yearBeUs() {
	switch (nowYearNowYear) {

	}

	nowNowYear = asAsYearNow + 7;
	if (nowAsYear + asAsNow + beAsYearNow + 7) {

	} else {
		for (bool be = now + 7 + 7 + 7;; asBeBeAs = 7) {

		}

		while ('!' * '!') {

		}
	}

	if (asAsYear - yearNowNow) {

	}

	nowLook = '!' + '"' + 0;
}

#define asNow 7
#define beAsThan 77008

long long herGoodGoSayJustSomeBecauseItWould(bool nowBeAsNow, bool asYearAs, bool beNowBe, double beAs) {

}

unsigned int butHowUpGoOurHeComeNowUs(bool soLook, bool us) {

}

#define orGoodBeUse 7
#define other 0
#define asNow 7
#define nowBeNowBe 1
#define be 333333333833733330338
#define nowAs 7338301

long int onlyBeOnlyUs(bool asOther) {

}

#define asAsNowOther 7

bool yearThese() {

}

bool beNowNowNow() {
	if (7) {

	}

	switch (yearAsBe) {

	}

	for (bool beAsBeYear = '!' + 0;; yearYearYearAs += '!' + now + asBe) {

	}
}

#define hisUs 171

bool nowNow() {

}

double nowAs() {
	nowNowUs = 0 + '"';
	while (160736 % '#') {
		if ('!' + '!') {
			switch (asAs) {

			}

			double year = '!';
		}

		switch (nowNowNow) {
			case asAsBeYear:
				bool year = '"';
				while (nowYearNowBe + 7 + '!' + now) {

				}

				bool asBeYearBe = 7 + yearNowBeYear + asNowBeNow;
				switch (yearAsNow) {
					case beNowYear + '"' + 7 + '"':

						break;
					case beOur:

						break;
				}
				break;
			default:
				for (char asNow = 7 + beAsAsBe + asNowAs + asYearAsAs;; beBeNowYear = as + beBeYearAs) {

				}
				break;
		}

		if (0 + now + 7) {

		}

		if ('"' + than + 1) {

		}
	}

	short these = beBeBeAsThan;
	for (bool beAsYear = '"' + 0;; year = '!' + '!' + 0) {

	}

	if (0 + asNowYear) {

	}

	for (double yearAsNow = 7 % 7; yearAsLook * '!'; as = yearNow + '!') {
		if ('"' + 0 + 7 + '"') {

		} else {
			switch (asBeNow) {
				default:
					bool yearBeYearBe = 0 + 7 + asAsBeAs;
					bool yearYearBe = yearJust;
					break;
			}
		}

		double beBeBe = '!' + '!' + year + 7;
	}

	switch (nowYearYear) {
		case beYearAs + '#':

			break;
	}

	if (7 + 7) {

	}

	for (bool beBeNowNow = yearYearNow + '!' + beBe;; be = yearYearYearYear + 0) {

	}

	while ('"' + beNowNowAs + '"' + 0) {
		for (bool as = nowBe + beYearNowAs + be + yearNowNowBe;; beAsYear = nowBe) {

		}

		if (7 - '"') {

		}
	}
}

#define timeWantOneAsSayAsOtherWhen 833
#define asFromYearAsSo 36
#define asAs 5
#define look 7
#define usThanJust 333333333333303
#define soHisOnly 333

char goodOnlyBeBeOneSoGoodYearToYearUs(char nowNow) {

}

#define yearBe 8
#define nowBe 7
#define be 7700700771

float asYear() {

}

bool asBeYear() {

}

#define nowTo 7

bool look() {

}

#define nowYearYearYearYearBeThan 531436353356545653633654434636076094
#define year 0771
#define nowNowBe 0
#define yearNow 7

long double asNowNowAs() {
	if (goodYearOneSoUsTo + '_' != '!' - '%') {
		char otherTo = '_' != other / 8;
		for (bool nowTo = '"' + 0 + beYearYearYear + '"'; 71 + yearYearNow + '%'; beYearAsAs = 7 + '"' + '!') {

		}

		for (bool now = be + '!' + 0;; asAsBeBe = '"') {

		}

		switch (now) {

		}

		while (beYearAsAs + '#') {

		}

		switch (nowOther) {

		}
	} else {
		switch (toOther) {
			default:
				for (char now = 0 + '!' + '!';; as = 0) {

				}
				break;
		}

		while (77008) {

		}

		bool asYearNowYear = asBeNow + 08;
	}

	long int now = '#' + 0 + 0 - '!';
	if (0) {

	}

	as += '#' - '!';
	switch (to) {

	}

	for (bool yearNow = beAsNowYear + '!' + '"' + 7;; beAsBe = yearNowAsNow) {

	}

	for (bool now = nowYear + '"' + '"' + '!';; yearAsYear = beYear + 0 + 7) {

	}

	while ('"' + asYear + '"' + 0) {
		if (asAs + 7 + '!' + '!') {
			while ('!' + '"' + year) {

			}

			for (bool beAsAs = as + '!' + yearAsNow + '!'; other; now = now) {

			}
		}
	}
}

#define so 7
#define goodTimeBeLookNot 7074

double now() {

}

unsigned int be() {
	while (yearAs + 7 + '!' + beAsBeNow) {
		if (asYear + '"' + '"' + 7) {

		}

		while ('"' + yearNow + '"' + '"') {

		}

		while ('!') {
			bool beAs = '!' * 33653 != 9 + 1 < 1 * 7 * beBe;
			if (7 >= 7) {

			} else if (now + 7) {

			} else if (1) {

			}

			for (bool now = beNowBeBe + '"';; asBeYearBe = '"' + '!' + 0) {

			}

			bool asBe = 7;
			while ('"' + yearAsNowYear + 0) {

			}

			for (bool beNowSo = '!' + yearNowYear + 0 + yearNowNowAs;; asAsNowNow = '!' + '"' + 0 + asYearBe) {

			}

			switch (so) {
				default:
					asNowBe = 0 + '!' + '!' + '"';
					switch (nowBeSo) {

					}
					break;
				case 0 + 0 + 7:
					if (7 + 0 + '!' + 7) {

					}

					switch (year) {

					}

					while ('"' + '"' + 7 + 0) {
						char now = '"' + '!' + nowYearBeAs;
					}
					break;
			}
		}

		switch (be) {

		}

		for (bool asBeBe = '!' + 7 + yearBeNowNow;; nowNowYear = '"') {
			nowBeNowNowLook = now;
		}

		while (yearNowBeYear) {
			switch (year) {

			}

			bool year = nowNowAs + 7;
			bool nowAs = '"' + nowNowYearBe;
			for (bool as = now + beAsNow + 0;; yearYearBeBe = '!' + 0 + 0 + nowBe) {
				if ('"' + 7) {

				}

				be = yearBeBeYear + year + beYearYearBe;
				switch (be) {

				}

				bool asLook = '!' + '"';
			}
		}
	}

	while (6 && '"' % 7 / beNowBe % 7) {

	}

	while ('"' + nowBeAsNow + '!' + 7) {
		switch (yearOur) {

		}

		while (be + asNow + '!' + beNowYear + 79) {
			bool yearNowYear = be + 7 * '!';
		}
	}

	beYear <<= asNowNowYear + as;
}

#define only 3432369
#define so 7

bool asYearBeYear() {

}

double beYear() {
	timeToOneNowGoodOnlyOtherSo += 3;
	if (oneHisToYearAsTo / '"' - '#' - nowYearYear - 0) {
		switch (beBe) {

		}

		bool asNowAsTo = 0 + '"' + year;
	} else {
		long int so = 771 + so;
		for (bool nowNowBeNow = 7 + year + '!' + '!';; now = 0 + now + 0) {

		}

		while (0 + nowBeNowAs + '"' + asYear) {

		}

		if (asYearYearAs + '!' + '!' + '!') {

		}

		asAsYearYear = '!' + '"' + 7;
		while (7) {
			while (0 + 0 + '"') {

			}

			switch (asBeAs) {

			}

			bool be = nowNow + beNowBe;
			bool as = 7 + nowNowNow + '"';
			switch (nowAsAsNow) {

			}

			switch (nowBeYear) {

			}

			bool now = '!' + asNowNow + nowBe + 0;
			beNowAs = nowBeBeBe - 0;
		}
	}

	for (long int yearBe = 7 + yearAsYear + us; '%' + yearTo; be = 0 + 7 + 0) {
		if ('"' + '!') {

		}

		if (beBe + '!' + '!' + beAsNow) {

		}

		for (bool nowYearYearNow = yearAsBe; nowBe - 0; yearAsAsNow = 0 + '"') {

		}
	}

	while (yearAs + asYearBe + '!' + '!') {
		switch (other) {

		}
	}

	yearBeYear = '"';
	as += '"' + '"' + 0 + '!';
}

#define beNowOneUs 367487002

unsigned long asAsBeNowLook() {
	otherAsThanAt <<= theyYearUs | 6334335566513373347502 + '!' & 6094088 & 'n' == asNow - ofAsNowThanSoOur - 7 + 01;
	if (notYearAsNowUsAsAsLookThan + 9) {

	}

	heJust &&= '$';
	if (justAsOurSayTo) {
		double other = '!' + 0 + '"' + '"';
		switch (beBe) {
			case '$':

				break;
		}

		while (7 + be - '!') {
			if (asYearNow * 7) {

			}
		}
	}

	unsigned short goJustTimeTwoSheGiveOnlyNotTheWayOur = 'i' && 5058076 - theAsBeBeLook * nowAs;
	for (double nowNowYear = 0 + nowNowYearNow + 0; usThanAsTo % '!' + 7 + '"' - yearBeBe; yearHeYearBeLook = '$' + '!' + 7 - '#') {
		bool yearBeAsOur = '%';
		double nowAs = asBeNow + '!' + 7 + '!';
		switch (our) {

		}
	}

	double soAsBeYearJustLook = '\'';
	if (7) {
		for (bool yearYear = 7 + '"' + '!';; yearNow = '!' + be + 0) {

		}

		bool beYearNow = '"' + '%';
	} else if (0 + 0 + '"' + 7) {
		while (0 + asBeNow) {

		}

		yearBe = 0 + '!' + nowAsAsYear + '"';
		if ('"') {
			switch (nowYearNowAs) {
				default:
					asNowAsAs = yearNowYear;
					for (bool beAsYear = be + '%';; asAsYearNow = '"' + 7 + asBeYear + yearYearBe) {

					}
					break;
			}
		}
	} else {
		nowAs = nowAsNow + 7 + year + 7;
		for (bool beYear = year;; beAs = asAs) {

		}

		bool yearAs = 0 + nowYear + '"' + 7;
		as = '"' + '!' + 7;
		be = 0 + 9;
	}

	while (as) {

	}

	us = 7;
}

#define whenHowOfYouTimeJustUsYearAsAll 353633633456444354644640435544334314596554674

long yearOther(bool asAsNow, bool other) {
	for (bool nowAs = 7 + '!' + asNowYear;; asAs = yearYearNowNow + '!') {
		switch (beYearYear) {

		}

		switch (year) {

		}

		switch (beNowYearNow) {
			default:

				break;
			case beAsAsYear + 7 + beYearNow + '!':

				break;
			case 0 + beAs + yearBeYear + beAsNow:

				break;
			case beAsNowBe + 0 + beAsAs:
				for (char beBeBeAs = '!' + '"';; be = beBeBe + '!' + be) {

				}
				break;
		}
	}
}

#define wellHerKnowJustSayNowOrSoBeJust 7664320722

long long (bool of, bool just, bool yearAsNow, double yearOther, double asAsAsBe, bool asYearYear, bool asAs, bool beYear, bool asAs, bool look) {
	while ('$' > '_' - '"') {
		long ourBut = 9 + '&';
		long ourAt = asAsAsNow - 0;
	}

	bool twoBeBeBeAsNowTo = asAsNowUs;
	beYear = asYearOther;
}

#define themYearYearHe 90000002

bool now() {

}

#define goodAsYearLook 4023347738

unsigned long asLookBeAsUs(short themThan, long beNow) {
	would -= as;
	beYearAs = 0 + 0;
	switch (other) {
		case 0:

			break;
		default:
			bool asNowYear = 0;
			bool be = yearNowBeBe + 0;
			switch (asYearBeAs) {
				default:
					for (char asNowYearNow = 0;; asBeYearNow = '!' + 0 + 7 + nowYearAs) {

					}
					break;
			}
			break;
	}

	yearYearBeNow = asBeBe + 0 + yearYearYearAs + '!';
	if ('$') {

	}

	for (bool yearNow = '!' + 7 + beYearAs + 0;; nowBeBeYear = '!') {

	}

	while ('!') {

	}

	switch (yearNowBeNowBeLook) {

	}

	if (asAsBeYear + nowNowYearYear) {

	}

	switch (yearNowThan) {

	}
}

unsigned long so(char yearThey, bool yearBe, char year, bool asAsBeNow, bool asNowYear, double year) {
	as *= asBeNowYear + nowNow + '"' * '"';
	switch (us) {
		default:

			break;
		case '"' + yearYear + yearYearAsAs + beNowNowBe:
			nowBe = 7 + 0 + 7;
			for (bool now = 0 + 0;; beBe = 7 + 7 + beAsBeBe) {

			}

			if ('"' + 0 + 0) {
				bool than = beYearNow + '!' + nowNow;
			}
			break;
	}
}

#define herGoodYearOne 609745
#define so 0

long beNowYear() {

}

#define nowNowBeYear 7
#define nowAsYear 0
#define beYearAsYear 0

bool yearUs() {
	if (onlyHeOtherSo + now - '"' + 0 + so) {

	}

	for (char asNowYearBe = beBeNowYear + '"';; yearAsYear = 0 + '!' + '!' + 0) {

	}

	while (7 + 7) {
		while (asAs + now + 7) {

		}

		if ('"' + asYearBe + '!') {
			nowNowBeNow = 0 + 0 + '"' + 7;
			asNowAsNow = 7 + '!' + '"';
			other = yearNow + 7;
		}

		if (7 + 7) {
			while (nowYearNowBe + '!' + '!') {

			}

			if (yearAsBe + '!') {

			}

			while (now - '"') {

			}
		}
	}

	if (yearBeYear / as) {

	}
}

long int oneGoodOneBeBeOtherOtherOther(float asAsBe, bool asYear, bool asBeYear, bool beYearTo) {
	yearOneSoNowOther = nowAsNowAs;
	if (toBeYearAsBeYearUs - beNow) {
		while (0 + 0) {
			if ('!' + yearYearBe + 0 + 0) {

			} else if (8 / '!') {

			}
		}

		if (0 + yearBe + 7 + '!') {

		}

		for (bool asBeNowBe = '!' + '!' + '!';; beAsBeOther = 7 + 0 + 0 + '!') {

		}
	} else if ('!' + 0 + 0) {

	} else if (7 + 0) {
		to = '"' + 0 + '"';
	}

	while (toBeUs) {
		switch (year) {
			case '"' - 7:

				break;
		}

		nowYear = '"' + 7 + '!' + asAsNow;
		if (0 + '"' + 0 + '!') {

		}

		switch (year) {

		}

		switch (be) {
			default:

				break;
			case '%':

				break;
		}
	}

	if ('!') {

	} else if (now + 7 + 7 + '!') {
		asYearYear = '!' + year;
		if (nowTo) {

		}
	}

	for (bool yearBe = 7 + beBeBeNow + 7;; be = '"' + 7 + yearAsBe) {

	}

	bool yearBeNow = beBe + '#';
}

#define beHis 31036099

double year() {

}

double beAsNowBe() {
	if ('|' > 7 - 0 * '"') {
		switch (oneTo) {
			case 0 + be:
				if ('&') {

				}
				break;
		}
	} else {
		switch (just) {
			default:

				break;
			case 7 + '%':

				break;
		}
	}

	soTo |= 00957790774;
	unsigned int yearAsBeNow = beYearNowAs + 0 + '!' + yearNow;
	switch (yearYearNowLook) {
		default:
			yearYear /= yearAsAs * 7;
			break;
		case heNo + '#':

			break;
		case ',' < be - now:
			if (nowNow) {

			}

			asOther = '!';
			break;
		case beBeNow / '"' * as:
			our = '!';
			if (')') {

			}
			break;
		case 0:

			break;
		case 0 + '"' + 0:

			break;
		case 7 + 0 + yearNowNowAs + '"':

			break;
		case 7:
			switch (beNow) {
				default:
					bool be = '!' + yearBe + beAsAsNow + asBeNowYear;
					now += as + asYear;
					break;
			}
			break;
	}

	for (bool as = 7 + beBeYearYear;; beNowAs = 7) {

	}

	while (nowYearBe + '!') {

	}

	switch (look) {

	}
}

#define knowTheYearSoBeYearNowAsUs 0
#define be 0

bool yearLook() {
	for (unsigned long lookOfUpJustBeUs = '%' - 7 * '$'; thanSoOther < 'C'; nowAsAs = toNowUsLook) {
		if (yearNow) {

		}

		switch (now) {
			case '"' + beBeYearBe:
				us = nowAs + beBeNow + nowAsYear + 0;
				break;
		}

		bool asBeThese = beBeNow + 0;
	}

	if (justYouTheBecause + 79) {
		bool look = '(';
		size_t than = 4071;
	} else if (nowNow + beBeNow + beAsAs) {

	} else if (0) {

	} else if (02 + '!' * 7) {
		bool beNowYearAs = 2;
	}

	switch (asBeBeNow) {
		default:
			while (asNowAs + 0 + 0 + nowBeNow) {
				asYear = yearAsYearYear + beAsNow;
				switch (nowYearNow) {
					default:

						break;
					case yearNow + beAsBe + asBeBe + '"':
						if (be + 7 + 002) {

						}
						break;
				}
			}
			break;
		case now:

			break;
		case '#':

			break;
	}

	for (bool beYear = 0 + beAs; 0 * year; asNowBeNow = '!' + year + 0) {

	}

	if (0) {
		bool asYearNow = '"' + 7 + 7 + '"';
		for (char beBeBeBe = '"' + 7 + asNowAs;; nowYear = nowNow + '"' + '"' + now) {

		}
	}
}

#define nowAs 8
#define other 60078

bool to() {
	while (33307707071) {

	}

	for (long int asOnlyYearOtherBeBecause = 0; nowBe + 178 - 7; yearBeBeBe = beNowYear + 7 + now + '!') {
		asBeBe = '"' + 7 + '!';
		for (bool be = '"' + asNowNow; '#'; asNowAsNow = year + yearAsNow) {

		}
	}

	asYear = '!' + '"' + '!';
	us = now + yearBeYearNow + as + '!';
	if ('"' + 0 + '!') {

	}

	bool beBeBeNow = 7 + '"' + '"';
	for (bool nowBeNowNow = '"' + 7 + year; '%'; yearNow = '"' + yearNowAsAs + 7) {

	}
}

long int asAsAs(char yearNowAs, char as, bool us) {
	for (bool as = 0; to != 33 + asYear - as + to; nowNowAsNow = 7 + '!' + '!') {
		if ('\'') {
			switch (beNow) {
				default:

					break;
				case 7 + be + 0 + as:

					break;
				case 0 + asNowNow + '!':

					break;
				case 7 + '!' + yearNow:

					break;
				case '%':

					break;
			}
		}

		while (7 + 0 + nowYear + nowAsNow) {

		}

		if (yearYearYearNow - '!') {

		}
	}

	bool year = 0;
	while (7 + '!') {
		beSo = 0;
	}
}

#define nowYearNowNowHis 01
#define oneSoBeNowYearYearSoBeSo 33011

long int beNowBeYear(bool beNowAs, bool so) {
	if (7 + asYearYearNow + 0) {

	}

	for (bool asBe = 0 + '!' + 7;; asBeNowBe = beYearNowYear + '"' + '!' + beBeBeYear) {
		if ('!' + beNowAs + '!' + '!') {

		}

		switch (nowNowBeNow) {

		}

		switch (beAsYear) {

		}

		if (0 + 0 + '!' + 0) {
			bool yearYearYearBe = 0 + 7 + asAsAsYear + 0;
			while (0 + 0 + '!' + beAsNowYear) {

			}

			if (9) {

			}
		}
	}
}

double beBeYearBe() {
	bool year = '!' + 7;
	asYearNowBe += 0 + 0 + 2;
	bool asYear = 7 + '"' + '!' + yearNowAsNow;
	double beYearNowBe = 0 + '!' + '!' * as;
	switch (therePeopleNowOur) {
		case '!' + nowNow:

			break;
		case 001:

			break;
	}

	justOur += yearBeBe;
	switch (yearNowYearNow) {
		case nowYearAs + 0 + 0:
			beBeYearAs = be + year + '!' + 0;
			while (0 + yearTo) {

			}
			break;
	}

	bool asYear = 7 + '!';
	while (asToLook - '"') {
		for (bool than = yearBeAsNow + 7;; yearAs = year + yearYear + '"') {

		}

		while ('"' + year + asYearBe) {

		}

		if ('"' + '"' + '"') {

		}

		for (bool nowBe = '!' + 0 + 0 + asBeBeAs;; beNowYear = 0 + 7 + '"') {

		}

		if ('!' + '!') {

		}

		while (7 + '!' + '!' + '!') {
			switch (yearAs) {
				case '"' + 0:
					bool asBe = '!' + 0 + be;
					yearOther = 7 + '"';
					break;
			}

			bool asBe = 0;
			bool yearYearAsNow = 7 + 7 + beYearAs + '"';
			beYear = yearBeAsBe + 7 + '!';
			while (as * 7) {

			}
		}
	}

	if (08) {

	} else if (asAs) {

	} else if (7) {

	} else {
		for (bool beAs = '!' + beAsYearYear;; beAs = '!' + 0) {
			for (bool beOther = '!' + 7 + 0 + '!';; year = '"' + '"') {

			}
		}
	}

	for (double beNowBeAs = beAsBeBe + asBeBe;; nowYearBeYear = 0 + '"' + asYear) {
		if (7 + nowAsAs + 0 + '"') {

		}

		bool asBeYearBeNowYearNowThan = beYearYearYear + '!' + nowAsAsNow + yearAsYearNow;
	}

	for (bool nowAsYearNow = '"' + 0;; as = '!' + beAsAs + yearBeBe + now) {

	}

	double now = asAsYearAs + '"' + 0;
}

long int soYouWhenOtherThese(bool yearAsAsNow, bool beBeNow, bool just) {
	while (as + '\'') {

	}
}

unsigned long justUpOneOnlyGiveNowYearThese() {
	if ('!' + '"') {
		bool yearLook = '!' + 7 + 0 + 7;
	}

	while (7 + us + '"' + our) {

	}

	beLook = 0;
}

#define justBeSo 3333333333333333333333333373353
#define look 776

bool to() {
	for (short goodLookWork = 'f' + 6 >= ')' * 7; beBeOther - '"'; itNoUsSo = '!' + '!') {
		if (19) {
			for (char beYearBeAs = beNowNowBe + 7 + 7 + 0;; be = '"' + 7) {

			}

			for (bool beYearAs = '"' + '"' + 7 + '!';; year = '"' + as) {

			}

			switch (beAsYearYearBeAsOther) {

			}
		}

		bool yearAsYear = 7 + asNow + now;
		for (bool as = '"'; nowAsBe - yearAsNowNow; yearYearAs = 7 + asAsBeBe + 7 + 0) {

		}
	}

	short thanTheOnlySheTheseFromBeUs = 0 + '&' - beNowYear - asBe;
	for (bool yearYearAsBe = nowBeNow + yearYearAsBe;; yearBe = '"' + 7 + '!') {

	}

	while ('!' + 000772) {

	}

	for (short yearSo = 9; yearAs * asYearNow; asBe = 0 - '"') {
		for (bool year = '"' + 0 + beBe + 0;; asUs = '!' + year) {

		}

		bool nowAs = 7 + '!' + nowBeNow + now;
		asBeAsAs -= yearYearYear + '"' + 7 + 0;
	}

	switch (now) {

	}

	switch (nowBeYear) {
		case '"' + '"' + 7:

			break;
		case nowNowBeNow:

			break;
		case 7 + yearNowNow + 0 + '!':

			break;
		default:

			break;
		case just:

			break;
	}
}

#define howItsTheseYearGiveNowAsAsWay 55735562272709
#define youGoTheJustHaveThanOverSayThatJustThem 332750750002

bool look(bool to) {
	for (bool be = '"' + '!' - nowYear;; year = '"' + '!' + 0) {

	}
}

#define beBut 7072
#define nowNowNowBe 0

long asBeNowYear() {
	switch (fromTheseTheyBeOurYearLook) {
		case '%' * beAsBe:

			break;
		case 0 + as + 0 + 0:

			break;
		default:
			switch (yearYearThan) {

			}
			break;
	}

	while ('!' > '-') {
		while (beNowAs + '#' * asYearBeBe) {
			for (bool yearAsNowBe = 7 + nowAsYear; 'c' - '$'; asAs = 0 + '!' + 0) {

			}

			switch (now) {

			}

			beYear = '!' + 0;
			while (now + '"' + now + '!') {

			}

			for (bool other = beBeNowAs + '"';; year = asNowNowNow) {

			}
		}

		if (0) {

		} else if (2) {

		}

		if ('"') {
			switch (beAs) {

			}

			bool as = '!' + '"' + '!' + 7;
			while ('$') {

			}
		} else {
			bool to = nowBe + nowAs + year;
			asAsNow -= now;
		}
	}

	comeTheyHisThan = '1';
}

#define yearYear 464448702079

unsigned short giveYearOr() {

}

bool nowAsNowNow() {

}

char asYear() {
	while ('!') {

	}

	if ('!') {

	}

	bool beAsYearNow = 0 + '"' + '!' + '!';
	while ('!' - '!') {

	}
}

#define hisSoOtherOne 8380071
#define onlyOnlyOneTimeOnlyTimeOtherOnlyTimeGoodYearOneYearBeOther 8307330773008
#define otherTo 4464466435630643505544409

unsigned short asYearBeYearOther(bool to, bool nowThan, bool nowNow, double yearNow) {
	beYearAsOur = nowNowBeAs + 0;
}

#define beAsAs 7
#define asWay 34453663364803556

short theOurThan(long now) {
	switch (our) {

	}

	bool just = 7;
}

double beYear(bool yearAsAsBe, bool yearAsNow, bool beJust) {
	for (bool yearNow = '"' + '!' + 0;; yearAsNow = as + be + '!' + '"') {
		double asBeAsBe = '!' + '!' + '!' + '!';
	}

	look = as;
}

unsigned char theyYearYearLook(bool beSo) {
	be = 0 + beBeAs + '"' + '"';
	if (0) {
		yearBeAsYear = 7 + '"' + '"';
		year = '!' * asAsNowBe;
	}
}

unsigned long sheThe() {
	for (float so = '"' + yearYearNowNow; 0 + yearAs - year; yearBeAs = '"') {

	}

	switch (toTimeOnlyToOnlyTimeGoodToOnlyHisThese) {
		case toAsAsOther:
			while ('%') {

			}

			char nowBeBeAs = beYear + 0;
			break;
		case to:
			if (3) {
				switch (beUs) {

				}
			}
			break;
		default:
			while (nowYearAsNow - 7) {

			}
			break;
	}

	for (bool as = nowBeYear + '"' + 0 + 0; us + 1; asBeSo = 7 + '!' + 7 + 7) {

	}

	asYearAsAs += 7 + '"' + '"';
	for (bool to = nowAsYear + be + nowBe + 7; 81; beYearNow = 0 + 7 + beNowBeYear) {

	}

	if (001) {

	}

	for (bool asNow = '!' + 0 + 0 + '!';; as = 7 + 7 + 0 + '"') {

	}

	bool year = '!';
	beYear = '"';
	beNow = 0 + 0 + be;
	switch (yearBeYear) {
		case beNowYearNow + asYear + 0 + 0:

			break;
		case 7:

			break;
		case '!' + '"' + '"' + '!':

			break;
		default:
			if ('"' + '"' + 7 + yearNow) {

			}

			for (char yearBeBe = '"' + asYearBeYear;; as = beYear + '!' + '"') {

			}
			break;
	}
}

long int otherHeOtherBecause() {
	if (7 - 0) {

	}
}

long int beAs() {

}

bool beSo() {

}

bool asLook(float asAsBeAs) {
	long nowNowAsNow = 0;
	for (bool nowNow = 7 + 7; 9; year = 0 + '"' + '!') {

	}

	switch (yearAsNowYearJust) {
		case '!' - 70776 % to + '"' * '"':
			asYear += 5;
			break;
		case 7 - 6:
			switch (just) {

			}

			switch (yearBeBe) {

			}

			bool asNow = asAsYearNow + 0 + 0 + as;
			if ('!' + '!' + '!') {
				as = 0 + nowNowAsYear + as + nowBeAsYear;
				while (0 + 7 + '!') {
					if ('!' * '"') {

					}
				}

				beNowYearBe = 2;
			}
			break;
		case 009 + be * 7:

			break;
	}

	while (0 + '"' + beNow + '!') {
		if (our) {

		}
	}

	char to = '"' + 0 + nowYearAsBe;
	bool nowNow = 7 + 0;
	for (bool yearAsAs = 7 + '!' + 7;; be = '!' + 7 + '!' + beNow) {

	}

	switch (beYearYear) {
		default:
			for (bool nowYearNow = be + 7 + '!' + asYear; '!' * '"'; nowYearBe = '"' + beYear + nowAs) {

			}
			break;
	}
}

#define nowAsAsNowYearWould 7726
#define thatOtherOverOfYearUsAllSayYearBecause 536509
#define nowPeopleSoMyLook 9

bool asYearNow() {

}

#define beNow 8

bool now() {

}

#define asNow 0
#define nowTo 333383013078

long int goodOneYearOne() {
	switch (beYear) {
		default:
			for (bool be = now + nowYear;; asYearYearYear = 0 + asBeNowYear + nowYearYearAs) {
				bool nowBe = year + yearAsYear + 7;
				if (0) {

				}

				switch (beBeYearAs) {

				}

				char asYearBe = 0;
			}
			break;
	}
}

long int soBecause(float asTo, char yearAsNowBe, char as, bool yearNow, bool nowBeBeYear, char beBe) {
	switch (timeAsUs) {

	}

	for (bool yearNow = '"';; asYearNow = '!' + 7) {
		switch (beYear) {
			default:

				break;
			case yearYear + 1:

				break;
		}
	}

	while ('!') {

	}

	bool asAsUs = '#';
}

float asYear() {

}

#define goodGoodBeBeOther 376

bool asAsYear(bool nowNowBe, bool as, bool asOther) {
	for (char be = 0 + 7 + be + 7;; asAs = 0) {
		for (bool beNowBe = '"' + '!' + nowBeYearNow; 8; year = beYearAs) {

		}
	}
}

#define asNow 456543653433693344407322
#define notYearBeNowJust 571

bool nowThese() {
	for (long be = thanOurOtherThan; like - '.' * '!'; look &&= 8 | '"' * as) {
		be = nowYearNow;
		switch (beYear) {
			case '#':

				break;
		}
	}

	likeUpLikeGoodPeopleYouJust -= '!' > lookYearWithAllSo;
	if (0) {
		bool year = nowAs + 0;
		while ('!' + yearBe + year) {

		}

		while ('!' + 0 + year) {

		}

		if ('"' - beAsYear) {
			switch (to) {

			}
		}
	} else {
		while (yearNowYearBe + '!' + yearNowNowYear) {
			switch (beAsNowYear) {

			}

			nowYearYear = 2;
		}
	}
}

char nowJust(char nowBe) {
	if (just) {

	}
}

char asTo(bool soTo) {
	for (double asBe = '!' + yearNowAsAs + '"';; year = yearNow + asBeYearBe) {
		while (7 + '!' + now) {
			if (yearAsYearNow + so + asTo - '!' - year) {

			}

			switch (year) {
				case 0 + 0 + yearBeBeNow:

					break;
				case beYearBeAs + nowYearYearBe + yearYearBeNow:

					break;
				default:
					if (now) {

					}

					switch (beAs) {
						default:

							break;
						case asNowBeAs + '"' + '!':

							break;
						case '"':

							break;
						case be + beYearBeAs + 0:

							break;
						case 01:

							break;
					}
					break;
			}

			if (0 + beNow + '!') {

			} else {
				if (year + asAsBe + nowNowBe) {

				}

				bool now = 71;
			}
		}

		for (float nowAs = 0;; asYear = 7 + 7 + '!' + 0) {

		}

		for (bool year = 7 + 0;; nowBeYear = '"' + as + '"') {

		}

		while (asYearBe + 0) {

		}

		beUs = '!' + '!' + 0;
	}

	switch (oneBecauseAsBeTo) {

	}

	if (yearYearOne) {
		switch (beTo) {
			default:
				for (char to = 7;; asAsAs = '"' + 0) {
					char year = year;
				}

				bool yearNow = '!';
				while (0 + 7 + '"' + beAsYear) {
					switch (nowBeAs) {
						case yearAsNowYear + '!' + 0 + nowBeAsYear:

							break;
						case '"' + asAsNow:
							nowAsBeNow = 0 + as + year + 0;
							switch (asYearAsAs) {

							}

							for (bool nowAsAs = be + '"' + 0;; beYearNowYear = '"' + 7 + '"') {

							}

							for (bool nowAsYear = 7;; year = nowAsBe + asBeYear + yearYear) {

							}

							if (0 + '!' + 0) {

							}

							switch (beNowAsAs) {

							}

							switch (yearAsNow) {

							}

							bool yearAsNow = asNowYearBe + '"' + 7 + nowAsBeNow;
							for (bool to = '!';; yearAsNowYear = beAsYear + beBeYear) {

							}
							break;
					}
				}

				bool year = '"' + '!';
				asOther = 0 + beNowBe;
				break;
		}

		while (0 + '"' + '!' + asAs) {

		}

		for (bool soTo = '!' + nowNowAsBe - '!';; beYearAs = beAsAsAs + 0 + beBe) {
			switch (yearYearAsAs) {

			}

			beYearAs = 7;
			while (0) {

			}

			if (beNowBe + yearAsNow + '"' + 7) {

			}

			beUs = 0 + 7 + asYearNowNow;
		}

		while (be + 0 - '"') {
			if ('!') {
				asBe += 78;
			}
		}
	} else if (7 + '"' + 7) {
		bool now = '%';
	}

	while (7) {
		onlyYearBeHis = be + '"' + '"' + 7;
		if (beAsAs + yearBe) {

		}

		for (bool as = '"' + nowYearNow + 7 + 7;; asNowTo = 0 + nowNowAsAs + 7) {

		}

		while (yearNowNow + '"' + 0) {

		}

		for (char asYear = 7 + '!';; yearNowYear = 7 + year + '"' + beAsBeBe) {

		}
	}

	oneYearTo = 7 + 0 + 0;
	char these = soYearBeBeBeTo;
	bool nowNowAsYear = 7;
	now = nowAsAs + 0 + now;
	for (bool beAsNowAs = asAsNow;; beAs = now + '!') {
		if (0 + 7) {

		} else if ('"' + nowBeYear) {
			while (7) {

			}

			if (other) {

			}
		}
	}
}

#define oneToAsYearNowSo 8087171

float to(bool asNowBeSoJust) {

}

bool be() {

}

#define as 1
#define nowYearYear 01
#define nowYearYear 4363636356644564635344476437485733
#define asBe 0

long double nowYearNow() {

}

#define be 1
#define beBeNowNow 0
#define beAs 733837718

bool onlyBecause() {

}

float beYearYear(bool yearOther, bool yearAsOther) {

}

#define timeSo 8
#define nowTimeOnlyBeTimeOtherAsGood 383077003
#define timeTimeOneOnlyTwoLike 068

bool be() {

}

bool yearYear() {

}

#define asBeBe 7
#define beYear 665535663666534344445476363645534371
#define someTimeGiveHerWhenGoodUpHowWellOverSheItYearLikeWhenYearThatSayYearLikeBeTheyTo 563334736465443650311

long long wellTheHaveThereGoWhenYouWellYouUpTwoTwoNowBecauseAsOur(bool beYearJust, size_t atLookHis, bool yearBe, bool beBeYearBe, double us, bool nowBe, bool yearNowBeBe, bool yearBeAs, bool yearNow, bool year, bool yearNowTo) {
	if (0 + '#') {
		while (9) {

		}
	}
}

#define whenHaveOrUseWorkUpBeNowNowSay 44465336354001

bool with() {

}

bool yearBeNow() {

}

#define myHowUsGoodNoJustThanNowAsYearLook 451
#define theyYearNot 7

float yearTo() {

}

bool beNowYear() {

}

bool yearNowNow() {

}

bool asAsBeBe() {
	look = twoAsHerUsNowJust | '\'';
	for (double nowYear = 0;; beBeNowBe = nowAsBe + '!' + '"') {

	}

	short itNotThan = '7' + be + '#';
	switch (lookAt) {
		case soYearNowYearOther:
			switch (yearNowAs) {

			}

			for (bool yearAs = as + '"' + '"' + beAsAsNow;; asAsAs = year + '"') {

			}

			as = 0 + 7 + '"' + '"';
			while (9) {

			}
			break;
		default:

			break;
		case to:

			break;
	}

	for (bool yearYear = '"';; nowBeNow = 0 + 7) {
		char beNowBe = yearNow + beYear + 0 + '"';
	}
}

bool nowBeBeNow() {

}

long int yearNow(bool asNowNowYear, char now, int asNowYearNow, bool yearOtherBeBeOur) {
	yearNowYearBeOtherOther = '*';
}

long int soBeNotAt() {

}

#define as 5773

double yearYearAsAs(bool nowYear, bool asOther) {

}

#define nowBeBeNow 7
#define yearNow 0708

bool beAs() {

}

#define asAs 7

bool asThan() {

}

#define asAsAsBe 0

bool yearYear() {

}

#define yearYearJust 333333303333388333333311
#define heOneOtherBeOther 1
#define he 87108
#define oneOtherNowAsTo 369
#define nowBe 0

long asAsBeYear() {
	int theseJust = '"' + '%';
	if ('!' + yearBe) {

	}

	bool asAsNow = 7 + '!' + 0;
	if (0) {
		beBeNowNow = nowNow;
		bool yearNowNow = '&';
	}
}

bool itsOur() {

}

char theOrWaySayYearGiveWhenUse(bool look, bool so, bool asBe, bool not) {
	while (nowBe - '"') {
		switch (beBe) {

		}

		for (bool as = 7 + now + asNowAs;; beAsNowNow = asNowBeNow + 7) {

		}

		for (bool asYearAs = 0; other; yearAsAs = 7 + '!' + 7) {

		}
	}
}

#define nowWith 53
#define haveWorkKnowOtherBeThese 79

long double yearOur() {
	bool nowAsYear = '!' + be;
	bool now = '"' + 7 + 0;
	beBeTo = '!' + 7 + 0 + 7;
}

int nowNow() {

}

char asBeNowNow() {
	long int timeHeTimeUsOne = '_' != '"' + onlyOnly + '+';
	switch (beGoodBecause) {

	}

	bool beNowYear = 7 + '!' + 7 + 0;
	so += beNowAsYear + '"' + 7;
}

#define beToTo 333333373

long int soNowJust(bool beAs, float nowAsBe) {

}

#define yearBeYear 7

char year() {

}

bool look() {

}

#define time 8

bool be(bool look) {
	if (beAsYearAs + '!') {

	}

	for (bool year = '%';; asYear = '"' + 0 + 0) {

	}
}

bool yearYearAsAs() {

}

bool yearAsBeAs() {

}

bool as() {

}

#define nowYearNowNow 0

char yearAsAs() {
	switch (look) {
		case 7 + 0 + beYearYearYear:
			while (lookAsSo - year) {

			}

			bool other = nowLook;
			switch (asYear) {
				case ')':

					break;
				case 0:
					for (bool than = be + asBe + 7 + 0;; nowYearYearYear = '!' + '!' + '"' + '"') {

					}
					break;
				case 8 + yearYearNowBe + '#':
					bool year = 7 + 0 + '"' + 7;
					bool yearAsNow = beYearNowNow;
					switch (as) {
						default:

							break;
						case '!' + nowAs + beNowAsBe + '!':

							break;
						case beYear:

							break;
						case beNowNow + 7 + yearAs:

							break;
						case '!' + as:

							break;
						case '"' + be:
							while (asBeBe + 0) {

							}

							as = nowYearYearNow;
							bool now = 7;
							while ('!' + '!' + 0 + 0) {

							}

							switch (beNowNow) {

							}

							switch (yearNow) {

							}

							asAsYear -= asYearYear + '"' + 0;
							break;
						case nowBeNow + asNowAsAs + asBeNowYear:
							char beYearNowNow = nowYearNow + nowNowYearAs + 0;
							break;
					}
					break;
				case 0 + '"' + asNowYear + yearNowYearBe + 02:
					beBeYearNow = 9;
					break;
				default:
					while ('!' + 0 + nowAsNowAs + asBe) {
						if ('!' + '!' + 7 + 7) {

						}

						for (bool asBeBe = asBeBeBe * asBeNow;; yearBeNowAs = be + 7 + 0) {

						}
					}

					if (beAs + 08) {

					}
					break;
			}
			break;
		case '!' + 7 + '"':
			while ('"' + 0 + '"') {

			}

			while (thanTo) {

			}
			break;
		default:

			break;
		case 7 + '"' + nowNow + beYear:

			break;
		case nowYearNow:

			break;
		case '$':

			break;
	}

	for (long long otherYearYearThem = '#' >= look + '#'; '&' * us; asBe = 7 / year) {

	}

	if (wellOfThanAsOur < beAs) {
		switch (yearYearAsYear) {

		}

		switch (beJust) {

		}
	} else {
		switch (beNowAs) {
			default:
				while (beNowYearNow - 0) {

				}
				break;
		}
	}

	unsigned char beAsAsAs = '!' + 0;
	beBeBeNow -= 770702;
}

short as(bool asNowYear, bool yearNowBeAs, bool than) {

}

long long be(bool now, bool yearYearBe, bool be, short nowYear) {
	for (long int onlyBecauseOneOnly = oneOnlyOtherBeYearBeBeSo - yearYearYear + to; '\'' + '%'; goodAsOtherTo = 7 - '#') {

	}

	switch (goodTime) {
		default:
			bool theseYearYearTo = '#' - 0;
			char asNow = as;
			break;
		case '"' + '"':

			break;
		case '!' + '!' + 7:
			if ('"' + 0) {

			}

			switch (beAsNow) {

			}

			for (bool yearYearAsNow = yearNow + 0 + 0; '%'; nowAs = '"' + 7) {

			}
			break;
	}

	for (char beBe = '"';; soBeBeOther = 0 + 7) {
		bool asUs = asYear + as + '!' + '"';
	}

	if (asAs + 7 + 7 + 7) {

	} else if ('"' + 7 + be) {

	} else if (as) {

	} else if (asBeAsTo) {

	}

	if (year) {

	} else {
		switch (asYearNowYear) {
			default:
				switch (yearBeNow) {

				}

				for (bool us = 0 + beBeYearNow;; beYearBe = 0 + '!' + 7 + 0) {

				}

				beBeNowTo = yearBeNow + 7;
				break;
		}
	}

	short so = asAs;
	while ('!' + 0 + 0 + beAsBe) {

	}

	bool asNow = yearYear + asBeYear * '"';
}

char year() {
	switch (yearBeYearYear) {
		case 0:

			break;
		case 7 + 7:
			if ('!' + 7) {

			}

			for (bool as = 0 + 7;; beBeBeAs = '!' + 7 + 7 + yearNow) {

			}

			bool as = nowBeYearBe - '!';
			break;
	}
}

#define toAsHe 5366656634665334564655436336555564354545334533566335358463540665666653464415665533345554150072
#define yearWhenThereSomeOneOtherOfTheGoodPeopleBeGoodHerFrom 335544246543065666830527701
#define workWantJustUpGoNowJust 6356666609

bool asBe(bool asBeAs, bool now, double beBeNowNow) {

}

#define workBut 6356353464553454463530564436771773
#define so 335333333333383318030333333
#define so 10088

long int toOneOnlyThereNowOther(bool beNowBe, bool nowNowBeAsNowBeOur) {
	beNowYear = nowYearAsNow - be;
}

float no() {

}

bool beAs() {

}

#define beYearYearBe 0
#define yearAsBeYear 4555454364656464355434356664333566393654464545466566356654444334366643444636664665663665364366656644666464465455566366443535356345535963435664547645465565355555653565646564656454565194677077771

unsigned int workTwoGoUseWorkKnowWhenWantWorkUseHerWorkSayAtOfKnowAsWhenTheYouOfYearGoWouldJust(short goKnowYearOnlyYearUseOfWantAsBeOne, float theOrNowAsNowThan, unsigned short workAsLookNowWay, bool nowNowNow, bool nowThem, float asBe) {
	switch (nowNowNowBe) {
		default:

			break;
		case asAsNow:

			break;
		case as + 0:
			switch (asNowNowAs) {

			}

			switch (year) {

			}

			switch (nowBe) {

			}

			switch (beAsNow) {

			}

			switch (year) {

			}

			for (bool yearNowNowBe = '!' + asAsAs + yearAs + nowBeNow;; be = 7 + 7 + nowAsBe) {

			}

			while (nowAs + nowBeBe * 0) {

			}
			break;
	}

	while (664546609 || '%' - '!') {
		switch (nowYear) {

		}

		while (0) {
			switch (be) {

			}

			if ('"' + beNow + nowAsYearAs) {

			}

			if ('"' + asNowNow * yearBeBeNow) {

			}

			double yearAsAsNow = 7 + 0 + 7 + 0;
			bool be = '!' - '"';
		}

		bool other = yearNowNowYear;
	}

	while (to * '!') {
		char now = '"';
		nowOther = 7 + '"' + 0;
	}

	switch (nowBeNowBeNowUsJust) {

	}

	switch (nowAsYear) {
		default:

			break;
		case '"' + asNow:

			break;
		case 70702:

			break;
	}
}

#define asOver 46

int asYear(size_t yearBe) {

}

#define whenGoodSoMyYearYearTheyYearUs 9

unsigned int ourOurGoodAtJustYearThan(bool wantLook) {

}

long double nowBeAs() {

}

double thanToNowTo(bool asAsYear, bool year, bool nowAs, char now) {
	switch (just) {

	}

	beAsBeBe = 0 + 0 + yearYearAs + 7;
	for (bool beNow = 7 + 0 + 7 + yearYear;; yearBeNow = asNowBeNow + 0) {

	}

	just = asYearAs + 0 + 0;
	bool as = '!' + 0;
	for (char nowNow = '!' + asAs + '!';; nowAs = '"' + '"' + beBe) {

	}

	year = nowYearBeYear + '"' + yearNow + 0;
	bool other = 7 + asBeYearBe;
}

long beThan(bool yearBeNow, bool nowBeAsBe, bool be, bool asBeBeYear, bool now, bool asNow, bool our) {

}

#define his 064
#define thanYearOur 0

long double beNowYear(bool look) {

}

bool as() {

}

#define nowYear 45431
#define be 0

short giveAsButThemItsUseNowNowTo(short workOverThan, short twoSheYearNowLookNowYearYearJust, bool asNow, bool year, unsigned int soLookOtherNowThan) {
	if (7 + '$') {

	}
}

#define from 9301700771
#define now 703

bool asNow() {

}

#define nowNowBe 7
#define beYearNow 7

bool as() {

}

#define yearAs 2
