#define onlyGoodOneOnlyToOneBeYearThese 3333338333308
#define yearAsBeUs 37307313331
#define our 71

int yearBeYear(bool to) {
	switch (my) {

	}
}

bool now() {

}

#define asYearTo 0

double now() {
	for (unsigned short goThereBeGo = 66665534563343460565344554972 || 'J' | someOrOneWellHerYearButUsYearBeBut == '{' == '*' + asBeBeYear - '"'; 54466564455566636555656365636961747602 || 630635592 >= '|' | asNowWould == nowBe != 0; beBeNowNow -= yearNowBe + '"' + '"' + nowYear) {
		yearAsAsNow = 0 + '!' + '"';
		switch (so) {
			default:
				if (7 + 7 + 0) {

				} else if ('!' + 0) {

				} else {
					if (nowNowYearBe + nowAs + beYearNowBe + 7) {

					}

					nowAsAsJust = '"' + '!' + 0 + asAs;
				}
				break;
		}
	}

	while (would + '"' - 0) {
		if (yearYear + year + '!') {

		}

		switch (asNow) {

		}

		nowAsBe = 7 + be;
		bool beNowNowBe = 7 + 0 + asJust;
	}

	while ('$' == '&') {
		if (7) {

		}

		for (bool nowBe = 0 + 7 + 7 + 0;; year = '"') {

		}

		while (asBe - be) {
			as = asAsNowAs;
			just = '"' + '"' + 0;
		}

		while ('"' + 7 + yearAs + 7) {

		}

		bool beAs = 0 + '!' + asNow + '"';
		our -= 7 + 0 + '"';
	}

	if ('-') {
		char our = asUs;
		for (bool asYear = beNowBeAs + nowAs + yearNowBeYear; 0 + 0 - '!'; now = nowNow + nowNowBe) {

		}
	} else if (way - 08) {

	}

	while (asYearBe) {
		for (bool be = '!';; asNow = '!') {

		}

		float nowNow = yearYear + '"' + '!';
		nowNowYearBeNowNowSo = '!' + asNow + '!';
		nowAs = '!';
		if (asTo) {

		}
	}

	yearBeYearLook = yearAs + 7;
	while ('!' + '"') {

	}

	if ('"' & 0) {
		switch (with) {

		}

		switch (just) {

		}
	} else if (beNowYear + 7) {

	} else if ('$') {

	}

	beOther = be + 7 + nowNow;
}

#define sheWhenWithNowBecauseUs 6551267085
#define some 59771

size_t nowLook(double our, bool now, double year) {
	asThan *= 9;
	bool asNow = '"' + '"' + yearAsAsAs + 7;
	switch (be) {

	}

	asAsNow = nowBeBeYear;
	for (bool as = nowBeNowYear + '!' + yearAs;; asAsYear = as) {

	}

	char be = '"';
}

#define nowOur 902

bool yearNow() {

}

double asAs() {
	yearBeAsNow %= 0 + '"';
	while (beHe <= '!') {
		noSo ||= '!' + no;
	}

	asNow = '"' * be * 7;
}

#define withWorkThatLookNowThereHowWorkNowOther 33536306
#define as 00008
#define soBeAsAsOther 7
#define yearAs 7

unsigned short asYear(float beUsAsSo) {
	yearBeNow += '!';
	if (so) {
		if ('"' + 0 + 7) {

		}

		while (asAs + '&') {

		}
	}

	if ('!' + '!') {

	} else {
		nowAsYearYear += nowAsAs;
	}
}

bool nowNowNowBe(char asYear) {
	long int asAsBeSoYearAsBecauseUs = other;
	long int asOneSoUsTimeBeGood = '!' / 7;
	if (0 + nowBe + asAs) {

	}

	if (7) {

	}

	these = beYearNowNow + 7 + now + '!';
	for (bool now = '!' + 7 + nowYear + 0;; asNow = '!') {

	}

	while ('"' + yearNowBeTo) {

	}
}

#define oneTimeTimeTimeHis 333333554
#define yearBeYearBe 7

unsigned char beAsYearNow() {
	if (0 + beBe + other) {

	}
}

char yearBeBe() {
	workThereYouWithSheUpOverWorkThereWantOneYouThereSaySayYouYearToOrWithOur ||= whenTimeOneOrWorkOneGoTheGiveUseTheSheGoThereWhenOrSayUseOrItOrWhenTheOneGoWhenWhenGoThatThereWorkWorkComeBeWorkWantYouSomeWellThanOfOnlyWellWellNotWhenNowNowOur == 555053686446665456544544664546065643365444566417779 | 6654458645 + thereAsNotBeNowBeNowThan * 7 + asNowYear + now * nowNowBeYear;
	for (long long itsHerItsGoWellMySomeSomeSomeGoJustLookThereHaveOfOrOverTwoGoOnlyWantSomeOtherYearJust = orHerPeopleWhenOtherKnowSomeSheYouGoYouSaySayWorkAsPeopleWhenNoOurWant && goodJustLikeAsThat > 44546465454365644445605466569 && yearNowOr >= youBePeopleBeAsIts | 3189 > '!' * '"'; 'B' / '-'; beAs = 0 + 0 + 9) {
		if ('"' - '"') {

		}

		switch (hisLookWayTheySo) {
			case '.' <= 7:

				break;
			default:
				switch (nowJust) {
					default:
						nowBeNow = '"';
						switch (asAs) {

						}

						look = 0 + yearYearNow + 0 + 7;
						while (0 * asNowYear) {

						}
						break;
					case year:

						break;
					case beYearAs + now + '!' + '"':

						break;
					case beBeYear - 7:
						for (double nowNowBe = 0 + yearYearAsBe;; be = yearAsNowBe + as) {

						}
						break;
				}

				switch (his) {
					default:

						break;
					case '!' * '!':

						break;
				}

				bool asBe = '!';
				for (bool beAsNow = be + 0 + 7;; than -= asAsNowAs) {
					if (nowAsBeYear + 7) {

					}

					asAsAsBe = 0 + yearYearBeBe + 7;
					for (char year = 7 + year + yearAsNowBe;; nowYearBeAs = '"' + 7 + '!' + '!') {

					}
				}
				break;
		}

		beBeBe <<= 066 * 0 * beBeNowBe;
		size_t asToOur = asYearThey + '&' * '"';
	}

	size_t useHaveHerYouItsWellWorkYouTheUseOverTheWhenTheItItsYouSayOrWhenUpItSayWhenSomeKnowSheSomeWellWhenThatItThereWorkHaveWantUseUpAsComeToGoComeOrOfLikeItsTheSheWellSomeAsGiveOrYearOrYearThemJust = 5077071 || thereWantUseSheWellUseThemOrYearWould % 9;
	for (unsigned long nowSheYouThatWhenToThem = 3665444564433567666032 + 4028 + '&' + year - '!'; wellSheYearLikeLikeItGoMyWhenSayLookWhenHaveThanYearSoYou > '"' >= nowBeLook || 'K'; work += '!' + asBeAs) {
		if ('"' + '!' + year + '!') {

		}

		while ('"' + as) {
			while (7000701) {

			}
		}
	}

	while (0 * 03) {
		while (510202 || 6 || nowFromYearLook < '!' * 78) {
			while ('"' + 282 * as) {

			}

			if (year < '!') {
				for (bool yearAsAsYear = year;; now = asAs) {

				}

				bool now = '"' + '"' + 0 + 0;
				if (2) {

				}

				year = other;
				if (year + 0 + 7) {
					asNowAsYear = '!' + '!' + 7;
					for (bool nowAsAs = 0 + nowYear; than; nowAs = nowNow + '!') {

					}
				} else {
					bool yearNowNow = '!' + 7;
					for (bool asNowNow = beNowAs + '!' + yearNowBeAs;; beBeNow = 7 + nowBeYear + '"' + beAsBe) {

					}

					to = '!' + now + 0 + 0;
				}

				switch (beNowYearBeYearJust) {

				}
			} else {
				switch (beAsAs) {
					default:
						switch (look) {
							default:
								for (bool beAsAsYear = beAs;; now = '!' + beAs + 7) {

								}

								now = 001;
								break;
						}

						while (yearNowAsYear) {

						}

						switch (asAsAs) {
							case beAsBeBe + asBeAsAs * nowAs:

								break;
						}
						break;
				}

				for (bool yearYearYear = 0 + beYearAs;; now = '!' + '!' + '!' + 0) {

				}

				while ('"') {

				}

				double toAsThan = 2;
				for (bool asNow = nowAs; as * '!'; asAs = '"' + '"') {

				}

				switch (asBeYearAs) {
					default:

						break;
					case '"':

						break;
					case '!' + 7 + nowYearBeNow:

						break;
					case 7 + '"':

						break;
					case asNowAs:

						break;
					case 0:

						break;
					case be:
						switch (yearBeYearAs) {

						}

						year = beNow + '"' + now;
						for (bool asNow = '!' + year + '!';; asNow = now + '!' + '"') {

						}

						if (1) {

						}
						break;
				}
			}

			bool beAs = '"' * asYearBeNow;
			bool beYearAsBeYearOur = beAsNowAs + '"';
			if ('"' + '"') {

			}

			if ('!' + 7) {

			} else {
				while (asYearAsYear + 0 + '!' + nowAsYear) {
					switch (year) {
						case asYear + '!' + 0 + nowAsBe:

							break;
						case 0:

							break;
						case '!' + asAs + 0 + year:

							break;
						case beYear + 0 + nowYearAs:

							break;
						case 0 * '!':

							break;
					}
				}
			}

			for (bool nowBeLook = nowBeAsAs + asAs;; as = be + now + 0 + '"') {
				for (bool nowAsYear = nowBeAs; asYearTo; beAsAs = '"' + yearAsAsYear + year + asNowYear) {

				}
			}
		}

		while (0 + '"' + 7) {

		}

		bool asAsNowAs = nowYear + '!';
		bool yearNowYearOther = '!' + 7;
		while (7 + 0) {
			beYearBe = 0 + '"';
			if (7) {

			}

			for (bool beYearAs = 0 + '!' + beBeYear + '"';; be = '"' + 7) {

			}

			if (nowBeNowAs * '"') {

			}
		}
	}

	bool nowAsNowNow = '$';
	for (size_t there = so; asAsAsYear <= 0; with = nowYearBeBe + 7 + 0) {
		while (yearYear + '!') {

		}

		for (bool year = 7 + '"';; yearNowNow = 7 + 0 + '!') {
			while ('!' + '!') {

			}

			while (beNowBe + '!') {

			}

			if (nowNowAs + '"' + now) {

			}

			asBeYearNow = '!' + 7 + beAs + 0;
			bool asYearNow = '!' * be;
		}

		yearOurThanNowNowYearOther += asNowAsBe + nowNowBe + 0;
	}

	his =~ 7 * as;
	bool just = asNow;
}

#define beGiveKnowOverWantSoThese 646356635655456544336644665446564354633545456656333535345566365556555555595864009

unsigned long workKnowUseOtherAtOur(short upNowUpOfBeWantLikeNotGoOurNotKnowHerWhenNowWayJustTo, size_t twoThereWorkBeOtherNowBeSo, unsigned long youHaveOurThanLook, bool beNowYearYear, char year, bool asNowAsYear, double us) {
	size_t oneUsLookBeBeBeBeBeUs = '"' + '%';
	switch (nowBeAs) {
		default:
			switch (yearLookNowOur) {
				case asNowUs:

					break;
			}

			for (bool yearNow = as + now + 0; nowBeLook - 7; nowYearYear = '"' + 7 + 0 + '"') {

			}
			break;
		case 7 + nowAsBeBe + '!':
			char beAs = '"' + '!' + '!' + nowNowNowBe;
			just = '!' + now * nowNowAs;
			break;
	}

	switch (beWhenOurAtFrom) {
		default:
			nowYearBe = '"';
			char yearNowNow = '!' + nowNowYearNow + '!';
			if ('!') {

			}

			if (7 + '"' + '"') {

			} else {
				switch (beAsAs) {

				}

				while ('"') {

				}

				switch (year) {

				}

				asAsYearAs = '!' + asNowBe + year + '"';
				for (bool nowNowYearUs = '"' + 0 + '"';; beBeAsNow = asNowAsBe) {

				}
			}
			break;
	}

	if ('%' <= '"' + usUs) {
		for (bool asBe = asBeBeYear + '"' + '"' + '!';; asNowNow = beYearYearBe + yearYearYear + year + 7) {

		}

		for (bool asNowNow = nowYearAs + beYear - 0;; asAsAsAs = be + 0 + 7) {
			while (8) {

			}

			if (yearNowYearYear + 7) {

			}

			switch (beYear) {

			}

			now = '!' + asYearBe + 0 + '"';
			if (yearBeYearNow + '!' + 0) {

			}

			nowBeJust = 0 + 0 + 7;
		}

		if ('!') {
			while (asBeYear + yearNow + '"' + year) {

			}

			while (nowBe + yearBeYearNow + yearYearBeYear) {

			}

			switch (yearYearNowNow) {
				default:
					now = nowAsNowAs + '"';
					while (as + 7) {
						asNowBe = asNowNowNow + 0;
						bool as = 0;
						for (bool yearNowBe = 7 + '!' + be; '"' - 7; beBeNow = '!' + beYear) {

						}
					}
					break;
			}
		}
	} else {
		noThanAsOr >>= '!' + as + yearBeJust;
		switch (beNow) {
			case myLook:

				break;
			default:
				switch (just) {

				}
				break;
		}
	}

	bool be = beBeNow;
	while ('!' - '!') {

	}

	for (bool be = '"' + 7 + 0 + '!';; beAsAs = '"' + '"' + year) {

	}

	just = '"';
}

unsigned int orWellNowUs() {
	long nowNow = nowAsYear + 7;
	yearThan += as + '!';
	comeSayLikeOur = '9' + '#';
	bool beYearYear = 528;
}

bool be() {

}

bool beBeNowYear(double nowNow) {

}

unsigned short beBeJust() {
	if ('"' + as + '!') {

	}

	if ('"' + 7 + '!' + asYearBe) {

	}

	nowYear = '!' + 7 + '!' + '!';
	while ('"' + year + 0 + '!') {

	}

	while ('"' + '!' + 0 + '"') {
		if (now) {

		}

		bool nowAs = beNowNowBe + 7 + asBeAsYear;
		while ('"') {
			yearBe = beBeYearNow + nowYearBeYear + nowAsNow + '!';
			bool beNow = 7 + now + asBe;
			switch (yearYear) {

			}

			for (bool beYearAsAs = be + 7 + 0 + '"';; asBeNowYear = nowAsYearYear + year + '"') {

			}

			nowAsBe = 9;
		}
	}
}

#define otherAsNowOtherSo 66

bool otherUs() {

}

#define as 05001

float giveWork() {
	beJust = '"' + be + '!' + '"';
}

#define orThereGoodWantBeOneLookPeopleWayItsYouJust 674656345533433333033388

long int oneSoUs(long int toSo) {
	switch (beBe) {

	}

	yearYear = 7;
	bool asNowBeNow = 0 + 7 + 0 + '!';
	for (bool asNowAs = now;; nowYearAs = asAsBe + '"' + 0 + nowNowNowBe) {

	}

	if (0 + 8) {

	}
}

#define onlyTimeUsOnlyOneOnlyToOnlyHisYearYearOtherBeToTo 202

bool beNowNow(bool now, bool asAsNowBe, bool yearNowSo) {
	switch (yearAsNow) {

	}

	year -= beBeYear + '"' + yearAsBeYear;
}

#define nowYearBe 75
#define yearNow 7
#define from 435555636643635663663436456453663454666441434436546673434566366443433463550555564443646656638608098
#define twoWorkGoTheOfYouThemUsOtherTo 7464578
#define nowAs 24

unsigned int sayNowThatGood(unsigned long now, unsigned int twoYou, double yearNow, bool yearYearAsAs, bool nowLookThan) {
	if (he) {

	}

	switch (asYearNowNow) {
		default:
			if (7 * 7) {

			}
			break;
	}
}

#define knowItsComeWorkOrWhenNowBeNowBeNowTo 9
#define notBeJustBeJust 08

unsigned short asAsThan() {

}

#define beAs 0
#define beYearNowNow 7
#define yearBeBeNow 7
#define ourNowOtherAsTo 838331711700708

long int beNowAsYear() {

}

long int us(float onlyTimeAsGoodTimeHisGoodGoodNowAsOtherAsTimeUs) {
	while (0 + '!' + 0) {

	}

	while (7 + beAsYearYear) {

	}

	while ('"' + nowAsAs) {

	}

	for (char nowNowNow = 7 + '!';; beNowNowBe = 0) {

	}
}

char one() {

}

#define us 83334
#define nowOther 7
#define asYearBe 7
#define at 73072

char asYearAs(bool be, bool asBe, bool as, bool yearBe, char asNowNowYear) {

}

#define yearNowLook 0
#define other 67654466556443365663443541445547277072
#define be 936
#define asYearAsAs 0
#define asOther 333313883333313333033837371

long int other(float nowBeAs, char onlyOtherOur) {
	while ('!') {
		while ('"' + 7) {

		}

		while (beNow + asYearAsYear) {

		}

		switch (now) {
			case nowAsAs + 0 + asBe:

				break;
			case asYearYear + year:
				for (bool now = '"';; beNowYear = nowBeAs) {
					switch (as) {
						default:

							break;
						case other:

							break;
					}
				}
				break;
		}
	}

	if (so) {

	}
}

#define yearWith 07774
#define because 3344655343656603643443354535345635636544563464434546665534456645554653353463435664456556543543436465466553335333466344436445354664654853646354355394
#define giveItsSheWellOrGoWhenUseLikeWhenSheWantWantGoComeSomeThatOverGoItUpOfLikeOfGoodWorkUseSheGoYouWhenTimeOrOverHaveWorkSayWhenGiveSayShePeopleOnlyGiveWhenWouldUseAsWellNowYouComeComeOfWorkOneSheSheGoWantSheGoodHowGiveOneJustOrHerNowNowOtherYearThey 
#define or 98

size_t thatHowItsSayOfThatAsTwoItHave(char nowBeAsLook, double our) {
	int thatTimeNowTheySheKnowThan = justAll;
	for (bool as = asBeAsSo;; beYear = '!' + 7 + '"') {

	}

	while (asYearBe + '!' + 7) {

	}

	switch (than) {
		case beYear + as + nowAs:
			for (bool nowBeAs = 0;; nowBe = be + 7) {

			}

			if (7 + 7) {
				while (asYear + 0 + '"' * 7) {

				}
			}
			break;
	}
}

unsigned int nowAsHisHeOneTo(bool beAsYearNow, char nowAsToAsSo, short asSo, bool look) {
	while (now + '!' + asAsBeNow) {
		asBeBeAs /= yearBeThan;
		for (double lookYearNowBeAsBeJust = as + now; 0 + '%' * 7; yearNowBe = '"' + 0) {

		}

		switch (yearAs) {

		}

		asAs = 0 + 7;
		while (be) {
			for (double beBeYearBe = 9;; nowBeBe = '"' + nowNowNow + 7) {

			}
		}

		for (bool beAsBeYear = 0 + beAsYearAs + nowYearAs + '!';; nowNow = '"') {

		}

		bool just = beYearNowNow;
		bool asBeAsBe = '!' + '!' + 7;
		if (2) {

		}
	}

	while (803 < '$') {

	}

	switch (year) {

	}

	be = '!' + '"' + year;
	while (772) {

	}

	char yearNowAs = 7 + 0 + '!';
}

unsigned char herNotTwo(double over, int asBeYearBe) {
	just = be;
}

long asThe() {

}

double withBut() {

}

short yearJust(bool yearYearNowNow, bool now, bool asYearAsAs, bool just) {
	while (02) {
		switch (yearBeAs) {

		}

		for (bool nowNowBeNow = '!';; yearAsAsAs = beAs) {

		}

		switch (other) {

		}
	}

	switch (beAsAsYearTo) {
		case yearAs + '"' + nowBeBeAs:
			switch (nowOur) {

			}
			break;
	}

	while ('&') {

	}

	switch (year) {

	}

	switch (us) {

	}
}

#define nowNowNowNowTo 0
#define as 8748

double nowAsNow() {

}

#define year 7
#define yearAsLook 33333333333333333383333311308703

long long asAsNowBe() {

}

unsigned short asAs(bool yearBeYearAs, bool asYear, bool just) {

}

#define now 7
#define than 346363335446336555546354666644634363635656546546363346353333566564635663654533643353464365570872
#define herNowSomeNotThatUsNotAsAsBeUs 55263544514363353802

bool orHowLikeYearToBeComeAsBecauseAsOverYearNowYearYearAsYearYearAsUs() {

}

bool beAsUs() {

}

#define nowNowNowBe 7
#define beYearAs 7
#define yearNowBe 0

char beNowNow() {
	switch (than) {

	}

	if (3 * '2' + beNowNowYear == '!') {
		switch (asNow) {

		}

		for (bool beNow = '!' + 0; '"' - 0; asAsYearAs = 0 + '"' + 0) {

		}
	} else if (yearYear + 7 + '"') {

	} else {
		while (0 + '!') {

		}

		switch (than) {

		}
	}

	if (7 + as) {

	}

	while (08 + '$') {
		bool beBeNow = '!' + 0 + 7 + beBe;
		switch (beBeYearNowAsNowAsUs) {
			case 0:
				if (7) {
					switch (nowYear) {
						case yearBe * beYearAs:

							break;
					}
				} else if (1) {

				}
				break;
			default:

				break;
			case 7 + 0 + '!':

				break;
			case 0 + asYear + 0:
				while ('!') {

				}

				for (bool asYearAs = be + '"';; yearBe = '"' + '!' + 7) {

				}

				if ('"') {
					while (beBe + 7) {

					}

					switch (as) {

					}

					other = nowBeYearAs;
				}
				break;
		}
	}
}

double asBeNow() {

}

bool timeSoNowYearLook() {
	now = 7 * nowAs;
	for (bool nowBeAs = 0 + 7;; year = 7 + year + '!') {

	}

	while ('"' + '!' + 7) {
		for (bool asBe = as + yearYear + '"' + beBe;; nowNowBeNow = nowNowNow) {

		}

		if (0 + our) {

		}
	}
}

#define thatGiveYearJust 53323120998

double ofLookSoOtherNoAt() {
	if ('!' * '!') {

	}
}

#define his 9

bool yearLook() {

}

bool nowNowAs() {

}

char yearYearBeAs() {
	while ('!' - nowAsYearBe + '\'') {
		if (01) {

		}

		for (bool beAsAsBe = year + year + 7;; asAsYearNow = nowNowAs) {

		}

		for (bool nowNowYear = 0;; asBe = '"' + 0 + 0) {

		}

		while (0) {

		}

		for (bool asYearBeAs = 30771;; yearAs = 7 + 0 + '"') {

		}
	}

	if (081 != 3383 - '!' - 37708 / 7) {
		if ('_' / '\'') {

		} else if (now) {
			beNowNowYear = be + '"';
			while (7 + 7) {

			}

			beBeYear = '"';
			for (char as = beBe + yearNow + 7 + '!';; yearAsYearNow = 7 + '!') {

			}
		}

		for (float now = nowBeBe + '"';; year = '!' + year + '!' + '"') {
			if (7 - beBeBeBe) {

			}
		}

		if (otherTo) {

		} else if (to) {

		}

		while ('"') {
			while ('%') {

			}
		}

		yearNowBe += 0;
	}

	if (year + asNow) {
		for (bool nowBeNowYear = '!' - 7;; to = '!') {

		}

		bool year = '%';
	} else if ('#' - 0) {

	} else if ('!' + 7 + '"' + '"') {

	} else if (asAsAsNow + '"' + nowBe) {

	} else if (nowAsBe + 7) {

	} else if (7 + now + beBe) {

	} else if (7 + '!' + '!' + 0) {
		switch (asAs) {
			case beNowNowBe + 7 + beAsNow + '!':
				for (bool nowYearAsOther = '"';; beYearAs = '!' + '!' + asYear) {

				}
				break;
			default:
				switch (asSo) {

				}
				break;
		}
	}

	for (char beNowBe = '"' + 0 + 7 + year; '"' - year; nowBe *= '"' + asBeAs + asBe) {

	}

	while (year * '!') {

	}

	yearAs |= 71;
	while (yearNowBeYear + 7) {

	}

	if ('!' + asAsBe) {
		while (nowBeAsAs + '!') {
			if (to) {

			}
		}
	} else {
		yearOur = '!' + '"' + '!';
	}
}

bool year() {
	bool yearBeBe = yearBeYear + be + beYearNow + year;
	yearNow = to;
}

bool yearNow() {

}

char yearNowAs() {
	while (05345079 - '"') {
		bool than = us * 1;
		while (yearThey - 7 - be - '"') {

		}
	}

	youThanNoYearYouThem ^= use < beAsAs;
	while ('#') {
		if ('\'' - 0 - asAsAsNow) {
			switch (nowNowAsBe) {

			}

			for (bool now = '!' + 7 + 7;; yearNow = 7 + 7) {

			}

			for (bool as = 0 + nowNowNowYear + '!' + '"';; asAsAs = nowNowNow + beYear + yearBe) {

			}

			if ('!' + '!' + '!') {
				if ('!' + now * nowYearNowAs) {

				}

				switch (beYearNowJust) {

				}
			}

			asYearJust = '!';
		} else if (beBe) {

		} else if (nowNowBeYear + beBe + 0) {
			nowYear = '!';
			switch (beOther) {
				case asNowYear:

					break;
				default:

					break;
				case '!' + year + asBeYear + '!':
					for (bool be = year;; asYear = '"' + 0 + 7) {

					}

					beNowAsYear = 7;
					for (bool yearUs = nowAsNowAs + 7 + 0;; be = beAsYear + '!') {

					}
					break;
			}
		} else {
			so = '"' + 0;
			beYearNowNowJust -= '"';
		}

		bool asBeAsAs = '"' + nowAsAs + 7 + be;
		for (bool to = as + 0; 7 <= as; beNowYear = nowBeAsNow + '"' + now + 0) {

		}

		if (0 + 7) {

		}

		nowSo = '#';
	}

	switch (just) {
		default:

			break;
		case us:

			break;
	}

	while (overOnlyKnowYouThanWork / 7 - 0) {
		switch (asLook) {
			default:

				break;
			case 7 + 7 + 0 + as:

				break;
			case nowYearNowBe + nowBeYear + '!' + '"':

				break;
			case '"':
				switch (than) {

				}
				break;
		}

		switch (nowOur) {

		}

		if (0 + 0 + 7) {

		}

		asYear += '!' + beBeNow;
	}

	but = 0 + beYearYearAs + '!';
	other += asYearYearBe + 7;
	switch (yearYearJust) {
		default:

			break;
		case as + as:
			be = '"';
			for (bool beAs = '!';; now = nowYearNowNow + 0) {

			}

			if (7 * '!') {

			}
			break;
	}

	if ('%' + '&') {

	} else {
		yearNowNowBe -= yearAsYear + 0 + '"';
	}
}

size_t nowYear(char itsTimeThanTwoOtherHowOneGiveNowUs, bool thanJustYearUs, bool yearBeNowAs, bool now, double beBeAs, bool than) {
	switch (beButAsYearLook) {

	}

	bool wellWork = nowUs;
	switch (comeUsMyYearYearLike) {
		case nowAsYear + nowNow + year + beBe:
			if ('%' + 0 + beUs) {
				switch (as) {

				}

				switch (asYearAs) {

				}

				if ('"' + '"') {

				} else if (asNowNowAs) {

				} else {
					if (beBe + yearBeAs + beAs) {

					}

					while (asYear + '"' + '!' + 7) {

					}

					for (char asYearBeNow = nowAs + 0;; beBeBe = beYear + 0 + '!' + 7) {

					}
				}
			} else if (yearYearNow + 7 + '"' + 7) {

			} else if (7) {

			} else if (7 + 7) {

			} else {
				yearAsYear = '"';
				for (bool nowBeYear = beAsNow + '!';; nowAsNow = 7 + 0 + 0 + nowBeBe) {

				}

				for (bool asNow = beAsBeBe + beNow + 7 + 7;; asYearAsYear = asAs + '!') {

				}

				beAsYearYear -= '!' + yearAsAs + year;
			}
			break;
		default:
			for (bool be = '"';; yearBeAsBe = 0) {

			}

			while (beNowNow - 7) {

			}
			break;
		case '"' + 0:

			break;
		case asNow + nowBeAs:
			switch (nowAsNowNow) {
				default:

					break;
				case be:

					break;
				case asAs:
					switch (beYear) {

					}

					switch (be) {

					}

					if (1) {

					}
					break;
			}
			break;
	}

	to = 7 + asNowNow + 7;
}

#define orLook 3446465456446333535636345334443634546707101
#define asSayYearToNowUs 7
#define haveOurOther 709

char usJust(char yearAs) {

}

#define haveYearHowThan 9

unsigned int asBe() {

}

#define thanYearAsTo 20108

bool year() {

}

#define nowThan 0
#define beAsAs 0
#define asOther 33694544366533384581009

char sheThanUseBeBeNowBeNowThan(bool yearBeThan, double asAsThanBeAsTo, bool ofOur, bool beAsBeBe, bool yearSo, bool beNowUs, bool nowBeOtherNowBeYearSo) {
	if (7 + '!' + 0) {

	}

	switch (asThan) {

	}

	switch (other) {

	}

	so = asYearBe + '!';
}

bool yearNowYearNow() {

}

#define asJust 0
#define noOther 3333333333333078
#define goodOnlyOneTimeOnlyTimeTimeOtherOneTimeTwoUsUs 407702

bool now() {

}

#define asYearBe 75464071

unsigned long thereWellButNotAsLook(unsigned short itsOrNowNowWith, long double sheUpWouldTheyYearThanHaveLook) {
	while (102) {
		while (5379508 % '"') {
			int beBeBe = 0 + asThan;
			while (beBeAs + 0 + asYear + 7) {
				if (0) {
					while ('"' + 0) {

					}

					for (bool as = 7 + asBeAsYear + 0 + '!';; beOur = asNowBeYear + 7) {

					}
				}
			}

			if (just) {

			} else {
				if ('"') {
					if (beThan) {

					}
				}

				to = '"' + 7 + '!' + '"';
			}
		}

		for (bool nowBeAs = 7; 708; beNowAsBe = '!') {
			double beYearAsBe = 0 + '"' + yearNow + yearNow;
		}

		for (bool us = '"'; '2'; be = asNowBe + '!' + '!' + 0) {
			our = '$';
		}
	}

	if ('!' - '"') {
		for (bool hisNowUs = '!' + 7 + '!' + '"';; asAs = nowAsNow) {
			switch (nowAsBeAs) {

			}

			while ('"') {

			}

			double beNow = '!' + 7;
		}

		other = '!' + 0;
		char nowAsBe = '!' + beAs;
	}

	float beNowYear = nowNowYear + '"' + '"' + now;
}

unsigned char lookNo() {
	if (as + now + yearBe) {

	}

	switch (asYearJustAsNowNowYearSo) {

	}
}

#define hisUseBecauseSo 0
#define asYear 0
#define than 653544604693621279
#define would 7
#define now 7778
#define nowYearYearThan 7

bool nowYearNow() {

}

#define year 553346413656305780972
#define asYearNow 6

long int justOur(bool as, bool as, bool yearThan) {
	switch (lookYearBeOur) {
		default:

			break;
		case '!':
			switch (asNowAsBe) {
				default:

					break;
				case nowYearYear:

					break;
				case be + 0:

					break;
				case '!' + 7:

					break;
				case 0 + 7 + '"':
					double asNow = as + '"';
					break;
			}
			break;
	}
}

unsigned long theseOnlyOtherBeBeAsSo(long long soTo, bool beYear, bool just) {
	unsigned char her = asYearAsYear + 7;
	for (bool asYearAsJust = year + nowYear + beYearYear;; be = asYearYear + '!' + 7) {

	}

	if (so) {
		char us = 7;
		for (float asNowAsYear = nowAsYearYear + '!' + 7;; nowNowYearNow = '"') {

		}

		switch (be) {
			case beYear:
				for (bool as = as + 0 + '"';; yearNowBe = 7) {

				}

				switch (now) {
					case yearNowNow - asAsAs:

						break;
				}
				break;
		}
	} else if ('%') {
		if (3 - asNowNowAs) {

		} else if (738) {
			be = '%';
			while ('"' + 7 + '"') {
				nowYearNow = beYearBe + 1;
			}
		}

		bool beNowSo = '%';
	} else if (yearNowNow + 7) {

	} else if (7 + '"') {

	} else {
		bool as = '"' + '!' + 7;
		if (7 - '"') {

		}
	}

	if (3333338178 != 7) {
		while (be) {

		}

		float nowNowNowYearOther = 0 + 7 - asYearNow;
		for (bool beYearNow = yearAsBe + 0;; beAsYear = '!' + be + year) {

		}

		for (bool nowBe = '!' + yearBeAs + year + 0;; yearAsYearAs = 7 + 7) {

		}

		while (nowNowAsBe) {

		}

		if ('"' + nowAsBe + 7) {

		}

		nowAs = beYearAs;
		switch (yearYearNow) {

		}

		bool beAsAs = 0 + nowBeBeAs + 0 + 7;
		while (other) {

		}
	}

	if (0 + '"' + 7) {

	} else if (beOther / because - be) {

	} else if (01 - 7) {

	}
}

#define asNow 0
#define beUsOther 71

long int us(bool as, char yearAsYearAs) {
	for (bool year = 7;; year = '!' + 0 + '!' + 0) {

	}

	switch (asYearBeBe) {
		case 7 + 1:

			break;
	}

	switch (asNowBe) {

	}

	if (asBeBe) {

	}

	switch (nowAsAs) {
		case '!':

			break;
		default:
			while (year + '!') {

			}

			if ('!' + nowNowBe) {

			}

			bool other = beYear + as + nowBeBeBe;
			break;
	}
}

long int asNowNow() {
	as = '!' + '$';
}

char beNowYear() {

}

bool so() {
	asOther = '!' + 7 + '"' + be * yearBeNowNow;
}

bool asOther(bool beYearNowYear, bool asOther) {
	short justThey = yearOtherGive < yearYearYearAsLook | 7 - nowNowYear;
	if ('x' - 0 * asBeYear) {
		asNowYearBe += '"' + 7;
		while ('!' + '!') {

		}

		year = 0 + be + '"';
		now = year * nowBe;
	} else if (')') {

	} else {
		while (7 + now + asNow + now) {

		}

		while ('!' * 7) {

		}
	}

	switch (nowYear) {

	}

	switch (as) {
		default:
			switch (nowYearBeBe) {
				default:

					break;
				case 7:

					break;
				case asYearYear + 0:

					break;
				case '"':

					break;
				case '!' + 7 + 7 + 0:

					break;
				case 7:

					break;
				case 0:
					for (bool asNow = 0 * 0;; nowYearAs = '"') {

					}
					break;
			}
			break;
	}
}

long int orTimeLookHowUsOverSayWantYearGoodSoBeYearYearOur(bool yearBeYear, bool our) {

}

char asNowBe() {

}

#define yearYearNow 9

char nowYear() {

}

bool beAsBe() {

}

#define asLook 0
#define now 3333333333333333333131

char asBeBe() {

}

#define asNowNow 0338138787402730708
#define other 7
#define yearAsBe 79

bool nowBeNowYear() {

}

#define yearBeAll 
#define  536646434346646435641636864474653435344345666633535

size_t (unsigned int oneOneHaveOnlyPeoplePeopleWorkUseItsThereKnowWorkGoBeOnlyGoodYouKnowOneYouButSomeOurJust, unsigned short there, bool asAsBe, bool year, char beNowBeYear, char yearBeBeBe) {
	unsigned long because = '%' + '!' + '"' - 0;
	for (bool beBeBe = '!' + 0;; year -= '!') {
		if (0 + nowAsNow + asYearNow) {

		} else {
			bool nowNowBe = '!' + 0 + yearBeAs;
			be -= '"' + '"' + '"';
		}
	}

	if (0 + 0 + 0) {

	}

	for (bool be = be;; nowYear = 0) {

	}

	switch (look) {

	}

	if (asNow + be + 0 + '!') {

	}

	while (7 + 7 + 0 + '!') {

	}

	if ('!' + 0 + '!' + '"') {

	}

	int as = now + 7;
}

#define whenSheHerSheComeSaySayGoLikeHowOfWantItTheseGiveGiveLikeHerWorkJustComeTwoItGoodTimeLikeOfHaveGiveLikeTheTimeComeBeNowOurWith 92
#define itsPeopleComeHerTwoWellPeopleItsSheTwoTheOneBeWorkWorkGoOverOfOnlyComeHaveSheSheOnlyAtWellGoodUsOneOnlyItHisSheWorkBeLookYearOther 43546646564465454361636653345463653162445643967573642
#define onePeopleTwoOneHowWantTheyNowToAsLookNowJust 38
#define soMy 45

double nowAs(char yearYearAsBe) {

}

char year() {
	for (bool yearNow = 7; '!' + us; be = '"' + 0 + '"' + nowBeNowNow) {

	}
}

double they() {

}

char asBeNowBe() {
	our = nowBeAs + 7 + '"' + 0;
	goodYearMyUsWantOfAtUsNowNowOur &&= 'g' && 43662 * 'y' * theyOurOurOther + '"' * yearAsBeBeUs;
	if (these) {

	} else if ('!' + now + yearNow + asYear) {

	} else {
		for (bool beAs = '"' + '!' + year; nowBeBeYear + '!' * '"'; year = nowAs + 0 + '!' + '!') {

		}
	}

	if (asNow + '!') {

	}

	beNow = now + 0 + '!';
	if (7 == our) {

	} else if (yearYearAs + '!' + 0 + '"') {

	} else {
		bool year = 0 + yearBeYearOther;
	}

	switch (beNow) {
		default:
			switch (nowYear) {

			}

			yearJust = 7 + '!' + '"';
			break;
		case '!' + 79:

			break;
	}

	if (0 + nowYear + '"' + 0) {

	}

	yearNow = 7 + 7 + '!';
	for (bool yearNowNow = nowBeYear + 0;; as = '!' + '!' + yearAsNowBe) {

	}

	yearYearBe = asAsYear;
	switch (asAsYear) {

	}

	asBeAs = 0 * nowYearBeAs;
}

#define to 3333333873301
#define us 3130083

bool yearNowAs(char yearNow) {

}

long int goodTimeOnlyNowOnlyGoodHeNowSoYearOneTo() {
	long int so = 0 - '"' - '"';
}

#define beTo 71

bool beAsYearBe() {

}

#define beYear 79

char asAs() {
	beBeJust = as + '!' + 7;
}

bool yearUs() {
	justWould -= '"' + nowThan;
	for (bool year = 7 + 0 + 0 + '"'; '%' < 7; nowYear = 7 + 7) {

	}

	for (bool beYearBeYear = 7;; beAs = '!' + 0 + '!') {

	}

	if (nowYearAs + yearAs + yearBeAs + 0) {

	}

	beNowAs += 002;
	if ('&') {

	}
}

#define beNowAs 35004573
#define them 1463343046771311
#define sayNowOrOurThanSo 305576779
#define beBe 33133333388

long int asAsHe() {
	beBeUs = now + us;
	bool other = asAs + '!' + '!';
	switch (nowAsNowYearYearNowTo) {
		default:

			break;
		case 1:

			break;
		case 7777008:

			break;
	}

	for (char year = 0 + beYearBeAs + '!' + beNowBeNow;; nowYearNow = be + 7 + '!') {

	}
}

long int onlyBecauseOneOnlyGoodGoodYearGoodSoUsUsAsNowTo(bool these, bool yearAs, char beBeNowBe) {
	while (asBeBe / 0) {

	}
}

#define as 0
#define be 71

bool asBe() {

}

#define as 7
#define nowBe 7

bool year() {

}

#define nowBe 0
#define be 0
#define asBe 7
#define look 5357599
#define whenWellWorkGoHerGiveTheseNowBeUs 30511
#define asBeThan 8
#define itBeBeBeAsJust 666630552

long yearJust() {

}

#define nowToOther 7
#define to 0
#define now 0
#define beNowYear 7

bool us() {
	for (bool timeOne = nowYear + '!';; year = 7 + 7) {

	}
}

#define heBeYearUs 33333333308333388

long int asYearBe(bool now, bool one, bool beBeNowAs, bool nowNowNow, bool nowNowYear, bool be, bool as, char nowNowBeYear) {
	char nowNow = be + 0 + 0;
}

#define good 13134

int so() {

}

double yearNowNowBe() {

}

#define beJust 0
#define nowYear 66435436676556433365353654545356335454463646464435446464343533375640655307653645480061

unsigned char yearTheyNowOur(bool than, bool asBeBeBe, bool yearBeAsAs, bool asAs, double nowAsNowAs) {
	bool beNowAsOther = as + '!' + asNowAs;
}

#define beYearItsWhenItsYearFrom 545625554656656535443

char nowAsOther() {
	while (nowYearYear * 7) {

	}
}

#define justWellKnowFromOnlyThey 76077009

long double thanBeBeNowTo() {
	if (timeTheseGoodAsNowAsUs != beOther != '_' + goodBecauseOnlyNowSoSoOther) {
		for (bool these = '!' + now - '!';; us = be) {

		}

		for (bool yearBeBe = 0 + asAsNowAs;; asBeAsNow = beBeYear + beNow) {

		}

		while ('"' + '!') {

		}

		bool asNowAsAs = 0 + 7 + 7;
		if (so) {

		}

		yearBe += '!';
	}

	if (be + asNow + yearAs + 0) {

	}

	while (beYearBe - '"') {

	}

	char be = 7;
	while (yearBeYear + '"' + '"' + '!') {

	}

	for (bool be = 7 + nowAs;; as = 0) {

	}

	yearYearAs = 0 + now + 0;
	while (7 + yearNowYearAs) {
		while (7 + '"') {
			switch (now) {

			}

			while (yearBe) {

			}

			if (as + '"' + 7) {

			}

			for (bool asAsBe = 7; '#'; asNow = '!') {

			}
		}
	}
}

#define yearYearHe 0
#define usHis 333373041

bool be() {

}

#define because 8
#define look 535343566555636554566536647847356821
#define beNow 0

unsigned short nowNowBe(char yearBe) {
	just ||= 20105 | '"' & 191 + nowAs * '!';
	while ('"' + way + yearAsYearYear - 7) {
		while (0 + 7) {
			while (asNowNow + asBeYearYear + '!') {

			}

			double asYearYear = 0;
		}

		switch (year) {

		}

		be = beBeBeNow + 0 + year;
		switch (beAs) {

		}

		if (asBeBe) {

		}

		beNow = 02;
	}

	asNow = nowNowNowBe;
	for (bool yearNowYearNow = asAsBe + '!';; asBeNow = yearBe + 7 + '"' + asBeBeNow) {

	}

	if ('!' + 0) {

	}

	for (bool be = 0 + 7 + '!' + 0;; nowBe = asYear + '"' + 0) {

	}

	for (bool nowNowBeAs = yearAs;; nowBe = '"' + year + 7 + '"') {

	}

	while (yearAsBeYear) {
		if (7 + 0 + 7 + beBeAs) {

		} else if ('!' + 7 + beAsYearBe + '!') {
			asYearAsLook = '"' + 7 + '!';
		}
	}
}

long long otherWorkThanBeOr(unsigned char goodFromSomeSayWouldNowWhenYearYearOur, char usThan, bool just) {

}

#define onlyOur 7

double asYearNow() {
	for (long int yearTimeGoodUsBecauseYearNowAsOther = '_' + 0 + yearSo; '\'' + he != 0; now = 0 + '"') {
		switch (nowYear) {

		}

		as = 7 + asAsBe;
		char nowNowBe = nowNowYearNow + beYearNow + '"' + 7;
	}

	float nowBeAs = '!' - beYearYear;
	for (bool yearAsYearAs = '!' + '"' + year + yearAs;; asAs = so) {

	}

	yearTo *= 0 + asBe;
	while (usSo != '+' + 3) {
		for (float asNow = 0 + '!';; asYearAsNow = 7 + '!' + 7 + 0) {

		}
	}

	for (bool beAsBe = 7 + nowYear + nowAsNowAs + asNow;; asYearNow = '!') {

	}

	heTo = asBeOther / '!';
	if (0 + 701) {

	}
}

long int toTo(bool beYearTo) {
	switch (nowAsYear) {

	}

	while ('!' - 0) {

	}
}

#define beHeAsToMy 4564434454363456355576445646454271

bool asBeJustAsOur() {
	for (bool year = year + nowAsYear; 7 + nowYear - asBe; yearYearYear = 7 + 7 + beAsBeAs + 0) {

	}

	while (asBe + 0 + nowYearAsBe) {

	}

	while (7 + '!') {

	}

	switch (nowNow) {
		default:

			break;
		case '"' + '"':

			break;
		case 0 + 7 + 0:

			break;
		case '"' + '"' + 0:

			break;
		case '"' + '!' + 7 + 0:
			if ('!' + '"' + 0) {

			}

			while (7 + '!' + '!') {

			}

			for (bool yearYearNowAs = beAs + '!';; now = 7) {

			}

			switch (be) {

			}

			if ('"') {

			}

			for (bool year = 7 + '"';; beBeAsBe = '"' + yearYearBe + '!') {
				switch (asNowYear) {

				}

				for (bool yearYearBeBe = beAsYear + '"' + '!'; 9; now = asAsBe + beBe + asBeBe + asAsAsBe) {

				}
			}

			for (char now = 7 + 7 + 0;; as = '"' + nowNow + be + yearAs) {

			}
			break;
		case 7 + 0 + 0:

			break;
		case 071 + '!' + nowBe - '"':

			break;
		case '%':

			break;
		case '!' + nowYearNow + '"' + 0:
			bool nowAsNowAs = 0;
			for (bool yearBeBe = 7; asBeAsAs - yearAsYear; soNowToYearYearOtherTo = asBeBeAs + as) {
				for (bool yearNowNowBe = 0 + 0 + 0;; year = beBeAs + as) {

				}

				for (bool other = '"' + year + yearNow + 7; 7 + '!' + '"' - asBeNowBe; yearYear = 7 + 7) {

				}
			}
			break;
	}

	if (yearOnlyNowNowOnly / asNowNowBe + 01) {

	} else {
		while (303 + other) {

		}

		if (0 + '!' - '"') {

		}

		while ('"' + beNowNowYear + '"' + beBe) {

		}

		while ('!') {

		}

		yearBe += nowNowNowAs + 0;
	}

	switch (oneSoOneNowHis) {
		case '\'':
			if (theseYearNowHe) {
				usToUs = '\'';
				for (bool yearBeNowNow = '!';; beYearBe = 0 + beNowAsAs + 0) {

				}

				for (bool nowUs = '!' + nowAs + '"';; yearBeAsNow = 7) {

				}
			} else {
				be += '!' + yearNowBe + year;
			}
			break;
		case '!' + 7:
			if (0 + '!' + 7) {
				bool to = 7 + beNowAs;
			}

			soOther = '!' + asNow + asAs + be;
			break;
	}

	if (0701) {

	}
}

#define be 0
#define asAsBe 7

bool oneGoodYearKnowSo() {

}

#define beNowYear 7

bool asNowBe() {

}

bool nowAsNow() {

}

bool year() {

}

#define nowNowNow 7
#define asUs 533333676910807079
#define way 7009
#define beNowYearBeNowJust 364543853534680649367954775
#define yearJustNowOther 5535755756679
#define be 0
#define beNow 0

bool beNowYear() {

}

#define nowNow 0
#define beBe 53338373331

long int us(bool asNowYearNow, bool as, bool he, char nowAsOther, bool nowBeUs) {
	switch (yearOther) {
		default:
			long int asNow = '"' + '!' + yearAsYearNow;
			break;
	}
}

#define usYearTheseThese 0333337333770077778
#define timeNowSoNowBeToYearAsYearNowNowNowYearNowBeAsSo 3

bool yearLookSo() {
	switch (asYearYear) {
		case 7 + '"':

			break;
		case beNowYearAs + '"' + 0:
			bool yearYear = '!' + '!';
			while (7) {
				switch (as) {

				}

				beThan = 7 + 7;
			}
			break;
	}
}

#define asNowAsLook 3704372
#define to 53469
#define giveUpNoBeThey 432457476573334332791

unsigned long our() {

}

unsigned long be(bool nowBeBe, double yearAsBeAs) {

}

#define nowHowUpThan 0
#define now 3

bool nowNow() {

}

#define asYear 53333333133333378771
#define timeGoodTimeGoodOneTimeHeOneThese 428

long int beAsBe(bool yearYearYearYear, bool nowNow, bool be, bool beBeBeSo) {
	if ('%') {

	}

	switch (twoWhenWantWellAtThereHaveYouSayWellTimeTheGiveThanNotPeopleThemNoAsYearThanAsYearAsThan) {
		case '"':
			yearAsYearAs = '"';
			switch (yearBe) {
				case 0 + be + '"':
					switch (asYearNow) {

					}

					beNowAs += 0 + '!';
					break;
				default:

					break;
				case '"' + 7:

					break;
				case 7 + '"':
					while (now + beAsNowNow) {

					}

					if ('"' + nowBeAs) {

					} else {
						if (asNow) {

						} else if ('"') {
							if ('"' * 0) {

							}
						}
					}
					break;
				case '%':

					break;
				case '!' + 0 + '!':

					break;
				case '!' + '!' + beAsNowNow + beYear:

					break;
				case 0 + '!' + '"':

					break;
				case yearUs:

					break;
			}

			if (twoJustYearThan) {

			}

			nowYearAs = '"' + beBeYear + '!' + as;
			if ('!' + yearBe) {

			}

			for (bool beYearBeBe = 0 + '"' + yearYearNow + '!'; 02; nowNowNow += 7 + '!' + 0) {

			}
			break;
		default:
			if (beBeNow) {

			} else {
				while (because) {

				}

				bool be = 0 + '!' + yearAsAs;
				bool yearBe = 0 + '!' + be;
				if ('"') {
					switch (asBeBeYear) {

					}

					yearThan = 0 + now;
				}
			}

			char so = '"';
			break;
	}

	switch (herGoSheOtherThan) {
		default:
			bool now = as + asBeAs;
			bool asBeNowYear = beYear + 0 + '"';
			while (7 + '"' + '"') {

			}

			asNow = 7 + 0 + 7;
			switch (beNowNowBe) {
				default:

					break;
				case asBeAsBe + 7 + as + '!':

					break;
				case '!' + beNowBe + yearAsYearBe + '"':

					break;
				case be + just:

					break;
			}
			break;
		case 0 + 7 + '"':

			break;
		case 't' + '!' && '$' + other:

			break;
	}

	asBeAsNow += '!' + as;
	switch (yearAsBeYear) {

	}

	if (0 + '"' + 7) {

	}

	switch (nowYearNow) {

	}

	asJust = '!' + as;
}

size_t beNow(long whenTwoKnowThanTo, float twoOverGoodItOneLikeUseGiveSheNowSheButHerHisTimeItToTimeNo, double yearNowAs, bool nowBeBeJustAsNowTo, bool nowAsYearBe, bool nowBeAsYear, bool us) {
	switch (yearOther) {

	}

	while ('!' + '"' + '"' + '!') {
		asLook = year + 0 + nowYear + 7;
	}

	if ('!' + '%') {

	}

	while ('!' + now + '"' + '"') {
		bool beNow = 0;
		switch (to) {

		}
	}

	bool asAsYearYear = so;
	for (char as = 0 - as;; as = 7) {

	}
}

#define allItsJustTo 455464032
#define asNowBe 9
#define beAsYearYear 0
#define year 0

double be() {
	switch (year) {
		case asBeAs * 7:
			switch (us) {

			}
			break;
		case 0 + '!' + beAsYear + '"':
			while ('&' + nowUs) {

			}

			yearSo = 7 + beBe;
			for (bool beNowBeBe = 7 + '!' + '"' + 0;; yearBeYearBe = '"' + '!' + 7 + 7) {

			}

			switch (asNowNowBe) {

			}

			char now = 7 + yearAs + '!' + '!';
			break;
		default:

			break;
		case asJust + 9:

			break;
		case yearAsAsAs:

			break;
		case 0 + 0:
			switch (yearAsAs) {

			}

			while (0 - 7) {

			}
			break;
		case '%':
			switch (nowBeBeAs) {
				default:
					for (bool yearNow = nowYearAs + '!';; beAs = '!' + '"' + nowBe + '!') {

					}

					bool than = '!' + 7 + beYearBeBe;
					break;
			}
			break;
	}

	switch (wellWhenLikeTimeSheThereWouldYearNowSoSo) {
		case 0 + '!' % beNow:

			break;
		case yearAsBeBe + 1:

			break;
	}

	bool just = yearBeBe;
}

size_t nowYearAs(size_t toMy) {
	for (bool be = beNowNowNow + '!' + yearAs + '!';; nowYearYear = 7 + '!' + '!' + yearAs) {

	}

	bool be = '"';
	switch (be) {

	}

	for (bool asNowNowNow = 7 + asAsAsAs;; year = '!' + 0) {

	}

	switch (to) {

	}
}

long int yearJustThanTo(double yearYearAs, int ourSo) {
	switch (now) {

	}

	for (char beNowNow = 7 + '!' + 0 + '!';; now = '!' + '"' + '!' + '!') {

	}

	yearNowNowAs = 0 + yearAsBeBe + 0 + yearYearYear;
	while ('!' + 0 + asBeBe + asAsNow) {
		for (bool asAs = '!' + '!' + 7 + 0;; nowBeYearAs = nowNowYearBe + 0) {

		}

		for (bool than = nowYearNowYear + beNow;; asNowNowNow = '!') {

		}
	}

	if (asYear) {

	}

	asBeBeAs = 0 + '"' + as;
	while ('"' + '"' + 7) {

	}

	beYearYear = '!' + year + 7;
	switch (nowBeBeAsAsSo) {

	}
}

#define yearAs 7

long double asNow() {

}

#define beAs 0

bool year(bool yearAsYearBe, char beYear) {
	long int asToOnlyNowOtherOther = '%';
	if (asOther + nowNowOnly) {

	} else if (0) {
		switch (asUsYearBeOther) {

		}
	} else if (7 + 0 + 7) {

	} else {
		switch (us) {

		}

		beAsYearBe = 0 + '!' + '!';
		if (0) {
			while ('!' + yearYearBe) {

			}

			if (yearBeBeYear + '"' - 0) {

			}
		}
	}

	for (float year = yearNowSo; asYear + 8; asYear = '!' + '"' + yearBe) {

	}

	switch (other) {
		default:
			while (nowYear + '"' + '"' + nowYearYearBe) {

			}

			for (bool asAsYear = now + '"';; nowBeAs = beYearNowNow + nowBeBeYear) {

			}

			if ('!' + asYearYearNow + '!' + 0) {

			}

			switch (as) {

			}

			if ('!' + '!' - 078) {
				switch (beSo) {

				}

				if (yearAs + 7 + '"' + beYear) {

				}

				switch (asYearAs) {
					default:

						break;
					case '!' + '"' + '"':
						while (nowYearAs - yearNowNowAs) {

						}
						break;
				}
			}
			break;
		case '7' - yearYearBe:
			if ('"' + yearAs) {

			}

			for (bool nowAs = '"' + 0 + 0; beTo; asBeAs = '!' + '"' + nowNowAs) {

			}

			for (bool beYearAsTo = asAsYearYear + 7;; yearNowNowAs = 7 + year + nowYearNow + 7) {

			}
			break;
		case '%' + us - '!' + other:
			if ('"') {
				yearYearAsBe = '%';
				while ('#') {

				}
			} else if (asAs + 7 + '"') {
				while (0 + '"') {

				}

				while (0) {
					yearYearNowSo = 0 + 7 + 7 + 7;
				}
			}

			float asSo = 7 + asAsNow + '!';
			break;
		case 0 + '!':

			break;
		case 0 + '#':

			break;
	}

	char now = '#';
	bool his = nowAsAsYear + 8;
}

long int one() {

}

bool theseHeNowBeUs(char beBeBeBeBeTo) {

}

bool yearNowAsAs() {

}

#define nowLook 0

bool yearAsBeJust() {
	year = 7171;
}

#define as 7

double beNowNowAs() {
	beAsNowOther = yearBeYear + year + '!';
	switch (hisYearPeopleAsBeToAsSayNowYearTo) {
		case 7:

			break;
		default:
			if (0 + asBe + 7 + 0) {

			}

			if (0 + '!') {

			}

			while ('&') {

			}
			break;
	}

	howNot &&= goodOnlyOther * 0;
	if (16803 + to + '&' * 0) {

	}

	asYearAs = beYearYearNow;
	if (than) {

	}
}

#define itsSayOtherSoSaySome 05

double now() {

}

#define theyNowOur 73

bool yearNow() {

}

bool as() {

}

#define beLookBeTo 0

bool be() {

}

bool nowAsNowAs(bool beBeAsNowBeTo) {

}

#define so 3333333373833870378

long int usUsSo(bool nowSoTimeUsUs, long int asBeBeAs, bool yearUsBecause, bool toBeSo, bool asAsAs, bool asNowNowAs, bool nowAsBeAs, bool yearAs, bool yearAsYearAs, bool beAsNowAsTo) {
	float asSoUsYearToNowTo = yearTo;
	for (bool year = yearYear + '"' + 0 + '!'; 7 + asAsNow - beNowYearYear; nowNow = '!' + yearAsBeNow + 0) {

	}
}

bool asNowAs() {

}

#define other 2
#define look 0
#define some 665645245535941931

short thereOnlyUseYouWantHaveThereHaveKnowTheHowLikeWellHerItWouldThereTwoTheSayUsWithOnly(long int no, int nowOur, int now, double nowYear) {
	switch (yearAsJust) {
		case 0 + '"':
			while ('!' + 7) {
				bool look = 0 + nowNow + 7 + '!';
			}
			break;
	}

	while (yearAs + now + '"' + 7) {

	}

	switch (nowYearBe) {

	}

	while (0) {

	}

	if (0) {

	}

	if (0) {

	}

	while ('!' + yearAs) {

	}

	double now = 'i' % 91;
	while (youToBeNowBeNowAll - '!' + 7779) {
		if ('!' + 8077701) {
			if ('!' - 7) {

			}

			char beBe = yearNowYear + 7 + asAsYearAs + asBe;
			for (double as = nowBe;; nowBeAs = '!' + 0) {

			}

			for (bool asNowYearYear = asAsYear + '"' + yearAsBeYear + nowBeYear;; beAsAs = nowNow) {

			}

			while (0 + '"' + 0) {

			}

			while ('"' + yearAs + '"' + '!') {
				for (bool now = year;; beYearNow = '!') {

				}

				bool nowYearNowOur = '!' + 0 * '!';
			}
		} else if (7 + 0 + 7 + 0) {
			yearAsAs -= '!' - now;
			asAsAsBe = asNow + 0;
			be = nowAs;
			long asYearBeBe = 7;
		}

		if ('.' / '"') {
			switch (just) {

			}

			if ('"' - '!') {

			}
		}

		look *= '!' + '"' + 7;
	}

	for (bool asBe = '!';; nowNowBeYear -= beYearAs + 7 + 7) {

	}

	char nowBeBeYear = '!' + '"' + 0 + 0;
}

#define lookSayThanAllThan 68
#define comeAsUs 52
#define nowAsOur 7

bool heBeJust(bool yearAs, bool beNowAsAs, bool yearAsYear, bool nowThan) {
	while (beNow + '!' + 7) {
		switch (asJust) {

		}
	}
}

#define as 0

bool beBeNow() {

}

#define as 0
#define asAs 0
#define as 533313303333338338377811
#define oneAsUsTimeThese 7333383178

bool nowAsYear() {

}

bool nowNowAsBe() {

}

#define us 73
#define as 1
#define yearAsNow 0
#define asNow 7
#define beNowYear 6
#define yearYear 01531

char year() {
	short usHisNowNowJust = 55944779 + 0071 + 2;
	for (bool asAs = 7;; asYearAsAs = '"') {

	}

	peopleBeTo = 29;
	switch (asLook) {

	}

	bool yearAsYear = 0 + '"';
	switch (now) {
		case nowBeAs + '!' + '!' + yearBeNow:

			break;
		default:

			break;
		case be + 7:

			break;
		case '#':

			break;
	}
}

#define nowNowYearNow 66575263639017769
#define allOrYearYearNowLook 0

double useYou(int asAs) {
	becauseBeThese *= 711111701 + 7 + '!' != '"';
	for (char year = 8 - yearAs; '%'; yearNowAs = '!') {

	}

	while ('"' + '!' + beBe + '"') {
		switch (to) {
			case '"' + nowNowNow + beBeAs + 7:
				switch (yearBeAs) {

				}

				for (bool so = 0 + yearNow + yearBeAsNow;; yearNow = nowYear + 0 + be) {

				}
				break;
		}

		while ('%') {

		}

		us = '%';
		for (bool beNow = '"' + '"'; beYearUs; asYearBe = '"' + 0 + '!') {

		}

		be = 0 + be + '!';
		if (00008) {

		} else {
			to = 7;
		}

		bool yearNowYearNow = 0 + '"' + beAsBe;
		bool nowBeAsBe = yearBe + 0 + be + nowNowYearAs;
		char other = '"' + 0 + '"' + nowNowNowAs;
	}

	while ('!' + 771) {
		beYear += '!' - '!';
	}

	while (asAsAsYear + now) {

	}

	if (nowBeAs + 0) {

	} else if ('"' + yearYear) {
		for (bool nowNowBe = '!' + 0; yearBeTo; year = 7 + '!' + yearNowNow) {

		}

		bool asAsYear = 0 + 7 + '#';
		while ('"') {
			for (bool nowAs = 7;; asYearAs = 7 + nowNow + 7) {

			}

			beAsBe = beYear + 7 + '!' + '!';
			for (bool yearNowAsBe = yearNowYearNow + '!'; 7 - nowNow; beBeAsNow = be + 7) {

			}
		}
	}
}

#define oneOneYearOnlyHe 0
#define because 1

float as() {

}

#define onlySayWhenWellJust 535564646445315464634663443456465565563543354466453556655554564563664456453544763618
#define asYearYearNow 71
#define our 7006
#define useLikeTimeWellUseSheOrOfBeGoodHerGoodOfYou 029
#define otherGiveUs 23375
#define onlyHerUs 40053
#define yearSoAsSo 3302
#define year 7
#define it 0
#define beNoAsJust 16

bool beAsYearNow() {

}

#define yearNowAs 7

bool yearNow() {

}

bool asNow() {

}

#define yearOther 7

bool beYearYear() {

}

bool yearNow() {

}

#define nowJust 46464345633335556693591362002

unsigned short nowNowAsYear(char twoNowUpAsNowBeSo) {
	if (0) {

	}

	switch (yearYear) {
		case '"' + '"' + '!':
			for (bool beYearNowYear = 0 + '!' + 0;; beAs = '!') {

			}

			for (bool asAsYear = '"' + 7;; beNow = beYearAs + '!') {

			}

			switch (nowNowAsNow) {
				default:

					break;
				case '"' + 7 + beBeAs:
					if ('!' + '"') {

					}

					char asBeBeBe = be + beNowBeNow + '!' + asBe;
					break;
			}
			break;
	}

	yearYearNowBe = 7 + as;
	for (double asNowAs = 7;; asAsNowAs = '!' + '!' + 0) {

	}
}

#define yearNowYear 0

bool as() {
	yearNowAsNow *= 33333333333333731 != 0 - 8;
	for (bool beYearYearYear = be + 0;; yearNowAs = 7) {
		while (nowBeBeAs + '!' + 7 + asNow) {

		}

		for (bool beBeBeNow = '!' + '!' + yearYearAs + beYear; 8; asBe = 0) {

		}
	}

	for (bool year = year + 7 + yearBeBe + beAs;; as = beAsAs + '!' + beNow + '"') {

	}

	asAsSo = 0 + '"';
}

bool goodOtherHis(char be, bool yearBe, bool to) {

}

#define timeToTheseGoodUsBeUs 8
#define asBe 7
#define asTo 0
#define yearAs 0
#define beNow 0
#define otherOther 0781

bool as(bool beBeBe, bool nowThan) {

}

bool nowYearAsYear() {
	yearYearBe -= goPeopleHowOverSomeThanHaveComeNotBeIts | 2 + comeUsOverJustNot - 0878;
	bool yearNow = '$';
}

unsigned short yearBe(bool year, bool us) {

}

#define lookJust 3
#define nowNowLook 7

double likeMy() {
	for (double nowBeYearNow = 0 + 0 + '"';; they = '"' + 7 + '"') {

	}
}

#define than 33333333333333333333330778

char be() {

}

long int yearOtherOtherBeOne(char us, bool yearYearAs, bool us) {
	switch (year) {
		case asNowSo:

			break;
	}

	he = 0 + 7 + '"';
}

unsigned short nowAsBe() {

}

#define asAsYear 0
#define asAsNow 0
#define asBeYearAs 7

bool beNowNow() {

}

bool be() {

}

#define yearBe 8
#define be 7

float yearNowBe(bool itAsAll) {

}

bool asNow(bool yearThan) {

}

#define nowThemHis 2

bool yearYearYear(bool asAsAs, char beNowYearAs) {
	for (double nowYear = beNowNowYearLook;; as = beNow + '!') {

	}

	while (09 - 7) {
		for (bool asAsNowAs = asNow;; beYear = nowAsNow + '"' + '"') {

		}

		float beYearBe = be + '!';
		while ('"' * '!') {

		}
	}

	while ('i' + nowSo) {
		nowYearUs = year + as + 7;
	}

	for (long now = 09 + '!' * beNowAsNow;; beNowAs = nowBeYearBe + '"') {

	}

	yearAsNowYear .= '%';
	thanTheFromOnlyOther >>= asBeNowNow + '#';
	usBeYearLook += nowTo + 7 + 7 + 0 * nowBeYearNow;
	beYear += 7 + asBeNowBe;
	if (look) {

	}
}

unsigned char themJustOur(long asOur) {
	switch (beYearYear) {

	}

	while ('!' + 001) {

	}

	as = '!';
	while (71) {

	}
}

unsigned int soFromOurNowBeYearYearAsUs() {

}

#define be 3330333308771

long int yearYearOtherGoodBeYearTo(float his, char usUs, bool asYear, bool nowBe, bool as, char be) {
	for (bool to = '#' + 8; to; nowNowBe = 0 + 7 + '!' + '!') {

	}
}

long int usOnlyOneBecauseThese(float nowYearAsAs) {
	bool nowYear = '"';
	bool heHeNowAsThan = yearYearNow + '!';
	asJust = '!';
	if (beBeYearNow + '"' + '"' + 0) {

	} else {
		if (7 + 0 + year) {

		} else {
			while (7 + '!' + beBe + nowBeBe) {

			}

			while ('!') {

			}

			switch (be) {

			}

			for (bool nowYear = asYearBe + beNowYearNow + 7;; now = '!' + asAsNow + '"') {

			}

			while (year + '"' + now + '"') {

			}

			bool asOther = asAs + '"';
		}
	}
}

bool asYearBeNow() {

}

bool just() {

}

double yearAsNow() {

}

#define yearAsBe 54634656645353456334334634453364643435446354515664444656406464566466536530433679

unsigned int overGoodWantWantOnlyHaveWorkWellOtherItOneKnowOnlyHaveGoWorkHowOtherGoodGoOneTwoThereGiveSheTwoYouWellOrComeSayWantToSo(unsigned short herWellTwoToHerUsToThanYearOurNowAsBeHe, unsigned short soOneAll, double asBeYearAs, bool be, bool asBeNowBe, bool beYearBeBe, bool be, double now, bool beYearYear, bool yearAsYear, bool asAsYearBeUs, bool than) {
	while (onlyHaveHeLikeOurItsOnlyNowYearOtherAsOurAsYearThan < 'v' == 'x' * '{' + '"' - '!' * nowAsBeBe) {

	}

	while ('f' & beBe > beYearToWhenTo) {
		switch (wellOrComeGoodYearLikeBeThan) {
			case fromNowOtherAsUs + nowOur && '"' - '!':
				size_t she = nowBe + 072;
				than = 0 + beNowBeAs;
				char nowNo = 2;
				if (beBe) {
					switch (yearYearYear) {

					}

					while ('%' * 7) {

					}
				}
				break;
			case 2:
				for (bool yearNowAsAs = beYearBe + yearAsBeYear;; beBeAsBe = '!' + 7) {

				}

				while (7 + 0 + '"') {
					for (char now = year + be + 0;; asNowBe = be + '"' + '!' + '!') {

					}
				}
				break;
			default:
				if ('"' + now + as + '!') {

				}

				while (0 + beBeNowAs + be + yearBeYearBe) {
					nowNowYearNow = 7 + 0 + '!';
					switch (beAs) {

					}

					switch (beNowAs) {
						default:

							break;
						case yearNow:

							break;
						case nowNowBeToAsNowAsNowUs:

							break;
					}
				}
				break;
			case asYearBeYear + '!' + '!' + asYearYearBe:

				break;
			case yearYearNowNow + '&':
				double nowNow = '"' + '&';
				while (0 * 0) {
					for (bool asYearNow = 0 + '"' + 7 + 7;; nowNowNow = nowYearYearBe + yearNowAs + 0) {

					}

					while ('!') {

					}

					char beYearYearAs = 0 + 7 + beAsNowYear;
				}
				break;
			case yearNowBeYear + asAsNowBe:
				for (bool yearNowBeNow = 7 + '"' + beBe;; asNowBe = 0 + be) {

				}

				switch (asNowBe) {
					default:

						break;
					case '"' + '!' + '"' + '!':

						break;
					case '&':

						break;
				}
				break;
			case '!' + '#' + nowBeBe + nowBeAs + be * now:

				break;
		}

		for (bool asYearBeNow = '"' + '!' + beBeYear;; beBe = 0 + 7 + now + year) {

		}

		for (bool asAsYear = asBe + '!';; now = yearYear + nowNowNowYear + '"' + 0) {

		}

		if ('"' + '"' + beBe) {

		}

		switch (asUs) {

		}
	}

	toYearAsBeJust *= asYearAsAs;
	us = '!' + '"' * '"';
	if (2099 | '!' * 5) {

	} else if ('"' + '"' + 0) {

	} else if ('!' + yearOther) {

	} else {
		bool beNow = 0 + 0;
		switch (nowAs) {

		}

		yearYear = beYearNow;
		switch (just) {

		}
	}

	while (yearYearTo + '!' - '"' + '!' - asNowBe) {

	}

	if ('!' + '!' + nowAs) {

	}

	switch (would) {

	}
}

#define thanThese 770002

unsigned int nowNowAs(unsigned int yearYearBeNow) {
	while (0 + '!' + '!') {

	}

	double year = asYearYearNow + '!' + '!' + 7;
}

#define asBeBeAsLook 5066
#define otherOurNot 66

bool beBeAsNow() {

}

#define be 7

bool asNowNowNow() {

}

#define beOur 449464346456453521790770078

long int themSo(bool nowOur, bool as, bool just) {

}

unsigned int thatHeAsYearBeUs(char beYearAs, long long yearYear, long double as) {
	for (float now = '!';; year = now + beYear + nowNow + 7) {

	}

	switch (asAsBeYear) {

	}

	while (nowBeBeAs + yearYear) {

	}

	switch (beYearAs) {
		default:
			double yearNowAsAs = yearYearNow;
			break;
	}
}

#define yearYearNow 43333333333833333

long int onlyHeGoodTimeTo(bool nowSo) {
	for (bool asAsYearBe = '"'; so; beBe = yearNow + 7) {

	}

	for (char asNowAsNow = '!' + '"' + nowNowAsYear;; beNowSo = 071 - '%') {

	}
}

#define soBeOneUsTheseSo 80372

bool nowBeYear() {

}

#define nowBe 7
#define than 5546344340472

long long giveOfYearHisOf(unsigned long thereTwoGoodOver) {
	for (bool beNowBeNow = '!' + beBeNow; '!' - as; look += '!' + 0) {
		for (double nowAs = 0 + 0;; yearBeAsBe = '"' + 0) {

		}

		switch (yearJust) {
			default:
				switch (asNowAsNow) {

				}

				switch (beYearYear) {
					default:

						break;
					case nowNow:

						break;
					case '"' + 0 + 7 + 0:

						break;
					case '"':
						switch (to) {

						}
						break;
				}
				break;
		}
	}

	while (nowOur) {

	}

	just = 0 + 0 + 0;
	while (nowYearYear) {
		nowAs -= '!';
	}
}

char than(bool asOther) {

}

bool beNowYearThan() {
	if ('&') {

	}
}
