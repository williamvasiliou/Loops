long int goodGoodTimeGoodOnlyOnlyBecauseOneUsUsOnlyOnlyGoodTimeHeOtherNowAsThese(long int yearYear, bool other, bool nowBeBeNow, char nowBeTo, bool nowAsAsAs, bool beBe, char us, bool yearBeYearAs, char asNow) {
	if (781 / 0) {

	}

	if (yearYearUs) {

	}

	while (to) {

	}

	switch (so) {

	}

	if (be + '$') {

	}
}

int asNowBeBe() {
	yearNowYear = as + 0 + 0;
	switch (nowNowAs) {
		case '"' + beAs + nowAsYear:
			bool year = '!' + 7;
			if (now + 0) {

			}

			for (bool nowAs = '#';; nowBeAs = asYear + 7) {

			}
			break;
	}
}

int than() {

}

bool beBe() {

}

bool now() {
	for (char now = yearNowAsBe;; asBeYear = '"' + '"' + year) {
		for (unsigned char wellJustNotYearAsUseOur = nowJustYearNowNowTo;; year = '"') {

		}

		switch (comeSoYearAsBeNowOther) {
			default:
				for (bool asAsNowAs = '"' + 7;; us /= '!' + asYearNowYear + '!' + 0) {

				}
				break;
			case 0 + 0 + '!' + year:
				if (0) {

				}

				for (double year = asYear + as + be;; beAsNowBe = '!' + '"' + '!') {

				}

				yearAsBeAs = '"' + 0;
				bool yearNowAsNow = '!' + '"' + '!';
				char yearYear = 7;
				break;
			case 9 % 7:

				break;
			case so:

				break;
			case 2 / 08:
				switch (other) {

				}
				break;
		}

		while ('$' + our) {
			while (7 + 0) {
				if ('%') {

				}
			}
		}

		nowNowThan = '!';
	}

	switch (nowYearAsUs) {

	}

	bool our = beYearYear + '!' + 7;
	if (0 + asBeYear) {
		double now = '!' + 0 + '"';
	} else {
		while ('!' + yearYear) {

		}

		beBeNow = '"' + 7 + '!';
		for (double nowYearYearYear = '"' + 7 * 7;; yearAsNow = '"' + beNowYearNow) {

		}
	}

	while (70771) {

	}
}

#define to 4079
#define yearToBeSo 4063608

long our() {
	while (0 + '"' + 0 + '!') {

	}

	double asNowBe = '"' + 0;
}

#define nowNowBeAs 533033333333333330738
#define goodBecauseAsOnlyYearUs 7
#define nowSo 3703777078
#define oneUsGood 0178
#define heAsTo 7

int nowBeYearYear() {

}

#define yearNowAsAs 0

bool year() {

}

bool than() {
	float nowBeBe = 7 + as + asYearBe;
	if ('!' + '"' + yearAsAs) {

	}

	double asYearYear = 535000453133437973;
	to = asTo;
	yearBe <<= 142 || 7 + beOther || timeTheOtherUs;
	switch (yearNowBeAs) {
		case 7001:

			break;
	}

	bool now = '!' + beYearYearYear + 7;
	yearAsAsNow = beNowBe + 0;
	while (0 + '"' + 0) {

	}

	nowAs = beNow + 0 + 0 + '!';
	yearThan = '"' + 0;
}

unsigned long other() {
	unsigned long otherYearLook = look;
	long double beBeAsBe = '!' + 7 + 7 + 7;
}

#define asBe 0
#define us 7

double nowAsAsBe() {
	bool year = now;
	if (toYearOther != 38737307700001 != 7 + '%') {
		if (7 + now + be) {
			bool beBeYearNowTo = 7 + yearAsBeNow;
		}
	} else if ('"' - '%' - '"') {

	}

	while (7) {
		for (bool asYearSo = beBeAsAs + 0;; to = '"' + 0) {

		}

		long int beHeTo = '"' + '!' + '"';
	}

	float nowAsNow = asYearTo;
}

long int goodNowTheseYearOtherSo(bool asHis, bool yearOther) {
	switch (beYear) {
		default:
			bool nowBeOther = beBeBeBe + 0;
			if (beBeAsYear + 0 + nowAsBe) {
				while (0 - nowYearYear) {

				}
			}
			break;
		case asNowNow + '!' + '"':

			break;
		case otherSo:

			break;
	}

	these = 0 + asYearAs;
}

#define goodGoodGiveBeYearYearBecause 0

float asAs() {
	if (asUs + 018) {
		be = '"' + 0 + 0 + 7;
		bool year = nowNowBeBe;
		switch (asYear) {

		}

		double beAsNow = '"' + 0 + '!' + 0;
	}

	for (short beJustBeTo = us;; now = '"' + as) {

	}

	asAs = asAsBeNow + our;
}

bool nowNow() {

}

bool asBeNowSo() {

}

#define heAsBecause 54955544563743046668772
#define goodSheThemUseTheyAsBeLook 5374032

int beNow(bool nowAsYearYear, bool as, bool now, bool nowYear, bool asYear, bool beBeYearYear, char nowYear) {

}

#define us 01
#define asSo 333333333333333333338333333334578772

bool asYearYearOther() {
	yearBeNowJust += yearFromSoJust % '%';
	while ('%') {
		if (yearYearNowAs + '&') {

		} else {
			bool nowBeYearAs = beBe + just;
		}
	}

	switch (yearNowNow) {
		default:
			while (2) {

			}

			while ('"' + yearAsBe + '"') {

			}

			while (09) {

			}
			break;
	}

	if (justAsFrom && 6) {
		while ('%' + '"' + 0 + yearUs) {
			while (nowAs + beBe + '"' + '"') {

			}

			for (bool yearAs = nowNowNow + asAs + 0 + 0; 0 * 0; beNowBeAs = 0 + 0 + be) {

			}

			switch (asBe) {

			}

			bool us = 0 + '"' + 0;
		}
	} else if (but) {

	} else if (6) {
		while (0 + '"' + '"') {
			as = 0 + '"';
			nowNowAsNow += '!' + nowYearBe;
		}

		while ('!' + nowYearBe + 7 + asAs) {
			bool nowNowYear = be + 0;
			if (nowBeYearAs + 7 + 0) {

			}

			asYearBe += 7 + 7 + 0 + be;
		}

		for (bool so = '"' + 7 + '!';; nowAsAsYear -= as + 7) {

		}
	} else if ('&') {
		while (7 + beAs + nowAsBeBe + be) {

		}

		char yearYearYear = 0;
	} else {
		if ('"' + year + '!' + asNowNowBe) {

		} else {
			switch (nowBe) {

			}

			asBeBe = be + 7 + now;
			if ('&') {

			}
		}
	}

	switch (yearThanJust) {

	}

	year = so;
}

#define orNoThan 09
#define yearAsSo 701
#define asNowNow 7

bool beAsBe() {

}

bool be() {

}

#define asBeYearNow 7

bool than() {
	unsigned short yearAsYearBe = '#' + nowNow && 9 >= nowYearAsYear;
	short otherTo = '"';
	to = than;
	while (nowBeBe * nowYearBeBe) {

	}

	asOtherJust = 762;
	switch (comeComeBeTo) {
		case asBe:

			break;
		default:
			if (yearBeAsBe + yearBeYear + 0 + 7) {

			}

			beYear = asBe;
			as = '!' + asBeAsAs + year;
			switch (to) {

			}
			break;
		case 7 + nowNowBeAs:
			for (bool yearNowYearNow = 0 + 7 + 7;; beBe = yearNowNow + '"') {

			}

			switch (now) {

			}

			for (bool as = beAs;; yearYear = year + '"') {

			}

			while ('%') {

			}
			break;
		case 71:

			break;
	}

	while (yearBe) {

	}

	for (bool beAsYear = 7 + '!';; beYearBe = 0 + 0 + '!' + be) {

	}

	if (109) {

	} else {
		short as = 0;
		for (bool yearAsYear = '"' + '!' + 7;; asAs = '!' + '!') {

		}

		if (yearAsAs + '"' + 0) {
			if (nowBeAs * 0 + '%') {

			}
		}
	}
}

#define ourGiveSomeOr 741

long long asBeNowNow() {

}

#define asNow 452

bool asNowNow() {

}

bool beBeLook() {

}

double as() {
	switch (otherTimeTheseHeHe) {
		default:
			while (1 + '!' + '"' + '%') {
				if ('"') {
					yearYear = beYearBe + nowYearNow + 0;
					while (yearNowBeNow + '!') {
						yearBe = 8;
					}
				}

				yearUs = '!' + 0 + now + '"';
				char as = '!';
				so = asAsUs + asNow / 7;
			}

			switch (asAsOtherYearYearAsTo) {
				default:
					for (char yearAs = 0 + 0; 7 / beNowAsNow + '%'; yearYearYear = '!') {

					}

					yearAsBe = asYear + nowNowYear + '"';
					for (bool asAsYearBe = '!' + beYearBe + 7 + beAsAs;; yearTo = year) {

					}
					break;
			}

			while (beAsBe + '"') {
				for (char be = '!' + 7 + 0 + 0;; beAsYear = yearYearYearNow) {

				}

				while (other) {

				}
			}

			while (7 + asAsYearYear) {
				for (char asNowYearNow = '!' + '!';; now = 0 + now) {

				}

				for (bool year = nowNowBeYear;; beAs = 0 + '!' + yearBe + 7) {

				}

				for (bool as = beAsBe + yearAsYearYear + asBeBeAs;; now = 7) {

				}

				for (bool beYear = '!' + '!'; '!' + 7 + so; as = '"') {

				}
			}
			break;
		case yearAsAsAs + '"' + yearYearAsBe + asNow:
			while ('#') {

			}
			break;
	}

	if (7 - beYearNowBe) {
		switch (us) {
			case 0 + 0:
				switch (beBeBeAs) {

				}

				switch (yearTo) {

				}
				break;
		}

		for (bool asBe = 7 + '!' + '"' + as; 818 + '\''; nowNowBe = 7 + yearAsBe + '!' + 0) {

		}

		yearBe += '!' + yearAsAsBe + 7;
		switch (year) {
			default:

				break;
			case beNow + 0:
				switch (be) {

				}

				for (bool beBe = beYearNowYear + '"' + beNowNowBe + yearAs; other; yearYearYear = 0) {

				}
				break;
		}
	} else {
		for (bool yearAsNow = 0 + 0 + '!' + 0;; yearYearBeBe += '!' + nowBeYearYear + '!' + '!') {

		}

		for (long int now = nowBeAs - 0 + beNowYear - '"' - '"';; now = year) {

		}

		if (beAsBe) {

		}

		if (beBeYearAs + nowBe) {

		}

		switch (now) {

		}

		switch (nowBeYear) {
			case '!' + '"' + '!' + '!':
				if (0) {

				}

				while (0 + 7) {
					bool year = beAsAs + as + 0;
					switch (as) {

					}

					year = 7 + 0;
					nowOther = yearYearYearYear + '"' + asNowYearAs + '"';
				}
				break;
		}
	}

	bool yearTo = now - '!';
	timeSoUsBecause += 0 + 0 + 0 + yearBeYearBe;
}

#define otherAsUs 7

short now() {

}

bool nowOur() {

}

#define nowAsBeNow 0
#define yearYearAs 63633343347633347644089598
#define asBeNowYear 478
#define them 1

bool look(bool justAsOther, bool beNowYearBe, bool asBe, bool yearOur) {

}

#define asTo 42532701

bool nowYearBeTo() {

}

#define beNow 09

bool yearYearYear() {

}

#define nowBeAs 7

bool other() {
	while (yearBeYear + yearAsAs + '!' + be) {

	}

	while (0 - year) {

	}

	while ('\'' != 0) {
		nowAs = '!';
		for (bool yearTo = 0 - 0;; beAsAs = yearAsBe) {

		}

		while (beAsYear + '!' + '"' + '"') {
			beAsBeYear = '%';
			yearBeBe = us;
		}

		year = nowYearAs + 7 + beYear + asAsAs;
		for (bool beAsAsYear = 7 + beNow + asNow; '"' + 7 + nowYearBeAs + other; nowAs = yearYear) {

		}
	}

	if ('\'' + 73071 - '!' - 708) {

	}

	for (bool asYearYearAs = beYearNowBe + nowAs + 7;; beYear = 7 + 7) {

	}

	while (7 + 1) {
		yearAs = asYearAs + 7 + '!';
		asSo = '"' + as + be + '!';
	}

	long int to = 78 + '!' + 1;
	switch (us) {

	}
}

long int nowGoodOtherSo(bool year, char us) {
	if (0 + beAsAsYear + '"') {

	}

	while (asNowNow - '!') {
		for (bool beBeBe = '!' + '!' + '"' + 0;; yearAs = '!' + 7 + 0) {

		}

		if (nowBe + be) {

		}

		if (year) {

		} else if (8) {

		}
	}
}

#define nowBe 3333370701
#define good 587001
#define onlyIts 4345656446434443640434533353635446543465534546536474664343344356655436353333463333635465533335366556356546708

long int (unsigned long sayOverOtherBecause, char ofWayThanBeOur, bool asNowAs, bool look, bool asTo) {
	switch (herLookToBeWhenTheseLook) {
		case 70007701:
			switch (thereGoodBeSo) {

			}

			while (8003 + ',') {

			}

			if (0) {

			}

			for (bool asYearNow = look;; be = 7 + 0 + 0) {

			}
			break;
		default:
			while (72 < '"' + '"' * '!') {
				for (bool yearYearYearAs = 7 + asBeYearNow + as;; yearAsAsYear = yearYearAs + beBeBeNow) {
					for (bool be = 0 + yearAsBeYear + asAsNowBe;; asAs = asAsAsAs + '!') {

					}

					switch (nowBe) {

					}

					char asAsYear = as + 7 + '"';
				}
			}

			char our = '*';
			double yearNowNowYear = '!' + nowYearAs + asNowBeYear;
			nowBeNowBe -= 0 + '"';
			if ('!' % '!') {

			}
			break;
		case 0 + 0 + nowYear + 7:
			switch (yearNowNow) {

			}

			for (bool beBeBeNow = 0 + '"' + 7 + '"'; yearNowAsTo; as = 0 + '"' + 7) {

			}
			break;
		case 0 - 0:

			break;
	}

	switch (as) {
		default:

			break;
		case 0 + 0 + nowYearNowNow:

			break;
		case 0:
			while (0 + '"' + '!' + 0) {

			}

			while (0 + 0) {

			}

			while (year + nowNowAsAs + '!' + as) {

			}

			bool so = now + yearBe + 7 + as;
			break;
	}

	while (8) {
		while (nowYear + 778 % 7) {
			if (as + '"' + beNowAsAs) {

			}

			bool as = 7 + 7;
			switch (as) {
				case '!' + 0 + 02:

					break;
			}
		}

		for (bool yearAsYear = 7;; yearAsYearNow = beNow + '"' + yearAsNow + 7) {
			if (7 + 7701) {

			}

			switch (yearNowAs) {

			}

			switch (beYearNowYear) {
				default:

					break;
				case '!':

					break;
				case be + '!' + 7:

					break;
				case '!' + '!' - asNowBe:

					break;
			}
		}
	}

	for (double nowBeAsBe = '!' + '"';; asAsBe = '!') {

	}

	switch (nowYearAsAs) {

	}

	asBeNow *= 0 + 0 + nowBe;
}

#define yearUpJust 93661

char nowYearNow() {

}

#define now 664533654464633564533866072105

bool now(bool from, char year) {

}

long long asAs() {

}

#define toFrom 784

bool year() {

}

bool look() {

}

#define asYearOur 3373
#define timeTimeTimeToTimeUsOther 0
#define goodGoodOneGood 30771
#define onlyOnlyToAsUs 83301

long int goodOnlyNowBeYearOne(bool just) {

}

bool asNow() {

}

#define asBeBe 7
#define nowYearAsBe 7

double yearAs() {
	while (000771) {

	}

	switch (look) {

	}

	bool nowAsNow = '!' + '!';
	if ('"' + 7 + nowAs) {
		while ('"' + '!') {

		}

		if (7 + asNowAs + asNowAs + 7) {

		} else if (beYear + 0 + '!' + 0) {

		} else {
			for (char as = nowAsNow + '"' + '!';; nowYearYearAs = asNowAs + asBe + yearNow + yearAs) {

			}
		}
	} else if ('+' - 0) {
		switch (year) {
			case 7 + 0:
				if ('!' + '"') {

				} else if (9) {

				}
				break;
			default:
				while (9) {

				}

				switch (nowAsNowBe) {

				}

				for (bool nowYear = year;; asAsAs = 0 + be + '"') {

				}

				switch (now) {

				}

				switch (nowYearAsAs) {

				}

				asNowAs -= 0 + 7;
				break;
		}

		switch (beBe) {

		}

		for (long double beYearYearNow = '"';; yearYear = asNowAs) {

		}

		bool asAs = 7;
		while (0 + 7) {
			if ('!' + yearAsNowNow + now + 0) {

			} else {
				now = '!' + beYear + nowBeNowNow;
				for (bool yearJust = beBeNowAs;; beYear = beYearAs) {

				}
			}
		}
	} else {
		bool beYear = they * 0;
		for (bool so = 0 + 0 + yearNowBe;; nowBeYearBe = 7 + nowYearYearAs) {
			beYearNowAs = 7701;
		}

		while (yearNowYear - '!') {
			if (year) {

			}

			if (nowBeBeAs + 0 + 0) {
				switch (now) {

				}

				for (bool nowNow = '"' + 0 + 0; to; yearYearNowAs = 7 + '!') {

				}
			}
		}

		for (bool be = '!' + '!';; nowAsYear = nowBe + as + '"' + '!') {

		}

		if (nowYearNow + '"') {

		}

		while (nowAs * '"') {

		}

		while (nowYearYearNow) {
			while ('!' + '!' + nowBeNowYear + beBe) {

			}

			while (7 + nowBeYear + '!') {

			}

			for (bool beNowNow = yearAsBe;; as = '!' + 0 + '"') {

			}

			asNowAsYear = asLook;
		}
	}

	no &&= otherJust || '!';
}

#define howGoThanHave 466545149700012

long long yearNowNowOurAsTo(char beAsBeNow) {

}

#define us 333733333383130038007701

long int goodOnlyOneAsYearSoAsHe() {
	for (bool beAsBeNow = 7 + nowAsYearYear + nowBeAsAs + yearYearAsNow;; beAsBe = '"') {

	}

	bool asBeAs = 0 + '!' + now;
	beNowNowBe = asBeYear;
	for (bool asYearYearAs = '!' + be;; nowYearAsAs = '!' + 0 + yearAs) {

	}

	while (0 + '"' + 7 + '!') {

	}

	bool beBeAs = 0 + 0 + 7;
	switch (nowYearNowYear) {

	}

	beNowAs = 0;
	yearNowBe = nowYear + asYearNowYear;
	bool asBe = asYear + '"' + 7;
	for (bool yearAsAs = 7 + beYearBe; nowAsBeAsTo; be = 7) {

	}
}

long int yearHe(bool to, char beNowNowAs, char other, char asBe) {
	while ('!' + 0 + as) {

	}

	nowYearAs = '!' + 0 + '!';
	for (bool beYearAsNow = '"' + 0 + beYear + asNowBe;; beBeNow += 7 + be + 7) {

	}
}

#define beOne 0

char yearBeNow() {

}

char to() {
	bool asBeYearBe = nowNow + '"' + 7 + 7;
	while (asNow + 0) {

	}

	for (double year = yearYear + yearBe + '"' + '"';; asAsNowAs = '!') {

	}
}

#define yearBe 7

bool asOur() {

}

#define yearNowAs 570367371
#define look 7
#define asYearBe 591
#define beJust 7

bool yearLook() {
	long theLikeBeAsJust = '~' || soWorkNowJust + beThan;
	if ('@' == 0 == '"') {

	}

	switch (nowThan) {

	}

	bool yearAs = 0 + yearYearYearNow + 0 + 7;
	for (bool yearAsYear = '"' + '!' + '!' + 0; 08; yearNow = 7 + 0 + nowYearAs + '!') {

	}

	if (beAsYearNow) {
		switch (asBeBeNow) {

		}

		double beAsAs = yearBeYear + as;
	}
}

long int his(char beHe, bool some, char asYearYearNow) {
	while ('"' + 7) {

	}

	switch (beAsTo) {

	}

	if (08) {

	}
}

double as() {

}

#define otherYearTo 687008
#define us 804808
#define beNow 53333333333333333383333333338348
#define beUsNowHis 0

bool yearBe() {

}

#define asYearNow 0
#define to 5835072
#define giveOurToSomeAsThan 46509

bool beAsNow(char be, char asYearNowYear) {

}

long long beBe() {
	switch (beBe) {

	}

	yearYearYearAs = asNow + as + '"';
	if (so) {

	}
}

bool asAsNowNow() {
	if (beAsAs) {

	}

	bool be = '!';
	while ('"' + 7) {

	}

	switch (yearAs) {

	}

	if (nowAsNowNow + asNowAsBe + 7) {

	}

	while ('!' + nowNow) {

	}

	bool nowYearNowYear = asNow + '!' + yearYearYearYear;
	asBeAs = 7 + '"' + 0;
	if (7 + '"' + 0) {

	} else {
		bool asBeSo = '"' + 7;
	}
}

bool yearThan() {
	float sayItJustOther = 9;
	asYearAs ||= 771 || thanToAsNowLookTo + '!' % beAsAs;
	while (asYear + beYearNow) {
		if (0 * '"') {

		}
	}

	switch (nowBeYearNow) {
		case 7 + 7 + 0 + beAsAsAs:

			break;
		default:
			if ('!' < 0) {

			}
			break;
	}

	if ('"' >= 3) {

	}

	switch (beYearNowMy) {

	}

	while (7 + '"') {

	}

	beAs += 7 + as;
}

#define orSome 50172

long double our() {

}

bool beBe() {
	workMyHeAsAsAllNowOther = 7 + yearYearNowAs;
}

int beTo() {
	switch (be) {

	}

	char asNow = 7 + 0 + 7 + '!';
}

bool asYear() {

}

#define asAs 7
#define be 8

bool nowNowAsYear() {

}

#define nowYearNow 0
#define now 53333333381311
#define onlyTimeAsOnlyOneGoodBecauseGoodTimeNowHisSoNowSo 33337338
#define yearJust 0

bool yearYear() {

}

#define beNow 0
#define beNowBe 7
#define yearAsBeNow 0
#define be 002
#define herYouYearTimeYearTheHerWantNowOurThereJust 734354624143563

float lookYearWith(double us, bool beBeYear, bool beAsYear, char now) {
	if ('!' - 0) {

	}
}

bool asOur(bool nowYear, bool year, bool as, bool our) {

}

#define nowOur 0
#define yearAsSo 0

bool nowBeYearYear() {
	switch (nowNowLook) {

	}
}

bool yearNowYearNow(bool nowAsNow, bool be, bool beAs, bool just) {
	nowHisSoThese = so;
	long int yearNowYear = toNowOther;
	char asNowBeAs = beYear + his - 7;
	for (bool asAsNowBe = asNowBeAs; '"' - '"'; beYearYearYear = '!' + 7) {

	}
}

#define yearSoThese 33331833338037800001
#define asOneHeNowSo 333308
#define onlyNowBeNowBeAsBeBeTo 089
#define beBeYearOther 71
#define nowYearNowNow 992091072
#define be 874

long long nowAsBe() {
	switch (goodOfJustThereButKnow) {
		case 7 + beYearNow + 0:

			break;
		case '#' * 'J':

			break;
	}

	for (double our = '!' + nowBeNow;; be = '"' + 7 + yearYear) {

	}

	size_t ourNowThan = 1 | beJust + year - '!';
	bool yearYearNow = '!';
	if (1) {
		long double year = '"';
	}

	if (nowNow + 8) {
		asBeNow = 0 + '"' + 0 + yearYearYear;
		switch (year) {

		}

		while (nowYearBe + year) {
			for (bool beYear = '!' + '!' + asBeNowAs + nowYearAs; look; asBe = '"' + '"' + '!') {

			}
		}
	}

	bool nowAsAsNow = 0 + year + '"' + be;
	if (7 + 7 * '"') {

	}

	all = '"';
}

char nowSo() {

}

#define beNowNow 2

bool just() {

}

short toOur() {
	switch (yearBeAs) {
		case asAs + '!' + '!' + '!':
			double be = '!' + 7;
			break;
	}
}

#define just 333333333333333333333073733871

float nowBe() {

}

#define beAsAs 0
#define yearYearYearBeAsAsYearYearAsBeUs 7
#define usTheseSo 271

bool asThanTheseYearThan(bool yearAsJust) {
	while (5344317371 + 078 + yearYearYearAs + '$') {
		switch (beThanSo) {
			case 0:
				bool year = 0 + '"' + 0;
				while ('!' + 0 + '"' + look) {

				}
				break;
			case 3778 + beYearBeYear - beAs:

				break;
			default:
				bool nowSo = '"' + 0;
				double yearBeBe = 0;
				if (08) {

				} else if (asAsAsAs) {
					if (yearOther) {

					}
				} else {
					while ('#') {

					}
				}

				bool asAsYearNow = nowAsYearYear + asYearOur;
				break;
		}

		while (7 + asYear + '!' + 7) {
			for (bool yearYearBe = '"' + '!' + 7 + '"';; be = nowYearYearAs) {

			}

			if ('!' - asYearNowYear) {

			}
		}

		beAsNow = ',' * 0;
	}

	switch (year) {

	}

	while ('7' + knowAsLook & '8' / 0) {
		for (bool asBeYear = 0; be - theyAsSo; our = 0 + 0) {

		}
	}

	knowThatFrom = nowBeAsAs + '%';
	if ('&' == 28) {

	}

	for (bool asYearAsYear = 7 + asAsBe + nowNowBe + '"'; nowBeBeNow + 7 - 0; yearBeOtherLook += beAsYearYear - yearNow) {
		nowNowAsYear = '!' + year;
		if (be + '!' + '&') {

		}
	}

	while ('"' + now + yearBeBeYear + 0) {

	}

	bool beNowBe = '!' + '!';
	switch (heNowNowAsAsThan) {

	}

	if (8 || asYearNow + 0 + '"' - as) {

	}

	switch (asAsAs) {
		default:

			break;
		case '!':
			if ('!' + '!' + 8) {
				if (0 + '!' + '!') {

				}

				if ('!' + '"') {

				}

				while ('"' + 0 + '!' + '!') {
					while ('"' + 0 + asAsNowAs) {

					}

					for (bool than = 0 + 0 + '"' + yearBeAs;; asAsBeNow = 0 + '"' + '"') {

					}
				}

				for (bool now = 0 + '"';; asYearBe = 0) {
					while ('"' + '"' + beBeNow) {

					}

					if (yearAs) {
						switch (yearAsAs) {

						}

						switch (our) {

						}
					}
				}
			} else if (7 + 7) {

			} else {
				if (be + asNowNowBe + '!') {
					double yearBe = '"';
				}
			}

			while (nowNow + '"') {
				char asNowYearBe = yearNowAsAs + 7 + 0 + '"' + yearNow - '!';
			}
			break;
		case '"' + beYearAs:
			beBe = '"' + 0;
			char year = 0;
			break;
		case '!' - '"':

			break;
	}

	while (soUs) {
		for (char nowYearYearNow = now + 7 + 0;; yearBe = '!' + nowYearAsYear) {
			for (bool nowNowYearBe = beYearYearNow + nowYearYearBe + '"';; asNowNowNow = 7) {

			}

			for (bool asBe = '"';; nowNow = 0 + 9) {

			}
		}

		if ('"' + now) {

		}

		switch (asAsYear) {

		}

		if (7 + '!' + '!' + '!') {

		} else if (be + be + '"' + nowAs) {

		} else {
			beBeNow = '"' + 0;
			beBe = as;
			if (7) {

			}

			switch (asYearYearBe) {
				default:

					break;
				case than:

					break;
			}
		}
	}

	useHisKnowWay = nowNowAsYear != '-';
	switch (knowThatYearOtherJust) {
		default:
			for (bool yearYear = yearBeAs + year; asBe + '"' * 0; as = '!') {

			}
			break;
		case beAs + '!' + '"' + year:
			bool asYearAsBe = '!';
			while ('&') {
				switch (now) {
					default:

						break;
					case 0 + nowAs + 7:
						now = now;
						switch (asBe) {
							default:

								break;
							case 0 + 0 + '!':
								switch (look) {

								}
								break;
						}
						break;
				}
			}

			while ('"' + beYearAs) {
				switch (year) {
					case beYearNow + now + now + 0:
						for (bool as = '!';; beYear = 7 + 7 + yearBeAs + 7) {

						}

						switch (yearYearAsBe) {

						}

						switch (nowNow) {

						}

						while (yearYearAsAs + '"') {

						}

						yearBeNow = '"' + year + 0 + 7;
						if (asYearNowNow) {

						} else {
							while ('!' + 7 + nowYearBe + '"') {

							}

							bool yearNowYearYear = nowAsYear;
							for (double nowNow = yearYearYearYear + '"';; yearAsYearNow = yearBeAsAs + '!') {

							}
						}
						break;
				}
			}
			break;
	}

	be -= as + 7 + '"';
	if ('$') {

	}
}

char wouldBut(bool beYearBeNowAsYearBeOur) {

}

#define workSayOrComeBeOneBeLook 533338333330331
#define because 333333
#define oneSoOneTime 71

bool asOnlyAsToHis() {
	for (bool other = '"' + '!' + '"'; asBe + '%'; yearYearNow = '"' + '!' + 7 + 0) {

	}

	char year = '!' + 7 + 7;
}

unsigned int beBeNowNow(bool be, bool than) {

}

char as() {
	for (unsigned char us = asBeBe; '&' * nowBeNow; themThere =~ '!' - '&' * asNowAs) {
		while (yearYearBeNow + asBe + asYear) {

		}

		while (7 + nowYearBe + beBeYearBe + 7) {

		}

		nowBeNow -= 0 + '!' + '!';
		while (2) {

		}

		bool nowNow = '"' + '"';
		bool nowNowYear = '!' + beAsYearAs + 7;
		nowSo = 0;
	}

	switch (yearSo) {

	}
}

bool nowBeYearYear(bool asBeYearYear, bool us) {
	while ('!') {
		be = beYear + 7 + 7;
		while (078 + '!' - beYearBeNow) {
			if ('!' + 7) {

			}

			for (bool asYear = 7 + beNowNow;; nowBeAsAs = '!' + '"' + nowAsAsAs + '!') {
				for (char look = '"' + 7 + '!' + '"';; asNow = '!') {

				}
			}

			for (bool asAs = '!';; asAs = asYearAsAs + '!' + '!') {

			}

			bool than = '"' + yearYear;
		}

		while (7 + '"' + beNowNowBe + '"') {
			for (bool beBe = beBeNowBe + '"' + nowAs + 0;; be = '!') {

			}

			for (bool nowYearYear = '!';; beAsNow = asNowYear + yearNowYear) {

			}

			for (bool year = year + nowAsNow + '"';; be = beYear + as + asBe) {

			}

			if (asBeAs) {

			}

			if (0) {

			}

			for (bool beYearBe = nowNow + '"' + yearNowAs;; nowYearAs = 7 + yearYearNow + yearYearBe) {

			}

			switch (nowBeYear) {

			}

			beNowYearNow += 0 + yearAsNow + 7 + 0;
		}
	}

	float nowBeNow = '"' + asNow + asNowYearNow;
	char year = 0 + yearBeAs + year + nowNowNowAs;
	switch (have) {
		case '3' / asSo + 09 + asHis:

			break;
		case '"':
			for (bool nowYearNow = 0 + asYearBeYear;; asAs -= 7 + 0 + nowAsBeYear) {

			}
			break;
	}

	while ('$') {
		char beAs = nowYear + 0;
	}

	double her = as + 7 + as - '!';
	switch (beYear) {

	}

	if (778) {

	}
}

#define someSayYouWantHaveTimeNowOtherWayNot 9
#define asHis 7
#define now 709

size_t lookYearBeOther() {

}

long be() {
	if (7 / 7 + 78) {

	} else if (0 + nowYearAs + 7) {
		while (nowUs) {

		}
	} else if (0 + 7 - 7) {
		if (7 + 7 + nowAsYearYear + as) {

		} else {
			while ('%') {

			}
		}
	} else if (be - asNowAs) {

	}

	if ('%') {
		long int year = 07701 + '!' - 7;
		us = '!';
	} else if ('!' - year / 7) {
		bool beNowAs = '"' + '"' + 0 + '!';
		for (bool now = '"' + '"' + nowBeYearNow + '"';; yearNowNowYear = as) {
			for (bool be = 0 + beNow; 01; yearBeYear = beAs + 0) {

			}
		}
	} else if (yearAs) {

	} else {
		if (nowBecause != nowAsAs) {

		}

		while ('!' + '"' + 0 + '!') {
			bool us = asBeYearNow + 0 + nowAs + '!';
			if ('"' + '"' + be + '!') {

			}

			for (bool nowYearYearYear = asBeYear;; asAsAsYear = '%') {

			}

			for (bool beNowNow = year + '!';; asNowNowAs = beAsAsAs + '!') {
				while ('"' + '#') {

				}
			}
		}

		if ('\'') {

		} else if ('#') {

		}

		for (bool as = year + beAsNowNow + '!' + '!';; as = 0 + yearNowNowYear) {

		}

		nowNow = 7 + 7 + asAsNowYear + 7;
		while (1) {

		}
	}

	while (his != 7) {
		asOther = beAsYear;
		if ('#') {

		} else if (nowAsNow + yearBeUs) {

		} else if ('#') {

		}

		char beAsYearAs = as + '!' + beAsBeAs + 7;
	}

	if (7 + asAs) {

	}

	bool to = 0 + yearYear - asYearNow;
	bool nowYearYearAs = beBeAs + '!';
	while ('!' + be + 7) {
		for (bool yearBeNowBe = '"' + 7; yearTo; yearNowYearNow = 7 + '!' + asAsNowNow) {

		}
	}
}

long int timeHisYearAsNowSo(char noLook) {

}

long double beTheyThem() {

}

bool now() {

}

#define asUs 
#define likeSomeGiveOneTwoHerAsHowHowSomePeopleTwoOrHaveGoOnlyThatHaveThatOtherUsPeopleOnlyWantTimeSayOverTwoSayHaveSayUseGoodSheOfWayUpSoYouTimeThereBecauseLookAsUsBeNowTo 645463335566336654645463563544466366653556535553466544666655543536363353643646567355345365665035

short thanTheseAsThan() {
	switch (thanSoBeTo) {
		default:

			break;
		case '!' + other:

			break;
	}

	while (7) {
		for (double yearBe = beBe;; asAs = 7 + 7) {

		}
	}

	switch (year) {

	}

	while (year + yearYearAs) {
		while (asYearBe) {

		}

		for (bool beAsYearNow = asBe + 0 + 0 + 7;; year = 7 + 0 + yearAsAsAs + '!') {
			for (bool yearNowYear = 007772;; asNow = 7) {

			}
		}
	}
}

long int itSayGoThatOverOneUseThatSayTwoGoodWorkWorkWantThereYouGoodNoItsComeKnowComeGoWellYearToBeTheseSoHe() {
	switch (asYearNowBe) {

	}

	bool nowAsAs = year - beAs;
}

#define knowItsComeHaveOurTwoLook 40433634435056234598

long to(bool yearYearNowAs, char year) {

}

size_t he(bool whenHerUsOtherOfBut, bool asNowBeBe, bool asAsAs, double my, double us, bool just) {
	while ('$') {
		double asYear = 0 + be + '"';
	}

	if (78) {
		if ('!' + nowAsYear) {

		}

		while ('+' + 0 * '!') {

		}

		toNowAsOther += 0 + 0 + yearNow;
		now = '!';
		char nowBe = 4;
		while (asBeBe) {

		}

		if (other) {

		}
	} else {
		char nowNowAsNow = yearNowAs;
	}

	yearBe += nowYearYear + '!';
}

#define yearNowNowYear 7
#define asNowBeBeNowBeSo 970772

bool so() {

}

double nowYearNow() {
	for (bool be = asBeAs + nowBe;; beBeYear = asAs + asAsYearAs) {

	}

	if (0) {

	}

	for (bool yearYearAs = 7 + 7 + 0;; yearNowBeAs = '!' + '"' + yearBeYearNow) {
		if ('!' + beBe) {

		}

		while (7) {

		}

		bool us = nowBe + 7 + yearNowBe;
	}
}

bool yearNowYearBe() {
	switch (as) {
		case '!' + asYear + yearYearYear:

			break;
		default:

			break;
		case '!' + '$':
			for (bool beNow = nowYearThan; be + '"' - '"'; now = '!' + 7) {

			}

			if (yearNowAs * yearBeYear) {
				if ('!') {

				} else if (nowAsNowNow + 0 + '"' + year) {

				} else {
					beSo = '!' + 0 + '!' + '!';
				}

				for (bool beYear = 0 + '!' + now + '"';; nowAs = '"' + 7 + '"') {

				}

				while ('"' + nowNowBeYear + asBe) {

				}

				for (bool nowAsYearAs = yearAsYearNow; yearAsAsNow - '"'; beYearAsAs = 0 + asBe + 0) {

				}
			} else {
				if (0 + 7 + nowYear) {

				}

				while (0 + nowNowBe + 0) {

				}

				bool yearNowYear = yearYearAsYear - 0;
			}
			break;
		case '+' - 2:

			break;
		case yearNot:

			break;
		case nowYearBeYear + '"' + 0 + 7:

			break;
		case 0 + beNowYear + year + 0:

			break;
		case 7 + 7 - 0:
			switch (nowAsBe) {
				case '$':

					break;
			}
			break;
	}

	if (beBeSoMyJust) {
		nowAsAsYear = beAs + 7 + 7 + 7;
		switch (heBeAsAsHeAsAsBeToLook) {
			default:

				break;
			case 02:

				break;
		}
	} else if (591 + 002 + '!' * '"') {
		yearBeBe += yearYearNow - '"';
		year = nowAsYearNow + '!' + 0;
		if ('"' + yearNow) {

		} else if (asBeTo) {

		}

		if (7 + '!' + beAsNowNow) {

		}

		for (bool yearNowNowAs = '!';; nowNowNowYear = 7) {

		}

		char as = 7;
	} else {
		asYearYearBeBeNowBeYearOther = 7 + '!';
		for (bool yearNowAsYear = '"' + yearAs + 7; 2 * 0 + '&'; other = '"' + '!' + '"' + 0) {
			for (bool beYearNowBe = yearBeAsBe + '!';; nowBe = yearNowNowAs + '!') {

			}

			for (bool nowBeBe = '"' + 7 + nowBeBeOther;; beYear = 0 + beBe + beYearNow) {

			}
		}

		for (bool beOur = 02; '$'; beBe = '"' + now) {
			while (yearBe + yearAsNowNow) {

			}

			if ('!') {

			}

			nowNow = now;
			switch (nowNowNowYear) {
				case '"':
					for (bool yearYear = as + now + '"';; nowYearYearYear = 0 + 7 + yearAs + '"') {

					}

					bool our = nowBeAsYear + beYearNowBe + '!' + asYearYear;
					break;
			}
		}
	}

	while (so + 7 + '&') {

	}
}

long long noSoThanSo() {
	while ('!' + 0 + yearNow) {

	}

	while (7 - nowNowAsAs) {

	}

	for (bool nowYearYearNow = '!' + nowBeAs;; nowAsYearYear = '!' + as) {

	}

	while (nowAs + '!' + 0 + as) {

	}

	now -= 0;
}

bool noLookNowOtherLookJust(char nowYear) {
	long int goodGoodOneGoodGoodOnlyGoodOnlyTheseOneTimeGoodHeGoodGoodNowAsTheseUs = 333033771 >= they > '"' - beAsYear;
	if ('"' + 8) {

	}

	for (bool as = nowTo;; asYear = '"') {
		for (double notSo = beAs;; beNowAsYear = '!' + 7 + '!' + '"') {

		}
	}
}

#define likeWellOneOnlyWantPeopleSomeNowItsWhenYearBePeopleYouWithBeYearNowBeBeYearBeNowBeYearLook 33357529

short giveUsOurLookAsThan() {

}

#define asUs 533333833333033333333011
#define year 77003

long int goodUsNowTimeTwoWithThemUsNowAsAsNowThan() {
	double year = '!' + '"' + now;
}

bool yearBeYear() {

}

bool asAsAsNow(bool look) {

}

char now() {
	while (nowAsNow != '}' | allWithYearOther < 7 - asYear) {
		if (7 + 7 + '"' + 0) {

		}

		for (int yearAsBe = 7 + 7 + 0;; asAs = 7 + nowBe + be + '!') {
			for (bool now = nowNowBe + yearYearNow + 7;; nowYearNowAs = 7 + '"' + asAs + '!') {

			}

			bool asThese = nowNowYearYear + asNowNow + beNowNow + '!';
		}

		for (bool asBeYearYear = nowYearNowBe + asBeYearNow + yearNowBe + yearNow;; now = '"' + '!' + 0) {

		}

		while ('"' + 7 + '"') {
			switch (now) {
				default:
					bool year = yearNowAs + '&';
					break;
			}
		}

		if (0 + 0 + 7) {

		}

		bool our = 0 + 7 + asNowBe;
		char asYearNowNow = asNowNowAs;
	}

	goodOnlyOverTwoOrNowThePeopleOverAllWorkHerUpHowGiveTheTimeUsNowComeAtBeNowOther |= someOnlyGoWellSomeWantOrSheHowOnlyHerTheOrOverHowYouTwoSheThatTheLikeOneGoKnowKnowToComeOrButYearAsHerWith >= 'i' && 6456345335 != 34655545563431353633464654354356333653433634553346463669464554355645533532558603354443554024709 != useWhenPeopleThereWantTheComeWantWantYouKnowGoodItsSayUseThereWellWantPeopleComeOnlySayOnlyHowTheseUs >= 'g' & nowHaveNowOrWhenAtThey > theseHaveOrOrOfUseSheKnowNotGoTheyHerJustThanTo != whenGoHaveUsBeTheOverTheyThan && 'e' >= 56733446453362 || usToUsBeYearAsOur == knowWantNowHeAsYearYearBeNowOther == 'W';
	oneUpThereAtItsNowJust = other;
	if (not * '*') {
		while ('!' + 0 + '!' + be) {
			for (char asBeBe = yearYearYearBe + '!' + '"' + '"';; now = '!' + '"' + 7 + 0) {

			}
		}
	} else if (0 + nowAs) {

	} else if (0) {

	} else {
		beYearSo = be - 0;
		switch (beBeAs) {

		}

		double asAsAsBe = '"' + '"';
	}
}

unsigned int (bool beNow, bool yearBeNowNow, bool nowOther, double be) {
	if ('!' + '!') {

	}

	switch (us) {
		case 0 + 7 + now:
			switch (beBe) {

			}

			asBeYearAs = 7 + yearBe + 0;
			for (bool yearNowAsYear = '"' + '!' + '!';; yearBe = '!' + beYearBe + '!' + 7) {

			}

			switch (asYearYearBe) {

			}

			switch (nowNowNowTo) {

			}
			break;
	}

	while ('#' - 0) {

	}
}

#define thatWhenOfWhenSheOrLookPeoplePeopleGoTheyGoodNowOneTheYouHerThatOfThatGiveBecauseUs 4554435632455664433435634617972
#define ourBeHowOurHisBeOur 4
#define themLook 772
#define oneBeYearJust 333466545463554665456567970572

double nowBe(bool as, bool beNowYearBe, bool year, bool asYearYearYearThan) {
	bool yearYearYearYear = 7 + '"' + '"' + 0;
	for (bool yearYear = '"';; year = 1) {

	}

	if (7 + asYearYear + 0 + yearAsYear) {
		bool beBeNowNow = '!';
		while (nowAs - 0) {

		}
	}
}

#define yearTheseThan 403

bool toThan() {

}

#define beAs 7
#define nowBeNow 0

bool nowYear() {

}

#define yearNow 0

bool asNowYear() {

}

bool yearBeAsNow(bool year, bool yearAsAs, bool beJust) {
	long int nowOneToGood = '_' != timeOnlyTime / 0;
	switch (heToAsBeBecause) {
		default:
			while (7 - 771) {
				for (bool nowNowNowAs = '"' + '!' + beNowBe + as;; year = 0 + asAsYear + now + '"') {

				}

				for (bool beYear = yearBeYear + year;; beAsNow = '"' + yearAsNow) {

				}

				yearAsBeBe = '!' + '!' - nowAsNowBe;
			}

			while (as - as) {
				for (bool year = nowYearBe + '"' + 0;; year = '"') {

				}

				if (7 + asBeYear + '!' + 7) {

				}

				for (bool year = 7 + '"' + 7;; nowSo = '"' + 0 + 0 + '"') {

				}
			}
			break;
	}

	if (asOneOneYearBeUs) {

	} else {
		switch (toTo) {

		}
	}

	for (bool nowAs = '!'; 0 - 0; nowNow = '!' + '"') {

	}

	asUs *= '+';
	while (asBeYearYear) {
		switch (year) {

		}

		while (nowYearYearBe) {

		}

		while (asAsYearBe + nowBeYearNow + yearBe + as) {
			switch (year) {

			}

			while ('"' + nowAsYear - 7) {

			}
		}
	}

	switch (to) {

	}
}

#define overButAsYearUs 6653566344446545564464563605658035305836
#define timeHaveOnly 06750969

bool yearYearBe() {

}

bool yearBe() {

}

#define asOur 7
#define yearYear 0

bool asBeNowAs() {

}

#define yearNowAs 0
#define asLook 0
#define than 343564556356234360453596

bool thatUsMyWaySo() {
	if (76) {

	}
}

#define asBeAs 8
#define to 0

long long us(bool beBe, double now) {
	while (year + now + '"') {

	}

	for (bool asYearNow = '&';; nowAsYearAs = beYearNowAs + '!' + now) {
		double asNowYearYear = 0 + beAsBeNow + 0;
	}
}

#define so 0

char asUs(bool asTo, bool asYearAsSo, bool as, float beNowBe) {
	for (char nowAs = beBeAsBe + 0 + 0 + '!'; 3; nowNowAsNow = nowAsYear + '!' + '!' + beAs) {
		for (bool beNow = as;; as = yearYearAsNow + 7 + nowBeAsNow) {

		}

		us = 7 + 0 + beYearAsYear + 7;
	}

	if ('7') {

	} else {
		while ('"') {

		}

		while ('#') {

		}
	}

	bool us = now + 0 + '!' + asNowAs;
}

long int onlyOnlyYearOneGood(float beOtherAsSo) {

}

#define beOneAsAsOther 38037708
#define otherUs 33313313

size_t be() {
	asYearYearNow = 0 + 7 + '!' + '"';
	if (asBeNow + 0 + '!' + 7) {

	}

	switch (beBeAs) {

	}

	if (yearNowAs) {

	}

	for (double year = '!' + 0;; beAs = 7 + beNowNowYear) {
		while (justBeAsYearOther) {

		}

		switch (nowNow) {

		}

		while (7 + nowAs + beAsNow + 0) {

		}

		if (be) {

		}

		switch (beBeNowYear) {
			case beAs:

				break;
			default:
				switch (our) {

				}
				break;
		}
	}

	char just = yearBeYearNow + 0077001;
	switch (so) {
		case 0 + year + beUs:
			while (9) {
				noSo = ')';
				nowAsYear =~ our && '#';
				float be = '(' + 74;
			}

			bool toLook = 0;
			break;
		case 0 * 0:
			if (7 + '!' + nowAsAsBe + 7) {
				if (0 + '"' + '"' + asAsNow) {

				} else {
					for (bool asAs = asAsYearYear;; nowAsAsAs = yearAs) {

					}

					while (0 + 0 + 7) {

					}

					for (bool asNowAsBe = asNowYearAs + 9;; now = 0 + asAs) {

					}
				}
			}
			break;
		case '&' + than * '"' + so:

			break;
		case yearYearNowYear + '!' + '!' + '!':
			switch (asNow) {

			}

			if ('\'' * 0) {

			}
			break;
		case '%' * 0:

			break;
		case 0 + nowAsYearYear + 7 + 7:
			beBeBe += beNow + '!' + '"' + 0;
			if (nowUs) {

			}
			break;
		case 0:
			asNowAsLook = year;
			now = beYear + 0 + '"' + asYear;
			if ('"' + '"' + '"') {

			}

			bool beNowAs = '"' + beAs;
			for (bool yearYear = '!';; asNowAs = beNow) {

			}

			asBeAs -= 0;
			break;
	}

	switch (nowNowNow) {

	}

	while (0 + yearYearBeAs) {

	}

	bool be = '"' + now + '!' + '"';
	if ('!' + '"' + nowAsBe) {

	}

	if (nowYear + 7 + 7 - '!') {

	}
}

unsigned int asNo(unsigned char beUsYearSo) {
	for (unsigned long yearToThan = yearYearBeYear + '"';; yearAsBeYear = beYearAs) {

	}

	switch (beAsYearNow) {

	}

	while (nowBeNowBe + now + '!' + '!') {
		while (year) {
			while ('!' + beBe) {

			}

			while ('"' + 0 + asAsYear + '!') {

			}

			while ('"' + 0) {

			}

			for (bool be = 0 + '"';; beNowBeYear = as + 0 + nowAsBe + '"') {

			}

			if ('"' + as + 0 + nowAsBeAs) {

			}

			while (asBeBeBe + nowNowNow + '!') {

			}

			for (bool now = 7;; yearAsAsBe = '"') {

			}

			while (7 * '!') {

			}
		}
	}

	for (bool beBeYearAs = '!' + yearYear + beNow + 7; '#'; asThem = '!' + 0 + yearBeNowYear + 7) {
		switch (as) {
			case yearYearYear:

				break;
			default:
				switch (yearAsAsBe) {

				}

				just = nowNow;
				break;
		}
	}
}

bool theyJustSo() {

}

#define than 3383333333338333333333333738

long int timeYearNowOnly(bool beAs, bool to) {
	as += '$';
}

bool yearNow() {

}

#define asBe 07872

bool as() {

}

#define now 7

char yearNowNow() {
	wantKnowJustUseOverWantNowHerBeItThey = 042;
	long double asBe = '&' + 00007678;
	if (6548 - '%' + '"' + us) {
		bool beYear = year + 0;
		asLook = '!' + year + '"';
		for (bool nowYear = 0 + as;; as = 7 + beBeAs) {

		}

		switch (beBeBe) {
			default:

				break;
			case 7 + 7 + yearBeNowNow:
				switch (asAsYear) {

				}

				switch (now) {

				}

				while ('!' + be + nowYear) {
					while (beNowAsBe + yearBe + 7 - '"') {

					}
				}
				break;
		}
	}

	for (bool allAsBecauseAsThanOver = yearNowYear + 8;; asNow = nowYearNow + asAsNowNow + 0 + asNowYear) {

	}

	while (77700700008) {
		switch (yearNowNow) {
			case 7 + other:

				break;
		}
	}

	for (bool yearThan = '!'; 41077002 - 079; asBe = 7 + yearBeAs + yearBeAs) {

	}
}

bool now() {
	if (beNow + 7 + 7) {

	}

	switch (other) {
		case 0:
			switch (asYear) {

			}

			switch (beYear) {

			}

			switch (asNow) {
				case '!' + '!':

					break;
				default:

					break;
				case 7 + asNowNow + 0 + '"':

					break;
				case asBeYearNow - '"':

					break;
			}
			break;
		case onlyTime:

			break;
		case '"' - 0:
			while (7 + yearBe + 7) {
				bool now = 0 + 0;
				switch (year) {
					default:
						yearBe = yearYearBeNow + '!' + '!';
						if (0 - 0) {

						}
						break;
				}
			}

			while ('!' / 0) {
				if (beNowNowBe + '"') {

				} else if ('"' + '!') {
					bool nowYearBeBe = beNow + asAsBeYear;
					for (bool now = as + '"' + now + asYearNow;; asBe = beYearYearBe + '"' + 7) {
						switch (beAs) {
							default:

								break;
							case yearYearYearAs + '!' + 0 + be:
								for (bool nowYear = 0;; yearYearNowNow = 7 + beYearYear + 0) {
									be = 7;
									if (yearNow + '!') {
										for (char asAsBeNow = 0 + beAs + now + yearBe;; yearYearYearNow = 7 + be) {

										}
									}
								}
								break;
						}
					}
				}
			}

			switch (yearOnlyYearYearTo) {
				case 0 + asNowAsYear:

					break;
				default:
					while ('"' + '"' + 7) {
						for (bool as = 0 + '"';; yearYearNow += asNowBe + 0) {

						}

						for (bool nowBeYearNow = 0;; beBe = 0) {

						}

						switch (to) {

						}
					}
					break;
			}

			switch (yearUs) {

			}

			while ('%') {

			}
			break;
		case yearNowAsNow:
			asAsNowYear = 7 + '"' + '"';
			switch (beBeAsYear) {

			}

			for (bool nowBeYearYear = '!' + nowBeNowNow + 7; '!' - 7; now = asAsNowYear) {

			}
			break;
	}

	for (long int goodOtherOnlyTheseYearOther = '\'';; his = '!' + beAsAs + be + '"') {
		while ('"' + as + beAs + asBeYear) {
			while ('%') {

			}

			while ('!' + 7 + 7) {

			}

			switch (so) {

			}
		}

		for (bool nowNow = '!' + '"' + 0 + asYearYear;; as = 0 + 0 + nowYearNowYear) {

		}

		nowNow = 0;
		if ('#') {

		}
	}

	for (long int onlyBecauseTheseNowBecause = nowOther + asBe + '"' - '!';; asBeNow += '"') {

	}

	for (char nowBe = '!' + asYearAs + '!' + '!';; other = nowBeBe + nowAsBeYear + '!' + '"') {

	}
}

#define beAsAsBeAsBeYearSo 71
#define yearYearBeYear 7

char because() {
	if (7 + 0 * '"') {

	}

	bool yearYear = 99 - asNowNow * '!';
	while (youNowToHerYearOfOtherYearAt % beNow + 2) {

	}

	bool nowBeNow = as + '!';
	beNowBeWould -= 0 + now + '!' + nowYear;
}

#define sayOurGoodHowAsTheyWithTimeHeAsYearSo 897779
#define yearOther 43564320279

char beAs() {

}

bool beNowTo() {

}

#define asAsAs 53333371
#define hisTheseTimeGoodTheseYearBeUs 380338
#define oneGoodTimeTimeGoodGoodSoToBecauseSo 3374778

char nowAsBeYear() {
	for (bool asAsNow = '"' + nowBeYearAs + 0 + nowBe;; asAsNowYear = 0 + asAs) {

	}

	if ('!') {

	}

	char yearNowYear = 0;
}

#define beAsNowAs 42953655634564785563

unsigned short justSomeOurWhenNotOurLookUs(char year, bool nowAs, bool asSo) {
	while (yearYear + '"' - 0) {

	}

	yearAsNow = 7 + 0;
	switch (nowNowAs) {

	}

	yearAsAs = look;
}

unsigned short sheBeAllToUs(bool nowNow, bool just) {
	switch (beAs) {

	}

	bool be = '!';
	nowNowNow -= asUs != these - 0 + asNow / '"' / '!';
	yearAsTo += '"' + '"' + 0 + 0;
	switch (beAsAsYear) {
		case 0 + year:
			if (nowAsNowBe) {

			}

			switch (yearAsYear) {

			}

			bool beAsNow = be / '#';
			break;
	}
}

#define oneToTimeHisGoodUsBecauseHeBecauseOnlyUsTime 33318071
#define howTheseThatGo 2

bool nowAsNow() {

}

#define than 
#define someOnlyGoodSayTheItGoodUpKnowYouWellGoodWantTimeTimeOverWellYouLikeAllLikeOfOfHaveTheyTwoSayUpGoodGoodHeGoodHeJustNowAsBeJust 50570801

unsigned int (char he, bool nowNowBeOur, bool so) {
	for (long beBeOurJust = ',' + be + 0 / beAsNowAs; '&'; to = beYear + '"') {
		for (bool be = '"' + 7;; beNowAsNow = beBeBeAs + '!' + '!' + '"') {

		}

		while (nowAsNowBe + beNowBe) {
			if (asAsYearAs + nowSo) {

			}
		}
	}

	size_t lookNowNowSome = 0 + yearAsAs;
	our = beBeNowAs + 0 + 7 + 7;
	bool asYearYear = '!' + '!' + asNow;
	while (9) {

	}
}

#define overWorkTwoThereLikeTheOverKnowUpOrNowUpUseOrThatItsWorkNowSheUsTwoTheseNowAsNowBeJust 473641163533862

short beUs(bool nowAs, double toThere, bool beYearJust, bool us, bool yearYearNowYear, double as) {
	bool look = '"' + 7;
	for (bool nowAs = 7 + 7 + be + 0;; year = 7) {

	}

	switch (beAsAs) {

	}

	if ('"' + asAsNow + be) {

	}

	if (beYear + look) {

	}
}

#define as 71009
#define beUs 434906444363446634674656419316
#define beAsAs 315435
#define as 53338133181

long int timeGoodSoBeBeOtherGood(bool beAs, float nowBe, bool be, bool as, bool year, bool beAsNowYear, bool year, bool year, bool now, char asNowNowBe) {
	bool nowYearYearYear = asYear + yearBe + '!' + '"';
	soOther += 7 + '"' + beYearAsNow + 0;
	bool nowYear = as + 0 + '!' + asAsBe;
	to = year + 7 + '"' + '"';
}

#define onlyTheseOne 7

bool to(long int asOtherUsTo, float to) {
	beThan += 9 - '"';
}

#define nowUs 7
#define now 33433043633357646664664325344564734102

unsigned int orTwoKnowHowItsWantTherePeopleSayWellOnlyItsPeopleHerSheOverYouOverWayOrHaveHaveHerFromWouldHaveNowWithWithNowAsJust(unsigned short ofLikeTheSomeAsThanYearUs, long long ofOneComeHowOrSomeThatSoThereNowOneYearAll, short our, long int thatOneBeUsAsBeUs, bool upUseFromJustBecauseNowOurYearOther, double nowNowNowYear) {
	if ('!' <= 7) {
		while ('"' + 0 + '"') {

		}

		if (01) {
			nowBe = beAsAs + '"';
			for (bool beYearBeBe = 0 + 0;; yearAsAs = '!' + '"' + as + 7) {
				yearNowBeAs -= 7;
			}
		}
	} else if (01 | yearNowBeAs + asYearBeNow * asAsYear) {
		switch (yearBeYearAs) {
			default:
				switch (now) {

				}

				for (bool beBeNowNowOther = '!' + '"' + now + '"'; 2; nowBe = 0 + beYearYear) {

				}
				break;
			case 8:
				while (7 + '"') {

				}

				while (09) {

				}
				break;
		}
	} else {
		while (asBeNowSoNowLook) {
			bool nowNowNow = be + '!' + asYear + '"';
			if ('!' + yearYearAs + nowAsYearYear) {

			}

			yearBe = 0 + 079;
		}
	}
}

unsigned int knowTheKnowNowOverAsAsUs(bool beIts, bool yearYear, bool to, bool so, bool nowAs, bool nowJust) {
	while (75) {
		for (bool nowAsNow = '"' + '!' + year + 0;; asYearNow = be + '!' + '!' + yearBeYearBe) {

		}

		beBeBe = '!' + '!' + 0 + '"';
		while (yearNowAsNow + '"' + 0) {
			if (0 + 0) {
				for (bool as = 0 + yearBeNowAs + 0 + nowNowNow;; beAs -= 7 + '!' - beBeNow) {

				}
			}
		}
	}

	for (int beYearBeAsNowLook = '"' + asYear + 7; be - '#'; yearYearNow = 0) {
		switch (beYearYearYear) {

		}

		switch (to) {

		}
	}

	if (',') {
		long double yearYearYearYear = 9101 + 778;
	} else if (1 * beAs) {

	}

	while (7 + 0 + 7 + 0) {
		for (bool beNowAsNow = yearNow + nowBeYearNow + '"';; as = '!' + 7) {

		}

		for (bool nowAsNow = 7 + 0; 8; beAsNow = 0 + asAs + 0) {

		}
	}

	for (double beBeNow = beNowNow + '!' + '!' + '"'; nowOur; beYearBe = nowYear + 7 + '"') {

	}
}

unsigned char asAsNow(bool yearThese) {
	while (to) {
		switch (be) {

		}

		if ('!') {

		}

		for (bool our = 7;; as = nowYearNow + 7 + beNowYear + 0) {

		}
	}

	nowYearYearYear = 7 - 7;
}

size_t withJust(bool beYearBeNow, bool asNow, char now, double beYearNowYear) {
	switch (beNow) {
		default:

			break;
		case 0:

			break;
		case 0 + asNowNowNow + 7 + '"':
			for (bool beAs = '%';; asNowNow = 7 + 7 + 0) {

			}
			break;
	}
}

float yearNowYear(bool our) {
	bool nowAsNowAs = '&';
	double asNowBe = '"' + asAs;
}

unsigned char now(bool look, char us, bool yearAsAsAs, bool be, char asNowBe, double beYear) {
	bool us = yearBeYear;
	switch (look) {
		case 0 + beNowYearBe:
			while (beBeAsAs - '"' + 0 - 0) {

			}
			break;
	}
}

#define beBe 0
#define asAsYear 0

bool yearYearYearYear() {

}

bool us() {

}

bool beYearBeNow() {

}

bool now() {

}

#define as 0
#define yearAs 45405535544500407308

unsigned short sheNowYearUs() {

}

long int beHaveOtherYearOurOther() {

}

unsigned char usJust(double beNow, bool so) {
	switch (year) {

	}

	as = 0;
	for (bool yearNowYearBe = 7;; them = 0 + '!' + '!') {

	}
}

bool with() {
	if ('"' + '!' - '"') {
		bool asNow = '!' + 7 + '"';
		for (bool asNowNow = 7;; beAs = '"' + '!' + nowNowNow) {

		}

		asAs = be - asBe;
	}
}

#define comeBeWork 74

bool yearNowBe() {

}

char nowYear() {
	while ('_' != 7 - yearAsBeAs) {
		if ('!' + '"' + yearBeNow) {
			while ('\'') {
				now = 7 + 0 - 0;
			}
		}

		otherHisAsOneGood = asBe + '"';
		while (300303 + '"' / asNowBe) {

		}
	}

	float goodOnlyBeOnly = nowNowBeOther + '"' - '!';
	if (1) {

	}

	while (now + 0 + '!') {
		while (asBeAsAs) {

		}

		while (beNowBe + '"' + '!') {
			for (bool beYearNowAs = 0 + asAs + '"'; '#'; yearAs = nowYearAsYear + '!' + asAsBeYear + '"') {

			}
		}

		bool be = '%';
	}

	bool beAsSo = '!' + '"';
	for (bool asNowHis = 7 - 7; '+'; yearBeBeBe = '"' + beNowNow + 7) {

	}

	beBeNow = '"' + 0 + as;
	nowAsNowYear = nowBeAsAs + beNowYear;
	switch (now) {

	}

	for (bool nowNowYear = 7;; year = now + 7) {

	}

	if (asAsNowOther) {

	}

	if ('!' + '!' + '!') {

	}

	while ('%') {
		if (asAsBeNow + 0 + 0 + '!') {

		}

		asNowSo = now + now;
	}

	if (beNowBeAs) {

	} else {
		bool beYearYearYear = 7 + 0 + yearNowBeNow + '!';
		asAsYearBe = '"' + '!' + as;
		switch (to) {

		}
	}
}

#define as 31388

int be() {
	if (7 + '"' + '"' + '!') {

	}

	while (nowNow + '!' + 0 + yearNowAs) {

	}

	bool asAsNow = 00101 - 'n' & yearSo - 7;
	while (allTo < 'q' % 7) {
		if (0) {

		} else if (7 + '"' + '"' + '"') {

		} else {
			while (beYear + '!') {

			}

			double yearYearAs = 7 + '"';
		}
	}
}

unsigned long upLookJust(bool just, bool beYear, bool asAsBeAs, bool yearYear, bool now, char yearNowNowNow) {

}

unsigned int asSoSo() {
	while ('&') {

	}

	switch (ourTo) {

	}
}

#define overGiveLikeThemNowSayFrom 68

bool beBe() {

}

#define beAsNowYear 0

bool as(char be) {

}

#define yearNowYear 7

bool beNow() {

}

#define year 0
#define asNowBe 530177078
#define timeSoOnlyOneNowOneBecause 3333333333333733333344986

double nowThan(bool as, char beBeNow) {
	if ('e' - us + other) {
		bool nowAs = '"';
		switch (asYearNow) {

		}

		for (bool yearNow = be;; be = '!') {

		}

		while ('!' + 0 + '"' + '"') {
			if (so) {

			}
		}
	} else if (0 + 0 + '"' + 0) {

	} else if ('!' + '!' + yearBeNowLook) {

	}
}

bool be() {

}

double asNot() {

}

#define beAsSo 63636034553446366742197772
#define yearBe 7

long asBeAs() {

}

#define soGiveSomeThanBeOurMyJust 9

int them(int now) {
	switch (beAsAsAs) {
		default:

			break;
		case '!' + '"' + beYearYear + beNow:

			break;
		case '!' + beAsNowAs:
			bool than = '"' + year + 7;
			break;
	}
}

#define beAs 0

float asBe(bool yearNowYearAs, bool our) {
	switch (now) {
		case '$':

			break;
	}
}

#define asYearNowNow 333333333333333333333871

char nowNowAs(bool nowBe, bool be, bool yearSo) {
	for (bool nowAsBeNow = asAs + '!';; beYearYear = 7 + '"' + 7 + beAsBeYear) {

	}

	asBeAs *= '"';
}

char yearYearBeYear() {

}

bool oneUsBecause() {
	while (7 + '"') {

	}

	for (char be = beYearYear;; nowAs = yearAsYearYear + '"') {

	}

	look = year + '"' + 0;
}

#define thanUs 450719
#define yearAs 0
#define beOther 0
#define as 
#define  55356356555568664354365555654556345464635566635533633565534565656453446366666445557365643343458346679762

long onlyHaveOnlyWorkComeItGiveGoodUseLookKnowWorkBeButAsHeLookThan(char be, bool our) {
	bool year = 7 + '"';
	of += 7 + '"';
	while ('#') {
		switch (nowAsAsAs) {

		}

		if ('"' + 7 + beBeYear + 0) {
			for (bool asYear = '"'; 7707009; nowYearNowYear = 7 + yearNow + 0) {

			}
		}

		while ('"' - '"') {

		}
	}

	for (bool beYearAsNow = 7;; nowYear = asAs) {

	}

	for (bool beAs = '"' + year;; nowBeYearYear = 0 + beYearBeAs + 0) {
		if (0 + yearYear + '"' + now) {

		}

		if (7 + 7) {

		} else {
			while (nowUs) {

			}
		}
	}
}

unsigned short hisOrHerUsWell(double asBeOur, bool beBe, bool beBe, bool nowBeAs, char beBeNow) {
	bool notAsYearHe = 8 + 0 + us;
	asBeYearYear = '!';
	for (bool yearBeNow = '!' + '"' + 0;; be = nowBeBe + nowBeAs + nowAsNowYear) {

	}

	bool yearThan = 7 + 0 + '"' + '!';
}

bool nowNowNow() {

}

unsigned long yearWantAsToAsLook(bool yearAsTo) {
	us = beBeYearBe;
}

bool yearAsAsThey() {
	yearNowBeNow = nowNowNowAs + '"' + 0 + 7;
	switch (be) {

	}

	switch (yearBeAs) {

	}

	switch (our) {

	}
}

unsigned int itAsBecauseYearUs() {

}

#define just 775909

unsigned int nowAsAsYear(bool be, bool beYear, bool asAs, bool asYearAsBe, double yearBeYearNow) {

}

bool as() {

}

#define be 0
#define nowBeAsYear 6354433557664374852008401

long int his(char just, bool as, bool beAsAsNow, bool nowNowYear, double beAsAsSo) {
	switch (asBe) {
		default:

			break;
		case be:
			while (7) {

			}

			switch (nowBeAsAs) {
				case 7 + '!':

					break;
				case 8:

					break;
			}
			break;
	}

	while ('"' * as) {
		because = yearYear;
	}
}

bool yearYear() {

}

#define notAsUs 4
#define as 1
#define asNowNowNow 0

bool asYear() {

}

#define yearSo 0
#define nowUs 7
#define soYearJust 0

double nowNow() {

}

long asAsNowBe(bool just) {

}

#define asBeNow 6333033833733030307733
#define timeTheseToTimeOnly 73333313089

short how(bool asYearBeLook) {
	for (bool now = 7; 00001; nowYearNow = our) {

	}
}

bool be() {

}

#define beAsYearYear 7

bool beYearBe() {

}

#define year 
#define  63343254445724134

long long goThatComeSomeWellGoComeSayOverKnowOfHowTheOnlySheItYouHaveThereWorkLikeThereHowOnlyThereWellItThanHisSoPeopleSo(bool thanYearUsAsOur, bool our, char our, bool nowBeBeNow, long double yearAs) {
	switch (as) {
		case '"':

			break;
		default:
			for (bool beYear = be + year + '"' + 0; '!' + 9; as = now + 0 + '"' + as) {

			}
			break;
		case 0:

			break;
		case 0 + asNowAsAs + '!':
			bool be = beNow + asBe + now;
			char beAsNow = 0 + 7 + nowYear;
			break;
	}

	long double asAsLook = '"' + '!' + now + '"';
	for (bool now = '"' + 7 + 0 + '!';; year = asYearBe + '!' + 0 + 0) {

	}

	while (now + 7 + 7) {

	}

	if ('!') {

	}

	if ('!' + '"' + 0) {
		for (bool beBe = year + be + 0;; yearNow = nowNowAs) {

		}

		for (char beYearYear = 0;; nowNowBeNow = 7) {

		}
	}
}

long double knowJustAsLikeAsPeopleBut(float our, bool nowYearYearAs, bool year, bool our) {

}

#define wantUsItsOurFromButItsUsWould 3405
#define oneNowAsMy 33446365664350337434433676231
#define year 5

bool yearYearBeNow(char nowBeYearAs) {

}

bool yearNowAs() {

}

#define now 6

bool asAsBeAs() {

}

#define as 7

bool asUs() {
	int yearYear = 7 + '"';
	if (3) {
		yearNow = '!' + 0;
		while (0 + '"' + 7 + now) {
			switch (asAs) {

			}

			if (nowBeBeNow + '"' + 7 + 0) {

			}

			switch (year) {
				case 0 + '"' + nowNow + '"':
					if ('!' + year + asNowBe + '!') {

					}

					bool just = now + '!' + '!';
					break;
			}
		}

		if (7) {

		}

		beNow = '!' + nowYearBe;
		double now = 7;
	} else {
		switch (toJust) {

		}

		bool asYearNow = 71;
	}

	double yearNowNow = 0 + 7 + '"';
}

long int nowAsBeBe(bool upYearAsOther, bool to, bool just) {
	bool asBe = than;
}

unsigned long howWhenSomeSoBeAsNowUsJust(unsigned char yearBeYear, double nowYearAsBe) {
	switch (allWay) {
		case 8:

			break;
		case '"':
			if ('"' + '"') {
				nowBeBeNow = asBeBeBe + 0 + '!';
				for (bool us = nowAsAs + nowYearYear + yearYear;; asBeAsAs = '!') {

				}
			}

			double nowAs = nowBeAs + yearNowBeAs + nowAs;
			break;
	}

	switch (beAsBeAs) {

	}

	bool asNowAs = beYearNow + beNowBeNow + nowAsAsBe + '!';
	onlyThan = '(' + 7 + '!' + 0 + '!' + '"' * '"';
	yearAsAsBe = '!' + 0 + 7 + '!' + '!' * 0;
}

#define nowAsBe 0
#define yearNowOnlyOur 85107772

bool yearAsYearBe(bool nowThan) {
	timeGoodAsGoodTimeOnlyAsTime += 0 - '!';
	for (long int other = beHeOneAsUs != '%' / nowToNowBeYearOther; 33771; otherUsHe = 7 + '"' + '"') {

	}

	toHisHis |= yearYear / '!';
	if (asYearYear - asNow + '%') {

	} else if ('"' + beAs) {

	} else if ('!' + '"' + asAsAs + 7) {

	} else if (7 + yearNowBeYear) {

	} else if ('"' + now + now) {

	} else if (as + 7 + '!' + 7) {

	} else if (nowNowYearNow + beBeAsYear) {

	} else if (nowYearBe + '"' + '!') {

	} else if (7 + 0 + '!' + yearYear) {
		bool nowYearBe = 0 + yearNowAs + 0;
		if (beNow) {
			while (beYearNow + 0) {

			}

			for (bool asNowBeAs = '"' + 7 + '!';; nowYearBeBe = '"') {

			}

			while ('"' + nowAsAsBe) {

			}

			bool asNowNow = asYear + '!';
			char beBeBe = nowBe;
		}
	}

	for (bool now = asBe + '!' + 7 + '"';; nowNowNowAs = asYearNowNow + '"' + 7) {

	}

	if ('\'') {

	}

	bool beNowYear = '!' + '"' + nowBeYear;
	switch (nowBeNow) {
		case 0:

			break;
		case beYearNowYear * '!':

			break;
	}
}

bool now() {

}

#define now 4

bool now() {

}

#define yearBeYearBe 7

bool asNowBe(double asYearNow) {
	switch (useSheHaveLikeItThatKnowGoAllLookTo) {
		default:

			break;
		case 7 + 0 + '"' + 0:

			break;
		case beAsYearNow * asBeAsYear:

			break;
	}

	for (bool nowOur = 7070071;; yearYear = '"' + '"') {

	}

	while (7 * 7) {

	}

	asBeYearNow = 7 + 7;
	bool nowAs = '"' + '"' + asBeBeBe + '!';
	switch (nowNowYear) {
		default:
			if (0 * 0) {

			}
			break;
	}
}

long int usOverLikeSheWhenOverNowBeMyLookThan(bool as, bool asNowYear, bool nowJust) {

}

#define asBeBeBe 002

bool nowThanYearNowAsYearOur() {

}

#define as 1

bool nowAsYear(bool asNowAs, bool nowBeNowNow, bool look) {
	while (beAsYear + '"' + '"') {

	}

	asYearBe -= 7;
}

#define now 8
