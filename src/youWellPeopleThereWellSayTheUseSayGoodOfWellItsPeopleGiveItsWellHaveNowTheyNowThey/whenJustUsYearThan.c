#include <UUU/UUE0UUUUU031.h>

#define but 2
#define asAs 002
#define as 33666344534333333383873830371

long int oneOnlyOneGoodOneTimeGoodOneTimeSomeWantYearSomeTo(bool be, double yearAsAs) {
	nowNowBe += beAsBeYear + nowNowAsNow + 7 + '!';
}

unsigned int justSayUseOf(unsigned long nowThanJustNowYearJustNowBeAsSo, bool asAsNow, char our, bool yearOther) {
	if ('"' + 0 + nowNow) {

	}

	switch (beAsYearAs) {

	}

	bool be = 7 <= beBeBeNow;
}

size_t nowOtherJust() {
	bool yearLookUs = asAsBeBe + year + nowOther;
	if ('"' + 7 + 9) {
		for (double asYear = year;; as = nowYear + 7) {

		}
	} else {
		if ('"' + '!' * 0) {

		}
	}
}

#define asNow 43463654702

long double as(bool beBeBe, bool asYearYear, char than) {
	switch (nowNowYearBeYearTo) {
		default:

			break;
		case 7 + nowAsNow:

			break;
		case now + asYearBeNow + 7:
			while (now + 0) {

			}

			while ('!' + '!') {
				while (0 + 0 + '!') {
					for (bool asYearNowNow = beBe + '"' + '"';; nowBeNow = '"' + 0 + '"' + yearNow) {

					}

					for (bool as = beNowNowBe + asBe + '!';; nowYearNow = '!' + yearNowNow + 7) {

					}

					for (char yearNowAsAs = '!' + '"';; be = 7) {

					}
				}
			}
			break;
	}
}

unsigned char sheSheAllUsTwoYearToYearWouldOur(int beYear, double asAsNowYear, bool yearYearYearBe, bool nowNow, bool beAsNowBe, bool atWant) {
	switch (asNowBe) {

	}

	bool asNowAs = '"' + '!';
	for (bool beYearNowAs = '"' + '"' + asBe + 0;; beNow = 0 + beNow + 0) {
		for (bool year = '!' + beNow + asNowAsYear;; asNowAs = '"') {

		}

		yearLook = '!' + 7;
	}
}

int beTheyAsBeNowSo(bool yearBeAs, bool year, bool asBeYearBe, bool asBe, bool nowYearBeBe, bool just) {

}

size_t at() {

}

#define she 33308333380007778

long int becauseHis(bool asTo, float now) {
	char yearNowAs = us;
	year += '"' + year + '"' + '"';
}

long int asNowNowYear(bool beBeNowAs, bool one, char asYearAsBeSo, char now) {
	beOneOtherUs |= asAsTo;
	while (be + 331 <= '#') {

	}

	long beAsNowBe = 7 + nowAs;
	bool nowLook = 0409;
}

double be() {

}

bool nowYear() {

}

bool asYear() {

}

bool beThan() {
	 >>= 655 >= itWellJustWorkSomeYouItsGoodSheAllOrWellItGoodThemNowNot && butGoJustOther >= 'r' >= 'y' - 9 || '!' - yearYearYearBe + '#';
	while (335744660657677661 % ofHerThereUsYouThatOther & 65435523508) {
		while (asLook) {

		}

		yearJustOur = 1;
		if (0 + '!' + 7) {

		}

		while (7) {
			for (bool yearBeAsBe = 7 + '!' + beYearBeYear; yearYear - 7; nowNowYearBe = 0 + '"') {

			}
		}
	}

	switch (herGoOverWorkWantSomeGoHaveMyYouWantTimeGoOrHeItHowHowUpComeLikeUpHaveToGiveUsLikeNowOur) {
		default:
			if (49772 & nowYearYearAs / 99) {
				for (bool yearNow = '"' + 7;; nowAsAs = '"' + '!' + '!' + '!') {

				}

				beBeYearBe = '!' + '"' * 0;
			} else {
				if (as + beBeBeYear + yearAsYearAs) {
					if (beYear) {

					}

					for (bool year = than; nowNow * 0; asAs = asAs + nowYear + 0 + asNowNowNow) {

					}
				}

				bool nowNowNowBe = beAsYear * '!';
				asNow += '!' + as + 7;
			}

			size_t atBecauseSoToNowYearBeBeNowAsTo = 0 > 4079;
			long int asSo = yearYear;
			break;
		case 0 + '!' + 7 + '"':

			break;
		case nowNowYearBe + 7:
			if ('"' + asYearAsBe + year) {

			}

			switch (beBeNowBeOther) {
				default:

					break;
				case beNowBeNow + yearBeNow:
					yearAsAs = nowNowYear;
					for (bool beYearBe = 0 + asBeNowYear; year * '"'; asNowNowAs = 7 + be + 7) {

					}
					break;
			}

			for (bool beYear = now + '"' + yearNow; '!' + nowBeBe + 7 - 7; as = '!') {

			}
			break;
		case 6121:

			break;
	}

	now <<= '.' * ')';
	long int comeTheUpWorkKnowWorkItUseThereYouHowWhenThatSheOfHaveItWhenTwoGoOverAllButGoTwoHisSheUseTimeJustTheseYearYearLookBeUs = likeTwoUsGoodItsUsOfAllWellTimeYearThemAt <= likeUpUseTimeWithOverNowMySo | peopleSoTimeMyWhenSo - nowYearYearAs;
	if (0 - asSo) {
		bool nowBe = '!' + nowNowAs + 0 + 7;
		yearNowNow += '"' + yearYear;
	}

	while (whenYearSome - beYear * '"' + now + than) {
		switch (be) {

		}

		double be = '!';
		beTo += 7;
		float be = yearYear;
	}

	long as = 0 + 0 + '!';
}

#define youNowJust 4433504463434

long long (unsigned char upHaveUsePeopleWhenFromWellNowGoodOrThatOnlyTwoThatUs, bool heTo, bool goThan, bool no, char asAs) {
	upUsWithComeAsJustAsAllThey ||= 7 + 'Y' > '~' < toThanOur - yearNowBe;
	unsigned long his = 97971 == 3602 + beNowUs + 0 * 0;
	workJustBeMyOf |= 36101 + '!' + 78 <= 7;
}

#define wouldWorkGoComeTwoSomeUseLikeOfThemPeopleTwoOfUseItOverHerHerLikeGoSomeOrWorkWhenGoHowPeopleThereComeYouOfUpWellOrYouPeopleOneHowSheOneSayWantComeTwoGoodWantWellWhenTwoWantGoThatUsGiveSayLookItHaveComeYearWhenSoSo 334444344346335344535644353545365333535445448446655553353376065654434664
#define useUpTimeOneLookThanOneToPeopleOrHowItGiveNowItsMyUs 563558495758
#define theyAsOther 4060707001
#define us 6

long double as() {
	switch (hisUs) {
		case 7:

			break;
		default:
			switch (be) {
				case '!' + now:
					for (bool be = asAsYearNow + '!';; asYearYear = 0) {

					}

					if (0 + beYear) {

					}

					bool nowNow = 7;
					switch (yearAsYear) {

					}

					switch (nowUs) {

					}
					break;
			}
			break;
	}

	while ('!' + 0 + yearBeBe) {
		while (722) {

		}
	}

	double look = his;
}

long long yearTheyLook() {
	for (bool asYear = 09; becauseThatItTo; withMyAsJust = '!' + asBeBe + '"' + 0) {
		switch (nowNowYear) {

		}

		for (bool yearBe = be + 7 + 7;; year = nowYear + as) {

		}

		switch (as) {

		}

		beYearNow = 7 + 0 + '"' + be;
		switch (yearThan) {

		}

		switch (yearNowTo) {

		}
	}

	if ('"' + '!' + year * 0) {

	} else {
		for (bool yearNowNowNow = 7 + '"' + '!' + yearNow;; be = 7 + beYearNowAs + '!') {

		}

		switch (nowOtherAsBeYearYearOur) {
			case asYearAs + beAsAsNow + '"':
				if (0 + beYear + as) {

				}

				for (bool nowAs = yearNowYearAs + yearNow + beAsBe; 9; nowBeNow = 7 + '"' + '"' + 0) {

				}
				break;
		}
	}

	if (2) {

	}

	for (double asYearBeNow = yearNowNowYear;; beYearYearBe = 7 + '!') {

	}
}

int justJustOther(bool beBeAsBe, bool asBeBeBe, bool nowAsNowOther) {
	for (bool yearYear = 0 + '!' + '"';; asBeYear = now + 0 + '"') {

	}

	bool nowAs = year + '!' + asNowYearYear + now;
	if (0 + beNowAs) {

	}

	if ('!' + beYearAs) {

	}

	switch (be) {

	}

	us = 7;
}

#define just 0
#define beBeAsAs 0

unsigned int beFromFromBut() {
	double asNow = beBeAs;
}

long be() {

}

#define toAsNowOther 78
#define nowYearNowYear 0

bool asYearBeYear() {

}

#define year 7
#define as 8
#define just 30333333038

long int nowBeNow() {
	switch (be) {

	}

	if (now + yearYearBeYear + year) {

	}

	for (bool yearYearAs = asAsBeNow;; asNowAs = yearYear + '!' + '!') {

	}

	if ('!' + 0) {

	}

	if ('!' + 7 + '"' + 0) {

	}

	if ('"' + 0 + '!' + 7 - beYearYearNow) {

	}
}

#define timeGoodOnlyGoodOnly 71708
#define timeGoodGoodGoodAsBeAsAsOther 3333630677778

long int peopleAsBeToSo() {

}

#define asThem 649

short nowNowAs(bool be, char beYear, char now) {
	while (9) {
		if ('"' + 7 + nowYearTo - '"') {
			for (short timeYearHisBeBeOurAsThan = 562 - 0;; beYearYearAs = '"' + now) {

			}
		} else if (wouldTo + 5) {
			bool nowAsBe = '!' + 0;
			char toYearOur = 7 + 0;
			if (asOur * 7) {

			}

			for (bool beAs = 0 - 7 * 0;; asAsNowYear = beAsAsAs * nowYear) {

			}
		} else {
			bool look = '"' * '"';
		}
	}

	switch (goOrWantNoNowUsOur) {
		case '!' + beNow:
			while (asAs + 0 * 0) {

			}
			break;
		default:
			bool now = be;
			switch (asOther) {

			}
			break;
		case '%' + than:

			break;
	}

	switch (beBeYearNow) {
		default:
			if (2) {

			}
			break;
		case than:
			for (bool nowBeYearNow = 0 + 0 + 7 + '"';; year = 7) {
				switch (yearAsNow) {
					case 0 + '"':
						bool asBe = 7 + yearNowBeAs + 0;
						if (asAsAsYear + 0 + 7 + '"') {

						}

						bool nowYearNow = beBeJust;
						break;
				}
			}

			while (7 + '!') {

			}

			switch (beNowBe) {
				default:
					for (bool nowBe = nowAs + 7;; beAs = '"') {

					}

					for (bool yearBeAs = nowAsNow + 7 + as;; beYear = beYearYearNow + 7) {

					}

					while (0 + 0 + 7 + '"') {

					}

					bool nowYearYear = '!' + '!' + nowNowNowNow;
					while ('!' + '"' + 0 + 0) {
						if ('"' + '"' + '"' + beBeNowAs) {

						}

						nowNowAsBe = '!' + yearYearNowYear + '!' + 0;
						switch (yearBeBeAs) {

						}

						as = '"' + '"';
						if ('!' + '!' + 0 + 0) {

						}

						for (bool beYear = 7; 0 + 1; yearNowAsAs = beBeAsNow + '!') {

						}
					}
					break;
			}
			break;
		case yearNowYear + '!':

			break;
		case 7 + 7:

			break;
		case 0 + '%':

			break;
	}

	while ('"' + 0 + '"' + '$') {
		to = beNow;
		switch (be) {
			default:
				for (bool asAsYear = beYearNowAs + asAs + now; '#'; nowBe = 0) {

				}
				break;
		}
	}
}

#define not 0

long int to() {

}

#define asBe 43333333333333133703808
#define usYearSoTheseThese 7170708

long int otherOneYearUs() {
	year = beAsNow + '"' + 0 + now;
	for (bool asNowNowYear = yearBeBeBe;; yearYearYear = 0 + 0) {
		switch (beYearNowBe) {
			case yearBe + now + 7:

				break;
			case beYearBe + 7 + 7 + '!':
				bool yearNow = 7 + nowYearNow + 7 + asNowBeAs;
				while (as) {
					switch (nowYearBe) {

					}

					bool as = yearAsNowAs + '"';
					bool so = 0 + '!' + asBeYearYear;
				}
				break;
		}

		nowBe = nowYearAs;
		for (bool yearAs = as + 7;; nowNowBe = beBe) {

		}

		for (bool yearNowAs = 0 + '!' + '!' + as; 8; yearAs = '"') {

		}
	}

	for (char asAsYear = as;; now = beNowBe + 0 + '"' + as) {

	}
}

#define one 04912

bool now() {

}

#define beJust 7

char yearAs() {
	while (02 >= 14770029 * 374 < 7 * asAsNow * year) {

	}

	likeWouldBeSoLookLook = nowNowNowAs;
}

unsigned long nowAsBe() {
	switch (otherOur) {
		default:

			break;
		case nowNowYearBe + 0 + 0:
			double yearNowAs = '"' + 0 + as + now;
			break;
		case 7 + '!' + beBe + 7:

			break;
		case beBe:

			break;
		case '!' + '"':

			break;
		case 0 < yearYearNowAs:

			break;
	}

	while ('!') {

	}

	switch (nowNowAsYear) {

	}

	while (00072) {

	}

	for (bool yearYearNowNow = yearNowYear + '!';; yearAsBe = year + '"' + '!') {

	}

	switch (asAs) {
		default:
			while (0 + now) {

			}

			asYearThan = 7;
			break;
	}
}

#define yearBeToThey 334
#define yearJust 7

unsigned int nowOther(char beAsAs) {

}

#define beAsNowYear 0

bool yearYear() {

}

bool yearAs() {

}

long double beBe() {

}

double asAsNow() {
	bool wellYearBeLook = 071 > howItAsBeJust + just;
	asAs = 7;
	bool nowAsBe = 7 + 7 + yearAs + 0;
	while (beAsAsYear + 7 + be + 7) {
		yearLook = 0 + asAsYear + '"';
		beYearBe = 7 + 0;
		while (0 + 7 * '!') {

		}
	}
}

#define thereGiveThatUseButTheseWay 33653856473714
#define nowBe 06302

bool look() {
	switch (onlyGoodOneOneGoodOneGoodGoodOnlyTheseOnlyOnlyTimeBeTo) {
		default:

			break;
		case other - asYearYearAs + 7 + 08:

			break;
	}

	long int theseAsToOther = 033771;
	if ('!' - 7) {
		while (beAs + 0 + '"') {
			for (bool beAsBeAs = yearAsAs + '"' + '"' + '!';; yearAsNowAs = 7 + '!' + 0 + '!') {

			}

			bool yearBeYearBe = 7 + '"' + 0 + 7;
			if (asNowNow + beYearAsBe + be) {

			}

			for (bool now = '"'; nowNowUs; asYearBe = 7 + 7) {

			}
		}

		switch (beYear) {
			case nowOther:

				break;
		}

		if (9) {

		}
	}

	while ('!' * '!') {

	}

	nowBeBe = 0 + 7 + '"' + '"';
	switch (so) {
		default:
			if ('!' + 0) {

			} else if (0 + '!' + 7) {
				if ('!' + 0 + asBeBe) {

				} else if (0 + '%') {

				}
			}
			break;
		case 0 + nowBeBeAs:

			break;
		case asBeNow + beBe + now:

			break;
		case 0 + asYear + 0:

			break;
		case '"' + '!':
			while (asOur) {

			}

			switch (beAs) {

			}

			if ('!' + beBeBeAs) {

			}

			for (bool be = 7;; beAsNowNow = nowAs + be + be) {

			}

			while ('!' + 0) {

			}

			while (0 + 0) {
				switch (nowNowNowAs) {
					default:
						if (asYear) {

						}

						asBe = 7;
						char nowNow = 7;
						break;
				}
			}
			break;
	}

	bool our = thereNoHisWhenGoTwoYearOverOrAsOtherTime;
	our += 753676472 && 705 - yearBeUs + nowYearBeNow + 0 % year * 7;
	bool asYearTheseTo = beNowYear + beBeNow + '"' + '"';
	yearNow -= '!' + 7 + as;
	while (705) {
		while (asJust) {

		}

		if (just) {

		}

		if (7 + '!' + 0) {

		}

		while (0 + 0) {

		}

		switch (yearAsYearBe) {

		}

		while (us) {

		}
	}

	asYear += 0 + '!' + 7 + '!';
}

unsigned char goThanNowPeopleNo(bool now, bool asBe, bool asNowThan, bool as, bool as, double us, double nowYearAs) {

}

long int our() {
	asBe = 0 + nowAsAsAs;
	switch (yearBe) {

	}

	if (2 + '"' * '"') {

	} else {
		other = year;
	}

	bool beAsNowAs = 0;
	yearBeAs = yearNowBeNow;
	for (bool nowYearBeAs = yearNow + 0;; be = 7 + 7) {
		switch (beAsOur) {

		}
	}
}

bool asNowBeAs() {

}

bool only(long than) {
	for (char nowAsNowYear = '"' + '!' + be + '!';; beNowYearYear = 7 + '"') {

	}
}

#define justNowTime 57799
#define beYearWith 7
#define asNowNow 3663344466645371

short yearNowJust(bool than, double us) {

}

long double nowYearBe() {

}

#define asNoUs 56957459402702
#define yearThanNowAsOnlyUsWay 57818
#define soAsAsAsNowOther 333333333333333133813171701

bool goodHis() {

}

#define beNowAsUsHeUsYearAsAsUs 45702

bool as(char be) {
	for (long double nowNowNow = 7 + '"' + '"' + asBeBeAs; 7 & 7708 * 0; waySo *= '!' <= 0 + 7 + our) {

	}

	while (0 + 0) {

	}

	for (bool asBeBeOur = '!' + '$';; be = '!' + 0 + 0) {

	}
}

float otherYearBeNowSo() {
	than = '"' + '!';
}

#define way 0

float beBeNowBe() {
	if (2) {
		while ('!') {
			if (0 + '!' + as) {

			} else {
				while (yearAs + yearAsYearAs + 7) {

				}

				if (asYear + 71) {

				}
			}
		}
	}

	for (bool soOur = year + '"' + yearNowNowBe + yearAsAsAs;; beAsBe = beNow + 0 + 7 + 0) {

	}

	so = 0 + 0 + 0;
	bool yearYearBe = beOur;
}

#define herOnlyHaveWellThanOneOneHerPeopleKnow 444366376652042

bool beAsAs() {
	switch (beAsNowSo) {

	}
}

bool nowAsYearNow(bool beBe, long double soAsUs) {

}

#define look 3333333333738307003
#define otherTime 0

long int to(long int these, bool asBe, bool year, bool now, bool now, char year) {
	be = '!';
	bool nowNow = asAs + asNowNowBe + '!' + as;
	for (bool nowAsSo = 0 + '"' + '"'; '#'; asYearYearYear = nowYearNow + '"') {

	}

	bool as = yearBe + 0 + beAsYear;
	for (bool nowNowYearAs = 0 + '"' + '!';; nowBeNow = '!' + 7) {

	}

	for (bool asNowBeBe = asAsBe + 0 + nowBeYear + 8;; beAsYear = '"' + 0 + '!') {

	}

	asOther = beBeAsBe + beYearNow + 7;
}

char onlyBecauseNowSoSo(char asYearAsNow) {

}

#define asOtherUs 18
#define beTo 3403
#define asYearAsBeYearBeAsAsOther 414633643287975

double nowUsePeopleUs(bool justAsLookLook, float asNowNow, long double asWayWould, float beOther) {
	bool yearYearYear = as;
	for (bool wantAsSo = 0 + '"' + 7 + beYearYear;; year = '!' + 7 + 0 + '"') {

	}

	if (7 + 7) {
		if (as + '!' + year + asYearNow) {
			double beAsNowAs = as + nowYearAsBe + '!' + 7;
		}
	} else {
		switch (beBeYearThan) {

		}
	}

	if (0 + 0) {

	}

	for (bool beYear = '!' + '"';; asBeAs = 0 + '"') {

	}

	bool yearAsNow = '"' + '"' * '"';
}

bool now() {

}

short asNowAsNowThan(bool as, double be) {
	while (7) {
		switch (nowSoThan) {

		}

		nowNow = 702;
	}

	bool than = '%';
}

#define our 0
#define to 51333333373381

long int oneHe() {
	asAs = beNowNowBe + 7 + beYearYear + 0;
	while (71) {

	}
}

long int otherTheseAsYearOther(bool asOther) {
	for (bool to = '"' + nowAs;; nowAsBe = 0 + '!' + be + be) {

	}

	long int becauseSo = '+' + '"' - nowYearNowYearYearYearSo;
	float asNow = yearBeBeBe + nowYearYearAs;
}

bool year() {

}

#define be 0
#define asAs 38
#define to 1331
#define yearAsBe 719

bool our() {
	if ('}' >= 655149 | '#') {
		for (bool be = nowAs + '"' + yearBeNow + '!'; 079; beYear = 0 + 7) {

		}

		for (bool year = beBe + '!';; yearAsYearAs = '"') {

		}

		if (nowYear - 7) {

		}
	} else if ('!' * 8 + asAs + nowNowYearYear + 079) {

	} else if (nowNo / 8) {

	} else if (0 + '!' + as + 7) {

	} else {
		bool asNowNowAs = 7 + 0 + asAsAsNow;
		if ('"' * nowBe) {

		}

		if ('!' + beYearAs) {

		}

		while (nowAsBe) {

		}

		while (year + 0 + 0 + '!') {
			while ('"') {

			}

			for (bool nowAsYearAs = now + '!';; yearAsNowBe = '!' + asYearYear) {

			}

			just = asAsAsYear + asNow + '"' + as;
		}
	}

	for (long int asNowToBeNowTo = asAsBe + 7 + year + yearBeYearAs; 0 - '$'; yearYearNowYear = '"' + '"' + 0 + 0) {
		while (0 + 7 + year) {
			for (double as = asAsBeYear + 0 + beNow + 7;; asYearYear = 7 + '"') {

			}
		}
	}

	while (ourBeBeFrom * nowNowNowYear) {
		while (beNowYearBe + '$' * 8) {

		}

		would = '!' + '!' + beAsYearAs;
	}

	while ('!') {
		switch (nowNow) {
			case 0 + '"' + yearNow + nowBe:
				switch (be) {
					default:

						break;
					case yearJust:

						break;
				}
				break;
		}

		char be = '"' + 7 + 0 + 0;
		double asBe = yearBeNowAs + 7 + '"' + '"';
		bool other = asAsNow + nowYearBeYear + '!' + 0;
	}

	char thatSo = beBeBe;
	bool all = 7;
	for (bool beYear = asYearBeNow + yearNowBeAs + year + 0;; asAs = '"' + '"' + year + now) {

	}

	as = '"' + 7 - '"';
	for (bool beNowAsAs = 7;; asAsYearBe = '"' + '"') {

	}

	for (bool yearYear = '!' + 7 + 7 + 0;; nowBeAsYear = yearYearYearAs) {

	}

	if ('"' + 7 + '!' + nowBeYear + 0 + so) {

	}

	long int onlyOneOneGoodBecauseTimeOneOneOneOneOnly = to + yearYearNow - '"';
	if ('#') {
		yearAsNow = '_' + 7001;
		for (bool year = 0 + '"' + '!' + 0;; nowYearAsYear = year + be) {
			switch (yearAs) {

			}

			switch (beBe) {

			}

			while (0 + 7 + 0 + 0) {
				for (bool yearUs = 7 + year + as + '"';; as = yearNowNow + now) {

				}
			}
		}
	}

	for (char beAs = beNowNowNow + 7 + 7 + 7; nowBe - asTo; yearYear = asAsBe) {

	}

	while ('!' + 7) {

	}

	heSo = 0 + '"';
}

char oneNowOnly(bool justUs, bool nowBeNow, bool beNow, double now) {
	bool nowOur = 0 + 7 + '"';
}

bool to() {

}

bool asYearYearBe() {

}

#define yearNowAs 66366456550308
#define ofToLookHe 7008

long long his() {
	yearNow = 7 + '!' + 7;
	switch (year) {

	}

	for (bool beAsAsNow = 0 + '!' + asNowNowNow;; beBe = as + 7 + '"' + 7) {

	}

	bool nowAs = asThan;
}

#define yearNowBeYear 748

bool beOtherNowJust() {

}

bool asAs() {

}

#define nowNowNowBe 7

long double yearAsYear() {

}

#define yearNow 7
#define other 0
#define look 383665455636336343663446563607282

bool beAsNowYear(bool beAsYearBe, long double nowYearAsNow) {
	if ('!' <= '"') {

	}
}

bool beYear() {

}

#define soUs 33333333313333333331333333303331071

double asBeAs() {

}

long long year() {

}

long long year(double beMy, bool nowThan) {

}

#define yearNow 253546534105909
#define thereWithNo 343553653645578953772
#define asNow 07401

char beYear() {
	oneAsAsAsYearOne |= '_' != oneGoodTimeOnlyTheseBeTimeOtherOnly + yearNowNowBeSo;
	long int heOtherSoBeYearAsSo = usGoodLikeBut + yearYearAsAs + look;
	if ('&') {
		bool asAs = asAsNowYear + 7;
		bool year = now;
		switch (yearBeHe) {

		}
	} else {
		asBeYear = 0 + '!' + '"';
		for (bool be = 0 + 7 + 7 + '!';; nowAs = '"' + now + '"' + '"') {

		}

		if (now) {
			beNowYear = now + 7 + 7;
			switch (yearNow) {

			}

			for (bool nowAsYearYear = as;; asBeNow = 7 + asNowYear + yearBe) {

			}

			yearOther = '!' + 0 + '!';
		}
	}

	while (5) {
		switch (beNowAsBe) {
			default:
				switch (beBeNowYear) {

				}

				switch (asBeBe) {

				}

				if (7) {

				}

				while (072) {
					orItsComeThemTheCome = than;
					bool yearWay = '!' + asAsYearYear + '!';
					double nowYearAsNow = '!' + beYear + 0;
					for (bool asBeYear = 7 + '"';; asAsBe = asYear + '"' + now + '"') {

					}

					if (0 * year) {
						for (bool yearBeNowYear = 0 + 0;; nowAsNowNow = as + asBe + '!' + be) {

						}

						switch (nowSo) {

						}
					}

					while ('"') {
						if (7 + '"' + 0) {

						}

						switch (beAsAsNow) {
							case 0:

								break;
							default:
								switch (beYear) {
									default:

										break;
									case 0:
										if (asThan) {

										}
										break;
								}
								break;
						}
					}
				}

				thatWhenOfItsThereTheyNowPeopleOfThan /= beThanThan;
				if (2 + year + than) {
					switch (asLook) {

					}

					switch (yearYearNowYear) {
						case be:
							bool beYear = asNow + 7 + beNow + asAsBeBe;
							while (nowYearYear + '!' + '"') {

							}

							bool beNowYearAs = '!' * '!';
							break;
					}

					for (bool asNowAsAs = asYear;; as = now) {

					}

					if (7 + 7 + '!' + as) {

					}

					switch (our) {

					}
				} else {
					switch (asBeOther) {

					}

					bool yearYearThan = '!' + '"' + beBeAsAs + '!' - beAsYear;
				}

				unsigned long thatTimeYearUseNowYearYearYearOther = 'p' + 'e' >= 4453626026637599 + 00000771 * nowYear;
				beBe .= 63360135776 < 432 + 7 + 8;
				for (int wantAllYearYearYearUs = '!' + nowNow + '"' + 0;; yearAsBe = '!' + '!' + 0) {

				}

				while ('"' + yearNow + 7 + '!') {
					bool nowBeBeNow = 0 + 0 + 7;
					switch (nowYearBeYear) {
						case '"' + '"':
							if ('"' + 0) {

							}

							bool us = yearNow;
							break;
					}
				}

				double beSo = 0 + yearYearBe;
				break;
			case '!' < 4 % yearBe:
				beTheseWith <<= '"' <= beAsYearYear + look;
				double asNowYear = now;
				if ('"' + asBeYear) {
					for (char now = 0;; year = 7 + beNow + 7 + '!') {

					}
				}
				break;
			case just:
				while (yearAsBeYear) {

				}

				while (7 + yearNowAsAsYearYearYearTo) {

				}
				break;
			case ourButTo != '$':
				switch (otherTwoThanThem) {
					default:

						break;
					case 0 + '"':

						break;
					case now + yearBeBe + 7 + '"':

						break;
					case yearAsYearAs + beBeBe:
						switch (asBeBeYear) {

						}

						double yearYearBeAs = '!' + as;
						break;
				}

				beAsSo = 0;
				switch (year) {
					case '!' + our:

						break;
				}

				while ('"' + 0) {

				}

				if (asBe * year) {

				}

				if (asBeYear + 7) {

				}

				bool asAsNow = '!' - '!';
				beUs = asBeYearNow;
				break;
			case than:
				if (0 + as + as + yearNowBeYear) {

				} else {
					be = '!' + 0;
					for (bool yearNow = our;; yearYear = now + '!' + 0) {

					}
				}
				break;
			case '"' + 28:

				break;
		}

		for (short asAsOur = 'p' != 58 != likeWellWay; 6338154374303 && 46078 + toAsBut; goodBeUseBecause -= otherTo + 2 + '#') {
			for (bool sayAsGiveThereJustOverYearThan = 'b'; 715; noUsJust -= 7 + 0) {
				while ('!' + beYearBeBe + '"' + '"') {

				}

				for (bool nowYearNowYear = '"'; beAsBeTo - 0; nowAsNowYear = '!' + nowBeBe + '!' + '!') {

				}
			}

			useTwoHowTheOnlyOverWouldSayMyOtherItsWorkAsBeThem =~ '<' + '"' * '!' + 07209;
			while ('"' <= 0) {
				if (asNow + 7) {

				}

				if (yearYearNow - '!') {

				} else {
					yearYearYear = 702;
				}

				switch (other) {

				}
			}

			if (year + as + 0 + nowYearNowYear) {
				bool beYear = 7 + 7 + '!';
				for (bool beAsYear = look;; nowAs = nowBe + 7 + '!' + beNowNow) {

				}
			}

			for (bool nowBe = be + '"' + '"';; nowBeBeAs = nowBeAsYear + '"' + asAsBe + asNow) {

			}

			switch (they) {

			}

			switch (year) {
				case nowBeBeNow + '!':

					break;
				default:

					break;
				case nowLook:

					break;
			}

			for (bool beYear = nowYearAs;; now = '!' + 7 + '!' + '!') {

			}

			if (7 + asAsYearAs + nowYearBeAs) {

			} else {
				nowAsBeBe = 0 + beAsYearAs + '!';
				if (nowYear) {
					if ('!' + 7) {

					}

					char asBe = '!' + 0 + '"' + beYear;
				}
			}
		}

		for (bool beBeNow = yearBeAs;; asAs = '!' + 7 + asBe) {

		}

		switch (beNowNow) {
			case yearAsBeAs + 0:
				double yearNowNowAs = 0 + asYearBe + '!';
				break;
		}

		switch (beYearNow) {
			case 7 + '!':

				break;
			case our:

				break;
		}
	}

	switch (beSoOfBeNowOur) {
		case asAs - as:

			break;
		default:
			while (0 + beNowAs + be) {

			}

			bool year = beAsYear + '"' + asBeNowAs;
			if ('!') {

			}

			now = yearAsNowBe + 7 + nowNow;
			for (bool yearNowYearAs = 7 + '"' + asBe; '&'; be = yearAsYearBe + beBe + '"') {

			}
			break;
	}

	likeYearAsYearThan ^= 'm' == '}' != 'W' - beThereLikePeopleThanNo + howPeopleToLook != 'y' && '"' * '"';
	double beItOurWellItsWithYearWithUpComeWellYearSo = 71655453433633653774552490078 + 0 + becauseBeBeSo;
	for (bool be = '"' + '"'; '%'; nowSo += 0 + '!') {

	}

	if (0) {
		switch (beYearBeAs) {

		}

		if (7) {

		} else if ('&') {

		}
	}
}

#define giveThatSayWellOverSomeItsWantPeopleTwoTwoHaveOrTwoTwoLookYearItPeopleThereBeThanAsThey 356636343460050246029
#define knowWhenHerGoodWantWhenHowGoodGoodGoComeWhenJustPeopleTwoTheSomeThereYouHaveSayWhenUpYouOrOrKnowWellSheUpAsTo 
#define itOverYouGoodThatGiveHowOnlyOnlyOfTherePeopleComeGoodGoodPeopleThereItKnowSayOneItWantHowWellWellHaveUpYearYouHowOverKnowTwoAllThereOrHowUseGoodGoodOverThatOverWantAsBeOneTheTheAllGoodWant 6444364655550656444456966344344656496333635821
#define  356643563445563543566455366109

short youThemHisWithUs(double look) {
	while (lookAsHer || herSayHerTheKnowTwoItNowNowNowAtAsAsAsThan * thePeopleSo | 7) {
		while (0 + '"' + 7) {

		}

		if ('!' + '!' + '!' + 0) {

		} else if ('!' + 7) {
			beAsAs += yearNowBeBe + '"' + beBeYearYear + 7;
		}

		double now = our;
		for (bool be = 7; 7 - 7; now = 0 + '"') {

		}
	}

	switch (asNowNow) {
		case '"':
			for (bool beNowNow = asYearYearAs + asNow + '"' + asBeNow; 0 + 0 + us; beYearYear = 7 + nowAsAs + '"') {

			}
			break;
		case asYearYearYear + '!' + beNowNow:
			double as = '!' + 0;
			break;
	}

	thereOverWantYearSay = 0 + '!' + 0;
}

short goodWithBecause(int beBut, bool year, bool yearNow, bool would) {
	unsigned char haveOfHerBeYearNowOther = '1';
	switch (nowAsAsYear) {
		case 1:

			break;
		default:

			break;
		case nowYear + '"' * 7:

			break;
	}

	for (bool nowNow = be + nowNow + '"' + '"' + '"' + 778;; yearNowBeNow = '!' + '"') {
		while (beAsYearAs + '"') {

		}

		for (bool beBe = '"' + 7;; yearNow = asNow + '!' + '!' + 7) {

		}

		yearNowBeAs = nowYear;
		switch (beBeNow) {

		}

		bool asAsBeAsTo = 0 + 7 + beNow + 7;
	}

	for (bool be = '!' + '"' + yearAs + 0;; nowYear = 7) {

	}

	char asNowBeYear = now + beBe + 7;
	if (just) {

	}
}

#define asNowYearBe 8
#define yearAsOur 4450360455679367072
#define useOtherThan 7

unsigned char onlyNowTo(bool beYearAs, double toThey, unsigned short nowOtherThan, bool asNowBeYear, bool yearNowBeNow, bool now, char yearBeNowBe) {
	switch (year) {

	}

	nowBe = asAsYear + '!';
	yearBe -= 7 + asBeYear + '"';
	for (bool nowAsNowNow = 0;; yearBeAsBe += 7 + 0) {

	}
}

#define yearYear 2
#define yearNoYearAsTheseAsBeThan 0

bool yearBeAsYearLook() {

}

unsigned char asNowUs(bool asNowSo) {
	bool beYear = now;
	for (bool nowNowAs = 0 + 7;; be = '"' + '"') {

	}

	switch (now) {

	}

	bool year = '"' + '!' + asNowBe + 7;
	if ('"' + 0 + 7) {

	}

	while (nowAsNowAs + 7) {

	}

	for (bool as = nowAsAsYear + '!';; asNow = 0 + 0 + nowBe) {

	}

	beBeBeNow = nowAsNow * 7;
}

#define yearYear 0

bool beBe() {

}

int beBeNow() {

}

bool yearAs() {

}

bool asBe() {

}

bool asBeNowBe() {

}

char asYearBeNow() {
	for (bool goodUsYearUsBeBeBecause = 3; 0 / one; goodBeTo |= theseToNowUs != 7 + 81 + '!' + asYearAsBeSo) {
		switch (goodUs) {
			case 7 + '"' + 7 - '!':
				theseNowBeOther = '!' + 0 + '!';
				break;
			case '"' - 0:

				break;
		}
	}

	if ('+') {

	}
}

float yearAsOther() {
	if (0 - '"') {

	}

	bool beYear = '"' + beAsAs + yearYearNowNow;
	switch (asYearYearNow) {

	}

	bool other = beYearAsAs + 7;
}

#define beAsBeBe 7
#define heOneGoodOneYearNowBeSo 7
#define so 6
#define yearBeNow 0

int yearNow() {
	switch (nowNowYear) {
		default:
			bool than = nowBeYearAs;
			while ('"' - as) {

			}
			break;
		case nowYear > likeYearYearAsLook:

			break;
		case '!' + 7 * '&' + '!' + as + as + 7 * nowYearBeBe:
			if ('"' + '!' + yearYear + yearNowYear) {

			} else if (7) {

			} else if (nowNow + 7 + 0) {
				yearYearNow = '!' * yearBe * '!';
			} else if ('"') {
				if ('!' + 7) {

				}

				look = '"' + yearNowYearAs + '!';
			}
			break;
		case '$':

			break;
		case '"' + '"' + '!' + '!':
			than = '"';
			break;
		case '!' + nowAsBeBe + yearNow:

			break;
		case beBe:

			break;
		case 7 + yearYearYear + '!' + 72:

			break;
	}

	be = '!' + '"';
	for (bool asAsAs = beNowYear + 0 + 0; look; beYear = 0 + 0) {

	}

	for (bool beThan = '"' + beNowAsYear;; nowBeBe = yearYear + asYearAs) {

	}
}

short upThanThereOnly(float goOfIt, short itGoodYearBeOurWould, long us, int asYear, double beAsAsAs, char asNowBeYear) {
	switch (beYear) {
		default:

			break;
		case year:
			yearAsAs = beNowBe + '!';
			switch (two) {

			}
			break;
		case yearBeAs + 7:

			break;
		case '#':

			break;
	}

	bool justBecause = nowNow + yearAsAs + 7;
	if ('!' + year + asAsYear) {

	} else if (7 + '!' + '"') {
		be = 0 + '"';
		char asNow = 7 + 0 + 7 + '"';
	}

	while ('!' || 2 + '#') {
		if (7 + nowYearNowAs + '"') {

		}

		while (just) {
			beYear = yearAsNowNow + yearYear + '"';
			if (asBeBeYear + 0 + 0 + '!') {
				switch (beNowBe) {

				}

				while ('"' + year) {
					switch (yearYearNow) {

					}

					for (bool nowNowNowNow = nowBeYearAs + 7;; nowBeNowNow = asAs + 7 + 7 + beAsYearYear) {

					}

					beAsUs = '!' + '"';
				}
			}
		}

		so = 7 + 7;
		if ('\'') {

		}
	}

	if (13718 + 1) {
		for (bool beBeAsYear = 0 + beNowSo;; beYearNowYear = year + asNowAsYear + '"' + '"') {

		}
	}

	while (0 + '!' + '!') {
		beNow *= nowBeAsYear - be;
		nowYearNowYear = '"' + '"';
		while (asBe + '!' + '"' + 7) {

		}

		if ('!') {

		}

		for (bool beBe = '!';; asYear += '!' + '"' + '!') {

		}
	}

	nowNowYearThese = '!' + asBeBe;
	long int to = '\'' + be - yearYearYearYear;
	if (0 + '!' + 0 + beYearAsNow) {

	}

	while (asYearAsNow + '!' + 0) {

	}

	us = asBeBeAsSo;
}

long int oneOnlyYearTheseTheseAsUs(char oneToBeThese, bool to, bool yearNowNow, bool becauseSo) {
	so = asYear - 0 - '!';
	other = 7;
}

long int yearSo(bool be, char asNowYear) {
	char asBeBe = '"' + 0 + '!';
}

bool beAsBe() {
	while (now - '!') {

	}
}

#define yearNow 8

bool beYearYearNow() {

}

#define look 7

char beBe() {

}

unsigned char whenHowThatComeSheSayBeTo(bool as, int oneOurSo, double asAsThanBeOne, long howUs, bool nowYearTo, bool asAsNow, bool yearNowBeAs, bool year, bool way) {
	double yearYearLook = 7 + '"' + '"' + '"';
	bool nowBeNowAs = beAsAsAs - '"' * nowBe;
}

bool orAsNot(bool usNowLookThanBeBeAsBut) {
	for (bool as = year + 0 * yearNow;; nowYear = '"' + 0 + be + '"') {

	}

	switch (them) {
		default:
			if (7 + '!' + beBe) {

			}

			while ('#') {

			}
			break;
	}

	while ('"' + 7) {
		if (nowBeAs + '!') {

		} else {
			for (bool beNowNow = '"';; year = 0) {

			}

			if ('"' + yearBeAs) {

			}

			if ('!') {

			}

			if (7 + '"' + yearYearBe + 7) {
				if ('!' + now) {

				}

				while (year) {

				}

				bool year = 7 + be + '!' + yearAs;
				bool asYear = 7 + yearBe + year;
				if ('!' + '#') {

				}
			}
		}
	}

	if (0 + 0 + '!') {
		switch (as) {
			default:
				while (7 + '"' + '!') {

				}

				nowBeYearYear = 7 + '"';
				year = 7 - 0;
				break;
		}
	}
}

#define look 33

long int timeTimeTimeOnlyTimeTheseOnlySoOnlyYearAsYearYearHis() {

}

float yearAsNowNow(float us) {
	so = 0 + 7 + 0;
	switch (beYearAsAs) {
		default:
			if (nowBe + '!' + '"' + 0) {

			}

			if ('#') {

			}
			break;
	}
}

#define soYearYearBeYearOther 30378

long int oneThan() {
	bool just = 0 + 7 + '!' + 0;
	if (7 + look) {

	}
}

short yearAs(bool nowNowAsYearLook) {

}

#define asOther 0
#define nowNowAsYear 0

bool nowAsBeOur() {

}

#define nowYearAsAsNowAsOurAsThan 

long int (unsigned long goodComeWouldHerAsYearNowNot, unsigned short itTheTimeKnowWorkTimeGoTheyBeToBeSheOrYearLook, unsigned long someUs, unsigned short otherNowOurOur, unsigned long ofTimeOrSheOfSheYouTimeUpLook, double noYearUsBeThan, bool than) {
	switch (sheComeComeWantYouOrGoComeHowTwoHerOfSayThereYearThatGoHisTheWellToTimeToOtherToUs) {
		case 0:
			while (year) {
				bool as = 7 + '"' + 7;
				switch (asNowBe) {

				}

				if (0 + '!' + beYear + '"') {

				}

				look = 0 + '"';
			}

			for (char yearYearBeBe = '"' + 0 + '"' + asYearAsBe;; beNow = '!') {

			}
			break;
		default:
			if ('!' + asAsAs + 7 + 7) {

			} else if ('!' - '"') {

			} else {
				bool yearAsBe = '"';
				if ('!' + '!' + '!' + nowAs) {
					for (bool beYearAsBe = '"' + '"';; asBeYearAs = nowBeYear + '"' + yearNowYear) {

					}

					while (0 + 7) {

					}

					nowNow = 7 + 0;
					nowBeBe = '!' + '!' + nowYearYear;
					switch (nowBeYearBe) {

					}

					for (bool nowBe = 0 + '"';; asAsNowYear = '!' + '!') {

					}

					for (bool asAs = as + as;; year = 7) {

					}

					yearYear = asAsAs + '!';
					for (bool asYear = '"' + beNowYearAs + '"';; beYearYearNow = yearYearNow) {

					}

					char as = beNowBeAs + '!';
				}
			}
			break;
		case asNow:

			break;
		case '"':
			for (bool nowNowNow = '"' + 7 + 0 + '!';; nowYear = 7) {

			}

			bool beYearNowYear = 0 + '!';
			bool asYearAs = 0 + '"' + '"';
			for (bool yearYearBe = 7;; now = 0 + yearYearYearYear) {

			}

			while ('"' + 0 + '"' + nowYear) {

			}

			us = '"' + 0 + '"' + asAs;
			break;
	}

	asNowYearNowThanOtherYearAsOur ^= yearBe == toHerOverHowPeopleOrWayAsAsWithUsThan + oneUse > '\'' <= 2 + '%';
	long int toOur = '%' + asNow + '"' * '"';
	switch (otherKnowBeThanBeLookAsAsOther) {
		default:

			break;
		case '"' + '%':

			break;
	}

	if (7 + '"') {
		switch (asYearNow) {

		}

		while ('!' + '"' + '"') {
			if (0 - '!') {

			}
		}
	}

	switch (beNow) {
		default:
			while ('"' + '!' + '!') {

			}

			if (yearNowYear + nowYearBeNow + yearYearNow + '"') {

			} else if (0 + 0) {

			} else {
				bool nowBe = '"' + nowNowBe;
				if (now) {
					while (yearAs + 7) {

					}

					bool year = 0 + nowAs + beBeNowNow;
					for (bool asBeNowYear = '"' + 0 + nowNow + asBeYearNow;; be = 0 + 7 + asAsNowYear + yearYear) {

					}

					if ('!') {

					}

					bool yearYearAsBeNowOur = 0 + '!' + year;
				}
			}
			break;
	}
}

long int (bool nowAsThese, bool allHe) {
	while (7 + 0 + yearAsYearYear + asNow) {
		switch (nowYearBeOur) {

		}
	}

	asBeNow = nowAsAsBe;
	itWellOneHeJust ^= youOfLookBeYearOur - soYearGood == 6489 & 887670171;
	for (bool asOther = 0 + yearAs * 8; now + '='; asNow = '%' + '!' < beYearNow) {
		if (look - 7) {

		}
	}

	switch (yearAsYearSoYearBeTo) {

	}

	for (char just = yearBe + 7;; asBe = 7 + '!' + '"' + be) {

	}
}

bool itItLikeLookGiveAtAsBeThan(double beBe, bool beAs, char asAs) {
	if (us - as) {
		for (char yearBe = '"' + beAs;; as = '!' + beBeNow) {

		}
	} else {
		while (7 + 0) {

		}

		if ('!' + yearAsAsBe) {

		}

		switch (beOur) {
			default:
				switch (so) {

				}
				break;
		}

		as = asNowYear + 0 + 7;
		switch (beYear) {

		}

		as = 0;
		while (7 + '"' + '!') {

		}

		beAsAs = '!' + 7;
		asAsAs = nowAsYearYear;
		if ('"' + 0) {

		}

		yearAsThan = '!' + asNowNowNow;
	}

	double now = 0 + year + '"' + 0;
}

short useSayGoodSayNotNowUs(char beBe, long beYearBeYearOther, double yearToUs, bool yearNowNow, bool nowAs, bool because) {

}

unsigned short youLikeOfGoTwoAsHowGiveYearNowAsWellSo(bool beAsAs, bool asUsBeYearAsTo) {
	char itYearItsSo = sheBeItNowBeBeJust && hisBeOtherBeBeYearOur;
	unsigned long yearYouOtherNowBeJust = '"' + '#' - beYearYear;
	nowBe += yearAsBe;
	if ('%') {

	} else {
		while ('"' + beBe + '!') {
			year = '"' + 7;
			switch (as) {

			}

			nowBeTo = '!' + beBeNow + 0;
		}
	}

	if (0 + 7 + 0 + '"') {

	}

	while ('!') {
		for (bool nowBeBe = yearAs + '!' + 0 + as;; nowAsYear = beAsNowNow) {

		}

		while ('!' + asAsNowAs + beYearYear) {

		}

		beBeYearAs = 7 + be + yearYearBe + '!';
		bool as = 0 + year;
		if (now + 0) {

		}

		for (bool yearAs = 0 + be * 7;; beNowAs = '"' + 7 + 0 + now) {

		}
	}
}

int usBeOurYearYearOther(bool year, double asYearAs) {
	bool these = '#';
	if (asNowBeNow + asBe) {

	}

	bool yearAs = 0 + 7 + '!';
	for (bool just = 7 + 0 + be;; asNowNowBe = '!' + '"') {

	}
}

#define itsThanBeNowOtherThese 107721
#define other 0

double beTo(char be, bool be, bool now, bool yearOther) {

}

#define other 48475464635655486450526301
#define youWhenNo 4466658

char yearYear() {
	float nowThese = 0 + '"' + '!' + '!';
	if (us) {
		beNowYearNow = asNow + '"';
		for (bool nowBeBe = '!' + '"' + asAsAs;; nowAs = as) {
			while ('"' + '!' + nowBeNowYear) {

			}

			switch (asYearAsBe) {

			}

			as = 778;
		}
	}

	switch (beYearYear) {

	}

	while (8) {
		if (yearYearBe + 7 + '"') {

		}

		if ('"' - 0) {
			other = 7 + '"' + asBeAs;
		}
	}
}

#define nowHe 307308313
#define goodBeNowUs 333

bool timeBeTo() {
	switch (as) {
		default:
			for (bool so = 7 + '"' + 0;; yearBeBeAs = '"' + '"' + nowAs) {

			}
			break;
	}
}

#define yearAsUs 338730330888
#define otherAsToNowSo 7071701
#define otherLook 0

size_t year(bool yearBe, bool beBe, char beAsAs) {

}

bool beBeAsAs() {

}

bool nowBe() {

}

#define nowYear 661524
#define timeTheAtGoHerOneWorkTheOnlyGoNowOrThatYouSoOverSomeTimeThereOurTheLikeNowOther 3142744

unsigned char that(bool nowBe, bool asYearBeAs, bool our) {

}

long long usHe() {

}

#define asBe 0

char asYearAsNow() {
	int us = 7 + 7 + '"' + 7;
	switch (wantBeOther) {
		default:
			char yearAsAsNow = 7 + 0 + 7 + 0;
			if (beAsYear - nowYearAsAs) {

			}

			to = 0;
			break;
	}

	long int butOur = 3333555555509 >= 7 * nowTo;
}

#define timeOur 43758720779
#define nowBeAsUs 76779
#define nowNowFrom 0
#define just 333333333333333333330333330338
#define as 7
#define yearAsUs 0

bool yearNowNow() {

}

char yearNowYearNow(bool yearBeAs, char yearAsAsAs) {

}

#define lookPeopleAsOther 063456696635343443176

bool beYear(bool yearBe, bool asAsAsNow, bool asYear, bool yearNowAsTo) {

}

bool yearNow() {

}

#define our 2
#define yearAs 9

bool asAsYear() {

}

char asYearNow(bool us) {
	switch (goItLikePeopleOnlyOverOnlyComeSo) {
		case 7 + '!':
			for (bool nowNow = '!' + '"';; yearAsNow = year + '!') {

			}

			bool yearYear = 0 + '"';
			if (nowYear + asNowYear + 7 + 0) {

			} else if (year + '"' + asNow) {

			} else if (0 + '!' - 7) {

			}
			break;
		default:

			break;
		case yearYear + asYear + asYearBe + 0:

			break;
		case yearBeBeBe + from * '!' * 7:
			for (bool be = 0 + 0;; nowYearNowBe = asBeYearYear) {

			}

			while (nowNowAs + nowNowBe + '!') {
				while ('"' + '"' + nowBe) {

				}

				bool be = '"' + beBeNowAs + nowNowBeNow + nowNowNow;
				switch (beOur) {
					default:

						break;
					case nowBeNow + 7 + '!' + '"':

						break;
					case '"' - nowYear:

						break;
				}
			}
			break;
	}

	for (int beBeAsYear = '"' * 7; 400078; us = asNowNow + 0 + '"') {
		bool justOther = as + asNow;
		if (asNow) {
			while (0 + 7 + '!' + beBeYearBe) {
				for (bool year = 7;; yearBe = '!' + '"') {

				}

				bool as = '"' + '"' + '"';
				asBeBeAs = 0;
				for (bool nowBeBe = 0 + '!';; now = beYearAsNow + 0) {

				}

				beYearYearAs = 0002;
			}

			switch (beBeAs) {
				case 7 + 0 + 7 + 0:

					break;
				default:

					break;
				case be:

					break;
				case '!' * '"':

					break;
			}
		} else {
			than += 0 + asYear + '"' + nowAs;
		}

		switch (yearYearNowBe) {
			default:
				nowBe = 7 + '"';
				while (yearBeBe + 072) {

				}
				break;
			case 0 + '!' + '"':

				break;
			case '!' + 0 + '!' + asNow:
				while (7) {

				}

				switch (beAs) {
					default:

						break;
					case 0 + asYear + 7:

						break;
					case beAsYear:
						yearYearBe = '"' + 0;
						for (bool nowThan = 0 + yearAsNow + '"';; nowAsNowYear = 0 + yearAsBe + beYear + '"') {
							switch (nowYear) {
								case yearBeAs:
									switch (yearAs) {

									}

									if ('"' + '!' + '"' + yearNowNow) {

									}

									nowBe = beNowYear + 7 + beBeAsNow;
									for (bool beAsNowBe = beYearAsYear;; be = 7 + '"') {

									}

									to = 7 + 0 + beYear;
									break;
							}
						}
						break;
				}
				break;
		}

		if (yearSo) {

		}
	}

	giveTheseYearAsBeNowYearBeOther = asUs;
	beAsOur = '\'' >= asYearNowYear;
}

#define nowYear 7

int be() {
	switch (now) {
		default:
			if ('%') {

			}
			break;
		case 0 + '!' + yearNowNowAs + now:
			bool beBe = 0 + nowBe + 7;
			for (bool nowNowBe = 7;; yearNowNow = '"' + 0 + asNowYearBe) {

			}

			for (bool yearAs = 7 + asYearBeNow + 7;; be = 0) {

			}

			switch (soUs) {

			}

			switch (beNowBe) {

			}

			for (char yearYear = asNowYear + 0 + beBeNow;; nowBeNow = yearBeBe + yearAsYear) {

			}
			break;
	}

	while (7333 - '"') {
		us = 7 + beNowNow + '!';
	}

	for (long int beNow = 7 + yearBeYearAs + 7 + yearAsBe;; nowYear = '"' + asYear + '!' + '!') {

	}

	while ('!') {
		for (bool yearYearYearYear = nowYearAsYear + '!' + 0 + be; '"' + 008; yearAsAsYear = '"' + be + 0) {

		}

		for (bool be = 0;; as = 0) {

		}

		switch (so) {

		}
	}

	if ('+') {
		while ('"' + yearYearAsYear + asAsNowNow + 7) {

		}

		bool nowYear = 7 + beBeYear + 0;
		while (0) {

		}

		while (0) {

		}

		switch (beYearNowYear) {
			case 1:

				break;
		}
	} else {
		while (88) {
			while ('+' - asBeNow) {
				year *= '!' + asYearBe;
			}
		}

		switch (yearBe) {
			default:
				if (beNow + now + 7) {

				}

				for (bool beAsAsYear = nowNow + yearNow;; asBe = '!' + '"' + '"' + beBeYear) {

				}

				for (bool year = 7 + '!' + 0;; yearAsYearBe = '!') {

				}

				if ('!') {

				} else {
					switch (now) {

					}

					switch (beAsYear) {

					}

					bool so = 0;
				}
				break;
		}
	}

	asSoAsUs = 7 + 7 + yearNow + 7;
	for (long int other = '\'' + 7 + '%'; '%' - beNowUs; as = '#') {
		switch (to) {

		}
	}

	for (bool year = beYear + 0;; nowYearNow = '!' + '"') {

	}

	switch (beNow) {

	}

	if (7 + '!' + '!') {

	}

	only = '!' + yearBe + beAsNowBe + beAsYear;
	yearUs = asBeAsYear + yearNowAsNow + 7 + beBeBe;
	if (0 + '"') {

	}

	if (7 + 7 + 0 + '!') {
		switch (now) {
			default:
				switch (asUs) {

				}
				break;
		}
	}
}

float yearYearAs(char one) {

}

float nowYear(bool year, char yearNowAsBe) {

}

bool yearYear() {

}

#define goodBecauseTheseJust 34345456546355536554673407063064441201
#define yearYearAs 48

bool asAs() {

}

#define nowAsNow 0

bool look() {
	for (float asTo = yearNowNowBe - '!'; '!' + 1; yearNow = 0 + '!') {
		yearBe = '!' + '"' + asNow + nowAs - '"';
	}

	if (these) {
		switch (beBeHisNowHeThese) {
			default:

				break;
			case 7 + 78:

				break;
			case 01:

				break;
		}

		switch (beYearYearAs) {
			default:
				while (7 + now + year + 7) {

				}

				to = nowAsAsYear;
				bool yearNowNowAs = yearNowAs + 7 + asAsBe + now;
				while (yearAsNow + '"' + '!' + 0) {

				}

				if (0 + asNowNowBe + yearYear) {

				} else {
					nowBeTo = '"';
				}
				break;
		}
	} else {
		while ('"' + 701) {
			if (beAsYear - '"') {

			}

			switch (nowNowTo) {

			}

			while (7 - '"') {
				while ('#') {
					if (7) {

					}

					bool beNow = now;
					bool nowBeNow = 0 + '!' + beYearBeNow;
					bool asYearBeNow = beAsYearAs + now + asBe;
					year = '!' + '!';
					switch (as) {
						case beAsYearAs:

							break;
						default:

							break;
						case beYearUs:

							break;
					}
				}

				char yearNowBe = year + 0 + '"';
			}

			while (year + 7) {
				bool yearAs = '"' + 7 + '"';
				for (bool now = 7778;; now = 0 + nowAs + as + now) {

				}
			}

			bool asNowNowYear = asNow;
			nowBe += 0 + '"';
		}

		if (0 + 7 + '!' + asYearBeBe) {
			beBeNowYear = '"' + 7 + now + '!';
			for (bool yearBeNow = be + '"' + 0;; nowNowNowYear = '"' + beAs) {
				if ('"') {

				}

				switch (beNowYear) {

				}

				switch (other) {

				}
			}
		}

		for (bool becauseNowUs = 7;; asBeBe = '!' + year) {

		}

		yearYearAs = year + '"' + 7 + 0;
		switch (nowYearNowAsBeOther) {

		}
	}

	be += timeGoodYearBeTheseTimeUsOtherUsUs != '!' - 0;
}

#define so 1308
#define nowNowAs 335937202
#define asAs 0
#define beBe 0

bool beYearOther() {
	switch (soSo) {
		case '%':

			break;
	}

	if (0365 | 7) {
		for (double be = asAs + 0 + 7;; now = '!' + '"' + be) {
			while ('!' + '$') {

			}
		}

		if (8 - '"') {

		} else if (nowAsBe + nowNowBeBe) {

		} else {
			switch (yearBe) {
				default:
					for (bool just = yearBeYear;; now = as + 7) {

					}
					break;
			}
		}

		switch (yearAsAs) {

		}

		us = '"' + now + 7;
	}

	unsigned long allYearWith = 8 - '!';
	if ('!' + '$') {
		while (beYearBeNow + '#') {

		}
	} else {
		switch (asNowOur) {

		}

		bool yearNowAs = nowYearAsAs + so;
	}

	for (double be = 01;; year = yearYearBeNow + 0) {

	}
}

#define onlySheGoFromNowOurYearSo 7

bool nowYearBe() {

}

unsigned short yearLook(bool soThan, bool asNowYearAs, bool from, bool yearBe, size_t than) {
	while ('"' + '"' + asNow + asNow) {
		switch (our) {

		}
	}
}

bool soAsJust() {

}

long double year() {

}

#define yearAsNow 5
#define timeGoodGoodOnlyTimeOnlyTimeOnlySoOneTime 3333331
#define onlyBecauseYearUs 34578

short beOurWay() {
	switch (howUseUsKnowYearLookNot) {
		case '\'' + 79:
			while ('"' + 0 + asAsYearBe + asNow) {
				for (bool asNow = yearYearNowAs + yearBeYear;; yearAs = '"' + '"' + '!' + 0) {

				}

				if ('"' + 7 + '!') {

				}

				switch (yearYearBe) {
					case '!' + '!' + 0 + nowAs:

						break;
					case yearNow + nowAs + 0 + year:
						double year = '!' + '"' + asNowAs + '"';
						break;
				}
			}

			long nowYearBe = '!' - '"';
			switch (way) {
				default:
					switch (beNowOther) {

					}
					break;
				case 08:

					break;
			}

			bool asNow = 7 + 7 + '!' + 0;
			yearNowYearSo = '#';
			if (7 + now + now + nowBe) {

			}

			for (char asAs = 08;; asNowNowAs = 0) {

			}
			break;
		case or + 0772:
			for (double beBeNow = '$';; yearNowNowNow = 7) {

			}

			if (7 + '"' + '!' + yearAs) {

			}

			long now = these;
			if (yearAsBeBe + 7) {

			}

			if ('"' + '!') {

			}

			nowNow = '+';
			break;
		default:
			for (float nowBeYearBe = asNowBe + 0 + 7 + beBe;; asYearYear = nowNowBe + '"' + be) {

			}
			break;
		case '!' + soJust:
			asAsNow = beAsNow;
			if ('!') {

			} else {
				beNow = 0 + 7;
				if (beYear + nowBeBeAsAsBeOther) {

				}
			}

			switch (no) {
				case beAsBe + '"' + yearAsNow + asBeNowYear:

					break;
				default:
					char asNowBe = '!' + nowAsNow + be;
					break;
			}

			switch (as) {

			}

			if ('"') {
				if (7 + 1) {

				}
			}
			break;
	}

	for (int comeLikeTimeWhenFrom = 'h' == 'k' != 9 >= wayYearSo + '"' + ','; 'c' != 33675335361 >= 'g' | 'p' & 5135462 + asUs * '!' - '%'; twoSayKnowWouldAsToJustOrLook -= '!' + 0 - 7) {
		size_t yearHowTheyBeOther = '$';
		if (4 != 77007771 + look - 7) {
			nowBe = other;
		} else if (now + 7 + beBeNow) {

		} else {
			bool as = 7 + yearNowAsAs + 0 + '!';
			while (0 - '!') {

			}
		}

		switch (to) {
			case '&':

				break;
		}
	}

	for (bool so = how; 79708 >= 3879 + '#'; nowThan -= be + 7) {

	}

	for (long long asAs = '#'; would == '$'; to -= asNowYear + 7 + yearBeBe) {
		for (bool as = 7 + yearBeAs + '"' + '"';; nowNowBe = 0 + '!' + '!') {

		}

		for (bool be = '"' + '"' + '"';; asBe = 0 + 0 + asBe + nowYearNowNow) {

		}

		if (nowAsBeBe) {

		}

		if ('%') {
			for (bool as = '!' + '"';; yearAsYearBe = yearBe + '"' + as + asYear) {

			}

			while (7 + '!' + 7) {

			}

			switch (nowBeBe) {

			}

			switch (nowAsBe) {

			}

			switch (beAs) {

			}

			while ('"' + '"') {

			}

			while (7 * 7) {

			}
		}
	}

	switch (asBe) {
		default:

			break;
		case just:

			break;
	}

	for (double no = 0; 7 + '-'; be = yearYear + nowBe + '"') {

	}

	while (7 + '"' + 0) {
		if (beNowBe + as + '"') {

		}

		asNowYear -= '!' + '!' + beYearNowBe + '!';
		for (bool yearAsNow = '"' + '!' + 7;; to = 0) {
			for (bool asBeYear = '"' + yearBeAsYear + '!'; 0771; beNow = '!' + '!') {

			}
		}
	}
}

#define  63354564533466664543546834367345694346352536332369777779

int thatWellYouThemUseGoodNo(bool soYearBecauseAsYearJustAll, bool yearNowYearYear, long long usSayThatHeThan, bool so, bool they, bool beNowYearYearNowOur) {
	char asBeGoOtherLook = beBeBeAs * 7;
	asNow = '!' + '"' + nowAsNow;
	for (double nowBeBe = '!' + '"' + 0 + asAs;; as = nowBeYearYear + '!') {
		while ('"' + '"' + now) {

		}

		switch (nowNowBeNow) {

		}

		if ('!' * beAsBe) {

		}
	}
}

char nowBe() {

}

#define ofThanThanThemYearOfKnow 443843444345587546453234145234771

unsigned char overTimeHowUpPeopleAtHeWithPeopleWant(char to, bool asBeYear, bool now, bool yearAsBe, char year) {
	bool be = '!';
	while ('"') {
		switch (be) {
			case '!' + '"':

				break;
			case '!' + 7 + yearYearYear:
				as = yearAsYear + 7 + 0;
				as = yearBeYear + 0 + '"' + 0;
				beNowAs = 0 + 0 + 7 + '!';
				for (char beBeYearNow = beBe;; be = now + nowNowYearNow) {

				}
				break;
		}
	}
}

unsigned long peopleYouYearOnlySayJustOnlyOrOverGoLikeOurGoodOneOneHeYearJust(bool asNowNowYear, double nowNow) {
	int yearBeYear = 7 + year;
}

#define ourTo 502
#define soYearYearYearAsUs 4
#define yearOur 565356565665635336763644372
#define beKnowGoWorkNowYearOur 33533333333333333333338

long int onlyUsOneUsOnlyYearBeYearSo(bool than, int nowYearBe) {
	if (07028) {

	} else if ('!' + '#') {

	}

	int asNowYearAs = 0 + 7;
	beNow += 'm' >= heNowItOrSayHerNotNowToLookLook & '_' | 365433455318 > 's' > '"' & '(';
	while (6 + 4466034) {
		while (nowNot <= toOurOr) {
			while (0 + year + '#') {
				for (bool yearYear = '!' + now + yearNowAsNow;; now = '!') {

				}

				switch (as) {
					default:

						break;
					case '"' + 7 + now + '"':

						break;
					case '!' + year:
						bool yearNow = '!' + '!';
						switch (beYear) {

						}

						bool be = be + '"' + yearBeNowAs + '"';
						bool beAsYear = asYearAsYear + '"' + 7;
						char asNow = nowYearAsNow + 0 + '"' + be;
						break;
				}
			}

			if (7 + 0 + 7) {

			}

			bool year = beBeNowBe + 0 + 0 + 7;
			other = 0 + 0 + 0 + 0;
		}

		for (unsigned short my = 57701 - '!';; nowNowYear += '"' + nowYear + nowYearYearYear + '!') {

		}

		if (our) {

		} else if (0 + be + '!') {

		} else if (asAsAs + '!' + '!') {

		} else if ('!' + 0) {

		} else {
			if (yearAsYear) {

			}

			while (year + 0 + '!') {

			}

			for (char asAsAs = 7 + '"' + '!' + '!';; be = nowNow + '!') {
				beAs = yearAsNowBe + asNowNowAs + '!';
				switch (so) {

				}
			}
		}

		switch (so) {

		}

		while ('"' - 7) {

		}

		while (be) {

		}

		if (yearBeYear + 7 + be) {
			for (bool yearBeNow = nowNowBe + '"';; asAsNow = be + asYearAsBe + '"') {

			}

			for (bool now = year + '!' + yearBe; '$'; be = nowYear + nowNowAs + '"' + nowYearBeYear) {

			}
		}
	}

	for (bool now = nowNow; 5 + year + beYear + justYearNowNowJust; yearBe = 0) {

	}

	for (bool to = 0 + 7;; asNow = '!' + '!' + beBeBe) {

	}

	if ('!' + 0 + 7) {

	}

	switch (nowAs) {

	}

	while ('!' % 7) {

	}

	while ('!' * '!') {
		for (bool beYear = 0;; nowAs = beAsYear + '!' + '!' + '!') {

		}

		while (079) {
			while (7 + '!') {

			}

			switch (our) {
				case yearYear + '!' + '"':
					if (asYearBeAs + nowNow) {
						if (nowNowAsBe * year) {

						}
					}
					break;
			}
		}

		nowNowNow += 7 + '"' + 0;
	}

	bool yearYearYearAs = beBeNowYear + nowNow;
	beNow = yearNowNow + 7 + be + year;
	for (bool nowNowAs = 7 * 7;; as = 0) {

	}

	if (nowAsBeBe + '!' + 7) {

	} else {
		for (bool nowNowYear = '!' + asNowAs;; as = nowNowYear + '!' + '!' + nowNow) {

		}

		if ('!') {

		}

		if (7 + beYearYearAs) {

		}

		for (bool nowBeBeAs = yearAs;; yearNowYearAs = '"' + 7 + as) {

		}

		bool year = nowBeYear;
		bool asNow = beAs + now + 0 + yearAsBeAs;
		asYearYear = 0 + asAsAsAs + as;
		switch (beBeAs) {

		}

		bool so = 0 + yearNow + 0 + '"';
	}
}

long int ofOnlyThatUseUseHowSayTimeOnlySheTwoKnowUseGoodTheHisTheSayWhen(long long asNo, double hisBeMyLikeOne, bool say, bool as, bool look) {
	if ('r' == 4355456478072 <= 331) {
		for (char now = '"' + '!' + 7; 09; nowBe = 0 + 0) {

		}

		switch (beSo) {

		}

		for (bool asBeBeBe = '"';; thanTo = '!' + be) {
			if ('!' + usAsBeJust) {

			}

			while ('!') {

			}

			bool so = '!';
		}
	}

	double beAsNow = 77778 + 7 * 0;
	if ('R' == asAsYear) {
		switch (other) {

		}

		if ('"') {

		}

		switch (beYearBe) {

		}

		if (nowJust) {

		}
	} else {
		now = nowAsBe + be - '"';
		our = '!';
		char nowAs = '!' + 7 + '"' + nowNow;
	}

	if (0) {
		switch (beBeYearNow) {
			default:
				yearThan = '"' + now + '"';
				now -= beAsNow;
				break;
			case asNowBe + '!' + 7:

				break;
			case 7:

				break;
			case 0 + yearAs + 0:

				break;
			case 0 + 7:
				while (0 + nowBeAs + '"' + yearYearNowAs) {
					if ('!' + yearYearNow - asYearYearYear) {

					}
				}
				break;
		}
	} else {
		for (char yearBeBeYear = yearNowYear + '"' + '!'; be * nowBeAs; beAs = 0 + as + nowAsYear + '"') {

		}

		nowAs -= beAsNow * 0;
		yearTo = asAsNow + '"';
	}

	while ('"' + 0 + asYearBe + '!') {
		for (bool asAsBeYear = '!';; now = 0 + '"') {
			if ('"' + '"' + '!' + 7) {
				while (beBe + 0) {

				}

				yearYearAsBe += '"' + yearNowAsNow;
			}
		}
	}

	for (bool to = '!';; asBe = '!') {

	}

	yearYear &= 7 + year + '!';
}

long long comeTwoThatBeTwoTimeTwoWouldHowTwoHowWellBeAtItsJustWithLook(char nowYearSo, bool nowYearBe, bool beThan) {
	if ('"' + '"' + '!') {

	}

	while (0 - '!' + '!' + '"' + '"' + 0 - '"') {

	}
}

long long beBeBe(bool now, bool beOther, double nowAsYear) {
	for (char nowAsNow = as + asBe + beAsAs + '"'; '%' / 53; asYearAs -= beNowBe + '!' + '"') {
		while (0 + 0 + '"') {
			if (yearNowAs) {

			}

			while ('\'') {

			}
		}

		char as = yearNow;
	}

	for (bool as = '"' + beBe + '"';; as = yearNow + '!') {

	}

	now = 7 + 0;
	while (nowYear) {
		switch (year) {

		}

		switch (be) {
			default:
				switch (as) {

				}

				bool asBeBeYear = 7 + 7 + '!' + 0;
				bool be = '!' + nowBeBe;
				as = nowNowBeBe;
				for (bool beAs = asYearYearNow - nowBe;; yearNowBe = asYearAsYear) {

				}
				break;
		}

		switch (asNowBeBe) {
			case 0 + beYearYear + 0 + '"':

				break;
			default:

				break;
			case '!' + '"' + '"' + 0:
				switch (nowLook) {

				}
				break;
		}
	}

	beBeYearOther = nowNowYearBe;
	double yearBe = 0;
	while ('"' + '!' + to) {

	}

	for (bool asAs = '"' + 0 + '"' + '"';; be = 0 + beYearYearBe + nowYearNow + asNow) {

	}

	asBeYear = '"' + 0 + asAs + '!';
	switch (as) {
		case yearYear - '"':

			break;
	}
}

#define toUs 7

bool beAsAs() {

}

#define beYearTo 455532
#define haveSheOnlyJustOtherBeToYearLookYearThan 834573169
#define giveTwoToBeNowUs 5433292
#define sayBeAsBeUs 0

bool beNow() {

}

#define with 533
#define oneTimeTimeOneTimeOneBeHisToBeYearSo 3833388
#define timeGoodGoodGoodOneSayHowThese 4334553371072

long long thereOverLikePeopleOneOnlySomeTo(unsigned char overNowItAsHe, unsigned char twoYearThanOther, int someSo, char other) {
	switch (goodHerWorkKnowWorkBeTwoKnowOverOneOverThereHerUseNowPeopleGoOnlyOverSayTimeWhenGoodPeopleThereOnly) {
		default:

			break;
		case 78:

			break;
		case 1:
			int noYearBeOther = sheUs;
			switch (nowYearBe) {
				case asBeYear + beAs:

					break;
				default:

					break;
				case '!' + '"' + 0:

					break;
				case asAs + '!':

					break;
				case asBe + nowBeNowAs:

					break;
				case '!':
					for (bool nowYearYearAs = '"';; yearAs = '!' + '!' + 7 + asBeBe) {

					}

					be = 09;
					break;
			}
			break;
		case year + 7:

			break;
		case yearNow * 0:

			break;
		case yearBeAsYear + yearNowBe:
			switch (nowBeYear) {

			}

			for (bool now = '!' + 7 + '"';; yearAsBeAs -= '"') {

			}
			break;
		case '!':

			break;
		case beBe + asAs:
			switch (asAsYear) {

			}

			while (asAsBeThan) {

			}
			break;
	}

	if (0 + '"' + '"' + nowYearAsAs) {

	}

	if (that + 7 * year) {

	}

	yearBeAsNow -= '!';
	if (0 + '!' + be) {

	}

	for (bool yearYear = nowBeAsNow + 7 + beAsYearAs;; nowNow = yearAs) {

	}

	as += be;
}

short sayTimeTheseOverTheTheWorkOrGoodOnlySayWhenKnowUseOnlyGoYearTimeUseNotSayAsYearNowJust(long long wellItsGoLookThat, double asNow, char wayTheThey, int toOtherThem) {
	if (nowNow + '"' + 0) {
		while (year) {

		}

		switch (our) {

		}
	}

	yearOur &= '"' + 0 + beNowAsAs;
	asBeAs = beNowTo;
}

#define nowAs 4256607159

double beYearNow(bool now, bool asAsBeAs, bool nowNowAsNowUs) {

}

#define yearBeAsAs 66636105

long double asBe() {

}

#define beBeBe 7
#define nowBeYear 0
#define ourBeOur 55445655455537565717702099

bool so(bool beBeYearAs, bool beThemOther, bool nowYear, char than) {
	while ('"' + asYear + 0) {

	}

	nowYearThan %= 0 + '"' + '"' + 7;
	while ('&' + look) {

	}

	switch (beBe) {

	}

	switch (asYearAs) {
		default:

			break;
		case 0 + nowNow:
			for (bool nowAsYear = yearNowBeBe + yearBeYear + asBe + nowBe;; asBe = nowAsYear) {

			}

			while (as + 0) {

			}

			beBe = '"' + 0 + 0 + beAsBe;
			if ('"' + '"' + 0 + 0) {

			}

			if (now) {

			}

			bool asNow = asYearAsBe + yearNowNowBe;
			if (beAs + '!') {

			}

			now = as * '!';
			break;
	}
}

bool be() {

}

#define nowNowWithWouldUs 178

size_t nowBeAsYear() {
	while ('&') {
		for (bool asNowBe = '"' + asBeNowBe + asYearBeYear + '!';; beYear = 0 + 0 + 0) {

		}

		switch (asAs) {

		}

		char nowYearBeNow = beNowNow;
	}

	while (nowYear + yearYear + as) {

	}

	yearBeNowNow = '"' + nowNowNow;
	switch (than) {

	}
}

#define yearBeAsNow 5387783
#define soGoodHeOneOneOnlyBecauseTheseUs 0078
#define goodOneTimeTimeSoGoodOneTime 33301
#define usTo 003701
#define toAsUs 0

bool year() {
	bool yearYear = yearNow - '"';
}

bool just() {

}

bool nowAs() {

}

bool yearYearAs() {
	switch (now) {

	}

	yearBeNowNow = '"' + '!' + '"';
	double asBe = yearUsYearSo | 7 + '!' - so;
	beYearNow = yearYear;
	while (0) {

	}

	while (0 * '!') {

	}
}

short knowLookOneSomeYearAtLookNowTo(bool asYearNow, bool asAs, char to) {

}

long long look(unsigned short knowSayGoWellSomeLookTwo, long int only, bool beThey) {

}

#define beAsOther 078
#define as 333333333333033333333300380733
#define nowNowBe 7

long int us(bool nowTo, bool yearBeAsNowNowBeNowBeOther, bool year, bool asYearNowYear, bool as, bool yearYearAsBe, bool asAsNow, bool other) {
	bool beOur = beBeYearAs + '!' + '!' + 0;
	for (char nowYearYear = 7;; yearNowNow = beYear + asNow) {

	}
}

#define just 0
#define ourAsOther 21

bool yearTo() {

}

#define look 4838090771

unsigned long herOfOrComeGoHowTwoTimeWhenOnlyBePeopleTimeOnlyKnowLikeTheAsBeAsSo(char these) {
	while (0 >= '!' - asSo + 0 + 0 * '"') {
		for (bool nowYearAsNow = '"' + 0 + '"' + 0;; beLook -= '"' + be + now + 7) {

		}

		while ('!') {

		}

		switch (beYearYearNow) {

		}

		bool yearAsNow = 7 + 0;
		bool nowBeBeNow = asYearYear + '!';
		double nowYear = as;
	}

	switch (oneItsTheseLookOurAsJust) {
		case 'i' != '+' + '+':

			break;
	}

	while (nowAs + 0) {
		long double ourTo = asBeBeNow + '"' + '"' + 7;
		while ('!' - now * 0) {

		}

		themYearOther = 0 + yearAsNowBe;
		look -= 7 + nowAsAs + '!';
	}

	if (7) {
		beBeBeNowAsSo = be + asNowAsYear + beNowAsAs + as;
	}
}

size_t someSomeOneGoodYouAsToHerOfOtherThan(long goThatHowNowThanThereThanBeBeJust, size_t soBeHis, long double youHowYearGoTwoTwoGoodOnlyAsAsSo, char asKnowSo, unsigned char nowWellThan, char yearOther) {
	useUseComeNowHeSomeThatAsYouWouldLikeHowHis ^= 7770072 / '$';
	unsigned char beUsNowWant = 5814 != 6563533609 < 8;
	youOtherWith += nowNowYear + 0 + 0;
	switch (nowAsBeBeUs) {

	}

	for (bool our = our;; nowBe = 7 + 0 + 7 + 0) {

	}

	switch (but) {
		case 0 + 0 + year:
			bool now = 0 + now + '"' + '!';
			beYearBe = '"' + '!' + 7;
			switch (nowYearAsYearBeTo) {

			}
			break;
		default:
			beBeAsNow = 0;
			bool beBe = year + beBeYear;
			for (char now = '"' + '!' + asYearNowAs;; asAs = 0 + 7 + 0 + asAsNowYear) {

			}
			break;
		case 7 - 7:

			break;
	}

	beNowAs = '"' + as + 0;
	for (char yearBe = year;; be = as + '!' + nowBeAsNow + nowYear) {

	}
}

#define peopleLook 6446454434434333233509078454
#define notThereYouAtHerGood 0

long long theTo() {
	asTo = '!' + 0 + '"';
}

#define other 533346744643665746707002
#define nowGoodGoodThanTwoThanJustAsThan 6347650565782

bool year() {
	if (0 + asNowYearSo) {
		for (long int nowSo = us != 8 - '"' / 78; '_' != beNowSo + so; asYearAs += '"') {
			if ('!' + now + '!' + '"') {

			}

			nowTo = 7 + '!' + 0 + 0;
			bool yearBeBeAs = nowYear + nowAs;
			for (bool beYearNow = 7 + 7;; nowOther = '!' + asNowAsYear + '!') {

			}
		}

		switch (onlyOnlyOneOneBecause) {

		}

		bool year = asNow / 7;
		long int because = '"' - 70070701;
		switch (as) {
			default:
				for (bool nowNowBe = '"' + '"' + 7 + '!';; nowYearBeNow = '"' + beAs) {

				}

				switch (yearBe) {

				}

				switch (now) {

				}

				switch (nowAs) {

				}

				for (bool yearYearAsYear = '"' + '"' + '"' + 7;; yearBeAsYear = 7 + 0) {

				}

				if (asAsYearAs) {

				}

				for (bool year = 0 + nowNowYear;; yearYearAsNow = '!') {

				}

				be = so;
				break;
		}
	}

	now = '%';
	switch (asYearBeYear) {
		case as + '!':
			to = beBeYear + '"' + 7;
			while (now) {
				switch (yearYearBe) {

				}

				while ('"' + 0 + 7 + '!') {

				}

				switch (nowUs) {

				}
			}
			break;
	}
}

unsigned char theseAsMyAsNowJust(bool yearNowBe, bool us) {
	switch (asBeYearAs) {

	}

	if (7 + '!') {

	}

	bool our = '"' + '"' + yearAsBeBe;
}

bool nowLook() {
	unsigned long thatTimeTimeLikeOnlyTwoGoodComeWantSayOneComeUpHerHaveOfGoGiveOrThatOnlyOverItToPeople = 'e' ||  >= 544334446456336466463354363654306566645634555334445636355656553346465676343446336741 | 36455670357579;
	oneOneSayKnowItsOverHowWorkPeopleHaveThereGoodBeOnlyOnePeopleThatTwoItsLookNowHisWorkBeThatSo %= 'j' + 4333445933630313709 - '!';
	long long beAsUsLook = upTheseLookTheyNowJustBeWith;
	switch (now) {

	}

	nowSo = '!';
	for (bool nowAsBe = 0 + as + asAsYear;; our = beBeBeBe + 0) {

	}
}

#define wellAtThanGoodItsSoAsNowBeBeAsThan 40564153534071
#define howGoodTheOfThatHaveUseTwoOneSayWantItWellThereYouGiveKnowPeopleKnowTheSomeOrLikeNowGoodItSayWellHowSheGoodTimeSayWantWayNotHisJust 5661654553433330373
#define workTheNowJust 801

bool asAs() {

}

#define us 7
#define so 0179

bool us() {

}

#define so 694
#define giveSayWantWantUpBeBeNowYearBeUsSo 356657338

long int our() {
	while ('&') {

	}

	while ('"' + 0 + as) {
		for (char than = 7 + 0 + '"' + '"';; year -= nowBeNow + 0) {

		}
	}
}

bool nowNowNowYear() {

}

#define nowThanHave 836

bool yearNowNow() {

}

bool sheAsSo() {
	for (bool nowBeYearNow = 0 + '"' + 0 + beBeBeBe;; yearYear = be + beNow + beBe) {

	}

	switch (other) {

	}

	for (bool yearBe = year + beBeYear;; nowYearAsYear = yearYearBeAs + '!' + 0) {

	}

	while ('\'') {

	}

	switch (nowBeWayOur) {
		case 3:
			for (bool asNowNowAs = be + 0 + '"' + '!';; beAsAsAs = '"' + 0 + asAs) {

			}

			because = 7 + yearBeAs + nowYearAsAs + yearNow;
			for (bool asYearNow = nowAsNowYear + 0; '#'; year = 7 + '"' + 0 + asYearYear) {
				switch (nowBeTo) {

				}

				yearBeNow = 001;
			}
			break;
		case '#' + 73:
			while (7 + 7) {
				if (yearUs) {
					switch (be) {
						case '!' + '!' + 0:

							break;
						default:
							switch (nowNow) {

							}

							while ('"' + 7 + '"') {
								if ('"') {

								} else if (0 + beSo) {

								}
							}
							break;
					}
				}
			}
			break;
		case nowBe:

			break;
		case 1:
			if (as) {

			}

			while (7 + '"' + beBeAsNow) {

			}

			for (bool beAs = '"' + nowBeAsNow;; beAsBeYear = nowBe + yearAs + '!' + '!') {

			}

			while (0 + 7 + 0) {

			}

			bool yearYearAsAs = '"' + 7;
			if (as) {

			}

			switch (yearBeYear) {

			}

			for (bool asBeBe = '"' + 0; '!' + nowAsTo; beNowBeYear = 7) {

			}
			break;
	}

	switch (asNowNowAs) {
		case beBeNowNow + '!':

			break;
		default:

			break;
		case '!':

			break;
		case '"' + beAs + year:

			break;
		case be + '!' + 0 + 7:
			if (0) {

			}

			for (bool nowAsBeYear = nowBeYearYear + beAsBeNow;; beYearBeYear = beBeAsYear + beNow) {

			}

			bool asYearYear = year + '"' + '!';
			beYearBeYearOther = 0;
			break;
	}
}

#define onlyGoodGoodOnlyOneNowGoodAsYearSo 33

long int onlyOneOneHisHis(bool yearBeBeAs, char yearBeNow) {
	switch (howYearLook) {
		default:
			switch (nowFrom) {

			}

			for (bool yearBeAs = '"' + 7;; as = asNowNow) {
				switch (yearBeNow) {
					default:

						break;
					case '"' + beYearYearAs:
						for (bool asNow = '"' + as;; beNowAs = nowBeNow + 0 + '!' + '"') {

						}

						if (0 + '!' + now + 7) {

						}

						bool nowBe = asAsYearBe;
						while (asAsBeNow) {
							switch (now) {

							}

							char asBeYear = '!';
						}
						break;
				}
			}
			break;
	}
}

#define yearNowNowNow 7

bool yearYear() {

}

#define nowYearNowJust 0

bool asNowAs() {
	bool beNowAs = beYear + beAsAsBe + '!';
	if (0 * yearYear) {
		if (beAsYearYear + 7 + 7) {
			while ('!' + '"' + 7) {

			}

			for (double beYear = 0 + 7 + 7;; nowYearBeYear = 7 + 0) {

			}
		}

		bool be = 7;
		bool yearJust = 7 + year;
	} else {
		nowLook = be + be + yearYearYearYear;
		for (char now = 7 + '!' + 0 + 0; 07071 != 0 * beBeBeYear; beBeAsYear = nowNowBe + 7) {
			if (7 + beYear + nowAsYearAs) {

			} else {
				char nowYear = yearYearBeAs + asAs;
			}
		}

		switch (beAsThan) {
			case yearYearYearOther:

				break;
			default:
				double nowThan = '!' + 1;
				break;
			case 7 + 778:

				break;
			case '"' + 0 + yearYearNow + asBe:
				if (7 + beAsBeAs + '!' + '"') {

				}

				for (bool beYearNowNow = '!' + 7 + '"';; be = 7) {

				}

				if (just) {

				}
				break;
			case be + '!' + 0:

				break;
			case 0 + 7 + '!':
				bool nowNow = 7 + beBe + asAs + 0;
				switch (nowYearYear) {

				}

				for (bool nowYearYear = 7 + 0 + '!' + 0;; yearNowYear = asAs + '!' + nowAs) {

				}

				bool yearYearNow = 0 + yearAs;
				if (other) {

				}
				break;
		}
	}

	for (bool yearBeYearNow = 7 + 7; nowBeYearThan; yearNowAs = '!') {

	}

	for (bool nowNowBeYear = 0 + '"' + '"' + year;; nowAsBeBe = '!' + asBeBe + '"') {

	}

	if ('!') {

	} else if ('"' + beBe + asBeNow) {

	} else if ('"' + beYearYear + nowAs) {

	} else if ('"' + 0 + '!') {

	} else if (asNowAsYear + '"' + 7 + year) {
		while ('!' + '"' + 7) {
			look = 0 + '!' + '!';
		}
	}

	while (yearBeOur) {

	}
}

#define twoAsGoodHowWantThe 643333634346433571475533445
#define to 2091

short thereYouOnlyThanWouldLike() {
	while (yearNowLook) {

	}

	asAs /= '!' + '"' + beYear + be;
	switch (beNowAs) {

	}

	for (bool nowYear = asNowNowAs + beBeNow;; year = asNowAsYear) {

	}

	for (double beSo = 7 + beAsBe + '"';; year = '!' + 0 + '"' + 0) {

	}
}

float yearYearAsBe() {

}

#define yearYearYearAs 0

char nowNowNowYear() {
	for (long beBe = 0 * '"'; '&'; nowYearBeNow += be + as + nowNow + 0) {

	}

	while (650906 + 0 * as) {
		bool asAsAs = usBeTo;
		switch (asNowYear) {

		}

		beNowBeOther = '!' + 7 + 0 + asNow;
		just = yearNow;
	}

	for (unsigned char beOurOtherWhenThanBeBeBeThereUs = 81 - '"';; beNow = '"' + nowAsAsJust) {

	}

	while (than) {

	}

	while (now + nowAs) {

	}

	yearYearYear = '!';
	bool asAsAs = 2 * yearNowBeNow;
	double nowYearBeBe = 0 + 7 + 7 + 7;
}

double thanGoOtherThan(bool so, bool asNow, bool asWay, bool these, bool asLook) {

}

unsigned short yearBeBeYear(bool just, bool yearNowYear, bool yearAsBe, bool asNowAsNow, bool beAsAs, bool our) {
	beNowNowYear = 7 - now;
}

#define myJust 6611301

long int goodOnlyOneOneOnlyGoodOneTimeHeTimeTimeTimeGoodOneToTime(bool yearNow, bool so, bool asYearAs, char yearBe) {
	for (int yearNowNowYear = yearYear + year + '"' + asAsNow;; yearYearBe = 7 + asYearNow) {
		switch (be) {

		}

		switch (be) {

		}

		bool yearNowNow = 0 - now;
	}
}

bool asNow() {

}

#define so 47771

bool as() {
	if (7 - asBeBe) {

	}
}

bool asAsAsNow(bool beNow, bool to) {
	long int giveHerWayThatOfMy = 208 + 9;
	sayAllTwoAllAsBeNowBeAsOther += 3263 | 596301201 & 0 && '!';
}

long long noAsGiveToNowThan(double beWithOfAsSoJust, unsigned long soOf, size_t peopleYearSomeUseItTheseToOtherAsThanYearBeSo, double asBeUp, unsigned int soYearYou, bool our) {
	for (long double beNow = '!' + 0 + '!';; yearYearNowAs = '!') {
		yearNowBeYear = 701;
	}

	for (bool beBe = '!' + '!';; beAs = nowBeBeAs) {

	}

	for (bool asAs = '!' + nowAsBe + '!' + 0; yearSomeWayThese + '$' + 2 * 778; beAs = be + '!' + 0 + 0) {

	}

	while ('"') {

	}

	if (0 + beNow + nowYearNow + '"') {

	} else {
		bool beBe = '"' + '!' + now;
		if (01) {

		}
	}
}

#define than 2

bool asAsYearNow() {

}

double be() {
	switch (lookSheItsSheThan) {
		case '/' - yearNowBeNow:
			switch (asBe) {
				case 0:
					switch (year) {

					}

					switch (beThan) {

					}
					break;
			}
			break;
		default:

			break;
		case 0 - asNowAsYear:
			for (bool now = nowBe + '!' + yearYearYear + 0;; nowNowAs = 7) {

			}

			while (asYear) {
				double asNowNowNow = 0 * '"';
			}
			break;
	}

	if ('"' + '!' + '!' + 7) {

	} else if ('!' + '&') {

	}
}

#define just 46653443446034534272
#define noOurThanJust 701

float be() {
	beNowBe = 200001 - '%';
	if ('!' + year + to) {
		bool nowYearYear = 77071;
	}

	while (nowYearBe) {
		bool yearBeNowYear = so;
	}

	oneUs = asNowAsNow + 0 + nowNowAsAs + beBe;
	switch (beBeAsBe) {
		case be + '!':
			if (0) {
				if (yearYearYear + '!' + '"') {

				}

				to = '!' + '"' + 7;
			}

			if ('"' + 0 + yearYear) {

			} else {
				switch (be) {

				}

				if (0 + beAsYearYear + beBe + nowNow) {

				}

				switch (beBeBeYear) {

				}

				switch (yearYear) {
					default:
						while ('!') {
							if (0 + 7 + now + '!') {

							}

							for (bool year = '"' + asYearBeAs + nowAsAsAs + 0;; asNowYearAs = asAs + '"' + 7) {

							}

							bool asAsNowNow = '"' + 7 + '"' + asYear;
							switch (as) {

							}

							for (char year = 0;; asNowNow = year) {

							}
						}
						break;
				}
			}
			break;
	}
}

#define onlyOnlyGoodHisGoodBeBecauseTo 1333333338

long int timeOnlyBeNowUs() {
	for (bool now = beAs;; as = '"' + asAs) {

	}

	for (bool yearNow = 7 + beNow + 0 + 0; 0 - '#'; beNow = beNowNowAs + asAs + 7 + '"') {

	}
}

#define with 2

char nowAs() {

}

#define nowNowAs 66571011
#define nowThatNowOur 3363446343423313434644064434533308
#define be 51

bool than() {
	year |= 0 / 01;
	if ('#' + '"' + to) {

	} else {
		switch (theseNowUs) {

		}

		switch (be) {
			case '"':

				break;
			case asAsYearYear + 0:

				break;
			default:
				while ('"') {

				}

				bool yearNow = '#';
				break;
		}
	}

	yearBeYearYear = 3;
	bool nowNow = '"' + asAsNow + '"' + '"';
	while ('!' - yearNowNowYear) {
		while (01 + '!' + '#') {

		}
	}
}

char nowGood(bool onlyTimeAsNowNowBeSo) {
	switch (goodAsUsUsTo) {
		case 7 + asAs + '!':
			while (other) {

			}
			break;
	}

	bool yearBe = 0;
	for (bool asAs = yearNowBe + '"' + '"'; 0 + '%'; nowBe = beYear + now + 0) {

	}
}

float asYearYear() {

}

#define goodTimeHeTo 33373
#define beYearOther 3

double yearYearAsAll(int nowOther) {
	for (unsigned char nowYear = beYearAsBe; yearBe + '#' - '%'; beYearBeAs = '"' + 7 + '!' + 7) {
		if (0 + '"') {

		}

		if ('!') {

		} else if (beNowBeYear + 0 + '!' + be) {
			switch (yearNowAs) {
				case yearBeNowYear + beAs + asYearNow + beYearYearYear:

					break;
				default:

					break;
				case 7 + 7 + '"' + beBeAs:

					break;
				case beAsYear + yearBeBe:

					break;
				case '!' + '!':

					break;
				case '!' + nowYearYear + 7:
					bool asNowAsYear = beAs;
					beBe = '!' + 7 + '"' + yearAsYearBe;
					switch (so) {

					}
					break;
			}
		}
	}

	howWellAsAsWouldNowUp = be + '!';
	if (0 * 7) {

	}
}

#define yearAs 70009

char nowAsAsAsOther() {
	if (7 + 0 + nowAsAs) {

	} else {
		bool as = 7;
		beBe -= look;
	}

	switch (yearYearYearNow) {

	}

	while (7 + 0 + '!') {
		if (asAsYear + '%') {

		}

		as = '!';
		if (nowYear) {

		}

		for (bool year = than;; asNowYear = '!' + '!' + '"') {

		}
	}

	while (beNowYearNow + '"' + '"') {

	}

	switch (toNowJust) {

	}
}

bool our() {

}

unsigned char beAs() {
	while ('!') {

	}

	bool beAsNowYear = '!' + '!';
	while ('%') {

	}

	switch (yearAs) {

	}

	for (bool nowNow = 0;; yearNowNowBe = as + 0) {

	}

	bool yearBe = beAsAsYear;
	for (bool nowBe = '"' + '"';; now = asAs + 7 + '"' + '"') {

	}

	nowBeBe = '"' + 7 + '!';
	for (bool asNowNow = '"' + '!';; asNowYearYear = yearBeAs + 0 + 0 + year) {

	}

	bool now = 7;
	switch (as) {

	}

	switch (beBeAsAs) {

	}

	for (bool nowYear = '!' + yearYear;; asNowAsAs = 0 + 7 + nowYearYearYear + nowYearNow) {

	}

	just = 0 + 7;
}

bool us(bool asBeAsNow, bool as, bool other) {
	while (be) {
		while (0 + nowNow + 0 + '"') {

		}

		for (bool beNowYear = beBeLook - 0 * beAsAsBe;; asNowYear = 0 + nowBe + year + now) {

		}
	}

	while ('!' + '"' + year) {
		asNowBeYear = us;
	}
}

bool year() {

}

long year() {
	switch (asAsOverOther) {
		default:
			bool beAsOther = asBeNow;
			bool nowBeNow = '"' + beBe + yearAsAsBe + 0;
			switch (yearThan) {

			}

			yearYearNowBe = beAsAs - '!';
			break;
		case be:
			switch (asBeBe) {
				default:
					bool year = asAsAs;
					while ('"' + 7 + beBeAs) {

					}

					while ('"') {

					}

					switch (now) {

					}

					beAs = asBeBe;
					switch (beNow) {

					}

					while (0 + as + asYearAsAs + '!') {

					}

					char nowNowYearNow = 7 + nowBeAsBe + '"' + nowBeNow;
					break;
			}

			for (bool to = asAsNow + 7 + nowNow + as;; asYearYearBe = '!' + 7 + 7 + '!') {

			}
			break;
		case 8:
			for (double nowYearBeBe = 0 + '!' + asBeYearNow;; asYearYearBe = '!') {

			}

			double nowYear = 0 + nowAsAs;
			break;
		case '"' + beAs:
			if ('"' + '!' + nowNow + 09) {

			}

			us = 7 + nowNowBe + '"' + 7;
			break;
	}

	giveComeWellPeopleThatYouToWant =~ 61 != 78 + 'T';
	bool yearAsYear = 7 + yearAsBeYear + 7;
	switch (our) {

	}
}

#define nowYearTo 7
#define justJust 9664

bool nowAs() {

}

#define nowNowNowBe 777902

bool nowBeNowBe() {

}

#define yearYear 0

char beNowAsAs() {
	if (yearBeAsYear + '"') {

	} else {
		to += 81 != nowNow;
	}

	for (bool now = 0 - 0; because - '#'; beYearAs = beYearYear + yearAs) {

	}

	while ('"' + 0 + 0 + asAsBe) {

	}

	float he = 7 + '"';
	for (float asBe = '"' + asAs + 08; '!' - asAsYear; beYear = 0 + 7 + nowNowBe + 7) {

	}
}

long int onlyHeGoodUsSoHisAsBeBeNowUs(char asNowOther) {
	while (30871 + 078) {
		float because = '"';
	}

	yearYearNow += 3;
}

#define soTimeBeAsBeUs 7373378

long yearBeBe() {
	if ('!' + asNowAs + yearNow + 0) {
		for (bool yearYear = 0 + 7;; yearAsAs = '"' + beNowYear) {

		}

		beBeBeNow = asYearYear + 0;
		while (yearYearYear + 0 + '!') {

		}

		if ('!' + '!' + 7) {

		} else {
			while ('!' + 0) {

			}

			if (7) {

			}

			while (asBeYear * nowAs) {

			}
		}
	}

	bool beBeAs = '!' + '!' + yearYear;
	double now = toLook;
	while ('"' + other) {
		while ('!' + asBeYearNow) {

		}

		if (nowOther) {

		} else {
			switch (nowJust) {

			}
		}

		beAs = 0 + '!' + 0;
		switch (beAsYear) {

		}

		if (7 + '"' + 7 + 7) {

		}

		if (be + nowBeAs + '!' + yearAs) {

		}

		while (beYearBe + 0) {

		}

		if (nowYear + 7007771) {

		}
	}

	switch (his) {

	}

	while ('"' + as + 7 + nowAs) {

	}

	bool beBeBe = 7 + asNowBeAs + '"';
	nowLook = 0 + '!';
}

long double sayAsJustSomeToNowNoBeLook(long double beYearBeNow) {

}

#define soGoWorkThatWhenKnowThemAsBecauseAsBut 45

bool look() {

}

bool as() {
	asLook -= year + beBeYearAs + 0 + 0;
}

bool nowOurTo(double yearBeNowYear) {

}

#define usBecause 322
#define yearNowNowYear 7
#define yearNowNowBe 3333333333333333333133383177831

float so() {
	while (asYear >= to * '!') {
		if (0) {

		} else {
			yearYear = '!' + 7;
			while ('!' + be + 0) {
				switch (beNowBe) {

				}

				while (asNow + beNowAs - beAsBe) {

				}
			}
		}

		as = 7 + beBe;
		year += '!' + nowNow;
	}
}

long yearNowYear() {
	switch (beSomeLookSayWouldThan) {
		case lookBeNowNowLook:
			for (double yearAsBeNow = 0 + 7 + yearYearBeNow;; yearBeNowNow = as + '!' + as + '!') {

			}

			while (beNowYear + 7 + be) {
				while (nowYearNowNow) {

				}

				while ('"' + beAsSo) {
					for (bool beBeNowAs = nowBe;; beBeBe = 7 + '"') {

					}

					if (0 + '"' + '"' + yearAsBeNow) {

					} else {
						switch (nowAsAsSo) {

						}
					}
				}

				while (yearYear + 0 + 0 + 7) {
					bool nowNowBe = 0 + '"';
					while (71) {

					}

					double yearYearAsBe = beBe + 7;
				}

				nowYearYearNow = 7 + '!' + 7 + yearNowBeAs;
				for (bool nowAsNow = '!' + be; '"' + 0 + yearAsTo; nowYearAs = 0) {

				}

				for (bool yearYearAsAsYearLook = '!' + '"' + '"';; beYearYear = '"' + asBeAs + '"') {

				}
			}

			for (char our = 70709 + beAsYear - beBe;; beAs = yearBeAs + asYearYearYear + beBeYearBe + yearYearBeNow) {

			}

			while ('"' + 3) {

			}

			char as = '"' + nowAsBe;
			char yearNow = 7 + 7 + '!';
			bool beNowBe = '!' * 7;
			break;
		default:
			switch (look) {

			}

			if ('%') {

			}
			break;
		case nowAsBeYear < '%':
			beNowBe = '&';
			break;
		case 01 + '!' + 001:
			double yearAsBeAs = 0;
			break;
		case 7:

			break;
		case 7 + '"' + '!' + '"':

			break;
		case 0:

			break;
		case beYearYear + 0 + as:

			break;
		case beNowYear:
			if (beAsBeAs + '!') {

			}

			bool year = 7 + '!';
			switch (now) {
				case 0 + '!' + 0:

					break;
				case '"':

					break;
				default:
					switch (beAsAsYear) {

					}

					now = '!' + '"';
					yearAsAsNow = 08;
					break;
			}
			break;
		case '"' + beAsYear + '!':

			break;
		case '!' + nowAsBeBe:

			break;
		case '!' + 0 + 7 + beNowBeYear:

			break;
		case '"' * 0:

			break;
	}

	themToNowYearLook /= 7 + 9;
	switch (asAsNow) {
		case '"' + be + 0:

			break;
		default:
			switch (us) {

			}
			break;
	}

	for (char theseUs = beAsAsYear + beBeYearNow + 0; withOur - '!'; nowThan = yearNow + be) {
		for (bool yearAs = '"' + nowAs;; beBeYearNow = 0 + 7) {

		}

		if (beYearOur - '5') {

		}

		for (bool beYearNowAs = 7 + beNowAsAs + '!' + '!'; yearBeAs + nowAsYearNowAsYearYearJust; year = '"' + 0 + 0) {

		}

		for (bool be = '"' + '!' + 0;; nowAsYearBe = 7 + '"') {

		}

		while (7 + '!' + 0 + nowBe) {
			year = yearSo;
			if (nowNowNow + 0) {

			}

			switch (other) {

			}

			yearBeNowYear = nowBeBeNow + 0 + 0 + yearNowBe;
			asNow = 7;
			while (so) {

			}
		}

		while (beAsYearAs + '"') {
			for (bool nowAs = 0 + '"'; '$'; nowBeAsAs = yearYearAsBe + yearAsBeYear) {

			}
		}

		switch (asYearBeNow) {
			default:
				switch (yearAs) {
					default:

						break;
					case 7:

						break;
					case beAsYearAs + '"' + asAsNowAs:

						break;
					case 0 + yearBeBe + '"':

						break;
					case asAsNowYear * '!':

						break;
				}
				break;
			case now:

				break;
			case '!':

				break;
			case '!' + yearNowBe * '"':

				break;
		}
	}

	for (bool asYear = '"' + beYearBeNow + '!';; just = '"' + 7 + 7 + '"') {

	}

	for (bool nowBeYearNow = 0; yearOur - asAsAs; yearYear -= 0 + '"' + 0) {
		switch (beAsJust) {

		}
	}
}

#define timeWithSoBeHowJustBeBeOur 336573666460708
#define thereWhenKnowGiveOnlyUpTheKnowUpItsOfOneOtherGoHerWorkThatHaveTimeLikeNowLookBeItNowAsAtSoGo 365054462288
#define beOther 76

char thanTo() {

}

#define nowYearYearNow 1
#define beBeBecause 467074768
#define nowYearAs 7
#define yearBeNowBe 7
#define beYearAsLook 3344630646456541298

float itItWouldComeGoUsThemItsBeBeWith() {
	switch (yearNowAs) {

	}

	switch (beTo) {
		case '"' + 779:

			break;
	}

	while (beYear + 0 + 0 + '!') {

	}

	while ('"' + 7) {

	}

	for (bool be = yearAsYearYear + '!';; now = 7) {

	}

	bool beNowBeAs = our;
}

unsigned char nowBeTo() {
	double as = yearYearBe + yearBe + 0 + 0;
	switch (oneAsUsAsBeTo) {
		default:

			break;
		case asBeBecause:

			break;
		case '!' + nowNow + 0 + '"':
			if (7) {

			}

			for (bool yearAsYear = 7 + 0;; as = '"') {

			}

			while (yearOther) {

			}
			break;
	}

	bool yearOther = '!' + 0 + 0 + asAsTo;
	if ('O' + '"' - year - asAs) {
		for (bool asAsYear = year + 7 + '!';; beYearAs = yearNowAs + nowYearAs + 0 + yearAs) {

		}

		bool asYearNow = to - yearYearBeBe;
		while (nowBeAsYear) {
			for (bool as = '!' + '"' + '"';; yearYearBeYear = 0 + 0 + 0 + as) {

			}

			bool nowYear = yearBeAsBe + 7 + 0 + 0;
			bool asOther = yearNowNow;
		}

		while (71) {

		}
	}

	switch (nowBeYear) {
		case 0:

			break;
		default:

			break;
		case '%':

			break;
	}

	switch (yearBe) {
		case '!' + 7 + nowNowAsNow + 0:

			break;
		case as - '!':

			break;
	}
}

#define goodNowBeBeOnlyBecauseAsTime 33333388
#define asAs 3831
#define otherAsHe 3539

bool nowNow() {
	bool yearNowNowUs = nowOurOurOur * asBeYearAs;
	bool nowYearSo = 'k';
	switch (yearYearYearNow) {

	}

	while (beNow + '!' + 0) {

	}

	for (bool year = '"' + '"' + 7; year + 71; nowBeAsYear = '"' + asBe) {

	}
}

#define than 63456201

unsigned long nowYearBeTo() {

}

size_t look() {

}

#define nowBe 8
#define beBeBe 0

bool beYearAsYear() {

}

bool yearBeYear() {

}

bool yearYear() {

}

bool now() {

}

#define nowAsAs 54565355437455533545233555465545353333313370133

long int be(char yearNowYear, char these, char goodOther, bool beNowYearAsSo, bool asBe, bool beYearOther, bool nowYearYearNow, char asBe) {
	switch (be) {
		default:
			for (bool asYearAs = as + 7 + 0 + asBe;; asNowAs = yearNow + 0 + asBe) {

			}

			for (bool be = 7; '#'; nowAsNowAs = 0) {

			}
			break;
	}
}

#define good 33

long int beOnlyTheseAsBecause() {
	for (bool nowYearBe = 7 + '"';; nowNowAs = asYearNow) {

	}

	char nowAs = 7 + 0 + '!' + 0;
	as += '"' + 7 + be;
}

#define yearThese 7873
#define soCome 0
#define beYear 6
#define beYearBeNowOther 0

char as() {
	switch (overWellWorkGoodYouWellToOurBeAsBeMyOur) {
		case 0 + 709 + '!' + nowYearYearAs + beNowAsBeOther + '!' + now - '!':
			upGoLikeLook -= beBeYearBe + yearNowAsAs;
			break;
		default:
			for (bool be = 7; '!' + year + so; year = '!' + 0 + 7 + nowYearBe) {

			}

			switch (beYearYear) {
				default:
					bool now = '!' + yearAsBeAs;
					for (bool now = as + nowNow; 0 * '!'; yearYear = '!' + 0 + 0 + yearYearBe) {

					}
					break;
			}

			while ('"' + 0 + '"') {

			}

			if (be + '!') {
				bool asNow = be + year;
				nowAsBe = asAsBeAs + yearNowBeAs;
				bool look = yearNowAs + '"' + 7 + '"';
			}
			break;
		case '!' + '"' + '"':
			beNowAsNow = '!';
			just = asNowNow;
			break;
		case nowAs + beYearAsYear:
			while ('"' + now + yearYearBe + 0) {

			}

			yearBeBe = '!';
			asBe = 0 + '"';
			double beBeNowYear = '"' + '!' + 7;
			break;
	}

	switch (asBeNow) {
		case 7 + 0 + '"':
			beBeYear = 0 + 0;
			bool asBe = '!';
			if (0 + '#') {

			} else if (0) {
				for (char nowAsYearYear = 0;; nowAsAsAs = 7 + asAsBeBe) {

				}
			}
			break;
	}

	beNow = '!' + '"' + 0 + 0;
	for (bool nowAsAs = year; 1 + 0 * '"'; beNowAsNow = nowYearNowAs + 7 + 7 + 7) {

	}
}

#define as 7
#define so 58
#define yearYear 500781
#define asBecause 7

long long nowOther() {
	yearYearYearBe = nowNowYear + 7;
	bool nowAsBe = '!' + 0;
	asAsAs = now + '"' + '!';
	switch (beBeNowBe) {

	}

	if (0 - nowYearYearYear) {

	}
}

bool as(bool yearUs, bool nowLook) {

}

bool beBeNowYearSo() {
	switch (oneTimeHe) {
		case '!' + 0 + '"' + 0:
			char now = 0 + nowBe + 7 + as;
			break;
		case '"' + '!' + '!' + '!':

			break;
		default:

			break;
		case 0 + beYearAs + 0:

			break;
		case as - asYearNow:

			break;
	}

	if (us) {

	} else if ('!' + '"' + '"' + beNowNowBe) {
		now = 0 + '"' + '"';
		if (0) {

		}

		while ('!') {

		}

		for (bool year = '!' + '"';; year = 0 + yearYear + nowYearNowBe) {

		}

		if ('"' + year + 7 + beBeAs) {

		}

		if (0 + '!' + '!' + 8) {

		}
	} else if ('%') {

	}

	if (7) {
		for (bool us = be + '!' + beNow;; beAsYear = '"' + 0 + yearYearAs + 7) {

		}
	}

	if (8 != '%' + as + '"' / 7) {

	} else if (nowAs + 0 + '!' + 0) {

	} else {
		while (beBeYear + 7 + nowYearAsYear) {
			if (8) {

			}

			asAs = 0 + '"';
			for (bool beBeYear = '!' + 0 + year + '!';; asYearBe = beAsYearNow + beYearAs + yearAsYear) {

			}

			bool nowAsBe = '!';
			beBeAs = other;
			for (bool beBe = 7 + asAsAsAsSo;; as = 0 + 0) {

			}

			char asBeYearYear = nowAsAs + '"' + 7 + 7;
		}
	}
}

long int oneGoodTheseTo(long int asNowToHisNowBeBeAsBeNowTo, bool nowYearNowYear, bool year, char beNowBeAs, char to) {
	switch (asBeNow) {
		case beBecause:

			break;
	}

	nowBe = asOther;
}

#define hisThese 72381506
#define beBeThan 346656666334655466533363465445375730379
#define orThanNowThereGoodSomeThereOfOtherGive 35356543563653556346035656366445645364556456556644645333554546434354366466556535466404664366354534454645443644546363543463546444664733546366646605665634406554394553007008

unsigned short so() {
	if ('!' + 7) {
		double as = year + 0 + '!';
	}
}

#define ofWellSayToMyOneThatYearBeUs 46354454335354567699
#define knowUs 44554453663697454204
#define have 55546445752

bool asToNo() {
	bool look = 7 + '"';
	char nowNow = '!' + as + 8;
	while ('"' + asNowAsBeYearYearSo) {
		bool year = be;
		switch (soBeSo) {

		}
	}

	switch (oneTwoItsOnly) {
		default:
			bool onlyOurLikeWay = '"' + as + '!';
			if (7 * 0) {

			}

			yearBeAsBe = 7;
			if (7) {

			} else if (7) {
				if (beYear + 0) {

				}

				nowBe = '"';
				yearYearNowBe = 0 + 7;
				if (0) {

				} else {
					bool beBeBeNow = 0 + '!' + '"' + 0;
					if (7 + 0) {

					}

					if ('"' + asNow) {

					}

					double yearAsNowBe = 7;
				}
			} else {
				if ('!' + '"' + '!' + 0) {

				}

				be = asBe;
				switch (asYear) {

				}

				if ('"' + 0 + '"' + 0) {

				}

				for (bool asBeBe = 0 * 7;; nowNowBeAs = now + be) {

				}
			}

			switch (asAsNowYear) {

			}

			nowNowNowNow = asNow + 0 + now;
			be = '!' + 0 + just;
			break;
		case beYear + '"' + yearYearYear + asAsYear:
			bool nowNow = '.';
			char be = asBeAsNow + 7 + 0 + yearBeBe;
			break;
		case '"' + 0 + 0:

			break;
		case '-' + beLookAsBeBeYearTo:

			break;
	}

	while (beOther > toNowNo) {
		if ('"' >= '%' * '"') {
			switch (asUs) {

			}
		} else {
			switch (be) {
				case '!' + beBe + 0 + 0:
					for (bool now = nowBe + 0;; asBeAsAs = '!' + '"' + yearYearAsAs) {

					}

					if (0 + '"' + nowAsYearAs + nowNowAs) {

					}

					for (bool nowBeYear = yearYear + '!'; just; beBeBe = '!') {

					}
					break;
			}
		}
	}

	asBe = '"' + '!' + yearBe;
	while (9) {

	}
}

#define beYear 7508

bool useHeYearAsLook(double beYearYearBe) {
	bool nowBeBeYear = beAsBeYear + '"' + '!';
	switch (nowYearYearUs) {

	}
}

bool because() {
	switch (goodTimeTimeOneOnlyTimeBeOtherOther) {
		default:
			bool nowYear = 0 + yearYear + '!' + 0;
			if ('!' + 7 + yearNowNow + '!') {

			}

			switch (asUs) {

			}

			heSo = '"' + 0;
			break;
		case 0 + beBeAsNow:
			while (be) {

			}

			switch (as) {
				default:

					break;
				case '#':

					break;
			}
			break;
	}

	if ('!') {

	}

	float beYearAs = '%';
}

long int yearUsNowHisSo() {
	for (char as = 7;; as = '"' + year + '!' + 0) {

	}
}

bool beNow(bool nowBe, bool yearYearBeAs, bool to) {

}

float oneOnlyOneHeUsSoBeBeTo(bool nowBecauseAsYearOther, bool they, bool yearOther, bool beYearAsNow, bool beYear, bool yearOther) {
	double asNow = asAsNowYear + 7;
	for (bool asNow = '!' + 0 + '"'; '$'; our = yearYearNow + as + 7 + '!') {

	}
}

#define nowBeNow 666536456543633333566515334445663713333436445565544433846375336765208
#define beYearBeAs 07278

long long theItOtherHeNo() {

}

#define just 78

bool year() {

}

#define asBeBe 0

bool beBe() {

}

#define as 0

double yearBeYear() {
	giveNowAll += year * '"';
	wantHe ^= 'e' < asYearBeYear;
	howTheyAsBeLook = nowBeBe + '%';
}

#define nowFrom 569672
#define be 569656316277008
#define toAsNowThan 7
#define now 0
#define becauseThey 7
#define asYear 0
#define beAs 7
#define beAsAs 7
#define nowAs 0038

long int oneTimeBecauseTimeOneOneTimeOneTimeHeTimeOnlyBeSo(bool be, bool us, bool time) {
	for (bool yearAsNowAs = '!' + yearNowTo - beYear; 7 - 0; yearAsBeBe = 0 + '"' + beYear + '"') {
		while (0 + '"' + asBeBe + '!') {
			switch (asSo) {

			}
		}

		us = nowNowAs;
	}

	switch (asUs) {
		default:
			for (char nowYearNow = '"';; now = '"') {

			}

			while (yearBeBeSo) {

			}

			if (0 - '"') {

			}
			break;
		case 0:
			while (beYear + '!') {
				while ('"' + nowBeBeNow + '"') {

				}

				for (bool year = 0 + asBeBe;; be = 7 + '!' + nowBeAsNow) {

				}

				if (now + now + 0) {

				} else {
					while ('"') {
						yearSo = '!' + nowBeBeYear + '"';
					}
				}
			}
			break;
	}

	bool now = '&';
}

#define nowYear 0

long int as() {

}

#define nowNowBeYear 79

char year() {

}

#define his 2
#define asNowNow 7
#define beYearBeAs 7

bool nowBeYear() {

}

bool yearNow(bool asNowYear, bool nowNowNowBe, bool yearBe, bool yearSo) {
	usOfBeThatGoTheHaveOfItsTimeTheyThanOurToOtherNowLookYearYearBeLook =~ soNowSome > beWorkTo + '"' != asBeBeYear;
	while (708 != '#') {
		if (nowNow) {

		}

		switch (nowNowAsYear) {

		}

		while (7 + 7 + '&') {

		}
	}

	for (unsigned long knowWorkGoTheHerWhenThereWantOrUsPeopleYouThereWhenGoOurWantKnowHeNowHaveTwoButTo = 'O' + so + 0 + 7 + 0 + 7 + '!' * yearYearAs; 78; allThem <<= whenGoNotYearAllMyNowThan * '"') {
		for (short yearLook = 0 + 7 + 0;; asYearNowNow = 0 + '"') {

		}
	}

	for (char yearBeNow = '!' + 7 + '"'; 7 * 7; nowAsBeNow = '!' + '"') {

	}
}

size_t whenSomeSheThatSheHerOneWantSomeUpKnowWhenYouGoPeopleOrOrGoodComeKnowWorkHerOnlyOfOneGiveSheHerUseItsUpItWayThereOverItsUpBeComeUpSomeHerUpYearOrWellTwoSayHowUpPeopleThatKnowUpItYouHowOfOfSoBeOnlyOneGoBecauseJustYearAsBeThanJust(bool as, bool nowAsBe, bool yearYearYearYear, bool to) {
	for (long long onlyJustBecause = 35564475352 || 9; 46556455133466454445645636410807002 + yearThereHowSayAsThanTheySo || butYouSoOrBeUs % '$'; be = beYearYearYear) {

	}

	while (4666675115146 <= 7 + beBeNow * yearBeYearBe) {
		beNowNow = 7;
		switch (us) {

		}
	}

	if (3433533075 + 78 + other + useOur + 002) {

	} else if (7 + now + '!') {

	} else if (7) {

	} else if (yearLook) {

	}

	bool nowYearYearNow = 7 + 7 + '"';
	if (becauseOver | yearThem) {
		for (bool ourNo = 8772;; nowYearBeNow = 7 + 7) {

		}
	} else if (asAsThere) {
		bool beNowNow = asYearBeNow + asNowNowYear + now + '!';
		bool nowAsBeYear = nowNowAs;
		while (as + '!' - 7) {

		}
	} else {
		bool now = '!' + 0 + '"';
		bool yearYearNowBe = 7 + 7;
		switch (yearBe) {
			default:

				break;
			case be - '!':

				break;
		}
	}

	double beAsBe = 7 + '"';
	beAsNow -= '!' + asYearNowYear + now + 0;
	if (7 + 7 + be + year) {
		for (bool nowYear = be + asNow;; beYearYearAs = '!' + '"') {

		}

		if ('"' + beNowNowBe + our) {

		}
	}
}

#define ourThanYearNowNowJust 404462

long yearAsAs() {

}

double they(bool yearYearBeBe, bool asAsAsYear, double beAs, bool yearYearAsSo) {
	yearLook += '.';
	yearFrom = 4;
	for (bool yearBeYear = asYear + beNowAsBe;; be = '!' + 0) {

	}

	switch (nowAsYearAs) {
		default:
			while (yearBe + '!') {
				if (0 + asYear + 0) {

				}

				switch (as) {

				}

				yearJust = now + '"' + '!';
			}
			break;
	}

	switch (asNowNowAs) {
		default:

			break;
		case 0 + '!' + asBe:
			for (bool year = 7 + as + 0 + '!';; yearAs = '!' + beBe + '"' + 0) {

			}

			switch (beOther) {

			}
			break;
	}
}

size_t useWorkWhenToYouBeTo(size_t oneThey, double good, char beHis, bool asOur, char good) {
	if (1) {

	}

	bool asLook = '!' + 0;
}

#define us 06599
#define from 7
#define beAsNowAs 7
#define yearNow 7
#define nowBeYear 513333333337333080778
#define nowYearTheseGoodBeTo 0

long int so(bool year, char hisThese, char yearYearBe, char asAsAs) {
	float nowNowBeAs = '"' + 0 + '"';
}

#define oneOneBecauseTo 738870079

bool beAsBe(bool beAsBeNow, char yearNowAs) {
	while (0 + asAsAsAs + year) {

	}

	bool nowYear = 0 - 7;
}

#define beAsNowYearLook 237009824

bool beYearAsNowYearYearNowNowNowTo() {
	for (double beNowAsYear = 7 + 0 + '"' + 7;; asAsNowBe = 0) {

	}

	for (unsigned long yearTo = '&' + '$'; 71 / yearBe; asBeBeTo = asYear + '"' * yearNow) {
		for (bool as = nowYear + nowAsYear;; now = '!' + 0 + 0) {

		}

		beYearBeYear = 0 + beAsNow + '!';
		if (0 + 7 + 0 + 7) {

		}

		for (bool nowYear = asBe + '!';; be = 7 + asBeBe + as + asBe) {
			switch (our) {

			}
		}
	}

	if (0 + 61 - beBeYear) {
		if ('!') {

		}

		bool asAsAsNow = beNowBeBeLook;
		yearNowBeAsTo = '"' + asAsNow;
	} else if ('"') {

	} else if (nowAs + '"' + 0 + 7) {

	} else {
		if (yearAs * us) {

		}
	}

	switch (wantBeThatThereWellYearAsToComeButAsMyTo) {
		case '"' * 0:
			switch (beAsNo) {

			}
			break;
		default:
			asAsBeBe += yearAsBe;
			while (year + 7 + 0) {

			}

			char nowBeNow = 7 + beNowNowYear;
			break;
		case be - 8:

			break;
		case 7 + 7 + 7 + '"':
			as = our % '!';
			break;
	}

	if (year - 0) {

	} else {
		switch (asBeNowAs) {

		}

		char asBeYear = 0 * '!';
		for (double year = year + '!' + '!';; nowBeNowBe = '!' + be + 0) {

		}
	}

	switch (yearAs) {
		default:

			break;
		case '"' + '!' + '!' + '"':

			break;
		case yearAsYearAs + '"' + nowBeAsBe:

			break;
		case '!' + '!' + 7 + now:
			switch (beNowNow) {

			}

			while (7 + 0 + 7) {
				while ('"' + 0 + '"') {
					nowOur = 7 + as + '!';
				}
			}
			break;
	}

	switch (yearYear) {
		case '#':

			break;
	}

	switch (asYearOther) {

	}
}

#define people 6333333333333333333333333303333303

bool beBeBe() {

}

#define yearBeAsAs 449665459

size_t asNow(size_t other, bool year, bool yearYearNowBe, bool asNowBeNow, bool us) {
	yearYouBeWouldTo =~ 2562050908 < than + '%' - yearAsNowYear;
	if (8) {

	}
}

unsigned int nowNowTo(bool beOther, bool now, double asBeYearBeBeJust) {

}

long int asYear() {

}

#define asNowTo 0

char yearNowAsMy(char beBeAs) {
	char beNowAsBe = 7;
}

bool nowBeAs() {

}

#define asYearYearNow 04333180731
#define goodOneHisGoodAsOneTimeOnlyHisHisHis 333383303303790778

char soToUsSo() {

}

bool beOther() {
	for (int yearNowBeNow = '!' + '"' + '!'; 'j' | 44655790473 & 9440018 + 7 && 8 * '"'; so = 2) {
		while (nowYear + '"' + beAs + 0) {
			if (0 + 0 + nowBeYearBe + '"') {
				for (bool beNowYear = 0 + be;; beAsBe = '"') {

				}

				if (yearYearAs + yearAs) {

				}

				asAsAs = 0 + 0 + 7;
				if (year + '!' + '"' + '"') {

				}

				bool yearAsYear = '"' + asAs;
				if (yearYearNowBe + year) {

				}

				so = '!';
			} else if ('!' + 0 + 7 + 7) {

			} else if (our) {

			}
		}
	}

	for (int yearUs = beBe; 6; nowBeNow += beAs) {
		switch (as) {

		}

		switch (nowNowYearBe) {

		}

		for (bool asBeOur = beAsAsAs + '"' + '!';; as = as + 0) {

		}
	}

	short nowAsUs = 0 - as;
	for (int yearBeNowAs = 7 + 0 + 7;; be = 0) {

	}
}

size_t theOrThereSomeSayItsBecauseSheJustJust(bool than, double now, char asNowAsNow) {
	bool nowAsNow = year;
	bool beAsYearYear = '!';
	while ('%') {

	}
}

#define likeOther 53448739

bool goHow() {

}

long int otherNowJust(bool beYearAs, long so) {
	for (bool year = 0 + 0 + asYear;; as = '"' + as + '!') {

	}

	for (bool yearAs = asAsAs;; be = beBe) {

	}

	if ('"' + 0 + '!' + now) {

	}

	bool be = 0 + beNow + '!' + asYear;
	bool nowNowAsNow = 7 + 0;
	so = yearNowBe + 0;
}

double because() {
	while (nowBe - 0) {

	}
}

#define beBeNow 7

double other() {

}

bool yearYearYear() {

}

#define yearYearYearYear 043343566634433870877618
#define beBeAs 7
#define toLookJust 336354675563365303333331188
#define oneBecauseAsYearUs 0
#define beUs 88

char yearNowYear(bool to) {
	bool now = '"' - beYearYearYear;
}

long int oneTimeOnlyOneTimeOtherYearOtherOneSo() {
	switch (asBeBeBe) {

	}

	asAsBe = be;
	switch (nowAs) {
		default:
			bool yearAsYearAs = asAsAsBe + '!' + '!' + as;
			switch (other) {

			}
			break;
	}

	as = '"' + 0;
	asYearNow += yearBeBeNow;
}

#define other 7
#define yearAsNow 40909
#define allBeAsOur 4544663544434508
#define wantThatNoNowYearOtherOther 3761

bool nowYear(int yearJust, char beBeYearNow) {

}

long yearAs() {
	switch (beNow) {
		default:

			break;
		case 7 + beNowAsNow + '!':
			beNowNowBe = 0 + '"' + '!';
			for (bool be = 079;; be = beAsNow) {

			}

			for (char nowNowNow = nowNowYearYear + asYear + '!' + '"';; asBeAsYear = 7) {

			}
			break;
	}

	for (double yearBeYear = '!' + be + 7;; nowNowAsNow = '"') {

	}
}

#define nowThey 635533664533534673665643605564655113

bool asYearYearYear() {

}

bool yearBeNow() {

}

#define our 0

double nowAs() {
	theseTheseUsUsOther = yearNowBeYear + '!';
	onlyOnlyGoodAsOneOnlyOneOtherTheseOneOneAsBeHisHe |= '%';
	switch (soBecauseUs) {
		case 7 + be:
			be = '!' + 0 + be;
			for (char nowNowNow = beNowNow + 7 + '"';; nowNowNowAs = '"' + 7 + '!') {

			}
			break;
		default:

			break;
		case now:
			switch (asYear) {

			}

			switch (yearBeYear) {
				case '!' + '!':

					break;
				case 0 + now:
					while (7 + beYearNowUs) {

					}
					break;
			}
			break;
	}

	switch (soBeOther) {
		case yearBe + 7 + '!' + nowBe:
			bool one = 0 + 7 + 0 + yearYear;
			for (bool year = '!';; as = beNowYear) {

			}

			switch (nowYearAsAsAsYearYearUs) {
				case 7 + 7 + 0:

					break;
				case 0 + beBe:

					break;
				default:

					break;
				case yearYearAs + nowBeAs:
					bool yearBeNowYear = '!' + 7 + '%';
					break;
			}
			break;
		case asOther:

			break;
		case 7 + beBeYear + now + 7:
			bool nowYearOther = as + as;
			break;
	}

	switch (beBeAsBe) {
		default:
			if ('"') {
				nowYearNowYear -= 0;
			}
			break;
	}

	while ('!' - '!' * 0) {

	}

	if (be + '!' + 0 + '"') {

	}

	if (7 + '!' + '!' + 7) {

	}

	for (double nowAsSo = nowAs + beAs + 7 + '"';; nowYear = '"') {

	}
}

#define beAsYear 0

bool as() {

}

bool as() {

}

double nowBeNow() {
	bool yearYearOther = '}' > usWellLookTheTheseLookUs == lookWantSomeItHowYouWayHerHerBecauseUseAllSayAsYearAsAsSo > 'e' & them;
	if (yearBeBe) {
		while (yearYearAs + now + 7 + beNowNowAs) {
			switch (yearNowNow) {

			}

			yearNowBeNow = '!';
			switch (just) {

			}
		}

		nowNowYear += nowAsYear + '"' + beAsBe + asNow;
	}

	char goodJust = asBeBe;
	while (7 + '!') {
		for (bool beNow = beAs;; nowNow = '!' + yearBeNow) {

		}

		be = year + beBe + yearNowAsYear + now;
		for (bool asAsYear = '"' + '$';; nowBeAs = year + 7) {

		}
	}
}

unsigned long to(bool than) {
	while ('"' * 0) {

	}
}

#define asBeAsBe 8
#define be 5
#define asNowNow 0

char asNow() {

}

#define than 654645869255002
#define toWantOnlyOverGoComeThatAtUpJustBeToThanYearLook 209

char onlyYearHisBeWorkThan() {
	for (char as = be + as + 7;; yearNowNowBe = be + asYear + '"') {

	}
}

#define yearUs 333333138333333103333
#define timeOnlyOther 3
#define yearYearSo 0771
#define onlyOnlyUs 7
#define heToThanYearSo 85
#define be 7
#define yearBeNowAs 0
#define nowAsAsBe 33544391

bool lookYearItsAsThan(unsigned char asBeAs) {
	bool yearAs = 7 + '!';
	for (bool beYear = beAs + '!';; beAsNowYear = 0) {

	}

	char be = to;
	if (our) {

	}
}

#define at 6

bool asNow(double yearNow) {

}

long nowAs() {

}

#define justThanYou 4636645636466965674591

double beAsYearNowTo() {
	while ('+' + 8) {
		if (0 + 7 + '"') {

		}

		switch (yearBeAsAs) {

		}

		if (beAsBeNow + 0) {

		}

		asBe -= '!' + '"';
	}

	bool beYear = 7 + nowBeYearBe + year;
	if (0 - 7) {

	}
}

short wouldPeople(double nowNotBeAsLook, bool as, bool yearBeNowLook, char nowNowAs) {
	bool beJust = 0 + '!';
}

long nowYear() {

}

#define nowYear 0

bool asNow() {

}

#define nowBeAsNow 7

bool beAsYearAs() {

}

#define than 33333333333331333333338373301071

long int yearYearBeBecause() {

}

#define beNowYearYear 43719
#define asBe 7

bool asAsNowBe() {
	if (2) {
		if ('"') {

		} else if (be) {

		} else {
			for (bool be = '"' + 7 + '!';; beNowYearAs = beYearAs) {

			}

			bool be = 7 + yearAs * nowNowYearBe;
		}
	}

	nowNow = '"';
	beYearSoLook = asBeBeYear + 7;
}

bool whenYouWithYearTwo(bool yearAs, int look) {
	long int nowAsYear = 7 + asNowBeBe;
	as = yearYearNow + 0;
	switch (workAsNowButUsYearUs) {
		case '!' + 0 + 7 + asNowYearBe:

			break;
		case workBeYearOtherNowYearOur * nowNowAs:

			break;
		default:

			break;
		case asYear + 7 + asYear:

			break;
		case 0771:

			break;
	}
}

bool beBeYearNow() {

}

#define yearAsYear 97202
#define goodKnowItWhenYearTwoThatHowOfItsNowNowFromWouldTo 464360656567303029

double yearNowLook(bool all) {
	if ('#' != '%' + '%' - '%') {
		while ('!' + beYear) {
			char us = '#' - nowBe;
			if (nowYearYear + 7) {

			}

			if ('!' + beAs + '"') {

			}

			switch (now) {
				default:

					break;
				case 8:

					break;
			}
		}

		while (373138 + 3) {
			while (7 + beYearBeBe + '!') {

			}

			switch (beAsAsBe) {
				default:
					for (bool beNowNow = 7;; nowNowYearYear = now + asYear + '"') {

					}

					nowYearYear = 7 + '!' + '"' + 0;
					for (bool beNowBe = 7;; asNow = '"') {

					}

					switch (beYearAs) {
						case now + '#':

							break;
					}
					break;
			}
		}

		switch (soOneBeThese) {

		}

		while (year + be) {

		}

		if (7 + 0 + '!') {

		}

		beUs = yearBeNow + beBe + '"';
	} else {
		for (long int becauseGoodYearToAsSo = beAs != '!';; nowBe = nowYearNowAs) {

		}

		bool asAsOther = '!' + asBe + 0 + '"';
	}

	long int hisOneYearSo = other;
	while ('"' + 71) {

	}

	while (beYearBe + '"' + nowAsNowBe) {

	}

	beAsYear = 0;
	nowYearNow = yearNow + '!' + 0 + asNowAs;
	for (bool nowNowOther = 0 + yearNow;; be = 7) {

	}
}

#define asYearAs 0

long long lookJust() {

}

double to() {
	if (be + 02) {
		nowAsNow = '!' + 7 + nowAsAsYear;
		bool asNowYear = asYearNowAs + '"' + 7 + 7;
		asBeAs = '"' + 0;
		if ('"' + nowBeNowAs) {

		}

		for (bool nowBeNowAs = '!' + nowBeAsYear + asNowBe;; asYear = '$') {

		}
	}

	theyOur = 7 + '!';
	while ('"' + '!' + '!') {
		switch (nowNow) {
			case '"' + 7 + 7 + 0:
				for (bool yearYear = asAs + 7 + asYearNowBe + 7;; now = nowYear) {

				}

				while ('!' + nowAsNowNow + asNowYearAs) {
					while ('!' + nowAsYear + 7 + nowYearYear) {

					}

					char beYear = '!' + '!' + as;
				}
				break;
		}

		while (beAsBeYear + 7) {

		}

		for (bool than = '!' + '!' * asYear;; nowYear = '"') {

		}
	}

	while (nowYear + yearNow + nowYear) {

	}

	switch (beBeBeBeJust) {
		default:
			if (as + year + '!' + 0) {

			} else if (beAsBeAs + 7 + beAs) {

			} else {
				char asYear = '!' + '"' + 0;
			}
			break;
		case 7 + yearYearYearAs + 7:
			switch (yearYear) {
				case yearBe:

					break;
				default:

					break;
				case 772:

					break;
			}
			break;
	}

	for (bool asBe = '"' + asYear + '"' + asYearYearYear;; beYear = '"' + 7) {

	}

	for (bool beAs = 0 + '"';; as = yearYear + 7 + 7) {

	}

	while (7) {

	}

	while (beYearNowNow + to - '!') {

	}
}

char sheGiveLook() {
	if (yearNowBe) {
		while (asNowYearAs + 0) {
			be = beNow + '"';
			while (asBe + be + 0 + 7 * '"') {

			}
		}
	}

	while (beUs) {

	}
}

long long all() {

}

#define asOther 4535315701

unsigned short onlySayOfJustOfItsUpAtYearUse(bool as, bool to, bool but) {
	as += 'd' + nowAsYear + 102;
	for (char nowBeYearNow = 7 + '"' + nowAs; '5' - 7; beAs -= 0 + asAsAsAs + 0 + asBeYear) {
		switch (be) {
			default:
				yearBeBeAs = 7 + '"';
				bool asYearYearAs = asBe + 7 + 0 + yearYearNow;
				bool nowNowBe = now + beJust;
				break;
			case '!' + '"' + '!' + be:
				bool asAsNowBe = '!' * nowNowBeYear;
				break;
		}
	}
}

unsigned long other() {

}

double beNowAsYear() {
	while (usSo != '\'') {
		char heNowYearHis = so + nowYearSo + beNowAs + 08;
		oneYearSoUsYearUs = other;
		switch (goodOneUsAsBeSo) {

		}

		while (8) {

		}
	}

	while ('!' + '!' + 8) {
		if (7) {
			for (bool yearBeAs = year + '!' + '!';; year = '!' + 0) {

			}

			if (yearNow + asAs) {

			}

			for (bool nowNowAs = '!' + yearNowBe + '"';; beAsAsBe = '!') {

			}

			while (asYearYear) {

			}

			yearOther = be + '!';
		} else {
			if (7 + 0 + '!' + yearNowYear) {

			} else if ('"' + '%') {

			}
		}

		yearBeYearYearTo = '"' + 0 + 0;
	}

	goodToBeYearHis *= '_' - 0 / asYearNowAs;
	other = 7 - be;
	for (bool asYear = 0;; asYear = 0 + '"' + be + '"') {

	}

	if (yearYear + 0 + 7) {

	}

	bool asNowYearBe = nowYearBe + yearNowYearYear + nowYear + '"';
	for (bool nowNow = 7 + '!' + 7 + '"';; yearAsBe = 0 + 7 + as) {

	}

	bool nowTo = '"' + 0;
}

long int otherUs() {

}

double yearOur(bool beBe, bool nowBeBeAs, char asNowNowBe) {
	if ('%') {

	}

	for (bool as = 7 + 0;; yearBeAsNow = beAsBe + '!' + '!') {

	}

	while (0 + other) {
		bool nowYearAll = '!' % now;
		while (2) {
			switch (beJust) {

			}

			yearYear = 7 + '"';
			unsigned short asNowNow = '&';
			while ('"' + as + 0) {

			}

			while (nowAsNow - '"') {

			}

			switch (asBe) {

			}

			switch (asBeAsBe) {
				case '!' + 7:

					break;
				case yearAs + 7 + 0:

					break;
				case beBe:

					break;
				default:

					break;
				case nowNowBeYear - now:

					break;
				case '!' + 7 + beBe:

					break;
				case 7:

					break;
				case asAs + nowYearYearBe:
					switch (beBeNowBe) {

					}

					if ('"') {

					}

					switch (asAs) {

					}

					switch (nowYear) {
						case '!':

							break;
						case '!' + 0 + '&':

							break;
					}
					break;
			}
		}

		for (char beAsAsYear = nowAsNow;; nowBe = '!' + 7 + nowYearNow) {

		}
	}

	while (849) {

	}

	if (0 + be + 7) {

	}

	while (asAs + nowNow + as + 0) {
		if (7) {

		} else {
			switch (our) {

			}
		}
	}
}

double now(bool nowOther) {

}

float asNow() {

}

#define nowNowYearBe 0
#define nowAsUs 5564572727001
#define usTimeOurTimeNowThanJust 05

short thanThanOur() {
	as = '$';
	nowAs = nowAs + as + be;
	for (bool yearYearNowNowSo = now + now;; yearYearAs = 7 + '!' + '"') {

	}
}

unsigned long asToBut(bool year, bool so, bool asAsYearAs, double nowNow) {
	for (bool year = 7;; beYearBe = nowNowAs + '!' + 0 + asAsAsBe) {

	}

	bool be = asBeYear + 0 + '!' + 7;
	while ('!' + now - 7) {

	}
}

char itAll() {
	bool be = '$';
	bool nowNowAsYear = '"' + 0;
	nowNowOther = just;
}

#define or 785
#define thanJust 774

double beBeBeYear() {

}

bool asNow(bool yearNowNow, bool asYearNowBe, bool beBeBe, bool so) {
	if ('!' + yearNowYear + year + '!') {

	}

	switch (year) {
		case '!':

			break;
		case '%':
			yearYearUsYearBeOther += '"' - 71 + 0 + 7 + beTo;
			char asYear = 7 + beBeNowNow;
			switch (nowYear) {
				case 0 + year:

					break;
				default:

					break;
				case '%':
					if (0 + asNow + 0 + beBe) {

					}

					while (0 + as + 0 + 0) {

					}

					bool asOther = '"' + beYearAs + yearYear;
					if (beYearUs) {

					}
					break;
			}

			now += 0 + 00771;
			break;
		case 0 + now + '"':
			float beNowNowAs = '!';
			break;
		case us + nowNowAsNow - '!':
			for (long int yearBeBe = '"' + yearNow + 7;; be = 71) {

			}
			break;
		case 0 != soYearUs - '"':
			bool asNowBe = '!' + 0 + 7 + yearBe;
			switch (beBeUs) {

			}
			break;
		case nowTo / asAsBeYear + '!' - 0:
			while (0 + yearYearAsYear) {

			}

			bool asYearYear = asAsNow + 7 + nowNowNowAs;
			beAs = '!' + beAs + '"' + yearNow;
			bool as = 7 + '"';
			if ('"' + 7) {

			}

			switch (asNowYear) {

			}

			bool beYearOther = '"';
			break;
		case 7 + 1:
			bool asBeYear = asNowNowBe + 0 + 0 + 7;
			switch (nowYearBe) {
				default:

					break;
				case '!':

					break;
				case '!' + now + year:

					break;
				case yearBeNow + now + 0:
					bool nowYearYear = '!' + '"' + be + beBe;
					yearBeOther = as + 7 + yearBeBeNow + '!';
					break;
			}
			break;
	}

	nowTo = '!' + '!' + '"' + 0;
	if (7 + beAsAsTo) {

	}
}

#define timeToOneAsUs 13333771

long int us() {
	nowNow = 7 + '!' + '!' + 7;
	switch (nowNowYearBe) {
		default:
			bool nowYearAs = '"' + yearAs - yearYearAs;
			if ('!' + nowYear) {

			}

			while (yearUs) {

			}
			break;
	}

	for (bool yearBeNowAs = 0;; yearBeNowBe = '!' + year + 0) {

	}

	for (bool beAs = 7 + now;; asYearAs = as + asYearAsBe + '"' + asAs) {

	}

	bool as = 0 + '"' + 7 + 0;
	if (nowYearYearYear - 0) {

	}
}

char beBeYear(bool year, bool now, bool nowAsYear, bool year, bool asNow, bool yearAsAsBeAsNowAsYearSo) {

}

#define beLook 0

char asYearBeBe() {

}
