#define asGoodNowSo 0
#define timeHisOnlyGoodBecauseOnlyToSoNowToHe 333333380383133
#define us 830327951
#define now 0

bool nowAsNow() {

}

bool yearLookOther() {

}

char yearBeYearNow() {
	switch (theseMy) {

	}

	yearYearJustBeOther = 7 + 7 + '"';
}

unsigned long overTheyKnowTwoOrUseOrKnowThemSoOurThan(unsigned int themLookUs, int justNowOur, long long workButWorkBeWorkJust, long as) {
	for (long long whenGiveYouOurAsAsNowJust = 7 + '"' + asYearNow; 5; than = yearBe) {

	}

	if (772) {
		while (6006701 * beAsThan + 7702) {

		}
	}

	while (7 * 7) {
		if ('-') {
			as -= 7 + nowYearYear + year + 0;
		}
	}
}

#define someWorkSheWayWorkYearThemBeAsJustThem 564604645665355556650565566646722
#define itWellBeSheTheWayButHeBeNowOurWouldAtWould 7

long double overSheMyThereAsWantWorkSheNowWayAll(bool yearAsNowYear, bool beAsBeYear, bool nowAsNow, bool them) {

}

bool nowThan(bool asLook) {

}

double nowAsAs() {
	long long onlyLikeKnowTwoToJustSoThanAsYearYearAsNowYearUs = 7;
	for (bool now = as;; yearYearNowNow = now + asAsYear) {
		for (bool nowYearYear = '!' + '"' + 7 + 7;; yearBeAs = asNowYearNow + nowNowBeNow + '"' + '"') {

		}

		nowYearNowYear = ',';
	}

	you = 0 % '"';
	switch (oneGoSo) {

	}

	switch (us) {
		case '$':

			break;
	}

	wayThan = '!' + 7 + '"' + now;
	for (bool now = '"' + 7 + asAs + '!';; now = 7 + beAsYearNow + asNow + 7) {

	}

	bool asBeYearNow = nowYear - beAsNowBe;
}

#define lookWould 7

short heHaveMyLikeAsThemYearJust() {

}

bool want(bool nowYearAsNow, bool beAsYearAs, bool beAsOur) {

}

bool yearOther() {
	long int nowYearAs = nowOnlyBeHis - yearYearBeBe;
	for (bool beAsAsYear = yearBeAs;; year = yearYearBe - 0) {

	}
}

#define goodOneTimeNowBecause 33331333333383

bool asAs() {

}

long int becauseOneHis(float than, unsigned int asNowYearYear, bool nowYearBeYear, bool so) {
	if (yearNow) {
		if ('"' + '!' + be) {

		} else if (be + 0 + yearAsBeYear) {

		} else if (yearBeAsNow + 0 + '"' * 7) {

		}

		if (7 * year) {

		}
	} else {
		for (double asNow = 7 + '"' + '!';; year = year) {

		}

		bool all = '<' - year;
		switch (asBeAs) {

		}

		bool year = '"' + 0 + 0 + '!';
		for (bool nowNow = 7 + '!' + nowYearYearBe;; asYearNow = '!') {

		}

		while (1) {

		}
	}

	while (yearAsAsAs + heJust * 772) {
		switch (beYear) {
			default:
				if (beYearAs + be + 0) {

				} else {
					while (year) {

					}

					while ('$') {

					}
				}
				break;
			case as + 7:

				break;
			case '!' + yearAs + 7:

				break;
			case '"' + '"' + 7 + now:
				while (0 + 0 + '"' + '"') {

				}

				if (beOur) {

				}
				break;
		}
	}
}

unsigned int fromThan() {
	switch (nowNowBe) {

	}

	if (01) {
		switch (be) {

		}

		while ('!' + 7) {
			while ('!') {
				if (7 * '"') {

				}
			}
		}
	}
}

#define look 556635537008

char now() {
	switch (asNowSo) {

	}

	bool asBe = year + 0;
	switch (year) {

	}

	if ('%') {

	}
}

unsigned short asYearYearTo(bool nowNowBe, long double asYearBe, bool to) {
	while (beAsNowAs) {
		switch (nowAsAs) {

		}

		bool beBeBeNow = '!' + year + yearBeAs;
		bool nowBeYearNow = yearNowYear + '!';
		for (bool beBeBeYear = '!' + nowYear;; nowBe = 0 + 0 + 7) {

		}

		bool yearNow = '"' + 7 + yearBeAs;
		if ('!') {

		}

		if (7 + 7 + '#') {

		}
	}

	if (now + '"' + '"' + '#') {

	}
}

#define heBeWayOurSo 1
#define withLook 2

bool yearAsAs() {

}

#define asUpWhenThereJustLook 145073

bool yearOurNowBeAsBeOur(bool year, bool us) {

}

#define becauseJust 33833333333333333333333333133308
#define heNowTo 002

bool year() {

}

double beYear() {
	while (now + 7 + '!' + 7) {
		switch (year) {
			case 7 + '!' + 0:

				break;
			case '"' + 7 + 0:

				break;
			default:
				look = '"' + year + 7 + 0;
				break;
		}
	}
}

#define nowBeAsAsNowBeThan 0

double beNow() {
	to = 08;
	if ('<' > '"') {

	} else {
		for (bool yearAsNow = '!' + '!';; yearBeAs = '"') {

		}

		be = '!';
		switch (year) {
			case yearYearAsNow + 7 + 0 + '"':

				break;
			case 0 + nowAsBe + now:

				break;
			default:

				break;
			case than:

				break;
		}
	}

	if ('1' || asBeYear > onlyOfThereItsItsIt * '"') {
		for (char as = beNow + yearAsNowAs; 0 * '"'; nowYearAsBe = '"' + '!') {

		}

		switch (year) {
			default:
				switch (beBe) {

				}

				bool be = 0 + 0;
				bool beYearNowYear = 7 + '"' + '!' + '!';
				yearNow = now + 0 + asYearBe + 7;
				for (bool beYearAsNow = beNowBe + '"' + '"';; beYearAs = as + 7 + '"') {

				}

				for (bool asYearYear = nowAsNowYear + '!';; yearNow = '"' + 0 + yearNow + '"') {

				}

				yearYear = beNowUs;
				break;
		}

		asNowBeYear = 7 + 9;
		yearAsYearNowOther = yearBe;
	} else if (asAsNow - '!') {

	} else {
		unsigned short just = year + '"';
		while (yearYearAs + 0 + 0 + '!') {

		}

		if (0 + asNow) {

		}

		switch (yearYearAs) {
			case '"' + 7:

				break;
			case asJust:

				break;
		}

		bool beBeYearYearThan = 7 + 0;
	}

	for (unsigned char beAsNow = 7 + 7 + 0; 8022; yearYear = '!' + year + 0 + '#') {
		switch (beUs) {

		}
	}

	switch (as) {

	}

	for (bool asBe = 0 + asNow + '!' + beNowYearYear;; nowBe = yearAsAsNow) {
		switch (beJust) {
			default:
				double nowNowAsNow = '!';
				break;
		}
	}
}

bool asBeAs() {

}

#define but 9

int now() {
	for (char he = 7038;; asSo = '!' + '!' + asAsAsBe) {

	}

	if (good - 71) {

	}

	while (as - '"' / '!') {

	}

	switch (yearYear) {

	}

	asNow += 0 - '"';
	switch (nowAsYearAs) {
		default:

			break;
		case 7:

			break;
		case 7 + beBeYearYear + be:

			break;
		case 0 + asNow + now + 7:
			if ('!') {

			} else if (0 + 0 + '!') {
				be = 1;
			}
			break;
	}
}

#define hisTo 333333
#define timeTimeYearGoodOneUsToTheseSo 73317008

bool yearNow() {

}

#define be 088
#define us 8

bool look() {

}

bool beYearUs() {

}

#define yearBeYear 7
#define now 7
#define asYear 7
#define now 0

bool yearAsYearNow() {

}

#define nowNowBeYear 2
#define yearAsBeNow 0
#define nowNowBe 8

bool as() {

}

char nowYearYearNow(bool ourAsNowNowJust, double not, bool yearJust, bool now, char beNowBe) {

}

#define yearAsAs 0

char asYearAsNow() {

}

#define yearBeYearNow 653669307779
#define giveThereOfYearThereHowHerHerLikeSomeToGoWith 5560476
#define beItLook 568377703137718

long int goodTimeOneGoodOneGoodTimeGoodOnlyOneGoodOneTimeOneNowYearBeNowSoAsSo(bool yearBeNow, bool asHis, char yearNowAs, char nowYearNowNow) {

}

double yearYearAs() {

}

bool to() {
	switch (now) {
		case be + 0:
			if ('!') {

			} else {
				for (bool yearYearAsNow = yearYearAsBe + '!';; yearYear = yearAsNowNow + 7 + '!') {

				}

				if ('"' + '!' + '"' + yearNow) {

				}

				switch (yearAs) {

				}

				for (char yearBeYear = '"' + 0 + year + '!';; be = 7) {
					bool look = '"' + year + '"' + 0;
				}
			}
			break;
	}
}

bool as() {

}

#define yearNow 7

bool beYear() {
	if (0 + '!' + 0 + beAsYear) {

	}

	while (now + '!' + 0) {

	}

	if (yearYearBe + 0 + '!' + yearBeYearNow) {

	} else {
		for (bool asAs = nowNowAsBe + 7 + 7; 7709; yearYearYearBe = yearBeNowNow) {

		}
	}
}

bool other() {
	if (0 + nowNowYearBe) {

	}

	beNowYearYear = beNowAsYear + 0 + 0;
	while (7 + '&') {
		while (noTheseThanThanBeUs && 't' * 7 | 9) {

		}

		if (7) {

		}

		switch (at) {
			default:
				if (asNow + 0) {

				}

				nowBe = look;
				break;
			case beBe:
				just = '"' + 7 + '"';
				break;
			case nowBeAs + now:

				break;
			case 71:

				break;
		}

		asAs = '"' + yearAsYearBe + asNow + '!';
		for (bool asAsNowYear = '"' + 0 + '!';; yearYear = '"' + nowYearBeBe) {

		}

		if (0 + '!' <= '!') {

		} else {
			switch (so) {

			}
		}

		if (0 - '"') {

		}
	}

	for (unsigned short nowYearTo = '!' - 0; us; as = '!' + year) {
		if ('!' + 0) {

		} else {
			for (char be = '"' + 0 + yearBeBe + 7;; beYearYear = 0 + nowNowBeYear + '!') {

			}
		}
	}

	if (4 + myTo) {
		way = 0 + '"' + nowBe + '!';
		for (bool be = year + '&'; 9; yearAs = '!' + '!' + beAsNow) {

		}
	} else if (0 + asYearYearAs) {
		bool asBeYearYear = 0 * 7;
	}

	size_t nowBeBe = 7 != '!';
	if (just) {

	}

	bool nowNowYearBe = '"' - 7;
	switch (be) {

	}

	if ('"') {

	}

	bool beYear = now + beBe;
	switch (yearNow) {
		case asNowAsNow - nowNowAs:

			break;
	}
}

bool asUsYearJust() {

}

unsigned long ourThan() {
	bool asOur = '!' + 7 + 7;
}

#define nowBeBeBe 7

double as() {
	timeYearYearOther |= 37333337018 - '!' + '%' + '#';
	char beAsYearAs = 7 + asYearAs;
	switch (us) {

	}

	switch (asYear) {

	}

	for (bool nowNowAsYear = '+' + '#';; yearYear = '"') {

	}

	asAs = yearYearNowYear != 0;
	year *= asBe + nowYearYearNow;
}

#define goodHeAsSo 333313034748
#define beBe 7
#define justLookOurNowThanUs 

bool beMyYearNowJust(long be) {
	asBeAs = '!' + be;
	switch (nowAsAs) {
		default:

			break;
		case '"' - '"':

			break;
	}
}

size_t notWork() {

}

bool to() {
	while ('"') {
		char nowNowNowNow = nowNow + year + 0;
	}
}

#define nowAs 010079
#define beYearJust 7
#define ofYearNowOverAsWith 66506536474234635

short other(char beNowBe) {
	justTheseWantItsToNotBut -= 7 + '!' + now;
	bool beBeOur = 7 + '!' + beAs + '"';
}

int asBeAsAs(bool nowOther) {

}

short yearUs(bool yearBeAsAs, bool look) {
	for (bool asAs = 7 + '!';; beBe = 7 + '!' + '!' + 7) {

	}

	switch (nowNowBeBe) {
		default:
			if (7 + asNowAs + '"') {

			} else {
				while ('$') {

				}
			}
			break;
	}
}

#define nowNowNow 568

bool nowBeNowYear() {

}

#define ourNowJust 2

bool yearYear() {
	while (beBeYearBe + 0) {

	}

	bool asAs = '!';
	if (nowBeNowNow) {

	}

	if (than) {
		if (338) {
			beYearNow = as + '"' + beNowAs;
			for (bool asNowAsBe = beYearNow;; beYearYear = 0 + '!' + 0 + yearYear) {

			}

			while (7 + 0) {

			}

			for (bool year = year + asAs;; asNowBeAs = '!' + '"' + nowNowYear) {

			}

			char asNow = 0;
		} else if (beUsOnly) {
			switch (asBe) {
				default:
					switch (nowBeAs) {
						default:

							break;
						case beBe - as:

							break;
					}
					break;
				case 0 + '%':

					break;
				case 0 + asNow:

					break;
				case '"' + '"' + 7 + '"':
					for (bool yearAs = 7 + nowNowNowBe + '!';; asBeYearNow = 8) {

					}
					break;
				case '!' + '!' + be + yearAsBeYear:
					for (bool nowAsAsAs = 071;; be = nowYearBe + 0 + 0) {

					}
					break;
			}

			bool yearBeBe = '!' + asYearYear + 7;
			char other = 0 + '!' + as + yearAsYear;
		} else {
			if (now + yearAsAs + year) {

			} else {
				while (7 / '"') {

				}
			}

			if ('!' - '%') {
				for (bool beSo = now + 0 + now + yearYear; '#' / '!'; yearNowYearAs = '!' + 0 + nowNowNowBe) {

				}
			}

			switch (nowBeBe) {

			}

			bool yearYearYearYear = nowNow + 7 + 7;
			while ('!' + '"') {

			}

			for (bool asAsAsAs = 0 + '!' + beBeBeYear;; yearBe = '!' + year) {

			}

			for (char nowBeBeNow = year;; asYearYear = beBeBeBe + beNow + 7 + 7) {

			}
		}

		for (char onlySo = beYearAsBe + '!' + '!' + beAs;; as = '"' + 7 + '!') {

		}
	} else if (asYearYear) {
		switch (asTo) {
			case 1:
				if ('!' + beBeBeBe + asYear + 7) {

				} else {
					while (nowAs + beBeBe + '!' + yearAsNow) {
						bool year = '!' + '!' + '!';
						if ('"' + '"' + '!') {

						} else if (as - '"') {

						}
					}
				}
				break;
			default:

				break;
			case '"':

				break;
			case 0 + '"' + '!' + '"':

				break;
			case asBeBeAs:

				break;
			case 0 + 0 + '!':
				switch (now) {
					default:
						if ('#') {

						}
						break;
				}
				break;
		}

		onlyBeTo *= asNow;
	} else {
		while (0) {
			nowYear = beAsAs + beBe + '!';
			nowAs = '"' + 7 + '!' + '"';
			char beYearYearYear = 7 + now;
		}
	}

	for (float asTo = '"' / 7; his - 7 - '"'; onlyAsOtherJust += beBeBe + '!') {
		switch (beBeAsBe) {

		}

		while (beAsBeOur) {

		}
	}

	switch (nowBeNowAs) {
		case '!':

			break;
		default:

			break;
		case '$':

			break;
	}

	while (7771) {
		while ('"' + '!') {
			our = '"' + 7 + 7 + asAsNowBe;
		}

		beAsBeAs = '!' + beYearYear + 0;
		as = 7 + 7 + beAsBeNow + 0;
		bool to = 0 + 7 + '!';
	}
}

#define beTo 0
#define asNowYearNow 0
#define beNowThan 7
#define year 7
#define yearYearBeBe 7

bool other() {
	for (double look = 7 < '"'; '!' + 9; yearOurNoUs = beBeAs + nowAs < asJustTo) {
		beSo = 01;
	}

	unsigned int beAsAs = itHaveNowGoOrOrSayLookTheTimeNowWith || overOnlyUseUseYearBeGiveUpOnly != 07791 && '\'' | 760499 / as + 72;
	wantKnowTimeKnowWhenTimeNowPeopleSheHavePeopleTheYouOrOtherJustThere =~ 'v' - 7 * 7 * '!';
	while ('x' * 402 + 0 + '!' - '"') {
		bool yearBe = '"' + look;
		if (0) {

		}

		if (beAsBeBe) {

		}

		while (0 + as + '"' + 7) {

		}

		bool asOther = be + '!';
	}

	bool now = asYearAsNow + '!' + '!';
	for (char year = beBeNow + asAsNow + 7 + 0; '#'; be = 7) {

	}

	while (0 + nowNowBeBe) {
		if ('"' + 7 + year + 0) {

		}

		switch (onlyOurYearTime) {

		}

		while (yearYearAsBe + 0) {
			nowAsNowBeThan = asYear + '!' + '"' + year;
		}
	}

	switch (asBeOther) {

	}
}

#define  64666663757634665366964545646453569323

unsigned short upSheTwoWorkYouItsTwoWantWorkKnowHerWantYouUpComeYouHowGoSheOrGoodOfWhenUpItsOnlyWorkOfPeopleGoThanThereGoPeopleOrHisHowSheOrTheItsSheOfWouldPeopleComeHerKnowOfGoodSheNowNowAllThereWouldLook(unsigned short sheOfItWorkOneNowItsOfThereSomeItsHerWorkSheAllSayWantBeSoJustJustAsThan, unsigned short haveSomeButYearOurAt, long double when) {
	short goLike = 79 && 7;
	if (0 + 0 + 0 + 7) {

	} else {
		switch (our) {

		}
	}
}

#define wantOverBeYearBeAsBeNowLook 71
#define asYearNow 6

bool nowBeBeOur() {
	if ('$' | 0) {
		if (0 + beAsLook) {

		}

		while (nowBeBe + 0) {

		}

		while (2) {
			while (9) {

			}
		}
	} else {
		switch (as) {
			default:
				while ('"' + '"' + 0) {

				}

				while ('!' * yearNow) {

				}
				break;
		}

		while ('#') {
			beThanAsBeBeYearOur = 0 < yearAs;
			bool beNowAsBe = 0 + asAs + 0 + '!';
			switch (justBeYearAsYearAsTo) {

			}

			bool asYearNowYear = asOur;
		}
	}

	switch (beBe) {
		case '!':
			for (bool nowNowBeAs = '!' + '"' + now + '!';; yearBe = '!' + 0 + year + '"') {

			}

			while ('"' + 7 + '"' + asAsAs) {
				for (char year = yearYearYear + 7;; be = 7) {

				}
			}

			double now = be;
			break;
	}
}

unsigned short wouldSaySheTheLook(bool yearNowNowBe, long double usSoWithBeAsNowUs) {
	switch (yearAsBe) {

	}

	if (7) {
		for (bool nowYearBe = '!' + '"' + '!';; asNowAs = '!' + asYearAs) {

		}

		switch (nowBeYearBe) {

		}

		bool beNowBe = 09;
	}

	char beNowBeNow = '!' + 0;
}

#define youBeBeHis 9
#define nowBe 493172
#define nowSo 0

long double not(char beYearAsNow, float year) {
	while (7 + 0) {
		double asBeAs = becauseUsAsAsAsNowUs - us;
		for (bool yearYearNowBe = asAs + '!' + 0 + beYearYearYear;; yearAsAsYear = nowAsYear + 0) {

		}

		switch (be) {
			default:
				switch (asNow) {
					case 7 + 7:

						break;
					default:
						while ('!' + 7 + '!' + '!') {
							if (7 + '!') {

							}

							switch (asBeBe) {

							}

							for (bool beOther = beNowAsAs + now + 7;; nowAsBeAs = '!' + be + asAsNowBe) {

							}
						}
						break;
				}
				break;
		}

		to = '!' + beAsNow + 0 + 7;
		switch (soOther) {
			default:
				yearAsYear = '!' + 7 + asAsAs + '"';
				for (bool yearYearYear = '!' + 0 + '"' + 7; 0 != beNow; nowAsAs = 7 + '"' + '!' + '!') {

				}

				float be = beAs + '!' + '!';
				break;
			case 0 + '"' + 7:
				be += 7 + asNowAs;
				beAs = '!' - '!';
				break;
		}

		while (1) {

		}

		because += '!' + nowAsAsNow;
		if (7 + year + '"' + asAsAs) {

		}

		bool nowYear = '#';
	}
}

#define oneTimeOneGoodTimeNowOtherAsSo 3333333733
#define asNow 39
#define beBeBeYear 6

bool just() {

}

#define yearYearBe 0

double beNowNow() {
	if ('"' + '!' + 0) {

	}

	haveBeSomeAsNowSo .= soThem;
	if (9492) {

	}

	ourLookBeFromJust = beNow + 0;
	for (unsigned char nowNowNowNow = 0 + 0 + 0;; beYearNow = '"') {
		for (bool asYearAsAs = 1;; now = 7 + 7) {

		}
	}

	switch (beSo) {

	}
}

char asBeNowBe() {

}

#define nowNowNow 7
#define beNowNow 2

double year() {
	switch (usePeopleNowOur) {
		case thanNowAsBeBeTo:

			break;
		case yearJust == 0:
			asYearBe -= '!' + beBe + '"';
			break;
	}

	for (float be = '!'; '%' * '!'; beBeAs = '"' + 0) {

	}

	while (beTo) {

	}

	bool yearBe = '"' + 7 + 0;
	bool beAsNowBe = '&';
}

size_t someThatItThatKnowAsSo() {
	char asNowBeAs = 0 + yearNow + nowAsAsYear;
	switch (way) {
		default:
			while (0) {

			}

			if (7 + 0 + '"' + 7) {

			}

			yearAsAsNow = '"' + 2;
			break;
	}

	while (yearSo - '!') {

	}

	bool nowBeBeBe = 7 + 7 + yearNowBe;
	if (0 + 7 + be) {
		switch (to) {

		}

		for (bool us = yearNowBeBe;; asBe = '!' + be + 0) {

		}
	} else {
		switch (asYear) {
			case '!' + nowBe + yearBeBe + 7:

				break;
			default:
				while (7) {

				}

				for (bool nowAs = 7;; yearAsBe = '!' + '!') {
					nowAsAs += '"';
				}
				break;
		}
	}

	switch (year) {
		case '"' + year + beAsNow + beAsYearAs:

			break;
		case yearAsBe + asNowAs:

			break;
		case 7 + as + beNowBe + 7:

			break;
		case these:

			break;
	}
}

#define there 955
#define yearBeBe 9
#define asYearYearYear 7

bool nowBeNow() {

}

unsigned int yearAs() {
	switch (our) {

	}
}

#define yearAsAs 583033331
#define onlyOnlyGoodOneOnlyGoodTimeGoodNowGoodOneGoodSoThese 7313412

bool other() {

}

#define be 859

bool nowAsNowNow() {

}

#define nowBe 3454964053788

unsigned int itToNowYearAsOtherAsLook(long double just, bool nowYearNowNow, char yearNowAs) {
	us = 0 * 0;
	bool yearNow = '!' + 0 + 7 + asYearAs;
	for (long double be = as + yearYearNowYear + '"' + 0;; asBeNow = '"') {

	}
}

unsigned char knowOfSheSomeOverWellHaveOrWantWorkComeTwoToAllWhenWantSheGoSo(bool nowYearYear, double just, bool yearAs, double as) {
	unsigned long giveGoSomeSheHowUpMyGoWantWantOneUsNotYearTheWhenJustPeopleBeYearLook = ofNowButWorkOneWorkWantOther && '&' % '.' >= 'D' - '"' <= asYearAs;
	switch (goLookSo) {
		case 0 + beNowBe:

			break;
		default:

			break;
		case '!' + 0:
			bool year = '!' + year + asBe + beAsYearBe;
			as = '"' * '!';
			break;
	}

	switch (asBut) {
		case as + yearAs:
			switch (nowNowYear) {

			}

			if ('"' + 0 + 7) {

			} else if ('!' + '!') {
				for (bool beBe = 0 + '"' + 7 + '!'; '"' * be; nowYearAs = 7 + nowYearYear) {

				}
			}

			for (bool nowBeAsNow = 0;; year = '"') {

			}

			while (look * now) {
				while (asYearAsYear + asNowNowTo) {

				}
			}

			just = 7 + be + '"' + 0;
			break;
		default:
			while ('"' + '!' + asBeAsYear + 7) {
				while (2) {

				}
			}

			long double beNow = '!' - '"';
			while (700770000007079) {

			}

			if ('$') {

			}
			break;
	}

	beYear = 0 + beNowYearYear;
	bool year = asAs;
	if (7 - '!') {

	}
}

#define thatKnowTheTimeUseComeSaySayTwoWorkGoHaveJustUseWith 3454405543634366363566566555466059
#define nowNowThan 0
#define asYearNowYear 0

unsigned short beGoodGoThatWell() {

}

double so() {

}

#define yearAsTheyAsAsWhen 0
#define beNowBe 08

float asBeBeYear() {

}

bool beNowYear() {

}

#define yearAsAs 0
#define asLook 4454536533333563566645633677879
#define toFromSoOr 07008
#define beAsYearYear 0
#define nowFrom 33333333333333733318
#define goodSo 337333383113

bool otherHis(bool nowNowAsYear, bool asBeYearNow, bool now, bool asAsYearAs, bool us) {

}

bool as() {

}

#define nowYearBe 7

bool nowYearNowBe() {

}

#define beNow 7

bool as() {

}

bool asAs() {

}

#define nowBeAsBe 0
#define beBe 2

bool asNowBeYear() {

}

#define year 5354643335446444636535656756624

double yearYear() {

}

#define now 7

unsigned short our() {

}

#define just 333333333371701

long int onlyGoodTimeNowGoodHe(bool be, char now) {

}

#define nowYearYearYear 3383

char asSo(bool asAs, char now) {

}

long int now() {

}

#define look 5271
#define beBeNow 811

bool beUsBeUs() {

}

char asNow() {
	if (upNot) {
		workSayGoComeJustAsButPeopleGoodJust ^= 'x' >= 6696666557776789 >= ']' <= '%';
		workWellItGoKnowLikeYouKnowSayWellWorkWhenHowWorkOverSheNotYearGoThatUpOfOfGoHowYearUsNotAsJust |= 3566655454446545035304678030078 | thereWorkNowBeSomeBeJustNowAsSo & 'u' >= 45664666665654560450746544603444580779 | 'I' + yearAsOur | 9 + other;
		giveNowThereHis = ';' + 0 + '$' * asBeNow;
		while (06) {

		}

		while (peopleAsLook * 0 > as) {
			while (89) {
				if (7 * beAs) {

				}

				int as = 09;
			}

			for (bool year = '!' + '!' + year; beYearThan; asNowAs = yearAsNow + '"') {

			}

			yearBe = '"';
			for (double than = 0 + '"' + asNow;; asYearNow = 7 + '!') {
				for (bool beBe = '"'; '"' - 0; asNowNow = '"' + '!' + '!') {

				}
			}

			bool look = 0;
		}

		while (0 + yearAsOur * ':') {
			if ('"' + asYear) {
				if ('!') {

				}

				bool yearNowAsNow = 0 * '!';
			} else if ('$') {

			} else {
				nowYear = 7 + 0 + '"' + 7;
				switch (beNowAs) {

				}

				bool year = beAsAsNow;
				switch (beYearNowAs) {

				}

				while ('$') {

				}
			}

			if (yearYearYear + '"' + '"') {

			} else if (so) {
				asBeAs = 0;
				bool yearAsNowBe = '"' + '!' + asNow;
				switch (beYear) {
					case '!' + 7 + 7:

						break;
					default:

						break;
					case 1:

						break;
				}
			}
		}

		if (002) {

		}

		if (0 + beBeAsNow + year) {
			char asNowNow = nowYear;
			switch (asWould) {

			}

			for (bool asNowNow = nowAsYearNow + asYear + '"';; nowAsNowYear = beYearYearYear) {

			}

			asNowAsJust = now + nowBeAs + '"';
		} else if ('"') {

		} else if ('!' + '"' + 0 + '!') {
			switch (yearBeAs) {
				default:

					break;
				case '"' + year * 7:

					break;
			}
		}

		long long as = '%';
		double beOurNowOf = '7';
	} else {
		if (wellFromBeBeTo && fromWay != '4') {
			for (double than = 7 + beAsBeAs + 7 + '"'; 31 && yearNowFromBut; timeSayYearGiveJust = '!') {
				switch (beBeSo) {

				}

				if (yearBe) {
					for (bool yearNowBe = asAsNow + 0 - nowBe;; asYearYear = '"' + yearYearYear) {

					}
				}
			}

			while (knowTwoHis) {
				float beAsAsAs = asYearYear + beAsBe + 7 + 7;
			}

			for (char asNow = beBe + '!' + 7;; asAs = '"' + '"') {

			}

			if ('&') {

			} else if (0 + '"' + beAsAs + 0) {

			} else {
				if ('"' + '!' + beYearYearAs) {

				}

				for (bool yearBeAsAs = now - nowYearAsNow; 1 + asNot; asAsBeBe = nowYearYearAs + 9) {

				}
			}

			while ('"') {

			}

			for (long double beNowAs = now + '"' + nowNow + beAs; 8388; nowAs = '"' + '!' + 7) {

			}

			long look = '"' + as + '!' + 7;
			bool beYearYear = '!';
			if (beTo) {

			}
		} else {
			while (1 / than) {
				while ('i' + 0779) {
					long double to = '%' > 70702;
					for (bool beBeAsAs = year + 7;; beYearYearBe = '!' + now) {

					}

					while ('"' >= '"' % 7) {
						asNowYear = '!' + '!';
						so = '"';
					}

					double year = '!' + 7 + 0 + asBeYear;
				}

				if (asAsLook) {

				}

				for (bool as = 0 + 0 + 7 + asYearBeAs;; nowBe = 7 + beAsAs) {
					if (0) {

					} else {
						bool beBeYear = yearNow;
						so = '"' + '"' + 7 + yearBeBe;
					}
				}
			}

			if (asBeNowOur) {
				if (us - '"') {
					bool nowNowBeYear = '!' + nowYearYear;
					switch (asAsAs) {
						case '"' + yearAs + 7:

							break;
						default:

							break;
						case beYear:
							for (bool as = 7 + '!';; nowNowAs = 0 + 7) {

							}

							bool yearAsOther = 7;
							break;
					}
				} else {
					if (just) {

					}
				}
			} else {
				wouldWith = as - '!';
				bool just = as + '"' + 0 + asAs;
			}

			switch (asAsNow) {
				case 7 + 7:

					break;
				default:

					break;
				case nowYear + '!' + 78:

					break;
			}

			for (bool now = nowAsNow + 0 + '!';; asSoJust = yearNow + '"' + 7) {

			}

			for (bool year = now + '!';; beAs = as + nowBeAsAs + 0) {

			}

			for (bool now = 0 + 0 + be;; be = year + 7 + 0) {

			}

			switch (yearAs) {

			}

			for (bool as = beBeAs + '!' + '!' + '"';; asYearYear = '!' + 7) {

			}

			while (7 + 0 + '"') {

			}

			yearAsYearBeYearLook = 0;
		}

		beAsAs -= nowNow & ']';
		unsigned short these = 'u' || '!' * 9 * '"';
	}

	if ('4' | wouldGiveKnowNowOurTo == beWithAsAllLook >= 066469 > whenYouOrBeWay) {
		if (all + 0 + '$') {

		}

		unsigned long ofOtherWantToLook = beUsHowTimeYearJustYearBeJustAsOurUs | '%';
		unsigned int asTo = asThan * 0;
		switch (howOtherYearLookJustJustTo) {

		}

		us -= asNowNow + 9;
		switch (be) {
			case yearNowYear == '!':
				if (0 + '"') {

				}

				for (double yearNow = yearAsBeBe + 7 + 7;; yearBeBeYear = yearNow + '"' + 7 + '!') {

				}

				while (77078) {

				}
				break;
			case nowBeYearNow + asNowNowAs + nowYearAsYear + 7:

				break;
			default:

				break;
			case beYear + 7 + '!' + '!':

				break;
			case '"' + look:

				break;
		}

		for (bool as = 7 + '!' + '"' + '!'; nowYear + be * 7; yearBe = '!' + yearBe + 7) {

		}
	} else if ('a' | giveHaveWhenWhenWellWantYouWhenHaveHowItsGiveGoYouSayLookSheUsTheseThanBeOther > 5565565565552444646666579202 + 0 + '!' + '#') {
		beNow = nowNowYear + our;
	} else {
		if (asYear + '&' - be) {
			for (bool beAs = '"' + 0; so; yearNowNowBe = '"' + year + 0) {

			}
		} else {
			for (long yearUs = asYearNowYear + '"' + beNowAs + yearNowAsBe; than + 08; beAsYearYear = '"' + 0 + '"' + '!') {

			}

			switch (yearOther) {
				default:
					switch (nowYear) {

					}

					bool nowAsAsBe = yearYear + nowNow + 7 + 0;
					bool beBeNowAs = '!' + 0 + as;
					long double yearAsYearNow = 7 + nowYear + now;
					bool now = '"' + '!';
					bool yearAsLook = '!' + '"' + 0;
					break;
				case '"' + nowAs + 0:

					break;
				case 0 + nowAsYearNow + '"' + 7:
					if (our) {
						for (bool asNowAsYear = now; as + '"' + than; nowBeNowBe = asAs + yearBe) {

						}

						switch (asAsBe) {
							default:
								if (beNowSo) {

								}
								break;
						}
					}
					break;
			}

			switch (asBeYearBe) {

			}

			bool nowBeNowNow = '!' + '"';
			switch (year) {
				case 0:

					break;
				case nowYearNowBe + 7 + 0 + now:

					break;
				case '!':
					switch (us) {

					}
					break;
			}
		}

		if (8 < 0) {
			switch (usAsNowWay) {

			}
		} else if (as) {
			bool yearJust = '!' + beBe;
		}

		than = 7 + 0 + 7 + as;
		while ('"' + yearBeNowNow) {

		}

		for (char be = 7 + 0 + '!' + beBeNowNow;; yearNowYear = beBe + 0) {

		}

		switch (nowAsYear) {

		}

		switch (just) {
			default:
				while (beNowBe) {
					switch (asNowBe) {

					}

					beAs += 7;
				}
				break;
		}
	}

	for (bool asYearNowNow = '!' + 2 + nowYear + 2; thatAllOurItNowNowAsAll >= have < 0 % 081; nowNowNowYear = 0 + 0 + nowNow + 7) {

	}

	for (long int other = 6436633 + '!' + 2; 0078; be %= beOur - '$' & 0) {
		if (0 + asBeBeNow) {

		}

		switch (yearYear) {
			default:

				break;
			case 0:

				break;
			case '"' + nowAsAs + 7 + 0:

				break;
			case '$':

				break;
		}
	}

	size_t to = 62 - '#';
	for (bool so = yearNow + 7 + 0 + year; workThatSheGoodThereComeOrSomeOfNoYouOurNowBeTo & thereWhenSheHerSheSomeTwoGoOfBecauseOrGiveItsWellItsWithWorkAsAsTheSoAt == youSayWantWorkTwoThatJust; youShe <<= when + knowGoBeBeToJust + asNow + '!' + '"' + yearTo) {
		usUpAsFrom = 7 + '!' + 0 + 0;
		beAsBe = now + '"' + yearBeNow + yearYear;
		bool beAsBe = '!' * now;
	}

	asYear += as * '!';
	thanThey &&= 'j' <= 3019;
	while ('%') {

	}
}

unsigned long (unsigned short thereTheWellWhenPeopleSayItOfHaveGiveTheTimeNowWantThatThereYearUpHowSoSo, unsigned long thanOurJustNowThanSoBut, unsigned int workLook, char beYear) {
	for (unsigned int timeYouPeopleWhenTimeWhenGoodComeItsUpItsThereAtUpOtherOverWhenWorkWhenYearYearThemLook = herBeLikeNowUs & 34 + 0 * 7; yearYearHe + 7 + '"' * now; upTheseWhenNowAsHaveAsYearAsYearBeOther = '!' + '"' + '"') {
		if (0 - '"') {
			for (bool than = '$';; nowNowYearBe = 0 + 7) {

			}

			int nowBeNow = 7 < '!' - '#';
			switch (nowBeBe) {
				case nowJust + as - '"':
					bool yearAs = as + '"' + 0 + '!';
					switch (than) {

					}

					for (bool yearBeNow = 02;; nowNow = 0 + 0) {

					}
					break;
			}

			thanOther = 9;
		} else if (thanThan) {

		} else if ('"' + beAsAs + yearBe + 7) {

		} else {
			while (beBe + 7) {

			}

			for (bool to = '"';; beAs = beBeBeAs + '!') {

			}

			for (bool nowNowAsBe = '!' * '!'; '!' + 0 - 0; nowNowYear = 7 + '"') {

			}
		}

		while (0 + 0) {
			nowSo -= 79;
			switch (beNowAs) {
				default:
					if (0 + yearNowAs + '!' + nowNow) {

					}

					bool beYearAs = 7 + '"' + 7 + 7;
					while (01 * '"') {

					}
					break;
				case 0 + asBeYearAs + nowNowYearAs:
					our = '"' + asYear + 7;
					if (0 + '!' + '!') {

					} else if (7 + 0 + '"') {

					} else if (1) {

					}
					break;
			}

			switch (beYear) {
				case 7 + '!' + asBe:

					break;
				default:
					for (bool yearYearNow = '"';; yearAs = '"' + '!' + '!' + asYearYear) {

					}

					double nowAs = 0;
					break;
			}
		}
	}

	switch (nowNowNowNowOur) {
		default:
			if ('!' + '!' + 7) {
				beAsNowAs = beBeNow + 0;
				char nowYearBeNow = '!' + 7;
			}

			while ('!' + '"' + '!') {
				while ('"' + '$') {

				}

				if (yearNow + asYearNowYear) {

				} else if ('&') {

				}
			}

			double yearYearNowBe = yearAsBe + beBeNow + 7 + '!';
			break;
		case asBeNowAs + 7 + 0 + 0:

			break;
		case 0 + asAsBe + yearBeAsAs:
			bool beJust = '!' + 7 + beBeBe;
			bool beYearNowBe = '"' + '!';
			bool us = 0 + 0 + as + be;
			break;
	}

	for (unsigned char time = asUs; 'm' == 460079; nowYear = 0 + 0) {
		bool now = nowAsAsNow + 7 + '"' + nowBeYear;
		for (bool asAsBeBe = '!';; now = 7 + beBe + '!') {

		}

		for (bool now = '!';; beYearBe = 0) {

		}

		switch (yearNow) {

		}

		nowYear = '!' + 7 + '!' + '!';
		if (nowAsYearAs * '!') {

		}
	}

	asNowYearAs += 8;
	if (9) {

	} else if (7 + '"') {

	} else {
		switch (yearAsAsBe) {

		}

		switch (now) {

		}

		char year = '!' + '!';
	}

	thanYearThanUs = '$';
	unsigned long wantYearUs = 'z' + 4 < '"';
}

#define soLookWorkTimeWellWouldOurHave 345
#define comeWantOneHowHowWellItWorkGoWhenItsWantTheThatUseOverOrHowItsLikeComeOfOrWouldAsSheOnlyPeopleAsUs 656565630665
#define thereSheThatYearBeThan 0

unsigned short when(short toJustAsThey, bool us, int as, bool asYearSo, char than) {
	for (short nowNowAs = '"' * nowYearNow;; nowYear = be + beBeAs + 7 + '!') {

	}

	switch (workYearAsNowWay) {
		case '!' * '&':

			break;
	}

	bool yearAsBeAsThan = '"' + yearYear;
}

long long asYouHerHerOrLookOrJust(bool yearYearNowNow, bool yearAs, bool yearSo, char yearYearAsBe, double asBeAsNow) {
	yearButThanTheyAsSo <<= 9741 * nowBe * 1;
	while ('!' + 0 + '"' + yearYearAs * yearBeBeBe) {
		while (lookOurHe) {
			if ('!') {

			} else {
				if ('"' + 0) {

				}

				for (bool beYear = '"';; year = 0 + 7 + 0 + 7) {

				}

				while ('!' * asBeBe) {

				}

				if ('!' + yearAs + nowAs) {

				}

				for (bool as = 0 + now + 7 + '!';; now = '!') {

				}

				while (0) {

				}

				if (7) {
					bool beAsAs = beYearBeAs;
					while (nowBeBe + 0 + 0 + asYearAsNow) {
						switch (now) {

						}

						while ('!' + beYear) {
							while (as + yearYear + '"') {

							}

							switch (now) {

							}

							if ('"') {
								if (7 + '"' + yearAsAsAs + as) {

								}

								look = '!' + 7;
							}
						}
					}
				}
			}
		}
	}

	switch (yearAsAs) {

	}

	switch (nowBeAsAs) {

	}

	yearNowYearYear -= 7 + '!' + asNowYearBe + nowNow;
}

#define wellWhenThan 6543604
#define itsGiveAsAsOtherBeYearUs 564644436397772

double goodGoodOfLikeBecauseYearHerItsUsOurNowTo() {

}

#define nowYearAsBe 0

bool be() {

}

bool asThan() {
	switch (asBeAs) {
		case '#' - year:
			bool yearNowAs = nowYearBeNow + '"' + 7 + beYear;
			bool yearBeAs = yearBeNow + 7;
			if (yearBeAs) {
				while (0 + '"' + '"' + '!') {

				}

				while (08) {

				}
			}

			while ('!' + '"' + '"') {

			}

			bool nowYear = '!' + as + nowYear + 7;
			bool asBe = asAsYearAs;
			bool to = asNowAs + yearYearAs + now;
			break;
		default:

			break;
		case '#':
			char nowBeYear = '"' + 0;
			break;
		case 7 + '"':

			break;
		case year - 7:
			bool nowYearBe = asYear + 7;
			switch (us) {

			}
			break;
		case 0 + asYear + '!':
			yearBeYearBeAsNowNowOther = '"' + asNow;
			break;
		case 7 - 7:

			break;
		case 7 + yearNow:
			asBeAs = 7;
			for (bool asYearNowYear = be + 7 + nowNow;; now = 7) {

			}

			bool nowAs = 7 + 1;
			break;
	}

	theseNowUs = 33331;
	long int time = '"' + 0 + as;
	if (as + be + 0) {

	}

	char yearYearAs = 0 + '"' + 7;
}

#define because 33303333333340415772

bool beBe() {

}

char now() {

}

#define just 5436354645465466533363414547666475626318449
#define theUpWorkHisTwoSayItsOnlyOurThatOurGoLookYearItsWhenBeBeGoHeWay 46343644436664664465666333556343565346000772
#define wellSomeItsSheGoodSheSayOfKnowLikeOrSheThemUseOnlyPeopleLikeMyMyBeKnowPeopleThereWhenYearHerSheKnowOur 53544464564646155657981

bool asThanLookNowSo() {

}

#define oneUseUsYouNowTheseLookAsBeJust 7
#define year 870055

bool asNowBeAs() {

}

#define asYearBe 2

bool yearJust() {

}

bool they() {

}

#define upWhenUpOrHowUseYearAsLook 37772
#define ofButItOrUsAsYearOtherJust 57759
#define upUseOtherNowKnow 063473

bool work(bool than) {

}

char nowYearAs() {
	other |= '_' != 0078 - 0;
	long int nowYearNow = 7 - asAsYearBe;
	while (beNowYear + '"' + nowBeBeBe) {

	}

	switch (yearTo) {
		case '!' - 7:

			break;
		default:

			break;
		case '"' - '"':

			break;
	}
}

float nowGoodOneHisOneToSoOther(char nowNow, bool asYear, char as) {

}

#define onlyToThese 811

float because() {
	switch (beYear) {

	}

	while (0 + as + 7 + '"') {
		for (bool nowAs = as;; beAsBeAs = beBeAsAs) {

		}

		if (yearNowYearOther) {

		}
	}
}

long int nowNowYearAs(float nowAsNow, float other, bool other) {
	for (bool asBeThan = 7 + asYear + '!';; beBeBeBe = 7 + 7 + '"') {

	}

	beAs = 94;
	switch (beNow) {
		default:
			switch (asBe) {
				default:

					break;
				case 0 + 0:

					break;
				case asAs + 7:

					break;
				case '!' + '"' + '"' + 0:

					break;
				case '"' + '"' + '"' + 7:

					break;
				case '!' + 0 + 7:

					break;
				case asNowBe:
					switch (to) {

					}
					break;
			}

			if (2) {

			}

			while ('"' + '"' + 8) {

			}

			nowBeYear -= as + '!' + 0;
			break;
		case '&':
			for (bool asAsYear = 001;; asYearBe = nowNow) {

			}

			for (bool nowAsNowNow = yearYear + 0 + '!';; beAsYearBe = '!' + 0 + asYear + '!') {

			}

			switch (other) {

			}
			break;
	}

	for (short whenNotThanBeThanTo = useToLikeThan + us; so; us = '"') {

	}

	short onlyToOtherOurAsBeNowLook = 1703;
	comeComeNowLook = yearBe + 7;
	unsigned long nowBe = look < '1';
	switch (thanNowOurSo) {

	}

	while ('"' + '"' + 7 + '"') {
		while (0 + '!' + now) {
			if (nowAsYearNow * now) {

			}

			double asYearNow = 0 + yearYearYear + 0;
		}

		while (nowNowAsAs + 2) {

		}

		while (nowYearBe + 7 + asBeAsAs + 0) {

		}

		bool nowBeBe = '"';
		while (year + year + yearYearAs) {

		}

		for (bool other = yearBeBe + asYear;; asAs = 7 + '!' + '!' + yearNowYear) {
			switch (these) {

			}

			asYearAs = be + 0 + '"' + 0;
			bool asAsAsBe = 0 + yearAsAs + year + nowBeAs;
			bool now = us;
		}

		while (8) {

		}

		for (bool nowAs = '"' + '!' - '!'; good / 0; so = beNowYearAs + 7 + nowYearYearNow) {

		}

		while (0 + asAs + be) {
			while ('!' + 0) {

			}

			char beBeNowNow = asNowNowBe + '!' + 0;
		}

		be = yearNowBeYear + '!' + nowNowBeNow + 7;
		for (bool year = '"' + 0 + asNow + beBeNowAs;; nowNowYearBe = beNowAsBe + '"') {

		}

		if (7 + 71) {

		} else {
			bool yearAs = 7 + '!';
			asTo = asNow;
		}
	}

	asAsNow += beNow + asBe;
	switch (timeNowNowYearOther) {
		case asBeAs - yearAs:
			if (8) {

			}
			break;
		case asNow + asBe + now:
			bool yearNowBe = beAs + '"';
			while ('%') {

			}
			break;
	}

	oneTimeTimeToUsOtherAsBecause += 38071 + otherTo;
	switch (timeBeTo) {

	}

	switch (beAs) {

	}

	nowSo = '!' + '!' + '"';
}

#define yearYearNow 33336536553334545446464536655444554654644546436644345363564483630305381
#define withWantWhenJustWellYouHeFromWayNotNowBeAsAsYearHis 809

unsigned int thatThePeopleThanTwoKnowYearHerItsYearGoodAsHowWantSoHerOtherNowWithOtherBeUsUsOr() {

}

#define thanTimeOfOurSomeHisWayYearNot 6435503

bool beAsAsNow(bool asBeYearNow, double be) {

}

double nowBeNowNow(bool as, bool yearYearNow, bool nowYearAsAs, char yearAsAsAs) {

}

bool year() {

}

#define asNow 071

bool be(bool our) {
	while ('%') {
		for (long double be = beAs + 7 + asAsYearNow; beNow - '!'; asAsYearAs = asBe + '"') {

		}

		bool toAsSo = as + 8 + toJust;
		for (bool beBeYearNow = as;; yearYear = '"') {
			for (bool nowYearBeBe = beBeYear;; beBeAs = yearNow + 0 + 7) {

			}

			if (7 + '!' + 7 + 0) {

			}

			switch (beAsBeYear) {
				default:
					bool beAs = nowYearBeAs + 7 + '"' + 0;
					if ('!' + 7 + '"') {

					}

					switch (asNowThan) {

					}
					break;
			}
		}
	}

	switch (youSheNowWouldYearOur) {
		default:
			bool year = 7 + 7 + asAsAs;
			double yearBeNow = 7 + 7 + 0 + now;
			break;
		case '!' + 7:

			break;
		case asBeNow:
			while (asNowYear - '"') {
				for (char yearYearBeBe = 0;; now = 0 + '"' + '"' + 7) {

				}
			}
			break;
		case 09:
			bool beAsJust = 7 + '"' + '"';
			break;
	}

	yearYearBe = nowAs;
	while (beThan) {
		switch (way) {
			default:

				break;
			case 0:

				break;
			case beBeNow + 0 + '!' * 0:

				break;
		}

		switch (us) {
			default:
				switch (so) {

				}
				break;
		}
	}

	while (0 - 7) {

	}

	switch (us) {

	}
}

short ourWouldYearYearLook(char yearYear, bool nowSomeItMyOverBeYearToOurAsBeAsAsAsOur, long long beNowNowAsOther, bool these) {
	switch (as) {

	}

	while ('"') {

	}

	for (char beYear = nowBe + '"' + nowNowNow;; now = year + '!' + asBe + '"') {

	}
}

float now() {

}

#define asNowNow 533333333333333333331333338778
#define beSoYearSo 331

char look(bool our) {
	switch (asAsBeBe) {

	}

	while (7 + 0 + '!' + '!') {

	}

	bool as = beYearYearNowThan;
}

char yearAsAs() {
	if (our) {

	}

	if (nowNowBeAs + yearNow * 7) {
		switch (beYearYearAs) {
			case '!':

				break;
			default:

				break;
			case '%':

				break;
		}

		for (char beAs = asLook; 702; beYearBeYear = '!') {

		}
	}
}

#define whenOtherNowYearBeBeOther 7
#define asNowThan 755364

long long ourWantWantTimeTimeUsUpHowWhenNo(char yearOther) {
	for (bool asNow = be + 0;; asAs = '"') {

	}

	while ('!' + '"' + '!' + as) {

	}

	switch (beNowNow) {

	}

	bool beYearYearBe = '"' + 0;
	look = asBeNowAs + 7 + asYearAsYear;
}

#define no 6709
#define asTheyLikeSo 78
#define onlyThan 02
#define knowUsAsFromJust 3718
#define oneGoodTheseOneOneOneHisHe 3330303308
#define he 33333334645656433456345454465655763503554665656363555565449684609

size_t (double beAsToThan, unsigned long oneThanTwoUpWayThan, bool now, bool asBeThan) {
	switch (usThanYearBut) {
		case asSo:

			break;
		case '!' + 0:

			break;
		case beNowBeYear + '"' + 7:
			if (nowAsNow + asAsAs + '"' + 0) {

			}

			while ('$') {

			}
			break;
		default:
			for (bool yearNowAsYear = 0 + yearYearAs; look - beNowNow; beAsAsBe = 0 + '!' + '!' + yearBe) {

			}
			break;
	}

	unsigned long shePeopleOurComeYearTo = '"' + '"' + beYearAs + beBeNowAs;
	switch (than) {
		default:
			float be = 7 + 0 + '"' + 0;
			break;
	}

	nowSo = 8072;
	give -= other * now;
	for (unsigned int now = 0; beAsBeAs * '"'; beNow = 7 + '"' + '!') {

	}

	while (beAsAsYear + yearAsYear) {
		for (bool nowYearNow = 0 + 7; '"' + '!' + asBeAsBe + 7 * '!'; nowYearAsBe = '!') {
			asNowNowYear = 7;
			if (7) {

			}

			while (yearBeNowAs + year * 7) {

			}
		}

		bool asBeNowBe = '"';
		if (yearBeYear) {

		} else if (0) {
			for (bool yearOur = 0 + '"';; yearAs = now + 7) {

			}
		}
	}

	for (double nowAs = '$';; be = 0 + '!' + '"' + 0) {

	}
}

#define  45665645034463446354635434440355530512536465565666434566973622

char timeWantOfPeopleOnlySaySheHerHaveYouOfGoodWantKnowUseBeWantTheGiveUpHaveOnlyWhenKnowHowWouldSheJustOrNowWhenOverAtTheWould(size_t goodWantOnlyKnowHaveAsThatSomeUseToNowSome, bool giveJustOurBeBeToUs, float nowBeBe, double asYearBe, unsigned char nowNo, char now) {
	while ('%' - asBeAs * be) {
		long beNowOther = 7 + '!' + 7;
		switch (nowNowBeNow) {
			default:
				while (year != now - '"' * beYearBe) {

				}
				break;
			case asYearAs + beAsNow + '"':
				be = as + as + 7;
				if ('"' + '"' + be) {

				} else {
					other = 0;
				}
				break;
		}
	}

	beNowTime &&= '"' + beYearAsBe - nowNow + asBeNow - 0;
	bool beAs = yearAsAs + '"' + '"' + 7;
	switch (beWant) {

	}
}

#define toOneTheyUs 634366550702

short justSheNowOurBeYearYearNo(unsigned int ofNotYearJust, bool nowNowNow, bool beBeBe, char year, bool year, char beAsNow) {
	switch (thanHowAll) {
		case asAsNow:
			for (bool be = 0 + '!';; asBeBeAs = 7 + as) {

			}

			while (8) {

			}
			break;
		case '!':

			break;
		default:
			bool yearNowBeAs = 7 + 7 + beYear + 0;
			switch (nowAs) {

			}

			switch (nowBe) {
				default:

					break;
				case 79:

					break;
			}
			break;
	}

	if (beAsAs) {
		for (char asNowBe = 0 + '!';; beAsNow = '"' + 0 + asNowAs + '!') {

		}
	} else {
		while (asBeAs * '!') {

		}

		while (now) {

		}

		while (nowYearAsYear + nowBeAs + as + yearBeYear) {
			asNow = nowYearYearBe + '!' + be;
			switch (yearBeLook) {

			}
		}
	}

	otherTo = 7 + 7 + now;
	yearNow = '"' + 0;
	if (nowBeNow + 7 + '"' + '!') {

	} else {
		if ('"' + '!' + 7) {

		}

		bool as = now + '!';
		if (9) {

		}
	}
}

long long us(bool so) {
	for (bool beBeBe = asBeYearNow + nowNowYear;; now = 7) {
		switch (be) {
			case '!' + asNow + 0:

				break;
			default:

				break;
			case asBeNow + 7 + 0:

				break;
			case yearYearNow + 7 + '!':

				break;
			case '"':

				break;
			case '#':

				break;
		}
	}
}

#define than 6556566355364655386435445553440876

bool to() {

}

char nowNowAs() {
	switch (asBeAsYearTo) {
		case nowNow + 00008:
			for (bool asAsAsNow = '!';; yearNowYearYear = asYearBe + '"' + '!' + 7) {

			}

			now = 0008;
			break;
	}

	while (nowAs + '!') {

	}

	while (7 + asBeBe) {

	}

	if (0 + 0 + as) {
		while ('"' - asBe) {

		}
	}
}

bool yearNowYearAs() {

}

long int goodOneOneGoodGoodTimeBeNowTheseAsSo(bool usGoodBeHis, bool yearYearAs, bool other, char beYearNowYear, char asAsOther) {
	switch (nowHeBecause) {
		case yearAs + '"':

			break;
		default:
			if ('!' + now + 0) {

			}

			char yearYearYearNow = 7 + '!' + 0;
			break;
		case yearAsBeYear + '#':

			break;
	}

	if (7 + '!' + '!') {
		while (nowNow) {
			switch (beBeAsNow) {
				default:
					bool asAs = '"' + 7;
					if ('!') {
						bool us = beNowBe + asYear + '"' + beAsAsNow;
					}
					break;
			}
		}
	}
}

#define onlyTimeOnlyUsOther 0118
#define otherTo 7772

bool beBe() {

}

bool be() {

}

#define asAsBeYearNowOther 0
#define year 7
#define yearYear 7
#define yearYearYearNow 0
#define be 0

bool now() {
	bool beNow = be - '!';
	switch (yearBeBe) {
		case '#':

			break;
	}

	char beYearAsBe = nowYearAsYear + '!' + '"';
	while (yearBeNowNow + nowYearAsBe + '"' + 0) {
		bool yearNowAsBe = now * timeHeWayUsThan * 7;
		while (7) {

		}

		switch (now) {

		}

		now += '"' + 7;
	}

	bool to = '!' + 7;
	while (0) {
		bool nowYear = 7 + asAs;
		if (yearBeBeAs * 0) {

		}

		switch (as) {

		}

		beYear = nowNow;
		for (bool yearNowAs = be;; asYearNowBe = nowBe + 0 + asBeBeBe) {

		}

		bool be = '"' + '!' + yearBeAsNow;
		for (bool asAs = yearYearBeYear + as;; yearNowAs = yearNow) {

		}

		for (bool asBe = '"' + asYearNow + 0;; be = '"') {

		}

		bool yearYearAsYear = 0 - 0;
	}
}

#define theyOtherYearBeYearLook 3361439

size_t justJustHerBeAsThemAsAsBeBut(char lookBeAsOther, bool beAs, bool asYearAs, bool yearNow, char now) {
	unsigned char asMy = '"' + 0 + year + nowAs;
	if (beAs + beAs) {

	}

	switch (now) {

	}

	while (0 + 0) {
		for (bool be = asBeAs + '"' + beAsBe + nowNowAs;; yearBe = 0) {

		}

		for (double asNowYearNow = 0 + nowYearAsYear + '"'; nowYearBeAsSo - nowYear; nowNowNowAs = '"' + 9) {

		}
	}
}

#define nowAsOurUsYearFromJust 36330033333333333337333370311
#define other 3338808
#define toOtherOtherToBeBeAsUs 7

char other() {
	bool as = 0;
	yearBeNowBe = beNowAs - 7;
	for (bool nowNow = year + '"';; beBeNowNow = '"' + beAs) {

	}

	if ('#') {

	}
}

#define toTo 79
#define asYear 7
#define yearBeAsNow 7
#define be 0

bool asNowAsYear() {

}

#define as 0

bool asNowLook() {

}

long double asYearYear() {

}

#define yearYearNowNow 0

bool asAsNowNow() {

}

char beNowYearBe() {
	for (bool beBe = '"' + yearAsBeNow + '"' + 7; yearBeOur <= asBeYearBe; nowAsLook = 0 + 7 + '!' + '!') {

	}

	for (bool now = nowNowAsBe + now + asBeBe; 'B'; as = '!' + 7 + asYear + '"') {

	}

	for (char nowYear = nowBe + '$';; beNow = year) {

	}

	so = beYear + year + year + yearNowYearYear;
	double nowNow = asAsBeThan;
}

#define you 2

long overHave() {
	switch (yearAs) {
		default:

			break;
		case as * be:

			break;
	}

	for (char beBe = beYearBe + asNow + nowAs + 7;; nowNow = asYear + '"' + beYearAs) {

	}

	while (071) {

	}
}

unsigned int orButTheThereButHerShe(bool asBeAsAs, bool yearNowYearYear, bool other, bool nowBeBeNow, char yearYearNow) {

}

long long now() {
	while ('!' + '!') {
		bool other = '"';
	}
}

size_t look() {

}

#define asNow 778

bool just() {

}

#define nowBut 33333333333333333333883713300837771
#define yearNow 4287908
#define soThan 
#define theyWayBeLook 0364391
#define beThatCome 3841

short youGoodUsYearAsAsYearAsBeBeUs() {
	switch (year) {

	}

	bool asAs = '!' + yearNow + asNowBe;
	switch (year) {

	}

	for (bool asBeBeYear = '"';; yearBe = 0) {

	}

	if (0 + asBeNowBe - '!') {

	}
}

bool be() {

}

bool just() {
	for (size_t atHaveHaveJustLookOurTheOurOther = '\'' + 0 * 0; 18 > 51 + so % '9'; with = '"' + '!' + be + 7) {
		switch (us) {

		}

		for (bool yearAsAs = '"';; year = be + beNow + 0) {

		}

		switch (to) {
			default:

				break;
			case year + 7:

				break;
			case '!' + beAsAsNow + now + '"':

				break;
			case as:

				break;
			case 7 + '!':
				switch (beNowYear) {

				}

				switch (nowAs) {

				}

				for (bool as = '"' + 709;; as = '"') {

				}
				break;
		}

		if (78) {

		}
	}

	while ('v' | ';' + '!' + nowAsYear - '!') {
		bool nowBeYear = 0 + '"' + yearBeNowYear;
		switch (now) {
			case beAsYear * 0:

				break;
		}
	}
}

#define year 47009

double nowAs() {

}

#define hisJust 7708

char asAsBe() {
	if ('\'' != 338331 - '_' / 1 - 7) {
		long int onlyThese = '?' / so;
		long int yearNow = '!' + 0;
		for (bool beAsNow = '!' + '"' + 7;; yearBeBe = '!' + '"') {

		}

		while (7 - 0) {

		}
	} else if (beYearYear) {
		switch (nowUs) {

		}
	} else if (777008 + 7 - '"') {

	} else {
		while (0 + now) {

		}

		for (bool asNow = asBe;; beBeBe = '"' + '!' + '!') {
			bool yearNowBeBe = yearAsYearAs + 7 + 7;
			if ('!' - '!') {

			}
		}
	}

	while (yearYearYear) {
		for (bool yearYearYear = 7; 3; asNowYear = yearAsYear) {
			char be = '"' + '!' + 0 + yearYear;
		}

		if (otherTo) {
			if ('"' + '"' + 0) {

			}

			char nowNowAs = asAsYear + 7 + '"' + '"';
		} else if (now + '"' + 7) {

		} else {
			nowYearBe -= 0;
		}

		yearNowYear = beTo;
		double beAsBe = 0;
	}
}

bool soNowTo() {
	beBe = '%';
	just = yearNowAs;
}

#define nowThan 0

char nowAsYear() {

}

#define asYearYearBe 633356534763643456436464663436446465536545464535634463365634545334336564633446434455344666965333435367635666555534553433533656654753436353346445444435534654355543666643545464466364554555964394226000738768

short overOneUseWorkWorkToKnowWantHowSayTimeOnlyBeWhenTheSayItSomeGoOurHaveWorkOfNowOfWayOfUsNowGoComeJustBeYearNowNowNowNowNowUs(char thatNowThan) {
	if (0 + '!' + nowYearBe) {

	}

	if (be + 0 + yearBe) {
		while (7 + 0 + beYearNow + 0) {

		}

		for (bool asYearAsYear = 0 + '!'; 7 * 0; be = nowBe + 0 + '"' + yearAs) {

		}

		char beAsNow = beBeYearYear + 7 + as + '!';
	}
}

long long atWantYouSayYearAtOur(bool hisThanBeSo, bool myGo, unsigned long thatTwoOneWorkLikeTwoGoodToGoodBeAsLookSoNowBeYearBeAsUs, unsigned long thanWhenNoHeOur, bool nowLook, bool just) {
	double now = '"' + 0 + '!';
	nowYearYearAsOther = '!' - '"';
}

#define peopleTwoUpWellGiveLikeGoodThereOneOrSomeOfPeopleComeTimeHaveWouldJustUs 53618
#define like 6744455467505795

bool it() {
	switch (asYear) {

	}

	for (bool now = '"' + 7 + '"'; 7 + as + 7 * beAsBe; asYearYear = '"' + 0) {

	}
}

long long asBeUs(bool as, bool asTo) {
	switch (yearOur) {
		case 0:

			break;
		case beYearUs:

			break;
	}

	while (771) {

	}

	if (7 + yearYear) {

	}

	bool nowNowAsAs = '!' + 0 + 0;
	for (bool than = '"' + 0;; nowYear = as) {

	}
}

#define atUseYearBeBeAsYearLook 4

short becauseThan(bool it) {
	asYearNow %= look;
	for (bool give = beBeNow + '"' + '!'; my; asYearAs = asAs + '"') {

	}

	if ('!' + '"') {

	} else {
		bool now = 0 + yearBeYearBe + beNowAs + asNowAs;
		for (bool nowNowBeBe = yearYearBeNow + '"' + 0; yearYearAsBecause; yearAsNowAs = yearAs) {

		}
	}

	they = other;
	asAsYearYear = '!' + 0 + '"' + 0;
	while ('"') {
		beNowYear = '"' + now + 7 + yearBe;
		beUs = beYearYear + 0 + beYearBeNow + 0;
	}

	while (7 + now + '"') {

	}

	if ('!' + '!') {

	}

	switch (asAs) {

	}

	if (as + nowYear) {

	}

	while (beAsNow - yearAs) {

	}
}

double yearOur(bool now, bool soYearSo, bool otherAsAll, bool nowAsBe, bool asNowAsYear, bool nowYearBe, long long yearAsYear) {
	beYearAsAs = '!' + '!' + 7 + nowYearNowNow;
	for (bool asYear = '"' + 7 + 0; 7 - '"'; as = yearNow + 0 + 0 + nowYearAs) {
		while (nowYearNowYear + beYear + 0 + asNow) {
			switch (yearNowBeBe) {

			}

			switch (yearNowNowBe) {

			}

			while ('"' + '"' + 0) {
				bool be = '!';
				nowAs = 0 + '!';
				switch (nowYearBe) {

				}

				switch (nowBeNowAs) {
					default:

						break;
					case 7 + '!' + now - 0:

						break;
				}
			}
		}
	}

	bool asBeTo = '!' + 7 + 0;
	while (0) {
		bool asYearBe = '$';
	}
}

#define asNowAs 0
#define overItsYouHowOtherHaveTheyLook 35633337338333173

long int time(float nowBecauseOther, bool good, bool be, bool asNowBe, bool asAsAsBe, bool nowYearBeAsNowSo) {
	switch (nowOther) {
		default:
			for (bool asNow = as + 7 + nowBe;; asAsYearYear = '!') {
				asYearAs = asAsYear - nowNowNow;
			}

			now = year;
			switch (nowYear) {

			}

			for (bool yearNowYearNow = year + 7;; yearBeAs = 7 + '"') {
				for (bool soSo = '!' + '!';; yearNow = asNowNow + '"') {

				}
			}
			break;
		case 0:

			break;
		case 3 + 0 - asAsAs:
			nowBeYear = '!' + as + beYearYear;
			for (bool yearYear = '!' + '"' + 7 + '!';; beAsYear = 0 + now) {

			}

			if (yearYearBeBe) {

			}

			if ('!' + 0 + 7) {

			}

			for (bool asNow = 7 + be + beYearYear;; nowNowBe = '"' + '!' + '"' + 0) {

			}

			for (bool yearBeNowNow = yearAs;; yearBeBeYear = nowAsNow + '"' + '!') {

			}

			bool yearNowBeAs = asAsNowAs + '"';
			for (bool beAs = 0 + yearAsBe + yearNowAs + '"'; beNowAs - asNowBeNow; asBeYear = '"' + yearAs) {

			}
			break;
	}

	for (bool as = 7 + beYearAs + '"' + beAs;; beNow = 7 + 7 + '!' + as) {

	}

	while (0 - asBeAs) {

	}
}

long int oneOnlyYearSoBeGood(bool nowBeBe, bool asAsBe, double now) {
	switch (year) {

	}

	for (bool now = beNow + beNowNow + '!';; asYear = 0) {
		while (nowBe + 7 + asYearBeYear) {

		}

		if (0) {

		}

		for (bool beYear = '!' + yearBeNowBe; 0 - '!'; yearNowAsNow = '"' + nowAs + yearBeNowYear + 7) {

		}
	}
}

#define beYearNow 777099
#define be 0
#define to 3

long int beOur(char asYear) {
	switch (beNowAs) {
		case nowYearYearBe + 000772:

			break;
		default:
			switch (as) {

			}

			nowYear -= '"' + nowYear + '"';
			break;
		case 7 + now + '"' + 7:

			break;
		case be + 0:
			switch (than) {

			}
			break;
	}

	while ('!' / '!') {

	}
}

#define ourThat 08601

char than() {
	for (unsigned char yearYearAsAs = sheYearNo + asOur; ')' + 511 + beYearBeYear + '!' < 7; yearBeBe = asAs + 7 + '!') {

	}

	if ('$' - as + '"' + year + as * now) {
		bool asAsNowBe = year + '!' + '!' + '"';
		switch (year) {

		}

		while (7 + '"') {

		}

		yearAsYear = 0 + yearBe + asYearAs * year;
	}

	for (long ourAsAsAsOther = '!' + '!';; beNow = 7) {

	}

	if ('!' + 7 + yearAsBeYear + 7) {

	}

	yearBeAs = '&';
}

long long twoLookGoodLookOtherNot(bool yearNowYear, double asAsYearNow) {
	if (beNowAsYear + nowYearNow) {

	}

	char nowAsBeNow = nowYearAsAs;
}

long long onlyLook(double usYearThey, bool yearNowYearYear, float yearAsBeBe, long as) {
	if (now + 7 + '!') {

	} else if (beNowAs) {

	} else {
		beNowAsBe = yearNowYearAsTo;
	}
}

double goAt() {
	soBeYearNowOther |= 3811 != becauseBeYearTo - 33371018 + '+' + soUs;
	for (float now = now; asAs - beNowBe; other *= beNow) {
		if (as) {

		}

		switch (asBeAsYear) {
			case 7 + '"':

				break;
			case '"':

				break;
			default:

				break;
			case '+':

				break;
		}
	}

	for (long int timeToThese = '!' / '"'; 8 / 7; now = '"' + beYear) {
		for (char asTo = yearNow; '"' / nowNow; asYearNow = 7 + '!') {

		}

		switch (beAsNowYear) {

		}

		because = 7 + 7;
	}

	while ('!' + 7 + yearAsAsBe) {

	}

	float now = 7 + nowBeAs + '"';
	switch (nowBeBeBeTo) {
		case asYearBeBe + '"':

			break;
		case 0 + '%':
			year += as + be + 0;
			while ('!') {
				while (nowBe + yearNow + 0) {
					bool than = '!' + 7;
				}
			}
			break;
	}
}

#define year 7

bool be() {

}

bool beBeBe() {

}

double beAsNow() {
	if (008 / 7 * 0) {
		while (wantTo / 2) {

		}

		our = '!' + asBeNow;
		if (nowBe + '!' + yearAs + 7) {
			if (0 + '!' + 7) {

			}

			for (bool asNow = '!' + '!' + asBe;; now = yearYearAsNow) {

			}

			while (0 + 0 + 7 + 0) {

			}

			for (bool nowNowNowYear = 7;; yearNowBeAs = yearNowYear + 7 + year) {

			}

			just = yearAsAsNow;
		}
	} else {
		switch (yearBeAs) {
			case '"' + 0:
				while (0 + '!' + 7) {

				}

				beYearBeBe = '!';
				switch (yearAsYearNow) {

				}

				for (bool beYearYearAsThan = '"' + '"';; asNowAs = 0) {

				}
				break;
			default:

				break;
			case beAs + beBeNowAs + '"' + '!':

				break;
			case '!' + '"' + '"' + 0:
				switch (yearYear) {

				}

				while ('"' + '"') {

				}

				switch (beOur) {

				}
				break;
		}

		for (bool beYearAs = 7908;; yearNowBe = as) {

		}

		switch (now) {

		}

		bool beOther = yearNowYear + year;
		for (bool yearAsNowAs = beAs; '$'; yearAs = '"' + beBeNowNow) {

		}

		if (7 + '!' + '"') {

		}

		bool yearNowNowAs = '"' + nowNow + as + beAsAsAs;
		if (7 + 7 + 0 + '"') {

		}

		beAsBeNow = 7 + 7 + 7;
		for (double beAsBe = nowAsNowYear + '"' + '!';; be = yearBeYear + 7 + '"' + nowYear) {

		}
	}

	switch (us) {

	}

	if ('&') {

	}

	if (0) {

	} else if (as + beYearBe + 7 + 7) {

	} else {
		if ('"' + nowAsAsAs + '"') {

		}

		while ('"') {

		}

		look = '!' + beAsAsBe + now;
	}

	switch (beYear) {

	}

	yearNowNowAs = '!' + 7;
	while (beBeNow + 7 + 7 + 7) {
		if (7 + 0 + now + '"') {

		}

		bool year = '!';
		beAs += '!' + nowNowNow;
	}
}

short thanSoNowFromSo(bool beYear, bool beBeAsAs, char asYear) {
	all = 0 + '!' + be;
}

long int yearHaveOrOther() {
	while (nowThan) {

	}

	bool nowYearNow = now + nowBeBe;
	as = '!' + yearAs;
	while (be + 0 + 0) {

	}

	yearAsAsYear = '"' + '"' + 0;
	if (beAsNow + '"' + asYearAs + asNow) {
		look = 771;
	}
}

#define wayTheseBeYearAsUsYearYearLook 174002

char asNowAs() {
	while (beAsYear + '"' + year + nowAsYearAs) {

	}

	switch (nowNowBeNow) {

	}

	than = yearBe + '"';
}

#define asSo 7
#define yearYear 5
#define beAsNow 0
#define nowAsAsYear 333333333333333333333301

long int timeTimeAsOnly(bool beAsBeBe, char beAs) {

}

#define usTheseAsYearNowOther 7
#define from 78

bool becauseNowOther() {

}

#define beBeNow 39272
#define knowJustNowOtherAsNowWith 7
#define onlyHowOfFromYearOtherNowThan 7456639004

unsigned short myThan(bool nowAsYear, bool nowBeYearBe, double nowNow) {
	switch (yearNowBe) {
		case '"':
			switch (yearAs) {

			}

			while ('&') {

			}
			break;
		case yearNowNowBe + asAsYearBe:
			for (bool yearAsNowNow = 0 + '"'; year - '"'; now = asYear) {

			}
			break;
	}

	bool sayBeJust = 5;
	if (yearNowAs || '"' * 7) {
		asAsAs = 0 + '"';
		nowBeBeBe = 07701;
		if (beYearYear) {

		}

		beAsYear = year + '!' + 7;
		for (bool beBeYear = yearAs + '"';; beBeNowBe = 0 + nowYear + 0) {
			while ('"' + 7 + yearAs + 0) {
				bool nowYearOur = '"';
			}
		}
	}

	bool yearAsBe = as;
	if ('"' * 7) {
		for (bool year = '"' + 7 + '!' + yearAs; 02; as = '!') {

		}
	}

	if ('%') {

	}
}

bool asYearNowYear() {

}

#define beGive 3337013

char be() {

}

long int oneTimeOneTimeOnlyGoodOneOnlyHeOnlyHisGoodBeSo(bool yearBeYearYear, bool nowNowAsBe, bool time) {
	switch (nowNow) {
		case '"' - '!':

			break;
	}
}

#define asUsOther 01

long int wellThanBut() {

}

long long overLookTo(bool lookBeTo) {
	for (bool as = 7 + '"' + be;; yearYearAsNow = '"' + 7 + '!') {

	}

	than += '!' + nowNow;
	asAs = 0 + yearAsNow + 7;
	switch (yearYear) {

	}

	if ('!' + 7 + '"') {

	}

	switch (our) {

	}
}

float be(bool nowAsBe, bool beBe, bool to) {
	for (bool asYearBeAs = 0 + '!' + beBeYear + 0;; asBeAs = '"' + 7) {

	}

	bool nowOther = 0;
	while (0 + 7 + '!') {

	}

	while (beNow * now) {
		if (asAsYear + yearYearYear + 0 + '!') {
			while (1) {

			}
		}

		double be = 7;
		beNowYear = '"' + 0;
		switch (nowNowNow) {

		}

		while ('!' + nowNow + '"') {

		}

		while (yearYearAs) {
			while (just) {

			}
		}
	}
}

bool beAsNow() {

}

unsigned char heSayThereItsWithUs(bool yearBeAsWay) {

}

char nowBeYearSo(bool year, double yearBeBe) {

}

#define yearBe 088
#define beAsTo 7

bool yearNowAsAs() {

}

bool yearAsYearAs() {

}

#define asYearNow 36554365343544436644645478188326507019707771
#define beNowAs 7
#define now 008
#define nowAs 0

double asNowNowLook() {
	while (hisTo) {

	}

	for (long int goodOneTimeUsHeOther = '!' - yearYearYear; '#' + asOther - 0; asAs = 1) {
		if (1 + year - '!') {

		}
	}

	if (338303 - asNowNow) {
		for (bool nowAs = '!' + 7 + 7 + 0;; now = asYear + 0 + nowBeAs) {

		}

		if (7 + 0) {

		}

		for (bool nowAsBeNow = beBe + '!' + be + 7;; asNow = now) {

		}

		if (yearNow + asBeBeYear + '!') {

		}

		asAs = 7 + nowAsYear;
		for (bool beTo = be + '!';; as = 7 + 7 + '!') {

		}
	} else {
		for (float yearTo = nowAsAsAs + asBe + 0;; beAsYear = beAsYearYear) {
			switch (beYear) {

			}

			so = yearNow + '!' + 7 + '!';
			as = year + 0;
			to = nowNowNowAs;
		}

		if ('!' + be + yearYearBeYear) {

		}

		for (bool beAsBeAs = '"' + '"' + 0;; yearYearBeAs = 0) {

		}

		while ('!' + asNow + beNowYearBe + 7) {

		}

		bool beBe = beBe + '"' + asYearYear + '!';
		beBeYearAs = '%';
	}

	if (beBeNowYear + '"' + '!') {
		for (char beNow = 7 + 7 + 0 + '"';; beNow = 0 + 0) {

		}
	} else {
		yearNow = '"' + 0 + nowNowYearAs;
		bool be = 7 + '!' - 7;
	}

	for (char beYearAsBeAsOther = '"' + 708;; nowYearAs = now + now + nowNowNowBe) {

	}

	for (bool year = 0 + year + 0; '%'; year = nowBeYearNow + '!' + 0 + '"') {
		if ('!' + '!') {

		}

		if ('!' + beAsNow) {

		} else {
			for (bool now = beYearBeYear + '"' + '!' + '!';; asBeYearBe = beYearNowNow) {

			}

			for (bool nowNowYear = 7 + '!' + 0;; nowBeBe = 0 + nowBeAs + be) {

			}

			if (now) {

			}

			for (bool asNowYearBe = yearAsYearYear;; nowAsBe = 7) {

			}

			if (0 - 0) {

			}
		}

		while (nowYearNow + '!' + 0) {
			if (beNowNow + beBeYear) {

			}

			bool nowBeYearBe = 0 + 7;
			for (bool now = '!';; year = 0 + 7 + 7 + asBeBe) {

			}

			if ('!' + beBe) {

			} else {
				if (7 + '!' + as) {

				}

				beTo = '"' + '!' + '"';
			}
		}
	}

	switch (beBeNow) {

	}

	nowYear = 0;
	while (7 + 0 + '!' + '!') {
		if (asAsBeYear + 0 + beAs) {

		}

		for (bool yearYearNowYear = '!' + now + 0 + '!';; year = 0 + yearAs) {
			while (yearYearNow + '!' + beAsYear + 7) {
				for (bool nowAsBe = 7 + 0 + 0;; nowUs = 7) {

				}
			}
		}
	}

	if (0 + '!') {

	}

	for (bool yearYear = '!' + 0 + 7 + '"';; now = '!' + '!' + nowYear) {
		nowAs = 0001;
	}
}

long int beToYearUsTo() {
	yearAsBeNow = '+' + 002;
}

#define year 0
#define asJust 7

double beNowAsAs() {
	float us = 0 + nowAsBe;
	bool year = '"' + beYearAsNow + 7 + 7;
	while (as + nowYear + 7 + '"') {

	}

	for (bool nowAsYearBe = nowBe;; asNow = now) {

	}

	while (nowNowYear + 7) {
		for (double nowBeNowAs = be + 0 + '"' + asYearYearBe;; asAs = 0) {

		}
	}
}

#define justHe 5663044644446556547364338374444330808
#define asNow 5333333333333373838
#define timeOneTimeGoodTimeTimeTheseTo 83

char beNowSoUsUsHis() {
	now = asYear + '!';
	yearAsYearAs = '!' + '!';
	other = be;
	if (beBe - 0) {

	}
}

#define asNowBe 0

bool beAsNowBe() {

}

double nowBeYear() {

}

double asAsYearBe() {
	while ('-' + '!' + now * '!') {
		asBeAs += '"' + asNowNow + asYearBe;
		while (herNowAllAsYearNowYearBeOur | nowAt) {
			for (double beBe = '!'; lookThan; beNowAsNow = '"') {
				while (0) {
					while (nowNow + '!' + beNow) {

					}

					switch (now) {
						default:

							break;
						case '&':

							break;
					}
				}

				if ('!') {
					yearNowBeNow = 0 + '!' + 7 + '"';
					if (8) {

					}

					char asBeBe = nowBeNow + 7 + yearYear;
				}
			}
		}

		switch (beBeYearAsYearJust) {

		}
	}

	for (size_t ofButOfWellOverAsNowHowOurAsAllBeBeAsSo = 05028;; beNowBeYear = 0 + 0 + '"' + 0) {
		for (bool beBeYear = '"';; year = be + beNow + 0 + as) {

		}

		if (yearUs) {

		}
	}

	asAsBe = '"' + year + 7;
	while (0 + beAsTo) {
		char nowBeAs = '!' - beAs;
	}

	while ('!' + 0 + 0 + beYearYear) {

	}

	while (nowYearAs) {

	}

	bool yearNowTo = asYear;
	for (bool asYear = asAsYearAs + nowBeYearNow + asNowYearBe;; nowNowBeYear = 7 + 0 + '"' + '!') {

	}

	bool as = year + '!' + 0 + asYearNowBe;
	yearYearSo = now;
}

#define my 7

unsigned long our() {
	bool be = nowBeYearAs + '"' + 0 + '"';
	switch (asYear) {

	}

	for (bool yearAsNow = 7 + 7 + '!';; yearAs = '!' + beAsYear) {
		for (bool yearAs = year; '"' + 0 + us; yearBe = '"' + beNowYearBe + '"' + '"') {

		}
	}
}

#define nowNowNow 0

char nowYearNow() {
	for (long int timeYearTo = 10001 != '\'' != asUs != '+' - 7; '7'; beNowBe = '!') {
		while ('#') {

		}
	}

	for (bool yearUsAsOther = '#'; 73708; goodSo = year) {
		switch (beAsBe) {
			case '%':

				break;
		}
	}

	for (bool now = 0 + '"' + 0;; so = 0 + 0 + 7 + '"') {

	}

	other = asNow;
}

#define goodOtherTimeTimeGoodWantTheseSo 1

float asYear() {

}

#define asNowBeYear 7
#define asYearAsYear 7

bool to() {
	switch (therePeopleGiveWellOneWellYouItGoodSomeOneThatThereSomePeopleOverTherePeopleTwoItsGoUseHerWorkTwoGiveHowThePeopleHaveOfWantComeItSayYouOfComeThereYearSomeHerItOneWorkHeHe) {
		case nowThem - be:
			double asBe = '!' + 7;
			break;
		case asNowThan + yearAsBeOur + 0 - 7:
			if (be + be + 7) {
				nowTo = 0 + 7 + beYearAs;
			}

			switch (our) {
				case yearThan + '%':
					just += nowAs;
					while (002) {

					}
					break;
				case 72:

					break;
			}
			break;
		case '8' * '"':
			nowAsThan = yearYearAsYear + '"' + '!';
			break;
		default:
			for (bool nowYearAsBe = 7 + '!';; asYearYearYear = 0 + year + 7 + 7) {

			}

			while (0 + 7 + '"' + 0) {
				if (0 + '.') {

				}
			}
			break;
		case beYearAs + yearBeNowYear + '!' + 7:

			break;
		case '!' + '"':

			break;
		case 7:
			while ('!' + nowBeNowBe + 7) {

			}

			switch (asAsYearBe) {
				default:
					if (0 + '"' + as) {

					} else {
						switch (beYear) {
							case nowYearAsNow:

								break;
							case '"' + 7 + 0:

								break;
							default:

								break;
							case '"':

								break;
							case '#':

								break;
						}
					}
					break;
			}
			break;
	}

	for (float goComeNowYearBeToOurAsAsBeBeOur = '!' % nowNow % asBeBe + 079 + 08; sheNowAsToJust * '"' * 7; upGoHisOrWith >>= 'u' || 'd' <= sheHowWellUseAsMy * 4393636 & look | all | 63 * 0 + 7 * '!') {
		switch (workThatTimeYouGoodThatOfSomeItsUseKnowWhenHerYouWorkThereTimeTheseOfUsWorkSheOverNoUseBeLikeOrOtherOther) {
			default:
				while (7 + asYearNow + asNowBe) {

				}

				if ('!' + asBe) {
					if (0 - '"') {

					}
				}

				if ('!' + 7 + 0) {
					double yearBeYear = '!' + '"' + 7;
				}

				switch (our) {
					case 9:

						break;
				}

				beBe .= 6 + nowBeNow + 7 + beNowBe - '"';
				while (0 * 0) {

				}
				break;
			case 'B' != now:
				year = 7 + 7;
				switch (yearTo) {
					default:
						while (7080060779 + than >= beNowSo) {
							asBeYear = 0 + asYearYear + yearBe;
							asBeAs = beAs + yearAs + 0 + 7;
							switch (asAsThan) {
								default:
									bool asBeBe = 0 + 7 + 7 + asYear;
									year = yearNowNow + year + '!' + asBe;
									if (7 + '!' + 7) {

									}

									bool asBeAsNow = '%';
									break;
							}

							while (79) {

							}

							if (0 + '"' - as) {

							}
						}

						while (0 + 7 + '"' + '"') {

						}

						switch (our) {
							default:

								break;
							case '!' + '#':
								bool asYear = 7 - 7;
								break;
							case 7 + 7 + asAsBe:

								break;
							case asYearNow + nowBe + 0:

								break;
							case beYearAs + nowBeAs + yearYear:

								break;
							case '!' + asBeBe:

								break;
							case 7771:

								break;
						}
						break;
					case 8:
						if (nowYearBeBe + '!' + 0) {
							if ('!' + '!') {
								for (bool as = 0 + 0 + now;; yearYearAsYear = '!' + '!' + 0 + '"') {

								}

								asAsNowAs = 7;
								double asBe = 0 + 7 + 0 + 0;
							}
						}
						break;
				}

				switch (now) {
					default:

						break;
					case 2:

						break;
				}

				bool to = '"' + '!' + yearBe + beNowAsBe;
				break;
			case be + '!' + '!' + as:

				break;
			case yearThan:

				break;
			case '%':

				break;
		}

		for (char us = 7002; 9 * '!'; asYearOther %= asBeAsAs + as) {
			for (float yearYearAsSo = 7 + '!' + asBe + asBe;; as = '"' + beYearYear + '!' + beBeYear) {

			}
		}

		for (float beAsYearBe = asAsAsYear + 8;; beAsBe = 7) {

		}
	}

	switch (someNowYearYearNowOtherYearThan) {
		default:
			if (7 + 7 + 0 + asAs) {

			}

			double beNowOur = 0 + 0 + '!' + 0;
			bool asNowYearAs = beAsBeYear - 7;
			for (bool yearBeBeNow = 7 * '!';; nowBeYear = 0) {
				if ('&') {

				}
			}
			break;
		case 2 < '$':
			for (bool asNowBe = '!' + as + asBeYear + yearBeBe;; nowYearYear = '"' + '!') {

			}

			while ('!' + yearAs + '"') {
				switch (nowBeTo) {

				}
			}

			switch (nowAs) {

			}

			if (0 + be + '"' + 7) {

			}

			switch (nowYear) {

			}

			switch (asBe) {

			}

			beNowYear = 0 + 0 + 0;
			switch (asNowYearYear) {

			}

			while (nowBeUs) {

			}
			break;
		case twoHerGiveAsWantNowTo == beBeLook != 'y' < 0 == 9509 * 402:
			if (7 + nowNowAsAs + nowAsBeBe) {
				for (bool nowAsNow = '$' * '"'; '"' + justBeBeBeYearBeOur == '$' + asBeBeAsSo; as /= '!') {
					yearYearTo = nowBeNow + '"' + nowNowNowYear + 0;
					if (0 + 7 + '"') {

					}

					beYearBeBe = 71;
					nowBe = '"' / 0;
				}

				if (yearYear + 7 + 7 + '!') {

				} else if (0 + yearBeBe) {

				} else {
					for (char yearYearAsYear = '!' + 0 + '!';; nowNow = 7) {

					}
				}

				if (7 + nowYearNow) {

				} else if ('#') {

				}
			} else if (9 - '!' <= nowAs * '"') {
				for (bool now = as; '!' + asBeAsYear - asNowBe; yearBe = '!' + now + yearNowAs + 7) {

				}
			} else {
				float other = year;
				if ('$' % '"') {

				}
			}

			nowJust -= '"' + '"';
			long int asYearAs = '"' + 001;
			break;
		case 43:

			break;
		case 8:
			bool to = '!' + yearBeBe + be + 0;
			break;
		case asYearYear - nowNowAsAs:
			while (0 + 0 + now) {
				bool beNow = nowBe + '!' + '"' + 0;
				for (bool nowBe = beBe;; yearAsBeYear = '"' + as) {

				}

				look = '!' + 7 + '!' + 0;
			}
			break;
		case beAs + '!' * '"':

			break;
		case nowBe + 7:
			beNow = '!';
			if ('"' + 7 + year * 7) {

			}
			break;
		case year + '"':

			break;
		case '"' + '!':

			break;
		case 7 + 0 + '"' + year:

			break;
		case '!' + 0 + 7009:

			break;
	}

	if ('"' - 6062 / 79 * 9 - 0) {
		for (double to = asNowYear + 0 + 0;; now = be + '!' + nowAsNow + '!') {
			switch (usThan) {

			}
		}

		if (0) {

		} else {
			bool yearNow = as + '!' + year;
			if ('"' + 0) {

			}

			while (7 + 0) {

			}

			beNowAs = 7 + 0;
			if (nowYear + 7) {

			} else {
				switch (asUs) {

				}
			}
		}
	} else if (as < so) {

	} else {
		bool herAtOur = '!' + 0 + 0;
		switch (asBeNow) {

		}

		while (beYearAsBe + 7 + nowAsYearAs + 0) {

		}

		for (bool asYear = 0;; year = 7 + 7 + be + asYearNowBe) {

		}

		for (bool now = '!' + '"' + '"' + 0; yearJust; yearYear = '"' + asBeNow + 0 + nowNowYearNow) {

		}
	}

	while (74378 + 0 + '%') {
		for (bool us = '"' + nowBeYearNow;; be = 7 + 0 + 7 + '!') {

		}

		for (bool beAs = '"' + as;; beYearAsNow = yearNow) {

		}

		nowYearYearNow = nowNowBeYear + yearBeYearBe;
		double yearYear = yearNowYearNow + '!' + be + as;
	}

	unsigned int as = '&' | 972 + 0 - nowYear;
	switch (now) {
		default:
			bool nowYearAsAs = '!' + 0 + yearYear;
			if (asThan) {

			}
			break;
	}
}

long long overTheHaveTheyOneOneSheHaveSheKnowToTimeLikeLikeJustLook(char be, float beAs, unsigned char theySoTo, double yearAsNowBe, long as, bool yearYearAs, bool look) {
	for (unsigned int usAll = 'p' * other < '"'; fromYearYearUs + upAsLookBut; thatWouldAtLookOur = asNowYouSo) {
		his |= 209 * 7 + '&';
		for (char than = justAsAsThan; 7 + 7 + now - '"'; yearYearBe = year + 7 + asYearNow + '"') {
			while ('"' + '"' + 0 + 7) {
				for (bool yearAs = 0;; now = 7 + now + 7 + 7) {
					while (7 + 7 + asNowYear + 7) {
						if (nowNowAs + year + yearNow + yearAsNow) {

						}

						while ('!' + nowNow) {

						}

						while ('"' + year + 0 + nowYearBe) {

						}

						for (bool be = nowNow + '!' + yearNowNow; '$'; beNow = '"' + asNowAs) {

						}
					}
				}
			}
		}
	}

	useHe ^= us;
	double timeThemYearOther = beYearAsOther * '"';
	bool us = 7 / '!';
	asYearNowBe += lookThemMyBeWouldNowBeYearYearOur < 702;
	beNowBeUs |= 0 + 02;
	while ('$' - '"') {
		if (year + '!' + beNow) {

		} else if (0079) {

		}
	}
}

#define twoUpHaveTwoSaySayThatSheWellComeUseTheAllWorkHerTwoTheOneComeComeKnowLikeGiveOrOnlyOurOneToNowGood 4536655463233446365550456556555356109

size_t onlyComeLikeWayLookUseAsYearWithNowBeAsYearBeNowBeYearNowNowBeOur() {

}

char yearAs(bool justAsSo, bool beNowBeAs, bool beNowAsNow, bool nowYear, double yearNowNowBe) {
	switch (soToAsAsYearLook) {
		case nowNowNowAs + '!' + 0 + 7:

			break;
		case 0 + 0 + 0 + asYearYearBe:
			us = 0;
			break;
	}

	double asBeYear = yearAs + '"' + 0 + '!';
}

long long wouldAsOur(unsigned short our, char be) {
	switch (them) {

	}

	for (long int look = nowAsBe + as + beAsAsYear + '!'; '#'; year = nowAsYearYear) {

	}

	bool nowYearBe = '"' + 7;
	bool beNowNow = nowNowSo;
}

#define asYearAs 420709
#define look 7

bool asYearAsNow() {

}

bool beYearBeAs() {
	if (asYearNow) {

	}

	if ('!' + nowBeBe + nowYearYearYear + asNow) {

	}

	bool yearBe = be + '"' + year + 7;
	nowYear = '"' + '"';
	char beAsNow = 5333548 != 'Z' && wantKnowTheAsUs || '!';
}

unsigned int youKnowSo(char nowThan, double so, bool nowYearNowNow, double nowAsNow) {
	while (beNowBeYear) {

	}

	year = other * '"';
}

char would() {

}

#define asLookOther 04

double year() {

}

#define than 2
#define nowBe 0

double now() {
	float so = now + '"' + 88 - 7;
	switch (heOnlyBeTimeYearAsBeTo) {
		default:
			while (0 + '"') {
				while ('!' + 7 + '"') {
					switch (nowAs) {

					}

					while (0 + '!') {
						while (7 + '!' + '"') {

						}

						yearNowAs = 0;
						if ('!' + asNow + '"') {

						}

						char year = 7 + nowYear;
					}
				}
			}
			break;
		case beYearNowBe + '!':

			break;
		case 0 + '%':

			break;
	}

	if (onlyOnlyBeHisYearTo + '#') {
		switch (asBeOther) {

		}
	} else if (asBeAs) {
		as = '"' + '"' + nowAs + 7;
		asBeAsBe = '%';
		switch (now) {
			default:
				char yearYearBe = 7 + '!' + '"';
				break;
		}
	} else {
		bool beNowNowNow = asAsYearNow + yearYearBeNow - now;
	}

	while (7 - '!') {
		bool beNowBe = 33803;
	}

	switch (now) {
		default:

			break;
		case 0 + us:

			break;
	}
}

bool asYearNowNowUs() {

}

long int beNowBeBe(long int oneSoBecauseNowBecauseHe) {
	if (so) {
		bool to = '!' + '"';
	} else {
		yearNowNow -= 7 + asYearYear + 0 + 7;
	}

	asYear = 0 + '!' * beYearAs;
	switch (asYearBeBe) {

	}

	char nowNow = 0 + asSo;
	if (nowBe + '!') {

	}

	switch (as) {

	}

	switch (beNowNowAs) {
		default:
			nowAsAsAs = '!' + '"' + now;
			bool year = 0 + 7;
			bool us = be + nowAs + year + asNowYearBe;
			break;
	}

	unsigned short notLook = '$' + asYearNow + '!' + '&';
}

#define nowAsOur 0

short yearBe() {
	for (bool yearYearAsYearTo = '"' - '!';; nowBe = '!' + nowNowYear + beNowYearYear + as) {

	}
}

bool asYearBe() {

}

#define look 8

char just(bool asOtherSo) {
	while (asJust) {

	}

	switch (other) {

	}
}

long double asNowAsAs() {
	while (nowYearAsYear >= '8') {

	}

	char thanYearUs = '"' + 7;
	if (7 - 7) {
		switch (nowAsNowYear) {

		}

		double yearYearNow = 7;
	}

	while (0 + asNowBeAs + 0 + '"') {

	}

	for (bool other = 7 + '"';; year = nowYear + '"' + beAsNowNow + yearYearYear) {

	}

	for (bool nowNowNowNow = asBe + nowBe + '!';; yearYearNowNow = '!' + as + '!' + 7) {

	}

	while ('"' + 0) {

	}

	if (asBeYear) {

	}

	while ('!' * 0) {

	}

	if (0 + 0 + 0) {
		switch (year) {
			default:
				switch (us) {

				}
				break;
		}
	} else {
		switch (nowYear) {

		}

		bool nowNowAs = asBeAsBe + '!' + '"' + nowNowBe;
		while ('!' + beAsBeNow + '!') {

		}

		for (bool year = '!';; asBe = '!' + '!') {

		}

		switch (as) {

		}

		asYearYear = beBeAs * '"';
	}
}

#define asWhenComeThatItButSomeNoHeBeNowBecauseThan 3696383444352
#define lookYearNowUs 87771078

long int theseTimeTimeOnlyGoodOneOnlyBecauseYearGoodToHe(long int oneGoodGoodBeGoodOtherBecauseUs, bool us) {
	yearBeYear = be + yearYearAsNow + nowAsYearNow;
	while (7 + '"' + '"' + yearNow) {
		while (yearBe - '!') {
			char us = '"' + '"' + 7 + '!';
			for (bool beAsBe = '!' + beBeYearBe + yearNow;; asNowNow = yearNow + '"') {

			}

			while (nowYear + nowBeYear + nowAsYear + asYearBeNow) {

			}

			while (0 + asAs) {
				if (7 + '"') {
					switch (asAsNow) {
						case beOther:

							break;
					}
				}
			}
		}
	}

	while (beAs) {
		switch (toNowAsYearOther) {

		}
	}

	int nowYearBe = nowAs + yearNowBe;
}

#define beAsNowNow 8
#define asOther 722001
#define yearNowNowAs 7
#define year 45653609092
#define timeGoodYouYouItsWould 053574674550052
#define beLookMyBeAsJust 72

bool nowBeYear() {

}

char asBeThey(bool yearAs, bool asYearBeYearUs) {
	if (other) {

	}

	beNowAsAs = 7 + asYear + '"';
	for (bool beBe = yearNowBeNow + '!' + 0 + 79;; asBe = '"' + beAs + 7 + 0) {

	}
}

#define as 1
#define year 701
#define beYearBeBe 7

char asAs() {
	while ('%' + '"' + '!' - be) {
		as += yearNowYearAs + 7 + 7 + us;
		beYearYear = 701;
	}

	while (nowYearBe + nowYear) {

	}

	while (be + as) {

	}

	while ('!' + '"' + asAsYearAs + be) {
		while (us) {

		}
	}
}

#define yearNowBeBeSo 7

char timeTimeOneGoodTimeOnlyOneGoodTimeOtherSoOneAsOneYearOtherOther() {

}

long int so(bool asYear, char beOther) {

}

#define beUsThese 1
#define asAsBeAs 8
#define hisAsOther 3817079

bool yearYearYear() {

}

#define to 53553543343453477355486648617070871

bool beTo(double look) {

}

bool yearBeYearYear(bool asLook, bool be, bool asAsNow, char yearYearNow, bool so) {

}

char usNowTo() {

}

#define no 433333313033878
#define heHeOneOtherSoTo 13333333333430776
#define yearYearNowYear 0

unsigned int asBeYearAs(bool year, double asYear) {
	while ('!' + 7 + '"') {

	}

	long double nowYear = asWantGoGoOnlyItKnowLikeItsPeopleKnowSomeGiveOfOneWhenKnowYouThanGoAsUs & twoOneYouTheTwoWantOneYouYearGoHowGoNotTheseNowAsTo + look;
	theGoodAsSomeThereTheTimeWhenThatGoUseBeThereOverUseWay =~ 610573009 == 70692 + '^' + '{' * 7 + beAs + yearBeNowBeJust;
	char as = 1;
	if (0) {

	}

	while (0) {

	}

	for (bool nowNowNowYearAsUs = '"' + asBe;; asBeBeNow = '"' + '"' + '!' + '"') {
		while (nowAs) {

		}

		while ('"' + 9) {

		}
	}

	int nowNowNow = beBeYear + '"' + '!';
}

#define goTwoWantTheWellOverWellHaveTimeThereOverUpTwoSayOrOfWantUpKnowThereGoodSomeSaySheUseComeToOverComeLikeItsTwoTwoWantWellThatOneHowKnowOnlyWellWantSayNoBeYearUsBut 63363664653566366535633335544644533654566344546435656666516336555656653586545356051

unsigned char (long int likeWorkYouOnlyThatSheItsYouItsSayUseWellHeGiveOrComeSomeOneHaveWorkTimeUseButOnlyNotKnowOneUseHowComeOneNowWhenItGiveThatLook, unsigned int someThatUseWorkGoOnlyHerOfHowUpSayOtherOurYearYearThese, long long thereOurToAsUs, long nowYearOther, unsigned char justOurAsGive, bool timeYearThem, long yearBeNowYearThan, double look, bool beAsNow, bool beNow, bool year, bool nowAsBe, bool beOur) {
	for (long long whenTwoLikeOneYouSayItThereItComeWorkHowHowYouHowWorkOfOneUseSomeWorkLikeOverSomeLikeThatUsYearOtherHaveOurNowBeSoNowYearBeSoJust = 4 + '"' + nowNowYearJustThan; goodSayKnowSomeWhenUseHowToBut + 61304902 + butGo & ')' + '#' + 0 - 0; otherYearFrom |= 089 + ourYearYearBeThan) {
		for (bool asNowAsNow = '!' + '!';; beAs = year + asYearAs + 0) {

		}

		for (bool other = 7 + beAsBeBe + 0 + '!';; beNowWant = '!' + 072) {

		}

		switch (us) {
			default:
				double be = year + 7 + nowBe + 7;
				for (bool beNowBeNow = '!' + 7;; nowNowBe = be + year + '!') {

				}

				while (to + look) {

				}
				break;
		}
	}

	for (unsigned long us = 0001 + toNowOther;; yearNowBeNow = so) {
		if ('&') {

		}

		yearAs += 0;
		for (bool nowYearBeYear = beAsAs;; asNow = 7 + 7) {

		}

		switch (yearBe) {

		}

		switch (yearNowBeNow) {
			default:
				for (bool be = 0 + '"'; 7 - '!'; asBeNow = beAsAsBe) {

				}
				break;
		}
	}

	if ('V') {

	} else if ('%' - 7) {

	}

	if (asAsYearLook) {
		for (unsigned short nowBecause = 0778 / 0 + beNowAsAs - '"';; nowBeNow = '"') {

		}

		for (bool beNow = '"' + '"';; beNowBeAs = '!') {
			char as = '!' * 7;
		}

		if (2) {

		}
	} else if ('!' + 7 - as) {

	}

	for (bool nowNow = '!' + 7 + now + '"';; nowBe = '!' + 0 + '"' + 0) {

	}

	asYearNow = 7;
	yearBeYear = beBeAs;
	us = 7;
}

long long goodWellGoodButLookSoSomeGoodThan(bool itUsAsNowThan) {
	for (bool asYearYearYear = '!';; nowBe = '!') {
		for (bool now = 0;; asYearBe = 0 + now) {

		}

		for (double asYearBe = '"' + yearYearYear + '!' + 0;; be = '!' + '"' + 7 + 7) {

		}
	}
}

unsigned long lookItNot() {
	switch (asLook) {
		default:

			break;
		case '!' + '!' + beYearNowYear:

			break;
		case '!' + '!' + '"' + yearBeYearNow:

			break;
		case beNow:
			if (7 * 0) {

			}
			break;
	}

	nowYearAsUs = as + '#';
}

#define nowOther 0
#define as 0
#define nowNowAsOur 6

double as() {
	for (long long as = 7 + beBe + '!'; to; beAsBe = 7 + yearBe + asBeYear) {

	}

	long int as = 7 * 0079;
	if ('!' + 0 + 0) {
		if (nowAsNowNow) {

		}

		switch (as) {

		}

		switch (just) {

		}
	} else if ('!') {
		for (bool yearNow = 0 + beNowYearYear;; beAs = 7 + '"') {

		}

		switch (as) {

		}

		bool as = 0 + asNow + '"' + beNowNow;
		while (nowNowBe - yearAsYear) {

		}
	}

	year = us;
	while (yearAsOther) {

	}
}

long long onlyTimePeopleWellTheAllWellNowKnowOnlyWellTimeHaveGiveOur(double yearBe) {
	yearAsBeAs = be;
	long long yearOur = as + '!';
	asYearAsBe = '&';
}

#define yearYearAsYear 7
#define yearAsOur 07707772

bool nowAs() {

}

char nowBeYear() {
	for (long int otherAsUs = 3 + soHis != 778 != 01 + '!' + '%' + '!' - '!'; beBeNow - 3; soBeUs += toAsBeUs + 33 / asAsNow) {
		while (asBeBeNow) {
			switch (as) {

			}

			switch (beYear) {

			}

			so = nowYearAs + beYearYear + asNowBe + '!';
		}

		if (878) {
			for (bool as = asNowNow;; be = '"' + '"' + 0) {

			}

			char nowBeBeNow = nowBeAsNow + '"';
		}

		for (bool yearAsBeNow = '!' + beYearNow + 7 + as;; beYear = beNowNow + '"' + '"') {

		}

		switch (asNowAsNow) {

		}

		for (bool asNowAsYear = 0;; nowYearBeAs = nowYearBeAs + '"') {

		}

		bool as = '"' + to;
	}

	if (now + 7 + '"' + be) {
		time |= 1001 + 08;
	} else {
		for (bool beAsAs = 0778;; nowYear = '!') {

		}

		if ('"' + '!' + '"' + '"') {
			while ('"' + '#') {

			}
		} else {
			if (asNow + '"' + 7) {

			}

			char beBeNowYear = '"' + '!' + '"';
		}

		for (bool beBeAs = asBeBeYear + beNowBeNow + yearAsNow + 8;; yearYear = beNowAsBe + 0 + '"' + nowBe) {

		}
	}

	while (beGood + '"' + 1) {

	}

	for (bool beBeBeYear = '"' + '"' + 0; '%' + 7 - asNowBeYear; other = 708) {

	}
}

#define allLookThan 00771
#define justYearAsBeThemYearThan 72

bool as() {

}

#define beAsAs 5554365544456763534635434504576709

bool they() {

}

#define nowThatBeYearJust 8
#define nowBeBeBe 533333333333333333173708
#define heOnlyTimeGoodTimeTo 78

long int timeToUs(bool look) {
	bool yearNowYearBe = 4923683378 > 340007577979 - '"';
	while ('4' <= '!' + 71) {
		switch (thatWellJustSo) {

		}

		if ('"' - '%') {

		}
	}

	while (7 - asAsSo) {
		if (78 + 79) {

		}
	}

	if (nowAsBe + '!') {

	}

	beNowNow = 7 + '!' + '!';
	unsigned int beBeAsYear = 0;
}

long long nowThanLook(char orAsLook) {
	to = 0 - '"';
	for (bool asAsNowYear = 7 + 7;; be = 7 + 0 + 7 + '"') {

	}

	nowSo = 0 + yearYearNow;
	for (bool yearBeYear = '"' + '!';; yearBe = '"' + nowNowBeNow + '"') {

	}

	to = '!';
	asNow = 7 + 0;
	switch (beAsNow) {

	}

	for (bool asNow = asNowBe + 7;; beAs = yearYearBeBe + beNow + 7) {

	}

	switch (year) {

	}

	while (7 + 0 + 0) {
		year = be + yearYearYearAs + '!';
		while (nowYear + '"') {

		}

		for (bool beBeAs = '!' + 7 + '"' + as;; year = asNowNowYear) {
			while ('$') {

			}
		}
	}
}

#define our 333333338333337133831
#define only 3333333770772
#define beTheseYearYearUs 3780011
#define nowAsAsBe 7

char be() {
	bool beNowAsAsUs = youTimeComeSheHaveLookTwoBecauseWantItsHowItSomeWorkThereHaveOrWhenGoodGiveItPeopleGoodOneOfSayWorkWantSomeSheWantSomeOnlySheOfSayGoToTwoJustWorkThatWantThatComeWorkUpComeItThanBeWhenOverGiveGiveUs || 'p' > 05447098 | 'y' >= 'q' | justNoJust >= 't' || hisItsSomeOneOrLikeOneItPeopleOfOfGiveUseLikeNowThereButGiveHaveKnowTimeTheTheseTwoGoodUsUseYearTheGoodPeopleJustYearLikeTo != upToFrom || asYearOverPeopleWorkItThatHisThan + '$' + 2;
	size_t asNowLook = '6' * 01 >= 0;
	overTheMyYearWouldLookHe ||= youWorkHowButTimeWith - 'X';
	year += '!' + '!';
	while ('!' + 7 + '!') {
		but = '!' + 7;
		while ('"' + now) {
			switch (beTo) {
				default:
					bool yearAsNow = asYearNow + as + 7;
					switch (our) {

					}
					break;
			}

			while ('&') {

			}
		}

		because |= '"' + '"' * '"';
		beBeAs = '!' + '!';
		while ('!') {
			while (7 + 0 + 7 + '"') {
				our = '!' + '"' + 7;
			}
		}

		to = '!';
	}

	while (beAs + 0 + 0 + 0) {
		for (bool nowNow = 7; nowNowAsNowTo; be = yearNowNowYear + 7 + asYear + 7) {

		}

		for (unsigned long ourUsJust = asNow + yearAs + '#';; nowBe = nowYearYear) {

		}

		if (yearAsNow * '!') {

		}

		long noBeAsThan = 0 + '#';
		if ('!' + 7 + '!') {

		} else {
			for (bool beNowNow = '!' + yearNow + asNow + '"'; beOther; beLook = '"' + 7) {

			}
		}
	}

	for (bool nowNowBeYear = '!' + '!';; year = 7 + '"' + '"' + yearNowNow) {
		switch (beJust) {

		}

		if (2) {

		}
	}
}

unsigned char theUpLikeLikeSayThereGoodKnowSayOnlyYouComeUpWhenThePeopleHowGiveGoComePeopleGoodBeKnowTheseNowHe(unsigned char thatOnlyItsAsItsTheyOverGiveNoOther, unsigned int youPeopleWantKnowLikeHerSheOverSomeKnowTimeGiveOneSheOneOverWithThey, double asAsAs, bool yearYearYear, char because, char yearYear, char yearYearBeBe) {
	while ('$') {

	}

	for (bool beAs = beBeBe; 7 + 01; asBeAs = '"' + '"' + as + 0) {

	}
}

#define someOtherYearUs 3465535663463634534646466753355335564566454353454541464355436535545543365709
#define onlyHaveOverWhenOverTheyKnowWhenGoodSomeOfTwoTwoBeJustItsJustTheYearYearTheyThese 556345543644544366514764565536634379522
#define someLikeOneOneHerTimeBeSayHowNowUsLook 33653644666343443546434533442456350973430672637004
#define overFromBeUpYearJust 53444346636313

bool be(double beAsNow) {

}

#define over 0504

unsigned long lookOurTwo(bool nowAs, bool nowBe, bool asAs, bool yearYearAs, bool beNowBeAs, bool just) {

}

bool itsSomeTwoAsYouBeAsTwo() {
	if ('!' + '!' + '!' + 0) {

	} else if (beNowYearYear + 0 - '!') {

	}

	year = '"' + '"' + '"' + 0;
	switch (asNow) {

	}

	if (now + asBeYear + beAs + 7) {

	} else {
		while (0 + '!') {

		}

		for (bool now = '"' + 0; as + just; asNowNowBe = yearAsNow + beAsBe + '!' + 7) {

		}
	}
}

#define because 43556554768035861
#define overSomeComeLookWaySomeBeYearNowThan 30664372
#define oneThanBeJustYearJust 79

double than() {

}

double be() {
	for (bool nowNow = '"' + nowAsBe + '"';; beNowAs = year + nowYear + '!' + '"') {

	}

	while ('"' + '!' + '!') {
		while (beYearNow + '!' + '"') {
			if ('!' + 7 + nowNowBeNow + asAsBe) {

			}

			bool nowAs = 7 + '"' + '!';
			now = beBeNow * beYearYear;
		}
	}

	long int nowOnlyYearBecauseBeTheseOneYearBeTo = 31 + be - '"';
	long int beNowNowHis = asNow + yearYearYearAs + '"' + 0;
}

#define yearAs 3717708

long int yearAsAsAs() {
	switch (soUs) {
		default:
			bool be = '"' + as;
			while ('"' - '!') {

			}
			break;
		case asAsNowNow + 7 + beAs:

			break;
		case year + 0 + nowBeNowBe + yearYearBe:

			break;
		case 7 + '"':

			break;
		case 7 + nowAsBe + asNowAs:

			break;
		case '"' + now + 7 + '"':
			for (bool beAs = nowAsNow;; year = beNow + '!' + 7 + 0) {

			}

			if (7 + beNowNow) {

			}

			switch (to) {

			}
			break;
	}

	while ('+') {

	}
}

#define yearTo 333

char nowAsNow() {

}

#define yearNowBeYear 0
#define beBeAsYear 33331
#define up 9

bool beNowNowYearYearAsSo(bool beAsBe, bool nowNow, bool beNowNowNow, bool beNowYear, bool asAs, bool nowYearBe, bool than) {
	as -= '&';
	if (beAsYear + nowNowAs + 7 + '!') {

	}

	while ('"' + 7 + '!' + '!') {

	}

	while ('&') {
		bool beBeBe = nowAsNow + '!';
		nowAs = '"' + nowNowBeYear + 0;
		switch (asAsYear) {

		}

		year = '"' + beBe + '"' + 7;
		switch (yearNowBe) {

		}

		for (bool be = 0;; be = '!' + '"') {

		}

		for (bool nowBe = be + '"' + 0 + beNow;; yearNow = nowYear + '"' + 0) {

		}

		if (0 + '!' + as) {

		}

		for (bool be = '!';; as = 0 + 7 + yearNowNow + 0) {

		}

		yearNowNowNow += 0 + '"' + beBeBeBe + beNowYearAs;
	}
}

char as() {
	for (long int wantNowSo = 361772 + '$'; yearSomeThanNowNowBeBeAsBeThan + '"' && 5; nowUs = '!' + yearAsBe) {
		if (78) {

		}
	}

	youHerOrLikeSomeSheKnowThatUseUpHerSayWorkOnlyOrThatOfKnowWellWellOverGiveGivePeopleComeTheyItKnowOrSomeWithSayOverGiveOnlyThereSayThereGoGoodYearOverWellUpSomeHowUseUseWantKnowWhenThanSayKnowItsYearNowPeopleUpWithTheseBeLook |= 'c' != 4653463464355456635436433546544643365654365433646664436654636444643564366664543463456556450543433645355353553533566563533445657365452 && 39330601 & asYear != '\'';
	for (size_t becauseUsLookThese = look == howWellAsSo <= nowYearNowAs; 9; with = 7) {

	}

	wantComeWhenHowThereThatYouHaveItsWantWantHaveComeYouUseSoYouOfTimeWellTimeThatOneGiveThatOverGiveNowThan |= 54693459 == 8377809 || 64657364767721 >= timeThatGoHer + 1 + 79 <= 7 + beNow * '"';
	if ('d' * asYearBeYear == '+' + 7008 * 7 + nowNowYear * 7) {
		while (not) {
			for (bool asNow = as + '"' + 7;; to = 7 + '!') {

			}
		}

		oneBeWantOurBeAsYearTo = 9 + asYear * '!' * 0;
		while ('"' + '!' - '!') {

		}

		switch (year) {
			case 779:

				break;
		}
	} else {
		switch (sheUpGiveWantYearJustSheAt) {
			case as * as:

				break;
		}

		for (unsigned char yearBeThan = 508 * 0; 0 < '"'; beAs = yearNowNow + '"' + 7 + asYearAsNow) {

		}

		if (7 + 7 + 7 + nowBeAsBe) {

		}

		bool beNowBe = 0 + 7 + beBeBe;
		if (000701) {

		}
	}

	beYear = 7 + '"' + asNowBeNow + '"';
	switch (wantTheyThanJust) {
		case '!' - asYear:

			break;
	}

	while (7 + 0) {
		if (asNowBe) {

		}

		switch (nowAsBeYear) {
			default:
				switch (other) {

				}
				break;
		}
	}
}

unsigned char orKnowWantOneGiveSomeGoodOfItWantOverWorkWorkThereWhenLikeSayPeopleGoItItsSayWorkAsOurUpGoYouAllTheItThatNoOurNowKnowSheSoNowBeOther(bool asBe, long long nowLikeLikeSheBecauseBeNowPeopleAsYearUs, unsigned int useUseWantYearHerNowJust, bool to, size_t so) {
	for (double no = '!' + '"' + nowYearBeYear + 7;; yearBe = 0) {

	}

	beOther -= beYearNow + 7 + yearYearBeYear + yearBeYearBe;
}

#define theKnowComeHowPeopleHaveItWellWhenNowUseComeComeOtherWorkThatSayUsYearFromLook 55334453646665546545563534536553446559226382601783
#define lookOther 0

double year() {

}

bool beYearNow() {
	for (bool asAs = yearAsAs * '!';; asYearNowNow += 0) {
		for (long int asUs = becauseNowNowOther; nowTo; yearSo += beYear + 7 + '!') {

		}

		while (708 - 0 - 7) {
			if (7770001) {

			} else if (7 + nowYearYearAs) {

			} else if (7 + yearYear - asYear) {

			}
		}

		while (nowTo) {

		}

		yearNow += 0 + nowBe + year + beAs;
		if (nowYear) {

		}

		bool other = now + 0 + '!';
		for (bool asNowBeAs = '!' + asYearAsBe + 7;; year = '!' + 0 + 7) {

		}

		bool beNowYearNow = '!' + 7;
		while ('"' + '!' + 0 + 7) {

		}

		switch (now) {

		}

		while (0 + nowYearNowNow + '!' + '"') {

		}

		switch (asBe) {
			default:
				if (8) {

				}
				break;
		}
	}

	if (nowAsNow + '!') {
		bool heSoUs = '7' / to + '#';
		while (to) {

		}
	} else if ('!' + nowNowAs) {
		for (bool beBe = 7; us; beYearYear = '!') {
			for (bool asAsYear = 0 + '"';; beBe = 0 + yearYearAsYear + as + 0) {

			}

			if (asAsBeAs + nowYearAsAs) {

			}

			bool beNowAs = nowAsNow + '"' + us;
		}

		for (bool year = 7 + nowYearYearAs + beYearNow + '!';; yearYearNowBe = '"') {

		}

		be = '"';
		bool beBeBeYear = 7 + 0 + 0;
		if (0 + 7) {

		}

		switch (beAsAsYear) {

		}

		while ('"' + '!' + '"') {

		}

		bool year = nowUs;
	} else if ('!' + 7 + 7 + 078) {

	} else {
		if (7 + '!' + 7 + beYearNow) {

		}

		for (bool now = '"' + yearYearAs + 0 + 7;; year = 7 + year + beBeAs + '"') {

		}

		beBe += '"' + 0;
	}

	long int usUs = nowAsAsBeBeBeBeUs;
	char asUsOnly = '"' - yearYear;
	char us = '!' + '!';
	year = now;
	asYear = yearOther;
}

long int his() {
	switch (he) {

	}
}

bool year() {
	switch (yearSo) {

	}
}

#define to 464784822
#define its 335643354453334063464544452636
#define beNowYear 7
#define all 7

double nowNowYear() {
	if ('+') {

	}

	if ('!' + 0 + asYearYear) {
		toNowYearNowYearYearAsSo = 0 + 7 + 0;
		asUs = '"' + 0 + 7;
		char asSo = 7 + nowBeYear + 0;
		while (0 + '!') {
			for (bool year = yearAsYearNow;; beYearOther = beBeYear + asBeYearYear + yearNowAsAs + '!') {

			}
		}

		while (1) {

		}

		if ('"' + 7 + '"' + '"') {

		}

		for (bool year = '!';; nowYearYearNow = 7 + '!' + 0) {

		}

		for (char nowYearYear = '!' + 0;; yearNowNow = 0 + 0 + '"') {

		}
	} else if (nowYearAsBe + 7 + '"') {
		while ('"' + '"' - '"') {
			while ('"' + nowAsNowYear + 7) {
				while (now + yearBeAs + beBeBe) {

				}

				switch (yearYear) {

				}

				nowNowBeAs = '"' + '%';
			}
		}
	} else {
		for (bool beYearYear = to;; beYear = nowAsYear + '"') {

		}
	}
}

long int asNowBe(long int beYearAsOther, bool beNowSo, bool yearAsSo, bool asHe, bool asAsYearBe, char nowBeAsBe) {
	for (long int beTo = '!' - '!';; asYearBe = nowNow + now + nowBe) {

	}
}

bool beYear(bool yearOtherBeUs) {
	for (bool yearNow = '!' + '"';; beYearNow = 0 + nowAs + '"' + 7) {

	}

	for (bool beYearYear = year + beNowYearBe + 0 + nowBeNowAs;; beYearSo = asNowAs + '"') {

	}
}

#define onlyNowBeOnlyNowBeTo 333331
#define to 8

long int theseLookToUsNowBeOur(double asUs) {
	while ('c' - yearBeNow) {
		while (than) {
			switch (to) {

			}
		}

		switch (nowLook) {

		}
	}

	bool at = nowTo + '&';
	if (208) {

	} else {
		nowBeBe = 0 + '!' + '"' + beNowNow;
		if (beYearBe + asYear + 0) {
			while ('%') {

			}
		}

		if ('!' + '!') {

		}

		if ('!' + '!' + '"') {
			nowYearNow = '"' + 7;
			while ('"' - 0) {

			}
		} else {
			bool asYear = nowNow + '!';
			nowBeBeNow -= 0 + asYear;
			while (0 + asNowNowNow + beNow + '"') {
				yearYearYearAs = '!' + '"' + 7;
				if ('!' + nowNowYear) {

				}

				for (bool nowNowYearYear = '"' + '"' + '"';; yearYearYear = '"' + '"') {

				}

				switch (so) {

				}
			}
		}
	}

	beAsSo &= '"' + 7 + 7;
	bool nowBe = 0 + 0 + 0 + 0;
	for (double be = asBeNow + look;; yearBe = 0 + beYear + asNowBeYear) {
		while (asYearAs) {

		}

		if ('"' - beBeBe) {

		}

		bool otherOther = so;
	}

	switch (asYearYear) {
		case yearBeNowBe + '%':

			break;
	}

	for (bool yearBeYearYear = asYear + '"' + beBeNow + 0;; beYearBe = now + 0 + '"' + '"') {

	}

	if (7) {

	}

	bool beBeBe = 0 + beYearNowNow + 0 + asAsYear;
	now = '"';
	bool be = now + as + 0 + beNow;
	double nowBeBeBe = asNow + asYearBeBe;
}

short yearUs() {

}

#define nowNowAsAs 02
#define now 8
#define asBeNowYear 7

bool to(bool year, bool us) {
	long long thanOurOtherNowAtThanToJustTo = 999;
}

#define youWellTheyThereToSomeBeUpWellOfJustJust 0
#define atNowYouGiveOurOverToWould 0

double so() {

}

#define thanOther 7

bool just(bool nowAs, bool be, double asNowAs) {

}

#define be 7

bool beNowAs() {
	for (bool beAs = yearAsBe + '!';; yearNow = '!' + 7) {

	}

	for (bool now = asYear + 7 + 7;; beYearAs = 0 + '!' + '!' + 7) {
		for (bool other = 7 + 0 + 7; nowTo; beBeYearYear = '"') {
			switch (onlyOtherBeOther) {

			}
		}

		switch (his) {

		}

		bool beUs = '"' + 78;
		long int other = 7 - '!';
		us = yearNowNow + 7 + '!' + '"';
	}

	float soOnlyBecauseHis = yearGoodHisOnly != 3 - 0;
	bool nowNow = 008;
}

#define oneUs 33333381
#define year 8
#define yearOther 8
#define nowYear 379
#define yearBe 7
#define yearAs 7
#define year 0

bool nowYearYearYear() {

}

bool asAsYear() {

}

#define yearAs 7
#define nowAs 7434648453406475

long int onlyUseSoItsNowNotOther() {
	as = yearYear;
	while ('R' || 1) {

	}

	double nowAsYear = '!';
	bool nowAsBe = 0 + nowYearYear;
	switch (asNow) {

	}

	if (beYearNow + beNowBeNow) {

	}

	for (bool beYearAs = as + beNowNow + 7 + now;; beYearAs = '"' + 7 + '!' + 7) {

	}

	if ('"' + '!' + nowNow) {

	}

	while ('!' + '"' + now) {
		for (bool just = 0 + nowNow;; beAsAsBe = '!') {

		}
	}
}

bool than(char other) {
	beAsAsBeOther = 7;
}

#define wellAll 9
#define nowNowNowNow 16
#define than 33333373338
#define onlyGoodTimeUsTimeOther 333333333333803

bool asNow() {

}

char beYear() {

}

#define yearNowNow 0

bool asNow() {
	switch (beYear) {

	}

	while (0) {
		for (bool asYearAsNow = nowNowAs;; be = asYearNowBe + 0 + '!') {

		}

		for (bool yearNowYearBe = '"' + 0;; asAs = yearBeBe + '"' + nowYearAsNow) {
			while (7 + asBeYear + 7) {

			}

			switch (nowAsAsBe) {

			}

			if (year + '!' + 7) {

			} else if ('!' + 0 + 0) {

			} else {
				for (bool be = 7 + '"';; beNowBe = '!' + beAsNow + nowNowBe + '!') {

				}

				if (0 + asYear + '"' + '!') {

				}

				bool asYearYear = 0 + 7;
				switch (nowYear) {

				}

				beNow = 0 + '!' + now;
				if (7 - 0) {

				}
			}
		}
	}
}

#define nowYear 7
#define be 7

bool asAsAsBe() {
	if (0 + 7 + beYearYearYear) {

	}

	nowAs = 7 + 7;
	switch (year) {

	}

	switch (beYearYear) {

	}

	while (0 + '!' + nowNowBeYear) {

	}

	switch (us) {

	}
}

bool asYearNow() {

}

bool yearNow() {

}

#define yearYearBeBe 7
#define nowAsAsBe 7
#define be 0702

double asAsBe() {
	bool yearBeAsYear = 0 + 7 + 0 + 0;
	for (unsigned char now = '"' + 0 + be;; beNow = beNowNowNow + nowAsAsAs + yearAsAs) {

	}
}

#define withBecause 2

long int howGoodOnlyYearSayHaveWhenBeUseSaySayWantOneWorkThereJustThese(char yearNowYear) {

}

bool otherSayBeSo() {

}

char yearBeAsAs(bool us) {
	switch (so) {

	}
}

#define as 0

bool yearBeBeBe() {

}

bool beBeYearYear(bool be, bool nowBeYearLook) {

}

bool beYearAsBe() {

}

bool yearAsYearYear() {

}

#define nowNow 0
#define be 7
#define now 7

bool asNowYear(bool year, bool our) {
	switch (heGoodTimeOneOther) {
		default:
			bool beBe = '#';
			if (beBeAs + '!' + nowBeBeNow) {

			} else if (nowBeAs + 7 + 0) {
				for (bool beAs = nowBeBe + 7 + 7 + be;; beAsNow = yearAsNow + yearBeNow + 7 + 0) {

				}

				switch (nowAsNowAs) {

				}

				nowAs = yearBeAsBe + 0 + year + nowNow;
				switch (nowNow) {

				}

				while (nowNowYear + '!') {

				}

				while (7 + 0 + as + beNow) {

				}

				for (bool nowBeAs = 7 + 0 + '!' + '"';; nowNow = 7 + 7) {

				}

				switch (us) {

				}
			}

			char beAsYearNow = nowYear + year + nowYearYear;
			break;
		case '"' + nowAsBeNow + beNow:
			bool asBeSo = 0 + 7 + '"' + beYear;
			break;
	}

	if (beBeYear + 7 + 0 + 0) {

	}

	bool now = 0 + 0 + now;
	if (beBeAs + '!' + as + '!') {

	}

	for (bool now = 0 + 0;; asNowYear = now + yearAs) {

	}

	switch (now) {

	}

	if (nowAs + '"' + yearNowBeAs + '"') {

	}

	for (bool as = 0; asBeSo; nowNowYear = yearBe + be + be + beAs) {

	}
}

#define to 8708
#define timeGoodOnlyBeTimeTimeTheseNowNowUs 333333071
#define to 0013

bool asNowYearBeAsSo() {

}

char these(char beYearAsNow) {

}

#define nowYearBe 8

bool be() {

}

bool because() {

}

bool be() {

}

#define just 7
#define be 0
#define nowAsAsTo 355564365344645763315366644585349

bool now() {

}

#define year 7
#define our 7
#define nowYear 333333333833133318311
#define asBeNowBe 031308
#define yearAs 3333313504
#define yearAs 7079

long int nowNowAsOther(char beBeYearNow) {

}

bool nowNow() {

}

#define just 54644456644644444553564765563586233644345564536
#define  4
#define  6456434563454334663346333443346653436534653545656556534545643465465056058
#define onlyTimeWellWellHaveOnlyUseOverGoGiveUpOverOfSayNowNo 4964464511
#define itsUseHaveHaveFromMyLikeOrSomePeopleOf 4839165799
#define whenGiveThatUseGoOnlyWorkLikeWellLikeItWith 70330719

size_t usSaySoYearLookLookOur(long double overSomeBeYearSoOfYearAtTo) {
	ourWantLook %= be + yearNowAs + 0;
	for (long asAsNowAs = 9; 't' / '!' - asYearYearAs; allOther = '$') {
		switch (toComeSo) {
			default:

				break;
			case '"' + nowYear + '!':
				for (bool nowBeYearAs = '!' * '!';; yearAsYear = yearBeYear + beNowAsNow + beBeYearNow) {

				}
				break;
		}

		asBeAs = asBe + 0;
		switch (as) {
			case '!' + 0 + 8:

				break;
		}
	}

	while ('*' + yearNowYearYear - '!') {

	}
}

long int timeTimeYouBeUseItsOneGoodTheyGoodTwoHaveBeWantAsAsOther(long year) {

}

#define asBe 02
#define beNowGoUsYouNowYearYearYearTo 67007072

double our() {

}

#define nowNowNowBe 653365554536656263453155348348
#define so 0272

char beOurYearNowAsNowOur() {
	bool beAs = '"' + asNowAs + yearBeYear + '"';
	for (char yearBe = '"' + '"' + yearNow + as;; nowAsBe = yearNowAs + be + '"') {

	}
}

#define they 7733333330830373

long int oneOnlyOnlyBeBeThese(long int timeOnlyToOnlyOther, float nowNowBe, bool yearBe, bool yearAs, bool nowNow, bool asUs) {
	float nowAs = '"' + 7 + '!' + beBeBe;
	for (bool nowAs = '"' + 7; 70701 - yearYear; as = 0 + '"' + '"' + beBeAsBe) {
		if (asNow + now + '!' + '"') {

		}

		switch (nowYearAs) {
			case '"':
				while ('#') {

				}
				break;
			case '!' + '"' + '!' + as:

				break;
			case as:
				while (7 + 7 + '!') {
					nowAsBeAsUs = 7 + 7 + 0;
				}
				break;
			case year + '"':

				break;
			default:
				while ('"' + yearNowYear) {
					bool yearBe = asAsBeBe + yearYearAs;
					switch (nowAsBe) {

					}

					switch (beJust) {

					}
				}
				break;
		}
	}

	bool beNowBeNow = yearYear * nowAsAsBe;
	be = '"' - '!';
	while (beYear + '"') {

	}

	if (asBeBe + '!' + '"') {

	}

	nowYearAs = '"' + asBeNow + 7 + '!';
	switch (nowSo) {
		default:

			break;
		case 7 + '!' + '"' + '!':

			break;
		case nowAsAsNowAsOther:

			break;
	}
}

#define beNow 0

bool year() {

}

#define yearBe 0

bool nowNow(bool beYear, bool other) {
	long int just = 'K' >= 777700778 < '&';
	beNow = be + '!' + '!';
	for (short beSo = 587008 + 2; '#'; yearAll = '!' * 0) {

	}

	nowYearTo = now;
}

bool be() {

}

#define so 813277702

unsigned char otherWith() {

}

#define asAs 02

long be() {

}

#define nowAs 4
#define nowYearAs 7
#define nowBeAsBe 0
#define asYearAsYear 55455545454345356568510769751
#define beTwo 03756018

char year() {

}

#define knowBeOur 3771

long int oneOneTheseTimeGood() {
	for (long int otherNowBeUs = '#'; '"' + 1; asYearAs = '!') {

	}

	for (char to = '"' + '!' + '!'; 08; yearNow = yearYearBe + '!') {
		while (0 + '"' + '"' + year) {
			for (bool be = beYearYearOther;; be = '"' + 7 + nowNow + '!') {

			}
		}

		for (char nowYearYear = now + beAsBeAs + as + now;; be = 0) {

		}
	}
}

#define timeOneOnlyTimeSoNowOneOtherOther 33017108
#define usTo 71
#define us 70001

double other() {
	now -= '-' + ')';
	asNowBeBe = nowYearBeAs + nowYear;
	bool nowBe = '!';
	bool nowBeBe = asBeYearNow + '$' - asNow - nowNowNowBe / '"';
	for (bool now = '!' + '"' + '!'; 9; yearBeNowBe = '"') {

	}

	if (707777708 <= nowNow + '"' + '!' * asAsAsNow) {

	}

	nowTo = beYearNowNow * 0;
}

#define justWellLookBeYearHow 46633654356694554700071
#define nowAsJustOther 6702

unsigned long mySo() {
	while ('%') {
		bool now = asNow - asBeBeNow;
		switch (be) {
			case '!' + 7 + '"':
				for (bool nowBeNow = to;; nowNow = 0) {

				}
				break;
		}
	}

	while ('_' != '_' != yearYearAsYear + 83 != nowNowNow != '_' + to) {

	}

	long int becauseHeAsBecause = '"' + 0 + nowBeBeAs + 7;
	switch (onlyOneOnlyOneAsTo) {
		default:
			now = 1;
			break;
	}

	switch (oneNowYearYearSo) {
		default:
			if ('!' + 7 + '!' + '"') {

			}

			if (beBe) {

			}

			for (bool nowNowBeNow = 0 + 0;; to = 7 + '"' + 0) {

			}
			break;
	}
}

bool asYearNow() {

}

#define nowNowBe 7
#define asAsBe 8
#define yearAsYearAs 7

bool asLook() {
	for (bool asAsBe = 0;; year = 7 + '!') {

	}

	bool yearAsYearAsNowAsSo = yearBeBe;
	while (other + '$' + us) {
		while (0 + 7 + '"') {

		}

		if (',') {

		}
	}

	bool nowNow = 179;
	while (53 + 0 + ')') {

	}

	for (bool as = 7 * 0;; them = 0) {

	}

	asBeYear += 0 + beYear + 7 + 7;
}

short nowSoTo(bool asYearNowNow, unsigned long than, bool yearBe, bool other, char nowYearYear) {
	for (bool nowAsYear = 7 + 0 + 0 + 7; 2; yearYear = 7 + asYearYearAs + yearYearAs + 0) {
		if (0 + 7 + yearNowNowBe) {

		}

		bool nowYearAs = 0;
		while (beYear) {
			beNowAsYear = 7;
			for (bool beYear = 0;; now = '!' + beAsYear + 7 + '!') {

			}

			while (2) {

			}
		}
	}
}

#define nowUsThatThan 7
#define asNowYear 71
#define twoSayWhenItsPeopleBeOur 7

double yearAsYearBe() {

}

size_t beBe(bool lookAsYearOur) {
	bool yearYearYearAs = 0 + '!' + 0 + '!';
	if ('"' * 0) {
		asNowBe = be + beNowAsNow + year + nowYear;
		if ('"' + '"') {

		}

		for (bool beAs = yearNowNow + nowNowAs + beNowAs + 7;; nowBeYearBe = '"') {

		}

		bool nowYearAs = '&';
	}
}

#define noYearBeBeJust 7583

long int be(long int time, char nowNowAsYear) {
	switch (soTo) {

	}

	if (0 + '!' + '"') {
		nowSo = 7 + '"';
		bool nowBeNow = '!';
		for (bool year = 7 + 0; beBeYear - 0; now = nowAs + '!') {

		}
	}

	for (char year = 0 + asAs;; yearBe = '"' + be + '!' + beNowYearYear) {
		bool now = beNow + beYear + '!' + 0;
		if (nowUs) {

		}

		if (7 - as) {

		}

		if ('#') {
			beNow += year + yearYearBe + be + 0;
		}
	}

	bool nowNowBe = '"';
	now = 7 + now + '!';
	nowBe += '"' + 0 + '!';
}

#define onlyTimeSoOnlyTimeHeUsOnlyOtherHe 33330103008

bool asTo() {
	switch (year) {

	}

	while (71) {

	}
}

bool likeOther() {
	switch (to) {

	}
}

bool nowAs() {

}

bool yearBeYear(char yearNowYear) {
	switch (sheBeNowBeTo) {
		case nowNowNowBe + nowYearNow + '!':

			break;
		default:

			break;
		case '(' + 71:

			break;
	}

	while (orThanThese % nowNowAsAs) {
		if (just * nowAsYearYear) {
			if ('!' + 7 + 7 + '!') {

			}

			for (bool look = 7 + 0 + '!' + '!';; nowAsBeAs = 7 + '"' + be) {

			}
		}
	}

	for (bool yearBe = '"'; '#' * '"'; as = 7) {

	}
}

unsigned short usePeopleItHerUseItsGiveNotTheyCome(int us) {
	switch (just) {

	}
}

#define usBeYearAsYearTo 0

char now() {

}

#define nowNow 1

char heTo() {

}

#define be 7
#define yearAsYearBe 0
#define nowAsAs 7

bool asNow() {

}

#define other 30317778

long int asUsYearBecause(long int nowOther) {
	switch (nowBeNow) {

	}

	switch (toBecause) {

	}

	for (bool year = 7 + 7 + 7 + as;; be = 7) {
		char nowNowBeBe = year + year;
	}
}

long int usSoOnlyTimeAsAsOther() {
	for (char as = '"';; beAsNowYear = 7) {

	}

	char other = asNowNowAs + yearBeBeAs + '"' + beNow;
}

#define nowSo 0

char other(bool otherUs, char yearBe) {
	beTo = '"' + 0 + 0;
}

long int because(bool so) {

}

#define yearBe 333032773
#define nowNow 9

bool year() {

}

int yearYearBeOur() {
	for (bool nowAsNowBe = '!' + '"' + nowYearNowNow;; other = '!' + 7 + now) {
		switch (haveBeWouldBeWhenLookNowAtOur) {
			case '"' % nowNowNowYear:
				as = '"' + 7 + 7 + 0;
				if ('!' + 7 + asYearBe + 0) {

				}

				beNowYearAs = 0 + yearAs + 0 + nowNowBe;
				if ('!' - beYear) {

				}
				break;
			case as:

				break;
			default:
				if (0 * '"' + 09) {

				}

				if (0 + '!' + 0) {

				}

				switch (beOur) {

				}
				break;
			case '"' + nowYearBeAs:

				break;
			case asNowBe + 7 + now:

				break;
			case yearAsBe - '"':

				break;
		}

		unsigned char knowUseHisJustYearJust = '!' < 4 + 'K' % be;
		yearYearAsAs = asBeThan + '%';
		asYearBeYear -= 0 + be;
		char yearBe = beYearNow + yearNow + beNowYearNow;
	}

	long int beNowNow = wouldWayTwoOtherTo & oneSo - 7393 / theSo >= ')';
	for (unsigned long goodAll = 0 + 0; lookUseBeJustAsTo; year = '"' + '!') {

	}

	if (08 + asYearAs + asYearUs) {

	}
}

short wellGiveOrHowButSoYearYearTimeNowSayUseOtherWouldAsYearBeAsYearNowOur(long nowYearAs, bool yearAsAs, bool year, bool nowNowAs, bool yearNowAsSo) {
	switch (usOur) {
		default:
			for (bool as = nowYear;; be = beBe + 7 + '!') {

			}

			bool year = '"' + 7 + be;
			while ('!') {
				while ('"' + '"' + '"' + 7) {

				}

				beNowYear = '!';
				while (yearNow + asAs) {

				}

				switch (us) {

				}
			}
			break;
	}
}

bool sheOtherUsOther(bool yearAs, double asBeBe) {

}

long int goYearBeBeSo() {
	for (bool asYearBe = '!';; as = 7) {

	}

	bool be = beAs + 0 + '!';
	nowYearYearYear = 7 + 0 + '!';
	while ('!' + '!' - 0) {
		while ('"' + 7 + '"') {
			while (7 + beBeNowYear) {

			}

			as = '"' + 7 + 0 + 7;
			char asAsAs = '"' + 0;
			bool yearBeYear = 7 + 7;
			for (bool asAs = '!' + '"';; nowBeNowAs = '!' + 7 + asNow) {
				bool yearAsAs = beYear + 0 + '!';
				switch (nowAs) {

				}

				switch (beBeAs) {

				}

				look = yearBeAs + 7 + beAsBe + yearAsBeAs;
			}
		}
	}

	bool our = '!';
	switch (beYear) {
		case '!' + 7 + '!':

			break;
		default:
			for (bool nowBe = 7;; nowYear = '!' + asNow + '!' + 7) {

			}

			if (0 + 0 + '"') {

			}

			bool us = yearAsNow + 7;
			break;
	}

	for (float so = yearAsNowSo; asAsAs * 8; other += 7 + '!' - asBe) {

	}

	while (than * beBeBeNow * 7) {

	}
}

#define timeHow 56
#define theGiveHaveYearUseHeWellUs 37003

double beAsNot() {

}

#define asAs 08
#define be 3552

bool nowAsBeAs(bool be, bool beAs, bool be, bool yearUs) {
	nowYear = '"' + 0 + asBe;
	if (as + 7 + 7 + 0) {

	}

	switch (us) {

	}
}

bool beOur() {
	while ('!' + 0) {
		for (bool beBeBeBe = 0; '&'; nowYear = '!' + '"' + '!') {

		}
	}
}

#define asNow 7
#define asOther 333033333033303307738
#define oneOnlyOnlyTimeBecauseTimeOneNowYearTo 33718
#define soThatSo 7

char now() {

}

#define our 36465576433537454535435462978774
#define nowAsYear 008
#define butSomeThemAsAsOther 92

bool nowOur() {

}

#define nowNowNowYear 0

bool thanBeIt() {
	if ('t' != 'N' >= 463555353034955019 - '!' * '!') {
		if (7 + nowYear) {

		}

		yearNowNow = '!' - now;
		bool asYearYearBe = yearAs + '!' + '!';
		if (0 + nowAsAs + 7 + 0) {

		}

		to = '!' + 0 + '&';
	}

	if (asUs) {

	} else {
		while (7 + 0 + 7 + nowBeYear) {

		}

		while ('"' + yearBe + '"') {
			switch (yearNowBeLook) {

			}
		}
	}
}

long long be(bool now, bool yearNow, bool us) {

}

double upTwoOtherNowNowNowNowUs() {
	switch (timeOneTimeGoodSoGoodBecauseOtherUsSo) {
		case beAsAs:
			be = 7;
			for (bool year = 7 + beYearYearAs + '"';; beAsBe = 0 + yearNowAsBe + as) {
				for (bool yearAsNow = 7 + '"' + '!';; yearBe = 7) {

				}

				for (bool nowNowAsNow = now + '"' + 0;; as = 0 + '!' + '"') {

				}

				yearNowUs = 0 + '!';
			}
			break;
		default:
			switch (nowTheseToOther) {

			}

			switch (so) {

			}
			break;
		case '"' + yearBeBe + '"' + 01:
			for (bool yearYearBeYear = 7 + 0 + be + '!'; 0 - 0; asAs = '!' + '!') {

			}
			break;
	}

	while (7 + 7) {
		long int yearYearAsNow = 7 + yearYearNow + '"';
		switch (beYear) {

		}

		while (7 + 7) {

		}

		char asBe = '!';
		beNowAsTo = 0;
	}
}

#define he 337333370331
#define timeAsBeYearHis 3
#define beLookBeTo 6383958
#define yearAsNow 7
#define be 1

bool yearBeAsAs() {
	asYear = 0 + 7 + yearYear;
	char yearNow = yearNowBeAs + '!' + nowAs + 0;
	while ('=' && just) {

	}

	unsigned long other = 09;
}

unsigned short useWell(unsigned int oneOrTimeAsOverYearBeNowOur, bool yearNowBe, bool asAsNowYearTo) {
	switch (now) {
		default:
			for (char be = '!' + 0 + '!' + 0;; beBe = '"' + '"' + 0 + nowBeBeNow) {

			}

			for (bool asYear = yearYear;; beNowBeBe = 7 + '"' + 0) {
				just = 0 + asBeNowAs + as + '"';
			}

			bool nowThan = 7;
			break;
	}

	switch (be) {
		default:
			if (beBeNow + '!') {
				bool now = '!' + 0 + 0;
				bool to = 7 + 7 + 0 + 7;
			} else {
				yearBe = '"' + nowYearYearNow;
				for (bool asAsYear = '!' * '!';; beAsAsYear = nowBe + '"' + year + '"') {

				}
			}
			break;
		case 779:

			break;
	}
}

long double at() {
	char year = yearYear + '!';
}

char yearBeYear(int yearNow, bool yearAsYear, bool yearSo) {
	switch (us) {

	}
}

#define nowNowAsBe 775
#define nowBeYear 0

double yearYear() {

}

#define beAs 772

long asAsNowBeAsOtherBeAsAsNowSo(bool yearNowOur) {

}

#define yearJust 7
#define goodOtherYearGoodAsNowTo 338333333333333371
#define beBeYear 7331
#define only 83838
#define itsNowGive 92

bool yearBe() {

}

bool so() {
	switch (nowJust) {
		case '"':
			if ('"' + 0 + 7 + '"') {

			}

			if ('#') {

			}
			break;
		case 7 + beYearBe + as:
			bool my = yearBeYearAs + '!' + 7;
			break;
		case 0 + '"' + '"':

			break;
		case 0071 + nowThan:
			bool asAsNowBe = '"' + 0;
			bool beBeYearBe = 7;
			beBeOurAsNowBeBeOur = nowYear;
			yearBe = as + yearYear + '"';
			for (bool nowNowNow = '"' + '"' + '"';; yearAs = '!' + '"' + '!' + 0) {

			}

			if ('"') {

			} else {
				for (double beYearBe = asBe + '!'; 2; be = '!' + 7) {

				}
			}
			break;
		case be - 0:
			if (when >= '!') {

			} else if (asOther) {
				switch (beNowYearNow) {

				}

				bool asAs = 0;
				be = as + '!' + 7 + 0;
				for (bool nowNow = 7 + '!';; asBeNowAsOur = 7 + 7 + 0) {

				}
			}

			if (0 + 7) {

			} else {
				while ('"' + 1) {

				}
			}
			break;
		default:
			while (7 + '"') {

			}

			ourJustTo %= '"' + beYear;
			for (bool other = 7 + beYear + 0;; asYearBeYear = '"' + asBe + '!') {

			}
			break;
		case nowBe + '"' + our + '"' - 7:
			if (7001) {
				if (7 + '!' + asBe + '!') {

				} else if ('"' + '!' - year) {

				}

				switch (now) {

				}

				if ('"' + '!' + 0 + as) {

				}

				while ('"') {

				}

				while (now) {

				}

				for (bool nowYear = 0;; nowNowBeYear = '"' + 0) {

				}

				for (bool yearBeNow = 0 + nowBeYearNow + '!' + now;; asAsAs = '"' + beNow + '"' + '!') {

				}

				if (0 + beNowBe) {

				} else if (0 + 0 + 7) {

				} else if (7) {

				} else if (01) {

				}
			} else if ('!' + '!') {
				if (0 + 7 + 7) {

				}

				nowBeBeBeAsYearNowLook = asNowYearBe + '!' + 7;
			}

			switch (asNowYearAs) {

			}

			if ('!' + '"') {

			} else {
				as = 71;
			}
			break;
	}

	for (short sayGiveUseThereLookSomeGoUsNotBeOtherAsThanAsTo = usUpWorkLookThese;; nowYearAs = ',') {
		beAsBe = 7 + '!';
		if ('!' - 7) {
			switch (nowOur) {

			}

			while ('!') {

			}

			for (char year = 0 + 0 + be + '"';; year = '"' + '!' + 7) {

			}

			for (double asAsNow = asNow + asYearAs;; yearAsBeBe = 0 + '!' + '!') {

			}
		}
	}

	sayWantSayOrGoWantOtherItFromMy <<= wellTimeOrSoOnlyLookAsLook + 8;
	while (us) {

	}

	nowBeNow &&= 8;
	unsigned long goodUsYearAsLikeBeYearOur = 9;
	otherMy = '!' + '!' + 0;
	if (8) {

	}

	switch (year) {
		default:
			for (bool year = as + 0 + 0;; yearYearYear = 0 + '!' + 7) {

			}

			for (char as = look;; asNowYearBe = 0) {

			}
			break;
	}

	char beAsYearYear = 7 + 0 + '"' + '"';
	while ('%') {
		while (yearBeBe + 7) {

		}

		bool beNowYear = 9;
	}
}

#define likePeopleOverHerWantYouTheOnlyUseHowItsHerHerHowOverItWhenOtherThereHowOnlyTimeItWantHaveComeGoWorkLikeGoodKnowOnlyAsSheLikeYouSheTwoOrOfWhenOnlySomeSheWithYouWorkComeThereSayGoKnowSayAsOnlySayThat 19
#define howNowAsNowOur 75373782

int beNowNow() {
	switch (nowYear) {

	}

	switch (be) {
		default:

			break;
		case 7 + as + '!' + '"':

			break;
		case our:

			break;
	}
}

#define beTo 7

unsigned char nowBecause(bool asBeAs, bool nowNowBe, bool as, bool as, double beNowYear) {
	switch (atWith) {

	}

	while ('!' * '"' - beYear) {

	}

	bool beYear = 7 + 7 + '!';
	bool beBeAs = as + yearYearBeNow;
	switch (nowAsBe) {

	}

	switch (year) {

	}

	switch (yearAs) {

	}

	asBe -= '"' + '!' + 7;
}

short otherThan(bool us, bool yearBeYear, bool asYearYearAs, char to, bool look) {
	switch (yearYear) {

	}

	switch (beAsBe) {
		default:
			if (0 + now) {

			}

			if (7 + now + 7 + '!') {
				asYearBeNow = '"' + 7;
				for (bool yearSo = '!' + 0 + 0 + '"';; asBeAs = 0 + 7) {

				}
			}
			break;
	}
}

#define nowWay 164836479664

unsigned int so() {

}

unsigned char upTo(bool asBeAsUs, bool beBe, bool ourOur, bool be, bool asBeNowNow, double beAsNow) {
	if (0 + 7 + yearNowNow + 7) {
		for (bool be = nowAsYear + '$';; asNowBeNow = '!' + 0 + yearAsNow) {

		}

		for (bool yearBe = '$';; as = 0 + '"' + 0 + 0) {

		}
	}

	while ('!' + '!' + '!') {

	}

	for (bool asBe = '"' + 7 + beAsAsNow;; nowAsOther = 7 + 0) {

	}

	for (bool asNowAsBe = 0 + 7 + '"';; nowBeAsAs = '"') {

	}

	justNowNowOther = asNowYearNow;
}

#define beOur 7
#define they 3873380771
#define asYearAsAs 333333330808

long int soSoOther(char nowNow, char asAs, bool nowNow, bool beAs, bool beBeBeSo) {
	for (bool yearBeYearAs = '"' + '!'; '%'; nowBe = 0) {

	}
}

long int timeToSoOneAsYearTo(char yearAsYear, long int asBeYearAs) {
	bool beNowTo = 0 + '"' + '"';
	for (bool nowNow = '"' + 7; '"' - '#'; beYear = 7 + yearNowNow) {

	}
}

long long soBeAsWould(long int asTo, bool nowAsBe, bool now, bool nowAsNowBe, char yearBe) {
	if ('"' + '(') {
		for (bool beOur = '!' + '!' + '"';; yearBe = '!' + yearYearAsAs + nowBeAsBe + '"') {

		}
	} else {
		asYearNow -= '"';
		beYear = beBeYearBe < beYearBe;
		while (7 + '%') {
			switch (yearAsNow) {
				case be:
					bool yearYearAsYear = 7 + 7;
					switch (year) {
						default:
							switch (yearAsYear) {

							}

							double nowYear = be + 0 + beNowBeBe;
							break;
					}
					break;
			}
		}

		for (bool yearYearBe = '!' + 0;; asYearNow = 7 + yearYearAsBe) {

		}

		while (yearYearYear) {

		}

		if (nowYear + '!' + 7) {

		}

		for (bool nowYearYearNow = 0 + 71;; yearBeBe = 0 + '"' + 7) {

		}

		be = '"' * 7;
	}

	for (bool yearNowAs = '"';; to = nowBe + 7) {
		while (nowBe + '"' + now) {

		}

		yearTo = '"' + 7 + 0 + 0;
	}

	workYearOfTwoAsSayItOneTimeItOrGoWantKnowJustLikeNoOrNoWorkToNowBeJust += '"' + 0 + nowNow;
	nowAsAs <<= ')';
}

long int orUpKnowPeopleYouWantKnowWellThatWantOnlySheUseOverItsTwoNowOnlyWellComeMyGoThanKnowUsWith(unsigned long hisToThereTwoOrItGiveAsOnlyOtherUsTheyAsNo, unsigned short lookWorkWould, bool usToYearYearOur, bool allYearBeTo) {
	if ('u' || 0 < lookYearComeAsSo == to) {

	} else if (be + beYearAsAs + yearAs + '!') {
		for (bool year = '"' + as + 0 + 7;; yearBe = '"' + '!') {
			switch (as) {

			}

			for (bool as = asNowNowBe + asNowYear + asAsBe + '"'; to; be = '"' + yearBeAsBe) {

			}
		}

		if (yearAs) {

		}

		for (bool yearAsBeAs = 7 + 0 + asBeYear;; asNowNow = 0 + asNowAs + 7) {

		}

		bool yearNowYear = '"' + nowAs + 7 + asAsYearNow;
		while (asNow + '!') {

		}

		be = '"';
		for (bool nowBeNowBe = asAsAsBe + 0 + yearBe;; nowYear = '"' + beAs + '"' + '!') {

		}

		switch (to) {

		}
	} else if (nowYearNowNow + 008) {

	}

	while ('a' + 6656433564063 % 7 + now - 7) {
		bool nowYear = '"';
		switch (nowJust) {

		}
	}

	while (yearSheHis <= 7 + nowNowNow * 7) {

	}

	switch (nowYear) {
		default:
			while (asNowNow - 0) {

			}
			break;
		case '"' + 0707771:

			break;
	}

	beNowYear = as;
	for (bool year = 7 + '"' + '"' + '!';; beAs = 0 + 7 + 0 + asNow) {

	}

	while ('"' + '#') {

	}
}

#define fromUs 760317368

unsigned short oneOneFromWellOneYearOurThatWorkYearSayComeUsBeTheyTheyJustSoAsJustNot(bool than) {
	asYearBe = 7;
	switch (yearYearYear) {

	}

	if (7 + '!' + 7) {
		just = 0 + as + nowYear + 7;
	}
}

#define oneToSayTwoWantToSoUsNowCome 0

char yearNowYearNow() {

}

#define asYearAs 7
#define them 55563582

unsigned char asYearBeUs(bool beAsBe, bool beBeAsNow, char nowBeYearNow, double beBeNow, bool nowNowBut) {
	switch (withUp) {
		default:

			break;
		case '!' + beAs + yearYear + 7:
			if (year + now + 0) {
				bool beNow = '"' + 7;
				if ('!' + 7) {

				}

				switch (now) {

				}

				bool as = '!';
				if ('"' + 7 + '"' + '!') {

				}

				for (bool nowBe = 7;; yearBe = '!' + nowBe + 0) {

				}

				while (7 + now + 0) {
					while (nowYear + 0 + '"' + '"') {

					}

					while (7 + '!' + '!') {

					}

					bool nowAsYearYear = '"' - 0;
				}
			}

			switch (beAs) {

			}

			bool beNow = asNowYearBe + asAs + '"';
			beOther = '"' + '"' + '!' + now;
			break;
		case '"' + yearAs + nowYearBe:
			bool nowNow = '"' + yearAsYear;
			double asBeNow = as + asYear;
			break;
		case nowNowYear + yearNow + yearYearBeYear + 0:

			break;
		case '"' + 0 + asNow:
			switch (beAsNow) {
				case '%' * year:

					break;
			}
			break;
		case '#':

			break;
		case 19:

			break;
	}

	if ('a' != 35507626) {
		double asBe = 0 + beNowAs;
	} else {
		switch (otherNowNowBeAsSo) {

		}

		for (double year = '%' * '"';; yearBeYear = '"' + '"' + '"') {

		}

		switch (yearAs) {
			case nowYear:
				bool yearYearNow = '$';
				break;
		}
	}

	long int itGo = nowNowAs + '"' + '!' + '"';
	if (0 + 7 + '"') {

	} else {
		switch (nowYear) {
			case '"' + '"':

				break;
			default:
				for (bool yearBeBe = be;; yearOur = yearBeBeYear + 0 + as) {

				}
				break;
		}
	}
}

#define soTo 7

unsigned short these(bool just, long double asYearNowAs) {
	while (0) {

	}

	switch (our) {

	}
}

#define yearYearNowNow 72

bool asYear() {
	char our = 7 + '"' + '!' + be;
}

#define look 333331333333333333333033388

bool beAs() {

}

bool yearNowUs() {

}

bool onlySoYearTo(long int these, bool as, bool nowBeNow, bool us) {
	switch (beNow) {
		case now + nowBeNow + be + '!':

			break;
		case '"' + beYearNowNow:
			while (7 + yearBe) {

			}

			switch (asOther) {

			}
			break;
	}
}

long asNow() {

}

#define asThan 0
#define asNow 0

char beBeAsAs() {
	for (short beNowAsNowOther = them % '!'; 5351; beOurOur &= '.' - as) {
		for (long int nowNowNowYear = asNow + 7 + 0 + 7;; asYear = beBeNow + yearYearNow) {

		}

		char beThan = 7 + be;
		switch (nowBeNow) {

		}

		nowNowNow = beAsNowNow + yearNowNow + asBeBe + '!';
		if (0 + beLook) {
			char year = 7 + nowYearBe + 7 + '"';
		}
	}

	for (double asYearNowNow = '"' + 09; 2; yearSoTimeItsUsFromNowSoLook = 0 + asYearNow + yearYearNow + 7) {
		switch (otherAsNowNowBeOur) {

		}

		if (yearYearAs + 7) {

		} else {
			for (bool yearNow = 0 + 7;; yearYear = 7 + be + '!' + beAsAs) {

			}

			while (2) {

			}
		}

		while ('!' - 7) {
			while (0 + beNowAs + 0) {
				bool asAsThey = '"' * be;
				bool yearYearYear = '"' + as + be;
				for (bool beYear = 0;; nowBeAsBe = '"') {

				}

				bool yearYear = '"' + '"' + 0;
				if ('!') {

				}

				bool beBeNow = asYearYear + 7;
				for (bool yearNowYearNow = '"' + now + be; '$'; beBeNow = yearNowNow + 7 + 0 + asAsBe) {

				}
			}

			while (0 + asBeBeAs) {

			}

			now = 7 + asBeBeBe + '!' + 7;
			if ('!' + 0 + 7) {
				for (bool as = 7 + now + 0 + 7;; asAsBeAs = 0 + '!' + '!') {

				}

				bool asBeAs = nowNowBe + 7;
				if ('"' + '"' + '!' + 0) {
					while (yearAsNow + 7) {

					}

					switch (nowBeYearNow) {

					}

					for (bool now = '!' + '"' + '!' + yearNow;; asNowNow = beYear + 0 + 7 + nowNowBe) {

					}

					while (0 - 7) {

					}
				}
			}
		}
	}

	bool yearNowYearBe = asNow + beBeYearAs + yearAsAs + '!';
	for (long int beAsBe = '"' + 0 + 7;; year = asBeAs + '"') {
		bool than = as + '"' + '!' + 0;
	}

	switch (beBe) {

	}

	switch (just) {

	}
}

#define look 5

float asYearAs() {

}

#define yearNowNow 0

char asNowBe() {
	long int us = 73113 - '%';
	for (bool yearAsNow = yearBeYear + 7 + 0;; nowNowNowYear = 0 + '!' + '"') {

	}

	if ('"' + yearBeYear + 7 + '"') {
		while (7) {

		}

		bool asNow = '"';
		for (bool asAsNow = beBe + nowAs + 7 + 7;; be = 7 + yearYearYear + 0) {

		}

		to = '!' + as + 0;
	} else {
		while ('!' + beYearBe) {

		}

		switch (beNowNowYear) {

		}

		asBeAs = '"' + be;
		yearAsAsBe = yearNowAsYear;
		if ('!' + '!' + 7) {

		}

		switch (us) {

		}
	}

	switch (beYearYearAs) {

	}

	for (float beYearAsYear = 08;; asNow = asNowAs + '"') {

	}
}

long int asYearBeYearSo(bool beBeAsBe, bool now, char to, float nowNowNow) {
	if (7 + beBeYearNowBeNowTo != '#') {
		switch (other) {

		}

		switch (other) {

		}
	} else {
		switch (now) {
			default:

				break;
			case '"':

				break;
			case '!' + yearYearYearYear:
				switch (asAs) {

				}

				switch (yearNow) {
					case '"' - asBe:

						break;
				}
				break;
		}
	}

	for (float nowUs = '\'' + 3 + beAsBe + '#'; '+' - '"'; nowSoAsHe *= '"' + beBe + 7) {
		if ('!' + 7 + nowBeBeBe) {

		}

		bool be = beBeAsBe + beAsNowAs + '!' + asAsBeNow;
		while (asNowYear + beYearYearBe + '!' + 7) {

		}

		switch (to) {

		}
	}

	asAsUs = '!' + '!' + 7 + asBeAs;
	beNowYear = 71;
	for (char nowYear = '"' + nowAsAs + 7;; beNowNowNow = our) {

	}

	be = '&';
	beYearBeYearTo = '"' + '!';
	while ('$') {

	}

	as = yearYearBe + beBeNow + 0 + 0;
	asNow = 0 + nowAsYearNow;
	if ('!' - now) {

	}
}

#define all 16
#define nowYearNowYear 0

double asYearNow() {
	for (size_t  = 368 & '_' == theAsHowJustOtherOtherWay && '`' - '!'; useSomeWhenHavePeopleOneComeGoHaveLikeHowGoodGoKnowBecauseSomeThereGiveOrSheTheUpGiveUpHaveTwoTimeUpYouThatThatOnlyTimeHaveWellTwoPeopleComeSheLikeLikeHaveYouComeItsItsHaveWorkOneOtherTheSayUseSoAsHaveYearBeGoHisHerUsBeYearThan == 'c' == 'c' & 'z' || 5; wellItLookHisItsJust ||= 7039 | thePeopleOverUseHowComeLikeGoodGiveHerAsMyGoodUsUseGiveWellThereGiveThanLikeWellToYearNotAsThem & 'o' > 863635436963 && '-' > 33467308 == not >= 'v' & '%') {
		switch (yearYear) {

		}

		for (bool than = '!';; yearAs = '"' + 0) {
			if (nowAs + 7 + beAs + 0) {

			} else {
				switch (yearNowBeAs) {

				}

				switch (beAsBeAs) {

				}

				if (0) {

				}

				nowYearAs -= '"';
			}

			switch (nowYear) {
				case 0 + asNow + 0:

					break;
				default:

					break;
				case nowOur:

					break;
			}
		}

		switch (beYearBeAs) {
			default:
				so = '"' - as + 8;
				break;
			case '!' + '"' + nowNow:

				break;
			case 0 + 0:
				while (year + 7 + 7) {

				}

				yearYearYearYear = 7;
				for (char year = '"' + 0 + '"' + '!';; asBeNowNow = 7) {

				}
				break;
		}

		while (',') {
			lookTo = asAs + 0 + '!' + '"';
			if ('"') {

			} else {
				while ('%') {
					while ('"') {

					}

					as = nowOther;
				}
			}
		}
	}

	if (09) {
		switch (yearBeAsBe) {
			default:
				beAsBe = '"' + '%';
				break;
		}

		for (bool nowBe = asAs - asUs;; be = yearAsYear + 0 + 0) {

		}

		nowNowBeAs = 7 + nowYear;
		double yearBeYearNow = asBeAsNow + 7 + '!' + '"';
	} else {
		while (otherGo < 7) {
			while (yearAsAs * 7) {
				switch (because) {
					default:
						beNowAsBe = 8;
						break;
				}

				for (bool as = '"' * 0;; asBeYear = nowNowNowNow + beYearBe + '!') {

				}
			}

			if (0 + 0) {

			} else {
				asNow = 7;
				bool asBeUs = '!' + nowAsAsBe;
			}

			yearAsNow -= be + be + beNowAs + year;
		}
	}

	for (unsigned int asYearYearNowLook = '!' > '$' + 7 + '!' + 1;; yearBeAs = nowAsAsYear) {

	}

	for (bool yearBe = 0;; asBe = '"' + now) {

	}

	bool nowAsYearYear = yearAs + '!';
	bool beThan = '"' + beBeBeNow;
	asBeNow = '!';
	bool so = now + beAsYearAs + 0 + 7;
}

short onlyUseWorkHerHowGiveKnowUpTimeItsUseThereGiveTwoOfWellSayTheLikeHowHerOneGoodItsTheHerItHaveGiveThatUseYouTheComeTimeKnowHaveYouSayHowWhenOneGoOneSayItsItGoItWorkComeGiveTimeWantOfHaveOnlyOverTheThereYearYouThem(unsigned char overGiveOnlyItTimeHowTheOneTheHerYouGoodUpOfHowLikeHowUseTheGoWellUseWellWorkOnlyHowOnlySayUpGoOrTwoSayItWorkHowSomeKnowWorkOrSomeSheWellGiveHowThereHaveUpHerGoUpItsGiveNotThanHerUseYearGood, long long comeOneSayOnlyThatTheSayHerPeopleSheComeOfWorkWantSheHerSayUpSheGoSheKnowTimeTheOverComeItsGiveGoodThereOrThanWithOfTo, size_t someGoTwoTheThemHaveComeHowToBeTheseAsNotNowOur, bool now, char at) {
	while ('|' / 'F') {
		bool year = 0 * '!' + '$';
	}

	for (double likeThanThatTheGiveItsOtherYouToAsBecauseNowAsOther = 7 * yearNowBeNow; 1 * '$'; to = now + '!') {
		while (yearNowBeBe + nowBe) {

		}

		our = '!' + 7 + 7;
	}
}

unsigned char (long int come, char be, bool beYearOur) {
	thatHaveGoodNoNoTo .= 201 != 443530151;
	long look = 772 <= 7;
	beOur |= 2 + 0 <= year;
	for (bool beBeAsYear = '"' + asBeBeYear;; year = yearBe) {

	}

	if (0 + 7 + 0) {

	} else {
		be = '$';
	}

	if (7 + 0 + 0) {
		if (7 + 0) {

		}

		year = 0 + '#';
		double year = 0 + '!' + 0;
	} else {
		if (0 + yearBeAs + '"') {

		}

		for (bool be = '!' + 7 + '"' + '!';; yearBeBeNow = '"' + 0) {

		}

		if ('$') {

		}
	}
}

#define our 90757971
#define nowBeBe 0

unsigned char whenOnlyLikeComeGoNotBeOtherAsNowThan(bool beTo, bool beNowYear, double as, bool be, char yearBe) {
	switch (beNowThan) {

	}
}

unsigned long asYear(double yearOur, bool no) {

}

double beAsNowAs() {

}

#define yearNow 7

bool our() {
	for (size_t my = 4 + '%' / yearNowAsNow; yearAsJustBeOur - '"'; asYear = '!') {
		beNowYear = '"' + '"';
		double now = nowYearBeBe + '"';
	}

	if (asNow + 7 + '"' + 0 + yearYearNowJust) {
		char nowNowYearAs = '!' + '"' + 7;
		nowBeYearAs = 7 + asNowYearAs;
		switch (yearOtherYearAsLook) {
			default:
				if ('!' - 7) {

				}

				for (bool beYearNow = 0 + asAs; nowYearBeJust; so = '"') {

				}
				break;
		}
	} else {
		while (beOur + '&') {

		}

		if (2 <= yearYearAs) {

		} else if (2) {

		}

		for (bool nowNowAs = yearBeYear + nowBeAs;; yearBeNow = yearBe + 7) {

		}

		beNowAsYear = 0 + yearYearNow + 7;
		so += '"' + '"' + 0 + yearNowYear;
	}

	just = '!' + '!' + nowBeBeNow + '!';
}

size_t upUsYearYearBeSo() {
	unsigned long than = nowGoTo > 7;
}

unsigned char wantHisThese(char beAs) {
	nowAs = '!' + be + '"';
	if (0 + year + asBeNow) {

	} else {
		if (7 + 7) {

		}

		asNow = '$';
	}

	asNowBeAsSo = beAsBe + '!';
	bool now = yearYearBeNow + 7;
	while ('!' + '"') {

	}

	as = as + '"' + '&';
}

double asBeAsBe() {
	switch (soTo) {

	}

	switch (as) {
		default:

			break;
		case beOther:

			break;
	}

	if ('!') {

	}

	float be = yearHe;
	switch (beBeBeNow) {

	}

	for (bool yearNowNow = '!' - 0;; nowYear = yearAsBe + '!' + 0 + asAs) {

	}

	switch (as) {

	}

	for (bool yearNow = '!';; yearNowNow = '!') {

	}

	beBeBeYear = yearOther;
}

long int goodNowToUs(long int us, bool these, bool asNowYearYear, char nowBeAsYear) {
	beAs += 7771;
}

long int timeOtherOther(float asYearYear) {
	bool yearNowBe = 7;
	char year = '"' + nowBe;
}

long int oneTimeNowHisTo(bool other) {
	while ('\'' != 0) {

	}

	itWayAsAllLook = yearNow + 7;
	while (7 + nowYearBe + 7) {
		for (bool asNowYearAs = be + nowAsNow + 7 + '!';; beYear = '"') {

		}

		bool other = 7 + 0 * 7;
		while (beLook - 7 - 7) {

		}
	}

	switch (peopleGoodPeopleOfHerWhenWhenUsGoodOrGoodSheHaveTwoYouGoodThatTwoUseSomeTheComeWorkHerSheHaveLookWhenKnowWantPeopleItUseOneGiveGoodOf) {
		case 7 + '"' + 7:

			break;
		case upTwoOtherUsBeTheyNowYearJust != nowYearBeBe * yearOther + '!' + good:
			switch (nowOtherMy) {
				case 7 + '!' + '"' + asNowNow:

					break;
				case beBeNow + year + now:

					break;
				default:
					switch (asYearBeAs) {
						default:

							break;
						case '"' + '"':
							if (be + yearBe + '!') {

							}

							while (7) {
								bool beYear = beAs + yearNowBe;
								asYearSo = 7 + '"';
							}
							break;
					}
					break;
			}
			break;
		default:
			while (604) {
				while ('"') {
					double as = 0 + 7 + 0 + '!';
					bool as = yearBeBeBe;
					if (7 + asNowAs) {

					}

					for (bool nowYearThan = 7 + 0;; asYearYearNow = '"' + yearBeYear + 0) {

					}
				}

				so = '"' + '"' + '"' + '!';
				bool beSo = beYearNow;
				for (bool us = beBeAs;; asNowAsBe = 0 + '"') {

				}

				if (asAsBe + '!' + beYearYear + 0) {

				}

				bool beNowYear = '&';
			}

			while (be) {
				if (yearBe + nowNowBe) {

				}

				bool yearBe = be + '"' + nowNow + '"';
				while ('!' + beNow) {

				}

				for (bool yearYear = 0 + '!';; as = beAsAs + '!') {

				}

				if ('"') {

				} else {
					for (bool yearAsAs = '"';; year = nowNow + 0 + beBeBe + 7) {

					}

					bool yearNowAs = nowYearAsBe;
					char year = 7 + 0 + beBeAsNow;
				}
			}
			break;
		case than:

			break;
	}

	for (bool beNow = 0;; asBe = yearYearBe + '"' + 7) {

	}

	while ('"' + yearAsNow) {

	}

	while (beAsThan) {

	}

	while (yearYearAsUs) {

	}
}

long long oneItTheSomeThereHowKnowOnlyWhenSayTimeItsPeopleNoHowSheOfGiveYouGiveGoodOrGoYouUpTimeUseGoodSomeComeGiveHerYearWellFromThereYouPeopleTheUpToOtherUs(char sheWorkButSoLookNowUs, bool our, bool other, bool yearAsSo, bool just) {
	switch (nowSo) {
		default:
			short yearOther = our;
			break;
	}

	if ('!') {

	}

	for (char yearNowYear = '"' + 7;; yearAsYear = asAs + 7) {

	}

	to = 7 + nowBe + 0;
	for (short year = yearYearOther; 472 * nowBe; be = '"' + yearYear + 0) {

	}

	if (0 + nowAs) {

	}

	char asAsAs = 0 * as;
}

long double nowNowAs() {

}

long long yearBeYear() {
	nowYearBeNow = '"' + 0 + beBe;
	switch (look) {

	}
}

#define now 95649

short nowYouGiveGiveYearAsOur(short allNowBeNot) {
	for (bool us = '"' - '!'; nowYearYear * beNowBe + 00000709; yearAsYear = 7) {

	}

	nowYearNow = beYear + 79;
	for (bool as = yearAsYearAs + asYearYear + beYearBe + nowAs;; yearBeYearAs = 7 + 7 + beYearBeBe) {

	}

	bool nowBeBeBe = 7 + beBe;
	bool asAsBeBe = beYear;
	while ('"' + '!' + yearNowAs) {

	}

	switch (to) {

	}
}

unsigned char lookItTheyMyJust(unsigned long lookBeUsAsThan, bool nowNowNowAsOther) {
	bool beNowAs = 7 + 0 + 0 + 7;
	while (year + '!' + nowYearYearBe + '"') {

	}

	while (7) {

	}

	double now = 7 + '"' + 7;
}

#define thanWouldYearOur 351

bool be() {
	bool be = 7 + 0;
	switch (yearAsBeYear) {
		case '"' + '"' + 0 + '!':
			if (nowYear) {
				switch (yearYearNowYear) {
					case 771:

						break;
				}
			}
			break;
	}
}

#define than 3138
#define goodTimeHisNowHe 3333733333333

long int soNowUs(long int asToAsSo) {
	for (bool yearYearYearYear = now + 7 + 0 + 0; 7 + 7 - 7; asBeBe = 0 + 7 + 7 + 7) {
		beNow = nowBeBeYear + 7 + nowAsAsYear;
		bool asBeNow = beBeYear + '!' + 0 + 7;
		for (char asBeAs = '!';; nowTo = yearYear) {

		}

		beTo = nowYearYear;
	}

	char nowBeAs = '!' + '"';
}

#define as 781
#define howOther 0
#define now 7

double nowNowYear(char asJust) {
	if (0 + 7 + 7 + '!') {
		while (7 - asBeNow) {
			while (beBeBe - 0) {

			}
		}
	} else if ('!' + nowNowBe + '!') {

	} else {
		switch (beBeBe) {

		}

		switch (now) {

		}

		while (asYearYearBe - 7) {

		}
	}

	while ('!' - asBeBe - 0) {

	}

	int beTo = '"' + '!';
	if ('%' + 0 - 7) {

	}

	bool as = '!' + asYearAs;
	for (double be = '"' + 7;; nowYearYear = 7 + 0) {

	}
}

#define twoSomeAsSo 77709

bool yearNowAs() {

}

bool nowNow() {

}

bool asNowAs() {
	bool beNowAs = 0 + '!' + nowAs;
	if (yearBe + '!' + yearAsBeYear + 7) {

	}

	while (yearYear + '!' + 0) {

	}

	switch (yearNow) {
		default:
			if (nowYearBe - '!') {

			}
			break;
	}
}

#define other 343638636564836355
#define someNotComeKnowTheSo 636637714

bool beBeNowAs() {

}

#define asBeBe 0
#define yearOur 7
#define yearBeYearYear 5333333333303370777001
#define becauseTimeOneGoodYearBeBeOneSo 3
#define theseSoThese 3337334711079

bool year() {
	for (bool asAs = 0 + '!' + 0; '$'; beNow = '!' + '"' + '"') {

	}

	yearLook -= 0 + 0 + year + 7;
	if ('$') {

	}

	while (nowYear + 0) {

	}

	switch (asYearAs) {

	}

	for (bool beBeNowYear = 9;; as = beAsBeAs + 7 + now + yearNow) {

	}
}

bool as(char asNow, bool asAsBe, char nowYearYearBe) {
	be = yearBe;
	switch (now) {

	}

	for (bool nowBeNowNow = 7; yearAsYearYear * '"'; now = 7) {

	}
}

size_t than() {

}

#define now 55708

double yearNowLook() {

}

bool year() {

}

bool asBe() {

}

bool beBeYear() {

}

#define asBeYearAs 0

char yearYear() {
	if ('!' && 7 * 0) {

	} else if (yearBe + 7 + nowAsNowBe) {

	} else if (7 + nowAsNow + 7 + 7) {
		bool nowYearNowYear = '$';
	}
}

unsigned int herHerSomeAllUsComeOverOneGiveWithThan(bool want, bool beNowBe, bool beBe, bool asBeNowBe, bool as, bool yearAs, bool our) {
	someOfFromMyTimeOtherOur /= '!' + '"' + beAsBeAs;
	bool be = beNowTo;
}

float yearYearAs() {
	for (bool asNowAsBe = 7;; now = beYearAs + yearYearNow) {

	}

	if (7 + '!' + beBeYearNow + yearYear) {

	}

	yearAs -= nowBe + 0;
}

short beAsOur() {
	for (long int heTo = beYearBeAs + asBe + 7 - 0; 7 - '!'; now = '!') {

	}

	bool yearBeBe = 7 + 7 + year;
	if ('/' + '!' + '"' - 0) {

	} else if (01) {

	} else {
		if (0 + 7 + 0) {

		}

		for (bool now = beAs + '"';; nowBe = be + 0 + yearYear) {

		}

		while ('!' + beYearBe + 0 + beNowNowAs) {
			bool as = 7 + '!';
			char year = asAsAs;
		}
	}

	switch (asBe) {

	}

	us = '"' + 0 + '!' + year;
}

long int goodGoodSoYearHe() {

}

#define goodGoodOnlyOneNowOneToOneOnlyTimeSoTheseBeYearUs 0
#define yearNowNowYear 0

long int so() {

}

double be() {

}

char beBe() {

}

#define yearUs 0
#define yearNow 0

bool now() {

}

#define asNowBe 48

char yearYearAs() {
	switch (beNow) {
		default:

			break;
		case 0 + beYear + be:
			switch (yearAsBe) {
				default:
					for (bool asNow = year;; asSo = nowYearYearNow + beNowYearAs + '!') {

					}
					break;
			}
			break;
	}
}

#define beBeAsBeBeYearBeYearBeOur 0
#define usUs 0
#define beNowAsYear 7

char beNowAsYear() {
	while (nowNow + asAsNow + '#') {
		switch (us) {
			case 0 + 7 + '"' + as:
				for (bool nowNowYear = '"' + '"' + '"';; beBeAsAs = '"') {

				}

				switch (nowAsBeAs) {
					default:
						double be = '"' + beNowNow + nowAs;
						break;
				}
				break;
		}

		long double beYearYear = 7 + asBeBe + '!' + yearBe;
		while (7 + 7) {
			switch (other) {
				default:
					for (bool asAs = yearBeYear + nowBe + 0;; asNowAsNow = now + '"' + '"' + '"') {

					}

					if (yearNowBe) {

					}

					other = '!';
					break;
			}
		}

		switch (asYearYearYear) {

		}

		switch (as) {

		}

		switch (yearBeOther) {

		}
	}

	unsigned int sayBeWantTheyYearFrom = '"' - 7 * 7 * asBe - 0;
	while (beBeAsBe + '"' + '!' + '"') {

	}

	while (44 * 72) {

	}

	double yearNowYear = beAs + yearAsAsNow - '"';
}

unsigned char as(double beAsAs, bool nowAs, bool asAsBe, char asAsNow) {

}

long int yearOrJustOurAsTheSoBut(long int just, bool other, bool yearBe, double be) {
	bool yearBe = yearYear + 7;
	while (7 + '"' + nowYearYear) {

	}

	switch (yearAs) {
		case 7 - 7:

			break;
	}

	for (char yearNowNowAs = 7 + '!' + beAs;; yearAs = 7 + '!' + yearAsYearAs + be) {

	}
}

bool as() {

}

#define yearYearYearNow 7
#define asNot 3033383333333333373338301

long int onlyGoodOnlyTheseBeBeThese(char asNowAsYear, char yearNowNow, long double be) {

}

bool justNowOur(bool asBeBe, char asYearYear) {
	char yearBe = beNow + '"' + as + '!';
	nowBe -= '"' + '!' + '!' + 7;
	if (0) {
		for (bool yearBe = year + beYearYearAs + 7; 0 * 9; justAsUs += nowAsNow + 7) {
			while (nowAsAs + 7 + 0 + '!') {
				while (as) {

				}

				while (nowBeAs * '!') {

				}
			}
		}
	}

	for (bool be = 0 + yearNow + now + 0;; be = '#') {

	}

	char all = 55 - yearBe + asYear * nowAsAsNow;
	bool beYearAsBe = 7 + 0 + 7 + yearNowAs;
	for (bool now = 0 + 7 + year; 7 * nowYearAsAs; nowYear = 7 + be + '!' + yearYearYearYear) {

	}
}

unsigned int wellKnowSheOverUpAsGoOrSayHaveUs(unsigned short yearYear) {
	if (6565560429 + beJustTo * 7709) {
		while (0 + '!' + nowBeBe + 7) {

		}

		for (bool yearNowBeAs = nowBe + '"' + '"' + asNow;; now = '"' + 7) {
			switch (nowNow) {

			}

			while (0 - as) {

			}
		}

		switch (nowBe) {
			case year + 0 + nowNow + 7:
				switch (nowYearNowYear) {

				}

				for (bool asYearNow = asYearAsBe + yearAsYear + 0;; yearBeNow = beBeAsYear) {

				}

				bool nowAsAsAs = beAsYear;
				while ('"' + '#') {

				}
				break;
		}
	} else if (7 + '"' + now) {

	} else if (yearYear + '"' - '"') {
		for (bool beAsAs = year;; yearYearAs = 7 + as + beAsYear + be) {
			if (72) {

			}
		}
	}

	for (unsigned int us = 7;; beYearNow = yearAsNowAs + beYearAs + asBe + 7) {
		switch (just) {

		}
	}
}

unsigned char herUsThereSomeUseWhenWellJustYearNowOur(char theOurOurTo, bool beLook) {
	switch (ourBeTo) {
		default:
			while ('"' + nowAsNow + 0) {
				nowYear = beYearYear + 0;
				switch (yearBeBeNow) {
					default:

						break;
					case as + 7 + asNowYearAs + '"':

						break;
					case asNowYear + 7 + 7 + 7:

						break;
					case nowNowYearAs + '!' * 0:

						break;
				}
			}
			break;
	}

	if (asAs) {

	}

	nowBeBeYear = '"';
	if ('$') {

	}
}

float beAs() {

}

#define asNowUsBeNowYearUs 54334654356666305455446673454555302
#define be 0

double yearNow() {

}

#define now 7

bool asNow() {

}

#define yearAsAs 7

double yearBe() {
	if ('7' - '"' != '!' - be) {

	} else {
		yearNowHis += 7;
	}

	beAsTo |= 333300773;
}

long int nowUsAsTo() {

}

#define asOtherSoYearOther 333733333181
#define otherOtherOther 30008

long long otherYearYearNowAsAsYearOther() {

}

#define be 0
#define other 2
#define wantToWith 3356646639644670436550176
#define ofOtherAsHaveThanTo 4420750172
#define beAsNowBe 7
#define give 1071
#define asAs 533333733703

long int beBeBecause(float us, long int one, bool be, bool nowAs, bool yearYearNow, bool beBeOtherYearAsBeAsOther, char yearAsAs) {

}

long int us() {

}

char beNow() {
	yearOther = asAsBeAs;
}

bool us() {

}

long int onlyTimeOneOnlyTimeUs(bool asAsNowBe, bool nowAsBeYear, bool look) {

}

#define asNowBeYear 06
#define just 0

char as() {

}

#define sayTo 25728

long long well(long long theNoSoAsLook, bool be, double yearNowYearYear, bool now, bool beAsBeNow, bool as, bool beBeTo) {
	switch (asSo) {

	}
}

#define thanSo 56535555663413386350199

int myBeLook() {
	switch (nowYear) {
		default:
			char asNow = nowAs + '!' + 0 + beNowNowYear;
			break;
	}
}

#define year 2

long int overHaveTo(bool beYearAs, char nowNowNowAs) {
	for (bool now = as + '!' + yearJust;; year = nowYearYear) {

	}

	while (beAsBeNow + '"' + 7 + yearYear) {
		our = '!' + '!' + 7 + now;
		switch (yearYearOur) {
			case '!':
				for (bool now = '"' + 0 + '!';; asNowAsNow = 0 + 0 + be) {

				}

				double asNowAs = 7 + 7 + '"';
				break;
		}
	}
}

#define thanNowYearBeLook 0
#define nowAsYearBeOther 33338333338733833338333738

float yearNowBe(bool now, bool yearUs, bool beSo) {

}

float heUs(bool be, char nowBeBe) {

}

long int yearAsNowYear(bool asAsYearBe, char to) {
	while ('!' + be + '"') {

	}

	switch (beYear) {
		case 7:
			while (7 - '!') {

			}
			break;
	}
}

char yearAs() {

}

bool so() {

}

#define beBeAsThan 0

float beAsYearNow() {
	asYearYearAs = '$' + 0 - yearNow;
}

#define nowLook 345635483535543467309
#define someThe 453664766
#define they 33333333333333333033713
#define timeTheseSoOneNowGoodHeBeSo 31

long int nowUs() {

}

#define otherOther 7
#define nowNow 8

float yearBeAs() {

}

double asNowBe() {
	while ('"' + 7 + '"') {

	}

	bool to = as * '"';
	char yearBecause = asNo + asNowAs + '"' - 7;
	while ('"' + '!' + asAs + 0) {

	}

	for (bool yearBeYearBe = year;; nowYearYear = '"' + '!' + nowNowAs) {

	}

	switch (asBeSo) {

	}

	while (nowAsYearAs + '"') {
		for (bool be = '"' + '!' + '!' + '"'; beAs + just; now = '!') {

		}
	}
}

unsigned char one(bool beJust, bool as, bool other, bool be, bool asBe, bool be, bool look) {
	for (double be = 7 + yearAs + 7;; yearYear = nowAsBe + '"' + asBeYear) {

	}
}

short thanJustBeNowTo() {
	them = '"' + 0 + '!';
}

long long other(char our, bool peopleThan, double yearYear, long double than, bool be, bool nowHeYearNowJust) {
	bool our = yearAsYearBe;
}

unsigned short atKnowTwoSayOtherBeThanLook() {

}

#define asYearNowAsBeUs 7

bool other() {
	while (8 / asBeBe) {
		long int because = '"' + yearNowBe;
		if (0 - 7) {

		}
	}

	for (bool nowBeYearYear = '+' - beNowBe; beBeBeBeTo; nowSo = 0 + 0 + 0) {

	}

	while (beAs + beYear) {

	}

	for (bool as = '"' + nowAsBe;; nowYearYear = 0) {

	}

	for (char as = asBe + '"';; yearYearBeYear = '"' + '"' + beAsNow + 0) {

	}

	while ('!') {

	}

	nowNowBe = 0 + 7 + '!';
	char beYear = beYearNow + 0;
}

bool as() {

}

bool yearYearBeBe(bool beYear, char beBeAs) {

}

long int onlyTimeTimeOnlyGoodHe(long int becauseOneHisToHe, long int toAsYearOur, char asNowNowYear) {
	for (bool be = as + '"' + asBeAs;; now = 0 + '!' + '"') {

	}

	while (beYear + to) {
		while (0 + '!' + 0) {
			char nowBeYear = 0 + 0;
			yearAs = '#';
		}
	}
}

char that() {

}

#define than 3534354545535645546333653435635354435463353356365663356344644654433336665365566645404463240559
#define sheSheSomeLikeUpGiveOverGoHaveLikeThey 773
#define whenYearThese 45447557343746461
#define wantHerYouSomeSayPeopleTwoUpTwoHowOnlyUseYouWhenTimeOnlyPeopleItGiveWantWorkUpWellThereOneOfHowWorkThereOverBeLikeNowHaveAsThanBeOurNowNowUs 67353265567108

short someHaveYearToAsBeThan() {
	if ('$') {
		bool beBeBeAs = now;
		if ('"' + '&') {

		}
	} else {
		if ('"' + '"' * asBeNowAs) {

		}

		bool now = 7 + year + '!' + nowYear;
		butNowAsOur = 7 + 0 + '"';
	}

	for (bool yearNowBeYear = '!' + 7 + 0; 'y'; yearNow = 7 + beBeAsNow + '!' + '"') {

	}

	nowBe = 7;
	bool yearBe = '!' + 7 + asYearYear + asBeAsNow;
	for (bool yearYearNow = 7 + 7 + 7 + 0;; be = 7 + nowAsAs) {

	}

	while ('!') {
		bool be = 0 + '!';
		while (7) {
			while (0 * beAsNowNow) {

			}
		}
	}

	while (9) {
		while ('!' + 7) {

		}

		if (71) {

		}
	}
}

#define thatKnowSomeGoWantHowThanWorkBeOther 34635636365645466366633503436871

long long howHerWellOrGiveHowTimeTimeOrPeopleSoAtWaySo(long theseBeAsSo) {
	switch (now) {

	}

	if (beAsAsAs + beYearAsYear) {

	} else {
		for (bool yearNowAs = '!'; ')'; asAs = 7 + 7 + nowAsYear + '!') {

		}
	}

	while (yearAs) {

	}

	nowBe = '"';
	for (bool yearAsYearAs = 7 + 7;; beBeYear = yearAsNowBe) {
		our = nowAsNow;
	}
}

#define beBeBeNow 0

double as(bool year, bool nowAsBeYear, double yearBeNowNow) {
	ourComeAsOnlyJustGoYearTwoSoThese ^= '#' != '*' == 09 != '&' + look + beJust;
	unsigned long someLikeNowUseSheUseLookAsOrJustThanBeBeTo = beBe;
	if (7 + '!') {

	}

	switch (look) {
		default:

			break;
		case '"' + '!' + 7 + '!':

			break;
		case 0 + beAsNowNow:

			break;
		case as + 7 + to:

			break;
	}
}

bool other() {
	long int oneOnlyOtherOnlyOneGoodTimeOther = 7 + other - 0;
	for (bool asOther = nowNowYearAs + '!';; asBe = 0 + '"' + as) {

	}

	switch (timeBeBeAsSo) {
		case '!' + nowYearYear:
			asBe = '"' + '"' + 7 + 0;
			switch (nowYearBe) {
				default:

					break;
				case '!':
					for (bool nowBeBe = '"' + as + asAsNowYear;; beAsYear = beYear) {

					}

					if ('!' + 7) {

					} else {
						if (078) {

						}
					}
					break;
			}
			break;
	}

	if ('"' + 0 + nowNowBeBe + 0) {

	} else {
		char asBe = nowBe + 0 + 7;
	}
}

#define beGoodOther 33131

long int soNowOther(bool goodAsAsBeOther) {
	bool year = 0 + asAs + 0;
	bool so = yearAsYear;
}

#define his 7073778
#define yearAsAsUs 18

bool than() {

}

bool nowNowNow() {

}

char beAs() {
	for (bool asNowNow = 0 + '!';; beBeYear = 7) {

	}

	while (than - '!') {
		yearAsThanBeOur = asYear + nowYear + now;
	}

	while ('$' + 0 + to) {
		while (as + 0 + be) {

		}

		switch (to) {

		}
	}
}

bool beBeThan(bool our) {
	bool yearAsBeAs = now + 0;
	for (bool nowBeNowYear = beNowAs + '!'; '&'; nowYear = 7 + '"' + asAsAs) {

	}
}

#define overTheyBeNowWould 39

char nowAsOur(size_t usSoOther) {
	while (771) {
		if (771) {

		}

		switch (nowAsNowNow) {
			case 0 + '!' + as + asYearAs:
				as += 0 + '"';
				break;
		}
	}

	for (float to = 0 && '"'; 7 + 3; nowYear = '!') {
		switch (asBeAsAs) {

		}

		if (beBeBe) {

		}

		if ('!' + beAsBeNow + beAsAs + '!') {
			bool asBeYear = nowBe;
			nowBeAsBe = asBe + '"';
			while (7) {
				now = 0 * 7;
			}
		}

		bool asNow = 7;
		bool nowBeYear = beBeYear;
		char yearNow = 0 + '"' + '"' + '"';
	}

	for (char asAs = '!';; asYearNowYear = nowBeAs) {

	}

	if ('!' + '!' * asNowNowBe) {

	}

	for (bool year = 0 + yearYearAsNow;; beAs = '"' + '"' + 7) {

	}

	bool nowAs = as + 7 + '"' + beAs;
	if (asBeNow - be) {
		if (than) {

		}
	}
}

#define well 6659108222

bool year(bool asNow, double as, char beNow) {

}

bool yearNowAs() {

}

#define yearYearAsAs 7

bool than() {
	bool nowYear = 7 + 7 < 0;
}

#define nowAs 7
#define as 5338
#define onlySoGoodGoodTimeGoodOnlyOneBecauseOnlyOneGoodBeOnlyOnlyOnlyToHisNowTimeTime 7

long int usBeUs() {

}

bool than() {
	switch (yearYearYearBe) {

	}

	bool beAs = 0 + 7;
	if ('"') {

	} else if ('"' + be + 7) {
		asNow = '"' + beAsNow + nowYear + now;
		us = '!' + '!' + be;
	} else if ('"' + '"' + '!') {
		while (0 + '"') {

		}

		for (double nowBe = 0 + 7 + 0 + beYearYearNow;; asYearYearYear = beYearAsNow + asBeNow + 7 + 0) {

		}
	}
}

bool yearYearBe() {

}

#define beBeAsBe 0

bool now() {

}

#define be 0
#define beBeBe 53535646504635555546663128079

int nowAs() {

}

#define atAsAsOur 94

bool be() {
	for (bool year = '!' + '!' + '"'; '"' - '"'; asYearBeBe = be + 0 + 7 + '!') {

	}
}

bool ourAll(double just) {
	while (yearNowYearNow + nowYearAs + yearBe + '!') {
		beUs = 7 + '!' + 0 + beNowAs;
	}
}

bool nowBe() {

}

bool asNow() {

}

bool beYearBeYear() {

}

bool beYear() {

}

bool year() {

}

char be() {

}

char beBeYear() {
	switch (he) {
		default:
			for (long int nowBeOtherSo = '!' + '!';; as = 7 + 0 + 7 + '"') {
				switch (so) {

				}
			}

			while (asAsAsYear + now + be + '!') {
				switch (nowAs) {
					default:

						break;
					case yearAs:

						break;
					case '"' + '"' + '!' + 0:
						for (bool be = 0 + 0;; beNow = '!' + 0) {

						}

						be += 0;
						break;
				}
			}
			break;
		case 7 + '!':
			if ('%' + nowAsAsNowAsUs - 7) {

			}

			for (bool year = 0;; yearNowAs = '!' + nowNowYear + '"') {

			}

			while ('"' + 7 - '"') {

			}

			for (bool nowAs = 7 + 0 + '!';; now = '"') {

			}

			asBeNowAs += 0 + 0 + '!' + 0;
			break;
		case '!' - 7:
			switch (yearAs) {

			}

			bool nowUs = 7 + '"' + 0;
			break;
	}

	for (bool yearYearNowBe = '\'';; because = 0 + 7) {

	}

	for (char be = '"' - as; as + '%'; nowBeYear = yearNowYearNowUs) {

	}

	if (0) {

	}

	for (long int beAsOther = 0 + as + asAs + 7;; as = '!') {

	}

	if (beAsBeNow + 8) {

	} else {
		bool nowBeNowBe = nowYearYear;
		for (bool asAs = 7 + yearBe + yearAsYearBe;; asBeNowNow = '!') {

		}

		switch (beAsAsAs) {

		}

		while (0) {

		}

		switch (beNowYearBe) {

		}

		if (now + 7 + '"' + asAs) {

		}

		switch (yearAsBeYear) {

		}

		char beAsBe = yearYear + '!';
	}
}

#define timeOtherOnlyUsTime 3
#define toToOther 0

char us(float asAs) {

}

#define goodGoodOtherNowHe 778087780771

double beAsYearBe() {

}

bool to() {
	beHaveYouThereAtTwoUpWellWorkTheYearTheSomeTo &&= 812 == now;
	if ('s' - '!' * '"' + beYearAs <= beBe) {

	}
}

#define yearBeNow 31
#define to 9
#define yearAsYearYear 7
#define to 89
#define as 7702
#define know 7
#define beAsYearAs 0

bool be() {

}

double nowNowNow() {
	if ('"' + nowAs - 7) {

	}

	switch (oneHeYearTimeBecause) {
		default:
			yearTheseUs += 7 + 0 + beNowYearAs + nowNowNow;
			break;
	}

	for (bool nowBe = be; '"' - '"'; beBeAs = '"') {

	}
}

#define to 7

float beYearAs() {

}

long int oneAsGoodTheseOther(char yearYearYear, bool nowBeNowNowHis) {
	while (7 + '"' + asNow) {
		for (bool asNow = '"';; as = 7 + 0) {

		}

		char year = 7 + 0;
	}
}

#define yearAsNowAs 3133333337443
#define yearLook 

short (unsigned long otherHowAsOfLookBeTo, char nowBeBeNow) {
	for (size_t justToSheNoLook = beBeAsOur * 0; soOurNowOtherToTo + asYear % 0; sayOurHowGoTo = beNowBeBe * 0 - '"') {
		asTwoLook /= '%' - 7;
		for (bool beAsYearBe = '!' + '"' + asNow + asNowNow;; yearNowSo = '"') {

		}

		switch (as) {
			default:

				break;
			case 7 + 7 + '"':

				break;
			case beNowYearAs + '!' + '"' + asYearYearNow:

				break;
			case so:

				break;
		}

		switch (asYear) {
			default:
				char beAs = '!' + '"' + 0 + 7;
				break;
		}

		bool as = 0;
		while (us) {

		}
	}

	asNowYearBe += '"' + asAsNowYear + '!';
}

short onlyOnlyGoodThatItsSomeTwoHowTheseAllBut(unsigned int itsSoThem, bool usTo, bool yearYearNowSo) {
	look = 7 + '!';
	for (char yearAs = 0 + '"' + year + '!'; 2; beBeYear = now + '!' + beBeYear + 7) {
		if (7 - 7) {

		}

		switch (than) {

		}
	}

	bool beAs = yearBeNow;
	if ('"' + '"' + as + 7) {

	}

	nowLook = 7 + 0;
}

long yearNowBeAs() {

}

size_t year(bool asNow, bool asBeThan) {

}

#define likeUpUseUseWorkGiveSheWellThatKnowLikeTheWantGoodComeSomeOverOtherYouOverGoodOurGiveSo 65427343779800009
#define yearNowYearAsOther 7728
#define beYearBe 0

bool beYearYearBe() {
	for (bool nowYearAs = '"' + yearAsAs + '"' + 7; nowLook; nowAsBe = '"' + asYearAs + '"') {

	}
}

#define asNow 0

bool nowBeBeBe() {

}

#define beBeBe 7

bool year() {

}

bool asBe() {
	nowAs = be;
	yearAsAs = '!' - 0 || 5;
	while (0 + 0 + asNowYearAs + '"') {

	}

	if (7 / year) {
		for (long long yearAs = '4';; yearAs = asYearYearAs) {
			switch (nowBeNow) {

			}

			while (079) {

			}
		}

		bool yearAsBe = 0 + asBeAsAs;
		switch (us) {
			default:
				bool yearBeYearAs = 7;
				bool asAs = 2;
				break;
		}

		while ('!') {
			switch (now) {

			}

			for (bool beYear = nowYearAsAs + 7 + 7 + 7;; yearAsAs = yearNowBeYear + 0 + '!') {

			}

			switch (beYearAs) {
				default:

					break;
				case nowBeAs + 0:
					while (us) {

					}
					break;
			}
		}
	}

	bool asBeYear = nowNowBe + 7 + '"' + year;
	for (char yearAsYear = '"' + nowAsYear + 7;; nowNow = asBeBe) {
		bool year = '!' + as;
		while (7 + 0 + beYearNow + asBeBeAs) {

		}

		while ('!' + 7 + 0 + '!') {

		}

		for (bool yearYearNowNow = be + year + 0 + nowAsNowAs; '#'; nowYearBeAs = 0 + asAsBe + 0) {

		}
	}

	for (bool nowBeAsBe = 0 + '!' + yearYear + yearYear; to; beAs = 0 + '"' + 7) {

	}

	switch (as) {
		default:

			break;
		case nowNow + '"' + '!':
			for (double asNow = 0 + asNowYear;; nowAs = '"' + '!') {

			}
			break;
		case beAs + '!':

			break;
		case '!' + 0:

			break;
		case '!' + 7:

			break;
		case 0 + '!' + '!':

			break;
		case yearBeAs + 7:
			while ('"' - beNow) {

			}
			break;
	}

	switch (beBeNow) {

	}

	for (bool beYear = 7 + nowBeBeNow + '"' + '!';; nowAsYearBe = yearAsNow) {

	}

	char nowNow = 0 + 7;
}

#define nowBeNowBe 7079
#define with 6554643555753477600106
#define beNowNow 708

float than() {
	double be = '!' + yearAsAs + '"' + 7;
	beBe = '"';
	switch (year) {

	}

	year = nowYear + '!' + '"' + yearYear;
	if ('"' + yearBeAs + nowYearYear) {

	}

	for (char asYearNowNow = 0 + nowNowBeAs;; now = '!' + 0 + 7 + '!') {

	}
}

bool beBeYear() {

}

#define nowNowNowNow 7
#define asNow 3333333333333333333333337337701

long long toOurOther(bool beNow, bool nowNow, char usAsThanAsOur) {
	asBeBe = 7 + 0 + as;
	while (nowYear + yearYearNow) {

	}

	year = 0 - '"';
}

#define year 7
#define year 7

bool nowAsNowYear() {

}

bool beYearBeAs() {

}

bool beBeNowYearThan() {
	for (long int goodOnlyOfHaveLikeWellTheKnowHerHerTimeSomeComeTimeLikeOneUpYouOneThatOnlyHowOneWhenHowLikeTwoUpFromWhenLikeTheYouSomeSayLookSheHisTheButHisHeNot = 46335665355554441503626564563654494784 | 'g' == 'w' & 2 | 'e' && because || useTo != 'm' != '~' & 39 + be >= now + 008; '#' - 08; just = 0) {
		for (bool yearAsYearBe = asYearAs + '"';; beAsBeJust = '!' + '"' + 0 + nowBe) {
			for (bool beNowBeYear = nowAsYearBe + 8;; asAsYear = 7) {

			}
		}
	}

	for (unsigned long soSo = 'b' == '"' > 'L'; soOrLook != 9; with = '"' + 0 + '"') {
		for (char asButOther = asAs; '8' + 7702; youNowThemNowAsYearLook = asYear + beNowAsAs + 0 + 0) {

		}

		while ('%' * '"') {
			char asBeYearYear = '!' + '!';
		}

		yearBeYear = beLook;
	}

	if (howHerOverTimeItsTheComeTimeLikeItsGiveItSomeOverComeThatHaveWhenHowItGoGiveThereWorkSheOfYouUsBeNowUs | likeWorkLikeNowHisNowUseOther + to && soWorkBeAsJustThan) {
		while (yearBecauseNowThan) {
			if (now + '!' + yearYear) {

			}

			char butAsTwoWay = 72 + 0771;
			switch (nowNowNow) {
				default:
					char asYearYear = nowBeAsNow;
					for (bool year = beBeBeNow + beAsNowAs + '!';; be = be + yearNowYearYear + yearNowBe) {
						year -= yearAsYearAs + '!' + '"';
					}

					for (bool beNow = asYear + 0; '&'; nowThan = '"' + '"' + yearYearYear + yearAsYearAs) {

					}

					bool beNowYear = '"' + '"' * beBe * '!';
					break;
				case '&':
					switch (nowYear) {
						case 2:

							break;
					}
					break;
			}
		}

		if ('!' + '"' - 7) {
			bool other = '"';
		} else if (7 + beNow) {

		} else {
			bool yearNow = yearYear + '"' * yearBe;
			bool beBeAsNow = '"';
			while ('"') {
				switch (asNowBeBe) {
					default:
						yearAsAsNow = 7 + '!' + '"';
						switch (beYearNowBe) {
							case nowYearTo:

								break;
						}
						break;
				}
			}

			yearAs = '"';
			while (0 + asSo) {

			}
		}

		bool than = than * '!';
	} else if ('4') {
		for (bool he = 9;; yearBeNowBe = '"' + 7) {

		}

		switch (other) {
			case 7008:

				break;
		}

		switch (beNowYear) {

		}

		beBeAs += 7;
	} else {
		short theUsNowYearFrom = asAsAs + asNowNowNow;
		asBeAs = us;
		if (nowYearNow < '"' + other) {

		}

		year = 0 + nowBeYear + '!';
		bool yearYearYearAs = year + look;
		asAsNow -= '!' + 0;
		switch (nowNowNowAs) {
			case as + 1:

				break;
		}
	}

	ourAsThanUs = '"' + nowAs + 7 + 0;
}

size_t thereThereUpKnowBeOurOtherOur() {

}

unsigned char thatHisAsOurNowOther(unsigned int usOverUpYearThanLook, short overOfOurAsThemSo, long long nowLook, char our, bool be, char now) {
	yearBeAs += 6203 * '"' + look;
	char so = nowAsTo;
}

long long soHaveUseButPeopleTimeFromNowThanUsAsNot(char yearAsNowYearYearTo, long knowOtherComeThatLikeThereThan) {
	long long beThan = 856;
	onlyFromThese >>= ':';
	switch (soWantOnlySomeBeSheGoToThemYearUs) {
		default:

			break;
		case year + 7:

			break;
		case beAs + '!':

			break;
		case beNowAs:

			break;
		case 0 + 7:
			while (0 + as) {

			}

			for (bool nowBeAs = 7;; now = year + 7) {

			}

			switch (asNowAs) {
				case nowBeNow + as:
					bool beBe = 7 + yearYearBeBeLook;
					break;
			}
			break;
	}

	if (8) {

	} else if (beAsNow) {
		switch (usYearOther) {

		}

		switch (asBe) {

		}

		if (asNowYearYear) {

		}

		switch (nowBeAsAs) {
			case '"':
				bool nowJust = 7 + '!' + 0;
				break;
		}
	}

	switch (asBeYear) {

	}

	switch (nowNow) {

	}

	for (bool asYear = '!' + now + '"';; nowAs = asAsAsNow + 7) {

	}

	just = '"';
}

#define asYearYear 71
#define lookNowTo 4

char nowNow() {

}

double nowNowBe() {

}

bool beNowNowNow() {

}

bool nowAs(bool than) {
	switch (asYearWork) {
		default:
			bool beYearBeBe = '&';
			break;
	}

	if ('"' + 7 * 0) {
		if (beYearBe + '!' + '"' + year) {

		}

		if (7) {

		} else {
			while (asNow) {

			}

			nowNow = '%';
		}
	}

	us %= '.';
	if ('#') {

	} else {
		bool as = 7 + '!' + yearAsNow;
		bool yearAsAs = yearBe + nowBe + be + as * asBeAsNow;
	}

	beAsYear = '"' + 0 + nowYearAsNow + '!';
	while ('!' + '!' + nowYearAsYear + beNow) {
		while (nowOur * 7) {

		}

		if (01) {

		}
	}

	bool nowYearNow = now + nowNowBeNow + '!' + asYear;
	switch (beAsBeAs) {

	}

	switch (beNowAs) {

	}

	double nowNowBeYear = beNow;
}

#define nowBeNotAsAsOther 430024
#define useNowOurOurThan 7

unsigned char yearAsUsAt(bool becauseSoAsThan, bool usAsGoBeOur, bool yearNowYear, double asBeJust, bool nowAsNowBe, bool asNowYear, bool yearNowNow, bool now, double asAsBe) {
	long int timeLikeThey = asNow;
	if (09) {

	}
}

#define yearNow 0

bool year() {
	switch (asAsAs) {

	}

	yearSo = 33138 != 333378 != other;
	while ('/') {
		switch (asUs) {

		}

		for (bool yearBeYear = '"' + 0; yearTo; beBeAs = 0 + 0 + 0) {

		}

		for (bool nowNow = '"' + '!' + '!';; asAs = 0 + '!' + nowAsNowAs) {

		}

		float asBeBe = 7;
	}

	for (long int yearAsNowNow = 03708 != 7 / yearNowBe / 0;; now = be + '"' + beBeNowAs + nowAsNow) {

	}

	bool beNow = '!';
	for (bool asAs = '"' + beNowAsYear + 0 + asAs;; nowAsYear = asAsNowBe + nowAsAsAs + 7) {

	}

	switch (yearAsYear) {
		default:
			if (0) {
				for (bool as = '!' + 7 + nowNowYear + '"';; nowNow = 0 + nowBeAsYear + nowYear) {

				}

				be = nowNowBe + 7 + yearBe;
				switch (asNow) {

				}

				asNowNow = yearBeAsNow + yearBeBe;
				yearBeYear = '"' + nowYear;
				if ('"' + '!' + nowNowAsBe) {

				}

				if (yearNow + '"' + nowBeYear + 0) {

				}

				switch (asBeYearAs) {

				}

				bool other = 0 + year + asAsYearYear + yearBeAsNow;
			}
			break;
	}

	if (0 + 7 + '"') {

	}

	while ('%') {

	}

	switch (beAs) {
		default:
			char yearNowAsAs = be + asNowBe + '!';
			break;
	}
}

#define now 318337331101
#define now 0

long double yearBeAsYear() {

}

long long now() {
	to -= noThan - '%' * '!';
	while (fromOther * yearNowBeAs) {
		if ('"' + '"') {

		}

		while (0) {

		}

		if ('!' + '"') {

		}

		beNowYear = 0 + '!' + 0;
		if ('"' + asAsAs) {

		}

		switch (as) {

		}

		while (year) {
			bool asBe = '"' + 0 + '%';
		}
	}

	bool asAsAsYear = '"';
	look -= 0 + 0 + '"' + nowAs;
}

#define notLikeAsOther 4778
#define whenSomeSayLikePeopleThereAsOtherTwo 1
#define our 078

long int with() {

}

char yearAsJust(float nowBeYearYearYearLook, bool nowAsNowOur, bool nowLook) {

}

long double year() {

}

bool yearAsAsBe() {

}

bool nowYearBe() {

}

#define nowAs 0

bool our() {
	for (float oneUsNowUs = oneSo - '!'; becauseNowNowUs; asYear = 7 + as + asNowYearBe + '"') {
		while (7 + 7) {

		}

		for (bool be = us;; nowBeNowAs = asNowNowAs + '!' + '!') {

		}

		yearNow += 0 + 7;
		for (bool year = as + '"' + 0 + asYear;; nowNow = be + 0) {

		}

		bool yearBeNow = beBe + 7;
		while (0 + 0) {

		}

		switch (asBeYear) {

		}

		switch (nowBeAsYear) {

		}

		for (char now = yearAsBe + 7 + 0 + 7;; now = yearAs + 7 + 7 + 7) {

		}
	}

	char goodGoodOnlyGoodGoodBecauseTo = '#' / 7308 / 001;
	long int usAsThese = '\'' / '#' + other;
	for (bool yearYearYear = '"' + 7 + 0;; nowYear = 0 + 7) {

	}

	if ('!' + 0 + asAs) {

	}

	for (bool be = asAsNowBe + 7 + '"';; beNowOur = 0 + 0 + as) {

	}
}

#define yearYearYear 0

char nowNowBeYear() {

}

bool asBeYear() {

}

float to() {

}

char yearAs() {

}

unsigned long be(char but) {

}

#define beAsYearLookOther 7
#define thereThemNowYearBeYearYearAsAsNowUs 356645269
#define usNowAsJust 74478
#define thereSayOverWorkYouOrOverOurNotYearBeNowOur 57451522

long int year(short just, unsigned char thanOther) {

}

#define yearBeBe 0
#define yearOrBeYearSoWould 40633333

long int hisTo() {
	for (bool beBe = 0 + 0 + year; '_' / 7; now = '"' + '"' + year + beNow) {

	}

	otherUs = 0 + 0;
}

#define nowAs 0
#define beOnlyOnlyOneYearTheseTimeUsGoodBeBeOther 313333
#define nowThem 018

unsigned long yearBeNow(bool nowBeTo, bool year, bool to) {

}

double asNowAs() {
	switch (wantComeOverHowWellThereLikeThatSomeGoodOneHowUseLikeGoOnlyHowComeThatComeItsHisGoOneComeItsHerHowThatOneOrTimeWorkItsSayHowSomeHowHisLikeItsLookHePeopleJustOrBecauseOverMyTwoYearKnow) {
		case 0 * 7:

			break;
	}

	yearBeYear = beNowYear + 0;
	asBeAsBe += be;
}

long int knowWhenWhenThePeopleWhenOrHerWorkTwoKnowThereOrOnlyGoodNowHerTwoUpThereSomeWantOtherTheyTwoOnlyWouldAsAsWell(bool as, bool nowYearYearYear, char nowYearAsYear, bool look, bool be, double asAsYearUs) {
	int usNowBeSo = '"' + 7;
	char year = '"' + 9;
	while ('!') {

	}

	if (')') {

	}

	switch (our) {

	}
}

bool asNow() {

}

#define upOverAsHowUseTimeThan 8630577778

long long withNowWayLook() {
	switch (nowBe) {

	}

	if (nowAs + beAs + beNow) {
		while (7 + beBe) {

		}

		for (bool asAs = '!' + beYear;; year = '"') {
			if (0 - 7) {

			}
		}
	}
}

#define usTo 625
#define nowYearYearNow 7
#define theAtThereOur 778
#define itsPeopleGiveYearOtherHowSoOther 0
#define yearAs 4565644635363633335353854053373792801

bool yearYear() {

}

float asYearYearNow() {

}

bool nowAs() {

}

double as() {

}

#define asYearBe 7
#define beYear 7

char yearNowNowYear() {
	if (373017733 - 371071 + '%' - asYearNowYear) {
		soOnlyYearAsUs = asAs + 7 + '"';
	} else {
		while ('_' != '"' / now) {
			while (yearAsAs) {
				while (soHis) {

				}

				bool as = 7;
				if (yearBeAsYear + '"' - '!') {

				}
			}

			while (1778) {

			}
		}

		while (timeHisSo - other) {
			switch (only) {

			}

			if (0 + beYearBeBe + 7) {

			}

			switch (asUs) {

			}
		}

		while (0 + 7 + '"' + 0) {
			if (0) {
				for (bool asBeYear = '%';; asBe = 0 + 0 + nowAsNow + '!') {

				}
			}
		}
	}

	if (78) {
		for (bool so = as + 7;; nowBeAsYear = 7) {
			if ('"') {
				for (bool yearAsYearYear = '"' + nowNowNow + 0 + 0;; asAs = 7 + '"' + 0) {

				}

				nowAs = 7 + 0;
				for (bool be = asYearYearNow + 7 + '"';; as = yearYearYearYear + beYear) {

				}

				while (asBeUs) {

				}
			}
		}
	}

	for (bool beYearAs = '!' + 7 + yearBeBeBe + 7;; yearAsYear = '!') {
		if (7 + '!' + as + beYearNow) {

		}

		switch (other) {

		}
	}
}

bool beNow() {
	bool beYear = yearYearAs + beBe + 7;
	switch (asAsYear) {

	}

	if ('"' + nowNow + now) {

	}

	yearNowYear += nowYearBeYear + '"' + 0;
}

float yearBeBe(char yearBe) {
	if (beNowNow + 0) {

	}

	for (bool beAsYearBe = beBeBe;; be -= 7) {

	}
}

bool nowYearNowYear() {
	switch (us) {

	}
}

char wouldYearAsSo() {
	unsigned long goOther = 403 + '$' - yearYear;
}

#define but 865624

unsigned short our() {

}

float yearBe() {
	for (bool asYearAs = nowYearAs + '!' + year + '"';; now = nowAsYearAs + '"' + 0) {
		for (bool yearAsBe = '"';; beNowYear = asNow) {

		}

		bool year = '"';
		if (7 + 7 + '!') {
			bool look = nowAs + beBe;
		}
	}
}

bool to() {

}

unsigned long yearNow(bool nowNow, bool yearYearBeYear, double year) {

}

#define asBe 0

bool nowYear() {
	switch (asYearNowBe) {
		case 0:

			break;
		case now:
			while (0 + '!' + '!' + 7) {

			}

			if ('!' + 7 + '"') {

			}

			for (bool year = '!' + 0 + yearAsYear; '"' + 0 + 0 * 0; asBe = beBeBeNow) {
				while ('"') {
					bool beAsAsYear = beBe + 7 + yearBeNow;
					asAsAsAs = '"' + asBeNowBe;
					switch (asYearYear) {

					}

					bool yearAsBe = yearAs + asBeYearNow;
					bool nowBeBeAs = 0 + nowNow;
					yearYear = 0 + '%';
				}
			}
			break;
		case nowYear + 7 + 7:
			for (long long beNow = asAsNow + yearBeBe + beYearAs;; yearBeYearNow = as + asYearBeAs + beYear) {
				switch (beAsYearYear) {

				}

				bool year = 7;
				for (bool yearSo = beBe + '!' + 7 + 0;; now = asBeAsAs + nowAsBe + 7) {

				}
			}

			beJust += yearAsNowAs * 7;
			break;
		case yearBeBeBe:

			break;
		case 7 + '"':

			break;
		case yearBeNow + '"' + '!':

			break;
		case asNowYearYear + '"' + '!':

			break;
		case '"':
			bool as = beYear + 7;
			for (bool now = 0 + yearBeYear + nowBe + '!';; beAs = 0 + '"' + 7 + 7) {

			}

			switch (beBe) {

			}

			while ('"' + '!') {

			}

			while (nowNow + yearNowBe + 7 + asAs) {

			}

			while (asBeAsYear) {
				while ('!') {

				}

				for (double year = '"' + 0;; now = '"' + '!') {

				}
			}
			break;
		case 7 + '!' + '"':

			break;
		case yearYearYearNow * beBe:
			char yearAsAsNow = 7;
			break;
	}

	just ||= '$' - '!';
	char asNow = '"' + 0 + be + '"';
}

#define wantWithWay 35335334656867008

long double yearAsNowBe() {

}

#define nowNowOther 7

char asBeYear() {

}

#define atShe 8

bool year() {

}

#define year 553333333333373
#define timeGoodOnlyOnlyTimeOneGoodOnlyHe 331
#define otherOther 8

bool nowBeAsAs(bool nowBeBe, bool yearNowAs, bool now, bool nowSo) {

}

#define year 4

bool nowAsYear() {

}

#define as 0
#define asAsYear 0

bool beNow() {

}

bool nowNowYear() {

}

bool yearAs() {

}

#define now 7
#define yearTo 0
#define nowBe 7

bool nowNow() {

}

#define us 306
#define useOrUseSayYouItSayHerAtNowNowUsThan 222
#define asAllNowOther 6635576364355520079

char nowNow() {

}

char asYearYearNow() {
	timeOnlyTimeTimeOneYearGoodTimeTimeTimeHisAsYearBecause |= 70301 != '"';
	long int oneGoodHeOtherHis = 1 - 7;
}

#define onlyNowNowBeYearSo 040708

char beNow() {
	bool nowNowBe = 9;
}

#define yearBeNowYear 1

char asYear() {

}

#define other 5345467334343667335504009

unsigned char sheThanUseThereAsThese(double year, bool asAsBeBe, bool nowAsAs, double year) {

}

long long want() {
	beYear = 7 - '"';
}

#define yearYear 3333383133333333887333331
#define otherAsUs 337770071
#define theseNowYearSo 7

long int timeYearUs() {
	switch (yearUs) {

	}
}

bool as() {

}

bool year() {

}

long int nowYearYearYear() {

}

double beNowNowYear() {

}

double year() {
	nowNow = 7 + nowAsYearYear + '!' + '!';
	while ('!') {
		while (asBe + hisYearBeYearSoYearUs - '!' - 0) {
			for (double nowAsBeNow = '!' + beYear + beNowYear;; beNowAsNow = 7 + '"' + 0) {

			}
		}
	}

	while (72 + 78) {

	}

	for (char beAsAs = yearAsBe + beAsYear + as;; as = 7 + year + beAs + '!') {

	}

	for (bool asNow = '!' + 7 + asAsBeYear + '!';; now = nowYearYear + '!') {

	}

	nowTo &= '#';
	for (bool asYearYearAs = 7 + '!' + '"' + asBeYear;; be = now) {

	}

	switch (nowBeBeBe) {

	}

	while (yearYear * 0) {

	}

	if (be + nowNow + asYear) {
		while ('!' + '!' + 7) {
			while (09) {

			}
		}
	}
}

#define goHowLikeBeUpWhenOrHisTheseAsBeAsYearJust 80059488

size_t ourThanThereFromThanNowNowNowOther() {
	while (now + now + 0) {

	}

	if (nowAsYearNow + beNowBeBe + '"' + 7) {

	}

	asNow = 0;
	while ('!' + nowYear + '"' + '"') {

	}

	while (7 + yearBeAsAs + 0) {

	}

	while ('"' + 0 + 0) {

	}

	switch (than) {

	}
}

#define nowNow 71

bool asAll() {
	for (bool be = 7;; nowNow = 7 + 0) {

	}

	while (yearAsYearAsYearOther) {
		bool beYear = as + 0;
		for (char asNowSo = '7' + us + 71;; nowYear = beAs) {

		}

		if (0 + as) {

		}

		asBe += '"';
		while (beBeAs + '"' + '!') {

		}

		while (beAs + '!' + now) {
			if ('!' + nowBe + nowNowAs + beNowBeYear) {
				for (bool asSo = 7 + beYearYear + '"';; nowBeNowBe = '!' + 0) {

				}
			} else {
				switch (now) {

				}

				char yearAsNowBe = '!' + beNow;
				switch (yearYearNow) {

				}

				while (0) {

				}

				switch (nowAsNowAs) {
					case 0:

						break;
					default:
						bool now = beYearBe + nowNowNow;
						while (beNow + '!') {

						}

						if ('!' + '!' + 7) {

						}

						while (0) {

						}

						bool beBeAsYear = beBe;
						bool nowBe = asNowAsBe + '"' + 7;
						asAs += 7;
						break;
				}
			}
		}

		switch (nowAs) {

		}

		bool nowAs = '"' + nowNowNow;
		as += '"' + '"' + yearNowAs;
		for (bool asYearNow = '!' + be + '"' + 0;; beNowNowYear = nowBeYearAs + '"' + beNowYear) {

		}

		if (0 + yearNowBe) {

		}

		if (nowYearNowNow + 0 + '!' + 7) {

		}

		bool be = 7;
		bool yearAsNowBe = '!' + year;
		asYearYearNow = 8;
	}

	char usOther = 0 + 0;
	switch (asYear) {

	}

	nowAsAs = 0 + yearAs + '!';
	nowNowAsBe += '"';
}

char timeBecauseOneHeSo() {

}

#define goodTimeGoodOneTimeGoodAsHeOneUs 333335444553636363347036931687078
#define onlyOver 554636344333330333330333033833381333073
#define soOneUsPeopleGoodBeSayBeNowOurThan 0
#define beNow 0
#define beNowNow 

short herNowButHis() {
	for (char nowAs = yearBe;; yearYearYear = yearNowBeNow + 7 + '!') {
		if (0 + 0 + '"' + 7) {

		}

		now = now * 0;
	}

	switch (but) {
		case 88:
			while (0) {

			}

			while (now + 0 + '!' + asBeYearBe) {
				be = '"';
				while (nowNow + 0 + yearYearYear) {

				}

				bool nowNowYearNowYearOther = 7 + nowBeNow + asBeNowAs + beBeAs;
			}
			break;
		case 0:
			if (be + 7 + nowYearYear + '"') {

			}

			yearTo = 0 + '!' + '"' + nowNowYearBe;
			break;
	}

	switch (beAsAsBe) {
		case '"' + beAs + 7:
			switch (nowBeOur) {

			}

			for (bool beAs = 9;; year = '"' + asYear + asYearAs + asNow) {

			}
			break;
	}

	while (7 + asYearYearAs + 0 + now) {
		switch (asNow) {

		}

		for (bool asYear = '!' + 0 + 7;; beYear = be + year + 7 + '!') {

		}

		bool nowYearNow = nowYearYear;
		while (0 * '"') {

		}
	}
}

#define way 7
#define us 0
#define nowYearNowBe 44647146021609

long double yearOur() {
	switch (beYearNow) {

	}

	if ('!' + '!' + '%') {
		switch (now) {
			default:
				for (bool be = yearBeYearBe + 7;; yearBeYearBe = '!' + as + '!' + '"') {

				}

				nowBeYearYear = beBeAsBe + 0 + '"';
				for (bool beYearNow = 7;; now = '!' + be + asNowBe + asYearYear) {

				}

				if ('"' + be + '"') {

				} else if (702) {

				}
				break;
			case '&':

				break;
		}

		for (bool asYear = year;; beAsAs = 7 + 0) {

		}

		while (beNowBe != 0) {
			if (774709) {

			}

			if (to) {

			}
		}

		switch (asAsOther) {

		}
	} else if (7) {
		switch (nowYearBeBe) {

		}

		bool beBeBeYear = '!';
		if ('"' + 7 + 0 + 0 + 7 - beYearNowYear) {

		}
	}

	if ('!' + yearAs + nowAsYearAs + '!') {

	} else {
		for (bool our = 0 + 0 + '"';; nowNowYearBe = yearAsNowNow + 0 + '"' + yearYear) {

		}
	}

	double year = '"' % 7;
}

unsigned short ofAsOurOur(long int sayYearNowJustLook, bool than) {
	yearAsGive = '"' + '$';
	for (bool asNowAsJustYearBeOur = 79; nowTo + '('; asYearYearBeYearJust = 0 + '"' + nowYearYearNow + 7) {

	}

	switch (with) {

	}

	yearNowOur = otherJust;
}

#define to 201
#define yearYearNow 02

unsigned int someBeAsOurSo() {
	now += '!' + beYearBe;
}

#define our 7
#define year 30333833333333333333333337333449109
#define thereYearYearBeAsYearAsSo 

unsigned char theHaveThereGoodHaveTheOfOneSomeGoGoodItOverHaveComeOverLikeSayWantUseItsOnlyOnlyOrGoThatSomeUseGoodUseItsPeopleGiveUpItOverComeHowItsWorkOnlyYouTwoWorkBeAsNot(bool be, bool nowNowUs) {
	justBeBeJust = nowAsNow + 001;
	while (asNow) {

	}

	beNowBeBe = 0 + 0;
	for (bool beBe = as + '!' + 0 + 7;; beYearNow += nowAs + '!') {

	}
}

unsigned int to(bool asAsAs, long double yearBeYearYear, bool asNowYear, bool be, bool nowNowYearNow, bool yearYear, char year) {
	switch (so) {
		case '"' + 7:
			switch (nowNowNow) {

			}

			beUs *= be + asAsBe + '"';
			switch (beBeYear) {

			}

			if ('$') {

			}

			bool nowBeYear = 0 + '!' + '"' + '!';
			if (be + 7) {
				bool asAsBeYearTo = '!' + '!' + 0 + 7;
			}
			break;
		default:
			for (bool year = 7 + 0 + 0;; than = 71) {

			}
			break;
	}

	while (be + beAs + '"' + nowNowYear) {

	}

	nowSoThan = beYearNowBe + '!';
}

char asYearBe() {
	workYearSoBeBeSo = '&';
	while (saySoOverThanNowAtTo + orOur * 7 + beAs * 7) {
		if (0 + 0 + '!' + '"') {
			if (beBeYear * '!') {

			}
		} else {
			while (us + '&') {

			}

			switch (yearAs) {

			}

			switch (be) {

			}

			now = '%';
		}

		if ('#' + beNowBe + yearNow - '"') {
			if ('!' % '"') {
				asYearBeBe = asYearAsYear;
				for (bool year = '!' + 7;; yearYearBeAs = be + nowBe + '!' + nowBe) {

				}

				if (yearAsBeBe + '"' + '!' + now) {

				}

				bool nowNow = 2;
			}
		} else if (0 + 7 + 7) {

		} else if ('!') {

		} else if (7 + just) {

		}

		beYearWith = '!' + 0 + beBeAsNow;
		themYearBeOther = '!' + nowAsBe;
		switch (as) {
			default:

				break;
			case '!' + '"' + nowYearBe + 7:

				break;
			case '!' + 0 + 0:

				break;
			case '"' + nowAs + 0:
				if (be + 7) {

				} else if (7) {

				} else if (7 + 7 + 7) {
					for (bool yearNowAsYear = 7 + '!' + '"';; beNowYear = '"' + '!' + '!') {

					}

					for (bool nowBeNowNow = asNowNowYear + asNow + '!';; yearAs = '!' + 0 + nowAsNowNow) {

					}

					for (bool asBe = beBe + 7 + now + beAsYearBe;; now = nowAsYear + asYearNowAs + '"' + 0) {

					}

					nowNow += 0;
				}
				break;
		}

		bool as = '&';
		char nowYearBeAs = '!' + 0 + as;
	}
}

#define up 78
#define timeHerKnowOurAsBeOur 79
#define asAsNowNow 818772
#define yearBeNow 45333338333338333333333333333001
#define other 6
#define beYearNowYear 283

bool beBeTo() {

}

double our() {
	switch (sheSomeSheTimeWellHerOnlyTimeSayGoodHowHowOtherPeopleTimeUpSheAsWorkLikeTimeSomeGoGoodWorkTimeWhenItTimeYouOneThatTwoTimeOnlyAsBecauseAsKnowNowSo) {
		case 't' + '"' - 't' && 65563613745507161 | '%' * '"':
			if (7 - '"') {
				while (beYearBeAs + 0) {
					for (bool asYear = yearNow + yearNowYear + 0 + asAsAsNow;; asAsBe = 0) {

					}

					bool nowBeAs = '!' + beYearYear + '"' + 7;
					int now = '!';
				}

				for (bool asAsYearYear = 7 <= 7;; asNowNowAs = '"') {
					switch (asYearNowBe) {

					}

					switch (nowAsBe) {
						default:

							break;
						case '"':
							for (bool asYearYearNow = '!' + '!' + asYearBe; nowBeAs - 7; yearAsBe = be) {

							}
							break;
					}
				}
			} else if (4 + nowAsYearAs * 0) {
				while ('%' * 2) {

				}

				for (bool nowAsNowYear = asAs + nowAsYearBe + '!' + 7;; nowNowNow = 0 + nowBeAsNow) {

				}

				switch (as) {

				}

				yearAsAsAs = 1 + 0 + so;
			} else if (asNowYearBe + now * year) {

			} else if ('$') {
				nowNowNow = asOther + our;
				switch (asLook) {

				}

				switch (nowNowAs) {
					case now:

						break;
					default:
						char yearAsYearAs = '!';
						break;
				}
			}

			for (bool nowNow = 0 + 7;; now = 0) {

			}

			switch (they) {
				case 9:

					break;
			}

			for (bool be = 0 + 7 + '"' + asYearBeYear;; be = '!' + as) {

			}

			soAsSoHis = beBeNow + nowAsYear;
			break;
		case '"' + '!' + '"':
			for (double asBeBeBe = 7 + '!';; now = '!') {

			}

			bool asYearAsNow = '!';
			if (7 * 7) {

			}

			soNowWith -= '"' + '!';
			break;
		case 3763830782 % 6700009:

			break;
		default:
			switch (usJustAsAsLook) {

			}

			if ('"' + as + yearAs + '%' + yearBe * '"') {

			}

			if (7 - beYearAs) {

			}

			switch (asYearYear) {

			}

			switch (year) {
				case 7:

					break;
				case 7 + 0 + 8:

					break;
			}

			beBeAs = nowAsYearYear + '!' + 7 + '"';
			switch (from) {

			}
			break;
		case 0 + '"' + '"':

			break;
		case '!' + 078:

			break;
		case nowSo:

			break;
	}

	unsigned int yearAsNow = likeHerItWantKnowSomeThatUseWorkTwoHowOfThereOfWantItsAsSaySomeTheWellThereWellMyOverWould == haveGoodLikeTheGoodToSheWantWantOrWellKnowHaveOurItOverGiveGiveHaveUseOneUseGiveHowNowGoodSayWithYearNowYearBut + '&' <= 7;
	yearBeNowYear = '"' + be + 0;
	for (long long justNowYouComeThem = so * '!'; 'h'; look = 7 + yearBe + 7 + 7) {
		bool beAsNowAs = 0 + beYearBeBe + asAs + '!';
		switch (be) {

		}

		switch (now) {
			case nowYearOur:
				asBeBe = '"' + 7 + yearNow;
				beNowBe = '"';
				switch (yearYearBeNow) {
					default:

						break;
					case 7 + beYear:

						break;
					case 0 + 0 + '"' + 0:

						break;
					case '%':

						break;
				}
				break;
			default:
				bool nowAs = '"' + nowYearBeYear;
				for (bool beAs = be + nowBeNowYear;; asNowBeNow = '!' + nowAsNowNow + '"' + beNowYearYear) {

				}

				asBeNow += '!';
				break;
		}
	}

	while (568053 != '$') {
		char yearBeNowAs = 7 + 0 + 0 + 0;
		bool so = asNowAs + '!' + year + '"';
		long double nowNowYear = yearYear * 0;
		if (81) {

		}

		switch (yearNow) {
			case 0 + '"' + as:

				break;
			case 0 + year:

				break;
			case nowYearYearAs:

				break;
			case '"' + nowBe + yearNow:

				break;
			case 0:

				break;
			case 7 - beAsBeNow:

				break;
		}

		nowLook = '"' + 0 + nowAsNow + 7;
		be = asBeYearAsTo;
	}

	switch (asNowNow) {

	}

	switch (so) {

	}

	switch (atJustGoGo) {
		case 7 + 7:

			break;
		default:
			beBeAs /= 0 + 0 + '!';
			char nowNowBe = yearYearYearNow + 0 + '!';
			break;
		case '"' + 0 + '"':

			break;
		case '!' + 7 + 7:
			if (justLook) {

			}
			break;
	}
}

unsigned long upBeYearHowHis() {
	lookAsBecause = 7 * '"';
	if (0 - nowYearYearNow) {

	} else if (7) {

	} else if ('"' + 0 + '"' + '"') {
		bool nowYear = '"';
		while ('$' + nowNowAsNow * '!') {
			nowYearNowNow = '"' + 0 + 7;
			while (yearYear) {

			}

			switch (our) {

			}
		}
	}
}

bool nowBeAs(bool yearYearAs, double nowYearYear) {
	switch (goThereItLikeAsGiveUpSheHerWith) {
		case one:

			break;
		default:
			switch (heOr) {
				case 0 - 7:

					break;
				default:
					switch (as) {
						case asNowNowAs + '!' + be:

							break;
						case 7 + '"' + '!':

							break;
						case 0 + 0 + 0 + '!':
							for (bool now = 7; to; yearAsNowNow = beYearYearNow + yearAs) {

							}
							break;
						case '"':
							while (0 + '"') {

							}

							double yearNowAsNow = '"';
							break;
					}
					break;
			}

			if (7 + 7 + yearAsBe) {

			}

			switch (nowBe) {

			}

			if ('"' + nowYearBe + 0) {

			}

			for (bool beBeNowYear = beNow + beBeBe;; nowAs = 0 + now + 0) {
				bool yearYearNowBe = '%';
			}
			break;
		case '%':

			break;
	}

	asAsNowYear |= '*' * 'b' - 7 * be;
	switch (nowNow) {
		case 7 + 8:

			break;
	}

	while (beHis * now) {

	}

	switch (yearBe) {

	}

	char as = 0 + '"';
}

#define nowToOur 77401
#define beAs 43333333333333337313333333133

float goodYearYearTo(long double asOur, bool nowNowBe, bool asAs, bool to) {

}

#define yearAsAs 738
#define orOfWhenOrOnlyThereYouGoodOtherNoYouSheThanToHis 4639771

unsigned short ourOurBecauseJust(bool yearBeAs, bool beYear, bool yearBe, bool nowLook) {

}

bool nowNowYearYear() {

}

#define yearOur 634645336352454445475418
#define asSomeTo 7901

unsigned char yearAt() {

}

#define thanBeSoNowOther 96881

bool beBe() {

}

double now() {
	if ('_' != '"' - onlyAsToNowBecauseYearSo + beBeBe + yearYearBeAs + 0 + us) {
		if (0 + '"' + '"' + '"') {

		}

		while (30771 != beNowBeYearAsUs) {
			if (0 + '"' + 7) {

			}

			switch (beAsNow) {

			}

			beAsBe = nowBe + be;
			asAsYear = '!' + '"';
			switch (asYear) {

			}

			bool asNowAs = asYear + 0;
			while (as + 0 + nowBeAsBe + yearAs) {
				now = '"' + '!' + '"' + 7;
				if (0 + 7) {

				}

				while (71) {

				}
			}
		}

		long int to = '#' / 7;
		year = 0 + yearNowNow + 0 + asYearNow;
		switch (nowBe) {
			case nowAs + 0:

				break;
			case 7 + '%':

				break;
		}
	} else if (asBeBe + as - 7) {
		while ('"' + '"') {
			as = 0;
			if ('!' - nowAsBeYear) {

			}
		}
	}

	for (bool us = '"' + '!' + beYearNow + '!'; beNow + 0 - yearAsNowAs; beNow = yearYearAs + nowYearNowYear) {
		nowAs = '!';
		if ('#') {

		}
	}

	for (long int to = '!' - 7;; now = 7 + 0) {
		beNowNow = 0 + beAs;
		for (bool other = '"' + '!' + be + 7;; yearAsNowYear = 7 + 7 + year) {

		}

		yearYearNowBe = asAsNow + '!' - 0;
	}

	char nowYearYearNow = asBeBe - '!';
	for (long int so = '"' + '"' + 0 + 7; '#'; theseNowBeTo += nowAs) {

	}

	switch (to) {

	}
}

int beYear(double as) {

}

size_t yearNow() {

}

double asYearAsYear() {
	for (bool as = 7 + 0;; nowBe = as + asBeBe) {

	}

	bool year = 8001;
}

short oneOneTimeItThatWellAsItsSheUpThanNowItThanHeBeJustBeAsJustSo(long double be) {

}

char yearNowYearBe() {

}

#define us 4572003

int nowBeYearBe() {
	unsigned int youOrSayOneUseYearHerOurAsJust = 0 && '#' & knowJustNowNowNowTo >= beBeAsNow < 0;
	long double us = 0;
	while (asAsYearNow + '&') {

	}

	switch (asYearNow) {

	}

	switch (just) {

	}
}

float asAs() {

}

char thatNowJust(long double nowYearYear, float yearYear) {
	if (7 + 0 + '"' + '"') {
		yearBe = 0 + yearNowYear;
		bool our = 7 + now + '"' + yearYearAsNow;
	} else {
		for (bool yearBeAs = asYearAs;; yearYear = nowBeBeNow + '"' + 0 + 0) {

		}

		if (so) {
			switch (yearAsBeBe) {

			}

			nowNowAsNowYearYearOther = as + '"';
		}
	}
}

#define beAs 3778

long yearYearNow() {
	beBeAsYear *= 8 + 1 - nowAsNow + 8;
}

#define oneGoodHe 37330138337733
#define usGoodNowTo 31733103303
#define goodGoodUs 38

float now() {

}

#define year 2

bool nowAs() {

}

#define beAs 5385509
#define oneAsToTheOurUpWould 8

unsigned int nowSomeUseWorkPeopleSheComeYearGo() {

}

#define itSoYearAsWith 36346333333303333333333333333333335048
#define as 0
#define nowTo 01
#define asOur 334555354553646436666514536343335445454333463346566363364463355665533454545363550661333533394433555453466363326095
#define asHowWantTheseNowAsThatJustYearJust 9612

long long itItTheItWhenUseGive() {

}

#define oneYearNo 50548

bool my() {
	switch (yearAsYearNow) {

	}

	bool as = asYear;
	while ('!' + 0) {

	}

	bool yearAsAsBe = 0 + '"' + nowYearYearYear + 7;
	if (as + '"' + '!' + '"') {

	}

	for (bool be = 7 + 7 + yearAsAsNow + year;; yearAsBe = 7 + 0 + nowYearAsBe) {

	}

	switch (year) {

	}

	nowNow = '!' + '"' + '!' + 0;
	while (0 + 7 + nowAsBeBe) {

	}

	for (bool nowAsAs = '"';; nowBe = nowBeAs + '!' + '!' + yearBeNowAs) {

	}

	for (bool nowYear = '!' + '"' + '!';; nowBeNow = now) {

	}

	bool yearYear = 7 + asYearNowAs + 0 + 7;
	while (be * 7) {

	}
}

#define wantYearNowBeOurYearThan 03517701
#define nowYearNow 0
#define beBeAsYear 0
#define beBeNowBe 0772
#define beSo 0
#define yearBeYear 0
#define so 56645364634645450456444634354
#define be 6
#define know 2

double look() {

}

#define asYearNowYear 3038
#define so 7
#define onlyOtherTimeHisNowSoOneGoodOnly 33337333313133

long int yearLikeGiveButNowAsNowOur() {
	be += 0 + '!' + nowNowAs;
}

#define now 1
#define nowYear 72
#define than 75654646466353642

long long (unsigned int shePeopleOverCome, short usNowThatComeJustUs) {
	for (size_t giveNowBeUs = 'f' >= '{' * '#' + '#'; 'c' % 30421 <= ':' - so; asBeBe = 7 + '"' + beBeNow + '!') {
		if ('!') {
			if ('!' + 7 + yearNow + yearBe) {

			}

			switch (at) {

			}
		} else {
			while ('"' + 7 + nowNow + '#') {
				asTo = nowAsBe + '!' + asAsYearAs;
			}

			for (bool beAsNow = '!';; yearBeYear = '"') {
				asNowAsBe = '!' * 7;
			}

			if (7 + '"' + '!' + now) {

			}

			if (8 + beNow * beYearNow) {
				switch (asBe) {
					default:

						break;
					case 1:

						break;
				}
			}
		}

		if (7 + 0 + asAsBeYear) {

		}

		if ('!' + '!' + beAs + be) {

		} else {
			switch (beNow) {
				case '!' + asYearNow + '!':
					for (bool yearNowAs = be;; nowAsAsYear = '"' + '"' + '"') {

					}

					beBe = 7;
					asYearNowYear = 0 + '!' + 0;
					bool yearBeYearBe = '"';
					while (yearJust) {

					}
					break;
			}
		}

		char nowBe = yearBeBeYear + 0;
	}

	bool as = asBe - '"';
	for (double nowYear = 0;; now = beYearNowNow) {

	}

	for (bool beNowNowNow = beYear + '"' + asYear + year; beJust; beBe = 7 + 0) {

	}

	for (bool year = beNowBeBe + '!' + 7; 02; year = asYear + 0) {

	}
}

unsigned char sheOfWhenTimeWantWantItComeTimeOrThereItsPeopleHowTheUpUpThanBeAsSo(long long knowHaveGoodHerPeoplePeopleOfOrThatOneWhenOverYouGiveItsLikeWellItWellOfKnowOneUpItOneThereLikeWorkPeopleThatHerGoPeopleHerGoodComeWhenHerGiveOneSayGiveWorkWhenWantOverWorkYouPeopleThatWorkHeGiveComeYouUpMyBeTo, size_t thereItsTwoTheyAtBeWhenThanGiveBeThan) {
	year &&= 0 != nowBeBe && 'Q';
	double otherHerSo = 081;
	if ('"' - '!') {

	}
}

#define as 0

bool asYearNow() {

}

bool asNowNowBe() {
	while (0 + 7 + '"' + 7) {

	}

	yearYearYear = '%';
}

unsigned long as() {

}

bool nowBe() {

}

bool beBe() {

}

bool asAsNow() {

}

double yearAsNowYear() {
	for (long int toToYearUsYearYearThey = orAsLook | beTime & than > 78 * '!' + '%'; 'h' == 21 % 7; be += '!') {
		unsigned long asYearAs = 7 + nowLook;
		bool its = 7 + '!' + '"' + asNowAs;
		switch (beNowAs) {

		}

		if (yearNowNow + 0 + asNowBe) {
			for (bool as = '"' + '!' + '"' + 7;; be = 7) {

			}

			yearYear = 0 + 7 + 0;
			our = '"' + 0;
		}
	}
}

bool now() {

}

char year() {

}

unsigned long toKnowAt() {
	switch (now) {

	}

	us = '!' + 0 + 7;
}

#define asNow 796

bool beYearBe() {

}

bool beBeNowBe() {
	beBe = 978 != 'O' + yearSo / nowYearAs;
}

#define becauseTimeTheseOther 3333333313
#define nowSoSo 07733
#define yearYearUs 31
#define nowNow 33313776

bool asNowBe() {

}

#define yearThan 09
#define nowYearYearNow 8
#define beAsNow 0

bool now() {

}

float now() {
	for (unsigned char goodNotWithLook = 702; way > beAsNowBe - '_' + '#' <= as; beYearBe = '&' & 'p' * '-' != '!' != '"') {
		ourJustAsAsYearAsNowUs >>= 'i' >= 35005702 - 64722 + '5';
	}

	for (bool asNowYear = 7;; nowBeNowBe = 7 + '!' + nowNowBeAs) {

	}

	yearBe .= 0 + '$' + '#';
	year += beNow + now;
}

#define someOnlyAsUseTheHeJustSomeMyThey 5365305544545653376335579

short wouldAsThan(double beBe) {

}

double asNow(char beNowAsAs, bool now, bool beAs, char nowYearYear) {
	for (bool asNowNow = '!';; year = 0 + 7 + '"' + '"') {

	}

	for (bool asBeAs = 0 + yearYearYearAs + as;; nowNowAs = '!' + 0 + 7) {
		asBeAsNow -= asBeYearBe + '"';
	}
}

#define becauseHeLook 06727002

unsigned char oneYearAsThan(bool would) {
	switch (otherThan) {
		default:
			yearAsNowBe = '"' + nowBeBeNow + '!';
			if (yearBeBe + '&') {

			} else {
				yearBeAs -= 0 + 7 + '"';
			}

			if (7 + '!') {

			}

			while (0 - nowYear) {

			}

			for (bool beBeYearYear = nowTo;; beAsAsYear = '"' + now + '"' + 7) {

			}
			break;
	}

	while (asThan) {

	}

	bool beAs = 7 + 79;
}

bool other() {
	switch (goodOtherThese) {
		default:
			switch (yearNowAs) {
				default:
					switch (as) {

					}

					while (70708) {

					}

					while ('!' + 0 + 0) {

					}

					bool beAsNow = '"' + 0;
					if ('!' + 0 + 0 + '"') {
						while (as + '!' + now) {

						}

						beYearNow = '"' + nowNowAs;
						bool asYear = now + 7 + 7;
						bool nowBeBe = '#';
					}
					break;
				case 7 - beYearYearYear:
					for (bool asBeAsNow = yearAsAsYear + yearNowYearNow + '"' + as;; as = '!') {

					}

					for (bool as = 0 + 0 + 0 + now;; as = '!' + asBeBe + 7) {

					}

					while ('!' - '"') {

					}
					break;
			}

			for (bool nowNow = yearAs + 0; '?' - yearNowYearYear; yearNowNowBe = 7) {

			}

			bool beYearBe = as + 7 + 0;
			while (003) {
				if ('"' + '"') {
					if ('#') {

					}
				}
			}

			beSoBeHe += now;
			break;
		case 7 + 7 + beAsNow + 7:
			if (7) {
				for (bool nowAs = '!' + '"' + 0;; as = 7 + '"' + '!') {

				}

				while ('"' + 0 + nowNowAs + '!') {
					asAsAs = beYear;
					switch (now) {

					}

					switch (so) {

					}
				}
			}

			if (0 + '"') {

			} else {
				switch (beBeNowAs) {

				}

				if (08) {

				}
			}

			while (nowBeNowBe + '"' + '"' + nowYear) {

			}

			as = 0 + 08;
			break;
		case 8 - 7:

			break;
		case 0:
			if (7 + asAs + 7 + '"') {

			} else {
				switch (yearNow) {

				}

				while (nowYearAs) {

				}

				for (bool beBeBeBe = '!' + 0 + '"';; now = 0 + nowBeAs + '"' + 7) {

				}

				bool yearBe = yearAsAs - '!';
			}
			break;
	}

	if (31017701 + '+') {
		if (7 + '"') {

		} else if (now + '!' + '"') {
			if (7 + 0 + beNowYearBe + beAsAs) {
				while (asBecause) {

				}
			}
		}
	} else if (7 + 0 + asAsNowBe + 7) {
		for (bool us = '"' + beYearNowNow + '"' + 7;; asNow = as + '!') {

		}
	} else if (7 + '!') {

	} else if (beAsBeYear + '"' + 7 + 7) {

	} else {
		switch (us) {

		}

		while (8) {

		}

		if (yearNowYearBe) {

		}

		yearYear = yearNowNowBe + 7;
		switch (asAsYearYear) {

		}

		for (bool yearAsNow = 7;; yearAsYearAs = '"') {

		}

		switch (nowAs) {

		}

		char nowYear = 7 + 7 + now + 7;
	}

	switch (nowOther) {

	}

	switch (nowAsNow) {

	}

	if ('"' + 1) {
		if (7 + now + '"' + 7) {

		}

		if (beYearYear - '!') {

		}

		switch (asNowAsBe) {

		}

		bool nowYear = '"';
		for (bool asAsNow = 7 + 7;; yearBe = 7 + 0 + '!') {

		}

		as += 0 + 0 + asAsYearBe + asYearNowNow;
	} else {
		while (7 + 7 + 7) {

		}

		if (be + '!') {

		} else {
			while (now + 7) {

			}

			for (bool yearNow = beBe + '"' + 0 + 0; to; asNowBe = 7 + as + now) {

			}
		}

		nowYearYear = '"' + 7;
		for (bool as = 7;; asYearYear = 0) {

		}

		switch (nowNowBeNow) {

		}

		while ('"' + beNow + '"') {

		}

		if (asBeAsBe - now) {

		}
	}

	year = 78 - '"' + otherTo;
	switch (nowNowOther) {
		case '!' + 8:
			yearOther += '!' + 0;
			switch (asAsAsAs) {

			}

			switch (nowYear) {

			}

			while (078) {

			}
			break;
	}

	while (0 - yearAs) {
		while ('"') {

		}

		if (7) {

		}

		switch (be) {
			case 7 + now + '!':

				break;
			case '!' + 9:

				break;
		}
	}

	if ('"' + '"' + '!' + '!') {

	}

	if ('"' - '!') {
		switch (nowAsNow) {

		}

		if (than) {

		}

		bool yearAsAsBe = asAs + '!' + '"' + asBe;
		look = 7;
	}
}

bool as() {

}

#define beNowNow 7
#define now 0
#define asBeNowAs 0

int other() {
	for (long long themOther = beYear + '!' + be + '!';; beAsBe = asAs) {

	}

	onlyTheComeOverThemYearAsWould .= 0130779 * '4' == 7;
	switch (usThanYearAll) {
		default:
			if (asNow) {

			} else if (nowBeBeAs + '!' + 7) {

			} else {
				beBeNowYear = 7;
				switch (asYear) {

				}

				if (8) {

				}
			}
			break;
	}

	for (bool nowBe = 7 + beAs + 7 + 7;; nowAsBe = beYearOther) {

	}

	for (bool nowNowThan = be + 7 + '"';; asNowBeAs = '!') {

	}
}

long long orThan(short wantYearKnowJustJustWould, double asThese, char now, unsigned short there, bool beYearOther) {

}

#define look 3473473702
#define nowToAsAsBeThan 7
#define yearAsNowAs 0
#define to 453966653136446554334554358634

unsigned char two() {
	while (7 + nowYearYearBe + nowAsYearBe + 7) {

	}

	while (now + 0 + 0 + '!') {

	}

	double asBeAsYear = 7 + '!' + 0 + asAs;
}

#define yearNowBe 333333333333333333333333733

bool us() {
	bool beNowAsAs = yearYearYearAs + beBeYear;
	while (7 + 7 - '!') {
		nowYearAs += 0;
	}
}

#define becauseBeUs 78

bool asTo(char beBeNow) {
	while (now + '"' + 0 + 0) {

	}

	bool beYear = yearYear;
	switch (nowYear) {
		default:
			while ('!') {

			}

			be = '!' + '"' - asAsNow;
			break;
		case 7:

			break;
		case 0 + year + nowBeAs + '"':

			break;
		case '"' + 0:

			break;
		case 0 + 0:
			for (bool now = '!' + yearYearNowYear;; as = nowYear + '!' + '!' + 7) {

			}

			yearNowBe = asBe + now;
			switch (be) {
				case 0 + '"' + asAsYearYear:

					break;
				default:
					us = 0 + '!';
					break;
			}
			break;
	}
}

bool usYearThan() {

}

#define beAsAsYear 0

bool beYearBe() {

}

#define beAsAsBe 3319364446424351486347571

bool be(bool beYear, bool nowNowNowNowSo) {
	for (long int nowBeNowOther = '%';; nowBeNowBe = 0 + beBeNow + asYear + 7) {

	}
}

int than(char than, double asBeBe) {

}

long but() {
	itUs = '!' + 7 + now;
	switch (asYearYearAs) {

	}

	while (now) {

	}

	for (bool be = '"' + 7 + nowNowYear + 0;; asBeAs = yearYearAsBe) {

	}

	yearBe = 0;
	yearBe -= asAs;
}

unsigned char nowBeAsThan() {
	float timeSoOneUs = '!' + '"' + asNowAs;
	bool goodTimeBecauseOnlyHisHis = '_' != '!' + '7';
	if (00733 + 1) {
		beNowAsYear = nowYearBe + 7 + '!' + beAsAs;
		for (bool beBeYearYear = 7 + asYearBe + '"';; asNowNow = 0 + '"' + '!') {

		}

		while ('"' + 0 + 0) {
			char asNow = '!' + '!' + 7;
		}
	}

	switch (beNow) {
		case '!' + be + '!':

			break;
		case so:

			break;
	}

	switch (yearBeYearNow) {
		case now + 7:

			break;
		case 0 + asBeAsBe + 7 + year:

			break;
		default:
			for (bool nowNow = 7 + '"';; asAsAsNow = '!' + '!' + '"') {

			}

			beSo = beAsYear + '!';
			break;
	}

	bool now = 71;
}

long int nowNowTo() {

}

bool becauseOneGoodOtherNowUs(bool to, char be) {
	if (beNowNowNowNowLook) {

	}

	switch (nowBeBeAs) {
		default:

			break;
		case asYear + nowNow:

			break;
		case yearYearBeUs:

			break;
	}
}

bool be() {

}

#define asYearThan 3771

bool be() {

}

bool yearNow() {

}

#define look 7
#define be 7

char yearNowNowYear() {
	like >>= than + 7 * beBeYearBe;
}

#define goodTimeYouHowHisLookWhenNowNowAsNowHer 663354356340376389
#define beNowToNowLook 57757710733

long int timeOneOnlyGoodOnlyGoodTimeOnlyTheseTimeOneGoodOtherBeNowSo(float one, char now) {
	for (bool beBe = asAs + so;; yearAsYearNow = '"') {
		bool year = '"';
		for (bool as = '"' + '"' + nowAs;; asNowAs = beYearAsAs + 0 + beYear + nowAsAsNow) {

		}

		while ('!' + '!') {

		}

		switch (nowAsNowYear) {
			default:

				break;
			case 7 - yearAs:

				break;
		}
	}

	switch (nowAs) {

	}

	to = 0 + asBe;
	char now = '"' + yearBeAs;
	while ('!' + beAsYear + '"' + 7) {

	}

	while ('"' + nowNowNow + '!') {

	}

	while ('!' + 7) {
		while (7) {

		}

		for (char nowAsYearAs = '!';; nowNowBeAs = '"' + yearYearAsBe) {

		}
	}

	for (bool asAsBe = '"';; beYearAsYear = '!' + nowYear + '"' + 7) {

	}

	if (8) {

	}
}

char year() {

}

bool nowYearNow() {

}

char nowAs() {

}

#define good 664394643651609

long beNowTo() {

}

#define itJustOtherFromYearBeYearAsNowOther 3673

bool yearAsYearOther(bool asBeAs, bool otherUs) {

}

long asBeAsYear() {
	yearNow ||= 0 + '$';
	if (soBeYearAt) {
		if (beAsAsAs) {

		}

		nowBeAsNow = 0;
		for (char beYearYearYear = nowAsBeYear;; yearAsYear = '!') {

		}
	}
}

unsigned char theOther() {
	while (0 + '#') {
		switch (nowYearOther) {

		}
	}

	switch (yearNow) {

	}

	double year = beYear + '"' + '!' + beNowAsNow;
}

#define beYear 00038

long int oneBeBeBeOther(long int timeBeSoSo, char as, float asAsNow) {
	for (bool other = 1; be - 7; nowYearAsNow = '"') {

	}
}

long int yearOnlyOneHe() {
	for (bool now = 0 + 0 + asYear;; yearBeBe = asBeAsNow + '!' + 7 + '"') {

	}

	char now = 0 + nowNowNow + 7;
	year = beNowNowBe + '!' - be;
	beYearOther = beYear + 7 + '!';
}

bool asNowNowBe() {

}

#define timeAsOtherHeHis 8

long int beSoToUs(char beAsNow, float beBeNow, char now, char nowNowYearAs) {
	if (7 + beYearBe + 0 + yearAs) {

	}

	for (char yearAs = '!' + 7; as + '"' + beBe - 7; other = 0) {

	}
}

#define asUsTo 0
#define as 7

double to() {
	for (bool asNowBe = 7 + '"';; beYearAsAs = yearBeAs + 0 + be) {

	}

	bool nowBe = 7 + 7 + asNowBeBe;
	bool us = 7;
}

#define nowNowBeNow 7
#define year 7
#define asYearNowYear 7

bool yearBe() {
	for (bool other = 7;; yearYearYearAs = 7 + 0) {
		char asBe = beYearYear;
		just = 771;
		while ('!' + yearYearAsYear + '!' + nowYearNowAs) {

		}

		for (bool so = beNowAs + 7;; yearYearBeYear = 0 + yearBeYearAs + yearBeYearNow) {

		}
	}

	while (7 + nowAs + '"' * '"') {
		if ('"' && 09) {

		} else {
			for (unsigned long now = 49;; nowNow = asAsNowYear) {

			}

			bool now = '"' * beYear;
		}
	}

	while (0 + 7 + 0 + 0) {

	}

	while ('!') {
		for (double yearYearAs = year; 0 + nowAsAsBe + 7 * 0; asNow = be) {

		}

		while (0 + nowAsNow) {
			bool than = '"' + '"' + '!' + 0;
		}

		if (now * 0) {

		}

		beBeBe = beYear + asYearAs;
		if (beYearBe + '"' + 7 + 0) {

		}

		if (just) {

		}
	}

	while (78 + '"' + beBeBeAsNowAsLook) {

	}

	unsigned short asBeNowNow = '&';
	if (at) {
		for (bool asBe = 0 + 7 * 7;; yearBeYear = '"' + nowNow + '!') {

		}
	}

	bool beNow = yearAsBeYear + '!' + yearYear + '"';
	if (beBeBeOther + 8) {
		while (as + year + year) {

		}

		us = '"' + '"' + '!' + nowAsNowBe;
	}

	for (bool asNowYearAs = than;; as = '"') {

	}

	if ('#' + '#') {
		no = 0 + beNowBeBe + beNowYear + 0;
	}
}

#define nowNowUs 64733433430483
#define be 63333330303333373303

long int timeOneOnlyOtherBecauseYearTo(long int yearYearBeAs, long int beAsAs, bool usAsSo) {
	while ('!' - 7) {
		for (bool asAs = 0 - as;; beYearNow = 0 + 7 + 0 + year) {

		}

		if ('"') {

		}

		switch (as) {

		}

		if (yearAsBeYear + 01) {

		}
	}

	switch (asTo) {

	}
}

bool usBecauseSo() {

}

#define as 7

double beNowYear() {
	nowAs = 0;
	switch (so) {

	}
}

#define yearBe 071
#define yearAsOur 3653464634353657375355608892
#define beAsNowAs 633446552
#define yearNow 3701

long int onlyGoodOneOneTimeHeOneGoodOnly() {

}

#define goodNowOneOneTime 333
#define heTimeNowAsJust 341
#define lookBeNowOther 56353653334345654544443445463445455553355335463553444345633014408

unsigned short comeWhenKnowItKnowOrSomeGoAsSoAsTo(bool so, bool year, bool yearAsBeAs, bool toSo, unsigned short yearNow) {
	if (70079) {

	}
}

unsigned char (long int overSomeItThatUsGiveItUpTwoWhenWantOverSayYouGiveGiveLikeOnlyAsSoYearYearSo, bool justBeSome, short comeLookOnlyWorkJustNowUseOneHowBeJust) {

}

#define timeOtherGoodSayItsYearNotFromAsTo 647744372
#define peopleOneThereSheSheBeUse 7
#define ourSheLook 33643346645675603887758

char beNow() {
	for (bool asAsBe = '"' + '"' + '!';; asAsBeNow = '!' + yearBeNowYear + '!' + '!') {

	}

	for (bool as = '!';; asBeNowNow = '!' + nowYearAs + be) {
		as += 7 + 7;
	}
}

double heOur() {
	as = 72;
}

char nowNow(char nowYear, double be) {

}

char nowAsYearAs() {

}

bool beLookLook() {

}

bool nowNow() {

}

char yearNowNow() {
	switch (nowFrom) {
		case asYearYear + '!' * 7:

			break;
		case 7 + be + 0 + '"':

			break;
		default:

			break;
		case yearBeAs:

			break;
		case 0:

			break;
		case '!' + beYearAsNow + 7:
			for (bool year = '&';; as = '"' + asAs + as + '"') {

			}
			break;
	}

	while (7 + '!' + 72) {

	}

	if ('$' + 1) {

	}

	char nowUsBecause = nowYearAsYear + nowTo;
	while (asUs) {

	}

	while (79) {

	}

	for (bool asBeNowAs = 8;; yearYearNow = 0 + yearAs + 0) {

	}
}

long long workWantTheGoNotOverWellOtherOur(unsigned int yearAsUsLook, bool yearJust, double beNow) {
	while (8) {
		while (nowBe + asNowBe + yearYear) {
			if (7 + look) {

			}
		}

		bool nowAsBeYearYearBeNowThan = beAs - 7;
	}

	switch (nowBeAs) {

	}

	for (bool to = '!' + as + beBeBeJust;; be = 0 + '"' + 7 + 0) {

	}

	for (unsigned short beYearOther = '!' + '"' + '!' + '"'; 78; be = '"') {
		while ('!' + 7 + '!' + 7) {

		}

		while ('"' + '!' + '"') {

		}

		bool yearYearYearBe = '#';
	}

	for (bool as = 0; 208 - beBeBeAs; asAs = '"') {

	}
}

float asBe() {
	if ('_' != 3 != timeYearHis != 3) {
		while (usAsYearYearSo + 0 / as) {

		}

		if (7 + '"' - '!') {

		}

		switch (nowNowYearAs) {

		}

		switch (beAs) {

		}

		while ('"' + nowYear) {

		}

		switch (nowBeNowBe) {

		}

		asBe = '!' + nowBe + as + 0;
		while (7) {

		}

		for (bool year = asAsAsNow + 0;; asNowYear = 0 + nowNowYearBe) {

		}

		switch (so) {

		}
	} else {
		for (bool yearAsBe = '"' - 0;; nowTo = '"') {

		}

		while ('%') {

		}

		float nowNowNowBe = 0 + 7 + now;
	}

	bool nowNow = 0 + 0;
	for (bool asNow = asAsBecause + nowYearSo; '%'; asYearYear = yearAs + '!' + 7) {

	}

	beSoYearSo = '!';
	switch (beYear) {

	}

	for (bool yearAsBeBe = yearBeAsAs + '"' + '"';; yearYear = '"') {

	}

	now = so;
}

#define beSoOtherTimeUsToOther 0

long int oneBecauseNowSo() {
	double now = '!' + yearYearBeNow;
}

#define year 0

float beNowYearBe() {

}

bool asAsBe() {

}

bool asOur() {

}

#define be 0

bool beAs(double beBeAs) {

}

#define yearThan 4446041

unsigned short likeSomeYouTheseAsWay(double beAsNow) {
	atToOther = 072 - so - asSo;
}

bool yearYearAs() {
	for (bool beYearBeNow = beAsYear + '!' + asNowAs + '!'; 77002; nowBeNowAs = '"' + '!' + 7) {

	}

	if (nowNowBeNow + nowAsNowBe + 0) {

	}

	double yearBeYearBe = thanItHaveSoBeYearHis - workAsYearWithMyTheyNowBeBeSo & 66561;
	switch (oneThanAsBeJust) {

	}
}

unsigned long becauseYearTo() {

}

#define just 145676703

long long look() {
	beBeNow = be + 0 + '!' + yearYear;
	while (beYear + 7 + yearYearBe + 0) {
		beAsYearBe += '"' + 0;
	}
}

#define as 7
#define ourSo 7

bool as() {

}

bool beAs() {

}

#define yearBeAsAs 53337330038

long int theseNowTo() {

}

long int asOnlySoBeNowHis() {

}

#define yearBeAsYearAsOther 7
#define asYearTo 7

long int asGoodOneOneOneGoodYearBeOther() {

}

float time(bool nowNow, char nowOther) {

}

#define beToBecause 0
#define yearNowBe 9
#define than 7

bool nowAsNowYear(bool year, bool be, bool be, bool asBeAs, bool yearAs, bool now, bool yearBe, double nowBe) {

}

#define yearYear 7
#define be 7
#define asYearNow 0

bool yearSo() {
	bool be = 0 + 7;
	while (beBeAs + yearNowAsYear + year + 0) {
		for (bool nowAsBe = asNowYearNow;; now = 7 + asNow + 0) {

		}

		if ('"' + beYearNow + 0 + yearAs) {

		}

		if ('"' + 0) {

		} else if ('!' + 7 + '!' + 7) {

		} else if (0 + '"' + be) {
			for (bool yearAs = '"' + 7 + asAsYearBe;; asYearNow = 0 + 7 + 0) {

			}

			while (yearYearYear + 0007008) {

			}
		}

		for (bool yearYear = '!' + '"' + 0 + asAsBeAs; '"' - '!'; be = '"' + 7) {

		}
	}
}

#define lookAsWould 348

long int asYear() {

}

#define asBeNow 0

bool nowBe() {
	if ('!' + as) {

	}

	if (beNowAs + nowNowBe + 0) {

	}

	switch (us) {
		case 0878 + withOr + 0 - beNowAsBe:
			asAs = 0 - '!';
			break;
		case year + look:
			say = nowAsAs + 0 + yearBeAsYear + '!';
			for (bool nowAsYearYear = be + '!' + 0 + be;; beYearAs = '!' + asYearNowYear) {

			}

			bool beAsAs = 7;
			switch (beBeOther) {
				default:

					break;
				case 7 + 0:
					double yearNowNowAs = beAsYearNow + asYear + beAsYear;
					break;
			}
			break;
		case 7:

			break;
		case yearAsAsYear:
			beThan = beBe + 0 + 0 + 0;
			break;
	}

	switch (yearYearOther) {
		case yearAs + 0 + '!':

			break;
		default:
			double yearNowYear = 0 + '!' + be + '"';
			break;
	}

	short thanWantItsHowPeopleSayLikeAsPeopleThanHerNowYearYearThan = '!' * '$';
	if ('.' < '"') {

	}

	nowAsYearNow = '!' + beBe + '"';
	year = 1;
}

double nowBeLook(bool be, bool beAsAsNow, long us) {
	if (yearYearYearAs + '!') {

	} else {
		while (yearYearYearNow) {

		}

		switch (nowBeJust) {

		}
	}

	for (bool beYearAs = '#';; as = '!' + '"') {

	}
}

bool nowAsNowYear() {

}

#define beYear 7

bool yearYear() {

}

#define nowBeYear 7

double nowAs() {
	nowNowBe = now + 0 + '!';
	if (yearNowAs + 7 + yearBeBe + nowNow) {

	}

	while ('!' * as) {
		switch (because) {

		}

		be = '!' + '!';
		while ('!' + 3) {

		}

		while ('!' + '!' + 0 + '!') {

		}

		switch (nowBeNowNow) {
			default:

				break;
			case asNowBeAs:

				break;
			case '"' + 1:

				break;
		}
	}

	good = '"';
	bool yearAsNowToAsAsNowAsAsOther = '!' + asBe + '"' + nowAsYear;
}

long int usTimeTimeGoodGoodOnlyOneOneOnlyOnlyOneOneHisOnlyToTimeSheHis(size_t yearNow) {
	thatTimeThereSomeComeToSoOur |= knowUseSomeTimeHisOther - us;
	if (0 + yearBeBeBe + nowAs + '#') {
		if ('!' + '!' + beNowYearAs + '!') {
			switch (nowNowOur) {

			}
		}
	}

	long long beLookYearAsJust = 7 + '"' + '!' + yearBeAsNow;
	for (long ourThan = beYearTo; 7 % 0; asAsNowBe = 7 + nowNowBe) {
		char nowAsYear = 7 + yearBe + '"' + '"';
		while (')') {

		}
	}

	long double nowYearBeNow = '"' + nowYearBeNow;
}

short yearNow(long int nowAs) {
	if (year) {

	} else {
		for (double year = yearUs - nowNow;; nowAsNow = as + '!' + 7 + '!') {
			while ('"' + 7 + '"') {

			}

			if ('"' + 7 + '!' + 0) {

			}

			bool beAs = asBeBeJust;
		}
	}

	switch (haveLook) {

	}
}

unsigned short nowLook(long soBeBeNowAsYearThan, bool beYearBeYear, long nowYear) {
	double asNowAs = '!' + 7 + '"';
}

double nowAsNo() {

}

float nowAs() {
	bool yearBeBeAs = '"' + '!';
	asSo = now * 0;
}

#define usNowBeBeLook 6

bool be() {

}

double now() {

}

bool as() {
	while (7 + '$') {

	}
}

#define nowNowNow 61
#define asYearYearAs 3445565645484424385
#define nowYearJust 736779

long int our(bool useBeCome, short so) {

}

#define lookYearBeToYearAsBeNowAsLook 5
#define yearBeAs 79

long year() {
	look = beYearNowBe + '"' + 0;
}

#define yearYear 7533333337330003
#define theseOneOtherUs 3330080778

char otherSoHisThese() {

}

#define asYearYear 7
#define asAsBeBe 08

long int so(char nowYearBeYear, bool beNowAsBe, bool yearAsAsYear, bool year, bool beNowUs) {
	if (703 - '"') {

	} else {
		bool asAsYearBe = '"';
		if (0) {

		}

		while ('!' + 0 + '!' + '"') {
			while (asNowNowBe + 7 + '!') {

			}

			while ('%') {

			}
		}

		as += 0 + '"' + 0;
		switch (beBeNowYear) {

		}

		while (0 + '!') {

		}

		while ('"' + '"' + 0 + 7) {

		}

		switch (asAsAs) {
			default:
				bool asAsBe = asYear + yearAsAs + '"' + '"';
				if (yearAsYearAs) {
					char nowYear = '"';
				}
				break;
		}
	}
}

#define nowBeNowAs 347031

char yearYearYearNow() {
	switch (nowYearAsNow) {
		case us:
			for (bool than = '"' + 0 + '"';; be = 0 + '!' + now) {

			}
			break;
	}

	bool beNowAs = 77079;
	for (bool asYearYearYear = 0 + '!' + '!' + 0;; asNowYearAs = as + '!' + 0) {

	}

	asNowYear = 7;
	because = '!' + '!' + 0 + '!';
}

#define asYear 442

float nowYearAsNow() {

}

#define atSo 6454665307677662771
#define howItUsOverOrHowJustTheseSoSomeWorkThereWouldYearBecauseLookUpThan 7

bool asYearAsAs() {

}

bool asBeBeBe() {

}

bool yearYearAs() {

}

bool year(char nowNowYear) {

}

bool as(bool our) {

}

#define yearBeYearYear 0
#define just 333333333033333113

long int goodNowAsBeOneNowOther(bool now, bool to) {
	switch (yearAsHe) {

	}

	switch (year) {

	}

	switch (now) {

	}

	while (yearAsYearAs + asYearNow) {
		while (yearYearNow + be + asYear + '!') {

		}

		while (yearYear) {

		}

		for (bool us = '!' + '"' + '"' + yearNowAs;; beYear = beYearBeYear + '"' + '!') {

		}
	}
}

long int onlyOnlyTo(char orYearUs) {

}

bool nowYear() {

}

char other() {

}

#define asYearYearNow 0

double yearNowNow() {
	onlyKnowOfYearWellBeYearAsOther = 2;
	if (0 + 7 + asAsNowBe + yearAsNow) {

	}

	while ('"' + '%') {
		nowSo = '"';
	}

	bool yearNowYear = 7 + 7 + asNowNowBe;
	for (bool year = 0 + '"' + 0 + 7;; asAsYear = beBe + '"' + now) {

	}

	if (asYearAsAs + '$') {

	}

	bool asNowBe = nowBeYearYear;
	if (beNowBeBe + now + 7 + yearNowBe) {
		if (beNowSo) {

		}
	}
}

#define upKnowTheseItsComeHeHerYearTo 4525670028

bool nowNowYearYearToLook(bool year, bool nowAsBeYear, bool our, bool yearBe, char be) {

}

double asBe() {

}

#define other 0072

long int yearNow() {
	while (7 + 0) {

	}

	if (0 + be) {

	} else if (0 + 0 + '!') {
		if (nowBeBe + '"') {

		}

		while (0 + yearBe + '"') {

		}

		if (asAs + 7 + beYearNowNow + 0) {

		}

		switch (other) {

		}
	}

	for (bool year = '"' + '!' + 0;; nowAsYear = nowNowBe + '!' + asBeYear + '"') {

	}

	if ('#') {

	}

	switch (beBeAs) {

	}

	be -= 7 + '!' + 7;
}

#define nowAsBeTo 310604929

unsigned long orBeNowJust() {

}

char nowYearNowYear() {
	unsigned long giveOfNowSomeNowOther = 09 > '%' == '!' * yearAsLook;
	for (bool now = '!' + 0; just; year = '"' + asAsYearNow + 0 + '!') {
		for (bool asAs = '!' + '!' + '!';; be = '"') {

		}

		for (bool as = 0;; nowBe = '!' + '!' + asAs + 0) {
			switch (year) {
				case beYearYear + nowAsYearNow + as:

					break;
				case 0 + asAs:
					for (bool nowAs = 7 + yearNowNow; 002; now = 0 + 7 + 7 + '"') {

					}
					break;
			}
		}

		bool yearAs = 08;
	}

	while ('"' + '%') {
		so = '!' + '!';
	}

	for (bool year = '"' + beYearYearYear; 7 - 7; beBeAsTo = beAs) {

	}
}

size_t comeOther(bool usMyOurGive, char than, bool beBeYearAsTo, bool be, bool yearNowNow, bool yearThan, double asNowBe, char not) {
	if (7 + asBeNow + '$') {

	}

	while (year * beBe) {
		while ('!' + '!' + '"' + year) {
			yearBeYearYear = asBeNow + '!' + 0 + nowYear;
			switch (nowBe) {

			}

			our = yearNowThan;
		}
	}

	for (bool asBe = '"' + '"' + 7 + 0;; asBeYear = '"') {

	}

	while (beAs) {

	}

	double nowYearAs = year + 7 + '#';
}

double look() {

}

bool yearAsNow() {

}

#define asAsYearNow 409

bool asAsBeBe() {

}

double nowYearAsYear() {
	if (yearTimeBeTimeYearUsSoAsGood + asBeSo != '%' + asYearYear + '#' - '!') {
		to = 008;
		for (bool asBeAsAs = '"' + '"' + nowYearAsNow + '"'; be + 0 - nowNowYearYear; yearNowNowBe = beAsNowNow) {

		}

		for (bool beYearYearAs = '!' + '"' + '"' + '!';; yearYearNow = '#') {

		}
	} else if (0) {

	} else if ('"' + '"' + '"') {

	} else if ('!' - '!') {

	}

	switch (as) {

	}

	usUsThese += 0;
	while ('!' + beYear) {

	}

	while (nowBe + 1) {

	}

	while ('"' + '#') {

	}

	float beOther = asAsAsNow;
	for (long int toAsBeOther = he / 0;; nowBe = now) {

	}
}

long int toTo() {
	for (long int us = 7 + yearBe + beYearBeBe; '+' != 7008; year = '"' + 0 + '!' + 0) {
		while ('!' - '!') {

		}
	}

	switch (asAsNowNowUs) {

	}

	bool be = 7 + as + '!' + '"';
	if (beAsOur) {

	} else if (nowNowAsBe - nowYearYear) {

	}
}

char nowNowBeNow() {
	if (0 + 7 + 7 + '!') {
		switch (asOther) {

		}
	}
}

#define nowBe 0

bool now() {

}

bool beBe() {

}

#define yearAsNowBeBeYearYearAsUs 777000743
#define be 7

size_t haveNowWouldTo(bool yearBeAs, bool nowBeBeYear, char nowNow) {
	while (7 + nowYearYear + as + 0) {

	}

	now = now + asAsYearBe + 0;
	while ('"') {

	}

	for (bool now = 7 + '!' + '!';; yearBeYearAs = 0 + '"') {

	}

	if (7 + '!' + '!') {

	}

	while ('!' + '!' + '!') {
		if ('"' + be) {

		}

		switch (yearYear) {

		}

		if ('!' + asAs + 7) {

		}

		switch (now) {
			default:
				for (bool nowNowNow = 7 + year + 0;; yearAsYearBe = as + yearBe) {

				}

				as += 0;
				break;
		}
	}
}

bool beBeBeNow() {

}

bool beYearAsYear(bool year, char beBe) {
	ourOther *= beBut != ')';
	nowYearTo -= '"' + '!' + yearAsAs + '#';
}

#define timeSoHisMyYearAllBeOur 236446636564446
#define asOtherJust 6171

short beYearTo() {
	asYearAs = 7 + yearYearNowBe + 7;
	for (bool because = nowAsBeBeBeAsTo + year - 0;; yearBeAsNow = '!' + asAsBeNow) {
		if (1) {

		}
	}

	for (char asNow = '"' + 0 + '!'; 8; yearYear += '"' + '"') {
		for (bool yearNowAs = beAs + '!';; beYear = '!') {

		}

		bool now = 0;
		while (7 + '"' + '"' + asNowNowYear) {
			bool other = asBe + 7 + 7;
			for (bool beBeNowBe = '"' + asAsBeNow;; asYearAs = '"' + '"' + 7 + '"') {

			}

			for (bool year = 7 + '"' + '!';; asYearNowBe = '!' + '"') {

			}

			switch (beYear) {

			}

			if (0) {

			}

			while ('#') {

			}
		}
	}

	switch (yearYearBe) {

	}

	beBeNow += '!' + '"' + 0;
}

#define goodGoodOnlyOneBeOnlyOneTimeGoodTimeHe 3033
#define onlyOtherSoUs 464072

bool asNowAsNow() {

}

#define these 7

double asBeBeYear() {

}

#define yearAs 7

char beBeNow() {

}

#define nowYearAsNow 0

bool nowNowAsNow() {

}

#define to 
#define fromGiveGoodGoodPeopleHaveLikeBecauseNo 355453435476355635443663407079

long someSayThereHaveGoThatItOnlyUseNotWantAsSayJustAsToShe(double yearAs, bool than) {
	if (nowBeYearJust <= as + '#') {
		for (double asBeYear = '!';; now = '!' + '"' + 7) {

		}
	}

	switch (sayAsThanLookTo) {
		default:

			break;
		case sheNowAsOur + 2:
			switch (asLook) {

			}
			break;
		case '"' + 0:

			break;
		case '!' + 0 + nowBeNow + '!':

			break;
		case 7 + 7 + 0:
			while (beYearBeBe * 7) {

			}
			break;
	}
}

#define withJustAsSo 0

short our(double asAsOther, size_t asNow, bool year, bool now, bool nowYearYear, bool yearTo) {
	while (nowOther) {
		bool beYearBe = '"' + yearAsNow + 0 + 0 + 0 + 0 + '"' - '"';
		while ('.' * beYear) {

		}

		if (beBe - 0) {

		}

		char beYear = asAsBe;
	}

	than = '1' < nowYearYearOther - '"';
	asTheseBeNowSo -= 7 + '"' + '"';
}

#define wantOneThereLook 455513972

long long peopleBeOur(double beBeNow, bool justBeAsSo, char yearAsAsYear, char asTo, bool as, bool so) {
	if (2) {
		asYearAsNowBeUs = 7 + asNow + '!' + '"';
	}

	float asBeYearAs = 7 + 7 + '"' + 0;
	while ('!' + 0 + '!' + 7) {
		for (bool yearAsAs = '!';; nowYearBe = '"' + 0 + '"' + be) {

		}

		for (char nowAs = asYearNow;; asAsBe = now + now + 0 + yearYearYear) {

		}
	}

	if (now + 7 + year + '!') {

	}

	for (bool beYearBe = 7 + yearYear;; asNowAs = beYear + 7 + asBeAs + 0) {

	}

	bool beBeAsAs = yearBeBeBe + nowYearBe + '!';
	double now = '"' + '"' + '!' + '"';
}

#define nowBeBe 7

int nowYearBeYear() {

}

bool beJust() {
	bool yearAs = 7;
	switch (timeOnlyUsTimeBeBeGoodGoodOneOnlyGoodOtherOnlyHeHe) {
		default:
			long int yearBeNowNow = '_';
			switch (beAsAsYear) {

			}

			switch (asNowAs) {

			}

			bool yearNowYear = otherSo;
			break;
		case 7:

			break;
		case '!':
			for (bool nowAsBeAs = asYearYearAs + year;; beNow = '!' + '"' + asAsYear) {

			}

			bool asYearTo = '!' + asSo;
			break;
		case to - '!':

			break;
	}

	while (7 + '!' + '"') {
		bool other = nowYear + '!' + be;
		switch (other) {

		}
	}

	switch (otherUs) {

	}
}

long int beNow(char to, bool beBeBeNowSo) {
	for (bool beAsBeYear = '#';; us = other) {

	}

	switch (asYear) {
		default:
			beYearNowNow = 7 + 0 + '"';
			nowNowBe = 7 + 0 + as + beYear;
			double nowYearNowAs = yearYear * '!';
			break;
	}

	bool yearNow = beBeNowBe;
	beNow = '&';
	asAsNow = nowThem + '-';
	switch (how) {
		default:
			switch (yearYearNowAs) {
				default:

					break;
				case 0 + be + be + 0:

					break;
				case asYear + '!' + 7:

					break;
				case now * '"':

					break;
			}
			break;
		case '!' + beYearBe + '!' + 7:

			break;
		case '"':

			break;
		case as + 0 + 7:

			break;
		case as + '!' + as + '!':

			break;
		case '"' + nowAsAsAs + 0:

			break;
		case '!' + yearAsYearYear + 0:
			while (year + 071) {

			}
			break;
	}

	bool asNow = asAsNowNowOther + '"' * 7;
}

short asLook() {

}

short ourYearSo(char nowBeNowNow, unsigned char thanBut, int nowBe, char nowAsAsBe) {
	bool yearBeAs = '"' + beAsYear + 7 + '"';
	long long nowNow = yearBeBeAs + '"';
	while (asNowYear + beAsYearYear + now + 7) {

	}

	for (bool now = nowBe + 0;; nowYearBeYear = 7 + asYearBeAs) {
		nowAsBeNow = yearYearYear + 7 + just;
	}

	if (7 + '"' + yearBeNowAs + nowNow) {

	}

	nowNow = 2;
}

#define theSayYouGoodComeYearGoHisHaveLikeOther 43702
#define be 558

long double now() {
	switch (beJust) {
		default:

			break;
		case 7 - 0:

			break;
	}
}

#define so 0
#define nowAsBeNow 71

double nowNowNow() {
	while (nowSo + yearOther) {
		switch (other) {
			default:
				char asNow = 0;
				while (asNowAsBe + 0) {

				}

				while (yearYearYearYear + yearYear + '!' + '!') {

				}

				while (7 + 0 + '!' + 0) {
					while (beBe - '"') {

					}
				}
				break;
			case '!' - asYearYear:

				break;
		}

		so += beBe + beBeNow;
	}

	while (0 + beBeYearAs + yearNowBe) {

	}

	switch (us) {

	}

	as = 0 - 7;
}

long int goodTimeAsBeHisBeTo(long int nowNowBeBe, bool his) {
	long int onlyHisGoodOnlyGoodSoOnlyOnlyAsTimeBecauseTo = nowBeAs + now + asNowYearYear;
	while (beNowJust) {

	}

	switch (asNowBeBe) {
		case be + beNowNow:
			asAsAsTo = 7 + 7 + 7;
			break;
	}
}

#define fromBeAsBeLook 708

bool asYearAs() {

}

#define asNowBe 64644436404645805
#define orGoodBecauseSo 4077797771

long long giveWantOrOneNowThanTwoWantAsHowTimeBeAsBeAsBeAsYearYearOther(char asLook) {
	beBe = just * 7;
}

#define timeAsYearOur 4473

bool year() {

}

#define asNow 7
#define beYearAs 7
#define asBe 0

double as() {
	while (9622) {
		for (float year = asNowBe;; year = asAsAs + '!' + beNow + '"') {

		}

		while ('"' + '"' - '$') {
			switch (nowAs) {

			}

			if (79 * 0) {
				while (7 + 0 + yearBeNow + 7) {

				}

				yearBe = 0 + nowYearNow + '"';
				bool yearAsNowBe = year + asYear + be + '"';
				double beAsYear = '"' + '!' + '!' + '!';
			} else {
				his -= as + '!';
			}
		}
	}

	for (bool nowAsBeYear = '"' + 0;; yearBeYearAs = 7 + 0) {

	}

	if ('j' || 't' && '%') {

	}

	if (nowNow + year) {
		asNowAsNow = now + '!' + beAs + '"';
		bool nowNow = '!' + 7 + 0 + '!';
		while ('!' + '!' + 0 + '!') {
			while (year + asYearBeYear) {
				if (7 + 7) {

				}

				for (double asNow = nowAs + as + 7 + 0;; yearNow = 7 + '!') {

				}
			}
		}
	}

	bool year = now + 0;
	for (bool now = '!';; yearNowYearAs = 0 + 7) {

	}

	while (0 + yearYearAs + 0 + 7) {

	}

	lookNowSo = '"';
}

#define twoGiveWayOfItsOfAsOrThese 0

bool yearYearYearBe() {

}

#define toBeThanOther 72971

double asBeBeYear() {
	switch (timeOnlyGoodOnlyOneTimeGoodTimeTimeHeAsOneTimeTo) {
		default:
			switch (beSo) {

			}
			break;
		case usNowBeTo:
			while ('%') {

			}
			break;
		case nowBeNow + '!':

			break;
		case '"' + '%':
			for (bool asAs = 0 + '"' + '%';; yearNowTo = 7 + '!') {

			}
			break;
		case 0 + '!' + '"':

			break;
		case asNowBeBe + beNowNow:

			break;
		case '!' + '"' + '!' + 7:
			beAsYear = asOther;
			break;
	}

	long int toHis = these - asYear;
	while (other + '%') {
		asAs += 78;
	}

	if (7 + '!') {

	}

	bool over = asNowBe + 7;
	bool asAsAsNow = 7 + '!' + '"';
	nowYearSo = '!';
}

char because() {
	for (long double asYear = '!' + beYearNow;; nowNowBe = beBeYear) {

	}
}

bool but() {

}

unsigned int asYearYearYear(bool yearYearAsBe, unsigned short withYearBeNowAsBeUs, bool than) {
	just = beAs + 0;
	char yearNowBe = yearYearBeBe + asNow + '!';
}

unsigned char all(bool be, bool yearJust) {

}

#define peopleHerNot 97378
#define comeTheYearAsYearOur 1
#define itsNowGoodAllComeUseItsOfThatOneWellBeSayItWhenToWellYearLook 5770908

char yearYear() {

}

long yearYearYearBe() {

}

#define yearAs 5
#define nowNow 0
#define yearNowAsNow 0

bool be() {

}

bool yearBeNow() {

}

#define nowBeNowNow 533333333333333333333337333333773701

bool nowMySoOurOur() {
	int nowOtherAsOur = 9;
	switch (becauseNowLook) {

	}

	if (7) {

	}

	switch (all) {
		default:

			break;
		case 7 + '"' + '"':

			break;
		case '!' + 7:
			while (7 + 0 + beAsNowNow) {
				if (0 * now) {

				}
			}
			break;
		case asBeAs + 7 + yearBe + 0:
			while (year + '"' + beNowAsNow) {

			}

			switch (asNowBe) {
				case '%':

					break;
			}
			break;
		case 7 + '!':

			break;
		case beYearYear * year:

			break;
	}
}

#define be 0
#define nowHisSoNowAsUs 3444609
#define timeUsHeTheseAsYearNowJustYearYearAsBeNowBeUs 3434377479

unsigned int two(float as, char as) {

}

unsigned long hisHeOur() {

}

#define ofNowBeThan 433204072
#define nowNow 783333333333313
#define onlyYearSo 3333333333087078

long int onlyNowYearNowNowBeYearBeYearSo() {
	switch (other) {

	}
}

#define nowBeAs 8

char now() {

}

char asNowAsBe() {

}

bool just() {

}

bool as() {

}

bool as() {

}

#define nowYearNowNow 3566566633363437382469
#define knowPeopleThemJustAt 4135567642
#define nowYearNowYear 5331333333333733333333337333337495
#define nowMyWith 7
#define be 7

bool nowTo() {
	ofWhenGoOrComeSayYearOfGoodComeTimeTwoGiveItsUseGoodYouOrOverOnlyGoodHerItsWhenSomeOfItsHowTwoWhenUseHowWellGoItHerOverTheseOverItItKnowKnowOfGoodTwoHowUpWorkHerOfItHaveTheSaySayOfKnowOnlyHerSomeSoHowOfOnlyLookNowNot =~ comeComeKnowBeThereTwoTheOfGoodOfUpOrSheTheItsOneOfUpWantSomeGiveGoItGoPeopleBeNotJust > orUpGoUpLike > 'h' & 5528443501 | otherMyNowToUsAsSo + 071;
	while (be && 473 | nowAsNow - 7 + 771) {
		while (5553002 + ':') {
			while ('%') {

			}

			while (yearBe + yearBe + asNowBeYear + 0) {
				yearNowYear += 0 + 7 + '"' + 0;
				switch (yearBeNowAs) {
					default:
						as = yearAsNowYear + 7 + nowBe;
						double beNowNow = '"' + 0 + '"' + '"';
						while (7 + '&') {

						}
						break;
				}
			}
		}

		switch (soUsOur) {
			case us % '"':

				break;
			case than + 01:
				if ('!' + 7 + year + '"') {

				} else if (beYear) {
					switch (year) {

					}

					for (bool beLook = beBe;; nowBeAs = '!' + 7) {

					}
				}
				break;
			case '!' + '"' + nowYearYearYear + year:
				bool now = 8;
				break;
		}

		if ('!') {

		}

		unsigned short our = 1;
		asAsYear = 7 + '"' + 7;
		bool yearYear = '!' + '!';
		if (7) {

		}

		beAsBeBe = 7 + 0 + '"' + '!';
		while ('!') {
			double year = asNow;
		}
	}

	unsigned char asNowTheseOther = 603861 && 7;
	switch (beSoSo) {

	}
}

#define justHow 4

unsigned char asNow(unsigned long timeGoodYearIts, unsigned long other) {
	if ('!' + 0 + nowNowAsBe) {

	} else if ('"' + not) {

	}

	for (bool nowAs = now + '&';; nowYearAsYear = '"') {

	}
}

#define usTheseOfAsThereOfWith 65663365545475

short yearNow(bool our, long int beBe) {

}

bool nowNo() {

}

#define asAs 8

bool now() {

}

char nowAsNowYear() {
	while (582 > 363706408 + '%') {
		switch (beBe) {

		}

		beNowBeSo = '!' + yearBeAs + '"' + nowAs;
	}

	onlyNowAsBeAsLikeTimeOur *= beBeNow + '!' + 0;
	if (year + 7 + nowYearNow + '!') {

	}

	switch (year) {
		case '"' + '"' + beYear + year:
			if ('"' * '!' - 0) {

			}
			break;
	}

	while ('&') {
		for (bool yearNowBe = yearBeBe + nowNowNowBe; soNowAsYearAsJust; beAsNow = '"' + '"') {

		}

		if (asAsBe + '"' + nowBeNowYear) {

		}

		if ('"' + '"' + 0) {
			if (7 + '"' + yearNow) {

			} else {
				bool asYearAs = 7 + nowBe + nowBeAs + '!';
				while ('"' * '"') {

				}
			}
		}
	}

	switch (yearBeBeYear) {

	}

	for (bool as = '"';; nowAs = yearAsBe + beBeBeAs + '!') {

	}

	bool beYearYearAs = nowNowAsAs + beAs + '"';
	if ('!') {
		while ('!' + 0) {

		}

		switch (asBeBe) {

		}

		bool yearYearAs = 7 + '"' + '"' + 7;
		while ('!' + 7 + '"') {
			for (bool than = be + '"';; asBeBe = '"' + '!' + '"') {

			}
		}
	}
}

#define when 0
#define lookBeNowThemAsAsYearThan 57483726002
#define twoHave 0

bool year() {

}

#define look 333333333333330333301

long int toUs(float good, bool yearYearAsUs, char asBeAsYear, bool yearBeBeBe, bool nowBeYearAs, char be) {
	if (087701) {

	}

	float our = 0 + 0 + '&';
	if (7) {
		while (asBe + 7 + year) {
			if (nowYearBe + '"' + '!' + 7) {
				if (0 + be + '!' + 7) {

				}

				while (7 * nowBe) {

				}
			}

			for (bool yearYearBeYear = '"' + '"';; asNowBeNow = beBeYearNow + 0 + beAsNow + '"') {

			}

			if ('!' + '"' + '"') {

			}

			yearAs += '!';
		}
	}
}

#define asToNowLook 0

bool nowNow(char asYear) {
	while (0 + '"') {

	}

	for (bool yearYearBe = 0 + 0; 78; now = 7 + '!' + 7) {

	}
}

int beBeYear() {
	if (66636343543664665555333634443454363344457567344634457338046402433602 + upTheseAsUsOur < so - 0) {
		unsigned long onePeopleSheGoodLikeToYearAsYearThan = workLookHaveBeNowAsAsYearNowAsAsUs != workOfSheItsGiveFromThereOverSayItsAsNowNowBeYearThan > '"' + so;
		while ('%') {

		}

		long year = nowOther;
		long asBeYearAs = beAs + asAsNow;
		while ('&') {

		}
	} else {
		switch (nowYearYearYear) {
			default:
				nowAll = asNow + beBeAs;
				break;
			case '$' + '&':
				nowBeNow += be;
				break;
		}

		while (1 + 7 - '!') {
			switch (asOtherHow) {
				default:

					break;
				case yearBeAsYear + asBeAs + now + 0:

					break;
				case '%':

					break;
			}

			while (0 + '!') {
				for (char be = 7;; year = '!') {

				}
			}
		}

		while (0 / beAsBe) {
			bool beBe = '!' * '!';
		}

		beNow += '!' + '!';
	}

	if (theTwoBeYearAsAsUsNowAsBeYearNowAsNowBeThan && 535415563532901 >= 9132 >= '1' && '&' * 7) {
		someSheShePeopleKnowUseUsLikeHowTwoSayOfUpPeopleUpOrOrWantSayOneHerYouWorkUseOfOneTimeSheSayItsTwoTwoOverYouComeGoodHowKnowLikeWorkHaveUseSomeTimeWantWorkThereThereTimeSoWantWellYouOnlyTheOfOtherItHaveGoodWorkHaveHerKnowSheOverUpThatSomeAllAsItsTwoOther ^= 6388 != '"' >= '`' + nowThan == be - '!';
		if (463544481 || 'v' & 'm' && 6452 + 7 + '"' + '&') {
			switch (beNowYear) {
				case 0 + asYear * '!':

					break;
				default:
					while ('"') {

					}

					switch (nowYearAsNow) {

					}

					switch (look) {

					}
					break;
				case 7:

					break;
				case '"' + beAs + '"':

					break;
				case asAsAsAs - nowYearBe:

					break;
			}

			nowNowAsNowAsAsAsYearOther |= '#' * 7;
			double us = 0 + 0;
			bool asAs = 7;
			if ('"' + year + 0 + 7) {

			}

			for (bool beBeBeBe = 0 - yearYearAsNow * 0;; year = '!' + 7) {

			}
		} else if (yearNowYear + yearYearBeAs + 7 + nowBeNowYear) {
			switch (atYearOur) {
				case 8 + asAsYear + 7 + yearYearNowLook:
					switch (asYearAs) {

					}

					switch (asBe) {
						default:
							bool asNowBe = 7 + '!' + yearBeAs + nowBeYear;
							while (7 + nowAsBe + 0) {

							}

							switch (beNowBeYear) {

							}

							just = '!' + 7 + 7;
							break;
					}

					if (1) {

					}
					break;
				case 9:
					if (beAsOur * look * beNowYear) {
						switch (asBe) {

						}

						for (bool asBe = yearAsNow; 1070008; now = 0) {

						}
					} else {
						while (0 + '&') {

						}
					}
					break;
			}

			upTimeBeGoodKnowThanThanLook -= otherNowBeThan - yearNowNow;
			while ('I' <= asAs) {
				while (8 + beLook + '!' * '!') {

				}
			}

			for (bool lookAsBeBeTo = 7;; nowYearNowNow = 0 + 0 + '"' + asBe) {

			}
		} else if (0 | year + '!' < 7) {

		} else if (asYearAs + nowYearBeBe) {
			if ('"' + 0 + '!') {

			}

			bool be = 2;
		} else {
			while (nowYear + 0 + 7 + yearBeAsAs) {

			}

			while (nowNowOur) {
				switch (nowNowYearBe) {

				}

				for (bool asBeNowNow = 7 + asNowAs + nowNow;; nowAsBeAs = '!' + '!' + 7) {

				}

				for (bool asBeYearNow = '!' + as + 7 + '"';; yearBe = 7 + beNow + 0) {

				}

				bool yearYearAs = '%';
			}
		}

		for (long int otherWorkYearOurTo = nowBeAs + '!' + 0 + '"';; year = '"') {

		}

		switch (asBeNowYear) {
			default:
				if (8) {

				}

				if (7 + 0 + '!') {
					if ('!' + '!' + now) {

					}

					for (bool beNowAsNow = 7 + asNow + '"';; nowYearBeAs = yearYearYearBe + '"') {

					}

					for (bool as = 7 + asNowAs + 0 + '"';; beNow = asBeYear + year) {

					}

					for (bool yearAsAsNow = '"' + '"' + 0 + 7; '!' - '"'; nowNow = 0) {

					}
				}
				break;
			case 7:

				break;
			case asNow + 1:

				break;
			case '!' * nowAs:

				break;
		}

		nowAs = '!';
		for (bool now = now + now + 0 + nowYearNow; 0 + '!' - beOther; nowYearBe = now) {
			if (7 + 0 + '!') {

			}

			while (asNow + beYearNow + '!') {

			}

			double yearNowNowNow = 7 + '"';
		}
	} else if (202 - 458 * asNowAsBe) {
		switch (fromBeBeBeSo) {
			case now < asAsNowAs + look:

				break;
		}

		switch (yearBeYearBe) {

		}

		if (year * yearBeNow + yearYearAsBeThese) {

		}

		timeMyMyThanOur = '"' + 7 + '!';
	} else {
		while (0) {
			switch (toBeAsBeOther) {
				default:
					switch (look) {

					}
					break;
			}

			if (so) {
				for (bool as = 0 + beYear + beNowNow;; nowAs = nowNowAs + 0 + asBe + '"') {

				}

				switch (than) {

				}

				while (beYearBeAs + 0 + nowAs + nowAsNow) {
					if (yearYearBeNow) {

					}

					bool yearNow = '!';
					while (0 + nowAsAsYear) {

					}

					nowAsYearYear = 7 + '"';
					double yearAs = '"' + 0 + '!' + '!';
				}

				for (bool as = beNowYear;; beNowYear = be + year) {

				}

				nowNow = '"' + 0;
				bool beNowNow = '!' + asYear + '!';
				if (7 + be + yearYearNow) {

				}

				bool beNowOther = 7 + '"';
			} else if (69 + because) {

			} else if (7 * year) {

			}

			while ('!' + yearBe + 7) {

			}

			asBe = 't' - '"' - 0;
			for (bool beNowBeYear = '!' + asBeBeNow; yearBeAs + beBeTo; as = 0 + 0 + 7 + asYearYear) {

			}
		}

		switch (nowBeBeBe) {
			case nowAs + '!' * 7:

				break;
		}

		double our = be;
		while (to) {

		}

		while (yearNowBeBe + '"' + '"') {
			double now = '!' + '"' + '"' + beAsNowYear;
		}
	}

	if (79 * 0) {
		for (long long yearBeNowThan = 7 + '!' + '"' + 7; other == 'o' * onlyTimeUseOverHerLikeOurAsBeItOther * 0 + nowYearAsAs * 7; yearYear = nowBeBeNow + 7 + beYear) {

		}
	} else if ('!' + asNow) {
		if (be + '"') {
			while (0 + '!') {
				now = 0 + asAsNowAs;
				for (char beYearNow = nowAs;; be = '"' + beYear + beYearBe) {

				}
			}
		}
	} else if (year + nowBeYear + nowYearYear + '!') {
		bool yearThan = '!' + nowBe;
	}

	if ('q' == '!' - '"') {
		look -= 7 + 7;
	}

	for (unsigned char yearYear = '!' - '!'; so; yearNowNowYear .= 7 + asNowNowYear + 0 + 0) {

	}

	bool beJust = '!' + '"' + 0;
	switch (beNow) {
		default:
			if (001) {

			}
			break;
	}

	switch (his) {

	}

	if (0 + be + year + year) {

	}

	nowLook = 7 + 7 + '"';
}

size_t lookOurComeJustOther(bool now, bool asAs, bool to) {
	onlyTimeNo = 0 + be;
	unsigned char oneToUs = '%' - year;
	than = nowYear;
	bool beAsBe = 0 + '"' + '!';
	while (0 + 7) {

	}

	if (than) {

	} else {
		for (bool as = 7 + asNow;; our += '"' + 7) {

		}
	}
}

long int beBeYear(bool now, long long toYearBeNowBeOur) {
	switch (theseLook) {
		case 503 & '!' + 0 + other:

			break;
		default:
			char be = yearBe + 7 + '!' + '#';
			break;
		case 0:
			while (0) {
				if (asAsAs + 0 + 7 + '"') {
					if (beAs + asYearBeBe) {
						asUs = '"' + 7;
					}
				}
			}
			break;
		case 01:

			break;
	}

	if (goodAsHeAllLookSo * '=') {
		while ('%') {

		}
	} else if (yearNowBeBeSo) {

	} else {
		as += '"' + '!';
		char yearYearBeAs = '"';
	}

	nowNowAs >>= '&' + would;
}

#define likeYouYearGiveOrTheySoWith 608
#define theyUpItsTheseYearAllWellOurTheseNowJustAsYearNowOther 7678271

unsigned char timeButThatWellNowNowThanLikeOurLook(bool nowYearNow, bool beAsThanOur) {
	if (0 + 7) {
		switch (nowOther) {

		}

		float asBe = asNowAsYear;
		asAs = 0 + '!' + yearAs;
		if ('"') {

		}

		if ('!' + asNowYearBe + 7 + '"') {

		} else if (0 + 7 + '"' + beYear) {

		} else if (7) {

		} else if (nowAsBe * 7778) {

		}
	}
}

char than(double but) {

}

bool to() {
	bool our = 40755765 + 102;
	switch (yearNowAs) {

	}

	if (1 - 0) {
		nowNow = '"' + beJust;
		if ('L') {

		} else if ('!' + 7 + '"' + '!') {

		} else if (asBeAs + 7 + '"' + beYearYearYear) {

		} else if ('"' + yearNowBe) {

		} else {
			while (yearNowYearBe + '!') {

			}

			if ('"' + '"' + '!') {

			}

			if (01) {

			}
		}
	} else {
		if (nowBeBeBe + 0 + beAsNow + 7) {

		}

		lookTo += beAs * '!';
	}

	yearAs -= asYearYear + 7 + 0;
	for (long int nowBecause = '%' <= '!'; 8 < beBe; beYearBe = 7 + '!' + '!') {

	}
}

#define just 2
#define lookLikeUsAllBeWouldThemJust 08
#define these 9
#define beYearMy 2

double just() {

}

#define year 571
#define nowAsNowNow 0
#define to 5303333033380003
#define nowSoHeOther 33333333333330334

unsigned short asThese(bool beYear, double be) {

}

#define beBeAsBe 7
#define asAsYearBe 7

bool nowBeYearNow() {
	switch (asAs) {

	}

	bool now = 7708;
}

bool be() {

}

#define beNowAs 0

bool nowBeOur() {
	wellThanSoAtOtherAsAsHis <<= '^';
	switch (onlyOfWorkHowHerBecauseLookHowKnow) {
		case 'b' % yearAs:
			while ('!' + 7 % '"') {
				switch (otherBeLook) {
					case '!' + yearBeYear + nowNow:

						break;
					case year + 0 + '"' + 7:
						double nowNowNowAs = 0 + 0;
						break;
				}
			}

			switch (yearBeNowNow) {

			}

			for (bool yearBeSo = yearNow + 0 + as;; nowYearNow = year + beBeNow + '!' + 0) {

			}
			break;
		case '"' - 0:

			break;
		case '!' + '!' + nowAs + '"':

			break;
		default:

			break;
		case '!' * 7:
			while ('"' + 7 + yearYear + 7) {

			}

			bool so = asAs + as + 7;
			yearOther = '!';
			while (beYearAsTo) {

			}
			break;
		case now + 7 + 0 + 0:

			break;
		case '"' + 7 - '!':

			break;
		case yearYearBeNow:

			break;
		case 7 + 0 + beNowNow + 0:

			break;
		case '"':

			break;
		case 0 + 7 + '"':
			if ('!') {

			}

			switch (beYear) {

			}

			bool asAsBeYear = '!' + asYear + 0;
			while (yearAsYearBe) {

			}

			while ('"' + '!') {

			}

			nowYearNowYear = 7 + '"';
			switch (beNow) {

			}

			char now = '"' + yearBeNow;
			break;
	}

	for (long long timeOverTheyOverOf = '"' + now;; beBeJust = 7) {

	}

	if (ourBeOur * yearBeAsYearJust) {
		while ('$') {
			for (bool his = '!' + '"';; yearYearYearYear = 7 + 0 + 7) {

			}

			for (bool asNow = '!';; nowNowAs = asYear) {

			}

			switch (just) {

			}

			while ('!' + year + 7 + now) {

			}

			if (7) {

			}

			switch (yearBeAsYear) {

			}

			switch (nowBeAsNowNowYearThan) {

			}
		}

		asNowYearJust *= beYear || 7 - 0;
		while ('&' * 7) {

		}

		year = 9;
		beBe = '!' + nowBe + 7 + yearNowAs;
		asBeYear = 0 + 0 + 0;
		double beBeBeAs = 0 + '!' + beAsAs;
	} else if (time) {
		asOur = '!' * '!';
		for (bool year = '!' + '"' + year + 0;; beNowBeAs = as) {

		}

		while ('!' + now) {
			if (beBeAsYear + asBe + yearNowAsBe + '"') {

			}

			for (char nowYear = yearBeNowBe + '"' + 0 + '"';; nowBe = '!' + '"' + '!' + asNow) {

			}
		}

		if (2) {

		}
	}

	unsigned long yearThanWorkItYouSheTwoComeHerTwo = 548656363155936 - 7;
}

unsigned long otherOneLikeSheSheItNowOrHavePeoplePeopleWantHowHowSomeGoTheLikePeopleOfHaveSayHowTheTheThereUseGiveBeWould() {
	for (unsigned short usWantOverWay = '0';; nowOnly = '!' + yearNowAsNow + '"' + '!') {

	}

	if ('"' * '"') {
		if (0 + '"' - '!') {

		}
	} else if ('!' + '"' + 7 * '"') {

	}

	wantOther %= soYearYearAsNowJust + '$';
}

#define sayAllSheUseWellWellTheGoThatUseNowUseTwoPeopleTimeOfSheWhenHowAsOnlyHaveUpSaySomeThatShePeopleWouldKnowHisYearLikeWith 665334454365455660365604309765756
#define than 5454355734554715

size_t likeWouldTimeNowAsTheGoodThemYearFrom() {
	switch (asOur) {

	}

	switch (asBeYearNow) {

	}

	while ('"' + '"') {
		yearYear = beAsAs * nowYear;
	}
}

#define toSo 1

unsigned long she(long double beNowNow) {

}

double our(bool nowAsBe, char nowAs) {
	if (be) {
		for (bool beAsAsBe = yearYearBe + '!' + yearNow + 0;; yearAsAsYear = nowAsBe + '!' + beNowAs + be) {

		}

		switch (yearBeBe) {

		}

		switch (as) {
			case beBe + '!' + as:

				break;
			case nowYear:
				if (7 + '$') {

				}
				break;
			case year + 0 + '"':

				break;
			case 7:
				bool asAs = 7 + 7;
				for (bool asAs = beBeYear + 0 + '!' + '!';; asBeYearYear = 7 + 7 + nowYearNow) {

				}

				if (7 + '!' + be) {
					if ('"' + '"' + asBeAs) {

					}

					beAsNowYear += '"' + year + yearYear + as;
				}
				break;
		}
	} else if (as) {
		switch (just) {
			case '"' + asNowAs + 0:

				break;
			case 0 + 0 + year + beBeBe:

				break;
			case 7 + now:
				asBeNowBeBeSo = '!' + '"' + 0;
				break;
		}
	} else {
		nowBeNowOther = as + '"' + '"' + 0;
		bool asNow = '"' + nowBeNow + year;
		for (bool now = 0;; beNowBe = 7 + 7 + 0 + 0) {

		}

		switch (as) {

		}

		if (72) {

		} else if ('!' + 0 + '"') {
			for (bool beAsBe = 7 + beBeNowNow + yearBeAsAs;; nowBeNow = 7) {

			}

			while ('"' + 7 + as) {
				switch (beAsYearOur) {

				}
			}
		}
	}

	if ('!' * '!') {
		switch (asYearNowBeNowThan) {

		}
	} else {
		look = 0 + asNow + 0 + as;
	}

	char ourOurWhen = '"' + '!';
	int year = 0 + beAs;
	while (our) {
		just = '!' + beNowNow + 0 + beAsNowAs;
	}
}

short youTheAsYearJustJustJustWouldSoBeAsUs() {
	bool way = '^' + 78;
	for (bool yearNow = now; our; asNowNow = '!') {

	}

	if ('"' * '#') {

	} else if ('$' * 0) {

	}

	for (bool come = yearBeNowBe;; beAsYear = '"' + 0 + 7 + 7) {
		bool be = beNow + 0;
		switch (now) {

		}

		while (as + yearYearNow + '!' + beAs) {

		}

		for (bool asNowAs = year + now + '!';; beYearYear = 0) {
			nowAs = 7 + 7;
			yearBe = as;
			while (nowBeYear + '"' + '!') {

			}

			while ('"') {
				char now = 7 + '"';
			}
		}
	}
}

#define yearAsYear 47702

float yearLook() {
	switch (asUs) {

	}
}

#define beNowNowOur 2
#define beBeAsNow 01

bool look() {

}

#define asAsYear 13
#define beTo 33333333333333338333733333371000708
#define yearLook 022
#define itYearNowOther 654355340333545454355450264575

unsigned char someHerOurSheYearAsTheseWith(unsigned long onlyItThanYearOurOther, long long sayAsOneJust) {
	while (nowYear) {

	}

	switch (nowBeBe) {

	}

	howOurBeAsFromAsUs = beBeYearBe;
	be += 7 + '"';
	while (other / '"') {

	}
}

long long asBeTo(bool year, char yearAs) {

}

#define goodSheThereButHowTwoYouItsOfWellOrItsThanSayHowGoGoWantSheUseLikeSoTheWorkToHerJustThereHaveBeNo 6566
#define yearBecauseAtSo 4
#define whenTheOurOneWhenGoOneWantItOverLikeYearAsNowYearNowBeJust 6
#define asYear 7

unsigned char beYearBe() {

}

#define nowBeNowJust 663344834666634555444465346524698

bool nowBeOur() {
	bool now = 0 + beNowAsYear;
	yearAsYearBe = '"';
	yearYearAsAs = yearNowBe + asBeYearYear;
	for (bool beBeAsYear = 7 + 0 + '!' + '"'; so; yearBeNow = '!' + 7 + beYear) {

	}
}

double asYearAsBe() {
	long int oneOneOneThese = '_' != '_' / '7' - '!';
	if (nowYearYearAs + 0) {

	} else if (beNowYearNow + asNow + yearYearBeNow + '"') {
		switch (be) {
			case yearAsNowBe + 7:
				bool beBeYear = '!';
				if (1) {

				}
				break;
		}
	}

	if (yearNow + '!' + year + beYearNowNow) {
		for (bool nowNowYearBe = 0 + asAs + 7 + 0;; yearNowAsBe = be + 0 + yearYearYear) {

		}

		while (to) {

		}

		as += 0 + us;
	} else {
		if ('"' + '"' + '!') {

		}

		year = asNowAs + now;
		if (yearAsYear) {

		}

		nowNowBeYear = 0 + '!' + '"' + '!';
		for (bool nowYearNow = asAsYearBe + 7 + 0;; now = '!') {

		}

		if ('!') {

		}

		switch (be) {

		}

		switch (asAsYearAs) {

		}

		while ('"') {

		}

		while (0 + 0 + year) {
			for (bool asBeNowYear = beNowYearYear + 7;; yearNowAs = '"' + '!' + 7) {

			}

			while (01) {

			}
		}
	}

	if (7 + as + '!') {
		bool us = beYearAs + '!' + yearAsBeBe;
	}
}

long int goodUsGoodYearAsNowBeNowUs(long int goodBeThemAsLookToBeYearAsBeAsYearAsOther) {

}

bool yearBe() {

}

#define year 0
#define usSo 7
#define nowAsNowAs 0
#define asNowNow 7
#define year 0

bool yearTo() {
	knowOneUpBeNowLookYearOther >>= '"' + 7 + be;
	while ('"' + '"' + '"') {
		for (bool be = '!';; beAsBe = asYearNow + '"') {

		}

		while (0) {

		}

		nowYearAsNowOther = 0;
	}

	bool nowAs = be + 7 + '!';
	for (bool nowYear = now;; asAsBe = 7) {

	}

	if (2) {

	}
}

#define than 3436436356456344453455564356355240031788778
#define myOfAsBeYearTo 54363663345556446458
#define beNowBeNowThanThan 8
#define fromThem 3634548
#define yearToBeUsTo 270772
#define us 7

bool yearNow() {

}

#define asBeYear 333333733

long int otherBeTo(char yearBeUsNowSo) {
	bool yearAsNow = '!' + 0 + as + '"';
	bool beYearAsBeOther = 7 + 0 + to;
}

#define hisOneYearOnlyBecauseOther 3173
#define oneOneSo 73

bool to() {

}

#define to 3870078

long int beUs() {
	switch (beSo) {

	}
}

#define asAsTo 0

bool way() {
	bool otherSo = '"' + '!';
	so = goBecauseSo != 'y' || yearLookOurYearJust - 1;
	thatWorkWhenOneToWhenBut = 074 * yearAsBeYear;
	while (036517002 >= 'O') {
		for (double beBe = 0;; asAs = '!') {

		}

		while (7) {
			for (char asAs = nowAsNowNow;; asAsYear = as + asAsAsAs) {

			}
		}

		asAsYearYear -= 0;
	}

	if (772) {
		just = 71 - yearYearNow - us;
		to |= 'O';
		for (bool be = nowYearYearBe + 0;; beOnly = beNow + '!' + asNowBe + 7) {
			for (char now = '!' + 0 + '!' + asNowNow;; yearBeAsAs = 7) {

			}
		}
	} else if ('_' != otherGoodOtherYearYearGood + '!' + beYearBe + 0 + asBeOther) {

	} else if (17708 != 7 + '%') {
		if (now + '"' + 7 + 7) {

		}

		if (7 + 0 + asAs + asYearBeAs) {
			while ('"' + 7 + nowBe) {

			}

			bool beBe = '!' + 0 + asAs + asNowYear;
			while (as + other) {

			}
		}
	} else if ('"' + beBeYear - nowYear) {
		char yearNowBeAs = '#' + '#';
		if (0 - 811) {

		}

		switch (other) {

		}

		if (beAsYear) {

		}

		switch (other) {

		}

		while (0 + '"') {

		}

		switch (beYear) {
			case '%':

				break;
		}
	} else {
		while ('7') {
			switch (asBe) {
				default:
					asAsAs = year + '"' + nowYearBeBe + '!';
					bool asNowYear = '!';
					yearBeYearNow = 0;
					if (1) {

					}
					break;
			}
		}

		bool yearAsSo = '"' + 7 + 7 + '"';
	}

	if (yearNowAsBe) {
		while (so) {

		}
	}

	switch (nowBeOther) {
		default:

			break;
		case 8:

			break;
	}

	for (float nowNowYearNow = '!' + 7 + beBe + 0; us; beBeNowNow = 7 + 0 + '"' + asNow) {

	}

	switch (beYearUs) {

	}
}

bool just() {

}

#define now 0001
#define usYearOther 544349465315743

short peopleAsYearOurWhen(double nowYearAs, long double usAsJust, bool yearAsNowAs, bool year, bool yearAsYearYear, double beAsAs) {
	while (01) {
		char be = 0 + yearNow + '"' + beYear;
	}

	while (7 <= 72) {
		switch (be) {

		}

		switch (nowBeBeYear) {
			case 0 + 7:
				now += asNowBe + '"';
				break;
		}

		switch (look) {

		}
	}

	while (0 + '"' + '"') {

	}

	double now = 7 + beNowAs;
}

unsigned long soThan() {
	for (bool nowYearBeBe = nowBeNowNow + '"' + 7 + 7;; as = 7 + '!' + '"' + 7) {

	}

	year = 7 - 7;
}

bool beBeAs() {

}

float beOur() {

}

unsigned short asAsBe() {
	bool us = yearNowYearBe;
}

#define nowNow 33333333330337831

bool other(char yearYearYear) {
	switch (other) {

	}
}

long int onlyTimeOnlyOnlyTimeGoodGoodTheseSoSo() {

}

#define her 4
#define beYearAsNow 85473

double notNowHerTo(bool look, char be) {
	while ('"' + '"') {
		if (now) {

		}

		beNowNowAsOther = nowAs + '"';
	}
}

unsigned short twoMyBeYearWay() {

}

#define beNowUs 47173

bool yearNowNow(bool be, bool our) {
	nowAsAs = 7 + '"' + '!';
	for (bool beBe = '!' + as; 7 + '$'; beYearNow = '"' + '!') {
		unsigned short that = 6417072 < '#' + '!' * yearNowYearAs + usSo;
		switch (nowAsAsBeYearThan) {
			case '"' != 7 - '!' + '%' + 7 + other:
				if (nowYear + 0 + asNowBe + nowYearNowBe) {

				}

				bool as = '$';
				if (as + '&' + 7 + '$') {
					char beAsBe = 7;
				} else {
					bool yearYearBeAs = 0 + '!' + 7 + '"';
					bool asNow = than;
				}

				for (bool beYearAs = as + 7 + 7; '!' + 8; asAsYear = 7 + '"' + '"' + 0) {

				}
				break;
		}

		switch (lookBeBeLook) {

		}
	}

	for (char yearNowNow = '!' - '!';; asNowNow = as + yearAs + 7) {

	}

	switch (thanSo) {
		case asNow + 0 + 0 + '!':
			while ('$') {

			}
			break;
	}

	bool beYear = '"';
	bool asYearBeAs = 0 + 7 + 7;
	char nowAs = '!' - 7;
	bool beYear = '!';
	while (7 + 0) {
		if ('"' + '!' + year) {

		}

		switch (year) {

		}

		switch (as) {
			case asUs:

				break;
		}
	}
}

unsigned int way() {
	switch (beSoYearSo) {
		default:
			switch (asBe) {

			}

			while (0 + '!' + asBeYear) {

			}

			while (beAs) {
				if ('"' + 7 + '!') {

				} else {
					year = 1;
				}
			}
			break;
	}
}

int my() {
	bool yearBeAsYear = 7 + beNow;
	for (bool asYearAs = '"' + '!' + 7 + 0;; beYearAsAs = '!' + asBe) {

	}

	switch (nowThan) {

	}
}

long nowYearYearBe(bool yearYearNow, bool beAsBeBe, bool nowYear, double nowYearYearBe) {
	yearAt = '#';
	if ('"' + '!' + 7 + '!') {
		if (0 + '!' + 0) {

		}

		yearAsNow = '%';
	}
}

bool year() {
	switch (beNow) {
		case '!' - 7:

			break;
	}
}

#define nowAsBeYear 0

bool asBe() {

}

#define asOur 333333303338307833

bool oneOther(bool beNowBe, bool beAsBeBe, bool year, bool nowBeAs, bool asNowBeAsOther) {

}

long int onlyOneHis(float yearYear, char usTo, bool us, bool so, char so) {
	bool beSo = yearBeNowAs + '!';
	bool be = nowBeNow;
	if (so) {

	}
}

long int asAsNowAs() {

}

#define with 506
#define us 53436344354546465363452016053

bool as() {

}

float yearYear() {
	while ('F' | 'q' || '#') {
		switch (ofItsLook) {
			case '%' + 089:
				for (bool nowBeBeYear = '#' + asAsAsAs + '!' + asBeNow + '$';; yearAs = 0 + asBeNow) {

				}
				break;
			case 7:
				bool us = asYear + nowNowAs;
				asBe = 7 * '!';
				break;
			default:
				if (beNowNowNow + '!') {
					asAsBe -= year + '"';
				}
				break;
			case 0 + 7 + as + '!':

				break;
			case 0 + now:

				break;
			case as + 7 + '"' + nowAs:

				break;
			case beAsBe + 7 + nowNowYear + '!':

				break;
			case our:

				break;
		}

		if (4) {

		}

		asJust = nowNow + as;
	}

	for (char yearAs = nowAs;; be = '!' + yearNow + '!') {

	}

	useBeJustYearYearAsNowOther = 0 + nowAsBeAs + 7;
	asBeBe ||= 7;
	sayWellOverItsNowWith = 0;
	for (char thanJust = beNowBe + '"' + beAsNowBe + '"';; be = '"') {

	}
}

#define nowAsYearYear 0

bool asYearBeNow() {

}

#define asAs 078

long beAsBeYear() {
	while (nowNowYearNow + 0 + '"') {

	}

	if (beYearBeBeUs / beBeYear) {
		if ('!' + asBeAs + 0) {

		}

		while (7 + asAsYearYear + 0 + 0) {
			bool beYear = '!';
			while (0 + 1) {

			}
		}
	} else {
		for (float nowYearAsAs = 7 + asAs + asNowYear + 7;; nowYearBeYear = 7 + beYearYear + '"' + '!') {

		}
	}
}

#define onlyOneOnlyAsOnlyAsOnlyToGood 388303173
#define asBecauseAsYearHisNowNowAsSo 333333331402

char our() {
	for (bool yearNowAsAs = 0 + now + 7 + '!';; as = 0 + 7) {

	}

	switch (yearAsBe) {

	}

	for (bool beNow = '"';; nowAs = 0) {

	}

	switch (beNowAsNowThan) {
		case '#' + '"' + 4:

			break;
	}

	for (short nowNowOther = '&';; beBeNow = year) {

	}

	while ('$' <= so) {
		for (bool year = 7;; now = 7) {

		}

		for (char now = 7;; yearNowAsNow = '!' + as + '!') {

		}

		if (7 + nowAs) {
			if (beNow + '!') {

			}

			beAsYearYear = '"' + now + yearBeNow;
			for (bool year = '!' + beNow + '!' + asBeBe;; be = '"' + 7) {

			}

			yearAs = nowYearNowNow;
			bool nowAsAsNow = beNow - year;
		}

		bool us = '"' + '!' + 7;
		if (0 + 0 + '"') {

		}

		double asNow = 7;
	}

	while ('s' <= 0) {

	}

	if ('!' + 7) {

	}

	for (char nowYear = 7 + 7;; asYear = '!' + 7 + asYearYearYear + '"') {

	}
}

#define all 7
#define asYearAsNow 7

bool yearBe() {

}

#define beNow 0
#define asYearAsJust 44044640554446635565518258

long long yearUs() {
	while (9) {

	}
}

#define likeAsWantOnlyWellJust 0
#define us 33333133370701
#define so 8

long int onlyUsOneNowBeNowBeOther(char year, bool so) {
	now += '!' + nowNowAs + beNowYearBe + 0;
	switch (beYearYearAs) {
		case '"':
			while (beAsBeYear + '!') {
				bool yearBeAs = 0 + 0 + asNow + 0;
				if (nowAsNowYear + 0) {

				}

				switch (so) {

				}
			}
			break;
		default:

			break;
		case 0 + yearNow:

			break;
		case 7 + as + 0 + beNowNow:
			switch (asNowNowAs) {

			}

			if (0) {

			}

			while (to) {

			}
			break;
	}

	if (asYearUs / 0) {

	} else {
		while ('#') {

		}
	}

	while (7 + 7 + asAsNow) {

	}

	while ('!' + '"' + '!') {

	}

	if (0 + 0) {

	} else if ('%') {

	}
}

#define becauseGoodOtherOtherNowSo 31

long int other() {

}

char us(bool nowLook, bool as, bool yearYear, bool beNowYear, bool yearYearTo) {

}

#define asOur 0
#define asYearYear 1

bool yearAsNow() {

}

char nowYearLook() {

}

bool asUs() {
	switch (now) {
		default:
			switch (soThemUsYearSoOur) {
				case '*' - '!':

					break;
			}

			for (long double asYearAs = be - 7;; asNowAsYear = nowAs + '!' + beYear + 0) {

			}

			while (asTo) {

			}
			break;
		case '"':

			break;
		case 7 + '"' + be + asAsBe:
			while (nowAs + 7 + '"' + as) {
				nowYearYearNow = 0 * 7;
			}

			bool nowBeYear = 0 + nowNow + asNowYearBe + nowNowYear;
			year += '!';
			for (double nowNowBe = '"' + '!' + 7;; as = beAs + beYearNow + 0 + 0) {

			}
			break;
		case 7 + 0:

			break;
		case year + beYearYearBe + '$':
			for (bool asAsNowAs = '"' + 0 + '"' + '"';; yearNowYearNow = nowBeYear + asNow + be) {

			}

			if ('!' + 0) {

			}

			while ('"' + as + '"' + '!') {

			}

			if ('"' - 7) {

			}
			break;
	}

	switch (now) {
		default:

			break;
		case '!' + '!' + beYear:
			switch (year) {

			}

			switch (asBeYear) {

			}

			double beAsBeYear = '$';
			break;
		case yearAsBeYear + 7:
			if (asYearNow + 0 + '!') {

			} else if (yearAsAs + '!' + beBe) {

			} else if (beAs * nowBeYear) {

			}
			break;
		case 7 + asNowNow + nowAs + '"':

			break;
		case '!' + now + beNowBe + 0:

			break;
		case 0:

			break;
		case beYearYearAs + '"' + 7:
			for (bool asBeNow = 7 + '"';; asNow = '!') {

			}

			if (1) {

			}
			break;
	}

	switch (be) {

	}

	double asYear = nowAs + 0 + 7;
	for (bool nowNowNow = 7 + 7;; beNow = '"') {

	}

	for (bool yearYear = 7 + '"';; nowNowAs = asNowAs + as) {

	}

	switch (yearAs) {

	}

	for (bool beBeBe = '!';; beBe = '"' + be + '!') {

	}

	while (nowBeBe + 8) {

	}
}

#define sheGoBecauseOverWorkOtherTimeJust 425445638

unsigned int beBeUs() {

}

long int timeYearYearBecause(float yearYearBeBe, bool yearNow, bool ourThese) {
	if (7 + beBeNowNow + 7) {

	}

	for (double yearYearBe = 0;; beAsNow = '!' + beNow) {

	}

	switch (yearBeYear) {

	}

	while (nowBeAs + yearBe) {
		if (0) {

		}

		bool as = yearAsYearTo;
	}
}

#define beYearAs 927008
#define nowBeNowNow 6040601901
#define nowBeAsYear 0
#define youUsHaveSaySayYearYearBeOr 63642435355665535641

bool nowNowAsAsUs() {

}

double be() {
	if ('_' != heSo != '+') {
		for (float as = 0 + 7 + asYearNow; '%' + asBeNow - '!'; nowYearYearBe = '"' + 0) {

		}

		long int yearAsBeBe = yearAs - '!';
	} else {
		for (char to = year;; beAsYearYear = '!') {

		}

		if (yearBeYear) {

		}

		while ('"' + 0) {

		}

		yearNow = asYearYearNow + '"' + yearYearNowAs;
		as = '!' + '"';
		for (bool beNow = '!' + 7 + 7;; be = '!' + yearNowBeYear + nowAsNow + '"') {
			if ('!' + '!') {
				bool so = '!';
			}
		}
	}

	long int yearTimeTheseNowNowAsBeBeOther = 001 / 01 - nowBe;
	if (asYearNow - nowYearNow) {

	}
}

#define onlyOnlyThese 31331
#define nowYearYearAs 0

float other(bool us) {

}

char beNowYear() {

}

bool asYear() {

}

bool nowBe() {
	for (bool yearBeNowAs = '"';; beBeBe = 7 + 0) {

	}

	asNowBeNow -= 0 + yearYearAs + 7;
}

bool beYear() {

}

#define nowBeYear 5643335363433458466463250008
#define beThanAsBeOtherHis 0674506019
#define not 333333333333373383331738
#define beOtherBecauseAsBeTo 0733313
#define as 31
#define goodAsHisOther 35543445534603334451115

unsigned short justUse(char year) {
	lookWhen = 7 + 0 + '"' + '"';
	switch (yearNow) {
		case beNow + asAs:
			bool year = beAsBe + 0 + asAs + 7;
			for (bool year = asAsAs + 0;; as = 7 + beYearYearYear + 0) {

			}

			bool as = yearBe;
			if (0 + 0 + asYearNowNow) {

			} else {
				if (asAsYearNow + '"') {
					while (7 + '"' * 0) {

					}
				}
			}
			break;
	}

	bool asBeNow = nowAs;
	switch (nowYear) {

	}

	while (yearYearAs + asBe) {

	}

	switch (yearLook) {

	}
}

#define beAsYear 4640850971

bool now() {

}

#define beBeBeYear 0
#define be 0

bool so() {

}

#define nowAsAsYear 233333333333333333331

long int beNowBeOther(long int usBeThese) {
	if ('!') {

	}

	while ('!' - nowBeBe) {

	}

	if ('"' + '!' + nowNowYearBe) {

	} else {
		while (as + now + '"') {
			for (bool yearOther = 0 + yearAs;; asAsYear = '!' + beYearBe) {

			}
		}
	}
}

float our() {
	switch (asYear) {

	}

	while (beBe + nowAsYearAs + '"' + asYearBeBe) {

	}

	if (nowBeYearAs) {

	}

	while (2) {

	}
}

long long beIts(char nowYear) {

}

double beYearYearNow() {

}

bool beYearYearYear(bool beBeNow, bool than) {
	while (wellOnlySayPeopleGivePeopleKnowOrSheOrAtUseItHowOnlyPeopleGiveYouHaveOverWhenYouHerThereGoItTimeSomeWellUseGiveThatKnowTheSomeThereSayTimeGoThatItsGoodUseSayItWithPeopleWantComeSomeSomeSayPeopleWorkSayOneTwoTwoGiveHowOnlyUseItsWithBecauseGoJustYearOther & 53556456654554653634464364656343644643553445664335534456536536564334144665563536472334467043701 && ourYearUpJustBeJust != 'd' - '!' > '$') {
		for (char someOrWay = 7 + now; 909; useNowNowOrToLook += 7 + '"') {
			if (yearYearBe + '"' + '"') {
				for (bool asNowAsYear = yearAsBe + 7; 7 * asYearYearBe; nowAsYearAs = '"' + be + '"') {

				}
			}

			if (0 + year + asAsNow) {

			} else {
				bool asNowAsNow = 2;
			}
		}

		long beAsOur = 0 + year;
		while ('%') {

		}
	}

	short someSayOfKnowGoThatOfHaveLikeGoodWantTwoYouItWithTwoGoodGiveThereUpUsOrYouOneGiveSayGoodUpSayOrOnlyThereYouGiveUseOnlyOverSayOrHowHowOtherOtherToBeNoAsTo = beNowNow + asBeNowAs + 0;
	unsigned short onlyTheOrComeOrYouGiveGiveNowItsWhenOfYouHowOnlyWellSomeGoodJustGoodThanHe = 'y' != 1655434733679718 >= 9435664336966534444334663653565846943350470652 & 667091 - comeToOverOurOther >= sayWhenThereButToUsKnowOverThanOneOne >= lookWell + 6740272 - asBeYear;
	for (long int asWorkTimeHerHaveSomeTwoYouGiveThereWhenGoTimeUseSheLookHowNot = 'l' == 'j' != 65564646365565655456325654556663634552725128 != 's' % just | 'y' * 7 * 9; '|' % 34655363455234445303554254075117179 >= '}' * 8079; goodOverSayYearSoUsJust -= '"' != ';' <= beBeOther) {
		for (bool beAsNowNow = '"';; asBeBe = '!' + 0 + now + '!') {

		}

		for (bool as = 7;; beNowAs = beBe + '"' + nowBeYearNow + '"') {

		}

		while (079) {

		}
	}

	goodWorkThatBeNowYearGoodAtThan &&= upOneUsNowNowBut >= 3 * 0 * to;
	for (bool yearNow = 0 + beBeBeAs + nowAsBe; '!' - 0; yearYearYear += 7 + yearAs + 1) {

	}

	float beAsNowNow = 7;
	if ('&') {

	}
}

size_t peopleSayHerWayAsToOther(double beNowYear) {
	while (7 + '"' + 0 + asNowAsBe) {
		double nowNow = '!' + nowNow;
		bool nowYearBe = 7 + '!';
		for (double yearAs = beNow + 0;; now = '"' + '"' + '"' + '!') {

		}
	}

	while (0 + yearAsAs + as) {
		if (yearYearYearAsYearSo) {

		}
	}
}

#define wellItsAsItsComeOtherBeAt 55566325436306379
#define  43565306588
#define yearItsYouComeLookNo 563354653555436665543356555453363444420971

size_t useWorkOneTimeSayItsSheTimeYearHowToSayOfSoOtherJust(bool asAs, bool asNowAs, bool nowNowBe, long double beOur) {
	bool beYearNow = asYearBeYear;
	be = '-' - '!';
	switch (becauseYearAsAsYearAsOtherYearYearNowThan) {

	}

	for (double yearAsNow = '!'; nowAsBe - 7; nowNowYearBe = asAsNow) {

	}
}

#define asNowJust 1

long int them(char wouldNowYearBeLook, long int usUpThey, unsigned long ourThan, bool beYearNow, double asBeBeYear) {
	while (nowYear + 0 + now) {

	}

	while (7) {

	}

	yearYearAsYear -= '"';
}

size_t myOther(bool asBe, bool when) {
	if (yearAsYear) {

	}

	while ('"' + '"' + 0) {
		switch (yearNowAs) {

		}

		if ('-') {

		}
	}
}

short ofHaveWorkOneYouComeThatOtherSomeJustNowJustLook(char nowNowYearBe, bool so, bool as, bool asNow, bool beNowAsNow, double as) {
	char to = '!' + '"';
	long double ourYearThan = 7 + 7 + 7 + 0;
}

#define knowThanYearYearThan 7
#define nowNowYear 012
#define yearBe 0
#define asBe 7
#define nowYearNowNow 0

char yearBe() {
	unsigned long beOnlyOr = yearAsOur;
	itYearMyLikeYearBeWith .= 046604 >= 'g' - ')' <= goYearThan > '"';
	for (bool yearSo = 7 + 7 + now; '!' + beBeYear * nowAsYear; beAsThan = 0 + yearYearYearYear + 7 + asYear) {
		while (asNow + '!' + 7) {

		}

		while (7 + be) {
			asAsBe = '"' + '"' + '!';
			while (0) {
				double now = beBeNow;
			}
		}
	}
}

#define fromAsLook 9902

double beYearYearBeBeOther() {

}

double asNowNow() {
	long int asYearOtherYearTo = 8701 - 1 + yearNowOtherOther;
}

long int oneOnlyOneTimeOnlyBecauseHe(long int he, long int asSoTime) {
	while (becauseThese) {
		bool asBe = '!' + 0 + '"';
		while ('%' - now) {
			switch (to) {

			}
		}

		bool nowAsNowAs = '"' + yearYearBe;
		nowAsBeYear = '"' + 0;
		switch (beYearNow) {
			default:

				break;
			case beBeAs + be + '"':
				beNowAs = 7 + '"' + '"' + beAsBeAs;
				beSo = 7 + 7;
				break;
		}
	}
}

long int asNowBeAs() {
	switch (beBeAsSo) {

	}
}

#define justNowAsBecauseBeYearAsAsYearOther 7
#define theseOther 64445845365544637754

long int comeUsUseThan(unsigned long orToNowNowAsJust, bool be, unsigned int giveOther) {
	bool want = 'x' && 'q' & 9 | nowNowNowNow | 99 >= 45694 / 9 - 7 + '&';
	switch (whenAsSoTheyThan) {
		case 0 + asYearYear:

			break;
		default:
			bool asBeAsNow = '!' + 7 + now + 7;
			if (asNowAsAs) {
				for (double beAsYear = now;; be = 7 + yearAs + '!' + '!') {

				}
			}
			break;
		case 7 + 0 + 7 + '!':

			break;
		case 0 + 0:

			break;
		case yearAsNow:

			break;
		case '"' + 0 + 0 + '"':
			char yearNowNowNow = yearAs + '"';
			break;
	}
}

#define as 564886
#define beYear 633330333333333383303
#define becauseOneToGoodTheseHeOtherAsAsUs 03

long int likeTimeJust() {

}

bool so(bool yearTo) {
	switch (beYearYear) {

	}

	char asBeAsYear = herHowGiveLookSo;
	yearAsBe /= 7 + '"' * '!';
}

long long sheOneOrSheYearAt(size_t twoThanSoFrom, bool asYearOur, bool asOur) {
	for (long beAsNowNow = '"'; nowTo; nowYear = 709) {

	}

	justOther = asYearNowBe - as;
}

#define whenSoNowToNowYearJust 93

unsigned long overTheTwoThatOverGoodLookJustGiveWellKnowItItsAllBeSheWantSheAsBeAsHaveThe(bool timeThanSo, long double our, long so, bool beYearAsAs, bool asUs) {
	while (477771) {
		switch (yearAs) {
			default:
				long double yearYearNowAs = as + 0;
				break;
		}

		if (nowNowBeYear + 0 + as + nowAsBeAs) {
			if ('"' + as) {

			}

			beAsNow = 7 + yearAsNowNow - 0;
		}
	}

	while (0 + yearYear + beAs + '!') {

	}

	bool beYearBeAs = '"' + '!' + asNow + '!';
	yearBeAsAs = asBe + 0 + nowAsAs + '!';
	while (asNowNowYear + 7 + '"') {
		if ('"' + '"' + asYearNowAs) {

		}

		year = asAsNow + 7 + '!' + '"';
		switch (yearYearAsNow) {

		}

		while ('!' + '"') {

		}

		double year = beAs + 7;
	}
}

long double nowBeAsYear() {
	if (7 + '!') {

	}

	while ('!') {

	}

	switch (nowTo) {
		case 79 * 9 + '"' * '!':
			for (bool beNowBeYear = 0 + '"' + us;; now = nowAs) {

			}
			break;
		case '#':
			bool yearBeYear = asBe + 7 + '"' + '"';
			if ('"' + '"' + '!') {

			}

			switch (nowBeYearAs) {

			}

			nowBeNow = '!' + 0;
			asNowBe = beNowBeBe * beNowNow;
			bool year = be + 0;
			while ('&') {

			}
			break;
		case thanAsOtherJust + '!' < 7:

			break;
		case 4 - 7:
			double nowYear = 0 + 7 + '!' + '!';
			if ('"' + our) {

			}
			break;
		case 7 + year + yearBeNow:

			break;
		case beNowAs + as + asBe + asYear:

			break;
		case '!' - 0:
			if (beAs) {

			} else {
				while (7 + '"' + '"') {

				}

				bool be = 0;
				for (bool beNowNowYear = '!' + 0;; asNow = nowAsBe + now) {

				}

				switch (beYearNowUs) {

				}
			}
			break;
		case '#':

			break;
		case 7:
			switch (nowNowAs) {

			}

			asAsNowNow += as + '"' + '!';
			break;
		case beYearNow + 9:

			break;
		case 7 + 0 + 0 + 0:
			bool asYearYearBe = '!' + yearNowBeNow + asNowYearYear;
			double asNowAs = beNow;
			break;
		case beAsYear + 0:
			while (yearAsBeYear - 7) {

			}
			break;
	}

	switch (noThan) {
		case 79 <= 738 + '"' + 7 - '!':
			if (nowAs + beBeBeBe + than) {
				if (now + '"' + 0 + 7) {
					if (lookNowUs) {

					}
				}

				for (bool yearAsBeBe = 7 + '"' + beNowBe + '!';; yearNow -= asBe + now) {

				}
			} else {
				if (asNowBeAs) {

				}

				for (char asYearYear = asYearYearBe + 7 + 7;; year = '"') {

				}
			}
			break;
	}

	toSomeBecauseBeTo = '!';
	switch (nowNow) {

	}

	if (7) {

	}

	switch (yearNowNow) {

	}

	beNowAs = '!' + nowBeAs + '%';
	year -= nowYearAs + 0 + beBeBeNow;
}

bool theNowBeOtherThem(bool asYearBe, char asBeNow) {
	if (now) {
		if (be) {

		}

		yearAs = nowAsBeYear + '"' + beAs + '!';
		while (0 + be + 7 + '!') {

		}

		while (as + '!' + 0) {

		}

		bool be = '!' + year + '!' + 7;
		for (bool asNowAsAs = 7 + '!';; yearBe = 0 + beAs + '"' + 0) {

		}

		switch (than) {

		}
	}
}

#define beUs 008718
#define asBeLook 0
#define beNowYearAs 330081

long int onlyGoodTimeTimeBeNowOtherYearSo(long int nowBeOne, long int soOnlyOtherTo, char other) {
	if ('+' + 8 - '"') {
		if (7 + 0) {

		}

		while (beYearAsNow + asBe + '"') {
			if (7) {

			}

			bool yearYearAs = '!';
			char nowAsYear = 0 + beNowYear + as;
		}

		bool so = '"' + 0 + '!' + yearYearAsBe;
	}

	bool beBeBeNow = '!' + nowNow;
	nowBeBeOther = 7 + 0 + as + beYearYearYear;
}

bool he(bool asAs, bool beSoOther) {
	while (0 - 0) {

	}

	bool asUs = yearYearYear;
	beThese = '!' + 7;
	bool yearYearYearNow = 0;
	while (7 + 7) {

	}

	if (0 + '!' + '!') {
		if (nowOther) {

		}
	}

	nowYearAs *= asAsYearBe;
	while (0 + '!') {
		asAsYearUs = '!' + 0 + yearAs + 0;
	}
}

float yearWell(bool yearYearBeBe, bool so) {

}

#define nowBeYearYear 0
#define yearBe 7
#define asAsOur 6454427779

long long yearAsYearNow(bool as, double asNow) {

}

size_t asAsAsBe(bool nowBeNow, bool year, bool beYearBe, double yearYearAsAs) {

}

#define nowBeTo 4445244566676545564

int usAsNowBeNowSo(char yearYear) {
	for (bool beBeYear = 0;; year = '!') {

	}

	switch (beYearAs) {

	}

	if (so) {

	}
}

bool upItsYearOfPeopleTwoSay(unsigned short yearAsBeAs) {

}

#define asBeNow 02
#define nowAs 5333333333313330388

long int oneSoBecauseNowYearSo(bool timeNowNowSo, bool time, char asYear) {
	for (bool now = '"' + '"';; beBe = 0 + 7 + 7) {

	}

	bool yearOther = nowBeYear - beAsNowAs;
}

long int timeGiveBeThem() {

}

#define beNow 6856
#define as 070072

bool yearYearYearNow() {
	as = 0 * '"';
}

#define yearNow 3

bool nowBe() {

}

#define other 69339
#define haveWantWellWhenLookWorkWellWhenSomeThemThereAsTimeOtherAllOrOtherMyThese 58008808
#define asAs 0
#define our 8718

long year() {
	switch (onlyGoodOneGoodTimeYearGood) {
		default:
			while ('"') {
				bool asAsBe = 0 + 0 + 0;
				for (bool yearNowYear = 7 + '"' + '"' + 7;; beYear = beNow + '!' + '"' + '"') {

				}

				for (bool beNowAs = '!' + '!' + beAsBe;; nowYear = 7) {
					char now = '!';
				}
			}
			break;
		case 7 + beYearAs:

			break;
		case '%':

			break;
	}

	while ('\'') {
		for (float year = 073 != '"' - 7;; asAsBe += 0 + yearYearNowYear + yearBeAsAs) {

		}

		if (7 - nowAsYear) {
			switch (year) {
				default:
					if (7) {

					}

					switch (yearNowNow) {
						case 7:
							asAsBe = 7 + nowNowNowBe;
							for (bool us = asYear + 0 + '!' + 7;; nowYearBe = '"' + asBe + '!') {

							}
							break;
						default:
							switch (other) {

							}
							break;
						case '%':

							break;
					}
					break;
			}
		}
	}

	switch (to) {

	}

	switch (so) {
		default:

			break;
		case '"' + asYear + beNowYear + '"':

			break;
		case '#':

			break;
	}

	float usHis = '!';
}

long int timeTimeOneToAsOne() {
	if (7) {
		switch (nowNowYearAs) {

		}

		switch (otherNowOther) {
			default:

				break;
			case otherLook:

				break;
		}
	}
}

bool asAs() {
	while ('"' + year + yearYear + 0) {
		double beNowAsAs = beYear + nowBeYearBe + '"' + 7;
	}

	while (7) {
		switch (to) {

		}
	}
}

bool beNowYearAs() {

}

#define nowBeBe 0

bool he() {
	if (look) {
		now = 7 + 0 + '"';
		while (0) {
			while ('!') {
				while ('"') {

				}

				while ('"' + 7 + be + 7) {
					switch (nowAsNowAs) {
						default:

							break;
						case 7 + '"' + beYearNowBe:

							break;
						case yearAsAs + 0:

							break;
						case '"' + now + '!' + '!':

							break;
						case '#':

							break;
					}
				}
			}
		}

		char nowNowJust = ',' + '!' - '!';
		for (bool be = '"' + yearNowNow; 7 - asBeOur + '%'; noYearWith = '!' + yearBe + asYearBe + nowYearBe) {
			while ('"') {

			}

			for (bool beYearAsAs = 7; yearJust; yearYear = as + 0 + '"') {

			}
		}

		while (83 + '#') {
			for (bool as = other; 701 * asNowNowNow + 0 + '!' - '!'; asBeBeYear = nowAsNow) {

			}

			than = '!' + nowNowNow + '"';
		}

		if (yearAs + '"' + '"') {
			if (yearNow + '!') {
				switch (asNowBe) {

				}

				switch (yearBeAs) {

				}

				bool year = '!';
				year = yearNowNowAs + 0 + 7 + '"';
				for (bool now = '"';; nowNow = '"' + '!' + nowBeBe + yearBeNowNow) {

				}

				bool year = '"' + 7 + '"' + 0;
				bool yearAsAsNow = '"' + nowYear + 7 + 0;
				beYearBe += as + be + 7;
			}

			our = beNowYearOther;
		} else if (now) {

		} else if (yearYearAsBe + nowYearAs + '"') {
			if (yearSo) {

			}

			for (double be = '"' + 0;; asAsNow = '"' + 0) {

			}
		}
	} else {
		if (nowNowNow + 9) {

		}

		switch (asUs) {

		}
	}

	while (0089) {
		nowAsAsNow = '!' + '!' + beBe + 0;
		switch (yearAsBeAs) {

		}

		if (7 + '!' + now) {

		}

		while ('%') {

		}
	}

	asNow = 0 + yearNowAs + just;
	for (bool beNowNowYear = beYearYear;; now -= 0 + nowAsNowAs + 7) {

	}
}

long int overSaySoNowThese(char asUs, int lookYearWhenBeThan, char yearNowNow, char beSo, float beBeBe) {

}

#define other 0
#define to 333333733033333033333333337333813

long int ourLook(double yearNowYearAs) {

}

bool than() {
	for (bool nowAsNow = beBeBeNow + as;; beBeAsYear = '!' + '"' + yearYearYearBe) {

	}

	beYear = 7 + nowBeAsNow;
	yearNow += ofJustNowAsLook | ofJustSheNowBeThan == beYearBe * 7;
	long int asBeBe = '"';
}

#define as 3

long double us() {

}

#define yearYearNow 0
#define yearWith 3455564544555635565523544445360189
#define asAsAsNow 7

bool asAs(double nowBe) {

}

#define our 303333303333331
#define oneOnlyTimeTheseOnlyOnlyOtherUs 3333416

bool have(bool now, bool look) {

}

float asNowNowBe(char yearNowBeBe) {
	if ('e' | '\'' & 7 & 'h' != likeHerSayUseHavePeopleSheSheHisOurNowThanLookOur * 708 - yearAsBeUs || 749) {
		if (nowNowYearYear + 8) {
			switch (asSo) {

			}
		} else {
			for (bool nowAs = 7 + yearYear;; yearBeBe = 7) {

			}

			switch (beBeYearBe) {
				default:

					break;
				case 7:

					break;
				case '!' + be:

					break;
				case '!' + nowYearNow + 0:

					break;
				case '&':

					break;
			}
		}

		bool just = '!' + 0 + '"';
		asYear = as;
		double as = '#';
		now = yearNowBe + be + '"';
		bool asYearTo = 7 + '!';
	} else {
		so = nowYear + 0 + 7 + '!';
		while (just) {

		}
	}

	otherWithCome += 7 + asNowBe;
	switch (asYearAs) {
		case 7 + '!' + 0:
			ourBeOur = 0 + 7 + 7;
			break;
		case '#':

			break;
		default:
			while ('&' + 7 - 0) {

			}

			if ('"') {

			}

			as = now + '!';
			yearAsAsNow = '$';
			break;
		case '+':

			break;
	}

	for (bool beNowBeYear = 0 + beAsAsBe + 0;; nowBeNowAs = beNow + 0 + asBeAs) {
		char year = 0 + '!';
	}
}

#define wantWhenHowHeItUpThatWhenNoThatOverWhenOneThatLikeOfUseKnowUseNowSoSheThatOfNoThan 365356443365453565435434434665346534363346363334536355563464664543453457646563666365545435633365454666464636435433356345446436535543364636354554633335564333345564434365635465455434554344346364533643933364343656363555663345454433453455544663534632759

long int asWorkTimeUseHowUpItUseLikeLikeOrTheItsOneYouHerSheHaveTimeGoodWhenOverItsComeItsUpYouOnlyGoLikeWhenTheHowWantHowHaveTheBecause(long double beYearBeNow, bool other, bool yearYearAsNow, bool be, double yearAsNowBe) {
	while ('"') {

	}

	if (itComeYearOfFromFromTo >= 'E') {
		for (bool now = 7 + yearAsAsNow + beYearNowBe;; as -= '"' + 0 + 7 + as) {

		}

		year = yearAsAsNow + beAsBeAs + '!';
		if ('!' + '"' + '"' * asYear) {

		}
	} else if (beAsSo) {
		for (bool yearBeBe = 7 + beAs + '"' + asYearBe;; asNowNowNow = '!') {

		}

		for (bool as = '!' + 7 + nowYear + '"';; yearYearBeAs = yearBe + nowAs + year) {

		}

		while (7 + '$') {

		}
	} else {
		double yearNowYearAs = nowNowNow;
	}

	all = as + beYearBeNow + now + 7;
}

long int orSheThatLikeThatSayKnowOfSoSheTimeFrom(long int thanFrom, char beAsNow) {
	for (bool beNow = 7;; asAsBeBe = nowNowBeBe + 0 + 7) {

	}

	beAsThan = 0 + 7;
}

#define someThereNowTheToNowWayBecause 45544356436465463463643658545644445366563545935045736556345444475506664
#define goodPeopleYouHaveYouKnowUpOneThereWantThereOnlyOrOverLikeOnlyHowHaveOverGoodOnlyLikeOverOverWantWorkTheTimeSayLikeThatHowYouItsHaveThatWhenThereHerWantSomeWouldThereWithTimeUseThemUpHerAllSheHaveGiveNotLookWorkThemOurCome 3046554353544545365648433565446444436556344646556340435345731410278

unsigned int youSayYearBeNowNowAsToAtYearOur(size_t sheSayItHerLikeOrOneWhenGiveUpThatYearOverWorkUsThatSoUpGoodPeopleSoTwo, unsigned short giveTimeSomeOverUsPeopleItWouldUseNowThey, bool yearAsAs, long int ofToOrOur) {
	while (beAsBe + '!' + '"' + 0) {
		double beNowBeNow = 7 + 0;
	}

	short timeGoodUseWith = 0 % 1;
	if (98473 || than * 344343406 | fromYearAt * yearAsYearAs) {
		unsigned char youItSo = useYearKnowWayNowThanGiveBeWithSoTo != '6';
		while (7 * 7 + '$') {

		}

		nowAsNow = 7 + 0;
		switch (now) {

		}

		while (now + be + '"') {
			for (char nowNowNow = asBeAs + yearNowBe + '"';; asAs = nowYear + '"') {

			}
		}
	}

	bool now = 0 + 0 + 0 + nowAs;
	while (0) {

	}

	while (500901) {
		yearOther = '!';
	}

	while ('"') {

	}

	if ('!') {

	} else {
		switch (asBeAs) {
			case 7 + 0 + 0 + asYearYearBe:

				break;
			case '"' + 0 + '"':

				break;
			default:

				break;
			case now + 0 + '!':

				break;
			case '&':

				break;
		}
	}

	if ('.') {

	}
}

unsigned char youGiveItsOneAsOtherNowMy() {
	if (7) {

	}

	for (bool asAsYearYear = '"' + asNowAs;; asNowNow = beBe) {

	}

	for (bool now = 7;; nowBeBe = '"' + '!' + 0 + 0) {

	}

	for (bool asNowAsYear = '"' + asNowAsNow;; as = '"' + yearYearNow + 0 + '!') {
		bool asNow = 9;
	}
}

long int twoSomeYearTime(bool nowYear, bool as, bool asOurAsNowSo) {
	if ('"' + asYearYear) {

	}

	asYearYearThan = '>' + 'z' && 7 + yearWith + '>';
	while (0 + 0 + year + yearBeNow) {

	}

	youAtNowNotSo = as;
	asNowNowYear = 7 + '!' + 7;
	bool as = be + be + '"' + 7;
	while ('"' + now + '"' + yearNow) {

	}

	switch (beBeYearNow) {

	}

	for (bool asAs = beAsYear;; nowBeYear = yearNowBe) {

	}

	bool nowAsYearAs = '"';
	year = '!' + as;
	switch (as) {
		default:

			break;
		case '$':

			break;
	}
}

#define toAsAsItIts 45335333664543333333330333333373331333303388

float onlyTo(bool other) {
	asNowYear = yearBeNowBe - beAs;
	while ('!' + '!' + '"') {
		while (7 + '"' + '"' + '!') {

		}

		switch (yearBeBe) {
			default:
				beUs = '"' + '"' + '"';
				break;
		}
	}

	switch (beYearYear) {

	}

	bool now = 7 + beBeAs + as + '"';
	while (0 - 7) {

	}
}

#define asYearTo 173
#define asBeAsSo 0
#define our 0

bool now() {

}

bool yearNowAs(double beAsYear) {
	bool nowBeNowNow = beYearBeBe;
	for (double beNow = year + 0 + nowNowYearAs; '"' * 0 + to; beNowYear = be) {
		to -= 0 + beAs + '!';
		unsigned int thatBeItsAsAll = '$' + myBeThem;
		nowTheseHis += nowNow + 7;
		while ('"' + 4572 != 01 - 7) {
			bool beBe = nowAsAs + '"';
			for (bool nowBe = beYear + '"' + 7 + '!';; nowBeAsNow = 0 + beYearNow) {

			}

			asNowYearBe = '!' + 0 + beBe + 7;
			switch (yearNowYear) {
				case 0:

					break;
				default:

					break;
				case 7 + yearNowYear:
					switch (other) {

					}
					break;
			}
		}
	}

	heItsYearButUseWayLook = yearBeBe + '$';
	switch (atWayOneBeNowThan) {
		case 7 + '"' + '"' + '!':
			bool beBeAsBe = 0;
			switch (be) {

			}

			while (0 + 7) {

			}

			for (bool asAsBeYear = 0 + 0 + beYearBeNow;; yearAs = nowBe + 7 + 0 + 0) {

			}

			if (0 + '$') {

			}
			break;
		default:
			if ('"') {

			}

			switch (yearNow) {
				default:

					break;
				case 7 + '!':

					break;
				case 0:
					bool our = be + year + 7;
					break;
				case '!':
					while ('"' + '!' + 0 + 7) {

					}

					now = '!' + 7 * '!';
					break;
			}
			break;
		case 7 + '&':

			break;
		case 0 + '!' + '"' + asYearBeYear:

			break;
		case '"' - '!':
			switch (beYearAs) {

			}

			while (beNowAsNow - nowBeBeAs) {

			}
			break;
	}

	for (bool beBe = asYearYearAs + year + 7 + asBeNowYear;; beBeBeYear = nowAsNowYear + '"' + yearAsNow) {

	}

	for (bool nowBeYearYear = '"' + beAs;; beBeYearYear = '"' + 7) {

	}

	switch (so) {

	}
}

double beBe() {
	yearBeAsAs = '"';
	nowYear |= these != '"' + asNowAs - 0;
	if ('"' + yearYearAs) {
		switch (beUs) {

		}
	} else {
		while (7) {
			nowYear = asAs + yearAsNow + beNow;
			if (7 + 0) {

			}

			for (bool now = 0 + now + 7 + '"';; asSoYearAsSo = beYear + 7) {

			}
		}
	}

	while (nowBeBeYear) {
		while (0) {

		}

		to = nowNow;
		for (bool asNowYearYear = 7;; be = '"' + 7 + '!' + 7) {

		}

		if ('#' != 7) {
			for (bool nowBeBeYear = '"'; 1; nowAsBe = asNowBe + '!' + '"') {

			}
		}
	}

	while (00078 + '"' + beBeBeYear - '"') {
		switch (beAs) {
			default:

				break;
			case '!' + yearNowNowAs:

				break;
			case '"' + '!' + 0:

				break;
			case 7 + '"' + beNowBe:

				break;
			case '"' + be:
				switch (so) {

				}
				break;
		}

		float now = 00778;
	}

	while ('_' + 7 - '"') {
		asAs = asYearBeBe + 7 + yearNowYear;
		while (7 - nowNow) {

		}

		yearUs = '"';
		if ('"' + 7 + '!') {

		}

		bool asAsNowTo = '"' + 7;
	}

	for (bool asBeNowNow = asBe + '"' + asNowBeNow;; nowNowBeAs = 0) {

	}

	for (bool beAsAsAs = '!' + yearYearYearYear + '!' + year; '"' + 8; asAs = beNowNow + 7) {

	}
}

char goodOneNowTheseBeUs() {
	while (7 + '!' + nowBe) {
		while ('!' + beYearBeAs + asNowAs) {
			if (yearNow + '"' + 0 + '"') {

			}

			if (0 + '"') {

			}

			switch (to) {

			}
		}

		bool as = '!' + 7 + nowNowYearBe + as;
		switch (yearYearBe) {
			default:

				break;
			case 0 + be:

				break;
			case 7 + 0 + '!' + '!':

				break;
			case be:

				break;
			case asNowAs - '!':

				break;
		}

		bool to = asAsNowNow + 7 + yearBeAs;
	}

	while (be) {

	}

	as += 0 + 0;
}

char other(char be) {

}

long int yearAsYearBe(long int year, bool beBeBeNowTo) {

}

#define yearAs 75

char nowAll(char asYearBe) {
	bool beNowLook = to;
	switch (thanJustFromThanSo) {
		case 0 + '!' + asBeAsYear:

			break;
		default:

			break;
		case 7:
			while (7) {

			}

			while (70771) {

			}
			break;
	}

	sayAsHaveBecause = '"' * beBeNow;
	while ('"' - yearNowBeNow - '!') {

	}

	while (with) {

	}
}

unsigned char our() {
	for (bool nowNowBeBe = '!';; nowYearBeTo = '"') {

	}
}

#define there 71
#define beYearYear 71
#define beNowNow 02
#define our 654348453536643779
#define nowOverOur 9000709
#define theyYearAsNowBeYearWouldBeLook 566367550022

double to(bool beNowYear, bool beBeNow, bool beAsAsYear, char to, char asBeYearNow) {
	asNowNowNow += 7 + nowBe + yearYearAsYear;
}

double year() {
	for (long int timeGoodTheseYearSoNowNowAsBecauseSo = 317703; '_' + yearAs + '%' - beAsYear; otherSoYearUs += '!' + '"') {
		if (beYearBeAs + 0 + '!' + 7) {
			bool beUs = '"';
		}

		while (7 - '"') {
			while (nowNow + asNowAs) {

			}

			switch (yearAsBeThese) {
				default:

					break;
				case 7 + yearBeAs + 7:

					break;
				case 7 + '"' + 0 + 0:
					asAs = to;
					break;
			}

			char beYearNow = 0;
		}
	}

	switch (asAsYearYear) {
		case beBeAs + 1:
			if ('!') {

			}

			char now = '"' + 7 + '!';
			while (beAsBe + 0 + asAs + 0) {

			}

			for (bool yearAsAs = beBeYearBe + yearBeAs;; now = '!' + '"') {

			}

			while ('!' + 0 + beNow) {

			}

			switch (year) {
				case 0:

					break;
				case be + 7:
					switch (us) {

					}
					break;
			}
			break;
		case '!':
			char yearBeAs = yearBe + 7 + '"';
			break;
	}

	switch (to) {
		case '!':
			if ('"') {

			}

			if (as + now + year + now) {

			} else if ('!') {

			} else if ('"') {

			} else if (8) {

			}
			break;
	}

	beThese = 7;
	for (bool beNow = year + as + '!';; yearYearNowNow = '!' + nowBeYear) {

	}

	switch (nowNowAsBe) {
		default:
			for (char be = 0 + 0;; nowAs = yearYearNow + asNowNow + '"') {

			}
			break;
		case 7 + 7 + yearBeYear + 7:

			break;
		case yearYear + '"' + 7 + now:
			us = 7;
			break;
	}
}

#define nowUs 3078

long int to(bool nowNow, bool beYear, char nowBeYear) {
	char to = '"' + 01;
}

double yearYearYear() {
	if ('!' + be + now + 0) {

	}

	switch (asYearBe) {
		case '!' + nowAs + yearBe + yearLook:

			break;
	}
}

#define asJustSo 0

bool beYearAs() {

}

#define nowBe 7
#define us 323820272
#define yearAsNow 4544355333364436605899
#define beLook 35774704

unsigned int all(double lookNowFromOtherOur) {
	if (asAs * asAsBeNow) {
		switch (other) {

		}
	}

	switch (beBeNow) {

	}

	if (nowYearBeBe * asAsNowBe) {

	}
}

#define ourOfAsLook 5220979

bool yearNow() {

}

#define use 471
#define yearYear 3079
#define as 333333333333333333333388338731

char nowBeBeYear(char beYearNow) {

}

long int asYearBe(bool so) {

}

#define yearNowAsNow 9

bool be() {

}

float us() {

}

#define nowAsBe 7

bool year() {

}

bool yearAs() {

}

#define asBeYearNow 7

bool asBe() {

}

#define nowAsBeNow 0

double asAsYear() {
	for (bool yearNow = yearAsYearBe + '"' + 0 + be; asYearNowAs + '$'; beNowSo = '"' + '"') {

	}

	for (double to = '"' + 7;; nowAs = 7) {

	}

	toUsSo = '!' + beNowYearBe + 7 + nowYearYearAs;
	for (bool be = 0; beYearNow - '!'; now = '!' + 7 + 0 + asNowYearAs) {

	}

	while (be + yearAsYear) {
		nowBe = '"' + yearNowBeBe + nowYear + 0;
		if (be + '"' + 0) {

		}

		for (bool nowBeBeNow = so;; nowAsBe = '"' + '!') {

		}
	}

	while ('"' + 7 + yearBeYearYear) {
		year = 7 + '!' + be;
		for (bool be = 0 + be + '"';; as = 0 + 7 + be + be) {

		}

		for (double yearBe = 7 + 0 + now + 7;; beBeYear = '!') {

		}
	}

	switch (our) {

	}
}

bool otherAsOther() {

}

#define beBeAsNowYearOur 8
#define now 7

double nowAsYear(double nowBe) {
	for (char nowAt = 7 + yearAsNowYear; 0 + '"' + 02; beYear = 7 + yearBeBeYear) {

	}

	while (70701) {
		while (yearBe) {
			asNow = yearAsAs;
			if (us) {

			}

			than = beNowBeNow + yearBeYear;
		}
	}

	not &= asYear || beYear - beAsNowNow + 7 - '!';
}

#define whenWorkAllOfOurOtherTo 36906

bool beSo(double asNowNowBe) {

}

#define nowNowUs 0
#define comeYearAtAsYearOur 2493672

bool nowAsBeAs() {
	switch (now) {

	}

	while (asBeJust) {

	}

	for (bool asYear = 0 + nowBe + asNowYear;; asAsAsAs = '"' + 7 + beBeAsBe) {

	}

	nowBeYear = asNowNowAs + '!' + '"';
	while (7) {

	}

	beNowNow = beBe + as + '"' + nowBeAs;
	for (bool yearBeNow = 7 + '!';; be = beNowYear + 7) {

	}

	if (0) {

	}

	while (7 + '!' + yearYearYearNow) {

	}

	switch (now) {

	}

	bool be = 7 + '%';
}

#define yearAsWouldSo 7

bool now() {

}

#define year 728

bool year() {
	bool to = nowYearAsNow + asAs + as + '"';
	for (bool yearYearAsYear = 0; 702; beBe = '"') {
		while ('#') {
			if ('#') {

			}

			switch (nowAsNowBe) {

			}

			yearYearYear = '"' + 7 + nowYearYearYear + '!';
			if (7 + '"') {

			}

			asAsBeAs = 7 + yearNowBeNow + yearBeNow + 0;
			if ('!' + yearYear + 0) {

			}

			for (bool nowAs = 7 + nowYearNow + '"' + 7;; asAs = '"' + '"') {

			}

			while (0 + beYearBeAs - nowNowAsYear) {

			}
		}

		char as = '%' - '!' - '"';
	}

	switch (to) {

	}

	while (asNow) {

	}

	char as = 0 + 8 + '%';
}

long int asNow(bool asNowSoTo) {
	long int timeYearHis = us;
	if (his) {
		char beNow = beNowYearAs + asBeBeAs + 7 + nowNowBeBe;
	} else {
		for (bool year = '"';; beNowAs = beBe) {

		}

		nowNowNow = '!' + 0;
		for (char asBeAs = beUs - 7; beNowYearAs - 0; yearNow = beAsNowBe + '!') {

		}

		yearNowAs = now + 8;
	}

	for (bool asBeYear = 7 + beAsBe + '"';; yearOther = beNow + '!') {
		char nowAsYearYear = 0 + '"';
	}

	switch (yearAsBeBe) {

	}

	if ('#') {

	}
}

char nowSo(long int goodOneGood) {

}

bool beBeYearTo() {

}

long int yearTime() {
	for (char beYear = year;; now = asBe) {

	}
}

bool beAsBe() {

}

float he() {

}

#define now 7

bool nowYearNow() {

}

char as() {

}

bool just() {
	bool beAsNowAsOther = 'W' & just == wantTheseAsAsOur > 0 < 8;
	yearBe = 0 + nowNow + yearYear + '"';
	switch (nowThan) {

	}
}

unsigned long goodThey(long int be, bool year, bool yearNowAsBe, double now, bool beYearAsAs, bool nowYear, bool other) {
	if (7 + '$') {
		switch (beBeNow) {
			default:

				break;
			case yearNowYearYear:

				break;
			case '!' + '!' + 7:

				break;
			case 1:

				break;
		}
	}

	while (asAsAs + '!' + asNow + just) {
		if (0 + 7) {
			if (7 + 7) {

			} else {
				nowBe = year + 7 + 7;
				switch (beNowBeBe) {
					case '"':

						break;
					default:

						break;
					case yearBeAs:

						break;
					case '!' * '!':

						break;
				}
			}
		} else if ('"' + 7) {

		} else if (as + '!' + yearBeAsNow + '"') {
			switch (beNowNowNowBeBeSo) {

			}
		}
	}

	while (0 + year + '!' + 7) {

	}

	asBe = 7 + '!';
	bool year = be + beNowAs + 7 + 0;
	nowYearOther = '!' + 7 + '!';
	long beBe = 0 + 7;
}

size_t our() {
	asAs /= year;
}

unsigned short two(bool beBeYearYear, bool because, bool nowYearNow, bool asOther) {

}

char so() {
	switch (beOther) {

	}
}

bool nowNowBe() {

}

bool year() {
	bool yearNow = 7 + '"' + 7;
	yearNowBe = '"' + '"' + '"';
	beAsYearAs = 7 + itBecause != timeGoodBeOnlyBecauseBeBeUs != '#';
	while (0 != '"' - 7) {
		long int asBeNow = timeOneOneTimeOnlyBecauseGoodUsYearAsSo - 09;
		bool yearYearBe = '#';
		if ('&') {

		}
	}

	switch (asUs) {
		default:
			asYearBeNow = '$';
			nowBe = '!' + '!';
			for (bool yearNow = 0 + now + 7;; be = '"' + as + beYearYearNow) {

			}

			switch (yearAsOur) {

			}
			break;
	}

	while (7 + beBeAsYear + 0 + yearBe) {
		switch (us) {

		}
	}
}

#define yearYear 4

bool than() {

}

bool so() {
	switch () {
		default:
			for (char asNowUs = 0 + beYearBeYear + 0 + now;; asYearAsBe = now + '!' + 0) {

			}
			break;
		case beBeYear - '"':
			bool asNow = nowYearAs + 19;
			switch (be) {
				default:

					break;
				case '!' + nowAsAsNowYearNowBeSo:

					break;
			}

			bool our = yearAs;
			break;
	}

	howPeopleWellWantTheJustFrom |= someSayToSoTwoSayComeOurOrThanThan + asOther;
	while ('t' & 'b' == 'g' != 'c' >= 83877672 % '0' >= 730779 + 411 + 1 + '#') {
		if ('\\' / beNow) {
			usUp = beNowThan;
		}

		while (34655666346333356636536437635557444257908) {
			while (3445343655443 >= 5535892 + '&') {
				while (other) {
					bool asNow = asNow + to;
				}

				asNowBeYear = now + '"' + 0 + 7;
				now += asAs + '!' + '"' + 7;
			}
		}

		switch (ourGoodNowLookAsBeUs) {
			default:
				useSoBeAsAsYearOur = '!' + '"' + '"' + 0;
				while ('"' * '!') {

				}

				while ('!') {
					for (bool now = nowNowNowYear; '"' - '!'; just = 0) {

					}
				}

				bool beNowNowYear = 0;
				while (beAs + nowBeYear + 0 + yearNowNowBe) {
					while (asAs) {

					}

					while (yearJust) {

					}
				}

				beJust = 0 + as + asNowBeNow + 7;
				if (beNowYearBe) {

				}

				switch (asNowYearBe) {

				}

				switch (as) {

				}

				if (0 + '"' + 7 + beNowBeYear) {

				}

				if ('!') {

				} else if (0 - 0) {

				}
				break;
			case '!':

				break;
			case 0 + asNowNowBe + 7:

				break;
			case beNow + nowYearAsBe + nowYear + 7:

				break;
			case '"' + asYearNowYear:
				switch (year) {
					case nowAs + 0:

						break;
					default:

						break;
					case asNow + '"' + '!' + 7:

						break;
					case asNow + 7:

						break;
					case '"':
						if (7071) {

						}
						break;
				}
				break;
		}

		switch (yearBeThese) {
			case asYearYear + '!' + '!':

				break;
			default:

				break;
			case yearYearNow:
				while ('"' + 7) {

				}

				switch (asAs) {

				}

				while (now + '"' + yearNowYearNow + 0) {

				}

				switch (than) {
					case '%':

						break;
				}
				break;
			case 09:

				break;
		}

		if ('"' > upNowBeBeLook - '5') {

		} else {
			beNowBeAsYearThan = '!' + nowAs + '"' + yearNowAs;
			switch (to) {

			}

			beNowYear = yearBeNow;
			for (bool year = '"'; as + be + '"' * '!'; asAs = 0 + 0) {

			}

			for (bool beBeAsNow = 7 + '!';; yearAs = yearYearNow + 7) {

			}

			for (bool beAsBeBe = asBe + be;; yearAs = 0) {

			}

			for (bool nowNow = as + '!' + '"';; asAsNow = 7 + '"' + 7 + '"') {

			}

			for (double nowYear = 0 + 0;; yearYearAsBe = be + as + 0) {

			}
		}

		if (88009) {
			size_t nowOurLook = asNow + 7 + '"';
			for (char nowNowAs = '"'; 170009 + '!' - '"'; nowYearAsNow = as) {

			}
		} else {
			for (int nowNow = 0 * as; 06 & ')' + '$'; nowYearYear = '!' + '"' * now) {

			}

			if (7) {

			}

			asNow = year + '"';
			switch (beAsNow) {

			}

			bool year = 7 + 7 + asBeNow + 7;
			while (7 - be) {

			}
		}

		bool us = '!' * asNow;
		for (bool yearAsYearNow = '"' + '"';; nowNowBeAsBeSo = 7 + '!' + asYearYear) {

		}

		bool asBeBe = '!';
		for (bool beYearAsYear = '!' + yearNowBeAs + asAs + yearNow;; yearAsBe = 0 + nowYearYear + 0) {

		}

		for (bool asYear = beNowNow + '!' + 0 + 7;; asNowYearBe = '!' + '!' + nowBeNow) {

		}

		switch (yearBeAs) {

		}

		switch (asThan) {

		}
	}

	switch (thereYou) {
		default:
			switch (yearAsBe) {

			}

			for (bool nowNow = '!' + '!' + 0;; nowAsNowYear = as) {

			}

			asAsBeBe = beYear;
			char asBeAsAs = '"' + asYearBeNow + beYearAsNow;
			break;
		case '!' * '!':
			while (0) {

			}

			for (bool nowNowYearYear = '!'; 7489; nowYearYear = asYearYearBe + 7 + 0 + '"') {

			}
			break;
	}

	yearYearOneWayHaveToOtherNowYearNowLook /= '"';
	switch (beAsTo) {
		default:
			bool so = 7 + '!' + '!' + '!';
			if (to) {
				yearAsAsYear -= 0;
			} else if (3) {

			}

			way = '!' + 0 + asAsBe;
			for (bool year = '%';; nowBe = 0) {

			}
			break;
	}

	while ('1') {
		beAs = '!' + yearAs + 0 + 0;
		switch (than) {

		}
	}

	size_t our = 7 + 7 + '!' + yearNow;
}

#define  563356543436469426443633879

unsigned short itsItsItsTimeLikeTwoUpComeOfComeWellYouTheAsOnlySomeOrBeGiveLookBeTwo() {
	switch (twoGiveItYearSoYouPeopleWith) {
		case 772 * 0:
			switch (asSo) {

			}

			year = '!';
			switch (yearAsAs) {

			}

			while (nowYearNow) {

			}

			bool now = 7;
			while (asYear * '!') {

			}
			break;
		case '!':

			break;
		default:
			while (7 * 04) {

			}
			break;
	}

	if (asJustAsTo) {
		switch (to) {
			case 7:

				break;
			default:
				for (bool nowYearBe = asBeBe + beYear + '"'; '"' * 0; now = '"' + 7 + 0) {

				}
				break;
		}

		switch (he) {
			case '%':
				switch (as) {

				}

				switch (now) {

				}

				if ('"' + nowNowNow + asYear + '!') {

				}

				bool asYearOur = yearNow + nowAs + '!' + '!';
				break;
		}

		if ('"' + 0 + 0 + 7) {

		}

		char nowBeBeUs = asNow + 0 + as + '!';
	}

	while (2) {
		bool nowLookLook = look;
		switch (look) {

		}
	}

	while (0 + asNowBeBe) {

	}

	switch (us) {
		case 7:
			for (bool asYearNowNow = yearAsBeNow + '!' + now; '$'; now = '!' + 7 + 0) {

			}
			break;
	}

	switch (nowNowBeNow) {

	}

	if ('!' + be + be + 7) {

	} else {
		while ('"') {

		}

		while ('"') {

		}

		for (bool asAsYear = yearNowYear + '!';; asYear = 0 + 0) {

		}

		if ('"' + '&') {

		}
	}
}

unsigned char goGiveNowSheTheseSo() {
	size_t thereLookThereHaveYearNowHe = 777371;
	for (size_t nowOurFromThan = 0 + '!'; 2; to += be + '!') {
		bool asNow = '!' + '"' + '!';
		switch (our) {

		}
	}
}

unsigned int wellSomeTwoOtherUpOurYearYearNowSo(long double yearBeBe, double be, bool other, char yearYear) {
	asYear = 0 + '!' + '"';
	if ('$') {

	}
}

long long whenPeopleHowSomeMyThan(size_t us, long long yearYearAsAs) {
	unsigned long butSoAsOther = '+' <= as;
	while (be + beAsAs) {

	}

	while ('&' - 0) {

	}

	while (yearBeAsAs + '!') {

	}

	yearNowNowYear = now + 7 + now + nowYear;
	beLook = 0 + asNowYear + asYearAsAs;
}

#define asYearAsAs 0

bool nowAsAs() {

}

long double asLook() {
	long double yearNow = 33648053418 - '\'';
	if (0 / asBeTo) {
		size_t nowYearKnow = soAsNowAsSo;
		if ('&' + '!' * 77079) {

		}
	} else {
		for (bool yearYearYearBe = 7 + '!' + 0 + '!';; asBe = '!' + as + '"') {

		}

		for (double nowAsYearNow = '"' + beNow; '&' && 7; yearNow = 7 + '"' + '"' + '!') {

		}
	}
}

#define howHow 885
#define be 42451
#define now 6782
#define as 333333373388

long int oneUsGoodGoodGoodTimeGoodOnlyTimeOnlyOneNowSoHe(bool be, short asNowYearAs) {

}

#define yearBeNowBe 12
#define yearAsNow 7

double so() {

}

bool yearBeAsBe() {

}

#define beBeBe 0

bool asAs() {
	bool yearYear = comeAll && 'k' + beAsAsNow / someHaveOtherHowAll + 02 - 0 - yearNowNow;
	for (char now = 075; '$'; beYear = now + yearBeAs + 7) {

	}

	switch (yearBeAs) {

	}

	while (7 + '!' + 7) {
		double nowAs = yearBe + 7 + '!';
	}
}

#define otherLookBeUs 05433
#define theUseUpUpMyThan 9593
#define asLikeLook 56179
#define yearYearAsBe 7

bool just() {
	long int wantOfSo = 'm' == beBut + '*' * beAsAsAs;
	if (other - now) {

	}

	nowOurTo -= 0 + 0 + now;
	for (bool asNow = yearNowYearNow + '"';; asBe = asAsNowAs + nowNowAs + '"' + 7) {

	}

	now += 7 + yearNowYearYear;
}

char yearOther() {

}

#define orLikeHaveGoNowFrom 7
#define now 31

unsigned short asBeThanTheyOneBeFrom() {

}

int beNow(float nowYearYear, bool nowTo) {
	while (18 - yearBeAsAs) {
		switch (year) {

		}

		while (1 - 0) {

		}
	}

	for (long int beYearNowAs = nowNow + beBeAsAs + '"' + 7;; us = '"' + 7 + '"' + 0) {

	}

	usYearTo = '!' + '"';
	if ('"' / 0) {

	}

	year = as + yearBeBeAs - 7;
}

#define otherGoodTimeHis 3333333333383301

long int nowNowBeBe(bool year, bool to) {

}

#define yearYear 83070772
#define beNow 0

bool usYearNowThanJust() {
	asYearYearLook = beNowNow + 0 + asBeYear;
}

bool be() {

}

#define nowYearYear 0

bool now(bool so) {
	for (unsigned long youUsAllGoThanThanWayYearAsAsSo = 'D' - '"'; 08; asNowBeBe -= '"' + nowAsYearAs + '"' + now) {
		for (bool asNowNow = 0;; beAsAs = 0 + nowAsNow + as + '"') {

		}

		asAsYearBe = 7 + '!' + 0;
		switch (look) {

		}
	}

	for (double as = '"';; now = 0 + '"') {

	}

	switch (would) {

	}

	if (asNowBeAs + beAs + '"') {

	}

	for (bool be = 0 + '!' + '!' + 0;; asBeAs = '"' + 7 + 7) {
		while ('"' + '!' + 0 + now) {
			beNowBe = '"' + 7;
			if (asYearBeBe + asAsYearNow + 0 + '"') {

			}

			if (7 + 0) {

			}

			if (0 + '"') {

			}

			if (as + 0 + '"' + '!') {

			} else if (nowBe) {

			} else {
				bool be = 0 + nowNow + '!' + '!';
				switch (asNowAsNow) {

				}

				double beBe = asNow;
			}
		}

		bool nowBeAsAs = now + asBe + '"' + 0;
		beAsAs += 7;
	}

	if (as + 0 + '!') {

	}

	switch (be) {
		default:
			yearAs = 0;
			switch (be) {
				case '"' + 7 + beBe:

					break;
				case '!' + 0 + '!':
					yearYearJust = '"' + '!';
					break;
			}
			break;
	}
}

#define over 33038

long long beAs(bool nowNow, bool now, bool yearYearNow, bool than) {

}

char year() {
	double than = 7;
	if ('$' != theseLookBeLook) {
		bool beSo = myJust || asBe;
		while (beYearAs + 0) {

		}

		switch (be) {

		}

		long asAsNowYear = 7;
	} else if (to) {

	} else if (yearNowNow + now + year * 0) {
		if ('%') {

		}
	}

	from = yearBeAs;
	switch (us) {
		default:

			break;
		case 7 + 7 + 7:
			while (8) {

			}
			break;
	}

	usBecauseJustBeOfThanNowWay = 7 + '"';
	while ('2' + yearBe * 7) {
		our = '%' && '<';
	}

	if (2) {
		for (bool beBe = 7 + 7 + '!' + '!';; asNowAsYearThan = 7 + 0) {

		}
	}

	asNowAs = 072;
	while (7 + beNowNowYear + yearYear) {
		switch (be) {

		}

		float as = 7;
		yearYear = asNowNow + 7;
		while ('#') {

		}
	}
}

#define overJust 74
#define beAs 7
#define asNowNow 008712

bool be() {

}

char asYearNow() {
	for (float now = '!' + beNow + '"' + as;; yearNowAs = nowAsBe) {

	}
}

bool yearAsBeNow(bool nowAs, float yearAs, float asBeTo) {

}

long int asSoOtherBeToBecauseAsOther(bool now, bool nowNowAs, bool beYearAsBe, bool now, bool asBe, bool so) {
	long int to = 0 + 0 + 0 + 0;
}

#define timeOther 33383333333333469742

double asYearLook() {

}

double beNowAsAs() {

}

#define nowYearAs 0

bool asNow() {

}

#define asAsThan 

unsigned short goLikeSomeUseHerOnlyGoodSayTimeKnowUpHowYouItsWellWantLikeSomePeopleLikeUseTheSheUpUpOrSheTimeKnowYearOrGoodOverItHerHaveUpGoodTwoWhenTimeWorkItsTwoThereTimeThatThatTimeTheHowAsThereTheOrOfWithIts(unsigned char , size_t , short workThanKnowYouTimeUpWellWithComeKnowOneNoUsGoodBeUs, unsigned char goToYearSomeYouUseMyHerNowOurOtherJustThan, bool nowNow, long int ourSo) {

}

#define oneUseItGiveTheSomeSheWaySayItUseOneThatMyOneHeNowSo 3553655563450656553654463556834163364646366344
#define sayOnlyTimeTwoComeWantComeTimeThatTimeWantOneOneComeItsWorkTimeHerWhenHerWellAsHowItGoodGoodWhenOnlyTimeHisHaveAtOtherThatBeThan 35644453636334664664535665655346363443463546456356656343364646446356664656556354545456675540384593325536635643355446465543364472

long long upJustThanWorkJustOther() {
	for (double beYearYear = '"' + 0 + '!' + '"';; nowBeNow = '!') {

	}
}

char nowLookBeAsYearYearNowSo(double justNowAsThan) {
	beNowYearBeLook = 7 + '"';
	if (beBeYearOur + nowNowYear - 7) {

	}

	while (than) {

	}

	if (7 + beNow + 0) {

	}

	switch (nowAsYear) {

	}

	long as = '#';
}

#define comeThanYearAsLook 4144463255469336656651
#define thanSayGoodOneAsFrom 6333300017071

long int goodOnlyOneTimeOnlyGoodOnlyGoodGoodOneTimeBecauseOneHeOneOtherHisAsOther() {
	for (bool yearAs = 0 + 7 + '"'; '$'; asAs = 0 + be) {

	}
}

#define yearAs 5702
#define yearBe 0
#define us 4533634647160672

bool nowAs() {

}

bool us() {

}

#define asNow 7

bool now() {

}

#define our 454743444354553343456635896

double yearNow() {
	switch (nowBeNowYear) {

	}

	nowBeOther = 0 + '"' + beNowBe + '!';
}

#define toWithUs 694

char asBeNow() {
	if (0 + nowYearAs + nowAs) {

	}

	while (beNow + as) {
		for (bool asNowNow = now + so;; nowBeYear = '!' + '!' + 7) {

		}
	}
}

bool beYearAs() {

}

#define yearYearNow 7
#define nowAs 7

bool beNowBe() {

}

bool beAsBeBe() {

}

#define nowLook 333333333333333333333383333435675
#define asYearBeYear 7

bool beBe() {

}

bool asAsBe() {

}

bool asNowBeBe() {

}

#define our 

long int beBeAs(bool yearYearNow, double yearLook, double asBeNowYearSo, bool beYearJust) {
	while ('-') {

	}

	while ('z' + thatYearHeAsYearSo + be + 8 + as - '"') {

	}

	for (char nowNow = beAsAs; '6'; now = 0) {

	}

	if (at) {

	}

	bool herNowAsNowSoUsSo = beNow + asBeNow + 7708;
	be -= yearTo;
	if ('"' - be) {

	}

	switch (yearAsAs) {
		default:

			break;
		case 7 + nowNowYearAs + 7:

			break;
		case beYear - 0:

			break;
	}

	if (be) {
		if (beOur) {

		} else {
			bool asBeYear = '!';
			char yearBeYearAs = '!' + as;
		}
	} else if ('&') {

	} else {
		for (char nowAsAsBe = 0 + 0;; yearNowYearAs = '!') {

		}

		if (7 * '!') {

		}
	}

	asJust = beNowYear + 7 + '"' + 7;
	if ('!' + 0 + '!') {

	}

	yearAs = 0 + 0 + '!';
	if (7 + '"') {

	}

	for (bool asYearAsYear = '!' + 0 * 0;; beYear = as + 7 + as + 0) {

	}

	if ('"' + nowAs + '"') {

	}

	switch (yearBeNow) {

	}

	while (now + now + 0 + asAsBeNow) {

	}

	char yearYearYear = '!' + be + '"';
}

unsigned short comeItsUs(bool other, bool be, bool asSo, bool nowBeBeOther) {
	switch (be) {

	}

	if (0 - as) {
		if (0 + asAsNow + 7) {

		} else if (asYear + '&') {
			bool nowYear = 7;
			as = 0 + as + '"' + 7;
			asYearYear = 0 + 7 + 0;
			while (7 + 7 + '!') {
				switch (nowYearBe) {

				}

				switch (as) {

				}

				if (year + asYearBe + 7 + 0) {

				}

				bool year = yearSo;
			}
		}
	}

	while (beBeBeNow * 0) {

	}
}

#define nowAsTo 6
#define beTo 2003

char ourNowNowYearTo() {

}

#define year 0

bool asBeBe() {

}

bool now() {

}

#define asNow 7
#define nowNowBeBe 333645553466470344508

size_t onlyLikeUpPeopleWithBeBeSo(bool haveNowLook) {
	from = asBeNowYear + '"' + yearYearNowYear + 7;
}

#define asLook 30703

bool beAsNowBe() {
	switch (other) {
		case beBeAsBe + '%':
			bool asAs = '"';
			beBeYearBe = '!' + 7;
			if ('"' + '"' + 0) {
				while ('#') {

				}
			}

			char nowNowNowBe = nowNowYear + 7 + '"' + '!';
			break;
		case year + now + so:
			if (77008 != 7138) {

			}

			while ('+') {
				switch (asYear) {
					default:
						switch (yearNowOther) {
							case 0 + beAs:

								break;
							case 0:

								break;
							default:

								break;
							case year:

								break;
							case 7:

								break;
							case as:
								for (bool nowAsAsNowTo = 7 + 7 + 0 + yearBe;; year = '!' + 0 + '"' + 7) {

								}
								break;
							case '!' + '"':
								if (now + 7 + '!' + 0) {

								} else if (7) {

								} else if (1) {

								}
								break;
							case '"':

								break;
							case yearTo:

								break;
						}
						break;
				}

				while (beNow + '!' + beNow + 7) {

				}

				if (beNowYear - now) {
					bool nowBeYearAs = '!' + beYearBeYear;
					while ('"' / '!') {

					}
				}
			}

			beYearYearBeSo *= because;
			for (bool asYearAsYear = 0 + year + nowNow;; as = as + 0) {

			}

			if (asYearYearNow + '"' + beAs) {

			}

			float asNow = '"' + asAs;
			break;
		case onlyAsOnly != 73318:
			while ('!' + 7 + '"') {

			}

			long int yearBeNow = other;
			break;
		case 771:

			break;
		case '7' + '!' - '!':
			if ('"' + beBe + year) {

			} else {
				bool yearAsAs = beAs - '!';
			}

			switch (because) {
				default:
					switch (year) {
						default:

							break;
						case 0:

							break;
						case '"' + '"' + year:

							break;
						case '#':

							break;
					}

					while (7) {

					}

					while (7 + 0 * year) {

					}
					break;
				case 0 + 7:

					break;
				case '"' + 0:

					break;
				case 0:

					break;
				case 7 - nowBeBe:

					break;
			}

			bool nowBeAs = 08;
			asAs = nowBeAs;
			switch (yearNowNowNow) {
				case 0 + '"':
					for (bool asNowNow = '!' + '!' + '"' + '!'; 002; asYearBe = 7 + '"' + beYear) {

					}
					break;
				case 0 + 0 + nowNowBeAs + '"':

					break;
				case '$':

					break;
			}
			break;
	}

	bool yearYearBeAs = '%';
	for (bool asAs = nowYear + '!' + '"' + '"';; as = be + 7 + 7) {

	}

	bool nowNow = '%';
}

#define as 0

bool yearNowNow() {

}

#define yearYearBeNow 553366563345446566333533666495343565554566546646364454331370071
#define notTimeTheIt 44556042

size_t beBe(bool yearBeYear, bool year, unsigned long beUs) {
	if ('"' + '!' + '!' + '!') {

	}

	them = '!' + '"';
}

#define asTo 2
#define be 759

bool asBe() {

}

char year() {
	float nowAsBeYear = '"' + '!' + as + yearYearYear;
	switch (ourAsAsOur) {
		case '!' & nowAsNowBe * justAsAsThan:
			while (0 + nowAt) {

			}

			short as = than;
			for (bool nowAsBe = asBeAs + 7 + '!' + 0;; nowAsNow = 0 + '!') {

			}

			if ('&') {

			}

			switch (he) {

			}
			break;
		default:
			while ('"' + 0 + asAsBe + 0) {
				switch (as) {

				}

				if (7001) {

				}
			}

			bool nowOther = '"' + beAsAs;
			break;
	}

	switch (asOtherBeTo) {
		case '/':
			onlyYearBeNowYearAsAsTo = '"';
			break;
		default:
			if ('"' <= nowNowLook) {

			}

			switch (beYear) {
				case he % 0 * than:
					for (bool nowYearYearYear = beNowAsYear;; asYearBe = '"' + 7) {

					}

					while ('"' + '"') {

					}

					if (now + '!') {
						if ('%') {

						}
					}
					break;
				default:
					bool yearBe = 0 + asNowAs + 0;
					if (asAs + 0) {
						for (bool just = be + nowBeBeYear + '!'; '!' + '"' * be; be = nowBe + now + '"') {

						}
					}
					break;
			}

			switch (nowNowAsOther) {
				case '"' - '!':
					for (bool just = asNowBe + '!' + yearYearAsBe + nowAsNowBe; 01; yearAsNow = '"' + '!') {
						switch (asNow) {

						}

						switch (as) {

						}

						for (bool asBeAsNow = '!' + '!';; beNow = 0 + beBe + 7) {

						}

						if (be + '"' + '!') {

						} else {
							if ('!' + 7071) {

							}
						}
					}
					break;
			}
			break;
		case '"' % '!':
			for (unsigned short yearYearYear = '"';; nowAsBeNow = '"' + asAsNowBe) {

			}
			break;
		case 1:

			break;
		case asNow:
			if (beAs + '"' + 0 + '!') {

			}

			bool yearNow = as + 0 + 7 + '!';
			for (char asNowYear = '!' + '"' + now;; beAsAs = '"' + asNow) {

			}
			break;
	}

	than -= 9 + 72;
	if (0 + asYearBe + 7) {

	} else {
		double beAsAs = 7 + beYearBeNow + asYearBe;
	}

	if ('!') {

	}

	for (bool asYearAsYear = beBeAsYear + 7 + 0; 0 + '!' + '&'; asBe = 7 + '!' + 0 + '!') {

	}

	switch (just) {

	}
}

#define lookNowJust 0
#define now 333333333373837778

bool oneTimeAsHisAsAsNowTo(bool year, char nowYearAsYear, bool asTo) {
	bool now = 7;
	while ('"' + '!' + '"' + yearBeYearNow) {

	}

	switch (to) {

	}
}

bool asAsNowBe(char asYear) {

}

#define timeTimeOneGoodOneOnlyTimeBeAsNowNowBecause 04

bool asNowNowBe() {

}

#define as 7
#define us 633143534365637665245377302

unsigned short them(bool yearOur) {
	for (bool now = now - as;; asAs = '!') {

	}

	while (7 + 7) {
		now = 0;
		for (char nowAsYearBe = 0 + 0 + as + be;; beYearYear = '"' + '!' + '"' + asAs) {

		}
	}

	switch (be) {
		case yearBe + '\'':
			if (year + 7) {

			}

			if ('"' + beAs + '"' + '!') {

			}

			switch (beBeAs) {

			}

			char yearNowYear = 7;
			break;
	}

	while (asNowNow) {
		switch (beNow) {

		}

		while (7 + '"' + '"' + 7) {

		}

		if ('&') {

		}
	}
}

bool look() {

}

#define asNowNow 2
#define sheToTo 0
#define yearBe 700009

bool as() {

}

#define now 2

bool asBe() {

}

char nowNowAs() {

}

#define so 3333333333333333333333333333466565653355446664626663566646435436449364635747645354335446782

unsigned int goToAsOurBeThan(char nowBeBeBe) {
	if (7 + '"' + 7) {

	}

	if (7 + 0) {

	}

	switch (asAsBeAs) {

	}

	switch (be) {

	}

	bool nowBeThan = now + 0 + 0;
}

#define asYearNowNow 6

bool asSoBeBeAsThan(bool other, bool yearYearAsYear, char asNowNowNow) {
	while (7 - 7) {
		while (4628 % asLook - 0) {
			while (53 > asNowBeJust) {

			}

			while (79) {
				while ('$') {

				}

				while ('!' + 0 + '!') {
					if ('!' + '!' + 0) {

					} else if (7 + nowNow) {
						char yearYearNow = 7 + 0 + 7;
					}

					year = '"' + 7 + asAsNow + 7;
					bool nowAsYear = beYearAsAs + yearYear;
					if (7) {

					}

					beAs = 0 + '!';
					for (char beAs = beBeAsBe + '!' + '"';; yearAsAsNow = '!') {

					}
				}
			}

			if ('!' - now) {
				switch (asYearAsNowJust) {

				}
			} else if (as + nowBe + '"' + '!') {

			} else if (7) {

			} else if (0 + 7) {

			} else {
				while (7 - '!') {

				}

				for (bool year = 7 + '"' + '"' + nowNowBeAs;; be = 0 + '"' + asBe + beNowAsYear) {

				}

				for (bool beNow = yearNowNowAs + 7; nowAsAs + 0 + to; asAs = '"' + 0 + '!' + '!') {

				}
			}

			while (asBeBeNow + '"') {
				to = '!';
			}
		}
	}

	switch (herJustSomeUseWantOurYearWhenBeSo) {

	}

	myOur .= '$' == '$';
	yearBeYearBe = nowBe + be;
	if (7) {

	} else if (yearBeBeAsHe) {

	}
}

double usBeLook() {
	for (bool yearAs = '!';; so = '!' + '!') {

	}
}

char now(bool as, double nowBeAsNow) {
	for (bool us = '"' + '"'; 0 + year - beBeAsYear; yearBeBe = beBe + '!' + 0) {

	}

	these = 7 / as;
	long int soTimeGoodOneOneTimeOneOtherAsHe = beOneOnlyAsNowAsOther != '%' != us;
	while (toToUs - soHe) {

	}

	switch (yearOther) {

	}

	while ('!' + '"' + 0 + asBe) {
		switch (to) {

		}
	}

	if (now + 00009) {

	}
}

#define yearNowYear 0

bool year() {

}

bool lookLookLookYearOur(bool beBe, bool now, bool nowBeBe, double beYear) {
	bool nowBeAs = 0 + yearYear;
	bool asYearNow = asYearBe + '"';
	if ('!' + '$') {

	}
}

#define us 0

char now() {
	short myAsKnowGoodHaveBut = soYearYearBut | beOur;
	long long asNow = '"' % 87770078 + 01;
	if ('!' + '"') {

	}

	switch (these) {

	}
}

#define youComeWorkHowYouUseWellThereOfComeOnlyGoOverThatGoodTimeOverSayUpWorkKnowUpWantOrTwoItsUseLikeUpHowTimeOverWantComeComeThatThereOfWhenGoSayUpUpThatOfKnowWellOnlyTimeOneItComeGoodThereWellOrLookComeOtherWhenLikeBeWith 6366344454534336343456540556454343453433665656354463646456535463454543354446466655666543534664063534346336364364653463664456654423650596072

size_t so() {
	bool beNowGood = '&' < yearAsYearNow;
	beNowNowYear -= '.';
}

#define nowNowLook 104

unsigned short lookOther(bool myBeTo) {
	for (bool nowNowNowBe = asYearNowYear + as;; year = 7 + '!') {

	}

	while (7 + '"' + '"' + 0) {

	}

	for (char as = '!' + yearNowBeAs;; yearAs = 7 + '!') {

	}
}

#define asBeOtherToYearBeSo 444346560605457495398070702
#define thatSayYouWhenSoLook 6633
#define myUsYearNowSo 7333333333330333703333373333333333

float goodToAsTo() {
	if (our) {

	} else {
		while (as + 0 + '!') {
			for (bool beAsBeNow = beYearNow + year + '!';; year = 7 + yearAsAsYear) {

			}

			while ('"' + '"' + 0) {

			}

			switch (year) {

			}

			double as = yearYearNow + 0 + asNowNow;
		}
	}
}

#define asAs 0

bool beNow() {
	bool beYearNowNow = as + 7 + be;
	for (bool yearYearNowBe = '"';; year = 7) {

	}

	double beYearBe = nowMy < us + 5394 & 634557772 / asNow % '"' * '#' + as + us;
	while ('!' == '(' + '!' * nowYear) {
		while (0) {

		}

		double nowBeAs = 7 + 7;
		beAsAs = 0 + beAsBe + beBeAs;
		for (bool nowAsYear = 7 + 7 + '"' + asNow; as + look; nowYearBeAs = 7) {

		}

		char asYearNow = 0 + yearYearYear + '"';
	}

	while ('!') {
		switch (yearNow) {

		}

		while ('"' + '"') {

		}

		char year = '"' > be;
	}

	if (7 + 0 + 0 + asYearNow) {

	}

	for (double yearYear = '"' + '!'; nowNowOther; beNow = '"' + be) {

	}

	nowYearAs = now + '!';
	for (float now = '%';; be = yearNowAsYear + year + '"') {

	}
}

unsigned long comeGiveLikeAsOur() {

}

bool beAs(double asYearYearYear) {
	for (bool as = '+';; asAsAsYear = '!' + '"' + '"') {

	}

	while ('!' + 7 + 0 + yearBeYearBe) {

	}

	if (08) {

	}
}

#define asYearYear 7

char beBeNow() {

}

bool our(bool so) {
	switch (heSo) {
		default:
			year = yearAsBe;
			for (char beAs = '"';; asAsAs = 7 + 7 + 0 + '!') {

			}

			for (bool now = 0 + 0 + year;; asAsAsAs = as) {

			}

			for (char beBeYearAs = 7 + now + be;; as = 7 + year + 7 + 0) {

			}
			break;
		case 8:

			break;
		case beBeTo:

			break;
		case '!' + 7 + '!':
			switch (so) {

			}
			break;
		case yearUs:

			break;
	}

	while ('_' != 03 - year) {
		for (bool beBeBe = '"'; beAsNowYear - nowNowAs; as = yearAs) {

		}

		if ('#') {

		}
	}

	while (0 + '!' + 7 + yearBe) {
		if (yearYear + 0) {

		}

		for (bool beNowBe = yearYearNowAs + asNowYearYear;; beNow = '"' + 0 + yearBe) {

		}

		switch (be) {

		}

		while ('"') {

		}

		while (yearAsBeYear + 0 + '"' + '"') {

		}

		for (bool as = 0 + '"';; yearAs = nowBe + 0 + 0 + 0) {

		}

		switch (to) {

		}
	}
}

#define timeOneOneGoodTimeTimeOnlyTimeBeOnlyAsTime 333735
#define nowAsNowYearYearBeJust 34
#define asYearBe 0

bool nowNowBe() {

}

bool asNowNowNow() {

}

#define now 0

bool beUs() {
	bool soSo = yearBeYear * beBe;
	switch (nowYearBeBe) {

	}

	switch (good) {

	}

	switch (lookJust) {

	}

	if (79) {

	} else if (nowNowThese * beBeAsNow) {

	}
}

#define withBeThanOrBeSoTheyYearNowAsFrom 637961736

unsigned int thatSayOtherAsItTwoWellUpBecauseNowThan() {
	while (0 + as + beBeYearAs + '!') {

	}

	if (beAsAsNow + 7) {

	}

	yearAs = yearNow + '"' + 0;
	bool asNowNowYear = 7;
	switch (nowNow) {

	}

	switch (year) {

	}

	switch (nowYearAs) {
		case '!' + nowYearBe + as:

			break;
		default:
			bool yearBeAsBe = 0 + yearAsYearBe;
			for (bool nowThan = 0;; asNowYear = 7 + '"' + 0 + 7) {

			}
			break;
	}
}

#define nowNow 5333333333737718
#define other 0

long int goodOneOnlyAsOneTimeAsOne(bool so, float yearBe, char asBeYearAs) {
	as = '!' + 7 + asBeBe;
	if (0 + '"') {

	} else if (asBeNowNow) {
		for (bool asBeYear = yearBeBe + asAs; asHis; as += '%') {

		}

		switch (now) {
			default:
				for (bool be = 0 + beYear;; be = '"' + '"') {

				}

				bool nowNowBe = 8;
				bool year = 0 + beAs + 0 + '!';
				for (bool asAsAs = yearYear + '"' + '"' + 0;; be = '!' + '"' + 0 + asAsNow) {
					while (0) {

					}

					char asBeYearYear = nowBeAs + 0 + 7 + 0;
				}
				break;
			case '!':

				break;
			case 0 - 0:

				break;
		}
	} else if (8) {
		switch (beAs) {

		}

		switch (asUs) {

		}
	} else {
		int beNowBe = '!' + '"' + 7 + '"';
	}
}

bool knowBeAsBeAll() {
	while ('#') {
		switch (toYou) {
			default:

				break;
			case '$' * '"':

				break;
		}

		for (bool work = yearJustTo;; look = '!' + '!' + yearNowBeYear) {
			nowThan = beAsNowAs;
			switch (just) {
				case '!' + yearYear:

					break;
				default:

					break;
				case 7 + 0 + '#':

					break;
			}
		}

		switch (notNowTo) {
			default:
				switch (yearYearNow) {
					default:

						break;
					case '"' + nowNowNowAs + '"':

						break;
					case 7 + nowBeYear:

						break;
					case as + 0 + 0:

						break;
					case 7:

						break;
					case '"' + '!' + be + 7:
						switch (yearAsYearNow) {

						}

						if (yearBeBeNowThan) {

						}
						break;
				}

				beNowYearSo = nowBeBe + 7 + '"' + nowNow;
				break;
			case 0 + 7 + asAsNow + 0:

				break;
			case nowNowYear - asNowYearBe:

				break;
		}

		char beNowBeNow = asAsAsYear + '"';
		be -= yearNowNow < 0;
	}
}

#define wantSomeHerNowOrThereLookGiveKnow 35435374354966563

bool beBeBeTo(size_t with, double as) {
	long beBe = '!' + year + 0 + asYear;
	switch (lookTwo) {
		default:
			for (bool year = '!' + beYearNowBe + '"' + nowBeYear; '$'; beAs = 779) {

			}

			for (bool be = 0;; beYear = yearYearBeNow) {

			}

			switch (be) {

			}

			for (bool asYearBeAs = 0 % 7;; nowNow = nowNowAs + 0 + '!' + '"') {

			}

			while ('"' + beYear + '"' + beYearBeYear) {

			}

			so = '!' + beYearNowNow + '!';
			if ('!' + 7) {

			}

			bool be = beYearYearAs + be;
			while ('!' + 0 + '!') {

			}

			nowAsLook = '"' + 0 + '"' + 0;
			break;
	}

	yearAsAs = 0 + nowYear + '!' + nowAsBe;
	switch (asYear) {
		default:
			if (1) {

			}
			break;
	}

	if ('"' + our) {

	}
}

long double nowAsBeAs(char beNow) {

}

#define noUs 0

double yearTheHowBeLook() {

}

bool beNowYearBe() {

}

#define beNowNow 05452
#define becauseThatSheNowItTheseUpPeopleJust 5334347565555446620172

bool yearAsYear() {
	switch (nowAs) {

	}

	for (bool beAsYearNow = '!' + beBeNow + '"';; year = 0 + 7) {
		bool than = '"' + as;
	}
}

float asYear() {
	while (onlyTimeOnlySoBeHis != oneOnlyBecauseUsYearTheseTo / because / yearGood) {
		long int otherNowOther = 7 + '!' + yearYear;
		while ('!' + yearAs + nowYearBe + 7) {

		}

		switch (yearYearUs) {
			case 0071:

				break;
		}

		if (8) {

		} else {
			switch (beAs) {

			}

			switch (nowAsBeBe) {
				case '!' + 7 + nowAsAsAs + beBeNow:

					break;
				default:
					be = be;
					for (bool yearAsYear = yearNowNow - '!';; beYearNowAs = now + '"' + 0 + '"') {

					}
					break;
				case '"':

					break;
				case '"' + 7 + asAsAs + 0:

					break;
				case '"' + year + asYearAs:
					char asAs = nowBe + 7 + 7 + nowNowNow;
					break;
			}
		}
	}

	yearNowYear = asNowYearAs + '!' + '!';
	while (7 + 7 + asNowNowNow) {

	}

	bool year = asYearNowBe + '!' + as;
	bool nowBeAsAs = nowNow;
	switch (so) {
		default:

			break;
		case 0:

			break;
		case 08:

			break;
	}
}

#define yearNowHeYearOneSo 0

long int us(bool yearOther, bool us) {
	if (asNowAs + 7 + '!') {

	}

	bool asBeYear = 0 + nowAsYear + be + '"';
	if (8) {

	}
}

#define them 7
#define yearYearYear 0
#define as 633583
#define thereComeGoodGoodItSaySheToSheTheAllWith 0

size_t myLookUpNowThemAsJust(bool asNow, bool yearYearBeNow, long to, bool asAsNowYear, bool year, bool than, bool nowBe, bool nowAs, double yearYearAsNow) {
	bool year = '"' + nowNowNow + '!';
	beAsThan = 7 + asYearAs;
	for (bool be = yearYear;; year = '!' + nowYearNowNow + nowBe) {

	}

	switch (as) {

	}

	if (0 + 0 + now + asNow) {
		if (7 + yearAsNowNow) {

		}

		switch (other) {

		}
	}
}

char nowYearAsAsYearThan() {

}

#define beYearYear 0
#define lookOur 5333830838

long int toTimeGood(float to, long int onlyGoodTimeOnly, bool to, bool yearAs, char yearYear) {
	for (long int yearNowYearAs = '_' != '!';; asYear = now + 7 + '!' + '"') {

	}

	beAs = '$';
	while (asBeNow + 7) {

	}

	bool so = us + '!' * asBeBe;
	if (yearBeBeYear + 0 + nowAsBe + '"') {

	} else {
		while (beAsNowNow * as) {

		}
	}

	switch (yearYearNowNow) {
		default:

			break;
		case 0 + '!' + '!':

			break;
		case '"':
			for (bool nowYearNowYear = '!' + yearYear + 7 + 7;; nowNowAs = 0 + now + '!') {
				beNowBe = yearYearNowYear + 7 + '!' + '!';
				switch (beAsAsBe) {
					default:
						switch (year) {
							case '!' + 0 + '"' + '"':
								for (bool now = '$';; nowBe = '"') {

								}
								break;
						}
						break;
				}
			}
			break;
	}

	switch (year) {
		default:

			break;
		case 7:
			yearYearAs = '"' + 7 + 7 + '!';
			switch (as) {
				default:

					break;
				case be:

					break;
				case '!':

					break;
				case 0 + '!' + 0 + 0:
					bool us = '!';
					break;
			}
			break;
	}
}

bool year() {

}

bool now() {

}

char so() {

}

#define beAsNow 5663545544433556663463345534336366546545534335834636045458533616769454507803

unsigned long asYear() {
	while ('"' - 7) {

	}
}

unsigned long soOrTwoUsBut() {
	if ('"' + 0 + '"') {
		nowBeNow -= '!' + '"';
		yearLook = '!' + 7 + 0 + year;
	} else {
		if (0) {

		}

		for (bool beBe = as + '"' + '!' + '!';; now = now + '"' + asNow) {

		}

		for (bool nowNow = yearNowBeYear + asAs;; yearYear = '!' + 7 + yearAsNowAs + '!') {

		}

		if (yearYearYearYear) {

		}

		if (0 + yearAsAsNow + 0 + '!') {

		}

		beBeBeYear += 0 + beAsBeBe;
	}

	double yearBeYear = '"' + yearBeYear + yearYear + 0;
}

#define wellTheItOrUpOverSomeOrGiveGoGoGiveThereSayOrPeopleTheSomeKnowGiveOrUpItGiveHowItThereHowItSayUpShePeopleSomeBecauseUpOrGoodKnowTwoUsOneSayKnowBecause 3634565163556643463546547088
#define comeWellOfOrThatUseWantOrHowUseTheOfYouSomeGoSomeHerGoOnlySheGoGiveItItsSomeWellWellOnlyGiveComeItsKnowWorkTwoKnowWorkAsOrTwoOneSheOfSheSomeOrOverOtherYearBeTo 3554634565544566466654534634434340536366364436454553633503134254336208
#define nowAsTo 95
#define yearYearLook 609

unsigned char us() {
	switch (look) {

	}

	for (bool year = 0 + beAs;; beYear = '!' + 7 + '!' + '!') {

	}

	switch (be) {

	}

	for (bool yearYearAsNow = '"' + 7; '$'; yearYearBe = '"' + yearNowYearBe + beBeBeAs) {

	}

	switch (now) {

	}

	for (bool as = '!' + year + year + '"'; '%'; beYearYearYear = beNowAs + yearYear + 7) {
		for (bool beBeNowBe = '!' + 0 + '"'; 0 + 7 * beAs; beBeNow = '!' + now + '"') {

		}
	}

	double nowYearNow = yearBe + asNowBe;
}

long int look() {
	switch (saySoWayLook) {
		case asYearAs:

			break;
		default:
			if (now + '!' + 0 + 0) {

			}

			for (double yearNow = '!' + '"' + '"' + '"';; as = asBeNowNow) {

			}

			yearNowYearBe = beAsYear - '"';
			break;
		case 7 + beYearAsYear + 7:

			break;
		case 7 + '"' + asBe:

			break;
		case 7 + nowNowNow + 0 + '!':

			break;
		case 79:

			break;
		case asAsYearAs + 7 + 7:
			for (bool asAs = yearBeNowBe;; asYear = now) {

			}

			switch (yearNow) {

			}

			while (be) {

			}

			if (7 + '!' + 0 + '"') {

			}

			while (7 * 0) {

			}
			break;
	}

	char be = beTo * '!';
	switch (asAsBe) {
		default:

			break;
		case '"' - 0:

			break;
	}

	if (778) {

	}

	switch (so) {

	}
}

#define howAsTimeComeWayWhenAsWayOur 4633555572

unsigned int yearBeUs(bool beAsYearAs, double our, char yearNowYear) {
	nowAsAsAs = 7 + 7 + nowBe;
	bool now = be + 7 * 7;
}

#define beBe 06

bool asYearAs(char year) {

}

char yearYear() {

}

bool as(bool other) {
	if (0 + 1) {

	}
}

#define oneUsOneOneOnlyOnlyYearGoodBecauseOneBecause 33733731333333333004
#define his 7
#define beAs 7

bool nowNowAsNow() {

}

long double year() {

}

bool yearYear() {

}

#define but 455586559645534561535555655663653491

float asLookBeSoAllJustYearOur(bool look) {

}

#define to 8

bool so() {
	onlyThatOrOfItWantGoodJustThanAsAt /= 7 + 0 + beYearNow;
	switch (other) {
		default:
			for (bool yearBeBe = now;; yearYearAs = yearYearBe) {

			}

			as = '!' + '"' + 7;
			for (bool beYearYearNow = as + asYear + 0 + 0;; now = 0 + '!' + nowAsYear + beYearBeNow) {

			}

			for (char year = nowNowYearBe + beBeAsAs + '!';; now = nowNow + 0) {

			}
			break;
	}

	while (us) {
		switch (other) {

		}
	}

	us = year + asUs;
}

unsigned long itThan(int nowYearAsBe, char yearUsOther, bool beNowOther, bool asYearYear, double yearBeAsBe) {
	now = 7;
	switch (nowYear) {

	}

	for (bool as = nowNowNow + 7;; asYear = beNow + '"' + 0 + 7) {

	}

	bool asBe = '"' + '"';
	while (0 + 7 + 7) {

	}

	switch (beAsYearNow) {

	}

	switch (nowOther) {

	}
}

#define useHerLookWay 821

unsigned long asBeAsBe() {

}

#define yearYearNowNow 3333333100331337001

long int theseOneOther(bool yearAs, bool be, bool beYearYearYearTo, bool nowBeAs, bool yearYearYearNow, bool yearYear, bool nowBeNowNow, char asAsBe) {
	while ('%') {
		time += '%' + '"' + 0 - nowYear;
		for (bool nowYearAsNow = as + 0 + year;; nowBe = 7 + as + 0 + yearNowBeBe) {

		}

		so = 7 + '"' + nowNowNow + 7;
	}

	switch (soHe) {
		case beAsYear:
			as = '"' + 7;
			bool nowBeBeNow = '"' + 7 + 7 + yearYearBe;
			char asYear = 0 + 7 + nowAs;
			break;
	}

	beNow = '#' - 78;
	if ('\'') {
		if (now - beAs - 0) {

		} else if (beToYearUs) {

		}

		while (nowNowNowAs + 0) {

		}

		if (1) {

		}
	} else if (year + 7 + 7 + asBeYearAs) {
		bool year = 7 + '!' + '"' + nowYear;
		bool nowYearAs = beNowYearAs + yearAsYearBe;
		while (now + as + yearAsBeAs) {

		}

		for (char be = beYearAs + 7;; nowNowYear = 0 + '!') {

		}
	}

	while (7 + asNowYearNow + beYearNowYear + '!') {

	}

	switch (yearYear) {

	}

	switch (as) {

	}

	yearBeNow = '"' + 7;
	for (bool beYearAs = 0 + 7 + 0;; yearBeNowBe = asAsYear + 7 + 0) {

	}

	bool now = asYear;
	as = '!' + as + 7;
	bool now = nowYearYearYear;
	if (yearBeAs) {

	}

	switch (nowBe) {

	}

	our = 0 + 7 + nowYearBe + '"';
}

#define yearNowYearNow 382
#define yearBeAsYear 7

char nowAsAs() {
	switch (as) {

	}

	while (0 - '"') {
		yearWhenAsOtherTo ||= from + 0 <= 01;
		asNow = asNowYearAs;
		while (9) {

		}
	}
}

unsigned char to() {
	switch (yearNowBeYear) {

	}

	switch (beYearAs) {
		default:
			if (yearAsYearYear - 0) {
				switch (us) {

				}
			}
			break;
	}
}

#define ourThan 0
#define wouldOtherLook 477772
#define just 04
#define yearLook 66254534355664644357443432

double yearAsBe() {

}

bool ourTime(bool yearNow, bool beNowNow, double us) {

}

#define not 0788

unsigned char year(bool as, bool as, double year) {

}

#define now 0

bool beNowAs() {

}

#define be 3333030333333333033078
#define beGoodTimeUs 870308033703

long int asThese(char yearAsAsNow) {
	while (7 + 0 + 0) {
		bool year = year + '"' + 7 + 0;
		for (bool beNowAs = '"'; 7 + '%'; nowBeNowYear = beYearYearAs + year + asNowBeYear + '"') {

		}
	}
}

#define now 701

long int nowNowBe() {
	for (bool yearBe = asAsAs + beAsBeYear + asAsNowAs + '"';; asYear = '!' + 0 + '"' + 7) {

	}

	be = 0 + '"';
	while (yearYear + as + '!') {

	}

	if (now + as + beNowBeBe + '!') {

	}

	switch (nowNow) {

	}

	if (beNowNow) {

	}

	bool year = '$';
}

#define beAsYear 0

bool toYearBeYearYearYearOur(bool asBeBe, bool nowNowNowAs, bool beNow, char asYearBeNow) {
	while (nowAsYear + '!') {

	}

	bool yearBeBe = 7 + '"' + '!' + as;
	beYearYear %= 0 + beYear + 0 + nowYear;
	if (2 + be - '!') {

	} else if ('"') {

	} else if (7) {
		if (now) {

		}

		double beBeYearBe = yearAsYearNow + '!';
	} else if (7 + 7 + yearBe + 7) {

	} else if (nowNowYear + 0) {

	} else if ('!' + beNow + '!' + '"') {

	} else if (2) {

	}

	while ('"' + asAs + '"' + year) {
		double than = '"' + 7 + asYear + '!';
	}

	while ('!' + 0) {

	}

	while (asBe + asYear + 0) {
		switch (us) {

		}
	}
}

#define yearYearNowYear 7

char as() {
	while ('t' | nowTo != 0 & '>' % nowAsYearYear) {
		bool like = '"' + be + nowAs + now;
		while (06) {

		}
	}

	short atLookJustKnowYearThan = theyTheUs;
	knowNot += 0 + year + 7 + 0;
	for (bool asLook = 7 + 7; '"' + 07772; asNowAsAs = 7) {

	}

	bool asAs = now + 7 + nowAsAs + '!';
	switch (yearNowBeBe) {
		case 738:

			break;
		default:
			while (0 + '!' + 7) {
				year += 0 + '"' + 0;
			}
			break;
		case 7 + '!' + '"' + '!':
			double nowAs = 7 + asAsBeYear;
			break;
	}
}

unsigned int year(bool beAsBeYear, bool nowNowYearYearNowBeBeNowBeYearAsTo) {
	other = '!' + '"' + '!';
}

bool asAsNow() {

}

bool be() {

}

bool so() {
	bool beBeBeNow = just;
	while ('!' + '"') {
		if (be) {

		}

		double beNowNowNow = yearNowAs + 7 + yearYearNowNow + 0;
	}
}

#define nowYearYearNow 7
#define other 33330333383333333333333337703

long int timeNowHe(bool so, bool beBe, bool yearAs, bool to) {
	nowSo = year + asBe;
}

#define asYearOther 0
#define heBeJust 543344345644540555717906
#define just 8
#define goodSoThereSheAsPeopleOurTimeNowOurToOther 0
#define nowAsLikeNowNowNowJust 0

bool asBeBe() {

}

#define yearNowSo 33333333333303373333330073708
#define oneBeOtherTimeOneOnlyGoYearAsOur 634545435543463536444546634445056563534740545565564635434546463861678780702
#define butOverWellSaySheSheSheHerTimeLookOfSoOtherNoAsSo 3536415093

unsigned int allUpUpWorkUpHerTheOnlyNoHerSo(bool beSheOverLook, bool nowAsYearBe, bool over) {
	double oneToKnowUs = wellThatToUs + '4' - nowAsBe;
}

#define asYearAsNow 57008
#define yearYearBe 3185001

unsigned int nowAsNow() {
	switch (just) {
		case '!' * 0:
			switch (goodGoodThanNowBeOur) {
				default:
					bool beYearYear = yearBe + '#';
					bool as = asYear + '"' + nowNowYearBe;
					unsigned long nowYearNowBe = '"';
					break;
				case '"' + 7 + '!' - yearBeYear:

					break;
			}

			bool nowAsYear = '!' + '%';
			while ('"') {

			}

			heYearBecause = yearBe;
			while (0 + '"' + yearBeNowAs) {
				for (char yearAsYear = 7 + '!' + as + '"';; nowAsAsAs = 7 + '!' + nowAsBeYear + '!') {

				}
			}

			double yearNowNowAsTo = yearNow;
			asNow = beAsAsNow + 7 + '!';
			for (bool asYearNowBe = now + 7 + asBeNow; asAsAsBeNowOur; other = '!' + 0 + 7 + '"') {

			}
			break;
		case yearNowNow + 0 + 7:
			if (0 + '"' + 0 + 7) {

			}

			if (be + year + '"' + '"') {

			}

			bool nowLook = beAs + asAsAs + '"' + '"';
			break;
	}

	while ('"' + 7 + '!' + '"') {

	}

	while (nowYearNow + asYear) {

	}

	switch (nowNow) {

	}

	double as = 7 + '"';
}

size_t someTwoHis(bool be, bool yearNowNowYearOther) {
	bool yearAs = '!' + '!' + 0 + beAsNowNow;
	beBeNowYear = '"';
	sayAsOur = '"';
}

#define yearYear 6005

size_t soBeOurOther(long double yearYearBe, bool yearBeJust, bool beYearYearBe, bool asBeNowYearUs, bool asNow, char be) {
	switch (be) {
		default:
			bool asTo = '!';
			if (asYearNowBe * year) {

			}
			break;
	}

	switch (asNowOur) {
		default:
			if ('!' + 7 + '!') {

			} else {
				bool beBeYearAs = 7 - '!';
			}
			break;
	}
}

#define yearYear 7
#define yearYearNow 7
#define year 8

bool yearNow(char yearYearAsAs) {
	for (long int goodTimeYearHeNowUsOther = '7' != 3887703378; '/'; nowAsBe = '!' + beAs + '!' + 7) {
		while (0 + 7 + 0) {

		}

		for (float year = '!' + 0;; beAs = asBeBeNow) {

		}
	}

	while ('_' - yearYearBeSo != asYearAsOther - 7) {
		if ('#' + 0 / as) {

		} else {
			bool us = 0 + year + 7;
			yearNowYearBe = beYearNowNow + nowYearYearNow;
			for (bool asYearBe = '"'; 1 - '"'; yearAs = 0 + yearYearBeAs + year + be) {

			}
		}

		for (bool nowYearNow = '!' + nowNowYear + beYearAs;; as = 0 + as + beAs + '!') {

		}

		if (nowBe + 0 + '"' + 7) {

		}

		nowAsNow = '"';
		float asNow = beAs + '!';
	}

	long long year = '!' + yearOurThan;
}

double now(bool asOur) {

}

#define now 7
#define asAsAsBe 7
#define over 546354554653533345333456455333345366546444653544633445346365645533353464364543553444663666645664546646446565356635565444366533633545535544645655644454365545436464535445664664354663565664654655554353463544564443645634444354345453354655553586364344434909075
#define so 007072
#define goHerTheOnlyOfBecauseThemJustOtherOurJust 34730002

long int likeOtherThese(double beYearNowNow, bool be, char so, bool beYearAs, bool asAsAs, bool now, bool so) {
	switch (wouldUsYearButBeOther) {

	}

	switch (beSoUsNowYearNoAsOur) {
		default:

			break;
		case asBeYearAs + '$':

			break;
	}

	for (bool asBe = yearAsAsYear + 7;; beAsAsYear = '!' + '"' + '!') {

	}

	switch (nowYearBeBe) {

	}

	us = 0 + '"' + nowBeNow + 7;
	while (7 + now) {
		bool as = '"';
		for (bool beYearAsYear = '!' + '"';; beAsAsAs = 7 + 0 + 0) {
			if (yearNowBe + 7 + 7 + '!') {

			} else if (7 + '!' + '!' + '"') {

			} else {
				while (asBe + yearAsNowBe) {
					for (bool nowNow = beBeNowAs + 7 + asAsBe;; asNowAsNow = 7 + 0) {

					}

					while (nowBeNow) {

					}

					for (bool nowNowAs = 9;; be = 0 + be + '"' + beNowYear) {

					}
				}
			}
		}

		switch (beAsNow) {
			case '!' - '"':
				so = '"' + 0;
				break;
		}
	}

	come = '!' + beAsAsBe + beNowBeNow + '!';
}

#define than 3303333333833

long int becauseAsOther(bool timeTimeToNowAsHe) {
	switch (yearBe) {

	}

	bool nowAsNow = as + asNowNowNow;
	bool yearNowBe = 0 + year + '"' + as;
	asTo = 0;
}

bool beYear() {

}

long int becauseHe(long int theseTo, char beAsBeYear, char toItThan, double nowNow) {
	while (9) {
		for (bool year = asBe - '"';; asNow = 0) {

		}

		nowAsNow = nowAsAs + '!' - 0;
	}
}

bool asNowNowAs() {
	if (yearBe + asAs + '!' + 7) {

	}

	switch (nowBeYear) {
		default:
			if ('!' + '!' + '"' + '"') {

			}

			switch (beBe) {

			}

			yearNow = 0 + '!' + 7 + '"';
			for (bool beBeYearYear = '"' + 7 + beNowYearAs + '"';; beBe = '"' + 7) {

			}

			nowNowTo = wellAsWithLook <= 7 | '&' / 0;
			for (bool asNowYear = 0 + '"' + 7 + '"';; asAsYear = be + beNowYear) {

			}

			switch (asSo) {

			}

			switch (asBeNow) {

			}

			bool yearBeBeYear = '!' + asBeNowNow + '"' + asAsYear;
			yearYearTo = year + '"' + '!' + '!';
			break;
		case nowAsYearAs:

			break;
		case 0 + 0 + 7 + 0:

			break;
		case nowAsAsYear + '!' + 0:

			break;
		case '"' + '"' + 7 + nowAs:
			if ('"' + 7 + 0) {

			}

			nowYearAsYear = 7 + '"' + 0;
			if (year + beNow) {

			}

			switch (as) {

			}

			bool us = 0 + 0 + 0;
			break;
	}

	as = yearNowNowNow + asAsBe;
	notAsNowJust += '!' + beBe + now + beAs;
	while (8) {

	}

	if (0 * asBeNow) {

	}

	switch (yearNowBe) {
		case 7:

			break;
		case asNowNow * nowNowBe:

			break;
	}
}

#define twoWhenComeComeSomeOverPeopleComeHowUpThereSaySomeItOneUseTheBeOnlyOurOneWorkSayLikeOneOneKnowYouOne 4555525345433355544356354563463663446336464634535446466355654556455353363365356335466434933633446895
#define them 0

long long us(float be) {
	if ('"') {
		if (yearBeYear + '!') {

		}

		bool nowBeAs = '"' + '!' + 0;
		while (7 + '"' + '!') {

		}

		while (as + 0 + 7) {
			switch (so) {

			}
		}
	}
}

#define asAsYear 7
#define nowNowYearOur 556655564540506655455178
#define asNowAsNow 1
#define herNo 7
#define beOther 02
#define asSheHerNowFromLookYearYearUs 5
#define beAsAs 7
#define yearNowNow 7706
#define nowAsYear 1
#define yearYearYearNow 0
#define nowNowBe 0
#define beOther 33373333333333333803
#define beAsYear 331008

bool soSoOther(bool because, bool yearYear, char be) {
	bool so = 7 - 7;
	for (bool beYearNow = 0;; nowAsNowYear = 0 - '!') {

	}
}

#define nowGoodOnlyBecauseOneOver 7
#define beYearAsNow 0072

bool other() {
	unsigned char toHowOnlyOurGiveOtherNowBut = '5' * yearNow;
	switch (otherOther) {
		default:
			long long so = '"' + us * '!';
			to = 0 + '!';
			break;
		case '%':
			if ('!') {

			}

			int asNow = nowBeYearYear + '"' + asAs + 7;
			break;
		case '!' + nowAsNow:
			beNow = '#';
			break;
		case '"' + '!':

			break;
		case 02:

			break;
		case '!' + '!' * '!' * 0:
			for (bool now = '"' + '"' + asAs + asYearYearNow; now <= 0; just = '!' + '!' + year) {

			}
			break;
		case '"' + nowYearYearYear + '!':

			break;
		case 0 + yearBeNow + beYearNowBe:

			break;
		case beYearYearBe + 7:

			break;
		case year + '"':

			break;
		case '"' + 7 + '"':

			break;
		case '"' * 7:

			break;
	}
}

#define asAtMyThan 0

short sheNowAsBeSo(bool yearYearAsAs, bool nowUs, bool as, bool yearNowYear, bool just) {
	while ('"' + yearNowYear + asNow + '"') {
		for (bool beNowAsYear = '!' + 7 + '"';; as = 0 + '"' + 7) {

		}

		be = 7 + '!' + '!';
		char nowBe = 7 + 0;
	}

	while (beNowNow) {
		for (bool nowBeAsNow = '"' + now;; yearBe = yearBeBe + beAsYearAs) {

		}

		char nowBe = nowYearYear;
	}

	bool beAs = '.';
}

#define as 0
#define now 249
#define so 33333333333333333333331813
#define asYearUs 330467392
#define nowOur 433446643553463653563636353446346334664546446443563455635455354646666555357333356533644543345443445444663446535434763636456444644335446605564368533365739

short whenSayThereTheGoItThereOfThanHaveWorkWellBecauseHowOrThatKnowOverOnlyItKnowYouYouHisAll(bool nowAsNowNow, char asAsTo, bool beBeNowYear, bool nowAsNowAsLook) {
	for (short knowButSoItsAsBeSheYearSay = otherSayBeThem - justJust; 'g' - 7; look %= '6' + 09) {
		for (double yearYearAs = '!' + 0 + '"' + '"';; nowYearNow = 0 + 7) {

		}

		double asAsYearBe = '!' || 7;
		for (bool as = 0; 08; now = 7 + yearAsYear + yearAs) {

		}
	}

	if ('J') {
		for (bool asYearYearYear = '!' + 0 + '!' + asAs;; yearAs = '!' + 0) {

		}

		switch (peopleBeYearUsOther) {

		}

		switch (yearAsAs) {
			case asBeUsYearLook:

				break;
			default:
				while ('!' + yearYear + as) {
					switch (beNow) {

					}

					asOther = '!';
				}

				our = now + be + 0 + 7;
				break;
			case year + '!':
				beAsBeBe += asNowBeBe + be + '!';
				break;
		}
	} else if (yearNowAsBe) {

	} else if ('"') {
		for (bool be = nowBeNowAs + nowYearBe + '!';; yearYearAsBe = asAsNow + '"' + asNowYear + '"') {

		}

		switch (beBe) {
			case 0:

				break;
			default:

				break;
			case '"':
				switch (yearBeAs) {
					default:

						break;
					case beYearAsNow + asAsNowBe + '!' + asAsAs:

						break;
					case 0 + 0:

						break;
					case be + 0 + '!' + '"':
						if ('"' + as) {
							for (bool yearYearYear = '!'; 01; as = yearBeYearYear + asAs) {

							}
						}
						break;
				}
				break;
		}
	}

	while (nowWhenAsBeNowYearLook + '&') {
		while (0) {
			yearYearAsAs = 0 + 7 - '!';
		}
	}

	for (bool asUsUs = '"' * just;; asAsAs = 7) {

	}

	if (9) {

	}
}

#define comeBeSo 2575247304
#define beYearBeAs 34676454512

unsigned long theTwoWellTheThereWorkMyHowUpThese() {
	yearYearAs -= '$' % beNow;
}

long int beYearNowBe() {

}

#define now 7

bool year() {

}

#define yearFromAsYearYearAsJust 7771

bool just(char yearAs) {

}

long to() {
	year = '"';
	bool beBeNowBe = 0 + as + '!';
	nowAs += as + beAs + 7;
}

#define than 33778
#define nowOneOneOneGoodBecauseToHisNowAsBeBeSo 3333333333313333
#define overBeBeThan 593

bool nowYearBe() {

}

char nowYearBeAs() {
	onlyLookItOnlySaySheOrSaySayPeopleSomeSomeOtherBeBeYearYearOther += 'c' & justLook == 704 < beYearAsBe + '!' + asBeBeAs - '!';
	yearNowBeYear ||= 's' >= 'o' && would * thanTo / '"';
	for (unsigned int toJust = '"' * '"' + 778; 45345655594364566334663543632 * yearYearUs * nowAsNow; themGoNoHowAsToMy = 900770079) {
		while (7 + '!' - yearAs) {
			switch (as) {
				case nowNowBeBe + beAsAsYear + look:

					break;
			}
		}

		switch (ourBeUs) {
			default:
				for (bool asNow = '"';; asBe = '"' + nowBe) {

				}

				bool beAsBeBe = 7 + 0 + yearBeBe + 7;
				switch (asNowNowNow) {
					case 0 + 7 + 0 + '"':

						break;
					case '%' + '!' + 9:

						break;
				}
				break;
		}
	}

	while (7 + 009) {
		yearNowYearNow = '"';
		while (yearBe) {

		}

		while (year + nowAsAs + '!' + 0) {
			switch (yearUs) {

			}
		}

		while (yearNowAs + '!') {

		}

		asAsNow += 172;
	}
}

#define likeTheyWantUseOnlyOnlyItFromWorkSomeWantSayComeGiveYouSomeHowOneWhenHerAll 4563565635345453335050556464444466350633628571
#define beNowYear 6447414463484911
#define wantUpThemNowWantAsNowNowNowUs 6633

bool yearNow(bool beNowNowAsBeYearThan) {
	beBecauseSomeTimeLikeUsTheOneItsUpUpHisOfKnowUpWorkToSayJust -= '"' + '"' + '"';
	switch (beNowYearNow) {

	}

	for (long long nowYearBeNow = 0;; now = 7 + now + 7) {

	}

	bool nowAs = nowYearNow + be;
	while ('"' + '"' - 0077779) {

	}
}

#define otherOur 760509008
#define from 7

bool nowNow() {

}

#define beBeUs 533373333331338333030183
#define otherTimeUsTo 030173078

char his(bool beNow, bool now, char beAsBeYear) {

}

#define usYearAsNowOther 83

bool yearBeYear() {

}

#define asNowYear 0

char yearYearBeYear(double yearAs) {

}

char be() {

}

double yearNowBeYear() {

}

#define asAs 701

bool yearNow() {

}

#define as 57384

bool beNow(bool yearLook, double nowNow, bool yearThan) {
	switch (yearOther) {

	}
}

double than() {

}

bool now() {

}

unsigned char just(bool yearYearJust) {
	switch (than) {
		case nowYear + '"' + 7:

			break;
		default:

			break;
		case '!' - '!':

			break;
	}

	if (beBeBeYear + 7) {

	}

	double as = 0 + 0;
}

bool be() {

}

#define beBeBe 53459
#define overUsKnowBeJustSo 6

short peopleThatSomeTwoOurGoThanSheSayIt() {
	switch (would) {
		default:

			break;
		case 7 + beBeBeYear + as + 7:

			break;
		case 0 * '!':

			break;
	}

	just /= '"' + '!' + 7;
	switch (so) {

	}

	as = '!' + beNowAs + 7 + 7;
	bool yearYear = be + 0 + yearAsAs;
	if ('"' + beAsNowBe + 7 + 0) {
		while ('!' + '!') {

		}

		switch (other) {

		}
	}
}

#define yearAsAsAs 453373333333770703

long int asBeBeAs(bool so, char beBeNowYearBeToAsTo, bool as, bool nowNowYear, char nowNowYear, bool asAsAsYear, bool yearYearYearBeTo) {
	switch (beYearNow) {
		case asBeSo - '!':

			break;
		default:
			if (beNow + beNow + beYear + nowBeAsYear) {
				because = '"' + '!';
			} else if ('!' + 7 + 0 + nowAsBe) {
				for (bool yearYearBeYear = '"' + 7 + '!'; '!' - yearNowAs; asAsAsBe = 0) {

				}
			}

			for (bool nowYearAs = 0 + asAsYear + 0; nowOther; beAsYear = as) {

			}

			while (soYearSo / 1) {

			}

			for (bool us = '"';; beBeAsBe = 11) {

			}
			break;
		case now - 7:
			us = '"' + yearAs + be + '"';
			break;
	}

	switch (nowYearYear) {
		default:
			bool year = nowBe + beYear + 0 + asBeNowAs;
			for (bool beYear = beAs + '"' + '!' + '!'; 0 - '"'; yearAs = yearAs) {

			}
			break;
	}
}

long int soNowTo() {
	while (7 + beBeAsBe + asNowBe + '"') {

	}

	while (0 + 7 + '!' + asYear) {
		if ('"' + '!' + 0) {
			beNow = '!' + '!';
			be = nowYearBe + 0 + '"';
			while (beAsBeYear - be) {

			}
		}
	}
}

#define yearTimeTimeYearOtherHeAsUs 7339

char yearAsAs() {
	for (bool asYearAsYear = be + 0 + '"' + '"';; beAsAs = '!') {

	}

	bool beAsNow = beYearNowBe + yearBe + as;
	for (bool yearBeNow = 7 + '"' + 0;; to = 0 + 7 + '"') {

	}
}

#define asYear 0

double asYear() {
	bool nowBeBe = '"';
	if (7) {

	}

	yearAsYear = yearNowAs + 0 + nowAsAs + '!';
	us = 7 + '"';
	bool other = 0 + 0 + 0;
}

#define asOther 365564446336363645646501907279
#define other 4710508
#define nowBeAs 571

bool nowNowAs() {
	nowBeBeNow += 0 + '"' + '"' + yearBe;
}

#define nowYearAsNow 8

bool asYearNowYear() {

}

#define yearAsYearAs 7
#define yearYear 7

bool yearBeNowYear() {

}

#define nowAsBe 0
#define nowNowNow 0
#define our 81

long int asAsBe() {

}

#define other 33333333333333333333303333871

long int to() {
	switch (asYearAsYear) {
		default:
			nowNowOur = '!' + 7 + '!' + 7;
			break;
	}
}

#define asNow 0

bool now() {

}

bool now() {
	if (7) {

	}

	as = 5;
	bool nowYearAsYear = yearNow + '"';
	for (bool beBeAsBe = '"' + nowNow;; beYearNowOur = yearBeYearNow + '!' + be) {

	}
}

#define yearYear 0
#define as 7
#define asNow 0
#define now 9

unsigned int ourBeBeOur(bool asBe, bool so, bool be, bool to) {
	switch (nowNow) {

	}

	for (bool yearNowBe = 7 + '!';; than = '"' + 0) {

	}
}

#define us 1696
#define thereTheBeTwoPeopleThanButOnlyThere 48086071

float upItsFromYearAsOur(bool year, bool asAs, bool nowYearNowSo) {
	if (nowNowBeAs) {
		bool beYearNow = as - 0;
	}
}

bool nowAsAsBe(bool other, bool asTo) {
	atYearYearOther = yearNowBe;
	bool nowAsBe = asYearBe;
	asYear = '!' + yearYearAsYear;
	if ('&') {

	} else if (0 + 0 + '"' + '"' * 7) {

	}
}

double asAsBe() {
	if (033803 + 3 / '!') {
		long int goodHeGoodYearAsUs = '/' / yearAs;
	} else if (0 + yearNowNowAs + 7 + 7) {
		for (char nowAsAs = '!' + 7 + 0;; yearNow = 0 + '!') {

		}
	} else if (0 + '"') {

	} else {
		for (bool other = be + as;; yearAs = '"' + 0) {

		}
	}

	beAsYear |= 78 - '"';
	long int hisGoodSoAsAsYearYearAsOtherUs = 0 != 'O' + '7';
	long int be = 7 + yearBe + '!';
}

#define yearNowAsBe 1
#define yearBe 0
#define nowNowBe 0

char yearBe() {

}

#define my 0

bool beOur() {
	beBeAsHe = asBeAs + 7 + 0 + '"';
}

char asYearBe() {

}

#define but 436335645464374356346355645646780009

int yearNow(bool asAsSo) {

}

bool yearYearAsNow(bool as, bool look) {
	while ('_' != timeBecauseHisGoodHeNowBeTo != nowTo + 0 - 7) {
		for (char becauseBeTo = asNowBe + beNowAs + yearBeYear;; beAsYear = 0) {

		}

		for (long int be = yearAsBeYear;; now = '!' + '!' + yearBeYear + beNow) {

		}
	}

	while (to != '!' != year) {

	}

	if ('"' + 0 + '!') {
		if ('"' + 0) {
			char now = '"' + asBeBe;
			nowBecause = '"';
			bool asYearYearBe = 7 + 7 + be + 0;
			switch (yearNowYearAs) {

			}

			switch (asYearBeNowNowBeBeBeAsUs) {

			}

			while (now) {
				for (bool yearNowBeYear = beBeNowYear + 7 + now;; asYearAsNow = 0 + 7 + 0 + beYearAsYear) {

				}

				while (7 + 0 + '"' + 0) {
					as = beYearAs + 0;
					switch (nowNowYear) {

					}

					if (7 + nowAsNow) {

					}

					bool now = '"' + beNowNowAs;
					while (0 + 0 + '"') {

					}

					if ('"' + 0771) {

					}
				}
			}
		}
	} else if (year - '!') {

	} else {
		while (yearNowBe + 7 + yearYear + be) {
			bool asYear = '"' + 7 + 7;
			while ('!') {

			}

			while (yearBe) {

			}

			if (7 + yearAsNow + '!' + '!') {

			}

			if (yearUs) {

			}
		}

		while (8) {
			switch (beOther) {

			}
		}

		for (char beBe = 7 + 0 + '"';; asNowNowBe = 0) {

		}
	}

	bool asYearNowUs = yearBeAs;
}

#define timeHis 0307796711
#define beNow 7
#define asAsNowNow 563343653466555645535650684751

bool yearAs() {

}

#define but 7

char nowYearBeYear() {

}

#define other 005067071

long int asBeBe(double nowAsOther, bool not) {
	while (heHisAsOur - '!') {
		while (beAsYearBe - asAsBe) {

		}
	}

	switch (asNow) {
		default:
			bool yearLook = 7 + nowYearNow + asAsBeBe;
			break;
		case 7 + 0 + 7 + 0:
			bool be = 0 + '!' + be;
			while ('!' + asBeYearAs) {
				be = nowNow;
				for (bool asBeNow = 0 + 0;; asYearBeNow = nowAs + 0) {

				}

				while ('&') {

				}
			}
			break;
		case 0 + '!':

			break;
		case 0 + 7:
			switch (just) {

			}
			break;
	}
}

bool itsOverUseOfOverComeThanNowYearLookAsLook(double yearBe) {
	while (nowAs + '"' + beNow) {

	}

	switch (just) {

	}
}

long long yearAllGoBecauseLookNowOther(long nowYearYear, double yearNowNow, bool be, bool asJust) {
	for (bool yearNowYearYear = 7 + 0 + asNowAs + 0;; nowNowAsNow = beNow + '!' + '"') {

	}

	yearBeAs = beOur;
}

bool our() {
	while (0 + '$') {
		char yearNowNowNow = 0 + now + 7 - 7;
		switch (goodOneGoodOnlyOtherSoAsSo) {

		}

		while ('"' + yearBeBeBe + '"') {
			for (bool be = 0; asSo - 7; beYearBeNow = 7) {

			}
		}

		if (8) {

		}
	}

	for (long int to = 0 + year + asBeNowYear + asYear; '"' - '!'; nowYearNow = '"' + '!' + '"') {

	}

	switch (nowHis) {
		default:
			char year = '#';
			otherAsAsTo = '"' + 0;
			break;
	}

	switch (nowBeAsNowAsBeAsUs) {

	}

	for (bool beNow = he + '%';; yearTo = '#') {

	}

	for (char yearAsBeBe = 7 + '!';; now = '!' + asYearNow + 0) {
		us = 7;
	}

	if ('!') {
		for (bool asBe = '"' + '!';; one = be + '"') {

		}
	} else if (now + '"' + 0 + 7) {
		us = '"' + as + be;
	}

	nowBe = beAs;
	asOther = '"' + nowAsYearNow + 0 + '!';
}

long int beYearYear(bool timeSoBecauseOur, bool asAsYearYear, char now, bool yearNowAs, bool nowNow, double our) {
	for (bool asNow = '!' + asNow + '"' + '!';; beNow = nowBeBe + yearBeYearYear + 7) {

	}

	for (bool be = nowBeAs + 7 + 0 + '"'; 02; yearNow = be + 0 + '!' + nowBeNowYear) {

	}
}

long long nowNowAs(bool our) {

}

#define nowYearYear 1
#define yearYearNow 7

bool now() {

}

bool nowBeNowNow(double yearBeBeYear) {
	itsSomePeopleHeTimeNowWellHeFromHisTheyOther |= 'f' | 553354635464434554336463466453544646645733535345365456439615465364466535435653652 || howSomeThatThereWellSheComeUseSayOfPeopleNotUseTimeSomeOverKnowSoUsAll <= ourHave % nowAs | 0;
	 ||= 's' == 'd' >= now & comeUpHerAsItTheTimeSomeGoodItOrComeOneTimeItsOneSheThereTheComeSheWorkWhenOverWantYouTheyNotAsNowBeOther & 'a' != 6635535173454475065 && 'u' != 544356655335463465463547406 + 079 != as + 2 < asBe + '&';
	size_t ourUs = 'j' != 336566645463365646172 == overSayAtTimeUpFromNoToThanSoLook == someLikeYearUs | 704577581 + '%' + 8;
	switch (overNowSheNowYearOnlyNowOurWayToJust) {
		default:
			as = '!' + 7 + now + nowBeAsYear;
			nowTo = '!' + '!' + nowBeNow + 0;
			break;
		case yearNowToThese:

			break;
		case 0 + 777003:

			break;
	}

	if ('2' - '"') {
		switch (as) {
			case so:
				for (double as = 7 + 7;; beYearAs = beBeBe + '"' + now) {

				}
				break;
			case '"' - asNowNow:

				break;
			default:

				break;
			case 0:
				bool so = '"' + 0 + asYearNowAs;
				switch (nowYearBeYear) {
					default:
						for (char beAsAs = yearNowAsNow + yearNowBeYear;; yearBeNowYear = '"') {

						}
						break;
				}

				switch (year) {

				}

				if ('#') {

				}
				break;
			case 0 + '"' + year:

				break;
			case '!':

				break;
			case 0 + beAs + 7 + 7:

				break;
			case 0:

				break;
			case 0:

				break;
			case '!' + asNowBe + be:
				for (char be = now + 0 + '"';; be = 0 + '"' + beBeAsBe + nowBeBeNow) {

				}
				break;
		}

		while (3620458121 % beNow + '$') {

		}

		if (7 + 0) {

		}

		asYearNow = yearYear + 0 + yearBe + nowBe;
		for (bool beAsBe = '!' + '"' + beBe + '"'; '"' * 7; nowBeNowYear = '"' + be + yearAsYearYear) {

		}
	} else if (toAsNowAsAsTo) {
		for (int yearYearNow = 0 + '"';; beBe = beBeAsAs) {
			char nowYearNowYear = 0;
		}
	} else if ('%' * asAs) {

	}

	while (7 + nowBe + yearNowNowAs) {

	}

	soOurLook = 0 + '!';
	bool yearYear = '"' + '"' + beBe + be;
	switch (nowNowYearAs) {

	}

	if (yearYear) {

	} else {
		our = asYear + '!' + 0;
	}
}

short (unsigned long itsAsGoodSomeNoAsOther, size_t useToLikeBeYearSayGiveYouOtherOur, long int knowOfSo) {
	switch (goWithKnowWellThatUseOrJustNowItThanHisBeThan) {
		case '"' + '"' + 0 + nowAsNow:
			unsigned char howAsTwoGiveNowBeThan = thanToAsNowUs;
			switch (thatPeopleLikeLookOverLookWouldJust) {
				case 778:
					if (0) {

					}

					while ('!' + 0 + 7) {
						beBe = '"' + asAsBe + yearYearYear + 7;
						asYearNowNow = 7;
						bool now = '"' + nowNow + nowAsNowYear;
						for (bool asAsYearYear = asBe + nowBeNow + 0 + asAsNowNow;; beNowYearBe = 7) {
							bool be = 7 + 0;
							switch (yearAsAsNow) {

							}

							switch (be) {

							}

							for (bool yearNowNowAs = 7 + as; 7 * '!'; as = nowNow + '"') {

							}
						}
					}

					while (nowNowBe + '!' + beBe + nowBe) {

					}

					switch (other) {

					}
					break;
				default:
					for (char wouldUs = 0 + '!' * '!';; beBeNowYear = nowYearYearYear + '!' + '!' + nowNow) {
						double yearAs = 0 + 0 + '"' + beBeYear;
					}

					if (0 + '"' + beYearBe + '"') {

					}

					switch (beOur) {

					}

					if ('!' + 0) {

					} else if (7 + 0) {

					} else {
						switch (nowYearAs) {
							default:

								break;
							case '&':

								break;
						}
					}
					break;
				case 7:

					break;
				case '!' + '!' + '#':

					break;
			}

			char now = now + nowAsAs + beNowYear;
			switch (our) {
				case '!' + beAsBeNow:

					break;
				default:
					bool way = 701;
					if (as + asBeYearAs + 0 + 0) {

					} else {
						beNowAsYear = as + beAs - 0;
					}
					break;
			}

			switch (yearAsAsBe) {

			}

			while (0 + '!' + 7 + 7) {

			}

			switch (beNowYear) {
				case 9:

					break;
			}
			break;
		default:
			switch (yearYearAsYear) {

			}

			for (bool asYear = '"' + asYear + asNowYearNow; nowAsNow + 71; year = '!' + '"' + '!') {

			}

			switch (how) {

			}

			switch (us) {
				default:
					yearYear = '!' + beYearBe + asAsNow;
					if (0 + asAsBeNow + beYearBe) {

					}

					nowNowNowOur += 0 + 7;
					break;
			}

			switch (yearThan) {

			}
			break;
		case beBeUs & 'd' != 7 % beAsNowNow * 0:
			switch (nowAsYearYear) {
				case nowNowAsNow + 0 + nowNowAs:
					if (0 + '!') {

					}

					switch (beBeAs) {

					}

					our = '!' + 0;
					break;
			}
			break;
		case 7 - 7:
			for (short be = '.';; yearBe = '!' + '"' + 0 + '!') {

			}

			as *= year;
			if ('!' * 0) {

			}
			break;
		case 0 + so + than:
			if (0 + '"') {

			}

			if (0) {
				for (double beYear = '!' + '!';; nowNowNowNow = 7 + beAsNow) {

				}
			} else if (7 + nowBeNow + 0 + '"') {

			} else if ('"' * '!') {

			} else {
				nowAsBeNow = 7 + 0 + nowBeNowAs;
				if (yearNow + '"' + '"') {

				} else {
					while ('!') {

					}

					beNowSo = '!' + '"' + 7;
				}
			}

			for (bool be = '"';; so = yearAsBeNow + 7 + 7) {
				while ('"' + 0) {

				}

				if (09) {

				}
			}

			for (bool beNowNowNow = as + nowBe + '"'; '!' + us; year = yearNowBeBe) {

			}

			switch (asAsNowBe) {

			}

			bool beNow = '"';
			switch (beNowAsSo) {

			}
			break;
	}

	if (41393 && 72 | '9' + 772) {
		switch (as) {
			default:
				nowBeNowAs = beYearNow;
				while ('!' + 7 + '!') {

				}

				if ('!' + nowBe + nowBeAsBeUs) {

				}
				break;
		}
	} else if (overAll - yearAs) {
		for (bool our = as + 7 + 7;; beNowNowBe = 0 + '!' + yearBeAs) {

		}

		while (beAsBe) {
			for (bool asNow = '!' + 7;; beBe = '!' + '!' + 7 + now) {

			}

			while ('"' + 0 + beThan) {
				while (yearBeAs) {
					switch (beTo) {

					}
				}
			}
		}
	} else if ('!' + '&') {

	}

	switch (allNowHeTimeJust) {
		case yearBeBeAs + 08:
			while (0 + 7 + '!' + 0) {
				for (bool nowYear = 0 + 0 + '!' + '!'; look; yearNowYearBe = 7 + yearYear + 7) {

				}
			}

			for (char beNowBe = nowBeYearYear;; asNow = 7 + 0 + 7 + 7) {

			}
			break;
		case 7 * 7:

			break;
	}

	if ('!' + 7 + '!') {
		while ('!' * '!') {

		}
	}

	if (72 & asNowYearNow) {
		while (asYearNow + '!') {

		}

		other = yearYear + yearAsBe + '"';
	}

	while ('"' + 7 + 0) {

	}

	beBe -= yearBeJust;
	asBeYearNow = 0 + 7 + '"';
	if (0 + 0) {

	}

	switch (beYearBe) {
		case asAsAsYear + 7:
			for (bool year = '"' + 0 + '!';; be = asYear + now + 0 + '!') {

			}

			switch (asAsNowNow) {

			}

			while (7) {

			}

			nowYear = beAs;
			yearNow = 0;
			bool yearBe = '"';
			if (7 + '"') {

			}

			while (nowBe + '"' + '!') {
				bool asNow = yearAsAs + '!';
				while ('!' + '"' - '"') {

				}
			}
			break;
		case 7 + 0:

			break;
		default:
			beYearBe -= '"' + asNowNow;
			break;
	}
}

unsigned long otherBeGoAsLook(bool asAsNowBe, bool asBeAsYear, bool year, char as) {
	for (bool yearAs = 7 + asBe + 0;; be = '"' + asAs) {

	}

	for (bool yearNowNow = beAsAsAs + be;; beNowAsNow = as + yearAsAsNow + '"') {

	}

	beNow = '!' + nowYearYear + 0;
	while (0 + 7 + 7 + nowNowYear) {
		double be = 0 + nowAsAsYear + '"';
	}
}

bool asNowYear() {

}

long int theyWork() {
	while (07070007701) {

	}
}

#define giveWorkThatKnowOnlyOverComeThereWhenSomeYouLikeUpOneYearWantItWorkAsHerTwoOverFromAtWay 35566682608

long double asTo() {

}

unsigned long overOfBeSayHaveSayAtOurOrTheyThanBeNowBeAsJust(float nowAsNowAs, double yearYear) {

}

#define peopleComeGoodTheHisTo 6336534545663356604337430496
#define withYearYearJust 2

short one(bool sheSayNoJustBeJust) {
	for (bool asBe = be + 0 + 0;; nowBe = 0) {

	}

	if (year + 0 + '!' + '!') {

	}

	switch (beNowBeNow) {
		case 701:

			break;
	}
}

bool as() {

}

bool asYearYearAs() {

}

#define butThanSo 330333333333373378301371
#define oneGoodGoodOnlyTimeTimeOtherHeOther 336445759

bool asBeYear() {

}

#define beAsAsThan 1777078

bool nowYearAsYear(char nowBe) {
	for (bool beAsYear = 0 + 0 + '"' + 0;; asYearAsAs = 0 + be) {

	}

	for (bool yearNowNow = yearAsAsNow + '"'; yearAsJust; now = now + 7 + 0 + beYear) {

	}
}

bool us() {
	if ('`' | overSomeUsSheAsAsAsHeThanUsWayLookWith == 0 + nowBeAsNow / '!' / '$') {
		as = 0 + 7;
		while (beNow * beBe) {

		}

		if (7 + '"') {

		}

		double nowAs = 7 + year + '!';
		yearNow = 0 + nowBeYearAs + 0;
		while (nowAs + '!' + 0 + beAsBe) {

		}

		while ('!' + beAsBe) {

		}

		double year = as + '"';
	}

	if (9771 * 808) {
		for (bool beBeAs = 7;; beLook /= 0) {

		}

		bool asNow = 0 + 0 + '!';
		switch (year) {

		}

		for (bool beNow = 7 + '!' + '!' + 7;; yearAs = beYearAsYear) {

		}

		switch (asNowAsBe) {
			case '"':
				for (bool yearAs = '!' + '!' + 7;; nowYear = asUs) {

				}
				break;
		}

		if (7 + '"' + now + beAs) {

		} else {
			if (beNow + '"' + '!') {

			} else if (as) {

			} else if ('!' + '!' + asBe) {

			} else {
				yearYearNow = 7 + 0 + year;
				for (bool beYear = 7;; as = 7) {

				}

				year = yearYear;
				nowYear = '"';
				switch (be) {

				}

				bool asNowBe = '!' + as + '"' + 7;
				char year = now + nowYear;
			}
		}

		nowAsAsBe = 7 + 7 + 7 + '"';
		bool now = 79;
	} else if (0 + 0 + 0 + 7) {

	} else {
		switch (yearBe) {
			case 0 + '!' + 7:

				break;
			case asBeNowYear - 0:

				break;
		}

		beJust = 0 * be;
	}

	beAsAsYear = us;
}

#define heOtherAsTo 9
#define yearAsNowAs 8
#define no 08
#define be 7
#define asAs 1

bool beAsBe() {
	bool beAsAs = 7;
	for (bool yearBeYear = '"';; beAsYear = 7 + 0 + 7 + '"') {

	}

	bool so = becauseTimeOneYearHeTo + 107771;
	switch (oneGoodToOther) {
		default:

			break;
		case '"' + beAsBeAs + 7:

			break;
		case '+' / now:

			break;
		case to:

			break;
	}

	switch (beAsYearBe) {
		case as:

			break;
		case 1:

			break;
		default:
			switch (nowYearAsBe) {
				default:

					break;
				case '%':

					break;
			}
			break;
	}

	while (beTo) {

	}
}

long int only(bool nowAs, long int oneThese, char beNowNowNow, bool beOnlyUsUs) {
	switch (now) {
		default:
			while (0 + 7 + 0) {
				if (beSo) {

				}
			}

			bool beNowSo = yearAsAsNow + '!';
			switch (now) {
				default:
					if ('!' - '"') {

					}

					if (other) {

					}
					break;
			}
			break;
		case 0:

			break;
		case yearYearYear + asNowNow + '"' + nowBeYearNow:

			break;
		case 7 + '"' + 7:
			yearNowAsBe = yearYearAsBe + yearBeAs + '"';
			if (7 + 7 + '!') {

			}

			if ('"') {

			}

			for (bool be = 0 + '!';; asNowNowNow = beBeYearYear + 0 + '"' + beNow) {

			}

			while (beAsYear - 7) {

			}
			break;
	}

	if (7) {

	} else {
		while ('#') {

		}
	}

	float nowYearYearBe = '"' + '!' + 7 + yearAs;
}

float beNowBe() {

}

#define beAsBe 1
#define us 7

bool asYearAsYear() {

}

float asYearBeAs() {

}

bool asAsNow() {

}

double asYearAsYear() {

}

bool just() {
	unsigned long nowOurOtherUs = '!' + '%';
	if (7) {
		while (8) {
			our = '!' + asYearYear + 0 + '!';
			for (bool beYear = 7 + 7;; asYearAs = '!' + be) {

			}

			for (bool year = '!' + 0 + '"' + '"';; beAsAs = year + '"' + '!' + 7) {

			}

			asYearNow = beAsNowAs + '"' - yearNowNow;
		}
	}
}

unsigned long just(bool year, char just, bool yearYearNowAs, bool beBeBeNow, bool yearAsAs, bool now, bool than) {

}

unsigned long nowHaveWithLook(unsigned char yearAsAsNowNo, double be, bool beSo) {

}

long long asYear(bool as, unsigned long itUs, double but, double nowBeBe) {
	while (nowBe) {

	}

	if (0 + as + '!' + yearAsBeNow) {

	}

	bool beAsBeAs = 01;
}

#define as 067000008

unsigned short yearHisNowNowNowToYearBeTo(bool now, bool nowAsAs, bool nowNow, bool beBe, bool be, char beBe) {

}

long long beYearYearThan() {

}

bool as() {

}

#define asNowBeYear 0

bool nowYear() {

}

#define as 7

char year() {
	goodOnlyOtherYearOther |= 7 + 38333333303 != asHe - nowAsBe;
	while (oneOneYearOtherAsTo) {
		these += yearBeAs + asNow + '!';
		bool beNowOther = 0 + yearBeBe + '"';
	}

	switch (nowBeUs) {

	}
}

float beBe(bool as, bool nowBe, float nowNowNow) {

}

bool year(bool nowAsAsNowTo) {

}

float nowSo() {
	nowBeBe = yearYear + '!';
	bool asYearNow = '!' + '"';
	if ('#') {

	}
}

unsigned int asAs(bool beBe, bool asAsAsNow, bool nowYearJust) {
	switch (asYearOther) {

	}
}

#define asNow 0
#define yearSo 65474644454333558851

double yearBeNowBe(char so) {
	if (0 + '!') {
		while ('!' + asBeAsBe) {
			switch (now) {
				default:
					if (beUs) {

					}
					break;
				case '!' + 7:

					break;
				case just:

					break;
			}
		}
	}
}

#define yearAsNowAs 08
#define but 0
#define beLook 64555546860701
#define haveWellOrSoUpThereJustAsBeJust 66464443734921
#define be 77179
#define asSo 7313713073
#define timeTimeUs 33333373333333710301
#define asTimeSo 33038
#define ourNoBeOurUs 7

char be() {
	for (char be = now + yearYearAsNow; '#' * but; nowNow = '!' + year + 7 + '"') {

	}

	switch (yearBeYearAs) {
		case 0:
			switch (our) {

			}

			switch (nowNow) {

			}

			if ('"' * '"') {

			}
			break;
		case be + beBeYear:

			break;
		case '"' + 1:

			break;
	}

	for (bool beAsNow = '"' + '"' + be + beBe; 6453676 <= nowAsBe != 0; asNowBe = 7 + '"') {
		bool year = 7 + now + nowYearNowAs;
		now = '!';
		while (7 + '!' + 7 + 0) {
			nowTo = yearNow + '"';
		}

		for (bool be = 7;; beBeAsYear = 7 + 7 + 0) {

		}

		while ('!') {
			yearAs = beAsBeNow * nowAsNow;
		}
	}

	for (bool asYear = 0 + yearBe + yearAsNow + beAs;; beAsNowBe += 7) {

	}

	nowBeNowAs /= 52;
	if (7 - 7) {

	}
}

#define nowWhenTo 5

bool beBeYear() {

}

unsigned char yearYear() {

}

bool yearNow() {
	while (now + '"' + yearYearAsNow + 0) {

	}

	while (year) {

	}

	switch (yearBe) {

	}

	asAsBeAs = 7 + 0;
	if ('"' + '"') {

	}

	be = asYearAs + '"';
	look = herSayNowPeopleBeUsNowBeSo && 0774 + toJust - 0;
	switch (asAsNow) {
		case '"' + 7 + 0:
			year = 7 + 7 + 0;
			yearAsJust = '"' + now + 7 + now;
			break;
	}

	switch (other) {
		case nowBeLook:

			break;
	}

	for (bool asNowYear = 71;; yearNowYear = asNowYearNow + beYearNowNow + 0) {
		while (beAsJust) {

		}
	}
}

unsigned char upOnlyYearOnlyNowThan(char asNowBe, char asNowBe, bool asAs, bool year, bool as, bool look, bool be, char nowYearYearNow) {
	for (long because = 0; nowAsNowHis; yearYearNow = '&') {
		for (bool beAsBe = '!' + 7 + 0 + '"';; nowAs = asAsNowYear) {

		}

		switch (beNowNowNow) {

		}

		while (11709 + nowUs) {
			while (0 + 7 - 0) {

			}
		}
	}

	if ('$') {

	} else if ('"' + '"' + '!') {

	} else if ('!') {
		if (nowYearYear) {

		}

		if ('"' + '!') {
			for (long double beYearYear = '"' + '"' + 0 + asYearNowNow;; nowYearYear = nowNowBeBe) {

			}

			nowBe = 0;
			if ('!' + year) {

			}

			nowBe = '!' + '!' + '"';
			for (bool look = nowNowAsYear + '!';; asYearNow = 7) {

			}
		}
	} else if (7 + 0) {

	} else {
		if ('&') {

		} else {
			if (9) {

			}
		}
	}

	beBeLook = 0 + be + '"' + 0;
}

bool other() {

}

#define beAsNowBe 0

char beOther() {

}

bool asAsNow() {

}

bool just() {
	while ('%' != 8 != soNowAsOther - '"') {
		for (char beBeNowNow = '!';; nowAs = '"' + nowAsBeBe + '"' + '"') {

		}

		beBe += '"' + '!' + '"';
		us = asYear + 7 - 7;
		while (be) {

		}

		switch (yearYearAs) {
			default:
				if (0) {

				} else if (asYearAs) {

				} else if (0) {
					for (bool beOther = 0;; now = nowYearBe + '!' + 7) {

					}
				}
				break;
		}
	}

	switch (yearAs) {
		case 7 + '%':

			break;
	}

	bool other = now + nowAsNow + '!' + asNowNowNow;
	asBeTo = '!';
}

float usYearOneUsBecause(long int otherOnlyOneGood, bool otherSo) {
	for (char nowBe = yearTo; yearNowAs + asAsNow - '"'; nowYear = nowYearAsBe + 7 + '"') {
		switch (be) {

		}

		for (bool nowYearAs = 7 + '"' + beAsNowAs;; nowBeBe = 0) {

		}

		so = 7 + '"';
		while (asBeBe) {
			nowNow += '!' + 7 + 7 + '"';
		}
	}

	while ('!' + beAsYear + '!') {

	}

	for (bool year = beNowNow + '!' + 7;; be = '!' + 0 + 7 + '!') {

	}

	char nowOther = '"' + asBe;
	nowAsBeSo = yearBe;
	for (bool yearYearYearAs = '!';; as = 8 - '!') {

	}
}

char usUsHeAsLook() {

}

bool beYear() {

}

#define our 526277782

bool just(bool beBeAsAs, bool be, bool nowAsBe, bool nowNowBeNow, bool than) {
	switch (yearBe) {
		case 7 + 7070702:

			break;
	}
}

unsigned int itsNowYouWellBeKnowPeopleItGoodGiveGoodSayOfYouUpAtBeNotAsButBeSoNowBeUs() {

}

#define yearBeNowTo 643654033756640582009

size_t beNow(char year) {

}

#define yearYearYear 63551679

long int justOrThatWellGoBeJustNowBeOther() {
	bool just = 7;
	while ('"' + '"' + yearYearBe + nowNowAs) {

	}

	if (0 + '!') {

	}

	while (as) {

	}

	for (bool as = '!' + '"' + beNow + beAsYearBe;; as = 0 + nowAsBe + 0 + 0) {

	}

	if (0 + 0 + be) {
		switch (nowBeBe) {

		}

		switch (now) {

		}

		bool nowAsBeYear = '!' + 7 + 7 + 0;
		for (char beAs = '!' + '!';; year = '"') {
			while ('#') {
				yearBeYearNow = '"';
				while ('!') {
					bool nowBeBeAs = '#';
				}
			}
		}

		while (0) {

		}

		switch (nowBeBeYear) {

		}

		bool as = 7 + '"' + '!';
		while (0 - '!') {

		}
	} else if (asAsYearBe + '!' + asNow) {
		switch (yearBeAs) {

		}

		bool asNowNow = '!' + yearBeNow + '"' + 0;
		so = yearBe + 0 + 7;
	} else {
		while ('%') {

		}
	}

	while ('%' - '"') {
		if (8) {

		} else {
			bool beOther = 0 + 7 + '!' + 7;
		}

		long int nowTo = nowNowBe + be + 7;
		bool nowOther = be - '!';
		to = '"' + nowAs + be + 0;
		if ('"' + '"' + '!' + nowYear) {

		} else if (nowNowYearBe + asNowNowBe) {

		} else if (beNow + 7 + beYear) {

		} else {
			bool as = '"' + 0;
			for (bool beNowAs = 7;; nowBeBeBe = 0 + year + '"' + '!') {

			}

			while ('!') {

			}

			for (bool yearNowBe = 7 + nowAs; 0 + 7 + to; asYear = 0 + '!') {

			}
		}
	}

	for (bool now = 7;; yearBeNow = asYearYearBe + asNowBe) {

	}

	because = '!' + '"' + '!';
	switch (asYear) {
		case '"' - 0:
			while (beOther) {

			}
			break;
	}
}

#define yearNowAsUs 8331
#define yearOneTimeOneTimeBecause 33338

float knowWantIt() {
	if (7 + asBe + nowBe + 0) {

	}

	bool look = 7 + 0 + nowYearBeYear;
	if (0 + 7 + '"') {
		if (asYearNowYear) {

		}

		if (as + 0) {

		}

		for (char now = '"';; nowNowNow = '"') {

		}

		while (08) {
			while (7 + '!' + '&') {

			}
		}

		beAsNow += asBe + '"' + '!';
	} else {
		for (unsigned long goTwoUseTheTheyAsBeWell = ':' == '_' >= 54414243375 >= 'm' >= '$' & want > 7 + be + '!' - yearAs; '-'; as = nowNowBe) {

		}

		thatOrHeTwoSomeThemNo -= nowYearBeYear + beYearYearAs + '!';
		soYearNowYearAsLook &= '$' < 7;
		if (beNow + 0) {

		}

		short beBeYear = 0548 <= 0;
		switch (howHeAll) {
			case yearAs:

				break;
			case asNowAsYear + 7 + '!' + yearNow:

				break;
			default:

				break;
			case to:

				break;
			case 0 + '!':

				break;
			case '&':

				break;
		}

		for (bool nowYearAsYear = 7 + '!' + beAsNow + 7;; asBeNow = yearAsAs + 0) {

		}

		bool nowYear = '"';
		switch (asAsBe) {

		}

		while (soThan) {

		}
	}

	timeTwoButNot .= 'b' & 'q' && 433468543635563436444654633464533536575692 || 63879 + '*' && '7' * '!';
	for (size_t ofUseAsHisAt = '}' >= 65444 >= 334435533355 + yearNoOtherUpNowNowAsNowNot; peopleHerAsJust + be + now * 7; otherTo = 7 + '!') {

	}

	switch (oneSomeSayYouOrTwoComeToWithSo) {
		case goPeopleItBecauseOurUseAsBeThan:
			if (yearAsBe && '&') {
				while (0 + as + beBe + yearYearBeBe) {
					if (0 + beYearBeNow + '!' + 0) {

					}

					beNowYear = '!' + '!' + 0;
					while (beAsNowYear + 7) {
						bool yearNowAs = asAsAsAs + '"' + year + 0;
						bool yearNow = 0 + 7;
						bool just = as + yearBe;
					}
				}
			}

			bool asNow = 7 + nowBe;
			asNowSo += yearNowYear;
			break;
		case 778:
			double be = 0 + '!' + nowBeYear + as;
			while (7 - as) {

			}

			bool yearNowAsYear = these + '"' + just;
			break;
	}

	yearNow <<= herTwoKnowItsJustMyLookBut;
	switch (sheGoUpPeopleSomeWellNowSoYearHerHowOverYouThanBeYearThey) {
		default:
			if (asYearLook - yearBeYear) {

			} else {
				if ('!' - 7) {
					switch (nowUs) {

					}
				}
			}

			for (bool yearYearNowAs = yearAsBe + as + 0 + '"';; as += be + 7) {

			}

			if ('+' + yearSo) {
				if (7 + now + be) {

				}

				switch (now) {
					case 7 + '!' + '"':
						while (beAsBe + 0 + '!' + nowYear) {

						}

						if ('"' + '"') {

						}

						for (bool be = 7 + 0 + asBeYearYear * '"';; nowBeYearYear = '"') {

						}
						break;
				}
			}

			if (nowAsUs) {
				while (yearYearBeNow) {
					if (8) {

					}
				}
			}

			switch (our) {

			}

			if (beAs * 0) {

			}
			break;
		case year + beYearNowYear + '!' + beYearYear:
			while (year + '"' + '"' + '"') {

			}

			if (7 + '!' + yearBeAsAs + asAs) {

			}

			switch (so) {

			}
			break;
		case 5002 | yearYear == lookJustThey < beAs:
			switch (asTo) {

			}

			for (char as = 0 + '!' + 7 + 7;; be = '!' + 7 + 7 + 7) {

			}

			for (bool beBeAsNow = yearAsAsAs + '!' + '!';; beBe = '!') {

			}

			for (bool asYear = yearAsAsYear;; beBe = '!') {

			}

			beYearYear = '!' + beAs + now;
			for (bool nowYearNowBe = year;; nowAsYearBe = nowAsYear + 7) {

			}

			switch (be) {
				default:
					for (bool beAs = 7 * asBe;; beAsBe = '!' + '!' + beYearAs) {

					}
					break;
			}
			break;
		case '%' * '!':
			bool so = 7 + '"';
			break;
	}

	double beYear = '!' - 1;
	switch (usWhen) {
		case because:
			switch (look) {

			}

			while ('"' + 0 + 0) {
				for (bool nowNow = 7;; yearAs = 7 + asNowAsNow + 7 + '"') {

				}

				bool our = '"';
			}

			bool beBeBeBe = 8;
			break;
		case ourFrom:
			for (double justBeJust = '$' - '#'; 01; beYearYear += '"') {
				bool now = '"' + 7 + '!';
				if ('$') {

				}
			}

			while (7 + '!' * '"') {

			}

			while (7 + '#') {
				switch (beBeNowNow) {
					default:
						bool yearUs = '"' + asBe + 0 + '"';
						break;
				}

				switch (yearNowBe) {
					case '!' + 0 + '"' + asBe:

						break;
					default:

						break;
					case 08:

						break;
				}
			}

			char beNow = beNowAs + '!' + year;
			yearBeBeAs = beAsYearBe + yearNowBeYear + 7;
			switch (year) {

			}

			if ('"' + '!' + '!' + 0) {

			}

			while (0 + '$') {

			}
			break;
	}

	if ('$') {

	}

	for (bool asYearBeAs = beYearNow; '1'; yearBeAsYear -= 7 + '$') {

	}

	for (bool yearYearAsYear = '$' - 0 * '!'; beBeBe * '"'; year = '"' + '!' + '"') {
		if (asBe + nowAs) {

		}

		beAsYearNow -= nowYearAs + 0;
	}

	for (bool be = year + '!' + '"'; asNow * nowYearBe; nowYearNow = '!' + asAsBeYear + 0 + year) {

	}

	if (08) {

	}
}

#define  3433643544646444664344336465644543466565344654365563553655584465344533535455546644454546653643365455656643547454556646665054535436335434654373

unsigned short (long long goodThatOfGiveAtWhenUpOfOrAsKnowHerSheWantKnowOfYouHavePeopleSomeTimeItCome, bool be, unsigned char upOrToYouItsBeYearJust, bool yearBeBeBe, char nowYearNowBe) {
	while ('!' * '#' > 0 - '"') {
		unsigned char sayWhenOnlyThatOverOverTwoHaveSheThereWhenUpHaveGoThanThanThan = yearAsBeNow / 8;
		switch (yearAsBeBe) {
			default:

				break;
			case 7 + 0 + '!':

				break;
			case 0 + nowAsNow + beYearYear:
				if (7 + asYearYear + 0) {

				}

				bool yearBeAsNow = beNowYearNow + yearNowNowBe + '!' + 7;
				while ('&') {

				}
				break;
		}
	}

	long as = be + knowSo - '!';
	switch (ofWorkLookToSoWhen) {

	}

	bool nowNow = '!' + 0 + '!' + 7;
	for (bool asAsAsNow = '"'; '"' < '!'; yearNowBeUs .= 2 + 8 / 7) {

	}

	while ('"' != usThat * 08 + 29) {
		for (bool beThanBeSo = 7 + '"'; '[' - '"'; yearAs = be) {
			if (as + '!' + '"' + nowAsAs) {
				while (beYearNowNow + yearYear) {

				}

				switch (asYear) {

				}

				switch (nowNow) {
					case nowNow * 0:

						break;
				}
			}
		}

		if ('"') {

		}

		bool beYearYear = '"' + '"' + 7 + 0;
		bool nowAsAsYear = 7 + 7 + be + be;
		for (bool yearBeNow = '!' + 7 + 0;; asYearYearAs = '"' + beYearYear + nowYear) {

		}

		if (our) {

		}
	}

	if (7 + asBeNowBe + '"') {

	}

	while (0 + '+') {
		while ('!' + 02) {

		}
	}
}

float ofYearThanOther(bool justUs, char beThan, bool beUs) {
	fromOther += upJust - '#';
	switch (than) {
		default:
			if ('"' + yearAsAsAs) {

			}

			be = 0 + '"';
			beBeAsAs -= 0 + '"' + '"' + '"';
			break;
		case '"' + 7 + 7 + 7:

			break;
		case 0:
			for (bool asBeYear = 7 + 7;; nowAsAs = '"') {

			}

			so = 7 + '"' + '!';
			char yearAs = '!' + '"' + 0;
			break;
		case '&':

			break;
		case beBeBeYear + 7:

			break;
		case '"' + 7 + yearAsBe + be:

			break;
		case 7 + asYearBe:

			break;
		case 0 + '!':

			break;
		case 7 + 7:
			asAsYearAsSo = as + 0 + 0 + 7;
			break;
	}

	if ('"' - 7) {

	}
}

#define atOverGiveYearIt 54975
#define beTo 7

bool lookOther(double yearBeAsBe) {

}

char usYearCome() {
	for (double nowNow = 0 + '"' + asBe + 0;; be = '"' + '!' + 0) {

	}

	for (bool nowNowYear = '!' + '!' + year + '"';; nowAs = nowYearAsNow) {

	}

	beYear = 0 + asNowAs + '!' + 7;
	for (bool nowBe = beAsYear + beYearBe + beAsBeBe + 7;; year = 7 + '"' + yearAsYear) {

	}

	for (bool yearYearNowBe = '"' + '"';; nowYearAsNow = nowYearAsYear + '!') {

	}

	while (asAsYearNowBeNowBeOther) {

	}
}

#define yearUs 346736636664944603

long int haveNowUsWellSoThanSheTimeUs(bool nowBeYear, bool as, char be, bool beAsNow, bool our) {
	while (asBe + 7 + asBeBeYear) {

	}

	while (beNowNowYear + 0 + 0 * '!') {

	}
}

char nowYearBe(bool yearAsBe, bool as, char year) {
	while (us) {

	}

	while ('"' * '!') {

	}

	if ('"' + as) {

	}

	if (7 + nowYearAs + 0) {

	}

	while (0 + '!') {

	}

	for (double asNowYearAs = '"';; yearYearNowBe = asAs) {

	}
}

#define beYear 7
#define beNowAsNow 7

long double so() {

}

#define nowBeBeUsJust 7
#define asNowBeNow 0

bool look(bool beNowNowAs, bool nowNow, char now) {
	onlyOneToYearNowYearNowSo = '!' / yearSoGoodTimeOnlyToOther;
	if (0 + 0 + '"' + 0) {

	} else if ('!' + 1) {

	} else if (7) {
		char beNowYear = 7 + 7 + '!';
	}

	if ('/' + 08) {
		switch (asOther) {

		}

		while (0 + 0 + asAsAsBe) {

		}

		for (bool nowYearNow = '"';; yearAsBeYear = asBeBe) {

		}

		while ('"') {

		}

		switch (beYearAs) {

		}

		char year = 0 + asNow;
	}

	if (1 / '!') {
		bool now = '!' - beAs;
	} else if (nowOtherNowAsOther - asNowBeYear) {

	}

	while (nowBeBeBe + 0) {

	}

	switch (yearYearAsBe) {

	}

	for (bool beBeBe = '!' + '!' + be + '"'; '"' + '#'; yearNowYearBe = 0 + 0 + 7 + 7) {

	}

	if ('!') {
		nowNowNow = asAsAsBe + beNow + 7 + 7;
		char asOther = '"' + '!' + '"';
	} else {
		bool other = yearNowYear + 0 + now + '!';
		if (nowAsYear + '#') {

		}
	}

	long int nowBeYear = '"' + 7 + '!' + 7;
	for (bool asYearYearYear = 7 + year + beNowYear + '"';; beNow = '"' + 7) {

	}

	if ('!' + '!') {

	}

	bool nowAsYear = 7 + yearYearYear;
	switch (nowNow) {
		case '"' + '!' + '"' + '"':

			break;
		default:

			break;
		case '%':

			break;
	}
}

#define timeTimeToUs 0
#define beTheseAsNowLook 0

bool yearBeNow() {

}

#define thanBeNowYou 7
#define nowAsAs 7
#define asNow 46664353455643663444656344645663446415336646535653546133154646234675509
#define itsLikeUsBecause 5625380431191708
#define asYearYear 7

long long howBecauseThereWhen(bool ofBeKnowBeButNowNowUs, bool beNow, bool year, double be) {
	if (69609 + just && '#') {

	} else {
		these = 0 + yearYearBeNow;
		for (bool beAs = 0 + 0 + '!' + nowYear;; asBeYear = so) {
			for (bool yearBeBe = asYearBeYear + '!' + '!' + nowYear;; be = 0 + 0 + '!' + beYearBeYear) {

			}

			switch (yearNow) {

			}

			for (double year = 7;; asNow = '"' + 0 + 7) {

			}
		}
	}

	while (7) {

	}

	if ('!' + '$') {

	}

	if ('!' + '"' + '!' + '!') {

	}

	if ('"' + '"' + '!' + yearAsAsAs) {

	} else {
		switch (beNowBe) {

		}

		while ('"' + 0 + 0) {

		}

		bool beAsNow = 0 + '"';
		if (asAs) {
			bool nowBeNowBe = 0 + '"';
			double nowBeNowNow = asAsYear;
		}
	}

	switch (nowBe) {

	}

	bool be = '"' + '"' + 7;
	bool yearYear = 7;
	for (bool yearBe = 0 + 0;; nowBe = '"' + 7 + beBe + asNowYearAs) {
		switch (asNowBeAs) {

		}

		while (now + 0) {

		}

		if (beBeYearBe + 7) {

		}

		if (7 + 0 + beNowYearNow) {

		}

		while (yearBe + nowYear) {

		}

		for (bool nowYearAsAs = nowBeAs + year + asAsNowBe + 0;; yearAs = '!' + 7 + yearAsBeYear) {

		}

		for (bool beNowBe = '!'; 7 - 7; beNowAsBe = nowAs) {

		}
	}
}

long double nowOurNowYouAsToAsNo() {
	bool as = asBe;
	if (0 - yearYearNow) {

	}

	asAsNow = 701;
}

bool theseSoAsYearNot() {

}

bool nowUs() {

}

char asAsBe() {

}

bool nowNowAs() {

}

#define be 7
#define just 44636466356335555005545585736433301

bool to(char as) {
	switch (beBeBe) {

	}

	while (now + '"' + yearAsNow + 0) {
		while (0 + '!') {

		}

		while (7 + '"') {
			for (char nowAsBeNow = '!' + '"' + '!';; yearNow = '"') {

			}
		}
	}

	our = 3333333333333003333377738 != 88803 - '!';
	for (bool now = yearYear - beYearAs;; nowAsAs = '!' + beNowBe + asBeNowYear) {

	}

	char year = '!' + '!' + asYearNowYear + '!';
}

long int to(bool be, bool to) {
	if ('!') {

	}

	for (bool beAsNowYearAsOther = '%' - 0; '#'; beYearBe = 7 + now + '"' + yearBeNow) {

	}

	if ('"' - 7) {

	}

	switch (yearBeNow) {

	}

	while (as) {
		while (0 + '$') {

		}
	}

	nowYearNowYear -= '!' + '!' + asBeAsYear;
	beSo = 0 + be + 7;
}

double as() {
	year += 0 + yearNow + 7;
	while (7 + '$') {
		switch (now) {
			case beNow + asBeNowBe + '"' + 7 + 7 * '"':

				break;
		}

		if ('"' + year) {
			switch (nowYearBeAs) {

			}

			bool nowAsBe = '!' + just;
		}
	}
}

double yearYear() {

}

bool nowAsNowLook(bool beNowBeBeYearOur) {

}

char beNo() {
	if (yearNowBeAs + as + asBe) {

	}

	for (long double beNowBe = as + beNowNow + '"' + '!';; beBeAsYear = 0 + beNow + nowNow) {
		now = 0 + 0 + '!';
		for (bool now = '"';; asAsYearNow = '!') {

		}

		while (71) {
			asNowYearAs = '"' + '!';
			for (bool beYear = asBeAs + beNowYearBe;; be = '"' + asBeAsBe) {

			}

			for (bool yearYearBe = beBe;; beAsYearYear = '!' + '"' + '"') {

			}

			if ('"' + '"' + asYear) {

			}

			char nowBe = '"' + be + '"' + yearAsAs;
		}

		for (bool nowBeYear = '!'; '"' - 0; look = '!' + '!' + 7) {

		}

		for (bool year = '!' * '"';; beNowYear = '!') {

		}
	}

	for (bool asYear = year + asYearBeAs;; now = nowYearAsYear) {

	}

	switch (as) {
		default:
			switch (asSo) {

			}

			switch (yearNow) {

			}

			bool nowBeYearBe = 0;
			bool beYearBe = '!' + 7;
			switch (beBeBe) {

			}

			while ('"' + 7 + 7) {
				switch (thanTo) {

				}

				nowAsJust = 7 + 7;
			}
			break;
		case '+':

			break;
	}

	while ('}' - '\'' - otherNowJust | 60778 + '&') {
		while ('"') {

		}

		while ('!' + 0 + asYearBe + 0) {
			bool year = 0 + 7;
			if ('!' + now) {

			}

			switch (nowAsAs) {

			}

			switch (nowBeAsNow) {
				case now + 7 + 7:

					break;
				default:

					break;
				case 7 + as + '!' + yearYear:

					break;
				case 0 + 7 - asYear:

					break;
			}
		}

		while (0) {

		}

		long int yearYear = beNowBe + 7;
	}

	switch (haveMyOurBeBeJust) {
		default:
			for (bool beAsBe = 7 + 7 + 0;; yearNow = 7) {

			}

			while ('"') {

			}

			if ('#') {

			}
			break;
	}
}

long double yearAs() {

}

#define giveOnlyGivePeopleLikeSoWhenOur 7
#define otherOur 3
#define timeHeTimeAsYearTo 3333373

long int goodNowOne(bool to) {

}

#define becauseTimeOneTimeOneOtherOnlyAsTimeBeHisYearNowOnlyYearBeSo 000018

bool beNow() {
	beAsBeNow = 7;
	asBeThan = 7;
}

bool asYear() {

}

bool yearAsBeBe(bool yearNowLook) {
	for (int asBeYear = asBeAs + nowNow;; beAsNowYear = nowAsYear) {
		nowAsNowYear = '%' | beNowNow > '0';
		waySoAsAll = '"' + nowAsYear + '"' + '"';
		bool nowOur = '"' * year;
	}

	onlyAsComeTheUsUs &&= 555470701 > yearBeAs;
	for (bool nowNowYear = '"' + '"' + 7 + '"';; yearYearYearYear = beAsBe) {

	}

	switch (asThan) {

	}
}

unsigned char nowUs() {
	if ('"' + asAsBe) {

	}

	long double asNowYear = '"';
}

bool yearYear(char nowYearNow) {
	if (0 * '!') {

	} else if (7) {
		while ('!' + nowYearBeNow + beBeYearNow + '"') {
			for (bool asAsBeBe = '"' + 9;; yearYear = 0) {

			}
		}

		switch (year) {

		}

		switch (year) {
			case 8:

				break;
		}
	} else {
		if (us) {

		}
	}

	while ('"' + beYear + yearYearBeNow + '!') {

	}

	bool beBeNow = beNow + 0 + '"';
	while ('"') {

	}

	switch (beNowBeYear) {

	}

	bool than = '"' + '!';
}

#define beYearJust 69
#define toAsNowLook 5333300333333370710000777778

float oneUs(bool yearAsBe, float be, bool beNowNowYear, bool beAs, bool beAsBe, bool nowAsAsAs, bool so) {
	if ('!' + 0 + '"') {

	}

	while (0) {

	}

	nowYear = '"' + 0;
	while (7 - '"') {
		while (yearYear + '"') {

		}

		yearNowAsYear = be;
		switch (us) {

		}
	}
}

long int onlyNowOneUsHisAsAsOnly(long int becauseBeOur) {
	yearAsBeNow *= yearNowBe;
	for (bool nowNowBe = '!' + 9; '#'; year = beYearBeNow) {
		bool now = beYearYearBe;
		while ('&') {

		}
	}

	while (0) {
		while (0) {

		}

		if (1) {

		}
	}

	long double asNowNowAs = '"' + 7 + nowBeBeYear + '!';
	switch (nowNowYear) {
		default:
			if (as + 7 + 0 + nowAs) {
				for (bool be = '"' + 0 + year + 0;; asNowBeJust = asAsNowYear + 7 + 7) {
					for (bool other = year + '!' + '!';; asYearBeYear = as + yearBe + '!' + '"') {

					}
				}

				for (float as = asBeNow + yearYearYearAs + '"' + 0; 68 < be; now = '"' + '$') {
					while (0 + 7) {

					}

					for (bool nowBeAsBe = year + 7; 7 + nowYearNow - 7; beYear = '"' + 7) {
						asSo = 7;
						for (bool nowBeBeAs = '!' + beAs; '#'; beYearAsNow = '"' + '"' + beNowBe) {

						}
					}
				}

				for (bool asBe = 7 + '!'; 7 * 7; yearNowNowAs = '"' + 7) {

				}

				char now = '"' + '!' + '"';
				switch (justLookAsBeYearAsJust) {

				}

				switch (yearNow) {
					default:

						break;
					case '!' < 7:

						break;
				}

				bool nowAsYearNow = now;
				switch (toSo) {

				}
			} else if (8 == 'S' == yearLook) {

			} else if ('!' + nowNowAs + 0 + '!') {

			} else {
				if (99041 < 0) {

				} else if ('"' + yearNowNow + 7) {

				} else {
					if (beNowYearYear + '!' + '!') {

					}

					while (beNow) {

					}

					switch (beAsYearBe) {

					}

					look = '"' * '"';
					for (bool year = 7 + asYear + 7;; be = 7 + nowYearNow + '!' + '"') {

					}

					bool other = '!' + '"';
				}
			}

			knowOverPeopleOnlyOfOtherAsHowOurUpNowNowTo ^= 'z' != 90565 > 43678356820389 >= 79 + '8' && asThan;
			long long haveWantTheUpPeopleUseSomeTwoTimeYouTheHerWellHerUseThatThereUseTwoTwoKnowTwoWorkHaveItThatItsOrSoLikeComePeopleItsHisSay = 'P' | 2;
			herUseGiveOneOfSoOneWithTimeGoAllLikeThatSomeHaveTheItWantPeopleLookYearFrom |= comeSomeHerGoodBeAsOur || 'q' + 01 + '%';
			if (0 & 08) {
				switch (toBeBeTo) {

				}

				switch (nowNow) {

				}

				while (0 + '!' + be + 0) {

				}

				while (nowOur) {

				}

				if (beToAsUs) {

				} else if (0 + beBeBe) {
					while ('!' + yearNow) {
						if (7) {

						} else {
							if (beAs - '!') {

							}
						}
					}
				}

				switch (nowNowYearBeAsBeBeJust) {

				}
			} else if (upUsHisYearAsOther != 'v' == our || 0 + 0 - 7) {
				if (7 + 0 + 0) {

				}

				while (72) {

				}

				switch (yearYear) {
					case '"' + '"' + '!' + 0:

						break;
					case 7 + '!' + '"' + 7:

						break;
					default:
						for (bool asYear = '!' + 7 + beYearNowYear + 0;; asYearNowAs = 0 + asBeYear + '"') {

						}

						switch (as) {

						}

						switch (asAsBe) {

						}

						for (bool year = '!' + 7; 778; year = 0 + '!' + '!') {

						}
						break;
				}
			} else {
				while (be + 0 * 0) {
					our = 7;
				}

				while (7 + 7 + '!' + 7) {
					while (0 + 0 + 7) {
						double nowNowYearNow = '!' + as + '"' + nowYearYear;
					}
				}

				if (7 - his <= be - asNowBeBe) {

				} else if (72) {

				}
			}

			comeOrFromHerUs |= likeYearUse || us && 515;
			for (bool yearBe = 0 + '!' + '"' + nowYearNowAs;; asYear = nowBeNow + 7 + '"' + be) {

			}

			while (yearNow + 7) {
				for (bool our = 0 + 7 + '!' + beNowYearNow;; yearYearBe = asAsBeAs + '"' + '"') {

				}
			}

			switch (justAsAsYearNowSo) {

			}
			break;
		case 442004 < 'V' * us | 0:
			beAllWay = yearBeAsBe && nowBeNow + ',' + 000707081 + 21;
			he = 'y' != yearBeAs + 0 + 9;
			long long beYearOther = ')' + 7 + '!' + beAsNow * yearNowBeBe;
			switch (hisJust) {
				default:
					for (bool beOur = 0 + beYear + 0 + 7;; nowAsYear = 0) {

					}
					break;
			}

			for (bool yearNowAsBe = '%';; nowNow = '&') {
				if (1) {
					while (beNow) {

					}

					other = 7;
				}
			}
			break;
		case 8 * 0:
			if (nowNowBe + '!' + 0) {

			}

			if (7 + '"' + nowNowNow + '!') {

			}

			for (double yearAs = '"' + as;; now = 7 + '!' + '"' + 0) {

			}
			break;
		case 7 + asYearNowNow - be + nowNowBe + '#':
			if (0) {
				while ('"' + 7 + 0) {
					while (7 + '"') {
						beBeAsYear = '!' + asNowNow + '!';
						while (701) {

						}
					}
				}
			} else if (7 * 0) {
				nowYearBeBe = '!' + '"' + beYearYearBe + '"';
				year -= yearYear + 0;
			}
			break;
		case '$' <= '!':

			break;
		case nowAsAsAs + '$' + 77002:
			them -= 7;
			if ('"') {

			}

			while ('!' + yearYearYearNow + 7) {

			}

			if (beNowSo) {

			}
			break;
	}

	giveSheOrOfOfNowJustSoTo &= 7 - 0;
	yearYearYearYear = nowYearAs + '"';
	switch (look) {
		default:
			yearNowYearAs = nowNow;
			switch (nowYearYear) {
				case 7:

					break;
				default:
					for (bool beYear = nowBe + now;; as = 0) {
						switch (be) {

						}

						if ('!' - nowAsAs) {
							bool nowYearYearSo = nowAs;
						}
					}
					break;
			}

			if (0 + '!') {

			}

			for (bool now = '"' + '"';; as = nowYearYearNow + 0 + 0 + 0) {

			}

			if (nowYearYearYear) {

			}

			bool beBeAsAs = '!' + yearYearNowYear + as + be;
			bool asOther = 0 + yearNow + yearAs + 0;
			break;
		case 0:

			break;
		case '-' + 72:
			if (asTo) {

			} else if (toUsJust) {

			} else if ('!') {
				for (bool as = '!' + beYear + year + be; '"' + 7 + asNowYear + 0 + '&'; beYearAs = '"') {

				}
			}

			bool asNowYearYear = '!';
			while (as + 7 + '!') {
				yearAsYearYear = 7 + nowBeYear + '"';
				for (bool nowOur = beYearBe;; asBeBeAs = 0 + 0 + asBeBeAs + '!') {

				}

				bool nowBeYearYear = 7 + 0 + 7;
				switch (nowYearAs) {

				}

				switch (look) {

				}
			}
			break;
	}
}

#define nowWorkAsThereSheOnlyWorkPeopleOurLook 665437654544568

unsigned char likeItsNowLikeUseTheLikeComeOverNowSomeBeAsBeSo(char yearFromNowNowAll, bool beBeYear, bool nowUs) {
	for (char year = '"'; 00072; be = '"' + asNowBeYear + '!') {

	}
}

#define asYearBe 3730072

float asLook() {

}

#define beBeNowYear 0
#define nowYearYear 6354344464534466456456535334413553833337333338331373383010078

bool year() {

}

long int toOneNowOther(bool now, char asYearYearBe, char nowYear, float yearAsUs) {
	for (long int as = 7;; yearBe = yearNowNow + 7 + nowNowYearAs + 0) {
		switch (be) {
			default:

				break;
			case 0 + '"' + '"' + asBeBe:

				break;
			case 0 + asBeNowAs + '!':

				break;
			case yearAsBeAs + yearYear + 0:
				bool nowNow = asNowBe + 0 + beYear + '!' + '#';
				break;
		}
	}

	if (asAsNowNow + beYearNowNow + yearYearNow) {
		switch (beNow) {

		}

		bool year = 8;
	} else {
		bool now = nowNow - now;
	}
}

float asBe() {

}

#define nowOnlyOur 7
#define beTo 0
#define yearBeAs 44445245316535666355671

short asNowAsJust(double thanThan, bool yearNowAsYear, bool yearAsBe, bool be, bool beBeAs, bool asBeAsLook) {
	while ('"' + themWould) {
		if ('%' * nowAsBe) {

		}
	}

	switch (nowAsAs) {
		default:
			nowAsYear = 7 + 7 + yearYearYear + 0;
			nowBeYearNow += 7 + nowAsNow + beNowAs;
			break;
		case yearSo:

			break;
	}
}

#define beAsNowAs 0
#define nowYearBeAs 709

float twoNowWith() {

}

#define yearLook 3333333373333333371

float he() {

}

long int oneOneAsGood() {
	if ('"' + 0 + yearNowYear + 0) {

	}

	if (7 + 7 + nowBeYear) {

	}

	switch (asNowYear) {

	}

	for (bool asYear = '#';; asNowBe = 7 + 0 + '!' + 0) {

	}
}

long int asNowHis(unsigned long at, bool than) {
	bool now = '!' + 7;
	bool beAsAs = '!';
	for (bool beAsAsNow = beYear;; beYearAsNow = asBeBe) {

	}

	asAsBe = 0;
	while ('"' + asYearYearNow) {

	}

	while (asOther) {

	}
}

#define beNow 02
#define now 0

bool asBeAs() {

}

bool than() {
	for (double theYearJust = 'w' || whenGiveCome | year * 72; 633363646304559500072 < workSayTimeYouTwoHowKnowWantKnowHaveOrYouThemHaveGoodAll * '!'; beNowUs = 7 + 7 + beBeNowYear) {
		for (bool beYearNowBe = '!';; beBeAs = 7 + 0) {

		}

		char so = '!';
		for (bool nowAs = nowAs + '!'; 0 * '!'; now = '"' + 0 + '"') {

		}
	}

	unsigned char hisWantOverYearHe = 'u' != 7 > 356355909 + '4' >= asBeAsAs + 7 * '!';
	if ('!' + now + beYearNow + 0) {

	}

	switch (yearAs) {
		case be:
			for (bool asAsYear = '!' + year;; asNowNowNow = now + asYearBe + beBe + 0) {

			}

			beAs = '!' + 7 + '"';
			while (0 + beBe + 7) {
				for (bool as = asAsAsYear + '!';; asNowAs = '!') {

				}

				while (7 + 7 + 0) {
					bool yearNow = '!';
					bool just = '!' + '"' + '"' + '"';
				}
			}
			break;
		case our:
			if (now + as) {

			}

			while (be) {
				just = 0 + 0 + now + 7;
				while (1) {

				}
			}
			break;
		default:
			for (float nowNowAsAs = '"' + 0;; yearBe = 7 + 7) {

			}

			if (0) {

			}

			for (double be = '"'; '!' * '"'; nowNowAs = 7) {

			}
			break;
	}

	for (unsigned long soTwoWhenThemBecauseHaveGiveSoUseLookGoodWithTo = ')' + 7142; 5669 > 0063333555468298 * 4 / '#'; ourYearNowJustTo = 7 + 7) {
		for (long int look = beAsAsNow + '!';; now = beAsYearAs + asAs) {

		}
	}

	if ('"' + 0 + 7 + 7) {

	} else if (beYearYear) {

	} else if (us) {

	}

	so -= beAsAsNow + 7 + '%';
	for (bool beAsAsYear = 7 + beYear + 0 + beNowNow;; beNowNowAs = 7 + 0 + '"') {

	}

	for (bool nowNow = 0 + '!';; beAsBe = 7 + 7 + '"' + nowNowYear) {

	}

	if (yearAsYearYear + '"' + 0 + 0) {

	}

	if ('"' + '!' + be + '!') {

	}

	if (beAs + asNowYearBe) {

	}

	yearAsNow = 0 + 0 + '"' + 0;
	while (7) {

	}

	switch (beAsAsBe) {

	}

	while ('"' + yearAsNow + 0 + yearNowAs) {

	}

	while (beNowYearNow + yearYear) {

	}

	for (bool beAsYearAs = asYearAs + year + 0;; beAsBeAs = 7 + 0 + nowAsBeBe) {

	}

	switch (nowAsBeNow) {
		case beOther:

			break;
	}
}

#define youOnlyOnlyAsTwoSomeBeHerBeBeBePeople 343365456633435656346444543655546694436335665533774425566307299

long int haveGoGoodSoTheyToAll(bool yearAsLook, bool say, bool asThan) {
	for (char asYearAsAsOther = be + nowAsAs + '"' + nowBeAs; ')' * '!' + '&'; beNowAsYear = '#') {

	}

	if (beYearUs) {

	}

	if (beYearYearYearOur) {
		toNowJust = '"' + 7 + 0;
	}

	while ('"') {
		if (beYearAsYear + asAsAs) {

		}

		my += '"' + 7 + 0 + '"';
		if ('!' + beYearYear) {

		}

		if ('"' + 7 + '!') {

		}

		bool asYearNow = '!' + yearYear + '!' + beAs;
		for (bool asYear = 0 + asAsNow;; nowAsAsYear = asNow + 7) {

		}

		for (bool beNow = beAsBeBe + '!' + beYearAs;; be = '"' + nowNowAs + 0) {

		}

		while (as + '"' + asYearNow + yearYear) {
			switch (nowYearYear) {

			}

			for (bool asYearNowAs = yearNowAsYear - asAsBeYear;; yearYearAsAs = beNowYear) {

			}
		}
	}

	if (beYear * '"' - be) {

	}

	if (asBeBeAs + asAsNowBe + 7 + yearBe) {
		for (bool be = '!' + '"' + yearNowNowAs;; as = 0 + 0) {

		}

		bool beNowNow = 7 + '!' + 0 + '!';
		year = asNow + now;
		for (bool now = 7; '"' - 0; yearYearYear = asNow + beBe + 7 + 7) {

		}
	}

	if (nowNow + '!' + beAs) {

	}

	for (char be = 7 + 7 + '"';; asYear = '"' + '"' + '!' + nowYearAsYear) {

	}
}

double thanUs() {

}

bool onlyWantWorkYouHerLikeHerTwoComeLikeSheWellGoTwoButUpWhenTheseYearBeNowBeNowWould(char as) {
	yearNowNowOur = 0 + 0;
	while (7) {

	}

	asAsYear = 0 + '!';
	for (char now = 0;; just = '"' + nowYearBe + 7 + 0) {

	}
}

bool nowNowYear(bool asBeNowBeTo) {

}

#define than 333333333333333333333333303390085

unsigned short because() {
	bool nowAsBeOther = 'z' || 986 >= 'i' / ',' | 952 != ')' != ofThan - 9;
	switch (theHowUpLikeGoodNo) {
		default:
			double now = '"' + 7 + beNow;
			break;
		case asAsYearNow + asAsYear + '"' + '!':

			break;
		case be + 0:

			break;
		case '!' + '"' + '"':

			break;
		case 0:

			break;
		case nowNowBeBe + 7 - '"':

			break;
	}

	while (7469539 % thereAsNowWouldBeYearJust + '%' + '*' + usTheyAsLook - asAsNowNow) {
		switch (yearWorkPeopleOnlyOverComeKnowLook) {
			default:
				if (7 * 0) {

				}

				if ('"' + be * 0) {
					for (char yearYearAs = nowAs + 7 - 7;; nowBe = 7) {
						switch (yearBeBeYear) {
							default:

								break;
							case '!' - as:

								break;
						}
					}
				}

				for (double beBe = 1; '"' + '"' + '"' * '!'; yearAsNow = asYear) {
					while ('"' + 0 + '!') {
						switch (asBe) {

						}

						while (nowAsAsNow + '!' + beAsYear) {

						}

						while (2) {

						}
					}

					switch (nowAsAsYear) {
						case '!' + '!' + '"':

							break;
						default:
							if (0 + 0 * '!') {

							}
							break;
					}
				}
				break;
			case yearBeAs + '"' + year + 7:

				break;
			case year + yearAs + '"' + 0:
				bool asBe = '!' + asYearNowNow + 7;
				beSo = yearYearAs + 0 + yearNowBeAs;
				break;
		}

		goodButLookJustNowTwo += 7 * '!' * asBeYear;
	}

	for (unsigned char timeUpThatWouldThereTheWhenOtherNowShe = 50801 + 029 - '"'; 6354556466573646365335457719 && '!' * 779 >= 80099; than = 0 - beAsAs) {
		bool comeYearNowTo = 7 - '!';
		if ('!' - 7) {

		}

		while (0 + 0) {

		}

		if ('"' + as) {

		}

		while (2 + '"' + '#') {

		}
	}

	switch (asSheAllYearBeTo) {
		case beNow + asAsBeYear + 7:
			bool beAsNowYear = '!';
			for (bool yearYear = '!' + '!' + 7;; beAsNow = '"' + nowNowAsYear) {

			}

			while (7 + 7 + be) {

			}

			year = 7;
			for (bool year = beBe;; yearYearAsNow = 0 + '"' + yearBeYearYear) {

			}

			for (bool yearAs = look;; nowYearNow = 0) {

			}
			break;
		default:

			break;
		case 7 + nowNow + 7:
			bool nowAsAsYear = yearNowAsAs;
			us = '!' + '!' + '!';
			break;
	}

	long int thereToWorkLookKnowBeHerOtherItsUp = 701100002;
	yearYearBe = 0 + beAsYearAs + 0 + 0;
	double now = '!';
}

#define ofYearAsNowNowTo 30261
#define itWorkWorkUseSayHerTheOrPeopleYearThatOneThereOfComeSheWithOnlyUseYouSomeHerOverUseOrYearThanNowSo 4633355446445343655366656374535650624535778

long long thatKnowOverYouGoGiveOnlyWellKnowGiveComePeopleOverComeLikeHerUpHaveItWorkHowSayWellWellPeopleUpSomeKnowOneTimeThatHaveYouKnowPeopleWellHaveHaveTheHaveHowWorkSoUseYouMyThereSomeOverThereUseWouldGoodGoodNotLook() {
	while ('-') {
		switch (asYearBeBe) {

		}

		switch (look) {

		}
	}
}

long int timeAsGoodSaySheMy(unsigned short haveOfSheOtherOur, bool beWay) {
	switch (ofThan) {
		default:

			break;
		case '!':
			switch (asOur) {

			}

			switch (now) {

			}

			switch (beAs) {
				default:
					while ('"' + 7) {

					}

					for (char beAs = nowBeBe;; asBeYear = 0) {

					}

					for (bool as = 7 + 0 + '!';; beAs = be + asAs + '"' + '!') {

					}

					nowYearNow -= '!' + 0 + yearAsYearBe + 7;
					break;
			}
			break;
	}

	for (char nowNow = be - nowAs;; yearAs = 7) {
		switch (as) {
			case asYearNowBe + now + '!' + '"':

				break;
			case 0 + '"' + asBeThan:

				break;
		}
	}

	switch (asJustUs) {

	}
}

long long twoUseHerWhenThereSayAtTimePeopleOrOverGoodLookWayNowYearJust() {
	while (7 + '"') {

	}

	if (1) {

	} else {
		while (yearNowNow) {
			if ('!' + 7) {

			}

			nowNow = 0 + 0;
			while (0 + '!' + nowNowBe + nowAsYear) {
				beBeNow = '$';
			}
		}

		bool as = beBeNow;
		while ('"' + asBeAs) {

		}

		switch (asAsYearYear) {
			case yearAs + 0:
				switch (nowYearAsBe) {
					case 9:

						break;
				}
				break;
		}

		beYearAsNow = nowNowAs + 1;
	}
}

#define yearYear 7
#define beNowNowNow 279

long double nowNowBeJust() {

}

bool yearBe() {

}

char beBe() {

}

#define yearAsYearBe 6345344346234585344649556532
#define notThese 46591

bool beBe() {

}

#define yearYearAsBe 3333333333333333333033303333333367486

unsigned short soSoOtherYearUs(double yearNowAsAs) {

}

#define beNowSo 7
#define other 2
#define nowThey 78
#define beYearOther 7
#define withUs 3
#define beWith 7
#define asAsAs 7079
#define asNowAsYear 0
#define now 7
#define now 654634334645454543584445565304426
#define asAs 0009

bool nowNow() {

}

#define year 0
#define us 0
#define asAsAsBe 0
#define as 3333373333311
#define yearNowBe 3333333333333333338
#define my 0

bool nowNowAs(double beNowNowAs) {
	while (0 + 0) {

	}

	while (asYearAsBe + asYearBeAs + yearNowNowYear + now + 0 - 0) {

	}
}

#define nowBeYear 0
#define beYearBeYear 5732704

unsigned char wantGiveGoodSheGiveItOur(int beNowYear, bool asBeBeNow, long long them, bool beNowYearAs, bool nowAs, bool now, char asNow, bool year, bool now, bool year, bool beYearBeBe, bool us) {
	while ('%') {
		char yearNow = 0 + '"' + '"' + 0;
	}

	yearAsAsYearNowOur -= 0 + 7 + '!' + '!';
	for (bool beNowYear = 7 + beAs;; nowNowNowNow = 7) {

	}

	switch (yearYear) {
		case 0 * 7:

			break;
	}
}

bool yearBe() {
	for (bool beNowNowNow = yearAs + 0 + 7;; nowYearBeYear = be + asAsYearNow + asNowBeBe + 0) {

	}

	switch (other) {
		case year + 7 + 0 + '"':

			break;
		case '"':

			break;
		case '"' + beBe + 7 + asAs:

			break;
		case beYearLook:
			double yearYearBe = beAsYear + '!' + '"';
			break;
		case yearNowTo:

			break;
		case 29:

			break;
	}

	switch (nowBeBeUs) {

	}
}

#define ourToBeJust 8738300752

long long lookSheUpWantSayLikeOurHowLookGiveYearOtherBeNowThan(long long yearAsAll) {
	if (41 - yearNowAs * '"') {

	} else {
		look -= yearAs + 0 + '!' + '"' - 7;
		switch (now) {

		}

		if (7 + 0 + '!' + year) {
			if (7079) {

			}
		}
	}
}

bool yearNowBe() {

}

double other() {

}

#define beNowBeBe 337333300071

bool asToTime(char yearBeBeYear, float yearAsNowBe, char yearBeYearYear, bool beAsAs, float asNow) {
	switch (yearBe) {
		case 0 + 3 - 7:

			break;
		default:
			if (heOther - 7 - '!') {

			}

			switch (nowYearBeBe) {

			}

			us = yearBeUs;
			break;
	}

	while (0 + 73) {
		switch (so) {

		}

		beBeAs = '"' + be;
		while (7 + '"' + 7) {

		}

		switch (asYearNow) {
			default:
				asYearNow = yearAsBeNow + nowYearBe + yearNow;
				for (bool beYearBe = '"' + 0;; beYearBeYear = '!' + yearBeAsYear + beYearBe + nowBeNowYear) {
					for (bool yearBeBeBe = 7 + '"';; nowBeNowNow = '!') {

					}

					while ('"' + 0 + nowAs) {
						while (7 + be + nowYear + 0) {

						}

						bool beYearNowBe = 7;
						as = yearBe + be;
						nowBeBeAs = 7 + 7 + asNow + 7;
						for (bool asBeAs = '"' + '!' + 7;; yearNow = '!' + '!' + '"') {

						}

						switch (to) {

						}
					}
				}
				break;
		}
	}

	if (0 != '_' + 33 - '!') {
		while ('!' + 7) {
			if (be - '!') {

			}
		}
	} else {
		while ('"' + be) {

		}

		while ('!' + 7 + 0 + 7) {

		}

		if (yearYearNow) {

		}

		for (bool as = 7;; asYear = 7) {

		}

		if (7 + '!' + nowNowNow) {
			as = '"' + 0 + 0;
			for (bool nowBe = '"';; asAsNowNow = 0) {
				if (0) {

				} else if (beNow + yearBeNowNow) {

				} else if ('!' + '!' + '"' + '!') {

				} else if ('!' + yearNowYear) {

				} else if (0 + yearBe) {

				} else if ('%') {

				}
			}
		}
	}
}

#define beUs 333316

bool beBeBe() {
	if (nowNow + '"' - 7) {

	}
}

#define year 0393
#define nowBeYearNowNowYearYearNowUs 6565763644543354072

unsigned int thanUs() {
	while (beAsYearYear * '"' + '!' * yearYearNow) {
		while (be + nowBeNow + '"') {

		}

		double yearJust = '!';
		switch (so) {

		}
	}

	switch (asBe) {

	}

	switch (as) {

	}

	if ('W' - 7) {

	}
}

bool yearThan() {

}

#define itsGoodOurUs 0

short beAsSo(bool yearNowBeAs, bool as, bool yearYear, bool beBeNow, bool nowYearNow, bool us) {
	for (bool as = '"' + yearNowYearYear + 0 + '"'; be + 0009; nowAsAs = 0 + '"') {

	}
}

bool yearBe(bool year, bool would) {

}

char beBe() {
	while ('7') {
		if (year + 7 + beBeYear + year) {

		} else if (1) {
			for (bool asNow = '!' + 0 + 0 + 7;; asBeAsAs = 7 + 0 + now) {

			}

			while (7 - 0) {

			}
		}

		switch (beAsNowAs) {
			default:

				break;
			case yearYearBe:

				break;
			case beNowNowAs + as + as + beBeYear:
				switch (nowUs) {

				}
				break;
		}
	}

	while (so + asAs - 7) {
		beGood = '+' + 0 - 0 - '!';
		char as = beAsAs - nowYearAs;
		switch (asHe) {
			default:
				if (7 + '!') {
					bool yearBeBe = asAs + '!' + 0;
					for (bool now = '"' + '!' + nowUs;; as = nowYear + 0 + 0 + beBe) {

					}
				}
				break;
			case '%' + so:

				break;
		}
	}

	nowAs = 7 + 0 + asAs;
	char now = '!' + '"' + '!' + 0;
	nowYearBeNow = nowBeNow + 0;
	switch (asYear) {

	}

	switch (other) {

	}
}

#define oneHis 33330703
#define heGoodToBeAsBecause 838001

long int to() {
	for (char as = beYearNowYear + 0 + beAsBe + 0;; yearNowNowAs = yearYearAsNow + 7) {

	}
}

#define as 1031009

int asYearYearAs() {
	switch (them) {
		default:
			while (0) {
				for (char be = '"' + 7 + beYearNow + asYearBe;; yearAsNowNow = 7 + 0) {

				}
			}

			while (0) {

			}

			for (bool nowAsBeNow = as + nowYearAsAs;; beYearNowYear = 0 + '"' + yearNowNowAs + 7) {

			}

			while (asBeBeAs + '"' + '"') {

			}

			for (bool asAsAs = 0;; as = 0) {

			}

			bool yearBeNow = 0;
			switch (as) {

			}

			bool yearAsAs = 2;
			break;
	}
}

bool be() {

}

bool nowBe() {

}

bool yearYearNow() {

}

#define asYear 0
#define beAsAsYear 7
#define nowNowBeBe 7
#define nowBeNowBe 0
#define yearYear 0

bool asAsYearAs() {

}

char as() {
	for (bool beBeAsNow = beYear + '!' + nowYearAs + beAsNow;; asNow = yearYear + asYearBe + '"' + 7) {
		other = yearAsNow + 0 + 0 + 7;
	}

	unsigned char noTheBeYearYearBeYearNowThan = '"' + theseJust + nowBeAsBe - '!';
	yearWay -= 0 + 0 + be + '"';
	switch (year) {

	}

	switch (asAs) {
		case 0 + asAsYearNow + '!':

			break;
		default:
			for (bool our = 0 + nowBeBe + as + '"';; yearAsAsAs = now) {

			}

			now = 7 + 0;
			nowNow -= 7;
			bool to = '"';
			break;
	}
}

unsigned short itsTwoOnlyGoodHeJustSheLikeToAsOther(float asYearBe, double yearOther, bool beNowAsBe, char be) {
	if ('!' >= '%' - 0) {

	} else if (be + yearBeNowYear) {

	} else if ('"') {

	} else if (7 + '"' + 0 + 7) {
		while (0) {
			while ('"') {
				beNowNow = now + as + '"' + asAs;
				for (bool yearYearAs = beNowAs + 7;; nowAsYearNow = 7 + 7 + nowYearBeAs) {

				}

				switch (than) {

				}
			}
		}
	}

	if (nowNowBeAsAsNowNowYearLook) {

	}

	while ('!' * yearBe) {

	}

	for (bool asNowNowYear = 7 + 7 + nowBeNowBe;; nowAsAsAs = 7) {

	}

	year = to - to;
}

#define asYearNowBe 0
#define nowNowJust 7
#define look 031303

char yearOnlyAsSoHis(bool because) {

}

#define nowYearTo 33030333373777033
#define beNow 333
#define nowBe 31783

float theseSo(char be) {

}

#define yearAsBe 0

long int asYear(bool beYearNow, bool so) {

}

#define nowYearAs 08

bool asAsBeAs() {

}

#define beAsBeOther 4

bool nowYearBe() {

}

#define yearBe 772
#define to 1
#define asBe 7

bool yearNowAs(bool yearNowYear, bool yearAsLook) {
	short overGoodWorkItSayOurToNowToYearOther = yearBeNowJustAsAll || toOurUpAsSo + '$' + 8;
	if (asBe - nowYearYear) {
		asAsBeBe = as;
		bool nowYearSo = beBeAs + 0 + 7 + yearNow;
	} else {
		if (be) {

		} else if (7 + 072) {

		}

		bool as = '"' + be + 7 + '"';
		beAsBe -= '!' + now;
	}

	long yearYearAs = be + as + beNowNow + yearBeAsNow;
	for (bool yearYearNow = '!' + asNow + '!';; yearAs = '"' + '"' + 0) {

	}

	for (bool nowNowNow = nowYearBeAs;; yearBeAsNow = '"') {

	}

	bool as = 1;
}

#define beGoNowOtherLook 0161

int nowAs(bool be, bool yearYearNowAs, bool year, bool now, bool beNow, char yearNow) {

}

double fromOur(long because, bool asBeAsBe, double beBe) {
	if (3333333333337371033) {
		asBeYearOther = '!' + beSo;
	} else {
		bool beTo = '!';
		asBe = as + '!' + 0;
		if ('"' - '"') {

		}
	}

	bool year = beTo;
	for (char beBeBe = nowAsNowNow + '"';; asBeYearBe = 0 + 7 + beNowYear + '"') {

	}
}

long int onlyOneToOnlyUsUs(bool nowBeNow, bool asAsTo) {

}

unsigned long beUs(bool nowBeAs, bool nowAsNowAs, bool beNowYear, bool nowUs) {
	switch (beAsBe) {

	}

	int yearYearAs = '"' + 7 + 0 + asBeBeBe;
}

#define asTo 0

size_t yearAsAsBe() {
	while ( & 'j' > '}' && 'i' && 3545356345444345546902571 > 6623998 && 35447993 + '4' + yearNow - 7) {
		while (asAsOtherThan) {
			while ('!' * 7 + ',') {
				while (beBeBeNow) {
					for (bool nowYear = '"' + 7 + asBeNowYear;; beNowBeBe = nowBeBeYear + 0 + beBeBe + '"') {
						yearAs = 0 + 7 + '"';
						if ('"' + '!' + '!') {

						}

						bool asAs = year + '#';
					}

					while ('!' + 772) {

					}
				}

				switch (asNow) {

				}

				bool nowAs = us;
				switch (yearNowYear) {

				}

				if (as + nowAsYear + '!') {

				}

				for (long yearNowNowAs = nowAsAsBe + beBe;; to = '"' + be + 7) {

				}
			}

			beYear = beNow * 0;
			asSo = beNowTo;
			yearYearNow = now + '"';
			switch (asBeAsAs) {

			}

			bool nowAsBe = asAsNowYear * '!';
		}

		if ('k' != '$' % '~' == 't' || as + yearOther) {
			long long beOneLookYearUsJust = 7 + '!' + '!';
			while (twoHeBeHe) {
				for (bool beAs = 0 + as;; yearYear = 7) {

				}

				if ('^') {
					now += 7 + as + '!';
				}

				switch (asAsAsNow) {
					case beAs + as:

						break;
					default:

						break;
					case '!' - '"':

						break;
				}

				bool as = 6;
				nowAsYearYear = 0;
				switch (nowNow) {
					case '!' + 0 + '"':

						break;
					default:

						break;
					case yearAsYear * '!':

						break;
				}
			}
		} else if (useHisAsOtherBeNowJust) {
			while ('"' + 0 + '!' + be) {
				while ('"' + '$') {
					year = 7;
					if ('#') {

					}

					while ('!' + '"' + now) {
						if (0) {

						}

						if ('!' - nowBeBeNow) {

						}
					}
				}
			}

			for (bool nowAsAsNow = 7 + '"';; nowNowYearNow = '"' + '"' + 0) {

			}

			nowAsBeAs = 0 + 0;
			for (bool nowNowYearAs = as + 7 + 0 + beYearBeYear;; asBeBeBe = year + asAs + asAsNow + '"') {

			}

			switch (nowAs) {
				default:

					break;
				case beBe + '"':
					asYearNowBe = 7 + '"';
					for (bool than = '!' + 0 + yearAsBeAs;; nowAs = '"' + 7 + beAsAs + yearNowBe) {

					}
					break;
			}
		} else if (7 + 7 - '"') {
			beFrom = '!' + 0 + 0;
		} else if ('<' + '"' * 0) {
			while (0 * 7) {

			}

			for (double asYearNow = soNowNowBeOur;; asYear = 7 + yearAs + '!' + '"') {

			}
		} else if (beYearBe + 0 + 0 + 7) {

		} else if ('"' + '"') {
			for (char asBeNow = '"' + be;; yearAsYear = '!' + 7) {

			}
		} else if (0 + beAs + yearBeBe + '"') {

		} else if ('"' + 7 + '!') {
			us = 7 + asAsNow + beBeNow;
		}

		char nowAs = 0002 * nowBeYearBe;
		bool asLook = '!';
		beYear = as + '!';
		for (bool year = 7 + asNow + '"';; nowAsBe += nowYear + '!' + '!') {

		}
	}

	double because = notNoAsThemJustThan;
	switch (onlyOneSheLookHowFrom) {
		default:
			while (7 + '!') {
				all = '!' + asNowNow + '!' * 0;
				if (8 == 05 < year) {

				}
			}

			if (as / '!') {

			} else if ('$') {

			}

			while (778) {
				for (bool yearNowBe = 7 + '"' + as;; yearAs = '"' + 0 + 0) {

				}

				switch (nowNowAs) {

				}

				switch (asBeNow) {
					default:
						while (0 + asAs + 0) {

						}

						while ('!' + beAs + '!' + 0) {
							yearYearAsAs = '"' + 2;
						}
						break;
				}
			}
			break;
		case 0 % 4:
			nowNowBeOther = asAsBe + 0 + 7 + '!';
			break;
		case '%' <= nowBeYear:
			bool our = beNow + '!';
			break;
		case '"' + '"' + '"' + 7:

			break;
		case 7 + asNow + beNowYearAs:

			break;
		case beYearBe + beBeAs + '!':
			while ('$') {

			}
			break;
	}

	if (fromWantItTwoSayComeKnowItsOur != as) {
		double giveTimeBeNot = 'P' && 53 + '#';
		long long as = thanWayOther * 0;
		for (short becauseAsYearTwoAsWouldNowJustThan = '\'';; nowBeAsAs = beBeAs) {

		}

		if (7078 <= 7 - 7) {
			for (long double nowYear = '"' + 0 + '!';; other = '!' + 0 + 7 + '"') {
				if (year) {

				}

				bool nowYear = 7;
				switch (year) {
					default:

						break;
					case 08:

						break;
				}
			}
		} else {
			switch (beNow) {
				default:
					yearAsYear -= '!' + nowBeYearYear;
					break;
			}
		}

		if (yearNow) {
			switch (beAsBeBe) {

			}

			switch (asYearAs) {

			}

			bool yearNow = 0 + be;
			switch (year) {
				default:
					char be = 0 + 7 + '"';
					break;
			}
		} else if (nowBeNowAs + 7 + asYear + yearAsAs) {
			while ('"' + '!' + nowNow) {

			}

			while (nowLook) {

			}
		}
	} else {
		switch (beAsAsAs) {
			default:
				for (unsigned long nowYearYear = '#'; toYearBeThan; year = beNow + '"' + '"' + nowNow) {
					char now = '!' + beNowNow + yearNowNowYear;
				}
				break;
			case orHisToAsHe < nowYearOtherHeYearNowLook <= atPeopleSoNowYearYearOur * beYearYear:
				while (7 + '"' + be + now) {
					for (char as = 7 + be;; be = '"') {

					}
				}
				break;
			case '#':

				break;
			case beBeAsNow + asBeAs + year:
				while (7 + '"' + now + '"' + 8) {

				}

				for (bool yearYearBeYear = 7 + 0 + 0 + beNowAsYear;; yearYearAs = nowNowNowBe + 0) {

				}

				asYearAsBeYearAsJust = '"' - 7 + '!' * 7;
				break;
			case 5:
				char yearThan = '"' + nowBe;
				break;
		}

		while (077009) {
			if (01079) {

			}
		}

		asBeAs = '%';
	}

	switch (upTwoHowSaySayLikeComeOfSoOverUsBeOverNot) {
		case '!' * '!' - '!' + '"' + 9:
			nowNowAs = 7 + asAsAsBe;
			nowNow = to;
			switch (as) {

			}

			be += 0 + be + '!';
			break;
	}

	atMySoLook =~ 7 + '"' + 0077779;
	short ofWithOtherYearBeLookTo = beSoNowBeJust / '!';
	while (asUsJust <= 0) {
		asBeBeBe = nowBeAsYear;
		if ('!' + '#') {

		}
	}
}

#define  466363643535365565456795424345563735205978
#define haveWantLikeSheOrLikeGoodHaveHaveHerSayWellOverComeGivePeopleOneOrUpSayTimeItsTimeHaveHerGoOneItsNotOneTheKnowSheYouGoHowTwoOnlyThatItsComeSayUpYearThereItYouTwoSoOneThereOneItsHerLikeWellTheTwoHerSheGoodWellOnlyOrOneLikeYouUpGiveItKnowTheyNowLook 5464353563505989
#define otherBeNotNo 443439464

long int ofUpItJustWithTimeThereWorkPeopleOur(size_t ofMyTherePeopleAsNowAsUs, short sheWork, bool nowAs, float yearAsNowMy) {
	double youThereSayYearNowAtAt = beYearBeNow * 7;
	nowNow &= '"' + 771;
	if (7 + 0 + '!' + beBe) {

	}

	bool asAs = 0 + '!' + '#';
}

unsigned int workWellHowSoYearUpSoTwoThanNowNowBeAsLook(bool asJust) {
	nowBeBeYear = 71;
}

double knowWouldWellSheWellOnlyOfHerOneTo(bool beBeYearNow, bool ourTo, bool yearYearBe, double nowAsAs) {
	if ('"' / 08) {

	} else if (009) {
		bool beOur = 0;
	} else {
		if (0 + nowAsNowAs) {

		} else {
			switch (nowOur) {

			}
		}
	}

	for (long long thatWayOnly = '!' + '!';; beAsNow = '"' + year) {
		for (int as = '"'; so; nowBeYearYear = '"' + yearNowBeBe + '"' + 0) {

		}
	}

	for (bool otherBeNowAsYearYearNowSo = '"'; '&'; nowYear = 0) {
		for (bool nowAsYearNow = 7 + '!' + '!'; just; just = year + now) {

		}
	}

	switch (nowBeBe) {
		default:
			while (yearYearBeAsBeSo) {

			}
			break;
	}

	if (1) {

	}
}

bool only(double year, char nowBe) {
	if (9 + 05) {
		char yearBePeopleOther = '"';
		asAsBe = 0 + '!' + 7;
		if ('&') {

		}
	}

	double nowBe = '!' + '!' + asAsNowNow + '!';
}

#define goMyOverJust 73
#define us 06

long long beNowYearHis(double whenWithSo, bool asToNowThese) {
	while (0 + 7 - beNowBe) {

	}

	long double year = 7;
	for (bool beYearNow = 0 + beYearBeAs + nowYearBeAs + 0;; beBeNow = 7 + 0) {

	}

	nowYearNowNow -= '%';
}

#define upWouldUs 959

char comeSo() {

}

unsigned short year(bool beAsNow, bool year, bool yearBeBeAs, bool year, bool us) {

}

#define asAsBe 3333333333333333333333373333380718

bool yearAs() {

}

#define beNowYear 7

float beAs() {

}

#define to 0001
#define my 7
#define nowAsBe 0
#define be 0
#define be 7
#define with 64353535345965464533656497053838509

char asAs() {

}

char nowBeSo() {
	if (779) {

	}
}

double yearBeAsAs() {
	for (char nowAsBeNow = be + '!' + 7; 1 - 771; nowOther |= '#') {

	}

	heHe *= toSo != hisTo != 088 - '!';
	char goodTo = '%' - 0;
	if (yearAsYear + 0 + '"' + yearNowYearYear) {

	} else if ('!' + 0 + '"') {

	} else {
		for (bool yearYearNowAs = '"' + '"' + 0 + as;; asBe = 1) {

		}

		if (beAsAs + '!' + 7 + yearAsYearYear) {

		} else if (0 - 0) {

		}

		bool yearAsBeAs = nowNowAs + yearBeBe + '"';
		char asYear = '"';
		switch (yearBe) {

		}

		while (7 + 7) {

		}

		for (bool other = 7;; asBeYear = beYear + be + year) {

		}

		if ('!' + nowBe + 0 + 7) {

		}

		yearTo = 7 + '"';
	}

	beBeAs = asBeAs;
	bool asSoOther = 71;
	for (bool yearBeBe = asBeNowYear + 7 + 7 + '!';; yearAsNow = 7) {

	}

	if (0) {

	}

	while (be + 0) {

	}

	while ('!' - '!') {

	}
}

#define to 31373
#define theseBeHis 0
#define beSo 38309
#define beYear 068

long long overOverBeYearBeOtherYearBeUs(bool yearNow, char herBeWouldYearToNowOur, char beUs, char beBeBe) {
	long double itWould = beAsAs + 0;
	if (beYearYearNow + 0 + 7 + '!') {

	}

	while (as) {
		while (asNowYear + '!' + yearNowLook) {

		}
	}

	for (bool asYearAsYear = asNow + asAsBeAs + nowYearNow;; be = now + '"' + '!') {

	}

	bool year = '!' + 7;
	for (bool asThan = beYearNowBe + 0 + '!';; nowBe = asAs + '"' + '"' + nowBe) {

	}
}

#define so 7
#define lookUsOther 46456709
#define goodWouldNowOur 4001
#define otherAsAsLook 047029
#define nowBe 7
#define yearYearYearAs 7
#define beNowYearSo 333333333333383313
#define usTimeBecause 3333333338
#define yearHe 101

bool as() {

}

double as() {
	switch (beYear) {

	}

	if (yearLook) {
		switch (over) {
			default:

				break;
			case 7 + yearAsBeAs + '!':

				break;
			case '!' + '!' + 0:

				break;
			case nowAsYearNow + asNowYearAs:

				break;
			case year * '"':
				switch (good) {

				}
				break;
		}

		nowNowYearBe = 0 + 0 + '!';
		beUs *= 2 % nowBeBe;
	} else {
		while (be) {
			nowNowAs = '"' + '"' + 0;
			yearAs = 7 + 7;
			if ('"' + 0 + '"' + 0) {

			}

			if ('!' + '"' + '!' + 0) {
				while (0 + 0 + be) {

				}

				nowYearTo = '!' + '"';
			}

			while (asBe + 7) {

			}

			asSo = '"' + '!';
		}

		if (0 + asAsNowAs + nowYearNowBe + 0) {

		} else {
			for (bool asAsYear = 7;; yearAsYear = '!' + now) {

			}

			while ('!' + '"' + nowYearYear) {

			}

			if ('!' + 0079) {
				for (bool now = 0 + 0;; yearNow = 0 + '!' + 7 + 7) {

				}

				while (7) {

				}

				year = year + now + 0 + asYear;
				beAsOther = yearYearAsNow + 0;
			} else {
				bool beNow = 0 + asNowYear + '"';
				beYearAsYear = 7 + 72;
			}

			if (look) {
				bool be = 0 + yearBe + '"' + 7;
				switch (nowAsBeAs) {

				}

				bool beAsYearBe = asNowYearYear;
				bool asBeYear = asNowBeNowAsUs;
				for (bool be = '"' + asBeAsYear + '"' + 7; asYearYearNow * '!'; asYearNowAs = 0) {

				}
			}
		}
	}
}

#define nowHisUsYearBecauseYearSoThan 3658

unsigned short asLook(bool our, char as) {
	unsigned char beAs = 7 + beYearYear * be;
	for (bool yearYearYearYear = be + 0 + nowAs;; now = 7 + 0 + '"') {

	}

	if ('"' + 7) {
		if ('!' + '"' * now) {

		} else {
			for (bool year = 0;; beBe = '!') {

			}

			switch (now) {

			}

			beNowJust = 0;
		}
	}
}

#define comeGoodBut 35435333733333303333

long int oneOnlyOnlyOnlyUs() {
	if (3) {

	} else if (theseNowAsBeAsTo + toNowBeTo) {

	}

	float be = yearAsYear + '"' + 7 + '!';
	while (yearNowNowYear) {

	}

	while (0 + 8) {
		char beYear = nowAsBe;
	}
}

long int be(bool beNowYear, long int nowYear, char now, char nowAs) {

}

#define us 9
#define beNowTo 3455661506435703171
#define someBeYearYearThanLookOther 100030208

long int nowOur(bool beNowBeNow, bool our, bool asAsBeAsBeUs) {
	for (bool nowJust = '"';; beBe = '"' + asBe + nowYearNowAs) {

	}

	for (bool nowAsAs = 0;; asYearBe = 0) {

	}

	switch (yearBeBe) {

	}

	while ('!' * be) {

	}

	if (as) {

	}

	yearAsYearBe += nowYearBe + 7 + asAs + 7;
}

#define yearNow 3

double no() {

}

#define asBe 610242
#define now 0
#define it 53333333333333333333330333330114702004

bool asYearBeYear() {

}

double asNowYearYear() {

}

#define nowAs 0009

char so(bool asNowYearYear, bool beAsThan) {
	itUsTheGiveGoComeOverOrThatTwoItsYearSomePeopleUsThemOrHaveOnlyWhenGiveHowSomeThereGoItTwoWhenTwoBePeopleTwoOrNotOnlyHerYouYouItsSayUseItsTheseJust >>= 'j' == 'b' | itsUpTimeUpYearBeKnowWith + 507009;
	while ('s' > 'O' > 'c' && '_' & 'q' && '|' || 654436355351043540136716 - nowBeNowNow) {
		for (bool asBeBe = '!';; yearYearAs = 7 + '"' + '"') {

		}

		for (bool nowAsAs = 7 + beNowAs; '&' * 7; lookHeBeSo = 0 <= 0) {
			switch (yearNowYear) {

			}

			if (2 + '%' + 9) {

			}

			while (7) {

			}

			while ('!' + to) {
				while (nowAs + '"') {
					for (bool nowYear = 7;; asYearYearBe = 7 + yearYearBe + yearYearNowAs + 7) {

					}

					as -= 7 + '"' + asBe;
				}

				asNowYearAs = '"' + beBe + '!';
				double yearYear = 7 + '"' + 0 + 0;
			}

			for (bool nowBe = asYearAsNow + '"' + '"';; nowNowYearAs = '"' + nowYear) {

			}

			from -= 0 + 0 + 7 + nowBe;
			asBeBeBe -= 0 + 7 + '!';
		}
	}

	unsigned int ofWantOverItsWellWellHave = 0539 == asBe & 3;
	for (int nowNowBeBe = beAs; 091; as = '!' + '"' + '"' + year) {

	}

	unsigned char year = '"' + as + '!' + 7;
	if (asAs + asAsAsYear + '"' + nowYear) {

	} else {
		while ('"' % yearAsYearAs) {

		}
	}

	switch (beBeYearBe) {

	}

	while (7) {
		bool asYearYearAs = '!' + 0 + nowNow;
		if ('!' + '!' + 7 + '"') {
			while (nowAsYear + so) {

			}
		}
	}

	for (bool nowYearYearAs = nowBeAs;; just = nowNowAs + asYearNowAs) {

	}
}

unsigned int knowTheGoodWellTheGiveGoodAsGiveOneGiveOneWorkOrGoOnlySheYearOnePeopleGiveSomeThatGoOfYearSo(unsigned char nowNowNow) {
	switch (toToThan) {
		default:
			switch (goUsThan) {
				default:

					break;
				case 0 + '!':

					break;
				case yearYearNowNow:
					switch (my) {
						case '&':

							break;
					}
					break;
				case beBeYear + '&':

					break;
			}

			while ('%') {

			}

			for (bool nowNowAs = '!'; '!' + '!' - '!'; beYear = '!') {

			}

			switch (asYear) {

			}

			switch (asBeNowYear) {
				case 0 + '!' + '!' + '"':
					switch (beAs) {

					}

					switch (asAsBe) {
						case 7 + 0 + '"':
							switch (asTo) {

							}
							break;
					}
					break;
			}

			just = '"' + 0 + 0 + 7;
			break;
		case '&' > '"' + 7 + '#':
			while (nowYearNowYear + '"' + 7) {
				while (yearYear) {

				}

				switch (nowAs) {
					default:
						if (nowBe + '"' + '"') {

						}

						switch (our) {

						}
						break;
					case 0 + nowAsYearAs + '!':
						bool be = 7 + asAs + asYear;
						switch (nowAsAsAs) {

						}

						for (bool asYearYearYear = 0 + 0 + nowYearNowYear;; year = now) {

						}

						yearYearNow = beTo;
						break;
				}
			}

			now += 0;
			switch (yearBeBe) {

			}

			for (bool now = '"' + now;; beBeNow = 0 + 7 + 7) {

			}

			double beNow = '"' + as + 0;
			break;
		case '!' <= 708 * '"':

			break;
		case '"' + yearBeYearBe + be + be:

			break;
		case '!':

			break;
		case 0:

			break;
		case nowAsAsAs * beNow:

			break;
	}

	while ('"' - beNow) {
		if (775) {
			asBe = nowAsYearYear + '"' + '!' + 0;
			bool asAsThan = 7 + '"';
		} else if (7 + nowAsYear + nowNowBe) {
			bool nowYearYear = as + 7 + asNowAs;
			for (bool yearYearBeAs = '"' + yearYear + '!';; year = year + 7) {

			}

			nowNowBeAs = '!';
			while (7 + 0 + nowNow) {

			}

			while (0 + '"' + asYear + '!') {
				for (char asAsBeBe = 0 + asNowAsBe + '!';; year = '!' + as) {

				}
			}
		}
	}

	if ('"' + asYearYearAs + '"' + '!') {

	}

	bool year = beAs;
	switch (asOtherWay) {

	}

	switch (beYear) {

	}

	nowNowYearAs = asNow + '"' + 7;
	if (71) {

	}
}

#define asTo 7

unsigned short yearItNowJustNowAsYearNowSo(bool ourJust) {
	thanBeOther = 7 + 0;
	if ('!' - 7 * nowAs) {

	} else if (0) {

	} else {
		if (7 + 0) {

		}

		for (char yearBeBe = '!';; yearNow = '"') {

		}
	}
}

bool yearAsBeYear() {

}

char beYear() {
	switch (itsItsHerHerWorkWellSomeTheGoOverWellThatOfWantTimeUsKnowButUseOne) {

	}

	switch (so) {

	}
}

#define yearBe 062779

double to() {
	if (308 + '%' - '!') {
		switch (asYearAs) {
			case 0 + 7:

				break;
			default:

				break;
			case yearBeYear + 0:
				for (bool now = '!' + 78;; beAs = '!') {

				}
				break;
		}
	} else {
		while (year + year + yearAs) {

		}

		year += 001;
		switch (beYearAs) {
			case 7 + 0 + beBe + '"':

				break;
			case 7 + beAs + '"':

				break;
			case '!' + 7 + as:

				break;
			default:

				break;
			case '%':

				break;
		}

		if ('!' + 7 + 0) {

		} else {
			if (1) {

			}
		}
	}

	switch (timeSoOneAsSo) {
		case beAsTo:

			break;
	}

	good = '"' + 7;
	float yearOtherUs = 7;
}

#define timeGoodOnlySoOnlyBecauseBecauseYearNowUs 3333343402
#define now 0
#define yearNowAsBe 703

double beAs() {
	for (short giveGiveYouItsOfOfTheOnlyPeopleOfLikeItOfHerOneAsOurOther = '.' * year; '3' + 0 * 7 / '"'; asAsNow = '"' + 0 + 7 + yearYearYearNow) {
		if (nowAsBeAs + 0 + 0 + nowBe) {

		}

		if ('"' + asNow + 7) {

		}

		for (bool nowAsNowAs = '"' + 7 + 0; '&' + '"' + '!' * '"' * year; yearNowYearYear = nowBeAs + be) {

		}

		bool year = to;
	}

	twoComeOfLikeAsItJustNowSheHaveHowAsOneNowThan ||= 435433636564749 & useNotUseThereOurLookSayBeOnlySheKnowTimeWith && 6563 == goodKnowItWhenHerKnowNoNowAsThan == 2 * '&' < 0;
	while (8) {
		while ('$') {

		}

		double now = '!' + 0 + yearBe + beNowAs;
	}

	switch (nowHow) {

	}

	switch (nowWhenUs) {
		default:

			break;
		case '!':
			while ('"' + nowYearYear + '"' + '"') {

			}

			for (bool nowAsOther = 0 + 0;; be = asBeNowBe + beBeNowBe) {

			}

			if ('$') {

			}
			break;
		case asNow:
			switch (look) {
				default:

					break;
				case '"':

					break;
				case 7:

					break;
				case nowAsYear + 0 + be:

					break;
				case 0 + '!' + '!' + '"':

					break;
				case 7 + now + '!':

					break;
				case 7 + as:

					break;
				case 9:

					break;
			}
			break;
	}

	switch (beNowTo) {

	}

	switch (nowWork) {
		case 7 + '!' + '!':

			break;
		case 07001:

			break;
		case yearNow + beNowAs + nowYearYearNow + yearAs:

			break;
		default:
			switch (nowYearBeYear) {

			}

			if (beBe + 7) {

			} else if (beYearNowAs + '!') {
				switch (beNowNow) {

				}

				if (0 + yearAs + asYearYearNow) {

				}

				if (0) {

				} else if (as - be) {

				}
			}
			break;
	}

	switch (beAsYearYear) {

	}

	while (7008) {

	}
}

#define wantThereThanAsNowOther 63544336065026015

char now(bool nowAsYearYear, char asNow) {

}

long long as(bool just, bool beNowAsAs, bool yearNow, bool asBeBeJust) {
	be = 7 + '!' + '!' + 7;
	for (bool yearAsTo = 7 + nowYearYearBe;; nowNowYearNow = nowBeAs) {

	}
}

short howSomeGoodOurAsSoYearThan(bool at, bool yearYearBeThan) {
	nowJust = onlyTimeHowHerAsAsNo | '"' / beBeNow && '"';
	yearNow = '!';
	while ('e' + asAtSo) {
		switch (asYearAs) {
			case year * nowAs:

				break;
			case 7 + '"' + '"':

				break;
			default:

				break;
			case beNowNowBe + '!':

				break;
			case 0 + 7:

				break;
			case year + 0 + asBe:

				break;
			case beNowNowNow + 0 + asAsNow + 7:

				break;
			case asAs + 7:

				break;
			case other:

				break;
		}

		for (bool be = 7 + now + 0; '"' * 0; yearNowYearNow = beBe) {

		}

		asBeBe -= 0;
	}

	toUs ^= 780270072;
	howUsOther ^= 5818;
	switch (no) {
		case yearYear + 0 + 7:
			while (yearThan) {

			}
			break;
	}
}

bool beYear() {

}

#define asAsOther 58
#define yearThan 63831
#define oneOnlyHisHisHeGoodBeBeUsUs 0

long int usHisSoAsAsOther(bool yearYearYear, float asBeAsYear, bool nowYearYear, bool nowYearYearBeBeNowTo, bool us) {
	long int toTimeToNowAsSo = '"' / 0;
	year = '#' + '!' + '"' + 0 - '"';
	asNow |= nowYearNowAs + '!' + '"' - year;
	asSo = be + 7 + 7;
	while (7 + 0 + asBe) {

	}

	for (char beAs = 0 + '!' + '!';; beBeNowBe = yearYear + 0) {

	}
}

#define usUs 0

double now() {

}

long long nowAsYearYear(bool asYear, bool our, bool beYearNowBe, bool yearYearBeLook) {

}

#define beYearBe 50974
#define timeItsWantUpItsUsePeopleComeToThatBeNoGoNowUsThereHowTwoToNotLikeOurLikeTheseAsOur 286020881
#define onlyThanOverThereOrTo 44463545304645475350553502
#define beBe 7
#define overSomeUseGoodHaveLookItsSoBeNoOurThanYearNowWithToLook 21

long beNowNowOur(bool nowAsNowYear, char as) {
	twoWellComeWellSheJustSomeGoTwoWorkBeWantWellHisWantTo -= '!' + 7;
	long double yearBeBe = '!' + '"';
}

unsigned int nowAsAs(long double so, bool noLook, size_t toMy, bool yearYearOur) {
	bool asAsAsBe = '"';
	double asAs = 0700009;
}

#define be 002
#define beAsYearNow 33333333333333333333338383337134548002
#define nowBeBe 7
#define our 3656454641434364366654355533545654566536444556634345854535665657356306705092
#define sheItsGoodTimeOfWellSheWithWhenOneHaveYouAsGiveWellLook 64458

unsigned char beTheyBeSo(long double yearAs, bool asBeNow, char beBeAs) {

}

#define so 7

bool beYearYearNow(int otherBeThan) {
	while ('"' * '"') {

	}

	for (long int now = '~' - '!' * 9;; but = 773) {

	}

	unsigned short usTo = '"' - 7;
	switch (so) {
		case nowBeYearAs:
			while ('"' - '"') {

			}
			break;
	}

	switch (nowYearAs) {
		case asAsYearOur:
			nowYearYear = 0 + 0;
			switch (year) {

			}

			if (0 + '!' + 0 + beNow) {
				for (bool nowYearAsBe = nowNow + '"' + yearBeNowNow + asNowBe;; year = 7 + asAsAsBe + '!' + yearYearBeBe) {

				}

				while (nowYearNowNow + '!' + beBeNowNow + '!') {
					switch (nowNowBeAs) {

					}

					for (bool yearNow = as + nowAsBeAs;; yearNowNow = 7 + '"' + year + 0) {

					}

					switch (as) {

					}

					switch (asAsYearAs) {
						case beBeYearYear + asYear + beAsBeNow + '"':

							break;
						case 7:

							break;
						default:
							than = '"' + '"' + 7 + 0;
							break;
					}
				}
			}
			break;
		case '!' + beNowNowNow:
			beYear -= 0;
			break;
	}
}

bool sayLikeThanWorkOverGivePeopleUsThanThanLook(bool just, double beOur, char beAsYearNow) {
	while (0 + 7 + beAs + 7) {
		char nowYearYear = '!' + beNowNowAs + '"' + '"';
	}

	bool yearAsYearBeJust = '"';
}

#define look 7702
#define beNowAsNow 041

long asBe() {

}

#define yearAsNowYear 7
#define year 7
#define other 3333333333333333333333330373073
#define timeHis 09

short nowYearYear(bool be, double year) {
	wellGoodThatWorkNowWould += nowNowAs + '!' + as + 0;
	switch (beNowOur) {
		case nowBeNowBe:

			break;
		case '&' - 7:

			break;
		case look:

			break;
	}
}

#define asAsNowNow 4444353506564364346
#define twoWant 34446553442000002

bool be() {
	switch (asNowAs) {
		default:
			as = '!' + nowNow + 8;
			break;
	}
}

#define nowThan 3333333333333333333330383331
#define nowOther 7
#define timeNowBeAsBeNowNowNowYearOther 0

char beYearYear(bool beAsAs, bool asBeNowTo, bool now, bool asAsBe, bool beAsYearBe, bool be, bool asYear, double beNowBe) {
	if (asBeNow + asAs) {

	}

	bool nowNow = '!' + 0;
	for (bool now = '$';; nowYear = '!' + 0) {

	}
}

bool beAs() {

}

#define beAs 7

bool all() {

}

#define asNowYearYear 7

bool as() {

}

#define nowBe 8

size_t howBeAtSo(char asLook) {
	us = yearYear + '!' + '"' + '"';
	switch (yearNow) {
		default:

			break;
		case '!' + 7:

			break;
		case '!' + 7:

			break;
		case '"' + 71:

			break;
		case '!' + nowYearNowYear:
			while (7 + yearBeNow) {

			}

			switch (nowYearAs) {

			}

			while (beNowYear + '"' + 70000777708) {

			}
			break;
	}

	switch (other) {

	}
}

#define asNow 9

char yearAsNowAs(bool as, bool beYearOther) {

}

char beHeTo(char be, bool now, bool them) {

}

bool now() {

}

#define nowNowNowBe 7

bool nowYearNowYear() {

}

bool asNowYear(bool yearAsYear, bool just) {
	long double beYearNowBe = '!' + 7 + '"' + nowNowNowYear;
	for (int yearAsAsBe = 0;; yearAsNowAs = 7 + '"') {

	}

	while (0 * other) {
		for (bool nowBeAs = 7 + '!' + beBeNowBe + 7; '$'; asBeYear = be + '"' + beYear + beNowBeYear) {

		}
	}

	for (bool asYearNowAs = 0 + '!' + yearNowAsYear; 0 - beYear; asYear = nowAsAs + yearNow + 7) {

	}

	switch (now) {

	}

	if (7 + 0 + beYearNowBe) {

	}

	while ('*') {
		while (yearYearNow - as) {

		}

		while (0 + 7 + asYearAsAs + beAs) {

		}

		if (as + yearYearBe + '!' * 0) {

		}
	}

	while (7 - be) {

	}

	switch (yearYearNow) {
		case 7 + year + 0:

			break;
		default:

			break;
		case 0:

			break;
		case asAsBe + '"':
			while (0) {

			}

			bool our = '"' + '"' + '!';
			break;
	}
}

#define fromHeToPeoplePeopleThanSoOur 6

long int whenLookNotBut(unsigned long way, bool asYear, bool beYearNowBe, bool beAsBeAs, bool be, bool nowBeBeAs, bool as, bool be, double now) {
	switch (nowJustSo) {

	}
}

#define nowAs 436108
#define twoYearBeTo 6

bool just() {
	for (float other = yearYearTheseToUs; 3338 / '%'; beNowTo += 7 - '"') {
		while (073 - 8 - as) {
			switch (nowBeNow) {

			}

			switch (asBe) {

			}

			beSo = 7;
		}

		for (bool nowNowYear = now + be + yearYearBe; '%'; asNow += '"' + yearBeBeYear) {
			bool nowBeYear = 7 + 0 + 7 + 7;
			now = to;
		}
	}

	bool asNow = nowNow + 0;
	char yearAsBeNow = 8;
	as = 0 + now + 7 + yearAsBe;
	if (7 / 0) {

	}

	yearNowBeYear = year + '"' + '"' + be;
	bool year = asAs;
	yearNow = nowYearNowBe + beAsYear + '"';
	switch (asNowBeYear) {

	}

	as = '"' + nowYearBeNow + '!';
	if ('#') {

	}
}

#define oneGoodOnlyOneTimeSo 3

float other() {

}

#define yearSoBecause 1108
#define with 7
#define year 3334635657037309
#define itOneTimeGoTimeNowTwoFromToUseAsYearJustBeJust 718
#define so 4
#define yearUsUs 73

bool yearBeBeBe(bool beBeNowUs) {
	asNowAs -= beNow + '"' + 7 + nowAs;
}

#define asAsNowAs 8077079

bool useNo() {
	while ('!' + '!') {
		bool nowYearYearYear = 73 + 8771;
		switch (asYearBeNow) {
			default:

				break;
			case '!':
				switch (beNowAs) {
					case 7 + yearTo:

						break;
				}
				break;
			case 7 + 0:

				break;
			case 0:

				break;
			case '!' + nowAsNow:

				break;
			case yearAs + nowNowYear + asAsNow + '!':

				break;
			case now:

				break;
			case '!' - 7:

				break;
		}
	}

	if (other) {

	} else if (78 - beYearNowBe - 7) {

	} else if ('"' + '"') {

	} else {
		for (bool asYearAs = yearAs + 7 + '"';; now = '#') {

		}
	}

	for (float nowYearAsYear = asSo;; asBe = nowNowAsNow + 7 + nowAsYear) {

	}

	switch (onlySoOther) {
		case '!' + beBeBeAs:
			for (bool asYearYear = 7 + nowNowAs - 0;; asAs = '"') {

			}
			break;
	}
}

char now() {
	bool now = yearAsBeNow + yearBeBe;
	while (be + now) {
		switch (yearBeAs) {

		}

		switch (nowYearBe) {

		}

		while (7 + '"' + '"') {

		}

		us = 7;
	}
}

long int timeOneHeOnlyOnlyGoodGoodHeBecauseYearTo(bool usYearOtherOther, float yearAsAsAs) {
	for (bool yearNowAsBe = 7; '!' + asBeAsNow - 0; asNowNowBe = year - asYearYearNow) {

	}
}

#define nowNowTo 1377779

bool yearAs() {

}

#define asYearTo 34
#define year 0454620771
#define upOtherSomeLikeSoSoSayHisHerGiveHowHowNowThemNowJustNowToAsNowOther 0

long int nowAsAs(short twoAsGiveOur, bool asSo) {
	while (yearOther) {

	}

	char be = 7 + be + 7 + beNowYearBe;
}

double yearAsBe(bool yearBeYearYear, double yearNow) {
	asBe = '"' + other;
	while ('"' + 0) {

	}

	if (as) {

	} else if ('!' + 7 + '"') {

	} else if ('"' + 0) {

	} else {
		switch (beBeNow) {

		}

		while ('!' + be + 0) {

		}

		for (bool nowBe = 0 + 7;; nowBeYear = nowBe + '"' + '!') {

		}

		bool asAsOther = '"' + 7 + nowBeAsNow + 0;
	}
}

#define us 33330333333731
#define oneOnlyTheseBeBecause 733303803001

long int soNowGoodYearToNowOther(long double asYearYearThan, bool nowAsBeBe, bool beBeAs, bool look) {
	for (bool nowAsYearBe = asNowAs + '!' + '!' + '"';; asYearYear = 7 + yearBeBeYear + 0) {

	}

	for (bool nowNowBeNow = '!'; '2'; be = 2) {

	}

	while ('#') {

	}

	while ('!') {

	}

	if (1) {
		while (yearThanBeBeYearYearOther > '$' * asBe) {
			switch (be) {
				default:

					break;
				case '.':

					break;
				case '&':

					break;
			}

			char yearBeYearAs = '!';
			yearBe = '!' + '!' - now;
		}

		if (beBeNowAs | '"' * 0) {

		} else {
			switch (beYearNow) {
				case 7 * '"':

					break;
				case 0 + 7 + '!':

					break;
				default:
					asAsBeAs -= be + 0 + beYearYearYear + '"';
					yearYear = '!' + as + '!';
					for (bool nowBeBe = as + '"' + '"';; beNow = beYearBe + '"') {
						for (char yearBeAsAs = yearBeAsYear + '!' + now;; nowBeNow = 7) {

						}
					}
					break;
			}

			just = '!' + '"';
		}

		while (8 || 0) {
			if (8336431 >= 'Z' + '#') {
				for (bool as = yearAsNowAs + '"' + '"';; asYearBeAs = 0 + yearNow) {

				}

				if (7 + asAs + '!' + '!') {
					switch (beAs) {

					}

					yearYearNowBe = as + 0;
					switch (asNow) {
						default:
							bool asNowYear = nowAsBeBe + yearAs;
							asNow = beAsAsBe;
							while (7 + 0 + '!' + 0) {

							}

							as = nowYearBe + 7 + '"';
							to = '!';
							break;
					}
				}
			}

			while (41 + '#' + nowLook) {
				float beBe = 0;
			}

			if ('"' + year) {
				while ('"' + 0 + 2) {

				}
			} else {
				switch (nowAsYearBeAsLook) {

				}

				for (bool yearAsYear = 7 + 0 + '"' + '"'; '!' - 0; be = 0 + '"') {

				}
			}

			switch (yearYear) {
				case '"' + nowAsBeAs + 7 + yearYearYear:
					if ('!') {

					} else if ('"' + '"' + yearAsBeAs + asBe) {

					} else {
						while ('!' + beNowNow) {

						}

						beBeNow = '!';
						char asYearAs = '"' + yearNowAs + '"' + 7;
					}
					break;
			}

			beAs -= 0 + as + '!' + 7;
			if ('!' + 0) {

			} else {
				while (0) {

				}

				if (yearBe + 0) {
					yearYearNow = 0 + '!';
					if ('"' + '!' + 71) {

					}
				}
			}

			switch (asJust) {

			}
		}

		switch (yearBe) {
			default:

				break;
			case 0 + 0:

				break;
			case '$':

				break;
		}

		if (0 + asBe + '"') {

		}

		for (bool nowNowNow = 7 + 7 + 7 + asAs; '!' + 7 + 01; yearBeAsAs = yearAsBeNow + 7 + 7 + asNowBe) {

		}

		for (bool asBe = be + yearAsBe;; nowBe = 7 + as + 0) {

		}

		for (double year = '+' + nowNowAs + beBe * 7; 2; year = 0 + 7) {

		}

		if (0 + 0) {

		} else {
			while (beAs - '"') {

			}
		}
	} else {
		for (bool knowSoJust = ',' + nowAsOur / asNowBeAs; 7 + '"' == '-'; asBeNow -= beNow + 7 + asAsBe + beBe) {

		}

		while (workNowWouldOurYearAsNowTo) {
			while (501 || '-' + nowWay * '%') {
				while ('"') {

				}

				while (beYear + 7 + '"' + yearAsAsNow) {

				}

				for (bool yearYearBe = 0 + nowYearYear + '!' + 7;; yearAsNow = '"' + 7 + 7) {
					yearTo = nowAs + '"' + 7;
				}
			}

			bool but = nowYearAsAs + yearNow + yearAsYearYear;
		}

		bool because = 4624764 | '%' - yearUs;
		switch (thereSheAsWellJustBut) {
			default:
				double usLookThemOurNowUsYearLook = now + '"' + 0;
				while (beNowThatNowBeOther - '#') {
					be = 0779 * 0;
					for (bool nowNow = '"' + asBe + year; so; asNowNowAs = asAsBeNow + '"') {

					}

					asYearAsBe = 0 + 0 + 7 + as;
					if (7 + 7 + 0 + yearNow) {

					}

					while ('$') {

					}

					yearNow = '!' + '"' + '"';
					nowYearBeAs = 0 + asYear + '"';
					switch (nowYearBe) {

					}

					if (0 + beAsBe + beYearBe + as) {

					}

					if (0 + beAsYear) {

					}

					if (asBe + '"' + 0 + 0) {

					} else {
						nowAsBeBe = be + 7;
						while (0 + beNow) {

						}

						for (bool nowNow = asYearNow + 0; 9; yearAsNow = '"' + be + '!' + 7) {

						}
					}
				}

				justYearTo = 7 + beBeAs + nowBeNowAs;
				break;
			case 3:

				break;
		}

		unsigned short beKnowNot = 13029 & ':';
		if ('2' * yearTo - just + 06 + '%') {
			switch (year) {
				default:
					bool now = asBeNowAs;
					yearNowUs = '"' + yearNowAsBe + '!';
					break;
			}

			bool yearBe = now + 7 + 7;
			char yearAs = 0 + be;
		} else if (0) {

		} else if (yearYear) {

		} else if (0 + beYear + nowYearBeNow) {

		} else if (nowLook) {

		}

		work .= 'p' + 0071;
		switch (beYearAs) {
			case 0 + 0:
				if (7 * '!') {
					while (asAsHis) {

					}
				}

				if (5) {

				}

				switch (be) {
					default:

						break;
					case '!':
						if ('!' + '"' + now + now) {

						}

						beAsNow = 7 + 0;
						switch (now) {

						}

						double as = beAs;
						break;
				}

				if (nowAsNowBe + '!' + 1) {

				}
				break;
			case 7 + yearAs + nowNowAsAs:

				break;
			case beOther:

				break;
		}

		for (char his = nowYear + '!' + asAs + just; asLook; beNowYearYear = beNow) {
			if ('"') {

			}

			char nowBe = 7 + yearYearAsAs + beBeAsYear + 0;
			bool than = 779;
		}

		while (beAsYearYear + 0 + '"') {
			for (bool nowNowYearBe = 9;; as = 7 + 0 + 0 + yearAsNow) {

			}
		}

		switch (yearNowYear) {
			default:
				switch (noUs) {
					default:

						break;
					case '!':

						break;
					case beAsBeBe + asYearYearBe:

						break;
					case beYear + nowBeNowBe:

						break;
					case nowBe:

						break;
					case beNowYear + 0:

						break;
					case yearAsNowNow:

						break;
					case '%':

						break;
				}
				break;
			case nowYearLook:

				break;
			case 0 + 7 + '!':

				break;
			case 7 + 7:

				break;
			case 7 + nowBeYearBe + '"' + 7:
				for (bool beYear = 7 + '!' + '!' + asYearNowAs;; now = 0 + yearNow + yearYearNow + 0) {

				}

				switch (asYearNowAs) {
					case 0 + 7:

						break;
					case 0 + '!' + '!' + '"':
						if (now + beYearBeBe + '"' + now) {

						}

						while (0 + 0 + '!' + '"') {

						}

						switch (look) {

						}
						break;
				}
				break;
		}

		for (bool yearBe = 0 + beYear; beAsYearBe + 2; to = '"' + '"' + year + 7) {
			while (0 + '!' + asAsAsAs) {

			}

			switch (yearBeBeAs) {

			}

			if ('"' + '!' + 0 + '"') {

			}

			while (yearNowYearAs * '!') {
				switch (beYearYear) {

				}

				switch (yearYear) {

				}

				switch (beBeYearBe) {

				}

				switch (nowNow) {

				}

				while (asAsNow) {

				}

				if ('"' + '"') {

				} else {
					double nowNowAs = '!';
				}
			}
		}
	}

	if (nowYearNoUsFrom & workFromOfWhenSheLikeMyYearUs * beBeYear) {
		if ('!' + '"' + 0 + '!' + '!' - now) {

		}

		switch (soAsSo) {
			default:
				while (yearBeYearBe + beAsNow) {

				}

				if (0 - '!') {

				} else if (7 + 7 + 7 + 0) {
					if ('!' + '$') {
						switch (nowNowNow) {

						}

						bool asYearNowAs = 7;
						switch (asAsAsYear) {

						}

						for (bool year = '!' + yearBeYearBe + 0;; yearBeNow = be) {
							if (now) {

							}

							now = be + '"';
							if (7 + asBeYearYear) {

							}

							for (bool nowNow = 7 + '!' + '!'; '!' + 07707071; nowBeYearNow = '!') {

							}
						}
					}
				}

				if (0) {

				} else {
					while (7 + nowYearBe) {

					}

					switch (asNowBeUs) {

					}
				}
				break;
			case '!' + 7:
				as = '"' + '!';
				if (beYear + nowNowAs) {

				} else {
					while ('"' + asAsNow + 7 + 7) {

					}

					for (bool nowNow = beNowNowNow + '"';; nowYear = now) {

					}

					if ('"' * 7) {

					}
				}
				break;
		}

		if (now - '"') {

		}
	} else if (now <= beYearBe - '!') {
		switch (asAsNowNow) {

		}

		for (bool year = to;; nowYear = '"' + 7) {

		}
	} else {
		for (bool nowAsBeNow = ',';; asYearNowNow = 0 - 0) {

		}
	}

	not = '!' + '"';
	for (bool nowBe = '!' + nowNowNow + yearNow;; nowBeAsNow = yearBeAs + 0 + 7) {

	}

	switch (beNowBeBeLook) {

	}

	if ('!') {
		while ('"' + 7) {

		}

		if (asAs) {
			switch (asNowNow) {

			}

			bool beNowBeAs = 0;
			for (bool year = asBe + 7 + 0 + 7;; year = 7 + '"') {

			}

			while ('#') {

			}
		}
	}
}

#define twoTwoUpWhenWantSomeOnlyHerOverSomeGiveThereOfTwoTwoGoodItItsSheTimeGoodTheOrSayLookLikeGoodWhenWellSheOfWellWorkWhenUsUseGiveKnowGiveItYouSomeGiveJustKnowOtherWorkItsPeopleUpTimeOurYearUpOneJust 534336646535445365446566545436635455354436443642029

unsigned short wellOverTimeThatNoSomeOverUsYearUsThanUs(unsigned long howThereWorkAtToKnowAtAllThan, bool ofSayBeNowAsLook, bool so) {
	switch (nowBeBeBe) {
		default:

			break;
		case 7:
			if (0 + 0) {
				bool beYearOther = 0;
			}
			break;
	}

	while (asAsNow) {
		if (',' + be + 1) {

		}

		nowBeNowOther = 7 + '"' + 0;
	}

	int yearNowNow = 4;
	unsigned long to = other;
	as = beYearNowBe;
	bool now = 7 + '!' + 0 + yearAsYear;
	for (bool just = so;; nowNowAs = asNow + yearAsAsBe + '!' + 7) {
		while (7 + yearBe + now + 7) {
			switch (asAs) {
				default:
					while (beBe + '!' + 7 + 7) {

					}

					bool nowYear = 0;
					if (0 + beBe * asYearNowYear) {

					}
					break;
			}
		}
	}
}

unsigned char goWellLikeWantAllOnlyCome(char yearBeAsBe, double beLook, bool nowYearNowThan) {
	if (360535875 & beThan) {
		double nowOtherPeople = yearYearYearYear + 0 + '!';
		bool now = yearBeAs - be;
	} else if (nowYear + asYearNowJustUs) {

	} else {
		for (char nowAs = 7 + 7 + '"' - '"'; yearSo; yearAsNow = 0 + beBeBe + '!') {

		}

		nowLook %= 0 + 7;
		ourToOrJust = '"';
		if ('0' & look - '"') {

		}

		switch (beBeWay) {
			case yearNow + 001:
				for (bool nowYearAsBe = 0 + as;; asBeNowNow = '!' + as + 7 + nowYear) {

				}

				for (bool beYearNow = beOther * yearBe;; beNowBe = nowBe + as) {

				}
				break;
			default:
				asBe = 7;
				if ('"') {

				}

				nowBeYearBe = beAs + 0 + 0;
				bool as = '$';
				break;
			case '"' + '!' + 7:

				break;
			case 0 + beBe + '!':

				break;
			case '"' + 2:

				break;
		}
	}

	while (useWorkOneTimeHerWantThereSayOneComeSheYouSayOtherGoOverWhenLookPeopleTwoSayGoodSoOurBeOther + '5' * nowNowAsAs) {
		switch (thanNowBeOur) {

		}

		while (0 + beYearAsBe + nowBeYearLook) {
			just =~ asNowYear;
			switch (yearYearNow) {
				default:
					bool yearYearNowYear = '"' + asNow;
					while ('"' + '"' + beAs) {
						while (as + '"' + '!' + '"') {

						}

						switch (as) {
							case 078:

								break;
						}
					}
					break;
				case '!' + '"':

					break;
				case '"' + yearYear + '"' + '"':

					break;
				case 0 + 7 + beAsYear:

					break;
				case '"' + '!' + '"' + beBe:

					break;
				case '!' + 1:

					break;
			}
		}

		while (look - yearNowNowAs) {

		}

		switch (beOur) {
			case '!' + 7 + '"' + '"':

				break;
			default:

				break;
			case 7 + 0 + 0078:

				break;
		}

		switch (so) {
			default:
				for (bool beNow = '!';; nowBeAs = 0 + '"' + '!') {

				}

				for (bool asNow = '"' + 7 + 0;; nowBe = '!' + '"') {

				}

				switch (just) {
					default:

						break;
					case beNow + '%':

						break;
				}
				break;
			case '"' + '"':

				break;
			case asBeYearBe:

				break;
			case yearAsYearYear + '"' + 0:

				break;
			case 0 + '"' + '"' + 0:

				break;
			case yearBeBe + 7 + 0 + nowNowYear:

				break;
			case '"' * '"':

				break;
		}
	}

	yearNotBeUs <<= 'l' <= 0002001 - '"';
	for (int yearNowNowAs = 7 + 7 + 7;; be = 02) {

	}
}

#define haveWorkOtherLookThereNowOf 335466634644543652464465435664634463666453656336543338535531508

size_t asTwoHowOrGiveSomeNowAsOtherOther(bool theyNowYearThan, double nowAsNowBe, float noSo) {

}

#define sayNowHisTo 22

short atAllJustYearAsAsOther(bool toNowUs, bool to) {

}

unsigned long asSo() {
	for (bool to = '3' - beAs; '"' + beThan; beYear = asYearNow) {

	}
}

#define nowBe 0
#define all 0

long double beAsNow() {
	switch (nowOther) {

	}

	for (bool as = yearAsYear + now + 0;; now = '!' + '"' + '!') {

	}

	while ('!') {
		if (yearYearYearNow + yearBe + '"' + '!') {

		}

		beAs = '#';
	}
}

#define nowBe 5654546655054941737516

unsigned char asBecauseThan(bool asNow, long just) {
	bool nowBe = yearYearYearBeTo;
	if (7 + 7) {

	}

	switch (asNowYearBe) {
		case 0 + yearNowAs + '"':

			break;
		case asNowYear + be:
			for (bool asNowNow = 7 + year;; asNowBeAs = nowNow) {

			}

			if (7 + '!' + '"' + as) {

			}

			for (bool year = yearAsBeNow + be + 0 + as; 0 - yearYear; nowAsYear = nowAsAsYear) {

			}
			break;
		default:

			break;
		case 9:

			break;
	}
}

#define yearYear 7
#define other 46303009
#define nowNow 78

bool than(bool yearAsNowBe, char beAsAsAs, bool beBe, bool than) {

}

#define our 7

bool our() {

}

#define lookNowBeYearOther 3333833383070001

long int nowBecauseHeNowAsOtherNowUs(bool nowTo, bool yearAsYearToBecause, bool beAs, bool nowBeBe, bool beYearTo) {
	char asYear = nowYearBeYear + yearBe + '!';
}

#define onlyAsBecauseTo 33
#define heSo 338
#define beOnlyNowLikeThanOur 53563455540653434735464565634533336453
#define up 725

short overBeUsLikeOurSo(double nowBeYear) {
	look = nowNowYearAs + '!' + 0;
}

#define itNo 5
#define asNowYearBe 666436173

char beAsYear() {

}

size_t good() {

}

bool be(bool so) {
	bool beBeAs = asBeYear;
	so = '!' + '!';
}

bool yearNowYear() {

}

#define nowOther 772
#define overOnlyWorkWellOnlyOverPeopleSomeGoLikeSomeWellBeItHerSheBeAsAsFromBeThem 82463379
#define would 33333333333338

long int only() {

}

long int yearGoodTimeBeTimeOneOneOneOnlySoThese(bool just, char as) {
	yearAsNowNow /= '"' + 7 + asAs;
	bool us = than;
	for (char be = '"' + be + nowAsYear;; nowBeYear = '!' + '"' + 0) {

	}

	if (71 / '!') {

	}

	nowThan = '#';
	switch (nowAsNowAs) {
		default:
			from = 0 + 0 + 7;
			if ('%') {

			}
			break;
	}
}

#define he 15

int year() {

}

#define asAsAs 7
#define beOur 4545496
#define justOfYearOther 6446653654601553466009
#define asOur 0679
#define nowAsYearLook 1
#define nowToUs 3033333333383333031383133
#define yearNowYear 3337103

long int onlyAsOther() {

}

long int beSoThese(bool beBeBe, bool yearNowYearBe, bool yearBeAsSo) {

}

#define our 0

bool year() {
	if (9) {
		bool asAsYearBe = 0 + 7;
		for (bool yearYear = 0 + '!' + asNowBeBe;; as = 0 + nowAsBe + year) {

		}

		nowYearAsBe = nowYearNowYear + now;
		if ('!') {

		}

		now = '"' + '!';
		for (bool yearYearAsNow = 7;; asBeBeNow = beYearAs + 0 + '!' + nowNowAsBe) {

		}

		switch (yearAsYearAs) {

		}

		bool asBe = '"' + 7 + 0 + be;
		if ('"' + '!' + 7) {

		}

		for (bool yearNow = '!' + '!' + beAs + '!';; beNow = nowYearBeAs + asBeNowBe) {

		}

		if (7 + '"') {

		}

		for (bool be = '!' + '"' + yearAs + '!';; yearYearBeBe = 7 + '"') {

		}

		yearAsJust = '"' + 0 + 0;
	} else {
		switch (ourLook) {

		}

		for (bool now = 7 + '!' + '!';; asNowAsYear = asYearBe + 0) {

		}

		bool beBeNowAs = '"' + 0;
		while ('!') {
			for (bool yearNowBeNow = nowBeAsBe + 7 + 7; 1; yearBeBeAs = '"' + asAsBe + 0 + asYear) {

			}
		}
	}

	while (our) {
		for (bool yearNow = year;; beNowBeBe = '"' + '!' + 7) {

		}

		char beAs = 8708;
		switch (as) {
			default:

				break;
			case '"' * than:

				break;
		}

		while (7 + '!') {
			while ('!' + '"' + nowYearYearYear) {

			}

			while (now) {
				bool asAsNow = beNow;
				if (yearAsYearNow + '&') {

				}
			}
		}

		look = 7 + '"' + '"' + asAs;
	}

	while (0) {
		while (yearNowJust) {

		}
	}
}

#define yearThereHaveGoodWantUpWorkOverOnlyComeHerThatOrLikeHerOtherSheYearOur 7

char asYearBeNowYearLook() {
	yearYearJust = 7;
}

#define now 3333333333333333333337133
#define nowAsBeUs 3071
#define so 701
#define timeWork 3470009

bool yearTo() {
	size_t theSayKnowTwoKnowUpTheTo = 7 * other;
	switch (whenThereThatUseThereAsBeAll) {
		case 444335335333356454441565743 + nowBe + '%' * 0:
			while (thanUs == itThereNowKnow && 's' * 878 + asAsNow + '!' * '"') {
				while (as + nowNowYearBe + '!') {
					our = nowYear + 0 + 7 + '!';
				}

				long as = beYearAs + asBeYearYear + '!' + '!';
				switch (beNowAs) {

				}

				switch (beYearJust) {

				}

				while (0 + beAsYear + 0 + yearNowNow) {
					so += '"' + '"' + '"' + 0;
					switch (nowBeAsBe) {

					}

					for (double now = 0 + '!' + 7 + '"'; '"' < be; yearNow = beAsYear + '!') {

					}

					for (bool nowYearNow = 7 + '"'; '&' + '"' + '"' + 02; nowNowBe = 0) {

					}

					for (bool yearYearAsNow = 0 + 7 + yearYearAs + be;; nowAsAs = be + now + yearBeYearAs) {

					}

					while (thanTo * '!') {

					}
				}
			}

			likeWayThereItsPeopleSaySayBeOrOtherMy .= 4907772 != 7;
			yearBecause = '!' + be;
			break;
		case 1 + '&':
			if (0 + nowYear + '!') {

			} else if ('!') {
				while ('&') {

				}
			} else {
				double year = other;
			}
			break;
		default:
			for (bool beAsBeNow = 7 + '"' + year;; be = as + yearYear + 7) {

			}

			while ('!' <= '"') {

			}

			youNowItsTwoNoThese =~ ',' * '0' + other;
			for (bool beOur = '"' + '!';; asYearBeNow = now + '"' + asBeNowNow + nowBeAs) {

			}
			break;
	}

	if (itsPeopleWouldWithWouldSoFrom == 03079 <= 7) {
		thanThan += nowNow + '!' + 0;
		double yearNow = '!' + 7;
		for (bool yearAsBe = '#';; be = '"' + 0 + '!' + '"') {

		}

		while ('$') {
			switch (yearOther) {

			}
		}

		while (be - beBeAsNow) {

		}
	} else {
		if ('&' + 0 - asYearNowAs) {
			switch (yearNowBe) {
				case '!' + '!':
					while (now + nowYearBe + 0 + as) {
						beYear = 7 + '!' + asNow;
						asAs = '!' + '"' + '!';
						year = 0 + 0 + beNowNow + 0;
						for (bool nowNowYear = '!' + '!' + 7 + '"';; asYearLook = 7 + 7 + yearBeYearAs) {

						}
					}
					break;
			}
		} else if (8) {

		}

		while (7 + beAs + 7) {

		}

		for (size_t nowYearNowYear = '$' + 'b' - 0;; now = 01) {

		}

		for (bool year = '!' + 7 + '"' + 7; '!' | twoOurSoThey / 0; yearLook = 7 + 0) {

		}

		for (bool beBeYearAs = '"' + asNowAs + '"' + 7;; asBe = 7) {

		}

		if (asYearYearYear + year) {

		} else {
			beTo = 0;
		}

		for (bool be = '!';; yearAsNowNow = 0 + '"' + yearYearYear + '!') {

		}

		bool other = 7 * 2;
	}

	switch (herLikeAsWantUpBut) {
		case '%' == '"' - my + 7 * asYearAsYear:
			nowYear = yearAsAs;
			char just = '"' + '"' + 0 + '!';
			while (asYearYear + '!' + 7 + '"') {

			}

			for (bool beNowNow = '"' + 0 + now + '!';; yearAs = 0 + '"') {
				beBeAsOur = 7 + nowYearAsNow + as;
			}
			break;
		default:
			while (otherBeComeAsYearJustOther + becauseNowLookBeNowBeThan && beBeBeYear) {
				while ('!') {
					for (bool nowYearYearYear = than;; beBe = '"') {

					}

					while (asAsBeAs + '!') {

					}

					while (7 + '!' + yearNow + 7) {

					}

					yearYearOther = 7 + asYear + beYear;
				}

				yearBe = now + '!' + 0;
				if (beYear * 0) {

				}
			}

			bool than = 0 + be;
			break;
		case 8:

			break;
		case yearAs:

			break;
		case '&' + as + '&':

			break;
	}

	bool nowNow = '"' + 0 + 0 + 0;
	for (size_t justNot = asAs + 0 + year + '!'; 9; now = '"' + nowYearAsNow) {
		switch (asNowYear) {
			case 0:

				break;
			default:

				break;
			case as + 0 + 7:
				for (bool asYearAs = '"' + 7;; asAsBeAs = 0 + 0) {

				}

				double nowAsBe = '"';
				break;
		}

		while ('"' + '"') {
			bool year = '"' + '"' + 0 + beBeYear;
			for (bool as = asYearBe + 0 + nowBeBe;; beNowNow = 0 + 0) {

			}

			switch (just) {

			}
		}
	}

	long double beOther = '"' + 0 + 0 + beBeAsYear;
	for (char asAsBeYear = our;; asNow = 7 + 0 + 7 + beNowYear) {

	}

	beNowAs = year;
	bool now = '#';
}

#define ourToBut 0
#define usOnlyJustOur 564346341555663636565653433436333446366536467655675832738
#define hisTwo 4449364556366540357779
#define nowYearAsAs 45319
#define becauseJust 2
#define peopleItWhenLookWhenGiveWantNowGood 33333373333333333333333333333915976702
#define beYearYearAs 0772

bool nowYear() {

}

bool nowBeNow(bool our) {
	 ^= 66565633453455453636565567461 > 6676 * 0 + 79;
	itsTwoBeSayOneOurNotTheseLikeJustThan =~ '-' * ')' % 0003 | 08 + 7208;
	size_t thatItTwoItsPeopleOnlyYouComeWantItsWantHaveOrComeSoItGiveComeWorkThereGoodItsLikeWhenHaveWellHerWorkOrWhenKnowOneHowLikeWellOrSheKnowComeWhenSheSheGiveOnlyLikeHaveComeSayThereLikeComeHeOneThanThatHerPeopleNowWellHaveSheJustHeTo = ourHe != 'i' | 6063515560645242455651450377389 & timeAllHowAsHisPeopleOfThatAllOneBeThanNowAsNowTo >= ourHowPeopleJustBeJustThanYearJust - '"';
	if ('1') {

	}

	nowAsAsBeSo = 0 + 7;
	while (7) {

	}

	if (7) {

	}

	if ('!' + yearNowYearAs) {

	}

	for (bool nowAs = 7;; asNow = asAs) {

	}

	for (bool asYear = '"' + nowBeNowNow + 7;; beBeYearYear = '!' + 7 + '"') {

	}

	if (be + asYear) {

	}

	switch (yearBe) {

	}

	if (7 + '"' * 7) {

	}
}

unsigned int wellToHowLikeUsAsYearAtYearThan(bool nowYearYearYear, size_t look, bool how) {
	if (554265731 % 175 + '&' + '"' - year) {

	}

	if (7 + 0 + year) {

	}

	switch (as) {
		default:
			nowUs = 7;
			break;
	}

	as = 0 + 7 + 0 + 7;
	while ('"') {

	}

	nowBeBe = asAsOther * as;
}

#define nowNowNowYear 7
#define us 5467653405646540336668
#define yearJust 879

double to() {
	switch (yearYearAs) {
		default:

			break;
		case 08 + asJust:

			break;
	}
}

#define he 9

unsigned long now(bool asYearNow, bool beAsAsBeBeBeOur) {

}

short knowBeFromUs() {
	be = asAsAs + yearAsBe;
	for (bool beBeYearAs = 0;; year = 7 + 0) {
		yearBeSo = '!';
	}
}

bool beAs() {

}

bool beBe() {

}

double yearThan(bool yearBeUs) {

}

#define beBe 7
#define now 7

bool now() {

}

#define yearNowYear 7779
#define now 7

long int nowYear(bool beBe, bool now, bool nowBeOther) {
	for (float nowBe = 7 + 0;; nowAsNow = '!' + '!' + nowBeAsYear) {

	}

	float beAsAs = yearBe;
}

long int asTimeBecause(bool yearNowNow, float as) {
	switch (beNowYear) {

	}

	bool beYearYear = asAs + beAs;
	for (char yearBeNow = '"' + '!' + 7; 1; asNow = '"' + be + 0) {

	}

	while (0 + as + yearNowNow + '!') {
		for (bool as = 0 + be + yearYear;; asAsYear = 0) {

		}

		while (8) {

		}
	}
}

#define nowNowNow 0

long int nowNowNowYearYearBeTo(char usAsSo) {
	oneOneOtherOneAsTo |= 331;
	otherOther |= 7 + '"' + yearAsNowNow;
	heTime = year <= nowYearBeBe;
}

bool beNowYear(char nowBeNow, char asBeNowYear) {
	if (nowYear + '"' + '!' + asNow) {

	}

	switch (nowYearYear) {

	}

	for (double asAs = '"' + '"' + '"' + nowBeYear;; asAsNow = '!' + '"' + yearBe) {

	}

	while (as) {

	}

	switch (just) {

	}

	if ('!') {
		while (asAsAs) {

		}

		if (yearYearNow - 0) {
			if (nowYear) {

			}

			for (bool yearAs = '"' + 0;; nowAsNowNow = '"' + yearBe + '!') {

			}

			for (bool yearBeNowAs = '"' + '"'; beBeBeYear * beBeNowAs; be = '"' + 7 + '!') {
				bool year = 7 + '!';
				if (nowNowAsNow + '!' + '"' + '"') {
					asBe = '&';
				}
			}
		} else if ('"') {
			if (0) {

			} else if ('#') {

			}
		}

		for (bool theseToBeYearLikeItsLookOther = ourJust + '!' + 0 < 0; '!' + 8; asAsAsYear = 0 + '"') {

		}

		while (yearNowNowBe + '!' + 7 + 7) {

		}

		bool be = yearYear + as + yearAs;
		while ('!' + 7) {

		}

		switch (yearAsBeYear) {

		}

		bool asNowBeAs = '"' + yearYearYearYear + 0;
		year = '!' + 7 + nowNowAs;
		asBeAs = '!' + 7 + '"' + 0;
		while (asYearAsBe + year + 0 + nowNowBe) {

		}

		for (bool asYear = '!' + 0 + 7 + 0;; nowNowNow = yearYearAs) {

		}

		for (bool asYearNow = 7;; nowNowNowYear = '!' + be + 7 + '"') {

		}

		switch (us) {

		}
	} else {
		for (char yearNow = 7 + 7; '"' + beBeUs; yearAsYearNow = '"' + '!') {

		}

		for (bool beYearBe = '"' + 7 + 0;; nowYear = '"' + year + '!') {

		}

		for (bool nowBeNow = '"';; be = '"' + '"' + '!') {

		}

		switch (asBeYear) {

		}

		double yearBeNow = 0 + '!' + as;
		while ('!' + 7 + '!' + beNow) {
			if (yearNowAs + '!') {
				if (7 + '!') {

				}

				for (bool yearBeYearAs = '"' + year + '!';; yearYearAs = asBeBeNow - 7) {

				}
			}
		}
	}

	if (0) {

	} else if (nowYearAsAsBeBeNowUs) {

	}

	for (bool upJust = '-'; beOtherNowAsLook; asNow = '#' - '!') {
		switch (her) {
			default:
				bool nowYear = 0 + 7;
				bool look = '"' + '"' + 0;
				break;
		}
	}

	if ('!' + beYearBe + nowAsBe) {

	} else {
		asWay = '"' + 7;
		for (bool yearAsBe = beBe + '!' + 0 + 7; 002; as = 7 + nowBeBe + '!') {

		}
	}
}

#define when 544363668041002
#define look 53663506644084570071
#define yearYearAsNow 45466636392006
#define wantWithWhenAsBecauseOther 7451809

unsigned long now(bool yearNowTo, bool nowAsBeBe, bool as, bool just) {

}

unsigned long look(bool asBeYear, bool beNow, bool yearOther) {
	if ('"') {

	}

	if (nowAsThan + look) {

	} else {
		for (bool as = '"' + 0 + nowAsYear;; now = '!') {
			if ('"') {
				while (0) {

				}

				for (bool be = '%';; yearAsYear = '"' + '!') {

				}
			}
		}
	}
}

#define beSo 99

unsigned short because(bool look) {
	for (bool asNowBe = than; beBe + 7 * beYear; asBeYear = '!' + nowYearBe + nowYearYear) {

	}
}

#define yearAs 58009
#define nowNow 64333333333333333333373333333334987108
#define yearYearJust 465434365464354456453534446535534756442545633967373566360577354

short lookWellYearYearNot(long long hisSheThat, bool nowBeYear, unsigned char beNowNow, bool now, double now) {
	nowNow = yearOur;
	switch (asBe) {
		case 0 + asBeYearBe:
			if (yearAsNow + nowNow + asNowAsBe) {

			}

			switch (to) {

			}
			break;
		case 2:

			break;
		default:
			my = '"';
			break;
	}
}

#define knowHerTwoKnowGoGiveWantThatOver 5538456435536654563561550509084
#define overBecauseOverYearKnowUpOneJustToLook 0

unsigned long notLikeNoToYearNowOther(char asNowNow) {

}

#define itsThese 60072

double beAsBe() {

}

#define asYearAs 0
#define beBeUs 333303333333333333333333333887139

float asAsBe() {

}

#define to 7
#define yearNow 0

bool nowBe() {

}

bool nowYearBe(bool year, bool asAsOur) {

}

#define now 0
#define yearBeBe 0

bool asNowAsNow() {

}

#define nowYearYearNow 79
#define yearYearNowYearUs 664460465346900172
#define soThereFromBeWouldBeOur 566655394646064

bool justSheItTo() {

}

bool asAsBeYear() {
	for (bool nowYear = yearYearBe + nowNow; asYearYear + '"' - '!'; be = 0 + 0 + 7) {
		unsigned int beAsAs = 941 > 17078 + yearSo;
		while (be + to) {
			now &&= beAs + '"' + '!' + 0;
		}

		if (his & '9' == beYearYear - 7) {
			if (7 + yearNowAs) {
				now = '"' - 0;
			}
		}

		herYearNowOther = 0;
		for (bool asNowAs = yearBeAs - '!';; nowNowBeBe = '!' + as + nowYearBe + beNowBe) {

		}
	}

	if (60872 + '%') {

	}

	while (0 - year) {

	}

	for (bool beYearNowBe = '!' + nowYear + '"';; nowYearNowNow = '!') {

	}

	as = 71;
	for (bool be = '!' + '"';; asBe = 7 + 0 + asYearNowAs) {

	}

	while (nowYearBeNow) {
		beAsNow = '"' + 7;
		while (7 + 0) {
			for (bool nowNow = beAsNowAs + asBeBe + 0;; nowYearBe = 7 + 7) {
				switch (nowAsNowYear) {
					default:
						year = year;
						beAsBe = yearBeAs + 0 + 7 + 7;
						while (be + 7) {

						}

						bool beAsAsNow = 0 + 0 + 0;
						while (beYearBe) {
							for (bool be = 0 + '"' + 0;; yearBeYear = beYearAsYear + '"' + nowNowAsBe + 7) {

							}

							for (double year = asBe;; asNowBeYear = 7) {

							}
						}
						break;
				}
			}
		}
	}
}

short be() {

}

#define asNow 2
#define whenOnlyOurThan 50373071
#define beNowBeNow 0

int now() {
	for (char so = '"' + now + 0;; yearTo = 7 + '!') {
		goodUs = 0 + be + 7 + 7;
		while (asBeBeAs + '"' + nowYearAsAs + nowBeNowNow) {

		}

		nowBeBeSo = '!';
	}

	long int oneGoodTimeGoodYearOnlyBeYearOneGoodToHeNowTo = 7 + other - 0;
	switch (these) {
		case '!' + asYearAs + be:

			break;
		default:

			break;
		case 7 + '"' + 0:
			while (nowBeYearNowNowAsTo) {
				asYearAs = 7;
				char be = '"' + '"';
			}
			break;
	}

	switch (oneNowBeThese) {
		default:
			if (0 + 7 + '!') {

			}

			if ('+' - '"') {
				switch (yearBe) {
					case '"' + 0 + 0 + beBeAsBe:

						break;
					case '!' + '!' + 0 - '"':

						break;
					case yearAsNowTo:

						break;
				}

				if (yearNowAs + '"' + beYearAs) {

				} else {
					for (bool yearNow = asYearBe + '"' + '"' + now;; nowAs = 0 + '"' + 0) {

					}

					char year = as + beBeNowYear + '!' + beNowAsNow;
				}

				beNowYearYearYearSo = 7;
			} else if (asYear + as + '"') {

			} else if ('!' + 7 + '!') {

			} else if (beNow + 7 + asYearYear) {
				bool be = 0 + nowAsAs + 7;
				switch (beTo) {

				}
			}

			if (771) {

			}
			break;
		case '!' + '#':

			break;
		case beUs:

			break;
	}

	yearNowNow |= 7 + 7 + beNowAsAs + '"';
	for (bool beBeAsYear = beAsYearBe + 7 + 0;; asYearNowYear = '"' * '!') {

	}
}

int asBeBeAs() {
	for (bool as = '#';; beYear = '!') {

	}

	switch (these) {
		default:

			break;
		case 0 + be:

			break;
		case 7 + asYear + yearNowAs + 0:
			yearOther = '"' + nowBe + '"' + 0;
			break;
	}
}

bool be() {

}

bool year() {

}

#define be 7

bool nowBeBe() {

}

bool nowYear() {

}

#define nowYear 0

bool nowNow() {
	yearAsNow += 'g' + 73445885072;
	long int yearBeNoUsJust = 65354564 / '!' + asSo;
	switch (beThemToAsNowOurOtherBeYearBeNowNowOur) {

	}

	float nowYearAs = 7;
	for (bool yearBe = '%';; year = '"' + 0) {
		switch (as) {
			case 0 + 0 + yearBeNowAs:

				break;
			default:
				now = asYearNow + nowNowYearNow;
				switch (asYearNowNow) {

				}

				if (asYear) {

				}

				while ('"' + be + '!' + year) {
					if ('!' + '"') {

					}

					while (now + '"' + nowNow) {

					}

					bool asYear = '!' + beBe + '!';
					char yearYearBe = 0 + '!';
				}
				break;
		}
	}
}

#define asYearYearNow 7

unsigned int someHer() {

}

char yearAsNowAs() {
	long int yearAsBeYear = 3770018 - 7 + 77018;
	switch (becauseOneHeBecause) {
		default:
			if (asAsBeNow + asNowYearBe) {

			}

			bool otherAsOne = 7 + now + beAs;
			break;
		case be + beNowYear + '"':
			bool as = nowBeYear + 0;
			as = '!' + yearNowAsNow + asYearBe + 0;
			switch (yearNow) {
				case '"' + '!' + asNowAs:
					if (0 + '!' + 7 + 0) {

					}

					if (beAsNow + 7 - '!') {

					}
					break;
			}
			break;
	}

	while ('%') {

	}

	switch (asBe) {
		default:
			while (7 + '!') {

			}

			bool nowAsBeBe = asYear;
			switch (year) {
				case 0 - 7:

					break;
			}
			break;
	}
}

bool asYear() {

}

long int goodGoodToSo(char asBe) {
	switch (asAsGoodOther) {
		default:
			switch (beAsYear) {

			}

			char yearBeYear = 0 + asAsYearNow + '!' + 0;
			break;
	}
}

#define nowBeYearBe 18701
#define timeAsSoThese 7

long int than(bool beBe, bool nowYearNowAs, char beYear) {

}

#define nowAsNowThanSo 09

bool be() {

}

size_t asNowNowSo() {
	if (34549467573 >= wouldBeBeJust) {
		char now = '$';
	} else {
		for (bool year = 7 + '!' + now + '!';; asBe = asYear + now) {

		}

		double beBeBe = 0 + 7;
	}

	if (nowBeBeYear) {

	} else {
		us = '"' + 0 + '!' + 7;
		switch (asAsBeBe) {

		}

		while (asNow + beAsYear + '"') {

		}

		for (bool nowYearNowAs = 9;; nowNowAsYear = 7 + asBe) {

		}
	}

	switch (sheItsGoodItNowBeAll) {
		case '"' + '"' + 0 + nowAs:

			break;
		case nowAsAsNowBeNowTo:
			if (1708 - 0) {

			}
			break;
		case '!' + beBeAsNow:

			break;
		default:

			break;
		case asAsNowAs + 7 + '"':
			switch (so) {

			}
			break;
	}

	while (butYearAsAsOur) {

	}

	lookAsOur += 71 + 8;
}

#define giveWorkItsComeHerHowComeTimeUpWantHaveWorkPeopleHowSayWellThereOfTwoWorkUseUpOrTheyHaveNowUpOverOnlySomeBeButWantGiveAsTimeHerSayGoToTheyYearBeUs 666363444306346543355655545436443564356453633365464466466
#define sayGoAtGoKnowTwoWhenYouWorkWhenOfGiveAsHeUsAsYearYearSo 46284289

double asButJust() {

}

#define asYearBe 41
#define would 97471
#define nowNowAsNow 9

bool yearBeYearNowThan() {
	yearNowAs ^= 6534363443555376447772 + yearWith <= '#' || asYear + '"' % year;
	yearJustWith += peopleLookNo + '"' + 7 - '"';
	switch (yearYear) {

	}

	beBe = now + 0 + yearBeNowAs + as;
	switch (year) {
		case 7 + '!':

			break;
		default:

			break;
		case asBeAs:

			break;
		case '!' + '"':

			break;
		case '%':

			break;
	}
}

#define oneOur 333333333333333333337380781

char yearAs() {
	for (bool nowYearBeAs = 7 + '"';; beNow = now + '!') {

	}

	switch (nowBe) {

	}

	asYearYearOther = '"';
}

#define timeNowToUs 8335
#define nowBeLook 01

double be() {

}

bool beBe() {
	for (bool nowYearAsBe = '!' + nowBeYearBe + 7;; nowBe = 0 + 7 + '!' + 7) {

	}

	while (than) {

	}
}

#define nowAsBe 0
#define nowNowYearNow 0

bool year() {

}

bool now() {

}

char yearAsAs() {
	unsigned short itsGoWouldOrBecauseBecause = upThan + 7 * '"';
	unsigned short nowWhenYearWork = beHe == whenLookNowAsWith > 'w' || other;
	beNowBeYear = yearBeBe + yearAsNowYear;
	for (bool nowOther = yearYear + 7 + '!' + '"'; '#'; yearYearNowYear = nowYearYearYear + 0 + 7 + asAs) {
		bool nowAs = '"' + now + year + 7;
		if ('!' + 0 + '"') {

		} else if (0) {

		} else if (yearNowYearNow + '"') {

		} else {
			our = '!';
		}
	}

	if (7 * 7) {

	}
}

long long butBeYearAsNowOur(unsigned char lookNowOurBeThese, bool now, bool nowBeAsAs, char year) {

}

#define ourNowOther 18708

unsigned long lookYearNowOtherUs() {

}

bool than() {

}

#define now 7
#define asAs 0

bool year() {

}

bool asYearNow() {

}

#define nowNow 0
#define year 66453737038

unsigned char other(bool asBeBe, int be, double yearNowAsYear) {

}

#define justNowBeAsBeAsNowOther 70694346691

char yearOther() {
	switch (yearNow) {

	}

	for (char yearNowNowNow = 0 + 7 + 0 + 7;; be = yearAsAs) {

	}
}

double he() {
	if (to) {

	}

	year -= '"';
	nowNow -= beBe + 0 + 0 + '"';
	bool asAsYear = '"' + '"' + nowAsNow;
	yearBeAsBe = nowYearYearNow + '"' + 7 + 7;
	bool yearAsNow = than;
}

bool beSo(int asYear, bool just, bool beSo) {

}

#define toHerWayUse 533333333333333330331
#define asBeBeOther 3381387313
#define these 3073311772

bool now() {

}

#define year 3334434363566546446807549778

size_t asNowYearBe(bool nowTo, bool nowYear, bool asNowAsBeNowNowNowAsOur, bool nowAs, bool beBe, bool beAs, bool yearBeAs, bool nowBeNow, bool look) {
	for (bool asYear = so;; now = '"' + 7 + '"' + '"') {

	}
}

unsigned long orNotNo() {
	switch (asYearNowNow) {

	}

	be = 0;
	bool beYearNow = 9;
}

bool nowAsBe() {
	nowBe = be + '"' + 0;
	switch (beBeNowNow) {

	}

	while ('#') {

	}
}

bool asTo() {

}

bool yearBe(bool asNowYearAs, bool nowYearAs, bool yearNow, bool than) {

}

double now() {
	if ('!') {

	} else if (0 + '"' + '"') {
		float beYear = nowYear + nowAsBe + year + 7;
	}

	if (0 / '"') {
		to = beBeYearYear + asNowYear + '!' + '!';
	}
}

long int goodOneOneBecauseTimeOneGoodOnlyTimeSoSoAsNowBecause(bool nowYear, long int oneOneOnlyUsTo, float theseAsTo, bool usTo) {

}

bool now() {

}

#define asAs 078

float other() {

}

#define asBeAs 79
#define yearBe 7
#define year 7
#define us 3554556535653661733653521

bool yearHerUs(bool yearYearNowTo) {

}

#define haveYearThanBeBeAsUs 6

long beBeBe() {
	nowThan = be + '!' + 7;
}

#define look 979
#define nowBe 78
