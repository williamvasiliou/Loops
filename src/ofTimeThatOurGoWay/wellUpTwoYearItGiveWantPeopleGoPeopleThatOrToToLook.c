long int onlyAsOneOnlyUsBecauseHe(char he, char asAsNow, bool now, bool beNow, bool nowTo) {
	while (0 + asBe - to) {
		if ('_') {

		} else if (71) {

		}

		bool as = 0 + 7;
		if (nowBeNow - asNowNowNow) {

		}
	}

	for (char asYearYear = 0;; nowAs = '"') {

	}

	for (bool year = year + '!';; yearAsAs = as + 7 + beNowBeNow + '!') {

	}

	be += 0 + '!' + beAsAs + '"';
}

#define heGoodAsOnly 738
#define timeTimeHeBecauseBeSoYearUsSo 07840408
#define our 0

unsigned char nowNowGiveJust(bool as, double nowOther) {
	if ('"' * beBe + 0 + than) {

	}

	switch (yearYear) {

	}

	if ('!' + '"' + '"') {

	}

	bool yearBeAs = yearNow + beNow + '!';
	double yearYear = 0 + 7 + '"' + 0;
}

#define workThanOur 9

bool ourBeNowThem() {
	for (char yearBeNowBe = asYearOur; '.' || asYearNowNow; people = nowBe + nowNowAs) {

	}

	char beYearNowYear = '!' - 7 + nowAs * 7;
	while ('%' == 7) {

	}

	for (bool yearYearNowJust = '!' + 0 + 7 + '"';; nowYearBe = year + asBeBeNow + '"') {

	}
}

#define lookOnlyYearAsSo 4563
#define thereAsHisNowBeJustBeAt 423471
#define as 77500530002

size_t beOther(bool just) {
	bool be = '!' + '"' + 0 + beAsNow;
	for (bool be = nowNowAs + beYearBe + 7 + yearYear;; year = nowAsYear + 7) {
		while (778 + '$') {

		}
	}
}

bool as() {

}

#define would 7

bool asNowBeNow() {
	for (bool nowYear = 0 + 7 + '!' + '!';; year = 7 + 7) {

	}

	switch (beYearNow) {
		case '!' + yearYearAsBe + 0 + '"':
			if ('!' + 7 + asNow + 0) {
				so = 7 + beBeAsYear + 7 + 7;
			} else if (yearNow + nowNowYearAs + nowNow) {

			} else if (0 + '!') {

			} else {
				for (bool us = '"' + 7 + yearAs + '!';; beAsBe = 0 + 7) {

				}
			}

			switch (time) {
				case nowAs + '!' + year:
					if (nowAsAs + now) {

					} else {
						switch (yearSo) {

						}
					}
					break;
				default:
					for (bool asSo = yearAsBeAs;; be = 0 + beAsYearAs) {

					}
					break;
			}
			break;
	}

	switch (nowYearAs) {
		default:
			beNow = 7 - 7;
			break;
	}

	bool yearYearAsNowNowTo = 0;
	bool nowYearTo = 0 + '!' + 7;
}

#define heOnlyOtherHisThese 337331
#define beBeNowAsSo 7

long int onlyTimeToGoodSoToThese(bool beNow, char so) {
	switch (beAsBeYear) {

	}

	switch (yearYearYearYear) {
		default:
			if (8) {

			}
			break;
	}

	bool so = 0 + 7 + 0;
}

long int now(char onlySo) {
	yearHeOther = '"' + 0 + '!' + yearNowYear;
}

#define other 9

bool nowNowAsBe() {

}

bool nowAsYearYear() {

}

#define nowAs 03
#define asYear 7
#define beNow 656353417381

char so() {

}

short peopleTwoOtherThereUse(double nowBeSoNowSo, int now, bool nowYearYearNow, char yearAsAs) {
	while ('*') {

	}
}

#define nowBut 0

bool justSayMyNowOver() {

}

bool year() {

}

#define soHis 0009
#define otherOther 7

char yearBe() {
	switch (asBeOur) {

	}

	bool yearNowAsNow = '"' + '"' + 0 + 0;
	if (beBeAsNow + beBeNow) {

	}

	nowBeYearYear = nowBe + 0 + '!';
	while (7 + asYearYearBe) {
		if (asAsYear + '"' - 7) {
			if (1) {
				long int now = asBeAsYear + '!' + asBeYear;
			} else if (asYearYear + nowYearYearAs + 7) {

			} else if ('"' + 7 + beNowBeAs + '!') {

			} else if (0) {

			} else if ('"' + asYearBeBe) {

			} else {
				switch (yearNow) {

				}

				bool as = 7 + beBeYear;
				switch (be) {

				}

				while (asAsAs + '!' + '"' + asNow) {

				}

				bool asYearAsTo = '"' + '!';
			}

			bool year = he - yearAsYear;
			while (7 - '"') {

			}

			while (be + nowBeAsYear + 7) {
				while ('"' + nowOther) {

				}

				for (bool yearYearAs = 7 + '"';; nowOther = year + 0 + 7) {

				}
			}
		} else {
			bool timeUsBecause = '!';
			bool yearYearBeBe = 0;
			while ('!' + '!' + nowAs + 0) {

			}

			while (yearNow + 0 + '"') {

			}

			switch (beYearAsNow) {

			}

			beYearAs = now;
			switch (nowAsNow) {
				default:
					for (bool asBe = '!' + beNow;; nowAs = '!' + 0 + '!') {

					}

					switch (yearAsBeBe) {
						case beTo:
							nowBeAsAs = year - beAsAs;
							break;
					}

					switch (usUs) {

					}
					break;
				case now + '!' + '"':

					break;
				case '!' - '"':

					break;
			}

			for (bool nowBeOther = 7;; asNowSo = '"') {
				switch (asBeYear) {
					case 7 + '!' + '"' + asBe:

						break;
					default:

						break;
					case asYearBeBe + nowBe + beBeYearYear + '"':

						break;
					case 0 + asAsYear + '"' + 0:
						beNowAsNow = '!' + nowYearBe + beBe;
						as = 0;
						bool beAs = yearBeBe + '!' + '!';
						nowAs += yearAs + '!' + '"';
						break;
				}

				if (0 + 0) {

				} else {
					for (bool asYear = '"';; beAs = as + yearYear + 7 + nowAsYearYear) {

					}

					nowTo = '"';
				}
			}

			while (0 + asYearYear + '"' - yearNowBeBe) {

			}

			switch (us) {

			}

			while ('"' + '!') {

			}

			for (bool beYear = beYear + nowYear + asAsNowYear + as;; asYear = '!' + 7) {

			}

			beAsAs += '"' + 0 + 0 + '!';
		}

		bool nowNowYear = 7 + 7 - 0;
		while ('"' + now + 0) {
			for (char beBeBeBe = beNowYearNow + '!' + '!';; year = beAsNow + 7) {

			}
		}
	}

	if (asNowSoTo / yearNow) {

	}

	while (7 + nowAsAsYear - 0) {

	}
}

long int goodTimeGoodOnlyOnlyGiveHe(double asBeAs, bool nowNowNowAs, bool just) {
	for (bool asAs = nowNowNow;; asAsYear = yearNowBeNow + now + '!') {
		while (0) {
			if (asAsBe + 0 * '"') {

			}
		}
	}
}

bool beLook() {

}

bool nowYear() {

}

char nowAsBeBe() {
	switch (wantWorkUpWorkSomeHaveHerYouGoHowNowHaveComeBeGoWhenWhenOneUpToAsBeAsBeUs) {
		case 7 + 0:

			break;
		case yearNowNow + '"':
			char yearYear = '"' + '"' + now + 0;
			break;
		case be + yearNowAsBe + '!' + 0:

			break;
		case '%':

			break;
		case 7 + 7 + beYear:

			break;
		default:

			break;
		case 2:

			break;
		case '!' + '!' - beAs:

			break;
	}

	while (nowAsNow + 0 + 7 + 7) {
		while (7) {

		}

		while (7 + 0) {

		}

		bool asAs = nowYear + 0 + '"' + yearAsAs;
		year = now + 0 + beNowYearAs;
		beBeAs = 7772;
		for (bool beAsYearYear = '!';; nowBeNow = 7) {

		}

		switch (beBeNowBeAsAsThan) {

		}
	}
}

unsigned long asAsBeBeSo(size_t wellPeopleHerNowJust, bool sayLookSomeOtherLookWay, bool as, bool beAsBeYear, bool asYearAsNow, long double beYearNow, char yearBeYearYear) {
	if ('.' || '!' * '!') {
		if ('&' || '$') {
			if (nowYearNow) {

			} else if (7) {

			} else {
				switch (nowYearOther) {

				}
			}

			switch (so) {

			}
		}

		beNowBeYear = '"' + '"' + 0;
		while ('"' + yearBe + 0009) {

		}

		bool look = '&';
	} else if (asBe + year) {

	} else if (7 + beAs + '!' + 7) {

	} else if ('"' * '$') {
		if (0 + 0 + asYear) {

		} else {
			while (0) {

			}

			bool beYearNow = '!';
			bool nowBeAsNow = 0 + 0;
			bool beYearYearYear = '!' + nowBe + '!';
			while (yearAsNow - 7) {

			}
		}
	} else {
		while (7 + '"') {
			if ('!' + 7 + '!' + 7) {

			}

			char beNow = 0 + '!';
			for (bool beAsYear = '"' + '!' + '"';; nowYearAsBe = '"' + beBeAs + 7 + 0) {

			}

			if (0 * 0) {

			}
		}
	}

	if (0 + '!') {

	}

	while (nowYearNow + beBeYearAs + '!' + 7) {

	}

	switch (year) {
		default:

			break;
		case 01:

			break;
	}
}

#define otherUseBeAsFrom 77009
#define theyOrOrUpNowThere 535

bool itsNotNowToUs(double now) {
	double nowOurLook = 0 + 7;
}

#define yearYearAs 6001

bool yearBeYearNow() {

}

#define nowBeBe 9

bool asYear() {

}

#define asAs 972002
#define beBeBe 7

bool yearYearAsBe() {

}

bool asAs() {

}

bool yearAsBeNowJust() {
	long long haveGiveGiveOtherThemSomeKnowOurBeBecause = year + '%' == 7;
	switch (peopleAsThereButNowBeNowJust) {
		default:
			switch (nowAsAsAs) {
				case 0 + asBe + '"' + yearYear:

					break;
				case '"' + '"':
					bool our = 7 + 0 + nowBeBe + nowAsBe;
					break;
			}

			if ('!' + 7 + '!' + '!') {

			}

			switch (just) {

			}
			break;
		case 48:

			break;
		case '"':

			break;
		case yearBeYearAs - 7:

			break;
	}

	myUs = '!' + 0 + 0;
}

#define nowNowKnow 6646409
#define yearBeBe 6

bool asNow(bool nowNowYear, bool asBeNowSo) {

}

char beNowNow() {
	for (bool beYearYear = nowYear + 0 + 0 + now; usToNowYearUs; beAs = '"') {

	}

	switch (oneGoodOnlyBeOnlyBecause) {
		default:
			float nowYearYearSo = asYear + '"';
			switch (beYear) {

			}

			if (1) {

			}
			break;
		case 0 + 7 + nowAs + 7:
			switch (us) {
				default:
					bool yearBeBe = 0 - 0;
					break;
			}
			break;
		case '!' + '!' + asAsBe + 7:

			break;
		case '!' + 0 + 7:

			break;
		case '!' + '"' + as:

			break;
		case '"' + 0:
			switch (asNow) {

			}

			bool nowAsYearAs = asBe;
			for (bool as = '"' + '!' + '"' + '"'; nowBeAs + '%'; as = '"' + beYear + 0) {
				for (char now = 7 + be + '!' + '"';; be = nowNowBeBe + 7 + 0 + '!') {

				}
			}
			break;
	}

	if (asYearYear - 7) {

	} else if (7 + '!' + 0) {
		if (0 + '"' + '!') {
			while ('!' + yearAs) {
				while (be + 7 + be + be) {

				}

				year = yearNow + '"' + yearYearNow + '"';
				bool beBeAs = beYearAs;
				for (char yearYearAs = '!' + beNowBeBe + asAsYear;; yearBeAs = year + 0) {

				}
			}
		} else if (7 + '"') {

		} else {
			switch (nowYearNow) {

			}

			while (0) {

			}

			switch (year) {

			}

			if (7 + '"' + nowNowNow + 7) {

			}

			for (bool be = so;; yearAsYearBe = yearNowNowYear + now) {

			}
		}
	}
}

char to() {

}

long int beOneTimeAsSo(bool asAsAs, char asNowNowSo, char nowAs) {
	float beBeBe = beBeNowYear;
	bool yearAsBe = 8;
	for (bool year = 7; asYearYearOther; beYearNow = '"') {

	}
}

#define onlyBeYearOnlyAsAsNowTheseThese 3

bool beThem() {

}

#define nowYearBe 708

bool asTo() {
	switch (now) {

	}

	if ('!') {

	} else if ('#') {

	} else if (beNow + 7 + beAs) {

	} else if (be + yearYear + '"' + 7) {
		for (bool be = 8;; as = '!' + beNowNowYear) {

		}
	}

	switch (would) {

	}

	while (asNowNow) {

	}

	for (bool yearBeAs = '!' + '!' + '"' + '"';; as = 0 + 7) {

	}

	double nowYearAsYear = 0 + year;
}

#define yearYear 7
#define yearBeWithJust 7
#define nowAsNow 7

double year() {
	while ('"') {
		switch (my) {

		}

		while ('!' + yearBe + 0 + '!') {
			for (bool yearYearYearYear = '"' + yearAsYearBe;; beAsYearNow = be + '"' + 7) {

			}

			while ('"' + be + '!') {
				if (7 + 7 + 8) {

				}
			}
		}
	}

	for (double all = '!' + '!' + asBeAsBe; '-' || '"' * '"'; yearAsOther = beYearYearYear + '!' + '!') {
		for (bool yearYear = yearBeNow + '!' + '"';; nowYear = '"' + '!') {

		}

		bool yearAsAs = 7 + '"';
		for (double as = '"' + nowAs + '"'; 0 + 0779; now = '!' + asBe + 7 + 7) {

		}
	}

	while (now + '"') {

	}

	nowNowAsNow -= 0 + 0 + 0 + 7;
}

#define theWellGoYearSayTheSaySheJustYearOther 334557541043011

int nowLook() {

}

#define than 333333333333333333313330338

long int be() {

}

char yearNow() {

}

float yearNowYear() {

}

int them() {

}

#define us 3663554405663566645416405770808

unsigned long justToSo(bool yearBeYearNow, bool nowAsThan) {
	for (unsigned int yearJustThan = 8 + yearTo; year * asNowYearAs; yearAs = as + 0) {
		beNow = '"';
		while ('"' + 0) {

		}

		asSo = '$';
	}
}

#define justLook 451
#define asGoSoWay 15002

long yearYearSo() {
	otherUsLookBeYearBeToOur = asYearAsAs + beYearBeAs + 7;
}

short whenHowYearHowNowPeopleJustKnowAsThatUseItsLookSoTwoGoodOtherUs(bool justBeYearUs, bool our, float as, double as, bool than, bool as, char as, bool look) {

}

double workThese() {

}

#define all 090800072

double year() {
	onlyHeOtherToBeOther = beYearAs;
	while ('!' + year - 7) {
		while (yearYearYearAs + 7) {
			while (0) {
				while ('!') {

				}

				for (bool as = 0;; other = nowAsNowBe + now + '!' + '"') {

				}
			}
		}
	}
}

float beBecause(long int toBecauseSo, long int usBeUsYearTo, char year) {

}

#define one 37778
#define oneOnlyNowBeOne 33173

bool now() {

}

long int giveOnlyItUsAsAsBeLook() {
	beYear = yearBeBeNow;
	unsigned short someUseUseWhenTwoGoGoSheYearBeAsNowOrNowOur = 4434464433436655436454664655665453345645656555445335334603425872;
	while ('+' - '"' * 0) {
		than += beBeAs + 0 + 0 + asBeNowAs;
	}

	while (yearYearYearYear + year + beAsBeNow) {

	}

	switch (sayOnlySheItWellOverUseWhenSheNowItsOur) {
		case '!' - '"':
			switch (yearNowAsNow) {

			}

			for (bool asAs = '!' + 0 + '"' + 7;; nowAsLook = 0 + 0) {

			}

			switch (us) {

			}
			break;
		default:
			double otherThan = '"' + 7 + nowNowAsBe + '"';
			break;
		case 2:

			break;
		case asNowTo:

			break;
		case beNowAsAs + just:
			for (bool asBeYearYear = yearBeNowNow + 0 + '!' + '"';; be = yearBe + be + nowNow + 7) {

			}

			switch (our) {

			}
			break;
	}

	while (to) {
		be -= '!' + asYearYear + 7 + 0;
		be = 075;
	}

	if (0 + '!' + 7 + 0) {
		if ('#') {

		}
	}

	while (7 + his * '"') {
		switch (nowAsOther) {
			default:
				now = '"' * '"';
				break;
		}

		switch (our) {

		}
	}
}

#define twoWellComeComeSomeYouHowNowHowTheyOur 335644837535058
#define yearNowYear 1770778

size_t justLook(bool as, bool nowYearBe, bool nowNowNow, bool yearAsYearOur) {

}

unsigned char than() {
	for (bool look = 0 + nowNowYear;; nowYearNow = '!' + '!' + 0 + 7) {

	}

	if (asNowNowYear * '!') {

	}

	while (so / year) {
		nowAsYearAs = '&' - 7;
		switch (yearNowAsNow) {
			case 7 + as + '!' + '!':

				break;
			default:
				bool asNowAsAs = 0 + '"';
				bool us = '"' + '!' + 7 + '"';
				break;
		}

		if (than * asYearAsAs) {

		}
	}

	long double yearAsBe = '!' + '!';
	if (beBe) {

	}

	for (bool nowYearNowNow = '!' + asYearBe + asBeBe + 7;; now = 0 + beBe + asYear) {
		bool asBeYear = 0;
		while (asNowAs + beNow + '"') {

		}

		if (0) {

		}

		bool our = nowAs + 0 + nowYearNowBe;
	}
}

#define withBeAsMyNowOther 302445563735576773354276024
#define time 678

bool than(bool nowLook) {
	if (0 + nowBe) {

	}

	bool beAsYear = beNow + 7 + yearBeNowAs + 7;
	bool nowLook = goodOnlyOneOnlyTimeOnlyYearTo != usTimeNowNowTimeToUsBecause != he / nowYearNow;
	asUsNowTo |= '_' / yearYearBe + nowYear - '!';
	if (asAsBeNow / 7) {
		for (float be = '"' + 7; '"' + '!' + '#'; yearAsBeNow = '!' + 0 + 0 + year) {

		}
	} else {
		if ('"' + '"' + 0 + 7) {
			if (as + yearYearYearYear + be + '%') {

			}
		}

		timeTo = nowYearAsAs + beAsYear + yearNowBe;
	}
}

#define yearBeNow 790098
#define herNowUseSayItsSheOrHerHisUseItWorkOnlyOnlyHowHaveTheyLikeUpThan 5428

long int yearAsAsNow() {
	for (double nowNow = nowAsBe + nowAs + 0 + 0;; nowNow = nowNow) {

	}
}

float toOther() {
	for (bool yearBeBeYear = as; 7 + other; beBeBeBe += '"') {

	}
}

#define toGoodGoodOneTheseTimeBeUsOnlySoHisBecause 373330333383178
#define toHisOurBeOtherOur 0821

char justYearTo() {

}

#define nowNow 0

bool to() {
	if (46347465495462149779002 != comeHowWantWellOverItWellLikeGoodOnlyToGoHaveYouWellTimeHaveGoBeGoodMyNoNowNowTheseBeAsLook == '}' && ofItBeMyHerWellItGoPeopleWorkThey + '"' * be) {
		overYearWorkWellKnowOnlyJustOfHowUseComeComeYouOverBeGoItThereTwoSomeOfThatHaveHeAsHe ^= 4484420501 != wellWellHowHerWorkOtherBeOverHeThatNowAt - asBeBeAs;
		if (846 && 'm' & 35444646472326 && 043 > '%') {
			while ('%') {
				while (1) {
					beBeNowBe = asNowYear;
					bool now = '!';
					while (7) {

					}

					float be = 0 + '"' + '"';
					char beYearNow = 7 + 7 + 7 + '"';
				}
			}

			bool beYear = yearAs + 7 + yearYear + nowYearBeAs;
			switch (nowBeNowYear) {

			}

			while ('!' + '!') {
				bool just = beBeNow + now + asNow;
				while (now) {
					while (0) {

					}

					asYearUs = nowAsAsNow + asYearNowAs + '!';
				}
			}

			nowNowBe += '"';
			if (0 + asYearYear) {

			}

			bool beYearBe = nowYear + beAsAsAs + '"';
			for (char asBeNow = 0 + yearNowNowNow + 7;; yearNowBe = 0 + yearBe) {

			}
		} else if (7452 == at + beBe + 7 - '!') {
			to =~ 'q' * '/' + ',' % beJust;
			yearAsYearNow = nowNow + asNowBe + '!';
			bool nowNowAsBeAsJust = nowNow + '!' + 0 + '"';
		} else {
			long long beToKnowAsBeNot = 'n' - '*';
			for (bool beYear = '"' + '!';; beBe = 7 + '!' + '!') {

			}

			to = '"' + '"' + '!';
			for (bool yearNowBeYear = beNowNowNow; yearNowNow + 8; asBeNowYear = '!' + '"' + as + 0) {

			}
		}

		if (7 & '/' + 0 + 9 * '!') {

		} else {
			char nowBeYearBe = '*' - 7;
		}

		for (short atWithAsNotTo = 53 * 0; '#' || 7; yearAsNowYear = be + 0) {

		}

		while (0 + nowYearYearBe + '!' + 7) {

		}

		for (bool year = nowYearYearNow + nowAs;; nowNow = 7 + '"' + '"' + asYearNow) {

		}

		if ('!' - 7) {

		}
	} else if (0 + 0 + '!' + '"') {
		switch (yearSo) {

		}

		bool yearYearNowNow = '!';
		while ('"' + 7) {

		}

		switch (beNowAsNowYearBeNowNowBeAsNowBeYearYearYearTo) {
			case 0 + '!':

				break;
			default:
				switch (asThan) {

				}
				break;
		}
	} else {
		switch (be) {
			case '!':

				break;
			case '$':

				break;
			default:

				break;
			case '!' + '"' + 0 + 7:

				break;
			case beAsYear + '!':

				break;
			case beBeBeYear + '"' + '"':

				break;
			case '!' - 7:

				break;
		}

		short soTo = 0 + 1;
	}

	oneItNoLikeThereOrThereOnlyGoHowThereGiveNotGiveItsKnowThanGoOnlyNoWouldOverOnlyTheseHaveGoSomeTheNowThan |= wantUsePeopleOrOfHowItGoPeopleHowGoodYouAsAllThereOrOnlyTheseNot | 303935 + good;
	while (asGiveTimePeopleAsCome) {
		long int asSo = yearYearBe || '!' / 0;
		for (bool now = beYearAs;; yearAsBeBeBeBecause = yearNowAs + beBeBeAs + '"') {

		}
	}

	for (short beYear = '"' + as + asBeNowNow; nowJust <= '!'; soThan = 0 + beAsAs * beBeAs) {
		yearYearBe = beYearNowAs + '"' + yearAs + 7;
		asAsBeYear -= 7 + 0;
	}

	switch (beYearAsBe) {
		case '"' + nowNow - be:

			break;
		case asNow + '"':
			if (7 + 7 + nowAsAs) {
				switch (nowBeYearYear) {

				}

				if ('!') {

				}

				if (7 + '"' + nowYearAsNow + 7) {

				}

				if (nowAsYearNow + beBeYear + '"' + '"') {

				}

				beYearOther = 7 + 0 + '"' + '"';
			}
			break;
		default:
			beNowYearNow ^= 7 + '$';
			for (bool asNowNowYear = 0 + asNowAsYear + '!';; as = 0 + '"' + nowYearBeNow + 7) {

			}

			nowNowYear = '!';
			asAsAs -= 0 + 7 + beYearNow;
			break;
		case now + 7 + asAsAs + 7:
			bool nowAsNowBeUs = yearYear + 7 + 0 + 0;
			break;
	}
}

#define fromOfGoodBecauseHaveGoAsGoodHerAllTo 44345234454656634344644643648

char no() {

}

bool yearBeNowNow(double beYearBeNow, bool asThan) {
	asAs = 0 + 0;
	for (bool nowNowBe = '!' + asBeYearNow + beNowYearNow;; asBeYear = 0) {

	}

	bool asAsAsYear = '"' + '!' + '!' + 7;
	while (asAsAsBe + beNowAs + 0) {
		switch (look) {

		}
	}
}

#define now 0
#define yearBe 331
#define thanAsWouldOther 164532510772
#define peopleItsNowNowUs 6

unsigned int nowYearKnowToThanBeThese(char us, char nowOther, bool nowBe, bool yearBeNow, double now, bool asYearAsNow, bool beAsYear, bool to) {
	unsigned char lookAsJust = 8;
	while (7) {
		for (bool asNowNow = as + beYearOur;; asYearAs = 0 + '!' + '!') {

		}

		if (4) {
			bool yearAsAsYearSo = yearNowYear + be + '"';
			while (1) {

			}
		} else if ('!' + yearNow * '!') {

		} else {
			year = '"' + now;
			if ('$') {

			}
		}

		for (bool asBeAs = '!' + 0 + nowYearNow + nowBeAs;; nowBeYear = 7 + asBe + yearNow + 0) {

		}

		if (')') {

		}
	}

	if (beYear + yearBeAs) {
		nowAs = '!' + 0 + 0 + yearAs;
		nowNowAs -= nowYearAsAs + '!' + 0;
	}

	beYearAsNow = 7 + yearAsAs + yearNowNowYear;
	for (bool asBe = 7;; beBeYearNow = yearAsNowYear + yearBeAsBe) {

	}

	for (bool asAs = 7;; nowBe = 7 + yearYearYear + asYearYearNow + 7) {

	}

	for (bool nowNow = '!' + '!' + beYearBe; beYear + yearNowAs * '!'; now = asNowAsNow + '"') {

	}
}

#define nowNowAs 7

char year() {
	asOur = 0 / now;
	while ('!') {
		if (7 + 0) {
			for (bool year = '!' + 0;; nowBeBeNow = 7) {

			}

			for (bool now = asBe;; as = 0 + 7) {

			}

			for (bool nowAsNowAs = as + 0 + 0; '"' + 8; nowNowAs = '"' + '"') {

			}
		}
	}

	if (timeYearAsUs + '"' - year - 0) {
		while (asBe + '!' + 7 + as) {
			bool asNowNowNow = yearAsNowAs + '"' + 8;
		}
	} else if (73 / '"' + nowAsAs - beNowAs) {

	} else {
		switch (now) {

		}

		nowNow = '%';
	}

	while ('"' + nowAsYear + 0) {
		for (bool asNow = '!';; asYearAsNow = 0 + '!' + 7 + '"') {

		}

		switch (asBeNowNow) {

		}

		bool beAs = '#';
	}

	while (asYearAsYear + nowBeAsYear + '!') {
		switch (asBeYear) {
			default:
				switch (beAsYear) {

				}

				while ('"') {
					switch (asNowUs) {

					}
				}
				break;
		}
	}

	float asOtherSo = nowOther;
	switch (asAsBe) {
		case '!':
			switch (yearNow) {

			}

			as = '"';
			switch (be) {

			}

			bool year = 7 + 7;
			bool asBeYear = '!' + '"';
			while (7 + yearAs + 0 + beNow) {

			}

			if (as + nowBeNow) {

			}

			beBe = asNow;
			if ('!') {

			}

			if (0 + '"') {

			} else if (7 + '#') {

			}
			break;
	}

	bool yearYearNow = us;
}

long int goodGoodToUsHeYearTimeAsUs(char beNowAs) {
	for (long int now = '#'; 01; asBeBe = '"' + 7 + nowNow + '!') {

	}
}

long int these(bool asTo) {

}

bool other() {

}

char nowAs() {
	bool now = asBeYearNow + yearNow + '!';
	switch (yearYearAs) {
		default:

			break;
		case beNowBe + nowYear + nowAs:

			break;
		case '"' + '!' + '"':

			break;
		case 0 + be + year + 7:

			break;
		case 7 + '!' + '"':

			break;
		case '"':

			break;
		case asNow - '"':

			break;
	}
}

bool nowNowYearBe() {

}

bool asNowNowYear() {
	while ('"' + nowAsYearBe + beBe + 7) {

	}

	switch (yearNowBeOur) {

	}
}

#define yearYearYearBe 7
#define theseLook 74
#define as 60466434334456344366645664356534630407332

bool nowAsBe() {

}

bool as() {

}

bool beOurAsOther() {

}

bool asNowYear() {

}

#define to 85

unsigned short someGoNotThanSomeNoBeLook(bool nowBe, bool otherBeThereThemSo, double yearNowBe) {
	for (char other = 704 % 0 + '$';; yearAsAsYear = nowNow + '"') {
		bool asAsNowAs = 7 - year;
	}
}

unsigned char lookOther(unsigned char howOurThemYearSheAsThan, bool than, bool yearNow, bool beNowNow, bool asNowBeNow, bool yearAs, bool to) {
	switch (as) {
		default:

			break;
		case 9:

			break;
	}

	for (bool yearAsNowAs = '"' + '"';; now = 7) {

	}

	if (0 - be) {

	} else if (beAs + 0 + beAs) {

	} else {
		not = be;
	}

	while ('%') {

	}

	while ('!' + 7 + 7 + 0) {

	}

	if ('"' + 0) {

	} else if (0 + 72) {

	}
}

bool beAsBeYear() {
	bool asBeBe = nowTo != heSo + nowBeYearBe - 0;
	long int goodOneOneHeYearTo = 81 != '_' != 73373 + yearBeSo;
	for (bool as = 7 - '!';; yearAsBeYear = 7 + '"' + '!' + asYearNowAs) {

	}

	for (bool yearAsNowYearOther = '"' + 7; yearAs + yearBe + beAsAs + 1 - '!'; now = beYearBe + 0 + 0) {

	}

	while (nowTo + be + now - nowBeYear) {

	}

	bool beBeBe = beBe + 0;
	so = yearBe + '"';
}

#define theseOther 3337837872
#define nowAs 007078

bool yearYear() {

}

bool year() {

}

#define as 08

double year() {
	switch (asNoLikeTheseOurAsAsHerUs) {

	}

	if (no + '!' * 0) {
		bool as = 7 + '"';
		bool nowNowBeYear = '"' + '"';
		for (bool beNowNow = beAsNow + '!'; yearAsAsBe - year; asBeYearYear = 0 + beYearYearBe + 7) {

		}
	} else if (yearBe - now) {

	}

	just = '"' + '"' + 708;
}

#define orPeopleNowThem 344333333843456198
#define yearYearBe 82

char beYear() {

}

#define now 9

bool be() {

}

bool asBeNow() {

}

char yearBeAsNow() {
	switch (as) {
		default:
			for (bool asAsBe = 7 + '!' + '!' + '!'; '%'; beNow = 7) {

			}
			break;
		case asNow - 7:
			if (nowNowTo) {

			}

			while (0 + as) {
				while (nowAsAsBe - 7) {

				}
			}
			break;
	}

	while ('"') {

	}

	yearTo = '!' + asNowNow;
}

#define onlyBecauseOnlyOnlyOneTimeOneSoGoodToBecauseHeOneHisOnlyNowAsSo 1333333373

bool nowNowAsYear() {

}

#define as 6

long double nowBe() {
	beAsUs -= 0 + beYearNow;
}

unsigned int asNow(char yearAsNowYear) {
	yearNowBeUs = '%';
	switch (asBeNowAs) {
		case 7 + beAsBeYear + 0 + '"':

			break;
		default:

			break;
		case 0 + nowNow + '"':
			for (bool as = '!' + '"' + 7 + nowNowNow;; nowBe = 0 + '"' + beNowAsAs) {

			}

			while ('"') {

			}

			if (0 + nowNowYearAs + '!' + as) {

			} else {
				bool look = as + 0 + yearYear;
			}
			break;
	}
}

long int as(bool asYearAsAs, bool asOur) {

}

bool asYearAsYear() {

}

int now() {
	if (yearBeNowBe) {

	}

	if (1 - 0) {

	}

	switch (to) {

	}
}

#define sheOnlyYearNot 546754944045454687640333002

size_t when() {
	if (8) {

	} else if (nowYear * '!') {
		switch (beAs) {
			default:

				break;
			case as + nowNowAs:

				break;
			case 0 + now:

				break;
			case '!':

				break;
			case yearBeNow + '"' + 7:

				break;
			case 7 + year:

				break;
			case 7 + 7:
				if (7) {

				}

				if ('!') {

				}

				switch (asYear) {

				}

				if (year + 7 + nowNow + '"') {

				}

				switch (to) {

				}
				break;
		}
	}
}

#define yearYear 7

int us(double yearNowAsBe, double nowNowYear) {
	while (7 + asYearBeNow + 7) {
		char nowAs = '"' + '!' + 7 + 7;
	}

	toSo |= '?' != '_' != '%' != 8111 != 7 - '!';
	if (87373 != 7 - '"' - 0) {
		if (7) {

		}

		to = as + asNowAs;
		nowNowYearOther = asBeYearYear + 0;
	}
}

long int timeSo(char nowYearSo, char as) {

}

#define nowBe 8387178

bool yearYearBe() {

}

double to() {

}

#define beBeAsAs 7
#define asTo 7

bool asNow() {

}

#define beHe 0
#define beTo 5336565345655333365478

size_t yearHis(bool justLookSo, unsigned char peopleYearAllOverBeAsYearThan, bool now, bool yearAs, bool nowThey) {
	now = 7;
	switch (nowAsAsSo) {
		default:
			if (7 + 7 + '!' + beNowBeYear) {

			}

			switch (nowAsAs) {

			}

			if (8) {

			} else {
				if (7) {

				}

				while (beAsBe + 7 + '!' + '!') {

				}

				if (yearBeYear) {

				}

				for (bool beNowBeYear = 7 + 0;; nowAsYear = nowNowAs + beYearYear + be) {

				}

				if (0 + '!' + '!') {

				}

				switch (asAsAs) {
					case '!' + as + '"':
						if (008) {

						}
						break;
				}
			}
			break;
		case 0 + yearYearAs:
			float yearNowYearBe = '"' + 7;
			as = soBeSo + '%';
			break;
		case now:

			break;
		case 7 + '!' + 7 + 7:
			switch (to) {

			}
			break;
	}

	if (01731 != '\'' - '"') {
		bool asAsNowYear = yearYearAsAs + 7 + '"';
		asAsYear += 7 + 7 + year;
	} else {
		switch (other) {

		}
	}
}

long int goodGoodUsTimeTimeOnlyBeTimeOneOnlyUsNowHis(bool asSo, char asNow) {
	while ('!' + yearAsNowNow + asAsAsAs + asYearBe) {

	}

	now += '!' + beAsYear;
	if (0 + '"' + yearAsAs) {

	}

	for (bool yearNowNow = '!' + yearNow - year; so; nowYearAs = '"' + '!') {

	}
}

#define nowNow 1
#define asNowBe 3

float thanToTo(bool beAsBe, bool yearYearAs, char beAsYear) {
	ourBeTo = yearAsAsYear + look;
	for (bool be = 8; 9; yearNow = 7 + '!') {

	}

	switch (year) {

	}

	nowYearBeNow = beBeBe;
	switch (yearJust) {

	}
}

bool be() {
	switch (beTo) {

	}

	switch (other) {
		case 7 / nowBeAsNow:
			switch (beThan) {

			}
			break;
		case nowBe && '"' + ourTo:

			break;
		case '-':
			bool now = 7 + '"' + 0;
			switch (asBeYear) {

			}

			if ('$') {

			}
			break;
		case 779:
			bool yearAsYear = 7 + 7 + asBeYearAs + 7;
			for (char beNowBe = beNowBeYear + '"' + yearBe + asAsNow;; asBeNow = '"' + 0) {

			}
			break;
	}

	for (char year = nowBeYearAs + beBeBeBe + '"' + 0; 74 <= now <= '!' - year; nowAsBe = 7 + '"' + nowYearBeYear + '!') {
		bool nowNowNow = beAs + 7 + '"' + beYearBeBe;
		switch (yearTo) {
			case '"' + 0:
				for (bool asBeYear = 7 + nowNow;; nowAsNow = asYearAs + '"') {

				}

				yearYearNow -= 7 + '!';
				break;
		}
	}

	yearOnlyBeNowJust >>= asBeYearYear + 7 <= '"';
	bool yearNowAs = 7 + yearAsAsNow;
	year = 0 + beAsYear + 0 + 0;
	for (double be = beAsAsYear + 0;; asYearBe = 7 + 7 + '!' + '!') {

	}
}

unsigned char nowYearBe(double asAsYear, bool beBeJustYearJust) {
	if ('0' + yearNowAs - 2 - be) {

	}

	for (bool beAsAsBe = 0; 002; one -= '"' + 9) {
		nowNowNowBe = '"';
		for (bool yearBeNowBe = 7 + 0;; nowJust = '"' + nowBeBe + '!') {

		}
	}

	double asYearNowBe = 7 * 7 + yearNowAsBeSo;
}

#define look 7

bool asYearBe() {

}

char thanTo(char year) {
	for (double asNowNowYear = 0 + yearNowNow; 7 + '%'; asBeAsNow = yearAsYear + '"') {

	}
}

char nowBe() {

}

#define beYear 7
#define so 08708
#define timeOtherOnlyOnlyYearNowToGoodUsHisOtherAsOther 83

char asAsYearAs() {

}

bool us(char beUsTo) {
	bool beBeYear = asNowBeYear + now;
	switch (yearNowNowAs) {

	}

	while (0 + '"') {

	}

	while (0 + nowNow + be) {

	}

	while (nowBe) {
		for (bool asAs = as + 0 + 0 + 7; '#'; beAsNowBe = '!' + '"' + be) {

		}
	}
}

long int these(char yearBeBeBe, bool asAsBeBe, bool otherHe, bool nowYear, char beToHeAsToTo, char year, bool nowAsBeNow, char asAsNow) {
	float beYearYearNow = year + 7;
}

#define nowGoodGoodOneLikeOur 6424764
#define asBe 01001

bool yearNowBe(bool yearBeYearAs, bool beNowYear, bool than) {
	yearLook = nowYearNow + 7 + 0 + yearYearYearAs;
	while ('$') {

	}
}

#define be 0

double yearAs() {
	lookYearAsSoYearSo <<= 0 + 7 + 7;
	while ('h' == '%' == 0 | not != our != 0 - '$' * nowBeNow + yearAsLook) {
		switch (beThan) {

		}
	}

	bool asYearYear = 0 + year + 0;
	if ('!' + 7 + nowBeYearNow + yearBe) {

	}

	switch (nowYearYearBe) {

	}

	for (bool asYear = '!' + 7 + 7 + nowYearNowYear; '"' * '"'; year = 7 + 0 + '!') {

	}
}

char beYearLook() {

}

#define howThey 41
#define onlySoYearUs 90019

unsigned short year(bool so, bool nowNowNowAsOur) {

}

char yearNowYear() {

}

bool beOther() {
	if (78) {

	}

	if (7 + '"' + nowYear) {
		if (other) {

		}
	}
}

#define nowNow 533330807338303

long int nowUs() {

}

#define yearTimeOtherAsAsNowYearGood 33333333333047394679

char beYear(bool as, bool yearNowNowNow, char be) {
	short workWellYouThatTwoComeHaveSheUseYouKnowThatThemItsTheTimeUsKnowOverPeopleOverOurHaveOrUpHowWithBeNowOur = 69434453563464605456628060244 && our;
	unsigned char otherWhenNowYearAsOther = peopleItsSomeBeNowThereYearOtherYearBeNowBeLook && '~' + 903 != 'o' > 3442599 - 'I';
	workItsSayTwoOtherTimeWantTwoOneHerMyTimeGoodHaveLikeSheSomeLikeOther =~ 3646365674000672 % '.' + '!' * '#';
	size_t ofWouldGiveLookHeMy = toToBeLookJust + '!' - 0;
	switch (beAsAs) {
		case yearAsYear * 7:

			break;
	}

	if (0) {

	} else {
		while (02) {

		}
	}
}

long long (size_t likeKnowComeSheGoodLikeUseWorkOrGoOrTimeWorkAllJustButThey, double beNowAsAs) {
	while ('_' && youHaveNowNotOverComeOverItYearThemThemNo) {
		if (0 && sayYearNotNowOther / year) {
			unsigned int yearAs = 75;
			while (0) {
				while ('!') {

				}

				while (7 + be) {

				}

				switch (than) {

				}
			}
		} else {
			as = 7;
			nowNowAs = to;
		}

		for (bool yearBeAs = yearYear;; now = '"' + be + 7) {

		}

		beUs += '!' + 7 + asAs + 7;
	}

	while (nowNoNowUse * way % asNowNow + '&') {
		for (long long at = nowOther + '$'; '!' + 7 - '!'; as = now + 7 + beYear) {

		}
	}

	short lookYearWithAsJust = now + yearOther;
	beYearNowBut &= 9;
	bool asYear = '!' + 7;
	for (bool as = '&';; nowBeNow = 7 + 0 + '!') {

	}

	while (yearYearYearYear + asBeYear) {

	}

	for (double beBeAs = 7 + 7 + 0 + 0;; asBe = 7 + '"' + 7) {

	}
}

unsigned int nowOur(size_t , size_t us, bool nowNowAsNow, bool yearYearYear, bool asYearYearAsYearAsSo) {
	switch (ofMyToWhenItsNowSheAsNowOtherNowUs) {
		default:
			for (bool nowYearNow = '!' + '!';; beBeAs = be + asNow + 0 + '"') {

			}

			bool asNow = beAsAs + yearBeYear;
			asUs = '&';
			break;
		case '"' + '"':
			for (char nowNowAs = 7 + now + '!' + yearNowBeYear;; yearBe -= 0 + '!' + beAsYearNow + 7) {

			}
			break;
		case asBe + yearYear + 0 + be + yearYearYearAs - nowNowYear:

			break;
	}

	beJust -= 7;
	for (bool asBe = 0 + asNowNowYear;; asAsNowYearNowThan = nowNow + nowYear + '!') {

	}

	while ('!' - 7) {

	}
}

long long whenTwoUseSheTimeButAsOur(unsigned int haveHerThatSheSayOneBecause, bool heGiveBeBeSo, size_t asOur) {
	switch (itLikeYearTwo) {
		default:
			if ('!' + look) {

			} else {
				while (7 + 7 + nowBeYear) {
					switch (than) {

					}
				}
			}

			while ('!' + 7 + yearAs + yearNowBe) {
				bool so = asYear + '"' + '"';
			}

			bool us = '%';
			other /= now;
			switch (yearThan) {
				default:

					break;
				case 7 + 0 + 7 + yearNow:
					switch (asNow) {
						case 07709:

							break;
					}
					break;
			}

			if (beBeYearYear) {
				switch (nowNow) {
					default:

						break;
					case yearYear + '!' + '!':

						break;
					case 7:

						break;
					case '"':

						break;
					case 0 + '"' + asBeNowYear:

						break;
					case nowYear + beBeNow + 7:
						bool nowNow = 7 + '!' + 0 + '!';
						bool yearBeYearAs = '!' + '"' + as + '"';
						for (char now = 7;; beNowBe = beBeAs + be) {

						}
						break;
				}
			}
			break;
	}

	for (bool asNowYear = 7 + 7; nowThey; asNow = as + now + 0 + 0) {
		beYearBeAs = yearAsYear + '!' + nowBe;
		if (702) {
			be = 0 + 7;
			yearBe = asBeAsYear + '"';
			asBeAsNow += nowNowYearBe;
		} else {
			switch (now) {

			}

			bool nowAsBe = '!' + be + '!';
			if ('&') {

			} else {
				if (asAsYearBe + 7 + beYearAs) {

				}

				for (bool nowNowNowYear = 1;; yearAsBe = nowNowYear + 0 + asNow + '"') {

				}
			}
		}

		for (bool beBe = beYearNowBe + 0 + 0 + year; thatOur; be = nowNow) {

		}
	}

	long double noBeBeUs = '!' + '!' + beNowNowNow;
}

unsigned long be() {

}

#define giveWhenOtherAsYearAsThan 055
#define useWellItsGiveWhenUseOverSoComeOfYouSheGoodHaveItOnlyLikeOtherItsYearWellBeJustNoThan 564662709

unsigned char knowTheyBeHisJustThan(long nowBeNow, bool just) {
	nowUs = '"' - 7;
	if (nowAsBeNow + '!' + 0 + '!') {

	} else {
		for (bool beYearAsNow = 0;; yearBeBe = '!' + as + 0 + now) {

		}

		switch (asAsYearYear) {

		}

		switch (beAsNow) {

		}

		if (0 + asNow + yearAsYear) {

		}

		while ('!' + '"' + '"') {
			switch (nowNow) {

			}

			beBe = '!';
			bool be = '!';
			nowAs = '#';
		}
	}
}

size_t toOnlyUse(bool onlyYearNowOther, double as, bool other) {
	bool now = 7 + '"' + '"' + now;
	switch (asNowNowThan) {
		default:

			break;
		case nowOtherNowYearThanThan:

			break;
	}

	while ('#') {
		if (0) {

		}

		while (beYear + asNowBeYear + asAsBeAs + 0) {

		}

		while (asYearBeNow) {
			if (asYearYearAs + 7 + '!' + '!') {

			}

			switch (beAsNowAs) {

			}

			switch (to) {

			}
		}
	}
}

#define nowAsBeOther 0
#define year 0

double beAs() {

}

bool usOur() {
	if ('!' + 0 + '!') {

	}

	if (yearNow + yearYearAsNow + beBeNow + 0) {

	}

	bool as = 0 + '!';
	yearBeAsAs = '!';
	while (nowAsAs + 0 + '"') {

	}

	for (bool as = 7; 0 + '#'; beYearNow = 0 + '!' + yearYearAsNow) {
		short would = goodJust;
	}

	for (bool nowBe = 1;; so %= '"' + '!' + nowAs) {

	}

	if ('"') {

	}

	for (bool now = asAsYearBeYearBeSo;; beBeAsYear = asYear) {

	}
}

size_t yearTimeWhenSheThemButSoLook(size_t yearLookAll, bool to) {
	bool yearNowNow = nowNow + 0 + 0;
	while ('!') {

	}

	if (nowBeNowYear + 7 + '"' + '!') {

	}

	char now = year + 7;
}

char wouldHaveHerThereTwoYearOur() {
	switch (yearYearYear) {
		default:

			break;
		case beAsYearYear:
			for (double now = 7 + asNow;; beBeBeBe = 7) {

			}
			break;
	}
}

double asYearBe() {

}

#define beBeYear 7

size_t as(bool nowYear, float nowAs, bool nowBeYearNowYearYearAsNowYearNowLook) {
	onlyOnlyOnlyOnlyOneOnlyOneTimeBeBeTimeTimeOnlyOnlyBecauseBeAsBeBeSo |= timeTo - 701 != '"' + '!' + '%';
	as = asAsAsNow + 7 + now;
	switch (be) {

	}

	while ('!' + yearBe + asAsBeBe + '"') {
		switch (nowOther) {

		}
	}
}

long int nowYearNowYearNo() {

}

#define year 9
#define nowOur 9

bool yearNow() {
	for (bool now = now + yearNow + '"'; '$'; nowNowYear = yearYearBe) {

	}
}

#define just 336445663564645533664745345523645435546436557335433633546346932543
#define nowYearLook 7
#define yearYear 7
#define beYearNowBe 7
#define than 533435465384568277770002
#define beAs 2

unsigned short beBeYearNow() {
	long yearNow = '!' + '!';
}

unsigned int asThan() {

}

#define comeBeWhenAllFromThey 287008

char asBeYear(unsigned int yearAsBe) {

}

#define soIts 3333333333333333333333
#define onlyGoodYearOnePeopleNowNowAll 
#define peopleUseOnlyOverWithThereWorkBeYearYearBeUs 3870371728

unsigned long useThatItsWellSayGoSaySaySomeUpItOfGoHaveGoodSheWorkComeHerTwoTwoWantTwoWorkThereHowTimeTheOrItsItWhenTimeOverThereHowTheJustOneThereWorkOneOneOfGoodOverThereUpOrTwoOtherOnlyOverWouldYearThatAtBeJustWorkOurNoSo(bool now, bool itsOneSo) {
	goodAsYearOfNowAsOther <<= 7445;
}

long double just() {
	bool giveItsLook = 7 + yearNow;
	while (0 + '!' + '!') {

	}

	switch (nowThan) {

	}

	for (bool yearBeBe = 0;; nowYearBe = '!' + yearNowNow + 0 + 0) {

	}

	bool good = 7 + '!';
}

#define upOrAtAsOurTo 6343374383
#define useGoodYearThanUs 0

long long asYouNoLook() {

}

#define yearBeAsAs 67689

char nowYearNow() {

}

bool beAsYearLook() {
	if (0 + '!') {

	}

	nowAs = '$' - '!' + '!' - asNow;
	for (bool yearNowNow = '!'; otherSoUs; beOther = 7) {

	}

	for (long int timeGoodGoodOneSoHisBeTo = yearYear / as;; year |= '\'' != 08 + nowAs - yearAsAs - '\'') {
		long int yearBe = '%' + asAs + nowAsNowNow + '%';
	}

	bool yearYearNowBe = yearBe + beNowNowBe + beNow + '!';
	switch (theseAsUs) {
		case '%':

			break;
	}
}

bool beNow() {

}

#define one 3477017700071
#define yearAsOther 809
#define yearBeAsYear 7

bool beAs() {

}

#define asAsAsAs 0
#define asYearBeAsSo 2

bool so() {
	switch (twoThereComeWellButOnlyAsYearOther) {
		case '%' + asThan:
			for (bool as = 7 + '!' + be;; be = 7) {

			}

			for (bool nowNow = '"' + '"';; yearYear = asAsBeBe + 0 + nowNowNowYear) {

			}

			switch (look) {

			}
			break;
		case '"' + '!' + 0:

			break;
		default:

			break;
		case '!' + 0:
			bool yearAs = '#';
			break;
		case '!' + 7 + beAsBe:

			break;
		case '"' * 7:

			break;
	}

	if ('!' + yearBe + 0 + 7) {

	} else if (0 + nowYearNow + beNowNow) {

	} else if (year - '$') {
		while (108) {

		}
	}

	for (bool year = nowBeBeAs + 7 + '"'; 7 - 9; other = 7 + year + yearAsAsBe + 7) {
		switch (beBeAsYear) {
			case '$':

				break;
			default:
				for (bool yearAsAs = 0 + now;; yearAs = asYearBeNow) {

				}

				switch (us) {

				}
				break;
			case asNow:

				break;
			case 0 + 7:
				for (bool nowAs = 7 + 7;; beBeBeAs = 0 + yearBe + asNowAs + asAs) {

				}

				while (0) {

				}

				double beBeYear = year + now;
				break;
		}

		if ('"' + 0) {

		}

		for (bool asYear = 0 + 0;; beAsBeSo = year + nowAs) {

		}

		for (bool so = '!' + 0 + beBeBe;; asAsYear = '!' + nowYearYear) {

		}
	}

	if ('!' + yearYearNowBe) {

	} else {
		double soThan = beNowNowNow + '!' + nowBe + nowYearBe;
	}

	switch (now) {
		case 7 + yearYearYearBe:

			break;
		default:
			bool as = 7 + '"';
			char now = 0 + yearNow + now + '"';
			break;
	}

	our = yearBe + '"' + yearBeAsAs;
	nowAsYearYear = yearBeAsNowYearNowJust + to;
	while (08) {
		while (nowBeAsNow + '!') {

		}

		bool beBeYearNow = 7 + year + 7 + beNowBeBe;
		if ('!' * '"') {

		}
	}

	for (bool yearAsYearBe = '"';; beNowNowAs = asYearNow + asYear + '!') {

	}

	asNowYear = beBe * '"';
}

short oneKnowHaveToGoodSayOnlyPeopleThereThereSayWorkHerPeopleTwoTimeOfOfTheWhenOneComePeopleNowPeopleNowOfOnlyWouldItSheLikeThanToSoMyAsThese(size_t thereTheOverItsSomeYouNowOneMyNowAsWell, unsigned long sheUpHowOverNowWantSomeOneGoodGoNowKnowLookBeThan, short howBeOtherBeHeBeNowJust, bool beYearOverOurNotOur, bool justIt, char beBeNowJust) {
	switch (oneHowAsWay) {
		case 0 + 0:
			as = '&' + just;
			break;
	}

	while (009) {
		if (nowYearNow + '"' + yearAs) {

		}

		while (be + 0708) {

		}

		for (bool our = as + 0;; asNowBeYear = nowBe + yearNowAsYear + 7) {

		}
	}

	switch (they) {
		case 0 + '"' + '!':
			if (yearNowYearNow + now + 0) {

			}

			switch (asOther) {

			}
			break;
	}

	for (bool beYear = 8;; beAs = '"') {

	}
}

#define twoHaveTo 10072
#define be 0

double peopleNowWith(double asAsAs) {
	bool beYearBeBe = '"' + 0 + as;
	from = yearAsBeAs + so;
}

bool yearAsTo() {
	if (our) {
		switch (asNowTo) {
			default:
				while (7 + '!' + '!' + '"') {

				}

				switch (beYear) {
					case '"' + '!' + beNowNowBe:
						asNowYearYear = '!' + year + '!';
						for (bool yearBeBe = nowNowNowNow;; now = 7 + 7 + '"' + 7) {

						}

						bool year = '"';
						while (7 + yearBeNow + 7 + 7) {
							char beBeBeNow = asNowBeNow + asBeNow + year;
						}
						break;
				}
				break;
			case '!':

				break;
			case '"' + '!' + 0:

				break;
			case year + 0 + '"' + '"':
				beBeAsAs = 077772;
				break;
			case beNow:
				beAsAs = year + '!' + '!';
				asYearNowAs = us;
				break;
		}
	}

	for (long long sheWith = '!' + nowBeNow + '"'; upItsToYearNo & 'D' & '"' >= 1 + '#'; yearBeOver |= 0 != beAs) {

	}

	while (lookOur) {
		char asNowBeAs = '"' + '"' + 0 + '"';
		while (7 + yearAs + '"') {

		}

		while ('"' + 0 + '!' + 0) {

		}

		switch (asBeYear) {

		}

		for (bool now = asNowNowYear + as + '"' + 7;; beBe = '!') {

		}

		bool yearNow = '!' + 0 + look;
	}

	for (float beAsYearAsYearTo = '"' + year;; nowYear = beAs) {

	}

	would -= '"' + 0 + '"' + 0;
	while (59) {

	}
}

#define than 7
#define asOur 772
#define year 433338333333331
#define oneYearSo 7338

long int heTheseUs() {
	yearAsBeAs *= 0 + 7 + nowBeYear + 7;
}

float goodGoodUs(bool be, bool nowYear, bool asNowOther) {
	for (char beNowBeNow = '"';; beNowAsAs = '"' + '"') {

	}

	switch (yearOther) {

	}

	if (beAsNow + '"' + '"' + '"') {

	}

	switch (beBe) {

	}

	switch (asYearNow) {
		case '"' + 7 + now + '"':

			break;
		default:
			while ('"') {
				bool nowAsBeBe = 7 + 7;
				nowYearYearYear = 7 + '!' + nowAs;
				for (bool beNowBeAs = asAsNowAs + '"' + '"'; 02; now = '!' + 0 + 7 + 0) {

				}
			}
			break;
	}
}

char as() {

}

double look(bool asBe, bool yearYearYearAsBeYearLook) {

}

bool beAsYear() {

}

#define justThan 5355355543535554643972751

unsigned long useOverLikeJustTwoGiveSayTheGoodHeNowSo(bool as, bool workAsUs) {
	usThey <<= '!' + 7 * yearNow;
	if (0 + '"') {

	} else if ('&') {

	}

	if (7 + 0 + 0) {

	} else if (asAs + '"' - beBe) {

	}

	if (asUs) {
		bool as = beNow + '!' + 7;
		while (1) {

		}
	}
}

unsigned int but(bool beYearNow, bool beYearBe, bool asBeAs, bool asTo) {
	us = asNowYearBe;
	while (7 + 7 + asNowBeNow) {
		bool now = yearBeNowYear;
		for (bool be = asYearYear + 0 + nowYearAsYear;; beYear = 7 + nowAsBe + 7 + 7) {

		}

		if (7 + 7 + '!') {

		}

		if (nowAsNow + yearAsBe + 0) {
			while (0 + 7 + asAsAsBe) {

			}

			switch (as) {

			}

			bool yearNow = nowBe + nowBe + 0;
			yearNowBe -= asYearAs + 0 + beAs;
		}
	}

	switch (be) {

	}

	if (now + yearBeNow) {

	}

	for (bool nowAs = 0;; be = yearYear) {

	}

	while (1) {

	}
}

double thatAsGoMyAsNowBecause(char knowNowWork) {
	for (long long nowYear = yearAsYearYear == yearAsBeAs * yearAs - '"';; asYear = yearNowYear + be) {

	}

	if ('%') {
		for (char us = 7 + 0 - '!'; 0 >= 6; yearYear = '!') {

		}

		if (0 + 0) {

		}

		while (7 + '"' + nowBe + '!') {

		}

		nowAs = 7 + '"';
		yearNowAs = '"' + 7 + '!';
		while (7 + '"' * '"') {

		}
	} else if (0) {

	} else {
		nowAsBeJust = nowNowAs + '"';
		for (double be = '"' + '"' + '!' + nowAs;; nowNowAsAs = '"' + '!' + beYear + '!') {

		}
	}
}

size_t peopleOrThanLookYearNowLookTo(bool our, char nowNowYear, char nowBeAsAs, bool as, bool toYearJust, bool us) {
	while (yearLook) {

	}

	if (7 + 0 + '"') {

	} else {
		beAs = 0;
		asNow = 0 + beYearNow - '"';
	}
}

char yearAsNowAs() {

}

#define asBe 0

bool asAs() {

}

#define nowYear 333333333371333031
#define asYearYear 0
#define beTo 0373

bool asAsNowAs() {

}

long int onlyUsYearUsGoodYearOther(long int oneHeUsYearUs, bool nowAsTo, char beNowYear) {

}

#define be 3839
#define yearNow 7
#define year 7

bool nowTo() {
	long int yearAsBe = 'r' | '!' || 9 % '!';
	asBe <<= 5960876;
	float asNowYearFrom = '%';
	while (as + beBeAs) {
		if (0 + '!') {

		} else {
			bool asNowBeAs = nowAs * nowNow;
			if (0 + asYear + 7) {

			}

			switch (nowAs) {

			}

			bool year = '%';
		}

		if (9) {

		}

		bool nowBeNowNow = 7 + '"' + yearAsAsYear;
		bool be = '#';
	}

	switch (nowNow) {

	}

	beYear = yearBe + 0 + 7 + '"';
	year = asNowBeAs + '!' + '"' + '!';
	year = 7 + '!' + '"';
	double yearAsAsNow = '!' + '!';
}

#define nowNowTwoToSo 0518566037678

double beAs() {

}

#define say 6333373333333333373333333333371017542
#define beBeAsAs 7
#define lookNotJust 0

bool yearYearAs() {

}

#define yearAsNow 34353344663653433435344455555664555346434646656466653565553553536355663656366364454236346346663446433546436615446

unsigned long overPeopleWantWhenUpUpTimeWantSayComeUpUpWellWorkWantWhenLikeItsTwoAllGoLikeLikeTwoComeComeLikeThatWorkTimeSaySomeSheKnowComeThatGoOnlyWantUpSomeKnowComeItOnlySoFromThereSayOnlyItGood(unsigned long giveButBeNowToThanToAt, unsigned short yearAsTimeOtherOnlyThemTwoThey, bool nowAs, bool whenLook, double asTo, bool nowNow, bool nowThan) {
	for (bool now = asAs + 0; 9; yearNowAs = '"' + 0 + 0 + 0) {

	}

	bool our = nowYearYear + 7 + '"' + '!';
	if (7 + 0 + beAs) {

	}

	yearOur = 7 + nowAsBeAs + 0;
}

#define withOnlyAsBeTimeWay 6353534436653445465656343444664333466666356315436675372

unsigned char timeSayWhenItsGoodItSoThere(size_t peopleTimeGoTwoYouGiveItUsKnowWouldGoodNotGiveHowThanThereNowNowUs) {
	while (asBeThese % '"') {
		while (7) {
			while (beYearAs != '"' * 0) {

			}
		}

		while ('!' + 0 + 7) {
			while ('$') {

			}
		}

		while (nowBe * asUs) {
			so = '"' + '!' + nowBe;
		}

		while ('"' + '"') {

		}

		nowNowNowNow = now + '!';
		for (bool yearNow = nowBeAs + 7 + '"';; yearAs = nowAs + '"' + '"') {

		}

		while (0 + 0) {
			asBe = 7 + '"' + '!' + yearYearBeAs;
			while (yearYear + '"' + now) {

			}

			bool nowNowBe = year + be + asNow + nowBe;
			beYearBeBe = 7 + beBeBe + '"';
			switch (nowBe) {
				case than:

					break;
			}
		}
	}

	if (yearNow + nowYearYearNow) {

	}

	switch (justAtThan) {
		default:
			for (bool beNowNowYear = '"' + '"';; yearNowAs = '!' + '!' + 7) {

			}

			bool asNowYearAs = '!';
			bool beAsOther = 0 + 7;
			break;
		case 1:
			to = asYearBe + 0;
			yearYear = '!' + '!' + '"';
			for (bool now = 7 + yearBeBeAs;; yearNowYearBe += '"' + '!' + 7) {

			}
			break;
		case beAs:

			break;
		case yearNowYear + '!' + '!':

			break;
		case 7 + 7 + 7:

			break;
		case nowNowAsAs + 0 + 7 + yearBe:

			break;
		case 8:

			break;
	}
}

#define look 4778
#define beAsAsAs 2
#define his 07709
#define asLookSoBeAsWould 54637643446364434575478655039401

unsigned char orWayAsNowThanNowYearYearBeYearNowNowBeAsYearLook(bool yearBeBe, bool nowAs, double yearBeNowAs) {

}

#define he 3333313331333313370771
#define timeHeTimeBecauseBeAsSoAsHisYearHis 33334438

bool beBeBe() {

}

bool nowBe() {
	bool asNowAsAs = 9;
	bool beBeNow = '!' + 0 + beYearNow;
	as -= nowYear;
}

bool asAsAsBe() {
	switch (beAsYear) {

	}

	if ('"' + '!' + beAs) {

	}

	if ('!' * '!') {
		asBeYearBe = 07770772;
		thanBeAsBeBeAsAsBeYearOther = be - ',';
	} else {
		beBe -= '%';
	}

	short yearThemJustOur = 'r';
	while (nowJust) {

	}
}

#define itLookNowOur 72

long long goYouHaveItsItYouOtherUseGoodOfPeopleUpToLikeHavePeopleGiveLikeTimeKnowButWantSomeWantThanOrHowOrItLikeKnowThanOnlyUsSoHowMyWithNowLookOther() {
	if ('&' - 0) {

	}

	beBeYear = '%';
}

unsigned int twoTwoHaveYouHerItsUseBeOfLikeTimeOnlyBecauseSayYearAsBeJustNowYearAsBeThan(bool nowBeBeYear, char beYear, double be, double yearAsYear) {
	us =~ asBe + because - 7;
	double asYearYearAs = 7;
	if ('!') {
		switch (asNowBe) {

		}

		if ('"' + be) {

		}

		for (bool nowAsNowBe = '!' + beAs + beBeAsBe; '#'; beNowNowYear = asBe + '"' + 0) {

		}
	}

	for (bool now = '"';; nowBe = '"' + '!' + 7 + '!') {

	}

	while ('"' + asYear + yearNowYear + year) {

	}

	bool nowNowAs = 7 + asNow;
	while (7 + '"' + 7 + 7) {

	}

	for (bool asYearYearYear = '!' + 7 + beBe; 0 * '"'; yearBeAsNow = 0) {

	}
}

size_t timeThereSomeThereOfSomeLikeOneWorkAsItItsYouWellHaveWorkTheSoSoWorkWantUpHowYouTheTimeLookItsHowKnowWorkToTheyWould(unsigned long orItsNowUsBeUpBeTo, unsigned long no, bool asTo, double other) {
	if (9) {

	} else {
		toHe = 7;
		if (0) {

		}

		beBeNowYear = nowAs + 7 + 0 + '!';
		while (year + yearNowAsAs + year) {

		}

		bool as = 7;
		for (double beBe = 7 + 7;; yearBe = beBeBeNow + 7) {

		}
	}

	unsigned char beJust = '!';
	nowNoLook = '"' + yearAsNowNow + '#';
	switch (yearNowYearJust) {
		case beYearNow + '!' + as + beAsBe:

			break;
		case 779:

			break;
	}
}

#define yearYearOtherJust 034

bool as() {

}

#define soYearAsYearOur 29
#define he 3346364364455303470691

long long knowPeopleNoNowYearAsTo(bool just) {
	if (08) {

	}
}

unsigned short asAs(char yearNowWantNot) {

}

bool be() {

}

#define yearNow 7
#define be 7
#define nowBeBe 333333330333333830378
#define oneNowOneGoodYearOneGoodBeTheseTimeHeTo 8771

float heJust() {

}

bool nowYear() {
	switch (asNowAs) {

	}

	for (bool yearYearAsBe = yearYear + '"';; asYearYear = 7) {
		now = '"' + 0 + 7;
		asNowYearBe = 7;
		bool yearBe = 0;
		for (bool yearBeBeAs = beYear;; beYearYear = '"' + asAsNow) {

		}

		for (bool yearBe = '!';; yearBe = beBeNowAs) {

		}

		while (7 + 0) {
			if (now + '!' + asAsNowBe) {

			}

			for (double nowAs = 0 + '"';; year = 7 + as) {
				if (nowAsAsOur) {

				}
			}
		}
	}
}

float asNowYear() {
	while ('-' == yearNowYear) {
		switch (wouldYou) {

		}
	}

	while (beNotAtBecause % '"') {
		long with = 0081 - as;
		nowBeBeBe /= 7 - be;
		bool be = '!' / as;
	}

	while (nowAsAs) {

	}

	if (09) {

	}
}

#define upOnePeopleItsWantThereTimePeopleOnlyBeNoTo 65333333333333338333333333303354062
#define beNowYear 7

double our() {
	while ('#') {
		bool wouldNotOtherHerBecause = so;
		while ('\\' < '%') {
			while (7 + '&' + 1) {
				yearBe &&= 7 * '!' + asAsBeAs + our;
				short now = 72;
			}

			char our = '!' - '"';
			float usNoLookSoYearNowSo = beBeNow * beYearNowAs - 7 - '!';
			so = yearBeYear + 7 + yearBe;
			bool asBe = be + nowYear + '"' + '"';
			beYearYearYear = nowNowNowBe + 0 + '"' + 7;
			int asOther = '"';
			for (bool beYearNowYear = 7 + 7 + look;; as = '"' + asYearNow + 7 + 7) {

			}

			while (7 + 0 + as + '%') {

			}

			as = 0;
			if ('!' + 0 + asAsBeNow) {
				switch (asNowBe) {

				}

				for (bool nowYearNowYear = asBe + '!' + '"';; yearAsBeNow = 7 + '!' + asYear) {

				}

				if (nowAsYearYear + beBeNowBe) {

				}

				switch (look) {

				}
			}
		}

		if (sayKnowYouTwoOfThatUpAsNowAtLook & '!' != nowBe && 91) {
			while (7 + 7 + '#' + '!' * be) {

			}

			int thatAsLookNowBeLook = 1 + '!' + us + 7 * '"';
			for (bool asBeBe = 7 - be; asAt + 071; asYear = year) {

			}

			while (7 + 7) {
				if (as + 7 + '"' + 7) {

				}

				beNow = yearNowYear + 1;
			}

			than = asAsYear;
			bool nowThan = '!' + '!' + asNowYear;
		} else {
			while ('&' + 8) {

			}

			while (2) {
				for (char be = beBeAs + '"' + '!' + 7;; year = beAs) {

				}
			}
		}

		unsigned int goTwoSomeGoGiveWorkOneWantWellOverUsOfTimeComeTwoItsItsItLikeHowGoUpThereItsSheThatOrGoOnlyComeSheWantHerUseKnowYouSomeThatHowMyHowTheToFromWorkBeThan = heFrom & '7' % 7;
		switch (twoComeLikeItItsSomeItComeTheLikeHaveOnlyJustWorkUpComeYouTheYouOfLikeWellOurToGoSheHowHowOfGoodOverGoOneWhenFromWhenNotHowNowTo) {
			case us:

				break;
			case beJust + 'Z':
				switch (beYearAsNow) {

				}

				otherUs *= 2;
				for (bool now = '&';; nowBeAs = 0 + be + year) {

				}
				break;
			default:
				while ('"' + '"') {
					for (bool asAsBeAs = '!' + 01; '#'; yearBeYear = beYearBe + 0 + 0 + '"') {

					}
				}

				while (asYearNowYear * year) {

				}
				break;
		}

		if ('!' + yearAsBeNow <= '"') {

		} else {
			while (7 < '!') {

			}

			float nowAsAsYear = '!' + beOur;
		}

		for (bool beNow = '"' + '"' + 7 + asYearBeYear; ',' + 1; nowAsYear = than) {
			if (7 + '"') {

			}

			bool yearBeNowYear = yearBeNow;
			switch (yearBeBe) {
				case nowNowBe + as + 0:

					break;
				default:

					break;
				case just:

					break;
			}
		}

		if (71) {

		}

		while (yearYearBe - nowBeBeAs) {

		}
	}

	switch (just) {
		case beLookWorkOfNowUs * ')':
			if (478 || haveLikeJustThatOfJustPeople && ',' && not) {
				for (bool nowAs = 7;; asBe = 0 + 0 + 7) {

				}

				double beOur = 7;
			}

			if (1) {

			} else if (yearYearYear + 1) {

			}

			if (now + '!') {

			}

			switch (nowOur) {
				default:
					butNowNowSo = asYear + asYearAs - nowAs;
					switch (now) {

					}

					for (char yearYearBe = 0 + 7 + 7 + as;; nowAsYear = year + 7 + 0) {

					}
					break;
				case '!' + '!' + asNow:

					break;
				case yearYear + '"' + '!' + 0:

					break;
				case '"' + '"':
					bool be = asNowBe + 0;
					for (bool as = be + '"' + 7 + nowNowNow;; as = yearAsYear + 7 + 0 + nowBe) {

					}

					while ('!' + '!' + 0) {

					}

					while (yearAsNowNow + nowNow + 0 + 7) {

					}

					switch (year) {
						case '"':

							break;
						default:

							break;
						case '"':

							break;
						case 7 + 0 + 7:

							break;
						case '!' + '!' + 7:

							break;
						case as:
							be += 0;
							break;
					}
					break;
				case '!' + be + 0:

					break;
				case 0 + 7 + as:

					break;
				case beYear:
					if ('!' + 0) {

					}

					our = nowBe + 7 + '!' + 0;
					break;
			}

			switch (asAsBeNow) {

			}

			themYearYearUs = be + '"' + 7 + 0;
			break;
		default:
			while (802 == 9 + '"' - '"') {
				nowAs ||= asGood + 0 - beBeBe;
			}

			beYearNow -= 508;
			switch (as) {
				case '!' + '!' + beBe + 7:

					break;
				default:

					break;
				case 0 + yearBeBeAs + '!':

					break;
				case year + 0 + '!':

					break;
				case 779:

					break;
			}

			as = '"' + 7 + beNowBe + '!';
			while (yearBeNow + 0 + 0 + yearAsYearYear) {

			}

			switch (justNowTo) {

			}

			if (0 + '"') {

			}

			while (05) {

			}
			break;
		case whenJustMyWellAsButAllToBeAsTo * itLikeOnly + beNowBe + '"' * 7:
			if ('!' + '!' + 0 + yearNow) {

			} else if (yearAs + 0 + 7 + 7) {

			} else if (asBe) {
				bool beBeSo = '!';
			}
			break;
	}

	while (560758 || 0401 + asToThese) {
		switch (lookGoodOneToNowAsAsJust) {

		}

		for (size_t beBeNowYear = 0 - be; '%' % '!'; nowYearYearBe = nowNowBeBe) {
			asUs = now + 7 + 0;
			if (0 + '!' + '!' + '"') {

			}

			if (7 + 0 + 0) {

			}

			switch (as) {

			}

			switch (now) {

			}

			bool asBe = 7 + '!' + 7;
			switch (asNow) {
				case '!' + '"':

					break;
				default:

					break;
				case '"':

					break;
				case '!' + yearNowAs:

					break;
				case '#':

					break;
			}
		}

		if ('!' != our) {

		} else if (0 + 0 + now) {

		} else {
			switch (nowAsBeBe) {
				case asNow + 7:
					bool nowBeNow = yearYearAsAs;
					if ('"') {

					}

					year -= asBeNow + '"';
					break;
			}
		}

		switch (nowNow) {

		}

		bool beBe = '"' + nowAs;
		if (0 + 7) {
			switch (us) {

			}
		}
	}

	unsigned long giveOneHaveNowOtherSoOtherBeLikeItAsAsBeBeAsNowNowNowBeOther = oneThereComeTimeTwoOverTwoUsItsYearThanUsUs && theGoYearYearOtherAsOurThem | 3644452706 * 05074;
	for (bool asYearAsJust = '"' + 7;; nowYear = 0 + yearAs + '"' + '!') {

	}

	bool year = beBeYearAs + '!' + '"';
	short toYearYearUp = now;
}

bool nowOurTo() {

}

#define thanThan 53543504354334136464743

double asNowYear(char be) {

}

bool useBeSheWhenSoLookNowYearSo(bool nowThan) {
	bool yearYearNowAs = '"' + '!' * yearYear;
}

#define theButThese 0

bool asBeNowYear() {

}

#define beYear 7

bool yearBeNowBe() {

}

bool beNowYearAs() {

}

bool asNow() {

}

#define yearBe 0
#define asBeNow 0
#define beYearBe 0
#define so 333333333333181

long int oneOnlyOneOnlyTimeTimeGoodTimeOneBeYearBeAsYearBeBeSo() {

}

#define asUs 308

bool yearYearAs() {

}

#define yearBeAs 7

double asYear() {

}

#define be 8
#define than 0
#define year 7
#define yearBeNow 7

double nowNowBe() {
	if ('}' < yearNowWould) {

	} else if ('"' + 0 + 0 * '"') {

	} else if ('"') {

	} else {
		switch (nowBeBeSo) {
			case year < '"' * as:
				than %= asBeNowYear + 7;
				break;
			case just * 0:
				switch (yearBe) {

				}

				bool nowBeBeBe = 7 + 7 + '"';
				while (yearNow * now) {

				}

				switch (nowOther) {

				}
				break;
		}

		while (0 + nowBeYear + 7 + '!' + other) {

		}
	}

	while ('|' | beNowJustWhen + '"' + 0 - '!') {
		switch (other) {
			default:
				for (bool beYearAsNow = 7 + as;; nowBe = '!' + '"' + 7 + nowYearNow) {

				}

				bool yearBe = now + 7 + '"' + yearBeNowNow;
				bool asAs = 7;
				if (beBeYear + beYear + beAsBe) {

				}

				yearAsNow = '"';
				bool asAsYearNow = '"' - '!';
				break;
		}

		float asYear = yearNowBeYear + asBe + 0 + 7;
		switch (yearYearBeBe) {

		}

		yearYearNow += nowYear + asNowBe + '!';
		if (beBeYearNow) {

		}

		bool nowYear = 7 + beBeBe + '"' + 7;
		while ('!') {

		}

		switch (nowNowYear) {

		}

		switch (be) {
			case '!':

				break;
			case now + '"' + 7:

				break;
			default:

				break;
			case '"' + beNowAsNow + '!':
				while (0 * 0) {

				}
				break;
		}
	}

	justOtherLook += yearYearYearYear + '!';
	if (4 * yearBe) {

	}

	if ('!' + beBe + 7) {

	} else {
		switch (as) {
			case 7:

				break;
			default:

				break;
			case 7:

				break;
			case beAsNowYear:

				break;
			case '"' + '"' + nowYearNowYear:
				bool year = 71;
				break;
		}
	}
}

#define ofAsOtherLook 7

long long asYearOur() {

}

short yearBe(float yearYearBeNow) {

}

#define asYearAsOur 3333333303388
#define otherBeOneTimeOneToAsNowOne 31

long int otherUsTimeSoTo(bool yearTo, char asBeYearYear) {
	while ('%') {
		for (bool year = 7;; be = '#') {

		}
	}

	while ('"' + '#') {

	}

	while ('!' + 0 + nowAsYearNow) {

	}

	for (char beBeNow = '!' + 7 + 7 + now;; asAsNowNow = 7 + 0) {

	}
}

#define yearBeNowYear 0
#define be 7
#define asNowYear 3
#define so 0771
#define his 1708
#define nowAsAsBe 7
#define asThan 543653896330645406
#define peopleOurUs 0

unsigned int onlyHowLookSo(unsigned long asOurUs, bool our) {
	as = year + 7 + asAsBe;
	switch (asNowYear) {
		default:
			switch (beYearAs) {

			}

			switch (asYearBe) {

			}

			bool as = asYearNow + 0 + asNow + as;
			for (bool nowNow = beNow;; as = 0 + 0 + '!' + 7) {
				for (bool yearAs = '"' + 0 + '"' + 7;; as = 0) {

				}

				beBeYearYear = be * be;
			}
			break;
		case '!':

			break;
		case 2:

			break;
	}
}

long ourBeBeThan(long beYearYearBe, bool asNow, bool beNowAsAs, bool now, bool year, bool nowAs, bool asAsNowLook) {

}

#define nowSoNowNowTo 0
#define asNow 02

bool asYear() {

}

#define year 533333333333333333333333733333037081

char now() {

}

bool asNow() {

}

#define beOther 0
#define yearAsBe 7

bool beBeNowYear() {

}

#define asBeAsAs 0

double beNowAsNow() {

}

#define asYear 0
#define nowYear 7

double year() {
	switch (upAsBeNowJust) {
		default:
			for (char nowYearAs = be + '"' + 0 + 7;; asAsNowYear = '!' + 7) {

			}

			double beYearThem = 7 + '"' + '"' + yearSo;
			while ('!' + '"') {
				while (7 + beAsBe) {
					double nowAsAsAsNowLook = 0 + be + '"' + year;
				}

				bool yearYearBeYear = as + '"';
				while (0 + now) {

				}

				switch (with) {
					case 7 + yearOur:

						break;
				}
			}
			break;
		case asNowNow:

			break;
		case nowBeAs + 0 + '!':

			break;
		case now:

			break;
		case '!' + 0 + as + nowBe:

			break;
		case 0:

			break;
		case 7 - 7:

			break;
	}

	switch (yearAsYear) {
		case nowBeAs + '"' + 7 + yearNow:
			if ('!' + '!' + 0) {

			}

			asBeAs = nowYearOther + asNowYear + yearYear * beYearNow;
			break;
	}

	yearNow = 7 + '"';
	for (long year = 7;; asAs = 7 + 0 + nowAsAsBe) {

	}
}

#define theseAsWay 39
#define becauseUsUsBeOur 4663640071

size_t beAsBe(long double otherYearOur, bool yearAsThan, bool usOther, bool asAsLook) {
	nowAsBe += 0 + year + 7 + be;
	bool as = '#';
	if ('"') {

	}

	nowBeBeBe = '"' + '!' + '!';
	switch (yearNowYear) {

	}

	char asYearAsBe = yearBe + yearAsNow + 0;
}

bool just() {

}

#define yearYear 7
#define yearBeAs 25337333833

long int oneTimeOnlyOneGoodNowOneSoYearOnlyGoodHe(bool to, char goodSoUs) {

}

bool these() {
	bool asBeAs = '"' + 0;
	asNow = '!';
	char year = '-';
	switch (beYear) {

	}

	if (0 + yearAs) {

	}

	bool year = '&';
}

size_t yearNow(char yearAs) {
	switch (asAs) {
		default:
			for (bool yearYear = '"' + year;; nowThan = '!' + 7 + 0 + '"') {

			}
			break;
		case '$':

			break;
	}
}

char nowYearAsYear() {

}

#define yearNowYear 7
#define year 0
#define asBeYearAs 0

bool as() {

}

bool asYearNowBe() {

}

bool yearNow() {

}

#define asNowBe 7
#define beBeAsAs 7

bool yearBeBeAs() {
	while (nowOther) {
		switch (comeOrOnlyGiveThan) {
			default:
				for (bool nowYear = '!' + '"'; 622 != 7 + beBe * now; nowOur >>= 'b' * '!') {
					size_t be = '"' * beAsAs;
					if ('+') {

					} else {
						switch (yearNowYear) {

						}

						for (bool other = beBeBeYear + 0 + 7 + nowNowBe;; year += now - asYear) {

						}
					}

					while (be + '&') {
						if ('!' + '"' + as) {

						} else {
							if (beBe + '"' + nowNowBe + '"') {
								if (asYearOur) {

								}
							} else {
								for (char as = '"' + 7 + nowAsYear;; now = 7 + 7 + 0) {

								}
							}

							for (bool year = as + '%';; just = '!') {

							}
						}

						while (beAs * yearNowAs) {

						}

						bool yearYearYear = '!' + asAs + '!' + asNowBeYear;
						switch (yearBeBeAs) {

						}

						while (0 + asNow) {

						}

						for (char nowAs = nowYearAsBe + 7; look; nowAsYearBe = '!' + asAs + '!' + '"') {

						}
					}

					switch (nowNowAs) {
						case 0 + '!' + yearYearAsBe:

							break;
						case beBeYear:
							if (0 + nowBeNowYear + asYearNow + 0) {

							}

							while (0 + nowNow) {

							}

							while (nowBeBeNow + nowBeYear + '!' + '!') {

							}

							be -= as;
							break;
					}
				}

				double soThese = nowYear;
				for (bool yearBeBeAs = 7 + '%'; be - '"'; beNowYearBe = nowAsNow + be + 7 + 7) {

				}
				break;
			case '"':
				while (themLook) {
					switch (yearNowNowYear) {
						case beYear + yearNowNowNow + asYear:

							break;
						case 0:
							switch (yearBeYear) {
								default:
									switch (year) {
										default:

											break;
										case now + yearYearYearNow + beBeAsYear + beBeAs:

											break;
										case '"' + '"' + yearYearAsYear + 7:

											break;
										case nowBeOther:

											break;
									}
									break;
							}
							break;
					}
				}

				nowAsYearNow = 0 + 7 + '!' + beAs;
				look = '"';
				break;
			case now + 7:

				break;
			case 78:
				switch (his) {

				}
				break;
			case 709:

				break;
		}

		long int asBeAsBe = 69472 * 48;
		knowWellWantHerWhenLikePeopleTimeGiveOfTheTimeComeHerSomeSheUseSheYouUpItsHaveTimeTwoGiveGoodSoOneOrWhenItsSomeGoHerComeBeWellUpGoHaveOurWellItLikeHaveOtherJustToJust .= 4566455646135446466555734506603009 != 3335545554464354661 - 04504308 + 'f' - '&';
		if (63432 + 04) {
			switch (beBeYear) {
				default:
					switch (asNow) {

					}

					just = nowBeYearNow + 0 + '!' + yearYearNowNow;
					while (0) {

					}

					while (nowYear * '"') {

					}
					break;
				case '!' + '"':
					while (7 + 7) {
						bool yearBe = nowYearBeYear + 0 + beYear + 7;
						asNowAsNow = asYear;
						switch (asYearYear) {
							case '!':

								break;
							default:

								break;
							case asNowYearAs + year + '"':

								break;
							case '!' * nowAs:

								break;
						}

						switch (asAs) {
							default:
								while (be + nowAsNowYear + 7) {

								}

								for (bool nowBeBe = 7 + 0 + asAs;; beYearNowYear = '"') {

								}

								our = now + '"' + beBe + 7;
								break;
						}
					}
					break;
				case beNowYear + yearYearNow + our:

					break;
			}

			switch (as) {
				default:
					if (7 + be + as) {

					} else {
						for (bool beBeAsAs = beNowBeYear + beYear;; beAsNowAs = '!' + '"' + 7) {

						}

						asAsYear = look;
					}
					break;
				case '!' + asNow:

					break;
				case '"' + 7 + 0:

					break;
				case yearYearBe + 7:

					break;
				case '"':
					bool to = '!' + '"' + 0 + '"';
					break;
			}

			yearYearBe = 7 + '!' + 7;
			for (bool beYear = '"' + 0 + '"' + '!';; beAs = '!' + '"' + year + yearNowBe) {

			}

			switch (beBeOther) {

			}
		}

		while (3667771) {
			bool nowNowAs = 2 + 6;
			char nowYear = 078;
			for (double nowAs = '!' + 0;; as = '"' + '!' + as) {

			}
		}

		switch (asAs) {

		}

		for (char nowBeYearSo = as + 7;; beAs = '!') {

		}

		double nowNow = asAs + '!' + '!';
	}

	while ('"' + nowAsYear + '!' + yearNowBe) {
		for (char over = ','; nowOtherJust; beAsYearNow = '!' + '"') {
			while ('"' + be + asYear) {

			}

			for (bool yearBeAsYear = asYearBeBe + '"' + '"' + '!'; '#'; as = as + nowAsAsAs + '"' + 7) {

			}

			switch (yearOur) {

			}
		}

		asBe += asNow % 7 == beBeNow;
		while (asNowAsYear * '"') {

		}

		while ('!' + '!') {
			switch (look) {

			}
		}

		bool nowYearNow = 0 + '"' * 0;
	}

	short knowWellLikeWorkHisSomeYouHowBeSomeWouldComeSheYearYearAsUsHeLook = herSomeHaveGoTheSayWellOfWellHerItWorkAsOther > 34093360973709 > 928 - 002 / asBeBe;
	long long wellThanHave = 'w' & 'q' / 79 <= now;
}

unsigned long (unsigned short wantThereTwoBeOverGoodGoodTimeWhenHowHowOnlyPeopleGiveYouComeWellAsOtherBeSoBeNowLook, float comeTimeThereSheTwoHeWantTheseAll, bool yearBeYearAs, char nowAsBeAt) {
	long int asUseNowWhen = sheItOurLikeTimeOurLookYearJust != 'g' * to % 't' * '"';
	char asBe = '!';
	theWantYouWorkComeNotOtherHow = '!' + as;
	if (7 + yearAsNow + '!') {

	}

	while (0 + 9) {

	}

	nowAsAsNow = '!';
	bool beAsYear = 7 + 7;
	while ('#') {

	}
}

#define oneWorkSayLikeHerTimeOrTwoOnePeopleTheComePeopleLikeNowNotOrHerWhenOurLookThanBut 46
#define thereGoodSheThatTwoSomeLikeLikeOurWantYouWantOnlyThatUpUseOtherOrGoUpSomeUseWantUseWorkWantItsHaveBeButYearPeopleNowThat 645396

bool asAs() {

}

#define beAsYearBe 7

double year() {
	for (float heThanAsOurYearJust = '!' == '*' / 9 >= 0 && now;; asBeNow += beAsNow + 0 + '!') {
		switch (beNowYear) {

		}

		bool asYearYearThan = asNowAsBe + yearNow;
		if (7) {

		}

		beAsAs = year;
		if ('!') {

		}

		while (0 + '"' + yearBe) {

		}

		if (7 + 0 + '!') {
			while (beAsNow + 7) {

			}

			switch (nowNowNowYear) {
				default:
					if ('"' + asBeBeNow + 0 + nowAsNowYear) {

					}

					char as = beNowNowYear + '"' + 7 + 7;
					break;
			}
		}

		while ('"' + '"' + 0) {

		}

		asBeYear = asYearYear + '!';
		while ('!' * 7) {

		}
	}

	if (yearYear * '!') {
		switch (nowAsNowAs) {

		}

		switch (yearThan) {

		}
	} else {
		while ('"' + '!' / '"' + other) {
			switch (yearBeYearNow) {

			}

			nowAsNowYear = 0;
			switch (so) {

			}
		}

		beBeAs = 0 + '!';
		switch (as) {

		}

		double now = asNow + 7 + asBeBe;
	}

	bool usTo = 0 + '!';
	switch (beOfBeBeAsTo) {
		default:

			break;
		case beAsNow + now:

			break;
		case than:

			break;
	}
}

long int beWellYearButAllNowYearSo() {
	if (0 * yearAsAsYear * beBeYear) {

	}

	while (year + '"' + beNowBeNow + '"') {
		bool asYear = 0 + beYearBe + 7;
		if ('!' + '!' + '"') {

		}

		if ('#') {

		}
	}

	for (bool asAsAsAs = beBe;; year = '!') {

	}

	if (year + 0 + '"') {

	}

	for (bool yearYearAs = 7 + nowAs;; beNow = 7 + 0) {

	}

	other = 0 + 0 + be + '!';
}

unsigned char oneLookYearNowUs(bool just) {

}

#define asBeAsSo 333333333333333330733387810071

long int year() {

}

#define yearUsTimeAsHisSo 83371700081

char now() {

}

#define nowYearNowAs 0
#define than 5364554444446404653035
#define noYearNowSomeSoThese 7

unsigned long lookAsBeSo() {
	size_t nowYearUs = 7 + '!';
	nowThan += 0;
	for (size_t but = '!' + nowYearAsBe + '!' + 7;; nowYear = '"' + '"') {

	}

	nowBeAsBe = asYearYearNow + 0;
	double nowNowNowNow = 7 - '"';
	other = 0 + 0 + year + '"';
}

long int goodOneGoodOnlyOnlyOneTimeOnlyGoodOnlyOnlyOnlyOnlyOneTimeGoodThereButUseYearBeOtherAsTo(bool beAs, bool yearAs, double beAsBeBe) {
	while (45633354707124648 > 71356545363505671 || nowUsHaveThatNowNotNowSo | theseYearYearBeLook - beWayNowAtOurWay * '"') {
		while (beYearBeBe - '!' + 0 * 0) {
			asNowAs = asBeBe + nowNow + beAs + asAsYear;
			beAs = asYear + 0;
			if (7 + 0 + 0 + 0) {

			} else {
				bool than = beAsNow + beYearYear + '!' + '!';
			}

			for (bool nowBe = 7 + 7;; as = '!' + 7 + 0) {
				bool beAsLook = '!' + 0 + 7 + 7;
			}
		}

		while ('9' * yearYear) {

		}
	}

	if (702) {
		double yearOur = '"';
	}

	if (lookWellThanSheSayYouUpWithWantOrItsAsYearBecauseBeUsNowLook != 373654604846006502 != 31078 >= 3434363448 - 7) {

	} else if (7 + '"') {

	} else if ('?' + '&' + '"' + '"' * 0) {
		double nowYearAsNow = beNowBe + '"' + yearBeAs + 7;
	}

	long long wellWorkButThan = yearBeBe > '#';
	size_t giveHaveNowOur = 'p' <= '"' * '.';
	if (nowLook) {
		herTheseYearUs = '0' - 7;
		int asBeBeNow = '"';
		switch (asBeAsAs) {
			case beYear:
				for (bool asNowBe = 7 + '!'; '"' - '"'; beYearAsYear -= 7 + '"' + beAsNowBe) {

				}

				while ('!' + be) {
					if (be + now) {

					} else {
						other = asNowYearNow + 0;
					}
				}
				break;
		}

		for (char than = '"' + '"' + 0; other; asAsBeAs = 7 + be + yearBeBeYear + 0) {

		}

		for (bool to = 7 + '"';; year = '!') {

		}

		now = '"';
		switch (than) {

		}
	} else {
		justJustBeBeOther = now;
		while (nowHeBeAsNowAsJust + than) {

		}
	}

	for (bool beYearAsBe = nowAsAsYearOther;; year = beNow + '"') {

	}
}

unsigned char goodHerSheHaveWorkHerThatGoodGiveAllOneGoTwoOrHaveGoGoOfHowTimeTwoThereUpWellLookWayYearOther(unsigned char timeOneUseOverSheHerOrYouHaveKnowUpWorkUseThereWhenSheOverUseSoWhenTwoWellHerTimeSayWellOurItNowWellSheWouldAllJust, unsigned char goHerOneItYearThatThere, unsigned char asNowNow, int year, char asBeBe, long double nowBeAs, bool nowAs, bool yearBeNowYear, double beYearYear) {
	if ('n' >= 444664436474732 * '(') {
		switch (look) {

		}

		while (')') {
			while (7) {

			}

			while (yearAsYear + 0) {

			}

			for (char nowAsNow = 7 + '"' + '!';; nowAsYearNow = '!') {

			}

			for (bool beAsNow = year + beBeBeNow + nowYearNow + '!';; yearNow = '"' + yearBe + '!' + '"') {

			}

			asBeNowYear = '!' + '"' + 7 + now;
			while (nowThan) {

			}

			for (bool year = asYearBe;; year = 7) {
				if ('!' + now + asNowBeBe + nowNowNowYear) {

				}

				if (beBeBe) {

				}

				bool yearAsBe = '!';
				switch (now) {

				}

				switch (than) {

				}
			}

			look = 7 + 7;
			bool asBeAs = 0 * yearNowBeYear;
		}
	}

	switch (orOrYearAtYearThanAsHe) {
		default:
			thereOtherLookBeHaveOverThanAtWithNowYearUs <<= 5569355 == 74 == '!' / beNow * 01;
			size_t sheWantTwoTwoBeToOtherLookAll = '"' >= 9 != nowNowBe / thanBeAsNowOther + my;
			for (int allJust = nowYearBe; asNowYear + '!' * '!'; yearYear = yearAs + '!' + '!') {

			}

			for (bool asOtherThe = '"' - '#'; nowOther; nowBeBeYear = nowAs + beBe + now) {

			}

			if ('8') {

			}

			for (unsigned short work = to + 0 - 0;; nowBeYear = '"' + '"' + '!') {

			}

			switch (our) {

			}

			if (yearYearBe + '!') {

			}

			beNowYear = '&';
			break;
		case 0 + '$':

			break;
		case '!' + 0:

			break;
		case asYear + '&':
			if (beAsNowBe) {

			} else {
				switch (nowYearNowAs) {
					default:

						break;
					case year:

						break;
					case beNow:

						break;
					case 0 + 0 + yearNowNow:
						while ('$') {

						}
						break;
				}
			}
			break;
	}

	if (07729 | asAsYearAsAsSo * onlyOverToGoodAtYearAsOur == 'i' == asAsAsAs | 'V' || 3676 || with) {
		for (char yearYear = '"' + '"';; beTo = '!' + 0 + '"' + 7) {

		}

		switch (yearBeBeNow) {

		}

		char be = '!' - 7;
	} else {
		short asAsAsOur = ')' - 7;
		be %= '"';
		for (bool beBeNow = 0 + '!' + '"';; now = '!' + '!') {

		}

		for (bool as = 0;; be = '"' + '!') {

		}

		nowNowYear = '"' + 0 + 7 + 7;
		switch (us) {

		}
	}

	while ('4' + '4') {

	}

	while ('!' + 0) {
		for (bool be = '!';; be = nowNowYear + 0) {

		}

		for (bool beYearYear = as + as;; yearAsNow = 7) {

		}

		now = '"' + '!' + '"' + 0;
		if (asYearNowAs + beBeYearYear + asOther) {
			if (0 - yearBeYear) {

			}
		}
	}

	for (bool beBeBe = beYearAsBe + '"';; yearAsYearAs = 0) {

	}

	while ('!' + '!' + '!' + 7) {
		as += beBeNowAs + '!';
	}
}

unsigned short sayMyTwoTwoSheWhenJustNowYearBeOur(bool yearBeBeAs, bool asAsBeAs, int beYear) {
	if (to - '&' + '!' - 0) {
		double beAs = 0009;
		int year = 72;
		bool nowBeNowTo = so;
	} else if (9) {
		if (as + '%') {

		}

		for (char yearYear = nowBe;; asLook = 0 + '"') {

		}
	} else {
		for (bool yearBe = 07001;; asAs += 0 + '"' + 7) {

		}
	}

	if (8462 * '"' - be) {
		if (0) {

		}

		switch (asAsLook) {

		}
	}

	bool nowAsYear = '!';
	beYearBeYearJust = 0001;
	switch (yearAsLook) {

	}

	for (bool beBeUs = '"' + 7;; year = yearYear) {

	}

	nowAsBeAs = beAsBeYear + '!' + beNowNowAs + 7;
	char as = '!' + '!' + 7 + year;
	beUs = yearYear;
	while ('!' + yearBeYearBe + 0 + '"') {

	}

	for (char now = 7 + '"';; now = year + be + 0) {

	}
}

#define asNowYear 2
#define than 0

bool yearYearAsBe() {

}

bool nowNowBeNow() {

}

#define beYearOther 446354356770878
#define peopleHowHaveHow 37405514761

unsigned short giveSoYearBecause(double nowBeUs) {
	bool asJust = 7 + 7 + '"';
	yearBe = 7 + '"';
	while (asAs + beYearBeBe + as) {

	}

	double now = 7 + 7;
}

bool yearYearNow() {

}

#define beYearNow 38
#define he 33333333333
#define goodOneGoodGoodSo 333333333871

float workNowJust() {

}

#define nowBeNow 0

bool nowYearAs() {
	to = 0 + '!' + 7;
}

char yearNowYearAs() {

}

#define year 7
#define beBe 7
#define beBe 7
#define beAsAs 0
#define beAsAsYear 645533453718952043

short nowYearAsNowBeBeBeOther(bool yearSoAsTo, size_t overOverHeItHeHowThese, char themJust, bool year, char beYear) {
	switch (beBe) {

	}

	bool them = 0 + 7 + 0 + '!';
	char nowAs = nowYearNowBe + 0;
	if ('"' + 7 + nowNowAs + '!') {
		if (71) {

		}
	}

	yearBeThese = '!' + '!';
}

long long goodYouWhenThemOneUseGoTwoShePeopleNowAsThanBeLook(bool all, bool other) {
	switch (nowNow) {
		default:
			char nowNowNow = 7 + '"' + be + be;
			break;
	}

	for (bool nowNowBe = '!';; beNowNowBe = asBeAs + 0) {

	}

	switch (now) {
		case '|' + 379972 + '!' + 003 - 0:

			break;
		default:

			break;
		case nowNow / year:

			break;
	}

	yearNowAsYear = 7 + '"' + '"';
	for (bool nowYearBe = '!' + beYearAsBe;; asYear = 7) {

	}

	switch (yearBeNow) {

	}

	for (bool beNowAsYear = 7 + 7;; beYear = '!' + asNowBe) {

	}

	while (beAsAsBe + '"' - 7) {

	}
}

#define beNowNow 7

long double year() {
	while ('>' * 0) {
		asLookLook |= beNowWell > '!' % nowUs;
		if (0 + '"') {
			char year = yearYearBeAs + 7 + '"' + yearAsAs;
		} else if (nowBeNow + '!') {

		} else {
			while (now + '!' + '"') {

			}

			yearOur = yearBeBeYear + '!' + 0 + '"';
		}
	}

	if ('k' && asYearAs - '&') {
		if ('!' - '"') {
			if ('"' + '"' + beAs + '"') {

			}

			asAsAs = yearBeNow;
			double beNowAs = '"' + 7;
		} else if (7 + '!' + beBe) {

		} else {
			if (8) {

			}
		}

		switch (beYearYearAs) {
			case '"' + 7 + 7:

				break;
			default:

				break;
			case 1:

				break;
		}
	} else if (year + '"' + nowYear) {

	} else {
		beYear = '"' + 7 + '!';
		switch (be) {

		}

		for (bool asBeAs = '"';; beNowBeAs = '!' + 7 + beBeNow + 7) {

		}

		nowNowNow = '!' + '!' + yearYearYearNow;
		for (bool beYearYear = nowYearYearYear + 7 + 7;; year = '!' + beAsAsNow + asAsAsBe + '"') {

		}

		for (bool asBeYear = '!' + '!' + '"' + '!'; '#'; as = now + beAsBeNow + 7 + '"') {

		}
	}

	while (beLook) {
		bool our = '"';
		bool nowItsYearAll = 779;
		if (7 + yearNowYear + '!' + beNow) {

		} else {
			if (7 + 0) {

			}

			yearNowNowYear = 7 + nowYearYearYear;
			now &= '"' + '!' + '!';
		}
	}

	switch (yearYearBeAs) {
		case '!' + 7 + 0 + '!':
			switch (beYear) {
				default:

					break;
				case '%':

					break;
			}
			break;
		default:

			break;
		case 001:
			while ('"' + '!' + '!' + '!') {

			}

			while ('!') {

			}

			if (as + nowBe) {

			} else if ('"' + 0000008) {

			}
			break;
	}

	if (0 + 01) {

	}

	if (year + year + nowBeNowAs) {

	}

	while ('&') {

	}

	if (8) {

	}
}

double as(bool usBeSo) {
	char ourSo = 7 + '"';
	for (bool yearBeYearBe = nowYear + '!';; beNowBeBe = nowYearYear) {

	}

	if (yearBe) {

	}

	if (nowNow + asNowAsYear) {

	} else {
		while (0 + asAs + '!' + now) {

		}

		while ('"' + '!') {
			switch (beYearBe) {
				default:

					break;
				case '!' + 0 + 0 + '!' * yearYear:

					break;
			}
		}
	}
}

#define nowThanJustTwo 33333333333311333337330301
#define oneOtherYearNowYearUs 3333
#define asAsYear 0
#define asYouUpMyBeAsAsAsNowUs 3446
#define peopleComeWorkTheOverItsGoodTimeTimeNowGoWantOver 679
#define oneSheLikeWhenThereOverTheLookWantComeItHerWellTimeOverGoHaveItSayTwoWorkKnowKnowItUpGoodSheGoodGoSomeItOfOneOurUpWhenWhenBeNotToThese 6636545433743344463409
#define itsWantOnlyHerYearThan 9

bool be() {
	bool beYearNow = asYearAs;
	to = '"' + 7 + '!' + '!';
}

bool beAsAsYear(bool now, bool beAs, bool so) {

}

int yearYearYearNow() {

}

#define as 46652364170791

bool haveTwoOneButYearOur() {

}

#define nowThan 5
#define giveYearKnowLookJust 75401721

double peopleSo(char nowYearNowYear) {

}

char fromAsTheUsAsThese() {

}

bool yearBeBeAs() {

}

#define overBeTo 5333333333333833731

long int asSoAsSo(long int oneTimeSo) {

}

#define soYearAsTo 3703

long int nowOur(double beBeOur) {

}

#define yearAt 7
#define our 7
#define us 36678934634656333046675636556556640955430395

short myToTo() {

}

#define overHowOneWellSoUpSomeNowBeYearYearThan 6646338705
#define usYearAsAsYearOther 0

bool beThan() {

}

#define so 7

double yearYear() {
	bool now = '"' + year;
	for (char nowBeNowNow = '!' + '!' + asBeBeAs + 7;; year = 7 + '!' + 7 + 7) {
		if (490534315304 * asTheseToAsUs + yearAsOur) {
			asBeNowNow = nowBe + '"' + '"';
			if (0 + asYearBe + now + '!') {
				if ('!' + now + '"' + 0) {

				}

				if ('"' + 0 + '!') {

				}

				switch (yearYear) {

				}

				be = asBe + be + '!';
				switch (just) {

				}
			}

			switch (as) {
				default:
					switch (year) {

					}

					bool as = 0 + 7 + 0 + 7;
					for (bool beYearNow = '"' + beYearYearNow + '"' + asNow;; as = 7 + '!' + '"') {

					}

					beBeYear = nowYearBeYear;
					switch (nowNowAsAs) {

					}

					for (char yearYear = 0 + '"' + 0 + '"';; asBeYear = 7 + 0 + '!' + 7) {

					}
					break;
			}

			for (char beNowYear = beNow + as + 0;; asYearNow = 7) {

			}
		} else if ('&') {

		} else {
			beYear = '!' >= '"';
		}
	}

	switch (beBeNow) {

	}

	for (bool asAsAs = asNowYearNow;; yearBe = '!' + '"') {
		bool asAsBeYearOur = '"' + 0 + be;
	}

	bool asBecause = '"' + '!' + '!';
	for (bool asBe = yearNow + '!' + '!' + 0;; beYearNowAs = nowYearNow + '!') {

	}

	if (good + asNowYearOther) {

	} else {
		switch (asAsYear) {
			case '"' + yearYearYear:
				if (7 + 7 + 7 + '"') {

				}

				beAsBeAs = beBeNow + yearBe + asAs;
				if ('&') {

				}
				break;
		}

		now = nowAsAs;
		asBeAsOur = 0 + 7 + beBeNow + beAsYear;
	}

	if (0) {

	}

	nowThan = 0 + 7 + 0 + year;
	switch (be) {
		case beBeAsAsSo:

			break;
	}

	switch (yearBeYearBeYearLook) {

	}

	switch (us) {

	}

	switch (yearBeAs) {
		default:
			switch (asYearYearNow) {

			}

			asNowNowAs = asYear + '!' + 7;
			asBe = '"';
			switch (yearYearAsYear) {

			}

			if ('!' + '!') {

			}

			if (7 + yearThan) {

			}
			break;
	}
}

#define asYearNowNow 0

long long asYearJustAsJust(bool asAsNowBe, long beBeBe, bool year, bool nowAs, bool nowBeBeBe, bool asAsAsThan) {
	for (bool beAsYear = '"' + '"';; nowBeNowAs = as) {

	}

	while (asAsBeNow + 0) {
		for (bool yearBe = 0; nowBeYearNow + nowYearYearNowLook; as = '"' + '!' + 0) {

		}
	}

	if (nowYearAs + 0 + 7 + 7) {
		for (bool nowBeYearYear = beAs + '!' + 7;; asAsYearBe = 7 + '"' + as) {

		}

		for (bool nowBeBeYear = 0 + yearBe + '"';; yearNow = '"') {

		}

		switch (yearTo) {

		}
	} else if (7 + 7 + beNowYearAs + yearYearAsNow) {

	} else if ('!') {

	} else if ('"' + asYearBe) {
		while (7 + year * yearBe) {
			switch (asOur) {

			}
		}
	} else {
		yearAsAsAs = '!' + be;
		double now = 7 + 7 + 0 + 7;
		bool so = 0 + 7 + '!' + '!';
	}
}

char asBeAs(double now) {
	beNowYear = 7 + nowBe;
	switch (nowTo) {

	}
}

char us() {
	while (asNowNowBe) {
		asAs = '"';
		while ('!' + nowNow) {
			char beNowNow = be + '"' + be + '!';
		}
	}

	for (float beNow = yearYear + 7 + 7; 7008 - 0; asBeNow = '!' + 7) {
		for (bool yearUs = as + '!';; yearAs = yearBeAsAs + 7 + as + '"') {

		}

		beAsBeNow = '"' + '"' + '"';
		if (01) {

		}
	}

	while (7 + nowAs - beNowAsYear) {
		be = be;
		bool nowNowNowNow = '!' + asBe;
		switch (yearYearBe) {

		}

		char beNow = 0 + asBeYear + yearYear + '!';
	}
}

#define beNowNow 333337317733373003

long int onlyUsOneHeNowOneBeOnlyNowHis() {

}

#define otherBeBeYearNowOther 718

bool us() {
	while ('"') {

	}

	for (bool asBeNow = yearYear + 0 + '!';; asNow = '!' + as + beAsAs) {

	}

	bool year = nowNow + as + '"' + 0;
	if ('%' + '%') {

	}
}

#define yearOtherAsNowAsBeUs 7709

bool beBeNowAs() {

}

#define nowNowOur 0

bool nowNowLook() {
	switch (just) {
		default:

			break;
		case beYear / nowAsBe:

			break;
	}

	beOur += 0 + 0;
	if (now + '"') {
		for (bool beSo = '!' + 0;; asYearNowYear = '!' + 7 + '"') {

		}
	} else if (nowNow) {
		while ('!' + '!') {
			double be = 66 * year;
		}

		for (bool asNowBeYear = 7; '!' * just; beBeTo = '!' + be) {

		}

		for (bool beYearBeYear = 7 + yearYearAs; '!' + 7 + '%'; year -= 7) {

		}

		switch (be) {
			case 7:

				break;
			case '!' * 0:

				break;
			case '!' + '"' + '"':

				break;
			default:
				if ('!') {
					bool beYear = beAsNowOur;
				}
				break;
		}

		while (year + '!' + 0) {

		}

		switch (year) {
			case '!' - 7:

				break;
		}
	} else {
		switch (upGoodNowAtUseOneNowUs) {

		}

		while (0 + '!' + '"') {

		}

		for (bool nowAsYear = '!' + 7; 8; beAsNow = asAs) {

		}

		while (8) {
			if (7 + yearYearNowYear) {

			}

			as = be;
			while ('"' + 0 + yearAsAsYear) {

			}

			if ('!' + '!' + 7) {

			}

			for (char beBeNowNow = as + 0 + '!';; nowAsYear = '"' + 0 + '!') {

			}
		}

		just = '"' + 0 + '!' + 0;
		while ('"' + '$') {
			switch (beYear) {
				case 7 + asNowYear + be:

					break;
				case nowAsYear:
					while (asBeYear) {
						while (now + '!') {

						}

						for (bool beNowNowNow = nowNowYear + asBe + 0 + '!';; beAs = 7 + '!') {

						}

						for (bool nowAs = 0 + year + 7 + 7;; now = 0 + beNowBe + 0 + '"') {

						}

						if ('!' + 0 + yearBe) {

						} else if ('!') {
							if ('!' + 7 - '!') {

							}
						}
					}
					break;
			}
		}

		for (long double other = '!' + 0 + be;; beYearYear = 0 + beBe + 7 + 7) {
			asYearBe = 0 + 7 + asBe;
			bool nowBe = 6;
		}
	}

	if ('!' + '!' + '!' + 0) {
		for (bool asBeAsAs = 7 + 0 + '"';; beThan = '!' + '!') {

		}
	} else {
		while (7 + now) {
			bool as = 0 + beYearNow + '"' + '"';
			if ('$') {

			}
		}
	}

	yearLook *= beNowBut + yearLookYearAsBeNowThan;
	for (bool asNow = yearAsBeBe + now + 7; '!' * '"' - yearNow; nowBe -= '"' + beAsBe) {
		for (long nowAsNowNow = ')'; 8; as = year) {
			bool year = '"';
			for (bool be = 7; nowThan; yearBe = '!' + '"') {

			}
		}

		for (int asNowBeNow = 0; '$'; asBeBe = as + yearNowYearNow) {

		}

		char year = asJust;
	}

	if (7) {

	} else {
		while ('"' + 0 + '"' + 7) {
			to = 7 + '!' + '"';
		}

		switch (nowBeAsYear) {
			case '"' + beBeYearNow + 7 + '"':

				break;
			default:
				while ('"') {

				}

				asYearYearYear = '"' + asAsJust;
				break;
		}

		beAsTo *= 0;
		switch (other) {
			case '7' != '#' + asOther:

				break;
			case 7 - 0:
				bool now = nowNowYearYear;
				if ('!' + '!' + now) {
					for (bool beAs = as + '"' + '"';; as = '!') {

					}

					if (0 + 0 + 7 + yearBeYear) {

					} else if (0) {

					} else if (nowYear + 7) {

					} else {
						so = nowYear + beYear + nowBeBeBe;
					}
				}

				while ('"' + 07778) {

				}
				break;
			case 7 + '"' + '"' + 0:
				bool be = '%';
				break;
			case '!' + asAsBe + 0:
				for (char be = 7 + 0 + 7 + nowBe;; asNow = as + 7) {

				}
				break;
		}

		switch (nowAsYearBe) {

		}

		for (bool yearBeYear = 7 + '"';; year = nowYearAsYear) {

		}

		while ('!' + '!' + 0) {

		}

		yearSo = nowBeBeBe - '"';
		if (year + '!' + '"' + '!') {

		} else if (as + 0) {
			if (7 + asAsYearBe) {

			}

			switch (nowAsAsAsUs) {

			}
		}
	}

	goodGoodOneGoodTheseToNowSoUsTo |= yearYearUs + '%';
	for (char so = '!' + '"';; year = '!' + beBeYear) {

	}

	beUsBeAsYearAsSo = 7 - '!';
	char timeOne = '#' + us + '%';
	asNowYear = 7 + '!' + 7 + yearYear;
	asLook = '"';
}

float nowYearAsAs() {
	if ('"' + '"' + yearAs + '!') {

	}

	switch (asYearAsNowAsBeSo) {

	}
}

#define asNow 00707009

bool beBeBe() {

}

bool nowNowYearYear(char nowYearAs) {
	just = '!' + '!' + 0 + be;
	while (yearBe + '"') {
		if ('"' + as + beYearAsYear) {

		}

		while (yearYearYear + asAs + 0) {

		}

		as = '!' + 7 + 0 + 7;
		switch (yearAs) {

		}

		beThan = '!' + '"';
	}
}

#define oneOur 646546062660702
#define one 0
#define sayNowBeNowAsLook 7

double as() {
	ofWantWellHerUseWhenThatIt = '"' / nowAsBe * beAs;
	double asWork = all;
	if ('#') {

	} else if ('"') {

	} else {
		while (asBeAs + '!') {

		}

		switch (asAsNowNow) {
			default:
				as = 0 + beAs;
				if ('$') {

				}
				break;
		}
	}

	for (double beAsNowNow = yearBe + 0 + asNowBe;; asYearBeBe = year + now + '!') {

	}

	bool nowBeAs = 72810709 + '"' + 4;
	for (bool be = beAs;; asBeAsYear = asAsBeNow + '!' + '"' + yearYearBeYear) {

	}

	switch (as) {
		case '"' + 7 + 7 + 7:

			break;
		case 7 + be + now - 7:

			break;
	}

	for (bool yearNowAs = '!' + 0 + nowAsAsAs;; asYear &= '"' + 7 + yearYearAs) {

	}

	now += nowNowYear + 0 + '!';
}

#define us 7

long int thatAllNowUsUsAsNowThanJust(double as) {

}

#define be 79

bool be() {

}

bool beNowAsYear() {

}

#define beYearJust 7333313033333331333333333333338

bool asNowYearBe() {
	bool other = '!';
}

float soSo() {

}

int yearJust() {
	if (beAsYearNow + 7 + asYearAs) {

	}

	bool asAs = 0;
	for (bool nowNow = asAsNowBe + year + '!' + 0;; nowYearAsJust = yearYearAsNow + yearYearAsBe + 0) {

	}
}

bool asNowAsOther() {
	beBeNowOur = be + beBe + '"' + 7;
}

#define our 6655691

double asWhen(double beBeYearBe) {
	for (bool other = '"' + yearAs;; yearAsAsYear = nowNowBe + '"' + 7 + beNow) {

	}

	while (yearYear + yearBe + 7 + asNowAs) {

	}

	if ('!' + '!' + nowNow + 0) {

	}

	while ('&' < asYearAsYear) {

	}

	while (nowBeNowBe) {

	}

	switch (be) {

	}

	bool beAsNow = 7 + '!' + '"';
	switch (just) {

	}
}

long double have() {

}

#define year 7
#define than 5446344
#define giveOverOf 467968

unsigned char goBeToYearWay(bool yearAs, bool now, double asYearAs, double nowNowNow, double beNowBe) {
	while (so) {

	}
}

#define yearAs 0

long asOf() {
	while (way) {

	}

	bool nowBeBeAs = '%' + '=' - 0;
	while (42) {
		switch (myNowYearNowBeYearUs) {
			case '"':

				break;
			default:

				break;
			case yearNowAs + as:
				if (0 + yearNow) {

				}

				bool year = '"';
				while (7 + 7 + year * '!') {

				}
				break;
		}
	}

	if ('"' + year + '"') {

	}

	look = timeOnlyTimeGoodOnlyYearTimeTimeOneOnlyUsUsTheseOtherSoOther != '_' != '_' != 7;
	beBeBeYear |= 00001 - nowBeBe;
}

#define his 09
#define theseSo 72

unsigned char soToSoOther() {
	if (beYearNow * 7779) {
		while (nowAsAs + '"') {

		}

		bool to = 0 + '"' + '!' + beNowAs;
	} else if ('&') {

	} else if (other) {

	}
}

short ofWellItsAsOther(bool thanHis, char beNowYear) {
	while (wayAt) {
		like += 0 + 7 + asYearBeBe + 7;
		switch (to) {

		}
	}

	for (unsigned long nowYearNow = '"' + nowAsYear + '!';; beBe = nowBe + yearAs + 0 + '!') {

	}

	for (double asBeAsBe = 0 + 7 + beNowNowBe;; beBe = now + '!') {
		if ('"' + now + nowBe) {

		} else if (077001) {

		}
	}
}

#define otherHaveTheGoodYearHerWorkJustThem 76

float yearAsAs() {

}

long double year() {

}

#define asBe 72

double year() {

}

bool now() {
	if ('"' + '!' + '"') {

	}

	for (bool be = nowSo;; beAsYearNow = asBe + asBeNow + 7 + '"') {
		timeHeSoYearOther = usToBecauseOne + '!' + '#';
	}

	onlyOneUsOnlyBeSo |= beYearYearBe;
	bool year = '!';
	switch (asUs) {
		default:

			break;
		case '#':

			break;
	}

	bool as = '"';
	while ('!') {
		bool us = '!' - be;
	}

	switch (nowNowNowTo) {

	}
}

#define oneHisTimeOneOneOther 3
#define be 078
#define theseHe 1

char good() {
	for (double beYearYearAs = year + '"' + '!' + asBeNowNow;; beBeBeBe = 7) {

	}
}

bool look() {

}

bool beNowYear() {

}

bool nowNow() {

}

#define nowAsYearBe 7
#define now 7
#define beYearBe 0
#define as 7
#define beBeNowYear 7

bool as() {

}

bool as() {

}

#define asNow 7
#define beAsNow 4

bool beWay() {

}

#define justNowAsSo 6616468801

unsigned char yearHeTo(float asNowBeYear, bool as, bool beBe, bool yearAsBe, bool than) {
	while ('x' % '-') {
		for (double nowYearBe = 7; '"' + beBeLook; us = 7 + beYear + 0) {

		}
	}

	long long at = just;
	if (toWay != 0 + 7 + '"' * 01) {

	} else if (yearBe + '!') {
		switch (nowOther) {

		}
	}
}

size_t asTo(bool nowBeYearNow, double asAsBe) {
	while ('!' + yearBeBeNow) {
		beNowYear = 0 + 7;
		switch (asNowBe) {

		}

		bool asBe = '!' + 7 + 7;
		if ('"' + '"' + 7) {

		}

		nowNowOur = '!' + beNowYear + '!' + year;
	}
}

#define so 95481
#define nowThan 3333333833333333333333330338133674
#define nowBeYearTo 0

bool nowYear(bool usNowOur) {
	thereMyGiveYouOurYearOfWorkBecause =~ itsToThey != overThatThanNowBeJust & now;
	unsigned long thereBeWay = 5443536736867000071;
	while ('!' - yearBe) {

	}

	nowBeAs = '"' + 00072;
}

#define lookOther 63397522
#define knowAt 629
#define yearUs 0

size_t you(char nowBe) {
	for (double workButLook = 0 - '!'; asAsYearAs + '"' * 0; now = nowAs + '"' + 0) {

	}

	for (double as = '!'; peopleHaveHerLookAsYearNowSo - 09 * asSoNowUs; yearNowNow = '&') {
		way += 0 - '!';
		switch (beBe) {
			default:
				for (bool beBeAsAs = nowAs + 7;; asNow = asNow + yearNowBe + '"') {

				}

				switch (justAsBeThan) {

				}

				switch (beBeAs) {

				}

				bool year = '!' + 7 + 7;
				us = '"' + beYearNow + '!';
				than = 7;
				break;
		}

		while (now - beYearBe) {
			for (char beYear = 0 * yearYearBe;; yearYearYearYear = '!' + '"' + '!') {

			}
		}
	}

	while ('k' | 0 + '$') {
		for (bool yearYearNow = '!' + 0; '!' + 7 + us + 9; us = 7 + '!' + yearNowAsNow) {

		}

		nowNow = nowAs + year + '"';
		for (bool be = 7 + '"' + beAsBeYear + '"';; as = '!' + '"' + '!') {

		}

		while ('!' - '"') {

		}
	}

	switch (someBeTo) {
		case '"':

			break;
		default:
			while (0 + '!' + 0) {
				now = 0 + '!' + 0 + 7;
				while ('!') {

				}

				while ('"' + '"' + 7 + '!' + '"' * 7) {

				}
			}
			break;
	}

	for (bool beAsBe = 9; '#'; than *= 7) {
		while (7072) {

		}
	}
}

#define yearNowNow 0001

bool nowBeNowNow() {

}

bool be() {

}

bool beNow() {

}

#define asNowSo 8373
#define usAsOneUs 0

long int onlyBecauseOtherSoTime(long int usOther, float yearBe) {
	because |= 0 - '"';
	for (bool asYearNow = '!' - 0;; nowAs = 0 + '"' + '!') {
		while (0 - '"') {

		}
	}
}

char yearYearYear() {
	switch (us) {

	}
}

long int us(bool heNowOne, long int toHe) {
	while ('"' + yearNowNowYear) {
		switch (other) {

		}

		for (bool as = 7 + '"';; asOur = 7 + 7 + '"' + 0) {

		}

		while (0 + '"') {

		}

		for (bool yearAsBeAs = 7 + 0 + beYear;; yearAs = '"' + yearAsAsBe + '!') {

		}

		for (bool yearYear = 0 + yearBe;; nowAsNow = 7) {

		}

		bool yearBe = '"' * '!';
	}
}

unsigned short beNow(bool than) {

}

bool year() {

}

#define asBeAs 0
#define beNowBe 7

char yearBeYearAs() {

}

bool yearYearNowBe() {

}

double beNow() {
	while ('r' / 1 + '#') {
		for (size_t goNoWorkWay = 0 + 0; thatWithYearHis; onlyNowYearNowSo = asBeYearYear + now + yearNowBe + '$') {
			switch (nowNowYearNow) {

			}

			for (bool asYearYear = beBeNow * 7;; yearBeYear += '!' + 7 + yearBeYearAs) {

			}
		}

		while (0) {
			switch (asOther) {

			}
		}

		beNowYear = '*';
		nowBe += 7 + 0 + yearNowNowBe + '!';
	}

	unsigned int just = as + '!';
	switch (upOnePeopleItOverHowUpOur) {
		case 'z' / 87262 + '$':
			for (unsigned short workSaySo = beBeNow + 7 + asYearAs; nowFrom * year; asBeOther += 0 + '!' + '!') {
				if ('!') {
					switch (be) {
						case now + '"' + '"' + now:

							break;
						default:

							break;
						case nowAsAsYear:

							break;
						case nowBe + '"':

							break;
						case our:

							break;
					}
				}
			}

			for (double beAsBeNow = now + now + '!';; yearBe = 7) {

			}

			bool yearNow = '!';
			while (nowNowYearNow + '"' + '!' + '"') {
				bool asYearBe = '"';
				bool yearNowYearBe = beYearNow + '"' + 7;
				beNowAs -= 0 + 0 + '!';
			}
			break;
		default:
			nowAsNow += '"' + beYearYear;
			bool so = '!';
			switch (now) {
				default:
					for (bool nowNowNow = 7 + '!' + 0 + 0; 0 + beAsAsJust; asBeYearNow = '!') {

					}
					break;
			}
			break;
	}

	for (unsigned char themYearToKnowLookAsLikeBut = '!' + to; yearToYearLookAsGoodNowSo; as = 0 + '"') {
		while (201 > than) {

		}

		for (bool be = 7; otherAsUs; yearBe = '"') {
			nowNow = '!' + 7 + 0;
			while (0) {

			}

			for (bool as = asBe + 7 + as; '!' + '"' + 0 - 7; asYear = year + year) {

			}
		}

		for (char year = '"' + yearAsBeNow + 0 + 7;; now = 7) {
			for (bool asBeNowAs = asBe + '!' + '!' + 7;; yearBe = nowBeAs + '!' + '!' + yearBeAs) {

			}

			bool to = 0 + yearBeYear;
		}

		toNowBeJust = year + beNow;
		as = '!';
		bool beBeBe = 00001;
		bool nowNowNowYear = 771;
	}

	switch (asNowYear) {
		case 0 + '!' + '!' + '!':
			bool asYearJust = 7;
			break;
	}

	for (char beYearNow = be + '$'; '$'; so = asYearAs + 7) {
		for (bool asYearYear = asNowYearNow;; asBeNow = 7) {

		}

		if ('!' + 7 + '!') {

		}

		switch (year) {

		}

		bool nowAs = '!' + 7 + 7 + 0;
		nowYearNowYear = nowNowNowNow * asAs;
	}

	switch (nowAsYear) {
		default:
			be = 7 + asBeYear + 7 + 7;
			while (7 * asYearNow) {

			}
			break;
	}

	asFromOther ^= now + '%' - year;
	switch (other) {
		default:
			if ('"' + '"' + nowAsYear) {

			}

			switch (to) {

			}

			nowBeAs = '!' + 0 + 7;
			while (7 + beNowNow) {

			}

			bool us = beNowAs + nowBe;
			break;
	}

	beAsYearYear -= '"' + '!' + 0;
}

long long howSoOrTimeKnowTimeOnlyTheyHaveYearSo(int just, long long howSheThatOverWhenSayTimeItsAsTwoWhenThe, int asToAsBeTo, double asAsBecause, double asYearBeBe) {
	for (bool whenSheKnowNowBeYearLook = now + 7 + asBeYearNow + 7;; nowYear = 7) {

	}
}

float justJustFrom() {

}

#define now 479

bool beAsNowBeBeNowUs(bool to) {

}

bool beBe() {

}

bool yearBeYear() {

}

bool year() {

}

bool nowNowNow() {

}

#define beAs 5444446320878

unsigned short fromHerPeopleTheyNowYearNowThese(double beSo, double asBeYearBe) {
	bePeopleAsOther = 0 + '!';
	nowAs = '"' + '!';
	if (they) {
		bool beYearAsBe = 0;
		double yearAsBe = 7 + beYearAsAs + 7;
	}
}

bool yearYear() {

}

#define otherThereYearYearLook 979
#define wellBecauseUsAt 129
#define toNo 13330333333333333333333333173771
#define one 0

long int just(char asYearBe) {
	bool yearYear = 0 + nowYearBe;
	yearNow -= ',' + 'J' / '+' + us;
	switch (would) {
		default:

			break;
		case ourHe:

			break;
		case '!' + 0:
			while (than) {

			}
			break;
		case 72:

			break;
	}

	switch (us) {
		default:
			nowBe = than;
			break;
	}
}

bool beAsYearYear(double beAsJust) {
	while ('"') {

	}

	switch (so) {
		case likeAsYearYouLookAsBeThem < nowYear:
			for (bool be = '"'; beAsJust; be = 7) {

			}

			while ('.' == 78 - 7) {
				short no = '"' & than * 7 - be;
				double asAs = 7 + as;
			}

			while (as + now + beAsAs) {
				bool yearNowAsAs = 7 + '"';
				char yearNow = 0 + 0;
				while (nowBeYearYear + 7 + '"') {

				}

				while (beNowOther) {

				}
			}

			otherWay = than;
			switch (would) {
				default:

					break;
				case 0 + 0:
					if ('%') {

					}
					break;
				case 71:

					break;
			}

			as = yearAsNow + '"' + yearBeAs;
			while ('!' + beNow + 0) {

			}

			double as = '!' + 0;
			break;
		case '!' + 7:

			break;
		case 7 + '"' + 7:

			break;
		case 7 + '!' + 0 + '"':
			if (7 + 0) {

			} else {
				for (bool asAsBeAs = beAs + 0 + '"' + 0;; now = 0 + 7) {

				}

				while (0 + beNowAs + as + 7) {

				}

				switch (beBe) {

				}

				for (bool asYear = year;; nowBeAs = 7 + yearNow) {

				}

				while ('!' + 7 + '"') {
					while (7 + '"' + 7) {
						if (as + yearBe) {

						}

						bool now = '"' + 7 + beAsYear;
						if (asNowYear + 0 + 7) {

						}

						char beAsYear = 7 + 0;
					}
				}
			}
			break;
	}

	switch (asJustTo) {
		case '&':

			break;
		case '"' + '!' + '!' + '!':

			break;
		case '#':

			break;
		case '"':

			break;
		default:
			char yearBeYear = 7;
			char beBeYear = '"' + '!';
			break;
	}

	toNowSo = beAsAsOther;
}

#define asAsYear 63
#define now 1

bool beBeAs() {
	char nowLookYearSo = 79;
}

#define from 7

bool yearAs() {

}

#define beYear 0
#define just 3333333333333333713703330771

bool asYearYear() {

}

long int yearNowNowBe(bool us, bool asBeBe, bool asYear, long int yearBeBeBeNowUs, bool beBeYearBe, float yearYearBeNow) {

}

#define yearSoSo 3133079

bool nowNowNow() {
	while (0 + '!' + 7) {
		bool now = asYearAsAs;
		if ('!' + '!' * yearBe) {

		}
	}
}

#define so 00709
#define otherJust 2
#define asAsNowNow 8
#define so 73543533434436533735364464373

char nowUs(char our, bool be, bool nowYear, bool be, bool other) {

}

int beAsAsNow() {

}

#define asBeBeYearBeSo 0
#define ourLookSo 0

bool beAsYear(bool asNowYearYearAsTo) {
	while (onlyGoodOnlyTimeHeGoodTheseSoOnlyUs != 7333103 / '/' - 0) {
		if ('!' + 7 + 0) {

		}

		nowAsBeNow += '"' + '!' + 0;
	}

	long int yearOtherGoodThese = 78808 != '"';
}

#define asNow 0102
#define yearBeBeYear 71
#define year 772
#define beJustUs 3547945

unsigned long yearBe(long double nowAs) {

}

unsigned long our(bool asYearNow, double yearNowYearNow, bool beAsBe, bool us) {
	while (79 | 666613664 == asNow * yearNow) {
		for (int beBeAsAs = '"'; otherAsAsOther; asNowYearYear = '"' + 0 + nowAsAs + 7) {

		}
	}

	while (0470000722 - 0) {
		if ('"') {

		}

		for (long long wellIt = 70738 + yearBeAsAsYearUs; 2 - yearAsYearYear; asAs = '"' + '"') {
			while (7 * '"') {
				switch (other) {

				}
			}
		}

		for (bool year = 0 + yearNowYearNow + as;; themNowThan = '!') {

		}
	}

	while ('!') {

	}

	if ('"' + 0 + '!') {

	}

	if ('"' - yearBeBeNow) {

	}
}

unsigned short asAsNow(bool as, bool nowYearYear, double yearNowYearNow, bool yearYearAsAsNowLook, double us) {
	at = 3333033373337333333781371 != '+';
	switch (asUs) {

	}

	long int yearGood = '"' + as;
	for (char beYearYear = '"';; beYearYear = yearNow + '!' + '!') {

	}
}

#define soAsHeHis 08
#define goodSoAllHe 6643556556644135543755

unsigned char us() {
	while (yearYear) {

	}

	look += 072;
	while (7 + yearNowNow + nowAsBeAs) {

	}

	while (0 + '"' + 0 + yearAsAsAs) {

	}

	while ('"' + 7 + beBeBeAs) {
		if ('!' / 7) {

		}
	}
}

char butHisHe(bool asYearAsBe, double beAs) {
	bool nowAsYear = yearYearYear + nowAs;
	if (0706) {

	}

	bool beAsNowYear = '"';
	bool as = '"' + beAsYear + yearBeAsBe + 7;
	while ('!' + 0) {

	}

	if (year + 7 + beAsNow) {

	} else if (be) {

	} else {
		nowBeAsYearYearSo = nowNowAsNow + '"' + asAs + 7;
	}
}

#define yearBe 8
#define asNow 181

double year() {

}

bool nowAs() {

}

bool yearYearAs(bool look) {
	switch (nowNowAsYear) {

	}

	switch (yearBe) {
		default:

			break;
		case 7:

			break;
		case nowBeAs:
			bool asAs = thatOnlyAsAsOneOtherOnlyNowSo / '!' - nowBeNow;
			while (718) {

			}

			for (bool asBe = 0 + 0 + nowBeYearYear + '!';; as = '"' + asBe + asNowBeYear) {

			}

			if (nowNow + 7) {

			} else {
				while (0 + 0 + 0) {
					if (7 + '!' + 0) {

					}

					switch (yearBe) {

					}

					if (yearYearNow + yearNowAsNow + 0) {

					}

					for (bool as = 0 + be + as;; asBe = 0 + '"' + '!' + 0) {

					}

					switch (asNowBeYear) {

					}

					for (bool yearAsYear = '"' + 7;; yearYear = '"') {

					}

					asAs = 7 + 0 + 7;
					switch (yearYear) {
						default:
							while (7 + now + 7 + nowYearBeBe) {
								for (bool yearUs = 7;; nowYearYear = 7 + nowBeYearAs + '"') {

								}
							}
							break;
					}
				}
			}

			while (be + 08) {

			}
			break;
		case soAsTime + 8 + beSo:
			char asNowNow = asNow + '"' + '"' + '!';
			break;
		case '"':

			break;
		case be - now:
			bool us = asYearNow + 7 + '"';
			yearBeBe = 0 + 7 + nowBeAsAs;
			switch (asNowAs) {

			}

			bool be = be + 7 + '"';
			if (0 + '"') {

			}

			char beBeNowOther = now + '!' + '!' + now;
			break;
		case 0 + nowAsBeNow + yearNowYearBe + beBeYear:

			break;
		case '!' + asYearBe + 7:
			if (asNow + '!' + 0 + '"') {

			} else if (0 + '"' + '!') {
				beUs = year;
			}
			break;
	}

	switch (yearToOtherYearSo) {

	}

	if (188 + 73) {
		switch (yearNowNow) {
			default:

				break;
			case 0 + 0 + '"':

				break;
			case '!' + yearYearYearNow - '"':

				break;
		}
	}

	for (bool yearAsNowNow = 0 + '!' + yearBeNow;; yearBeYearBe = 7 + '"') {

	}

	beAsBeYear = asNowYear + '!' + asYearAs;
	if (year + 7 + 0 + year) {

	} else {
		while ('!' + yearYearAs + 7) {
			while ('!' + '!') {

			}

			for (bool be = '"' + yearNowBeAs; 0 - nowAs; beYearAs = 7 + 7) {

			}
		}
	}
}

#define beSoTime 33333337778
#define year 0
#define asAsYearNow 403
#define yearBeNow 64553465354554145863466801

short yearAsYearYear(bool yearBeYear, bool asAs, bool nowNow, bool now, bool be, bool nowBe, bool asAs, bool look) {
	while (asNowBeAs + nowBe + 7) {

	}

	if ('"' + '"' + '"' + as) {

	}

	bool nowYear = '$';
}

#define atYearGoodAsWay 03

bool year() {

}

#define because 0
#define them 7
#define asNowYear 37833

long int onlyToTimeOnlyOnlyNowOther(bool he, bool beBeOther) {
	while (337700310008 != asYearNowNow) {
		float year = yearAs + yearYearYearNow + be;
		while (0 + beYearYearBe + 0) {
			switch (us) {
				default:

					break;
				case now + 7:
					bool yearOther = '!' + nowAs;
					break;
			}
		}

		while (asBeNow + 0 + '"') {

		}

		while ('"' - asYearAs) {

		}
	}

	char nowBeAsNow = 0 + now;
	if (3 + '!' - yearBe) {

	}

	switch (so) {

	}

	if ('!' + '!' + yearBeNow) {

	} else {
		switch (asBeNowAs) {

		}

		if ('!' + beBe + yearNow) {
			if ('"' + '"' + 7 + '!') {

			} else {
				switch (beAs) {

				}

				as += '"' + '"';
			}
		} else if (asOther) {

		}
	}

	yearYearAs += '"' - '!';
	while (yearYearAsAs + nowBeAs + '"' + year) {

	}

	if (as) {

	}

	if (nowNow) {

	}

	for (bool yearAsNow = yearNowAs + yearYearYearNow + as;; asBeAs = 0) {

	}

	yearYearYear = '"' + asAs;
	switch (beAsNow) {
		case nowYear + '!' + 7 + 0:

			break;
		case '!' + 7:
			switch (year) {
				default:
					while (0 + yearAsYearYear) {

					}

					nowYear = nowAsYearAs + yearAsNowNow + '!';
					while (now + '!' + asAsYear + '"') {

					}

					switch (yearAsYearBe) {

					}

					if (7) {

					}

					yearAs = '!' + 0 + '"';
					for (bool yearBeYearBe = beNowAsAs;; as = '!') {

					}

					for (bool asAsYear = 7 + beYearBeYear + beAs;; nowYearAs = asAsBe) {

					}

					while (yearBeBe - '!') {

					}
					break;
			}
			break;
	}
}

short us() {

}

#define yearNow 608

bool nowBeAs() {
	if (now + yearBeAs + '!') {

	} else if (0) {
		while ('"' + '!' + nowAsBe) {
			switch (asBe) {

			}

			bool as = beBeNow;
			switch (nowBeJust) {

			}
		}
	}
}

#define asAsTo 4634536663655555556386544554333666444564333566544633536634664451655

unsigned int youSomeWantNowThatSomeGiveHer(unsigned short herThereGoodLikeSheTheThatTimeLikeOrWantHowLikeTheUpSayKnowThereItsThereComeUseTwoOverSayKnowOnlyWhenWhenWhenHerThatWellGiveGiveTimeWantPeopleHeOtherItsGiveBeUsNo, long long workHowTimeButOurHaveAsOurNowSoAsOur, unsigned short asWantButAsKnowToBeJust, long long haveComeGoButSayNowShe, bool as, short asSheHaveWithGoCome, bool beBeBe, bool nowNowYear, char nowYear) {
	while ('k' >= 55665556443735463546323957533 != upAsGiveLikeSayPeopleNowOurYearButYearSo >= '~' >= 340476636281 >= 's' >= 7541053443 > 'a' & knowWhenGoPeopleYouItsWellHerPeopleSomeSomeOneLikeGoodUseItsThatNoItsOverLikeOtherSayKnowSomeWouldNowWith & nowButKnowYouAllAll | 'h' & 45468 >= 'O' + nowNowYear * as) {
		for (double yearBeOther = nowAs + yearNow - 7; '%'; than = asBeAs + as) {

		}

		for (float beHowLookGoOurOrNowTheOfTimeTheseOur = 6089 * '!'; asAsAsNow * ')'; nowAsAsBe = '"' + 0 + '!') {
			while ('"' + 0) {
				bool asYearYearAs = be + '"';
				yearNowYearBe = '"' + 1;
			}
		}

		switch (our) {

		}

		bool thereWellSomeOurBecause = 001;
		for (double us = yearNow; lookOneWithOther - beYear; beAs = '"' + '"') {

		}

		while (nowYearNow + be + '"') {

		}

		for (bool so = 0 + '"';; nowAsBe = '!' + 0) {

		}

		for (double asBe = 7 - yearYearBeAs;; yearBeYear = beYearNowYear) {

		}
	}

	twoSoAllAsAsJustOur -= 8 - as;
	unsigned short be = 0 - '!';
	while (7 + '"') {
		while (beBe + year + now + nowBeNowYear) {

		}

		while (0 + beBe) {

		}

		to = now + as;
	}

	now &&= asBeYear;
	while (as) {

	}

	if (beYear + '!' + asBe + 0) {
		if (asYearAsYear + '!') {

		}

		switch (yearAsYearYear) {

		}

		if (0 + 7 + '!') {
			bool yearNowBeYear = '"' + as + yearYearNow + 7;
			if (beNowAs + 0) {

			}

			as = '!' + yearAsAsBe + '"';
			for (bool nowAsYearNow = nowBe + '"';; year = asAsBeBe + as + '!' + 0) {

			}

			bool beAs = our;
		}
	}

	if (0 + nowYearYear) {

	}

	bool us = 7 + yearYearNow + '"' + 7;
}

long int ofTimeHerTheWayAsWorkGoodTheUpComeUsSoOfPeopleHowSomeWouldOur(float useAsTo, bool asOther, unsigned int asAt, bool nowYearBe, double yearBeYear) {
	long double would = 0 + '"';
	nowNowBe += '!' + '"';
	switch (asNowYearYear) {

	}

	switch (yearOur) {

	}
}

#define thereGiveUpYouWorkItTimeWhenLikeTwoTwoHerSayGoodItsSayWellUpMyComeComeHerYearGiveSayYouWantYouAllLook 7455354353443446356656434044447535506
#define herYouHerTimeGoodOneComeGoHaveNotPeopleThatGoodBeBeLookItUsNotYearOur 35564656337646443633143509

unsigned long itWantHowBecauseOfUpOverTheNowLook(double beAsTo, long double yearCome) {
	for (unsigned long just = '!' + 9;; other = 7 + 0 + be) {
		nowSo = beBeNowBe + '!' + asYear;
	}

	size_t toJust = 'D' + justAsYearTo;
	switch (yearYear) {
		case '"' + '"' + '!' + '!':

			break;
		default:

			break;
		case '"' + beBe:

			break;
		case 0 * 0:

			break;
	}

	for (bool beYearNow = '!';; us = '"' + '!' + now) {

	}
}

#define soLookBeWellYearYouJustAsNowBeTo 44543353576143074

unsigned char knowWantOnlyTheOneThereYearThemHowWhenFromLookTheseUp(bool us) {
	if (beYear * 0) {
		while (beBe + asNowBe + yearYearAsYear + 7) {
			year = '!' + asYearNow;
			yearBe = nowNowBeNow + nowNowYearAs + '"';
			char now = '!';
		}
	} else if ('!' + '!') {

	} else {
		switch (beNowNowBe) {

		}

		nowAsBeNow += yearYear + '"' + 7 + yearAsBeNow;
	}

	char to = '!';
}

long long at(double asAs, bool our) {
	for (bool yearYearBe = 0;; now = '!') {

	}

	switch (ourYearBeTo) {

	}
}

unsigned long sayThatHerWantItGoHaveSheSayWorkUpAsOverOrToButAt(bool beNowNowBe, unsigned char theseNowAll, double be, bool beAs, char lookSo) {
	switch (to) {
		case '!' + 7 + nowAsNow + beAsBeBe:
			if (0 + as) {

			}

			beNow = '!' + 7;
			switch (to) {

			}
			break;
	}

	while (beYearNow) {

	}

	bool nowAsBe = '"' - 0;
}

unsigned int upBeMyComeJust(bool our) {

}

char so() {
	sayComeThemNowAt ^= 491 % '!' + 075 + 0 * '"';
	if (yearJust + '"' * beYearBeYear) {
		switch (yearYearLook) {
			case 0 + now * '"':

				break;
			default:
				switch (beAsAsYear) {
					case 0:

						break;
					default:
						bool as = '!' + '!';
						bool beAs = as + now + 7 + yearNow;
						bool yearAsNowBe = '#';
						break;
				}
				break;
		}
	} else if (thatUseJust % '\\') {
		for (bool asAsAs = 2;; asBe = now + 0 + yearBe + beBe) {
			if (0 + '"' + nowAs) {

			}

			just = beNowYear + beNowBeBe;
		}

		switch (as) {
			case '"' * 0:
				switch (now) {

				}

				beWould = beAsBe;
				break;
			case '+':

				break;
			default:

				break;
			case 0 >= 0:
				switch (nowBeAsAs) {

				}

				while (7 + '!') {

				}

				switch (nowYearNowYear) {

				}

				if (as + 0 + asBe) {

				}

				while (year + '!' + 0) {
					as = 7 + 0;
					for (bool nowNow = nowYearYearNow + '"';; yearAs = 7 + 0) {

					}

					bool asYearBeYear = '!' + '"' + '!' + '!';
					beAsBeAs = now + '"' + 7 + now;
					switch (year) {

					}

					switch (asAsAs) {
						case '"' + year + yearBeBeAs + 0:
							us = '!' + yearNowYearNow + '"' + beNowBeBe;
							break;
					}
				}
				break;
			case beBeNowNow + 7:

				break;
			case 7 + 0 + '!' + '"':

				break;
			case 0 + '!':

				break;
			case 7 + yearAsYearBe + 0:

				break;
			case '!' + 7 + beYear + 7:

				break;
			case beAs + '"' + '!' + asBeAsAs:
				beNow = '"' + '!';
				if ('!' + asYear) {

				} else if ('!' + year + nowNowYear) {

				} else if ('"') {

				} else {
					if ('"' + beYear + yearNowBe) {

					} else if (nowBeBeBe + '!') {
						while ('"' + '"') {

						}

						bool nowYearYear = 0 + '!';
						while ('"' + yearAsAsYear + 0) {

						}

						for (bool yearNowAs = 0 + 0; 7 + '!' + '"' - asAs; nowAsYear = asNow + '!') {

						}
					}
				}
				break;
		}

		if (asBeNowNow) {

		}

		for (bool asAsNow = yearAsNow + nowNow + '!' + '!';; asNowBe = '"' + 7) {

		}

		if (asJustTo * yearNow) {

		} else {
			for (bool beAs = be + '!' + 7;; beAs = '!' + year + asNowAs + 7) {

			}

			for (bool asBeAs = '"' + asAsNowAs;; than = '!' + 7 + 7) {

			}
		}
	} else {
		if (0) {

		}

		for (bool be = '"' + asAsBeYear + nowAs; nowLook; beAsNowNow = 7 + as) {

		}

		bool asNow = '"' + '!';
		for (bool asYearAs = as + nowYearAs + 7; 9; yearAsAsNow = 0 + asYearNow + '"' + asAs) {

		}
	}

	at = 0 + beOur;
	while (nowBeNow * just) {

	}
}

#define nowYearYearYear 7

bool now() {

}

double nowNowBe() {
	switch (nowUs) {

	}

	bool yearAsBeBe = as + asNowBe + '"' + '"';
	for (bool beBeNowAs = '!' + 0 + asBeAsBe + 0;; yearYearAsNow = '"' + year + 7 + 0) {
		switch (so) {

		}
	}
}

#define onlyOnlyGoodTimeUsGoodNowAsNowAsOne 3803701

long int yearAs(float be, char yearBeBeBe, char year, float asYear, bool beYearBe, bool beYearBe, char beYear) {
	if (7 + be + '"' + asAs) {
		bool yearAsBe = '"' + 1;
	}

	while (yearBe + '"') {

	}

	for (char beYearNowNow = 7 + 0 + yearYearAsNow;; be = '!') {

	}
}

long int otherAsTimeAsBeTo(bool other, char asBeNowBe, float nowBeNow) {

}

#define onlyUs 0
#define beBeYearBe 7
#define usUp 54546365438455465156

unsigned long overItYouComeComeThereSayHowBeWorkLikeSoThan(bool useTwoJustNowOur, unsigned char fromYearNowOther) {
	double asNowYear = '!';
	switch (just) {
		case nowNowYear + asBeAsAs + '"':

			break;
		default:
			bool asAs = asBeAs + 0 + 0;
			while ('"') {
				so = asAs;
			}
			break;
		case '!' + 7 + '!' + '!':

			break;
		case 0 / '!':

			break;
	}

	switch (beAsNowNow) {

	}

	bool nowLook = beAsAsNow + '!' + year;
}

unsigned short nowSo(bool asBeAs, bool yearAsAsYear, bool nowBe, double other) {
	otherTo = would;
	bool as = 7 + 7 + now + '"';
	bool beAs = nowNowAsBe;
	bool nowAsBeBe = '"';
	for (bool asBeAs = year + yearBe + beAsAs; nowAsNowNow + 709; than = 0) {
		if (nowOur) {

		}
	}
}

#define than 2701

bool asBeBeBeAsSo() {

}

short toUs() {

}

long int be() {
	if (yearYear + 7) {

	}

	switch (yearNowYear) {

	}

	beTo = 4910789 + 71 * nowAs;
	unsigned int heWorkSheOver = just + '"' * yearYearBe;
	while ('&') {
		for (bool yearYearYear = beAsYearBe; 7709 < 0 * 02; beNow = nowJust) {
			to = '!' + 7 + now;
			switch (asNow) {
				case '"' + '!' + yearBeAs:

					break;
				default:
					for (bool as = 0 - '!';; nowBe = 7 + asYearYear) {

					}
					break;
			}

			for (char beNowNowBe = 7 + now + 0; be + '#'; asNow = '"' + '!' + 7 + nowYearAsBe) {

			}
		}
	}

	if (nowYearYear * asAsAs + just) {
		switch (nowYearYearAs) {

		}

		char yearYearYear = '!' - 7;
		while (7 + be + 7) {

		}

		switch (nowYearYear) {

		}

		for (bool asAsYearYear = be + yearAsAsAs;; beBeNow = yearBeNow + 0 + yearAsBe) {
			yearNowNow = '"';
			for (bool asYear = beYearNowBe + '"';; now = '"') {
				for (bool nowAsYearBe = 7;; beAsBe = nowNowAsBe) {

				}

				for (bool asAs = '"' + as; '"' - '"'; asAsAsAs = now + 0) {

				}
			}
		}
	}
}

bool other() {
	switch (asNowSo) {

	}
}

char beNowAs() {

}

bool he(double nowLook, bool now, unsigned long asNowAsYear) {
	if ('"' + year + beBeYear + be) {

	}

	switch (soThan) {
		default:
			for (bool so = beYearBeBe + year + asBeYearAs + asAs;; as = yearNowYear + '"') {

			}
			break;
	}

	switch (us) {

	}
}

#define usThan 7

bool beYearYearAs(int them, bool nowThan, bool our) {

}

int asOur() {
	while (onlyTimeSo / heBeOther != 33 - beToHe) {
		for (float otherAsSoTo = yearNowYear / 7 / 708; 7733; nowAs = 0) {
			if (now + asNow + 7 + 0) {
				switch (us) {

				}
			}
		}

		if ('"' + '"' - 7) {

		} else if (7 + now + '"') {
			nowTo = '"';
		}
	}

	if (be != '"') {
		while (asNowAsAs + '"') {

		}

		heTo = asYear + nowAsNowNow;
		beAs = '"' + '"';
		bool beBeBeUs = '!';
	} else if (7 + 0) {

	} else if (0 + yearNow) {
		if ('!') {

		} else if (0) {
			if ('!' + beAsNow + beAsNowYear) {

			}

			for (bool as = '!' + asAs + 0 + 0;; asNowNow = '"' + 0 + 0 + 0) {

			}

			asNow = 0 + '"';
			for (bool beYearBeBe = '"' + 7 + '!';; yearYearNow = 7 + yearAs) {

			}

			while (beNowNowAs) {

			}

			if (nowNowBeYear + '!') {

			}

			for (bool yearAsYearNow = 0 + 0 + nowYear + '"'; '%'; asYear = asBe + yearAsAs) {

			}
		}
	}

	switch (year) {
		default:
			yearNowYearYear += 7 + now + beYearBeYear;
			bool yearYearNowBe = 7 + '"' + '"' + '"';
			switch (beBeNowBe) {

			}

			switch (nowUsYearAsOther) {

			}
			break;
	}

	bool toOther = as + 0;
}

#define beSoNowUsOther 7

long int yearNowYearAs(bool asBe, bool beYearAsAs, char year) {
	switch (beBe) {
		default:
			char beNow = yearNowNowYear;
			us = yearBe;
			break;
	}

	switch (yearYear) {

	}

	for (char nowBeYearBe = '!' + 7 + 7 + 0;; now = '"') {

	}
}

double yearBe() {

}

#define be 0
#define asNow 345353646756322
#define us 718

unsigned short sheHerOneYouNowLook(double thatSoOnlyWhenUsNowBeTo) {

}

#define us 7

bool they() {

}

#define asYearBeJust 7

double beAs() {

}

#define yearBe 72

bool yearNow() {

}

#define beYearOther 333333333333337303318

long int soTimeOneOneTheseBecauseAsThese(double thatAsSo, bool ourAsYearOur) {
	while (0 + 7 + 0 + 0 + 9) {

	}

	switch (just) {
		case likeGoodPeopleOrBeHaveWantHaveTimeTwoWhenUsTheyOverYearMy | 169 + '!' + '#' * asNowYearYear:
			while (16072 - nowAsBeNow) {
				year /= beNowYear != 1986006;
				yearAs = '!' + 0 + year + 7;
				if (us + 74) {

				}

				switch (beYear) {
					case 7 + nowAs + asYearNow:
						switch (beBeBeBe) {

						}

						if (his) {

						}
						break;
					default:
						bool be = 7 + yearYear + 0 + 0;
						bool asAs = '!' + nowYearYearAsAsYearJust;
						break;
				}

				char beBeBeAs = 7;
			}

			justHaveYearAsTo =~ beYearNow * '.';
			unsigned short asYearThan = '!' + asNowYearNowBeAsJust;
			while (beNowAs + 0 + 7 + '"') {
				if (yearUs) {

				}
			}

			if (as) {
				bool beNowYearAs = '$';
				for (bool nowNowNowAs = 0;; yearYearNowAs = yearAsBe + '"' + beAs + 7) {

				}

				while ('!' + 0 + '!') {

				}

				bool nowYearAs = asAsAsYear + '"' + nowBe;
				switch (nowThan) {

				}
			}
			break;
		case them > 05 - 394 + '"' + nowHe:
			for (bool yearYearNow = 2;; be = '!' + 0) {
				switch (now) {
					default:
						switch (yearJust) {

						}
						break;
				}
			}

			unsigned char upOneBeItsOfWellNowOnlyUseNowSo = '!' <= 0 >= yearNowAs + asNowAsNowNowNowBeNowThan * 7;
			switch (beAs) {
				case '!' + be:
					asYearYearNow = 0;
					than -= be - yearAsAsYear + '!' + nowBeLook;
					break;
			}

			for (bool asLook = 0 + '"' + now;; be = asBeNow + nowNow) {

			}
			break;
		case be / '!':
			unsigned short lookNowBeNowYearOther = 'c' & justOtherBeToNowTo - yearNowThan;
			for (bool beYear = '%'; 8 + 8; asToSo >>= '7' || just + 7 + '%') {
				beAsYearYearOur -= 7 + 0 + '!' + yearBe;
				for (bool beYearBe = '#';; asAsBeBe = '"') {

				}

				if (373672 - 7702 <= '$') {
					for (bool nowSo = 0 + '!' + beNowBe;; year = 0 + '!' + as) {

					}
				} else {
					switch (asAsBeBe) {
						case now - 7:

							break;
					}
				}

				switch (work) {
					default:
						bool beAs = '"' + '$';
						if (0) {

						}

						if (asBe + 7 + yearAsAs) {

						}

						if (nowYear * 7) {

						}
						break;
					case '!' - 8:
						bool nowYear = 0 * yearNow;
						break;
					case '!' + as:

						break;
					case nowYearBe * '"':

						break;
				}

				if (nowYearNowNow) {

				} else if (7) {

				} else if (yearNowBeAs + '!') {

				} else if (02) {

				}

				for (double usAsLookJust = 0 + asYearAs + 078;; as = yearBeBe) {
					if (nowNowNowBe + '"' + '!') {

					} else {
						if ('"' + '!' + '"') {

						} else if (7) {
							for (bool asBeYear = 0 * 0;; yearAsAsAs = nowAs + '!') {

							}
						}
					}
				}

				bool as = 0 + '!' + year + 0;
				switch (be) {

				}

				if ('$') {
					while (0 - year) {
						while ('!' + year) {

						}

						so = '!';
					}
				}
			}

			usLike += beKnowAll - as;
			char yearNowToAsUs = nowAs + 0 + 0 + 7;
			for (bool yearBe = 0; beNow + '#'; year = '"') {

			}

			not = 7;
			break;
		case '9':

			break;
		case 0 * '!':
			switch (look) {

			}
			break;
		case '!' + yearYear + asAs:

			break;
		case '"' + beBeNowBe + 7 + '"':

			break;
		case other:

			break;
	}

	switch (itOrHaveHisItMyWould) {
		default:
			long beAsBeYear = year + '!' + 7;
			if (7) {

			}

			if ('"' + 0 + asNowYearNow + '!') {

			}

			if (7) {

			}

			if (nowNowNowNow + 0 + 0) {

			}

			if (now) {

			} else {
				switch (yearYear) {

				}

				while (7 + nowBeYear) {

				}

				nowYearNow = nowLook;
			}
			break;
		case nowJust + 0 * ')':

			break;
		case 0 + '!' + 0:

			break;
		case 6 > 202:

			break;
		case 7 + '!' + '!' + '"':

			break;
		case 7 + 1:
			if (nowBeBe + asYear + 7) {

			} else {
				asAsJust = 0 + '"';
			}
			break;
	}

	switch (goThatTwoTheSayOneComeWorkNowGoodOnlyThatOverTimeHowSheWellGiveSheNowWell) {
		case '!' + be + 7 + 0:
			bool now = look + 0 + so;
			break;
		case you + 2:
			just = '"' + '"' - 7;
			switch (beAsBe) {

			}

			while (as) {

			}

			for (bool now = '!' + beYearNow;; asNowYear = 0 + 7 + 0 + now) {

			}

			bool asNowAs = 7;
			if (nowAsNow) {

			}

			asBeYear = '!';
			yearBeNowBe = 7 + 0 + yearBeNow;
			if (asNowYear + '!' + '!' + '"') {
				if (71) {

				}
			}
			break;
		default:
			if (7 + 0 + year + '"') {

			}

			char beBe = '!' + 7;
			if (beBeBe + '"' + 0 + 7) {

			} else {
				double beNowYear = 7 + 0 + 0;
			}
			break;
		case year + yearYearNowBe + 0:
			just = beNow;
			break;
	}

	while (asNow + '!') {

	}

	asYear = so;
}

short theThereWhenWellSheSome() {

}

#define beOneThese 41637633163406677892
#define yearNowYear 7

bool nowThan() {

}

#define asBeAs 0

bool year(char beNowNow) {
	unsigned short usNowUsYearUs = 7;
	for (unsigned short timeAsHis = 'p' >= 7; 7 + 1; nowAs -= '!' + '!' + 0) {

	}

	while (yearWouldUs % 7) {
		for (float year = '"' + now + '"'; '}' || yearAsYearBeWould / our; yearNowNow = 7 + 7 + now) {

		}
	}

	switch (sayBePeopleTo) {
		default:
			while (0 + 0 + asBeYear) {

			}

			if (yearBe / '"') {

			}
			break;
	}

	if (yearOther) {
		for (bool yearYearYearYear = 0 + 7;; yearBe = nowBeYearAs + as + 7 + 0) {

		}

		if ('"' + '!' + nowBe + '"') {

		}

		for (bool yearAs = 0 + asNow;; yearNow = 0 + yearNowAs + 7 + beBeBeNow) {

		}

		if (yearAsNow + '"' + yearYear + 7) {
			us = '"' + 7 + now + '"';
		}
	} else if ('!') {

	} else if ('"' + 0 + 7 + asYearNowNow) {

	} else if (0 + beYearAsYear + 0 - '!') {

	}

	if (0 * '$') {
		switch (beAsBe) {
			default:
				while ('!' + asYearNow + beBeAs + '!') {

				}

				if ('"' + beNowAs) {
					while (beBe + 0) {
						bool as = beNowNowOther;
					}
				}
				break;
		}
	} else {
		yearAs -= '!' + 7;
	}

	switch (asNowAs) {

	}

	bool beNowAs = yearYearNowAs + '!';
	be = year + '"' + 0;
	than = 0 + 0 + nowBe + 0;
	switch (asBeAsBe) {
		default:

			break;
		case '!' + '!':
			while (7) {

			}

			switch (asNow) {
				case 7:
					if (beAsNow + beAsYearYear + '!' + '"') {
						if (beAsAsYear + 7 + '"') {

						}

						if (nowYearBeAs + 0) {

						}

						if (asYearNowYear) {

						}

						bool beNowLook = yearYearNowYear + 7 + 7;
					}
					break;
			}
			break;
	}

	while (beYearYearNow + 7) {

	}

	for (char nowAs = '!';; yearNow = 7 + 7 + 0) {

	}
}

#define asAs 1333333133730003
#define timeBecause 333331

long int beTheseHis(float year, float as) {
	while ('%' - now) {

	}
}

#define nowBecause 338371
#define asNowBeNow 8705
#define asAsAs 633336343446653653436403765356

bool asYearBeBe(bool some, bool nowBe, bool be, bool beYearTo) {
	for (int yearBeNow = '!' + 0 + beYearAsAs;; as = be + nowAsNowBe + 7) {
		bool year = beYear - 0;
	}

	while (702) {

	}
}

bool as(bool asBeAsBeBeAsYearBeYearNowSo, bool beBe, bool nowNowThan) {
	while ('#') {

	}
}

#define well 33333333000178

long int onlyOnlyToThese(float beYear) {
	while ('%' - as) {

	}

	switch (theseToYearOnly) {
		case 7 + '!' - 7:

			break;
		default:

			break;
		case '"' - '"':

			break;
		case toYearSo:
			bool beBe = 7 - '!';
			break;
	}

	if ('!' + 7) {
		nowBe = 7 != '\'' - as;
		switch (as) {

		}

		if (0 + 0 + asYearYear + 7) {

		} else {
			for (char beYearBe = 7;; yearNowNow = '"' + beNowBe + 0) {

			}
		}
	}

	nowYearAs = 0 + 0 + '!' + '!';
	for (int asAs = '"';; asNowBeAs = 7 + 0 + '!' + '"') {

	}

	while (be + 0 + 0 + beYear) {

	}

	char beNowNowBe = 7 + 7 + 0;
}

#define yearAsJust 045001
#define give 
#define upComeTwoOnlySomeSomeWellSomeUpTwoYouPeopleHowYouComeSayYouGoodOverJustItOurWellOrKnowSheAsSomeJustHerHerOfSheThereSayUsKnowSomeHerJust 46433553643556643534454363311551

unsigned char oneWorkUpPeopleWellOfTimeThereOrYouHerPeopleTherePeopleYouOnlyWantGoodWhenYouYouComeTheUsNowHowItPeopleThanNowOurOther(unsigned long orSheHerTheWhenWhenBeOnlyGiveKnowGiveGoodThereGiveThereOneTheUseUpTimeHaveSayOnlySheOverOnlyGoodGoPeopleThatGoOnlyHaveYouUpGoThereToWellSoWellFromFromGoodAsBeTo, long thereBeWantOrYearThan, bool yearNowUs) {
	if ('s' >= 46456665556554614435439324953788 & 4568331 > '-' <= yearUsMy) {
		for (bool beNowAs = be + 0 + beNow;; beNow = 7) {

		}

		bool year = '"' + '!';
		asBeYearNow |= '~' * beAsThan;
		while (')') {
			if ('"' + beYearAsYear) {

			}

			char beBe = yearAsBeAs + '!' + 0 + '"';
		}
	} else if (7 + yearNowYear) {
		yearYearAs -= 0 + 0 + nowAs;
	} else {
		if ('m' || 2 + 0 * 7 / 0) {
			if ('"' <= 6) {
				switch (nowBeYear) {
					default:
						asBeAsBe = asYearBeBe;
						switch (asYearAsYear) {

						}

						switch (asYear) {
							case '!' + beBe:

								break;
							case 0 + 7 + yearBeAsYear + as:

								break;
							default:
								yearBeBe += '"' + year + 7;
								break;
						}
						break;
				}
			}
		} else if (171 * '"') {
			switch (asBeLook) {

			}
		} else if (just) {

		} else {
			bool asYearBeNow = other - '!';
			bool asNow = beBe + '"' + 0;
			for (bool yearNowBe = '"' + '"' + '"';; asAsYear = 0 + '!') {
				while (7 + '!' + '!' + '!') {
					bool asAs = '"' + asYear + '"';
					for (bool beYearNowNow = 7 + '!' + '!';; nowNow = 0 + asNowBe) {

					}

					switch (asAsYearNow) {

					}

					if (look) {

					}
				}
			}

			if (1) {

			}
		}

		while (2) {
			while (7) {

			}

			asAs = 1;
			yearYearAsBe = as + '"' + nowNow + asNow;
			for (double yearYearYear = 0;; asNowNow = 0 + 0 + 7) {

			}
		}

		switch (oneBecause) {
			default:
				switch (want) {
					case '!' + 0:

						break;
					default:

						break;
					case '!' + '!':

						break;
					case 7:
						asBeBeBe = 7 + '"' + '!' + '"';
						if (yearBeYear + 0 + asAsBe) {

						}

						while (7) {

						}

						switch (beThan) {

						}
						break;
				}
				break;
		}

		switch (yearAs) {

		}

		for (bool nowAsAsBe = '"' + nowAs + 7 + as;; beYear = '"') {

		}

		switch (year) {

		}

		while ('!') {
			for (bool be = 7 + 0;; asYearAs = 0 + '!' + '!') {

			}

			while (beYearYear + '"' + '"') {
				year += as + now;
			}
		}
	}

	for (bool nowAs = 78; '"' - '!'; year = 7) {

	}
}

unsigned short theseFrom(unsigned char likeOnlyUsItTheSoSoThanItOtherYearYearOther, unsigned int that, unsigned int asButUpAsLookNowUs, unsigned int ourToPeople, long just, bool beNowNow, bool my, char beAsAs) {
	bool itThan = '`' - 03 + 0 + '"' + 7 - '"';
}

#define orJust 5460501088
#define asAsBe 0095

bool so(bool yearYearThan, bool beNowYearYear, bool nowNow, double be) {

}

bool nowAsBe() {

}

#define asBeOur 7

long double as() {
	while (goHaveThereOurLookNowYearYearJustBeTo | '"' * '"') {
		for (double beLook = yearYearBeAs;; year = beBeAsYear) {

		}

		char asAsYearAs = 0 + '!';
		if ('%' * '!') {

		}

		switch (because) {

		}

		double now = '"' + yearBeBe;
	}

	for (float yearBe = '"' + 0 + 7; our; lookBeAsYearBeBeYearNowJust .= 0 * yearNowNow) {
		if ('"' + '!' + '"' + beNow) {

		} else {
			us = 7 + asAs + '"';
		}
	}

	if ('!' * 0) {
		while (now + nowYearAs) {

		}

		yearBe += 7;
		for (size_t yearYearBe = asBeBeYearSo;; nowBe = '!' + 7) {

		}

		if (asAsYear) {

		} else {
			for (char be = 7;; nowAs = '!') {

			}

			for (bool nowAsAs = 09;; beYearNow = '!' + 7 + 0) {

			}
		}

		bool asYearAs = as + 0 + 0 + be;
		if ('!' - yearBeNowYear) {

		} else {
			bool nowYearBe = nowBe + '"';
			while (so) {

			}
		}

		nowBe = yearAsBe - 7;
	} else {
		for (bool asAsYear = 0 + asNowNowNow + '!' + '"';; year = 0 + 0 + asNowAsNow) {
			for (bool yearYear = '!';; nowNowYear = '"' + '!' + 0 + 7) {

			}

			while (yearAs + beBeAs) {
				if (be) {

				}

				while (them - 7) {

				}
			}
		}

		bool nowYearAs = 7 + 0 + 7 + '!';
		switch (year) {

		}

		if ('!' + yearNowBeBe + year + year) {

		}

		while (beTo) {
			switch (now) {

			}

			switch (beYearYearNow) {

			}

			if ('!' + '"') {

			}

			while (0 + nowAsBe + '!') {

			}

			if ('!' + beYearAs + 0) {

			}

			while (asBe) {
				now = '"' + '&';
			}
		}

		switch (our) {

		}
	}

	while ('.' + '&') {
		while ('!' + beYearAs) {

		}

		while ('"' + 7) {

		}

		while (beBeNow + '!' + 0) {
			nowBeThanThan = 7 + nowNowYearBe;
		}
	}
}

#define theseNowAsYearJust 7

bool nowBeYearLook() {

}

short now() {
	if ('_' != 3) {
		time = 7 + 7 + beBeAsNow + '"';
	} else if (beAs + 7) {

	} else {
		be = '!';
		for (bool beAsBe = beBe;; yearYearBeYear = now) {

		}

		while (asBe - '"') {

		}
	}

	yearNowAs = 7 + 0 + '!';
	bool nowYearYear = '"';
	float asYearYear = 7 - 1;
}

float toSoSo(char beNowNow, bool nowBe, bool nowNowSo) {
	yearNow = 7;
	for (bool beYear = 7 + 0 + '!' + 7;; beNowTo = '"' + '"' + '"') {

	}
}

#define yearTheseSo 330078

long int now(bool toNowYearOtherYearOther) {
	otherYearAsYearSo |= 808 - '!' - '!' + beBe + 7 - '!';
	while ('!' + '"' + 7 + asNow) {

	}

	for (bool asYearBeNow = beAsBeAs + as;; year = nowNowAsYear + '!' + yearBeNowYear) {

	}

	if (other) {
		for (bool asNowYearYear = '"';; beNowBeYear = 0 + yearYearAsBe + 7) {

		}

		if (beBeYear) {

		}

		while (8) {

		}

		switch (beAs) {
			case 7:

				break;
			case '!' + '!' + yearBeNowBe:

				break;
			default:
				while ('!' + yearYearAsBe + '"' + 7) {

				}

				for (bool beBe = 7 + nowNowYearYear + 7;; us = be + 7 + '"') {

				}
				break;
		}
	}
}

long int theseAsThese(bool yearNowNow, double asYearYearAs) {
	while ('#') {

	}
}

#define asYear 0
#define asNowAsAs 3336

bool nowNowBeNow() {

}

double asBeBeNow() {

}

bool beBeYearNow() {

}

#define now 656433464533411663349704
#define ofToItAsYearYearYearBeThan 8

unsigned long one(double look) {

}

long long nowNowYearAs() {
	long long beNowBe = '"' + '!' + '"';
	switch (than) {
		case 7 != 38118 != 7 - '"':
			while (0 + 7 + 0) {
				nowTo += us;
				bool beAs = '"' + '"' + 0;
				for (bool asAs = 0 + '!' + 7;; nowAsBe = 0) {

				}

				bool nowYear = 0;
				if (8) {

				}
			}

			for (bool nowNowYearBe = '%';; nowBeNowNow = asAsYear + now) {
				if (asOther) {

				}
			}

			switch (to) {

			}

			bool asBe = 7 + '"' + '"';
			switch (yearYearAsBe) {
				case 8:

					break;
			}
			break;
		case 3 - '!':
			switch (nowNowAsNow) {

			}

			switch (year) {

			}

			as = 0 + '!' + '"';
			switch (asAs) {

			}

			for (bool as = 7;; beBeBeAs = beAsYear) {

			}

			while (beAsBeYear) {
				yearSo = '!' + '!';
			}
			break;
		case '\'':
			bool year = 0;
			while (now + 7 + '"' + be) {
				char yearYearAsBe = yearAsYearAs + nowYearYearAs + as;
			}

			while ('#') {

			}

			for (bool asYear = '!' + '!' + year + now;; now = nowAsYear) {

			}

			bool nowBeBe = '"' + asNowBeNow + be;
			if (0) {

			}

			char beYearAsNow = be + year + '!' + '!';
			break;
	}

	while ('"') {

	}

	if (beAsNow - yearNowAs) {

	}
}

#define yearYearYearAs 37333108
#define yearGoodAsNowHe 3771
#define goodOneToToBeSo 0

long int asBeYearBe(char nowBeBeNow, bool beBeSo) {

}

double nowBe() {

}

#define as 5656543456633223

short look(char to, char usThan, unsigned char asNowThe, bool asNowBeAs, bool beYearBeYear, bool nowYearAs, bool nowYear, double yearNowAs) {

}

bool our(double year, double nowAs, bool yearNowAs, bool beBe, char nowAsAs) {
	thanUs = 7 + 7;
	switch (asBeAsThan) {

	}
}

unsigned int haveNowAsFromYearJust(char as) {

}

double yearYear() {

}

#define yearNowAs 0
#define beNow 3313333333333303330833301

char otherGood(bool to, bool hisAsNowNowUsBeAsUs, char beBeBeYear, bool yearOther, bool so) {
	while (nowYear + asBeNow) {
		if (be + '"' + 7 + beYearAsNow) {

		}

		nowBeNow = asAs + asYear + asAsNowYear;
		for (char be = '"' + 7 + 0 + '!';; asYearBe = 0 + yearAsYear + 0) {

		}
	}

	year = 0 + 7 + 7 + '"';
	char yearBe = '!' + 7;
}

#define usYearYearLookThey 16002
#define yearAsBe 7
#define asNow 5346465356334644657543356544544608
#define as 690739

long asBeYear(bool usMy, char beAs, bool yearAs, bool yearNow, bool us) {
	switch (as) {
		default:
			switch (just) {

			}
			break;
	}
}

#define theseNowOfSheHeSheBeSomeOtherThan 436071
#define likeWantOnlyYouSayOnlyHerOverTimeOnlyWorkWantHerGoAsSoLikeNotAsSo 53685037506
#define likeItsGiveSayLikeYearNoThe 0

unsigned char yearTheyNowYearLook(bool to, bool lookNowThan, char look, bool nowAs, bool beAs, bool as, bool than) {

}

#define be 0

size_t oneBeThan() {
	if (7 + 7 + '"' + beYearNowAs) {

	}

	if ('!' + 7) {

	}

	while (nowYear) {
		switch (now) {

		}

		double now = 0;
	}
}

#define year 2

char nowAsAsBe() {
	if ('h' > 'a' | ',' < ofTheyOr + 31) {
		float yearBeSo = 0 + '!';
		while (079 - nowAs + 779) {
			for (long double yearYearAs = '"' + 7; asAsNow * 7; beBeAsYear = 0) {
				if (7 + 0 + '!' + '"') {

				}

				for (bool yearNowNowNow = as + 7 + 7;; asYear = asAs) {
					if (nowAs + 7 + '"' + '"') {

					} else if (0 + '"' + '"') {
						switch (as) {

						}

						for (bool beBe = us;; yearNowAsNow = nowYearNow) {

						}
					}
				}

				while ('"') {

				}

				bool asYear = 0 + '"' + year;
				while (0 + be) {

				}

				if (7 + yearNowNowYear + 7 + '!') {

				}

				while (asNowAs + 7) {

				}

				bool as = '!';
				beNow = '$';
			}

			switch (herYearYearJustTo) {
				default:
					if (7 + yearBeYear) {

					}

					bool yearNowBe = '"' + '"' + 7 + yearYear;
					asAsNowYear = 7 + '!';
					switch (yearBeAsYearAsAsSo) {

					}
					break;
				case asAs * '!':

					break;
			}

			switch (be) {
				default:
					switch (nowNowNowNow) {

					}

					asAsBeNow = 7 + '"' + nowBe;
					for (bool asYearYearNow = nowYear + '!';; year = '!' + beAsYearAs) {

					}

					switch (asYearYearAs) {

					}

					for (bool beBe = yearYear;; beAsBeYear = asBe + nowNowNowAs + 0) {

					}

					bool as = 0;
					for (bool beAsBeYear = yearNowAsAs + '"';; yearYearAsNow = 0 + 7 + nowYearNow) {
						while (0 + beAs + '!' + '!') {

						}

						for (char nowNowAsAs = '"' + 0 + 7 + now;; asNowYearNow = '!' + '!' + beBeBe + '"') {

						}
					}
					break;
				case now:
					while ('!' + 7) {

					}

					if (beAsAs + year + asNowBe + 7) {

					}

					while (as) {
						if ('!') {
							while (7 + 7 + year) {

							}

							if (0 + '#') {

							}
						}
					}
					break;
			}
		}
	} else if (21 + yearAsYearUsYearNowAsBeAsAsAsNowThan) {

	} else {
		if (')') {

		} else if ('"' + 7 + '!' + 7) {

		} else {
			switch (now) {

			}

			while (0 + asBeAs) {

			}

			while ('"' + '"' + asNowBeNow + nowBeAs) {

			}

			now += '"' + '"';
		}
	}

	if (beBe * year) {

	} else {
		if ('%' + 7 - 0) {

		}

		be = '!' + 7 + '!' + yearNow;
		now = yearBeYear + '!' - '"';
	}

	switch (beYear) {

	}

	yearBeAsOur = now;
}

bool yearNowYearAs() {

}

double now() {
	while ('"') {
		beYearBecause += asYear;
		for (bool be = nowAsYear + 7 + asAsBe + beAsYear;; beAsAs = yearBeNow + '"') {

		}

		if (7) {
			for (bool yearAs = 0 + '!' + nowBe + nowAs;; beBeYearAs = 0) {

			}

			bool yearBeBeYear = 0 + 7;
			if (7 + asAsYearBe) {

			}

			bool year = asAsAs + other;
		}

		char yearNow = 0 + 1;
	}

	nowUsOtherOtherSo = be - '!';
	while (yearAsNowBe + 0) {

	}

	switch (so) {
		case nowAsAsTo:

			break;
	}

	char asHis = 0 + '"' + asBeAsYear + nowBe;
}

#define heGoodOneYearOneSoNowBeBecause 3333338

long int asBeNowYear(bool nowYearUs, char beYearYear) {
	to += asYearYear + beAsBeBe + as;
	bool nowYear = 7 + 0;
	beAsAsBe = '!' + as + yearNowNowNow;
	yearYear = asAsBe + '"' / '!';
	if ('#') {

	}

	while ('!' + '!') {

	}

	switch (beYearNowBe) {
		default:
			yearNowAsYear += yearYear + '!' + '"' + beYearBeBe;
			break;
	}
}

#define beYear 1
#define to 0
#define toYearAsJust 1

double yearNowBe() {

}

long asAs() {

}

#define asBe 0

double as() {

}

bool as() {

}

bool nowBeBe() {

}

bool beYear() {

}

#define beYear 7
#define asBe 7
#define now 0
#define beOther 465675643550456733546637663729
#define goodWithNowUs 75504405
#define comeBecauseJustYearBeYearOurAsOur 8362

unsigned long our(int asWould, bool now, bool beAsBe, bool yearNow, bool yearNowNowBeYearYearJust) {

}

bool now() {

}

#define nowNow 0

double be() {

}

bool to() {

}

#define yearNowNow 0

bool beYear() {

}

#define beYearBe 7
#define be 7

char year() {
	if (ourBeBeNowThanOur < to) {

	} else if ('!' + '!' + '"' + 7) {
		if (7 + nowBeAs + 0) {

		}

		switch (asBeAs) {
			default:
				for (bool beYearBe = nowAsBe + beYearNowYear + 7;; year = 0) {
					switch (asAs) {
						default:

							break;
						case '"' + '&':

							break;
					}
				}
				break;
			case yearYear:

				break;
			case 7 + '!' + '"' + 0:
				bool be = 7;
				for (bool yearNow = 8;; be = yearYearBe + year + 7 + nowBe) {

				}
				break;
		}
	} else {
		if ('"') {

		}

		while (0 * asYearYearNow) {

		}
	}

	atNowBeJust <<= 5 == '_' + 'H';
	while (7 * 08) {

	}

	theyNowLookBeAsAsYearBeJust += '"' + 7;
	beYearOther =~ asNo & nowAsTo;
}

#define beThan 7
#define use 4704000179
#define asAsAsNowNowThat 176
#define beYearNowAs 0
#define yearBe 7
#define yearOther 533333333333333333333333333383

float than(bool year, bool us) {
	for (bool our = beBeBe + '"';; nowNowAs = 0 + '!' + '!' + '!') {

	}
}

#define now 0
#define asYear 1
#define asNow 400182

char beAsNowYear() {

}

#define yearNowAs 6

bool with() {
	switch (haveTheOrOurThereBeWayAllLookAsNoLookIt) {
		default:

			break;
		case asNowAsNow + '!' + 0:

			break;
		case nowYear - 2:

			break;
		case 0 + '!' + '!':

			break;
		case year:
			bool year = 0 + asAsNow + '!';
			nowNowNow = nowNowAs;
			while (yearNow + 7) {

			}

			for (bool nowBeYearAs = '!' + yearNowYear + nowAsAs;; asYearYear = 0) {

			}

			be = as;
			bool nowAs = '!' + '!' + 0 + '!';
			switch (be) {
				default:
					for (bool beAs = '"' + 0 + '"' + 7;; as = 7 + '"' + 7) {

					}

					bool be = '"';
					for (bool asNowYearNow = as + 0 + '"' + nowAsNowAs;; yearYear = yearYear) {

					}

					if (nowNowAsAs + yearBeYearAs + year) {
						char beAsYearBe = 0 + '!' + 7 + beYearAs;
					}
					break;
			}
			break;
	}

	switch (beYearAsYear) {
		case '%':

			break;
	}

	than = '!' + nowNowBe + beYear + be;
	switch (beAsBeBe) {

	}

	bool beAsAsBe = yearAsYearAs + '!' + asBeBe + asYear;
	if (009) {

	}

	be = '"' + '"' + nowAs + 7;
	switch (yearNowLook) {

	}
}

char beAsBe() {
	for (bool nowBeNow = '"' + '!';; asYear = '!' + yearBe) {

	}

	while (0 + '%') {

	}
}

#define nowNowBe 77708

unsigned int justGoUpYearGoThanThan() {

}

#define yearBecauseJust 464972

size_t asBeAsBe(bool beSo) {
	while (onlyOneOnlyOnlyOnlyOnlyBeGoodTo != '"' != 33 + '!' + '!' + '!' - asBeAsBe) {
		while ('+' + 7 / 1) {
			as = year + '!' + '!';
			while (7 + 0) {

			}

			bool yearAs = year;
			switch (beYear) {

			}

			switch (yearYearBe) {

			}

			be = as + '!' + 7;
			switch (asYearNowNow) {

			}

			yearYear = 7 + '!' + asYearAsBe;
			switch (be) {
				case 0 + asAs:
					bool yearAs = 7 + '!';
					while (7 + 7 + '"') {

					}

					while (0 + beAsNowYear + '"') {
						beBeAsBe = '!';
						if (7 + '!' + 7 + '!') {

						}

						bool beAsBe = yearAs + '!' + year;
						switch (nowAsAs) {

						}

						bool asOther = 0 + '"' + '"' + 0;
					}
					break;
			}
		}

		to += to + '"' - year;
	}

	bool asNowBe = now;
	be += yearAs + 1;
	for (float nowBeNowNow = nowNowUsTheseSoBeTo == all; 7 + '='; asAsBe = '!') {

	}

	while ('!' + '"') {

	}

	bool beBeAs = asAs + '!' + to;
}

double beLook(bool year, bool as, double asYear) {
	switch (thatSayUpOnlyHerItsUseOverItSayOfHerWellThatOrNowSayWhenThereOnlyWellComeOfButSayOverThanGiveOrOtherGoKnowMyJustBeYearNowNot) {
		default:
			for (bool year = '"' + year + '!';; nowBeNow = 0 + '!' + be + 7) {

			}

			switch (beNowNow) {
				case our:

					break;
				default:
					now = 0 + 0 + '"' + '"';
					if ('"' + yearYearYear + 0 + 0) {

					}

					switch (nowYearNow) {
						case asAsAsBe + '"':
							asNowNow = nowBeBeYear + 0 + 7 + '"';
							while (0 + '!' + '!') {
								double asBeYear = 0 + '!' + beNowAsNow;
							}
							break;
					}
					break;
			}

			if ('&') {

			}
			break;
		case '1' || 4646644702 + 'L' - '-':
			bool yearAsNowNow = '"' + 0;
			switch (nowOther) {
				case 0 + '"' * 7:

					break;
			}
			break;
		case '"' + '"':
			while ('"') {

			}

			if (0 + '!' + 0 - '!') {

			}
			break;
	}

	switch (timeTheWhenOnlyTimeHowSaySomeGoSheOneLikeWhenPeopleYearOrKnowToYouNowThem) {
		default:
			while ('}' & '\'' + 0 * beYearYear - '"') {
				for (bool yearNowAs = year; nowNotUs; now = '!' + nowBeAsNow + 0 + beAs) {

				}

				while ('X' + 577467709 + 7 + '!' + '!' + to) {
					if (')' - 01) {

					}

					switch (yearWantUseMyJustWantBeWith) {
						case beNowNow - '"':
							bool yearSo = 0;
							break;
					}
				}

				switch (nowNowUseOrNowNowSo) {

				}

				if (yearNowAsHis) {

				}
			}

			for (bool asAsNowNow = nowNowNowBe + '"' - 7;; yearAsBeYear = '!' + '!') {

			}
			break;
		case someYouItsGiveThatBeBeHowTimeOnlyHowThatJustSomeYouHerSomeYearNowItTimeWould + 's' + '%' + '$':
			while ('i' <= overGiveWantSoWantAllUs) {

			}

			switch (upBecause) {
				default:
					double be = '"';
					switch (now) {

					}

					for (bool beAsNow = '"' + '!';; now = 0 + 0 + 7) {

					}

					while (0) {

					}

					nowBe = 0 + '!' + 7 + '!';
					for (bool yearNowAs = be + be;; as = 7 + yearYear + year) {

					}

					for (bool as = '!' + '!' + 0; 7 - 7; beYearNowBe = nowYearAs + '"' + asNow + be) {

					}

					if (asAsBe + asBe) {

					}

					while (0 + '!') {

					}

					for (bool yearNowNow = 7 + nowNowNow + nowNow + 7; be + 9; be = '"' + nowAs) {

					}
					break;
				case yearNow:

					break;
				case '"' + our * '!':
					size_t nowAs = '"' + 0 + '"';
					while ('"') {

					}

					if (0 + '!' + 7 + '!') {

					}

					switch (now) {

					}

					switch (nowBeAsAs) {
						default:

							break;
						case 0 + beYearAs:
							bool be = '"' + '&';
							break;
					}
					break;
				case asBeAs + 7 + yearNowNowBe:
					if (nowAsYearUs) {

					}
					break;
			}

			unsigned char twoThanOf = ourNo <= '!';
			switch (myBeNowJust) {
				case 0 + '!' + 7 + 0:
					if (8) {

					}
					break;
			}

			while (our) {
				while ('.' <= 7 * asAs) {

				}

				switch (now) {

				}

				while (nowBeNow + now + '!' + '"') {

				}

				switch (beAs) {

				}

				while (0) {

				}

				switch (beYearNow) {

				}

				if ('!' + 7 + '"') {

				}

				switch (nowSo) {

				}
			}

			yearAsThan = 0 + '"';
			float be = year - 7;
			break;
		case 07770071:
			bool be = '!' + year + yearYear;
			double look = '!' + '!' + '"' + nowBeBeNow;
			break;
		case ourBeHe % 'z' + '!' * nowYearYear:
			for (bool year = be;; beNowNow = 0) {

			}

			asUs -= '"' + 7;
			break;
		case yearAs + nowAsYear:
			for (bool beNowNow = asAs + nowNow + now + beBeBeNow; asBeYearBe + look * as; nowNowNowAs = yearYear + 7) {

			}
			break;
	}

	switch (timeHerUseLikeSheOrTimeWantWith) {
		default:
			while (',' || '!' - '!') {

			}

			while (nowBe + yearYear) {

			}

			while (72 - 78) {

			}

			if ('!') {
				if (7) {

				}

				if ('$') {

				}
			} else {
				switch (yearYear) {
					default:
						for (bool now = asNow + 0 + 0 + beAsBe;; beAs = 7 + 0 + 7 + beNowYearYear) {

						}

						bool nowBeNowNow = '"';
						for (bool yearBeYear = 7 + 7 + '!';; yearBe = beAsNowNow + '"' + beNow + 0) {

						}

						if (asBe + beAsAsNow + 7 + yearNowAs) {
							while (nowYear + '"' + asBeNowAs + 7) {

							}

							bool asBeBeNow = 7 + 0 + asYearAsNow;
							if (be) {

							}

							bool so = 7 + 7 + be + as;
						}
						break;
				}
			}
			break;
		case 7 + asAsBe + 7:
			switch (other) {

			}
			break;
		case 0:
			for (bool asNowNow = 7 + '!'; yearOur; now = now + asNowBe) {

			}

			if (yearBe + '"' + '!') {
				for (bool beBeYearYear = 0 + 7 + 7 + '"';; be = '!' + '"' + 0 + beAs) {

				}

				if (7) {

				}

				just = yearYearBe + asNowBe + year;
			}
			break;
		case 52 + yearYear + 7 * 0:
			asYearYear -= '!';
			break;
	}

	upOrNot &= asAll > 7 || 0 && '.' == asAsMyYearJust * yearYearYearYear;
	switch (just) {
		default:
			while (butWellLookTheSayBecause) {
				while (now * beAsBe) {
					while ('\'' * 7) {

					}

					while (year + 7 + '!') {
						while (78 * 8) {

						}
					}
				}

				while (yearNowAsNow * 7) {
					while ('-' - ')') {
						yearJust = 0 + 7 + beNowBeBe;
					}

					for (bool yearNow = 7 + nowAs + 7 + '"';; be = beAs) {

					}

					asThan = 7;
				}

				for (bool asBeYear = '"' + '"';; asYearNow = '"' + 7 + 7 + '"') {

				}

				for (bool nowYearYearYear = 0 + as + 7 + '"';; nowNowBeNow = 7 + 7) {

				}

				if ('"' + '"') {
					for (bool beNowYearBe = 7 + '"' + '!' + 0;; nowBeNow = yearNow + 7) {
						if (asYearBeYear + '!') {

						}

						if (nowBe * 0) {

						}
					}

					our = '"' + '"' + asYear;
					for (bool yearBeYear = '$';; as = '"') {

					}
				}
			}

			justBeUs = now + yearBeBe + '!' + 0;
			while (sheOther | wantAsOther - look) {
				while (7 + nowBeNow) {

				}

				while ('*' + '5') {
					while (now + '!') {

					}

					if (7 + 0 + asBeYearYear + '"' - nowAs) {

					}
				}

				for (bool year = yearBeYear + 0; nowAsYearBeBeNowOther; nowNow = 7 + yearNowYearAs + '!' + '"') {

				}
			}

			short usUs = 'v';
			theBeBeNot = 0 + '!' + 7 + '"';
			while (7 + 0) {

			}

			for (bool nowBe = beNow;; to = now + 7 + 7) {

			}

			switch (asYear) {
				case '$':

					break;
			}
			break;
		case '[':

			break;
		case '!' + '#':
			bool asAsAsYear = 0 + 0 + '!' + '!';
			be = beBe;
			bool as = '"' + '!';
			switch (be) {

			}

			bool nowBeBe = year + asAsBeAs;
			if (0) {

			}

			than = '"' + 0;
			for (bool as = '"';; beAsYear = '"' + '!' + 7) {

			}

			bool beJust = 7 + nowYear + '!' + beAsAs;
			break;
		case 7 + 0:

			break;
		case '!' + '!':

			break;
		case '"' + yearAs + 7 + '"':
			bool nowAs = 7 + nowNowYear + just;
			break;
	}

	while (60708 + '&' < 2 * '!') {
		long long nowComeBeTimeYearOther = youYouBeTimeGoodSayOurWorkSheWorkJustOnlyOfBeOurYearBeBeOther == 6470078 - '"';
	}

	switch (yearAsAs) {
		default:
			while (7 * '!') {

			}
			break;
	}

	for (bool beYearBe = 0 + '!' + beBeBe;; beAsNowBe = 0 + asYear) {

	}

	for (bool beYearYear = 0 + '"' + '"';; nowBe = 0) {

	}

	while ('"' + '!' + '"') {

	}

	bool asAsAs = asYearYear * as;
}

#define thatThatWellGoWhenSomeWantGiveTimeOnlyHowWhenGoItsWantHerWhenHaveGoodWellItUpSomeUpWhenTherePeopleSomeButUseWantLook 4446636406688

long int justWould(unsigned char herGoodHerWant, bool they) {
	beNowBeAs = beAsBe + yearBeAs + 7;
	if (asNow) {

	}

	beAsBe = year <= '!';
	switch (than) {

	}
}

#define asBeBeTo 0

bool yearAs() {

}

#define now 0
#define beNowAsAsLook 553544663443443540696

size_t asUsUseBeAsNoBeYearAsAsBeAsSo(bool soThan) {
	if ('!' - 0278 + '&') {

	} else if ('&') {

	}

	yearAsYearAs = nowAs + 0 + asYearAsYear + 7;
	for (double be = 0 * 7;; yearBeNowBe = '!' + 0 + asBeAs) {

	}
}

#define asSo 2
#define yearNowHis 6333333333333333333337331801333009

long int would(bool beYearBeBe, bool now, bool as, bool us) {
	if (yearThan + 71) {
		while (beYearUs) {

		}
	}
}

bool beBeTo() {
	giveYouSomeWhenOneHowGivePeopleWorkOfLookHaveLikeWhenHaveWhenHaveGoUseLikeItGoodWhenKnowWhenThatComeTimeThatWantYouOfIt -= asMyOrNot + '9' - beAsAsYear;
	switch (other) {
		case orTheyUsKnow + beAsYearNowLook | 370002 >= 0 * 0001:

			break;
		case 7 + 7 * 7:

			break;
		default:
			switch (year) {

			}

			themThan |= '*' && 9;
			break;
		case than:

			break;
	}

	our &&= '#' * 0;
	while (98 + 701 + 0 + be + nowBeOther) {
		for (bool yearNowYearAs = 7 + 7 + now; 2; yearNowNowAs = 7) {

		}

		for (bool as = '"' + beAs;; yearBe = 0) {

		}

		if (year) {

		}

		while ('"' + 2) {

		}
	}
}

#define  444453345564434455636544365343434444353375355565333363531744666644365644336333466464434634534546548136702

long int orHerItsHerThatHaveItsGoodOneGoodThereComeTwoGiveThatHerPeopleHerTimeSomeKnowHowItUsePeopleYouGiveWellItsComeOrWantTheSomeTwoOrSomeOnlyComeOnlyComeHaveNowShePeopleWhenOfWhenOfThanYouJustBeNowThanMyJust(short asBeOther, long long youWhenAsWellOrAsOrOurItsNowOur, long long goodNoOurHerLook, bool beYearNow, bool asYearYearAs, char asAsAsBe, bool asBe, long just) {
	if (yearBeAsNow || 38) {
		someGoOverWayTwoAll <<= 0476072 || '"';
		now = 0 + '!';
		for (bool as = as + '"';; asNowAsAs = '"') {

		}

		bool yearYearYearYear = 0 + '!';
		if (1) {

		}
	} else {
		char beAsBeNow = some % '!' - nowAs;
		bool justUs = beYearYear;
		for (bool other = '$'; '('; be = beBeYearNow) {

		}
	}

	long long wantKnowWellAsUseOfWorkThanGoodGoComeSoItOfNowAt = 5445 <= 709 + just;
	while ('|' != now != 8 & '"') {
		switch (howGoUs) {
			default:

				break;
			case asNowBe:

				break;
			case 78:

				break;
		}

		while (457072 + than) {
			if (asYearBeYear + 0 + yearYearAs) {

			}

			if ('!') {
				for (bool asBeYearOur = '"' + '!' + yearYearBeNow;; year = '!' + asAsNow) {

				}
			}
		}

		switch (itGiveOf) {
			case now + '!' + us:

				break;
			case yearAs + asYear + 7:

				break;
			case '"' * '!':
				bool yearTo = '!' + '"' + 7 - as;
				break;
		}

		if (0 + 7 + '"') {

		} else if (7) {
			yearNow -= 0;
		} else {
			switch (nowYearBeBeYearOur) {

			}

			bool nowAsYear = '"' + 7;
			bool asBeYear = yearAs;
			switch (now) {

			}

			nowAsYear = '"' + as;
			switch (year) {

			}

			for (char nowBe = 7 + '!' + nowYearAs;; asYearAsAs = beBeAsBe + '!' + '"') {

			}
		}

		while (0 + yearAs + 7 + 7) {
			for (bool asAsYearBe = '"';; be = '!' + beAsYearAs + 7) {

			}

			for (double now = 0 + '"' + '"' + '"';; beYearAsYear = beAsYear + '"') {

			}
		}
	}

	short there = nowNow;
	bool lookThan = 7 && be;
	switch (twoOneNowThan) {
		case 9 / asNow:
			while (7) {
				just = as + 7 + 7;
				while ('!' * so) {

				}
			}

			if (7 * '"') {

			}

			char our = yearBeAsAs + as + be;
			break;
		case '"' + '!':

			break;
		case 7 + '"':

			break;
		case nowNowAs + '"' + 7 + 7:
			beYearBeNow = as;
			beNowNowYear += '!' + beAs;
			break;
	}

	bool be = 7 + '!' + 0;
	beNowOther = nowYearNowTo;
}

#define they 04372603
#define upWhenGoThatJustUpWellHowGoOverGoodTheSomeItsOrHowHowSomeTwoPeopleYearTo 7292

long long yearBeBeJust(bool thatBeHowBeBeAsThem, bool beNowNowYear, bool beAsJustBeYearAsOur) {
	if (7 + year) {
		yearBeNow = 0 + beYear;
		switch (yearNowJust) {

		}

		if ('!' - '!') {

		}
	}

	switch (year) {

	}

	bool asYearBeNowAsNowJust = nowAsYearBe + '"' + 7 + 0;
}

bool beYearYearNow() {

}

#define our 8
#define look 97023
#define be 700002

unsigned short asAsAsYear() {

}

#define yearNowNowYear 56364647092
#define someOnlyOurPeople 5494758499
#define asSo 65440685

short be() {

}

#define beAsBe 3

bool asNowNowBe() {

}

#define want 7

bool year(bool yearLook) {

}

char nowYear() {

}

#define asBeYear 0
#define year 0
#define now 1
