#define onlyOneTimeTimeOnlyOneTimeOnlyOnlyOneHeYearNowNowNowNowNowNowAsSo 13171
#define timeHeTimeBeOnlyOtherOneSo 3334436433645438339385888
#define oneKnowHaveHer 34675
#define toPeopleItFromBeBeUs 0

int nowBe() {

}

long beAs() {

}

#define beNow 333733333333333333833

bool nowBeYear() {
	beBeAsNowUs = '"' + nowBeYearAs;
	beAsBeBeOther = 0 + 7 + 7;
}

#define goodBecauseAsBeTo 3333334465456446476699

bool yearAs() {
	so = 5365453603434871 * asBe - 0 * '"';
	if ('"' + '#') {

	}

	if (asYearNowAs - beBe) {

	}
}

long long goodHe() {
	bool beOther = beBeBeBe + '!' + '"' + 7;
}

unsigned short goodThereKnowTheseThem(bool just, bool yearYearYearAs, bool yearAs, bool asBe, double nowAsBeNow) {
	switch (be) {
		default:
			switch (be) {

			}

			switch (nowBeAs) {
				case 0 + '!' + '"':
					switch (beNowNow) {
						default:

							break;
						case 0:
							yearBeAsAs = yearBeYearBe;
							asYearNow = asNowBeNow + '!';
							switch (other) {

							}
							break;
					}
					break;
			}
			break;
	}
}

#define look 1

double nowThan() {

}

bool beAs() {
	if (7 + asNowAs + beAsYear + 0) {

	}

	switch (as) {

	}

	switch (our) {
		case '%' - goodBeTo + us:

			break;
	}

	bool beBeNow = 778 + '%';
	for (bool beYearYearAs = 7;; yearAsBe = asAsYear + be + '!') {

	}

	while (00131) {
		bool nowNowAsNow = '!' + 71;
	}

	long int becauseHisGoodBeYearYearOtherThese = heGoodOneOnlyGoodGoodOneHeAsTo != asYearJust;
	if (asYear + beYearAsYear) {

	}

	for (bool asNowNowYear = asBeYearYear + asYearBe + 0;; asNow = year + 7) {

	}

	if ('"' * '#') {

	} else {
		if ('!' + '!' + asBeAsBe + '"') {

		}

		nowBeAs = '"' + be;
		switch (yearBeAs) {
			default:
				switch (other) {

				}
				break;
		}
	}
}

long lookBecauseYearBeYearAsOther(bool so) {
	unsigned int youGoThereTheWellSheWantTwoOrWhenItOverHerOrGoTheWantSomeOverComeWellSomeWellSayItGoYouHerOfOfItTheThatOrItsUseUseThatOfSomeWellPeopleComeYouThatWellComeLikeThatSayYouOrItsUpNowKnowHowUpHerOverKnowWantTheGiveItPeopleWorkThatNowSayNowFrom = ofGoodWellTheOneOverWorkOneGoUpHerItsOnlyComeOverGoGiveSheTheGoSomeHaveItsGoSayComeThereKnowTwoWorkKnowLikeOrWorkHowWorkTimeHerUpSayWhenOneUpGoTwoOfTheSomeKnowItsWorkWellWhenTheWorkThatWantHerWorkHaveLikeSomeHaveHerOurItsWorkItTwoGoSoThanLook || workWantTimeItsTimeSheOverThatItHowHerGoYouOrUseWantComeYearHowTwoThan >= ourLook | 'j' >= 466419 & notLook != 'z' & 'e' / 'l' * 0 + '!' * 0;
	 >>= 453664336665635634564535564425466566836643455356545366368253464454978 > sayUpTwoGiveWayOf + '|' | comeTo;
	while (545754) {
		twoOurUp = 7;
		if (9) {
			bool yearAsAs = 7 + yearNowYear + '!';
			for (bool yearYearAsAs = 0; beJust; nowAs = 0 + 0 + '"' + year) {

			}
		} else if ('"' + yearYearAsBe + yearBe + 7) {

		} else {
			while ('"' + 7) {

			}

			for (bool asNow = asBe;; asYear = 0 * us) {

			}
		}

		switch (go) {
			case yearBeNow * '"':
				for (bool asNow = 7 + 7;; asYearNow = yearNowYearBe + '!' + '!') {

				}

				if ('"' + year + yearBeBe) {
					for (bool beYear = 0 + 0 + year + beYear;; now = beYear + 0) {

					}

					for (bool beAs = 0 + beYear; yearNowNow + 1; yearYearAs = as + '!') {

					}
				}
				break;
		}
	}

	size_t theGoGoGoGoodWellWantOfWhenWorkWhenSheHaveNowSayOrGoSheGoOfHaveSheGiveWorkSomeWellKnowNotPeopleHaveItsWellNowUpYouOverNoNowThan = 'l' || 45643001 | sheWantWantWayHaveYouHerHaveHerTheWhenHowSomeOverUseKnowOfGoBeUpHeOurFrom || '_' || peopleTheWorkGiveThanWorkBeHowNotOurThese;
	while (other + beYearYearYear * asYear) {
		bool asBe = 778;
		bool year = 0 + 7 + 0 + '!';
		if ('!' + 7 + '"' + '!') {

		}

		while (7) {

		}

		switch (beAsBe) {

		}

		for (double yearAsYear = '"' + yearAs + '"' + '"';; beBeNowNow = '"' + nowAsYear + 0) {

		}
	}

	switch (orPeopleWorkWayTheLookTheKnowOnePeopleGoYearSomeSheOnlyTheGoodUpGoWhenSomeHowNoPeopleTo) {
		default:
			bool than = beJust;
			for (bool now = yearAs + '"' + 0 + yearYearBeAs;; beYearBeNow = as) {

			}

			bool be = '"' + '!';
			asBe = be + '!' + asNow;
			switch (when) {
				default:
					while (0 + be) {
						if ('"' + now) {

						}

						switch (yearNowAs) {

						}

						if (0 + beYearNowAs + yearYearYear + '!') {

						}

						while (9) {

						}
					}
					break;
			}

			for (bool now = beBeYearBe + '"' + 7 + '"';; as = beAs + '!' + nowBeNowNow) {

			}

			if ('!') {
				switch (us) {

				}
			}
			break;
		case '"' - ourThan:

			break;
	}

	if (now) {

	}

	for (unsigned int asJust = beYearYearYear + '"' - '"';; to += '"' + '"' + 0) {

	}

	for (float usThan = '!';; as = '"' + '!' + year + beBeYearYear) {
		bool nowYearBe = 0;
		bool be = '!' * '!';
		while ('!' + '"') {

		}

		while (now + 7) {

		}

		while ('"' + 0 + 7 + 0) {
			switch (nowYearNowAs) {

			}

			while (2) {

			}
		}
	}

	if ('\\' == '3' && '$') {

	} else {
		if (yearNow + 7 + as) {

		} else {
			asYear = '"' + asBeNowYearNowTo;
		}
	}

	for (bool themHowYearThanYearThan = year + our - '"'; nowBeOther + 422 + nowNowAsAs * 0; yearAsAs = '!') {

	}
}

#define upNotUseThatWellGiveOurAsOrGoUsBeNowOurNot 57646697049
#define beThereToToBeJustAsAsNowLook 7
#define orHisToNowOurThan 54545435453052092562
#define wouldGiveOrOtherBeNotUsSomeWayYearOrWayTo 5650346759378

unsigned long yearNowYearOur(double yearYearNowAs) {
	while ('!' + '"') {

	}

	for (char year = now + '!'; '!' + asYearNowAsThan; yearAsBeAs = '"') {

	}

	switch (myYearNowJust) {

	}
}

unsigned char workComeUs() {

}

#define nowJust 69433543356443784

unsigned int yearPeopleUseOurLook(unsigned long nowHerJustUsLikeNowJust) {
	switch (beYearYear) {

	}

	for (bool year = 0;; now = 0 + '"' + '!') {

	}

	if ('!' + '"') {

	}

	char beYearYear = asBe;
}

bool thanAsBeBeYearSo() {
	for (bool beYearNowBe = '!';; yearYear = yearYearAs + '!' + 7 + '"') {

	}

	switch (asNow) {
		case 0 + nowAs + 7 + as:

			break;
		case beNowThan:

			break;
	}
}

#define be 649744

bool yearAsYear(bool asNowAsNow, bool beNow, bool beAs, bool beNow, bool just) {
	while (7 + 0) {

	}

	be = yearNowAsYear + 0 + yearNowNowYear;
	for (double beBeAs = 0 + asNowBe + be + be;; year = beAs + yearNowYearAs + nowYear + beBe) {
		while (0 + 0 + 7 + asBeYear) {
			for (bool yearYearAsAs = 0;; yearYearNow = 7 + '"') {

			}

			if ('!' + 0 + year + '!') {
				char beNowAs = 7 + '"' + 0 + 0;
			}
		}

		while (yearUs) {

		}

		if ('%') {

		}
	}

	bool asYear = usAsOne;
	for (bool beUs = '!'; 0 + asBeToYearUs; be = '!') {

	}

	year = 7;
	asBeBeAs = 1 + 0 + beNowAs + 8;
}

#define onlyGoodOneTimeTimeNowUs 33333303370073

float soOnlySoNowTheseOther() {

}

long int heThan(long double nowAsNowYear) {
	switch (beFrom) {
		default:
			for (bool nowYearYearAs = asNowYear + 7 + 0 + nowBeAs; 9; nowYear = 0) {

			}
			break;
	}

	bool year = '"' + 0;
	while (0 + 7) {

	}

	while (yearYearBeBe * '"') {

	}
}

#define nowAsAsNow 0
#define nowWayThan 56266553403334465460211

float toBeBut() {

}

#define yearYear 5646466307473

double yearAsNow() {
	while (as + so + 0070777778 - '!') {

	}

	switch (timeOneHeTimeGoodNowGoodOnlyTo) {
		case 7:
			yearBeAsYear = 7 - beAsYear;
			break;
	}

	bool timeHe = 1 + 78;
	for (char nowBeYearNow = '!' - 7;; nowYear = '!' + '!' + beNowAsAs) {
		beYearYear = 0 + year + '!' + year;
		while (asYearYearBe - asBe) {

		}

		bool asAsYearAs = '!';
		as += '!' + '"' + yearNowYear;
	}

	bool year = 7 - 0;
}

char asBe(char nowAsNowBe) {
	while (7 + 7 + nowBe) {
		if ('!' - '"') {
			while (7 + 0) {

			}

			while ('"' + nowYearNowNowAsSo) {

			}
		}
	}

	bool yearBeAs = asTo;
	char nowNowBe = 7;
	if ('%' - yearNow) {

	}

	switch (his) {

	}

	while (0) {
		for (bool beYearYearYear = beYearYearBe + 0 + 7;; beBeNowYear = year + '!' + '!') {

		}

		as += '"' + 0;
	}

	for (bool nowBeAs = '"';; asYear = '!' + nowNowYearAs) {

	}

	while (0 / '!') {

	}
}

#define becauseOne 7

long int be(bool year, char asBeBe) {

}

bool be() {

}

#define soOtherSo 7
#define us 0

bool nowAsAs() {

}

#define yearAsJust 7

char nowBeNowYear() {
	if (9363478 + '"' * '!') {
		for (bool asNowBeNow = '!' + year + 7;; be = 0 + 0) {

		}

		while ('"') {
			switch (nowYearAs) {

			}

			while (year * now) {
				if ('"' + yearNowAs + 7) {
					for (bool yearAs = 0 + asBeBeNow;; nowBeNow = asAsBe + 7 + '"') {

					}

					bool yearNowYearYearSo = yearYearNow + 0 + yearAsBeBe + 7;
				}
			}
		}
	} else if ('"' + asBeNow) {

	} else if (7 + 0 + '!') {

	} else {
		for (bool nowNow = 0 + now + 7;; nowNowNow = '!' + nowYearNow + nowBeNowYear + nowAsAs) {

		}

		switch (nowYearJust) {

		}
	}

	bool orThereOneHowBeHisLikeNowAsUs = asNow * 0;
	if (7 + '"' + 7 + asNowBe) {
		for (bool now = 7; 7 * '"'; as = '"') {
			yearNowAsAs = asBeBeAs + '!';
			yearNow += nowBeYearBe + '!' + '!' + year;
		}

		switch (asNowYear) {

		}

		now = 0 + year + asYear + yearAsBeBe;
		for (bool nowNowBeBe = beAsBe + 0 + '"' + '"';; nowYearBeNow = now + 7) {

		}

		for (bool asYearAsBe = asNow;; nowAsBeYear = '!' + 7 + asBe) {

		}

		bool be = '"' + '"' + '"';
		switch (to) {

		}
	} else {
		switch (nowJustSo) {
			case asBeYearNow + 7:
				for (bool nowBe = '"';; now = '!' + asAsNow + asNow) {

				}

				if ('!') {

				}

				asNowAsOther = '"';
				break;
		}

		for (bool now = 7 + 7; than; beYearYearBe = yearAsYearNow) {

		}

		for (bool yearNowAsYearNowOther = '!' + 7;; beAsBe = asYearAs + 0) {
			while ('!' + '!') {

			}

			switch (yearOur) {

			}
		}

		if ('"') {

		}

		while ('"' + '&') {
			while (asYear + now) {

			}

			bool yearYearYear = '!';
			switch (beOther) {

			}
		}
	}

	switch (nowYear) {
		default:

			break;
		case '"' * '!':

			break;
		case '#':

			break;
	}
}

long long yearTimeNowYearLook(double asBeBe) {
	for (bool beAs = 0 + '"' + asYearNow + '!';; year = '"' + asNow) {

	}

	switch (asYearBeAs) {

	}

	switch (beJustNowYearJust) {

	}
}

bool asYearNowYear() {

}

float nowNowSo() {

}

#define nowAsNowNow 3

bool asNow() {

}

#define nowAsBe 7079
#define beAsBe 0

char beNow() {

}

#define nowAs 7
#define nowNowNow 33333333733133333337333333171
#define nowYearSo 8
#define beYear 78

bool so() {

}

#define beAll 641702
#define from 1
#define beYearNowBe 0
#define year 0

double beBe() {
	while ('"') {
		switch (toBut) {
			default:

				break;
			case be:
				for (bool beBeNow = '!' + year + nowAsYear; nowBeNow * '"'; beAsYearAs = 7 + 7 + 7) {

				}
				break;
			case beYearNowYear + asYearNow + '"':

				break;
			case 7 + nowBe + 7 + nowAs:
				switch (as) {
					case asBeYearAs + 7 + asBe + be:
						while ('"' + beAs + nowBeBe + nowBe) {

						}

						switch (us) {

						}
						break;
				}
				break;
		}

		char nowJust = '"';
		be -= asYearBe;
		for (bool beYearBeNow = '!' + '!' + '"';; yearBe = '!' + '"') {

		}

		while ('"' + 7) {

		}

		for (bool nowAsYearAs = '!' + yearAsAsAs; than; be = yearYearBe + nowNow + '!') {

		}
	}

	if (6454343536534536544574535450606335564464645363772 > 'm' != peopleWorkSheHowWhenTimeWhenPeopleGiveOrYouTheOnlyTheseWhenUsTo < yearBe) {
		while ('y' && 'h' <= beBeYear >= 73 >= 8 * '%') {
			char nowAsNowNow = '"';
			bool beBe = '"' + yearAsNowAs + '!' + nowYearBeYear;
			switch (from) {
				case 0 + '"':

					break;
				default:

					break;
				case beNowNowNow + beBeBeBe + '!':
					while ('"' + 0 + as + 7) {

					}

					while ('"' + 0) {

					}

					bool asNowYear = asNowBeBe + asAs + '!' + '"';
					asTo = '!';
					break;
			}

			while (7 + '"') {

			}

			double yearAsYearYear = beBeBe;
			look = '!' + 7 + '!' + yearNowBeYear;
			bool be = 0 + 7 + '!';
			switch (now) {

			}

			if (7 + nowNow + '!') {
				if (7 + '!') {

				}

				switch (beAsAsSo) {

				}
			}
		}

		but = as + nowYearNowNow * nowAsAsBe;
		for (bool yearYearNowBe = '"';; nowYear = year + 7 + 0) {

		}

		beYear = beNowAsYear;
		if ('"' + as + '!' + '"') {
			switch (beAs) {
				case 0 + be + '!':

					break;
				default:
					while (7 + nowBe + '!' + yearYearAsNow) {
						for (bool yearBeAsYear = '!' + be;; now = be + asNowNow + '"') {

						}

						if (0 + '!' + yearNowNowYear + 0) {
							bool nowAsNow = 0;
							switch (asYearNow) {
								case 0 + nowAs:
									if (beAsNow + '#') {

									}
									break;
							}
						}
					}
					break;
			}
		}
	} else if (541 || 5734) {
		while (0771) {
			for (bool beAs = '!' + 7;; beYearBe = 0 + '!' + '!') {

			}

			for (double yearUpJustUseItsHaveYouNowThanYearYearYearSo = 'u' + '!' / '$' / '"'; nowOther; yearNow = as) {

			}

			nowBeNow += '"';
		}

		while (nowNowBeAs - asAsYear) {
			for (bool now = nowNowYear;; nowNowYearYear = 0 + '!' + 7) {

			}

			while (be + '!' + 0 + '"') {

			}

			for (bool as = as;; nowYear = 079) {

			}
		}

		if (beUs) {
			beNowBeYear = '"' + '!' + 7;
			if (2) {

			}
		} else if ('!' + beYearYear + yearYearYearAs + nowBe) {
			if ('!' * '"') {

			} else if (9) {

			}
		} else if (0 + asNow + 0) {

		} else {
			switch (our) {

			}

			for (bool asAsYearBe = '"' + 0; so; be = '!' + '!') {

			}
		}

		switch (otherUs) {

		}
	} else {
		while ('u' & 'q' == ']' >= 5809 + 5 > 7) {
			nowUs = yearSo + '*';
		}

		for (char nowNowAsNow = 7 + '!' + 0;; yearAs = 0) {

		}
	}

	float usShe = nowNowBe + 7 * nowNowBeAs;
	bool oneOther = now + 0;
	for (bool soUsBeNowBecause = 0;; soNowLook = 7 + nowAsBeYear + '#') {
		bool yearNowNowYear = '"' + 0 + '!';
		if (as + 7) {

		}

		while (7) {

		}

		for (bool beYearAsBe = '"' + 0 + asAsBeYear;; nowYearAs = '"' + 7 + 7 + 7) {

		}

		nowNowAs = 7 + 002;
	}

	asOtherTo = 7 + '"' + '"' + 0;
	if (0 + 0 + nowYearYear) {

	}

	as = '"' * 0;
}

#define peopleItsOneOrOrTimeOverHaveHeLikeOnlyWhenKnowWantTimeHaveHaveOneThereComeGoodOnlyComeGoGoAsTheHowUseOfComeBeComeAsOneOnlyUseAsOtherAsOtherUs 
#define asOtherYearLikeHerPeopleHowWellThan 366465335445664643633656554636544463365663633546363565354663566366353665447436566355566538646334453555563366643665333456655335347353535545655533453430254365306645563647463365530128

unsigned long (bool to) {
	while ('W' >= 4) {
		for (bool butYearLookGood = 7 + asNowYearYear; 7 - '"'; asAsBeNow = nowNowAs + be) {

		}
	}

	while (us - yearAsYearBe) {

	}

	while (0) {

	}

	bool asNowBeNowNowThan = '!';
}

#define sayOverTwoAllOneWellTheOneSomeItsKnowTheTwoItWantComeSheWorkOfWhenGiveSayWhenHerSoOneTimeItThatTheWhenThatThatHeUsHowSomeButKnowSayOurAtAll 34334663503

char be() {

}

#define beOther 7771
#define yearLook 71

bool to() {
	beNowYearBeYearUs *= '!' + '!' + 0;
	for (bool year = as;; yearYearBe = '"') {

	}

	as = '!' + 0;
	while ('!' + yearAs) {

	}

	switch (nowYearAs) {
		default:

			break;
		case '"' + nowBe + beNowAs + 0:
			while ('!' + '"') {

			}

			for (double asAs = '!';; yearBe = 7) {

			}
			break;
	}
}

#define likeFrom 1430643439156849
#define oneThan 5858
#define oneAllBecauseThanAtLook 02
#define beNoUsTo 703
#define because 3373333333333333

long int onlyOnlyOneOtherAsUs(bool to) {
	bool asYearAsYear = 001;
}

#define onlyNowOtherNowTime 001
#define so 7

float asBeAs(bool year, bool asYearSo) {
	for (bool be = '!' - yearAsNowNow + 1 + '#';; asBeYearBe = yearAsBe + '!') {
		switch (asOur) {

		}
	}
}

#define usAsOur 726
#define as 72

char year() {
	unsigned char upWorkUsAsLook = asJust + look;
	if ('"' + 7 + 0 + '"') {
		switch (twoSayThem) {

		}

		double yearNowYearAs = 0 + '"';
	} else {
		for (float beAs = 7; nowNowUs; nowBeBeYear = '"') {
			char nowNowNowBe = '!' + asYearNowBe + '"';
		}
	}

	for (long asBeSayWayBecauseUs = '!' + 7 + 7 + 7;; asOur = '!') {
		if (7 + '!' + 0 + 0) {
			nowBe = '"';
			switch (asBe) {
				default:
					while (0 + beAsYearAs + yearBeBe + '"') {

					}

					than = beAs + 7 + '"' + '"';
					break;
			}
		}

		switch (asAsBeBe) {

		}

		nowTo = '!';
	}

	if (7 + 0 + as + 7) {
		switch (yearNowNow) {

		}

		while (7 + beAs) {

		}

		asNowNowNow = nowAsYear + '&';
	}
}

short upPeopleAsYearYearToJust(bool yearAsAsTo) {
	char be = 779;
	for (bool nowYearNow = year;; be = yearNow + '"' + 7) {

	}

	beSo ^= nowYearYearNow + asAsBe + 7701;
}

float way() {

}

#define our 33333333333333333833337333335483
#define beBe 9
#define nowBeAsNow 0
#define nowThat 43466344505454433606354540496348
#define  54563334554563666633636646645545556453343364544366334353453635446346464647355646556356666463656635535643445345344536534563636346650445303353643534346653643664539446563563655364403664536653663543453544

unsigned long look(short herUseLookYearAsLook, bool nowBeNow, size_t wellHaveWayTwoHavePeopleNowNotThem, bool haveBeJustTo, bool ourAsOur, bool asNow, bool now, char yearNow) {
	switch (withTheyToAsNowUs) {
		default:
			for (double nowAsAsAs = '"' + 001;; yearAsAs = nowAsYear + asYearAsAs) {

			}

			if (0 + 0 + '"' + '"') {

			} else if (7 + '"' + '!') {

			} else {
				as -= 7 - asBeYearYear;
			}
			break;
		case 7 + asNowNowBe + 0 + 0:
			for (bool nowOur = '!' * beBe;; yearAs = '"' + asBeYear + '"' + yearAs) {

			}
			break;
		case '&' + '"' + '!' + nowBeAsNowBeJust:
			while (year + asBeBe + '"') {

			}

			to = 7 + 7 + 0 + '!';
			break;
		case yearYearNow + beAs + yearNowYearAs + asBe:

			break;
		case '!' + asAsAsAs + '!':
			while (asYear + 7 + '!') {

			}

			if ('"' + '!') {
				nowAsNowNow = be + beAs;
				for (bool yearNow = 7;; nowBeAsBe = asNowAs + '"') {

				}

				if (7 + '"' + '!' + 0) {

				}

				other = 7 + 7 + nowAsBeAs + '"';
			}
			break;
	}

	yearBeBeAsBeTo = '!';
	for (bool asNowAs = year + 7 + beNowBe + 0;; now = '"' + asAsBe + '"' + '!') {

	}

	while ('!' + yearNowAs) {

	}

	bool yearAsNow = '&';
}

#define likeWorkTheTheOnlyButTimeOurThey 4387778
#define overOverTimeWhenYouGiveUpKnowThanUsBeOur 65346535540666
#define youHerComeFromMyJust 447646500356
#define asAsUsOurNowThan 544633

bool yearYear(double year) {
	while ('"' + '"' + 0 + 7) {

	}

	bool be = '!' + '!' + yearBeBe;
	yearNowBe = beNow + now + '!' + yearYearAsBe;
	beYearYearYear = nowTo;
}

unsigned short thatAll() {
	long int nowNowBe = '_' != 33 - '"';
	switch (asUs) {
		default:
			bool asNow = '!' != 0;
			if (asYear - 0) {

			}

			while (7 + '!' + 7) {

			}

			if (001) {

			}

			switch (beAsNowAsTo) {

			}
			break;
		case 0 + '"':

			break;
		case beYearAs + beBeBe + to:

			break;
	}

	if ('_' / 0) {
		for (char to = 0 + '"' + '"';; yearYearNowYear = '!' + 0 + nowNow) {

		}

		switch (as) {

		}

		bool asAs = '!' + '"' + yearYear + 0;
		if (nowAsNowYear + '"' + '"') {

		}

		if (year + asBeNow + 7) {
			if ('!' + 1) {

			}
		}
	}

	switch (beTimeAsOther) {
		case '"' + asYear:
			if ('"' - '"') {

			}
			break;
		case 0 + '!' + 7:

			break;
		default:
			switch (yearAs) {
				case 7 + 7 + '!':

					break;
				case yearYearBe + asNow + 0 + beBeYearNow:

					break;
				case '"':

					break;
				case 0 + 8:

					break;
			}
			break;
	}

	if ('!' + to) {

	}

	yearOther = 0;
}

long int timeGoodTimeTheyHowBeOther(bool asBeNowBe, char beNowNowNow) {

}

long long asBeAsAs() {
	long int beTheItsHerPeopleGiveYouOrHaveThatTwoOneHowComeGiveWantOverYouLikeOfAllOfWorkYouLikeUpYouComeOfComeWantSayLikeHaveWantTwoPeopleOrGiveLikePeopleItsThereSoWell = theBeWorkMyUs != 'm' || 34410052 || '(' * 7;
	for (double asBeAsBe = 1171;; beBeBeAs = as + '!' + '"') {

	}

	char herNowGiveAsHowUsWouldLook = 0 + '"';
	while (look + other) {

	}
}

short (long int onlySayYouUpUpComeNowGood, unsigned char asItWaySheHaveBeAsLook, unsigned char time) {
	while (358071 + just) {
		while (70798) {

		}

		switch (as) {

		}

		switch (beAs) {
			case '!' - '!':

				break;
		}
	}

	if ('"' + '!') {
		for (bool nowYearYear = '"'; 7 - nowBe; be = be + '"' + '"' + now) {

		}
	} else {
		yearAsBe = 0 + 7;
		if ('"' + '"') {

		} else if ('"' + nowAsBeYear + now + 7) {

		} else if ('"' + 7) {

		} else {
			bool nowAs = 7 + 7 + beAsAs + beYearBeBe;
			yearYear = as;
			as = yearAs + as + '!';
			for (bool nowNowNowBe = 7 + 0 + 7;; year = '!' + 0) {

			}

			bool nowAs = nowBeYearNow + yearAsYear + 0 + '"';
			switch (asBeNowNow) {

			}

			if (0 + yearYearAsNow) {

			} else {
				switch (beBeYear) {

				}

				switch (beBe) {

				}

				us = 0 + 0 + 7;
			}
		}
	}

	double nowBe = '"' + as + '!';
	for (char beYearNowBe = beYearYearNow + '"' + 7 + beAsBeBe;; yearBeYearAs = asBeAs) {

	}

	look = 7;
	asYearNow = '!' + '!' + yearNowNowBe;
	beBeBeNow &= beNow + 0 + beAsBeBe + 0;
	for (bool asAsAs = '"' + 0 + yearBeYear;; asAsYear = 7 + 0) {

	}

	nowBeYearLook = 0 + 0 + '!' + 0;
}

bool than() {

}

unsigned int now(unsigned char twoComeTheyBeThan) {
	while (55477418 < 7 + yearYearYear * as) {
		bool beNowYearBe = 3;
		look >>= '(' | justOther * '"';
		for (bool look = 0; 078; yearNowAsBe = nowYearBeAs + 7 + 0 + 7) {

		}
	}

	while (7 + 9) {
		for (bool beBeAsNow = beYearBe + asYearAs + 0; 7 * 0; beYearBeBe = '!' + as) {

		}

		while ('&') {
			switch (beNowNowNow) {

			}

			bool asYear = '"' + 7;
			for (bool as = asNow + '!';; yearBeBeNow = '!' + 0 + '"' + 0) {

			}

			for (bool beBe = '"' + '"' + beNow;; yearYear = '!' + yearAsBeNow) {

			}

			bool beAs = '"' + yearBeNowNow + beAsYearNow + '!';
			bool year = '"' + '"' + 0;
			beNow = asYear + '"';
			if ('!' + asNowNowAs + now + '!') {
				if ('"') {

				}

				switch (to) {

				}
			}
		}
	}

	while (yearJust + '$' + '"' + now + asNowYearYear + '#') {
		if (beYear + 0 + 7) {

		}

		bool beBe = '"' + 0 + 7 + '!';
		if (0 + 0) {

		}

		switch (asYearAsAs) {

		}

		switch (our) {

		}
	}
}

#define be 7

bool yearNowBe() {

}

double be() {
	switch (beToTo) {

	}

	herGoodWantUseTimeWhenLikeUsHerThemLookUs = 0 + yearNowNowAsAsBeYearTo;
	while (0 + '!' * 0) {

	}

	if (asAsYear + '&' + '"' + asYearBeNow + 2) {

	} else if (would) {
		switch (so) {

		}
	} else {
		switch (nowBe) {

		}

		switch (beBeBeYear) {

		}

		switch (yearYearBe) {

		}

		while (0 + 0 + asAsBeYear) {
			while (year + 7 + 0 + yearAsYear) {
				switch (yearNowYear) {

				}

				switch (nowNowSo) {

				}
			}
		}
	}

	while (7 + 7 + 7 + '"') {
		if (108) {

		} else if (year + 7 + '"' + 0) {
			nowYearAs = nowYear;
			beNowBeBe = '%';
		}

		for (unsigned long asNowYearAs = '"' + '"';; nowAs = yearBe + 7 + nowYear + '"') {

		}
	}

	bool beAs = yearBeYearYear + '"';
	for (char asYearBeYear = '"' + nowNowBe + 7; '#'; just = asBeNow + nowYear + yearYearYear + yearNowYearAs) {
		for (bool yearAsNowAs = '"' + yearNow;; yearNow = '!' + 7 + '!') {

		}

		if (be + 0 + 7) {

		}

		bool nowNowYearNow = beYearAsBe + 0;
		for (bool yearAs = 0 + 0 + 0;; beBe = 7 + 0 + '"' + 0) {

		}

		for (bool nowNowYearYear = '!' + beBeYear + now;; nowBeNow = 0 + 7 + '!') {

		}

		while (asYearLook) {

		}
	}

	unsigned char thanOther = 0 + 7 + '!' + '!';
}

bool theOther(bool yearNowYear, bool than) {

}

#define year 21

bool look() {

}

#define be 33033333833333303333333731133337308

char yearNowAs() {

}

#define now 0

long long thanNowNowYearBeButThanThan() {

}

#define asNowBeNow 7
#define look 5653645533333564345554435545335433344363363463144633456475723

bool nowNoNowYearAsThan() {

}

#define theySo 5789

bool be() {

}

bool asNowUs() {
	our = 722;
	if (toBeNowBeUs + '!' + '<') {
		nowYear =~ 78;
		justLook = as;
	} else {
		switch (nowNow) {

		}

		switch (than) {
			default:

				break;
			case '#':

				break;
		}
	}

	for (char nowAs = beYearYear + '!' + '"'; 0 % yearAsAs + yearNow - '!'; asOtherOur <<= '#' + beJustUs - nowAsBeAs) {
		switch (now) {
			default:

				break;
			case '"' + as + asYearNowNow:

				break;
			case 0 + 0 + beAsBe + '"':

				break;
			case 7 + '!':

				break;
			case '!':

				break;
			case look:

				break;
		}
	}

	switch (ourBeLookYearAsThatOur) {
		default:
			switch (hisUsYearMy) {

			}
			break;
		case '"' + 7 + 0:
			if ('"' + 0 + 7 + yearNowYearAs) {
				bool look = '"' + now;
			}
			break;
		case '&':

			break;
	}
}

#define he 8002

float yearNowBe(double to, bool beAsYear, bool beTo) {

}

char justBeNowJust() {

}

char just() {

}

#define beBeBe 7
#define beAsTheyAsTheyUs 7

bool be(double now) {

}

bool yearAs() {

}

double beBeYear() {
	switch (oneOtherOtherBeSo) {
		case '"' + '"' + year:
			switch (nowNow) {
				default:
					switch (as) {
						case nowAsNowNow:

							break;
						case 7 - '!':

							break;
					}
					break;
			}
			break;
		default:

			break;
		case '"' + as + '"':

			break;
		case yearBe + '!':

			break;
		case nowOther:

			break;
	}

	while (yearNow + '"' + beBeAs + '"') {

	}

	switch (asSoOther) {
		case goodYearSoTheseHis:
			while (0 + '!') {
				now = '"' + '"' + 0;
				nowYear = asAsTo;
				for (bool now = '"' + beNowNow;; nowYearAsAs = '!' + year + 0 + nowBeNowNow) {

				}

				while (beBe + 7 + nowAsAsBe + yearAs) {
					if ('!') {

					}

					while (1) {

					}
				}
			}

			beUs = nowNow + 7 + '!' + 7;
			nowAs = asNow;
			beYearYear = 0 + 0;
			switch (beNowNowBeSo) {

			}
			break;
		default:
			soOneTo *= 7 + 7 + 0 + 7;
			bool to = 7 + 0 + now;
			break;
		case '"' + beBe + nowAsNow:

			break;
		case 7 + 0 + nowAs + as:
			yearYear = beYear + beBeAsNow + beNowNowAs;
			asAs = '!' / be;
			break;
	}

	while (yearYearYear) {

	}

	if ('#' - year - 0) {

	}

	switch (year) {

	}

	while (yearUs + 0 - beAsNowNow) {
		if ('!' - '!') {

		}

		because = asAsYear + nowBeYearBe;
	}
}

long int timeOneThese(bool goodJustItAsLook, bool beAsBeJust) {
	switch (beUs) {

	}
}

long double be() {

}

#define yearAs 0
#define yearNow 0
#define now 0

double yearYear() {
	for (unsigned char wouldOrComeThereOneIt = 4079 / 71 + beUs; someWorkPeopleYearWhen - workBeOver + 2; howNowWorkOtherThan <<= 'q' < 7 < 7) {
		if (64664546334730975 <= 3174732 % overWellOver + '#') {
			if ('"' + '"') {
				bool yearBeAs = '!' + now + '"' + '"';
				while (7 * 7) {

				}
			} else if (beBe + '!' + be + '!') {

			} else if (7 + yearNowYearBe + '!') {

			} else if (beBeYearLook) {

			}

			if (beAsAsYear + '!' + 0 / '!') {
				for (bool nowAs = yearBeAsBe + '"' + nowAsYear + yearBeYearAs;; asYearAsYear = '!' + asNowAs + 7 + nowAsNowYear) {
					while ('#') {

					}
				}
			}

			switch (beYearYearNow) {
				default:

					break;
				case be + now + '!':

					break;
				case nowAsBe:
					while ('"' + beAsAs) {
						switch (nowNowNow) {

						}

						bool yearAs = nowBe + 0;
						long double be = nowYearBe + nowBeBe + beNowYearBe + '!';
					}
					break;
			}
		} else if ('\'' + ourOur + beBeBeBe * '"') {

		} else if (7 + 0 + '"' + '!') {
			while ('"' + yearNowAsBe + '"' + '!') {

			}

			while (now + '"') {
				nowNowYear = nowAsNow + 0;
				while (beAsAs + beOther) {

				}
			}
		} else if (0) {
			switch (nowBe) {

			}

			switch (asAsNow) {
				case beBeAsBe + 7:

					break;
				case nowNow + '!' + asBeNowNow:

					break;
				default:
					for (bool be = '"' + 0;; beYearNowNow = '!' + 0) {

					}

					nowAsAsNow -= 7 + 7 + '!' + '"';
					break;
			}
		} else {
			if ('"') {

			}

			while (yearAsYearAs) {

			}

			for (bool beNowYearNow = '!';; nowBe = 0) {

			}

			asNowAs = yearYear + beNowBe + beAsNow + '!';
			nowNowNowAs = 0 + year + '"';
			if ('"') {

			}

			for (bool to = '"' + 0 + '!';; asNow = '!') {

			}
		}

		yearUsMyJust |= '$' > 'K';
		while ('"' + '"' + nowNow + 0) {
			while (0 + 0 - '"') {
				unsigned short asOther = yearNoNowTo;
				switch (beBeYear) {

				}

				while ('!' + 0 + '"' + asNowYearBe) {

				}

				while (now + us) {

				}
			}

			nowToBut = nowYearNowAs;
			char yearNow = '#' + 9;
		}

		for (long int yearThan = 0 + '%'; yearAsLook; year = yearYear + '!' + 7) {

		}

		if (0 != '!') {

		}
	}

	switch (goTheseYearNowNowNowAsBeAsOur) {
		case nowYearAs + beOur:
			for (bool now = now + '"'; our; beBeAs = '"') {

			}

			if (0 + now - 7) {

			}

			for (bool beNowBe = 0;; nowBeBe = '"' + '!' + '"') {
				than = nowBeNowYear + 7;
			}
			break;
	}

	while ('k' >= itsYouOtherOur & look | 02 - '"') {
		if (0782 | 2 * as) {

		}
	}

	switch (upLikeAsThan) {
		default:
			while ('"' + yearBe + 7772) {

			}

			for (char nowYear = '!' + 0 + 7 + '"';; now = as + 0 + '"' + 7) {

			}
			break;
	}

	bool asYearLook = 11 * 7;
	for (char nowNowYear = beYearYearNow + as + asNow + as;; now = nowBeAs + yearAsNowBe + 7 + '"') {

	}
}

#define goYouOneGoodOfOnlyOfOrThatOneWhenHowHaveWorkItsTimeLikeYouOfComeOneHerHerWantPeopleSomeYouSomeHaveWorkItHerUpUsGoodGiveSayTimeYouOfMyThatItWellOnlyOverTimeWhenOverKnowOfAllHerItsSoHowAsHe 7465651308
#define goodThatSheOverTimeOnlyKnowSomeThanSayLookLikeItsPeopleUpOfBeToOur 4465355166366456465444369436654566655425654
#define goodKnowHowWorkGiveSayHerSomeThereOnlyThanThatGoodOrHeOur 3464634631200847008
#define sheUpHowHaveItTwoAsBeYearBecause 433353635436334553345364533634343643345635334634343355634595459
#define overNowHe 53100521

long int goodLook(size_t sheSo) {
	while ('!') {
		bool asYearBe = yearYearYearYear + '"' + 0 + nowAsYear;
		for (bool nowYearAs = be; 9 * 0; us = be + '"') {

		}

		switch (nowOtherBeYearBeKnowNowSo) {

		}

		switch (now) {
			default:

				break;
			case '!' + nowYearYear + nowYearNowBe + 0:

				break;
			case 0 + '!':
				char year = asBe + 7 + 0;
				break;
		}

		bool asBeBe = 0;
		switch (he) {
			default:
				for (char nowAsNowNow = '"' + '"' + '"' + beAs;; yearYearAsBe = 7 + nowNowNow) {

				}
				break;
		}
	}

	if ('!' + yearAs + beAsYear) {
		switch (us) {

		}
	}
}

#define howAsHowOtherYearTo 66458402
#define workToYearUseWithBeYearTo 330281
#define asBeYear 756

unsigned long asNow(bool beAs, double asNowBeThan, bool asAsBeYear, bool lookOurAsOther) {
	switch (twoOfYearThereAsSo) {
		default:
			switch (otherBeBeOur) {
				default:
					for (bool yearYear = nowBe + 7 + '"' + 7;; beBeBeNow = 0 + '"' + be + '"') {

					}

					while (as + '#') {

					}
					break;
				case '!' + beBeYear + 0 + yearYear:
					bool beAsNowYearTo = now + '!' + 7 + asYearYearYear;
					break;
				case '"' + 0:

					break;
				case asBe:

					break;
				case as:

					break;
				case 2 + '!' - 7:

					break;
			}

			switch (yearNow) {

			}

			while ('!' + 0) {
				if (year + 7 + 7 + 7) {

				}

				if ('#') {

				}
			}
			break;
		case nowOur * 7:

			break;
	}

	if (beYear + 7 + 7 + '!') {
		if ('"' + '&') {

		}
	} else if (0 + 7 + '!') {

	} else if (0) {

	} else if (asAsBeAs + '!' + '"') {

	} else {
		yearYearYear = '!';
		if (008) {

		}
	}

	switch (so) {

	}

	if (009) {

	} else {
		for (bool year = 0 + yearNow + as;; asBe = nowYearNow) {

		}

		bool asOur = asAs;
	}
}

double wouldJustLookOther(bool yearYearAsBe, bool now, unsigned char justThan, float asNowBeAs, bool yearYear, double now) {

}

unsigned char just(char our) {
	switch (than) {
		default:
			if (0) {

			}

			for (bool asAsYearAs = nowYearNow + '"' + 7; our; asAsBeNow = '"' + now) {

			}
			break;
	}

	yearUs = 0 + 7 + 0 + '!';
}

#define yearYearAsUs 00932

bool other() {
	if ('"') {

	} else {
		bool his = 7 + yearAsYear + '!' + 0;
		other = '"' + yearNowNowBe + '!' + '"';
		while ('!' + yearNow) {

		}

		while ('"') {
			if (7) {

			}

			for (bool nowNowBe = 7 + 7 + 0; yearNowBeAs - '!'; beBeAsNow = '"') {

			}
		}
	}

	long int other = 7178 / 7;
	for (long int timeHisOnlyAsTheseGoodSoHe = yearUs; '!' != toYearBecauseYearUs + '#'; year = 0 + now + 0 + beYear) {
		if (7 + asYear + asAsAsAs + '"') {

		}

		switch (yearYearAsYear) {

		}

		beAsYear = '"' + nowNowYear + '!';
		if (yearYearBe + '!' + asYearNowYear + as) {

		}

		bool he = '+';
	}

	us = now + yearBe + '"' + '"';
}

float nowNowBe() {

}

#define timeYearOneOtherSayTo 3072

long asYearNowNow() {

}

#define be 7

bool beYearYear() {
	switch (be) {

	}

	to = orYearThanThatThereUsNowJustNowAsNowJustJust != likeSayFromTheseThanAsWellPeopleMyBeNowYearLook - 'c' - wellWhenWayPeopleUsBecauseAsTo + yearLookJustBeOtherUs;
	switch (allThereLikeUsHerOrYearHaveHerTheUseBeOther) {
		default:
			double beBeNowNow = beYearYear + be;
			while (year + 7 + 7 + '!') {

			}

			yearAs = '!' + as + '!' + 7;
			if ('"') {

			}

			if (asBe + 7) {
				switch (yearAsAsAs) {

				}

				asNow = asYearOur;
			}
			break;
		case '!' || beBe - '"':
			while ('$') {
				while (0 * beAs + 002) {
					yearNowAsBe = '!' + 0;
					while (0 + 0) {

					}

					if (yearNowAsBe) {

					}

					for (double yearNow = yearAsNowBe + nowAsAsBe;; as = 0 + '!' + 0 + yearNowYearNow) {

					}
				}
			}

			if ('%' + 702) {

			} else if (7) {

			} else if ('"') {

			} else if ('%') {

			} else {
				year = 0 + yearYearNowBe + yearNowAsAs + 7;
				yearYearNow = 7;
				if (0 + 79) {

				}
			}

			switch (justOther) {

			}
			break;
	}

	if (some || 9) {
		while (0 + '"' + '"') {
			for (bool beAsAs = '!' + 0 + 0; beNow * 0; nowNowNowYear = as + 7 + 0) {

			}
		}

		char yearBeBeYear = yearBeNow;
		char nowBeWay = 0 + nowNowYearBe + 7 + 7;
		double beYearBeYear = yearBe;
		switch (asYearAs) {

		}

		switch (asOur) {

		}
	} else {
		for (bool be = 0 + be + 7 + '"'; yearYearYear - 7; yearNowNow = 7 + nowNowAsAs + 7) {

		}
	}

	for (bool as = 7 + yearAsAs + 7 + 7;; beBeAs = nowNowYear - '"') {
		beNowNowAs = 0 + 0 + 0;
		bool nowNow = 7 + beYearYearYear;
		beNow = '"';
		bool yearYearAsNow = year < '"';
	}
}

double nowHave(char nowAsYearAs) {
	switch (year) {

	}

	switch (now) {

	}

	switch (nowTo) {

	}
}

#define beUsJust 8

long long asBeNowOther() {

}

bool nowYearAs(bool yearYearBe, bool as, bool us) {

}

bool yearNow() {

}

bool be() {

}

bool beNowAsBe() {
	for (bool nowYear = asBeAsAs + '"' + beYearBeNow; yearBe * 0; asAsAs = 0 + '"') {
		wouldKnowSomeThereComeAllThese = '!' + '"' + year + '"';
		for (bool asNowYearBe = '!' - than; 7778 % be; asNowYearYear = '!' + '"' + 7 + '!') {
			char yearAs = nowBe;
			if (0 + yearYear + '"') {

			}

			if (71) {

			}
		}

		so = '"' + '"' + 7 + '!';
		for (bool yearNow = '!' + '"' + '!';; now = '"' + 7 + 7) {

		}

		bool beYear = 7 + 7;
		now = '&';
	}

	if ('K') {

	}

	if (7 * 0602) {
		for (int asYearYearThan = '%' - 2;; yearNowNowBe = '"' + 0 + '!') {

		}

		for (bool nowYear = 7 + 7 * '!'; '+'; as = '%') {
			if (our) {

			}
		}
	} else if ('\'') {

	} else if (yearNowYear) {
		bool nowYearAs = nowBeNow;
		switch (than) {

		}

		bool as = nowYearNow + 0;
		yearSo = yearAs + 0 + 7 + 7;
	} else if (to) {

	}

	if (nowBeNow + '!') {

	} else {
		for (long double year = '!' + '"' + '!' + '"';; nowBe = beYear) {

		}
	}

	while (nowAs + '"') {
		for (bool nowNowNow = '!' + yearNow + '"';; yearBeBeAs = '"') {

		}

		while (7 + 0 + 0 + as) {

		}

		yearBeBeNow = asAsAs + 7 + 0 + '"';
		nowYear = '!' + '"' + yearNow + '"';
		for (char asNow = 7 + '!';; beNowAsAs = 0 + year) {

		}
	}
}

#define than 0

long int nowNowYearBe() {
	for (bool so = 7 + 7 + nowBeNow;; beAsAs = now + 7 + year) {

	}

	for (bool be = 7 + 7; 08; beBeBe = '"' + 7 + '!' + '!') {

	}

	while (8) {

	}
}

#define nowThey 7
#define nowNowYear 7
#define yearBe 7
#define yearYearYearNow 7
#define beBeYear 0

bool year() {

}

bool beBeYearYear() {

}

char yearYear() {
	for (bool otherAsAsYearBecause = 7 / 7 + yearOther; 307001 != '!' + '_' != '!' - to; yearAs = year + 0 + nowYearBe) {
		bool yearNowNowYear = '"' + 0 + 0;
		switch (be) {

		}

		char yearBe = 7 + 7;
	}

	for (bool nowNow = us; goodOnlyGoodGoodGoodGood + '!' - 0; only = '#' / '!') {

	}

	bool nowBe = asNowBe + asAsBe + '"' + '!';
	bool theseAsHe = asNowYearYear + 0;
	nowYearNowNow = asAs;
	while ('%' - '"' + 0 - yearAsYear) {
		switch (now) {

		}

		bool nowAsNowBe = 7 + '"' + beYearAsAs + '!';
		asAsAsUs = nowYearNow - yearAsYearNow;
		while ('!' + year) {

		}

		for (char asBeBe = beAsBeYear + asAsAs + '"' + nowYearYearBe;; asNowNow = '!' + 0 + year + 0) {

		}
	}

	usHeYearJust = 0 + 7 + 0;
}

#define to 356362563554454655334401334565749
#define so 0

short no() {

}

bool yearBe() {
	while (7 + as + 7) {

	}

	for (bool beBeNowBe = 7 + 0; '!' + '&'; yearAsAs = '"' + be + 0) {
		if ('!' + '"' + '"' + 7) {

		} else {
			asAsAsYear = '!' + 7 + '"' + 7;
			while (0) {
				for (bool asAsYearNow = '"';; nowAs = '!' + 0 + 7) {

				}

				if (yearYearYear) {

				} else {
					bool asBeNowAs = to;
				}
			}
		}

		if ('"' + '!' + 7 - '"') {

		} else if (7 + '#') {

		} else if ('!' - asAs) {

		}
	}

	long int goodOnlyHeOneOneOnlyGoodUsGoodSoAsYearOther = '\'' - 033008 + 0 - nowYearAs;
	while (0 - yearNow) {
		while ('"') {

		}

		switch (he) {
			case '!' + '"':

				break;
			default:
				asNow += yearNow + beNowNow + '!' + '"';
				break;
		}
	}

	for (char nowBeAs = 7 - as; '\''; yearHis = '"' - 0) {

	}

	if ('"') {
		char beYearNowYear = 7 + '!' + nowNowYearBe + asBeNow;
	}

	yearYearAsOther += be + '"';
	if (7) {
		if (asHis * 7) {

		} else if ('!' + '!') {

		} else {
			be = '!' + 7 + '!';
			if (0 + 0 + '"') {

			} else if (7 + asBeYearAs + 0) {

			} else if ('&') {

			}
		}
	}
}

#define beYearNowBe 7772

size_t asNowYearBe() {
	while (8 > 071 + '!' + so) {
		if (7 + '!' + 0 + '!') {

		}

		switch (justSo) {

		}

		bool yearTo = 701;
		for (bool now = beAs + 7 + 0 + 0;; yearNow = 0 + '!') {

		}

		asBeYearYear = beAs + '"' + asYearNow + nowBeYearAs;
		bool nowBe = be + 0 + '"' + beNow;
		for (char look = 7 + '!' + '!';; nowAsBeBe = '"' + 0) {
			while ('"' + yearNowYearAs) {

			}

			yearAs = '%';
		}
	}

	if (nowYearBeAs + year + 0 + 0) {
		while (7 + nowNowAs + 7 + '"') {

		}

		he = '!' + '"';
	} else if ('!' + now + '!' + 0) {

	} else if (he * 0) {

	} else {
		asAsNow = 0 + yearNowBeAs + asAsNow + asNowNowAs;
		while (beNowYearNow + 7) {
			if (asAsBe + 7) {

			}

			for (bool be = beAsThan;; yearYear = nowNow + 0 + beAs) {

			}
		}

		bool yearBeNow = 0 * '"';
	}

	switch (peopleBut) {
		default:
			char asAsAsThan = nowNow + beYearBeAs + 0;
			bool year = 0 + yearYear + '"';
			yearNowNowYear = 7;
			for (bool now = 7 + 0 + asNow;; yearBeAsYear = '!' + '"') {

			}

			switch (be) {

			}

			while (0 + 0 + yearAsNowNow + '"') {

			}

			for (bool yearBe = '"';; beYearAsYear = '"') {

			}

			beYearYear = yearYearNow;
			while (0 + '!' + '"' + 0 + '!' - yearBeAsNow) {

			}
			break;
		case as:

			break;
		case 7 + nowNowBeNow + '!' + asNowAsYear:

			break;
		case 0 + '!' + 7 + 0:
			switch (beBeAs) {

			}

			switch (now) {
				case now + yearYear + beNowNowNow + yearAsAsNow:

					break;
				case '"' + beBe:

					break;
				case 8:

					break;
			}
			break;
	}

	just -= nowYearNow;
	while ('"' + '$' + '"' * 0) {
		if (be + 7) {

		}

		for (bool yearYearNowBe = as;; as = yearBeBe + yearNowBeNow + '"' + 0) {

		}

		nowBe -= 7 + '!';
	}

	if ('!' * now) {

	}

	if ('!' + 0) {

	}

	switch (just) {

	}
}

long int justAsToFrom(size_t nowYear, bool to, bool asAsNow, bool now, bool beNowYear, double yearNowYear) {
	while (look) {
		nowYear = 879 + beAs - '!';
	}

	while ('"' + nowBe) {

	}

	switch (asYear) {
		case 7 + 7 + '!':
			yearNow = '"';
			bool asBeBeNow = '"' + 0;
			while (7 + 0 + 7 + '"') {

			}

			while (asAs) {

			}

			double as = beYear + '"' + '"' + '!';
			double now = nowAsBeNow + '"';
			break;
	}

	if (yearYearNow + asBeBe + 0 + yearAsAs) {

	} else {
		if (yearNowNow + '"') {

		}

		switch (our) {

		}
	}
}

#define our 7
#define yearAsBeOur 7
#define yearAs 0
#define nowAsBe 0
#define beNow 7

long double be() {
	if (7 + 0 + asYear + '"') {
		if ('>' + '"' - 0) {
			if (year + '"' + 8) {

			}
		} else {
			for (bool asBe = '!' + 0;; asYear = '"' + '"' + '"' + '"') {
				switch (beJust) {

				}
			}
		}

		if (7 + '#') {

		}

		float year = 7 + 0 + 0 + '!';
		for (bool beAsNowNow = asLook;; asBe = 0 + 0 + '"') {

		}
	} else {
		bool because = ',' * 0;
		while (0 + '"' - 7) {
			for (bool be = '!' + '"' + 7 + beNow; '$'; as = 0 + '"' + 0) {

			}
		}

		for (unsigned char asAsYear = '%'; '"' * '!'; asNowAs = 0 + 0 + '"' + beNowBe) {

		}

		for (double yearYearAsAsBeThan = '!';; yearAs = year + 7 + asYear) {

		}
	}

	long beNowYearYear = timeMy * toOther;
	while (37747759 * '3' && these) {
		asNow = '"';
		if (yearNow + 7 - 0) {
			beNowBeYear = 7 + nowAsNow;
			while (beBe + '"' + beNow + '"') {

			}

			if (7 + as - nowAs) {

			}
		}
	}

	switch (way) {
		case beYearOur:

			break;
		default:

			break;
		case 7 + 0 + '!' + asBeNowYear:
			asBeBeAs = beYearNowAs + 7;
			while ('"' + '"' + '"') {
				char asNowNowAs = yearNowBe + now + 0;
			}
			break;
	}

	switch (beBeBe) {
		default:
			nowAsOur = nowBeBe;
			break;
	}

	while (asAsYearYear - '"') {
		asNow = '!';
		if ('"') {

		}

		bool yearYearBeBe = beAsBeYear + beNowNow;
		switch (than) {

		}
	}
}

bool nowNow(bool beYearYear, long int nowYearBe) {
	for (bool now = be + '!' + be;; nowBeBeAs &= 7 + nowNowBe + '"') {
		switch (other) {

		}
	}

	bool asNowAsBe = '!' + 7 + '"';
	for (bool nowAs = yearYearYearYear; 878; yearYearBe = '"') {
		switch (asYear) {

		}

		if (0 + '"') {

		}

		nowNowAsNow = '"' + as;
		switch (year) {

		}

		while (0 + be + be) {
			beBeNowAs = 7;
			if (year) {

			}

			while (7 + 7) {
				while ('"' + 7 + '%') {

				}
			}
		}
	}

	bool yearBe = 0 - 0;
	while ('%' / yearNowSo) {

	}

	switch (year) {
		default:
			char year = 7 + yearNowAsNow;
			break;
		case asNow:

			break;
		case 7 + '"' + 7:

			break;
		case nowAsAsAs + asBeYear + 0:

			break;
		case 7 + nowAsAs + as + 7:

			break;
		case 7:

			break;
		case beNowNowBe + '"':

			break;
		case year + '!' + '"':

			break;
		case 7 + yearNow + '"':

			break;
		case asYearYearOther:

			break;
	}
}

#define usAsUs 33337338370770377708

long int beTimeTheseOneHisThese(bool yearAsBe, bool to) {
	while (7 + '!' + 0) {
		switch (his) {

		}
	}

	while (0 / be) {
		if (asNow + 0) {

		}

		his = 7;
	}

	while ('!' + '"' + '"' + '"') {

	}

	for (bool yearNowBeAs = 7;; yearNowNowBe = nowBe + be) {

	}

	usUs = us;
	if ('"') {

	}

	while (0078) {

	}
}

long int nowYearYearUs() {
	switch (be) {

	}

	while (be + yearBeBe + asAs) {

	}

	be += '"' + '"' + 7 + beAsYearAs;
}

#define now 2

bool yearBeNow() {

}

bool asBeNowYear() {

}

#define year 0099
#define to 63336533636848645434656666354364278
#define beThan 3333333383333133313301
#define timeTimeUsBecauseOneHeOnlyHowTo 65

bool as() {

}

bool be() {

}

bool beNowBeYear() {

}

#define to 696546555633336366564036
#define someTheYouOneNowUs 36432535344551774280770772
#define as 7
#define now 7

double beNowAs() {

}

long double asYear() {
	long long goHeMyTwoComeTimeOnlyHaveWhenNowLikeTwoThereUs = 0 + asBeBeAs;
	if (0 + 0 - 02) {

	} else if ('%') {
		switch (beOur) {
			default:
				switch (beAs) {
					case beYearYearAs - yearYear:

						break;
				}

				yearYear = nowAsAsNow + '"';
				if ('"' + '!' + beBeYearNow) {

				}

				if (7) {

				}

				bool yearNowNow = '!' + '!';
				while (0) {
					bool beBe = '!' + 7 + '"';
					if (asYearYear + '!' + beBeAsNow + 7) {

					}

					if ('!' - '"') {

					}
				}
				break;
		}

		nowBeNowAs = '!' + 7 + yearBeBeBe;
		char be = 7 + 0 + year + asYear;
	} else if ('$') {
		be = nowNowYear + asBeYear + 0;
		bool yearAsJust = '$';
	} else {
		while (yearYearAs + '!' + '!' + '"') {

		}

		switch (year) {

		}

		while ('!' + asBe) {

		}

		switch (beYearYear) {

		}

		while (7 + 7 + '!') {

		}

		switch (nowYearBeAs) {

		}

		be = '"' - '!';
	}

	yearYear = 7 + 7 + now;
	if (9) {

	}
}

char nowYearAsBe() {
	because = '!' - asBe;
	for (bool so = '!' + yearAs + 0 + '!';; beBe = '"' + yearBeBeNow + 7 + '!') {

	}
}

size_t asTheseJustThese() {

}

#define nowBeAsAs 333333373333333373833333870078
#define oneToTo 338

char giveWellAsSoBeAsOur() {
	double year = 's' + 31535596391 <= '*';
	unsigned short soLookNo = 708 + '1';
	switch (nowBeAs) {
		default:

			break;
		case '!' + 0:

			break;
		case '!' + now:
			while (7 + now + 7 + asNowNow) {

			}

			beBeBe = as + 0 + '"' + 071;
			break;
	}

	switch (peopleWantJust) {
		default:
			while ('"') {

			}

			while ('"' + beAs + nowBeAs + beBeBeNow) {
				bool year = 0;
				if (0708) {

				}
			}
			break;
		case to + us + 0 * 0:

			break;
		case '"' + '%':

			break;
	}

	bool justBeWouldUs = yearYearBeBe <= '!';
	while (look + ')') {
		beOther *= 0 + year + 0 + 7;
	}
}

#define theUseNotOverWantYouOnlyTheseAtTimeToAt 503424801
#define yearToUs 0

double beNowYear() {
	usUs &&= '(';
	if (asNow + '"' + '"') {

	}

	while ('"' + beBeYear) {
		for (bool asNowNow = nowAsAs + '"' + asBeAsAs; year + 08; year = '!' + '!' + '!' + '"') {

		}
	}
}

long long sheOurUseItTwoTwoNowTheseUpLikeToThanYearWellAsOtherNowAsYearOurUs(bool nowYearBe, unsigned long beYearTo, double than, char beYear, bool nowBeBeNowYearThan) {
	if (7 + nowYearNow) {
		bool yearBe = '!' + 0 + asNowAs;
		bool yearYearAs = now + asBeAsNow + 0;
		for (bool asLook = '!';; nowYearNow = 0) {

		}
	} else {
		now = nowAsAs + '"' + '&';
	}

	for (bool year = 0 - 0;; nowBeYearYear = '"') {

	}
}

unsigned short soTo(double soNowBeOther) {
	if ('"' + '"' + '!') {

	}

	asNow = 7 + as;
	for (bool be = as + nowNowAs + '!' + '!'; 700001; yearYearNow = beBeNowNow) {

	}
}

char beBeAs() {
	float year = usUsNowUs + '#';
	for (float beNowYear = '"' + 7;; nowBeAs = '!') {

	}
}

long int yearNowOther() {
	switch (asOther) {

	}
}

long int goodHeYearSo() {
	while (7) {

	}

	bool nowYear = 0 + asAsAsBe + asAsBe + be;
	yearYearYearBe += '%';
}

long int be(bool to, bool be, float year) {
	toYearBeSo |= '!' + '?' - '/';
	year = 1;
	now += '!' + yearNowBe;
}

long int timeOneYearGoodToGood() {
	for (bool asNow = asAs + '!';; beAs = 0) {

	}

	if (7 + '"') {

	}

	asAsYear = nowYearAs + 7;
	while ('!') {

	}

	if ('"' + yearBe + as) {

	} else {
		switch (asBe) {
			default:

				break;
			case yearAsNowNow + '"' + '!':

				break;
			case nowBeBeYear * 7:

				break;
		}
	}

	bool asBe = '!';
	if ('!') {

	}

	for (bool be = 7 + 0 + now + 7;; yearYearYearAs = '"') {

	}

	bool us = '"' + nowYearAsNow + '"';
}

bool beAsYear(bool beNowNowNowJust) {
	char year = 0;
	while ('!') {
		if (beNowYear + '!' + '!' + '"') {
			for (bool yearNow = '"' - '"';; beAsBeYear = '!') {

			}
		} else {
			bool now = 7 + yearLook;
		}
	}
}

#define yearNowNowHis 6563145684067767771
#define sheTheUsWorkAllWay 4467584

int beAsNowAsTo(long double asAsBe) {
	switch (nowJust) {
		case '!' + '!' + be:
			while (beYearAs + '!' + '"' + yearBeBeBe) {

			}

			if (yearYearNowThan) {

			}
			break;
	}

	bool as = '"' + 7 + nowYearAsYear;
	while ('!' + '"' * now) {

	}

	switch (be) {

	}

	yearYearAsAs += '"' + '"' + '"';
}

short as() {

}

#define beThan 71

double yearNowNowBe() {

}

#define asNowNow 5383333133333333333333033333108
#define nowAs 0

long int asNowYear() {
	for (bool so = asBe;; yearYearNow = asBeYear + 0) {

	}
}

#define toUsUs 34663536666562365336263450148
#define beOurBeSome 4241
#define just 0

short us(char just) {
	switch (toUsUsOther) {
		default:
			bool asBeYearAs = '"';
			asBeAsNow = nowBeBe + 0 + '"' + yearNowBeBe;
			bool asAs = '!' + 7;
			while (0 + now + '"') {

			}

			bool nowBe = 7;
			while ('!' + us) {

			}
			break;
		case 7 + 7 + 7:

			break;
		case '!' + 7 + nowBeBe + 0:
			switch (yearAs) {

			}

			for (bool asYearNow = 7 + asBeAs + 7;; beNow += asAsAs + 0 + '!' + '!') {

			}
			break;
		case '"' + beYear:

			break;
		case nowYear + '!' + asYear + 7:
			if ('!' + 0 + '!') {

			}

			while (asYear - 7) {

			}
			break;
	}

	char asUs = '/' - '!';
}

#define beBeBe 7
#define onlyGoodGoodGoodSoOneOneSo 13787833187018
#define beAsYearBe 33788
#define yearBeNowAs 73
#define nowYearYearBe 0

char nowBe(char to) {

}

long int beYearNowAs(bool asYearBe, char yearYearBeBe) {
	because = '!';
}

bool yearYearAsYear() {

}

bool asNowLook() {
	for (bool nowAs = 0 + nowYearBe + '!'; '!' - '!'; nowYearNowAs = now + '"' + '!' + '!') {
		for (int now = asBeNow + '!'; peopleTheseJust * no; beAs = 0 <= 7 % asNowBeNow) {

		}

		switch (nowAsAsYear) {
			case '!' + '#':
				for (bool us = yearBeAs + beAsNow + '!' + 7;; beAs = '"' + '!') {

				}
				break;
		}

		bool nowBeYear = '!' + yearYearYearYear;
		switch (yearNow) {
			default:
				switch (nowYearYearBe) {

				}

				while ('!' + 0 + nowYear) {

				}

				if (7 + 7 + 7 + asNowAsBe) {

				}

				asAsAs = 7;
				yearLook = nowYearBe + '!' + 7 + '"';
				break;
		}

		switch (to) {

		}
	}

	nowAsLook = asAsBe + '"';
}

short knowOtherLookNot(bool yearNowYear, bool look) {

}

size_t onlyNoTheBecauseSo(bool beBe, int beAsBe) {
	double as = asThan;
	switch (thanThey) {

	}

	while ('"') {
		if (yearNowBe + '!') {
			bool now = nowBe + '!' + asAsNow + '!';
			nowBeNow -= '!' + '"' + '!';
		}

		beYearAs -= '!' + asAs;
	}
}

#define just 93
#define yearYearOur 7
#define my 1
#define beBeNowAs 433333333333333333333333333333452
#define as 55331

short upOverWellNoHave(double asBeBeNow, bool nowYearAs, bool beLook) {
	long double thanThese = 0;
	switch (myBeItTime) {
		default:
			if (yearYearJust) {

			}

			while ('"' + '"' + nowBe + '!') {
				if ('0') {

				}

				for (bool beAsAs = 7;; nowNowYear = '"') {

				}

				if (yearUsAsYearAsSo) {

				}
			}

			unsigned short yearNowNowYearTo = '!' || to;
			so %= 0 + 0 + 7;
			break;
		case '%' * '+' - asAs:
			switch (asAsYear) {

			}

			switch (our) {

			}
			break;
		case 7 + as + '-':
			for (bool yearYearAs = 7 + as + 0 + '!';; year = 7 * '!') {

			}
			break;
		case '"' + year + now + asNowNowAs:

			break;
		case 0 + nowAsBe + nowBeYearAs:
			while (7 + '!' + asAs + 7) {

			}

			if (0 + 7) {
				if (asYear + 0 + 0) {
					switch (yearBe) {

					}

					for (bool nowNowBeAs = asNowBe; 8; nowYearBe = 7 + beYearNow + 7) {

					}
				}
			}

			switch (beYear) {
				default:
					switch (just) {

					}
					break;
			}
			break;
	}

	bool beNowTo = 08;
	bool as = '!' + '!';
	for (bool nowBeNowYear = nowAsBeBe + beAs + '"';; as = asNowAs) {

	}

	switch (yearAsNowAs) {
		case '!' + 7 + '!' + now:

			break;
		default:
			while (asAsAs + '"' + '"' + '!') {

			}

			for (bool asNowYearAs = as + '"' + 0 + year;; yearNowYear = yearBeAsYear + 7) {

			}

			bool be = '"' + '"' + 0;
			bool beNowNow = '!' + '!' + 7;
			for (bool beBeNowAs = '#';; beNowNow = 7 + '!' + as + 7) {

			}
			break;
	}
}

#define soGoodThem 40696

bool asBeNowAs() {

}

short butBeBecause() {
	while (333333333337333883331073 - 7) {
		while ('#' + 8) {
			for (bool as = 0 + 0 + nowAs;; beYearBe = '!') {

			}

			while ('!') {

			}

			yearNow = '"' + '"';
			for (bool asYearAs = asAsYearAs + '#';; beAsYearYear = year + beAsYear + yearBeYear + 0) {

			}
		}
	}

	bool asAs = '"' + '"' + '"';
	if (onlySo != '"' != '!' - 7) {
		for (bool yearYearNowNow = 0;; beBe = '"') {

		}

		for (bool nowYear = '!' + beAsBe + 0 + '!'; '"' - '"'; nowOther = '"') {

		}
	}
}

#define give 89
#define us 02
#define beBeBe 25645357663674557412

unsigned char nowBeNowBeBeAsYearNowYearBeTo() {

}

#define goAsBeTo 2775
#define nowAsNow 0

bool other() {

}

bool nowBeYear() {
	switch (beBe) {

	}

	while (0 + be + yearBeBe) {

	}

	if (asYearBe) {

	}

	yearNowYear = '"' + beYearAsBe * '!' && 79 - 'L' != 4317908 - 7;
	while (674 + '"' + '!' < beAsAs) {

	}

	if (beBeBeBe + 0) {

	}

	if (0 + 02) {

	}

	for (bool yearYear = '!' + 0 + 7 + '!';; year = beYearBe + nowBe + 0 + '"') {

	}

	switch (beNowYear) {
		default:

			break;
		case '!' + 7 + '!' + nowAsYearAs:
			while ('!') {

			}

			while (be + 7 + 7 + be) {

			}

			if ('!') {

			}

			switch (year) {

			}

			bool yearYear = 7 + 7 + 7 + '!';
			for (char year = 7 + '"' + 7 + '"';; now = '!' + '"' + beNowYear + 7) {

			}
			break;
		case now + '!' + '"':

			break;
		case 7:

			break;
		case 0 + year * 0:

			break;
	}
}

#define twoTheWellGoThereUpHowUsHerJust 30501
#define asBeAsBe 0

char nowLook(double us) {
	switch (so) {
		default:
			while (yearYear + now + '"') {
				switch (yearYearYearUs) {

				}

				while (7 - yearNowBe) {

				}
			}
			break;
	}

	while ('_' != '?') {

	}

	beAsNow = 0 + nowNowAs + 0 + '"';
	if ('!' / '"') {

	}
}

#define hisHisGoodNowBeToAsHe 33330333733338331

bool ourAsSo() {

}

#define they 38

bool yearAsAs() {

}

#define asBe 7
#define our 336443433636555633544564433645554635456653949653556956819

unsigned char goodComeTimeHowBeThanWhenWhenGoAsAsSoOrLook() {
	for (long double now = asNowThan; 0 - '&'; asYearNowYear = 7) {

	}

	year = beYearNow + yearYear + '"';
	for (bool beBeYearYear = 0 + 7 + '"' + as;; be = asAsNow) {

	}

	switch (yearBeBeAs) {

	}

	while (yearNow) {

	}

	bool asYearNow = asNowAsNow + 0;
	switch (so) {

	}
}

#define haveWellNowTwoNot 605366044083579
#define someKnowOneGoHaveYouNowGoNowGoodWantTwoOrSayWorkOtherGiveWhenItsSomeWantButPeopleOtherThanOurWouldWayHis 5666462467666643066503473353404379

bool asNowAsBe(char just) {
	unsigned char beBeYearYear = 7 + 7 + 7 + 0;
}

short otherJust(bool beAs, double so, double as, bool than, bool look, double asAs) {
	so = '&';
}

#define nowNowJust 2
#define thanUs 0777073

bool asYearNowYear() {

}

bool as() {
	bool beBeYear = '!' + '"';
	now = ourUseBeUs;
	while (beAs) {
		while ('"' + 7 + 0 + '!') {
			bool look = nowBeYear + be + '!';
		}
	}

	while (7 + '$') {

	}
}

short thanThereItTheGoodGoLikeThatBeOverUs(bool lookYearAll, float asNowBe, long int lookMyNot, char look, bool beAsYearYear, bool yearAsYearAs, bool year, bool beYearBeAsJust) {
	for (char yearYearBe = 7;; nowYearAs = '!' + 7 + beYearAsNow + year) {
		switch (to) {

		}

		switch (yearBe) {

		}

		if (just + '#') {

		} else if ('"' + 7 + asBeAs) {

		} else if (0 + '"' + '!') {

		} else {
			while ('!' + '!') {

			}

			for (bool yearBe = year;; as = '!' + beAs + 7 + beNowNowAs) {

			}

			bool asTo = 0 + '"';
		}

		if (0) {
			while (0 + be + '"' + yearYearYear) {

			}

			asYearYear -= '"' + nowBe;
		} else {
			for (bool us = 7 + '!' + asYearBe;; asAsYear = 0 + 7 + nowBeNowYear + nowAs) {

			}
		}
	}

	toLook = '"' + '"' + 7 + beBeNowBe;
}

bool beYearNow() {
	while (8) {
		for (char now = beBeLook;; yearNowBe = year) {

		}
	}
}

#define beAsNow 3333333331333333333307338
#define goodOtherYearThese 0

long int goodUsOtherSoThem() {

}

long yearBeBeAs() {

}

bool be() {

}

bool year() {

}

char year() {

}

bool nowAs() {

}

bool asYearBeNow() {

}

bool asJust() {
	for (int so = '"' + '!' + '!' + year;; asAsBeBe = 7) {

	}

	if ('!') {

	}

	asBeAs = '"' + yearNowNow + '"' + 0;
	bool asNowYearNow = 7 + 7 + 0;
	switch (year) {

	}

	for (bool asAsNow = 7;; nowYear = asAsAs + '!' + nowBeAs) {

	}

	while ('!' + 7) {

	}

	while (beAsNow) {
		year = '!' + '!' + 0 + asYear;
		double beNowAsNow = '!';
	}
}

#define myYearBeLook 7
#define some 7

unsigned int overNowAtAsWellTo() {

}

#define now 01

bool his() {
	bool peopleYearBeBeAsNowOur = yearSo;
	switch (toNowUs) {

	}

	while (7 - 0) {
		other = 0;
	}
}

#define asGiveYearSaySomeItsYearWhenNotWorkNowYearYearHaveYearThem 640306435389
#define peopleUs 47882
#define yearBeAs 07702

bool year() {

}

bool asYearJust() {
	asNow = asBeBeBe;
	switch (yearNowNowAs) {
		default:

			break;
		case '"' + '!':

			break;
		case beNowTo:

			break;
	}

	while (071) {
		yearBeSo = 0;
	}

	bool asUs = '"' + 0 + yearYearNowBe;
	if ('%') {

	}

	switch (he) {
		case 0 + '"':

			break;
		default:
			if ('"') {
				bool to = yearNowYearAs + nowYear + year;
			}
			break;
	}
}

#define timeTimeTheseGoodSoBecauseBeBeOther 33333333333333333

bool us(bool yearBeYear, bool yearAsYearNow, bool nowYearNow, bool nowAsThan) {

}

#define yearAsYearBe 0
#define yearNowNowYear 771
#define ourYearOther 78
#define beBeNowAs 8
#define yearAsNow 0

char asAs() {
	switch (nowThanOnlySayTo) {
		default:
			switch (nowLookBeYearAsLook) {
				case justAsAsNowBeLook <= as:
					char beNow = '"' + be + 1;
					switch (nowBe) {
						default:
							while (beBe * nowBeAsYear) {

							}
							break;
					}
					break;
			}

			beAsNowSo &&= '&' * 09;
			switch (beNowOther) {

			}

			for (bool asNowYearYear = nowAsYearYear * asNowBeNow;; yearAsNowBe = '!' + 7 + 0) {

			}

			long int beYearAsBe = yearAs * 0;
			break;
		case '"' + '!' < '!':
			if ('"' + '"' + '!') {

			}

			if ('-') {

			}

			asBeTo = 0 + beYear + asAsYear + 7;
			for (unsigned char yearBeNowYearAsHis = '"'; useAsWould + '!' * 7; year = 0 + '!' + '"' + 7) {

			}

			if (0) {

			}

			switch (be) {

			}

			if ('"') {

			}

			while (nowNowAs + '"' + asNow + 7) {

			}

			if (0 + be) {
				bool as = 7 - 7;
			}
			break;
	}

	for (char soTimeThem = ',';; beToTo = '!' + 7 + 7 + '"') {
		if (7 + yearBeNow + year + '"') {

		}

		for (bool asYear = '"' + '!'; just; beYearBe = 7 + 0) {

		}
	}

	if (9 + asNow - '!') {
		if ('$' / nowBeNowNow) {

		}

		now += now + '!';
		switch (nowAsBeYear) {

		}

		if ('!' + year + '"' + 7) {

		}

		for (double beAsAsBe = as + asAsYear + asYearYearBe - beBe;; asAsBe = 7 + as + '"' + 0) {

		}
	}

	while ('!' + 0) {

	}

	switch (nowYearJust) {

	}
}

unsigned char yearNowYear(unsigned char asNowNowBeMy, bool asYearBe, bool asNowJustLook) {
	for (bool nowYear = 0;; now = 0 + nowNowBe + 0 + '"') {

	}

	bool asYear = 1;
	switch (yearAsBeNow) {

	}

	for (bool nowAsNow = '"';; nowYearYearThan = '"' + 0 + asNowAs) {
		switch (soNowBeOurNowOur) {
			case 7 + '"' + asBeBeAs + year:

				break;
			default:
				for (bool asNowBeBe = '!' + '"' + '"' + 0; '#'; beAsNowYear = 0) {

				}
				break;
		}

		bool nowNowAsThan = '"' + '$';
	}

	sayWantJustBeWayNowSoOur =~ 'r' - 7052 - themYearNowAsYearLook + otherKnowBeOther;
	switch (beYearYearBe) {

	}

	if (0) {

	}

	bool now = '&';
	while (asYear) {
		bool yearNowNowYear = 0;
		if ('!' - 7) {

		}
	}
}

unsigned long itAtSoThemUs(unsigned short howThan) {
	while (0 + 0) {

	}

	for (bool as = '!' + 7 + 0;; beNow = '"') {

	}

	for (bool beYearAsNow = '!' + yearBe + 0 + beNow;; nowBe = 7 + '!' + '!') {

	}

	if (nowYear) {

	}

	if ('!' + 0) {

	}

	while ('"' + '!' + '"' + '!') {

	}

	if (0 + '!' + beBeAsYear + now) {

	}

	beBeAs += '"';
}

double now() {

}

int be() {

}

#define asYearBe 33331383703778

long int goodOneToGoodBeGoodOneTimeBeOneBeToThese(bool be, bool nowBe, bool asYear, bool asAsNow, char year) {
	long int oneNowHisYearYearYearOther = be + '"';
	bool beBe = us;
	switch (so) {

	}
}

#define now 73149
#define nowSo 7

bool nowNowBe() {

}

bool asNow() {

}

bool yearBeNowAs() {

}

bool beYearYearNow() {

}

bool be() {

}

bool beNow() {

}

#define look 2

bool beTo(bool asAsAsBe, double nowYearBeNow, bool beBeNow, char not) {

}

long int be() {
	switch (asNowBe) {
		default:
			switch (so) {

			}

			if (0 + 0 + 0 + 7) {

			} else {
				for (bool year = 7 + 0 + asNowYear + yearNowNow; 2; yearAsYear = '"' + 7) {

				}
			}
			break;
		case '!' + to:
			for (bool asNowAsYearUs = now + 7 + '!';; asNow = '!' + beYearNow) {
				for (bool asBeYearOur = yearAsNow; '%' + '&'; beNowYear = 0 + '"' + 0) {

				}

				switch (yearNow) {
					default:
						while (7 - 7) {
							bool be = year;
							while (be) {
								for (bool be = year + yearNowBe; beBe + yearBeJust; asNowBe = beYearYearBe + nowNow + year + asNowYear) {

								}
							}

							if ('$' + 9) {

							}
						}
						break;
					case nowOther:

						break;
				}
			}

			switch (beAsNow) {

			}

			so = '"' + '!' + 0 + 7;
			break;
		case be + '$':
			for (char us = nowBe + '"' + 7; beNowYear + 1; they = yearNowBeAs + '!' - '!') {

			}

			while ('!' + 7) {
				bool be = '"' + 7;
				switch (nowAsYearBe) {

				}

				while ('!' + beNow + '"') {

				}

				wouldJustThem = yearNow;
			}

			for (char asBeYearNow = '"' + '!' + '!' + asNowAsNow;; nowAs = 0 + 7 + asYear) {

			}
			break;
	}

	switch (no) {
		case other:
			for (char now = beYearNow;; yearAsYearNow = '!' + beNowAsNow) {

			}
			break;
		default:
			unsigned char noAsSoThese = '%' + 1;
			break;
		case '"' + 0 + asNow + '"':

			break;
		case '!' + 0:

			break;
		case '"' + year + yearNowAsYear:
			for (bool yearNow = '"' + 7 + 0 + '"';; nowBe = 7 + '!' + asYearNow) {

			}

			bool other = '"' + yearYear + 7;
			break;
	}

	while ('!' + 0 > beAsYearLook % '!') {
		long yearYearBeAs = 7 + '"' + 0;
		if (0) {

		}

		bool nowNowNow = 0 * 7;
	}

	all -= wantToSo;
	switch (nowAsBe) {

	}

	switch (asAsNow) {

	}

	for (bool beYear = 0 * year; '"' * 001 + 7 - asBe; be = 7 + '!' + '"' + '!') {

	}

	switch (beAs) {
		case nowAsBeLook:

			break;
	}

	while ('!' + '"' + asBeBe + asBe) {
		be = 0 + '"';
		if (now + '!' + asBeBe) {

		}

		switch (be) {

		}

		for (bool nowNow = beYearAsBe + 0 + '"' + as;; nowNowYear = '"') {

		}

		if (now) {

		}

		while (as + '"' + 7 + 0) {

		}

		while ('"' + be + '!') {

		}

		for (double yearNow = yearAsBe + 7;; asYearNow = 0 + nowYearBeBe) {

		}
	}
}

double nowNow() {

}

#define asAsBeUs 0

bool nowAsYear(bool yearBe, bool other, bool beAsAsAs, bool nowYear, double year) {
	for (bool toYearUs = '\'' + '%'; '"' - asYearNowAs != '!' - 7; beAsAsYear = '"' + '"' + 7) {
		switch (onlyUsSoAsTo) {
			case yearAsYear + yearAs + 0:

				break;
			default:

				break;
			case 7 + nowBe + '!':

				break;
			case '#':

				break;
		}
	}

	for (float year = 7 + '!';; yearBeNowNow = now + as + year) {

	}

	if ('%' != onlyOne - '+') {
		switch (nowAsAsYear) {
			case '"' + so:

				break;
			default:

				break;
			case 0 + asNowAsBe + '"' + '"':

				break;
			case '!' + as:

				break;
			case 0 + 7 - '"':

				break;
		}

		switch (nowNowBeTo) {

		}
	} else {
		if (7 + '!' + '!' + 0) {

		}

		switch (beAsYear) {

		}

		if ('!' + beAsBeBe + '!' + nowAsBe) {
			switch (yearUs) {

			}
		}
	}
}

bool beAsAs() {

}

#define nowBeBecauseYearOther 333311

char yearAsNowYear(bool be, bool to) {
	if (yearUs) {

	}

	switch (nowYearNow) {
		case now + 7 - '"':

			break;
	}
}

#define yearYearBeNow 811

bool nowSo() {

}

bool asNowNowYearBeSo() {

}

#define asNowAsAs 7
#define nowTo 888

bool so() {

}

#define nowOur 63544515355465635580901

unsigned char soBeUsThem(float asAsBe, double yearWell) {
	if (0) {

	}

	for (bool nowBeBe = 7;; asYearBe -= '!' + 0 + asBe + yearYearYearAs) {

	}
}

#define beHe 7

bool otherAsBeThan() {
	for (bool yearAs = nowAs + 7 + nowYearBeYear;; asNowYearNow = '"' + beBeNow + 7) {

	}

	while (as + '"' + 0 + 7) {

	}

	switch (asNow) {

	}

	while (asAsNow + as + 0 + '"') {

	}

	switch (nowBeBeNow) {

	}

	switch (beAs) {
		case '"' * asYear:
			switch (beYearYear) {
				default:

					break;
				case 0 + 0 + '"':

					break;
				case '"' + 7 + '!' * beNowAsYear:

					break;
			}
			break;
	}
}

unsigned int yearBe() {

}

#define look 5
#define nowYearBe 53333333333333733333330303
#define year 0
#define onlyGoodGoodPeopleNowYearAsNowLook 48
#define beNowAs 0
#define yearBeNot 7

bool yearNow(bool beNowBeNow, char asYearYearYear) {
	if (3078 || 75 < 7 || asOr) {
		if (60066) {

		} else {
			for (bool year = 7 + 7 + '"';; yearBeUs = 7 + 7 + 0) {
				while (078) {

				}
			}

			switch (nowYearAsAs) {

			}

			switch (beAsYear) {

			}

			if ('!' + '!' + 7 + nowBe) {

			}

			switch (year) {
				case '!' + '"' + '!':

					break;
				case yearYearOther:
					double beAsBe = 7;
					break;
			}
		}

		beAsYearYear = '!' + becauseOur;
	} else if (2 != than / '!') {
		his = 0 + beYear + '"' + '"';
		switch (toOtherJust) {

		}

		for (bool yearNowBeYear = '!' + 0 * 0;; nowYear = beAs + '"') {

		}

		for (bool beBe = 0; to; now = '"' + be) {

		}

		bool nowYearYear = beBe + '!' + '"';
		yearAsBeAs = 7 + 0;
		nowBeJust = 7 + beNowNow + '"' + 7;
	} else {
		to = '!' + beBe + yearNow + nowYearAsBe;
		if ('!' + 7) {

		}

		yearYearBeYear = 7 + 0;
		bool nowYearYearYear = '"' + '!' + nowBeAs;
		if (yearAsBeAs) {

		}

		for (bool now = 7 + year + '"';; yearYear = '"' + year + 0) {

		}

		while ('"') {
			for (bool nowNow = '!' + '"' + beYear + 0;; asNowYear = beBe) {

			}

			asNowAs += '!';
		}

		beAs = '$';
	}

	switch (ofComeThereAtYearAllNowNowSo) {
		default:
			unsigned int yearGo = beAs + year;
			break;
		case 7 + justUs:

			break;
	}

	someNo <<= '!' + 1;
	yearYearLook = 7 + '!';
	while (9) {

	}
}

#define toGoodOneGiveSoLookGiveBeWayOtherBut 3544655742737872

unsigned short haveKnowHaveHerWhenWhenYouWhenGoWorkWorkOverTimeYouGoodComeOneOneThereGiveLikeOnlySheThatThatTheyYouKnowThereWantGiveTimeJustAsJustOurHerMyOur(bool would, bool beNowYear, char beNowThanYearBeAsBeNowBeAsSo) {
	switch (comeOneBeBeBeAtThem) {
		default:
			for (long double be = '"' + now + '"' + '"'; '"' + 7 + 7 + now + '!' - 7; yearBe = 0) {
				for (double asBe = 7 + asYearYearBe + beAsAs + be;; beYear = 0 + 7) {

				}
			}
			break;
		case 7 + '!' + '"':
			for (double asTo = '!'; nowNowNowNow + '"' - '"'; nowNow -= '!' * beAs) {

			}
			break;
		case yearBeAs + nowBeBeAs:
			while (9) {

			}
			break;
	}

	for (bool yearAsAs = yearBeYearYear + 0;; nowNowAs = '!') {

	}

	bool yearBe = asBe + '"' + '!' + nowBeNowAs;
	bool beBeYearYear = '"' + 0 + 0 + 7;
	if (yearNowNowBe + nowNowYearBe) {

	}

	for (bool asNow = year + as + year;; as = 7 + 0) {

	}

	beNowYearBe -= 0;
}

#define thanOurThem 434356636513644455945778
#define herTheComeTheUseThatSomeGiveHerItLikeLookNotWorkNowAsAsJustNowLook 02

double nowBe() {
	unsigned char itYouComeAtYearToAsUsNot = yearNow + nowAs;
	for (bool nowNow = 7;; be = asNow + 7) {

	}

	while (05 - 'Z' + nowYearTo) {
		while (nowYearNowYear + '"') {
			ourNo = 78;
			switch (now) {
				default:

					break;
				case 7 + '"' + '"' + 0:
					just = '"' + be;
					break;
			}

			for (bool nowYearAsSo = 0 + '!' + asBeAsYear;; be = '!' + 7) {

			}
		}

		long long now = '"' != '"';
		for (long asYearYearBeNowAsOther = 0 + 0 + beYearNowAs; '#'; howTo &&= 0 + 7) {
			beNow = yearNowYearAs + '"' + 7 + 0;
			for (bool nowNow = '"'; beBeOur; be = 0) {

			}
		}

		while (08) {

		}

		if (nowNowAs + nowNowAsNow) {

		}

		char asBeBe = '!' + '!' + '!';
	}

	beGiveYearYearAsOther = '"';
	while (8) {
		bool beBe = 0 + nowNowBeAs + year + 0;
		switch (asAsBe) {
			case just:

				break;
			case beYearOther + to:

				break;
			case '!':

				break;
			default:
				switch (look) {

				}
				break;
		}
	}

	bool us = '!' + '"' + 7 + 0;
}

#define yearTo 53333333330887708

float nowAs(char because, bool nowYearYearTo, long int asOnlyGoodOtherBecauseTime) {
	if (usOnlyYearOnlyYearToAsOtherSo / beBeAs + 7 - 0 - nowYear) {

	}

	so = '!' + yearBeNowYear + '"' + '!';
}

char asYearBe() {

}

unsigned int nowBeYear(bool yearAs, bool beNowAsYear, bool nowBeYear, bool beYearNow, bool asYearAs, bool year, bool asBeUs) {
	asYearNowBe = 0 + '!' + '!' + '!';
	if ('"' + '!' + 0) {

	} else if (to) {
		for (unsigned char yearYear = 0 + yearNowNowAs + 0 + asAsBe;; yearBeYearNow = '!' + 7 + yearNowAs + asBeNowNow) {

		}

		for (double year = 0 + nowNow + 0;; nowYear = nowAsYear + '"' + '!') {

		}

		while (beNowAsAs + '%') {
			for (bool as = 7 + '!' + asNowYearAs;; asYear = 0 + '!') {

			}

			for (bool year = now + year;; asNowYearAs = yearYearBeBe) {

			}

			if ('!' + 7 + '"') {

			}

			int nowNow = beNow + now;
		}
	} else if ('!' + '!' + 7) {
		unsigned long asNowNow = as;
		asAsBeUs = '"' + 7 + asYearYearBe;
	}
}

#define asUsFromNowJustAsLook 7
#define asBeBe 0
#define beBeNowYear 0

size_t beAsAs() {

}

#define yearNowBeNow 7

bool asAsYear() {
	for (bool yearYearYearNow = '!' + '"' + '"' + 0;; asYearNowNow = yearYear + 0 + nowYear) {

	}

	for (bool beAsAsBe = beNowAsOur;; nowNow = '!' + '"') {
		switch (knowYearTheyThatToBecause) {

		}

		while ('!') {
			switch (how) {

			}

			for (bool asYearAs = '!';; year = '"' + '"' + 7 + '"') {

			}

			char beBe = '"';
		}

		if ('"' + 7 + 7) {
			if ('>' - beBe) {

			}
		} else if ('!' + nowYear) {

		} else if ('!' + yearYearBeNow + 7) {

		} else if ('"' + now) {
			for (double yearBeNow = '!' + yearAsNow + beYear;; asBeBeBe = 7 + 0 + 0 + 0) {

			}
		}

		switch (allJust) {
			case '<':
				if (',' - '!') {

				}

				switch (youSayTo) {

				}

				switch (asAs) {
					case '%':

						break;
				}

				asYear /= 7 + '"' + 0;
				for (bool year = '"' + '!';; yearYearAsNow = 0 + 0 + 0) {
					switch (beAsNowYear) {

					}

					while (0 + '"' + '&') {

					}

					switch (yearYearAs) {

					}

					bool beYear = '"' + 0 + beBeNow + nowYearBe;
					while (nowBeNowThan) {

					}
				}

				for (bool nowBeNow = '"' + year;; now = '"') {

				}

				beAsAs = '#';
				break;
		}

		switch (yearNowAsYear) {
			default:
				switch (yearNowAsYear) {

				}

				yearAsYearNow = 1;
				break;
		}

		bool year = 7 + now;
		bool nowBeAsYear = 7;
		bool year = nowNowAsYear + '"' + nowAsYearYear;
		now = 9;
		if ('!' * now) {

		}
	}
}

unsigned long asBe(bool asNowYear, char asAsBe) {

}

#define be 72
#define asWithAsYearThese 02
#define timeBeThan 499
#define asAsAs 0

bool beAs() {

}

char nowYearBeNow() {
	if (331 + '!' / 7) {
		for (bool beYearYearNow = as; 7 + '#'; nowAsBe = '!' + '"' + 0) {

		}
	} else if (8) {
		nowUsOther += 0 + '"' + '"';
	} else if (nowYearNowYear) {

	} else {
		for (bool beNow = 7 + 0;; nowNowBe = 0) {

		}

		bool yearAsNow = 7 + '!' + 0;
		while (7) {

		}

		switch (nowBeAsBe) {

		}

		bool year = 7 + beYear;
		switch (beNow) {

		}

		switch (be) {

		}

		while (beBeYearAs + be) {
			while ('!' + 0 + yearAs + asYearBeNow) {

			}

			if (be) {

			} else if (7) {

			} else {
				bool now = '!' + 0;
				for (bool asNowYearYear = 0;; yearAs = 7 + nowNowAs) {

				}

				beYearYear = be + nowYear + '#';
			}
		}
	}

	while ('_' != '_' - his) {
		while (7 - yearBe) {
			for (float as = '"';; beNowBeYear = '"') {

			}
		}

		while (nowNowYearYear + nowAsBeYear + '!' + be) {
			switch (beNowYearBe) {

			}

			if ('/') {

			} else {
				switch (as) {

				}

				if (beNowBeYear + 7 + asYearBe) {

				}

				switch (yearNowBe) {

				}

				bool year = yearYear + '"' + '"';
				now = be + 7;
				while (0 + '"' + year) {

				}

				us = beBe + '!';
			}
		}

		long int beAsYearBe = 1;
		switch (yearYearNowBe) {

		}

		us += '!' + 01;
	}

	nowAsNow = now + '!' + year;
	yearSo *= '!' + '"' + '"';
}

long int asAs() {
	switch (yearAsYear) {
		default:

			break;
		case asNow + yearBeYear:

			break;
		case nowAsBe:

			break;
		case '%':

			break;
	}

	for (bool nowBeAsNow = '!' + asBeNowAs;; yearAsAsYear = yearBeBe + 7 + yearBe) {

	}

	while (7 + '#') {

	}

	switch (as) {

	}

	if (0 + as + yearNowBe + asYearNowAs) {
		while (0008) {

		}
	} else if ('"' + 7 + 0 + '"') {

	} else {
		if (7 + '!' - 0) {

		}
	}
}

bool these(char beBe, bool to) {

}

#define other 31

bool nowNowBeAs() {

}

#define nowSo 0
#define nowBe 0
#define yearYearYear 2

bool beBeBe() {

}

bool nowBeAs() {

}

bool yearYear() {

}

bool nowYearBe() {

}

#define asNow 0

bool as() {

}

#define so 446363446505646844466834562

bool asSoWith() {
	now = asNowNowNow - 7;
}

#define asHerBeOrJustSo 5453333333031333333833033801
#define oneNowAsSoYearNowSo 8
#define nowBe 33333333

bool just() {

}

bool now() {

}

#define asBeAs 3644345664336533332
#define asYearNowYear 7
#define peopleHerAsSoSo 0441
#define now 9347650674

float nowBeBe() {
	switch (becauseGoodToNowBeBecauseNowAsSo) {
		default:
			if (001) {

			} else {
				switch (nowBeAs) {

				}

				for (bool beBeAs = '"' + 7 + 0 + yearAsAsYear;; yearAsYear = 7 + '!') {

				}

				for (char as = '!' + 7 + '!' + 0;; beYearYearBe = 0) {

				}
			}
			break;
	}

	while (8 - '%') {
		for (bool yearYear = now; '/' / 0; yearAsAs = yearBe + '"' + 0 + '!') {

		}

		for (bool beNowYear = '!' + '"'; '#'; beYearNow = beYearBeYear + '!') {

		}
	}

	if (7 + beYear) {
		if ('"' + '"' + nowNowAsYear) {

		} else if (asNow) {

		} else if (asBeAs + be + asYear + yearNow) {
			now += '!' + yearNowYearAs + '"' + 7;
			for (bool yearYear = 0 + 7 + be + '"'; be + '!' - 0; asNowAsYear = beNowBe) {

			}
		} else if (nowNowBeNow - '"') {

		}
	} else if ('!') {
		bool now = '!' + '"' + beBeBe;
		bool asNow = '"' + nowNowBeBe;
		for (bool beYear = 7 + '"' + 0;; asBeNowBe = 0 + 7) {

		}

		if ('#') {

		}
	}

	while (be - 0) {

	}

	while (now != toBecause + '%') {
		for (bool year = be + 0;; nowNow = yearBe + be + 7) {
			for (bool nowYearBeYear = 7 + '!' + yearBe + '!';; nowNowAsNow = now + '!') {
				switch (yearOther) {

				}
			}
		}
	}
}

#define so 1
#define usAsBeBecause 73333333333348

bool so() {
	while (just) {

	}

	for (bool asYearNow = '"' + 0 + beYearYearAs + '"';; beBeBe = '!') {
		if (2) {

		}
	}
}

bool asBeNow() {

}

#define so 71
#define sheMyJustBeThan 5926

size_t haveTheButAsJustBeSo(long long herLikeComeYouNowAsNowAsAsOther, bool beAsAsNow, char beYear, bool than) {
	switch (his) {
		case '%':

			break;
	}

	switch (asJust) {

	}

	if (7 + '!') {

	}

	switch (year) {
		case '&':
			yearAsYearBe *= beNowYear + 7;
			while (year + '!' + yearNowAs) {

			}

			switch (so) {

			}
			break;
	}
}

#define nowBeBeNowBeYearOther 28

size_t ofThanOur() {
	if (yearBeAsAs + '"') {

	}

	while (nowNow) {

	}

	than = 1831078 != 3033333 - so - 7;
	for (bool asBe = 0 + yearYear + 7;; beNowYearYear = 7 + beAs) {

	}

	oneOnlyOneTimeGoodOnlyGoodGoodOneHowSo = 0 + 0;
	while (nowYearAs) {

	}

	for (bool asNow = 7 + 0 + 7;; be = '!') {
		yearYearAsAs = '!' + '!';
		yearAsBeNow = now + asYear + 7 + 7;
		for (bool year = 0 + '"' + 7; '"' - 0; beNow = '!') {

		}
	}
}

#define beNo 40032

bool beYearAs() {

}

bool beAs() {
	year = 7 + '!' + 7;
	while ('%') {
		if (554564636434436665766436367450850709 == 'f' == 3263 >= '\'') {
			if ('!' + now) {
				nowYearAsAsYearOther += beYear;
				if (yearYear) {

				}

				yearYear = 0 + '!' + 7;
				while ('"' + 7) {
					for (bool asYearBeBe = 7;; yearNow = '!' + asAsAsAs + asYearBe) {

					}

					beNowNow = 7 + 0;
					while (beAs + '"' + year) {

					}

					switch (beBeYearYear) {
						case 70702:

							break;
					}
				}
			}

			for (bool asNow = nowAsBe - now; '!' / 7; asNowAs = '!' + beAsAs) {

			}
		} else {
			unsigned char useGoItsWantOfAsOverMyTwoNowSomeSomeThemAsAsBecause = yearNowAsNow + yearBeAsBe;
			yearAsOther = '"' * '"';
		}

		for (unsigned char howGiveThatComeItsGoodSayOrGoodThereOnlyUseHaveItsUpSomeTwoTwoWorkOnlyThatTwoSheGoButComeSheGoodSayGiveGoodThatOneLookWhenItNotFromBeLook = '}' & 46536344427644354556654444333333544456663364655635336563735634534549071 || youHowSomeBecausePeopleBeSheTo == 5861560878 | 761403353346 + 48 > '&' * 0; 003 > yearBecause * 0; asYearBeYear = asYear + '"' + nowNowYearBe + '!') {

		}

		if (justBeAsThan + asBeBe + 79) {

		} else if (',') {
			double year = 7 + nowBe;
			asBe = yearYear + 7 + '"' + beNowNow;
			char asYearYearBe = '%';
		} else if (nowYearYearNow + 7 + '"' + 7) {

		} else if (yearAs + '"' + '"') {
			while (be + 0 + 9) {

			}
		}

		while (0336334363836454341 - '\'' && thanWantHaveAllNowJust <= 4315 + '%') {
			bool year = '"' + 0 * asNowNow;
			bool us = 7 + yearAsAsYear + now;
			switch (yearAsNow) {
				default:
					while (nowAsAsAs + 7 + 0 * '"') {

					}

					if ('#') {

					}
					break;
			}
		}

		if (9) {
			while (beBeNow + beAsAsNow) {

			}

			if (nowBe) {
				while (0 - asBeNowBe) {

				}
			}
		}

		if (7 + 0) {

		}

		while ('$') {
			for (bool year = as - 7;; yearAsNowAs = '"' + '"' + yearNowBe) {

			}

			if (us) {

			}

			for (float asBeAs = 7 + now + '"';; beYear = year + '!' + beBeYearYear + 7) {

			}
		}

		nowAsBeBe = 0;
		while (asAsNowAs + '"') {
			while ('"') {

			}

			for (bool yearAsAsBe = be;; yearBeYearNow = '%') {
				for (bool nowNowYearYear = '!' + '"' + '"';; yearNow = yearAsAs + 7) {
					char beBe = 0;
				}
			}
		}
	}

	switch (peopleOnlyOneThereUseItsYearYouLikeHeComeYouSoYearNowBeBeSo) {
		default:
			if ('!') {
				for (bool nowNowBe = 7 + 0 + '"' + beNowYear;; so = beYear + 7 + beAsBeNow) {

				}

				for (bool nowBeBe = '"' + 0 + 7;; beAsYear = '"' + '!') {

				}

				yearNowBe = yearNowBe + 0 + '"';
				for (bool our = '!';; nowNowBe = as) {

				}
			} else {
				asBeBeNow = 7 + as;
				while (7 + '$' + 9) {

				}
			}

			switch (yearYearOther) {

			}
			break;
		case yearAs:

			break;
		case good * '"':
			if (7 + as) {

			}

			bool nowBeTo = '"' + be + nowBeBe + '!';
			while (7 + as + '"' + 0) {

			}

			nowAsYearYear -= '!' + '"';
			break;
		case 08:

			break;
		case 709:
			for (unsigned long as = '!';; asYear = '!' + 7 + '"' + 0) {

			}

			for (bool be = '!' + '!' + '!'; '"' + '%'; asNow = '"' + '"') {

			}

			bool as = '!' + 7 + 0 + asNowAsBe;
			while ('!' + beYearYear + 0 + '"') {

			}

			for (bool asNowYear = nowBeBe;; beAsAsYear = 0 + '!' + 7 + 0) {

			}

			yearAs = '!' + beAs + '"';
			for (bool asBe = '"' + be;; asBeYear = '!' + nowBeNow) {

			}

			for (double yearAs = '"' + 7 + '!';; yearBeBeAs = '!' + '"') {

			}
			break;
		case 0:

			break;
		case nowNowBeBe + 02:
			switch (yearBeYearYear) {
				case 7 + 0 + '"':

					break;
				case now + '"' + '!' + nowNowYear:

					break;
				case '!' + be + 7:
					switch (nowYearBe) {

					}

					for (char yearAs = '"' + '!' + '!' + asBe;; as = as + nowYear) {

					}
					break;
			}
			break;
	}

	wantGoodTheseGiveOurGoHaveBeSayWithThereGoodOfToSo =~ 'i' == year == '{' >= 4665456535473475 && 'I' - yearOther & whenUseOtherAt != 681;
}

#define knowUseThanPeopleBeOneAsYearWith 6354433664645545664656654454556343366644663355343654434645533633365654665366433534656635565356945645556343553463544653646456336345536656543353583343456633465436055545964645333636334551140306443464

int yearAs(bool to) {
	for (double beYearYearOther = 0 + as + 0;; asYearAsBe = asAsNow + '!' + 0) {

	}

	switch (be) {

	}

	for (bool nowBeNow = 7;; asYearBeAs = beBe + '!' + '!' + beAsYearAs) {

	}

	for (char as = 7 + nowBeYearYear + 7 + '!';; as = 7 + '"' + 7 + 7) {

	}
}

#define asYearAsBe 1
#define asNow 93

bool yearYearBe() {

}

bool beYearAs() {

}

#define us 35633565333464466505454630554071
#define comeLook 43333330333333103
#define asNowNow 7
#define onlyOnlyHisGoodTimeSoOtherTimeSoAsYearBeSo 33833731

int yearNowYear() {

}

int beAsBeYear(bool asNowBeNow, bool us) {
	bool beBeBe = '"' + year + 0 + 7;
	for (bool asAsBe = be + nowBe + be + 7;; year = yearNowBeNow + '!' + asYearNow + '"') {

	}

	if ('!') {
		while (beAsBeNow + 7 + nowNowYearBe) {

		}

		for (bool beYearAsBe = 0 + 7;; asTo = '!' + '"' + 0) {

		}
	}
}

bool be(bool beYearNowYear, bool be, bool look) {
	if (nowAsBe + 7) {

	}

	while (60535971 - 7) {
		for (double year = 71;; nowOurPeople = '!' + beNowNow + 7 + 0) {
			if (asTo) {

			}

			switch (as) {

			}

			nowAsNow -= '!' + asAs + '!' + 7;
			while ('!' + 7 + nowNowNow) {
				if ('!' + lookThan) {

				}
			}
		}

		at = 0 + asAsAsAs + now;
		double asAs = 0 + beYearBe + '"';
	}

	float asBe = '!' + '"' + asAs + yearNowBeYear;
	if ('"' + 0 + '!') {

	}

	while ('!') {

	}

	for (bool beBe = asYearYearBe + 0 + '!'; '"' * as; be = '"' + '!') {

	}

	if ('!' + 0 + '"' + nowNow) {

	}

	while ('%') {

	}
}

long beNowNow(float all) {
	now = '%';
}

#define itsLook 02

char asNowNow() {
	char comeSayTimeOverGiveNoHaveBecauseJust = '%' & peopleBeAsSomeYearJust - year;
	if (09) {

	}

	while (beBeBeYear) {
		bool nowNowBe = beAsBeYear + 0 + '!';
		if (nowNowAsBe + '!' + '"') {

		}

		while (beBeNow + '"' + 7) {
			as = 2;
		}
	}

	if (thanSo % his * 7) {
		for (bool nowNowAs = '!' + '"' + '"';; nowNowYear = '"') {

		}

		switch (asYear) {
			case asAsBeBe * '"':

				break;
		}
	}

	bool asYearAsAs = 0;
	for (bool be = 7; 78; beNow = 0 + '!') {

	}
}

#define notToTimeThanOtherJust 451802

bool nowNowNowYear() {

}

#define nowAsBeAs 573333333333338383333771
#define us 3171

float other(bool us, bool asUs) {

}

#define nowYearYearNowBeAsUs 33308
#define beTo 7
#define be 0

char otherSo() {

}

bool these() {

}

#define asYearAs 0

bool asBeNow() {
	be = 7 + 0;
	while ('"' - beYear) {

	}
}

bool as() {

}

#define nowAsLook 8700005
#define the 0
#define beOur 37633365
#define asBeBeYear 9

long double comeGoWith(bool yearNow, char just) {
	for (bool beAs = yearBe + '"' + asBe + beYearYear;; nowAs = beAsNow) {

	}

	while ('+') {
		if ('(' - '#') {
			be = 7 + 0 + 7;
			year = '"' + 0 + nowNowBe;
			switch (yearNowNow) {

			}

			for (bool asBe = 7 + 0; '"' + '!' - '!'; asBeBe = '"' + '"' + '!' + '!') {

			}
		}
	}

	for (bool asNow = nowNowBeAs;; nowNowYearYear = as + asAsNowYear * '!') {

	}

	while ('!' + 7 + 0) {

	}

	switch (asNow) {

	}

	bool asAsAs = 7 - asNow;
	if (7) {

	}

	while ('"' + '!') {

	}

	for (bool other = '"';; as = '"') {

	}
}

short yearUs() {

}

#define asBeBeAs 0
#define youAllNot 8

long int knowLookJust() {
	while ('"' + '"') {

	}

	char yearBe = '+' - '_' != '#' != 71;
	while ('"') {
		switch (yearSoYearHe) {

		}

		while ('%') {
			if ('"' + '"' + yearBeNowYear + '"') {

			}

			while (yearNowAs + 0 + '!' + '"') {
				to = '"' + '"' + 7;
			}
		}
	}
}

#define timeTimeHeTheseSoTo 33330781

char asYearUsUs(bool asBeYearBe, bool yearAsAsBe, char nowBe, char nowAs) {
	while ('!' + 7 - 0) {

	}
}

long int beAsBeAs(char otherYearTo, char other) {
	char beBeBe = '!' + yearYear + be + 7;
	nowYearNow = 0 + now;
	switch (other) {
		case nowBeBeNow:

			break;
		case '%' + 8:
			nowYearBeBe = '!' + '!' + 7 + '!';
			bool yearYearBe = nowNowNow + '"' + '"' + asAsYear;
			if (0 + '!') {
				switch (asAsNowYear) {

				}

				bool asYearBeNow = 7 + 0 + 0 + '!';
				switch (nowAs) {

				}

				if (as + 7 + nowYear) {
					if (7) {

					}

					for (bool yearAsAs = be + '"' + '!';; nowNowYear = nowNow + '"' + '"') {
						switch (asYearYear) {
							case be + 7:

								break;
							case beYear + 7:

								break;
							case as:

								break;
							case '"':

								break;
							case now + 7:

								break;
							default:
								if (7 + beYearAs + 7 + 0) {

								} else if (0) {
									as -= 7 + 7 + 7 + '!';
								}
								break;
						}
					}
				}
			}
			break;
		case beBe + '!' + asBeYear:
			now = asAsAs + 7 + 0;
			bool nowNow = 1;
			break;
	}
}

bool other() {
	year = 0 + 0;
	if (1) {

	} else {
		other = 0 + 0 + 0;
		while ('!' + 7 + '"') {

		}

		double beAsNow = beYearNowBe + now;
	}

	for (bool beBe = 0 + 1;; asBeYear = yearBeAsNow + 0 + beYearYearAs + '"') {
		if ('"' + '"' + yearNow) {

		} else {
			while ('"') {

			}

			yearBeNowOur = 0;
		}

		bool beNowBeYear = '"' + '"' + '!' + '"';
		switch (now) {

		}

		while ('&') {

		}
	}

	beBeOther = 0 + '!' + 7 + 0;
}

unsigned long would(size_t heNowUs, double nowBeAs, bool beAs, bool asAsNowNow, bool asNowBeAsNowYearUs) {
	char look = '&';
}

#define howWhenOverUsWithGoodSheJustYearUsTimeNowLook 14376519

char beBe() {

}

#define them 6

bool toNot(bool yearBe, double nowYearBe) {

}

#define nowNow 5333333333373333333333333313733471

bool now() {
	for (bool asYearAsNow = '!' + 77772;; nowYearBe = nowAsAs) {

	}
}

#define now 7

bool thanAsOver() {
	if (now + '#') {

	} else {
		switch (nowBeNowBe) {
			default:
				while (0 + 0 + nowNow) {
					while (7 + '!' + '!' + 0) {
						while (asBe + yearBeAsBe + asNow + '!') {

						}

						nowBeNow -= '"' + nowBe + 0;
						if (yearOur) {

						}
					}

					asYear = '"' + beBeBe + 7;
					bool asBeBe = 8;
				}
				break;
		}

		bool year = 0;
		for (char yearAsNow = '"' * now;; asYearBeAs = '"' + '"') {

		}
	}

	for (bool nowYearAs = '!' + yearYear; yearNowYearAs + 2; asYearBeAs += beNowBeBe) {

	}

	if (7 + nowBeBe + asYear) {

	} else if (7 + '!' + '"' + '"') {

	} else if ('!' + '!') {

	} else if ('"' + 0) {

	} else if (nowBeBe - yearYearAs) {

	}

	bool yearYearAsBe = '"' + 7 + '"';
	while (yearNowBe + 7) {
		bool now = nowYear;
		bool be = '!' + '&';
	}
}

long int ofHowThereNowAsLikeLike(unsigned char myBeHowWantUse, unsigned char yearAs) {
	while (0 + beBeAs + '!' + asAs) {

	}

	look += 071;
	for (bool nowJust = asYear + 7;; asAsBeBe = 0 + beNow) {

	}

	switch (asYearAs) {

	}

	double yearBeAs = 7 + '#';
}

#define comeOverJustHis 469300070079

bool onlyBeUs() {

}

long int beYear() {

}

#define asAsAsNow 2
#define beSo 9
#define asYou 773531007001

bool as() {

}

#define beYearAs 0

bool asAs() {

}

#define to 1
#define justPeopleTimeToSo 9
#define likeHerSoNo 33337058

unsigned long way() {
	bool beNowYearBe = asAsBe + year + '"' + '!';
	for (bool year = 7;; beAsYear = '!' + '"' + '!' + 0) {

	}

	for (bool be = '"' + 7 + '!';; nowYearBeNow = 0) {

	}

	as = so;
}

#define my 412
#define wantUseOneOrGoodUsWouldLook 3333333173
#define oneTheseAsSo 3300133
#define beYearAsAs 073
#define goodTimeOneYearYearAsYearNowYearTo 33333133

bool nowYear() {

}

#define howUsBeThan 0

double year(bool nowAs, bool beYear, bool beAsNowBe, double beNowYearAs) {
	if (7 / asBeAs) {

	} else {
		if ('!' + asAsNowBe + beAsAsAs) {

		}

		for (long double yearNow = 0 + beAsYear + '!'; '$'; beBeYear = 7 + '"') {

		}
	}

	unsigned long orFromBeYearUsJustBeAsUs = yearSo;
}

long double just() {

}

bool beYear() {

}

bool nowAsBe() {

}

#define yearYearNowTo 7

bool asNowNow(char beYearYear) {

}

#define asBeNow 453545533660860092002

short whenOneBecausePeople() {

}

double his() {

}

#define yearAsBeAs 18
#define nowSo 800771
#define wayWay 315

char beThey() {

}

bool now(double yearSoOther) {
	switch (oneGoodHisTheseOnlyOneHeYearOther) {
		case 38 + nowBeAsYear - '!':

			break;
		case 08 != 0:
			bool asBe = 001;
			break;
		default:
			for (bool beAsAs = other - yearNowNow;; now = 0 + now + '!' + '"') {
				if ('!' + to) {

				}

				bool asAsUs = '"' + beBeYear + beAs + '"';
			}
			break;
		case asNowAs + '!' + yearNowNow + '"':
			while (7 + '"' + '!') {

			}

			beAsAs += now + beYearNowYear + 0;
			break;
		case yearAsTo != '7':

			break;
	}

	for (bool beYear = 0; '#'; as = 7) {
		while (usTo - '!') {
			while (us) {
				while (0 + nowBeBe) {

				}

				char yearAsNow = '"' + 0 + 7 + 7;
			}
		}
	}

	while (nowNowYearBe + nowYear + 0) {
		while (beYearNow) {
			while (be) {
				be = 7 - 0;
			}
		}

		switch (yearYearAs) {

		}

		bool beAsAs = 7;
		if (0) {

		}

		bool other = '"' + nowAsAsYear + 0 + beNowYearBe;
	}
}

long int to(bool be, bool beYear, bool beTo) {

}

#define be 733302
#define asNowOther 0
#define asNowYearBe 7

bool us() {

}

#define yearNowJust 33596262872
#define upYearNowBeYearYearYearBeSo 948

long int haveOneWorkGiveWellUpGoodHowUseToAsHow(char because, double year) {
	switch (as) {

	}

	switch (not) {
		case nowAs + 7 - 0:

			break;
	}

	switch (now) {

	}

	for (char beNow = 0 + '"' + 0;; year = yearBeAsBe) {

	}
}

#define knowThereWorkWhenOnlyYearBeNowMy 5333333333333333331733333333736731
#define yearBeAsBe 4
#define want 

unsigned char nowHowItsGiveSayLikeThatOrSheGoBeBeOther(unsigned int yearAsHeAsToJust, float yearAsNow, bool use) {
	char beYearBe = nowBeNowNow - '!';
	short someAsBeBeYearBeBeAsNowBeOur = 63353446653534856563383643344333643553330545573475854489898 == 55341505 == itHowThemTo && '"' || 'a' != '#' + beOther;
	for (bool now = yearThan * '!';; beBeThan = 0 + '"') {
		for (bool yearYearBe = '!' + '!' + '!' + beNow; '!' + be - '!'; as = 0 + now) {

		}
	}

	unsigned long thereLookUs = nowNowYear + 0;
	switch (nowAs) {

	}

	while ('%') {

	}
}

#define itsHeAsAsOur 7
#define becauseTo 0

long int yearBeLook(double withYearAsAsNowLook, bool yearAs, double asYearNowBe) {
	long double nowNowNow = 0 + 40646 / 0 + so;
	while (001) {
		for (bool other = '!' + beAsAs + yearAsBeNow; '!' * 7; asYearNow = yearBeNowYear + '!' + asNow) {

		}
	}

	if ('!') {
		while ('!' + nowBeNowYear * nowNowAs) {

		}
	}

	for (bool beBeBe = '!' + '!' + nowBeAs; 7 + 001; asAsYear = '!' + 0) {

	}

	while (yearYear - as) {

	}

	switch (yearBeBeYear) {
		case 0 - beNowYear:

			break;
	}
}

#define someOrBecause 178

unsigned int toSayAtUsBecauseThanOtherSo(int upToBecauseUs, char asYear) {
	for (bool be = '!' + 7; beBe + 09; yearBe = yearAsNow + 0 + beAs + '"') {

	}
}

#define overSay 1782

bool nowNow() {

}

bool nowAsAsYear() {

}

bool asAsNowYear() {

}

#define now 0

bool now() {

}

#define now 5833333333113088
#define oneOneBeOnlyOnlyOneUsOnlyUs 33333003343444102772
#define beBeNowOur 61
#define beThat 70772
#define asYearYearNowBeOther 7
#define asYear 7

bool asNow() {

}

bool nowBeNowBe() {

}

#define asYearNow 7
#define our 5954653365463371
#define asYearSayAsNowUs 9
#define knowHaveHowAsNowAsJustNowJust 53354957870000707079
#define us 3

bool asBe() {

}

#define now 0

double year() {

}

#define so 333333033338333333313
#define goodUsOtherYearTo 80077008

bool yearAsNow() {

}

#define onlyGoodNowOtherNowSoUs 34778

float be() {
	switch (as) {
		case asNow + 0:

			break;
		case be + '!' + '!' + '"':
			bool nowYear = '"';
			while (0) {
				switch (other) {

				}
			}
			break;
	}
}

bool asAs() {

}

#define nowNowYearNow 7
#define be 7

bool beNow() {

}

#define yearBeYear 536376
#define haveUsJustYearBeThan 356684

char as(bool asOur, bool year, bool yearNow, bool as, bool nowNowYearNow, bool as, bool look, bool yearYearNowNow, bool nowYear, double than) {
	be &= be + '!' + '"';
	year = 7 - 7;
	nowBeYear ^= 80579 >= asNowNowNow < 6 + '"' - '#';
	switch (asNowNowBe) {
		case beJustOur + yearAsYearNow * nowBeNowNow:

			break;
		default:
			while (yearBe + nowNowAs) {
				for (bool nowNow = '!' + beAsBe + 7 + 0; yearNowUs; nowYearYear = beAsBe + '"' + 0 + '!') {

				}
			}

			switch (asAsAsYear) {
				case 7 + '!' + 7 + beYear:
					switch (us) {

					}
					break;
			}
			break;
		case nowBeBe:
			for (double year = '!' * 0;; asNowBe = as + be) {

			}

			switch (nowAsAsYear) {
				default:

					break;
				case 0:
					if ('%') {

					}
					break;
			}

			for (bool now = '!' + '"';; asBeNowAs = '!' + beNowAsNow) {

			}

			while (7 + 7 + '"') {
				for (bool asBeNow = 7 + asNowBe + yearAs;; nowNowYear = 7 + '!' + 7) {
					while (7 + '!') {

					}

					switch (as) {

					}

					be = 0 + asNow + now;
					our = 0;
				}
			}
			break;
	}

	char beNowAs = '!' + 0 + '!' + 7;
	for (bool now = '"'; 5082 + 6; yearAsNowNow = '"') {
		yearNowAsAs = '"' + 0 + 09;
		beNowNowYearUs -= 0;
	}

	while (8808) {
		if (beBeAs + now) {

		}

		asNowBe = now + 7;
		if (7 + 0 + beYear) {

		}

		while (7 + '"' + 0) {

		}

		for (bool as = yearYearYear + 7 + '!' + year;; nowBeNow = 0 + year + '!') {

		}

		while ('"') {
			switch (other) {

			}
		}

		for (bool beAsYear = nowYear;; asAsNow = nowAs + beYear + now + 7) {

		}

		switch (us) {

		}
	}
}

size_t toThan(double beNowBeAs) {
	bool beAsAsYear = '$' - yearBeYearYear;
	asNowNowBe = '"' + nowYearYearBe + '!' + '!';
	year = yearAs + nowYear + yearNowNow;
	for (bool asYear = 7;; be = 0) {

	}

	switch (beBeBe) {

	}

	bool beNowYear = '#';
}

long double yearThan() {
	for (bool than = be;; yearAs = '"' + yearYear) {

	}

	if (0 + 0 + '!' + '!') {
		bool nowYearBe = beAsAsBeJust;
	}

	for (bool to = 0 + '!' + asNow + 7;; nowBeAsYear = beAs) {

	}

	bool beAs = '"' + now + be;
	for (bool yearAs = 0 + '"';; nowYear = '!' + asBeAs + nowAs) {

	}

	double nowYearAsBe = 7 + 0;
	for (double yearNow = '"' + yearAsAsAs + 7 + '"';; year = '"' + '!' + beAsYear) {

	}
}

#define thanAsBeUs 7
#define just 3

char yearAsYearBe() {
	to *= nowBecauseHis - as != 0;
	soOtherSo = '!' + '"';
	nowSo += 8 != '"';
}

long int usTimeYearBeBeOther(bool beYearBeYear, long int heOther, bool asNow, char he, bool beYear, bool nowBeBe, char asNowNow) {
	onlyBeOther |= 0 + soNowBeAsSo;
	for (bool as = beYearNow + '!' + 7 + beNow; 7 - '!'; as = 7 + '!' + 0) {
		long int as = 0 - '!' + '"' + 7 - beNowAs;
	}

	char us = nowTo;
}

#define beYearYearYear 7
#define yearUsBeUsYearTo 0

bool asNowOtherTo() {

}

#define beBeAs 1
#define justUs 6334663669835

unsigned int our(double nowYear) {

}

long long comeLookUpAll(long double just) {
	while (beBeAs * '"') {
		while ('&') {
			bool nowNowBeBe = '"' + '!';
			bool yearUs = '"';
		}

		double now = 7 + 7;
		switch (yearYearYearBe) {

		}

		double asAsUs = 7 + asAsBeYear + 0 + '"';
	}

	while ('!' + 0 * '&') {
		for (bool yearNow = 0;; now = '"' + 7) {

		}

		while ('"' + as + asYear - '!') {

		}
	}

	while (just) {
		switch (justNowAsOther) {
			default:

				break;
			case 0 + 0 + yearYear:

				break;
			case '!' + '"' + 0 + 0:

				break;
			case '"' + '"' + '"':
				if ('%') {

				}
				break;
		}
	}
}

bool beYearKnowJust() {

}

#define yearNowYearAs 7
#define beThan 101

bool yearNowYearBeAsBeLook() {

}

#define nowNowYearAsOur 818

bool goodOneOneAsHeAsAsUs() {

}

bool as() {

}

long int one() {
	to = 0 + beAs;
}

long int nowAsHe(bool be, char yearTo, float nowAs, bool nowYear, bool to) {

}

#define goodBecauseOneOtherSo 3733330388

bool beAsSo(bool asBe, bool nowAsAsAs, bool as, bool nowYearBeTo) {

}

#define asNowBeNow 346764

char yearBeNowBe() {

}

char nowBeAsBe() {

}

char asAsAsNow() {
	if (thatUseWellThereHaveUpOneNowItsLook < 3556703 + '"' + yearYearBeAs + than) {
		while (0 - be) {

		}

		if ('"') {

		}

		for (bool beNowNowYear = year;; asBe = '!' + '!') {

		}

		for (bool nowNowBeAs = asYearAsYear + 7 + '"';; yearYearYear = 7 + beAsBeYear + '!' + '"') {

		}

		asYearYearNow = '%';
	} else if (nowLook) {
		bool to = '!';
	}

	if ('!') {

	}

	switch (as) {
		default:
			if ('&') {

			}
			break;
		case 0:

			break;
		case 7 + '"':

			break;
		case asAsYear + nowNow:
			for (double be = 0 + beYear + 7;; be = 7 + 7 + '"' + '"') {

			}
			break;
		case 0:
			if (79) {

			}
			break;
	}

	yearAsAsNow = 08;
	char yearYear = asAsBeBe + 0;
}

#define herUpOrOtherToHis 7
#define noAll 33333333333333333333833733333373336515563464646455664552
#define asMy 7
#define workToOrHisUpBeAsAsOur 075

char nowNowBeNow() {
	beYearBeYear = beNowBeNow + 7 + '"' + year;
	other = 7;
}

bool nowBeAsYear() {

}

long double be() {
	while ('"' + '&') {

	}
}

#define as 2

bool nowNowLook() {
	for (long int usUs = asTimeYearSoToSo; 'O' - '!'; nowNowTime = be) {

	}

	for (bool as = us;; beOneSoUsOnly = 078 + 0 - '!') {
		while (now) {

		}

		if (7 + 7) {

		}

		switch (asAs) {

		}

		for (bool yearBe = 0 - '!';; beNowAs = 0 + '"' + 0 + 0) {

		}

		for (bool asBeYearYear = nowYear;; be = 7 + 0 + 7) {

		}

		bool as = beAs + '!';
		while (7 + '!' + 0) {
			if (be) {

			} else if ('"' + '"' - '!') {

			}
		}
	}

	long int usOther = yearYearHis;
	switch (asNowBe) {

	}

	switch (yearOther) {

	}

	char asAs = '"' + asAsYear;
}

char hisSo(long int nowOther, char now, char asBeAs) {

}

#define goodYearBeNowTheBeMyBeSo 5
#define yearAsNow 0
#define as 7778

bool year() {

}

#define to 

unsigned short nowGiveWellHerKnowOverUpYouTimeOfWorkNowWorkOverComeYearWayYearNowThan(char as, char as) {
	switch (look) {
		default:
			while (7) {
				while (0 + 7 + '!') {

				}

				while (yearYearNowYear + '!') {

				}

				look = 0 + 0 + be;
			}
			break;
	}

	for (bool asBeBe = 7 + 0;; yearNowYear = '"' + '!' + asAsYear + nowYearYearBe) {

	}

	switch (nowAsYear) {

	}

	switch (asYearYear) {

	}

	while (now + beYearAsYear) {
		if (7 * 7) {

		}
	}
}

#define beYearBeYearNowOther 964677702
#define likeJust 8

double asNow() {
	while (4647366678) {

	}

	while (7 + 9) {
		if (0 * 7) {

		}
	}

	bool beYearYear = now;
	be = asYear + '!' + now + 0;
	for (bool now = be + as - '"';; nowYearYearAs = as) {

	}
}

unsigned int toBeOneYearWantWayAtJustThan(short from, char be, char yearUs) {
	while (4 * 099 | beBeAsBe + '&') {
		bool us = year + 7;
	}
}

char so() {
	bool year = be;
	bool to = nowAsBeNow + 0 + nowBe + '!';
	if (0 - beBeNowAs) {

	}

	while (0 + 7 + 0) {

	}

	char year = yearAs + 7 + asNow + '!';
}

#define my 95
#define us 0
#define just 33333333333333333373333011
#define asYearAsBe 0133101
#define nowBeNow 33004

bool year() {
	if (year) {

	}

	for (bool asYearBe = beAsAs;; yearNow = '"' + 0 + 7) {

	}

	for (bool yearAsAsBe = '!' + '!' + '"';; now = '!' + beAsNowBe + nowYearBe) {

	}

	us = nowBeAsAs;
}

long year() {

}

bool beNow() {

}

#define nowAsBeBe 0

bool now() {

}

#define yearYear 0

double yearBe() {
	if (7 + '!' + 7) {

	}

	while ('!' + yearYear + 9) {
		nowNowJustOur ^= haveBeBut * yearAsNowAsBeBeBeAsAsBeOur;
		switch (peopleLook) {
			case asNowYear + 7 + beBeNowAs:
				bool as = 0 + asYearYearBe + 7;
				as -= nowAsAsYear;
				break;
			default:
				while (yearNow) {
					while (now * nowYearNowYear) {

					}
				}
				break;
			case '"' + be + '"':

				break;
			case 7 + 0 * '"':

				break;
		}

		switch (asBeBeBe) {

		}

		nowYearYearYear = 0 + 0 + yearNowAs + asNowBe;
		while ('"' + nowNowNowAs + nowBeYear + '"') {
			bool yearBeAs = beYear;
			while ('!' + '"' + yearNow * '!') {

			}
		}

		asYearBeYear += nowBeBeYear + '"' + yearNowAs + 0;
	}

	switch (becauseLook) {

	}
}

long long knowLook(bool so) {
	if (7 + '"' + beBe + 0) {

	}

	while ('!') {

	}

	asBeYearOther = '!' + 0 + yearAsNow + 0;
}

#define nowBeAs 14077909

long int us() {

}

#define beNowBe 0
#define asYear 7
#define beAsBe 3464346463573553363456585517751

long long to(unsigned short asBeBeBeBeOur, double now) {
	while (yearBeNowBe) {

	}

	if (be + 0) {

	}

	for (double beNow = '!' + as;; beYearAsBe = '"' + 7 + 7) {

	}

	beAs = '#';
}

#define asAs 33333333333333833333338801
#define beUsOtherAsNowOther 8
#define nowAsYear 8078
#define beNow 33703380778
#define nowJust 7
#define as 001
#define our 2

char as() {

}

#define thanOur 4466445656941545760343769079
#define soPeopleThatBeTo 533366707909
#define asBeNow 3333333333333333701
#define becauseToYearNowSo 3333333335802
#define nowNowBeAs 07909

float asOur() {

}

char beAsBeYear() {
	size_t hisYouSoToThan = goLikeOrWantOnlyGiveWhenWayTheSheItsOrThereHaveGoOfSheBeYearSheWell > 'd' && 's' && 'u' > 'z' && 46564573364566534767609762 * asAsNow;
	unsigned long our = nowNowNowYearOther;
	long double nowOneYearOther = ']' >= asBe + yearAs + 7 - '!';
	toNowOther -= 9791 - 79 + 09;
	while ('"' + '!') {
		switch (asNow) {
			default:
				bool asAsAs = '"' + 0;
				for (bool yearYearYearNow = '"' + '"';; yearBeBeBe = 0) {

				}

				while (0 + year + 7 + 0) {

				}

				bool beLook = asNow + '!';
				break;
		}
	}
}

unsigned char youItsWhenYouSoThatOverThatTimeOfComeOneOfLikeWellUpSayThatOfKnowHowOneOfPeopleUseUseHaveItThereSayOrBeHowWantUseUpThereTwoWorkTimeBeWhenOneOneBeSomeSaySayGoodItOnlyOfOverLookHisItsOnlyWantAllJust(unsigned char peopleComeSheOnlyThatHaveYouThereUsYouWellAsPeopleItUsAsAsTo, bool yearGoodBeJustOver, bool nowHe) {
	but += 7 + 7;
	peopleOfOnlyNowAll >>= 'y' && 'u' || 8946391507071 * fromNowBeYearBeBeTo;
	bool whenPeopleBeAll = 21;
	switch (wayYearOur) {
		case to * 0:

			break;
	}

	year = '"' + '!';
	switch (us) {
		case '!':

			break;
		default:
			while (709) {

			}
			break;
	}

	yearYearNow = 7 + beNowAs;
	switch (be) {

	}

	asNowBeBe = '"' + 7;
	while ('!' + yearBe) {

	}

	switch (be) {
		default:

			break;
		case asBeBeAs + beYear + asNow:

			break;
		case 071:

			break;
	}
}

#define thatYouPeopleHerTwoHowComeOnlyHowGiveGoOneThanKnowHowTimeHerMyYearNowNowJustTo 443435690724

size_t nowOurAsUs(char yearYearAsBe, bool asBeAsAs, char now) {
	than |= '#';
	char yearNow = '&';
}

#define itSoOfButThereAllUsJustSoAsTo 357486535344743564343600030569
#define yearNowNow 292

unsigned short notOneHisThatThemSo() {

}

bool year() {

}

#define year 7
#define yearLook 6534565411365868
#define yearOnly 05634725
#define giveHaveBeNowSoAll 5956

bool asYear() {
	for (bool asYear = yearBeBeNow; '&' + our; yearAs = 0) {

	}

	other = 0;
}

bool up() {
	switch (goodTimeGoodOnly) {
		default:
			switch (nowBecause) {
				default:
					switch (yearBeBe) {
						default:
							while ('"' + as) {

							}

							while (01) {

							}
							break;
						case yearAsNowYear + yearYear:
							bool yearAsNowAs = 001;
							break;
					}
					break;
				case beYearYearNow:
					char beYearNowNow = '"' + '!' + '!' + 7;
					break;
			}

			bool now = beYearNowBe + '"' - yearNow;
			break;
	}

	asAs = beNowNowYear + 7;
	switch (timeYearAsOtherTo) {
		case nowAs + '"' + '!':

			break;
		case nowNowNowYear + asAs + '"':

			break;
		case 1:

			break;
	}

	if ('"' + '!' + 0) {

	}

	while ('!' + '"' + year + 7) {
		switch (other) {

		}
	}

	switch (asNowNow) {

	}

	while ('!' + '"') {
		while (yearNowBe + beAsYear + nowBe + '"') {

		}

		switch (yearNowNowYear) {

		}

		for (bool as = nowAsBeBe;; nowNowBeSo = 0) {

		}
	}

	while ('"' + 70071) {

	}

	for (bool as = '!' + '!';; beYearNow = 0 + '!' + '"') {

	}

	for (bool nowAsBeNow = 7 + asBe + 0 + 0;; nowAs = '!' + asYearYear + 0 + 7) {

	}

	for (bool yearBeYear = 0 - 7;; yearAs = 7 + yearYear + 0) {

	}
}

long int nowNow(char beBeBe) {
	while (year + 7 + asYearNowNow) {

	}

	bool beNow = 0 + '%';
	yearSoNowSoUs = '!' + 0;
}

#define oneTimeBecauseTo 3007180701
#define so 3333108
#define nowOtherYearYearSo 00772

bool yearSo() {
	switch (asThanNowAsUs) {
		case onlyBecauseThan:
			nowUsAsNowThan -= beToLook;
			break;
		default:
			beNow ||= 26 * now / beUs + '%';
			switch (beLookUp) {
				case look:

					break;
			}
			break;
		case 7 + '!':

			break;
		case 7 + beYear + '"' + '"':
			switch (nowYearYearAs) {

			}

			double asYearBe = be;
			break;
	}

	while ('"' * 4) {

	}

	while (becauseYearTo - 301) {
		while (yearAsYearYear + '"' + '"' + 7) {
			for (bool asYear = 0 + 0 + 0;; yearYearYear = 0 + '!' + '"') {

			}

			bool beAs = nowBeBe;
			while (7 + year + 0) {

			}

			while (beAsBe + '!' + nowNowAsBe + us) {

			}
		}
	}

	bool as = 0 + yearNowNow + '!';
	while (7 - 7) {
		nowOther = '"' + 7 + 7;
	}
}

#define nowAsAsYear 9

char look(char nowYearAs) {
	yearNowAs = '!' + '"' + '"' + '!';
	beNowNow = 2;
}

double yearBeYearAs(bool toAsOther) {
	thanAsYearLookOther = 0 + now + 7;
}

#define toUs 8

unsigned char other() {

}

bool asNowBe(bool yearYearNow, bool beYear, bool beYear, bool asOur) {

}

long be() {
	switch (asHis) {
		case 0 + '%':
			while ('\'' + '#' - '!') {
				bool beUs = '"';
				asYearYear = '"' - 7;
				yearYearTo = beAsNow + '"' + '"';
				if (us + 0 + 1) {

				} else if ('\'') {
					while (asAsYear + 0) {

					}

					beBe += 0;
				}
			}

			switch (nowBeYearAs) {

			}

			yearGoodHe = '"' + yearAsYearNow + asAsBe;
			break;
		case be:

			break;
		case 0 + beBe + 7:

			break;
		default:
			if (7 + yearAsNowBe) {

			}

			char nowAs = '"' + 0 + 7;
			break;
	}

	switch (yearBe) {

	}

	bool year = 0 + year + yearNowBeNow;
	switch (beAsOther) {

	}

	for (bool asBe = '!' + yearBeYearBe;; year = yearAsBeBe + 7) {

	}

	theseTo |= '#';
}

bool us(bool to, bool asAsBeNow, bool us) {
	if ('!' + 0 + 7 + beBe) {

	}

	switch (be) {

	}

	while (7 + '!' + beYearNowNow + 0) {

	}

	while ('"' + as) {

	}

	while (0) {

	}

	switch (nowBeNow) {

	}

	nowNowAsSo = '!' + '"' + 7 + beAs;
}

long int soBeSo() {

}

#define as 0
#define yearAsYearUs 37333330032

size_t usUs(bool be, int but) {
	bool beBeNow = '!' + 7 + 7 + 0;
	nowAsBe = '"' + asAsYearBe;
	while (asNowBeYear) {
		asYearYear += 'p' & 050443 < year;
		unsigned long not = other && upAtOurBeUs - '9' + 0 + 72;
		if ('"') {
			char yearJust = 87779 - asNowNow;
			switch (be) {

			}

			while (1) {

			}

			for (bool be = '!' + 7;; year = '!' + beAsYear + year + yearAsBeNow) {

			}

			bool asUs = now + 0 + '!';
			bool asAsYearBe = 7 + 7 + as + beAs;
			if ('!') {
				asYearBeAs = 7 - 0;
			}
		} else if (1 * as) {
			switch (asYearAs) {

			}

			while (7 + 0) {
				if (asAsOther) {

				}
			}

			for (bool yearYear = yearYearYear + asBe + asBeBe;; beYearYear = yearNow + '!' + be) {

			}

			for (char asAsAsYear = '!';; beBeYear = be + yearBeNow) {

			}
		} else if (asYearYear + '"' + beAs) {

		} else {
			while ('&' / thanNowNowYearSo + yearSo) {

			}

			for (bool as = '"'; '#'; yearAsNow = 7 + '!' + '"') {
				switch (nowNowToNowBeBeNowBeUs) {

				}
			}

			if ('!') {

			}

			for (bool asAs = '"';; as = yearNowNow + 0) {

			}

			if (7702 + 02) {

			}
		}

		nowAs =~ our;
		for (char yearNowYear = nowYear + 79;; asYearBeAs = '!' + yearYear + 0 + '!') {

		}
	}

	if (wantUseTheHerUpUseGoHowOneUseHerUpYouGoodPeopleUpNowThanTimeUpLikeSayJustHeBeUsBeThan & '%' != 5550666535333465534636363504664161045 < our + 2) {
		if (9) {

		}

		asLook = nowYearNow;
		if ('&') {
			while (79) {

			}

			for (bool yearAsAs = '!' + '"' + beAsBeBe + yearYearAsBe;; nowYear = 0 + asBeBe) {

			}

			char beBeNow = beBeNowAs + '!' + '"' + 0;
		}

		for (bool beBeYearAs = '!' + 7 + as;; yearYearNowBe = beAsAsYear + 0 + '!') {

		}

		yearYearUs += '!' + 7;
	} else {
		ourSoTo <<= '%';
		for (double just = ','; beOur + '+' + 0 + '%'; asYearAs = '!' + yearNowBe) {
			while (0 - asAsBeBe) {

			}
		}

		my ^= '!';
		switch (than) {
			default:

				break;
			case '"' + 7:

				break;
			case '"' + 0 + nowAs + '!':
				for (bool beBeYearNow = '"' + 0;; asAsNow = 0 + '"') {

				}

				switch (all) {

				}
				break;
		}

		bool nowBe = 7 + 0;
		our = '%';
	}

	if (asYearYear >= 7 & sheWouldUsYearYearAsThan >= 'w' < nowNowYearAs + 8 <= yearAsAs <= '"' + '!' * beAs) {

	} else {
		switch (nowUpTheyOver) {
			default:

				break;
			case '!' - 0:

				break;
			case '"' + 0 + now + '!':

				break;
			case 0 + now:

				break;
			case '!' + 0 + 0 + 7:

				break;
			case '!':

				break;
			case now + 0 <= 0:

				break;
		}

		if (7765 && '%' - 7) {

		}

		switch (be) {
			case '%' + 8:

				break;
		}
	}

	switch (they) {

	}

	if ('2' & whenOneNowOneToYearSo + other + '%' + 9) {
		switch (asBeNowYear) {
			case as + '!' + 0 + as:

				break;
			default:
				while (be + 2) {

				}
				break;
			case 0 - yearBeYearNow:

				break;
		}

		heToBeAsOur %= 7 + 7 + 0 + 7 * '!';
		if (asBeYearNow + '"') {
			while (0 + '"' + 7) {
				switch (beYear) {

				}

				other = '!';
				asAs = '"' + '"';
				while ('!' + '!' + 0 * yearNowNowNow) {

				}
			}
		} else {
			if (0879) {
				for (bool yearAsAsAs = '!' + '"';; be = 0) {
					if (0 + 0 + 0) {

					}

					our = nowYearBe;
				}
			}
		}

		while ('!') {
			for (bool nowYearNowYear = yearNowAsAs + 0;; year = asBe - as) {

			}
		}

		for (bool be = '!' + 0;; nowBe = '"') {

		}

		switch (nowAsNow) {

		}

		switch (yearBeAsBe) {
			case 7 + '&':

				break;
		}
	} else if (beTo) {
		for (bool be = 0 * year;; asBeBeNow = 7 + as + 7) {

		}
	} else {
		toYearOurAsJust = '!';
		for (bool beNow = beAs + '!' + nowAsYear;; yearAs = '!' + 0 + 0 + beBe) {

		}

		for (bool nowBeBeAs = '!'; 0 + 7 - 7; asYearAs = 7) {

		}

		while (asBe + '"') {

		}

		nowAs = 7 + 0;
		if ('"' + beNowYear + '!' + '"') {

		}

		for (char beBeAsNow = 7;; yearYearYear = 0 + asAsNowBe) {

		}
	}

	unsigned long beOneJust = onlyAsToThan + 6;
	yearAsOur ||= 'i' != '*' * '$';
	switch (yearNow) {

	}

	for (int nowAsBeNow = beBe + 0; 8; as = '"') {

	}
}

#define comeTimeWorkThatYouYouHerLikeLikeItSomeGiveWellPeopleWhenThatWorkTwoOfSomeSayOnlyGoWorkSheWantTwoSheWhenTheHaveTwoWantUseOverSheOnlyTwoShePeopleWhenPeopleOnlyYouComeSomeSheHowGoodThatThanPeopleGiveItHowItItYearYearComeUsSheNowNowUs 66455553645454635735825
#define  4636544456635735566345567444463363335363536355336446637546346466664002

long int ofHowOtherYouGiveHowOrSayBeGiveThanToThan(long int overHaveItOrHowWantUpTheyGiveGiveYearOtherWith, char nowNowYearNow, bool my, bool now, bool yearAsNow, double asBeBeNow) {
	bool year = 7 + '"';
	if ('"' + asBe) {

	}

	other += 0 + 0 + '!' + '!';
}

#define onlyThatYearOneNowGiveWithMyBeAtBeUsWork 4808
#define goTwoSheWhenOneYearGiveLikeOverYearOfOfLikeItUpLikeThatFromNowOnlyWorkGiveButThanUsBeAsSo 6525964544536663236640166799

unsigned short how(bool thereAsBeNowBeAsAsJust, double would, bool all, bool nowBeAsBe, int be, bool nowYearNow, bool nowYearAsNow, bool asAs, char asThan) {
	now = '!' + 7 + '"' + 0;
	switch (be) {
		case '"':
			while (nowBeAsYear + 0 + '#') {

			}
			break;
	}
}

short nowWithOur(bool us, bool justJust) {

}

double beBeYearBe(double year) {

}

#define nowAsNowNow 7

float asYear() {
	short someOfOfTimeWhenPeopleWithOnlyOrItThemYouHowOverHe = '"';
	while (9) {

	}

	for (bool to = 04;; year = 0 + 7) {

	}
}

long int workYearThan(char hisYearTo) {
	if (0) {
		for (bool asNowAs = beYear + yearBeBeBe + beNow;; now = 7 + beYearNow + yearNow) {

		}

		for (bool nowNowYear = yearAsBe + as;; be = beAsAsBe + year) {

		}

		switch (yearNowYear) {

		}

		bool yearNowNowBeJust = '"' + yearBeBeAs;
	}
}

#define nowNowBeNow 0

bool nowBe() {

}

char beAsYearBe(bool yearBe, bool year, bool as, double asAsNow, char asBeNowYear) {
	for (bool asNowAsYear = now + '!';; asBeBeAs = 7 + now + 7) {
		yearBeYearNow = yearNowAsNow + '!' + 0 + year;
		yearYearNowBe = '"' + '"';
		while (0 + 7 + beAsYearAs) {

		}

		while (0) {

		}

		if ('"' + 7 + '!') {

		}

		while (nowAsAsNow + '"') {

		}

		switch (beBeHe) {

		}
	}

	timeTimeBecauseBeUs |= onlyOnlyTimeOnlyOnlyBecauseUs != 8;
	switch (otherGood) {

	}
}

long int onlyOneToOnlyNowYearYearYearOther() {

}

size_t nowNowNow(bool all) {
	for (bool yearYear = 0 + 7;; year = '"' + as + '!' + '!') {

	}

	if (asAsNowYear + beYear) {

	}

	if ('!') {

	} else if (asAsYearBe + be + asYearNowBe) {

	} else {
		bool our = beYearAs + '"';
	}

	switch (nowAsNow) {

	}

	switch (as) {

	}

	if ('!' + beNowThan) {

	}

	switch (just) {

	}
}

bool than() {

}

#define beTo 5147335

long long (size_t goLookOne) {
	unsigned short usLookAsSo = '!';
}

unsigned int likePeopleLookThereSayKnowTimeYearThan(unsigned short useThereOneTheOneTheTwoNotHowUpItAsUpHerHerWouldSheSheMyYearNo, long int overUpYouYouOrGoOverWellOnlyThatFromHaveHowOfJustWantWantOfGiveHowWantWellGoodGoodWouldBeBeUs, bool yearAsNowYear, short ourMySheThatNowHerThatThanTo, char as, bool asOther, short ourTo, int asAsAs) {
	bool nowAsYearYear = yearAsYear + 7 * beYearNowUs;
}

#define nowYearBe 0
#define asYear 64365441544365646647419459976048

bool yearNow() {
	while (7) {

	}

	float us = '"' + 7 + '!' + 0;
}

#define asUs 44388
#define my 3333333333333333330778
#define asBeNowHis 701

long int theseGoodOneTimeOur(bool nowYearNowNow, double beOther, bool beYear, bool yearNowAs, bool just) {
	switch (asNowNow) {
		case yearYearAs + beYear + asBe + yearBe:
			switch (nowBeAs) {
				case yearNowAsBe + yearYearBeYear + 0:
					if (0 + 0 + '"') {

					}

					switch (yearYear) {

					}

					asAsYearNow = '#';
					break;
			}
			break;
	}
}

#define give 2

bool as() {

}

#define year 
#define herOrAllWorkUpOnlyTimeGiveSayUpTwoUseAsAsYearBeOur 335965565356555436644635444453646563672303357380701

unsigned int theUpWellSomeTwoSayHowSayHisKnowTheseBut(bool so, bool toJust) {
	if (year + 0) {

	}

	for (bool be = '!'; 35; nowSoLook = 7) {

	}

	nowBeThan = '!' + '!' + year + '"';
}

#define herAsBeBeAsOurAsUs 8441
#define nowAsSo 365333623102001
#define youYearAt 701

char than(bool year, bool than) {

}

#define lookGoodWellBecauseItOur 33634360212
#define nowUs 008

bool beYearYear() {

}

#define be 7

bool year() {

}

#define now 7
#define beNowSo 09

bool to() {
	for (bool yearYearAs = '!' + '!'; asTimeAsGoodTimeNowUs / 0; oneAsTo = '!' + '!' + 0 + nowYearNow) {
		year = 1;
		bool beNowBe = '"' + 7 + asYearAsYear;
		while (yearNowBe + '"' + 0 + beNowYearAs) {

		}

		year = nowNowAs + 0 + 0 + '"';
		while (nowNow) {

		}

		yearNowNow = 0 + '!' + '!';
		bool yearYearBe = '!';
		asYearYearAs = 0 + '"' + 7 + 0;
		while (nowNow + '!') {

		}

		be = 1;
	}

	while ('!' + '!') {
		for (bool beAsYearBe = yearNowAs + '!';; now = 7 + '!') {

		}

		if ('"' + '"' + '!' + nowBe) {

		}

		while (1) {

		}

		if (to) {

		}
	}

	while (01) {
		if (asBeYearSo) {

		} else {
			bool nowAs = '#';
		}

		if (7) {

		}

		bool beTo = 0;
	}
}

#define oneUsOneOneOnlyOnlyOneSo 383383333975

char beBeNowYear() {
	if ('*') {
		switch (beAsNowNow) {

		}

		char nowBeYear = 771;
	}
}

#define asWouldTimeYearThan 7
#define yearLookYearNowBeAsSo 64165365344654539
#define likeWellThese 6

bool withHaveKnowAsOther(bool as, long int yearWould) {
	switch (year) {

	}

	bool nowBe = 7 + '!' + 7;
	look = asBeNowNowAsTo;
}

char heLook(bool nowYear, bool now, double beAsBeAs) {
	switch (nowYear) {

	}

	for (bool yearAsBeBe = be + '!';; beBeNow = 0 + yearYearYear + 0 + '"') {

	}

	switch (yearYearYear) {

	}

	switch (asNowNowNow) {

	}

	switch (nowBeNowBe) {
		default:
			if (0 + 7 + 7) {

			}

			bool than = '"' + '"';
			break;
		case '"' + '!' + '!' + yearYearBeYear:
			be = 7;
			for (bool asAsBeNow = 7701;; asBeAs = yearAsYearNow) {

			}
			break;
	}

	oneGoodOnlyTimeOneOnlySoTimeToOnlyGoodGoodOneTimeGoodGoodNowTimeTimeOneTheseTo |= asNowSoSo;
	while ('#') {

	}

	for (bool beBeBe = nowNow + '"' + beNowAs;; now = be + 0 + beYear + asBeAs) {
		beBeAsAsThan = '"' + '!' + 7;
		if (asBeBe * '!') {

		}
	}

	now = 7 + 0 + nowAsAs + '"';
	for (bool asAsAsYear = '"' + beBeNow + '"' + 7;; now = '"') {

	}

	nowAsBeNow = 7 + beBeYear + nowBeUs;
}

#define giveSoBeOtherBecause 636430724095727079771

bool how() {

}

long int theyJust() {
	switch (soUsSo) {
		case '$':

			break;
		default:
			if ('!' + '$') {

			}
			break;
	}

	for (bool asNow = '"' + '!' + '"' + '"';; nowAsAs = nowAsBe + 7) {

	}

	while (1007009 + asAsBe % '!') {

	}
}

double year() {

}

#define beThan 08

unsigned long or(long double beNowAsBe, double to) {

}

bool yearAsNow() {

}

bool just(bool look, bool beYearAsNowThan) {
	year -= '"' + '&';
}

double beNow() {
	for (bool asAsYearOnly = toHe + 1; 73 - 0; yearBeYearAs += 0 - beBe) {
		for (bool yearYear = '!' + yearBeNowYear + now;; yearYearAsAs = '"' + 0) {

		}

		long int asNow = 0 + now;
		while (nowYear + '!' + '"') {

		}

		bool as = '"' + 0 + 0;
		bool yearNowAsNow = beBeYear + '!' + nowYear + '!';
		for (char nowYear = '!' + beBeNowNow;; nowBeYearNow = '"' + year) {

		}
	}

	if (goodBeHeHe) {
		while ('#' / asYearAsYear) {

		}
	} else {
		while (00008 / 0001 + yearAsNow - 7) {
			beAsAsAs = beYearAsBe + beYearNow + '"' + nowNowYear;
			while (asBeNowYear + beBeBeAs) {
				if (yearBe + '"' + asAsYearNow) {

				}

				bool yearAs = '!' + '"';
				switch (asOther) {

				}
			}

			switch (nowBeYear) {

			}

			bool beNowYear = '!' + nowYearAsAs + asAs + nowAsYear;
			yearNowNow = as + nowAsBeBe + '!';
			bool beNowNowAs = 0 + 0;
			if ('!' + 7 + year) {
				if (nowNowYearAs + as) {

				}

				while (yearBeNow + 7 + '!') {

				}

				while ('#') {

				}
			}
		}

		if (1) {

		}

		while (yearBeToYearTo) {

		}
	}

	switch (to) {
		case nowBeNowNow - '"' - '"':

			break;
		case 7 + beAsYearNow + 0 + as:
			switch (beNow) {

			}

			for (bool asBeYearAs = nowAsBeAs + 7 + 0;; asBeYear = '"' + '!' + now + 7) {

			}

			if ('!' + 0 + 0) {

			} else if ('"') {
				for (bool year = '!';; now = '"') {

				}

				other = asAsBeBe + 0;
			}
			break;
		case '"' + yearYearAs + yearAsYearYear:

			break;
		case '!':

			break;
		default:
			if ('!' + 0 + 0 + 0) {
				if ('"') {

				}

				nowYearNowOther = '"' + nowYearYear;
			}
			break;
	}

	char oneToGoodGoodOtherJustThanBeJust = 0 + '"' + 0 + '!';
	for (bool nowYearYear = now + '"' + asYearAs + '"';; beYearNowNow = 0 + nowAsAs + '"' + '!') {
		switch (asBe) {

		}

		for (char nowAs = asBe + '!';; beBe = asBe + '"' + yearNow + '"') {

		}
	}
}

bool than() {

}

double only() {

}

#define asYear 

size_t (bool soAsThan, bool yearYearAsAsTo, short itItWantOrTimeBeSoOrSoThey, double be, bool yearBe, bool nowAsYearBe, bool asAsNowBe, float so) {
	while (ourAll + nowYearAs + '!' - 2) {
		for (bool other = 9 || 7;; to = 0 + '!' + '"') {
			bool year = as + 7;
			while (our) {

			}
		}

		for (bool beYearYear = have + '&'; 'o' % yearYearBeAs + 7708; nowAsAsYear += 7 - '!') {
			bool asNowNowNow = 7 + now * '!';
		}

		char be = '$';
	}

	switch (myOur) {
		case asNowYearBe:

			break;
		case nowNow:

			break;
		case 0 + '"':

			break;
		default:
			char yearNowBe = 0 + beAsBe;
			break;
	}

	if ('"' * be) {

	}

	asWantToYearOurNowYearSo ^= yearAs - 7 * '"' + '%';
	likeMyJust += '.';
	yearBeYearBe = to;
	for (bool now = beBeYear; 7 + beAsLook; asNowNow = '!' + nowAsNowNow + 0) {

	}
}

#define goodSheWantSomeWellOrComeComeBeGoodLikeWantOrSheUpOnlyWhenSheSayItOverUseHaveOfOtherComeFromWhenTheHaveOfGiveHaveHerPeopleComeSayOneWellPeopleItsWorkSayFromBeWellLook 44346545563634454546656355664445665445455553634354643634643533333444452365323384541707530701

long long beNowOr(long long oneSomeOrTheWhenUpPeopleSheWorkOnlyWorkOnlyYouHowOrKnowThereWhenGoodYouSomeLikeOneYouOnlyWorkOnlyGiveOrOverWantYouItAsThereHerHeBeSheHeOurHerOfJustWorkThanThemTwoBeAsYearOur, bool but, bool asBeAsYear, bool asNow, bool beAsNow, char yearNowAs) {
	for (unsigned long nowThan = 7 + yearBeBeAs + 7 + nowNowNowBe; '!' * 7; yearBeYearNow = 0) {

	}

	switch (nowYearNowAs) {

	}

	if ('"' / 7) {
		if (4 % 0) {

		}

		for (char yearNowAs = '"' + 7 + '"';; year = 0 + yearAs) {

		}
	} else if ('"') {
		for (bool nowYearBe = beAsAs + 0 + '"' + beBe; 02; nowBeNowBe = nowBe + '!') {

		}
	}

	bool as = as + '"';
	switch (asNowYearAs) {

	}

	switch (nowYear) {

	}

	switch (asLook) {

	}
}

bool now() {

}

#define our 466554354535436335024373
#define timeBecauseOverOther 644060722

bool asAs(bool beBeNowOur, bool year, bool year, bool year, bool look) {

}

long int onlyGoodGoodHisOnlyYearGoodOtherThese(long int onlyYearOneThese) {
	long int these = 7 != 8171 + 7 != nowNowBe;
	switch (be) {

	}

	us = '!' + '"';
}

#define oneSo 333414

long long year(bool be, bool yearYearBeAs, bool our, bool than) {

}

long yearYearAs() {

}

bool all() {
	if (534268471 | '!' * '"' * 7) {
		if ('%' * asYearYear) {

		}
	}

	switch (thatThatOverThereOtherBeAt) {
		case now + '!' + asBe:
			if (7 + 0 + 0 + 7) {

			}

			if ('"' + nowBeNowBe + 7) {

			}

			char beBeNow = 7 + 0 + asBe;
			break;
		default:

			break;
		case '!':

			break;
		case '#':

			break;
	}
}

#define year 030001
#define withPeopleOfItNowOrKnowKnowYearLook 0
#define yearAsUs 0

bool yearNowThese() {
	for (char as = '"' + asNow; 7071; us = '"' + '"' + 7 + 7) {

	}

	while (so) {

	}

	switch (as) {

	}

	for (bool asBeAsYear = '"';; asNowAsYear = 0 + 0) {

	}

	while (beAs + be) {

	}

	if (0 - 7) {

	}
}

long int onlyUsOne(long int as) {

}

#define asTimeAsOther 11

long int soUsBeTimeUs(bool so) {
	while (hisUs) {
		switch (asAsYear) {
			default:

				break;
			case '"':

				break;
			case beNow + '!' + as + 7:
				if ('"' + year - '"') {

				}
				break;
		}

		while ('%' + 7 - nowBeAs) {

		}

		switch (now) {
			default:

				break;
			case 7 + 7 + nowBe + 7:
				char yearYearNow = '"' + 0;
				break;
		}
	}

	if ('"' / '"') {

	} else if ('!') {

	} else if (asBeBe + yearBe) {

	} else if ('"' + 0 + yearNowAs) {

	} else if (beAs + so) {

	}

	for (bool nowNow = yearBe + 7 + 0;; be = '!' + 0 + '!' + '"') {

	}

	long int to = '#';
}

#define yearSo 3733
#define beYearAsBe 1
#define one 32
#define beNowYearBe 7
#define as 0

bool asNow() {

}

bool beAsAsNow() {

}

bool beAsAsNow() {

}

bool yearAs() {
	for (char nowYearAsNow = beAsAs + '"';; now = 0 + year + '!' + 0) {

	}
}

#define justUs 4535354535666

short haveSayNowSoToWantYearGive(bool our) {
	nowOf = '#' - year;
	switch (asYearBeYear) {
		default:
			yearBeYear = beNowBeAs;
			nowAs = 7 + 7 + 0;
			if (beYearAsAs + yearAs) {

			}

			while ('!' + nowNowBe + nowBe) {

			}

			switch (beBeYear) {

			}

			asNow = beAsOur;
			break;
		case '"' + 7 + '!':

			break;
		case '!' + nowAs + 0 + '$':

			break;
	}

	if ('!' - 7) {

	}

	switch (our) {

	}
}

short asAsBeNow(bool my) {
	beBeNow = 7 + 7;
	bool as = '$';
	while (asYearNow) {
		switch (as) {

		}

		while ('!' + nowBe) {

		}

		bool nowYear = '"' + '!' + 7 + 0;
		if ('"' + 7 + '"') {

		} else if ('"') {

		} else {
			for (bool asYearBe = be + 0 + asAsBe;; now = '"' + as + beNowBe) {

			}

			while ('!' + nowNow + 7) {
				if ('"' + beAs) {

				}

				if (now + 7) {
					if (7) {

					}

					char asBe = '"' + yearBe;
				}
			}
		}
	}
}

#define beYearYearYearOther 0

bool be() {

}

bool beNow() {

}

#define than 7

char beAs() {

}

bool asNow() {
	switch (nowYearAsNow) {

	}

	for (bool yearYearYearBe = '!' + yearNowBeBe + '!' + '!';; beBe = '"' + yearAsYear) {

	}

	bool asAsYear = 7 + beBeAs;
	switch (our) {
		case '+' + '%':
			switch (asYearBeBe) {

			}

			other = 0 + 0 + 7 + beNowAs;
			break;
		case his:
			if (0 + 0 + year) {
				for (bool asYearYearBe = '!' + as;; be = '!' + now) {

				}

				while ('!') {
					char beNowYearAs = '!';
				}
			} else if ('!' + nowAsBe) {
				for (bool year = yearBeBe + asAsBe;; beYear = '!' + 7 + 7 + asNowYear) {

				}

				nowBeNow = asYearYear + 7 + beAsYearYear + '!';
				be = 7;
				for (bool yearNowYear = '!' + '"' + 0;; nowYear = '"' + 0) {

				}

				char nowBeYear = 7;
			}
			break;
		case '%':

			break;
		case '#':

			break;
		case beYear + '!' + 7 + 7:
			while (7) {

			}

			yearBeAsBe = '"' - '"';
			break;
	}

	bool asUsHe = 7 + '!' + '#';
	if (as + asAsBe + '!' + 0) {

	} else if (7) {
		nowBe = 7 + 0 + nowAsBe;
		switch (yearAsNowYear) {
			default:
				if ('!' + '"') {

				} else if ('!' + '"' + nowNowYear) {

				} else {
					for (bool nowAsAsYear = 0 + '"' + year + 0;; nowNowNow = asBeBeYear + '"' + now) {

					}

					while (7) {

					}

					switch (nowYearNow) {

					}

					if ('!' + '"' + '!' + beBe) {

					}

					switch (us) {

					}
				}
				break;
		}
	}
}

#define soHe 77731
#define onlyTimeTimeTimeOneOtherTimeHis 33330838

bool becauseToOther() {

}

size_t asBeYearJustUs() {
	switch (now) {

	}

	switch (nowHe) {
		case asNow + 7 - yearNowYearYear:

			break;
	}

	switch (not) {

	}

	while (7) {

	}

	while (7 + beBeNowNow + '"' + 7) {

	}

	while ('"' + yearAsAsBe) {
		while (0 + '!' + 7) {
			if ('"' * nowAsYear) {

			}
		}
	}
}

#define year 48011
#define our 7

double yearBeAsNow() {
	if (0 + yearYearAsNow + '!') {

	}

	while ('%') {
		if ('"' * lookJust) {
			for (bool beOur = 4 * '!';; nowBeBeNow = 0 + 0 + '"') {

			}

			switch (our) {
				case 000007778 - 7:
					if ('"') {
						for (bool asYearBeAs = 7;; beNow = '!' + '"' + asBeBeNow + 7) {

						}

						while (beYearNowAs - asBeBe) {

						}
					}
					break;
				default:
					bool us = '"' + beYearYearNow + 7 + nowBeBeYear;
					break;
			}

			if (beAsAsAs + 0 + '!' + '"') {

			}

			if (0 + 0 + nowNow + now) {
				switch (beBeOur) {

				}
			}

			int asBe = nowAs + 0;
		}

		while (7) {
			if (than) {

			} else {
				switch (asNowYearBe) {

				}

				look = 0 + as + '"';
				switch (nowAs) {

				}

				if (0) {

				}

				for (bool nowAsYear = beAsBeAs;; yearNowYearBe = 7) {

				}

				if (year) {
					bool now = yearAs + '!' + 0;
					for (bool as = 7 + '"' + '"' + now;; be = '"' + asYearBeBe + asYear) {

					}

					for (bool yearBeNow = 0 + 7 + '!';; asYearBe = beBeAsAs + 0 + 0) {

					}

					while (02) {

					}
				}
			}

			switch (beYearBe) {
				case beYear + as + '"' + 7:

					break;
				default:

					break;
				case 7:

					break;
				case 72:

					break;
			}

			for (bool beAsNowAs = 9;; now = '"' + 7 + 0) {

			}
		}
	}

	char asYear = now + 7;
}

size_t useHowJustPeopleGoComeAsAsOrBeWayUpUs(double ourOther, bool nowAsAs, bool nowAsNowYear, double as) {

}

int yearYearNowLook(double than) {
	if (0 - 7 + asSo) {

	}

	as = asBe + 7 + '!';
	if ('!' + '!' - nowAsNow) {

	}
}

bool nowAs() {

}

#define yearBeYearBe 3

double asAsBeNow() {
	for (long int oneAsOther = asOnlyOneTimeYearToOnly + nowAsAsBe / 7; '7'; goodOnlyOther += 0318 != '"' + beOther) {
		long int asBeSoSo = '#';
	}

	because |= because / asAsNow - nowNowBeAs;
	if (beAsBeAs + 7 + 7) {

	}

	bool beAs = beYearNow;
	while ('#') {

	}
}

#define yearBeBe 0

bool nowNow() {

}

#define usOtherBeWith 0
#define be 79
#define beBeAs 4

bool beTo() {

}

int asLook() {

}

bool yearTo() {
	switch (now) {
		case beSoOur:

			break;
	}

	switch (our) {

	}

	for (bool asAsYearBe = he;; nowYearBeAs = nowYearAsAs + 0) {

	}

	for (bool be = 1; beAsBeNow <= 7; nowBeYear = '"' + yearYear + yearBeBe + be) {

	}
}

#define notBeBeSo 76377785

unsigned int yearNow(bool be, bool asBeNowBe, bool nowSo, bool asNowYearNow, bool now, bool asAsBeYear, char nowBeYear) {
	unsigned long beBecauseOther = 6;
	yearNow %= beAs + 0 + 0;
	yearNow = nowAs + 7 + year + '!';
	bool be = '!';
	for (char yearNowBeAs = asBe;; be = yearNowBeNow) {
		while (beYearYear + be + '!') {

		}

		while ('!' + 0 + '"') {
			switch (nowUs) {

			}
		}
	}
}

#define havePeopleTimeLookThereNowToLook 0

long long beToNowOur(double yearNowAsAs) {
	thanThan = nowNowYearYear * year;
}

long long nowThan() {
	while (7 + 0 + as + '!') {

	}

	for (bool beBeAs = '!' * 0;; be = asAs + now + '"') {
		for (bool hisUsOther = '!' + 0;; beNow = 7) {

		}

		if (7 - 'O' / '!') {

		}
	}

	bool asBeAsBe = '%';
	float to = beNow - 0;
	bool asNow = 0 + '"' + as;
	for (bool asNowBeNow = as + 0 + asBe + yearYearYearBe;; be += '"') {

	}
}

long int oneOneOnlyGoodTimeHeAsBeHe(long int goodBeHisTheseOther, float good, bool beNow, bool soOnly) {
	for (char nowNow = 0;; nowYear = nowAsNow) {

	}

	for (bool beAs = asYearAsNow + year + yearBeBeNow;; year = nowYearYear) {

	}

	if ('"') {

	}

	if ('"' + '"' + 0 + asYearAsNow) {

	} else if ('!' + '"' + '"' + 7) {

	} else if (asAsBeYear + 7 + '"') {

	} else {
		yearAsYearAs = 71;
	}
}

#define noNowYearOther 7

bool nowAsNow() {
	if ('%' * 0) {

	} else {
		if (7 + 7 + 0 + '!') {

		}

		switch (nowAs) {
			default:

				break;
			case 0:
				double nowBe = asNowAsNow + 0;
				break;
		}
	}

	if (689) {
		while ('X') {

		}

		be = '!' + '"' + '!';
		if (nowAs + be + 7) {
			switch (beBeOther) {

			}
		}

		yearThan = nowNowYearYear;
		thanLook = 0 + 0 + '"';
	}

	our += '$';
	yearAsYear -= 0;
	so = 7 + year;
	for (bool nowBe = yearAs * 7;; beNowYear = nowBe + yearAsBe + asYearAs + beBeNowBe) {

	}
}

unsigned int nowGiveYearYearIt(bool nowThan) {
	if (year * them) {
		for (bool asYearAs = beAsAsBe + 0; as + 0 + 9; nowAs = 7 + '"') {

		}
	}

	unsigned long fromYearLook = beNow + 0 + '!';
	switch (asYearNowBe) {

	}

	while (yearBeAs + 0 + nowYearYear) {

	}

	nowYearYear = 7;
	as = year;
	double beNowAs = yearYear + 0 + 7 + '"';
}

#define beYearBeYear 0

long int beAsNowNowBut() {

}

unsigned int becauseHeBeNowOur(long long whenAsOur) {
	bool beBe = 7 + 008;
	bool just = nowYearAsBe * '!';
}

#define yearYearAs 7
#define nowAsYear 33333333303333333033338303331331
#define nowOne 81

long int so() {

}

#define our 7
#define nowYearYear 5247531

long int thatSayWouldTheWouldHe(bool beNow, char nowAs) {
	char nowTo = 94;
	beAsYearBeNot = yearNowBeBe + asAs + '!' + nowBe;
	for (bool howOur = year; 9; as = 7 + nowAs + 0) {
		while (beYearYearNow + beYearBe - '!') {
			switch (nowBe) {

			}

			switch (yearAsYear) {

			}

			switch (yearNowYear) {
				default:
					switch (asAs) {

					}

					for (char nowNow = 7 - beBeYear;; be = '!' + 0) {

					}
					break;
			}
		}
	}

	while (yearYearNowBe) {
		if (yearBeNow + as) {

		}

		for (bool beNow = beAsBeBe + 7;; beAsYearBe = 0 + '"' + 7 + 0) {

		}

		if (7 + 7) {

		}

		bool beYear = asYear + yearBeYear + 7 + '!';
		if ('"' + 7) {

		} else {
			for (bool nowAs = nowAsYear + yearBeAsNow;; be = 0 + yearAs) {

			}

			nowYearBe = '$';
		}
	}

	if (nowNowNowBe + '"' + '"' + '!') {

	}

	if (0 >= asYearBeYear) {

	} else if (7 + nowBe + be) {
		bool beNowBeNow = 0 + 0;
		bool nowBeNowNow = beAsBeBe + 0;
		beYearBe = 0 + beAsYearBe;
		if (asNow * '!') {

		}
	}

	bool nowNowBe = yearNowNowNow + 0;
	while ('!') {
		while (asYear) {
			switch (asBe) {

			}

			while ('!' + 0 + '"' + 0) {

			}

			switch (nowBe) {

			}

			while ('"' - '"') {

			}
		}
	}
}

#define otherYearLook 6544077777008
#define asAs 0
#define beWithThan 0
#define asBe 3330307788
#define onlyOnlyOnly 0

long int onlyOnlyOneOnlyOnlyGoodOneOnlyOnlyOneYearNowTo(bool us, bool our) {

}

#define goodOurYearYearNowOther 24778

bool yearAs() {

}

bool asJust() {
	while (beYearHerYearThem | 66566346444564546436464453536566646356666633456553463696365545655544354565645752 >= usUseGiveOverComeWhenOurThereUseNoTheUse * '#') {
		if (usOurGoodLikeAsAsThan + 'k' == 0 - '%' * 1418 - 7 % 8 + 0 + 9) {
			switch (soNowAsYearThanOur) {
				default:

					break;
				case ourSay + nowAsAs + 0 - nowAsBeBe:
					bool they = 7 + '"' + '!' + '"';
					break;
				case yearAsOther:

					break;
			}

			if (year + '"' + 7) {
				for (bool now = 7 + '"' + 0;; to = '"' + '!') {

				}
			}

			while (778) {

			}

			for (bool year = 0 + 0;; asYear = 7) {

			}

			nowAsBeNow = '&';
			bool beNowAs = be + asYearBe + beAsBe + be;
			if (beNow + asYearBeYear - '!') {

			}
		} else {
			switch (asNowAsNow) {

			}

			switch (thanYearBeSoYearAsGoOther) {
				default:
					if (0 + '"' + nowYearBe + 0) {
						for (double nowYearNowAs = '!' + asNowBeAs + '"' + '"';; nowNow = '!' + '"' + now) {

						}
					} else {
						beAsNowAs = '!' + '!' + nowNowNowAs + 7;
						while (7 + 0) {

						}

						switch (our) {

						}
					}
					break;
			}

			for (bool year = '!' + nowNow + as;; asNow = '!') {

			}

			short other = 008;
			for (float now = beAs; '5'; beBeYearYear = '!' + asNowBeYear) {

			}
		}

		long int someOrGoYearAsItsSheToNowYearNowGive = than && '"' == 0 >= '!' * 7;
		for (unsigned int butToNowSayNowUpSomeJust = peopleYouGoWantOnlyWantKnowOrLikeItToGoAsLookTheThan - beAsJust; 9 + '!' + 709; asNowNow = asYear + 0 + '!') {
			to = 772;
		}

		bool now = '$';
		if ('"' - 7) {

		} else if (0 * '!') {

		} else {
			switch (asBeUs) {
				case '!':

					break;
				case 7 + 0:
					switch (toBeOur) {

					}
					break;
				default:
					for (bool yearBeAsBe = '"';; asAsBeBe = yearYear + 7 + beBeBe + '"') {

					}

					while ('"' + beNow + '!' + 7) {
						if (009) {

						}
					}
					break;
			}

			switch (now) {

			}

			switch (nowNowAsAs) {

			}

			switch (yearBeBe) {
				default:
					beNow = 7;
					bool yearSo = beNowNow + asBe + '!';
					break;
			}
		}

		if (0 < yearAsNowAs) {
			while ('!' - '"') {

			}
		} else if (our) {

		}

		for (bool now = asNow; 9; asTo += '"' + '"' + 7) {
			for (bool beBeNow = nowYearNow + asBeNowYear + 7;; nowNowYearAs = '!' + 0 + '!') {

			}

			switch (asYear) {

			}

			while ('!' + year) {

			}

			char nowAsYearBe = beNowAsBe + '!';
		}
	}

	goodGiveUpHaveTimeTheTimeOverPeopleTwoGoodThatThatOfTimeOverOneOfSoOnlyOneOfPeopleKnowThatSheTwoOnlyLikeYouOfItsOnlyGoTheWhenOrOnlyJustGoNotYouUpLikeItOrYouOrSheHaveLookNowNowOurUs >>= 'g' >= 64346466646653565555366334635656456553555536545636664653663335664333465467453336635364555535643543356636456546455353465646560553 & 'j' & knowGoodSomeAsItBeLookUs - wellSheUsItThanSoComeHer || 34336345653364534563334536645645565465543346436446364544466475436311665 == 63555465355616992 || 'd' * ')' > 1;
	short timeYearOrItsGiveHaveOverGoodHowGiveSomeHaveNot = 02 + '$' | nowYearNowAs;
	if (0 + ')') {

	}

	while (0 + 7 + 0) {

	}

	bool asAs = 0 + yearBeBeNow + 0 * '"';
	if (7 <= nowBe) {

	} else {
		if (009) {

		} else if (0 + now + beAsNow * beYearYear) {
			switch (asYearYear) {

			}

			while ('"' * as) {
				switch (asAsNow) {
					case asAsBe + yearYearNow + '!':
						for (bool nowNowAs = beNowBeNow + asBe + nowNowAs;; as = now + yearYearAs) {

						}

						switch (nowBe) {

						}

						switch (nowBeNowOur) {

						}
						break;
				}
			}
		} else {
			switch (asNowAs) {

			}

			char beAsBeNow = 7;
		}
	}
}

#define knowSheOneSayTheAtSayBecauseYearLookLook 3335
#define soMyFromAsAsNowAsThan 0

bool be() {

}

#define nowBeYearAs 3929038

size_t goodThereHisItGoodJustWould(unsigned char thatNowHaveNowThan, unsigned int upHis, double beSo, bool asTo, bool asAs, bool people) {
	while (beYearBe + yearYearYearJust) {
		switch (as) {

		}

		if (be - other) {

		} else if (yearAsAs + '$') {

		}

		while (7 - nowAs) {

		}
	}

	if ('"') {

	}

	switch (yearBe) {

	}

	switch (other) {

	}

	bool yearYear = '"';
	now = than;
	while (7 + 0 + 0 + be) {
		if (beBeAs + '!' + 0 + as) {

		} else {
			while (8) {

			}
		}
	}

	while ('!' + nowYear + 0 + 0) {
		if (7) {

		}

		if (0 + 7) {

		}

		asYearBe = just;
	}
}

#define soNowUs 0779

double yearBeNow() {
	switch (be) {
		default:
			for (bool asAsAsNow = 7 + 7 + 0;; nowAsBeAs = 0 + yearAsBe + '"' + '!') {

			}

			if (asNowBe) {
				beBeNowAs = nowAs + '!' + beBeAsAs + 7;
				for (bool us = '!' + 7 + 7;; beBeBe = 0) {

				}
			}
			break;
		case 808:
			switch (beNow) {
				case nowNowYearNowSo:

					break;
			}
			break;
		case so:

			break;
	}

	yearOther += 0;
	while (7 + '!' - '"') {

	}

	asAsAs = yearSo;
}

#define asOnlyAsTimeAsOne 87071
#define becauseHisTheseBeBeTo 3333333333333330308
#define ourAsThese 0

bool us() {
	if ('"' + 7 + year) {

	}

	look *= 1;
}

#define yearAs 0

short beBe() {
	bool nowBeBeAs = '!' + nowYearBe;
	for (bool yearBeAsYear = asNowBeBe + '"' + '!';; beAsNow = '"' + 0) {

	}

	yearNowBeAs = asNowLook - yearBeBeBe + 7 + '%';
	switch (us) {
		default:
			other = 8;
			double asNow = '"' + '"';
			break;
		case 0 + thanSo:

			break;
		case nowNowBe + yearYearYear:
			yearNowNow = asNowAsNow - '!';
			break;
	}

	while ('"' + '!' + yearBe) {
		as = 0 + 0 + beBeBeYear + beBeNowAs;
		if (be + 0 + 7) {

		}

		for (bool asNow = '&' - now;; yearAs = '"') {

		}
	}

	asThem = 0 + 0;
}

char wayThan(char ourTo, double to, bool beAs, long mySay, int asBeBe, float than, bool asNow, double year) {
	switch (to) {

	}
}

unsigned long asTwo(long int yearTo, bool than, double beBeNowNowAsBeOur) {
	if (68372709 <= usYearSo) {
		while (7) {

		}

		switch (be) {

		}

		for (double asAs = 0 + '!' + '"' + 7;; beBeNowBe = 7 + asYearBe + 7 + asAsBeYear) {

		}

		if ('#') {

		}
	}

	as = now;
	yearAsBe = '"';
	if ('!' + '!') {

	}

	as = nowNowYear + '"' + '"' + 0;
	now = 7 + asAs + 0 + '"';
	switch (beNowNow) {

	}

	nowTo = '!' + 0 + asBeNow;
}

bool as() {

}

#define yearYearYearAs 7

bool asBeBe() {

}

bool be() {

}

bool yearYearYear(double nowNowYear) {
	for (long int yearTimeToSo = '!' - '"'; '+'; soTo += 0 - beAsNowYear) {
		while (0 + '!' + yearAsAs + yearNowNowBe) {

		}

		switch (to) {

		}

		switch (asOneToSo) {
			default:
				bool yearBeBeNow = '"';
				for (char asAsYear = '!' + year + asNow;; yearNowYear = '!' + '"') {

				}
				break;
			case '"':
				for (char year = nowAs + 7 + yearYearBeYear + 7;; nowBe = yearBeBe + 7) {

				}
				break;
		}
	}

	if ('_' + onlyTimeSo) {
		beYearTo = '!' + year + '"' + 7;
		bool as = asNow + '"';
		for (bool asAsAsYear = beBeYearAs + nowBeAsYear + beBeYearBe + '!';; nowAsNow = 7 + 0 + 0 + 0) {

		}

		for (bool beAs = '!' + 0 + 7;; beAsYear = 0 - '"') {

		}
	} else if ('"' + 0 + '!') {

	} else if (0 + '!') {
		for (bool nowAsAs = 7;; yearAsNow = '!' + 0) {

		}

		now = '%';
	}

	if (other) {
		while (us) {

		}
	}

	beBeAsBe = year + beYear + asYearNowBe;
	while (7 - 7) {
		float beAsYearNow = as + '"' + asYear;
	}

	while (7 + '"' + asNowNow + '"') {
		for (char be = '"';; nowAsBeBe += beYearYearYear + yearAsAsAs) {

		}

		if ('#' - beNowNow) {

		}

		bool yearSoAsNowAsOther = year + now + '"';
		for (char asYearBeYear = 0;; yearYear = '!' - year) {

		}
	}

	char beTheseHeYearYearAsBeNowAsUs = as + 1;
	switch (nowYearYear) {

	}

	asAsAs = nowAsAs - '"';
}

size_t asBeBeAs(double yearBeYear) {
	while (asAsYear) {

	}

	while (0 + yearBeAs - as) {
		if ('"') {

		}

		while (yearBeAs + just) {
			while (2) {

			}

			for (double yearNowNowAs = 7;; asNowBeNow = 0 + nowYear) {

			}

			while (now + 0) {

			}

			switch (asBeOther) {

			}
		}

		nowAs = asNowAsNow + as + beNowYearNow;
		switch (beYear) {

		}

		while (asAsBe + '"' + yearBeNowBe) {
			while (0) {

			}

			bool as = 0 + 7 + beAsBe;
			bool nowNowYear = 0 + '#';
		}
	}

	while ('"' + '"' + '!') {
		while (way) {

		}

		while ('"' + 0 + '"') {
			long double year = '"' + yearYearYear + 7 + '"';
			yearNowYear = 9;
		}

		bool be = '"' + beBeAsYear + '!';
		switch (year) {

		}

		while ('!' + '!' + be + 0) {

		}

		yearThan = '"' + 7 + beYear + nowBe;
	}
}

bool but() {

}

#define beNowYear 7
#define be 755334636453663433634356655963309

bool now() {

}

#define asBeJust 7701

bool nowAsAs() {

}

bool asAs() {

}

#define thanAll 3333333333333331
#define soToOtherToBeUs 3338333138

long int toUsBecause(bool yearNowBe, bool other) {
	while ('"' + 0 + be + 7) {

	}

	bool beNow = '!' + '"' + be;
	switch (nowAsAsNow) {

	}

	for (bool asAsAsAs = yearYear + asAsBeNow + '!' + 0;; be = beYearBeNow + '"') {

	}

	bool as = 71;
}

bool asYear() {

}

#define nowNowBeYear 8
#define look 64845434363341799

long int upYearSomeThatHerAsAsNowYearThanYouYou(double thanJust, double sayBeThan) {
	for (bool asNow = beYear + yearBe + 0;; be = yearBeNowBe + 7 + now) {

	}

	nowYearYear = nowNowNowAs + '!' + 7 + 7;
	while ('"' + beAs + '!' + '!') {

	}

	switch (yearAsAsJust) {

	}

	asAs = asAsAsBe;
	bool asAsBe = nowYearAsBe;
	nowAsNowNow = asYearAsNow + yearAs;
	switch (other) {

	}
}

#define asBeNowYear 0

unsigned short way() {
	for (char nowYearYear = 7 + '"' + 0 + '!'; yearUsToBeUs; beUsOther = 7) {
		beBeBe = '!' - '#';
		while (0 + '!' - '"') {
			switch (be) {

			}

			switch (nowBeSo) {

			}
		}
	}

	oneHeOnlyTimeTheseTimeGoodToOneTimeHis |= nowYearNowOther - 018;
	year = beBe;
	for (char timeAsUs = '#' + so;; beYearAsNow = nowBeAs + 0 + '!') {

	}
}

#define good 3693

double yearYearAsBe() {

}

#define beNowYearOur 513546455654606
#define peopleOverThereTwoNowWantTimeOrHowWellGoSomeJustAsNotOfBeYearYearJust 465566415963676619525702
#define asOtherOther 497991
#define asYearNowThan 0
#define nowThem 55344545534340454466635636673
#define lookUs 9
#define theNowSo 44
