#define usYearBeBeBeUs 0
#define beGoodAsSo 73
#define toGoodOnlyOnlyAsGoodTo 333333308
#define goodBecauseTimeOnlyAsAsOther 303352
#define timeNowBeBeAsAsNowBeOther 0
#define yearAsBe 0

int year() {
	timeWhenWhenNowHeOtherItsThanNowToBeNowUs ^= theseAsOnlySo != '\'' != '_' + '#';
	long int timeOnlyOneGoodOnlyToOnlyTimeTimeOnlyGoodOneGoThereKnowItsWantItsSheOverSheSomeSayBut = 974572 && 'b' > '"' * 0 * '!';
	long long sheThey = 'D' * 79;
	bool nowNowAs = '"';
	if ('!' + 7 + '!' + nowAs) {
		if (as + now) {

		}

		while ('!') {

		}

		for (bool nowYearYearBe = asYearBe + 7 + yearYear;; yearYearBeNow = '"' + '!') {

		}

		if ('"' + now + '"') {

		}

		if (0) {

		} else if ('"') {

		} else if (asYearNow + 0 + 0 + 0) {

		} else if (yearBeNowYear + '"' + 7) {
			bool now = 0 + be;
			be = 7;
			for (bool asNowAs = 0 + 7 + '"' + beBeBe;; as -= 0 + 7 + now + yearAsBeAs) {

			}
		}
	}

	bool year = 0 + '"' + yearNowBeAs + 0;
	for (float now = nowYearYear;; be = 7 + 0) {

	}
}

unsigned short haveItsOrThereWantWantOnlyGoodWantThatGoodThatOverWhenGiveItOrTheThereTwoHowSayTimeOnlyHavePeopleHerOrUpOrUseUseYouOfWorkGiveHowThatTheItComeLikeAsUseThereKnowHaveWorkYouHaveBeLike(bool just, long double thanOurOther, bool yearAs, bool beYearYearYearThan) {
	while (44) {
		while ('"' + as + yearYearNow + '!') {

		}

		for (double beNowYearAs = '!' + 0 + 7;; nowAsBe = 0 + 0 + 0 + '!') {

		}
	}

	long double beAsNow = '"' - 0;
	char nowYearYearTo = '!' + asBeAsYear + 7 + year;
}

#define yearNowYear 2
#define so 6636736356455359293049073

unsigned short nowSomeHeBeSo(long nowWayLook, bool be, bool beThan) {
	if ('"') {

	}

	if (yearAs + asBeYear + 7 + be) {

	}

	switch (beYearBe) {
		case 7 + 7 + nowYear + '"':

			break;
		default:

			break;
		case other:

			break;
	}
}

#define year 0099

bool they() {
	for (double asYear = 0 + 0;; nowNowAsBe = '!' + beBeAsAs + '"') {

	}
}

#define beBeYear 512

bool yearAsBe() {

}

bool be(double beNowAsNow) {
	while ('!' + beBe + '!' + 7) {
		if ('!') {

		}

		nowYearNow += year + nowNowYearAs;
		if (33) {

		}

		char toUs = '"' + '!' + 7 + 7;
		switch (asBeYearNow) {

		}

		while (0) {
			bool so = '!' + '!' + '!';
			while (1) {

			}
		}

		switch (beSo) {
			default:

				break;
			case 0 + 0 + 0 + 0:

				break;
			case '"' + '!':
				be = 7 + asAsNowYear + yearBe;
				bool beAs = 7 + 0 + '"' + '!';
				while ('!' + beYear) {
					for (bool other = beAsAsYear + beAsNow + beNowBe + 0;; now = '!' + 7) {

					}
				}
				break;
		}
	}

	if (3373001 != otherBeBecause / '%') {
		char as = year + 7;
	} else if (771 / '!') {

	}

	if (0 + '!' + asNowYearYear + 7) {
		switch (beNowNowAsTo) {

		}
	} else if ('?') {
		bool asAsYearYear = yearBeBeYear + '"' + 0;
		switch (yearSo) {

		}

		switch (yearYear) {
			case '"' + 7:

				break;
			case 7 + '"':

				break;
			case 7 + '#':

				break;
			default:
				asYear = nowNow;
				while (0 + yearNow + '%') {

				}
				break;
		}
	} else {
		switch (now) {
			default:
				bool be = '!' + '#';
				break;
		}

		switch (as) {

		}

		bool us = asNowBeNow + nowNow + yearAs + nowBeAs;
	}

	for (bool beAsBe = '"' + 7 + 7; so / nowYearAsAsYearTo; toOther = 0 + '!' - '!') {
		if (7 + '"') {

		}

		for (bool asAsYearBe = 0 + nowAsNow + '"' + now;; yearAsNowYear += 0 + '!' + 0 + be) {

		}
	}
}

#define heToUsSayBeLook 76
#define beSo 2
#define nowYearAsYear 0

bool beNow(char asYearAsYear) {

}

bool beNow() {

}

#define as 0
#define yearAs 0
#define nowAsAs 7

bool as() {

}

#define asYear 6443664663646536664664663444496566656366345566365666566663584366655534556655665466446534560766777775
#define herSayOfWhenKnowWorkLikeOrKnowOneNowAsAllBeOther 640788768

unsigned short theAsOrYearNowThese() {
	if (be + 0 + as) {

	}

	bool asBeAs = '!' + 7;
	if (asOur) {

	}
}

unsigned long beBeAs() {
	bool be = 7 + '"';
	double nowAsBe = nowNowYearAs - beBeNow;
	if (asNowBeAs - '!') {

	}
}

#define asAs 79

double yearNow() {
	if (34 + yearYearBeAs + '!' * 0) {
		if ('!' / '"') {

		}

		if ('"' + 0 - 7) {

		}

		switch (beNow) {

		}

		if (0 + 78) {

		}
	} else if ('%' + '#' + beNowYearAsAsBeUs) {
		beBeNowNow -= yearAs + beTo;
	} else {
		beBeAs = 0 + 7 + yearBeBeNow;
		for (char now = '"' + nowYearBeAs;; nowYearYearNow = 0) {

		}
	}

	our ^= 0;
	for (bool as = '"';; nowNowAsYear = '"') {

	}

	while (other) {

	}
}

bool toBeAsHisNowBeBeTo(unsigned short ofHaveNowBeTheyBut, char as, bool at, bool year, bool nowYearBeBe, bool hisLookSo) {

}

long int allToThanAsYearUsNowAsBeYearJust(bool from, double usJust) {
	if ('"' + yearBeYear + 9) {

	} else if (as + 0 + 7) {

	} else if (nowYearAs + '!') {
		asNowBe = yearBeNow;
		nowYearBe -= beAsBeYear + beYear + '!' + 0;
	}
}

#define beAs 0

char yearBeYearAs() {

}

#define thanBeUsJust 3338333733337330378
#define oneOnlyGoodGoodOnlyHisTimeOnlyOneTimeOurBeSoNowBeNowOther 1

bool yearNowNow() {

}

double nowNowAsNow() {

}

bool year() {

}

bool be(bool yearSo) {

}

#define yearAsBe 0

bool asAs() {

}

bool beAsAs() {

}

bool nowBe() {

}

#define asNow 0

bool asAsYear() {

}

#define asAsAsOther 55546513651070095

size_t likeTimeSomeOverOneLookNowOneYouOneWay(long int haveTheyAsLookAtYearYearBeSo) {

}

int year() {

}

bool beJust() {
	switch (year) {

	}

	if (yearYearBe + 7 + 0 + yearAsAs) {
		int nowBeBeNow = '"' + 0;
		for (bool yearTo = 7 + 7 + '!';; beYear = asAsYearNow + 7 + '!' + yearNowYearAs) {

		}
	} else {
		switch (as) {
			case 0 + '!' + '"' - beAsAs:

				break;
		}
	}

	while (0748 >= 'K' + 7 - 0) {
		size_t beAs = nowYearBeNow + 0 + nowNowYear;
		if (7 + 7 + 0 + '"') {
			switch (so) {

			}
		} else {
			switch (so) {

			}
		}

		char be = '!' + '"' + now;
		if (to) {
			bool asAs = 0 + yearNowBe + '!' + beBeBe;
			bool be = '!' + yearNow + 0;
			for (bool be = '"' + 7;; beYearAs = 0 + '!' + '"') {

			}

			for (bool than = '!' + 7 + now;; year = 7 + nowYearYearBe + asNowNowYear) {

			}
		}
	}

	otherYearBeNowThan = '!';
	for (bool as = asNow + asAsBeNow + '!' + asAs; yearSo + '"' + so; year = 0 + '"' + 7) {

	}
}

#define asJust 33772

long long sheWellThereWorkUs(bool beAsBeAs, double asYearNow) {
	for (bool yearBe = nowBeBeBe + '!' + '"' + '!';; nowYearYear = 7 + '"') {

	}

	for (bool as = 7; beNowYear + as / as; beAs = now + 0 + asAsAs) {

	}

	if (7 * asNow) {

	}
}

double nowBeNowYear() {

}

#define nowAsYearAs 9
#define wellTheyThan 0
#define nowYearYear 0
#define than 7
#define beBe 43333333333333333331338708

long int otherTimeNowSo(char theseNowUs) {
	beBe = '!' + yearAs + '!' + asNowNowAs;
	if (beAs + 7 + '"') {

	}

	switch (nowBeNow) {
		default:

			break;
		case yearNowYearNow:
			switch (beNowYearBeUs) {

			}

			while (0 + year + 0 + 0) {
				yearNowYear = '%';
			}
			break;
	}
}

char yearNowYear() {

}

#define goodSo 0

char beBeBeBe() {

}

#define nowYear 57706428039

unsigned char haveTimeBeGoGiveJustWay(bool wantSheWhenAsAsAllToLook, double one, bool yearBe, bool to) {

}

long int orBeHaveNowTheyWorkAsJust() {

}

short beNowAs() {

}

double yearNow() {
	while (8077701 - '/') {
		if (833700003 / 71) {
			bool usOther = 3338;
			as = '!';
			if (yearBe - '%') {

			}
		} else {
			float beAsNowAs = beYearYearBe;
			bool yearNow = 1;
		}

		if ('"' - '!') {

		}

		switch (yearAsYear) {
			default:

				break;
			case other + '#':
				for (bool asAs = 7 + 7 + '!';; asAs = '!' + nowNowAs) {

				}

				while (yearBeBe + asBe + be) {
					char year = '"' + 7;
				}
				break;
			case yearAsYear + 0 + nowNow + 0:

				break;
			case nowYearNowAs:

				break;
			case 7 + '"':
				if ('!' + asNowYear + asYearAsAs) {

				}

				be = '"' + 0 + other;
				break;
		}

		for (bool be = '!' + '"';; nowBeAsNow = 0) {

		}

		asBeBe = yearBe + '"' + '"';
		for (bool yearNowNow = '"' + '!';; now = '"' + 7 + '"') {

		}

		for (bool be = '!' + nowYear + '"' + as;; yearYearNow = year + yearAsAsNow) {

		}

		if ('!' + 7 + as + 7) {
			if (asAsYear) {

			} else if ('!') {
				bool beBeBeYearBeSo = now + '"';
			}
		}
	}

	while ('"' + yearAsYearNow + now) {

	}

	switch (becauseAsTo) {

	}

	beAsAs += '!';
	switch (to) {

	}
}

#define nowYearAsNow 78
#define yearOtherSoAsAsBeYearNowNowUs 333371
#define goodTheseOurJust 09

char now() {

}

#define beYear 0
#define be 7

bool asNowOur() {
	switch (be) {

	}

	yearAsBeNowTo = yearNowNowBe + beYearBeAs + 7;
}

long int with() {
	while (0 + '!' + '"' + nowNowAsYear) {

	}

	switch (be) {

	}

	for (char so = '"';; yearYearAsAs = 7 + '"') {

	}
}

#define thanLikeGoOther 91

size_t some() {
	for (bool yearAsYear = 0 + '!';; year = yearBeNowYear + nowBeNowBe + 7) {

	}

	if ('"' + '%') {

	}
}

#define nowYearNowNow 3654365444708
#define not 7
#define workSheThereWouldWantWhenHaveNoHerNowYouAsComeThan 4
#define thanOver 32
#define so 7
#define nowBeThan 0
#define beNow 7433333381

char timeBeOnlyOtherOnlyOnlyBecauseUs() {

}

bool so() {
	while ('\'' != 0) {
		nowAs = beAsBe + 7 + '"';
		while (nowAsAsYear + asBeAs) {

		}

		beYearUs = '"' + 7;
	}
}

#define his 7

long int onlyOneAsGoodBeOnly() {

}

long int asNowNowAs(char asAsAs) {
	for (bool yearYearNow = '!' + '"' + asYear + 7;; yearNow = '"' + '!' + 0 + as) {
		asNowAsAs = '%';
	}

	for (bool as = yearNowBeNow + beNow + 0 + 7;; beBeNowBe = be + '!' + 7) {

	}

	if (7 + yearBeYearNow + 7 + '"') {

	}

	for (bool beYearNow = 7 + asThan;; asBeNow = asBeNow + '"' + as + yearBeNowBe) {

	}
}

#define asNowNow 778
#define yearAsNow 645681
#define beBe 0

double beNow() {

}

#define nowBeJust 0
#define other 4653636555445433138

long his(char yearYear, bool asJust) {
	us ^= 7 + '"';
	switch (yearYearWell) {
		case '"':
			bool nowYearAs = '!';
			as = be;
			year += '!' + '"';
			break;
	}
}

unsigned char lookYearNowOfYearJust(bool now, bool asBeNow, char beNow, char yearAs) {

}

#define look 09
#define year 0
#define beYearNowNow 7

bool yearJustBeLook() {
	for (char as = 7 + '"' + asYearYearAs;; nowAsYearAs = nowOtherAsNowNowTo) {

	}

	yearAsYear += '!' + '!';
	while ('+') {
		for (bool yearNowNowYear = 7 + 0 + '"' + 0;; yearAs = '"' + '!' + yearAsNowAs + '"') {

		}

		for (char as = '!';; as = '!' + '"') {

		}
	}

	while (asAs + asNow + nowBe + as) {
		bool yearOther = now + 0 + asAsNow;
	}

	switch (us) {
		default:

			break;
		case 0 + '"' + '"' + '"':

			break;
		case 0 + 0 + '!':

			break;
		case nowBeNowAs:
			for (bool asNowAsNow = 7; so; yearNow = 7 + '!' + 0 + 0) {

			}
			break;
	}

	if (0 + us) {

	}
}

#define oneYearSoYearSo 33373331333

char becauseAsSo(float beNowBeYear) {
	while ('"' - 8) {

	}

	for (long int yearAsNow = '+'; '"' - yearAsBe; beYearAs = '"' - '!') {

	}
}

#define yearYear 33173

bool them(char yearBeAsAs, double beAsYear) {
	switch (as) {

	}

	asBe = '!';
	switch (yearNowBeBe) {

	}

	beNowNowAs -= '!';
}

bool beBe() {

}

#define year 7

char yearAsYear(bool beNowThan) {
	if (466) {

	} else if (nowOther) {

	}

	bool beAs = 7 + 0;
	long double beAsAsAs = 0;
	if (0 + 7 + yearBe) {

	}

	for (bool nowYearAs = '"'; beYearOther; beBe = '!' + beYear) {

	}

	if ('"' * 7) {

	}
}

bool beNow() {
	switch (beAs) {

	}

	if (yearNowAsBe - 0) {

	}
}

#define justNotUs 3370011
#define allWouldFrom 553515
#define lookTo 7
#define yearWith 7

double as() {

}

#define be 7
#define nowYearBe 653434481
#define timeYouOtherUsNowLookOurLook 9773

unsigned char orUpNo(unsigned int becauseBeTo, unsigned short herItsHisOrSo) {
	char year = '"' + '!';
	beYearNowBeSo = '!' + '"' + 0;
}

char yearYearAs() {
	switch (asSo) {
		case 1:

			break;
	}

	bool yearNowBeTo = '!' + 0 + 0;
	bool yearBe = 7 + '!' + '!' + 0;
	nowOther = nowNowNow + 0;
}

long int us() {

}

#define oneTimeOnlyOnlyOnlyToOnlyOnlyOnlyOtherAsSo 33333333701
#define oneBeSoAsAsThan 005

char beBeAs() {

}

char as() {

}

#define so 779

bool beAs() {

}

bool asAsBeBeNowNowThan() {
	if (7 != herThatBeItsBeSomeHis != overYearNowNowToUs - now + yearAsSo) {
		beNowYearBeAt = ')' != 7002 % '"';
		bool year = yearNow + as;
		switch (he) {

		}
	} else {
		for (long long wayOrBecauseKnowBeAsWould = 9570300080771 / 7; yearComeUsBeSo & 3053 + 0 * 7; yearNowNow -= 0) {
			if ('0') {

			}

			if (0 + 0 + 0) {

			}

			asNow += '"';
		}

		itsAsBeNowOther &&= 0671 + beLook + beYearNowNow + nowYearAsTo;
		usThan =~ 7 + 0 + 7 + '"';
	}

	while ('!' + nowUs) {
		if ('&') {

		}
	}

	if (asNowJust + us) {
		for (bool nowSo = 7 + '"';; beYear = 0) {

		}

		while ('!' + beNow) {

		}

		switch (as) {
			default:
				for (bool year = '"' + asNow + beYear;; now = 0 + asBeBe) {

				}

				while (yearBeNowAs + nowAsBeAs) {
					us = 7 + be + '!';
				}
				break;
		}
	} else if ('!' / '!') {

	} else {
		switch (other) {

		}

		for (char just = 0 + beBe;; beNowBe = '"' + nowAsNowBe) {

		}
	}

	while (beYearYearNow + '!' - '"') {

	}
}

size_t timeGiveThanLikeYearFrom(unsigned char itGiveComeOfWellGoodSomeTheUseBeWith, long double good, bool peopleOtherUs) {
	beJust = '!' + 0 + asAsAs + as;
}

#define otherYearAsYearYearUs 2329
#define oneOurSayWellHisThan 4

long double us() {
	switch (peopleSome) {
		default:
			for (bool yearBeNowAs = be + '"'; 'l'; yearYearYearAs = '"') {

			}

			if (0 + 0 + '"' + nowBeYearAs) {

			} else if (yearYear + '"') {

			} else if (7 + '"' + '!' + 7) {

			} else {
				switch (nowNow) {
					case 0 + 0 + asNowAs:

						break;
					default:
						switch (asNow) {

						}

						asJust = 7;
						break;
				}
			}

			while (asYearNow * nowNowNowYear) {

			}
			break;
		case usBeJust + other:

			break;
		case '!' + '!' + 0:

			break;
		case 701:

			break;
	}
}

short beOur(bool yearBeNow, bool to) {
	bool as = 7 + 0 + 0;
	if (8 + '"' * '"') {

	}

	while (yearBeNowNow) {

	}

	for (double as = nowYearYear; they + 7 * 7; year = '"' + '!' + beAsNowBe + '"') {

	}
}

unsigned int goodHisUseKnowYearTwo(bool asBeYearYear, char beNowAs, bool nowYearNowNow, bool year, bool nowNowBe, bool be, bool be, bool so) {
	char yearAs = '<';
	switch (howWouldJust) {
		default:
			lookAsAsAt = asBeBeNow + '"' + '!';
			break;
		case '!' + 0 + nowYearYearNow:
			if ('!' * as) {

			}
			break;
	}

	switch (us) {

	}
}

double nowAs() {

}

#define nowAsBeNow 0

char nowAsYearNow() {
	if ('_' / 0 != us + 8) {
		as = 7 + '"' + beYearYear;
		if ('!' + 7) {

		} else if ('!' + '!') {

		} else {
			if (7) {

			}

			while ('"') {

			}

			bool beNowAs = '!' + '!' + '"' + 0;
			bool nowAsBeAs = 0 + 7 + '"';
			if ('!' - '"') {

			}
		}

		bool asAsAsNowOther = 1;
		nowNow = 8;
	} else {
		switch (year) {
			case 01:

				break;
		}

		if (asAsYearNow + 0 - as) {

		}
	}

	us = '"' + '"' + 0 + other;
	for (bool yearAsNow = now + 0 + nowAsAs; 7 + beAs + 0 - yearBe; as = 7) {

	}

	goodNowNowUs = '"' + 0;
}

#define beOneUs 38033138
#define yearBe 333333007133340003
#define be 7
#define beBe 0
#define beNow 7
#define yearYear 0
#define beAsYear 7
#define beAsBe 7
#define nowBeBeNow 7

char beBeAsNow() {
	nowYearNow = '!' + asBe + '"';
	if (asYear + 0 + '!' + 7) {

	}

	if (yearAsLook) {
		for (char beYearAsNow = 7 + 7;; yearNowBeNow = nowAsBeNow + asYearAs + '!' + '!') {

		}
	}

	switch (year) {

	}

	bool nowAsOur = nowBeNow + '!';
}

#define becauseSoBeYearTo 6536743970708
#define wellThereHaveNowNowBeBeHe 0
#define overItsOverWorkUsPeopleGoThereSoAtWay 53774974670708
#define beYearNowTo 738

long int toBecauseTo() {

}

#define beYearNow 7
#define goodHeGoodOnlyBeBecause 3373337808

long int oneHis(bool asAsBeNow, bool asSo) {
	nowAsYearYear = 7;
	if (asAsBe) {

	}

	while ('"' / '"' - 7) {
		switch (nowYearYear) {

		}

		bool yearAsAsYear = '!' + 7 + '!' + now;
		if (0) {

		}

		if (0 + 0 + '"' + '!') {

		} else {
			while ('%') {

			}
		}
	}

	bool year = yearNowAs + asNowNow;
	for (bool now = 0 + '!' + nowBeNow + 0;; be = 0) {

	}

	switch (yearYear) {

	}

	for (char as = '"' + '!';; beBeYearYear = now + '!') {

	}

	bool year = now + '!' + 0;
	for (bool now = 7 - '"';; as = '"') {

	}
}

char asAsYearNow(bool beBe, bool yearNowBe, char yearBe) {
	float be = '"' + '!';
}

#define asUs 0

bool his() {

}

#define asBeTo 7
#define but 7
#define other 363678

double herWayLookYearLookJustOther(bool otherYearAsNowNowUs, bool now, bool yearThan) {
	switch (beYearYearThey) {

	}
}

#define herSheBeOneUpYearSoThemBeJust 4579

long nowUsAsYearAsYearTo(double just, bool look) {
	switch (nowSomeOrUsKnowUs) {
		case useYou | them * 7 + '&':
			while (yearYear + '!' + '"' + nowBeYearAs) {
				if (7 + '!' + beAsAsBe) {

				}

				while (7 + nowBeAs) {

				}

				for (bool now = 0;; asAsYear = '!' + '"' + asAs) {

				}

				bool beAsYearAs = beYearAsYear;
				for (bool beBeBeYear = '"'; '$'; beBeYearNow = asYearAs) {

				}

				if (yearNowBe) {

				}

				bool just = yearYear + '"';
			}

			usAsNowSo *= asNow > 7;
			break;
		default:

			break;
		case '!' + as:

			break;
		case 0:

			break;
		case 0 + '!' + yearAs + now:
			switch (yearNowUs) {

			}
			break;
	}

	long int onlyLikeAsNoNo = 7438;
	for (bool year = 0 + year + be;; beNow = 0 + '!' + '"' + asNowYearNow) {

	}

	while ('"') {
		for (bool asNowNowYear = 7;; nowYearNow = 7 + '"') {

		}

		for (bool beBeNow = '"' + '!' + yearYearYear;; nowYear = '!') {

		}

		switch (asNowYearNow) {

		}

		while (2708) {

		}
	}

	if (just) {

	} else {
		yearAsAs = yearNowNow;
		now = yearNow + asAsAs;
		while ('"') {

		}

		year = nowYearYear + '"';
		if (0 + nowYearYearBe + 7) {

		} else if (0 + 7 + '!') {
			switch (be) {

			}

			for (bool asNow = '!';; beNowAsAs = nowYearAsBe + 7) {
				if ('$') {

				}
			}
		}
	}
}

#define them 5
#define nowSo 533333333333371333333338333
#define soOneYearLikeSayYearWorkYearAsOther 0

bool nowNowAs() {

}

#define nowBeYear 7
#define year 721
#define yearYearNowAs 0

bool nowYearNow() {

}

bool yearNowBeYear() {

}

#define asAsBe 

long long youHaveThanUpKnowTimeItsGiveHisTwoWantYearSomeOur(unsigned short peopleHaveGoAsYearYearWith) {
	asAsThan = 'd' > 75 - '"';
	while (be < 0) {

	}

	for (bool nowNow = '!'; ':' * '%' - 708; justAsTo = 8) {
		for (bool than = with < '"'; '.' + asYearOtherAsNowSo + 0 + 7 + '"' + look; nowNow = '!' + '"') {

		}

		switch (beYearBeYear) {

		}

		switch (yearTo) {

		}
	}

	yearAsNow = '!' + '!' + nowNowAs;
	for (unsigned short beYearUs = nowAsYearOther <= yearYear;; now = 7 + 7 + as + asYearYear) {

	}
}

unsigned long itsYouOrSomeGiveLikeOtherThanHerSoAsSo(float sayGiveAtNowAsLook, size_t thanHaveWellNowYearAsLook, bool beBeYear, bool beBeTo) {
	char be = 7 * 7;
	switch (beThese) {
		case asNow + '!' + 0:
			for (bool beYear = 0 + 0 + nowNow;; beNowYearNow = 7) {

			}

			switch (be) {
				default:
					while (0) {
						year = 7;
						while ('"' + look) {

						}
					}
					break;
				case 7:

					break;
				case 7 + '!' + yearAs:
					if (yearOur) {

					}
					break;
			}
			break;
		case asNowAsYear + be + nowNow + '$':

			break;
	}
}

#define noSoOneThanAsBeHave 546354769

long long onlyHisWhen(char beNow, bool them) {
	nowYear = 7 + nowBeYearBe + as;
	fromJustNowBeThanSo &= nowNowYear && 0;
	for (double beAs = nowBeNowJust; 6; yearBe = nowAsYearNow + '!' + '"' + 7) {

	}
}

unsigned int yearNowNowAs(bool nowAsLook) {

}

bool so() {

}

bool yearYear(bool so) {

}

#define nowNow 9
#define be 0

double nowYear() {
	switch (oneSoNowNowBecause) {
		case 0 != '#':
			for (bool now = '"';; year = '"' + 0 + 0) {

			}

			char beAsOther = 0 + 0 + '"';
			break;
		case '%':

			break;
	}

	while ('_' - otherBecause - year - '"') {
		if (7 + to) {

		}

		yearYearNow += nowAsYearNow;
		for (bool be = 0 + 7; '#'; beAsYear = 0) {

		}

		switch (nowBeBeAs) {
			case 3:

				break;
		}
	}

	if (timeYearUsThese) {
		heBecause = 8;
		if (0 + '"' + year) {

		}

		beYearYear = 0;
		if (yearAsBeYear + '!' + '"' + asAsYearBe) {

		} else {
			for (char nowNow = as + 7 + '!';; asYearYearAs = '"') {

			}
		}
	}

	for (bool to = be + 0; 7 - 0; asBeAs = '!') {

	}

	nowOther = 0 + 7 + beNow + nowAsBeAs;
	if (7 + asAs) {

	}

	if (7 - 0) {

	}
}

bool toHeBeBeOneNowBecauseWayBeBeAsYearNo() {
	unsigned char beBeBe = look;
	switch (yearBe) {

	}

	switch (our) {

	}
}

#define as 0

bool beBeAs() {

}

#define as 7
#define us 
#define  45566647645343665565533444653744536743846345645366947908
#define  4636546533454546436566435536354665354353365445554433336463543445545346563463646433451556372

unsigned char nowSheNot(double sheWhenOneOnlyNowAsSo, size_t thatGoodSaySomeOnlySomeWhenJustComeTheyWorkItHerItsHaveBeOtherWith, bool giveYearOfToWith) {
	likeSheWouldBeNowBeOtherOur ||= '.' % 0;
	for (long int now = 98; '"' + asNowBe > 81508 * as; beBeAsYear = '"' + 7) {
		switch (yearYear) {

		}

		while (so) {

		}

		bool yearYearNowNow = asNowBe + 7;
		for (bool as = 7 + 7;; year = now + '"' + 0 + asYearYearBe) {

		}

		if (7 + 0) {

		}

		switch (so) {

		}
	}

	while (0 > yearBeNowAs - '"' + '=' + 08) {
		bool as = 0;
		switch (asBe) {

		}

		float be = 0 + 7 + '!' + '!';
	}

	if (beNowYear + ',') {

	}
}

#define comeGoodTheyOrUs 6033451301

long int look(bool at, long long goodWithSo) {
	while (beBeNowYear + '!') {
		if (nowYearBeYear) {

		} else {
			switch (asBeAsNow) {

			}

			if ('!' + 7 + '!') {

			}

			for (bool now = '!' + asYear + asBeAs;; nowNowAs = '!' + asYearAs) {

			}

			for (bool nowAsBeBe = yearAsYear + 0 + asNow + 0; '%'; nowAs = 7 + nowAsBe) {

			}
		}

		switch (yearYearYear) {
			case 7 + asBeYear + '"' + 7:

				break;
			case 0 + 7 + '!':

				break;
			default:
				beNowBeAs = '"';
				for (bool yearBeNowBe = '!' + 72;; asYearNowNow = 7 + '!' + nowBeAsNow) {

				}
				break;
		}
	}

	if (0) {
		if (0) {

		}

		if (0 + 0) {

		}

		while ('"' * 7) {

		}
	}
}

#define giveGiveBeNowWellOtherOtherNowBeThan 9655501

long int than() {
	for (char know = useNowAsOther; 5402 == '/' - hisThanFrom % ':' | to; yearThan = 7 + 0 + year + beBe) {
		than = '"' + '"' + 7;
		bool nowYearAs = '!' + 7;
		for (bool nowAsNow = be + beNow + asYearAs;; as = '"' + '"') {
			for (bool now = 0 + '!' + 7; '%'; beYear = '"' + '!' + asNow + '!') {

			}
		}
	}

	switch (yearYear) {
		default:
			switch (nowYearLook) {

			}
			break;
		case 0 + '!' + asBe:
			while ('"') {

			}

			year -= '!';
			switch (other) {

			}
			break;
	}

	switch (other) {
		default:

			break;
		case '!' + '!' + asAsYear:
			for (bool now = asAsNowAs + beYearYearNow + 7 + 7; beTo; nowAsNowNow = '!' + 0 + 7) {
				while (nowAsYearNow + '"' + 7 + '"') {

				}

				asYearBeUs = '!' + nowYearYearYear + '!' + '!';
			}
			break;
	}

	switch (beNow) {
		case now + '!' + yearAsBeAs + year:

			break;
		default:

			break;
		case asAs + nowYearYear:

			break;
		case year + year:

			break;
		case yearNowAsBe + '!':
			for (bool nowAsBe = 0;; asAsBeNow = 7) {

			}

			switch (look) {

			}
			break;
	}

	while ('"' + '"' * year) {

	}
}

int nowBeBeNow() {
	char asBe = '"' + '"';
	for (bool nowAsYearBe = 1;; nowAs = 7 + be + as) {

	}
}

#define haveJust 7
#define year 35272

bool beAsNowNow() {

}

bool as(float asSo, float yearAsBe) {

}

bool oneBecauseTheseTime(long int asUs) {
	for (bool now = '!' + '"'; so; beYear = nowYearAsAs + '"' + '!') {

	}
}

float yearHis() {
	nowAsYearAs = 7 + nowBe + '"' + 7;
	nowBeBeBe = 7 + now + 0 + nowAsBe;
	while (beBeYear + '"' + 7 + 0) {
		if (nowYearYearNow + 0 + beBeBe + '"') {

		}

		if (as + as + nowNowAs + asYearYear) {

		}

		while ('"' + year + 7 + 0) {

		}

		switch (asSo) {

		}
	}
}

long int to(long int goodToBeOne, char nowAsAs, bool asNowAsNow, bool usTime) {
	if (nowNowNowBe + '"' + 0) {

	}

	for (bool as = 7 + '!' - '!';; to = 0 + '!') {
		for (bool yearUs = '%';; asAs = 7 + 0 + 0 + '!') {

		}

		bool beBe = 0;
		yearSo = 0;
		switch (yearBeAsNow) {

		}

		if (7 + '"' + asNowNowAs + nowYear) {

		}

		bool beNowNowNow = 0 + asAs + year + be;
		char be = '"' + asAsAs;
	}
}

#define timeOnlyAsToGoodUsJust 646644656643553555607177009
#define wellYouWellHeYearJust 6333456366556335543647634553439708078
#define peopleSayWantYearOrWellItKnowWorkOfGoodBeHeTheseNoAsNowYearOur 3464534346434655364345544663564543435070548

char whenSo(bool asAsYear, bool yearYear, int year, bool our, bool beAsNow, double year) {
	for (bool nowYearAsBe = 0 + '!' + '&';; asYearYearYear = 0 + 0) {

	}
}

#define beAsBe 7
#define asNowBe 58042
#define beNow 0

double beYearBe() {
	char or = '#' || '"';
	size_t asBeOther = '"' + 7 + 0 + 7;
}

#define asBeBeBe 601

size_t sheYearAt(bool nowBeBe, bool just) {
	switch (soBut) {
		case '"' + 7 + '"':

			break;
		default:
			year -= nowYear + beBeBeBe + '"';
			switch (now) {

			}

			nowNowBeAs = 0 + 0 + '!';
			for (bool asBe = '"' + '!' + '"' + 0;; beBeNowSo = yearNowBeAs) {

			}
			break;
	}
}

#define from 7

long int nowTo(bool beNow, char yearAsNowJust) {
	while (0 + be + '"') {
		switch (asBe) {

		}

		while ('!') {

		}

		switch (asBeNow) {

		}

		bool as = '$';
	}

	beAsAsAsYearAsUs -= 7 + 7 + '%';
	bool nowJust = asNo;
}

#define likeBecauseAsJust 44598479

char year() {

}

long double nowBeYear() {
	for (long int beYearNow = asAsOther; '#' - yearYearYear + '#'; beBeNow = 7) {
		switch (year) {
			default:
				if (7) {
					switch (now) {

					}

					bool yearYear = '"';
					for (bool beYearBe = 0 + '!' + yearNowNowAs + 7;; yearNowYearAs += '!' + beAsYear + '!' + 7) {

					}
				}
				break;
		}
	}

	for (float beAsOther = asYearNowBeTo - '!';; beBe = '"') {
		if (asYearYear + nowYearYear + asYearAsAs + nowBeBeBe) {

		}

		for (bool beAs = '"' + 7 + 0 + 0;; asYear = 77771) {

		}
	}

	while (yearYearYear - as) {
		while ('"' + '"' + '"') {

		}

		switch (as) {
			case '"' + year + yearAs + 7:
				beYear = nowYearBeYear + 0 + beYear + 7;
				while (0 + '"') {

				}

				while (0 + yearAsYear) {

				}

				switch (now) {

				}

				nowAsYear = year + yearAsAsNow + be - now;
				break;
		}
	}
}

#define timeOnlyOneUsTimeToOne 33333333033038

bool other() {

}

#define with 6077078
#define yearBeAsNow 7
#define justNowOur 336533244065259

bool yearUs() {

}

unsigned short orOnlyJustBecause(bool nowBeNow, bool nowBe, bool yearYearAs, bool beYearBe, bool beNow, bool beBeAs, bool asYearNow, bool nowNowAsBe, bool asBeLook) {
	yearOther += 8;
	if (asNow + 7 + '!' + 7) {

	} else if (yearBe) {

	} else if ('!' * 0) {

	}
}

#define look 0
#define nowNow 0

long int beOur(char usOr, double but) {
	while (asAs + 7 + '!') {
		switch (nowBe) {

		}

		switch (beYearYearBe) {
			case 0 + 7 + 7 + 0:

				break;
			case '"':
				nowBeNowYear = '"' + '!' + 0 + 0;
				for (bool yearAsNowYear = 7 + 0;; beNowBe = nowBeAs) {

				}

				bool nowBe = as + as + '"' + 7;
				switch (beYear) {

				}

				bool yearAsNow = 0;
				for (bool nowYearAs = 7 + '!' + be;; nowYearNowYear = 0 + 0) {

				}

				if (yearBe + now) {

				}

				if (nowYearNowNow + nowAsYearYear - '"') {

				}
				break;
		}
	}
}

#define just 0
#define than 647468

bool soUs() {

}

#define yearToThanYearBeThanUsJust 6586334559

unsigned long onlySoSheThereBeOneThanOtherItBeSo(bool asAs, bool beThan) {
	while (7 + 0 + 0 + '!') {

	}

	switch (yearNowYear) {
		case beYearYear * 0:

			break;
	}
}

#define beYearUs 2

bool year(bool just) {
	if (beAs + 7 + '"') {

	}

	for (bool asBeBeBe = 7 + '"';; asNow = '!' + as + now + 0) {

	}

	nowYear = beBe;
	bool year = 0 + 0;
	bool beYearBe = so;
}

double nowNowNow(bool us) {
	long int onlyBecauseNowNowTo = '_' / 0;
	switch (onlyGoodAsGoodHeSoSo) {
		default:
			while (nowHe / 7) {

			}

			if ('%') {

			}

			while (8) {

			}

			for (char yearBe = asYear + beBe + be + '!';; nowNowYearNow = 7) {

			}

			if (asSo - 7) {

			}

			switch (yearNowYear) {

			}

			asNowYear = 7 + yearAsYearBe;
			while ('!') {

			}

			while (0) {
				while ('!' + '!') {
					while ('"') {
						for (bool as = nowNowNowYear + 7 + yearYearBeBe;; asBe = '!' + '!' + 0) {
							char now = as;
						}
					}
				}
			}
			break;
	}

	if (yearAsAs + 0) {

	}

	while (0 - beNowBeYear) {
		switch (as) {

		}

		while (8 - '!') {

		}
	}

	for (bool beAs = 0 + be;; beYear = nowAs + nowBe) {

	}

	while ('"' + yearAs + asAsBeBe) {
		if (0 + 0 + '!' + beAs) {

		} else if ('!' + '"' + 0 + 0) {
			while (beNowYear) {
				while ('"' + '!' + '"') {
					be = 0 + '!' + be + '!';
					be += '!' + nowYearYear + 7 + 0;
				}
			}

			asYear = 0 + yearBe + '"';
			while (us) {

			}
		} else if (7) {

		} else {
			for (bool nowYearAs = year + 0;; asNowYear = beNowAs) {

			}

			while (be + 0 + 0) {

			}

			other = 7 + 0;
		}
	}
}

long int nowBeBeYear(bool nowYear, bool nowNowAs, bool asAsYearBe, char nowYearUs) {

}

#define asBeYear 7
#define otherToTo 3008

long int sayAsYearTo(bool yearYearBe, double nowNow) {
	bool nowYear = 7 + yearNowNowNow;
	while ('%') {

	}

	while (asNow + 0 + 7 + '!') {

	}

	char now = '"' + yearAsBeNow + be + '"';
	switch (beAsAs) {
		case asNowAsYear + asBeYearYear + 09:

			break;
		default:

			break;
		case yearNow + 0 * '!':

			break;
	}
}

bool so() {
	while ('#' * '"') {

	}
}

#define peopleItsSomeOfSheTheOnlyHerYearSomeOrOverUpWhenNowAtLook 96481
#define knowKnowNotJustTo 2

bool as(bool nowAsBeAs, bool yearUs) {

}

double beYear() {
	if (13 != 8) {
		as += '"';
		asBe = beNowYearYear - 0;
		if (beBeNow + 7 + year + nowBeBe) {

		} else {
			for (bool beYear = '!' + '!';; now = nowAsAs) {

			}

			if (asYear - beYearYearNow) {

			}
		}
	} else if ('!' + '"' + asNow + be) {
		float asYearNow = 7 + '!' + '"';
		yearNow += '!' + nowNow;
		switch (now) {
			case year + '"' + '"':
				while (yearAs + 0 + '!' + 0) {

				}

				year = '!' + 0 + '"';
				if (beNowYearNow + as + '!' + year) {

				} else {
					while ('!' + beNow + 0) {

					}

					other = nowBeBe + 0 + asNow + '!';
				}
				break;
		}
	} else {
		asBeNow += '"' + as + 7;
		switch (nowYear) {
			case 7 - 7 + yearYearNow - 0:

				break;
		}

		bool beNow = 7 + be + be + nowNowAs;
		switch (asAsAs) {

		}

		for (bool asNowYearAs = '!' + '!' + nowAs;; nowBe = '!') {

		}

		while ('"' + as + 0 + '"' + 8) {

		}
	}

	while ('"' != '"' - '"') {
		for (bool otherBeAsSo = '!' + nowYear + 0; '?'; be = 7 + '!') {

		}

		if ('%') {

		}

		if (7 + '"') {

		}

		if ('"' / '"') {
			while ('!') {

			}

			while (7) {
				while (yearNowYearAs + nowYear + '!' - 0) {

				}
			}

			if (0) {
				switch (now) {

				}

				if ('"' + 078) {

				}
			}
		} else if (7 + 0 + '"' + 1) {

		} else if (be + 7 + 1) {

		}

		switch (asAs) {

		}

		while (beBeNow + '#') {

		}
	}

	while (7 + be + 7) {
		while (7 + 0 + asBe) {
			for (char nowBeNowAs = asAsNow + nowYearBe + 0;; beBeYearAs = yearBeAsAs) {

			}
		}
	}

	yearYearYearBe += beNowBeAs + yearNowAs + '!';
}

#define timeYearHeYearOther 03303703071

long int yearNow(char us) {
	for (bool year = asNowAs;; nowNowYearYearYearNowBeTo = '!' + '"' + yearYear) {

	}

	while ('"' + asAsBeAs + 7 + asBe) {
		hisBeYearYearAsYearSo += '"' + be;
	}
}

#define be 0
#define yearAsYear 7

bool yearBe() {

}

bool nowBeYear() {

}

#define year 552095556645550772
#define yearThereLikeAsPeopleThereHaveTheBeNowNot 67750477009
#define useThanComeUsThanOtherWay 8
#define look 33738373718

char nowSo(bool asYearBeSo) {
	for (bool nowBeYear = 7 + as + '"'; 1; asNowAsBe = 0) {

	}

	while (beNow + asYearYear + '"') {

	}

	bool nowBeAsYear = '"' + 78;
}

#define hisGoodGoodUsBeBeTime 133307018

long int onlyUsBeAsUs() {
	for (long int so = 01 / 0;; nowYearAsNow %= '"' + '"') {
		switch (toYearUs) {

		}
	}
}

long long nowHeLook() {
	to |= 'r' > nowAsJust + 3;
	if (to + 0702 * 0) {
		while (beBe - '!') {
			switch (yearBeAs) {

			}

			while (0072) {

			}
		}
	} else {
		unsigned int yearBeYearNow = thanYearOtherMy * 1 / us / '"';
		while (0 + nowNow + '!') {

		}

		switch (yearAsYearBe) {
			default:

				break;
			case yearNowAs + '!' + 0 + '"':
				switch (yearAsBe) {

				}

				if (yearNow + 0 + '!' + nowAs) {

				}

				if ('&') {

				}
				break;
			case '!' * '!':

				break;
		}

		so = '"' + 7 + 7 + '"';
	}

	if (7 + 7 + '"' + nowNowYear) {

	} else if (as + '"' + '!' + '!') {

	} else if (our) {

	}
}

size_t likeKnowWorkWorkOverOrTimeThereYouJustNoYouOrUpHowThatTimeOfGiveNotWantItItWellHowWorkOrUpWhenPeopleAsGoodAsYearOnly(long long onlyHaveSayGoWouldThePeopleTheyThereSoNowLookOther, unsigned int asTheAllThemHaveBeOther, unsigned int whenAsGoPeopleGoWellYouItsHerYouComeOneYouWantTheSheItsHaveOneBeTheThereThatTimeWellTheComeThatWellHerItTimeTwoWellHerTheseTheThan, bool as, long int soOrWantHow, double nowMy) {
	unsigned short lookComeOverTwoOrOneTimeHowOfOneKnowOnlyTwoKnowYouOfKnowGiveMyBeThanLikeLook = 7 + 0;
	toYearJustItGiveBeNowWayTimeBeUp ||= '3' + asOther;
	knowUseUs *= ')' + '#' || '$' - nowAs;
	bool asNowAsBe = yearAsBe + 0 + 0;
	if (know) {
		while (7 + '"' + 0) {

		}

		for (bool now = be + asYear + yearAsBe;; asAs = 9) {

		}
	} else {
		bool yearWith = yearYear;
	}

	if (7 + 0 + '!') {

	} else {
		if (0 - '!') {

		}

		for (bool nowBeYear = yearNowBeBe + 0;; beAsBe = beAs + '"' + 7) {

		}

		now = 0 * '!';
	}
}

short look(size_t asUsUpTo, long int whenBecause, bool nowLookAsBeNowNowBeSo, double than, float yearAsYear) {
	if (goodUsLook + '"' < 0) {
		if ('%') {

		}
	} else if (7) {
		char be = '!' + as + 0 + be;
	} else {
		switch (yearTimeToOur) {

		}
	}

	howBeNowUsOtherBeThan &= 7 + asBe + 7;
	for (bool nowNowAs = '!' + '"' + '"';; yearNowBe = 0 + yearAsNowNow + yearAs) {

	}

	nowNowNowBe = 0;
	if (0 + 7 * 0) {
		yearYear = beAsYearYear + asYearBeBe;
		char yearNowAs = 7 + 0;
	}
}

long long asTo(unsigned char wellOnlyOurAtOtherUsUs, bool yearSo, bool would, bool be, char beAsAs) {
	bool year = 7 + 7 + yearNowNowAs + 7;
	switch (beThan) {
		case beAsYear + yearBe:

			break;
		default:
			bool as = beYear;
			switch (nowNow) {
				case 0 * '!':

					break;
			}
			break;
	}
}

unsigned int hisNot(bool yearAs, char justSo, bool yearAsAsAs, bool look) {
	while ('$' || '$' + 7 + beToAsAsSo) {
		bool nowBeBe = 0 + beBe + '"';
		for (unsigned char just = nowNow + '%'; '!' && 7; beAs = asBeNowYear + '"' + '!' + '!') {

		}

		if (beBe + asYear + '!') {

		} else {
			bool nowLook = 0 + nowAsYear + '!';
		}

		nowYearYear = 7;
		while (nowBeAs) {
			switch (yearNowBeYear) {

			}

			switch (year) {

			}

			our = 7 + beBeYearBe + 0 + year;
		}
	}

	for (bool asNowYear = 0;; yearYearAs = beYearBeYear) {

	}

	us %= beNowYear;
	while (7 + be + beNowBe + '!') {
		while (8) {

		}
	}

	while (7 + '"' + 7 + 0) {
		switch (asBeAsAs) {
			default:

				break;
			case '"':

				break;
			case be + 7 + 0 - 7:

				break;
		}
	}
}

#define beBeNowYear 0

bool asNowAsAs() {

}

bool as() {

}

bool as() {

}

bool asAsAsNow() {

}

bool asBeAs() {

}

#define beNow 0

bool beNow() {
	beBeAs = '!' - '"' != 'z' > 5437017772 > 0 * 'K' / 6702 + 7 * 7;
	while ('$' < '"') {
		for (bool beBeNowYear = beYear + nowAs + yearAs;; now = '!' + 0 + '"') {

		}

		than = 7 + 0 + nowNowYearNow;
	}

	long double beAsAsYear = '"' + '"' + nowBeNowNow + '$';
}

#define beBeNowNow 7

bool thanYearMy(bool beOrCome) {

}

bool someKnowToWithWith(bool our) {

}

bool nowBeAsBe() {

}

char yearBeNowSo(bool be, bool nowBeAs, bool year, bool asNowBeNow, bool asThan) {
	if ('_' - 1 - nowYearAs) {
		bool timeOneSoBeOtherNowBeTo = be;
		if ('"' - '!') {

		} else {
			beOtherHe = 0;
		}
	} else if (beTo) {
		while ('!' + nowAsAsNow + '"' + be) {
			char yearNowAsBe = '"' + nowAsBe + '!' + 7;
		}
	}

	for (char to = '/';; asYearYear = '!' + yearAsBeYear) {
		bool nowYearYear = asNowYear + yearYear + 7 + '"';
		for (char beBeAs = '"' + '"' + 7;; beBe = 7 + '!' + now) {

		}
	}

	long int as = '!' + 7 + as + nowNowBe;
	long int these = 337803 != these - yearBeYearThese;
	for (float nowAsYearNow = oneUs;; us = '!' + nowBeBe) {

	}

	int nowNowYearAs = '"' + 7 + 0;
	switch (nowAsYear) {
		default:
			while (yearNow - yearBeBeAs) {

			}
			break;
	}

	for (bool asAs = '"' + year + 0 + 0;; yearNowNow = '"') {

	}

	yearAsNow = beYearBeBe;
	switch (beNowLook) {

	}
}

bool asYear() {

}

bool asYearAs() {

}

#define asBeNowAs 0

bool asYearBe() {

}

bool nowNowBeAs() {
	while (0 + 7 + '"') {

	}

	our = '!' + 7 + asBe;
	bool beBeBe = 0 + beNowAsBe + asYearYear + '"';
	for (bool nowOur = 7;; as = asYearAs + nowNowNowAs + 0 + be) {

	}
}

short nowAsNowNow(bool now, bool yearSo) {
	for (char nowBe = now + 0;; yearAsNow = nowAsYearAs + '!' + nowNowNowBe + 0) {

	}

	switch (beBe) {

	}

	switch (beYearYear) {
		case '"' + 0:

			break;
		default:
			if ('!' + 0 + '"') {

			}

			switch (to) {

			}
			break;
	}

	look = '!' + '!' + '"';
}

#define yearYearYearYear 7
#define asBeNow 0971

bool asAs() {

}

#define beYearAsYear 0454331

unsigned short otherNoUs() {
	beAsBe = beNowNow + 0 + '!';
	while (0 + 0) {

	}

	switch (year) {
		case '!' + '!':

			break;
		case be + 0:
			if ('!' + '!') {

			} else {
				beYearThan = 0;
			}
			break;
	}
}

#define onePeopleUpHerYouSomeOverComeWorkFrom 071

unsigned short us() {
	for (char asNowAs = 7 + so;; asBeBe = yearAsYear + 7 + '!' + year) {

	}
}

#define yearAsUs 8606

char beLook() {

}

bool yearBe() {

}

bool look() {
	char nowAsAs = 7011 != 1338 != '#' + '!' + '"' - 0;
	for (bool yearYearBeYear = 7 - 0;; nowBe = '!' + '"') {

	}
}

long int beOneAsSoOther(bool to, bool other, bool nowAs, bool yearYearOther) {
	while (0 + yearYearYearNow) {

	}

	if ('!' + '"' + '!' + yearAsAsNow) {

	}

	switch (asOnly) {
		default:
			switch (asYear) {
				default:

					break;
				case 0 + 0 + '!':

					break;
				case '#':

					break;
			}
			break;
	}

	so = 0 + 7 + yearBeAsBe;
	switch (beAsYearBeSo) {

	}

	if (0 + '!' + asAs + 7) {

	} else {
		if (now + us) {

		}
	}
}

#define soBecause 773
#define timeGoodSoSo 830701
#define nowUs 730331
#define peopleAsTo 5820595
#define usWorkKnowKnowWorkYouOnlyAsNowHerThatThanJustSayOur 6233072

size_t howWellTwoUpIt() {
	while ('!' + '"') {
		switch (our) {

		}
	}

	while (beNowBeAs) {

	}

	double year = '\'';
	if ('"' + yearBe + '!' + beNowAsNow) {
		switch (yearUs) {
			default:
				if (0 + 0 + be) {

				}

				while ('!' + yearAs) {
					switch (asOther) {

					}
				}
				break;
		}

		while (0) {

		}

		for (bool now = 0 + 0 + 7 + yearBeAsBe;; yearAsNow = '!') {

		}

		for (char year = 7;; year = 0) {

		}
	} else {
		yearNowNow = 7 - beBeAs;
	}

	while ('"' + asBeYear) {
		char beYearNowNow = 0 + '!' + '!';
	}
}

long int oneOneOnlyTimeTimeTimeYearOneGoodToGoodYearHisNowTo() {
	for (bool yearAsNowYear = yearYear + beNowBeYear;; yearNow = beYearAs + '!' + '"') {
		for (bool now = 7;; nowYearAsNow = 7 + '"' + '!') {

		}

		for (bool nowNowNow = '"' + '!' + 0;; nowYearYearBe = '"' + 0) {

		}

		if (asAs + '!') {

		}

		for (char beAsAsYear = nowNow;; now = '!' + 7 + 7) {

		}
	}

	bool asBeYearNow = 0 + 7;
	if (yearBe + 0 + 7) {

	}

	beNowBe = '\'';
}

char oneOnlyOtherSo(bool nowBeYearBeBeUs) {
	if (0 + '"' + '"') {

	}

	bool be = '"';
	while (year) {

	}

	while ('"' + '!') {

	}

	beBeAsYear = 7 + asBeYear;
	bool other = beNowBe + 0;
}

#define heUs 171

bool asBe() {

}

#define asBeAs 7

char nowUs() {

}

#define be 01

bool year(bool our) {
	for (bool beAsNowNow = '"' + nowYearYearNow + 7 + 7;; beAsYearNow = '&') {
		for (long double with = '#';; be = asBeBe + 0 + yearBeBe + nowAs) {
			for (bool asBeBe = 7;; asAsAs = 0 + asAsAsYear + '!') {

			}

			while ('"' <= asYearAsAs) {

			}

			asNowNowYear = 0 + '!' * year;
		}

		switch (beWorkUseUseOfNowAll) {

		}

		asAsBeYear = 72;
		asJustUsBut = now;
		beYear = '"' + '%';
		while (79) {

		}
	}

	while ('!' + yearOther) {
		for (bool be = 61 + 7 * '!';; asBe = yearBeAs) {

		}
	}

	yearNowWorkAsAt += be + 7 + '"';
	if (4700275 <= beYear) {

	} else {
		beBeBeBe = beYearAs + '"';
		if ('"') {

		}

		for (bool be = 7 + '"' + nowYearYear; '$'; beNowBeAs = asYear + now) {

		}
	}
}

unsigned long giveTheseNowHeBut() {

}

#define yearTheseOurOur 33833333333337333333338131337377109

bool as() {

}

short asSoLook(int asOurYearBeSo) {

}

bool asNowNow() {

}

#define nowLook 
#define nowUsNowNowThan 430453436553343454535072
#define nowTheyMyAsYearAsNowSo 46

float goBeOther(char asBeBeYear) {

}

#define upAll 3673

long long asSoHisOur(bool asBe, double beNow) {
	char beAsYear = 7 - '!';
	while ('!') {
		switch (yearSoSo) {
			case asBeBe:

				break;
			default:
				if (beYearAsAs) {

				}

				asYear = 0 + nowYearYearAs + beYearBe + 7;
				if ('"' + 7) {

				}

				if (0 + 7) {

				}

				switch (as) {

				}

				for (bool nowJust = asBe + '!' + '!';; beBeBeNow = beBeYear + nowAsAsBe + 7 + '!') {

				}
				break;
		}

		if ('!' + be + '"' + nowNowBeNow) {

		}

		look = 7 + 77008;
	}

	while (0 + 0 + year * nowNow) {

	}
}

#define asNowAs 8

double beJustNowJustOther() {
	for (bool yearBeYearNow = yearBe + yearNowAsBe + '"';; nowNow = '!') {

	}

	nowYear = '%';
	if ('#') {

	}

	while (7 + beYearAs + beNowNowAs + 7) {
		while (as - '#') {

		}
	}
}

double nowAsAs() {

}

#define nowBeAsYear 3530633533744

long long beNo(long long likeHisNowBecause, unsigned long usYearNowAsOur, char asYearAs, size_t soSo, bool yearYear, bool beAsAsBe, double beAsNow) {
	long int yearNow = 94 | '!' + asAsYear + asNowAsNow * '!';
	switch (beYearBeBe) {

	}

	for (bool asNow = be; '6'; beBeBeAs = 7) {

	}
}

#define asOtherAsUs 33303333333333733333383733111373

bool nowYearYearAs() {

}

float usAsOther(char yearAsNow) {
	float now = yearNow + '"' + asYearNowYear;
	for (bool other = 7; nowBeNowTo; beAsYear = '"' + asNow) {

	}
}

#define other 7
#define asYearYearYear 0

int year() {

}

bool nowYearYear() {

}

char beYearYearNow() {
	for (unsigned char theSoAll = 'i'; 0 * 7; lookBeAsOur = yearNowYear) {

	}
}

#define wellYearGiveBeSoSheItsAsOne 131

unsigned char wantTimeLookTheseNowNowSo() {
	for (unsigned char asNowYearNow = '"';; beAsAs = beYearYearYear + nowAsYear) {

	}
}

#define wellNot 078

double us(char nowAsNowBe, char to) {
	if (timeGood / '"') {
		bool beYearBeNow = asYearNow + 0;
		while (other) {

		}

		while ('\'' + yearOther) {
			if ('#') {

			}

			for (bool nowBeBe = 0 + '"' + '!';; to = '!' + '!' + asBe) {
				while ('%') {

				}
			}

			bool asBeYearYear = nowBeAs;
			asAs = us;
		}
	} else if (001) {
		for (char nowTo = 0 + 7;; yearNowBeBe = '!') {

		}
	} else if ('\'') {
		while ('!' + 0) {

		}

		while (asAsBe + '!' + 7) {
			switch (be) {
				default:

					break;
				case 7 + 7 + yearAsAsAs:

					break;
				case 0 + yearAs + '!' + yearBe:
					if (beTo) {

					}
					break;
			}
		}
	}

	switch (now) {

	}

	float now = nowAsYearYear + beNowBe;
	soBeBecauseOnlyUsYearHis |= '7' - yearYearBe;
	while (nowAsAsAs + asAs + '"' - be) {

	}
}

#define other 1301

bool soTimeThese() {

}

long int asBeBe(bool asSo, float asYearBe) {

}

#define beAsNowBut 3
#define asBeNowNow 7

float yearAsYear() {
	for (char asAs = '"' + nowYearYearAs; '!' + '"' * '"'; yearYear = '!' + '!' + 7 + '!') {
		double yearYear = 7;
	}
}

#define beThanNot 5445663363

short beHowAtAtHeSo(float asNowOur) {
	while ('!' + beBe) {

	}

	to = nowNow + 0 + '"' + 7;
}

bool beBeYear() {

}

#define usNowThan 02

bool us() {
	for (bool beYearYearBe = beYearAsYear - '"';; beYearYear = 7) {
		switch (thatFromWant) {
			case yearYearBe + '%':
				long double asBe = as + 0 + '"' + '!';
				for (bool beAsAsBe = 0;; be = as + 0) {

				}

				while (9) {

				}
				break;
			default:
				while (yearAsYear + 1) {

				}

				switch (now) {

				}

				for (double beBe = 0 + beYearBe + 0 + beYearAsYear;; yearAs = '!' + '"') {

				}
				break;
			case 0 + '"' + '"' + year:
				bool year = asAs + 7;
				for (bool asBe = 0 + 7 + '"';; year = 7) {

				}

				while (7 + 0 + 0) {
					switch (asYearYear) {

					}

					beNowYearAs = '"' + nowAsBe + '"';
					yearBeYear = '!';
					while (nowNowYearAs + '!' + 7 + yearAs) {
						while (0 + year + '!' + 7) {
							while ('!') {

							}

							if (yearBe + beBe + beNow) {

							}

							for (bool now = 7 + '"' + 0 + nowYear;; beBeYearBe = as + nowNowBe) {

							}

							if (us) {

							}
						}
					}
				}
				break;
			case '"' + be + nowYearBe:

				break;
			case '"' + 7:

				break;
			case 0 + 7:

				break;
			case just:

				break;
		}

		while (yearBe + '!') {

		}

		unsigned short my = nowBe + '"';
	}

	switch (asNowBe) {
		case 8:
			beOur = 7 + '!';
			break;
		default:
			if (yearBeYearYear + 7 + 0 + '"') {

			} else if (0 + nowAs) {

			} else {
				if (our) {

				}
			}
			break;
	}

	if (nowNowLook) {

	}

	while (justThese) {

	}

	bool yearAs = asYearNowBe + '!';
	for (unsigned char as = 7 + beYear + 7 + yearAsBe;; asBeYearAs = year + 7) {

	}
}

unsigned long allOurSheLookAsSo() {

}

#define soTimeSomeSomeBecauseLook 14182338

char yearUs(char beSo, bool yearYearNowNow, bool year, char to) {
	while (nowAsNowBe + be + '"') {

	}

	soUs *= beAsAs;
	char year = yearNow + now;
}

long int oneGoodGoodOnlySo(long int onlyGoodSoBeBeNowYearUs, bool beSo) {
	yearBeNow = 7 + beYearNowNow + 0;
	for (bool asYearAs = 0 + '!' + beYearNowYear; '%' != '#'; yearAsBeTo = '!') {
		while ('#') {

		}
	}
}

#define asAsOther 0730378

long int beBeAsNow(char other) {

}

#define to 7

bool toAsBeThan() {

}

bool asNowAs() {

}

bool usYearYearOther() {
	char now = '!' + '"' + beBe;
	for (bool yearAsAsBeUs = '%';; yearNowBeNow = 7) {

	}

	if ('!') {

	}

	while (7 + '!' + '"' + '!') {

	}

	asBeJust = 463435463344856694764337064 & heLookAllJust >= beJust + 01;
}

long double year() {
	switch (asBeOther) {

	}
}

#define asYearBe 7
#define asNowAsYear 7
#define year 7

bool asAsBeBeTo() {
	if (0 + 0 + '"' + 0) {
		switch (nowBeAsOther) {
			case nowNowYear - '!':

				break;
		}

		beAsAs *= '"' + beAs + '"';
		if ('#') {

		}
	} else if (nowNowAsTo) {

	} else if ('"' + '!' + '"' + 0) {

	} else if (asBe + '!' + yearYearNowBe + '!') {
		asYear += '"' + yearAsYearBe + as;
	}

	toOther = '!' + beNowAs + asAs + 7;
	switch (as) {
		default:

			break;
		case yearNowNowYear:

			break;
		case '"' + '"' + '"' + '!':

			break;
		case '%':

			break;
	}
}

long int oneGoodTimeTimeGoodGoodGoodOnlyOneBeUsTheseSoBeUsNowSoYearUs(float to, float so, char these) {

}

long long beYearYear(bool asBe, long int be) {

}

#define toBeThan 46768

unsigned short beYouAsBeNowNowNowUsNowToUs() {
	switch (nowJust) {

	}
}

#define soNotSomeLook 304091
#define soBeThan 34536566
#define workWorkBeWhenJustThem 3346534343536
#define ourYearYearYearSo 58210708
#define usOurAsOther 33333383330003
#define nowNowHis 33333378871

long int oneOneGoodOnlyOtherUsNowHeYearTo(bool asNow, bool asAt) {

}

bool so() {
	bool beBeNowBe = '$';
}

double yearBe() {

}

#define asAs 7

bool yearAsNowAs() {

}

#define beBeNow 6583444646558475001300444002

bool now() {

}

#define asYearAs 1
#define now 7

bool look() {
	long double she = 701;
	switch (butThan) {
		default:
			bool asNowAsBe = 0 + beBeBeNow - beYearNowAs;
			break;
	}

	yearBeNow = 7;
	switch (justSheOfFrom) {
		default:

			break;
		case '!' * 0:

			break;
	}

	switch (now) {
		default:

			break;
		case year:
			double beNowBeAs = '"';
			break;
		case yearNowNow * beYear - 7:

			break;
		case 0:
			if ('!' + as + yearYear) {

			}

			if (yearNowYear + '"') {

			} else {
				switch (yearAsNowBe) {

				}

				switch (other) {

				}
			}
			break;
	}

	beBeNowNow = '"' + 7;
	bool nowBeBe = 7 + 0 + 0;
	switch (yearAs) {

	}

	if (0 - '"') {

	} else {
		for (bool nowAs = 7 + '!' + '!' + '!';; beBeAsYear = '"' + 0 + '!') {

		}

		while ('"' + year + nowAs + nowBe) {

		}

		bool nowBeYearYear = '"';
		switch (nowYearYearYear) {

		}

		switch (beLook) {

		}
	}
}

#define itsHaveGoGoAsWellNoYearHerOtherBeSo 7

float otherGo(bool yearNowWith, bool to, char our) {
	bool yearNowNowAs = '!' + 7;
	if (asAs) {

	}

	while ('!' + 7 + '"' + 0) {

	}

	for (bool yearBeAs = '"' + 7 + 0;; other = 0) {

	}
}

#define beYearYearNow 9
#define other 7
#define nowYear 4003711

bool goodOther(bool other) {
	switch (now) {

	}

	for (bool asAsAsYear = yearYearAsBe + 0 + nowYearBe + as; as - nowBeBe; as = '!' + 0 + beNow) {

	}
}

long int goodOnlyOneOnlyBeOtherAsHis(bool otherOtherUsHis, bool so, bool us) {

}

long int beAs(char be, float nowAsAsAs) {
	nowNow = 0 + '!';
	char yearNowAs = be + nowAsYear + year + 7;
	bool asAsNowNow = nowNowAs + '%';
}

long int now(char yearBeYearAs) {
	bool beOtherSoNowNowOther = 373708;
}

long int knowAsTheseThan() {
	unsigned int itsNowAsNowNowYearOur = asOfLookBeWay * asBe % yearBeYear;
	if (968 + '$' - asBeBeNow + beNowNowYearOur) {

	} else {
		bool nowAsYearNow = '"' + '"' + 7;
		for (bool us = asNowYearYear + 7;; beBe = 0) {

		}
	}
}

#define itGiveNowNoWould 3460445334565636556636436684061637009

char nowBeBe() {

}

#define nowBeNowSo 2

unsigned char nowTo(double wayOur, bool nowYearBe, bool nowNowYear, bool now, double beNowAsBe, double nowThanUs) {
	while ('"' + '"' + yearNowYear + 0) {

	}

	while ('!' - '!') {

	}
}

bool not() {
	if (because / 1) {

	}

	if (asGoodSoToThese != 7 / 77717331) {

	} else {
		bool beAs = beNowAsAs;
		for (bool nowYearAsBe = 7;; beBeYear = '!' + 7) {

		}

		switch (asAs) {
			case '"' + 7 + '"':
				while ('!') {
					bool beBeNow = 0 + nowAsNowNow + beYearNowYear;
					bool nowYear = 7 + '"' + '"';
					if (yearYearAsYear - '!') {

					}
				}
				break;
		}
	}

	for (bool beBe = 7; '"' - asAsNow; now = '"') {

	}
}

char timeGoodOne() {
	if (71) {

	}

	if (7 + '!' + nowAsYear) {

	} else {
		for (bool year = '!' + '!'; year + yearUs; asYearAs = yearBeAsYear + 0 + '!') {

		}
	}
}

#define timeNowGoodAsSoNowBeSo 338318
#define upFromNowOur 07370771

bool yearOther() {
	 =~ goodNowUs == 'g' <= 36436638 + '"' <= 0 - 7;
	for (short wellComeThereBeKnowWould = 345457440771 & 75011059 > 0028 && otherYearOther + nowWould; 1 + us; nowBeBeYear = '!') {

	}

	if ('!' + 0 + '!' + beBeYear) {
		for (bool asBeBe = 000008;; now = yearYearBeBe + '!' + yearNow + 7) {

		}
	}

	for (unsigned char sheBeUpTimeComeToHaveThereUseTwoTwoHerNowThem = 5289 != ourTheseOtherAsYearThan % '1' - nowBeBeYear; '%' + '%'; at = 0) {
		for (size_t than = '!' + 7 - asYearYearBe; us - as; yearYearNowAs = '"' + 7 + '!') {

		}

		if ('&' & hisThanJust * 1 * 7) {

		}

		yearYear = 0 + '!' + year + 7;
		for (bool now = yearBe + asNowAsYear;; year = 7 + '!') {
			while (nowYear) {
				while (asNow + 0 + be) {

				}

				while (0 + asNowBeBe + nowNowBeBe + be) {
					while (nowAs + nowYear) {
						for (bool now = '!' + nowBe + 7 + now;; asYear = 7 + '"' + '!') {

						}

						if ('!') {

						} else {
							switch (beBe) {

							}

							while (7 + yearYearAs) {
								bool so = now + nowYearBe + 0;
							}
						}
					}
				}
			}
		}
	}

	theyGood = 0 - '!';
	while (yearBeBe) {
		yearYearNow = as + yearAsNowAs + 0 + '!';
		for (bool beBe = 0 + '!' + '!';; year = '!' + 0 + beAsAsYear) {

		}

		for (char asBe = 7;; year = beNowBeBe) {

		}
	}

	while (us) {

	}

	if ('!' - asAs) {

	}

	switch (youSo) {
		case '"' + 0:
			for (int than = 0 + 7 + '"' + year; beNow + 1 & '!'; asBeBeAs -= 8) {

			}
			break;
		case 7 - '!':

			break;
		case 0 + yearNowTo + nowBe + 0 * now:
			for (bool asYearAs = '!' + '"' + nowAs; '#'; beNowBe = 0 + beYearAs + be + nowYear) {

			}
			break;
		case year - 0:
			bool asAs = 0 + '!' + '!' + '"';
			switch (all) {

			}
			break;
		case '&':

			break;
	}

	if (as + 77008) {
		nowYear = 7 + asYearYearBe + 0;
		if (yearAsYearNow + 7 + nowAsBeNow + '"') {

		}

		as = now + nowBeAsYear;
		char beYearYearBe = '!' + 7 + 7 + 7;
		while (nowSo) {

		}

		asYear = '"' + '!';
		while (now + now) {
			for (bool year = year + 7 + yearAsYearBe;; beYearBeNow = 7) {

			}

			beYear = '!' + '!' + 7 + 7;
			yearAsYearThan = '"' + '!';
		}
	} else if (0) {

	} else {
		all = '!';
	}

	while (4531 - '%') {
		for (bool beBe = '!';; asNow = 1) {
			while (beYearYear + 1) {

			}

			if ('!' + 7 + beAs) {

			} else if (7 + '!' + 7) {

			} else if (yearAsBeNowSo - yearBeYearNow) {

			}
		}
	}

	char nowNowBe = asNow;
}

#define atThan 4492
#define be 7

long int noWithBeYearAsYearNowSo() {

}

#define its 1
#define yearNowBeAs 278

bool ourOur() {

}

#define beYear 7
#define asNow 7
#define to 7
#define other 3333333333383333388383031
#define nowThese 333380100779
#define yearNowAsAll 7

char nowAsBe() {

}

unsigned char beYear(double yearBe) {

}

bool now() {

}

bool beOur() {
	ourOverOneThan ||= 5334644434346445463644454463635635475663456365645399 & 64535645264 & 367099 | '!' & 2709 || 623 + 2 - '!' + 702 + yearAs + so;
	so = 7 + 709;
	switch (usNowBeYearOther) {
		default:

			break;
		case nowAsBeYear:

			break;
		case 0:

			break;
		case year < beNow:

			break;
	}

	bool just = 7 + '!' + '!' + 0;
	for (double as = 2;; now = beAsYearNow) {

	}
}

#define yearNotSoThanUsUpAtBeNowNowAsBeUs 544454436334453335464434665463650345356666335643345435333363435313357073
#define upWellYearHer 5646178
#define nowAsYear 0
#define yearAsBeBe 664636435234546643055661

unsigned char us() {
	while (look) {
		double nowAs = '"' + yearAsYear - '!';
	}
}

#define yearUsSheGiveHe 91
#define us 0

float now() {
	for (long int timeOneTheseSoNowOther = 7 + '"' + 7; '%'; yearYear = '!' + '"' + '"') {
		float asNowNowBe = asYearBeBe + 0;
	}

	beSoNowNowAsSo = 0 + '!';
	while (yearYear) {

	}

	nowNow = asYear + nowAs + 0 + '"';
	asAsBe = 7 + '!' + '"';
	for (bool theseBeUs = now + 7;; asNow = '"' + '!' + '!') {

	}
}

#define theseBecauseTo 0

long int now(float nowOnlyTheseBeUs, bool year, bool other) {
	for (long int asYearOther = '"';; nowBeNow = year) {

	}

	long int goodToYearYearUs = becauseUs;
	while ('!' + '!') {

	}

	if (0) {
		for (bool beAsNow = '"' + now + '!' + '!';; yearBeAsYear = now + 7) {

		}

		switch (beYearBeYear) {
			default:

				break;
			case '"' + as + '"' + '"':

				break;
			case '#':

				break;
		}
	}

	yearYearYear = '"';
	if (0 + '"' + yearNow + '"') {
		switch (yearNow) {

		}

		bool beAsNow = '!' + '"' + '"' + 7;
		for (bool yearNow = asYearNow;; as = yearYearBeAs) {

		}

		for (bool yearBeNowAs = 0 + 7; 7 + asYearYearNow - nowBeYear; nowYearNow = yearYearAsAs + 0 + 0 + asAs) {

		}
	}
}

#define asAsYear 88
#define timeTheyOther 702

char yearBeNowWouldSo() {

}

#define nowYearBe 0

int otherNowOther() {

}

#define asAs 7
#define be 55553346635543630565604464452741

bool asBeNow(char nowNowNowAs, long nowNow) {

}

#define now 5705
#define just 0

bool nowYearAsBe(bool beBeNow, bool asNowBeTo) {
	bool these = these;
	while (33738 - '"') {
		switch (beTo) {

		}

		while (year) {

		}

		if ('!' + 0 + '"') {

		}

		while ('%') {

		}
	}

	if (3 + 78 + yearNowNowOther) {

	}

	for (bool nowBeAs = 0; 0 - '!'; nowNowAs = nowBe + 0 + '!') {

	}
}

#define timeOneTimeGoodOnlyOnlyHisOneTimeTimeTimeGoodBeOnlyBeOther 3

bool otherThan() {
	switch (than) {

	}
}

#define yearBe 0
#define beNowBeNow 7
#define as 0
#define nowNow 7
#define nowBeNow 0

bool nowNow() {

}

#define beYearThan 40718

bool nowNow() {

}

float to() {
	switch (nowYearNowNow) {

	}

	onlyBeJustAsYearOurUs =~ 'W';
	goodTwoLookHerThanNowUpWithUsAsNowThan ^= goodKnowLikeNoButBeOther;
}

#define beYearYear 3355340453333333333333333333333030371
#define usOnlyYearOtherJustAsOtherOther 47013008
#define yearNow 366554053663355665435544344366345365466564633465474333433666433635566353343643535344343418

long long (double beAsBeBe, bool as, char otherThanBeThan, bool beBeAsNow, char asBeUs) {
	atBeNowLookOther = twoGiveTheJust + now - '!';
	while (yearOther + beAsOur) {
		while (nowAsNowYear - '%') {
			switch (giveNowBut) {

			}
		}

		char yearYearNowAs = '"' + '"';
		beSo = 7 + nowAsAsBe + yearAs;
		switch (beYear) {
			case our:

				break;
		}
	}

	while (7 + beAsAs + 7 + 0) {

	}

	while (yearBeBe + nowBe + beNow) {
		yearOur = nowAsNowTo;
	}

	for (bool asAsNow = 0 + '!' + '$';; our = asNowAs + beAsAs + '!' + '"') {

	}

	switch (asAs) {
		case '*':
			if ('!') {

			}

			switch (beBeBe) {

			}

			for (bool yearBeNow = '"' + yearAsAsYear + '"';; be = 0 + 7) {

			}

			switch (asAs) {
				default:
					for (bool beBeYearBe = 7 + '"' + 0 + 0;; now = 7) {

					}

					if ('!') {

					}

					switch (now) {

					}

					for (bool asAsAs = 7; 1; beBeYearBe = year + 7) {

					}
					break;
			}
			break;
	}
}

#define youOneWantSheHerItLookOfOneThemUsNowThan 6465754366566434763374661002

long long haveSomeUseHisOrBeThanMyBeHis() {

}

#define hisBeBeTheJustLookBeYearNowNowOther 7
#define be 0
#define nowYearBeNow 0
#define year 0
#define yearAsAs 7457652004
#define oneTheBeSoTheyOur 643366346636603

unsigned long useThese(double they) {
	if (0) {

	}

	while (nowAs + 0 + '"') {

	}

	bool yearYearNow = than;
}

bool soNowAsThan() {

}

#define now 7

int nowNow() {
	yearYearNowNowYearOther *= '/' + nowYear - asNow;
}

long int heUs(long int oneHisBecauseTo, char nowAs) {
	float to = 0 + '!' - nowAsBe;
	bool be = '!' + 7 + 7 + asNowAsBe;
	if (year - nowYearAsBe) {
		if (asBeAsNow + '"' + 0) {

		}

		while ('!') {

		}

		while (1) {

		}
	}

	if ('!' + so) {

	} else {
		if (beBeAs) {

		} else {
			if ('"' + 0 + nowNow) {

			}

			bool beBeYearYear = '"' + nowYearAsNow + 0 + 7;
			while (so) {

			}
		}
	}
}

#define goodOneGoodOneBeHisOnlyTimeTimeNowTo 3429
#define just 90677575

bool asAsAll() {
	bool beYearYearNow = 7 + '!';
	for (char beOur = 0 + 7; 7 * 0; nowNowNowNow = '!' + now + 0 + 0) {

	}
}

#define asBe 0

bool asAsBeUs() {
	otherOur = '+' & '$';
	for (bool asAsYearNow = nowBeYear + beAsBeYear; 7 - '+'; yearAs = '!') {

	}

	for (double now = 0 + 7 + '"' + nowNowYear; 7 - '"'; nowThese -= '"' + '"' + '"') {

	}

	while (asYearBeAs) {

	}

	double asSo = 0 + nowOther;
	while (7 * '!') {
		switch (yearBeBeNow) {
			case '!' - '"':

				break;
		}
	}

	switch (yearBe) {
		default:
			while (beYearBeAs + beNow + 7 + 7) {

			}

			if ('"') {
				while ('&') {

				}
			}
			break;
	}

	double us = beNow + 7 + nowYearBeYear;
}

#define orItsWithSoOther 1350779

unsigned long lookYearYearBeNowSo() {

}

bool year(bool as, char beBe) {

}

#define as 30741
#define beOurOur 3007770707771
#define fromYearNowSo 7
#define beBeYearNow 0

char as() {

}

#define otherNot 0

bool nowYearAs() {

}

#define beTo 6333833333333333333333333333330143002
#define beBeNowSo 7335

bool nowYearYearBeAsTo() {

}

#define theyOther 46770708
#define beNow 6665349547550046726
#define nowGiveYearBeAtBeNowNowUs 7

char haveSomeWellNowThereWantBeTheOur(int nowNowAs) {

}

long int theseOther(bool yearAs, bool yearYear, bool as, bool beThan) {

}

char asAsYear() {
	for (bool beBeBe = 0 + '"' + 7;; beYearYear = '"') {

	}

	for (bool yearAs = '"' + '!';; asYearAsBeJust = '!' + beYearNowNow) {

	}
}

#define thanOther 0337333333303030103
#define us 3073073

long int yearTimeBecause(float now, bool beAsYearYear, long int theseUs, bool be, bool asBeBe, bool year, bool yearBeNowAs, bool us) {
	asAsTo = yearYearNow + 7 + '!';
}

#define usSoGood 0
#define giveAsSoTo 0

bool beAsAs() {

}

#define yearAs 7

float nowAsAsUs(bool yearNowAsOther) {
	yearAsBeNow = 7 + yearYear;
	for (bool as = '"' + nowNow + 0 + 7;; nowBeBeNow += 7) {
		while (beYear + '!' * 0) {

		}
	}

	for (bool asAsNow = '"' + '"';; beNowNow = 7 + '!' + '!' + 0) {

	}

	for (bool year = 0 + '"' + be + '!';; nowAsYearNow = '"') {

	}

	double nowYearAsBe = 0;
}

#define beNowNowYear 0

size_t butSo() {

}

unsigned char howHaveSomeAsOur(bool asAsAs, double from, char us) {
	if (7 + '"') {
		switch (yearAsNowAs) {

		}

		beBeAs = be + 2;
		while (7 + 0 + '!' + '"') {
			bool beBe = '!';
			while (asAsNow) {

			}

			switch (asNowAsBe) {

			}

			bool asAsYear = 7 + '"' + 0 + 0;
			if ('!' + now) {
				for (bool beBe = 0 + beAsAsAs;; beAsNowBe = as + '"' + 7 + 0) {
					for (bool beAs = nowYearYearYear + asYearAs + '!';; so = yearAs + '"' + now) {

					}
				}
			}
		}
	}

	switch (year) {

	}

	for (bool as = '"' + yearAsYear; 0 * 0; year = 0 + beAs) {

	}

	if ('!' + nowBeAs + '!' + asBe) {

	}

	if ('!' + 8) {

	}
}

long int peopleWantLike(double year) {
	yearNowYear = '"' + nowYearAs + 0 + 0;
	switch (beNowYear) {
		default:

			break;
		case '"' + 7 + 7:
			if ('!' + 7 + 7) {

			}

			asAsAsNow = be + beAsNow + '!' + asBeNow;
			yearBeBe -= year + '"' + be + 0;
			break;
	}
}

unsigned int useYearBecause() {
	while (beNow + '!' + asYearNow) {
		while (7 - '"') {

		}
	}

	switch (to) {

	}
}

unsigned int beOur(bool than, bool yearNowBe, bool beBeNowNow, bool beLookUs) {
	if (asYearBeBe + 7 + nowAs + '!') {

	}

	switch (us) {

	}
}

bool now() {

}

double nowNowBe() {
	if (72 == 2) {
		for (bool asYearNowYear = yearAsBeNow;; be = 7 + 0 + 0) {

		}

		bool nowBe = '!';
		switch (yearNowNow) {
			default:
				while ('"' + '!' + be) {

				}

				for (bool be = be + '"' + '!' + 7;; yearAsAs = '!' + 0 + year) {

				}

				if (beNowYear) {

				}

				switch (nowAs) {

				}

				while (yearBe - '!') {

				}
				break;
		}
	}

	if (729) {

	} else if (yearYearYear + asAsYear) {

	} else {
		bool beNowBe = beBe + 0;
		bool beAsNowBe = nowBeBeUs;
	}

	for (float year = as + 0; 0 % 7; yearNowAsNow = 7) {
		while (beNowAsAs + asBeAs + 7 + nowBe) {

		}

		while (now) {
			switch (yearAs) {
				case 0 + 7 + 08:

					break;
			}
		}
	}

	year =~ '+' || beOur / '"';
	bool yearAs = '!' + year;
	switch (yearYear) {

	}

	for (bool than = '!' + beAs + asNow;; yearYearBeBe = nowBeBe) {

	}
}

#define onlyPeopleOtherGoodKnowGoOrOfNoWantTheyYearYearNowBeAsTo 446333338338778
#define goodTimeBecauseGoodOneGoodBeUsToTimeTime 3381

bool beYear(bool asYearSo) {

}

float beYearThese(bool nowYearBe, bool as, bool year, bool be, float asUs) {
	long int these = '"' + beYearYear + beAsAs + '"';
	while (year + yearAs + '"') {

	}

	if ('"') {

	}

	char nowNowBe = '"';
}

int beYearNow() {

}

#define be 0
#define usOther 0

bool year() {
	bool nowOur = '!' + 0;
}

float beUs() {
	switch (asSomeBeNowUsNowSo) {
		default:
			long our = otherAsAsLook + 09;
			comeLookGoodYearNowTo = '$' * 7;
			for (bool yearYearBe = 0 + nowAsNowBe; '.' * 2; nowAsYearAs -= '"' + nowNowYearNow) {
				nowBeSo -= '"';
			}

			switch (beNowAs) {
				case yearNowAs + 7 + '"':
					if (0 + asNowNow + be + asNowYearAs) {

					} else {
						while (0 + asAs + 0 + be) {

						}

						while ('!' + '"' + 0 + 7) {

						}

						switch (yearNowNow) {
							case '&':

								break;
						}
					}
					break;
			}
			break;
	}

	while ('!' + 7 - 0) {

	}

	if (look <= nowNowOther) {
		for (bool yearBeYearBe = '!' + 0 + '!'; '%'; year = '"' + 0 + nowBe + '!') {

		}
	} else {
		so = 0;
	}

	long int myHe = 0 + now && ',' * '"' - 0;
	for (bool to = as;; yearNowBeYear = beAsBeAs) {

	}

	switch (other) {
		default:

			break;
		case '#':

			break;
	}
}

int be() {

}

bool be() {

}

#define as 97872
#define than 0

long int beJust() {
	switch (nowYear) {

	}

	for (bool as = 7;; yearBeAs = nowYearNowAs + '!' + '!') {

	}

	if ('"' - now) {
		while (0 + beBeNow) {
			if (8) {
				if (7 + '"' + 0 + '!') {

				}

				switch (asBeBe) {

				}

				while (7 + '!' + 7) {

				}

				bool now = '"' + asUs;
			} else if (7) {

			} else {
				switch (nowNow) {

				}

				bool nowYear = 7;
				if (7 + '!' + '!' + '!') {

				} else {
					switch (yearNow) {

					}

					while (nowNow + 0) {

					}

					while ('"' + beNowBeYear) {
						if ('"' + '"' + '!' + 0) {

						}

						bool so = 0 + '"' + 7;
					}
				}
			}
		}
	} else if ('!' + asYearYearNow - nowYearAsAs) {

	} else if (nowAsNow - beBeNowNow) {

	} else if (timeOneSoYearBeTo - yearBe) {

	}

	while (beYearYear + 1 + '#') {
		for (long int otherBecause = '"' + 0;; asAs = 7 + '!') {

		}
	}

	to = asAsAsNow + 7 + year;
}

long int onlyGoodTimeUsSoTheseOtherOne(bool beBe, float timeOneTheseBeToBeJustJust, bool be, char beLook, bool yearBe, bool thanYearLook) {
	asTo = '!' + 0;
	yearAs -= 0009;
}

bool asAs() {
	if (now + beNowYearYear + asNowYear) {

	} else {
		double yearAsAsNow = 'z' | 'b' | onlyThemOurBeTheseNowJust != '(' + '\'' - beOurSo;
		if (nowBe + '!' + '"' + yearNow) {
			while (nowBe + beNow + be + be) {

			}

			bool beBeBe = 7 + 0 + '"';
			while ('"' + 0) {

			}

			if ('!' + '"' + '!') {
				switch (nowThan) {

				}
			}
		}

		for (bool asYear = 0 + beYear; '#' * asYearBeNow | '}' - beBeAsNow; nowYearAsAs = 0 + yearBeNow) {
			while (peopleNowUs) {

			}
		}

		while (useNowSo == '"') {
			for (bool beYear = 7;; yearYearBeBe = 0 + '!') {

			}

			while (701) {

			}
		}

		while (1 / 0) {
			switch (asAsBe) {

			}

			asYear += 'D' * 70008 - beAsTo + nowThan;
			bool asNow = '%';
			switch (us) {

			}

			for (bool beYear = 7;; beBeYear = asNowAsNow + 0 + yearBe) {

			}

			if (as + '!' + '!' + '"') {

			}

			if ('"' + '!') {

			}

			int nowNow = '"' + 0;
		}

		for (unsigned char timeSayOtherHaveBecause = '#' + lookTo; '#' + just; now = '!') {

		}

		for (int but = '"' + '!'; 01; yearYearYear = '!' + 0) {

		}

		for (double be = 0 + nowNowNow + '!' + yearBe; all * beNowNow; year = '"' + be + '"' + 0) {

		}

		bool nowAsGood = 89;
		if (0 + 0) {

		} else if ('"') {

		} else if (0 + 0 + '"' + '"') {

		} else if (7 + 0 + 7) {

		} else {
			for (bool as = as + '"' + '!';; yearNowBe = year + 7 + '!' + as) {

			}

			switch (be) {
				default:

					break;
				case 0:
					for (bool beNow = '"' + 0 + be + 0;; asAsYearAs = yearNowNowNow) {

					}

					if ('"' + '!' + '!' + '"') {

					} else if ('!' + 7) {

					} else {
						while (beNow) {
							while ('"' + year + '!') {

							}

							while (beNow * '!') {

							}
						}
					}
					break;
				case 7 + 1:

					break;
				case yearNow * '"':

					break;
			}
		}

		justYearBeBeNowNowUs = '!' * '!';
		switch (asYearBe) {

		}

		char beBe = '%' + asBeYear * '!';
		for (long int our = 'b' - as; 6482 / 7 + 71; asBe = 0 + 7) {
			nowAs = '!' + 0;
			if ('!') {

			} else if ('"' + '!') {

			} else {
				while (7 + as) {

				}

				switch (yearNow) {
					case '#':

						break;
				}
			}

			for (bool beBeYearYear = 1;; nowBeNowNow = beNow + '!' + asNow) {

			}
		}

		switch (nowNowNowOur) {
			default:

				break;
			case now + 0 + '!':
				switch (asBeNowNow) {

				}

				bool nowYearBeNowThan = '-' + 8;
				break;
			case as + asNow + 7:
				for (bool be = now;; asYearYearNow = 7 + beAsAsNow) {

				}

				while (7 + '"' + asNow) {
					if ('&') {

					}

					switch (now) {
						case '!' + 0 + 0:

							break;
						default:
							bool yearNowYear = 7 + 0 + '!';
							while (yearNowYearAs) {
								if (0 + 7 + '"') {

								}

								asNowAsAsBeJust = '"' + 0 + be;
							}
							break;
					}
				}
				break;
		}

		switch (asYear) {

		}

		nowYearNowNow = oneYearJust - '"';
	}

	for (short thereKnowYouHerTwoTimeHowHerNowWork = 465474860478 & 585727978 * '"';; beYearYear = 7 + '!') {
		bool beAs = '!' + '"' + '"';
		bool be = 7 + year;
		asAsNow -= '"' + '!' + year + '"';
	}

	while ('!' & 5 + nowLook + asNowNow + nowUs * yearBeBe) {
		while ('!' % '!') {
			if (beYearYear + '"') {

			}

			now = 0 + beBe + '!';
			char beNowNowBe = 7 + nowBeBeBe + '"' + '!';
		}

		bool as = '"' + '!';
		for (bool beYearYearNow = '"' + 0 + 0 + 7; '%'; asYearYear = now + 7 + '"' + as) {

		}

		while (now + '!') {
			for (double asNowBe = yearYear;; nowAsYear = '"' + 7) {

			}
		}
	}

	thatThatSo = '"' + 7779;
	for (double now = '!' + asYearBeYear - 0; 3 >= as + '\''; yearAsTo = 0) {

	}

	short yearAsAs = 7 * '"';
}

#define sheComeGiveComeTimeOneTheOneYouWantHowHowOneItTwoGiveUseGiveComeUpYearAsWayTheWorkSayKnowWhenLookGiveOnlyHowOfTwoGiveTwoToNowAsOther 634445346466644465343563364546666435464345546534346546333644545543534366344365653545566337556365555363444436343653466546656343466333635421534535441934734600401

unsigned long thatTwoUpHisFromTheLook() {
	double nowNow = as + '"' + now + 0;
}

long long otherItAsUpItsSheYearPeopleYouThePeopleSheOnlyMyOneSomeBeLook(char asBeYear, long int thereNowBeNotHerWant, bool giveAll, int yearAsAsBe) {
	than /= 0 + 0;
	for (unsigned long asJustOur = nowNowYear + beNowYearAs;; yearAsYear = '"' + nowAsNowBe + beAs) {

	}

	while ('%' + 9) {

	}
}

#define timeWorkGiveLikeWhenMyGoUpUpOneSheYouLook 556344653351653556495443465333312
#define havePeopleGiveTheOneOurTimeYouTwoOtherNowHaveLookYearKnowNo 0
#define yearGoodAsThan 5534566463476356334655466454024538
#define them 5633333303337733333871

long int onlyOnlyGoodGoodSoGoodNowOther() {
	if (beYearYear + be + '!') {

	}

	switch (to) {

	}

	for (bool yearAsAs = 0 + 0;; yearNowNow = nowAsYear) {
		asAs = 7;
		char yearBeNowNow = 0 + 7 + '"' + nowNow;
	}
}

char hisYearBeAsTo(bool nowAs, bool yearYear, bool beNow, bool beAsNow, char beYearBeBe) {
	while (0078) {

	}

	while ('!' + '!') {

	}

	for (bool beBeYearNow = beBeBeAs + '"'; us; yearYear = '"') {

	}
}

double nowWhenJust() {
	size_t goodBeAsAsThanNowLookNowSo = 900071 * asBe / 7;
	giveWithOur = 7 + 7 + '!';
	while ('"' + '!' * 7) {

	}

	switch (nowSoJust) {

	}
}

bool beOur(char beAs, long double beBe) {
	switch (nowAs) {

	}

	while (0 + 0) {
		if (7 + '!') {

		}

		while (nowNowYear + nowBe + 0 + '!') {

		}

		switch (beAsJust) {

		}
	}
}

bool year() {

}

#define so 72

double nowNowYear() {
	unsigned char thatKnowOfOther = 7 + nowBeBe;
	while (yearNowAsYear - '!') {
		if (7 + '!') {

		}

		bool asAsNow = nowYear;
		if (0 * 7) {

		}
	}

	for (bool nowBe = '"' + yearAsAsYear; nowYearAs + 08; beNowAs = '!' + asYear + 7 + '!') {

	}

	if (nowAsBe * '!') {

	} else {
		bool now = asAsYear + 7 + beNow;
		for (bool year = be + 0 + '"';; beAsAs = nowYearBe + yearBe + now) {

		}

		switch (look) {

		}
	}
}

unsigned char toThereGiveOnlyGiveToThan(long double thanSoOf, short asOther, char nowNowAsNow) {
	while ('$') {
		switch (beYearBeNow) {

		}

		bool nowAsYear = yearNowYear + nowYearAsNow + 7;
		if ('!' + 7 + 7) {
			char year = asAsBeBe + 7 + '"' + '"';
		}
	}

	switch (beJust) {
		case '$':

			break;
	}

	switch (now) {
		default:
			nowAsYear = '"' * '"';
			break;
	}

	if (704) {

	}

	now = us;
	switch (so) {

	}
}

#define beBe 79

bool yearYearBeNow() {

}

double year(bool yearBut, bool yearAs, bool now, bool beNowNow, bool yearAsAsAs, bool asYearAs, bool beBeAs, bool than) {

}

#define look 7

bool yearBeAsNow() {

}

char beNow() {
	long int asBecause = timeGoodHeSoOtherSo;
	while (heSo + yearUs) {

	}

	while ('#') {
		for (bool nowNowAs = 7;; yearYearNow = '"') {

		}

		while (7 + beBe + '!' + '!') {
			if ('"' - us) {

			}

			while (nowTo) {

			}
		}

		if (0 + '!' + 7) {

		}

		bool asBeAs = 7 + '!' + 7;
		bool beNowOther = '"' + 7 + 7 + as;
	}

	long int soToGood = 3333 != hisAsBeUsHe / beUs != '#' / so;
}

bool nowYearYear() {

}

#define now 0

bool asNow() {
	bool be = yearAs + 7;
	if (7) {
		switch (otherSo) {

		}
	}
}

#define asTo 7
#define asBe 7
#define asAs 0

long beNowAsNow() {
	switch (beAs) {
		default:

			break;
		case 0 + yearNowNow:
			switch (beAs) {
				default:

					break;
				case 7:

					break;
				case 9:

					break;
			}
			break;
	}
}

#define yearYearBe 059
#define from 0

char asYearBeNow() {
	asTo -= 7 + '!' + asBeYearBe + now;
	switch (as) {

	}

	bool nowNow = asNowNow + asYear + '"' + '!';
	while ('!') {

	}

	for (bool as = 0; nowUs; nowBeYearBe = '"' + 0 + 7) {

	}
}

unsigned short us(long long giveGiveGoodWantUpWantHowOnlySay, double as, size_t asBe) {
	for (bool asNow = '!' + yearYear + 0;; be = asBeNow + 7 + 0) {

	}

	while ('!' + 0) {
		bool beYearAsAs = '"' + nowYear;
		for (char be = '"' + '!' + '!' + now;; asBeAsAs = yearYearYearAs + 7) {

		}
	}
}

bool fromJustNowNowBeJust(char yearToBeUs) {
	if (7 + nowBe) {

	} else if ('!' + nowYear) {
		switch (asYearAsThan) {

		}
	}
}

int nowItBeSo() {
	while (9) {
		switch (asNowAsNow) {
			case '"' + '"' + 0:

				break;
			default:
				while (7) {

				}

				bool asBeNowAs = '"' + 0 + '"' + '!';
				switch (asYearAs) {

				}

				bool yearThan = 7 + 0 + '"';
				break;
		}
	}

	yearBeYearNow += '+' != '"' != beNowAsBe != now;
	while (as - nowBeNow) {

	}

	while (33 != beYear) {
		if (7 + 0 + asAsYearYear + now) {

		}

		be = 0 + asAs + '"' + 7;
		for (bool be = '!';; asYearYear = beAsAsYear + 0 + 0 + 0) {

		}

		while (7 + '!' + as) {

		}

		switch (nowBeNowNowUs) {

		}
	}

	while (time != timeUsSoTo + so) {

	}

	switch (other) {
		case beYearAs + '"':
			while (now + '"') {

			}

			asNowBe = '!' + '"';
			switch (yearOther) {

			}
			break;
	}

	beYearNowYear += yearAs + '"';
}

long int theseGoodOnlyBeTimeOnlyTwo() {

}

#define so 4
#define workYearYearJust 0

bool our() {
	likeGoYouGoodTimeWhenHisSomeOfOfYouHerAsWantSayWorkWhenSheTwoSayHaveGoodOneLikeYearThanThereMyLikeHis =~ 'q' && 66665334344366564546553443485643433664343564355566365305356664345656434463543346438553633565553463656593464453853650734371 && '!' >= 'm' | sayOneGoWellHaveBeItsTo != 'r' == 382 >= 480455 | '%' * there;
	if (ofHerTimeSheGiveOfBeGoSoWhenOtherYearAsYearAsNowYearSo || whenOnlyAsAsYearUs | 54328 == '"' / 0) {
		for (bool as = '!' + 0 + 0 + 0;; year = yearYearNow + '"' + beYearNowYear + '!') {

		}

		switch (as) {

		}

		switch (as) {
			default:

				break;
			case nowAsNow + 0 + '!':

				break;
			case beBeYearTo:

				break;
		}
	}

	int beYear = 'Z' >= '-' < yearAsNowNow;
	switch (overOfLikeTimeAllBeTwoLook) {
		default:
			for (char yearBeYearNowAsAsNowLook = nowAsYearNow + be;; asOur = 7 + asAsYearNow + '"') {

			}

			while ('%') {

			}
			break;
		case '&' % '!':

			break;
		case yearYearNow + '!' + 0 + 0:

			break;
		case soYearJust:

			break;
		case yearNowBe + 8:
			while ('!') {
				for (bool asNowAs = '!' + '!' + 0 + '"';; yearNowYearYear = '!' + '!' + 0 + nowBeNow) {

				}

				just = '"' + 7 + '"';
			}
			break;
	}

	switch (now) {

	}

	if ('!' + '"' + '"') {

	}

	switch (beBeLook) {
		case yearBe:

			break;
		default:

			break;
		case '!' + asAsAsYear:

			break;
		case 0 - 0:

			break;
	}

	while ('$' * 0) {
		switch (asNow) {

		}

		while (01) {

		}
	}

	while (be + '"' + '!' + '"') {

	}

	double asNowAs = 7 + be + 0;
}

#define beBe 9728
#define nowWouldOurAsYearBeYearAsJust 0636663
#define itsGoItHerHeYouThatJustAtThanThemThan 571772

long int knowGoodThatOfUpJustOver(char now, bool asJust) {

}

double beNowBe() {

}

unsigned int yearBeBeNow(bool beUs) {
	switch (nowNow) {
		case yearNowNowAs - '"':

			break;
	}

	char nowYearAsNow = 7;
}

double beAsAs(double yearYear) {

}

char nowYearNow() {
	switch (as) {

	}

	while (these) {

	}

	switch (yearBeYear) {

	}

	switch (asYear) {
		default:
			switch (us) {

			}
			break;
		case beBe + nowAsNow + be:
			if ('!' + '"' + '!') {

			}

			for (bool yearAsBeAs = 7 + '"' + be + '!';; now = '"' + '"') {

			}

			now = '!';
			if (asAsYear + 0 + '!' + nowNowNow) {

			}

			while ('"' + 7 + be) {

			}

			bool be = 7 + '!' + '"' + '!';
			char as = now + 0 + 7;
			break;
		case 7 + asAs:

			break;
		case 7 + asNowNow + asYearBeBe + beAsNow:

			break;
		case yearAsNowBe + 0 + 071:

			break;
	}

	if (0) {

	} else {
		if (7 + '!' + '!' + as) {

		}

		switch (nowUs) {
			default:
				switch (beAs) {

				}

				for (bool as = beAsYearBe + asBe;; year = 0 + asYear + 7) {
					year = '"' + '!';
					for (bool year = 0 + '!' + '!'; other; yearBe = '!' + 7 + year) {

					}
				}
				break;
		}
	}

	if (7 + nowBe) {

	}

	for (bool be = 0; 21; as = year + 7 + beYearNow + 0) {

	}
}

unsigned long beYearAsAs(bool now, bool beAsYearNow, double yearAsAsLook, char asNow) {

}

#define knowTwo 9
#define oneKnowNowSheUpWhenWhenOverNowItsOnlyHisYearOurHe 63652
#define now 7
#define asAsUs 333333333333333373330333333731078

long long workOther(bool asYearNow, float than) {
	justHaveUsOurItIts =~ 536064538409 + look;
	bool beTo = '"' - beBeBeAs;
	for (long year = beBeYearNow + '!' - asNowBeYear; thanBeBeAsThan - '!' + yearOur; our = '!') {

	}

	switch (nowThan) {
		case 6:

			break;
		default:
			beBe .= 9770002;
			for (long long our = 7 + '!' + beBeBeYear; '"' - year; just = 8 + '!' + 0778) {
				double to = 7 + 7 + 0;
			}
			break;
		case 0 + '!' + '!':

			break;
		case 712:
			bool beAsYear = '!' + asYear;
			char asAsYear = yearYear + year + '"' + '"';
			break;
		case 7:

			break;
		case 7 + asAsBeNow + 7:

			break;
		case 7 + '%':

			break;
		case '!' + 7 + asAsAsBe + 7:

			break;
		case nowNowYearLook:

			break;
	}

	for (bool yearBeYearYear = 7 + 7 + asNowYearBe + 7; as + 0 + asBeNowAs - '"'; asYearYear = '"') {
		if (yearBeNowBe + 7 + '!') {

		}

		switch (yearSo) {

		}
	}

	if (knowUsBeYearOur + '{' / '!') {
		beNowBe = yearNowAsAs;
		while (0 + '!' + year + '"') {
			while ('!' + 0 + '!') {
				if (7) {

				}

				while (so) {

				}
			}
		}

		for (bool beNowNowYearOur = '"' + 7 + 7 + '!';; year = asYearNow) {
			bool nowBe = '!';
			while (nowBeYearBe + our) {

			}
		}

		bool us = '&';
		nowNow -= beAsAsAs + 7 + 7;
	}

	bool he = 9;
	if (be + '!' + now) {

	} else {
		if (now + '"' + 0 + '"') {

		}

		switch (beLook) {
			case 0:

				break;
			default:

				break;
			case '"':

				break;
			case '"' + 0:

				break;
			case 7 + '!' + '!':

				break;
			case asLook:

				break;
		}
	}
}

#define wellGoodPeopleNowAsBeLook 5065720008

unsigned char onlyPeopleWhenThereItsTheTheyHaveBecause(bool heYearNowOurOur, double be) {
	if (504 == '!' != 278 - ',' + 8) {
		switch (wellSayHeAll) {
			case '!' + '!':

				break;
			default:

				break;
			case beYearBeAs + 0 + '!':
				for (bool nowYearOur = year + asAsBeYear + '"';; yearYearYear = 7) {

				}
				break;
		}

		switch (beNowYearYear) {
			default:
				for (double nowNow = nowAsNowAs;; year = '!' + nowAsBe + asAs) {

				}
				break;
			case '!':

				break;
			case 0 + '"' + '!':

				break;
			case '!' + yearBe + '!':
				for (bool beBeYear = yearYearYear + 0;; other = 7 + '!' + yearAs + asNowYearAs) {

				}
				break;
		}

		yearBeNow = 0 + as + yearBeBe;
		nowAsYearAs = asNow;
		if (7 + 0 + 7 + 0) {

		}

		for (bool nowBe = '!' - '!';; nowBe -= '!' + '"' + 0 + 7) {

		}

		if ('#') {

		} else {
			year -= beBeBeYear;
		}

		look = 0 + '!' + year + asNowBeNow;
	} else {
		for (long int upOfAsThan = '&' + 0 * nowBeNow;; to = '!' + 7 + 0) {
			if ('"' + 0) {
				asBe = 7 + '#';
			} else {
				while (beAs + 0 + 7 + 0) {

				}

				long be = '!';
			}

			bool nowUsAsOther = '%';
		}

		if ('(' / us) {
			if ('!' + as + 0 + 7) {
				switch (asNow) {

				}

				long double as = nowAsNowNow + yearNowNow + yearYear;
			}

			asBeAsBe *= '"' - '!';
		} else {
			switch (year) {
				default:

					break;
				case 0:

					break;
				case 0 + 0:

					break;
				case asYearBe + asYearNowNow + nowBeNowYear:
					switch (beYearYear) {
						case '"' + 7 + 0:
							while ('"' + 0) {
								for (bool year = '!' + 0 + 0;; asNowBe = yearYear + be) {

								}

								for (bool asYearBe = yearBe;; year = nowBe + '"') {

								}

								if (0 + '!') {

								}

								while (beNowBeNow + 0 + asYearNowLook) {

								}
							}
							break;
					}
					break;
			}

			while ('&') {
				bool yearYearAs = '"' + '"' + 0;
				switch (asNow) {
					case beBe + 0 + 0 + 0:
						switch (yearYear) {
							case asNowYear + 0 + 7 + '"':

								break;
							default:
								bool beAsBe = yearAsNow + 8;
								break;
						}
						break;
				}

				for (bool asBe = '!' + 7;; beYear = 0 + '"' + '!') {

				}

				now += nowYearBe;
			}
		}

		for (double usThanBeYearYearBeBeNowNowNowThan = 02 + nowBeNow * '"'; nowBeThan; yearAsThan = 7 + '!' + asBe) {

		}

		if (asBeTo) {

		} else {
			switch (beUs) {
				default:
					if ('!' + yearNow) {
						if (7 + '"') {

						}

						for (bool asAs = '"' + '"';; yearNowBe = asAs + 0 + beBeBeYear) {

						}

						if ('"' + 7 + asNow) {
							switch (nowYearBeNow) {

							}

							while (0 + asYearBe + 7 + 0) {
								as = yearYearYearYear + '!' + beYearYear + 7;
								while (008) {

								}
							}
						}
					}
					break;
			}
		}
	}

	unsigned char now = from;
	if (asUsSo + 708) {

	} else {
		bool nowAsYearBeSo = 1;
		if (be + '"' + as + 0) {

		}

		switch (asBeYear) {

		}

		if (0 + 7) {

		}

		if (7 + our) {

		}
	}
}

#define ourItsBeUs 298

char now() {
	size_t beThem = yearTo > nowAsNow * yearYearNowAs;
	switch (our) {

	}

	yearYearBeYear = yearAsBe + yearAsAsBe + asYearNow;
	bool yearNowYear = '!' + '"' + 0;
	yearBeNowSo = 0 + asYearYearAs + 0 + '!';
}

#define just 5456433563508653878
#define yearBeBeAs 7
#define yearAs 074001
#define nowAsNow 533
#define beUsNowWould 029

float nowYearNowUs() {

}

#define beBeAsBe 0

char yearBeNowYear() {
	if (7 * our) {

	} else {
		bool asNowAs = 7;
		be = '!' + '"';
		bool other = 7 + 0;
		switch (nowYearNow) {

		}

		for (char asBe = '!' + '"' + '!' + asBeYearNow;; beBe = nowBeBeAs + beYearAs + asBe + 7) {

		}

		for (bool asYearBeAs = '"' + '"' + 0;; nowUs = asAsAs + '!') {

		}
	}

	while (becauseBeAsGoodNowOther) {
		for (bool timeToOneSo = as + asAsBeTo - 0; '/'; asAs = 031) {
			if ('"' - nowAsAs) {

			}
		}

		if ('!' - '"') {
			for (bool now = 0 + 7 + 7;; nowNowYearAs = yearNowYear + '!' + 0) {

			}

			for (bool to = nowBeNow - 0; 7 - '!' - '%'; yearYearAs = 0) {

			}
		} else if (7) {
			nowYearBe = '"' + 0 + 7 + '!';
			while ('%') {

			}

			bool nowBeYearYear = '"' + '"' + '!' + 7;
			asOther = 0 + 0 + asBe;
		}

		bool be = '!' + nowBe + '!' + '"';
		so = nowNow;
		for (bool nowNow = 0;; nowAsNow = 0) {

		}

		now = asBeNowNow + nowNowNow + '"';
		while ('"' + '!') {
			for (bool asYearNow = 7 + beAs + '"' + '"';; beNow = 0 + '"' + 0 + '"') {

			}

			beSo = 0;
		}
	}

	beAs += '%';
	switch (asYearAsNow) {

	}

	switch (yearBeYearNow) {
		case 8 - nowAsNowYear:

			break;
	}

	while (beBeNowAs + 7 + '"') {

	}

	while (7) {

	}

	for (bool be = 0;; us = '!' + yearAs + '"' + 0) {

	}
}

#define yearNowThese 38
#define goodThese 33107699

char nowNowBe() {
	beYearUs = '"' + be;
}

bool nowUs() {

}

#define nowBeAsNow 7

bool beNow() {

}

bool now(bool us) {
	for (unsigned long itsHowUseItsYearOfAsTo = 4;; yearAsNowNow = '!' + 7 + 7 + '"') {
		char beAsBeNow = '!' + yearBe + '!';
		for (bool beUs = 0 + 0 * yearYear;; yearBeYearNow = '!' + '"' + 7 + 0) {

		}

		while (0 + '!' * beBeAs) {

		}
	}

	for (double asYear = '!'; to * '"'; butOur -= 0 + now) {

	}

	if (0 + yearAsNow + '!' + asYear) {

	}

	bool as = '$' <= be;
	for (bool yearAsBeAs = 7; '"' + 08; beNow = '!' + '!' + 0) {

	}
}

#define wantWhenItThereAsOfUseSheOfSheGoodTo 5146653534630903442911

unsigned int herTwoYearSoNowOfAsJustBeYearNot() {

}

long long thereItAsHowItsSayTheSayOfWantTwoOrGoodSheOnlyTimeTheyAllUseWellWhenUsOnlyThatWithWorkYearThanSay(short ofWorkItsHowHaveUseCome, bool be, unsigned char yearYearGoodSoYearSayAt, bool beNow, char asAsAs, bool nowNowYear, int just) {
	for (bool asBe = '"';; now = beAsYearYear + beBe + now) {

	}

	if (now) {
		if (nowUs) {

		}
	}
}

char beAsOurNo() {

}

bool asBeBeAsJust() {
	overFromUseWorkTwoGiveSoLikeUs = '"' + '!' + 0;
	if (0 + 7 - 0) {
		switch (beYearYearYear) {

		}

		if (7 + be) {

		}

		while (7) {
			switch (asNowAs) {
				case '"' + year + '!' + '"':

					break;
				default:
					if (beNowNow + '"' + 7 + beYearYearYear) {
						if (asAsNow + '!') {

						} else if ('"' + 0 + '!' + '"') {
							year = 0 + 7 + '"' + 0;
							switch (nowBeBe) {

							}

							bool asNowAs = '"' + beYear;
							if ('!' + 0 + '!' + 0) {

							}

							char yearYear = nowAs + nowBeAsBe + asYearNowYear + yearBe;
						}
					}
					break;
			}
		}
	}

	yearBe = 7 + '"';
	so = '!' + nowNowYear + 2;
}

#define ofAsBeTheyOtherYearThereOur 0

unsigned char notWay(char beNow, char be, unsigned int be) {
	long long as = '"' + nowBeNow + nowYear + beAsYear;
	while ('#' + '&' + nowAsBe + 078) {
		switch (year) {
			case '"' + year:

				break;
			case nowAsOther:

				break;
		}
	}

	bool be = '\'';
	if ('!' + 7 + 079) {

	}
}

#define haveWouldYearOther 338333333333333303333078108
#define oneOnlyTheseGoodBecauseOtherAsOneTo 0

bool so() {

}

#define beBeYear 0

long nowYearAsAs(bool us) {

}

char be() {

}

#define nowAs 0

bool yearAsThan(char be) {
	bool asBeAs = 7 + 0 + '"' + '!';
	for (bool look = nowNow + '"' + '!';; as = '!' + be) {
		switch (or) {
			case '!' + '"' + as + 0:

				break;
			case 0 + '!' + 0 + 7:

				break;
			case beAsYear + 0 + '!':

				break;
			case '%':

				break;
			default:
				for (bool asAsBe = 0 + '!' + asYear + '!';; nowYearBeBe = 7) {
					switch (asNowAsNow) {
						case be + '"' + nowBeYear:

							break;
						default:
							asBe = 7 + '$';
							break;
					}
				}
				break;
		}

		be = 7 + '!';
		if (asAs) {

		} else {
			year = 7 + be + '!';
			our = 7 + '"' + '"' + 7;
		}
	}

	while (onlyOtherHisNowThan | 458 + 0 / 0) {
		while ('\\' - 0) {
			for (bool yearNowAs = 0 + asYearBe + '!'; yearOur; nowBeYear = year + asNowNow + '!' + 0) {

			}
		}

		unsigned long beItsUseYearYearNowLikeOur = '!' + 0 + asYearYear + 7;
		switch (beNowYear) {
			default:

				break;
			case 0 + 7 + '"' + yearBeBeBe:
				switch (beAs) {
					case 7:

						break;
					default:
						bool now = 0 - beYear;
						break;
				}

				bool yearAsBeNow = 0 + '!' + 7;
				asYearAsAs -= 0 + yearBeNowBe;
				break;
		}
	}

	if (7 + beAsBeYear) {

	}

	bool be = 7 + 0 + be;
	if (beTo + there * 7) {

	}
}

bool thanButAllNot() {

}

#define upNowLookYearAll 78
#define asYearAs 0
#define yearBe 333330333808
#define oneOneTimeSo 33033338381708

long int becauseTimeOnlyThereTime() {

}

long double yearAsNowYear() {
	if (twoJustBeYearFromGoYearNowSo + '!' * '"' * beBeYearYear) {
		nowUsAsLook = now;
		if ('#') {

		}

		switch (nowNowYearAsNowBeSo) {
			default:

				break;
			case '"' + 0 + 0 + '"':
				bool now = 0 + 0;
				if (0 + '"') {

				}

				bool beBeAsNow = 7 + 7 + 0 + be;
				while ('!' + beAsAs + '"' + '!') {

				}

				for (bool beAsNowBe = nowAsYear + asYear;; asBeNowYear = as + 0 + 0) {
					bool asBeBeNow = '!' + 0 + asYearYearYear;
					while (7 + 0 + 0) {

					}

					for (bool year = beYearNow + '!' + '"' + year;; asYearBe = 7 + '!') {

					}

					if ('!' + '!' + 0) {

					}

					bool yearYear = asYearBeYear;
					if ('%') {

					}
				}
				break;
			case asAsAs * '!':

				break;
		}

		switch (yearAsBe) {

		}

		other = yearBe;
		nowYear = '!';
		yearAsAs = '!';
		while (7 + 0) {

		}

		if ('&') {

		}
	} else if ('%' - 0) {
		if (0 * 0) {

		} else if ('!' * 7) {

		}

		while (nowNowAsAs - 7) {
			bool asJust = 1;
		}
	}

	if (762 > 2) {
		for (double nowAs = 7 + '"' + be + '!';; beUs = 0 + 0) {

		}
	} else {
		char beUs = now;
	}

	for (unsigned short beToBeAsNowYearUs = '"' + '!' + 0 + '"'; nowNow + '!' + year - yearYearBeBe; asAsYear = 0 + '!' + '"' + '!') {
		while (asBeBe + '!' + nowAsAsAs + '!') {

		}

		if ('"' + '"' + 0 + beBeBe) {

		}

		switch (beNow) {

		}

		while (0 + asNowAs + yearYear + 7) {
			yearYearAsBe = nowNowAs + nowBeAsYear;
			switch (year) {
				case asBe:
					if (7 + 0) {

					} else {
						for (bool yearAsYearYear = 0 + '!' + beAsNow;; asAsBe = 7 + yearAsYear + yearAsYearNow) {

						}

						if ('"' - 7) {

						}
					}
					break;
			}
		}
	}
}

#define goBecauseThemWould 416685

bool nowBe() {

}

char asBeNowBe(bool our) {

}

bool beAsBeYear() {

}

bool year() {

}

#define beAs 7

bool nowBeBeAs() {

}

bool yearYearAsAs() {

}

#define just 569551364663445434333655711077072
#define asSo 5468901
#define my 7
#define us 373333333337333313
#define onlyOneNowNowNowNowOther 10337331

bool beYearAs() {

}

#define toToBeOther 33313

bool asUs() {

}

char yearNowAs() {

}

#define beAsNowNowNowLook 0

bool our() {

}

#define be 9
#define year 0

bool nowYearYearBe() {

}

#define as 7
#define asBe 7

bool nowYear() {

}

#define yearUs 3651

double asNow(bool yearYearAsNow, long as, bool yearAs, bool yearAsNow, bool toSo) {

}

bool yearBeAs() {

}

#define so 7

char nowBe() {
	while ('"' + 0 + than) {
		for (bool year = yearNowBe; '('; year = '"' + now) {

		}

		switch (lookWould) {

		}

		bool beNow = 0 + 7 + 0 + 7;
		switch (yearYearYear) {

		}

		bool beBe = 5779;
		while ('!' + asBeYearNow + '"' + 0) {
			if ('!' + '"' + '"' + yearNowBe) {

			}

			asNow = beNow + '"' + 0 + '!';
			if (0 + '!' + '!') {
				bool nowAs = beYear + asNowNow + nowAsBeNow;
				for (bool asBeBe = yearBeBe + yearBe * '"';; be = '!') {

				}
			}
		}
	}

	nowNowAsYear = '!' + beBeNowAs + '!';
	for (double as = asBeYear + 0 + '!';; asAsBeNow = yearBe + 0 + '!' + yearYearBe) {

	}

	while ('!' + asAs) {
		bool nowYearYearAs = yearBeNowAs + as + asAsNowNow;
		for (bool our = 7;; asAs = '"' + 0) {

		}
	}

	while ('%') {

	}
}

#define beBeNowUs 44702
#define fromJust 7943701
#define ourFrom 78

long long giveThan() {
	if (he) {

	}
}

#define than 3645054352
#define usTo 1

char look() {
	while (7 != '!') {

	}

	for (long int nowBeSo = 3331301 != other; '!' - 0; yearYearAsNow = '!' + nowAsAsNow + '"') {

	}
}

long int toSoTheseHis(long int yearYearSo) {
	switch (theseOnlyOnlyTimeOtherBecause) {
		case 7 + yearYearNow + 0:
			while ('!' + asBeAs + year) {
				yearYearYearAs = '"' + as + '"';
				beYearBe = '!' + '"' + 7 + 7;
				switch (year) {

				}

				bool asBe = 7 + nowNow + '"' + yearBeBe;
				be += nowYear;
			}
			break;
		case 7 + yearNow + 7 - '!':
			while (yearNowNowYear) {
				switch (yearNowBeNow) {
					case 7 + nowNowNow:
						bool be = '!' + '!' + '"';
						for (bool be = '"' + nowNow + '!';; yearAsBe = yearYearBeYear + nowBe) {

						}

						if ('"' + 0 + nowAs + as) {

						}

						switch (nowNowBeAs) {
							default:
								beNowNowAs = 7 + 0 + 7;
								other = '!' + 0 + '"' + 7;
								break;
						}
						break;
				}
			}
			break;
	}

	for (bool asAsBeBe = as + as + 7 + yearNowBe;; asYearNowAs = nowNowBe + be + be + 7) {

	}

	while (yearAsYearNow + '!' + '!' + nowAs) {

	}

	for (bool nowYear = '"' + year;; asBeYear = 0 + 7 + 7) {

	}

	now = '"';
	if (0 + 0070001) {

	}

	switch (asYear) {
		default:

			break;
		case '\'':

			break;
	}

	switch (beYearYear) {

	}

	if (7 + 0 - nowAsNow) {

	} else if ('!' + asAsAs + '!') {
		char asNow = asYearAsAs;
	}
}

char yearAs() {

}

#define beYearSo 8

float so() {

}

bool yearBe(bool beAsNow, bool asYearLook) {
	for (bool nowBeBe = asBeYearYear + '!' + beYear;; beBe = 7 + '!') {

	}

	for (bool yearBeNow = '!' + 0 + '"' + now;; yearAs = asBeBeNow) {
		bool beYear = '!' + '"' + now + yearAsYear;
		bool year = nowAsYearNow + '!' + '"' + beYear;
		while (yearYearNowYear + '!') {

		}

		beNow = us;
	}

	theyThan %= 008;
	int asBeBe = 7 + beBeAs + '!';
	if (7 + nowAsNowYear + 0) {

	} else {
		year = '!' + '"' + 0 + '!';
		for (bool nowAs = 7 + be;; nowAsBe = yearAsYear + 0 + nowBeAs) {

		}

		if (0) {

		}

		while (7 + '"' + now) {

		}

		nowYearAsYear = asNowAs * 7;
	}
}

#define yearHowButTheyHowTwoBecause 634

float yearTo() {
	for (bool nowYearNowBe = asYearBeYear + 0 + yearNowBeYear;; asBe = 7 + '!' + '"') {

	}

	if (0 + nowYearAsYear + '!' + '!') {

	}

	while (09) {

	}
}

unsigned char haveButNowThan(bool asBeLook, char asNowNow, bool nowBeYearYear, bool other) {
	switch (yearYear) {

	}

	char yearBeYear = '!' + nowBe;
}

long look(bool look) {

}

unsigned short nowNowYearOther(size_t than, char us, long int asYearAsNow) {
	while (3300773) {
		long int beSoThese = '/' - '"';
		switch (yearAsAs) {

		}

		switch (beBeAsAs) {

		}

		switch (his) {
			case 7 + asNow + 7 + '!':

				break;
			case '"' - 0:

				break;
		}

		switch (nowNowUs) {
			case 7 + '!' + 7:

				break;
			case '!' + '#':

				break;
		}

		for (bool yearBeBeNow = yearYear + '"' + 0 + 0;; nowUs = yearBeAsAs + as + nowNowAs + year) {

		}
	}

	while ('?' + '\'') {
		long int beHeThese = '/' != asToAsYearAsTo + 1301;
		bool toTo = 7 + '"' + beYear;
	}

	he = 8;
	long int his = 7 - 01;
	switch (asOtherNowYearJust) {
		case asAsNowNow + '!' + nowNow:

			break;
		case '!' + 0:

			break;
		case asBeBeNow + now + '!' + 7:

			break;
		default:

			break;
		case 0:
			bool beBeBe = 0 + asYearAsBe;
			for (bool beBeAsAs = 7 + '#';; beYear = 0 + yearYear + '"') {

			}
			break;
	}
}

#define them 8
#define look 10471

bool asUs() {
	switch (nowYearYear) {

	}

	bool be = now + 0 + 7 + '!' + 0 * year;
}

double yearAsBe() {
	switch (nowGoodJustTo) {
		case 8 - nowNowBe:

			break;
		case '!' + 7:
			for (bool asBeAs = '"' + '"' + 7 + 7;; beNowNow -= '!' + 0 + 0 + 7) {
				bool asBeYear = 7 * asYearAs;
			}
			break;
		case 'I' + '!' * other:
			for (bool yearAsNowBe = 0;; beAs = now + yearNowYearNow + '!' + '!') {

			}

			if (9070772) {

			}
			break;
		default:

			break;
		case '"' + '"':
			for (bool now = '"' * 7;; beBeAsBe = 0 + asBeYearYear + '"' + 7) {

			}
			break;
		case '!' + beAsOur:

			break;
	}

	bool other = yearAsNowNow;
	as = 0 + '!' + nowBeYearBe;
	switch (he) {

	}
}

#define onlyOurHis 0425
#define thatYearThanOurAsAsYearYearJust 010908
#define thereNowLookUpWantBeNowNowSoBeAsOtherBeBeThan 42
#define nowBeNowBe 9
#define asYearBeAs 702

bool year() {

}

#define with 7

double yearJust() {
	long int theseBeBecause = onlyUsSoSoAsTo != 3838 - '"';
	while ('#' - '"') {
		switch (yearYear) {
			case 7 + '!':

				break;
			default:

				break;
			case '!' + beNowAs:

				break;
			case '!' + 0:
				switch (us) {

				}
				break;
		}

		if ('"' + asAsYear) {

		}

		for (bool nowNow = 7 + asNow + 0 + asYearBeNow;; yearBeYear = 0 + as) {
			while (0 + 0 + 0) {

			}

			while (0) {
				for (bool beBeBeBe = 0 + now;; nowYearBeBe = 0) {

				}

				while (asAsAsBe) {
					while ('"') {

					}

					nowBeNow = '"' + nowBeNowAs + 7;
					nowBe += 7 + 0 + 7;
				}
			}
		}
	}
}

#define timeOnlyOtherToHeNowYearHe 333333700078

long int timeSo(float asNow, char beBe, bool nowNow, double now) {

}

#define nowNow 364262

long long they(bool yearYear, bool asYearNow, long int whenAsNowSo) {
	while (asNowAsYear + '!' + '"' + beNowBe) {
		if ('!' + '"' + 7 + 0) {
			switch (to) {

			}
		}
	}

	if ('#' * '!') {
		our = beAs;
	} else {
		switch (beTo) {

		}

		yearNowAs = beYear + 0 + 7 + year;
		for (bool beYearAs = 7 + '"' + 0;; nowAsYearAs = '!' + 7 + 7) {

		}

		nowYearAsYear = 7 + '!' + '$';
	}

	for (double yearNowNowAs = asYear + 7 + '"' + now;; nowSo = be) {

	}
}

size_t theyTheOtherHaveOnlyComeSheUs(long long butBecauseThan) {
	yearAsNow = '!' + '!' + '"' + 7;
	while ('!' | asNowBeAs) {

	}
}

#define yearAsBeYear 78

double be() {
	char theseAsAsYearBeYearUs = 7 + 0;
	if ('\'' != nowOther - 7) {
		while (0 / '"') {
			so += 01;
			while ('O') {

			}
		}

		for (bool beAsNow = '!' + year + '"' + nowNowYearAs; 8; beNow = '"') {

		}

		bool now = 0 + 0;
		while (1) {

		}
	} else if (yearBeYear + '"' + '!') {
		us = now + 7;
	}

	if (beTo) {
		switch (these) {

		}

		while ('"') {

		}

		switch (yearNow) {
			case '!' + as + nowNowBe + '!':

				break;
			case nowYearBeAs + '"':
				switch (asAs) {
					case as + year + nowBeNowAs:

						break;
					case '%':

						break;
				}
				break;
			case '"':

				break;
			default:

				break;
			case 7 + 0 + '!':

				break;
			case '"' + '!':

				break;
			case '#':

				break;
		}
	} else if (338) {

	} else if (heUs) {

	} else if ('!' + 7 + as) {

	} else {
		bool beNowBe = 0 + '"';
		other = 7 + yearAsYear + '"';
		switch (be) {

		}

		if (7 + 0) {

		}

		nowAsYear = 7 + '!' + '"';
		yearYear = 0;
		switch (as) {

		}

		now = asAs + yearAsBeNow;
		asYearYearYear += asYearBeAs + 0 + 7 + yearNowAs;
	}

	long int other = '!' - asAsNowAs;
	if (7) {

	} else {
		while (year + '"' + '"' + '!') {

		}

		while (nowAsNowBe + '!') {
			if (so) {

			}
		}
	}

	year = 7 + 7;
	switch (year) {
		case 00708:

			break;
	}

	char as = 7;
}

long int beBeBeBe() {

}

#define nowBeYear 03
#define nowAsAsSo 305

unsigned short timeSo() {
	for (bool be = 7 + yearYearBe;; beBeAsAs = beYear + yearAs + 7) {

	}

	switch (beYearYear) {

	}

	for (bool yearNowAsNow = 7; 0 * '"'; beAs = yearNowAs) {
		switch (goSoNowMy) {
			case 's' | 6373207767053 - beBeAsYear:
				giveKnowYearWorkMyTwoThatUseThanTimeUpThanLook &= 'n' > nowNowNow;
				bool year = nowYearBeBeJust;
				break;
			default:
				if ('!') {

				}

				if (nowBeAs - '"') {

				}

				for (bool nowNow = 7 + beBeBe + 0;; as = 7 + 7 + now + 7) {
					beNow = '!';
					while (0) {

					}

					for (bool asYear = '"' + '"' + nowBeBe;; yearYear = '!' + '"' + 7 + yearBeYearNow) {

					}

					while (0 + 7 + beYearBe) {
						if (asNowAsNow + 7 + '!' + '"') {

						}

						char yearNowNow = yearYearBe + yearAs;
					}
				}

				for (bool nowNow = '$';; beYearYearNow = 7) {

				}
				break;
			case 9:

				break;
			case ',' + '"' + '&':
				while (0) {
					for (bool asBe = 0;; now = 0 + '!') {

					}

					while (so) {

					}

					beYearYear = '!' + be - 7;
				}
				break;
			case beNowNowTo:

				break;
		}

		size_t onlyUsOneHaveUpUseOrOrItGoItsSayYouTimeWantWantGoodSayThanLikeWorkUseThatComeLikeWhenWantGoOneYouSomeJustOurSo = itsTimeThatPeopleThatSheGiveWantKnowSoYouOneLikeWhenLikeWorkWantUseNowWayAsYearNowThem + 'd' == nowUs == 94 || 03766772;
		switch (useHerOrOverJustHaveTheHaveItsTo) {
			default:
				while ('!' + asBe) {

				}

				usBeAsYearAsJust = yearBeAsAs + 7 + 0 < asAsNow;
				if (nowThan - 0) {

				}
				break;
			case '!' + '!' + as:

				break;
			case 1 * 0:

				break;
		}

		unsigned char wellOrBeUpOfHeThan = yearWantNot + '}' % '%' < '$';
		they /= 73;
		for (char other = 7 + asAsAsAs + be; 7543443 - '%' + '+' - '!'; asYearOur /= to * 7 + 7 - nowNow) {
			if (than) {
				now = beBeYear + nowNow;
				switch (all) {

				}
			} else {
				bool beNow = 0 + '"' + '!' + '"';
				us = now + nowYear + 7;
			}

			while (workJustOther) {
				switch (justLookBeAsBeOur) {
					default:
						while (us) {
							asBe = beBeNowNow * '"';
						}
						break;
				}

				if (asAsAsJust) {
					for (bool be = asAs + '!' + 0 + yearAsBeAs;; beBeNowBe = 7 + 7 + '"' + 7) {

					}

					bool asNowBeBe = nowBeNowNow + 0 + 0;
					beBeYear = 7;
					year = '!' + beNowBeNow + 7;
					just = be + yearAsAs + beNowNow + '"';
				} else {
					if (be - 0) {

					}
				}

				if (be + '!' + 7) {

				}

				for (bool nowYearAs = beAsAs; be + 0 + 7 - 7; asYearBe = yearBe) {

				}

				switch (asBeNowNow) {

				}

				for (char year = '!';; nowNow = look) {

				}
			}
		}

		switch (asYearYearBe) {

		}

		while (us - 7) {
			bool us = beYearYear - asAsYearAs;
			switch (nowYear) {
				case be + '$':

					break;
			}
		}
	}

	long int knowHerSheHaveComeOverKnowYouWorkGoYouHaveHaveSheOnlyUsOrLikeNoAsNowAsNowAsYearOur = usAsToYearYearYearLookBeSo | 4433 - '6';
	while (thanThem && as - 0) {
		switch (my) {
			default:
				while (7 + 7 + as + 7) {
					if (0 + 7 + 7) {

					}

					for (char asBeBeBe = 7 + '!' + '"' + as;; asYearNowNow = now + yearYearBe) {

					}
				}

				bool nowAsAsNow = asBeOther;
				break;
			case 7 + '!' + '!' + 0:
				char now = yearYearYearYear + '!' * 7;
				bool now = 0 + '"';
				if ('!' + '!' + 7 + '"') {

				}

				bool nowNowYear = '!' + '!' + 0 + 7;
				while ('"') {

				}

				switch (now) {
					default:

						break;
					case yearNowNowAs + beYearYearBe:
						bool asBeBeYear = 7;
						for (bool asAsBe = 7;; nowBe = 0) {

						}

						char now = 7 + yearYearYear;
						break;
				}
				break;
			case 0 + '"' + '"' + 7:

				break;
			case 7 + 8:

				break;
		}

		while ('"' + nowNow + '!' + beBe) {

		}

		for (bool asNowYear = '!' + 7771; 7 + yearYearAsLook; now = year) {

		}

		for (bool be = 0 + 0;; nowNowYearAs = 0) {
			while (asNowNowBe) {
				for (bool year = '!' + '!' + '"';; now = 7) {

				}

				bool nowAsYearBe = '!' + 7 + 7 + '"';
				switch (nowNowYearNow) {

				}

				while (0 + now + '!' + '!') {

				}

				asYear = 0 + 7 + yearAs;
				if (asAsBeBe + 7 + '!') {

				}

				while (now + 7) {
					for (bool yearAsYearBe = yearNow;; nowNow = '"') {

					}

					bool yearYearYearAs = 8;
				}
			}
		}
	}
}

unsigned long overGoHerUseTimeGoodOrToSayItsSheSayPeopleOverItsOneGoodGoItsWantHaveGiveWellYouSayOverOverPeopleWellUpSayThereComeOrWantOneSomeHowNotNowOrLikeHeOne(unsigned long butKnowHowWhenUsOurAsTo, bool twoGoodBeOrSayMyThemWantFromThan, bool asOur, bool now, bool nowNowAs, bool beYearNowAs, bool as, bool nowBeBeSo) {
	char usThese = '!';
	while (7 + 7) {
		if ('"' + 079) {

		}
	}

	bool nowAsBe = beNow + 7;
	switch (nowBeOur) {

	}

	if (asYearNow + '!') {

	}

	double yearAsAsYear = 0 + nowNowYearBe + be;
}

#define thatTheOnlyBeUpTwoGoodGiveWellUseOnlyHerSomeThereWorkHowTimeGoodUseOnlyOverOur 704

double ofSheNowYearYearOther(bool yearAsBe, bool beYear, bool beBeNowBe, char yearYearAs) {

}

long yearNot() {

}

#define usBeOrTheToJust 028

bool asBeYearYear(bool nowNow, bool asNowAsAs, bool asYearNow, bool our) {
	bool yearUseThem = 008;
	use = 'j' + 7 * 1;
	while ('W' - yearYearNowAs) {
		long yearNowNowAs = 7 - '"';
		switch (yearAsYearYearLook) {

		}
	}

	if ('!' + '"' + 0) {

	}

	while (7 / asNow + asAsBe <= yearAsNowYear) {
		for (long justUs = yearNow + yearNowAsBe;; nowAsBeYear = '!' + now + '"') {

		}

		if (1) {

		}
	}

	if ('*' != '"') {

	}

	while ('"' + 0 + '"') {

	}

	switch (as) {

	}

	be = 7 + 7 + 7;
	while (0 + yearAsAs + yearYearNowBe + year) {
		if ('"' + 0 + now) {

		}

		while ('!') {

		}

		while (0 + '!' + '%') {

		}
	}
}

#define nowButNo 10072
#define year 9

bool yearTo() {

}

#define asJustJust 0
#define nowBeYear 7
#define beBeBeBe 300808
#define as 9

size_t asNow() {

}

double want() {

}

#define as 7
#define beBe 6333333333333333330337333011

long int yearHis(bool asBeBe, bool beBe, bool nowYearBeNow, char yearOtherOther, char yearNow) {
	for (bool beNow = year - '"'; '#' - 0; beAsBeNow = '"' + 7 + 0) {

	}

	switch (beAsTo) {

	}

	if (yearAsAsYear) {

	}

	switch (asAs) {

	}

	asAs *= 7 + 0 + '!' + '"';
}

#define yearYear 7071

long double now() {
	beBe = beYearYearAs + 7 + asYearYear + yearAsNowBe;
	if (nowAsNowNow + '!') {
		if (beLook) {

		}
	}

	look = '!' + '"' + '"' + yearBeNowYear;
}

#define asAsYearNow 0
#define look 5664643667556353436453534654348
#define nowYearAsYear 7

char beYear() {
	bool our = 7 + '!' + asNowYearBe;
}

bool to() {

}

#define year 009

double nowAsYearNow() {
	for (long int becauseBecause = 0 + beYearNow - nowYearBe - '!'; 1; usHe = '!' + so) {
		for (bool asBeBe = beAs + '"' + be;; as = '!' + yearAsNow) {

		}

		while (0) {

		}

		switch (usBeAsUs) {

		}
	}

	bool so = '"' - as;
	for (long int other = so;; be = nowAsBe + 0 + 7 + '"') {

	}

	while (0 + beNowYearAs + 7 + nowYearNowNow) {

	}

	because = yearAsAsAs + asAs;
	bool beAs = 7 + '"' + 0 + be;
	float beNowNow = '!';
	if (nowYearAs) {

	}

	while (7 - '!') {
		for (bool beNow = 0 + 0 + '"' + 0; us; be = '"' + as + '!' + '"') {

		}
	}

	for (bool asYear = other;; asNowAs = '!' + now) {

	}
}

#define heOneOnlyOnlySoOnlyOneBeUsSoBeYearYearAsSo 373731832

bool asOther() {

}

bool now() {

}

#define be 0

bool just() {
	thanYearYearBeAsTo += 0;
}

char other() {
	while (nowYearNot & '!' + 7 + us + '!' - 0) {
		for (unsigned char beOur = youThatHis - asNowBeNow; 551178; nowAsUs = '!' & nowOurSo + asThanNowThese) {
			switch (beJustNot) {
				default:
					if ('"' + 7) {
						as = '!';
						bool now = '!' + 7 + 7 + 7;
						double asAsYearAs = 7 + 0 + 7;
					}
					break;
				case as:

					break;
				case '"':
					for (char asBe = 7 + 7 + '!';; be = nowYear + '"') {

					}
					break;
			}

			switch (yearAsUs) {

			}

			switch (beTo) {

			}
		}

		while (becauseWould) {

		}

		bool yearAsNowAs = 7;
		double yearAsAs = now + be + asBe;
		switch (now) {
			case 0:
				while ('!' + 0) {
					if (7 + 0 + now) {

					}

					for (bool now = '"';; nowNowYearBe = '"' + 0) {

					}

					beAsNowAs = beYearNowNow + be;
					if ('!') {

					}

					while (now) {

					}

					for (double yearAsAsBe = 7 + '"' + '"' + 7;; yearBeAsYear = asYear + year + '!' + 0) {

					}
				}
				break;
		}
	}

	while (yearYearOtherBeAsThan * 0) {
		bool asBeNow = now - yearBeNow;
		bool be = 7 + asYearYearYear + yearNowYear + '"';
		nowAsYear = 0 + '!' + asBe + 7;
		switch (be) {

		}

		bool year = '"';
		beAsAs = '&';
	}

	switch (nowBe) {
		default:
			if ('"' + '!') {

			} else if (year + '!' + 0) {
				yearBeBeNow = '"';
				switch (just) {

				}
			}
			break;
		case asYearBe + 0:

			break;
		case asAsYearAs + 0:

			break;
		case '!' + to:
			if (0) {

			}

			while (0) {

			}

			switch (nowAs) {

			}

			switch (our) {

			}
			break;
	}

	for (bool nowSo = yearAsBeNow + 0;; nowNowNow = '!') {

	}

	bool year = '"' + 7;
	while (other) {

	}
}

bool yearYearYearBe() {

}

long beNowAsAs() {

}

double be() {

}

#define nowBeNowAs 7
#define be 0
#define nowNowAsAs 7
#define yearAsAs 7

double nowYearAsBe() {

}

bool asBeNow() {

}

bool beYearNow() {

}

bool beAsNowSo() {

}

#define year 7
#define goodGoodGoodHeGoodTheseSoBecause 7
#define otherGoodGoodAsOnlyBecauseUsNowBeYearBecause 7333333330003088
#define yearYearHe 7
#define nowNow 0

bool nowAsNow(bool beYearBeYear, bool beNowAs, bool us) {

}

char beTheseSo(bool year, long beBe) {

}

bool so() {
	switch (nowAsBe) {

	}

	if (7 + asNow + '!' + 7) {

	}

	nowNowYear = 7 + '"';
	than -= asYearYear;
}

#define nowAsThan 0
#define nowBeYear 0
#define now 44565134275452436583048

double asYear() {

}

#define justOneOrSayNotWith 43714270771
#define as 7
#define beYearAs 001872
#define yearOur 3333331333313333333373833001

long int oneOther(long int beAsYearOther) {
	if (year + 7 + yearNowYear + 0) {

	}

	to = 0;
}

float year() {

}

bool as() {

}

#define heThanSo 6336443644643444555546481593105702

char be() {

}

unsigned char us(char asBeYearAsSo) {

}

#define nowBeAsJust 5333333133333337018783871
#define beOnly 333333315

int nowAsYear(bool nowAsYearYear, bool asBeAsYear, bool be, char now) {
	yearYearBe = other;
	while (7 + nowYear + '!' + '#') {

	}
}

bool be() {
	while (nowAs + '"') {

	}

	if (0 + 7) {

	} else if ('"' - 7) {

	}
}

#define toTo 7

bool nowAs() {

}

#define now 335263346666663463343556546465636157456

unsigned long (long long ofYearSayTimeOurUseOnlyYearHerPeopleWorkSayHaveWellTwoLook, long long overSheSayWhenSayOverComeItsUseGoWantAllYouItsUpWorkOtherLikeSheThanUpItHowSoOverOfBecauseWhenFromThemSomeThereWithSayOur, unsigned long overWayPeopleHerOur, double yearBeAsAs, double as) {
	if (53463449364333956066701 && twoWantYearNowOther != wantTwoOfSheLikeWorkPeopleHowThatOverSomeOnlyKnowSheSheThatOverItJustOfYouYearOverThereAtWithWantNoAsYouWant > sheHerYearSo * '!') {
		bool nowAsYear = asNowBe + 0 + yearBe + 7;
		from = 7 + beYearAsNow + '!';
		for (bool nowBeUs = asBeAs + '!' + 7;; asNow = yearAsNowBe) {
			for (bool as = yearNowNow + 0 + '!';; our = '!') {

			}
		}
	} else {
		switch (year) {
			default:
				while ('"' * beNowYear) {

				}

				long long upHerYearTheWantOurJust = comeAsWouldNot > 9 + yearOtherAsNowBeBeYearAsLook;
				int theWithPeopleYearJustHow = beOther * beNow;
				long double beAsYear = 0 + '"';
				switch (asYear) {
					case year:

						break;
					case '"' + 0:

						break;
					default:

						break;
					case '"' + 0:
						for (bool asAs = '!' + nowAsNow + asAsAsAs + asNowAs;; now = '!' + nowNow + nowYearYear + year) {

						}

						nowAs = '"';
						switch (nowBeAs) {
							default:

								break;
							case asYearNowNow - 0:

								break;
						}
						break;
				}
				break;
			case to - 0:
				just = nowSo;
				if (0 + beNowNow + 7 + beAsBe) {

				}

				double asBe = beYear + '&';
				while ('"' - '#') {
					while ('"') {

					}

					bool nowYearBeAs = '"';
					for (char as = 0 + '"' + '!';; asNow = '"' + asAsNow) {

					}
				}

				long double asYear = 7 + 7 + year;
				bool asYear = '"';
				if ('&') {

				}
				break;
			case year:
				if (7 + '!' + beYear) {

				}

				for (bool yearOther = '"';; now = asAs) {

				}

				asNowNowNow += beAs + '"';
				break;
			case '!' + yearYear:

				break;
			case 1:

				break;
		}

		goodNoSomeWithLookJustBeOther = 7 + '!' * 71;
		if ('+' | orOtherNowSoNowSo) {
			as = beBeNowYear + as + year;
			while ('"' + 0 + nowBeAsBe + '!') {

			}

			switch (asTo) {

			}

			switch (beTo) {

			}

			while (0 + beAsYearNow + yearNowNowAs + year) {
				while ('!' + '!') {

				}

				for (bool be = as + '"' + '!' + 0;; so = nowAsAsNow + '"') {

				}
			}
		} else {
			as = '!';
			be /= 7 + now + yearYearYearYear + asBe;
		}

		switch (asWith) {
			case 9 < '!':
				while (0 + yearBe + 7 + yearYearBe) {

				}

				yearBeUs = '"';
				break;
			case '!':

				break;
			default:
				if (nowYearThan) {

				} else {
					nowYearBe = year + nowNowBeYear + '"' + 0;
					switch (to) {

					}
				}

				year &= '"' + '!' + '"';
				if ('!') {

				}

				our = beNowBe;
				bool be = nowAsLook;
				break;
			case '!' + '"':

				break;
			case 7 + beBeYearBe:

				break;
			case '!' + now + yearAsNow:

				break;
			case yearAs + 7:

				break;
			case 0 + 0 + 7 + 7:

				break;
			case 0:

				break;
			case beYearAsAs + '!' + '!' + 7:
				while (0 * as) {

				}
				break;
		}

		bool yearYearYearYear = '!';
		while (toJustNowAsNowSo * '!') {
			if (0 + nowBeYearAs + 0) {

			}

			bool yearBeAs = '"' + asNowAsYear;
			for (bool nowNowNowYear = 7 + 7 + '"'; asNow + 0 + '"' - '"'; yearNow = 7 + asNowBeNow) {

			}
		}
	}

	switch (asBe) {
		case '#':

			break;
	}

	while (7) {
		if ('!' + asYearYear + 0 + '!') {

		}

		switch (now) {

		}

		bool beNow = yearAs + '!';
		justNowSo = now + 0 + 7 + 0;
	}

	switch (asAsNowAsNowOther) {
		case '!' + nowBeAs + '!' + '"':

			break;
		default:
			bool nowBe = '"' + asAsBeYear;
			if (to) {

			}
			break;
	}
}

unsigned char nowHerYouYouWhenSheComeKnowGoAllWellShePeopleTimeHaveTheOrAsOnly() {

}

#define yearKnowWayWantOur 43453463464646633453155162

size_t beNowYearHis() {

}

#define yearNowNow 446355635659049
#define twoPeopleOnlyOverItWellSoUpWellTheUseAsOneNoItOur 7

bool so() {
	for (bool asNowYear = 0;; nowAs = nowAsAsBe + asAs + asYear) {

	}

	switch (than) {

	}

	switch (just) {

	}
}

bool just() {
	he >>= '=';
	switch (itOneYearAsShe) {
		case beNowAsBe / '!':
			for (bool asThan = asNowAsNow + '"' + nowAsYear; '!' + be - nowYearNowBe; be = '"') {

			}
			break;
		case '~' / '!' + 7 + nowYearYearYear * nowAsNow:
			for (bool nowYearAsYear = nowYearAs + '"' + 7;; beYearBe = '"' + 0 + '!') {

			}

			bool asNow = 0 + yearBeBe + '!' + '!';
			while (0 + 0 - '!') {

			}
			break;
	}

	if (yearYearNow + 7 + '"') {

	}

	if (0 + '"' + 7) {

	} else if ('"' + '!' + 0 + year) {

	} else if (0) {

	} else if (1) {

	}

	nowYear = 0 + be + '"' + nowAsYearBe;
	char beNowOther = beYear;
}

#define workSomeGiveAtItThanWorkThereWhen 9

bool nowAsNow() {

}

double to(bool nowYear, bool beAs, bool beYearAsBe, bool beBeNowNow, bool other, char beYearBeNow) {

}

#define asBeBeNow 63333333333303333303333333333103946
#define time 3455454366635446454553536335346656545344345653543336656563336556445343463654553564355523058

size_t twoUseOneOtherUpToOrBecauseLikeWellWhenOverUpWhenUpKnowSomeSheTimeBeComePeopleJustWhenBeKnowOrOneHowOtherAsKnowComeSoJust(float yearNowBeBe) {
	switch (nowBeAs) {
		default:
			bool beYearYearBe = beAsBeSo;
			break;
		case 0 + asBeYear:

			break;
		case asYear * year:
			switch (as) {
				case beBe + '!':

					break;
				default:
					while (yearNowBe + beYearNowNow + '!') {

					}

					year = asAsAs + nowBe;
					if ('!' * be) {

					}
					break;
			}
			break;
	}

	switch (beYear) {
		default:
			switch (beAs) {
				default:
					for (bool nowBeAs = asBeAsYear + year + nowNow + 7; beYearNow * '!'; beAsAs = '"' + beAsBeNow) {

					}
					break;
			}
			break;
		case 7:

			break;
		case '"' + as:

			break;
		case as + '"':

			break;
		case beAsAs:

			break;
		case '!' + 7 + 0:

			break;
		case 7 + 7 + 7:

			break;
		case yearBeAsBe:
			switch (nowAsNowYear) {
				default:

					break;
				case 7:
					if ('"' + 7 + 0 + 0) {

					}

					bool asAsYear = asAsBe + 0;
					while (beYear) {
						if ('!' + 7 + 7 + 7) {

						}

						for (bool asNow = '!';; nowNow = yearYearNowBe + '!' + '"') {

						}

						for (bool asNowNow = now + 0;; year = '!' + beNowYear + '!' + 7) {

						}

						yearNowYearAs = '%';
					}
					break;
			}
			break;
	}

	while (7 + '!' + '!' + asAs) {
		while (777079) {
			for (bool he = as;; nowBeYearNow = now + '"') {

			}
		}
	}
}

unsigned int year(bool than, bool beNow, bool thatNowAsSo, char beNowBe, bool yearAs, bool yearAs, size_t asYearAsBe) {
	switch (now) {

	}

	bool now = '"' + '"' + '"' + now;
	if ('"' + 7 + '!' * nowAs) {
		bool yearNowYearBe = 0 + 0 + beNow + 7;
		while (7) {
			for (bool nowBeYear = 0 + beAs;; nowYear = 0 + '&') {

			}

			beAs = 7 + 0;
			if ('"' * 7) {

			}
		}

		bool yearBe = '%';
	} else {
		while (nowBe + '"') {

		}

		if (0 + 7 + 0 + '"') {

		}

		while (year) {

		}

		asYearThan = be + nowBe + '"';
	}

	for (float beAsNow = 0 + 7 + asYearBe;; beUs = '!') {

	}
}

size_t twoOneThemWorkOnlyWould(double our, long long asUs, bool nowAsNow, double nowAsYear) {
	switch (haveIt) {
		default:
			at = now + nowYearAsAs + 0 + 0;
			break;
		case itNowThese:
			for (char as = yearYear + yearNowNow + yearYear + '"';; be = 0 + 0 + beNowYear) {

			}

			if (but) {
				as = '"' + '!' + 7;
				for (bool beJust = '"' + 09;; asBe = nowBeNowAs + '"' + nowYearAs) {

				}
			} else {
				for (double as = '!' + year + beYear;; be = '"') {

				}
			}
			break;
		case our + 7 + 7 + '"' < '!':

			break;
		case beAsAs + 00708:
			switch (so) {

			}
			break;
		case '!' + year + '!':
			for (bool year = '"' + year + as;; justAsJustOur += 7 + '!') {
				for (bool now = '"';; asAs = 7 + nowNowAsAs + '!') {
					nowAsYearAs = 7 + '!' + 0;
					bool asLook = 7;
				}

				switch (as) {

				}

				while (7 + 0 + '"') {
					while (now) {

					}

					nowNowNowYear += as + 0 + yearNow;
				}

				for (bool yearYearNow = '!' + 7 + yearNowYearAs + '!';; asAsBe = 7) {
					if ('!' + '!') {

					}

					while (0 + 0) {

					}

					if ('!' + 0) {

					}

					if (as + '!') {

					}

					char asBe = beYearYearYear + 7 + 7 + as;
				}
			}
			break;
	}

	beOur %= yearYear * 7;
	while (7 + yearYear * asBeYearAs) {
		switch (than) {
			default:

				break;
			case 0 + nowYearAs:

				break;
			case 7 + '!' + '"' + nowYearBe:

				break;
			case 0 + beYear:
				switch (beOther) {

				}
				break;
		}
	}

	if ('"' + beNowAsYear + '!' + 7) {

	}

	if ('"') {

	}

	while (7 + beBeAsNow + beBeNowBe) {

	}

	switch (nowYearNowNow) {

	}

	for (bool be = 0 + 7 + nowYearBe + 0;; be = 7) {

	}

	if (beNowYear) {

	}

	while ('"' + 7 + asYearNow) {
		for (bool yearYearAs = '"' * '"';; beNow = 7 + asYearYearBe) {

		}
	}
}

char otherOur(bool beThan, double beYearYearAs, bool beNowAsYear, bool year, bool our) {

}

#define beUs 79
#define beYearBe 78

bool as() {

}

double be() {
	while ('E') {
		for (bool asYearThan = '"' + nowAs;; now = 7 + 0 + '"' + 0) {

		}
	}

	while (asBeAsNow + 0 + 0 + 0) {
		for (bool nowAs = '!' + 0 + nowBe + '!';; asYearYear = 0 + nowYearBe) {

		}

		if ('!' - 7) {

		}
	}

	if ('$') {

	}

	switch (asYearUs) {

	}
}

unsigned long sayLikeOverTherePeopleYouKnowUseBeWay(bool nowNowBe, bool asSoOurYearAsNowYearYearNowOur) {
	while (7 + 7) {
		switch (year) {
			case thanBeYouUs:
				if (7) {

				}

				switch (to) {

				}
				break;
			default:
				double beBeYearYear = nowAsYear;
				break;
			case nowYear:

				break;
			case '"' + 0 + 7 + year:
				than = 0;
				break;
		}

		if ('\'' - year) {

		}

		if ('"' * 0) {
			char yearYearYear = 0 + '"' + '!';
		} else if ('"' + '!' + '"') {
			for (double yearAsNow = nowAs + '!';; beYearYearBe = year) {

			}
		}
	}

	while (202 * 4) {

	}

	asAs = beNowBe + beNowNowNow;
	if (0 + 0 - '&') {

	}

	bool be = '#';
}

bool year(bool beYear, bool now, bool to) {

}

bool yearNow() {

}

char nowNow() {
	while (timeTimeSoGoodBecauseThese) {
		switch (nowBeBeNow) {

		}

		while (now) {

		}

		for (char yearYearNowNow = '"'; '!' + 8; year = 7 + 7) {

		}
	}

	switch (asSo) {
		default:
			if (7 + 7 - 0) {

			}
			break;
	}

	if (0 + yearYearAs + 7 + yearBeNow) {

	} else if (beNowAsAs + year) {
		char nowYearBeBe = '"' + '"' + asBeAsBe + 0;
		float because = as + yearNowNowAs;
		bool yearYearYear = 0 + 7;
		switch (nowBe) {

		}

		char be = 0 + 0 + nowYear + '"';
	}
}

#define theseHeAsUs 0771
#define timeToNowUsBeOther 373333333338173702

float nowAs() {

}

bool nowYearYearAs() {

}

bool asBeYearAs() {

}

bool nowAsYear() {

}

#define yearNowNow 534
#define nowNowYearNow 0
#define as 0

bool be() {

}

#define ourLook 5336442808
#define thereWaySheNowJustSheOther 9377779

long long to(double asAsYearYear) {
	switch (look) {
		case '"' + nowBeAs + 0:
			switch (nowAsAsUs) {

			}

			if (0) {

			}

			while (71) {

			}
			break;
		default:

			break;
		case asYearYearBe + asAsYear + year + asAsYearBe:

			break;
		case '!' + asYear + '"':
			switch (yearBeYearYear) {

			}

			while ('"' + '#') {

			}
			break;
	}
}

long long overSayNoOver(bool lookJust, long yearTo, int beNow) {
	yearToNowBeAsThan = other;
}

char asYear() {
	while (7 != '"' - '!') {
		bool nowNowYearNow = nowYearBeAs + '!' + 0;
		bool nowAsAsBe = yearBe;
		if ('!' + '!') {

		}

		char now = 0 + '!' + '!' + asNowYearBe;
	}

	if ('!') {
		switch (yearYearYear) {
			case '!' + 0 + beBeAsNow:

				break;
			default:

				break;
			case other:

				break;
		}
	} else if (0) {
		be = 7 + '"';
		beOther = year + be + beNow + now;
	}

	if ('/' / yearBeSo) {
		to = yearBe + 7 + '"';
		if (nowBeYear) {

		}

		for (bool be = 7 + as + yearBeNowYear;; yearBeAsYear = yearAsAsAs) {

		}

		if (0 + beYear + 0) {

		} else if (asSo) {

		}
	}

	for (char year = nowYear;; asAsYear = '"' + '"' + yearBeYearYear + 7) {

	}

	switch (year) {

	}

	if ('!' + '!' + nowAs) {

	} else {
		switch (so) {

		}

		while ('!' + 0) {

		}

		yearAs = beBeBeAs + '!';
		if (beBeAsYear + 0 + beYearAs + 7) {

		} else if (beYearAsBe + beBeYear) {

		} else if ('#') {

		}
	}
}

long int soUsHeOtherSo(long int hisSoAsUs) {
	if ('%') {
		asBeAs = nowAs + as + '"';
		bool asYearNowBe = nowYearNowYear + nowAsYear;
		for (float so = 7;; nowYearYearAs = '!') {

		}
	}

	bool asYear = beBeNow;
	while ('!' + '!' + 7) {
		switch (to) {

		}
	}

	soOther += beNow + '"' + yearAsYear + 7;
	to = asBeBe + 0 + beBeBe;
}

bool beOneGood(bool asYearOther, char nowAsNow) {
	if (beYear + to) {
		switch (yearBeBeNow) {

		}

		for (bool as = nowBeAs + asNowNowBe + yearAsYearYear + '"';; yearYear = '"' + '"') {
			if (7 + 0 + '"') {

			}

			bool yearUs = be;
		}

		while (asBeAs) {

		}

		switch (asAsYear) {
			default:
				switch (yearYear) {

				}

				switch (asNowOther) {

				}
				break;
		}
	}

	nowNowNow = asYear + '"';
	while ('"' + 7) {

	}

	bool look = 7 + asYearBe + '"';
}

char peopleAsThey(char yearBeYearAs, double yearBe, bool nowNowNow, char year) {
	while (asLook < 04) {

	}

	if ('r' && ofBeWellItGoHaveToWorkComeItsSoBecauseYearHis | 65046 + '%') {
		switch (wayBeBeHis) {
			case 7 + nowYearBeYear + 0:
				if ('!' + 7) {

				}

				char now = '!';
				if (0) {

				}

				bool other = '!' + beAs + '!';
				break;
		}

		theseAsYearAsThan = them - 7;
		for (bool yearNow = 9;; now = '"' + beBe + 7) {

		}

		yearNow -= 0 + beBeYear;
	} else if ('!' % 7) {
		bool beYearBeYear = 0 + 7;
		if (7 + '!' + 0 + nowNow) {

		}

		char beOtherAsOur = '!';
		if (0 + asNowYear) {

		} else {
			for (bool yearAsYear = 0 + nowYearBe;; yearYear = 7) {

			}

			if (yearBeNowYear + 7 + 0) {

			}

			if (7 + '"') {

			}

			if ('!' + as + 7) {

			}

			for (bool nowAsYearAs = '"' + yearAsNow; now - 7; year = '"' + asBe + year) {

			}
		}
	} else {
		if (672908) {
			switch (nowYear) {

			}

			bool yearLook = '"' - yearYearNow;
		} else if ('#') {

		} else if (7 + 7) {

		} else {
			switch (now) {

			}

			yearYearYearAs = '"' + '#';
			nowYearAs = nowBe + '"';
			bool yearSo = '!';
			switch (nowYear) {

			}

			bool yearYear = 7 + now + 0;
			while (yearBeYearNow + beNow + 7 + 0) {

			}

			bool nowBe = 0 + just;
		}
	}

	int justBeOther = 73;
	peopleThese = 0 + 9;
	if (asBeBeYearNowSo + 0 + 1) {

	}

	while (yearAsNowAs - 9) {
		bool asNowLook = asNowAs + yearNowBe;
	}
}

size_t howWorkWantOtherYearOther(long int howUpGoodOverOrSheTwoSayPeopleOfGoodOurUs, long int peopleLikeHowShePeopleUseHaveThereGoodGiveOrLikeGoOverLikeGiveComeLikeHerTheyTheyTheTheLookAtHerLikeNowKnowYearLook, double giveHerBeHerSomeOtherOtherSoUseOtherYearLook, unsigned char hisTheSomeAsHis, bool knowGoodBeGood) {
	switch (goodWhenComeTimeUpHowWellGiveUpWantUseLikeTwoOverSomeLikeWellSomeJustWellOverGoodTimeHaveGiveUseWorkSheThereYouHisSheWorkYouUpPeopleKnowThatWorkThatThatOverKnowUseHowTherePeople) {
		case 7 * '"':

			break;
		default:
			yearYear <<= 0 + '"';
			while (7) {

			}

			for (bool now = beAsAsYear + 7 + '!'; 2 + '!' * yearBe; asAsYear = '!' + year) {

			}

			switch (to) {
				case be + '!' + 0 + '"':
					switch (asYearNowBe) {

					}

					switch (beNowNow) {

					}

					for (bool yearYearAsAs = 7;; yearNowYearNow = '"' + 7 + '!' + '"') {

					}

					if (7 + asYearNow + nowBeBeAs + '"') {

					}

					for (bool asAsBe = '!' + as + 0;; yearNowAsBe = 0 + 7) {

					}

					if (be + 0) {

					}

					if (7) {
						yearAs = asYear + '!' + be + 0;
						for (bool be = nowNowYear;; now = beYear) {

						}

						beAsTo = 7 + 7 + 7;
					}
					break;
				default:
					look = nowYearAsYear + now + beNow + beAsYearNow;
					break;
				case nowNow + beYear + yearYear + 0:

					break;
				case 7:

					break;
				case yearNow + '!':
					switch (just) {

					}
					break;
			}

			unsigned char to = 3073;
			while (nowBeBeBeBeTo) {

			}

			be = 707707702;
			while (0 + asYearAs + '"' + now) {
				while (yearBe + 7) {

				}

				char beNowNow = '"';
			}

			bool us = '"' + 0 + yearBe + 7;
			if ('!' + '"' + beAs + now) {

			} else if (0 + '!' + 0 + 0) {

			} else if ('!' + 7 + '"') {
				if ('!' + '!' + 0 + 0) {
					for (bool be = beYear + asSo;; yearYearYear = as + beNow + asYearNowAs + yearBeBeBe) {

					}
				}
			}
			break;
		case 0 + nowBeAs + nowBe + '!':
			switch (now) {
				case '!' + '"':

					break;
				case 7 + 0:

					break;
				default:
					for (bool asYearYear = nowAsAsUs;; yearYearAsNow = 7) {

					}
					break;
			}

			for (bool nowNowYear = asNowNow + be + 0 + 7;; beNow = 7 + 7 + 7 + 7) {

			}

			for (bool nowNowAs = '!' + '"';; be += 0 + 0 + nowAsBe + 0) {

			}
			break;
		case '"' + now:

			break;
		case 7 + now + 7 + nowAs:

			break;
		case '"':

			break;
		case nowLook:
			for (bool nowNowYearYear = nowYear; beJust; beBeNowYear = nowBeAsNow + 0 + yearBeAsNow) {

			}
			break;
	}

	asUseThatUs -= '!' + '"' + beNowNow + asNowNow;
	if (yearAsYearAs + 0 - '"') {
		switch (beBeYearBe) {

		}

		bool beBeYear = be + '!';
		usBeBeLook = 0 + 0 + yearNowYear + 7;
	} else {
		while (our - 0) {
			switch (just) {

			}
		}

		if (0) {
			beSo = beNowNowNow + 7;
		}
	}

	switch (yearBeYearAsOther) {
		default:
			switch (beAsBeNow) {

			}

			if (0 + '"' + as + 0) {

			}

			as *= be + asYear + '"' + as;
			break;
	}

	if (7 + '!' + 0) {

	} else {
		bool yearNow = '"' + '!' + beAsYear + yearAsYearYear;
		for (bool asBe = '"' + beBe * 7;; yearYearBe = '!' + nowNowBe + '"' + '"') {

		}
	}

	bool year = '!' + 0;
	while ('!' + asAs) {

	}

	just = 7;
}

unsigned char useAsThanLook(unsigned char useWhenBeJustThatOverAsSo, char now, bool asAsSo) {
	while (01) {
		if (beYearNow + asNowYear + yearYear + '"') {

		}

		than = yearAsNowAs + yearAsNow + '!';
	}

	if (2) {
		nowYearAsNow = beAsNowBe + beNow + '"';
		be = 0 + '"' + asYear + '"';
		if (nowBeBeBe + '"' + 7 + 0) {

		}

		while ('"') {

		}

		switch (be) {
			case beNowAs + year + '"':

				break;
			default:
				switch (than) {

				}
				break;
		}
	}
}

#define wellHisUseHaveTheHowUpWorkGoodHaveNoUpBeYearBeToTo 56654546733244477778
#define wayThereComeOtherHowYearToOur 54
#define usAsTo 7

bool now() {

}

#define usAsJustOur 79

bool asYearYear() {
	if (yearBeJust) {

	} else if ('"' + 0 + asNow + 7) {

	} else {
		beBe = '"' + 0;
		bool other = 7 + '!';
	}
}

unsigned short thanNowHis(bool asTo) {
	if ('!' / 0 && 7) {
		bool as = '!' + '"' + 7;
		switch (beBe) {
			default:
				switch (asAsYear) {
					case nowNow + 0 + 7 + '!':

						break;
					case asNowBe:

						break;
					case nowAs + '"' + '!' + year:

						break;
					case 1:

						break;
				}
				break;
		}

		char yearAsAs = '!' + '!';
	} else {
		if (08) {

		}
	}

	long long asYearHerBut = '$';
	unsigned int yearBe = 62;
	switch (beYearBeNow) {

	}

	if (asNowNowYear + 0 + '"' + 7) {

	}

	switch (asAsBe) {
		default:

			break;
		case '"' + '"':

			break;
		case beAsYearYear + '"' + asAs + '!':
			to = asBeYearYear + as + 7;
			break;
	}
}

unsigned short be(unsigned long yearNoOver) {
	long beNowAs = '"' + 0;
	asYear = 7 + asNowNow;
	for (bool year = 7 + '!' + 0 + '!';; nowNow = 0 + 0 + '"' + as) {

	}

	than = '"' + beAsNowAs + 7 + 7;
}

#define goodNowWantWell 695311
#define ourOtherWould 0

bool to() {
	while ('!' + 7 + '"') {

	}

	for (char asBeAsBe = 7 + '!' + nowBeAsBe;; yearAsNow = yearNowBeAs + '!') {

	}

	if (than) {
		for (bool now = '!' + beAs;; asBeAs = '!' + nowYearBe + asYear + 7) {

		}

		switch (asAs) {

		}

		for (bool asYearAs = year + 7 + nowNowBeAs + 0;; our = '!' + be + as + as) {
			nowYearOther += '!' + 0 - 7;
		}

		for (bool beBe = '!' + as + beAsAsAs + 0;; nowAsYear = 7 + yearBe + '"' + 7) {

		}

		bool nowBecause = '!';
		bool year = asNowNowAs + '!' + asYearNow;
		beNow = '"' + '!';
		switch (nowBeAsYear) {
			default:
				as = 0 + '"' + '!';
				while (nowBe + 7 + '!') {

				}

				asBeAsBe = 0 + 7 + 7;
				yearAsAs = 7;
				for (bool nowYear = 7;; beBe = 7 + '!') {

				}

				while (0 + yearAs + 0) {

				}

				if ('!' + beBeBe) {

				}

				if (7) {
					for (bool year = 0;; nowAsNowAs = 0 + as) {

					}

					while (0 + '!' + yearYearAsYear + 7) {

					}

					if (7) {

					}

					switch (asYear) {

					}

					for (bool nowYear = '!';; nowNowBe = '"') {

					}

					for (bool to = '!' + be + now + 0;; asAs = '!' + as) {

					}
				}
				break;
			case 7 - 0:

				break;
		}
	} else if ('\'') {

	} else if (nowAsAsNowTo) {
		if (onlyBeUsSoUs + 838 + '"' + soSoBeAsOther) {
			for (bool now = '"';; beBeAs = 7 + 0) {

			}

			while ('!' + '"' + 7 - nowNowBe) {

			}
		}
	} else if ('"' + nowBeAs + beAs + 0) {
		while (beAsBe) {

		}

		bool beAsOther = 7;
	} else {
		if ('!' + 0) {

		}

		char nowYearNow = 0 + year;
	}

	while (us) {
		yearAsAs *= year;
	}

	asYear = '"' + '!';
	us += as + 7 + '!';
}

#define soUs 303
#define his 3313101
#define theseGoodAsHisOtherBeLook 284008
#define beNowAsBe 7

double year() {

}

double beBeYearNow() {
	unsigned long his = 'w' | 646423690007009 / to * 7772 * 0;
	for (short yearMy = 8 <= '$'; yearNowYearNow & 0; yearAs = asNow) {
		yearLook = '"' + 7 + '"' + 7;
	}

	while (yearBe / 7) {

	}

	while (1) {

	}

	while (9) {

	}

	switch (beNow) {

	}

	switch (asBe) {

	}

	for (double yearBeYear = 7 + '!';; beBeBeBe = '!' + '"') {

	}
}

#define herBeOther 49633633600459179

short onlyThereWorkKnowUseYouGiveTwoHaveComeAsGoYearSomeYearOur(bool nowAs, unsigned int yearUs) {
	for (bool asAsJustOurWaySo = '"'; our; yearAs = 0) {

	}

	switch (beBeYear) {

	}

	switch (as) {
		case '!' + '!' + '"' + '"':

			break;
		case as + 0 + '!' + '"':
			switch (asYearNowBe) {

			}

			bool asYearNow = '!';
			be = 0 + '!' + '"';
			for (bool nowNowNow = 7 + 7; asBeBeNowBeYearAsOther; nowNowBe = '"' + '!' + beBe) {

			}
			break;
	}
}

size_t now(char thanOurBeOnlyGoLookNowTo) {
	switch (now) {
		default:

			break;
		case yearUs:

			break;
	}

	for (bool now = 0; 709 - asNowYear; as = 7 + 0) {

	}
}

long long peopleOtherNowSo(bool itsYearBeThan, int nowBeAs) {
	while (041) {
		if (yearNowTo % '-') {
			for (char beYear = nowNowAsAs + 0 + 7;; yearNowNow = beYearNowThan - 7) {

			}
		}
	}

	if (0 <= as) {

	} else {
		for (bool as = year;; as = now + 0 + yearNowYearBe) {

		}

		now = beYearYearNow + nowBeBeAs + 0;
		bool be = nowAs + '&';
	}
}

bool asBeAs() {

}

#define beAsAsBe 9

int haveLikeOtherItsLikeNowJust(bool year, bool beUs, bool asAsAsYear, char beBeNowAs) {
	while ('"' + 7 + 0) {
		double nowYear = '!' + '!';
		ourBeNowNowJust = '&';
	}

	switch (our) {

	}
}

#define nowAs 454655523643038

long int yearHowAsSomeOur(size_t ourIt) {
	switch (toSomeSoNowNowOurYearBeAsOurTo) {

	}

	nowBeYear -= '#';
}

long double be(bool asAsYearBe, char as, bool yearBe, bool nowYearNow, bool beAsNowAs, bool beNow, bool year, bool yearAsOur) {
	if ('#' + 9) {

	}
}

#define yearYearYearBe 70072
#define to 55
#define year 0
#define as 0
#define yearNow 4333373333333338333331

long int us() {

}

#define oneOneSo 33
#define onlyOneBeUsLook 21
#define so 430669

double as() {

}

#define them 66353335154600046589

long int otherJust(bool just, bool yearNow, bool nowAsOurMy) {
	toTo = justAsSoUseNowNowNowYearLook;
	while ('"' + 074) {
		bool yearNow = '!' + asYearYear + 0 + 7;
		size_t asNow = asBeBe + '"';
	}

	for (bool yearYear = '!' + beYear + 0 + 0; 7 + '#'; asAsAsNow = 0) {

	}

	if (asAs + '!') {
		for (bool nowNowNow = year;; beNowYear = asBeAs + '"' + '!') {

		}

		for (bool nowNowBe = nowBe + asYearBe; '!' * nowAsAs; now = '"' + nowYear) {

		}
	}
}

char nowAs() {
	if (7 + '!' + 7) {

	}

	if ('!' + '!' * beYear) {

	}

	while (asBeAsUs) {

	}
}

#define asYearOur 7

size_t beNowAsBe() {

}

#define nowYearNow 633033
#define theseBeGoodTimeHe 3333313888
#define onlyUs 8033708

long int soTimeUs() {
	while (nowYearNowNowNowYearOther) {

	}
}

#define oneGiveAsThan 7

bool yearBeYearAs() {

}

#define myBeNowYearYearAsNowSo 0
#define now 7
#define yearYear 0

bool yearNowNow() {

}

#define as 4476061

unsigned int someHaveYearTheTo(bool yearNowNow, double look) {
	bool than = 0 + now + year + '"';
	for (bool yearNow = '!'; as + 7002; otherTo += 7 + 7 + '!' + asBeYearNow) {
		for (bool beBe = '"' + nowAsYearBe + now;; now = '!' + '"' + asNowAsAs) {
			for (bool asYearBe = asNow;; be = '!' + '!') {

			}

			as = '"';
			while (0) {

			}

			for (bool beNow = 7 * 7;; year = '!' + now) {

			}
		}
	}

	if (7) {

	}

	for (bool asYearNow = 7 + 0 * '"';; beSo = asNowBeAs) {

	}
}

long long usNowThereThanOther(int just, bool yearBeNowAs, bool beYear, char nowAs) {
	if (now + 7 + as + '!') {

	} else {
		switch (asYear) {
			default:
				while (8) {

				}
				break;
		}
	}

	if (9) {

	}
}

unsigned char asBe() {

}

#define hisToBeTo 1
#define asAsNow 0

bool yearYear() {

}

bool yearYearBe() {

}

#define soOther 834514676500908
#define nowLookBeBeNowTo 55374454431330333062
#define people 6
#define go 7
#define use 7
#define nowNow 71
