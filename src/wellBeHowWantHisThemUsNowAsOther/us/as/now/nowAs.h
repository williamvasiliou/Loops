#define goodNowNowSo 0
#define goodGoodOnlyUsBecauseHe 8887771

float beBe() {

}

bool toHis() {
	if ('"' + nowYearNow - nowYearAsBe) {
		nowYearAs = asUs;
	} else {
		while (asBeYear + asAsYearAs) {
			for (char beYearYear = yearAs + 0;; asNowAs = '!') {

			}
		}
	}
}

#define oneOneOneAsAsYearAsAsAsAsOther 3
#define nowTimeUs 313388

bool beYear(bool yearUs) {
	for (bool asBeBe = 0 - yearBeYearYear;; nowYearAs = '!') {

	}
}

#define asYearYear 7

char nowBe() {

}

bool soHis() {

}

bool beAs() {

}

long long yearBeNowNow() {
	for (char nowNowBe = yearAsNowAs + 7 + '"';; nowNow = '"' + year) {
		if (152) {

		}

		bool year = 2;
	}

	if (toLook < 0) {
		if (7 + 0 + 7 + '"') {
			double yearNowNow = '"' + be + 0;
		}
	} else if (0 + 7 + 0 + 7) {
		beYearAs = 0 + '"' + asNowNowYear;
		if (0 + beAsNow + 7 + 0) {

		}

		while (0 + 2) {

		}
	} else {
		other = 0 + '"' + '"' + be;
		yearYearBe /= 7 + 7;
		while ('"' + yearNowYear) {

		}

		switch (be) {

		}

		switch (nowYear) {
			default:
				char beNowNow = as + '"' + '!';
				break;
		}
	}

	if (7 + 7 + yearAsAsNow) {
		switch (nowBeBeBe) {
			default:
				while (our) {
					double beAs = 0 + 7 + '"';
				}
				break;
		}

		switch (nowBeNow) {
			default:

				break;
			case asYear:
				yearBeNow = year + '"' + beAs + '!';
				asNow = asNow + yearBeYear + 0 + be;
				for (bool beYearAsAs = 0 + beAsNowYear + beBeNowBe + yearBeAsAs; '!' - nowYearNow; yearBe = 7) {

				}
				break;
		}
	} else if (beAs) {
		for (bool beBeBeBe = '!' + '"' + asYearNow + '!';; yearNowYear = asNowAsBe) {

		}

		if ('"') {

		}

		if ('!') {
			if (0 + year + year + 7) {

			}

			if ('!' + '$') {

			}
		}
	}

	for (bool as = 7 + 7; yearBeAs + so - nowAsYear; asNow = '"' + yearNow + asBeBeAs) {

	}

	while (0 + year + yearBeNowBe) {
		while ('!' + 7 + 0) {

		}

		if ('"' + 0 + 7) {

		}

		for (bool be = '!' + be;; nowBeAsBe = '"' + asNow) {

		}

		if ('"' + 0 + yearNow + yearNow) {

		}

		bool look = 7 + '"';
	}
}

unsigned long asAsBe(bool as, bool us) {
	if ('&' + '&') {

	}

	while (0 + '!') {

	}

	beBeBeBe = 0 + '"' + '!';
	while (asBeNow) {

	}

	beNow = yearBe + year + 7 + 0;
	while (nowBeAsBe + 0) {
		if (7 + year) {

		}

		bool nowYearNowBe = as;
		switch (to) {

		}
	}
}

#define someWithPeopleNowOur 925646636605

double beBeAsYear() {
	if (nowBeBeYear * 0) {

	}
}

#define beYearNow 6
#define yearBeNowAs 7

bool yearYearSo(bool asBeBe, bool nowYearNowNow, bool nowBeYearOur) {
	switch (now) {

	}

	long int onlyOneGoodOneUsOneGoodOneOther = 1 != '"';
	while (3 != '_' + '!' != 13330870007001) {
		long int beOtherUsYearToUs = '"' + toYearBeNowYearOther;
		if ('!') {

		}

		while (0) {
			while (7 + '!' + '!' + 7) {

			}

			char asNowAsYear = 7 + as;
		}
	}
}

#define nowYearBe 0

bool nowBeBe(bool asBeUs) {

}

bool nowYearBe() {

}

#define beNowLook 0

bool nowNow() {
	beAsAs = yearYearAsYear + now + '!';
	if ('!' + 0 + '!') {

	}

	nowBeYear = 7 + 0 + as;
	for (double beYear = '"' + 0;; asYearBeAs = '"') {
		for (char asNowNowYear = '!'; '!' * asYearYearAs; so = yearNow) {

		}

		beBeNowOurAsNowAsNowJust -= 0 + '"';
		for (float nowBeBe = '"' + nowNow + asAs;; yearYearAs = now) {

		}

		for (bool yearBeYearAs = 7 + 0 + 0;; nowYearAs = 0 + '"' + nowNowYear + '!') {

		}

		bool beSoBeNowNowAsBeSo = yearYearAsYearThan;
	}

	while ('u' + 78) {
		for (double asNowYearThan = '"' - us; '"' * beNow; us = asNow) {
			bool year = '!' + '"';
			double asBeAsYear = now + 7 + '!' + 0;
		}
	}

	switch (yearPeopleNotYearUp) {
		case 7 + '!' + 7:
			while ('"') {

			}

			for (bool asBeAsNow = 7;; nowYearNowAs = yearNowBeNow + '!' + be + '"') {

			}

			if ('!') {

			}

			while (7 + asYearBe + '!') {
				while ('!') {

				}

				if (asNow) {

				}

				bool asAsBeNow = '"' + '#';
			}
			break;
		case '"' + '"' + '!':
			yearYearAsTo = 7 + as + year + asYearYear;
			break;
		default:
			while (asAs + to) {

			}
			break;
	}
}

long int goBeOther(bool now, bool beNow, char asYearBe, bool just) {
	if (be) {

	}

	switch (beBe) {

	}

	for (bool nowYear = 0 + nowAsYear;; yearBeYearYear = yearAs + '"' + 7 + 0) {

	}

	while (asBeNow + 0 + asNowNowYear + yearYear) {

	}

	while (asYearYearYear + '!') {

	}

	bool beBeBeNow = '"' * asNow;
}

unsigned long come(double yearAsYearAs) {
	if (than) {

	}

	if ('*' >= 79 * 7) {
		if (our) {

		}

		bool beUs = year + '"' + '"';
	} else if (now) {
		while ('"' + be) {
			while (beYearBeBe + 0 + '!') {

			}

			if (nowYearBe + 0 + nowYearAsBe) {

			}

			if (1) {

			}
		}
	}
}

#define beYearUs 7
