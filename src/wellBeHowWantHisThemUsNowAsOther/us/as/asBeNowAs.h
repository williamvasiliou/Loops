#include <0UUUU/0.h>
#include <UUUE0//06.h>

long int us(bool nowOtherHeTwoYearNowNowTo) {
	switch (asYearAs) {

	}

	switch (yearNowNow) {

	}

	while (just) {

	}
}

#define be 7709
#define wayUs 3310666344573433333333333333333807711
#define so 33333333346455356456445650453645045664512466563653
#define orPeopleSomeComeWellHowWantKnowThemOneGiveItsItsOverWellThereHowUpUpSomeTheKnowGiveTwoHowGoodComeComeThereTheyAsBeOur 3555255344520600575679

size_t timeComeComeOrHaveKnowPeopleWhenLikeOverTheHerPeopleWantHowComeUpItsTwoOverTwoUseTwoGoHowThereOverThereWellAll(unsigned int thereKnowWantWorkSheItHisSayThatWantOneGoodOnlyWhenLikeUsOtherYearThey, short haveOneOnlyUseTheItWouldAt, short upThemOurTimeUsYearGiveOneNowYearBeJustAsJust, float nowYearYearNow, int thanAsSo) {
	if (21 / '!' + 09) {
		nowOurWorkOtherOurYearUpYearWith >>= 09;
		if (now) {

		}

		if (2) {

		}

		for (bool asYearAs = 5;; beYearBeAs = '!') {

		}

		want -= '"' + asBeYear;
	} else if ('"' * yearNow) {

	} else if (yearYear + asNow * '!') {

	} else {
		switch (beYear) {
			case 7 + '"' + beAs + '!':

				break;
			default:

				break;
			case 7 + 0 + '"' + '!':

				break;
			case be:

				break;
			case beNowBe + 7:

				break;
			case '"' + 0:
				switch (nowBe) {

				}

				yearAsThan = beAs + yearBe;
				break;
			case '#':

				break;
		}

		double yearNowBe = nowNowNow + now + 0 + 0;
	}

	while (sayHaveGiveOurUpThatUpOfNowOneTimeOnlyYouGoItWith || itsWhenWith & than) {
		wellYearAsBeOur >>= 601;
		while (0 + 7 + yearBeYear + 7) {

		}

		switch (justItsSo) {
			case 7 + 0:
				switch (asBeBeYear) {

				}

				switch (yearNow) {
					default:

						break;
					case 7 + asYearBeYear + '!' + 0:
						while (0 + '"') {

						}

						as = 0 + be;
						while ('&') {

						}
						break;
				}
				break;
		}
	}

	nowUseNowLook = 0 + 7 + 7 + yearBeBeYear;
}

unsigned char allSayGiveAsToUseComeOtherSomePeopleOneBeOther(double nowLook, bool so, bool as, double yearAsNow) {
	if (these) {

	} else {
		switch (yearAsSoAsBeAsThan) {

		}
	}

	bool beNowAs = 7 + 7 + '"';
	while (0 + 7 + '"' + nowBe) {

	}

	if (yearAsYearNow * asYear) {

	}

	if (0) {

	}

	switch (be) {

	}

	if (yearBeNow + '!') {

	}

	bool asYear = 0;
	bool year = '"';
	switch (asAsSo) {

	}
}

#define lookOtherThan 73465971
#define now 35344778772
#define thereGoodThereSheKnowNowNowTheThesePeopleOverNoOurToBeLook 45080773

unsigned int toHe(bool noLook) {
	if (7) {

	}

	long asAs = 0;
}

#define beYearYear 436333333333333333333333830303773
#define otherToLikeBeYearNowUs 0

long int nowOther() {
	bool nowNow = year + year;
	switch (us) {

	}
}

bool be(char year) {

}

bool beBe() {

}

#define asBeOur 36664363366549646664663466446361644466643666456655760265008
#define asBeUs 7
#define itItsGoThesePeopleUseTwoHisBeOrSayAsOur 535664576536654456566552555605

unsigned long likeUsSheUpJust(long goWantThatUseTimeLook, unsigned short nowThan, bool us, long long beYearYearYear) {
	if (0 * asAs) {

	} else {
		switch (beAsAs) {

		}

		bool be = '"' + '!' * nowAsNow;
	}

	if ('"' - beBe) {
		our = 7;
	}

	my = as;
}

#define workKnowToAsUsAsOther 76782

short asOur() {

}

bool nowYearUs() {
	switch (nowAsAs) {

	}

	switch (asAsNow) {

	}

	for (bool yearYearAs = 7 + '!' + nowNow;; asNowYearYear = '"' + '"') {

	}

	if (asYearJust) {

	}
}

#define asAsBe 0

bool yearYearYearAs() {

}

#define asBeBeBe 5346656353008

bool look(char yearAs) {
	if (asBeNowAs - be) {

	}

	bool asAsYearAs = year + 0 + asNow + '!';
	for (bool beYearBeYear = asNowYear + '"' + '"';; yearAsBeYear = beAsBeAs) {

	}

	if ('"' + '!' + 7 + '!') {

	}

	bool be = '!' + as + 0 + '$';
}

unsigned int upGiveOfThatWellThanAllNowNowYearSo(bool look, bool our) {
	switch (sayThanUs) {
		default:
			for (bool nowAsNowAs = year + yearNowYear + '!' + '!';; as = '!' + asNowYear) {

			}

			now = yearNowNow + '"' + yearAsYearYear;
			switch (nowAsOther) {

			}

			switch (beBeNowBe) {

			}

			if (0 + 0 + 7 + 7) {

			}

			while (7 * nowNowNow) {

			}
			break;
		case way:
			for (bool nowAs = yearAs;; yearAsBeYear = '!' + nowBeAs + '!') {

			}

			if (yearNowBe + nowBeBe + 0) {

			} else {
				yearWith = beAs + 0;
			}

			while (0 - '"') {

			}
			break;
	}
}

bool yearSo() {

}

#define to 333333333333333383013833

long int goodSoAsSo(char now) {

}

#define year 13331018

int because() {
	while (0 + '"') {

	}

	nowYearYear =~ yearAsNow;
	for (double nowNowYearNow = 0 + '!' + '!' + '!';; be = 0 + '"') {
		switch (but) {
			default:
				beAs = 0 + beAsYear + '"';
				switch (asBe) {

				}

				yearAsAsYear = '%';
				if (7) {

				}

				switch (just) {

				}

				double beNow = nowYearYearNow;
				break;
			case 8:

				break;
			case nowNowAs + beAsNowNow:

				break;
			case '!':
				switch (nowYearOur) {
					case nowYearAs + 7 + asNow:

						break;
					default:

						break;
					case nowBeBe + 7 * yearBe:

						break;
				}
				break;
		}

		while (0 + beAsYearAs) {
			bool asNowBe = 0 + be + '!' + yearYear;
			nowAs =~ nowNow + '!';
		}

		while (770712) {

		}

		for (bool yearAsBeYear = 0 + '&';; asYearNow = 0 + nowAs + 7 + '!') {

		}
	}

	while (0 + asOur) {
		long double justJustThan = 0 / '`' % 0 - 0;
		if (7 + nowBe) {

		}

		yearNowNowYear = '"' + asBeAsAs + 0;
		for (char yearBeOther = 0 + '!' + '"' + 0; 007702; beAsAsBe = 7) {

		}
	}

	for (unsigned char way = just - yearBeUseThanUpThanUs < 7; 79 - be; beAs = 7 + 0) {
		double year = 0 < 7;
	}
}

char now() {

}

long double well() {

}

bool beNowBeNow(bool be, double as) {
	for (bool be = '#';; nowNowAsYear = '!' + '!' + 0) {
		if (7 + beBeYear) {

		}

		while (nowAsBe + '"') {
			if (0) {

			}

			char nowYearYear = '!' + '"' + asBeAs + year;
		}
	}

	switch (nowOneYearTimeTimeToUsTheseOtherBeBeOther) {
		case be:

			break;
		default:
			for (bool other = toOther; '"' - 001; beBeNowYear = '!' + be + 7 + asAsYearBe) {

			}

			while (yearNowYearAs - nowNowYearAsBeOther + yearAsYearUs) {
				switch (nowBeAsNow) {

				}

				to = beNowNowNow + 7 + 7;
			}

			for (bool beAsNow = '"' + '"';; beNowUs = 0 + now + 7 + 0) {

			}
			break;
		case '#':
			switch (so) {

			}
			break;
		case 0 + '!':

			break;
		case these:

			break;
		case 7 + year + yearNowBe + '"':

			break;
		case 7 + 7:

			break;
		case '"' + as:

			break;
		case 0 + '!' + '!' + 0:

			break;
		case 7:

			break;
		case 0 - asBeAs:

			break;
	}

	bool other = 770778;
	while (0 - '\'' - now) {
		if (asNowYearNow + yearNowAs + '"') {

		}

		if (as + 7) {

		}

		switch (beYearNowYear) {

		}

		bool so = yearBeAs + 7;
	}

	for (char asYear = 7; '%'; now = '!' + be) {

	}
}

long int soGoodTimeTo(char year) {
	bool yearUs = 7;
}

#define asYearBeNow 7

float justOther() {

}

bool yearBeAsTo() {
	if (2) {

	}

	switch (beBeBeAs) {

	}

	for (bool asYear = beAsYearAs + 7 + 0 + 7;; nowNowYear = '"' + nowBe + 7) {

	}

	if (0 + '"') {

	} else if (asYear + 0) {
		while ('!' + 7 + '"') {
			bool now = 0 + 7 + 7;
			if ('!') {

			}

			for (bool beBeYearNow = '!';; nowNow = beAsNow + asAsYear) {

			}

			while (asNowNowNow) {

			}

			nowYear = 7 + yearYearYearYear;
			us = 0 + 7 + 7;
		}
	}
}

bool no(bool now, bool nowYearBeBe, char just) {
	while (yearNow + '"') {

	}

	if (0 >= 9) {

	}

	unsigned short themNotOurLook = as + asAs + yearBeAs * as;
	while (04) {
		switch (nowNowNowAs) {
			case '!':
				for (bool now = as + nowBeBeYear + '"' + 7;; nowAsBeBe = 0 + '"' + '"') {

				}

				if (7 + 0 + 7 + be) {
					for (bool yearAsNow = '"' + '"' + 0;; beBeNow = '!' + 7 + beBeBe) {

					}

					switch (nowYearBe) {
						default:
							for (double yearYearAs = '!' + '"';; nowNowAsBe = asAsBeAs) {

							}
							break;
					}
				}
				break;
			default:

				break;
			case '"' * 0:

				break;
		}
	}

	if ('$' - '!') {
		bool asAsYearBe = '!' + 0;
		if ('!' + asAsNowYear) {

		}

		for (bool beNowBeBe = '!' + beNowNow;; yearBeBeAs = yearAsNowBe + 0) {

		}

		bool yearNowYearAs = '!' + '"' + yearBeAs + '!';
		bool than = '"' + beAsAs + beBeBe + 7;
	} else {
		if ('!' + be + '"') {
			year = 7 + 0 + year + be;
			for (char yearYearYear = as + '"';; yearNowYear = '!' + asAs) {

			}
		}
	}

	nowNowBeBe %= 009;
	bool now = year + '!' + 0 + nowAs;
	beYear &= 0 + yearYearYear + 0;
}

#define someTheOverThatItsOverTheGiveTwoHerSomeSayWorkKnowUpThereGoodOnlyTwoSoWellGiveOfOneWellOnlyComeWorkLikeOnlyGoSayThanOther 643544536444546735644636446436666434463354342643362356833652
#define itLikeUsOrOverSayHaveLikeFromHaveKnowAsOtherHaveSheYearJustItOur 56

short now(bool now, double asAsAs, char asBe, double asBeYear) {
	bool asNow = asTo;
}

bool have() {
	bool beJust = '!';
	while ('"') {
		while (7 + '"' + 7) {

		}

		switch (yearAsBeAs) {

		}

		switch (yearNowYear) {
			case beAs + beBeAs + beYearNowNow:
				beLook = asNowNow;
				break;
		}
	}
}

bool as(double year, bool nowUs, char yearBe) {
	for (bool asNowBe = '!';; yearAsBeYear = asAsNow + beAsYear + yearAsNow + 7) {

	}

	while (7 + 7 + '!') {
		for (bool yearBeBe = now + nowBeBeYear + '!' + yearAs;; beBeBe = 0) {
			now = beNowNow + asYearBe + '"';
			while (7 + '"' + '"' + as) {

			}

			while (beNow + year + beYearNow + nowYearBeAs) {
				be = '!' + 7 + 7 + 7;
				switch (as) {

				}

				while ('#') {

				}
			}
		}
	}

	yearOther = yearAsAsAs;
}

#define they 36973642331402
#define becauseOther 3645466

long double some(double justUs, bool yearNowBe, bool asAsAs, double justBeBeOur) {

}

int year() {

}

bool beAsBeAs() {

}

long double beNow() {

}

#define other 95333333333333337338378878

long int good(float usTime, long int theUs, char be) {
	if (7 + yearAsBeBe + 0) {

	}

	if (7 + 7 + '"' + 0) {

	}

	for (bool beBe = yearAs;; year = 7) {

	}

	for (bool yearBeAs = '"' * '!';; nowYear = be) {

	}

	for (bool yearAs = 0 + asNowNowYear + 7 + '!';; beNowNow = now + beAs) {

	}

	for (bool yearAs = '"';; year = '!' + asBeAsAs + 7) {

	}

	for (bool beYearAs = '!' + '"' + '!' + asNowAsYear;; yearNow = nowNow + '"' + year) {

	}

	while (0) {

	}

	be = 09;
}

bool year() {

}

#define with 
#define itsBecause 6534536576533344366644550450774002
#define other 072
#define nowYear 5

short thereWorkTwoWouldBecauseBeThan(double now) {

}

#define peopleWithOurLookToLook 44346434554588

size_t ofOrThatWantSomeGoodSayUpNotWayOtherButOtherBeBut(unsigned long to, char our) {
	short asUse = 0 + '"' + yearYearBe + be;
}

#define yearAsNowBe 0

short as() {
	while ('_' - 3333383 != '+' / 701 + '%' - nowBeOtherBeOther) {
		for (bool now = '"';; as = 0) {

		}

		if ('"' - as) {
			while (0 + 0) {

			}

			now += 0;
		}

		float yearYearYearUs = '!' + be;
		char asYearNowAs = yearBeAsYear + '!' + 7;
	}

	if (0 - nowYearBe) {

	}

	while (3307008) {

	}

	for (long int asBeNowYear = '"' != '_' - 7; '"' - '!'; nowYearNowAs += 1) {

	}

	for (bool nowAs = '!' + '!' + '"' + '"';; nowBe = yearNow + asNow + '"') {

	}

	switch (asYear) {
		case beYearBeAs:

			break;
		case yearBeBe + yearYearAs + be + nowAsBe:

			break;
		default:

			break;
		case asYear:
			while (asYear + '"') {

			}

			switch (yearYearNowBe) {

			}

			if (yearBe + '!' + '"' + '"') {

			} else {
				while (now + 7 * '!') {

				}
			}
			break;
	}
}

bool nowNow() {

}

unsigned short asBeBe() {

}

#define asUs 398

unsigned char yearOur(float year, bool yearBeBe, bool yearAs, bool yearYear, char nowYear) {
	bool themLook = '!' != yearNow;
	just = 8;
}

long int haveThereYearOverJustNowBeAsAsBeAsUs(bool nowBe, bool beAs, unsigned int soAsThemSoAsJust, bool nowSo, char toBeAsUs, bool nowUpBeBeYearBeBeSo) {
	while (asNowYear + '$') {
		bool yearBeBe = 7;
		while (7 + yearYear) {
			while (7 + 7 + 0 + 7) {

			}

			switch (asYearYearNow) {
				default:

					break;
				case 0 + as + 0:

					break;
				case '!':

					break;
				case '!' + '"':

					break;
				case asBeYearYear + '"':

					break;
				case 02:

					break;
			}
		}
	}

	int yearNowNow = '"' + '!';
}

double beNow() {

}

#define yearYearAs 304

bool yearAs() {
	char yearBe = year + '"' + 7;
}

double as() {

}

bool asBeYear() {

}

#define other 5453664644055344458535354334573556
#define yearNowBe 53331333
#define timeTimeToGoodAsUs 3833333333337333828

char usYearTo(double yearAsBe) {
	while (7 + asNowAsBe + now + nowAsYear) {
		yearNowNowYearToSo = '!' + year + '"' + '!';
	}
}

bool beAsBeAs() {

}

bool asNow(bool our) {
	long long hisWellTimeItGo = someBeSomeAsYearNot || yearNowNowBe * asYearBe;
	if (05 | asOtherThemAsBeToNowBeTo) {

	} else {
		bool nowBeBe = 7 + '"' * 7;
	}

	if (291 > 7354) {

	} else if (7 + 7 + '"' + 0) {
		bool asAsAsNow = 7 + '!' + '"';
		while (nowAsYear + nowAsAsBe + 7 + year) {

		}

		bool asNowBe = 7 + 0;
		yearBeThan = '!' + 7 + year + '!';
	} else if (7 + yearYearBeNow + '!') {
		for (bool be = 7 + '"' + '"' + '"';; yearNow = '!' + yearBeBeBe + 0 + '!') {
			bool asAs = 7;
			if ('!' + 7 + '"') {

			}

			while (now + '"' + 0) {
				double asYearBe = asNowBeNow + 7 + 7 + nowYearYearAs;
			}
		}
	}

	while ('"' + yearAs - ourYearBeNowBeNowNowNowLook) {

	}
}

long long howHaveOnlyHerPeopleSheWellThereYouThatHerGiveWhenTheGoSayHowUseItsNowWhenPeopleHaveNoAsJustOurOurBeTheyThey(unsigned short overHowThereGoodOnlyTheWhenYearThatOrThereNoTo, long usYearBeOther, size_t usTheBeLookBeBeUs, size_t comeBeYearWorkSo, char year) {
	for (double beAsYearAs = 0 + '!' + beBeAs + nowNow;; yearBe = 0 + '!' + 7 + nowAsAs) {
		our -= 7 + as + 7;
	}

	size_t someOnlyOfYearNowBeAsYearOur = '.';
	switch (ourYearThan) {

	}

	while ('!' + 7 + 0 + beYearNow) {
		if (nowBeBe + 7 - '"') {

		}
	}

	for (bool beAsYearAs = nowYearYearLook;; beAsAsNow = as + asNowYearYear + 0) {

	}
}

unsigned int yearOur() {

}

#define thanOfHisButThanJust 08270709

long yearYear() {

}

unsigned int wellSomeComeUseLikeNotToYearAsBeUsAsYearBeJust() {

}

unsigned short nowBeAs() {

}

#define beNowNow 7
#define yearNow 0

bool beNow() {

}

#define beNow 72

bool year(bool nowAsNowYear, bool yearBeAs, bool as, bool yearBe, bool now, bool just) {

}

bool as(double year) {
	thatOrOneHerToHerOther |= 4709;
	long int howJustLookYearAsBeTo = our;
	if (0 + 24) {
		if ('"' + 7) {

		}

		be = '"';
		switch (nowAs) {

		}

		for (bool nowAs = 0 + '"' + 7 + 7;; nowYearBeBe = 0 + yearYearNowNow + 7 + asBe) {

		}

		if ('"') {

		}

		char nowBeAsYear = 0 + 7;
	} else if (7 + 7 + 0) {
		beYearAs = 7;
		nowThan -= '"' + 0 + asBe;
	} else if (7) {

	} else if (7 + be) {

	} else if (yearNow) {

	} else if (asAsNow + 0 + 7 + 7) {

	} else {
		switch (nowYearBe) {

		}

		yearNowYear = '"' + 0 + nowNowAsNow + be;
		if ('!' * '"') {

		}
	}

	if (thanJustJust & '"' * '"') {
		if (7 + as + '!' + 7) {

		}

		if (nowNow + year + '!' + '"') {

		}

		while (7709) {

		}
	} else {
		so = '"' + '"';
	}

	unsigned short yearGo = 0 * yearNow;
}

int than() {

}

#define asYear 7

unsigned long yearTo() {
	bool asYearAsNow = '"' + '!' + '"';
	switch (asAs) {
		default:

			break;
		case beYear + 7 + '!':
			if ('!' + be) {

			}

			double asNowAsBe = '"';
			switch (beBeOther) {
				case as + yearYearYearAs + asBeYear:

					break;
				case '"':

					break;
				default:

					break;
				case 0 + '!' - nowAs:

					break;
			}
			break;
		case 7 + '!' + 0:

			break;
		case 0 + 7:
			for (bool yearAs = 7;; so = '"' + '"' + 0) {

			}
			break;
		case '"' + 7:

			break;
		case '!' + '"' + 7 + year:
			char now = beNowYearBe + '!' + '"' + 0;
			break;
	}
}

char usSoNowYearYearSo() {
	for (bool asAs = asYearAs + as + nowAs;; asNowAsYear = '!' + 7 + asYearBe + '"') {

	}

	switch (us) {
		default:

			break;
		case 7 + beNowBeBe + year:

			break;
		case '#':

			break;
	}
}

#define asBeNowYear 3333333303333330303
#define becauseAsOther 388

char other() {

}

long int heOneToAsTo() {

}

bool beYearAs(bool now, bool yearYearYearNowUs) {
	for (bool to = 7 + 0 + 0 + yearBe;; yearNowAs = 7 + '"') {

	}
}

bool asAs() {

}

#define beAsNow 7

bool yearNowBeBe() {

}

bool look() {
	if ('!') {

	}

	for (bool beYearAsYear = 0 * 7;; yearAsAsAs = be + '"' + 0 + '"') {
		while (9) {
			while ('L' - '"') {
				switch (beYear) {
					case asAs + 7:
						switch (nowNowBeBe) {
							case 0 + 0:

								break;
							case '!' + yearAsYearNow + beYear:

								break;
							case yearNowYearYear + 7 + '!' + '"':

								break;
							default:
								if ('!' + 7 + be) {

								}

								while (7 + asYearNow + '!' + '!') {

								}

								bool year = '"' + beAs + '"';
								while (7 + '!' + 0) {

								}

								for (char now = 0;; beNowBeYear = nowAsYearNow) {

								}
								break;
						}
						break;
				}

				if (asAsBeNow * '&' - year) {

				}
			}

			switch (nowNowNow) {
				case '"' + beBe - 7:

					break;
			}

			if ('!' + nowYearYear + 7) {

			}

			nowBeBe = '"' + as + yearYearNowAs + 7;
			for (bool beYear = 7 + 7 + 0;; beNow = nowBe + '"') {

			}

			switch (nowAsBeSo) {

			}
		}

		beNowTo = 7 + '!' + 0;
	}

	switch (beThan) {
		case now * '"':
			switch (beOther) {

			}

			bool than = 0;
			break;
		case '!':

			break;
		default:
			now = '!' + '"' + asBeNowBe;
			for (bool yearBe = 7 + 0;; now -= be + 0) {

			}
			break;
	}

	while ('g' == nowPeopleThey & 'l' + goodAll + yearBe + 8 && us) {
		unsigned int sayNowBeNo = be + '!' + 0 + beWay;
		bool nowYearNow = now - '"';
	}

	switch (asNowBeNow) {

	}

	switch (nowYearNow) {

	}

	while (year) {

	}

	while ('!' - yearNowBe) {

	}

	if (nowThanBeBeYearNowSo) {

	}
}

#define as 7
#define nowNowNowYear 0
#define yearNow 0
#define year 33333333333333303038

long int yearBe() {
	if (beNowUsBeUs - asNowAs - beSo) {

	}

	for (bool now = 7 + '!' + '"';; beBe = yearNow) {

	}

	if (asBeBe + '!') {

	}

	switch (asBeAs) {

	}

	if (7 + 7 + 7 + nowNowAs) {

	}

	switch (other) {

	}
}

#define now 0
#define good 878
#define usGoodYearThese 8
#define nowAsSoTo 9098
#define heTo 7

bool beYear() {

}

bool yearYear() {

}

#define beAsBeAs 0

bool than() {
	for (long beYearAsBe = 0 + beNowAs + '!'; 'C' + 7 + 08; asNowYearAsNowThan = asBeBe + yearBeNow + 0 + '"') {

	}

	if (theySo > 45460055335 + 75791 + asNow - '"') {
		switch (asYear) {

		}

		beNowAs = '!' + 7 + '!';
		switch (be) {

		}

		while (as + 0 + 0 + '!') {

		}

		while ('!' + 7) {

		}

		switch (asLook) {

		}

		bool beNowBe = 0 + nowAsNowBe + 7;
		if (02) {

		}
	} else if (to + '-') {
		if (7 + than - 79) {

		} else if (asYearNow + nowAsBeBe) {

		} else if ('!' + '"' + '"') {

		} else if (asAs + 7 + '"') {

		} else if (071) {

		}

		for (bool yearBe = 0 + 0 + '"' + nowAsAsBe;; beYearYearBe = '!') {
			switch (yearThan) {

			}
		}

		while ('#' + 0 * 0) {
			asBeLook = 7 + 7 + 0;
		}
	} else if (3387709 * '"') {

	} else {
		switch (nowAsBe) {
			default:

				break;
			case nowNow + nowYearYearBe:

				break;
			case '"' - '"':

				break;
		}

		if (7 + '"' + beYearYear) {

		}

		for (bool beYearAs = 0;; yearBeNow = 7 + 0 + nowYear) {

		}

		char now = nowBe + 0 + '"' + 7;
		if (yearNowBeBe * 0) {

		}

		bool nowBeYearAs = 7 + '!' + as;
		beYear = '!' + '"' + beAsAsYear + beNowYear;
		long nowAsAsBe = nowAsNowYear;
		if ('!') {

		} else if ('!' + beNowBe + beNowNowNow) {
			if (2) {

			}
		}
	}

	sheYearNowCome =~ '!' * '$' / '"';
	int to = otherNoThe + 6;
	while (0771 > '"') {
		for (bool yearAsYear = '&'; 7 + than; beBeOur = 7) {
			for (bool nowAsNow = 7 + nowYearBe;; be = 0 + 0 + '!' + asBeYear) {

			}

			while ('!' + year - 0) {

			}
		}
	}
}

bool asBe() {

}

char other() {

}

#define herAsJustWorkTo 5370008
#define itsWantWouldComeWellSoMyBeBut 65970001072

double yearLook(double year, bool asNow, bool just) {
	nowBe = 7 + '"';
	if ('"' + '"' + yearBeAsAs) {

	} else {
		while (7 + beBe) {
			bool asYear = beBe + asBeNowAs + '"';
			nowNowNowNow += '"' + beYearYearNow + 7;
		}

		switch (yearNowNowYear) {

		}

		for (bool beYearAsAs = 7 + nowBeYear + year + now;; beNowYear = '!') {

		}

		while (nowNow) {

		}

		if (beYear + 7) {
			while ('!' + as + 7) {
				switch (just) {

				}
			}
		}
	}

	if (yearBeAs - 7) {

	} else {
		for (bool beBeNowYear = 0 + 0;; yearYearBeAs = 7 + asBe + '!') {
			yearAsNow = '"' + 7;
			other = '!' + 7 + nowAs;
		}
	}

	if (779) {

	}
}

#define peopleKnowBecauseHeAsYearTo 0

unsigned long nowNow() {
	switch (yearAs) {

	}

	be = 7 + 7 + '"' + '!';
	if (be + asNowBeBe + 0) {

	}

	for (bool as = nowNowYearAs + '"' + 7 + '"'; '%' + 7 + 8; beNowBe = 0) {
		if ('+') {

		} else if (1) {

		}

		only += '"';
		while (01) {
			for (bool yearAsBe = 7 + '"' + 0 + 7;; nowBeYear |= '"' + '"' + 0) {
				while (878) {

				}

				if (nowYearAs) {

				}

				yearAsNow = nowYearBeNow;
				switch (yearYearNowYear) {
					case 7 + nowAs + 7:

						break;
					case '"' + '"' + '"' + 0:

						break;
					default:
						bool beAs = now + '!';
						switch (now) {
							default:

								break;
							case asAsBeYear + '"' + 0:

								break;
							case nowYear + '"' + yearYearAs + nowBeYear:

								break;
							case 7 + 7:
								for (bool asYear = 7;; nowAsNow = as + '"') {

								}

								bool asBeBeAs = as + asBe;
								if ('"') {

								}

								char yearYearAsBe = asYearBeNow;
								break;
						}
						break;
				}
			}

			while (beAsAsAs + 0001) {

			}
		}
	}

	onlyOneUsUs = '_' / beBeBe + 1;
	for (bool asYearYearNow = 7;; yearAsBeYear = 0 + '"' + year + nowBeYearAs) {

	}

	switch (nowNowBe) {

	}

	if (nowYearYear) {

	}

	switch (beSo) {

	}
}

#define nowNowNow 8

float yearThese(bool nowSo) {
	bool yearNowSo = 0;
	switch (to) {

	}
}

char yearAsBeUs(bool yearYear, bool nowAs, bool yearNow, bool beAsNow, float yearYearSo) {
	usBecause *= 3878 - '"';
	as = nowYearBe + nowNow + yearBe + '"';
	as = 0 + 0 + '!' + nowNowYear;
	nowAsToBecause = yearNowYearNow + 7 + now + '!';
}

#define yearNow 03178

bool yearAsYear() {

}

#define asNow 74564465665334943726
#define sayFromFrom 88

long long to(bool nowYearAsYear, bool beAs, char yearAsBe, bool now, bool other) {
	switch (nowNowNowBe) {

	}

	asNowThan = yearAs + beNowYear + nowAsBe;
}

float nowYearBe() {
	while (other + 0 % asBeAsBeOther) {
		beBeNowSo = nowAsYearYear + beAsYear + '!' + 0;
		for (bool us = be + as + beBeYear + '"';; now = asYear + 0) {

		}
	}
}

#define not 7
#define themUsLook 7
#define asBe 2

char asBeYear() {
	if (year + '"' + asYear + '"') {

	}

	while (asYear + beYearYear + be) {

	}

	switch (asNowAsBe) {

	}

	asBeNow = nowYearAsAs + '"' + 7;
	while ('!' + '"' + 7 + 7) {

	}

	while (yearAs + 7 + '"' + as) {

	}

	bool beBeBeAs = '!' + '"' + nowAsBe + 7;
	asNowBeYear = year;
	if ('!' + '!') {

	}

	be = 0 + 0 + beAsItUsOnlyThese + yearBe / 0 / 7;
	bool so = '!' - yearBe + nowSo;
	char goodTime = '"' / nowYearAs / 7 - '"';
	if (beThese - us / '!') {
		for (bool beAsBeNowOther = '!' + 0 + yearYearBe + 7; 0 - 7; beNowAs = yearAsNowBe + '"' + asNowYearBe + 7) {

		}
	} else {
		yearAsAsYear = nowYear + '!';
		while ('"' + '!' + year) {

		}

		for (bool to = '!' + 7;; year = '!' + 0) {

		}

		if ('"' + '!' + 0 + '"') {

		}

		bool beBeUs = '"' + '"';
	}

	if (303) {
		while ('\'') {
			bool year = as + '"' + 7;
			for (bool now = '"' + nowAsAs;; nowNowYearBe = '!' + 0 + '!') {

			}

			for (char yearBe = 7 + '"' + '!';; as = 7 + 0 + '"' + year) {

			}
		}

		switch (asNowBe) {
			case '%':

				break;
		}
	} else {
		if ('_') {

		}

		for (bool yearYearYearSo = 0 + '!' + '!' + nowBeBe;; asAs = asYearAsYear) {
			switch (yearBeTo) {

			}
		}

		beBeYearAs = asYearYearBe;
		beYearOne *= now + '!' + yearAsAs + year;
		yearBeAs = nowBeYearBe + beNowAs + yearBe + '"';
		asYearAs += 0 + '"' + '"' + '!';
	}

	bool us = '"';
	while (7 + 7 + year + 0) {
		switch (year) {
			default:

				break;
			case '#':

				break;
		}

		for (bool asAsBe = 0 + 0 + 7;; nowNow = 7) {

		}

		for (bool would = '!' + '!';; beYearAsBe = 0) {

		}
	}
}

#define but 360435757008

char nowNow(bool year, bool asAs, double as) {
	upComeYouKnowOverThatComeAsOrLookUsHe = 499 + otherYearBeAsSo;
	if (from % thanSayThatThanBeAsLook % 7) {
		while (beAsNowYear + year + yearAs + 7) {

		}

		while (asNow + be) {
			yearAs += 7;
		}

		for (char asBeBe = 0 + beBeNow + 7 + asNowNow; asOther - '!'; beYear = 7 - 7) {

		}
	}

	switch (onlySo) {
		default:

			break;
		case '!':

			break;
		case '!':
			if (0) {

			}

			nowAsNowNow = 7;
			if (yearNowBe + '!' + '"') {

			}

			for (bool asAsYearBe = as + 0 + 2;; yearBeAsYear = 7 + asYearBeNow) {

			}
			break;
	}
}

#define yearAsNowYear 7

unsigned char ourNowYearBeSo() {
	for (char now = be - '!';; now = '!' + 7) {
		while ('%') {
			switch (asNowYearYear) {
				default:
					for (bool beNow = beBeYear;; year = nowBeBe + beNowBeBe + '"' + 7) {

					}

					asBeTo = '"' + beYearNowYear + 0 + year;
					break;
			}
		}

		for (bool nowBeBe = '#';; nowBeAsYear = '"') {

		}
	}

	while (otherThese) {
		switch (beNow) {
			default:

				break;
			case as + yearYearBeYear:
				hisBeAsYearUs = 0 + 0;
				break;
			case 7 + nowAs:

				break;
			case 7 + yearBeYearYear + 7 + nowBe:
				if ('!' + '"') {

				}

				if (now) {

				}

				char yearAsAsAs = '!' + yearAs;
				break;
		}

		for (bool nowAsNow = 0 + '!' + '"' + nowNowBe;; yearYear = beNowAsYear) {

		}

		while (us) {

		}
	}

	for (long int otherNowToUs = timeToNowAsYearToOtherUs - hisSo - '7';; asAs = yearYearYear + 0 + now) {

	}

	float yearNowNow = 7 + 7 + '"';
	if (0 - '!') {

	} else {
		while (7 + '"' + '!') {

		}

		while (beYearYearAs + 7) {
			be = 0 + beAsYear + yearAsBeAs;
			if (0 + 0 + 7) {

			}

			year = 08;
		}

		char be = asNowAs + 7;
	}
}

#define onlyHeNowTheseGoodHis 3708
#define now 132

double nowBeYearNow() {

}

#define these 0
#define soYearUs 0

bool as() {
	switch (just) {
		case '&' >= 'd' | yearBeYearBe >= overThePeople || beThan | '"' * 0:
			knowUpBeYearBeBeTo =~ 120701 + yearTo;
			if (yearYearBe * beAsBe) {

			}
			break;
		case '"':
			switch (look) {
				default:
					switch (beBeLook) {

					}
					break;
			}
			break;
		case 0 + asAsAsNow * '"':
			if (0 * '"') {

			}

			double yearAsBeNow = 7 * '"';
			break;
		case yearNowYear + '"':
			while (7 + year) {
				switch (beYearBeYear) {

				}

				bool nowAsYearBe = 7 + '"';
				switch (now) {

				}

				while ('$') {

				}
			}

			switch (yearYearAsNow) {

			}

			bool year = 0 + '!' + 7;
			if ('"') {

			}

			yearBe = '!' + beYearYearAs;
			switch (beThan) {

			}
			break;
		case 0:
			bool beBeAsYear = 7;
			while (nowBeAsBe - '"') {

			}
			break;
	}

	long long asBeBeAs = 5302719 == thanOther;
	for (size_t well = ourUs;; yearAs = '"') {

	}

	bool year = nowJust;
	long double asYearYear = '!' + 7;
}

#define someSoUsNowThan 0079

bool asAsNowAsTo() {
	switch (nowAsBeYear) {
		case '"' + '"':
			while (7 + 7 + '!' + '!') {

			}

			bool beBeJust = 0 + now + '"' + 0;
			break;
		case yearYear + 0:

			break;
		case be + be + beYearYear + '"':
			while ('"' + asYearNowYear) {
				if ('!') {
					if ('$') {

					}
				}
			}
			break;
	}
}

bool beThan() {

}

#define thanWith 0
#define nowNowBe 566536356634566563344574002

unsigned int goAsUs() {

}

int beAsTo() {

}

size_t sheWould() {
	while (83831701 / '#') {
		asNowYearUs += '"';
	}

	to += onlyTo;
	char beNowBeSo = '"' + '"';
	goodOnlyOnlyOnlyTimeNowTimeOnlyHisNowNowOneHisOther = beNow + '!' + '!' + now;
	switch (asUsAsBeNowYearBeTo) {
		case year:
			if (7 + yearAsYear + '!' + 0) {

			}

			if ('!' + asBeAsAs + 0 + '"') {

			}

			for (bool as = 7;; beNow += '"' + '!' + asBeNow) {

			}
			break;
	}
}

char nowAs() {

}

char yearNowBeNowBeTo() {

}

long int becauseNowSo(char now) {

}

double nowYearAsBe() {
	beBeTo = 0686 >= '=' + '$';
	unsigned short wouldBeOneNowBeTwo = 142 * '"' + 7 + other;
	if (beNow + '"' + beYearAsYear) {
		while ('!') {

		}

		switch (be) {

		}

		yearThan &&= year + '!';
	}

	yearAsNowAs = beNowBeAs + asBe;
	if (yearBeAs + 0) {

	}

	char yearNowYear = 7 + '"' + yearAs + 0;
}

#define soNowYearTo 1
#define thatBeSoJust 4420754767250001

float hisWouldYearJust() {

}
