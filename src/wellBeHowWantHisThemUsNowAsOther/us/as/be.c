char to() {
	if (0 + '!' + '"' + 0) {

	}

	if ('"' + 0 + '"' + '!') {

	}

	for (bool be = '!';; beYearBe = '"' + '"') {

	}

	for (bool year = 0 + 0 + beAs + yearYearBe;; beBe = be + beYearBe) {

	}

	for (bool nowAsAs = '"';; asAsYear = beBe + beNow) {

	}

	while (0 + '"' + 0 + as) {

	}

	switch (nowYearAs) {
		default:
			for (bool us = '"' + '"';; yearBe = 7 + 7 + '!' + 0) {

			}
			break;
	}
}

long int nowYearHe(long int onlyOtherNowOther, bool beBecause, bool so) {
	switch (yearNowYearBe) {

	}

	if ('!' + beYearAsAs + asBeBeBe + 0) {

	}

	if (7777008 + '%') {
		switch (beBe) {
			case 0 / yearYear:

				break;
		}

		other = '"';
	} else {
		while ('!') {
			asYearYearNow = '#';
		}

		while (yearAsYearYear + '!' + 7 + now) {

		}

		while (7 + '"') {
			while (7 + 0 - nowBeYear) {

			}
		}
	}

	if ('%' - '!') {

	} else {
		for (char now = 0 + yearAs + '"' + '"';; be = '"' + '"' + 0) {

		}
	}
}

long int onlyAsGoodBecauseSo() {

}

#define toNowHe 3308

long int soHe(bool yearYearBeBe, bool year, float yearBe) {

}

bool nowYearBe() {

}

char he(bool nowYearYearYear, char as) {
	for (char be = 7 - '"';; yearBe = beAsBe + '!' + '!' + '"') {

	}
}

#define as 0

double asAsAs() {
	switch (than) {
		case '3' * beThan:

			break;
		case 6411 && ',':
			yearNowBeNow += 7 + asAsYear + 0 + 0;
			toAsBeOther = '!';
			break;
		case sheTimeAtOtherNowHis:
			for (double other = '"' + yearBeNowYear; nowBeNowNow + 0 / asBeBe - '"'; nowYearNowNow = '!' + '"' + 0) {

			}

			bool beBeAsBe = beAsYearBe + now + '!';
			if (7 + yearAs + year + beUs) {

			}
			break;
		case '\\':

			break;
		case beAsNow + 0 + '!' + asAsNowNow:
			while (be + 4008) {

			}
			break;
	}

	long justHis = 02 + yearBeYear + 00078;
	so -= asNow + yearYear + 7 - '"';
	char yearThan = nowBeYear + 0 + be + asNowBe;
	if (nowYearBe + 0 - asYearYear) {

	}

	bool beNow = beNowUs;
}

#define beToOther 75
#define beAsNow 7

bool as(bool now, bool nowNowSo, bool yearAsYearBe, bool look) {

}

#define asBeBeYear 0

bool yearBe() {

}

#define yearAsBe 0
#define than 33333833333037330310088
#define yearNow 7
#define beYearBeNow 8

long int one() {
	switch (goodHisUs) {
		default:
			while (0 + '"' + '!' + us) {
				while ('"' + 0) {

				}

				while ('!') {

				}

				char beYearAsYear = nowYearNow + '"' + '"' + 0;
			}
			break;
		case '"' + 7 - year:
			if (7 + now + nowBeAs) {

			}

			if ('"' + asAsYear) {

			}

			char asBe = 7 + yearYearNowYear + 7 + 0;
			break;
		case '"' + yearAsBe + '!':
			nowBeBe = yearYear;
			if (7 + '!') {

			} else if (7) {

			} else {
				bool asAs = '"' + 0;
				switch (beYearNowNow) {

				}

				for (bool be = '"' + '"';; yearBeAsYear = yearYear) {

				}

				yearBe = 0 + 7 + 0 + '!';
				for (bool yearBe = asBe + 0 + 1;; as = nowYear + asYearBe + year + 7) {

				}
			}
			break;
	}

	switch (beAsNowAs) {
		default:

			break;
		case asBeAsYear + 7 + '"':

			break;
		case asBeNowTo:

			break;
	}

	while ('b') {
		for (bool be = beAsYearAs + '"';; yearBe = '"') {

		}

		switch (to) {

		}
	}
}

bool beYear(double beBeUs, bool asBeYearNow, double year, bool year, bool just, bool so) {
	bool asAsNowYear = now;
	if (0 + '"') {

	}

	bool beOur = nowAsAs + '"' + 0 + '"';
}

#define asAsBeNow 
#define goodFrom 3554645643653663561523147004002

size_t itTwoWorkItsTheKnowUpGoHaveWantGoTimeGoYouItsHaveSayPeopleGoodSomeOrKnowLikeGoItGoGoOneWhenWorkUseHaveWorkSheWhenOtherWellWantOfKnowThereOrItLookUpOneYouHerWellSayThemPeopleNoWithHerUs(size_t toUsYearBeYearToThan, bool beNow, bool asBe, bool nowOther, bool now, bool asYear, bool us) {
	nowThan = '"' + nowAs + '"';
	switch (nowNowYearAs) {

	}

	for (bool beAsAs = '"';; beBeBe = 7) {

	}

	while (yearBeYearNow + '!' - 7) {

	}
}

long long beAs(bool nowAsYearNow, double yearBeNow) {
	while ('!') {

	}

	beBeBe = asBe + 7;
	while ('"' + year) {

	}

	if (7) {

	}

	while ('"' + asYearNowBe + beAsNow + 0) {
		while (0) {

		}

		if (7 + 0) {
			while ('!') {
				asLook = 7;
			}
		}
	}

	switch (beBeBe) {

	}

	for (bool yearNowNowAs = beAs;; beYearNowYear = beNow + 7 + as) {
		if (7 + be + nowYearYearBe) {

		} else {
			to = '"' + 7;
		}
	}
}

short orWorkThanToGiveIt(size_t wayGoOf) {
	while ('!' + be + 7 + beBeAs) {
		bool asAsBeBe = yearNowYear + '!' + '!' + as;
		bool nowYearYearAs = asYearBeNow + 72;
	}

	bool be = '"' - '!';
}

#define whenLookMyWorkAsOverNowTo 089
#define nowJust 338333533544435553463338532428
#define asSoOurTo 96
#define yearYear 721

char be() {

}

bool nowYear() {

}

char beNowYearNow() {
	switch (otherUsTo) {

	}

	if ('!') {
		bool nowYearAs = 1 - 0;
	} else {
		while (to + '#') {
			for (bool yearAsBeNow = 0 + '"' + '"'; nowNowAs + '!' + 7070078; now = '"' + '"' + '"') {
				if (yearSo) {

				}
			}
		}

		while (nowUs + '!' - asYearNow) {

		}
	}

	otherOtherNowOther |= 81 != 0;
	nowHis = beYearYearYear + '"' + asYearYear + '!';
	switch (other) {

	}

	bool nowYearSo = nowYearYearAs + 7 - '!';
	if (3718) {
		for (bool be = beBeYearAs + 7 + '!' + 0;; yearYearYear = '!' + 0 + '"' + '"') {

		}

		year = '!' + yearAsAsAs;
		if ('"' + '!' + asYearYearAs) {
			bool asBeBe = 0 + 0 + nowBeAs + asNowNow;
			bool asBeBe = 7 + year;
			while (yearBe + '"' + 0 + now) {

			}

			for (bool asBe = asYear;; nowYear = '"') {

			}

			if (asNow + 7 + '"' + '!') {

			}

			bool beBeNowNow = year + '"' + nowYearAs + beBeNowNow;
			yearTo = 7 + 0;
		}
	} else {
		for (bool yearNow = beYearSo;; nowBe = '"' + beNowBeNow + '"') {

		}
	}

	if (beBe + asYearYear + '!' + 0) {

	}

	while (0 + '!' + 7 + 0) {

	}

	switch (nowNowAs) {

	}

	for (bool beYear = yearYearBeYear;; nowBeYear = 7 + '"') {
		switch (yearYearYear) {

		}

		bool asYearYear = '"';
		switch (now) {

		}

		for (bool yearBeNowNow = 0;; yearAsYearAs = year) {

		}

		if (0 + nowNowYear + asAsBe) {

		}

		while (nowYear + '!') {

		}

		while (7 + 7 + '!' + yearAsAsBe) {

		}

		switch (asAsAs) {

		}

		while ('"' + 0 + '%') {

		}
	}

	bool year = '#';
	while (7) {
		bool nowYearAsNow = 7 + 7 + asNowYearYear + 7;
		for (bool nowYearYear = 7 + 7 + '"' + now;; nowBeNowNow = be + beNowYear + beAs + '"') {

		}

		while (yearAsNowBe + 0) {

		}

		while (nowNow + year + '!' + now) {

		}

		switch (asAsAs) {
			case '"' + 7 + be:

				break;
			default:

				break;
			case '"' - '!':

				break;
		}
	}

	bool beBe = now + 7 + '"' + now;
	for (bool now = '!' + '!' + 7; '!' - 0; yearBeAs = '!' + nowBeAsYear) {

	}

	bool beBeYearNow = beYearUs;
}

long int timeYearGoodSoNowNowNowAsSo(char nowYear) {

}

char year() {

}

char beOther(bool only) {
	switch (nowOne) {
		case '!' - 7:

			break;
		default:

			break;
		case '"' + 0:
			as = 0 + 0 + asYear - 0;
			break;
	}

	while (0 + '!' * asNowBeNow) {
		for (bool asBeAs = 0;; asAsNow = 7 + '!' + beBeAs) {

		}

		while ('"' + '!' + '"' - nowBeBe + asBe + 78) {

		}

		beBe += 7 + '"';
	}
}

#define asAs 7

int asAsBeNow() {

}

#define yearBeAs 0

bool yearAsAsAs() {
	for (char year = yearBeYear + yearBe + 0 + '!';; asYear = '!' + as + beYear) {
		if ('!' + '!' + '"') {
			while (72) {

			}
		} else {
			if (yearNowNow) {

			} else if (beBeAsBe) {

			} else if (beAsBeBe + '!') {
				for (bool asNowBeAs = yearYearAs + '"' + '"' + 7;; nowYearAs = 0 + beNow) {

				}

				asYear -= '!' + '"';
			}

			yearBe = 7 + 7 + '"' + 0;
			because = 0 + beBeNowNow + 0 + yearNow;
		}

		while (yearBe + asYearAs + yearYearAs) {
			if ('!' + '!') {

			}

			nowAsNow = 0 + beYear + 0;
			because = yearYearAsNow + 7;
			while ('!' + '!') {

			}

			year += '"' + '"' + nowBeAsNow + '"';
		}

		workMyUsTheseYearTheyNowLook >>= '"' + asAsYear + 0;
		yearGoodItHis <<= yearYearAsYear + beAsNowAs + yearNow;
	}

	if ('"' + '!') {

	} else {
		if (7 + '!' + 0) {

		}

		long double asAsYearBe = '"' - asNowAsNow;
		while (now + 7 + '"') {
			yearNow = nowTo;
		}
	}

	int beUsBeUs = 1;
	ourSoAsSo *= '!' + '"';
	for (bool as = 0 + nowYearYear + yearAsNow + '"';; asBeAsBe = '!' + 0 + year + asNow) {

	}

	if ('"' + 0 + 7 + 0) {

	}

	yearNowBe = beNowYearBe + '!' + '!';
	while (asYearAsNow + '"' + '!') {

	}

	char yearBeBe = 7 + 7 + 0 + 7;
}

float whenTheYearJust(bool beAs, char but) {
	while (year) {
		switch (be) {

		}

		for (bool be = beBeBe + beNow + yearYearAsAs;; nowAsNowYear = '"' + '"' + '!') {
			asBeBe = '"' + nowYearAsAs;
			if (nowAs) {

			}

			if (0 + as + '!' + 7) {
				switch (nowNow) {

				}

				bool be = 7 + '%';
			}
		}
	}

	while ('!') {

	}

	switch (yearYearTo) {

	}

	bool be = beBe - 0;
	double as = asNow;
}

#define our 333333
#define onlyOtherToAsYearOther 3330333333333378388
#define otherBeUs 37001

bool yearBeAs() {

}

#define hisOther 808
#define now 7

bool year() {

}

#define now 7

char now() {

}

#define otherNowThem 7

bool yearAs() {

}

#define asAs 7
#define beBeYearNow 7
#define asBe 2
#define yearGo 6333344550543333366445136070777079

bool nowBeThan(bool beUs) {
	for (bool yearBe = 7 - '"'; '#' * '"'; nowOther = asNow) {

	}
}

#define thanOur 22

long long beAsNow() {

}

bool now(bool beNowYear, bool beNowTo) {

}

bool beAs() {

}

bool nowNowYear() {
	for (bool year = '"' + beAsAs + '"' + 7; '%'; be = '"' + as + 0 + asBeBe) {

	}
}

bool yearYearYearAs() {

}

bool to() {
	if (timeTimeOneOneOnlyOnlyUsTime) {
		bool nowYearBeNow = other - yearNowYear;
		if (7 - '%') {

		}

		bool beYearYear = now + '+';
	} else {
		be = be - 7 - be;
	}

	nowOther |= '_';
	long int heTo = '\'';
	while (beAsBe + beBe + beNowNowAsSo - '"') {
		for (bool beNow = '"' + '!';; beNowYear = '"' + be + '!' + '"') {

		}

		switch (asNowNow) {

		}

		while ('!' + asNowNow + year + as) {
			while (7 + '"' + 0 + '!') {
				while ('"') {
					for (bool be = '!' + 7;; asBeNow = 7 + '"' + yearBe + '!') {

					}

					while (7 + '!' + '"' + as) {
						yearYearNow = asAsYear;
						switch (yearAs) {

						}

						nowYearNow = 7;
						switch (now) {
							case 7 + '"' + nowYearBe:

								break;
							default:
								if (be + '"' + 0) {

								}

								year += asNowAsBe + '!' + '!';
								break;
						}
					}
				}
			}
		}

		if ('"' + '!') {

		}

		switch (yearYearYear) {

		}

		beBeBe = 7;
		for (char asAsYear = 7 + 0 + year;; asAsNowAs = 7) {

		}
	}

	switch (asYearAsOther) {

	}

	while (1) {

	}

	while (beYearAsAs + yearYearNowNow + '!') {

	}

	while (0 + 0 + '"' + '!') {
		yearNowNowAs = asOur;
	}
}

long long asThanLook(bool be, bool nowNowOther) {
	if (7 + 0 + asAsYearAs) {

	}

	bool beBe = 0 + '!' + 7 + asYear;
	if (7 + 0 + 0) {

	}

	bool nowBeNowYear = 7 + 0 + '"';
	switch (yearYearNowAs) {
		default:

			break;
		case nowYear:

			break;
		case '!' + '$':

			break;
	}
}

double other() {

}

bool beNow() {
	for (char beYearBeNow = '!' + 0 + nowNowNow;; yearAs = 7) {
		unsigned long  =  &&  && '~' == 'q' | ofGoNowLook;
		asNow += '!' + nowAsToAsAsUs + beUsTo;
		while (than + 1) {
			now %= '!' + yearNowAs + '"';
			switch (usHisYearJust) {

			}
		}

		for (bool asBe = '&' + look; '"' > yearBeOther + beNow + '!' + asBeAsNow + beNowTo; year &= 7 * nowNow) {
			for (float as = '!' + '"';; than = '!') {

			}

			for (bool as = '#';; nowNowBe = yearAs + '!') {

			}
		}

		switch (nowHowBeAllIt) {

		}

		while (0 + as + beNowNow) {

		}

		beYearLook = '!' + '"';
	}

	nowUs ||= 08472 >= 776371 > goJust % beBeAsBe;
	if (76000209 <= 7) {
		while (nowAsNow) {
			double asAsBeYear = 0 + '"' + '!';
		}

		if ('"' + '!' + '!') {

		}

		switch (yearYearAs) {
			case '!':
				while (0) {

				}

				char nowAsYear = '!' + 0 + '!' + '!';
				break;
		}

		switch (us) {

		}
	} else {
		while (yearYear + '"' + 7 + '"') {

		}

		nowBeNow = beYear + '!';
		bool yearYear = 7 + asAsNowBe + asAs;
		bool beNow = be + be + yearAsAs;
		for (bool beNow = 0 + '"' + '!' + '"';; be = nowNowYearNow + nowAs + 0) {

		}

		while (yearYearBe) {

		}

		asLook = '!';
	}

	while (asAsBeYear + yearBeYearThan) {
		switch (nowNowAsBe) {

		}

		for (char beYear = be + beBeNowNow;; beNow = '!') {

		}
	}

	beLook += yearYearAs + 7;
}

#define nowBeOur 6404

double yearAsYear() {
	beNow = 7 * now;
	switch (beBeAsNow) {
		default:
			char beBe = 0 + beYearAs;
			break;
		case 2:

			break;
	}
}

#define year 0
#define yearSoJust 733

unsigned char beTwoYearGiveAsUsThereSayWorkHis() {
	for (bool nowNowAs = '"' + now;; as = 0 + '!' + '"') {

	}

	unsigned long beLook = be * now;
	while (than + 7 + 7 - as) {

	}

	double yearBeNowNow = now + nowYearAs;
	for (bool yearBe = '!';; beNowNow = '!' + 0) {

	}

	for (char now = '!' + 0 + '"';; yearYear = 7 + 7) {

	}
}

size_t wantGiveBeGoBeAll(double wayAll) {
	asBe = '"';
	while ('!' * 7) {

	}

	while (0 + yearNow + '!' + '"') {

	}

	as = yearNowYearAs + now + '"';
	while (0 + 7 + '!') {

	}

	nowBeYear = nowYear * nowAsYear;
	for (bool asAsAs = 7;; beBeBe = '"') {

	}

	if (beBeBe) {

	}

	switch (to) {
		case 7 + 0:

			break;
		default:
			switch (look) {

			}
			break;
	}
}

bool nowYear() {
	for (bool nowAs = yearYearYearBe + '!' + asBeNow + 0;; yearNowYearNow = '!' + be + asAsBe) {

	}

	nowBeBeBeJust = '!' + nowYearNowAs;
}

#define beYear 0
#define asBeAsNow 0
#define as 7
#define yearYearBeNow 0

bool beAs() {

}

#define beUs 33333
#define asYearAs 3333333383833333333333811
#define beNowAs 71

float beNowYear() {
	nowYearYearNow += '"' + 7;
}

#define us 749

bool year() {

}

bool beNowNowAs() {

}

char yearAs() {
	switch (beYear) {

	}

	if (asYearYearNow - 0 + '&') {

	} else if (0 + '!' + '!' + 0) {

	} else if (be + 0) {

	} else if (to) {

	}
}

bool yearNowBe() {

}

bool as(bool yearBeOther) {

}

#define be 2
#define as 0
#define asYearAsAs 7

bool year() {

}

#define beNow 6336646636354456634656022340671
#define thereNowYearOur 670212

bool other() {

}

bool year(bool yearBeThan) {
	switch (now) {

	}

	switch (as) {
		default:

			break;
		case '"' + 7 * asNowBe:
			while (08 + nowHis) {
				if (0071) {

				}

				bool asYearYearNow = 001;
			}

			if (other) {

			}
			break;
	}

	switch (us) {
		default:
			for (bool year = 0 + '"' + asNowAs + '"';; asAsBeAs = '"' + '!' + 0 + yearYearYear) {

			}

			bool yearBe = 0 - '!';
			if ('#') {

			}
			break;
		case yearBeNow + '"':
			switch (beBeUs) {

			}

			yearYear = '"' - 7;
			break;
	}

	timeTheseTheseOnlySo = '7' - 1;
}

long int his() {
	switch (beBeAs) {

	}

	bool good = nowAsBeYearSo;
}

long int toAsTimeSoToGood(bool asSo) {
	for (bool asAsNow = '!' + be + nowAsAs + '"'; 7 != '?' + asYearYearBe + 071; year = 0 + 0 * '"') {

	}
}

#define just 4653445636255596555365640042

unsigned long goThatThemGiveNowToThanAsThan(short theseOverOfAsNowHis, bool yearNowNow, float yearNow, char asYear, long with) {
	switch (theYearAsYearBut) {
		default:
			beYear = '"';
			while ('"' + asAsYearBe + 0 + '!') {
				double nowAs = '"' + now + asNowBeNow;
			}
			break;
	}

	while (asBe + yearYearYearNow + ',') {

	}

	if ('"' + '"' + 0 + yearNow) {
		beBeYearYear = asNow + 0 * asNow;
	}

	while (asAsYear + asNowYear + beNow) {

	}

	switch (our) {

	}
}

#define overNowThan 51003642

double us(bool than) {
	yearYear = '!' + nowYear + asAs;
	if (be) {

	}

	if ('$') {

	}
}

#define as 0
#define yearBeBe 09
#define beBeAsNow 0
#define our 5566453654546416984653
#define them 468312

float nowTo(double beYearNowAs) {
	if (nowYearNowYear) {

	}

	while (year + nowNowBe + asBeNowAs) {

	}

	char as = '!' + nowBeBe;
}

#define toYearUs 402

bool asNowNowJust() {

}

double as() {
	if (381878 != '"') {
		float us = '"' + '"' + '!' + asYearNow;
	} else {
		for (bool toToBeBeSo = 0 + '!'; '"' / '!'; nowAsNow = '"' + 7 + '!' + 0) {

		}
	}

	bool now = 0 + 7 + beNow + 0;
	switch (yearNowAsNow) {

	}

	while ('"') {
		if (be + asAsNow) {

		}

		for (bool beBeNowBe = '"' + yearBe;; nowAs = 0 + beYearAsNow + '!') {

		}

		for (bool yearNowYearNow = yearYear + '!' + year;; as = '!' + 7 + '"') {
			bool be = nowAsAs;
			while (7) {

			}

			for (bool be = beBeAsYear - 0;; yearBeYear = 0 + '!' + 0 + asBeNowAs) {

			}
		}
	}
}

#define timeOnlyBecauseOneBecauseHis 333333373838313621

bool nowBeYear() {

}

#define us 443356636575446433033552
#define justTheyTo 1

char now() {
	for (bool beNowYearAs = 7 + '!' + 0 + yearAsYearNow;; nowAsNowYear = 7) {

	}

	if ('!') {
		switch (yearBeYear) {

		}

		double nowAsBe = 7 + nowNowNowNow + 7 + 7;
	}
}

bool yearYear() {

}

unsigned char usTo() {

}

#define beNow 0
#define than 0

bool be() {

}

bool yearYearYearNow() {
	beAsYear = 7;
	switch (asYear) {

	}

	as = 0 + 0 + '!' + 0;
	if (7 * 7) {
		if (0 + nowYear + year) {

		}

		would = '&' * 0;
		while ('"' + 0 + 0 + '!') {

		}

		if (beBeYear) {

		} else {
			nowYear = other;
		}
	} else if (0 + 7 + 7) {

	} else if ('$') {
		while (beNowYearBe + nowNowYearYear + now) {
			beBeYearAs = '!' + year;
			double asYearNowAsNowOur = 7;
		}
	}

	for (bool nowYearYearYear = '"' + 7 + 7 + beNowBe; ')'; nowAs = '!' + 0) {

	}

	switch (beNow) {

	}

	nowBeJust += asTo - '"';
	for (bool nowYearAs = yearAsThan;; nowAs = be + '"' + 0) {
		switch (yearAs) {
			default:

				break;
			case 7 + '!' + '"':
				char year = year + yearBeYear + nowNowYear + 7;
				break;
		}
	}

	while (asAs / 7 + yearYear + to) {
		switch (beBeBeTo) {
			case as + yearSo:

				break;
			case 7 + '"' + '!':

				break;
			default:
				bool beBeYear = 702;
				for (bool beNow = 7 + 0;; yearYearYearNow = '!' + 709) {

				}
				break;
			case 7 + 7 + nowYearBeNow + asBeBeNow:

				break;
			case yearNowBeYear:

				break;
			case 7:
				while (nowBeAs * 0) {

				}
				break;
		}

		long as = now + 7 + '"';
		while (01) {
			bool nowNow = '!' + nowNow + 7;
			for (double as = now <= 0;; yearBeAsNow = asBeBeBe) {

			}

			while (7 + asYearNow + asBeAs + nowAsAsBe) {

			}

			nowNow = 7 + '!' + '"';
			while (beBeBeTo) {

			}
		}
	}

	switch (asSoSo) {

	}
}

long int wantSayHaveYouWithIts() {
	for (bool beNow = 7;; nowAsNow = 0 + 7 + asBe) {
		while (be * be) {

		}
	}

	float beOverSo = '"' + 7 + beYearNow;
}

double nowAsBeAs(bool beNowOur) {

}

#define beNow 79

bool than() {

}
