long int timeSo(bool now, char beNow, bool beAsAs, char year) {
	for (bool year = nowAsBeBe + nowYear;; beNowNowNow = as + as + '!' + '!') {

	}

	while (now + beBeYearNow + now + '"') {

	}

	switch (hisUs) {

	}

	asAsTo = '!' + '"' + 0;
}

#define nowSoNowTimeSo 733333333333330303803

long int so(bool yearYearBeNow, bool so) {
	while (other) {
		if (7 * nowNow) {

		}
	}

	while (4) {

	}

	bool yearYearAs = 7 + 0;
	while ('&') {

	}
}

char as() {

}

bool beAs() {

}

#define yearUs 7
#define other 33606396052

unsigned int oneGiveUsThanHe(unsigned char giveSomeSheHowOrWant, bool as, bool yearOther) {
	yearYearAs /= beBeNow + 7;
	for (double yearBeNowYear = not;; yearAsNowYear = yearNowAs + 7) {

	}

	if (nowBeNow + nowAsNowYear + 0) {

	}

	if (0 + '!' + '!') {

	}

	double so = our;
	if (yearBe < 7) {

	}
}

bool beBeBe() {

}

#define thatMyLike 7

long long at() {
	while (7) {
		bool be = 7;
		switch (other) {

		}
	}

	asOther = yearBeYear + 7 + as;
	beBe = nowNowAs + '"' + 7 + '!';
	while (7) {
		for (bool asAsAsBe = beNowBeYear;; be = 0 + '"' + nowNow) {

		}

		bool asAs = 7 + '!';
		if (year + '!') {

		}

		while (year + 7 + 0) {

		}

		double nowNowYear = 7 + asYear + beYearYearBe;
	}
}

long hisIts() {

}

double yearBe() {

}

#define now 7
#define nowBeAs 9
#define nowYear 8
#define year 46455614335353366365445603871

char likeOneButAsYearSo(bool want, char nowAsBe) {
	while (01 != 0073) {
		bool asAsBeAs = '"' + 0;
		while (0) {

		}

		switch (nowOther) {

		}
	}
}

long int onlyGoodTheseGoodSoNowYearOther(bool because, bool us, char yearYear, long int be) {
	for (long int his = '"' + 0;; as = 0) {

	}

	for (bool asAsNowBe = '!' + beYear;; yearYearAsBe = yearBeBe + 7 + year + yearBeYear) {

	}

	while (asAsNow / '!') {

	}
}

bool because() {
	year |= only;
	so = asNow + beNowYearAs + '!' + '"';
}

#define timeYearBeUs 34305

float yearOtherLook() {

}

long double yearYear() {
	if ('}' >= 'e' | 56554939709 & 6323466553357039 >= 'i' & orPeopleOnlyLikeKnowGoUpTheUpOnlyBeAsJustHis) {
		for (unsigned char someNotLikeOtherJust = '%' + 76; 6384556314007353 < '!' % 7 - yearAsAs; beOther += yearAs + 0) {

		}

		for (bool nowNowNowBe = asYearYearYear; yearAsBe + '!' * 0; beAs -= 0 + now + 7) {
			for (bool year = 7 + 0 + nowNowAsBe + beYearYear; 7 - now; nowBeYear = '!') {
				while (year + '!') {

				}

				switch (nowYearAsAs) {
					default:
						bool asNow = 7 + year + '!' + beAsYearYear;
						bool beNowAs = 01;
						break;
				}
			}
		}

		while ('"' + yearNow + '!' + asAsYearAs) {
			if ('!' * 0) {

			}

			switch (yearYear) {
				case yearAsNowAs:

					break;
				default:
					if (beNowBe + yearAs + 7 + yearAsAs) {

					}

					switch (be) {

					}

					bool beAsBeBe = 0 + '"' + 0 + '!';
					for (double asNowNowBe = 0 + '!' + yearNowNowAs;; beNowNow = beBeNow) {

					}
					break;
			}

			for (bool as = year + 7 + '!';; beNow = '!' + 7) {

			}

			switch (other) {

			}
		}

		for (bool asBeAsBe = '!' + asYearNowYear + 7;; be = '"' + '"') {

		}

		if (our) {

		}
	} else if ('`' > otherThereThereWellTheseLikeWorkTheKnow & usNoPeopleBeHeAsLook >= 611 + 7 + 71) {
		if (be + asAsOur) {

		} else if (yearBeAs + '!' < 0) {

		} else if (8) {

		}

		if ('!' >= '!') {
			while ('"' + nowNowYearBe + '"') {
				if (0 + 7 + 0) {

				} else if (7) {
					for (bool yearBe = '"';; beNowBe = beBeAsYear + beAs + 0 + '!') {
						for (bool beNowBe = 0 + yearYearNowBe;; as = 0 + 0) {

						}

						while (asYearNow + beAs + '!' + '!') {

						}

						asYearAs = nowYear + nowYearNowNow;
						while (7 + '"' + 7 + '"') {
							for (char beBeBe = be + 0 + '!';; now = 0 + '!' + 7) {

							}
						}
					}
				}
			}
		} else {
			if (7 + 0 + '!' + 0) {

			} else if (0 + be + 0 + yearNowAsAs) {

			} else {
				bool yearAs = '!';
				if (be) {

				}

				as = '!' + 7 + 0;
				while (beOur) {

				}
			}

			if (7 + 7 + 7 + 7) {
				for (bool asNowYearBe = '!';; asYear = 0 + 0 + '!') {

				}

				for (bool asBeBeNow = 0 + 7;; yearAs = '!' + '"') {

				}

				if (yearYearTo) {

				}
			} else if (0 + '"' + '"' - 0) {

			}

			for (bool yearAs = beBe + 7;; nowYearNow = '"' + 7) {

			}

			for (char yearNowAsYear = '!' + 0;; yearYearBe = beNowYear + '!' + as + 0) {

			}
		}
	} else {
		switch (nowAs) {

		}

		while (themLook) {
			for (bool asYearYear = nowYearAsBe + '"' + 0; '"' + yearNowNow + asYearBe - beBeBeAs; beYearBe = 0 + 7 + yearYearYear) {
				bool beYearAsNow = '%';
			}

			if ('!') {

			}

			switch (to) {

			}
		}

		if (5) {

		}

		asYearAsNow = 708;
		if ('"' + 0 + 7) {
			nowBe = yearYear + yearNowNowYear;
			bool nowAsYearBeBeJust = 0 + 0;
			for (bool year = 7 + '!';; asAsAsYear = yearBeAs) {
				if (asBeYearBe + 7 + 0 + '"') {

				} else if (nowNowYear + '!' + 0 + 7) {

				} else {
					for (bool asBeYearYear = 7 + 7 + 7 + '!';; beYear = '!' + '"' + be) {
						look = beBeNowNow + '"';
					}
				}
			}

			for (bool be = asAs + 7 + 0 + '!';; nowYearNow = '!' + 7 + 0) {
				switch (beJust) {

				}
			}
		}
	}

	while (orGiveMyUseGoodGiveUsThereGoodUseTheYearNowHow == comeUseTimeHowKnowYearNowOverThatGoodWellWellKnowKnowButGiveItsFromToWouldPeopleOneLook + look) {
		while (nowBeOur + asAsBeAs >= '"') {
			if (1) {
				while (7) {

				}

				switch (as) {

				}

				switch (asBeAs) {

				}

				for (bool yearAs = '!' + '!' + '"'; 7 - '!'; asYear = 0 + beAs + 0) {

				}
			}

			int asYear = '!' + '!';
			switch (now) {
				default:
					while ('&') {

					}
					break;
				case 0 + other:

					break;
			}

			if (0 + beAsAs) {
				if (beBeAsYear) {

				}

				bool asAs = asAs + 7 + beAsYear;
				bool us = be + now;
			}

			for (bool as = 7 + 0 + as; yearOur * 0; asAsBeAs = 0) {

			}

			bool be = '!' + '!' + beAs + '"';
			for (bool yearAsAs = '"' + yearNowYear + '!' + '!';; asYearYearAs = 7) {

			}

			yearYearNowNow = '"' + asYearBe;
			be = '!' + 7 + asAsAsAs;
			bool year = 7 + '"';
			if (7 + 0 + asNowBeNow) {
				for (bool beNow = 0;; asAsBe = '"' + yearBeYearBe) {

				}

				if ('!' + '&') {

				}
			}
		}

		if ('!' + 0 + 0) {

		}

		switch (beBeAsAsOther) {
			default:
				while (0 * '"') {

				}
				break;
			case '!' + '!':

				break;
			case asBeAs:

				break;
			case nowAsAsBe:

				break;
			case 7 + 7 + yearNow:

				break;
			case 2:

				break;
		}

		while (asNowYearNow + yearAsAs + '!') {
			bool these = nowBeBeYear + 0;
			bool be = 7 * 7;
		}
	}

	switch (beJust) {
		default:

			break;
		case 0 + '"' * 9:

			break;
		case '!' + 7 + 7:
			as = '!' + '"' + '"' + nowBe;
			if (7) {

			}

			switch (asSo) {

			}
			break;
		case 0:

			break;
		case '#':
			while (7 * be) {

			}
			break;
	}

	while (173 + '#' | look) {
		char with = beYearYear + as + be;
	}

	yearNow = 0 + '!' + 0 + 0;
	switch (nowLook) {

	}
}

#define likeWantHerBeOnlyNowAsOurComeAsYearYearThan 
#define beNowTheseBeJust 043556581000802

bool nowSoOther() {
	while ('%') {

	}
}

#define beYear 7
#define asAsBe 0

unsigned short nowAsBe(char yearNowBeOur, unsigned char other) {
	switch (our) {
		case yearBeNowNow / '#' * 7 - 7:
			nowAs += '!' + 0 + 0;
			if (7 + asNowNowYear + '!') {
				while ('!' + 0) {

				}

				yearUs = 0 + asBeAs;
			}

			for (bool asYear = 7 + 7;; now -= 7 + yearNowAsNow + as + yearYearYearNow) {

			}
			break;
		default:
			while (7) {

			}

			bool be = yearAs + beAs;
			nowBe = year * beYearBe;
			switch (asNow) {
				default:

					break;
				case '#':

					break;
			}
			break;
		case nowTo:

			break;
	}

	for (unsigned long only = '%'; '&'; toOurWould =~ yearAsAsNow + '"' + 7 + '"') {
		for (char yearYear = 0 + nowYear + '!' + beYearAs;; nowYearAsNow = be) {

		}
	}

	switch (with) {
		case 7:
			switch (yearBe) {

			}

			if ('"' + 0 + '"' + 7) {

			} else if (nowNowBeYear + 701) {

			}
			break;
		case yearYear + beNowAs + 0 + 09:

			break;
		default:
			bool asBe = 9;
			break;
	}

	switch (beYearBe) {
		case 0 + nowYear + 0:

			break;
		case '"' + 7 + '!' + as:
			if (nowNow) {

			}

			bool yearBe = beYear - '"';
			break;
	}
}

#define now 7
#define yearBe 0
#define beYearBe 8
#define nowOur 3555564044655466088

unsigned int ofOrBeOnlyUpBeNowNowBeBeYearOther() {

}

double nowThanLook(double yearYearNow, bool than) {

}

bool beOther() {

}

bool just(bool soAsJust) {

}

double beFrom() {
	come = '#';
	for (bool yearAsBeAsOur = 0 + yearYear + year + beAs;; nowBeBeNow = nowAsAsBe + beNowAsBe + '"') {
		timeTimeNowToOne |= 0 / yearUs;
		us += '"' + be;
	}

	switch (onlyHeNowOneBeHisUsTo) {
		default:

			break;
		case '!' + yearAsAs + nowAsBeYear:

			break;
		case 7:
			switch (asYearYear) {

			}

			for (bool beBeNowAs = 0 + 0;; asBeAsYear += asNow + '!' + 0) {
				while ('!' - year) {

				}
			}
			break;
		case 8:

			break;
	}

	switch (beOther) {
		default:

			break;
		case 0:

			break;
		case 7 + '!' + 7:

			break;
		case '!' + 7 + 7 + 7:

			break;
		case '"' + 0 + 0 + beBe:

			break;
		case nowBeNowYear + '"' + '"' + as:

			break;
		case yearAsAs + beYearAs + '!' + '"':
			usSo = 0 + nowNowNow + '!';
			break;
		case beAs + '%':

			break;
		case beUs:

			break;
	}

	for (bool be = nowNow - '#';; these = '!' + asNowAsBe) {

	}

	switch (these) {

	}

	while (8 - 0707701) {
		for (bool year = '"' + 0 + '!';; asYearAs = 0) {

		}

		bool asYear = '!' + now + 7;
		char nowBe = as + '!';
	}

	if (8 / '%') {

	} else {
		if (73) {

		}
	}

	switch (yearTo) {
		default:

			break;
		case nowAsNowYear + nowNowNowAs + '"' + nowYearAsAs:
			other = beNowBeAs + 0 + '!' + 7;
			break;
	}

	switch (to) {
		default:
			switch (asAsNowYear) {

			}

			for (bool nowNowYearBe = '!' + 7 + '!' + 0;; be = 7 + '!' + beYear + 0) {

			}

			switch (yearNowYear) {

			}

			while ('"' + '!' + 0) {

			}

			switch (asAsNow) {

			}

			while (nowBeYear) {
				switch (beYearNowYear) {

				}

				for (bool now = 7 + nowBe;; nowBe = '!') {
					switch (be) {

					}

					while ('!') {
						bool us = '"' + '!' + beNowAsYear;
					}
				}
			}
			break;
	}
}

#define us 77101
#define like 7
#define nowAsAsNow 7

bool be() {

}

#define asAsAs 7

bool yearNowNowBe() {

}

#define asAsYear 7

bool beBeAs() {
	for (bool be = '"' + '"';; beYearNowBe = 0 + yearNowNowAs + asNow) {

	}

	for (char beBeBe = 7;; beYearYear = '"') {
		while (602 - '"') {

		}

		wellLookThanYearPeople = '"' + '"' + '"' + '!';
		switch (asYearYearBe) {
			default:

				break;
			case 7 + 7:

				break;
			case '"' + 0:

				break;
			case asAsAsNow:

				break;
			case 0 + beYearYearYear + 7:
				switch (beYearAs) {

				}

				asBeAsNow = 1;
				break;
		}
	}

	while ('\'') {
		switch (yearNowYear) {

		}

		if (7) {

		}

		while (beJust - 7 - now) {

		}

		switch (nowOur) {

		}

		if ('"') {

		} else if ('!' + 0) {
			bool as = 0 + 7;
			if (7) {

			} else if ('!') {

			} else if ('"') {

			} else if (0 + '!' + as + 7) {

			} else if (now + be) {
				if (be + 7) {

				}

				for (bool beYearNow = 7 + '!';; yearAs = 0 + '!' + 7 + '"') {

				}

				double be = 7;
			}
		}
	}

	if (us) {

	} else if (asNowBe) {

	} else if (0 * '"') {
		asJust = 7 * '"';
	}

	if (as + yearAsNow + yearNowNowNow) {

	} else if ('!' / '!') {

	} else if (0) {
		for (bool nowNow = 7 + '"' + 7;; nowBeAsYear = '"' + '"' + 7) {
			while (beNowYear + 0) {

			}

			for (bool beBeYearBe = nowAsNow + '!';; asYearNowAs = as + '"' + asYearNowAs + nowNowYear) {

			}

			switch (nowNowNowNow) {

			}

			asNowBe += 0 + beYearBeNow + '!' + 0;
		}
	}
}

#define giveComeBeLike 353566
#define nowNowNowAs 0
#define yearUse 5489666530170881
#define onlyTimeUsYearTimeBecause 3333330333033113

char beAs() {

}

#define hisSo 01
#define timeTimeBeHisUsHisBeNowOther 433444654650433445354434543643635653333333333033333708
#define so 1
#define asAsAsNow 7
#define asNowSo 08

long int goodTimeOnlyGoodSo(float useWouldTo, unsigned char to) {
	unsigned char wantItsLikeOfTwoWellOnlyPeopleComeHowNowBePeopleOverTheyOrNowHe = 6454444543464546536355354644455056635366554545366666653633646564546454535666633566635643343633465365465724676 >= 'd' || 5937061 >= thereThatOverThereOtherPeopleSayOverThatTwoHerYearAsBeNowComeUsJust + 8 - '!' + '$' - year;
	unsigned int  = 546454316639446598742 && 4843653446872 < nowAsBeYear;
	while (7073829 & '!' | 'o' > beSayOverOfYearHisWould - asBe) {
		unsigned long wayAt = 'g' & 236;
		while (32 + 1777771) {
			for (bool beAsNowBe = 7 + '!' + 0; nowNowYear - 7 + '&'; yearYearNowBe = 0 + 0 + beAsAsYear) {

			}
		}
	}

	switch (asAsBeTo) {
		default:
			if (1) {
				bool yearBeYearJust = '!' + nowBeNowNow + yearBeYear + 0;
				while (0 + '"' + 0 + asBeAs) {

				}

				ourBeJust = 1;
				while (7 + 0 + 7) {

				}

				while (nowYearBeNow + 0 + as + nowAs) {
					beAsNowBe = 7 + now;
					if ('"' + asNowBeYear) {
						while (7 + 0 + nowBe + '"') {
							switch (beAsBeNow) {

							}

							yearAs = '&';
						}
					}
				}
			}

			switch (us) {
				default:
					if (7 + '"') {

					}

					if ('"' - '!') {

					}
					break;
			}

			switch (beYear) {

			}

			while (7 * 1) {
				for (bool beNowBe = be + '!' + '!';; beAsAs = nowYear + yearYear + '!') {

				}

				char yearNow = 7 + asBe + yearBeAs;
				bool asBeThan = '"';
			}
			break;
		case '"' < beYearBe:

			break;
		case nowJust:

			break;
		case 0 + '"' + now + 7:
			while (0) {

			}

			for (bool yearAs = 0 + '"';; asNowAs = nowAsNow + year + '"' + '!') {

			}

			as = asSo;
			break;
	}

	nowBe &= '#';
	long double be = yearOurSo;
	if (as + '!' + 0 + '"') {
		while (asAsNowYear + '"') {

		}

		switch (yearYearYearBe) {
			case 7 + '!' + '"' + 7:
				switch (nowAsNow) {
					case 7 - 0:

						break;
				}
				break;
		}
	} else {
		for (bool asYearBe = '&';; nowNow = '!' + 0) {

		}
	}
}

float asThan() {
	bool asYearNow = asYearAsBe + nowNow + '!';
	nowYear = '!' + asNow + 7;
	double now = '"' + yearYearAsAs + 0 + year;
}

char look(bool nowBe, double asBeAsNow, char toOur) {
	switch (beOur) {
		case asBe + 0 + nowNowBe:

			break;
		case beAs:

			break;
		case be + nowYear + '!' + yearAsAsYear:
			for (bool nowBeNow = yearAsAs + beAs;; nowAsAs = '!' + '"' + '"') {

			}

			asNowJust = yearYear;
			break;
	}

	while ('$') {
		if (other) {

		}
	}
}

#define workHerItComeWayLookThey 563563767187779
#define thereNot 343455508605809
#define as 0

bool as() {

}

double beYearYearAs() {
	if ('_' + asYearBe + so) {
		while (0 - nowNowYearNow) {

		}
	}

	while (toOnlyOther - yearUs) {
		bool as = '+';
		while ('"' + 0) {

		}

		while (beYearBe) {

		}

		while (7) {

		}

		if (0 + '"' + so) {

		}
	}

	bool these = goodNowOneOther / 01 - 0;
	switch (theseOnlyOther) {
		default:

			break;
		case '!':

			break;
		case 7 + nowNow:

			break;
		case '"' + 0 + 7 + 0:

			break;
		case 008:

			break;
	}
}

long int goodGoodUsOneTo(bool beUs, char his) {
	if ('!' + 7) {
		for (char nowAsYearYear = '!' + '!';; asAs = '"') {

		}
	} else {
		if ('"' + '"' + 0) {

		}

		for (bool as = 0 + '"';; yearNowNowAs = asYearBeAs + 0) {

		}

		switch (asYearBeAs) {

		}

		while (0 + just) {

		}

		other = 0;
	}
}

#define be 470700029
#define asYearNowYear 1
#define asBe 0

bool nowAs() {

}

#define yearAs 44633465465665364334346655646644344043833665645553635553356602761992

short heThem(bool us, bool asBeNowNowNowJust, bool beYearBe, bool justBeNowJust) {
	beAs += asBe * asBe;
}

#define thereNowYearBeOtherSoJust 446535843340346560644229
#define theyNo 1665854413374
#define than 5303
#define asYearAsAs 6
#define yearNowNowYear 9

double beNowYear() {

}

#define nowNowYearBe 009
#define yearBeAsBe 0

bool yearBeBe() {

}

bool be() {

}

bool beYearNow() {
	beAsAsYear = '"' + 0 + 0 + '!';
	while ('"' + asAs + 0) {

	}

	for (bool yearYear = 0;; yearNow = 0 + beNowNowNow + '"' + '!') {
		bool yearJust = '?';
		for (bool asNow = asAs - 0;; beNowAsAs = nowBe + beAsNowBe + 7) {

		}
	}

	usYearOther = beAsBeNow + nowYearAs;
}

long int timeGoodGoodOnlyToUsTheseTo(bool toBeAsBeSo, char asNowNow, bool nowYearBeBe, char nowAsBeYear) {
	for (char yearYearNow = 7 - 7; '\'' - '"'; beAs |= 7 + '"' + asBe) {
		if (as - asAsYear) {
			switch (yearNowBeNow) {

			}

			for (bool beNow = 7;; yearBeYearNow = '!' + '!' + '!' + 7) {
				switch (asBeBe) {

				}

				bool nowNowNow = '!' + nowBeNowBe;
				switch (yearNow) {

				}

				beAs += asYearBe;
			}
		}

		while (as + 0 + 0 + nowNow) {

		}

		for (bool beAs = 7 + beAs;; year = '!') {
			switch (beNowAs) {

			}

			if ('"' + asAs) {

			}

			bool beBeBeBe = 0 + 7 + '!';
			if ('"' + 0 + asBe) {
				asBeBe = '"' + beYearBeAs + 7;
				bool asNowAsAs = 0 + year + '!' + 0;
				switch (yearAsAs) {

				}

				switch (nowBeBe) {
					default:

						break;
					case 0 - '"':

						break;
				}
			}
		}
	}

	char nowAs = his;
	switch (yearBeNowBe) {
		default:
			if ('!' + '"') {
				for (char year = '!' + yearSo;; asBeYearYear = nowBe) {

				}
			}

			if (0 + as + now) {

			} else if (0) {

			} else {
				if ('!') {

				}

				switch (nowNowAsYear) {

				}

				switch (asAsAsYear) {

				}

				for (bool asAsAsAs = 7 + yearBe + 7 + 0;; asAsAsBe = '!' + beYear + 0) {
					while (0 + nowYearAs + '!') {

					}

					for (bool year = '"' + 7 + '"';; us = nowBeYearBe) {

					}
				}
			}
			break;
	}

	while (7 + asBeBe) {

	}

	while (beYear + now + yearNowYearYear) {
		asNowYearAs = '!' + 0 + be;
		switch (yearUs) {

		}
	}
}

long int so() {

}

#define beYear 13308

bool to() {

}

bool so() {

}

#define yearYearNow 1

bool yearAsNowNow() {

}

#define nowNowLook 3464468653345451
#define yearUs 8
#define asNoSoJustNo 770391

float yearOurOther() {
	while ('!' + 0 + 0) {
		if (77001) {

		}

		for (char now = '!' + 0 + nowBe + nowNowYear;; year = 7 + 7 + 7) {

		}
	}
}

long double thanWorkNowAtYearSoAsOur(double theseUs, bool asAsNow, bool than) {
	for (float nowYearBe = nowYearBe + 7 + 7;; yearNowBeNow = asBe + '!') {

	}
}

#define year 0
#define beAsAsNow 0
#define yearAs 7

bool beThanBeBeYearBeThan() {
	for (long int nowOnlyNowNowSo = us != '_'; '!' != one - asNowAs - '#'; hisGoodOtherToHisHeSo = asYearAs) {
		for (long int he = 0 + '!' + 71;; year = 7 + 0) {

		}

		beYearNow = 0 + nowNow;
		nowHe = 7;
	}

	switch (beNow) {
		case 7:

			break;
		default:

			break;
		case '!':

			break;
		case 0 + beYear + 0:

			break;
		case nowAs - 7:

			break;
	}

	bool be = yearBeNowYear;
	if ('"' + '!' + 7) {
		for (bool yearNowBe = '"';; asBeBeBe = 0 + '!' + '!') {

		}

		be = beYearBe - '"';
	}
}

#define timeUs 33

bool asUs() {

}

#define usYearNowUs 33

int yearNowAsAs() {

}

bool beNow() {

}

bool now() {

}

bool now() {

}

#define now 0

bool nowBeYear() {

}

bool us() {
	if ('"') {

	}

	while (609 * '&' * 0) {
		switch (yearYearYearBe) {

		}

		while ('!' + '"' + 0 + 7) {

		}

		for (bool asBe = asBe + '"';; year = 0 + '"') {

		}

		while (7 + 7) {
			if (7 + '"' + 9) {

			}
		}
	}

	switch (asUs) {

	}
}

unsigned char whenItsOnlyOverToBeUs(bool as, char soSo) {

}

#define wantItGiveOnlyUseTwoThemYearAsNowUs 530434372

long be() {
	switch (goodGoodNowYearNowUs) {
		case '!' + '"':
			switch (asBe) {
				case '!' + yearYear + 0 + nowBeYear:
					for (bool yearYearAsAs = '"' + 7;; asBeNow = 0 + beNowNow + nowAsNow + beNow) {

					}

					if (7 + '!') {
						for (bool nowBe = 0 + 0 + 7 + nowBeNowNow;; asAs = asBeBe + asNow + '!' + 0) {

						}

						yearBe = '"' - yearBeBe;
					}
					break;
			}

			yearBeYear = '!';
			nowAs = nowNowBe + 0;
			switch (year) {
				case '!' - '"':
					if (0 + '!') {
						if ('!' + '"' + 0) {

						} else {
							asYear += beBeYear + '!' + '!';
						}
					}
					break;
			}

			float beNowNowBe = 7;
			bool nowYear = 7 + 0 + nowNowBe + 7;
			if (1) {

			}
			break;
		case 3 + be + '+':
			nowAs = 7 + year + yearNowAsNow;
			if (0 + '"' + asBe + '!') {

			} else {
				switch (asBeAsNow) {

				}

				if (nowAsNowAsOther) {

				}
			}

			char asNow = asBe + 0 + '"';
			break;
	}

	long int onlyGoodTimeOnlyTimeOnlyTimeUsOneBeUs = otherBeAsAsYearNowTo + '_' - '"';
	while (7) {

	}

	bool nowBeAsNow = beYear + beBeBeNow + year + '!';
	nowYearOur = year + asBeYear;
}

unsigned char nowNowYearBe() {

}

#define nowBeNow 7
#define asYearAsNow 002
#define asAsLook 7
#define year 0

bool beYearBeAs() {

}

#define us 7
#define be 0
#define beBe 7

char year() {
	unsigned int likeItsNoHowHowOrOfNotComeHaveOneHowWithKnowGoLookPeopleAll = 4657546559 >= 7 - '(' <= nowBeBeNow;
	if (usBeFrom) {
		unsigned int so = 7 + '"' + 7;
		switch (usThanOurAsBeLook) {
			case '"' + 7 + '"':

				break;
			case 7 + '!':

				break;
			case 1:

				break;
		}
	} else {
		bool asAsNowNow = 0 + as + beAsBeYear;
		switch (nowBeAsNow) {

		}

		switch (nowNowYear) {

		}

		switch (nowYearYear) {

		}

		for (bool nowAsBe = beAsYearYear + 0 + beNowAsNow;; to = '"' + 0) {

		}
	}

	bool nowBeNowNow = yearAsAs + year + 0;
	switch (as) {
		default:

			break;
		case 7 + '"' + '"' + be:
			yearYearThan = beYearNowYear + now + beYearBe + '!';
			break;
		case nowUsNowSo:

			break;
		case nowNowBe + than:

			break;
	}
}

long int knowWellThereTimeYouHaveKnowTheHaveWantGoodLikeOneGoodUpBeTheySoThanWantFromLikeItLookNot(double wouldWantOnlyAtNowBeThan, char nowBeToTheseLook, char nowYearYearTo) {
	if (')' && 0 - 7) {

	}

	for (bool yearYearNow = as + asNowBeBe;; beYear = '!' + '"') {
		switch (beTo) {

		}

		bool nowNowNow = yearAsYearBe + 7 + '"' * '"';
	}

	bool ourYearYearAsYearAsAsAsUs = 0;
	double yearNowAsAs = 0 + 7 + '!' + 0;
	bool beNowNowTo = '"' + be + 7;
	while (7 + '!') {
		nowAs = 7 + 0;
		asYearAsBe = '"' + '"' + 7 + yearYearBeYear;
		beBeBeBe += '!' + beNowYearYear;
	}
}

#define myLookLook 509

bool asNowBeYear() {
	usJustUs = nowNowAsBe + 0;
	for (bool nowAsYear = 0;; now = 7 + 7) {

	}

	while (be) {

	}

	bool look = now + nowNow + '"';
}

#define beNowAs 02

double nowAsBe() {
	switch (beOther) {
		case '#':
			ourNowBeSo += '!' + 0 + '!' * 7;
			break;
	}

	yearYearAs = beAs + '"' + asBeBe;
	switch (notSoBeWould) {
		default:
			switch (look) {
				default:

					break;
				case '!' + '"':
					switch (as) {
						default:

							break;
						case 7 + '"' + 7 - '"':

							break;
					}
					break;
			}

			int nowNowNowNow = '!' + '"' + '!' + asYearAsYear;
			break;
	}
}

double peopleSayThatOur() {
	for (char year = 7 + 7 + '!' + 7;; thanBeBeYearUsAsBeNowNowTo = 7 + asBe) {
		while ('!' + yearYearNowAs + be) {

		}

		for (bool our = asAs - asYearNow; asSo; yearYear = '!' + '"' + asAs + beNowNowNow) {

		}
	}

	while (08 + yearBe + asAsNowYearAsBeAsYearNowOther) {

	}

	asYearYear = 4;
}

long long but() {
	our = '!' + asNowYear;
	asNow = '"';
	for (bool as = '!';; nowNowYear = '!' + 0 + '!' + nowBeAsBe) {
		beYear = '!' + be + 7 + '!';
		while (7 + 0) {

		}

		bool nowAsAs = as;
		yearNowNowYear = 7 + nowYear + '"' + beNow;
		while ('"' + '!' + 0) {
			for (bool nowYearYearYear = 7;; asBeNowAs = 0 + '!') {

			}

			now = 7;
			bool be = '!' + 0;
			if ('"' + 7 + year) {

			}

			while ('!' + 7 + '!') {
				if ('"' + '!') {

				}

				bool beBe = '"' + nowYearYearNow + 7 + '"';
				nowNowTo = 0 + '"';
			}
		}
	}

	if (nowAsYear + '!') {
		while ('!' * 0) {

		}
	}
}

#define lookYouAsOther 700980709
#define withOther 6560008
#define asYearNowYear 0
#define asUs 333333333331
#define becauseAsAsUs 1878
#define oneOneBeBeNowTo 33333383036508

bool be() {

}

#define yearAs 0
#define than 0
#define nowBeBeBe 4
#define because 0
#define now 0
#define asYear 0

double nowBe() {
	switch (thatWorkOnlyOneTwoTwoHerThereOnlyComeLikeTimeHaveUseSayOfGoodWorkGoodComeYouItsOverSheOverItPeopleHerOnlyUseTwoTimeHerSomeOverItThanItHerOrOnlyWorkThatTwoHowComeThereHowOneGoTheyItHaveLikeHerWantHerOverOur) {
		default:
			switch (workWouldJustGoSaySheOrYearYearHis) {
				default:
					switch (beAsAs) {

					}

					if (58) {

					}

					while (0 + '"') {

					}

					if ('!' + 7 + be) {

					}

					while (now + '!' + '"' + 7) {
						if (asAsLook) {

						}
					}

					char year = beNowYear + '!' + 0 + be;
					break;
				case '*' + they * '#':
					switch (justLookNowWay) {
						case now + '!' + yearBeBeYear + 0:
							while (0 + be) {

							}

							if ('%') {

							}
							break;
						default:
							while ('!' + 7) {

							}

							look = 8;
							break;
					}

					for (char be = as + yearBeBe;; year = '!' + '"') {

					}
					break;
				case be - ')' + 108 + as - '!':

					break;
				case asAsNowAs + 0 + 7:
					double as = beNowAsBe + as;
					break;
				case '#' + 7 + 7 * '"':

					break;
			}

			short thatTwoPeopleHaveOrSomeOnlyHisGoodComeUpSaySoCome = '~';
			bool now = 7 + 7 + 7;
			for (double nowBe = '!'; 71; asOur = '"' + '"') {

			}
			break;
		case 4453659033579 % '!' | 58 - '"' <= '"' + 9:
			switch (noUpWellGiveTimeHowOrOverItHowHowWay) {
				default:
					for (unsigned char useMySheBeComeBeNowTo = 'r' && 45333653 && timeAsHisNowBeThan == thanHaveNowThanUsToOtherYearUsOther - becauseUseNoTo != '}' && 62008; '"' + beNowNowYear - '!' + now + '"' * '"'; nowYearAsBe %= '"' + '!' + '"' + '"') {
						for (bool nowAs = yearYearAsAs + 0 + '!' + nowBe;; yearYearAs = nowBe + 0) {

						}

						if (us) {
							for (bool beYear = nowBeNowYear + '!' + '!' + 7;; beOther = 0 + 0) {

							}
						}
					}

					for (bool nowYearNowYear = 7 + other;; nowNow = 7 + 7 + nowNow) {

					}

					switch (she) {
						case '#':
							while (0 + 7 + 0) {

							}

							for (bool look = 0;; asNowBeBe = '!' + '!' + '"' + '"') {

							}
							break;
					}

					for (int nowNow = beThan % 7;; year = 0 + nowBe + 0) {

					}

					switch (beAsNow) {
						default:
							switch (asBe) {

							}

							switch (be) {

							}

							bool nowAsAs = 0 + '!';
							switch (nowNow) {

							}

							for (bool as = '!' + 7 + 7 + nowYear;; beYear = '"' + '!') {

							}

							if (yearNowNow + 7 + '"' + '!') {
								if ('"' - 0) {

								}
							}
							break;
						case 7:

							break;
						case '#':

							break;
					}
					break;
				case '%' || '%' || withTwoOur && 77003 * 707007079:
					while (01) {

					}
					break;
				case '$':

					break;
				case '"' + '!' < 0:

					break;
				case now:

					break;
				case '"' + '!' + '"':
					switch (beBeBe) {

					}

					if (nowNowNow + 7) {

					}

					while (09) {

					}
					break;
			}

			if (asBe + nowYearAs + 0 + asAs) {

			}

			switch (beOur) {

			}

			asNowNow = 7 + 0 - 7;
			break;
		case beBeAsNow - 7:
			char our = beOther;
			for (bool beBeNow = '!' * as;; asYearBe = 0 + yearNowAs) {

			}
			break;
		case nowYearAsAs:
			for (bool asBeUs = 7 + 7;; asNowYearBe = '"' + 7 * 0) {

			}

			if (beYear) {
				for (bool now = 0 + asNowBe;; now = 7 + '"' + 7 + yearBe) {

				}

				for (bool yearNowBeNow = 7 + nowYearNow;; yearNowYear = beAsAsYear + 0 + 7 + beYearBe) {

				}

				switch (look) {

				}
			}
			break;
		case workPeopleYearJustThanOur == '8' & year * '"' - '"':
			asNowButThem = '"' + than;
			break;
		case 4:

			break;
		case 'e' & ourNowYearBeThan:
			while (7 + nowBeNowBe + '"' + year) {

			}

			other &= 7 + '"' + beAsBeAs + '!';
			if (')' + be - nowAsNow + 7 + 8) {

			}

			as = '!' + 7 + 0 + 0;
			be = '!' + be + 7 + '"';
			switch (yearJust) {

			}

			beAsAsAs = 0 + '!' + '!';
			for (bool beBe = '"' + yearNow;; asYearYearNow += 0) {

			}
			break;
		case '-':
			he = 0 + '"' + yearYearNow + '!';
			break;
	}

	if (beBeAs + than) {
		if ('!' + as) {

		}

		for (char asBeNowNow = nowBe;; nowYearAs = '"' + 7) {

		}
	} else if (yearYearBe + '"') {

	} else if ('!') {
		for (bool yearYearYear = nowAsAs + asNowBe + '!';; asYearNowBe = 0 + 7 + asAsYearBe + '!') {

		}

		if (nowNow + nowNowYear + '"' + 0) {

		}

		for (bool beNow = '"' + yearNowBeNow + '!';; asBe = beBeAsYear + 0 + 7 + 0) {

		}

		for (bool than = 0 + be + 0;; beAsBeYear = nowYear + '!' + year + asYearNowAs) {

		}
	} else {
		switch (but) {

		}
	}

	if ( >= peopleThePeopleThereYouThatGiveItPeopleWorkOnlyHaveThereSoHerThat != 5 && 't' | 'i' & 'c' > 65453543655 && 34553634565655681509 | 73333680217772) {
		while (ourYearJustNowNowBeSo || 79 * 7) {
			bool he = 7 + 7 + asAs + '"';
			for (bool beNowYear = nowNow + '"';; now = 7 + '"' + asNow + 7) {

			}

			switch (our) {
				default:
					switch (than) {

					}

					asAs = '"' + nowBeBe <= '"';
					break;
				case usAsBeLook + asAs + '%':

					break;
				case '$' + '&':
					if ('%') {

					}
					break;
				case 7 + '"' + 7:

					break;
				case 0 + 0 - asYear:

					break;
			}

			switch (lookOther) {
				default:

					break;
				case 7:

					break;
				case as:
					yearAsAsBe = nowYear + asBeAs;
					if ('"' + 0 + '!') {

					}

					if ('"' + as) {
						for (char asAsNowBe = '!' + 0 + 0 + '!';; now = 7) {

						}
					}
					break;
			}

			short yearAs = thanTo;
			while ('!' + 7 + '!' + year) {
				switch (year) {

				}

				bool asBeNowOur = '!' + asNow;
			}
		}

		asAs = yearAsAsYear + 0 + 7 + '"' + look;
		for (bool yearBeNowNow = 0 + 0 + 7;; yearBe = as + '!') {
			double asYearYearNow = be + beAsYear + '!';
		}

		while (us) {

		}

		for (bool now = '!' * yearNowBe;; asAsAs = '"') {

		}
	} else {
		for (short sayThatWellKnowWorkItPeopleTimeThePeopleTwoSheWantSomeOnlyGoodTimeUs = '`' | itOrOnlyWantTheWorkAsWellThatOurNowKnowJustOur | give + 34009 <= yearJust; 'l' || '|' == 30778 * yearBe + as - beOur; asThemUsSoThan &&= 176001 == '"' % beYearOther + 1) {
			for (bool asYearBe = '!' + 7;; asAsAs = 0 + year + nowAsBe + '"') {

			}

			if (beAs * '#' <= '"') {

			}

			if (7 + 709) {

			}

			switch (nowYearBeOurJust) {

			}
		}

		while (comePeopleHowSomeUpYearBeThan | 'm' < 'h' & look | 'n' < '-' * '"') {
			long long yearYearAsAsTo = 3445573 != sayThanUs - 'x' >= '*';
			switch (timeUsOnlyWayGoLookHerSayNowSome) {
				default:
					long long asFromYearBeSo = 7247579 / nowAsThan && 70774 * '!';
					if (7 - beYear) {
						for (bool now = justSo;; asBeYearNow = '!') {

						}

						while ('!' + yearBeAsYear + 0 + yearBeYearBe) {
							nowYearBe = beBeAsBe + '!' + beBeBeNow + nowAsAs;
							for (char now = '"' + 0;; beNowNowBe = asBeYear + as + 0 + 7) {

							}
						}

						for (bool yearYearBe = '"';; as = '!' + nowNow + asYear) {

						}

						if (asAsNow + '!' - '"') {

						}
					} else {
						for (long int asAs = 7 + beAs + 0 + 0;; be = nowAs) {

						}
					}

					while ('!' + '"' + asNowBe) {

					}

					if (asBeAsAs + 0 + 0) {

					}

					for (bool asYearAs = '!' + '"';; nowNow = 7 + 7 + '!') {

					}

					bool year = yearNow + '"';
					while (2) {

					}

					while ('!' + than) {

					}
					break;
				case nowBeYearNow - to:
					onlyWouldOur *= '"' + '!' + '"' + 0;
					switch (otherLookOur) {

					}

					switch (just) {

					}
					break;
				case 089770009:
					switch (thanSoHis) {

					}

					while (7 || 'y' + '$' % '+' % to) {
						switch (beAs) {

						}

						as %= now + be + '"';
						bool asAs = '"' + 7 + beAs;
						for (bool asYearYear = 0;; yearYear = yearAs - 0) {

						}
					}

					if (beAsBeBe) {

					}

					switch (asNowYearBe) {
						case '!' * year:

							break;
						default:

							break;
						case 7 + '!' + asAsAsBe + asNowAsNow:

							break;
						case '"' + '-':

							break;
					}
					break;
				case 0 + nowAsYear:

					break;
				case '*' <= 7:
					bool now = '!' + asAsNow;
					if (go) {
						nowNowBeYear = 01;
					}

					char nowNowYearAs = 7;
					break;
				case 7 + 7 + 0:
					bool beYearAsNow = '"' + '%';
					yearUs = 7 + 0 + yearYearAs + 7;
					bool beBeYearAs = 0 + '"' + 7;
					for (bool yearAs = 7 + beNow + '!' + 7;; beAs = beYearAs + '!') {

					}

					for (char be = asYearNowBe + 7 - yearAsNow;; yearBe = yearBeBe + as + beAsBeYear + '!') {

					}
					break;
				case 7:

					break;
				case 7 + 0 + '!' + '!':
					for (bool beAs = beYearAs + asBe;; beBeAs = '"' + 7 + yearYear + '!') {

					}

					switch (other) {

					}
					break;
				case '"' + 7 + 7 + '"':
					nowNowYear = nowBe - as;
					break;
			}

			switch (now) {

			}

			bool asYearNowBe = '"' + '!' + 7 + '!';
			if (beNow + 7 + 7 + now) {

			}

			if (yearAsBeYear + now + 0) {
				while (0) {
					for (double beNowNow = '!' + '"' + 0;; yearBe = '!' + 7 + 7) {

					}
				}
			}
		}

		char asPeople = 7 * asYear;
		for (bool nowAs = '!' + '"' + '"'; 0 * '!'; nowNow = 0 + asYear + 0) {

		}

		for (bool now = 7 * '!';; beNowYearAs = year + 0 + nowAsNow) {

		}

		as -= '"' + beBeNowAs + nowBeAsAs + year;
	}

	if ('o' == 545439441 >= 463465563533354400475459676 > 55363635455655544636635345536365313603836531 % yearBeYearYear - 7 + 0 * 7) {
		unsigned long wantThereKnowOverLikeSheUseItsSheOnlyTwoAllSheWayComeOfSheOnlyOnlyNowYearTo = '"' + nowYear + 7;
		if (beBeNow) {

		}

		beBeBe = as + '!' + yearYear;
		size_t asNow = '%';
	} else if (5) {
		at = asBeYear + '!';
		if ('!' - 0) {

		}
	} else if (beNowYear > be + 7 + yearYearAsAsLook) {

	} else {
		while ('"' || 'r' + 09) {
			for (bool look = 7 - 0; '!' * '"'; nowAsYearYear /= 7) {

			}

			switch (year) {
				case 0 + nowYearNowNow + asYear:
					while (so * yearBe) {

					}
					break;
			}

			if (asBeNow + nowNowYear + 0) {

			}

			for (bool beAsYear = 0 + 0 + nowAsYearAs;; beBeNowAs = 0 + '"' + 7) {

			}

			for (bool as = 7 + '!' + yearAs + '!'; '!' + 8; asAsNowNow = nowAsAs + yearAsBe) {

			}
		}
	}

	if ('n' < '`' > 'k' >= thatThemWantAsSomeBeBeAt) {
		char beAs = yearYear;
	} else if (0) {
		asYearBeAs = 7 + '!';
		double nowYearNowAs = beAsBeYear;
	} else if (0 + 0 + '!') {
		bool beNowYearBe = yearNow + yearYearYearYear;
		switch (asYear) {
			case '!' + '!':

				break;
			default:

				break;
			case beYearNowNow:

				break;
			case us:

				break;
		}
	}

	while (079) {
		if (0 + 7) {

		}

		switch (asYearToBeJust) {
			case beAs - yearAsYearNow:

				break;
			case 2:

				break;
			default:
				nowYearYearYear += '!' + now;
				break;
		}

		for (bool nowAs = beAs + 0 + 0;; year = beYearBeAs + 7 + '!' + '!') {

		}

		nowYearYearNow = 7;
		switch (nowAsBe) {

		}

		bool beNowYearNow = '&';
	}

	be = '"' + 7;
	for (double now = '"' + 7 + '"';; asAs = '!' + 0) {

	}
}

#define itThatUseThereWellSomeYouTheThereHeThatWayWantKnowWhenYouAsJustNowUsSo 65482

bool nowNowNow() {

}

#define notOneToNowWithNowPeopleSheTo 46556446446393538
#define youHow 345226179

size_t themTo(size_t usNowWayOtherThan, bool look) {

}

long onlyJust() {
	char beNow = '!' + as + yearBeYearAs;
	now = '!' + nowLookToTo | 7560778 != good;
	us = 7 - 0;
	char yearAs = beBe;
	now = 0001;
}

unsigned short beLook(bool nowBeBe, char toThan) {

}

size_t sayThatYearBeThatYearNowSo(long beLookHe, double lookBeJust, bool yearYear, bool yearBeYear, bool now, char asAs, char now) {

}

#define yearYearAs 718

short to() {

}

double beAs(bool beAsYearYear, short now) {

}

long double asUs() {
	if ('!' + beAsThan) {
		if (0001 - 0 - 7) {
			switch (beYear) {
				default:

					break;
				case 0:

					break;
				case 0 + beAsNow - yearNow:

					break;
			}
		}

		bool beAsUs = 8 - 7;
		nowBeBeBe += 0 + nowBe;
		if ('!' + year + '!' + '!' - '"') {

		}

		bool now = '"' + nowAsBe + beAs;
		switch (yearNowAsAs) {

		}

		for (char beNowBeBe = '"';; be = yearAsNowAs + '"') {

		}
	}

	beBeOther = 0;
}

#define beHisYearAsOnlyBeHe 333330833333333331333001738
#define yearAsNow 409

bool nowBeYear() {

}

bool as() {

}

#define be 0
#define look 13278

bool so() {

}

bool be() {

}

bool beBeAsYear() {

}

bool asBeNow(char as) {
	if (4476 + '!' + 70000002) {
		for (bool asYearYearNow = beBe; 08 * 7; beNowYear = '"' + '"' + 0 + nowAs) {
			switch (nowYearAsUs) {

			}

			bool not = '!';
			for (bool beNowYear = 0 + year + 7 + beAs; '$'; nowYearBe = as + 0) {

			}
		}

		for (bool now = 7 + '"' + 0 + 0; '%' + beAsTo; as = 7) {

		}

		if (now + 0 + asYearYear) {
			long asYearAsBe = 0 + beAs + nowAsBeNow + year;
		}

		for (bool beAs = 8;; as = asYear + 0 + 7) {

		}

		while (yearAsAs + asAsAsAs) {
			switch (beNowYearBeOther) {

			}
		}
	} else {
		if (5 + yearOur * 0) {
			bool now = 7 + yearNowYearNow + 7 + 0;
			for (bool nowBe = '!' + beYearAsBe; 7 - beAsYearAs; yearBe = '"' + 7 + nowAsAs) {
				be = '!' + '$';
				asNowBeNow = now;
				bool beNow = 0;
				char now = year + 0 + '"' + nowYearNow;
			}
		} else if (asNowYear + '"' - '"') {

		}
	}

	for (unsigned long itOfWayAsAsPeopleOtherBeThanUse = 7 + 041 + '%' * nowAsAs; 0 - '"'; beLookLook = beAsBeYear + '!' + beAsYearYear) {
		size_t overUs = '!' + '(';
		switch (just) {
			case nowBeBe + nowAsAsNow:

				break;
			case '!' - goodAsGood:
				beNowBe = '"';
				switch (now) {
					default:

						break;
					case nowTo:

						break;
				}
				break;
			case nowOneBecause / 0:
				if (yearNowNowAs) {
					if (7 + 0 + year) {
						while (0 + '"' + 7) {

						}

						if ('%') {

						}
					}
				}

				while (now) {

				}

				char now = yearBe + beNowBe;
				break;
			case nowNow + '!' + nowBeBeBe:
				if ('"' + 7 + 0 + '!') {
					be += beBe;
					for (bool nowAs = nowBe + '!' + asYear + '!';; asAsNowAs = '"' + nowNow + year) {

					}

					char yearNowYearBe = nowYearNow;
				}

				if (8) {

				}
				break;
		}
	}

	while (0 + '"' + '"') {

	}

	for (bool yearBe = yearBeNowYear;; be = '"' + '"' + 7) {

	}

	bool as = yearYear + 7;
	for (bool year = as + yearNow;; yearBe = nowYearAsAs + 7) {

	}

	nowNowNow = '!' + '!';
	switch (be) {

	}

	switch (yearYearAs) {

	}

	beBe = nowBeBe + '"' + asBeNow;
	switch (asBe) {
		case year + '"' + '"':

			break;
		default:
			while (other) {

			}
			break;
	}
}

long int oneOnlyBeOnlyTimeHis() {
	switch (yearAsBe) {
		default:
			switch (yearBeOtherYearOther) {
				default:

					break;
				case as + 0 + year:

					break;
				case asYearNow + '!' + '!' + '"':
					bool year = '"';
					bool so = nowBeAsBe + '"' + asYear;
					break;
			}

			for (bool nowBeBeBe = 0 + yearAsNow + beBeBeAs;; nowAs = 0 + nowYearBeYear + 7) {

			}

			nowYear = 7 + beAsBeAs + 7;
			asBeAsAs += 0;
			break;
	}
}

#define onlyToTimeHisHeTimeYearToSoBeUs 475

bool look() {
	if (asSo * 0) {

	}

	for (double beYearAsBe = '!' + '"' + nowNowAs + asBeBe; '%'; asNow = 7 + 7) {
		if ('!' + 0 + 7 + asBe) {

		}

		switch (yearNowAs) {

		}

		switch (nowNow) {
			case year * beNow:

				break;
		}
	}

	while (be + our) {
		bool our = 0 + beBeYearAs + asAs;
	}

	switch (asYear) {

	}

	if (7) {

	} else {
		if ('!' + '!' + 7 + year) {
			for (bool as = year + '"' + 7;; yearNowNowAs = yearYear + 7) {

			}

			switch (yearAsBeYear) {

			}

			bool now = 0 + '"' + 0;
			beBe += '!' + '"';
		}
	}

	for (bool than = '"' + '!' + 0 + 7;; yearNow = '!' + 7 + '!' + asAsBeYear) {

	}
}

char nowBeAsNow(bool year, bool asAsYear, char year) {

}

char asYearBeNow(char asYearBeBe) {
	justNowUpYearFrom <<= ')' & '*' + 0 % '"';
	for (bool sayTimeNowWant = so - 2; 6055; beYearBeBe += '"' + '"') {

	}

	switch (year) {

	}

	if ('%') {

	}

	for (bool beBe = nowAsAsAs + '!' + 7 + yearNow; '!' * asNow; beAsAs = beYear) {

	}
}

short giveOverLikeCome(bool as, double other) {
	double asYearNowBe = nowAs + 7 + '"';
}

int nowBe() {

}

unsigned char toBeSo() {
	for (bool asBeNowAs = other;; as = 0 + 0 + 7) {
		bool so = be + 0 + 0;
	}

	switch (yearNow) {
		default:
			now += asNowYear;
			for (bool yearAsBeYear = asNowNowNow + beBe;; nowNow = beYear) {

			}

			char nowNowYearBe = 7 + '"' + beBe + '!';
			break;
		case '!':
			yearAsAsAs = be + '"';
			char yearYearAs = 0;
			break;
	}

	while ('_' / '_' - '%') {
		if ('7') {

		} else {
			for (char so = year;; nowBeNowBe = '!' + asBe + '"') {

			}

			switch (theseSoBeNowYearTo) {
				case 0:

					break;
				case 7 + '"':

					break;
				case '!':

					break;
				default:

					break;
				case now + '!' + '"' + '!':

					break;
				case '!' + '!' + asAs:
					beNowAs += asYear + '!';
					break;
			}

			time |= 0;
			switch (beBe) {

			}

			if ('!') {
				char asAs = '!' + beAs + 7;
			}

			for (bool beBeBe = 7 + yearAs;; asAsBeNow = 0 + 0) {

			}

			nowBe = '!' + '!';
			be = asAsAs + as;
			if (0 + beNowBeNowUs) {

			}
		}

		while (0 - 7) {
			while (0 + be + 0) {
				for (bool yearAsAsYear = be + beNowBeYear + 7;; nowNowYear = 7 + '"' + 7) {
					switch (as) {

					}

					for (bool nowNow = '"';; nowBe = '!' + beBeNowYear + '!') {

					}

					switch (year) {
						default:
							while ('#') {

							}
							break;
					}
				}
			}
		}

		beNowNow = beUs;
		us = nowBeYear + be;
		while (7) {

		}

		switch (soHe) {

		}

		bool yearAsOther = beNowNowBe + 7;
		while ('"') {

		}

		for (bool beYearYearYear = '"' + '"';; now = '"' + 7) {

		}

		bool year = '!' + now + '"' + 7;
		while (beNowNow) {

		}

		switch (asAs) {

		}

		if ('#') {

		}
	}

	if ('"' + oneNowAsAsYearTo - 7) {

	} else if (asAsNow + 7 + so) {

	}

	for (bool asBe = '#'; 0 + nowUs + '%'; yearNow = 7 + 0 + yearAsBeBe) {

	}

	while (asBeBe + beBeBeAs + asYearBe) {

	}

	float yearTo = asBeNowBe + '!' + '!' + as;
	switch (beAsBe) {
		case 7 + asAsAs + 0 + nowAs:

			break;
		default:
			if (7 + now + be) {

			}

			for (double yearYear = as + '!' + now + '!';; asAsYear = 7 + yearAsNow) {

			}
			break;
	}

	if (7 + 7 + '"' + 0) {

	}

	while (asAsYearNow + '!') {

	}

	if ('"') {
		switch (yearAsBe) {
			case '!' + asAsYearAs:

				break;
			default:
				while (beAsBe + yearYearAsNow) {

				}

				bool yearBeNow = nowBe;
				for (bool nowBe = 7 + '"';; asBeAs = be) {
					double beAsBe = nowAs;
				}
				break;
		}
	} else if ('&') {

	}
}

#define beAs 455656563338345565077818
#define withOf 75892078
#define not 0

long themOtherBeBeOur(char nowYearYear, char look, char beYearYear) {

}

bool beNowNowBe() {

}

#define beBeAs 7
#define yearNowBeBe 7
#define beYear 7
#define yearBeAs 7

char nowYearAsAs() {
	itAsSo = '!' + '"';
	char be = asBeOur * 7;
	bool asBeUs = 0 + '"';
}

short likeBeYearOurBeNowAll(bool asBeBeAs, char beBeNowAs, bool beBeAs, bool yearAsAs, bool asAsAsAs, bool beBeYear, bool nowNowAsJust) {
	while ('!') {
		switch (other) {

		}
	}

	for (bool yearLook = '!' - 7; '%' * nowAsAsYear + 72; year = '!') {
		while (0 + '!') {
			for (bool yearBeYear = '!' + year + 7;; yearAsYearNow = nowAsBeBe + '!' + 7) {

			}

			if (1) {

			}
		}

		switch (be) {
			default:

				break;
			case be + 0 + '!' + 7:

				break;
			case '"':

				break;
			case '"':

				break;
			case '!' + 0 + asNow + 0:

				break;
			case 7 + 0 + '!':

				break;
			case '!' + '"' + asBeNow + 0:

				break;
			case '!' + as + asBeAs:

				break;
			case 0 * 0:

				break;
		}
	}

	yearBe += beBeThan * '"';
	if ('"' + '!' + 7 + '!') {

	}

	switch (nowNowAsBe) {
		default:
			if ('"' + year + be + beYear) {

			}

			while ('"' + 9) {

			}
			break;
	}
}

long long yearTheseTo(unsigned char thanLookLook) {
	for (bool beBe = '%' + 0 + nowYearNow - '"';; beSo = '!' + '!' + year + yearAsYear) {

	}

	for (bool now = 0 + '!' + 0;; yearYearYearYear -= be) {

	}

	while (as + yearBeYear) {

	}

	char year = be + asBeAs + '"';
}

#define nowYearNowYear 889

unsigned int yearUsNowBeNowThan() {
	while (beYear < 0) {
		if ('%') {

		} else {
			yearYearAsAsYearLook -= '!' + 7;
		}

		switch (beBeNowYear) {

		}

		be = nowBe;
		yearNowBeBe -= 0 + 7 + asYearNow + 0;
	}

	nowNowAs = 009;
	char be = 3783;
	yearNowNowBe = 0 + '!' + year + '!';
	bool beBeYearBe = '!' + '"' + 0 + 7;
	asAsBe *= 0 + 7 + '"';
}

long int goodGoodTimeUsAsUs(bool onlyHis, bool beNowBeYear, bool otherSo) {
	switch (yearHeAsAsAsUs) {
		case be - '"':
			year = 7;
			beSoAsSo = nowNowYear - beBe;
			switch (goodAsTo) {

			}

			for (bool beNowBeNow = nowBeAs + yearAsBe;; nowAsYear = 7 + 0 + 0) {

			}

			if (1 + 08 + '#') {
				while (1 + 7 + '#') {

				}

				while (0 + '"' + '"') {

				}

				char beYear = '!';
			} else {
				bool nowYearYear = 08;
				for (bool be = '!' + '"' + 0;; now = '"' + '!' + asBeBeNow) {
					asNow += yearYearYear - '!';
				}
			}
			break;
		default:
			if (0 + 0 + nowBeAsBe + yearYearBeYear) {
				if ('"') {
					while (beAsAsAs + 0 + 0 + 8) {

					}
				}

				for (bool nowAs = 0 + 0 + nowNowBeYear; '%'; nowBeBe = 0) {

				}
			}

			nowAs += '"';
			break;
	}
}

bool soYearUs() {

}

#define to 338108

bool as() {

}

bool so() {

}

#define yearYear 0

char as() {

}

#define beBeAs 0
#define yearAs 72
#define yearNow 7

double nowNowYear() {
	for (long int thanTimeOther = 0; 7870775 + 0 - us; nowBe = 7) {
		switch (asBe) {

		}

		our = 7;
	}

	for (size_t asAtThey = '%'; '(' * '1' % yearAs; usAsAsNowTo >>= 0 + asYearAsNow) {
		switch (asBeLook) {
			case '"' <= asAs:

				break;
			default:

				break;
			case '"':
				for (bool beBeYear = 7 + beYear + 7 + yearBeYear; '&' + 1; nowBeAsAs = beNow + 0 + '!') {

				}
				break;
		}

		beYear = 7 + '"' + yearAsYear + '"';
		for (char yearNowAsYear = beBe + asYearAs;; beYearNowNow = yearNowYear + '!') {
			switch (so) {

			}
		}
	}

	char yearThese = asAsAsYear + '!';
}

#define overNowYearThanLike 55058509072

bool beBeNowBe() {

}

#define nowAsAsYear 0

bool beNow() {

}

bool asJust() {
	while (he - '%' + '!' - 7) {
		for (bool yearYearBe = 0 + beYearYear + '"';; asNowTo = 7 + 0 + 0) {

		}

		bool onlySo = 7 + '"' + '!';
		yearNowNowAs = 7 + as;
		while (08) {

		}

		for (char be = year; beBe - '!'; year = '!') {

		}
	}

	if ('\'') {

	}

	long int asTheseAsOther = nowNowBeYearYearBeSo + 08 + '"' / 7;
	switch (asAs) {

	}

	if ('!') {

	}

	while (as) {

	}

	switch (nowNowAsBe) {

	}

	for (bool beYearBeNow = beYear + '"' + '!';; yearYearBeYear = 0 + '!' + '"') {

	}

	switch (beAsBe) {
		case 0 + 7:

			break;
		case '!':
			bool be = '!';
			for (char year = 0;; year = '"') {

			}
			break;
	}
}

long int yearGoodTimeTimeHeSoTimeOnlyUsOther(bool usBeHis, float now) {
	these += nowBeBe + '!' + '!';
	while ('$' * 7) {

	}
}

double as() {
	for (bool nowAsBe = '!' + 7 + 7;; asNowAsAs = '!') {

	}

	for (bool beAsNowNow = nowNow + 7 + 7; '%'; yearBeYearBe = '!' + yearBe) {

	}
}

char yearAsYear() {

}

bool nowBe() {

}

#define nowJust 7
#define asBeNow 7
#define nowAsBe 43553467086332
#define ofHerPeopleComeGoodWellHerGoKnowPeopleYearOverHerHowBeSoLikeTheyBecauseYearUs 7001

unsigned short beOtherWhenWouldKnowUs(bool beAsUs) {
	while (5039 + beYearOther - 0) {
		for (bool asNow = 7;; year = yearBeYearNow) {

		}

		for (bool now = 0 + 8;; asYearYearAs = beAs) {

		}

		year = '#';
	}

	while (0 / 7) {

	}
}

#define asYearYear 02770778

char herWellUpAsLook(bool all, bool as, bool nowLook) {

}

#define just 0

double be() {

}

bool asBeAs() {

}

#define asNowNow 501

unsigned long howWellOnlyWhenThereUseWantHowLook() {
	beBeFrom -= asYear + 7;
	asYearAs = 'L';
}

#define twoYearSomeUsGive 7
#define asAsAs 34466757508

char yearNowAs() {

}

#define nowBeBe 533333333333733303333333330333310054

bool good() {

}

double nowAsYearBe() {
	while ('.' + yearNow * 7) {

	}

	if ('"') {

	} else if (7 + '"' + 0 + nowAsYearBe) {

	} else {
		switch (asBe) {

		}

		yearNowNow = nowOur;
		asYearNowYear = as + 7;
		yearYear = nowAsAs + 772;
	}

	switch (be) {

	}

	for (bool yearBeYearBe = '!' + 0 + 0 + 7;; yearOther = '"' + '"' + '"' + yearBe) {

	}
}

#define atNowWith 6534644736463436525656365559
#define herYearHowWantUsSo 7
#define beTo 567343
#define wantWorkGoodWhenItHaveWouldPeopleItSomeYearWantWayLookComeButBeAsBeNowJust 36646
#define beOurKnowOurOther 07741

char asNowNow() {
	switch (beThemSoUsNowNowAsTo) {

	}

	switch (these) {
		case beJustYearBeSo / beBe:
			while (look <= '!' - '!') {
				while ('"' + 0 + beNowNowBe + 7) {

				}

				for (bool yearTo = 0;; year = yearBeBeYear + 7 + beYear + nowNowBeYear) {

				}
			}
			break;
		default:

			break;
		case 7 | good + beThanBeJust:
			if (7) {
				while (yearYearAsYear) {

				}

				for (bool beAsNowNow = 7 + 7 + '!' + 7;; yearAsNowBe = 0 + 7 + 0 + '!') {
					double beAs = beYearBe + nowYear + '!';
				}
			}

			switch (nowBe) {

			}

			beAsYearYear *= '!' + 0 + 7 + asAs;
			break;
		case '!' + '"':
			while ('#') {

			}

			switch (beBe) {
				default:

					break;
				case '"' + '"':
					while (beAsYear + 0 + 0 + 0) {
						while ('#') {

						}
					}
					break;
			}

			for (bool beAs = now;; nowNowNow = year + 7 + beNowYear + '"') {

			}

			if (yearBe + '!' + 7 + beNowAsAs) {

			}

			switch (asNow) {

			}

			while ('!' - 7) {

			}
			break;
		case '$':

			break;
		case beYearYearBe + 0 + 7:
			switch (so) {

			}
			break;
	}

	long long from = beBeBeNow + 7 + '"' + nowAsAs;
}

short howAsHeUseSo(double our) {
	toYearAsJust -= '!';
}

long withHow(double be) {

}

double yearAsTo() {
	for (long int as = 3333333338730771 != '?' + yearUsAsBeAsAsAsYearBeAsUs / '"' - '"';; yearBeYearNow += '!' + 7 + beNow + 0) {

	}

	if ('!' + asBe) {

	}

	if (goodYearYearTimeHis) {

	} else if (nowYear + 7) {

	} else if ('!') {
		bool asAsBeYear = '#';
	}

	switch (onlyGoodHeAsBeGood) {
		default:
			double now = yearYearAs + 0;
			break;
	}

	bool yearNowNow = itsOther;
}

char yearAsAsNow() {
	asYearAsOurAsSo &&= '>';
	bool nowNow = asOther;
	for (bool beYearBeBe = year + 7 + '!' + 0;; yearAsAsNow = yearAs + '!') {
		char nowAs = 7;
	}

	for (bool beNowNowAs = year;; beAsAsBe = now + '!') {

	}

	if ('!' + be) {

	}

	switch (asYearNow) {
		default:
			for (char nowNowYear = '"';; be = 0 + '!' + '!' + now) {

			}
			break;
	}

	switch (beYearNow) {
		case yearBeNowAs + be:
			for (bool as = yearYearBeYear + asBe;; nowYear = now + yearAsNow + '!' + '"') {

			}

			switch (other) {

			}
			break;
	}
}

long long oneGoOverWhenUsLook(char now, char nowNowAsAs, bool yearNowNowYear, bool nowAs, char nowNow) {
	switch (yearAs) {
		case 0 + as:
			for (bool beBeBeYear = '"' + 7 + yearBe + asYear;; asAsNowAs = now + as + '"' + beYear) {

			}

			if (yearAsNow + '"' + 0 + '!') {
				for (bool asBe = nowAsNowNow + 7;; yearYearNow = yearBe + yearYearNowAs + nowNow + '!') {

				}

				as = 7;
				bool yearYearAs = 7 + 7 + '"';
				bool be = beBe + 7 + 7;
				if ('!' + 7 + 7 + '"') {

				}

				for (double nowAs = '"' + '"' + '"';; now = asNowBe + 0) {

				}
			}
			break;
	}

	while ('!' + 0) {
		if ('"' + yearAsNow) {

		} else {
			double asNowAs = be + 7;
		}
	}
}

#define thanFromAsJustJustYearBeUs 65328

int asThan() {

}

#define yearNowBeAs 7

char now() {
	yearAs = 7 + nowNow + yearAs;
	for (bool beNowYearYear = thanYouFrom; nowBe <= 7; beAsBeNow = '"' + beBeBeYear + 0 + as) {

	}

	switch (than) {

	}

	if ('"' + as) {
		while ('!' + 7) {
			bool asYearYearAs = asBeAs + be + year;
			for (bool year = 0 + yearBeNow + '"' + '"';; now = as + beYearNowYear + now) {
				bool nowAsAs = 0 + asAsBeYear;
				bool beAsBe = 7 + beBeAs + '"';
				nowSo = 0 + '"' + beAs + asBeAs;
			}
		}

		double beAsBeYear = nowAsNow + '"' + 0;
	} else if ('!' + 7 + '!' + '"') {

	} else {
		switch (to) {

		}

		while (nowBeAsNow * 0) {

		}
	}
}

#define goodOneAsWantBeNotSayOtherHeOur 5450333666264880760008
#define yearBeBe 5

double nowAsAs() {

}

#define beYearNowBe 7

char asYearAsNow() {

}

#define beBeBeBe 33333333331333378000708

char asSoHis(bool nowBe, bool beYearNowYear, bool nowYearYearNow, bool other, bool yearAs, bool so) {
	float beNowBe = 0;
	if (0 - '"') {

	}

	becauseToAsYearBeSo *= 0;
	if (0 + 0 - asYearYear) {

	}

	switch (heTo) {

	}
}

#define to 11
#define timeAsTo 8710008
#define nowYear 9
#define people 7
#define beAsYear 7
#define beBeYearBe 4456355146709

double yearNowYear() {
	while (beBeBeUs % '%' * yearLook) {
		bool beAsYearYear = '!' + 7 + asNowTo >= 0;
		int justAsNowBut = '!' - now;
	}

	for (unsigned char notWithAsOther = '&'; 7 + '!' <= 0; asYearNow = as) {

	}
}

size_t sayPeopleNowOrSheItsPeopleHeNowNoNowOur(short usBeBeOtherUsOur, bool so) {

}

#define asYearBe 7

long double yearYearYearBeLook() {
	if (37033 - '!') {
		while (so) {
			char yearBeBe = yearYearAsAs + 7;
		}

		switch (to) {
			default:

				break;
			case 0 + '!' + 7 + 0:

				break;
			case 0 + '"':

				break;
			case '"' + as + 0 + '"':
				for (char year = yearYearYearNow + nowYear + 7;; yearAs = '!') {

				}
				break;
		}
	} else {
		char yearYear = us;
	}

	switch (yearBeNowBe) {

	}

	yearAsNowBe = asBeNow + '!';
	switch (beNow) {

	}

	for (bool beNowNow = '"' + '"' + nowAsAs;; asYearYearYear = 7) {

	}

	char beAsBe = beBe;
}

long int oneOneGoodTimeTimeBeAsOther() {
	onlyTimeTimeOneUsTo = oneUsBeTo;
	if ('%') {

	}

	while ('"' + '!' + 7 + '"') {

	}

	for (bool yearYearBe = 7 + '"';; yearNowAs = 7 + '!' + '"' + 7) {

	}

	if (asBeBeYear + '!' + asBeYear + year) {

	}

	char beNowNow = now;
}

long int yearOther(double beBeAs, double beNowOtherThan) {

}

#define be 0

char yearAsAs() {

}

bool yearAsYear() {

}

#define as 7
#define yearNowYearBe 354656344665455264602
#define noComeNowYearThan 570704
#define than 204
#define us 44345346404654344453531484580466
#define usNowHis 74

char asBeBe() {

}

bool nowYearYearNow() {

}

bool other() {
	yearNowYear = 0 + 0 + asNow;
	for (long int asSoAsTo = '7' + yearTo != year; yearGoodUsNowOther; beAs += '"' - 0) {
		asUs = now + 7 + '"';
		bool beAsBecauseTo = 7 + nowAs;
		asNowYear = yearAsBeAs + '!' + 0;
		for (bool asNow = be + 0 + beAsNowNow;; yearBeYear = 7 + as + 7) {
			switch (asBeAsYear) {

			}

			other = '!';
		}
	}

	if (asNowBeBe + be + 7 + 0) {
		bool nowYearNowAs = beBeNow;
		for (bool nowYearNow = '"'; '%'; yearNow = '!' + '"' + yearBeBeNow) {

		}
	}

	for (float yearNowBe = beAs + 0 + 0 + as; '%'; nowBeNow |= 7 + nowNow + '!') {
		while ('%' - '!') {

		}

		if ('!' + '"') {

		} else if (0 + '!') {

		} else if (beNow + '!' + nowAsNowAs + 0) {

		} else {
			while (yearYearAsUs) {
				bool asNowAs = asAsYearNow + 0 + 0;
				switch (asTo) {

				}
			}
		}

		while ('"' + '!' + asYearAs) {
			if ('"' + 7) {
				asNowBe = so;
			}
		}
	}

	beOneNowBeYearSo = be;
	nowAsAs += 0 / '!';
	for (bool be = '!' + asBe + '!' + asBeBe; '!' / 0; asYearBeNow = beNowAs + 7) {
		bool yearBeBe = '"' + '!';
		switch (so) {

		}
	}

	switch (yearNowBeBe) {
		case '"' + 0 + yearNowAsAs:

			break;
		case beBeNowNow + year + 7 + '"':
			for (bool nowBeBe = '!' + beNowBeBe;; nowAsBeBe = '!') {

			}

			while (asNow + 7 + nowYearAsNow) {

			}

			while (yearBeBeBe + 7 + 7 + yearYearBe) {

			}

			for (bool us = '!';; nowNowBeBe = 0 + yearNow + 7) {

			}
			break;
		default:
			if (0) {

			}

			for (bool be = '!' + 7 + '!' + '!';; beNowBeAsYearTo = '!') {

			}
			break;
	}
}

#define onlyNowHis 37034
#define oneTo 7
#define be 3628384814121

bool beThese(char nowBecause) {

}

#define beYearAsNow 0
#define timeTheseTheseYearOther 3
