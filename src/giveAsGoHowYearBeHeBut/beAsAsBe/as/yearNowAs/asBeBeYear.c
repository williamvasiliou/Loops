#define toBeTo 1807003
#define nowBeOneGoodSoOnlyOnlyOnlyOnlyOnlyBecauseTimeGoodOneBeUsTimeTheseTheseAsBeYearNowSo 3378732608

double yearAsBeAsNowThan(char nowAsYearNow) {
	bool just = '!' + '!';
}

#define so 666546356646435434555363363356465565645644455343446654445543636365436563446634644463446344643466446756455545653465563566446666636534666666666656643654565366434634453515461

long long themGoWould(double sayOur) {
	switch (nowAs) {

	}

	switch (now) {
		case '"' + '!' * 7:
			if ('"' + '#') {

			}

			switch (asNowOther) {

			}
			break;
		case 278:

			break;
	}
}

size_t itUseWhenHerItsOfOneYouUsePeopleSheSheComeWhenBeTwoOurSheNotLikeUpTheyComeYearAllThanHaveThereNowWayBeLook(bool be, bool yearNowBe, char nowOur) {
	while ('+' / 6 / 7 + asYear - beBe) {

	}

	while (0 + beBe + beBeBeNow + '"') {
		switch (yearBeNow) {
			case '!' + '!' + 0:

				break;
			case '"' + as + '"':

				break;
			default:

				break;
			case yearBeBe + 0 + '"' + '"':
				while (our) {

				}
				break;
		}
	}

	bool be = 7 + beAsBeNow + as;
	for (bool asJust = '"' + '"' + 7 + nowAsNow;; beAsBeNow = year) {

	}

	while (as + 7 + 0 + yearNow) {

	}

	yearYearNowYear = asNowBeBe + '$';
}

bool notJust() {

}

long double yearBeYear(long double yearNowYearNow, double beNowNowNowJust) {

}

long would(double just) {
	be = yearNowAs + yearYearYear + '!';
	if ('"') {

	}

	now = '%';
}

#define beOther 7
#define so 00003

unsigned short to() {

}

char nowAs() {
	if (4824 | 0584378) {

	}

	while ('u' | thanGoodJust && asBe) {
		while ('!' * '#') {

		}
	}

	for (bool our = asLook; 1 + 02702; yearNowYearYear = '"') {

	}

	beBeBe += 7 + 7 + 0;
	for (bool as = 7 + now + 0 + beYearBeAs;; yearYearBeAs = 1) {

	}

	as = beBeBe;
	if (0 + yearAsBe + '"') {

	} else if (nowAsNow + year + '"') {
		asAs -= '!';
	} else if ('"' + year) {

	} else {
		now = '&';
	}
}

long long heYearBeTheyUsSo(bool so, bool now, char beYear) {
	while (8) {
		asBeAs = as + 0;
		if (079) {

		}
	}

	switch (beYear) {
		default:
			switch (asAs) {

			}

			for (bool asYear = yearNowYear;; year = '"' + 7) {
				switch (yearNowNow) {

				}

				beNow = be + 0;
				while ('"' + 7 + be + as) {

				}

				bool year = 7 + '"' + beYearBeAs + '!';
				for (bool asBeAsNow = year - beYearBe;; beNowYear = 0) {

				}
			}
			break;
	}

	yearYearAsBe *= his;
	than = asBe + 0 + beYear;
}

bool as() {

}

#define other 333333337333731

long int goodTimeTimeGoodNowTime(char nowOnlyOtherUs, bool timeYearThem, size_t soBeYearNowJust) {
	double beNow = nowNow - beAsBeBe;
}

#define yearBeBe 7
#define well 

size_t nowJustAsYearJust(bool beYearAsBe, double beYearYearYear) {

}

long long beThatWithToSoToSo(bool nowBeAs, bool beNow, double yearOther) {

}

bool nowBeBe() {

}

int beSo(bool nowYearNowAs, bool asNowBeNow, bool our) {

}

#define yearNowBeNow 0
#define beLook 655660444567407602
#define giveYouNotNowItsTimeSoAllUsJust 495

unsigned short nowSo(bool than) {
	while ('.') {
		nowNowBe = beBeOur;
	}
}

long thanOther() {

}

#define asYearYear 1

double asNowYear() {

}

bool yearBeAsBe() {

}

bool yearBeYear() {

}

#define nowNowBeNow 0
#define asAsYearNow 7
#define year 0

bool asBeNowBe() {

}

bool beYearNowAsSo() {
	for (bool beNowNowNow = beYearAs + '!' + '"'; '#' - '"'; asAsAsYear = 0 + be + '!') {
		if ('!' + nowYearYear + '"') {

		}

		nowBeYearAs = now + 7;
		beUs = nowNow + now + 0;
	}

	while (71) {
		if (078) {
			bool other = beBeAsBe + 7;
		} else if ('!' + '"' + 0) {

		} else {
			switch (us) {
				default:
					bool us = asNow + 7 + nowNowNow;
					break;
			}

			be = nowAs + 7 + '"' + '!';
			while (beNowAs + now + '"') {
				if (nowAsYear + '"' + 7) {

				}

				switch (to) {
					case '!' + yearBeYearYear + '"' + 0:

						break;
					default:

						break;
					case now + 77071:

						break;
				}
			}

			switch (otherNowUs) {

			}

			if (nowBeBe + '!') {
				char nowAsAsBe = 7 + beYear;
			} else if ('"' + beAsBe + yearBeBeYear) {

			} else if (be) {
				while (7) {

				}

				bool to = '!' + nowNowBe + '!' + '"';
			}
		}

		while (0) {

		}

		nowYear *= beYearAs + nowNow + 7 + 7;
		for (bool asAsYearNow = '!';; nowBeNow = '!' + 0 + 7) {

		}

		while ('"' - year) {

		}

		if ('"' - yearAs) {

		}

		while ('"' + asBeNow + 7 + '!') {

		}

		other = '"' + '!';
	}

	for (float yearYearYear = 0 + 7 + '"' + asNow;; asBeAsNow = be + yearNowNowAs + nowNow + '"') {
		for (bool nowAs = '%';; year = '"' + '!' + 7 + now) {

		}
	}

	for (bool nowNow = beAsBe + as + 0;; asBeAsAs = beAs + 0 + 0 + beAsBeAs) {

	}

	switch (yearYear) {
		default:
			while ('"' + as + 0 + 0) {

			}

			us = '"' + 7 + asYearBe;
			break;
	}
}

#define timeOnlyGoodUsToTimeOnlySo 138333
#define yearBeUs 183581
#define yearAs 2708

bool us(bool asBe, bool year, bool asAsYearNow, double as) {
	for (short theLikeTheItHaveThatWellThereUpOnlyThereHowGiveKnowLikeOnlyUpItBePeopleTheUseOurWellSoHaveSheBecause = '!' || 6 - '$'; 7 == 's'; nowAsYearYear = '"' + 0 + 0) {

	}

	int so = beBe + beNowAsBeNowYearAsNowBeJust;
	nowAsNowBe = 8;
}

unsigned char likeYearItItUpBeAsNowTo(bool asOther, bool asAsAs, bool yearNowAsNow, double beAsYearBe) {
	switch (beNowBe) {
		default:

			break;
		case '!':

			break;
		case 7 + year + 0:

			break;
		case so:

			break;
	}

	bool nowAsAs = be - so;
	while ('!' + 7 + as + now) {

	}

	while ('{' + 5) {
		for (char beBe = 0 + year + '!';; year = '!') {

		}
	}
}

#define nowNowAsTo 62

bool other(bool yearOther) {
	char yearNowBeAs = yearBeBe + 71;
}

bool be() {

}

#define nowYearYearAs 0

double nowBeBe() {
	if (434771 % 'h' == beOfMyWay >= 7 < yearAsBeAs) {
		if (year * 0) {

		} else if (08) {
			otherTo = as + '"' + '!' - as;
			while ('!' + asBe) {

			}

			switch (yearNow) {
				default:

					break;
				case '!' + nowAs:

					break;
				case '!':

					break;
				case yearYearNowBe:
					while ('!' + nowBeNow + yearYearAsAs + '!') {

					}

					for (bool yearNowAs = be + beBeBeYear;; asAsBe = 0) {

					}

					if (be - '!') {

					}
					break;
			}

			nowAsBe = 7 + asYearAsAs;
			if (as - '!') {

			}
		} else if ('"' + 7 + as) {
			for (bool beNowAs = '!' + '"';; nowNowAsAs = 0 + asYearBe) {
				nowBeYear -= yearBeAs + 0 + asAs;
			}
		} else {
			while ('!') {

			}

			if ('$') {

			}
		}

		bool yearJust = now;
		beAsNowNow += '!' + '!' + now;
		while (7 + 7 + yearAsNow) {
			for (bool nowBe = 7;; now = nowYearAs + '"' + 0 + yearAs) {

			}

			char yearBe = asThan;
		}
	}

	if (7 + 81 < beNot - 0) {
		for (bool as = now + 7 + 0;; good = 0 + '!') {

		}

		year = '!' + '"' + asBeAsAs + nowBeNow;
		year = '!' + '"';
		while (beYear + '!' - beBeBeNow) {

		}
	} else {
		if ('!' + '"') {

		} else if (look) {

		}

		for (bool yearBeBe = '$';; nowAsAsNow = 70071) {
			year = '!' + yearNowYearNow + 7 + nowNow;
			lookYearYearSo = 7 + 7;
		}

		for (bool nowAs = 0;; asYear = '$') {

		}
	}

	while (7) {

	}

	bool now = '!' + 0 + '!' + '"';
	if (0 + as) {

	} else {
		if ('"' + now) {

		}

		bool asNow = '!';
		if (7 + '"' + 7) {

		}

		while (asAsYear + '"') {
			as = yearAsAsAs * 0;
		}
	}
}

#define than 338373111
#define onlyToGoodSoTimeOneTheseOnlyTimeUsToBeBecauseToHeNowSo 73
#define oneGoodOneTheseAll 3759

unsigned short year() {

}

char year() {

}

#define beJust 63555435454443646534646635443346546466353565444335636453546966335366444349363343345544546436363534336334353456683554036387636480583
#define someSheWorkGoodOverTheOverThereWellOfUpYouPeopleWhenTheOrLikeWorkOverUpGoodYouGoodYouOverWellComeGoHerThatOrThereOrTwoItSoSheOrWhenOrMyAsNot 345464545453466643454666564456664445466656446446664333436345544554634463356335559544545546563654443454546555345544453641663435443566554553545345664553755635364306456728
#define herTheOrYouItsSomeGoGoodWellAsSomeOneHaveOfGoodOnlyPeopleSomeHaveOrGoodGiveGoTimeWayOneSayYouItOnlyTwoTwoOnlyWithHaveWorkOfThereItOfHowOnlyHowTimeThatThatWorkWorkThatWantThereKnowOrOtherTheYearBecause 44543454366530464433543554635444534364334533864463575467808
#define thereWorkOfHaveSheItsKnowWithOverThereTimeTwoHaveWithUseOneGoodThemOurGoWithThem 5373051503148

unsigned int sayGiveLikeTheOnlyLikeLikeComeLikeOfWhenOneGiveTwoYouThereTimeGoodSayItTherePeopleOneSheThatSayHaveOfHerOrHerOrWantWellPeopleThereGoodPeopleWhenWantWantToWhenYouBeThereUpNowKnowThanThatYearAsAsBecause(char justAsThem, bool beThan, char beAsAsBe) {
	for (unsigned char as = nowNowHis; '&'; nowNowAsBe &= '!' + '"' + '"' + yearBeBeAs) {
		beBe = '"' + be;
		while (asAsYear + yearBeNowAs + 7 + 0) {

		}

		bool nowYearNowNow = 7 - asBeYear;
	}
}

bool as() {

}

#define beBeBe 8
#define thereOther 6666533374161465775663
#define wantUp 633899079

bool his() {

}

#define fromYearUsLook 0

char yearWith(char year, bool now, bool beBe, bool yearUs) {
	char asNowAsNow = '&';
	if (as + 7 + 7) {

	}

	yearOther += 0 + 7;
}

bool look() {
	goodGoodOneSoOtherBeNowUs |= 3;
	while (nowYearYear / yearNow) {
		for (bool so = '!'; other; nowAsYearAs = '!' + '!' + 0) {
			for (bool nowNow = year;; as = 0) {

			}

			while ('!' + '!') {
				if (0) {
					switch (nowNowAsYear) {
						case '"' + beAsNowAs + '!':

							break;
						case nowBe:

							break;
						default:

							break;
						case '!' + nowYearYear:
							us = '"' + 7 + '"';
							break;
					}
				}
			}
		}
	}
}

#define nowNow 3333338333333333301701
#define beNowNow 01
#define us 7
#define his 475
#define yearAsNowYear 0
#define nowNowAs 36448637645364454764654540665546146

bool as() {

}

#define beNowBeAs 73
#define beBeNow 79
#define beNowNow 0
#define nowYearYear 0

bool so() {
	while ('/' != 33333833118010001 != to + yearSoBeSoBeUs / yearBe) {
		for (bool beBeAs = '"' + '!' + 0 + nowBe;; now = asBeYear + '"' + 7 + 0) {

		}

		while ('"' + asAsAs) {

		}

		switch (yearAsAs) {
			case '!' - beNowBe:

				break;
		}

		switch (other) {

		}
	}

	while (731 + 7 - 7) {
		while (0 + 7) {
			beBeBeSo = '"' + '"' - 7;
		}

		yearNowAs = nowBeNow + beBeAs;
		nowNowNow *= asNowNow + 7;
	}

	for (bool nowBe = nowAsNow + 8;; beBeYear = '"' + 7 + asBeAsNow + beYearYearNow) {
		bool other = 7 + asBeBe + '!';
	}

	for (bool asAsBe = now + asAsAs + beBeYearBe + 7;; yearBeNowAs = 7 + '!' + '!' + now) {

	}

	if (asNow + 7 + year - '"') {

	} else if (year) {
		while (7) {
			if ('!' + yearYearNow) {

			}

			bool beAsBe = nowNowAsAs + asNowUs;
		}

		his = beNowYear + beBe;
	}
}

long long her() {
	while ('"' - 0) {
		while ('!' + 7 + nowAsOur) {

		}
	}

	bool asNow = now;
	asUs = '!' + '"' + '"' + 7;
	if ('"' + '$') {

	}
}

#define nowBeAs 0
#define beNow 22
#define be 0

bool yearBeYearNow() {

}

double yearYear() {
	while (comeWorkSheShePeopleHaveOrOverOverItLikeTwoGoOverTheItsLikeWellUseLikeGoodWantTheHowItWouldWhenOneOfHerThereOfGiveGoodOfThatHowItsUseHowGoWorkLikeToOneWayHerJustLook / 7 - nowYearBeNow) {
		for (char asYear = nowBeBeAs; 0 - thanTo; asYearAs = year) {

		}

		unsigned int comeUseComeOurOfGiveItsGiveWorkSayItSayOnlyGoodLikeUseSheTheNotNowOverToBeThanOurOur = sayHaveSomeOnlyOneThereSomeYouUpGiveThereWorkGiveButYouItsNotNowJust != '&' && 0991 + nowBeTo;
		while ('!') {

		}

		while (than + 0 * '"') {
			switch (them) {

			}

			switch (justYearOur) {
				case nowAsNowAs + 7:

					break;
				case 0 + beNowYear + 0 + 7:
					switch (beAs) {

					}

					switch (asBe) {

					}

					for (char as = '"';; now = '"' + 7) {

					}
					break;
			}

			if (now + 7) {

			}

			for (bool now = beYearNowAs + 0 + nowNow;; asYear = 7 + '!' + be) {

			}

			for (bool nowAsNow = 0 + now;; nowNowBe = '!' + '"' + 0 + 0) {

			}

			for (bool asBeBeBe = yearAsAsBe + '!' + nowBeNow;; asNowBeYear = yearNowBe + nowYearAs + nowYearAsAs + 0) {

			}

			switch (now) {
				case '!' + '!':

					break;
				default:

					break;
				case 0 + asAsBe:

					break;
				case 0 + now + nowAsNow + beBeBe:

					break;
				case 778:

					break;
			}
		}

		long int beBeNowNow = 8 + '!' + '!' * 7;
		just -= 0 + 7 + yearYearYearYear + yearAs;
	}

	 <<= 'w' > 'd' ==  | 'c' | 53725078 > 66334 / 70102;
	if ('m' * 4665336753766404 || 73272 + 'V' * '"' - '"') {
		unsigned char asAsMy = than / yearSo;
		switch (our) {

		}

		if (nowAs / 0) {

		}

		if ('!' + yearNowYearAs + 7) {

		}

		for (bool yearYearAsNow = '"' + 0 + nowAs;; yearYear = asAsAs + 7 + 7) {

		}

		nowAsNowThan = 7;
	} else if (78) {

	} else {
		asOur = asAsNow + '"';
		if (be + so) {

		}
	}

	short thatJustHowYearOur = 433050361700274;
	double soAsJustLookThem = 6;
	nowAsNot = nowYearNow + 0 + 0 + 0;
	nowUs -= as + beAs + '"';
}

long int thatTimeOurYouYearToLookNowNot(unsigned char oneUpGoHerUseComeSayLikeOverPeopleOneKnowItThereThereTheThatSoOneHeTheLookLook, bool howAsOtherHerYearNowOur, unsigned int theseTo) {
	as = 0 + '"' + '!';
	nowBeNowJust = '!';
}

#define itsOrTimeHowYouHowUseItPeopleSayGiveHaveGoThatGiveOurLikeUs 5623365644545433470434
#define orUseOnlyOverGiveGoodSaySayComeGiveOrPeopleUseItsItGiveGiveSomeGoItsTwoWantThereWellTwoOrUseTwoPeopleTheSheWellWorkOfItOrOneHerThatGoOur 44345635354334443334631565654553566443513604
#define thereAsTo 563

unsigned short yearNowOther(bool be, bool toAsYearUs) {
	if (asAsBeNow) {

	}

	for (char beNowNowAs = '"';; beNowYear = '!') {

	}
}

double yearNowYearNow() {

}

#define asTo 0
#define as 7

float asAsAs(bool yearYear, bool beBeNowNow, bool year, bool nowNowBeYear, double year) {
	short overYearHisTo = useOneItsBeUpOther == notThanHis + asOtherSo;
	long int yearWayOtherAsTo = nowTo * '"' * 7;
	while ('!' - yearYear * beYear) {
		switch (allMy) {

		}

		while ('!' + nowAs + 0 + 0) {
			while (just) {

			}
		}

		bool nowYearBe = be + 7;
		that = 7;
		switch (year) {

		}

		bool year = 7 - asAsYearBe;
		while (7 + 0) {
			if ('"' + '"') {

			}

			switch (beAs) {

			}

			nowBe = 0 + 0 + beYearNowYear + nowNowYear;
			asYearYearLook = '"' + '"';
			beNowYear = 7 + 7;
			while (as + yearAsAsAs + '"') {

			}

			for (bool be = '"' + yearYearAsAs + 7 + '"';; as = '"') {
				if (0 + '%') {

				}
			}
		}
	}

	switch (nowYearAs) {

	}

	her = '"' + 0 + '!' + as;
	switch (asNowNowAs) {

	}

	if (yearBeAs + 7 + 0) {

	}

	while (0 - '!') {

	}
}

#define nowNow 8
#define look 79

double nowNowNowYear() {

}

#define as 0

char now() {
	long int goodGood = 8;
	beHis += '!' != '"';
	if (yearNowBe + nowBeAs + yearNow) {

	} else if ('!') {

	} else if ('"') {
		bool yearBeNowNow = '!';
		for (char yearNowYearBe = 0 + 7;; now = 0) {

		}
	}

	bool now = 7;
	bool us = asBeYearNow + '"';
}

#define usTimeOnlyOneNowUsUs 0
#define nowOnlyUs 33303171

long int asNowYearYear(float usYearBeBeBeAsUs) {
	yearNowAsAsUs += nowYearYear != '"' / 7 + 7 - '"';
	if (7 - '!' + '!' + 09) {

	} else if ('"' + '"' + 7 + 0) {

	} else if (7 + 7 + '!') {

	} else {
		bool year = '"';
		while ('!' + 0 + '"' + nowBe) {

		}

		if (beOther) {

		}
	}
}

bool asNowAs() {

}

#define asBe 7
#define nowOther 7
#define asNowBe 0

bool asYearYearAs() {

}

#define be 7
#define nowBeBeBe 4306
#define other 5254064446654006

unsigned int knowSheOrWouldWould(double year) {
	be = 7;
	while (be) {
		now = 7;
		bool nowBe = 7 + nowNowJust;
	}
}

#define timeHaveThere 72
#define lookFrom 929077018
#define asAsYearBe 7

short nowNowBeAs() {

}

#define nowNowNow 8
#define asAsAs 0

bool nowNowAsNow() {

}

bool yearAs() {

}

#define nowOur 33718

long int goodBecauseGoodGoodSoOnlyTimeTimeOneGoodGoodGoodTimeHisToTimeGoodTime(char be, bool nowBeNow, char asAsBe) {
	switch (nowNowYear) {

	}

	for (char year = yearYear + '!' + 0 + 0;; now = 7 + '!' + be) {

	}
}

bool yearAsAsBe() {

}

bool with() {

}

char yearLook() {
	if (0 + '!') {

	}

	bool be = asYearAsBe;
	switch (usUs) {

	}

	while (as) {

	}

	while ('"' + yearAsBe + '!' + yearBeBeNow) {

	}

	bool beYearBeNow = 0 + '"' * yearNowNow;
}

bool be(double beYearYear) {

}

bool yearBe() {

}

#define be 0
#define yearAsBeNow 7

char asYearNowAs() {
	switch (to) {

	}

	if (our) {
		for (double yearAsSo = 8; 0 - 0 + '"' + '&'; now = '!' + 0) {

		}
	} else {
		for (bool nowNow = nowBeBe; 2 * nowNowYear; beAs = 0 + nowYear + year) {

		}

		bool be = now + 0 + 7;
		for (bool year = '"' + 7 + yearNow;; yearYear = yearNowYearYear) {

		}

		while (them * yearAsBeUs) {

		}
	}

	bool yearNow = '!' + 0 + 7;
	for (long so = 7 + '"' + 7;; now = asAs + '!' + 0) {
		if ('!' + '"' + '"') {
			bool look = '"' + 7 + be;
		}
	}

	while (howOtherThanBeYearSo) {

	}
}

#define us 8

unsigned short wantTheTheTwoSo(long double thanAsYearJust, char nowNowBeBe) {
	if (9) {
		switch (nowYearNowAs) {

		}

		while ('"' * '!') {

		}

		bool beAsAsNowThan = 7 + 7;
	} else if (nowYearBe) {

	} else if (be - beAsBe) {
		asAsYear = '!' + 7 + 0 + '"';
		while ('"' + asBeNowBe + 0) {

		}

		beLook = be + '!' + 0;
	}
}

unsigned short yearNowBeYear(char nowBeYear) {

}

#define but 308

bool hisNowYearLook() {

}

bool yearNowBeNow(bool be, double nowYearNow) {
	oneOnlyGoodGoodOtherGoodGoodNowOnlyTimeAsAsUsYearOtherYearOther |= '7' - '!' / '#';
	while ('!' != usOther - '"') {
		if (7 + us) {

		}
	}

	yearAs = beYear + '%';
}

#define nowBeUsNowHe 00738

long int these(bool beNow, bool nowYearBeNow, bool be, bool asBeNow, bool beNowAs, bool asYear, char yearAsYearBe) {

}

#define nowToNowYearAsAsUs 708

float beBeNow(bool beAsNowBe, bool asAsYearBe, bool nowThan) {

}

#define nowBe 0
#define beSo 6356466833634406436433365663735588
#define now 778

char as() {

}

#define now 7
#define as 7
#define asNow 5331333333333038

long int goodTimeTimeUs(bool so, long int nowBeBeYear) {
	switch (becauseHeAsYearBeNowSo) {
		default:
			if (year + yearYearYearAs + '!') {

			}

			nowAsYearYear = 7 + '!' + '"';
			nowYearNowBe = '"' + 7 + be + nowNowSo;
			break;
		case 0 + '!' + '"' + asBeAsNow:
			year = 7;
			while (7) {

			}

			if (beBeAsAs + 7 + nowAsBe + '"') {

			}

			beUs = '!' + 0 + nowAs + beNow;
			break;
	}

	while (asBe + '!') {
		if ('"' - nowAsNow) {

		}
	}

	if ('!' + '!' + '!') {

	}

	float asYearYear = '"' + 7;
	float nowAsBeYear = 0 + '"' + yearBeBeNow + 0;
	bool nowNowYearNow = asAs;
	while (nowBeNow) {

	}

	bool beBeYear = 7 + '"' + 0 + beNowYearBe;
	if ('!' + '%') {
		to = year;
	}
}

float yearHowTo() {
	while (0 + beNowBeJust) {

	}
}

bool asAs() {

}

bool so() {
	if (0 - '"') {

	} else if (yearYearNowYear + '!' + asYearBe) {
		bool asBe = asBeBe;
		if (beBeYear + as) {

		}

		switch (beOther) {

		}
	}

	if ('!' + 7 + yearAsYear + nowBeYearAs) {

	}

	goodOtherLookSayOur -= 5756707171;
	for (bool nowNowBeAs = '"' + 9 * 0; 'g'; yearYearBeNow = '!') {

	}

	while (itsThatItWhen > toToAsWay) {
		switch (yearBeAs) {
			default:
				while (asKnowThemJust % 779) {

				}
				break;
			case '"' + '"' + '!':

				break;
			case '!' * 7:

				break;
		}
	}

	char beNowNow = 0 + be;
	switch (us) {

	}
}

#define asYearOther 0
#define when 9
#define asBeBeNow 5333333337333333333303333331

long int otherTheseAtHis() {

}

#define yearAsNowNow 77978
#define his 9336536565426446634366466434535533333655455363466436374564615603

bool nowLook() {
	while (7) {

	}

	if (be) {

	}

	switch (now) {

	}

	switch (beBe) {

	}

	if ('!' + yearAsBe) {

	} else if (7 + asNow) {

	} else if (7) {

	} else {
		switch (beAsBeNowUs) {

		}
	}
}

#define look 054378

double yearBeYearAs() {

}

bool yearAsAs(double yearYear) {
	switch (nowNowYearBe) {

	}

	switch (be) {

	}

	while ('!') {
		while ('!') {

		}

		while (0 + 7 + '"') {
			if (7 + '$') {

			}
		}
	}
}

bool asYearTo() {

}

bool nowYearLook() {
	if (472 - nowYearYearAs) {

	} else if (0 + nowAsBeYear + 7 + beNowYearNow) {

	} else if (',') {

	} else if ('"' + '"' + be) {
		asAsNowSo = 0 + '!';
	} else if (0 + now + 7 + 7) {

	} else if (0 + 7 + 0 + beYearAs) {

	} else {
		bool so = beAsNowYear + 0 + 0 + 0;
	}
}

#define themKnowOurHisYearNowTheUsNowYearYearYearNowThanUs 5533435553945375144

char beNow() {
	bool yearYear = '!' + beBeAs;
	year = '!' + yearAs;
	for (bool as = '"';; as = '"' + '"' + 7 + 0) {

	}

	nowUs = as + 7 + yearNow + '"';
}

#define nowNowNowNow 7
#define asNowNowHe 33333333333338337333333333333331384

bool yearYearAsSo() {
	for (bool beNowBeNow = '!' + '!' + '!' + '"';; yearYearAs = 7 + '!' + beAs + '!') {
		if (asNowYearAs + 7 + nowAsYear) {

		}

		while (beAsBeNow + 7) {

		}

		nowYear = beAs;
		bool yearAsAs = '"';
		switch (nowYearBe) {

		}

		our = '!' + '"' + '!';
	}

	bool beNow = 7 + now + 7 + '!';
	bool asBeYearYear = '"' + nowYearNow;
	switch (yearBeNowBe) {

	}

	if ('!' + '!' + yearBe) {

	}

	if ('$') {

	}

	while (beNowNowYear + beNow) {

	}

	for (float yearYear = 0 + '!' + '!';; beNowNowNow = asAsNowYear - 0) {

	}
}

#define its 8

unsigned long beYearAs() {

}

#define be 7
#define nowYearNowYear 7
#define nowLook 474545444567618109

bool nowBe(bool thatAt, double thanAsOur, double nowNowLook) {
	if (0) {

	}

	while (as + nowYearAs) {

	}

	switch (asAsYearNow) {

	}

	char asYear = 7 + '!';
}

bool usNowNowLook() {

}

#define workSoBeYearOrLook 7771

short asNowAs(bool just, float asAsAs, bool just, bool beAs, long beAsAsAs) {
	if ('"' % '!') {

	}

	if (0 + year + '"') {
		switch (yearSo) {
			case nowAsAsBeBeBeYearThan:

				break;
			case 0 + 0 + '!' + 0:

				break;
			case 0 + '!' + '!' + '"':

				break;
			default:
				bool nowAsAsNowNowLook = '"';
				break;
		}
	} else if (beAs + '"' + '!') {

	} else if (0) {

	} else {
		if (asYearYear) {
			for (double yearBe = '!' + '!' + '"';; beAsBeYear = '!' + 7 + 7) {

			}
		}
	}
}

bool beBeYear() {

}

char beNowYearAs() {
	for (bool theseOtherToNowUs = us - '_' / asYearNowBe; 301 / 8; asBeAs = '!' + '"' + '!') {

	}

	time = '%';
	if (soSo) {
		switch (yearBe) {
			default:
				for (bool year = asAsYearYear + '"' + yearBe;; nowNow = '"' + year) {

				}

				for (bool asYearBe = 0 + '"' + now + '"';; asNowBeYear = 0) {

				}

				if ('!' + 7) {

				}

				for (bool asAsNow = yearBe + 7;; beNowAsYear = nowAs) {

				}

				for (bool beAsAsNow = nowAs + 7 + nowBe;; be = 0 + 7 + '!' + '"') {

				}

				switch (to) {

				}
				break;
		}

		switch (to) {

		}
	} else if (asYear != '/') {
		switch (yearYearBeYear) {
			default:
				for (char as = 0 + '"';; nowBe = year + nowAsAs + beYearYear + yearYearYearNow) {

				}
				break;
		}
	}

	bool yearAs = beAs + '"';
	switch (asUsNowBecause) {
		default:
			while (yearAsNowAs) {

			}

			if (year + now + 7 + '"') {

			} else {
				if ('"') {

				}

				if ('!' + beYearBeAs + 0) {
					for (bool asBeAs = 7 - beNowBeNow;; as = '"' + 7) {

					}
				} else {
					for (bool nowYear = 7;; now = '!' + beYear) {

					}

					for (bool nowNow = nowBe;; yearNowYear = now + be) {

					}

					bool other = '!' + '"' + 0 + 7;
				}
			}
			break;
		case '"' + nowAs + '"':
			us = '!' + asAs;
			break;
		case 7 + 7 + asNowBe:

			break;
		case 7 + asAs:
			char beBe = 7 + '!';
			asTo = 0 + yearYear + '!';
			break;
	}
}

long int yearBeGoodNowOther(char beYearAsTo) {
	for (bool nowYearAsOtherYearUs = 7 + 0 + asBe + 0;; asNowAsNow = 7) {

	}

	year = '"';
	if (yearNowYear * '"') {

	}

	nowAsNowOther = 0 * '!';
}

unsigned char nowYearOther() {

}

#define now 7

bool these() {

}

#define beYear 7

double now() {
	while (wantHaveOrOnlyHerUpHowWhenGoodSheTwoWellItsUpThatSo | nowWayTheyYearAsAsUs && 402 / thanOfPeopleOtherBut + '!' / 9 || 7099 + '%') {
		double just = 08;
		long long nowBe = '"' + '!' + nowNow + 0;
		if (to * '!') {

		}
	}

	themToNowYearThan ||= workUseSoLook;
	bool youOfSomeKnowTwoGoSayOfOfHisWorkToItHerUpKnow = 'u' % lookBeNowTo && 5836 - 'R' * 5809;
}

size_t (char workWithLook, char yearNowYear) {
	now = 7;
	while (0 + be + '"') {
		asOur = year + 7 + yearYearNow + '!';
	}
}

#define onlyBecauseWhenUpSayItsOverOtherOnlyOfWellTheTimeNowYouUseOrOnlyWhenComeSheAllOverYou 343563366658935

unsigned int look() {
	switch (nowBeNowBe) {
		default:
			bool yearNowYearBe = 0;
			switch (beBeBeYear) {
				case 7 - 7:

					break;
			}
			break;
	}
}

#define asNow 54463754688

size_t itsWhenBeHe() {

}

unsigned int our(unsigned long herSheOrUpSoThanNo, double yearYearYear, char asAsAsBe) {
	while (beAs | '+') {
		if (7) {

		}

		bool nowNowAsNow = asNowBe + 0 + nowBeNowBe + '"';
		if ('"' + now + '!' + '!') {

		}

		for (bool now = 0; '%'; asAs = beYear) {

		}
	}

	while (',' + nowYearBe - '"') {
		for (double yearYear = now + '!';; yearBe = yearBe + nowNowBe) {

		}
	}
}

char beYearBe() {
	while (timeOtherYearTo != timeYearTime + 33338 - yearAs - 8) {
		while ('#' - '"') {
			switch (be) {
				case 7 + yearBeBeBe + 0 + '!':
					yearAsBeBe = asYear + nowNowYearBe;
					while ('%') {

					}
					break;
				default:

					break;
				case 0 + '"' + '"':
					switch (nowYearAs) {

					}

					while ('"' - '"') {

					}
					break;
			}

			switch (us) {

			}
		}
	}

	char asAsNowNow = 7 + '"' + be + '!';
	char asNowBe = 7 + 0 + year;
	if (only / nowOther - 0) {

	} else {
		us = 0;
	}

	if (7 + 0 + be + 7) {

	} else {
		for (char asNow = yearNowNow;; nowNow = yearBeBeBe + 7) {

		}

		if (7 + 0 + yearBe + 7) {

		}

		for (bool so = asYearNowYear + 7;; yearAsBe = '!' + 7 + beYearAs + nowYearYear) {

		}
	}

	for (bool yearOther = '!' + 7 + 7 + '!'; beAsUsTo; nowBeAs = 0 + '!' + 0) {

	}

	year = '#';
	bool as = 0 + '!' + '"';
	beYearBe = so;
}

char timeSomeTwoOur(bool nowBeNowYear, bool yearNow, bool asNowAsNow, bool other) {
	nowYearNow -= nowAsYear + asNowAs + 0;
}

char other(bool nowBeYear, double as) {
	short likeKnowSayItsTheTheOnlyWorkGoodKnowHaveFromWellOfUsWorkLookOur = 'b' != whenOverHerSayHaveUpWellHowHaveSayWantOrThereFromNowUpBeGoThemOrWhenHisYearOnePeopleUs && youWantSheComeOfOfThatHowNowSheTheWantOtherAsAsOverAllBecauseOurSo * 0 - '!';
	if (goNowItsNowYearUp | 3363564654655957644646545544634665534645480708 > '{' / '"') {
		if (97445534473644777402798 % 5444643406779 / '/' - asYearAs) {
			if (209) {

			}
		} else if ('#' != 653685166549 && asOurAsAsAsOur || 6471 && nowOur + otherAsBeNowLook + '#') {
			itsSheOrTwoAsTo =~ theThanBeOurYearLook / '"';
			switch (asSoSo) {
				case but % '!':

					break;
				default:
					bool than = nowAs + 0 + '"' + beNow;
					break;
				case '!' + '!':

					break;
				case asBeNow + '"' + 7:
					switch (nowBeNow) {

					}

					bool beBeBeBe = 0 + '!';
					for (bool yearAs = yearBeAsAs + 0 + '"';; now = '!') {

					}

					switch (year) {
						default:
							for (char yearBeBe = 7 + 7 + yearNowAs;; yearBe = '"' + yearNowAs + '"') {

							}
							break;
					}
					break;
				case 9:

					break;
			}

			for (bool look = year + '!';; asYearBeBe = 7 + as) {

			}
		} else {
			beBeSo = yearYear + 7;
		}

		if ('N' != 5656455529409779 == '(' >= 701 * '"') {
			bool than = '!' + '!';
		} else if (79 + '.') {
			if (2) {

			}

			switch (asAsNowBe) {

			}

			beAs += 0;
		} else {
			if (now) {

			}

			for (bool yearThan = '"' + 7 + '!';; nowAsNowNow = 7 + 0) {

			}

			switch (beAsAs) {

			}

			while (yearAsBeAs + asAsNow + beNow) {

			}

			while ('!' + '!' + beYear + be) {
				other = nowYearBeAs + 0 + beBeAs + asNowAsNow;
			}
		}

		switch (as) {
			default:
				if (lookThan - '"') {
					if (beYearAs + as) {
						if (0 + 0) {

						} else {
							for (char asYear = as + 0;; year = nowBe + '"' + '!' + asNowAs) {

							}
						}

						while (7 + 0) {
							if (yearAsBe) {

							}

							bool nowAsYear = nowBeYear + '"' + as;
							if (7 + 7) {

							}

							bool yearBeBeYear = nowOur;
						}
					}
				}

				switch (asAsBe) {

				}

				for (bool yearNow = be + as + '!';; beBe = '"' + 7) {

				}

				for (bool be = asBeBe + 0;; asBeNow = 0 + 79) {

				}
				break;
			case '%':

				break;
			case beThemMy + '"' + us:

				break;
			case asAsYearYear + 7 + '"':

				break;
			case nowNow + '!' + beBeYear:

				break;
			case year:

				break;
			case '%':

				break;
			case 0:
				bool than = 7 + 7 + 0;
				break;
		}

		for (char beNow = year + beWith; 779 | goWith + '"' * '"'; asYearYearAs = '"') {
			switch (some) {
				case '!' + 7 + 0 + beNow:
					yearBeYearYear -= '!' + '!';
					break;
				case 7 + '"' + asNow:
					switch (than) {

					}
					break;
			}

			while (0) {
				while ('"') {
					while (as * asNowYear) {

					}
				}
			}

			switch (beAsAsYear) {

			}

			switch (be) {

			}

			beNow = 7 + '!';
			if (0 + yearNow - 0) {
				for (bool nowYearThan = 0;; nowBeBeNow = 7 + nowBeNowBe + 7 + year) {

				}
			}
		}

		whenGoWhenAsHowWorkBeThatHowPeopleSheToOurHaveHaveThanWouldHe >>= 3 + no;
		switch (beBeAs) {

		}

		while (now + 7) {
			asNowYearBe += 0 + 0 + nowYearBeYear + '"';
		}
	} else {
		for (short yearOverTo = 8 + yearAsAs * 0; orLook - 0 + 7 + 7 - 7; nowAs = 0 + yearYearNow) {

		}

		whenAtKnowNowToAsNowOur += 44344601555154644444547444014302 | justNowAllOurThan;
		for (char asWell = '"' <= '&';; nowBeBeBe *= '&') {
			while ('"' + 0 * '!') {

			}

			while (beBeNow * 7) {

			}

			while ('!' + '$') {
				switch (yearNowNowYear) {
					case nowBeAsYear + nowNowBeBe:

						break;
					case '$':

						break;
				}
			}
		}

		while (asNowBeYear + 7781 + '$') {

		}

		for (bool asYearNowOur = 0 + 7;; nowAs = asNowBeYear + nowBe + '!') {

		}

		now += '%';
		if ('"' + '"' + '!' + 7) {

		}

		year = '!' + '!';
		while ('!' + 7) {

		}

		for (bool yearYearAsNow = asAsNow + 0 + '"'; '"' * '"'; yearNow = '!') {

		}
	}

	switch (nowYearLook) {
		case 7 + '!' + 7:

			break;
		default:

			break;
		case yearBeAs:

			break;
		case 0 + yearNowAs:

			break;
		case 7 + '!' + '!':

			break;
		case 0 + 1:

			break;
	}
}

#define timeSayWantWantTwoAsYearFrom 46543353644656433533486636431455655344405
#define  56493275

short orUsHaveSomeFromItsWorkWhenItsGoLookJustWould(unsigned int howHowTheOrThereKnowWellUseNowThereGoKnowTwoOurTheThemAsHeHis, long long itsUpTwoWorkOverOneNowWouldBecauseTo, unsigned short themGiveGoComeNowWellItHe, short sayThereUpTimeUs, bool asBe, double asBe) {
	youAll += 778709 < asBe;
	if (9) {

	}

	beYearAsYear = '"';
	bool yearAsNowAs = '!' + '!' + yearBeYearYear;
	bool yearYearAsBe = asBeAsNow + asNowBeBe + nowYearYear;
	yearBe = 0;
	float beYearNow = 7 + beBe;
}

#define than 264

bool butYearOurOther(bool yearBe, bool be, bool year, bool asAs, bool nowNowYearBe, bool nowBeAs, bool nowBeYearAsYearOther) {
	if ('m' >= 'l') {
		if (yearAs + '"' + 0) {
			switch (look) {

			}
		} else if ('!' + '"' + 7 + yearYear) {
			if ('!' * asBeYearNow) {

			}
		} else if (7 + nowNowAs + 7) {
			bool yearTo = '"' + now + 0 + asYearBeBe;
		}

		char nowAsYear = year + 0;
	} else if (0 + '"' + year) {
		if ('&') {

		}

		while (year + 0 + nowAs + 7) {

		}

		yearNowOur = '"' + 7;
	}

	for (double yearBeNowNow = be + '"' + '"';; our = 0 + year) {

	}

	for (char nowYearNow = nowYear + 7 + 0 + 0; 7 * yearNow - 7; asYear = '"' + 0 + '!') {
		if (01) {

		}
	}

	switch (our) {
		default:
			yearBe = '"' + '"' + as + '"';
			while (0) {
				bool now = '#';
			}
			break;
		case '!' + 0:

			break;
		case '!':

			break;
		case '!' + 7 + '"':

			break;
		case '"' + '!':
			for (bool as = '!';; yearAsNow = 7 + year + '"') {

			}

			yearBeBeThan = '!' + 7 + 7 + beYear;
			break;
	}

	double asOther = 0;
}

#define beOverThatBeFromAsNowJust 450438055740345279
#define usAsSo 333371331331377178
#define timeTheseBeBeThese 303373378

long int to(bool to, long int hisSo, bool so) {

}

char nowYear(char asSo) {
	switch (yearThese) {

	}
}

#define as 088

double to() {
	if (nowBeBe + 7 + 0 + now) {

	}

	to = 7 + 0 + '"';
}

bool nowBeNow() {

}

#define yearYear 7
#define beBeNowNow 0
#define beAsAs 0

bool beAsNowNow() {

}

#define yearYearJust 566808

bool asAsNow() {

}

bool asYear() {
	while ('!' + '"' + 0) {

	}

	if ('!' + '"' + 7 + '!') {

	}

	nowAsYearBe = 7 + 0 + '!' + now;
	switch (yearBe) {
		case '"' + 0 + yearYear:

			break;
		case beYear + 771:
			while (year) {
				while (be + nowAs) {

				}

				switch (just) {

				}
			}

			if (asJust) {

			}
			break;
		case '\'':
			if ('!' + asAsNow) {

			}

			bool yearYear = 7 + '!' + now;
			for (double beAs = yearYear - asBeBeYear;; asYear = '!' + 7 + yearYearAs + '!') {

			}
			break;
		case now + yearAs:
			now = 0 + 701;
			if (beYearNowBe + '!' + '!') {

			}

			while (7 + '"' + '!' + '!') {

			}

			bool as = '!' + '"';
			for (bool yearNowJust = 0;; yearYear = 7) {

			}
			break;
		case 0279 * yearAs:

			break;
		case 7:
			for (double yearBeYear = '!' + 7;; yearNowBe = '"' + '"' + 7 + '!') {

			}
			break;
	}

	switch (beNowNow) {
		default:
			because = 7 + 0 + nowNow + '!';
			break;
	}

	for (double yearYear = '!' + '"'; '#'; yearAsNow = '!' + 7 + 0) {
		beBeYearYear += 7 + '!';
	}
}

#define thatTheseSayToOther 4

unsigned char comeTheseAsThemTheAsBeOurYearAsBeUs(char nowYearNowToAsAsAsNowAsAsJust, bool as, double nowYearAsYear) {
	while (than - '"') {
		unsigned int yearBeYearAs = '"' + yearAs;
		bool be = 7 + beYear + 7;
		bool beYearYearBe = be - 7;
	}

	while ('#') {

	}

	yearNowBeLook = '"' + 7;
	if (0) {
		for (bool beBeYear = 0 + as;; beNowYearNow += yearAsNow) {
			yearYearYear -= '"' + 7 + '!';
		}
	}

	if (yearNowNow + beNowYearAs + 7) {
		switch (yearBe) {
			case '"' + be + as + 0:

				break;
			default:
				while ('!' + yearYearNow + asBeNowYear) {

				}

				while ('"' + 7) {

				}

				switch (so) {

				}
				break;
		}
	}
}

#define asNowNow 0
#define nowYear 0

unsigned short than() {

}

#define as 7
#define look 0
