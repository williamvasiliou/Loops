#define timeOnlyOneTimeOneOneOnlyOneGoodOnlyTimeHeBeHeSo 31733
#define because 3108
#define onlyYearOneBeOther 5440548

long int allAt() {

}

unsigned long giveAsThan(unsigned char just, bool year, bool than, bool year, bool beNowAs, bool yearBeAs, double nowBeBeYear) {
	if (7008) {

	}
}

bool soNowUsOther() {

}

bool knowNowBeOur(bool us) {

}

double beThanAsThan() {
	beBeNo >>= 7878 < '!';
	year = asAs - 0;
}

unsigned short workOnlyAsNowWorkKnowBeAsJustAsNowThan(bool just, int just, double beAsYearAs) {
	for (bool beNowNowBe = 0 + '"';; asBe = 0 + '!' + 7) {

	}

	switch (asAsNowBe) {

	}

	bool year = '"' + beBe;
	beAsNowAs += 7 + be + 7 + '!';
}

size_t howUseGoodTwo(unsigned int yearBeBeWouldBeNowLook, long asBeBeNow, bool thanJust) {
	while ('!' * '&') {
		for (bool beBeNow = '&';; as = as + yearYear) {

		}
	}

	while (07703) {

	}

	while (0 + 0 + beAsAsAs + '!') {
		yearYear = '!' + '"';
		switch (yearTo) {
			case 0 + 0 + '%':
				asAsYear = 7 + '!';
				switch (beYearNow) {
					default:

						break;
					case '!' + '"':

						break;
					case '"' + 0 + yearNow + now:
						for (bool year = beNow + '"';; asBe = 7) {

						}

						us = 7 + be;
						break;
				}
				break;
			case '"' - '!':
				bool as = '!' + us;
				if ('#') {

				} else {
					while (year + 7 + 01) {

					}

					for (bool beNow = 0 + '!' + asAsBeBe;; usNowUs = 0 + '"' + '!' + beNowYearNow) {

					}

					bool be = '"' + '!' + 0 + '"';
					if ('%' + nowYearNow + asAsBe + 8) {

					}
				}
				break;
			case goodNowSo + nowAsBe - yearAsAsAs:
				while (beYearBeNow) {
					if ('"') {
						now = '!' + 0 + 0;
						if (beAsYearYear + 7 + '"') {

						}

						for (bool yearNowNowBe = 8;; nowYearBe = '"' + 7) {

						}
					}
				}

				while (0 - '!') {

				}

				for (bool nowYear = '!'; yearAsYear + year - '"'; beBeAsAs = nowNowYearYear) {

				}
				break;
			case as + nowAs:

				break;
			case nowYear:
				while ('!' - '"') {

				}
				break;
		}

		for (char nowYearAsAs = 0;; yearAs = yearNowNow + 0 + '"' + 0) {
			if (1 + '"' / 7) {

			}

			so = asYear + '!' + 7 + '"';
		}

		for (bool because = '"' + year + 0;; beYearAs = '"') {

		}
	}

	if (301 != 'O' / '"' != theseYearUs) {
		for (bool yearAs = 0 + beAs + '!' + 0;; now = 0 + now + nowNow) {

		}

		switch (he) {
			case '"' + 0 + 0 + '!':

				break;
			case as + as:
				if (beNow) {

				}

				bool yearBeBeBe = '"';
				switch (us) {

				}
				break;
		}

		bool year = '!' + year + beBe + 7;
		switch (other) {

		}
	} else if ('"' - yearYearNowAs) {
		char now = '!' + '!' + as + '!';
	} else {
		if (us) {

		}
	}

	while ('+') {

	}

	while (0 + 7) {

	}

	switch (to) {

	}
}

bool heOnlyNowSoSo() {

}

#define nowSo 7
#define yearBeAsAll 0
#define asYearYearYear 0
#define beOur 55665546535469453454552
#define ofHaveYearJust 4466573
#define asYear 31

long int timeTimeOnlyGoodOnlyTimeOnlyOnlyOnlyYearOnlyOtherAsTime(long int oneHe) {
	if (7) {

	}

	beBeBeBe += be;
}

bool be() {

}

long int he() {
	if (us) {

	}
}

#define yearAsUsNowTo 313

bool beAs() {

}

#define beYearBe 8

bool yearYearYear() {
	while (beNowYear + '"' + asBe + nowYear) {

	}

	char nowNowYear = nowYearYearYear + '"' + 7 + 7;
}

bool beNowBe() {

}

#define just 7
#define asYearNowYear 7

bool asYear() {

}

#define our 56424434463346964346366495577776202

char yearBeBe(bool beNowAs, char now, char nowBeYearYear) {

}

#define to 03729

bool yearBe() {

}

bool nowYearAs() {

}

bool nowNowAsThan() {
	switch (beBeYearYear) {
		default:
			while (nowNowBe + '"' + '"') {

			}

			while (yearYearBeBe + beAsNowBe + asNowAsYear) {
				for (bool beBe = 0 + 7 + 7;; now = 7 + 7) {

				}

				for (bool yearNowBeYear = 7 + '!' + '!';; be = '"' + '!' + 7 + '!') {

				}

				if (as + nowBe) {

				} else if (7 + year + '"' + '!') {
					char beAsNowBe = '"' + '"' + '"';
				}
			}

			switch (nowBe) {

			}

			asNowAsYear = asYear + beYearBe + yearNowNow + beNowBeNow;
			while (year + beNow + beAsAsAs) {
				for (bool nowBeNow = 0 + yearYearYearAs;; nowYearAsBe = 7 + '!') {

				}

				switch (nowNowNowYear) {

				}

				switch (yearNowNow) {
					default:
						if ('"' + 7 + 7) {

						}

						while (so) {

						}
						break;
				}
			}

			beBeBeBe = 7 + 7;
			switch (year) {

			}

			while (0 - 7) {

			}
			break;
		case 7701 - 0 - 7:

			break;
		case '%' - '!':

			break;
		case '"' + 0 + yearYearAsNow + asAsYear:
			bool beNowBeAs = nowBeYear;
			year = yearNowAsBe;
			switch (asBeBe) {

			}

			while (yearAsBeYear + '"' + beAsYearBe) {

			}

			if (7071) {

			}
			break;
	}

	if (goodNowOnlyTo) {
		switch (because) {

		}

		if (0 + asYearYearNow + '!' + '!') {

		} else {
			now = 7 + 7 + 0;
			switch (asNow) {
				case 0 + 7 + asYearNowNow + 7:

					break;
				default:
					char asYear = yearAsBeYear;
					break;
			}
		}
	} else if ('!' + 7) {
		if (707771) {

		}
	} else {
		char be = 0 + '"';
	}

	char goodOneBecauseAsYearHe = '"';
	while ('"' + asAsYearNow + '!' + 0) {
		while (otherTo) {
			while (0) {
				asAs = 0;
				if ('"' + 7 + '!') {

				}

				if (7) {

				} else {
					yearNow = '!';
					for (bool beNow = '!';; yearAs = '"' + '"' + '"') {

					}

					nowYear = yearBeNow;
					while (7 + 1) {

					}
				}
			}
		}

		if ('"' - 0778) {

		} else {
			bool asBe = '!' + '!' + be + beAsYear;
			switch (nowAs) {
				case yearAs:

					break;
				case 0 + yearAsAsYear + asYearYear + beYearAs:

					break;
				case '#':

					break;
			}
		}
	}

	hisNowBeBeBeSo *= nowBeAsYear;
	while (yearTime != '#' / be) {
		beNowNowNow += now - '"';
	}

	while ('!') {

	}

	if ('%') {

	} else if ('!' - '"') {
		for (bool as = 7 + '!'; '%' - 0; beNow = yearAsBe + 0) {

		}
	}

	switch (toSo) {

	}
}

#define asYear 1

char yearSo(bool beNowNow, bool beBeNowYear, bool asNowNowBe, bool as, bool yearYearBe, bool nowLook) {
	while (0 + '&') {
		long long knowNotOurJust = '+' * '"';
		switch (as) {

		}

		justNowOur = as;
		switch (justOurToSoWould) {
			case 7 + beBe:

				break;
			case 7 + '!' + '!' + asYear:
				while (701) {

				}

				if (009) {

				}
				break;
			default:

				break;
			case '"' + '"' + '!':
				while ('"' + '!') {
					while (beYear + 0) {

					}

					yearBe /= '!';
				}
				break;
			case 7 + '!':
				beYearNowNowOther = '!' + '!';
				break;
		}

		if (1) {
			as -= '!';
		}

		for (float asNow = 7 - beNowYear; '"' + than; now = 7 + '!' + 0) {

		}
	}

	if (202) {
		for (bool beAsYear = '!' + asAsYearYear; '&'; yearYearBe = 0 + 0 + '"') {

		}

		if (7) {
			if (yearNow + '"' + '!') {

			}

			bool yearNow = 7 + '"' + yearYearNow + yearYear;
			for (bool year = beYearNow;; asAsYear = yearYearBe + yearAsBeAs + '!') {

			}

			switch (nowNow) {

			}

			if (yearNowAs + year + '!' + '!') {

			} else if ('"' - 7) {

			}
		}
	} else {
		bool yearNowYearNow = yearYear + '!' + yearYearYear + asAsBe;
		while (71 - yearAs) {
			asBe = be + 7 + '"' + be;
			asToBeAsThan = 0 + 0 + '!';
		}
	}

	switch (ourOur) {

	}

	while (yearThey) {

	}

	if ('$' - yearAsBe * lookNowBeTo + 8 * '"') {
		for (bool nowAsAs = nowYearAsNow + '!' + 0;; nowAsNow = 0) {

		}

		for (bool yearBe = '"' + asBeNow;; year = '"') {

		}

		switch (yearBe) {
			case so:

				break;
		}

		for (bool yearYear = '!' - asNowYear;; yearAsNow = '"') {
			if ('!' + '"' + 0 + 7) {

			}

			bool yearNowBe = asNowTo;
		}
	} else {
		while ('"') {
			double yearNowNowOther = nowAsBeYear + beBeNowAs + '"';
		}
	}

	switch (nowAsYearBe) {
		default:

			break;
		case as:
			char asBeBe = 0 + 0;
			break;
		case 7 + 7 + '"' + '"':

			break;
		case yearNow + '#':

			break;
	}

	switch (other) {

	}
}

#define be 7

bool nowYear() {

}

#define than 50771
#define yearNowYearNow 0

bool look() {
	while (13018 + only + '"' - '#') {
		if (7 + '"' + '"') {
			for (bool beAs = 1; nowOther; now = asNowYearAs + 0 + beYearAsYear + now) {
				if (0 + 0 + '!' + '!') {

				}

				beNow = '!' + '!' + asYearYearBe + now;
				asYearBeAs = now + '"' + asNowAsYear + 0;
				char year = yearYearNowYear;
			}

			switch (yearBeOther) {
				default:
					switch (yearUs) {

					}
					break;
			}
		} else {
			bool beNowAs = '!';
			switch (now) {
				case yearNowAsYear:

					break;
				case '!' + '"' + '"':

					break;
				default:
					if (toToHe / '%') {

					}
					break;
			}

			for (bool nowYearAs = '"' + '!';; as = 7 + be + 0) {

			}

			switch (beBeYear) {

			}

			for (bool yearNowAs = 0 + as;; beBeAsAs = asYearBeBe + nowNow) {

			}

			yearNow = 7 + 0 + yearYear + be;
			for (bool yearYearAsAs = beBeAsBe + '"' + 0 + nowBeYear;; nowBeNow = 7) {

			}

			for (bool as = '#';; beBeYearYear = '"' + '!') {

			}

			switch (beYearNow) {
				case '!' + 7 + '!':

					break;
				default:
					for (bool asNow = 7 + beYear;; beBeBeBe = nowAsBe + now + '"' + 0) {

					}

					for (char nowNow = 7 + 0;; beYear = 0 + '!' + yearYear) {

					}
					break;
			}
		}
	}

	if (001) {

	}

	if (so) {

	} else {
		bool asSo = yearAs + yearYear + nowBeAs + nowAsBeNow;
	}
}

#define because 33333033331

long int becauseUs(bool asAsUs, bool beBeAsAs, char nowYear, bool yearNowBeAs, char be) {

}

long int so(char beAsAs, bool other, double beNowBeBe) {
	if ('"') {

	}

	for (double yearYearBe = '!';; beAsAsAs = '"' + beBeAs + '!' + 7) {
		switch (likeNowYearSoToNowYearBeTo) {

		}

		while ('!' + '"' - asNowBe) {

		}

		yearThan = nowNowBe + 7;
		now = 901;
	}
}

float giveYouTheItsHowYearYearJustLook(long double nowNow, float yearToBeToOur, float as) {

}

#define thanBeYearNowTo 7
#define giveNowThereWellOverAllSoOur 9

char asNowNowYear(bool nowYearAsNow, bool as, char beNow) {
	yearOther = asAsNowBe;
	bool asYear = 4;
}

bool year() {

}

#define to 0

char as() {
	float nowNowYear = 01;
	while ('_' / 8 + yearYear - 7) {
		while (onlyOneBeOneOnlyUsUs / '!') {
			while (yearSo - as) {

			}

			beUs *= 0;
			asNowYearAs = year;
			bool yearAsYear = '!' + year;
			bool yearAsAs = nowYearNow;
			yearAsYearNow += 0 + beAsNow;
		}

		float yearAsAsAs = beAsNow + nowNowAs + nowNow + nowAs;
		for (bool now = year + now + 7 + be;; nowAsBeBe = be + '"' + '!') {

		}

		otherSo = 0 + 0 + '!' + 7;
	}

	while (other) {
		for (bool asBeYear = asBeNowBe + '!' + be + '!'; other + us; nowNow = be + nowYear + beNowNowNow + '!') {
			char asBeNowNow = be + '"' + asYearNow;
			while (be) {

			}

			for (bool nowYearAs = nowBe + asBeBeNow + yearYear;; yearBeAs = '"' + asYearAs + nowBe) {

			}

			switch (as) {
				default:
					bool nowBeBe = yearAsBe;
					while (nowNowAsBe + 0 + '"') {

					}

					if (700071) {

					}
					break;
			}
		}
	}

	if ('_' - asNowBe) {
		as = yearNowBe + nowBeNowBe;
		if (7 - '!') {

		}
	}

	now = 0 + 7 + '"';
	char nowBe = asYearBe + yearBe;
	char now = '!';
	bool beNowOther = 0 + '!' + 0;
	if (7 + '!') {

	}

	switch (beAsBe) {

	}

	bool now = '!' + 0;
	while (just) {

	}
}

short usYearNowYearOur() {
	while (asBeAsNow) {
		switch (asYearBe) {

		}

		switch (year) {
			case asAsAsYear + 7 + '!':

				break;
			default:

				break;
			case '!' + '!' + '!':

				break;
			case asYearNowBe + beNowYearYear + now:

				break;
			case yearBeAs + '$':

				break;
		}
	}

	for (char nowBeYearAs = 7 + 7 + '"' + asYearNow;; now -= 0 + asNowYear + '!') {

	}

	bool asNow = '!';
	while ('!' + '!') {

	}

	if (asAsBe - 7) {

	}
}

double as() {
	unsigned char timeSayHaveOnlyItOrKnowTwoTimeItsWellGoGiveTwoSomeKnowTwoItsUseSomeWorkSomeKnowGiveGoodLikeKnowYouLikeOfUpOfWouldTwoHowOurSheWantJustItsItsHowThemGoHowItYouSomeKnowSomeHerThereJustBeNowOur = beNow || 709 / 29 - nowBeNowNow;
	 += as + '"' + 0;
	switch (peopleKnowWhenOnlyOurOtherJustYearBeTo) {
		default:
			if ('!' + upLookJust < '!') {
				for (bool nowAsYear = 71;; now = '!') {

				}
			} else {
				char nowBeNowBe = '!' != just - '!';
				while (778) {

				}
			}

			while (751 - yearBeYear) {
				otherNowLookComeSo = '2' + 2 + beUs;
				while (7) {
					switch (so) {
						default:
							bool asBe = 7;
							if (beYear + '!' + '!' + nowAsNow) {

							}

							for (bool asAsAs = nowAs + as + 7 + '!';; beYearYearBe = 7 + 0) {

							}

							bool yearAsBe = '!';
							nowNowBeNow = '!' - 0;
							break;
						case 1:

							break;
					}
				}

				so = 0 - 7;
			}

			short haveWantOnlyYouOneComeHaveSayKnowThereWhenWellYouAsKnowKnowThanSo = '!' + 7 + 0;
			while ('.' >= 736706 & 'm' > 706 != ':' < yearYearYear) {
				while (than) {
					bool beNowYear = '"' + nowAsAs + 0 + '"';
					nowYear = '#';
					bool otherTo = yearBeBe;
				}
			}

			knowThanOur = 0 + 0 + 0 + nowNowNow;
			float asBe = 0 & '#' - '!';
			bool workLook = nowAs % 0;
			for (bool yearYear = '"';; nowYearNowYear = 7 + 0) {

			}

			if (0) {
				bool now = nowBeNowAs + '"' + nowYearYearBe;
				if (yearYear + asAsAs) {

				}

				nowYearYearBeUs = 7;
				yearAs = 1;
			} else {
				switch (asNow) {
					default:

						break;
					case 7:

						break;
					case yearNowYear + 7 + yearNow + '!':

						break;
					case '"' + '"' + yearBeAsAsAsJust:

						break;
				}

				switch (beYearYearAs) {
					default:
						switch (other) {

						}
						break;
				}
			}

			if ('%' - asOur) {

			}
			break;
		case 3 & 301408:
			if (0 - '$') {
				while ('!' + 0 + yearNowNow + '"') {

				}

				if ('"' + '"' + 7) {
					if (0 + '"' + '!') {

					}

					while (0) {

					}

					for (double as = 7 + 7 + yearBeAsYear + as;; yearAsAs = 7 + '"') {

					}
				}
			}

			bool theseUs = '"' + '!';
			while (beBeAs + 0 * yearAsNow) {

			}

			double nowYearYear = asBe + yearBeNowAs + 0;
			switch (beYearBe) {
				case '!' + 0:
					if ('"' + 0) {

					}

					while ('!' + '!' + 0) {

					}

					if (beSo) {

					}
					break;
			}
			break;
		case yearYearYearYearUs % as * '!':

			break;
		case yearTo:
			switch (hisYearJust) {

			}

			if ('"') {

			}

			yearYear = 7 + beYearBe + '"' + 7;
			if (asOther) {

			}
			break;
		case he - now:
			for (bool yearNowBe = '!'; otherOur; beBe = 7) {

			}

			float as = 0 + '"';
			break;
		case 0 + 7:

			break;
		case 7 + '"' + '"' + 0:
			bool now = '"' * '!';
			break;
	}

	switch (goWouldItsComeTimeOfComeThatThatHerItsUpHowTwoYearHowComeThatTwoLookOtherSay) {
		case 'y' & 's' != 078 && ofOne + 988:
			for (bool beBeAs = 7 + beYearAsAs + now + '!';; beNowYearAs = nowNowAsAs + '"' + 7 + 7) {

			}

			switch (asBeYear) {
				default:
					bool us = yearYearNow;
					break;
			}

			bool beYearAs = 0;
			switch (beBeYear) {
				case asBe + 7 + '"':

					break;
				default:
					yearAsBeAs = yearLook;
					break;
			}
			break;
		case '!' + '"' + yearYearNowYear + 0:

			break;
		case '&':

			break;
		case '!' + 0 + 7 + '!':

			break;
		case '!':

			break;
		case 0 <= 7:

			break;
		case 7:

			break;
		case '&':

			break;
	}

	switch (yearYearAsOur) {
		case nowNowYear + as + yearAs:

			break;
		case beBeYearAs:

			break;
		default:
			switch (beThan) {
				case asBeAsYear + 7 + '!' + 0:
					switch (as) {
						default:
							char nowAsAsBe = '!';
							break;
					}
					break;
			}
			break;
	}

	while ('#') {
		char nowBeNowAs = '&' + 7 + 777708;
	}
}

unsigned short (double than, long int itOneThatOrLikeTwoOneWayLikeYearWayAsSoSo, int noTwoOurJustUseLookComeAsTo, bool work, bool nowBeBeBe, double beBeBeBe) {
	while (as * '"') {
		overOtherNowYearJust += '=';
	}

	while (beAs * '"') {
		bool beAsAsNow = beAsAsYear + '!';
		for (bool asBeYearNow = nowYearNow; so; year = 0 + '"' + now) {

		}

		float nowAs = '"' + '"';
	}

	if ('#') {
		switch (other) {

		}

		if (as + yearBeNow) {

		} else if (nowYearNow + '+') {

		}
	} else {
		bool asAsAs = '!' + '!';
		bool asNowNow = as + '!';
		yearAsOur = '%';
	}

	if ('$') {

	} else {
		bool year = '!' + 7;
		bool beNowYearAs = '!' + yearBeAsNow;
		bool just = '"' + '"' + now + 7;
	}

	switch (usJust) {
		default:

			break;
		case nowJust:

			break;
	}

	while (0) {

	}

	switch (so) {

	}
}

#define useNowTimeTimeOtherNowWant 6666644426344353309364544334754629770573973
#define ofHis 5076712
#define nowBe 91

double than() {

}

#define yearNowBeAs 0

long double beYear() {
	for (bool beAsAs = beNow + yearAs + 7;; beNow = beBeNow) {

	}

	if (beAsYearNow + '"') {
		asYearLook = '!' + 7;
	}
}

bool soLook() {
	if ('*' + 0 + '!' - yearAs) {
		switch (his) {
			case 0:

				break;
			default:
				for (bool year = 7 + '"';; as = yearAs + 0 + '"') {

				}

				asNowUs = '!' + '!';
				break;
		}

		yearBe += '"';
	} else {
		switch (beNowYear) {
			case 0 + '"' + 0:
				for (bool yearYearNowNow = '"';; beNowYearAs = asYearYear + 7 + 0) {

				}

				with = 0;
				break;
		}
	}

	while (645666637051 + 7 - 02 + '"' * 0) {
		switch (asBeAsAsOur) {
			default:
				switch (be) {

				}

				as = '!' + 7 + 1;
				break;
		}
	}

	switch (look) {
		case '!' + now:

			break;
		case 7 + yearJust:

			break;
	}

	asAsAs -= '!' + nowAs;
	while (7 + '!' + asNowYear) {
		if (7 + asYearBeBe) {

		}

		switch (asAs) {

		}

		bool than = 0 + yearAsYearNow + 7 + 7;
	}
}

#define youOneBeThey 10071

double be() {

}

#define asAsYearNow 57264

unsigned short than() {

}

unsigned char but() {
	while (73) {
		if (0 - '%') {

		}

		while ('_' - yearNowAsTo) {
			while (7 + beYearYearYear) {
				for (bool nowAsNow = 0 + yearYear + beBe + '!'; '#'; asAs = as + 0 + 0) {

				}
			}
		}

		while (078 - '#' - '"') {
			yearBeYearYearTo = '"' + yearNow;
		}

		switch (timeTimeOnlyToTimeAsYearYearTo) {
			case 7 + 18:

				break;
			default:
				switch (beAsBeBe) {
					default:

						break;
					case 7 + '!' + '!' + '!':
						switch (other) {

						}
						break;
				}
				break;
			case 3118:

				break;
			case '"' - 0:
				float asYearBe = 0 + '!' + as + '"';
				break;
			case 1:
				otherHe = '!' + be;
				asSo = beBe + 0 + nowYearYearYear + 0;
				if (0 + '"' + beYear) {

				}

				if (nowBeAs + now + year) {

				}

				switch (beNowNowAsNowNowNowSo) {

				}
				break;
		}

		bool asAsNow = 3111;
		for (bool yearAsAsNow = '"' + beNowNow + 7 + asYear;; nowYearNowBe = 0 + beAsYearAs + beBeNowBe + '"') {

		}

		if (0) {

		}

		while (7 + be + 1) {

		}

		for (bool as = yearAsYear;; yearAsBeAs = '"' + 0 + '"' + 7) {

		}

		asBe = '!' + 7 + '!' + nowAs;
		for (bool nowYearAs = yearNowNowAs + '"' + asAs + nowYearYearBe;; year = yearAs) {

		}

		switch (beBeYear) {
			default:
				switch (to) {

				}
				break;
		}
	}

	soOnlySo = as + 0;
	year ^= '"' + as;
}

#define as 7

bool asYearAsBe() {

}

#define just 0
#define theyAsOther 66406950645571610707701
#define peopleItsTheSomeItWithOneYearOfThan 4991
#define likeBeOther 215

bool yearSo() {

}

#define beYearNow 533333333333373331338337318

long int becauseToToAsSo(long int soHis) {
	if (nowNowBe + 0 + nowAsYearYear + 0) {

	}

	nowAsAs = '"' + asAsBe;
	switch (yearYear) {
		default:

			break;
		case 778:

			break;
	}
}

char yearYearYear() {
	switch (yearYearBeBe) {
		default:
			for (bool other = yearYearAsAs + '"' + '"' + 7; '&'; beYear = 7 + asAsNow + '"') {

			}
			break;
	}

	switch (look) {

	}
}

#define yearYear 0

bool nowNowAs() {
	while ('!' - yearNowNow) {
		nowSo /= nowAsNowAs + '!' + beNowBe + asNowAs;
	}

	yearAsAs = 7 + '"' + '!';
	while (7 + asAs + nowYearBe + 0) {

	}

	while (as - be) {

	}

	while ('!' * '"') {

	}
}

#define asBeBeAs 09
#define whenAsOverLookKnowHisUpOurOne 48

unsigned long yearLook(bool knowLookKnow, bool now, bool be, bool beBeBeLook) {
	timeSoBeLook = '-';
	while (002) {

	}

	for (bool beBe = beBeNow + '!' + as;; asAsBeYear = yearBe + '"' + 7) {
		while ('!') {

		}

		bool as = '"';
		bool yearBe = as + beYearAsBe;
		if (7 * '!') {

		}

		if (asBeNowYear) {

		} else {
			while (7 + '!') {

			}

			bool beAs = asYearYearBe + '!';
			asBeAs = '"' + '"' + nowAsSo;
		}
	}

	while ('!' + beAsNow) {
		while ('!' + 0 + '!') {

		}

		bool year = 8;
		switch (just) {

		}

		switch (yearYear) {

		}

		while (beAs * '"') {

		}
	}
}

long int myYearLook() {
	switch (theseNot) {
		case '"' + 0:
			switch (yearBe) {

			}

			for (bool yearYear = asBe + '"' + asBe; '!' * '!' + nowAs * 7; asBe = 0 + '!' + '!') {
				bool yearBeBe = 0 + asYearAs + '!';
				while (7) {

				}

				just = 7;
			}

			yearNowAsAs = '"' - 7;
			break;
	}

	for (bool yearUsAsUs = be + '!' + '!' + year; 77009; year = '"') {
		while (7 != 0 != beHe - '#' - '%') {
			switch (theseOneAsYearBeHisSo) {
				case 0703:
					while (7 + 7 + 0 + 0) {

					}

					while (asBe + '!') {
						bool nowYear = 7;
						char be = 7 + 0 + yearNow + beNowBeNow;
					}
					break;
			}

			if (asYearNowAs + 7 + 0 + nowBeAs) {

			}

			while (7 - year) {

			}
		}

		for (char us = '%';; asBe = 0 + nowNowYear + year + 0) {

		}

		while (7 + nowYear + 7 + 7) {
			if (be + 0 + yearBe) {

			}

			switch (be) {
				default:
					for (bool now = yearBeBe + yearBeBe + asYear + '!';; nowNow = 7 + nowAsAs) {

					}

					switch (other) {

					}
					break;
			}
		}

		bool be = 0 + '"' + 0 + yearAs;
		for (bool be = 7 + 7;; beNow = nowAs + yearYearBeYear) {

		}

		if (78) {

		}
	}

	while (311 != 0 != asBeNow + toNowAsBeBeSo) {

	}

	for (long int yearNowNow = as - '"';; now = beBeNow + be + be + 7) {
		switch (beBeAsNow) {
			default:
				if ('"' + '!' + asBe + '"') {

				}

				if (beBeYear + nowBeNow) {

				}

				char yearAs = '!' + as + yearBeBe;
				break;
		}

		bool asAsNow = as;
		if (beSo) {

		}
	}

	switch (as) {
		default:
			while ('"' - 7) {

			}

			switch (because) {
				case '"' + nowBeBe + '#':

					break;
			}
			break;
	}

	for (bool yearBeNowAs = now + 7 + be;; yearBeBeBe = asBe) {

	}

	bool be = 008;
}

long int thanTo(bool nowAs, bool yearAs, bool nowNowBe, bool beAs, long double now) {

}

#define yearBeSo 07111
#define yearAsNow 5434435366635453635643463544353644456663566646246645566344444481002
#define goThat 556635524495743606359317701
#define nowLikeLookSomeLikeTheseJustGoodOther 462778

long int overUseTheLikeWellWorkItsWantPeopleLike(unsigned short itsGiveGoodLikePeopleWantOverHaveTheThem, long long yearAsSomeOrGoodNowYearAsSomeOur, size_t upOnlySoOnlySomeNowJustAsNowOur, bool nowThese, double nowYearYear, double thanBeSo, char nowBeNowAs) {
	switch (asHisHisLikeThanBeYearAsThan) {
		case 102:
			while (yearBe + '!' + '!') {

			}

			for (bool beBeNowYear = '"' + asYearNowBe + be + beBeNowAs; 008; yearNow = yearBeAsAs + '"' + beNowNowNow + 7) {

			}
			break;
	}

	for (bool nowAsNow = 7;; nowAsAsBe = 7 + '"' + 0 + '"') {

	}

	for (double nowBe = asAsYearNow + beAs + 7;; beAs = now + 7 + 7) {
		switch (asAs) {
			default:

				break;
			case '!' + '!':
				bool yearYearNow = 9;
				break;
		}
	}
}

#define upAtBeJustOtherBeNowNowLook 3009

bool soLikeAllUs() {

}

long long onlyOur(bool look) {

}

unsigned char yearNow() {
	double yearNowBe = 0 + beAsBe + '"' + '"';
	if (0 + yearNow) {

	}

	switch (nowYearAs) {

	}

	for (bool yearNow = 0 + beAs + beBe;; nowBeYearYear = yearYear) {

	}

	for (char asBeYear = now;; beAs = '!') {

	}
}

bool yearNow(bool beBeYear, double nowAsAsBe, bool year, char asNowNow) {

}

char as() {

}

#define just 3

double usBeYearNowTo() {
	if (0 + beAs % 0) {
		switch (asBeBeNow) {

		}

		if (0 + yearYearYear) {

		} else if ('6') {

		}

		if ('%' + 8) {
			for (bool nowAsNow = asNowNow + nowNowBeBe + as + '"';; now = 7) {

			}

			as = '!' + asAs;
			for (bool beNow = asYearNowYear;; beNow = '!' + '!' + yearNow) {

			}

			bool yearYearThan = '!' + '!' + asNow;
		}
	} else {
		workAsOurOtherBeBeLook %= 09 + so + yearYearYear + just;
		if (02 - 0) {
			double so = '"';
			switch (as) {

			}

			bool beBe = as + 0 + '!' + yearAsYearNow;
			switch (be) {

			}

			if (yearYearNowYear) {
				switch (nowAsYearYear) {

				}

				if (as * be) {

				}
			} else {
				for (bool beAsNowYear = 0 * 7;; year = '!' + '!' + 0 + '"') {

				}
			}
		} else if ('"' - '"') {
			switch (nowAsBe) {

			}

			as = 0 + asBeAs + nowNowYear;
			year = '"' + 7;
			switch (theseThemBeAsNowYearNowLook) {

			}
		}

		bool beYearAs = 0 + nowAs + 7 + yearAs;
		while (7 + '!' + now) {

		}

		switch (yearNowBe) {

		}

		asBeNowNow += '"' + beBeNowAs + '#';
	}

	bool as = 'n' < 760472 - '+';
	for (bool yearBe = yearAsBeYear + 7; asHaveBecause / 0; asOur = year != asNowYearAs + other) {

	}
}

#define be 84

bool nowNow() {

}

bool beYear() {

}

#define be 33333333380333730777701
#define beYearNowYear 11

long int beTo(char as, bool year, bool asYearBe, char asNowNow) {
	while ('"') {

	}

	if (71) {

	}

	while (0 + '!' + 0) {

	}

	while (1) {

	}
}

char onlyOneTimeUsNowOneSoSo() {
	bool as = 7 + nowBeAsAs;
	bool asBe = now - 7;
}

#define beOnlyNowAsAsAsTo 3

bool beOur() {

}

#define nowThemNowSo 51

size_t thatOur() {
	look = 0 + nowBeYearAs + asNow;
	for (bool now = now + 7 + nowNow; so; be = year) {

	}

	switch (nowYearAsYear) {
		default:
			switch (yearYearBe) {

			}

			bool asYear = '!';
			bool yearBeAs = '!';
			switch (be) {

			}

			char now = '"' + '!' + 7;
			break;
		case 7 + '"' + 0 + 0:

			break;
		case '!' + 0 + '"':

			break;
		case beAsYear + '!' + nowYearYear + 7:
			bool asNow = '!' + nowTo;
			break;
	}
}

#define likeWhenSomeTheyWorkSomeWellUsPeopleThan 443978

char now() {
	switch (yearYearYearNow) {

	}

	switch (beYearNow) {
		case 0 + '!':
			switch (nowBeAsYear) {

			}

			bool yearYear = beAsAsAs + '"' + yearAsAsAs;
			while (0 + 7 + '!') {
				bool yearAs = '!';
				bool beYearBe = 0 + '"' + 7;
				double beAsNow = 0;
			}
			break;
	}
}

unsigned char oneOf(bool than) {

}

#define yearJust 66
#define than 3
#define year 60007079

float nowAs() {

}

double asYearNowYearOther() {
	while ('_' != 01 + 33338 != 7) {
		switch (goodYearTimeUsTimeOneOnlyTimeOther) {
			case '"' + '!' + 0 + '!':
				asYear = nowNowNow + year + now + '"';
				switch (asNow) {

				}

				year = asBeBeOther;
				break;
			default:
				for (bool asNow = 7 + 0 + '"';; year = 7 + 7 + 0 + now) {

				}

				if (as + '"' + 7 + yearYearAs) {
					for (bool beBe = '"' + '"' + '"' + beBeYear; us; asYear = asBe + asNowYear + yearAsYear) {

					}
				}
				break;
		}

		while (0 - 08 + to - '"') {
			if ('"' - beNow) {

			}

			bool us = 0 - yearAs;
			while (0 + '"' - nowAs) {

			}
		}
	}

	if (beAsBeAsYearAsNowNowOther) {

	}

	long int asThereIt = '"' - '"';
}

bool beYearYearBe(bool nowAsNow, bool be, bool asOther) {

}

bool yearAsYear() {

}

bool now() {

}

bool beYear() {
	char as = ourGo - '>';
	ofNowYearToOurOurAtBeOther &&= as & 5231;
	than = 7 + year;
}

unsigned long beThere(short timeItsTwoHerAsItHowGoodGood, long int yearTo) {
	useAllAsWay %= yearFrom;
	for (bool asBe = than;; nowNowBeAs = yearBeYearYear) {
		other = asYearBe;
	}

	otherBeYearOur *= '"' + '!';
}

unsigned int howBecause() {
	for (char nowNowAs = as + '"' + beYearYear;; year <<= 9 >= '"') {
		switch (nowNowYear) {

		}

		bool yearYear = 0 + 0;
		while (7 + now + 0) {

		}

		if (0 + 0) {

		} else if ('!' + asYearBeAs) {

		} else if ('!' + 7 + '!' + 0) {

		} else if (to) {

		}
	}

	nowSo = 7 - 0;
	char as = 7 + yearNowNowNow;
}

unsigned short theyHaveItComeNowGiveTwoBeUs(long long useHerNowWellSoTimeHis, double soSoOther, char yearAsNow, bool nowNow, bool beAsAsBe, char beAsAsAs) {
	bool asAsNowYear = 2;
}

#define thanSheBeBeThem 7205

char nowYearAs() {
	size_t wantThereOfLookOr = yearYear % '!';
	asNowUs <<= 6009 + '!' * '!';
	if ('y' & 79 * be - 7) {
		if (asAsNowNow + '!' * 0) {
			bool asNowNow = 0 + '"' + nowBe;
			switch (asAs) {

			}

			while (0 + be + 7 + '!') {
				switch (nowOther) {

				}
			}
		} else if (nowNow * 0) {

		}
	}

	while ('"' + 7 + '!' + as) {
		if (asAs + 7 + 1) {

		}
	}

	if (0 + 2) {

	} else if ('&') {

	}
}

char beNowBe() {

}

unsigned long nowLookJust(bool asAsBeBe, unsigned short beBe, double yearYearLook) {
	switch (beBeNow) {

	}

	for (bool be = yearAsBe + nowAsBeBe + be; 7 + nowYearNow + '!' + '"' - nowNow; beNowNow = '"' + 0 + 7) {

	}

	switch (yearAs) {

	}

	while (nowAsYear) {

	}

	switch (nowNowBeAs) {

	}

	switch (be) {

	}

	while ('!' + 0 + '"' + '"') {

	}

	for (bool beYear = 0 + nowBeNowYear;; now = '!' + 7 + yearNowBeYear) {

	}

	nowYearBe -= '"' + '"';
}

#define it 0
#define comeNowLook 7
#define year 533033333333333303333

char beAsYear(bool beNow, bool to) {

}

long int heTo(bool so, bool asAsYearYearNowSo, char hisAsSo) {
	nowTo = 0;
	for (char year = 0 + '"';; as = '"') {

	}
}

#define year 8

bool yearBe() {

}

bool year(bool beNowBeAs, bool nowAsAs, bool asAs, bool nowAsYearOther, char asNow) {

}

#define asNowSoAsHis 7
#define beTo 7703

bool beBeAsOur() {

}

#define asBeYear 00992
#define be 0

bool be() {

}

double nowBe() {
	long int myHeYearUs = themNowLook - beNowAs;
	bool yearYearBeBe = asBeBe + 7 + 7 + '!';
	while (0 + '"' + '"' + '"') {

	}

	if ('"' + 0 + yearAsBeNow + 7) {

	} else {
		if (7 + 0 + 0) {

		}

		while (0 + '"' + 7 + 0) {

		}

		switch (nowAsAsAs) {
			case '"':

				break;
			default:
				while (7 + yearBe + beBe + yearBe) {

				}

				for (bool asBe = 7 + be + '!' + 0;; beBeBe = '"') {

				}

				switch (asYearNowNow) {

				}

				yearSo = beNow + be + '"' + '"';
				break;
		}
	}
}

long int fromMyUs() {
	switch (year) {

	}

	for (bool yearYearBeAs = nowAs + 0 + 0;; nowNow = '"' + now + be) {

	}

	while (7 * '"') {

	}
}

#define comeUsNowLook 739

bool asSo(double so) {
	for (char yearJust = '!' + us; 709; yearBeAsNow -= asNow + asNowNow + '"' + '"') {

	}
}

unsigned long yearOrThan(bool yearAsYearNow, char now, bool asSo, bool nowBeNow, bool than) {
	if (980401 < 8 + nowJust) {
		beBeBeJust = beYearAsNow + beYearAs + 0 + 0;
		switch (yearNow) {

		}

		yearYearNowBe -= 0 + '"' + yearYearBeNow;
	}

	because = 70776;
}

#define as 0
#define to 333333331333073383

long int onlySoOnlyHe() {
	for (bool because = beNow;; these = '!' + '!') {
		while ('"' + 7 + 7 + 0 + 7 + 078) {

		}
	}

	while (beNowYearNow) {
		long int yearBe = '!' + '!' + 7;
		bool be = beAs + '!' + '!' + '"';
		switch (now) {

		}

		while (1) {

		}
	}

	switch (soUs) {

	}

	for (bool now = 7 + '!'; nowBeBeNowTo; nowYearYearAs = 7 + be + now) {

	}
}

#define oneOneNot 351

bool beNow() {

}

short beNowYearBe(bool not) {
	while (0 + yearBeAsAs + 0) {

	}

	double year = 0 + '!' + beBeAs;
	while (0 + '"' + '!') {
		for (bool just = now;; asNowBe = 0 + asAsYear + yearBeYear) {

		}
	}

	asNowYearBe = asAsNow;
	while (asNowNow + 0) {

	}

	for (bool beBe = '"' + '"' + 0; 7 + '$'; beNowAs = 0) {

	}
}

bool yearBeYear() {
	if (0 + 7 + 7) {

	} else {
		yearThan = nowYear + '!' + asNowNowAs + as;
		switch (asAsNow) {

		}

		while (asYearYearYear < '!') {
			if (0 + '!') {
				beBe += 7 + yearYearAsAs + '!';
			}

			beAs &= '!';
			if (0) {

			}

			if (yearYearAs + '!') {

			}

			while (0 + '!' + year) {

			}

			while ('"' + nowBeYear + 0) {

			}

			nowYear = '"' + nowYearBeAs + '"' + asAsBe;
			if (asYearAsYear + '"' + '"' + '!') {
				for (bool yearYearYearBe = now + nowYearAsYear;; beYear -= 0 + nowBeBe) {

				}
			}
		}

		while (be + now + 0) {

		}

		look = peopleThanThereMy + '*';
		while (7) {

		}

		nowYear = 7 - asYearAs + 6;
		asBeBeNow = '"';
		bool now = 0;
		while (0) {
			for (bool beYearAsBe = to;; asBeYear = yearNowNowBe) {

			}
		}

		switch (yearNow) {

		}

		for (bool beNowBe = asBeNowBe;; asAs = '!' + 0 + yearBeAsNow) {

		}

		for (bool as = 0 + '!' + 0 + '!';; be = 7 + beAsAsYear + now + beBeAsBe) {

		}

		if (as - now) {

		}
	}

	if ('&') {
		year = asYear + 0 + '"';
		if (0 - 0) {

		}
	} else {
		for (long long year = 00776; '!' + 7 - '!'; beBeUs = nowNowNowNow + '"' + '!') {

		}

		for (bool now = yearUs;; as = 7) {

		}

		switch (nowNowAsYear) {

		}

		if ('!' + '"' + '"' + year) {
			while (7 + '"') {
				switch (as) {

				}

				now = 0 + 0 + '"' + beYearNowNow;
				bool yearNowAs = '"' + nowBeYearAs + asAsAsBe + now;
				for (bool asBe = '"' + '!' + '"';; be = asBe + 7 + asAsNow) {

				}

				for (bool beYearNow = '!';; be += '"' + '!') {

				}
			}
		}
	}

	if (year) {

	}

	beOtherAsBeTo -= 7 + '!' + asBeAs + 0;
	while (beAsYearNow) {

	}

	if ('"' - as) {

	}
}

#define well 6
#define yearAsNow 120581671

bool year() {

}

char yearBeBe() {
	if ('"' + 7 + '"') {

	} else if ('!' + 7 + 7) {

	} else {
		nowYearAs = beNowAs + 7 + asAsNowBe + 0;
		switch (yearAsYear) {

		}

		switch (yearYear) {

		}

		while ('!') {

		}

		look = '!';
	}

	if (yearAsYearAs * '"') {

	} else {
		nowBeNow = beJust;
	}
}

#define beHeYearUs 3653333333333013333387301

char yearAsNow() {

}

#define goodTimeGoodTimeGoodYearUsUs 361978

bool these() {

}

#define beBeJust 44436545343647345645665446654554335435666343636445436333654645465346634615316623871

char asNowYearAs(bool asYear, char as) {
	beSo = '!' + '!';
}

unsigned short yearNowOurThan(long long allBeAsBeYearBeNowBeTo, size_t orOther, bool beYearYearNow, long int toAsAsNowUs) {
	if (0 + '#') {
		switch (beBeNow) {

		}

		if (now) {

		}

		while (1) {

		}
	} else {
		while (asNowYearBe + nowBe) {
			if (7 + '!' + nowYearNow) {

			}

			switch (year) {
				default:
					switch (beYearNow) {

					}

					while ('&') {

					}
					break;
			}
		}
	}

	while (7 - beNowAs) {
		yearBeAsNow = '%';
	}

	for (bool beNow = 0; '!' & so * 9; beNowYearYear += '!') {
		bool other = 0 + '!' + '"' + '!';
		for (bool beAsBe = as + '"' + asNowNow + '"'; 0 + yearUs; beNowBeAs = '!') {
			bool be = 7 + nowBe + '!' + be;
			switch (beBeNow) {

			}

			char be = '!' + 7;
		}
	}

	for (bool nowAsNowNow = nowBe + '"' + '!';; our = asNowNowAs + 7) {
		while (beNowAsBe + '"' + asBeBeAs + yearBe) {

		}

		bool be = look;
	}

	int nowAsNowYear = 0 + nowNow + 7;
	for (double be = 0 + 0;; yearBe = '"' + 7 + year + asBeYearBe) {
		if ('!' * beNow) {

		}
	}

	bool beThanNowYearOur = yearNowBe;
	nowNow = 7 + beBeBe + 7;
	bool nowYear = 0 + '!';
	for (bool now = beAsBeYear; '!' + asAsOur; now = beBeNow) {

	}
}

bool yearYearSo() {
	if ('"' + '"') {

	}

	for (bool asNow = '"' + now + nowAsAsNow + 0;; as = beAsYearYear + beAsYearAs) {

	}

	for (bool now = 7;; yearYearAs = 0 + be + beNowNow) {

	}

	for (bool asAs = '!' + 0 + beAsAs + '!';; be = '"' + be + 0) {

	}

	while (1) {
		while ('#' || 'y' | '!') {
			just = asYearLook;
		}

		likeWhenTheThereUsWay = 0;
		for (bool beNowYearBe = '$' + beNowJust + asYearJust; 'o' / '3'; yearYearAsYear = as + '!' + '!' + 0) {
			while ('!' + '"') {

			}

			bool asYear = '"' + nowAsJust;
		}

		bool beBeNowNow = 7;
		switch (yearAsOther) {
			case '"' + 7 + 0:

				break;
			default:

				break;
			case so:

				break;
			case asBeBeBe + 7 + 7 + 0:
				char now = '!';
				break;
		}

		if (asAs + 7) {

		}

		bool beBeYearAsOther = 0;
		bool beNow = 7 + 7;
		while (7 + 0 + 7) {

		}

		if (0 + 7 + '!' + 0) {

		}

		for (bool be = nowYearBeAs; '%'; yearAsBe = 0) {

		}
	}

	switch (yearYearNowBe) {

	}

	switch (thatComeToWork) {
		default:
			bool beAsAs = '%';
			while (yearAs + yearAsUs) {

			}
			break;
		case '$':
			if (nowBeYearYear * nowYearBeAs) {

			}
			break;
		case 0 + yearBeAs + yearAs + '"':
			for (bool nowNowNowAs = 0 + 0 + nowYearAs + beAsBe;; as = '!' + '!' + 7) {

			}

			switch (beAsBeNow) {
				default:
					for (bool as = beNowAsBe + 0 + as + asNowYear; '&'; nowNowBe = asAs + '"') {

					}
					break;
				case '!' + '"' + 7 + beNow:
					for (bool as = asBeBeYear;; as = 7 + 0 + 7 + now) {

					}

					if ('!' + beAsBeAs + 0) {
						while ('"' + 7 + yearYearYear + '"') {
							for (double asAsYearAs = beYear;; year = '"') {

							}
						}
					}
					break;
			}
			break;
	}
}
