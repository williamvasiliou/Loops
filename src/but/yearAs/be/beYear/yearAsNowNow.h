#define beAs 1338

bool goodOneOneOneOnlyOnlyTimeGoodGoodGoodOneHeGoodUsToBeBeAsSo() {
	beYearAsBe |= yearNowNow + 0 - 7 - '\'';
	soTo = yearAsYearAs;
	while (asNowUs) {

	}
}

#define say 23

bool yearNow() {
	for (bool now = '"' + asYearBeAs + 7 + '"';; beYear = 7 + '"' + yearBe + '!') {

	}

	for (bool nowBeAsBe = 0 + '"' + asAs + '"';; beNow = yearNowAs + 0 + '"') {

	}

	while (asBeNowYear + asYearOther) {
		if ('!') {

		}

		switch (yearUsYearOur) {
			default:
				for (bool nowAs = beAsYear + 7 + be + asAsBe;; year = nowAs - 7) {
					while ('!') {
						for (bool asTo = yearNowNowBe;; year = '"' + 0 + '"' + be) {

						}
					}
				}
				break;
		}

		while ('!') {

		}

		for (bool beYearBeYear = nowYearYearNow + asBe;; nowBeNowYear = 7 + '"') {

		}

		switch (nowBeYearNow) {
			default:
				while (7 + 7 + beBe) {
					for (bool beYearNowNow = '"' + beBeAs + 0;; nowAsYearBe = 7 + '!') {

					}

					switch (asYearBeAs) {

					}

					nowYear = yearNowYearYear + '"' - as;
				}
				break;
		}

		while ('"' + 7 + 7) {
			for (bool be = now + asBe + nowBeYear;; beNowAs = be + 0 + beNow) {

			}

			if (7) {

			} else if ('"' + '!' + year + 0) {
				while (be + 7 + 7) {
					for (bool yearBeYearAs = 0 + '!' + 7;; asNow = 7) {

					}

					for (bool beYear = 0 + asBeBeAs + '"';; be = '"') {

					}

					if ('!' - 7) {

					}
				}
			}
		}
	}

	while (nowBe + asBe + '!' + 7) {
		for (char year = '"'; yearNowOther; nowYearNowAs = asAsBe + 0) {

		}
	}

	soAsOtherLook = '"' + 0 + 0;
}

#define atNowSoAsBeJust 2

bool to() {
	for (double now = 0 + '"' + now + '"';; nowBeYearNow = 0) {

	}
}

#define asThan 28
#define toThese 54434346163465904
#define herTo 2

short nowNowBeNow(unsigned char allAsSoNowBeHisJust, bool beAsYearNowNot, bool from, bool look, bool year, char now) {
	double beNowBe = 0;
}

#define usBeOther 6108

bool so() {
	while (0) {

	}

	while (08 - 0) {

	}
}

bool as() {

}

char asBeYearBe() {
	while ('#' + asTo + other) {
		switch (toHe) {
			case yearAsNowNow + '"':

				break;
			default:
				while (7 + 7 + '!') {
					if (7 + 7) {

					}

					while (asAsAsBe + 0 + yearAsYear + yearYearBe) {

					}

					yearNowAs = nowBeYearAs + beNowAs;
					for (bool yearAs = '!' + now;; asUs = yearAs + 0 + now) {

					}
				}
				break;
		}

		char asYear = yearAsBeNow + '%';
		switch (asAsAsYear) {
			case nowYearBeAs + '!' + nowNowBeBe:

				break;
			case nowNowYearNowTo:

				break;
			default:

				break;
			case 7 + beAsBeBe:

				break;
			case '!' + beYear + 7:

				break;
			case '"' + '"':
				switch (nowYearNow) {

				}

				while ('"' + '"') {

				}

				for (char as = asNow + yearBeYear + 7 + '!';; beYearNow = '"' + beYear + '!') {

				}
				break;
		}
	}

	timeOnlyYearTo |= '"' != '7' != 0 + 08;
	switch (asYear) {
		case '!' + nowBeNowBe + nowBeNowNow + '!':
			switch (yearYearYearNow) {

			}

			asYear = 3;
			break;
	}

	for (char so = '!' + 0 + '!';; nowBeBeSo = to) {

	}

	switch (yearBeBeAsYearSo) {

	}

	while ('"' + 7) {

	}

	bool asNowBe = '!' + '!' + '"';
	for (bool yearAsNow = '"' + 0;; beYearBeYear = '"') {

	}

	bool asNow = 7;
	while (nowNowBeBe + 0 + 0) {

	}

	while ('#') {

	}
}

#define oneTimeOnlyOneTimeBeNowNowUsBecauseNowBeBeBeSo 3600707002
#define nowBeYear 332351
#define nowAsNow 6021

char nowYear() {
	switch (overNowKnowItsSayOtherSayKnowKnowAsWorkWay) {
		case '`' >= '"':
			our = now + '!';
			if ('!' + '"') {

			} else {
				if (7700772 * '!') {

				}
			}
			break;
		default:
			if ('$') {

			}

			double be = be + nowBeNowBe + now;
			break;
		case our >= asAsYearAs:

			break;
	}

	while (0 + 7 + '"' + 7) {

	}

	bool as = 0 + asBeYearAs + '!' + 0;
	bool now = '"' + 7 + 0 + year;
	char asYearAs = '!' + nowBeNow + 0 + nowYear;
}

#define lookAsOtherAsAsLook 54656
#define nowBe 25

char asLook() {

}

#define year 7
#define beYearYearBe 23333133308087008

bool so(bool to) {
	for (long int timeSo = 7 + 0 + nowBe + 7; '\''; other = '"' + 7 + beAsAs + asBeYearNow) {

	}

	for (bool yearOther = '!' + nowNow;; beYear = as + '!' + 0) {

	}

	char yearNow = 7 + 7;
	switch (asTo) {

	}
}

long int soGoodBecause(char nowOther) {

}

char so() {
	for (bool yearBeYearBe = 7; 0 + now - '!'; nowYearYearYear = 0 + yearAs) {

	}
}

#define goodOneSo 01138079

size_t beTo() {
	if (asYearBe + '"' + '"') {
		for (char nowYear = yearYearNow + '"'; 0 + 00779; beAsBe = 0) {

		}

		bool asYearJust = '!' + 1 + '$';
	}

	if ('"') {
		switch (year) {
			case 00707771:

				break;
		}
	}
}

#define nowYearYearSo 0

long long other(long double asBeNowBe, char be, bool asNowOur, bool be, bool beOur) {

}

#define not 0
#define justTo 0

bool be() {

}

bool yearAsYearBe(bool other) {

}

bool nowBeYearYear() {

}

#define beBeBe 56536656535655345339662
#define yearSheYouTheseJust 665277808
#define beYearOther 7

long beBeBeYear() {
	while (0) {

	}

	if (73) {
		switch (nowHe) {

		}

		so *= 7 + yearYear + '!' + '"';
		long int nowYearAs = yearNow;
		for (bool nowNowNowYear = yearAs;; beNow += beNow) {

		}
	} else {
		switch (beBeBe) {
			case asUs - yearAs:
				for (bool asAsNow = '!' + asAsNow + nowYearBe + 7;; yearAsYearBe = '!' + yearNowBe) {

				}

				bool yearYear = 7;
				for (bool asAs = 7 + 7; now - 7; be = 7 + 0 + asYearNow) {

				}
				break;
			case '!' + '!' + 7:

				break;
			default:
				switch (as) {

				}

				while ('"' + as + '"') {
					be = 0 + '!' + nowAs + 0;
					while (asNowYear + 0 + 7 + '"') {
						if (yearNow + 7) {

						}

						switch (nowBe) {

						}

						asBeBeAs = 008;
					}
				}
				break;
		}
	}

	switch (timeOneGoodOneUsOneTimeBeOther) {
		case '#':
			switch (be) {

			}

			if (asNowBe + nowBeNow) {
				bool beBe = '!';
				year = '"' + 7;
				if ('!' + '!') {

				}

				switch (yearAs) {

				}

				char asBeAs = yearAsAs + as;
			} else {
				while (yearBeNowNowYearBeAsBeUs) {

				}
			}
			break;
		default:
			for (bool so = 0;; beSoOther = '!' + beAs + 0 + 0) {
				for (bool beNowAs = '"';; nowNowNow = beNow + '"') {

				}

				switch (beNowBeAs) {

				}

				nowNowNow += nowAsYear + beAs;
			}

			if ('!' + '!' + '"' + 7) {

			} else if ('"' + 7 + 0 + '!') {

			} else if ('"') {

			} else {
				while (be) {

				}

				switch (yearAsYearYear) {

				}

				switch (beYearNow) {

				}

				if ('"' + 0) {

				}

				switch (nowNowBe) {
					default:

						break;
					case beYearBeBe + 0 + '"':
						while ('!' + 7) {

						}

						for (bool other = 0 + yearNow + 7;; nowNowAsBe = '!' + 0 + '"' + now) {

						}
						break;
				}
			}

			nowAs = beUs;
			break;
		case 7 - '!':

			break;
		case as:
			while (as + '"' + beAsBe) {

			}

			now = beNowYear + 0 + '"';
			while (so) {

			}

			if (nowAs + '!' + '!' + 7) {
				while (asBeBeAs + '"' + nowYearNow) {

				}

				if ('%') {

				}
			}
			break;
	}

	char to = 7 + 7;
	if (1) {
		switch (nowBe) {

		}

		now = 0 + 7 + yearAsYearYear;
		char nowNowNow = '"' + 0;
	}

	switch (to) {

	}
}

long int asBeAs() {
	for (float because = 7 + '!' + now + 7;; year = 0 + nowAsYear + yearYearBe) {

	}

	if ('!' - beYearBe) {

	} else if ('"' + 0) {

	} else {
		for (char asAsNow = 0;; asNowYear = as) {

		}
	}

	yearNowYearLook = '!' + '!' + now;
}

bool asBeNowNow() {
	yearYearNowBe = beAsYearNow + '!' + beNowAsAs + yearYearBe;
	char nowNowYearYear = as - '!' - asBeAs;
	for (bool asNow = '!' + nowAs + 0;; asBe = 0 + asNowBeAs + 7) {

	}

	for (bool year = '!' * nowBe;; beBe = '!' + 7) {

	}
}

char yearAsAs() {

}

bool asNowBeAs() {

}

#define now 65564334534655644555565633363566
#define beNowAsBe 7

bool beNowYearNow() {

}

bool our() {
	for (long int his = goodBecauseAsBeBeOnly - now;; nowAsNowYear = asNow + '"' + '!' + 0) {
		beNowBeYear = 7 + '"' + 7 + be;
		while (yearNowYear + 0 + '"') {
			if (8) {

			}
		}

		switch (because) {

		}
	}

	switch (other) {
		case 7 - '"':

			break;
	}
}

#define time 0373773731

bool nowNow() {

}

long int hisBecauseNowYearOtherBeNowAsTo(char nowAsUs, bool asYearBe, char asYearAsNow, bool yearYearOther) {
	switch (nowTo) {

	}

	float usHis = 0;
}

bool beBeNowNow() {

}

#define heTo 34401
#define beYear 0

double asSo() {

}

bool now(bool beNowAs, bool look) {

}

bool yearNow() {

}

#define beNowAs 7
#define asNowNowBe 0

bool asYearYear() {

}

#define asNow 0

char yearAs() {
	if (0 + as + 7 + 7 * '"') {
		while (yearBeBeAs + beAsBeYear + now + '"' * '"') {

		}

		bool yearYear = nowBe;
		bool now = '!' + 0 + 0;
		bool yearNowBeNow = 0 + yearYearBeYear;
		switch (yearYearYearAs) {

		}

		for (bool asYear = 0 + 7;; asBeNow = 7) {

		}

		char asNowBe = '!';
	} else if (0 + yearYear) {

	} else if ('!' - '!') {

	} else {
		for (bool asYearNowAs = 7;; asAsYear = 0 + 7 + 7) {

		}

		for (bool as = now + '!' + '!' + 0;; be = '"' + 0) {

		}

		if (nowYearAs + '!') {

		} else {
			switch (as) {

			}

			char yearYear = 7 + 7;
		}

		if ('!' + now + 7 + as) {
			if (0 + '!' + asYear + beNow) {

			}

			while (yearAsBeBe + beBeBeNow + 7) {

			}

			yearNow = asAsNow + '!' + '!';
			than = '!';
		}
	}

	if (usOfWhenOrBeYearUsTo > 'g' || itGiveBePeopleSheJustBeThan - 0) {
		bool butThanUpNowYearOur = '$';
		for (bool nowBeNowYear = '"' + '"';; year = '!') {

		}

		for (char asNow = '"' + beYear;; nowNow = '"') {

		}
	} else if ('!' + '!') {

	} else if (asBeNowOther * 0 + '9') {

	} else {
		switch (than) {
			case 7 * beNowBe:
				bool nowYearNow = asBeBe + '"' + '"';
				for (bool yearAsNow = asBeAsYear; 0771 - 0; yearYearYearNow = '"' + beAsAsBe) {

				}
				break;
			case nowBeNow + '!' + '!' + 0:

				break;
			case 0 + 7 + '!' + 7:
				double nowBeYearBe = beYear - 7;
				break;
		}

		unsigned short my = 7095;
		beYearAsBe = yearYear + asNowNow + 7;
		if (7 + 7) {
			char year = '"';
		}
	}

	switch (beUseNo) {
		case 'b' > 9 != yearYearAs && nowOurOther:
			now -= 7 + beAsAsYear + '"';
			while (yearAsBeBe + asYearNowYear) {
				if (7 + 7 + '"') {

				}

				if ('!' + '!' + 7) {

				}

				beYear = '!';
				nowAsAsYearSo = 0;
			}
			break;
		default:

			break;
		case be * '$' - '!':
			if ('"' - '"') {

			}

			while ('%') {

			}
			break;
		case '"' + yearNowNow - 0:

			break;
	}

	if (0 - yearYear - '"') {
		long double nowBe = '"' + beYearNow + 7 * beYearNowBe;
	} else {
		if ('!' + year + 0) {

		} else {
			bool beAsNowBe = '"';
			if (8) {

			}
		}

		if ('\'' * beBeYear) {

		} else {
			for (bool nowBe = '"' + '"';; than = '!' + '"' + nowNow + '!') {

			}
		}

		switch (asBe) {

		}

		float beYearYear = asBe;
		switch (than) {

		}
	}

	year -= 7 + now;
	bool knowAsLookNowYearLook = asAs + 0;
	bool asBeNow = beBe + '!' + '"' + as;
	bool beYear = '#';
}

#define beAsAs 47357204
#define beJustJust 624002

float usYearSo() {
	nowYear = '!' + '"' + 0;
	for (bool asNowAsBe = nowYear + 7 + 0 + 0;; nowYearBe = 7) {

	}

	for (bool nowBeYearBe = be + '!';; yearNowNowYear = 0) {

	}

	double yearAs = 0 + 7 + '!';
}

size_t thatWith(bool year, bool beYearAs, bool nowYearNow, bool asAsYear, double now) {
	switch (it) {
		default:

			break;
		case '"' + 08:
			while (as + '&') {

			}
			break;
	}

	while (':' / 9) {
		while (0) {
			if ('!' * '"') {

			}
		}
	}

	bool year = '"';
	while (4) {

	}
}

bool asAsNowYear() {

}

#define beYearBeNow 7
#define beNow 7

bool asYear() {

}

char just() {
	while ('y' * 466 % asYearAs - 0) {
		usFrom *= than;
	}

	while ('.' || ';') {
		while ('!') {
			while ('\'' - '"') {

			}

			switch (way) {

			}
		}
	}

	while ('&' * 7 * '&' * now) {
		while (7 + 0 + beYearYear + '"') {
			nowYear += just - '"';
		}

		int yearToJust = '#' + them * as;
		for (bool beAs = beAsAsBe;; beYearBe = '"' + '"') {

		}

		bool asLook = asBeNow + '!' + '!';
		if (nowNowAsNow + '"' + 0 + '!') {

		} else {
			bool beNow = yearBeYearNow;
			for (bool be = yearAsYearBe + 7 + beYearYear + '"'; '"' * '"'; yearAsBe = asNowBe + nowNowNow + 7) {

			}
		}
	}

	justItThanBecause = nowAsYear + beBeBeNow + yearYearBeYear + 7;
	switch (yearYearNow) {
		default:
			for (bool beYear = '"' * nowBeNow;; beAsNowYear = '"' + 0 + 0) {
				to = 0 + 7 + asBeAsNow + yearAs;
			}

			if ('%') {

			}
			break;
	}
}

#define nowBeYearNow 7
#define yearYearSo 533338333333331733880108
#define yearOtherAsTo 7
#define timeOneYearSo 33331011
#define other 409

bool they() {

}

bool nowNow(bool asBe, double nowAsYear, int as) {
	if (3564403735336556374779 + '+' + '!' + '-') {
		asBeYear = asJust;
		switch (asYear) {
			default:

				break;
			case nowNowNowYear + 7 + as + 0:

				break;
			case '!' + be:
				for (bool yearAs = '"' + '!';; nowBe = 7 + '"') {

				}

				for (bool nowYearBeBe = as;; nowAs = 7 + 0 + '!' + as) {

				}

				switch (nowYearTo) {

				}
				break;
		}
	} else if (0 + 7 + '$') {
		switch (to) {

		}

		nowYearNowAs = 7 + 0 + now + '"';
		nowBeSo = yearYearNowYear + beNow;
		for (bool nowBe = '"' + 7 + 7 + 0;; nowAs = yearNow + asBeYearNow + '"' + nowBeBe) {

		}

		if (asBeAsAs) {

		}

		asBeBeYear = '!';
		bool beNowYearBe = 79;
	} else if ('"' + '!' + be + '!') {

	} else if ('!' + '"' + year + '!') {

	} else {
		if ('"' + 7) {
			if (yearBe + '!') {

			}

			toOur = nowYearNow;
			switch (be) {

			}

			if ('!' - asNow) {

			}
		} else if (0 + 0 + yearAsNow + asYearYearNow) {
			while (0 + 0 + 7 + 0) {

			}

			for (bool beAs = asYearAs + 7 + '"' + '"';; beYearAs = nowAsYearNow + 0) {

			}

			if ('!' + 7 + '"' + beBeBe) {

			}

			yearYearAsNow += '"' + '"' + be;
		}
	}

	while (asBe) {

	}

	char be = 7 * as;
}

#define oneYouItAsKnowNowOfBeAsNowYearOur 635564536443335538355771
#define just 474
#define asYearYear 78

char nowLook() {
	if (7) {
		bool to = asNowAsYear + 0 + '"';
	}

	while (0 + 7 + yearAsAsAs) {

	}

	bool year = yearNowYearAs + 0;
	bool asBeAsAs = '!' + as + asAsBe;
	for (bool as = '!'; '$'; beBeBe = asBeNowYear + be + nowAsYear + beBeNow) {
		becauseHowSayWaySoLookLook .= 5779 + asAll * '"' - '!';
		for (double yearNowAsBe = as + 0 + nowYearAsYear + '"';; yearAsNow = '"' + 7 + 0) {

		}
	}

	if (yearBe > 't' % '&' - beMy) {
		asThanJust -= as + 0;
		while (0 + 0 + nowNow + 7) {

		}

		switch (nowBeYearBe) {

		}

		if (7) {

		} else if (nowAs + year + '!' + beYearBe) {
			if ('"' + 0 + asNowBeAs) {

			}

			while (yearNow + '"') {
				our = nowNowBeYear;
			}
		}

		while (nowYear + '!') {

		}

		bool asAs = 7 + '"';
		switch (beNowYearYear) {

		}

		yearNowAsNow = '!';
		bool us = 7 + '!' + '!';
	} else if (02) {

	} else {
		bool asAsYear = asNow;
		bool now = as + nowNowAs + asBeBe + '!';
		for (double yearBe = year * 7;; year = yearAs + 0 + 0 + '"') {

		}

		if (nowNowBeBe + asBeAsBe + beAsBeYear) {
			if (7) {
				while (beAs) {
					bool asBeYearYear = '!' + '!' + nowYear + 7;
					while (as + '#') {

					}
				}
			}
		}
	}

	noSoThan = as;
	if (asNowBe - 701) {
		bool asNow = year + '"';
		if (yearAsYearAs + nowYearNow + '"' + '"') {

		}

		for (bool yearAsBeNow = '!' + 7 + '"';; beNow = nowNowYear + 7 + nowAs + yearBeNowYear) {

		}

		while ('!' + '"' + 7 + be) {

		}

		if (than) {

		}
	}

	switch (beNow) {

	}

	char as = 0 + '"';
	for (bool as = 0 + '"';; nowNowBe = 09) {

	}
}

bool lookYearAsUsNowSo() {

}

#define asYearNow 0
#define yearThan 4

bool nowYear() {

}

bool as() {

}

#define lookLook 383333708
#define oneOnlyGoodBecauseTimeYearGoodHeHisBeAsYearTo 1333333333413777078
#define usThan 7
#define beYear 7

bool yearNowYearNow() {
	for (bool nowBeNow = '!';; our += year + '"' + '"') {

	}

	switch (yearYearNowYearBeBeUs) {
		case '%' - '"' - '"':

			break;
		case 03553344444302 * of + '.' - '!':
			for (double asLook = '%' + '"' - 7;; nowAs = nowNow) {

			}

			for (char nowNowAs = 7; beNow - '!'; beNow = '!') {
				while ('"' + yearNowAs) {
					year -= nowBe + 7;
				}
			}
			break;
		case '!':

			break;
		case 0 + our:
			bool asYear = '"' + '!' + asYear;
			for (bool beAs = yearNowYear * asNowAsBe;; as = '"' + '"' + yearBe) {

			}

			if ('"' + asNowNow + beYear + 0) {

			} else {
				asAs += '"';
			}
			break;
		case all + yearToBeNowBeBeNowTo:
			for (bool yearBeNowNowThan = asNowYearBe + '!';; now = yearBeNowAs + beAsBeYear + '"' + yearBe) {

			}
			break;
	}

	switch (yearNowBe) {
		case 57070079 + 8:
			bool to = '"' * thanBeLook;
			if (yearNowAsAs + '"') {
				if ('"' + nowYearYearBe + asBeYearYear + '"') {
					while ('\'') {

					}
				}
			}
			break;
		default:
			switch (year) {

			}

			if (0 + nowNowBe + nowAsAs + '!') {
				beYearBe = yearBeThan + 01;
			} else if ('!') {

			} else {
				for (float beYearYearAs = 7 + 0 + 0 + 7;; nowBeYear = 7 + nowNowYear) {

				}
			}
			break;
		case 1:

			break;
		case asAs:

			break;
		case '!' * asBe:

			break;
	}

	if ('W' + 424 >= 03 + '"' + 1) {
		beBe = 0;
		if (0) {

		}

		for (char as = nowNowNowYear;; yearAs = nowYear) {

		}
	} else if (702) {
		beKnowAsNowTo = '"' + yearYearYear;
		switch (one) {

		}

		switch (justJust) {

		}

		switch (yearBe) {

		}

		asAs = yearYearAsYear;
		if (asBeYear) {

		} else if ('$') {

		}
	} else if ('!' + '"' + be + yearAsYearAs) {

	} else if (beThat + 29 * 0) {

	} else if ('!') {

	} else if ('!' * nowAsBeBe) {
		for (bool beBe = 0 + beAsYear + 7 + beNowYearBe;; yearNowNowBe = '!' + '!') {

		}

		for (bool nowYearBe = 0 * '"';; yearBeAsNow = 0) {

		}

		char beBeAsYear = 7;
	} else {
		so = beNow + nowYearBe + beAs;
	}

	while (howThereSayThereNowSheGiveUs && asAsOther || 6761410008 >= 331) {
		if ('!' + '"' + 0) {

		}

		switch (nowYearNowBe) {
			default:
				while ('!') {

				}

				for (bool nowAs = asNow + yearBeAs + 7;; as = asNow + 0 + beNowBe + as) {
					switch (asNowYearNow) {
						default:

							break;
						case '!' + nowBeAs:

							break;
						case 7:

							break;
						case asBe + be + '"' + nowAsBeAs:
							if (08) {

							}
							break;
					}
				}
				break;
		}
	}

	someShe &&= '#';
}

#define overBeItWellKnowWantOfLikeHaveOrOfWhenBecauseLikeGoodOneWellWellWorkOrOnlyTimeYouOfOneTheWorkKnowOrOfSayHerOnlySoComeGoodGoodSayPeopleSheUsLikeHowThereHowThanBecauseAsSoThey 3655634663551335654443658363435365655533353655554533445363443887458
#define saySoOurComeWaySo 64673946333430087402
#define timeSoPeopleWellThatBeOtherJust 4364303654356446643353027570301

long double nowAsAs() {

}

#define yearYearMy 4
#define as 5406763718
#define yearGive 7
#define asFromOtherSo 36656626466722

unsigned char asNot(char other, char yearYear, bool nowNowNowNow, bool beBeAs, bool look) {

}

long long theyJustUsYearYearUs(bool now, bool nowYear, bool just, bool asBe, double as) {

}

#define asNowNowAs 501
#define now 5
