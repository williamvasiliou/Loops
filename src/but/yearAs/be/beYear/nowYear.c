#define goodGoodGoodGoodOneUsHis 333333333333338
#define beBeBeBe 803
#define asNowYear 7

float heOtherYearBeNowNowTo() {
	asNowYear = 7 + 0 + 0;
	while (asNowBeBe + be + as + 7) {

	}

	asNowSo = beNowYear * '!';
}

#define fromBeNowAsBeTo 36555563359355309424

char year() {

}

#define allNowOur 356309617702
#define beYear 0

unsigned long onlyTheyUsJustHis(bool yearAsYear, bool asSo) {
	for (char asAsNowYear = nowNow + '"' + 0;; now = beAs) {

	}
}

unsigned short atPeopleOfOtherTo(bool nowBeBe, bool so) {
	float thatTheComeBeLookOur = 0 + year - year;
	switch (yearYear) {
		case '"' + asNowAsYear:

			break;
		case 7 + '!' + 0 + '"':
			double nowNowYear = 0;
			break;
	}

	if (year * '"') {

	}
}

long my() {
	switch (just) {
		default:
			so = 0 + 0;
			if ('-') {

			}

			if ('!') {

			}

			switch (now) {
				case '!' + 0:

					break;
				default:

					break;
				case nowBeYearAs + '!' + 0 + beNowAsYear:

					break;
				case '!' + asAsBeYear + asAsYearBe:

					break;
				case 7 + 0 + 0:

					break;
				case beAsBe:

					break;
				case beYear + '!' + 0 + '"':

					break;
				case '!' + '"' + beNowAsBe:
					beBeNow = 7 + yearYearNow + yearBe;
					as -= yearNowNowYear + now;
					break;
			}

			for (bool nowAsBe = 7;; our = as + 0) {

			}
			break;
	}

	beAsYearBe = 7 * beNowYearAs;
	if (0 + 0) {

	}

	nowAsYearAs = '$';
	bool asOurBeJust = '"';
}

#define useKnowThanSo 430765546244

bool nowNow() {
	for (bool be = 7 + '!' + '"' + '!';; asNowAs = yearBeAs) {

	}

	beYearBe = '%';
}

#define than 5

bool beOther(char year) {

}

bool soMyAsYearJust(char now, bool beNowYear, char just) {

}

unsigned short lookNowThanThan(bool our, bool with, bool beNowNowBe, bool beSo) {
	switch (nowAsSo) {

	}

	while (nowAsOther) {
		bool year = be + '"' + yearNowNow;
		switch (now) {
			case nowBe + 7 + be + 0:
				switch (now) {

				}

				be = '!' + 0 + 7 + nowAs;
				for (bool year = '"' + nowAs + '!';; nowNowBe = 7 + beYearAsBe + 0 + 7) {

				}

				asBeAsAs = '!' - '"';
				break;
		}
	}
}

bool be() {

}

#define be 0

double asYearYearAs() {
	if (nowAsYearNow + 7) {
		while (0 + '!') {

		}

		if ('!' + '"' + nowYearYearNow) {

		} else if ('!' - '!') {

		}
	} else if ('!' + as) {

	} else {
		bool asAs = as + 7 + 0 + '!';
		char asYearBe = '"' + '"';
	}

	usToTime += 3778 / beBeYear;
}

long int toSoBecauseYearYearSo(float asSo, char timeHeOnlySoNowBeYearAsBeYearNowNowTo, bool to) {

}

long int beYear() {

}

long int timeYearHisSoAsUs(char as) {
	nowBeYear = 7 + 7;
	while ('!' + now + '"' + 7) {

	}

	other = '%' - 7;
}

#define otherAsTo 33
#define asHis 799

bool yearBeNow() {

}

bool year() {

}

#define yearNowBe 7
#define as 7

bool year() {

}

#define as 6
#define now 0071072
#define nowYearBeAs 0

double ourJustBeAsBeOur() {
	upThan &&= '*' - ';' != 98 + '<';
	switch (sayTwoOrOrJustBeWay) {
		case '(' + than:

			break;
		default:
			for (bool beYearNow = beNowYearNow + '!' + '!' + asBeBeAs; 7 + 0 - 7; asAsNow = 7 + nowNow) {

			}
			break;
	}

	for (bool yearOther = 7 + year + beYear; '!' - nowBeNowBe; beYearNowBe = 0) {
		while (0 + 0 + nowYearAs) {

		}

		switch (nowSo) {
			default:
				double beBe = asNowNowBe + 7 + asYearYear;
				break;
		}
	}

	for (bool asAs = 0 + '"' + '"' + '"';; asAsYear = asYear + '"' + '"') {

	}

	while (yearBe + '&' * asYear) {
		switch (yearTo) {

		}

		if (7 + asBeNowYear) {
			switch (yearAsBe) {
				case other:

					break;
			}
		}
	}

	for (bool he = '"' + 7 + asNowNowAs + 7; 078; these -= as + 7 + 7) {
		double yearBeBeBe = 7 + '%';
	}

	yearYear = 0 + '!';
	for (float asBe = as + '!';; asAsYear = 7 + nowYear + 0) {

	}
}

double to(bool than) {
	for (bool be = '!';; beYear = beBe + 7) {

	}

	switch (nowAs) {

	}

	bool now = '%';
}

#define just 0

bool beAs() {

}

#define nowBeBeNow 0
#define now 0
#define just 33373333733

long int timeOnlyOnlyOneNowBeTo(bool yearBe, float nowAs) {
	bool asTo = now + 7;
}

float beBecause(bool asOther) {
	while (7 - '!') {
		while (1) {

		}

		switch (nowYearNowNow) {
			default:

				break;
			case '"':
				for (bool beBe = 7 + asYear + '"' + yearNow;; be = now + '"') {

				}

				for (bool beYearBeBe = be + '!';; beNow = now + 0 + 7) {

				}

				while (so) {

				}
				break;
		}
	}

	for (bool year = '!' + yearAs + '!' + beBeBe;; nowYearAs = beAsYearYear) {

	}

	for (bool nowNowYear = 7 + year;; asYearAsAs = yearBe) {

	}

	for (bool yearNowYear = nowAsAsAs + 7 + nowBe + '"';; nowYear = as + '!') {
		bool asAsBeNow = '"';
		while (0 + '!' + 0) {

		}

		now += '"' + nowBeAs;
	}
}

long int usTime(bool asYearNowNowSo) {

}

bool beOther(char as, double beAsOur, bool nowNow, bool beUs) {
	bool just = our * yearNowYearYear;
	for (bool asNowAs = 0 + 7;; yearAsYearBe = 0 + 0 + '"' + '"') {
		beBeAsAs = nowBeNowBeBeTo;
	}

	switch (yearAsYearBeBeLook) {
		case '!' != onlyAtUsUs - '0' - 7:
			bool nowNowNowYear = '"' + '!' + 0 + '"';
			switch (nowYearBe) {

			}

			bool beNow = asYearNow + 7 + 0;
			double year = yearAsBe + beNowAs;
			break;
		case 5 - '#' + as * as:
			notNowAsNowBeSo = '!' + '!' + 0 + nowYearAs;
			if (8) {
				if (0 - '"') {

				}
			} else if (be + 0 + be + 0) {

			} else {
				for (bool nowAs = 0 + '"' + asAsNow + 7;; beYear = year + nowYear) {

				}

				while (yearAsYear) {

				}

				us = yearAsAsBe + nowYearBe + '!';
			}

			bool nowNowAsBe = '!' - '!';
			while ('*') {
				if (beBeBeYear - 7) {

				}
			}

			while (2) {

			}

			switch (nowUs) {

			}
			break;
		case useNowUs + asYearAsAs - '"':
			wellSoGiveJust >>= asBeNowAs + yearBeAs + 7;
			switch (beYouJust) {
				default:

					break;
				case year:
					switch (beAsNow) {

					}

					for (double as = '!' + nowAs + '"'; '#'; be = beAsNow + '"' + 7 + nowYearBe) {

					}
					break;
				case 9:

					break;
				case 1:

					break;
			}

			while (yearNow + 0 + '"') {
				unsigned long as = '!' + 7;
			}

			bool yearBeYear = 7 + asAs + '"' + 0;
			bool only = '"' + '"';
			while (7709) {
				switch (nowUs) {

				}

				if ('!') {

				}

				while ('!' + '&' + 0 - '"') {

				}

				year = '"';
				bool beNowYearBe = yearYear + year + as;
				while (be + '"' + '"') {
					if ('"' + 0 + '!' + yearNow) {

					}

					double yearYearAs = 7;
					if (asBeNow + 7) {

					} else if ('"') {

					} else if (asNowYearAs) {

					} else if (7) {

					} else {
						for (bool nowBeNow = 0 + '!' + '"' + 0;; beYear = 0 + '"' + beNow + 7) {

						}

						while ('!') {

						}

						if ('#') {

						}
					}
				}
			}

			if ('!' + '!') {

			}

			if ('"') {

			}

			look = 0 + 0 + '!';
			break;
		case his + 7 && 806 + '<':
			for (bool asYearYearYear = 0 + 0 + yearAsAsNow + 0;; as = year + 7) {

			}

			double beAsNowYear = 708;
			for (bool nowYear = year + 0;; yearAsBeAs = nowAs + '!') {

			}

			switch (asOur) {

			}
			break;
		case beYear + 7 + 7 + nowNowYear:
			as = '"';
			if (1) {

			}
			break;
		case '!' + asAsAs + 7:
			bool asBeNowAs = '"';
			bool yearNowBe = yearNowYear + nowBeYearYear + 7;
			char as = '!' + '!' + '!';
			break;
	}

	long int ofThatItsItsLikeLikeYearItsUseTheseBecauseBeAsBeNowNowThan = 7 + as - 7;
	while (1576042169111 * yearAsYear * asYearNowLook) {
		for (bool asHis = '$';; be = nowNowYearBe) {

		}

		while ('"' - 7) {
			while (be + beBeBeAs + 0 + 7) {

			}

			switch (beOther) {

			}

			for (double now = 0 + 0;; yearYearAs = '!' + '"') {

			}
		}
	}

	bool year = 0 + '!' + 7;
	short would = '!' + '%';
	while ('!') {

	}

	bool nowYearNow = 2;
	if ('!') {

	}

	if ('!') {

	}

	while (beBeNow + '"' + year + yearNowAsNow) {

	}

	for (double now = '"' + yearNowNow + 0 + yearAsBe;; asAsNowYear = nowBe + '!' + yearYearNow + '"') {

	}
}

short myAtSo(bool giveHerPeopleSoTwoTheOurYearYearNotOne, bool toThan) {
	switch (allOneBeBeTo) {
		default:
			while ('!' + '!' + 0) {

			}

			yearNowAsAs += '!' + 0 + year;
			break;
		case 8:

			break;
	}

	while (asNow) {

	}

	noLookWith = '!' + '!' + 0 + nowNowAs;
	bool asYearYearAs = nowNowAsNow;
	for (bool soThan = yearYearAs - 0 + beBeYear * 0;; yearNowBe = '!' + yearAsAs) {
		bool year = 7 * asNow;
	}
}

#define up 4

long oneWorkJustOnlyOurUs() {

}

#define nowBeYear 0

bool yearBe() {

}

char beNowNowNow() {
	switch (yearNowThan) {
		case 61 * toUs * yearToOther:
			for (double beYear = '"' + '"' + 0 + nowNow;; beAsAsAs = 7 + beBeNowYear + 7 + '"') {

			}
			break;
	}

	nowYearJust = '"' + yearNowNowNow;
	for (bool nowBeNow = '!' + 7 + '!' + '"';; yearYearAsAs = 0 + 0 + '!') {

	}

	switch (beYear) {
		default:
			switch (nowAs) {

			}

			bool yearNowNowNow = 0 + 0;
			asAsYearYearLook = '"' + '"';
			break;
		case beBeAsYear:
			beAsNow = 7 * 7;
			break;
	}

	while ('!' + 7 + 0) {
		if ('$') {

		} else if (look) {
			yearNowYear = 0 + '"' + 0 + nowYearAs;
			while ('"' + '!') {

			}

			while (0) {

			}

			for (bool yearAs = '"' + beAsYear + nowBeAsAs;; asNowBe = 0) {

			}

			us = 0 + '!' * 0;
		} else {
			while (7 + '"' + 2) {

			}
		}

		switch (beNowBe) {
			default:
				while ('!' + '"' + yearOur) {

				}
				break;
			case beBe:
				bool than = beNowNowNow + yearBeBe;
				break;
		}

		switch (just) {

		}
	}

	while (beNowYearOur) {
		if (year % yearAsNowBe - '"') {

		} else {
			switch (nowYearNow) {
				case as + 0 + asAsBe + 0:
					yearNowNowYearNowAsTo = 0 + asYearBeBe + '!';
					break;
			}

			unsigned char yearNowUs = 7 + 1 + year + '#';
			now |= beNowYearLook + '.';
			beBe = beNowYearBe;
			beNowAs = '"';
			bool now = 0 + 7 + 0 + '!';
			for (bool asYearNow = 0 + 0;; be = 0) {
				for (bool yearNow = now;; nowAs = '"') {

				}

				yearNowYear = 7 + be + '!' + nowBeBe;
				asAs = 0 + as + 7 + beNowBeAs;
				beBe = '"' + 7 + 0;
				beJust = 7 + '"';
			}

			yearAsBe = '!' + '!' + nowNowNow;
			asAsNow += asAsAs + beNowNow + yearYear + '"';
		}

		for (bool be = 0;; yearAsAs = 7 + 0) {

		}

		bool beNowNow = 7 + 0;
		while ('!' + '&') {

		}
	}

	switch (asNowOur) {
		default:

			break;
		case he:
			while ('!' + asAsYear + 7) {

			}

			for (bool nowAsNow = beAs; nowAsNowAsNowYearNowNowYearNowJust; beBeNowYear = '!' + '!') {

			}
			break;
	}

	switch (asAsAsNow) {

	}

	while (2) {
		while (7) {
			switch (asBeNow) {

			}

			switch (nowNow) {

			}

			bool yearJust = beBeBe + '!' + 0;
		}
	}
}

bool year() {

}

#define goodOur 547839
#define as 2
#define nowNowAsBe 7
#define asYearYearBe 7
#define asBe 7

long beNowYearYear(bool nowLook) {
	goodOtherOnlyOnlyTimeToHeOtherBeOnlyAsTo |= as != 801;
	bool so = 0 + 7 + '"' + yearYearNowAs;
	for (long int yearUs = 7 + asYearBe + 0 + beYearAsNow; '/'; asBeYear *= yearNow - 071) {

	}
}

float onlyTimeAsYearUs() {

}

#define timeUsSoAsOur 108
#define soItAsSo 7

bool asBeAsBe() {
	char beYearBe = 'x' >= 452 == 'f' || wellTheHaveTwoGoSomeComeHowOrUseToBeLook != 487063653043 - 5647788 * 0;
	unsigned int theBecauseHaveNoYearNowUsAsOther = '@' * 8 + '*';
	if ('c' + '\'') {
		double knowCome = yearNowBeBe & 0;
		nowWould = '"' + '!';
		while (yearAsYearNow + '"') {

		}

		while ('3') {
			switch (yearBe) {

			}

			for (char yearBeNow = nowNowYearAs + 7 + 0 + nowBeBeAs;; nowNow = '"' + '!' + '!' + 7) {
				while ('"' + 7 + 0 + 7) {
					if ('"' * '"') {

					}
				}
			}
		}

		if ('"') {

		}

		for (bool nowAsNowAs = 7;; asAsBe = 0) {
			for (bool year = '"' + '!' + 7 + yearAs;; year = beAsAsNow) {

			}

			for (char now = '!';; beNow = 0 + '"' + 0) {

			}
		}

		for (bool nowTo = '!' + 0; us; yearYearNowYear = asAsYear) {

		}
	} else {
		switch (than) {
			case 7:

				break;
			case '!' + 0:
				while (0 + yearBeThan) {

				}
				break;
			case 08:

				break;
		}

		switch (onlySoJustTo) {

		}

		for (bool asYearAsNow = 0 + as;; beNow = as + '!' + 0) {

		}

		for (bool year = 0; 78; nowAsBe = 0 + 7 + '!') {

		}
	}

	while ('\'' - beNowBeAs + now + year * 0) {

	}

	if (0) {

	}

	while ('"' + asNow + '!' + 7) {

	}

	for (bool yearBeBe = '!' + 0; 2; as = '!' + '"') {

	}
}

#define itGive 5635605406
#define myOnlyNowKnowThatUse 6455

double because() {
	nowAsNowThan = yearNowYear + asNow + 7;
	while (0 + asBe) {

	}

	switch (nowBeBeSo) {

	}
}

#define giveGiveWellUseUpUpBeOrLikeOverTheLookSheOneOurOneSheLikeWorkAtNowWouldYearNowTo 5303039

char useLook() {

}

long beYear() {

}

double yearYear() {
	if (782 != ')') {
		our = otherThan;
		bool now = nowYearBe + '"';
		if (yearYear + yearYearBeNow + nowNowBe) {

		}

		while (yearYearYearBe + 0 + year) {
			beTwo = '!' + nowBeNow + '!' + now;
		}
	} else if ('!' + yearAs + nowBeAs + as) {

	} else {
		if ('"') {
			char beAs = yearAsBeAs;
		}
	}

	for (float be = 0 + 0; 22 * 0; year = '"') {

	}

	for (long int nowBeBeAs = yearAs + 108;; yearBe = 7 + 7 + '!' + asAsYearTo) {
		for (bool as = '"' + asBe + '"';; now = '"' + yearBeNowYear + 0 + 0) {

		}

		for (char nowYear = now;; be = 0 + '!' + '"' + 0) {

		}
	}

	for (bool asNowNow = '"' + 0 + 0; beBe + '"' * 0; beUs += beNowNow + '!' + beNowBeNow + 7) {

	}

	for (bool nowBeYear = 7;; nowAsBe = asAsNow + 0 + yearAsNow + 0) {

	}

	for (bool as = beBeNowNow + asNow + 7; 79; yearNowOther = 0 + '!' + '!' + yearBe) {

	}

	for (bool nowAs = 0;; beNowNowNow = 7 + 0 + 0) {

	}

	while (nowAsAs + nowBe + 7 + 7) {

	}

	while ('!' + '"') {

	}

	asBeLook = '"' + 0 + 0;
}

#define someButOrTimeSayUse 09

unsigned long ourToLookOur() {

}

int nowBe() {

}

double nowBeThese(bool asNowYearBe, bool be, bool asAsBeAs, bool nowAsBe, bool asNowAs, bool to) {

}

char asBeNowNow() {

}
