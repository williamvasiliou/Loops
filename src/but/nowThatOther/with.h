#include <U2UU6UUUUU6E00.h>

#define goodTo 3

long asYearBeYear() {

}

#define asNowBe 0
#define asTo 7

bool other() {
	bool nowNowNowAs = '!' + '!';
	bool yearNowYearYear = '"' + yearAsAs + beAsBeNow + nowNowYear;
	if (asYearYear) {
		bool yearBeYear = '"';
		for (bool year = '$';; year = yearNowBeNow + '!' + '"') {
			for (bool nowAsAs = beNowBe + 7 + '!';; be = 7 + '"') {

			}

			for (bool be = be + 0;; be = 0 + 0 + 7) {

			}

			while (0 + '!' + 7 + yearYear) {
				while (yearAsBe) {

				}

				while (0 + '!' + 7) {

				}

				for (bool yearBeBe = as + '!' + '"' + 0;; year = 7) {

				}

				beAsYear = 8;
			}
		}
	} else if (beAs) {
		than = 7 + '"' + nowBe;
	} else {
		if ('#' * '"') {

		}
	}

	while ('!' - yearYear) {

	}

	switch (as) {
		case as + '!' + '!':

			break;
		case beAsAsNow + 7 + '!':

			break;
		case now + '"' + '"' + '!':
			switch (beYearYearBe) {

			}

			nowBeYearBe -= 0 + '"';
			break;
	}
}

char as() {
	for (bool thatNowSo = '"';; nowAsNowAs = 7 + be) {

	}

	to = '!' + 7 + 7 + '!';
}

#define be 04608

long int timeGiveSoSoSoYearTo(long long theseYouBeAsSo, double our, unsigned char lookThese, long double us, unsigned long come) {
	while (5) {
		unsigned char year = asAsYear + 0 + nowAsAsBe;
		switch (be) {

		}

		while ('!' + 7 + 0) {
			switch (asYearTo) {
				default:
					for (bool just = beNow + '"' + '!';; beYear = '"' + year + '"' + as) {

					}
					break;
			}
		}
	}

	to ^= 798;
	for (bool year = '"';; beBe -= asYearBeBe) {

	}
}

#define now 7

bool as() {

}

#define nowNowYear 7
#define asYear 5333310303381
#define oneTimeSoOneTimeOneTimeToHeOneYearTo 33833333737474
#define yearYearBeNow 7

bool nowAsThan() {
	while (89 + beYear - 0) {
		while (beYearNow + 0 + be) {
			while ('"' + '"' + 7) {
				if (nowYearAsBe + 7 + 7) {
					if (beLook) {

					}
				}
			}
		}
	}

	while ('f' + 9 + 0 * 7) {

	}

	char yearBeAs = '"' * yearNowAsAs + yearAsBe + 02;
	if (077007071 + beBe < '"') {

	}
}

long long herUpWantOtherTwoFromYearSay(size_t asThanOur, double so) {

}

#define thereBeWould 4171078
#define nowNowBeNow 1
#define than 7

long long nowAsAllAt() {
	if ('!' / nowNow) {
		switch (nowNowAsNow) {
			case 08717000707701:

				break;
			case '!' - '"':

				break;
		}

		while (0 + '"' + 0 + 0) {
			if ('"' + 0 + '"' - 0) {

			}
		}
	} else {
		if (asNowUs + 008) {
			while (yearYearNowUs) {
				while (0 + '!' + us) {

				}

				switch (asTo) {

				}
			}
		} else if ('!' + yearYearBeNow + 7 + 7) {
			switch (asNow) {
				case us:

					break;
			}
		}

		for (long int beAsBe = yearBeBeNow / now;; yearBe = '"' + beAs) {

		}
	}

	oneTimeOnlyBecauseGoodUs |= beOne;
	for (char nowYearNowAs = '"'; nowYear - '!'; year = 7 + 0 + '!') {

	}
}

long int beBeYear() {

}

#define timeOneOnlyOurNowBecauseAsLookJust 9
#define ourYearAsLookOur 77702

double asAsNowNow() {
	while (asOnlyLookLookNowNowYearOther || 'c' > 66434633368664655443545364635654354456543035556334526336335664443356353406635454362564465335471 != 'u' && 'n' && howItsTwoHaveHerSomeComeUseGoKnowSheGoodNowTheyItsBeItsNotThemBeYearYearBut && 'p' == 3344466353335355344564355665354436654646435344534544665665606594658634474 >= itsOurItBeUs < 446430466) {
		while (7 + 7 + asNowNow) {

		}

		nowYouOneOrTwoThereKnowItsComeWantPeopleGoodItsThatUse ^= 6366356655443075 == twoUseTimeHavePeopleThatOneItLikeGiveThereSheHerOrOnlyItWantOverOfUpHowTwoWhenItBecauseOneWantGoodOneTimeGiveUpHaveGiveAsGoodSheWorkAsComeBecauseOnlyGiveHeTheAsIts != 6653345644653 >= '{' <= '4' + 78;
		switch (herOfThatPeopleComeOfOrSheOneGoodUseUseWantBeHerThereSheOnlyHerItsWantGoOverWantTwoTimeToOfOnlyItLikeItsTwoGoodOverUseBeGoodAtThemSoLookBeNowBeThan) {
			case 332436303329 | 33554346435463044343558699 - '-' != 91 > use:
				if (536674570675 | 7 != 'D' + year + '"' - beAsAs) {
					for (bool beBeBe = 7 + '"';; beBeBeNow = yearYear + asYear) {

					}

					for (float yearNot = '!' + '"';; yearBeBeAs = nowNowBe) {

					}
				} else {
					theThanJustThese -= ',' - beNowAs;
					float asNowAsYearNowSo = '"' * 0;
					if (7 + 0) {
						while (7 + as + 7) {
							so = '!';
						}
					}
				}

				if ('~' + now || now - now) {
					bool asNow = yearNowBe + 9 * '!' + '$';
				} else {
					switch (year) {

					}

					for (bool asOurUs = 7 + '!' + '"' + 0;; asYearBeYear = '"' + 7 + 7 + 7) {
						while ('"') {

						}

						double nowBe = '!' + 7;
					}

					nowIt %= '"' + '!';
				}

				nowYearYear += 0;
				while (yearNow != 7) {

				}

				switch (beYearAsNow) {

				}

				while ('"') {
					while (yearAsAsAs + beAsYearThan) {

					}
				}
				break;
			default:
				while (asYear + asNowNowYear) {
					while (thereHerThanJustOfAsSomeJust > '#' < '!' >= year) {
						switch (thereThatWayJustPeopleNowJust) {
							case 0 + 7 + '!' * 0:
								switch (beNowYearBe) {

								}

								while (other + '"' * be) {

								}
								break;
							case asAsYear + '#':

								break;
						}

						if (other) {

						}

						nowNowAs = nowAs + 7;
						beBeBe = 7;
						switch (beNow) {

						}

						switch (asAsBe) {

						}

						nowNow = '"' + 7 + 0;
						for (bool beBeNowBe = 7 + 0 + 0;; now = 7) {

						}

						bool yearBeAsAs = '"' + 0;
						if ('&') {

						}
					}

					while ('%' && toOur - 0) {
						bool beAsAs = '!' + 0 + asNow + 7;
						switch (as) {

						}

						char beBeYear = '"' + nowNowAs;
					}

					long long asBeBut = 3028 + 6 - 7;
					double other = yearNowAs + as + nowNow + yearNowBeAs;
					unsigned long lookBeNowNowUs = '!' + '!' + 0;
					beAsNowBe = yearThan * '"';
				}

				if (6304 - 1384) {

				} else if (3) {

				} else {
					year = '"' <= nowNow;
				}

				bool year = 0 + yearYearAs + '!';
				switch (year) {

				}

				while (yearBeAs + 0) {

				}

				nowAsBeYear = 7 + '!' + beYearYearAs + '"';
				for (bool asAsNow = yearBeYearAs + be;; as = '"' + 7) {

				}

				for (char as = yearBeBeNow + asYearBe + '"';; nowNowNowBe = asAsNowNow + yearNowYear + '!' + yearNow) {

				}

				if ('"' + yearBeYear + 0 + 0) {

				}

				if ('"') {

				}

				bool year = '!';
				if ('"' + 0 + now) {

				}

				bool to = 7;
				break;
			case '!' + 7 + 0:
				switch (so) {
					case nowNowAsYear + '"':

						break;
					default:

						break;
					case '"' + '"':

						break;
					case '"':

						break;
					case '#':

						break;
				}
				break;
			case '!' + yearNowBe * 0:
				if (just / beAsAsBe) {

				} else {
					if ('"') {

					} else if (beBeNowNow) {

					} else {
						while (0) {
							for (bool nowNowYear = '"' + 0 + as + nowBeNow;; nowAsBe = 0) {

							}

							if (now - 7) {

							}
						}
					}

					if ('!' + yearBeAs + '!' + asBeBeBe) {

					}

					while (0 + 0) {

					}

					char year = year + 0 + now;
				}

				if (to + '"' - 0 < year) {

				}

				for (bool nowNowNowBe = '!' + beBe; '!' - nowNowBeAs; beYearBeYear = yearYear + '!' * '"') {

				}

				double yearYear = '!' + 7;
				break;
			case 0 || 'p' & as + 1 + asAsAs - 7:

				break;
			case '"':
				bool beNowBeAs = asNow / yearYearBe;
				break;
		}

		while ('"' % so * 7) {
			switch (yearOtherYearYearOur) {

			}
		}

		while (thatNowBeYouNowOur - 'z' - '$' != 319 + 0 + '#') {
			while (asBe - '&') {
				if ('\\') {

				} else if ('"' + nowYearAs + '"') {

				} else if (7 + 7 + '!') {
					bool yearBeNow = yearAsAsNow;
					for (bool be = '"';; asAsAsAs = asAs) {

					}

					switch (asAsBeBe) {

					}

					bool beNow = beBe + 7 + as;
					while ('!') {

					}

					switch (other) {

					}
				}

				unsigned long useBeYearOur = 0 + '"' * asAsNowAs;
			}
		}

		asNow .= justSoNowJust % beTheseAsBeWant * '"';
		oneNowAt = asAsThan;
		if (to + be - '!') {

		} else if (look + 7 + nowAsBeNow * 0) {

		} else {
			if (0 + 7 + '!' + '!') {
				as = asNowBeYear;
				for (bool asNowAs = '"' + asNowNowYear + '"'; 78; asNowAs = 7 + 7 + year) {

				}
			}

			bool now = 7 + 7 + '!' + asYearAsBe;
			while (as + '!' + '!' + beYear) {

			}

			bool so = beBeNowAs + asBe;
			for (bool yearAsBeBe = '!' + '!' + 0 + 7;; nowAsYear = yearNowAsBe) {

			}

			bool yearNowAsBe = '"' + nowBeAs + '"' + '!';
			for (bool nowNow = 0;; now = '"' + 0 + asAsBeBe + 7) {

			}

			so = '"' + year;
		}
	}

	long long wantOnlyUseAsThan = soAsSo;
	for (long int asAsOurLookOther = 'r' == themTo; 053 > nowTheseNowHaveTo < beBeAs + 8; yearBeAsYear = nowNowNow + '!' + 7) {
		be = '"';
		while (7008) {
			while ('"' + '"' + 0) {

			}

			switch (now) {
				default:

					break;
				case '&':

					break;
			}
		}
	}

	nowTo -= 001051 + '&';
	unsigned int goodWayTo = 89;
	while (7 + '"' + asYear + '"') {

	}

	if (asNowYearBe + 0 - '!' + '&') {

	}

	us += '#';
	while (0) {

	}

	for (bool nowYearAsBe = 0 + '"'; '!' + 7 + '%'; beAsNowYear = '!' + asBe) {

	}
}

long int thatYouWellWaySoYouTheBeNowBeAsYearNowBeYearNowYearNowYearOther(int nowAsAs) {
	for (char nowNowTo = asNow + 0 + nowBeBeBe; 0 + beOther; asBe = beBeNow + 7 + 0) {

	}

	while (beNow) {
		while (7 + '!') {

		}

		if ('"' + yearBeBe) {
			nowYearBe = 79;
		}
	}

	for (bool asBeYearBe = beBeBe + beNow + yearYearNow;; year = nowYearBeNow + '"') {

	}

	if (now + nowYearNow + '!' + 7) {

	}

	for (char our = nowYearAs + 7 + '!' + 0;; yearNowBe = 7 + 0) {

	}
}

#define nowSoAll 46646546430633858
#define upHowOther 45585635571302
#define ofTimeUpWantGivePeopleWay 7

char beBeYearBe() {

}

bool yearAs() {

}

double beBeAs() {

}

#define than 3333731313
#define oneOneOtherAsBeThese 733333331333373
#define other 0
#define oneOnlyTime 38028078

double year() {
	int theseHerNowJustUs = 68;
	for (char asNow = '"' * '!'; 701 + notJustLook; thanBeJust = '"' + '!' + '"') {
		if ('!' + '"' + 0) {

		}

		bool beAsAs = 0 + '!';
		for (bool asBeYearBe = 0;; asAsNowAs = '"' + yearAsAsAs + '"') {

		}

		switch (beYear) {

		}

		switch (asYear) {

		}

		double yearBe = 7 + '"' + 7 + '!';
	}

	for (unsigned int nowYearBe = '"' > 'c' - asYearBe; than - timeThanTo; yearOther = 0 + 7) {

	}

	if ('!' * '=') {
		switch (beNowNowBe) {

		}

		if (0) {

		} else if (9) {

		}
	} else {
		for (bool other = 1;; yearBeBeAs = 0 + '!' + yearYearBe) {

		}

		for (bool beBeNow = asBeAsNow + 0 + '!'; 01; nowYearBeBe = yearNow + 7 + yearAsBe + '!') {

		}
	}

	if (7 + '#') {
		switch (nowAsYear) {
			case our:

				break;
		}
	}

	if ('"' * 0) {
		switch (our) {

		}
	}

	asNowNowYear += asAsBeYear + be + 7;
}

unsigned short when() {

}

#define beSoYearNowThan 7
#define yearNowAs 8
