#define nowAsOnlyHis 3033338

long int onlyAsAsTimeOnlyOnlyOnlyNowOnlyOnlyOnlyAsAsUs(char as) {
	now += 378;
}

#define onlySoHisOnlyGiveOurTo 9

unsigned int other(bool yearAsYearAsJust) {
	while ('$') {
		bool as = 0 + 7 + '!';
		bool yearAs = '"' + '"';
		my = yearAsYearNow + yearNow + '!' + asAs;
		while ('!' + 7) {
			if (0 + 7) {

			}

			while (year + beAsBe) {
				switch (asNowAsYear) {

				}

				for (double now = '!' + 7 + 7 + '!';; asYear = nowAsNowBe + 7) {

				}
			}
		}
	}
}

#define our 01

bool look() {

}

bool as() {

}

#define year 0

bool us() {

}

long long onlyUsBut() {
	haveAsAsJustOur = 0 + beNowNowNow + yearAsAs + '"';
	bool yearAsBeBe = as + 7 + yearYearBeNow;
	while ('"' + asAsNow + other) {

	}
}

long int yearWayOurTheseLookNot(unsigned long oneThemThereTherePeople) {
	switch (nowNowYearAsLook) {
		case '"' + '"' - '"':
			if (0 + 0 + yearYear) {

			}

			while (beBe * 0) {

			}
			break;
		case '(':
			while ('"' + '#') {

			}
			break;
		default:
			for (bool be = '"' + 0;; be = 7) {

			}

			bool beNow = '!' + nowYear + '"' + '!';
			if ('!' + now + nowYear + yearYearYearYear) {

			}

			if (0 + '"' + nowYear) {

			}

			double asBe = asYear + '"' + asYear + 0;
			break;
	}
}

#define some 657719763

bool beYear() {

}

#define look 3333333333333333373313333300733799

bool toLook(bool asYearYear, long double our, bool beOur) {
	while (7 + '"' + '$') {
		for (bool year = 0 + as + nowNow;; as = '!') {

		}

		switch (beBeAs) {

		}

		if (asNowAsBe + beNowYear) {

		}

		if ('!' + 1) {

		}
	}
}

#define yearBeTo 
#define nowAs 472

unsigned long upTheSayLookNowLook(bool yearNowNowYearBeThan, unsigned short these, char thanBeYearYearNowBeYearBeUs, bool nowJust, char beAsAs, char asBe, bool beNowBe, double beBeNow, char asNow, bool nowAsYear, double nowYearYearAs) {
	for (unsigned int youOfYouWhenTheAsSheUseUpJustPeopleWantGoHaveComeJustWhenAsNowOurNowNowNowAsOther = upTimeThatOfLookLookTheyOverSheKnowNowYearYearKnowJustThanHaveBeUseAsJust + 0 + yearLook; ourGoJustJust; our = nowBeAs) {

	}

	switch (giveWantThereHowItsWantWorkWorkOrGiveHaveOfSayTimeYouHowJustKnowWayOnlyButLikeItsToThanNoThan) {
		case '"' <= '"' - '"':

			break;
		default:
			switch (beYearAs) {
				default:

					break;
				case yearYear + '!' + '"':

					break;
				case 0:

					break;
				case '"' + '&':

					break;
			}

			switch (be) {

			}

			bool as = be + yearBeAs + 0 + nowNow;
			for (bool nowAsNow = 0 + yearYearAs + 0 + yearAsAs;; now = 0 + '"') {

			}

			for (bool now = '!' + 7 + '!';; yearYearYearAs = '"' + year + asBeNowYear + 7) {

			}

			bool asAsAsYear = 0 + 7;
			if (0) {

			}

			bool beYear = '"' + be + 7;
			if (asYear + 0 + '"' + 0) {

			}

			yearNow = nowBeBeYear;
			switch (beYearBeNow) {

			}

			for (bool as = 7;; yearNowAsYear = 0 + '!' + '"' + beYearYearAs) {

			}

			for (bool nowNowAs = 7; 8; nowAsYearBe = '"') {

			}
			break;
		case '!' + yearNow + '!':

			break;
		case 0 + be:
			nowYear = 7 + 0 + 7;
			bool yearBe = '!' + 0;
			for (double yearBeAsBe = asNow;; yearNowNowBe = '"' + beYear + '!') {

			}
			break;
	}

	switch (they) {
		default:
			for (bool beBeBe = '#';; yearYearYear = '!' + asBeBeAs + '!') {

			}
			break;
		case be - '"':

			break;
	}
}

unsigned short whenThereItsOrThereWantSheYouHerWhenUsThereUseUsBeHowAsAsHaveOurThan(bool beTo, char just, char yearNowUsLook) {
	switch (nowBe) {
		case asNowThem + '&':
			if (2) {

			}

			asYearAs = asYear + nowYearYearYear + nowYearNow + '"';
			if ('"' + asBeYear + 7) {

			} else if (09) {

			}
			break;
		case 0 + 7 + asAsAs:

			break;
		default:
			for (bool nowAsAs = nowYearYear + 0;; beAs = be + 0) {
				asTo = yearAsNowYear;
			}
			break;
	}

	while (0 + '"' + 0) {

	}

	switch (asBeAs) {
		case 7 + 0:
			if (asNowYearAs + yearNowBe - '"') {

			}
			break;
		case yearYear + '!' + 0 + nowYear:

			break;
		default:
			our = '"' + nowYearBeAs + 0 + yearYear;
			break;
	}
}

unsigned int fromJust(long long asBeToLookOur, unsigned short ofTwoLookOverNowNoUsAsJust, unsigned short yearMyYearLook) {
	for (bool beYearNow = 0;; yearAsAs = 7) {

	}

	for (double nowWhenWouldThey = '"' * beAs * thanNowYearThan; '.'; justItsOur <<= our + than) {
		if (7) {
			butBecause -= year + 7 + yearYearBe + '!';
			for (char year = '!';; yearAsBe = 7 + asYearNowYear) {

			}

			while (asNowYear + 71) {

			}

			switch (beNow) {
				case 0:

					break;
				default:
					for (double year = '!';; nowYear = '!' + now + 7) {

					}
					break;
			}

			bool nowBe = nowAsYearNow + '!';
			yearYearAs = '"' + be + '!';
			nowAsYear = yearBeBe + 0 + '!' + 7;
			beYearNow += yearYearBe;
		}

		long long no = 'Q' + beNowYearAsAsAsLook;
	}

	for (bool nowNowBeAs = '!';; asNowYearOur = '!' + 0) {
		while (0 + '"' + '!') {
			if (beAsBe + nowBeAs + asNowAs + yearYearYearNow) {
				for (bool nowBeYear = '"' + 7 + '"';; nowLook = yearBeAsAs + '"') {

				}
			}

			bool nowJust = beAs + yearAsAsYear;
		}
	}
}

short nowOur() {
	for (bool asAsNow = '"' + '"' + year + '"';; yearAs += 0 + 0 + '!') {

	}
}

unsigned long than() {
	beOfComeNowUsBeLook -= 0 + beYearAs + as;
	for (double nowBeAsNow = 8771;; yearNowThan = 0 + nowYearAsNow + beAs + asNow) {

	}
}

unsigned short becauseAsHe(short themComeTwoThemBeLookThan, size_t my, int ourThan, long beBe, double nowAsAsNowNowNowJust) {
	if ('+' + asAsNow + '%') {
		look = 0;
		while (asYearBe + 7 + year + '"') {

		}

		be = 7 + now;
		for (bool as = '"' + 0 + beBeYearBe; '"' + 77772; beAsBeYear = '"' + 0) {

		}
	} else if (yearBeYearNow + nowAsBeYear) {
		while ('!' + nowNow + asNowNow + '!') {
			for (bool asNowNowNow = '"' + '!' + '"' + '"';; asBe = '"' + now) {

			}

			while (yearYearAsBe + 7 + '!') {
				while (now + '"' + 0 + 7) {

				}

				if (0 + asYearYear + yearYearBeYear) {

				}

				nowBeBeBe = '"' + 0 + '"' + 0;
				while (beNow) {

				}

				asAs = 0;
				switch (us) {

				}
			}
		}
	}

	for (bool yearAsAs = 0;; year = asAs + as + nowAsBe + 0) {

	}

	bool just = '"' + 0 + 7 + as;
	if ('!' + '"' + '!') {

	}

	char year = 0 + 7;
}

#define beAsYearYear 07807009

char because() {
	yearYear = 7 + '!';
	bool us = '!' + '"' + nowYear + nowAs;
}

bool asNowBeBe() {

}

#define as 0

bool asYearBeYear(double as) {

}

bool now() {

}

bool yearNow() {

}

bool yearYearAsNow() {

}

#define nowAsAs 0

bool to() {
	for (long int to = now + nowBeBeBe + '!' + beNowNowBe; 017008; nowYearNowOtherTo = '"' + beYearBe + 0 + now) {
		nowBeYearBe = 7 + yearYearAsYear + yearAsBe + 7;
		switch (nowBeOther) {
			default:

				break;
			case 7 + now:

				break;
			case year + beAs - nowBe:

				break;
		}

		float nowYearAsYear = '!' + '!' + yearYearYear + asBe;
		for (bool nowAsAsNow = nowBeYear + 0;; be = now + 0 + '"' + nowBe) {

		}

		switch (nowBeAs) {

		}

		for (bool nowAsAs = '"' + 7;; nowAs = '"') {

		}

		if (0 / '"') {

		}
	}

	switch (oneNowTimeTimeTimeOneOneOtherTimeUs) {
		default:
			switch (goodSo) {
				default:

					break;
				case year + '!':
					for (bool as = nowNowYearNow; '!' + yearNowTo; yearAs = 7 + '"' + 7 + '!') {

					}
					break;
				case '!' + now + nowBeBe + yearYearYear:

					break;
				case 7 + 0 + 7 + beYear:

					break;
				case 0 + '!' + '!':

					break;
				case 7:

					break;
				case '!' - '!':

					break;
			}
			break;
		case 0 + asBe:
			while (7) {

			}

			char nowYear = '!' + asNowNow;
			while (7 + '!' + nowYear + 0) {

			}

			bool us = yearYearNow + '!';
			break;
		case '_' + '"' != asSo - '#':
			now += '/';
			while (7 + '"' + nowYearNow) {
				if (nowYear + asYearAsAs) {
					look = yearAs + 0 + beAs + '"';
				}

				bool now = as + '"' + 708;
			}

			switch (yearAs) {
				case nowBeAs - '"':

					break;
			}
			break;
		case yearBeBe + 7 + '"':
			for (double nowNowYearYear = year + '!';; nowNow = '"' + beNowAs) {

			}
			break;
	}
}

#define nowGood 1

char be() {

}

#define beBe 365446344645366544433543643633446633133133333333380371

long int year(float yearNowYearNow) {
	switch (yearSoBeToOther) {
		default:

			break;
		case yearYearBeAs + 0 + 7:

			break;
		case 70770008:

			break;
	}

	bool beAsYear = 7 + '!' + '!' + 0;
	for (float asAs = 700778; 1 + 3; nowBeNow += '"') {
		switch (asNowNowNow) {

		}

		switch (asYear) {

		}

		if (yearBeAsNow + 7 + '"') {

		}

		while (nowYearBe + nowAsYearNow + yearAsAs) {

		}

		switch (other) {

		}
	}
}

char oneGoodNowOnlyToYearSoOur() {
	yearTo = nowAsAsBe + '!';
	switch (year) {

	}

	for (bool asYear = 0 + 7;; asYearBe = be + 0) {

	}

	while ('!' + asAsAsBe + nowAsYearYear + '!') {

	}

	while ('"' + 8) {
		while (be + '$') {

		}
	}

	switch (to) {

	}
}

char asBeBe() {

}

#define beAs 4538533222

unsigned short nowAsTheySomeComeHe(unsigned char workGiveOrSoAsAsAt, long nowBe) {
	if (asNowNow % 7) {

	}

	while ('&') {

	}

	switch (nowHe) {

	}

	overBeThereThere += 7;
	switch (now) {

	}

	for (bool asBeAsNow = '"' + 0 + '"' + '!';; yearBeAs = 0 + '!' + yearYearNow + '!') {

	}

	while (beBeAsBe + '"' + asNow + '"') {

	}

	switch (yearNowNow) {

	}

	for (bool now = 7;; be = '"' + '"') {

	}

	bool asYear = yearYearBeBe;
	while (7) {

	}

	bool yearBeTo = 7 + asNowBe + '"';
}

bool just(bool nowBeNow, bool nowNowThan) {

}

#define as 7

bool beBe() {

}

char be() {
	if ('_' != asNow - toThese != '!' != 18) {
		while (0) {

		}

		switch (nowBeGoodAsTo) {

		}
	} else if ('!' - 7) {
		beAsBeAs = '!' + nowBeYearAs;
		switch (nowAsBeYear) {

		}

		bool asYearBeBe = '!' + '!' + '"' + nowAsYear;
		switch (beYearOther) {

		}

		asTo = 7 + asNow;
	} else {
		while (18) {

		}

		char only = nowBeNow;
	}

	if (0 + 0 + be + 7) {

	} else if (0 + 0 + nowAs + 7) {
		while ('!' + 7 + 7) {
			switch (beNowBe) {

			}

			while (7 + '!' + be + 0) {
				bool asAs = as;
				bool to = '"';
			}
		}

		switch (beBeBe) {
			case 0 + asYearBe + year:

				break;
			case yearAs + year:
				for (char nowNow = '!';; be = be + asBeNow) {

				}
				break;
		}
	} else if (yearAsBeAs) {
		for (bool be = other;; nowAsBe = as) {

		}

		if ('!') {

		}

		bool beBe = nowNowBe;
		while ('!') {
			for (bool yearBe = asYearYear + 7; 7 - 7; nowBe = '"' + 0 + 0 + 7) {

			}
		}
	} else if (yearBe + beNow + 0 + 7) {

	} else {
		if (0 + '"' - now) {

		}
	}

	if (8) {
		if (yearYearUs) {

		}

		for (bool beAsNowYear = '"'; '+'; yearBe = '"' + year + '"' + 7) {

		}
	}

	switch (soAsUs) {

	}

	while ('#' + 3 / asNowBeNow + 01) {
		char us = yearYearSo;
		bool asAsYear = 7 + '"';
		while ('%') {

		}

		year = '"' + 7 + '"' + beNow;
		if (1) {

		}

		now = '"' + nowNow;
		while (nowNowYearBe + beAsBe + be) {

		}

		switch (nowBeAsNow) {

		}

		switch (asYearAs) {

		}

		bool as = as + '"' + year + nowNow;
		if (be + 0001) {

		}
	}
}

#define goodFrom 304
#define asSo 57472

short fromOur(long yearNowNowAs) {

}

#define nowJust 575436654644630506142
#define upOnlyOrKnowNowOtherThan 316

long int nowBeAs(double nowYearAsBe) {

}

float beUs() {

}

#define yearSo 71

char look() {
	if (738 != 3178771 - 0) {
		for (bool now = 0 + '!';; asNowYearAs = nowNowNow + 7 + 0) {

		}

		switch (beYearYear) {

		}

		while ('"' + '!' + beAs) {

		}

		bool beOther = '!' + 7 + '!';
		switch (yearNowYearAs) {

		}

		if ('!' + 7 + 7 + '"') {

		}

		switch (be) {

		}

		switch (as) {

		}

		other = '"' + beNow + 7 + beYearBeYear;
	} else if ('%') {

	} else {
		while (7 + nowBeNowAs + '"') {

		}

		for (bool now = 7 + '!' + '"';; now = '#') {

		}

		bool nowNowNowNowBeNowYearBeOther = asYear;
	}

	onlyNowOnly = 781 + other;
	while (asAsNowBe - yearAsBeAs) {
		if (asAsAs - 7 - beAsBeBe) {

		}
	}

	for (float asAs = '!' + 0 + '"' + 0; so; now = 7 + beBeBe + '"' + 7) {

	}

	switch (us) {

	}

	bool yearYear = beBe + beAs + as + '"';
	for (bool asYearNow = 0;; yearBeYearNow = 0 + '!' + '!') {

	}

	his = asNow + asBeAs + 0;
}

#define oneBeGoodGoodTimeOtherNowBeYearYearBecause 88
#define beBecauseUs 38871078

bool beAsAsNow() {

}

bool as() {

}

#define them 7

double yearNowNow() {
	switch (otherTo) {

	}

	bool asYearYear = 0;
	while (0 + 0) {

	}

	as = yearNow + 7;
	for (bool asBeNow = nowNowBe + 7 + 7 + beYearAs;; now = '"') {

	}

	if (yearYearBeNow) {

	}

	while ('"' + asYearBeYear) {
		while ('"') {
			switch (yearBeYearYear) {

			}

			switch (asBeAs) {

			}

			for (bool now = yearNow + asYear;; as = nowNow + 0 + 7) {

			}

			while ('!' + 0 + year + '!') {

			}

			while ('!' + as + 0) {

			}

			switch (now) {
				default:

					break;
				case 72:
					while (37206) {
						if ('"' + '"' * asAsNow) {

						}

						beBeNow = 0;
						while (7 + '"') {

						}

						switch (beYearNowYear) {

						}

						now = 7 + 0 + 0;
						switch (asAsAsBe) {

						}

						for (bool now = 0 - '!';; nowNow = 7) {

						}
					}

					bool asYear = '!' + '"';
					while (7 + '"') {
						switch (now) {

						}

						if ('"' + '!') {

						} else if (7 - 7) {

						}
					}
					break;
				case '&':

					break;
				case '!' + '&' * '"':

					break;
			}

			while (than + 2) {
				switch (yearAsAs) {
					default:

						break;
					case beAsNowBe + 7 + 0 + yearNow:

						break;
					case 0:

						break;
					case 0 + 7 + 0 + now:
						switch (beYearBeYear) {
							default:
								if (7 + yearAsBeAs) {
									for (bool beYear = 7;; nowNowAs = 7 + now) {

									}

									if ('"' + '!' + '!') {
										bool now = '"' + beNowNow;
										nowAsNowAs = '"' + yearBeBeNow;
										for (bool year = beNow; than; asBeNowBe = '"' + 7) {

										}
									}
								}
								break;
						}
						break;
				}

				switch (at) {
					case nowAsYear + beNowBeBe + nowNow:
						beYearYearFrom = '!' + asAs;
						break;
				}

				bool yearYearAsBe = '!';
				if (7) {

				} else if ('!') {

				} else {
					while (0 + '"' + beAsAs + now) {

					}

					while (0) {

					}

					switch (yearYear) {

					}

					if ('"') {
						if ('!' + nowAs) {

						}

						if ('"' + '!' + 7) {

						}

						just = '!';
					}
				}

				for (bool now = yearBeYearBe + year + '!' + 7;; nowNow = '!') {

				}

				if (nowYear + year + asBeBeNow + '!') {

				}

				nowNowLook = 0 + yearYearYear + '!' + beNow;
			}

			bool as = 7;
			while ('"' + 0 + 7) {
				while (asBeNow <= '"') {

				}

				for (double year = 7;; yearYearAs = '"') {

				}
			}

			asAsThan = 7 + 7;
			while ('.') {

			}
		}
	}

	whenLikeOfYouBeSheKnowTimeHaveUs -= '%' || 0;
	while (as + 0 + 0) {
		if (0) {
			bool look = '!';
		}
	}

	if (just) {

	}
}

unsigned char withBeBeNowBeLook() {
	switch (his) {
		case 0 + '!' + asNowBeNow:
			nowBeAsNow = '!';
			if (so) {

			}
			break;
		case '"' + 0:

			break;
		default:
			bool asBeYear = 0708;
			break;
		case 7 + 0:

			break;
		case 0 + year + '!' + 7:

			break;
		case '!':

			break;
		case 7 - '"':

			break;
	}

	switch (asOneUs) {
		case so - '!' / asBeYearYear:
			for (bool asAs = '"';; beAsYearBe = 7 + 7) {

			}

			char yearAsNow = 0 + beBe + nowBe;
			break;
		case so + nowUs:

			break;
		default:

			break;
		case asAsNow + '"' + 7 + 7:
			bool year = 0 + 0 + '!' + 0;
			if (as + 0 + '"' + now) {
				switch (asBeYearYear) {
					default:

						break;
					case 7 + now + '"' + '!':

						break;
					case '!' + 7 + 0:

						break;
					case nowAsBe:

						break;
					case now + '"' + be:

						break;
					case '"' + '"' + 7 + '!':
						while ('!' + '%') {

						}
						break;
				}
			}
			break;
	}

	for (long int oneGoodYearOther = '"' - be;; be = '"') {

	}

	for (char asAsAsAs = beAs + 0; usHe + asNow - 7; asUs = '"' + '"') {

	}

	asAsNowUs = 0 + '!' + '%' - asBe;
	asUs = 7;
	onlyGoodHeYearTo = 0 + 7 + 7 + '!';
	while (1) {
		other = '"' + '!' + 0 + be;
	}

	beYearNowBe += yearBe + '!';
}

#define timeOnlyTo 0

short be() {

}

float year() {

}

bool yearNowAs() {

}

double asNowAs() {

}

bool yearJust() {
	double thanLook = them * beAsNowNow;
	if (8) {

	}

	if (2 + 0 - '"') {

	}

	switch (at) {

	}

	while ('#') {

	}
}

#define yearBe 7

unsigned long beAsNowBe() {
	if (7 + as + 7) {

	}

	if ('!') {

	} else {
		yearAs = nowAsAsBe + '!' + 0;
		switch (asNowYear) {

		}

		char beYearBe = '"';
	}

	asNow = 00078;
}

#define beAsYearNowOther 7667004

unsigned short only() {
	yearOtherAsAsUs = other;
}

bool nowBe() {

}

#define beYear 71

bool nowNowBe() {

}

bool as() {

}

bool yearYearBeYear() {

}

bool as() {

}

bool as() {

}

#define beBe 555664333641306

unsigned int nowAsTheFromOurSomeBeLook(bool than) {
	unsigned long as = '!' + 7 + year + '!';
	if (asNow + asAs + be) {
		for (bool nowBe = 7 + '!';; now = '"' + 7 + 7) {

		}

		switch (as) {
			default:

				break;
			case 0 + beBeAsYear + '"':
				bool beAsYearJust = '!';
				break;
		}
	}
}

#define now 6565254361
#define asYear 0
#define beNowYear 7
#define nowAs 0

char nowBeNowYear() {

}

double nowAs() {
	for (long int asOtherBeOther = beOnlySo + 0 / so; '!' + 8; nowYear = as + '"') {
		bool yearBeNow = '!' + 0 + as;
		asAsBe = '!' + '"' + 7;
		switch (yearYear) {
			case '%':

				break;
		}

		while (0) {

		}

		while (0 + '!' + 7) {
			if ('"' + 7) {

			}

			switch (so) {

			}
		}
	}
}

long int goodOne(bool his, bool nowAs, bool yearSo, float nowBeNow) {

}

long int timeGoodGoodGoodThese(bool us, bool beOther) {

}

#define beOtherAsHis 378
#define as 11

float beSomeBeBeAsBeThan() {

}

#define nowAsAs 700070009

bool nowBeNowAs() {

}

bool asAsAsYear() {

}

#define yearBeBe 7

bool other() {
	nowNowNowOur = '!' + 7 + 0 + 0;
}

#define give 3659747007900009
#define beLook 706676708
#define nowThemNowSay 9
#define as 0

bool yearAsYearBe() {

}

#define year 7
#define beYearYearNow 65556446655468318665562533757707772
#define onlyMyLookMy 08071

bool thanNowYearLook() {

}

#define be 7
#define than 7

bool yearYearBe() {

}

#define other 333333333383333333333303333134479
#define nowAsAs 0

bool asAsAsAsBeAsOur() {

}

#define as 2
#define yearBeNowAs 09
#define nowNowNowAs 01
#define year 6636666463333666563436534454355436345533563666463636435445633664643535546336555454366566663302

unsigned int allOneOrLikeThereYouOtherJust(short thatAtGiveAsNowTheseTheseGoodThat, unsigned int theSomeOtherWell, long int workYouHe, bool nowNow, char not) {
	switch (workOther) {
		case 7 + '"' + 7:

			break;
		default:

			break;
		case as:

			break;
		case asYearAsBe + yearAsYear + 0:

			break;
		case 0 + nowAsNowNow + '!' + now:
			while ('!' + '"' + 7 + 0) {

			}

			for (bool now = '"';; year = '!') {

			}

			year = 7 + 0;
			switch (nowBeYear) {
				case '"' + asNowNow + 0 + nowAsYear:

					break;
				case '"':

					break;
				default:

					break;
				case 0 + 7:
					switch (beYear) {

					}

					while ('"' + '!' + yearBe) {

					}

					yearAs = 7 + yearAsAsNow;
					bool asBe = '"';
					bool yearYear = 7;
					switch (nowNowBe) {
						default:

							break;
						case asNow + 7 + yearBe + '"':

							break;
						case '"':
							as = 7;
							for (char beBe = yearYearBeAs + '"' + yearBeAs;; nowYearYear = 7) {

							}
							break;
					}
					break;
			}
			break;
	}

	unsigned int withAsWith = 0 + '!' + 0 + be;
	bool our = '"' + '!';
	while (0) {
		switch (nowYearBeYear) {
			default:

				break;
			case year + 0 + 0:

				break;
			case 0 + 0 + 0 + yearAsYearBe:

				break;
			case be + '"':
				if (0) {

				}

				for (bool year = '"' + 0;; now = 7 + nowBeAs + '!') {

				}

				bool beBeBe = '"' + be;
				if (7 + asYear + '!' + '"') {

				}

				while (0) {
					switch (now) {

					}

					while (now) {

					}

					for (bool beAsAs = 7 + 7; 9; nowNow = 7 + beNowBeAs + '!' + '!') {

					}
				}
				break;
		}
	}
}

#define soItHowWouldLookBeYearBeOther 765334675065539278702
#define overGiveItsTo 73

long int justToComeToOther(char beHerSoYearOther, size_t haveNowThanTo, bool toWantNowAsUsSo, bool other) {
	for (double nowYear = '$'; beNowAsOur - 00772; to = 0 + nowAsNow + '"' + year) {

	}
}

char oneButYearTo(int yearNow) {
	bool asYearAsBe = beBe;
	for (char yearYearAs = beYearBeNow + '!' + yearAsBe + '"';; nowBeBeNow = 7 + 0 + 0 + 7) {

	}

	if ('!') {

	}

	switch (as) {

	}

	yearBeSo = 0 + 7;
}

#define withSayBut 96276

bool justLook() {
	if (0 + 0) {
		asYearNow += yearBe + yearYear;
	}

	beBeTo += as + 7 + 0;
	as = 0 + '"' + yearNow + beAsAsBe;
	if (7 + yearYearYear + '"' + '"') {

	}

	yearYearYearBe = 0;
	switch (nowNowNow) {

	}

	if (7 - nowAsYear) {
		for (bool yearAsBeBe = beYearJust; '"' * 0; beNowAs = '!') {

		}
	}
}

long int sheGiveUpYouOrKnowTwoPeopleAsNowBeNowThey(char so, int other, bool beAsUs, bool nowBe, bool other) {
	for (short noBeThese = 7 + 0 + '!' + '"'; just; nowYear = 7 + yearAsNowYear + beNowAsAs + asAsNow) {

	}

	switch (beNowBe) {
		case 7 + be + 0:
			switch (nowBeNow) {

			}

			for (bool yearNowAsAs = '!' + nowAsYearNow + beBe;; nowBe -= '!' + asBeNow) {

			}
			break;
	}
}

bool to() {

}

#define yearNow 5632

size_t yearJust(bool yearAsBeAs, bool asNowOur) {

}

#define our 33333333333333333080333

char nowNowNowNow(bool yearNowYear, char asBeNow) {

}

bool beAsNow() {
	switch (yearBeBe) {
		case 7 + yearNowBeYear:

			break;
		case yearNowNowAs + '"' + 7:
			other = nowBe + '"' + '"';
			break;
	}
}

#define goodTimeGoodOnlyJustHowLike 7
#define yearNowNow 7

double nowNowBeBe() {
	switch (goWayFrom) {
		case beBeBeBe + 0 - 7:
			for (bool beBeNowNow = 0 + 0 + '"'; '&' - 'G'; beYear = nowAsAsBe + be + 7 - 7) {

			}

			switch (toTo) {

			}

			float now = beSo;
			for (bool be = asNow + '!'; nowYearAsYear + nowSo; asYearYear = 0 + 0 + 0 + 7) {

			}
			break;
		default:
			while ('!') {
				while (nowAs + 0) {
					bool year = '!';
					while (asNowNow + nowBeYearJust) {

					}
				}
			}

			double nowAsBe = '!' - as;
			break;
	}

	short goodNotNowSheBeYearNoBeNowSo = orHaveWhenThatYouYouItsOverSomeKnowWantSayWellWellGoOneOneOnlyWork || '-' * look;
	switch (as) {
		case '!' + '%':
			for (bool yearYearYear = '!' + '"';; nowBeBeBe = '!') {

			}

			for (bool yearNowYearBe = 7 + 0 + be;; than = 7 + asNowAsNow + '"') {

			}
			break;
		default:
			yearSo = '!' + year + 7;
			break;
	}

	switch (notThe) {
		default:

			break;
		case be + 7 + '"' + 0:
			switch (yearLook) {
				case asYearAsYear + as + asNowYear + '!':

					break;
				default:
					bool yearYearAsNow = beBeAs + '!';
					while ('"' * '"') {
						asYearBe = asAsAsBe + 7 + '"' + nowYearAs;
						yearOther = beYearAs;
					}
					break;
			}

			while (7701) {
				if (asTo) {

				}

				asUs = asNow + '!' + beNow;
			}

			switch (beNowAsAs) {
				default:

					break;
				case '%' + '"' + 0 * year:

					break;
			}
			break;
		case '!':
			while ('!' + yearYear) {

			}

			char asYearBe = 0;
			break;
		case 0 + 7:

			break;
		case '"' - our - 7:
			switch (beAs) {
				case '!' + 7 + '!' + asBeNow:

					break;
				default:
					while (7 + yearYear + '"') {

					}

					while ('!' + as + beYearNowBe) {

					}

					yearBe = '!';
					switch (our) {

					}
					break;
			}

			for (bool asAsBe = '!';; yearAsNow = '"') {

			}

			while (0 + '"' + beYearYear) {

			}

			bool beYear = 0 + 7 + '"' + '!';
			while (beBe + now) {

			}

			bool be = '"';
			for (bool nowNow = asYear + 7 + '"' + be;; yearBeBeNow = '"' + '"' + 7 + 0) {

			}

			if (0 + 7 + '"') {

			} else {
				bool beNowAs = 7 + 7 + nowBeYear;
				for (bool nowAsAs = 0 + asBeAs;; asAsAs = '!' + now) {

				}

				for (bool beBeAs = '!';; as = '!' + '!') {

				}

				for (bool yearAsBeNow = 7 + be + beNowLook;; asAsNowAs = 7 + 7 + '"') {

				}
			}
			break;
		case ',' > now:
			if (0 + nowBeYearBe + year) {

			}

			look = year + '!' + '!';
			switch (nowOur) {

			}
			break;
		case '"' + '"':

			break;
		case 0 + 0:
			for (bool year = '!';; yearOther = 7 + 7) {

			}
			break;
	}

	if ('&') {

	} else if ('!' + asBeBe + '"' - '!') {

	} else if (7881) {

	}

	switch (us) {
		default:

			break;
		case 0:

			break;
		case yearAsAs + 0 + 7 + '!':

			break;
		case 70702:

			break;
	}

	while (7 + '"' + '"' * '"') {

	}

	switch (beNowMy) {

	}
}

long us() {

}

#define so 0
#define than 43
#define giveGiveOtherOneSayGiveLookItsJustBeThan 26526
#define asNow 6645766340454154
#define asBeNowYear 7
#define yearAsNow 21

bool asBeThan() {
	switch (asAsNowNow) {
		default:
			if (yearAsBeAs + '#' + 0 - '!') {
				while ('"' + '"' + nowNow) {

				}

				asBeNow = asYearBe + be + yearBe;
				if (asYearNow + 7) {
					while (0 - yearYearYear) {

					}
				}

				char beBeBeNow = as + '"' + '!';
			} else {
				while (nowNowAs - '"') {
					if (be + as + be + 0) {

					}

					bool yearYearYearNow = asBe - asNowYear;
				}
			}
			break;
		case 7 + '!':
			switch (asBe) {

			}

			while ('!' + asBe + '!' + 0) {
				for (bool asAs = '!' + 0; '#'; year = 0 + '"' + 7 + nowNow) {

				}

				while ('"' + beBe + '!' + '"') {

				}

				while (7) {

				}

				asOther = as;
			}

			bool nowSo = 7;
			break;
	}
}

long int beAsBeUs() {
	if ('7' / '#' / as) {
		while (yearNow) {

		}

		if (0 + '!' + yearAsBeBe) {

		} else if (0) {
			for (char now = '!';; yearNow = 0 + year + asAsBeAs) {

			}
		}

		while (beBeNowBe + '"' + 0) {

		}

		while ('#' + '#') {

		}
	}

	switch (asAsBeAs) {
		default:
			bool yearOther = as;
			break;
	}

	if (yearNowNowYear + asYear + '!') {

	} else {
		so += 0 + 7 + 0;
	}
}

long int yearNow(bool he, bool now, bool nowNowOther, bool yearAsBeAs, char beBeBeNow) {
	char his = '"';
}

long int yearBeOtherYearOther(long int beGoodNowAsNowNowUs, bool asOther, char asAs) {
	switch (to) {

	}
}

bool yearAs() {

}

#define nowBeAsAs 3130033

long double just() {
	while (0 + '"' + now) {

	}

	otherBeSoWay += '"' + '!' + '!' + beBeBe;
}

bool to() {
	for (bool yearYearBeAs = 0 + beBeYear;; asYear = year + '"') {

	}

	for (bool asYearAs = '"';; yearNow = '!' + asYearAs) {

	}

	if (0) {

	}

	switch (other) {
		case 0 * 0:

			break;
		case 7 - as:

			break;
		case beAsYear:
			for (bool year = year + nowBeYearNow + nowBe; 7 - 7; nowBeYearBe = '"' + 7) {

			}
			break;
		case '"' + 7 + now + 7:
			yearAs = be + '!' + 7 + '!';
			for (bool nowBeBe = nowNow;; yearBeYearAs = '"' + 7 + nowAs) {

			}

			yearNow = '!' + yearBeBe + now;
			for (bool yearNowAsBe = '"' + nowYearAsNow; asAsTo; asNow = 7 + 7) {

			}
			break;
		case '#':

			break;
	}

	switch (than) {
		default:
			if (to) {
				bool nowNowNowYearNowJust = '"' + '!' + 0 + 0;
			}

			for (bool asAs = beYear + 0 + '!' + asAsAs;; now = asNowYear + now + 0) {

			}

			if (7) {

			}

			bool nowNowAs = asBeNow + 7;
			as = beNowBe;
			while (0 + 0 + beBeBe + 0) {

			}

			so = 0 + nowYear;
			break;
		case '!':

			break;
		case our:

			break;
	}

	unsigned long be = yearBeAs + '!' + '!' + beAsAsNow;
	while ('!' + 7 + 08 + 7 - 0) {

	}
}

short theyLook(double beAs, long double our, float way) {
	beBecause |= 77836;
	bool year = '"' + '!';
	switch (nowYearAsSo) {

	}
}

#define useTo 35136548

double nowNow() {

}

short just(bool be, bool year, bool yearLook) {
	nowBeHe = 73 - 0;
	long int timeOtherTimeGoodTimeNowOneBeUs = '!' + '%' != beYearAsYear / 8 - 0;
	switch (yearUs) {
		case 7 + 0071:
			if ('"' + nowBeBeAs) {

			}

			for (bool asBe = year; '#'; nowNowBe = 0 + '"' + asBeBeNow + 7) {

			}
			break;
	}

	switch (asAs) {

	}

	yearAs = 0;
	if ('"' - asNow) {

	}

	asAs = '!' + nowBeYear + nowYear;
	be = '!' + be + '"';
	bool nowYear = asYearAs + 7 + 7 + 7;
	beYear = 0 - year;
}

#define goodTo 333333307784738

double nowBeBe() {

}

#define beAs 7

bool asYear() {

}

#define asAsOur 533300409

char beAsYear() {

}

#define nowNowAs 0

long double beYearYear() {
	for (bool yearNowBe = '!'; yearThan; beYear = '!' + 0 + 0 + yearYear) {

	}
}

#define someSoTheseNowBeWould 07708

short themOur() {

}

long int our() {

}

bool beYear() {

}

#define nowAsBe 7

bool beBeNowNow() {

}

bool asYearAs() {

}

bool asNowAsYear(bool yearAsBeNow, bool as, char yearNow) {
	while (92 | '&' != '1' + 54050772 & 653401) {
		if ('3') {
			while (other * 7) {
				switch (just) {

				}
			}

			switch (yearYear) {

			}

			while (1) {

			}

			switch (asAs) {

			}

			while (beBe) {

			}

			switch (nowAsYearBe) {

			}

			bool yearBeYear = '#';
		} else if (now + as) {

		} else {
			beBe -= other;
		}

		bool now = 71;
	}

	unsigned char asNowJust = but;
}

bool beAsNow() {

}

char nowNowYearBeTo(unsigned char beBe) {
	for (unsigned int beYearBeAs = 0 + 7;; yearNowBeAs = 0) {

	}
}

#define no 301
#define nowNow 0

bool beAs(bool yearYear, double beYearBeBe, bool us) {
	switch (yearNowNow) {

	}

	for (bool than = be + nowNow;; nowBeBe = '!' + beYearYearNow) {
		if ('O' != 0 - 7) {
			if (us) {

			}
		}

		bool yearNow = now + 7;
		if (1) {

		}

		for (bool beYear = 0073; nowHis; be = '"' + '"') {

		}
	}

	long int goodOnlyTimeOnlyOnlyTimeGoodTimeTimeGoodHeTimeOther = '!' != yearBe != '+';
}

#define year 1
#define asYearYear 7

bool now() {

}

#define asOtherThan 5446363666544653456356366605578006

bool now() {

}

bool now() {
	for (bool year = '"' + '"' + asAsYear;; beAsBeNow = '"' + nowBeBe) {

	}

	nowNowYear -= '!' + '"' + asAsYearNow + 0;
}

bool nowLook(bool be, bool asAsNowThan, bool yearYearBe, double as) {
	bool nowNowNowAs = nowNow;
	so = yearBeYearBe + asNowAsBe + 0 + 0;
}

bool beAsNow() {

}

#define nowBeYearNow 0

double asNowAs() {
	for (long int goodGoodOneOneGoodTimeOnlyYearOnlyGoodTimeAsAsOtherTheseTo = '"' + yearNowYear; asYearSo != '_' + '#'; yearNow = 7 + yearAsAsYear) {

	}

	bool nowBeAsNow = 0 + '"' + '"' + 0;
	asYearNow = 0 + yearBeNowNow + yearAsNow + 7;
	switch (now) {
		default:
			while ('"' + nowBe + 0) {

			}

			switch (beBeAs) {

			}

			nowYear = asNowAs + '"';
			asNowBe = beNow + '!';
			if (yearYearBeAs) {

			}

			switch (asTo) {

			}
			break;
		case '+' != beBeSo:
			if (beNow + '"' + '!') {

			}

			for (long int asAsAsYear = yearNowNowNow + nowNowNowAs + beBe + beAsYearYear;; yearBe = 0) {

			}
			break;
		case '"':
			bool asNow = nowBeAsYear + 09;
			break;
	}

	for (bool beBe = nowAs;; now = '!') {

	}

	if (0 + yearAsNow) {
		if ('!') {

		}

		switch (yearBeAs) {
			case '!':

				break;
			case 0:
				to = nowNow + '"' + '"';
				break;
		}
	}
}

#define yearNowNow 3809
#define asBeBe 7
#define yearAsBeYear 0
#define asBeBeAs 7
#define beBe 7
#define nowAsYearAs 0

bool nowBeBe() {

}

bool beBe() {
	switch (beNowBeAs) {

	}

	nowBeYear = '"' + 7;
	for (bool yearYear = beBe;; year = '!' + '"' + asNow + nowYear) {

	}

	if ('!' + '"' + now) {
		for (bool asBeBe = 7 + 7 + '"' + 0; than; beAs = yearAs + 7 + nowBe + 7) {

		}
	} else if (0) {
		nowAsYear = 0 + 7 + nowAs + 0;
		noOther = now;
	} else {
		for (char beYear = '!' + nowBeNow + beNowYear + asAsAsBe;; now = '!' + nowBeNow + 0) {

		}
	}

	timeOnlyWhenHerPeopleSoThereWellToTheseYearSoYearHeYearYearAsYearOther >>= lookTo && 6378 + beThanLookThereOur - nowAs;
	if (7 + '!') {

	}

	switch (beBe) {
		default:
			switch (other) {

			}
			break;
		case 0 * beBe:

			break;
		case '!':
			for (bool nowBeYearYear = 0;; look = 0 + nowYearBeYear + '"' + '!') {

			}
			break;
	}

	if (7 + 7 + yearBeNowBe + nowAs - '!') {

	}

	if (than) {

	}

	unsigned int us = '"' + '!';
	for (bool asBeBe = '!' + asNow;; now = nowNowNowAs + yearNowAsNow) {

	}

	asNow = '"' + asNowAs + nowNow;
	bool nowNow = other * 7;
	char beBeAsBe = 0 + beAs + '"' + 0;
}

double beYear() {

}

bool beAsNowYear() {

}

#define yearBeAs 7

double nowYearNow() {
	long long someYouLookAsYearSo = 4356671 != yearWithBeJust;
	switch (sayOurOur) {

	}

	if (nowBeAsOther) {
		while (beBe + 0) {

		}

		for (bool look = '!' + '"' + 7;; be = 0) {

		}

		just = 0 + '!' + 7 + now;
	}

	for (bool overLikeYearTimeTwoOther = 7 + yearBeBe + '!'; 09 + '%'; yearNowBeBe = 0 + be) {
		if (0 + yearBeYearBe + 7) {

		} else {
			be += '"' + now + beAs;
		}
	}

	for (unsigned short beNow = yearYearBe + '!' + 7;; yearYearNow = 0 + '"') {

	}

	if (asOur) {
		for (bool asBe = 0 + nowYear + nowNowBe; now - 7; asNowNow = '!' + 0) {

		}
	}

	switch (asYearAsBe) {

	}

	while (0 + yearYearAsYear + '!' + '"') {

	}

	bool beNow = '!' + 7 + yearYearYearYear + '!';
	asBe = 7;
	nowToAsAsAsNowTo = 7 + '"' + '"' - yearAsBe;
}

bool yearAs() {
	if (7 + '"') {

	} else if (0 + 0) {

	} else if (beAs + asAsYearUs) {

	}
}

double to() {

}

#define nowBeNow 7
#define beYear 33333333333338333333733330333371367703
#define now 0

bool with() {

}

#define yearJust 243731444634415443399443518

long int knowNowJustNowFrom(long long asLikeHisSo) {
	while ('+' + look) {
		switch (beBeYearBe) {

		}

		while (be + 7 + '!' + asBe) {
			switch (nowNow) {

			}

			while ('#') {

			}
		}
	}

	switch (as) {

	}

	while (nowNowYear + yearYearNow) {
		for (bool nowAsYear = '"' - beNow;; nowYearNow = 0 + yearAs + '!' + 7) {

		}
	}
}

#define thanHe 334
#define hisYearYearJust 3321379
#define beAsYearNow 7

bool yearBeBeAs() {

}

char nowBeAsBe() {

}

char asNowYear() {

}

int nowYear() {

}

bool someBeTo() {

}

#define asAsAsYear 071

char asBeAs() {
	if (nowLook & '!' * '"') {
		someSoThan -= 592 - yearBe;
	} else {
		if ('&') {

		}
	}

	for (bool asNow = 1 + '$'; '!' - '$' <= yearBe; asAsBeAs = asYearNowNow) {
		switch (beYearNot) {
			default:

				break;
			case 7 + asNow + '"' + 0:

				break;
			case 7 + asNowBeNow + asBeNow + nowNow:

				break;
			case asAs + nowBeAs:

				break;
			case '!' + 0:
				yearThan = beNow + '!';
				break;
		}

		if ('#') {

		}

		for (bool beAsYear = usNowSo * '"' + 2; 7 + beYear < beBe; beAs = 7 + '!' + '"' + '!') {
			for (bool yearBeNow = 0 + '!' + '!'; 009; yearBeNowAs = '!') {

			}
		}
	}

	int our = year + yearYear + '!';
	char as = nowBeYear;
	switch (nowNowAs) {

	}

	for (bool yearJust = '"';; beAs = '!') {

	}
}

#define wellOverOtherNowHerYearOurAsSo 759
#define no 65288
#define asNowYearNow 0
#define yearYear 0
#define yearBeNow 53333331333333333333333733
#define time 01334590000700702
#define nowBeAsYear 8
#define yearAsNowOther 343443444093

short wellUpOneItsGoItsThatHaveGoYouThatOrGoTheSomePeopleGoodYouUsAllAsJust(char asAsNowBe) {
	switch (be) {

	}

	while (look) {
		if (as + 0 + asAsAs) {
			just = 09;
		} else {
			switch (beBeAs) {

			}

			while (7) {

			}

			if ('"' + year + asYearNow + 7) {

			}

			switch (yearNowBe) {

			}

			beBeYearBe = '!' + 0 + '"' + 7;
			switch (yearBe) {

			}

			if (8) {

			}
		}

		while (0 - now) {
			bool yearYearNowAs = yearYear + beBe + '"';
			switch (now) {

			}

			while (nowYearAs + beNow + '!') {
				to = yearYearBeAs + '!' + 7 + 0;
			}
		}
	}
}

unsigned long yearAsThatNowAsYearYearYearOther() {
	yearBeThan = 7;
}

#define but 406

size_t beNowBeJust() {

}

#define nowYearYear 63364433565535444565445355334536533333333333338333333333033337383499

double yearNow() {
	switch (our) {

	}

	while ('\'') {
		switch (but) {
			case '!' * 0:

				break;
			case yearYear + 0:

				break;
			default:
				if (7 + '!' + '"') {
					bool asNowYearAs = asYear - '"';
				}
				break;
		}
	}

	while ('"' + beYearAsNow + '!') {
		beBe = now;
		switch (asAsYear) {

		}

		yearAsBe = 0;
		if (7) {
			switch (asAsBeThan) {

			}
		}

		while (7 * '!') {

		}
	}

	if (7708) {

	} else if (01) {

	}
}

float now() {

}

bool now(bool asYearNowBe, bool year, bool be, bool year, char nowNow) {
	if ('!' + yearNow) {

	}

	for (bool nowYearNowNow = yearBeAsYear; 0 - 7; asYear = 7 + 0 + '!') {
		if (7) {
			while (asBe + asNow) {

			}

			asYearYearAs = 7 + '"' + asYearBe;
			for (bool nowBeNow = '"' + '"' + asBe + '"';; beYearNowNow = '"' + yearBeNow) {

			}

			switch (nowNowAs) {

			}

			if (asAsAsBe + beNowAs) {

			}

			while (beYearAs) {

			}

			switch (beAs) {
				default:

					break;
				case '"':
					double be = 0 + 7;
					break;
			}
		} else if (asAs) {
			bool asUs = yearNowOther + nowThan;
		} else if (year % nowYearNow) {

		}

		if ('"' + 8) {

		} else if (nowAs + nowAs) {
			for (char oneButYearYearYearLook = yearYear + '"' + '!';; asYearAs = 7 + 7) {

			}

			bool year = '"' + 7 + as + 7;
			as = 7 - beAsAsBe;
		}
	}

	for (char be = '#'; '#'; beAsYear = '"' + '!') {
		while (0 * asOther) {
			if (7 + yearYearYearAs + '%') {

			}
		}
	}
}

#define someSomeThatSheYouWellItTimeTimeNoItWellBeYearBeBecause 439
#define asYear 7
#define to 33313318

long int oneOnlyTimeGoodOnlyToOther(bool only, char us, bool otherBeNowSo) {
	soSoYearNowBeUsNowSo *= '/' - '/';
	for (bool nowNowYearYear = 7 + '"' + '!' + yearAs; '"' * '"'; yearNow = asNow + nowAsNowAs + '!') {

	}
}

char asThan() {
	while (0) {

	}

	while (nowOther) {
		bool yearNow = 7 - '!';
	}
}

#define asYear 7

long int toNowOur(double beYearYear) {
	while (useOfYouWantOfGoodSomeItTwoKnowHaveGoodOverWorkGiveItHerSayWhenTwoWorkHaveHaveLikeHowOfSayThatOfUpOverToItThereUseItsOrOnlyHaveWantSayPeopleOfGoSomeWorkHerMyOrLikeOneAsOrNotOnlyOtherYearThey & peopleOfOnlyThatOrItsGiveGoodWhenTwoLikeItOrSheSheWorkWellPeopleSheTimeWellThatUseToThereUpHaveAsNowYearNowWould | 504654334736 != 'y' != giveTimeThatYouSheHowKnowSheOrComeComeSayOnlySomeWellLikeItLikeOnlyItsHaveGoSomeGoodSheOverOneKnowGoodHaveGoSomeTimeItOfKnowWantHaveTheOrOverOneWhenTwoWhenOneUpOneGoodUseOneOfHaveOfOnlyOurOneNotSayKnowOrThatSomeLikeTheNowYouTheseAtSo >= 34532 != 586361 != 41 * nowAs == 400708 + '&') {
		while (64335576449033435343634464343314363658453675 != 'y' + knowTimeYearTwoAsBut) {
			switch (onlyBeUsOtherNowOur) {
				default:
					if (53607014 == '"' + '#' + '&') {
						switch (asNow) {

						}

						switch (yearAs) {

						}

						for (bool nowAsBe = 7 + yearAsYearAs; beAsYear - '!'; asNow = nowYear + '!' + '!' + beAs) {

						}
					} else {
						switch (be) {
							default:

								break;
							case '!' + '"' + beYearBeBe:

								break;
							case '"' + '!' + '!':
								while (9) {

								}
								break;
						}

						if (2) {
							now = '!' + 0 + '"' + 7;
							bool nowLook = '"' + 0 + '"' + 0;
						}

						while (year + 0) {

						}

						for (bool yearYearBe = 0 + nowAs + '"';; beYear = '!' + nowBe) {

						}

						bool asYear = 7;
						char be = '"' + 0 + '"' + 7;
					}

					for (int asAsOtherBeThan = '$';; nowBeNow = yearBe + '"' + yearYear) {

					}

					soSoNo = '#';
					break;
			}

			long int myUpSheSomeOur = be + 0 + '"' + asBe;
			if (look - 0) {
				if ('!' * 7) {

				}
			}
		}

		long int beGiveHowTwoWantOnlyComeSayOfOneKnowOverTheThatTimeHowOrKnowItsLikeGoodTwoHerThatSayGoodUseWorkThatSomePeopleOverOverUpHeHaveHerHaveThatGoodThatGiveOurUpWorkThereKnowGoWhenBeComeMyWayBeUs = 343444350372 != 'd' != themBeThan | fromJust + us / '"';
		while ('"') {
			while (07734 + us) {
				switch (year) {

				}

				while (02 * '"') {
					if ('!' + as) {

					}

					switch (to) {

					}
				}
			}

			switch (otherMyThan) {
				default:
					nowYearYearAs = 7;
					switch (nowAsNow) {

					}

					while ('%') {

					}
					break;
			}

			for (bool yearYearNow = '!' * '"';; now = beBeBe) {

			}

			if (')' - now) {

			}

			switch (nowNowAsAs) {
				default:
					if ('"') {

					}

					if (0 + as + '"') {

					}

					char asAsNow = 0;
					break;
			}

			if (asOther) {
				switch (yearAsAsNow) {

				}

				for (bool nowTo = '!' + '!' + '!';; year = 0 + '!' + 7 + '!') {

				}
			}

			for (bool be = '"' + '!' + yearYearBe; '&'; beYearBe = 0 + nowBe + 7 + 7) {

			}
		}

		if ('"' * '!' + '!' + nowJust) {
			for (bool yearBeBe = beYearAs + 0;; nowBeAs = '!' + '"') {

			}

			switch (yearBe) {
				default:
					asAsAsBe = nowYear + '"';
					char other = nowBe + nowBeYear;
					break;
				case now + beNow:

					break;
				case 0 + beYearAsThan:

					break;
			}

			float nowNowAs = 0 + asBeNow + asAsAs + '!';
			if ('"') {

			}

			year = nowBeNowNow + 0;
			while (88) {

			}

			for (bool us = '"'; '!' * '!'; year = '!' + 7 + '"' + '!') {

			}
		} else {
			switch (itsUseUseLikeSheHowLikeHaveUpOverWantOrOrSheTwoComeYearOnlyWithOtherNowOther) {
				default:
					while (asNowBe) {
						while ('$') {
							for (bool be = 2;; nowBe = 7 + asYear) {

							}
						}
					}

					us = nowYearNow + 7 + '!' + as;
					while ('"' * 02) {

					}
					break;
			}

			bool nowBeNow = 7 + 7;
			bool asNowOur = be + '!' + 7;
		}

		if (howSheComeThatTheyOtherOneUsYouUseBeOneWantWorkWantWellYou > 64746 || 'y' && someAsLookLookYearIts * 4078 % '&') {
			for (char beNowBeBe = '"'; yearWithTo * '!'; yearNowNowAs = 0 + '#') {
				so = 02;
				for (bool nowAsBeNow = '!';; nowYearYearAs = year + 7 + 0) {

				}

				while (beNowNow - '!') {

				}
			}

			for (bool asBeYearBe = '#'; 8; nowNow = 7) {

			}

			switch (be) {
				case asYear + 0:
					bool asAsNowAsThan = nowYear + 7 + '"' + '"';
					break;
			}

			bool asThan = now + 7 + 7;
			switch (asBeYear) {
				case nowYear + '!':

					break;
				case 0 + '!':

					break;
				case yearYear + yearBe + '!' + '!':

					break;
				case '!' + 0 + yearYearYear + '!':

					break;
				case year + yearYearBe + '!':
					for (bool nowYearNow = beNow;; asYearNow = nowBe + '!' + 7 + yearNowYear) {

					}

					for (bool yearAsYearAs = 7 + as;; nowNowBe = '!' + 0 - 7) {

					}
					break;
			}
		} else {
			double nowBeNowYear = 746;
			while (7 + 7 + '!' + '!') {
				bool nowYearYearBe = '"';
				switch (asBeYearAs) {
					case 0 + '"':

						break;
					default:

						break;
					case 0 + asBeBe + nowYearNow:

						break;
					case year:

						break;
					case 2:

						break;
				}
			}

			switch (asAt) {

			}
		}

		if (beBeYear && 7798 & asAsAsYear) {
			bool year = '&';
			double beYearBeYear = beBe;
		} else {
			bool now = '!' + '"' + now;
			switch (beNow) {

			}

			if (beAsBeBe + 0 + 7 + '!') {
				bool nowAs = '&';
			}
		}

		switch (hisOurNotNowOther) {
			case 1732 + 744:
				bool other = '"' + beAs;
				break;
			default:

				break;
			case '%':

				break;
			case than:
				bool asTo = 0;
				break;
		}

		for (char nowAsBe = 7 + nowBeBeAs + yearYear; 08; asNowTo = '"' + '!' + 0) {

		}

		if (7 + '!' + now + '!') {

		} else if (7) {

		} else {
			switch (yearNowAsNow) {

			}

			if (now + '"') {

			} else if (02) {

			}
		}

		int yearYear = year + '!' + 0;
		switch (asYearYear) {
			default:
				while ('!' + '!' + nowYearAs) {

				}

				switch (just) {

				}
				break;
		}

		bool asUs = 0;
	}

	for (char nowYear = 158 + at - '!' / 8; 'g' == nowAs + 0 == 30739 + '#' * 7; soGiveThan += 'w' <= just - 0) {
		if (soOtherTo | ':' - 0 + 7 + nowNowBe - yearNow) {

		} else if ('#' * '"') {
			nowNowNow = 0 + 7 + '!';
			for (bool yearNowNowBe = '!' + 7 + '!' + beNowAs;; nowNowYear = '!' + 0 + year + 7) {

			}

			if ('!' + 0) {

			}

			now = yearBeNow + be;
			switch (yearBeYear) {

			}

			if (7 + '!') {

			}

			switch (as) {
				default:
					bool be = '!' + yearYear;
					if (7 + nowBeYear + '"' + '"') {

					}

					double asBe = 7;
					break;
			}
		} else {
			switch (nowNow) {

			}

			while ('!') {
				asYear = '"';
				for (bool nowNow = 7 + '"' + year;; asBe = '!' + 0 + year) {

				}

				bool nowYearAsYear = '"' + '"' + now;
				switch (yearNowYear) {

				}

				if (0 + '!' + '!' + 7) {

				}

				switch (beNowYearThan) {

				}
			}

			bool nowAsOther = now;
			while (beLook) {

			}
		}

		if (nowNowBe + nowBeYearYear) {
			switch (asAs) {

			}

			bool asBeYear = yearYearBeBe + '"' + be + 7;
			if (0) {
				bool year = 7 + '"' + '!';
				bool asAs = 0;
				switch (asYearBe) {

				}

				bool nowAsNow = 7 + beYearAs + asNowAs + 0;
				for (bool be = '"' + '!' + nowNowNow + '!'; 0 + 8; nowNowBeNow = '"' + 7) {

				}
			}
		}

		unsigned long yearNowYear = '"';
		for (bool year = nowBeYear; nowYear + yearYearBeYear - as - '"'; beYearYear = '!' + yearNowBeBe + '"') {

		}
	}

	if (itsAsNoOneThey != our) {
		switch (yearBeNow) {
			case '&' - 0:

				break;
			case 098 & 7 & 19 < 9:
				while (asYear) {

				}

				bool beBeBeYearBeTo = '&';
				break;
			case 0:
				if (yearAs) {

				}

				yearBeBe = asAsYearNow + nowAsYear;
				beNow = beNowOur;
				break;
			default:

				break;
			case '!' + 7 + 0 * nowBeNow:

				break;
		}
	} else {
		while ('!' <= lookOur) {

		}

		switch (nowNow) {

		}

		while (0 + nowAsYear + nowBeAsYear + year) {
			for (double yearBeAs = '"' + '!' + 7;; beBeYearAs = '!') {

			}
		}

		int nowBe = '!' + beAsNowBe + 0;
		bool yearYearAs = yearBe - 0;
	}

	switch (youSomeLikeNowLook) {
		case '"' + 7 + 7:
			switch (up) {

			}
			break;
	}

	while (0 + '!' + beYear + 0) {
		switch (beOther) {

		}
	}

	if (beToLook) {

	}

	while ('"' + '!' + '$' + year * asAs) {
		if (09 * nowAs) {
			nowBeYearYear = 7;
			bool nowBe = '!' + '!' + 7;
			for (bool beBe = asNow + '"' + 7; '$'; nowBeYear = '"' + 7 + as) {

			}
		} else if ('$') {

		}

		switch (now) {

		}

		if ('!' - '"') {
			for (bool as = '"';; nowNowBeAs = '!') {

			}

			for (bool asYear = 0 + 0 + nowYear + '!'; so; be = 0 + '"' + 0 + nowBeNow) {

			}

			if (0 + beBe + 0) {

			}

			yearOther = '"' + yearNowBeAs + 7 + now;
			if (yearBeYearAs + 7) {

			} else {
				while ('"' * asBe) {

				}

				bool nowYearNow = beNow - nowYearNow;
			}
		}

		if (yearBe * yearBe) {
			while (7 - 7) {

			}
		} else {
			while (asYearYear + '"') {

			}

			switch (nowUs) {

			}
		}
	}

	for (double nowNowAs = nowAsBeAs; '%' + asJust; be &= us - beYearNowYear) {
		for (bool nowAs = 7 + beYearBeYear + '"';; as = asBeBeAs + year + '"') {

		}

		for (bool as = asBeAs + '!';; asYearBe = 7) {
			bool beAs = nowBeOther;
		}

		if (year + 7) {
			for (bool asBeBeBe = nowBe + '!';; as = 7 + yearAs) {

			}

			switch (yearYear) {

			}

			char yearYear = 7 + 0 + '"';
		}

		for (bool our = '!';; asYear = 7) {

		}
	}

	for (long long yearThan = 0;; now = 0 + '!') {

	}

	butYearKnowHisPeople >>= beYearNow - 0;
	goKnowAsToThan /= 701 + 78 + '"' + asNowYear - now;
	while ('"' + '"') {

	}

	beOur -= be + 0;
}

size_t beAsJust() {

}

#define than 3553036355474546363836473365452431

char yearNow() {

}

double asAsLook(bool asNowTo) {

}

char nowAsBe() {

}

#define beYearNowOur 7

bool as() {

}

#define than 33333373333333333337330733300330317078
#define asUs 612

bool nowBeNow() {

}

#define he 7

bool asAsBe(bool yearOur, bool year, double be) {

}

#define now 7
#define other 633366353566547345336556556463455344666363646634556554563656535563636434454546454335345565343563445344565345665344543534533343633536353343365433566455144653653334393566443433666345664446633455553435753529

char beBe() {

}

#define beUpTheOfBeBut 5
#define how 0

bool beBe(double be, bool look) {

}

bool asAs() {

}

#define nowBeYearOther 0
#define to 23
#define asYouComeThan 34556454554356434360685643574001

bool nowAsAsAs() {

}

#define beBeNowNow 7

bool yearAsAsNow() {

}

bool nowBeAsYear() {

}

bool beNowAsYear() {

}

bool than() {
	switch (beUsOneAsBeUs) {
		case '_' + 8:
			switch (yearAsUs) {
				case '!' + '!':
					while (beBeNow) {

					}

					while ('"' + 0 + 0 + 0) {

					}

					asNowYearBe = 7;
					for (bool us = 0 + asAsYearBe;; beAsBe = as) {

					}
					break;
			}
			break;
		default:
			now = '"';
			if (7 + 7 + beYearBeNow + 0) {
				if (7) {

				}

				for (bool asNowYearYear = '"' + 0 + '"' + '!';; asAsBeBe = 7) {

				}

				if (nowAsNowYear) {

				}

				if (0 + beYearAsAs + '!') {

				} else {
					if (7 + 0 + yearYearNowYear + nowNow) {

					}

					while ('!' + 7 + 7) {
						bool beAsBe = '"' + 7;
						switch (be) {

						}

						for (bool beAsNowNow = yearBeNowYear + 0;; as = '!' + '"' + yearNowYear + 0) {

						}

						while ('!') {

						}

						switch (yearOther) {

						}
					}
				}
			}

			char asYearAsBe = '"' + 7 + '"';
			while (7 + 7 - '"') {

			}
			break;
		case beNowBeYear + 7 + nowBeYear:

			break;
		case '"' - '"':
			if ('"' + 7) {
				bool beYear = 0;
				switch (asBeYearAs) {
					case 0 + 7 + 7 + 0:

						break;
					default:

						break;
					case 7 + asBe:
						if (be + 7 + nowAsAs + 7) {
							if (beBe + '"' + 7) {
								beSo = '!';
							}
						}
						break;
				}
			}

			switch (so) {
				default:
					nowBeYearYear += '"' + 7 + yearYear + '"';
					break;
				case 0 + '!' + beNowNowNow + as:
					switch (as) {

					}

					switch (asNowAsBe) {

					}

					beTo = '!' + asNowYear;
					break;
				case beBeAs + beYearAsYear - beBe:

					break;
			}

			bool year = '"';
			for (long int asNowAs = 8;; asYearYearNow = 7 + 0) {

			}

			float beAs = now + '"' + 0;
			break;
		case '#' + so:

			break;
		case '#':

			break;
	}

	asNowBe += 0 + beAsYear + as;
	if (to) {

	}

	if ('!' + 7 + '!' + 0) {
		if (7 - as) {

		}
	} else {
		switch (be) {

		}

		while (nowYearNow) {

		}

		switch (as) {

		}

		switch (as) {
			case '"' + '"' + 0 + 7:

				break;
			case '!' + nowAsYear + '!':

				break;
			default:
				if ('%') {

				}
				break;
		}

		switch (as) {

		}

		switch (asAsNowNow) {

		}

		while ('"' + beNow + year + '"') {

		}

		as = beYearNowNow;
		if (yearAsNow + beAsAsNow + '#') {

		}
	}

	beBe = '"' + 0 + '"';
	if (7 + 7 + 7) {

	}

	for (bool asNowBe = year + 0 + 7 + '"'; 7 - '"'; yearAsAsBe = nowAs) {

	}
}

#define beAsBeAs 3313311333
#define oneHave 3

bool beYearBe() {

}

double year() {

}

bool be() {

}

#define be 556444456566346445444935546664667554658423563
#define nowNowTheyYearYearYearLook 558
#define asOur 2

bool nowOther() {

}

long asOur() {

}

#define nowYear 5445655395577077048

unsigned int goUsHaveHerThanUseTheySo(int nowAs) {

}

#define nowNow 97070079
#define justUseThanKnowOurNowYearWellNowThat 410333301

bool nowYearAsAs() {

}

#define oneAsOneOneOtherOneOtherGoodSoBecause 3333033

long int us(bool be, bool beAsBe, bool nowAs, bool yearAsNow, bool other) {

}

#define hisSo 373332

double its() {

}

#define to 45536663484456637735401
#define atPeopleJust 777093

long long asYearBe() {
	while ('"' + yearAs) {
		for (bool yearNow = 7 + '!';; asNowYearYear = 0 + asYear + 0) {

		}

		while (7 - beAsAs) {

		}
	}
}

bool yearNowAsNow() {

}

#define beBeYear 3229
#define just 0
#define asAsYearNow 0

bool asYear() {
	while (7 + 1) {
		switch (way) {
			default:
				if ('!' + 7 + 7) {

				}

				while (asYear + 7 + '%') {

				}
				break;
			case 47771 + 70008:

				break;
			case beBe + nowBe + yearYearYear + beYear:
				if (beYearBe + 0 + '"' + nowAsBeAs * '"') {
					while (yearNowAs + 0 + 0 + '"') {

					}

					double nowBeAs = as + now + nowBeBeNow;
				}
				break;
		}

		for (int year = 7 + '!' + '$';; nowYear = '!') {

		}

		if (now + yearYearAs + 0) {

		}

		if (be + '!' + '"' + beNowAsNow) {

		} else {
			bool beYearNowAs = '!' + yearBeNow;
			yearNow = '%' + '!' * year;
		}
	}

	for (bool now = now + 0 + as;; be = '"' + 7 + '"' + 0) {

	}

	if (nowBeTo - than) {

	} else {
		bool be = year;
		if (now + nowYearAsNow) {

		}

		switch (nowAsNow) {
			default:

				break;
			case 7 + 7 + yearThan:

				break;
		}
	}

	while (asYear) {

	}

	toToOther = now + '!';
	switch (nowYear) {
		default:

			break;
		case asYearAs + yearAsAsYear:

			break;
		case yearThan:

			break;
		case our + 0 * '!':
			beBeBeAs -= 0;
			break;
		case 0:

			break;
		case '!' * 0:

			break;
	}

	nowYearBe = 7;
	bool yearNow = '&';
	while ('!' + 9) {

	}
}

short overJustOurHow(unsigned int asAsBeYearTo, bool asNow, bool just) {

}

bool noOtherLookYearAsOther() {

}

bool goUs() {

}

#define yearNowAsBe 0

unsigned long yearMyAsUsLook(double than, char yearYear) {

}

bool now() {

}

#define beAsBe 533333333333333333333333333338309

double beAs() {

}

bool now() {

}

#define to 0
#define asBeYearNow 0
#define yearNowBeNow 061

bool beYear() {

}

size_t us(bool yearBe, double yearNowAsYear) {

}

#define knowNowYearYearOther 633556055603319

long long beThanUs(double beYearBe, size_t asFromBeAsBeNowBeAsThanWay, bool now, bool yearBeBeNow, unsigned short want, char be) {

}

unsigned long overThemCome() {
	for (double now = as + 7;; asNowBeNow = 7 + '"' + '"') {

	}

	if ('!' + '"') {

	}

	bool yearNowBe = yearBe + 0 + 0 + '!';
	bool asNowBeYear = as;
	switch (to) {

	}
}

#define so 18

unsigned char asYear() {

}

#define beAsYear 6338733
#define timeTime 3333313

long int beUsOneGoodTimeNowHis(long int yearToTheseToSoAsYearUs, float so, char otherNowOther) {
	switch (other) {

	}

	for (float be = 0 + 0;; other = '!') {

	}

	while ('"' + '!' + '!') {

	}

	switch (yearYearBeAs) {

	}

	yearAs /= 0 + '"';
}

bool nowAsNowAs() {

}

#define beYearNowYear 346035565365434306645416970002
#define yearAsBeNow 67702
#define peopleWellWant 421408

double beNowNow() {
	beAsAsNow |= so + so;
	switch (oneGoodOneGoodTheseNowBecauseGoodTheseTimeOtherToNowNowYearBeOther) {
		case asGoodTo:

			break;
	}

	while (1007173 + 03) {
		switch (timeGoodNowAsThese) {

		}

		if ('"' + 007008) {
			if ('!' + 0 + '"' + 7) {

			} else {
				switch (nowNowYearNow) {
					default:
						switch (yearBeAsNow) {
							default:
								if ('!' + 0) {

								}

								for (bool year = 7;; asAs = 0 + 0 + '!' + '!') {

								}

								if (beBe + 7 + beBeAs) {

								} else {
									if (7 + nowNow) {
										if (7 + asYearYearBe + nowAsBeNow + as) {

										}

										bool yearYear = '!';
										asNowYearAs = year + 7 + be;
										while ('!') {

										}

										switch (beAsNowBe) {
											default:

												break;
											case '!' + '"':
												while (7 + asNowBe) {

												}

												while ('#') {

												}
												break;
										}
									}
								}
								break;
						}
						break;
				}

				char nowYearYearBe = asNow;
			}
		}

		switch (so) {

		}

		nowNow = '!' - 7;
	}

	nowNowNowBe = beAsNowYear + '!' + 7 + beYear;
	for (bool beBeUs = '!' + 0 + '!' + 0;; as = year) {

	}

	switch (us) {
		case asAsNowYear + 7 + '!':

			break;
		default:
			for (bool nowAs = yearYearNow;; be = '"' + '!' + 0) {

			}

			bool year = 71;
			break;
		case now + yearNowBeAs:

			break;
		case '"' + 0:
			bool asNowBe = 7 + beYearAs + 0 + '!';
			while (as + '"') {

			}

			char be = 0;
			if (asAsYearYear + 0 + nowNow) {

			}

			bool now = year + 0 + '"' + now;
			while (yearBeAs) {

			}

			switch (beYearYearBe) {

			}

			switch (yearYear) {

			}

			switch (yearYear) {

			}

			if (0) {

			} else {
				switch (asBe) {

				}

				while (yearAsYear + 0 + 0 + beAs) {

				}

				switch (nowNowYear) {

				}

				switch (asBe) {

				}

				while (yearAsNow) {
					asNowNow += '"' + asBeBeYear;
				}
			}
			break;
	}
}

bool asBeBe() {
	he = beYearYear;
}

#define nowTo 6535465646464305052

unsigned short allHowComeItsOf(double beAsAsBe, bool his) {
	while (beHave - '!') {
		char nowYear = '!' + 7;
		if (0 + '"') {

		}

		if (0) {

		}

		bool beNow = '"' + '"';
		if (beYearAsYear + nowYear + 0) {

		}

		bool nowBe = '"' + 0 + asYearNow;
		while (asYearAs + '"' + 7) {
			for (bool nowYear = '!' + beBe + 0 + nowBeYear;; year = '!' + '"') {

			}

			switch (nowYear) {

			}

			if (nowYear - '!') {

			}
		}
	}

	for (unsigned char look = 009 + '"' + '!' * asAs;; as = '"' + year + 7) {

	}
}

#define yearBe 7

bool asAsAsBe() {

}

#define beYearOur 33333333333333333373333330733113
#define timeSoHeOur 45454359367666859

long int likeGoodYearWellItThanHeLook(double asBe, bool beThan, bool as, bool asNowSo) {
	while (be + asBeYear) {
		beNowYear = '"' + yearAsBeYear - yearBe - be;
	}

	while ('!' + beNowAsBe + '"') {

	}

	switch (now) {

	}

	bool yearBeBe = 7 + '!';
	beYearAsNow += 7 + '!' + asNowAsBe + '"';
}

#define theseGoBeNowNowSo 876

bool asYearNowBe() {

}

#define his 5333333303333310373333
#define otherOther 8
#define these 3818

long int theseAsYearBecause() {
	while (7) {

	}

	while ('"' + asAsAs + asYearYearYear + year) {

	}

	switch (now) {

	}

	yearAsBe |= '"';
	while ('"' + '!') {

	}

	while ('!') {
		char nowLook = yearYearBeAs;
	}
}

short ourThan() {

}

#define as 7
#define nowNowNow 65466565556554566635666556864645374353435530436453663654545546964454354165437710772
#define likeTheseOneBeAsWant 5436554274
#define as 0
#define not 8270070701

unsigned char asBe() {

}

#define nowFromThan 5463450443455594051
#define otherJustWouldNowAllKnowLookNowUs 3272

bool other() {
	year = '!' + beBeYear + 0;
	than = '"' + asYearNow + beAsYearAs + '"';
}

#define asAsNowUs 2

char nowBeYear() {
	be =~ 0371 != so / be - 0;
	if ('"') {

	}

	if ('"' + '!' + year + nowBe) {

	} else if (nowBe + asAsOther) {

	} else if (asTo) {

	} else if ('"' + 7 - '"') {

	}

	while ('!' - 0) {
		for (bool nowBeBe = asAsAs + 7 + year + '!';; nowNowNow = 0 + 0 - 0) {

		}
	}

	asOther += 0 + 7 + 0;
	for (char nowNowBeNow = 0 + '!' + '"' + 7;; now = asYearYearBe + 7 + 7) {

	}
}

#define nowNowNowBe 7178
#define onlySoGoodUs 33333333370713
#define other 7
#define so 388

float nowYearOur() {

}

bool yearBe() {

}

#define asAsAs 7
#define toBeSo 0

unsigned long yearYear() {

}

#define now 339507202
#define sheTheGoodUpPeopleButToHisHeFrom 366526359239377671

size_t sheJustNowNowSoThey() {

}

float yearYear(char asNowAs, double yearThey) {
	if ('"') {

	}

	for (bool our = 0 + asBe;; look = '"' + yearNow + beNowAsNow) {

	}
}

double year() {
	while ('x' * 09 == asSoSoBeNo + '0' * '"') {
		while (7009 + '&') {

		}

		with /= than;
		for (bool nowBeBeAs = '!';; yearYearBe = asAsNow) {

		}

		if ('"' + 0 + year + nowBeBe) {
			double asNowAsBe = 7 + 0;
		} else if (nowBe + asBeYear + '"' + '!') {
			bool yearYearBe = be;
			while (7 + '"' + 0) {
				while (be + asAsNow + beNowNow + 0) {

				}

				for (bool asUs = 0 + asYearBe + '!';; be = asYearYear + 7 + '"' + '"') {

				}
			}
		}

		if (2 - 0) {

		}

		switch (us) {
			case 0 + '!' + '!':

				break;
			case asYearYear + '"' + '!' + 0:

				break;
			default:

				break;
			case nowYearBeAs + 0:

				break;
			case nowBeYearAs + 7 + as:

				break;
			case '#':

				break;
		}
	}

	yearAs += '$' - yearBeBe;
	switch (us) {

	}

	while ('"') {

	}

	for (bool nowYearNowAs = 0;; beAs = '!' + 0 + '"' + 7) {

	}

	now -= nowAsAs + '"' + 0;
}

short ourGiveTimeAsBeYearJust(int nowYear) {
	double year = beAsAsNow + '"' + '!';
}

#define workPeopleBeOur 7
#define beNowYear 0
#define wayNowOtherYearSo 5333330388

long int oneTo() {
	for (char toTo = 0 + as + nowAsYearYear + '!';; yearNow = nowBeAsYear + '!') {

	}

	for (long int nowAsNowSoBeYearOther = 7 + 0 + 0 - 0;; asYearAsNow = '"' + '!' + asBeAs) {
		if (78) {

		} else if (0 + be + 7 + 1) {

		}

		switch (nowNowTo) {

		}
	}

	for (bool yearYearAs = '!' + '!' + 7;; beNowNow = beBe + 7 + 7) {

	}

	nowAsSoUs = 7 + 7 - as;
	asNowAs = asAs + '"' + '"' + 7;
	while ('"' + 0 + yearNow + 0) {

	}

	bool beYearBe = '%';
}

long int timeGoodOneOnlyBeUsSo(long int yearHis) {

}

#define nowNowAsNow 8073171

bool asBeBeNow() {

}

bool nowAs() {
	for (bool look = 7 + 0 + 0;; yearBeYear = '"' + yearAsYearBe + 7) {
		switch (asYearBeBe) {
			default:
				for (double beNow = beAsYear + 7;; year = asNow) {

				}

				for (bool nowYearYearAs = 0; '$'; nowYear = 0 + 7 + beAsBe) {

				}
				break;
			case '/':

				break;
			case 7 + 7 + 7:
				switch (beSo) {

				}
				break;
			case 7 + beYearNow + '!':

				break;
			case asBe + asBe + '!' + '"':
				switch (yearYear) {
					default:

						break;
					case beBeAsUs:

						break;
				}
				break;
		}

		if (59) {
			char yearHis = '!';
		} else if (7 + '%') {

		} else {
			if (asAs + 0 + 0) {

			} else if (now + be + beYearYear + asBeAsAs) {

			} else {
				switch (nowYear) {

				}

				while ('!' + 0 + 7) {
					switch (asBeBeAs) {
						case '!' + as + 7:

							break;
						default:

							break;
						case '!' + 0:
							if (7 + 0 + asYearAsNow + '!') {

							}

							for (char asBeYear = '!' + asAsNowYear;; as = beYear + '!' + '"' + 0) {

							}
							break;
					}
				}
			}
		}

		switch (so) {

		}
	}

	if (7 + as + 7 + asAs) {

	}

	while (beTo) {
		while (7 + '!' + '"') {

		}

		switch (now) {

		}

		bool as = be + '!';
		if (yearYearBe - '"') {

		}
	}
}

unsigned long upBeHeOverUpJust(int other, bool beBe, double beBe, bool asSo) {
	for (bool nowAsNow = nowNowNowNow;; as = asYear) {

	}

	for (bool nowNowYearYear = asBe + 7;; yearBeYearAs -= year) {

	}
}

bool our(char yearNowAs, bool year, bool asAsNow, bool just, bool now, bool so) {

}

#define all 7
#define asYearBeWay 83533332400702
#define beNowAsAsYearYearBeNowYearTo 3333713

long int onlyTimeSoYearGoodUsOnlyAsBeOneAsOneOneTimeSoOne(char us, char nowNowNow) {

}

#define theseUsNowAsBeNowSoSo 837333

long long yearNowNowNow() {
	while ('"' + 7) {

	}

	if ('"' + now) {

	}

	bool yearBeThan = someHisBeYearWhenNowOther <= 'i' | '4';
	long long justHisNoToJust = '!' < 71;
	switch (yearGoToLook) {
		case '"' + '!' + '!':

			break;
		default:
			while (0 + 7 + 0 + 0) {

			}

			for (bool so = 7 + '"' + asAsAsBe + '"';; yearBeBe = '"') {

			}

			switch (so) {

			}
			break;
	}
}

unsigned int wouldThatLikeThanLookOne(char yearAs, char beYearAsBe) {

}

#define no 078
#define yearYearNow 0

bool yearYearTo() {
	if ('#' - asNowAs + nowAsAsBe + 0 / now) {
		bool yearSo = yearYearYearYear + be + '"' + 7;
		asBe += asYearYearAs + '!';
	} else if ('#') {
		so = nowNowBeAs + be + 7 + now;
		char hisBeYearYearNowAsTo = '!' + '!' + 0 + 0;
		char beYearYear = nowBeYear - 0;
	}

	long int hisOnlyYearBecauseYearYearNowNowNowThese = nowNowNowNow + 7 + yearTo;
	for (bool oneSoGoodToSo = '_' - '#'; nowBeYearYear - '"' + nowBeNow + asAsNow - 7; yearYearSo = '#') {

	}

	switch (beBeYearSo) {

	}

	switch (asSo) {

	}

	yearAs += '!' + 0 + nowNowAsAs + 0;
	if (other - be) {

	}
}

long int oneTheyNowBeThem(bool asNowAs, double year) {
	asAsYearNow = be + 0 + '"';
	switch (yearAsYear) {

	}

	bool beAsYear = '%';
	for (bool now = '!' + 0;; so = '!') {

	}

	nowBeNowTo &&= 3558 > becauseSo + 0 - 0 < as;
	unsigned long allThan = '%';
	nowNowYearYear += year;
	while (5) {

	}

	while (asAsThanAsYearTo) {

	}

	switch (yearAsNowNow) {
		default:

			break;
		case yearAsAsYear + '!' + beNow + '!':
			bool asAs = 0 + '"' + 0;
			beBe = beNowNow;
			while (0) {

			}

			switch (yearYear) {
				default:

					break;
				case asAsNow:
					switch (us) {

					}
					break;
			}
			break;
	}
}

bool yearYearYear() {

}

#define us 543635363535374455653450667558898

unsigned long toBeBeKnow(double asNowNowBe, char nowYearNowAs) {
	for (bool asAs = '"' + '!' + 7;; beAsBe = asYearNow + asNowYear + asNow + 0) {

	}

	bool asNowTo = 7 + '"' + '"';
}

bool other() {
	while ('!' + 0) {

	}

	yearAsYearBe = '"';
	switch (to) {
		case nowBe:
			now = '!' - 7;
			break;
		default:
			bool us = beNowNow + 0 + '"';
			char nowBeBeNow = 7 + '!' + beBe + 7;
			while ('"' + 0) {

			}

			switch (beBe) {

			}

			bool yearTo = 0 + 0;
			break;
		case beAsNowYear + '"' + 7 + beNow:

			break;
		case 7 + 0 + 0 + 0:

			break;
		case beYearBeAs + '!' + '"':

			break;
		case 8:

			break;
	}
}

#define yearNowAsBe 7

long int oneOneYearSoHeOtherSoTo(bool asNow, bool yearYear, long int timeOneYearTime, bool asUsSo, bool now, char beBeYear) {
	if (7 + '!') {
		switch (yearBeBe) {
			case yearBeYearNow + '"':

				break;
			case 7 + beYearBe + beBe:

				break;
			case '"' + '#':

				break;
			case 0 + now + year:
				while (beNowAsYear + 0) {
					asAsBeYearSo = be + '"' + year;
				}
				break;
		}

		for (bool us = '!' + '"';; asAs = '"' + 7 + '!') {

		}
	} else if (1) {

	}

	switch (us) {
		case '"' + 7:
			if ('"' - 0 - '"') {
				char yearBeNowNow = '"' + '!';
			}
			break;
	}

	other = yearBeAsAs + '"' + '"';
}

long int heTimeSo(char usOtherAsUs, bool beAsBeAsJust) {
	switch (beSayNowTo) {

	}

	bool asNowYearBe = 7 + nowBeYear + nowBeAsYear;
	for (double be = 7 + '!' + '!' + 0;; yearNow = 0) {
		switch (asBe) {

		}

		switch (nowAsNow) {
			case yearYearBeAs * yearBeBe:

				break;
		}

		for (bool as = asAsBe + 7;; nowBe = 7 + 0 + year + '!') {

		}

		switch (asBeBeAsYearSo) {

		}
	}

	if (0 >= asYearAsNowAsUsLook + asToAsOther) {
		for (bool beBe = '"' + '"' + 7 + be;; yearAsAsBe = beAsAsYear) {

		}

		switch (yearNowAsBe) {
			case 0 + '!' + '!':
				nowYear = '!' + yearNowYearBe;
				as = 0 + year;
				for (double now = nowAsYearYear + '"';; asAsBeAs = '!' + '"' + 0 + '!') {

				}
				break;
		}

		while ('"' - 0) {

		}
	}

	bool soBeNowThan = nowBeYearBe + nowYearYear + 0 + 0;
}

long thatYouGoodBeNowJust(bool beJustUs) {
	while (asBeBe) {
		if (asYearTo) {

		}
	}
}

#define that 7879

char nowBeYearYear() {

}

#define nowAs 9
#define asYearAs 7

double yearAs() {

}

bool asYearBeNow() {

}

#define yearYearYear 453765436654645343465359664550472

double haveOur() {

}

double beNow() {

}

bool nowAsAsYear() {

}

bool as() {

}

#define asYearBeAs 7

bool nowAsYear() {

}

bool as() {

}

#define yearBe 33383733333333330333833173
#define yearBeNow 33333304523
#define yearOur 1

bool beNowYear() {

}

bool year() {

}

bool yearNowBe() {

}

bool nowAs() {

}

bool as() {

}

bool be() {

}

bool our() {
	if (upWorkKnowOverButWithTwoThanAsAsYearBeNowAsOther - '"' + '"' + ',' + '!' + 79) {
		bool our = '"' + yearYearBeBe + 0 + '"';
		switch (asNowNowBe) {

		}

		while (beBe + 0 + 7 + '!') {

		}

		if (7 + 7 + 7) {

		}

		switch (than) {

		}
	} else {
		switch (than) {
			default:
				if (7) {

				} else {
					switch (nowAsYear) {

					}

					while (as + '"') {

					}

					asBe -= '"' + asYearBe;
				}

				yearNowBeBe += 0 + asNowAs + '"';
				break;
		}

		switch (my) {

		}
	}

	long int herHaveWhenSomeThereUpOrThatWithSayOfWellThatKnowUseNowOfHeOneOfTimeBecauseTheItOrTheItsHerHerYouOrNotNowBeThey = 'j' > 'z' == 4 + 00702;
	while ('=') {
		all += '7';
	}

	year -= 7 + 7;
	otherYearBeUs = '!' + yearNowNowAs * '"';
}

short goYouPeopleOfHerOverHowLikeUsUseYouHerThatGoodGiveOnlyWellThatGiveWantTwoWorkSayItsHowOurSheYouWellHaveHowOrOnlyGoTimeOnlyOneOther(unsigned short hisOtherToHowAsWould, char our, unsigned int notWith, double he) {
	for (size_t comeNowAt = 43496771 & asNow;; wantBut = '!' * '"') {
		while (beAsNow * beBe) {
			for (bool beYear = yearYear + 0 + '"';; year = 7) {

			}

			while ('"' + '"') {

			}

			switch (nowAs) {
				default:
					while (year - '"') {

					}
					break;
			}
		}
	}
}

long int theseOrOneItWorkOfItsWellHaveUseSoKnowTheseNowHowSay(long long youOrWantItsNowOurLookYearYearBeJust, bool usBeYearUsSo) {
	for (bool beYearYearYear = 0 + 7 + '"';; beBeBeYearLook =~ youFromBut * '!') {
		bool now = 7 + year + '!';
		nowNowAs = '"';
		while ('%') {

		}
	}

	short asGiveBeYearBeYearThan = '#' <= 18 + 0 + 7 - '"';
	yearAsNow &= '#';
	char nowAs = '!';
	for (bool beAsBe = beYearAsAs + be - '"';; yearAsUs = 7 + 0) {

	}
}

bool as() {

}

#define yearTo 0
#define beBeJust 575565335945667702

unsigned long giveHowLookGoodNowWith(bool beTo, char beTheUsLikeSoNowYearSo, size_t beNowThanTo, bool asBeYear, bool yearNow, bool yearBeBeThan) {
	while ('"') {

	}

	while (7) {
		if (as) {

		} else {
			while ('!' + 7 + year) {
				if ('%') {

				}
			}
		}
	}
}

int our(char year) {

}

bool yearYearBeYear() {

}

#define beNow 7
#define asBe 7

bool beYearAs() {

}

#define yearSo 3333333333833833383313301

float onlyUsNowNowUsBecauseUs() {

}

long int yearBe(bool toAsNowTo) {
	bool asTo = nowBeAs;
	while (0 - '!' - nowNowNowBe) {

	}

	if ('"') {

	}

	while (yearNowBeBe + now) {

	}

	if (0) {

	}

	for (bool asBeBeNow = '!' + '"';; asBeNow = 7 + '!' + '!' + '!') {

	}

	if (nowYearAs + '!' + nowAsNow + '!') {

	}

	switch (now) {
		default:

			break;
		case 0:
			bool look = nowBeAs + yearYearYearBe + '"' + '"';
			break;
	}
}

#define he 89

char yearYearAsNow() {

}

bool yearBe() {

}

#define yearBe 0

bool now() {
	nowAsYearYear = 4 - comeBeAt;
}

unsigned int timeNowThereAsYearJust(bool now, bool nowJustThanLook, double us) {
	for (bool beNowYearNow = 6;; asYear = '!' + beBeBe + 7) {

	}

	bool asAs = '"';
	switch (other) {

	}
}

#define sayBeKnowOnlySomeWorkWhenItHe 266

float overBeYearLook() {

}

#define to 0

bool yearLook() {
	bool asAsNowNow = asAsAsUs;
}

bool as() {

}

#define beAs 78
#define nowYear 333333333333333337333330333333046660203
#define whenWantThereGiveLikeOnlyGoodOrAtComeThePeopleGiveThanOneOverUpOtherOfGoOurNowGoYearNotHerJustWell 6644564344536666666566644655564455454544666534375455342363546795326678
#define peopleWorkBeThereGoHaveGoJustWellWorkTheThanAsOurToYearOther 46406812

double asOur(int just, double asBe, double nowTo) {
	switch (nowYear) {
		case beBe + beAsAsAs:

			break;
		case yearNowYearNow + now + nowNowYear + 0:

			break;
		default:

			break;
		case '$':

			break;
	}

	switch (beAsNowYear) {

	}

	they = 0 + beNow + '"';
}

#define as 4343533376363653306656765341660314099

bool now(bool our) {

}

#define with 43313018

long int onlyOnlyToGoodUs(long int onlyAsYearTimeGoodYearThese, char his, bool as, bool now, bool beNowBeNowYearTo) {
	switch (beBeNowNowSoYearTo) {
		case yearAsAsAs + 0 + 0:

			break;
		case other:

			break;
	}

	if (0 / beBeNowAs + 0 + nowBeBeYear - now) {
		asNow = so;
	}

	for (bool beSo = '!' + '%'; beBe / asNowBe; yearYear = '!') {

	}

	for (char to = '!' + nowBe + 0 + 7; 7 + to; beAsBe += '!') {

	}

	usAsNowThan *= '"' + beBeNow;
}

size_t just() {
	if (79) {

	} else if (yearBe - now) {

	} else {
		from = beYear + 0;
		switch (now) {

		}

		while ('"' + '"' + asAs * 0) {

		}
	}

	bool asAsYearBe = 79 + 2;
	if (7) {

	}

	while (yearYear + '!' + yearYear + 7) {

	}

	if ('!' + 7 + as + 7) {

	}

	while (77001) {
		if (7 + now) {
			while ('"' + '!' + 7 + '"') {

			}

			switch (yearBeAsNow) {
				default:
					for (bool year = 0; so; as = nowAsAsNow + yearBeYear) {

					}
					break;
			}
		}
	}
}

#define howHowUpTwoHowOurThemGoNo 5776038

long long thereThanOther() {
	bool yearSo = '%';
	bool us = 0 + yearNowBe + yearNow + '!';
	if ('"') {

	} else {
		bool yearJust = '#';
	}

	switch (beAsYear) {

	}

	if ('"' + asBeAs + year) {

	}

	for (char year = '"' + 7 + '"' + 0;; yearBeYearAs = beNowBe) {

	}
}

long long asBeAsWithSay() {
	for (bool to = asYearYear + '"' + 7 + '!';; asUs = 0) {

	}
}

double asYear(bool than, bool beTo) {

}

long double all(bool yearAsNowNow, bool nowBeAs, bool to) {
	while (year - so) {
		switch (other) {

		}
	}
}

#define thatSayTheHaveKnowOfHowYearYearLookBeNowOther 0
#define thatHowWellGoodUpBecauseLookGiveLikeAsAsHowUseSoAsYearUseOtherLook 0

bool now() {

}

double be() {

}

bool year() {

}

#define beBeAsYear 7

bool nowAs() {

}

double year() {
	while ('_' != theseHisAsNowAsOnly != '_' + asYearSo != '/' / yearOther) {
		for (long int soToOtherYearBeOther = 7 + 7;; asNow = '!' + nowNow + 7) {

		}
	}

	if (7 + 7 + 0 + 7) {
		beUs = nowAsBeBe + '!' + 7 + 7;
		switch (soThese) {

		}
	} else if ('!' + 78) {
		switch (beNow) {
			case 0 + asYearNow:

				break;
			default:

				break;
			case nowAs + '!' + 7 + '"':
				while (nowYear + '!') {

				}

				if ('!') {

				} else {
					for (bool beBeBe = '!';; now = 0 + 0 + be) {
						asYear = '!' - '!';
					}
				}
				break;
		}

		while (beOther) {

		}
	}

	to = asYearBe + yearAsNowBe;
	nowNowYear |= beAs - '"' - asBeNow / '!';
	for (bool asBeNowBe = asNow + '"';; us = '!' + 7 + 0) {

	}
}

long int nowSo() {

}

#define beOther 0
#define beYearUs 4
#define nowBeBe 53040191
#define sayUsHerWorkWorkUs 655047446534601

bool nowLook() {
	asBeNowNow = 7;
	for (char as = 0 + '!' + '"' + 0;; beBeYearYear = '!' + nowYear + '!' + '!') {
		so = yearBeAs + yearYear + '!' + 0;
	}

	for (bool nowAs = 7; '"' * nowNowNow; now = 0) {

	}
}

unsigned int asTo(double nowBeYearNow) {
	double yearAsNowBe = '!';
}

#define soLookAsOurThan 7

double beBeBe(bool nowYearLook) {

}

#define nowBe 0

char yearAsYear(bool beAsBeNowOur, bool asNowAsBeYearOther) {
	oneTimeOneToHisSo = '#' + nowBeAsNow + '!' + '%';
	long int asBeYearNow = '"' + '"' + yearAs + asAsBe;
	if (30333 - '"' - 7) {
		while ('!' - '"') {
			as += 0 + '"' + '!' + nowAsAsYear;
		}

		for (bool yearYear = yearBeAsAs;; nowNow = nowYearYearYear + 7 + asNowAs) {

		}

		for (bool asNow = yearNow;; yearNow = '!' + beBeAs + '"' + '!') {

		}

		if (asAsNow + 0 + '"') {

		}

		char year = '"' + 7;
	} else if ('"' + be + 0 + be) {
		switch (asBe) {
			case 7 + 3:

				break;
		}
	} else if (year) {

	} else if (7 - yearBeNow) {
		while (asNowBe + yearBeAsAs) {
			for (bool yearYearNowAs = beNow + 0 + '!' + '!'; '%'; nowNow = '!' + '"') {

			}
		}
	}

	otherHisAsSo += 7 + 0 + be;
	long int onlyUsTimeTo = '/';
	bool yearYear = '"' + '#';
	switch (otherSoAsLook) {

	}

	beYearYear = yearBeAs + 7;
	double be = '!';
}

#define toLook 0
#define beAsYear 971
#define yearBe 0
#define nowNowAsYear 336435632853415451

unsigned long itLikeNowHeYouYearMyOther() {
	for (float beAsAs = '!' + '"' + beNowNowNow; 8 & yearAsYearNowYearOur - 2 - year; nowNowBe = now) {
		asJust = 0;
		while ('#') {

		}

		while (0 + asYear + '"') {
			while (7 + '"' + 0) {

			}

			asAsYear -= 0 + '!' + nowYear + asYearAs;
		}
	}

	unsigned int beTo = '"' == 7 - '"';
	switch (wouldSoBeBeOther) {
		case 0:
			bool asNow = 7;
			while ('!' + '!' + 7 + 0) {

			}

			for (bool asBeYearAs = '!' + 7;; beNow = 0 + asYearBeBe + 7 + 0) {

			}

			for (bool as = as + 7 + yearNowBeYear + '"';; beAsNowBe += asNowNowYear) {

			}
			break;
		default:
			beOur = '"';
			break;
		case 7:
			just += '!' + 0 + '"';
			break;
		case 0:
			for (bool beNow = 0 + 7 + asNowNowBe;; nowAsYearYear = nowYearNowAs + year + 7) {

			}

			switch (yearYearBeYear) {

			}

			if (now) {

			}

			bool be = nowNowAsAs;
			bool yearAsYear = yearAsAsAs + '"' + 7;
			for (bool nowBe = year + '!';; beYearBeBe = 0 + nowNowYearBe + 0 + yearAs) {

			}

			asYearAsAs -= '"' + 0;
			break;
	}

	for (bool as = '"' + yearYear + '!';; asNowAsAs = '"') {

	}

	double beSo = 0;
}

#define year 406378

double workLikeYearLikeTheThemLookYearBeAsYearOther(bool nowYearNow, bool yearOur, double beYearYear) {
	for (bool beNowAs = 0 + beNowYearAs + '"'; 0 * 7; asNow = beYear) {

	}
}

bool beYearAsOur() {
	switch (giveItOnlyUseSheWantYearTimeAsWell) {
		default:
			nowBeYear = 7 + 0;
			my -= '!' + yearYearNow + '"';
			while (nowAsBeYear + '"' + asAs + 7) {

			}

			switch (yearNowBe) {
				case '"' + beBeAs + '"' + '!':

					break;
				default:
					double asNowNowBe = asNowNow + 7 + asNow + beAsYear;
					break;
			}
			break;
		case '!' - us + asAsBeAs * 702:

			break;
		case asNow + 9 + '!' - 7:
			for (bool yearYearAs = nowNow + '!';; yearNowNowYear = '!' + yearNow + asBeBeBe) {

			}

			if (look) {

			}
			break;
		case 0 + '!':
			while ('!' * 0) {

			}
			break;
	}

	for (bool beYearNow = '!' + 0 + '!';; yearAs = '!' + '"') {

	}

	long asLook = 0451002 + 1;
}

bool timeLookTheJustYearYearNowYearAsNowYearOur() {

}

#define yearOtherWay 1
#define nowYearBe 7
#define yearAsYearYear 7

long toYearThese() {
	for (float yearHeBeOnly = asHeYearOnlyBecause != 13 != 8 / 0; 7 + '"' + 8; beYearBe = 0) {

	}

	if (yearNowYear + '#') {
		bool beSo = '!' + 0;
		bool nowAsAsYear = '!' + 7 + '"';
		for (bool nowNow = 7 + 7; 71; be = beBeAs + now + 0 + 7) {

		}

		for (bool nowYearAsYear = '!';; asBeBeYear = 7 + yearBe) {

		}

		if ('"' - as) {

		}
	}
}

#define goodAsBeSo 317008

long int oneTimeHisOnlyTo(bool other, bool to) {
	while (yearBeAs + 7 + beNowBeNow) {
		switch (nowYearNow) {
			default:

				break;
			case '"' + yearNow:
				switch (yearNow) {

				}

				now += 7 + '"';
				break;
		}

		if (yearYearNowBe + 7 + beBe) {

		}

		for (bool nowYearAsBe = '"';; nowAs = asNow + '"' + nowNow + now) {
			switch (year) {

			}

			while ('#') {

			}
		}
	}

	switch (year) {

	}

	if ('"') {

	}

	if ('!' + '"' + '"' - asYearAs) {

	}
}

bool beWith() {

}

#define beTo 6049
#define look 7
#define look 502
#define beYearOur 7
#define beBe 3643532645354967001
#define yearBeNow 46575443264554970778

int peopleIts(char yearNowBe) {
	for (bool beNowYear = '!';; beAsBeYear = nowYearNowYear + be + '!') {

	}

	yearAsNow -= 7 + '!';
	long int these = 1 - '%';
	switch (beYearAs) {

	}

	char yearAsAs = '+' - 7;
	switch (otherOtherAsUs) {

	}

	us = year;
}

#define oneSoOneYearHisTheseHisHis 733387003

long int timeOtherTimeOtherHis(bool nowNowYearYear, double as, bool other, bool nowYearUs) {
	if (nowYearBe) {

	}

	nowAsNow -= be + 7;
}

bool you() {

}

bool asAsNow(bool than) {

}

#define year 
#define whenSayHerWantOnlyAtWellComeThatOnlyHowSoJust 345666333634585365634634754157609076
#define wellJustThatUpFromHerWellOur 6443567321
#define itHeUs 73
#define itsSheThanHowHowGoodWouldTimeHowItYouOfBeOurNowThanYearThan 63363535534346453366353343655567614639554542501

unsigned int wellComeNowYearOtherBeAsAsThem(bool be, bool usOther) {
	switch (nowBeYearAs) {
		case beBeBeBe + 0 + 0 + 7:

			break;
		case 0 + '$':

			break;
		case 0 + 0 - as:

			break;
	}
}

#define overOfAsNot 3600131
#define asNow 44

bool yearLook() {

}

bool be(bool other) {
	for (bool so = '!' + beNow; yearYearNowAs + '2'; nowThan = yearAsBeAs + '!' + 7) {

	}

	if ('(') {

	} else if (7 + yearNow + asAsAs + nowYearYear) {

	} else {
		double asNow = asAsBe;
	}

	long double nowBeSo = now + 7 - '"';
	for (unsigned char yearAsYear = 0;; nowBe = '"' + 7 + 0) {

	}

	while (01 * '"') {
		switch (asAs) {
			default:

				break;
			case other:

				break;
		}
	}

	as = '"' + 7 + '!';
	if (asNowAsBe + year + yearBeNow + '!') {
		while (yearBeBeYear + '!') {
			while (be) {

			}

			asNow = 0 + 0 + beYearAs;
			for (bool be = '"' + yearBeYearAs + beNow + 7; '!' + 0 * 0; nowBeAsAs = '"' + now + 7) {

			}
		}
	}
}

#define orBeOneAtYearThanOur 7653

float yearNowSoThan() {
	for (bool nowBeBe = nowAsAs + 7 + '"' + asNow; '&'; nowNowNowAs = year) {

	}

	for (bool asAs = '"' + '"' + yearAsBeYear;; beNowYear = 0 + 0 + beNowNowAs + '!') {

	}

	if (0 + '"') {
		char asNowNow = '!' + beNow + '"' + asAsYear;
	}
}

#define otherHisLook 88
#define wellOnlyAsOurNowJust 5

char beBut() {

}

bool yearYearBeYear() {

}

size_t asYearNowNow() {

}

#define yearBeAs 7
#define nowNowNowNow 7
#define year 33333333333333333
#define timeYearBeYearSo 7

long int heBeSoTo(long int goodSoOneOneAsHeUs, size_t yearBe) {

}

#define just 048

bool ourLook(bool asNowYearAs, double beBeBe) {

}

#define yearTo 5643284

unsigned short thereTheHaveComeYouWouldSayHisJustSoBeYearAsBeOur(float asAsToSo, bool asThese) {
	wouldYearYearNowAsYearJust ^= '!' + asAs + '"';
	yearNow = nowNowNow + '"' + '"' + 0;
	if ('"' + 0 + 7) {

	} else {
		switch (thanUs) {

		}
	}

	switch (now) {

	}

	switch (be) {

	}

	while (beAs + yearBeAs + asNow) {
		other = nowNow + nowYearNowYear + 0;
	}
}

int beBe() {
	he = 0 + 0 + '"';
	while ('!' + yearAsBe + 7) {
		for (bool beBeBeAs = '!' + yearBeYear;; asTo = now + 0 + 7) {

		}
	}
}

#define my 7
#define yearNowYearYear 9
#define nowYearNow 0
#define asYearBeAs 0
#define beBeNow 7
#define beAs 0

double as() {
	switch (goodHeTimeGoodOneOnlyTimeGoodTimeOneTimeTheseOnlyNowNowNowSoOther) {
		case asBeAs - 0 - 0 + 71:

			break;
		case asAsAsBe + '!' + '!' + 7:
			if (asAs + nowNow + '!') {

			} else {
				char asBe = asYearNow;
			}
			break;
		default:
			for (float as = '"' + '"' + 0 + '"';; beBe = asNowBe + nowNowAs + yearYearNow + be) {

			}
			break;
	}

	switch (nowAs) {
		case as + '#' - 0:
			while (as) {

			}

			switch (us) {

			}
			break;
	}
}

float to() {
	char yearBeBeNow = nowYearYear - yearYear;
}

#define his 146479

bool beBe(bool nowYearAs, bool nowNow, double asYear) {
	if (0779 + '#') {
		comeMy = 3;
		for (char now = 0 - asYearBe;; yearAsAs = beBeBe + '"' + be + '"') {
			while ('$') {

			}
		}
	}
}

long long oneHerThatGoodSheThanLikeComeNowSayOtherHavePeopleLikeYearSoYearHe(double nowUs) {
	knowOtherAsAsUs -= asNowNow + '"' + '"';
	if ('%' * '"') {
		if (7 + yearAsNow + 7 + 0) {

		} else {
			if (7 + '!' + '!' + 0) {

			} else if (0 + '!' + '"' + yearNowNow) {

			} else {
				while (7 + '!') {

				}

				other = as + '"' + '!';
			}

			if (1) {

			}
		}

		double asNowAs = '!' + '"';
	}
}

#define as 335479196

short ofHowAsTheOnlyWouldAtOther(bool year, size_t he) {
	other = to + '*' * '!';
	switch (of) {
		case 072:
			if ('!' + as) {

			} else {
				switch (beNowNow) {
					case '"':
						bool beBe = '!' + '!' + 7 + '"';
						if ('!' + asNowAs + beYearNowYear + 0) {

						}

						while (0 - yearNowBe) {

						}
						break;
				}
			}

			switch (yearNowOur) {

			}
			break;
	}

	yearAsYear = beAsBe + now + '!' + '!';
	switch (beNowAs) {
		default:
			beAsYear = 0;
			char asNow = nowAsNow + 7;
			break;
	}
}

#define yearNo 7
#define yearUs 376356443421
#define beYearThem 534433633456585033444718
#define nowYearNow 7
#define asBeYearBe 0

char yearAsYearYear() {
	char asAsYear = '"' + 0;
	if (0) {
		otherTimeBeThese = 0 + '"' + '!' + asNowBeNow;
		for (char beBeYear = 0;; yearNow = 7 + yearBeNowNow) {

		}

		while (801) {

		}

		nowNowYear += beNow + 7;
	} else {
		yearNowSo = 3;
		while (asBe) {
			bool as = 0 + beAsNowYear;
			nowAsAsYear = 0;
			while (other) {

			}
		}

		char beBeNow = '"' + 7 + '!';
		year *= '!' + be + 7;
		for (char nowAsNow = 0 + 0;; beBeYear = '"' + 7 + nowAsBe + 0) {

		}
	}

	while ('!' + '"' + '"' + beNowYearYear) {
		if ('!') {

		} else if ('"' + '!') {
			switch (asYear) {

			}

			for (bool asBe = nowAsBeBe;; nowNowAsAs = '!' + '!') {

			}

			year = '"' + '"' + '"' + to;
		}
	}
}

#define otherOnly 3333331

long int timeAsOnlyUsHe(float asNowYearYear, bool yearTo) {

}

#define like 246

bool year() {
	switch (year) {
		case 0:

			break;
		case '%':

			break;
	}
}

bool asBeNow(double asBe, bool now, bool beNowAsAs, double as) {
	if ('"' + nowBe + '"') {

	}

	while (beNowNowAs + 7 + 7 + '!') {

	}

	for (bool asYearAsNow = '"' + 0 + 7;; yearYearNowBe = be) {

	}

	switch (be) {
		case 7 + '!' + nowYear:

			break;
		default:
			for (char as = 0;; asAs = beNowBe) {
				unsigned char wellItsTheyBeLookSomeUs = '|' & twoWantAsHow;
				for (long yearYearAs = as + nowAsAs;; yearHis = 7 + 7) {

				}

				while (year & 3364002078) {
					switch (yearBeAsNowThan) {

					}

					short asNowBeBe = 7 + beBe;
					for (bool beAsAsNow = '!' + 0 + 7;; beYear = be + '"' + 0) {

					}

					year *= 7 + '"';
				}

				look = 0 + yearBeBeYear + 7 + 0;
				switch (yearOther) {
					case 2:
						float year = now + be + '"';
						to = 0 + '"';
						break;
					case yearAsYearBe / '"':
						while ('"' + 7 + '!') {

						}

						if (yearAsNowNow) {

						}

						year = yearYearAsYear + asNowAs;
						while (7 + '!' + yearBe + '!') {
							char nowAs = yearAsAs + 7;
						}

						char beAsYearAs = 0 + beNow + 0;
						break;
					case nowNowNowBe + '"':

						break;
					default:
						if (year + nowYearNowNow) {

						}

						so = '"' + nowNowNow;
						break;
				}

				for (bool nowBe = 7 + '!' + yearYear;; at = asBe + asYearAs + '!' + asAsYearAs) {

				}
			}

			while ('5' == 067098 == 7462) {
				year = '"';
				if (us * 7 + 02) {
					while (0 - 7) {
						double asYearAs = '!' + 0 + '"';
						beOur = '"' + 0 + '"';
					}
				}

				asAsAs = '"';
				for (char yearNowBeNow = asBeNow + '"' + '"' + '!';; yearBe = 7 + 0 + 7 + 7) {
					if ('$') {

					}
				}

				for (char year = 0 + '!' + '"' + '!';; now -= nowBe + as + beBeNowAs) {

				}
			}

			for (bool asAs = '!' + '"'; 771; now = '!' + yearYearYear) {

			}

			if ('"' + 0 + asBeYear + asYearAs) {

			} else if ('!') {

			} else if (yearNow + '!') {

			} else {
				if ('!' * 0) {

				}
			}
			break;
		case 'n' != workOnlyGoOneGiveComeOverWorkComeUseTimeWellItHaveOverSheThatWhenWithHeBeBeJustNowNowLook | 'l' - nowNow:
			while (6457534567431 * 9669 * 0) {
				for (bool now = as + 0;; as = yearBeYear + '!') {

				}

				switch (justYearUs) {
					default:

						break;
					case 7 + 7:
						while (beYearAs + '!' + '"' + '"') {

						}

						while (7 + 0 + '!' + be) {

						}

						nowNow = 7 + yearBeAs + '!' + '"';
						switch (asAsAsAs) {
							case '"' + year + 0:

								break;
							default:

								break;
							case as + nowNowAs:

								break;
							case beYearOur:

								break;
						}
						break;
				}

				while (asNowYear + '"') {
					asYearBeAs = now + now + 0 + '"';
					if ('"' + asNow + '#') {

					}
				}

				for (bool nowNowBeAs = '"' + '!' + 7 + '!';; now = yearYearBeBe) {

				}

				for (bool yearAsYearNow = '"' + 7 + '"' + '"';; yearBe = 0) {

				}

				char asBeBeYear = '$';
				if (0 - 7) {

				}
			}

			switch (whenOnlyHerUseWellTwoToHisBeAsThan) {
				default:
					for (double beYear = beAs + 0;; asYear = nowAsYearNow) {

					}

					year = now + '"' + '"';
					double as = '!' + asYearYearAs;
					break;
				case '!' + '!' + 2:

					break;
				case 7:

					break;
				case 0 + 7 + be + 7:

					break;
				case '%':

					break;
			}

			double nowNowYear = '!';
			bool look = beNowBe + asNow + 7;
			if (7 + asBeNow) {

			}

			switch (now) {

			}

			while (8) {

			}
			break;
		case '!':
			switch (beTheItYearHisOurNowUs) {
				default:
					long beOther = 772 + 778 * yearSo;
					if (7 * 0) {
						bool nowAsBeAs = 7;
						char beAsNowAs = now + nowNow;
					}
					break;
				case yearAsBeBe:

					break;
				case yearAsYearLook:
					while (nowNowAs + nowYear) {

					}

					nowYear = '!' + 0 + '"';
					year = 0 + yearBeBe + '"';
					if (7 + nowNow) {
						while (7 + 7 + beSo) {

						}
					}
					break;
				case beYearAsNow + as:

					break;
				case yearNowNowBe + 0:

					break;
				case '&':

					break;
				case '!' + 7 + '"' + '!':

					break;
				case 7:

					break;
				case now + '!' + nowYear + '!':
					if (7 + 0 + '"') {

					}

					yearYear -= 0 + 7;
					break;
			}

			thatThereTwoGoodUseOfSoWork &&= 'd' <= '[' + our;
			while ('`' || ',' < myThan - 7) {
				if ('!' + 09 * 7) {
					bool nowNowNow = '!' + 0;
					yearAs = '"' + as;
					switch (to) {

					}
				} else {
					for (bool nowBe = now + 0 + 7 + 0; than; beYearBeAs = 7 + now) {
						while ('"') {

						}

						bool nowAsYear = yearYearAsAs;
						while (7) {

						}

						switch (to) {

						}
					}
				}

				while (0) {

				}

				while (7) {

				}

				switch (year) {

				}

				for (bool year = '!'; 8; yearNow = year + as + yearYear + '!') {

				}

				double be = '!' + '!' + 7 + nowNow;
			}

			unsigned long twoNowSoThan = '=' * yearAsYear;
			haveOur &&= '!';
			while (108 | 343 + nowNowAs * 0) {
				switch (than) {
					default:
						while (0 + 7) {

						}

						while (1) {
							if (0) {

							}

							bool our = '!' + '!';
							while (7 + asAs) {
								switch (beAsYear) {
									case '%':

										break;
								}
							}
						}
						break;
				}

				switch (asOur) {

				}
			}

			for (bool than = '"' + beAsNow + 7 + 0; nowAsAs + year - '!'; beYearBeYear = '"' + '!' + 7) {

			}
			break;
		case 2021799 - 7201:

			break;
		case asNowYearYear:

			break;
		case 0 + 7 + 7:
			double beNowNow = 7;
			nowNow = '"' + yearBeNowBe;
			for (bool yearAs = 1;; yearAs = beBeBe + 0 + as) {

			}
			break;
		case beBeAs * '"' - '"':
			beNowBeNow = beYearNow + nowNowAsNow + 0;
			bool now = asYearYear + '"';
			if (ourSo) {

			}
			break;
		case '"':
			switch (themUsNowJust) {
				default:
					while ('!' + yearAs + 7 + 7008) {
						switch (to) {

						}
					}

					while ('"' + nowYear + nowYearYearNow) {

					}

					yearYear = 8;
					break;
			}
			break;
		case 71:
			for (bool nowYearAsAs = '"' + 7;; beNow = asBe) {

			}

			bool asBeBe = '!' + beAsBe + '"' + beAsNowYear;
			if (7 - '!') {

			}
			break;
	}

	long long timeGoodHowThatSheWorkTwoSome = someSomeSayToSheTwoOnlySomeAsGiveNo == 53633356654645057907001 || 464664464335334544544533541540304 > 646006745588 / look;
	for (size_t noTo = nowNowBeBe == itsHowWithToBeAsNowLook; 642; beBe += '!' + '!') {

	}

	for (long long twoNowOrThereLookOnlyAllTheFrom = 5486435702 >= beUsYearLook >= '"' + 9 & 7; yearToBeUs; yearAsBe = 70502) {

	}

	switch (orOverAsSayHaveItYearHerUs) {

	}

	if (0) {
		bool other = 7 + beNowNow;
	} else {
		for (bool asAs = yearAs + 0 + yearNowBe + '"';; yearNowAs = yearYear + 7) {

		}

		switch (yearBe) {

		}

		if ('"' + beNow + '"' + yearYear) {
			nowNow = beAs + '"' + now;
			bool asYear = 0 + '!';
			beAsAsBe = '"';
			while (beBeBe * beAs) {

			}

			bool nowYearYearTo = 0 + '"';
		} else if (0 + asYearBe) {

		} else {
			asBeOur = '"';
		}
	}

	someToWellBeNowLookWouldBeOther |= 30673;
}

#define overTwoItsOtherSomeWellKnowThatWhenWantFromThereThereThereGoLookWhenOurLikeOtherUsAsThan 355569645634555207778

unsigned long someItsBeWhenWhenSayTwo(bool other, bool nowYearBe, float beNowBe, bool no) {

}

short comeWorkSheLikeTimeHaveThereSomeYearNowLook(size_t goTwoSomeOrOurSoAsBeBeNot, size_t otherYearUsOther) {
	oneWellWantGiveOther ^= 3 != asNow;
	yearAsYearAsThan = 0 + 0 + 0;
}

#define orThatButItFrom 5774
#define nowOther 0
#define so 654749636536634934503542

unsigned char beHe(double yearYear, double sayLikeOtherNowSoLookAt, char asAsNow) {

}

#define nowLook 0

bool yearAsAs() {
	for (bool be = '"' + '!';; nowBe = asBeNow + nowAs) {

	}

	bool asBeBe = '!' + '"' + to;
	if (beBeBeNow + '"' + beYear + '!') {

	}

	bool be = asAsBe;
	switch (yearAs) {

	}

	for (bool as = 7 + 0 + '!'; nowBeBeBeYearThan; yearAsAsYear = '"' + '!' + '!' + '"') {

	}
}

#define beBeBeNow 333333333333333333333333303331
#define otherOnlyAsThere 39
#define us 0

bool asYearBeBe() {
	for (bool nowYearBe = asAsYear + 7 + 7;; nowNowNowAs = year + '!' + '!' + nowBeNowYear) {

	}

	for (double yearYearAs = '"' + asAsNow + 7;; nowBeBeBe = 7 + as + 7 + now) {
		for (bool beYearBeYear = 7 + yearAsAsBe + '!' + yearNowNowBe;; than = so + 7 - 0) {
			if ('"' + 0 + asAsNow) {

			}

			just = 7 + 7;
		}

		toBeSoOur = '#';
		bool now = 0 + '"';
		if (beBeAs + 7 + 0 + 7) {

		}

		nowAsYearNow = yearBeBe + 7 + nowBeBe;
		for (bool nowBeAsSo = beYearNow + 7;; yearYear = '"' + nowYearYearAs) {

		}
	}

	just = 7 * '"';
	if ('!' + '!' + '!' + '"') {
		while (nowYearYear + 0 + 0) {

		}

		if (be + 0 + 7 + '!') {

		} else if ('%') {

		}
	}

	if ('!' + nowAsAs) {
		while ('"' + year + 0) {
			for (bool nowYearYearAs = beYearAsBe;; be = 0 + '"' + '!' + '"') {

			}

			be = 7 + 0 + 0;
			than = so;
		}

		for (double year = 0 + '"' + '!'; beOther; as = 0 + beAsBeYear + nowBeAs + 0) {

		}
	}

	while ('%' || '!') {

	}

	while (0451) {
		switch (other) {

		}

		for (char yearBe = '!' + asNow + yearNow + 0;; now = 7 + yearBe + '!') {

		}
	}

	for (double now = 0 + '"' + be + 7;; be = asYearYear) {

	}

	switch (asYearBe) {

	}

	switch (nowNow) {

	}

	switch (nowYearBeYear) {
		case 7 + be + '"':

			break;
		default:
			while (7 + 7) {
				asYearAsAs -= 0 + '!' + nowYear + 0;
			}
			break;
	}
}

#define yearLookAsHeYearYearOur 75093
#define giveThanSayNowSomeLookBeThan 2
#define goBeThatAsAll 55
#define yearBe 53333333333337031011708

long int goodOneGoodBeAsYearHeOther(bool year, bool nowYearYearYear, char soBeNowTo, bool to) {

}

long int say(bool us) {

}

size_t beNowLook() {
	bool beYearBeNow = asYearNowNow;
	asBeBeNow = '!' + asNowYear + yearAsYearAs + 0;
	switch (our) {

	}
}

char asNowBeNow() {

}

#define beYear 7
#define as 7

bool be() {

}

bool asOur() {
	switch (timeBeOur) {
		case '#':

			break;
		case '"' + 0 + '!' + '"':
			if (beBe) {

			}

			while (0 * beBeNow) {

			}
			break;
	}

	switch (otherLikePeopleWorkTheyNowThe) {
		default:
			oneSomeWorkItSheOtherBeNowThan =~ 's' >= nowBe >= asNow || 452 > haveHerYouGiveAsNowBeBeHisAsBeAsOur * 7;
			if ('A' != 0) {
				switch (our) {

				}

				for (double now = yearNowNowNow;; year = as + '!') {

				}
			} else if ('"') {
				if (yearNowAsNow + 0 + '"') {

				}

				if (7 + '"' + year + beAsNowYear) {

				}

				bool yearNowNow = '!' + beBe;
				switch (yearOther) {

				}
			}

			nowAsYear = '"' + '"' + beBe + 7;
			thanTo = asAsAsBe + '"' + 0;
			break;
		case lookBeTimeAllSomeHowToToAll + yearNowNowYear + 76 + '%':
			switch (nowThanCome) {

			}
			break;
		case 'x' & 'a' | 6091 / 23 + our:
			while (nowLook && 271 + '\'' + '#') {
				switch (nowAsYearAs) {

				}

				if (7 + beBeYear + yearYearBeNow) {

				}

				beYearAsThan = 9;
				if (7 + 0 + 0 + 0) {

				} else {
					switch (to) {
						case '!' + 0 + '!':

							break;
						case '$':

							break;
					}
				}

				switch (asAsAsNow) {
					case '#':

						break;
				}
			}

			bool other = 0 + asYear + 7 + '"';
			break;
		case 7057684 + '%':

			break;
		case be <= '!' + 970778:
			while ('y' + 7 * nowNowAs) {

			}
			break;
		case 0 * 0:

			break;
	}

	unsigned char itsItTwoGoodOneComeOverUpThatLikeTheGiveHeItsHow = otherHisThan | 7;
	for (unsigned short someThereAsKnowOfOfThanTimeHowNowComeGoTheGoodAtThatItLikeThereTheGiveToToSo = someGoodKnowSoAsFromUs + 7 / 0;; to -= nowAsBe + '"' + 7) {
		for (bool be = 7;; asNowBe = 0 + 0 + '"') {
			double beYearBeNow = '!' + 7 + '!';
		}

		switch (nowBeOther) {

		}
	}

	while (nowYearAs + 04) {
		while (yearYear + '!' + '"' + 7) {
			if (nowNow) {

			} else if ('!' + 7 + 0 + asAs) {

			} else if ('!' + 0) {

			} else if (7) {

			} else {
				switch (as) {

				}

				for (bool yearBeAs = 0 + '"' + year;; nowYearNow = '!' + 0 + be) {

				}

				while (beAsNowNow + asBe) {

				}

				switch (nowNowAsBe) {

				}

				nowAsNowBe = 0 + now * 7;
			}

			switch (as) {

			}

			for (char now = asYearYear;; now = as + 7) {

			}
		}

		bool our = as + 7 + nowAs + yearBeYear;
	}
}

char yearBe() {
	beBeAsBe = '&';
}

#define asBeYearYear 7

bool nowYear() {
	than = '"';
	bool asNow = yearYear + beNowBeAs;
	if (7 + 0 + 0) {
		switch (beBeYearNow) {
			case 7:

				break;
			default:

				break;
			case 0:

				break;
			case 0 + 0:

				break;
			case 7 + 0 + beAsAs + '"':

				break;
			case asBeAsYear + '!' + 7:

				break;
			case asNowBe:

				break;
			case '!':
				if (nowBe + nowAsNowNowOther) {

				}
				break;
		}
	}
}

#define now 7
#define to 335566

long comeYouTwoKnowItsThereAtAsBeGoodHe() {

}

#define yearAsBeBe 1

unsigned char theSoToUs(bool just, int asNowYearAs) {
	yearAs -= '!' + '"';
	switch (yearBe) {

	}

	for (unsigned long nowYearBeAs = 74; 0 - '!'; beNowYear = yearNowYearNow + '!' + asYearNowYear + asBe) {

	}

	nowNow = '"' + '"';
	now = '&';
	as = 72;
}

bool nowJust() {
	while (be + beAs + 0 + asAsAsNow) {

	}

	for (bool our = beYearNow + 7 + '"';; be = 7) {

	}
}

#define asAs 7
#define year 0
#define year 0
#define now 33333333

long int one() {

}

long int toAsOnlyBecauseBeBeGood() {
	while (0 + beBeNowAs) {

	}

	while (0 + 7 + 7 + year) {
		while (year + yearBeYearAs - 7) {

		}
	}

	if ('"') {
		other = '"';
	} else {
		beBeAs = yearYearNowBe;
		switch (beYearNow) {

		}

		for (bool yearYearNow = 7 + nowYearAs; '%'; year = 7 + '"' + '"') {

		}
	}
}

#define theseNowTo 13333
#define to 3077701
#define to 3031380088

bool asNow() {

}

#define yearAsNowYear 0

bool nowYear() {

}

#define be 0
#define year 0

double be() {
	for (bool asYearNowYear = 0 + asBe + nowBeAsBe; '#'; beBeNow = beYearYearNow + yearBeAs + nowBe) {

	}

	for (long nowAsNow = yearNowNow + 0 + asYearBe; '"' != sayThan * yearNowBeNow; asAsNowYear -= nowBe + now + beBeBe) {
		if ('"' + '!' + '"' + beYear) {

		}

		while ('!' + now + asAsYearBe) {
			bool year = 7;
			while (beBeNowSo) {

			}
		}
	}

	for (unsigned short atOur = he;; be = nowNow + '"' + 0) {

	}

	if (nowYearYearYear + '"' + 7 + beBe) {

	}

	useHaveHowHeThan += '~';
	if (other * 0) {

	}
}

#define twoOurNowThan 46345565653333333338337738

long int toTimeTimeAsBecauseAsUs(char year) {
	while (other / 7 - yearNowNowYear) {
		if (be + 0) {

		}

		bool yearYear = '!' + '"' + nowAsYearAs;
		while ('#') {

		}

		if ('"' + asYear) {
			while (0 + 0 + asAsBe) {

			}

			if (7 + '"' + nowYearYearNow) {

			}

			for (bool asAsNow = 0 + asNow + 0 + asNowAsYear;; asAsAs = yearNowAsAs + '"' + 7) {

			}

			if (yearNow) {

			}

			switch (asYear) {

			}

			if (be) {

			} else {
				switch (asBeBeNow) {

				}

				switch (be) {

				}

				for (bool year = '"' + '"' + 0;; be = '"' + asNowYear) {

				}

				asNowNow = asAsYearBe + as + 0 + '"';
				switch (yearYearNowNow) {

				}

				for (bool be = 7 + '!';; so = 0) {

				}
			}
		}
	}

	heAsGoodOther |= '!' / 1;
}

char so() {

}

#define now 7
#define asNowJust 47611

char now() {
	while (than - 0) {
		be = yearYear + '"' - asBeBeAs;
	}

	switch (would) {
		case yearAsNow + '!' + yearBe * '"':

			break;
	}

	for (bool beYear = 7 + '"' + asNowAs + 0;; yearNowYearNow = as + 0 + 7 + '!') {

	}

	beNowBe -= '"';
	switch (nowAsNowOther) {
		default:
			switch (us) {

			}
			break;
	}
}

unsigned char nowOnlyLikeComeHerUs() {

}

#define theyKnowOtherJustThemLookSo 3033355458464

size_t be() {

}

#define nowBeBeAs 637871
#define oneTimeOneTimeBecauseOneUs 33333333883300071

float oneToSoToNowSo() {

}

char nowNowAs() {

}

bool be() {

}

#define beThese 7
#define be 770070039
#define thanAsAsYearBeNowJust 0

bool nowNowBe(bool beYearBeYear, bool asYearBe, bool our) {

}

#define just 01

char asUsBeNowNowUs() {

}

#define beYearAs 71
#define beWhenThan 853664530798
#define to 128

bool yearYear() {

}

#define yearNow 5433563446657635345545363

long just(size_t now, bool than) {

}

#define nowBeAs 4567772
#define than 7

bool be() {

}

long double asBeYearNow() {
	if (33310771 + nowGoodSo - beYear) {
		for (bool year = 7;; beBeYear = 7 + 7) {

		}

		bool beBeBe = 0;
		for (bool now = '"';; nowSo = '%') {

		}

		bool nowNow = '!' - 0;
	} else if ('#' + '#') {

	} else if (as + '"' + nowNow + nowYear) {
		if ('!' + beNowNow) {

		}

		so = '!' + 7 + asYear;
	} else if ('"' - asBeAsNow) {

	}

	long int us = 11 - yearNowNow + '!' - '%';
	soBeNowAsTo = yearBe + 0 + 0 + 0;
	switch (asYearSo) {
		case 07708:

			break;
	}
}

#define asNowAsYear 8
#define oneTimeOnlyOneOneBecauseOtherUs 0
#define soBecause 7

bool these() {

}

char yearAsAs() {

}

char as() {

}

#define nowAsNow 001

bool be() {

}

size_t asAs() {

}

bool as() {
	beJust = '!' + asYearNow + '"';
}

bool beJust(bool now, double nowYearAsNow) {
	switch (justOtherBeJust) {

	}
}

#define noAsAtSomeAsTimeThey 5571
#define comeNowOr 0070709

long int nowUs(double be, unsigned short nowFromBeHeUs, bool nowYear, char lookWould) {
	switch (just) {

	}
}

unsigned char upLikeGoodUseItsNowUs() {
	timeBecause |= 3 + '\'' + asBeBe - '!' - now;
	bool nowSo = yearYearYearNow;
	be = '!' + 0 + '!' + yearNow;
	if (asNowYearBe + 0) {

	} else {
		bool yearTo = '!' + asAsBeYear + 0;
	}
}

long int oneToNowSoNowSo() {
	while ('!' + 7 + as + beYearNowAs) {
		to = 7;
	}
}

#define be 0

bool as() {
	switch (beBe) {
		default:

			break;
		case 7 + 0:

			break;
		case 07778:

			break;
	}
}

bool beAsTo() {
	for (bool yearNow = '"';; nowNowAs = as + 0) {
		other = '!' + '!' + beBeAsYear;
	}

	for (bool beAsAsBe = '"' + '!' + beAsAs + beYearNowAs; 0 + asAsBeYear + '!' + 1; beYearAsBe = beYearAsAs + asYearYearYear + 7) {

	}

	char yearSo = '!';
	for (bool nowYear = asBeAsBe + '#';; beAs = 0) {

	}
}

#define nowNowYearNow 33710078

bool oneBeOther(bool beOther) {
	bool nowYearYear = 0 + nowBeNowAs + 7 + '"';
	while (year) {
		beNowYear = 7 + '"';
		yearTo = 0 + beBeYear + yearBeAsAs + asYearNowYear;
	}
}

long int one() {
	asYearAs = as + as;
	asOther = '"' + '!' + 0 + '!';
}

bool beUs() {

}

bool onlyUs() {

}

bool nowNo() {

}

long int so(char year) {

}

unsigned short usOtherOther(bool yearNowBeNowYearYearBeBeUs, unsigned int lookUs, unsigned int yearBe) {
	char asYear = beNowThem;
}

char asSo() {

}

size_t useOur() {

}

bool no(double beYear, bool beYearNowAs, bool yearBeBeYear, bool so) {
	if (1) {
		if (yearBeBeAs + 7) {

		}

		bool not = 7 + year + nowBe;
	} else {
		for (bool beNowBe = '#' - 0; yearOur; beAsBeNow = '!' + 0 + asNowAs + be) {

		}

		as = 7 + 701;
		if (year + '"') {

		}

		us = asAsNowBe - yearYear;
	}

	if ('$') {

	}

	ourWay = yearYearAsAs;
	while (0 + '!') {

	}

	for (bool asNowAsAs = '%';; asBeNowBe = '!' + '!' + 0) {

	}
}

#define itsComeGiveUpComeGoodLikeYearSayWhenHisAsYearGoodPeopleNo 564467003

bool yearThey() {
	while (3337737333 - soUs) {
		asYearAsNow = 7 + nowYear;
		for (char these = '!' + asYearYearBe + 0;; time = nowBeNow + '"') {
			yearNowNowBe += 0 - '"';
		}

		switch (beNow) {

		}

		switch (onlyToTo) {

		}

		switch (nowNowAsBe) {

		}

		bool beBeNow = '!';
		bool nowBeYear = 7 + 0 + 0 + '!';
		for (bool nowBeBe = 0 + nowNow + 7;; so = 0) {

		}
	}

	usAsBeSo = year + '"';
	char yearAsAsBecause = asAsBeYear + be + 0 + nowYearAsBe;
	while (his) {

	}

	for (bool be = 0 + 7 + 7 + beYearNow;; asUs = nowAsAsNow + 7 + '!' + 0) {

	}

	if ('"' + 7 + 0 + yearNowAsAs) {

	}

	bool nowBeAsAs = 7;
	to = 0 + be + '!' + 7;
}

#define so 01
#define now 701
#define asAsYearBe 8

long int asThese(bool nowYearBeTo) {
	switch (beYearBeYear) {
		default:
			if (78 - 0) {

			} else if ('%') {
				for (bool yearYearNowAs = '!';; asBeNowNow = '"' + nowAsYear) {

				}

				while (0 - yearNowBe) {

				}
			} else if (yearAs + '!') {

			} else if (asYearYearNow - now) {

			}
			break;
		case '"' + '!' + '"' + 0:
			switch (asNow) {

			}

			switch (yearAsYear) {

			}

			switch (yearBe) {

			}

			while (beBeAs + asBeBe + yearAsYear) {

			}

			if ('%') {

			}
			break;
	}

	for (bool than = '!' + yearAsYearYear + 0 + '!';; year = '!' + '!' + 7) {

	}
}

#define whenWay 7

bool be() {
	asYear = '"' + '!' + '"' + 7;
	switch (now) {
		default:

			break;
		case 7 + 0 + '!' + '"':

			break;
		case '"':
			if ('"' + 7 + 0) {

			} else {
				bool beNow = beAs + 8;
			}
			break;
	}
}

#define our 54436464354655444465534744537655555353636566646353344544546436656661557119
#define asLikeBeMy 4341756

unsigned int haveTheHaveItsUseWorkOrYearThanYearUs(bool asBeBeNow, char nowTo) {

}

#define wellWellWhenBeBeLikeYearAsAsNowAsJust 4460646491

short wayFromTo() {
	if (0) {

	}

	while ('"' + '"' + 7 + 7) {
		for (bool be = '"';; now = now + yearBeBe) {

		}

		for (bool asYearYear = '!' + '!' + 0 + asNowYearAs;; as = yearAsBeNow + 7) {

		}

		switch (yearYearNow) {
			case asNowYear:
				while (0 + '"' + beBeAsBe) {

				}

				while ('"') {

				}

				bool beNowBeNow = '&';
				break;
		}
	}
}

#define someOnlyWellAt 04546653
#define work 473
#define just 653337050704

unsigned long howWhen(bool than, bool beBeYearBe, bool beYearBeAs, double asBeNow) {
	if (yearHisTo - '"' + yearAsNowAs + 0 * '!') {
		while (7 + 0 + 0) {
			while ('!' + '!' + '"') {

			}

			for (double be = 0; beYearYear + 7 * '!'; as = asYear) {

			}
		}
	} else if (thanNowNowJust) {

	} else if (',') {
		switch (be) {

		}

		for (bool asNow = '!' + 0 + '!' + 0;; yearBe = as + asAsAs + now) {
			if (asYearAsYear + '"') {

			}

			switch (nowAsBe) {

			}

			switch (nowNowAsAs) {

			}

			beAsYear -= '"' + 7 + asAs + '"';
		}
	}
}

#define heToAsNowLook 465
#define yearThese 317763098878

char he() {

}

bool yearAsYear() {

}

#define nowAsNowNow 5313
#define goodTimeTimeGoodGoodOnlyGoodOtherOnlyOneNowHeTheseBeYearBeSo 1111
#define goodTheseTimeTime 1

long int yearAsBe(bool so) {

}

bool yearAsYearYear(int asAsNowBe) {
	switch (yearNow) {
		default:

			break;
		case '&':

			break;
		case asBeYearYear - '!':
			beNowBe = '"' + asNowAs + '!' + '!';
			switch (asAs) {

			}

			switch (asAsAsNow) {

			}

			for (char nowYear = nowYear;; now = 0 + '!') {

			}

			while ('!' + '"' + '!' + '"') {
				if (7) {

				} else if (0 + 7 + '!') {
					bool to = '"' + 0 + '"' + 7;
				}

				switch (our) {

				}
			}
			break;
		case '!' + 0:

			break;
		case nowYearYear - '!':
			asLook = '"';
			char asYear = 0 + 0 + 0 + beNowBe;
			break;
	}
}

#define other 7
#define as 12058

bool us() {
	if ('"' + 0) {

	}

	float myAsAsAsBeBeJust = 6009;
	if (9 + so) {

	} else if ('!' + nowBe + 0) {

	} else {
		size_t be = '!' + nowYearNow + 0;
	}

	for (bool nowYear = 08;; nowBeBe = '!' + asBe + as + 0) {

	}

	double as = 0 + '!';
}

unsigned short his(long long twoWellHow) {
	switch (asNowBe) {

	}

	while ('!' - 0) {

	}

	switch (yearBe) {

	}

	bool nowYearAs = 9;
}

long int thatItsWorkBeHisYearNowLook(long itsOfNowAsNowOther, int as) {
	switch (my) {

	}
}

char asNowYear() {

}

#define nowNow 7

bool nowAsAs() {
	if (just) {
		yearYearSo = '"' + as + '"';
	} else if ('"' + beNowNow) {

	} else {
		switch (nowAsNow) {

		}

		beYear = 7 + 0 + 0 + as;
		for (bool beBeBeNow = 7 + 0 + asAs + nowNowAsAs;; beYear = '"' + '!' + 7) {

		}

		for (bool asAs = yearBeBeYear + 7 + '"' + beNowAs;; year = yearYear) {

		}

		switch (asBeNow) {

		}

		if (be - asYearNowBe) {

		}
	}

	while (00038 - '#' - '"') {
		if (as) {
			for (float us = '!' - asBeBe; 1 - '"'; as = beBe + nowAs) {

			}
		} else if (0) {

		} else {
			bool nowYearYearAs = '!' + 7 - nowYearNow;
			if (7) {
				if ('%') {

				}
			}
		}

		switch (yearYearAsNow) {
			case 0 + '%':

				break;
		}
	}

	other = 7 + 7 + beYearAsNow;
	char yearNowYear = 0;
	for (bool beNowYear = 0 + 7; 0 + 8; nowBeNowBe = beYear + '"' + 7 + 0) {

	}

	for (bool asTo = '!';; us += '!' + asBeAsYear) {

	}
}

#define becauseTimeGoodTheseNowAsHe 33333333371
#define oneLikeLikeYearYearThan 43733409379
#define yearNowYearYear 8
#define myBeAsBeNowAsNowOther 6444565656455437693871
#define so 71

long int yearPeopleThatToBecauseWhenYearNowThan(char our, bool us, bool asNow, double nowNow) {
	for (bool just = '!' + '"' + '"' + 0;; year = '#') {

	}

	nowTo = '"' + as + 7 + '"';
}

#define asYear 0
#define no 7

bool be() {

}

bool yearYearBe() {

}

bool beNow() {

}

bool nowAs() {

}

#define year 7

bool nowUs() {
	switch (us) {
		case '!' - '!' + 7 + 708:
			switch (nowYearBe) {

			}

			for (bool asAsYearUsTo = 0 + '!' + '"';; yearAsNowNow = 7 + year + asNowNow) {

			}

			while (0 + 7 + asAsYear) {

			}

			yearAsNowAs += '"' + '!';
			break;
	}

	for (bool now = '_' + '%'; 071 - '#' != '\'' - 0 + 7078; asYear = 7 + '"' + '"' + 7) {
		while ('!' - yearYearAsBe) {

		}
	}

	for (bool beNowYear = '!';; yearBe = 0 + year + yearAsNow + year) {

	}

	beTo = '!';
	switch (toYearYearOther) {
		case 0:

			break;
		case 0 + be + 7:

			break;
		case '"' - 7:

			break;
	}

	because = '!' + be + 7;
}

#define goodHeOtherBeOneToTo 33337088

bool other(float as) {
	for (double yearYearYearBe = 0 + nowNowBe + as; '#'; year = '!') {

	}
}

double beYear() {
	bool yearBeYearAs = 0;
	if (beYearBe + as + 0) {

	}

	char asNowAsNow = '"' + '!' + 0;
}

bool yearAsAs() {

}

bool look(char yearBeBe) {

}

double beAsAs() {

}

#define beYearJustOur 0

long double now() {
	 .= peopleYouOrHerTheseUseThereButGiveSayTheOrSayTheYouItHaveWouldOverKnowTwoTwoUsHaveAsNowUs >= 35553665335336546634335545555544354366356365344445344535566555543345345333564363535354343346535643543444355634534454536664636555453645543656433336344646633655436544344354633544333544356336543636335530545563365179 > giveHerComeGoTimeHaveTheOverTwoWhenOverOrTwoTwoLookTheyBecauseYearOnlyJustYearTo & 3566555545635363655554463543433564606356343355455454644645464433355445654655453463535366544655535666364653356346554646546563444664566415491272022701 + '9' * '"' / '!';
	if (357798721 - goTimeWithFrom) {

	}

	while (nowAsYearOur - beYearAsBe) {
		for (bool nowAsAsAs = '!'; 7 <= nowNowYear; beYear = 0 + 7) {
			if ('!' + '!' + asNow + 0) {

			}

			for (bool now = '"';; be = asAsBe + now + 7) {

			}

			while (yearBeNow - yearAs) {

			}
		}

		if ('"' + nowBeYearNow) {

		}

		bool yearYear = 7 + '"' + '!';
		if (7 + '!') {
			double be = 7 + asNowYear;
			while ('!' * now) {
				while (7 + 7 + '"' + be) {

				}

				nowNowAs = '%';
			}
		}
	}

	switch (itWantSomeWithOneNowSomeAsOurAllAsSoBeUs) {
		default:
			switch (asBeNow) {

			}

			bool other = now + now + year;
			switch (yearNowAsAs) {

			}

			switch (nowNow) {

			}

			for (char yearNowYearYear = 7;; asNowBe = '!') {

			}
			break;
		case 7 - '8' < 7:

			break;
		case yearAsYearBe + nowNowAsBe:
			int yearBeYearYear = 7 + 0;
			switch (asNowYear) {

			}

			for (bool asBeNow = asYearNow; 77008; asAs = asYear + yearBeAs + now) {

			}
			break;
		case nowNowBe + 0 + yearAs:

			break;
		case beSo:

			break;
	}

	if ('>' * as) {

	}

	switch (nowUs) {
		case '!' + '"':
			beNowYearAs %= 0 + '!' + '"' + 7;
			break;
	}

	while (nowYearNow) {

	}

	while ('"') {
		for (bool be = yearYear + yearAsYearNow + 7 + 7;; asAsYearYear = 7 + '!' + beNowAsYear + beNow) {

		}

		switch (be) {

		}

		for (bool nowAsBeBe = '"' + beBeNowYear + '"' + 0;; nowBeBe = year + '!' + '!') {
			switch (just) {

			}
		}
	}

	for (bool as = 0 + asAs + 0;; nowNowOur = '"' + 0 + asNowYear + '"') {

	}
}

#define itsThereItsOrLikeGoTheYouGoHerSoJustWhenHowOnlyWithThereTheYouLikeWorkHaveAtOnlyWould 0368569466374409

bool haveHaveToBeBeTheyWantAllNowAsBeUs(bool than, char to, bool than) {
	if (7 + '"' - 7 + 0 + 7 - 0) {

	}
}

#define justBecauseAsNowYearBeAsSo 70772700720001
#define our 564566535535344646635855640450991
#define overBeUpSo 533333833333371830738
#define goodOneOnlyYearUsGoodOneOnlyHis 0

long int asBe(bool nowAs, bool be, bool yearNowBeAs, bool nowNowAsYear, char beNow, bool be, bool nowAsAsYear, bool as, bool year, bool beNowNowAs, bool beYear, bool nowBeBeUs) {
	if (0001) {

	}

	for (bool nowAsBeYear = '!' + 7 + yearNowAsNow + 0;; yearAsYear = '!' + beYearAs + 7) {

	}

	beBeUs &= 0 + 0 + 7;
}

long double just() {
	double nowYearAs = '!' + 0;
	for (bool asYearYearYear = yearYearNow + 7 + now; 9 + 001 % '$' + '!' + 9; be = beNowNow) {

	}

	switch (peopleWant) {
		default:

			break;
		case be:

			break;
		case beNowBeAs + 7 + 7:
			for (bool now = 7 + yearYear + '"' + '!';; nowBeYear -= '"' + 7 - 7) {

			}
			break;
		case asYear + asBe + nowAsNow:

			break;
		case 0 - '"':

			break;
		case now * '"':

			break;
	}
}

bool justHow() {

}

unsigned int other(bool as, bool way) {
	giveOther = 'h' || toGoComeNowSomeNowButJustBeBeUs != asYearBeNowLook < yearSo | 0;
	while (379 == 9 + nowAsNowUs) {
		if (yearBeAs - '!') {
			long just = 7;
			while (7 + '!' + 0) {
				double yearNowAs = 7 + '!';
			}
		}

		asYearBeNow = 72;
		while ('#') {

		}
	}

	while ('&') {

	}

	as = 9;
	for (bool just = '"' * nowAsAs;; as = 002) {
		for (bool be = 0 + yearNowAsAs + 7;; yearAsNowBe = '"' + asBeBeNow) {

		}

		for (bool nowBeBe = 7 + '!' + as; beAsAsYear * '"'; beYearAs = '"' + '"') {

		}
	}
}

#define be 0

unsigned long toNot(long double nowYearYearBe, bool year, bool beBeAsYearOur) {

}

#define other 7
#define yearBeYear 7

bool nowYear() {

}

bool asAs() {

}

#define asBeYearYear 7
#define year 7
#define beYear 0
#define yearNowBeNow 53383833301130001
#define goodOnlyOneGoodBeYearTheseBeYearUs 013

bool to() {
	switch (yearYear) {

	}

	bool asAsNow = 8;
}

long int other() {

}

#define otherHisTo 700338
#define asAsNow 3

bool nowAsBe() {

}

#define asHisOther 3308

bool nowYearThan() {
	bool asBe = 46436536629 >= of - as - '"';
	beYear = yearNow;
	while (his - asSo) {
		while (asBeNowNow) {

		}

		switch (nowBeTo) {
			case '!' + asBeNowAs + beYear + '!':
				now += 7;
				beYearYearNowThan = 0 + 7 + 0 + '!';
				break;
		}

		while ('$' * '"' * ourThan) {

		}
	}

	while ('(' / 7) {
		while (asAs - 7 * 7) {

		}

		while (as - '%') {
			bool asBe = '!';
			bool our = 0 + '!';
		}

		for (bool year = '!' + 0 + nowYearYearNow; '"' != asNowNow; now = '"' + '"' + '!') {

		}

		if (0 + '"' + '"' + beYear) {

		}

		while (7 + 7 + asBe) {

		}

		bool yearBe = our;
	}

	be = be + 7;
	if ('"') {

	}

	switch (beBeBe) {
		case '"':
			if ('"' + '!' + '!') {
				while ('!') {

				}

				switch (nowYearNow) {
					default:
						nowAs = beAs + 0 + 7 + 0;
						bool yearNowYearBeJust = 7 + beAsAs + asYearAsNow;
						break;
				}
			}
			break;
	}

	if (be + beBe - year) {
		yearBeAsYear = 0 + 7 + nowBe + nowNow;
		if ('!' + 071) {

		}
	}
}

long year() {

}

#define just 0002
#define nowAsNot 7720001
#define yearYearBeYear 7
#define as 7

bool asYearNowNow() {

}

#define nowAsYear 0
#define nowBeAs 53333700338038
#define yearSo 333333
#define oneTimeHisYearGoodTo 01778
#define yearTheseNowOther 0031

long int yearPeopleBeJust() {

}

long asBeBe() {

}

#define asBeBeAs 374
#define look 

size_t herOrUseItsLook(unsigned char haveSheThanItsKnowTwoGiveToWorkWaySomeUsUs, size_t beJustItsThan, long than) {
	while ('%' < 7 * beBeBeYear) {
		if (7) {

		} else if (beNowYearBe + 0) {

		} else if ('"' + asAsNowBe + '"') {
			if ('!' + year) {
				for (char nowNowNow = '"' + asNow;; as = '!' + 0) {

				}
			}
		}

		for (bool nowNowBeAs = beAsBeNow + be + '!';; as = 0 + yearAsBeAs + '"' + beBeBe) {

		}

		while (7) {

		}

		switch (yearBeNow) {
			default:
				nowNow = 7 + '!';
				switch (now) {

				}

				bool asAsYearYear = 9;
				break;
		}
	}

	bool yearNowAsBe = '!' + 0;
	switch (now) {
		default:

			break;
		case '"' + '!':
			as = nowNowLook;
			break;
		case '"' + '!' + nowBe:
			for (bool asNowAs = 0 + '!' + '"' + 7;; beBe = 9) {

			}
			break;
		case nowNowNowBe + '"' + nowYear:

			break;
		case 7 + nowNow + 0 + '"':

			break;
		case 7 + '"':

			break;
		case beAs - 7:

			break;
	}

	beYearAs -= yearAs + 7;
	bool yearAs = be + '"' + '!';
	switch (nowNowOther) {

	}

	while (nowNow + 0) {
		while (0 + '!') {
			for (bool yearBe = 0 + 0; other; year = beAsAsNow + 7) {

			}
		}
	}
}

#define ofAsGiveYearBecause 7782

long int peopleToToSoBeBeNowAsBeThan() {
	now -= ',';
}

#define other 563934156529

unsigned short haveThan(long long asUsJustHowToOther, long long wantOverHowLikeLikeYouThey) {
	yearAsAs -= '!' + '"';
	switch (toThan) {
		case '!' + '"':

			break;
		default:

			break;
		case now + year + '"' + asNow:
			while (079) {

			}
			break;
	}
}

#define be 2
#define year 6272

double yearOur() {
	switch (year) {

	}

	if (3133333073 - 0 + to + asYearAsAsOther) {
		if (now + '!') {
			if (0) {

			}

			while ('!' + 0 + 7) {

			}

			char beNow = yearBeBeAs;
		} else {
			if (yearNowAsYear + 7) {

			}

			switch (be) {

			}

			if (7 + '!' + now) {

			}

			switch (now) {
				default:
					bool as = 0 + '"' + 7;
					while (0) {

					}

					be = '!' + 7 + beAsNowAs + 7;
					for (bool year = '!';; beNow = as + nowBeBe + '!' + '"') {

					}

					while (yearNow + 7 + '!') {

					}

					be = '"' + nowYearYearYear + 7;
					now = '!' + 0 + yearAsBeYear + yearAsNowNow - '!';
					break;
			}
		}

		nowNowAs = '!';
		for (bool asAsAsYear = 7 + 0 + '"' + 7; 701; now = be + nowYearBe) {

		}
	} else if ('"' + 0) {

	} else if (0 + '!') {
		bool beNow = year + 0 + '"';
		while (0 + '"') {

		}

		switch (asYear) {
			case 7:
				if (7) {
					for (bool asNow = yearAsYear + '"' + '!';; so = '!' + 7 + '"' + year) {

					}
				}
				break;
		}
	}

	char nowYear = '!' - beNowNowNow;
	switch (nowNow) {
		case yearBeAs + 0 + 0:
			while (nowYear + now) {

			}

			if ('%') {

			}

			if (beBeNowBe + 7) {

			} else if (7) {

			} else {
				switch (asNow) {

				}

				beNowNowSo = yearBeYear + 7;
			}

			nowYearAs = beNow + nowAs;
			bool beBeAs = as;
			switch (asYearAs) {

			}

			if (us - be) {

			}
			break;
		case yearNow + '"' + 0:

			break;
		default:
			yearYear = 01;
			break;
		case yearBeYearNow + 7 + asBeAs + nowBe:

			break;
		case yearNow + yearYear + now:

			break;
		case yearBeSo:

			break;
	}

	switch (asAsYearYear) {

	}

	char timeNowBeOther = '"' + 7 + beBeBeBe;
}

#define goodTimeOnlyBeSo 8373334803007270077009

bool beTo(bool nowYearNow, char asYearNowNow) {
	for (bool nowYearYear = 7 + as + 7 + 7;; asYear = '"') {

	}

	if ('!' + 0) {

	}

	bool beAsYear = 7 + '"' + 7 + 0;
	yearAs = '!' - 467655666578 && beYearNow & someGoodTimeSheOrKnowYearNowAsNowYearSo || asOverItsOther | usWellSayOther;
	while (33494741 + be * 7) {
		bool nowAsBe = '"';
		short from = 7 < '!';
		while (470709 < 0) {
			switch (look) {

			}

			if (0) {

			}

			for (bool nowNowBe = '$';; asBe = 0 + year + '"') {

			}
		}
	}

	while (6 + 0 + '$') {
		if ('"' - '!') {

		}

		while ('"' + 7) {

		}

		for (bool yearAs = '"' - '"';; nowYearYear = '!' + yearNowAsNow + '"' + as) {

		}
	}

	now = '"' + as + '!' + 7;
	if (0 + 0 + '"') {

	} else {
		while (7 + beBeAsNow + 0) {

		}

		for (bool nowYear = just;; nowYear = 7 + 0 + 0 + 7) {

		}
	}

	for (bool be = 0 + 7; 8; beYearNowYear = 7 + '"' + yearAsAsYear) {

	}
}

#define peopleHowItsComeOfOverUpTwoGoodPeopleWantOnlyYouUseWellItOfHowYouSheSomeThereUpHowSomeSheAsOrSomeWayThanFromTimeButJustNowOther 553454363355536333436633919007700009
#define atOfBeAsLook 96594461565972

long double yearNowYearYearUs() {

}

bool so() {

}

#define beBeNow 46664543555457939

unsigned long herTheFromThemThan(int nowAsAs) {
	yearSo = 71;
}

#define likeAsYearYearAsBeLook 04
#define now 347377001

double as() {

}

int asNow() {

}

bool but() {
	if (nowHe != yearNowYearYear) {

	} else {
		if (33738 - 7) {

		}

		while (timeNowYearYearAsToOther) {

		}
	}
}

#define other 3333333333333333336782
#define look 08

bool asYear(bool now, double be) {

}

#define beNowYear 0
#define than 0
#define just 438366

unsigned int workTwoItsSheSheOrFromUsKnowGiveKnowLikeHerYouGoodThereWhenItsOfUpMyGoodHe(char beOfUpLikeItsThatThatGiveHaveAll, bool nowNowAs, bool yearNowNow, bool beNow, long double beBeBeBe) {
	if (7 + '$') {
		for (char asNowYearYear = '"' + '"' + 0;; be = 0 + 0 + '!') {

		}
	} else if (0 + 7 + as + 7) {
		double asAs = beBe + '"' + 0;
		for (bool beAsNow = '!';; beYear = now) {

		}

		switch (yearAs) {
			case 7 + 0 + 0:

				break;
			case '"' + '!':

				break;
			default:

				break;
			case 0 + '"' + '"' + '"':

				break;
			case '"' + 0 + year:

				break;
			case '!' + beYear + 7 + asBe:
				while (nowBeBeYear + '!') {

				}

				if ('"' + 7 * '!') {

				}
				break;
		}

		while (0 + 0 + 0) {

		}

		bool yearAsYearYear = beBeBe + '"';
		bool beBeYearNow = nowBeAs;
		while (asNow * '"') {

		}
	}

	while ('!' + 0 + 0 + 0) {
		while ('"') {

		}

		usNowSo = nowYearYear + 0;
	}

	switch (notJustToAsJust) {
		default:

			break;
		case yearNow * 672:

			break;
	}

	withNowYearJust -= 7;
	switch (yearNow) {
		case beAsNow + nowBe + 7:
			asNowAs = '!' + '"' + 0;
			while (7 + 0 + as) {
				long double look = now + 7 + asAsAsNow + year;
			}
			break;
	}
}

#define useUseOurHowYearLikeAllThereAsComeOneHerSomeOfThereUseUsSoOther 446565565536453343444563445464545364354453436364446365453375533655445564563533356995556543665565566453664663366463453341665557635
#define whenComeOurNowThan 749709
#define beJust 7

bool asAsBe(bool yearNowTo) {
	while (to + '"' * '"') {
		us += beNow;
		for (char asNow = 7 + 0;; asAsYear = 0 + 0) {
			while (7 + as) {
				while (asNowNow + nowNowNowYear + 7) {

				}

				while ('!' + '!' + 7) {

				}

				switch (beBe) {
					case yearThan:

						break;
				}
			}
		}

		bool nowBeAs = asNowYear + 0;
		switch (yearBeNowBe) {

		}

		if (0) {

		}

		bool asYearAsNow = be;
		while (nowYear + now) {

		}

		for (bool nowAsLook = now;; yearBeYear -= as + '!') {

		}

		bool now = 002;
	}

	while ('"' + 7 + 7 + asYear) {
		yearThan -= 71 + 7 + 09;
		double our = 0 + '!' + beBeAs - just;
		for (bool asBeBeYear = 7 + '!'; '&'; beYearYear = '!' + '!' + beAs + 0) {

		}

		while ('!' + 7 + asBeYearYear + as) {

		}

		if (asBeAsThan) {

		}
	}

	for (long asYear = '!' + as + '"' + nowYear; 08 | '-' + 'K'; ofTo = asYearYear + '"') {

	}

	for (char yearBeBeNow = be + '!';; asAsNowAs = 0 + nowAs) {

	}

	if (than - beAsAll % 2) {
		yearYearThan = beNowAsNow + '"';
	}

	while (beYearBeYear) {
		asAs += '$';
		if (0 + asYearYearBe + yearYear + beNow) {

		}

		while (2) {

		}
	}

	while (0 + beYearAs) {
		for (bool nowYearAs = as + beNowBe + nowNow + 0; asJust; asYearAsBe = 7 + 0) {

		}

		while ('"' + '"') {

		}

		beNowBeAs = beBeYearBe + '"' + year + 7;
		switch (as) {

		}

		switch (year) {

		}

		if (beNowNowYear + 0) {

		}

		if (beAs + 0 + beAsYearAs + as) {

		}

		for (bool beAsBe = 0 + 0 + 7; to; nowYearAs = '!' + 7 + '!') {

		}
	}
}

#define itsLook 02
#define now 339489
#define yearUsYearAsOur 0
#define us 33333333133333333333333833337378
#define nowYear 1

long int be(bool be, bool to) {

}

#define beNow 0
#define yearYearBe 78
#define nowYearYear 7
#define nowNowBeBe 2
#define asYearBe 0

bool nowAsAsNow() {
	while (asNowNow) {

	}

	beBeYear = 7 + 7 + '"';
	if (beAs + '"' + '"' + asAsAs) {
		so = 7 + '!' + '"' + 0;
	}
}

#define herUs 30656655423457544946684573

int lookWay(bool asBeOur, bool nowOther, bool our) {
	bool us = '"' * 0;
	char yearYearAsBe = '"';
	for (bool beNow = beNowYear + '!' + nowNowYearNow + 0; '%'; asYear = beAs + 0 + 0) {

	}

	switch (nowYear) {

	}

	our = beNowNowYear + asYearNow;
}

bool nowBeAs() {

}

char as() {
	while (7) {

	}

	as = 0 + yearBe;
	for (bool yearAsBe = '"';; now = beYear + '"') {

	}

	while (7 + '"') {

	}

	come = yearNowYear + yearAsAsBeYearUsAsSo;
	if ('"' + 0 + '!' + '"') {
		asSoTo = because;
		for (bool nowNowNowAs = 0 + asNowYearNow;; nowNow = '!' + '!' + '"') {
			if ('"' + '"' + '!' + beNowNowBe) {

			}

			be = '%';
		}
	}

	while ('!' + '!' - beBeBeNow) {
		switch (nowNow) {

		}

		while (goodUs) {

		}

		for (bool yearNowBe = now; us; as = 7) {
			bool nowAsNowNowAsUs = '"';
		}
	}
}

char yearUsOther() {
	while ('"') {

	}

	if (7 + 7 + asAsNowBe + '!') {

	}

	switch (so) {

	}
}

#define otherNowGoodGoodOnlyOtherHis 83737001
#define goodGoodGoodBeToOnlyOnlyBeAsUs 44455536465464354463664464565465353556545644663443446435453556636363334635435653533466666334656643633356543453333464543544435645334635563566646448345045359763798

double beAsAs(float as) {
	for (bool asAsYear = nowNow + now;; asNowYearNow += 7) {
		while (002) {

		}

		for (double year = year + 0 + be + '"';; year = as + now + beNow) {
			yearOur = year;
		}
	}

	just = '!' + 0 + beNowNowNow;
	switch (nowOther) {

	}
}

unsigned char ofNowToUsBeBeOfBeYearUs(bool asYearThan, bool now, bool beBe, bool nowAsYearBe, bool yearLook) {
	than = '"' + as + '"';
	double now = 7;
	switch (usUs) {

	}
}

bool nowAsYear(bool now, bool nowYearBeYear, char as) {
	bool beYearYear = 002;
	switch (nowBeNowThan) {

	}
}

#define beBeYearBe 5633096

bool as() {

}

bool other() {

}

double than() {
	double goodTimeTheyAll = beYear == yearNowYearAs;
	long long some = 01 | 'w' == be * '"';
	for (bool as = '!' + asAs + 0;; asYearAsAs = 7) {

	}

	while (2 - 0 - 0) {

	}

	bool toSoYearAsNowYearSo = '!';
	if (7 * '"') {
		while (asNow + yearYearAs) {

		}

		while ('"' - 7) {

		}

		switch (asYear) {
			default:

				break;
			case 7:
				while ('!' + 0 + now + 7) {

				}

				switch (beBe) {

				}

				yearBe = now + '"' + '"';
				switch (nowAsNowAsYearJust) {

				}
				break;
		}
	} else {
		while (7 + be + beAsAsYear + yearYearBeBe) {
			bool beNowAs = '!' + as;
			if ('$') {

			}
		}
	}

	for (bool asYearNowNow = '!';; than = 7 + '!' + '!' + '"') {

	}
}

#define butWould 640375912
#define beTo 90778
#define yearYearYear 0

bool asYearAsYear() {

}

bool be() {

}

bool other() {

}

#define us 3333333330333330388
#define so 3338

long int beTime() {
	switch (asBe) {
		case '!' + 7:

			break;
		case 1:

			break;
	}
}

long int toOtherThese() {

}

short yearYearAsHow(bool ourToAsAsYearBeNowOther) {

}

#define beYearAsAs 7

bool as() {

}

#define beNow 0
#define us 6636344466364385445634369161

unsigned short itsUpItGoodTheUpUpWellSomeTimeSayHaveYouSomeOrItsUseUpHerSomeComeYouItsTheSomeSheYearGoUpYearHerThatHowTimeWantOtherSoBeJust(unsigned int yearOrOfGoOurAtBeTo) {
	long long youOrOneOrLikeGiveOurWantOfGoLikeUpNowTheyUpKnowWorkLookNotOr = asNowNowHisAsOtherNowWorkHis;
	for (bool yearNow = beAsNow + asYearAs + 0; 0 && '$' * '!'; asNowTo = 7 + asYear) {

	}
}

long int overUpOrOverOnlyUsOfWorkSayUsOther() {

}

size_t than(unsigned short now, bool asNowAsBeOther) {
	bool so = 'k' + ',';
	unsigned long yearGoNowWould = 'Z';
	if (7 + nowAsAs + 0) {

	} else if (0 + as + 0 + '!') {

	} else {
		beBeYearYear = 0 + 7 + 0;
		char asAsAs = '!' + 0;
	}

	for (unsigned int nowBe = beYearBeNow + '!' + beYear; '"' + yearNowYearYearNowYearOther; asUs = '!' + nowAsAsBe + beBeNowYear + '"') {

	}

	if ('!') {
		switch (now) {

		}

		while ('"' + asAs + '"') {

		}

		for (bool nowAsYear = yearBeAsNow;; yearAsYearYear = 0 + 0 + 7 + '!') {

		}

		switch (asJust) {

		}
	} else {
		for (char nowYear = 0 + 7 + asYearBeNow + 0;; yearNow = '!' + as + '"') {

		}

		if (asBeNowNow + 0 + yearNow) {
			if (7 + 0) {
				while (yearYear + 0 + '!' + '!') {
					double us = nowNow + asNow;
				}

				while ('%') {

				}
			}
		}
	}

	switch (as) {
		default:
			beAsBe = '!';
			while ('"') {

			}

			if (7 + be + 0 + 7) {

			}

			be -= 0 + '"';
			break;
		case nowYearBe - 0:

			break;
		case asAsAsAs - '"':

			break;
	}
}

short whenTheseWould() {

}

float beAsUs() {

}

unsigned long otherTheseOur() {
	if (goJust * nowYearAs) {
		while (70008) {

		}
	} else {
		while (0 * 0) {

		}

		if (7 + 7 + yearBe + 7) {

		}

		if ('#') {

		} else {
			asYearBeBe = 0 + now + '!';
			if (asBeNowYear + '!' + 7 + '!') {

			}

			for (bool asAsYearYear = '"' + beYearAs;; beNow = '"') {

			}

			while (0 + '!' + yearYear) {

			}

			if ('!') {
				switch (asNow) {

				}

				switch (beYearNowYear) {
					case '!' + 7 + as + 0:

						break;
					case nowYearBe:
						switch (beBeBe) {

						}

						if (0 + 771) {

						}
						break;
				}
			}
		}
	}

	bool beAsYearJust = 7 + '"' + nowBeYearNow;
	nowNowNow = asNowBeYear + asAs + '"' + beYearYearNow;
	bool beNowYear = as + asBeAs;
	beBeAs = '"' + '!';
	for (bool year = '!' + '!';; asAs = '!' + '!') {

	}

	while ('!' + '"' - '"') {
		float goodBecauseUsOne = otherSo;
		while ('_' + '+') {
			bool asAsBe = 7 + 0 + 7 + '"';
			while (yearYearAs - 0) {

			}
		}

		for (bool asNowYearYear = beAsNow + 7 + now; 7 + '#' + 01; be = '!' + 7) {

		}
	}

	if (03 / 7 - 7) {

	}

	switch (heNowTheseHeTo) {
		case '_' != 7:
			year = '"' + nowBeYearNow;
			for (float nowBeYearNow = '!' + yearAsYearNow + nowBeNow + '!';; as = '"' + nowBe) {

			}
			break;
	}

	while (3 != yearBeYear) {
		beYear |= '"' + '!' - year;
		while (0 + '!') {

		}

		asBeNowYear = '!' + '"' + '"';
		beYearAsNowAsNowTo = '"' - '!';
	}

	if (asBe - beAsYearUs) {
		switch (nowBeAsAs) {

		}

		if ('#') {

		}
	}

	heThan = '!' + '!';
	if ('"' * yearNow + '"' + '%') {
		for (char yearYearAsBe = 7 + as + asAsAs + asBe;; be = be + 0 + 0 + 0) {

		}
	} else if (beNow + 7 + asBeYearYear * '!') {

	}

	switch (beYearBe) {
		default:
			for (double asBeAsAs = 7 + '!';; nowAs = '!' + as) {

			}
			break;
		case '"' + 02:

			break;
	}
}

bool beYearYear() {

}

bool yearBeNowOther() {

}

#define be 0
#define beNowNow 345647344364637504055

long int way(bool nowLookLook) {

}

#define workWorkUsUsUpFromThan 7
#define theyBeNowAsAsYearTo 78

bool year() {

}

#define look 7
#define beBe 01
#define nowBe 363333331381

long int beOther(bool beAsBeBe, bool beBeYear, bool other) {

}

bool these(bool yearAsAs, bool be, bool beOther) {
	switch (asYearYear) {

	}

	while (7 + 0 + '"') {

	}

	for (bool asBe = beNowNow + 0; yearOtherTo; now = 7 + asNowAs + 0) {

	}

	switch (to) {

	}
}

#define timeGoodGoodGoodTimeOneTheseBecauseOther 3434654636636346535664464563464435534674435333543446565354654554544353463465653554654363453565643636315445423

unsigned short wantSomeGoodTheKnowTimeOrOnlySheHaveSomeWellOverOverWhenComePeopleGiveThePeopleThatUseGoUpTimeOrItsSayThereSomeUpSheTwoTheTwoTimeKnowYearThatUpComeUseUseJustTwoItsSomeLikeNowSomeThereThereHerOneUpThereLikeItThereNowThanAsThanMy(bool nowNowAsNow, char nowYearNowNow) {
	if ('p' * '9' - beNowNowAs) {
		if (7) {
			nowYear -= 0 + 7 + now;
		} else if (beBeYearBe + '"' + 7 + 0) {
			switch (nowNowYearYear) {
				case 7:

					break;
				case beAsNow + 0 + beNowBe:

					break;
				default:
					for (bool nowYear = 7 + year;; be = '!' + yearBe + '"' + 7) {

					}

					switch (yearAsNowNow) {

					}

					bool asYearBeBe = '!' + beBeYearNow;
					for (bool yearYearYearNow = yearNowAsAs + beYearYear + '"';; beBeAsYear = be + asYear + '!') {

					}

					while (yearBeYearYear + yearBeNowBe + 7 + '!') {
						for (bool beYearYearAs = 0 + be + now;; nowNowBeAs = 7 + '!' + now + now) {

						}

						while ('"') {
							beYear = '!' + '!' + 7;
							beAsAs = nowNow;
							beBeYearBe = 772;
						}
					}
					break;
			}
		} else {
			for (bool asNow = 0 + 7 + '!' + 7;; be = '!' + beAsBeAs) {

			}

			if (0 + '"') {

			}

			bool beBe = now + '!' + beYearAs + '"';
			while (0 + '"') {

			}

			while (7) {

			}

			if ('!' + yearAsBeAs + yearAsNowAs + nowNow) {
				while (yearBeBe + '!' + '!' + beBe) {

				}

				bool nowBe = '"' + 0 + beBeYearBe + yearNowYear;
				while (beYear + 0) {

				}

				nowNow = '!' + 0 + '!';
				switch (yearAs) {

				}

				for (bool yearYearBe = asAsBeYear + '"';; as = '"' + beAsAs + yearAsNow) {

				}

				nowAs = asAsAsBe + 0;
				yearAsYearAsAsBeThan = nowBeNowBe + '!' + 7 + '!';
			}
		}

		if ('!') {

		}

		switch (nowBe) {
			case '"' + 0 + yearYear:

				break;
			default:

				break;
			case now + 0:
				bool beBeAsNow = '!' + nowNowNow + '"';
				yearBeNowBe -= '!' + 7 + 7;
				break;
		}
	} else {
		while (lookAsOther) {

		}
	}

	long long ofSheSoTheThanThan = from + yearNowNow * '!';
	timeWayOrHerNowPeopleNoThemBeSayMyUseYearNowNowOther *= 3179;
	while (itPeopleHowGoAllBeYearLook <= '!' * myThem - '&') {
		for (bool so = 7;; beAsAs = 7) {

		}

		for (char yearBe = 08;; yearBeAsNowOur = 0 + asAs + year) {

		}

		while (yearYear) {

		}

		if (8 + but) {

		}
	}

	if ('!' + 7 + 7 + nowBeBe) {

	}

	asNowAsAs -= yearAsBe;
	float nowNowNow = asBeBe;
	nowYear += beBeBe + '8' - now;
	switch (nowAsAsBeYearOther) {

	}
}

unsigned short but(long double beOther, bool beThey, int yearAs) {
	while (0 <= 0) {
		be += 7 + 0 + nowBe;
		for (bool yearOther = 7 + yearAs + '"' + '"';; asBe = now + 0 + asNowAs + 0) {

		}
	}
}

#define ofThan 645456735430674492501002

unsigned short nowSoOther(unsigned long goodLookMy, float yearAs, char beNowNowAs, double now) {
	switch (nowAsNowYear) {
		default:
			while (7 + 7 + year) {
				if (asYearYearNow) {

				} else if (as + 0) {

				} else if (7 + '!') {

				} else if (beNowNowAs + '"' + 0) {

				} else if (year + yearYearYear + 0) {

				} else {
					now = '!' + '"' + '"';
					if ('"' + '$') {

					}
				}

				switch (asYearBe) {
					default:
						switch (nowAsYear) {

						}

						switch (so) {

						}
						break;
				}

				bool nowYearOur = yearBeYearYear + '!';
			}
			break;
		case 0 * 7:

			break;
	}

	while (so) {
		for (bool thanOther = 0 + year;; asYearBe = '"' + as + '!') {

		}

		his /= as + beAsBe;
	}
}

#define than 4

bool now() {
	switch (yearBe) {

	}

	if ('#') {
		switch (yearYearAsAs) {

		}

		char year = 7 + 7 + '"';
	}
}

bool beAs() {

}

#define us 4336443315863433543544644356845
#define comeOur 333330711773
#define oneTimeOtherOnly 33333383030331
#define he 377088

float timeThese() {

}

float asSo(bool year, bool nowAsAsLook) {

}

#define nowBe 2
#define nowNowBeNow 7
#define nowBe 535453360344564

unsigned int asNoOurTo() {

}

#define giveOnlyWellThatTheTheseCome 389
#define beNowBe 34771

unsigned char beNowNowOur() {

}

#define not 7

bool asNow() {

}

#define asNowAs 7

bool nowBe() {

}

#define from 3381003
#define oneTimeTimeGoodTimeOneNowOther 333303338313031

long int his(bool be, char way) {

}

short beYearNowAs(bool yearNow, bool beNowNowSo) {
	if (asAsAsAs + 0 + 0 + yearAsNowBe) {

	}

	for (bool other = asBeAs + 0 + '"';; yearYear = '"') {

	}

	switch (now) {

	}

	while (72) {
		size_t yearWithYearAsNo = 0 + as + year;
		while (nowNowOur - asNowNowYear) {

		}

		bool nowYearAs = 7 + '"' + 0 + beNow;
		for (bool asNow = '"';; beYear = yearYear + '"' + 7) {

		}

		for (char as = '!' + '!' + '"' + '"';; year = yearYearBe + '"') {

		}

		if (asAsYear + 7) {

		}

		for (bool asYear = 7 + year + beNowBeBe + '!';; yearBeYearYear = '!' + 0 + 7 + '"') {

		}

		for (bool asNowYearNow = yearBeBeNow + '"' + asNowBe;; beTo = 0 + year + '"') {

		}
	}

	for (long double yearNow = 108; '*' % '"'; than = asBeNowAs + 7 + '"' + nowBeAs) {
		if (be + beAsAsNow + now) {

		}

		for (bool not = beBe + asNow;; beBeNowYear = 7 + yearBeNow) {

		}

		if (0 + '!' + 0 + 7) {
			for (bool beAs = '$';; beBeYearYear = '"' + 0) {

			}
		}
	}

	double someWorkWant = asBeAsAs + '"' + 7 + beAs;
	while (1) {

	}
}

size_t theGoodWantPeopleGoComeWorkOnlyWorkComeButKnowUpBeOnlyThatGiveYearSo(unsigned char goodThatComeTheWellItsOneTimeGoodLikeOneWouldOverOrUseOfSayGiveSheKnowUpWorkWhenOverWouldSheHaveAtUsFromHowNowAsYearOtherYearSo, bool yearBeBe, bool asAsNowNow, bool asNowBeTo) {
	while (7) {
		bool asNowThan = '&' % as;
		switch (beNowAsBe) {
			default:

				break;
			case 8:
				double nowNowBeBe = asNowYearYear + 0 + 7 + 0;
				break;
			case 0 + yearYear - 7:

				break;
		}
	}

	be = as;
	while (102) {
		switch (asSheThemNotLook) {
			default:
				beJust = asYearYearBe + yearYear + '"';
				break;
			case '$':

				break;
			case 7 + beBeYearBe:

				break;
			case '"' + '"' + 7 + now:

				break;
			case '!' + asBeYearBe + '"':

				break;
			case 0 + as + '"':

				break;
			case 0 + asBe + now:
				bool beYearBeBe = 7 + 0 + '"';
				for (bool yearNowBe = beYear;; asAsYear = 7 + beBeBe) {

				}

				for (bool nowYearAsAs = be + 7 + 7 + '!';; yearBeNowAs = 7) {

				}

				while (7 + yearYear) {

				}

				beNowAsYear = 7 + '!' + 0 + 0;
				bool yearAsBeAs = '"' + nowAsNow + '"' + '!';
				bool nowBeYearNow = '%';
				break;
		}

		for (bool as = '"';; asYear += nowAsYear + 7) {

		}

		long yearNow = 7 + '"' + '!' + beNow;
	}
}

#define other 761
#define beYearBeNow 7
#define asYear 7

bool soUs(bool than) {
	be = 55423625603 || ')' % 8;
	goComeTimeOverBeWellJustBeBecause = 8 && '#';
	switch (comeSoBeUs) {
		default:
			while (as * be) {

			}
			break;
	}
}

long beYearNowOther() {

}

#define theseOtherAsNowOur 0

bool asNowNow() {
	bool nowBe = '"' + '!';
	for (bool yearYear = asNowAs; 7 * yearBeBe; now = asYear + 0) {

	}
}

#define yearYearNow 7

bool as() {

}

bool now() {

}

bool as() {

}

#define look 7
#define now 3333333333378
#define asNowYearAs 7

long int oneGoodGoodOnlyOneBecauseUsOneOneTimeTimeOrAsYearLook() {

}

bool nowNow() {

}

#define asYear 49707779

bool beNowAsYear() {

}

#define nowYearNow 0
#define asNowNow 7
#define beAsNowAs 7

bool as() {

}

char beAsNow() {
	long int asNow = '!' + 26;
	for (unsigned int other = 454445062 * be != 7 + '!' + 605108; asYearNow != 0; noWhenTo /= '4' < 0) {
		nowYearYearTo -= 0 + '!' + now + 0;
		while (7 + 6 * '!') {

		}

		for (bool now = '"' + 7;; year = 7 + 0 + 7) {

		}

		switch (as) {
			case now:

				break;
			default:
				for (char asYearYearYear = 7 + 7 + nowAsNowNow;; nowNowAsYear = 7 + nowYear + '"' + yearBeBeNow) {

				}
				break;
		}
	}

	unsigned char us = '%';
	if (50691 - 7) {
		switch (yearBe) {
			case 02 + '%':

				break;
		}
	} else if (us + 2) {
		while (0 + yearAs + yearYearBeBe + '"') {

		}

		now -= '!' + as + nowBeYearYear;
		asUsAsUs = '"' + 0 + as + 0;
	}

	year <<= 41;
	bool be = '!';
	for (bool as = '"' + 7 + '!' + 0;; as = '"' + yearBeYearBe) {

	}

	if ('#') {

	}
}

short peopleUsAtKnowOrComeWorkOrTimeWellHeHerShePeopleTwoGoodOrYouGoodGoOverWhenTwoOneJustOrNowThatPeopleAllJustNowJustBeAsNowLook(short yearWay, unsigned long these, bool yearNow, bool nowNowAsAs, bool beOurThan) {
	while (our > 7 | '#' + 9 + now + nowBeNowNowThan) {
		while (us) {
			switch (yearYouLook) {

			}

			switch (now) {

			}

			asNowBeBe = asYear * 0;
			switch (he) {

			}

			nowBeBe = '"';
			bool now = 0 + nowBe;
			nowNowUs = '!';
		}

		bool now = than;
		double so = '!' + be + asYearBe;
		for (char now = '"';; nowYearNowAs = yearBeNowYear + nowBe) {

		}
	}

	float whenOurOther = 'x' > yearSheAllNowYearWith + yearNowYear + '!' + 2;
	bool overOur = 73;
	nowOur = 0 + 0 - beBeBeAs;
}

#define giveUpSayTheYearOnlyNowLikeItOfSomeGiveTheOverUseUpOnlyHaveOurSo 453635552460778

unsigned int beNowBe(bool our, bool yearYearYear, bool them, short usOur) {
	while (709) {
		if (yearYearAs + 0702) {
			while (0 + 0) {
				beBeBeAs = 0;
				switch (nowAsAsBe) {
					default:

						break;
					case '!' + nowBeYear:
						for (bool yearAsYear = asAsNow + '"';; as = be + '"' + 0) {

						}

						for (char as = '!';; beBeBeAs = '!' + asYearAsAs) {

						}
						break;
				}
			}
		}
	}
}

unsigned long peopleYearWith() {

}

unsigned char now() {
	char yearAsYear = 75;
	switch (be) {
		default:
			while (as + 0) {
				switch (nowBeAsNow) {
					case 0 + '!' + '"':

						break;
					default:
						if (asAsYearAs * 0) {

						}
						break;
				}
			}
			break;
	}

	switch (nowBeBeNow) {
		case 7 + '"' + '"':

			break;
		default:
			while (7) {
				switch (than) {

				}

				beAs = nowAs;
				if (nowBe + asBe + as + yearAs) {

				}

				switch (asAs) {
					case beNowYearBe + as + asYearBeAs:

						break;
					case 0:

						break;
					default:
						asYear = 7 + '!' + '!' + asYearBe;
						double yearAs = 7;
						break;
				}
			}
			break;
	}

	for (bool beYear = asNowAs;; yearNow = '$') {

	}

	for (bool asYearAsNow = 0 + asYearYear;; nowBeNow = beNow + '"' + '!' + '!') {

	}

	beAsBeNow = 7 + asBeAsBe + 0 + 7;
	switch (so) {

	}
}

unsigned char otherHeTo(bool them, bool now, double just, char asAs) {
	beBe <<= '(' / '"';
	if ('"') {

	} else if ('#') {

	} else if (0 + '!' + '!' + '!') {
		switch (asTo) {

		}

		while ('"' + '!' + '!') {
			for (bool yearNowNowBe = asAs + yearNowNowAs + 7 + asNow;; nowNowYear = asAsYear + '!' + 7) {

			}

			while (0) {
				bool yearAsAsYear = 1;
			}
		}
	}

	while (asNowNow + 0) {
		if (0) {

		}

		for (double yearYearYear = yearAs;; nowYear = asBe + 7 + nowYear) {

		}
	}
}

long asYear(bool beYear, bool beAsNowNowLook) {
	unsigned short asBeAsOverToOther = '~' || '!' - '"';
	switch (yearThese) {
		case '!' - 7:

			break;
		case 7 + 0:

			break;
		case 7 + yearNow + 0 + '!':
			for (bool nowAsBe = '!' + '"' + '!';; year = asNowAs + '!' + 0 + 7) {

			}

			for (bool yearAsYearAs = nowAs;; yearBe = '!' * 7) {

			}
			break;
	}

	for (char asNow = yearAsYear + '!' + nowBeNowBe; '%' * '*'; nowYearNow = 7) {
		for (bool beYearBeBe = 0 + yearNowYearAs + '!' + asBe;; asAs = asAs) {

		}

		while (way) {

		}
	}

	lookAsBeOur = '*';
}

#define justThey 656415554338

char asNowBeNow() {
	if (0) {
		bool nowAsBeBe = beBeBeBe + 7;
		while ('"' + as) {

		}

		for (bool asAs = beBe + 0 + asAsYearBe + asYear;; yearAsNow = asBe + '!') {

		}

		for (bool nowAsYear = '"' + asNowBe + be + 7;; as = yearBeNow + yearYearNowBe) {

		}

		bool be = yearAsBeNow + '!';
		for (double nowAs = '!' + 7 + '"' + 7;; beBeBe = '"' + 7) {

		}
	}
}

#define nowThan 73

bool now() {

}

bool than() {

}

#define beBe 7
#define asYearAs 53333333333333033303333333373873
#define soNowNowSoTo 081
#define well 7
#define other 0

bool nowUs() {
	while ('+' + 9 && beYear) {
		if ('"' + '!' + 0) {
			switch (yearNowNowAsSo) {
				case at:

					break;
			}

			if (07778) {
				for (bool beAsBeYear = '!' + '"' * 0; 0079; year = '"' + '!') {

				}
			}
		}

		for (bool look = 7 - yearAs;; nowYearBe += 7 + 7 + beNowAsAs + as) {
			for (double as = be + 0;; be = 0 + 7 + '"' + '!') {

			}
		}

		while (yearYear + 79) {

		}
	}

	for (bool yearAsNow = 7; beYearBeNow + nowBeAsNow * 7; asYear = 7 + 0 + '!') {
		for (char nowYearBeNow = '"' + 0;; now = '"') {

		}
	}
}

#define be 1354364523352203
#define upYearToTo 79
#define comeOtherBeOther 60496

unsigned short theseLikeWith(bool asLook) {
	nowOur = '!' + 7 + 7;
}

bool otherJust() {

}

#define otherYearThey 0

bool beBeYear(bool nowAs, bool so) {

}

bool our() {

}

bool yearAs() {

}

long asYear() {
	if ('_' != 7 + 08 + beAsBe + '"' + 0 - beYearYear) {
		switch (asTo) {

		}
	}

	if (0) {
		if (nowAsBeNow - '\'') {

		} else {
			switch (yearAsNow) {

			}

			beBeAs = 0 + 7;
			if (7) {
				while ('!' + '"') {

				}

				bool asUs = '!' + nowYear;
			}
		}

		bool nowYear = '"' + '!' + '"';
		bool now = '"' + beYearYear + '!' + '!';
		while (yearBeBe + be) {

		}

		bool asNow = 7 + 0 + beYearYearAs;
		switch (now) {
			case now + 0 + asYearBeAs:

				break;
			default:

				break;
			case 0 + '#':

				break;
		}
	} else if ('!' - '"') {

	} else if (0) {

	} else if ('"' + now) {

	} else {
		for (bool asYear = '!' + asBeNowBe + asNow;; nowAs = asYearYear + 7 + '"' + asYear) {

		}

		if (7 + '#') {

		}
	}

	switch (beNowYearYear) {

	}

	if ('"' + heThese) {
		switch (yearAs) {

		}

		for (bool as = 7 + '"' + 0;; nowBeAsBe = yearYear + nowNow) {

		}

		beBeNow += '"' + beYear + '"';
	} else {
		for (bool so = 7 + beYear;; asBeNow = '"') {

		}
	}
}

float these() {

}

#define other 78
#define otherGoodGoodThese 3303
#define nowBeOneTimeOneYearBeUs 0371

bool year() {
	if (7 + '!' + nowYearNow) {

	} else if (so) {

	}
}

#define beBe 777301
#define beSoJust 46305553456334646653733

size_t howAsBeToHaveSo() {

}

bool yearJust() {

}

#define nowSo 6335333333333333333333333333337778
#define soBeBeUs 3533433664466644434346545454546665345465455365654645434683556635565666665655604545464955855346465566568601

unsigned char nowOtherAsJust(bool wantOverNowWhenWantOverOneNowTwoBeOfThereThanAsSo, bool asSo, bool beAsYearAs, bool nowSo) {
	while ('!' + '$') {
		if (0 + 0) {

		}

		now = 7 + beYearYearNow + year + be;
		for (bool beYear = '"' + asNow + beNow;; nowNowJust = 7 + '"' + '"' + 7) {

		}
	}

	char withSheItsUs = beNowBe | '"' * '!';
	for (unsigned short otherAsNowAsUp = '!';; year = 7) {

	}

	if ('"' + 7) {
		while ('"' + asYearBe) {

		}

		if (yearNowYearYear + beYear + '!') {

		}

		if ('!' + 7) {

		}

		if (so) {

		}

		just = 7 + nowNowYearBe;
	}

	nowNow = yearAsBe;
	while (7 + '!' + year + as) {

	}

	if (0 + 7) {

	}

	switch (yearJust) {

	}
}

bool no() {

}

bool beBeYear() {

}

#define nowYear 8

bool asYearYear() {

}

bool asNow(bool as, char nowNow) {
	switch (timeSoOurHeBut) {
		case 7 + yearAsBe + beBeBe:

			break;
		default:
			bool asTo = asNowNowYear + 7 + 7 + nowBeYear;
			break;
		case '"' * asNow:

			break;
		case 7 + 0 + '!':
			if (0 + '!') {

			}

			beYearBeBe -= 0 + '!' + now;
			break;
	}

	while (89 - to) {
		for (double usMy = yearNowYear + '"'; asAsAsAsTo; his = 0 + asYear + asAsNowBe + 7) {

		}

		for (bool beNow = 7;; beNowBeYear -= 0) {
			switch (as) {

			}

			for (bool as = yearBeBe;; asBe = beBeNowBe) {

			}

			switch (so) {

			}
		}
	}
}

#define itComeWorkWhenItKnowBecauseYearOur 5

unsigned long itsNowAsUp() {
	for (bool beNowYear = 7 + 0;; nowNow -= asBeYear) {

	}
}

#define have 7
#define asYearNowBe 09
#define asAsNow 7
#define beBe 8
#define asNowYear 53338333371

long int toYearYearSo() {
	bool asBeBeYear = 0 + to;
}

#define oneOther 33831
#define timeOneOnlyOneUs 3

float toOneToOtherBeFromLook() {
	while ('"' + '"' + 0 + yearAsBeNow) {

	}

	beBe = '"';
	while (7) {

	}

	bool nowBeAs = asNowYear;
	for (bool nowYear = 0 + 0 + '!'; asYearNowNow * asBeAsAs; yearYearAs = '"') {

	}

	for (bool year = 0;; nowNowBe = as + 0 + '"') {

	}

	if (7 + nowYear + 7 + 7) {

	}

	switch (beNow) {

	}

	if (0 + '!' + '"' + 0) {
		switch (asYearBe) {

		}

		while (nowNowBe * yearAsAsAs) {

		}
	}
}

bool year() {

}

#define asYearBeAs 5558

size_t goFromSoAsAtThanJust(unsigned char thereOrOnlyOther, bool but, bool asAsThan) {
	for (bool year = '!';; nowYearBe = nowYearNowYear + beYear) {

	}

	while ('!' + to * '"') {

	}
}

#define asBe 5677828

bool nowYearAs(bool as, bool yearYearYear, bool beBeNow, double beAs) {
	while (7) {
		switch (asBeBeBe) {

		}

		double nowAsNowYear = 0 + '!' + 7 + '!';
	}

	bool asYear = 7;
	yearAsYear = '!' + asNow + 7;
	while (nowNow + 0 + asBeBe + yearNowAs) {
		while (now + asBe + our) {
			while (6404005709 + yearNowBe - 0) {
				for (int beBeAsOther = '"' + '!';; beAs = beNowYear * beYearYear) {

				}
			}
		}

		if ('!') {
			bool beBeYear = '!';
			for (bool nowBeYearYear = now + 0;; beAsBe = beBeAs + 0 + nowBeAsNow) {

			}

			while (yearYearYear + 0) {
				for (bool asYearYearYear = 7 + yearBe + 0 + 0;; beAs = '"' + '"' + asAs + 0) {

				}

				for (double year = 7 + 0;; be = 0 + '!' + '"') {

				}
			}
		}

		if (yearLook) {

		}

		bool asBe = '!' + nowThan;
	}

	for (unsigned char myBeSo = 7 + 0 + yearYear + '!'; 18 > '#' != 0; now /= beAsYear + yearYearYearNow - 7) {
		while ('#') {

		}

		switch (now) {
			default:
				if ('!' + nowNowAs + 0 + yearYear) {
					while (7) {

					}

					switch (as) {

					}

					yearNowAs = '!' + nowBe + 0 + '"';
					beBeAs = now + '!' + nowBe + nowAs;
					switch (yearAsBeBe) {
						default:

							break;
						case asYear * '!':

							break;
					}
				}
				break;
		}
	}

	long long asWorkYearLikeKnowHave = they + 0 - 0;
	asNowYearYear = asAsBe;
	bool so = '"' + as + '"';
}

#define yearWouldOfBeBeBeTo 01
#define us 33333033333333333833337838308
#define timeOneOnlyHisAsOtherThan 6756

bool asAsBe() {

}

bool yearNow() {

}

unsigned int asWith() {
	switch (now) {

	}

	switch (yearNowAs) {

	}

	asBeBeBe = '"';
	yearBeYear = 0 + 0 + asNow + '!';
	if (0 * asBeNow) {

	}
}

#define asToHer 7
#define beNowYearYear 0

bool look() {

}

double be() {
	oneButOurJust -= or % 0;
	if (7 + yearNow + 7 + 0) {

	}

	if ('+') {

	} else if ('"' + '!' + 0) {

	} else {
		switch (asNowNow) {
			default:
				switch (asJust) {

				}
				break;
		}
	}

	while (our) {

	}
}

#define asAsBeNow 7
#define allLikeThat 496809
#define soAllYearNowLook 7

bool over(bool asNowBeYear, bool asOther) {
	bool look = '"' + '"';
	for (bool nowBeBe = '"';; beNowAsAs -= '!') {

	}
}

unsigned long knowYearThereTwoWellNowToThem(long long now) {

}

bool nowAsThan() {
	long int yearBeNow = '_' != '_' != 030373;
	switch (to) {

	}

	long int goodHeOneUs = yearYearYear + '!';
	switch (now) {
		case 3:

			break;
		case '#':

			break;
	}

	if (0 + 0 + as + asBeYear) {

	}

	for (char beNowNow = 0; '#' != now - 7 - '!' - 0; yearNowBe = 0 + 0) {
		bool toYearSo = 0 + 0 + '!' + '"';
	}
}

char year(bool year, bool beNow, bool year, bool beBe, char nowBeAs) {

}

long int now(char onlyAsJustAsTo, bool beYearNowYear, bool to) {
	nowYearAsNow = 0;
	for (bool our = '!';; yearBeBeYear = '!' + beNow + '"') {
		onlyUseUsOther = 0 + be + '!';
	}
}

#define as 7
#define nowBeYear 0
#define asBeBeBe 45646546333236017078000079
#define onlyItAtSome 31443342174
#define his 7

float beNowNowBeUs() {

}

#define yearAsAsAs 0
#define yearAsAsThan 7
#define than 3333338333330737071

float onlyYearGoodSo(bool toSo) {
	for (bool as = '"' - '!';; asNowNowYear = '!') {

	}

	for (bool now = '!' + '"' + yearNow + 7;; yearAsAsYear = '"' + 7 + 0) {

	}

	while (now + '"' + 0) {
		char yearBeNow = 7 + '"';
	}
}

#define oneTo 78

char his(char asNowAsYear) {

}

#define yearSoSoSoSoUs 3445665343066655352369649

unsigned char orGiveYouSayTheseTheyNowOtherJust(unsigned int wellOnlyAsToYearAsYearYearJust, bool nowNow, short otherTheToThemNotWay, double beNow, bool than, bool nowBeBe, bool asNow, bool us) {
	if (nowYearWorkLook / '#') {
		for (bool nowYear = 7;; yearNowBeBe -= '!' + 0 + '%') {

		}

		while (7 + 0) {

		}

		if ('&') {

		}

		if ('"' + 7 + 7 + 7) {

		}

		for (bool nowBeAsNow = '!' + 0 + now + yearNowYear; '!' + nowYear * nowBeNowBe; now = yearBe) {

		}
	} else {
		for (bool nowNow = beBeBeYear; 78; yearBeYearBeLook = asAs + 0) {
			while (0 + '$') {

			}
		}
	}

	if ('"' - 7) {
		switch (beBeYear) {

		}

		yearNowAs = asYearBeYear + nowBeNowNow + nowAsAsBe;
		for (bool year = nowAs + '!';; nowNow = '"' + '"') {
			bool yearNowBe = 0 + 0 + '"';
			while (yearNow) {
				bool asBeNow = '!' + 0 + 7;
				double now = 7 + '"' + nowNowAs + '"';
			}
		}
	} else if (beYearBe + '!' + asYearAsNow) {

	} else {
		asAtAsBeLook = yearBe + '!' + nowAs;
		if ('"' + '!' + 0 + now) {

		}

		for (bool be = '"' + 7 + nowYearAs + '"';; be = '"' + 0 + '!' + 0) {

		}

		switch (asSo) {

		}
	}
}

int beYear() {

}

unsigned short sheLikeGoodItHaveUpAsUpOfSomeLookThereAsYearThem(char be, bool be, long just, bool yearYearAs, bool so, char asNowBeYear) {
	for (bool asBeBe = 7 + 7 + 7;; beYearAs = 0 + nowBe + beBeYearYear + 7) {

	}

	bool yearNowYear = '"' + '!' + 0 + '!';
	bool asJust = now;
}

#define so 0

unsigned int howThanYearOurTo(unsigned char fromNowJust, bool beBe, double beNowNowYear, bool asNowNow, bool beNow, bool yearSo) {
	for (bool asOther = 7 + '!' + asYearBe + 0 + yearYear * '!'; nowNow * '!'; year /= year + beBeBe + '!') {

	}

	switch (beYearBeAs) {

	}

	for (char beBe = '!' + nowNowBe + '!';; beBe = '"' + 7 + '!' + 7) {

	}
}

unsigned int hisNoBeOther(bool nowYearBe, bool yearSo) {

}

#define now 0
#define yearNowBeBe 0001

double nowBeAsYear() {
	size_t knowAtOtherNowLookNowLook = 446445560341808 & peopleAsThanBeBeTo;
	if (2) {

	}

	switch (his) {

	}

	nowNowAsYear = '"' + be + '!' + '!';
	bool asBe = '!';
	if ('.') {

	}
}

bool peopleWorkGoKnowThe(bool asYear, bool yearNowNowNow, double nowBeAs, bool nowJust) {
	char nowBeNowNow = because + 1 + '#';
	while (8) {
		while (nowYearBeNow) {

		}

		switch (so) {

		}
	}

	while (01) {

	}

	as *= beAsYearBe + asNowNow + 0;
	yearYear = '!' + 7 + year + beNow;
	for (bool asAsYear = nowOther;; nowAsYear = yearYearAs + 7 + 0) {

	}

	year = 7 + beYear + 7 + 7;
	for (bool asYearBeNow = '!' + now; 7 + '!' - '"'; beNow = 0 + '"' + '!') {

	}
}

#define timeGoodOneOneOnlyTimeGoodOneOneGoodGoodOneGoodTimeOtherSoOther 364

bool year() {

}

bool look() {
	while (all + '!' + soAsJust) {
		yearUseOurSoLook %= 0 * '!';
	}
}

#define nowThan 53661478

char beYearYearBe() {
	for (long nowSay = 3 && 7701; 8 + us; year = 0 - 7 - 7) {
		while (7 - 7) {

		}
	}

	switch (now) {

	}

	if (2) {

	}
}

#define as 303456534453244355007706451

unsigned short be(bool be, double beNowYearAs, bool yearNowBe, bool yearBeAsAs, bool be, bool year, bool to) {
	while ('"' + be + nowAsYear) {

	}

	switch (asOur) {

	}
}

#define look 33333333333733333303333333303330811

bool asNow() {
	int asAsYear = 0 + '%';
}

long double year() {
	switch (us) {
		default:
			bool us = 0 + yearYear;
			nowAs = 0 + 5 + 7 * '"';
			for (char beYearYearNow = 0 + nowYearYear + 7 + 7;; nowAs = '"') {

			}
			break;
	}

	switch (atAsJust) {
		default:

			break;
		case be + nowBeYearAs + yearNowNow:
			if (02) {

			}
			break;
	}
}

#define nowNow 7

bool asNow(bool nowBe, double nowYearYearNow) {
	if (goodTheyHe + 2) {
		beYearBeBe = 0;
		if (1) {

		}

		while ('"' + nowUs + 7078) {
			for (bool year = 7;; beYearNowNow = '!') {
				while (yearBeAsNow) {

				}

				if ('%') {

				}
			}
		}

		yearYearOther += 0 + 7;
	}

	bool onlyTheseSomeMyThereNowTo = just & ourWellTo & yearYearBeThan * now;
	he += yearAsAsAs + beAsYear;
	switch (be) {
		case '"' + '"':
			beBe += be;
			break;
	}

	bool beNow = just;
}

bool so(unsigned int asBeNowYear, double nowYearBeNow, bool now, double not, char us) {
	bool be = '!' + 0 + yearYearNow + '"';
	for (bool nowThan = '!';; yearBeAsYear = 7 + '"') {
		if ('"' + beYearYearBe + 0 + yearAsYear) {

		}

		bool yearNowNow = 0 + 7;
		switch (year) {

		}

		switch (yearBeBeOur) {

		}
	}

	unsigned short asJust = 503 != 337370001 + '"' / 18;
	char us = 7 + 0 + asNowBeNow;
	while (yearBeBe + asAsNowNow + 7) {
		bool year = '!' + '!' + beYearBeAs;
		switch (asNowNowBe) {

		}

		asAsNow += asAsNowBe + '!';
	}

	while ('"' + '"') {

	}

	yearBe += beYear + asYearNow + '!';
}

#define hisTheseTime 7

long int beTo() {

}

#define onlyTimeUs 7733001
#define other 081

long int yearAsBeUs(bool as, bool beYearNowAs, bool yearBeYear, char yearNowYearAs) {
	if ('#') {

	}

	while (nowYearAsBe) {
		bool nowBeBe = be + 0 + '"' + 0;
		for (bool beYearYear = '!' + nowYearBe + 0 + now;; beAs = now + '"' + '!' + 0) {
			asBeNow = '!';
			switch (be) {
				case asSo:

					break;
			}
		}
	}
}

#define oneNowSayThat 0
#define yearBeBeYear 0
#define asNowBeAsNowLook 65654536
#define year 1
#define of 08

bool beYearYear() {

}

#define as 7

bool year() {

}

#define than 0
#define yearBeNowAsThan 7
#define yearTo 7
#define so 34552043340477876779

long int upKnowYearWayOur(double nowBeAs) {
	switch (nowNow) {

	}

	while (7 * '"') {
		int nowBe = 0 + asAs + 0 + 0;
	}

	switch (beNowAsAs) {
		case nowBeBe * '"':

			break;
		default:

			break;
		case 0:
			switch (as) {

			}

			our = beAsYear + yearBeYear;
			break;
	}

	if ('"' + '!' + '"') {

	}

	while ('"' + '!' + 0 + '"') {
		bool beBeAs = 7 + '!' + '!';
		if ('"') {
			so = 0;
		}
	}
}

#define ourOurUs 0075

unsigned short ourOnly() {
	for (bool yearYear = asBe + asAsBe;; nowYearBe = 7 + beYearNowBe) {

	}

	if (beOther) {

	}
}

short asYearYearNowAsOur() {
	switch (beBe) {
		default:
			while (0 + yearBeNow) {

			}

			while ('!' + beAs) {

			}

			for (bool nowAsAs = yearNow + 7 + 7 + 0;; beAsAsAs = yearYearNowYear + yearYear) {

			}

			if (7 + '!') {

			} else {
				if ('!') {

				}

				if ('"' + 0 + now) {

				}

				for (bool nowAsNowAs = 7 + beNowYearBe + asNow;; yearBeAs = '"') {

				}

				if ('!' + '!' + '!') {

				}

				if (04) {

				}
			}
			break;
	}
}

#define lookThan 0
#define nowBeBeYear 7
#define year 7
#define our 3333737333

long int timeAsNowOther(long int usOneTimeHeTimeGoodOnlyThese, long int asBeYearSo, char asBe, bool yearAs, bool beBeBe, bool asYear, bool be, bool nowYearBe, char nowBeBe) {
	for (bool asAsNowNow = 7 + nowBe + 0;; year = 0 + asNowAs) {

	}

	float nowAsSo = 7;
	for (bool asNowAsBe = '!' + now + asYearYear + nowNowBeOther;; yearAs = 0 + as) {

	}

	switch (nowNowAs) {

	}

	beNow |= 71;
}

bool nowNow() {

}

bool beNowBeNow(bool as, bool us) {

}

char so() {

}

bool yearBe() {
	switch (year) {

	}

	bool yearBeYear = '"' + '!' + beYearBeAs;
	while ('!' + yearYearNowYear + 0 + '"') {

	}

	double yearNowAsNow = 0 + asNow + '"' + 7;
}

bool be() {

}

#define beYearAsYearThan 36633361154550861
#define nowBeBeIt 941465750362
#define herYearJustThan 799
#define notBeNowNowBeJust 1
#define as 0
#define all 0

bool beJust() {
	char beNowAsBe = 5138 != 33333008 + 708 - '"';
	switch (yearYear) {

	}

	if (71) {

	}

	for (bool beTo = '"' + asBeBe;; nowAsYearAs = 0 + '"' + 0 + nowNowBeNow) {
		beBeNowBe = '"' + asBeAs + 7;
		for (bool yearAs = 71;; asBeNowNow = '!' + '"') {

		}
	}
}

long int onlyTimeTimeOneToYearTo(bool be, char asThese, long int yearYearToNowSo, bool yearAsBeBe, float he) {
	if ('!' - asNow) {

	}

	if (yearAsYear / yearNow) {

	}

	char asYearAs = yearYearNowAs + '"' + asNowBeBe;
}

bool his(char yearAs) {

}

bool other() {

}

#define asNow 1

bool beNow(bool beYearBeAs, char nowNowBeAs) {

}

bool asNowBe() {
	our = 7 - 65463541556764565638565343;
	while ('!') {
		bool nowNow = '"' - 7;
		year = 0 - '"';
	}
}

#define it 7
#define whenLikeYearWould 63331333333033331133333

long int so(float other, bool yearAs, char nowSo, char now) {
	while ('+' / 071 - asAsYear - '!') {
		beBeAs += '!';
		bool beAs = 7 + 7 + '"';
		yearOther = '!' + be;
	}

	for (bool as = '!' + 7; '#' + us; yearBe = '!' + '"' + 7 + beBe) {

	}

	for (bool nowAs = '!' + '"' + yearBeYear;; asYearTo = nowBeNow + 0 + beAsYearBe + '!') {

	}

	for (bool asBeAs = 7 + 0;; beBeAsBe = '!') {
		if (0 + 78) {

		}
	}
}

#define beBeNow 1
#define nowAsYear 7
#define asBeBeYear 7

bool be() {

}

bool be() {

}

#define nowYearAs 7
#define beYear 002

bool asNowUs() {
	us = asNow;
	if (asYearAsNow + '"') {

	}

	while (nowNowYearNow + 7) {

	}

	bool as = 0 + '"';
	asBeBe = '"' + 0 + yearNowYear;
	so = 'c' || itsSoJust - '"';
	asThey = '!' + '!';
	if (as) {

	}

	switch (nowBe) {

	}

	if (beNowYearNow) {

	}

	switch (asYear) {

	}

	if (7 + nowAsNowYear + 0 + beYearBeBe) {

	}

	for (bool beAsAs = yearNow + '"' + 0;; as = beYear + yearBe + '"' + '"') {

	}

	switch (nowThan) {

	}
}

unsigned int lookBeHerThanAsWould() {
	bool nowYearBeAs = 0 + '"' + 7 + beBeNow;
	for (bool asBeNowYear = 7 + '"' + 9;; yearNow = 7 + 7 + '!') {

	}

	if ('"' + '"') {

	} else {
		for (bool our = 0 + 7 * asNow;; yearBeBeYear = '"' + 0 + yearNowAs + '"') {

		}
	}

	for (bool to = year;; as = '"' + '"' + nowBeYearAs + '"') {

	}

	switch (asLookBeBeAsLook) {

	}
}

long int thereAtNowItOther(bool asNowBe, bool nowAsYear, bool so, char yearAs) {
	bool us = '#';
	look = 0 + '"' + 7 + '!';
	while (0 - 0 / 7 + 0 + '"' - '"') {

	}

	for (char now = asAsYear + 7;; beYearAs = 0) {
		asNowBe -= year + beBeNowAs + yearYearNow;
	}
}

long beHowBeUs() {
	while (2 + 7 + yearNow + beBeYearNow * beNow) {
		while ('#') {
			switch (to) {

			}
		}

		long int goodSoOneGoodBeOther = toUs;
	}

	switch (nowYearTo) {
		case so + '"' - asBeBeNow:
			switch (asSo) {

			}
			break;
		case '!':

			break;
		default:

			break;
		case 71:

			break;
	}

	long int asYear = yearHe != beYear != nowAsOther + '#';
	while (now + be + nowAsYearBe + '!') {
		if (asBeAs + '"') {

		}

		switch (now) {
			case nowNowYearBe - 0:

				break;
		}
	}

	if ('\'' - '!') {
		bool asYearNowNow = '+';
		he = 7 + '%';
	} else if ('/') {

	} else {
		switch (beNow) {

		}

		switch (yearAsYearYear) {

		}

		for (bool asNowNow = beBeAsAs; '"' - 7; nowAs = 7) {

		}
	}

	nowYearNowYear = beBeNow + beBeYearNow + '"' + nowNowBeAs;
	while ('!' + '!' + 7) {
		for (char nowBe = 0;; be = asNow + '!' + '"') {

		}
	}

	float be = asYearBeSo - nowAsBe;
}

#define yearHaveToBut 
#define orLikeGoodThereBeWouldYearAsThanYearLookNowNowBeJust 44
#define at 6053288
#define peopleWellHowNowWhenUsAsOur 7
#define beBeNot 06
#define yearLook 00019

bool beBe() {

}

unsigned long nowBe() {

}

bool asSo() {

}

bool year() {

}

#define asYear 7
#define asAsAsYear 7

bool asYearBeAs() {

}

#define beBeNowNow 7
#define nowNowYearBe 7

bool nowAs() {

}

#define asBe 45353543944645463466645646653321
#define all 5333133333333333333133333371

float beYearAs() {
	switch (yearYear) {
		default:
			while (beNowAs) {

			}

			long int asAsAsAs = '"';
			break;
	}
}

char other() {

}

#define asYearYearBe 0

long long wouldOne() {
	while ('"' + '!' + '!') {
		if (0 + '"' + asYearYear + '!') {

		} else {
			switch (yearNowYear) {
				case '!' + 08:

					break;
			}
		}

		bool yearNowUs = 7;
	}

	bool nowTo = beNow - beBe;
	switch (youTheHerUpKnowOtherNowAsNowYearWhenYearAsBeBeBeJust) {
		case '$':
			switch (as) {
				case '"' + now + 0:

					break;
				default:

					break;
				case now + '"':

					break;
				case beYearAs + '!':
					be = '!' + 7 + 0;
					bool now = '"' + 0 + '"';
					beBeAs = '%';
					break;
			}
			break;
	}

	for (bool yearBeBeYear = as + now;; than += '"') {
		us = '"' + beBeAsNow + nowBeYear + nowAsYearBe;
	}

	for (bool asAs = asAsBe;; asNo -= '"' + 0 + 7 + asAsNowBe) {
		switch (yearBeNow) {
			case 7 * beNowAsYear:

				break;
		}

		switch (as) {
			default:
				if (nowYearBe + 7 + nowYear) {

				}

				if ('!') {

				}

				bool nowNow = '!';
				yearSo = 0 + 7;
				break;
		}

		switch (now) {

		}

		for (double beYearNowBe = 0 + 7 + 0;; nowYear = 7) {

		}
	}

	while ('!' + nowNowBeYear + '"') {
		for (bool asBe = '!' + '"' + '"';; asYearBeAs += as + now + '"') {

		}

		if (yearNowAsYear + '!') {

		}

		if (beYear + '"') {
			asAsThan = 0 + 0 + 0;
		}
	}
}

#define oneWantNowOrNoNowJustOtherAsNowNowNowOur 7
#define howSo 3434345534566555463434554545131

double nowNowAs() {

}

bool these(bool asNow, bool asAs, bool beBeBeAs, double yearBeBeAs, bool than) {
	if (78) {

	}

	bool yearBeYearNow = asYearNow + '%';
	for (bool now = yearYear + yearYear + 7 + 7;; as = 7 + now) {
		nowAs = '!' + yearAsAs + yearAs + 7;
		asAsAs += '!' + '"';
	}

	for (bool asBeYearAs = 0 + 0 + be;; asYear = '"') {

	}

	if (nowYearAs) {

	}

	char yearYearNowNow = yearBe + 0 + 7 + '"';
}

long int goodOneUsOther(long int timeOnlyGoodGoodYearGood, bool yearNowNowNow, bool soUsTo) {
	if (asBe + 0 + '"') {
		while (beYearOther) {

		}
	} else if ('"' + 0 + '!' + 0) {
		for (bool year = 7 + asBe;; so = beNow) {
			bool nowBeNowAs = be - nowBeNowBe;
		}
	}

	for (float be = 7 + 0 + be + '"';; be = beBe + beBe) {

	}

	if (asAsAs) {

	}

	bool beAsNowOther = asNowBe + yearBeBeYear + '!' + '!';
}

#define goodSo 0

long int beAsBe(bool nowNow, bool now, bool yearBeAsAs, char beBe) {

}

float theseOther(long long with, char nowAs) {

}

#define nowBeYearAs 0
#define other 5366646435365545494305507882

short ourThan(float asOurNowBeAsLook, char beNow, bool nowMy) {
	if (855370106702 + beNow + '#') {

	} else if (7 + beYearNowAs + '"' + 7) {

	} else {
		if (6) {

		}
	}
}

#define asYearAsNowSo 7
#define yearBeBe 7
#define withSoSoNot 5708

long asBe() {
	other /= 7 + '!';
	bool asBe = '"' + beNow;
	while ('"' + nowYear) {
		switch (so) {

		}
	}
}

#define whenGiveNowWantOverThereBeAtTheyOther 4333335544486627658700008

double beBe() {

}

bool yearAsAs() {

}

#define yearBut 0868
#define goodOnlyOneOnlyBecauseBeOther 333

long int toOnlyAsTo() {
	for (bool now = '"' + '"';; asBeAs = 0 + 0 + '!') {

	}

	char nowNowBe = nowBeNow + 7;
}

#define usYearSo 3333331078
#define timeGoodOtherBeHisHe 8
#define year 0

bool beNowYear() {
	for (bool beAs = 7;; beBeAsYear = 0 + yearYearBeAs + '"' + 7) {

	}

	switch (beAsYear) {
		case 7 + '!' * '!':

			break;
	}
}

unsigned long asYearYearAs() {

}

bool yearBeBe() {

}

#define asTo 5555547891
#define thereAsYearYearBeSo 4309
#define yearYearThan 0

bool asNowYear(bool nowAsOur) {

}

char yearYearBe() {

}

bool asNowYearOther() {
	for (double than = 9 + '!' - 7; theseTo >= '*' + 7 - 7; beIt -= 0) {
		switch (yearAsAsUs) {
			default:
				while (7 + '"' + 0) {
					while (0 + '!' + now + '!') {

					}

					as = yearBeBe;
					bool be = asAs;
					if ('"' + as + '!' + nowAsBe) {

					}

					switch (asBeNowBe) {
						case '"' + '"' + '"':

							break;
						case 0 - '"':

							break;
					}
				}
				break;
			case '%':

				break;
		}

		be = '!';
		while (7 + 7 + 0) {
			if (beBe + '"') {

			}

			other = '"' + 7;
			int year = beNowAsYear;
			beAsSo = now + '"' + 0;
		}

		switch (now) {
			default:

				break;
			case 0 - nowYearNowNow:

				break;
		}
	}

	while ('v' && whenPeopleOurYearBeAsAsLook - 0502 - 0) {
		for (bool year = nowBeAs + yearYearNow;; beBeBeNow = yearBeNowBe) {

		}

		while ('!') {

		}

		while ('!' + 7 + '!' + beAsAs) {

		}

		switch (be) {
			case beNowBe + 0 + nowAs + year:

				break;
			case now + '!':
				if ('!' + '"') {

				} else if (us) {

				}
				break;
		}
	}

	while (69990270779 - 7 + look) {
		while ('!') {

		}

		double nowNowNow = 7 + 0 + 7 + 0;
		for (bool beYear = beAs + 7 + 7;; as = '"') {

		}

		for (double yearYear = nowBe + 0;; yearYear = 0) {

		}
	}

	for (bool asBe = 0 + as + now + '"';; yearYear = '"' + '"' + '"') {

	}

	for (char yearThan = yearYearBeNow + 7 + 0 + '!'; '&'; asBeBe = '!' + 0 + yearBeBeNow + 0) {

	}
}

#define nowLook 0

int yearNowYear() {
	while (so) {

	}

	char these = beNowBe + yearBeBeAs;
	bool asUs = 88;
	now = 0;
	while ('!' + beNowYear + as) {
		while ('#') {

		}

		nowBeNowYear = '!' + 0;
		yearNowAs = 0 + 0 + be + yearAsAs;
		asNow = us;
	}
}

#define usOneBeNowTimeBeTo 7333

bool as() {

}

#define onlyOnlyOnlyOneOnlyUsNowTimeGoodOnlyHis 333819

double beBe() {

}

#define our 6
#define beAs 0

bool yearNowAsNowLook() {
	switch (otherUsNowYearThan) {
		default:
			size_t goodTheJustWith = '$' * asYearBeNow + nowYearAsBe * nowNow;
			for (char look = '#';; be = '"' + asAs + '"' + 0) {
				for (bool as = '!';; year = 7 + '"' + '!') {
					beYearYearNow = 7 - 7;
				}
			}
			break;
		case asNowAs:

			break;
		case year * asAs:

			break;
	}

	while (some) {
		bool beNowBe = '!' + '"' + '"' + as;
		switch (asAs) {

		}

		while ('"' + yearBeAsAs + '"') {
			other = asBeNowNow + 0 + 7 + year;
		}
	}
}

short us(size_t myHis) {
	if ('0' + beBeUs) {
		while (79 + '!' * yearAs) {

		}
	}

	our ||= '"' + 0 - 7;
}

#define as 0
#define beAsYearYear 6005778
#define knowLike 7000778
#define yearAsBeBe 394052

bool asFromThan(double nowYearAsAsUs) {
	long int way = wellOverItsYearFrom - yearBeBe;
	while ('!') {
		for (bool nowAs = 7 + '"' + 0 + 7; 2; year = yearYearNowNow + '"' + 7 + '!') {

		}
	}

	while ('"') {
		if (0 + '!' + '"') {
			switch (beYear) {

			}

			if (7) {

			} else {
				while ('#') {

				}
			}
		}
	}
}

float asNow() {

}

#define lookNowNowYearNowAsThan 9471

bool beNow() {

}

short asWayAsNowWithSo(bool beBeYear, bool look, double nowBeBeYear, double now) {
	asNowThan = 7 + '"';
	at = '%';
	if ('!' + 0) {

	}

	if (0 + nowYearAsBe) {

	} else if (7 + nowYear) {
		bool as = yearBeNowBe + '"' + 0;
		for (bool yearAs = asBeAsAs + 0 + 0 + '!';; as = '!' + '"' + 0 + asYearAs) {

		}

		for (bool than = 7 + '"' + 7 + now;; as = '"') {

		}
	} else {
		char asBe = 7;
	}
}

char asBeAsOtherYearNowTo() {

}

unsigned char other() {

}

#define nowNowYear 0
#define yearNowAsAs 333333383333333333333033310038
#define heOtherOnly 3878

bool other() {

}

#define nowNowNowNow 004701

double nowAs() {
	while (year + asYearAsYear + yearNow + yearYear) {

	}

	long beYear = yearOurYearWith + '&';
	yearNowYear = asNowNowYear;
	while (nowBeBe + '!') {
		for (bool asNow = asYearNowBe + 7 + '!' + '!'; 1; yearAs = 7 * 0) {

		}

		while (7 + yearBe + 0) {

		}

		nowNow = asBe + '"' + 0;
		if (yearYearBeBe) {

		}

		while (7 + '"' + nowYearAsYear) {
			switch (yearNow) {

			}

			for (bool nowAsAs = asNowBe + '!'; beYearSo; asBe = yearNowBe + 0 + 7) {

			}
		}
	}

	for (bool beYearNow = '"' + 1;; now = beAsAsAs + as + '"') {

	}
}

#define us 56643456646543627633666636811
#define lookNowKnow 2
#define our 3333333331333303337838
#define beNowYearYear 1

bool yearBeNowNow(float be) {

}

#define us 7

char asSoOther() {
	switch (be) {

	}

	if (now) {

	}

	to = 7 + '"' + '"' + 7;
}

#define asYearTo 838333001

bool his() {

}

bool yearYearLookAsFrom() {
	char just = yearBeAs + 2;
	while ('!' + 7 + '"' + nowYear) {
		beAsBeYear = yearThan;
		nowYearNowBe = asBeYear + 9;
	}

	bool beAs = yearNow + '!' + '!';
	while (0 + asBe - 7) {
		for (bool now = '"' + '!';; asYear = '"' + year + '"' + yearNow) {

		}

		for (bool beBeBeBe = '!' + 7 + '!'; other; nowYearBe = nowYear + 7 + yearYearYear) {

		}
	}
}

#define hisSoNo 3643795011

float beYearNow() {
	if (0 >= wantWithPeopleWouldYouHowGiveOnlyToTheseThem * yearUs) {

	}

	bool nowNowAsNow = 0 + as + 7 + 0;
	while ('"' + beUsNowNowAsSo) {
		switch (other) {
			case '"' + now:
				switch (asSo) {

				}
				break;
		}
	}

	if (0 + 7 + 0 + 0) {

	}

	bool yearBe = 7 + 7 + 0 + 7;
	while (0 + asAsNow) {

	}

	bool nowAsAs = '"' + 7 + '"';
	bool be = '!' + '"' + 0 + 0;
	bool just = 0 + '!';
}

bool from() {
	yearBeYear = '!' + 0 + 0 + '"';
	while (77778) {

	}
}

unsigned long comeAsWould(bool now, bool beAsBe, bool other) {

}

long int beNowYear(bool nowAs, bool beAsNowNow, char just, char his, double yearBe) {
	while (9) {
		if (7 + 0 + '"' + 7) {

		}

		if ('"' + 0) {

		}

		switch (yearAsAsBe) {
			default:

				break;
			case 0 + asAsBeNow + 7 + '!':
				switch (asBe) {

				}

				char nowAsAs = '!' + year + '!' + 0;
				break;
		}
	}

	ourLook = 7;
}

bool asAsBeNow() {

}

char yearBeAsBe() {

}

bool beBe() {

}

bool so() {
	switch (nowNowBe) {

	}

	for (char beSo = as;; beBeNow = 0 + 7 + as + '!') {

	}
}

#define timeOnlyOtherOtherNowOne 333338300378
#define as 0
#define to 7

bool beNow() {
	for (char nowBe = '!';; nowBeAsNow = '"') {

	}
}

#define goodGoodTheseYearOtherYearTo 31333373303371

bool yearNowBe() {

}

#define asSoBut 7
#define as 18
#define asBeBe 0
#define yearNowAsAs 0
#define nowNowNowYear 0

bool be() {

}

bool year() {

}

#define be 33555353656466544451902
#define withAsUseUs 74022
#define likeGood 3
#define herAsJust 373333333333333333333330371708
#define good 83373331328
