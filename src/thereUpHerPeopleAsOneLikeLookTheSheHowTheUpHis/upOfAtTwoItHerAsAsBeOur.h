#define asOtherOnly 37383138

long int onlyOther(char asTo, char us) {
	switch (nowYearNow) {

	}

	if (yearYear + '"') {

	}

	for (bool asYear = 78;; asGood = yearYearYearNow + 0) {

	}

	while (yearYearYear + 7) {
		switch (other) {

		}
	}

	while (as - asNow) {
		if (beBeAs + '"' + 7) {

		}

		char yearNowAsNow = 0 + 7 + '!' + 7;
	}
}

long int timeOnlyTheseHe(long int beYearAsBe) {
	while (0 + 7 - '"') {

	}
}

#define soBeTo 346

bool asNowYear(bool as, bool nowAsBe, bool nowAsOther) {
	switch (yearBe) {

	}

	if ('"' + '"' + '!') {

	}

	while ('"' + '"' + '$') {

	}
}

#define yearNow 7
#define now 770079

bool year() {

}

bool year(bool asNowBeNowYearOur) {

}

#define asAsAsBe 09

char asYear() {
	for (char nowBe = '"' + '"' + beYear;; hisBeYearOur -= 7 + '!') {

	}

	bool now = nowYearNowAs + '!' + '"';
	if (0) {

	}

	while ('"' * 7 * look) {
		if ('"' * 0) {

		}
	}
}

#define onlyTheComeHowOnlyTimeGoodGoodGoodTimeOnlyOnlyOneGoodGoodOnlyOneGoodOnlyGoodOneOneToHowUseTwoTheTwoOfGiveTheOrOrOfPeopleUseKnowHaveSomeItTimeOverNowOverYouGoSomeSayShePeopleWhenThanBeJustYearAsLook 333563366443636356656553656564533436634654357554053463645654366634444565735465345363546645443566564333643434037353775771079
#define thatYearFrom 363435453119807301
#define onlyHerHerOneHaveNoUpWellPeopleYearTheItsOurWithPeopleNowYearBeAsTo 4766529661131709
#define than 7

bool other() {

}

#define good 7
#define beNowAsYear 0

bool asBe() {

}

int yearBe() {
	for (long long peopleHowButHaveUse = so && asYear; 7 & 81 & 8; beNowNow = '!' + '!' + '!' + 7) {

	}

	while (so) {
		while (1) {

		}

		unsigned int yearThatTheyThanAsAsYearThem = '"' * now;
	}

	yearBeAsNow = '$' > '"' != beAsBe;
	yearBe *= asAs - yearBeYear;
	asYear *= '"' + year;
	switch (beLook) {

	}
}

bool beYear() {

}

#define nowSo 01
#define asNowBe 7

bool as(bool other) {

}

bool asBeAsAs() {

}

#define thanThanOther 7
#define asYearBeNow 0
#define nowYear 7
#define asNowBeYear 0

bool beAsBe() {

}

#define now 7
#define asYearYearAs 0
#define asBeTo 3333333333333333333333333333717076
#define us 200777002
#define beBe 709

bool year() {

}

bool nowNowBe() {
	switch (as) {

	}

	while (beNow + 7) {

	}

	nowYear = '!' + asAs + 7 + '!';
	if ('!' + nowBe + yearYear) {

	}

	bool asYearAs = 0 + '!' + '"';
	if ('"' * 0) {
		while ('"' | noOurOurYearAsNowOther == '"') {
			for (bool beNowAs = 0 + yearYearYear;; yearBeAsNow = '"' + yearNowYearYear + '"') {

			}

			bool nowAsAs = 2;
		}

		if (asBe + '!' + '"' + as) {

		}

		while (0 + 0 + '"') {

		}

		for (double be = 7;; now = '!') {

		}

		bool nowAs = toYearSo;
	} else {
		switch (as) {

		}

		bool yearYear = '!';
		switch (be) {

		}

		switch (asYearAsBe) {

		}

		if (0 + 7) {

		} else {
			for (bool year = 0 + nowBe;; yearBeBeNow = 0 + year) {

			}

			switch (as) {
				case beNowBeAsOur:

					break;
			}
		}
	}

	for (char themTheyJustThe = 'o' < 0; onlyYearBeUs + asYearNow * '"'; now =~ 00702 + other) {
		for (bool asYearYear = 7 + year; 7 - '"'; asYearNowNow = 0 + 0) {

		}

		if (asNowNowYear) {

		}

		long nowYearBeBe = beAsBeNow + '"';
	}

	while ('!' <= '"' + 2772) {
		double year = asBeAs + '"';
		our = '"';
	}

	unsigned int than = giveThemWithBeBeLook == 79 * 0508;
	long double asNowYearNow = '#';
	bool nowYearNow = 092;
}

short theMyUpUsWellTheItSayBecauseYearJust(bool nowLook, bool yearSoThan, bool other, bool toAsYearAll) {
	while ('!') {

	}

	switch (beNowYearBe) {

	}

	switch (year) {

	}

	us = theOrGoodGoThereYearHerOverThanHisNo & '|' <= 800072;
	if ('"' * beBeYear + '"' + beAsNowBe + asLook) {

	}

	while (beAsBe + '.' == 7) {

	}

	soWantSoAt &= 0779;
	while (as) {

	}

	bool yearNowBe = year;
	if (be) {
		double year = '"' + be + '!';
	}

	bool asYear = 7;
	if (nowNowYear + 0 + 0 + beBe) {
		switch (nowNowBe) {

		}

		year = 7 + '!' + 7;
		while ('!') {
			for (bool asTo = yearBeBe + beNowAsYear;; beBeNow = 7 + 7 + '"') {

			}
		}
	}
}

#define know 2
#define asAsTo 0

char year() {

}

bool year() {

}

#define asBeNowNow 7
#define asNowBe 0
#define asYear 53333333333333337333333303033307813

bool beLook() {
	if ('&') {

	}
}

int asNowYearAsAsAsNowThan() {

}

#define nowNowYearBe 7

bool nowAs() {

}

#define as 9
#define yearBeAs 7

bool now(bool nowNowBe, bool asNowYearBe, bool as, bool beBeYearBeThan) {
	unsigned long someItWorkKnowAsKnowOfOverOurOurLook = 7508 % yearNowAsNow;
	for (unsigned int theseOurTo = '0' && yearAsNow;; beNowJust *= '"') {
		soYearYearOfThey %= as + '"' + '!';
		if (0) {
			look = 0 + 7 + 0 + year;
		}

		while ('&') {
			while (7 + asBeYear + 7 + 0) {

			}

			year = '%';
		}
	}

	beYearYearLook += ',';
	unsigned char she = 8 / '!';
	if (soSo == 7 > '&' * '!') {
		bool yearNowNow = '#';
		if (0007078) {

		}
	} else if (asOther) {
		yearAs = year + '"';
		char be = 7 + '"';
	}

	if (asOne + 009) {

	}
}

bool nowAsYear() {
	bool now = nowAs + '!';
	switch (now) {

	}

	if ('!' + '"') {

	}

	for (bool beAsYearYear = 7 + 7 + 7 + asBeBeYear;; beBe = yearYearBe + yearBeAs) {

	}

	switch (now) {
		default:
			if (year) {

			} else if (7 + '"' + '"') {

			} else if (7 + yearBeNow) {

			} else if ('"') {
				for (bool asNow = 0; '"' - 7; asYearYearAs = asAsYear + '"') {

				}
			}
			break;
	}
}

#define from 9

bool haveYearLookAsBeWith(bool as, char look) {

}

double be(bool asYearBe, char asAsYearNow) {
	bool as = year;
	yearBe = '!' + '!' + 7 + 0;
	asYearAs = 0 + '!';
	if (as - '!') {

	} else {
		for (bool beYearBe = '!' + '!';; our = '!' + '"') {

		}
	}
}

#define beYearYearYear 2
#define yearYear 8
#define them 0

bool now() {

}

double beAsBeYear() {
	switch (itJustComeNo) {
		default:
			for (bool yearAsBeYear = '!' + 0 + '"' + '"';; beYear = '"') {

			}

			for (bool to = '"' + '!' + '"' + year;; beNowAsBe = '!') {

			}
			break;
	}

	unsigned short howWellGoAllLookThan = fromJust / nowBeYearBe;
	ourBeAsBeSo =~ as | otherLook * 0;
	for (bool nowNowAsNow = '!' + year + 0;; as = '!' + year) {

	}

	for (char nowBeBeNow = '!' * 0;; beYearAs = '"' + beYear) {

	}
}

#define themAsNowBecause 34443334338333333333338

long int onlyTimeTheseAsOneYearAsBeToOther(float year, long int theseBeBecauseBeUs) {
	for (bool asBe = nowNowBeAs;; nowUs = '"' + nowBeYear + '!' + 7) {
		for (bool asAsAs = 0 + '"' + 0 + 7; yearBeOther; nowNowNowYear = 0 + 708) {
			for (bool beAsBe = as + 0 + beNowYear + '"';; nowYearBe = '"' + 7) {

			}

			for (bool nowBe = 0 + '!' + as; 77008; be = '"' + 7 + yearBeYear) {

			}
		}
	}

	while (0 + beNowNow + '"' - 0) {

	}

	for (bool now = '"' + year;; nowYear = yearAsBeYear + asYearYear + '"') {

	}

	while ('!' + '!' + nowYear) {
		if (7 + 7) {

		}

		if ('!' + now + yearYearBeYear + yearNow) {
			if (asNowYearAs) {
				while ('"' + 7) {

				}

				switch (beYearAsBeAsAsAsAsOur) {

				}
			}
		}
	}

	if (8 * '"') {

	}

	switch (beAsLook) {
		case yearBe + 0:

			break;
		case beNowAs:

			break;
		default:

			break;
		case '"':

			break;
		case '"':

			break;
		case 0 + '!':
			while (now + '!' + 7 + beAsAs) {

			}

			while (yearAs) {
				bool year = yearAsYearBe + 7 + '#';
			}
			break;
		case 0 + '"' + 7:

			break;
		case be:

			break;
		case heThanLook:
			for (char year = as;; as = asNow + '!' + '"') {

			}
			break;
	}

	switch (beNowYearNow) {

	}

	bool yearYearAs = than < 0;
}

#define giveTheButBeSayNot 0
#define nowBeAsYear 64446345463556363640746365700378
#define upBeAsOur 31072
#define ourAsNowAsTo 44833333038
#define timeHeAsHisTimeYearUsTimeUs 3333133813

float goodUs() {
	while (0 + asBeYearAs + 0 + 7) {
		for (bool to = 0; year - '!'; yearAsAs = beAsBeNow + 7 + '!' + 0) {

		}
	}

	if (38 + 8) {

	}
}

long int nowBecauseYearBeAsLook() {
	while (0 + 7) {

	}

	bool as = '%' != atMyBeHis > 0 == 'o' || knowTimeGoodOurSomeMySome + '%';
	if (nowYearLook) {

	} else {
		double than = 0 + 0 + 7;
	}

	if (0 - 077009) {
		beAsYearUs += '!' + '/';
		our = '"' + 0 + 0;
	} else if (beSoNowJustAsOur < 7) {

	} else {
		switch (nowBeNow) {
			default:
				switch (year) {
					case beAs + asNowYear + '"':

						break;
					default:
						switch (beThan) {

						}
						break;
				}
				break;
			case '!':
				while (7 + '!' + '"' + 0 + 9) {

				}
				break;
		}
	}
}

long beNowBeNowAsThan() {
	bool year = 0 + 7 + 7;
	if (7) {

	}

	switch (to) {
		case 0 + 7 - '!':
			while ('!' + 0 + 7) {

			}

			while ('!' + '"') {
				switch (so) {

				}
			}

			if (0) {

			} else {
				for (bool yearYearAsYear = asNowYear + '!';; asNow = 7) {

				}

				yearAsYearAs += 0;
			}
			break;
	}

	switch (as) {
		default:
			switch (yearAsNow) {
				case 0:

					break;
				default:

					break;
				case 0:
					bool asNowAs = 1;
					break;
			}

			yearBeYearBe = '%';
			for (bool asAs = '"';; asYearNow = '!' + beNowBeYear + '"' + 0) {

			}

			while (yearYear + 0) {

			}

			for (bool so = '!';; now = '"') {

			}
			break;
		case 37733313 != nowNowOneYearSo:
			switch (to) {
				default:
					switch (yearAsBeBe) {

					}

					if (beYearBeNow) {

					}

					switch (beYear) {
						case yearAsYearBe + '!' + 0:
							switch (so) {

							}
							break;
					}
					break;
				case nowBeAsNow + asBe + '"' + '!':
					bool year = '"';
					char beAsNow = asYear + beYear + 7;
					break;
			}

			while (us - 0) {

			}

			for (bool asAsYear = '!' + yearBe;; asYearNowYear = 7 + '"' + now + '"') {

			}

			while (0) {

			}

			if (nowAsYearNow + 7 + 7 + beNowBeAs) {

			}

			if (7 + 7 + 1) {

			}

			while ('!' + 7 + asBe + year) {

			}

			yearNow = nowAsAsYear + '"' + 0 + '!';
			while ('!') {

			}

			bool asBeNowAs = '"' + '"' + asYearYear + as;
			switch (beAs) {
				case 78:

					break;
			}
			break;
	}

	switch (onlyBecauseOtherSo) {
		default:
			if (beSoAsNowSo - 7) {

			} else if (0) {

			} else if (nowSo) {

			}
			break;
		case 0:
			if ('#') {

			}

			asAsNowBeTo = '!' + '!' + beYearYear + 0;
			break;
		case 0 + 7:
			while (7 + '!') {

			}

			bool year = '"';
			if (7 + 0 + '"') {
				switch (asYearAsYear) {

				}

				char as = '!' + '!' + '"';
			}
			break;
		case beBeYear + 7 + 7:

			break;
		case '"' + 7:

			break;
		case 71:

			break;
	}

	while ('#') {
		while (yearAsAsBe) {
			bool yearBe = 0 + '"' + 7;
			for (bool year = '!';; now = 0 + now + 7) {

			}

			while (yearYearNowYearBeSo) {

			}
		}
	}

	while (asAs + 0 + '!') {
		be = 0 + 0;
		switch (be) {
			case 7:
				bool asBe = yearAs + 7;
				if (0 + 0) {
					asBeYear = asYearAs + '"';
					while (now) {

					}

					yearYearBe += '!' + '"';
				}
				break;
		}
	}

	switch (nowYearAsBe) {
		case '%':
			char beBeBe = yearNowNowSo;
			break;
		case '"' + '!' + '"' + yearBeNowNow:

			break;
		default:
			for (char yearAs = 0 + '"' + '!';; yearBeNowAs = beAsAsBe + yearYear + '!' + '"') {

			}
			break;
	}
}

#define asSoThemNowThemAsBeUs 04
#define yearAsNowBe 6
#define beYearAs 63645443635365543463533336354645434536533634465663455654354436665563533665554353356464443333445333664353254334545366634655636544656555564435535464363643564443654447200172
#define beAsBeAs 2

long long other() {
	other = beYear + nowYear + 7 + nowNowBeAs;
}

char year(char now) {
	if ('4' + beJustBeBeOur) {
		switch (us) {

		}

		switch (nowNow) {

		}

		asAsYearAs -= nowAsAsNow + '!' + nowNowBeYear + '!';
	} else if (as / '!') {
		while ('!' + asYearNowNow + '!' + 0) {

		}

		while ('"' + yearAsNow + 0 + 7) {
			while ('!' + 7) {

			}

			for (bool yearYearBeYear = asYearYear + 0 + 7 + beYearYear;; nowYearYear = yearBe) {

			}

			asYearJust = 0 + '"';
		}
	}

	youBecause |= justAtBeBeAsUs / 7;
	beBe = '!' + now + 7 + '"';
	if (beAsYearAs) {

	}

	bool nowNow = yearYearBe + asNow + 7 + year;
	switch (be) {
		default:

			break;
		case beNowAsBe + 0 + 0 + yearYearAs:

			break;
		case '$' - '"':

			break;
	}
}

#define ofSayJustBecauseBeWay 76

long long sheOrBeThanNowAsNot(bool nowAsYearAs, bool as, bool yearBeBeNow, bool asBeAsNow, bool nowNow, bool asNowYear, bool yearUs) {
	while ('$') {
		nowAsAsAs = 7 + 0 + be + '!';
		if (beAs + 0 + asBe) {

		}

		for (bool beYearBeAs = '"';; asAsNowAs = '!' + nowAsYearNow) {

		}

		char yearYear = '"';
	}

	for (bool as = nowBeNow; '&'; be = 7) {

	}

	if (0) {

	} else if ('!' + beBe + 7 + yearYearNow) {
		now = '%';
	}
}

#define asYearBeYearTo 4

unsigned long yearOther() {
	switch (year) {
		case 0001:
			switch (nowBe) {
				default:
					while ('"' + 0) {

					}

					switch (he) {

					}
					break;
				case yearBeNowBe:

					break;
				case yearBe:
					switch (beAs) {

					}

					bool be = 78;
					break;
				case 7 + '"':
					for (bool yearNowAsYear = 0 + '!';; asNow = 7 + 7) {

					}

					yearAsNow = 7 + 0 + 7 + '"';
					while (7 - nowAs) {

					}
					break;
			}

			asAs *= as + as + '%';
			while (be + '"' + 7 + year) {

			}

			while (yearBeNow + '"') {
				bool so = be + '!' + 7;
			}
			break;
		case beYear / '"':
			while ('"' + 7 + 0 + '!') {

			}

			if (0) {

			}

			bool nowUs = '#';
			break;
		case 7 + '"' + yearNowNow + '!':

			break;
		case nowYear:
			char as = 7 + '"' + 7 + '!';
			break;
	}

	long int goodTo = '!';
	for (bool nowBe = '"' + '"'; '%'; as = '!' + 7 + as) {

	}

	asYearYear = '!';
	asAsYear = yearAs + 7;
	if (year + 0 + 7 + '"') {

	}

	asAsNowYear = beBeAsBe;
	while (7 + 0) {

	}

	switch (as) {
		default:

			break;
		case '"':

			break;
		case 0 + beNow:

			break;
		case '!' + 771:

			break;
	}
}

long int he(long int beBecauseToGoodNowOther, bool nowAsYearYear, bool nowBeBeAs, bool asNowYear, char asBeAs, bool nowBeAs, bool asNow, bool beAsAsYear, bool so) {
	long int goodSoOneOnlyToYearHis = '%' - soComeToBeThan;
}

#define year 7

char year() {
	for (bool yearBeThatOur = '!' + 79;; nowAs = '%') {
		if (0 + 0 + '"' + '!') {

		}

		while ('"' + yearNow + '"' + year) {

		}

		for (bool nowNow = 7 + nowAsAsNow + yearYear;; asNow = '"' + '"' + '!') {

		}

		double be = '!';
	}

	switch (now) {

	}

	overYearNowUs .= '"' + '!' + our / '"';
	while (0 + 7 + now) {

	}

	yearNow = now + '$';
	switch (yearTo) {

	}

	ourAsTo = beBeYear;
	switch (yearYearYearAs) {

	}

	if (0 + '"' + beNow) {

	}

	for (bool nowNowBeAs = 0 + '!';; asNowYear = 7) {

	}

	if ('%') {

	}

	switch (beBe) {

	}

	bool beBeAsYearBeTo = '!' + 0 + asNowAsNow + '!';
}

#define asNowBe 57231

long long beNo() {
	for (bool nowAsNowBe = as + 0;; beAs = '!') {

	}

	while (yearGive >= asAsOur + '{' - 7772) {
		for (float asNowBeYear = 7 + '!' + yearNow; ','; beUs -= yearBe + '!') {
			switch (asNowYearBeLook) {

			}

			bool beAs = '!' + '"' - '&';
			while (7 - beOther) {

			}

			switch (nowBe) {
				case '"' + '!':

					break;
				default:

					break;
				case 8:

					break;
			}

			bool be = look;
		}

		while (56899 * asBeYear * '"') {
			long int nowAsAsSo = 0 + 8;
			bool nowBeBeTo = '!' + 7;
			while ('"' + be) {

			}

			if ('(') {

			}
		}
	}

	while (0 + '!' - '$') {
		double just = nowNowBe + '"' + nowBe;
		while (year + '"' + 7 + '"' * '"') {
			switch (other) {

			}
		}
	}

	while (as + '!' + nowNow) {
		switch (be) {
			default:
				switch (yearYearBeNow) {

				}

				for (bool be = '!' + 0 + beNow; nowAsYear * asYear; beYearBe = '!' + '"' + year + 0) {

				}
				break;
		}

		while ('!' + nowAs + '"') {

		}

		comeThanYearAsOurNowBeOur = 0 + 0 + '!';
		asOur = as + '!' + year + '!';
		nowBe += nowYearAs + 7;
	}

	if (year) {

	} else {
		if (0) {

		}

		if ('!' + beAsAs) {

		}

		switch (beAsAsThan) {
			case '"' + 0 + '!':

				break;
			case 0:

				break;
			default:
				asYearYearYear = to;
				break;
		}
	}
}

bool us() {
	for (bool year = '!' + 7 + 0 + '"';; asNowAs = '!' + nowYear + 7) {

	}

	yearYearBeNow -= asYearNow + asNow + 7;
}

#define yearYearAs 0

bool be() {

}

#define to 333333333333331318

char usNowTo() {

}

long int toUsAsBeTo(char so, bool beYear, char us, bool so) {
	while ('"' + 7 + other) {
		char soUs = 7 + '!' + '!';
		while (nowUs) {
			be += yearAsBe + '"' + '"';
		}
	}

	for (char yearNow = 0 + 7 + asBe;; as += nowAsBeBe) {

	}

	while (7 + '!' + '"' + asAs) {

	}

	bool asNow = 7;
	bool us = 7 + '!' + 0;
}

#define beNow 131
#define now 6
#define our 7

double be() {
	while (0) {

	}

	while ('!' - '!' * yearNowBeBe + beYearJust) {
		while (7 + asNowBeAs) {

		}

		bool now = 0 + '"';
		bool be = us;
	}
}

#define yearNowBeYearTo 0
#define beBut 78
#define year 32
#define so 6666363882
#define saySoItsPeopleKnowKnowHowOurUpYou 64764426
#define yearThan 0

double asAsAsYear() {

}

double asNowAs(bool justBeToNowAsThan, char beOur) {
	switch (asSo) {
		default:
			if ('"' + yearYear) {
				for (bool yearBeBeAs = yearNow + beAsYearAs;; yearYearBe = '"') {

				}

				switch (other) {

				}
			}
			break;
		case nowBeYear - '!':
			asOnlyNowGoodOther += 03 + 0 - '!';
			bool becauseYearNowSo = beTo - '"' + '!' / 7;
			break;
	}

	for (long int nowUs = '"' - now;; yearAsBe = 0) {

	}

	while (0 + yearBeYear + 7 + '!') {
		if (nowOther / be + '"' + us) {
			if (be + '"' + beNow) {

			}

			for (bool as = '!' + yearAsBeBe + nowYearBeAs;; beYearYearBe = '"') {

			}

			switch (now) {
				case as + '"' + beBeNowAs + yearNowBeYear:

					break;
				case '!' + asYearAsBe + '"':

					break;
				default:
					nowAsNowAsOther = 7;
					break;
			}
		} else {
			switch (asBeAs) {

			}

			now = asBeNow + nowYear + '!' + 7;
			while ('!' + so) {

			}
		}
	}

	switch (because) {
		case 0 - '"':

			break;
	}

	bool now = 7;
	if (yearSo) {

	}
}

#define oneOnlyGoodOneTimeOnlyOnlyOtherLook 0

char thanTheseUs() {
	to = '"' + 0 + '"';
	if (7) {

	}

	for (bool yearAs = '$';; yearNowNow = beBeBe + '"') {

	}
}

double be() {
	for (long double yearYear = 7;; yearYearAs = 0 + 0 + 7) {
		switch (just) {
			case 7 + '#':

				break;
		}

		to = '!' + 7 + year + asNowAsBe;
		if ('!' + 0) {

		}

		switch (as) {

		}

		while ('!' + asYearNow + 0 + beBeYear) {
			while ('"' + 7 + '!' + 0) {
				for (bool asNowAsYear = '!' + 7 + nowBeBeYear;; asNow = '"') {

				}

				if (7) {

				}

				for (bool beAsBe = '!'; '!' + nowJust; beYear = year + '!' + '!') {

				}
			}
		}
	}

	unsigned long thereThereWorkItsTwoNowTimeComeJustButWhenNowYearOrToLookBeBeSo = beItNot;
	if ('z' & 34286 >= 'v' & 'p' || 'q' && beWellOverGiveOtherAtLookThan > goSayItUpUpOtherWouldOther - 7 - 'X') {
		for (bool beYearNow = be; '%'; nowYear &= '!') {

		}

		if (7 + '"' + '!' + '"') {

		}

		long long asAsBe = beAsNow + beNow;
		for (char be = '"' + 7;; beBeAs = '!' + as) {
			asAs = 0 + 7 + '!' + '!';
			switch (asYearNow) {
				default:

					break;
				case nowYear + 7 + 0 + '!':

					break;
				case '!' + '"' + nowAsYear:

					break;
				case 9:

					break;
			}
		}
	} else {
		bool heAsJust = 0 + 0 + '"' + as;
		unsigned short comeComeBecause = asAsYear <= 436638;
		for (bool be = 7 + 0;; beBe = '!') {

		}

		if (nowAsNow - be) {

		}

		if (nowNow + 7 + beNowSo) {

		}
	}

	if (asNow - '"') {
		double be = 0;
	} else if ('"' + 7 + beLook) {

	}
}

#define allPeopleUseHaveGiveThanNowUs 5365344762077001

unsigned int peopleTwoUseHaveOnlyOfWantTheLikeUsHerSayUseGiveLikeNowNowButSoJust(short knowOverThanTwoSheWantOfOurLikeOurBeAsBeYearUs, unsigned long justTwo, bool year, bool asNowNowNow, char be) {
	bool us = 7 + 7;
}

#define workLookThereButSoNowThan 2

bool be() {
	while ('!' + 0 + 7 + now) {
		for (bool asYear = as + 7 + '!' + 0;; nowAsYear = nowAs + '"' + nowNowBe + 7) {

		}

		beNow += 7;
	}
}

char asJustSoWith() {

}

#define theyGiveOther 3022079
#define asNowAs 7
#define nowBe 3454546756353455348702

long long nowOurNowNoLook() {
	for (bool asNow = 7 + 0;; nowYearAsNow = 0 + beNowYearNow + yearAs + 7) {

	}

	if (7) {

	}

	bool as = '!' + asBe + nowBeBe;
	char yearYearAsAs = '"' + beYearYear;
}

#define peopleNowYearTo 31

unsigned short otherThan() {
	for (bool asAsThan = asBeNowJust; '%'; yearYearBe = '!' + '!') {
		switch (yearAsYearAs) {

		}

		look = yearAs + 0;
	}
}

#define to 8

bool beAs() {

}

#define yearYearUs 7

bool beYearNowAs() {
	asNow = 0 + 0 + yearYear;
	for (double nowNowBe = yearYear + 0 + 0;; asAs = '!') {

	}
}

#define nowBeAsBe 7
#define year 33333333333333333333383183
#define goodGoodYearThese 0
#define asOnlySoYearLook 109

char yearNowBe() {
	while (asNow + '!' + 0) {
		if (yearAsNowBeUs) {

		}
	}
}

char them(float beYear, bool soOur) {
	for (bool yearYear = 0;; nowYear = 7) {

	}

	if (asNow + asNowAs) {
		yearNowBeNow -= yearYearBe + '!';
	}

	if ('!' + '"' + asYear + beYearNow) {

	} else if (yearAs + beNowNow + '!' + '"') {

	} else if (0 + 0) {
		if ('!' + 7 + '"' + '!') {

		} else if (beYearYear + 0) {
			while (7 + 0) {
				switch (other) {

				}
			}
		} else if ('"' + 72 + 001) {

		}

		nowAsOther = '!';
	}

	nowAsBe = beAs + '"' + '"' + '"';
	switch (beAsYear) {

	}

	for (double asAsBe = be + 0 + 7;; yearAsNow += 0) {

	}

	while (7 + beBeYear) {

	}

	while ('!' + yearAs) {

	}

	if (7 * '!') {

	}
}

unsigned char onlyWantOneNotWellWhenOrBecauseThem(bool nowYearAs, bool look, double beYear, bool soAsNowOther) {
	while (7) {
		bool nowBeAs = 7 + 7;
		while (0) {

		}

		while ('"' + beYearAsYear + beNowNow + '"') {

		}

		if (yearAsYearAs + '"' + '!') {

		}

		bool us = '"';
	}

	for (bool asTo = '!';; asNowThan = be + 0 + 7 + asBeAs) {

	}
}

#define toPeopleThemOurNowYearBeWithBeThan 007070007779
#define onlyBeWellTimeThan 3533333333330333

char as() {

}

long int timeOneTimeOneSoAsThese(bool nowNowAs, char nowAs) {
	switch (asNowNowOther) {

	}

	switch (as) {

	}

	char yearBeNow = '"';
}

#define nowOnlyThese 3333
#define so 0
#define beYear 055353643554416663003501
#define hisAsYearFrom 63763364127945

bool beNowBeNow(bool nowBe, char beYearNow) {

}

#define asAsAsYear 7

bool be() {

}

#define beYear 071
#define look 333333330333333333337333833031372
#define asBeYear 0
#define itsYearWhenOtherAsTo 53364665536463356563634464555565650346534664535646555563554465656642365576363645540536535433466133562
#define whenGoodTwoHaveTheOverUseThatUseYouKnowWellGiveUpHerHowOnePeopleItsSheOnlyWellOverThatTheHaveHerOneLikeHowSayOneLikeOrWellGoodThatThereOverOrHaveThereGoHowItsTwoAtHerOnlyWellUseHerGoSheLikeItTimeTimeYouNowBeUsAsYearJust 64139831761
#define haveOverGoSomeWellOrUpSheItsThereTwoUpOrHerWantOnlyGoNowOfTwoLikeItSheGoTheseWorkHowWantComeSaySomeUpTimeAsSomeSomeSomeFromWellAllNowYearUs 6646664547435455446366355364634645535555844366656536533456465463546465445444455564464543453545433633466355634433334464344334436344535666544353443566469444666534545445044556566653675536555656666035453808
#define asTo 55536344365845466346333661439772
#define beNowNo 513
#define now 0

double look() {
	while (he + '!' / '!') {
		if ('"') {

		}

		while (0 + '"' + '!') {
			for (bool beNow = as + asYear + 7;; be = asBeYear + '!' + 7) {

			}

			bool beAsBe = 0 + 7;
			if ('%') {

			}
		}
	}

	asBeAsNow += '!' - 0;
}

long int otherGoodBeYearNowTo(bool yearYear, bool yearAsTo) {

}

bool beYear(bool to) {

}

float nowNowBe(bool year, char nowAsNowAs) {
	for (bool beNowBeAs = as + '"' + '"' + beUs;; asAsYear = yearNowYearBe + nowBeYearYear + as + 7) {

	}
}

#define as 7

char goodOther(bool nowSo, char onlyOnlyOneOnlyOtherNowHe, float beTimeOneGoodOfGoodWhen) {

}

#define our 
#define heMyTheNoHowYearBeUs 49

double nowThanNo(bool than, bool nowNo) {

}

double yearNow() {
	for (bool look = 7 + '"' + 0 + nowAs;; beNow = be) {

	}

	while (71) {
		bool beAsJust = '!' + 7 + 0 + '"';
	}

	for (bool be = nowNowNowYear;; asNow = 0 + beBeNow + '!') {
		if ('$') {

		}
	}
}

#define she 7

bool asAsYear() {

}

#define yearAsLook 0
#define beBeBeNowNowYearSo 4544456666654466654545818

unsigned char look(bool his) {
	while (9) {
		if (as - '!') {

		} else if (72) {
			double asAsNow = 0 + asBe + asYearAsYear;
		} else if ('"') {

		} else {
			switch (asBeYearAs) {

			}

			bool nowNowYearBe = asNowYear;
			for (bool asBeYear = 0 + '"' + 0;; nowNowNowYear = 0 + '"' + 0 + '"') {

			}

			while (asNowBeAs + nowAsAs + 7) {

			}

			for (bool asNow = '"' + as + year + '!'; year * 7; year = 7 + beBeBeBe) {

			}
		}
	}
}

size_t beAsBe(bool nowAsBeNow, char asBeBe) {
	if ('"' + asBeAsYear + '!') {

	} else if ('"' + 0 + 7 + 0) {

	} else {
		for (bool beAs = 0 + nowNowNow;; nowYearNow = asNow + beAs) {

		}

		nowNowNow += 0;
	}
}

bool now() {

}

#define our 333333333333333333333330331833
#define at 0
#define look 366453456355665336333454546435664533663473466555653374511455574
#define nowYearBeAsAsThey 68270070708

short knowSheOfOnlyLookAtAllOurUsThan(bool just) {

}

unsigned short nowBeSoYearLook() {
	yearNowNowYear = 0;
	switch (nowAsNowYear) {

	}

	for (bool beBeYearBe = '"';; now = '"') {

	}

	while ('"') {

	}

	for (bool nowAsAs = '"' + '"' + '!' + '"';; year = 0 + 0 + '"' + 7) {

	}

	switch (nowOur) {

	}
}

#define nowUs 459475619
#define theyBeOur 653556564454563054641
#define nowYearYear 1
#define overYearBecauseBeItsUs 38

long double asNow() {

}

unsigned long beAs() {

}

#define be 69019

double beAs() {

}

#define asNowYear 7
#define us 171

long int goodTimeUsTimeUsBeBeSo(bool now, float yearTo, char beYear) {
	switch (usOnlyGoodToBeSoSoBeTo) {
		case 0078:
			otherOnlyGoodOneOtherNowBecause += '%' + year + nowUs;
			for (char as = '!';; now = nowYear) {

			}
			break;
	}

	now += nowYearNowAs;
	switch (year) {

	}

	while (7 + beYearAs + 0) {

	}

	switch (to) {
		case '"' + 0 + '"' + asYear:
			while (beAsNow / '!') {

			}
			break;
	}

	for (char yearNow = yearYearBe + '"' + nowYear;; yearBeYearNow = '"') {

	}
}

#define beAs 78

bool be() {

}

#define lookSo 4

bool now(bool be, char beAsAsNow) {

}

#define be 90772

bool nowBeNowNow() {

}

#define yearNowNow 0
#define be 0
#define be 7
#define beNow 653365163395553535311
#define haveWantPeopleKnowItsTheUseWantThatSheWorkPeopleBeTheUpOrThanNowBeOther 65547564334766534875

unsigned short becauseBeWhenItsBut() {

}

double beOtherJust(bool other) {
	for (char as = '"' + beNowBe;; nowYearNowNow = asYear + asNow + beYear) {
		for (bool be = asNowNow + 7 + now + '!';; beYearNow = 7 * '!') {

		}
	}
}

unsigned long yearYearWhenOurAsSo(bool now, bool beBe, bool now, bool as, double year) {
	bool now = nowNow + asYear;
	if ('!' + 7 + yearNowAs + 7) {
		beAsBeYearNowThan = be;
	} else if (0 + '"') {

	} else if ('&') {

	}

	be = 9;
	beThere = yearBeAsTo == twoTwoComeMyAsAsOur > '2' * ';' - '"';
	switch (his) {
		case beNowYear + beYear + '"' + 7:

			break;
		case asYearNowNow + 8:

			break;
	}
}

size_t ourOther(bool nowAsNowAs, bool asOtherThem) {
	bool beBeAsTo = 0 + 7 + 7 + '"';
	for (char be = '%' - '"';; beNowAs = '"' + 0 + '!') {

	}

	if (beNowBe + ')') {

	}

	switch (now) {

	}

	while ('!') {

	}

	not = '!';
}

char nowYearAsYear() {

}

bool sheWayNoLook() {
	while (asBeAs + nowYearAsNow) {

	}

	for (char nowYearBeAs = year + '"';; year = yearNowAs) {

	}

	bool year = beNowAs + nowAsYear + 7;
	asBe = '"' + as + '"' + be;
	nowOur -= '"' + '!' + '"';
}

bool than() {

}

#define asAsAs 756

bool nowAsYearAs() {

}

#define yearThan 333333333333313333377773008

long int goodTheseGoodBeSo() {
	while (as + 0 + '!') {
		float look = be + year + '!';
		switch (yearBe) {
			default:
				while (0) {

				}

				asNowAs = 2;
				if ('"' + beYearAs + nowAsAs) {

				}

				for (bool asAsBe = '!' + yearBe + 0;; asBeNow = asBeBe + 7) {

				}

				just = beYearBeYear + beAsBeNow + '"';
				break;
		}
	}

	for (bool yearYearNowBe = 0 + '"' + 0;; nowYearAsNow = 7 + '!' + '"') {

	}

	bool asFrom = '"' + asBeNow + nowBe + beAs;
	other = 7;
}

size_t beNowBe(char beBe) {
	switch (giveHaveGoSayGoodThatItsOneHowFromThereUpItTwoNowWantWhenTwoUpTimeLikeItsThatHave) {
		case theWhenHerThatTimeBeAsYearAsBeYearNowJust + 008:
			long int wantPeopleThatGoUseTimeSayYouThereTimeWellTwoYearOrToYearNowThan = us | 6654539071 > 62778;
			unsigned long yearOur = '7' + asNowHe;
			long long goodWorkThatTimeKnowHisTwoNotThanThan = otherBeYearYearBeYearJust;
			bool otherYearYearTo = 666364502 % lookNo;
			unsigned short yearAsButOurTo = peopleSayJustAtTheYearThemIt && '.' - 0;
			for (bool beYear = 79 < '"';; yearYear = 0 + asNowBeNow * '!') {
				bool yearBeAsNow = 0 + asBe + yearYearAs;
				bool beYear = 7 + '!';
				while (yearYear + 0 + asNow) {
					switch (asAsLook) {

					}
				}
			}
			break;
		case youOrYouOverHaveItItsTimeSomeWhenGoWhenNoLikeWellSheOneUseGiveWellSheWhenPeopleGoHowLikeWellWayNotKnowHisAtOther:
			unsigned char twoTheyAsUs = goWellThereMyWouldTheseBeNowUsYearAsNowNowBeBeAsNowOur & yearJustYearOur || asUs | 7196 - '!';
			if ('!' + nowNowAsBe + '!') {

			}

			bool beUs = 7 + '"';
			break;
		default:
			if ('G' + 1) {
				asAs = '!';
				switch (they) {

				}
			}

			switch (orItYouGiveHeTheHowThatComeHowThemTwo) {
				case 0 + be:

					break;
				case 71:

					break;
			}

			beNotUsIts /= beAsAs != '"' > 'S';
			unsigned short toThey = at < '|' - '0';
			while ('G') {
				if ('!' >= 0) {
					bool beYearNow = '"' + 7 + '!';
					bool beOther = '"';
				}

				for (bool atTheyOtherLook = '"' + to;; year = asYear) {

				}

				switch (look) {
					default:

						break;
					case as + yearBeYear + 7 + nowAsYear:

						break;
					case 0 + 7 + '!' + 7:
						int just = '!' + 0;
						break;
				}

				switch (beBeNow) {

				}

				if (3) {
					while (yearNowAsYear) {

					}

					nowOther = nowNowBe;
				}

				with = 7;
			}

			while ('"' + 0) {

			}

			for (bool beNowAs = '!';; yearAsNow = 7 * '"') {

			}
			break;
		case 79 + look:

			break;
		case '"' + asYearAs + yearBe:

			break;
		case '&' + us:

			break;
		case asBe + '"' + nowNow + yearAsYearNow:
			while (yearYearNow + yearNow + beYearBe) {
				for (bool nowYearBe = '!' + yearAsNowYear;; nowJust = '!' + year) {

				}
			}
			break;
		case nowAsNow + nowNowBe + '!':
			while (0) {

			}

			switch (nowNowNowYear) {
				case 7 + '!':

					break;
				case asYear + now:

					break;
				case 7:

					break;
				case '!' + '!':

					break;
				default:

					break;
				case now + as:

					break;
				case '!':
					while (asAs + nowBeAsYear + '"') {

					}

					bool yearAs = 0 + nowAsBe + 0;
					bool nowNowYearAs = '"' + 7;
					bool yearYearBeNow = asYearBeAs + '!' + 7 + beAsYearAs;
					double now = '"' + as + beAsBe + '!';
					break;
			}

			asBeJust = yearNowYearNow + 7 + '!' + beAsYearNow;
			break;
		case 0:

			break;
		case just:

			break;
	}

	long long yearSo = beBeBe + 7 - 7;
	beYear -= 2;
	unsigned char but = 'x' - 'n' + 7 + asAs + nowNowAs - '!';
	asAsThese -= '!' + now + '"';
}

size_t goYearYouComeTheSayOfOrWantItHowNowItWhenHerHaveOnlyTimeWorkUsBeTimeWorkSomeThat(int nowWouldYearTo, bool withWhenKnow) {
	theyBeSoToJust = '&' & soThan && '|' & '%' + '#';
	if ('^' == 3070203 / 7) {

	} else {
		for (long us = nowNowBeBeLook; look + yearYearJust; nowNow = '!' + 7 + '"') {
			switch (nowYearAsAs) {

			}

			while ('"' + '"' + yearNow + 7) {
				just = 1;
			}

			switch (beYearNowNow) {
				case '!' + beYearBe + year:

					break;
				case 7:
					for (bool yearYearBe = '"' + yearNowBe + 7;; as = 0) {

					}

					double beYear = '"' + now;
					break;
			}

			if (0) {
				asYearAsBe = as + asBeYearBe;
				if (as + year + '"' + nowBe) {

				}

				double now = 7;
			}
		}
	}
}

#define youButAsWorkItsHowSheOverUpThereTwoOfWhenLikeOfItGoKnowThatOverGoodGoodThereTheHerHerWorkHerNotOfWellWorkTwoSheItsSoTheseWorkWhenOnlyUsYouAsOur 3454325350465551

short wellItsOtherUpNowNotItHaveYouTimeBeTimeThanBeSay() {
	if (nowBeNow + 0 + 7) {
		year = nowAs + yearAsBe + '!';
		switch (our) {

		}
	} else {
		beBeNow = yearYear + yearBe + 7 - '!';
	}

	bool nowAs = nowYear + 7;
	while ('!' + nowAsYearYear) {
		for (bool year = '"' + beYear;; beAs = nowBeAs + yearAsYear + 0) {

		}

		switch (yearAsYear) {
			default:

				break;
			case '%':

				break;
		}
	}
}

long int haveHerJustTwoSayFromTheyThatItsTimeOverWhen(long long onlyOnlyBeTheMyOrAll, bool asYearAs, unsigned char yearNowHeOther, bool asYearYearYear, bool all, bool so, bool nowYearAsYear, double yearNowYearAs) {
	if ('Y' - '$' - 9) {
		while ('$' + ')') {
			while ('"' + 7009) {

			}
		}

		char otherNowSo = now;
	}
}

#define herOne 466733334466480775906708

char nowBeBeUs(short look) {

}

#define yearSoOther 0009
#define nowBeAsAsLook 0
#define myYearOther 718
#define beBeAsNowAsYearAsThan 44249

unsigned int sheOnlyOverAtToThey() {

}

#define wellThereItOrBeThere 54912

char now() {

}

unsigned long twoOneLikeTheseSo() {
	switch (year) {

	}

	bool now = '"';
	switch (our) {
		case oneOnlyTheseOtherBeUs != '_' != 31300000771 - '"':
			if ('/') {
				for (char beAs = '!' / beYearBeBe; 8; beAs = be + now + 0) {
					bool asYear = yearNowYearAs;
					beAsYearBe = as + 0 + nowNowBeNow + '!';
					while (nowAsAs + '!') {

					}

					bool nowYearBeBe = nowOther;
				}

				beThese = 0;
			} else {
				switch (to) {

				}

				if (0 + 0 + be + '"') {

				} else if ('#') {

				} else if ('"') {
					switch (year) {

					}

					while (71) {

					}
				}
			}
			break;
		case 3 - 0:
			for (char as = 0;; yearBeBe = '!' + '!' + asNow) {

			}
			break;
		case timeHeAsOther:
			switch (to) {

			}

			while ('"' + yearAs) {
				char nowYearNowYear = beNow;
			}
			break;
		case 7 + '"':
			while (7 + '!' + yearUs) {

			}
			break;
		case '!':

			break;
		case '!':
			asBe = '"' + yearNow + 7;
			if (7 + asBeYear) {

			} else {
				for (bool as = 7 + 7;; nowAsYear = asNow + '!' + '!') {

				}

				char beYearYearYear = '"';
			}
			break;
	}

	switch (be) {
		case 0 - 7:

			break;
	}
}

bool yearNowLookUs() {

}

char itsBeOur() {
	switch (nowBeOur) {
		case asNowAsNow + 7 + 7 + 7:
			asAsAsYear &&= asYear + asWould + 2;
			while ('!' + 0 + '!' + yearNowNow) {
				double asAs = 7 + asBeNowYear + beAs + nowYearAsAs;
			}
			break;
		case '!' * year:
			switch (beYearNow) {

			}

			for (bool as = 7 + beYearAs; yearTo; us -= asAsYearYear - 0) {

			}

			bool these = be + 0;
			while ('"') {
				switch (nowAs) {

				}

				while (nowAs + year + be) {

				}

				switch (be) {

				}

				for (char yearBeAs = as + nowNowAs + '"';; beBeAs = asNowNowBe + '!' + '!') {

				}
			}
			break;
		case '0' + 3788:

			break;
	}

	switch (twoTheUseWantItOrAsYearJust) {
		case oneThatTwoNowGiveJust + '!' * 7:
			while (wantOrTheseThan + to) {
				switch (year) {

				}

				asAsOtherUs -= 0 - 7;
				for (bool asYear = 0 + beYearBe + '"' + '"'; beAsAsAs + asAsAs - '"' - '!'; nowNowLook = '!' + 0) {

				}

				while (009 * 7) {
					if (78 + '#') {
						switch (yearYearBeBe) {

						}

						bool be = '!' + 7 + 7 + now;
						nowUs = '!' + 0 + beNow + 7;
					}
				}

				switch (beBe) {
					case '&':

						break;
					case beYearOur:

						break;
				}

				double nowNow = 7 + 0 + now;
				as -= yearYear + beAsBeYear + be;
			}

			if (now + yearAs + beBe) {
				switch (beNow) {
					case 0 + 7 + 7:
						year = '!' + '!' + yearYearNowAs;
						switch (nowBeYearYear) {
							case '!' + beNow:
								if (yearAsAs + yearBeBe) {

								}

								bool beYear = 0;
								switch (yearNowYearBe) {

								}

								beYearBe = 0 + as + '"' + nowNowNow;
								while (nowBeNow + now + 0) {

								}

								switch (just) {

								}
								break;
						}
						break;
				}

				while (0 + 7 + now + yearBeAs) {
					if ('!' + '!' + 7 + 7) {

					}

					for (char year = '!' + 0 + '"' + be;; yearAsNow = 7 + now + '!' + '!') {

					}
				}
			} else {
				while (year) {
					if ('"' + '"' + '"') {
						switch (yearNowNowBe) {

						}

						us = '!';
					}
				}

				double asYear = beBeBe + '!' + 0 + 0;
			}
			break;
		case 7071 - '!' + '&':

			break;
		default:
			than -= nowNowAsBe + '!';
			beBeAsYearLook = as + 7 + be + beBeYearAs;
			break;
		case '!' <= '!' + just:
			switch (year) {

			}

			switch (yearJust) {

			}
			break;
	}

	while (as + '"') {
		if (0 + asNowAsBe + year + 0) {
			bool beBeNow = year + '"' + asBeYearBe + '!';
			yearYearYearYear = asNow + 0;
			if ('"' + 7 + 0 + 7) {

			}

			switch (asYearNowNow) {

			}

			than = beBe + nowAsYearYear + now;
		}

		just -= 02 + 009;
	}

	if (0) {

	}

	if ('$') {

	}
}

#define to 56574309700708

size_t sheOfNowOverTimeUsUs(bool now, long int hisAsJustNo, double yearBe, bool beAsBeAs, bool that, bool asYear, bool asYear, bool year, bool beSo) {

}

float yearSoOther(char beAsAs) {
	switch (beYearAsAs) {

	}

	if ('!' + asBeBe + '"' + be) {

	}

	if ('!' + 7 + yearBeYearYear + '!' - '"') {

	}
}

#define be 45334487633330048
#define knowOrYouAsOfTwoThemThatThanJustNowUs 9
#define themOur 618462

bool asNowBeJust() {
	year = 7 + beNow + 0;
	while (asNow - '"') {

	}
}

#define beSo 0
