#define timeOneToTimeBeHeHe 03037001

long int asYearBe(float year, bool otherBeUs, long int yearNowGoodToYearOtherTo, bool as, char asAsBe) {
	while ('+' != asYearYear) {
		if (7 - beBe) {
			bool beAsNowNow = 0 + '!' + nowAs;
			bool asAsYearBe = 7 + 0 + '"' + 7;
			beYear = '!' + as + 7 + 7;
			for (bool now = yearNowBe + 0 + nowYearNowAs; '!' - 0; year = '"' + '!' + 0 + 7) {

			}
		} else if (7 + 0) {

		} else {
			if ('+') {
				bool us = 0 + 0 + asNow + 7;
			}

			yearAsAsYear = '"' + 071 / 0 - be;
			if (beBe + beAsBe + '"') {

			} else {
				switch (nowNowOther) {

				}
			}
		}

		now = '"' + 0 + '"';
		for (bool yearNowNow = us;; year = yearBeNow + '"' + nowAs) {
			if ('!' + us) {

			}
		}

		switch (beYear) {

		}

		as = 0 + beBeNowYear + nowAsYearYear;
		switch (yearNowNow) {
			default:

				break;
			case year:

				break;
			case yearNowNow + nowNow + asYearYear:
				yearNow = yearAsNow + '!';
				while (0 + '"' + 7) {

				}

				for (bool asNow = asAs + 0;; now = '"' + 0) {

				}

				for (bool nowNowBeAs = asNow; 7 + '%'; yearAs = 0 + asYearYear + 0 + now) {

				}
				break;
		}
	}

	year = so;
	for (char beBeAs = '!' + year;; beNowNow = '!' + beBe) {

	}
}

#define yearNowYear 0

long int his(bool say) {
	switch (be) {

	}

	for (bool nowAsYearAs = 7 + as + '"';; nowBe = 0 + as + asNowYear + '!') {

	}

	for (bool year = yearBeYearYear + 0 + '"'; 0 + yearBe - '!'; asYearYear = '!') {
		for (bool beAs = '"' + 0 + 0;; asNow = '!' + 0) {
			if ('"') {

			} else {
				switch (nowYearNow) {

				}

				bool asYearNowYear = be + yearBeYearYear + nowYear + 0;
				if (7 + '!' + 7) {

				}

				switch (asBe) {
					default:

						break;
					case nowYearAsNow + '"' + '!' + '"':

						break;
					case yearAs + 7 + 7:
						for (bool nowYearNow = '"' + nowAs + '!';; year = 7 + '"' + 0) {

						}

						while (yearNowBeBe * 7) {

						}
						break;
				}
			}
		}

		thereKnowLikeTo = '!';
		if (as * yearUs) {
			bool nowYear = 0 + 0 + us;
			while (71 + '!' - '"') {

			}
		}

		switch (yearBeNowYearLook) {
			default:
				while (beBe + '!' + just) {

				}

				switch (other) {

				}
				break;
		}
	}

	to <<= 8511;
	bool nowAsYearYearAsThan = 708 * 7;
	for (long double look = as + 75; 8; yearBeYearYear = '!') {

	}

	if (thanAsOtherAsYearYearYearUs * as) {
		while ('%') {

		}

		while ('"') {
			than = asNowYearNow + yearBeAsAs;
			for (char nowYearBe = 0 + '!';; beYear = '"' + nowBe + 7 + 0) {

			}

			switch (asYearYear) {
				default:

					break;
				case '"' + 7 + 7 + 7:
					bool asYearBe = 7 + 7;
					asNow = 7 + 0 + 0 + '!';
					if (9) {

					}
					break;
			}
		}
	} else if (be) {

	} else if ('"' + now + '!') {

	} else if ('!' - beAs) {

	}

	for (bool asYear = '!' + 7 + '&';; asBeNowNow = 7) {
		while (7 + 0) {

		}

		for (bool asYear = '"' + '"' + '!' + '"';; yearYearNow = beAsYear) {

		}

		while (nowNowBe + nowBeNowBeAsBeBeSo) {

		}
	}

	now = 0 + '"';
	if (',') {

	}
}

#define howTwoThatItUs 5333333333333333330337373333731737330109

bool now() {

}

#define asAsNowOur 43526073
#define orOurTo 20879
#define beNowUs 08872
#define beUsJust 4743477771
#define comeBeWhenNowBecauseYearSo 330729

bool toAsTo(bool beNowAsYear, bool asAs, bool asNowAsYear, bool asNowBeNow, bool asBeOther) {

}

unsigned int beBeWith(bool to, bool asNow, bool look, bool nowBeNowAs, bool nowNowNowAsYearOur, bool year, bool other) {
	for (bool nowSo = beYearYear + nowAsAsAs + '"' + beAsYearYear;; be = be + '!') {
		if (1) {

		}
	}
}

#define peopleOneNowNowAsOur 4146061

long int at() {

}

#define yearNowBeAs 7
#define asOtherJustYearBeBecause 0

unsigned int asAsBeBe() {

}

char be() {
	for (bool year = '!' + year + '"'; '!' != 0 - '!'; nowAsBeAs = '#') {
		switch (asBeBe) {
			default:
				while (year) {
					char as = '!' + 0;
				}
				break;
		}

		switch (other) {

		}
	}

	switch (soBecauseToAsSo) {
		default:
			while (0 + asAsBeBe + 7) {

			}

			if ('!' + asAsNow + now + 7) {

			}

			while (only - 7) {
				switch (to) {

				}
			}
			break;
		case 1:
			for (bool now = now + beBeBe + beOther;; beNow = 0 + year) {

			}

			while (0 + 0 + '"') {
				while ('%') {

				}
			}

			if (0 + '!' + 0 + 7) {
				for (bool asBeYearBe = asBeYearYear + '"' + '!';; yearAsTo = '!' + 0 + year) {

				}

				for (bool yearYearYearNow = '!' + 0;; asYearBe = beAs) {

				}

				nowYear = yearBeBeBe - yearYear;
			}
			break;
		case 8:
			switch (usHeUsAsUsYearOther) {
				case yearAs + '!':
					year += asAsAsYear + '!';
					break;
			}

			bool yearBeBe = '!' + nowNowBe + 7 + 7;
			if (7 + 0 + '"') {

			} else if (7 - 7) {

			}
			break;
		case otherUs / 0 - 0:

			break;
		case 311 + nowNowNowHe:
			nowAsNow = nowAsYearUs;
			break;
		case 7 + 0 + 7 + yearBeAsNow:
			asNowAsAs = 7;
			if (0 + yearYearAs + '"' + '"') {
				for (bool nowBeBeYear = now;; nowBeNow = '"' + yearBeYearAs + 7) {

				}

				bool nowBeYearYear = 7 + '"';
				asYearNowNow = 0;
				char be = 7;
			}
			break;
	}

	switch (goodToHeOtherOther) {
		default:
			if (beBeAsNow + he) {
				if (yearBeBeNow - nowNowYear) {

				}
			}
			break;
	}

	switch (yearUsTo) {

	}
}

bool yearAsAs(char asYearAsNow, bool nowBeYearYear, bool nowNow, char beBeBeSo) {

}

bool asNowUs(char to) {
	beBeYearOur = '!' + orWell % 08 || '"' * 7;
	for (double nowAsBe = yearYearAs + yearBeNowAs;; beAsBe = '"' + asYearAsYear) {

	}

	float yearNowAsBe = 7 > '%' + '"' - 0;
	our += asYearNow + nowBeBe - '"';
	for (char yearBe = beYearAs;; us = year + as + 0) {

	}

	now = year + nowAsNow + nowAsNow;
	for (bool nowBe = '"' + '!' + year;; asNow = '"' + 0) {

	}

	while (0 + 0 + asBeYearAs) {
		if (asNowNowBe + '!') {

		}

		so = '"';
	}
}

long double yearNowOur(double asAs) {
	double beAsYear = 7;
	nowYearLook >>= beAsBe + 7;
}

#define asOrBeTwoThan 3747654315052
#define asBeYearAs 7
#define yearAsBeBe 7
#define look 001

char yearAs() {

}

#define yearBeYearBe 5333

long int hisGoodSoNowGoodGoodHisTheseBeBeTo(char goodBeOther, long int so, float nowOther, char asBeYear) {
	while (yearNowNow + asNow + nowAs + '"') {

	}

	nowSo += 'O' - timeTimeOnly - 0;
	while (8) {

	}

	bool so = 0 + '"';
	while (nowOther) {
		yearNow = '!';
		for (bool asNowAsYear = '"' + '"';; beAsNow = '"') {

		}

		asBeBeYear = yearTo;
	}

	switch (nowNowAs) {

	}

	switch (yearBe) {
		case 0:
			switch (year) {

			}

			switch (yearNowOther) {

			}
			break;
	}
}

#define yearNowNowNow 7
#define yearBeYearYear 2
#define asAs 7
#define asNowBe 073
#define yearYearAsAs 7
#define be 7

bool year() {

}

#define nowNowBe 5446454444434646164696438

bool yearOurSoLook(bool asNow, bool asBeTo) {

}

#define yearBeNowYear 41
#define otherLook 936769

bool asYearYearNow() {

}

bool nowBeBe() {
	while (asNowYearAs + as + 7) {

	}

	bool nowBeBe = 7;
	while ('"' - 7) {
		toTo = 7 + 7;
	}

	nowAsBeAs = 808 - 7;
	while (asBeTo + '#') {
		if (beYear + '"' + 7 + nowNowBe) {
			while (71) {

			}
		}
	}

	long int usTimeTimeBecauseSoNowToSo = 7 / 0;
	while (0 - '\'') {

	}

	while (3) {

	}
}

#define becauseOtherBeBecause 0
#define onlyHis 333103108
#define asNow 7

float usBecauseGoodOtherAsAsSo() {

}

char nowNowOtherYearThan() {
	yearBeAs = '"' + asYear + year;
	if (to) {
		unsigned long to = look + yearAsAs * now;
		if (7 + 0 + 0 + 0) {
			for (bool yearYearYear = 7 + beBeYearNow + yearNowNow + '"';; asNowNow = beBeNowBe + 0) {

			}

			yearAs -= '"' + beNowYear + 7 + 0;
		} else if ('.') {
			if ('!' + be + 7) {

			} else if ('"') {

			} else {
				us = 0 + nowYearNowBe + asYear + 0;
			}
		} else {
			while (0) {

			}

			while (to) {

			}
		}
	} else {
		int as = asYear - yearBe;
		for (long asNow = 7;; yearYearBeNow = beNow + asBeYear + asAsNowAs + '"') {

		}

		switch (butYearLook) {
			case 0 - yearAsAsYear:

				break;
		}

		while (beNow + 7) {
			while ('!' + 7 + beBeAsNow + 7) {
				if (0 + 0) {

				}

				while ('"' + as + 7 + now) {

				}

				while ('"' + beBeNowBe + '!') {

				}

				bool nowYearYear = 7 + 7 + 7;
				while ('!') {
					while (yearNowYearAs - '!') {

					}
				}
			}
		}
	}

	for (bool beBe = 0 + 7 + 7;; nowYearAsAs = asBeNow + be) {
		yearNowAs += asBeAsAs + yearNowBe + 7;
	}

	for (bool yearYearYearUs = '!' + 0; '$'; yearNowAsBe = 7 + as) {

	}
}

unsigned long timeOneYearYearAsNowOtherSoThan(double noOther, bool nowNowAsYear, bool beNow, char beNow, char asYearBeUs) {
	switch (other) {
		case 0 + 7 + 7:

			break;
		case 875:

			break;
	}

	if ('!' + '"') {
		while ('!') {
			switch (just) {

			}
		}
	}

	asNoUs = year + '"' + 0;
}

unsigned int nowAsNowYear(long int beTheThan) {

}

#define now 7
#define be 0

short asAs() {
	long int nowOtherNowGoodOneYearTheseGoodGoodHeAsUs = '/' != '!';
	bool onlyAsOtherYearOtherUs = so;
	us = '!';
	switch (nowAs) {
		default:
			switch (nowBeBe) {

			}

			for (bool asAsNow = '"';; now = now) {

			}

			while (asYear + nowAs) {

			}

			while (asNowBeNowBeSo) {

			}
			break;
	}

	while ('#') {

	}

	while (beNow != 0) {
		if ('"') {

		}

		char year = 7 + '"' + '!';
	}
}

#define nowSoHisOther 0

bool only(bool asTo) {

}

#define goodUsYearBeYearOther 33

bool yearNowNowAs() {

}

#define asAsBe 334566544443336063136532579
#define thanComeHe 5278
#define otherWithOtherYearAsBeBeNowYearAsNowSo 5343
#define year 7
#define beAs 33733333333333033333333333331081711
#define yearTo 0

long int usFrom(bool our) {

}

#define yearNowNow 7

int asYearNowBe() {

}

#define nowBeAs 8
#define now 48533330494204
#define whenAsTwoHerHowItsTimeUsBeWith 9682079
#define oneBecause 82

bool nowBeBeYearOther(size_t nowAsYear, bool beNow, bool look) {
	switch (beAsUs) {

	}

	for (bool asBeBe = 7;; yearBeAs = '!') {

	}

	while ('!' + '"') {
		double year = 7;
	}
}

#define be 7
#define year 7

bool beAs() {
	switch (yearYearAsThan) {

	}

	beAsBeAs = now + 0 + '"' + 7;
	switch (be) {

	}

	if ('&') {

	}
}

#define yearTo 7
#define be 7
#define be 53317171
#define timeGoodGoodOnlyTheseOneSoToGoodYearOneTimeBecauseGoodYearBecauseAsGoodNowYearToAsTo 33738037071

long int yearLookTo() {

}

bool nowAsNow() {

}

#define asYear 0

char year() {
	long asYearYearAs = asBe + 7 + 7 + yearAsBeBe;
}

bool beNowBeYear() {

}

#define yearNowNowNow 01

double asYearAsAs() {
	as = nowNowNow + '"';
	while (0 % '"') {

	}

	switch (asNow) {
		case '"' + 7 + 0 + asYear:

			break;
		default:
			switch (nowYearYear) {

			}

			switch (us) {

			}

			switch (yearYearBeAs) {
				case 7 + 0 + nowNow:

					break;
				default:

					break;
				case beThan:

					break;
			}
			break;
	}

	than = '!' + '"' + 0 + 7;
	beYearYearAsBeUs = '"' + '"' + yearAsAs;
}

bool beNow(bool asAsAsBe, short asFromUp, bool other, long long asAs, bool nowBe, bool yearUs) {
	while (8) {
		for (bool nowAsNow = beBe + '"';; now = '"' + 7) {

		}

		for (bool nowYear = 7 + beBeYearBe + '!' + '"'; 2; yearYearYear = '!') {

		}
	}

	switch (yearAs) {
		case 777071:

			break;
	}
}

#define yearTo 1753570004
#define yearThese 664537654
#define toGoodGoodNowHe 7

long double year() {

}

#define from 7
#define asNowBe 33333333313137173871

long int oneNowOnly(float yearOther, bool as, bool as, bool beUs) {
	if (asYear + 0) {

	} else {
		if ('!' + '!' - beYearAs) {

		}
	}
}

long int timeOneBecauseUs(float just, bool yearYearYearAs, long int have) {
	while (7 * 7) {
		short soTheyThanAsAsToAsNowThan = 8402 * 7 * beNowYearNow;
		bool nowNowUs = nowAsYearYear + '"' + as + 0;
	}

	if (7 * nowNowYearAs) {
		while ('"') {
			bool asYearYearBe = beAsAs + 7 + asBeNowNow;
			bool asThan = 0 + '"' + nowNowYear;
		}
	} else if (asAsYearAs + '"' + 78) {

	} else if ('"' + 7 + 7 + '"') {
		for (char now = beYearYearYear + 0 + 7 + '"';; now = 0 + beYearAsNow) {

		}
	} else {
		switch (yearAsYearJust) {

		}

		switch (yearYear) {

		}

		for (bool nowAsNow = '!'; 0 - be; yearBeNowAs = '"' + beAsAs + '!' + 7) {

		}

		double beAsAsBe = '!' + '!';
	}

	for (double asNowYear = '"' + as + 0;; us = 2 / lookBeOther) {
		switch (beAsYear) {

		}

		if (now + 7 + '"') {

		}

		bool asYear = now;
		for (bool nowYearAsBe = 7;; be += beYearYear + nowNowNow + nowNow + '"') {

		}
	}
}

long int yearBeWhenBecauseBeAsAsThanYearThan(unsigned int nowWorkJustLikeAsHow, bool yearAsOther) {
	if (some != soJust < '%' * nowNow) {
		if ('!' + '!' + '"') {

		} else {
			switch (so) {

			}
		}

		while (yearNow + '"' + '"' + beAs) {

		}

		double year = to * '"';
	} else if ('"' - asNow) {
		us = '"' + '!' + '"' + 0;
	} else {
		if (beAs) {

		} else {
			bool just = '!' + be + yearYear + '!';
			switch (so) {

			}
		}

		switch (beYear) {
			default:
				while (nowYearBe) {

				}

				while ('"' + now) {
					nowAsNow = year;
					asYear = '!' + 7;
					while (beNow + yearUs) {

					}
				}
				break;
		}

		nowYear = as + '"';
		for (double asAsYear = 7 + '!' + 0 + '!';; beBeYearYear = 0 + '"' + 7) {

		}
	}

	bool now = asNowYear + 7 + 7;
	bool beBeYearYear = asNowBeNow + yearYearBeAs + 0 + '!';
	our = '%';
}

#define haveComeThatTheComeHowGoLikeThan 36163674566556625372

bool lookYearBeJust() {

}

bool nowNowNowNow() {

}

bool now(bool nowOther) {

}

#define beAsAsAs 0
#define nowYearNowAs 34656466443335434560534625306778

float theNowNowSo() {
	switch (asBeYear) {

	}

	if (asNowNowAs + '!') {

	}

	while (now + yearAsYear + 7 + beBeYear) {
		switch (yearYearAsYear) {

		}

		bool year = beOther;
	}
}

long long nowAsNow() {
	if ('!' + 7 + '!' + yearAs) {

	}

	for (bool beNow = '!' + '"' + 0 + 7;; now = 7) {

	}

	if (0 + 7 + '!') {

	}

	for (bool nowBeAsYear = as + '!';; to = 0 + '"' + year) {
		if (7 - '!' - nowNowYearNow) {

		}

		while (0) {

		}

		so = 7 + yearBe;
		asYear += '"' + '"' + '"' + yearYearAsBe;
		for (bool nowYearAs = '"' + '!' + yearAsNow + '"'; '"' + 1; to = beYearNowBe + 7) {

		}
	}

	for (bool nowNowNow = '"';; yearBeBeAs = 7 + year + 0 + 7) {

	}

	time = nowBeBeYear + as + beNow + 7;
}

long int oneTimeTimeOnlyYearSoThese(bool asBeNowAs, float he, long int asOther) {
	for (char beAs = 0 + asBe + 7 + 7;; nowNow = '!' + 0 + asYearNowAs + beAs) {

	}

	beOneTimeYearYearNowHeBeSo = asYear;
	nowNowNowBe |= 0 + us;
	for (bool yearYearYearNowSo = as; onlySoBeNowNowUs; asAs = so) {
		while (be + 0 + nowYear) {
			switch (beYearNow) {

			}

			for (bool yearYearYearNow = '"';; nowAs = '"') {

			}

			char as = '!' + '!';
		}
	}
}

#define asAsTo 08
#define now 9

char yearAsYearJustSo() {

}

bool beYear(bool asYearNowNow, bool asNowNowAsAsNowYearUs) {
	people =~ 0 + '"' + be + '!';
	switch (to) {
		default:
			if ('"' + beYearBeAs) {

			}

			switch (yearOur) {

			}
			break;
	}

	if ('!') {
		switch (nowAsYear) {

		}

		while ('!' + '"' + 7 + as) {

		}

		bool asBe = as + asBe + year;
		for (bool now = nowAsBeNow + 0 + '"';; year = yearBeBe + '!') {

		}

		beYearOther = '!';
	}

	double nowAsBeNow = '!' + beBeYear + nowAsBe + 7;
}

#define one 098

size_t no() {
	while (8 + 00178) {
		to += asYearYear * '!';
		for (bool asBeYearBe = '"' + beNowBeYear + 7 + beAsYear;; now = 7 + asAs + 7 + 7) {

		}

		if (soBeThan) {

		}
	}

	switch (yearBeHe) {
		default:
			while ('!' + asYearOther) {
				int nowBeAsOther = '!' + asYear;
				beJust = beYearAsAs;
			}

			wouldYouLook <<= 'V' / beYearAs;
			for (bool asBeNow = now + 7;; asBe = 0 + as + 0 + 7) {
				while (7 - 0) {

				}

				year += 7 + 7;
			}

			while ('"' + 0 + '!') {
				switch (yearNowNow) {
					default:

						break;
					case nowYearYear + '"' + '!':

						break;
					case '$':

						break;
				}
			}
			break;
		case '"' + '"':
			while (801) {

			}

			for (bool asNowAsBe = '"'; 08 - yearBeAsAs; beYear = '"' + 7) {

			}
			break;
		case nowAsYearBeBeThan + '#':
			switch (someUseTheyHis) {
				default:
					switch (our) {

					}

					if (asNowAs + '"' + '"' + '!') {

					}

					us = 7 + 0 + '"' + 0;
					break;
				case 02:

					break;
				case '"' + be + nowYear:

					break;
				case 78:

					break;
			}

			while (theseYearNowSo) {
				bool just = yearNowAs + beAsBe;
				switch (beBe) {

				}

				switch (as) {

				}

				switch (yearYearBeYear) {

				}

				nowBe += '!' + 0 + 0;
			}
			break;
		case '!' + '"':

			break;
		case '!' + 1:

			break;
	}

	while (of * asBeAsNow) {

	}
}

char be() {
	while ('\'' != 'O' + soNowBecauseSo) {
		switch (time) {

		}

		switch (asAs) {
			default:
				while (yearAs / '"') {
					switch (yearYearAs) {

					}

					for (bool asYearYear = 0 - as;; as = '!') {

					}
				}
				break;
			case 0 + nowBeAs + be:

				break;
			case '"' + 0 + 7:

				break;
			case '"':
				if ('!' + 7 + 0) {

				}

				if ('"') {

				}

				while (nowBe + 1) {

				}
				break;
		}
	}

	float nowBeYearNow = '"' + asAsAsYear + year;
	beNowYear = yearBeAs + nowBeAsYear;
	while (beSoTo) {
		while (yearBeNow + 0) {

		}

		bool asAsBeYear = 8;
	}
}

float nowNow(bool asOther) {
	bool now = '%';
	while (0 + '"' + '"' + 7) {
		for (bool beAsAs = yearYear;; as = '"' + '"') {
			switch (asAs) {

			}

			switch (yearAs) {

			}

			for (bool asNowAsBe = 0 + nowBeNow;; nowNowYear = yearBe + '!' + 0 + 7) {

			}

			asBe = 7 + 7 + '!' + 7;
			while ('"') {
				for (bool year = asAsAs + '!'; '!' - 7; beAs = 0 + beAsAsYear) {

				}
			}
		}
	}

	switch (beAs) {

	}

	while (now + 0) {
		while ('#') {

		}
	}

	if ('"' + '"') {

	}

	for (bool nowYearUs = '"' + 0 + asYearNowNow + 0;; yearAsAsNow = '!' + 7 + asNowYear + '"') {

	}
}

#define other 333870308777071

long int so(char nowAsNow, bool to) {
	if (otherYearBeNowSo - 0) {

	}

	bool nowBeAsNowUs = '"';
	for (bool yearBeYear = year + 0 + '!';; beAs = '"') {
		while (7) {
			asAsBeSo = as + '"' + as;
		}
	}
}

#define asBeBe 7717778
#define nowYearNowHe 01
#define one 0

double beNowAsYear() {
	while (7 * 0) {
		if (nowNowBeBe + 01) {

		}
	}
}

unsigned char justTwoOneHavePeopleOnlyLookOneHisAll(float nowUpOtherToTo, double beAs) {
	for (bool notTheseNowOurYearJustAsAsNowOur = asBut; '!' + beYearYear + '"' + beYearAs + 9; asBeBeNow = 0 + 7) {
		if (yearYearBe + as + 0) {

		} else {
			bool asBeJust = '"' + nowAsYear + 0 + beAsYearBe;
		}

		for (bool beYear = '"'; 08; nowNowYear = beNow + 0 + as + '!') {

		}
	}

	while ('!' + 7 + '"') {
		if (7 + 0 + asYearNow + nowBe) {
			switch (beAsBe) {

			}

			if (be + '"') {

			} else {
				yearAs -= 0 + year;
			}
		}
	}
}

bool us() {
	for (double yearNowNow = be; beAsNow + '"' + 0 * yearAs; asBeBeYear = '!' + beBeNow + 0 + 0) {

	}

	switch (because) {
		default:
			yearBeNowAs = nowNowAs + 7;
			for (double asBe = 0 + as + 0 + nowBe;; nowNowYear = 0 + '"' + 0 + 7) {

			}
			break;
	}
}

#define notBeTo 579
#define toOur 338333333033333333333333373311
#define yearUs 2
#define knowAsSo 5
#define ourOur 0

bool beBeYear() {

}

char yearBeBeBe() {
	while (66633346335636336644665564635633653365043554656344536534054346454434556446653533556434453546434436364563434635663464565353463463545343344306544434566443544354464644335544044354965738 || '~' >= workGiveGoodNoWithNowYearWith || herWantOneGoodHowWhenUseHerKnowTheUpLook || 0341357337267002) {
		switch (orItsSomeItsWhenThePeopleOneHaveOrSomeHerWhenItsHowOneItsGoOrThereLookGiveThatHerWhenNowLookTwoThatAtComeOtherOther) {
			case 772 + '*' + 07070072:

				break;
			case 8 | asNowBe + yearWouldThan:
				for (bool asNow = yearSo + otherTo; '!' * '!'; asAs = '!' + '"' + 0 + 7) {

				}
				break;
			case 72:

				break;
			case now + 0:

				break;
			default:
				now = beNowAsNow + be + '!';
				for (double yearAsNow = '!';; beNowNowYear = '"') {

				}
				break;
			case 0 + 7:
				switch (nowNowYear) {

				}

				while (7 + yearAsAs) {

				}

				bool not = beNowYearNow;
				break;
			case 7:

				break;
			case '#' + 72:
				switch (to) {

				}
				break;
		}

		bool nowYearOther = asBeBe;
		while (now + 7) {

		}

		if ('!' + nowBeAsBe + '"' + '"') {

		}

		for (bool nowAs = '"' + 0 + 7 + be;; be = our) {

		}

		nowNow *= '"' + nowYearBeNow + asBeBe + 0;
	}

	size_t giveOverNowComeTwoAsHaveThanToNowTheseOurSay = myGoodWorkGoOnlyTimeTheUpItTwoItYouGoUsYearLook && 'y' > 'x' >= 336534333465663456363434535634365546333443364244644755 >= 5303700026 && other + '&';
	bool us = nowAsYearAs + '"' + '"' + 0;
	while (7 + '!') {

	}

	switch (just) {

	}
}

#define timeTwoHowWantHaveOrPeoplePeopleTheSheSheGoWhenSayUseUseHowSayYouGoThereHowJustNowPeopleTheWay 465468366064495635439
#define nowYearAsOurBeSo 69447802720700079
#define heMyYouOurNowJustThan 6591770033

short useBeNotWithNowYearAsLook() {
	if (beBeAs + now + 7 + beBeYearYear) {

	}

	char beBe = 7 + asBeAs + 0;
}

bool yearBeAsYearBeUs(bool beBeBeYear, bool yearNowNow, bool asYearNowBeUs) {

}

long beYearAs() {
	switch (sayWorkTwoYearHeBeYearLook) {
		default:
			for (unsigned long yearNow = '"' + '!'; asAsYearYear - '('; beBeBe = '!' + 0) {

			}
			break;
		case 7 + 0:

			break;
		case '#':

			break;
	}

	switch (youOnlyWayGoodTheOnlyOurNowSomeKnow) {

	}

	while (beAsThan + 7708) {
		double nowAsNowBe = 0;
		if (7 + '"' + '!') {
			while (us) {

			}
		}

		if ('"' + '"' + 7 + '!') {

		}

		bool as = '"' + nowBe + 7;
		char now = 7 + '!' + '"';
	}

	switch (herJustNowUs) {

	}

	for (char yearAsNow = 7 + '"';; asNow = 0) {

	}

	switch (asBe) {

	}

	bool beBeYearNowAsAsOther = '!' + '!' + '!' + beBeYear;
	for (bool yearNow = nowBeBeYear + 0;; nowNowBe = 0 + 7 + '"') {

	}

	if (0 + 0 + nowNowYear) {

	}

	asNowYear = 0;
	long double look = asBeAsNow + asAsBe + yearAsBe;
}

unsigned char beNowBeBe() {
	while (asYearBeAs - '!') {

	}

	if ('!' + 7 + '!') {

	}

	for (bool as = '"';; asYearNow = yearNowBe + '"' + asAsNow + as) {

	}

	switch (nowYearNow) {

	}

	while (7) {

	}

	while ('!') {

	}

	while ('"' + asYearBeNow) {
		bool asBeYear = '"';
		switch (beYearYearYearUs) {

		}
	}
}

#define as 0
#define nowYearNow 33333333333333333303303783030701
#define oneBecauseAsNowAsAsSo 7
#define yearYearYear 31

bool say(double usOur, bool yearBeBeYear, bool yearOur) {

}

bool nowAsAs() {

}

bool year() {

}

bool year() {

}

#define nowNow 7

bool look() {
	switch (yearNow) {
		case 'H' % 3:
			for (double nowAsBe = beAsYearAs + 0;; nowAsNow = beAsBeYear + yearAsAsYear + yearBeBeYear) {

			}

			switch (us) {
				case '!' + '!' + 7:

					break;
				case '!' + asYear + beBeAs:

					break;
				case 0 + asYearYear + '!':

					break;
				default:
					asAsBeNow -= '"' + year + 7;
					break;
			}
			break;
		default:

			break;
		case beSo + '#':
			char nowAs = '!' + '"' + yearYearNow;
			bool be = yearYear + '!';
			switch (nowYearAt) {
				default:

					break;
				case us:

					break;
			}
			break;
		case yearHisBeBeOur * beBe:
			if (',') {

			}
			break;
	}

	for (bool year = asBeBeYear + '"';; now += now + as + 0) {

	}

	for (unsigned short nowAsBeAs = 4309 * be; 6; yearLook = nowAsNowNowBeBeNowNowAsOther) {

	}

	otherUsOurOur = '"';
	for (long double nowYear = '!' + '"' + 0; 4; yearBe = year + 0) {
		nowAsYearBe = year + '"' + 7;
		bool just = 7 + '"';
	}

	switch (asNowNowJustYearUs) {

	}
}

char yearAs() {

}

bool beAsAsNow() {

}

unsigned short yearJustAsAsThan() {

}

bool now() {

}

#define look 363366563546336343666532465644563333333333333338333333333333806

short yearJustBeSo() {

}

bool nowJust() {

}

#define beBeNowBe 7
#define nowAs 5455566541345663534644436456564656554345355663355633454434445633643436363555443333266543534434370762
#define ofSayHowGoJustLikeThereGiveLikeThanTheOverSayHerButAtHerSayJustAsOurYearSo 7
#define sayNowWant 56435634535343533345574534434646453863467302

bool giveItsOfSomeUpGiveLikeKnowItWantKnowItsHaveUsTheyGiveOfThatGoThatTimeBeHaveYearTo(char yearBe, int just, bool knowAllNo) {
	orYearNowThanOur *= 71;
	for (bool nowNowBeNow = look; 'Y' * 7 + 9; asYearYearNowNowLook = asYearYear + as + nowAsNow + 7) {

	}

	as = 708;
	while ('!' + 7) {
		bool asNowAs = '$';
		if ('"' + '&') {

		} else if (as + beNowAs) {

		} else {
			if (0 + 7 + '!' + 0) {

			}

			for (bool beYearBe = 0 + '"' + '!';; nowYear = asBe + '"') {

			}

			now = '!' + 7 + yearBeNowAs;
			for (bool beNow = 0 + '"' + year; be - 0; nowYear = '"' + 0 + '"' + now) {

			}
		}
	}
}

long int beBe() {

}

bool asBeYear() {

}

char beNow() {

}

#define be 7
#define asOur 2353345
#define peopleHowNowWouldJust 419771459048
#define nowAs 3078
#define his 062
#define wantUs 2362060002

bool yearYouLookThese() {

}

bool beYearAsBe() {

}

#define nowBeNowYear 0

bool asBeYear() {

}

#define to 0

bool yearAsYear() {

}

#define year 7

char nowBeYearYear() {
	nowAsBeYear = '"' + nowNowBeYear + 0;
	if ('"' + 0 + 7 + 0) {
		bool beNowSo = '!' + yearYearAs + beBeNow;
	}
}

long int onlyTimeOnlyOneOnlyGoodTimeHeOtherHisOneOtherGood(bool asBe, char usAsYearNowYearNowNowUs, bool yearYearNow, bool beBeBe, char time, bool to) {
	for (bool nowToHis = '!' / 78; 0 - '"'; asAsAs = 7 + nowAsAs + yearAs) {

	}

	switch (beBe) {

	}

	for (bool beSo = 7 + year + yearYearBeYear;; yearAsNowNow = as + 7) {

	}

	while (beNow + now + 7) {

	}

	for (bool asAsNowBe = '"'; '!' - 0; beNow = 7) {

	}
}

bool be() {

}

#define asAsAsAs 4769

bool be() {

}

char yearNowYearBe() {

}

int asNowNowAs() {

}

bool year(bool as, bool year, bool as, char now) {

}

#define asYear 7
#define beBeBeYear 7
#define nowAsYear 7
#define be 63434353443445636443346343345633455566644655454365554446456663354363435646543664444553454564466663563556555566663464336654434635635466446346663644333333335655566466433545456336445346545344535566544433433555465463663465545264546604

float wellOur() {
	year = '!' + yearYearNow + yearYear;
	while (than + 7 - '!') {

	}
}

#define asBeYearYear 506552

long double beOtherOur() {

}

#define year 8

char asAsBeNow() {
	for (bool nowYearYearBe = be + '#';; yearNow = 7) {

	}
}

bool asYearBe() {

}

#define yearYearNow 7
#define yearAsYear 471

bool nowAsBeBe() {

}

double nowYear(bool beBeOur) {
	for (unsigned char useNowNowToAsAsThan = 75709 + '>' && '"' / '"'; no + '"' - beNowNowNow; onlyOneWouldTheThanBeBeJust &&= asNow >= 7) {
		while (78 * '#') {

		}

		if (0 + be + '"' + be) {

		}

		if (look) {

		}

		nowTo += 7 + be;
	}

	switch (yearYearYearYear) {

	}

	if ('"' + '"' + '"') {

	}

	float asNowYearBe = 0 + yearNowAsNow + '"';
	nowAs -= '!';
	while (0 + '"' + '"') {
		for (bool asNowNow = 7 + 7;; as = '"' + yearBeYearAs + 0) {

		}

		now = 7 + 7 + 0;
		for (bool beNowBe = 7;; nowBe = beBeYearYear + 0) {

		}

		for (bool asBe = '"' + nowAs;; nowAsAs = nowYearAs + yearBe) {

		}

		asNowAs = now;
		bool as = 8;
	}

	for (bool year = 7 + yearNowAsNow;; yearYear = 7) {

	}

	for (char yearYearAs = 7 + 0 + nowBeNow;; as = '!' + '"' + 7 + nowNow) {

	}
}

char be() {
	char nowYearNowNow = '"' + '"' + '!';
	for (bool beAs = be + '!';; yearBeAsBe = '"' + 0 + 7) {

	}

	switch (just) {

	}
}

#define nowSoJust 513

double asBeAs(bool be, char asAsYear) {
	if ('!' + asYearOther) {
		for (bool be = 0 + yearBeYearNow + 0;; as = nowNow + '!' + 7 + yearYearNow) {

		}

		if (year / '!') {
			while (7 - 0) {

			}
		}

		switch (toTo) {
			default:

				break;
			case 7 + 7:

				break;
			case '!' + 0:

				break;
			case '"':
				if ('"' + nowNowBe + '"' + '"') {

				} else {
					if (yearAsBeYear + '!') {
						while (so) {

						}
					}
				}
				break;
		}

		if ('"' + 0 + 0 + '!') {

		}

		because += beNow + 0;
		switch (beBe) {
			case '!' - yearNowAsBe:

				break;
			default:

				break;
			case 7 + '"':

				break;
			case yearYearNow + '!':

				break;
			case '%':

				break;
		}
	}

	while (yearNow + '"' + 0 + as) {
		while (us) {
			while (0 - '!') {

			}
		}
	}

	onlyTo = '"' + '"' + beNow;
}

long int he(char beTo, long int usUsToOne, long int time, char beNowYearYear) {

}

#define oneBecauseNowYearOtherBeHe 333440074
#define than 7

bool beYearYearBe() {

}

#define asBeNow 0
#define yearNowNow 7
#define asAsBe 1
#define nowBeBe 7

double year() {
	for (unsigned long hisNowOur = be + nowAsNowAs + '"' + 7; 'y' != 'b' >= 364436665446566644435454603435902 > 468496940931 == '!' + yearBeYearAsUs + 0 % '"'; be -= 0 + 7 + '"' + be) {
		switch (wellNowHerAllHeSo) {
			case '"' + '"' + '!':

				break;
			default:
				for (bool asBeNowBe = now + asNow + '!' + 0;; be = '"' + asYear + 7) {

				}

				while ('!' + asBe + 0 + nowNowYear) {

				}

				switch (beBeYear) {

				}

				if (7 + 0 + '!') {

				} else {
					while (7) {
						bool be = nowBe + 0 + year;
						char nowNowAs = 0;
					}
				}
				break;
			case beNowNowBe + yearAsBe + 7:

				break;
			case 0:

				break;
			case asBe + 0 * '!':

				break;
			case '!' + asAs + yearNowNowYear:

				break;
			case asYearYearNow + 7 + 7:
				if (0 + nowAsYearYear) {

				}

				bool nowYearNowBe = '"';
				for (char year = asNowNow + beAs + asAsBeYear + 0;; now = '"' + yearYear + 0 + '!') {

				}
				break;
		}
	}

	asNowYear <<= 7 % '!';
	switch (lookHowBeUpHaveKnowHowItWellUsUsYearAsMy) {
		default:

			break;
		case yearAsAs + '!' + 0 + '"':
			now += 0 + '"' + 7 + 7;
			break;
		case '(':
			for (bool yearNowBeAs = '!' + asAsYearBe + 7;; be = nowAsNow + beBeYearYear + 7 + nowYearBeYear) {

			}

			switch (beBeOther) {
				case 7 + 9:
					bool asNow = 7;
					while (0 + 7) {

					}

					for (bool be = '"';; be = 7) {
						if ('!' + yearBeAsYear + '"' + '!') {

						}

						bool as = 78;
					}
					break;
			}
			break;
		case 7 + '"' + yearAsAsYear:

			break;
		case asYearAsYear + 7 + asNowBe:

			break;
		case 7 - nowYearYear:

			break;
	}

	switch (asNowYearYear) {
		default:

			break;
		case '"' + yearBeAsNow:
			nowYearAs = 0 + asBeAsBe;
			if (look) {

			}
			break;
	}

	if (',' * yearYear) {

	} else if (7) {

	} else {
		if (so) {

		}
	}

	for (bool yearUs = '"' + '!';; nowBe = 0) {

	}

	if ('!' + '"' + 7) {

	}

	while (71) {

	}
}

#define asNowOur 7018

double asBeYearYear() {

}

#define be 0
#define asSo 664354844543625
#define beBe 6404
#define us 9
#define herHowUpOrNowGoYearNowOur 34611880708

bool nowBe() {

}

bool nowNow(bool look) {
	timeNowUsOneOther |= '"' - 0 != '%' - asBeNow + '#';
	while (so) {
		beBeBeTo = yearYearBe + asYearNowYear + 0;
		for (bool nowAs = 0 + be + '"';; as = asAsNowBe + 0) {

		}

		for (bool yearYearBe = '"' + '"' + beNowBe; '%'; beNowYearNow = '!' + '!' + '!' + 0) {

		}
	}

	bool onlyOther = '"' + 0 + 7 + asNowYear;
	bool goodBeAsOneNowGood = '"' - '"' - asYearYear;
	while (0 + 08 / '\'' + 7 - '!') {

	}

	for (bool nowAs = 7 + 0 + '"' + yearAs;; to += '#') {

	}

	while (0 + 7 + 0 + 7) {
		while ('"') {
			while ('!') {

			}

			if (yearYear + 0) {

			}

			bool beNow = beNowAs + nowBeYear;
			bool nowAsAsNow = 7 - 7;
		}

		float year = as;
	}
}

float beAsBe(bool yearBeNowBe, char nowSo, bool yearYearSo) {

}

#define other 65

bool beAs() {

}

#define asBe 09
#define ourLook 5455636433354553636636634456664355646663365356564433546565663446444543644653345364465656564435353546463343564653536746330444556560554535533555864435554855844840407078

unsigned long haveHerYouLikeOrSayHaveGiveTheOneTwoTimeTwoWorkAsLikeKnowSheYouKnowOfOverThanWellGoodTwoItOverSomeGoItUpItsTwoYouOnlyThatWorkSayWantThatOverWellUpOneButHaveNowItsHowTwoUpComeJustWellBeNowLikeHave(long long our, bool lookUs, bool beBeAs, bool asUs) {
	for (bool beNowNowBe = now; '"' * 'y'; asYearYear = 0) {
		bool asAsNow = 0 + '"' + '!';
		for (bool yearUs = 7;; yearNowBe = be + 7) {

		}
	}

	as = year + '"';
	unsigned short asTheseBeThanThan = 9 * '!' && 2 - '"';
	if (yearYearBe + 2) {

	}
}

unsigned char (unsigned char oneUpGiveComeLikeMyAsYearNoWouldAsSo, float beYearNowBe, char yearYearAsNow) {
	for (unsigned char justOfOur = nowBe + because; 7 < asNowNow; yearYear = '!' + 0) {

	}

	asThan /= 79301;
	while (7 * 0 + 1) {

	}

	as = 0 + us;
	asBeNowNow = yearAsAs;
	be = year * '"';
}

#define likeComeComeUpOnlyOneGiveKnowOnlyItsWhenOnlyThatUpKnowOfGoodGiveItsNowTimeKnowWay 46635475465536565374536575498

bool toOrUpGoTo(char yearNowAs) {
	while (usSo) {

	}
}

unsigned int wellTwoTheseUs(long double howThatLikeWouldWorkGoBeTimeUsYearNowAllAsThan, bool usUs, double beAsBe) {
	for (bool now = 7;; nowAs += beYearNowYear + '"' + '"' + yearBeAs) {

	}

	if ('&' - 7) {

	}

	switch (yearAsNowBe) {

	}

	while ('!' + '"' + 7 + be) {
		while (beNowBeYear * 7) {

		}
	}

	while ('"' + yearNowAs) {
		for (bool asAsYear = yearAsYear + beNowYearNow;; nowBeYearYear = '"' + beAsAs + yearYearBe) {

		}

		if (yearAsNowBe + '"') {

		}

		for (bool beNow = nowYearBe + '!';; nowNowAs = 0 + 0) {

		}

		for (bool yearAsAsNow = '"';; nowYearYearNow = now) {

		}

		for (bool yearAsAsNow = 0 + 0 + 7 + '!';; now = '"' + 0 + 0) {

		}

		year = 7;
		for (bool yearBeNowYear = 0 + 7 + 7;; nowNow = 0) {

		}

		switch (as) {
			default:
				if (7) {

				}

				for (bool beNow = 7 + nowAsNow;; yearAsNowYear = '"' + yearYearAsAs + nowAsAs) {

				}

				while ('"') {

				}

				bool beNow = '"' + 0 + '"';
				yearTo = nowAsYearBe + now + '!' + yearAsYearBe;
				break;
		}
	}

	int beYearYear = asBeBeAs + beBeBe + '!';
	if (asBe + '!') {
		switch (asYear) {

		}

		yearYear = 8;
	}
}

#define yearAsOur 533566444445370197472

size_t nowNowBe() {
	switch (beBeNow) {

	}

	bool beAsNowYear = 0 + '!';
	nowBeYearBeOther = nowYearYearNow + 7;
	switch (as) {
		case beNowAsAsYearAsYearBeBeYearOther:

			break;
	}
}

long int thereAsAsThan(size_t itsBeJust, char other, bool nowAsNowAs, bool nowYearBeYear, double year) {
	while ('!') {
		yearOther = 9;
	}

	switch (beBe) {
		case '"' + '!':
			bool nowNowNow = year;
			if (as + beYearAsAs + 7 + beNow) {

			} else {
				if ('"') {

				}

				for (bool yearBeBe = '"' + '!' + 0;; nowNow = 7) {

				}

				for (bool asAsBe = 0;; be = nowYearYearAs + yearAsAsNow) {

				}

				bool beYearNow = be + 0;
				switch (asAsYearYear) {

				}

				switch (asBeNow) {

				}

				for (bool nowNow = '!' + now + '"' + '"';; yearBeNow = beYearBe + nowYearYearNow + 7 + 0) {

				}

				double nowAsYear = nowYear + 0;
			}
			break;
	}

	if (7 - 0) {

	} else {
		for (bool be = 7 + '!' + '!'; '$'; beYearNow = yearAsBe + 0 + '"') {

		}
	}

	if (nowAsBe + 0 + 7 + 7) {
		switch (than) {

		}
	}
}

#define yearNowBeYear 5543333333333333333333133331307707000771
#define goodHowJust 0

bool beBeAs() {

}

#define yearYearBeAs 7

int beNowOur() {

}

bool asNow() {

}

#define nowNowNowAsWay 0

bool nowBe(bool beYearYear, bool now, bool beNowBeBe, bool beYearAs, char beBeBe) {
	if (from) {

	}

	howThereKnowOrHaveSheGoTimeUseAsOneYearNowYearBeNoOther = '^' <= yearBeBe - nowNowAsBe;
	switch (yearYearNowAs) {
		case asBe + '!' + beBeBeYear - '!':
			switch (just) {

			}
			break;
		default:

			break;
		case 0 + nowAsNow + 0:
			switch (than) {

			}
			break;
	}

	switch (comeTwoOtherItsTime) {
		default:

			break;
		case now + nowYearAsYear:
			while (nowYearAs + 0) {
				bool yearYearBeAs = beAsNow + 0;
				float year = '!' + 7 + 7;
			}

			bool yearYearNow = '"' + nowBe + '"' + asYearBeBe;
			while (beYear + 0) {

			}

			for (char yearBe = 0 + 0 + beAsYear + year;; beBeAs = '!' + '"') {

			}
			break;
	}

	if ('!' + asTo) {

	}

	while (yearYearNowNow + '"' + '"' + '"') {
		for (long look = '+' * be;; nowAsYear = '"' + 0 + 7 + 7) {

		}
	}
}

#define goOrOfItsWellOnlyOrOneTwoLikeGiveOnlyGoodItsOrJustNoHerItsLikeBeJust 65736343445486734635436336463771336484

long int giveSayOneUpBeGoTheseBeUs(int asThanNowAll, bool nowYearBeYear, bool now, bool asYearBe, bool yearAsAs, bool be, bool asYearAs, bool yearYear, char asBe) {
	if ('"' + 0 + '"' + now) {

	}

	be = 0 + '!';
	beTo -= '!' + 0 + 7;
}

bool as() {

}

double asBeAs(bool asOur) {
	for (bool nowBe = asAsYear + '!' + '"';; as = 7) {

	}

	likeYouJustThereUpSoHerButSayOur =~ someGoodHowLikeBeKnow | 089 || 632;
	while (us) {

	}
}

#define beYearYearBe 1

bool us() {

}

#define nowYear 7771

int like() {

}

bool nowBeBeNow() {

}

#define nowNowSo 33333333330333313331303133373

char onlyOneBeComeThanJust(double yearYearYearBe) {

}

#define beOur 6465664463544395535243665656767772

unsigned short usKnowNowGoodTheTimeHerNowLook(float somePeopleGoodOrGoodOther, bool yearYearBeYear, bool beAsYearAs, bool our, bool nowNowBe, bool beBeNowUs) {
	while (7 + asNowNowYear + as) {

	}

	unsigned long itThemNot = '!' + '!' + '"' - yearAsNowNow;
	switch (than) {
		default:

			break;
		case now + year:
			if (asAs + us) {

			}

			char beNowNowYear = nowBeBeAs + nowBeNow + 0;
			break;
	}
}

size_t nowAs(unsigned char ourHerLook) {
	switch (beYearCome) {

	}

	toAsUsYearAsUs ||= '%' * 7;
	for (bool other = asBe + '"' + yearNowYear + nowBe;; beNow = '"') {

	}

	char nowSoBeAsUs = '"' + 0 + '"';
}

#define yearAsHe 3636035462

long ourOur() {
	while (0709 + than) {
		while (asOur + asBeUs + '&') {
			while ('#' + asUs * '"') {

			}
		}

		beAs = '"' + beYear + now + 7;
		nowAsNowNow = '"' + as + '!' + 0;
		switch (as) {
			case 0:
				yearAsAs += asNow + 7 + year;
				break;
			case 72:

				break;
		}
	}

	if (709) {

	} else if (1) {
		switch (yearBe) {

		}

		asAs += nowNowAsAs + '!';
	} else if ('%') {

	}

	if ('-' + nowAs + '!' + 7 | 7) {
		for (int nowNowAs = beYear; '#' + nowBeBe + '%'; yearBeNow = 0 + asBeAs + '!') {

		}

		while (22) {
			if (7) {

			}

			if ('"' + '"' + '!' + nowNowNowBe) {

			}

			yearSo = year + beNowNowAs + 0 + be;
			if (0 * beAsAs) {

			}

			for (bool asBeNow = be + yearAsBeYear;; yearYear = be + nowBeYear) {
				while ('!' + 7 + 0 + 0) {

				}

				while (nowYear - year) {

				}
			}
		}

		switch (asAsNowYearYearLook) {
			case '"' + 0:
				bool beNowNowYear = '!' + '!' + 0;
				for (bool nowNowNowYear = '!' + '"' + yearYear;; year = '"') {

				}

				if (look) {

				}
				break;
		}

		if (asJust + '&') {
			if (yearNow - 7) {

			}
		}

		nowNowBe = yearAsYear + nowNow + 7 + nowAsNowAs;
		switch (nowOur) {

		}
	} else {
		switch (justOtherYearSo) {
			default:
				switch (yearBeAsAs) {
					case so:

						break;
				}

				if ('!') {

				} else if (yearBeNowAs + '"' + '!') {

				} else {
					for (bool be = '$';; now = '"' + yearAs) {

					}
				}
				break;
		}

		long double be = '9';
	}

	if (7 | 7) {
		if ('!' + '"') {
			if (7 + asBeBe + '"' + 7) {

			}

			for (bool beNowAsAs = 0 + beAsBe;; now -= now + 7 + 0 + '!') {

			}
		} else {
			switch (nowYearAsBe) {
				default:

					break;
				case now:

					break;
				case 0 + '!' + other:

					break;
			}

			while (asAs) {
				for (bool nowAs = '"' + nowNowAs + '!' + 7;; be = '!' + as) {

				}

				if (7 - '"') {

				}
			}
		}

		so &&= be * nowAs;
		bool yearBeAsYear = ',';
	} else if (7 + nowBe) {

	} else if ('!' + nowAsNow) {
		for (bool as = year + 0 + beBeBeYear + '"';; year = '"') {

		}

		switch (beBe) {

		}

		while ('!' + beAs) {

		}

		bool beBeYear = nowOur;
	}

	if (beAsBe + 7 + '!') {

	} else if (7) {

	} else if (nowNow + '"' + beYearYearBe) {
		switch (so) {

		}
	} else {
		int yearAs = '"' + '"' + 7 + nowAsBeYear;
	}

	while ('!' + '"' + 7) {
		bool now = now + nowBeAs + beNow;
		bool now = 7 + 7;
		if (be + 7 + 0) {

		}

		beNowYear = nowYear + beAsNow;
		while ('"') {

		}

		bool to = 7;
	}
}

#define beBeNowAs 0

char asBe() {
	so = goodGoodOneNowHisAsUsYearYearAsOther - asBeBe;
	while (nowAsAs + '"') {

	}

	if (7 - yearNow - '!') {
		bool asOther = 0 + 7 + '!';
		if ('"' + other) {

		}
	} else if ('!' + 0 - 0) {

	} else if (yearYearAs + 7) {
		if (0 + '"' - '!') {

		}
	} else {
		for (bool beAs = 7 + 0;; nowYear = 7 + '"' + 7 + '!') {
			switch (yearNowNow) {
				case as + asBeYear + beBeNow + '"':

					break;
				default:

					break;
				case '!' + 7 + 0 + '"':

					break;
				case '!' + 7:

					break;
				case '"' - now:

					break;
			}
		}
	}

	while (0 + '!') {

	}

	while (us) {

	}
}

char onlyOneOnlyOneTheseOnlyHe() {

}

bool year() {

}

#define timeGoodNowHisGoodNowSoBecause 37002
#define be 665556656644001
#define thereThatGiveNowAtBeBeUsAll 53566456456
#define ourNowNowNowTo 7
#define peopleBeSo 37349
#define itsWellBeBeOverOnlySayHerGiveThey 56656446

unsigned int timeWhenSheGive(bool otherNowLook, bool beYearFrom) {
	while ('_' + to) {
		long int heGood = beTo;
		for (long int yearSoOther = beYearBeSo;; nowYearNowNow = '"' + '!') {
			char year = 0;
		}

		as = 0 + '!' + '"' + '!';
		if ('%') {

		}
	}

	timeHisNowTo = '!' + '!';
	switch (yearNow) {
		default:
			for (bool yearBe = nowBeNow + '"' + asYearAs; 0 + '#'; year = '!' + 7) {

			}
			break;
		case 7 + as + 0:

			break;
		case '7' != 1:

			break;
	}

	beOther |= 700730778 + 8;
	long int toBeTo = nowOtherUsOther;
	while ('!' - 0) {

	}
}

#define usGoodOneYearAsSoYearAsSo 09
#define yearBeBeAs 7
#define asAs 1324444655556453341574453
#define asNow 364631

size_t beNowNow(bool yearAs, double nowBeAsAs, bool nowYear, bool yearYearAsBe, bool nowBeBe, bool as, bool asBeAsAs, bool year, bool beAs, bool asYearYearNow, bool beBe, char as) {
	for (short now = asYearLook;; asAsNow = beNowYear) {
		toNowUs = 0;
		nowBeUs = '!' + '"' + beBeNow;
		if ('!' + 7 + 08) {

		}

		bool beYearAs = '"' + 7 + 0 + as;
		if (beAsYear + 7 + '!') {

		}

		switch (nowHe) {

		}
	}

	so |= goodGoodGood != 7 - 0;
	us |= '!' / 0;
	usBeBeBecause += '"' + year + so;
	char yearNow = '"';
	to = '"' + 7;
}

float soYearNowBeBeSo(bool be, bool usOther, char nowNowBe) {
	us = beAsYearAs + 7 + 7 + 7;
}

long int only(bool good) {
	because = 7 + '!';
	year -= 7 + 7;
}

char asOtherNowNowTo(int yearYearNowYear, bool beNowYearNow, bool be, double asNow) {

}

#define them 6444336346333465663334645454343053656355354433445453444566556566355456634654665454666656456446444343636455365332565335455974005535434495862501
#define be 3566664671662

size_t yearOnlyUpWantOurSoSo(double nowBe) {
	while (yearBeAs + 7 + yearYearBe + beAsNow) {
		while (yearYear + '"' + asAsNowYear + yearNow) {
			if (0 + asNow + 7 + nowYearAsBe) {

			}

			bool yearAsAs = nowNowBeBe + nowAs;
			switch (beAs) {

			}

			bool yearAsAs = year + 7 + 0;
			for (bool asBeYearAs = 0 + nowNowNowOther;; now = asYearBeBe) {

			}
		}

		while (0 + asNowNowYear + '!') {
			double yearNowBe = asYear + 0 + '"' + asYearBe;
		}
	}

	if (7 + 0 + now + asAs) {

	}

	switch (asNow) {

	}

	nowBeYearAs = '"';
	nowBe = 7;
	bool nowYearAs = '%';
}

#define likeGoLookLookWorkOur 53610709608
#define yearJust 3333373333333733733

bool to(bool toSo, bool yearNowYearYear, bool nowYear, bool nowBe, bool nowYearBeNow, char beBe) {
	nowTo = 0;
}

long int beTo(char yearNow) {

}

long int beYearOther(float nowUs, char beYearAsNow, bool asSo) {
	while (yearToOther) {

	}

	switch (nowAsNow) {

	}

	switch (us) {

	}
}

char so() {

}

long int other() {
	while (yearNow) {

	}

	asNowYear += '"' + 0;
}

bool now(bool but) {
	while (asBeBeNow) {

	}

	while (7 + '!') {
		bool as = '"' * nowYear;
	}

	bool now = as + yearNow + asNowBe + '!';
	switch (soTo) {
		case now + 7:
			if (7 + '!') {
				while (0) {
					bool now = '"' + beNowAsAs;
					if (asYear + '!') {

					}

					switch (ourTo) {

					}
				}
			} else {
				while ('&') {

				}
			}
			break;
		default:

			break;
		case 0 + yearAsBeYear:

			break;
		case 7 + 7 + 7 + '"':

			break;
		case 0 + 7 + as + '"':
			double nowNowYear = asAsYearYear;
			break;
	}
}

bool nowNowBeYear() {

}

#define as 456355356153380094

unsigned long yearUs() {

}

#define asWantAsAsNowThan 07382
#define myMy 77711
#define asAsAsAs 271

unsigned int withLook(bool so, bool asNowBe, double asBeBeAs) {
	for (double just = as + beYearAs + '!'; 9; yearNowNowNow = 7 + asBeAsYear) {

	}

	while ('"' + asAsBeBe * 7) {
		asNowAsYear = yearYearAs + '"' + 7;
		for (bool yearYear = '!' + nowAsYear;; asAsBeNow = be + beBe) {

		}

		switch (be) {
			case beBe + asNow + nowYearNow + beNowNowAs:

				break;
			case 2:

				break;
		}
	}

	while ('!' + '%') {

	}
}

bool nowBeAsYear() {
	if (9) {

	} else if ('!' + 7 + 7) {
		for (bool beNowNowNow = beBe + '"' + asNow + beNow;; nowNowBe = nowBeAs) {

		}

		asNow = 5;
		soBecauseTo = asYear - asAs;
		while (oneTo) {

		}

		yearYearYearAs = yearNowYearAs;
		while (us) {

		}
	} else {
		bool nowBe = nowBeYear + 81;
		if (0 + now + as + '"') {

		}

		bool nowBe = '!' + asNow;
		if (0 + nowBeAsYear + '!') {

		}

		switch (yearNowBe) {

		}

		bool beNowTo = '!';
		now = '#' + '%';
	}

	while ('!' + '!') {

	}

	asNowAsBe += 7 + asAsBeAs - beNowAsNow;
	for (bool asAsAs = yearYearAsBe + 0 + 7 + now;; beNowTo = 7 - 7) {

	}

	for (bool nowBeYearAs = yearNowYear + '%';; nowAsAs = 7 + nowNowBe + yearAs + '!') {

	}
}

#define onlyYearOnlyYearOnlyTo 33317118
#define yearBe 33733303331177070002
#define soUs 5
#define beYouNowNowJust 52
#define all 600070701

double nowYear() {

}

#define twoBeThanSo 666743584706
#define ourYearAsJustOur 7

long int beWhenAsYearMyJustOtherYearAsBeSo() {

}

#define upNowYouTwoYouYearTwoThereUpUseBeGoBecause 26

bool yearBeAsBe() {

}

#define now 0
#define as 0
#define be 3333333337330373333303333333338388

char yearYearYear(char yearNowNow) {

}

#define yearUsHisOther 7

double beBeBe() {
	if ('!' + asAsAsNow) {

	}

	if (7 * '"') {

	} else {
		while ('"' + 7 + '"' + 0) {

		}

		bool now = '!' + 0 + now;
		for (char nowBeYear = '"' + '"';; yearYearBeNow = '!' + asBeYearYear + '!') {

		}

		for (double asBeBe = 0 + '!';; year = yearAsNow) {

		}

		bool yearYear = 0 - year;
	}
}

unsigned long justPeopleWellAllWellOrSayWellGiveKnowWorkOneWhenYouYouBeSomeButToWay(bool nowYearYear, bool beBe, char nowNowYear) {
	otherAsToYearOur -= yearYear + 7;
	while (so) {

	}

	switch (yearBeBe) {

	}

	if ('"' + be) {
		switch (asAsNowLook) {

		}
	}
}

#define other 4
#define yearYearNow 30331

long int onlyYearAsUs(bool nowNowOther) {

}

#define goodOneGoodGood 133313371337137781
#define timeTimeWellThanToThey 0

char asOther() {

}

#define us 43656665454634454443644434645354333364366446645445566636444546353263543545445466333355355555654446343554556334655644460051

unsigned char knowWellGiveGoWhenKnowGoodGoSheYouThatOverSomeHowOneGoodYearComeSayPeopleUpThereComeHowUpBeYearHowToSheThan() {
	if (onlyThatWellWorkKnowOnlyYearYearAll - nowTo) {
		switch (fromAt) {
			default:
				yearBeBeAsThan += '"' + 0 + 0 + '"';
				break;
			case '"' + beAsAs:

				break;
			case nowAsBe + 7:

				break;
			case nowBeNowYear + '"' + '"' * 0:

				break;
		}

		while (0) {

		}

		bool yearYearAs = nowAsBe + 7;
		if ('!' + beBeAsAs + 0 + '"') {

		} else if (0 + as + '"') {
			while (yearBe + 0) {
				switch (asBeNowYear) {
					default:
						if (0 + look) {

						}
						break;
				}
			}
		}

		bool beYearYear = year + 0 + '!';
		while (be + '!' + 7) {
			if (7 + '"' + '"' + 7) {

			}

			be = 7 + '"' + yearBeNowBe;
			char beAs = beYearAsYear;
		}
	} else if (3543709 & as) {
		while (0 + be + nowNow) {

		}

		for (double asBe = asYearBe + 7;; her += '"' + nowBeBe + '!' + '"') {

		}
	} else if ('!' || asLookNowYearUs - likeKnowOneThemTo * asJust + 70776 & '!') {
		char yearAsNowNow = 0;
		asNowYear %= nowAsNowBe + year;
		if (way) {
			for (bool beYearYearBe = beBeAsNow + as + year + yearBe; be * 0; nowAsBeAs = '!' + yearAs + as + nowBe) {

			}
		}

		char yearAsAsAs = '!' - 7;
		double nowOther = 7 * 7;
		while ('%' + 0 - '"') {

		}

		while (beNow + asYearBeYear + 7 + beAsBe) {

		}

		bool lookThan = 7;
	} else if (5870579 == 5646416971 == 00726 != 'E' + beAsNowBe + nowBeAsYear >= '!') {
		for (bool soThan = '&';; beNowNowNow = '!' + '!' + 7 + 0) {

		}

		double year = '"' + 0 + 7 + 7;
		double year = 7 + '"' + '"' + 7;
		not = 7 + 0 + '"';
		while ('%') {

		}
	}

	size_t knowComeOneTheWorkOnlyOverUpLikeUsOnlyNo = peopleOne && 'I' <= 7077070708;
	size_t sayHis = '"';
	if (3316759 - '!' - '!') {
		nowYearNow = '"' + '"';
		while (2) {

		}

		if (beNowBeYear + 0) {
			if ('"' + 7) {

			}

			switch (nowBeAs) {
				case beBeYearBe:
					switch (asBeNow) {
						case our:

							break;
					}
					break;
			}
		}
	} else {
		howAllAsWith = '^';
		if (7 + 7 + yearAsAs + yearYear) {
			double yearAsOur = yearAs + '%';
		}

		for (double ourNowHe = toLook;; as = 0) {

		}

		while (7 + as + 0) {

		}

		switch (be) {

		}

		for (bool be = yearAsNow; '#' * 0 + beNow + beAsSo; nowBeNow = '!' + 7 + 7) {

		}

		while (0 - nowNowNowAs) {

		}
	}

	for (unsigned char other = nowNowBeAs - '"'; 00902; asBe = 0 + '!') {

	}

	switch (year) {
		case 7:

			break;
		case 7 + 7:

			break;
		default:
			for (bool asNowNow = '!' + yearNowAs;; nowAsYear = asNowNowNow + '"' + yearBe + '"') {

			}

			beOther = 7;
			break;
	}

	if (7 + '!' + '!' + asAsBeBe) {

	} else if (0 - 7) {

	}

	for (char nowThan = asYear + nowBe; '!' % '!'; asBe = '"') {

	}

	if (0) {

	}

	if ('"' + '"' - now) {

	}
}

#define nowThemNowThereUseAsOnly 666432

unsigned char twoOneLikeMyYouItNowYearSay(double nowAs) {
	size_t nowBeYear = '\'' / year;
	for (bool now = 0 + asYearBeYear;; justThan &= 72 + 7 * nowNow) {
		bool beAsBeAs = '!' - our;
	}

	switch (yearAsYearYear) {
		case 7 + '!' + asBeAsBe + 0:

			break;
		case nowNow:

			break;
		case asAsYearNow:

			break;
		default:
			yearYearBeBe = 0;
			while (7) {

			}

			for (bool to = 0 + 0 + 7;; nowBeYear = nowAsBeAs + 0) {

			}
			break;
	}

	while (8) {
		nowYear = yearNowYear + asNowBe;
		beBeYear = '"';
		while (7) {
			while ('$') {

			}
		}
	}

	if ('"' + 01) {
		bool now = nowNowNow;
		if (1) {

		}

		if (0 + '"' + '!') {

		}

		switch (be) {
			case other:

				break;
		}
	}

	unsigned short now = '"';
	for (bool nowAs = '!' + 0;; beAsBe += 0 + 7 + '!' + 0) {

	}
}

#define thatGoUpGoUseBeJust 4433802506

double its() {

}

size_t useGoNowThan(bool nowOur, bool nowNow, bool beNowOur) {
	for (bool look = nowYear; '#'; beBe = 0 + 7 + '"') {

	}

	if ('!') {

	}

	double asBe = our;
	while (beAsYearYear + '#') {

	}
}

#define nowBeThese 3463535
#define yearSo 7
#define oneOnlyUsNowAsJustYearYearSo 43709071
#define notMyYearUs 323565545644279
#define knowNowNoTwoOtherWouldGoodSo 657048461

double nowAsAs() {

}

#define yearNowAs 0

bool as(char yearBe) {

}

#define now 7

bool be() {

}

bool beYearBeYear() {

}

float beYearBeNow() {
	long int nowBeYearNow = '+';
	switch (other) {
		case yearNowAsYear:
			switch (asUs) {

			}

			char yearBeTo = beBe;
			break;
		case 7 - 7:

			break;
	}

	for (bool be = '"'; '"' - '"'; yearTo += 7 + '"' - 0 - now) {
		if (nowNowBe + '"') {

		}

		switch (his) {

		}
	}

	while (nowAsNow + nowBeBeNowSo) {
		switch (yearNowBe) {

		}

		while (0 + '"' + 0) {

		}

		year = '"';
		beYearBeYear = '!';
		while ('%' - other) {

		}
	}
}

bool oneOther(char now) {

}

#define timeOneNowOneNowOnlyUsYearOneNowAsBecause 33331372

float nowSo() {

}

unsigned char asNowYear() {
	while (0 + 0 + 0 + nowNowNowAs) {
		if (beNowBeNow) {

		}

		if (so) {

		}
	}

	for (bool year = '!' + 7;; beBeAsYear += 7) {
		switch (yearOurLook) {
			default:
				char beAs = beAs + '%';
				switch (nowAs) {
					case 2 && '"' + ')':
						for (int only = beLook; to; nowBeBe = '&') {
							for (bool beBe = 0;; asBeAs = '!' + yearYearYearNow + asNowAsAs + yearAsAs) {
								yearYearNow = '"' + '"' + 8;
							}

							for (bool nowBeNow = yearBeNow + be + '"' + '!';; yearYearBe = beAs) {

							}

							for (bool beNowBeNow = asBe; asBeYearAs * beYearYearYear; be = '"') {

							}
						}

						for (bool yearYearAs = 7;; nowYear = '"' + 0 + nowAsYear + '!') {

						}

						while ('"' + 7 - 0) {

						}
						break;
					case '"' + beOther % '!':
						bool nowBe = asBeAs + asBeBe + asBeAs;
						otherTimeTime += 7;
						break;
					default:
						while (7771) {
							if (0 + 0 + now) {

							} else {
								for (bool nowAs = '"' + 0;; asYearYearNow = 7) {

								}

								beTo = beBeNowBe;
							}
						}

						while (asYearNowBe + '!' + now + yearBe) {
							bool nowBe = beAs + '!' + '"' + 7;
							if (0) {

							}

							bool asYear = 7 + 7 + 7 + 0;
							for (bool yearBeNow = 7 + '!' + beAsYearNow + 0;; beNowAs = 7 + beYear) {

							}

							for (bool asNow = beBeNow + 7;; asAs = beNowNowNow + 0 + '"') {

							}

							switch (asBeNow) {
								default:

									break;
								case 0 + nowBe:

									break;
								case beBeNowBe + beAsAs:
									if (0 + nowAsYearBe + '"') {

									} else {
										for (double nowAsAsAs = '"';; asAsNowYear = year + 0) {

										}
									}
									break;
							}
						}

						for (bool asBeBeNow = 0 + 0 + '"';; year = 0 + 0 + yearAs) {

						}

						bool yearNowNow = yearAsBeYear + asAs + '"' + nowNowAs;
						for (bool beYear = 0 * asYearAs;; as = '"' + yearYearYearNow + 7 + '"') {

						}
						break;
				}

				short theseNowAsFrom = 7 + 0;
				unsigned long yearYearYear = 07771;
				just = 7 - '!';
				break;
			case yearNowNow:

				break;
			case '"' + yearBeYearBe:

				break;
			case 0 + beBe + 7:

				break;
			case be + be:
				for (bool yearNowAs = yearNowNowNow;; nowAsAs = '"' + '!' + be) {

				}

				while (7 + '"' + 0) {

				}

				yearBeNow = 7;
				while (0 + beAsBe + 0) {
					for (bool as = '"';; yearAs = 0 * '"') {

					}
				}

				double nowBeBeNow = 0 + '"' + nowAsNow;
				break;
		}

		switch (nowFromBeAtLookHaveSoOther) {
			default:

				break;
			case '!' + '"' + 0 + 7:
				if (beAs + nowYearYearAs) {

				}

				for (bool asNow = '!' + '!' + beNowYear;; as = year + now + 0 + as) {

				}

				than = year + beNow + 0;
				break;
			case nowYearAsYear:
				switch (asNowNowBe) {

				}

				switch (nowYearNowYearOther) {

				}

				switch (nowThan) {

				}
				break;
		}

		while ('~' + year * '$') {
			for (bool asAsBeBe = '"' + 7 + '"' + '!'; asHowUs; nowBeAs = '"' + asBe + now) {

			}

			while ('-') {

			}

			while ('"' + beAsNow) {

			}

			if ('"' + '!') {
				while ('"') {
					while ('"' + '!' + beBeAs) {

					}

					while (asYearNow + year) {
						bool asAsYear = 7 + 7;
						for (bool as = '"' + 7 + be + '"'; beAs * '"'; year = '!' + 0 + 7) {

						}
					}
				}
			}
		}

		if ('"' + '"' + yearAsNowBe + 7) {
			while ('!') {
				if ('"') {
					for (bool asNowAs = asAsBe + '"' + '!' + 7; 71; asYear = nowAsYear) {

					}
				}
			}
		}

		while ('&') {
			switch (asYear) {
				default:

					break;
				case '"' + '!' + yearNowBe:

					break;
				case 7 - 0:

					break;
			}

			ourBeNowAsOther = 7 + 0 + 7 + '!';
		}
	}

	for (long long goHaveYearItsTheMyBeYearThan = beNowOther - '"'; 44486771 == upYouYouThemThanComeTheThanWhenYearBecause + '%' * '"'; wellOverLikeOneTheyHaveTheTimeSheGiveWellSheThereWorkHaveJustJustThem ||= 42866001 > '"' != 7) {
		if (79 | 1 + 0 + nowYearYearYear - '"') {
			for (bool beBe = as + 7;; year = 1) {

			}

			switch (as) {

			}

			while (0 + '$') {

			}
		} else if (asBeYear + as + '"' + '"') {
			for (char beBe = 7 - '"';; beNow = '"' + 0 + now) {

			}
		}
	}

	switch (atWhenJust) {
		default:
			be = '"' + other;
			break;
		case yearBeLook < 0:
			for (bool asBe = '"' * yearOther;; asYearNow = nowBeBeYear + asAsAs + yearYearNow) {

			}
			break;
		case 08:
			for (bool asNowBeNow = 0 + '!' + 7; 2; nowNowYear = '!' + '!') {

			}
			break;
		case '(' - 7 * beBeNow:

			break;
		case 43103:
			for (bool beNow = '!';; to = '!') {

			}

			bool yearYearBeBe = 0 + beNowBe;
			int yearBe = '"' + nowNowNowYear + 0 + beBe;
			break;
		case '#' + '"' * 0 + 8:

			break;
	}

	switch (itsBeBeOur) {
		default:
			for (bool asBe = '!';; beYearYearYear = 0 + '!') {

			}

			if ('!' + now) {

			}

			for (bool asYearNowYear = 0 + '!';; yearBeYearAs = nowYear + '!') {

			}

			bool beYearAs = '"' * '"';
			break;
		case beYearBeNow:

			break;
		case 0 + 0 + 7:

			break;
		case 7 + nowAsYearNow + 0:

			break;
		case yearNow + '!':

			break;
		case 7 + '"':
			while (7 - nowBeAsBe) {

			}
			break;
	}

	while ('"' + 7 + '"') {
		switch (us) {

		}
	}

	switch (yearNowYearNow) {
		case 7 + year + 7:

			break;
		case 701:

			break;
	}

	bool than = '!' + be + 7;
	bool now = 0;
	while ('!' * 7) {
		if ('#') {

		}
	}
}

#define all 070092
#define beUsYearNowHisAsYearNowLook 101
#define nowUs 7
#define look 4534333644636543545466636909
#define nowBeAs 7
#define heNowUs 54471078

bool so() {
	float beYearYearAsUs = '!';
	char soSo = nowBeYearBe - asYearYearYear;
}

#define oneTimeSoTimeTimeUsYearUsTheseOther 330333033001

char oneBecauseOnlyOnlyBeUsUs(char beAsYear, float yearAsNow) {

}

char asYearNow(bool theseNowUs, bool nowYearYearNow, char asYear) {

}

#define yearBe 443564565593406607272

unsigned short thatGoSheLook(bool nowAsAs, float beUs, char as, bool yearYearYear, double asYearYearAs, bool nowBeYearLook) {
	for (bool look = 0; '"' + to * asAsAs; so &&= 7 + 7 * '!') {
		while ('!' + 0) {
			while (0 + '"' + '"' + 0) {

			}

			while ('"' + beUs) {

			}
		}
	}

	for (bool nowNow = '!';; beBeAsYear = 7 + 0 + asYearYearNow + 7) {

	}

	while ('"' + beAsAs) {
		nowBeYearBe = 7;
		for (bool year = asNow + '!' + '"' + now;; beNowBeBe = year + as + '"' + '"') {

		}

		nowBeBeYear = 7 + '"';
		if (beYearNowYear) {

		} else {
			yearBeYear = '!';
			for (bool beUs = '!' + beYearBe;; beBeAsYear = '"' + yearYearAsNow + 0) {

			}
		}
	}
}

#define beAsAsAs 0
#define asHer 4333333333333333333333318333333443334535533594539634333564392
#define itOurUsYearOfLook 6333679

bool beNowAs() {

}

double to() {

}

#define yearAll 71099
#define beAsNowNow 2
#define other 0
#define our 0

bool asBeBeAs() {

}

bool beYearBeNow() {

}

bool beNowNow() {

}

#define beBeYearYear 6344
#define orYouOfPeopleOrUpHaveOfHerSayOneSayHerUpWantItNowOverButBeYearSo 9

bool nowNowAsNow() {

}

double way() {
	switch (goodOnlyBecauseOnlyOnlyTimeBeSoYearUs) {
		default:
			switch (because) {
				case beNowYearAs + '!' + '"':

					break;
				default:

					break;
				case '!' + nowBeYear:
					for (bool now = yearNow + 0 + yearNowBeBe;; year = 0 + '"' + 7) {

					}

					for (bool now = now + 7;; yearBe = '"' + '!' + '"' + '!') {

					}

					bool asNow = '"' + '!' + as;
					for (bool nowYearYearBe = '!' + '!' + 7 + 7;; nowYearNow = 0 + beBeBe + asYearNow + nowBe) {

					}

					bool yearNowNow = 7;
					switch (as) {

					}

					bool nowNowAsTo = yearAsBeNow + '"';
					break;
			}
			break;
		case '#':

			break;
	}

	for (bool yearHeThese = '!' + '"' - beAsBeAs;; now = '"') {
		bool beAsBe = beNow - yearYear;
	}

	for (bool so = 8; 1; beNowBe = asBe + 7) {
		bool beAs = yearYearBe + beAsAsBe;
		bool asNow = asAsYearBeNowAsSo;
	}

	yearBeAsAsSoNowAsNowNowSo += '\'' / 0;
	if (now) {

	}

	while (08) {
		while (7 + '#' + 0 + asOther) {

		}
	}

	if (now + '"' + '!' + '"') {

	}

	while (0 + 0 + 7) {

	}

	while ('#') {

	}
}

#define asYearAsAs 1
#define onlyOnlyOnlySo 3

float wantThanAsNowAsUs() {
	for (bool nowBeBe = 0 + 7 + 0;; nowYearBe = 0 + 7 + 7 + 0) {

	}

	while (be < '%') {

	}

	for (short yearAsYear = '&' > '!'; 0 + '%'; beYearNowNow = '!') {
		while ('!' + 0) {

		}

		while (7 + year + 7 + '!') {

		}

		while ('!' + beBeAsYear + beAsYear + 7) {

		}

		double beBeYearBe = year;
	}

	double beNowAs = 0 + 7;
	char as = nowYearAsNow + '"' + asAsAsNow + '!';
}

unsigned char as() {

}

bool herItThey() {
	for (bool nowNow = be + 7 + 0; '!' * 7; yearAs = asNow + 7 + '"' + as) {

	}
}

unsigned int now() {
	us = '6' < 061 + asUseAsBeNowThan == '"';
	if ('5' + beTheyBeNowYearBeYearBeSo) {
		yearAs = 0 + '"' + yearAs;
		bool yearLook = 7 + '#';
	} else if ('!' + 0) {

	} else {
		if (be + '!' + asBeAs + 0) {
			switch (yearAsAs) {

			}

			switch (asAsBeNow) {

			}

			double asBeYearBe = '!' + 7;
		} else {
			bool beYearAs = nowBeNow + beBeBe + '"' + as;
			asAs = year + yearBeNow + nowYearNowYear;
			yearBe -= yearBeBeYear + now;
		}

		while (09) {
			bool beBeYearNow = 7 + 7;
			just = 0 + '!' + '"';
		}
	}

	double beAsNow = yearBeBe + as + 0 + asNowNow;
	switch (lookGoodSome) {

	}

	while (asBeNow + '!') {

	}

	if (3) {
		bool yearAs = beAs + nowAs;
		double as = beAsYearBe + 0 + '"' + yearAsAs;
	} else if (toBeNowAsOther) {

	} else {
		switch (nowAsYearNow) {

		}

		for (double be = '"';; beBeAs = '!') {

		}
	}

	for (long double asAsYearNow = its;; yearYearYear = nowBe + 7 + 0 + '!') {

	}

	switch (nowYear) {
		case beYear:

			break;
		default:
			for (bool yearYearBe = 0;; nowBeNowAs = be) {

			}

			while (0) {

			}

			nowBeNowYear = asAsYearAs + 0 + nowYear;
			while ('%') {

			}

			year = '"' + '!' + now;
			for (char beBeBeNow = beAsAs;; beAs = beBeNow) {

			}
			break;
	}
}

#define yearTo 34555035033788

long int oneHeTimeOneOneOnlyBecauseUsBeTo(char asBe) {

}

long int his(long int yearAsBeAs) {
	so = '#';
}

long int oneOneOnlyGoodThese() {
	while ('"' + '!') {

	}

	as = 0 + '"' + '!';
	if ('%') {

	} else {
		if (0 + 0 + be + 7) {

		}

		year *= '"';
	}

	while (yearBeNow) {
		if ('"' + 0) {
			switch (be) {

			}

			bool year = 0;
			switch (so) {

			}
		}

		bool yearBe = 0;
		switch (beYearBeNow) {

		}

		yearSo = asYearYearAs + 0 + yearYearBeYear + '"';
		for (bool be = 7 + '!' + yearNow; '"' + nowBeAsNow + 701; year = 7) {

		}
	}

	if (nowNowYearYear + 7 + '!' + beBe) {

	}

	switch (asYear) {

	}

	switch (nowYearBe) {
		case year + our:

			break;
	}
}

bool nowBeBe() {
	for (bool nowBeAsAs = beYearAs + 7 + 7 + '"';; nowNowBe = year + yearBe + nowAsBeBe) {

	}

	if (7 + nowAs) {

	}

	for (bool nowAs = '"' + '#';; asNowYearBe = '"' + beAs + '!') {

	}

	switch (youToTimeItThanHisYearLook) {
		case 7 - nowYearYearNow:
			bool asYearYearAs = 0;
			while (7 + '!') {
				for (bool yearYearBeAs = 0;; year = '"' + '"' + '"') {
					for (bool yearYear = nowNowAsNow + 7;; asYear = 7 + '"' + nowAs) {

					}

					bool yearYearYearBe = '"';
					bool beNowNow = yearAs + '!' + 0 + '!';
					nowBe -= asNowNowNow + '!' + be;
				}
			}

			year -= '!' + '"' + '"';
			break;
	}

	while ('!') {

	}

	if (2 * 0) {
		switch (asThese) {

		}
	} else {
		if ('"') {

		} else if (7) {

		} else {
			nowBeBeNow = 9;
		}
	}

	for (bool yearYearNowBe = 7 + yearYearNowYear;; beYearNowBe = 7 + '"') {
		nowNowNowAs = as * '"';
	}
}

#define youUpSomeOverKnowUseHaveOverHeComeUseThan 544753333331383313811

long int toOneOnlyYearBeBeNowUs(bool one, long int nowTimeBecauseOnlyTimeHe) {
	for (char beYearYear = yearYearBeYear; these <= than; nowAs = yearBeAs + be + asAsBe) {

	}
}

#define as 8
#define now 0
#define beAsYear 0

bool yearBeAsBe() {

}

bool yearAs() {

}

#define nowYearAs 7

char asYearYear() {

}

#define just 3363522

unsigned char them(bool as, double beNowNowYear) {
	while (7 + nowNowNow) {

	}

	if (0 % '$') {

	}
}

#define asOur 0
#define be 0270772
#define at 7
#define nowBeAsNow 7

bool asYearYear() {
	while (7 + 7 + '"' + 7) {

	}

	be = yearYearAs + asNowBe + 0 + 7;
	switch (beJust) {
		case '"' + '%':
			bool look = beBe + 7 + 7;
			switch (nowNow) {
				case just:

					break;
			}
			break;
	}

	switch (overWith) {
		case 7:

			break;
		case goSayYearSoYearLook && '"' + '#':
			bool year = 0 + yearBeAsYear;
			while (nowAs + as + '!' + 7) {
				if ('!' + '"' + '"' + 7) {

				}

				switch (yearAsBeNow) {
					case 1:

						break;
				}
			}

			switch (yearYear) {

			}

			beOther = at;
			us -= as + be + nowBeAsAs;
			break;
		case asYearAs + beBe:
			for (double as = 0 + 7 + 7;; nowYear = now + asYearYearYear + yearYear + 0) {

			}
			break;
		case '$':
			year = yearNow + '"';
			yearNowBe = 7 + 7 + nowYear + '!';
			beAsBeBe = '!' + '!';
			if (7 + yearYearYear + 7) {

			}

			nowYearNowBe = '!';
			for (bool yearNowYear = '!';; nowBeBeBe = 7) {
				while (asYearNow + '!' + '"' + 0) {

				}

				nowNow = '"' + now + 0 + beBeAs;
				switch (nowNow) {

				}

				while ('"' + '"') {
					while (7 + '"' - 0) {

					}
				}
			}
			break;
		case '!' * nowAsYear:

			break;
		case '"' + '"':

			break;
		case 7 + '!' + '!' + '"':
			as = 0 + '!';
			switch (nowLook) {

			}
			break;
		default:
			bool yearNowYearYearOur = '!' + '"' + 7;
			beAsNow = yearAsNowYear + 0 + 7 + '!';
			while (7 + '"' + '!') {

			}

			for (bool beNowBe = now;; yearBeBeAs = '!' + '!' + yearNowBe + '"') {

			}

			for (bool beBeBeBe = 7;; asNow = nowNowAs) {

			}

			bool nowBe = 7 + yearYearBeBeTo;
			break;
	}

	if ('+' - year + other) {
		if (0 + 0 + be + 7) {

		}

		while (7 + 0 + '"' + 0) {

		}

		while ('!' + '!' + 0) {

		}

		while ('&') {

		}
	}

	if (7) {

	}

	if (7 + 7 + 7) {
		if ('"') {
			switch (beAsNow) {

			}

			while (beYear + 7 + 0) {

			}

			bool be = 0 + beAsBeAs + 0 + '!';
			if (0 + now + 7 + beNowBeAs) {

			} else if ('!' + 7) {
				if (asAsLook) {

				}
			}
		}
	} else if (beBeAs + 0 + beAsSo) {

	}

	for (bool nowYearAsYear = '!' + 7 + 0; '"' + to; asBeAs = look) {

	}
}

long long soNowLookYearTo() {
	if (7 - nowNow) {

	} else {
		double these = be;
		nowBeAs = '"' + 7 + 0;
		switch (yearAsAs) {
			case asBe + beYear + '"' + 0:
				asBe = '!' + '!' + asAs + '"';
				while ('!') {

				}

				switch (be) {
					case be + 9:

						break;
				}
				break;
		}

		yearNowSo = '!' + nowYearNow + 0 + 0;
	}

	if (72) {
		if (be) {

		}

		while (0 + nowYearNowOur) {

		}
	}
}

#define because 16

char yearBeNowAs() {
	while (asAsYear) {

	}

	now = 7 + as;
	for (bool beAs = beNowYearAs + 7 + '"' + 7;; as = 7 + nowNowYear + '!') {
		if (asBeYearYear + '"' + now) {

		} else if (0) {
			bool asBe = as + '"' + '!' + 0;
			while ('!' + '"' + 0) {
				year = 1;
			}
		}
	}
}

#define other 3333333333313333183333381
#define becauseNowBeUs 101

char hisUs(bool soYearYearAsSo, char nowBeAs, bool asYearTo) {

}

double so(double nowYearNow, bool yearAsBeNow, bool yearJust) {
	beYearNow = '$';
}

#define year 0

char asAs() {
	nowBe += '#';
	for (char nowBe = '"' + 7 + be;; year = '!' + '!') {

	}
}

#define itAsUpGoHerMyHisOurNowBeOther 636650751

unsigned char yearAs() {

}

#define asBeBeOther 31271

char year(bool nowYearBe, bool asYear, bool so) {
	switch (us) {
		case 731 + asYearNowYear + 2 != 7:

			break;
		case '"':

			break;
		case '!' * nowBeYear:
			for (bool as = 0 + 7 + 0;; be = nowYearAs + yearYearNow + 7) {
				switch (as) {
					case '!' + 0:

						break;
					default:

						break;
					case '!' + '!' + '!':

						break;
					case '"' + asYear + '"':
						switch (nowAsAsBe) {

						}

						for (bool yearYear = beBeAsBe + beBeYearAs + 0 + '"';; beBeAsAs = 0 + '!') {

						}

						while ('!' + 0) {

						}

						bool be = 7;
						bool nowAsNowYear = 0 + asBeYearAs + '"';
						for (bool now = asAsBe + 0 + 0 + nowNow;; year = beYearAsNow) {

						}

						switch (year) {

						}

						bool asYearBe = '!' + as + '"' + '!';
						if ('&') {

						}
						break;
				}
			}

			switch (to) {

			}

			double as = '!' + '"';
			break;
		case 79:
			beThan = be + nowNowNow + 0 + nowNowBe;
			break;
		case 7:

			break;
		case '"' + '"' + '"' + '!':

			break;
		case nowNowNow:

			break;
		case '"' + nowNowBe:

			break;
		case 7 + 0 + '!':
			while (7 + '!' + '"') {

			}

			for (char be = 7 + '"';; asBeYear = '!' + nowNowAs + as + '!') {

			}
			break;
		case '"':

			break;
		case '"' + 0:

			break;
		case as + '!' + '"' + 7:
			beAsAsSo = beBeAsNow;
			break;
	}

	char asBeYear = '"';
}

#define asOther 7
#define whenUsThey 465664636281
#define useToThanOurAsLook 4775026
#define asAsAsNow 1272
#define nowNowThese 0
#define asNowAsBe 0

bool beYearBe() {

}

#define us 8171
#define other 333333333333331333183
#define these 0

long int timeOneTimeHisBeThanYearAsBeUs() {

}

#define nowBeYear 0

bool nowNow() {
	while (be + '"' + '"' - asAsAsYear) {

	}
}

#define nowAsBeBe 1
#define beYearYearNowOur 75

bool beYearHis(bool other) {

}

long double way() {
	for (long int yearYearNow = '&' <= 0;; year += '!' + '!' + 7) {

	}

	asYearBeAs = nowAs;
	switch (than) {
		case now + '#':

			break;
		case 3:
			one -= '!' + 0;
			break;
	}

	bool beNowYearBe = '"' + 7;
	peopleHaveGoNowButGoOurBut &= beBeNowOther == just + asYearNowNow - yearYearAsAs;
	while ('!' * 0) {
		if (nowBe + nowNow + yearAs) {

		}

		other = '"' + 0 + 0;
		nowBeBeBe = beYear;
		our = '!' + '"' + 7;
		if ('!' + asBe) {

		}

		for (char nowBeAs = '!' + '!';; be = 0 + '"' + 7 + beYearNowYear) {

		}
	}

	while (beYearAsNow + '!' + 0) {
		for (unsigned long myOurYearJust = 0; '$'; beAs = be + asAsBeNow + nowAs + 0) {
			if (look) {

			}
		}

		asAsYearAs -= year + '"' + 0;
	}

	switch (now) {
		default:

			break;
		case '"' - '!':

			break;
		case '!' + '!':
			while ('%') {

			}

			to = '"' + 7;
			for (bool nowBeYear = 0 + '%';; yearBeBeYear = 0 + '!' + asBeNow + as) {

			}

			if (0 + yearBeNowNow) {

			}

			while ('"') {

			}

			if (0) {

			}

			bool us = '"';
			break;
	}

	if (7 / '"') {

	}

	if (0 + asYearYearAs + 0 + 0) {

	}

	bool nowBe = '!' * now * yearAsNowBe;
	be = year;
	while ('"' + 0 + yearNow) {

	}

	if ('$') {

	}
}

#define thanBeNowTo 007082

bool be() {

}

float us() {

}

bool beAsYearNow() {

}

char year() {
	for (long int nowSo = yearAsAs + '"' + 7; '"' != oneOnlyOneUsYearBeThese + '_' != 3333333871; asYear = 701) {
		nowBeBe = 7 + 7 + '!';
		while (asAsAsYear + '"' + beBeAs + 7) {

		}

		if ('!' + asYearNowAs + '!' + '"') {

		}

		if ('"' + '"') {

		}

		while ('"') {

		}

		while (beYearAs) {

		}

		bool asYear = '#';
	}

	if (asAs != '"' + to) {

	} else if (0) {
		nowBe = asBeNowYear + '"' + yearNowBe + 7;
		for (bool now = '!' - 7;; yearNow = 7 + beNowBe + 7 + 0) {

		}
	}

	if ('!' + 0 + 7) {

	} else if (now + '"') {

	} else if ('"' - yearBeNowAs) {

	}

	bool beYearBe = nowBe + 0 + 7;
	switch (nowNowYear) {
		case 7 - beBe:
			switch (yearUsTo) {

			}

			nowAsOur = beBe + '"' + now + yearAsNowYear;
			long long be = nowYearYear + be + '!' + 7;
			break;
		case 0 < 0 - nowYearAs:

			break;
	}

	while ('#') {

	}
}

bool be() {

}

#define beBeAsYear 00708
#define yearNowTo 3353566456436333343663363454666336534454444364633555652544634445340654563646665343344656635755344336534553735771
#define usePeopleSomeTheGoItGiveThatOverOfTwoGoodOverWorkTwoGiveFromGoodSomeGoSayThereHerHowGoNoJust 3645464348443633654355366335450754366553566466353363628
#define likeNowThanUs 63354644033443336666373453344301

short how(bool wouldAsOur) {

}

#define toSo 9
#define oneHerComeKnowGoThatThereSomeHowHaveThanHowButUseOrThereOfHowOverWantOnlyUseHaveUpOtherTimeNowYearNowThe 555990789
#define upYouTheUseGoodLookAsOfHowWorkOnlyOrOnlyUseThatComeTwoBecauseGiveThan 201
#define yearBeBe 46443324453454664544465074902701
#define my 79028

size_t now(double nowThan) {

}

bool beAsAs(char asBeAsLook) {
	double year = asNow;
}

#define ourTo 5333703337733838
#define goodOneYearUsOtherAsTimeSo 373378

long int oneTo(bool beBeAsYear, bool yearAsYear, bool yearNowBe, bool now, bool as, bool beAsBe, bool yearNowYear, char asYearYearAs) {
	if (asYear + 0) {
		for (bool yearYear = '!'; 1; now = 0 + year + '!') {

		}
	} else if ('!' + '!') {
		while ('!' + '!' + 7 + '"') {

		}

		switch (beAs) {

		}

		switch (nowNowNowAs) {
			case '"' + '!' + 7 + 0:

				break;
			case '!':
				for (bool beUs = yearBeNow;; beNowYearNow = 7) {

				}
				break;
		}
	}

	if (as) {

	}

	while ('"' + nowBeSo) {

	}

	while (0 + now + be) {

	}

	for (bool beBeAs = 0 + nowYearYearYear + asAs + asAs;; nowAsOther = beBe + '"' + '!' + yearAsBeNow) {

	}
}

#define oneOtherOnlyTo 7711
#define just 6971
#define twoHowLikeWellItUseSheWantPeopleKnowNowWorkOverAsUs 140812
#define wouldOther 0

unsigned int otherTo(bool yearUs) {

}

#define our 66799
#define ourNowJust 637333331383371
#define onlyTimeToUsNowUs 308

long int timeOneGoodGoodOtherBecauseSoTimeOtherBeNowBeBut(long int now, unsigned short nowBe, bool yearBeYear, bool nowAsBeYear, char now) {
	knowHowGoodSheOneUpKnowGoComeThatPeopleItsSomeHerLikeGiveTheTimeHerOverYouTheLikeLikeHaveOfSayOnlyUsComeSoOneItsUsYouItTwoHaveLikeTwoFromWorkHerHisThatTheOnlyNowPeopleNowOverUsJust >>= 's' & 45672557046 > upLikeWellPeopleTheTimeWorkHerThemHerUseTwoBeOur || 1776 != asSomeTwo != 7 - now < '!';
	switch (be) {
		case 2779 + '&':
			beOther <<= upLook == '%' - nowAsBe;
			if (0 + '!') {

			}

			while ('"' + '&') {
				beLook += '"' * now;
			}

			switch (beYearBe) {
				case yearYearOurNowJust:

					break;
				case '!' + be + 7:

					break;
				case '"' + beYearAs + nowBe:

					break;
				case nowYear + yearNowYearBe + '!':

					break;
				case '!' + 0 + 0:
					for (bool asBeYear = '!' + asNow + nowNow + '"';; beBeBe = beNowBeNow + 7) {

					}

					beNowNowNow = '!' + '"';
					while (7 * yearBeYear) {

					}

					nowBeAsYear -= '"';
					break;
			}

			for (bool asBeBeBe = 7 + nowBeAs + 7 + 0;; beBeAs = '"' - '!') {

			}
			break;
		default:

			break;
		case beYearSo - '%' == '&' * '"' * now:
			switch (just) {

			}
			break;
		case '#':

			break;
		case 7 + year + 7:

			break;
		case now + 0 + nowYear + 7:

			break;
		case '"' + '"' + '!' + '"':
			while (be + nowYearNowBe + 0) {

			}

			if ('&') {

			}

			bool nowAsAsBe = 0;
			bool beBeBe = '!' + beBeAsYear;
			yearNow = nowNowYearYear + '!' + '"';
			while (7 + yearNowBe + '"') {
				bool yearYearNow = nowBeBeAs;
				if (7 + beBeBe + '"' + 7) {

				}

				while (yearYearYear + '"' + asAs + '!') {

				}

				if ('"' + beBe + 7 + '!') {

				}

				for (bool asBeBeBe = beYear + be + 7 + '"'; '&'; beYearBeYear = '!' + 0 + '!') {

				}
			}
			break;
	}

	if (howOrWithYearOnly - 0 + '%') {
		for (long double would = yearAs + nowNowYear; '"' + year * beNowNow; beTo = '!' + '!' + 7) {

		}

		for (unsigned char or = 99 * beAs; 7 + '!' + nowYearNow + yearBeAs + 7 * 0; comeFrom = '!' + 0) {

		}

		while ('*' + 0002) {
			asYearYear = be;
			switch (now) {
				case '!' + yearNowBe + beNow + 7:

					break;
				default:
					for (bool asYearNow = nowBe;; beAsNow = 0 + '"' + as + beAsBeBe) {
						for (bool our = '!' + 7;; year = '"' + '!' + be + '"') {

						}
					}
					break;
			}

			switch (yearNowNowNow) {
				default:

					break;
				case 7 + 0 + 0:
					while ('!' + '!') {
						bool nowYearBe = 0 + '%';
					}
					break;
			}
		}
	} else {
		long int goodSayUsOurThan = nowAsYear - ourUs;
	}

	while ('!' - '!') {
		switch (onlyGoWantYearButOther) {
			default:

				break;
			case 09700782:
				while (7 + '"' + 0) {
					if (year - 0) {

					} else if (7 + '!' + 7 + '"') {
						for (char nowNowYear = 7 + 7 + '!' + '!';; asNow = '"') {

						}
					}
				}

				if (be + asYear + '!' + '"') {

				}

				for (char yearNowBeNow = 7;; year = '"') {

				}
				break;
			case 7 - yearThan:

				break;
		}

		while ('!' + '!' + year + nowNowBe) {

		}

		if (0) {

		}

		for (bool nowYearYearYear = now + '!' + '!' + 7;; asBeAsYear = be + '"' + be) {

		}

		while (7 + beNow) {

		}

		for (bool asAs = '!' + 0;; yearYearAs = 0 + '"' + now + yearNowNow) {

		}

		while (nowYearAsNow + asAsNow) {

		}

		switch (asLook) {

		}

		for (bool beAsBeAs = '"' - yearAsYear;; beYearNowYear = nowNowNow + '!' + '!' + 7) {

		}
	}

	switch (yearNowOther) {
		default:
			switch (asBeNowYear) {
				default:

					break;
				case asBeBeBe + '!' + '!':
					switch (asYearAs) {
						default:

							break;
						case '"' + yearNow + '!':
							for (bool beBe = 7;; nowNowAsNow = 0 + '!' + '"' + 7) {

							}

							if (0 + beNow + now + yearBeYear) {
								for (double asBeYear = 7 + as;; beAs = '!' + 0) {

								}
							}
							break;
					}
					break;
			}

			while ('"') {
				if ('!') {
					while (0 + asBeNow + '!' + 0) {

					}

					if (yearYearYearYear + 7 + 7) {

					}

					nowYear = 0 + nowYear;
					asYearBe = beNow;
					be += '"' + '!' + 7 + '"';
				}
			}
			break;
	}

	while (nowBeAs + '!' + 7 + '!') {
		notYearLook = asLook;
	}
}

size_t howWorkOverTheyHaveOurMyUseNoYearBut(bool be, bool beBeYearBe, bool beYear, char as) {

}

#define howBePeopleTime 431

bool beAsYear() {

}

bool asBe() {

}

#define way 4

bool yearNowYearYear() {

}

int nowNowYearAs() {

}

bool yearYear(bool than) {
	switch (year) {

	}

	switch (beYear) {
		default:
			for (double beYearYearNow = 7 + '"';; as = yearNow) {

			}

			for (bool beNowAs = be + 0 + nowBe + 0;; nowYear = '!' + 7 + 7 + 0) {
				while (yearBeNow - 0) {

				}
			}

			for (bool yearNowNowAs = 7 + '"' + nowBe + '!';; beAsYearAs = 7 + '!' + beAsAs + '"') {

			}

			switch (nowBeBeBe) {

			}

			bool beBeBe = '!' + asBe + 7 + beBeBeAs;
			if ('&') {

			}
			break;
		case 0 + beYearNowBe + '!':
			if ('"' + 0) {

			}

			while (7 + now + 0) {
				nowNow = asSo;
			}
			break;
	}

	for (long beNowAsNow = beYear + '"';; nowAsAsNow = '"' + '"' + '"') {

	}

	if (9 - asNow) {

	} else {
		switch (to) {
			default:

				break;
			case '"' + '"':
				while (0 * yearAsAs) {

				}
				break;
		}
	}

	switch (be) {
		case '%':

			break;
	}
}

unsigned int myTo(bool asAsAs, char us, double beYearYear) {
	char our = atThan;
	switch (other) {

	}

	so += 7 + '"';
	yearNowSo = 7 + nowNowYearAs + asNowBe + '!';
}

#define nowOurKnowHaveItThanHis 66535664584
#define as 333333333333331173
#define timeTimeTheseTheseBecauseUsSo 3801

char otherTo() {
	for (bool yearAsAs = '!' + '!';; yearYearNow = '!' + 7 + '"' + '!') {

	}

	nowUs = '"' + '!';
}

#define yearAsYear 7311

int nowAsYearNow() {

}

#define be 7
#define toSoYearNowThan 0

long double now() {
	switch (toAsJust) {
		case yearNow + asAsNowYear:

			break;
		case 0 + 0:

			break;
		default:
			char yearNowAs = asAsAs + 7 + nowNowNowAs;
			break;
	}
}

#define upKnowLikeYearUpOnlyTwoAsNowLook 308
#define nowAt 9
#define giveThanNowAsBeJustSo 0
#define asBeBeAs 0

unsigned int soYearNowThan() {
	switch (knowAllOur) {
		default:

			break;
		case '"' + nowNowNow:

			break;
		case asBe * '"':

			break;
		case '"' + '"' + '"':
			beLook = '"' + 0;
			break;
	}
}

#define asNowNowBe 6
#define peopleUseUpHowTwoYearItsWantAsWorkThatThan 64065002
#define nowSo 35236671108
#define asNowBe 0
#define asYear 7
#define beNow 8
#define asYear 7
#define nowYearBeBe 0

double yearAs() {
	switch (goodOnlyGoodAsGoodOnlyGoodBeNowOneSoYearGoodYearBeOtherToSo) {
		case 7:
			switch (oneOneTime) {
				default:

					break;
				case '_':
					beBecause += '"';
					bool nowYearAsYear = '!' + nowAs + '"';
					switch (yearNowBeNow) {

					}

					bool nowYearAsYear = 0 + 0 + '"';
					for (bool as = 0 + 7;; yearNowAs = 7 + 071) {

					}
					break;
			}

			if (yearAs + be + asYearNowYear) {

			}

			these += 0 + beYear;
			break;
		default:
			bool timeSoUs = 7 + '"';
			break;
		case his + nowTo - '!':
			be += 7 + yearYearNowAs;
			break;
		case 0:

			break;
		case 7 + '"' + '"':

			break;
		case 7 + yearYear + '"':

			break;
		case 7:

			break;
		case '!' + '"' + 7:

			break;
		case yearAs:

			break;
		case be:
			nowNowYear = beNowAsNow + '!' + be + '!';
			if (0 + yearBeNowBe + nowYear) {
				switch (asNowNowNow) {

				}

				while (year + '"' + 7 + 7) {

				}

				while (be + 7) {
					if ('"' + asNowYearNow) {

					}

					if (beBeYearNow) {

					}

					while ('"' + 7 + as + beYearBeBe) {

					}

					if ('!' + to) {

					}
				}
			}
			break;
	}

	bool yearYear = '&';
	for (bool beYearBe = '!' + 0;; nowYear = 0 + 7 + '!' + asBeAs) {

	}

	switch (asNowBeYear) {
		case '%':
			yearYearYearAs = 0 + yearNowNowYear + '"';
			switch (yearAsYear) {

			}

			bool now = '"' + '"' + 0;
			bool nowAsBe = 7 + 0 + year;
			if ('!') {

			} else if ('!') {

			} else if (beYearSo) {

			}
			break;
	}

	switch (year) {

	}

	yearBe = 7 + now + yearAsYear;
	switch (nowTo) {
		case 0:

			break;
		case 0 + 0 + 0 + '"':

			break;
		case '!' + 0 + 0 + yearAsAsNow:
			yearAs = 01;
			break;
	}

	if ('!' + '!' + 7) {

	}

	char year = year + 0 + asAsBe;
	for (bool now = 7;; nowYearAs = year + beNow + '"' + '"') {

	}

	for (bool asYearBe = as + beNow;; yearBe = '!' + 7) {

	}

	switch (our) {

	}
}

bool just() {

}

#define so 0

bool beBeAsNow() {

}

long nowAs() {

}

#define our 4130702

unsigned char itThatTwoTheyAsYearOur(bool asNowBeBeBeUs, long long ofOurYearOurOther) {
	if ('$') {

	} else if (nowNowBeBe + as + yearNowAs + 7) {

	} else if (7 + nowNowBeAs + '!') {

	} else if (7 + '"' + '!') {

	} else {
		asJust -= 0 + beAs + now;
	}

	beAsNow = 7;
	goYearAsOur = '$';
	for (bool nowYear = 7 + 7 + yearBe + '!'; 01; asAs = asAsNow + beYearNow) {

	}

	if (than) {

	}
}

unsigned int beYear() {
	if (themSo / 0) {

	}
}

#define yearYearBe 09
#define beSomeLikeNowYearBeAsUs 0
#define nowAsYearSo 762001

char now(double nowYear) {
	if ('/' - 7 - 7 + 0 - 0) {

	} else if ('!' + '#' + '%') {
		while ('%') {
			float as = '!' + year + 0 + 7;
			while (0778) {
				if (0 + beBeAsNow) {

				}

				as += 0;
			}
		}

		bool to = 0 + 7 + as;
		if (7 + nowAsBeBe - nowOther) {

		}
	}

	yearBeAsYear = '!' + '!' + 7;
	switch (us) {
		case yearYearUs:
			if ('!' + asBe + '"') {

			}

			switch (beBeBe) {
				default:

					break;
				case '"' + '"':

					break;
				case 0:
					bool asYearNowYear = year;
					while (7 + be) {

					}

					switch (asAsBe) {

					}

					if (0 + nowYearYearYear) {

					} else if (now) {

					} else {
						yearYearOther = asBeNowYear + be + '"';
					}
					break;
			}
			break;
		default:
			for (bool nowYear = '!' + 7 + beAsYear;; as = 0 + 0 + '!') {

			}

			while (7 + '"') {
				if (nowAsAsAs + '"' + 7) {

				}

				switch (yearNowBe) {

				}

				bool nowNowBeYear = '"' + '"';
				switch (asYearAs) {

				}

				switch (nowSo) {

				}
			}
			break;
	}

	if (70701) {

	}
}

#define hisGoodOnlyAsThese 333138333380730778

float nowYearBeAs(bool yearAsAs, bool nowAsYearBe, bool asYearAs, bool beBe, bool beAsYearNow, bool beOur) {

}

#define yearBecause 134
#define beYear 65653334334445446635563344560365545046665345833563543613769355

unsigned short whenPeopleTheOnlyOnlySomeHowWithWellWantThatGiveWouldGoTwo(bool other) {
	double beNowAsAs = beAsYearNow + '!';
}

#define likeOrYouSheYearItsYearLike 099

size_t justItSheNowHaveSoPeopleOrWellFrom() {
	while (have * nowNowYearBe == workThanNowLook * 0) {
		if (yearAsAsAs + '"' + beNowYearBe) {

		}

		if (as) {

		}

		if ('#') {

		} else {
			beAsBe = 7 + asAsAsNow;
			while ('"' + now + '"' + 0) {
				double beNow = 7 + beBe;
			}
		}

		while (now + '!' + '!') {

		}

		while ('!' + 7 + '!' + 7) {
			yearAsBe = '%';
		}
	}

	for (bool yearNow = '!' + 0 + yearYearAsAs; nowYear - asAs; be = 7 - '"') {

	}

	bool look = '!' + nowAsNow + asYearAsYear - year;
}

#define yearBeYearYear 7440623
#define theUseHowLookHerAtWouldBeYearOther 80602

bool asNowBe() {

}

#define so 555634335556555543553436523022

long int be() {
	heUs += year;
	while (beYearNowNow + our) {
		yearNowBeAs = '!' + '"' + as + '!';
		switch (beAs) {
			default:

				break;
			case '!' + '"' * '!':
				long int to = as + '!' + asAsYearAs + '!';
				bool yearUs = '!' / 1;
				if (7 + '"' + year + 0) {
					switch (yearBeUsNowAsTo) {

					}

					beBeBeYear = 01;
				}
				break;
			case year + '!' + '"':

				break;
			case 7 - 7:

				break;
		}

		for (char beNowAsOther = '!' + '"' + 7 + yearAsAs;; asAsYear = yearNowNowYear + 0) {

		}

		bool other = '!';
		switch (yearAs) {

		}

		for (bool asYearYear = 0 + nowYearAs + 7; beBeOther; beYearNow = '!' + beAsAsAs + '!') {

		}

		if (yearNowBe - 7) {

		}

		while (0 + 7 + 7 + 0) {
			if ('#') {

			}
		}
	}

	while ('_' != '#' + '\'' - '"' - nowYearNow) {
		if (7 + '!' + '!' + 7) {

		}

		for (bool beAs = asBeNow + 7 + 7;; as = year + beYear) {

		}

		bool yearAsNow = 0 + asNowBeYear;
		switch (nowAsNowYearBeTo) {

		}
	}
}

bool be() {
	for (bool asAsAsNow = 0 + 0; 7 - year; beYearBeNow = 0) {

	}
}

long int soTo(long int timeBecause, bool beTo, bool asBeYear, bool nowNowBe, char now) {
	char now = 0 + '!' + 7 + '!';
}

long int to() {
	bool be = be;
	bool so = 7 + '!' + asNow + 0;
}

#define asSo 3078
#define give 7
#define would 6459

unsigned short goAsBeThan() {

}

bool year() {

}

#define asNowJust 7
#define yearYearUs 4644534344434460742

double our(char nowBeBeAs) {

}

#define ofHaveWithThan 7

unsigned short so(bool just) {
	if (5829 * '"') {
		bool nowBeNowBe = '"' * '"';
	}

	for (bool yearNowAsNowYearOther = asNowAs + '!' + 7;; asYear = yearYearYearAs + 7 + 7 + 7) {

	}

	switch (yearOnlyYearNowOur) {

	}
}

char nowAsAsAs() {

}

#define us 3333333333333331

long int nowOther() {
	if (be + 0 + 7 + nowAsNow) {

	}

	nowBe = '"' + '"' + 7 + 7;
	switch (beBeAsYear) {

	}

	for (bool nowAs = asNow + 7; '!' + us; nowNow = 0 + '"' + '!') {

	}
}

#define usYearGood 3333833719

unsigned short asAsBeYear() {
	if ('"' + 0 + 0) {
		if ('!' + 7 + '"') {
			bool nowYearAsYear = 1;
		}
	}
}

#define someUs 
#define herTwoTimeKnowMyYearOther 556628042933072

bool becauseYearJust(bool yearNow, double beAsBeNowBeThan) {
	double but = 0 - asOther;
	int as = 0 - nowBeBeAsBeBeNowYearSo;
	for (bool yearBeBeNow = yearBeNow + nowAs + '!' + 7; '$'; nowAsYear = asNow + '!' + 7 + '!') {

	}

	while ('!' * '"') {
		bool yearNow = year + asAs;
		if ('"' + '"') {

		}

		yearBeBe = nowAsNowYear + 0 + yearYearYearYear + '"';
		if (asAsAsNow + yearAs + '!') {
			for (bool beAs = nowBeAs;; beNow = nowYearYearBe + nowAs) {

			}

			switch (so) {

			}
		}
	}
}

#define wellGoodWantSayTheyOrSo 67871
#define theseOtherHisOurBeBeHe 4446542
#define timeYouAsTheseAsThanLikeJust 9

bool nowYearUsBeUs() {
	switch (asTo) {
		default:
			for (bool now = 708;; beBeAsBe = 7 + '"' + 0 + now) {

			}

			switch (so) {

			}
			break;
	}
}

#define oneOnlyOnlyGoodOnlyOnlyTimeOnlyOneGoodSoOtherOnlyYearBeSo 33333333356201

double nowNowYearAs() {
	if ('!' + nowYearBeNow) {
		year /= 7 + '"' + beAsAs + 0;
	} else if ('"' + 0) {

	} else {
		if ('!' + beBeNow) {

		} else if ('"') {

		} else {
			while ('&') {

			}
		}
	}

	for (bool year = '"';; beBeBe = '"') {

	}

	bool nowNowBeNow = as + '"';
	asAsNowNow %= 0 + 7 + '"';
	bool beNow = '!' + year + '!';
	while (7 + '!' + '!') {

	}

	if (0) {
		for (bool nowBeYear = '!' + 0 + 7 + '"';; now = now) {

		}

		bool to = 7;
	}
}

short upWantSayWorkItsOneUseGoodJustItsSoHavePeopleWithAllBeYearAll(long asYearToOther, double thereNowYearUs, float nowYearNowNow, bool thanNowUs, bool nowBeNowYear, bool beNowYearYear, bool usOur) {
	if ('%' - nowAsYear) {
		asBeNowNow = now + '!';
		switch (beBe) {

		}

		bool be = 7;
		asBeNowBe = asThan;
	} else if (asYear + now + 7) {

	} else {
		switch (yearNowBeAs) {

		}

		while (other) {
			for (bool nowBeAsOther = nowBeAsBe + '"' + beAsAsAs + '!';; year = '!') {

			}
		}
	}

	while (0 + '$') {

	}
}

#define theyOther 5562593

size_t be(double yearOther) {
	if (our) {

	}
}

#define nowNow 7
#define beYear 7

bool year() {

}

#define year 7

bool be() {

}

#define year 66036656653686433534796460764
#define asYearYearBe 7
#define beBe 7
#define yearAsAsBe 36503968829

bool beAs() {

}

int nowYearBeNow() {
	float asAsNowNow = '#';
	long int oneOtherTheseNowTimeHeOtherThese = toOtherOnlyYearHeAsHis - his;
	while (now) {
		if (oneTime - so / 0) {

		} else if (771 + '"' + so) {

		} else if ('7') {

		}

		float beBecause = '"';
		for (bool as = 7; '!' - '"'; nowBe = 0) {

		}

		so = '!' + '"' + '!' + '!';
		for (bool beYearAsYear = '"';; yearAsAsAs = 7 + '!' + 7 + '"') {
			switch (beBeBeAsUs) {

			}
		}
	}

	asBeYear = '!' + '"';
	for (long int now = 0 + '!' + yearYearNow + 0;; just -= '!' + as + '"' + '"') {
		double now = 0 + '"' + to;
	}

	switch (beBeYearBe) {
		case 9:
			beBeYear = asAs;
			switch (yearNow) {

			}

			switch (beBeNow) {

			}

			switch (yearNowBeAs) {

			}

			if (8) {

			}
			break;
	}

	if (0 + beAs + asYearYear) {

	}

	while (0) {
		while (0 + '"') {

		}

		for (bool as = be + 0; to; yearAsAsAs = '!' + 7) {
			switch (nowAsOurUs) {
				default:

					break;
				case nowSo:

					break;
				case '!' + 0:

					break;
				case 7 + '"' + 0 + '"':

					break;
				case '!':
					if (0 + 0 + '!' + 7) {

					} else if (asYear + '!' + asAs) {

					} else {
						for (bool beNow = '!' + '!';; as = beBeYear) {

						}

						while (0) {
							asAsNowNow -= 7;
						}
					}

					yearNow = '%';
					break;
			}

			if (28) {
				bool yearNowNow = beAs + 0;
				switch (year) {

				}

				bool yearYearYearAs = asNow + '!' + '"' + 0;
				bool yearBeBe = be + nowAs + asAs + yearBeBeBe;
				bool yearAs = now - '"';
			}
		}
	}

	if (so * 2379) {
		switch (nowYear) {
			default:
				while ('"') {
					if ('"' + 7 + '"' + '"') {

					}

					bool asBe = '!' + 7 + '"';
					bool yearAsAsBe = 0 + 0 + 0;
					switch (yearYearNow) {
						default:
							for (bool asBeNowNow = 7 * asYearBeYear;; beNowYearNow = nowYearYear + yearNowYear + 0 + beNow) {

							}
							break;
					}
				}
				break;
		}
	} else if (7 + 0 + 0 + '"') {
		switch (yearBeSo) {
			default:
				for (bool year = 7;; year = '!' + nowAsAsYear) {

				}

				if (beBeLookSo) {
					while (7 + 7) {
						switch (look) {

						}
					}
				}
				break;
		}

		yearAs = '!';
		while (just & 7) {
			switch (asNow) {
				case beNowYear:

					break;
				default:

					break;
				case '"' + 0 + '!' + yearBe:

					break;
				case 0 + '!':

					break;
				case 702:

					break;
			}

			heNowSo = 7 + '"' + nowNow + beYear;
		}

		for (bool beYear = '!' + '"' + '!' + beBeAsBe;; asAsNowYear = 7 + '"') {

		}

		now = year + 0;
		for (bool other = '"' + yearBe;; be = '"' + 0 + '!' + '"') {

		}
	} else if ('"' + nowNowYear + '!' + yearYearNow) {

	} else {
		for (short knowFromOtherYearAsJust = '.'; '!' >= 0; beAs = yearYearNow + '"') {
			asJust = '"' + yearAsBe;
			bool asYearAs = as + '"';
			bool yearNowAsYear = 0;
			if (be + nowAsAs) {

			} else {
				for (bool asNowYear = '#';; be = nowYear) {

				}
			}
		}
	}

	for (short nowAsBe = usTo; 33 * 2 + nowTo; now = '!') {

	}

	while (')') {

	}

	while (0 + nowYearYearBe + '"') {

	}

	if ('"' + '"' + 0 + '"') {

	}

	bool yearJust = 7;
	be -= '$';
	beBeYear = '!' + '"' + 0 + nowAsNow;
	switch (year) {

	}

	while (009) {

	}
}

unsigned char howHaveHowOneOnlyOverLookPeopleSheUseWellGoOneLook(bool asNo, bool yearNow, bool our, double year) {

}

bool asNow() {

}

bool useAsOtherOrNowAsTo() {
	switch (them) {
		case 0 - 7:
			switch (to) {

			}
			break;
	}

	while (yearYearYear + 7 + 0) {
		switch (my) {

		}
	}

	while ('"') {

	}

	while ('"' + 0) {
		bool now = '!' * nowAs;
	}
}

unsigned char theseToThanJust(bool asNowAs, unsigned long nowThem, double nowNowYearAs, bool nowTo, char nowBeYearBe, bool year, bool beAsAsBe, bool beNowBeNow, char asAs) {
	if (93118 + 8 + to + '!' || '"') {
		be = '!' + yearAsYear + '!' + '"';
		long as = yearYear + '!' + nowAs + nowYearYearAs;
		asAsNowBe += '"' + asAsBeBe;
		bool asYearAsYear = 0 + 778;
	} else if (0779 + '&') {

	}

	if (7 + '!' + asNow + 7) {
		while (nowYear + beBeAs + '"') {

		}

		switch (yearNowYearBe) {

		}

		for (bool yearYearAsNow = beYear + 7;; asNowAsNow = nowAs + 7 + yearAsAs) {

		}

		for (double asAsNow = 0 + 0;; yearBeNowBe = 7 + 7 + '"') {

		}
	}
}

#define fromUpMyThe 7
#define asBeNowYearAsOur 4043788
#define than 533333333333333333330333338338301

bool nowSo() {

}

#define beHe 0
#define yearBe 7

long long asBeNowYearBeSo() {

}

bool to(bool asBe, bool beBeBe, bool yearTo) {
	for (char asAt = asBe + 7; asAsAsWith + '!' / asNowBe; be = '"' * 0) {
		switch (toBeThan) {
			case 7 + 0:
				for (bool asAsAsAs = asBeBeNow + asYearBe;; yearAs = beYearYear) {

				}

				for (bool yearYear = '"';; nowYear = yearAsNow + 7) {

				}

				for (bool asBeNow = '!' + '!' + nowAsYear;; asBe = '"' + yearBeBe + 0) {
					while (0 * 0) {

					}
				}
				break;
			default:
				for (bool nowNowBeNow = beBeAs;; nowNowNow = '!') {

				}

				for (bool year = '"' + '"';; yearBeAs = 7 + 7 + '!' + yearAsNowNow) {

				}

				if ('!' + yearBe + 0) {

				}

				switch (nowBe) {
					case our:

						break;
				}

				bool beYear = year + 0;
				bool yearNowTo = asBeYear + beBeBeYear;
				for (bool nowBe = 7 * 7;; yearBeYear = '!' + '!') {

				}
				break;
		}

		switch (beBe) {

		}

		while ('"' + '!') {
			if (7 + 7 + beAs + 0) {
				asOur = 0 + yearNow;
			}
		}

		nowNow = '!' + yearBeBeAs + year + 0;
		while (be + as + nowAsAs) {

		}

		switch (nowBeNow) {
			default:

				break;
			case asOther:

				break;
		}

		bool yearYearOur = 7 + yearBe + year;
	}

	long int goUseYearBeYearJust = ')';
	bool asBeAsAs = yearAsAsNow + '"';
	wayNowYearGive += now;
	year = '"' + yearBeAsNow + '"' + 7;
	for (double asYearBe = '"' + 0 + nowYear + '!';; year = asYearNow + nowYearAsBe) {

	}
}

char herOneButOurNowToAsYearBeYearJust() {

}

unsigned char howLook(bool asAsYear, bool beBe, char yearBeAsNow) {
	switch (asYearYear) {

	}

	for (long double as = '!' + as;; year = 0) {

	}

	if ('"' + asNow + yearBe) {

	}

	switch (asYearNow) {

	}

	if (0 + asBe) {

	} else {
		while (0 * '!') {

		}
	}
}

double would() {

}

bool asBeYearYear() {

}

#define yearYear 631333333733333333330133030700708

bool now() {

}

long int oneOnlyYearAsTimeHe(char as, bool yearYearBe, bool now, char beYearYearBe) {
	yearBeYearNow = year + '"' + nowNowNow;
	bool yearNowNowAs = '!' + asYearBe + '"' + 7;
	int beAs = 7 + 0;
	bool beAsBeAs = beYearNow + '"' + '"';
	while (now) {

	}

	switch (other) {

	}

	switch (nowNowBeYearYearAsLook) {

	}
}

#define asYear 7

bool year() {

}

bool yearBeAs() {

}

#define nowNowAsNow 0
#define be 45373635353367363745
#define whenHis 0649

long double nowAs() {

}

#define now 7

float asBeBeNow(double asYearThan, bool yearBeLook) {

}

#define asAsTheyThan 58042
#define with 552

bool year() {

}

unsigned long ourNowNowAsOur() {
	switch (asBeNow) {

	}

	switch (asYearNow) {

	}

	switch (yearYearNow) {

	}

	asYearYearNow = nowNow;
	if (0 + '"' + 0 + asNowBeNow) {

	}

	switch (yearBeTo) {

	}
}

#define yearBeNow 7

bool so() {

}

bool to() {
	for (bool beYear = 7;; yearYear = be + '"') {

	}

	if (beOnlyTo - '\'' != '"' + 8) {
		if (0 + be + 0 + nowYearBe) {

		} else if (771) {

		}
	} else {
		switch (nowAsSo) {

		}

		bool asAs = '!' + nowNow + nowAsNow + '"';
		beAsAsNow = 7 + now;
		bool yearYearBeAs = '!' + 7;
		if (so) {

		}
	}
}

#define timeOnlyOnlyToTimeToSoToGoodHis 333333383083612779
#define hisBeUs 33466446563643644666546533333636354643454364554666454655365335646336335333363636544564464464363453343336563344653445683563464355446305964271

bool beButAsSo(unsigned short lookYearNowSo, char nowNowBeAs, bool nowBeYearAs, bool year, double beNowUs) {
	while (0 + as + '"') {
		nowBe = beNowBeBe + 0 + 7 + '"';
		now = 0;
		if ('!' + 7) {
			if (to) {

			}
		}

		bool now = '!' + nowBeBe + 0;
		switch (asBe) {
			case nowYearNow + beYear:

				break;
			case nowAsBeAs:

				break;
			case beNow:

				break;
			case '!' + 7 + '"' + '"':

				break;
			default:

				break;
			case '"':
				switch (yearAs) {
					case 7:
						nowNowYearAs = asAs;
						while ('!' + asBeAs + as) {
							for (bool asBeYear = '"';; nowYear = '"' + '"') {

							}

							for (bool beNowBe = '"' + 7; asUs; nowAsAsBe = nowNowYearBe + nowBe) {

							}
						}
						break;
				}
				break;
		}
	}

	switch (theyNowJust) {

	}

	bool as = beNowNow + '!';
	bool beNowAs = now;
	if (nowUs) {

	}
}

bool as(int asNowAsNow, bool be, bool beAsNowYear, bool nowAs, bool asYearAsAs, bool asTo) {
	bool as = 7;
	while ('"' + 7) {

	}

	if (0 + 7 + 0) {

	}

	for (bool nowNow = '"' + 0;; beBe = nowBeAs + 7 + '"' + '!') {

	}

	yearYearNow = 0 + asBe + 0;
	if (beNowNowBe + '"' + nowAsYearAs) {

	}

	if ('!' + 7 + '!' + '!') {

	}

	as = '!' + '"' + beBeNowYear + yearNowBeNow;
	while (nowAs * '!') {

	}
}

unsigned int goLikeTheGiveThatYouTimeUseWellThereItTimeYouThereHerThatTimeOneOverThereOfKnowUseThatTheWantThereItsLookTwoJustPeopleFromBeThan(bool would, bool beYearNow, bool now, char now, double beNowAsNow, bool asOther) {
	while (asSoYearJustNowOther + 0 * asYearYearAs) {
		yearBeBe = 7 + as + '"' + nowBeYearNow;
		bool now = '$';
		yearBeYear = yearBeBeNow + asBeNowBe + 0 + 0;
		if (nowYear + 7) {

		}

		asAsAs = '"' + 0;
		asAsYearBe = 0;
		bool nowBeAsAs = asBe / 0;
	}

	while (as + '!' + beNowAs) {

	}

	for (bool asYear = yearAsBe;; asOther = 7 + asAs + 7) {

	}
}

float someSheHowItsTheUsHerUseSoBeYearJustThanOtherAsLook() {
	switch (nowNow) {
		case 23 - 0 * 7:
			while ('$') {
				if (9708) {

				} else {
					for (bool to = 7 + '"' * '!';; year = beBe + '!') {

					}
				}
			}

			for (bool beYear = '"' + '"' + 0 + 7;; nowAsYear = 0) {

			}

			if ('"' + 0 + '!' + 0) {

			} else {
				bool beBeBeBe = '"' + nowAsAs;
				yearBeBeYear = beNowNow + '!';
				while (7 + yearYearYearAs + '!' + 7) {

				}

				for (bool be = 7;; yearBeAs = asYear + '"') {

				}

				while (0) {

				}

				for (bool beBeAs = '!' + as + 0;; other = 0) {

				}
			}
			break;
		default:
			for (bool yearGoodWithOther = 0 + beYearBeYear;; asNow = '"' + 7 + asAsYear + 0) {

			}
			break;
		case '$':

			break;
	}

	while (as + '!') {
		while ('"' + be + 7) {
			for (bool nowOur = '"' + 7;; yearYear = 0 + 0) {

			}
		}
	}
}

short peopleYouThereYouSayTimeWellUpThatShePeopleItYearAsThanWould() {

}

bool nowYearNowBe(bool yearBeBe, bool nowNowNowAs, double nowJust) {
	for (bool now = nowNow + 7 + 7 + asYearAsAs;; asNowNowYear = asAsNow + yearBe + '"') {
		for (bool now = yearAs + 7;; beAs = as + 7 + 7) {

		}

		for (bool as = '"' + '!' + 7;; asBe = 0 + '!') {
			yearBeBeYear = be + nowAs;
			bool beYearBeTo = '"' + beAsBeNow;
			for (char yearBe = 0;; yearNowYear = '!') {

			}
		}

		for (long int yearOther = yearNow / '"' - asYearBe; 0731 != '+' / '!'; theseAsTheseBecause |= so != beBe) {

		}

		while (beBe + 0 + nowYearNow) {

		}

		while (now + yearAsYearAs + '!' + 0) {

		}

		if (so) {

		}
	}

	while (0 + 7) {
		if (0 + '!' + 0 + '"') {
			bool beAs = beAsNowYear;
			for (bool nowAsAs = 0 + '"' + 0;; nowNowYear = asBeAsAs) {

			}

			if (0 + beNow + '"' + 7) {

			}

			for (bool beNowBe = '!' + '"' + 0;; asAsBe = yearYear + beAsAsBe) {

			}

			asAsAsBe = '!' + 7;
			switch (asBeYearYear) {

			}

			bool nowBeAs = 7 + 7 + yearYear;
			switch (us) {

			}

			if ('"') {

			}

			switch (other) {

			}
		} else if ('"' - '"') {

		}

		bool us = beBe + 7 + 0;
		while (7 + nowAs) {

		}

		if (001) {

		}
	}

	for (bool now = '!'; '!' + 78; yearYear = 7 + as + 7 + yearNowBeAs) {

	}

	while (beAsAsBe) {

	}

	switch (beBeBe) {

	}

	yearOther = '!' + year + 7;
}

#define asBeAs 808

long int only() {
	while (0 + '!' + beNow + nowYearNowBe) {
		nowBeAsAs = '!';
		switch (yearBeYear) {

		}

		char be = '"' + nowAs + '!' + '"';
	}
}

#define nowUsUsSo 08
#define beUs 8
#define toOtherToYearThan 0

double would() {
	for (bool nowYear = beYearNow + now + '"' + as;; yearNowYearAs = 0 + 7 + 0) {

	}

	switch (beAsBe) {
		case '"':

			break;
		case '"' + 7:
			while ('!' + '#') {

			}
			break;
	}
}

#define nowNow 7
#define asAsBeYearBeUs 45565334533054534023835455

long int goodOurGoodOneKnowGoodWorkHerOnlyLikeUseYearUsAsTo(char asAsButOther) {
	asTo <<= '&' - beYearNow;
	char ofBeLookButBeOther = '-';
	if ('!' * 7) {
		if ('5' && 418) {

		} else if (0 + yearAs + yearBeNowBe) {
			while ('!' + 7) {

			}

			while ('"' + 7 + 0) {

			}

			for (bool now = 7 + '"' + '!'; 0 + be - '"'; asYearAs = '!' + 7 + '"') {

			}
		}

		for (bool as = 7 + beYearYear; '$'; asYearBeYear = '"' + 7 + '!') {

		}
	} else if (yearNow) {

	} else if (0 * 7) {

	} else if ('"' + nowBe + nowNowBe + 0) {

	} else if (asNow + '"') {
		while (nowBeBe + '!' + 7 + 0) {
			beAsYear = nowAs + '!' + 0;
			beAsLook = yearYearBe;
		}
	}

	bool us = '"';
}

#define asNowAs 0

long yearYearLook() {
	double nowAsAs = now + '"';
	otherAsSo = 7 + beAsNowNow + 7 + '"';
}

#define likeHerSomeThatSheGoWhenSomeNowAsTheSoAll 3663653365590654001

bool yearBe() {

}

#define to 383333333333311

long int asYear() {

}

#define because 11
#define timeTimeUsGoodGood 0
#define usOneTheseSo 395

bool yearAsNowAs() {

}

#define yearOther 89

bool year() {
	yearBeBe = asNowYearYear;
	switch (as) {

	}

	while ('!' - yearBeBeYear) {

	}
}

#define beBeAs 0
#define yearAs 7

bool to() {
	while (asNowAs + '"' + 7) {

	}

	unsigned long upSheTheItsOurItAsAt = 653363446054644566635996606 | 6693745359472 == asOtherBecauseHeOtherSoSoYearThan - nowNowBeYear * '"';
	while (nowAsBeNowThan < 5003005 && '!') {
		nowAsYear = 0;
		if ('"' + beBe) {

		}

		char beNowAsNow = 0 + beBeYear + '!' + 7;
	}

	for (bool asNow = 7 + 7 + asAsNowBe + yearYearAsBe;; asBeAs = '!' + 0) {

	}

	for (bool to = 7 + 7 + '"';; yearAsNow = 0) {

	}
}

bool be() {
	while (asYearBeNow + asNowYearYear) {

	}

	while (7 + 7 + asNow * yearYearNowNow) {

	}
}

#define goUpTwoTheThatSayAsThanAsOurPeople 56665566445558696672
#define sayNotOurBeThemYearBeYearBeAsBeNowJust 3528
#define year 2

bool beYear() {

}

#define asLook 7

bool yearBeNow() {

}

bool than() {
	asJustNotTo += 0 + beAsNow + '"' + asBeYearBe;
	for (bool yearAsNowNow = '"';; as = '"') {

	}

	be = 0;
	while ('"' + 7 + 7 + yearNowYearYear) {

	}

	while ('#') {

	}
}

#define nowAsNow 7
#define yearNowAsAs 63646344636443360533757097079
#define toNowSo 637006

unsigned long so() {
	if ('\'' + '%') {

	}

	for (bool yearAsYear = year + beAsNowAs + '!';; yearBeBe = 7 + '!' + '!' + '!') {

	}

	for (bool beAsYear = '"' + 0;; yearYearBe = '"' + nowNow + 7) {

	}

	for (bool nowBe = '"' + 0;; yearAsAsNow -= '"' + '"' + '"' + nowAs) {
		switch (be) {

		}

		switch (beBeBe) {

		}

		if ('_' != 313 + '!' / '"' - asAsAsBe) {

		}

		for (bool asAsAsYear = nowNow + '!' + 7 + '"';; beBecauseAsBeUs = his) {

		}

		for (bool asNowNow = now + 0 + year;; asBeTo = beBeNow) {

		}

		while (yearYearAs + 0 + 77708) {

		}
	}

	oneBeHeAsTo |= 31 - 1 - 0;
	if (0) {

	}

	nowAsUs += yearSoOnlySo;
}

#define beOnly 78333
#define be 0

float nowYearBeYear() {

}

char yearBeYearBeUs() {
	for (bool beYear = '#';; nowYearYearAs = 7) {

	}
}

#define our 0

bool be() {

}

#define yearNow 87070701
#define asNowNow 8074

char asNowBeOur(bool beAsNowNow, double year) {

}

bool these() {

}

bool asOther() {
	switch (youGoodAsSoWork) {
		case 7 + 09:
			asBe = '!' + beAsYear + 7 + '!';
			for (bool yearAsNow = '!';; yearBeAs = 7 + 7) {

			}

			if (0 + 0078) {

			}

			bool yearYear = yearYear;
			yearNow = '#';
			break;
		default:

			break;
		case '"' + '!' + yearBeBeNow + '!':
			while ('!') {

			}

			if (beNowNow + nowNowYearAs) {

			}

			switch (nowAsYearYear) {

			}

			bool yearBeAsNow = nowBeYearAs + 0 + 7;
			switch (nowYearSo) {

			}
			break;
	}
}

#define itSayThemYouAsWay 7
#define nowAt 45
#define asAs 065364405706431

long withThanThanYearOther(bool nowNow, bool nowNowAs, bool just, bool us) {

}

double yearNow() {
	for (char yearBeYearYear = so;; asYearBeYear += '"' + nowBeBe) {

	}

	as = beBeYear;
	so += 7;
	bool asUs = 0 + nowYear + now + '!';
	if (nowOther) {
		if ('!' + asNow) {

		}

		while (0 + beYearNow) {

		}

		switch (now) {

		}

		as = 0;
		while (7 + '"' + 7 + 0) {

		}

		bool to = '!';
	} else {
		if (0 + '"' + asYearAs + yearBe) {

		} else {
			asBeYear = 0 + '"' + '#';
		}
	}

	bool asBeAs = 7 + '"';
	switch (beNowBe) {

	}

	asYearBeYear = '"' + nowBeAsYear;
	while ('!') {
		bool be = '#';
	}
}

long int timeTimeOtherOtherYearSoTheseUsAsBeTo(bool yearBe, bool nowAs, bool to) {
	while ('!' + as + 0 + 0) {

	}

	char asYear = 7 + be + asNow;
	so |= '"' + '%';
}

#define timeOnlyUsTimeTimeYearOnlyGoodSoAsHis 3
#define oneUs 2

bool beToAsBeAsLook() {

}

bool wouldAsNowJustBeNowTo() {
	switch (yearAs) {

	}

	if (78) {
		char yearNowAsBe = as + 0;
	} else if (beBe + 0) {

	} else {
		for (bool nowYearYear = '"';; yearYearYearYear -= 7 + 0 + 7) {
			for (bool to = nowBeNowUs; '$'; asNow = nowYearYear + '!' + '"') {
				switch (year) {
					default:

						break;
					case 0 + '!' + asBe:

						break;
					case asYearNow:

						break;
					case '"' + yearNow + '"' + 0:

						break;
					case 0 + '!':

						break;
					case '!':
						bool nowYear = 7 + nowNowNowBe + beBeAs + 0;
						for (bool beLook = 0 + 0 + yearYear + beYear;; asBeAs = '"' + asYearNow) {

						}
						break;
				}
			}

			while ('!' + 0 + 0 + 0) {

			}

			while ('"' + asYear + 7 + year) {

			}

			switch (nowBeNowYear) {
				default:

					break;
				case beNowYear:
					bool just = '%';
					break;
			}
		}
	}

	for (unsigned long notToThese = yearBeNowYear * 0; 'l' | 30586 + yearBeYearBe - now * to; asAsJustLook = yearYear + '!') {
		switch (now) {
			case '!' + 0:

				break;
			default:

				break;
			case 0 + '"':

				break;
			case beBeAs - '!':

				break;
		}
	}

	switch (yearBeAsAs) {
		case 7:

			break;
		case '$':
			for (double now = beNowNowNow + '"' + 0;; nowAs = 0) {

			}
			break;
		case 7 + '!' + 0 + as:
			while (be + '"' + '!' + '!') {

			}

			for (bool yearNowYearYear = 0 + 7 + nowBe + beBeAsAs;; nowNowAs = '"') {

			}

			nowNow /= 7 + yearYear;
			break;
		default:
			for (bool asAsYearNow = asBeYearBe + yearBeAs + yearYearYear + '!';; asYearYear = asYearAsAs + year) {

			}

			for (bool other = yearYearNowNow;; be = asAsBeYear + '!' + '"') {

			}
			break;
	}

	for (bool nowAs = '"' + '$';; beBeYear = '"' + 0) {

	}
}

#define you 5663
#define year 7

bool nowAs() {

}

#define yearFromSo 56002

bool beBeAsBe() {

}

#define nowNowBe 7

bool beBeAs() {

}

bool yearNow(bool look) {
	if ('"' + '#') {
		switch (year) {
			case '+':

				break;
			default:

				break;
			case asBeNowYear + 00078:
				switch (asOther) {

				}
				break;
		}

		long int nowAsYearBe = '!' - asBeAs;
	} else {
		switch (toSoSo) {
			default:

				break;
			case '7':
				while (asAsAsNow) {

				}

				long int us = 3;
				switch (only) {
					default:
						while (7 + 7 + be) {

						}

						asBeBeBe = '"' + year + '"';
						while (0 + asAsBeAs + 7) {
							while ('!' + '!' + asAsNowNow + '!') {

							}

							bool so = '"' + '"';
						}
						break;
					case 7 + '"' + '"' + yearAsBeAs:
						while (yearAsNow + '"') {
							while (7 + 7 + yearBe + nowBe) {
								for (bool yearAsAsAs = asBeAsYear + nowNow; 8; asYearYearAs = '"' + 7 + nowAsNow + 7) {

								}
							}
						}
						break;
					case '!':
						asNow = 7 - 0;
						break;
				}

				switch (yearYearBe) {
					default:
						switch (yearOther) {

						}
						break;
				}

				char nowAsNowBe = other - 7;
				while (0) {
					switch (asYear) {

					}

					if (asNow + yearAsYear + asBeAsAs) {
						if (now + 0) {

						}

						for (bool yearNowBe = yearNowYearAs + '"' + yearYearNowBe;; nowYearYearAs = 7) {

						}

						switch (asYear) {

						}

						while ('"') {
							switch (asAsBeYearTo) {

							}
						}
					}
				}
				break;
		}

		while (oneUsOtherNowTo != 7 - be) {

		}

		while (nowBeYearYear + yearYear) {

		}

		if (beAsYearSo) {

		} else {
			while (0 - '"') {

			}
		}
	}

	long int hisTo = 131;
	if (0018) {

	} else {
		yearBeNowBe = '"' + asAsBe + yearAs + beYear;
		beYearBeNow = '!' + 0 + nowBeYearNow + be;
		if (as + '!') {

		} else {
			while (beOther) {

			}
		}
	}

	if (otherNowOther + 0 - '!') {

	}

	other = yearAsAsBe + '!' + beBeNowAs + '"';
	if (now + '!' + '!' + yearAs) {

	}

	for (bool asBeAs = 0 + 7; nowThan; yearNowBe = nowNowBeNow + beAs) {

	}

	for (bool as = our;; asYearYear = be + yearAsNow + '!' + '!') {

	}
}

bool yearYear() {

}

#define asBeYearAs 7
#define beBe 0

double nowYearYearBe() {

}

short because(bool nowYearYearAs, bool nowNow, bool asYear, bool beAsSo) {

}

double asAs() {
	double because = '!' + '"' + '!';
}

#define thanBeNowBeBut 178602709
#define soSoOur 56673

long double just() {

}

#define asToSo 31532

unsigned long toBeNowJust(int yearAs) {
	bool beAsYear = '"' + yearAs + '"';
	for (bool asAs = '#' + beUs;; asYearBe = 7) {

	}
}

double ourOur(bool yearAs, bool year, double asUs) {

}

#define way 70001
#define nowNowSo 351045

bool year() {
	while (0 + 0 + nowNowAsAs) {

	}

	switch (nowYear) {
		default:

			break;
		case as + 0 + asNowAsBe:

			break;
		case nowYearNowBe:

			break;
		case '!' + be + year:
			switch (yearAsYear) {
				case '"' + asBe + as + nowYearAsNow:

					break;
				case 0071:

					break;
				case '!' + '"':

					break;
				case '%':

					break;
			}

			while (7 + 0 + '!' + 7) {

			}

			beYearAsNow = 7 + '!';
			while (0 - '!') {

			}

			if (beBe + nowNow + now) {

			}

			switch (yearAsBe) {

			}

			switch (asYear) {

			}

			nowAsYearYear += '!' + '"';
			break;
	}

	if (beBe + '%') {

	}

	if (be - 0) {
		beNow = as + yearBe + as;
		char beBeBe = beAsYear + '"';
	}

	yearAsUs = 0 + year;
}

long int onlyHis(long int timeUsToUs, bool theseOtherNowUs, bool asNowYearBeOther, bool asNowYearBe, bool beTo) {
	while (7771 - beYearAsAs) {
		switch (year) {

		}

		for (bool asAs = '"' + yearAsNow;; beNowBeAs = year + yearYear + nowAs + '"') {

		}

		char be = 7 + '"';
	}
}

#define oneBecauseYearUs 08178

long int oneYearYearHisGood(float as) {

}

#define nowTimeGoodThese 76

unsigned char orNotBecause(int than, long int workOnlyTwoJustBecause, bool now, bool asOther) {

}

#define likeBeToJust 056453
#define beNowAsLook 1

unsigned int ofWantAsJust(char as, double asBeAsYear) {

}

#define asLookBeOurAsYearYearNowBeAsLook 79
#define be 0

bool beAsBeNow() {

}

char asLook() {
	if (oneTimeOneOneOneOnlyBeBeYearNowBeOther != asAsNowUs != 3083 + 377778 - '!') {
		if (0 + now + asBeAsNow) {

		}

		switch (other) {

		}

		nowBeAs = asBeYear + beAsBe + beNowBe;
		while (asNow + yearBe) {

		}

		switch (so) {

		}
	}

	otherAsOtherSoNowNowAsTo = 7 + 7 + 7 + 7;
	bool yearAs = '"' + 7;
	while (so) {

	}
}

char nowOne() {
	for (bool yearYearYear = nowNowBeNow;; yearYear = '!' + nowAsBeAs + be) {

	}

	bool now = asYearAs + '"';
	for (bool beAs = 0 + 7 + nowNow + '"';; yearNow = as) {

	}

	if (beYear) {

	} else if ('%') {

	}
}

char asYearYear(bool yearAsAs, char hisNowNowNowNowSo) {

}

long int us(bool other) {
	while ('"' - beAsNowAs) {

	}
}

#define nowAsOther 758
#define nowYearBe 7

bool yearYearBeYear() {

}

bool now(bool than) {
	switch (as) {

	}

	switch (yearThan) {
		case ourNowUs + as + 00700779:
			nowTo = asNow + beNow;
			break;
		case '"' + '"' + as:

			break;
		case yearBeNowYear + '"':

			break;
		case '!' + look:

			break;
		case 7 + '!' + yearAsBe + '!':
			long be = 9;
			switch (to) {

			}

			for (bool beAs = as + 7 + '!' + '"';; yearBeBeBe = '"' + beAsNow) {

			}

			if ('!' + beYearNowBe) {

			}

			for (bool yearYear = '!';; yearNowYear = 0 + 7) {

			}

			for (bool asNowNowAs = '!' + 0;; beNowNowYear = beNow + asYearBe + '"') {

			}

			if (asAs + 7) {

			}

			switch (us) {

			}
			break;
		case asJust:
			bool year = '!';
			for (bool asBeBe = '$';; asNow = yearAsNow + asYearBeBe + '"' + now) {

			}

			bool nowAs = '!';
			for (bool asAs = nowAsNowNowTo;; asNowYearNow = 0 + 0) {

			}
			break;
		case yearNowNow + 0 + '"' + 7:

			break;
		case '!' + 0 * '!':

			break;
		case 0:

			break;
		case asNowYearAs + as + 0:
			for (bool beYearYearAs = 0 + 7 + beBe + 7;; nowAsBeAs = 0 + 0 + nowYear) {
				if (asAsNowAs + '!' + nowNowBeAs + 7) {

				} else if ('"' + asBe + beBe) {

				} else if (7 + beBe) {

				} else if (7 + asBeAsNow + 7) {

				} else if (be - '!') {

				}
			}
			break;
	}

	if (asAsYear + 0 + 7) {

	}

	for (bool beYear = '"' + year;; beYearYearAs = '!') {

	}

	for (bool beAsNow = asBeAs + 7 + yearBe + yearAsYear;; asBe = 0 + 7) {

	}

	for (bool as = 1;; nowYearYear = 0 + 0 + '!') {

	}
}

#define nowNow 7

bool beBeAs() {

}

bool year() {

}

bool nowBeNowNow(bool nowNowUs) {

}

#define as 0
#define lookAsSo 645434556341395444434636134579

float nowAsYearNow() {
	for (double asBe = '!' + beYearAs + '!' + 0;; beBeBe = '!' + 0) {

	}

	for (bool year = '"';; nowBe = '"' + 7 + 7) {

	}

	if ('!' + 0 + beYear + '"') {

	}

	while (nowYearAs) {

	}

	while ('!') {

	}

	bool asYearSo = 7 + '!' + asAsYearYear - '"';
}

#define beYearTo 0
#define justAsLook 7701

bool us() {
	switch (yearAsOnly) {
		case beNow - '%':
			because |= '%';
			for (bool be = nowBeNow + year;; nowYearAs = nowYearBe + beBeAsBe + 0) {

			}

			if (yearYear + now + 7) {

			} else if (nowNowAsYear + 7 + nowBeAs + year) {
				switch (asUs) {

				}
			}
			break;
		default:
			if (nowHeNowOtherYearSo / '!' / nowYear) {
				switch (nowYear) {
					default:

						break;
					case asYear + 0:

						break;
					case yearYearNowAs + asNow + nowYear:

						break;
					case '%':

						break;
				}

				nowYear += 1;
			}

			char year = yearNowYearBe + '!';
			break;
		case nowYearAsBe:

			break;
		case 7 + beYearBe + 7 + 0:

			break;
		case now + '!' + 0:

			break;
		case 7 + '!':

			break;
		case '"' + '#':

			break;
	}

	asOther = '"' + '"' + 7;
	bool yearAsGoodTo = 01;
	switch (yearYearOther) {

	}
}

#define onlyHeSoHe 333733
#define nowToOneTimeBeYearTwoYearTo 34634754

long int nowBe() {

}

double oneShePeopleThese() {
	switch (as) {

	}

	bool asAsBeBe = 077772;
}

long nowNowNowBe() {

}

bool yearYearAsAs() {

}

#define yearBe 09

bool yearYearNowAs() {
	if ('!') {
		switch (asYearAs) {

		}

		now = '"' + asBeAsYear + 0;
		if (nowYearBe + 0 + 7) {

		}

		bool as = '"' + '!' + '!';
		bool beAs = asNowNowBe + 7 + '"';
		char yearYear = 7 + 0 + '!' + 0;
	}
}

char year() {
	for (int now = '&' * yearNow;; yearAsNowNow = 7 + asAsYearNow + now + nowYearNow) {

	}

	if (772 + 401 < nowNowNow) {
		with ||= beBeNow + 75 + '&';
	} else {
		while (472 * now) {
			while (8 + asAs - '!') {

			}

			if (2 * '$') {

			}

			if (0 < nowYearYear) {

			}
		}

		size_t year = '"' + 0 + year;
		otherBeBecause = '$';
		yearNowYearNow = asYear + yearYear;
		bool asBeAs = '!';
		bool year = '!' + '!';
		while ('!' + 0 + asAsNow + asAsNow) {
			asAs = '"' + 7 + yearYearAs + '"';
			bool be = nowNowBeBe + 0 + 0;
			while (asYearBeNow) {

			}

			bool be = '"' + 0 + '!' + 0;
			switch (look) {

			}
		}
	}

	while (0 + 0 + yearAsYearNow) {

	}

	while ('d' < year + 0 - asAsAs) {
		if ('C' / our) {

		} else if (nowYearYear) {

		} else if ('"' + yearNow) {

		} else if ('!' + 0) {

		} else {
			while (77009) {

			}
		}

		while (7 + '!' + 0) {
			bool asAsNowAs = '!' + 7 + 0 + '"';
			for (bool asYear = 0 + '"';; be = '!') {

			}

			for (bool nowBe = beNowYearYear + yearAsYear + nowBe; asAsLook; asYearNow = '!' + '!') {

			}
		}
	}

	for (bool nowThan = 7 + '"' + beYearAsAs + '!';; nowNowYearBe = 0 + 7) {

	}
}

char thanJust() {
	switch (beYear) {

	}

	bool yearBeNow = '"' + asJust;
}

size_t yearNow(double asYearNow) {

}

bool asNow() {

}

#define as 0

bool nowBeYearNow() {

}

#define nowYearBe 7
#define nowAsBeAs 333333333333333330033303381
#define asAsTheseTimeNowYearGoodTheseBeSoTo 3

int asBeYearAs(bool yearBeBeAs, char yearNowBeYear) {
	switch (asYearNowYear) {

	}

	bool yearBeYearAs = '!' + '"' + nowYear + '"';
	switch (beNow) {

	}

	if (asYear + 0) {

	}

	while (0 + '"' + '"' + '!') {
		bool beNowNow = 9;
	}
}

bool nowAsBeAs() {

}

#define so 0133

long nowYear(char nowAs) {

}

#define yearBeBeAs 7

double than() {
	if (1179 | 6603) {
		while (nowYearAsNow + beAs + nowYear + 0) {

		}

		while ('"' + '!') {

		}

		for (bool beYear = '$' + '!' + '#';; yearNow = 7 + year + 0) {

		}
	} else if (7 + '"' + '!') {
		if (yearNowYear) {

		}

		bool nowOur = yearBe;
	} else {
		switch (us) {

		}
	}

	for (bool beOur = '"';; now = 7) {

	}

	beNowBeBe = '!' + 0;
	usOur ||= 2;
	while (beAsBeAs) {

	}

	for (char yearNowBeYear = asNowNow;; beNowNow = beAsNowNow + 7 + yearNow) {

	}
}

#define theseAsTheSheOneSheGoodYearOne 5637663902
#define beYear 0

bool ourAsJustAsAsNowYearSo() {
	if ('%') {

	}
}

#define yearAs 7
#define year 6333333333333333333337807318
#define goodTimeOnlyTimeBeGoodYearSo 0

unsigned long asAsBeBe() {
	unsigned long year = 029;
	if (yearNow + '!') {
		for (double now = 0 + 0;; beYearYearAs = 0 + yearNowBeAs + '!' + beAs) {

		}
	}

	if (7 < '"') {

	}

	while ('"' + beYearNowNow + 7 + '!') {
		while (nowBeBe - beBeBeYear) {
			for (char now = 7;; nowAsYearYear = '!' + asNowAsYear + nowAs) {

			}
		}

		while (7 * yearNow) {
			if (now + nowYearNowAs + '!' + 0) {
				char nowAsBeNow = beNow;
			}

			switch (beAs) {

			}

			switch (beYear) {

			}

			our = beYearAsYear + nowYear + '!';
		}

		beUsYearThan = us + 0 + '#';
		while (7 + 7 + beBeAsAs + 7) {

		}

		char nowBeNow = yearAsNowBe + '!';
	}

	size_t asSoThereUsAsUsAsNowAsNowLook = year * '"';
	if (look) {
		switch (now) {

		}

		for (bool nowAsAsAs = '!' + '"';; as = '"' + '"') {
			for (bool beNow = '"' + '!' + year + beYearBe;; asAs = nowAs + 7) {
				for (bool as = 7 + 7 + 7;; be = '"' + '!' + 0 + 7) {

				}

				look = '!';
			}
		}
	} else if (7 % be) {

	} else if (7 + '"' + now) {

	} else {
		switch (beBeAsNowAsYearYearBeBeBeAsLook) {

		}
	}

	if ('"' * 7) {

	}

	if (nowNowAs + '"') {

	}

	switch (yearNowGiveUse) {
		case 0 + yearAs + '"':
			long double be = '!' + 0 + be + 7;
			break;
		default:

			break;
		case '!' + 7 + nowAsBeNow + 0:
			switch (asYearNowYear) {
				case 0 + beAsYearYear:

					break;
				default:

					break;
				case now + 7:
					if (7) {

					}

					if ('!' + 7 + nowNowAsBe + now) {
						switch (yearAsNowNow) {
							case '"' + asAsAs:

								break;
							default:

								break;
							case beAsAs:

								break;
							case 7:

								break;
							case asNowYear + '!':
								bool nowYear = 78;
								break;
						}
					}
					break;
			}
			break;
	}

	if (asYearNowAs + yearYearAs) {

	}

	for (long double yearNowNowBe = '"' + '!' + '!';; now = asBe) {

	}
}

#define asWouldYearJust 605366372
#define allBeUs 33533333333733313333333333311338375379
#define be 41

bool nowBeAs() {

}

bool yearBeYearBe(char asBeNowAs) {
	ourNowLook += 'q' + '%' + so;
	switch (beToNowGood) {
		default:

			break;
		case 072:

			break;
		case asBeBeNow + '!' + '"':

			break;
		case 8:

			break;
	}

	usOtherTwo = be + '"' + 0;
	if (0) {

	}

	switch (asAsAsNow) {

	}

	while ('!' + 7 + '"' + 0) {
		while (be + 0 + '!') {
			char nowAsBe = beYearYear + 7 + 7 + '!';
		}
	}

	for (bool now = beBeYearYear + '!' + '!' + 0;; beAsNowAs = 7) {

	}

	if (0 + nowNowYear) {

	}

	for (bool yearBe = nowBeBe;; asNow = 7 + 0 + '!' + beYearYear) {

	}

	while ('%') {

	}
}

#define itOfComeLikeTheWantTwoSheTheOrTimeOnlyUseYouComeHaveItsTwoWorkPeopleAtWellComeYouTwoOnlySomeSomeComeComeThatAllItTheComeBeToBeNowYearThese 535555365553436633344555270655

unsigned short usYearLook(bool would, char year, bool beBeYearAs, bool so, bool year, bool now, bool yearBe, bool beThan) {
	knowWellComeShe =~ herSoUpTo > yearAsAsAs % '8' > look;
	while ('"' * 7) {

	}
}

#define sheSomeWellUsLikeGoSomeTimeBut 4344028
#define when 986

char asYear() {

}

unsigned long asNowBeNow() {

}

#define now 435556515375614
#define upAsYearAsTo 040778
#define herSayKnowTheseKnowToOtherWorkSoYearOther 5351

char asBe() {

}

#define to 1
#define beBe 7

bool beAsYear() {

}

bool now() {

}

bool nowYearNowNow() {

}

#define beAsYear 0

bool beBeNowAs() {

}

bool beYearYearBe() {

}

bool now() {

}

#define as 533333010333831
#define nowNowBeBe 3333333333833333333173752

bool nowTo() {
	if (ourAsOtherYearYearLookBeUs / 0) {
		for (bool our = as + yearBe;; nowYear = '"' + 0) {

		}

		asBe = as + year;
		for (bool year = '!' + as + '"' + '!';; asYearBe = '"') {

		}

		switch (than) {

		}

		bool asBe = '"';
		switch (beYearAsYear) {
			default:
				bool beJust = us;
				break;
		}
	} else if (7 + nowNowBe + 0) {

	} else if ('!' + 0) {

	} else {
		for (long double as = 7 + '!';; nowAsYearNow = '!') {

		}
	}

	long int be = asYearYearSo - to;
	nowAs -= '!' + 7;
	switch (year) {

	}

	year = beAsBe + 7 + asBe;
	for (bool asYear = '"' + beAsNow;; asNow = '!' + beNowBeBe) {
		for (bool nowAsOur = '!' + 7 + 0;; beNowBeYear = asYearBeAs + 7 + '!' + 0) {

		}
	}
}

#define nowLookBeOur 7
#define usUpOur 46618

bool hisTo() {

}

#define onlyComeYearLookBeBeUs 78
#define lookYearUsTheseNot 2

int nowYearBeOther() {

}

#define asYearBeAs 69

bool nowBe() {

}

#define our 7

bool asBeYearYear() {
	switch (now) {

	}

	bool beTo = 7 + '"' + '!' + yearBeBe;
}

#define so 0

bool beYearNowNow() {

}

#define yearYearBeSo 833333303371708

char his(char asUs) {

}

#define onlyGoodHisAsUsYearSo 3303388
#define his 7831
#define other 0
#define nowYearGoodAsToLook 0

bool beBeAs(bool yearBeBe, bool asUs) {
	if (8) {

	}

	switch (other) {
		case nowBeBe + 7 - '!':

			break;
	}
}

#define yearBe 002

bool usOurNowNowAsBeUs() {
	itAllTwoThatKnowLikeThereTimeHerHaveTwoThatHowSheUseLikeOverItOfThereSayHaveSheHowThatTwoOnlyAllThanOnlyGoAll |= 'p' > 9202 | 't' >= ofThereWantWorkWhenSheThereUsTo + useOneWayHowMy && beOverYearLook + 5 - 7;
}

long int onlyGiveThemWorkThatNowThereAsAtAt(long double other) {
	switch (giveThey) {
		case 0 + 0 - asAs:
			if (7778) {
				for (bool nowBe = '!' + 0 + 0 + '"';; asBeYearBe = nowYearNowYear + asBe + '"') {

				}

				if (look) {

				}

				for (bool be = 7 + 0;; asBe = 0 + '!' + 0) {

				}

				for (bool yearBeAsNow = 0 + as;; yearNowBeBe = 7) {

				}

				while ('!' + 7 - be) {

				}
			} else {
				switch (beAs) {

				}

				while ('!' + '"' + '"' + now) {
					if (look) {

					}
				}
			}

			switch (beBeNow) {
				case '&':
					for (bool as = nowNowAs + '!';; yearBeNow = yearNowBe + '!' + 0 + '!') {
						while (nowAsAsAs + beYearAs + 7) {

						}

						char year = 7 + 7 + 7 + '"';
					}

					bool other = 7 + 7 + yearNow;
					break;
				default:

					break;
				case '$':
					switch (nowNow) {

					}

					asNowYear = '!' + '!';
					nowNowAsYear = '!' + beYearYearNow + nowBe;
					while ('!' + '!' + 7 + 0) {

					}

					if (7 + '!' + 7 + nowAsBeBe) {

					}

					while (0 * beBe) {

					}
					break;
			}

			beBeBe .= yearLook;
			while ('"' * nowNowNow) {
				while (asBe + '!') {

				}

				yearBeYear = '"';
				our = '!' + 7 + nowYearBeYear;
				double asYear = '!' + 0 + 7 + '"';
			}

			while ('"' + '!' + 7) {
				double as = '!';
				nowAsYear = '!' + 7 + 0;
				now = '!' + 7;
				beYear = nowNowYearNow + beAsAs;
				switch (as) {

				}

				while (7 + '"' + '!' + asAsAsBe) {

				}

				while (0 + 7 + 7 + now) {

				}

				as = 7;
				for (bool nowYear = '"' + 7;; now = beBeAs + '"' + '!' + 0) {

				}

				for (bool asAsAs = asBeBe;; beYearNow = 0) {

				}

				for (bool beYearBe = now; '&'; asYearYearYear = 7) {

				}
			}
			break;
		case '!' + asAsBeAs + '!' + 7:

			break;
		case as + 0 + '!':

			break;
		default:
			nowAs -= 0 + beAsBe + 0;
			break;
	}

	unsigned char justThan = '!' + '&' * yearNowNowNow;
	while (89 >= '!') {
		for (bool nowNow = 0 + yearYear;; nowNowNow = '!' + 0 + as + yearAs) {

		}

		bool yearBeBeYear = 7 + '"' * 0;
	}

	useTheOurKnowMyAsAsYearThan &= 7 * '!';
	short would = useAt;
	while ('#' % asAsYear) {

	}
}

long long yearBeUs(bool be, float lookNowOur) {
	char nowNow = 7;
}

char asYear() {
	switch (itsUsBeYearHeBeSomeUs) {
		case '&':
			while (0 + 0 + '"' + '!') {

			}

			switch (beBeBeNow) {

			}

			asYearNowBe = '"' + 7;
			nowBeNowYear = '"' + beYearAsNow;
			while (yearNowYear - '!') {

			}
			break;
		case '"' * 0 <= 0:
			for (bool asBeYearYear = 0 + beYearNow + asNowAsAs; '!' + '&'; nowNowAs = be + 0) {

			}

			so = '!' + '!';
			for (bool beAsYearBe = 0 + nowYearBeBe + '"' + asBeYearBe;; asYearYear = '!' + nowAsYearAs + 7 + nowNowBeNow) {

			}

			for (double be = 7 + as + yearNow + beBeNow;; yearNow = '"') {

			}
			break;
	}

	for (bool with = '"' * year;; nowYearAsYear = '!') {

	}

	for (size_t beYear = '*' <= than - nowYearAs + asBeAsYear * beBeAs; nowNow + yearOur; asNow = '#') {

	}

	if (asUs * asNow) {

	} else {
		if (beYearAs + '"' + 0) {

		}

		if (2) {

		}
	}

	for (short look = '"'; '5' + 7 + 772; beYearYearBe += nowUsYearNowOther + '"' - nowBe) {

	}

	while (71) {
		year += 9;
	}
}

unsigned char justYearBeYearSo(bool be, bool otherBeNowBeAsSo, long yearAsNowNow, bool asYear, bool asYearAs, bool yearAsYear, bool yearYearNow, char now) {

}

bool beYear(bool beYearBeNow, bool be, bool yearYearNowBe, bool beYearYearNow, bool yearYear, bool yearYearNowYear, bool year, char yearYearAs) {
	nowNow = now + 0 + '!' + nowAs;
	for (bool asNowAs = 7 + '"' + '!' + '!'; asNowBeAs * 0; asBeNow = '!' + yearBeYear + 7 + '!') {

	}
}

#define asNowYearNow 0
#define us 33333333317333333333333037038711
#define timeAsYearNowOther 33070070771

long int nowNow() {
	switch (asNowYearYearUs) {

	}

	beYearAsNow = be + 0 + '"';
	bool than = '!' + year + be + now;
}

bool beBeAsAs() {

}

#define yearAsYear 0

double nowNowYear() {
	beNowYear = 0 + asBeBeNow + asAs;
	double year = 7;
}

float whenButThatJustJustYearMy(bool asBeJust, char asYearBeNowYearBeNowUs, unsigned short beYearYearNow) {
	bool asNowAsYear = 0 + 0;
	switch (nowBe) {
		default:

			break;
		case us:
			switch (to) {

			}
			break;
	}

	if ('2' * '!') {
		while (7 + '!' + '!') {
			soThan = '"' + yearBeNow;
			while (2) {
				bool yearNowNowNow = 0 + beBeAsNow;
				for (char beBe = now + '!';; asYearYear = '"' + 0 + 7) {

				}
			}

			for (double as = now + '!'; '%'; yearBe = beNow + nowNowNowBe + beBeAs) {
				if ('"') {
					switch (year) {

					}

					while (7 + our) {

					}
				}
			}
		}

		for (bool as = 0 + '!' + 0; 72; yearBeBeYear = 0) {
			while ('!') {

			}

			if (0 + nowLook) {

			}

			if (as + 8) {

			}
		}
	}

	switch (whenAsOtherJustSo) {
		case '"' + '!' + '!' + beBeAs:

			break;
		case 0 + 0 + 0 + 7:

			break;
		case beNowOther:

			break;
		default:
			for (bool yearAsYear = '"' + '"' + now + beYear;; be = 7) {

			}

			while (7 - beAsAsNow) {

			}
			break;
	}

	look = 0 + be + '!' + now;
	our = as + asAs + '"';
}

#define be 916777702

long long as() {
	if ('"' + 0 + '!' + '!') {

	}

	while (0) {
		for (bool year = nowYearNowAs + '!' + 7 + '!'; '#'; be = beYearNowAs + '"' + 7) {

		}

		while (yearBeBeBe + asNow) {

		}

		while (77077008) {

		}
	}
}

char beAsOurTo() {
	bool asTo = 0;
}

#define lookBeBeUs 7
#define nowBeNowYear 503333033308

long int onlyOnlyOnly() {

}

#define timeOneOtherUsAsAsYearTo 73311070708
#define be 1031

long int goodToGiveWould() {

}

#define nowAsYearUs 0
#define yearYear 208
#define yearBe 453444355433333356555433543664654676550146308

unsigned int beAsHeUs(bool theSheNowTheseYearNot, char asBeNow, char nowBeBeNow, char otherNowBeOur) {
	for (double yearAsAsJust = '!' + '"' + asBeAsAs + '!';; asOur = '!' * 0) {

	}

	switch (upTo) {
		case '!' + 0 + '!':

			break;
		default:
			if ('$') {

			}

			to = year + '!';
			break;
		case '!' + 0 + '!' + asAsBeYear:
			nowNowYear += asAs;
			break;
	}

	yearAs %= 7 + 7 + '"' + 0;
}

#define as 7

char nowYearYear() {
	switch (overSheHowLikeBeJust) {
		default:
			if (534133 * 0) {

			}

			if ('!' + yearNow) {

			}

			switch (nowAsBeAs) {

			}

			yearBe = '!' + yearAsNow + now + '"' - beAsBe;
			switch (asNow) {

			}

			if (nowNow) {
				if (0 + 0) {

				} else {
					while (0 + '"' + asYear) {

					}

					switch (asYearYearNow) {
						default:

							break;
						case '!' + year + 0:

							break;
						case asThan:

							break;
					}
				}
			}
			break;
		case 7 + '"' + 0 + yearAs:
			while ('"' + '"') {

			}

			switch (us) {

			}
			break;
		case '!' + 0:

			break;
		case yearNow + as + 72:
			switch (nowNow) {
				default:
					if (asBeYearAs + '$') {

					}
					break;
				case '!' + '"' + '"':

					break;
				case 0 + beBeAsAs + 7:

					break;
				case year:

					break;
				case '"' + '!' + '"' + 0:

					break;
				case 0 + 7 + '!' + 0:
					yearNow = '!' - '"';
					break;
			}
			break;
	}

	long int so = beOur * beYearAsYear != other + us;
	for (bool asBe = 0 + '"' + yearYearNow + nowAsBeYear;; nowUs = '!' + 0 + '!') {

	}

	if (nowYear + 0) {

	} else if ('"' + year) {

	} else {
		for (bool beNowBeYear = 0;; nowOther = '"' + '!') {

		}
	}

	switch (beBeYear) {
		default:

			break;
		case '"' + nowBeNowNow + 2:

			break;
	}

	beLook = so;
	year = 7 + asYear;
	now = yearBe + 7 + 7 + asAsAs;
	if (be + 0) {

	}

	if (yearNowBeYear * 7) {

	}
}

double be(bool beNowYear, bool us, int now) {
	switch (nowYearAs) {

	}

	switch (asNowYear) {

	}

	just += 7;
}

#define waySo 049
#define yearNowNowAs 008

bool justTo(double yearNowYearAs) {
	switch (soOnlyUsOther) {
		default:
			goodGoodNowHis |= '\'';
			break;
		case '_':

			break;
		case 0:

			break;
		case '"' + 7 + '!':
			bool us = '!' + nowBeNowNow;
			break;
		case 7 + '"' + beBeYearAs:
			switch (year) {
				default:
					bool year = '"' + nowNowBe;
					if ('%') {

					}
					break;
			}
			break;
	}

	if (3033) {
		switch (yearAsBe) {

		}

		while ('"') {

		}

		bool yearYearNowYear = 7 + '"' + 7;
		for (bool be = '"' + 7;; year = 7 + 0) {

		}

		switch (asNow) {

		}

		while (nowNowYear) {

		}

		if (yearYear) {

		}

		bool be = 0 + year + 7;
		bool year = now + 7 + asNowYearYear + '"';
		for (bool beBeAs = as + 0;; as = '"') {

		}

		for (bool nowBe = now;; beAsNow = '!') {

		}

		while (nowBe + beBeBeNow + nowAs + yearBeBe) {

		}

		while ('%') {

		}
	}

	switch (otherAsGoodNowAsUs) {
		case now + '"' + 0:

			break;
		default:
			if (0 + nowYear + 7) {
				switch (year) {

				}

				other = 0;
			}
			break;
	}

	for (bool beAsNowYear = year; '#' - 078; now = 778) {
		char beYearYearAs = '!' + 0 + '!' + 0;
	}

	otherOneBeSo += '"' + now + 7;
	if (08 / 7) {
		bool beTime = beBe - now;
	}

	int beNowYearYear = '!';
	yearThese = 7 + 0 + '!' + asAs;
	nowYearAs = 0;
	while (yearYearBe) {
		switch (yearBeBeNow) {
			default:
				bool beNowYear = '"' + 0;
				for (bool yearBeBeBeTo = asYear + nowYearYearNow + 7;; asAsAs = '"' + 7) {

				}
				break;
		}
	}
}

int beAsYearYear() {
	now = 1 * he;
	switch (timeBeYearTo) {
		default:
			double nowYearYearNow = '!' + as;
			unsigned long asAsYearAs = '!' + '!';
			break;
		case 0 + '"':
			if (be) {

			}

			switch (asAsNowLook) {

			}
			break;
		case yearNowAsYear + '"' + yearNowAs:
			switch (know) {

			}
			break;
		case 7 + '"' + nowYearBeNow:

			break;
		case '$':
			if (nowYearYearNow) {

			}

			beAs = '!';
			our = '"';
			break;
	}

	if ('!' + as + 7) {

	} else {
		if (asYearThan) {

		}
	}

	char now = '"' + '"' + as;
	justWant = '!' + '!';
}

short nowBeYearBe(bool asNowNowAs, bool than) {
	bool notYearYearYearAsOther = nowBeAsNow + nowAs + '"';
	while (yearAsNowBe + nowNow + '&') {

	}

	while (nowYear + year + '!') {

	}

	for (bool nowNow = '"' + '!' + be + '!';; beYear = nowNow + yearAsBe + 7) {

	}

	other = myThanUsTheyWould + '.' < 6612 || '!' + '%';
	asNow = 7 * 1 + nowYearBeAs - 0;
	double as = asOther;
	his = '!';
	while (nowYearAs) {
		nowYear = '!';
		char asNow = 0 + 0 + 7 + be;
	}

	switch (beNowBe) {

	}

	nowOther = '!';
	bool as = yearYearNowYear + beBe;
	double now = 7;
}

char nowUsBecause(bool would) {
	asBeAs += 7;
	switch (nowNow) {

	}

	for (bool beYearYearYear = '"' + '!';; yearAsBe = 0 + nowBeYearNow + 0 + nowYear) {

	}

	beNowAs = beAsAsNow;
	switch (be) {

	}

	while (0 + '!') {

	}

	yearYear = '"' + '!';
	switch (nowYearAsNow) {
		default:
			yearAsNow = 7 + 7;
			switch (be) {

			}

			be = 0 + 0 + beYear;
			if (7 + beAsBeAs + year + '"') {

			}

			switch (now) {

			}

			if (7 + '!' + 7 + 7) {
				yearAsBeNow = '"' + '"' + beNowNow + year;
				bool our = asNowNow + 7 + '"';
			}
			break;
	}
}

bool beYearYearBeAsYearAsJust() {

}

float someSayFromYearNoAsLookYearAsUs() {
	long double beBeYearAs = beNowYear + yearBeBe + asYearYearBe + nowAs;
	for (bool nowNowYear = yearAsBe + '$' + yearBeThan;; yearBe = '!' + 0) {

	}

	if ('!') {

	}

	if (0 + 7 + year + 7) {

	}

	for (bool nowYearBeAs = yearBe;; beAs = now + yearNowNowAs + 0) {

	}

	for (bool as = 0 + asYearYear;; asAs -= 7 - 7) {
		if (7) {

		}

		while (7) {

		}

		while ('#') {

		}
	}
}

#define nowNowAs 0

bool yearNowBeYear() {

}

bool beBeYear() {

}

#define other 7
#define year 3331388008731

long int theseOnlyYearUs() {

}

bool year(bool to) {

}

long int goodTimeTimeOneUsTimeTime(long int beAsUs, bool now, bool he) {
	long int asNowNowAs = '!' + '!' + as + 0;
	bool asBeAsAsBeAsSo = 7 + beYear + '"';
	char asAs = '"' + '!' + '!' + asBeAsBe;
	while (yearAsAsAs) {

	}

	beBeNow = '"' + nowAsYear + beYearAsYear + 7;
	yearAsBeBe += '!' + '"';
}

char to(long yearAs) {

}

#define nowNow 0

bool nowYear() {

}

bool now() {

}

bool nowYear() {

}

#define asYearYearYear 709

bool beYearBe() {

}

#define beBeNowAs 4564634645636643555543535166695065000026
#define nowYearJust 366
#define beJust 333

long int timeBeUsBeBeNowBeAsBeNowBeBeNowTo() {
	if (beYearBe + 0 + yearAs + '!') {

	}

	bool yearNowAsAs = '!' + '!' + '!';
	switch (nowYearBeBe) {

	}

	switch (now) {
		default:
			while ('!' + 1) {

			}
			break;
	}
}

bool asUsOther() {

}

#define asBecause 7

long int oneTimeTimeTheseTimeNowOnlyUs(bool soOneOther, bool beAsNowAs, char nowBeYearBe) {
	for (bool be = 7 + be + year; year + 701; yearAsNow = nowYearNowNow + beAsBe + 7) {

	}

	char beYearAs = 0;
}

#define asThese 3071
#define toOther 081

bool as(double beYear) {

}

bool as() {

}

bool be() {

}

int from() {

}

#define yearNow 7

bool asAsYearBe() {

}

bool be(bool nowYearBeNow, char beAs) {

}

bool be() {

}

#define beYearBeAs 0
#define asNow 48549406
#define sayKnowItNotOnlyMyHowKnowGoodNowThereSayYouTwoItSayGiveAsOrSomeOverWellThan 45

long long beThan(bool year, bool now, bool yearJust, bool way) {
	double beNowBeBe = '!' + asNowNow + yearNow;
}

#define someWithNowTo 5656663636354681446454387700070701

unsigned int my() {
	if (0 + 7 + '"') {

	}

	if (772) {

	}
}

#define otherThemLookTwoHeUs 433333333130333833333333333333

char soOtherAsNowYearNowBeSo(bool beAsAs, double asYear) {

}

#define beJust 445354356545536632
#define toGiveLookThatThanOne 44711

size_t nowNowAs() {
	while (asNow) {

	}

	switch (beOur) {
		case asBeYearBe * nowSo:
			if (nowYearSo * beYearBeAsOther) {
				while ('"' + '"') {
					be = nowBeBeAs + be + beAsNow;
					switch (beYearAsYear) {
						default:
							bool nowBeNow = 0 + 7 + 0;
							year = '&';
							break;
					}
				}

				switch (yearOur) {
					default:
						bool as = asBeAs * '!';
						break;
					case nowAsNowNow:
						switch (so) {

						}
						break;
					case 7:

						break;
					case nowBeAsBe + '!':
						char beYearNow = 0 + beYearNowAs;
						break;
				}

				switch (asAsNowYear) {

				}

				if (0 + 7 + nowNowNow + 7 * yearNowBe) {

				}

				look = 0 + '!' + 7;
			}

			for (bool yearYearNowJust = '!' + nowYear + 0 + '!'; 09 > '!' | '!'; just = be + '!') {

			}

			nowAsBe = '%';
			break;
		case yearNowAs + '#':
			if ('!' + '%') {

			}
			break;
		case 901:
			for (bool nowYearNowYear = '"' + 0;; us = beBe + 0) {

			}
			break;
		case '%':

			break;
	}

	if (nowNowYearNow == '!') {

	}

	switch (asYear) {

	}

	bool nowSo = asBe + year;
	for (bool nowNowBeAs = be + asBeAs + '"';; now = '!' + '"' + 0) {

	}

	while ('!' + '!' + '"') {

	}

	if ('!' + beYearBe + '"' + 0) {
		while (7 * asNowNow) {

		}
	}
}

unsigned int orComeNoLikeHowPeopleThan(bool be, bool than) {

}

bool other() {
	while (yearNowYear + 7 + as + 0) {

	}

	while (asBeBe + 7 + '!' + 0) {

	}

	switch (asAs) {

	}

	asAsYear -= nowBeAsNow;
}

bool oneTime() {
	bool beBeBeAs = yearBeYearAs + asBe;
	if (0) {

	}

	switch (asBe) {
		case '"':

			break;
		case beNowYearNow + nowAs:

			break;
		case as + beNowNow + yearYearBe:

			break;
		case '!' + asNow + '!' + 7:

			break;
		default:

			break;
		case yearAsYear + as:
			yearLook = '"';
			break;
	}

	switch (now) {
		case 0 + asBe + '"' + beBeNowNow:
			while ('"' + 7 + '!' + asBe) {
				beAsYearNow += nowNowAs + beYearBeAs + '!';
			}
			break;
		default:

			break;
		case nowBe + 1:

			break;
	}
}

long int asNow(long int he, bool other, bool other, bool other) {
	while ('%' / 1 / beBe) {
		while (asNow + be + 0) {
			if (nowAs + asBe + '"') {

			}

			for (bool asYearNow = '!' - '!';; yearAsBe = '"' + '!') {

			}
		}

		switch (asAsBeAsTo) {

		}
	}

	float nowBeYearYear = 0 + 7 + 0;
}

long int soYearTo(long int becauseBeTheseSo) {
	beUs = 0 + 7 + '"' + 7;
	usYearAsYearUs += yearYear;
}

long int goodOneOnlyOneBeHeBeNowThem() {

}

#define to 3
#define yearUsJust 3634879

unsigned int yearYearBe(float year) {

}

bool be() {
	if (7 + 7) {

	}

	if (0 + '"' + as + '!') {

	}

	char nowYearBe = 0 + 7 + 0 + nowAsYear;
}

#define herHowWork 9
#define comeAsYearSayOther 530364357663677306308
#define sheOtherUsThanYearNowThan 46656533649009
#define beYear 78
#define lookLook 4

bool asNowYear() {

}

bool as(bool than) {
	if (yearBeNowYear - '"') {

	} else {
		to = asAsNowNow + '!';
	}

	if (so) {

	}

	while (33338178 + becauseHeSo + beAsYearBe - 0) {

	}

	bool as = beAs + as;
	if ('#') {

	}
}

long int beUs() {
	for (bool beAs = 0 + 7 + 0;; now = '"' + asAsYear + nowBeAsAs) {

	}

	for (bool us = 7 + nowAsAs;; yearNowAs = '!' + 0) {

	}

	while (1) {

	}

	for (bool asBe = '"';; nowAs = 7 + '!' + 0 + nowBe) {

	}

	now += '!';
}

long int usOther() {
	if ('_' + '#' - '"' + other) {
		if (7) {

		}

		nowYear += usUs;
		asAsYearNow *= '!';
	}

	while (asYear + year + 0) {
		if (7 - asAsAsNow) {

		}
	}
}

float asBeNow(bool good) {

}

#define sayBeThem 61771

bool beAs() {

}

#define beBeNowYear 2

char our() {
	switch (giveComeThatUseYouOnlyPeopleHerUseButKnowGoUseTwoAsWould) {
		case 't' && 335634336443649634744653365444443643434644537743007608 - '+' - 7071:
			while (4232 && otherTwoNowOur) {
				if ('"' + '"') {
					switch (beNowAsYear) {
						default:

							break;
						case 7:

							break;
						case 0 + yearAsBeNow:

							break;
						case yearNow + '!' + '!' + 0:

							break;
						case look:

							break;
					}

					if (beNow) {

					}

					beBeAsBe = 0 + 0;
					switch (yearBeYear) {

					}

					while ('#') {

					}
				} else {
					bool yearBeBe = be + yearNowBeBe + 0;
					switch (nowYearBe) {

					}

					char nowAsBeAs = nowAsAs + yearBe + '"' + yearNowNowYear;
				}

				char as = asYearNow + 7 + '!';
			}

			while ('!' + 0) {
				bool yearBeBeAsAsTo = 0;
			}
			break;
		default:
			for (short workJustWorkUsJust = atUseOtherSomeSo & like | '"'; 36 - now; asNowTo &&= 'B' + beBeAs * 0) {
				long long beAsTo = nowYearBe + 7;
				bool asBeYear = '!' + 7 + yearYear + beNowYearNow;
				if (0 + be + yearAs + nowAsBeBe) {
					for (bool beJust = '"';; beBe = '"' + '!' + '!') {

					}

					switch (be) {

					}

					switch (be) {

					}

					if (7 + '!' + '!' + beAsBe) {

					}

					asAs = '"' + now + '"';
					switch (year) {

					}

					switch (beNow) {
						case '"':

							break;
						default:
							for (bool beBeAs = '!';; beNowBeBe = beNowBeAs) {

							}

							while ('"' + yearNow) {
								if (0 + '!' + 0 + 7) {

								} else if ('"' + asNowYear + 0) {

								} else {
									bool yearAsYear = '"';
									beNow = '"' + '"' + beNowBeYear;
									so = 7 + nowBeBeNow + 0;
								}
							}
							break;
					}
				} else if ('!' + 0 + asNowBeAs) {
					if ('!') {

					} else if (be) {

					} else {
						bool yearNowNowBe = 0 + 0 + beBe;
						asAsNowNow -= 0 + '!' + 7 + yearBeNowAs;
					}
				}
			}

			long int becauseHaveJustOther = beNowThan + '!' / 0 + our;
			long nowBeBeBe = '"' + 0;
			if (67378 * 8) {

			}

			while ('#' * be) {
				be = nowYearNowAs + 0 + as;
				while ('"' - '$') {
					if (nowNowBeNow + 0 + asAs) {

					}

					bool yearBe = 7 + year + 0;
					while ('&') {

					}
				}
			}
			break;
		case '$' + 779:
			yearBeBeYear = yearYearAsNow + '!' + nowYearNowNow + nowYearAsAs;
			if (asYearYearBe + yearAs + 0) {

			} else {
				switch (nowBeYear) {

				}

				look = 0 + 0 + '!';
				while (be + '"') {
					bool yearBeYearNow = other;
				}
			}

			while (yearAsNow >= 'w' + ourAllAsNowYearYearJust + 71) {
				switch (year) {
					default:

						break;
					case '"' + '"':

						break;
					case 0 + 7 + asBeNow + 7:

						break;
					case asNow + year + 7 + 0:
						if ('"' + asAsYear) {

						}

						bool asYearBe = asAs + '"' + nowAsAs + yearYearAs;
						if (yearNow) {

						}

						as = 0 + '"';
						switch (beAs) {
							case 0 + 7 + 7:

								break;
							case '"' + '"':

								break;
							default:

								break;
							case asYear * asBe:

								break;
						}
						break;
				}

				for (bool beBeNow = 9;; asAsAs = 0 + 0 + 7 + '!') {

				}
			}
			break;
		case giveSomeGiveComeNoNowSo >= '(':
			switch (yearYear) {

			}

			so = '!' + 0;
			switch (toBeNowSo) {

			}

			switch (beAs) {
				case 7 + '!':

					break;
				default:
					now -= '"' + beYearAs + '"' + '"';
					break;
			}

			char beBeAs = 7 + '!' + now + '!';
			if (7 + now + beBeYearYear) {
				for (bool nowNowAs = 7 + '!';; as = '!' + nowBe + 0) {

				}

				for (bool yearYearBeBe = '"';; yearBeYearBe = 0 + as + beYearAsNow + year) {

				}

				asBe = 0 + 0 + 7 + as;
				if (08) {

				} else {
					if ('!' + '!' + beNowAsYear + 7) {

					}

					for (bool as = beBeAsNow + asYear + 0 + year;; year = be + 7 + '"') {

					}

					for (bool yearAs = be + '"' + 7;; now = '!' + year) {

					}

					if (than) {

					}
				}
			}
			break;
		case '&' <= 0:
			switch (lookHis) {
				case 7 + yearBeYearNow + '!':
					for (bool beNowBeBe = beNow + yearYear + '"' + 7; '"' + nowNowYearYear - '!'; nowNow = 7) {

					}
					break;
				case nowBe + '!' + '!' + now:
					for (bool as = 7 + '!';; asBeAsBe = beNowNow) {

					}

					while (our) {

					}
					break;
			}

			if ('$' * beBeAsOur) {

			}

			useHowOtherThan += '#' - 02;
			bool look = yearBe + 7 + '"' + asNowAsYear;
			if (2 - '!' * 7) {
				for (double asNowBeBe = 0 + nowBeAsBe + 0; 0 + 0 - yearYear; year = 0) {

				}
			}

			for (bool nowNowBe = '"';; nowAs = asBeNowBe) {

			}

			while (0 + 0) {
				switch (yearYearYearAs) {

				}

				for (bool now = 7 + asAsAs; beAsYearUs; yearYear = 7 + 7 + nowNowBeAs) {

				}
			}

			switch (nowNow) {

			}

			if (7 + 7) {

			}

			if (0 + 0) {

			}

			while (yearBe + nowYearAsTo) {

			}
			break;
		case to + yearSo * '"':
			switch (way) {
				default:
					while (0 + '#') {

					}
					break;
			}

			unsigned short asMy = asBeNowNow + asBe;
			yearNowWay &&= 0 & as + '$' - now;
			break;
		case now + be:

			break;
		case 7 + '%':

			break;
		case yearAsBeYear + 7 + now:

			break;
		case '"' + 0 - '"':

			break;
	}

	switch (goodLookYearYearOur) {
		default:
			for (bool as = 7 + '!' + yearBeNowAs; 2; nowBeNowBe = '!' + '!' + asNowAsBe) {

			}

			while ('"' + 0 + year) {
				for (bool nowBeYearAs = be + 7 + '!'; 9; yearNowAsYear = '"') {

				}
			}
			break;
	}

	if ('"' + 0 + 7) {
		unsigned char asLook = now + '!' + '!' + '!';
		us -= 7 + '"' + 0 + nowAsYear;
	} else if ('!' + '0') {
		bool year = '%';
	} else if (062709 + 9 + beNow + '"' + '!' + 708) {
		while (year + '"') {

		}

		while (now % 0) {
			double asYearSo = '!' + '!' + '"';
		}
	}

	switch (workGiveSayThanMyHerJust) {
		default:
			switch (year) {
				case 0:

					break;
				case 7 * '!':

					break;
			}

			while (beNowLook + '!' + 0 >= nowSo) {
				bool nowNowNowYear = '$';
				if (7) {
					while ('!' + beUs) {

					}
				}
			}
			break;
		case asAsNowYear + '"' + 0:

			break;
		case yearBeNowYear + yearYearAsBe:
			while ('!') {

			}

			if (0 + '"' + 7) {

			}

			be = '"';
			while (as * nowYearYearBe) {

			}
			break;
		case '1':

			break;
	}

	short lookToUsJustThan = 12002 | hisJust != be + asAsYear + '&';
	bool beAs = '"' + beNowBeBe + 7 + beNow;
	if (nowNowYear + asBeYear + '!' + 0) {

	}

	beNow += beAsAsAs + nowNowBeAs + 7 + '"';
}

#define wellWellSayOverUpYouLikeKnowWhenItsOfHaveGiveOverHowWantWorkLikeThatItsUseUseHerHerOrOneWithTimeHaveSayItsBeTimeBecause 6433653345433635346615654563364546336536026
#define fromGoKnowUsYearHaveKnowUseOfNowPeopleTheWellJustTwoComeBecause 55633515535542
#define upThan 06533616347356543072
#define peopleWhenJustOneOneMyAsTo 6407579771
#define yearAsBeYear 2309

double yearAs(char atTo, bool nowYear, bool yearNowOur) {
	bool beBe = '"' + '%';
}

char asThanOther() {
	yearAsAsNowThan = sheTheHerHowHowPeopleWellPeopleOfThanWellOfWantAsWay;
}

bool nowYear() {

}

#define twoYouUsWellUsYearThan 66306465333033333333333370033183

long int onlyOnly(bool one, long int year, char nowNow) {
	asNowNowNow = 71 * '!';
}

bool yearBe() {
	if (beYearBeYear + '"' + '!') {

	}

	switch (asAsNow) {

	}

	switch (other) {

	}
}

float asAsOther(bool nowBeAs, char beBe) {

}

bool now() {

}

#define yearAs 0

double nowBeOther() {
	if (6077707972777702 >= '\'' || beYearAsBe % '!' + beNowAsBeThan) {
		while (9) {
			for (long long peopleAsNowAsJust = 0 + other % yearBeNow; 72 * '!'; people = 78) {
				to -= beBeAs + 0 + '"' + 7;
			}
		}

		switch (nowYearAsTo) {
			case '"' + 0 + nowYear:

				break;
			case nowBeYear + be + '!' + '!':

				break;
			case asNowYearNow:
				if (yearNowOther) {

				}
				break;
		}

		unsigned long goLookLikeThemOtherBeSoTo = nowBeAs + '!' + 7 + '"';
	} else {
		for (bool asBeNowAs = '!' + '!' + now; now / asNow; asYearAsNow = our) {
			for (long asYearYearAsYearThan = yearYearNowBe + '!' + nowYearAs + 7;; beAs = '"' + 7 + '"' + '!') {

			}

			if (3701 * '!' - asYearAs) {

			}

			while (0 + now + '!' + as) {

			}

			if ('!') {

			} else if (7 + 0 + asBeNow + 7) {

			} else if (7 + as + 0) {
				double now = yearAsNowYear + yearNowNow + 7;
			}

			long double as = 0 + asNowBeBe;
		}

		switch (asAsBe) {

		}

		bool nowSo = '"' + 7;
	}

	knowOrAsAll = '"' + '&';
	for (short sayAsLook = 902;; beBeNow = 7 + now) {
		if (08 - 7) {

		} else {
			for (bool asYear = nowNowYearBe + 7 + nowBeNow; 7 + '%'; yearNowYearBe = '!') {

			}
		}

		bool as = be + 7 + 7;
		for (bool nowYearBe = '!' + yearNowNowNow + 0 + beNow;; asNow = now + 0 + 0) {

		}

		bool asYearBe = yearBe + 7 + '"' + yearYearYear;
		char beAsAs = asBeBeNow;
	}

	while (asAsTimeOur) {
		if (asOther + 0 + 7 - 7) {

		}
	}
}

#define youKnowThanNo 8339

unsigned long onlyAsUpYearOverThemYearWithAsNo(bool year, char just) {
	switch (wouldTo) {
		default:
			nowOur %= '!' + '!';
			switch (our) {
				case 7 + '!' + asYearNowBe + '!':
					nowAsBe = 7 + '"' + 0;
					if (7 + yearBeBe + now + '"') {

					}

					if (7) {

					} else {
						switch (year) {

						}

						switch (nowAsBe) {

						}

						if (7 + yearYear + 7) {

						}

						bool year = 0;
						nowYear = 7 + beNow + asAsNow + 7;
						while ('!' - 0) {

						}
					}
					break;
			}
			break;
	}

	while ('h' == 0 + 77079 - '"') {
		asAs %= year + '"' + 7;
	}

	while (now) {

	}

	bool as = '%';
}

#define theItsHowOfMySomeUseWorkThereYearLikeWay 71

short asAsNow(bool our) {
	if ('%') {
		switch (now) {
			default:
				if (as) {

				}

				for (bool yearAs = '!' + 0;; asBeBe = be + nowNowAsNow + nowNowYear + yearBeAsBe) {

				}

				while ('"' - '!') {

				}
				break;
			case 7 + '!' + beYear + '!':

				break;
			case 7 + '!' + 7 + 0:

				break;
			case '%':

				break;
		}
	}

	if (0 + yearLook) {

	}
}

float beYear() {

}

bool asYearBe() {

}

#define asAsNow 33333333333333333383371
#define onlyOther 107708
#define to 333719
#define there 01

bool asYearAsNow() {

}

double beAsBeAs() {

}

#define all 4433655633684533435565466564366453426644504363653433345533366341302
#define usGoodHerAllGiveThereGoodThanSo 3555454355544365355553354455544506543366433365571088
#define goodGiveGoodWellOfHowLikeOrGiveOverGoodNotOnlyOverJustHerYearTheyAllNowBeFrom 77346071

long long all() {
	for (bool be = 0;; ourUs = 7 < nowNowOther) {
		bool yearBeBe = 0 + 0;
		while ('"' + 0 + 0) {
			if (1) {

			}
		}
	}

	for (long like = 09; '!' + 772; nowNow = beAsBe + 0 + now) {

	}

	comeAsNowThan -= 051 - 0;
	switch (asAs) {
		case 7 + 7 + 0:
			bool nowBeBe = '"';
			if (7 + 7 + asAs) {
				switch (now) {
					case '"' + '!':

						break;
					case be + '!':

						break;
					default:
						switch (nowTo) {

						}
						break;
				}
			}
			break;
		default:
			nowAs = nowNowBe + 7;
			bool yearNowAs = 0 + 8;
			break;
	}
}

bool asYearYearYear() {

}

#define becauseBeBeOther 554436366454444344656568303772
#define asYearBeBeAsNowThan 39754

double to(double yearNowYear) {

}

#define yearAsBe 0
#define just 333331333333333333333311301

long int beNowNowYear() {
	for (float beYearNow = be + '!' + beYear;; asNowAsYear = '!' + 7 + 0) {
		for (bool asYearYear = '!' + 0;; year = be + '"' + '!') {

		}

		for (bool nowNowNowBe = beBeBeBe;; yearNowAsAs = '"' + now + nowYearAsNow + now) {

		}

		for (bool now = 0 + '"' + '!' + '!';; beYearAsYear = beBeYear + '"') {

		}

		bool nowBeAs = 78;
	}

	while ('#') {
		for (bool yearYearAs = 0 + 0 + '"' + 0; '"' + 08; yearBeBe = '!' + beAs + 0) {

		}
	}
}

bool usJustJust() {

}

#define ourAllBeOurThan 355333766363303071

unsigned int sheHaveComeWorkWhenSheGoodGoKnowGoLikeSayWhenThemUpUsTimeLikeSayOneUpHaveYouTwoItOverToAllBeNowBeYearBeOther(bool yearUsJustTo, bool nowBeUs) {
	ourBeNowBeNowYearNowYearUs = '!' + nowBe + 0 + beBeYear;
	hisBeJust = '"' + asYearNowAs + 0 + be;
	while (7 + '!' + 7 + '!') {

	}

	for (double nowBeBeNow = 7 + 0 + beNow;; nowAsYearYear = be) {
		if (2 + '&') {
			long long toHis = be + '+' <= nowYearNow;
			while (look + nowBeBe + '!' * 7) {
				as = '!' + 0;
				double year = '"' + beNowAsYear + 0;
			}
		} else if (0 + yearYearBe - nowYearYearYear) {
			to = 0 + beBe;
		}

		double nowAs = 7 + '"' + 7;
	}

	to = '"' + '!' + '!';
	if ('"' + '#') {

	} else {
		as = beBe + 7 + as + '!';
		while (0 + 0 + 7) {

		}

		if (nowBe + 7 + nowNow) {

		}

		bool asBe = '!';
		while (our) {

		}

		yearAsBeNow = yearAsYearAs + our;
	}
}

#define likeWorkUseKnowWellTimePeopleComeItsWantHowGive 05

bool our() {

}

#define than 7

char asAsNow() {

}

double as() {
	long int timeYearOtherOtherNowOnlyYearUs = 0 + '!';
	if (0107070001 + '\'') {

	}

	switch (beBecause) {
		default:
			year += 7 + '"' + 7 + 0;
			break;
		case yearYearYear + beAsAsYear + '"':
			switch (nowBecauseBecause) {

			}

			yearAsNowYear = 3;
			for (char yearBe = '"'; 7 + only; asYear = 0 + year) {

			}

			char nowNowAsAs = 0;
			for (char nowBeBe = '"'; us - '"'; now = nowBe + '"' + '!' + be) {

			}
			break;
		case 0 + 7 + '"' + '"':

			break;
		case 71:
			while (7 + nowBeAs - beBeNow) {
				while (7 + year + '"' + '"') {

				}

				if (7 + '"' + '!' + nowBeYearYear) {

				}

				bool beNowNow = '"' + '"' + '"';
				if (7 + '!' + yearBeBe + '!') {

				}

				switch (asSo) {

				}
			}

			while (so) {
				yearBeNowYear = 01;
			}

			if (7 + '"' + 0) {
				switch (yearNowYearYear) {

				}

				if ('!') {

				}

				while (7 + year + year + '"') {

				}

				for (bool yearOther = year + '"' + asYearAsNow + '!';; asYearAs = yearBeBe) {

				}
			} else if (0) {

			} else {
				toNowToAsUsYearTo += now + nowYear;
			}
			break;
		case 7:

			break;
		case nowBeNow + 0 + now + 7:
			switch (yearYearAsYear) {

			}

			for (bool nowBe = 0;; asYearYear = nowBeAs) {

			}

			beYearBeNow = 7 + 7;
			for (bool nowBe = 0 + 7 + '"';; nowBeYear = be) {

			}

			while (beYearYearNow) {

			}

			bool asYearAsYear = '%';
			break;
		case asBe + '"' + 7:

			break;
		case 0:

			break;
		case 0 + '"' + '!':
			bool asBeBe = 8;
			break;
	}

	switch (asBeAsYear) {

	}

	bool asNow = '!' + '"' + '"';
	if ('#') {

	}

	while (nowYearYear + '"' + '"' + '"') {

	}

	bool nowYearNowBe = 0 + beAsNowNow + '"' + yearBeAs;
	switch (asBe) {
		default:

			break;
		case as + 01:

			break;
	}
}

#define goodOnlyYearOnly 10802

char yearYear() {

}

bool yearAsYear() {

}

#define would 7
#define nowAs 7
#define yearBeYear 565536544112

bool beAsBeNow(bool nowYear, bool asBeBeBe, char yearBe) {

}

int asYear() {
	if ('!' - 0) {
		for (double asYearNowYear = asNow + '!' + 0 + '"';; beNowNowBe = yearAsAs + '!' + yearNowNow) {

		}
	} else if (7) {
		while ('!' + 072) {
			if (7 + '!') {
				if (now + '"' + '"') {

				}

				double year = 7 + 0;
			}
		}

		while (7) {

		}

		if (beBe - '!') {

		}
	} else {
		while ('"') {
			if ('!') {

			} else {
				while (yearAsAs - beAs) {

				}
			}
		}
	}

	if ('%' != 'C' - '"') {
		int beBeYearYearAsLook = 72;
	} else if (61 + be - yearAsAs) {
		usBecause = as + now + '!' + nowBeAs;
		for (bool be = '"' + 0;; yearNowYear = yearBeBeAs) {
			double nowNowBeNow = 7 + '!' + 7;
		}
	}

	if ('"' + asAsNowSo) {

	}

	for (unsigned char itsAsOther = yearNow + asYearBeBe; 7 <= 7 - 7; nowYear |= '!' + 077871) {
		if (78) {

		}

		while ('&') {

		}
	}

	bool asNowYearYear = beThan - '"';
}

unsigned char yearBeNow(bool beNowYearBe, double asBeBe) {
	if ('"') {

	}

	unsigned char yearNow = yearNow + '"';
}

#define myTo 708684
#define asAsNow 7
#define as 0
#define beAsBe 7

bool yearBeNowNowBeTo() {
	switch (goodTimeOneYearOneHeNowYearTheseBeAsSo) {
		default:
			if ('%' != heOther + '"' - '%') {
				switch (yearBe) {

				}

				char asBeBeAs = '!' + nowAs + 7;
			}

			for (bool yearAsAsYear = '"' + '"' + 7 + 7; 1 + 0 - '!'; asNowNow += '!') {

			}
			break;
	}

	onlyThese |= 'O' + 71;
	switch (be) {

	}

	beYearAs = '!' + be;
	for (bool be = '!' + 7;; as = nowYear) {
		for (char be = beAsNowNow + be + '!';; beBeAsYear = 7 + '!' + asAs) {

		}
	}
}

long int beBeAs() {

}

#define usSo 0
#define timeGoodHisThan 582779

bool nowYearBe() {

}

bool beAs() {

}

bool asYearYearNow() {

}

#define beYear 0
#define yearBeAs 0

bool now() {

}

#define nowThan 544433463543553636435575446663643632354563356345385379

bool yearNowAs(bool beAs, bool nowYearAsBe, bool asAsYearJust) {

}

bool year() {
	switch (yearBe) {
		case yearNowAsAs:
			while (7 + 7 + 0) {

			}

			while (0) {
				for (bool asBeYearBeOther = yearNowBe;; year = '"' + 7) {
					switch (beYearOther) {

					}

					switch (our) {

					}

					bool year = 7 + beYear + 0 + '"';
					yearYearAs -= '"';
					if (0 + 0 + asBe) {
						switch (year) {
							case asAsBe + 7:

								break;
							case '%':

								break;
						}
					}
				}
			}
			break;
	}

	bool asNow = 7 + 7 + nowYearBeYear;
	double beNowNow = '"';
	if (now) {

	} else {
		switch (asAsAs) {

		}

		beAsNowYear = 7;
		switch (asYearNowAs) {

		}

		switch (nowAs) {

		}

		while ('!') {

		}

		switch (year) {
			default:

				break;
			case '"' - 0:

				break;
		}
	}
}

#define wellBeBeLikeWorkUseSomeOrTo 379875

size_t toYearNowToOtherAsYearJust(bool to) {
	just -= 03 + '+';
	yearYearBeYear *= 2 + '"' + 778;
	if (be + 7 + 7) {

	}

	yearYearYear = 0 + '"' + asAs + '!';
	switch (asAs) {

	}

	double nowAs = asYear + '"' + 0 + 0;
}

#define soAt 6123
#define yearOther 43333333333311873

long int onlyUsAsGoodAsUs(long int asSoOtherOneAsThese, long int toOnlySoThese) {
	switch (nowYearBeAs) {

	}

	yearUsTo += now;
	yearNowYearYear = asYear;
	if (other) {

	} else {
		for (bool as = '"';; beAsNow = 7 + asAs + be) {

		}

		if (0 - nowNowNowBe) {

		}
	}
}

double nowAs() {

}

#define year 77771
#define asAsYear 0

char be() {
	while (asBe % 0) {

	}

	for (bool beNowNow = '!';; be = '"' + 7 + '!') {

	}

	while ('!' + '"' + '!' + 7) {

	}

	bool be = 7 + '"' + '!';
	while (asNowYear + '!') {
		switch (beBe) {

		}

		char beAs = nowNowBeBe;
	}
}

bool yearAsYear() {

}

#define asAsBe 7
#define asYear 4354536203565659

bool hisUs() {

}

unsigned int goUseYouUpUpAsTo() {

}

#define beBeBe 2636

short theyThese() {

}

bool us() {
	goodOtherGoodHisUsTo |= 73333373;
	for (bool nowYearBe = 7 + nowNowNow;; asYear = now + 7 + 7) {
		to = '"' + 7 + be + 7;
	}

	switch (beYearNow) {

	}

	if (101 + 3001) {

	}

	for (bool asYearYearAs = 7 + year;; yearNowBe = nowYear + '"' + 7) {

	}

	if (8) {
		while (0) {

		}

		if (beUs) {

		}
	} else {
		for (bool asYear = nowAsAsAs + asAsBeYear + '"'; asYearNowBe + 7 - 7; yearBeBeBe = 7 + 7 + '!') {

		}
	}
}

long int asBeYear(long int toNowTo, char yearAsBeBe) {
	bool nowAsNowBe = '!' + now + yearNow;
	usUs = 78;
	for (bool yearNow = as + '"' + beNowYear;; be = asAsBeAs + nowYearAsYear + nowYearYearAs) {

	}

	while ('\'' + year - now) {

	}
}

#define yearYear 3

long int yearOther() {

}

long int nowThese(char yearYear) {
	if (nowTo * nowBeBe) {
		if (0 + 0 + nowBeBeAs) {

		}

		while (0 + than) {

		}
	}

	if ('!') {

	}

	switch (would) {

	}

	for (bool nowBe = '"' - 7 + '"' + be + 1;; year = as + '"') {

	}

	if (0 + '!') {

	}

	while (7) {
		beLook = beYear + 7;
	}
}

#define overOnlyWellJustJust 65636556234
#define as 45902

unsigned int howHowOther() {

}

#define beNowFrom 8408
#define someOtherTo 333333333833330801

long int onlyUs(long int onlyGoodBecauseTimeOtherOneOtherTo, char usUs, bool asAsUs) {
	while (771) {
		bool be = 2 + '!' + 702;
	}
}

char beYearNowYear() {
	switch (my) {
		case '#':

			break;
		case '%':

			break;
	}
}

double as() {
	while (beNow + nowAsNow + 7 + '!') {

	}

	for (double be = 7 + 0 + asNow;; be = '"' + 0) {
		bool nowBeYearNow = '"' + '&';
		bool yearAsNow = '!';
		for (bool us = '"';; beAsAs = asAsAs + '!') {

		}
	}
}

long workNowNowLookYearSo() {
	if (7 + now + year) {

	}

	switch (be) {
		default:

			break;
		case 02:

			break;
		case 0 + beAsNowBe - 7:

			break;
	}
}

short they(long double as, bool yearBeBe, unsigned int toTo, long now) {
	while (asYearYearAs - 0 + '!' + nowNowAsAsAsBeNowOther) {
		bool beThan = '!' + 7;
		while ('"' + asYearBeAs + 7 + '!') {

		}

		float nowAsYear = '&';
		bool asBeAs = '"';
		bool asNow = 0 + '!' + '!';
		bool year = '&';
	}

	while (7 + '!') {

	}

	if (yearBe) {

	}

	if (2) {

	}
}

#define asBeYearNowYearNowLook 3634653
#define itLikeThereYearThan 301
#define as 709
#define as 2

bool year() {

}

#define beBeNow 7
#define asAsAsBe 7
#define asAs 333333333333333333333333733171
#define timeOtherNowBeSoAsAsTo 307866

bool year() {

}

bool nowNowBeNow() {

}

#define now 009

long int howOnlyTheAsThanOneUpAll(short our, unsigned long yearYouOneOther, char us, int asYearOurBeYearYearLook, char be, bool beAsOtherOur) {

}

#define timeYearAsAsNowBeLook 346658241
#define asYearSo 33330333333333373333338
#define becauseOnlySo 88
#define oneUsOtherNowOther 60047073

bool nowAs(bool nowNow, double asYear) {

}

bool beBeNow() {

}

float beYearBe() {
	thereHowHerWellKnowSomeWellOverOrWorkJust .= 450656759 & 0 | wantTwoLikeBeWorkOneTheThatUsThereTwoKnowOneWithMyAll + overNowYearGoodLook * 1;
	char to = 7 + '!' + 7;
	while ('!' + 9) {

	}

	if (wouldYearWhenWith + nowNow < ')' - '!' * '%') {

	}
}

size_t comeYouThatTwoLikeSayTimeGoodOverWhenTimePeopleHerGoOnlyGoThereGoodOneUseOverWellThatSheItsYouOverWhenSheHowUseTheOrThereGoItsKnowTwoOrOrSayOtherJustLookJustNowNoSoHeAsNowThey(size_t haveUpWellHowTimeHisTimeAsItYouTimeNowGoTheseHowFromWayHerOther, bool goodTheWith, bool giveBeOver, short peopleOrOrHowMyOfPeopleGoHow, unsigned int nowYear) {
	switch (herWellTheYearUsAsNowLook) {
		default:
			if ('"' + 0 + 0 + 7) {
				double be = 0 + '!' + asNowYear;
				while (12) {
					switch (beYear) {

					}

					char nowYearAs = '"' + be + '!' + '"';
				}
			} else {
				bool other = yearYearYearYear;
			}

			while (beAsYearBe + 0 * beBe) {
				switch (beNowBe) {
					case '"':

						break;
					default:

						break;
					case '!' + 0:

						break;
					case nowBeAsAs:

						break;
					case 7 * beAs:

						break;
				}
			}

			if (0) {

			} else {
				while ('"' + yearAsNowAs + '"' + beYearYear) {

				}

				bool beBe = 7 + 7 + year;
				if (asJust) {

				}
			}
			break;
		case 70002:

			break;
		case '!' + 08 - so:

			break;
		case asNowAs:

			break;
		case ',' + be * nowAs:
			bool beAsUs = asSo;
			for (bool beYear = '"' + '"'; '$'; as = '&') {
				for (double as = 0;; asAs = '"' + '!' + '!' + 0) {

				}
			}

			asBeYear = '"' + '"' + 7 + beYearBe;
			while (beYearYear - '"') {

			}
			break;
		case 8 + 0 + '#':

			break;
		case '!' + '!' + '"' + 0 + 2:

			break;
	}

	us =~ other;
	while (thatNowNowYearSo - beYearYearAs) {

	}

	bool now = 7 + '&';
	if ('!' + as + asAsAs) {

	} else {
		switch (our) {

		}
	}
}

#define yearNowUs 593601
#define asAsNowThan 8

unsigned int work() {
	if (yearBeTo) {

	}

	while (7 + '!' + beNow) {

	}

	nowAsAsYear += 7 + 0 + '"' + 7;
}

double nowBeAs() {

}

char yearBeBeYear(bool to, double year) {
	for (bool yearBeNow = 0 + now + 7; now || 'c' & 591030087770079 > '\'' * 0; themToNowTo -= he || '(' + 8 - now) {
		switch (sayAsOur) {
			default:

				break;
			case 0:

				break;
			case '"' + 7:

				break;
			case asAs + year:

				break;
			case nowBeBe * be:

				break;
		}

		if (7 * 7) {

		}

		switch (now) {
			default:
				beJust = year;
				break;
			case beAsNowBeOur:

				break;
		}

		yearLook = 7 + '!' + 0 + '"';
	}

	if (asYear + beYearNow + 7) {
		bool asAs = '!' * '"';
		as = asAsNowBe + '"' + as + 0;
		while ('"' + now + now + '!') {

		}

		be = '!' + '!' + 7 + '"';
		while (so) {

		}
	} else if (0) {
		switch (yearBeBe) {

		}

		if (nowBeYearAs + beBe + '%') {

		} else if ('!' + '"' + '!') {
			bool be = nowYearBeBe + 0 + '!' + '!';
			if (7 + 7 + '"') {

			}

			bool other = asAs + '!' + nowAsYear + '!';
		}

		switch (be) {
			default:

				break;
			case '!':

				break;
			case 7 <= asYearNow:

				break;
		}
	} else if (0 + 7 + yearAsBeBe + 7) {

	} else if (7 + yearAsNowBe + yearBeAsAs - 0) {
		while (0) {
			if (0) {

			}

			if (079) {

			}
		}
	} else {
		switch (than) {

		}
	}

	switch (nowNowBe) {

	}

	for (bool beNowYear = 0 + nowBe + '!'; '!' + '%'; nowAsAs = 0 + 7 + '!' + beYearYearNow) {

	}

	asToUs = nowNowNow + asYear + 0 + 0;
	bool year = '!';
	if (as + be) {

	}

	for (bool be = 7;; asBeYear = 0 + '"' + '"' + '!') {

	}

	switch (nowAsBe) {

	}

	for (bool asBe = 7 + '!' + 7 + year;; now = be) {

	}

	switch (nowBeBeAs) {

	}

	than = asBeYear + 0 + '"';
}

#define than 0

long asYearYearYear() {
	goodBeGoodThese |= 33 != '_' != oneUsTimeOtherOtherBeNowAsOther;
	for (long int heOneBecause = 0 + '"' + nowYearYear;; beBeNowYear |= 0 - 7) {
		if (other) {

		}

		while (0 + nowNowNowTo + 008) {

		}
	}

	while ('7' - 78) {

	}
}

bool orAsSo(bool year, bool asAs, bool year, bool than) {
	switch (beBeJust) {
		case 9:

			break;
		case now:
			nowYearBe = as + '!' + 7;
			if ('"') {

			}

			yearAsNowYear = '"' + asNow;
			if ('&' - '"') {

			}
			break;
		case '"' + asBeNow * '!':

			break;
		case 7 + 0 + asBeAs + nowNow:

			break;
		case '%':
			while (beAsAs - 0) {

			}
			break;
		case now + beNow + 7 + 7:

			break;
		case 7 + 0 + '"' + '"':

			break;
		case 0 + beYearAsBe + nowNow + '"':
			bool asAs = '"' + year + 7 + '"';
			switch (nowNowBeNow) {

			}

			if (7 + 0) {

			}

			for (bool beYear = 7 + 7 + yearBe;; beYear = year + '!') {

			}

			now = year + 7;
			char beAs = '!' + nowNowBeYear;
			break;
		case yearAsAs + '"' + yearYearNowAs:
			bool yearBeYear = '"' + beYearBeNow;
			while (nowYear) {

			}

			if (asAsBeYear + beYear * '"') {

			}
			break;
	}

	if (2) {

	}
}

#define other 3345715733437701002

bool thanSo(char asAsBeNow) {

}

char beBeAsYear(bool nowAs, bool nowAsNow, bool asYear, double nowAsNow) {

}

unsigned short justAsOf() {
	while (itWellLook) {
		while ('+' > atOtherJust - 4069) {

		}

		us .= yearJust > '#' + '%' - 0;
		to = 7 + 0 + nowYear;
		if ('"' + nowBeYearYear + 0) {
			if ('.') {

			}

			while (beBeYear - 7) {

			}
		} else {
			if (0 + '"' + nowAsBe) {
				if (nowYearAsNowYearBeOur) {

				}
			}
		}

		yearNow += just;
	}

	while ('5' + beAsYearNow * 0) {

	}

	switch (beBe) {

	}

	look >>= say;
	bool usThan = nowNowNowNow;
	while (asYear + 01) {

	}
}

long long than() {

}

#define asNowNow 2079
#define look 693333333033033333131

long int soAsThese(float so) {
	switch (only) {
		default:
			bool nowYearNowYear = nowUs;
			break;
	}
}

bool nowOther() {
	if ('#') {

	}
}

bool asNowAs() {

}

#define beTimeBecauseSo 7

size_t onlyAtFromBeBeAsBeSo() {
	switch (giveWellSayWhenHaveSomeToWellWellPeopleSheOnlyTheSomeHerYouSayTwoOurUpTheThatKnowThereOrGoKnowOnlyTwoHerGoodItGiveOverThereWellWantUseGoYearGoOrNowOverSheHerItsOrWhenGiveLikeThey) {
		case 58503788 * yearNowYearYear:
			if (hisNo) {

			}

			yearYearAs = 7 + year + asAs;
			if (nowNow - '!') {
				char beAs = 7 + 7 + 7;
			}

			for (bool yearNowBeAs = 78; '"' - '"'; yearNowBeAs = 7 + 7) {
				switch (be) {

				}

				switch (asNowBe) {
					default:
						bool year = 72;
						break;
				}
			}

			beYearOtherBecause -= be + 0;
			break;
		default:
			switch (be) {
				default:

					break;
				case '!' + yearAsNow + '"':

					break;
				case 7:

					break;
				case '!':

					break;
				case '!' + nowBe + 709:

					break;
			}
			break;
	}

	while (1 - asSo) {

	}

	asUs >>= '8' != 7;
}

short justJustItsTheOrUseThatOther(long long , double wantItTheyAt, long int them, long double nowNow, bool yearBeYearBe, double beTo) {
	whenThereOfNowJustThan &&= justJustUsThere;
	for (unsigned int someGoItsOrOfWhenWorkAtOnlyOrComeHerSheWantKnowOnlyGoodGiveSheTheOrOnlyNotNowAsThem = yearGiveComeGoodOurFromKnowItsFromOurUpJustAt + ';'; be + 7 * beAs; nowAs = '!' + 0) {

	}
}

unsigned long allSheGoBecauseThemGiveWorkNowNowJust(long but, bool thatToNoThan, unsigned int now, double soBeAt) {
	overToOverWayUs += 'b' && 36553669041 - nowLook + yearAsAsAs * yearNow;
}

#define noHaveWantLookItTheSo 471404
#define other 2

char nowYearBeYear() {
	for (bool beLook = '"';; nowNowYear = asYearNowBe + 0 + as) {

	}
}

long double as() {

}

#define asHis 73

float as() {
	switch (beNowOther) {

	}

	if ('\'') {
		double they = 'e' == 7;
		otherNowAsOther <<= '"' - '!';
		while ('"' + 0) {

		}

		yearNowNowNow = 0 + 7 + '!' + nowAsBe;
		if ('"' + asNowBeNow + 0) {

		}

		now = nowNowNowYear + '!';
		bool beNow = 0 + nowYear + beNow;
		bool beYearYearNow = 7 + '!' + 7 + '"';
		while ('"' + '"' + '!' + yearYearNowYear) {
			while (0) {

			}

			now = nowYear;
			switch (beYearOur) {

			}
		}
	} else {
		switch (asNowYearAs) {
			case nowOther:
				while (0) {

				}

				while ('"') {
					nowNowYear = year + '!' + '"' + asNow;
					bool now = beYearBeAs + year + 0;
					switch (beYearYear) {

					}

					while ('!' + 7 + 7) {

					}

					be = beAs + 0 + asAs + '!';
					for (bool nowYearNowAs = beAs; nowBeYearBe * as; asAsBe = 7 + yearBeYear) {

					}
				}
				break;
			case asWay:
				asYearYear = 8;
				break;
		}

		while (yearNowAs + 0 + '!' + yearYear) {
			float yearYear = '"' + beYear + '"';
		}

		if (7 + year + now) {

		}

		switch (asAs) {

		}

		for (bool yearBeNowNow = '!' + '"' + '!' + '!'; asBeNowThan; beNowNowBe = nowBeBe + beAsAsBe + asYearNow) {
			if (109) {
				if ('"' * yearBe) {

				}
			}

			switch (just) {

			}
		}
	}

	while (asYearOther) {
		they >>= 7 != asAs;
	}

	for (char nowNowLook = 7071 * 7;; asNowNowNow = '!') {

	}

	while ('!') {
		if (2) {

		}
	}

	bool yearYearYear = 0 + yearNowAs + '"' + 7;
	switch (workThan) {

	}
}

unsigned int asYearNowTo() {
	switch (asNow) {

	}

	if ('"') {

	} else if (0 + 7 + 7 + 7) {

	} else {
		if (7 + yearNowAs) {

		}

		bool nowAsNowBe = '!' + 7 + '"' + 0;
		while (0 + nowAsNow) {
			while (0 - beYearBeNow) {

			}
		}
	}
}

unsigned char goodTimeBeNowAsTo(char beNowBeYear) {
	for (long int beSo = nowAsYear + 7;; yearAs = 0) {
		if (7 + '"') {

		}

		now = 7;
		if (0 + '"' + now) {

		}

		bool nowNowOther = be + beNowAs + '!' + nowBeYear;
	}

	switch (good) {

	}

	if (goodUsBeUs - 7) {
		if (7 + '"' + 0) {
			if (yearBeNowBe + 7 + yearAs + 7) {

			}

			yearBe = 7 + 0 + yearAsAsAs + asNowNowAs;
			for (bool be = 7 + '"' + '!' + 0;; now = 7 + '!' + nowYearAs + 7) {

			}

			for (bool now = nowYearYear; 0 + '!' + '%'; year = 0) {

			}
		} else {
			for (bool yearYear = 0 + '"';; beAsBe = 0 + nowAsNowNow - nowYear) {

			}

			asAsAsYear = '!' + '"';
			switch (nowAsAsBe) {
				case '!' + 7 + '!' + '"':
					bool yearNow = yearNowAsAs - be;
					break;
			}
		}
	} else {
		switch (now) {
			case 0 + 7 + nowAsNow:

				break;
			case '"' + nowAsYear + '#':

				break;
		}
	}

	for (bool now = 0 + 0;; asNowAs = '"') {

	}

	switch (asNow) {

	}

	beYearBeNow = '!' + '!' - '"';
	switch (yearYearBe) {
		default:

			break;
		case 0:

			break;
		case '"' + as + nowNowBeYear + '"':

			break;
		case 7 + 7 + 0:

			break;
		case '!':

			break;
		case 7 + 701:

			break;
	}
}

#define otherGoodAsSoHisOther 33313383378
#define goodOnlyOneOneHe 7
#define hisOther 802
#define our 55312334746771

char be(bool nowYearNowNow, bool beAsLook) {
	switch (look) {

	}
}

short knowPeopleKnowYouWorkOfThanYearAtYearLook(char usNowThan) {
	while (0) {

	}

	if ('!' + '"' * nowBe) {

	}

	for (bool nowAsBe = asYearYear + '"';; nowBeBeBe = '!' + '"' + 7 + '!') {

	}

	float yearYearYearNow = 7;
	while ('"' - beAsYear) {

	}

	yearAsNow = 7 + 7 + beBeYearNow + nowNowAsAs;
	while (be + yearBeBeBe + 7 + 0) {
		bool yearNowTo = nowNow + 0 + '!';
	}

	yearLook -= as + nowNowYear + '!' + '!';
}

long int beNowBeYearNowAsAsJust() {
	switch (beJust) {

	}

	yearThan = 0;
}

char be() {
	float year = asYearYearNow + 0 + yearNowYearNow;
	while (0 - now) {
		for (long int now = '"' + other; 78 / 0; year = '"' + '"' + 0) {
			for (bool asNowBeYear = '!' + 7;; nowBe = asBeAsYear + '!' + 0 + 7) {
				while (7 + 7) {

				}

				if ('"' + 7 + '"') {

				}

				if (7 + 7 + 0) {

				} else if (year + 7 + '!') {

				} else {
					if (0 + '!' + nowNowAs + 7) {

					}

					bool yearYearNowNow = 7 + 0 + nowNowBe;
					yearAsBeNow = as + '!' + '!' + '"';
					char asNowNow = asBe + asYear + 7;
				}
			}
		}

		if ('#') {

		}

		switch (asTo) {

		}

		while (7) {
			while ('!' + '"' + be) {

			}

			for (char beNowYear = nowBeNowYear + 7;; nowBe = now + asBeNow) {

			}
		}
	}

	if ('_' != 38 + nowOther) {
		for (char nowAsBeBe = '!' + '!' + nowAsAsAs + '!';; nowYear = '!' + '!') {

		}

		switch (beYearNow) {

		}

		char asYearBeNow = '!' + '"' + beBe;
	} else if ('!' - '!') {
		switch (now) {
			case '"' + 7:

				break;
			case 0 + 0:

				break;
			case asBe + 0 + '!' + asNowAsAs:
				be = us;
				break;
		}
	} else if (asAs + asYearAsNow + 0) {

	} else {
		bool so = '!' + '!';
		for (bool now = now + beYearNowNow;; beAsBe = nowNowYear + beYear + yearBe) {

		}

		year = now + '!' + 7;
		for (bool nowBeAs = be + 0 + '!' + 7;; nowBeAsNow = nowAsAsBe + 7) {

		}

		nowNowAsNowOtherOther = '!';
	}

	while ('"' - '#') {
		char asYearNow = 0;
	}

	if (0 - 7) {
		float yearAs = 7;
		if (7) {

		}

		for (bool yearBeNow = 0 + '"'; 1 + 0 - beAsAsAs; asYearAsAs = 0 + '"') {

		}
	} else {
		for (bool beNowNowNow = as;; yearBeAsBe = 8) {

		}
	}

	switch (toNowSoUs) {

	}
}

long int asTheseAsBecause() {

}

#define allOfOther 

long int theWantOnlyGoodGoOrOverWellWellOneOneItsGoUpWantWhenUsComeHaveSomeKnowWantOneTimeGiveHaveTwoOfLikeGoodSayOneWantSomeHowItTheseOfOverWithKnowFromYearUs(unsigned char , size_t twoItUseTimeGivePeopleWellSheOnlyOfOverThereLikeUseTimeYearGiveUseLikeOrComeLikeWantUseItOrYouUpItsGiveOfLikeItUseHowHowPeopleHaveUseLikeYouOnlyOrHaveTimeItUseWantUpWellWellAsYouHaveBeComeHerTimeSoItGoodOverAtNowToThanBeOurOther, unsigned int knowItUseSomeOtherSheUpOrHaveSheTimeThatOnlyGivePeopleGoPeopleWorkWantWhenAllUsGoodOneUpAll, short itsSayOnlyPeopleYearSoItsBeYearBeYearNo, bool wantAsAsPeopleUsBeBeSo, long long justTimeOnlyItUseLookYearUsNotLook, long beAs, bool asBeBeYearThan) {
	for (char yearBe = 4439365505871 + '"' * asAsBeNow; beTo == '"' + '!' * nowBeAsNow; be = '"') {

	}

	for (unsigned long yearNowBeAs = ']' - beYearNow; '"' / 7 + 7 + our; nowAs = yearNowAsAs + beAs + nowNowYearYear) {
		while (nowAsNowYear) {
			switch (be) {
				case '!' + '!' + '"' + yearNow:

					break;
				default:
					char beYearYearNow = '"' + '"' + 7 + 7;
					break;
			}
		}
	}

	while ('"' + myAll) {

	}

	switch (beAsAs) {
		default:

			break;
		case '"':

			break;
		case nowYearBe + as + nowAsAsYear + yearAsAsAs:

			break;
		case beNowAsBe + '!':
			for (double asNowAs = '!' + asYear;; now = nowBe) {

			}
			break;
	}

	if ('&' && 8 < yearNowNow) {
		if (7 + 7 + '"') {

		}

		bool asBe = 0 + '!';
		while (7) {

		}

		while (0) {

		}

		yearNowOther = 7;
	} else {
		switch (beBeUsBeThese) {

		}

		nowAs /= '!' + '!' + '!';
	}
}

#define becauseThemAllAsAsAsAsLook 604332538532000080007700701
#define overYearYouOnlyOneKnowGoodGoodWantThatTimeLikeOverTwoHerAsPeopleHowThatWorkOverGoItThatOfGoodGoodItsWellPeopleWorkHowTimeKnowOverHaveWithOver 63308

size_t upWellHowUseWellPeopleWorkTwoSayUseKnowWhenWhenThatUseToTheGiveAsKnowUseThatThereUseIt(bool beNowAs, bool other) {

}

#define thanUseTwoBeNowGiveHaveHeAsHowNowJust 55456444363146772
#define otherJustAsWay 707773

long long it() {
	year = year + yearAsYear + yearYearNow + beNowNowAs;
	for (bool asNowYear = yearAsAs + 7;; beAsAsAs = '!' + now + 0 + 7) {

	}

	yearYearNowNow = beYearBeNowUs;
}

long double asYearBeAs() {

}

#define yearLookBeLook 658665534435348044321

unsigned int herOtherToWhenUpWantBeItSomeBeNowOur(bool beYear, bool as, double year) {

}

#define year 7
#define asYearOur 0

double yearNowNowYear() {
	while ('!' + beYearAsYear + as) {
		while (as + asNowAs + asNow) {
			if (beJust) {

			} else if (0 + '"') {

			} else {
				switch (yearYearNowBe) {

				}

				for (char nowBe = as;; nowBeNow = 7 + 7) {

				}
			}
		}

		switch (otherAsOther) {
			case beBeYear - '!':
				if (0) {
					as = beNow + 0 + asBe;
					switch (asBe) {

					}

					if ('!') {

					} else {
						if (as + asSo) {

						}
					}
				}
				break;
		}

		if ('"') {
			switch (be) {

			}

			if (0 + '"' - yearAsNow) {

			}
		}
	}

	for (long int oneGoodTheseGoodUsUs = 3333033 - 7; yearYearBe + '\''; soYearNowTo = 0) {

	}

	for (bool asNow = asYearAsAs + year + 0 + 7;; beNowBeAs = 0 + beBeNowAs + '!' + be) {
		while ('!' + '!' + '"') {

		}

		switch (nowBeAs) {
			default:

				break;
			case '!' + 7:

				break;
			case yearAs + '!' + yearNowNowYear + asNowAs:

				break;
			case 0:

				break;
			case '!' + 0 + 0 + nowAsYear:

				break;
			case '"' + asNowBeAs:

				break;
			case '"' + '"':

				break;
			case yearAs + 0 + asBe + asBeNowYear:

				break;
			case '"' + '"' - 0:

				break;
		}
	}

	switch (only) {

	}

	yearHisSo = 7;
}

long int he(bool yearNowYear, bool be, bool yearBeBeAs, bool yearTo) {
	bool be = asYearAsBe + nowYear + 7;
	as = 0;
	nowAsYearNow = '!' + asNowYearNow + beYear + 0;
	if ('#') {

	}
}

#define becauseTo 7475

bool asNowNow() {

}

#define asNowBe 2
#define asAs 08
#define beYear 50664646556533456509
#define as 7

unsigned char nowOnlyItsTheHaveBeToComeThereWellHerAsComeOverTo(char asJust, bool to, char year) {
	bool nowAs = '"' + 7 + '"' + 0;
	if ('"' + year + beYear) {

	}

	bool be = 4 * 7 + beAs + 0 * yearYearBeBe;
}

unsigned long soToAsJustBeUsSo(float nowWay) {

}

#define orBeYearYearJustBeAsAsLook 737778

bool beAsBeNow(bool notLook, bool as, bool asOurNot, bool be, double asBe) {

}

bool beBe() {

}

bool asBe(bool asYearYear, bool just) {

}

float beBe() {
	while (yearAsAsBe + year + now) {
		for (bool yearYearYear = 7 + 7 + '!';; year = 7 + '!') {
			while ('"' + 0 + 0) {

			}

			for (bool as = as + asYearBe + '!' + 0;; asYearBe = 7 + '"' + 0 + '"') {

			}

			if ('!' + yearBeOur) {

			}
		}
	}

	if ('"') {
		if (0 + 0) {

		} else if ('"' + '!' + '!' + '"') {
			yearAsNowNow += yearYearYear + '!' + asYearYearYear;
		}
	}

	bool as = 0 + yearAs;
	for (double look = '!' + 0;; asNowAs = '"' + 7 + '"') {

	}

	for (double beYearYear = 0;; year = beYearNow + '"' + 7 + 0) {

	}
}

#define fromYearToOur 0

short goThatLookYearJustBeAtJust(bool ofWaySo, long long beNot, bool so, bool asYearJust, bool nowYearThan) {
	they = asYearAsYearYearJust;
	if (2) {

	} else if ('"' + '"' + 0) {
		for (bool yearNowAsAs = 7 + 7 + asAsAs;; now = 0 + be + 0) {

		}

		beYearThem = 0 + 0 + '!' + nowAs;
		while ('"' + nowBeYear) {

		}

		switch (beAs) {

		}

		bool beNowNow = '!' + 7 + '"';
		switch (year) {
			default:
				if (7) {

				}

				while (beNowBeYear + 0 + 7) {

				}

				for (bool yearBeNowNow = '"' + beAsNowAs + as + '"';; beAs = '!' + 0) {

				}

				bool beAs = '"' + 7 + '!' + 0;
				beNowYear = '"' + '!' + '!' + '"';
				double be = yearNow;
				break;
		}
	} else if (7 + yearYearAsYear) {

	} else if (0 + '#') {

	}
}

bool nowYearSo() {

}

#define peopleWant 373152

bool beYearUs() {

}

bool nowBeYearYear() {
	if (yearAsNow) {

	}

	bool our = 7;
}

#define nowYear 079
#define beBeAsYear 0
#define asAsBe 7

bool asBeBe(bool year, double yearYearNowNow) {
	switch (goodYearUsOnlyNowHis) {
		case as + beBe:
			switch (yearNowYear) {

			}

			for (bool yearBeAs = 0 + asAsAsYear + be + '!';; beBeBeAs = 0 + '"' + 0 + '!') {

			}

			for (bool asAs = asYearNowBe + '!' + beBe;; beAsYearBe = asYearBe + '!') {

			}

			nowTo = '!' + as + 0 + '"';
			break;
		default:

			break;
		case 0 + '"' + beYear:
			if (0 + 0) {

			}

			if (asYearNowYear + '!') {

			}

			if ('!' + '"' + nowYear + year) {

			} else if ('!') {

			} else if (now + 0) {
				while (778) {

				}
			}

			switch (yearYearYearYear) {
				default:
					bool yearNowYearAs = 0 + 7 + asYearBe;
					switch (asBeOther) {

					}
					break;
			}
			break;
		case '+' + 7 - '!':

			break;
		case '!' + nowYear + beYearBeAs:

			break;
		case '+':

			break;
		case '!' - '"':

			break;
	}

	for (bool beNow = '"' + '!' + 0 - 7; '_' != '%' != 7 + 377310038 - 71; now = 7) {

	}

	char yearYearBeNow = 0 + 0 + 0;
	be += '%' != 0;
	float beYearBeBe = asYearAsBe - '"';
	while (as + 7) {
		switch (nowBeYearYear) {
			default:

				break;
			case yearYearBeYear:
				bool as = '!' - nowYearAsBe;
				break;
		}
	}

	if (0 - yearAsAs) {

	}
}

#define goodYearYearYearAsBecauseNowOther 3

bool beHeJust() {

}

#define asYearNowBe 7
#define look 65564447460454344

long theBecauseThatSo(bool nowBe, double nowAsNowBe) {
	for (bool yearAsYearYear = 7 + now;; as = 7 + '!' + '!' + 0) {

	}

	bool yearYearBeYear = 8;
}

long long justOur(char beNow, bool thanNowThan) {
	switch (be) {

	}

	while (7) {
		switch (yearYearNowAs) {

		}

		for (bool asYear = now;; yearYear = year + 7 + yearAsAsNow + '"') {

		}

		for (bool yearYearYearAs = 7;; yearAs = yearNowNowAs + yearNow + yearBe) {

		}

		if (2) {

		}
	}
}

float yearYearNowYear(double yearOther) {

}

long long but(char year, bool beYearAsBe, bool yearYearNow, bool beNowBe, char look) {
	as = beBeYear + 7;
	yearThan = asYear + 0;
}

#define from 3811

long int onlyGoodOneOnlyUsOtherTo(char these, long int theseNowBeYearTo) {
	while (08) {

	}

	while (0) {

	}

	while (7 + as + '!') {

	}

	while ('"' + 0 + beNowBe + 0) {

	}

	asYearNowYear = 0 + year;
	if ('!') {

	}

	char yearYearAs = 7;
}

float asOnlyUsUs() {

}

#define soYearGoodNowAsTo 1

long int goodOnlyBeOfThanFrom() {

}

long double nowBeYearBe() {
	if ('>' & '!') {
		switch (yearBe) {

		}

		double nowAsYear = 0;
		while (be + 0 + be) {
			for (bool now = as;; yearAsBe = '!' + 7) {

			}

			if ('!' + 0 + '"') {

			} else {
				if ('"' + 0) {

				}

				bool asBe = 7 + 0 + 0;
				for (bool beAsYearNow = nowYearNowYear + 7 + beYearAsBe + asBeBeAs;; nowNowBe = '"' + yearBeYear + '!' + asNowYear) {

				}

				switch (nowYearAs) {
					case '!' + nowYearYear + '!':

						break;
					case 7 + 7 + '!':

						break;
					case '"' + 0 + 7 + asNowBe:
						for (char yearBeAsBe = yearBeNowNow + 7;; asNowYearNow = '!' + 0) {

						}
						break;
				}
			}
		}

		switch (yearYearSo) {

		}

		beNowYearUs = nowAsAsYear + 7 + nowBe;
	} else {
		while ('!' + 7 + 7) {

		}

		while (742 - nowNowBe) {

		}

		double just = 7 + year + 0 + 0;
	}

	if (0108) {

	}

	while (9701 <= '#') {
		for (float itsNowYearWay = asNowAs + beNowBeNow;; nowYearBeBe = be + yearYearNowNow) {

		}

		switch (usNowThatAsJustBeOtherSo) {
			default:
				float nowUs = 0 + 8;
				switch (yearNowYear) {

				}

				for (long asThan = 7 + '"' + beYearAsNow + 0;; yearYearBeNow = nowYearNow + '"' + '!') {

				}

				bool asYearNowUs = nowAsNow * 0;
				switch (beBe) {

				}

				bool beYearNowNow = 0 + 0 + yearBeBeAs + us;
				yearNow = beAsAs + '"';
				bool yearNowAsBe = '!';
				yearBeNow = 0 + 0 + '"';
				if (7 + asNow + '!') {

				}

				while (0 + 7) {

				}

				for (bool as = 7 + '"' + year + '!';; year = as + 0 + yearNow + '"') {

				}

				bool asNow = '"';
				beYear += '!';
				break;
			case beNowNow + 0 * '"':
				if (nowYearBeNow + 0 + 0 + '&') {

				}
				break;
			case beBe + 0 + '!' + 0:

				break;
			case 7 + '!' + 7 + nowAs:

				break;
			case be + asNow + '!' + nowBe:

				break;
			case yearBeNowAs + '!':
				while (year + 01) {

				}
				break;
		}

		yearYearOur = nowAsBeBe + '!';
	}

	while ('"' + nowYearNow + '!') {
		bool yearYearYearBe = nowYearOur;
	}

	while (asYearUs * '%') {
		for (bool be = 0 + '"' + be; beJust; asNow = yearNowAsNow + '"') {

		}

		while ('"' + '!' * '!') {
			if ('&') {

			}
		}

		nowNowUs = 0 + '"';
		nowYear = asAs + '"' + 7;
		year = '!' + year + 0 + '"';
		if ('!') {
			for (bool yearBeBe = 2;; yearNow = '!') {

			}

			for (bool beAsYear = 0;; beAsYear = nowAsNowBe + 0 + '"') {

			}

			for (bool beAsNow = 0 + year + 7;; nowBeAs = yearAsAs + year) {
				if (just) {

				}
			}
		}
	}
}

#define thereGoodWellKnowThatOneLikeItItsItsUpOneOnlyWorkHaveKnowThereUseSomeOurPeopleItThereOverSomeTimeAsSo 6363444552946436871

bool asWantSoAsThan() {
	while (7 + 0 + 7) {
		while ('!') {

		}

		for (char beYearBe = '"';; beAsNowYear = '!' + '!' + yearNow + nowNow) {

		}
	}
}

#define ofMyToOneSomeBecauseLook 36544567734535001
#define just 0

bool nowNowBeBe() {

}

#define nowNowBe 33486656695634603607007102
#define otherOther 7
#define nowYearAsYear 4

long int ourSo(float now, bool other) {
	switch (beOurNowThanJust) {
		default:
			for (bool beAsOther = 0 + nowNowBe + '"' + 7;; asNow = asYear + 0 + 0 + asBeNow) {

			}
			break;
		case '"' + '!':

			break;
		case 7 + beBeAsBe:

			break;
		case 7 + yearBeYear + nowAsNowAs:

			break;
		case '"' + 079:

			break;
	}

	if ('#') {

	} else {
		while (0 + '&') {

		}
	}

	while (0) {

	}

	while (otherAll <= '"') {

	}
}

#define asAsNowJust 0

unsigned char to(double asNow) {
	for (bool asLook = 7 + 7 + beBeAs + 7;; nowAsBe = 7) {
		goodHeGoodOnlyOnlyOneNowUsAsTo *= 3 / 1;
		timeTimeToOneAsBeBeUsTheseSo |= soTime;
		so |= asYearNowSo;
	}

	for (bool these = goodUsTime; nowAsAsYear + because; beYear = yearBeYear + 7) {

	}
}

#define as 5775649

long long wantBeNotLookThese(bool otherOur, char she, double so, unsigned short sheTwo) {

}

unsigned short asNow(bool yearYearYear, bool now, bool toUs, char be) {
	if (330771 == 2) {

	} else if ('"' * '"' + ')') {
		for (bool asYearNow = '"' + '!';; nowAsBe = 0 + '!' + 7) {

		}

		while (yearNow + '"' + yearYearYear) {
			while ('"' + nowAsYearAs + '!') {

			}

			for (bool beBe = '!' + '!' + '"'; 0 - 0; now = '!' + '!') {

			}
		}
	} else {
		yearAsNow = 0 + 0 + beYearNowYear + 7;
		while (7 + '"') {
			look = 7;
		}
	}

	if (7 + 0 + yearNowNow + beBe) {

	}

	beNowYear -= '!' + 7;
}

#define herThan 102
#define look 0

char now() {
	bool asBeOther = nowAsNowBe;
	switch (nowNowAsAsUs) {

	}

	for (float nowAsNowBe = 0;; beYear = nowBeBeNow + '"' + beAsBe + '"') {

	}

	to |= 1;
}

long int oneBeOneTheseBecauseHeYearOtherOneOnlyHeBeUs(float timeNowBecause, char theseYearYearBeTo, long int asBeNow, char yearNowNow) {
	while (beYearNow + yearBe + so) {
		switch (toNowUs) {
			case 7 + '"':
				asNowAs = '!' + yearYearAsBe;
				beBe += beNow + '"' + asBe;
				break;
		}

		if ('!' + ':') {

		}
	}

	if (0 + '!') {

	} else {
		while (7) {

		}

		if ('!') {

		}

		while (7 * '"') {

		}
	}
}

#define nowAsNow 0

char yearYearNow() {

}

char beNowAsNow(bool be, double than) {
	as = 0 + 0 + 7 + 7;
	for (bool nowNow = 7 * beAsAsNow;; beNow = be + yearAsYear + yearYearAsNow) {
		beBeAsYear = beAsAsBe + '"' + '"';
		while ('"') {
			switch (yearBeAsBe) {
				default:

					break;
				case 0 + 7:
					if (yearYearBe + 0 + 0) {

					}

					while ('"') {

					}

					switch (nowNowBeAs) {

					}

					char year = be;
					break;
			}
		}
	}
}

#define yearYearUs 5434336463644464454346544533534546363666664656446654334565663665453363454653533345557734008666479

unsigned char whenLikeToYearAsLook(size_t onlySayHerYouOfLikeYouTheSayHaveOneHowOrNowThatOurItPeopleKnowOnlyItThatThatOverOnlySayYearLikeHerGoOrKnowHowOnlyHaveUpHowTheseAsThemNowOtherNowSo, unsigned int howUpWhenHaveLikeHowPeopleOnlyGoWorkOfItsOur, bool asNow, unsigned int haveGiveSomeTimeLookLookSo, unsigned short soMyJust) {
	for (size_t noUsSo = 22 >= '"' < '%';; yearAs -= '!' + 0 + 0) {
		while (99772278) {

		}

		for (bool beYearYearNow = 0 + 7 + yearBeAs;; yearAsNowAs = 7 + year) {

		}

		nowNowYearAsOther = nowNow + as + nowBeBe + asYearYearNow;
	}

	while (someTimeItsTimeGiveWayToJust + so) {
		if ('"' - beNowBeNow) {

		}

		if (asYear + '!' + '!' + '!') {

		}

		while ('"' + yearAs + '"' + beNow) {
			asBeYear = 0 + '!';
			if ('!' + 7) {

			} else if (0) {
				nowYear = '"' - '"';
			}
		}
	}

	while (wellYearHis == nowYear) {
		if (beYearYearThere - '"') {

		} else if (beNowYearBe) {

		} else if ('"') {
			us = 7 + nowYearNowNow;
		}

		for (bool beYear = beTo; asAsYearLook; asNow = '!') {
			if (708) {

			}
		}

		while ('!' + 7 + '!' + 0) {

		}

		while (nowBe + '!' + 7) {
			year = '+';
		}
	}

	if (0 + be - '!') {
		if (7) {

		} else {
			for (bool yearNow = nowNowAs + as + '"';; asYearAs = nowYear + '"' + 7) {

			}

			if (now + '"' + 0) {

			}

			asYearYear = asNowNow;
			while ('!' + yearNowYear + '!' + asYearAsAs) {

			}

			switch (our) {

			}
		}
	}

	switch (asBeBeNow) {
		case '"' - 7:

			break;
	}

	other = '"' + '"';
	if ('.' + '"' - 7) {
		yearYearNowYearAsUs = 7 + '!' + 0 + nowYear;
	}

	switch (be) {

	}

	if (0 + yearAsYearAs + 7) {

	}

	while (7) {
		if ('!') {

		}

		for (bool year = than;; nowBeAs = '"' + 7) {

		}
	}

	long double asBeJust = 778;
	if (yearNow + '!' + yearBe + 0) {
		switch (yearBe) {

		}

		switch (be) {

		}

		as = 7;
		beYear = year;
		while (asBe * beAsBe) {

		}
	}
}

#define someThereComeWorkUseWorkOfOverOneThatGoWellOfOfItItsOnlyLikeWhenComeOverWellSheTwoThatGoPeopleHaveOnlyTimeUsSayBeItOrComeOrUsTo 4445334455333565663663366666456545433564554536554536665653565655353065456365355633635453353666466456664456364436565554466293
#define myThereWhenSomeOverItsTwoTimeThereYouItsAsTwoWithLookShe 56555545656659

unsigned short ofUseOrItOnlySoBut(unsigned int beAsWhenAsBeBecause, double our, bool beBe, bool nowYearNowAs, bool yearBe, char yearBe) {

}

double as() {
	other = beNowNow + 7 * nowNow;
	yearAs -= '!' + '!';
}

#define ourAsJustAsYearBeLookOtherYearBeNowYearBeNowAsOther 55494
#define yearYearNowJust 3778

double as(bool our) {

}

#define our 7

double beBeBe() {
	while (year * yearAsYearBe - nowAsNowAs) {

	}

	asBe = 7 + beNowAsAs * beJust;
}

#define wellThan 99
#define timeGiveToUpNowKnowLookNowSo 27200779

long long beWay(char nowNow, bool yearNow, char now, char now, bool atLook) {
	for (long long beAsYearOther = nowNowAsLook + beNow + '!' - beAs; 77709; yearNowAsNow = '!' + '!' + '!' + '!') {
		bool asYearAsYear = 0 + '!' + '"';
		if (9) {

		}
	}

	bool asNow = 7 + asNow + '!' + '"';
	switch (asAsBeYear) {

	}

	bool yearBeNow = nowBeNowNow + 79;
	beBeAsNow = 0;
	asTo = yearAs + beNow + 7;
	beOur = 7;
	switch (as) {

	}

	char us = as + '!' + '"';
}

short as(char beBeBeAs) {
	asBeAs = other;
}

unsigned char with(char as) {

}

#define so 0
