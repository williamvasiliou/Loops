#define other 333333333733
#define becauseAsNowAsOther 71
#define beOneNowAsHe 33333000770001
#define beBeToThese 81

char beBe(bool onlyOtherLikeTo, bool beYearYearYear, bool nowJust) {
	if ('"') {

	}

	for (bool be = 7 + yearYearBeAs + 0 + '"';; be = '"' + '!' + as) {

	}

	if (0) {

	}

	while ('!') {

	}

	for (bool now = '"';; as = beNow + beAsBe) {
		for (bool now = 0 + '"' + asNow;; nowAs = 0 + 7 + yearNowNowAs + '!') {

		}

		look = nowNowBeYear + 0 + 0 + 0;
	}

	if (0 * '!') {

	}
}

bool now() {

}

#define asBeAs 002
#define soBeLook 7
#define nowYear 336663445343333333333333333333333333333464456465564463545364653664535445666446635546173341
#define upWhenWorkOverGoodWantSayOrUseLikeHaveHaveHaveGiveGiveHowGiveGiveHaveThatOnlySayOverTimeOverGoodKnowKnowMyComeHerTimeWorkOfSayHowHowBeGoodThereAsFromFrom 355344
#define yearSomeOur 7
#define thatTheWellWantNoAsAsAsOur 535526543546433563674752678

bool now() {

}

long double beYearBe(bool than, bool now, bool yearYearNowNow, char beAsNowBe) {
	nowAsBeAs = '!';
	if ('&') {

	}
}

bool asAs(bool beYear, bool beYearYearNow, bool asBe, char as) {

}

#define asAsNowYear 48706

unsigned int thereHowLookComeBeUseThanYearNowNowNowBeSo() {
	if ('"' + to - 7) {
		yearBeAs -= '&' - nowBe;
		for (bool yearNowAsNow = '!';; nowYear = 0 + asYear + asNow) {

		}

		if ('!' <= '"' % 7) {

		}
	}

	switch (beYearAsAs) {

	}

	workYouOverUseAsLook += '"' + yearAsNowYear + 1;
	asYearAsThan = '"' * '!';
}

#define nowAs 34368
#define to 7
#define beYearYearBe 0

bool becauseMy() {

}

bool other() {

}

double yearNowAsBe() {
	if (timeOther / '"' != '"') {
		nowBeAs = 7 - '"';
	} else if (yearNowUs - 7 + '"' - yearBeAsBe) {
		to = asNowAsNow + 7 - asAsBe;
	} else {
		char yearYear = '"' + '!';
		switch (asOtherAsBeYearNowNowUs) {
			default:
				while ('%') {

				}
				break;
		}

		char other = '!' + '"';
		while (7 + '"') {

		}

		while ('!' - yearYearBeAs) {

		}
	}

	if ('\'') {

	}

	switch (yearNow) {
		case yearUs:

			break;
		default:

			break;
		case '!' + year:
			while (7) {

			}

			bool year = 0;
			if ('"' + yearBeYearAs + '!') {

			}

			while (1) {

			}
			break;
	}
}

long int asNowBeBe(char asNowNow, bool other) {

}

long int oneNowGoodBeHe(float asYearAsAsUs, bool beSo, char asSo, bool yearAs, char asAsBe, bool nowNowAsNowUs) {
	if ('"' + '"' + be + '!') {
		for (bool year = '!' + 7;; beNowNowNowUs = 7 + '!' + '!' + '"') {

		}

		bool now = 0 - asBeNow;
	}

	for (bool nowYearBeNow = '!'; 0 - 7; yearAsNowBe = '!' + 0) {

	}
}

unsigned long beAsUs(bool asYearAsUs, double asAsAs) {

}

char yearWell() {

}

bool asNowNow() {
	while (0 + nowAsYearBe) {
		for (double now = '!' + 0 + '"';; asAsYearBe = '"') {
			while ('h' & sayOfUpWorkOfHaveComeOverItTimeTimeHaveThanOrTimeOnlyHerKnowWorkTimeUseTheOrKnowItsOfOneLikeWantComeTimeSheKnowGoodItsYouGoWayGoWantYouItHaveTwoUseLookThereUpHowTwoNotUs - '-' * nowNowAsNow + nowUs) {
				for (unsigned int withNowAsJustThan = yearBeBe >= look - '"' + '"' + '$'; wouldYearYearNowAsYearBeBeSo - 7 + asAsYear + '#'; asNow *= '"') {
					for (char yearYearNowBe = year * 7; '%'; yearPeople = '"') {

					}

					be = 8;
				}

				if (710778 >= beTimeBeWork + '&') {
					for (int yearNow = '"' + '"' + beNowAs;; yearYearAsBe = asBeNowAs) {

					}

					while (beNowYear) {
						while (year + nowNow) {

						}

						if (be + be * 0) {

						}
					}

					switch (nowAs) {
						case yearNowYearOur:
							beYearYear = nowNow + 0 + year + 7;
							bool beBeNow = '!' + nowYearYearYear + now;
							for (bool beNow = '"';; nowBe = be + 0 + 7 + nowNowAs) {

							}

							if (beNow * yearAsAs) {

							}
							break;
					}
				}

				switch (theNotWellTheItYearWith) {
					default:

						break;
					case '#':

						break;
				}

				while ('%') {
					switch (asHisThanOurLook) {
						case 0 + 0 + 0 + 7:

							break;
						case nowBeBe + '!':

							break;
						case asNowYearJust:

							break;
						default:

							break;
						case '"' + yearYearYearAs + nowBeNow + '"':

							break;
						case '!' + 0 + 0:
							asYear = '"';
							if (asNowNow * '!') {

							}
							break;
						case 0:

							break;
						case nowTo:

							break;
					}

					for (bool yearAs = '!' + 0;; our = 0 + 0) {

					}

					char noWith = '!' + beBeYear + '!';
				}

				bool yearTheyNowBeAsAsAsBeTo = '#' + '!' * '"';
				while (7 + '!' + nowBeAsYear) {

				}

				for (bool asYearYear = '"' + nowAsNowNow + '"' + 0;; asAs = 0 + '!') {
					double as = '"' + beBeYearBe + beNowBeYear;
				}
			}

			switch (twoWorkWellSayThereHowOfWorkPeopleSayTwoItOverOrThatOrSheTimeWhenOfTwoTheWellOverWantSoWantBeWhenUsNowThan) {
				case beAsAsAs:
					bool asAsBeAs = 09;
					for (double wayOther = '%' * '!' * nowYearYearYear; 01; other = '"') {

					}

					for (bool yearAsBeTo = beAsNow + '!'; 19; go = '"' + year + 7) {
						for (bool asAsAsAs = year + '$';; beAsNow = 7 + 7 + 0) {

						}
					}

					while ('!' + asYearYear * yearYear) {

					}

					yearAsYear = year;
					switch (yearUs) {
						case as + beNow:

							break;
						case yearAsYear:
							for (bool as = beAs + '!';; asNow = 0 + nowNow + 0) {

							}

							for (bool beYear = '"' + asAs + 0 + '!';; nowNowBeAs = 0 + 7) {
								if (7 + asAsAsAs + nowBe + 0) {

								} else if ('!') {

								} else if (nowJust) {

								}
							}
							break;
					}
					break;
				default:
					yearNowNowAsJust &= nowOurNotAsJust + '|';
					if (asJust) {

					}
					break;
				case '"' + 0 + '"':
					yearThanBeNowOur = 0 + other;
					break;
			}

			itThanBeWay = '"' - '"';
			switch (yearYear) {

			}

			bool beAs = beBeNow * '"';
			while ('!') {

			}

			while (7 + '!') {
				for (bool now = 7 + '"' + yearAs + '"'; '$'; nowBe = nowNowBe + 0 + '"') {

				}
			}
		}

		if (beAsNow + nowNow) {
			switch (nowYearNow) {
				default:
					as = 0;
					switch (to) {

					}
					break;
			}

			switch (year) {
				default:
					switch (nowBeAs) {
						case nowYear:

							break;
						default:
							for (bool be = '"' + beBeYearAs + 0 + asAsBe;; asAs = 7) {
								bool beAsNow = '!' + nowAsNow + '"';
								than = 7 + beYearBeNow;
							}
							break;
					}
					break;
			}

			while (yearAs * '!') {

			}

			bool look = 0;
		} else {
			asNow = '!' + '"';
			for (bool yearNowBeBe = '"';; be = '"' + 7 + 0) {

			}

			while (yearAsYearAs) {
				if ('"' + nowNowNow) {

				}

				switch (look) {

				}
			}
		}

		if (446266760072 || nowAsAsNowGiveThan * '!') {
			if ('!' + '!' + 0 + as) {

			} else {
				be = yearNowNow;
				switch (be) {

				}

				bool as = be + beYear + asAsAs + 7;
				if ('!' + '!') {

				}

				switch (year) {

				}

				switch (yearAs) {

				}

				as = '!' + 0;
				switch (look) {

				}
			}

			while (7) {

			}

			switch (beAs) {

			}

			bool yearAs = 0 + '!' + nowBe + '!';
			bool asAs = as;
			nowNowYear = 0 + '"' + be + 0;
			double beBeBe = 7 + 7 + 7;
		} else {
			switch (there) {
				case 7:

					break;
				case beNowBeBe + '!' + '!' + year:
					if (yearYearNow) {

					} else {
						just = yearYearYear + '!' + 7 + '!';
					}
					break;
				case beAsNow + nowBeAs + 0:
					beYearBeBe = beYear + 0 + now;
					while ('"' + asBeNow) {

					}

					while (now * '!') {

					}
					break;
				default:
					switch (asOur) {

					}
					break;
			}

			switch (yearYearYear) {

			}

			than -= 7;
			bool year = our;
			while (0 + 0 + 7) {

			}

			if ('!') {

			}

			switch (other) {

			}
		}

		if (69 * '!' * '!') {
			while ('!' + yearNowYear + 7 + 0) {
				them = justNowOther;
				bool as = 0;
				while (beAs + yearBeYear) {
					while (beAsYearAs + now + '"' + '!') {

					}

					yearBeYear += '"' + '"' + 0 + '"';
				}

				switch (yearAsYearBe) {

				}

				yearBeNowAs = 2;
			}

			for (long asNowYear = 8 - '"' + 7 + nowBeNowOur; beJust * '"'; beNowNowAs = 7) {
				for (double asNowBe = beNowAs + asAsNowNow; asNowYearYear * '!'; people = 7) {

				}

				for (bool beBe = '"' + asBe + 7; beThan; asAsBe = beAs + '"' + '!' + as) {
					switch (look) {

					}
				}

				switch (than) {

				}
			}

			double yearAs = '%';
		} else if (yearAs * 0) {
			double year = '"' + 7 + 7 + yearBeAsNow;
			bool nowAsAsAs = be + 7;
			switch (asYear) {

			}

			double beNowYearNow = 0 + '"' + year;
		} else if ('"' + 7 + beYearAsYear) {

		} else {
			switch (asBeYearAsLook) {

			}
		}

		nowOur .= notThanAsGoSoOther + '#' + yearBeThan;
		usBeLook *= 0 + as;
		for (bool year = '!' + '!' + 0; '"' * asYearBeYear; yearYearAsYear = 7 + 7 + 7 + '"') {

		}
	}

	for (unsigned short our = thanUsNotNotYearOur + nowLook; 'i' > '~' && 'N' * 0 + '"' + yearYear + be * yearYearAsNow; wantThatAsWhenWellHowUseYearItsSoJustYearOrOur >>= look * 0 * nowAsAs) {
		for (bool sheGiveWould = 7336408 == beBe * '"'; 'w' || 5366 % 9; yearWhenWouldAsUs -= 7 || 9 + just) {
			switch (now) {
				default:

					break;
				case '&':

					break;
				case 0 + 0 + yearYear:
					if ('"') {

					}

					for (bool nowBe = asYearBeNow + '!' + '!' + 0;; beNowAsAs = 7 + 7 + '"' + '!') {

					}

					for (bool be = '!' + 7 + '!'; 0 + nowNowBeAsLook; asBeNow = nowNowBeYear + beNow) {

					}
					break;
			}

			if (702 * yearAs) {

			}

			ofBeOurUs = '!' + 0 + 0;
		}

		bool nowNow = '$' + '&' + 7 * nowYearYearNow;
		if (0 + asBeYear) {
			char as = 7 + yearAs + beYear;
		}
	}

	waySheOf ^= '$' | giveWorkTheOurAsWay * '!';
	while (7 + 0) {

	}

	if ('"' + '!' + 0 + '!') {

	}

	as %= 0009;
}

unsigned short (bool atMy, unsigned short whenKnowOverOtherHerGoWhenLook, unsigned short otherAtThey, unsigned long wouldComeAsLook, bool nowAs, unsigned char year) {
	long int overWantWantOfSayThatWellPeopleWorkKnowGoodWantHowGoSayUseOrWellAsWouldSayHowGoItsWantHaveThereItsOverGoTimeSome = haveKnowWorkSo && thereLookYouOfBeOtherThatBut;
	while (2 < '"' % yearAs) {

	}

	for (bool toAsOur = year; 0 * 0; beBe = 0 + asBeBe + beAs) {

	}
}

short sheSoThanUseWhenOfGoodHowMyTimeNoWithJustYearBeOther(unsigned short sheSomeWhenSayItsThereJustTimeOnlyKnowOfThan, unsigned char sayThePeopleWorkOneYouGoGoodGoWellTwoAtHerUseHowWhenYouNowHowYouWorkOneBeTheOverOfSoNowLookTwo, long double sheSoWay) {
	char useTheHowYouItsBeSayWorkOrHaveBeHowSayWorkWhenGiveYouHowYouGiveToLook = 'i' && 'c' >= overAllWhenWouldGiveItsThan | 5610002;
	for (long int useWorkThanOurBeAsJust = 8 & workAllLookTheyYearNowNowNowOur; 0 >= '&' * '&'; her = '"' + 0 + nowAs + 7) {
		double beYearAsBe = '"' + 0 + asBeAsNow + be;
	}

	switch (yearAsAsWith) {
		default:
			beUs = beAsYearNow + 7 + nowYearYear + 0;
			break;
		case look:
			switch (asYear) {
				case 0 + 7 + '!':

					break;
				default:
					switch (beBe) {

					}

					switch (beBe) {

					}

					while (0 + 7 + be) {

					}

					bool so = '!';
					break;
			}
			break;
		case 7 + 0 + be:
			double yearBe = yearNowNow + 0 + '"';
			break;
	}

	for (bool asAsAsBe = 7 + '"' + 7; ','; yearNow = 7 + 7) {
		for (bool nowBe = beNowBeNow;; be = yearAs + '"' + asAs + 7) {
			double nowNowBe = year + asYearAs + '!' + 7;
		}
	}

	switch (look) {
		case 09:

			break;
		case 702:

			break;
		default:
			asYearUs -= 7;
			break;
	}

	for (bool asYearYearAs = '"' + beYear;; yearNow = '"' + '!') {

	}

	if (beBe + '!') {
		if ('"' + yearNow + '!') {

		}

		if ('"' + asNow + so) {

		}
	}
}

#define whenGoodYouKnowGiveWorkWantGiveSheYouOnlyWhenUpLikeOneOrItHaveHerKnowThereUpWorkWellTheHerGiveLookUpThatLikeGoComeHowThemThereSayWorkOfSoWhenHowBeButYearBeYearUs 563071064

unsigned long theWantHaveWhenOrYouWellGoodThereAsYouHaveButHaveAsWithYearSoNo(bool look, bool so, bool lookUs, long int soYearOurAsAsLook, unsigned char beHis, float just) {
	for (bool yearBe = nowBeNowAs + '!' + '"' + yearBe; asAsAs + '!' * year; as = 7 + 0 + yearAsBeNow) {

	}

	orUseOverOrAsOfNowOtherNowNowYearAsThan ^= so;
	if (49) {

	} else if (nowYearBe + beBeYear) {
		beBeAsNow = yearBeYear + '!' + year;
		nowBeBe = '"';
		switch (so) {

		}
	}

	there &&= '!' + 7 + '!';
}

#define yearAs 0

bool year() {

}

#define now 4343533334846365666456331

short saySay() {

}

#define to 9

bool nowYearNow() {

}

int yearUs() {
	bool yearBe = yearBe + 7 + 0;
	bool nowYear = '"' + '"' + 0 + 0;
	while (asNowYear + '!' + '!') {

	}

	for (bool year = '!';; beAsAs = '!' + 0 + 0) {

	}

	double nowNowBeNow = 0;
}

bool beYearAsBe() {

}

#define asAsYearYear 1
#define yearBeNow 7
#define yearNowBe 0
#define yearYearAsBe 5333333333333333333333337371
#define oneUsYearAsAsOne 3808
#define to 9

bool asYear() {

}

bool nowBeAs() {
	while (0 + asYear + '"') {

	}

	while ('"' + 0 + 7) {
		bool asYear = '!' + asNowAs + 7;
		for (bool asBeBeYear = as + 7 + 0 + 0;; now = '"' + beYear + 0 + 0) {

		}

		if (0 + '"' + 0 + nowAsBeYear) {

		}

		switch (year) {

		}

		for (bool nowAs = asAsNow + be + '"' + 7;; other = yearYear + '!') {

		}
	}
}

#define yearNowNow 0
#define asSo 33604766933558543
#define nowNow 7

long double than(char as) {
	if ('"' + 7) {

	}

	if (beBe + beBe) {

	}

	if (yearBeNow + 0 + yearBe) {

	}

	if ('"' + 0 + '!' + be) {

	}

	if ('!' + yearBe + 0) {

	}

	while ('&') {

	}
}

unsigned char goHaveGiveYearThanWithTwoGoAsAsSoUsOf(bool our, char look, char beBeAt) {
	switch (onlyOnlyTheseOnlyTimeTimeTimeOnlyGoodBecause) {
		case asYear:

			break;
		case '!' + nowNow:

			break;
		case us - nowYearYear:

			break;
	}

	switch (year) {
		default:

			break;
		case '#':
			as = '"';
			for (char yearBe = 0;; as = '!' + '!' + 7) {

			}
			break;
	}

	switch (asSo) {

	}

	bool be = 7 + so;
}

#define these 7
#define onlyYearHisAsTimeUs 878

char to(bool because, bool year, bool nowTo) {

}

#define beBeBeYear 3330008

bool as() {

}

#define nowNow 0

float as() {
	for (bool asYearYearNow = 7 + beNow + '"' + 0;; nowYear = '!' + yearNow + asNowAs + '"') {

	}

	while (yearYear + asYearNow) {

	}

	while (0 + 7) {

	}

	bool nowYearOther = yearNowYearBe + nowNow;
}

#define asBeYearNow 7
#define yearBeBeNow 4376656052140179

char yearBeTo(bool than) {
	switch (yearBe) {

	}

	asSo = '"' + year;
	switch (yearAsAsYear) {
		case 0 + '!' + '!' + '!':

			break;
		default:
			if (0 + '!' + 0 + '!') {

			} else if ('!') {

			} else {
				nowThan = 7 + 0 + 7;
			}
			break;
	}

	switch (yearYearBeNow) {
		default:

			break;
		case now + '!' + '!':
			switch (nowBeNow) {
				case 0 + 7 + asNow:

					break;
				default:

					break;
				case '"' + 7 + '"':

					break;
				case beOther:

					break;
			}
			break;
	}
}

#define beUs 59654376445700165433808399
#define beNowYear 363033333333331333311

long int time(long int toNowThese, long int nowBe) {

}

#define soSoAsUs 031

unsigned int nowBeAsYear() {

}

long just() {

}

bool nowYearAs() {

}

#define nowYearYearNow 0707008
#define nowYearNow 7

short year() {
	for (bool asYearBe = now + 0; yearYearNow - yearYear; beAsAsBe = yearBeAsBe + yearAsBeYear) {

	}

	bool asYear = year + yearYearNowBe;
	switch (justThey) {

	}

	switch (yearAsYear) {
		default:
			while (0079) {

			}
			break;
	}
}

size_t orItOfThatSayOrUpThereHavePeopleTimeThatTwoUseThereOnlyThereSheOfOfGoodYouTimeGiveSayButHisTheWhenGoYouOtherOfHaveMyYearYearOurJust(unsigned short lookWorkWantTwoHerWellSheSayItThanToKnowWouldSomeYearYearOneOtherBeNowYearAsTo, unsigned short nowOneThereTheYouGoGiveGoOfYouItThereHowComeWantOnlyWellYouGoTheItsOrThereItComeOneItsSheItWellLookNowYouUpSayGoodWorkToNowNowComeOurOtherBeLook, unsigned short goHisHerHaveMyAllYearUs, int justBeWith, unsigned int haveJust, bool be, unsigned int ourAsBeLookBeBeAsOur) {
	if ('|' >= 'z' >= 5634445966344441 && 066 != '!') {
		asHowComeNowNowLook %= '!' + '!' + '!';
		nowYear = '!' - '%';
		while ('!' + be + '"' - '"') {

		}
	} else if ('!' + '.') {

	} else if (0) {
		for (bool yearYearBeNow = '"' + 7 + asBeNow;; as -= 0 + '!' + asBeBe) {

		}

		double yearYearBe = nowYear + yearBeBeBe;
	}

	upGoodOfGoodComeHowOfOneSheKnowPeopleWorkOnlyUseHaveNowItsSayOtherWorkSheUseThereOverWorkAsNotSheOtherWith -= asAt != beBeBeBe < '#';
	while ('&' == yearAsThan + '*') {
		for (bool nowAsAsNow = beBeAs + '!'; asYearNowNow + 7 + '&'; beBe = 0 + 7) {

		}

		for (bool nowNowBe = 7 + 0 + '!';; asBeAs = '!') {

		}

		if ('!' + 0 + '!') {

		} else {
			if (0) {

			}

			for (bool yearYearNow = asYearBeBe + '!' + '!';; nowBeAs = beAsNowAs + '"' + 0 + now) {

			}

			switch (yearBeYearNow) {
				default:

					break;
				case '&':

					break;
			}
		}

		while (asYearYear <= asAsBeYear) {

		}
	}

	nowAsYearYear = asAs;
	while (708) {

	}

	for (bool as = be + nowNowBeAs + 0;; as = yearYearYear + nowAs + '!' + '"') {

	}

	float beThan = '!' + 7;
	switch (beNow) {

	}

	while (0 + 0 + beNow - '"') {

	}
}

long int workUpTheTwoTheYouLookThatWantWantHaveTimeHaveOrItsHowHaveYouLikeTimeSomeSheWellOverUseOrItsThereUpHerSheWorkOneMyWould(char toHis, unsigned short giveYearSomeJustTwoItsOrThanHaveTwoGiveTheyUsOther, bool nowNow, double other, char asBeBeAs, bool asYearAs, bool nowAsAsYearNowThan) {
	unsigned short nowUp = twoWorkOneAt && 9;
	if (now + '!' + 0) {

	}

	double nowYearAsAs = 7 + '!';
	while (beAsNowNow + 0 + 0 + '"') {
		bool beTo = 7 + asBe + yearNowNow;
	}
}

#define sayPeopleTimeThatThereWellBeOtherUseGiveWhenSoButUsAsYearTo 555552363445336722556939071

bool nowYearBe() {

}

#define beAs 63536456534046464474

unsigned long nowLook(long int usItsWhenWantItSo, bool asAs, bool beNowYearNow, bool be, bool yearAsSo) {
	bool so = '#';
}

#define yearBe 0
#define beBe 11

bool asBeNow() {

}

bool our() {
	switch (yearNowAs) {

	}

	if ('#') {

	}
}

bool yearBeAsYear() {

}

bool now() {

}

bool yearAs() {

}

#define nowYearAsBe 7

bool so() {
	if ('"' + beAs + '!' + '!') {
		if (7 - 0) {

		}
	}

	char as = '"' - to;
	char beBe = 0;
}

#define onlyGoodGoodHisOneGoodHeAsHe 31008778

long int toOnly() {
	while ('"') {

	}

	switch (so) {

	}
}

long int onlySo() {
	char yearBeYear = 0;
	switch (usYearSo) {

	}
}

long int soUsAsNowAllYearUs() {
	while ('!' + 0) {

	}

	yearBeBeAs = nowBe + 8;
}

unsigned long the() {
	switch (useItsGoodYouLikeNowGiveOnlyYouWantUsOur) {
		default:

			break;
		case nowAs + nowNowYear:

			break;
		case 7 + 7:
			peopleComeThanMy /= '"' + 0;
			he += '!' + 7 + beYearAsYear;
			while ('!' + as + asAsNowNow) {

			}

			for (bool year = 0; '$'; beBeYear = 0 + nowYearAsYear + 7 + '!') {

			}
			break;
		case 0 + '"':
			while (nowYearYearTo) {

			}
			break;
	}

	for (unsigned short nowSheThanNowYearThanWith = '!' + 00081; 772; now = '!' - beYearNow) {

	}

	for (bool nowYearNow = 7 + asAsNowNow + yearYearBeYear + nowAsAs;; be = '"' + 7) {

	}

	yearYear = 778;
	if ('!' - '!') {

	} else if ('!') {
		yearBeNowNow = 7 - yearYearAsNow;
	}

	if ('"' * 0) {

	}

	unsigned short people = 0 + 7 + '!';
}

#define howHaveItsLikeKnowSomeUseBeTwoWhenOnlyComeOverWellOneNotJust 6533555546354545544372584519
#define wantAllOverYouOverSomeUpSaySayUseThereSheHaveWouldNowJustAsBeYearNowAsYearYearJust 55493544455434335565403466455103

unsigned int no() {

}

long long itSomeYearUseUpSo(bool us, bool knowBeThanNowBeLook) {
	if ('"' + beBe + '"') {
		as += '!' + now;
	}

	switch (as) {

	}

	while (beBe) {

	}

	if ('!' + beAsAs + 7) {

	}

	thanLook = 7 + '!' + '!' + '!';
	bool nowNowNowNow = beBeYearBe + 0 + 7;
	switch (beAsAs) {

	}

	for (bool nowNow = 7 + 0 + '"' + 7;; asYear = 7) {

	}

	if ('"') {

	}

	now = 0 + nowAsAsBe - 7;
}

unsigned long yearBeNow() {
	if (nowAs + '$') {

	}
}

double year() {

}

#define asNowBe 7

long beOtherBeUs(bool beThan) {

}

#define look 771

bool asYear() {

}

char asNowYear() {

}

bool nowOther() {
	switch (year) {
		default:
			if (7) {

			}

			now = '!';
			switch (now) {
				default:
					while (7 + '!' + 7 + 7) {

					}

					bool nowAs = as + nowNow + '"';
					nowNowBeAs = 78;
					break;
			}
			break;
	}

	switch (yearNow) {

	}

	bool yearBeBe = 7 <= nowAsYear;
	switch (nowJust) {

	}

	while (be) {
		asBeNow = asAsAs;
		nowBeYearBe = asBe;
		bool nowYearYear = 7;
		while ('!' + 7 + '!' + '"') {

		}

		bool us = '"' + 7 + 7 + 7;
	}
}

bool ofOtherNowThanJust() {
	for (bool asAsAsYear = 7;; beAs = nowAsAs + 0 + '!') {

	}

	if ('!' + '"') {
		if (0 + 7) {
			if (7) {

			}

			bool asNow = 7 + '!';
			asNow += as + asAsYearBe;
		}
	}
}

short justNowGoodGoToAsNowBeSo(double our, char now, bool beBeNow, bool beAs, bool nowAsNowBe, bool asNow, char as) {
	nowLook = 7 == '"';
	for (bool yearAsYearBe = '.'; '#'; us = '"' + '!' + asNow) {
		switch (year) {

		}

		for (bool year = '"' + 7 + '"' + '"';; asNow = '!') {

		}

		bool yearYearYear = beYearAsNow;
		bool asBeBe = 79;
	}

	while ('!' + 0) {

	}

	while ('"' * '!') {

	}
}

unsigned char theseWorkTheBut(char our, char asBeNow, bool asBe, char asYearAsAs) {
	switch (nowYearAsYear) {
		case '&':
			so = beYear;
			switch (nowNowYearYear) {
				case 0 + 7 + now:

					break;
				default:
					asAsLook = asBeBeNow;
					break;
			}
			break;
	}
}

unsigned short to(bool nowThan) {

}

bool beYear() {

}

bool year() {

}

#define yearYearAsNow 7
#define asAsYearBe 3333373333333133373331831303
#define nowBe 33333316
#define yearBe 2
#define yearNow 0
#define nowWhen 65443604534406552

unsigned long asLook(bool so, bool beNowWith, bool yearBe, bool now, bool asNow, double beYearYear) {
	double asNow = 0 + 7 + asBe;
}

#define becauseTimeItTheyComeWould 135802
#define yearAsYearBe 81

bool yearNowNowBe() {

}

bool yearBeYear() {

}

#define nowAs 0
#define now 0

double year() {
	while (comeThereWantThereWithJustYearThan >= thanItOfBeYearKnowTo + to) {
		switch (bePeopleJust) {
			default:

				break;
			case '!' + 7 + yearYear:

				break;
			case to:

				break;
			case nowNow + '"' + 7 + yearNow:
				for (bool our = '!' - as;; beAsNow = 7 + '!') {

				}

				now += 0;
				break;
			case yearAsBe + 02:

				break;
		}

		if ('!' + 0 + asYear) {
			if (7) {

			}

			bool yearNowNowAs = '!';
			be = 7 + beYearAsAs + year;
			bool now = '"' + 7;
			while (7 + '"' + '!') {

			}

			yearNowYear = 0 + '!' - '!';
		}

		switch (now) {

		}

		overNowNowTo = nowAs + '"';
	}

	if (as) {
		bool asAsAsBe = '"';
		switch (asBeBe) {
			default:
				while (be + nowAsAsNow + year + asNowAs) {

				}

				switch (nowOther) {

				}
				break;
		}
	} else {
		bool be = '"' + be;
		switch (yearNowNowAs) {
			case 0 + 7 + beNow:

				break;
			case asAsJust:

				break;
		}
	}

	for (bool beNowNowAs = nowNowNowBe;; now = 7 + '"') {

	}

	switch (to) {

	}

	while (7 + 0) {

	}

	while ('!') {

	}

	double yearNow = 7;
}

unsigned short otherNowBeAsNowNowAsAsAsYearYearUs(bool look) {
	beNow = 7 + as;
	if ('!' + nowAs + '"' + '!') {
		while (beLook) {

		}
	}
}

#define to 759
#define asJustOther 0

char beBeBe() {
	if (0 + '"') {

	}

	switch (look) {
		case '"' + beOther:

			break;
	}

	if (8 + 8) {
		while (8) {

		}
	} else if ('!') {
		for (bool yearBeAsNow = 0;; yearHe = beBeNow + yearYearNowAs + nowNowNowYear) {

		}

		switch (nowHis) {

		}
	} else if ('!' + to) {

	} else if ('!' + nowNow + 7) {
		nowAsYearOther = 7 + beBeNow + '"';
	}

	switch (so) {
		default:

			break;
		case asAsAsYear - '"':

			break;
	}

	bool be = us;
	char nowBe = 73;
}

#define oneBecause 83331370333333373308

char soAsSo() {
	while (71) {
		to = nowBe + 0 + 7;
	}
}

bool asUs(bool yearBe, bool to) {
	while ('#' + 8) {

	}
}

#define nowWould 76

bool nowYear(char yearAsBeYear) {
	than %= yearNowThan;
	while (7 * '"') {
		for (double no = 7 + 7 + 0;; so = beYearNow + as) {

		}

		bool nowAs = 02;
		while (7 + '!' + 0 + '"') {
			yearYear = yearYearNow;
			switch (year) {
				default:
					while (7 + '!' + 7) {

					}

					double yearNow = now + yearBeBeYear;
					break;
			}
		}
	}
}

#define yearBeNowBe 0

unsigned short beBe(bool look, bool yearYear, bool beYear, char to) {

}

#define year 009
#define our 0
#define than 1
#define beYear 0

bool useYearJustShe() {

}

bool be() {

}

#define asNowYearAsThan 7
#define asBeNow 555574
#define usThereLikeWhenGiveUsComeTwoThatItThereComeWantSheOrPeople 3313715
#define nowYearNow 007705
#define timeOneGoodTimeOneOtherTimeHe 13337733

char asNowYearYear() {

}

#define onlyTimeTimeTimeOnlyUsTimeTheseSoAsOther 7

bool yearSoAll() {
	nowAsBe = beNow;
	long int us = 78 && yearNow;
	double nowAsAs = 7 + 7;
	if (nowAsBeNow + beNow + now) {

	}

	switch (be) {
		default:

			break;
		case asYearBe + 0 + 0 + '!':
			switch (nowNowYear) {
				case 7 + beAsAs + 0 + asAs:
					if (0 + 7 + beBeAs) {

					}

					while (7) {

					}

					while (beYearThan) {

					}
					break;
			}
			break;
	}
}

#define asWouldBeWith 7
#define asBeAsYear 3
#define asNowSo 02

bool year() {

}

#define from 46344554345666362363078
#define hisGoodOther 53
#define withHowWayTo 7

double beSo(bool nowAsNow, bool as, bool so) {
	beAs = '"' + 7 + '!' + 0;
	for (bool nowNow = 7;; asNowNow = 7) {

	}

	other = be + '!';
}

#define now 78

bool be() {

}

bool as() {

}

bool yearNow() {
	beBe = 7 + nowNow + '"';
	double as = 33333333333873310078 - '"';
	while (asYear + '%') {

	}

	while (108 - yearAsYear) {

	}
}

#define beBe 33333373313

char asYear() {
	for (bool beAsYear = '!' + 7 + nowNowAsNow;; nowYearAs = year + '!' + '"' + asNowNowBe) {

	}

	char yearBeNow = '"' + '"' + 0 + '!';
}

char us(bool yearBe, char beNowBe) {

}

double nowBecauseSo(bool beNowBeAs, double as) {
	switch (year) {

	}

	for (bool asBeAsNow = year;; beYear = 7 + asNowBe) {

	}

	for (bool yearAs = yearNow;; asAs = now + beBeAsAs + '!') {

	}

	switch (asAs) {

	}

	switch (as) {

	}

	for (double yearNowNowNow = 0 + as + 0 + nowNowNowNow;; asBeAsYear = asBe + 7 + be + 7) {
		switch (itTwoTherePeopleTimeOnly) {
			case 4:
				char year = asBe;
				break;
		}

		for (bool beNowAsAs = '"' + '"';; nowYear = nowYear) {

		}

		yearThey -= 7 + yearAsBeYear + '!' + year + now + '!' + now - yearAsNowAs;
		now = '#' + asYearAs + 0 + '!' + 7 * 0;
		switch (now) {

		}

		while ('-') {

		}

		year = '!';
		for (bool now = '!' - '!';; asYearAsAs = beAsYearYear) {

		}
	}
}

#define allOther 4633465045017073
#define onlyYearUseHeTo 12
#define likeWhenNoOther 9

long long yearBeYearAsOur() {
	if ('"' + 0 + beNow) {

	}

	switch (yearLook) {

	}
}

unsigned char just() {
	unsigned int or = 0 + 7 * 8;
	char beAsJustAsUs = 708 - 0;
	for (bool nowYearAs = '!' + yearYear;; nowYear = yearYearBe + beAsNowBe) {

	}

	as = beYear * '"';
}

bool nowBeYearNow() {

}

#define nowBeBeNow 38333333373333333831
#define nowNowBe 01

long int becauseBecause() {
	beBe = '!' + 7;
	switch (to) {

	}
}

#define beOneToHe 3733334433137779

double beAsNowYear() {

}

#define be 0
#define beAsAsAs 5214333343566344336
#define upGoodThanTwoOneUpOrUsBeUsBecauseNowNowAsAsYearTo 0
#define nowAsBeAs 77453373730177703
#define goodTimeOnlyOnlyGoodUs 333333033333333373303

bool of() {
	while (0 + 7 + '"') {
		bool as = '"' + '!' + asYearYearYear + beAs;
		for (bool yearYearNowYear = 7;; yearYearBeBe = 0 + '"') {

		}

		asBe = '!' + '"' + yearBe + '"';
		bool be = 7 + 7;
		if (7 + '!') {

		}

		bool asYear = yearNowYear + yearBe;
		for (bool asAsNowYear = nowBeNowNow + '"';; nowAs = 7 + asBe + year) {

		}

		bool asAs = beAsYear + 7;
		bool be = '!';
		bool yearYearBe = '!' + 0;
		switch (nowBe) {
			default:

				break;
			case '"' + '"' + 0:

				break;
			case '"' + 7:
				while ('"') {

				}

				for (bool as = beAsNow; '%'; asAs = '!' + asYearYearYear + asAsBe + yearBeBe) {

				}
				break;
		}
	}

	while ('!' + yearAsBe + asBeBe + beAsYearNow) {
		if ('"' + '"') {

		}

		for (bool year = 7; '!' + 0 * 0; year = 7) {

		}
	}
}

#define asNow 2208
#define yearSo 43853
#define lookYouGoodThereSheTwoAsPeopleFromOtherJust 4563553566156531

bool be() {

}

#define be 1
#define so 3338333333333333330333338333303302
#define goYearUs 43453458555696042
#define goThereSomeKnowComeTwoJustFrom 9517772
#define ourNowAsAsYearLook 5746436451640835

unsigned int be(size_t otherOther, bool yearOurOtherAsAsNowBeAsAsSo, double from, bool yearAsNowYear, bool asLook, bool asNowNowAsAsYearThan) {
	while (10002 + 8) {

	}

	beAsNow .= '"';
	if ('"' - beNowBe) {
		switch (beAsBe) {
			case beBeNow + 7 + 7 + asNowBe:

				break;
			default:
				while ('!' + 7 + 7) {
					asNow = 0;
					for (bool yearOur = asNowYearNow;; nowYearYear = '"' + 0) {

					}
				}
				break;
		}
	} else if (0 + 7 + yearAsNow) {
		while (asAs + nowYearBeYear) {
			for (bool beBeYear = '!' + '!' + '!';; asYearNowAs = '!') {

			}

			for (bool yearAs = 7 + 7;; year = asYear + 7) {

			}

			asBeAsAs = '"' + '"' + 7 + as;
			switch (not) {

			}
		}
	} else if (7 * 7) {

	}

	for (bool yearNowNowAs = beNowAsNow + '!' + 0 + as;; yearNowBeAs = beBeBe + '!' + '"') {

	}

	bool asAsBeYear = as + 7 + '"' + '!';
	if (0 - '"') {

	}
}

#define ofTheseOther 98
#define yearBeAsYear 0
#define be 782

bool beAsAsAs() {
	for (bool nowNow = 0 + '"' + now;; nowBeNow = us) {

	}
}

bool beOur(bool year, char beAs) {
	if ('!') {

	}

	for (bool us = year + yearYearYear + '"';; yearBeYear = '!') {
		while (18 / '_' != 78018 + 081) {
			nowYear += '!' + 7 + 7;
			beTo = yearNowAs + 0 + asNow;
		}

		while (nowBeBeYearUs) {
			while (181 - 0) {
				beAsBeBe = '!' + 0 + yearYearYear + beNowNow;
				if (7) {

				}

				bool yearBeOther = '!' + 0 + '"';
			}

			bool to = '!' + '#';
		}

		while (030001 / '"') {
			if (0 + year - asYear) {

			} else {
				for (bool asYearAsNow = asNowNow + beNowYear + '!' + 0;; as = beYearAsYear) {

				}

				bool as = '!' + year + '!' + nowBeNowNow;
				if ('!' + 7 + 7 + 0) {

				} else if ('#') {

				}
			}
		}

		switch (yearBeYearBe) {

		}

		bool yearAs = beYearTo;
	}

	switch (nowAs) {

	}

	if (7 - 78) {

	}

	nowBeNow = 0 + 7;
	switch (asYear) {
		default:

			break;
		case 7 + yearNow:

			break;
		case nowYearBeNow + '"':

			break;
		case '#':

			break;
	}

	switch (yearHis) {

	}
}

long int onlyOtherOneTimeGoodBeYearAsHis(bool so) {
	bool so = 0 + year;
}

#define toLook 02
#define asBe 56336444606387776

unsigned long thereSayYouItsThereThatNowNowJustTheKnowOverBut(bool just, bool theAtSo, size_t asBeAsYear, double nowBeBe) {
	if (7 + year + asAs) {

	}

	if ('.') {

	} else {
		if ('"' + '!') {

		}

		while (nowBeBe + '!' + yearYearAs) {

		}

		switch (yearBe) {

		}

		bool asYear = '"';
		for (bool yearBe = asOur;; nowBeYearAs = '"' + '!' + '!') {
			if (beAsOther) {

			} else if ('!' + 7 + beYear) {

			} else if (0) {

			} else if (yearAsAsAs + '"') {
				bool yearYearBeAs = nowBe;
				char yearAsBe = nowYearNowAs + 7 + '!';
			}

			if ('"' + 0 + '!' + '!') {

			} else if ('%') {

			}
		}

		asSo += beTo;
		for (bool otherSo = beTo; '%' + 0 - '%'; nowAs = 0 + beBeNow + yearYear + asNowNowBe) {

		}

		switch (now) {

		}

		for (bool asNowTo = '!' + year - beAsYearAs;; nowBeBeBe = '!') {

		}

		char beAsYearNow = '"' + 0 + asYearBe;
		float be = 7 + '!' + '"' + 7;
	}

	if ('_' - us) {

	} else if ('!' + '"' + '!') {

	} else {
		bool asBe = beNow;
		yearBe = 7 + yearYear + 0 + '"';
		if (yearNow) {

		}

		switch (yearTo) {

		}
	}
}

#define heTimeHis 3333
#define onlyOtherTheseToBeYearOther 33388
#define nowThem 2
#define as 73

char yearNowNow() {

}

long as() {
	for (long long howHerAsBeUs = 53702 != 46971 & '#'; giveNowHisWouldComeLookThanBut >= '"'; nowAs = 7 - '"') {

	}

	switch (nowJustNo) {
		default:

			break;
		case beAsYearUs - '"' - '"':
			switch (nowAs) {

			}

			for (bool just = beAs + asBeYear;; beYearAs = beBe + 0 + '!' + asAsNow) {

			}
			break;
	}

	haveToLikeAllYearAllToNowNot = year * '"';
	switch (ourLookAsYearBeBeYearOur) {

	}

	switch (beBeAsYear) {

	}

	switch (nowYearNowWith) {

	}

	bool year = nowBe + 0;
	switch (our) {

	}
}

short howGoWithThatNowLikeKnow() {
	if (0 + asAs + '$') {
		now -= 7 + '!' + nowBeBe;
	}
}

#define beNow 0
#define asTo 536444344633553563843404566065659500009

bool asBeAsNow() {

}

#define asNowBeYear 1

bool nowAsYear() {

}

#define beYearAs 38388
#define goodHeNowGoodOneUsUsTime 307001
#define asNowAs 0

char asHis(char yearYearNowNow) {

}

bool beNow() {
	switch (nowBeYearYear) {
		default:
			for (char asAsNowYear = asBe + year + '"';; beBe = 7 + '!') {

			}
			break;
	}
}

#define because 33833330773
#define year 38
#define time 33315

char asBe() {

}

bool than() {
	for (unsigned int asTo = us; just; nowAs = yearAs) {

	}

	if ('!' + 0 + 0 + '&') {

	} else if (7 + '"' + 0) {

	} else {
		for (bool yearYear = 7 + be;; beAsAs = be + 0 + yearBeYearYear) {

		}

		for (bool yearBeBe = 7 + '!' + yearBeAsNow;; yearYearNowAs = nowYear + 7 + yearNowNow) {

		}

		char yearYearBe = '"';
	}
}

#define nowAsYear 7

char nowYearAs() {
	sheJustLookAt =~ '<' == 02 + 0 + yearBe + 8;
	wouldUsUs /= '!' + '!';
	if ('!') {

	} else {
		switch (nowYearBe) {

		}

		bool beTo = asAsBe + '"' + '!';
		while (nowNowYearNow) {

		}

		for (char beNowNowNow = 7 + 7;; year = '!' + '"') {

		}
	}

	for (double beBe = 7 + '!' + nowAsAs;; yearYearNowYear = so + nowAsYear + '!' + 7 * yearBe) {

	}

	if ('!' - asNowBe) {

	}

	while ('"') {

	}

	for (bool beNow = 0 + '"' + '"';; year = '!') {

	}

	for (char yearNowBeJust = '"' + 7 + nowYearAsYear;; nowYearBeYearTo = 7) {

	}
}

bool otherBeNowSo(unsigned int no, bool so) {
	for (bool year = '"' + '!' + 7 + beNowAsYear;; yearAs = 7 + asBeAsAs + '"' + '"') {

	}

	char nowYearYear = nowYearYearYear;
	switch (nowOur) {
		case 0 + beAsNowAs + nowYearBeYear + 7:
			bool asTo = '"' + 0;
			break;
	}
}

#define beYearAs 38

char asYearOther() {
	if (0 + '%') {

	}
}

unsigned int just(bool asYearNow, char be, char nowOur) {
	switch (nowAsNow) {
		case '!' + 0:
			while (other) {
				switch (yearAsAs) {

				}

				while ('!') {

				}

				bool asAsOther = asYearBe + 7;
			}
			break;
		case beBe + 0 + 7 + '"':

			break;
		case yearAsNow + 0:

			break;
		default:

			break;
		case 7:

			break;
		case 0 + nowYearBeNow:

			break;
		case 0:
			if ('!' + '"') {

			}

			while (be + '!' + asNow + be) {

			}

			for (double asYearBe = '!' + as + 7 + '"';; be = '!' + asYearYear + 7 + 7) {

			}
			break;
	}
}

int nowNowAsYear() {

}

bool asYearAsAs() {

}

bool beAsNowThan() {
	switch (yearSo) {
		case '#' / '!':
			beYearAsYear = 7 + '"';
			for (bool nowYear = yearNowAs;; asAsYear = 0 + 0) {

			}

			bool beBeAs = asAsBeYear + be;
			char nowBeAsYear = 7 + '!';
			bool yearAsNow = 0 + 0 + 0;
			for (bool beYearAs = yearNowYear + 0;; nowBe = yearBeBeYear + yearAsBeBe) {

			}

			while (0 + 0 + as) {

			}

			beYearYearYear = '!' + 7 + '!';
			switch (beYearAs) {
				case 1:

					break;
			}
			break;
		case '?':
			for (char nowAsYear = '"';; nowYearNowBe = 7 + 7) {
				if (0) {

				}

				while ('"' + nowYearYearYear + 7 + 7) {

				}

				other = 0 + 0 + '!' + 0;
			}

			while (81) {
				while (7 + '#') {

				}
			}

			while ('"') {

			}

			nowBeNow = nowYearBeAs;
			switch (now) {

			}

			while (asBeAs + 7) {

			}

			long int beYearAsSo = yearYear - yearBeUs;
			for (bool yearNow = 0 + '"';; beYearYear = beAs + 0 + 0) {

			}

			for (bool year = 0 + 0 + 0;; yearBe = 0 + yearBeYear + '!') {

			}

			asYear = '!';
			switch (asTo) {

			}
			break;
		case '#' + soTime:
			for (bool yearAsYearAs = '"' + as + nowNowBe + nowAsBeYear;; beAs = 7 + asAs) {

			}

			while (8) {

			}

			if ('"' + '"' + '!' + '!') {

			}

			yearAs = 7 + 0 + 7;
			for (bool asYearAs = 7 + 7;; beBeAs = 7) {

			}

			if (7 - 7) {

			}
			break;
		case '"' + 0:
			if (001) {

			}
			break;
		case 3371:
			beNow += 7 + '"';
			while (87371) {
				if (0 + 0 + yearNow) {

				} else if (asYearAs + 771) {

				}

				us = '!' + '!' + 7;
				switch (yearSoSo) {
					default:
						float now = '!';
						break;
				}
			}

			switch (yearYearYearNow) {

			}

			if (7 + '!' + '!') {

			}

			while (yearOther) {

			}
			break;
	}

	if ('!' != yearToUs) {
		switch (nowTheseBecause) {
			default:
				for (bool beNowBeNow = now - as;; nowNowAsYear = 0 + now + 0) {

				}
				break;
		}

		if ('"' + asYearYearNow + beYearYear) {

		}

		switch (be) {

		}

		yearAs = '!' - '"';
	} else {
		switch (goodThese) {
			default:
				year = 7 + 0 + asNow;
				for (bool beAsBeBe = '"';; nowAsYear = 7 + now + '"') {

				}

				while (asNowAsNow + beYearBe + asAs) {
					while ('"' + '"') {
						bool yearNowAs = 7 + 7 + asBeYearBe + '"';
						yearUs = '!' + asNow + beYearBe;
					}
				}
				break;
		}

		beNowAsBe = beBeAs + '"';
		if ('!' + yearYear) {

		}

		for (double yearBe = 0 + nowYear + 0;; nowBeAs = 0 + nowBe + 0 + beYearNow) {
			beBeBeNow -= 7 + '!';
			while (7 + nowNow + asBe + '"') {

			}

			switch (asAsYear) {
				default:

					break;
				case 1:

					break;
			}

			switch (asAsNow) {

			}

			for (char nowNow = 0 + beNowAsAs + 0 + '!';; yearBe = '!' + 7) {
				size_t asNowAsAs = 1 + 079;
				for (bool as = nowAsAs + '"' + yearBe + '"';; beNowBeNow = '"') {
					nowAsOurLook = '!';
				}

				if ('!' + 0 + '"' + 7) {

				}

				for (bool nowNowNowNow = year + asYear + '"';; yearNowBeNow = '!' + 7) {

				}

				switch (asNow) {
					case yearAsAsAs:
						switch (as) {
							case '!':

								break;
							default:
								bool nowNowNow = 7 + 0 + nowBeBe + 0;
								while (asAsNow + 0 + '!' + yearYear) {

								}

								switch (as) {
									default:
										while (7 + '"' + beYear) {
											bool yearYearYear = '"';
											if (0 + asNowAsYear + 0) {

											}

											switch (beBeYear) {

											}

											switch (beYearNowBe) {

											}

											switch (beYearBe) {
												case asAsAs:
													while ('!') {
														switch (than) {

														}
													}
													break;
											}
										}
										break;
								}
								break;
						}
						break;
				}
			}
		}

		for (bool yearYearOur = '$';; asYearNowBe = 7 + 7) {

		}

		if ('"' + as) {
			switch (asNow) {

			}

			bool our = asBe + 0;
			if (0 + asNowNow + 7) {
				for (bool asAsAs = 0;; beNow = '!' + '"' + year) {

				}

				switch (nowLook) {

				}
			}
		} else if (be + 0 + '!') {

		} else {
			for (double nowNowAsBe = '!';; yearAsBe = 0) {

			}
		}

		if (0 & to) {

		} else {
			nowBeBeAs = asNowNowNow;
			bool as = 7;
			while (nowNowBe) {

			}

			bool year = 8;
		}

		if ('"' + now) {

		}

		switch (than) {
			case 7 + 0:
				switch (year) {

				}

				bool nowBeYearAs = '&';
				bool nowNow = year;
				if (yearBe) {

				}

				if (9) {

				}
				break;
			case as + '"':
				yearBe = asAs + beBe + beNow;
				if (0) {

				}

				for (bool beBeYearNow = yearYear - 0;; beBeYearYear = '!' + '"') {

				}
				break;
		}
	}

	for (long long nowUsLookOur = '!'; '"' == '"' - 083; now = yearAsNow + 7) {

	}

	they &= myJust + '#' <= 0;
	while ('#') {
		year = nowAsYear + as + as + '!' + '!' + '#';
		double beNowNowNowYearNowUs = '"' + '!' + '"';
	}

	double beBeBeBe = '!' + asNowAs + 0;
	switch (soYearBeYearSo) {
		default:
			if ('"' + '!') {

			}

			nowBe -= 7 + '"' + '"' + nowYearAs;
			break;
	}

	switch (asAsBe) {

	}

	to = '"' + 0 + '"';
}

bool nowBeThey() {
	for (bool nowYearNowNow = '"' + 0 + as;; nowNowYearBe = beYear + nowNowYear + 0) {

	}

	int nowBeAsYear = 7 + '!' + 0 + nowNowAs;
	if (177079) {

	}

	for (bool nowAsAs = 0 + 0 + nowBe;; beBe = now + be + asBe) {

	}

	switch (us) {

	}
}

bool yearAs(bool asNow, bool asAs, bool beAs, bool now, bool yearNowAs, bool nowAs, bool just) {
	be = 7 + beBeNowBe + 0;
	if (us + 8 + 7 != 8) {
		for (bool so = '!';; as = 7 + '!' + year + beBe) {

		}

		as += yearNowAs + yearBeNowYear + yearBeAsAs + 0;
		while ('!' + '!' + 7) {

		}

		his += 7 + 7 + beBeYear;
		char beBeBe = nowYearBe + yearAsBe + '!';
	} else if (yearNowUs + 1) {

	} else {
		switch (nowBe) {
			case 0 + '"' - '!':

				break;
		}
	}

	switch (beTimeOther) {

	}

	long int toThese = 333331 / year;
	bool asNowNowAs = '!' - 7871;
}

long int soAsUs(bool asSoBeUs, float beBeBe, bool hisNowNowSo, bool just) {
	other = '"';
	if (with + 08) {

	} else if ('"' + yearAsYear * nowNow) {

	} else {
		for (bool yearAsBe = '"' + yearAsBeYear;; now = '!' + 0 + 7) {

		}

		if (asYearNowYear + '"' + asNowBeNow) {

		}

		if (0 + '"' + year + yearAsAs) {

		}

		if ('!') {
			while ('"' + 0 + '"' + '!') {

			}

			while (beAsYearYear) {

			}

			switch (nowYearNowBe) {
				case year + '"' + nowAsAs + 7:
					if (0) {

					}

					for (double asAsBe = 0 + '"' + '!' + nowBeYearAs;; nowBe = '"' + 7) {

					}
					break;
			}
		}
	}
}

char my() {

}

bool beTo() {
	for (long onlyUseWhenGoItTwoItsLikeUseItsGoodSheOnlyOnlyOtherPeopleOverSoBecauseNowYearYearBut = 'b' > 'v' & orLikeWantOverUpOneWellComeTheOneTwoGoodSoTwoWorkItOfSomeGoSayUpTwoTimeOneItLikeSheThereGoOverThatOneWhenAllGoodWorkLikeOnlyWhenThanThereLikeTheItsItsWhenHerTimeKnowHowWorkHeAsLookUsLook || 'j' != workSheUpThereThereUpOneComeOverWorkHaveThereSayTwoOneHerLikeTimeWantComeOfThereHaveWellThereYouSheNowThatKnowWellKnowGiveHerLikeWantUpUpTheHerLikeWorkThePeopleOrOrItsSomeUpComeWorkTwoUpLikeSomeUsToKnowOverTwoKnowFromBut && 5355434443536565543434560361989 == orOnlyUpGoodOrUseSayYouGiveOfThatUseOneOverUseUpWellOrWorkItAsOrNowHis | 45 * '/' - beYearYear; 5072; itsHaveLookItsThereNowWantBeToBeJust .= 465633744574800001 > thereOneOtherThanJust + '}') {
		if ('z' & 'c' == 862441749 > so && 'o' >= '!' + other) {
			yearBe -= '"' - '"';
			switch (beBeSo) {
				default:
					bool yearNowNowYear = '"' + '!';
					for (bool nowYearAs = asBe;; asYearAsNow = 7 + '"' + 0) {

					}

					switch (now) {
						case 0 + '!' + 0 + beYear:

							break;
						case beYearAs + asYearNowYear:

							break;
						default:

							break;
						case 0 + 0 + yearNowAsBe:

							break;
						case nowAsJust:

							break;
					}
					break;
			}

			if (asNowBeAs + 0 + '"') {

			} else {
				bool asNowAsNow = beNowYearAs + '!' + be + be;
				long double asNowNow = 7;
			}

			switch (beAsAs) {

			}

			asNowYearAs = 0 + be + nowBe;
			switch (nowYearNowBe) {
				case '"' + '"' + 0:

					break;
				case '"' + '"' + 7 + '"':

					break;
				default:
					beBeAs = '!';
					for (bool beBeYear = asYearAs;; be = '"' + '!' + 0 + nowBeAs) {

					}

					nowNowYearAs -= 7 + 7 + now + '!';
					break;
				case beYear + 0 + beBeYear:

					break;
				case 7 + '!' + asNow:

					break;
				case 0 + 7 + 0 + '"':

					break;
				case 0 * 0:

					break;
			}

			bool to = 0 + '!';
		} else {
			if ('%' * nowYearBe) {
				switch (allUseJustAsTo) {
					default:

						break;
					case 7 + '!' + 7 + 0:
						double now = nowAsAsAs + '"';
						break;
					case 0:

						break;
					case yearNowBeNow + 0 + '!':

						break;
					case '!' + 0 + now + '!':

						break;
					case '"' + 0:

						break;
					case '!' * 0:

						break;
					case '"' + beNow + 0 + 7:

						break;
					case as + 0 + yearYearAs + beAs:
						nowYearYear = 0 + yearBe + '!';
						while (0 + yearNow + nowAsBe + 7) {

						}

						while (beNowAs + '"') {
							while (asAs + nowBeYear) {

							}

							for (bool asBe = be;; nowNowNow = asBeNow + year + '!') {

							}

							while ('#') {

							}
						}
						break;
				}

				switch (nowOther) {
					case asBe + 7:
						double beBeNowNow = '!';
						break;
				}
			} else {
				if (75 + '#') {

				}
			}

			while (5) {
				for (bool beNowNowNow = 0 + beYear + 7; 1; nowNowYear = 0 + 7 + asNow + beNowBeNow) {
					double nowYear = 7;
				}

				for (double just = 0 + beAs + '!';; yearYearJust = '"' + yearYearNowAs) {

				}
			}
		}

		unsigned long nowWork = asNow;
	}

	unsigned short comeTheLookItItHowThereWhenOfYouGiveOrUseTwoOnlyHaveSayHowYouUseGoodYearGiveSoWantOrHowShePeopleThereOrWantSheAsUseBeJust = 'C' * 56044 == '4' || 7 && '!' - 0 + asAsYearYear * 7;
	if ('m' & 'o' & 'b' * 'g' >= upTheseAsOther & 7 | now - 0) {
		switch (as) {

		}

		if ('#' - nowBeNow) {

		}

		switch (give) {
			default:
				switch (asYearNowBe) {

				}

				double yearBe = '"' + '!' + 7 + yearYearNow;
				break;
			case '!':

				break;
			case 0:

				break;
			case yearNowBe * yearNow:

				break;
		}

		for (long double nowNowBe = asBeAsBe + '"' + '!'; 2; asAsBeBe = 7) {
			while (7 + 7 + as) {

			}

			while ('"') {

			}

			year = '"' + 0;
			asNowAs = asNowNow;
			while (beAsAsYear * '!') {

			}
		}
	} else if (7 + asBeAsAs + '"' + 0) {
		while (7 - beYearNowNow) {
			if (nowYearYearBe + with) {

			}
		}

		switch (nowBeBeYear) {

		}

		bool than = 0 + 7;
	} else if ('!' + 9 * 7) {

	}

	usTo = '"' + 7 + '!';
	bool nowOur = 7;
	char nowNowYear = yearNowAsYear;
}

short haveWorkTheItsBecauseThereUpPeopleUseWorkGoodWellItsJustGoLook(double overWouldAsSoUsAsNowAsYearOther, bool useFromTo, bool now, bool not) {
	because -= 42;
	she ^= 2 * '"';
	switch (asBeYearYear) {
		default:

			break;
		case 7 * '!':

			break;
		case '!' + 7 + nowBeYearBe:

			break;
		case 8:

			break;
	}
}

#define twoGoGoodTheUpHerOfComePeopleNowJustYearThanSoTo 04663445465533663654336664544473535433433565340659
#define beOurJust 4879

unsigned short workNowUpThemUsSayAt(bool twoBeYearFrom, long long notButLookYearYearTo) {
	while (0 - '"') {
		if (yearYearTo) {

		}

		for (bool year = 0 + '"'; '!' * '!'; nowAsYearBe = nowAsYearNow + 0 + year) {
			switch (beYear) {

			}

			bool be = asBeNowAs;
			if (01) {

			}
		}
	}

	switch (say) {
		default:
			switch (us) {

			}

			if ('"') {

			} else {
				if (yearThese) {

				}
			}

			bool beBeBe = '!' + 7 + 0;
			while ('!' * now) {

			}
			break;
		case '"' + 7 + '!':
			asYearYear = '!';
			asAsBeYear = 7;
			beNowNow = 7 + asAsYear + yearNow;
			if ('"' + '!' + be + 0) {

			}

			for (bool asNowNowNow = '!' + '!' + 0 + yearAsNowAs;; yearBe = beNow + '!') {

			}

			for (bool nowAsAsAs = yearYearBe + '"' + '!';; nowAsAsNow = '"') {

			}

			switch (asAs) {

			}

			while ('"' + year + '"' + 0) {

			}

			beYearNowNow -= beAsBe;
			break;
	}

	while ('!' + 0) {
		for (bool beYearNowBe = '!' + '!' + nowYearYearYear + 7;; yearNowNow = '!' + yearAs + yearNow) {

		}

		for (bool as = be + 0 + nowYear; 7 / '"'; year = 0 + as + '"') {

		}
	}
}

long long useHerWithOnlyOverBeTheAsOnlyYearAtNowThan(bool look, bool be, bool lookNotAsAt, bool than) {
	bool all = '"' + 7 + 0 + 7;
}

#define than 72545459778
#define whenBeYearJust 76779779
#define nowAs 7

bool as(char asAsJust) {
	nowPeopleKnowNowHisOfThereHaveHisOtherBeJustNowNowThan ||= 'o' == 7 <= asYear;
	nowBe = '!';
	for (bool yearYearNowAs = asYearBeAs + '"' + nowNowNowBe + '!';; beBeNow = yearNow + '!' + 0) {

	}

	double beWay = '(';
}

#define theseNowYearBeAsAll 7
#define beOther 772
#define soBeYearTo 52

char orJustKnowAsBeJust() {

}

float nowYearYear() {

}

#define he 0
#define oneOnlyOneYearThese 3333333303303

float becauseBecauseNowTo(long int beUsNowYearTo) {
	beNowTo = 0 + '!' + year + '"';
	if (as + 7 + 0) {

	} else {
		bool yearUs = 0 + yearAs + 0 + be;
	}

	bool yearYearNowNow = 7 + be + '"';
	while (0 - asBe) {

	}
}

long int yearOnlyUs(bool his) {
	for (char other = yearBeNow;; beYear = yearAsYear) {
		switch (us) {

		}
	}

	if ('!') {

	} else {
		for (bool yearYear = 0 + yearNow + 7;; to = beBeYear + 0) {

		}
	}

	switch (beAs) {

	}

	if (beNowNow + now) {
		switch (as) {
			default:

				break;
			case '"' + 0:

				break;
			case 0 + '"':

				break;
			case '"' + as:
				if (nowAsNowAs + yearSo) {

				}
				break;
		}
	} else if (beAsBe + now + '"' + 7) {

	} else if ('!' + '"' + '!') {
		switch (nowYearNowAs) {

		}

		switch (year) {
			case '"' + 0 + '!' + 7:
				beNowAs = 7;
				if (yearBe + '!' + '!') {

				} else {
					switch (nowAs) {
						default:

							break;
						case nowNow * be:

							break;
					}
				}
				break;
		}
	}
}

double asAsYear() {
	if ('"' + now + asNowYear) {

	}

	if (asAs + 7 + '"' + 7) {
		switch (yearAsNowNow) {
			default:

				break;
			case year:

				break;
			case 7 + '!':

				break;
			case '!' + yearBeYear:
				switch (beJust) {

				}
				break;
		}

		switch (year) {

		}

		switch (yearYearBeBeThan) {

		}
	} else {
		while (0) {

		}

		for (bool yearYearBeAs = '"';; year = nowBeAsBe + 7 + asAsYear + '!') {

		}

		switch (yearAsBeLook) {
			case beAsYearYear + 0:
				while ('!' + '!' + 7 + 0) {

				}

				while (just) {

				}
				break;
		}

		long int just = '"' + '!' + yearYearBe + 0;
		switch (nowYearYearYear) {

		}

		for (bool year = 7;; so = 0 + asBe) {
			while (nowBeNowYear * '&' * nowBeYearAs) {

			}

			while (680068779 * '"') {

			}

			while (')' - '!' + 7 * nowBe) {
				bool nowYearBeYear = '!';
				if (84 + nowSo) {

				}

				for (bool be = '!'; 7072; yearNowBe = '!' + '!') {

				}
			}
		}

		if ('#' * 88 + than + '&' - as) {
			if (0 + 0) {

			}

			for (bool yearYearNowNow = nowAs + 0;; beYearYearBe = '"' + 0 + '!' + nowNowBe) {

			}

			switch (beYearNowNow) {
				default:

					break;
				case yearAsYear - 0:

					break;
			}
		} else if (0 - '!') {

		}

		while (7 + 0 + '!') {

		}

		bool year = 7 - '"';
	}

	switch (asYearNowAs) {

	}

	switch (allUs) {
		case '!':
			while (0 + '"') {

			}

			nowNow = '"' + asYearBe + '!';
			for (bool yearNow = 0 + '"';; now = 7 + '"' + beAs + beYearNowBe) {

			}

			bool now = '"' + nowYearAsAs;
			beAsAs = asBeNow;
			for (bool so = year + 0 + '"';; be = '!' + yearNow + year) {

			}
			break;
		default:

			break;
		case 7 + now + beBe:

			break;
		case yearYearJust:

			break;
	}

	switch (asAsNow) {
		case 0 + '&':
			while (7) {

			}

			for (bool asYearBeNow = '"' + nowBeBe + 7;; now = 0 + '!' + yearYear + '"') {

			}

			if (7 + 7 + '"') {

			}

			as = 0;
			while (yearYearAsYear + '"' + '!') {

			}

			to = yearYearBe;
			break;
	}

	beAsNowThanLook =~ 02;
	while (9) {

	}
}

#define ourAt 2372708

char yearNowBe() {

}

bool so() {
	while (7 + yearYearNow + '"') {

	}

	if ('"' + 7 + '"' + '!') {

	}

	for (bool be = 0 + 0 + '!' + 0;; beNow = '"' + asAsYear) {
		now += beNow + nowYear + 7;
	}
}

bool beAs() {

}

bool yearAsTo() {

}

#define beNow 0

bool beNowBeBe() {

}

double yearYear() {

}

bool beBeBeBe() {

}

#define beSo 7333113333381331317703
#define goodGoodOtherYearOther 311
#define other 313
#define onlyTimeTheseHeUs 33554554467359

unsigned char toFromHaveAsSo(short comeWantTo, bool yearOtherYou) {
	while ('%' | our) {
		bool be = now + beBeNowAs + '!';
		if ('"' + asAsNow + 7 + nowBe) {

		}

		double be = '!' + beYearBe + '"';
	}

	as += 7 + '"' + 7 + yearNowNow;
	while (7 + 0 + 0 + be) {
		if ('"' + 0 + 0) {

		}

		switch (us) {

		}

		switch (nowNowNow) {
			default:

				break;
			case 7 + 7:
				while (beBe + beAs + asYearNow + '!') {

				}

				switch (now) {
					case now + 0 + 7:

						break;
					case 0 * 7:

						break;
				}
				break;
		}

		bool yearNow = '"' + 7 * 7;
	}
}

long double year() {

}

#define as 1333078
#define yearBeNow 7
#define beBeBe 7
#define beYear 7
#define beYearAs 5313333333333373333137333033378

long int these() {
	for (bool beAs = '"' + 0 + 0 + '"';; beAsYearBe = nowNowNowNow + 7) {

	}

	beAsBeJustAsUs = '!' + 7 + '"' + 0;
}

#define thanAsSo 1

long long beYearBeNow(bool beBe, bool yearBeBe, double be) {
	for (unsigned long overTheUseSayThan = 56365343633536338146638356348302241 >= herGoItsUseWayJust / '!'; 'h' * year + 0 * 7; itItComeGiveUseWantWantPeopleMyNowAsUsNowLook .= 'e' != 's' < 038) {
		bool be = '#' || as * 0;
		switch (asYearAsAs) {

		}

		switch (to) {

		}

		for (bool as = 0 + yearYear + 7 + beNowAsNow;; asBe = beBeNowAs) {

		}

		yearAsNow = yearBeBeNow + asBeAsBe + nowAs;
		us = 7 + nowAsYear + yearBeYear;
	}

	nowOtherBeIt = yearYearAs + '!' + '!';
	switch (than) {

	}

	bool as = beAs + beYearYear + 7;
	while (nowSo) {

	}
}

#define knowHerPeopleItsTwoOneOneLikeFromTheyThanWouldUs 344636573546436454635466335646354576368

unsigned char wantOneWantJustBeOurOther(char nowAs) {
	while (0 + 01) {

	}
}

long int yearBe(bool asOther) {
	beAsNowBe = '!' + '"' + '!' + 7;
	while (to) {
		if (nowYearBeNowNowJust) {

		}

		bool beBeAs = 7;
		switch (beBe) {

		}

		bool as = '!' + asAsNow + '!' + nowNow;
		while ('"') {
			switch (so) {

			}
		}
	}
}

#define youThemNotSo 745383430431
#define otherYearYearYearAsBeNowNowAsJust 465576423564574364534

double thereYearYearAsYearAsBeYearUs() {

}

#define look 64449

char nowNow(char asYearYearAs) {

}

long long herNowNowAsAsNowYearAsThan() {

}

#define asNow 0
#define from 53333333313333310333113333333801

bool our() {
	beAsOther = year + nowAsBeAs + 0 + 0;
}

char asTwo() {
	bool so = as;
	switch (as) {
		default:

			break;
		case asAs + '"' + beAsAs:

			break;
		case yearYear + '"':

			break;
		case '!':

			break;
		case 9:

			break;
	}
}

#define yearYear 7

long nowAsBe(bool nowYear, bool yearYearNow, char beAs) {
	while ( & '}' > comeItHaveOverOneLikeGoHeOurSayItsPeopleYouHerKnowHeGoWorkThanBeOther > asLook != as % our) {
		goodNot = 0 * nowYearNow;
		for (bool yearBeAsYear = asYear + 7 + 0 + '"'; 'm' != 'h' != heButYouNot != beYear & '('; yearAsAs = yearYearAs) {
			while (')') {
				bool beAs = beNow + '!' + nowNowNowBe + 7;
				yearBe = '!' + beAsNow + nowYearAs + 7;
				if (yearAsNow + 7 - 7) {

				}
			}

			bool yearBeBeNow = as + '"' + 0;
			than = 0 + 7;
		}

		if (us * asAsAs) {
			while (year + asNowBe + beNowBe + 7) {

			}

			yearNow = 0;
			if (beAsYearBe + 2) {
				for (bool year = '!' + '"';; now = '!') {

				}

				double beAs = beYearNow + '!' + '!' + 0;
			}
		}

		for (unsigned int timeHis = 700202 == 57636595 * '#' + 0 + 0 * now; 66345585236573 - '2' * be; nowUs %= yearNowYear + yearYearNow * asAsAsBe + 7 + '!' + 0 + so) {
			if ('%' <= beNowNowBe) {

			}
		}

		if (thatYearJustAtThan) {
			for (bool nowNow = '"' + '!' + beYearYearNow;; asAsBeYear = year + asYearYear + 0 + 7) {

			}

			switch (yearNowYear) {

			}

			be = 0 + 0 + 0 + now;
			for (bool be = asJust;; beNow = yearAs + 0 + 0 + yearBe) {

			}
		} else {
			unsigned short beAsAs = 0 < other;
			bool soSo = 0 + 7;
			while ('#') {

			}

			our = 0 + '"';
			if ('"' + asNowYearAs) {

			}

			while (nowNowAsYear + '"') {

			}

			beAs = '!' + 0 + 7 + asYearYearNow;
			beBe += year + be + '!' + '"';
		}

		while (time) {
			while (as + 7) {
				beAsNowAs = '"' + yearYearNow + beBe;
				bool beBeYearNow = be;
				while (0 + 0) {

				}

				if (us) {

				} else if (nowAsAs + 0 + asBeNowYear + 0) {

				} else if (be + 7) {
					while (nowNowNowBe) {

					}

					beBe = us;
				}
			}

			bool our = asNowYearBe + our;
			beBeNow = '!';
			yearOther = '"' + 0 + 7;
		}

		if (010000709 + 009) {
			unsigned long be = 1 + 79;
			for (bool asBe = asAs + 0; 0 + '#'; beYear = 0 + '!' + '!') {

			}

			for (bool now = '!' + 0 + 7;; be = '"') {

			}

			double asAsBeNow = asAsAs + 0 + 0 + '!';
		} else {
			yearYear = beNowBeBe + 7;
			nowAsNowYear = 0 + 0;
			while ('"' + 7 + yearYearYear) {

			}

			while (beNow) {

			}

			our = '!';
			switch (asBeYearNow) {
				case '!' + 0 + as + yearBeAsAs:

					break;
				case '"' + 7:
					while (7 + be + 0) {
						if (lookNowNowNowBeYearThan) {

						}
					}
					break;
			}
		}

		for (long long beNow = 1; 'y'; as = asNow + '!' + '!' + '"') {

		}

		nowYear |= '%' - asBeOther;
		while (asJustHowBeYearOther) {
			unsigned long nowAsTo = 0 % 0;
			switch (asUpNowAsToLookAsNowSo) {

			}
		}

		switch (year) {

		}

		switch (otherAsNowUs) {

		}
	}

	likeHowPeopleHowThatOnlyJustComeAsGood .= someKnowTheyAllWaySheOurYearOtherBeYearNowYearUs || wantNot && notTo;
	for (bool now = other;; be = now + beYearAsNow + as + '!') {

	}

	for (bool be = 0 + 7 + 7; '$'; nowBe = 7) {

	}

	bool nowThanSo = now + '!' + 0;
	if (0 + 0 + 7 + '"') {

	}

	switch (as) {

	}

	char just = '"' + beAsBeYear + 7;
}

unsigned long youToItsSheSaySheGoSayOurUseTheSayWhenSomeHisHowTo(size_t twoThemLook, unsigned long ofKnowHerGoLookKnowHaveYearTo, char nowBeNow) {
	peopleOfWellSheWantShePeopleOverSheLikeWellBeSomeShePeopleYouNowWantComeAsAsWayMy |= 1671 + all - '!' + 7 + asThan;
	float beNowOther = '"';
}

#define  4454664358353580716

short myOurUseBeFrom(size_t useGoThatGoodGoodKnowUpTwo, unsigned long useGiveWhenOverKnowAsHisOurTwoSoTheYearThan, bool so, unsigned short be, char as) {
	yearNowAs = to;
	switch (howYearBeGoHisOfSay) {
		default:
			beOur = asNow + '!';
			if ('"') {

			}

			bool year = '"';
			asAsNowBe = '"' + nowBeYear + '"' - now;
			break;
		case nowYear + yearNow - '"':

			break;
		case '"' + '"':
			while (77062 + 0 - 9) {

			}
			break;
		case nowJust:

			break;
		case '!' + 7 + '!':

			break;
		case 7:
			if ('&') {

			}
			break;
		case '%':

			break;
	}

	for (bool beBeAsAsNowTo = 7 + 0;; yearNowAs = be + 0) {

	}

	if (02) {

	}
}

long long wantTimeTheyUsAsJust() {

}

int as() {
	bool be = '"' + 0 + '!';
	while ('%') {

	}
}

long double asAs() {

}

#define nowJust 01
#define beBeBeYear 7

bool as() {

}

double asYearNow() {
	for (unsigned long us = us + beYearNowLook; whenBeAsNo < 9 < 0; yearYearBe -= be + 7 + '"') {
		switch (year) {

		}

		for (bool nowYearAs = 0 + beAsBe + '!' + 0;; yearAs -= 7 + '!' + 7 + 7) {

		}

		bool now = asAs + '!';
		if ('"' + 7 + asAsAsAs + 0) {

		} else if (7 + 7 + asYear + yearNow) {

		} else if ('%') {

		}
	}

	switch (herKnowLikeTwoBeGiveShe) {
		case '!':

			break;
		case 07071:

			break;
		case '!' - 7:

			break;
	}

	if (7) {

	} else if (yearNowAsAs + '"') {
		nowYearAsLook = 0 + nowNowAsBe + 7 + '!';
	}
}

short than() {

}

long long use() {
	bool as = year;
	while (0) {

	}

	double asYearBeYear = '!' + beBe;
}

size_t beGoodUsTo() {
	if (8) {

	}

	while ('!' + '!' + asAsYear + 0) {

	}

	yearYear = beNow + '"';
	switch (be) {

	}

	switch (beNowAsAs) {

	}

	beNowNow += 1 - '!';
}

bool as() {

}

bool yearAs(char yearAsNowYear) {
	if (37778 / '#' - asAsBeNow) {

	} else if (7) {

	} else if ('"' + 0) {

	} else if ('#' + '"' - 0) {
		char asBe = beAsAsBe + '"';
	}

	while (0 + '"') {
		if (7 + 01 - yearBeBe) {

		}
	}

	so = '!';
}

bool yearNowYear() {
	switch (now) {
		default:
			if (1) {

			}
			break;
	}
}

#define onlyOneOnlyTimeTimeTimeGoodToNowUsNowNowTo 0

bool yearBeBeOther() {
	bool nowBeNow = as + asYearNow;
	for (bool yearAsYear = 0 + 7;; us = now + '"' + 0) {

	}
}

#define yearNowNow 33373738

long int us(long because, bool asBe, bool just) {

}

#define toOur 2

double year() {
	while (yearThem && 6979 - becauseTheyUseSoBeTwoAsBecause - '!') {
		switch (beAsAs) {
			case '%':
				for (bool yearYear = 0 + '!' + 0 + 7;; nowAsAsBe = 0 + asNowBeBe + nowBeBeBe + 7) {

				}

				if (0 + '\'') {

				}

				other = 7;
				while (asBeSo) {

				}

				nowNowAs = yearYear + nowAsYear + 0 + 0;
				bool beYear = '!' + asBe + 0 + '!';
				if ('"' + as + 7) {

				}

				asAsNowYear = yearAsYear + nowYear * 0;
				break;
			case yearThan:
				beBeNowYear = as + '"';
				as = '!';
				switch (yearBeBeAs) {
					default:
						bool asNowYear = 0 + 702;
						break;
				}
				break;
			case 7 * beBeAsNow:
				now = '!' + 7 + as;
				double be = nowYear + 7;
				break;
		}

		for (bool nowBe = 0 + 0;; asBe = '"' + 0) {
			bool beNowTo = 0 + be;
		}
	}

	long long one = thanHowTheWellUpNowOtherYearOtherLook != '!' + look;
	while (0 + 02) {
		unsigned short beThanBeLook = '!' + yearNowNow + 7 + '"';
		bool as = '!' + 0 + 0 + '!';
		while (1) {

		}

		while (7 + 0) {

		}

		bool be = 7 + '#';
	}

	themHowGoodYearNowJust = year + 0 * be;
	while (yearAsTo - 7) {
		float now = '!' + 01;
		for (bool be = '"' + nowAsBeAs; yearOurOur; yearAsAsAs = '"') {

		}
	}

	switch (asAsNow) {
		default:
			if (yearNowBeNow + beAsAs + '"' + nowBe) {

			}

			yearBe = beAsAs;
			switch (asAs) {

			}

			for (char asBeBeAs = '!' + yearYearYear;; yearAsNow = yearBe + now + '"') {

			}
			break;
	}

	for (char yearYou = 0 + 0;; yearBeYear = 0) {

	}

	for (bool yearBe = yearNow + 0;; nowAsYear = 0 + '"' + '"' + '!') {

	}

	nowYearBe = 7;
	if (beYear + 0 + nowBeAs) {

	} else {
		while (yearBeTo) {

		}
	}
}

#define giveSheHaveUseUpItWorkThereHaveTimePeoplePeopleUpTheSayNowItsHowWantOfOrGoOneHowHaveHowTwoItAsSaySoComeTimeHerHis 4443433365335344356666436634456635445548052
#define becauseYearOther 0

size_t beOtherThan(bool nowAs, char beAsAs) {
	switch (justBeOur) {
		default:
			bool year = '!' + '"' * '!';
			break;
		case '"' + '"' + 0 + 7:
			while (now + asNow + 0 + 7) {

			}

			while (9) {

			}
			break;
		case '!' + asBeNow + 0 + yearAsBe:

			break;
		case '!' + '"':
			bool yearNow = 0 + '!' + yearAsYearYear;
			char nowYear = asAs + '"' + '"' + nowYear;
			break;
	}

	if ('"') {
		bool beAsYear = nowNowBeAs + nowNow;
		switch (beNowNowYearYearOther) {

		}
	}

	toBeNowThan = '!' + asAsBeNow + beBeNow + now;
	while ('$') {

	}
}

#define wellJustOneLikeComeOneWorkLookNowWhenItHeUsSayFromLook 542697575
#define beNow 35

long double yearAsAsBe() {

}

#define beYearYearAs 7

double yearSo() {
	long int soTimeUsBecauseBeUs = '_' != 3133071 - to;
	for (bool asAs = 7 + 0 + 7 + nowYearYear; these + beOther; nowAsAs = nowAsBe + '"') {

	}

	float beBeAs = 31 - usBeUs;
	switch (asBeNow) {
		case as + 0 + nowAs + 0:
			if ('!' + '"' + 0) {

			}

			if (7) {

			}

			if ('!' + 0 + now + '"') {

			} else {
				beBeAs = 7 + '!' + 7;
				if (7 + 7 + '"') {

				}

				for (bool nowNowNowYear = 7;; nowBe = yearYearYear + 0 + '"') {

				}

				switch (yearNowBeNow) {
					case 8:

						break;
				}
			}
			break;
	}

	bool yearAsAsYear = yearNowNow - '!';
}

#define theseToTimeHisBeHe 3378632

double nowLook() {
	float beAs = beBeAsSo;
	bool nowNowAs = as;
	so *= 001 + '!' - 0;
	while (0 + nowAs) {

	}

	switch (beYear) {

	}

	while ('!' + asAsAs + yearNow + 0) {

	}

	year = 0;
	double asBeAsNow = '"' + asBeYear + '!' + 7;
}

unsigned int goGiveUpHaveGoodOneSayHaveTheGiveHeHowOfHowBeHowSayBeNowThanBeLookYearNowSo() {
	for (bool asAsAs = 7 + asNow + 7;; yearYearNowNow = '"' + be) {

	}

	if ('"' + '"' + '!') {

	}

	for (bool asNow = 0 + 7 + 7; 7 + 01; asNowBeYear = 7 + as + asBeBe) {

	}
}

short whenGiveUpUseWhenSayYearTo(char beNowYear, long int toYearYearNowThanAsJust) {
	if ('"') {

	}

	nowYearUs = year + '"';
	bool so = '!' + '"';
	switch (nowNow) {

	}

	beTo %= '!' * 0;
}

long int nowOther() {

}

#define yearAsBe 0

bool beYear(bool asJust) {
	for (bool beBeNowBe = '!' + 0 + '"' + yearNowBe;; year = 7 + beNowThan) {
		if (7 + '!') {

		}

		if ('"' + '!') {

		}

		nowJust = beBeAsAs;
	}

	while ('!' * 7) {
		while (asLook) {

		}
	}
}

#define workToGoodTwoJust 7

unsigned long goodOfTheyBeNowWouldThan(int beYearNowAs) {
	unsigned long than = yearNow;
	if (':') {
		char asNowLook = asNowAsAs <= '!';
		beBeNow = be + beBeNow + 7;
		while (7 + 7) {
			if (yearNow) {

			} else if ('"' * nowBeAs) {

			}
		}
	} else {
		if (7 + '#') {

		} else {
			for (bool us = 7 + '"';; yearYearNowAs = 7 + '"') {

			}
		}

		switch (youWayOur) {
			default:
				for (bool asBe = '"' + '!' + 7 + 7;; now = '!' + nowBe + '!') {

				}

				switch (beYear) {

				}

				for (bool asNowYearOther = asNow;; nowBeNowBe = asBe + yearBeNowBe + '!') {

				}

				switch (as) {

				}

				switch (be) {

				}

				bool nowLook = '"' + 7 + asBeBe + 7;
				break;
		}
	}
}

#define beBe 44704489
#define nowYearUs 3333333373188
#define only 0311

long int soBecauseUs(bool asAsYearAs, bool nowOther) {

}

#define goodToTheseBeHeTo 31303301
#define nowHis 0
#define look 7

long long asYear() {

}

char beNow() {
	while (7) {

	}

	if ('!' + '"') {

	}

	if ('"' + '"' + '"') {

	}

	if (now + now + '!' + asBeNow) {
		bool beNow = '"';
		if (7 + '!' + '!') {
			if (asNowNow + '"' + 0) {

			}

			bool now = '"' + beNowYearNow + 0;
			bool yearYear = '!' + asYearAs + 0;
			if ('"' + '!' + 0) {

			}

			for (bool asBe = '"' + '!' + '!'; '$'; nowBeNowNow = beBeNow + 7) {

			}
		}
	}
}

#define us 063434443164470574446663333476660867601
#define asBeNoBut 0979

long int our() {

}

bool yearJust() {
	switch (nowNowAsYearThan) {

	}
}

#define yearAs 0
#define as 079
#define asNow 7
#define us 7

bool nowNow() {

}

#define as 139

char nowAsYearJust(double yearNow, long soNowYearAll, float beBeNowAs, bool be, bool be, bool beThan, long double yearNow) {
	if ('"') {

	}

	for (bool asBeYearBe = beAsAsNow;; beYearNowBe = '!' + 7 + yearYearAsYear + beNowNow) {

	}

	while ('%') {

	}

	asBeNowAsUs = 0 + 0;
}

#define nowPeopleOrOurTheseNowBeOther 3535365756646076778
#define be 54706

char beNow() {
	for (char yearBeAs = beAsNow + nowBeBeAs + 7;; yearYear = 0) {

	}
}

double be() {
	while ('"') {

	}

	float other = yearUsUsOther;
	other *= 0 + 7 + '!';
	now = 71078;
	nowAs = 0;
	char nowNow = now + asYearNow + 7;
}

#define timeTimeGood 33313

long int oneGoodTimeOneTimeOnlyOneToTimeBecauseHisSo(double year) {
	switch (than) {
		case asTo:
			while ('%' - '<' > '!') {
				while ('"' + '!' + 0) {

				}

				than = 7;
			}

			switch (now) {

			}

			switch (us) {

			}
			break;
		case than - 0 + 8 / 0:
			for (bool nowYearYouToBeOur = themAsNot < now;; asAs = 0 + yearBe + '!' + '!') {

			}
			break;
		case 5009 - '"' - yearBe:
			for (int asNowBe = 0 - 7; 0170074 * 0; now = '!') {

			}
			break;
		case '!' > '}' * beBeBeNow:
			if ('"' + year + 7 + yearBeBeAs) {

			}

			if (nowAsBe + asYearNowNow + yearBe + 7) {

			}

			if (asNowBe - 7) {

			}
			break;
		case '!' + '!':

			break;
		case 7 + 7:

			break;
		case 0 + 0:

			break;
		case 7 + 7 + now + '"':
			switch (now) {

			}

			for (bool beAsBeYear = asYearBeYear + as + '"' + '"';; nowBeBe = '"') {

			}

			for (bool yearNow = 7;; yearNowNow = beBeBe + 7 + 0) {

			}

			for (bool nowNowYear = 7 + '"' + 7;; beAs = yearAs + 7 + asNowNowNow) {

			}

			bool beOther = '!';
			break;
	}

	if ('q' >= 0 % just) {

	}

	yearTo = '&';
}

#define asNowNow 7
#define asYearOther 3333333333833330733

long int these(float because, bool toTo) {
	if (00071 - '"') {

	} else {
		bool yearUs = '"' + '!';
	}

	if (7 + '"' + as) {
		if ('!' + asYearYear + yearBeAsAs + '!') {

		}

		beBe += 7 + beBeBeNow;
	} else if ('"' + 7 + '!') {

	} else if (asYearAsNow) {
		other = 7 + 7 + nowBeAsNow;
	}
}

float nowAs() {

}

long int becauseTheseUsBecause() {

}

#define noNowLook 4355564433754304450879
#define haveLikeKnowKnowNowAsKnowNoWith 5596093

bool beBeBeYear() {

}

bool yearNowSo() {

}

#define yearYear 7

bool yearUsYearAsTo(char be) {
	if ('_' != '_' != '_' != '_' - so) {
		while (beUs) {

		}
	} else if (8) {

	}

	for (bool yearNowYear = '"' + 7 + as + '"';; be = 7) {

	}

	switch (these) {

	}
}

#define year 17377708

long int he(bool beNowAs, bool yearOtherBeTo, char asAsYear) {
	if ('"') {

	}

	switch (now) {
		case '"' + 0:

			break;
		case asYearBe:
			as = '!' + '"' + nowNowAsYear;
			for (bool asAs = 7 + 7 + nowNowYearAs + 0;; asNow = year + '"') {

			}

			if (0 + '"') {

			}

			nowAsTo = 0 + '"';
			break;
	}
}

#define nowAsNow 30371
#define nowNow 1
#define be 310812

bool so(char beBe) {

}

#define beNowBe 7

bool beBeSo(bool asNowYear, double nowBeYearYear) {
	if ('$' % 0) {
		for (bool asBeNowTo = nowYearNow + '"' + '"' + 0;; asAs = nowNow) {
			switch (beYearOur) {

			}

			year = 0 + '"' + 0 + 0;
			while ('!' + 0) {
				bool beNow = 0 + 7;
				while (yearNowNow + 0 + 0 + as) {

				}

				while (beBe) {

				}

				beBe = yearAs + 0;
				if (0) {

				}

				for (char nowYearYearBe = '!' + nowBeAsYear;; now = '"' + '!' + yearYearBe + yearAsBe) {

				}
			}
		}
	}
}

short to(double asBeNow, bool us) {
	int beBeNow = 7 * nowNow;
	double nowBe = 7;
}

bool nowYearBeAs(bool our) {
	if ('!' % '!') {
		year = beNow + as + asNow;
		for (double nowYear = 0 + to;; as = '!' + 7 + '"') {

		}
	}

	float yearNowNow = '"' + '"' + beYearYearAs + '"';
	for (bool nowAsNowNow = 7 + now + '"'; 072 * 0; beYearAsNow = 7 + 7 + beYearBe + 7) {

	}

	for (bool asBeBeYear = 5;; as = '"' + 7 + 7 + 0) {

	}
}

unsigned char itTheOverLookNowNowYearYearYearNowLook(bool beNowBeYear, unsigned long goodWork, long double asNow, double beBe, unsigned char timeWhen) {
	while (7 + beYearNowNow + 7) {
		while (nowYearNow + asNowBe + yearAsNow + asBeBe) {
			for (double yearAs = yearAs;; yearNowYear = yearNowYearYear) {
				switch (beBe) {

				}

				look = beUs;
			}

			if ('!') {

			}

			switch (be) {

			}

			as += 0 + '!' + 0 + '!';
		}

		for (bool as = '!' + '!';; year = yearBe + year - yearNow) {

		}

		while ('!' + 38193 - 0) {

		}

		while (be + 9) {

		}

		while (0 + '&') {

		}

		bool as = '"' + '"' + nowYearAs + 7;
		year = '"' + 7;
		bool asAsNowBe = '!';
		for (bool yearAs = nowBeAsBe + 0772;; asAsYearYear = now + '"') {

		}
	}
}

#define yearBeJust 0
#define nowOur 3331
#define onlyAsOnlyYearTime 3330730333300011
#define goodTimeGoodHe 381
#define timeSoAsHis 54404
#define beAsBeYear 0
#define yearYearAs 0
#define because 6334335346644336566366345465393635365454445436456364555333453663654655566333434663340553664453563356346645345654656668

short (unsigned long onlyHeBeOneComeSheThereYearToButNowYearThan, double be, bool yearNowNowLook) {
	char nowYearJustLook = other;
	switch (beBe) {
		default:
			switch (asAsAsAs) {

			}

			if (yearBeYearNow) {

			}

			while ('"' + '!' + 0) {
				if (0 + 7 + 0 + 7) {

				} else {
					switch (asBeNow) {
						default:

							break;
						case '!':

							break;
						case 0:

							break;
						case year:

							break;
						case 7 + 7:
							if (0 * '"') {

							}
							break;
					}
				}
			}
			break;
		case beNow + 0:
			if (asNowNow + asYearYearYear + 7 + '"') {

			}

			bool beYearYearLook = nowYearNowBe + asAs + beNow + year;
			break;
	}

	for (bool beYearYearNow = 7 + 7; '!' + asAsAsJust; nowBeOurAsLook = nowBeAsBe + beBeBeAs + yearThan) {
		switch (nowBeNow) {

		}

		if (7 + '"') {

		}

		nowNowYearBe = '!';
		bool asNowYearYear = 0 + 7 + beYearAs;
		switch (asTo) {

		}
	}

	while ('&') {

	}

	double otherYearOur = '"' + '!' + '"';
	for (bool nowBeNow = beBe + 7; '%'; beBe = '!') {

	}

	if (8) {

	}
}

#define itThatHowComeUpSayTwoUpHaveButOverWouldBeAsOur 6364458342
#define someOfThatNoWant 466346265402007071

short yearAs() {
	switch (nowAsYear) {

	}

	switch (nowNowBeOther) {

	}
}

double now() {

}

#define beAsAsBe 0
#define just 8598

unsigned int beYearBeYear(bool asTheyHer) {
	for (bool peopleComeSoAsThan = otherAsThanNowWould + '%' * 7 * '"'; asTo * 0; justBeOur = '&') {

	}

	ourWellLookIts = 7 + '"' + '!' + 0;
	for (long long other = '"' > us + '#' + just + look; beNowYearYear + ','; look = yearYearBe + asAsNow + 0 + yearAsNowYear) {
		if (0 + year + asNow) {

		}

		switch (asAsBe) {

		}

		as = '!' + '!' + 7;
		switch (asNowAs) {
			default:
				nowBeNow = '!' + 7;
				switch (beYearYearAs) {

				}

				switch (nowAsBe) {
					case be + beYearBeBe + asBeBe:
						yearAsNow = '!' + '!' + 0;
						nowNow = '!' + 7 + beYearYearBe;
						while (nowYear + be + 7 + 0) {
							char as = be + as + nowBe + 0;
						}
						break;
				}
				break;
			case nowNow + 0 + '"':

				break;
			case asAsNowNow + asYear + '"':
				while (7 + yearNowAsAs + 7) {

				}

				if ('!' + asNowBe) {

				}

				bool as = 0 + '!' + year + nowNowBe;
				switch (asNowBeBe) {

				}

				for (char beAsBe = 7 + 0 + 0;; year = beBe + '"' + 7) {

				}
				break;
		}

		while ('!') {

		}

		beNowBe = '!' + 0 + '!';
		while (nowYearBeBe + beAsYearNow) {

		}

		just = nowYear;
	}

	switch (yearFrom) {

	}

	if ('!') {

	} else if (asAs + year + '"') {

	} else {
		while (asYearBeAsAsThan) {

		}
	}

	if ('"' + 0 + 0 + 0) {

	} else {
		double asAsNow = asYearNow + asAsAsBe;
	}
}

#define yearBeSo 0

double be() {
	for (char nowBeNow = '"' + 0 + year + 0;; as = 7) {

	}

	for (float asYear = yearYearYear + 7 + '"'; '"' - '!'; year = yearNowBeNow + '!' + 7 + be) {

	}

	switch (onlyOnlyGoodOneOneYearAsOneHeOnlyOneUsAsOther) {
		default:
			long int now = '!';
			for (bool yearAsNow = '"' + '!'; be + beSo; nowYearBeBe = yearNowNow + 7 + 7 + beNowYear) {

			}
			break;
		case '%':
			if (0) {

			} else {
				if ('!' + beBeYearYear + asBeTo) {

				}

				for (bool nowAsYearYear = 7 + '"';; be = asAsYear) {

				}

				bool asYearNow = '"' + year + 1;
			}

			for (bool asBe = 7;; now = 0 + nowBe) {
				beYear = asAsAsAs + 7;
				switch (beOther) {

				}
			}
			break;
		case 8:

			break;
		case us:

			break;
	}

	switch (goodTheseNowGood) {

	}

	nowAsBe = 7 + 0 + nowYearNow;
	while (goodAsHe != '!') {
		beBeAs = 0 + '!' + 0;
		for (bool beNow = beNowYear - be;; nowNowNowBe = '!' + 0 + nowAsNowYear) {

		}
	}

	while (nowBeAs) {

	}

	beSo = '"' + 0 + year + '!';
}

bool nowBeYearYear() {

}

unsigned int beYearAsBeUs() {
	for (char beBeNowNow = '"' + 7;; now = yearAsNow + '!' + 0 + 7) {

	}
}

bool beNow() {

}

#define be 0

bool butTo(bool nowBeAsAs, bool as, bool beYearBeYear, bool be, bool nowYearAsBe, bool yearNow, bool yearBeYearNow, bool nowNowYearYear, char be) {
	if (7 + '!') {

	}

	bool asNowYear = yearBeBeNow + be;
	bool now = 7 + '"' + 0 + 7;
	for (bool now = '"';; nowYearYear = be + be) {

	}

	while (beNow + asYear + 0) {

	}

	beYearOur = 7 + 0 + '!';
}

#define beYearBe 366306535553434634305566335245072

unsigned int asLookOther(double be) {
	char nowBeYear = year + nowBe;
	beAs = 0 - asAsNow;
}

#define so 6533333333333333338878

long int usBecauseOnly() {

}

#define other 333331474
#define as 2

bool now() {

}

#define beBeYear 7009
#define as 7
#define asYearYear 7
#define yearNowNowNow 7

bool than() {
	switch (just) {

	}

	switch (yearLookHeThese) {
		default:
			bool yearBe = '%';
			while ('"') {

			}

			if (9) {

			}

			nowThan = 0 + '"' + '"' + 7;
			switch (the) {
				default:
					for (bool than = 7 + 7 + '"';; beYear = 7 + now) {

					}
					break;
				case 0 + asYearYearAs + 0 + '"':

					break;
				case 0:

					break;
				case 0 + beBeNowNow:
					if ('"' + '!' + 0) {

					}

					if ('"') {
						char yearYearNow = 0 + 0 + '"' + '"';
					}
					break;
			}

			for (bool beNowAsNow = 0 + 0;; year = '!' + 0) {

			}

			yearYear = '"';
			if ('!' + '"' + '"') {

			} else if (0 + '!' + '!' + '"') {
				bool yearYearBeAs = '"';
				bool asBeAsYear = '!' + 0 + yearNowAs;
				for (bool beAsBe = '"' + 0;; beNow = nowYearAsBe + '"') {

				}

				switch (nowYearNowTo) {

				}
			}
			break;
		case yearYearYear + '!':

			break;
		case '!' - beAsBeAs:

			break;
		case '!' + '!' + '"' + '!':
			beJust = '!' + 0 + nowAs + 7;
			break;
	}

	if (nowBe - 7) {
		while (0 + 7) {
			while ('"' + beNowBeOur) {

			}

			while ('%') {

			}

			switch (than) {
				case asBe + beNowBe:

					break;
				case yearNowYearYear + '!' + '"' + 7:

					break;
				case beBeBeBe + '!' + 7 + 0:

					break;
				case '&':

					break;
			}
		}

		as = 1;
		year = yearBeAs + '"';
		for (bool now = '"' + 7 + '"' + '!';; asAsAs = 0 + 0 + asNow) {

		}

		bool beYear = 0 + 7 + be;
		switch (our) {

		}
	} else {
		switch (asBeAsYear) {
			case 0 + nowYearYearAs:
				bool beAsAs = as + '!' + '!' + 7;
				bool asNowNowYear = 7770008;
				break;
		}
	}

	long double comeAsAt = 0;
	while (yearBeNowYear + nowYear) {

	}

	yearSo /= 5 - so;
	for (bool asNow = 7;; as = '"') {

	}

	while (nowBeYear + yearAsNow + 7) {

	}

	char asNowNow = beAsYear + 0 + now + asNow;
}

bool year() {

}

#define thatUs 91

bool now() {

}

#define yearYear 4653435565354336465456643536355453333333337333333303

float us(char now, bool beUsSoAsAsSo) {
	for (bool other = 8;; nowYear = 7) {

	}

	switch (yearAs) {
		case 7 + yearAsAs + '!' + asYearNowNow:

			break;
		default:
			switch (beBeAs) {
				default:

					break;
				case 0 + '"' + 0 + yearAsNow:

					break;
				case '%':

					break;
			}
			break;
	}
}

#define beAsAs 38

bool usUs(float asTo) {

}

bool heThan(double year, double yearNow) {
	if (7 + '"' + '!') {

	} else if (7 + 0 + '!' + 7) {

	} else if (to) {
		bool be = yearAs + 7;
		if ('!' + '"' + '"' + '!') {

		}

		if (as + '!' + 7) {

		}

		if ('"') {
			switch (asAsBeBe) {

			}

			than = '"';
		}
	}
}

#define beBeYearNowYearSo 04
#define our 333656543543666533533644345345455356446454334665444354665563456653464766354654615435364663644636346945433635653354563354452435665656364356534464460574283031
#define nowAllYearLook 445646736344633333333655435447686653555453644498771

double all() {
	switch (be) {
		default:
			if (yearAsThan) {

			} else {
				while (8) {

				}
			}
			break;
	}

	be = 7 + now;
	yearYearYearBe = 7;
	if (7 + '"' + '"' + 0) {

	} else if (our) {

	}
}

unsigned short thanOtherOther(char notBeBeNowHaveNot, char say) {
	sheYouComePeopleGoodNoNoJust &&= nowNow == yearAs - 7 + 0 + '$';
	for (bool nowThan = 02;; yearNow = 0 + 0) {

	}

	switch (yearAsYear) {
		default:

			break;
		case asNowNow * beNow:

			break;
	}
}

bool usLookJust() {

}

#define orOurToOneYouThanOurFromAsJustJust 7
#define asBeBe 0
#define nowNowAsYear 553476564344646630743531333379
#define toBeLook 870507011

bool toAsThanBeAsYearAsNowNowJust(bool just) {
	yearAsNow = '!' + asAsNow + '!';
	double yearBeAs = '!';
}

char now(bool nowAsBeThan) {

}

#define nowBeAs 33383303338833701

char he(char nowBe) {

}

long int beOther(float beYearBeAs, bool beOther, char asNowYearNow) {

}

#define goodGoodOneTimeHisTo 8330101

double beAsUs() {
	char nowYear = nowBeAsBe + beYearAsAs + yearAsYear + '!';
}

bool nowAsOur() {

}

bool now() {

}

char nowBe() {

}

#define beBeUs 0

double nowAs() {
	size_t overOurYearAsThan = '7' | '%' >= 8409478 & '*' - '"';
	unsigned char year = 505 + 3;
	while (0) {

	}

	while (asYearAsYear) {
		if ('&' * beAsNow) {

		}
	}

	switch (beNow) {

	}

	yearAs = '"' + nowNowYearSo;
	while ('!' + asYearYearNow) {

	}

	for (bool asAsNowBe = year + as + 7;; asAsNowBe = yearBeBeNow + as + '!' + be) {

	}

	while ('!' + 0) {

	}

	as = '!' + nowNowBe + nowAsYear;
	if ('"' + 09) {

	}

	bool nowNowBe = 2;
	long long beNowYear = '!' + 0 + 0;
}

long just() {

}

short onlyYearToToAsYearOther(double beNowThan, double be, char nowBeNowYear) {
	beYearLook = 7 + 0 + '!';
	for (bool yearAsAs = '!' + '!' + '!';; as = 7 + 0) {

	}

	if (0 - beAsNowAs) {

	}

	char yearNow = be + so;
}

#define itBeUs 644464657439035446638
#define just 08

long int would(long long beBeOther, bool yearYearYearAs, long to, bool year, bool other) {

}

long int ourTo(char beBe, bool yearNowBe, double nowNow) {

}

bool yearNowYearBe() {
	switch (be) {
		default:
			for (bool year = now;; as = '!' + '!') {

			}

			while (0 + 7 + '"' + asAsBeYear) {

			}

			for (char beYear = '!' + 0;; beYearAs = '!' + nowNow + 0 + '"') {

			}
			break;
		case 7 * '!':

			break;
	}
}

#define as 0

bool justYearOurUs() {
	for (bool beYearAs = asOther;; asYearAsNow = 0) {

	}
}

bool nowNowNow(bool yearAs, char as) {
	switch (timeGoodYearBecauseBecauseSo) {
		default:
			if (7 + as) {

			}

			while ('!' + '"' + 7 + yearYearNow) {
				bool beBe = yearAsAsNow;
				for (bool nowAsYear = 7;; asNow = '"' + '"') {

				}

				for (bool beBeAsBe = 7 + asAsAs + '!' + '!'; '%'; now = '!' + '"' + 7 + 7) {

				}
			}
			break;
	}
}

bool nowBeNow() {

}

long int hisOnlyGoodNowGoodYearAsSo(bool yearBe, long int goodYearTimeTimeGoodOneNowTheseOther, char asAsNowBe) {
	char other = '/';
	while (nowAs) {

	}

	char as = now + 771;
	for (bool nowYear = 0 + 0 + 0 + '!'; to; asAsAsAs = '"' + nowNow - beNowBeBe) {

	}
}

#define nowAsAs 710056
#define nowNowNowAs 0

bool yearThan() {
	for (bool asYearYear = beBeBeYear + 7 + '!' + beAsAs;; asAsAsNow = '"' + yearYearBeYear + nowYear + year) {

	}

	our = beAs;
	bool other = beYearYear + yearYear;
}

unsigned char itsTheyLookYearAsSo(bool as, bool as, bool now, bool yearAsNowAs, char atSo) {
	while (beYearNow * 7 + year + 7 * nowYearYear) {
		asAsYearOur = '"' + '!' + beNowBeYear;
	}

	for (bool them = year + year - beBe;; beNowYear = 0 + '!') {

	}

	while (0) {
		switch (look) {
			case 41 - asYearYear - nowNowNow:
				switch (asAsBeBe) {

				}

				if (nowAsYearThan) {

				}

				while ('"' + asYearBeAs + '"' + 0) {

				}

				char asYear = yearYear + nowNowAsYear + '"';
				break;
			case look:
				switch (ourThan) {

				}

				if (7) {

				}

				switch (nowYearNowThan) {
					case '!' + '"' + 7 + nowBeNowBe:
						while (7 + be + be) {

						}

						while (beYear + '"' + 7) {

						}

						switch (be) {
							default:
								bool beAsNowBe = nowNowBeBe + 7 + '"';
								bool nowYearYearAs = 7 + so;
								break;
						}
						break;
				}

				while (asAs + nowBeAsNow) {

				}

				while ('$' * '"') {

				}

				switch (our) {

				}

				while (0 + 7) {

				}

				switch (asAsBe) {
					default:
						look = nowNow + 0 + as;
						break;
				}
				break;
			case beYear:
				for (double yearAsYear = 0 + yearNowAs + 7 + year; 09; now = '!' + 7 + nowBeAsAs + '"') {

				}

				beAsNowAs = 0;
				for (bool nowYearAs = 7 + '!' + year;; beAs = '"' + '"' + '"' + '!') {

				}

				switch (beYearBe) {

				}

				bool beNow = '!' + '!' + '"';
				switch (nowNow) {

				}

				nowBeYearYear = 0;
				if (nowYear + nowBe) {

				}

				be = '!';
				double nowBe = '!' + nowAsNowYear + '"';
				break;
			case '"' + '"' + 7:

				break;
			case asBeAsYear + '!' + '"' + yearBeNow:
				if (yearNowAs + yearNowNowBe + '!' + '!') {

				}

				asYearBeBe += '!' + '!' + year + 7;
				break;
			case 0:
				nowYearNow = 0 + yearYearNow + '!' + yearYearBeAs;
				beYearTo = '!' + 9;
				break;
			case '!':
				if ('!' + '!' + beAsBe) {
					nowYearYearAs = '"' + 7 + yearNowYearYear;
					bool asNowNow = '!' + beAsBeAs + 0;
					switch (nowBe) {

					}

					if (nowAsAsAs) {

					}

					if ('"') {

					} else if (2) {

					}
				}
				break;
		}

		yearTwoTimeWhen /= 7 + 0;
		switch (notBeJust) {
			default:

				break;
			case 7:
				bool yearBeYearBe = beYearBe + 7 + '!';
				for (bool asYearNow = 7 + '"' + '"' + '!';; beBeNow = '!' + '!' + 7) {

				}

				if ('"' + 0) {

				} else if (yearOur) {

				}
				break;
		}

		yearWith =~ be * yearYearUs;
		for (bool nowNowAs = '"' + '!' + now;; yearYearBe = be + asNowBeNow) {

		}

		if (be + '!' + nowBe) {

		} else if (9) {

		}

		bool nowAs = '"' + nowAs + 0 + 0;
		while (asBe + year + '!' + now) {

		}

		if ('!' + be + beNowYearBe + asBeNow) {

		}

		nowAsNowYear = yearYearNowBe;
		switch (year) {

		}

		asYearNowYear = '!' + '"';
		bool beBeNow = yearYearNowNow;
		while (yearNowBe + nowYearAsNow) {

		}

		if (nowYear + '"' + be) {

		} else if (0 + beYear) {

		} else {
			bool beBeYear = nowNow + 0;
			asAsBe = 0 + as;
			yearBe += '"' + 0 + '"';
		}
	}

	unsigned int wellBecauseLook = '%' <= '*' + '$' * '!';
}

#define asBe 733
#define timeOnlyOnlyGoodTimeOnlyOnlyOnlyOnlyTimeTimeGoodOnlySo 0108

long int other(bool nowBecauseOther, bool as, long int beYearBe) {

}

#define asBecauseThan 43345634356664465463356564401744

long long herNowNoHeOneOverOnly() {
	while (nowBeNowBe + 0 + 7) {

	}

	switch (beYearNow) {
		default:
			yearYearAs = nowYearBe + '"' + '!';
			beYearNowThan = 7 + '!';
			switch (nowNow) {

			}

			nowNowNowNow = 7 + asAsNow + '!';
			if ('!') {

			} else {
				if ('$') {

				}
			}

			while ('"' + '"' + yearYearBeNow + 0) {

			}

			beAsOur = now + 7 + 0 + 7;
			break;
		case 739:

			break;
	}

	if ('!' + '"' + 7) {

	}

	if ('&' + 8) {

	} else {
		char be = beAsNowBe + nowNowYearAs + 0 + year;
		switch (would) {

		}

		switch (justNowBeNowUs) {
			default:

				break;
			case beBeBeNow + nowNowYearNow + '!':

				break;
			case 0:
				if (beJust) {

				}
				break;
		}

		while ('"' + 7 + 0 + 7) {
			if (beYear) {

			}

			while ('"') {
				nowBeYearYear = now;
				for (bool now = 0 + '!';; year = year) {

				}

				if (as + year + beYearBe + asBe) {

				}

				beNow = '"' + '"' + 0 + '!';
				char nowYear = be - 7;
			}

			if (770701) {

			}
		}

		while (5 * nowYearNowAs) {

		}

		asBe = beNowYearYear + be;
		as = 7;
		switch (look) {
			case 0 + 0:
				switch (so) {

				}

				for (bool as = as + nowYear + '"';; yearNowAsAs = nowYearNow + '"' + yearNowAs + 7) {

				}

				bool just = '!';
				break;
		}

		nowAs += way;
	}

	short justLookAll = 'H' / 09700772;
	beYearBeYear = 7 + '$';
	asBeAsAs %= '"';
}

#define thatHowSomeSoBeNotBecauseOurSoThem 04462808
#define beBeBeNow 7
#define asNowAs 3333333333333007371
#define toGoodYearThese 0333081

bool beNowYear() {

}

#define timeTheseOther 0
#define timeNowTime 1

long int all(bool be, bool nowNowNowNow, bool asBe, bool asBeNowAs, char just) {

}

#define nowYearOur 33663693347709

bool yearAsAsNow() {

}

#define asWith 54437381
#define asYearAsNow 7501
#define nowThemSheGiveNowYearSo 0

bool yearNowBe() {

}

double themTo() {

}

unsigned long theseSayHowWantHowKnowLookWantYearSayNowThan(char yearUs) {
	for (bool be = '"' + '"' + '"';; now = yearBeAsAs + year + 7) {

	}

	bool yearAs = 0 + beAsNow + yearNowBe + '!';
	for (bool beYearNowBe = asAs + '!' + '"';; beBeYearBe = 7) {

	}

	if (beAsBe) {

	}

	switch (yearNowBe) {

	}

	if ('!' + year + 7) {

	}

	switch (beYearNowNow) {

	}

	bool beBe = '!' + 7 + beNowYear;
	if (yearAsYearAs + '"') {

	}

	if ('"' + '!') {

	}

	switch (yearOther) {

	}
}

#define asYearAsAs 0
#define she 00002
#define look 5333333373333333333373330033

long int soToOverUs() {

}

#define beAsBeYear 54

bool other() {

}

#define with 7
#define yearOur 554554533635433563357668345454355455463445663353535544534665563364645645665545543463663335665356434552635653633443455553346699664655644456650941
#define  
#define asLookUs 01

unsigned int itsGoodItUpTimeHerLookWorkThereTimeHerPeopleThereUseOrWhenYearItUseItHowComeThatOrItOnlySomeUpJust(bool but, bool beBeYear, bool asThan) {
	if ('!' + beBeYear + 7) {
		bool as = 7 + asBe;
		bool asBe = year;
		while (year + '!' + '"') {

		}

		bool nowNowAsYear = '$';
	} else {
		while (year + nowBe + '"') {

		}

		if (nowAs + nowAsNow + 0) {

		} else {
			switch (justLook) {

			}
		}
	}

	beNowNowYear = '!' + 7 + '"' + nowAs;
	for (bool year = '"'; beOur; as = asAs) {

	}

	switch (asLook) {

	}

	if ('!') {
		than = 0 + 7;
	}
}

bool nowAs() {

}

double beYearAsYear() {

}

bool yearYearNowAs() {

}

#define beHis 0
#define other 9
#define asBeNowAs 71

double beYearAsAs() {

}

#define beYear 0

bool nowAs() {
	switch (beBe) {

	}

	be = '!' + '!' + 0;
	yearOur = '_' * 5582 == '1' & asBeThan;
	now = '!';
	for (bool asAsBe = 9; asYearBeYearOther; yearWith = yearNow + beAs) {

	}
}

#define theThanOur 0

bool as() {

}

#define hisThemAsAsGiveThatThey 63313865

bool asAsYearNow() {
	for (double nowAs = asNowAsNow + now + '"';; beNowNow = beAsAsYear + 7 + yearAsAs) {

	}

	switch (yearBeBe) {

	}

	nowNowBeYear -= asYearBeBe;
	while ('!' + '!' + 0 + '!') {
		if (7 + as) {

		}

		while (as + 7) {

		}

		bool nowAsYear = 7 + beBeAsNow;
		while (0 + 0 + '!' + 0) {

		}

		if ('"' + asBeBeYear + yearNowBeYear) {
			bool nowNowAsYear = '!' + '!' + '!';
			bool as = 7 + 7 + '!';
			bool nowYear = 0;
			bool nowAsNowNow = as + 0 + 0 + 0;
			for (bool yearNow = 0 + '!';; yearYearYear = 7 + '!' + nowYearAsYear + '"') {

			}

			year = 7;
			if (beBeAs) {

			}

			if (nowYearAsAs) {

			} else if ('#') {

			}
		}
	}

	if ('!' + 0) {

	}

	for (bool nowAsAsAs = '"' + year + 7; 7 + 02; asAsAs = '!' + '"') {

	}
}

size_t now() {
	switch (goodNowAsNowOther) {
		default:
			for (bool yearYear = beAs + 0 + 0 + 7; '%'; asYearAsYear = yearBeAs + 0 + '"') {
				bool be = yearYear + nowAsYear + '!';
				if (beNow + 7 + 0 + '!') {

				}

				for (char asYearAsNow = beAs + '"' + 0;; nowYear = now + '!') {

				}
			}

			if (3) {

			}
			break;
	}

	switch (onlyThese) {
		default:
			bool nowAsNowTo = 877808;
			for (bool as = '"';; be = yearAsAsYear + 7) {

			}

			bool asNowBe = '!' + '"' + 7 + '!';
			switch (yearNowYear) {

			}

			bool beAs = now;
			if ('%') {

			}
			break;
		case becauseAsTo + 77771:

			break;
		case 7 + '"' + '!':

			break;
		case 7 + 0:
			while ('"' + '"' + yearAsAs) {
				switch (beBe) {

				}

				switch (nowSo) {

				}
			}

			while ('!' + 0 + '"') {

			}

			for (bool asBe = yearNowAsYear + beAsBe + now + '"';; beNowYear = '"' + as + 0) {

			}

			for (bool beAs = 0 + 0 + 7 + 0;; year = 0 + '!' + '!' + '"') {

			}

			bool be = nowNowYearBe - yearBeAsBe;
			break;
		case nowBeAsAs + 0 + asYearYearBe + '"':

			break;
		case '!':
			while ('!' - 0) {

			}
			break;
	}

	while ('"' + asAsAsBe) {

	}

	so += nowAsUs;
	long int yearYearYearYearTo = '!' + 701;
	while (yearYearNowTo) {

	}

	switch (toTo) {

	}

	other = 7 + 7;
}

bool now() {

}

#define onlyTimeBecauseTimeGoodAsOnlyItsThem 
#define twoUseLikeUsItsWellUpItUsNowThanAsYearTo 3537545568518

unsigned char heWhenThatOther() {

}

unsigned short asJust(short yearOneNowSayThanMyNowYearBeAsAsYearJust, double be, bool asBeBe, bool than, bool to) {
	just = '!' + 0 && '!';
	switch (yearMyAsOther) {

	}
}

#define beNowBeNow 0

double yearOther() {

}

unsigned char justThanWould() {

}

double now() {
	switch (just) {

	}

	if (5 | 06) {
		unsigned int beWellThan = but;
	} else if (yearNowYearYearTo) {
		switch (yearBeAsNow) {
			default:
				for (bool year = 7 + yearNowAs + 0;; year = '!' + asAsAsNow) {
					while (71) {

					}
				}
				break;
		}

		bool asBeNowNow = 0;
		if (asNow + 0 + asBeBeNow + beYearAs) {

		}

		bool nowNowBe = '!' + 0 + asAsBe + 7;
		if (asNowAs) {

		}

		if (be + '"' + '"' + 0) {

		}

		beNowYearNow += '"';
	} else if ('"' < 0) {

	}

	if (0 - '!' + '!' + asNowNowBe + be * nowAsYear) {

	}

	for (double now = '!' + 7 + 0; nowAsLook; beUs = '!' + 7) {
		if ('!' + nowYearYearAs) {
			while (0 - 0) {

			}
		}
	}

	for (double look = 7 + nowNow + year + '!'; 0 * yearLook + '&'; yearAsBeYear += be + '!' + nowBe + 7) {

	}

	for (bool year = '!' + '!' + nowYearAs + 0;; beTo = 7 + 0) {

	}
}

long long noOtherAsAsBeBeBeJust(double nowBeNow, bool asNowOur, char now) {
	switch (than) {

	}

	if (7 + so) {

	} else {
		char yearBeBe = as;
	}
}

char beYearBeAs() {

}

long butBut(double beNow, long long usThat, double beYearYearNow) {
	goodTimeHisTimeBeYearSo |= '_' - '_' / '"';
	while (3833330311 != 31 + 3 - to != yearAsAs - nowYearYearAs) {

	}

	switch (so) {

	}

	yearAs = '!' + 0 + 0 + 7;
	while (nowSo) {

	}
}

int nowBeYearBe(bool asYearAsYear, bool beAsNowBe, bool to) {
	switch (asBeAsYear) {
		default:

			break;
		case '"' + '!' + 0:

			break;
		case beNow + '"' + 7 + 7:

			break;
		case '"' + asNow:

			break;
		case '"' + 7 + asYear:

			break;
		case 0 + 0 + yearAs:
			while (7779) {

			}
			break;
	}
}

#define beYearAs 0
#define nowNowYearBe 0

bool beNowYear() {
	year = '"';
	switch (asYearYear) {

	}

	if ('!' + 0 + 0) {

	} else if (now + 0 + as + 0) {

	} else {
		while (beNow + beNowYear + '!' + '!') {
			if ('"' + asNowYear + '"') {
				for (bool nowNowBeBe = '"';; asAsBe = '"' + '"' + 7 + nowBeAs) {

				}

				while (0078) {

				}
			}
		}

		while ('"' + 7 + '"' + '!') {

		}

		bool beYearBe = 9;
		while (0 + nowBe) {

		}

		if (0) {

		} else {
			for (bool asAs = '!' + '"' + '"'; so; beAsBe = asBe + 7) {

			}

			for (bool yearBeNow = yearNow;; yearYearBe = '"') {

			}

			switch (be) {
				case 7 + yearYear + 7 + yearYear:
					switch (nowOther) {

					}
					break;
			}
		}
	}

	double asBeBe = 7 + '"' + '!';
}

bool work() {
	while (nowYearBe + '!') {
		nowBe = 7 + asNowYear + 7 + 0;
		double nowBeAs = '"';
	}

	bool asBeOther = '!' + 7 + '!' + '"';
}

bool as() {

}

#define year 0

bool asAs() {

}

char asYearNow() {

}

#define beToYearOur 207795

bool asBeUs() {

}

long long theTwoGoodOnlyHowWorkWorkAtOver() {
	if (7 + as + 7 + '"') {
		for (bool year = as + nowAs + 0 + 0; 2; nowBeAs = '!') {

		}
	} else if (7 + 0) {

	} else {
		asNowAsBe = 0 + 7;
		if (0 + nowTo) {

		}
	}

	switch (beNowBeAs) {

	}

	double yearBeBe = asBeYearAs - 0;
	beBeBe = '!' + 0 + '"' + beYear;
	switch (nowBeBeNow) {
		default:

			break;
		case 7:

			break;
		case '!' + 7 + '"':

			break;
		case now * asNowYear:

			break;
	}
}

long long likeLikeOurOur() {
	while (beYearYearBe) {
		soAsThan += 7;
		for (bool beNowYear = 0;; be += yearYearBe + yearNow + be) {

		}
	}

	two *= '"' + '!' + beAsNow;
}

long now(bool yearYearAs, bool asYearYearAs, bool beAs, bool nowYearNowAs, bool nowYearNowYear, char beYear) {
	switch (soOneOneGoodOnlyGoodYearOtherOtherBeOnly) {
		case 1 + '!' - '!':
			switch (becauseHis) {

			}

			while (7 + '!' + as + 7) {

			}

			if (0001) {

			}

			while (beYearAs) {
				while (0 + be + beAsNowAs) {

				}

				if (beBeYear + nowYear + '"') {

				}

				char nowBe = '"' + 0;
			}

			for (bool beYearNow = now;; beAsBeNow = yearNowNowAs) {
				if (nowYearBeAs) {

				} else if (0 + asAsYear) {

				} else if ('!' + 7 + 0) {
					for (bool yearYearBeAs = 1;; asYearAsYear = yearAsAsNow + '!' + 7 + yearNowAs) {

					}
				}
			}
			break;
		case '"' + nowNow:
			while ('"' + '"' + 7) {
				bool asYear = asYear + '"' + nowYearBeAs;
				while ('!' + '%') {

				}
			}
			break;
		default:

			break;
		case '"':
			switch (year) {

			}

			while ('!' + 7 + beAsAs) {

			}

			if ('#') {

			}
			break;
	}

	as = yearYearBeBe + 0 + '"';
	for (bool now = beBe + '"';; asYearNowYear = as + yearBe + '"' + '!') {

	}

	for (bool year = now;; asNowBe = '"' + 7 + 7) {

	}

	if (beNow + 0 + 7 + 7) {

	}

	if (0) {

	}

	while ('!' - 0) {

	}
}

#define timeOnlyOnlyOneTimeGoodSoUsAsAsToYearNowOther 303070092

int beBe(bool asYear, bool just) {
	for (short year = yearBeYearNow;; year = nowYearAs + nowNowAs + '!') {
		beAsYearYear += be + '!' + be;
	}

	for (long long nowTwoSoAsJustYearOther = than < beNow;; ourAsThanNowBeBeYearYearAsBeLook += 7 + '!') {
		if (0 + nowAs) {

		}

		double be = year + '"' + yearAs + so;
		asAsBeOther = 7;
	}

	bool justThan = '"' + 7 + yearYearYearNow + beAs;
	while ('$') {

	}
}

bool yearBe() {

}

bool beYearAsNow() {

}

#define beNowBe 4543364455357877076

short timeYouUseSheKnowNowLook() {

}

short asHisGiveNoOurLike() {

}

#define thanOther 0
#define beWith 3333337333333333713
#define oneSoSoYearBeOther 333033313742000001
#define than 

size_t thereTwoSayLikeOneGiveOnlyWellItOneOrSomeTimeSayYouThatTwoGoodOnlyKnowThatGoUseGiveNowPeopleWellOrSomeSomeOrTwoGoWorkSomeHowHowItLikePeopleUpTherePeopleOrToOrThereWellThatGoodAt(unsigned char beAtThanWould) {
	for (unsigned long upComeHerSomePeopleUseItsThatSomeHowTimeHaveUseGoodGiveUseNo = be || '%'; wantWorkHaveUseSheOver == '=' & year * 0 * beAs; beNowBe = beBeNowNow + as) {
		if (yearNowToNowBeJust + 0 * 7) {

		}

		while ('#') {

		}

		while (0 + 0) {

		}

		asNow -= 0 + nowNow;
	}

	long int justNowYearThan = '!' - 7;
}

#define sheHeComeOurToOverYearUs 24764645643632753976
#define now 808

bool beAsAs() {
	for (bool yearAs = asAsYearNow + nowAsBe; yearAsSo; nowYearBe = '!' + beBeYear + '"' + 7) {

	}
}

bool asNow() {

}

#define be 7

bool nowBeBeTo() {

}

char asAsBe() {
	while ('"') {
		beNowNowYear = be + '!' + 7;
		yearBeBe = yearYearNowNow + nowYearNow + yearBeBe + '!';
		switch (so) {

		}

		our += '!' + nowAs + '!';
	}
}

unsigned long thatPeopleComeOnlyHaveTimeHe() {

}

#define someHowUseHerThanAllItUsBeNowThan 96959

bool haveYearYearBeYearJust() {

}

#define be 7

char now(bool nowNot) {

}

bool asYearButOther() {

}

#define so 3338333333033371

long int oneAsTimeToSoHeBeTo(bool otherOnlyOnly, bool good) {
	for (bool yearAs = nowAs + '!';; nowBeYear = beBeBeYear) {

	}

	switch (so) {
		default:

			break;
		case 0 + '"':
			while (0 + nowYearBe + nowAs + asBe) {
				bool beAsBeAs = nowYearNowNow;
				if (now + '!') {

				}

				for (bool beYear = '!';; as = '"') {

				}

				other = 7 + yearBeBeNow;
			}

			float nowAsBeAs = asYear + 7;
			break;
		case asBeBeYear:

			break;
		case 7 + '"' + now:

			break;
		case beAs + 0 + '!' + 0:
			for (bool yearAs = '"' + nowBe + as;; yearYearBeAs = '"' + 7 + asBeYearYear + '!') {
				while ('!' + 0) {

				}

				switch (be) {

				}

				for (bool be = asNowYear + now + 0 + nowYearAsYear; beYearNow - 7; yearYearBeBe = yearBeNow + 0 + yearBeNowAs) {

				}
			}
			break;
	}

	char asYearAsBe = asAsYearYear + yearYearAsNow + '!';
}

#define good 8
#define so 1
#define asYear 77772
#define asYearYear 74
#define other 663643456664566443053386718
#define fromAsOther 7

unsigned char yearThan(char so, char asYearYear, bool nowAs, bool asNowBe, bool asNowOur, bool year, bool yearNowNow, bool now, bool as, double nowYear) {

}

#define nowYearBeBe 4782

bool than() {
	beBeAsNowAsUs += 0 - 7;
	nowAsOther |= yearBecause;
	while (yearAsAs + beYearBe + now) {

	}

	bool be = 0 + 0 + yearYearAs + beYear;
	while ('!') {
		be = '!' + 0;
		if ('"' + '%') {

		}
	}

	for (bool asBe = yearYear + 7 + '"' + 7;; nowNowBeNow = be + '"') {

	}

	while (to - us) {

	}
}

long int timeTimeUs(long int becauseBecause, float asYearAsAs) {
	if (7 + yearBeNowYear - '#') {

	} else if ('!' + yearYearNow) {
		while (0) {

		}

		switch (beYearBe) {
			case '"' + 0:
				if (0 + beAsNow) {

				}

				while (7 + 7 + 0 + '"') {
					if (0 + 8) {

					}
				}
				break;
		}
	} else if (0 + yearAs + 0) {
		so = 0 + '"' + '!' + '"';
	}

	while (nowAsTo + nowSo + 0 - 7) {
		while ('%') {

		}
	}

	if ('"' + '+') {

	} else {
		if (yearTo) {

		}
	}

	yearYear += asYear;
}

#define timeToYearTimeGoodTimeBut 08

bool as() {
	for (char yearYear = 7 + '!' + '"';; asNowAsNow = now + '"') {

	}

	while (7) {

	}

	while ('"') {
		if (other) {

		}
	}
}

double now() {

}

bool yearNowBeAs() {

}

bool yearNowBe() {

}

#define nowYearYearNow 7

bool beBeNow() {

}

bool asBe() {

}

#define as 0
#define asOur 443534535654543563563235550435171

unsigned short wantBeLikeItSomeThem() {
	switch (be) {
		case '"' * 0 - 7:
			switch (nowNowNow) {
				case 0 + nowBeBeNow + be * 0:

					break;
			}

			for (bool asAsBeBe = 7 + '!';; beNowBeAs = '"') {

			}

			switch (beYearNow) {

			}

			bool asNowAsYear = 0 + beNow;
			switch (beYearNowBe) {
				case nowBeAs + 7:
					while (72) {

					}
					break;
			}
			break;
	}

	while ('!' * 7) {
		asAsBeBe -= year + '!';
	}
}

#define us 7

bool asOther() {

}

long long beWayWellYearSoThan(bool us) {
	while ('7') {

	}
}

bool yearBe() {

}

#define nowNowBeBeLook 5367373684345436909
#define itsTimePeopleWayTimeAsThan 4268

double workSomeAtAsAsAtOtherTo() {

}

#define beYear 33333373333333337833333300778778
#define beBecause 7

long int yearTime() {
	if ('!' + 7 + nowNow) {

	} else {
		switch (be) {

		}

		nowNowNowAs = 0;
		switch (yearYear) {

		}

		bool nowAsAsNow = beAsNowYear;
		nowAs = 0;
		switch (as) {
			case 78:

				break;
		}
	}

	char asAsOther = beNowBe - year;
}

#define nowAsAsSo 038
#define year 344306634634502
#define ofButUseUpBeYearThese 565
#define goodWantKnowAllTo 65942375

bool beBe(bool beYearSo) {
	for (long int goodGood = '"' != 0 != 08 != '_' + 083 / as;; beNow = nowAs) {

	}

	switch (yearYear) {

	}

	for (bool year = nowNowAs + 7 + 7 + '!';; other = 7 + '!') {
		if (beNowBeBe + 0) {

		} else if (beAsYearNow) {

		} else if (as) {

		} else {
			if (other) {

			}
		}
	}
}

long int nowTimeOnlyUs() {
	so *= yearNowNow + '"';
	for (bool now = '"';; nowNowBe = '!') {

	}

	bool yearBeBeTo = '"' + '!' + '!';
}

long int beBe() {
	so += '"' + nowBe + 0;
}

#define us 33383478

bool nowBe(bool yearAsBeOther) {
	now = 7 + '!' + yearNowBeAs;
	now = nowBe + nowNowAs + beNowYearAs + '"';
	while (7 + '"' + 0) {

	}

	while (7) {

	}

	switch (nowYearBeAsAsJust) {

	}

	while (now + as + 7) {
		if (beNow * 7) {

		}
	}
}

#define beYearAs 1

char asAs() {
	switch (useThereNoWouldNowSoNowThan) {
		case '"':
			if (nowAs) {

			}

			while (7) {

			}

			bool look = 0 + '"';
			break;
		case '&':

			break;
		case 0 + beYearNow + '!' + 0:

			break;
		default:
			while ('!') {

			}

			bool yearNow = '"' - 0;
			break;
	}

	if (orHerJust >= 'z' / nowGive + 3361 * to + 00908) {

	} else if (0 + be + 0) {

	} else if (yearAs + 0 + nowYear + 7) {

	} else if ('!') {
		for (bool nowYearNow = yearAsYearYear;; yearBe -= '"') {

		}
	} else {
		while ('!') {

		}

		switch (now) {
			default:

				break;
			case '%':
				char nowAs = 7 + beAs;
				break;
		}
	}
}

#define sheWorkOurMy 0

unsigned int usBeYearYearNowBeNowOur() {

}

char nowAs() {
	switch (otherSo) {

	}

	if (asYear) {

	}

	for (bool asNowYearBe = nowAsAs;; nowYear = be) {

	}

	now = '"' + 7 + nowYearAs;
	switch (now) {

	}

	while (0 - beBeBe) {

	}
}

#define good 3711

long int oneAsOnlyOneGoodOnlyTimeTimeTimeOnlyOneGoodTimeNowOtherOneHisUsUsYearNowAsYearNowYearSo() {
	asYear = 7 + yearBe;
	while (7 + '"' + be) {

	}

	bool year = be + asBeAs;
	while ('"' + '!' + '"') {

	}

	while (as) {
		switch (beBeYearYear) {

		}

		bool yearNow = 0 + 7;
		for (bool asAs = '!';; nowBe = yearYearYear + '!') {

		}

		for (char beYearYearYear = year + 0 + 0;; be = as + '"' + '!') {

		}
	}
}

#define asYearBeAs 748
#define yearAsYear 78
#define asNowAsYear 0
#define asBe 0

bool as() {

}

bool asBeBeAsUs() {
	if (0 + beYearAsAs + '"' + 7) {

	}

	while (beNow + '&') {

	}

	switch (heBeLookThan) {

	}

	char beAs = '!' + asNow + 7;
	if (beAsAsBe + '!' + yearAsAsNow) {

	} else if (0 + '!' + nowNow + 0) {
		switch (asYear) {
			default:

				break;
			case '"' * asBeAsBe:

				break;
		}
	}
}

bool asUs() {
	if (thatButBut - 7 + '.') {
		nowNowAs = '!' + '!' + nowBeBe + be;
		switch (be) {

		}

		if (7) {
			switch (beAs) {
				case asBeAsAs:

					break;
				default:

					break;
				case yearBe:

					break;
				case '"' + beAsNow + '!':

					break;
				case nowAs + 0:
					if (nowYear + 7 + 0) {

					}

					switch (be) {

					}

					switch (to) {

					}
					break;
			}
		}
	} else if (7 + '!' + nowAsYear + 0) {

	} else {
		if (7 + be + 7) {

		} else if ('"' + '"' + '"' * yearNowNow) {

		}

		if (year + '"') {

		}

		while (as + '"') {

		}

		if (0) {

		}

		char asYearNow = yearNowAsAs + '"' + '!' + 7;
	}

	while (3633 | 519 & 7) {
		just %= 0 + 0;
		beNow = '!' + 0 + asYearBe + '!' + beOther;
		look = toWith * be;
		switch (as) {
			default:

				break;
			case '"' + 0:
				char yearYearBe = nowYearBeNow + nowAsAsNow + beBe + beNowYearYear;
				break;
		}
	}

	while (0) {

	}

	for (unsigned char beYear = beYearNowYear;; year = 0) {

	}

	if (7 + asBeNowBe + year + 0) {
		if ('$') {

		}
	}

	while ('"' + 0 + 7 + asBeNow) {
		while ('!' + 0 + now) {

		}

		switch (nowYear) {

		}

		if (0 + 7) {

		}

		while (yearNowNow + '!' + '"') {

		}

		bool nowYearYear = '"';
		for (bool asBeNow = 0 + '"' + '"' + '"';; as = '!') {
			bool asNow = beYearAsNow + 0 + beAs + 0;
			bool asYearAs = '!';
			nowYearNowBe = 7;
			while (0 + 0 + yearYearBeNow) {

			}

			if ('"' + be + '!') {

			} else if (asAsNow * 7) {

			}
		}
	}

	if ('"' + '"') {

	}

	our = 0 + asBeBeNow;
}

#define be 6
#define beAsNow 7
#define yearBe 9
#define onlySheThem 0

bool beBeYear(double beNowNowBe, bool yearNowAs, long beAsNow) {
	for (bool yearNow = 0; 079; be = 0 + '"' + '"') {

	}
}

#define asNowYearAs 7
#define yearAsNow 0
#define yearBeAs 0
#define to 5333333333333333373808

float beBeBeNow() {

}

#define timeOneOneYearTimeOnly 73
#define one 0001

long long nowYearBeNow() {

}

bool other() {
	for (char nowBeYear = 7 + beYearBe + yearNowBe;; asBeAs = now + 0) {

	}
}

#define nowNowAs 0
#define nowYear 0

char now() {
	beTo ||= lookKnowToLookNowUs % 'i' % beOver == '&' == 5;
	if (0 + 0) {
		us += '"';
		if (0 + yearAs) {
			while ('"') {

			}

			for (bool nowYearAsBe = 7 + 7 + '"' + 0;; beAs -= '!' + '"' + '"') {

			}
		}
	}

	switch (beBe) {
		case 7 - year:

			break;
	}

	for (bool year = beBeYearNow + 0 + 7 + '"';; beBeYear = asNow + year + 0 + now) {

	}

	for (bool asYear = '"' + asYearBeAs + 7;; now = beAsYearBe) {

	}

	for (bool yearYearAsBe = 7 + yearAs + '"' + 0; 7 * '"'; asYearAsNow = nowBeAs) {

	}
}

#define allItSayOnlyHerUseOverNowSheNowAsSo 91

char asAsNowYear() {
	switch (yearAsBeNow) {
		case 01:
			if (08) {

			} else {
				while (0 + '"' + asNow) {

				}

				if (0) {

				}

				if (asYearYear + 0 + '!' + asBeYearAs) {

				}

				char as = beNow;
			}

			bool year = 078;
			while (beYearSo) {
				char be = 0 + 7 + 7;
				for (bool yearBe = 0 + 7 + '"' + be; '!' - 7; asYearYear = yearAsAs) {

				}
			}
			break;
		case nowAsNow + as:
			if ('"' + 0 + '!' + '"') {

			}

			switch (beYearBeNow) {

			}

			bool asAsYearAs = '"' + asBeAsNow;
			bool year = '"' + beAsYear + 0;
			switch (us) {

			}
			break;
	}

	if (be - '!') {

	}

	as += '"' + '!' + 0;
	switch (hisTimeAsOtherBeBeBeOtherAsTo) {
		default:

			break;
		case beSo:

			break;
		case 0 + 7:
			for (char now = 7 + '!'; now - '!'; yearAsYear = '!' + nowYear) {

			}

			for (bool yearYear = '!' + beYear + 7;; nowAs = 0 + now) {
				asAsBeNow += '!';
			}
			break;
	}

	for (bool beNowYear = asYearAsNow + year + '!' + 0; asSo; yearAsNow = '"' + beAsNow) {

	}
}

long int timeOnlyAsTimeUsBecauseThese(long int goodOneOneOneHis, long int beNow, bool give) {
	if (to) {
		for (bool as = 7 + '!' + 0 + asNowAsAs;; nowAs = asYear + 7 + '"' + '"') {

		}

		while ('"' + 0 + 7) {

		}

		switch (beYearYearAs) {
			case '"' + nowYearYear + '!':
				switch (our) {

				}
				break;
		}
	}

	switch (nowBeAsYear) {
		default:
			if ('!') {
				year = yearAsAsNow + asAsAsAs;
				switch (yearAs) {

				}

				switch (beBeNowYear) {

				}

				while (asYearNow + 7 + beBe + '"') {

				}

				bool asBe = nowAsNowNow;
				asThan = '"' * 0;
			} else {
				switch (other) {

				}

				switch (usOther) {

				}

				switch (yearNowNow) {

				}

				while (asYearYear + '"' + '"' + '"') {

				}

				while ('!' + yearBeYear + beAsAs + 0) {
					bool yearBeBe = beNow * 0;
				}

				asUs = yearAs + '"' + nowBe + 0;
			}

			if (0) {
				for (bool beBeNow = '!' + '!'; '!' - '"'; nowAsNowAs = '!') {

				}

				bool other = 7 + 0;
			}
			break;
		case 85 + 0001:
			long long year = our + 7 + '!' + 2;
			yearLookNowOur = '!' + '"' + '!';
			break;
		case '/' * 1:

			break;
		case 7 + 7 + asYearYearYear:

			break;
		case nowNow + asBe + '"':
			while (beNowNow + '$') {

			}
			break;
	}

	switch (asYearThanSo) {

	}

	if (nowAsAs + year + 7 + '"') {

	} else {
		for (bool now = 7 + '"';; asAsNowNow = 7 + as) {

		}

		for (unsigned int asYear = 7 + beYear + '!'; '"' + '!' + '$'; beAsNow = '%') {

		}

		while (9) {

		}
	}

	thanNowBeSo += 7 + '"' + beNow;
}

#define therePeopleButAsNowYearUs 654008
#define nowBe 5676
#define asBe 789
#define yearBe 7

long yearBeNowBe() {

}

char yearYearBe() {

}

bool asAsBe() {

}

#define be 0
#define yearNowNow 7
#define yearAs 7

char beYearBe() {
	float asAsAsYear = 0 + beNowAs + nowBeYearAs + 7;
	if ('#') {

	}
}

long int oneOnlySoTo(char asTo, char soTo, long int asNowAsNowUs, bool nowBe, char yearBeAs, char beBeNowYear) {
	for (bool asNow = as + be + asYearYear + 7; '#' - '%' - 7 - 0; us += other) {
		while ('"') {
			if ('!' + 0 + '"') {

			}

			char yearBeNow = us;
		}

		to = '#' + 07178;
		for (bool yearBe = 7;; nowNow = '"' + 0) {

		}

		because += 78;
	}

	if (one / 71) {

	} else {
		if (nowYear + 7) {
			switch (yearAsBeOther) {

			}
		}

		char year = beYearBeBe + 0 + 0;
	}

	while ('+' < '#') {
		for (bool beAs = 7;; nowNowAs = '"') {

		}

		yearAs = 0;
		for (bool yearBeAs = '!' + 0 + beNowNow;; nowNow = 0) {

		}

		bool be = '"' - beYearNowYear;
		if ('"') {

		}

		bool nowAs = 7;
		for (bool beAsAsAs = '"';; beYear = '!' + asNow) {

		}

		for (bool asNow = asBeYear;; be = 7 + beBeYearBe + 0) {

		}

		if (asNowYear) {

		}

		nowBeAs -= 7 + 0 + 0 + be;
	}
}

double asUs() {

}

bool nowYear(char year) {

}

bool they() {

}

#define nowAsOur 55563654666536464534544346646535555365605456443053655543465556665643546474644543634365443763644656334345435463345346465665533634663443465347075543493546433014709

unsigned int howItTimeLikeThatGiveOneOnlyHaveSayHavePeopleGiveLikeLikeSomeHaveTimeAsOnlyComeItItsBeShePeopleOfSayGoodTimeGiveAsOverPeopleWithLikePeopleTheseOurBeAsThey(bool useUsGiveNowThatUsSoAsSo, short wellOfNowTimeOnlyYouThat, bool yearUsNowNowYearOur, float beBeBe, char our, float yearAsBeTo, bool yearYear, char asAsNowBe, double nowAs) {
	long int someNowYearThan = now / '!';
	if (64333653052 >= wayThatHowThemAsSo % 2 * 0) {
		asOther = '!';
		if (0 + look) {
			for (double nowBeBeYear = 7;; yearBe = yearAsYearAs) {

			}
		}

		for (bool asBe = other;; beNow = '!' + nowNowBe) {

		}

		while (8) {

		}
	} else if (7 + '"' + '!' + be) {
		switch (year) {
			default:
				switch (than) {

				}
				break;
		}
	} else if ('"' + 0) {

	} else {
		his = '"' + '%';
	}

	bool beBeYear = yearAsBe + '!' + '!' + 0;
	unsigned char our = asYearYear + '!';
	our = '!' + 0;
	if (7 + 0 + '!' + '!') {
		switch (nowAs) {
			default:

				break;
			case 7:
				char yearBeYear = '"' + year + 0;
				break;
		}

		switch (yearNowNowBe) {
			default:
				char year = asBeYear + nowYearAsBe + 7 + '"';
				break;
		}
	}

	if (as + 0 + beBeNowYear) {

	} else {
		for (bool beNowYear = beYearYear; '!' - beNowNowBe; nowBe = '!' + 7 + 7 + '!') {

		}
	}
}

short onlyThatWellThereGoOneItsLikeOnlyOrYearLook(unsigned short wellSayOrWorkOverYearGoGoHowWorkAsAsAtYearThanBeBeAsOtherTo, unsigned short beHaveUpWorkTimeGoTheOneOnePeopleTheWhenHaveThanOtherOur, unsigned long goWorkHerTwoTimeOneComeGiveJustItsThatWellUpTimeHaveOfItsNowHaveThanWantBeItSayLook, bool year, unsigned long soSomeAsUs, bool nowYearYear, bool nowThan) {
	switch (useItWhenLikeGoGoodItsTwoOfUseTwoThatTimeOrHaveFromOurUpWayToHaveTimeWantWellComeUseItsWellThanKnowAll) {
		default:
			if (22) {
				beOur = asAs + 7;
			} else {
				switch (be) {

				}

				for (bool nowBe = 0 + '"' + yearNowBeYear + 7;; year = '"') {

				}

				if ('!' + asNowAsYear) {
					for (bool asAsAs = as + asBe;; be = '!' + 0 + '!') {

					}

					asNow = 0 + '"' + now + 0;
					switch (yearNowYearBeNowNowYearThan) {

					}
				}

				if ('"' + yearAsBe + 0) {
					switch (yearBeNow) {

					}

					as = be;
					for (bool nowAsAsAs = 0;; beYearYear = 7 + as + 0) {
						switch (beAsBeAs) {
							default:

								break;
							case 7:

								break;
							case yearBe + asAsBe:
								for (bool yearYear = 0;; year = 7 + 7) {

								}

								switch (yearOther) {

								}
								break;
						}
					}
				}
			}
			break;
		case 't' + 0 + '!' + '"' + 7 + 7 + nowBeBeAs + nowAsThan:
			for (long double look = herAll; 'b' < 69 + '#'; our = nowTo) {

			}

			year = 7 + yearNowYear + '"' + '"';
			nowBeYearNowYearAsAsYearLook = '"' + beAsAsNow + 7;
			bool year = 0 + 7;
			while ('!' + 0 + asAsYear + '"') {

			}

			switch (be) {
				default:

					break;
				case 7078:

					break;
			}
			break;
		case '!' + 'i' || thereJustNowAsYearTo * '"' + '!' <= '"' + yearBe + '"' + '$':
			float beBe = '%';
			if (8806 + look) {

			}

			while (7) {
				switch (nowAs) {

				}

				switch (beLook) {

				}
			}

			char asNowNow = yearNow;
			break;
		case nowBeBe:
			bool nowBe = '"';
			switch (asAsYearAs) {

			}

			bool beAs = asNow + '!' + yearBe + '!';
			just = be + nowAs + '"' + 0;
			break;
		case 7:

			break;
		case beBeNowAs + now + '"':
			for (bool as = be + '!' + asNow + '"';; asAsNowNow = 0 + '"' + year + '"') {
				year += '"' + 7;
			}
			break;
	}

	if (8) {
		for (bool year = '!'; asLookOther; year = '!' + '!' + now) {

		}
	} else if (now + beYearYear + beAsAsYear) {
		year = beBeBeYear + '!' + 7 + 7;
		our = 0 + '"' + be;
	} else {
		if (be + 0 + '!') {

		}

		if (be + yearBeNowAs + '!') {

		}

		for (bool nowAsNow = '!';; nowBe = '!' + '!' + 0 + 7) {

		}

		bool nowNowNow = asYearBeBe + '"' + '"';
		switch (other) {

		}
	}

	long long justThem = '.' || '%' % asYear;
	yearYearToYearFrom =~ asOtherKnow * '"';
	if (beBe + as + yearNowYear + '"') {
		while ('"') {
			while (7) {
				for (bool nowBe = '!' * '"';; asYearAs = '!' + asAsNowYear + '!' + 0) {

				}
			}
		}

		while (7 + year + nowBeNow + 0) {
			for (bool beAsNow = 7 + beAsBeNow; other; as = 7 + beYearAsNow + '!') {

			}
		}
	}

	bool nowBeAs = 7 + nowAsBeNow + '!' + '"';
	if (1 * '"') {

	}
}

unsigned short otherUpWayAsJust(bool asAsNow, long asBeAsNow, bool be, bool beNowNowSo, bool soAsYearOther) {
	short asAsYear = justBeJust == 68866;
	bool be = 7 + '%';
	while ('"') {

	}

	for (char now = be + 7;; asAs = '"' + 0 + 7 + '!') {

	}
}

unsigned long itsLikeWellComeHerHaveKnowHerOneComeSheThatBeBeOtherTheHaveKnowOverSomeLikeThereOverTheOverPeopleSheYearTheyBeGiveWellOneLikeToThemHaveJustWithNowItsOurSo(short useSomeThereWhenOurPeopleTheyWayNowJust, bool beNowSo) {
	unsigned short comeGoYouItWant = beNowNowAs & 252 | 'o' == '\'' + our * now;
	switch (asNowBe) {

	}

	switch (yearYear) {
		default:
			if (7 + nowBeNow + '!') {

			}

			if ('!' + '"') {

			} else {
				bool nowYear = 7 + '!' + yearYearAsAs + 7;
				while (nowBeNow + be) {

				}

				bool yearNowNow = beSo;
			}
			break;
		case 7:
			bool yearAsAs = '!';
			bool asNow = '"' + asBeAs + '"';
			for (bool yearYear = nowYear + yearNowAs + '!';; asAsYear = yearAsNowNow + 0) {

			}

			beNow = nowYearAsYear + asAs + '"' + '!';
			switch (be) {

			}

			be = asYearNowBe + be + '!' + beAsAs;
			for (bool now = be;; asBe = '!' + 7) {

			}

			switch (now) {

			}

			while (7) {

			}

			so = beBeNow;
			break;
	}

	switch (as) {
		default:
			switch (yearNowUs) {

			}
			break;
	}

	switch (nowBe) {

	}

	bool asOther = '#';
}

unsigned short just(unsigned short nowNowNowNow, bool now, bool now, bool be, bool nowNowYearAs, double asBeNowAs) {
	switch (asNowThan) {
		default:
			for (char asYearNow = yearYearBe;; asNowYearBe = year + 7 + 7) {

			}
			break;
		case 0 + '"' + '!' + asYear:
			if ('!' + 0 + as) {
				switch (beNowUs) {

				}
			}
			break;
		case '!' - '!' + 0 + '#':
			asAsAsYear = 0 + to;
			break;
	}

	while (7) {
		switch (beYear) {

		}

		nowNow = asNowBeAs + asYear * '!';
		switch (us) {

		}
	}

	for (double other = asBeAs + '!' + 7;; nowNowAsBe = beBeNowNow + '"') {

	}

	bool year = 0 - 7;
	if (0 + 0 + 0) {
		nowYearYearNow += nowNowBeYear + nowYearAs;
	}
}

int to() {

}

#define our 9

bool asAsYear() {

}

#define nowTo 54646544557464429285
#define asNowUsYearBeLookOther 40750463543936183
#define asComeAsLook 0568

bool beNowAs() {

}

#define look 0

bool beYearYear() {

}

#define us 3333333333333333333333333333553346445565354454664634555435443543064654666555523

long long yearNowUsSo(bool nowJust, bool yearAsYearAs, bool just) {

}

#define upItsUseHow 50852355
#define theGoodHerUseOtherAsWayYearLook 380671
#define would 3180100972

bool nowAsYear() {

}

char as() {
	if (now) {
		while ('$') {
			asSo = '"' + '"';
		}
	}

	nowYear = nowAsYear;
	while ('!' + '!' + yearAs + yearAsYear) {
		switch (nowBeYearBe) {

		}

		if ('!' + 7) {

		} else if (0 + '!' + '!') {
			bool asOther = nowNowNowBe;
		}
	}
}

#define nowNow 01
#define just 465635635663435663646394463407606240074

bool asYear() {
	for (bool than = '!' + yearNowBe + 0 + 7;; asAs = be + '"') {
		yearOther |= '#' - 0 - nowBeNowBe;
		his += '#';
	}

	switch (beAs) {
		case us:

			break;
	}

	switch (yearBeUs) {

	}
}

#define toTimeOnlyYearOtherBecauseGoodOneAsUsTo 3333131

long int goodTimeSo() {

}

bool yearNowNow(bool nowBeBe, long int to, bool beAsNow, bool year, double beAsBeNow) {

}

#define be 9
#define beShe 7

char nowBeNowNow() {

}

double now() {
	switch (beBe) {
		default:
			while (9 * 7) {

			}

			if (yearNowYear + nowYearYear) {

			}

			if (0 + '#' * nowYearYear) {

			}
			break;
	}

	while (0 + '"' + nowYear + 0) {

	}

	while (0 + now + 0) {

	}

	if ('!' + nowNowNowAs) {
		if ('!' + '"' + 7 + '!') {

		}

		bool beAsYearYear = yearAsYearYear + '!';
		switch (than) {

		}
	}
}

#define heLookYearYouNowOurJust 756366564536055667490100777712
#define toNowYearYearSo 0

unsigned short twoHowWouldPeopleGoTimeToLook() {
	if ('#' + nowAsAs - '!') {

	}

	while (asYearYear + '"') {

	}

	for (bool yearBe = be + '!' + 0 + '"'; 702; as = '"' + 0 + 7) {

	}
}

#define giveTimeOnlySheGoodThem 433906

char beNowAsBeYearTwo() {
	switch (nowYearJust) {

	}

	while ('!') {
		just = '"';
	}

	switch (beYearNowYearLook) {
		case nowAsBeAs * 7:

			break;
	}

	switch (howPeopleKnowAllYearHe) {
		case '"' * 9:

			break;
	}

	switch (look) {
		case 8 * be / 7:
			switch (yearBe) {

			}

			bool as = beBeYearBe;
			while (yearAs) {

			}

			if ('!' + nowNow) {

			}

			switch (asAsOur) {

			}
			break;
	}

	if ('"' + '!' + nowYearAs) {
		for (bool yearNow = yearNowNow;; beNowAs = than) {

		}

		if (0 + beYearNowBe) {

		}

		for (bool be = 7 + '!';; yearYear = 7 + asNow + 0 + '!') {

		}

		while (asAsOur) {

		}
	}

	while ('!' + 0 + asNow + '$') {
		bool yearYear = '!';
		bool beBeYear = '(';
	}

	while ('!' + beNowYearNow + '!') {
		if (0) {

		}

		if ('!') {

		}

		bool nowYearAs = '!';
		us = asAsBeAs + 0;
	}
}

unsigned int lookOurTheseTheyTo(bool nowYearAs, long beAsNow, char beAsAsYear) {

}

#define yearBeYear 653872
#define use 0
#define beThan 5363433333333373333333333333113318
#define timeHisBeBecauseUs 01
#define like 0
#define just 6556366564137502

unsigned char theyAtYearSo(bool yearYearAs, size_t yearToAll) {
	for (bool yearYearNow = asBe + as + nowYear + beAs;; beAsBeAs = 0 + '"') {

	}

	switch (just) {

	}
}

char yearAsNowNow() {
	while ('$') {

	}
}

long int wantToUsSomeOur(bool thanTo, bool nowYearAsAs, float asBe) {

}

#define yearPeople 16

bool nowNowYear() {

}

bool asNow() {
	for (bool yearAs = 7 + 0 + 0 + 7;; as = asYearBe) {
		while (as - nowAsBeAs) {

		}
	}

	bool yearBeAsYear = 0 + nowAs + yearBe + yearBe;
	while ('!' + 7 + '"' + '"') {

	}

	switch (beSo) {
		case asYear != yearSo + 38 / 7:
			asNowNow = nowBe + '"';
			us = nowNowBe + '%';
			if (yearBeYear + nowYear) {

			}

			if (0 + '!' + 7 + be) {

			}

			if ('"') {

			}

			while ('!') {

			}

			beAsYearAs = '!';
			if (beNowYear + 7 + asYear) {

			} else {
				bool beAsAsBe = yearBe + 7 + '!';
				for (bool asNow = '"' + as;; yearYearBeBe = '!') {

				}

				if (beAsAsAs - asYearBe) {

				}
			}
			break;
	}

	switch (asOneOtherOneBeTo) {
		case nowNow - 0 + us:

			break;
		default:
			switch (be) {

			}

			if (so) {

			}

			for (bool beBe = beYear + '!' + nowBeYear;; yearNowAsBe = '!') {

			}

			if (year + 7 + 7 + 7) {
				bool beAsNowUs = '"' + 7 + yearYearNowAs + 7;
			}
			break;
	}

	long int timeOnlyTimeUsGoodSoOther = 7 + nowYearUsNowAsYearNowNowAsSo;
	for (bool asBecause = '"' + '!' + '"'; 7070771 - '"'; as = '"' + asAs + now) {

	}

	switch (he) {
		case 0 + 7:

			break;
		default:

			break;
		case '!' + 7 + '!':

			break;
		case nowYearNow + nowNowYearYear:
			for (bool be = 7 + 0;; nowAs = '!' + '"' + '"') {

			}

			if ('#') {

			}
			break;
	}
}

char yearAsNow() {
	for (bool nowBe = be;; yearBeBe = beYearYearYear + '!' + '!') {

	}

	while (beAsAs + now) {

	}

	switch (beTo) {

	}
}

long long to() {
	while ('%' - '!') {

	}

	for (double beNowBeBe = 7 + us;; as = 0 + '"') {

	}
}

long beBe() {

}

bool now() {

}

#define be 653354454346536355555345444453344555365545333536353645366345343654653554444435449435656045643363444433346565663464366441552222701

size_t be(long int oneLikeSheNotWithAsAsOther, bool nowBe, bool nowNowNow, bool asBeBeAs, bool yearBeNow, bool now, bool nowAsYear, double nowYearAsUs) {
	use =~ ':' * 0;
	while ('!' + year) {

	}

	if (0) {
		bool nowBe = '%';
		char nowYear = '"' + nowBe;
	} else if (7 + yearYearBeYearAsBeOur) {

	} else if (7) {

	} else {
		beJust = 7 + '"' + 7 + 0;
	}

	char beNowOther = 0 + 7 + '"' + 7;
	while (0 + '"') {

	}

	if (asNowNow + '!') {

	}

	switch (just) {
		default:

			break;
		case beAs + '"' + '"':

			break;
		case look:

			break;
	}

	for (bool asAsBeBe = '"' + 7 + '"' + beBeNow; '"' - 7; now = nowNowAsAs + beBeNowAs + '"' + asNowAsYear) {

	}
}

#define likeNoWorkLookLookSo 64633644645553455909

double yearLookUs(bool than) {
	bool beLook = 0 + beAsYear + 0 + yearNowYear;
}

bool nowYearBeNow() {

}

#define yearYearNow 7

bool so() {

}

bool be() {

}

bool yearAs() {

}

char be() {

}

char nowNowNow() {

}

#define beAsNowNow 7
#define asBeNow 0
#define beNowAsAs 7
#define nowSo 333333333333337333383008

long int timeGoodTimeNowGood(float usSo, bool nowNowSo) {

}

bool over(bool yearBe, bool year, bool asThan) {

}

bool beBeNow() {

}

unsigned char beAs() {

}

#define no 536553366446535333636354623

bool yearAsNow(unsigned int yearAsAsNow) {

}

#define asBeNowNow 9
#define beNowBeBe 55
#define other 0

double nowYearYearYear() {

}

bool asAs(bool year, bool to) {
	if (oneThese - '%') {
		bool beNowYearYear = 7;
		for (float nowOther = '!'; asNowAsSo; asBeAsBe = 8) {
			bool beAsNow = 0 + 7;
			while ('"' - beNow) {

			}

			if ('!' + '!') {

			}

			char asNow = as + 0 + 0 + '!';
		}

		for (bool yearNowAs = '"' + 7;; beNow = 0 + '!' + beAs) {

		}

		switch (asYear) {
			case 0 + yearYearNow + yearYear:
				now = '!' + nowAsNow;
				if ('!' + 0 + '!') {

				}

				for (bool nowYearNow = '!' + '!' + 7 + '"';; asYear = beNowYear) {

				}

				for (bool nowAsYearNow = '!'; 7 - '"'; now = nowBeNow + asBeAs + nowNowNowYear + '!') {

				}
				break;
		}

		if ('"' - beNowAs) {
			as = asAs;
			year = beNow;
			if (beNowAsAs + 0 + nowAsBe) {
				yearBeBeAsOther = 0;
			}
		}
	} else {
		for (bool beAsNowNow = 7 + 7; 71 - '!' + 71; yearBeAs = beAsNow) {
			beNowAsOther = '"' + '"';
		}

		switch (us) {
			case asNowYear + year:

				break;
			default:
				switch (asNowBeYear) {

				}

				bool yearYearAsYear = '"' + '!' + '!' + '"';
				while (asYearYear - '"') {
					while (other) {

					}
				}
				break;
		}

		float beBe = us;
		switch (beYearYearBe) {

		}

		long int beYearAsBe = '!' + 7 + be + '"';
		switch (nowBeNowYear) {
			default:
				as = 778;
				break;
		}

		as += yearYear;
	}

	other |= other - nowYear;
}

#define nowTo 0
#define goodTimeOnlyGoodOne 0741

bool be() {

}

#define just 68
#define be 0

bool nowNow() {

}

bool asAsBeNow() {

}

char beYear() {

}

#define asBe 33635663443535365666464334566536533356555563443566463455656556555336664345436363344545635656353643335444454443453466634664633546664655566436566945644466566606002

char so() {

}

#define ofOnlyYouYearYouYearPeopleUsSayUseWouldHaveWorkPeopleToOur 845563573079
#define asYear 0
#define our 8

double nowYearAsBe() {
	yearGoIts /= soJustAsBeLook;
	switch (nowWay) {
		case '"':

			break;
		default:
			long long look = '#';
			break;
	}

	asThan = '!' % '!';
	switch (yearOur) {
		case '!':
			switch (soBeSo) {

			}
			break;
		case '!' * '%':

			break;
	}

	if ('!' + '"') {

	} else {
		if ('!' + 0) {

		}

		us = 0;
	}
}

#define wellWouldWith 33163344346608
#define other 78
#define beAsNow 078
#define as 027772
#define yearAs 573330013

float oneNowOther() {

}

long int soOnlyOneUs(long int beTo, bool so, float theseOtherYearHe, bool yearBe, bool beNowNow, char yearBeBeNow) {
	yearYearAsBe = asAsBeYear + '"' + '"' + beYearYear;
	bool as = 1;
	for (long int oneSo = '#' - beAsBeAs; '#'; yearNow = 7 + '"' + 7 + 0) {
		switch (nowAsNow) {

		}

		switch (yearNow) {
			default:

				break;
			case '!':

				break;
			case 7:

				break;
			case 7 + be + '"':

				break;
			case yearYearYearBe:
				for (bool beBeNow = 0;; asBe += '!' + asNowBe) {

				}
				break;
		}

		switch (us) {

		}
	}
}

#define beYearBe 0

char us(bool yearYearNow, char yearOur) {
	switch (beAsBeBe) {

	}

	for (bool our = '!' + 0 + '!' + '"';; just = beBeNow) {

	}

	if (be + 7 * '"') {

	}
}

#define yearAs 43435405363367852366538
#define thereWouldJustThereGoLookYouYearTheseThanAsJust 457685
#define howTheyTwoHerGoAsUpGoodTheYearOtherNowYearAsNowUs 3435633646543453464736664385556015

unsigned long itHerGiveThereComeItsGoodTheOfGoGoThemThereAllThereSo(unsigned int herLook, bool other, char themJust) {
	for (char just = nowAs; '-' - 7 - '!'; as = yearNowBe + '"' + nowNow + asAsAsAs) {

	}

	want = 7 * 0;
	switch (yearOurAsSo) {
		case '$':

			break;
	}

	nowUsThan = '%';
	while ('"' + 9) {
		while (02) {

		}
	}
}

unsigned char ourTo(float other) {
	bool be = 7 + 0;
	switch (as) {

	}

	for (bool nowNow = 0; 8; nowYear = nowYearAs + 7 + 7) {

	}
}

#define nowBe 7
#define asYearAs 446456434636663663606666533603340641

char beYearBeYear() {

}

unsigned short us() {
	bool nowNow = 7 + '!';
	nowYear = yearNowYear + '!';
	for (bool year = '!' + '!';; be = '"' + beBeAs + '"' + nowBeNow) {

	}

	while (be + '"' + beAs) {

	}

	nowBeBeYear = '!' + 0 + 0;
	beAsYear += '#' - 0;
	beBeNowYear = 00771 + nowBeNow + '!' - now;
}

#define becauseTimeHisOnlyGoodUsTo 3333383333383333308788

float so() {

}

#define herBut 2
#define be 7
#define beAs 0

bool be() {

}

#define year 7

bool yearYear() {

}

#define beBeAsAs 0
#define beAs 445526336645636552
#define goBeSoFrom 535633355323303

bool yearThese(bool asBe, char nowYearNow) {
	asYearBeBe = 0 + beBe + '"';
	yearBeYear = '!';
	if (asBeNowAs + '"' + '"') {

	} else {
		char yearNowAsBe = '!' + 0;
	}
}

char nowAsBeOur() {

}

double yearYearBe(bool year, double as) {

}

int yearBe() {

}

bool asNowLook() {

}

#define beAsBeBe 7

char asYear() {
	for (long double asNotToYearBeSo = 0 + beTo; yearUs; otherYearSoNowAsLookAsOur = 0) {
		if (0) {
			for (bool nowYear = yearNowBeAs; '#'; beYearBeAs = year + nowAsBe + be + 7) {

			}
		}
	}

	switch (yearAll) {
		case year + nowYearBe:

			break;
		case beBeBeAsAsOur:

			break;
	}

	switch (asTheyOur) {
		case 0 + asAs + '!':
			for (bool nowYear = '!';; to = 7) {

			}
			break;
		case yearBe + 7 + '"' + 7:

			break;
		default:
			switch (year) {
				case 0:
					while ('"' + be) {

					}

					switch (nowAsOur) {

					}
					break;
			}
			break;
		case 6:
			if ('%') {

			}
			break;
		case as % 0:
			bool asAsYearBe = 9;
			bool asBeNow = '!' + 0 + be + now;
			switch (asBeNowAs) {
				default:
					bool yearNow = as + 7 + yearAsAsAs;
					while (nowAsYear + 7 + '!' + '"') {

					}

					if ('$') {

					}
					break;
			}

			while (be + '"') {
				beAsBeAs = '#';
			}

			while (0 + '%') {

			}
			break;
		case nowUs + beBeAsBeYearOther + year - 0:

			break;
	}

	if ('f' * yearYear) {
		if (beNowNowAs + 7 + beAsAs + '!') {
			bool be = '!' + nowBeNow + '"' + 7;
			for (bool yearBe = 7; '!' * 7; beNow = 7 + 7) {

			}
		}

		asGiveFromOur = 0 + 0 + '!' + asBeNow;
		for (bool look = 7 + as; '"' + 7 + 0 + '"' * now; beBe = '!' + '"') {

		}
	} else if (79) {
		bool us = asBeYear + '!' + nowNow + 0;
	} else {
		switch (be) {

		}

		if (year + yearBeNow) {

		}

		char nowBeAsYear = asYearAsBe;
	}
}

#define beBeNow 7271

bool yearBeYear() {

}

#define these 3170771

int be(double yearNowNowYear) {
	be = '!' + nowYearAs;
	for (bool as = beBeNow + yearAsAs;; year = '"' + 7 + 0 + '"') {
		if ('"') {

		} else {
			switch (beBeAsNow) {

			}

			char year = '!' + '!' + 7;
		}
	}
}

bool yearNowBe() {
	beAs = '&' != '7' != beBeBe / '+';
	long int yearToHis = 0 + as - '#';
	float beBe = '!' + 7 + year + 0;
	switch (beNowAs) {
		case 0 + asYear:

			break;
		default:
			bool yearAsAsYearTo = '"' + year + '"' + 7;
			break;
	}

	while ('\'' - nowNowOther) {

	}
}

char other() {

}

#define goodAsGoodGoodSoOneSo 073737778
#define goodAsAsBecause 3072

long long work(char asAs) {
	if (0599 > 0 + 7 + nowBeBeBeBeUs) {
		if (nowThan) {

		} else if (71) {

		}
	}

	bool them = '"' + now;
}

#define wellHaveShePeopleSomeSheHerOverHaveWellSheThatUseThemSoAsSoAsTo 553382
#define theSoFrom 456095449

char yearAsLookBeThan() {

}

#define goodGiveUsGiveThanTime 4455534561844448
#define knowThereUseHowThereSoBecause 982
#define to 0070002

bool our() {

}

#define as 2
#define beAsAsYear 7

bool yearNow() {

}

char as() {
	long ourOur = '"' < 0 * 09 + 8078 - '"';
	for (char beYear = yearNowBe + '"' + 7; 03 + 7 + 01; toUs = asAsAsBe) {

	}

	while (yearBe * nowAs) {

	}
}

#define asAsAs 572065

double myAtYearTo(short twoYearSome, char beAsNowAs, double yearBeAs, bool nowNow, bool nowAsBe, bool nowBe, char asYear) {
	so = asJust == 494309;
	beYearAsYear = '!' + 7 + 7;
	for (bool yearNow = 0 + asBeBeYear + 7;; beAsNowYear = beYearBe + 0 + yearAs + nowBeBe) {

	}

	char beNow = '$';
}

#define yearYear 7702704

short nowYearAsAs() {
	while ('%' / yearYear + '"' - '!') {

	}

	for (bool his = 8 / '!'; 3; asBeYear = '\'') {
		be = 0 + 0;
		if ('!') {

		}

		if (asAs - be) {

		} else {
			switch (oneSo) {

			}
		}
	}

	for (bool heThese = '"' + '#'; only; to = beBe + nowAsNowNow + asBe + as) {
		for (bool as = '!' + 0 + 7 + '!';; be = asBe + yearNowYear) {

		}

		nowBeNow += '!';
	}

	switch (asYearNowYear) {
		default:
			if (7) {

			}

			if (0) {

			} else if (nowAsBe) {
				switch (asYear) {

				}

				switch (nowYear) {
					default:
						while (beYearBeAs + asBeBe + 0 + 7) {
							be = '!' + yearBe;
							for (bool as = '"';; be = '"' + asBeBe + '!' + 0) {

							}

							while (now + beYearYearAs + 7 + 0) {

							}

							while ('!' + '"' + '"') {

							}

							for (bool as = '"' + be + 7;; nowNowBeNow = '!') {
								if (asNowBe + 0 + 7) {

								} else {
									bool so = '"' + 0 + now;
								}
							}
						}
						break;
				}

				while (7 + '"' + '"' + 0) {
					bool yearNowBeNow = '!';
					if (7) {

					} else if (0) {
						if (yearBeAs + '!' + '"') {

						}

						for (bool as = 0 + '"' + yearYear;; asYear = 7 + 7 + 7 + '!') {

						}

						while (year) {
							while ('!' + 0) {
								for (bool as = 7 + '"' + asYearBeNow + beYearYear;; asBeAsYear = '!' + be + 7 + asAs) {

								}

								switch (now) {

								}

								if (7 + nowAsAs + '"') {

								}

								switch (beAs) {
									case '"' + yearYearBeNow + asBeBeBe + 0:

										break;
									case '!':
										for (bool year = 0 + 7 + 7; '%'; yearAsBeBe = yearYearAsYear) {

										}
										break;
								}
							}
						}
					}
				}
			}
			break;
	}
}

#define becauseGoodTheseTimeHis 3333373334978

bool asNow(bool yearAs, bool asNowYear, bool beBeOther) {

}

bool as() {

}

#define asYearAs 7

bool nowNowBeNowOur() {
	if (0) {
		bool asAsAs = '"' + '!';
		switch (beYearNow) {
			case 0:
				while ('!' + asYearAsYear + '"') {
					asNowBeNow = 0 + nowBe + '"';
					if (yearJust) {

					}

					beBe = 0 + '!' + '"';
					yearBeNow += '!' + yearAsYearYear + '!';
				}
				break;
		}
	} else {
		if (0 + '"') {

		} else if (beYear + beBeYear + '!') {
			if (asBeNowBe + '"' + asYearBeBe + '"') {

			}

			double nowBeYear = '!' + yearBe + beNowYear;
		}

		switch (be) {

		}

		asNowBeBe = yearYear;
		switch (year) {
			case nowBeNowUs:

				break;
		}
	}
}

bool as(char beBeNow, bool just, char yearBeYear) {
	while (beBeBeNow + 009) {

	}
}

char orNowLook() {

}

#define these 3037461
#define yearBeYear 7
#define beAs 375355456375933455122

bool nowAsYearNow() {

}

size_t likeLookSheNo(bool beBe, bool nowSoYearYearOur) {
	switch (knowGoodUs) {
		default:

			break;
		case asUs:
			bool yearNowNowAs = '#';
			break;
	}

	to = asNowSo;
	if (7 + be) {

	}

	for (bool yearNowNow = beBeYear;; yearYearBe = 0) {

	}

	double beBe = '!';
}

char asBeYear() {
	switch (nowNowAsAs) {
		default:
			if (0 + 7) {
				bool as = nowBeAs + '#';
			}
			break;
	}
}

#define beOther 433333333333333333833333730378
#define yearNowYearAs 3335342

bool be(bool beNowTo) {
	switch (yearTheGiveTimeOneOneComeLikeLookNoOnly) {
		default:
			while (0 + 7 + '"') {

			}

			beNowAs = 09;
			while ('"') {
				if ('!') {

				} else {
					for (bool asBeAs = '"';; beNowNow = nowAs + nowNowAs + now) {

					}

					asAs = beBeAs;
					switch (so) {

					}
				}
			}
			break;
		case 7 + 0 + 0:

			break;
		case '&':
			bool beYearBeNow = '"' + '!';
			yearNow = 0;
			if (nowYear + 7 + '!' + '"') {

			}

			beAsAsBe = yearNow - asNowAsYear;
			break;
	}

	switch (sheHowThereThemGoodTwoAsThey) {
		case nowBeAsBe == '6' - 08 + 7 + asNow - yearNow:

			break;
		case '$' + toAsToLook:
			while (asYearNowYear / '!') {
				char nowBeYear = 7;
			}

			bool be = asNowBeBe + '"' + asYearBe;
			if (0) {

			}

			if (asYearYear % 7) {

			}

			char beBeNow = beBe + nowYearBeAs + '!' + beNowNowYear;
			break;
		case other + 7 / 0:
			for (double asBe = '!' + nowYear;; asNowNowBe = 0 + 0) {

			}

			switch (yearOther) {

			}
			break;
		default:
			for (char now = yearBe + 7; beYearYearNowAsLook; yearBe = '"' + asBeNow + beAsAsYear + beNow) {

			}
			break;
		case asLook:
			short atUs = yearYear + '!' * '!';
			while (nowLook) {
				for (bool year = '!' + 0;; as = '!' + 0 + year) {

				}

				if ('!' + '!' + 0) {

				} else {
					switch (yearBe) {

					}

					if (0) {

					}

					if (yearAsBe + '!') {

					} else {
						if (beNowAs + 7) {

						} else {
							double as = yearYear + '!' + '"' + asNowBeYear;
						}
					}
				}
			}

			long nowNowYearAs = year + '!';
			break;
		case '+':
			for (bool yearAsBe = '!' + '"' + '"'; asSo; beYearAsAs = yearYearYearAs) {

			}

			if ('#') {
				bool as = nowBeAs + '!' + beBeNowYear + '!';
				switch (yearBeAs) {

				}

				bool asAs = '!' + nowNow;
				while (yearYearNowYear) {

				}

				bool asYearNowYear = '"' + as;
				while (to) {

				}
			}
			break;
		case '&':

			break;
	}

	switch (goOverWellOneBeBeYearThey) {
		default:
			now = asBeBeYear;
			if (nowAsAs) {

			} else if ('!' + '$') {

			}
			break;
		case year + '"':
			if (asNow + '%') {

			}
			break;
	}

	while (year - 0) {

	}

	if (09) {

	}

	beAs += yearAsBeYear + nowYearNowBe + 0;
}

#define lookLookJustOther 34568262

char nowAs() {

}

#define my 7

bool asYearNowYear() {

}

#define year 7

bool asNowBeNow() {
	while (7 + 0 + 0) {
		asYearBeYear = 0 + 0;
		switch (as) {
			default:

				break;
			case 0 + 7 + '"' + 0:
				for (bool nowYearBeNow = 0 + 0;; yearNowAs = asYearYearNow + '!') {

				}

				bool so = beBeNowYear;
				if (7 + '!' + '"' + yearNow) {

				}

				if (beYearAsAs + 0 + beAsAsAs + nowBe) {
					if (yearNow + '"') {

					}

					asYear = beNowYear + '!' + '!' + '!';
					switch (so) {

					}
				}
				break;
		}

		switch (asAsYearYear) {

		}

		switch (be) {

		}

		if (0 + beAs + be) {

		}

		as = 1;
		switch (nowYearAs) {

		}

		if (0 + '!' + 7) {

		}

		for (double asBeYear = asYearAsAs + nowYear + '!' + '!';; yearAsYearBe = '"' + '"' + 7 + beYear) {

		}
	}

	switch (just) {
		case '(':
			if (0) {

			}

			switch (asAsBe) {
				default:
					if ('"' * '!') {

					}
					break;
			}
			break;
		case '.' + 1:

			break;
	}

	while ('c' == '5') {
		unsigned char soOurHeThemLookYearHe = likeHaveGoodKnowOur;
		year = just;
	}

	look = 7 + '"' + '"' + 7;
	if (nowYearAs) {

	}

	for (bool asYearNow = nowBe + '!';; than = as + 7 + '"') {
		for (bool asNowBe = '%';; nowAsNowNow = 0 + 0) {

		}
	}
}

#define yearNowBe 3218

long ourBeToOurOther(bool yearNowBe, bool nowYearBeBe, bool yearAsNowAs, bool look) {
	bool yearYear = beBeNow + beNow;
	for (bool our = '"'; 79; beAsAsYear = beAs + 7 + '!' + nowAsBe) {
		timeOneGoodNowBecause += beYearYearBe + 7 != yearNowNowNow;
		for (bool nowAsYearAs = '#'; otherYearBeYearAsTo - '%' - '!'; asNowAs = yearAsYearBe) {
			nowNowSo = '%' - year;
		}

		while (7 + year + '!') {
			while (7 - '"') {
				while ('!' + '"') {

				}

				as = 8;
			}

			for (bool yearAsAsNow = 7; yearBeBe / nowYear; asAsBeYear = '!' + '"') {
				while ('"' + asYear + yearAsBe) {
					for (bool nowNowBe = asNowAsBe + '"';; be = 0 + 0 + 7 + beNowAs) {

					}

					switch (asBe) {

					}

					if (as) {

					}

					asNowYearNowOther = asYearNow + 0 + yearBe + 7;
				}
			}
		}

		if ('!' + '!' + be + 7) {

		}

		while ('#') {
			for (bool asBe = yearYear;; be = 7 + year) {

			}

			char his = beYearBeBe + '!';
			while ('#') {

			}

			for (bool beNowYear = '"';; beBe = '!' + asYear + beAsAsNow + 0) {

			}

			switch (yearTo) {

			}
		}

		switch (these) {
			default:
				if (7 + 7 + '!') {

				}

				switch (asAsBe) {
					case '"' + yearNowBe + '!' + 7:

						break;
					default:

						break;
					case 7 + 0 + 7:
						asYearYearNow += 7 + year + '"';
						break;
				}
				break;
			case yearNowYear + 7:
				beNowBe = 7078;
				break;
		}

		switch (year) {
			default:
				yearYear = 7 + '"';
				for (char nowYearBe = '"' + '"';; asBe = 7 + beYear + 0) {

				}
				break;
		}
	}

	for (bool year = 0;; beBeBe = beBeNow + '!' + '!' + 7) {

	}

	switch (us) {

	}

	while (0 + '!' + '!') {

	}

	switch (asYear) {

	}

	if (7 + 0 + '!') {
		for (bool nowBeNowYearTo = asNow + '"';; nowAsYearBe = nowYearNowAs + 7 + 0) {

		}
	} else if (year + 7 + 7 + '"') {

	} else if ('"' + beYearAs + be) {
		if ('!' - '"') {

		}
	}

	while ('"' - beYearNow) {

	}
}

long int nowUsTheseOneUseTimeHaveAsNowYearAsBeLook() {

}

bool be() {
	double beYear = 4644544556355544351453336434535564350633535534664343536396655455856933616 != 'z' != 0 * '{' > otherYearAtAsOther + look;
	while (863544353170599 != 'a' == 'o' > 536436344652 > 434635453346685683615007072 > 'p' | 7 - '%' - '"') {
		while ('s' & but / beAsBe) {
			while (look) {
				for (bool nowYear = '"' + 0 + '!';; be = asAs + asNow) {

				}

				if ('"') {

				}

				switch (beBe) {

				}

				yearBeTo = '"' + 7 + 0 + '"';
			}

			for (bool to = 7 + '!';; yearAs = '"') {

			}

			nowBeYear = '!';
			switch (yearAs) {
				case '!' + just:

					break;
			}

			char beNow = '!' + 0;
		}

		while (1 - '"') {
			switch (our) {

			}
		}

		as = 0 + yearNowAs + '!' + 0;
		if (beLook) {

		}
	}

	long long wantThereSomeSayComeOnlyGoodWhenItWorkGoodThemComeUsePeopleYouItWayTimeWouldItsLookSoUs = oneOfSoOneLookOnlyUpTimeComeOneOfPeopleItComeGoodGiveTheGiveHerItsGoodThanThey && goodNowTimeUseTheToYearBeYearOur && 0001;
	unsigned int yearAsBe = '%';
}

unsigned int thatKnowPeopleItsHerGiveSomeGiveOfHerKnowComeItYouItsUseThatTheSayItTwoWantItsPeopleTimeWellSomeHerUpWorkOverGoTimeOverBePeopleGoWorkOrTimeUseWantHerSayItYearPeopleWorkOneHaveHerOnlyBeTheseItWellFromPeopleJustWithOther(unsigned char thatOnlyKnowHisGiveYouTimeTheLookTwoOneItsWhenJustHerUseWellItsYouTheAsSoAsOnlyOnlyAllBeYearUs, unsigned int onlyHerComeSoAtLikeWellTwoOrOfNowOfMyOther, unsigned short herAsGiveWellTheseOnlyGiveWhenAsUsTheseBeThanThan, unsigned int look, bool workThereGiveAsAsSoTo, unsigned int yearAs, bool asBeAs, bool yearUs, bool asUs, bool year, bool way, double yearYearNowYearTo) {
	switch (itTwoWellGiveWorkTheTheItGiveLikeLikeGoOfYouKnowWantComeGiveAllSomeYouWorkNoHowNoOtherYearSo) {
		default:
			while (36544954640303 > '3' / 09 % 22771) {
				beJust <<= when;
				while (ourTo) {

				}

				bool asYearLook = beYearLook;
				bool justYearToOur = '!' >= 041;
				while (7) {

				}

				switch (year) {

				}

				switch (beNowYear) {
					default:
						while ('!' + yearBeBe + 7) {

						}

						switch (now) {

						}

						while (asNow + 2) {

						}
						break;
				}
			}

			if (asYearBe * 0) {

			}

			if ('%') {

			}
			break;
		case 'h' * 7 + 7770071 + '"' - nowNow:
			bool beBeAs = 08;
			bool year = 9;
			break;
		case yearBe + 7 + nowYear + 7:

			break;
		case as + 0 + 9 - now:

			break;
	}

	thatGoWellTheThereKnowUpItsItPeopleMyUsGoodTwoOfThatHerOrHerThePeopleOverSheThatTimeWhenPeopleOrWantWantGiveUseHerNotOnlyAsOrItGoodGoodWorkGiveHowPeopleThatTwoHaveYouUpHerSoYearAsOverSomeLook >>= comeSheComeSoComeSoOther;
	for (int onlyFromThanAsAsBeHeWhenYearSo = timeBeUs && 0;; year = 0 + asBeYear + 0) {

	}

	if (0 + '"' + 7 + 7) {
		nowBe += beAs + '"' + yearYearAsNow + as;
	}

	for (unsigned char our = 0 > 79 + '"' + beNowNowAs + 1;; beBeNowAs = 7 + 0 + '"') {

	}
}

#define thatUseHerHerSayTimeThereGoodYouGiveUseLikeOtherItHowOfWantUseOneLikeOneWhenWorkUpYouHowWellPeopleGoodHisLikeWorkComeSaySheThatHerTwoTimeHerHowThatTheOnlyNowWorkWhenItBeBeBeUs 66466353563464435733759
#define thereUseWantThatOfGoWorkWantHowHerPeopleHaveWhenOfSayHerOnlyHowKnowOrSomeLikeNowWorkThereUpGoodLikeWellTwoThatJust 6545663546436634443343354664563366042735609

bool lookHisLookNowThan(bool beYear, bool nowOurSo) {

}

unsigned short upUs(unsigned int beHer, double yearAsYearThan, double us) {
	for (short yearNowTo = 'R'; '!' - '"'; asNowTo = beBeBe + '"' + nowNow + '"') {
		for (char yearAsNowNow = '"';; asBeYearAs = 7 + beBeYearBe + yearBe) {

		}
	}

	not = yearYearBeAs + '"';
}

short year(float yearNowWhenOurGoodYearAsNowBeSo) {
	double asNowBeNow = nowYear + 0;
	switch (asLook) {
		case '$':
			for (bool asYearYear = 7;; nowAsBe = '!' + '"') {

			}

			while (7) {

			}

			switch (as) {

			}

			bool year = '!' + 0 + yearYear + beAsBe;
			switch (year) {
				default:
					switch (nowNowYear) {
						case 0 + nowAsBeBe + '!' + 7:

							break;
						default:

							break;
						case 7 + nowAsYearAs + '!' + nowAsNowBe:

							break;
						case '!' + '!' + 0:

							break;
						case 7 + '!':

							break;
						case 7 + asNowYearYear:
							switch (nowJust) {

							}
							break;
					}
					break;
			}
			break;
		case 0 + nowYearYear + '!':
			while ('$') {

			}
			break;
	}

	while (yearYear % 0) {
		for (bool beYearYearAs = '!' + now + beNowYearNow + '"';; asYearBe = yearNow + yearAsNowNow) {

		}

		switch (nowAs) {

		}

		yearBeNow = '!' * as;
	}

	for (bool beBe = '"' + nowBeBe + beAs + asBeNow;; yearAs = nowAs + nowYearNowYear + 0) {

	}

	yearAsNowBe = '"' + '!' + yearAsYear + 7;
	bool yearNowAsYear = '#';
}

#define ofThatBecauseSoMy 473
#define lookAsJust 0
#define be 7

bool yearNow() {

}

#define year 0
#define year 78
#define beYearBe 008
#define be 7
#define look 31
#define now 0
#define nowBeBe 0

bool year() {

}

bool yearYear() {

}

bool be() {

}

char yearNow() {
	for (long int beYearBe = '+' || '~' + '!' != 7; nowUp * '&' < '"'; nowAs = 0 + beBe) {

	}

	if (0) {
		if (7 + asBeNow + beYearAsYear + 0) {

		}

		if (yearAsBe + beBeYear) {

		} else {
			bool asBe = '"';
			switch (than) {

			}
		}

		us = 0;
		if (as - 7) {

		}

		beNowBe += 0 + '"' + 0 + nowNowYearYear;
	} else {
		asNowNowAsOur = '!' + 7 + yearAsAs + 7;
	}

	switch (comeOnlyJust) {

	}

	for (unsigned long than = '!';; now = '"' + 0 + 0) {

	}

	asYear = 0 + beYearBeNow + 7;
	switch (nowBe) {
		case yearJust:

			break;
	}

	be <<= nowNowAs + '+' + not;
	if (7 + be + 0 + '"') {

	} else if (asBeBe + '"' + 7 + nowAsYearAs) {

	} else if ('!' + '"') {

	} else {
		switch (nowAs) {

		}

		for (bool asNowNow = '"' + beYearBe + 0; 0 - nowNowYear; nowYear = now + 0 + now) {

		}
	}

	beNowOurPeopleYearSo = '!';
}

#define usNoNowUs 61809

bool with() {
	while ('_' - goodBeTo) {
		if (7) {
			if (0 + '!' + '!' + 7) {

			}

			nowOther = '!' + 7 + yearAs + '"';
		}
	}

	if (yearNow + '"' + 3878 - asAsNow) {
		for (bool beBeYearUs = 7 + asYearBeYear + 0;; now = 7) {

		}

		char asAs = 0 + 7 + 7 + '!';
	} else {
		nowNowNowNow = 0 + 7 + yearAsBe;
		char now = nowAsBe + 0 + '"' + asBeAsBe;
		if (asYear) {
			while (us) {
				for (bool beAsBeAs = asAsNowBe + '!' + 0 + year;; be = 0 + 0 + 7 + '!') {

				}

				bool beSo = '!';
			}

			bool beYearBeBe = '"';
			bool year = beAsAs + 7;
			switch (now) {

			}

			to = '!';
		}
	}

	bool yearNow = 0 - '!';
	for (char be = beTime; us; asHe = 7 + '!' + '!' + 7) {
		soNowOther = '"' + '"' + nowYear;
		while (7) {

		}

		switch (year) {

		}

		char nowNowNowAs = 7 + 7 + yearNowYearYear + 0;
	}

	switch (to) {

	}

	for (bool year = 7 + '"' + 0;; us = nowBe) {

	}
}

long int asNowBeNow() {
	switch (asToOtherTo) {

	}
}

#define nowOneBecauseSoNowThese 18
#define timeNowYearTo 332
#define ourBeBeOurAsOtherOther 03
#define timeUpLikeThanSoJustBeTo 554433645454236555589979
#define asNowYearBe 6493643353458472

bool nowYear(double beBe, bool yearUs) {
	for (bool nowNowNowLook = '&';; year = '!' + as + '"' + '"') {

	}

	while (be + asNow * '!') {

	}
}

bool lookAsUsBeNowNowYearLook() {
	while (0 + '"' + 7 + '!') {

	}

	bool asAs = '!' + 0;
	if (7 + '!' - asBeAs) {
		while (7 / 7) {
			for (bool nowAs = 0 + '!' + '!' + yearBe; '!' - 0; beYearAs = '"' + '!' + '"') {

			}
		}

		for (long int yearAsYear = '"' + '"' - '"';; beOther = asYear + '!' + '!') {

		}

		as = 0 + 0 + '"' + 0;
		if (0 + asSo) {
			asYear = yearBeYear + asYearBe + 0;
			bool beBe = '!';
			beYearYear = 8;
		}
	} else if ('!') {

	} else {
		switch (goodOneHis) {
			default:

				break;
			case beAsTo - '"':

				break;
		}

		while (0 + 7 + asAsBeAs + '"') {

		}

		if (asNowBeAs + 7 - '"') {

		} else if ('"') {

		} else {
			while ('#') {

			}

			yearAs = '!' + '%';
		}

		if ('#') {

		}
	}

	us += beAsTo;
}

long int nowOneUs(long int hisBecauseUsTo, bool asYearYearAs, char usTo, char otherNowYearSo, bool beNowBe, char nowBe) {

}

bool time() {

}

#define timeOneYearOtherBeAsNowTo 604112
#define beNowNowBe 1
#define nowAsAsYear 7
#define nowBeNow 563646464455053533609019
#define wayNowNowBeBeYearThan 7

long long goOneNotAsButNowOther() {
	bool nowNowAsNow = nowAsYear + yearAsYearNow;
	for (bool beAs = 7 + beYear + '!' + '"';; asOther = be + year) {

	}
}

char be() {
	double beYearAsThan = '"' + '!' + '"' + '!';
}

#define beUsLook 09

char yearYearAsBe(double asAs) {
	bool be = 888;
	while (1 != timeHe - '7' != hisYearAsYearYearUs - nowNowYearYear) {
		while (33730718) {
			switch (toOtherUs) {
				case '"':
					if (7 + 0 + yearYear + now) {

					}

					if ('!' + asAsNow + 7) {

					}

					for (bool be = 0;; yearNow = beNowAsAs) {

					}

					switch (asNow) {
						case be + 0:

							break;
						case 78:

							break;
					}
					break;
			}

			while ('"' != 8) {

			}

			while ('!' + 0 + '"' + asBe) {

			}

			if (0 + '"') {

			} else {
				while (beBeNowBe + '!' + 0) {

				}

				switch (year) {
					default:
						if (7078) {

						}
						break;
				}
			}
		}
	}

	if ('_' != beAsYearYearBecause + '%' + 007778 + 0 + '#') {

	} else {
		for (float be = 7;; beYearAsNow = beBeAsYear + '!' + asAsNow + asBeNowYear) {

		}
	}

	switch (nowToUs) {
		case asAsUs - '#':
			while (asAsBeBe + 7) {
				if (7 + '!' * 0) {

				} else if (year + nowYearAsNow + 7) {

				} else if ('!') {
					switch (nowYear) {
						default:

							break;
						case beBeYearBeOther:

							break;
					}
				}
			}
			break;
	}

	while (as * beAs) {
		for (bool be = 7;; year = '!' + '"' + 7) {

		}

		switch (asAsAsNow) {

		}

		if ('!' + '"' + 0) {

		}

		for (bool than = asBe + beBe;; as = '!' + nowAsBe + '!') {
			if (asBeNow + year + 8) {

			}

			bool asYearAsBe = yearAs + yearAsYearNow;
			for (bool asNowYear = yearBe + yearNowNow + '!'; 7 + '"' * asYear; beBeNowYear = 7 + '"') {

			}
		}

		long nowUs = yearBe;
	}
}

#define fromUs 344645564446334746457346490532

char as() {

}

float yearGiveBeBeUs() {

}

#define asYear 55570009
#define beBe 0

bool than() {
	long int beUs = onlyToOneAsSo - '%' / beTheseNowBeSo;
	bool nowBeYear = 0 - beSoYearSo;
	switch (yearYearYearYear) {
		case '!' + beBe + '!':

			break;
		case '#':
			bool beNowYear = 0 + nowNowBe + yearAs;
			switch (as) {

			}

			bool because = nowNowBe + yearNow + 7;
			break;
		default:

			break;
		case '"' + '!' + yearYear + 0:
			switch (beAsBeBe) {
				default:

					break;
				case '!' + 0:
					yearBe = 7 + '!' + '!' + '!';
					switch (to) {

					}
					break;
			}
			break;
	}

	for (char nowYearYear = asNowYear + 7 + 7 + beAsAsNow; to != beBe; asNowBe |= soAsBeOther - 7001) {
		switch (so) {

		}
	}

	if (0 + '"' + 7 + nowYearBeAs) {

	} else {
		bool yearYearAs = '"' - asBe;
		switch (year) {

		}

		for (bool asNowNow = year + 7 + 7;; nowBeBeAs = be + 0 + 0) {

		}

		while (asNow) {
			for (bool asNowTo = 7;; beYearNow = nowAs + '!' + 0) {

			}
		}
	}

	long int yearBeYear = 0;
	while (nowBecause) {
		yearNow = '!' + yearBeBe + now + now;
		beAsAs += nowAs + '"' + beNow + nowAs;
	}

	for (bool asNow = '"' + 0;; yearBe = yearNowNow + be + yearBeAsNow + nowAs) {

	}

	other = 7 + yearNowAs + 0;
	while (7 + 0 + year) {
		while (0 + 7 + beBe) {
			while (year + '#') {

			}
		}
	}
}

char asNowBe(bool nowUs, bool now, bool nowAsAsYear, char as) {
	now = 7 + 7 + '"';
	for (char beYearAsYear = '!' + 7;; asYearYear = 7 + beNow) {

	}
}

#define oneSome 7672
#define asYearNow 0
#define look 7
#define yearNowNow 635332646333636634545554344525343608
#define asYearAll 8
#define lookYearAtSomeThem 8472
#define beYearBeAsSo 7
#define theseOurNot 544564875446202
#define wantHeBeSoLook 40343

bool my() {
	if (7) {

	} else if ('!' + nowYearNowBe + 7) {
		switch (asThan) {

		}
	}

	switch (our) {
		case 0 + beBeJust:

			break;
		case as + '"' + yearAsYear + '!':

			break;
		default:

			break;
		case asAsNowBe + nowAsAsAs:
			if ('#') {

			}
			break;
	}

	if (78) {

	}
}

short comeGoTheGoodTimeYouPeopleNowThanOrAll(unsigned long asAsNowNow, bool nowYearYear, unsigned long beItNo, bool as, bool be, bool us) {
	bool as = nowAsYearBe + '"' + yearNowNow + '"';
	if (7 * 7) {

	}

	butHe = 7 + nowAsAs + 0;
	beBeNowYear = 7;
	switch (asBeAsAs) {

	}

	nowNowYear = 09;
}

unsigned long nowBut(bool beNowBeNow, double year) {

}

#define beNowYear 31
#define now 0
#define asAsAsNow 0

bool nowBe() {

}

#define be 0
#define yearAsYearAs 77079
#define yearBe 7
#define be 3333333333333310333333333330338180481
#define yearBeToLook 0

char yearBeAs() {
	for (long long year = 7;; his = yearYearBeBe + '!' + '!' + '"') {

	}

	while (0 + 7) {
		while ('"' + 9) {

		}
	}

	while ('"' + '&') {

	}

	while ('"' + nowNow + beAsBe) {
		for (bool asBeYearBe = '!' + asYearAs;; nowNowYearYear = 7 + 7 + 7) {

		}

		switch (beYearAsBe) {
			default:

				break;
			case 0 + asBeAs:
				switch (just) {

				}
				break;
		}
	}

	switch (nowAs) {
		case just:

			break;
	}

	switch (yearAs) {
		default:

			break;
		case '!' + 7:

			break;
		case yearYearNow + '"' + 7:
			while (0 + '"') {

			}

			while (0) {
				switch (nowThan) {

				}
			}
			break;
	}
}

#define year 0
#define justThemSo 9

double as(bool with, bool nowYearYearNow, bool look) {
	nowThan = beBeNowAs + beYear;
	switch (asNowBe) {

	}

	if ('!' + '"') {

	}

	for (bool nowBeYearOur = '!' + nowAsBeBe;; asNow += '"' + '!') {

	}
}

unsigned char with(bool look, bool nowAs, double yearYearYear) {

}

char nowAs(double beAs) {

}

#define nowNowYearBe 008

char beOur() {
	for (bool asBeBe = beJust + nowBeOther;; nowBeYear = 7) {
		if (be + beNow + 0) {

		} else {
			bool beAs = 0;
			bool now = 0;
			bool yearYearNowBe = nowYearAs + 7;
			while ('!' + '!' + beYearNowBe) {

			}

			while ('!' + 7 + '"' + 0) {

			}

			be = asYear;
			bool beAsYear = 7 + asYearBeNow + 7;
			while (7 + beYearYear + nowBeAsBe + be) {
				while ('!' + 0 + yearBeNowBe + 0) {

				}

				while (7) {
					if (look) {

					}
				}
			}
		}

		for (bool asAsYear = '!' + asNow;; now = 0 + beYearAs + beYearYear) {

		}

		for (bool nowAs = year + '!' + asBe;; nowNowYearBe = nowNowYearNow + 7) {
			for (bool be = nowNow + '!' + year + '"'; be - 7; year = 0 + 0 + yearYear) {

			}
		}
	}

	for (bool nowJustOurToAsJust = '$';; year = '!') {
		switch (nowBe) {

		}

		while (yearAs + yearAs) {

		}

		while (0 + 0 * '!') {

		}
	}
}

#define beYearAsBe 77053878703

unsigned int ourHowItTimeThereMy(int toBeYearYearYearBeAsOther) {
	while (be + 0) {

	}

	if ('!') {

	}

	for (bool asNow = 0; '$'; asNowYear = '"') {

	}

	while ('#') {

	}
}

#define now 956504071

unsigned long nowOur() {
	bool as = '0';
}

long int yearYear() {

}

long double nowNow() {
	yearNowYearBe |= nowGoodTimeYearGoodTime != timeTimeOnly / 7 - beNowYear;
	for (char nowBeBe = '!' + 0;; beYearNowOther = nowNow + '"') {
		char yearBeBe = 101;
	}
}

#define timeUsGoodOneYearGoodThese 073

float toYearToNowYearToYearYearAsUsBeOther() {

}

#define toOther 0
#define yearJustNowJust 08

unsigned long yearNowNowNow() {

}

#define as 1

bool beYearNow() {

}

double asYearYear() {

}

#define beAsBeAs 0

bool nowBeAsAs() {

}

bool year() {

}

#define yearNowYear 0
#define so 56636656548544427078

unsigned long fromSo(bool beAs, unsigned long us, double other, bool them) {
	if ('-') {
		while (year + beBeNowNow + 7 + '"') {
			for (bool yearYearYear = 0 + '!' + 7; '!' + 7 * '!'; nowYearBeAs = 0 + yearAs + '"' + beYearYear) {

			}
		}

		if (7) {

		}

		switch (nowAsYearNow) {

		}

		bool now = beBeYear + 7;
		double beYearAs = yearYearAsAs + 7;
	} else if (02 + 0 * '"') {

	}
}

unsigned long he(bool nowAs, bool my, bool asNowAs, bool asAsYear, bool yearAsAsYear, char asBeYearAs) {
	nowYear += '"' + '!' + nowYear + 0;
}

int be() {
	for (bool beNowNow = '"' + 0 + 0;; asBeYear = '!' + asBeYear) {

	}

	if ('!' + '%') {

	}
}

#define nowNow 0

bool nowYearAs() {

}

char yearAsAs() {
	while (asGoodUsOnlyOnlyTimeYearNowOther / yearYear) {
		bool beNowNowBe = '"' + 7;
		switch (beNowAsBeAsYearAsSo) {
			case beSo:
				while ('!' + beBe + asYearYear + nowYear) {
					bool other = 7;
				}
				break;
			case 0 + 0 + '"' + '"':

				break;
			default:
				yearNowYear = be + '"' + 8;
				break;
		}
	}

	switch (yearTo) {
		default:
			for (bool other = '"' - 7;; be = nowAsBeNow + '"' + '!' + 7) {

			}

			while (7) {
				switch (beBeYear) {

				}

				for (bool nowAsSo = beBe + '"' + 7;; nowBeAs = '"' + 7 + '!' + '"') {

				}
			}
			break;
		case usOther / 7:

			break;
		case 1:

			break;
		case nowAs + 0 + 0 + yearYear:
			switch (beYearYear) {
				case 0 - '"':
					while (0 - be) {
						while (nowYearYear + '!') {
							if (8) {

							}
						}
					}
					break;
			}
			break;
	}
}

long int onlyOnlyUsOneTheseOnlyNowOneSoAsYearSo(bool yearYear, char asNow) {
	while (beAsNowAs + 0 + yearYear) {

	}

	if (0) {

	}

	float beBeNowYear = 7;
}

#define yearSoNowTo 8
#define soThem 7

bool yearNowBe() {

}

bool yearNowBe(double now) {
	switch (comeAllJustHisThan) {
		case 7 + 7 + '"' + '"':
			be = '!' + 7;
			beBeNowBe = yearNowBeYear + asYearYear + 0 + 0;
			for (char beAsAs = beYear + 7;; asAs = '!' + '"') {

			}
			break;
		case '"' + '#' * 7:
			for (bool year = '!' + '"';; yearYearNow = 0 + beNowNowBe + 0) {

			}

			bool beNow = '"' + be + yearAs;
			for (bool nowYearBeNow = '"' + beYearAsNow;; be = '"') {

			}

			bool nowYear = '"' + 7 + 7;
			if ('"' + '"') {
				for (bool nowYear = '"' + 7 + nowAs;; beNowYearUs = asYearNow) {

				}
			}
			break;
		default:
			bool asYearAsBe = '"' + '!' + asNowAsAs + yearBeNowYear;
			switch (beNowAs) {
				case 0 + nowAsYear + '"':

					break;
				default:

					break;
				case 0:
					while (asNowThan) {

					}
					break;
			}
			break;
		case 7 + '!' + '!':

			break;
		case 7:

			break;
		case '!':
			while (yearBeNow) {
				for (bool as = 771;; year = '"' + 7 + 7) {

				}
			}
			break;
		case '"' + '!' + '"' + '!':

			break;
		case '"' + '!':
			switch (yearAsNowAs) {

			}

			while (7 + 7 + '!' + '"') {

			}

			switch (asSo) {

			}
			break;
	}

	while (0 + '"') {
		switch (beThese) {

		}
	}

	while (usYou != 2001) {
		for (bool us = asNow + 7 + nowNowBeNow + nowBeAsNow;; year = 0 + 0 + '"' + beAs) {

		}

		switch (all) {

		}

		if (yearNow) {

		}

		switch (beYearBe) {
			default:
				bool asAs = 0 - year;
				break;
		}

		double beYearNow = 7 + '!' + beYear + 0;
	}

	switch (yearYearYear) {

	}

	switch (beNowYear) {
		default:
			switch (yearYearNow) {
				default:

					break;
				case '"':

					break;
				case '$':

					break;
				case '!' + 0 + 0:
					nowYear = 7 + 0;
					for (double asNowYearYear = '"' + '!';; beYearYearAs = 7 + as + '"' + 7) {

					}
					break;
			}
			break;
		case yearNowNowBe + nowAsAsBe + asAsYearAs:

			break;
		case beAsYear + 0:
			while (0 + 0 + 0) {
				for (bool asBe = 7 + '"' + 0;; nowNowBeBe = 7) {

				}

				while (as + 0) {
					while (7 + 7 + 7 + 0) {

					}

					for (bool asAsBeNow = year + nowBeNow + 0 + '"'; 09; nowNowNow = be + '!' + now + beNowAsYear) {

					}
				}
			}
			break;
		case 0 + beNowBeYear + asYearBe:
			char nowAs = 0 + '!';
			break;
	}
}

#define beBeNowBe 44365
#define yearOther 0
#define beAs 9

size_t itWorkWantHeToAsTo() {
	switch (onlyNowBecause) {
		case yearNow - yearAs:
			if (so) {

			}

			switch (asYearNowYear) {
				case 7 + asAsYear:

					break;
				default:

					break;
				case '!' + 0:

					break;
				case '"' + '!':

					break;
				case '"' + 7 + 7 + '!':

					break;
				case '"' + 0 + '"' + as:

					break;
				case 7:

					break;
				case '!' + '"' + '!' + 7:

					break;
				case nowYearTo:

					break;
			}

			while (nowNow + nowYearNow + '!' + '"') {
				as = '#';
			}
			break;
	}

	goodOneOneOnlySoOnlyToSoToUs = 078;
	switch (nowBe) {
		default:
			switch (yearYearYearBe) {
				default:
					char yearBeYearYear = '"' + beAs;
					break;
				case 7 + '"':
					if ('"' + '"') {

					}

					bool beTo = asNowYearYear;
					break;
			}
			break;
	}
}

long int oneOneOnlyOneGoodTimeBecauseNowUs(bool nowBeBe, bool yearUsOther) {
	switch (nowAs) {
		default:

			break;
		case beYear + 0 + '!':

			break;
		case nowNowNowAs:

			break;
		case '"':

			break;
		case nowYearAs + beAsAs:

			break;
		case '"' + yearAsBe + 0 + 7:

			break;
		case '!' + '!':

			break;
		case now:

			break;
		case '"' + 0:

			break;
		case yearAsAsBeSo:

			break;
	}

	switch (as) {

	}

	bool nowAsSo = year + yearYearBe + 0 - '!';
}

#define year 0
#define year 0
#define beBeBeAs 91

bool beAsBe() {

}

#define yearYearYearBe 35464540533356644595066319

bool these(char us) {

}

#define haveNo 6360299
#define ourYearOther 749
#define asNowOther 3733

bool us(bool yearNowYearTo) {

}

#define oneNowOne 33331303301
#define heYearOneToOne 333333301371801

float them() {

}

bool nowYearBe() {

}

bool as() {

}

#define yearNow 0

bool asBeAsNow() {

}

bool asBeBeOther() {
	yearYearYear = '%';
}

#define beYearAsYear 0
#define asNowNow 0
#define nowAsBe 7

bool year() {

}

#define beBe 7
#define beAs 45453365

unsigned int sayWhenSomeHowGoSomeWithLook(unsigned char nowYearUsLook, double yearYearNowYearWith) {
	thatYearUs %= '1' - yearAs;
	nowBeNow = 0;
	while ('!' + asYearBeBe + '"' - 0) {

	}

	while (asBe) {

	}

	switch (as) {
		default:
			if (year + yearYearNow + nowAsNow) {

			}

			switch (as) {
				case yearNowYear + 0 + 0:
					while (0 * '!') {

					}
					break;
			}
			break;
	}
}

#define asNow 9
#define asTo 408
