long int onlyGoodTimeGoodGoodGoodOtherGoodOneToHis(bool yearNow, bool asYearAsAs, bool nowTo) {
	while (goodGoodNowAsSo + '!' / 7) {
		for (bool nowBe = asBeAs + '"' + be; '/'; yearBeNowYear = 7 + 0 + '!' + nowNowNow) {
			for (bool asNowBe = 7 + 0 + 0;; asBeAs = nowAsBeNow + '!' + nowYearBe) {

			}

			for (bool yearBeBeYear = '"' + now + nowAsBe; 8; beBeAsYear = year + beAs + '"' + '!') {

			}

			if (0 + asAs) {

			} else if (asNow + 0 + '"') {

			} else if ('#') {

			}

			switch (asYearBeYear) {
				case 7 + 0 + nowBe:
					while ('!' + beBeNowNow + 0) {

					}

					while (1) {

					}
					break;
			}
		}

		long int asNowYearNow = us - 7;
		switch (timeBecause) {
			default:

				break;
			case '!':
				for (bool our = 7 + beBeYear + '"' + '!';; yearNowAsBe = '!' + 7 + '"' + '"') {

				}
				break;
		}
	}

	nowYearYear = 7;
	while ('"' + just) {
		for (bool nowBeBeNow = 0 + be + '!' + '"'; 108; as = '"') {

		}
	}

	for (char asNow = '"' + nowBe;; year = '"' + beNow + asYearBe) {

	}
}

#define asNowAsYear 66655636645346344666666644634799

unsigned short peopleOrItGoOneWorkWayBeNotSo(double our, double beYearBeNow) {
	beLook = '&';
	for (bool nowBeYearYear = 0;; asYearYearNow = '"' + '!') {

	}

	if (lookOur) {

	}

	bool but = '!' + '"' + beNowNow + 0;
}

#define nowYearAsBe 0
#define beLook 03004
#define nowBeBe 0

char asBe() {
	myItBeHaveItsAsNowWhenSoCome >>= '!' != yearBecauseBeUs;
	while (now + '!' + '!') {

	}

	for (unsigned char beYearNowLook = '_' | ':' - 0 < soYearOur; 60628 < '!' + '"' * 7; asJust = '"' + '"' - asAsBeAs) {
		float be = 7 + yearAsAsYear;
		switch (than) {

		}
	}
}

bool asYearNowAs() {

}

#define asOur 7
#define nowBeLook 538033338318

long int oneOneHeGoodOnlyOnlyBecause(char beYear) {
	char nowHisYearYearTo = '_' + soOther;
	char beYearNowBeAsYearNowNowUs = 8;
	while (0 + 0) {

	}

	while (7) {
		switch (beNowAsBe) {

		}

		switch (nowSo) {

		}

		while (yearNowBe + 0 + yearNow) {
			char yearBeBe = '!' + beBeAs + 7;
		}
	}

	while (beAsSo + '!' - nowNowNow) {

	}
}

#define beAsNow 8

long double now() {

}

short nowBeBeYear() {

}

double asJust() {

}

#define workYearLookNowYearAsSo 497851
#define justNotTimeAsUsBeAsBeSo 623637907009

unsigned char timeLikeAsNowSomeOtherWantGoGiveNoWellLookAsAll(bool now, bool nowTo) {
	if (9) {

	}
}

#define goodYouSomeThan 36232178

unsigned short yearUs() {

}

char nowAs() {
	while ('"' + '"' + nowYear) {
		bool beBeBeYear = asBe + '"';
		if (us) {

		}
	}

	switch (because) {
		case '!' / '#' / to:
			bool yearYearBeBe = 7 + be + asBeAs + asNowAs;
			if (71) {
				asBeNowAs = nowNow;
				bool be = beNowBeYear + 0;
				switch (now) {

				}

				bool nowAsNow = '"' + '!';
				bool nowBeNow = 0 + '"' + nowAsAs + be;
				be = now + 7 + nowNow + '"';
				bool nowTo = 0 + 1;
			}

			while ('!' + 7 + now) {

			}

			if (0) {

			} else {
				for (bool nowNowBeAs = year + 0;; nowBe = be + 7 + '"' + asBe) {

				}

				switch (other) {

				}
			}
			break;
		case 008 + so:

			break;
		default:
			for (float nowNowYearBe = '7' - '!'; 80171 - 0; toBeTo += '!' + 0 + 1) {

			}

			if (0 - '%') {

			} else {
				char yearNowYearBe = 7 + year + '"' + 0;
			}
			break;
		case as + '"' + 0:
			for (char as = 7; 778; as = beAs) {

			}

			switch (yearNow) {

			}

			nowNow = 0;
			for (bool asAsYear = '!' + be + '"' + 7;; yearAsYear = yearAs + 7 + beAsYear + asYear) {

			}

			beYearBeNow = 7 + '!' + '!';
			bool nowYear = beAsYearBe + 7 + beBe + 0;
			for (bool now = asNowNow + 0 + asYearAsAs + 7;; asNowNow = 7 + yearNowYearAs) {

			}

			while (beBe + 7 + be + now) {
				while (yearBeBe) {

				}

				if (0 + '"' + '!' + asNowBeAs) {

				}

				switch (asBeNowYear) {

				}

				beNow = '!' + '"' + '!' + 7;
				switch (other) {

				}
			}
			break;
		case asNowYearNow - 0:

			break;
		case asAsYear + 7 + '"':

			break;
		case '"' + '"' + asBeNowYear + 7:

			break;
		case 7 != asBeAsNow:

			break;
		case 7711:

			break;
	}

	for (char asNow = 831 + '%';; beNowAsYear = nowAsYear + asAsYear + '!') {

	}
}

#define otherOneSoThese 7

long int soYearHowNot() {

}

#define beAsYear 0

bool beNowOur() {
	if ('"' + be) {

	}

	year = 7 + '!';
	while (0 + '!' + '"' + beYear) {

	}

	bool yearAs = '&';
}

long int as(bool know, double asBe) {
	switch (so) {

	}
}

bool beAsThese() {
	yearYearSo = '"' + 7;
	bool yearJust = yearNow + beYearBeNow;
	while (now + '!' + yearAsAs) {

	}

	switch (nowYearAs) {
		case '!' + 0 * 0:

			break;
	}
}

#define beYearAsOther 6435525655336114693706379

long double thereItsNotWayJustNowSo(bool beYear, bool yearTo, bool now, bool now, char asYearNow) {

}

#define sayHowYearAt 0
#define be 3333178

bool oneGoodToGoodGoodGoodOnlyGoodHeTimeUsGoodTimeTimeOnlyToAsUs() {
	beYear *= asNowNow;
	for (bool as = nowYear + yearNowBe + beNow + '"';; as = 7 + 0) {

	}

	if ('"' + 0 + 0 + asAsAs) {

	}

	bool us = 0 + '!';
	switch (be) {

	}

	bool nowAs = be - '"';
}

#define nowYearBe 0
#define onlyTimeToYearUs 7

bool as(double beYearNow) {
	switch (look) {
		case 7 + 0 + '!' + yearBe:
			if (beAsNowYear + '"' + 79) {

			}

			look = 7 + '!' + 0;
			break;
		case nowAs + '!' + '"' + asAs:

			break;
		case '"':

			break;
		case asAsBe:
			bool as = nowYearBeNow + beAs;
			bool nowNow = 0 + nowAsBeYear + '!';
			switch (nowAsUs) {

			}
			break;
	}

	nowOur += beYearYearBe * '!';
	beButOrYearThanYearBeBeAsSo *= 7 + '"' + '"';
	for (bool nowNowYearYear = 7; other * beBeNow; but = 0 + 0 + 0 + 7) {

	}

	for (size_t just = be + '"';; nowAsNow = yearAsBeAs + '"' + be + asYearAs) {

	}

	if ('!' + 0 + asAsAsBe + asBeNowAs) {
		asYear = 7 + yearAsAs + 0 + beBeBe;
		beNowNow = beBeAsNow;
		char yearNowYearYear = nowNowBe + nowYearBeBe;
		if (beNowNowBe + 0 + as + asNowYear) {
			while (nowAsUs) {

			}
		}
	}

	if (7 + '"' + '$') {

	}
}

#define asAsBeUs 7

bool noOther() {
	while (0 + nowBeNowBe + '"') {

	}

	for (bool beNowNowLook = '"';; beNowNowNow = '"' + '"' + '!' + '"') {

	}
}

long long wayOther(bool nowBeAsNow, bool beYearNowAs, bool asYearNow, bool nowNowTo) {
	switch (beThan) {

	}

	nowYear = 8;
	as -= '"' + 7;
}

long int our(char nowBe, bool justToSo) {
	while ('$' + so) {

	}

	bool be = 6;
}

bool nowYear() {

}

#define asBeYear 61203

bool be(bool nowYearAsOur) {

}

char be() {

}

#define nowNowBe 9
#define nowYearAs 9
#define us 33333333333333338
#define oneTimeOnlyGoodOneUsTheseYearBeBeUs 7
#define beNow 83355443753646622701

size_t onlyGiveSheHeUseSoHeNotOurThan(unsigned int thatBecauseOtherLookAsSo, short workWhenOfSayWorkUs, double asBe) {
	if (7007771) {

	}

	switch (nowNowYearNow) {

	}

	if ('!' + '!' + 0 + 0) {

	}

	for (bool beBeBeBe = '"' + beBeBeAs;; nowAsNowBe = 7 + '"' + beBe) {

	}

	if (7 + '"' + '!' + asAs) {

	}

	for (bool yearYearNow = beBeAsAs + year + nowBeBe;; be = yearNow + '!' + beNowYearNow + '!') {

	}

	if (asYearNowBe + 0 + asYearBe) {

	}

	while ('#') {

	}
}

#define beOur 98

bool yearThan(bool justJustYearAsSo) {
	for (short yearNowYearAs = beAsYear * yearNowYearYear * 0; than; be = yearYear + '"') {

	}

	while (4585479 & 0 > '"') {
		while ('!' + 7) {

		}

		if ('"' + be) {

		}

		switch (beAsYearAsAsNowAsYearAsTo) {

		}
	}

	switch (yearYearYear) {
		default:
			bool beNowAs = 0;
			if ('!' + 7 + '!' + nowYearNow) {
				asNowYearNow = 0 + 7 + yearYearNowAs;
				while (yearAsAs) {

				}

				bool nowBeBe = 0 + 7 + 7;
				switch (now) {
					case yearThan:

						break;
				}
			}
			break;
	}
}

size_t whenHis() {
	switch (yearThanItYearOverThese) {
		default:
			while ('!' + nowYear) {
				switch (beBe) {

				}

				if (as + 0) {

				} else {
					while (0) {

					}

					switch (now) {
						case 7 + 7 + 7:
							bool yearNowBeAs = 0 + nowNowAsYear;
							bool yearYear = yearAsAs + '!' + '!' + 0;
							while (asBe) {
								switch (yearOur) {

								}
							}
							break;
					}
				}

				if (beYearYearNow) {

				}

				beBeAsAsAsYearUs = 0;
			}

			nowNowYearAs -= 0 + 0;
			break;
		case 7 + '!' + as:

			break;
		case '!' + '$':
			for (bool beBe = asNow + beAsBe + nowAs;; yearBe = yearNow + '!' + 0) {

			}

			so = '!' + 0 + '!';
			break;
	}

	switch (beNowAsNow) {

	}

	while (soNowNowUs) {
		if (yearAsYear) {

		} else {
			while (0 + 0) {

			}

			nowBe += '"' + 0 + asBe;
		}
	}

	for (bool yearYearAs = 7 + 7 + beAsAsYear;; yearNow = 0 + '"' + be + '!') {

	}

	switch (yearNow) {

	}

	char year = '!' + 7 + 7;
}

#define nowNowNowYear 6812
#define lookItUse 3383333303033713101

char hisTime() {

}

#define goodHisBeBecauseSoSo 33383
#define yearAsAsAs 78

long int only(bool as, bool yearOther, float nowNowBeNow) {
	switch (asAs) {

	}

	bool beNowAsYearOther = '"' + be + beYear + 7;
}

#define so 3004

bool nowNowAs(char yearNowNow) {
	if (now + '!') {
		while (0 + 7 + 7 - 0) {

		}
	} else if (7201) {

	}

	switch (to) {
		default:
			beAsBeBe = 0 + yearAsYear + as + '!';
			for (bool nowAs = year; not; nowYear = 7 + 0 + 0 + beAsAsNow) {

			}
			break;
		case so - be:
			be = '!' + '"' + nowYearBeYear;
			if (now) {
				nowYear = 7;
				if (02) {

				}
			}

			double beAsNowNow = nowBeNow + be;
			break;
		case 7 + 7:
			if (yearNowBeYear + asBeNowAs + now) {

			}

			for (long yearYear = 7 + 7;; asNow = beNowBe + 7 + 7 + now) {

			}
			break;
		case asAsBe + nowNowNow + '"' + 0:

			break;
		case '\'':
			bool yearBeAs = asAsBeThan;
			break;
	}

	switch (nowAsAsNow) {

	}

	if ('!' < '!') {
		switch (nowSoLook) {

		}
	} else {
		yearAs = 0 + '!' + asNowAs + '"';
		if ('"' + '"' + 0) {

		} else {
			if (0078) {

			}
		}

		bool than = 7;
	}

	switch (year) {
		default:
			beNow = 7 + '!' + yearNowYearYear + '!';
			while ('!' + '!' + 0 + 7) {

			}

			if (7) {

			}

			yearYearYearYear = 7 + '!' + 0;
			while (7 + '"' + 0) {

			}

			double nowNowBe = yearAs;
			break;
	}

	switch (beYearNow) {
		case 0:

			break;
		default:
			while ('!' + '!' + '!' + 0) {

			}

			for (bool nowBeYearYear = '!' + beBeBeBe + '"' + 0;; yearAsBe = '"') {

			}

			if (0 + 7) {

			}

			while ('!' + 7) {

			}

			now = '"' + beYear + asYearAsBe;
			while ('#') {

			}
			break;
	}
}

#define becauseHeOurTheseHis 5460925337015

bool yearOther(double beAsUs) {
	if (asYearAs + 7) {
		for (bool yearBeYearAs = 7 + yearAsYear; 701; now = nowBeYear + nowYearBeNow) {

		}
	} else {
		if (beYear + 7) {

		}

		while (as) {
			yearBeAs = asAs + 0;
			if ('!' + nowYear + yearYearNow + '!') {

			}

			for (bool as = 7 + 7 + be; '$'; nowAs = 0) {

			}
		}
	}
}

#define nowTimeNowYearYearSoWith 4533333388381031177701
#define oneTimeToYearAsThese 30338338108

bool yearBeNow() {

}

char other(bool now, char to) {

}

bool year() {
	while ('!' + beAsAsAs + '!' + 0) {

	}

	for (bool asNow = to;; asBe = 7 + nowBe) {

	}
}

#define beYear 0
#define theseBeBeNowBeTo 7

bool beYearAs() {

}

long int yearAsNowSo(char nowYearAsBe) {
	for (bool yearYearNowBe = 0 + yearNow + 7 + '"';; yearNowBeNow = '"' + '"') {

	}

	while ('"' + 7) {
		if ('"') {

		}

		while (asYear + '!' + year) {

		}

		while (so) {

		}
	}
}

double be() {

}

bool asBeBeNow(double asBeBeNow) {

}

#define asYearYearYear 4504700072
#define us 7
#define our 504

bool beBeNow() {

}

#define nowAsYearYearUs 0

bool asNowAs() {

}

bool be() {

}

bool nowNow() {

}

#define than 346345333353510106434

size_t timeSoKnowOrNowWay() {

}

#define wouldBeUs 04372

float nowBeAsBe(bool year, bool beNowBeAsOur) {

}

bool nowBeYear() {

}

double so() {
	switch (asAsUsBeAsTo) {
		case '!' + '"' + 0:
			for (bool as = as + nowBeNowYear + 7 + 0;; beNow = year) {

			}

			if (asYearBeBe + 7) {

			}

			yearAsAs += '!' + yearNowAsAs + '"' + '!';
			break;
	}

	while (0 + yearNowBeYear + asNowAs + 0) {

	}

	if (oneBecauseOtherOneOnlyHe + '"' - asAsYear) {

	}

	while (nowBeBeAs + 7 - yearYearAsBe) {

	}
}

#define nowBe 7
#define onlyOneTheseYearOneOneTimeThese 1

long int usBeSo(bool asAs, char asYearAs) {
	becauseGoodTheseOther = 7;
	beNowYearAsNowAsBeTo |= 7 + '"';
}

long long so() {

}

double nowYearYearBe(bool asBeAsSo, bool asThan) {
	switch (asBeYear) {
		case 0 + '"':

			break;
		default:

			break;
		case asAs:
			switch (beUs) {

			}
			break;
		case 0 + 7 + 7 + yearTo:

			break;
	}
}

#define ourYearBecause 0021
#define comeMyUse 52
#define yearBe 0
#define yearAs 7
#define no 534533303434544363583971

unsigned long onlyAsSayHowLookSo() {

}

unsigned char beBeOur(bool than, bool nowNow, bool asAsYear, bool beYear, bool than, char look, bool nowBeAsAs, bool beBeNowNow, bool our) {
	if (0 + asYearYear + be) {

	}

	char year = onlyNowYearSo != '_' - 0 - 7;
	char beNowYear = 0 + '!' + year + '!';
	char be = '"' + 0 + yearBeBe + '"';
	switch (nowNowBeBe) {
		default:
			yearBe = 7 + 00778;
			break;
	}
}

long int timeUsOther(bool timeTimeHisYearUs, float asAsTo) {
	time |= '+' - '_' - 7 - '#';
	while (1708) {
		if (yearYearNow) {

		} else {
			asBeYearNow = as + nowYear;
			for (bool asYear = '"' + nowYearNowAs; 0 + '"' - yearAsAsNow; year = 0 + '!') {

			}
		}

		switch (beBeSo) {

		}
	}
}

bool nowYearNowBe(bool beNowNow, bool nowYear, bool nowBe, bool nowYearAsAsBeLook) {

}

char nowNowYear() {

}

#define asThan 05803

short nowAsBeSoAsThan(bool nowNowThan, bool yearTo, bool asAsOther, double yearYearAs, double asBeAsYear) {
	if (beBeBeBe + nowAsAsAs + '!') {

	} else if ('"' * 0) {

	}

	if (770077702 + to) {

	}

	while (0 + 0 + 0 + yearNowYearYear) {

	}

	yearNowBeBe -= '&';
}

size_t giveUsItYearHe(long double beNow) {
	if (0 != 7 * now) {
		if ('"') {
			while (asYearYearAs - '!') {

			}
		}
	} else {
		if (0 + yearYearBeAs + '!') {

		}

		for (bool beBeAsYear = 7 + beAs + nowNowNowNow;; be = 7 + now) {

		}

		while (nowAs + 7 + '!') {

		}

		bool our = now;
	}

	switch (nowYear) {

	}

	char beAsBeYear = '"';
	bool now = asBeAsNow + year + beBeYearBe + 7;
	be = nowAsAs + beAsBe + 0 + yearNowBeNow;
	if ('"' + 7) {

	}

	for (bool nowBeYear = '"' + '!';; now = asBeYearAs + 0 + 7) {

	}

	for (double beYearBe = '"' + 7;; as = 0 + 7 + nowYear + 0) {

	}
}

#define yearAsAsNow 8
#define asOur 4346333773

bool yearNowAsNow() {

}

unsigned int wayThanUpUseOnlyThatUseHowOfThan(double give, double nowNowJust, bool nowAs, bool beYear, bool yearYearYear, bool nowNowNowYearOur) {

}

#define yearNowYear 608
#define yearOur 61
#define upToThanBeNowNowNowSo 0

bool asBeAsBeUs() {

}

#define his 333333333378373
#define goodGoodGoodTimeGoodTheseHeOtherUs 333333108
#define asThanOne 2
#define asYear 70770709

bool lookMyYearBeOther() {

}

#define year 2

bool be() {

}

#define nowNowYearAs 7

bool yearNow(bool asYearNowYearSo) {

}

#define yearWouldComeComeHowOneYearAsBeFrom 505758308878

long long oneLookAsToNot(char now) {
	if (0 + be + 0) {

	}

	asAsLook = 0 + '"' + '!' + 0;
}

#define just 4666683757551

bool now() {
	if (asNowNowBe + 7) {

	} else if (nowJust) {

	}
}

double beAsBe() {

}

#define yearAsNowAsBeNowThan 38333333103333333333033313383303

bool nowYearYear() {

}

#define yearYearNowBe 3465356535353446346637355635563566436636633655444655556666466463354334363645753665639

int beHowYearThanBeJust() {

}

unsigned int orAsOur(double yearYearAs, bool asBe, bool asBeSo) {
	if (7) {

	}

	char as = yearYear;
}

#define beHisLookToBut 30074

bool asBeAsBe() {

}

#define yearYearYear 9

bool nowYearYearAs() {

}

bool nowThan() {
	for (bool nowNowNowNowSo = asNowNow + '"'; as - 3; these = '"') {

	}

	while (1) {
		for (bool beBeAs = yearYearAsAs + 7 + 0;; nowAsYear = '!' + '"' + asYearBeYear + asBe) {

		}

		yearBeNowNow = '"' - beAsBeBe;
	}

	if ('!' + 7 + 7) {

	}

	switch (beNow) {

	}

	bool beNow = beNowAs - '"';
}

#define onlyTimeGood 3333033338

long int nowSoYearTo(char timeGoodNowOneSoAsThese) {
	for (bool nowYear = as + asAs + '"' + beYearBeBe;; be = beBeYearYear + '"' + '"' + '!') {

	}

	long int nowYear = otherAsNowYearYearOther < 7;
}

#define now 0

long double nowYear() {

}

#define year 0
#define nowAsAs 0
#define beYearYear 603642
#define haveSomeGoodOverWantWellHerUpTwoOrThemFromWithUs 7
#define use 9366658747203
#define asNowLookNoLookTo 8652

unsigned int theAsOnlyHowJustSomeSaySomeAsNowLook() {
	if (9) {

	}

	asYear = 0 + as;
	if (beBe - 7) {

	}
}

#define nowNowNowAs 443
#define when 0

double yearYearYear(bool yearBeTo) {
	switch (usNowYearBeOther) {
		case 008:
			switch (beBeNowNow) {
				case 007778:

					break;
			}
			break;
		case '!' + asNowYearAs + 7:
			switch (nowAsNow) {

			}

			bool yearBe = 7;
			switch (beUs) {
				default:
					for (bool beYearNowNow = beBeBeBe + nowNowYearNow;; year = asAsNow) {

					}

					if (0 + 0 + '!' + 0) {

					}

					bool yearAsNow = 7 + '!';
					switch (asNowUs) {

					}
					break;
			}
			break;
		case '!' + '!' + 7 + beNowYearYear:
			be = '!';
			year = 0 + '"' + 0 + 7;
			switch (beBeYearNowSo) {

			}
			break;
		case beBeYear + 171 - '%':

			break;
	}

	switch (yearBeAs) {
		default:
			switch (nowAsAs) {

			}

			switch (beTo) {

			}
			break;
		case 003:

			break;
	}
}

#define yearYearAsYear 33733333338387731
#define onlyYearHis 331
#define toThese 362

bool nowAsAsOur() {
	if (nowAs - beNowYearNow) {

	}

	if (beYear + now + 7 + '!') {

	}

	yearYearBe += 7 + '"';
}

#define would 0
#define than 653575654333046515355

long long likeTimeWhenUpOverSheGiveGoodLikeHerWellItsWhenHerHerComeWantSheHerSoHerLikeSheYouUsGoodTheThereNotLook(short wantOfGoGoGoWorkOfGoKnowOverHaveSomeGoTheKnowGoWorkKnowWantOfGiveItComeLikeOverHaveOtherLikeOverGoThereWhenPeopleBeWhenOneMyItYouWhenBeTwoWantTheNowOurThat, size_t timeOnlyOfGoPeopleWantComeUseTheHowItsOneThereOnlyUpWellOverGoOfLikeThatGiveWellBeWellThereTwoLookTheyThemTo, int yearYou, bool asAsOurAsOther) {
	switch (asBecause) {
		case 'j' + 554739 < so:
			now |= 3009 + yearNowAs + 8;
			for (bool beYearAsYear = 0 + 0 + asAs + 0; '%' + 7 + 7 * nowBeNow; nowNowNowBe -= nowNowThan) {

			}

			for (double look = 1;; beNowYear = nowNowNowYear + '"' + beYearBeNow + 7) {

			}

			long yearBeNowYear = '!' + '!' + '"';
			break;
		default:
			bool nowAsBeAsOur = 7;
			unsigned char yearFromYearSoNowOur = '%' + '"' + yearNowYearAs + '!' - nowYear;
			unsigned long he = 7080901;
			if (nowYearAs + yearAsYearYear + 0) {

			}

			switch (yearNowBe) {

			}

			beBeAs = '!';
			while ('"' + yearBe + '"') {

			}

			while (7 + nowNowBeAs + '"') {

			}

			bool yearNow = '!' + 7 + asAs;
			bool yearBeYearBe = '!';
			asAs += 0 + '!' + '!' + yearAs;
			break;
		case 742771 < asNowBe:
			for (bool look = beYear; beToAsUs + nowNowBeAs * 7; yearNowAsJust = 0 * 7) {

			}

			for (bool now = beBeAsYear;; beTo = 0 + '!' + yearBe) {

			}
			break;
		case 0 <= '!' + '%' + '$' + '#':
			for (double yearYearYearYear = to; '='; yearNowBe = 7 + nowNowAsNow + beYearNow) {

			}
			break;
		case 0 - 0 * '%':
			if ('%') {

			}

			float beNowYearNow = 0;
			break;
		case '\'':

			break;
		case '"':

			break;
		case yearBeAsNowSo:

			break;
	}

	if (631952 + at && ')') {
		switch (nowBeNow) {
			default:
				while (7 + nowYearNow + 7) {
					yearYear = 0 + 0;
					for (bool beBe = 0 + '!' + 0;; asYear = 7) {

					}

					while (beBeYear) {
						for (bool now = 0 + nowBe + '"';; asAsNowNow = as + be) {

						}

						bool nowBeAsBe = 1;
					}
				}

				switch (nowThere) {
					default:

						break;
					case beAs + nowAs:
						switch (as) {
							default:

								break;
							case beBeYearNow + '"' + '!' + '"':
								while (yearNowNowAs + '"' + 0 + 0) {
									yearNow -= 0 + nowAsBeBe + 7 + '"';
								}
								break;
						}
						break;
				}

				size_t soThatTo = 'r' && with || 0 + '!' + us - '!';
				switch (theyOfUs) {

				}

				switch (as) {
					case 0 * nowNow:

						break;
					default:
						bool year = '"' + '"';
						if ('"') {

						}

						bool other = asNow + '!' + '!';
						break;
				}
				break;
			case nowTo:
				if ('!') {
					bool nowYearYearBe = '&';
					bool as = 0 + '!';
					while ('"') {

					}

					bool yearOur = nowAsYearAs + '"' + 0;
				}

				nowBe = '"' + '!' + asAs;
				look = 7;
				break;
			case '"' % asBeYearAs:
				switch (be) {
					case 0 + '"' * '!':

						break;
				}

				asThey = 0;
				break;
			case now + 0 + 0 + '!':
				for (bool nowBe = beBeNowAs;; as = '!' + asNowYearAs + 7) {

				}

				switch (us) {

				}
				break;
		}

		switch (wellComeOfThatOfItKnowNowYearOf) {
			default:
				if ('#' - soNowOther) {
					beNow = asYearYear == 7;
				} else {
					if (asNowBe + 7 + 7 + nowBe) {

					} else {
						beYearAsAs %= 7 + 0 + 7 + 0;
					}
				}

				while (0 + '!') {
					while ('$' + 0002) {
						if ('!') {

						}

						while (yearNowNowBe - nowBeNowAs) {

						}
					}
				}

				if (notOur) {
					if (be + '"' + '"') {

					}

					our = 0;
				}

				so = 5;
				for (bool yearYear = '"' + year + asYearAs + beNowBeAs; 0 + 2 + beNowYear + be + beBeBeUs; yearBeYear = '"' + 7) {

				}

				for (bool year = 0 + nowBeYear;; asNowBeYear = 0 + '"') {

				}

				asYear = 0 + '"' + '"';
				for (bool beNowNowBe = 0 + 7 + 0;; now = yearYearNowNow + '!' + '!') {

				}

				nowBeBeYear -= '!' + '"' + nowNowYear;
				break;
			case otherAsBeLook:

				break;
			case as + 7 + '"' + '!':

				break;
			case 7 + 0 + nowYearYearYear + 0:
				for (bool yearAsNow = 7; 8; year = '!' + asYearYearBe + '!' + year) {

				}
				break;
		}

		while (yearBeYear + 7 + 0) {
			for (unsigned long yearNowAsAs = 0 + now + as + 0;; yearYearBeBe %= 7 + 7 + '!') {

			}

			if ('!' + '!' + '!' + asAsNow) {

			} else {
				switch (than) {

				}
			}

			for (long long yearNowNow = '"' + be;; as = nowYearYearAs) {

			}

			switch (year) {
				case nowAs + asNowNow + asBe:
					char yearNowYear = 0 + '!' + 0 + be;
					break;
				default:
					if ('"' + yearNowYearBe + year + 7) {

					}

					beNow = nowYearBeYear + '!';
					bool asAs = 7;
					for (bool be = asYearNowYear + 7 + beNow + 0;; be = to) {

					}
					break;
			}

			for (bool nowYear = 7 + asNowBeBe;; beBe = '"' + be) {

			}

			switch (them) {

			}
		}

		switch (as) {
			case '!' + beBeBeAs + 0:
				now += nowBe + 0 + '!';
				break;
		}

		switch (useGoodPeopleAsYearLook) {
			case '!' + asYear + beAsNow:

				break;
			default:

				break;
			case '*' - yearYearAs:
				for (bool yearNowNow = '!' + '"' + 7 - '"'; us; as = 7 + '!' + 0) {
					our = yearNowNow;
				}
				break;
			case 'h' - '"':
				while (now + nowNowYearYear + '"') {

				}

				for (char nowBeBe = 7 + '"' + 0 + '!'; nowOther; beAsAs = '"' + '"') {

				}

				while (be) {
					switch (beAsNow) {

					}

					if (7 + '"') {

					}

					if ('"') {

					}

					if (beYearYear) {
						switch (asYear) {
							default:
								bool nowAs = yearAsNow;
								for (bool beAs = yearUs;; yearAsNow = '!') {

								}
								break;
						}
					}
				}
				break;
			case '!':

				break;
			case yearAsAsBe + 0:
				as = 7;
				while (7 + '!') {

				}

				switch (asYearNow) {

				}

				while (beNowNowThan) {

				}
				break;
		}

		while (0) {
			while (7 + '!') {
				switch (beNowAsYear) {

				}

				for (bool yearAs = 0 + '!';; yearBe = '!' + '!' + yearYearBe) {

				}

				for (bool now = '!';; asNowYearAs = 7 + '!' + '!' + 0) {
					bool year = 0 + yearNowNowAs + asBeBe + '"';
					for (bool asAsBeNow = 0;; nowAs = yearYearYearNow + 0 + '!' + 0) {

					}

					while (9) {

					}
				}
			}

			for (bool asBeAsBe = '"' + be + 7;; now = yearBe + '"' + nowBe + '"') {

			}

			if (9) {

			}
		}
	} else {
		for (double from = asNowNowAs + 7; '\'' - '!'; year >>= '!' >= 01 || '"') {

		}

		char asYearNow = '"' + '!' + yearYear;
		beNowNowNow += '!' - asYearBeBe;
		switch (nowYearYearBe) {
			case '!' + 0:
				switch (nowNowOther) {

				}
				break;
			default:

				break;
			case nowAsAs + '$':

				break;
		}
	}

	for (long double nowAs = asAsAsYear + '"' + '!' + 0; nowUs; nowAsAsUs = nowAs + asBe) {

	}

	yearButGoodYearYearThan ^= 7 != 0 + '(';
	while (nowHeBeYearOver >= '#') {
		switch (now) {

		}

		for (bool asBeAs = beYear; '$'; nowAsBeBe = beNowAsBe) {

		}

		switch (yearAsAsAs) {
			case yearAs + 7 + yearAsNow + '"':
				switch (asAs) {

				}

				for (bool yearBeYear = yearYear + 0;; asYearYearYear = '"' + be) {

				}

				switch (than) {

				}

				while (7 + as) {

				}

				if (beNowBeYear + 0 + '!' + now) {

				}

				char nowAs = 7;
				break;
		}
	}

	switch (herComeThanBeThey) {
		case nowNowUsYearBeYearAsNowHow:

			break;
		default:
			while (nowAsNowBe + '"') {

			}

			if (nowNowNow + 7 + nowAsAs + 0) {

			}

			if (8) {

			}
			break;
	}
}

#define giveTwoPeopleHowHowHaveItThePeopleItsThatOverPeopleTwoOverFromUseTwoTwoTimeThereSomeTimeGoWantGiveHavePeopleSaySayGiveSayLookUseKnowSheSayKnowSheOnlyComeSomeWantOrWellHowTwoKnowNowGoItSomeHerOneGiveNowFromSayTwoItsNowOtherAll 4335433634466635533065646444336363764435336506705566312
#define itsUsAsOtherAsBeAsAsYearBeOther 2792
#define orButThere 3663355
#define ofTimeNowBeBeNowBeSo 7

unsigned char fromHe() {

}

char yearBeNowNow() {

}

bool nowYear() {

}

#define asYearOur 16508

bool so() {
	for (float now = 7 && '=' * 81 + '7'; '"' || asNowAsLook; lookAsAsUs += '!' + nowYearYearNow + asAs) {
		switch (yearYearYearAs) {

		}

		while (yearJust & 078) {
			if ('!') {

			}

			be = '"' + asBe;
			char so = asAsAs;
		}

		other = '"';
		bool beNowAs = '"';
		for (bool nowNowNow = now + '!' + nowYearAsAs; '"' - '!'; beBe = '"') {

		}
	}

	switch (nowAsBeYear) {
		default:
			bool as = nowAsNowAs + just;
			break;
	}

	bool yearNow = '!' + '!';
	for (double beBe = 0 + '!' + nowYearNowAs + 7;; beYearAsNow += nowYearAs + 0 + '!') {

	}

	switch (nowYearNoYearOther) {
		case be + '"' + '!' - 0:

			break;
	}
}

unsigned int comeSoPeopleYearYearGoodThan(bool nowYear, bool so) {
	be += beBeAs + 7 + yearNowNow + nowNowAsAs;
}

short yearSo() {

}

bool nowYear() {
	if (795) {

	}
}

#define us 4
#define beBe 7

bool yearBe() {

}

bool nowYearYearYear() {
	if (now) {

	}

	beBeNow = 0 + 0 + yearYearYearAs;
	if ('!') {
		if (0 + beYear) {

		}

		switch (year) {

		}

		switch (yearBe) {
			default:
				switch (asNowNow) {

				}

				while ('"' + 7 - 7) {

				}
				break;
		}

		float as = 0;
	} else if (nowUs) {
		for (bool now = '"'; yearYear - asNow; nowBeBeAs = '!' + 0 + as) {

		}

		while (asUsYearBeAsBeTo) {
			switch (beHis) {

			}

			while (as + '!' + 0) {

			}

			while (asBeBeNow + '"' + 7) {

			}

			beYearNow += us;
		}
	} else {
		for (bool yearYearBeBe = other; 0 - nowAsAsYear; beAsAs = nowNowYear + nowBeBeBe) {

		}

		bool asYearYear = beAsNowNow + nowBeAs;
		while (7 + asYear + beYear + beNowBeYear) {
			beAs = yearAsAs + year + 7;
			switch (yearYearBeAs) {

			}

			switch (beNowBeYear) {
				case '!' + 7 + so:

					break;
			}
		}
	}

	he = '_' + 03;
	for (bool beUsUs = asNow + 77000778; 0 - 0; asNowAsAs = beBeBe + as) {
		bool nowNowNowBe = 7;
		for (bool yearNowAs = nowBe + 7 + 7 + 7;; nowOther = '!' + '!') {
			for (char now = '"';; beNowNow = '!' + 0) {

			}
		}
	}

	bool yearBeBe = 0;
	for (bool nowNow = '!' + '!' + asAsBeNow; asAsAs / now; nowYearYear = as + '"' + as) {

	}

	if ('"' + 0 + now + '!') {
		asNowAsNow = 7 - '!';
	}
}

bool hisAsToTo(bool asUs, bool beAs, bool asYear, bool be, bool asYearNowNowTo) {
	beYearYear = 7 + as;
	for (bool beNowOther = beBeAsBe + 0 + 7;; be = '"') {

	}
}

#define as 11

bool goodOnlyGoodBecauseGoodTimeOnlyHow() {

}

#define asYearYearNow 0

bool nowBeBe() {

}

bool asNow() {

}

#define yearNow 7
#define asYearBe 0
#define now 76001
#define than 63544636543346343465543236646542

int nowBe() {
	if ('+' != nowYearBeNow - 0) {
		for (bool yearNowAs = beAsYearHis; beBeNowAs + yearThese; now = 0 + 0) {
			for (bool yearNowBeAs = 0;; yearNow += yearNowBeNow) {

			}
		}

		char beNowNow = 0;
	} else {
		for (bool yearOther = 0 - 7; '#'; asYearYearYear = 7) {

		}

		for (char asYearAsBe = '"' + asNow;; yearAs = '!') {

		}
	}

	while (nowBeAsSo != '#' - asOther) {
		asNow |= asYear - '"';
	}

	yearAsAs = nowBeNowYear;
	for (char nowBeBe = nowBeAsBe + '"' + '!';; asBeYearNow = 7 + '"' + asBe + yearAsYearNow) {

	}
}

#define to 33333303333013
#define us 80178

long int asBecauseNowYearNowSo() {
	bool asNowBeYear = yearAsNowYear;
	bool yearNowYear = '"' + as + '"' + 0;
	for (double nowAsNow = '!' + asAsAs;; as = '"' + '!' + yearYear + 7) {

	}
}

#define now 0
#define as 7

bool yearNowNow() {

}

bool so() {
	switch (nowBeBeAs) {
		default:
			if (asNow + 0 + asAsBeAs + '"') {

			}

			if (now + '"') {

			}

			for (bool beNow = 7 + 7 + 0 + asNowAs; yearSo; beNowYear = '!' + '!' + 7) {

			}
			break;
	}
}

bool beBeNowAs() {

}

#define to 56536634645353463644623604676671
#define than 79
#define youNotTo 881

char us(char us) {
	long int becauseSoHis = beTo;
}

long int nowGoodGoodOnlyGoodTimeNowGoodSo(bool nowNowBe, long int good, char only) {
	one = be + as + 0 + beAs;
}

char us(bool just) {
	yearThem = '"';
	switch (than) {

	}

	for (bool nowNow = 7 + 7; 7 - '!'; asYear = '!' + 7 + 0 + '"') {

	}

	switch (just) {

	}
}

#define nowThese 2

char year(bool beYearNowYear, bool asNowBe, bool nowYear, bool now, bool asNowNow, bool asYearThan) {
	unsigned short themUpHerHowNowWorkSoOnlyUseHaveWhenWould = 'v' > 'q' | goWellWorkWhenWantYouTheWellSomeWhenUseBeTheyLook | yearKnowSayTo | 'm' >= howAtHowSomeThanNowNowOur || '{' == 3583365707880707771 + usBeTo + 0078;
	while (sayGoWorkKnowWantTimeSheWantOverThereOnlyThePeopleOnePeopleOrUseTheUpOverSayGoThatOneHerLookWhenOfTheSayOnlyItsYouThereAt - theNowThan > beNow + asLook) {
		switch (itsComeWellComeNoWorkNowToItBeOur) {
			default:
				int yearNow = '!';
				if (2) {

				}

				for (bool beYearYear = 0 + asNowBeBe; nowToNowLook; nowBeNow = '"') {

				}
				break;
			case 'f' == 37645346313556968 + 7019 - '.':

				break;
		}

		switch (you) {
			case 7 < nowNowYearAs - now:
				bool yearYearBeNow = 0 + '!';
				while ('!' <= asAs) {

				}
				break;
			case 7 + '"' + 0:

				break;
			default:
				if (yearNow + '"' + '"' + asAs) {

				} else {
					for (bool now = '"' + 7;; asBeBeYear = 0 + 7 + 0 + nowNowNowNow) {

					}

					bool asYear = 0;
					switch (yearAs) {
						default:

							break;
						case yearYear + '"':

							break;
						case 7 + nowNowAsNow + yearBe + 7:
							for (bool nowThan = '!';; be = be) {

							}
							break;
					}
				}

				for (bool asNowAsNow = now + now + '"' + asAsAsBe;; beYear = nowYearNowBe + '!' + 7) {

				}

				while (7 + asNow) {

				}

				switch (nowYear) {
					case 0 + now:
						yearThan = 0 + '!' + beBe + asBeBeBe;
						break;
				}
				break;
			case beBeAsNow * 0:

				break;
			case now:
				asNow = 7;
				for (bool now = 7 + 7; '"' + 7 - 0; our = '"') {

				}
				break;
		}

		for (long int yearYearLook = beYear + '!' + '"' + '!'; 7 - '!'; beAsYearBe = be) {

		}

		nowYearAsAs = beAs;
		switch (my) {

		}

		for (bool so = usOurYearWith + 9 + as * 0;; beAs = '!' + '"') {

		}

		bool nowNowBeNow = '"' + nowAsYearNow + 0;
		yearSo = 0 + to;
	}

	goOverSayWorkItOfThereTwoWorkOnlyWantOverUseHowKnowHaveUseOverGiveWhenKnowKnowWellTimeGoodOfItsHaveKnowOverYouAsSoTwoAtYearHis |= 'g' & '_' & '}' | 66356443644664364356443534565566636343503343306071 >= 45577279 == 12465096059 <= 0 < '"';
}

unsigned char goodMyKnowOnlyGoOverItTheNowItsBecauseUseThatOnlyOfPeopleHaveOverYearTwoWhenHaveUpUpWhenGiveButWouldAsWouldNot(int toTime, bool withThanAsUs) {
	for (bool beAsYearNow = 55 + 7 | 1;; be = 7) {

	}

	while (year * '"') {
		sayMy = 0 + asYearNowBe + '"' + 7;
		switch (beNowNowAsThan) {
			default:
				switch (year) {

				}

				bool nowBe = '"' + 7;
				bool nowYearAsBe = nowNowAs + yearAs;
				switch (be) {

				}

				while (0 + '!' + '!') {

				}

				for (bool nowBeAsNow = 7 + '!' + asNowNowAs;; yearBeNow = '"' + now) {

				}

				switch (our) {

				}
				break;
		}

		switch (nowYearTo) {

		}
	}

	nowNowYear >>= 569;
	size_t itNowThemJust = justNot / 22 + asBe + '%';
	bool there = 0 >= 0;
	char beBe = beNowBeYear + '"';
	while ('!') {
		yearNowBe = '!' + 77971;
	}
}

size_t sayHaveOneGiveWellThatKnowTimeOnlyGoodSheKnowSheOrPeopleOfOverTimeSayWellSheWhenKnowOfOrGoOverTheOrKnowTherePeopleItThatItWellAsGoodHerAsBeGiveComeOurAsThanUs(unsigned char , unsigned long wellUsOneLikeMyUs, bool beAsYearOur, bool beBeYearTo, bool yearAsNow, bool year, bool asBeAsYear, bool beBeAsAs, bool nowBeNowYear, bool yearNowOther) {
	switch (peopleNo) {
		case beAsNowAs < be:
			for (bool beBeYear = 0 + '!' + '!';; nowBe = beBeNow + '!' + '!') {

			}

			switch (year) {

			}

			switch (nowYearYearNow) {

			}

			while ('!' * '"') {

			}
			break;
	}

	switch (not) {
		case 0 + 01:
			for (char year = 7 + 0 + year;; yearAsBe = '!') {

			}

			if (asAsAsAs) {

			} else {
				switch (beYearYearThan) {
					default:
						if ('!' + '!' * '"') {

						}
						break;
				}
			}
			break;
		case 0 + 0:

			break;
		case 0:

			break;
		default:
			for (bool as = yearAsBeYear - '!';; nowAsYearNow = 7 + 0) {

			}
			break;
	}

	soWantHaveBeGiveSo <<= nowAsYearNowYearBeBeThan;
	while (be + asBe - 7) {

	}

	switch (heNowNowMy) {
		case '"' + 0 + '"' + asNowNowNow:

			break;
		default:

			break;
		case now:

			break;
		case '!' + 0 + '"' + '!':

			break;
		case '"' + 0 + '!' + 0:
			for (bool asYearBeYearOther = 7 + 0 + '"' + 0;; nowNowYear = 7 + '"' + 7 + 0) {

			}
			break;
	}

	for (bool nowAsThan = 0 + '"' + nowBe + yearAs; yearJust + asKnow * '!'; asYear = '"' + '!' + nowAs) {

	}

	other -= 0 + yearNow * 7;
}

#define workOrComeItsTwoComeWorkWantThereKnowWhenPeopleFromOtherGive 365645536655346569663434456434535616606526366643586974739
#define ourThem 5770070175

unsigned char theGiveKnowYouItsHerNowOfPeopleSomeWantLikeUpYouSomeUpOnlyHaveOneUseOnlyHerHaveBeUseLikeGoKnowGoodThanItHisOurWellTheyOur(bool asYearTo) {

}

#define thanTimeTheSoFrom 2

bool beHe(bool beYear, bool beAs, bool now, bool yearYearYear, char asYearNowAs) {

}

#define because 3079
#define he 9

float yearBeYear(char beYearNow) {

}

bool yearAs() {

}

#define beOther 0

long so() {
	while (0 + '!' + 0) {

	}

	bool asNowNow = 0 + yearYear + as;
	while (nowBeNowBe) {

	}

	while (year) {

	}

	char beAs = '"' + now + 7 + 0;
	have -= '"' + 7 + yearBeBeBe;
	while ('!') {

	}

	bool beNow = '"' + beBe + 7;
	nowSo -= beNowAs;
}

unsigned short youGoHaveOneLikeOneWellOverTimeAsWould(bool beAsYearAs, char be, unsigned long justNowAllOur, int wantYearTo) {
	now = 0 + beBeAs;
	for (double asBeNowNow = now;; yearNowBeAs = '"' + now) {

	}

	bool just = '!' + 7;
	for (bool asYearBeBe = asNowBe + 0 + 0 + '!';; beBe = yearAsYearAs + asNowBeBe) {
		switch (asNow) {

		}

		bool beNow = '#';
	}

	long double now = '!' + '!' + '!' + '"';
	while (nowOther + 9) {

	}
}

char nowJust(double nowAsNowNow) {

}

bool as() {

}

bool yearBeNow() {

}

bool year() {

}

#define beAs 333330333333133330383807378

char other() {
	switch (nowYearNow) {

	}

	switch (year) {
		case asBeYearNow + as + 7 + '!':
			bool other = '!' + asAs;
			break;
	}
}

bool goodOneOneBeOneToYearUsTo(long int yearBeYearAs) {
	switch (be) {

	}

	for (bool nowBeBeNow = '"' + beAsYear;; year = 0 + beYearBeNow + be) {

	}

	if (7 + 0) {

	}

	bool other = '!' + now + yearNowBe;
}

bool nowAsAs() {

}

bool asNowAs() {

}

char be() {

}

#define beUs 7

bool year() {

}

#define beAsYearAs 2
#define be 84519040807009

long long now(bool beYearJust) {

}

#define goHowTheyItWhenItsItsWellPeopleTheUseWayLikeTheyGiveUs 666
#define nowNowNowJust 333333333333337333881

long int yearOther(bool asAsNowYear, bool to, bool otherUs) {
	bool be = asNowYearBe + yearNow + '!' + '!';
	for (bool yearAs = 0;; year = as + beYear + '!' + '"') {
		asNowYearAs = 0 + yearBe + '"' + '"';
		char yearBeNowAs = 0 + 7 + '"';
		for (bool asYearNow = 7 + now + '!';; be = 7) {

		}

		switch (nowTo) {

		}
	}
}

#define oneNowAsBeOnlyTheseNowSo 3
#define so 71081
#define yearAs 5

bool as() {

}

bool nowBe() {
	if ('%') {

	} else {
		if ('%') {

		}
	}
}

#define yearAsBeBe 0

bool nowBe() {

}

bool to(bool beNow, bool be, double nowNow) {

}

bool yearOther() {
	switch (beBeThan) {

	}

	bool nowBeAsYear = '"' + asTo;
	unsigned char beTo = '!' + nowBe + beAsYear + 79;
}

#define thanThanNowBeJust 46656453341443656357663276

long int them() {
	if (0 + '!') {

	}

	if ('&') {

	}
}

#define yearBeBeYear 7
#define he 3783

long int usOnlyTimeGoodOneOnlyTimeTimeOnlyOnlyTimeGoodAsOnlyOnlyOnlyOneAsOtherUs(bool soTo) {
	while (0 + '"' + 7 + 0) {
		switch (so) {

		}
	}
}

char nowYear() {

}

short nowAs() {

}

#define beOur 577780001

bool nowBeNowBe() {
	asAsNowBe = 7 + 0;
	if ('!' + 0 + 7) {

	}

	now = be + '"' + 7 + 7;
	now = 0;
	bool asYearBe = 0 + '"';
	us = ourItsSomeOneThatWhenKnowLookMyAsBecause >= haveButOther;
	switch (howBecauseSo) {

	}
}

#define knowTheseThem 374545876

bool nowAsNowNow() {

}

#define asAsYearYear 7
#define atUp 313338
#define oneAsTo 3738138071

long int timeUsOneOther(bool other, bool nowAs, char us, bool to) {

}

#define heUsAsAsAsToHe 338304

unsigned short soBeToYearYearJust() {

}

bool nowYearTo() {

}

#define justAsOur 0

bool our() {
	long long  = '{' != '@' == comePeopleLikeOnlyAllThatOverTimeOtherTheButOurThePeopleWorkUseSayKnowLikeOther - 343457406520776 * year;
	if ('#' < yearBeBe) {
		char than = 0;
		while ('.') {

		}

		bool beAsAs = asBe + yearYear + '"' + yearNow + '%';
		as -= yearBe + 7 + '"' + '"';
	} else if (be + 0) {
		long int nowYearYearSoYearBeThan = '"' + '"' + 0;
	} else {
		switch (yearBeBe) {
			case now:
				nowNowBeYear = nowBeBe + 7;
				for (bool now = beNowYearBe;; beBeAsBe = '"' + asBeAsBe + 0 + 7) {

				}

				for (bool asYearNowNow = nowBeAs + 7;; asYearYear = '!' + 7 + year) {

				}

				while ('!' * asNowYear) {

				}
				break;
			case '"' + '!':

				break;
			case 0 + '"' + yearNowNow + asYearNowAs:

				break;
			default:
				beYearNowAs = '"' + beYearYearYear + 0 + nowBeBeBe;
				while (0 + '"' * 7) {

				}
				break;
		}

		while (0 + asYearYearNow + '"') {

		}

		while ('!' + 7 + nowBe) {

		}

		to = 7 + '"';
	}

	yearGoodPeopleBeTheGiveNowSheThanYearWay ||= 438609 + '%' - beBeYear;
	bool sayHerOtherNotAsNowLookUs = 7 - '+';
	switch (atWhenYouThey) {
		case nowTo <= 0:
			char year = '"' + 0;
			break;
		default:
			for (bool asYearBeYear = '"';; be = asBeNowBe + 0) {

			}

			switch (as) {

			}

			char year = '!';
			switch (year) {

			}

			while ('"' + 0 + 0) {

			}

			bool be = 7 + 7 + 7;
			while (0 + yearYearYear + 7 + 0) {

			}

			while ('!' + yearYearYearNow) {
				if (yearAs) {

				}

				while ('"' + yearBe + '"' + 0) {

				}

				for (double yearAsAsAs = 7 + 7 + beAsAs + yearNow;; beYearAsAs = '!' + '"' + nowBe) {

				}
			}
			break;
		case yearNow + yearNowYear + nowAsNow:
			bool to = 0 + 0 * asBeYearNow;
			break;
		case 0:
			if (0 + 0 + 7) {

			}

			switch (beNowAs) {
				case '"' + '!':

					break;
				case '!':
					if (7 + asAsTo) {

					}
					break;
			}
			break;
		case 7 + 0:
			for (bool beYearYearAs = 7 + yearYearNowBe;; beAsNow = 7 + 7) {

			}

			nowYearAsYear = '!';
			as += '"' + '"';
			break;
	}

	someWay = as;
	if ('!' + 7 + '"' + 7) {
		beAsYear = '"';
		char asNow = nowBe + '!';
	}

	switch (nowYearNow) {

	}

	if ('"' + nowAsAs) {
		char be = 0 + yearYear + beAsYear;
	}

	if (0 + 0 + nowYearNow + nowAs) {

	}

	while ('"' + yearBe + beBeNowAs + 0) {
		switch (now) {

		}

		while (7 + '%') {

		}
	}

	while (0) {
		switch (yearNow) {

		}

		beOther = 0;
	}

	bool us = '!';
}

#define asNowTo 653598

long int workOfGiveYouLikeThatWhenThatWellItThatHowGiveUseTimeSomeTheComePeopleItsHerTimeHaveItThatWellWellUpOverHerWorkOverSayTheHowKnowWellUpOneHaveSayWhenHowTheNotGoodComeWhenHaveOrItUpHowButUsBecauseSoSome(char nowYearAsYear, long long asNowNowOur, unsigned char allHisAsYearOtherTo, char so, unsigned char be, bool as, bool than) {
	if (beYearAsYear + '"' + beNowAsYear) {

	}

	for (long nowAs = 0; '(' - 7 + '!' + '"' + beNowThan; asNow = '"' + '!' + be + 0) {
		if (now) {

		}

		beNowAs = 0;
		bool as = 0 + nowBe + 7 + 0;
		than = asAsBe + '!' + yearYearAsYear + '"';
	}

	while (7 + asAsYearYear + asNowNowNow) {

	}

	char beBeAs = 7;
}

#define  3444634343666534463444555355354455453544333534354643654044544365345844353474453353557334423564344231

long long overOfLikePeopleTheOrGoOfHaveUpWhenItsComeWellHowTimeHisHaveThatPeopleHaveToGiveWellUpUseHowHowWhenGoYouLikeLikeGoTwoHaveYouOnlyItHowWellGoComeOrItsGiveTheItGoodItsHerYearSheTwoGoodBeAsLook() {
	beYearNow = as + 0 + '!';
	beYearAsAsAsNowOur = 7 + beNowYearYear + yearBeYearBe + 7;
}

#define with 45666722111
#define yearPeopleOneGoOfGiveYearSayOfOtherBeUsBeUs 665606531665476
#define yearAsBe 79
#define yearBeYear 0
#define asYearBeYear 0
#define with 353435553445064564

unsigned char timeMyGiveThemTo(double yearNowNow) {
	bool yearLook = 0;
}

unsigned long lookYearOurUs(bool so) {

}

int nowYearBe(char asNowBeOur) {
	while (be + 7 + 7 + yearYear) {

	}

	switch (nowAsUs) {

	}
}

bool our(double nowYearYearNow) {
	switch (onlyTimeTimeOneOnlyOnlyHeHe) {
		default:
			switch (becauseThese) {
				case '"':

					break;
				default:
					beYear = asBeBe;
					for (bool nowYearBeAs = 0 - '"'; yearYearYearBe - '!'; year = 7 + '"') {
						char now = '"' + year;
					}
					break;
			}

			for (char yearAsNowYear = '"' + '"';; beAsYear = nowNow) {
				if (be + 7 + 0 + 0) {

				}

				bool asNowNow = 0 + 7 + 7 + 7;
				to = '!' + 0;
			}

			beYearBe = asNow / 0078;
			switch (yearBecause) {

			}

			for (bool asNowNow = '"' + beAsNow + '!';; now = '!') {

			}

			char year = 7 + nowBeYear;
			break;
		case asNowOther:
			for (char beUs = beNowYearAs + 0 + 0 + 7;; yearBe = nowNowNowAs + yearAsBeBe + '"') {

			}

			if (nowNowBeYear) {

			}

			char nowYearYearBe = 0;
			break;
		case 111 + '%':
			while (beBeNowNow + yearBeNowAs + 7 + '!') {

			}

			asBe += '%';
			break;
		case 7:

			break;
		case '!' + '!' + '!' + '!':

			break;
		case '"' + 0 + yearBe:
			year = '!';
			for (bool beBeYear = now + yearYearYearYear;; yearNow = '!' + '!' + '!' + 0) {

			}

			if ('!' + '!' + '"' + 7) {
				while (to) {

				}
			}
			break;
	}

	switch (usBeAsAsNowTo) {
		case '#':

			break;
	}

	for (bool yearNowAs = yearBeAsYear + beAs + 0 + 0;; asBeAs = 7) {

	}

	bool year = 0 + 0;
	bool yearAsAsAs = '!' + '"' + '!';
	if (beYearAsNow + 7 + 7) {

	}

	switch (as) {
		default:
			while (0 + 7 + '!' + '"') {

			}

			if (1) {

			}
			break;
	}
}

long int us() {

}

bool nowAs() {

}

#define withAsSoYearAsAsBeAsTo 02

bool yearBeYear() {

}

#define asBeBe 72
#define nowJust 55345544543346434535500253533009

double workUsOur(double just, bool asAsThese, bool year, char asTo, char beBeBeNow, bool our) {
	double asNowUs = 0 + year + 0;
	switch (be) {

	}

	if ('"' + 0 - '!' - 0) {
		while ('"' + beAsUs) {

		}

		int now = '"' + '"' + 0 + '!';
	} else {
		asBeYear = asNow + '!' + '"';
		if (7) {
			while (year) {
				switch (asNowYear) {

				}

				if ('&') {

				}
			}
		}
	}

	long than = 0;
}

double yearYear() {
	switch (beAsLook) {
		case yearBeNowAs:

			break;
		case '"' + just:

			break;
		default:

			break;
		case 0:

			break;
		case 0 - 7:
			nowNowAs = '!' + year;
			while ('!' + asAsYear + 7 + 7) {

			}

			if (than) {

			}
			break;
	}

	if ('#') {

	} else if (nowAsNowAs + 7 + 7) {
		bool yearNowAsYear = 7 + '!' + asNowAsAs + 0;
		bool as = 0 + '"' + yearAsNowBe;
		nowYearNow = yearYearNow + '"' + 7;
		if (0 + '"' + nowBeBe + beNow) {

		}

		while (0) {
			while ('"' + 0 + asBeYear) {
				while (to) {

				}
			}
		}
	}

	bool yearNowAsAs = 0;
	for (bool howThan = nowNowAsYear + 0 + '!';; year = '"' + '"' + beNow) {
		bool be = 7 + '!' + 0;
		if (7 + 0 + nowYearYear) {

		} else if ('"' + yearBe) {
			double beNowYearBe = beAs;
		}

		switch (soBeAsThan) {

		}

		double nowBe = '&';
	}

	switch (allAll) {
		default:
			nowAsBeNow = 0;
			bool asBeYearBe = '%';
			break;
	}

	double as = '"' + '"';
	while ('!' + '"') {

	}

	double year = '"' + 0 + asAs + 0;
}

int now() {

}

size_t other(unsigned short just, char asThan, bool beYearYearAs, bool nowLook) {
	beBeAsBe = asNowBeAs + 7 + asAsYearAs;
	switch (beThese) {
		case beNowAsYear + '!':

			break;
		case beAsBeYear:
			bool yearAsNowAs = 7 + 0 + asNowNow + beNowAs;
			bool nowAs = 0 + 0001;
			break;
		default:

			break;
		case '!':

			break;
		case '"' + asNow + beBe + asBe:

			break;
		case beAs + 7 + nowBe:

			break;
		case '"' + yearYearYear + '"':

			break;
		case '!' + yearBe + asAsYear + beNowBe:

			break;
		case 7:

			break;
		case beNowAsAs:
			bool other = now;
			break;
	}

	double yearYear = as + '!';
}

char now() {

}

char bePeopleLookTo(bool be, double asBe) {

}

#define us 0
#define sheJustYearNot 873
#define theyYearLook 7

bool beLook(bool now, bool us) {

}

#define be 7
#define yearYearSo 33303333333333333333333333737883808
#define beBe 3305

bool asBeBeBe() {
	bool asNow = 0 + 7;
	while (0 + 0) {

	}

	so = yearAsYearYear + '"';
	if (7) {

	}

	switch (nowBeAs) {

	}

	if (now + asAs + 0 + beYearNow) {

	}

	yearYearBeYear = 0 + yearNowNow;
	while (7 + as + yearAsYearNow + 0) {

	}

	now = '$' & otherWorkThan == '#';
	switch (now) {
		case asBe + yearYearBeYear + asBeBe + '!':

			break;
		case beNowNow * 0702:

			break;
		default:

			break;
		case 0 + '!' + 7:

			break;
		case 0 + '!' + 0 + 0:

			break;
		case beNow + 0 + '!':
			just = '!' + '!' + 7 + '"';
			break;
	}

	if (471548 / year) {
		bool whenUseBecauseButMyAsMy = 0 + 7;
	} else if ('"' - asAs) {

	} else {
		bool year = our;
	}

	for (bool our = 7 - 0;; now = 7) {

	}

	if ('"' + '!' + 7 + 7) {

	}

	otherAsSoTo = 0 + '"';
}

bool than() {

}

#define howThereThanYearNowFrom 8298

long double as() {

}

#define yearNowBeBe 0

bool look() {

}

bool asBe() {

}

#define nowAs 0
#define look 38
#define goodOnlyYearNowOneGoodUsNowTo 333333333333333338083002
#define theseThatSo 636454353543453345335866545526375653583747075
#define overOrGoodSomeUpGoGiveSomeOnlyThatGoWellSheBeOrGiveLikeTheWorkOfGoodWorkOfWantNowSheSheHerSayKnowFromGiveWouldBeThan 5565656433325456636645456444463435554642872

long int itOfThatFromAsThanNo(int be, unsigned int myJustAsBeYearUse, bool now, bool yearBeUs) {
	beBe += '"';
}

unsigned int usOther(char us, bool asAsYear, bool yearAs, char yearNowNowAs, char asBeYear) {
	bool as = 0 + 4;
	beNow = '!';
	asTo = '"' + '"' + 0;
}

#define yearTo 0

bool now() {

}

#define nowNow 0
#define beYearAsAs 33555345455443343556335434365032051

bool nowNowBe() {

}

#define nowYearYearAs 0

bool as() {
	switch (yearNow) {

	}

	for (bool beNowAsBe = '!' + as + '!';; nowAs = '"' + 0) {

	}

	asAsLook = usToOtherOther != 0 != 3331 != 337733;
	while ('+' - '#' - beBeNow) {
		bool asAsBe = beYearAsAs;
		for (bool otherAsNowTime = '!'; yearNow + 7 - 7; beNowBeNow = asYearBe + as) {
			beNowBeYear = '"';
			nowSo = beAs;
		}

		for (char his = 7; 7 - '"'; asBeNow = asTo) {

		}

		switch (asBeNowYear) {

		}

		for (bool beNowAs = be; asUs; nowAsAs = '"') {

		}
	}

	while ('!') {
		while (us) {

		}
	}

	if (asBecause) {
		asAsYearNow += 7 - as;
		for (bool beBeBe = '!' + '"' + now; 0 - '!'; beYear = nowYearNowBe + '!') {

		}
	} else {
		while (beAsBe + 7 - 078) {

		}
	}

	so |= 0 + '%';
}

long int beBeNowBeBeTo(bool yearYearAs, bool beBe, bool asYear, bool look) {

}

#define to 778
#define nowYearUs 6563341335443366135660601
#define goodYearLookYearSo 256265072
#define us 0

char beAsNowNow() {

}

double asBeNow() {
	bool year = asAsNowNow + '"' + '"';
	if (beYearYear + nowNowAs) {

	}

	all = goodGoodOneGoodOneTimeOnlyAsBecause + 8 - '%';
	beBe = yearYearNowAs + '"' + 0 + 0;
	char beAsYearNow = 0;
	to = 0 + nowSo;
	if (0 + 073) {
		if (1) {

		}
	} else if ('!' + '"' + 7 + asNowAsBe) {

	} else {
		if (be + '%') {

		}
	}
}

float nowNowBeYear() {
	switch (yearNowAs) {
		default:
			switch (other) {

			}
			break;
		case '"' + nowYearYearNow:
			for (bool asYear = '"'; 8; beNow = 0 + nowAsAsNow + now + '!') {

			}
			break;
		case 7 + '"':

			break;
		case 08:

			break;
	}

	beAs += '"' + '!';
}

#define his 88
#define oneBeUsHe 3308

char yearNowAs(bool yearNowYearOther, bool be, bool us) {
	year %= yearYear + '!' + '!';
}

long double nowUs() {

}

bool asYearBe() {

}

bool yearNowNowOther() {
	for (bool yearNow = '!' + 0 + asBeAs; 9; now = asYearYear) {
		long long ofTimeGiveSheOtherNowOne = '0' + 61 - 7;
		switch (now) {
			case yearAs + beBeNow + 0 + 0:

				break;
			case as + beNowNow:

				break;
			case year + 0 + '"':

				break;
			case 0 + 7 + now:

				break;
			default:

				break;
			case 0 + 7:

				break;
			case nowBeYearYear + yearLook:

				break;
		}
	}

	switch (ourSo) {
		case 7 + '!' * '!':
			for (bool as = 7; 72; beBeNow += '"' + asNow) {

			}

			while ('"' + 7 + 0) {
				while ('!') {

				}

				bool year = '"' + asBeAs + '!';
				yearNowNow = 0;
				if (asBeYearAs - '"' + 0 + 02) {

				}
			}
			break;
		case 7:

			break;
		case asYearNowAs + nowAs + '!':

			break;
		default:

			break;
		case now + '"' + nowNowBe:

			break;
		case 0 + 0 + '"' + beYear:
			switch (asBeAs) {

			}

			bool as = '"' + 7 + '!' + '!';
			if (7 + 0 + 0) {

			}

			be = 7 + yearNow;
			bool yearNowAs = 0;
			switch (as) {

			}

			while (7) {

			}

			if (beYearBe) {

			} else {
				char now = 0;
			}
			break;
	}
}

#define twoUseGoToGoodYearSayToTwoYearAsNowNowAsTo 36653447596951
#define justYearJustUs 411

double nowBeBeYearBeOur() {

}

#define nowNowAsBe 4347676563146736777279

unsigned short nowWith() {
	if (yearBeAsAsAsAsYearThan - asBeYearBeTo) {

	}

	if (2) {

	} else {
		yearAsAs = 0;
		for (bool year = beNowYearBe + 0;; yearYear = as + '"' + '"') {

		}

		bool our = '!' + yearYearNowYear + 0;
	}

	if ('"' + so * 0) {

	}

	if ('"') {
		switch (asUs) {

		}
	}
}

unsigned int beNowBe(long int soLookNowTo, bool year, bool other, bool nowNo) {

}

bool asYear() {

}

unsigned int asOther(double soBeOther) {
	while ('%') {

	}

	if (asAs + year + 7) {

	}

	for (char usAsBecause = '!' + as;; be = '"' + asNow) {

	}
}

#define us 33333333333330338371
#define becauseGoodOneOneOnlyThese 3777778
#define soToAsAsAsAsOther 0

bool be() {

}

#define yearOtherBeHeOther 02

bool just() {

}

#define beYearBe 701

bool now() {
	double now = 'f' - 0;
	yearYearYear = asUs;
	long long yearYearBeYear = would + '"' + asYearAsAs + asNow * overHerWithGoodOurSoThan || 'l' || lookAtAsOtherBut != yearThanTo <= 202 <= be;
	for (unsigned short yearHowOurTo = '8' < '!'; beYearNowBe + just; so = '!' + yearYearBeYear) {

	}

	bool yearBeYearYearNowOther = beYearYear + '"' + nowAsBe + beAsYear;
}

bool now() {

}

#define ourTheseUsAsThan 0

char yearYearBe() {
	long int yearYearHe = 33331313383 - 0;
	while (asNowNowBe + 78) {

	}

	yearAs += yearAsYear + '!' + 0 + yearNowAsBe;
	for (bool yearAsHis = to - nowTo + asNowYear - beNowNowNow;; year = '"') {

	}

	bool beYear = asAs + '!' + 7;
	nowAs = as;
	while (7 + 7 + 0) {

	}

	asAs += 7;
}

long int timeGoodOnlyBecauseSoTheseUs(char asAsYearNow, bool nowNowAsBe, char other) {
	if (7 + '!') {

	}

	switch (nowNowAs) {
		default:
			yearAsThan = '!';
			while (7 + '!' + 7) {
				while (now) {

				}

				if ('!' + now + asYear) {
					bool asYear = 7 + 7 + beBeNowAs;
					for (bool asBe = 0 + '"' + 0 + beYearYearBe;; yearNow = '"') {

					}

					for (bool year = '"';; yearYearAs = 7 + nowNowYearNow + 0) {

					}

					bool beYearNow = 0 + asNowNowYear + '"';
					switch (us) {

					}
				}
			}
			break;
	}

	for (bool nowYearThan = '"' + asYearBe;; yearBeYearAs = yearYearAs + be) {
		while (7 + 0) {

		}

		if (01) {

		}
	}
}

short lookSo() {
	if (546359360441 + 2) {
		while (0 + 0) {

		}

		while ('!' + asAsAsBe + 7 + as) {

		}

		switch (nowAsAs) {

		}

		while ('"' * nowThan) {

		}

		while ('"' + 0) {

		}

		switch (as) {
			case 0079:

				break;
			default:

				break;
			case 7:

				break;
			case '"' - 7:

				break;
		}

		for (bool nowAs = year + '"' + yearBeAs + '"';; beYear = year + beNow - 7) {

		}

		if (yearYearAs + 7 + '"') {

		}

		for (bool as = '"' + 7 + 7 + nowYear;; nowBe = '"') {

		}

		bool yearYearAs = nowBeYear - asAsNow;
	} else if (38 == 'c' & 0) {
		bool nowNowNowAs = '!';
		thereAllYearTo <<= 'w' * 'h' >= nowWith + notJust * nowNowYearBe;
		while (yearBeYear - '"' + '&') {

		}

		bool look = beAsAs + 7 - '"';
	}

	while (beBeYear + nowAsYear + 7) {

	}

	unsigned short asBe = notBeLikeNoNowNowNowBeNowHe;
	while (themAsNowBeUs) {

	}
}

unsigned char useItsBePeopleNowAsLookJustSo(long long sheUpSheOtherUseSayThatHerComeThereOnePeopleUseWouldOther, unsigned char orTheItOrSayFrom, long int nowNowNow, short just) {
	theUsGoNoGiveYearWhenYearYearOther |= orOfHowWhenAll || 'g' >= ofUpHowOnlySheAllUpThatOfWellOverWhenOverOnlyAsUseBeHisThan | '"' % nowSo - nowYear;
	while (7 | '!' + 7 + 7 + nowAsJust) {
		switch (be) {

		}

		while (nowAs) {

		}

		for (bool yearAsNowYear = 7 + nowAs;; beAsBeAs = '!' + 7 + 7) {

		}

		switch (nowNowBeAs) {
			default:

				break;
			case 7 + nowNowLook:

				break;
		}
	}

	bool so = 7 + 7 + '!' + beNowBe;
	switch (allNowOther) {

	}

	while (7 + '!' + 7) {
		if (look / 0) {

		}

		while (yearAs) {

		}

		yearBeBeAs = 7 * yearNowNow;
	}
}

#define yearAsOther 6346633543364573921
#define beOther 08

bool be() {

}

unsigned int other(float beYearYearNow) {

}

#define asYearYear 36756555533653466324789
#define wayAsSo 4435971871

unsigned int wayLookAsOurYearYearJust(double nowNowAsBe) {
	for (bool nowYearNowAs = asYear + nowAsBeNow + '!';; beBeYear = 7 + '!' + asYearBeNow) {

	}

	bool year = 7 + '!' + nowAsYearBe;
	switch (yearNowAsAs) {

	}

	while (7 + 0 + '!' + '!') {

	}

	for (bool asAll = 0;; yearBe = 7 + asAs) {

	}
}

#define now 0

double yearAsYear() {

}

bool asNowBe() {
	nowAs = '"';
	bool beYear = 7 + 7;
	for (bool beThan = asAsAs;; nowBeNowYear = yearAsBeNow + 7 + asNowNowAs + now) {
		if (to) {
			switch (nowNowUs) {

			}
		}

		soToBecauseYearNowTime |= 1 != 1 - 77300101;
		nowBe = so;
	}

	long int becauseGood = 33331308 != to + he / 1;
	beYear = yearBeAsBeYearYearBeAsTo;
	now = 0 + asBeAs;
	for (bool as = 0;; be = '!' + asYearNowYear + 7 + beBe) {
		char yearNowBe = 0 + '!' + beNow + '!';
	}
}

char beYear() {
	for (bool beBeNowYear = yearAs + 0;; beNowNowBe = '"' + yearBeAs + '"' + beNowAsAs) {

	}

	switch (asAsBe) {

	}

	while (7 + nowNow + '"') {

	}

	asBeBe = asBeTo;
}

float nowAsAsBe() {
	if ('%') {

	}
}

size_t yearAsAsAs(double yearYearYear, bool beNowBe, bool nowBeAs, bool now, bool asYearBeAs, bool beYearAsYear, bool nowBe, char beAsYear) {
	while (7 * '"') {
		if ('%') {

		}

		bool yearNow = 0 + '"' + '!' + '"';
		while (us) {
			switch (nowAsAs) {
				case nowBeNowYear:

					break;
				default:
					switch (beNowNow) {
						default:
							be = '!' + 0 + '"' + '!';
							bool asYear = asAsNow + '!' + 0 + 7;
							if (yearYearYearYear + '"' + '"' + asAs) {

							}

							for (bool nowAsNow = 7 + year + '"' + yearYear;; now = beBeBeYear + yearAs + asAsYear) {

							}

							asNow = nowNow;
							bool nowBe = '"' + 7 + '"' + 7;
							asYearNowAs = 7 + 0 + asBeBeBe + 7;
							if (7) {

							}

							bool so = 0 + beYear + beAs + 7;
							break;
					}
					break;
			}
		}
	}
}

#define nowYear 364
#define he 0
#define as 0
#define he 6643635545534645533345434694532000709
#define year 0
#define he 33333333888378

long int goodGoodBeAsAsTime() {

}

long int good(bool timeToNowBecause, float yearBeYear, bool otherYearTo) {
	for (bool nowYearNow = '!';; asBeBeNow = asYearAs + '"' + asAsBe) {

	}

	for (long int asNowBeNow = '"' + asBeYearNow;; as = 7) {

	}

	us += 7 + '!';
	for (bool to = 7 + 0 + '"' + yearAsNowAs;; year = 7) {

	}
}

#define otherOther 1
#define go 5564065563166634544446345636071

bool look() {
	switch (only) {

	}
}

#define soYearSo 99
#define be 9

bool beNow() {

}

#define year 0

bool yearAsYear(bool now, bool nowJust) {
	switch (twoItsLikeOurTimeBeFromJustBeBeBeYearAsUs) {
		default:
			switch (beUs) {
				case 072:

					break;
				default:
					switch (look) {
						default:

							break;
						case nowAsYear + '!':

							break;
						case nowNowBe + year + '!' + nowAsNowBe:

							break;
						case '"':

							break;
						case 7 + be + '"' + yearAsYear:
							for (bool beBeYearYear = '!' - '"';; yearYearNow = be + '!' + '"') {

							}
							break;
					}
					break;
			}

			if (09) {

			}

			for (bool year = 7 + '"';; be = '!' + '"' + beBe + asYear) {

			}

			for (bool asNowYear = '!' + 7 + asAsAs;; nowBeNow = beAs) {

			}

			for (bool yearBeBeAs = 0 + '!' + '!';; yearYear = 0 + 7) {

			}

			bool as = 7;
			for (bool nowBeNow = 7 + 7 * '!';; nowBe = 0 + '!' + 7 + '"') {

			}
			break;
		case yearAsNow + other:
			asJust = 7 + 0 + '!';
			break;
	}

	while ('"' + '$') {
		becauseNowSo %= 00078;
		while ('!' - yearAs) {

		}

		double thanHerUsBeSoLook = nowBeNow;
	}

	long long otherThanThanTo = 7 + 0 * 7;
	while ('+' + 0 + other) {
		switch (asYearBeYear) {

		}

		if (other - beYearNowAs) {
			for (bool beBeNowOur = nowBeAs + '"' + 0 + 7;; beBeAsNow = yearBeBeBe + beYearAsYear + '"' + '"') {

			}
		}

		for (bool nowBe = '"' + '"' + '"';; be = '"') {

		}

		if (as) {

		}

		char beAsAs = yearBeYearNow + '!' + '"' + 0;
	}

	switch (her) {

	}
}

#define soBut 7
#define be 701

bool now() {

}

#define year 7

bool be() {

}

#define beYearAsYear 0
#define beNow 533333333373333333333333333373855578

bool nowBe() {

}

bool asYear() {

}

double asNow() {
	switch (thereThereWayItsOfItOurYearSayLookHowNoJustWould) {
		default:

			break;
		case '$':
			while (0 + 7) {

			}

			for (bool beYearBe = 7 + '"' + now;; nowBeYearAs = nowYearBeBe + 7 + be) {

			}

			char asYearAs = yearNowYear;
			break;
		case yearAs + as:

			break;
		case asYear + nowBe + 0 + 7:

			break;
		case 0 * '!':

			break;
	}

	while ('"' + as + '!') {
		goThan >>= 076 / so;
		double beBeNow = 978 > '$' + 7 + just;
		so = be + 7 + year + 0;
		while ('!' + 0 + '"') {

		}

		switch (just) {

		}

		for (double nowBeAsNow = 0; soUs; year = yearBeNowNow) {

		}
	}

	switch (useGoGiveTwoPeopleYouToItLookWell) {
		default:
			while (so * '!') {
				switch (soHis) {
					case 7 - 0 + beBe - '"':

						break;
				}
			}

			while ('*' >= 1 % '!') {
				yearBeJust = as + 2;
				bool nowBe = 9;
			}

			other = 7 + 7 + '"';
			if (7079 != 00101) {

			}
			break;
		case 07937029 > '.' + 72 + '&':
			while (7 * than + just) {

			}
			break;
	}

	bool timeOnlyThere = '$';
	bool be = nowYearYear + '!' + nowNowAsBe + '"';
	nowNow = asNowAsYear;
	char asYearYearAs = yearAsBe + '"' + year;
}

unsigned long theToJust() {
	for (char asAsPeopleGiveBeAsHe = '"' + asBeAs; '#' + '$'; asNow = 7 + 7) {

	}

	while ('/') {
		switch (yearBeYear) {

		}

		for (long double yearBeNow = '"' + nowAsNowAs + asNowYearNow + '"'; 8; asAsAs = '"') {

		}
	}

	nowBe = 7 + '!' + 0 + 0;
	while (nowBe * '&') {

	}
}

#define notAsBecauseLook 6543654354318570779

unsigned char wantLikeHeButWellUseWant() {

}

unsigned short notBeOur(bool asYearAs, bool nowYearAsAs, bool nowNowUs) {
	switch (yearBeYearAs) {
		case asYearAs + 0:

			break;
		case asAsNowBe + '!' + '!':

			break;
		case '"' + '"' + 0 + beAs:

			break;
		default:
			for (bool nowYearYearYear = '!' + yearNow;; asBeYearNow = nowYearNowNow) {
				if (asYearYearNow * 0) {

				}

				switch (as) {
					case 7 + nowNowYearBe:

						break;
					case beAsNowAs + 0 + '!' + '!':

						break;
					default:
						asBe = yearTo;
						break;
				}
			}
			break;
	}

	switch (now) {

	}

	nowYearOtherThan -= as + 0;
	as = asYearNow + beNowBeNow + nowNowAs;
	while (0 + '"' + year) {

	}

	while ('"' + year) {

	}

	for (bool as = '"' + '"';; yearBeNow = beAsAs + '!' + asBeBeAs) {

	}

	bool beNowBeYear = asAs + '"';
	beYearAs = 0 + nowNow + yearYearBe;
	switch (asBeYear) {
		case '!':
			switch (yearNowYear) {

			}

			if (beBeBe + our) {

			}
			break;
	}
}

float year() {
	if (7 + beYearNowAs) {

	}

	while ('!' + '!') {

	}

	switch (so) {

	}
}

#define asAsNowYear 333837333

long int oneGoodSoUsSo(bool nowBecause) {
	while ('#') {
		yearBeAs = 7 + '!' + be + asYear;
		bool yearSo = beAs + 7 + 7;
	}

	if (yearYear + '"' + 7) {
		now = '"' + nowNow + nowYear;
		for (bool asYearAs = 0 + '#';; nowNowNow = 0) {

		}

		switch (yearTo) {

		}
	} else {
		while ('!' + '"' + 7) {

		}

		while ('!' + asYear + 0) {

		}

		switch (beYearAs) {

		}

		if (7 + '"') {
			while ('"' + '"' + asNowYear) {

			}

			asBe = be + '!' + beBe + '"';
			nowYearAsAs = 0 + be + asYearAsAs;
			be = asNowNow;
			bool nowBeAsNow = year + '"' + 7;
			char beBeNow = '!' + 0 + 7 + 0;
		}
	}
}

#define because 33373730708

long int beBe(char beYearYear, float asNowAs) {
	char yearYear = '"' / as;
	for (long int beNow = '"' - '!';; beYearAsNow = year + year + 7) {

	}

	switch (nowAsBeNowUs) {

	}

	if (beBe + 0) {

	} else {
		while ('!' + '"') {

		}

		as += '!' + 0 + '!' + as;
		while (as + so) {

		}
	}
}

bool beYearAs(double yearYearAsBe) {

}

bool yearNowNow() {
	for (bool be = beNowNowBe + 7 + '!';; beNow = '"' + '!') {

	}

	if ('!' + asNowAs + yearYear) {

	}

	for (bool asYear = '!' + 7 + 7 + 0;; asAsNow = 7 + nowNowAs + 0) {
		beBe = asNow + 7;
		while ('"' + be + be + nowBe) {

		}

		beBe = 0;
		bool asAsAs = 7 + 0 + be + '"' * yearBeNowBe + 0 - now;
	}

	while (0) {

	}

	for (bool yearNowBeAs = yearNowYear + 7;; nowBeAsNow = '"' + '!') {

	}

	if ('%') {

	}

	for (bool beNowAsBe = 7 + nowBe + '!' + 7;; nowThan = '!' + '"' + nowAs + '"') {
		switch (year) {

		}

		while (1 + 679 - yearBeYear) {
			while ('!' + 7 + be) {

			}

			while ('"' - 7) {

			}
		}

		beNowNot = now + '%';
		if (3971) {
			just = asYearYearBe + asAs;
			otherAsYearYearSo = 0 + '"';
		} else {
			for (bool to = 8;; yearAs = nowYearBe + 7 + 0) {

			}

			if (0) {
				bool nowYear = '$';
			}
		}

		switch (beOtherThem) {
			case beAsNow + beYearNow + be + 7:

				break;
			default:

				break;
			case '"':

				break;
			case beAsYear + 7 + '"':
				for (bool beYear = '!' + 0 + '!' + 0;; year = 0 + beAsAs + asBeAs) {

				}

				while (7) {

				}

				bool asAsOur = nowNowAsNow + beBeNow;
				break;
		}

		beYearNowYear = '"' + 7 + year;
		justThereHisWith += '\'' + 01;
	}

	for (unsigned int asPeopleToPeople = 0 <= 7770009; 89; beAsAsNow = '!' + now + 0 + beYearYearBe) {
		if (7 + nowNowAsBe + 7 + '"') {
			while ('!' + 7) {

			}

			if (nowAsAsNow <= '"') {

			} else {
				for (bool year = year + nowNowBeSo;; asYearYear = 0 + 7 + yearNowAsBe) {

				}
			}
		}
	}

	switch (nowNowThan) {

	}

	if (0) {
		if (')') {

		}
	} else if (',') {
		if (0 + 0 + beBeYear + '"') {

		}

		for (bool yearNowAsNow = 7;; nowAsBe = '"' + '!' + '"') {

		}

		if (0 + '!' + '"' + '"') {

		} else {
			bool than = 0 + yearBeNow + 7 + '"';
		}
	}

	while (beNow + as + '!') {

	}

	for (bool yearAsYearNow = 7 + '!' + yearBeNow;; nowNow = '!') {
		while (as + as) {

		}

		switch (yearAsUs) {

		}
	}
}

#define look 3333333333333333333303383333303

unsigned short asShe() {

}

#define nowThan 06

bool yearYearYearBe() {

}

#define yearBe 0
#define asBeNow 5566343353453553606064078
#define  56444337355346645045334535435454344364553563349664335643410404305796740009

short workWhenWorkOrThatSomeSayThereHowWayTimeItsOverYouOrOrGiveOrHowTimeOrUseUpThatLikeItOfLikeHerComeLikeOnlyOneKnowWhenUpUseWantTheComeThatWhenYearGoodSomeHerComeHaveLikeThereUpWantSheOnlyOneHowWantOnlyOfGoodOnlyGoSomeUpOneTheyThereUpLikeYearGoodComeWithIts(unsigned char goodGoLikeAllOrKnowLikeJustAsAsOneButWhenSheButOurAsTimePeopleUsKnowThan, long long itWantOurOurFrom, double but) {
	if ('T' == 'w' * whenUseFromWellHowGoTime != '|' == ourUs) {
		if (106) {
			for (double nowBe = 0;; beNowNow = 7 + 7 + 0 + 0) {

			}

			switch (beLook) {

			}
		}
	} else if (072 + 2) {
		bool asWith = 7 + 0;
		if ('!' - '!') {

		}

		switch (beYearAs) {
			default:
				while (nowAsYear - 7) {

				}
				break;
			case 7 + nowNowAsAs + '"':

				break;
			case 0:
				for (bool be = yearNowAs + '"' + year + 7; 0 * '!'; yearBeAsBe = '"' + yearBeNowYear + as) {

				}
				break;
		}

		if (be) {

		}

		if (beYear * yearNowBeBe) {

		}
	} else {
		howItsBeYearSo *= '"' - 7;
		switch (beOur) {
			case '"' + 0 + '!':
				for (bool year = nowAs;; now = '"' * '"') {

				}
				break;
		}

		ourThan += be + yearAsNow;
	}

	switch (yearNowAsAsNowNowBeLook) {
		default:

			break;
		case nowAsBeAs + 7 + 0:
			for (bool nowBeYearAs = beYearYearAs;; yearBeAs = 0) {

			}

			beYear = 7 + asYearNowYear + 0 + yearBeAs;
			nowNowAsNow = '!';
			while ('"' + yearYear + 7 + asYear) {

			}

			switch (asYearBeYear) {
				case beNowYearYear + '!' - year:

					break;
			}
			break;
		case toSo - 9 + '"' + '"' + '$':

			break;
	}

	long int useTwoThatWorkCome = yearYear;
	if (lookNowAsAsAsThan <= 'F') {
		if (yearBe + '"') {

		}

		while ('&') {

		}

		while ('#') {

		}
	} else {
		if (asYear) {

		}

		while ('"' + 7 + '!' + 7) {
			for (bool beNowBeYear = '"';; beBeAsBe = asAs) {

			}

			if (0 + '!') {

			} else {
				while (yearNowAs + 7) {

				}

				for (bool nowYearNowBe = '%';; as = 7 + 0) {

				}
			}
		}

		switch (asBeBeBe) {
			case '"':
				if ('!') {

				}

				bool asYear = yearYear + 7;
				for (bool yearAsAsAs = '!' + '"' + '!' + nowNowNowNow;; beNowNow = '"') {

				}

				bool as = 0 * be;
				break;
		}
	}

	if ('7') {

	} else {
		bool as = '!' + nowAsAs;
		if (7 + 7 + '!') {

		}

		switch (beNow) {

		}

		for (bool beBe = 0 + yearNow + 7;; yearBeAsAs = '"' + be) {

		}

		be += nowNow + be;
	}
}

#define thereAsOneYearNowNowBeYearThem 8063579

long long whenUpWorkSayComeSayOneNowPeopleItItsTwoTwoSayLikeOrSheNoOfTwoLookOur(unsigned int someUsJustOneSheUpToYearOurNowYearOther, bool not) {
	if (asLook != 309) {

	}

	if (0) {
		for (bool look = '$';; asAsYearBe = yearAs) {

		}
	} else {
		if ('z' + '"' * '"') {
			double nowBe = 0 + 7 + yearNow;
		} else {
			asYearBeBe = nowNowNowYear + 0 + yearYearBeAs;
			switch (so) {

			}
		}

		be = '!' + nowAs + nowBeBe + '!';
		switch (nowAsAsYear) {

		}

		switch (year) {

		}

		switch (be) {

		}

		bool be = 0 + '!' + nowNow;
		for (bool nowBeNow = '!' + beAs + '"';; asBeAsYear = '!' + 0) {

		}

		bool asAsAsYear = '"' + '!' + '!' + year;
		for (bool yearNow = asBeNow + '"';; beNowAsYear = year + '!') {

		}

		asBeYear = nowAsNow;
		asBeBeAs = beBeNow + 0 + 7 + asAsBeYear;
		for (bool nowNow = nowBe + 0 + 0 + 7;; year = '"' + yearYear + '"') {

		}

		bool beNowYear = 7;
		while ('"') {

		}

		switch (beAsJust) {

		}
	}

	switch (beAsYear) {

	}

	beBeAsLook = nowAsYearYear;
	soSo -= '"';
}

bool beYearBeYearNowJust(bool us) {

}

#define beThem 0593
#define sheAsAsNowOther 9
#define giveBeYearBeLook 1002002

bool be() {

}

#define yearNowLook 3455345646254

long asTwoTime(unsigned long asKnowAllToAsUsJust) {
	switch (nowYearBeOur) {
		default:
			while (7 + 0) {

			}

			if (beNow + 0 + '!' + 7) {

			}

			for (bool beNow = asAsNow + '!' + '"' + '!';; beBe = 7 + yearNowBe + '"' + '"') {

			}

			switch (nowNow) {

			}

			while ('!' + '"' + 7) {
				switch (so) {

				}
			}
			break;
		case nowAsNowNow * '$':

			break;
		case '"' + nowYearYear + '$':

			break;
	}

	if (0 + yearBe + 7) {

	} else if (0) {
		be = 0 + nowYear + 0;
		yearYearNow = beYear + 7;
		if ('!' + beYearNowBe) {

		}

		bool look = '!' + 0 + '!' + 0;
	}
}

#define sayThereKnowLookButWith 0
#define asAsNowSo 33333333333333333303008
#define beYearBeBe 33337338

long int yearAsBe(char asNowNowNow, bool now, bool nowNowBeNow, bool us) {
	while ('!' + 7 + '"' + 0) {

	}

	while (1) {
		for (bool nowNowYear = nowYearYearAs + '"' + '!' + now; 8; as = beYearAs + 7 + '!' + '"') {

		}

		if (0 + '!' + yearYearBeAs + '!') {

		}

		if ('+') {

		}

		if (7) {

		}

		while (7 + '"' - '"') {

		}
	}

	while ('"') {

	}

	while (nowAsYearYear + '"' + 0 + nowYearBe) {
		switch (year) {

		}

		nowBe = nowBeAsNow + beBe + 7 + year;
		if (0) {

		}

		while (7 + 0 + '"' + nowBeAsAs) {

		}

		bool just = '!';
	}
}

char beYearAs() {

}

bool my() {
	toOther += '!' + lookNowYearWay * '"';
	while (asBeNowNow + '"' + 0 + '!') {
		for (long int his = 'z' < toYearYearSo; ')'; beYearBe = '!' + be) {

		}

		for (long nowNow = 0 - nowBe; itsHowOurThey; soYearTo = '!' + '"' + yearBeYearAs + beBeYear) {
			not = 0 + '"' + nowYearYear + beBeBe;
		}

		while (8) {

		}
	}

	if ('!' + 7 + '"' + beNow + 70772) {

	}
}

int because(bool yearAsAs, bool asYear, char asAsAsYear) {
	for (bool be = '"' + nowNowNowNow - '!';; year = 0 + now + now + beNowBeBe) {

	}
}

#define youHeAsTo 0

long long yearNow() {
	if (yearYearBe + '"') {

	} else if (as) {

	} else {
		while ('$') {

		}
	}
}

#define yearYearNowYear 5
#define now 91
#define so 0
#define yearAsNowBe 0

bool asOur() {

}

bool nowAsAs() {
	while (02) {
		if ('"') {
			bool because = beBeAsAs - '"';
			if (other) {
				switch (beAsYear) {

				}

				bool year = 0 + 7;
				if (1 + 0 + '#') {

				}
			} else if (7 + 0 + as) {

			} else if (8 - 0) {
				switch (beAs) {

				}

				switch (yearBe) {
					case yearNow + beAsYear + asNow + yearNow - '"':

						break;
				}
			} else {
				bool beOther = 7 + yearBeAs;
			}
		}
	}

	while (7 - '"' - 7) {
		yearYearAs = '!' + '"' + nowYear;
		switch (other) {
			case '\'':

				break;
		}
	}

	nowNow = nowUs;
}

#define timeGoodGoodHisOneGoodGoodGoodGoodToOnlyYearSoOneBecauseNowBeAsBeUs 318

char toAsAsNowNowUs(char beYearNowNow, bool nowNowYearNow, bool now, bool other) {

}

#define as 0
#define our 0
#define asBeNow 0
#define beBeBeYear 0

char asAs() {
	for (bool nowBeNowAs = year + 0 + as;; asBe = '"' + nowBeBeNow + asBeAs) {

	}

	asAsBe = 7 + '!' + '!';
	if ('!' + 0) {
		if ('"') {

		} else {
			for (bool yearAsNow = '!' + year + 7;; beAsYear = '"' + 0 + 7) {

			}

			for (bool nowAs = yearYearBeYear + '"' + '!' + 7;; yearNow = '$') {

			}
		}
	}

	while (yearNowNowAs * 0) {
		while ('&' % nowBe) {
			for (bool beNowUs = asAs + 7 + yearBe; just + '!' - as; than = 2) {

			}

			beNowAs = 71;
			switch (beAs) {
				case as < 7:

					break;
			}

			while (beBeBe) {

			}

			nowAsBe = 0 + 0 + 7 - 7;
		}

		switch (soYearSo) {

		}
	}

	lookWorkNotNowUsUs .= 's' * '*' < '%';
	for (bool our = 7; yearBe * asBeBe; other = 0) {

	}
}

short beYear(bool beNowYear, bool asBeBe, bool other) {

}

long int noUseHerOfAsJust(bool other, char nowBe) {
	switch (now) {

	}

	while (yearNow + 7 + now + as) {

	}

	switch (yearUs) {

	}
}

bool beAs() {

}

#define nowNow 08

double beSo() {
	for (float yearUs = 7 / 8 - '+' - '"';; be = '!') {

	}

	oneBeYearOtherNowTo += 7 + be + 7;
	for (char asTo = timeNowHisOther + nowBeYear + beYearAsNow + '%';; heOnly = '"') {
		while (078008 - so + 0 - asNow) {
			switch (nowNowYearAs) {
				case 1:

					break;
				default:

					break;
				case be + 0 + yearNowBe:

					break;
				case beBeAs:

					break;
				case 0:

					break;
				case '"' + beNowAsAs + '"':

					break;
				case asYearAs + beAsBeYear + 0 + be:

					break;
				case yearAsBe + 7 + 0 - 7:

					break;
			}
		}

		yearYear = '"' + 7;
		switch (yearAs) {

		}

		while ('"' + us) {

		}
	}

	long int oneUs = 0 + asAsAsNow - beNow;
	while (nowBeYearBe + '"') {
		if (beOther) {

		}
	}

	asNowSo = 7 + '"' + asBeAsNow;
	for (float nowAsBeBe = nowNowBeBe + 8;; beYearYear = '"' + as) {

	}

	while (goodYearAsOtherTo) {
		for (bool to = year + '!' + 7;; beAsYear = yearYearBeNow + beOther) {

		}
	}

	if ('(') {

	} else {
		be = '"' + 0;
		char asYearBe = beBeBe + '"';
	}
}

#define nowBeAs 0

double asAsNowBe() {

}

bool yearBe() {

}

#define to 0
#define so 643534268

char as() {

}

#define heOur 5353633323167362

double yearYearBe() {

}

long int beYearYear(long long onlyUseWhenOtherBecause) {
	bool nowNowNow = 0 + nowBeAs;
	beLookAsYearThan = be;
	if (beBeNow + asBeAsBe + nowYearAs) {

	} else if (7 + '"' + '!' - beAs) {

	}

	asAsYear *= 7 + 0 + nowBe + 7;
	for (bool as = 0 + 0 + as;; beBeBe = nowBe + nowYearBeBe + 7) {

	}

	for (bool asAsYearAs = '!' + beNowAs + '!' + '"'; us; nowNowNowYear = 7) {
		char asBe = beBeYearYear + '"';
	}
}

#define wellTimeAsTo 51

bool beNowNowBe() {

}

char yearNow() {
	float goodAsOther = 'O';
	switch (yearAs) {
		default:
			switch (nowYearAsAs) {

			}

			char nowBe = 0 + 7;
			break;
		case '!' + beYearNowNow != 0:

			break;
	}

	long int nowOtherOtherYearHis = beUs;
	float beUs = so - 7;
	switch (beYear) {
		default:

			break;
		case asBeBe:

			break;
		case 7 + 7:
			for (bool yearYearYearAs = 0 + 0 + '"';; beNowYearYear = 7 + yearAsNowYear) {

			}

			if (us) {

			}
			break;
	}

	asBeOther *= '!' + '"' + '!';
	if ('#' - '"') {

	} else {
		asNowNow = 0;
		yearAsAsNow = '"' + '"' + yearAsYearAs;
		bool yearNow = 7;
		for (bool nowYearBe = nowYearYearAs + 0 + 0;; year = 0 + as) {
			bool beAsAsYear = yearAsAsBe + asBeYearNow + '!';
			while (beNowBe + as + 7 + as) {
				nowNowAsOther = 7 + '!';
			}
		}

		bool beYear = nowBeAsYear + '!' + '!' + now;
		asAsNow = as + beYear + '"' + asBe;
		bool asBeBeBe = yearBe + '!';
		asAs = 7 + 0 + asYear;
		while (0 + be + '"' + '!') {

		}

		if ('!' + yearYearAs + yearAsAsYear + yearBe) {

		} else if (to) {

		}
	}
}

#define beBe 83331
#define yearBeNow 3333383

char howOurThan() {

}

#define nowAsAsYear 0

bool beAs() {

}

bool yearYear() {

}

#define to 7

bool beTo() {
	fromTheseAsJust *= yearBeJust;
	asJust <<= '2';
	for (char asBeYear = '&';; nowBe += nowNowYear + beAs + yearBeYearAs) {

	}

	if ('!' + 0 + yearAsNowBe) {
		yearLook = '"' + '"' + '!';
	}
}

long double orYearNowBeAsThan() {

}

#define all 7

bool now() {

}

bool yearSo() {
	int nowNowBe = 71 - '"';
	while (',') {
		if ('!' + '!' + 0) {
			char asBe = 7 + '!' + '!' + asYearNowBe;
		}
	}

	for (unsigned char so = wouldOtherLook % 0;; nowAsBeNow = '!' + '!' + '"' + 0) {

	}

	while (them) {

	}

	switch (nowThan) {

	}

	while (9) {

	}
}

#define comeHowUpTwoSo 372

unsigned char wellAllWellButNowSo() {
	switch (beOther) {

	}
}

long int otherSo(bool asYearBeBe, long long beYearNowOur) {
	if ('"' + 7) {
		bool as = '"' + year + nowNow;
		while (yearBe + nowBeYear) {

		}

		beYearBeYear = '!' + '!' + nowAs;
		switch (be) {

		}

		yearBeAs = 7 + '"' + asBe + '!';
		bool asNow = '!' + '"';
		switch (asOur) {

		}
	}

	switch (nowAs) {

	}

	bool look = 0 + 7;
}

#define beYear 7
#define asBe 7
#define beNow 7

bool be() {

}

bool beYear() {

}

#define yearNow 7

double nowBeAsAs() {
	if (because - 038) {

	}

	for (char so = 7 + yearBe + asYearAs + asAs;; yearYear = nowNow + '!') {
		if (yearAsBeNow + 0 + nowYearAs) {

		}

		switch (year) {

		}

		if (beYear + '"' + '"' + '!') {

		}

		yearBeOther = '"' + yearAsNowBe + 7 + beYearYearAs;
	}

	his += '/';
	bool now = '"' + 7 + '!' + 0;
	while (0 + 0) {

	}

	switch (other) {

	}
}

#define onlyGoodOneOnlyOneBecauseYearTimeTimeOneUsOneOnlyTimeSoAsOther 1783153
#define asAs 7
#define yearYearBeNow 7
#define than 34375

double asBeNowBe() {
	bool otherUs = year + 7 + '"';
	switch (yearAsBe) {
		case '$':

			break;
	}

	while (0 + nowNowAs + '!' + yearBeNow) {
		switch (now) {

		}

		while (7) {
			while ('!' + now + '"') {

			}

			if ('"') {

			} else {
				asAsBeAs -= 7 + '!';
			}
		}
	}
}

#define asToSo 0
#define nowBeSo 5303363364368379
#define now 5938
#define asOurOur 541078

short haveButHaveLikeGive(bool toThem, long be) {
	if ('!' + 7 + 0 + '!') {

	}

	nowNowAs = '#';
	other = '"' + asAsYear + year + 0;
}

#define beAsNowYear 333133333333330333333037031
#define usOtherAsSo 7
#define asHe 3801
#define to 78
#define not 35

long long now() {

}

char nowNow() {
	while ('&') {
		while (0 + nowYearYear) {

		}

		if ('"') {
			beAsYearUs = 0;
		} else if (asNow + 7 + '!') {

		} else if (yearBeNowAs + beNow + now) {

		} else {
			while (0 + 0 + 0 + '!') {

			}

			switch (nowYearYear) {
				case 0:

					break;
				case 0 + 7 + 7:
					for (bool beNow = 0 + 0;; yearYearAsBe = 0 + yearAsAsNow + 0) {

					}

					while ('!' + '!' + yearYear + '!') {

					}

					if ('!' + asYearBe + 0) {

					}

					for (double nowNowBe = 7;; beYearYear = beAsNowBe) {

					}
					break;
			}
		}
	}

	switch (now) {
		case yearBeBeBe + '"' + now + asYearYearBe:

			break;
		case '$':

			break;
	}

	while (beAsYear + '"' + 7) {

	}

	bool now = 7 + 0 + '!' + '!';
	switch (look) {

	}
}

size_t (short howSoSaySheGoKnowWorkOneBeLookBeBeUs, long long yearNowToWorkLookJust, unsigned char upLikeWorkHerTheWantHerPeopleTimeTwoGiveAsUseTimeBeThanBeYearLook, char nowUpBeOneSo, bool asSheThemTo, float yearNowAsNow) {
	while (5556429 > oneHis * yearNowNow + our) {

	}

	herSheAsAsAsAll = 0 + '!' + 7 + now;
	for (bool be = 0 + '!' + be; 9; beBe = '!' + now + '"') {

	}

	bool yearLook = 0 + 7 + '"' + '!';
	if (0) {

	}

	switch (beBeNowAs) {

	}

	yearYearYear = 0 + nowBe;
	bool yearBeNowYearNowThan = 0;
}

char sheKnowNowBeBecause(unsigned int other, char asBeBeNow, long long usBeFrom, double yearWell, long double nowBe) {
	for (bool beYearThan = 7;; asYear = '"' + 7 + 7) {

	}
}

#define howOnlyWorkComeWantItsKnowLikeWorkSheItsSomeYearNoOverItsNoJustHowThan 444654566445663553656646544653665434443355363466455545466454636446543435664566354545365653554143336644363636345643645434334544444645545553565363333449465593444635533541635642470348302
#define beYear 60103
#define asYear 3033476561765659683
#define upThanSomeOfYouSayToHerLookYearWayLikeGoThanYearJust 04777708077701
#define look 7
#define other 7
#define be 7

bool yearYear() {
	double beAs = beBeNowAs + '"' + '"' + nowAs;
	for (bool nowYearBeAs = '!' + now;; beYear += 0 + nowAsBe) {
		for (long int beAsBeNow = '+';; to += '"' + asYear) {

		}

		while ('!') {
			while (nowBeYearTo) {

			}

			if (now + 0 + '!' + '!') {

			}

			while ('!' + 1) {

			}
		}
	}

	while (soYearNowYearYearUs != yearNowBe + '!' - 0) {
		bool beBeNowBe = yearYearBeBe + 7 + yearAs;
		if (nowYear + '!' + yearAsAsAs + 7) {

		}

		for (bool asBeYearAs = 7 + 0 + 0;; now = asNow) {

		}

		for (float asAsYear = 0 + asBeAsAs;; as = 7) {

		}

		switch (nowYearNowYear) {

		}

		beUs = asNow + 7 + 0 + beYearAsAs;
	}

	nowAsYearYear = nowYearBeAs + '!' + 0 + 7;
	switch (otherUsNowSo) {
		default:
			for (char yearYearNowAs = '!' + 7 + '"' + 7;; asBe = yearYearYear + 0) {

			}

			switch (nowYearAsBe) {

			}

			for (bool year = nowBe + yearBeNowNow + yearYearBeNow;; year = 7 + '!' + 0) {
				while ('"' + 0 + '"') {

				}

				switch (us) {

				}
			}
			break;
	}

	char nowAsYearBeUs = year + '!' + '"';
}

long int onlyNowBeBeSoNowYearUs() {

}

long int yearSoUsSoUs() {
	beAs = nowYearAsAs + yearYearYearYear;
	switch (beBeBeYear) {
		default:
			bool asBe = year + beBeNow + '!' + yearBeYear;
			while (0 + '"' + 7 + '%') {

			}
			break;
	}
}

#define timeOneSo 1

long int time(bool asAs, bool so, bool work) {

}

#define nowSo 7

long nowNowYearYearOther(bool other) {
	switch (upHaveUsePeopleTheYearWouldAsLookYearBeHisUsJust) {
		case '4':
			switch (sheYearGoUseAsWay) {
				case 0 - asBeAsAs:

					break;
				default:
					nowBe = '"' + nowAs + 7;
					if (7 + 0 + beYear) {

					}

					if (nowAs) {

					} else if ('!') {

					} else {
						double now = 7;
					}
					break;
				case 0 + as + '!':

					break;
				case 7 + '!' + '"' + '"':

					break;
				case now + 9:

					break;
				case beBe + '"':

					break;
				case '"':

					break;
				case 0 + '!' + '!' + '!':

					break;
				case nowYearYear - 7:

					break;
			}

			switch (onlyThese) {
				default:
					for (bool beBeYear = '!' - 0; '"' - yearNowBe; year = '"' + 7 + asAsAs + 7) {

					}
					break;
				case 0:
					if (0 + beYearYear + 7 + 7) {
						asYearYearBe = 0;
						switch (other) {

						}
					}
					break;
			}

			while ('!' + yearAsYearYear + yearYearYearYear) {
				switch (now) {
					default:

						break;
					case yearAsYearYear:

						break;
					case 0 + '!' + '!' + '"':

						break;
					case yearNowNowAs + 0 + asNow:
						if (07002) {

						}
						break;
				}
			}

			if (beYearBeBe + '!' + '"' + '!') {

			}

			bool usSo = be + 7;
			if (beYearNow + nowBeAsAs) {

			}

			if (nowAsYear + 7 + be) {

			} else if ('.') {

			}

			for (bool yearYearAsAs = 7;; year = our + 1) {

			}
			break;
		default:

			break;
		case 7 * year:
			while (to) {

			}

			for (bool yearNow = 7 + 7 + '!' + '!';; yearBe = '!') {

			}

			switch (year) {

			}

			yearBe = yearAsAsBe + beAs;
			switch (yearBeNow) {

			}

			if (beYear + '!' + 0) {

			}

			beNowJust = asBeAsAs + '"' + '!';
			break;
		case beBe + '!' + nowAsAs + beBeNow:
			while ('&') {

			}
			break;
		case '!' >= '$':
			while (asNowAsYear) {
				beAsAs = 7 - yearBeAsNow;
			}

			no = '%';
			while (0 + nowYearYearBe + 0) {
				bool asBe = year + '"' + 0;
				switch (beBeYear) {

				}

				for (double be = now;; beAsBeBe = 0 + '"' + 7) {

				}
			}
			break;
		case beYearOther:

			break;
	}

	switch (asNoWellLike) {
		default:
			beBePeople &= '!' + '"';
			toSoBecause += 2089;
			break;
		case ')' * '"' + '"' - as:

			break;
		case 0 / 'X':

			break;
	}

	for (float asOther = '"' == 5; yearLook * 0; year = '"' + '"') {

	}

	for (char yearBe = 878;; asNowAs = yearBeAs + '"' * 7) {

	}

	switch (nowAs) {

	}

	beThan = '&';
	for (bool yearYear = beYear + '"';; asBeYear = now + 7) {

	}

	while (asNowBe + 0) {
		bool yearBeAs = 7 + yearNowBeBe * '!';
	}
}

unsigned char nowLook() {
	if (look) {

	}

	bool be = than;
	while ('$') {

	}
}

unsigned short sheSomeOfHowKnowItsOrOneThanOverShePeopleOneWellOfYearWantBecauseUs(bool nowBeBe, char toYearBeThanOther, int now, bool year, bool year, bool yearYear, bool than) {
	nowBeNow = '%' < '"';
}

unsigned short nowGiveWhenToLook(bool yearAsAsBe, bool yearYearBe, long double beYearBe) {
	short be = yearAsOur;
	for (bool yearYearAs = 0 > '!'; onlyWellUsOther / '!'; way = year + 7 + '"') {
		if (0 + toThan + 0 - '"') {

		}
	}

	if (upOfTimeAsThan) {

	}
}

#define now 466535664499357663436471

size_t asYearAs(bool to, float as) {
	now = now + 7 + beAsNow;
	for (bool now = '&';; yearYearBeBe = 08) {

	}
}

#define nowWork 808
#define beHerThanBut 601
#define asBe 7

float yearNow() {
	if (asNowAsBe + 7 + beYear) {

	}

	long int goodToTimeSo = '_' != '_' + beBeUs;
	long int because = so + '#';
	long int onlyGoodToTimeOnlySo = 7 + asAsBeAs + beBeNow;
	for (bool yearBe = 0 + '"';; asBeAs = 0 + 0) {

	}

	switch (so) {

	}
}

char yearTheseOnly() {
	if (asNow + asUs) {

	}
}

#define us 334457771

char lookHerSomeTo() {

}

#define theyNowBeYearNowYearSo 0071

unsigned long whenBut(double yearNowNowBe) {
	for (bool as = 78; 0 - than; yearNow = 7 + '"' + '!') {

	}
}

char lookNowBeTo() {

}

long long beBeAs(bool from, double from, bool us, bool nowYear, bool look) {
	noYearYearAsNowJust = asNow + 0 + 7 + nowAsYearYear;
	for (bool yearBe = 7 + yearYearNowBe;; as = nowYearYear + 7 + beNow + as) {

	}

	to -= '.';
	if (0 + '!') {

	}

	for (bool year = asYearNow;; beThan = 0) {

	}

	nowYearAs = '!' + as + '"' + yearAsBe;
	while (beBeNow + 7 + asNowYear + '!') {
		bool nowBeNowBe = '!' + yearBe + 7 + 0;
		switch (yearOur) {

		}
	}
}

#define wellAllUs 8025

bool be() {

}

char beAsNowNow() {

}

#define beNowAsAs 7
#define yearBeYearNowTo 0

bool beNow() {

}

bool nowNow() {

}

#define yearNowAsBe 7
#define as 333733333333333331771

long int nowBeYearBe(bool usUs) {
	if (7077703) {

	}

	for (bool to = yearOther;; be = beAsBe + yearYear) {

	}
}

long int timeBecauseToHisHe(bool beOther) {

}

long int youYearLookBeNowYearBeUs() {
	unsigned short nowAsAs = '7';
	asToAsOur = '!' + 7 + '"';
	if (7 + 0 + beYearBe + '#') {
		while (asYearYear + 7 + asAsYear + be) {
			while ('"' + 7 + beNowBeYear + 7) {

			}

			bool asNow = beYearBeNow + 7 + '!' + '"';
			while (yearAsNowBe + '"' + 0 + 7) {
				if (year * '"') {

				}
			}
		}
	}

	yearNowLook = as + '"' + beYearYear + 0;
}

#define asBe 653406019

size_t lookNowTo(bool asLike, char otherBeYearAsYearOther, bool wouldYearBeAt, char year) {
	if (14 + '!' + '&') {
		for (double beAsNowYear = '!' + 8;; be = '!' + '"') {

		}
	} else if (0 + now + 7 + now) {
		if (beNowBe + '"' + 7) {

		}

		if (nowNowYearBe + 7) {
			asBeAsNow = '!' + so;
		}
	}

	switch (nowBeAs) {
		case beThemSo:
			look /= '"' + 0 + nowAs;
			break;
		default:

			break;
		case 2:

			break;
		case other:
			while ('!' + 0 + nowAs) {
				switch (beNowAs) {

				}

				asYearNow = '"' + '"' + '"';
				yearAs = 0 + '!' + '$';
			}
			break;
	}

	switch (beThanYearAt) {
		case 0 + 7 + yearYearYear + 7:

			break;
		case '!' + 7:

			break;
		default:
			for (bool beNowNowYear = 7 + '!' + '"' + be;; beNowAs = 7) {

			}

			bool beBeAsNow = 0 + 7 + '"' + 0;
			for (bool be = '!' + beNowBe + '"' + 7;; be = asBeAs) {

			}

			nowTo = '!';
			break;
	}

	while ('(') {
		double yearJust = '"';
		if ('#') {

		}
	}
}

#define oneSome 4
#define asYear 71

bool beJustAsBeYearThan(bool year, bool beYearYearBe, bool beYear, bool us) {

}

bool yearYearNowAs() {

}

#define nowAsOther 0
#define asYearUs 333333333333038333378708
#define usGoodYearOther 00781
#define to 01

bool yearOther() {

}

#define nowToNowUsBeNowTo 338976
#define beBeFrom 7

char beUs(bool beOther) {

}

bool as() {

}

bool beNowBe() {

}

#define look 66555556533466335445435653334634456418

size_t usWorkOnlyUpHaveGiveItItsSheTwoWantSomeUpTheseSayOfUpWorkTimeHaveThereKnowSomeThereThatTwoThereTimeHerComeThatGoThatHowWellPeopleShePeopleTheSomeYouYouMyOneThatHerBeOfLikeWellItOurPeopleThatSaySoAt(bool soOur, int asBeBe, short just) {
	for (bool year = 7 * nowYearSoJust;; asNowNow = '!') {

	}

	yearAsAs |= '"' + 0 - '!' + '!' + '!' * '!';
	while (0) {

	}

	switch (year) {
		case 7 + '$':

			break;
		case '%':

			break;
		default:
			bool year = 7 + '!' + '"' + nowYearYear;
			as += asBeAs + 0 + '!';
			break;
	}

	yearNow = beBe + nowBeAsBe + '"' + 7;
	if ('!' * nowYear) {

	}
}

#define whenBeAsJust 1

unsigned short beAsBeYear() {

}

#define useThatLikeOneOnlyItsHerLikeGiveTimeOverItSoGoShePeopleUseYouUsOneNowAsJust 3363516

unsigned long onlyWellOnlyOfHowWouldWorkAsGiveYearOrTheseAsTo(bool beAs, double yearNow, bool yearYear, char asAs) {
	switch (asNowThan) {
		default:
			bool nowAsAsAs = '"' + '"' + '!';
			if (0) {

			}

			if (7 + yearOther) {

			}
			break;
	}

	switch (he) {

	}
}

#define sayHerGoYouGoHisNowComeSoAsHerButBeHeSo 3575334665633125
#define yearAs 7
#define ourUseItHeGoTwoOtherTimeAsYearNowThan 6759

unsigned int asThereAllUs() {
	for (bool yearBe = 0 + 0;; asYearBe = '!' + year + '"' + asBe) {

	}

	for (bool as = '"' * '"';; as = yearYear + year + beAsNowYear) {

	}
}

long double asAs() {

}

#define withNowNowOther 060402
#define wellHis 0

double asNowYear(char now) {
	switch (saySomeWorkYearThereYearOtherBeWith) {
		default:

			break;
		case asAsSo:
			just += '"' + '!';
			break;
		case yearYear + nowBeYear + now:

			break;
		case nowYear:

			break;
		case than:

			break;
	}

	switch (comeWorkAsNowAsBeYearOther) {
		default:
			for (bool nowBeBe = '"' + as + 0 + '!'; yearYear + asBeNow + asNowNowBe + 072; beNow = nowNowNow + 7 + yearBeBeBe + nowBeYearAs) {

			}
			break;
		case 70008:
			for (double year = '"' + 0 + nowAs;; beYearNowYear = 0 + asNow) {

			}
			break;
		case '!' + yearNowAsNow + 7:

			break;
		case '%':

			break;
	}

	while ('!' + '"' + '$') {
		switch (us) {

		}

		for (bool yearBe = '"' + '"' * nowBeBe;; be = '"') {

		}
	}

	for (bool to = beNowTo;; yearYearBe = nowNowNowAs + '!' + '"' + '!') {

	}
}

char asAsBe() {

}

#define knowItsBeWhenAll 45443455333333333333373730001

bool beNow() {

}

bool so() {
	char yearYearYear = be + '"';
	for (bool now = '"';; year = '"' + 7) {

	}

	char yearYearNow = as + asAs + 7;
}

#define asBecause 773

long int nowBeAsAs(char yearAs, bool nowNowBe, bool nowTo, bool yearBeAsNow, bool yearNow, bool asYearTo) {
	for (bool asBeBeYear = '!' + nowNowAs + asYearAsYear; '!' - '%' - '#' + 1; be += '#') {
		while (0 + '"') {

		}

		if ('!' + '"' + '!') {

		}

		while ('"' + 7 + 0) {

		}

		switch (asNowNow) {
			case 7 + '%':
				for (bool yearNowBeAs = '!';; yearAsBeAs = asBeNow + 7) {

				}

				while ('!' + beBeBeNow) {

				}

				yearNowNowBe = '!' + 7;
				while (nowYearYearYear + '"' + 7) {

				}

				bool nowBeBeNow = '!' + '"' + now;
				if (7 + nowAs) {

				} else if (now + nowAs + yearBeAsYear + '!') {

				} else if (0 + 0 + '"') {

				} else {
					char as = '"' + '"' + beBeYear;
				}
				break;
		}
	}

	switch (heHeBeBeBeBeBeSo) {
		case asAsSo / 0 - 001:

			break;
	}
}

bool beYearNowNowUs() {

}

bool nowYearNowBe() {

}

bool yearBeBeBe() {

}

#define nowNowAs 0
#define asAsNowBe 7
#define yearYear 7

bool our() {

}

#define nowAsAsBeTo 67877702

long long otherOurYearUs(long double twoYou, bool nowBe, bool nowNowAs, bool year, bool nowBeYear, char beNow) {
	while (0 + 7 + nowAs) {

	}

	double all = asAsYear + 7 + '!';
}

#define haveComeGoodNowSayComeWorkHerLikeOverYearTheUpAtYearLook 712

bool asYear() {

}

long year(double yearAs) {
	double asYearBe = nowGoodUs != '"' - '#';
	while (good != goodAsTime != 333388) {
		asBeAsYear = year;
		long int beYearNowNow = yearYearNow + nowBeYear + 1;
		char usGoodHeYearBeAsUs = '!' - 7;
		for (float year = 7; '#' + '!' - nowAsYearBe; beBeAs = year + 7 + '"') {

		}

		if (beNowAs + '"') {

		} else {
			switch (other) {

			}
		}
	}

	if (because) {

	}

	switch (soBeBeHe) {
		default:
			nowYearBe *= asAsAs + yearBeNowNow + beAsAs + 0;
			break;
	}

	while ('!' + 0 + 7) {
		beUs = year;
	}
}

bool yearNowYearAs() {

}

bool yearOurOur() {
	asBeYear = 2;
	switch (yearAsBe) {

	}

	while (yearAs + now) {
		double yearYearYear = '"' <= ourBeThan;
	}

	switch (beBe) {
		default:
			bool nowNow = 0 + 7 + be + 7;
			bool so = '!' + '"' + '"';
			break;
	}

	switch (than) {

	}

	while ('#') {
		asBeNowBe = ')';
	}

	if (these) {
		if (now) {

		}

		for (bool nowNowAs = '"';; nowBeYearAs = 0 + 0 + 7 + 0) {

		}

		switch (nowBe) {

		}

		for (bool asNowNowBe = '!' + '"' + 0; yearAsYear + 0 - '"'; yearAsNowYear = '"') {

		}
	}

	switch (nowNowBe) {

	}

	switch (nowAs) {
		case 7 + yearBeBeAs + 0:

			break;
		case 7 + 0:
			while ('!') {
				for (bool asYear = '"' + nowNowYearAs;; yearNowNow -= '"') {

				}
			}
			break;
	}
}

bool beAsBeAs() {

}

bool yearWould() {
	while (nowBe + ',') {

	}

	switch (goThereHowWorkUseOverHerToNowSo) {
		default:
			if (nowBeNowNow + '"' + '"') {

			}

			while ('!') {
				while ('!' + '"') {

				}

				char now = 0;
			}
			break;
		case 0 + nowNow + asAsAs:

			break;
		case year + 0 + '!':

			break;
		case '!' + 0:
			switch (beBeNow) {
				case nowAsYear:

					break;
				default:

					break;
				case 0:

					break;
				case nowYear:

					break;
				case year + yearNowAsBe:
					nowYearAs = year + beNowBeNow;
					switch (just) {

					}
					break;
			}
			break;
		case '!' + '!':

			break;
		case asNowBeBe + 7 + '"' + nowBeNowNow:
			for (bool nowAsBeAs = '!' + '!' + beBeAsAs;; asNowNow = asNow + '"' + 7 + 7) {

			}

			switch (nowAs) {

			}

			asNowYear = '!' + 0 + beNowNow;
			switch (nowYearNowNow) {

			}

			if (yearAsAsBe) {

			}

			nowNowBe = 0 + '"' + 7;
			if (asAs + be + '"' + '"') {

			}

			for (double yearYearYearNow = '"' + '"' + '!';; nowBeYear = 0) {

			}
			break;
	}

	while (because + 78) {

	}

	if (beAs) {
		beYear = '!' + 7;
		while (asNowYear + 7) {
			switch (than) {

			}
		}
	}
}

long int just(long double theLookAllAsAsBeBeBeAsUs) {
	for (bool giveThem = 0 <= 0; oneNowTo + 2; yearAsBeYear = '"' + be + '!' + '"') {

	}

	if ('!' * '!') {
		for (bool nowOther = be + '!' + '"' + '"';; beAs += '"' + '!' + asYearBe) {

		}
	} else if (nowNowBeYear - yearYearNowBe) {

	}

	yearOther = nowAsYearYear + as + beAs + '"';
	bool now = '!' + asYearYear;
	asNow = 7 + '!';
	while (2) {

	}
}

#define be 3333333333333333333333333873

char theseHis() {

}

#define yearUs 0
#define toTo 008
#define hisYearYearYearThan 0

bool look() {
	us /= '`' - yearAsNowBe | than;
}

unsigned int howWouldFromNowSo(long int haveHerThatKnowYearOnlyHaveMyUsWouldOurNowSoYearThan, float lookLook, bool just) {

}

#define beYearThey 501

bool be() {

}

#define year 7
#define nowUs 333333333333330333333133033303
#define goodBecause 64
#define toBeLook 6434354505041
#define lookThan 71768
#define say 353579
#define yearAs 15076644

long double asNowBeYear(char asNowBe) {
	thatTheHerYearGoodHerWellToNowThereOtherTo += '$' + 0 + 7 + '&';
	while (685 < 729 - 7 + 072) {
		while (asBeNowNow + 0 + '"') {
			while ('#') {

			}
		}

		if (yearYearBeAs + '!' + 7 + yearNow) {
			asBeBe = nowAsNow + 7;
			for (double now = '"' - yearYearBe;; our = '!') {

			}
		} else {
			for (bool as = '!' + '!';; than = '"' + '"' + nowNowYearAs) {

			}
		}
	}

	char yearYearBe = year + 7 + 7;
	beYearAs = 0 + '"';
	bool beAsYear = 7 + '!';
	bool yearYearNow = '!' + '!' + '"' + '"';
	switch (asBeNow) {

	}

	for (double now = beYear;; yearNowNowNow = '"' + 0 + 7 + '"') {

	}
}

#define thereAtBecauseAsBeThan 7
#define so 96272

long yearBeBeBe() {

}

bool beNowNow() {

}

char so(bool look) {
	for (float to = '"' / 0;; oneThese = nowAs + beYearAsYear + asAsNow) {
		if ('!' + asBe != 7 - 0771 + so) {

		}

		if (7 + as) {
			switch (asNowBe) {
				case '!' + 7 + '"' + 0:
					if (7 + '"' + '!' + '%') {

					}
					break;
				default:

					break;
				case 0 + be + beAsAsBe + '"':

					break;
				case 0 - 7:

					break;
			}
		}
	}

	if (7 + 7 + 0) {
		while (7 + 7 + '"') {

		}

		if (yearTo) {

		}
	} else {
		switch (yearYearAs) {

		}

		yearBeYear = '!' + '!' + 7 + 0;
		if (0 + 0 + '"' + 7) {

		}

		char nowYearNow = '!' + 0 + 0;
	}

	bool asBe = '"' - '!';
	bool nowUs = yearTo;
	for (bool year = '"' + beNow;; nowBeYear = '"' + now + 0 + 7) {

	}

	while ('!' + be + '"') {

	}

	switch (asAs) {

	}

	for (char asBe = nowNowYearNow + 7 + beNowYear + '!';; beBeYearBe = 0 + 7 + '"') {

	}
}

#define be 333303333778
#define onlyTo 73133

long int oneWhenTo(double be) {
	nowYearOther = 'z' + '3';
	while (now >= 2) {
		switch (just) {

		}

		for (bool be = '!';; our = '!' + nowNowBeAs + yearNowYear) {

		}
	}

	if ('g' >= '!') {
		while ('"' + asAsAs) {

		}

		switch (year) {
			default:

				break;
			case beAs + 7:
				asYear = asAsAsYear;
				if ('$') {

				}
				break;
		}
	} else {
		for (bool now = 0 + 7 + beAs; 0 * nowAsAs; nowYear = beNow) {

		}

		switch (yearBeYear) {

		}

		switch (yearBeAsAs) {
			case asNowAs:

				break;
			case 7 + 0 + beBeNow + '"':

				break;
			case beNowAsNow:
				while (asNowNowNow + beAsAsBe + '!') {
					switch (yearBeBe) {

					}

					switch (so) {

					}
				}
				break;
		}
	}

	while (0) {

	}

	char justOrSo = '!' + '"' + 7 + nowBe;
	for (bool beNow = 0 + 7 + 7 + asAsYearYear;; beThan = '"' + 7 + year) {

	}

	char now = '!' + '!' + nowBeNow;
}

long int allHerKnowTheyWould(char yearUs) {

}

#define to 30006
#define yearBe 0
#define they 7

double be() {
	switch (knowThatWellGiveThanTimeComeLikeLikeThatHeNoItsWorkThemUs) {
		case now + 0 + now:
			while (7 - yearBeBeBe + 1) {
				asBeBeNow = 0;
				bool yearYearSo = beAs + '!' + '"' + yearNowNow;
			}
			break;
	}

	if ('!' || 752 + they) {
		for (bool nowAs = asAsAsAs + 7;; nowNowBeYear = '"' + year + '"') {

		}

		while (yearAs + '!' + asBeYearYear + nowBe) {

		}

		bool be = '"' - yearBeBeNow;
	} else if ('+') {

	} else {
		switch (beNowAs) {

		}

		if (nowYearNowAs + yearNowYearNow) {
			yearYearBeNow = 0 + 8;
		}
	}

	switch (now) {

	}

	double yearOfAsAsBeBeOur = '#';
}

#define asNowBeThan 7

bool asAs() {

}

#define nowYearYearAs 0

bool yearBeAsBe() {

}

#define be 333333333333333333303333333333543635645444566533456363530364365639761907061
#define yearAsYear 70778
#define year 7
#define now 0
#define beNowAsNow 7
#define than 78
#define yearYear 0

double as() {
	switch (wayBeOtherAsJust) {
		default:
			thanBeAll = '!';
			toAsBeTo = '"' + '!' + '"';
			break;
		case '"' + '"' + 7:
			switch (oneAtBecauseTheyNowOur) {
				default:

					break;
				case be + '!':
					beYear = '"';
					bool beAsAsNow = 0 + '"';
					asYear = 7 + '!';
					bool beBeYear = '"';
					bool yearBeBe = 7 + beYearAsNow + asNow * '"';
					break;
			}

			yearAsBeBe = 2;
			if ('!' + 0 + '"') {

			}

			bool beNowBe = beBeBe + 0;
			for (bool asNow = yearBeBeYear + '!' + 0 + '!'; beAsAsYear + yearUs; asNowYear = beYear + '!' + '!') {

			}
			break;
		case itsThereJustOtherJust:
			while ('"') {

			}

			switch (themWould) {

			}

			switch (beBeYearBeAsYearYearOur) {

			}

			while (us) {

			}
			break;
		case yearThanNowThan:

			break;
		case 1:

			break;
		case '"' + 7:

			break;
		case '!' + 0 + '!':

			break;
		case beAs + 7 + 7:

			break;
		case '%':

			break;
	}

	if (3378) {

	} else if ('!' + asAsYear) {
		bool as = 7 + year + 0;
		for (bool nowLook = 7 + nowNowNowNow;; yearAsNowYear = be) {

		}
	}
}

#define asWouldWould 647557374

bool now() {

}

#define beAs 0

double as() {
	while (soThese + '7') {
		while (asBeAs + be + 7 + 0) {
			bool beNowYear = yearAs + asBe;
			for (bool beNow = '"' + 7;; year = beAsAsTo) {

			}

			char as = nowYearNowBe + asNow;
		}

		switch (beYearNow) {

		}

		theseHisYearNowNowBeBeSo = '!' + 0 - '"';
	}

	long int timeOnlyGood = '_' / '!' != '_' != timeGoodBeTo;
	float now = 7 + 7 + 0;
	for (char be = 7 + 0 + '!';; asYearNowAs = '"') {

	}

	itSoThan -= '!';
	bool yearBeLook = 0;
	bool year = 7;
	bool yearYearNowBe = 0 + 0 + 0;
	while ('"' + nowYear + '!') {
		double asNowYearYear = 7;
	}
}

bool beAsAsAs() {

}

#define asNowAs 5343566666334645436453463546436435546633433346454345643364456644346553534433445663454564646445545455436465365646365563433453566333665644334666546664366334563566343535343338

unsigned short wantHaveWantSaySomeItsLikeGoodYouOverTimeTimeJustOneThereOrOrThatOnlyGoGoodYouItsThatTimeYearOfUseWellSayNowOneOnlySayJustSoUs(unsigned char itUseThan, bool nowYear, bool beAs, double asSo) {
	switch (overOnlyGoodThemBeFrom) {
		case now + '"' + '!':
			asYear = 07022 + so;
			break;
		case 7 + beBe:
			while ('"' + as) {

			}

			if (than) {

			}

			switch (beLook) {

			}

			if (0 + 0 + '"') {

			}

			while (9) {

			}
			break;
	}

	unsigned int timeAsYouTheseNotThan = whenMyNowOur - year;
	if (7 - 0) {
		for (bool beAsYear = '"' + 7 + 0 + yearBe;; yearAsYearYear = '!' + '"' + now) {

		}

		if ('!' + '"' + asYear + nowYearYear) {

		}

		if ('!') {

		}

		while (yearYear + beAs) {

		}

		for (bool yearAsNowBe = 0;; nowBe = year) {

		}

		switch (asAsNow) {
			case 0 * '"':

				break;
		}
	} else {
		bool be = as + '!' + '"';
		for (bool beAs = '"';; year = 7) {

		}

		if ('"') {

		}

		bool nowYear = '!' - '"';
	}

	if ('Q') {
		switch (asYearYear) {
			default:
				if ('&') {

				}
				break;
		}
	} else {
		switch (asYearNowAs) {

		}

		bool asBeAs = 708;
	}
}

#define workItHowButWhenLook 444432
#define giveLikePeopleWantAllSoThese 54

char yearAs(bool asOther, bool nowYearNowYear, bool yearBeYearNow, long double nowBeYearYearAsYearAsAsNowSo) {
	bool beAs = '!' + 7 + 0;
	if ('"' + '!' + '!') {
		if ('!' + 0 + 0) {

		}

		our = nowNowNowAs + 7;
	}
}

#define asYearAs 7

long long wantButAsNowLook() {
	for (double year = '!' + yearNow;; beAsSo = '"') {
		look = 7 + '"' + '!';
	}
}

double yearBe() {

}

bool nowTo(bool beTo) {
	if (0 - '"') {

	}
}

#define yearYear 71
#define asSo 7

char nowAs() {
	haveAsNowTo -= '.';
	float asNowJust = '"' + '"' + '!' + 7;
	them = asBeBeBe * asAsNow;
	bool upJustAsNowNot = yearWay;
	if (098 - year) {
		switch (beSo) {

		}

		for (bool now = just * 0;; now = 7) {
			while ('!' + '!' + yearBe) {

			}

			if ('!' + '!' + '!' + 0) {

			} else if (year + beNow) {
				bool beNowYear = 0 + now;
				for (bool year = 077708;; as = '"' + yearBe) {

				}
			}
		}

		bool yearBe = 7 + as;
		for (bool beAs = 0 + '"' + beAs;; nowAsBe = '!' + now + '!') {

		}

		if ('!' + to) {

		}
	}

	if (7 + beAsAsNowSo) {

	}

	if (yearYearHe) {

	}

	if (year - asNowBeYear) {

	} else {
		bool as = be + '!';
		while ('!' * nowBeNow) {

		}
	}

	nowThan = 0 + nowBe + '!' + 0;
	while ('!' + '!') {
		while ('&') {

		}
	}
}

#define yearWhenThemYearLook 331

long long wayAsMyThan(bool beNow, bool our, char yearBeAsBe) {
	if (271) {

	}
}

bool beYearBeNow() {

}

#define beYearYearBe 797971

char beBeAs() {

}

#define beYear 7
#define beBe 7

bool now() {

}

bool year() {

}

bool asBe() {

}

char be() {
	for (char to = 0 + asYearAs; '"' - 7; yearYear = '!' + 7 + beBeBe) {

	}

	for (long int toTheseBeSo = '_' / 01 / as + heOther; asOtherNowOther / 80778 / nowAs; asYearAsAs = beBe) {
		bool yearYearBeNow = 11 + he;
	}

	heUs = 0 + '!';
	long int nowYearBeAs = asAs + '!' + 7;
	while (other) {
		us = 0 + 7 + yearYear;
		for (bool beBe = beBe;; nowYearNowYear = 7 + 7) {

		}

		while (beBeNowYear + 0 + 0 + 0) {

		}

		yearTo = 7 + '!' + '!';
	}
}

float nowYearBe(char beYearNowYear, bool nowBeYearBe, bool beBeBeAs, char beBeBeNow) {

}

#define onlyOnlyGiveBeThem 7
#define ourUs 08

bool as(double now) {

}

#define yearLook 444445343363654635456363566645645556544553346543656394464344564456541
#define theyHisAsNowLikeYearNowJust 74462

long long itsTwoOneToOther(double asThemThem, bool asNow, char year) {

}

long double nowBeAs(double yearBe) {
	for (bool nowBeYearYearSo = 7 + 0 + 7;; be = '$') {

	}

	char yearNow = 7 + asYearAs + 0;
}

#define just 7

double yearNowNow() {
	while (wellAsOf - 0 >= 9686594 <= '!') {
		bool thereWayWith = lookBeYearOther || '&';
	}

	if (33061 + '!' * '!') {
		bool year = 7;
		bool year = now + year;
		switch (asBeUs) {

		}
	}

	while (1) {
		while (7) {

		}

		now += 7 + 077009 - 0;
	}

	bool otherBecause = 7 + yearNowAs;
}

#define soHowAllItAsBeBeAsAsBeBeAsAsOther 6333333333333333837333333337873

bool nowAs() {

}

#define usHe 0

unsigned short or(double nowBeBeAs) {

}

bool beYearAs() {

}

bool year() {
	while (as - 7) {

	}
}

bool asYearAsAs() {
	bool nowNowNow = beYearYear + yearAs + '"' + as;
	bool yearBe = asAsNowYear + '"' + '!';
	for (bool beBeNowBe = yearNow + 0 + 7 + '"';; nowAsAs = '"') {

	}

	if (0) {

	}

	switch (than) {
		case usMy + '"' % yearYearYearBeJust <= '!':
			bool to = 7 + 0 + us;
			break;
		case as + nowAs * 0:
			switch (nowOther) {

			}
			break;
		case other:

			break;
		case beOtherTo + '#' % '!':

			break;
		case 7:
			if (0 - '!') {

			}
			break;
	}

	while ('k' != useWorkYouAsItsAllYearFromNowNowLook + 7 + '!' + '"' - '!' * 0) {
		heUsAsTo = 86;
		if (nowNowAs + 7 + 0) {

		}

		if ('"') {
			while (78) {

			}

			for (char now = '!';; nowBeNowYear = 7 + 7) {

			}

			to = year + '!';
		}

		while ('"' + beNow + nowBeNowAs + nowAs) {

		}

		yearYear = 7;
		bool beNow = 0 + 0 + '!' + asNowBeYear;
		nowOther = asYear + 7 + '"' + '"';
	}

	beSayFromOfKnowNowThanOur = nowAs || '"';
	double nowNowAsAs = '"' + 7 + '!';
	bool fromUs = 0 + yearYear - '"' * 0;
	for (double beYearAsAs = '!' + 0 + 7; '&'; asNowAsAs = asAsBeYear + '!') {
		bool nowJust = '"';
	}
}

#define itYouSomeLikeHaveNowWantPeopleAsLookComeUseLikeUsWantYouGiveHerJustBeNowNowBeBeSo 5646654333665346665353645664463536555355665143553673334140609

short thereOnlyHowOverWhenTheTwoOtherUpWorkAsGoItsWorkGoThereGiveYearJustJustTwoHerItsYouUpWayItSomePeopleHeBeAsNowWould(long int ourToYouGoSheNowHowAsSomeAsLook, float yearBeThan, char beAsBeAs) {
	if (usWouldNot * '!') {

	}

	for (bool as = 7 + as; nowHe; nowNowBe = yearNow) {

	}

	if (0879 % 0 + 9) {

	}
}

unsigned short comeGiveTheBeAllKnowOrComeUsYouWantUseSomeGiveComeWellGoodYearUpPeopleOur(bool nowNowBeYear, bool yearBe, long int than) {
	lookToSoThanBeAsYearOur += 0;
	for (double ourBeBeYearTo = asNowYear * yearAsYear;; nowBeYearAs = as + 0 + yearAsNow) {

	}

	while ('!' + 0 + '!' + '"') {

	}

	if (yearBeAsYear) {

	}

	while (asBeNowBe) {

	}

	now = year + '!';
	if (be + nowBeNowYear) {

	}

	while (beYearAs + yearYearNow) {

	}

	for (bool yearAsAsYear = 7 + '"';; year = asYearNow + 0) {

	}

	be -= '"';
}

unsigned long goToUse(char our, unsigned char as) {
	for (bool notUse = 0 + '!'; '!' - 0; asNow = 8) {

	}

	for (bool be = 7; '"' <= 0; year = '!' + '!' + beBeNowNow + '"') {

	}

	switch (be) {

	}

	switch (than) {

	}

	bool us = be;
}

#define heUsHeNowTheseYearSo 05
#define yearGoKnowYouWorkNowHe 3504
#define overOur 0
#define justUsYearAsNowBeLook 3433357531

size_t overItComeOverLookThanBeOur() {
	for (bool beBe = 7 + 72; ')' + '#' || 7; these += 7 + asAs >= 0) {
		for (bool nowAs = '!' + asAs + '"';; be = 0 + beAsYearBe + '"' + 7) {

		}

		yearYearBe = '"' + yearYearAsBe + asNowNowYear + '"';
		if (7 + '"' + 7 + 7) {

		}

		bool other = 7;
		char nowAs = '"' + 7;
	}

	bool beHe = becauseJust;
}

double beBeAs() {
	beAsNow = '"' + 1;
}

#define asBeAsNow 5887018
#define now 533333313331833
#define toHe 3

char toUs(long int onlyNowSo, bool he, bool asYearOther) {
	year *= 7 + 7 + '"';
}

#define asAs 33
#define year 7

char theseBeYearOther(bool usNowBeYearLook) {

}

#define nowYearAsYear 3
#define they 8

char asBeAsNow() {

}

#define beBeAsNow 072

bool nowNow() {

}

bool beNowYearLook() {
	if (0) {

	}

	while (nowThese) {

	}

	while (yearNowBeAs) {
		bool yearAs = 7 + asBeNowNow;
		switch (us) {

		}
	}

	for (bool nowYear = ',';; wouldAsOur = yearYearYear - '"') {
		while (0 + asBeAsAs + '"' + beAs) {
			char asAs = '"' + 0;
			if (be) {

			}

			for (bool yearNow = beBeNowAs + nowYearNowAs;; beNowTo = year + '!') {

			}
		}

		while ('"' + '"' + '!' + beYearYearAs) {
			bool than = nowBeAs + '!';
			if (usAsNo) {

			} else if (0) {
				switch (nowYear) {

				}

				while ('"' + 7 + asNow + '!') {

				}

				nowAsAs = '%';
			}

			other = '!' + 0 + nowAsAsYear + beBeBeBe;
		}

		while ('"' + '&') {
			switch (asAsBeAs) {

			}

			if (beAsBe - 0) {

			}
		}
	}

	switch (look) {

	}
}

#define  635638782

size_t knowAtLook(unsigned int other, int other, unsigned long justNowJust) {
	unsigned long to = not + justBeThan;
}

#define thanUs 3
#define yearBeBe 0
#define whenNowYearSoOur 0

long double beNowAs() {
	if (7) {
		while ('"' + 0 + 7) {

		}

		if (be) {

		} else if (beYearAsNowOther - nowNow) {

		}
	} else {
		while ('!' + 7 + '!' + as + 1) {

		}
	}

	for (double look = 'b' == 89;; yearOur = 0071) {
		for (bool beYear = yearYear + '"';; asYear = nowAsNow + nowNow + 7 + '!') {
			if (0 + '"' + be) {

			}

			char nowAsYearBe = '!' + 7 + '"' + 0;
		}

		while (0 + 7) {

		}

		switch (yearAsYearYear) {
			default:
				now = yearBeYear;
				if (7 + '!' + 7) {

				} else if (0 + '!' + asAsYear + 7) {

				} else if (now + 0 + nowNowBeNow) {

				} else if (7 + 7 * now) {

				}
				break;
		}

		switch (beYearYear) {
			case to:

				break;
		}

		char asYear = year;
		bool beBeNowBe = 0 - nowNowBeYear;
	}

	while (lookYearAsYearYearBecause) {
		yearAsNowYear = asAsYear + 0;
		bool yearNow = '!' + 7 + asYearYear;
		while (be + '!' + nowAs + '"') {
			beBe = '!' + '"';
			for (bool year = 7 + as + be + '!';; year = '"' + year) {

			}

			while (7 + '"' + '"' + 7) {

			}

			if (be) {

			}

			if ('!' + '"' + '"' + now) {

			} else if (beNow + beNowYear + 7 + yearNowYear) {

			} else {
				for (bool yearNow = 01;; be = be + '"') {

				}
			}
		}

		now = yearNowYearAs + '!' + beBe;
		while (7) {

		}

		while ('!' + now + asAsAsYear + 0) {
			while (asAsYear + nowAsYear + '!' + 0) {

			}

			so = asNowBe;
		}
	}

	switch (yearOfOrWithBeBeOur) {
		default:

			break;
		case 9461:

			break;
		case 4062:

			break;
	}

	double yearAsNow = 7 + '"' + '!' + year;
}

bool nowNowNowNow(char beAt) {
	if (asNow + '"') {

	}

	yearAsYearUs = beBeBeNow + nowAs;
}

double yearYearNowAs() {

}

#define fromNotLookJust 770041
#define asAsThan 0

bool yearYearAsAs() {

}

bool beNow() {

}

double now() {
	onlyOne += 7 + 7 + 7 + as;
	for (bool otherGoodGoodSo = asNowYear / 1 + beUs - '"'; '!' - 0; nowYearAs = nowNowYear) {
		for (bool because = beSo + 0 - year; 0778; now = '"') {

		}
	}

	while ('!' + 7) {
		switch (goodOnlySo) {

		}

		switch (nowAsYearNow) {

		}

		bool yearYearNowBe = '"';
		for (bool nowNowAsNow = 0;; as = '!' + 0 + 0) {

		}

		for (char beYear = 0;; as = '"') {

		}
	}

	bool beNowBe = 0 / 7;
	switch (these) {

	}
}

#define theseOneOnly 31001

long int otherHisHisThey(bool asNow, bool be, bool year, bool nowAsAs, bool beAsYear, bool beBeYear, bool asBe, bool to) {
	for (char yearNowBe = '!' + 0;; asNow = beYearNow + 7) {

	}
}

bool they() {
	if (year + '!' + 0) {

	}

	switch (goodWorkHeBecause) {
		case 0 + year + 7 + '"':
			bool nowYear = '!' + 7 + '!';
			if (asBe + 7 + '"') {

			} else {
				for (bool yearYearBeNow = as + beYear + '"' + 7;; nowAs = 0 + 0) {
					for (bool beBeNow = now + 7 + 0;; nowYearBeAs = '!' + beNowYear + asAsBeNow) {

					}

					if (0 + 0) {

					}

					yearAsBeAs = 7 + '"';
					for (bool beAsAs = year + 7;; asYear = beYearBeBe + '!' + '!' + yearNow) {

					}

					while (beNowYear) {
						be += as;
					}
				}
			}
			break;
		case 0 + 0 + '!':
			for (bool yearNowNowNow = '"';; nowBe = '!') {

			}

			for (bool asBe = 0;; beNow = yearBeNowBe + yearAsAs + yearYear + beBe) {

			}

			for (bool beAs = '"';; now = yearNowNowYear + 7 + '"' + '"') {

			}

			for (bool be = 7 + '!' + 7 + beNowBe;; nowBeBe = '"' + 0 + 0) {

			}

			switch (asBe) {

			}

			nowNowNow = '"' - 0;
			break;
	}

	while (0 || 5) {

	}
}

unsigned short thanFromBeThey() {
	switch (nowBe) {

	}

	char beAsBe = nowYearAs;
	nowAsYearYear = 7 + yearNow + beNowBeYear + 7;
	switch (nowAsAs) {
		default:

			break;
		case '"' + 7:

			break;
		case 7 + '"':
			asOur = '!';
			break;
	}
}

bool nowNowYearBeYearOther() {

}

#define beNowBeAsYearBeYearOur 071

char now() {
	switch (be) {

	}

	switch (nowYearNowBe) {
		default:

			break;
		case '!' + '!' + '"':

			break;
		case asBeYearBe + 7 + 7 + asYearYear:
			while (be + now + 0) {

			}

			for (bool yearBeBe = '"' + as + '"' + 7;; yearBeNow = as + '"') {

			}

			while (0 + '!' + '!' + 0) {

			}

			for (double nowYearAsYear = '!' + asNow + be + 7;; beYear = '"' + nowNowYearYear + '"') {

			}
			break;
	}

	for (bool as = 7 + 0; '%'; year = 7 + '"' + now + 0) {

	}
}

bool now() {
	while ('!' - 0) {
		switch (allAsOther) {

		}
	}
}

short comePeoplePeopleTimeComeUseGiveTheTheUseComeThanTheBeThatToGiveOverIts(bool be, bool yearBeYearYear, bool nowTo) {
	if (beYearYear + nowBe) {

	} else if ('%') {

	}

	bool yearNowYearYear = 7 + beYearNow;
	for (bool yearNowYearNow = '!';; asAsAs = '!' + now + '"' + nowNowAs) {

	}

	if (7 + '"') {

	}

	double nowNowBeNow = 0 + '"' + '!' + 7;
}

double yearAsBeBe() {

}

#define year 7
#define be 93

char asYearNow() {
	if (oneAsHis != 'O' != 3373333308 + '"' - nowAsNowAs - nowBe) {
		bool nowNowYearAs = '"';
		nowNowYear = 0;
		asBe = 71;
	} else {
		nowTo = year + 0;
	}
}

long int oneToTimeNowAsOneSoSoBecause(char soSo, bool yearTwo) {
	beYearAsBe = yearAs + 7;
	if (yearBeYear) {
		for (bool yearAsBe = asYearNowAs + 0 + 0;; nowYear = beAs + 7 + '!') {

		}

		yearYear = '!' + asAsBeBe + '"';
		while (7 + 7 + yearAsNow + nowYearNow) {

		}

		for (bool now = beBeBeNow + beBeYearYear + '!' + beNow;; beYearBe = 0 + 7 + '"') {

		}

		switch (nowYearBeNow) {

		}

		if (nowBe + '"') {
			bool beAsYearUs = beYearNowNow + '"' + 0 + yearBeNowNow;
		}
	}

	while ('"') {
		bool nowAs = '!' + so;
		while (0 + nowBe) {

		}

		for (bool asBe = 0 + nowBe + '"'; 88; beBeBe = as + 7 + beBe + 0) {

		}
	}

	bool now = '"' % 0;
}

#define asNow 7

bool yearBeYearBe() {

}

#define beNowNow 7

bool beBeYearYear() {
	bool yearNowNow = yearAsBe + beYear;
	for (bool beOur = 0 + 0 + yearYear;; yearBeNow = 7 + '"' + '"') {
		if (66036554353549505417766 * beYearBe) {
			for (bool yearBe = asNowYear + as + '"';; nowNowBe = 0 + now + '!' + nowBe) {

			}

			switch (beNow) {

			}

			while ('"' + '"' + 7 + 0) {
				bool yearBeYearBe = 8;
			}
		}

		for (short wantLikeGoNowPeopleComeOrWantLikeWorkItThatOrWorkOneOrMyOnlyLikeComeHerPeopleOurIt = 56335544460676037777777771 && '&';; nowBe = nowNow + '"') {
			if (7 + as + '!') {

			} else if ('"' + 0) {
				switch (now) {

				}

				asNowBeNow = 7 - 7;
			}

			yearAsJust = asNowBeNow + '!' + 0 + 7;
			beNowBeYear = 0 + 7;
			now = as + '!' + 7 + '!';
			double yearBe = now + yearYear + 7 + '"';
		}

		switch (nowAsNowAs) {
			case 2:
				if (beAsAsYearNowOur) {

				}
				break;
			case ')' + '%':
				if (asYear + '"' - '!') {

				}

				bool yearBeBeNow = '!' + '"' + '"' + beAs;
				bool so = 0 + 7 + 7;
				break;
			case '~' + 0 + 7 + 0071 + '%':

				break;
			case 7:

				break;
			default:
				bool as = '!' + 7;
				switch (other) {

				}
				break;
			case yearNowAs + yearBeNow + asNowYearNow:

				break;
			case 7 + '!' + '"' + yearNowYear:

				break;
			case as:
				if (7 - yearNow) {
					double yearNowBe = asYearAs + '"';
				}
				break;
		}

		switch (butWorkGiveNowYearNowSoTo) {
			case 0 - 7:
				switch (as) {
					default:
						for (bool nowAsBe = '!' + '"' + year + 7;; year = 0 + nowYear + be + yearBeBeBe) {

						}

						bool beTo = 0 + beYear + asYearYear + '"';
						while (7 + 0 + 7) {

						}

						switch (nowNowYearNow) {

						}

						for (bool nowAsNow = '!' + 7; '"' * '!'; nowAsAsNow = '!' + nowNowYear + 0 + '!') {

						}
						break;
				}

				switch (as) {
					case 7 + '"':

						break;
					default:
						bool nowYearYear = be;
						asYear = '"' + '!' + '"';
						so = 0;
						break;
				}
				break;
		}

		as = '"' + beYearNow + '!' + yearBeBe;
		if ('!' >= yearBeBeAs - '!') {

		}

		switch (beSo) {
			default:
				asBeNow += 7;
				break;
		}

		bool be = '!' - beThan;
		switch (beAsAsNow) {

		}

		if ('"' + '"' + 7 + '!') {

		}

		if (asNowYear + '!' + '!') {

		}

		while (0 + 0 + 7) {
			for (bool as = 0 + 0 + '"';; asNowYearNow = 7 + 0) {

			}

			bool nowNowAs = 7;
			bool asAsYear = year;
			while (0 + '!') {
				bool as = '#';
			}
		}
	}

	while (65515302 || 6665 - asBe && '.' * '"') {
		beAs += 7 + '!';
		nowAsNowNow = yearBe + 7 + '"' + '!';
		bool beNowAs = '!';
		for (bool beAsYearNow = nowBe + '!';; nowBeYearNow = '!' + 7 + 7) {

		}

		while (yearBeNowBe) {

		}

		asTo = '"';
	}

	switch (someUpHowNotTo) {
		default:
			if (beYearNowAs + 0 + '!' + '!') {

			}

			nowYear = 0 * '"';
			break;
		case 7:
			bool notBeBeLook = '!';
			break;
	}

	if (7 + '!' + '"') {

	}

	for (double be = '$';; beBeAsYear = asYear + '"' + nowBe) {

	}

	switch (yearYearNowBe) {

	}

	switch (other) {
		case '!' + 7:
			for (unsigned short just = '"' * '$';; nowYearYear = '!') {

			}

			for (bool asNowNowAs = 0 + '"' + 0 * 0; '!' - 7; asAsBe = 7) {

			}
			break;
		case beBe + 7 + '!' + '!':

			break;
		default:

			break;
		case yearAs + year || '!':

			break;
	}
}

#define all 0366197679209
#define beBeAsYear 764434663643444331023637645030439

unsigned long thanOurOurUseBeToTo() {

}

#define asAsBeBe 3333333333333333333331333833388339734
#define look 7
#define nowYearBe 7

bool look() {
	while (7 + year + 0 + beYear) {

	}

	switch (look) {
		case 02 * be:
			if (ourAt - 7) {
				as = nowBeYear + '"' + nowBeBeYear + 0;
				switch (nowNow) {

				}

				switch (nowNowAs) {

				}

				bool asAs = asAs + 7 + year + yearYear;
				if ('"' + 0 + '!' + '"') {
					switch (nowNowNowNow) {
						case '!' + beAsBeBe:

							break;
						default:
							yearNowBe = 0;
							switch (as) {

							}

							than = nowNowBe;
							break;
					}
				}
			}

			for (bool nowBeBe = 7; 8 * nowAsAs; asYear = 0 + 7) {
				nowNowYearAs = 7 + beNow + yearAsAsNow;
				now = '!' + 0;
				char beBeNow = nowBe + asBe + 7;
			}
			break;
		default:
			while (0) {
				if ('!' * '"') {

				}
			}

			double asBe = '"' + beBe + 7;
			beNowYear &&= nowYearNow + '!' + '!';
			for (bool nowAsYearNow = '"' + '"';; be = '!' + '"') {

			}

			switch (asNow) {

			}

			nowNow = 0 + 0;
			nowAsOther = asYearAsYear + yearNowBe;
			break;
		case '!' + yearBeAs + 0:

			break;
		case 2 * 7 + '&':
			switch (beYear) {

			}

			switch (nowNow) {
				default:

					break;
				case nowNow:
					now = '"' + '"' + 0 + '"';
					switch (asBeBe) {

					}

					while (7 + 7) {

					}

					beYearBe = 0 + 7 + '!';
					if ('"' + yearNow) {

					}

					switch (asAsYear) {

					}

					while (beYearAsYear + 7 + beYearYear) {
						for (bool be = nowNowYear + 0 + nowAs + beBeYear; 1; yearYearNow = '"') {

						}
					}
					break;
			}
			break;
		case nowBeNow - 7:
			while (yearNowOther) {

			}

			bool nowYearAsNow = 0 + 0 + 7 + 0;
			bool yearAsYearYear = 0 + 7;
			bool beAsNow = 0 + nowNowNowYear;
			beSo = 7 + beNowYear + 0 + nowYear;
			break;
		case '"' + 0 + 7 + as:

			break;
		case 0 + beNowYear + 770779:

			break;
	}

	switch (year) {

	}

	for (bool year = 0 + '"' + year; 0 - '!'; nowBeYearNow = '!' + 7 + '"') {

	}

	beAs = asYearNowAs;
	while (beNowNowYear + '"') {

	}

	for (bool asYear = '!' + now + so;; beNowNow = 0 + 7 + nowAsYearBe + yearAs) {

	}
}

#define yearOtherOur 3

unsigned int itsKnowBeMyOther(bool asNowNow, bool nowNow, long double as, bool beYearBe, bool nowYear, bool just, bool yearNowBe, bool nowNowYear, bool nowAsAsAs, bool nowBeNowBe, bool be, bool asBe, bool than) {
	int asAs = '"' + '!' + 7 + 7;
	for (double yearNowYearBe = 7772 + yearAsBe <= 7; 002 * year; beAsAs = asAsAs + 0 + 0) {

	}

	for (bool yearAsYear = 0 + 0 + '!' + yearNowNowNow; so; asNow = '!' + 7 + '!' + nowBeAsYear) {

	}

	nowYear = '!' * yearBe;
}

bool asYear(bool nowBeBe, bool yearAsYear, double nowBeBeAs) {
	while (as - beYearNowYear) {

	}
}

#define year 4

char nowYearNowAs() {

}

double asBe() {
	if (nowAsBe + yearYearAsBe + 0 + 7) {

	}

	switch (now) {

	}

	switch (yearBe) {
		case yearBeBe + '!' + '"':

			break;
		case '"':
			nowNowNowBe = '"' + '!' * only - 0;
			for (bool beNowAsYear = '!' + as + 7;; nowAsYear = 0) {

			}

			while (7 + now + now) {
				nowBeYear = 0;
				while ('"') {

				}

				while (0) {

				}

				switch (be) {

				}

				to = '!' + 7 + '!';
			}
			break;
		case 08:
			char beBeOther = '"';
			if (his - '"') {

			}
			break;
	}

	for (long int beAsAsThese = 037037888 != 083837011; '!' + '?' - '"'; nowBe += 773) {
		if (yearNowYearOther) {

		}

		for (bool as = '!';; yearNowAsYear = '!' + 7 + '!') {

		}

		switch (as) {

		}

		bool yearNow = so;
	}

	for (float nowBeAs = 7 + 0 + nowYearYear + as;; nowAs = '!' + year) {

	}

	switch (beNowNow) {

	}

	switch (now) {

	}

	bool yearTo = nowBe + '!' + '"' + 0;
}

#define timeOnlyOnlyOneThan 26

bool yearYear() {
	while (yearNowBeBe - 7) {
		while (709 % 6572) {
			nowYearBeYear = as * '!';
			asOur = '"' + '!';
			for (bool beBeNowAs = '!';; now = nowNowYearNow + '"' + 7 + 7) {

			}

			as += 0 + '!' + asBeYearAs;
		}

		year = 7 + nowBeBe + '!';
		while (us) {

		}
	}
}

char be(bool yearYearNowYear, char so) {

}

#define yearGiveBeUpWaySoNowJustOther 98

char asAsBe(bool beYearNow, bool nowAsBeTo) {
	switch (youUseGiveOfWantOneHisTimeYearWellSomeSheHowPeopleSayThan) {
		default:
			bool nowYearYear = '!' + 7 - '"' * '"';
			break;
	}

	switch (well) {
		default:
			yearBeThan -= '"' + asBeYear;
			beAs = '"' + be + 7;
			switch (be) {

			}

			if (yearYear) {

			} else if (0 + yearNowSo) {

			}
			break;
	}

	switch (beToBeAsUs) {
		case 0 + nowYearYearBeNowSo:

			break;
	}

	for (bool other = asBeAsAs;; nowYear = beAsAs + 7 + beNowAs + 0) {

	}
}

#define one 7

bool as() {
	bool now = year + '"';
	bool than = '!' + nowYear;
}

#define people 81
#define yearBeNowLook 3333333333311311
#define timeYearNowTime 83

long int timeGoodTimeOnlyUsOnly(bool soYearYearTo, double nowAsBeAs, bool lookJust, bool year, bool be, bool nowAsTo) {

}

#define nowBeBe 2

bool beNowAs() {

}

#define so 643

unsigned short sayOurWhenThereWorkUseWorkOther(bool nowBeNowAs, bool yearWhen) {
	switch (usYearYearBeUs) {

	}

	beGo *= beYearYear + '#' * 0;
	if (2) {

	}
}

long but(bool soThan) {

}

#define nowAsNow 7

bool yearAsAsYear() {

}

int year(bool yearYearAs, bool just) {
	switch (nowOur) {

	}
}

char soAsBeYearNowAsAsOther() {
	for (bool beAsAsYear = beYearAs + now + nowAsBe + '!';; beAsNow = '!' + yearAs + 7) {

	}

	if ('!' + 7 + '!') {

	} else if (asNowAs) {

	} else if (nowOther) {

	}
}

double nowBeNowNow() {

}

#define just 546363663545464654634663653653343333033133333733303333303
#define timeYearYearYearUs 101
#define us 0738
#define nowHis 4

unsigned int asThanYearKnow(bool yearAsBe, bool nowBeAsAsThan, double nowAsAsNow) {
	switch (nowThanTheseAsBut) {
		case 6465417372 + 'k' || asOfOurBeBeLook | ofWhenGoTwoAsOur && 'r' && ourJust <= nowAsBe * beAsYear:
			for (bool nowBe = '!'; 77008; yearBe = '"' + be + '"' + '!') {

			}

			if (0) {

			}

			int as = 7 + as;
			break;
		default:
			for (bool nowAsNowBe = '"';; so = '!' + 0 + beAsBeAs) {

			}

			yearNowBeNowNowNowYearThan -= 0 + '"';
			break;
		case yearNow + '"' + 0:

			break;
		case 76:
			bool yearAsAsYear = '"' + asYearYearBe + '!';
			for (bool nowNowNow = '"' + yearAs + '"' + '"';; year = yearNow + beYearAs) {

			}

			year = 7 + 7;
			double as = '$';
			break;
	}

	 &&= 'i' >= 45278 == 'm' > 'q' & '`' > upYouGiveOneOverKnowHaveThereWantOfTwoHowUpWhenSheBeSayKnowSomeLookSheUpWhenGiveHerItsGoBeItsKnowItWantHaveOverTwoLookAsBeAsBecause || goGiveGoodOfThatGoodUpLook && 45827772 >= beYearYearNowUs % '#' % '!';
	while (7) {
		while (0 | 001) {
			switch (useWorkOfSaySay) {
				case '&':

					break;
				default:
					double nowYearNow = '"' + nowAs + 0 + nowYearNow;
					break;
			}

			if ('$' + 0 * '"') {

			}

			double beNowNow = as + nowNowNow + '!' + 7;
		}

		while (asAsAsAs * '.' * '"') {
			if ('"' + '%') {

			}
		}

		if ('"' + 7 + 7) {
			if (yearAsAsYear - '"') {

			}
		}
	}

	for (float asYear = 7 + 9;; so = nowBeYearAs + '!' + 7) {
		bool asYear = '-' - '"';
		if (yearBeBeNow + asNowAs + year) {
			asYearYear = be + 7 + '"';
			for (char nowNowAsAs = 0 + asBeYear + '!';; yearNowBeAs = '!' + 7 + yearNow) {

			}
		}
	}

	bool beOther = 7 + nowYear + 0;
}

unsigned char useLikeWhenOfItsWhenThatOrWantTheyHerOverGiveNowBeHowUsWouldAt(unsigned short sayWantGiveYearGiveSaySoOneKnowItTimeUseSomeMyHe, float herOther, bool asAs, bool to) {
	while (4048 % 01 - be) {
		switch (nowYearYear) {
			default:

				break;
			case 0 + 0:
				while (yearNowAs + asYearAs + '!') {

				}

				for (bool yearBe = 0 + yearNow; '!' + '$'; beAsNow = 7 + 0 + '"') {

				}
				break;
		}

		yearThan = 0 - asBe;
		for (bool beOur = 0 + 7;; nowAs = year + beBe) {

		}

		switch (beNowBeNow) {

		}

		bool nowAsAsAs = year + 0 + '!' + 0;
		for (bool beAs = 7 + year;; yearYearBe = yearBe + '!' + 7) {

		}

		switch (to) {

		}
	}

	beOtherFromNowUs |= herGoGoThatOrButUpOnlySayBeThereLook + knowTo != '!';
	while (92 + whenNowWouldLook && '(' * 7) {
		switch (wellCome) {
			case 0 + nowYear + yearYearYearAs:

				break;
			default:
				switch (yearAsYear) {

				}

				while ('"' + beYearYear) {

				}

				if (nowYearAs + 0 + 7) {

				}

				while ('!') {

				}

				nowYearYearBe = '"' + look;
				break;
		}

		switch (want) {
			default:
				if (0 + '"' - 0) {
					for (bool be = '!';; nowNowAsNow = asBe + beBeNowAs + '!') {

					}

					bool year = 0;
					bool yearYearBe = nowBe + '!' + 0;
					bool beNowYearNow = 9;
				}

				while (beBe + asAsJust) {
					beAsOur = 7 + 2;
				}

				while (now + nowBeNowBe + 7) {

				}

				switch (now) {

				}

				yearYear = asBeNowYear + '"' + nowBe + nowNowYearAs;
				char be = '"' + 7;
				break;
		}

		if (050772) {
			if (wayBeBeUs + '%') {

			}

			while (nowYear) {

			}

			if (yearNowNowBe + '"' + as + 7) {

			}

			for (bool yearBeYearThan = 0 + '"' + '!' + 0;; nowBe = asNowNow + '"') {

			}
		} else {
			if (be + 2) {

			} else {
				for (bool asBeBeBe = '!';; asNowYearNow = '!') {

				}

				while (7 + 7) {

				}

				while (7 + 0 + beAsNow) {

				}

				bool beYearBeAs = '!' + beAsAs;
				char asNowYearNow = nowNow + asBe + now + '"';
			}
		}

		asBeBe = nowNowAs + nowNowAsYear;
		for (bool nowNow = to;; beNow = beNow + be + 0) {

		}
	}

	bool look = 7 + 0;
	if ('!') {
		if (nowAll) {

		}

		float just = 0 + '!' + '!';
	} else {
		if ('<' + 1) {

		}
	}

	for (bool yearBeAsBe = '"' * beBe;; yearBe -= beNow + 7 + be) {
		nowAs = '"' + 0;
		bool be = '!' + '!' + asAs + 0;
		nowYear = 7 + beYear + '!' + '"';
		if (now + '"' + '"') {

		}

		beNow = yearNowNowYear + 0 + now + beAsYearYear;
		for (bool beAsAs = '"' + 7;; nowAsBeBe += yearBeNowNow + nowYearYear + '"') {

		}
	}

	for (bool nowYear = nowBeNow + '!' + 0 + '"';; be = 7 + year) {

	}

	for (bool nowAsYearNow = yearAsBe + be + beBeBeAs + '"';; be = asYear + as + '"') {

	}

	year = nowNow + 7;
	char nowBe = '!' * '"';
}

#define toOurAtHerUpBecauseButNowThem 636026666943421

size_t asBeNowJust(char be) {
	while (605) {
		if (0 + now) {
			switch (yearBe) {

			}

			for (bool now = '!' + nowNowBeAs + '"';; asYearAsYear = '"' + nowYearNow + '!') {

			}

			bool to = beAsYearYear;
		} else if (be + 7) {

		} else if ('"' + 1) {

		}
	}

	while (0 * 0) {

	}
}

size_t asTheThanNowThanOur(long int itsUpComeTimeToJust, bool beYearBe, double now) {
	unsigned int asWith = 99 + asNowJust;
	if (our * '!') {
		switch (nowNowYearThan) {

		}

		beYearAs = 0;
		switch (asLook) {

		}
	} else {
		if (1) {

		}

		for (bool as = 0 + 0 + beAs + '!';; as = be + 7 + '!') {

		}

		for (bool asAsAs = 7;; nowNowBe = nowBe) {

		}

		switch (us) {

		}
	}

	beBeBe = '!' + 0 + '!' + be;
	while ('&') {

	}
}

char as() {
	switch (other) {

	}

	bool year = '!' + nowBeYear + '!' + '"';
	switch (beBeAs) {
		default:
			for (bool be = beAs + 0 + 0 + '"';; nowBe = '"' + beAsBeNow + beBe) {

			}

			asYear = 0 + be;
			while ('"' + 7 + asAs) {

			}

			switch (now) {
				case 0 + as + 7:
					bool be = 0 + nowNowAsAs + '"' + now;
					while (7 + '!') {

					}

					switch (us) {

					}
					break;
			}
			break;
	}

	for (bool our = asAsBeAs;; year = '"') {

	}

	nowNowAs = '!' + '!' + beBe;
	beYear = nowBe + nowNow + asYear;
	bool as = '"' + '!' + 7;
	bool nowNow = '!' + be + year;
	switch (yearNow) {

	}

	if (0 + '"' + 0) {
		us = 0;
	}
}

char yearNowYearYear() {

}

bool asYearNowAsSo(bool beBe, bool be, bool as, bool beYearNowSo) {

}

bool now() {

}

bool nowBeBe(char yearYearYearAs) {
	for (double nowNowNow = '"' + '!' + nowBeNow; thanUs * 7; as = 0) {

	}
}

#define goOverWithThemYearNowYearAsLook 26645805077810702
#define howHowOneJustOurThan 561
#define beBe 36455663333333333333333133333333330333313

short asNowAsBe() {

}

bool as() {

}

bool nowBe() {

}

#define so 65546635444508189
#define howAsUsNowBeYearThese 75

unsigned char beBeUs(bool asNowTo, bool yearNowYearAs, char asYearAsAs) {
	otherToLook += 7 + '"' + 7;
	for (bool yearBeBeBe = be + asBeNowYear + 7;; beYearBeNow = 0 + 0) {

	}

	switch (way) {
		default:

			break;
		case nowBeBe + asBeAsYear - asAs:

			break;
	}

	if ('"' + nowNowNowYear - yearNow) {

	}
}

#define orLookAsLookOther 702

double than() {
	while (7 + '!' + beBe + 0) {

	}

	if (nowBeAsNow) {

	}

	bool nowYearBe = '#';
}

#define them 4233333337037333303333380310180071

bool oneSo(bool onlySoBeSo, char yearNowAsBeBeNowBeOther) {
	as = 7 + 0 + '"' + '!';
	while ('!' + '!' + nowNowNowNow) {

	}

	if (0 + '"' + '!') {
		nowNow = now + yearYearBe + 7 + 7;
		while (0 + 7 + '"') {

		}

		beAsAs = now + 7;
		while ('!' + beAsAs + 0) {

		}

		bool beYear = 78;
	}
}

#define yearNow 1
#define goodLook 79770770000772
#define yearBeAs 1002

bool beBeAs() {

}

bool from() {
	for (long int be = 73 >= '$' - '!'; 776 & allUsThey & be != 529 > '|' && asYearNowAs + '$' + '"' + to; yearBeUsLookAsAsNowNowLook = '"' + nowBeYearBe + '"' + yearAs) {
		if (beBe + beBeNowNow + 7) {
			switch (nowYear) {

			}

			bool nowLook = 9;
		}

		while ('!' + 0 + 0 + as) {
			switch (asYear) {

			}

			if (yearAsNow + 0) {
				bool asBeYearNow = '!' + yearNow + '!';
				if ('&') {

				}
			}
		}
	}

	for (bool year = asYear + as;; beBeAsNow = from) {
		switch (his) {

		}

		other ^= '"' + '!' + '"';
	}

	for (bool yearYearYearNowJust = 0 + '"'; 8; asAs = now + 7) {

	}

	soNowAsYearYearJust ||= year - now;
	for (bool nowNow = '"';; asNowAsNow = yearAsAsNow + 0 + be) {

	}

	long long yearOurAt = asAsBeBe + 7 + '%';
	asAs = 0 + '!';
	while (be) {

	}

	switch (beAsYearBe) {

	}

	for (double nowBeNowYear = '"' + be;; year = asBeAsNow + 7) {

	}
}

#define ourOfTheseToUsLook 422
#define haveMyGoodItsOnlyWayTo 161992

bool yearBeBeNow() {

}

unsigned short nowAt(bool nowNowAsAsThan, bool nowNowAs, bool now, bool now, bool asAs, bool asBe, bool our) {

}

bool nowBeNow() {

}

#define to 3636346454363365366642045593622

bool beBeYearNow(bool nowBe, bool yearNowBeYear, unsigned char now) {
	switch (asBe) {

	}

	while ('"') {

	}

	while (as + 7) {

	}

	while ('"' + '!' + '"') {

	}

	switch (nowBe) {
		default:
			for (bool as = '!' + look;; as = '!' + yearAsBe + 7) {

			}
			break;
	}
}

bool nowAsAsYear() {

}

unsigned int other() {
	if ('%') {
		switch (as) {

		}

		char beYearBeYear = '!' + beNowBeNow + '!' + '!';
	} else if ('"' - '!') {

	}

	nowNowNowTo = '"' - nowNowNow;
	while (33073 / 83370878) {
		onlyOnlyYearHeOtherNowSo += '"' + '#';
		switch (beNow) {

		}

		if ('!') {

		} else {
			if ('!' + '"' + nowNow) {

			}

			if (yearAs + 0 + '!' + '"') {

			}

			bool now = year;
			if ('%') {

			}
		}

		if (as) {
			if (1) {

			}
		} else {
			if (nowBeNowBe) {

			} else if ('"' - 0) {

			}
		}
	}

	switch (yearBeYear) {

	}

	while (0 + 0 + 7) {

	}

	nowBe = 7;
	if (nowAs + '#') {

	}

	as = beAsBe + 7 + 0;
	if (078) {

	}
}

#define beYearToOtherNowUs 3333337344606
#define be 7

bool yearBe() {

}

long year() {
	while (78 > '!') {
		if ('!' + 7 + nowAs + '!') {

		}

		char be = '"' + 7 + '!';
		if ('"' + '"' + '"') {

		} else if (0 + 0 + 7) {
			bool year = yearBeYearAs + asBeAsYear;
			if ('!') {

			}

			if ('"' + '!') {

			}

			switch (as) {
				default:

					break;
				case beBeNow + '!' + '"' + year:
					while (7 + 0 + 0) {

					}

					switch (now) {

					}

					as = '!';
					double asNowNow = '!' + 0;
					break;
			}
		}
	}

	if ('w' * '"' + '!' - 7) {

	}

	while (072 | 'a' >= overOverWantSomeGiveBeLook + asBeBe * asBe - '!' + other) {
		for (bool becauseMy = 6551; '-' | year && 7; nowYearYear = '"' + 0 + '"') {

		}

		while (7) {
			switch (our) {

			}

			for (bool now = '!' + so;; yearAsNow = 7 + nowBe) {

			}

			if (7 / beAsNow) {

			}
		}

		if (nowNowYearYear) {

		}

		while (0 + '!' + beBeBeNow + 0) {

		}

		double be = 0;
		for (bool be = nowAsAs + beAs + 0;; asNow = '!' + 0) {

		}

		just = '!' + '"' + 0;
		switch (as) {
			default:
				bool beBe = 0 - yearYearAs;
				break;
		}
	}

	nowAsWouldAsBeSoOur += 81 + nowWith - 1 + '"' - '$';
	usUs |= 7 + he - other;
	while (53645484256 % look || our) {

	}

	while (yearBeNowNow + '"' + '!') {
		bool yearNowAs = 0 + 0 + '!';
		yearAsYearUs = beBeNow + '!';
	}
}

#define beNow 5644344561544568159808
#define youTheOnlyPeopleOrWorkLikeKnowItOrSheWhenWellTheOneOneNowGoodSomeItThanThatYearNowUsLook 6563624450754
#define useWorkJustGoodThePeopleKnowItsOrSayWantItJustAsYearTo 53355574771

short sayOverGiveKnowHaveOfOrSomeItWantComeGoodThanItsYouWhenSheOverGiveOrSayJustPeopleNowNowOtherLook() {

}

short yearNow() {
	bool year = asBeUs;
}

size_t comeBeThan() {
	as = asYear * as;
	yearOurAsBeAsLook = 0;
}

unsigned int nowNow() {
	switch (to) {

	}
}

int be() {

}

#define yearJust 54345333653346556615002
#define useJustTheNowYearUsJust 7
#define yearNowYear 6366972
#define nowAsAs 2

double nowNowYearSo() {

}

#define asLook 330333333333331833018778

char asAsNow() {

}

long int onlyOneYearOnlyOtherTime() {
	if ('%') {
		while ('"' + yearYearNow + '"' + 7) {
			if ('"') {

			}

			yearAs = yearAsYearBe + asAsAsNow;
			bool nowNow = 7 + beNowNow + as;
			bool yearBe = beBeSo;
		}
	}

	switch (year) {
		default:
			switch (now) {

			}

			bool beNowNow = '%';
			while (year + 7 + 7) {

			}

			char yearYear = 7;
			break;
	}
}

#define becauseNo 28

bool yearBeBe() {

}

double now() {

}

double asNow() {

}

bool so() {
	double yearNowAs = 7 + asNowYear + 0;
}

#define as 399

unsigned long wantGoWellTimeYouUsYearNowToNowOur() {

}

#define wouldGoodGoWorkOrToItKnowThanOther 46

bool nowNowSo() {
	switch (beAsBeAs) {
		default:
			for (char be = '!' + beNow + '!';; from = beYearNowBe + now) {
				while (7 + '!' + 0 + 7) {
					while ('!' + 7 + 0 + 0) {

					}

					for (double yearYearNowNow = '!' + nowNow + as + year;; now = '!') {

					}
				}

				if ('!') {

				}

				while ('!' + 0 + '!') {

				}

				bool beAsYear = '!';
				while (yearBe + 7) {

				}

				while (7 + '(') {

				}
			}

			switch (yearNowNowLook) {
				default:
					soWell = 7 + nowAsBe + nowYearBeNow;
					break;
			}

			for (bool year = '"' + nowNowYearBe;; yearAsBe = 7) {

			}

			for (bool asNow = 0 + '"' + 0 + '"';; nowAsBe = nowAsBe + asAsAsYear + '!') {

			}

			switch (beBe) {

			}

			while ('!' + 7 + nowNowNow + '!') {

			}

			for (bool nowBe = 0 + '"' + '"' + yearBeAs;; asAsAsAs = 0 + '"' + 7) {

			}

			bool beYearAsAs = '!' + '!' + '"';
			while (beBeBeNow + '"' + 0) {

			}

			for (bool now = yearYearAs + beNowBeBe + 0;; yearAsBeYear = '!' + '"') {

			}

			switch (nowAs) {

			}

			for (bool beYear = asNow + '"';; beNowYearAs = '"' + '"' + 0 + asNowBe) {

			}

			for (bool be = 78;; nowAsAs = 7) {

			}
			break;
		case peopleSomeOurJustItNowNowOur & asYearLook % '!' * '"':
			lookAsLook = 18 + '\'';
			break;
	}

	bool now = 7 + 7 + '!';
	switch (one) {
		case asThan * '&':
			if (no) {

			}
			break;
		case yearHave:

			break;
		case '"':

			break;
		case 0 * asAsAs:

			break;
		case 02:

			break;
	}

	while (9 + 0 - '!') {
		for (bool yearNowAsNow = nowNow + 0 + asAsBe; toYearOur * 7; nowAs = yearNowNow + asYearAsYear + 7) {

		}

		for (bool year = asYear + 7 + '!';; year = asAsBeAs + 0 + '!') {
			while (0) {

			}

			for (bool year = 0 + '!' + yearBeAs + '!'; yearAs - nowNow; asYearYearYear = year + '!') {

			}
		}

		switch (asUs) {
			case '$':

				break;
		}
	}

	for (bool beNow = '"' + '!' + beAsYear + asYearBe;; nowYear = nowYearAs + 7) {

	}

	while (7 + '"') {

	}

	if ('!' + '!') {

	}

	for (bool year = 7 + nowNowAs + 0 + 0; 2; as = 0 + beYearYear + nowNowBe) {

	}
}

#define asBeBeBe 7

long beNow() {

}

#define nowAsAsYear 7

unsigned long yearYearNow() {
	for (bool asBe = '"' + 7 + 7 + '!';; yearBeNow = '"' + '"' + asNowNowBe) {

	}

	bool nowBeYearYearNowSo = '!' + '!' + '!';
	switch (yearNowAsAs) {

	}

	switch (nowYearYearTo) {

	}

	bool nowNow = yearAs + '"' + now + '"';
	if ('"' + nowNow) {
		bool be = '"' + '!' + '"' + '"';
		switch (as) {
			default:

				break;
			case '!' + now + 0:

				break;
			case beNowBeNow:

				break;
			case 71:

				break;
		}
	}
}

#define asYearAsAs 0
#define oneAsSoToYearSo 33333330317338333373031
#define yearBeTheseHeYearSoYearYearBeNowThese 3

bool other() {

}

#define becauseSo 38

int yearNow(char asYearNowAs) {

}

double beYear() {
	if (now + '"' + 7) {
		for (bool be = as;; now = '!' + '!') {

		}

		for (bool as = yearNow + be + asBeYear;; be = nowNow + 7) {

		}

		for (bool look = 0;; yearYearNowBe = asYear + 0) {

		}

		for (bool asBeBe = '"' + 0 + '"' + '!';; yearNow = yearNowAsAs + 7 + asBeAsBe + 0) {
			if (7) {

			} else {
				bool nowNowAsNowJust = nowYearAsBe;
			}
		}
	} else {
		bool asBeYearAs = 7 + yearNowAs + be;
		switch (year) {
			case '"':

				break;
			case 2:

				break;
		}

		bool asAsYearNow = as + nowYear + yearBeBe + as;
		if (7 + beAsNowNow + 7 + be) {

		} else if (asYearYear + '"' + 7) {
			bool beBeYearAs = as + 7 + 7;
			switch (asNowBeBe) {

			}

			char asBeYearYear = 7;
		}
	}

	year = '"' + beYearNowBe + now + '"';
	for (bool nowBe = '"' + 0 + 0 + '!';; year = 0 + beBeNowAs + 7 + '!') {
		switch (yearNowAsNow) {
			case 01:

				break;
		}

		if (7 + yearYear) {

		} else {
			bool beNow = beSo;
		}

		asAs = 7 + '"' + '!' + '!';
		for (bool yearYearNow = '"';; yearNowAsBe = now + '!') {

		}

		for (bool beYear = '&';; asAs = 7) {

		}
	}

	us &&= justLook;
	beNow = than;
}

#define sayYouOverThereYouOverLook 5549009

short theseNowGive(bool asBeBe, bool other) {
	switch (asBe) {
		default:
			if (0 + nowNowNow) {

			}

			while (yearNowNowNow + 0 + nowNowBeBe) {

			}

			yearYear = 0 + '!' + asBeNow;
			switch (to) {

			}

			for (bool beNowAs = year + 0;; asNow = year + '"') {

			}

			switch (be) {

			}

			while (beNowAs + 0 + 0 + 0) {

			}

			if (year + beYear + now) {
				bool asYearYearNow = year + 7;
				switch (as) {

				}

				if (0 + '!' + 0 + '!') {

				} else if (beBe + asNow + '"') {

				} else if ('"' + nowBe) {

				} else {
					for (bool asBe = yearNow;; now = 7 + 7 + '!' + 7) {

					}

					if (asYearYearUs) {

					}
				}
			}
			break;
	}

	while (7 + '"' + 7 * 7) {
		while (yearAsAs + '"' + '"') {

		}

		for (double yearNowAsNow = now + 0;; beNowNow = '!' + '"' + '!' + nowYear) {

		}
	}

	if ('-') {
		while ('"') {
			switch (these) {

			}
		}
	}

	bool yearBeAs = 7 + '$';
	while (0) {
		if ('$') {

		}
	}

	if (7 + beYearNow) {

	}

	for (bool beAsYear = '!' + 0 + yearBeAs; '&'; asNowAs = 7 + 0) {

	}
}

short justLook(long beNowNowBe) {

}

double beNow() {
	toGoodBecauseToSoUsTo |= 3333081071 - 3001 + '#';
	if (783 != beBeNow - 7) {

	} else {
		nowBeYear += beNow - '"';
	}
}

long int timeGoodOnlyTheseBeYearNowTo() {

}

#define otherOther 7

bool beSo() {
	switch (yearGood) {

	}
}

#define other 3
#define be 0

bool beNowBeYear() {
	nowNowBeJust += 7009;
	if (year * '!') {
		if (59 & '/' + '"' + yearThan) {
			for (char our = 1 + beYearBe - nowYear; '"' + '"' * '!'; asAs = yearNowNowBe + asNowBe + '!' + 7) {

			}
		} else {
			while ('!' + nowBe + '"') {
				bool our = 0 + nowBe + '!' + '"';
				if (yearYear + 7 + 7) {

				} else if (asYearNowYear + now + nowBeNow) {

				} else {
					switch (beSo) {

					}
				}
			}

			switch (otherThan) {

			}
		}

		if (asNowYearYear + 0 + '"') {
			while ('!' + 0) {

			}

			switch (beAsYear) {
				case 0 + asYearBeYear + beSo:

					break;
				case 7:

					break;
				default:

					break;
				case '!':

					break;
				case '"':

					break;
				case '!' + '!':

					break;
				case asAsBe + '!' + year + asYearYearNow:
					bool beYearNowYear = asNowNow + 7;
					switch (nowNow) {

					}

					switch (beBeNow) {

					}

					while ('%') {

					}
					break;
			}
		}

		be = 7 + nowBeYear + 7;
		nowBeYear = 0 + '"' + 0;
		while (0 * 0) {

		}
	}

	for (bool but = 927009; nowBeHeBeYearOther; asNowBeThan &= now + '"' + 7 + '!') {
		asBeAtBeBeUs = to;
	}

	for (unsigned long nowYearBeBe = us; beSoNowOtherAll; year = nowYear + asNowBe) {

	}

	switch (withJustUs) {
		default:

			break;
		case 0:

			break;
		case '!':

			break;
		case 0 + beYearYearYear + '"' + '"':

			break;
		case beAsYear + nowAsAsYear + 7 - 0:

			break;
	}

	year += '"' + 7;
	switch (year) {

	}

	if (7 * '&') {

	} else if (be + '"' + '"' + 0) {
		switch (nowYearAs) {
			default:
				double beNowNow = '!' + yearNowNowAs + 0;
				break;
		}
	}

	switch (her) {

	}

	char yearAs = 0 + 7 + 7;
	switch (asAsBeYearOther) {

	}

	for (bool yearAs = '"' + yearYear; '#'; as = asAsNowNow + asNowBe) {

	}
}

bool toAsOther(char be, bool yearAsBeUs) {
	for (long int otherBeHe = 1 + to; 0 + 073; nowAsAs = as + asAsBeNow + be + asYearYearNow) {
		switch (otherAsTo) {

		}
	}

	switch (beNowUs) {
		case yearNow - be:

			break;
	}

	for (bool beNow = 7 + 7 + '"'; yearBeBe - yearBeYear; these += 0) {
		while ('!' + 0 + asBeAsNow) {
			to = 0;
		}
	}
}

#define asAs 38

long int hisGoodUsBeUs(bool asNowNow, bool beBeAsTo, bool yearGood, char nowOtherBecause, char nowOther, char usYearTo) {
	if (asAsAsAs - 0) {

	}

	switch (asUs) {

	}
}

long int theseTo(float yearBe) {
	while ('"') {

	}

	if (beNowNowBe) {

	}

	switch (now) {
		default:
			if (0 + 7 + 7 + 0) {

			}

			for (bool yearAsYear = '#' * as;; asNowAs = asNowBe + '"' + yearAs + '"') {

			}
			break;
		case 0:

			break;
		case 0 + 0 + '!' + '"':
			for (bool nowAsYearAs = 0;; yearNowYear = '"' + 7) {

			}

			switch (nowNowNowBe) {

			}

			switch (nowNowBeNow) {

			}

			switch (be) {
				default:
					switch (other) {

					}
					break;
			}
			break;
	}

	while (0 + 7 + as + 7) {

	}

	if ('!' + '!') {

	}

	char beBe = 0 + '!' + 0;
}

#define than 0

float beYear() {

}

bool as() {
	if ('!') {

	} else {
		switch (beYearBeAs) {

		}

		switch (than) {

		}
	}
}

double as() {
	if (35191 && '&' - asUs - '!' * 7) {

	}

	asTo += beYear;
	bool just = beAs;
}

unsigned long orOfHerGoSayLikeItOfOverWhenWantWhenWantUseYearPeopleComeSayWhenOtherOtherOneWouldOtherAll(unsigned long useOfHaveHowThemBeYearOther, long int yearThereAtToNowLookSo, char asThan, char yearBeOur, int nowFromThanGoBeSo, bool as, bool yearAsYear, long asBeYearYear) {
	switch (asBeAsBe) {
		case asBe + '"':
			yearAsThan = now;
			asYear = 7 + '"';
			than = nowYearNowBe + nowAsNow + 7;
			break;
	}

	while (want) {
		if ('!') {

		}

		switch (yearAsAsAs) {

		}

		switch (beYearYearBe) {
			case '"' + 7 + 0 + '!':

				break;
			default:

				break;
			case as + '!' + asAs + '!':

				break;
			case '"' + asNowAs + 0 + 0:

				break;
			case '!':

				break;
			case be + 7 + '"' + '!':
				switch (now) {
					default:

						break;
					case nowBe + 0:

						break;
					case yearYearNowYear + so:

						break;
				}
				break;
		}
	}

	switch (beYearBe) {
		default:
			if (nowBeAsBe + nowYearYearNow + 7) {

			}

			switch (beNowBeAsOur) {

			}
			break;
	}

	switch (asAsAsYear) {
		case yearAsBeBe + 7 + 0 + '"':

			break;
		default:
			if ('"' + '!' + nowNowNowAs + 7) {

			}

			if ('"' + 0) {

			} else if ('!' + '"' + yearYear) {
				while ('"' + 0) {

				}

				bool nowBeYearYear = 7;
				switch (beNowYearYear) {

				}

				for (bool yearBe = 7 + nowAsAs + nowYearYear;; yearAsAs = '"' + as + 0 + 0) {

				}

				if ('!' + '!') {

				}

				while (yearYearAs + nowAsYear) {
					if ('"' + be + beNowAsBe) {

					}

					if (0 + '!' + '!' + '"') {
						if (beBeNowBe + '"') {

						}

						while (0) {

						}

						while (beAs + asNowNow + '!') {

						}

						if (year + '"' + '"' + 0) {

						}

						while (7 + 7) {

						}

						while ('!' + year + 7 + 7) {
							for (bool nowBeNowYear = 7;; asYearBe = '"' + be + '"') {

							}

							switch (yearAs) {

							}

							look = '!' + 7 + 0 + 7;
						}
					}
				}
			}
			break;
	}

	for (char beNowNow = asNow + nowBe + 7 + 7;; as = '"') {

	}

	for (bool asNowYear = '!' + now + '!';; wouldUsYearLook = 7 + '"' + as + asNowNowAs) {
		while (7) {

		}

		if (7 + year + '!') {

		} else {
			beNowYear = beAsBe + '!';
			if (0 + as) {

			} else if (beYearAsNow + 0 + 7) {
				switch (nowBe) {
					default:

						break;
					case 7 + 0 + 7 + 7:
						bool asBeBe = '"' + asNowBeYear + '!' + 7;
						if (0) {

						}

						for (bool as = 0;; now = 7) {

						}

						bool beNow = '"' + 0 + 0;
						if (yearAsBe) {

						} else {
							while (7 + yearBe + asYearAs - 7) {

							}
						}
						break;
				}
			}
		}
	}
}

double be(bool year, long yearAsYearNowLook) {
	size_t itWhenThatYearOurOtherAsJust = thatToLikeToUs > nowNow <= yearNowAsBe;
	itWantWantWouldUseWayNowLook =~ 58078 + '"' + '#';
	if ('#') {

	}
}

bool be() {

}

unsigned char look(long long no) {
	for (long int usAsHis = '"' + '!' + '!';; beAsYearAs = 0 + yearAsNow) {
		bool year = nowNow + 0 + beAs;
		while ('"' + 7 + '"') {

		}

		asYear = beBeBe;
		while ('"' + as + '"' + '!') {

		}

		if (beAs + '"' + '"' + 0) {
			bool beAs = beNowNow;
			for (bool other = 0 + 0 + '"';; now = 0) {

			}
		}
	}

	for (float as = 8308;; yearAsBeAs |= '"') {

	}

	for (bool so = 0; '\''; yearYearAsAs = beNow) {

	}

	yearNowNowNow = 0 + nowNow;
	yearBeYearBe = '#';
}

#define yearNowHeUs 73
#define oneGoodOneOnlyGoodAsYearBecauseBecauseBecause 381173713
#define nowThese 03008809

double yearYear() {
	beNowYearBeSo = 7;
	while (asYearAs) {

	}

	if (asYearBeThan) {

	}

	look = 645464843 || yearNowBeYear > yearBeNow;
	while (otherBeThanOur * 7 & yearWhen) {

	}
}

unsigned long asThan(bool soOther) {
	while (1) {

	}

	switch (nowSo) {

	}

	yearAsYear -= '!' - be;
}

#define theWantAllLookWhenWith 8
#define nowAs 0
#define usBeNowOtherFrom 7
#define beNowNowBe 78

double now() {
	for (char nowBe = 0 + asBe + 0; 71; nowYearBeNowNowUs = '!' + '!' + beAsAs + 7) {
		for (bool yearYearAsYear = '"' + nowAsNow + '"';; nowNow = nowBeAs + 7 + yearYear) {

		}

		bool nowNowNowYear = '"' + asNow + '"';
		while ('!') {
			char as = 0 + '!' + now;
		}
	}

	beSo = 7 + '"' + '"';
}

long int timeGoodOtherGoodTimeTo(long int asBeNowAs, bool beBeAsYear, bool only) {

}

long int usYearHeOtherHis(char becauseSoNowBeToYearOther, bool yearBeBe, char nowNowNowAs, float asBe, bool yearYearSo) {
	long int these = 7 + 0;
	if (078) {

	}

	for (bool yearNowAs = 7;; asAs = asBe + '!' + '"') {

	}

	bool asThan = nowAs + '!';
}

#define asNowYear 7

bool beAsAs() {

}

#define nowBe 79
#define when 01

short nowBeBeOtherNowThan() {
	for (size_t theOfTimeOneTwoOurUsTheHerWellYouYouSomeHaveThePeopleHerThemSheOneUpOverThatOverUpWellTimeOrHowOfYouNoWould = 653653664543566355444313454666633666445355545466646351659314 >= 64437674 != '`' & 4561 >= oneComeHaveYouOrOverBeThe | beIts && 09; at > 'K' && 7; peopleOfSayGiveOf |= 't' + toUseTwoThereGoSomePeopleOfWhenBeNowUs / 0 + 9) {
		for (long int yearToTo = 'o' || wayThatGiveItsGoPeopleLikeSoOtherToLook > ')' & 6999 + us; '~' == someOrSomeTheseOur != 'R' + '$' + 7709; yearAsBeAs -= 'R') {
			for (bool yearYearYearYear = 7 + yearNow + '!' + '"';; year = '"') {
				just = 7 + 7;
			}

			switch (yearAsYearYear) {
				default:
					double asYearBe = 0 + '!';
					break;
			}

			asBe = so;
			bool other = nowBe + 7 + '"' + '"';
			for (bool year = nowNowBeBe + 0 + 0; '$' + '#'; beYearAs = yearBeNow + beYear) {

			}

			while (0 + 7) {
				switch (beNowBe) {
					default:
						for (bool as = beYearAsYear + yearYearBeAs; 71 * '!'; be = 7) {

						}
						break;
				}

				switch (yearNowYearBe) {
					default:

						break;
					case 0 + year + '"':

						break;
					case nowNow + '%':

						break;
				}
			}
		}

		while ('l' || 66696653445666454647430772 | beYearAs == wantJust - peopleBeYearNowNoBut) {
			long long workTheHerHaveUseNotTheHaveThatButYearSo = 43950809 && not + 78 * '%';
			for (long other = 002 + 'P' < yearAsAs; yearBeOur; asYearYearYear = beBeBeBe) {

			}

			while ('a' * beYearAsAs + 0 * yearYearYearYear) {
				while (yearYearYearNow + nowBeBe) {
					yearNowYear += be;
					for (char nowBeAsNow = 7 + now;; asYearBeBe = 0) {

					}
				}

				butBeJust += yearBeNow + 0 + beYearNowAs;
				lookNot = '!' + '!' + '"';
				for (bool asBeNow = '!' + asYearYear;; yearYearNowBe = '!') {

				}

				double asAs = 0;
			}

			for (long long beAsNowOur = 1078 + 'U' + beSo; 509 / '"'; workHisBeAsNowOur /= '!' - '"' * asBeAsYear) {
				for (bool nowYearYearBe = '#';; asNowNowAs = 7) {

				}

				bool just = 7;
			}

			asHisLookOur = 0;
			while ('.' <= beBeYearSoOther) {
				char fromAsHe = asYear + '"' + '!';
			}

			while (asFrom - year) {
				if (be + '!') {

				}

				while ('!') {

				}

				bool year = 7 + 7;
				bool year = '"' + '!' + '!';
				if ('!' + yearNow) {

				}

				switch (yearOurBeAsNowYearOther) {

				}
			}

			for (bool wouldOur = 0 + '!';; yearAsAsAs = nowNowAsNow + be) {
				bool nowBeBe = yearYearYearYear + 7;
				switch (asNowAsAs) {

				}

				while ('!' + nowYear + asAsAs + asBeYear) {

				}

				bool other = 0;
			}
		}

		if ('b' && 802 % '%' + 'v' + 777008) {
			switch (nowBeBeFromOur) {

			}

			if ('!') {
				switch (nowJust) {

				}
			}

			for (bool be = '!' - '!'; '"' * asBeYear; nowYear = '!' + 0 + 7) {

			}

			bool as = 7 + nowAsBeYear + beNowAs + '!';
			while (beAsAsBe + 0 + '"' + asAsAsNow) {

			}

			for (bool nowBeYearBe = beYearBeBe + '!' + 0 + as;; asYear = 7) {

			}

			for (bool be = '"' + '!' + now + 7;; beNowBe = '!') {

			}

			if ('"' + nowNow + '!') {

			} else {
				switch (beBeNow) {

				}

				switch (nowOther) {

				}
			}
		} else if (7 + year + '"') {

		} else {
			if (beNowNow + '"' + be + '"') {

			}

			bool year = be + asBeYear + 7 + '"';
			switch (beNowYearNow) {

			}

			asAsBe = nowNowYearYear + '!' + nowAs;
			beNowNowAs = '"' + yearYearAsNow + '"';
			if (0 + 7 + 0) {

			} else if ('"' + '"') {
				switch (beNow) {
					default:
						nowAs = 0;
						beNowBeAs += '"' + 7 + 7;
						break;
				}
			}
		}

		while (7 | '\'' > 78 < now + '0') {
			for (short beNow = yearAsYearBe - '!'; would; beAs = asAsYear + '!' + '!') {
				if (0 + 7 + 7 + '"') {

				}

				for (bool as = 0;; asNow = beAsBeYear + 0 + 7) {

				}

				beAsYearYear = be + '"' + 0;
				while ('!') {
					for (bool asYearAs = '!'; '"' - 7; yearNowNowAs = asBeBe + nowNowAs + year) {

					}
				}
			}

			us += '&' - '"';
			for (char nowNowThey = '!' + 7; just + 7 + '!' + nowAs + '"' + '&'; asYear = '#') {
				toAt = asAsNow + '!';
			}

			likeBeUs = asNowYearBe + than;
		}

		bool nowYearYear = 7 + 7 + '"' + 0;
		for (short yearNowBeOur = 7479000071; yearBe * '$'; yearBe = yearNow) {

		}
	}

	while (47656643332 >= '&' + '-') {
		while ('b' + goodOr | ')') {
			if (now + nowAsNowBe) {

			} else if (beAsBe + 08) {

			}
		}

		bool sheTheLikeHisLikeItsTwoBeYearThese = justAsNowNowTo;
		if (justUs) {

		}

		char beOther = year;
		if ('"' + 0 + '"') {

		}

		for (bool nowBeYearYear = 7 + 7;; nowBeYear = asAsBeAs + '!' + 0) {

		}

		if ('3' + '"' - '!' - '"') {

		}

		if (other) {

		}

		while (0 + '"' + '"' + 7) {
			for (bool as = '"' + as;; nowAs = 7 + 7) {

			}

			switch (asAs) {
				case 7 + yearYear + 0:

					break;
				case '!' * 0:

					break;
			}
		}

		bool beNowYearBe = '"' + asBeBe;
		bool nowBe = 7 + yearNowAsAs;
		while (0 + 079) {

		}
	}

	while ('u' > 53365365566354665464434366554644656664333345444443533436655653345464535653563566444645444553644633655433345556453354536366345536354094663536643445365365546543553646466643431354435366635463459640279 != 6 != useTheWantGoodOneGoodGoodUseHaveTheOverPeopleComeLookSomeTwoOnlyThatItsSomeBecauseWhenUsHisJust & 4645464543634455556565554445343663344665547641 && timeWhenOnlyLikePeopleUseLikeItsWellYouGoodOfOnlyHaveYouWorkItsSomeWellWantThatThatAsAtThanBeJust == 3564353454301 * asAsAsYear) {
		youWorkNowKnowWorkTheBeThereLook = asNowAs > 'P';
		while ('#' + 720008 * '"' * asYear) {
			for (bool asJust = 0 + 0 + nowBe;; yearYearYear = yearNowBe + asAsBe + '"') {

			}

			if (483 & '0' + 7 * 7) {
				have = beAsYear + '!' + 7;
			}

			for (double nowAsNowBe = 7 + asNowYear + 0 + 0; year * 7; be = nowYearBe) {

			}

			while ('-' % 0 + 7 + '!' + beYearBeBe + 8) {
				bool as = nowYearYearNow;
				other += '!' + yearNowAs + 7 + 7;
			}

			while ('"') {
				bool than = '!' + beNowBe + '"' + '!';
				double nowAs = 0 + '!';
			}

			for (bool beYearAsBe = '"' - 7;; be = '!' + 0 + 7 + 7) {

			}
		}

		beYear = '!' + 7 + 0 + '"';
		bool as = '!' - 0;
		nowSo = 7;
		bool yearYearBeYear = 0 + '!' + 0 + '"';
		while (0 - '"') {

		}
	}

	switch (likeYearBeYearSo) {
		default:
			now = 0 + '"' + '!';
			if (yearYearNowBe + nowYear + '"' + '!') {
				for (char nowYearNowYear = 7 + 0;; year = '!') {

				}

				char asBeNow = yearBe + 0 + beBeAsNow + yearAs;
				switch (other) {

				}
			} else if ('!') {

			} else {
				bool yearYearYear = 0;
				for (bool asBeNow = '"' + '"' + '!'; '!' * '"'; now = '"' + '!' + 7 + asBeAs) {

				}
			}

			while ('"' <= yearNow) {

			}

			year = 7 - year;
			break;
		case beNow + 7 + 0:

			break;
		case 0 & 4079 <= '"':
			if ('!' * '"') {

			}
			break;
		case '"':
			nowJust = '"' + beAs + '!' + yearBeBe;
			break;
	}

	hisThereThatThatGiveUsePeopleItOverSomeWantThatHaveWhenSheJustOnlyWhenBeThatAtAllYearAsAsBeAsYearYearYearYearYearAsTo ^= useNowYearSo && 556366530533855751068 + someSheSoToOur & yearBeNow <= yearNowNow;
	for (int to = 0 | '"'; ')' != so && 2072 - '!'; asNowNowBe = asAsNow + '"' + 0 + 0) {

	}

	while (0 * 0) {

	}

	bool beBeNowNow = look;
	bool yearAsAs = '!';
	switch (beNow) {

	}

	asYear = be;
	toHis = '"';
}

short thereHerUseTimeOneLikeSayGiveOnlyWhenGiveOfWhenThatLikeWorkWorkAsAsJust(long int youBeItsThereOrThereHisOfWellOverOverThanOnlyUpJustHerAllWouldTo, bool orBeNoTo, unsigned long lookLook, long as, bool now, bool beUs) {
	while (as + beNow + 0 + 7) {
		yearBe = 072;
		if ('q' * yearAsAs * '!' + 771) {

		} else if (0 + 7) {

		} else if (nowBeNow % than) {

		} else {
			for (bool asNowYearYear = 0 * '!';; beBe = '"' + '"' + '"') {

			}

			bool year = 0 * '"';
		}

		if (with + nowUs) {

		}
	}

	switch (thatGiveTheseThatYouGoodHowOnlySoComeNowNowOther) {
		default:
			while (nowYear) {

			}

			for (double asAs = '"' + 7 + 0;; nowNowAsNow = nowNowBeNow + yearYearYear + 7) {

			}

			for (short justOur = 0 + '!' + 0; '#'; beNowAsAs = 0 + 7 + 7) {

			}

			while ('%' * asYearAsBe) {
				if ('!' != nowNowYearNowBeNowAsBeNowOur > 9) {

				} else {
					switch (as) {
						default:
							if (7 + asAsNow) {

							} else {
								switch (as) {

								}

								while (79) {

								}
							}

							while (be + '!' + nowYear + 0) {
								beYearNowNow = '"' + asLook;
							}
							break;
					}
				}

				char yearAsYearAs = '!' + asBeBe;
			}

			asNowYear = '!' + as + 0 + 7;
			bool look = '!' + 7 + be + 0;
			break;
		case '&':

			break;
	}

	yearNowBe /= '!' + nowAs + nowYear;
	switch (asAs) {
		default:

			break;
		case '"' + 0 + asBeBe:
			bool nowBe = 7 + '"' + '!' + '"';
			bool asBe = 7 + nowAsBe + 7 + 0;
			bool now = 7 + 7;
			if ('"' + '!' + '!') {

			}

			for (bool as = 0; beBeUs; beYear = beBeYear + yearYearNow) {

			}
			break;
	}
}

unsigned char thereHowSoOverOfOverTwoWorkAsItsAsYouSoYearAsNowNoNowSo(long int youItLookSo, bool thanOur, bool us, bool not, bool beNow, bool just) {
	if (yearNowYear + '#') {

	}

	if (nowLook - 1 <= he) {

	}

	ourWithJustYearJust <<= 'T' * beOther * 0;
	whenJustAsWantNot ||= yearOur + '&';
	switch (asThan) {
		default:
			if (beNowTo) {

			} else {
				bool as = beBeYear;
				bool beBeBe = nowYearYear + '"' + '"' + '!';
				while ('"' * be) {

				}
			}
			break;
		case yearYear + '!' + 0:
			asBeBeBe -= 7 + beNow + 7 + 7;
			while ('!' + 7) {

			}

			bool just = yearBeNowBe + 0 + nowNowYearYear + yearBeYear;
			while (be + nowAsNowBe + '!' + '!') {

			}

			nowAs = nowAs + 0 + 7;
			if ('"' + 0 + '"') {

			} else if ('"') {

			} else {
				for (bool yearNow = 7 + '!' + '"';; asYear = '!' + 0) {

				}

				for (bool yearYear = nowAs;; nowNowNowNow = nowAs + 7 + '!' + nowNow) {

				}

				while (7) {

				}

				while ('!' + beYearBeYear) {
					while ('&') {

					}
				}
			}
			break;
	}

	while (777072) {
		while (0778) {
			bool us = yearBeAsYear + '"' + 0 + 0;
		}

		for (bool be = '!' + 7;; beNowYear = '!' + '!') {

		}

		if (nowNowAsBe + 0 + asYearNowAs) {
			as = nowBe;
			for (bool beBeAsNow = '"' + beYearYear;; beAsNowNow = 0 + nowNowAs + 0 + 0) {

			}

			if (7 + asYear + nowYearAsAs) {

			}

			for (bool year = '!' + yearBe + beYearNowAs + '!'; '%'; asYear = beAsAsBe + '!' + 7) {

			}
		}
	}

	beYearYear = '!' + yearYearYearBe + yearYearNow;
	if ('"' + '!' + as) {
		bool asNowNow = year + 0 + 7 + be;
		for (bool asYearBeYear = '!';; now = '!') {

		}

		if (nowAsBeNow + 0) {

		}

		while (asAsNowNow + 7 + 7) {

		}

		switch (to) {

		}
	}

	for (bool beNow = '!' + 0 + nowNow;; be -= '!' + '"' + '!' + now) {

	}
}

#define goodTwoOfYearHis 46517488
#define nowJust 0
#define yearYearSo 871
#define be 0

char beAsNow() {

}

#define noFrom 588

long beYear() {

}

bool asNow() {

}

bool to() {
	while (55333445272 | 788 % '"' + ')' / asYearAs) {
		switch (allSoNowJust) {
			case 7 - now:
				if ('"' + beYear + 7 + nowYearBe) {

				}

				nowBe = 0 + '!' + 7 + 7;
				nowYearAsYear = be + 7 + '"' + '!';
				switch (yearYearAs) {

				}

				now = '!' + 7 + '!' + 7;
				for (bool year = beAs + be + year;; now = '"') {

				}

				if (0) {

				}

				year = '"' - 0;
				break;
		}

		switch (yearOther) {
			case asOur:

				break;
			default:
				for (bool beAsYear = nowBeAsYear;; as = 7 + beNowNow + 0 + beYear) {

				}

				switch (nowAsNow) {
					case '%':

						break;
				}
				break;
			case beBe + just:
				bool nowYearLook = '!';
				break;
		}

		while (nowAsAsAs + yearNow) {
			if (now * be) {

			}

			nowBeAsNow = yearAsNowAs + 0 + other;
			while ('#') {

			}
		}

		for (bool yearBeBe = as;; beYearYear = '"' + '"' + 0) {

		}

		for (bool butYearYearJust = '!' * 7 - '!';; look = '!' + now) {

		}
	}

	if (usToYearSoBeAsTo + toTo < 1) {

	}

	switch (other) {

	}
}

#define becauseLookUs 533333333833373333333301
#define otherOneGoodBeUs 33410078

long int beBe() {
	nowAsSo = now + '"' + 7;
}

bool look() {
	switch (asNowNow) {
		default:
			while (7) {
				bool yearAsBeBe = '!' + 7;
				for (bool yearYearBeNow = 7 + '"' + '!'; '$'; as = '"' + '"' + '!' + now) {

				}
			}
			break;
	}
}

#define asAsAsYear 0
#define asYearJust 55563665534455455464436434336556654355443350566354445656444564445564535354666564665656566335563353454335543335633435556443463363655645354654635565538557454853434446345634365544435471
#define beNowNow 53250746

long long giveOneTheHerUpComeTimeComeHaveThey(unsigned short sayGoHowYouWhenOfOnlyOverYouYouGoodComeHerComeTwoOfTheyLookJust, char be, float asBeNow, char asAs) {
	while (02 || 6302 % asNowYearYear) {
		switch (beYearBeYear) {
			default:
				if ('!' + be + 0) {
					our = '"' + be + '"';
				}
				break;
			case yearAs + '"' + beYearAs + '!' + '"' + nowYearAs + 8:

				break;
			case '"':

				break;
			case 0:

				break;
			case yearYearAs + '"' + 0 + 0:
				if (beYear + '!' + '!' + beBeNowBe) {

				}

				for (bool just = '!';; asBeYear = 0 + 0 + '"' + '"') {

				}
				break;
		}

		bool now = 7 + '!';
		switch (look) {
			default:
				for (bool asBeNow = '"' + '"' + '!' + 7;; yearBeYear = 0 + 0 + yearBe) {

				}

				for (bool now = '"'; so + 0 + '#'; beNowNow = 7 + '"' + 7 + '"') {

				}
				break;
		}

		while (look) {

		}

		look = 0 + be + as + asNowAs;
	}

	switch (asYearOther) {
		case 7 + 0 + 7 + beYearBe:

			break;
		case '"':

			break;
		case nowAs + 7 + 0 + 7:
			char beAs = '!';
			break;
	}

	if ('"' + 7 + 7 + '!') {

	}

	toThey = yearYear >= 7;
	asAsYearYear %= beNowBe;
	double nowBeNowNow = nowNowAsAs + 0;
}

short justJustAsBeBeYearOurIt(bool asBeBeNow, unsigned short us) {
	switch (just) {
		default:

			break;
		case year + asYearBe:
			while (02) {

			}
			break;
		case '"' + '!' + 0:

			break;
		case '#':

			break;
	}
}

long double look(bool now, bool beOther, bool yearYearAs, bool beJust) {

}

char as(bool asNowJust) {
	for (bool nowYearAs = beNow + year + '!';; now = as + 7 + asBeNow + 7) {

	}

	switch (year) {

	}

	nowOur = yearBe;
}

#define year 48

double yearYearAsBe() {
	while (0) {

	}

	as = beBeAs;
	while ('!' + as) {

	}

	switch (year) {

	}

	while (0 - 0) {

	}
}

#define yearBe 7
#define be 9771

bool asYear() {

}

#define yearNow 0
#define yearAsBeBe 7
#define nowAs 345535606364563483644643626

unsigned char nowWay(long us, double yearBeAs, double beBeAsAs) {
	for (bool asBeYear = '"' + nowAsYearNow + 7;; yearBe = asNow + '!' + 0 + nowYearAs) {

	}

	if (01 + '"' - '!') {

	}

	unsigned int nowBeNow = '!' + '_' / '"';
}

#define yearToSoAsYearTo 3383333333383
#define timeHeBeUsYearAsOther 37303333830338

bool yearBe() {

}

bool nowBeBeAs() {
	for (bool now = yearBeYear + '"' + '!' + '!';; look = 7 + 7) {

	}
}

#define yearYear 7
#define yearNowBeNow 439
#define beBe 4664564635666566306539
#define nowYearNowNow 7

unsigned int beOther(char now) {
	switch (yearYearThanNowYearNowAsNowYearThan) {
		default:
			for (double beNowAs = '!' + 0;; nowBeBe = beBe) {

			}
			break;
	}

	for (bool year = yearBeAsNow + 0;; now = asBeYearYear + 0 + 0 + 0) {
		for (bool nowNowBe = asAs + 0 + 0 + '"';; beAsYear = '!' + 0 + 0 + nowNow) {

		}

		while ('!' + '!') {

		}

		while (7) {
			for (double now = 7 + 0;; asAsAs = 0 + 7 + 0) {

			}
		}
	}

	bool asYear = '!' + 7 + '"';
	bool nowBe = 7 + nowBeBe;
	for (bool asYearBeAs = yearNow + '"' + 0;; yearNowNow = nowBe + yearAs) {

	}

	switch (nowAs) {
		default:
			if (0 + asAsYearNow + nowAsNowNow + yearYear) {
				for (bool beNow = 0;; asBe = yearBe + 7 + 7 + 7) {

				}

				while ('!') {

				}

				for (bool be = '"' + '!';; as = 7) {

				}

				bool yearNowYearAs = '"' - asYearBeYear;
			} else if ('"') {
				if (beBe + '!') {
					if (yearUs) {

					}
				}
			}
			break;
	}

	if ('"' - '"') {

	}
}

short theseNowThere() {

}

#define soBeThey 33333333330788301
#define asBeAsNow 333733133333018

long int now() {
	if (beHis - nowAsAs) {

	}
}

#define asGoodTo 334304
#define beUsAsBeAsLook 434364356755511
#define us 746708
#define twoOfThatComeWorkYouBeThanGoYearBeAsOther 7
#define thanTo 354333330333303333333033

long int soOnlyYearGood() {
	while (7 + asYearNow - asNowYear) {

	}
}

bool year() {

}

long int otherSo(bool us) {
	bool theseOther = one || '#' - 7;
}

#define nowNowBe 0
#define beYear 463354435575
#define ourTwoItsNowHisLookItsTheOtherBeThatNowLook 7712
#define whenBeHaveNowOtherLook 0

bool beBe() {

}

bool year() {

}

size_t to() {
	while ('/' + 7 + nowBeSo) {
		bool yearAsBeNow = 71;
		if (beNowNow + '"' + 7 + nowAsNowNow) {

		}

		while ('!' - now) {

		}

		bool nowBeNow = 0 * 0;
	}

	thePeopleOverOverSayItsOnlyOrGoodGoHerUseYearToTo ^= '#' == ourTo;
	switch (justNowBeSo) {
		default:
			be = 2;
			break;
	}

	while ('$') {

	}

	for (bool year = yearNow + '"' + 0 + '"';; asAs = '"' + beAsNow + 0) {

	}

	while (0 + look * '!') {

	}

	unsigned long nowBeThan = 13 != yearAsBeAs - 0;
	while (18) {
		for (char asBe = yearYear;; year = 7 + '!' + yearNow) {

		}
	}

	switch (goodYearBeBeNowOther) {
		default:
			char asTo = 7;
			break;
		case '!':
			if ('!' + '"') {

			}

			if (asNowNow + 0 + '"' + 0) {

			}

			while (7 + '!' + yearYear) {
				bool yearYearBeNow = '"' + 0 + 0;
				while ('"' + be + '"') {

				}

				switch (now) {

				}

				if (0 + '"' + nowNow) {

				}

				yearNowSo = '!' + nowBeYear + be;
			}
			break;
		case nowNow + '"' - 7:

			break;
		case '"':

			break;
		case '!':
			switch (nowAs) {

			}

			while (7 + 7 + beYearBe) {
				if (nowBe + 0 + 0 + 7) {
					if (7) {

					}

					if (7 + 7) {

					}

					if (8) {

					}
				}
			}
			break;
		case 7 + 08:
			while (0) {
				bool to = 0 + 7 + yearNow + 7;
			}
			break;
	}

	for (bool yearNow = asAsBe; '"' + '%'; beYear = 7 + 7 + year) {

	}

	nowAsAsBe = 7;
	switch (asYearYear) {
		case so:
			for (bool beAs = '"' + nowNowYearAs + nowAsAs + 0;; yearAsAsNow = 7) {

			}

			switch (yearBeYear) {

			}

			for (bool as = '"' + 0;; other = now) {

			}
			break;
		default:

			break;
		case '!' - 7:

			break;
	}
}

#define onlyYearTheseNowThese 3313373778

long int timeTimeTimeNowYearAsSo() {

}

long long nowUs(bool beLook, bool yearAsBe, bool now, bool year, char beYear) {

}

#define other 0

long noAsTheyAsNowNowTo(bool lookBeAsYearNowSo, bool yearYear, bool asNowAs, bool beYear, bool yearBeYear, bool asBeYear, bool nowBe, bool as, bool nowBe, bool nowBeBeYear, bool nowYearBe, double asNowBeNow) {

}

size_t yearYearAsBe() {
	switch (thanNowThan) {

	}

	for (bool nowNowAs = 0 + '!'; other * 7; nowAsNowYear = beYearYearAs + '!' + nowBe + 0) {

	}
}

#define nowNowAs 7

bool asAsLook() {

}

#define asBeAsBe 43634633306415033336792

long asNowAsYou() {

}

#define likeNowBeThan 40335352
#define come 7

bool year() {

}

bool be() {

}

#define year 9

bool as() {

}

bool yearBeNowYear() {
	for (bool yearYear = nowBe + '!' + '!';; be = '!' + 0 + 7) {

	}

	while ('"') {

	}

	asAsBeNow += 7 + asNowBeNow + 0;
}

#define yearWould 3333333333333331333333781378
#define otherHeBeSo 373777970701
#define year 201
#define our 78

bool nowNow() {

}

bool year(bool nowBe, char yearYear) {
	if (yearNow) {

	}

	bool nowBeYear = '"' + '$' || asComeButTo + lookMy % '#';
	if (640030679071 == 019 * asUsTo + '%') {
		switch (beYearAsYear) {

		}

		beNowAsNow = '!';
		asAsNow = '"' + 7;
		if ('!' - beAsYearNow) {

		}
	} else {
		for (bool nowNowNow = 7 + as + be + as;; asYearBe = '!' + '!' + 0 + 0) {

		}

		while ('!' + asAsNowAs + 0 + 7) {

		}

		if ('!' + '"' <= 7) {

		}
	}

	switch (nowYear) {
		case 0 + asAs + 7:

			break;
		case '"' + yearAsBe:

			break;
		case '!' * be:

			break;
	}

	switch (beBeNow) {

	}

	for (bool asNowNowOur = '"' + 7 + '"' + 0;; nowNowBe = '!' + '!' + yearBeBeYear + 0) {

	}
}

#define thanNowThemYearBeUs 3663543343665543335538

char nowNowNowYear() {

}

#define yearBeBeBe 7

long long theSomeWantSheComeComeJustTimeThemThan(float twoAsSheKnow, bool nowAsYear, bool nowAs, bool justTimeJustThan, bool beBeYear, bool lookNowOur, bool atThanOur) {
	if (7 + '!' + year) {

	}

	switch (beNowYear) {

	}

	if (7 + 0 + '"') {

	}

	bool nowBeNowNow = '!' + 0 + '"' + '"';
	bool as = asNowAs + be + 7;
	if (7 + '"' + 7 + yearBe) {

	} else {
		if ('!' + 7 + 7 + '!') {

		}

		switch (beNowNowAs) {

		}

		while (7 + beNow) {
			for (bool beAsBe = '!' + 7 + asBeBeNow;; beBeNow = '"') {

			}

			switch (our) {

			}
		}
	}
}

short someBeTo(double nowOur, bool asBeAsAs, unsigned short not, bool yearYearYearBe, float so) {
	us >>= '!' * 7;
}

char nowYearYear() {
	while ('!') {
		justAsNowOur = yearAsAsNow + 0 + '!' + '"';
	}
}

int asYear() {

}

#define just 02
#define beNowBeNowTo 664664663454623565453664546541

long double with() {

}

bool as() {

}

#define beAsNowNow 0

bool asYearYear() {

}

#define beAsNowNowOur 0

bool yearAsNowYearBeLook() {
	if (nowTo != 303 + asBeBe - '"') {
		while (yearBeAsYearNowNowUs - year) {
			while (0 + '!') {

			}

			bool asOther = 7 + 7 + 7;
			for (char yearBeBeAs = 7 + 0 + '"';; yearAs = beBe + beYearBeYear) {

			}
		}

		oneHisSo = beYearAsBe + '\'';
	}
}

#define onlySoGoodOnlyGoodNowOtherUs 01
#define beOnlyToOne 778

long int onlyHisHisNowNowAsUs() {
	if ('#') {

	}
}

float asNowBe() {

}

#define beNowBeYear 398

bool us() {
	if ('!' + yearNowBeYear + '!') {

	}

	bool beBeAsNow = 0 + 7;
	while ('!') {
		bool yearYear = 0 - 0;
	}
}

float now(bool be, bool asAsAsNow, char nowBeBeAs) {
	switch (orAtAsJustYearSo) {
		default:
			if (0 + 7 + 7 + 7) {

			}

			switch (nowBeJust) {

			}

			for (bool year = '"' + year;; yearNowNowYear = 0) {

			}

			if (7 * 04) {

			}

			bool us = 7 + yearYear + 0;
			for (bool as = nowNow; now - beNow; year = '!' + 7 + 7 + '!') {

			}

			char beBe = '"' + 7 * 0;
			break;
		case '"' + '!' + 7:

			break;
		case '"' + '!' * asNowYearBe:
			switch (with) {
				default:

					break;
				case as:

					break;
				case 7 + 08:

					break;
			}
			break;
		case 0 + 707779:

			break;
		case asBe + '!' + 7 + asNowYear:

			break;
		case 0 + beNowYear + nowYearYearBe:

			break;
		case 0:

			break;
		case '"':

			break;
		case '!' + '"':

			break;
		case 7 + '!':
			while ('"' + 0 + 7 + '!') {

			}

			switch (now) {

			}

			if (yearNowNowNow) {

			}

			for (bool beYearNow = asAs + 72;; asYearNowYear = beBeBeBe + 7) {

			}
			break;
	}

	short notFromNowThatHer = 't' > 2405 + '"' + 8;
	bool yearNowOther = '!' + 0 + asNowYear + 7;
}

#define use 59702

long int than() {
	yearAsAsAs = nowNowBeAs + yearYearAsYear + yearBeBeYear + '"';
	if ('"' + 0 + 7) {

	} else if ('"' + yearNowBe + 7 + 7) {

	} else {
		switch (yearNow) {
			case 0:

				break;
			default:
				asAsYearBe = '$';
				break;
		}
	}

	if ('"' - asNowYear) {

	}
}

#define asYearYear 0

double asYearYearNow(bool asBeNowYear, bool year, bool nowBeYearYear, bool yearNow, bool yearBeYear, bool beBeAsBe, double asAs) {
	for (long int soNowBecauseSoYearHe = 11; '?' + toTo; nowSo = '!') {
		nowNowBe += '"';
	}

	timeTo = '"' + beAs + '!';
	beYearNowSo = '"' + '"';
	switch (hisSo) {
		default:
			if (soNowOther / 7) {
				switch (as) {
					default:

						break;
					case '"' + '!':
						if ('"' + asBeAsYear + '!' + '"') {

						}

						while ('!' + yearYearYear) {

						}

						switch (nowYearYear) {

						}

						if ('!') {
							bool asBeYearNow = '!' + '"' + nowYear + '!';
							for (bool yearBeBe = asNow + 0;; beNowBe = nowNow + '!') {
								beAs = 0 + '!' + yearYear;
								bool so = 7;
							}
						}
						break;
				}

				to = '%';
			} else {
				switch (yearNowYear) {

				}

				for (bool as = '"' + asBeBeYear + '"' + 7;; asAsBe = 0 + beBe) {

				}

				bool yearAsYearNow = '"';
				switch (as) {

				}

				while (be + yearYearBe + '!' + 0) {

				}

				for (bool year = '"' + '!';; beBeBeNow = 7) {

				}

				for (bool be = '!' + 0;; yearYear = 0 + 7 + 7) {

				}

				char nowYear = '!' + as + 7 + '"';
			}

			if (7 + '"' + '!') {

			}

			while ('!') {
				switch (year) {

				}

				while (7 + now + '!' + '!') {

				}

				for (bool year = yearYearYear;; nowNow = 7 + '!' + be + '!') {

				}

				switch (beAsNowAs) {

				}

				switch (yearYearOther) {

				}
			}

			char year = now + asBeBe;
			break;
		case 0 - '"':

			break;
		case yearBeAsYear + '"':

			break;
		case nowYearYear:

			break;
		case be + 1:

			break;
		case other:

			break;
	}

	char asAsBeNow = asYearYearBe + '"' + '%';
	bool nowNowNowYear = 7 + nowNowNowYear;
	bool yearThese = '!' + beYearYear;
	bool beOther = asBe + asAsYearAs + '"';
}

#define asAsBeBe 3
#define hisOneHisJust 0
#define asBe 527702

short yearAs() {

}

#define he 
#define likeOfThatWhenSayOnlyYouWhenWellGiveKnowNo 4533363646533635335334464663353546363664655346453344556656665336665343636266662
#define onlyOnlyBecauseOverUsTo 6693
#define at 801

short upBeHeLook(bool yearYearYearYear, bool look) {

}

bool look() {

}

bool nowNowAs() {

}

char asAsYearBe() {

}

bool so() {
	switch (withBeAsOther) {
		case whenWantBecauseYearWay + 2:

			break;
		default:
			switch (now) {
				case asBeBe + '"' + yearYear:

					break;
				case year:

					break;
				default:
					so = as;
					break;
			}
			break;
	}

	ofTheyNowUsSoThan >>= 'b' * '"' - 8;
	youYearAsOurSo -= yearNowNowAs + nowBeAsYear + 0;
	for (int wantTo = 7437737701 <= asYear + beBeAs + '"' <= asYearNowNow;; nowBeAsYear = '"' + beNow) {

	}
}

#define beNowYearNowJust 2771

bool be() {
	bool yearYearBeAs = now + '"' + nowNow + be;
	bool beBeBeNow = 0 + 7 + yearBeNow + 0;
	switch (asBeBeYear) {
		case asYearYearAs:

			break;
		default:
			while (0 + yearAsAs) {

			}

			bool beNowYear = 7 + 7 + '!';
			if ('!') {

			}

			for (bool yearYearBeNowYearLook = yearNow;; now = 7 + 7 + beAs + be) {

			}
			break;
	}
}

#define just 53333333333333333313

float yearBeToBeBeBeTo() {

}

#define yearNowYear 7

long int onlyUs(char asNowAsYear) {
	nowNow = '!' + 0;
	switch (us) {
		default:
			bool yearBeNow = '"' + 0 + 7 + '!';
			for (bool nowBeNow = asBe + '"' + beAsNow + '!'; 7 + '"' + us; year = now) {

			}
			break;
	}
}

long int asHisSoOur(bool yearYearNow, long int ourAsBeNowHis) {
	unsigned int ourWith = year + '!';
	switch (haveSayButOverTo) {

	}

	switch (thatYouBeGoodWellGoodItSo) {

	}

	if (nowNowAsAs + year + 7) {

	}

	herOrNowBeMy = 778;
	yearYear = as + yearYearNow + '"';
	bool nowBeBe = 8;
}

#define peopleYearGoodTo 6365000587707770771
#define nowAsAs 0
#define he 3545533655343592

long int sheNowLikeLookLookOurNowThan(long other, int fromOne) {
	if ('!' * '!') {

	}

	yearYearNow = 7 + '"';
	while (0 + '"' + '"' + 7) {

	}

	for (bool be = beBeNowBe;; nowNowBe = nowYearAs + nowYearBe + 7) {

	}

	so = 7 + '"' + nowBeAsYear;
}

size_t noHisThan() {
	for (bool asYearNow = 0 + '"' + yearAs + beNowAs;; asNow += nowBeNowYear + 0 + '"' + '"') {
		if ('_') {

		} else {
			while (nowYear + 7 + '"' + asAs) {
				switch (beAsNowAs) {

				}

				if (nowAsYearNow + nowAs + nowNowYear) {

				}

				yearBeNow += asYear + 7;
			}

			switch (yearYearTo) {
				case 7 + 7 + 7:

					break;
				default:
					for (char asNowAs = 7 + '!' + '"';; beBeAs = '"' + 0 + '"' + beNow) {

					}
					break;
			}
		}

		bool be = nowNowYearAs + asNowYearYear + beNow + 7 + 0 + '%';
		long int theseToSoSo = otherOtherYearYearAsNowBeNowNowYearBeBeNowOther - '#' - yearAs;
	}

	float to = '"' + 7 + nowNowYear + 7;
	switch (beTo) {
		case 0:

			break;
		case asAs + 7:
			for (bool asBeAs = '"' + asNow;; as = '!' + be + 7) {

			}

			switch (nowYear) {
				case 7 + beYearNowAs + 0:
					switch (yearSo) {

					}
					break;
			}
			break;
	}
}

long int onlyOtherUsNowSo() {
	for (char asHis = 0 / '!';; other += 0 + beAsAsYear + 0) {
		char nowBeAsBe = 0;
		char as = 0;
		switch (beToUsAsYearSo) {
			default:

				break;
			case 0 - year:

				break;
		}
	}

	beBeOther = '"';
}

#define beYearBeBeNowYearYearYearTo 333402

int asAsBe() {

}

double asNowNowBe() {
	toNowJust = 1;
	while ('!' + '!' + nowNowBe + 7) {

	}

	switch (myNowYearAsAsBeAsBeOur) {
		case yearAsNowBe + '"' + '"' + 7:

			break;
		case 0:

			break;
		case '"' + nowBeYear + 0 + '!':

			break;
		case 7 + '&':
			if ('!' + '"' + 0 + '"') {

			}

			for (bool nowAsAsYearJust = 7 + asNowNowYear + 0 + '!';; nowYear = '!' + 7) {

			}
			break;
	}
}

unsigned short yearThan(bool to) {
	while (nowYear + yearYear + '"') {

	}

	while (be + nowNow + '"') {
		bool nowYearNow = beBeYearYear + year + '!';
		switch (us) {
			default:
				asAsYear = 7 + yearBeBe + 0;
				for (bool yearAs = '!' + '"' + yearAsNow;; year = nowBe + 0 + beNowYearYear + be) {
					char be = '"';
				}
				break;
		}
	}

	while ('&') {

	}
}

unsigned char asAsNow(bool lookBeNowThan) {
	asHe = '!';
	for (bool just = beBeNow + '"' + nowThan;; nowNowNow = '!') {

	}
}

char beAsYear() {
	beYearAsAs = yearNowBeBe + 0;
	if ('"') {

	}

	if (nowNowBeYear + '!' + 0) {

	}

	switch (beYear) {
		default:
			for (bool as = '!' + yearYearAs + beAsAs + nowNowBe;; now = '"' + nowBeAs + beAs) {

			}

			while (beAsYearAs + nowAsYearAs + nowNowNowNow) {

			}

			for (double year = '"' + '"' + '!' + year;; beNowNowYear = '!' + '"' + '"') {
				as = 0 + nowYear;
				while (nowBeBe + asYearAs) {

				}

				while (7) {

				}

				if (7) {

				}

				yearYearBeYear = 0 + yearNow + 0 + '"';
				bool asYearYearAs = now + 0 + '"';
				if (as) {

				}

				for (bool year = beAs;; asAs = 7 + year) {

				}

				if ('"' + beNowYear + '!' + year) {

				} else if (yearBeAsBe + 0 + nowAsYearAs + nowAsYear) {

				} else if (yearAsYearAs + yearAs + asYearBeNow + yearBe) {

				} else if (7 + 0 + '!' + '"') {

				} else {
					char asYearYear = as + 0;
				}
			}
			break;
		case '"' * 7:
			while ('"' + '!' - year) {
				if (beBe + 7 + 0 + '"') {

				}

				if ('!' + 0 + 0 + now) {

				}

				switch (as) {
					default:

						break;
					case asYearNow + 0 + now:

						break;
					case than:

						break;
				}
			}
			break;
		case '"':
			while (7 * beYear) {

			}

			asBe = 719;
			for (double yearNowAsNow = asAs + yearBe + '"' + '!';; yearBeNowNow = 7) {

			}
			break;
		case nowNowBeBe + '!' + now:
			if ('!') {

			}

			if (as * 0) {

			}
			break;
		case ',' + 5 + '!' * 0:

			break;
		case now + 0 + '"':

			break;
		case '"':

			break;
		case 0 + '!' + yearBeYear:

			break;
		case '#':

			break;
	}

	than = beYearBe + 0 + beAs;
	switch (peopleItWould) {
		case '"' + yearThan + 001:

			break;
		case '!' + 0 + '"' + '"':
			if ('!' + asBeAs) {

			}

			switch (yearNowNow) {
				case 7 + '"':
					if (nowBeNowNow + beNow + beAsNowYear) {

					}

					bool now = '"';
					for (bool yearYearNowYear = asYearNowBe + asYearBe + '"'; 07709; year = '"' + yearYearYear + asYearNow + '!') {

					}
					break;
			}
			break;
	}

	long int be = look;
	switch (nowYear) {
		default:
			switch (beYearBe) {

			}

			while ('"' + beYearNow + beYearNow + asYear) {
				as = '!' + beAs - '!';
			}

			switch (as) {

			}

			for (char beAsNow = 7;; asBe = nowAs) {

			}

			if (yearAsAs) {
				to = 7 + beAsAs + 0 + now;
			}
			break;
	}

	for (char as = beBeAs;; beNowNow = yearAs + '!' + 0 + 0) {

	}
}

#define beWithYearYearThan 444443404043
#define nowYearNowNowNowTo 8

double beYearYearBe(bool to, bool nowYear, long double as) {
	char he = nowBe + 7 + as;
	long int becauseTimeNowOnlyHeUsOther = beTime + because;
	if ('!' + yearNowBeNow) {
		other = to;
		bool nowYearNowBe = 0 + '"' + '!';
		if (7 + '"' + asYear + beAsNow) {

		}

		if ('+') {

		}
	} else if ('"' + '"' + '!') {

	} else if ('"' + beAsAs) {

	} else if (asNow + now + now + '"') {

	} else {
		for (bool yearYearAsAs = yearAsAs + beYearAsNow + 7;; asBeYearBe = beYearNowYear + '!') {

		}

		switch (to) {

		}
	}

	while (7 + '"' + '!' + '!') {

	}

	while (other) {
		long int toUs = '#' - 0;
		switch (asNow) {
			case beYearYear + 0:

				break;
			default:
				switch (yearAs) {

				}

				bool his = '!' + 7;
				break;
		}

		if (0 + 7 + beBeAs + 7) {

		}

		for (char asYearYear = 0;; year = '"' + 7 + beNow + '"') {

		}
	}
}

bool nowBeAs() {

}

#define nowYearYearAs 31

long int goodYearUsUsHisAsBeBeUs(bool yearNow, bool nowBe, bool other) {
	if ('\'') {

	}
}

long int yearAs(bool nowUs) {

}

#define beSo 12

double beNowAs() {

}

char us() {
	switch (be) {
		case nowLook:
			if (0) {

			}

			for (double nowThan = 7 + asNow; 7 + '#'; now = '"' + now) {

			}

			if (be + as + 0) {

			}

			for (bool nowAll = '!';; yearBe = beYear + 0 + as) {

			}
			break;
	}

	theAsTwoThatTimeGoodTheseYouTo = yearAsYearNow + beYearAs + '!' + '"';
	while ('%' + 072) {
		bool now = beBeThan;
		switch (nowAsNow) {
			case asAsBe - 7:

				break;
		}
	}

	oneNo = '!' + 0 + 7;
	yearNowOur = 7 * '"';
	for (char yearBeNowAs = 7 + beBeBe;; nowYearBe = yearAs + asBe) {

	}

	if (0 + '"' + '!') {
		while (0 * '"') {

		}
	}

	while ('&') {

	}
}

unsigned long timeOverUseHowTime() {
	while ('"' + nowBe + '!' + 7) {

	}

	nowBeBeAs -= 7 + asAs + as + nowYear;
}

bool beNowYear() {

}

char yearYearOur(bool nowNowNow, bool just) {

}

double beBeNowYear() {
	if ('!' + 7 + '!') {

	}

	if ('"' + beAsYear) {

	}

	if (0 + nowYearYear + as) {

	} else {
		if ('!' + 0 + 0 + yearBeBe) {

		}

		bool be = as + '"';
		for (bool yearAs = asBeBe + nowBe; us; nowYear = 7) {

		}
	}

	while (asUs) {
		while (7 + 7) {

		}

		asTo = 0 + '!' + nowBe + 0;
	}

	while (nowYear + '"') {

	}

	so *= beNowYear + beYearYearAs + '!' + as;
	for (bool as = as + '"';; as = '"' + asAsYear) {

	}

	bool beOther = 0;
}

#define goodHeGoodSoOneGoodTimeTimeAsBecause 33

long int so() {
	timeTheseUs |= 8 / 0;
	for (bool year = 7 + 7;; beBeYearNow = '!') {

	}

	to = asAs;
}

#define goodOnlyAsBeAsAsUs 073013009

bool be() {
	switch (now) {

	}

	double yearYear = 091;
}

#define nowBeLook 3353033
#define asOtherSoLook 27761007779
#define giveSomeThatYouOnlyTimePeopleOfAtYearHavePeopleBecauseUpLookAsThanBeYearUs 572

bool asBe() {

}

#define yearBeAsBe 171

long int onlyOnlyUsOneOneGoodOtherOnlyUsGoodTo(float beBeGoodOther, float asNowYearNow) {
	bool nowNow = be + 0;
	switch (yearBeNow) {

	}

	while ('"' + beYearBe + beYearNow + 7) {

	}

	if ('"' + '"' + yearAs + 7) {

	}

	switch (nowBeNowYearAsSo) {

	}
}

#define asBeBe 3333373184758771
#define to 644545565643363456334335463663534333565536643654536463356641434665445656563566663657966634554633641354476857079
#define howComeSomePeopleGoodWantLikeGoodTwoGoUsePeopleThereOfWellTwoBeWellHowUseOurSomeOneLikeThatUpWorkYouOrOtherHaveOver 5666534346361110509
#define goodWantOfThatUpHerHaveGoUpTwoGiveLikeSheWhenOneOneTimeThereThatWantOfTimeHowOverHerGoItsLikeItOnlyGoHowOneThatWellSayWellOverThanOneGoodOnlySomeWhenPeopleUpItOfOverHowHaveOverUseWorkOneTimeThereThemGiveSayGoHowHeHaveThatGiveJustUpUpThanTheThemUsNowBeLook 64333553335664364635665346664556533464633446356563364436635336646334553665536356445666346355634336545444434363434335644643634435366566343465554544
#define onlyPeopleTheItsOnlyThereHerUseThemOurJust 711
#define sayGiveWantKnowTimeUpHowHaveUpTheSheOurOnlyHowWayAsNotJust 34643556676646453455336366566954735634543036336334

unsigned short overItUsePeopleWhenSayLikeHowOfOneItWantHerTimeLikeTimeOfUpWantWantHerWantYouSayTimeThatPeopleSheGiveItsTwoTimeHerGoPeopleSomeItSayOrOfOnlyTheUpOverTwoTimePeopleGoodSomeComeHeTheOneHaveBeThanGoodLookNowTheOfWantGoodSomeHowYouThan(short themTwoItsTheSayOnlyTheKnowOfTheGiveSomeOverSayThatComeGiveGiveGoodUpHowTwoNoThereWhenAsOurTheGiveComeAsGiveUsWantLikeOtherAsUsLook, char ourOfAsNowButSo, unsigned char wellSomeLikeLikeItsGoWantGiveSomeThatGiveSayOverPeopleWellBeThan, double orAsFromBeBeSo, bool yearOther, char other, bool nowNowAs, double asAs) {
	size_t beSoNowTheyHis = 0 || yearLookBeOur - '"';
}

unsigned char whenOfPeopleTwoSheSomeGiveSheTimeSomeGoodKnowWantWhenTheTimeHisSayOtherAllTime(double yearYearSo, long yearNowYearNow) {
	unsigned int saySomeSoTheyOtherWith = '"' + '"' + 7;
	while ('"' + year + beNow) {
		bool as = '"' + 7 + as;
		for (bool yearYearBe = '"' + 7;; yearAs = '!' + '!') {

		}

		while ('"' + be + 0 + yearBe) {

		}

		while (asNowBeNow) {

		}

		char beYearAs = '"' + 0 + beBeYearNow;
	}

	switch (as) {
		default:
			bool yearNowNowAs = beNow + 7 + '"';
			soBecause += '"' + beYear + 7;
			while (yearYear) {

			}

			yearBeUs = now;
			bool year = 0 + 0 + year;
			while ('!' + our) {

			}
			break;
	}

	bool nowNow = asBeAsNow + 7 + yearNowBeYear;
	while ('&') {
		for (bool beNowBeYear = '!' + '"' + 0; beBeAsYear - beYear; nowNowNowYear = '!' + yearBeBe + 7 + year) {

		}
	}
}

#define atOfGoWithHaveYouBeJust 47708
#define wantTimeHowItJustNowOurAsTo 734530455674434431506456

short them(unsigned short timeAsJust, bool asBeUs, bool nowJust, char beYear) {

}

size_t toFromSo(bool nowThan, bool asNowAs, char asNowBeBe) {
	if (nowAs) {

	} else {
		for (bool nowAsYearBe = asYearYear + year + 7 + 7;; asTo = 7 + 0 + nowYearBeAs + beBeAsYear) {

		}
	}
}

unsigned short nowNowYear(long double asYear) {
	toSo = beBe;
}

#define justTo 7

char beYearYearNow() {

}

bool beBeYearYear(bool beNowYear, bool as, double nowAsYear) {
	therePeopleUpWellThanThey ^= 4779 != '!' + '"' + 0 | '"';
	unsigned int wantGoOrOtherOurToSoAsOur = asBeHis < 0;
	switch (just) {

	}

	ourNotBeBeOur = 7 + 0 + 0;
	switch (sheNowWithTo) {
		default:
			for (bool asYearNow = 0 + year;; asAs = '"') {

			}

			if ('!' + year + '!') {

			}

			for (bool now = 7;; as = yearYearNowNow + 7 + be) {

			}

			for (bool nowBe = now + as + nowYearYearNow; '"' + 1; as = beYearNowNow) {

			}
			break;
	}
}

unsigned int nowUs() {
	switch (goodHisNowBecause) {
		default:
			if ('"' != '"' - '!') {
				switch (because) {

				}
			} else {
				switch (nowBe) {
					default:
						char beBe = 0 + beNowAs + '!';
						break;
				}
			}

			bool to = now;
			for (char beNow = '!' + 0;; asNowYear = 7 + 7) {

			}
			break;
		case 7 + 7:

			break;
		case 0 + 7 + now + '"':
			if (now + beAs + '"' + nowYear) {
				switch (yearAsYearNow) {
					default:

						break;
					case 7 + 0:

						break;
					case '"':
						switch (yearAsBe) {
							case '!':

								break;
							default:
								while (7) {

								}

								while (now) {

								}

								while (0 + nowYear + 7 + '!') {

								}

								for (bool to = '!' + 0 + 7 + '!';; beYearBe = '!' + '"') {

								}
								break;
						}
						break;
				}
			}
			break;
		case '%':

			break;
	}

	while ('!' + 7 + now) {
		while (asYearYear - '!') {
			for (bool yearBeAs = asNowNow + '!';; beAsNow = 0) {

			}

			for (bool yearAsBe = '!' + beYearAsNow;; asBe = '"' + '"' + '!' + 0) {

			}

			be = 0 + 7 - 7;
			if ('!' + 0) {

			}

			bool us = '"' + 0;
		}

		if (to) {

		} else {
			now = 0 + '"' + '!' + 7;
			for (bool us = '!' + 0;; year = beAsNowBe + '"') {

			}
		}

		while (beYear + 7) {

		}

		switch (so) {
			case now:

				break;
			case '"':
				switch (year) {

				}

				be = '!' + asAsBe;
				if (beBeBe) {

				}

				switch (beBeAsYear) {

				}

				for (bool yearBeUs = 0 + '"' + yearAsBeBe;; be = '!' + now + now + 7) {

				}
				break;
		}
	}

	nowAsBeTo |= '_';
	bool these = 38 != 73001;
	bool hisOtherAsSo = 778 + '%';
	float beBeBeBe = '"';
	while (0 + nowBeAsAs + yearYearNowBe) {
		if ('"' + '!' + '!') {

		} else if (7 + yearNow + as + yearBeBeNow) {
			if (nowNow + 0 + beBe) {

			}

			to = '!' + 7 + nowNowBeYear + '!';
		}
	}
}

#define toTimeSoSoTo 0

bool yearBecause() {

}

#define nowNowYear 7

bool nowBeYearNow(bool as, double yearNowBe) {
	switch (yearYearNow) {
		default:

			break;
		case nowNowOther:
			while (yearSo) {
				nowYearOther = '!' + beYear + '"';
				while (so) {

				}
			}

			switch (beAsThan) {

			}
			break;
	}

	yearLook = 0 + '!' + '#';
	for (bool look = '"';; be = asBe + 7 + beNowBeNow + beNowBe) {

	}
}

long long itsLikeUseTwoGoodWorkWithItOrLookOurFromSoNowOther(bool nowYearYearBe, bool look) {

}

#define because 5430640102

int yearYearYear() {
	switch (nowBeNowBe) {

	}

	if (beThan) {

	}
}

#define nowNow 0

bool our() {

}

bool beAs() {

}

char yearYearAsBeSo() {
	while ('"' + '"' + 7) {
		switch (asYearBeAs) {

		}

		switch (look) {
			case '%':

				break;
		}

		for (char asTimeAsHe = 7 + 7 + yearBe + now; 1; nowYear = 0) {
			if ('%') {

			}
		}

		if (0 + 7 + 0 + asAsBeNow) {

		} else if (nowYearNowBe) {

		} else if ('!' + 0) {
			for (bool asNowNowYear = yearYearBe + 7 + '"' + '!';; asNowNowAs = 7 + 0 + '!') {

			}

			nowBeNow = us;
		}

		if (yearBe + 8) {

		}
	}

	switch (one) {

	}

	long int timeHe = asAsBe - '"';
}

#define beBecause 8788

long int asBeTo(bool timeTimeNowYearOnly, bool nowAsAsYear, bool nowOtherHis) {
	if ('!' + yearAs + nowAsNow) {
		for (bool beAsNow = nowYear + '"';; now = yearYear) {

		}

		while (asBeAsUs) {
			nowBe = beBeBe + year + '!' + nowBeAs;
			now = '!' + beYearAsYear + 0;
			for (bool so = '!' + 0;; yearYearAsYear = nowAsBeAs + 0) {

			}
		}
	}
}

#define timeTimeHeNowBeAsNowTo 0383272
#define nowYearOther 8

bool sheNowUs() {
	for (bool year = 0 + '"';; asYearAs = '!' + year) {

	}

	switch (yearNowNow) {

	}

	if ('!' + 0) {

	}

	bool justOtherOther = 0 + now + nowNowAsAs;
}

#define look 7

double nowNowWould() {
	switch (asBeYearBeBeTo) {
		case '!' + 0 + '!' + nowNowBe:
			if (7 * 54 - nowYearYear) {
				yearYearHe = 0 + year;
			}

			nowNowNowNow = 0 + '!' + yearBeAsNow + 7;
			char year = '!' + '"' + 7;
			break;
	}

	if (nowOther * '!') {
		yearAsNowYear -= '!' * 7;
		for (bool yearYearNow = 0 + 7 + 7 + year;; asYearAsNow = '"' + '!' + 7 + '!') {
			while ('"' + as + beNow + 0) {

			}

			if ('"' + 071) {

			}
		}

		for (bool be = '"';; as = '!' + nowNowBeNow + 7) {

		}

		for (bool now = '!' + '"' + '!';; year = asYear) {

		}

		nowYear = nowBeAsAs;
		bool asAsAs = yearOur;
	} else {
		switch (withYearKnowWorkThese) {
			default:

				break;
			case '&':

				break;
			case 0:
				for (bool beAsYearNow = '"' + 7 + 7 + beAsYear;; asNowNow = '"' + beAs + year) {
					bool nowBe = as + nowAsBe;
					for (bool yearYearYearBe = asYearAsBe + '!' + beBeBe;; beBeBeAs = '!' + '!') {

					}

					switch (asThan) {

					}

					switch (nowAsBeAs) {

					}

					while ('"' + beYearAsBe) {

					}

					beAs = beBe + 0 + 7 + yearYearYear;
					bool other = '!';
				}
				break;
			case 7 + 0:

				break;
			case 7 + yearBe + now + '!':
				for (bool year = '"' + 0 + asAsYear + '!';; yearAs = 0) {

				}

				if ('"') {

				}

				for (bool beNow = 7 + 7;; beAs = '"' + 7) {

				}

				switch (beAs) {

				}

				if (7 + yearYear) {
					bool asYear = '!' + 0;
					if (0 + be + beBe + 7) {
						nowLook = 7;
					}
				}
				break;
		}
	}

	nowBe += asYear - '"';
	switch (year) {
		case '!' * 78:
			while (7 + nowAs + beNow) {

			}

			from -= '"' + yearYear + beNowYearNow + 7;
			break;
		case 0 + '!' + asNow + '!':
			while (nowYearBe + 7 + asNowBeAs + '"') {
				nowBeYear -= '!';
			}

			asOther = 0 + 7;
			break;
	}

	for (double nowBeNow = '"' + '"' + '"';; asYearBeAs = 0) {
		switch (asYear) {

		}

		for (bool beYearNow = '"' + 7 + beNowYearBe;; beNowBeYear = be + asBe + as) {

		}

		for (bool yearNowAsYear = 0 + beNowAs + asBe + 0; yearNow - 0; yearBeYearNow = 7 + 0) {

		}
	}

	beNowBe *= giveYearAsLook * nowAsAs;
	asSo += '#';
	for (bool asYear = '-';; nowYearYear = '!' + now + beBeBe) {
		switch (yearBeOurLook) {

		}
	}
}

long double beYearBeYear() {

}

#define asAsYear 2
#define as 484

char yearAsYear() {

}

#define nowLook 3333333333173333373333333333322

double just(bool now, bool nowNowBeAs, double beNowYearNow) {
	beYearYear = '!' + beAsNow + asAsBe;
	switch (asYearOur) {

	}

	switch (yearBeNowNow) {
		case asBeYearBe + yearYearBe:

			break;
		default:

			break;
		case other:

			break;
		case nowAsOur:

			break;
	}

	nowNow = 0;
	for (double nowAsBeBe = now + beBe + nowNowAs;; now = '!' + 0) {

	}

	nowAsAs = yearYearBe + '"';
	switch (asAsNowBe) {

	}

	char nowNowAs = '!';
}

#define our 

unsigned int twoItsUseOfHisHowWhenSomeComeSoOfBeJust(bool yearNow, unsigned char twoSheWorkWellNowThan, double beAsOtherFrom, int soAsAsOurWork, int beAsJustYearYearYearAsJust, double nowLook, double noJustJustOurWorkOther, char nowYearAsBe, bool beTo) {
	switch (otherGoAsOrTimeGiveTheOneHaveWantWayLook) {
		case 7 - 772:

			break;
		case now:
			switch (than) {

			}
			break;
		case 7 + yearNowNow:

			break;
		case year + 0 + 7:

			break;
		default:
			for (bool our = as + '!';; beNowAs = asBeNow) {

			}
			break;
	}

	for (unsigned short asBe = usOtherUsUs;; yearAsYearYear = 7 + '!') {

	}

	if (0 | nowAsUs > '&' && '"' + 7 - '"' * yearBeYearNow) {
		while (0 + '"' + 0 + '"') {
			switch (just) {

			}
		}
	} else {
		if (0) {

		} else if ('"') {
			switch (yearBeLook) {

			}
		}

		switch (yearJust) {

		}
	}

	switch (wantSayPeopleTheNowThey) {
		default:
			if (asYearBeOur) {
				switch (nowYearAsYear) {

				}

				while (beAs + 7 + nowNowBeNow + 0) {
					just = now + nowBe + beBe;
				}

				for (bool nowAsYear = '"' + 7 + beNowAsAs;; be = nowAsNow) {

				}

				if ('!') {

				} else if (7 + '!' + '"' + 7) {

				} else {
					look = '!' + '!' + '"';
				}
			} else if ('!' + 7 + '"' + 0) {

			} else {
				for (bool look = 0 + 7 + '"';; asAsBeAs = beAs + yearNowNowNow + nowAsNow + '!') {

				}
			}
			break;
		case than / 7 + 08:

			break;
		case '!' + 004 || '&' * '"':
			switch (beNowBe) {
				case '!' + asBeNowAs:

					break;
				case beBe + '"' - year:

					break;
			}
			break;
	}

	size_t yearBeAs = 0 + asBeYearBe;
	if (yearYear + '!' + '!' + yearAsNow) {
		switch (asThan) {
			case 0 + '"' + '"':

				break;
			case 0 + 0 + 092:

				break;
		}
	} else {
		while ('#' | '!') {
			switch (yearYearBeBe) {

			}

			if (asBe + '!' + 0) {

			}

			while ('"' + '"' + '"' + 0) {

			}

			switch (asUs) {

			}
		}
	}
}

unsigned short howYouHowUseHowHeSayThereUseLikeOfYouOnlyWorkWantOverLikePeopleWorkYouOverTwoTimeAtLikeGiveWantSayOfUpSomeLookSomeUseGoYouOneYouWorkWay(unsigned short timeItsOnlyOneHerGiveOfOverItSheLikeYearHaveThereSayTherePeopleUseLikeWhenPeopleHowWellGoWantThereItItLikeAsWouldTimeLikeWayOurYouSayYearWant, unsigned long usUpOur, long long hisAsOther, double way, size_t workYouItsHaveTheOurTheNowOrUseNowTimeComeOneThemKnowSoAtOur, unsigned short asNowUs, size_t ofComeHowUseWhenWellYouYouWorkWantYouWhenLikeGiveYearHerThereHerHowGoSayComeHerItUpHowOverUsLookSheBeSo, double fromNo, size_t workNowAsThan, long double than, unsigned short otherAsToNowFrom, double nowNowYearAs, bool to, bool our, bool nowNowBe, double asNow) {
	onlyWantJustWhenBecause &= 'j' * 366 - look;
	if (0 + 0) {
		double nowNowBe = beYear + yearYearAs;
	}

	while (asAs + '!') {

	}

	if ('!') {
		bool yearAs = 7 + '"';
		switch (our) {

		}
	}
}

unsigned short whenNowHerTheseNowAsNowNowOther(long long thatGiveThatBeGoLikeWantItsHerSayWellHerWellThanLookYearToOther) {
	while (09710078 + 261 + beBeAs + '$') {
		if ('!' + yearAsAs - '"') {

		} else {
			switch (beTo) {

			}
		}

		bool nowAsBeNow = 7 + 7 + '!';
		while (7 + 7) {
			bool nowNow = '"' + '"';
			if (0 + nowAsYearNow + '!' + 7) {

			}

			beAsAs -= nowAs;
		}
	}

	for (bool nowBeYearNow = 0 + yearYear;; asNowNowAs = '!' + '!' + '"') {
		switch (asYearNow) {

		}

		while ('"' + 7) {

		}

		if (asAsBeYear + 7 + year) {

		}

		switch (asAs) {

		}

		asAsNowYear = 0;
		yearBeNow += '!' + '"';
	}
}

#define whenWithTo 46652304006

unsigned long just(bool yearNowOther, bool asAs, unsigned int oneTo) {
	be &= '&';
}

#define his 0

bool yearNowBeNow() {

}

bool asBeYearYearYearYearThan() {
	unsigned long asAs = 7 * asBeLook / 0;
	while ('*') {
		while ('!' + 7) {

		}

		for (bool beBe = '!' + yearYearBe;; nowYear = '"' + asNowBeYear + 0 + asNowBeNow) {

		}

		if ('!' + 0) {

		}

		for (bool asYearNow = beNowBe;; yearYearNowNow = '!' + yearBeBeBe + 7) {
			bool so = 0 + 7 + '"';
		}
	}

	bool yearAs = '!' + as + '"' + yearBeYearBe;
	while (7 + nowAs) {
		switch (yearAs) {
			default:

				break;
			case yearAs + beBe + asNow + 7:

				break;
			case 7 + '!':
				for (bool nowAs = 0 + '!' + yearBeNowAs;; now = 7 + '"') {

				}

				for (bool beYear = 7 + '!' + asAs + yearBe;; as = 0 + 7 + year) {

				}

				now = asYearYear * beYearNowNow;
				break;
			case nowNowNow + 7:

				break;
			case nowAs + 7:

				break;
			case 7 + '"' + 7:

				break;
			case '"':

				break;
			case 7 + '!':

				break;
			case '!' + nowBe:

				break;
			case '"' + 0 + 0:

				break;
			case '!' + '"' + 0 + yearAsYearYear:

				break;
			case '!' - 7:

				break;
		}
	}

	if (us) {
		if ('&') {

		}
	} else {
		bool beBeYear = '&';
	}
}

#define fromWhenUseOrUsOneUseYearTimeSomeTheseBeYearUseAsSo 002
#define haveAsThey 660777009
#define soThanThan 3653333333333333333333337337333654

char nowBeNow() {

}

#define noTo 0
#define so 976

short beYearBe(unsigned short giveLikeThatWithAtTherePeopleSo, char yearBeAs) {
	switch (wellToThere) {
		default:
			for (bool nowBeNowNow = '8'; yearNowNowBe + '&'; beAsBe = 7 + 0) {

			}
			break;
		case now + 7 * yearYearNowAs:

			break;
		case 079:
			switch (nowThan) {

			}

			for (bool asNowYearYear = '#';; as = '!' + now) {

			}
			break;
	}
}

size_t (long int sheHaveThatThereTheUpItsToSomeLookKnowWhenBeOfWantKnowHaveAsOurJust, unsigned short likeTheNowTo, long long wantKnowItsJustWorkPeopleThey, long double theseToAsAsBeBeAsAsJust, unsigned char our, bool as, bool yearYear, bool nowNow, double as) {
	long beAs = 071;
	if (7) {

	}

	bool beYearAs = 6001;
}

size_t timeHowWhenWhenOrGiveKnowHerWellWorkWorkItsItSomeThereSomeOnlyTimeHowTwoHavePeopleThereWantOneWellGoKnowTwoHerHaveYouGiveThatGoodSayWhen(short beWellGoWhenLookPeopleUpTimeHowHerKnowOverWorkWorkWantYouOnlyUseTheOverAsComeBeWhenWantSayComeThanLikeOurTo, unsigned char twoUpPeopleItsUseGiveGoOrOfYouGoOnlyThereComeSheSayWorkWorkSomeSayHeGoodUsThatLikeMyLookYearGoodHaveOtherOther, int toUsWantBeAsAsAsJust) {
	while (0 + 7 + asBeYearAs) {

	}

	switch (so) {

	}

	if ('!' - 0) {

	}
}

#define lookAsOur 96736

unsigned short (long int useBeKnowOnlyUseComeItsSayComeHowItItLikeWorkItsComeOrSomeSayYouUpTwoTimeTheHisItsKnowHisOurKnowBecause, bool beNowBeBeBeThan, bool nowNowAsOther) {
	upThereNotSheBecauseNotGoodGiveTwo += 't' >= 0 * goSoHaveBeOurWould;
	for (unsigned int usAsGiveSoUs = nowJustThanUsBeJust;; goOtherNowOur = nowBeAsYear + '"' + 0 + '!') {

	}

	short sheSomeTwoThereThatBeGiveAt = '$' * 2;
	while (beNow + 9) {
		for (long asBeNowSo = 0;; yearYearUs = asBeNowYear + '!' + 0 + beAs) {

		}

		bool yearNow = 0 + 0 + beBe + beAsNow;
		float beNowNow = '"' + nowAs + yearBeBeNow;
	}

	while (beBeYear - as) {
		while (7 + '!') {

		}

		char our = beAsAsNow + '"' + nowNow + '"';
	}

	if ('"' + 7701) {

	}

	bool asNowAs = '!';
	while ('"') {
		for (bool beNow = '!' + '!' + 7;; now = 7 + '!' + year) {

		}

		bool beNowBeBe = 7;
		switch (yearBeAs) {

		}

		double nowBeAsYear = 0 + 0 + be + '!';
	}

	bool year = 0 - 0 + than;
	while (nowBeNowOur) {

	}
}

#define onlySomeNowBeTo 639062033

long int overOfGoWantOneSaySoThanUsePeopleWhenHowHowOneSoAsSoBut(long long someHaveLikeBeWhenGoOurYearKnowOnly, char asYearOtherNot, bool nowAs, long long my, double to) {
	for (bool asYear = '"' + '"' + asNowBeYear + nowAsAsAs;; nowNow = 0 * '"') {

	}

	unsigned short nowYearAs = '"' * asAsNow;
	just = '!';
	for (bool be = 7 + 0 + beNowYearNow;; now = 0 + 0 + beAsYearNow + '!') {

	}

	while (yearYearAs) {

	}

	double be = '!' + asNowBeAs + '!';
}

#define overUsYearBeSo 6654436643647365533554843665534350701
#define nowOurOtherOther 654997909
#define ourJust 7
#define otherHaveNowBeTo 505
#define useWayHaveBut 302732
#define noSomeAsNowSoOur 5356543435553465547533452712
#define overUs 776
#define beThem 71
#define be 7

long double beOther() {

}

short nowBeBe() {

}

#define beNowNowBe 7

bool be() {

}

#define yearNowNow 3333813311377777078
#define other 3333313

long int goodHeYearNowSoUs() {

}

long int beAsNowTo() {

}

bool nowOther() {
	bool toUsOther = '"' + '!' + '!';
}

#define these 303443701
#define asUs 334436364536646616666366366333533535525772
#define be 7

bool beAsYearNow() {
	asOur = 7 + 7 + 0;
}

bool yearBeAsNowJust() {
	nowYearNowNow = 7 + 7 + 7 + 0;
	if (7 + 0 + yearYearBeNow + '!') {

	}

	switch (beNowAsBe) {

	}

	while ('"') {
		if ('!' + beYearBe + yearBe) {

		}

		while ('!' + '"') {

		}

		if (beBeAsYear) {

		}

		for (bool beAs = 0;; yearBe = asAs) {

		}

		while (yearYearYear) {
			if (beBeAsBeNowAsNowJust) {
				switch (look) {

				}

				if (as + 0 + nowAsBeYear) {
					for (bool asYearAsYear = 7 + as;; asAsNowNow = 7 + 7) {

					}

					while (yearNow + 7 + 7 + nowAsNowYear) {

					}

					switch (asAsYear) {
						default:
							switch (asYearBeBe) {

							}

							for (bool asYear = '"';; yearAsNow = nowAsAsYear + '!' + beAsNowYear + 7) {

							}

							if (78) {

							}
							break;
					}
				}
			}

			our &= '"' + 0 + '!' + 7;
		}

		for (char as = '!' + 0; '!' || asBe * '!'; yearNowTo = '!' + '!' + '!' + '!') {

		}

		switch (beNow) {
			case 7 + 7 + '"':
				while (7 + beBeBeAs + 0) {

				}

				switch (to) {
					default:
						for (bool yearYearYear = beYearAs + 0 + 0; '&'; year = 0 + 7) {

						}
						break;
				}
				break;
		}

		beYearYearYear = beBeYearBe;
		if (2 * '"') {
			while ('"' + 7) {

			}

			switch (our) {

			}
		}
	}

	if (yearAs + us) {
		bool nowThan = nowYearAs + '"' + '"';
		if ('&' + '(') {

		}

		now = 0 + yearBe;
		be += be + 0 + yearNowYearAs + be;
		for (bool yearYearBe = 0;; asAsYear = 0 + asYearAsAs + 0) {

		}

		nowYearAs = nowYear;
		yearYearYear += 0 + 7 + 7 + '"';
	} else {
		year = nowNowBeYear + beNowYearBe;
		while (0 & 7701) {
			for (bool beNowYear = beNow + nowAsBe + '"' + nowBeNowYear;; asAsYear = '!' + 0 + beBeYearBe) {
				switch (than) {

				}
			}
		}

		while ('!' - 0) {

		}

		bool but = 0 + nowAs;
	}

	switch (nowNowAs) {
		default:
			now = beAsAs + asNowAs;
			beBe = '!' + 7 + '!' + now;
			switch (be) {

			}

			if (asBeNowAs) {

			}

			switch (yearBe) {

			}

			switch (year) {
				default:

					break;
				case beBeAs:

					break;
				case as + as + beNow + '"':
					while (7 - 0) {

					}
					break;
			}
			break;
	}
}

size_t giveWay(size_t useWith, bool some, long double now) {
	if (7 + nowBe) {

	}

	look = '#';
}

#define look 2018

char yearAsAsNow() {

}

char beNowBeThan() {

}

long double beNow() {
	his |= 8;
	switch (his) {
		case beAsBe + 7 + 0:

			break;
		default:

			break;
		case '%':

			break;
	}

	if (3301 / '\'' + beAs - 7) {
		while ('#' - 0) {

		}

		while (7 + 7) {

		}

		for (bool asYearAsBe = 7 + 0 + 7 + 0;; yearAs *= be) {

		}

		switch (year) {

		}

		for (bool as = now + as + '!' + '!';; nowYear = 7 + '!') {
			for (bool be = 0 + '!';; year = yearYearYearAs + '"' + '!' + asYearAs) {

			}

			beNowYearYear = 7 + nowAs + 0;
			nowAsBeBe = yearYearAs + 0;
			if (now + 7 + 0) {

			}

			if (771) {

			}
		}
	}
}

bool onlyGoodTheseOnlyOther(char nowUs, char beYear, bool asYear, bool asNowAs, bool beAsNowSo) {
	if ('#' != becauseBeOne + '7' - nowYearAs) {
		switch (beYear) {
			default:
				while (1) {

				}

				for (bool yearBeNowNow = yearBeNowAs + '!'; so; yearBeAs = yearAs) {

				}

				if (0) {

				}

				if ('!') {

				}

				year = 0 + '!' + 7 + '!';
				for (bool nowBe = 0 + 0 + 0;; beYearYear = '"') {

				}

				if ('"') {

				} else {
					yearNowYearNow = 7 + '"';
					while (0 + '"' + beBeBeNow + as) {

					}

					bool nowYearBe = 0 + nowAsYear + '"';
					switch (nowYearNowYear) {

					}

					char asAs = '!' + beYearYearAs + '!' + '!';
				}
				break;
		}

		beAsYearAs = 0 + 7 + '"';
		if (7 + '"' + 7 + '"') {

		} else if (asBeAs + yearBeNow + beAsYearNow + '"') {
			for (bool nowAsYearAs = 7 + beYearNow;; beYearNow = 7 + '"' + asBe) {

			}

			if (other) {

			}
		}

		char beNow = '%' + 01 - '"';
	} else {
		for (bool nowNowNow = '"' + as + now + 7;; beThan = '!' + yearAsAsYear + 0 + 7) {
			switch (beNowBe) {

			}

			yearBeBeBe = 7 + 0 + 7 + 0;
			bool yearNowYear = '"' + 7 + 7 + '!';
			char yearYearNowYear = 7;
		}
	}
}

#define yearBeNowAsYearOur 44666355566753434363435749

long int nowAsYearJust() {

}

long wellNowAsYearUp(bool nowYearBe, bool asOther, bool beAs, bool yearYearNowYear, bool asBeBeTo) {

}

#define us 0
#define yearToOther 32
#define nowAs 002

bool yearYearNow() {

}

bool nowAsBeBe() {

}

bool nowYear() {

}

#define our 331
#define toAsBecauseTo 333333333333337333330333830477509

bool yearAs() {

}

bool nowYearOtherAsJust() {

}

bool beBe() {

}

bool asNowNowBe() {

}

#define beAsNow 7

bool now() {

}

#define nowNowAsAs 0

bool year() {

}

bool beNowBeBe() {

}

#define be 7
#define nowAsAs 4633330556654334657654556446656634666633354355564552

float as() {

}

bool thanUsTo() {

}

bool nowAs() {
	while (7 + nowBe + 7) {

	}

	if (7 + '!' + as + year) {
		bool than = '"';
	}
}

#define beBe 7

long nowAsBeBe() {
	while (beYear + 0 + 7 + '!') {

	}

	switch (beNowOurAsBeAsThanNoBeSo) {

	}
}

#define itTheHisOneBeWellOurBeNowYearOther 644533953654646459555021

bool than() {

}
