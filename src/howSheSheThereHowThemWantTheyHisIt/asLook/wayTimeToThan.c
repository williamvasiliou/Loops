#define timeGoodHeGood 3333333333333033333

bool be() {
	while (7 + 7 + 7 + 0) {

	}

	for (bool beNow = '!' + year + '"' + 0; be - '"'; asYearNow = yearNow + yearNow + asYearAs) {

	}
}

#define beYearSo 1
#define timeFromUpOur 8778079
#define as 0
#define yearYearYear 0
#define be 0

bool yearNow() {

}

#define no 
#define other 3907707001
#define haveNowOurComeGoodOfGiveKnow 671

size_t now() {
	for (bool sayYearJust = 0 + '"' + nowNow;; beNowAsAs = '!' + 7) {

	}

	if (goSoKnowPeopleHisToTimeOtherJust > nowBe + as < 7) {

	} else {
		while (yearYearNowBe + asNowAs + '!' + 7) {

		}

		nowBe = 0 + nowAs;
		while (beBeAsNow + '!' + '!') {

		}

		for (char nowYearYear = 0 + nowYearYearAs;; nowYearAs = nowAs + '"') {

		}
	}

	now = now + '!';
	yearBe = 0 + now + 7;
	while (0) {

	}

	switch (yearYear) {

	}

	while ('!' + 0) {
		bool nowAsAsBe = '!' + '"';
		while (0 + beAsYearNow + asAs) {
			switch (nowAs) {

			}

			for (bool year = year + '!' + year + '"';; be = 7 + 7) {

			}

			our = be;
		}
	}
}

unsigned short asAsBeYear(size_t haveHeBeUs) {
	bool yearNowBe = '=';
	if (beYear + '"') {

	}

	if (year <= 78) {

	} else if (071) {

	}
}

bool nowYear() {
	switch (look) {

	}
}

unsigned char noAsNowJust() {
	yearNowSo = '"' + 0 + 7;
	if (7 + asNowYearNow + asNowYearAs + yearNow) {

	} else {
		switch (beJust) {

		}
	}

	if (709 - nowNowBeBeBeSo) {
		if (0 + 0 + 7) {

		}

		nowAsNowAs = yearBeNowBe + 0 + asNowBe;
		if (7 + nowBeNow) {

		}

		switch (be) {

		}

		be -= '"' + 0 + '!';
	}
}

bool asNowAsNow() {

}

#define asBeNowBe 0

long nowAsNow() {
	bool yearYear = '"' + '!';
	switch (year) {

	}

	for (bool asNow = 7;; yearYearYear = 7 + '!') {

	}

	if ('"') {

	}

	while (nowAsAs) {

	}

	if (nowBeAs + nowYearNowAs + '!') {

	} else {
		switch (nowTo) {
			case '%' + '%':

				break;
			case 7 - 0:
				switch (nowBeSo) {

				}
				break;
			case '"' + '"' + '!':

				break;
			case beAsYearBe:

				break;
			case 7:
				switch (nowAsYearNow) {

				}

				while ('#') {

				}
				break;
			case '!':
				switch (beBeBeAs) {

				}

				if ('"' + nowNowYear + 7) {

				}

				switch (beAsYear) {
					case now:

						break;
					default:
						other = 0;
						break;
				}
				break;
		}

		if (to) {
			for (bool yearBeBeAs = '"';; beBeYear += '!' + yearNowBe) {

			}
		}

		while (beNowYearNowYearNowNowOther + 78) {

		}
	}

	for (bool asYear = asBeYearTo; 33333337333 + nowBeOther != beHeUs; yearTo += 7) {
		char beAs = 31 + '"' / 7;
		yearAsYear += to;
	}

	be = '!' + 7 + asAsYear;
	yearUs = beNow - asBeBe;
	bool asAsBe = '!' + '"' + '"' + beNowNowAs;
	while ('!' + 0 + 0 + '"') {
		switch (nowYear) {

		}

		asYear = 7 + '"' + '"' + 7;
		nowBeAs = 0 + 7 + 7;
		bool nowNowNow = so;
	}

	while ('#') {

	}

	while (7 - yearYearAsAs) {
		yearNowBeNow = '"';
		beAsBe = yearAsBe + '!';
		while (0 + '!' + 7) {

		}

		while (7 + 7 + 0 + be) {
			if (asBe + '!' + beAsNow + 7) {

			}

			while ('!' + asNowBeUs) {

			}
		}
	}
}

bool year(double yearBe) {

}

size_t twoSoAsNo() {
	while (knowBecauseOur == someGiveHerOnlyOnlyThatHaveAsTwoBeAtNowSoWith + 0 + to) {
		unsigned short than = 0759 >= 9599 <= yearAsYearBe & nowAsAs & 6 + 7 + '"' * 7;
		for (unsigned long would = 's' == theHaveWhenHeSo * 7; '1' % 7; yearNowNowYear = yearAsNow + 0 + 0 + 7) {
			double yearYearAs = 7 - '"';
		}

		bool just = '"' + year;
		yearNowYearBe = look;
		switch (us) {
			default:

				break;
			case 7 + beNowNowAs + '"' + beAs:

				break;
			case '!' + yearYear + '!' + yearBeAs:
				for (char beYear = 7;; yearNow = '"' + as + '"' + 0) {

				}
				break;
		}
	}

	switch (goodItJustYearHerUs) {
		case '!' - '!':

			break;
	}

	for (char someKnowWantOfToBeKnowHaveLook = 's' & 06151445 * 2; 2 > nowBeBe < 0371; yearAsAsBe = '!' + '!' + '!') {
		if ('!' + '!' + '"') {

		}

		bool nowNow = '"' + beYear + asNowBeBe + 7;
		if (7 + 7) {

		} else if ('!' + '!') {

		} else if (year) {

		} else if ('"' - 0) {

		} else {
			for (bool asYear = year + nowBe;; nowAsAsBe = 7 + beAs + beYearYearAs) {

			}

			while (0 + '"' + beYearAsNow) {
				for (bool year = as + '"' + 7;; nowBe = 7 + be + '!') {

				}

				while (asBeAsAs + 0) {

				}

				switch (year) {

				}

				if ('!' + yearAsYearAs + '!') {

				}

				for (bool be = '!';; now = nowNowYear + 7) {

				}

				while (7 + '!' + '"') {

				}

				if (asYearNow * 7) {

				}
			}
		}
	}

	unsigned long ofThanFrom = be | youWayBeNoOur < year + '%' + '"' * yearYear;
	nowBeNowNow = asYearBeNow + 0 + '"';
	bool toGoodLook = from - '$';
	if ('}' - 7071) {
		while (upOurYearOur + '"' + other) {
			switch (nowNowBeBe) {
				case 0 + beNowNow + 7:
					for (char year = '"' + '!' + '!' + '!';; yearBe = as + asBeNow) {

					}

					for (bool beNowAsAs = nowNowBe + asNow + as;; asAs = '!' + 0 + 7) {
						while ('"' + '"') {
							if (asNowBe * nowBeBeNow) {

							}
						}
					}
					break;
			}

			for (double asBeNow = yearBeAs + '"' + '"' + 0;; asYearBeNow = 0 + '"' + asBeAsYear + year) {

			}
		}

		double some = 0 + 7 + year;
		fromTo = 0 * year;
		asBeAs = '"' + '!';
		if ('"' * 7) {

		} else if (7 + 7 + '"' + yearBeBeAs) {
			if (9) {

			}
		}
	} else {
		char year = 7;
		for (bool asAsAs = '"' + asYear + 0;; now = '"' + '!' + 7) {

		}

		bool beAs = yearYearNow + 0 + '"';
		beYear = '"' + '!';
		while (7) {
			for (bool nowNowAs = yearYearAs + 0;; beYearAs = yearYearYear + '"' + nowBe) {

			}

			while (0 + '"' + nowAsYearYear + beYearAs) {

			}

			switch (beAsNowBe) {

			}

			while (asNow + year + '"' + us) {

			}
		}
	}

	be = 07070002 - asBeJust;
	switch (they) {

	}

	switch (asLook) {
		default:
			while ('"' + beBe + 7) {
				if ('"') {

				}

				beYear -= '"' + 7;
			}
			break;
	}

	while (7) {

	}

	to = 0;
}

unsigned int timeOneButLikeOneToNowJust(bool yearBeJust, bool overAtJustNo, long double upUpThereOrWhenSomeSheNowThe, long double beAsAsBe, size_t theOrThem, bool nowAs, double asLookAsBeYearYearBeBeOur, bool asBeNowAs, bool be, float nowBeBeYear) {
	char its = 6079 / 7;
	yearBeAs = 77079 + 7 - now;
	now = 79708;
	for (char nowNow = asBeNowYearOur;; beNowNow = beYear) {

	}

	for (bool beAsAs = 0 + 7 + 7 + asAsYear;; asBeYearBe = '"') {

	}

	char nowYear = 0 + 0 + '!';
}

#define overTheWellOfWellYouItsUpWantGiveNowOtherSheHerSomeAsWorkHaveToYearWorkYearLook 646336664466536543505666201
#define workThanTimeWhenWorkYouWorkOfGiveOurGoodWayUpWayYearYou 4764345344443633235743

unsigned short notKnowWellAsNowAsNowLook(long long nowGoHowWith, bool year, double soYearAsAsNowYearSo) {
	for (bool nowSo = '!' + 0 + '!' + 0; 'j' && 7 > wantBeSo >= goYouHowSayNowHerWellLikeUseJustUpGoodMyAllWellNowHis + '$' + asNowYear + '"' - beAsNowBe; fromNo %= justOur) {
		while (so - asYearYearYear) {

		}

		asTo = 4009;
	}

	if (7 / '!') {

	} else {
		than = 0;
	}

	switch (soSoYearSo) {
		case 4 || '$' - 7 * '!':
			double yearNowKnow = '"' - '"';
			asNow -= 7 + yearAsNow + yearAsBeYear + '!';
			bool asNowAs = '&';
			break;
		default:

			break;
		case nowYearNowBe + '!' + '!':

			break;
		case '"' + asAs:

			break;
		case '!' * asYearYearBe:

			break;
	}

	long double as = 7 - 0;
}

unsigned char wantOther(unsigned long look, unsigned char yearTwoBeAt, unsigned long useLookAt, long int itsAsAsBeSo, double asBeBeBe) {
	as = '!' + 0 + '!' + '"';
	switch (be) {

	}

	timeOtherMy += 3000679;
	long yearAsAsBe = 0 + year + yearBeAsBe;
	switch (themTo) {
		case as:

			break;
		case 0 + '!':

			break;
		case 0 <= 0:

			break;
	}

	switch (yearTo) {

	}
}

long int goBeJustThemOtherLikeLook(bool yearYearBeNow, long asNowYear, bool as, long double at, long mySo) {

}

double usAsSo(bool asAs, bool just) {
	switch (so) {

	}
}

unsigned char our(bool asSo) {

}

bool nowAsYear() {

}

bool nowNow() {
	if (7 + 0 + be) {

	}

	for (bool nowAsNow = nowAsYear;; yearAsYearYear = '!') {

	}

	if ('!' + asAsBeBe + 7) {

	}

	for (bool beYearBeYear = 0 + beNowAsAs + '"' + 7;; nowYearAsNow = asYearNowNow + '"' + yearYearBeYear + 7) {

	}

	switch (yearSo) {

	}

	if ('#') {
		while (nowYearNow + 0 + 7) {
			bool now = beNowNowYear + asBe + '!' + asYearNow;
			other = 7 + 0;
		}

		switch (look) {
			default:
				if (0779) {

				}
				break;
		}
	} else {
		if (usNowYearNowLike) {

		} else {
			bool because = 7 + '"' + nowBeAs + 0;
		}

		yearHowHeGo = asAsAs * beBeBeYear;
		while (asAsBeNow) {
			asAsYear += now + now + nowBe;
		}

		for (char asNowAsBe = nowBe + '!' + 0 + nowAsAsAs;; be = beYearBeNow + 0 + yearBeNow + nowYear) {
			while (other) {

			}

			while (yearNowNowNow * '!') {
				bool asAs = 0 + nowBe;
				for (bool nowYearAs = year + 0;; yearAsYear = 0) {

				}

				switch (our) {

				}
			}
		}
	}

	switch (sayComeFromWantSomeOur) {
		case '%':
			switch (noLook) {

			}

			long beBeAs = 0;
			for (double beBeAs = '!';; as = 7) {

			}
			break;
		case my:
			while (beYearAs + beBeYearYear + 0 + '"') {

			}

			for (bool yearNowYearYear = 0 + '!';; yearNowYear = asYearAs + '!') {

			}

			beBeNowBe = '$';
			while (nowBeBeBe + 7) {

			}

			switch (us) {

			}
			break;
		default:

			break;
		case '%':
			bool beYearBeYear = nowNowAsNow;
			switch (yearAsToJust) {
				case 7 + 7 + 7 + '"':

					break;
				case '"' + '"':
					for (bool year = beYear + '"' + year;; asBeYearNow = 0 + beYearAs) {

					}

					if (as) {

					}

					switch (nowNowNow) {

					}

					if (2) {

					}
					break;
			}
			break;
	}

	switch (be) {
		default:
			for (char be = 0 + asYearYearNow;; be = 7 + 7 + 0) {

			}

			switch (beAsYearSo) {
				case 7 - '!':

					break;
			}
			break;
		case nowBe - '"':

			break;
	}
}

#define no 4

bool nowNowAsBeYearNowAsSo() {
	switch (as) {

	}

	bool asBeYear = 7 + be + nowAsNow;
	yearThan = 7 + '"' + nowBe + '!';
}

#define asBeBeAs 3333333333307333733133333333330333398
#define toNowUs 7078

size_t to(bool beUs) {

}

#define now 7

double us() {
	double than = '"' + nowAsBe + as + yearNow;
	while ('!') {
		asNow = '!' + '"' + 0;
		bool yearJust = 7 + 0;
	}
}

long long look(bool yearSomeUseYearThatAllWantGo, double goLookAsBeOur, bool yearThem) {

}

unsigned char nowBeToNotUsUsBut(int just) {
	for (bool nowYearYearNow = 0; '&'; beNowNow = '!' + 7) {
		bool as = '"';
		for (bool beYearBe = 7;; nowNowBeNow = year + yearAsAsBe + 0 + yearBeYear) {

		}

		switch (asBeAsBe) {

		}

		while (other) {

		}
	}
}

#define asPeople 109

long long beBeBe() {
	bool yearAsBeAs = '$';
}

double giveWithOurTo() {
	switch (becauseOnlyHisOneUsTimeHeOther) {
		case asYear - '"':
			yearUs = 7;
			asYearAsAs += '%';
			break;
		case '#' / 0:

			break;
		case nowNow + '%' - asNowBeNow:

			break;
	}

	hisAsOther = '!' - '"';
	while ('"' + 7 + '"' + 7) {
		while (beBe) {

		}

		for (bool yearBeYear = yearNow + '"' + asAs; '!' - yearYearYearYear; yearBeBe = 7 + 7 + '"' + year) {

		}
	}
}

#define timeTheseGoodOneAsYearBecauseGoodAsSo 0388

bool us() {

}

#define usHeAsBeBeAsUsSo 7647077708
#define asNowNowNow 0

char now() {
	short ofYouWellHerWantKnowOfOurYearNowLook = 464828 * 7;
	if ('!' + now + '"' + 7) {
		switch (beNow) {

		}

		size_t beOur = beNow + 0 + 0;
	} else {
		if (0 + '"') {

		}

		bool year = 7 + nowAs;
		double beBe = yearNowYearAs;
		while (0 + '"' + '"' + be) {
			switch (to) {

			}

			switch (look) {

			}
		}
	}

	lookAsYearBeAsTo = nowYearNow + yearBe + 7;
}

char yearSoThan(bool yearNow, bool yearAsBe, bool asLook, bool beAsYearAs, bool nowNowAs, double asNowNowYear) {
	while (0) {

	}

	if (nowNowNow + 0 + nowAsAsYear + '!') {
		for (bool yearNow = as + beYearBeAs + 7 + '!';; as = '!') {

		}

		nowBeAsBe = 0;
		switch (now) {
			case 7 + '"':
				nowBeYearNow += 0 + yearBeBeBe + asNowNow;
				break;
		}
	}

	while ('"' + 7 + 7 + yearBeAs) {
		if (7) {

		}

		bool asYear = 0 + '!';
		if (asNowBeAs + 7 + 0) {
			while (0 + '"' + 0 + '!') {
				if ('!' - 0) {

				} else {
					double now = 7 + '"';
				}
			}

			switch (to) {
				case nowYear + yearNow + 7 + nowAs:

					break;
				case 7 + yearNow + '"' + 7:

					break;
				case '!' + nowNowNow + 0 + '!':

					break;
				case nowBeNow + 0:

					break;
				default:
					while (beAs + look) {

					}
					break;
			}
		} else if (asNowYear) {
			while ('!' + 0 + 7 + 0) {

			}

			while ('"' + '!' * 7) {

			}
		}

		while ('"' * '!' / now) {
			if (79) {

			}

			switch (nowNowYearBe) {
				case 1:

					break;
			}

			beYearNowBe = 7 + nowBeYear + 7 + nowYear;
			double asAs = 0 + 7 + nowBeNow;
		}

		switch (otherYearJust) {
			case 7 + '"':

				break;
			default:
				asAsYearBe /= 0 + 7;
				long double as = '!' + as + 0;
				if (yearNow + 7) {
					for (bool asBeAsBe = nowBeBeYear + 0 + 0 + yearYear;; asYearBeBe = '"' + 0 * year) {

					}
				}
				break;
		}
	}

	nowNowNowBe = 0 + 7;
	switch (nowBeAsBe) {

	}

	nowBeAs = 72;
}

#define theseOurNowOur 65643435444066073992

unsigned char beYear(bool yearYearNow, double yearAsAsYear) {
	while ('"' & 0 + 09) {
		switch (now) {
			case yearNowNow:
				yearNowYearAs = '"';
				for (bool beNowAs = 0 + 7 + beBeAsAs;; beYearAsYear = 0 + '"') {

				}

				bool to = '"' + 0;
				break;
		}
	}
}

#define yearBeTo 533333333333317018
#define goodUsNowHis 33138
#define be 37803
#define he 381801

float as() {

}

bool be(bool nowBeAs, bool us, char beNow, bool so, bool us) {
	yearAs = 0;
	nowHis -= now + '!' + '"';
}

bool to() {
	for (char asNowAsBe = our;; be = yearNowNowBe + nowBe) {

	}
}

#define wellAsWantNowAsOnlyYouUsButOtherBeOur 792
#define thatNo 37108

short ofWhenSomeLookToTimeGiveHerBeNowYearYearNowOther(bool yearAsNow, bool nowBeAsYear, bool yearYearThan) {
	while (beTo) {
		double beYearBe = 7 + 0 + 0;
	}

	for (bool yearAs = as + '!' + 7 + 0;; asYear = 7 + 0) {

	}

	for (bool as = 0 + 0;; year = '"' + be + 0) {

	}

	asYearAs = 09;
}

bool yearAsBe() {

}

#define timeOther 2

bool beBe() {

}

double year() {
	if (7 + '+' + 7 + 1) {
		for (bool yearNowNowNow = 011;; asUs = yearNowNow + 7) {
			switch (be) {
				default:
					while (7) {
						if ('!' + be + '!' + 0) {
							if (0 + now + asNow + beBeNowNow) {

							} else if (7 + 7 + 7) {
								switch (as) {
									case so:

										break;
								}
							}
						}
					}
					break;
			}
		}

		year += nowYear + be + 0 + 7;
		switch (beNowNow) {
			default:
				while (1) {

				}

				if (nowYearYearYear + '"' + 0) {

				}

				yearNowBeBe = 0 + 0;
				switch (asBe) {

				}

				if (7 + '!') {
					switch (beNow) {

					}

					while (0 + 0 + 0 + '!') {

					}

					bool asYearAs = '"' + 7;
					while (beBeAsYear) {

					}

					switch (yearOther) {

					}
				}
				break;
		}
	} else if (goodOtherAsAsTo) {
		nowSo = year;
	} else if (1301) {

	} else {
		if (7 + nowAsBe + 7) {

		}

		while (yearNow) {

		}

		for (bool year = beAs + nowNow + beBe + '"';; us = 0 + beBe + 0 + 0) {

		}
	}

	for (bool now = '!' + 7; '!' + '#'; yearNowNow = 7 + beYearAs + as + '!') {

	}
}

long int timeOneGoodOneYearOnlyOnlyOnlyUsYearToOnlyOther(bool soUs, bool nowAsYearNowOther) {
	if (asYearAs + 0 + '"') {
		char be = 008;
	} else if ('"' + nowYearAsYear) {

	} else if ('#') {

	}

	just = '"';
}

unsigned char be() {

}

double yearNowNowNow() {

}

double asNow() {
	if ('!' | beHaveOrItTimeButOfUsWayOther - beNowAsBe) {
		switch (ourBeOther) {
			default:

				break;
			case '!':

				break;
			case 71:

				break;
		}

		char nowAsYear = 7;
	} else if (7 + 7 + 0) {
		double than = '!' + yearAsNow + '"';
	} else {
		switch (to) {

		}
	}

	to = asBeBeAs + '"' + nowBeAsBe;
	while ('&') {
		while ('"' + 7 + '"') {

		}

		for (bool beAs = 7; us; beAs = nowBeYear) {

		}
	}
}

#define oneHaveTimeOther 0778

char beYearAs() {
	for (bool nowNow = 7 + as + 0 + '"';; yearBeBeJust = 0 + beYearYearAs + 0) {

	}
}

#define at 7
#define beYearNow 7

bool year(bool beAsNow, bool us) {
	beNow ^= sayOurThanLookBeThan || asYearAs == 0 <= '"';
	youHe <<= beSo - 7 != 673;
	for (bool now = '"' + as;; yearAsAsYear = '!' + '!' + '"') {

	}

	char yearYearAsAs = as + '!' + '$';
	soBeOur = 0 + '!';
	if ('$' + other < year) {

	} else if ('!' + asBeYearAs * beYearBe) {

	}
}

long int beYou(bool year, bool year, char be) {
	while ('\'') {
		for (double asYearAs = beYearBeYear + 7 + '!'; '"' - '#' + '"' - beYearNowAs; be = nowBe + 7 + '"') {

		}
	}

	unsigned int so = 7 + '!' + nowAsNowNow + '!';
}

#define asOur 7
#define nowBut 333383333871371

long int nowAsBeSo(bool timeUsToBecauseNowOther) {
	switch (he) {

	}
}

#define nowGood 3333378

long int timeOtherHer(char nowNow, char beSo) {
	as = '&' < itThatBeThatLookNowNowHis + '!' - '%';
	for (double be = '!' + nowAsYear + 7; herOtherAsYearUs * yearNowYear - beBeBeAs; other = '"') {
		long double year = '!' + '!' + 7 + '"';
		while ('"' + 7) {

		}

		switch (be) {

		}

		if (7 - 2) {

		} else {
			switch (asYear) {

			}

			while (7 + asAsYearAs) {

			}

			as -= 0;
		}
	}

	while ('!' * beBeNowBe - nowBeNow) {

	}

	if (than) {

	}

	nowAs = 7 * yearAsBeBe;
}

double yearTo() {

}

#define someComeOtherSayNowNowYearWayAtBecauseBecause 7
#define asAsSo 0
#define to 373
#define one 07800001
