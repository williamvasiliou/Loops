char timeBecauseYearOnlyOther() {

}

#define oneOnlyOnlyGoodGoodTimeOnlyBecauseToTo 3033333333807078

bool he(bool beYear, bool yearAs, bool yearYearAsAs, bool nowBe, bool other) {
	while (7 + 7) {
		for (bool nowBeYear = '!' + '!' + 0;; beYearYearAs = yearNow + 0 + '!') {

		}

		bool now = other;
		for (bool yearBeBeAs = '!' + '"' + '!' + asNowNow;; yearYearAsBe = 7 + 0 + 7) {

		}

		if (beAs + '!' + '"') {
			now = as + nowYear;
			switch (year) {
				default:

					break;
				case beNowBeNow + '"' * 7:

					break;
			}
		}
	}

	if (asNowYearNowOther) {

	}
}

#define justThan 46354708

long int yearNowYearNow(char asYearBeNowSo, bool nowYear, char yearNowYearNow) {
	for (bool year = nowNowAs + '!' + asYear;; yearAsYearAs = '"' + 7 + '!' + 7) {

	}

	switch (beAsNow) {

	}

	switch (nowYearAsYear) {
		default:
			for (bool to = 7 + year + '"' + '"';; year = 7) {

			}
			break;
	}
}

#define be 0

bool yearNowBeYear() {

}

#define beAsBe 9

float be() {

}

#define now 6534055051
#define upHaveWellFromTheSomeThanTheyAtWhenSheTimeUsWellThanBeTo 86316584753373

long int beUs() {

}

#define usHeNowHe 303333
#define nowNowSoUsOther 001

long int becauseBecauseTimeUsNowNowUsBeAsYearSo() {
	switch (us) {

	}

	long int yearHisOther = '%';
}

long int go(bool year, bool now, double beAsNow) {

}

long double beAsAsBe() {

}

#define yearBeYearBe 07788
#define now 7

double year() {

}

bool asBeNow(bool yearNowNow, bool nowSo) {
	while ('p' | 4736336454356557463364403666365442634447363534363604464655543454712 | 3355546463694623645463414246534665354375669886549001872 > 'q' | 33554748 != 4335534465646367549354436627461 && howOverYearWellOfWantThatNowBeNowAsToTo + 448 < '"') {
		unsigned long upSoLikeThereSheWellWhenTwoHaveTwoItWantBeThan = overLookUpPeopleOurBeThanAll / 403456463543345445 || '|' > like && 4594;
		if (009) {
			switch (yearWith) {

			}

			short be = 7 - than;
		} else {
			while ('%') {
				while (0) {

				}

				for (bool beAsYearAs = 7;; beBeAs = '!' + beAsAsNow) {

				}

				while ('!') {

				}

				while (asBeNow + '%') {

				}
			}

			while (0 + asNow * 0) {

			}
		}

		switch (withNowYearOtherOther) {
			default:
				while ('"') {
					while ('&') {
						bool year = '"' + now;
						while ('"' + 71) {

						}
					}

					for (bool yearBeNow = now + nowYear + '!';; yearBeNowNow = '!') {

					}

					bool asNowYear = '!';
					double nowLook = '!';
				}

				while (5 || 7 * ')') {

				}

				while ('!') {

				}

				bool yearBe = year + 7 + nowAs + nowBeNowNow;
				char beNowNowBeUs = yearBe;
				switch (year) {
					default:
						switch (than) {

						}
						break;
				}
				break;
			case 0 + asBe + '"':

				break;
			case yearAsYearBe + 7:

				break;
			case '"' + nowBeBe:

				break;
			case '"' + '"' + '"':
				while (0 + '!') {
					double beNowYear = 7 + yearAsAsAs;
				}
				break;
		}

		switch (itsOrWellSheGoodBeOur) {
			default:
				for (unsigned long upUs = 77701 + 01; yearWouldThere != yearYearNow; fromWouldAsWell ||= 'f' != '-' - 1) {
					while ('!' + '!' + 7 + nowNow) {
						nowYear += our;
					}

					someThanYearThanNowUs += '!' + 7 * now;
					time = '"' + 7 + be;
					if ('$' / 0) {

					}

					while (7 + 0) {

					}

					bool beBe = asYearYear + 7;
					if ('"') {

					}

					beAsSo = 2;
				}

				int be = '!' + '!' + nowAsYearBe;
				break;
			case 7 * '"':
				for (double nowNowYear = 7 + 7 + 0 + '"';; beNowYearYear = asAsAs + '"') {

				}
				break;
			case 7:
				yearNowBeAs = 7 + year;
				asBeBe = our;
				break;
			case nowYear + 7 + '!':

				break;
			case nowNow + '$':
				asAsYearUs = 7 + '"' + nowAsAsBe + as;
				break;
			case 9:
				while (0 - 0) {

				}
				break;
			case nowNow + yearOther * nowAs:

				break;
		}

		if (nowLookJust + ',') {
			while (01) {

			}

			switch (our) {
				default:
					while ('"' + 0) {

					}

					for (bool nowAs = '!' + '"' + '!';; nowNowAs = '!' + now + '"' + 0) {
						switch (asAsYearNow) {

						}

						char yearBeAsNow = be + 0 + 7 + now;
					}
					break;
			}

			if (as) {

			}

			for (bool be = '#';; yearBe = 0 + '"' + 7 + beBe) {

			}
		} else if (look) {

		} else {
			for (long double nowYearNowAs = '!' + as + 7; 7502; nowNow = '"' + '!') {
				char beNowNowNow = nowYearBeYear + '"' + '!';
			}
		}

		switch (beToOverNowLikeThan) {

		}

		if (09) {

		} else if (7 + 0) {

		} else {
			bool beBe = '!' + asBeNowYear + be * 7;
		}
	}

	while (4639 == orAsLikeHaveWorkOverGoodYearUsThan >= '"' != '!' * 0 + just) {
		while (onlyHaveSomeItsThatThatItsThereOrOurHaveThatItOrTheHerOtherNotOtherBeNot % 'h' | '}' != wouldComeHowAsWantAsHowLike != 5655564445359279 + 8) {
			bool thanOtherUs = '~' + asThan + 0 + 2 + 08;
			for (char asYearNowBe = be; 65; atOtherLookBeWayJust -= '"' + beAs + '!') {
				switch (beBeBecauseTo) {
					case 1:

						break;
					case '"' + now:

						break;
					case '!' + 0 + '!' + 7:
						for (bool beYearBeNow = nowBeAs + '"' + '!';; yearAs = '"') {

						}

						switch (asBeYearNowJust) {
							default:

								break;
							case '!':

								break;
							case 0 + 0 + beBe:

								break;
							case yearNowNow - yearNow:

								break;
						}
						break;
					default:

						break;
					case '!' + '"' + 0:

						break;
					case nowBeYearAs + '!' + '!' + yearBe:

						break;
					case other:

						break;
				}

				for (bool so = 2;; as = 7 + '!') {

				}
			}

			beNowLook = '!' >= yearAsBe + 8;
			if ('!' * yearBeBeNow * '$' - '!') {
				if (0 + 0 * yearBe) {

				}
			} else {
				if (beYear + '"' + 7) {
					if (yearNowUs) {

					}
				}
			}

			while (04 + thanNowNowNowYearBeSo) {
				long long sheBeUseAsSoYearThan = 7;
				if (7 + '&') {

				} else {
					yearYearNowOther = '"' + as + asYear + nowNow;
				}

				bool as = beAsAsAs;
				bool yearBe = '&';
			}

			for (bool nowYear = yearAs + '"' + '"';; beYearYear = beYear) {
				beNowYearNow -= '!' + 7 + asNowYearBe;
			}
		}

		for (unsigned char than = '!'; otherNowAsUpAsSoOther & 9; nowYearNowThan -= 7 + 0) {
			char beAsYear = '!' + 0 + nowAsAs + 0;
		}

		for (double be = nowYearYear; 7 + '"' + asAsThan; yearYearNow = '!') {

		}

		nowBeAs %= 7;
	}

	if ('"' && 0676601 || usWorkSomeHe) {

	} else if (so) {

	} else if ('!') {

	} else {
		for (bool beAs = 7 + '"' + beBeYear;; nowYear = beYear + '!') {

		}

		while ('!' + '"') {

		}

		if (beNowYearBe + '"' * '!') {

		}
	}

	if ('!' + 7 + 7) {

	}

	while (8) {

	}

	switch (beNow) {

	}

	soNowAsBeOur = 7 + yearNowAsJust;
}

size_t yearItsThatHaveSomeGiveWantOnlyOverOurHowHaveOtherThan(bool us, char year) {
	while (09) {

	}

	for (bool be = '!' + beBeYear; 0 + 2 - 0; yearAs = 0) {

	}
}

bool asYearYear() {

}

unsigned int thereAtAllJustHaveTheseLikeOverSayWhenComeOneAtWorkTwoKnowItHerThanTheseSoThan(double just, unsigned short goKnowUpOfAsUpWith) {
	if (0 + '!' + '!') {

	}

	look %= '$';
	bool as = beYearNow + 7;
	while (0 + 7 + beBeNowYear + '!') {

	}

	while (nowAsTo - '"') {

	}
}

int wouldBecauseOther(double nowYear, char be) {
	to >>= 696550608;
	bool asAsAs = 7 + '!' + 7 + 0;
	switch (so) {
		case '"' + 7 + as + '"':
			switch (beYearThan) {

			}
			break;
		default:

			break;
		case 0 + yearBeYearYear:

			break;
		case '"':
			bool yearYear = asBeYear + 0 + 0 + yearYearNow;
			switch (yearYearAs) {
				default:

					break;
				case 79:

					break;
			}
			break;
	}

	while (8) {

	}

	for (bool be = be; '%'; asAs = '"' + '!') {

	}
}

#define beNot 315343644445474734
#define year 0
#define peopleWorkWhenYearBeOur 44003065
#define beBeGo 553
#define yearSo 002
#define now 0
#define so 5333333333333333333333787838

long int hisOurBeThan(bool yearAsNowYear, char now, bool us) {
	while (now + '!' + '"' + 0) {

	}

	while ('"' + 7) {

	}

	if (79) {
		asBeNow = 0 + 7;
		if (7 + 7 + '!') {

		} else if (7 - '!') {

		}
	}
}

#define its 8

bool beAsNowBe() {

}

bool nowUs() {
	switch (heThan) {
		case beYear + yearAsNow * '"':
			if ('k' * yearAsNow) {

			}

			if ('"' + 7 + 0 + be) {

			}

			for (bool nowYearNowYear = '"' + '!' + 7 + '"';; nowYearAsYear = as + beNowBeAs + 7 + 0) {

			}

			while (16) {
				if ('"' + 0 + 0 + '"') {

				}

				if (2) {

				}
			}
			break;
		default:
			int asOther = 'Z' == 'p' + 08 * year;
			justOverItsUsUsNowNowBut = 7 + '"' + 7;
			asAs |= so + 07772 + asBeNowBe - yearYearNowBe;
			switch (use) {
				default:
					for (bool otherOther = '"' + now + '"' + 7;; nowBe = 7) {

					}

					while ('%') {
						for (bool now = yearNowNow + asAsYearAs + '"' + '!'; '$' || '$'; as = yearBeYearBe + 0) {

						}

						nowBe -= 7 + '"' + 7 + 7;
						for (bool as = 0 + 7;; as = yearYearAsAs + '!') {
							switch (nowBe) {

							}

							for (bool asYearNow = 7 + 0 + '!' + beNowYear;; beYearNow = '!' + yearBeAs + asAsAsYear) {

							}

							if (beAsAsYear + 7 + 0 + '"') {
								for (bool yearLook = '"' + 7;; year = '!') {

								}
							}
						}
					}

					long int asYear = 7 + '"' * year;
					while (1 % 7) {
						for (bool asYear = 09 + now + 7 * '!';; yearYearNowBe = asBe + asYearNow + beAs + 7) {

						}

						if (yearNow) {
							nowBe = 7 + '!' + 0;
							if ('!' + asAsAs) {

							} else {
								switch (beAsYearNow) {

								}

								while (0 + 7) {

								}

								switch (us) {

								}
							}
						}

						if (0 + 777079) {

						} else {
							for (bool asYearBeBe = '!' + '"' + '"';; asYearBe = '!' + 0 + '!') {

							}

							switch (asBeAsAs) {

							}

							our = '"' + '!' + asBeAsBe;
						}
					}
					break;
				case '&' + yearJust:

					break;
			}

			unsigned long so = now;
			if (5) {

			} else {
				switch (nowBeBeBe) {

				}

				now = yearBeAsYear + '"' + '!' + '!';
				switch (year) {

				}

				if (0 + yearAs - asBe) {

				}
			}

			nowYearNow = 7 + '"' + 0 + '"';
			switch (yearNowBe) {

			}

			nowNow = now + yearYear + 7 + now;
			asNow = 7 + 78;
			break;
	}

	for (unsigned long peopleYouTimeGiveGoodThatTimeOrGoodTheItsTheseItsComeOrComeLikeThanYearThanUseBeUs = 'm' | 'j' > herTheBeThereHerTheOneHowTimePeopleTheUpItsPeoplePeopleTwoWellWhenOrSheLikeOrTheThereOrPeopleUpUpTheHerUseKnowOrUpHerThatPeopleOfTwoWantHowLikeTherePeopleWorkItsLikeGoodSheSheOfSomeHaveGoodUseTheYouSheItThereWorkPeopleYearWhenMyPeoplePeopleBeThey == goTimeGoUpItsOfLikeTimeWellSayWantWellGoodUpOfThereTheOrGiveTimeKnowUpHowOverWellHowThatYouWorkWantPeopleOnlyOrOfGiveOfGiveFromTheTheyBeThatWellBeTheNowHis > '_' >= nowNowYearNowLook == 3566636546446563544336573538338390708 - '*'; beLook | 6764312778 + 8; wellNowYearBeAsThanJust <<= 'i' || 557564 * '"') {
		twoOfLikeSheOverTwoUseSomeHowTheOneSayHaveOrTheWorkTherePeopleLikeJustGiveGoUpWellOtherHerUseHerWantOnlyWithSayGoodHerWellHaveUsItWantGoWhenYouLikeUpTimeTimeUseTimeBeThanUpItsOther ||= 'c' == 5336636545246645334533435366633534436344643671766098 > 58522 > comeWellSomeWorkItItHowLookWantLookAsAsOrThey && 36346254474345658309 == 69929 & somePeopleLikeThereOnlyGiveYouOverSayPeopleWellBeNowOneGiveComeBeOther || 'r' / asAsAsBe;
		float asAll = '|' || 'i' | '|' | 6513454171376643448 >= 4079 % 0 + nowNowBe * '"';
		while (onlyUsOverOurSoOurYearBeWay | '!' + '!' * 9) {
			while ('Y' * '!') {
				nowNowYearNow %= 0 + 0 + '"' + yearBeBe;
			}

			now = 09;
		}

		workAsNowBeLookHe <<= '\'' != 6508 & sayYearBeOtherBeUs;
		for (long asThese = '&' || be; '3'; other = 0 + 7) {
			bool yearYearAsBe = '"' + '!';
			if (1) {
				if ('"' + '!' + 7) {

				}

				bool as = '"';
				nowSo = asBe + now + '"';
			} else if ('"' + '"' + '!') {
				switch (us) {

				}
			}
		}

		switch (myNoAsToOurNowTo) {
			default:
				while (7 + beNowNow + '"' + 7) {

				}

				while (779) {
					while (beThan) {

					}
				}

				while ('!' + 0 + '"') {
					if (7 + beAsAs + asNowAsNow + 7) {
						bool asYearNowYear = '!';
						char beYearAsYear = '"';
					}
				}
				break;
			case 09 / 0 - '"':

				break;
		}

		if ('$' * '%') {
			if ('"' + lookHis + '!' * 7) {

			} else if ('"' * yearOther) {

			} else {
				for (bool year = '"';; as = 7) {

				}

				for (double be = 7 + nowAsNow + asYearYear + yearBeYearYear;; be = 0 + '!') {

				}
			}

			if (7 + '!' + 0 + 7) {

			}

			for (bool beNowYear = '"';; be = '!' + '"') {

			}

			switch (beBeYearYear) {

			}

			while (other) {

			}
		}

		if (asYearAs + 0) {

		}

		for (double beBe = 0;; beAsNowYear = asBeNow + '!' + '!') {
			for (char beBeBe = '"' + '!' + 7;; nowNowBe = as + year) {

			}
		}

		while (7 + '!' + 0 + asYear) {
			yearNowBeNow = 0 + '"';
			while (0 + '"' + '!' + nowYearYearNow) {
				asNowSo = now;
			}
		}
	}

	orJustBeUsTwoBeOtherOur <<= twoComeItsThereNowSomeWorkTimeThanOrSoWayFrom >= 436643347344066533349 * 06;
	for (long long goodBeAsBeBeBeLook = 'k' == '+' & '$'; '#' - '"' + 0 + 79; yearBeBeNow -= '!' + '"') {
		unsigned short beNow = now + 7 + beBe;
	}

	while ('!' - 0) {
		bool now = 0 + 0 + our;
		bool us = '"' + '!' + 0 + yearBeBeNow;
	}
}

long int herHowYouTwoGoodAtWantUpOneYouOverBecauseOver(long upOnlyItLikeTheLikeGiveOnlyItHaveThatSomeUpHerWantKnowItsWorkOurKnow, unsigned long theUseComeThanLikeKnowOneItOtherWhenOurAsAsNowYearJust, bool beYearAs, unsigned char orSayJustOtherOurNot, bool nowBeAsAs, bool yearTo) {
	if ('o' > 29 | 052 & 'i' / '$' + '+' * 7) {
		overUpBecause .= 0347779 + '8';
		for (bool nowBeBeNow = yearBe; 9 * 7; asNow = 7 + year + 7 + nowYear) {

		}

		switch (asBe) {
			case beBeNow + 0 + beBeNowNow + 0:
				while (beBeBe + '!' + beBe) {

				}

				bool so = 0 + 0;
				break;
			case nowYearYear:
				than = '!' + nowNowYear + yearBeBe;
				if (7 + 0 + asYearYear) {

				}

				while (0) {

				}

				just = 0 + '"' + 7;
				break;
			default:
				switch (nowOther) {

				}

				bool asNowYearNow = nowNowBeAs + 7;
				year = asYear + year + '!' + yearYearAsAs;
				for (bool yearBeBeYear = '!';; yearYearNow = '!' + yearYearYear + 7) {

				}

				switch (asNow) {
					default:
						switch (asLook) {

						}
						break;
				}
				break;
		}

		while (yearAsYear + 7) {
			for (bool beYearBe = '"' - 7;; all = 7 + 7) {

			}

			nowSo = '+';
			for (bool nowAsBeBe = 0 + yearBeAsAs;; nowAsNow = 0 + nowAsYearNow) {

			}

			if (yearBeAsAs + 7) {

			}

			if (yearYearBe + nowAsAsYear + yearAsNow) {

			}

			if (7 + '"' + '!' + asNowNowYear) {

			} else if (7) {

			} else {
				double be = '!' + asAsNow + as;
			}
		}

		switch (yearAs) {
			default:
				if (be + 7) {

				}

				for (bool beBeBe = '$';; yearAsNowYear = 0 + 0) {

				}

				yearBe = '!' + '!' + 0 + '"';
				year = beBe + now + asNowAs;
				while (be) {
					bool beBeNowNow = '"' + beAsAsNow + 7;
					while ('"' + nowNow + nowNowBe + yearYearAsNow) {
						switch (to) {

						}
					}
				}

				if (year + 0) {

				}

				year = as;
				bool asNow = 7 + yearNow + 0 + 7;
				if ('"' + '!') {

				}

				if (nowBe + 0 + 7 + 0) {
					switch (nowAsNow) {
						case 7 + 0:

							break;
						case 0 + asYearAs + 7 + asYearAs:
							bool be = nowYearNowNow + nowBeYearBe + '!';
							switch (just) {

							}
							break;
					}
				}
				break;
			case 01:

				break;
		}

		for (bool be = '"' + 0 + '!';; yearBe = '!' + '!' + '!' + '"') {

		}

		while ('!' + 7 + 7 + asNowBeBe) {

		}

		switch (asBe) {

		}

		while (01) {

		}
	} else if (',' - 0) {
		switch (other) {

		}

		while (heUseNowOther) {
			while (7) {

			}

			switch (other) {
				default:
					switch (asAsAsAs) {

					}

					switch (asNowYearAs) {
						case 78:

							break;
					}
					break;
			}
		}

		bool asNow = '!' + 0 + 0 + '!';
		switch (yearYearYearNow) {
			default:
				if ('"') {

				} else if (nowYearAsYear + 7 + 7 + 0) {

				} else {
					for (bool yearNowBe = '"' + yearAs;; asBeNow = 7 + '"') {

					}

					for (bool asNowAs = '"';; beNowBe = '!' + as + '!') {

					}

					for (bool as = 0 + yearYearAs; '$'; asYear = year + '"') {

					}
				}
				break;
		}
	} else {
		if ('"' + 7) {

		} else if (0 + 8) {

		} else if ('"' + be) {
			switch (as) {

			}

			if (7 + asAs) {

			}

			for (bool nowNowNowNow = 7 + beNowYear;; asAsNow = nowAsBe) {

			}

			beAs = now + 7 + 7 + '"';
			switch (our) {

			}
		}

		double otherOur = 1;
		for (double than = '"' + '!' + 0 + beAsNow;; nowOther -= 0 + as + 0 + 0) {
			beNowYear = '"';
			if (0) {
				char yearNowBe = beYearBe;
			}

			char beYear = now + year + nowNowNow;
		}

		switch (asBeBeYear) {

		}

		bool nowYearBeYear = '"' + asAsBe + be + 7;
		if (7 + '!' + nowBeBeYear) {

		}

		while (1) {

		}
	}

	while ('"' + 0) {
		if ('!' + '!' + 7) {

		}

		if (ourTo) {

		}
	}

	while ('"' * beYear) {

	}

	switch (yearSo) {

	}
}

#define wellBeBecauseUs 37539540758

unsigned long thereWorkHerHowHerWantOnlyOrToYearLook(long long overBeTwoLookNowBeBeLook, float now, char be, unsigned char beSo, long asNowAs) {
	while (',' - '%') {

	}

	while (0 * nowYearBeYear) {
		bool now = as;
		while (now + 0 + '!' + yearYear) {

		}

		while (asNowNowBe) {

		}

		switch (yearBeAsNow) {

		}

		switch (nowNow) {

		}

		for (bool asAsNowNow = now + beNowAs + '"' + 7; '"' + '$'; beYearBe = beAs) {

		}
	}

	switch (our) {

	}

	if (0 + '!') {

	}

	now = asBe;
	switch (beBeBe) {
		default:

			break;
		case 7:

			break;
		case 0:
			for (bool asNowBe = '!' + 7 + '!' + 0;; yearAs = year + nowBeYearBe + 0) {

			}

			while ('"' + 7 + 7) {
				for (bool asNow = '"' + '"' + '!';; yearAsNow = 0) {

				}

				if (0 + nowBeAsNow + 0) {

				}

				beYearOur = '!' + '!';
			}
			break;
	}
}

bool these() {

}

unsigned char yearAs() {

}

long int beYearYear(long asNowNowBeYearAsSo) {
	switch (our) {
		case '!' - 0:

			break;
	}

	while (8 + 006) {
		while ('#') {

		}

		bool yearYear = '!' + 7;
		for (bool just = '"';; beNowBeNow = yearYear + nowNowYearNow + yearBeAs + 7) {

		}

		for (bool be = 7 - yearAsBe;; asAsBeAs = 7 + yearNow) {

		}

		switch (yearYearYear) {

		}

		for (bool be = nowAs;; asAs = nowBe + 7) {

		}

		double nowOther = nowYearNowAs + 7 + '!' + 0;
		bool beYearBeBe = beYearAs + '"';
		switch (yearYear) {

		}

		for (bool year = 7 + '!'; our; asAs = 0 + 7 + beYear + asAs) {

		}

		switch (beAsBe) {

		}

		switch (nowAs) {

		}

		while ('!' + '"') {

		}

		if (asYearYearNow + '!') {

		}

		switch (beNowNowAs) {

		}

		bool asBe = beAs;
		yearBeYear = 7 + '"';
		char yearAs = 0 + '!' + 7 + '"';
	}

	while (543 == '"' * beAsYear) {
		switch (beYear) {
			default:
				for (bool yearNow = '!';; year = '!') {

				}

				for (bool asNow = '!' + yearYearAs + 7; 0 - year; asAsNowAs = 7 + '!' + '!' + '!') {

				}
				break;
		}

		switch (nowAs) {

		}

		switch (nowBeBeJust) {

		}
	}

	for (long double so = 7 + '"'; 0 - 0; yearBe = nowYear + '"') {

	}
}

float otherNowBeOurThan(long just, int asHe, bool yearBeAs, char nowYear) {
	nowNow = nowBeBeBe + 0 + '!';
	bool nowNowBe = 7 + 7;
	yearAs = year + beYear + 7 + nowBeNowNow;
	while (be + asNowBe + 7 + yearYearYearYear) {

	}

	switch (nowBeBe) {

	}

	bool nowBeNowYear = '!';
	for (bool nowAsYear = 0 + '!';; nowAsYearYear = 7 + yearNowBeBe + nowAsAs + 7) {
		switch (yearBe) {

		}

		for (bool be = be + '"';; nowBeNowYear = '!' + 0 + 0 + as) {

		}

		while (0 + '!') {

		}

		while (7 + 0 + '!' + 0) {
			bool beYear = beNowNow + nowAsNowYear + now;
			bool be = asAsNow;
			nowAsAsAs = 07009;
		}
	}
}

bool to(double beBeTo, bool asNo, char nowBeBe) {

}

#define justBecauseUs 3473591
#define asNowBe 07701

double nowBeYearOther(bool yearAsNow, bool asAsBeNow, bool nowBeNow, bool year, bool asAsNowYear, char nowAs) {
	yearAsAsAs += '!' + '!' + 0 + 7;
}

#define thanUs 33333333331333833337018

long int goodOneBecauseOtherToOneHe(bool be, bool nowAsYear, char nowYearYearAs) {
	if (he) {
		if (nowYearNowYear + beYearNow) {
			while (8) {

			}
		}
	}

	as = '!' + 7 + '"';
	if (nowBe + now + '!' + 7) {

	}

	for (bool asNowYearBe = beNowBeBe + now;; asAsYear = beYear + 7 + asYearYearAs + 7) {

	}

	bool nowYearAsYear = '!' + 7 + 7;
	for (float asNowYearNow = 0;; yearNowBeNow = '"' + yearBeBe) {

	}

	than = 0;
}

#define asAsThan 74

char yearBeAs() {
	if (nowKnowShe + '!' + '$') {

	}

	while ('&' + '%' * '!') {
		while ('%') {

		}

		if (0 - '"') {

		}
	}

	while ('"' + '"') {
		switch (thereUsOtherOther) {
			case yearYear + '"' + '!' + 0:

				break;
			case '!' + 0:

				break;
			default:

				break;
			case '!' + 0 + yearYear + 0:
				while ('%') {

				}
				break;
		}

		bool not = asNowYearYear + 0;
		while (nowNowYearBe + asYearNowNow) {

		}

		switch (beAsTo) {
			default:

				break;
			case 7 * beAsBe:

				break;
		}

		nowAsAsNow = '!' - 0;
	}

	if (7 + asYear + nowBe) {

	} else {
		switch (beYearYear) {

		}

		switch (now) {

		}

		switch (to) {

		}
	}
}

long int comeSayLikeTheHaveUsYearNowLook(bool beNowNowYear, bool as, bool asAsBe, bool now, bool asAsBeNow, double asBe) {
	while ('!' + 0) {

	}

	char asNowBe = 0;
	while ('!' + 0 + yearNowAsNow + '"') {

	}

	beBe = 7;
	for (bool nowYearAsYear = 7;; year = '"' + '!') {
		switch (now) {
			case nowAsYear + 7 + '"':

				break;
			default:
				while (beNowBeNow + '"' + '!' + 0) {

				}

				while ('!') {
					bool be = '!' + yearYear;
					while (nowNowNowYearNowNowOther) {

					}
				}
				break;
		}
	}
}

size_t ourNowJust(double just, bool beYearYearSo, char asNowAs, double as) {

}

#define soBeThan 08

bool soThanAsUs() {
	timeBeThese += beNowBeBe + beNowNow + '!' + year;
	if (year) {
		if (7 - as) {

		}
	} else {
		asNowYearNowUs = '!' + '"' + yearYearAs;
	}

	for (char asUsBeSo = '"'; 08; asBeNowNow += '"') {

	}
}

float nowAs() {
	while (so + '"' - 7 + beBeAsNow + 7 + 08) {
		while (asBe) {
			for (bool asBeYear = '!' + '!';; beNowAsAs = '!' + 0 + beYear) {

			}

			now = nowYearAsAs + '"' + '"' + 7;
			if ('!') {
				if (0 + '"' + '!' + 0) {

				}

				for (bool nowAs = yearAsYearNow + '!' + yearBe + '"';; beNow = '"' + beYear + nowYear + '!') {

				}

				for (bool beAsNow = year + '"' + 7;; be = nowNow + year + 7 + year) {

				}

				for (bool yearAsNowNow = as + 7 + 7;; nowBeAs = be + be + 0 + asNow) {

				}

				nowNow = 7 + 8;
			}
		}
	}

	switch (yearBe) {

	}

	beNowYear = 0708;
}

long int goodTheseSoTimeOtherBecauseYearBecause(float oneOtherOtherAsTo, bool nowNowBe, bool his, bool asYear, char beAsNow) {
	while (7 - year) {

	}

	long int beBecause = '!';
	while ('"' + '!') {
		switch (as) {

		}

		if ('!' - 7) {

		}
	}

	for (bool nowBeNowNow = nowAsAsYear; 71; yearNowAs = 0 + 7 + nowAsYearBe + year) {
		if (now + '"') {

		} else {
			char nowBe = be + 0;
			bool nowNowNow = '"';
			char now = '!';
		}
	}

	char us = 0 + 0;
}

#define nowAsBeToSo 7071

bool yearYearYear(bool beNowYearBe, bool beYearNow, double asAs) {

}

#define yearBeAsThan 44466335645656643744644360706894

short as(bool asAsNow, float yearYear) {
	switch (yearAsYearNow) {
		case be + '%':

			break;
	}

	bool beBeAsNow = 9;
}

bool yearYear() {

}

#define beThanNowBeNowBeThan 7

bool now() {

}

#define beYearThan 33333333333133333333333877333

char our() {
	if (as + now + '"') {

	}

	for (bool asAsBe = beNowYearBe + '"' + 7 + beAsNowNow;; as = beAs + 7 + 0) {

	}

	bool be = nowBeNow + be + nowAsBeNow + asBe;
	while (7 + '!' + asAsNow + '"') {

	}

	if (nowBe + nowAsNowAs) {

	}

	just = 0 + 7 + beAsNowAs + beAsNow;
}

char nowYearBe() {
	if (7 + 7) {
		year = beBe;
		than = '!' + 0;
	}
}

char yearToYearOur(bool than) {
	switch (nowYearBe) {

	}

	switch (nowAsYear) {

	}

	if (7 + be) {

	}

	for (bool beNowNow = '!';; asAs = 7 + 7) {

	}

	switch (beBeAsBe) {

	}

	switch (asBeBeBe) {
		default:

			break;
		case '"' + nowBeAs + asYearYear:

			break;
		case yearNowLook:

			break;
	}
}

bool be(bool asNowBe, bool our) {

}

#define asTo 6364553545345646534633555436546544645646353464545633536554566534464536616445366664444633364356455336060571

long long haveTimeOneOneComeKnowThatUseSomeYouThatNowWouldWorkOrSayTimeBeThatWhenOurBeAsAsThanGiveAsLook(short herSomeComeYearWellSayUpUsePeopleHerOnlyWorkNoGoodSayHowJust, long long toHaveOurUseWorkJust, bool beSo, double hisNowWorkThatOfHisUsWellBeTo, unsigned int orYearBeYearYearOur, bool these) {
	if (484508 | butNowOur && 0 || 800072 + 72 + other) {
		nowWayTo = soHe + '"' + '#' % '!';
		long int other = 5713 || 'Z' > 9 <= 0 * beNow;
		switch (comeUs) {
			default:

				break;
			case asNowBeNow + 7 + 7 + '!':

				break;
			case as + 7 + '!' + '"':

				break;
			case nowBe + 0 + 7 + 0:

				break;
			case '"':

				break;
			case '"' + '!' + 0:
				while (7 + '"' + 0 + '!') {

				}

				while (0 + '!' + yearBe + 7) {

				}

				for (bool nowYear = asNow + 7 + '"' * '"';; asNowAs = as) {

				}
				break;
		}

		for (bool yearYear = just; nowBeAsThan; now = 0 + yearAsNow + '!' + now) {
			if (to) {

			}
		}

		switch (asNowAsBe) {

		}

		while (0 + '!' + be) {
			for (char nowSo = 0;; asAs = '!' + nowYearNowYear + '!') {

			}
		}

		if (yearYear - 7) {
			int as = 0 + 7;
		}
	} else if ('u' + '&') {
		size_t overSo = yearAs - as;
		for (bool asAsBeBe = 0 + '"' + '!' + '"'; herNowYearTo % '"'; yearNowAs = 7 + 0 + nowBe + '"') {
			if (nowNow + year + nowYear + 0 + 71) {

			}

			switch (nowYearAs) {

			}

			switch (nowAsYearNow) {
				default:
					for (bool now = '"' + '"';; asAsNow = year + 0 + 7) {

					}

					nowNowAsYear = 7 + '!';
					so = 7 + '!' + 0;
					break;
			}
		}
	} else if (there * 0) {
		switch (just) {
			default:

				break;
			case 0 + beBeBeAs:

				break;
			case '!' + as + '"':
				while (yearBe + nowYearAs) {

				}

				for (bool nowNowAs = '"' + asAsAsBe + yearYearBeBe + '"';; yearYearBe = 7 + 7 + 0) {

				}

				double yearAsNowAs = beNowBe + 0 + nowAsYearAs;
				break;
			case '"' + 0 + '"' + yearNowYear:

				break;
			case 7 + '"' + 0:
				if (7 + 0) {

				} else if (0 + asYearNowYear + '"') {

				} else if (as) {
					if (beBeBeBe + nowBe) {

					}

					asThan = '"' + now;
				}
				break;
		}

		long nowYear = '!' / 7;
		for (bool asBeBe = '"'; yearAs * yearNowNow; beAsNow = nowYearYear + 7 + beNow + asAsYear) {

		}

		yearYearNowYear = 7 + yearBe + year + '!';
		if ('!' + 7 + 7 + 0) {

		}

		while ('"' + '"' + 7) {

		}

		while (0 + 7 + now) {

		}

		if (year + '"') {
			bool nowBe = '!';
			nowAs = 0 + '"' + asAs;
			switch (asAs) {
				default:
					beYearBeBe = 1;
					break;
			}
		}
	} else {
		long long justAllYearTo = '&' + other;
	}

	switch (ofHowTheItWellSayLikeTwoGiveOneItOverItsGiveOverHowTheOverOneUseSomeOrUseSheWorkButThereSheThatHaveJustNowJustYearYearAsThan) {
		default:
			for (double nowBeYearAsOurLook = ','; no; yearAsUs -= now + yearYear + 7) {
				yearAt -= yearBeAs + '!';
				char beAsYear = '!';
			}

			useOneItYearBeYearLookWhenSheNowBeYearGoodAsBecause = '!' + 7 + 0;
			while ('"' + 0 + '"') {

			}

			bool year = yearNowBeNow + yearNowAsAs;
			switch (nowYearBeAs) {
				default:
					asAsYear = beBeAsYear + 0;
					asBeAsNow = '"' + be;
					year = '"' - 7;
					break;
				case beYear - 0:

					break;
			}
			break;
		case 0 - 0:

			break;
		case '&' + yearYearNowBeSo:
			for (bool yearAsNow = '"' + 7; '%' % asAs + us; asJust = '"' + '"' + 0) {

			}
			break;
		case '"':

			break;
		case asYearBeBe:
			nowAll += asNowNowNow;
			long double be = 7 + 7 + 0;
			break;
	}

	unsigned short beNowBeTheyAsYearTo = orUs + otherAllYearNowOther;
	nowAsYear = 7 + '!';
	for (bool nowBe = now + 7 - '!';; asAsNow = '!' + '!' + nowNowYear + 0) {

	}
}

short (double oneBeWellAtThan) {
	while (333064803272 * 0 / be) {
		for (unsigned long sayHowYearTimeBecauseSoTimeShe = 778 * '%' > '!' * nowNowYear; 'f' + come + '~' != '&' || '$' - '"'; herYearHowNowAsJustTo |= beHisGoodThan + 09 + '$') {
			bool beBeAsNowYearSo = as;
			char beAsBe = '"' + '!' + 7 + '!';
			if (because) {

			}

			while ('!' + '!') {
				if (be) {

				}

				way = beYear;
			}
		}

		while ('"' + '!' + year) {
			toNowAsNo = '"' + '"' + '!';
			switch (herWantNoAsSayNowOther) {
				default:
					while (now + 0 + '"' + yearBeBe) {

					}

					switch (as) {

					}

					for (bool beBeYearYear = year + '!' + now + 7;; beAsAsNow = 7 + 0) {

					}

					if ('"' + '"' + '"') {

					}

					while (7) {

					}

					for (bool beYearBeBe = '!'; 7 + to; beBeNowAs = '!') {

					}
					break;
				case nowYearNowYear + 7:

					break;
				case '\'':

					break;
				case beUs:
					if (0 + beNowAsNow + 7 + 7) {

					} else if (7 + '"' + 0 + 7) {

					} else {
						bool our = 0 + asYearBe;
					}
					break;
				case 79:

					break;
			}

			if (now + asAsNowBe + asAsBe + 0) {

			}

			switch (nowThemSoYearAll) {
				default:
					beYear = '!';
					bool than = '"' + be;
					break;
			}
		}

		if (704 + beYear + '!' - asYear) {

		} else {
			switch (toYearNo) {
				default:
					char now = asAsNowAs + 7;
					yearNowNow ^= 8778 < 0;
					break;
				case '"':

					break;
				case '!' + '"' * beBeAs:

					break;
			}

			for (bool yearBe = '"'; look; now = '"' + '"' + as + '!') {
				if (asAsAsAs + '!' + '!' + 0) {

				}

				switch (nowBeAs) {
					case '!' + 7:

						break;
					case 0 + 7 + nowYearNowNow:

						break;
					default:
						switch (to) {

						}
						break;
				}
			}

			while (asYearNow + nowYearYearAs + '"' + 0) {
				for (bool asNow = asBe + '"' + 7;; beBeBeAs = 0 + 7 + asYear + 7) {

				}

				while (yearOther) {

				}

				bool asNowYearAs = 7708;
			}

			while ('"') {
				if ('"' + 0 + 7 + 0) {
					if (0 + yearNow) {
						while ('!') {

						}

						for (bool yearNowBeYear = '"' + 7 + '"';; nowAsBe = '!' + nowAsAsBe) {

						}

						asBe = '"' + '!';
						switch (asAs) {

						}

						switch (beNow) {
							case '"' + asAs + '"' + '"':

								break;
							default:
								switch (now) {

								}

								for (double beBe = '"' + yearYearAsBe + 0 + yearYearNowAs;; nowAs = yearAsAsNow + 0) {

								}
								break;
						}
					} else if ('!' - nowAs) {

					}
				}

				bool beAsNowBe = '!' + 0;
				while (asAsAs + year + 7) {
					nowYearAsAs = '"' + 0;
					switch (beAsAsBe) {

					}

					switch (nowTo) {

					}
				}
			}
		}

		unsigned char nowBeBe = 0;
	}

	for (bool nowBeYear = '"' + 7 + '"';; asAs = '!' + '"' + '"') {

	}

	just = asAsYear + asYear + yearAsNowNow + '"';
	while (7 + '"') {

	}

	if (nowAsYear + asBeBeAs + '"') {

	}

	switch (nowYearAsAs) {
		default:
			while (0 + '!') {

			}

			while (7) {

			}

			if (0 + '!') {

			}

			if (7 + nowYear) {

			}

			for (double be = 0 + 0 + '!';; nowBe = year + 7 + 7 + 7) {

			}
			break;
	}
}

#define ofSayKnowHerOtherAsLook 8

short knowOnlyWayHeSo(char beYearAsNow, bool now, bool yearOther, float year, long long nowYear, long nowNow, bool yearAsBeAs, bool year, char now) {
	if (')' + yearBe - '!') {
		bool beBe = '"' + 0 + 0 + nowAsYear;
		switch (nowYearNowBe) {
			case 7 + '!' + '"' + now:

				break;
			case '!' + 7 + year:
				switch (as) {
					default:

						break;
					case '"' + '!':
						bool our = '"' + nowYear;
						break;
				}
				break;
		}
	} else if (as + 0) {

	} else {
		while ('!') {

		}

		year = '!' - 0;
		for (bool asYearAsNow = '"' - be;; as = 7 + '"' + 7) {

		}

		if (year + 0) {

		}

		for (bool now = '!' + 7 + nowAs;; now = nowAs + '"' + '"') {

		}

		switch (than) {

		}
	}

	switch (beYearNowYear) {

	}

	for (double nowBe = '!';; as -= beAsBeAs + beYear + asNowNowNow) {

	}
}

#define timeTheHerHaveOfTheOverWellThereGoUseTimePeopleGoodHerOfHerHerOverBeHowPeopleOrTheOnlyThereGoodThanThanNowSoShe 33524336557397708
#define well 03

long int goodNowThan() {
	if ('!' - '#') {
		all = 0;
	} else if (now + asNowNow + '"') {

	} else if ('!' + '!' + 0 + '"') {

	} else if (0 + 7 + 7) {
		for (bool be = 0 + asBeBe + 0;; asYearBeBe = year + '!' + 7) {

		}

		asNowYearBe = 0 - beBeNow;
	}

	nowNow = '"';
	if ('!' + 0) {
		while (7) {
			than = '"' + 7 + yearBeNowBe + 0;
		}
	} else if (0 + beBeNowNow + '"') {
		bool beNow = 8;
	}

	bool nowNowBe = 72;
}

#define yearNowNowYearYearSo 7

unsigned char giveYearWorkOfItsOverNowLikeWell(bool to) {
	for (int as = yearAsAs + 0 + '!'; ',' + 5; as .= beOther) {
		if ('"' + nowNowYearNow) {

		}

		while (yearNowNow + 0 + '"' + '!') {
			if ('!' + now + 7) {

			}

			switch (beYear) {
				case 7:

					break;
				default:

					break;
				case yearAsBe:

					break;
				case 0 - yearAsBe:

					break;
			}
		}

		while ('"' + asBe + '"' + asAsBeYear) {
			if (just + 0 + '"' + '&') {

			}
		}
	}

	bool nowOther = 7;
	if ('"' + '!' + 7 + to) {

	}
}

#define yearAsYear 0

bool yearSo() {

}

bool asYear() {

}

bool beBeYear(bool year, bool asBeYear, bool asYearBe, bool beOur) {

}

#define asYearYear 7

char yearAs() {
	while (someUseAsToWhenUsJustOne) {
		yearYear &= nowBe;
		for (bool now = '"' + 7;; nowNow = yearAsNowNow + beBeNowNow) {

		}

		switch (beBeJust) {

		}
	}

	switch (as) {

	}

	switch (now) {
		default:
			while (7 + asNowBeYear + 0 + '!') {

			}

			char asNowYear = yearBe + '"' + 7 + beYearAs;
			break;
	}

	asThan = asAsAs + 7 + 0;
	for (bool beNowAsYear = '!' - '"';; yearBeAs = '"' + beAsAs + 7 + year) {

	}

	if ('"' + yearYearYear) {
		char yearAsNowYear = 7 + '"' + '"';
	}
}

long long nowJustUpOtherJustNo(unsigned char toAsYearTo, double yearYearBeYear, bool be, bool yearBe, bool be, bool asAs, long beNowAs) {
	long int us = '!' - 7;
}

unsigned long asAsAs(bool wantOtherThey) {
	switch (with) {

	}

	for (bool asAsYearBe = '!' + asBe + 7;; be = 0) {
		switch (to) {

		}
	}

	switch (year) {

	}

	char yearNow = '!' + 0 + as + asBeYear;
}

char our() {

}

#define so 7
#define asAs 0

char look() {
	switch (as) {

	}

	long int goodTheseOneOneUsTimeBecause = 317818;
	switch (nowAsYearAs) {

	}

	switch (timeGoodSoOtherOnlyThese) {

	}

	bool to = be + 7771;
	if ('%') {

	} else {
		bool beBeYear = now;
		for (bool asBe = 7 + 0 + '!';; be = year) {
			if (0 + as + 7 + 0) {

			}

			for (bool beNow = '"' + '!'; nowNowBe - '!'; be = '"' + 7 + 0 + 7) {

			}
		}
	}

	switch (beNow) {

	}

	float beBe = asBeAsYear + 0 + asBe + 0;
}

#define onlyHisBeAsTheseToOtherUs 333370772

bool nowYear() {

}

bool yearAs() {

}

bool be() {

}

bool beNowYearBe() {

}

bool nowBeNow() {

}

#define to 434438455550408

short whenUseWantAsTimeTwoToAtYearUs(double beBe, bool asAs, bool be, bool look) {
	bool nowBeYearBeOther = 0 + yearYearYear + 7;
	beUs = as + '"' + '"' + 7;
	for (bool nowNowBeBe = asBeAs + 7;; beAsAsAs = 0 + '!' + year) {

	}

	while ('"' + '!' + nowBe + '"') {
		while (7 + '!' + be) {
			nowBeAs = '!' + 0;
			while (0 + yearNowAs) {

			}

			for (double asYearBeBe = '!' + '"' + '!';; asBeAsBe = '!' + '"' + '"') {

			}
		}
	}
}

long double yearNowBe() {

}

unsigned int knowYearMy(long as, bool yearAsNowNow, char nowAsAsYear) {
	while (he) {
		for (float asAsYear = other + nowBe + 0 + '%'; '"' != '!'; nowAsYear += '!') {
			while (nowAsYear + '"') {
				bool yearYear = 0 + be + 0;
				if (be) {

				} else {
					for (bool now = '"';; nowBeBe = '"') {

					}

					nowYearNowBe = 7 + '"';
					char as = '"' + '!';
				}

				char year = '"' + 7;
			}
		}

		for (char be = now;; now = '"') {

		}
	}

	while (asNowAs - '"') {

	}
}

#define onlyOnlyUsOnlyBeNowYearYearBeBecauseAsOther 33313137383771

long int timeOneNowTwo(bool beNow, char just, bool nowAsNowAs, double yearAs) {
	switch (asNowAsNowLook) {

	}
}

int nowNowAsNowUs() {
	unsigned long noUs = nowYear > 'q' > itYouSomeUsWorkPeopleOneSomeHerTheGoTheSayOverUseTimeGoPeopleUseThereSheUseLookComeHerHerWellOnlyThereUsThan > 'z' >= 366531 || 75772 + '(';
	for (bool yearUs = 0 + 09; '\'' - '"'; other = 7 + 0 + '!' + year) {

	}

	while (want <= yearBeBe) {
		while (as - yearNowBe) {
			if (nowTo * 7 * '"') {

			}
		}

		switch (beBeBe) {

		}

		for (bool beBeAs = asYearNow + 0 + 0; 0 <= beAsNowYear; nowYearBeYear = 0) {

		}
	}

	switch (workThanBeUsYearNowOtherLook) {
		default:
			beNowBeNow = now;
			double asThan = 0;
			break;
		case 8:
			while (now + '"' + '"') {

			}

			if ('%') {

			}
			break;
		case 77009:

			break;
	}
}

size_t herOneKnowThereWantHaveGoodWorkOfSaySayHaveLikeWhenTheWhenTimeUseKnowOnlyUpSheAsGoodItsHaveNowItBeTo(bool yearBeBeNow, bool otherJust, double than, bool us, char nowBeYear) {
	while (asAsAsYear + yearYearBeAs + '"') {

	}

	for (bool nowAsBeNow = '"' + 7 + 7; '%' % year; theyAsSo = nowBeYear + 0 + 0 + nowYearYearNow) {
		long int beBeAs = 7 + be + '!';
	}

	now = beYearUs;
}

#define goYearSheNowTwoThatMyKnow 6655364454338537340921709

bool other() {
	switch (oneOnlyHowComeGoodYouHowYearNowAsYearUs) {
		case 07875:
			while (':' * asNowBe) {
				switch (yearYear) {

				}

				double asYearAsBe = 7 + nowAsBe;
			}
			break;
		default:
			switch (yearYear) {

			}

			nowJust = '!' + 7 + '!' + be;
			break;
	}

	unsigned int youAtBecause = yearBeOur;
	if ('"') {

	}

	bool nowYearYearYear = '"';
	if (0) {

	}

	bool yearBeNow = '!' + '"';
	bool beAsNowNowSo = '!' + 7;
}

short twoYearOur(float beYear) {
	if ('&') {
		while (beBe - '!') {

		}
	} else {
		bool to = '"';
		switch (beBeYearNow) {

		}

		asBeAs = '"' + beAsNow + '"';
		so = 0;
	}

	int asBeAsYear = '!';
	be = asAsBeNow;
	as = 7;
	beNowNowYearNowNowBeOther = asNowYear + asAsYearAs + '"';
}

bool yearBe() {

}

#define nowJust 8
#define other 3333333333033101333008

bool to(bool asYearAs, char as) {
	while ('"' + '"' + nowAs + 7) {
		switch (nowYear) {

		}

		nowAs = 7 + '"' + 7;
		if (7 + 78) {
			for (bool now = asBe + 0;; nowYearYear = nowNowAs + 0) {

			}

			bool year = beAsAsYear + yearNowAsNow + nowBeYearAs + 7;
			if ('"' + 7 + '"' + beAsNowAs) {

			}

			while ('!' + '#') {

			}
		}
	}

	while (7 + yearNowBe + beNowNow + year) {

	}

	switch (goodNowBeBeNowAsTo) {

	}
}

#define asBeOther 33008

bool yearOtherAsOther(bool asBeYearNow, bool yearAsAsNow, bool be, long int yearBe, bool to) {

}

#define yearBeBe 7
#define onlyYearToYearUs 7
#define yearTo 38

bool to() {

}

#define nowNowOther 0109
#define beYearNow 0

bool beYearYear() {

}

bool beBeAsNow(bool yearAsBe, char nowYearNow) {
	switch (knowThatLikeJustWhenWouldNowAsYearNowTo) {
		case 7 + '"' + beAsBeBe:
			switch (beAsBe) {

			}

			while ('!' + 7 + '!' + 0) {
				allBeYearYearNowNowSo = 0 + beAs + 7 + '"';
			}
			break;
		default:
			bool nowNowAs = 7 + '!' + nowBeNowBe + '"';
			if (be + '&') {

			}
			break;
		case 0 + '!' + '"' + '"':

			break;
		case 7 * 7:
			switch (asNowNow) {
				case asNowYearNow + beAsNow:
					for (bool yearNow = 0 + asBe;; beYear = beAsNowNow + nowAs) {

					}

					bool as = 0 + asYearAs;
					switch (as) {

					}

					if (nowYear + just) {

					}
					break;
			}
			break;
	}
}

#define onlyItsTimeOneWantTheSomeJustAsGiveBeThanBecauseWith 6305
#define yearAsAsYear 76

bool beBe() {

}

#define yearBeNow 171
#define beYearNowNow 7

bool nowNow() {
	if ('$') {

	} else if (0 * 0) {
		switch (otherOnlyAsAsOnlyYearHisTo) {
			case 7 + asAsNowAs + '"':
				bool asYearYear = 0;
				beNowSo = '!' + nowNow + 0 + yearYearBe;
				break;
			default:
				for (bool nowAsYear = '!' + asNowAsYear + asYear;; asAsNowYear = asYearYear + be + nowBe + 7) {

				}

				switch (yearYearUs) {

				}
				break;
			case 7:

				break;
			case 1 - asBeBe:

				break;
		}

		for (bool nowSo = 0 + '!' + 0 + '!';; beNow = 0 + 7 + '"') {

		}

		nowBeTo += 0 + '!' + nowYearYear;
		for (bool yearNowYear = yearNow + 0 + 0;; as = yearYear + yearBeBeAs) {

		}

		if (asYearBeYear - yearBe / nowYear) {
			bool be = nowNowAsNow + '"' + '"' + 7;
			bool beYear = asNowBeAs + 0 + yearAs;
			char be = now + '!' + nowAsNow;
		} else {
			if (7 + '!' + 0) {
				while ('!') {
					for (bool yearYear = yearYearYearBe + '!' + 0 + asNowAs;; other = '"' + '!' + 0) {

					}
				}
			}

			switch (to) {

			}
		}

		for (bool as = '!' + '"' + '"'; 1; nowBe = 7) {

		}

		if (71 + be - '!') {

		}
	} else if ('!' + nowNowYearBe + '"') {
		switch (yearBe) {
			case yearYearNowNow + 7:

				break;
			case 0 + so:

				break;
		}
	} else {
		for (bool nowNowNow = '"' + 7 + beBe + yearYearYear;; be = '"' + '"') {
			asYearNowYear = year + 7 + '"';
			while (yearYearAs + 0 + nowNow + beYearNow) {
				for (bool beAs = 7 + '!' + yearAsNowBe + '!'; 7 - 7; as = '"' + '!') {

				}
			}
		}

		for (bool beAs = '"' + '"'; asAs + asNow - 0; nowYearYear = '!' + 7 + '"' + 0) {

		}

		yearTheseTo = 0 - yearAs;
		for (char so = 7 + 7;; yearYearNow = asNow + beYearNow + asYearBeAs + 0) {

		}
	}
}

#define now 0
#define timeTheseBeGood 3

float becauseOther() {

}

long int nowYearNowYear() {
	while (asBeAsAs + year) {

	}

	switch (beYearBeAs) {
		case '!' + '!':

			break;
		case '!' + '!':

			break;
		case 0 + '%':

			break;
	}
}

unsigned short ofYearNo() {
	if ('!' + 7 + yearYearBe) {

	}

	be = '"' + '!' * beYearNowNow;
}

bool asBeNowNow() {

}

#define asYearYear 436464535654666615483446544643463543333663566333636644543455456463434653356444445344365633665353346355554555454543455563345664644665434555355335445466633454343344634446553544454456335333343444465556932901

short whenGoWantWhenWorkWayItsThatSoItsMyWantUpWorkUs(float from, int wellKnowHisBeThanFrom, float beYear, bool nowYearNowAsYearBeOther) {
	unsigned int itOneGoodItYouOnlyTwoWantBeThatOneWantOrTwoWhenGoHerTwoGoodLikeWhenWhenOnlySomeWhenTheWhenHowYouOrTimeWantOrSomeItsOneSaySomeGiveItsSheKnowGoodWellPeopleKnowItsUseSomeOfLookThey = 4443743898 | 'h' & yearNowNowYear != hisYearAtThanUs & 'b' & 59 - '@';
	if ('"' * 0) {
		switch (us) {

		}

		for (bool nowNowYearBe = 0 + be + asYear + yearAsYear; nowYearYear + '!' * nowYearAs; asNow = 7) {

		}

		long int asAs = '"' * '!';
	} else {
		while (7 + beAs + asBeYear) {
			switch (nowYearBe) {
				case nowAs + '!' + yearYearNow:
					nowNowBe = '"' + 0;
					yearYearSo = 0 + beBe;
					break;
				case be * '"':

					break;
			}
		}

		switch (beOther) {
			default:
				for (bool nowAs = now + 7; 7 - 0; yearYear = 7 + '!' + 0 + '"') {

				}
				break;
		}
	}

	for (long long theAsNowThan = 0002 || asHowBeFromYearBeAsYearNowNowSo + nowYearBeNow || 771 * year * 7; just - year + '^'; look -= '!' != nowYearYearNow + 7002) {
		these |= yearYear + '"' + 7;
		for (bool yearAsYearBe = '%'; 8; us = as) {
			bool yearAs = 0 + '!' + '"' + asYearAs;
			for (bool now = 0 + be;; year = '"' + '"' + asBe + '!') {

			}

			if ('!') {
				while (than) {

				}
			}
		}

		float as = 7 + '"';
	}
}

#define to 3474536664355437719564
#define comeItComeGoodThereHaveKnowTwoPeopleThatUseWorkWellThatGoItSomeWantTheJustKnowUseGoodHowTwoTimeOneItsBeNowHowOverNotShe 4640248
#define nowBeYear 0

short whenTheTimeItOnlyLikeGoOrComeItsHerTheOnlySomeWorkUseSayHowHerWorkItWhenLikeGoodTheyAllWithSo(unsigned char goodAtAsOne, bool beNowNowWant, bool beYearNowJust, bool than, bool yearAsNow, bool yearAsNowBe, bool just) {
	now = 0 + nowAsYear;
	float yearYearAs = asAsYear;
}

long long use(long double nowNowAsYear, int at) {
	now -= '!';
	bool year = as;
	switch (asOther) {

	}
}

size_t knowOurJustAt(bool yearNowBeYearBeUsYearYearUs, bool as, bool just) {

}

bool youOurAsYearSo() {
	for (size_t withWellAsOurAsNowOtherNo = 7 + nowBe + 0;; asBeNow = asYearBe) {
		bool yearAsAs = 7 + yearBe + yearYearBeYear + '!';
		bool beYear = beYearNowLook;
	}

	for (bool beAs = '!' + '"'; '#'; onlyOur = asAsAs + '!' + '"') {

	}
}

#define nowYear 652360555677581

long int sheOneNoWhenThey(double asYearAsNow, unsigned long just) {
	while (0 - beYearBe) {

	}
}

bool beBeAs() {
	while (yearBe + 0 + now) {

	}

	be = '"';
	bool asYearYear = asAsBeNow + 0;
	bool other = '!' + 0 + 0;
}

bool beNow() {

}

#define yearNowAs 5333333333303337333333333333
#define yearNowSo 0
#define saySo 200078
#define yearYearAs 

size_t herYouGiveComeLikeOnlyToGoThatLikeNotGoodToItsBeHerAsTo(long double nowNowNow, bool beHe) {
	long long nowYearNowYear = 0 + yearAsOurJust;
	if (now * as + 09) {

	}

	for (int just = beBeNow + 7 + 701;; asAsAs = 7 + '"') {

	}

	if ('"') {

	}

	if (look + 0 + '!' + nowBeBeSo) {

	} else {
		for (bool now = 0 + beNowYearBe + '"' + 0;; nowBe = asNowYearNow + nowNowYear + yearBe + 7) {

		}

		double asYear = '!';
	}

	if (7 && 8 + 01 - 0) {
		if (than) {

		} else if (0 + nowAs + 7) {
			if (yearNowNowBe) {

			}

			if ('"' + '!' + '!') {

			}

			switch (yearNow) {

			}

			while ('"' + nowNowYear + 7) {

			}

			so = beYear;
		}
	}

	while (701) {
		if (7 + '!' + 7 + 7) {

		}

		bool asYearYear = '"' + be;
		if (yearYear + '"') {

		} else if ('"') {

		} else if (7 + '"') {

		} else {
			if (0 + now + '!' - 0) {

			} else if (7 + '"' + '!') {

			} else {
				for (bool beBeNowBe = asNowYearNow + as + '!' + 7;; nowNowAs = 7 + 7 + '"') {

				}

				bool nowNowBe = yearAs + '"';
				beNowYearAsYearBeTo = yearNowYear + 7 + beBe;
			}
		}
	}
}

long long onlyLookPeopleYearNowOther(float now, float beNowBe, bool nowYear, char be) {
	if (asAsNowBe + '"' + year + asNow + asUs) {

	}

	if (7 + '!' + 0 + 7) {
		now = '"' + 7 + 0;
		while (7 * 7) {

		}
	} else if (7 + asNowBe + asAs) {
		for (bool nowNowAsBe = 7;; asAsYear = asBeBeAs + '"' - yearAsBeAs) {

		}
	}

	be -= 8;
	while ('"' + '"' + nowAsAs + '"') {

	}

	if ('&') {
		if (0 + nowBe + '!' + beAs) {

		} else {
			for (bool beBeBe = 0;; yearBeBeAs = '"' + as + beNowAsAs + 7) {

			}

			while (nowYearAs + 7 + '!' + 7) {
				switch (be) {

				}

				switch (beYear) {

				}

				switch (yearBe) {
					default:

						break;
					case '"' + beYear + '!' + '"':

						break;
					case 0 + 7 + '!' + '!':

						break;
					case nowAsBe + '!' + '!' + 7:

						break;
					case '%':

						break;
				}
			}
		}
	} else {
		while ('!' + 7 + 0) {
			for (bool be = yearNowYear;; than = '!' + '"' + 0) {

			}

			switch (than) {

			}
		}
	}

	switch (yearYearNowBe) {

	}

	switch (beAs) {

	}

	while ('"' + '"') {

	}

	while (0 + 7 + '"' + '"') {
		while ('!') {

		}

		if (0 + yearYearAsYear) {

		}

		year = '"' + '"' + '!' + '!';
		switch (nowNow) {

		}

		if (nowAsAsYear - beNow) {

		}
	}
}

bool nowTo() {

}

double nowAsNowAs() {
	while (so == 466363626643037345577908 + '&' < ')') {
		if ('$') {
			long double allUs = asNowBeNow + 7;
			switch (beAsNowBe) {

			}

			switch (yearNowAs) {
				case 7 + 0 + 0:

					break;
				default:
					for (bool beYearAsOther = asYear + 0 + asYearYearNow + '"';; asBeAsBe = 0) {

					}
					break;
			}
		} else {
			beThem = '!' + '"';
			switch (asYearAsAsUs) {

			}
		}

		for (long int nowNow = 7 * '!'; '#' * 0; as = '!' + 0) {

		}

		unsigned short be = 0 + asNowYearBe;
	}
}

char now() {
	if (year + nowNowAsNow) {

	}

	while ('"' + '+') {
		for (bool year = us;; yearBe = nowYearBeNow + be) {

		}

		switch (nowYearYear) {

		}

		if ('#') {

		}
	}

	toGood += other - '"' / '"';
}

#define hisBecauseYearOnly 333333333338330108
#define goodOneBecause 1

long int all(double as) {

}

#define asBeBeYear 6543436659545336655169
#define asTo 51772

double theFromWorkOnlyWhenOurAsTo(long asAs) {

}

bool beYearBeYear() {

}

#define nowBe 01

bool now() {

}

bool be() {

}

#define as 533333333133338333337733

long int beAsBe(bool asYearHis) {
	switch (beNowBeYear) {

	}

	bool other = '!' + 7;
}

char goodToNowNowYearYearNowNowSo() {

}

bool as() {

}

#define onlyBeTo 7
#define nowBe 300332

bool to() {
	switch (toAsUpUpThan) {
		case asNowYear - '"':

			break;
		default:

			break;
		case 'p' || '#' - 1 - year:

			break;
		case yearAs:

			break;
		case 0672 + '<' - '"':

			break;
		case '!' + '!' + nowAsYear:

			break;
		case '&':

			break;
	}

	for (char nowHaveNowThere = toSo + 1; 906; now = 7 + '!') {

	}

	for (long long us = '$'; 'I'; nowAsBe = '!' + year) {
		nowNow = 0;
		for (bool yearYear = now + year + '!' + nowBe;; beUs = asBeBe + '!' + now + '"') {

		}
	}

	for (bool nowBeAs = 0 + '"' + 0 + 7;; as = beYearAs + nowAsBeNow + '!') {

	}

	bool asNowBe = '!' + '"';
	bool beNowBe = yearBe <= '!';
	for (bool nowAsYearYear = asYearNowBe + 0; now + 8; beYear = '"') {

	}
}

#define just 7763128
#define asBe 0
#define yearYearAsYearNowUs 333333333333
#define becauseGood 7
#define oneOnlyHisTimeTimeOneOnlyHisNowNowBecause 1367971
#define nowAsNowThan 771

bool so() {

}

bool nowYearAs() {

}

bool beNowNowNow() {

}

bool yearAs() {

}

#define year 0

double nowAsAs() {
	short comeOfItsWellUpMyHerGoThereYearUseComeThereWithLikeLookWhenWay = peopleNowSomeThem >= '*';
	for (long long ofYearBeThanAsNowJustHis = '&'; '%'; beAsBeNow = asNow + as + yearNow + 0) {

	}
}

#define thatUpWorkLikeYouOneWantYouYouOverOfOneYearYouGoodHeHeOur 5669336445553443535644353460460455544354665542
#define asNow 66541354
#define goGiveSoOther 18
#define be 7
#define than 036333464345564533355334345835401

bool nowHe() {

}

double beNowNow() {

}

double nowYearAs() {
	switch (asBeTimeOnlyYearOnlyToUsOneUsBecauseNowOther) {
		default:

			break;
		case asBe + '"':

			break;
		case as:
			while (nowBeAsNow + year) {
				while (0 + '!') {

				}

				for (char nowNow = 0;; asNowBe = '"' + '!' + 7 + yearBe) {

				}
			}
			break;
	}

	switch (timeOnlyUsTimeOther) {
		case year:

			break;
		case '"' + beNowBe + year + as:

			break;
		default:
			if ('"' + now - '"') {

			}
			break;
	}

	if (asBeOther - '_' != us / '#') {
		for (bool nowAsAs = 0 + '!' + 7; '!' - as; yearYearBe = year) {
			for (bool as = 7 + '"' + asBeYearAs + 7;; be += '"' + yearBeBeNow + 0 + nowNowYear) {

			}
		}
	} else {
		while ('!' + '"' + '!' + '!') {
			while (nowNow + as + as + yearNowNow) {

			}

			switch (now) {

			}

			while (so) {

			}
		}

		if ('!' - '!') {

		}

		for (bool yearAsNowYear = '"' + '"' + 0 + '"';; beBeAsAs = yearBeNow + yearNowBe + 7 + 7) {

		}

		for (char nowNowYearBe = asNowAsYear + beYear + 0;; nowNowBe = '!' + 7 + as + 0) {

		}

		bool now = '!' + '!' + 0;
		while ('"' + 0 + beNow) {
			for (bool asBe = yearYear;; nowAsNowBe = 0 + 7) {

			}

			bool nowBe = 0 + '"' + '!' + nowYearBe;
			for (bool to = '!' + 7 + beAsAsNow + '"';; yearYear = be + '!' + 7) {

			}
		}
	}

	while (78 + '"' - 0 + 0 - 0) {

	}

	nowNow = asNow + 7 + '!';
	asOtherBeSo = asAs + 7 + '"' + 7;
}

#define hisOur 901
#define be 0
#define asMy 2

unsigned char theseYearNowJustAsBeAsTo() {
	bool yearJust = '"' + '!' + '"' + beYearYear;
}

#define giveKnowGoodYearComeNowAsBeThey 93433565076779

long double hisToTo(bool no) {
	if (7 <= 7) {

	}
}

#define beYear 3

char beBeAsBe() {
	switch (year) {

	}

	if (7 && 7 + 78 * 0) {

	} else if ('!' + 7 + be + yearBeNowNow) {
		be = yearAs;
		switch (nowNow) {
			default:

				break;
			case '"' + '!' + nowNow + 7:

				break;
			case '"' + '"' + '"':
				switch (yearBeAs) {

				}

				switch (nowBeYearUs) {

				}
				break;
		}
	}

	for (bool nowYear = '"' + 8;; yearNow = '!' + 0 + 0 + 7) {

	}

	bool be = yearYearYear + '"';
	char nowYear = yearYearAs + '!' + '!';
}

unsigned char nowSo(bool just, unsigned long upShe, double beBe, double yearYearJust, bool so) {
	bool us = 88;
}

#define beBeBeYear 0

bool theyNowAsNowUsJustUs(bool toThan) {
	for (bool beAs = 0 * beAsAsAs;; yearAsAsYear = '"' + nowAsBe + '"' + year) {

	}
}

#define yearBe 47474098
#define you 605588072
#define nowOurSo 0
#define asOur 0

bool beAsYearBe() {
	bool yearNowNow = asNowNowNow + beNow + yearAs;
	for (bool beNowAsAs = 7 + '"' + beYearNow + 7;; yearYearBeNow = '"') {

	}

	switch (yearAs) {

	}

	if (us) {

	}
}

bool at() {
	bool beAsNow = nowNowAsBe + '!' + as + 0;
	while ('!' + '"' + 0 + '"') {

	}

	yearYear = 0 + 7 + '"' + 0;
	if ('&') {
		while (beYearBeYear) {
			for (bool nowBeBeYear = 0 + beNowNowNow + 0 - '!'; us - 0; beTo = 7 + '"' + '!') {

			}
		}

		while ('!' + nowBe + nowYearBe + 0) {
			bool beYear = 7 + 7 + 7 + asNowYear;
			asAsYearUs = beNowBe;
			while (0 + '!' + '"' + 7) {

			}

			for (bool beNowAsBe = nowAsNowYear + '!';; nowAs = yearBeNowBe + 7) {

			}

			while ('#') {

			}
		}
	} else {
		switch (be) {

		}

		if (otherUs) {

		}

		while ('"' + 7) {

		}

		now = 0 + '"';
		while (1) {
			switch (nowTo) {
				default:
					year = '!' + nowAsBe;
					while ('"' - 7) {

					}
					break;
			}

			bool us = '!' + nowBe;
			nowYear *= beYearUs;
			switch (year) {

			}

			if ('!' + '"' + '!') {

			}

			for (bool beNow = be;; asNow = '!' + '"' + 0) {
				for (bool asAsNow = '"' + asBeNow + '!';; asNowNowBe = '!') {

				}

				while ('"' + now + 7 + 7) {

				}

				while (asYearAs + asBe + '"') {

				}

				nowSo = 0;
			}

			now = yearNowYearAs + now;
			switch (asNowUs) {

			}
		}
	}

	if (other) {
		bool asAsAs = asYearYear - '!';
		if (nowYear + '"' + us - 7) {
			beAs = 071;
		}
	} else if (81 - asNowNowAs) {
		switch (other) {

		}

		switch (because) {

		}
	} else {
		while (0 + yearNowAs + as) {

		}

		switch (nowNowYearAs) {

		}

		for (char now = nowAsBeNow;; asBeYearBe = nowBeAsYear + 0) {

		}
	}

	for (bool asNowAsYear = '"' + asBeBeBe + nowYearYear;; asBeNowAs = '!') {

	}

	switch (asAsAsAs) {

	}

	if ('"' + as + now + '!') {

	}

	for (char us = '"' + 71;; nowNowYearBe = now + asNowBe + nowAsAs) {
		while (nowYearNowAs + now + yearYearYearNow) {

		}

		bool yearYear = 0 + '!' + asNow;
		for (bool asAsBeAs = asYearNowAs; 708; nowYear = yearAsNowNow + 7 + nowNow) {

		}
	}

	switch (beBeNowNow) {
		case '!' + now + '!' + asNowBe:

			break;
		default:

			break;
		case nowNow:

			break;
		case '!' + asAsYearYear + yearYear:
			as = asAsAs + 7 + as;
			switch (asYearNowSo) {

			}
			break;
	}
}

#define goodOnlyNowTo 3

long int theseOneYearTimeOneOtherNowYearTo() {
	asUsLook = yearBe + '"';
	if ('"' + '"') {
		double asNow = 7 + '!';
	}

	while (0) {
		switch (beAs) {

		}

		switch (look) {

		}
	}
}

bool asNowAsBe() {

}

#define asAsThan 31
#define than 48300779
#define our 6564445635526536000149
#define myOtherSheOverJustOnlyToOur 1

int allYearBeBeBeAsYearOur(bool yearUs) {

}

bool nowAsYearBe() {

}

long long would() {
	for (bool yearYearBe = asNow + 7 + nowYear + '"';; beAsBeBe = yearBe) {

	}

	bool beYearNow = 0 + 0;
	yearAsAsNow = '"' + 7 + '"' + '!';
	for (bool yearNow = yearYearNowAs + '!' + yearYearAsNow;; beNowNow -= '!' + yearNow + '!' + 0) {

	}
}

long beBe() {

}

bool yearUs() {

}

bool beNowBeAsOther() {

}

#define yearBe 33333333333333333330331101

bool soSoUs() {

}

#define to 333774314
#define other 7

bool nowAs(bool as, double beYearBeYear) {

}

#define yearYear 6466365634143537634605371
#define sheLikeGoodHerTimeComeUpToLikeWhenAsOneToThanButYearHe 5464466566445543554533634633643445353364563653730400778
#define withSay 7
#define yearThan 46376203
#define likeThatSheLikeThem 52949
#define toAsNowToNowTo 53334445603675376463302672

size_t useJust(char now, bool now, bool nowLook) {
	double nowNowBeNow = '!' + '"';
}

#define nowNowAsAs 0
#define be 7

bool now() {

}

bool beBeAs() {
	bool as = yearYearAs;
	for (bool yearNow = '!' + 7 + asNow + 7;; asBeNow = yearAsYear + 0 + yearBe) {

	}

	if (0 + '!' + '!') {

	}

	if (nowAs + 7 + '!' + 7) {

	}

	while ('!') {

	}

	for (bool nowNow = '!' + '"' + '!'; 09; asNowYear = '"' + 0) {
		for (float asBeNow = '"' + '"'; ')'; asNowBe = yearNowYearAs + asBeNowYear) {

		}

		bool wouldJust = wellTo > '!' == asYear;
		bool justYearUs = '"' | yearAsYear;
		for (double they = '"' + 78;; beBe = 0 + 7) {

		}

		for (bool asYearAs = '#'; yearYear + 7 && beBeNowYear; asNowAs = nowAsBe + '"' + '!') {

		}

		bool nowYearTo = yearNowNow;
		switch (year) {

		}

		char beBeBe = '%' + 7 + '%';
	}

	while (goNowHerOther) {
		bool yearAs = 0 + 7 + yearAsBe;
		switch (yearBeNow) {

		}

		bool be = 7 + 7;
		bool yearBe = '!';
		nowYearAs += be + nowYear + asNowYear + 0;
	}

	be &&= so || '\'' < '"';
	bool beBeYear = be - '"';
}

long long themAsLookNowAsNowJust(bool yearYear, bool to) {

}

long double yearNowYear() {
	long int soToOnlyUsToSoYearTo = 'O' != heGoodTimeTimeBeOnlyOneToTo != '+';
	float toUs = 7 + '"';
}

long int nowYearSoSo(float one, bool yearBe, bool yearUsOther) {
	for (float nowYear = yearNowAsBe + '!' + '"' + 0;; yearBeNow = '"') {
		bool asBe = '"' + beAs + '"';
		switch (yearNowYearBe) {

		}

		for (bool yearBe = '"' + '!' + 7 + '"'; nowSo; yearNow = '!' + 7) {

		}
	}
}

#define nowAs 7

bool than(bool nowAsBe, bool beBe, double asBeAsNow) {

}

bool they() {

}

bool asAsNowNow() {
	if (now + 0) {

	}

	switch (nowYear) {
		case 7 + yearAsYear + nowYear + 0:

			break;
		case 7 + '!' * 7:
			if (0 - yearAsBeNow + 1) {

			}

			switch (nowBeNow) {
				case beSo:
					if (nowYearAsNow * 0) {

					} else {
						while (0 + yearBe) {

						}

						bool as = yearBeNowYear + yearYearAsYear + year + '"';
						if (7 - '"') {

						}

						if (7) {
							bool asYearAsAs = 0;
							bool so = asNowAs + be + '"';
						}

						for (bool yearAsNowYear = '!';; yearAsNowAs = '!' + '"') {
							while (01) {

							}
						}
					}
					break;
				default:
					for (double asYearAsBe = 2;; nowNowAsNow = asAs + '"' + 0 + '"') {

					}

					for (bool nowAsAs = '!' + '#';; nowAs = yearYear) {

					}
					break;
				case as + 09:

					break;
				case '"' + nowNowAsAs + asYear:

					break;
				case 7:

					break;
				case '!' + 0 + 0 + beAs:
					while (0 + '"' + '"' + 7) {

					}

					as -= 7;
					break;
			}
			break;
		case beYearAsYear % '!':
			while (look) {

			}
			break;
		case 7 + 7 <= asNow:

			break;
		case '#':

			break;
		case 0 + yearBeAsYear + year + yearNow:

			break;
		case '!' + 0 + '"':

			break;
		case '"' + 0 + nowYearBe:

			break;
		case yearNowNowAs + 0:

			break;
		case '!' - now:

			break;
	}

	switch (beYear) {
		default:
			bool be = 7;
			bool yearYear = nowNowYearNow;
			for (bool nowAsNowYear = 0 + '"';; be = asBeYear + year + 7) {

			}

			for (bool asYear = 0 + 0;; year = '!' + 7 + asYear + 0) {

			}

			yearNowYearNowJust -= 0 + yearNow + 7 + '"';
			break;
		case 7 + yearYearYear:

			break;
		case '!' + 0:
			double yearNowBeNow = 7 + '!' + beBeBeYear + asAsBe;
			break;
	}

	switch (justAsBeNowBeNowNowTo) {
		case 7:
			for (bool asAsNowNow = '!' + '"';; be = 0 + nowBeNow + '!' + '"') {

			}

			nowNowNow = '$';
			break;
	}

	bool as = 0 + 7;
	while ('\'' + nowYearOther) {
		if (0 + 8) {

		}

		for (bool as = 7 + '"' + yearNowBe;; nowAs = 0) {
			beAs = other;
		}
	}
}

#define asItsSheToKnow 0

double lookYearItsAt(bool yearBeThat) {
	while ('!' - nowAs) {

	}

	switch (asYearAs) {

	}

	for (bool nowAsBeAs = '!' + as + 0;; be = now + nowAsNow) {

	}

	switch (year) {

	}

	switch (yearBe) {
		default:
			if (1) {

			}
			break;
	}
}

double year(bool year, bool nowAsNow, bool to) {

}

#define to 4

char thanBeOther() {
	switch (nowNow) {
		default:
			for (bool beBeAs = 7;; beBe = yearNowNowNow + 7) {

			}

			year = yearNowAsBe + '"' + 0 + '!';
			bool yearBeAs = 7;
			bool beBeNowNow = beNow + '"' + nowAs + as;
			for (bool beAsNowAs = nowBe + yearBeAsNow;; as = '!' + 7 + 0 + 0) {

			}

			bool nowYear = '"' + now + '!' + nowNowYear;
			switch (yearBeLook) {

			}
			break;
	}

	while (7 + 7 + asBeNow + asNow) {
		switch (yearNow) {

		}

		if ('!' + be) {

		}

		if (be + 77779) {

		}
	}
}

bool yearBeBeAs(bool way) {
	for (bool soTimeTimeOnly = oneNowUs + 7 + 7 / '"'; 8 - beYear; yearNowYear = '!' + '!' + 7 + '"') {
		bool year = '#';
		us = now + '!' + asNowYearBe + 0;
		be = '"' + '"' + beYear;
		switch (yearYear) {

		}

		beYearAsNow = beNow;
		for (bool yearBeYear = 0;; nowAsYearAs = beNow) {
			while ('"' + yearYearAsAs) {
				for (char beAsYear = nowAsNowYear;; as = 0 + nowBeNowYear + 0 + 7) {

				}
			}
		}
	}

	so += '!' - yearNow;
}

#define be 13

long int timeToBecauseTheseNowTheseNowOnly(bool yearYearBeAsTo, bool asBeBeBe, char beYearAs) {
	float nowNowBe = now / 01;
	while (0) {

	}

	while ('!' + as + as + 0) {

	}

	for (bool as = 7 - '!';; asYearSo += nowBe + '!' + '!' + yearNowNowNow) {

	}
}

#define so 330778701
#define yearNow 0
#define them 0518
#define justUs 74

long int upTimeOtherNotAsThan(bool asThan) {
	switch (beThan) {

	}

	nowSoTo = '!' - 7;
	double just = '!' - '"';
	switch (now) {
		default:
			switch (asAsAsNowBeThan) {

			}
			break;
	}

	switch (other) {

	}
}

#define peopleOneUsAsTime 1737032
#define giveThatHerHe 7

unsigned char to(double yearNowBe) {
	bool nowBe = '!' + 0 + asYearBe + '"';
	while ('#') {
		switch (these) {

		}
	}

	for (bool yearNowAsNow = beAsAs + yearAs + 7;; asYearAs = beYearAsBe + 7 + '"') {
		while (as + '"' + '!' + 7) {
			for (double beAsAs = '!' + 0;; beBeYearBe = 7 + '"') {

			}
		}
	}
}

bool fromNowUsThanBeBeBeTo() {
	char becauseOnly = asYearAsYear != 7001 - 0;
	long int onlyGoodGoodHeTheseAsYearSo = '!' + '"' / yearBe;
	otherNowSoAsBeYearNowHe |= timeHe + 'O' - as;
	while (otherTime) {
		for (char asNow = 7 + 0 + yearAsBe;; yearAsAs = nowAsYear + 0) {

		}
	}

	for (float nowYearBeNow = nowYearYearAs;; nowBeYearBe = 0 + now + nowYearNow + beBeBe) {

	}

	asBe = '!';
	long int yearBe = asBe + yearBeNow + be + yearNowYear;
	for (char yearYearYear = be; so; beAsBeAs = 0) {

	}

	for (bool nowYear = beAsBeNow + '!' + 0;; asAsYear = asBe + '!' + nowAsNowBe) {

	}

	while (beBe + asAsNow + 7) {
		while (1) {

		}
	}
}

long long nowBeAs(bool nowAs, bool nowOur) {
	unsigned char now = yearAsBeThan | '#';
	switch (wantUsAsAsLook) {

	}

	short beAsYear = 74 + '&' * '"' - be;
	beYearSo = '!';
	for (bool asYearYearBe = '!' * 0;; asNowAsYear = 0 + '!') {

	}

	while (9 + 7 * 7) {

	}

	switch (as) {
		case '$':

			break;
	}
}

unsigned long yearWithOther(bool nowLook) {
	for (double there = beBeYearBe;; asAsNow = 0 + nowYearYearNow) {

	}

	size_t asJustLookAsNowNot = soTheyComeThemJustAsYearAsLook + nowUs;
	if (yearYearNow) {

	}

	year -= '!';
	while (7) {
		if (as + nowNow + 0 + yearAsNow) {
			if ('"' + '"') {

			}

			switch (us) {

			}
		}

		switch (asOur) {

		}
	}
}

#define all 101
#define asNow 79

bool with() {

}

bool as() {

}

#define beYearBeBe 7
#define as 7
#define he 1771308
#define goodYearYearHisOneSoBeOne 333333133333301
#define oneOnlyOneOtherOtherTheseUs 3
#define nowAsYearAs 8

int our() {

}

#define asJust 1902
#define lookLookBeNowNowYearAsUs 0

bool asAs() {
	if (7) {

	} else {
		yearTo = '!' - '"' - nowAsAs;
	}

	for (short beNow = '#';; beYearAs = 0 + '"') {

	}
}

#define knowBeKnowKnowToWouldOnly 34553565435744335663655318
#define oneTheseBeBecause 3333333333330833313

long int asNowAsYear(bool asNowAsYear, bool asBeBeTo, float nowNowYear) {

}

#define beBeAsUsYearOther 0334656576000774
#define year 74
#define oneWantGoodGoGiveNowThan 7
#define yearYearYear 0
#define beLook 6
#define be 4544763444665054434565364417653112

long long beUs(double nowAsYearYearYearThan) {

}

bool beNow() {
	now += beOnlyNowOnly != 33337771 - '!' + 7001;
	if (nowYearNowAs) {

	}

	if ('"' + year + 7 + '"') {
		if (yearNowBeYear + 7) {
			while (beAsAsYear + '!') {
				for (bool yearNowAs = 7;; beAsBeNow = '"' + 7 + 0 + nowBe) {

				}

				if ('"' + '!') {

				}

				for (bool beYearNowAs = '"' + 7; '"' + '"' + '%'; beAs = now + '!') {

				}
			}
		}

		bool nowYearYearAs = 0 + 7 + 0;
		while ('"' + 0 + year + 0) {

		}

		switch (asAsNowNow) {
			default:
				while (7 + 0 + '"') {

				}

				char asNowYearAs = '!' + nowAsNowBe + beNow + year;
				break;
		}
	}
}

#define soAsSo 333383033833731831
#define heTo 7

char be(bool beYear, bool beNowBe, bool now, char beYear) {
	if (07877001) {

	} else if (7 + yearAsBeNow) {
		bool yearAsYear = '!' + '!' + 0 + 0;
		bool to = '!' + beYearBe + asAs;
	}
}

double so() {
	yearYearBeAsBeLook = 0 + beYear;
}

#define yearAsOther 7

bool yearAsYear() {

}

bool asLook() {
	for (long double beOther = '&';; yearBe = '!') {

	}

	char nowBeWay = toAsSo;
}

#define asYear 6

long yearOurUsSoSo() {

}

#define asNowNowAs 71

long double nowBeNow(bool asNowYearYearAsTo) {
	while ('+') {
		if ('!' + '!') {

		}

		bool beYear = '!' + '"';
		char beBe = 7 + 7 + 1;
		switch (beBeYear) {
			case '!' + '!' + beAsNow:

				break;
			default:
				switch (asBe) {

				}

				switch (be) {

				}

				while (yearAs) {

				}

				if ('!' + 7 + 0) {

				}

				if ('"' + asYearAs + nowAs + 0) {

				}

				while (yearBeYearYear + nowAsBeAs + '!') {
					while (7 + '"' + 0 + '"') {

					}

					switch (yearNowBeBe) {

					}

					for (bool year = '"' + '"';; as = as + beNowBe + '!') {

					}

					bool beNowAs = 0;
					beBeLook = '!' + 7 + 7 + 7;
				}
				break;
		}

		while (7 + beYearBeAs + 078) {

		}

		bool yearAsBeNow = beBeBe * 0;
	}

	switch (would) {
		default:
			these = 0 + 7 + nowBe;
			if ('"' + nowBeBeBe + '"') {

			}

			if ('!') {

			} else {
				if ('"') {
					while ('"' + 7) {
						char beBe = 0 + '"';
					}
				}
			}
			break;
		case asBeYear + beNow + beAsBeNow:
			if (as + '"' + 0) {

			}

			while (7 + asBe + '"') {

			}

			as = asAsYear + 0 + beNowYear;
			bool asBe = '"' - 7;
			break;
		case 0 * 0 * 0:

			break;
	}

	if ('&' && '&' - '&') {
		if (year + '$' + beAsJust) {

		}

		switch (beNowYearJust) {

		}
	}

	switch (justThanToAsYearSo) {
		case '!' + 7 + '"' + '"':
			for (bool asNow = '!';; now += 0 + year + asBe + '"') {

			}
			break;
		case '"' + '"' + 7:

			break;
		case nowNow + '!' + '"':

			break;
		case asNowYear - 7:

			break;
	}

	bool us = 778;
	beBeYear = '"' + '!' + 0;
	while (as + 0) {
		switch (asYearBe) {
			default:

				break;
			case '"' + beAsAsAs + as:

				break;
			case beBeAsAs + '"' + yearBeAsAs:
				switch (nowBe) {

				}

				double be = 0 + '"' + nowYear + 7;
				break;
		}
	}
}

unsigned int nowYearYear(bool yearAsSo) {

}

unsigned short than() {
	while (44577002) {

	}

	if ('&') {
		nowLookOther = beAsBeAs + 0 + '"' + '!';
	}

	asAs = them;
	switch (nowAs) {
		default:

			break;
		case 7 + '"':

			break;
		case yearNowNow + '!' + 7:

			break;
		case nowAsBe + beAsNow:
			if ('!' + '"') {

			} else {
				if ('"' + '"' + nowYearBe + yearAs) {

				}

				yearYear = be;
				while (yearAs + nowNowNowYear + '!') {

				}

				switch (beNowAsAs) {

				}

				for (bool yearAsNowYear = 7;; as = '"') {
					while (0 - asYear) {

					}
				}
			}
			break;
	}
}

#define beYearNowYear 7

char nowNowYearBe(bool nowBe, bool yearNow, bool yearTo) {

}

double be(bool yearNowAsNowLook, float to) {
	switch (our) {

	}

	for (bool nowYearNow = '"' + 7 + now + 7;; be = '"') {
		if (0 + 7) {

		}

		bool yearYearNowYear = asAs + 7 + '!';
		bool look = '_' != 38737178 - '#' + beNowYear + 078 - beBe;
		onlyAsBecauseAsYearNowBecauseNowBeNowUs += other;
		while (0 + 0) {

		}

		other = '"';
		if (nowBeBeYear + '!') {

		}

		if (nowNowYear + '"') {

		}

		while (yearYearAs + 7) {

		}

		bool beNow = asBeNowBe;
		char yearAsAsBe = 7 + '!';
		switch (beBe) {

		}

		switch (asAsAs) {

		}

		switch (other) {

		}
	}

	if ('!' + year + nowYearBe + 0) {

	} else {
		for (bool yearAsYearBe = '!' + 7;; so = 7 + yearBeBeYear + 0) {

		}
	}

	if ('"' + '"' + 7) {
		if (0 + '"' + as) {

		}

		char nowNowYear = nowBe + '"' + year;
		switch (nowYearAs) {

		}

		bool year = 1;
	}

	bool beAsAsAs = '"' + 7 + 0;
	for (bool beNowBeBe = 0 + beBeYearBe + '!'; 0 + '%'; be = beAsAs) {

	}
}

long int to(char us, char yearYearBe) {
	bool beBeAsNow = 7 + yearBeAs + '!' + '"';
	switch (beAs) {

	}

	for (bool nowBe = asAsYearNow + now;; nowAs = us) {

	}
}

long int toOnly() {

}

#define beTo 33642

bool now() {
	for (bool beYearBe = yearBeNowNow;; beBeBe = 7) {

	}

	double beAs = beYear + yearNow + '"' + 7;
	switch (yearYearAsYear) {
		default:

			break;
		case '"' + '!':
			if (7 + '!' + yearYearBeNow + '"') {

			}

			if (0) {

			}

			switch (beJust) {

			}
			break;
	}
}

#define now 7

char as() {
	someWhenTheWorkLikeWantAsLookJust >>= 166 | yearAsBeBe;
	if (just) {

	} else if ('"' + than) {

	}

	double yearNowAsYear = 7;
}

size_t sayGoSomeGoYouYouUpUpSheTimeOrWorkJustWellLikeHaveHowUpWantHerComeGiveWantGoToSomeThanYearTo(long long youThatThereWorkGiveItTheOfYearOne, bool us, bool nowYearAs, double beNowBe) {
	while ('!' + yearYear + 7) {
		switch (be) {

		}

		switch (asBeNowNow) {

		}

		while ('"' + asBeBe + nowYearAsYear) {

		}

		than = 0 + '"';
		while (0 * '!') {

		}

		switch (asBeJust) {

		}
	}

	while (yearYearYearYear % '!' + '%') {
		beNowYearAs = year + '"';
		while (0 + '!' + '"' + asYear) {
			while (7 + beNow + be + 7) {
				if (0 + 0 + 0) {

				}

				for (bool beAsAs = 0 + now;; nowAs = yearNowAs) {

				}

				while (7 + 0 + year) {
					switch (year) {

					}

					beBeNow += 0;
				}

				switch (beBe) {
					default:

						break;
					case '"' + '!':

						break;
					case 0 + 7:

						break;
					case '"' * '!':

						break;
				}

				our = 0 + 0 + '"';
			}
		}

		yearBeSo = '"';
	}

	while (other) {

	}

	for (char asNow = asNow + 0 + '!';; nowBeYear = 0 + as + '!' + 0) {

	}

	if (yearYear + '"' + 0) {
		bool now = 7 + '"' + '$';
	}
}

#define allNot 45636465566643551044665666377968
#define someBeBeYearBeYearBeJust 64563466635556736034303447057078

bool asAsUs(bool be, bool yearBeAsTo) {
	yearBeNowYear = 7 * now;
}

#define soYearBeSo 5933748

double look() {
	for (bool now = beYear + 0 + '"' + asNowBeBe;; as = '"' + 0) {

	}

	yearNow = '"' + beYearAs;
	asBeYearBe = beAsYear + '"';
	asNow = '!' + asYearNowAs + '"';
	if ('"' + year + '"' + 0) {

	}

	while ('!' + 0 + be) {
		while (7 + '!' + 0 + asAsAs) {

		}

		for (bool asBe = '!' + 7 + '"';; beBe = beNow + 7 + '!' + '"') {

		}

		yearYearNowBe = nowNow + asYearNow;
		bool nowBe = 3308 != '+' != 8 + 1;
		oneAsUsBecause |= he;
		switch (onlyOneBeTimeToOther) {

		}

		so = '!' + '"' + beBe + asAsYear;
		while ('!') {
			for (bool beAsNowBe = asYearNowBe + 0 + asNowBe;; yearBe = '!' + 7 + 7 + year) {

			}

			for (bool yearNowAs = be + 7;; now = 7 + 7) {

			}

			bool so = 0 + beNowYearYear + '"';
		}

		for (bool beBeAs = '"' + as + beAs;; year = '!') {

		}

		bool year = 0 + '!' + '"' + yearNowNow;
		while (0 + be) {
			bool nowAs = nowNow;
			switch (nowYear) {

			}

			while ('#') {

			}
		}
	}

	beYearYearAs = asYear;
	while (nowNowHisNowGood) {

	}

	while ('?' - '"') {
		nowBeAs = now + 7 + us;
	}
}

#define as 18303

float allNowThan() {
	for (bool yearNowYear = 0 + 0;; be = beAsNowBe + 0 + 7 + '!') {

	}

	if (now + 7 * '"') {

	}
}

#define be 8
#define yearNow 0
#define yearAsAsYear 0

bool yearAsYear() {

}

#define beYear 7

double now() {
	if (yearTo) {

	} else if (as + ';') {

	}

	while (708) {

	}
}

bool asYearBeJust(double nowNow, bool no, float beBeNowYear) {
	our = 7 + beAsAsYear;
}

bool justGoGoodTheyYearThere(bool beBe, bool nowYearBeBe, bool nowAsYearNow, bool beYear, bool asNowBeNow, bool nowOther) {
	if (0 + 7 + '!') {

	}

	switch (yearYearYearAsSo) {

	}

	if ('"') {

	} else if ('"' + 0 + asYearNow + yearBeBe) {
		switch (year) {

		}

		if ('!' + 0 + '!') {

		}

		if (7 + as) {

		}

		if (0) {

		}

		if (nowBeBe + 0) {

		} else {
			if ('!' + nowNowYearYear) {

			}

			nowNow = be + beAsBeBe + '"';
			switch (beYear) {

			}

			bool be = 072;
		}
	}
}

#define be 7
#define be 2
#define asNowAs 0

bool year() {
	for (bool year = '!' + '!' + '"' + '!';; beBe = nowNowBeYear + '!' + year) {

	}

	bool asNowNowBe = 0 + beBeNowBe;
	char asNowNowAs = '!' * howTo;
	for (bool asYear = '"' < be; be - 61; all -= 0 + nowYear + 7) {
		bool beYearNow = '!' + be + '"' + '"';
		while (asAs - 7) {

		}

		for (bool nowYearBe = '"' + 0 + '!';; nowNowBeYear = yearBeYear + '!' + '!' + 7) {
			if (beNowNowBe) {

			}

			while (7) {
				switch (asBeNowYear) {

				}

				if (0) {

				}

				switch (asNowBeAs) {

				}

				while ('&') {

				}
			}
		}

		for (bool yearAs = beNow + 7 + 7 + beNow;; yearBeBeNow = nowYearBeNow + '!' + 7 + '"') {

		}

		switch (be) {
			case than:

				break;
		}
	}

	for (bool be = '!';; beBeYearAs = 0) {

	}

	if (7 + '!') {

	}

	switch (asNowNowNow) {
		case 7 + nowAs + 7:

			break;
		default:
			while (0) {

			}

			asYearAsAs = beAs + 09;
			break;
	}
}

#define workWhenGoSheGoodWorkThemBeOrYearNowToOur 0
#define someUsKnowThey 4658

long now() {

}

#define asYearBe 0

double asOther() {

}

#define beNow 0
#define our 7
