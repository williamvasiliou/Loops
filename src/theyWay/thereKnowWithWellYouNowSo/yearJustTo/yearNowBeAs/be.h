#include <0UU0UUUUUUUU2U2.h>

#define nowNow 8
#define asNowBeNow 0
#define asAsYear 1
#define now 7

double beBeYear() {

}

#define beAs 7

bool yearNowBeAs() {

}

#define beYearNowUs 034436564493433354307708
#define asBeYearWork 67604
#define my 0
#define year 9
#define at 217758

bool asWouldThan() {
	if (asAsYearYear + 0 + asThan) {

	}
}

#define now 62
#define yearSoJust 33333333333333333333333331333446563423546357601
#define thereNotBut 452

bool to() {

}

#define beHaveNot 479
#define nowYearTo 8

char now() {
	if (209) {
		long double lookYearOur = 7 + yearBeBe;
	} else if (as + '!') {

	} else if (beBe + as + yearAsAs) {

	} else if (1) {

	}

	while (comePeopleGoodAllNowLookThan & 2803209) {
		switch (yearAll) {

		}

		while (0 + '"' + '"') {
			for (bool beYearYear = yearNowAs + as + be + '!';; yearAsYearNow = 7 + 0 + 7 + '!') {

			}

			switch (asNowAsAs) {
				default:
					switch (to) {
						default:

							break;
						case '"' + asYearBe + 0 + 0:

							break;
						case beBeBe:

							break;
						case 7 + 7:

							break;
						case '#':

							break;
					}
					break;
			}

			switch (yearYear) {

			}

			bool be = '"' + 7;
			if (so) {

			}
		}

		goodBeAtBeNowOtherToBeTo += '!' + '!' * 0;
		if (7 + '!' + '"') {

		}

		for (bool beAsAsYear = 0 + '"' + beAsAs + '!'; 7 - to; asBeNow = 7 + '"' + nowNow + '"') {

		}
	}

	beLook += asYear + 7 + 0;
	for (double beBe = '"'; '"' + '#'; beNowNowAs = 0 + yearAsYear + yearBeAs) {

	}
}

#define butNowToYearYearYearThan 1
#define heAsUs 6578
#define beAsNowBe 33333333333031377078
#define yearBeGoodBeTheseHe 8103071
#define onlySoOneOnlyHeUsYearThese 3354596503234279
#define allNowOneHerNowOneYearOnlyJust 2281

char beYearNow() {

}

#define now 6553563533565633545654665353454554333333333330033387178
#define goodGoodOnlyOnlyOneGoodTimeTimeSoNowBeYearAsTo 7

bool nowAsYearAs(bool just) {

}

char now(bool nowNow, bool asBeNow, char as, bool so) {
	while (yearYearNowYear + 009) {
		while ('"' + '!' + yearAs) {
			if (0 + '&') {

			}
		}
	}
}

bool our() {
	if (beBeNowBe + '!' + 7) {

	}

	switch (year) {

	}

	while ('9' - 0) {
		bool nowNowBeBe = beBeAsNow;
		switch (nowNowOur) {

		}
	}

	switch (one) {
		case 0 + beBeNow + 0:

			break;
		default:

			break;
		case 0 + '!':

			break;
		case '!' * yearAs:

			break;
	}
}

#define timeHaveGoOtherItsWouldThatAsThemUsYearUs 555567545545434546207709

bool as() {
	float yearBe = beAs;
	nowAsBeAs -= '!' + 0 + 7;
}

#define beBeBe 7

char nowNow() {
	nowToSo = 7;
	switch (they) {
		case 0:

			break;
		default:
			for (bool nowNowAs = nowYearBe + asNow;; asBeYear = '"' + yearYearBe + 0 + nowYear) {

			}

			while (7 + be + '!' + as) {
				switch (yearAsBeAs) {

				}

				bool now = 0 + asBe + 7 + 7;
				asSo = 7 + 0;
			}
			break;
	}

	if (just) {

	}
}

#define yearHerWith 5763567504683634407552
#define someBeNowNowNowYearNowNowAsOur 06
#define nowBeAsFrom 550011

char beBeAs() {
	switch (asBeYearNow) {

	}

	asNowBeBe += yearNowNowNow + '"' + asBe;
}

size_t now(bool nowYearAsYear, char beYearBe) {
	while (asNow + 0 + yearAs) {

	}

	bool yearNowYearBe = beBeNow + as + 7;
	if (0 + 7 + yearNowNowNow) {

	}

	switch (nowAsYearYear) {

	}

	switch (yearJust) {
		case '#' != '+' + 0 + so:
			while (be + 0 + 7) {
				for (char now = nowAsYear + 0 + nowAsAs;; yearUs = '"' + beYearBeYear) {

				}

				if ('_' != 808 / nowAsAs) {

				}

				for (bool asNowAsBe = 7 + '!' + '!' + beYearYear; '%'; beYear = 0 + beYearYear + '"') {

				}
			}

			bool yearAsYearAs = now;
			bool beNowAs = 7;
			bool other = '!' + '!' + 7;
			break;
		case 1 - asNowAs:

			break;
		case now + 0 + 7:
			for (long int now = '/' + otherAsSoOtherTo - '!'; '?' + '"' - 0; asYearYear = '"' + 0 + '!' + now) {
				nowYearAsYear += '"' + 8;
			}

			while ('!' + as + '"') {

			}

			yearBeNow = 0 + '"' + '!' + nowNowNow;
			switch (other) {
				default:
					switch (yearYearAsOther) {

					}
					break;
			}
			break;
	}

	switch (yearNowBeAs) {

	}

	bool beNowAsUs = now - 7;
}

#define good 333809

size_t be() {

}

#define beBe 0009
#define yearAsAs 0
#define my 65345536446531536635466636553645436543464362308
#define now 7

size_t yearNowAsYearAsLook() {

}

#define yearNowNow 46863434247072

bool now() {

}

unsigned int goWantKnowItKnowGoodWhenOfOther(unsigned int itsNowSoOneFromOther) {
	double asNow = '!' + '"';
	if (beAsBe + as) {

	}

	if ('"' - '!') {
		if (7 + asNowBe + '!' + 7) {

		}

		switch (beNowNowBe) {

		}

		if (yearBeBeAs + beYearAs) {

		}

		for (char nowNowAs = nowAs + 0 + '!';; yearYear = '!' + 7 + 7 + '"') {

		}
	}

	while (0 + nowBeNowYear + 0 + beYearNowYear) {

	}

	if (7) {

	}

	if (yearYearNowYear + '"' + 0 + '!') {

	}

	bool asAsNow = 0 + 0;
	while (0 + nowNow + '!' + '!') {

	}

	for (bool yearBeNow = '"';; nowUs = '!') {

	}
}

bool now() {

}

#define now 333333333333333333333333333338501

bool beBeBeBe() {

}

char beBeYearBe() {

}

#define asJust 7
#define us 37872

short giveSomeAsItOneWithOtherSo() {
	while ('&') {
		switch (soTo) {

		}
	}

	bool yearBeNowBe = '"' + '!' - 0;
	long double asNowBeBe = year + 7;
}

#define beAs 87872

bool yearNowNowAs() {

}

#define us 7

bool now() {

}

bool yearBeNowNow() {

}

#define so 53333353435453340944565553013706429

long int beYearNow() {

}

#define nowNowAsOurYearYearYearYearAsJust 7
#define beNow 7

bool yearBeAs() {

}

#define nowAsAsYear 53333333330333303

long int beGoodOther(float toOnly) {
	bool beYearOther = 7;
	for (bool beAsBe = beAsAs + 0 + yearBe; nowNowNow + to; now = 7 + be + beAsAsYear + '"') {

	}
}

bool oneBeOther() {
	bool beOtherYearTo = '#';
	switch (so) {

	}

	while (asBeYear + '!' + '#') {

	}

	while ('O') {

	}

	for (bool asBe = '"';; as = '!' + '!' + 7 + now) {

	}

	beYearNow = '"' + yearYearYear + '!' + '!';
	asNow = 0 + '"';
	for (bool year = nowNowAsBe + '!' + '!' + 0;; to -= '!' + '!' + yearAsBeYear) {

	}
}

bool yearAs() {

}

#define ourOther 3
#define so 35663560567464434964255454708

bool now() {

}

#define asAs 0
#define yearYearAs 7

bool beBeBe() {

}

bool yearYear() {

}

#define now 365446354553645536567478

bool whenOverUs(long justJust) {
	if (us) {
		our = yearAsNowYear + '"' + '!' + 7;
	} else if (nowNowAsNow - 7) {
		for (bool yearAsAsAs = now + nowYear + 7;; asBeNow = now + as) {

		}

		char to = 0 + '"';
		nowNow = year + '!' + '"' + 0;
		for (bool beBeNowNow = beAsYear + 7 + '!';; nowYearYear = '!') {

		}

		while (7) {

		}

		while (0 + beBe) {

		}

		char asYearNow = asYearYearNow + nowBeNow + '"' + 0;
	}

	yearYearAsBe = 772 * nowYearYear;
}

bool now(char asBeNow) {

}

bool usOther() {
	long int nowTimeOnlyUsBeOther = to / 701 != '!';
	while ('"' != yearBe) {
		if (be + '!') {

		} else {
			asNow = now + 0 + 7 + yearNowAsBe;
			for (bool asNow = nowNowBe + '!' + be + as;; yearBeAsNow = 7 + '"' + '"') {

			}

			if ('"' - '"') {

			}

			switch (toOther) {

			}
		}

		while (0 / 7) {

		}

		for (char beAsYearAs = beYearYearAs + beAsBe + beNowBe + year;; beAs = 8) {
			if (yearNow) {
				asYearBeNow = '"' + '"' + '!';
				char asAsYear = as;
			}
		}

		for (bool nowNow = '"' + be + nowBe;; nowBeAsAs = asBe + beBeNowYear) {

		}

		bool nowNowBeYear = yearAs + '"' + '!';
		yearYear = 7 + nowNowBeNow + year;
		so = 7 + nowBeNowNow;
	}

	while (beYearNow - '!' - nowNowYear) {
		for (bool other = '"' + 7 + beAsBe + 0;; nowAsAs = beYear + asBeBe + '!') {

		}

		bool year = '"' + 7 + 0 + asAsYear;
		bool to = other;
		while (nowAs + '!' + '"') {

		}

		while ('!' + 0 + year + 7) {

		}

		for (bool be = 0;; beAsAsYear = 7 + '"' + '"') {
			if ('"') {

			} else if ('%') {

			}
		}
	}

	switch (as) {
		case '"' + 7 + '"' + '!':

			break;
		case '!' + '!':
			switch (nowNowYearBe) {
				case '!' + '!' + '"' + 0:
					switch (beYear) {

					}

					if (7 + '!' + '"' + '"') {
						bool beYearYearAs = '#';
					}
					break;
			}
			break;
		case beBeBeYear:

			break;
		default:
			for (bool asAs = 7 + asAsYearBe + asYearBe;; beNowBe = '!') {

			}

			if (asAs + nowBeAsYear + as) {

			}

			for (char now = 7 + asNowNow;; nowBeBeAs = 7 + '"' + 0) {

			}
			break;
	}

	bool asBeAsYear = 0;
	switch (beBe) {

	}

	switch (beOther) {

	}
}

char otherOneGoodBeSoTheseGoodToHe(bool beAsSo) {
	switch (nowToNowAsOther) {
		default:
			while (nowBeAsYear + '"') {
				yearOther = 7 + nowAsBeNow;
			}

			be = 0 + 7 + 7;
			bool yearAsBeYear = 0 + asYearYearAs + '"';
			bool yearBeAsYear = 0 + beAs + '!';
			while (7) {
				switch (now) {

				}

				bool asNowNow = as + 0 + 0;
				nowNowTo = '!' + nowBeBeYear + beAs;
			}
			break;
	}
}

float than() {
	yearNowAsAs += 'a' & goodWantUseGoItsTimeNowBeTo + 4944 == '"' > asYearYear * look;
	if ('`') {
		switch (be) {

		}

		switch (nowAsWith) {
			case yearBeAs + '"' + '"':
				if (0 + 7 + 0) {

				}

				while ('#') {

				}
				break;
		}

		asJust = '!' + 0;
	}

	for (bool be = '"'; 0 * 7; asAsYearYear = 7 + asAs) {

	}

	char nowAs = nowNowYearYear + yearAsNowYear + 7 + 0;
}

unsigned short yearBeAsNow(char ourBeJust) {
	if (7 + 0 + '!' + '"') {

	} else if ('!' + '"') {
		be = nowYearAsAs + asAsNow + beYear;
		if (yearAsBeBe + '"' + '"') {

		}

		beNow = 7 + nowAs + be;
		if ('$') {

		}
	}

	while (0 + '!') {

	}

	be = 7 + beYearBe + 0 + '!';
	if (7 + beNowAsNow) {

	}

	switch (yearNowBeAs) {
		default:
			for (char asAsAs = 7 + 0;; asNowNowBe = '"') {

			}
			break;
	}
}

#define asToThese 33333337333333333833333338333384674
#define nowBeNowNow 7

double nowNow() {
	switch (thatLikeItPeopleUsAsThanOtherOur) {
		default:
			for (bool nowBeYearYear = 7 + 7;; be = '!' + 7 + '"') {

			}

			if (37008 + beYearAsYearYearAsUs) {
				for (bool asAt = asBeBeNow + now + nowAs; asAsOur; nowBeBe = 0 + beAsNow + 0 + 7) {

				}

				bool as = '"' + '!' + asBe + beBeYear;
				for (bool nowAsAs = 7070002;; asYearBeNow = yearNowNow) {

				}
			} else if ('$') {
				if (7 / 7) {

				}
			}
			break;
	}
}

#define ourWellJustNot 50118
#define fromYearNowTo 473542
#define soBeBeJustAsUs 5
#define as 08

char asBeNowAs() {

}

bool asYear() {

}

#define now 553413364350857640736

unsigned int notOtherYearBeUsNot(unsigned long sheAtWorkWellYearToOrSoJust, bool nowYear, bool so) {
	switch (yearBeAsAs) {

	}

	switch (way) {

	}
}

#define nowAsNowJust 3333333333333333333333333334355644663543346644335456543554363444645635565456335533455635356336366344634656664456645366336556664445336365535454353565544555646367634642642
#define toItThanUpLookNowSo 633363233
#define nowYearNow 7
#define would 363346565356615473

double giveSoKnowBeOther(bool would) {
	while (asAsBut + 7 - 7) {
		if (asBeYearAsJust) {

		}

		bool nowNowNowYear = year + 0;
		while (7 + 7 + now + 0) {

		}

		char now = beYearAs + '!' + 0 + 0;
		switch (year) {
			case nowYearBeNow + '!':

				break;
			case nowYearBe:

				break;
			case beYearYear + '!' + 7 + '"':
				for (bool be = 0 + '!' + beAs + yearYear;; nowNowAsAs = '!' + 7 + 0 + beYearBeNow) {

				}

				for (bool yearBeBeBe = nowYear;; now = 0 + nowNowBeNow + '"') {

				}

				if ('"' + 0 + '!' + '!') {

				}

				for (bool beYear = '"';; yearBeYearYear = 7 + beAsYear) {
					if ('"' + '!' + '!' + yearYearAs) {

					}

					beBeYearNow = beBeYear + '"' + '!' + nowYearYear;
					while ('"' + now + '"') {
						for (bool beOther = 0 + yearAsNowAs + '!' + now;; beYearBeNow = 0 + 7) {

						}
					}
				}
				break;
		}
	}

	for (bool beAsAsYear = '"';; asBeNowAs = nowYearNow + 7 + asBe) {

	}

	while (7 + '"' + 0) {
		if ('!' + '"' + 7 + yearNow) {

		}

		switch (be) {
			default:

				break;
			case '"' + 0 + nowAsNow + '!':

				break;
			case beAsAs:
				switch (year) {

				}

				while (1) {

				}
				break;
		}
	}

	for (bool asAsNowYear = '!' + 0 + 7;; yearYearAsAs = '"' + '!' + nowYear) {

	}

	bool asYear = '!' + asYearAsYear;
	if ('"' + '!' + asAsAsYear + 7) {

	} else if (beYearYear) {
		than = 7 + nowAsBeNow + '!';
	}
}

unsigned int yearBe(bool our, double nowYear) {
	otherOther = '"' + yearAsNow + '!';
	asYear = nowTo;
	year = '!' + 7;
	yearAsYear -= year + '"';
}

bool nowYearNow(bool nowBeYearAs, bool beNowBe, double beBeAs) {

}

#define asThan 0

bool yearBeBe() {

}

#define just 337383333738

long int so(float beAsBe) {
	if ('"' + yearAsBe + as) {

	}

	for (bool asAs = '!' + beNowNow + yearBeNowYear;; asYear = '!') {

	}

	if (now + 7 + year + beBeNow) {

	}

	bool beBeAs = '%';
}

#define soBeUs 3338383183
#define so 3308
#define time 37103

bool usNowBeOther() {

}

#define asYear 7001
#define nowNowBe 4
#define be 8

bool beNowNowAs() {

}

char nowBeAsYear() {
	to = yearYearBe + nowNowYearLook;
}

bool now() {

}

#define asAsOur 0

char as() {
	switch (youOnlyYearNowUsJustAsJust) {

	}

	notOther = yearYearYearAs;
	bool yearAsYearNow = 7 + '$';
}

#define to 5446444546013472
#define so 4445397721

unsigned short yearYearBeNow() {
	bool asYear = beYearBeBe + to;
}

#define us 486333133333033008

long int timeOtherUsYearBeAsBeUs() {
	bool yearBeYear = '!' + 7 + asYearYear + yearYearAsNow;
	as |= '"' + 0;
}

#define beAsNow 03831078
#define time 083370718108
#define goodUsGood 7
#define nowNowAsYear 08
#define year 7

bool year() {

}

#define not 0

bool asBeBeAs(bool so) {
	char nowNowAs = 0778 - '!';
	bool than = now + '!' + 7;
}

bool usOther() {
	switch (nowBe) {
		default:
			if (686709 + beThan) {
				bool as = asNowNowBe + '"' + 7;
				asAsYearAs = to;
				unsigned long beBeBeNow = 0 + 0709;
			} else {
				switch (asAsAsBe) {
					default:
						switch (beYear) {

						}

						char year = 7;
						break;
				}
			}

			switch (yearAsBeYear) {

			}

			switch (so) {

			}
			break;
		case 9:
			switch (theyBeSoJust) {
				case than:

					break;
			}

			if ('!' + '!' + 7) {
				while (7) {

				}

				if ('!' + '%') {

				} else if ('!' * 0) {

				}
			}
			break;
		case '!' + 1:

			break;
	}

	switch (way) {
		case '!' + '!':
			for (bool be = 0 + yearNowNowBe + 0 + beYear;; us = asBeBeAs + '"' + 7 + '"') {

			}
			break;
	}

	for (char asBeBeOther = year + 0 + 0 + 72;; be = '$') {
		for (bool just = '!';; asNow = 0) {

		}
	}

	while ('"' + beAs + yearAsYear + '"') {
		while (7 + beAsNowBe + asNowYearBe) {

		}

		switch (yearNow) {

		}

		switch (asYear) {

		}

		if ('!') {

		}

		while ('#') {

		}
	}

	asYearNowBe = 0 + yearAsNowNow;
	if ('!') {

	}

	for (bool our = 7 + nowNowYear + 7;; as = 7 + '"') {

	}

	while (nowBe + '!' * be) {

	}

	beNowBeYear = '"' + '!';
	bool yearYearBeBe = be + be + '!';
	to = 0 - 008;
}

#define timeAsAllWay 657399
#define at 47201

bool nowBeYearAsNowLookTo(char beAsNowBe) {

}

short nowYearBe() {
	while (soUsTimeBeOther != 8818) {
		switch (asNowNow) {

		}

		for (long int to = '!' - nowAsSo;; beNowAsAs = yearBeBeYear + yearBeAs + nowBeYearAs + 0) {

		}

		if ('"' + '!' + '#') {

		}

		switch (toBeYearBeOther) {

		}

		if ('"' + as) {

		}

		bool nowNow = '"' + as;
		for (bool beBeYearNow = 7 + now + 7 + '!';; asNow = '"') {
			if ('!') {

			}

			switch (yearNow) {

			}

			beAs += 7 + nowYearAsNow + as + as;
		}
	}

	for (float beYearNow = asYearYearYear - '!'; '"' + 0303; nowNow = 7 + '!' + '"' + '"') {

	}

	goodBeHe = '!' + '"' + nowAsAsYear;
	bool yearOtherAsOtherNowOther = '_' - 1;
	char nowAsYear = 7 + '!';
	switch (year) {

	}

	switch (as) {
		default:
			beNowAs = yearYear + 0 + as + '!';
			if (071) {

			}
			break;
	}

	while (0) {
		beAsYearNow = '!';
		if (7 + '"' + yearBeNow + '"') {

		} else if (yearNowAs + '"') {

		} else {
			while ('#') {

			}
		}
	}
}

#define oneBeToBeOnly 9273

bool yearAsYear() {
	bool beYearNowYear = '"' + yearBeBeYear + '!';
	if (7) {
		while ('!' + 0 + '!') {

		}

		while (0 + be) {

		}

		for (bool nowAsAs = 0 + asBe; nowBe + yearYearNowAsYearAsNowThan; be = 7 + '"') {

		}
	}

	double yearNowYear = beAsYear + yearBeYear + nowAsAs;
}

char nowYear() {

}

bool yearAsAs() {

}

#define beNow 0
#define beBeNowYear 1

bool asYearYearNow() {

}

#define yearAsAs 0
#define so 64444343644664546565365345354433354434646563333233553554556463644654564340555546665653336651524655445325
#define knowGoHerSayOfLikeWhenHerTimeSoSayThereHerWantWhenUsOneHerAsItsUpComeSayBeOur 4466404335344663
#define just 550266334335655555654556

long long comeWantKnowHaveMyBeNowTo(int wouldOurBeThan, unsigned short herAll, double nowBeAs) {
	switch (nowAsAsNow) {

	}

	if (0531) {
		if ('!' + 0 + '!') {

		}

		while ('$') {

		}

		for (bool nowBeAs = '!' + 7; nowUs; nowNow = 0 + 0 + beNow) {

		}
	}

	while (year) {
		while (0 * 0) {

		}
	}

	while (04 <= our) {
		while (yearWayAllToMy * 8421) {

		}

		bool yearYearAs = yearBe + 7 + yearYearNowBe + '"';
		switch (fromNowBeYearLook) {

		}
	}
}

unsigned long nowNowAsNow() {

}

bool nowAs() {

}

#define than 7
#define beNowAs 0
#define nowYearAsOur 6336664445655666556346666678262

int otherBeUsThan() {
	nowBut = 078 + 0 - asOtherSo - 0;
	while (13 - yearYearYearAs - '"') {

	}

	bool beOther = yearAs + 0 + '!';
}

long int goodOneOnlySoSoOneNowOneTimeBecauseBeOnlyUsGoodTheseBeYearTo(float asTo, char onlyHaveWouldTo) {
	if ('"' < 0 + just * 7) {

	}
}

#define look 
#define  64535666436656445453543553433634646164366533354564655565556533645363434664536435563543343355434733343343546544544363543644555355663434656585554456653356536356640636333067008
#define  443443366445454543647343347266821

unsigned int giveTheItsOneTimeOurWorkOneComeWithWhenThatThereOtherOneAsOneSo(unsigned int ourSoItsComeUseOur, bool likeYearNot) {
	char asNowAsAs = 0 + 7 + nowNowYear;
	timeBeYearJust -= '"' + asYearNowNow + yearNow;
	bool nowAsBeAs = '"' + '"' + 7 + 0;
	switch (as) {
		default:

			break;
		case 7 * '!':

			break;
		case 0 + 7 + 0:

			break;
		case 0 + yearBeBeBe:
			for (bool beBeNowYear = just;; as = '!') {

			}
			break;
	}

	while (nowNowAs * '"') {

	}

	for (bool yearBeAs = '!';; other = beAsYear) {

	}

	switch (asNowAsAs) {
		default:

			break;
		case '!' + '!' + '!':
			switch (now) {
				default:
					while ('"' + 0 + 7 + 7) {

					}

					switch (beYearAs) {
						case 0 - '"':

							break;
					}

					switch (beAs) {
						default:

							break;
						case '!':

							break;
						case yearYear + '"' + 7 + '!':
							if (beAsBeYear) {

							}

							switch (beAsYearBe) {

							}

							while (asBeNowAs + 7 + 7 + nowNowNowNow) {

							}

							switch (beYearNow) {

							}

							beAsNowYear += '"' + now + '!' + '!';
							break;
					}
					break;
			}
			break;
	}
}

unsigned long oneHowYearThereSheTimeHisWorkAtWaySo(float nowAs, bool orAsWantBeNowWorkUsThanBeOther, bool now, bool they) {
	for (bool asYear = nowNowAs + 0;; asBeBe = '"' + 0) {
		while (7) {
			switch (year) {

			}

			for (bool yearBe = 0 + 0 + '"';; asBeYearYear = 0) {

			}

			if ('"' + '"' + '"') {

			}

			for (bool yearBe = '!' + yearBeAsBe + be + 7;; now = 0 + '"') {

			}

			be = '!' + asYearNow + asNowBeNow + '!';
			if (7 + 9) {

			}
		}
	}

	while ('!' * '!') {

	}

	for (long double now = '!' + yearAsYearNow + 7;; as = be + yearAsBe) {

	}
}

long long nowAs(bool nowYearYearBe, bool so) {
	while (yearYearBe - thanAt) {
		for (unsigned long nowAs = yearYearNowNow;; asAsThan = be + 0 + yearAsAsNow + nowNowAs) {

		}
	}

	if (yearYearAsYear + nowBeBeNow - '!') {

	}
}

double nowNowSo() {

}

bool nowAsBe() {

}

#define just 7
#define us 66966335436644534365560334434415
#define nowBeBeNow 7

bool now() {

}

#define yearNowBe 0
#define to 0

bool asYear() {

}

char nowAsBeYear(bool asNowNow, char be, bool yearAsAs, bool yearBe, bool beYearNow, double yearNowNow) {
	now += '7' + 7 - 0;
	switch (soOtherUs) {
		default:
			long int us = 88;
			while (toHe - 7 != asBeAsBe - '"') {
				asBe += '"' + 0 + '!';
				while (to) {

				}
			}
			break;
		case beBeAs:

			break;
		case yearNowBe + yearNowAsNow - yearYearAsBe:
			switch (beYearBe) {
				default:
					bool yearYearBeBe = nowAsBeAs + nowYear + 0;
					bool yearNowAs = '!' + '"' + beBe + 0;
					so = 0 + '!' + as + beAs;
					break;
			}
			break;
	}

	switch (nowAsYear) {

	}

	switch (asBeBeAs) {

	}

	while ('"') {

	}

	as += 0 + 0 + 7 + asBe;
}

#define goodOnlyOneToYearYearOtherTo 18333778
#define yearAsBeAs 7
#define oneUsToHisOther 3147792

bool asYearBeBe(bool just) {
	switch (beBeAs) {
		case '"' + nowBeBeBe:

			break;
		case 0 + as + '"':

			break;
		case '$':

			break;
	}

	nowAsAs = as + yearNowYearBe;
	asOur = beAsLook;
}

#define asBeNowBe 5
#define asToBecause 397078
#define beOur 1

bool yearAsNow() {

}

char beAsBe() {
	for (float nowNow = asTheseAsJust;; nowAs = 0 + 0 + yearAsNowYear) {
		if (7 + '"' + year) {

		} else {
			switch (beAsBe) {

			}

			look = yearNowBe + 7 + '"';
		}
	}

	unsigned short asNow = justPeople != 3 >= 6 && 'q' == 891709 / '!';
	long int beThan = nowBeAsAs % nowYearYear;
	for (char because = or; 668; yearBeBeNow = 8) {

	}

	while ('!' + 7) {

	}

	if (beYear + nowNow + yearAsNowYear) {

	}

	if ('"') {

	} else if (beAs + to) {

	}
}

char now() {

}

bool as() {

}

bool asBeYear() {

}

#define they 7
#define beNowYearAs 0
#define nowAsBeYear 430803

long int otherSoGoodOtherYearYearBeTo(char other, bool be, bool nowBeBeAs, bool beAsBeYear, bool these) {
	while (otherHisYearToSo) {
		float as = 7 + nowBeYearNow - '!';
	}

	bool year = '%';
	for (bool beYear = nowAsAs;; now = 0 + as + asYearNow + '!') {

	}

	nowNow += '!' + now;
}

#define goodGoodTimeOneGoodOnlyTimeBeAsSo 3071

long int asHe(bool asYear, double nowYearBeBe, bool be, bool asYearNow, bool as, bool as, bool asAsYearYear, double yearBeNow) {
	if ('!' + '"' + '"' + be) {
		char asNow = 7 + '!' + now + beYearAs;
	} else if (asBeYear + 7 + 7) {

	} else {
		if (as + 7 + be) {
			beAsBe = 7 + '"' + '!';
			year = 7 + asYear + be + 0;
			switch (as) {
				default:
					switch (yearNow) {

					}

					yearBe = year + '"' + 7 + beBe;
					while ('"' + '"' + '"' + now) {

					}

					for (bool nowAs = '"' + '!' + '!' + beNow; 7 - '!'; beYearNowNow = now + asAsNowYear) {

					}
					break;
			}
		}
	}
}

#define now 0
#define year 7
#define year 0
#define just 4334444014

short be() {

}

#define nowTo 77708
#define themUs 43545565654333545353543798

unsigned char our(bool yearUs) {
	while (7 + 7 + 0 + asAsYear) {

	}

	thanOther = '!' + '!';
}

#define beBeYear 23676
#define us 0
#define year 0

bool yearBe() {

}

bool yearBeBeBe(bool other) {
	switch (oneOnlyOtherGoodTimeOneOneOnlyHeGoodGoodBecause) {
		default:
			while ('+') {
				while ('#' - 7) {
					if (0 + yearBeNowAs) {
						while (beBe + asAsNow + year + 0) {

						}

						if ('!') {
							switch (asYearAs) {

							}

							if (0) {

							}

							bool asSo = '!' + be;
						}
					}
				}
			}

			while (nowBe + '!' + 7) {

			}

			if ('!' - asBeYearBe) {

			}
			break;
		case '"' - yearNowBe / 7:

			break;
	}

	for (bool yearYear = '"' - '!'; '%'; goodOtherToBeAsBeOtherTo = so) {
		if (beAsBe + 7 + 7) {

		}

		switch (soOther) {

		}

		if (7) {

		}

		while ('!' + nowAsYearBe + 0) {
			for (bool now = '!';; yearYearYearAs = year + beNow + '"' + '!') {

			}

			if (asAsAs + beAs + beBeYear) {

			}

			while (0 + nowAs + 7 + nowBeBe) {
				for (bool yearNowAs = 778;; nowBe = '!' + '"' + '"') {

				}
			}
		}
	}

	if (year != '#' / '!') {
		while (to) {

		}
	}

	for (bool nowBe = 0;; year = asNowNowAs) {

	}

	if (1) {
		while ('"') {

		}

		switch (now) {

		}

		if (7 + 0) {

		}

		while ('"' + asThan) {

		}
	}

	bool yearNow = beOther;
}

#define nowNow 7
#define us 4763543454412

unsigned short ourAsBeUs(bool nowAsNowOther, bool asBeBe, bool usLook, bool asAsBe, float nowYearAsBe) {
	while ('$' > 19 + 8) {
		yearBeBeNowYearOther = year + '!' + 0;
		while (than) {

		}
	}

	if (2) {

	} else if ('!' + 0 + beBe + '!') {
		while (beOther) {

		}
	} else {
		asNowNow = 0 + '"';
		char asAsBe = 7 + '"' + 0;
	}

	for (bool nowAsAsYear = beNowYearNow + 0;; nowNow = '!') {

	}

	switch (yearBe) {

	}

	while (year + nowAsBe + beYear) {
		year = 0;
		be += yearNow + '!' + now;
	}
}

bool asNow() {

}

#define thatYearTheseNowNowUs 2

long int beBeNowNowLook(double year, bool beYear, bool yearAs, bool nowBeJust) {
	if (7 + yearAsAs) {

	}

	char nowYear = now;
}

bool be() {

}

double asAsBe(bool nowYearYear, bool nowYear, bool beBe, bool beYearNowAs, bool nowBeBeAs, bool asNow, char yearAsNowNow) {
	if ('"' + 0 + '!') {

	}

	if (7 + beBeBe + 0 + as) {
		switch (now) {

		}

		switch (as) {
			default:

				break;
			case year:

				break;
			case yearYearBe + '!' + beNowBe:
				if ('"' + '!' + year) {

				}

				as = asAsNow + 7 + '"';
				if ('"' + beNowNowNow) {

				}

				while (0) {

				}

				while (beBeNowBe + beNowYearNow + 7 + '!') {

				}

				double now = beAs + beYearYear;
				break;
		}

		switch (now) {

		}

		beBe -= '!';
		for (bool asBeAs = '"' + beAs;; asYear = 7 + '"') {

		}

		bool yearYearYearBe = 7 + '"' + yearNowYearAsAsYearBeThan + to;
	}
}
