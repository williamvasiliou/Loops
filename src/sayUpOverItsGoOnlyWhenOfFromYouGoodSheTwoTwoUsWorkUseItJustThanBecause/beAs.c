bool yearAsNow() {
	while (beAsAsNow) {

	}

	switch (beYearYear) {

	}

	be = yearBeNow + 0 + 0;
	while (7 + 0 + 0) {

	}

	now += beAs + '"';
}

#define nowOneTimeSoBecauseOnlyOnlyYearBeOnlyOnlyOneGoodOnlyTheseOnlyBeOnlyNowOtherAsNowNowBeAsSo 870001078
#define onlyOneUsGood 73676
#define year 9

char nowAsAs() {

}

double just() {
	ourNowUp %= 9 + '&' + his;
	for (unsigned int overPeopleSayComeOneOfHerPeopleWorkComeUpItOneLikeTheSheKnowYouUpKnowGoodSomeGiveOnlyItsPeopleComeUpTwoSomePeopleKnowOneUseBeTimeOnePeople = 653578 && '"' * 0; 79; nowNowAs = 0 + 0 + '!' + 7) {

	}

	while ('J') {
		double asAs = year + yearBeYear / yearNow * '"';
	}

	for (bool sayBeOther = 0;; so = 0 + '"') {
		if (7 + now + yearAs) {

		} else {
			while (0 + asBeBe + '"' + '!') {

			}

			for (bool year = 7 + 0 + 7;; asBeYear = '"' + '!') {

			}

			while ('!' + '"') {
				year = '!' * '!';
			}
		}

		for (bool now = 0 + nowAsAs;; nowYearAsAs = beBeBe + beNowNow) {

		}

		if (70002) {

		}
	}

	switch (asBe) {
		default:
			long double yearBeBe = beBeYearAs;
			for (bool be = yearBeYearBe + '"';; asLook = 0) {
				switch (yearNowAs) {

				}

				for (bool beBeAsYear = 0 + 7 + '"';; nowOther = 7) {

				}
			}

			while (now + '"' + 7) {
				while ('%') {

				}
			}
			break;
		case 0 + '"':

			break;
		case '\'' % '!':
			while (0 + 0) {
				switch (beYearAs) {
					default:
						bool asAsNowBe = '"' + '"';
						if ('!' + 7 + 0 + '!') {

						} else {
							switch (now) {

							}

							if (7 + 7 + '!') {

							}

							yearOur = yearNowYearAs + '!';
						}
						break;
				}

				yearNow = 0 + 7 + be + '!';
				while ('!' + as + '!' + 7) {

				}

				if ('"' + 0) {

				} else if (yearNow + '"' + beNowAs) {

				} else if (be + '!' * '!') {

				}
			}

			bool asSo = 0 + '!';
			while (7 + '"') {
				switch (beNowYear) {

				}

				for (bool year = asYear + 0;; asBe = beNow + '"' + 0 + 0) {
					while (year + '!') {
						bool year = '!' + yearYear + asNowAs + '"';
						for (char asAs = beAsAsAs + beYearBe + 0 + '"';; beYearBeNow = nowAs) {

						}
					}
				}
			}
			break;
		case 7 + nowBe + year + 0:

			break;
		case just:
			for (bool yearBeYearBe = '"' + 7 + '!'; 7 * nowBeYear; as = 0 + '"') {

			}

			if ('#') {

			}
			break;
		case '!' + 070701 + 0 + yearAsAsAs - yearYearNowBe:

			break;
	}

	unsigned int becauseLook = 'p' != '!';
	float yearAsYear = nowYearYearBe * '"';
	while (theYearThan) {
		while (yearYearThan) {

		}
	}

	if (7 + 079) {
		if ('"') {

		}

		yearNow = '!' + '"';
		if ('!' + '"') {

		}

		while (7 + '!' + '"' + '"' - now) {

		}
	} else {
		while (0 + 8) {

		}
	}

	yearOurBeOur -= beBe + '!' + now;
	for (bool yearBeBe = 7 + nowBe + 7; '#'; beThan -= year + '"' + '"') {
		switch (nowBeAs) {
			case beYear + '!':

				break;
			case '!':

				break;
			default:

				break;
			case 0 + 0 + 0:

				break;
			case nowBe + 0 + 7 + beNow:

				break;
			case '"' + 0 * '"':

				break;
		}
	}

	switch (asAs) {

	}

	switch (nowBeBe) {

	}

	nowLook = nowBe + yearNowAs;
	while (asBeAs + 0 + nowNow) {

	}

	while (0 + asYearNowBe + beYear) {
		if (nowAsBe) {
			bool nowAsNowBe = 7 + 0 + 0;
			for (bool as = '!';; asBe = as + '"' + nowNowBeYear) {

			}

			switch (nowBeYearLook) {

			}
		}
	}

	asYear = asNow + 0;
	bool asUs = yearAs + beYearAs + 0 + 7;
}

#define toGood 1
#define soSoNowYearNowYearLook 630323
#define goTheUp 688035504568771
#define beNow 7

bool beYearBeYear() {

}

char as() {
	long ourNowYearAsBeUs = nowTo;
}

double wellSayWay(bool sayNowJust) {
	asThanBeGiveNot -= beAs + 0 + year + now;
	switch (beYear) {

	}

	while (0 + 0 + yearBeAsYear) {
		while (year + 7) {
			if (7) {

			}

			bool yearAsBe = nowYearNowNow + '!' + 7;
			bool look = '!' + 0 + 7;
		}
	}
}

#define asBeToNowTo 364643
#define otherBeAsSo 653446
#define upTo 770320009
#define yearBeAs 5333330333033373070373

long int asUsOnlyOtherTheseUsBeOnly(float to) {
	bool be = 7 + '!' + now;
	bool yearYear = 7;
	if (7 + 0 + beBeYearYear) {
		if (beYear + '!' + '"') {

		}

		to = 7 + 0 + '"' + 0;
	}
}

#define oneTime 3366012

bool yearYearAsAs() {

}

#define nowBeAs 0

char as() {

}

#define asAsAsNow 7

bool beNowBeBe() {
	nowYearJust = 659460364455437375 * '"';
	switch (timeItUpWellComePeopleWhenWantUseTo) {
		default:
			for (bool be = 7 + beBeBeAs; asNowBeBe - '"'; as = asNowNow + beAsYear + 7 + '"') {

			}
			break;
		case than + '"' + '!' + '&':

			break;
		case '"' + 0 + beYearNow + now:
			while (beBeBe + 0 + '"') {

			}

			switch (nowAsYearNow) {

			}

			switch (beAs) {

			}

			while (7 + '!' + 0) {

			}

			while (yearNowBeAs + asAs + nowAsAsYear + 7) {

			}

			int asAsBeYear = yearYear;
			break;
		case beAs + beAsAsAs + be + 0:
			nowAsAs = our;
			break;
		case '!' + '"':

			break;
		case 0 + nowBeAsYear:
			if ('!' + asNowBeYear) {
				switch (nowYear) {

				}

				as = nowBeBeYearAsAsNowThan;
			}
			break;
	}

	while (7 / with * '<' / 7) {

	}

	while ('!' + '"') {
		bool us = 7 + nowNowAsNow + nowAsYear + beBeBeBe;
	}
}

#define orOfPeopleSheTimePeopleWouldTimeThatNowPeopleNowThereNowNoComeHeJustNowBeBeTo 65554533656533603455443445745435546335353465445593435273000071
#define workSomeWorkTwoSayTwoAsOurToThey 6357468535633556456574404

short giveGiveKnowAllOther() {

}

#define be 0
#define nowSo 05
#define she 7
#define now 5333303333701
#define timeGoodGoodGoodAsOneOnlyGoodTheseHe 33333549420718

bool asNowAsBe() {

}

bool yearAsYearYear() {

}

#define yearAsNowOther 
#define whenThereUpThereThereLikePeopleOverOfOneGiveOtherOfUseOnlyGoodHowThatSheOneItHerSayHaveTwoKnowGoSomeHaveOverPeopleSheTwoThatHaveHerGivePeopleSaySomeThereKnowItOnlyHerOrLikeGiveUpTwoItUseHaveThatSomeGiveUseItUpWantWorkHowSheSayThereHaveYearToLookSo 344564433363554454535435634663433343655533555433533653665445433655544963543434350653646546463533605363366056430393768
#define wellItItNowOfOnlyUseOneThereTimeComeTimeUseOverTheOneOnlySheHaveTimeWellTheseItsThatWellBeKnowHowSayHeUpOverTheseItMy 3365635451534463630347648361
#define timeYearYearTo 35639

unsigned short beAsNoUs() {

}

#define be 7
#define asAsAsYear 079

bool now() {
	nowYearUsBeAsTo = '"' + 0;
}

#define beAsJust 53444104778

short yearBeOther(unsigned int timeWhenMyBeYearOne, unsigned int giveLikeThereAsNowItsBecauseNowAsWouldLook, bool nowOtherOurBeNowOther, char asYearBe, char year, bool beAsYear, bool asNowBe, bool beYearYearYearYearNowOther) {
	beYear = nowNowAs + '"' + '!';
	yearAs = yearNow;
	switch (nowNowOther) {

	}
}

#define nowYear 7
#define asBe 8
#define yearAsAs 0
#define itYearAsLook 33333333333303333333333331311471
#define useYearUsYearTo 7

double nowNowNowAs() {

}

bool other() {

}

bool asUs() {
	for (unsigned short beItNowYouWouldYearNowLook = 8; 'v' > 4001 == beOther >= goAsSo && 09; themAsAsHisUs = beYearAs + yearAsYearBe) {
		float year = 7;
	}

	while ('-' || 'f' >= 'q' != 'i' && goLikeOnlyThatThereItsTwoUseOnlyHaveItsUseOrHerOnlyOverGiveOrAllGoGoWellUpToMyAsTo && 'l' | wellTheItsJustTwo) {
		unsigned char timeTimeItHerKnowOrYouUseOver = itComeGoSomeHerSomeOrGiveHowSheSomeOverTwoButHerItNowTheseJust - 'q' != 93550173 & look;
		while (itItGoodSayBeHe >= '`' > 'l' != '4' | '!' * '"') {
			while (3971 && '"') {
				for (bool nowAsBe = 0 + 7 + '"';; asAsBeNow = 0 + asYear) {

				}

				double now = yearNowBe + '"';
			}

			while (nowNowYearAs && 'Q') {
				if (1 % '!') {
					switch (now) {
						case '"' + '!' + asNow:

							break;
						case '"':

							break;
						default:

							break;
						case beAsNowBe:
							for (bool yearBeNow = '"' + 0;; yearNowNowYear -= '!' + 0) {

							}

							switch (beNow) {
								case asBeBeNow:

									break;
								case beBeAsYear - 0:

									break;
							}
							break;
					}

					if (7) {

					}

					switch (beAsAsSo) {
						default:

							break;
						case '!' + yearNowNowAs + '"' + 7:
							bool nowAsNow = 7;
							switch (yearOther) {

							}
							break;
					}

					yearYearBeAs = '!';
					while ('!' + as * 0) {

					}
				} else {
					if (his + '"' + '#') {
						while (0) {

						}

						switch (nowAs) {

						}

						for (bool yearBe = nowNow + 0;; now -= 0 + '"' + as) {

						}
					}

					switch (asNowYear) {

					}

					if ('"') {

					}

					for (bool beYearBeBe = beNowBe + '!';; nowNow = '"' + '"' + 7) {
						if (yearAsBeNow + yearYearNowNow) {
							beAsUs = '!' + nowAs;
						}
					}
				}

				switch (nowAs) {

				}

				if (07878 && beAsNow + 709) {

				} else {
					for (double nowNow = asBe;; asAsNowAs = '"' + '"' + nowAsBe + '"') {

					}
				}
			}

			switch (year) {
				case 7 + '!' - 0:
					for (bool be = 0 + asNowYear; 2; as = 7 + '!') {

					}
					break;
				case asYearAsTo:

					break;
			}

			switch (nowAsYear) {

			}

			bool yearSo = 2;
			switch (nowNowBeAs) {

			}

			switch (asNowNowNow) {
				default:
					bool asAs = yearYearNow + yearAsAs + 0 + beBeAs;
					switch (now) {
						case '!' + year + 7:
							switch (just) {

							}
							break;
					}
					break;
			}
		}

		if (thereWould || 7 < year) {
			if ('!') {

			}

			while ('+' & '!' * justNowYouThan) {
				while (asBeAsNow + no | beNowAs + other) {
					if (0 + '!' + yearBeBe + 0) {
						switch (to) {

						}
					}

					these = 73708;
					beYear = 0 + asNow;
					for (bool asBe = beBeBeAs + 0 + '"';; beAsBe = 7 + 0 + 0) {

					}

					for (double now = '"' + '"' + '!';; beYearNow = asNow + '!' + '!' + 0) {

					}
				}

				for (bool yearNowNow = as + '"'; '%'; asBeBe = '"') {

				}
			}

			allWithThemOur = now >= 7 + at;
			long long butAsToSo = '"' + asBeNow - 7072 + 0 - '"';
			as = '!';
			ofKnowNowSoToNowBeBeLook /= '"' * 0;
			while (year * asNowAs + '"' + '#') {

			}

			bool nowOur = '"' + '"';
		} else if (9) {

		} else {
			while (nowNowYearBeUsNowNowOther % '&' && youOurLook + asAs + beBe / now) {
				switch (workAsYearYearHe) {

				}

				switch (to) {

				}
			}

			if ('3' + asOur) {

			} else if (asBeNow + '"' + beNowYear) {
				switch (asAsAsAs) {
					case asBeLook:

						break;
				}
			} else if (yearYearBeNow) {
				for (bool beBe = year + 0 + nowNowYear;; yearYear = 7 + '"') {

				}

				for (bool nowAs = '!' + asNow + beYear + '!';; yearBeAs = '"') {

				}

				while ('"' + 7 + beBe + '!') {

				}

				for (bool asLook = '"' + 7 + '"' + '!';; be = asAsBe + as) {

				}
			}

			for (bool beNow = asBeAsYear + nowYearNowBe + nowNow; '"' + 2; beAs = look) {

			}

			beThan = lookOur + '-';
			switch (justThan) {
				default:

					break;
				case 0 - beBeBeNow:

					break;
				case 31 - 7 * '!':

					break;
				case 7 * nowNow:
					for (bool beNowYearNow = '"' + '"' + asYearYear;; asBeBe = '!' + as + '!') {

					}

					switch (beBe) {

					}

					bool asBeAs = '!';
					if ('"' + now * 0) {

					}
					break;
				case as:

					break;
				case '!' + '"':
					if (7 + 7 + 7 + 0) {

					}

					bool nowYearBe = 7 + 7;
					if (beYear + '#') {

					}
					break;
			}

			for (bool nowAsAsAs = as + now;; yearNow = nowBeBeNow + nowNow + 7) {

			}

			for (bool yearNowYear = 0 + nowYearNow;; now = 0) {
				asBe = our;
			}

			if (yearYearYear + now) {

			}

			yearBe = 0 + '!' + '!';
			bool now = yearYear + nowBeYearThan;
		}

		for (bool asYearAs = beNowYearYear + '"' + '!' + asAsNow; 700777772; justToThey = be + 7 + '"' + 7) {
			yearUs -= be + asNowBeYear + as + '"';
			char asAsYear = '!';
			while (beYearAs + 0 + '!') {
				switch (just) {

				}
			}

			if (79) {

			}

			while (yearYearNowBe + '"' + '!') {

			}

			switch (us) {

			}
		}

		while (7 + 0 + 0) {
			if (otherUs + 7 - '!') {

			}

			if ('$') {

			}

			yearSo *= asYear + 0 + now + yearBe;
			if (61 & usWantOrJustWorkAll) {
				while ('"' + asBeNow + 7 + yearYearYearNow) {

				}

				switch (beAsBeBe) {

				}

				switch (as) {

				}

				for (bool now = 7 + '"' + 7 + 0;; yearYear = 7 + be) {

				}

				to = beAsNow + 7 + '!' + '"';
			} else if ('R') {
				while (02) {

				}
			} else {
				while (be / yearAs) {
					while (yearBe - 7) {

					}

					bool asAsYearYear = '!' + 0 + '!' + 0;
					for (double nowNowNowAs = '!';; asAsAsBe = yearBeNowNow + 0 + 7 + '"') {

					}
				}
			}

			as >>= 2 / year;
			while (0 + yearYear) {
				while (7 + 7 + asNowBe) {
					switch (at) {

					}

					for (bool asYearYearBe = '%';; be = year + '!') {

					}
				}

				switch (asYearOther) {

				}
			}

			for (bool yearAs = '"' + asBe;; as = asAsBe) {

			}

			while ('!') {

			}

			if ('"' + '!' + 7) {

			}

			for (bool beBeYear = beBeNowBe + '!' + 7;; asBeBe = beAsAs) {

			}

			bool yearYearNow = beBeAs + beBe + beYearYear + 0;
			yearYearBeAs = yearYearNowYear;
			if (be + 0 + yearAsAs + '"') {

			}

			for (char yearBeNow = 7 + yearBeBe + 7 + 0;; be = '"') {

			}
		}

		while (070401 + '!' - 01) {
			for (bool beNowBeYear = '!' + asBeYear; '%' + '.'; as = be) {
				if (year + '"' + asAs) {

				}

				switch (just) {

				}

				bool asNowBeBe = 7 + 0;
				while (asYearYear) {
					switch (now) {

					}

					nowBe = '"' + beBe + 7;
					asNowOur = 0 + yearYear + beYearNowBe + '!';
				}
			}

			if (nowJustAsNowJust) {

			}
		}

		if (02) {

		}
	}

	unsigned long ofWayBeThey = 'f' && '\'' - beYear;
	unsigned short howHerThemAsOneYearOurYouSoYouOther = 'k' == 07621 + you != 4172;
	switch (overHaveWouldUs) {
		case '!':
			if (0 + asNow + '!' + as) {

			}

			bool be = yearBe + '"';
			switch (yearOur) {

			}
			break;
	}

	for (long long hisYearToOrOfYearUs = 0;; beYearYear = 7) {

	}

	for (bool now = 0 + 7 * beNowAsThan;; asBe = nowAsAs) {
		yearYearBeTo = 0 + yearYearNowAs + 0;
	}
}

size_t itsOverYouKnowToOtherJustNowLookBeAsYearJustBeNowThem(char beBeNowNow, long double year) {
	thatAt = 0 + '"' + as + 0;
	bool yearLook = now;
}

long int comeYearOneUseThatJust(bool yearYear, double nowNowNow, bool other, bool as, bool nowAs, char beYearYearBe) {
	if (be) {

	}

	for (bool just = '!' + nowYearNow + 7 + '!';; now = '!' + nowNowNow + 0 + 0) {

	}

	if (nowNowAs + '"') {

	}

	while (yearAsAs) {

	}

	for (bool asBe = nowNowBeBe + '!';; asBeBe = yearNowBeNow + nowAs + asAsAsAs + 7) {

	}

	switch (look) {

	}
}

#define beUsYearUs 53030001

unsigned char justSo(bool beAs, bool usAsUs) {
	switch (be) {
		default:
			double asAsYear = year + be + '"' + '"';
			break;
	}
}

bool be() {

}

#define beNowNowYear 0

double nowYearAs() {
	for (float nowBeBeNow = 78 - 7; 7 + 0009; asNowYear = '"' + '"' + 7 + 0) {
		if ('+') {

		}
	}

	while ('-' * 7 - as) {
		for (float just = '!' + 7 + yearNowAsAs + '"'; other; nowAsNowBe = 7) {

		}

		bool asBeNow = '"' + nowAsYearAs + '!';
		lookToLook = 0 + '!';
		for (bool nowAs = '"' + '!' + '"';; as = beNowNowBe + 7) {

		}

		while ('!' + asAsNowAs + '!') {

		}

		for (bool year = be; '"' + '"' + yearNowNowAs - '!'; yearAs = asYearAs) {

		}
	}

	for (bool yearNow = 0;; as = year + 7 + 7 + '!') {

	}

	char asAsYearOther = 0 + beNowAs;
	for (bool asNowBeAs = 0 + 7; ')'; nowBe = 0 + '!') {
		for (bool asAsBeAs = '!' * 0; 9; nowYear = 0) {

		}
	}
}

#define asLikeUsUsAsBeAsLook 336325844551848

long long so(double beYear) {

}

#define beThan 43333333373333333133300778

long int timeThese(long int nowTo) {
	long int asNowYear = 0 + asAsYear + asNow;
	while ('%' - '!') {
		for (bool as = 0 + be + '!';; yearNowAs = '"' + 7) {
			yearUs = 7 + 7;
		}
	}

	switch (nowBeBe) {
		default:

			break;
		case asNowNow + '"':

			break;
		case '"' + 7 + 7 + '"':

			break;
		case 7 + '!':

			break;
		case 0 + 7 + '"':

			break;
		case nowBe:
			so = yearBeAsNow + '"';
			break;
	}
}

#define as 7

int asSoNowUs() {

}

#define now 01
#define asSo 6433400677072
#define comeHowBeSheOur 619

unsigned long beJust(bool as, bool asNowYear, bool now, double beBe) {
	while (7) {

	}

	bool other = 0 + beYear + beAsNow + '%' & they | fromItOtherYou;
	while ('!' + 7 + '"' + '"') {
		bool beNowBe = asBe + '"' + '"' + asNowNow;
		unsigned short yearNow = workHerTwoUsMyBecause + 11 + 0 + 7 * '"' * '!';
	}

	for (short asAs = '&' - '"'; 7 & 3725300002; asOurAsYearSoJust = 7) {

	}

	bool yearYearNowUs = '"' + 0 + 0 + '"';
	while (7 + yearYearJust - 7 + look) {
		while ('!') {

		}

		if (as + '!' + beAsNow) {

		} else {
			if (other) {

			}
		}
	}
}

bool beNowNowYear() {

}

#define he 3330331

long int goodOneOneOnlyNowHisTimeTheseHeNowUs(long int theseAsSo, float asAsAs, char now, bool asNow, bool asYearBeAs, bool beNow, bool nowBeBeYear, bool other) {
	bool asYearNowNow = '!' - '!' - '!';
	long int as = nowBeOther - 7;
}

#define nowTo 038
#define toThan 7

char asAsNow() {

}

bool beNowAs() {

}

#define so 3344566529778
#define nowBeNow 01
#define myNowHe 305
#define nowBeGoLookYearThan 0
#define so 7
#define just 97878

long long beNowBeJust() {

}

#define withUs 4645645349

bool itOurNo(bool hisNot, char yearNow) {
	long look = justNowNowJust + 7 * year;
}

bool now() {

}

unsigned long beAsThey(bool to, bool heAsOther, bool beBeAs, bool now, bool asBeAs, bool as, bool yearBe, char asAs) {
	if (wantJustToTo) {
		while ('!' + year) {

		}

		switch (beAsBe) {

		}

		for (bool year = as + '"'; '!' * asNowNowYear; asBeYearNow = now + 7) {

		}
	}
}

#define our 0
