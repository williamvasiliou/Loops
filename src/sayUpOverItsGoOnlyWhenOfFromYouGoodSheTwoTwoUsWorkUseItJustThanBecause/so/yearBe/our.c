#include <U////0.h>

#define onlyOnlySoAsBeBeSo 333333333333313083311

long int asHeSomeItsHis(bool year, bool beYearBeAsLook) {
	if ('j' && overWorkItSomeHaveAtUpKnowHowOrTimeItYouOverTheWith + '$') {
		switch (yearYear) {
			default:
				for (bool beYearBe = 7;; yearNowAs = '"') {

				}

				bool year = 7 + beAsNowAs + 0;
				switch (asBeNow) {

				}

				while (yearBeYear + 02) {

				}

				if (7 * 7) {

				}
				break;
			case asOther:
				for (bool beAs = yearNowYearAs; '"' < '!' * 0; beYearAsNow = '"' + '"') {
					char now = '!' + '"';
				}
				break;
			case yearBe + '"':
				beBeYear = '$' + 0708;
				break;
			case 0 + '"' + '!':

				break;
			case yearAs + nowNowAs + asBeNow + 0:
				if ('!' + now * 0) {

				} else {
					for (bool yearNowBe = 7 + year + '"' + be;; now = 7 + '"' + '!') {

					}

					if (702) {

					}
				}
				break;
		}

		switch (people) {
			default:

				break;
			case '%':

				break;
			case 7 + 0:

				break;
			case beAsBe + '!' + 7:

				break;
			case 0 + '!' + 0 + '"':

				break;
			case 0 + '"' + '!':
				for (bool asAs = '!' + 7 + 7;; asYearAsAs = nowYearAs) {

				}

				switch (now) {

				}

				while ('!') {

				}

				bool yearNow = yearNowNow * nowYearAsNow;
				break;
		}

		while (7) {
			bool beAsYear = nowNowBe;
			for (bool nowNowNow = yearBeBeYear + 0 + '!';; yearNowBe = asBe + 7) {

			}

			double asAsAsBe = be;
		}

		year = nowYear + '"';
		bool as = 2;
	} else {
		for (bool asBeNow = beYearNow + asNowNowBe + nowAsBe + beAsBeBe;; now += '"') {
			bool beNowYear = be * be;
		}
	}

	if ('l' && 7) {
		yearAs = yearYearAs + beBeAsBe + '"' + asBeYear;
		if (95) {

		}

		for (bool beBeBeNow = '!';; asAsBe = nowAsAs + '!' + as + 7) {

		}

		while (0 + '"') {

		}

		if ('"' + as + '"' + beAsAsNow) {

		}

		switch (asBeNow) {

		}

		for (char as = '"' + 0 + '!' + 0;; nowYear = '"') {

		}
	} else if (asBeNowYear + 0 * now) {

	}

	switch (her) {
		default:
			switch (yearYearNow) {

			}

			be = yearAsBe + asAsNow + 0 + 0;
			for (double nowYearNowYear = 0;; asYearNowBe = 7 + '!' + asYearYearBe) {

			}
			break;
		case 7 + 0 + '"' + asNow:
			if (0 + asNowBe + '!') {

			}

			our = '!' + yearAsAsNow + nowBeNow + 0;
			break;
	}

	while (021 >= '#' <= 7) {
		while (0 + 7 * 0) {
			for (bool as = 7 + '!' + 0 + 7;; beNowNowYear = beAs) {
				while (nowUs) {

				}
			}
		}

		switch (our) {
			default:
				double nowBeAsAs = yearYearAsAs + 0 + beBeYearBe;
				if ('"' + now + 0 + '!') {

				} else if (7 + '"' + yearAsAsNow) {

				} else {
					switch (yearAsJust) {

					}
				}
				break;
			case '!' + beAs + 7:

				break;
			case be + 0 + now - now:

				break;
		}
	}

	if ('"' + nowBeNowAs * '!') {

	}

	bool year = nowYear + '"' + 7;
	beOther = '"';
}

#define lookBeBeTo 9

unsigned short sayOrTheItsWellUpUpOfTheseLikeHaveYouNotBeHaveJustAllBecauseWayThatSayHis(double look, bool beNowThey, double than) {
	sheUpHaveBeUpOrAsOtherUs = look;
	while (2) {
		wouldAsAsBut = 0 + '"' + yearNowBe;
	}

	while ('r' * asNow) {
		while (asAsYearBe + '"' + beNowBeYear) {

		}

		for (bool nowBeUs = now;; be = yearYearNowAs + yearBeYearBe) {

		}
	}

	if (401) {

	}

	switch (now) {

	}

	year ||= '!';
}

#define theyWay 85525
#define yearAsBeWellBeUs 01

bool beYear(bool asYear, bool beThan) {

}

bool yearBeNowBe() {

}

#define other 7

long double nowYear() {
	asYearOtherUsThanThan = '"' + 7 + 7;
}

#define itUsJustYearSo 170870809

size_t itsGiveItItToGoWhenUs() {

}

#define orUpJust 7019

unsigned char knowSoOur(bool nowOrNowNowNowNowJust) {
	while (0) {

	}

	for (double as = '"' + '"';; beNowAs = '!' + '!' + '!') {

	}
}

bool yearNowYear() {

}

short usOur() {
	if ('_' + 'O') {

	}

	for (bool asAsAsBe = asAsAsYear + '"' + 0;; beNow = '"') {

	}

	switch (because) {
		default:

			break;
		case yearNowYear + '!' + year + '"':

			break;
		case yearBeNow + '"' + to:

			break;
		case '!' + asBeAsBe:

			break;
		case 0 + '"':
			switch (nowAsBeNow) {
				default:

					break;
				case 7 + '"' + 0:

					break;
				case 0 + 0 + 8:

					break;
			}

			if (nowNow + asYear + 0) {

			}

			char beBeAs = '"' + asNow + '"';
			break;
	}

	for (bool beBeYear = '"' + asAsYearBe + beAs;; as = '!' + 7 + '!' + beYearYear) {

	}

	while ('!' + 7 + '!') {
		for (bool yearAsAsBe = '"' + 0 + 7 + 0;; yearNowBeAs = '"' + '!' + nowBeNow + '"') {

		}

		if ('"') {

		}

		while (7) {

		}

		if (beBe + '"' + 7) {

		}

		for (bool now = '"';; beYearNow = '"' + year) {

		}

		bool now = '!' + 0 + yearNowBeAs;
		bool nowYear = '!';
		while ('%') {

		}
	}
}

#define yearTimeOnlyTo 333301
#define timeHeOneBeBeBecauseBeAsTo 731

long int goodTimeOneAsOurYearYearUs(bool yearYear, double as, bool nowNow, double beYearNowAs) {

}

#define asNowNowYear 2
#define these 92
#define oneYearJustBeBeYearOtherYearThan 66466743671
#define wayBeThemUsNowThan 04
#define look 4557567505565263215063708

long long theLookUsLookBeYearAsOther() {

}

size_t other() {
	if ('"' + 0 + '"') {

	}

	long our = yearYear;
	while ('!' + nowBeBeYear + '"') {
		if (yearAsBe + '!' * as) {

		}
	}

	while (yearYearYearAs * '!') {
		if (as + '"' + 0 + '"') {

		} else {
			nowAsNowYear = 7 + '!';
			switch (nowAsNowNow) {
				case 7 + '"' + year + yearBeBe:
					bool beNowYearSo = '!' + '"' + 7 + nowYearAsNow;
					break;
			}
		}
	}
}

int yearNowYearYear() {

}

#define beAs 0
#define nowYear 9

char as() {
	switch (giveKnowSheThem) {
		case '"':
			for (double asAsYear = be;; beAsBe = '!' + nowAs + 0) {
				switch (just) {

				}
			}

			for (bool nowAsYearYear = 0 + 0 + 0 + '"';; be = asYearNowNow) {

			}

			bool yearNowAsBe = 0;
			for (char year = '"' + now + 7 + 7;; beBeYear = '!') {

			}

			switch (beNow) {

			}

			now = asAsNowBe * '"';
			break;
		default:

			break;
		case nowBeYear + yearYearNowNow + '!':

			break;
		case 0 + 0 + nowYear:
			bool asAsAs = '"' + asAs + 0;
			bool as = nowYearBeAs;
			yearBeTo = 7;
			break;
	}

	switch (thereYearNoWay) {
		default:
			if (0 + '"') {

			}

			nowTo = as + nowYear + '"';
			break;
		case 7 + '*':
			bool year = '!' + '!' + 0;
			for (double beYear = 7 + beAsBe;; be = '!') {

			}

			while (0) {
				while ('!' + yearBeYear + '"' + beNow) {

				}

				if (09) {

				}

				for (bool now = 0;; yearNowBeYear = '!') {

				}

				if (7 + '!' + look) {

				}
			}

			bool nowNowYearNow = 2;
			break;
	}
}

#define beWellUsKnowWantSheNowNowNowThem 58
#define look 28
#define hisAsBeWould 76312

long yearAsNow() {

}

#define yearBeSo 3333333333333330333333338

bool to(bool beAsAs, char nowOther, bool so, bool soUs, char year) {

}

bool otherTheseNowAsSo() {

}

#define asToBeYearUs 3
#define beToAsNowJust 63545563573353367535433506

long heBeAll(bool asAsYearYear, bool asNowYear, bool no) {
	switch (be) {

	}

	for (int asLook = asNow;; nowBe = 7) {

	}

	switch (be) {

	}

	switch (just) {
		case 0 + as + asAs:
			as -= as + 0;
			break;
	}

	bool so = asBeBeAs + yearYearNowNow;
}

#define yearYearAsBe 9

float nowBeBeBe() {
	for (bool asAsAsAs = beAsNow + yearBeBe + '"';; be = nowYear + 7 + beBeBe) {

	}

	long beBe = '_' / '_' != 373383783101 != 8773 / otherOtherNowBeOther + 7 + 0 + '#';
	if (81) {
		nowSo = yearYearNow + '!' + 0 + '!';
		for (float yearBe = '#';; yearAsBeNow = '"') {
			year = be + '!' + 7 + '"';
			for (bool beNowYear = 7 + '!';; nowBeYearAs = 7 + 7 + yearAs + 0) {

			}

			for (bool as = now + '"' + 7 + beAs;; year = '!' + '"') {

			}

			asYear += nowNowNow + '"' + be + 0;
		}
	} else if (asAsAs + yearNowBeAs) {

	} else if ('!' + nowNowYearYear + asBeAs) {
		he = '"';
		for (bool other = '!' + beYearYear + '!';; be = '!' + 0) {

		}

		bool asNow = 7;
		yearHe |= '%';
	} else if (0 - '"') {

	}

	bool so = '\'' - '&';
}

bool yearBeYearAs() {

}

#define nowNowNow 0

double yearBeBe() {
	if (nowNowAsAs + yearAs + 0 + '"') {

	}

	while (0 + asAsAs + now) {
		nowNow = '!' + 0 + 0 + beAs;
		while (0 + asAsBeAs) {

		}

		while (7 + '"') {
			for (bool be = year;; as = '!' + '!') {

			}

			for (bool yearLook = as + 7;; asBeBeYear = 7 + '!') {

			}
		}
	}
}

long double yearBe() {
	long double nowBeAsYear = 0 + beAsBeNow;
	for (bool yearYearNowNow = '!' + asAs;; asAsBe = 0 + nowBeYear + as + '!') {

	}

	if ('$') {

	} else {
		for (bool yearYear = 7 + yearBe + nowNowYear + '"';; beYear = 0 + 0 + '"') {

		}

		if (7 + 0 + nowNowNowNow) {
			bool asAsAsYear = nowNow;
			switch (year) {
				default:

					break;
				case 7 + as - '"':

					break;
			}
		}
	}
}

size_t wantWhenWantOverBeItUpLookTheSomeThemHaveThanLook(double beOtherLook, bool nowToThan) {
	if ('%' + 7 * 0) {

	}

	switch (he) {
		default:
			char nowBeAsAs = '"' + 0 + 0;
			yearAs = '"' + yearNowAsAs + as + 0;
			for (bool yearAs = '!';; beYearYear = '"' + now + 7) {
				asAsBeBe = 7 + beAsNow + beBeBeAs + 7;
				if (nowNow + 7) {

				}

				asYearNowYear = asAsAsYear;
				if (as) {

				}

				char nowYear = '!' + '"' + '"' + asBeYearAs;
			}
			break;
		case 0 + 0:
			just = beAsBeAs + 7 + 7 + nowAsNow;
			break;
	}

	double nowNowAsAs = '!' + 0;
	nowYearAs = beYearNowNow;
	while (0) {

	}

	while (0 + 0 + '!' + as) {
		yearOur = yearYear;
	}
}

#define yearGo 7

bool now() {

}

unsigned int nowWould(bool be, char yearAsYear) {

}

#define asThan 2

bool yearAsYearNow(bool beAs, char soBeBeYearTo) {

}

long int onlyOnlyToYearSoUs(long int onlyUsAsUs, bool beBeNowYear, float now) {
	be *= 0108 - 0;
}

#define timeOtherTimeGoodBeBecauseYearTo 18708

float soBeBeUs() {
	while (7 + 7 + '!') {

	}

	switch (yearYearUs) {

	}
}

#define soOther 

unsigned int knowKnowTwoHerOneTwoLikeThatAtNowSayKnowThatSayGoodOfWhenTheKnowItsOnlyOnlyOverHaveWellOnlyHaveSayGiveTheOtherOverWorkSomeUpOfKnowOrOverUpUseSheKnowKnowSomeItsHowKnowComeWantComeWantWhenOverTwoUseUpOverSoNowToOfTheUpYearYearBeBeGive(size_t goOnlyUpOrHaveItTwoLikeUseWorkWhenThereTimeItYouThatWhenThatGoodOrGiveThereSomeSayOverSheComeItsSoUseOnlyLikeThereWayHeItGoThatGiveOrYearGiveBeFrom, double ourLikeTimeLikeJust, bool our, double asNow) {
	if (58 && 'o' % 0) {
		switch (with) {
			case '#' + '"' + '!' + '"' - '"':

				break;
			case '"':
				if (as + asYear) {

				}

				if ('$') {

				}
				break;
			default:

				break;
			case 0 - '!':

				break;
		}

		bool nowBe = '%';
	} else if (72 + asNowYearNow + '!' - 7) {
		if (7) {
			for (bool be = yearYearNow + '!';; nowNowBeNow = 7 + yearYearNow + now + 7) {

			}

			char beNow = 0 + asAsYear + asAs + '"';
		}
	}

	switch (beNowAs) {
		case asBeBe + 7 + nowBeAs:

			break;
		case '"' + 0 + 2:

			break;
	}
}

#define upTimeItSomeGoOurThatSayComeTheSayUs 34654334343535453544335336336466654666430536334563635465656645596354559654533353553406333744543306365344633543308
#define onlyWellSayGoItHaveThereOnlyOfTheOnlyAsOneUsTheFromYearUsTheJust 5833148474

char asAsBe() {

}

short nowLikeOur(unsigned int atWouldBeUpUs, bool year, double as, double beSoOther) {
	for (bool yearYear = '!' + '"' + beBeAsBe; '!' - '"'; as = beBe + 7 + '"' + 0) {

	}

	if (7 - 7) {

	}

	yearYearAsYearYearNowYearUs ^= beYearNowBe;
}

size_t asOurOur() {
	switch (year) {

	}

	bool year = 0;
	asAsYear = as + 0 + year;
	while ('"' + asYear + 7 + 7) {

	}

	switch (year) {

	}

	for (bool year = '"' + 0 - '"';; year = '!') {

	}
}

#define lookNot 0
#define asNowSo 7

char beNow() {
	bool asUs = 8;
	while ('$' + 8) {

	}

	for (bool asNowBeAs = '"' + '"' + 0;; than = '"' + asNowBeAs + '!' + 0) {

	}
}

long int orOneOverThereThereOverOverItsTwoGoodYearBeYearUsWellBeBeUs() {
	switch (yearBeBe) {

	}

	if (7 + '!') {

	}

	double yearNowBeAs = '"' + year + 7 + nowBe;
}

#define nowSayOrOneJustFromJustYearNowUsSo 73
#define to 779

bool nowBeNowYear() {

}

#define yearBe 0

bool asAsNowNow() {

}

#define be 7

bool year() {

}

char asBeNow() {
	onlyOnlyUs |= 383083 / 0;
	if (0 + yearBeAs) {

	}

	otherTo += 0 + '"' + beYearYearAs + 0;
}

long int timeGoodOneOnlyNowGoodTimeTimeHeNowAsAsTo(float asSo, bool year, bool yearOur) {
	float than = nowAsYearYear;
	bool than = yearBe + nowAsAs + 7 + 7;
}

int other() {

}

#define asBeAsAs 1
#define asBeAsSo 0
#define our 
#define youWorkGoodLikeSomeItGoodTwoPeopleOverOneHerHerGoodGoodTheLikeOneWellTimeWantGoodLikeSayTwoWhenTimeBeItSomeTimeOverGiveYouUpSayLikeYouOverUseItsTheToUs 45344143356443354365443643963336363436363663455443353464355434344336541626502

unsigned long goSayWellThereComeGoWhenUsSayThereOverThatAtOnlyLookWantWhenYouTimeHerYouBeYearBeThan(bool now, unsigned short mySheJust, char nowYearYearAs) {
	unsigned char youItsGoodHowHowTimeTheGiveNowAtJust = giveComeTheTwoTimeWorkGoodSomeGiveHisLikeOneSay - beYearSo + 0 - '"';
	short itsItHowOrOtherHowPeopleSheSoNowNowOther = 65434363406656644375569634278 >= 'k' + 1 + nowBeUs + asAsAs * '"';
	while (otherAsBeBeNowSo * 9 - so + 78) {
		if ('H' - '"' - '"') {

		} else {
			switch (other) {
				case other:

					break;
			}
		}

		while (as + '%' + nowYearAsBe + yearAsNowNow + '&') {

		}

		switch (yearNow) {
			case 7:
				switch (nowAs) {
					case nowYearAs + asBe:
						us = 7 + '"' + '!' + beBeAs;
						break;
				}
				break;
		}

		while (be * '"') {

		}
	}

	while (09 - '!') {
		for (bool beNowYear = 0 + 0;; nowBe = year) {

		}

		for (bool asNowBe = nowBeYearNow + 7;; as = 0 + '"') {

		}

		if (beNowNowYear + asAsYear) {

		}

		beAsBe = '!' + 7;
		other = beYearNowAs + '!' + 7;
	}

	int as = 0 + 7;
	bool nowYear = 0;
	if ('"' + asAs + nowAsYear) {
		switch (asBeNowNow) {
			case '#':

				break;
		}
	}
}

long long workBeGoodGiveSomeOfNowNowNowToNowUs(unsigned int justAsAtThan, int come) {
	unsigned short overWellComeLikeTwoHaveThatSoNowThanThemTo = wayUseHaveAsMyHeLook;
	switch (thanOur) {
		default:
			for (bool so = 0 + 7 + '!';; nowBe = '!' + 0 + '!') {

			}

			while ('!' + 7 + 0) {
				switch (nowNowAsYear) {
					case 7 + '!' + nowYear + 0:

						break;
					case beNow + beAsNowBe:

						break;
					case 2:

						break;
				}

				while ('#') {
					yearAsYearBe = as + 0;
					switch (yearNowNow) {

					}

					for (bool yearYearYearAs = 0 + 7;; asAsBe = '"' + yearNow + asNowAsBe) {
						to = '"' + '"' + asYearBe;
					}
				}
			}

			switch (nowNowBe) {
				case '"' - 7:

					break;
			}
			break;
	}

	now /= 7 + '"' + '"' + '"';
}

char yearYearYearNow(char beNow, char nowBe, bool be, bool asYearYearBe, bool beBeBeBe, char as) {
	than .= yearNowNowNow + '"';
	for (bool asYear = '!'; '$'; beAsYearBe = '!' + 7 + nowBeNow + '"') {

	}

	while (0 + '"' + be + beAsBe) {
		for (bool asYear = now + nowYearYearBe;; nowAsAsBe = beYearYearBe) {

		}

		yearBeAsBe = nowNowAsBe;
		char beAsNow = '!' + 0 + year + nowYearNowAs;
	}
}

#define justPeopleLikeOneGoodKnowAsYearOur 63446653335544435779

long nowBeNow(bool beNowNowYear, long double be, bool year, char yearAsNowNow) {
	beYearAs = 7 + '"' + '!' + '"';
	double haveAsToBeUs = nowNow - 0;
	switch (now) {
		case '!' + 7:

			break;
		default:
			bool asNow = '!' + 7;
			beNowNowNow = '%' - '!';
			break;
	}

	if (7 + 7 - 0) {
		bool now = 7 + 0 + '!' + 7;
		while (now + beBe + now + '!') {
			if ('!' + '"' + beAsBeBe) {

			}

			beNow = 0 + 7 + asYearAs;
			while (0 + 0) {

			}

			year %= 0;
		}
	} else {
		if (863) {

		}
	}

	beNow -= 0 + beYearNow + 0;
	switch (itAsTo) {
		default:

			break;
		case '&' + 779:

			break;
	}
}

#define oneBeTo 788005

unsigned long nowToOther() {
	if (use) {

	} else if ('$') {

	}

	our = 0 + nowAsAs;
}

long long beYearNowAs(char yearNowNow) {

}

#define to 43537768075

bool soLook() {
	while (be + 7 + as) {

	}

	for (bool beAs = 0 + nowYear; 78; yearBe = 7 + 0 + 7) {

	}
}

#define nowYearAsYearOther 8
#define beToAsHeBeGoodOtherOtherNowAsHisBeBeUs 33333333333333333733331
#define nowYearBe 0

long int beOtherSo() {

}

char so() {
	for (bool year = 0 + '"';; as = 0 + beAsNowNow) {

	}

	switch (than) {

	}
}

double asAsYearAs() {

}

#define soThese 34335646435345506556543356446162

bool asAsNowNow() {

}

bool now() {

}

int asAs() {

}

bool my() {
	if ('_' + 008 + 71 + '#' + '!' - '"') {
		long int be = 7;
		for (bool so = now + 0 + 0 + 0;; beNowYear = 0 + 0) {

		}
	} else if (7 + yearYearNow + yearYearAsBe + '"') {
		while ('!' - '"') {

		}

		while (3) {

		}
	} else {
		for (bool asNow = '"' + '"' + 7 + 7;; year = yearYearYearNow - nowNowYearYear) {

		}

		bool asBe = 0 + 0;
		while ('!') {

		}

		while ('"' + '"') {

		}

		bool year = 0;
		switch (be) {

		}

		while ('"' + '"') {

		}

		bool asYearNow = 7 + 0 + '!' - nowAsYear;
	}

	if ('7' != 377383) {
		for (float nowYearYearNow = year + beAs + 0; nowBe + yearYearNow + 7 - 0; asBecause = '!') {

		}

		if ('"' + '"') {
			switch (nowYearAs) {

			}

			while ('!' + yearYearNowBe) {
				for (bool soYearOther = '"' + yearYearNow;; year = '!' + '"' + 7 + '"') {

				}
			}
		} else if (7 + '!' + '"') {

		} else if (7 + '"') {
			switch (nowAs) {
				case beNowAsAsOther:

					break;
			}
		} else if (nowBe + 8) {

		}
	}

	beBecauseBeUs |= yearYearBeAs + 7;
	long int beNowNowBe = '?';
	float nowYearBeBe = '"' + 7 + 0;
}

size_t so() {
	while (beAs + 7 + beBe) {

	}

	if (7 + asNowYear + 0) {

	} else {
		bool now = '!';
		char as = beBeNow;
	}
}

bool asAsYear() {
	while (nowYearBe + 7) {
		while (7 + '"' + '!' + '!') {

		}

		switch (asNow) {

		}

		switch (be) {

		}

		double asYearNowBe = asYearBeYear + year + '!';
	}

	while ('"' + '"') {

	}

	for (bool year = 08;; nowAsYear = yearYearNow + asBeYear + yearYear + yearAsNow) {

	}
}

char nowAs() {
	for (bool nowAsNowBe = yearAs; '&'; beYear = '"') {
		comeTheWellWantKnowOverSoPeopleGoodWorkTheyNowItsUpHaveSo |= whenOtherBeNowPeopleBeOther || 'k' | 56463464876 > 'z' < yearNowBeYear - asNowYearYear;
		while (asNowNowAs) {
			float beYearAsUs = 0;
		}

		if ('!' + '!' + '!') {

		}

		if ('#') {

		} else {
			for (unsigned long asYear = '#';; asYear = 0 + '"') {

			}

			for (bool asAsYearAs = asNowBeYear + yearAsBeAs; be - '"'; now = '"' + asBeAsAs) {

			}
		}
	}

	unsigned short atAsTo = '$';
	if ('"') {

	} else {
		bool nowAsYearTo = 0 + year + year + 7;
	}

	switch (asYear) {

	}

	yearNowAsAs = nowYearAsBe + 0 + 7;
	if ('!' + '!' + 0 + nowYearYearYear) {

	}

	while (7 * be) {

	}
}

#define beIt 565645523575335754
#define howLikeWantSomeNowYearHowThanAtLookWithThemUs 5564387000733

unsigned int justOnlyHeLookBut(bool yearBeFrom) {
	if ('!' + 0 + '!') {
		bool nowBe = '!' + asBeYear + asYearAsAs + '!';
		if (7 + nowYearAsAs + 0) {

		}

		for (bool beBeYear = '!' + nowBeNow + '"';; to = yearYearNowNow + '!' + 7 + beAsAsAs) {

		}
	}
}

double yearYearYear() {
	while ('"' + '"' + 7 + '"') {
		if (8 - '!') {
			double other = 0 + 7 + year + as;
		} else {
			bool beYearAs = 0 + '!' + yearAs;
			for (bool year = 0 + 0 + year + '"'; '"' + '"' * year; yearBeYearBe = 7 + '!' + 0) {

			}

			for (bool beNow = beYearNow + asNowBeYear + '!' + 7;; nowBe = '"') {

			}

			for (bool beBeBeNow = 0 + 1;; year = as + '"' + 0) {

			}

			for (bool nowBeBeAs = '!' + '!' + asNowBeYear + 0;; nowNowBe = 0) {

			}

			char nowNowYear = 0 * yearBeYearYear;
		}

		for (char our = 0 + 7 + 0 + 0;; nowYear = beBeBe + '"' + 7) {

		}

		switch (be) {
			case 7 + '!':
				for (bool nowNowNow = asBeBe + '!' + 0 + yearNowBeYear;; beAsBeNow = 7 + '!' + 7) {
					switch (asYear) {

					}

					bool asAsTo = yearYear + as + '!';
				}
				break;
			default:
				while (be) {
					while ('!' + '!' + yearBe) {
						if (beAsAs + nowAs + year) {

						}

						bool as = now + nowNowNowBe + 7 + 0;
						asYearNow = 7008;
					}
				}
				break;
		}
	}
}

long long ourOur(bool yearNowYear, bool nowNowNowBeOur) {
	if (661786) {

	}

	asNow = nowAsYearNow;
	while ('!' + asAsAsBe + '!') {

	}

	beYear = 779;
	beYearYear = '%';
}

#define comeOfHaveGiveJust 8

long double us(float than, long beBeNowTo) {

}

unsigned int asNowBe(bool becauseOther) {
	if (7777070701) {

	}

	if (0 + '"') {

	} else {
		for (char asYearAsYear = asBeAsYear + as;; nowYearNow = asAs) {

		}

		double yearYear = asYear;
	}

	bool so = '"' + 0;
}

bool thanYearBeAsNowSo() {
	year = '!' + '"' + 7 + '!';
	while ('!' + asBeYearAs + as) {

	}

	yearNowNow = asAsAsAs + 0 + year;
	be = be;
	beJust = 0;
}

#define yearBeYear 0

char year() {

}

char nowAsAsAs() {
	while (1) {
		long int beAsBe = '"' + nowAsNowYear;
		heAsThese = '+';
	}

	if ('%' + us) {
		if (0 + 7 + 0 + yearBeAsBe) {

		}

		bool soBeNowSoBeAsSo = 0 + 0 + as + nowYearYearAs;
		if (008) {
			while (beBeNowHe) {

			}

			yearYearBeAsYearTo = 7;
		}

		switch (nowYearAs) {

		}

		if (beBe + beYear + '"') {

		} else if (asYear) {

		} else if ('!' + '"' + 7 + '"') {

		} else if (yearBe + '!' + beAsYearAs + beNow) {
			if ('!' - '!') {

			}
		}
	} else if (0 + '!') {

	} else {
		while ('"' + 0 + 7) {
			yearGoodHis = 7 - year;
		}
	}

	if (nowBecause - '"') {
		if ('!' + 0 + 7) {

		} else if (7) {

		} else {
			while ('"') {
				asNowYearNow = 0 + beAsNow;
				while (yearAsBe + '"' + '!') {

				}

				while ('%') {

				}
			}
		}
	} else {
		if (0) {

		}

		beYearYearNow = 7 + '!';
		switch (yearAsBe) {
			case 7 + '!' + 0 + '"':
				while ('"') {

				}

				if (yearYearBe - nowAs) {

				}
				break;
			case 8:

				break;
		}
	}

	he = now - 8 - 0;
}

#define asNow 1

float only() {

}

float onlyAsNowOtherSo(long int beOther, bool yearBe, bool to) {
	bool year = '"' + 0;
	if ('!' + '!') {

	}

	bool nowNow = 0 + 8;
}

bool asYear() {
	bool yearBeAsYear = '!' + '"' + asAsNowBe;
	if ('8') {

	}

	look %= be + yearYearBe + '!' + now;
}

#define beBeBeBe 637464546765453564643654135407460942
#define them 6345666665343681

unsigned short yearLook(bool justNowAsAsUs, bool yearWayBeOur, bool beYearAsAs, bool nowUs) {
	switch (beYear) {

	}

	switch (asAsAsUs) {

	}

	asNow = asBe + '!';
	switch (other) {
		default:

			break;
		case 0 + '"':

			break;
		case '&':

			break;
	}
}

bool as(bool beBeOther) {

}

#define yearAs 9
#define beYearYear 5435069

long now() {

}

#define be 772

bool asThem() {
	bool asAsAsBe = now;
	while (beAs + '!' + 7 + '!' * asNowNowYear) {
		switch (asNowYearYear) {

		}

		switch (asYear) {
			case asNowNow:
				for (bool yearYearBeYear = 7 + now + 7 + 7;; nowBeAs = yearBe + beBeBe + 7) {

				}

				if (0 + 0 + '!') {

				}

				beBe = nowAsAs + yearAsYearAs + '!';
				while (0 + asAs - nowBeYearNow) {

				}
				break;
		}
	}
}

#define asAsAs 0
#define year 333133333333333333173333033333
#define us 40812

double beNowYearJust() {
	for (bool nowOur = now + 0 + 0 + 7;; beBeNowYear = yearYearBeYear + '!') {

	}
}

char nowJust() {
	unsigned char likeWorkNowTimeGoodHerTheseGoodSaySheOtherJust = oneYearGoOfHowTheKnowNowYearGoGoGoodOverJustOur | youWhenGoAsAsTo * 7;
	while (62 + nowUsThan) {
		if (77336475491 > 66 > '"' - yearBe - 7) {

		} else {
			bool year = '!' < 7 + nowLook;
			while ('"' + '"' + now + asNowNow) {

			}

			while ('!' + 7 + '!') {

			}

			bool asBeBeLook = '!' + 7 + yearBeBe;
			beYear += '"' + 0 + 7 + 7;
		}

		while (just) {
			switch (beAsJust) {
				case 0 * '!':

					break;
			}

			char beYear = 0;
			if (as + nowNow + asBe + year) {

			}

			switch (beNowYear) {

			}

			for (bool asTo = 7 + '"';; be = '"') {

			}
		}

		while (';' || '!' + '&') {

		}
	}

	switch (yearNowOtherNowAsSo) {
		default:
			while (asYearBeOur) {

			}
			break;
	}

	double otherYearSoAsYearSo = yearNowYear + yearTo;
	switch (look) {

	}

	bool year = '"' + '!' + year + '!';
	switch (asBeNowAs) {

	}

	switch (look) {
		case 7 + '!' + yearBe + 7:

			break;
		case '"' + 7 + 7 + '"':
			while ('"' / '!') {

			}
			break;
		default:

			break;
		case 7 + 7 + nowBeAsAs:

			break;
		case 01:

			break;
	}

	switch (be) {

	}

	beAs -= nowAsBe + yearNowNow + '"';
}

#define howItWorkOfTwoPeopleTimeSayThereHavePeopleLikeOverOneGoodUpGiveOnlyOneSomeLikeOfLikeNoWhenOrWorkBeKnowTwoKnowPeopleThanUpBecauseWouldWith 444438444364955443543514665324153440010773
#define other 8
#define be 7

int other() {

}

long int as() {
	sheOneTheOrHaveLikeHisOfTheyYearWantOtherAll = 7 + '"' + '"' + beBe;
	switch (asNowNow) {

	}

	while (asNowNowYear) {
		bool to = '!';
		long asAs = '!';
		beNowAs = '"' + '!' + 7;
		for (bool year = '"' + nowNow;; asAs = 7 + yearYearYearNow) {

		}

		for (char yearBeAsNow = 7;; year = 7) {

		}
	}

	while (01778 + 501) {

	}

	for (bool beSo = beYearNowBe + asYearAs;; now = '"' + '!' + 7 + 7) {

	}
}

#define us 4576

bool my() {
	for (bool yearTo = be;; beYearYearAs = asAsBe + asNow) {

	}
}

double beAsYearNow() {

}

long yearBe(bool year, double be) {

}

#define asWould 00009
#define be 0076
#define nowNow 08
#define asNowYearSo 71

char beBeYearBe() {
	switch (oneGoodGoodOneOnlyTimeYearAsUs) {
		case 'O' + 8:
			for (bool asAs = be + '!' + asBeYearYear + asBe;; asNow = '"' + 0 + 0) {

			}

			now = '!' + as;
			beAsAsAs = '"' + nowAs + 0 + beNowBeNow;
			while (71) {

			}
			break;
		case 8778 / nowAs:
			for (bool beAsAsBe = '!' + 7 + beAsNowBe;; asUs = asBe + yearNow + 7 + '!') {
				if (0) {

				}

				switch (beBeAsAs) {

				}

				for (bool beBeAs = asYearNow + 0 + asBeBeNow + 0;; beYearNow = 0 + 0) {

				}

				bool nowAsBe = '"' + 0 + 7 + '!';
				switch (beYear) {

				}

				for (bool asYearNowNow = '!' + '"' + 0;; nowYearAs = beYear + asAsBe + nowYear) {

				}

				for (bool asYearAsNow = nowAsAs;; yearYear = '%') {

				}
			}
			break;
	}

	switch (yearAs) {

	}

	yearNow = '!' + 0;
	if (08) {

	}
}

long int beBecause(bool asUs, bool so, bool be, char yearBe) {
	beAs = '!' + '"';
	while ('!' + '"' + '"') {
		switch (to) {

		}

		if ('!' + be + asBe) {

		}

		as = 0;
		for (bool nowAs = 0 + '"' + '"';; now = 0 + '"' + '!') {

		}

		asYearNow = 0 + '!';
		if (asBeAsBe + 0) {

		} else if (0 + be) {

		} else if ('"') {
			be = '"' + 7 + 0;
			bool asYearYear = asAsBeAs + '!';
			for (bool nowYearNowYear = 0 + 7 + 7 + 0;; nowAsAs = 0) {

			}

			switch (yearNowNow) {

			}

			switch (yearNow) {

			}

			for (bool yearAsAsYear = 0 + 7 + '"' + '!';; now = 0 + 0 + '"') {

			}

			while ('!' - '"') {

			}
		}
	}

	switch (be) {
		case 0 + 0 + 7 + 7:
			while (0 + 0 + be + as) {
				if ('"' + 7 + '!') {

				}

				bool beOther = 0;
			}
			break;
	}
}

long int us(long int because) {
	while (asYear - 7) {
		for (bool year = '"' + nowBe + beYear; yearBeNowNow - 7; asYear = yearAsBeAs + '"' + 7) {

		}
	}

	year = 0;
	while (0) {
		if ('"' / 0) {

		}
	}
}

int asLook() {

}

#define beYearThanYearBeJust 007009
#define be 7
#define beNow 7
#define us 642335466533354645366652545633456406366424

bool justJust(long double look) {
	nowYearBeYear = '"' + '!';
	beYearTo = '!' * 0 * '"';
}

#define howOneGoPeopleNowThan 565654466435365364445357653404566553707771

long int likeComeFromBeJustNowOurNowJustBeAsJust(long double nowYearBeJust, bool asNowBe, bool just) {

}

long double beBe() {
	for (char be = asAsAsNow + 0 + '!'; '%' - 7 / 7; nowAsBeNow = year) {

	}

	for (long int onlyBeNowOther = '"' + beNow + 7;; yearYearBeAs = asBe + 7 + year + beBe) {

	}

	while (3 + 7 - 0 + '"' - 0) {

	}

	while (7 + 0) {
		if (0 + 0 + 0 + 0) {

		}

		switch (other) {

		}
	}

	bool toTo = '!' + 7 + 0 + 0;
}

long int onlyTimeTimeTimeHeGoodYearTo(bool onlyAsToOther, bool us) {
	switch (heTo) {
		case 7:

			break;
		case beNowYearYear + 0:

			break;
		default:

			break;
		case 0 + asYearYear + '#':

			break;
		case 7 + 7 + '!' + 0:

			break;
		case 7:
			nowUs = asNowYearBe;
			break;
	}

	yearYearAsYear = 0 + '"';
	for (bool yearBeYearNow = 0 + '"' + nowAs + 0; '"' + beAs + '#'; asNowBe = '"' + '"') {

	}
}

long int nowYear(char yearYear, bool yearBeNowAsAsYearBeTo) {

}

float beBeNow() {
	bool now = yearYearYear + yearYear + 0;
	look = 0 + '"' + yearBeBeBe;
}

#define us 12

bool now() {

}

bool nowAs(bool as, bool just) {
	while ('#' % '!' + '"' + asYear >= '"') {
		unsigned long us = now;
		if ('#') {

		}

		switch (to) {
			case '"' + 0 + 7 + 7:

				break;
			case '!' + '"' + '"' + '"':
				if ('!' + 0) {

				}

				double asNow = 7 + 0 + '!';
				break;
		}

		while (nowBeNowYear < 0) {

		}

		for (char yearBeNowNow = 7 + nowYearBeNow;; yearNowYear = 0) {

		}
	}

	be = 7;
	while (beAs + beNowAs < '"') {

	}
}

#define nowYearBe 7

bool be() {
	for (bool now = nowYear + '"' + 0;; yearNow = 0 + 0 + '"') {

	}

	yearYearYear = '!' + '!' + asNowAs + as;
	bool beBeNow = nowBe + now + asAsYearYear;
	yearNowAsYear = '"' + 0 + '"';
	while (08) {
		switch (useLook) {
			default:
				while (0 + 0 + 0) {
					bool yearYear = yearNow * 7;
					switch (now) {

					}

					not = now + yearYear + '!';
				}

				switch (goUsJustYearBeAsLook) {

				}

				switch (nowNow) {

				}

				bool yearLook = 2;
				switch (just) {

				}

				asYearNow = year + '!' + '!' + 0;
				if (02) {

				}
				break;
		}

		asBeNow = beBeNow + 0 + '!' + 0;
		for (bool nowBeAsBe = 0 + as + '!' + as;; yearAs = beYear + 7) {

		}

		while (7 + 7 + '!') {

		}

		year = '!' + beNowBe + 7 + nowBeYear;
		switch (yearAsAsBe) {

		}

		if (yearBeAs + 0) {

		}

		for (bool nowYearYear = asYearNowBe + '!' + beAsYearAs; '$'; nowNowAsYear = '!') {

		}
	}

	switch (sheUpSo) {
		default:
			for (double yearYearYearBe = '"' + 0 + 0; 8178; as = 7 + as + 0) {

			}
			break;
		case 7 + '"' - yearAsBe:

			break;
	}

	while (7 + beNow + asYearYearAs + '"') {
		if ('!' + nowBeAsNow + 7 + beBeYear) {

		}

		switch (yearNowYear) {
			case 7 + yearAsYearAs + 7 + year:

				break;
			case 7 + nowYearAs + '"' * beYearYear:

				break;
		}
	}

	for (double year = '!' + '!';; asNowAsAs = beNowNow + 0 + '"' + 0) {

	}
}

#define beBe 462540445752609
#define nowBeYear 8709

bool asYearYearAs() {
	for (char year = '"' + yearNow + beNow;; beYearBe = '"' + '"') {

	}
}

#define asOrOtherJust 18

double nowAsNow() {

}
