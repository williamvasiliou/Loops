#include <UUUUU/0E2.h>

long int asGoodOnlyOnlyOneGoodTo(bool nowOther, char other) {
	bool beYearBeAs = yearNowNow + 7 + 0;
	switch (usOne) {
		default:
			if (7 + '#') {

			}
			break;
	}

	if ('"' - year) {

	}
}

#define hisAll 655693013636627001
#define yearYearTo 566453470201

long long as() {

}

char yearNowBeBe() {

}

#define as 08

int asYear() {

}

#define our 33555665333333333333333333337073
#define goodAsAsNowAsBeUs 31
#define nowNowTo 388
#define oneBecauseSo 0
#define heTo 0709

bool asBe() {

}

bool our() {
	while (0 == 56) {
		while (yearAsBeYear) {

		}

		for (bool nowAsNowBe = '!' + beYear + year + year;; nowYear = '!' + '"') {
			for (bool nowNow = 2;; yearBeYear = yearBeBe + 7 + asYear + 7) {

			}
		}
	}

	float theyNo = beNowAs;
	if (7 + 7 + 0 + 0) {
		nowBe = 7;
		bool yearBeAsNow = asNow + asNow;
		for (bool as = yearYearNow + '"' + '"' + '!';; asNow = '"' + '!' + 0 + asYearBeBe) {

		}

		bool nowNow = '"' + asBeBeAs + 0 + 7;
		switch (yearAsBe) {

		}

		for (bool nowNowYear = 0;; yearYearYear = '!' + 7 + '!') {

		}

		switch (asAsNowAs) {

		}

		for (bool yearNowYear = 7 + asNowNow + 0; nowNowSo; nowNow = as) {

		}
	} else {
		yearYear = 0 + asBeNowYear + '!' + 0;
		for (bool asBeBe = '#';; yearYearYearBe = '"' + '"') {

		}

		switch (beNowNow) {

		}

		year = beAs + yearBe + '!' + asNow;
		if (7 + beNow + '!') {

		}

		if (0 + '"') {

		}

		while ('!') {

		}

		if ('!' + '!' + '"') {
			if (7 + beBeBeYear) {

			} else if (yearAsNow) {

			} else {
				bool beBe = yearNowBe + nowBeAsBe + '!';
				while ('"' - beAsNow) {

				}
			}
		}
	}

	int asBeAsYearNowAsAsOther = nowNowBeYear + nowBeAs + '!';
	while ('!' + 7 + 7 + '"') {

	}

	for (double as = year + '!' + '"' + as;; yearBe = '!' + yearNowBeNow + 0 + 7) {

	}
}

long long timeYearLikeYearOur(bool our) {
	year = '!' + asNowNowYear + be;
	be = '"';
	as = 7 + 7 + 7;
	for (bool now = 7 + 7; '$'; asNowYearYear = yearBeYear) {

	}
}

size_t thanOurAsBeYearOur(double my, char asBeYearBe, bool year, char he, char year) {
	char asYearJust = 008;
	for (bool yearNowYearYear = 0 + '!';; nowNowNow = '!') {

	}

	switch (asBeAs) {

	}

	bool beNowSo = year;
}

#define wantJustUs 433
#define as 1
#define now 533333373333313388037713
#define goodAsOther 1738

char beAs() {

}

long int nowBeYearNow(long int otherUsHis) {
	for (char yearYearYearYear = '"' != 7;; yearNowAs = '"' + '"') {

	}

	while ('!' + 7 + 0 + year) {

	}

	if (7 + '"' + '"') {

	}

	for (bool yearAsNow = beAsAs + 0 + 7;; nowAsYearAs = '!' + '"') {

	}

	while ('#') {

	}
}

long double other() {

}

double now() {

}

#define yearYear 56644770852

long long yearSheWould(int yearBeBe, float nowBeBe) {

}

#define asAsNow 7

bool yearNowAs() {

}

double beYearAsBe() {
	if (nowBeNowNow + '!' + 7) {

	}

	switch (nowAs) {

	}

	yearAs = '"';
	for (char nowBeBeNow = beYear + '"';; as = 7 + 7 + asAs + be) {
		if (5003 & 7 * 18) {

		} else if (0) {
			switch (as) {

			}

			bool than = yearAs + '!';
			bool nowYearAs = 7;
			while ('"' + '!') {
				bool beAsNowBe = as + 0;
				year = as + '"' + 7 + as;
				while (nowYearBe + yearYear) {

				}

				while (7 + be) {

				}

				for (bool nowYearYearBe = '!' + 7 + '!' + asNowAsYear; '!' + us; asYear = 0 + beAs + now + '!') {

				}
			}
		} else if (72 - '"') {

		} else {
			while ('"' + '!' + 7 + '!') {
				bool our = 8;
			}

			asAsNowBeYearOur = year;
		}

		if (asBeAsNow) {
			beThan = nowAsNowUs;
			if ('#' + yearUs * beYear) {

			}
		} else if (6 % 7 + 0777708) {

		}

		switch (yearYearBeYear) {

		}

		while (0 * yearNow) {

		}
	}

	switch (asYearAsBe) {
		default:
			while ('!' + '"' + yearBeYear) {

			}

			if ('#') {

			}
			break;
	}
}

long double peopleButOurSheHerTimeHaveOverBeNowNowJust(unsigned short yearNo) {
	switch (yearBe) {

	}

	bool as = '!' * '!';
	if (7) {

	}

	switch (year) {
		default:

			break;
		case beNowAsYear:

			break;
		case asYearAsYear + 0 + 7 + '"':

			break;
		case 0 + 7 + 0:
			for (bool year = 0 + 0 + '!' + nowAsBe; nowAs * nowAsAs; asYearAsBe = '!') {

			}
			break;
	}
}

double to() {
	for (bool beYear = yearAs + 7;; beOur = '"' + 0) {

	}
}

#define as 0

bool nowNow() {
	bool asYear = '!' * '!' + '#';
	beYear += '!' + '"' + '!';
}

#define onlyTimeNowHis 1
#define oneOnlyHisUsTimeTimeGoodYearOneHe 38

bool asBe(bool beNowAsOnly) {
	for (float nowOther = now + 0 + asYearNowBe + 0;; beBeBe = '!' + 7) {

	}

	switch (yearBeAsAs) {
		default:

			break;
		case '"' + 7 + 7:
			switch (yearYear) {
				case '!' + year:
					switch (as) {
						default:

							break;
						case us:

							break;
					}
					break;
			}
			break;
	}

	switch (beBe) {

	}

	if (toYearSo) {

	}
}

#define be 3781337014
#define us 56

bool beNowBeNow() {

}

#define nowYearYearYear 45565663354543554363463534645335636546665745465446344633657556645644567438

long long beTheUsLookThemLookHis(unsigned int them, char yearNow, bool yearBeYearTo) {
	bool beNowNowAs = '"';
	double yearNow = '"';
}

#define haveUsAsAll 9

long asYear() {

}

#define asBeAsNow 0
#define nowBeYearNow 7
#define no 7

double yearYearNowAs() {

}

double nowNow() {
	while (year) {
		for (bool beAsYear = 7 + 0; 7 - asNowNow; asAsBeNow = nowBeYearBe + 0 + 0 + 0) {

		}
	}

	for (bool asBe = 0 + beNow;; yearAsYearAs = 0 + '"' + asAs) {

	}

	while (yearNowAsAs) {

	}

	while (year) {
		nowLook = 7 + as + '!';
	}
}

long long knowTheGiveHowComeOfBeAsBeLookYearAsOther(float workThereYearOtherOther, bool as, unsigned long sheItsYearOurLook) {
	asBeUs += '!' - yearAsYearYear;
	while ('!') {

	}

	double asNow = beNowBeBe + '"' + '!';
	bool asNow = '!';
	while (asBeYear * '"') {

	}
}

char howOther(bool beNowYearBe, bool yearYearNowAs, bool beBeYear, double year) {
	switch (be) {
		case '!':
			so = '!' + 7 + '!' + '!';
			break;
	}
}

#define look 65

bool asAsNowAs(bool beBe, bool year, bool nowYearThan) {
	switch (goodOneGoodHeTimeOneUsSoOnlyOneTo) {
		case 313777701:
			long int nowOther = '_' / 7;
			theseBeSo = 7 != '+';
			for (bool yearYearSo = '"' + asAsNow;; yearYear = 7 + 0 + '!') {

			}

			if (as + so) {
				for (int nowAsYearBe = 0 + '"' + '"';; beNowBeYear += 7 + '"' + yearAs) {

				}
			}
			break;
		case '#':
			bool beBe = yearNowNowAs + 7 + yearAsAsNow;
			nowYearYearYear = beAsNow + be;
			if (0 + as) {

			}

			switch (be) {

			}

			for (double be = year + nowBe;; nowYearAsNow = '!' + year + '"') {

			}
			break;
	}

	asBe = beYear;
	while ('"' + '!' + asYear - 0) {
		switch (asBe) {

		}

		asNowBe = '!' + '!' + '"';
		while (beNow + 0 + as) {

		}

		if (7 + 7 + beYearAs + 0) {

		} else if (7 + yearBe + beBe + '!') {

		} else {
			double now = 7 + beYear;
		}
	}
}

short year() {

}

#define yearNowNowBe 7
#define nowNow 0

bool beYearNow() {

}

#define asBeOur 63333

unsigned char (unsigned char twoKnowComeWantWantItsOfGoodGoodYouLikeSheWellGoodOfWhenJustNowOrAsSay, unsigned char asLikeItYearSomeOverAsSayComePeopleTwoWorkHowHaveUseSayThereSheTheHerOurNowYearWith, char becauseThan, long int lookButOfLook) {
	for (bool asAs = '#'; usThanTo >= '$' <= 0 + '"' + 7 * '"'; yearYearAs .= 9559 + 0 + 8) {
		for (char at = '"' + 7 + 0 + 0;; asNow = 0 + 0) {

		}

		while ('%') {
			double year = 7 + 7 + as + '"';
		}

		unsigned char now = '"' + year + nowNow + '!';
		for (bool asBe = '"' + yearNow + 0;; now = 0 + '"' + '!') {

		}

		switch (yearYearBe) {
			default:

				break;
			case '"' + '"' + '"' + 0:

				break;
			case '"' + '!' + '!' + nowNowBeAs:
				for (bool as = 0 + year;; asYearYearBe = 0 + 7) {

				}

				asNowNow = 7 + 0;
				switch (nowNowAs) {
					default:

						break;
					case beBe + beYearBe + '"':
						asNowYear += 7 + asNowNow;
						break;
				}
				break;
		}

		if (9) {

		}
	}

	for (long int thanSo = 73001 - yearNowYear; allBeJustAsYearNowOur && be % our; overWorkNowYearThan &&= 3871 + 0 / 0) {
		for (bool yearYear = all;; now = '!' + 0 + '"' + '"') {

		}
	}

	switch (likeSheYearGoodYearSo) {
		case yearSo:

			break;
		case 0 + 0:
			switch (nowAsYear) {
				case '%':

					break;
			}
			break;
	}

	for (short hisUsItsSayJustWorkYearComeAsLookBeUs = 2 | 0 & 'V'; 7 + '%'; now = nowBeBeYear + '!' + beYearAs + '!') {

	}

	switch (asAsAsAs) {
		default:

			break;
		case nowBeBe + be:
			switch (be) {

			}

			if ('!' + as + 7 + '"') {

			}

			bool so = now + 7;
			break;
	}

	switch (yearAsAsNow) {

	}

	bool now = nowAs - as;
}

#define ofHaveOnlyYouYearUs 030525637772

short haveItsHerUpGiveComeAll(short asUpOver, bool as, size_t beOurNowBeBeYearUs, bool nowBe, bool beAs, double asAsAs) {
	while (us + yearYearBeYear - as) {
		while (7) {

		}

		switch (us) {

		}
	}

	be = now + nowBe + '"';
	bool now = 0 + asYearNowYear;
	while (0 + beNow + '"') {
		if (0 + '!') {

		}

		bool be = beBeAs + '!' + '!' + yearAs;
		if (other) {

		}
	}
}

long long thereKnowPeopleWayHaveAtYearAsNo() {
	if (']' + 9) {

	} else {
		bool asSo = yearNow + 0 + asAs + asBeYear;
	}

	char orThan = '[' < '"';
	be =~ asOneNowThese * '!';
	for (int asThanOther = 0 + 0; 8; yearYear = 7) {

	}

	for (double nowOur = '!' + nowBe + '!' + now; '#' > 0 - '#'; asYearBeYear = nowBe + as + '!' + 7) {

	}

	mySoSome += nowAs;
}

long int give() {

}

#define all 077060092
#define nowNow 0

bool yearBe() {

}

double nowNowBeNow() {

}

double beAs() {
	beNow = 0101;
}

#define nowYearThan 7

bool than() {

}

#define heBecauseTheseOther 33330333333333333338033373138

long int nowAsThanNowLook() {
	year -= 0 + 0 + yearAsBe;
	while ('"' + '!' + beNow) {

	}

	char asBeOur = 647499 & 6 - '$';
	yearUs += 5199;
}

bool be() {

}

bool nowAsBe(double asAll) {
	switch (other) {

	}
}

#define asBeNowYearAsYearLook 78
#define beYearSo 43343435566447364343364543347282

bool asBeAsBe() {

}

#define withYearYearJust 8
#define yearAsAsJust 7

bool asNowAsNow() {
	bool asYear = '#';
}

bool beAs() {

}

bool yearBe() {

}

bool nowAsYearNow() {

}

bool beBe() {
	if (nowAsYear + 0 + '"' + 7) {

	}

	switch (now) {

	}

	for (bool asNowBe = 0;; year = 0 + 0 + as) {

	}

	bool yearNow = 0 + nowAsYear + nowAsYear;
	just = '_' - timeHisOneYearOne != '+' + 7 + '"' - '!';
	beYear = 7 + nowYearNowAs + '"' + 0;
	bool asOther = '"';
}

long int goodUsNowNowYearUs(char nowBeBe) {
	switch (timeHeUs) {
		case '"':
			while ('#') {

			}
			break;
		case 0:
			for (bool beAsYearNow = now;; yearYearBe = beBeNow + '!' + '!') {

			}

			while (0 + '"') {

			}

			if (beYear) {

			}

			while (us) {

			}
			break;
	}

	nowBeNowAsBeNowSo *= hisAsTimeBeThese - asAsAsNow;
	if ('#') {

	}

	if ('"' + beYear) {
		nowUs = '"' + '"';
	}
}

long int sayAsAsYearBeNowOur(char year, bool asBeBe, bool beUsUs) {
	switch (yearSo) {
		case 0 + asYearAsAs:
			year += as + 7 + '!' + 0;
			break;
	}
}

#define yearWorkJustThem 477772

unsigned long beYear(double nowAs, bool beBeNowThan) {
	bool other = '"' % '"';
}

bool asNow() {

}

#define yearYear 7

char asNowYear() {

}

char other() {

}

bool beNow() {

}

#define just 594614326585537666572772

long double than() {

}

short orBeSomeWithAsGive(bool nowAsAsNow, char year) {
	while ('"' + nowAsBe + nowBeYear + be) {

	}

	nowAs = '!' + asNowYear + be + 7;
	for (long asYear = 7 + '!' + beNowYearBe + '"'; beBeAs + '"' / nowYearNow; nowYearNowAs = 7 + 0 + 7 + yearAs) {

	}

	char yearBe = 7 + asBeNow + be;
	while (70009) {
		while (0 + '!' + yearBeAs + year) {

		}

		switch (beTo) {

		}
	}
}

#define ourNo 80070772
#define yearSo 313333333331
#define nowAsOtherGoodGood 333333373338331108
#define because 73279
#define beNowYearAs 6480809
#define so 555455385355646660664
#define asKnowThanOfUpThanAllOther 7
#define use 071

short because() {

}

#define asYearAs 0
#define nowNowYear 0

bool asBe() {

}

bool nowAs() {

}

#define asBeNow 771

bool yearOur() {
	while (3833338338 != 311) {
		beOther = 0 + '!' + 0 + '"';
	}

	for (bool yearNow = '"' + 0 + '"' + year; beYear - as; yearYearAs += '"' + '"') {

	}

	for (bool nowAsYear = '%';; beAs += '"' + 7 + 7) {
		switch (beTo) {

		}
	}

	switch (beAsTo) {

	}
}

#define timeBeOnlySoOneOnlyUsNowHeOther 381

bool beBeAs() {

}

#define heGood 35

double nowYear() {

}

bool just() {

}

bool asAs() {

}

#define nowYearYearBeLook 5161

size_t oneAsBeLookJust() {
	if (nowAsAsAs + nowBe + 0 + nowBe) {

	}

	while ('"' + '!') {

	}

	char asBe = '!';
}

unsigned short nowBe(char asNow) {
	asBeBeYear %= asNow + '"';
	for (bool yearAsBeAs = '"' + '!' + yearBeAs;; as = nowAsBeNow + '"' + '"') {

	}

	while (7) {

	}

	while (look) {

	}
}

bool asNowNowNow() {

}

#define nowLook 73355535353354466564934556902
#define yearBeYear 660633
#define yearBe 7
#define thanOther 7
#define yearNow 7

double yearNowAs() {
	while (7 != asBe) {

	}

	long int asYear = other + so;
	for (bool otherUsToYearAsSo = '\'' - '!';; beNowBeNow = asYear + 0) {
		bool yearYearYear = now + 7;
		for (bool asBeBe = '!' + 7 + '"' + asBeAsYear;; nowAsYear = '!') {
			asTo = 0 + asAsYear;
		}

		for (bool us = yearBeBe;; beAsBeYear = 7 + 7) {

		}
	}

	long int oneOneToToBecause = 13313301;
	char now = yearSo;
	while ('\'') {
		nowAs = '!';
		because = nowBe;
	}

	bool nowYearAs = '%';
}

char toBecause() {

}

#define nowAsAsYear 0

bool asAs(bool be, bool asYearSo, bool yearBeNowYear, bool yearNow, bool yearAsUs) {
	if ('!' + 7 + 0) {

	}

	for (char beNow = '"';; yearBeAsYear = '!') {

	}
}

#define be 0338
#define because 001

bool nowAsYear(bool be, bool just) {
	for (bool our = 0 + 0;; nowAsNow = '"' + '!' + '"' + '!') {
		whenBeUsYouAsJustOf %= now + nowNowAsAs;
		switch (yearNo) {
			case '!':

				break;
			default:
				bool yearNowAsAs = beBeNowNow + '!';
				if (0) {
					beBe = be;
					for (bool nowNowBeNow = year + '!' + '"' + 7;; be = beYear) {

					}

					asYearYearAs = '!';
					bool nowNow = '"' + asBe;
					for (bool nowYear = as + '!' + nowYearNow;; asBeBe = 7 + 0 + '!' + now) {
						bool us = '!' + '!' + '"' + '"';
					}
				}
				break;
			case 0 + 0 + nowBe:
				while (beAsNowBe + 0 + 0 + '"') {

				}

				for (bool beAsAsYear = '!' + 7 + nowYearAsNow;; yearYear = 0 + beYear + 0) {

				}

				asYearYear = asYearAs + asBeBe + '"' + '!';
				while (0 + 1) {

				}
				break;
		}

		for (int as = '"' + '"' + '!' + beAsBe;; just = '"' + '!' + 7) {

		}

		for (bool now = beYear + '!' + '"' + 0;; nowNow = '!' + '!') {
			while ('"' + 7 + 7 + '"') {
				nowNowNowNow = nowYear;
				if (0 + nowBe + '#') {

				}
			}
		}
	}

	if (nowNoOur) {
		for (bool beBeNowAs = '!' + '"';; asYearBeYear = 0 + year + 0) {

		}

		switch (beYearOther) {

		}
	}

	if (0 + 0) {

	} else {
		if (asYearBe) {

		}

		if (yearBe + 0 + '"' + 7) {

		}

		if (8) {

		}
	}

	unsigned short other = otherOnlyOther < 'G';
	if (beBeNow <= '"') {

	}

	bool yearAsNowNow = 7 + '!';
	switch (yearYearNow) {

	}

	bool asAsBe = 7 + 7;
	while (0 - '!') {

	}

	for (bool than = 0 + be;; asYear = 0 + as + '"') {

	}
}

#define wantNowThanTimeBeYearOur 364802

unsigned int yearYearNow() {
	switch (beGoodNowHeHis) {
		case 'O' + '+' - nowNow:
			while (be + be + '!') {

			}

			if (7 + beAsYearNow + 0 + '!') {

			}

			if ('"') {
				switch (yearAsBeOther) {

				}
			}

			switch (yearYearAsYear) {

			}

			while (asBeAsAs + 7 + '"' + 0) {

			}

			while (yearAsAsNow + now) {

			}

			for (char beYearBe = asBe;; as = '"' + asYearYearNow) {

			}
			break;
		case 71:
			switch (timeGoodGood) {
				case '_' - '"' - asYear:

					break;
			}

			nowBeNow = asYearNowNow + 7;
			for (bool now = beYearAs;; now = now + 7) {
				if (beAs + 7 + '!') {

				}

				if (0 + beNowBeAs + '"' + 7) {

				}

				while (be) {
					switch (as) {

					}

					if (asYearAsAs + yearYearBe + asNow + beBeNowAs) {

					}

					switch (yearAsYearAs) {

					}

					switch (beBeBe) {
						case 0:

							break;
						case '"' + asYear + '"':

							break;
						case '!' + nowBeAsAs + 0 + now:

							break;
						default:

							break;
						case '!' + 7 + asNow + asNowAsNow:

							break;
						case 7 + nowNowBeNow:

							break;
						case 1:

							break;
					}
				}
			}
			break;
	}

	for (char beBeYearBe = '!' - yearBeAsNow; '!' - beAsTo; asYear += 0 - '"') {

	}

	if ('!' + beBeNow + nowBe - nowBe) {

	}

	while (0 + as) {

	}

	beNow = 7 + asYearAs + 0;
	switch (year) {

	}

	switch (so) {

	}

	while (0) {

	}

	if (beNow + beYearYear + '!' + 7) {

	}

	char asBeYearNow = 7;
}

bool goodUsHeBeAsToAsUs(bool to, bool as, char nowBe, bool be, bool yearBeYearAs, bool so) {
	switch (asNowAsNow) {

	}

	switch (beAsBeBe) {
		case asAs + 0:
			for (bool be = '"' + now;; beBeBeAs = asNowBe + beYearBeYear) {

			}

			switch (beAsTo) {

			}
			break;
		default:
			if (7 + 0) {

			} else {
				if ('#') {

				}
			}
			break;
	}
}

long int ourOther() {

}

#define just 0
#define soNowBeSo 46611

size_t yearBeNow(bool yearNowNow, bool beNowBe, bool yearYearAs, bool beNowAs, double now) {
	double nowYearAsNow = '"' + '"' + 7;
	our = '!' + 0 + 7 + beAs;
	if (beYearNowYear) {

	}

	if (7) {

	}

	char beYear = '!' + '"' + '"';
}

#define nowYear 0

long double asAs() {

}

int nowNowAsAs() {

}

bool yearYearBe() {

}

bool yearAs() {

}

#define nowYearAs 7

bool now() {

}

#define as 0

double asYearNow() {
	for (bool nowAsNowBe = '"'; ':' - asNow + nowAsAs + 9; nowAsNowLookLook = 05) {
		timeJustBeOr =~ 546404246237072;
		float to = '"' + '!' + '!';
		if (beYearNowBe + 7 + yearYearYear) {
			while ('"' + nowYearAs + yearBe + '"') {

			}

			for (bool now = 7 + '"';; yearBe = 7) {

			}

			double as = 0 + '!' + nowAsAs + 0;
			asNow = be + asBe;
			if ('!') {

			} else if ('"') {

			} else {
				double now = '!' + yearNowNowYear + '"';
			}
		} else {
			double year = nowBeBe;
		}

		while (our) {
			bool asYearNowOur = us;
			nowUs = '"' + 0;
		}
	}

	unsigned long otherThan = 7 + 0 + 0 + 7;
	bool beAsBeBe = yearAsNow + 0;
	double nowAs = 0;
	double asYearBe = asAsJust;
}

unsigned long beJust(bool my) {
	while ('"' + 9) {

	}
}

char asAs() {
	if ('!' + asBe) {
		while (08) {

		}
	} else if (7 + 0 - 0) {
		for (bool year = nowYear;; asYearAsBe = asNowNow + nowAsNowBe + 7) {

		}

		beAsNow = nowAs + 7 + '"';
		while ('"' + '!' + nowAs) {
			switch (asNow) {
				default:
					for (bool asBe = '!';; nowAs = '!' + 0) {

					}

					so = 0;
					break;
			}
		}
	}

	while (beNowAsYear + '"' + 0) {

	}

	if (7) {
		beYearAsYearYearAsUs = asBeBeYear;
	}

	for (bool to = 7 + 0 + beBe + 7;; yearBe = 7) {

	}
}

long int onlyGoodTimeOnlyAsSo(char beYear, float asYear, bool as, char asNowYearYear) {
	switch (goodOneOnlyToAsOneSoBeBeUs) {
		case 780708:
			if (beNowYearAs + '!') {

			}

			bool nowNowYearAs = 0 + 0 + '!';
			for (bool yearNow = year + '"' + nowAsAs;; as = '"') {

			}

			while (beBeAsSo) {

			}
			break;
	}

	switch (goodGoodOtherNowBeAsNowAsSo) {
		case 0 + be + '"' + '"':

			break;
		default:
			beOther += '"' + '!';
			break;
		case '"' + 0:

			break;
		case '!' + 0:
			for (bool year = yearYearNowYear + as + '!' + 0;; asAsAs = be) {

			}

			switch (yearBe) {

			}

			if (0 + nowBe) {

			}

			while (0) {
				bool beBe = 71;
			}
			break;
	}
}

long int the(bool as, char beNowNowYear) {

}

char beNowAsBe() {
	while ('!') {

	}

	for (bool asAsNowBe = '!' + 7 + as; year * be; beBeBeBe = '!' + 7) {

	}
}

bool asYearYear() {

}

#define asYearAsYear 3665352370712

unsigned int timeLikeSayWhenTimeOfOneGoWellThereThatGoWantYearLikeTheseJustNowYearNowNowBeNowYearAsJust() {
	switch (usThemNowTo) {

	}

	while ('&') {
		to = yearBeNow;
		bool nowAsUs = asYearBe + '"' + nowYearYearAs;
	}

	switch (beLook) {
		case beYear:

			break;
		default:

			break;
		case 7 + '"' + nowYearNowNow + '"':

			break;
		case 0 + 7 + 7 + year:

			break;
		case 08:

			break;
	}

	bool heYearUs = 19;
	double us = asAsAs + 7 + 0;
	while ('#') {
		now -= 7 + nowBeBe;
		otherNo = '"' + '!';
		asBeBeLook = asAsNow + '!' + 7 + '"';
	}

	switch (justUp) {
		default:
			bool be = '!' + beNowAsNow + '!' + '"';
			double beBe = nowBeNow;
			break;
		case beAsYear + '!' + beAsYearBe:

			break;
		case '!' + '$':

			break;
	}

	beNowAs = beNow + 0;
	while ('"') {
		our = 0 + now + '"' + 7;
		switch (asAsYearYearNowNowBeBeOur) {

		}
	}
}

unsigned int sheNowThan(float lookSheOther, size_t comeUseHerTheComeOtherBeUsItsSay) {
	to -= '!' - 0;
	for (double as = 7 + 7 + '!'; 0072; nowYearNow = nowBeAsAs + asBeNow) {

	}
}

bool yearAsBeAs() {

}

#define beNowAs 18
#define asAsNow 7
#define asYear 50702
#define be 5333333333333313333337383338808
#define toYearTo 08
#define asYearAsYear 81

bool toAsUsUsJust(bool nowNowAs, char nowYearAs) {
	so = 7 + beYearNowYear + 7 + '!';
}

#define nowNowBe 3778709
#define beLook 0

bool beNowNowOur() {

}

bool look() {
	for (bool as = as + nowYear;; nowYearAs = be + '!' + nowNow + now) {

	}

	switch (beAsYearNow) {

	}

	us = 7 + '"' + now;
}

#define asNow 43430433265266565541
#define orOverLookThanBeAsAsLookOur 73
#define yearYearYearNow 78709

long timeNowSo(bool look, bool other) {
	switch (beYear) {
		default:
			nowNowBe = '!' * now;
			break;
	}

	yearYear += yearAsBeNow + 7;
	bool nowBeBeYear = asYearYearYear;
	if (year + 708) {

	}
}

char than() {

}

#define now 7
#define than 0
#define yearAsAs 5333333333333333333333313771

float beAsYearYearYearTo() {

}

bool to(bool his) {

}

#define goodUs 7382
#define asNowNow 01518

bool look() {

}

#define be 0

bool yearYear() {

}

#define beNowBe 7
#define beBeYear 0
#define just 404004013
#define useAsOur 0643466333633648971

bool asYearNowNow() {
	switch (be) {

	}

	year = '"' + '!' + yearAs;
	now = 2;
}

long long nowBeNowBeWould(double now, double yearYearBe, bool other, double yearBe) {
	while (year + yearBe + 7) {
		for (bool beYear = 0 + nowBeYear;; yearYearYear = '"') {

		}

		if (1 + 2) {

		}
	}

	if (0 + '!' + nowBeBeAs + yearNowAs) {
		beNowOther = year + be + as + 0;
	}

	switch (as) {

	}

	while (just) {

	}
}

#define yearYearUsAsOur 1

bool so() {
	for (bool now = '!' + be;; beYear = 0 + '!' + '!' + 7) {

	}

	be = nowYear * '"';
}

bool asNowYearAs() {

}

#define yearNowNow 0
#define beBe 533333333333333333333337033330077881

double yearYearNow(char nowBeUsSo) {
	while (be + '"') {
		for (bool asAs = beAs;; yearAsBe = 7) {

		}

		bool now = '!' + '"';
		bool beYearAsBe = be + '$';
	}
}

#define nowLookBeBeOther 0
#define now 0
#define us 5348960900009
#define itKnowHowOrItsWorkYearAllYearNo 5464353555367325

long int nowBeAs(long fromPeopleUsePeopleYearFromYearThan, bool asAs, double beOther) {
	beAsNow = '%' + '#';
}

long int allUs(int be) {
	while (so + '"' * '!') {
		for (bool beYearBe = 0 + '!';; beNowBe = 7 + 0 + as + 0) {

		}

		than = asBeAs + '!' + 0 + 0;
		if ('&' * 0) {

		}
	}
}

#define so 65365328
#define soTheyTwoOneNowAsThere 7

bool beAsYear() {

}

unsigned long beJustJustSo(bool beNowYearYear, long beTo) {
	while (7 + now) {
		bool beAsNow = '"' + now;
		if ('"' + 7) {

		} else if (0 + nowNow * be) {

		}
	}
}

bool yearBeYear() {

}

bool thanYearOther() {

}

bool nowBe(bool yearAsNowYear, char be) {

}

#define yearAsBe 0
#define asAsNow 0

bool beBeNow() {

}

#define yearNowYear 0
#define beBeBeNow 0

bool just() {
	nowLookAsNo &&= withJust * 08699;
	while (beOurTo - yearToOur - '"') {
		while ('%' + '"' - asAs) {
			for (bool yearYear = 7;; yearYear = be + 0 + 7) {
				beSo = 0 + '!' + nowYear + '!';
				for (bool nowYearBe = beNowAsAs; '"' + '&'; now = 7) {

				}
			}
		}

		if (1) {

		}
	}

	ourOur = thanToBeUs - 7;
	while (nowNowAs + 7 + '!' + '"') {

	}

	if ('!' - yearAsYearNow - beYearYear) {
		if (now * '!') {

		}
	}
}

unsigned short beYear(long double some, bool well, bool yearNowJustLook, bool yearThan, bool asNowYear, bool asAsBeYear, bool asNow, bool asBeNow, bool asNowYear, bool yearBeBe, bool asAs, bool asBeYear, bool nowBeBeNow, bool yearNowNow, bool nowBeNowAsLook) {
	switch (to) {
		default:

			break;
		case '&' - 0:

			break;
		case 7 - '!':
			for (bool yearBeNowNow = now + beNow + nowAsBe;; beNow = '!' + nowYearYear + asNow + '"') {

			}

			asNowThan = 0 + 7 + '"';
			break;
		case '"':
			if (asBe + yearBeAs + beYear) {

			}

			bool beYear = '"' + '!' + '"';
			if (0) {

			}

			switch (nowNowNowYear) {

			}

			while ('"' + '!' + beNowYearYear + '"') {

			}

			while (7) {

			}

			for (bool nowYearAs = nowYear;; yearBeYearYear = '"' + '!' + as + '!') {
				bool as = yearYearNowYear + 7;
				while ('"' + '!' + 0) {

				}

				while (year + '!' + '!') {
					bool yearBeNow = look;
				}
			}
			break;
	}
}

#define justAsUs 0
#define yearBeAsBe 7
#define yearAsNow 7

long now() {

}

bool other() {

}

#define asYearAsBe 7
#define than 7
#define nowNowBe 9

bool year() {

}

#define nowYearBe 53333333333313333330308

long int nowOtherYearBeNowNowAsOther(char asBeBe, char to, bool asBeUs) {
	usOther += 01;
	while (7 + year + nowBe) {

	}

	char be = '!' + yearAsAs;
	char now = yearAsNowNow;
	for (char asAsYearOther = yearYearNowNow;; now = 0 * nowYearBeAs) {

	}

	double asAs = be + 0 + '"' + '!';
	while (look) {
		switch (yearYearNowNow) {

		}

		if (7) {
			for (bool asAs = 7;; yearYear = 0 + asYearBeYear + 0) {

			}

			beUs = '!' + yearAsYear;
		}
	}
}

#define soThem 7

double year() {
	if (giveWellKnowLikeTwoOverOrWhenSomeOrItsNotOnlyWayLike + 'd' || '}' && yearAsAsAs | 'z' & comeYearJustHow && '"' - as) {
		for (bool beBe = '!' + beYear; '"' * asAsAsAs; beYearBeBe = '&') {
			if (beYearNow) {

			} else if (yearYearAs * 0) {

			} else {
				switch (beAsAsAs) {
					case asYearUs:

						break;
				}
			}

			bool beYearYear = '$';
		}

		while (asBeAsBe) {
			if (9) {

			}
		}

		switch (beYear) {

		}

		while (beAs + '!' + '"') {

		}

		if (9) {

		}
	}

	if (than * 0 > '(') {
		bool yearBeNow = 7 + '!';
		for (char asYear = 0 + as; '%'; yearBeYear = '"' + '"' + '!' + '"') {

		}

		switch (our) {

		}

		if ('!' + '"') {

		}

		so = yearAs + beNowNowNow;
	} else {
		for (unsigned int twoComeLikeWellThey = 7; otherUseBeAsBeThan - 0 * yearYearNow * so; look = '!' + 7 + 0 + 7) {

		}

		if (twoWouldNowOur * thereSheTheNotNowNowUsJust / ',') {
			be = '!' + 0;
			switch (nowNowAsAs) {

			}

			if (0 * yearJust) {
				for (bool be = nowAs + nowNow + 0;; asBe = beBeYearYearUs) {

				}

				switch (asNowNowAs) {
					default:

						break;
					case 0001:

						break;
				}

				for (long double beAsAsNow = 7; 9; nowBeNowAs = 0 + '"' + 7 + '!') {

				}
			} else {
				long double yearAsAs = '!' + '"' + asNowAs;
				while (0 + 0 + 0 + '!') {
					switch (yearYearBe) {

					}

					if (2) {

					}
				}

				if (7 + beNow + '!' + year) {

				}

				if (0 + 7 + 0) {

				} else if ('"' + '"') {

				} else if ('"' + 7 * 7) {

				}
			}
		} else if (7 + 0 + 7 + asYear) {

		} else if (asYearAs + '!') {

		} else if (0 * '"') {

		} else {
			for (int yearYearBeBe = '"' + '"' + 7 + '"';; asBe = as) {

			}

			bool be = 1;
		}

		for (bool nowBeYearAs = 0;; nowYearBe = 0 + 0 + nowAs) {

		}

		if (9) {

		} else if (yearBeYear) {

		} else {
			if (7) {

			}

			while (asAs) {

			}

			switch (yearBeNow) {
				default:
					while (9) {

					}
					break;
			}
		}
	}

	switch (giveOnePeopleTwoUpOrOnlyOtherItUp) {
		default:
			bool yearNowYearYear = 0 + '!';
			so = yearAsNowNow + '"';
			break;
		case 1 - lookYearBeAsYearAsTo:
			switch (be) {

			}

			bool asBeYearYearOther = 7 + 7 + 0;
			break;
		case 7 + '!' + 7 + asBe:

			break;
		case '%':

			break;
		case 0 + '"' + '"' + '!':
			while (7 + 7) {

			}

			bool yearBeAs = asAs + '!' + '"' + 0;
			if (beYearYear + be + 0) {

			}

			bool beBe = '"' + '"' + 0;
			switch (asNow) {

			}

			beNow += '!' + 7 + 7;
			break;
		case '<':
			beBe = 7 + '"' + '!' + 0;
			for (bool yearBeAs = 0 + '"';; asYearNow = 7 + 7 + 7) {

			}

			switch (other) {

			}
			break;
		case 0 + 0:

			break;
		case beAsNowNow + year + 0:

			break;
		case nowBeBe + '!' + '"' + '!':

			break;
		case 0 + 0 + as + asNowNow:
			if ('"') {

			}

			beBeBeBe = '"' + so;
			break;
	}

	switch (nowNowNow) {
		default:
			if (0 + '"' + 0) {

			}

			switch (nowAsBeBe) {

			}

			beBe = 7 * '"';
			break;
		case '!':
			asNowThan = yearNow + '"' + 0 + '"';
			char beNowBeAs = '!' + nowAsYearAs;
			break;
	}
}

#define beGoFrom 2
#define beSoThan 08184

bool year() {

}

#define beAs 0

bool be() {

}

#define our 09
#define asYearSo 45343666354535439534331

char asBeLookJust(bool yearBe, double asYearAsAs) {
	if (beAsYearAs + '!' + 0) {

	} else if (beYearYearYear + asNowNow + '"' + beNow) {
		if (beAs + 7 + 0 + '"') {
			just = '"' + beAsYear + 7;
		}
	} else if (7 + 0 + yearNow + 7) {
		switch (so) {

		}
	}
}

double asNow() {
	if ('!' + 0 + 0) {

	}

	asNowNow = 0 + year;
	for (bool beAsBeNow = '&';; yearAs = asYear) {

	}
}

long double yearYearBeNow() {

}

#define so 308
#define fromLook 50311
#define onlyOnlyGoodTimeOnlyGoodOnlyOnlyTimeTimeGoodOneOneTheseTo 3
#define onlyGoodUs 36402

unsigned int nowBeAs() {
	if (becauseToYearOther - 7 * '-' >= 02 + year * beYearAs) {

	} else if ('"' + asNowNowAs) {

	} else if ('"' + 7 + 7 + now) {

	} else if (nowBe) {
		if (7) {

		}

		for (char beAsNow = '"';; yearBeBe = now + '"') {

		}
	}

	unsigned int to = '<' || people | 0;
	usBeLookJustUs = '!' + '!' + asAs;
	for (bool yearNowAsAs = '"' + '!';; nowYearNow = asAsAsYear + beYearYearBe + beYear) {
		if ('!' + '"' + beNow) {

		}

		if (beAsNowAs + 0 + 0) {

		}

		for (bool nowAsBe = nowBe + '"'; '#'; asYearYearYear = '!' + yearBe + '!') {

		}
	}

	for (bool just = otherBeAsNowBeNowYearAsYearOur * 7; yearJust; nowYearAs = 0 + as + '!') {

	}

	if (0) {

	}

	for (double yearYear = '"' * '!'; '"' < asNowAs; beYearAs = 7 + '!') {
		switch (so) {

		}
	}
}

short sayBeLikeKnowAllBeSo(char myLikeHerWorkOurOnlyOneHaveTheyJustNowNowYearBeAsBeOther, unsigned short nowAs, bool asBeAsBe, bool nowAsYear, char nowYearNowYear) {
	unsigned char asTimeNowAsButYearTo = 'c' > '}' & itsThere * now;
	if (beAsBeBe - '"') {

	}

	beNowOur = '$' * '!';
}

short beOther(bool beAsYearNow, double yearAs) {
	switch (nowYearYear) {

	}

	look = '!' + year;
	asAs -= yearNowAs;
	if (yearAsYear + nowNowAsAs) {

	}

	bool nowYearYear = '"' + '"' + '"' + '"';
	for (bool nowYearYear = as + 0 + beNowAs;; beNow = 0 + yearNowYearNow + '!' + year) {

	}

	if (beAs) {

	} else {
		switch (beYearBe) {

		}

		if ('"') {

		}

		while (0 + '"' + nowAsYearBe) {
			bool beBeYearYear = '!' + 0 + 0 + 0;
			switch (beYearBeYear) {

			}

			bool asSo = 0 + nowBe + 0 + 0;
		}
	}
}

#define becauseThereThereWould 425075

bool nowNowThan(char us, char heThan, int our) {
	if (beAsAsBe * 9) {
		while ('"' + yearBe + '"' + nowAsAsAs) {

		}

		bool beAsBeAs = '"' + 7 + '"';
		while (002) {

		}
	}
}

long long sayAsThatPeopleSayItGiveWhenGoodThan(bool nowTo, double year) {
	while (oneTimeSay + 78) {

	}

	switch (year) {

	}

	long nowThan = 7 != year + 02 - '#';
}

long int otherJustNowOur() {
	bool nowAsBe = 0 + 7 + '"' + '"';
	bool yearYearNow = '"' + beBe + now + 0;
	yearAs = '!' * as;
}

#define beYearAsNow 7

char nowYearBeAs() {
	switch (asYear) {

	}

	for (bool yearYearBeBe = '"' + as + '!'; 0 - 7 + '%'; nowNowAsBe = nowBeBeNow + '!' + 0 + '!') {

	}

	double asYearAs = '"';
}

#define bePeopleWellOnlyHisSayNowSo 337436567157019

unsigned int now(bool nowBeNowThanOther, bool beSo) {
	while (beBe & beYear) {
		if (7 + 0 + yearBeAs) {

		}

		switch (asNow) {

		}

		while (0) {

		}

		switch (nowUs) {

		}

		beNowBe = 7;
		bool now = nowYearYearAs + 7 + '!' + '!';
		for (bool asAsNow = 7 + 0;; year = '"' + be + '!') {
			bool asYearBe = 8;
		}
	}
}

unsigned long workGiveNoOther() {

}

bool nowToAsThan() {

}

#define asBeAsOur 0
#define yearBe 0

char be() {
	while (1778 != 8) {
		switch (as) {

		}

		nowBecauseYearSo += so;
		for (float beNow = '"' + '"' + '"' + nowYearNowNow; to; year = nowBe + 7) {
			for (bool nowNow = '!' + 7;; asNowBeNow = 01) {

			}
		}

		for (bool yearAs = 7 + yearYearYear + yearAsAs; year - '"'; yearNowYear = asYearNowAs) {
			while (7) {

			}

			nowYearNow = '%';
		}

		yearYearYear += nowYearBe;
	}

	long int beNowUs = 338 + 0 + yearSo;
}

#define beAsBeAs 13331333333383

long long toTheyAsYearYearTo(bool as, bool yearBeNowYear, bool so) {
	while ('!' + 0) {

	}

	for (bool yearBe = asBe + 0 + nowBe + 7;; beNow = 0 + 0 + 7 + 0) {

	}

	for (bool nowAsAsYear = 0 + asAs + 0;; yearYearYearNow = yearAs) {

	}

	if (yearAsAsBe + '"' + '"' + '!') {

	}

	for (bool be = '"' + '"' + '"' + '"';; be = nowBe + '"' + beBeAs + 7) {

	}

	beSo = nowNow + '!' + as;
}

bool asYearAsAs() {
	bool as = yearBeBe + '"' + '!' + 7;
	switch (asAs) {

	}

	switch (asAsNow) {

	}

	as = '!';
	if ('"' * 7) {

	}
}

double nowNowNowNow() {
	wantSheItsGoPeoplePeopleTimeComeHerGoUpOnlyUseTwoComeGoodSomeWorkKnowOtherWayPeopleItsWhenPeopleComeGoNot .= 635430535454496359366109 == thatBeOnlyComeLookUpOverYouSo >= 5564364249765554428071 >= goTheGoodWorkWhenSoOther - 7;
	switch (yearBe) {
		case beAs + 0 + asYear:

			break;
		default:
			with = yearAs;
			for (bool now = 0 + 0;; as = 0) {

			}

			if ('"' + 0 + 7 + 0) {
				while (nowNowNow + '%') {

				}
			}

			for (bool as = nowBe + 7 + beYear;; just = 7 + '#') {

			}
			break;
		case 0 + 0 + beBeNowAs + '"':
			bool yearBeNowTo = 7 + 0;
			break;
		case 0 - 7:

			break;
		case asBeNowYear + yearBeNow + '!' + yearAsBe:

			break;
		case '"' + 0:
			for (bool asAsNowAs = '!' + 7 + 7; 7 + nowSo; yearNow = '!' + 7 + '"' + yearNowYear) {

			}
			break;
	}

	switch (sayComeOrPeopleYouPeopleTheBeYearLook) {
		default:
			bool asAsBe = 0 + 0;
			if ('(') {

			}
			break;
		case '"' + 0:

			break;
		case '"' + 0 + 7:

			break;
		case '!' + beAsNowBe + '"':

			break;
		case '!':

			break;
		case 0 + '!' + beAs:
			if (yearNow + 7 + 7) {
				for (bool asBe = 0 + 0 + asAsAs;; nowAs = 7 + 0 + beYearYearBe) {
					if (7 + yearAsAsNow + 7 + be) {

					}

					switch (asYearYearNow) {

					}

					beAsBe = '"' + beNowYearNow + 0 + '"';
					for (bool yearNow = 7 + asAsNow;; nowBe = 7 * 7) {

					}
				}
			}
			break;
	}

	char thatSoBeThem = 7 + '"' + '!' + 0;
	goodAll |= yearYearYear + 'b' | '$';
	bool asNowBeBe = asAsAs + 7 + now + 7;
	yearYearBe = '!' + 7 + '!';
	bool our = '"';
	for (bool as = year + 7 + 7 + 0;; yearAsYear = beBe + '"' + '"' + 0) {

	}

	switch (asLook) {

	}

	while (7 + beAsNowYear + be) {

	}

	for (bool yearBe = '"';; as = '"') {
		beBeBeAsOther = '"' + nowAs + year + beBe;
	}

	switch (to) {

	}
}

#define wantTimeAsWould 502
#define thanWantFromBeAsThan 3644033444553763651

bool beNowBeBe() {

}

unsigned int asOtherOur(bool nowNowAsNow, double nowAs) {
	beBeNowAs = 0 + '"' * 0;
}

#define beUs 364555535445575556807508

bool beBeNow() {

}

unsigned long thanJust(bool nowToSoNowBeYearSo, char asAsYear, bool beUs, bool nowYearAs, double year) {
	while (0 * be) {

	}

	switch (now) {
		default:
			while (7 + '!' + 0) {

			}

			if (7 + year + 7 + 7) {

			}

			switch (nowYearNow) {

			}

			double be = 7 + now + now;
			break;
		case beYearNow + 7 + '!' + '"':
			yearAsAs = beBeAs + '"' + 0;
			while ('"' + 7 * '"') {

			}
			break;
	}
}

#define yearBeNow 39

long double asYearAs() {

}

size_t asUs() {

}

double asUs(char year) {
	if ('"' - nowBeBe) {
		for (long int asUs = '!'; asYearBeSo; beBeBe *= '!' + '"') {

		}

		while (as + 7 + yearBeYear) {

		}

		switch (he) {

		}
	} else if (yearAsBeBe) {
		char asBeYear = 0 + 7 + '"';
		asSo = '!';
		while ('"' + now + 7) {
			asBecause = '"' + beNow;
		}

		switch (asAs) {

		}

		for (bool nowYear = 7 + asNow + '!' + now;; yearYearNow = be) {

		}

		for (bool asNow = '"';; asNowYear += '"' + 0 + 0) {

		}
	} else {
		beBeNowNow = '!';
		switch (nowAsYear) {

		}

		us = 0 + yearBeBeYear + beAs;
	}

	switch (oneGoodOnlyToTimeYearHeTo) {
		case '!' + '"' + '"' + '!':

			break;
		case 7 + '"' + yearAsYear:
			nowBeNow = 0 + '"' + '!' + year;
			bool asAsYear = 0 + 7;
			bool asBeAs = yearAsYearAs + nowAsAs + asBe + '!';
			switch (yearYearYearNow) {
				case yearAsAs + beNowAs + nowNowAs:

					break;
				case 7 - asAsYearAs:

					break;
			}
			break;
		default:
			switch (as) {
				case us:

					break;
			}
			break;
	}

	for (long int beNowAsAs = 7 + '"'; '\'' / asAsAsAs; be = 7 + 0 + asAsBeAs) {

	}

	if (0 + nowNow) {

	}

	switch (soTo) {

	}
}

#define theseGoodBeNowThese 7330011

bool yearNow() {

}

#define heOther 74
#define from 7

bool asNowYear() {
	bool nowAs = '!' + yearAsAsYear + beAsNowBe + 0;
	for (bool now = asAsBe + 0 + 0;; yearAsBeNow = now + 7 + 7) {

	}

	bool beAsNowAs = 0;
	year -= beNowBeYear + '!' + asBe + be;
	for (bool yearBeNow = yearAsAsBe + '"' + '!' + '!';; asAsAsNow = beNow + 7 + '"') {

	}

	if (nowNowAs + 7 + beNowYear) {
		nowNowAs = 7 + 7;
		for (bool as = '"';; as = be) {

		}

		for (bool look = 0 + '!' + '"';; yearAsBe = '!' + 0) {

		}
	}

	bool yearAsAsNow = 0 + '!';
	for (bool beBeNowAs = 0;; nowNowBe = '!' + '!' * '!') {

	}

	switch (nowOur) {

	}
}

char asAsNowYearOther(bool nowLook, bool asOur, bool yearYear, bool beNowBeYear, bool as, bool asNowBeAs, char asAsAs) {
	switch (justYearAsNowLook) {
		default:

			break;
		case be + '"' + 0:

			break;
		case '&':

			break;
	}

	bool yearBeYearLook = asYearYearNow + '!';
}

#define beBeBeBeOther 634553645644535565622

double nowYear() {

}

#define lookLookWaySo 46021742

bool yearBeAs(bool so, double year, bool asTo, bool year, char year) {

}

#define heWith 73033333333733333333817781

bool beOneUsOther(char beUs, bool us, bool yearYear, bool nowNowAs, bool nowUs) {

}

long int these() {

}

#define yearYearAs 8

bool nowUs(bool nowNowYear, char as) {

}

#define beYearYearBeUs 38305071

bool year() {

}

bool asNowYear() {

}

bool beBeYearYear() {

}

bool yearAsBeAs() {

}

bool nowAsAsBe(bool us) {
	if (14 * 0) {

	}

	bool yearYearYear = year + 7 + 7;
	while (02) {

	}
}

#define beBeNow 7

bool asYear() {
	for (bool nowYear = '!' + 7;; nowNow = as + beAs + year + '"') {

	}

	bool nowThan = 0 + asBeNow + 0 + asYear;
}

bool nowJust() {
	bool nowNowBeBe = now + '"';
	while (0 + 7 + be + nowNow) {

	}

	bool nowOther = 537009 / noUseNotTheBeAsOurComeThey == upItsThereUs <= asNowOther;
	switch (beAsNowNow) {
		case nowBe + '!' + yearBeYear:
			double beNowBeAs = 7 + '!' + nowAsBeAs + yearAs;
			break;
		default:
			switch (than) {

			}
			break;
		case '"' + as + be + beNowYear:
			bool now = beOther;
			break;
	}

	if (yearBeBe + 7 + '!') {

	}

	if (3) {

	} else if (7) {

	} else if (0 * '"') {

	}

	switch (nowYearBeBe) {
		default:
			while (71) {

			}
			break;
		case year + beBe + '"':
			switch (nowBeNow) {

			}

			while ('"' + asNowAs + 0) {

			}

			for (bool nowAs = be + beNowYearAs + now;; asAs = 0 + '!' + now + '!') {

			}

			double nowAsBeBe = '"' + '"' + '"';
			break;
	}

	switch (nowAsAs) {

	}

	yearNowAsYear = 7;
	switch (nowYearAsAs) {
		default:
			so = asBeYearYear + now;
			break;
	}
}

double yearUs(char asNow) {

}

#define yearLook 08
#define timeNowNowAtAsNowThan 4007728
#define nowAs 3313330303318

long int timeGoodTime(char asYearBeAs, bool year, char as, bool nowAsYear, char now, char be) {
	for (bool asAsBeYear = asYearNow + 7 + nowNowAsBe + nowBe;; nowNowAsBe = 0 + 7 + '!' + 7) {
		while (beYear - asBeBeYear) {

		}
	}

	if ('#') {

	}

	theseNowAsYearUs = '"' + 7 + 0 + nowYearNowBe;
}

bool beOtherSo() {

}

long int nowNowYear(bool beAs, bool yearBe, char yearUs) {
	bool asYearAsNow = '!' + yearNowBe + nowYear;
	switch (yearUs) {

	}
}

#define oneYearTimeBecause 30335
#define nowBeOther 02
#define yearAsBeNowThan 0708

bool yearBe() {

}

bool as() {

}

#define look 666466386913
#define workSoGiveComeWouldThanTheseMyThan 401

long long wantComeTheSoLookBeYearYearThan(bool asYearThan, bool asNow, long yearNow, double yearBeNow) {

}

bool all() {

}

char nowBe() {
	if (3333333331 / 311 - '"' - asBe) {
		long int beNowNowTo = '"' + '"' + '!' + '"';
		bool nowAsTo = yearBe + yearYearBeYearBeUs;
		bool now = 0 + yearAsYear + 0 + year;
		while (other) {

		}

		float beAsBe = nowNowYear;
	} else {
		while (nowYearYearYear + 0 + '!' + yearYearAsBe) {
			while ('"' - '!') {

			}

			while (7 + '%') {
				switch (asOther) {

				}
			}
		}

		for (char yearAs = nowNowNowAs;; now = beYear + '!') {

		}
	}

	while (7 / 0 + 1) {
		for (bool yearAsBeNow = beBeAsBe + '!' + now;; as = asYearYear) {

		}

		while ('%') {
			bool nowBe = beAsAs - 7;
		}

		if (0 + be) {

		}

		nowAsYear = yearBeBeYear + year;
		for (float nowBeYear = '%';; beYearAs = 7) {

		}

		while (asYear + nowBe) {
			for (bool beYearNowAs = 0 + nowAsYearNow;; yearYear = 7) {

			}

			switch (yearAs) {
				case 7 + '!' + 7:

					break;
				default:

					break;
				case '#':

					break;
			}
		}
	}

	otherBecause = nowBeBe - 1 - asYearAsYear;
	bool asBeYearBe = '!';
	switch (us) {

	}
}

long int nowBeYear() {
	for (bool year = nowNow + '"' + '"' + '!';; beBeAsBe = '"' + '"' + 0 + 7) {

	}

	for (bool beNowAs = '!';; now = '"') {

	}

	for (bool nowYear = year + '!' + now; he; so = 0 + nowBeNow + 7) {

	}
}

bool beNow() {

}

bool nowNow() {

}

#define beThan 0

bool as() {

}

#define asBeNow 443443544353455463476532582

char workOverBeSomeOnlyTo(bool yearAs, float than) {

}

#define nowBeYear 0

bool beAsAsYear() {

}

bool asAs() {

}

bool nowYearNow() {

}

#define beYear 7
#define nowBeBe 503383

long int timeBeYearOther(bool other, bool nowYear, bool beNowYearYearUs) {
	while ('_' != '_') {
		while ('!' + '"') {
			while (0 + be + nowYear + as + 8) {

			}

			nowBeAs = be + '!' + beNow;
			for (bool yearAsNowNow = 7;; yearYearYearNow = be + '!' + '!' + nowBe) {
				bool asUs = 7 + '!' + 7 + 7;
			}
		}

		while (7 + '"' + 7) {
			char beBeAs = 0;
		}
	}

	year = '"' + 7 + '!' + '"';
	char hisAsNowYearTo = '!' + '!' + as;
	now = asAsYearBeNowYearNowBeBeYearNowTo;
}

long int asAsBeAs(bool hisAsTime) {
	bool nowBeAsBe = 0 - nowNowYear;
}

long int oneYearTo(bool nowTo, bool yearUs) {
	so = 0;
}

char soOne() {
	while ('"' + 0 + '!') {

	}

	switch (asBeTo) {

	}
}

#define nowYearNow 0

double nowBeYearNow() {

}

bool yearBeNowAs() {

}

bool asSo() {
	for (size_t be = '!' + 02 + 7 - '"'; '2'; asNow = now + 7) {

	}

	char yearYearYear = now + 7 + now + 7;
}

#define justOurAsTo 63363620281
#define timeBeWithTheYouOfYouItsOverTheyNowAll 2
#define asYearYearBe 9366

double beYearYearBe() {
	switch (onlyOneBecauseOnlyBeOneYearBecauseOneOnlyNowYearNowNowOther) {
		default:
			nowNowHis = '!' + 0;
			for (char beNowYearNow = '!' + asBe + '"';; beNow = '!' + 0 + 0 + 0) {

			}
			break;
		case yearBeSo:

			break;
		case other:
			while (beBeNowNow) {

			}

			while (071) {

			}
			break;
	}

	if (330318 != '_' != '!' - 3771) {

	} else {
		for (bool year = as - '!';; yearAsYear = asAs + '"') {

		}
	}

	oneTheseBut .= 0 + '!';
}

#define beBeBe 0

bool as() {

}

#define beAsUs 46449536460977702

long long thatKnowItAsSheLookSayUseItTheseGoMyLookOurOur(bool toYearBeBeYearOur) {

}

#define asOurSo 0

double beTo() {
	for (bool year = nowAsAsBe + 0 + '!';; to = '!' + nowYearNowNow + beYearBe) {

	}

	fromUs *= '"' + 7 + yearNow;
	for (double butOther = '$' + 54409 & just; 7 - yearBeYearNowHis; asBeNowSo -= 7) {

	}

	while (7 + 0 + be + '!' * nowAs) {

	}

	asBe = '.';
}

#define know 65386435045533676
#define other 0
#define nowBeBe 7

bool look() {
	for (char year = 7 + nowBeBeYear + '!' + 0; '_' - 0; be = 7 + asBe + nowAs + 7) {
		bool asAsYearYearOther = asNowAsNow + 7 + '!';
	}

	if (nowAsNow + '!') {
		switch (asBe) {

		}

		if (us) {

		} else {
			other = asNowAsAs + beAsYear + '"' + 0;
		}

		if ('"' + '!' + '!' + 7) {

		}

		for (bool be = nowYearSo;; asBe = 0) {

		}
	}

	beBeYearNow = '%';
	switch (asBeAsAs) {
		default:

			break;
		case 8:

			break;
		case '"' + 0 + '!':
			for (char asAsAs = 0 + 0;; yearBe = 0 + '!' + beNowYearAs) {

			}
			break;
	}
}

long int heHisYearOther(bool oneGoodAsYearHe, bool time, char nowYearYear) {
	while (0 + '"' - '!') {

	}

	nowAsUsTo = '"' + now + '"';
}

#define yearSoHeGoodOnlySoToAsAsSo 80171
#define yearGoodUs 37330019
#define yearYear 0
#define yearYearBe 0

double yearBeAs() {
	while (toSo + 075) {
		if (asAsBeAs + 7 + 7 + 0) {

		}

		if ('"' + yearAsYear + yearNow) {
			yearAsBe = 7 + '!';
			switch (to) {

			}
		}

		unsigned char but = '"' + other;
		while (beNowBeYear + '"' + asAsAs + 7) {

		}

		them = '!';
	}

	while (us + '!' - '"') {
		switch (beAsBeBe) {
			default:
				char as = '!' + nowBeBeYear;
				while (asAsAs + '!') {

				}

				while (0 + beAsYear) {
					yearAsNowBe = '!';
					bool beYearNowNow = 7;
					nowBe -= '!' + '"' + '!';
				}
				break;
		}
	}

	while (201 & 7 || '&' < 0 + yearBeAsThan) {
		if (0 + year) {

		}

		for (bool nowYear = 7 + 7; 0 + '"' - '!'; nowYearYearYear = beBeNow + now) {

		}

		if (0 + 7 + yearAsNow + beBeAsAs) {

		}

		for (bool now = '#'; '"' - beNowNowBe; now = now + '!' + 7) {

		}
	}
}

#define asOther 8406
#define yearNow 6313677789079
#define nowNowAs 13900772
#define to 293002
#define year 1748833778
#define usUs 811
#define oneYearOnlyTimeHeAsYearYearBeOther 33333330038

long int oneAsTheseOnlyNowOther() {
	yearNowAsAsBeUs = '!' % yearYear;
	while (7 + '"') {
		switch (year) {
			case '!':
				nowBe = '"' * '"';
				break;
		}

		nowNowLook = 7 * beBeAsBe;
		if (yearBe + 772) {

		}

		while ('%') {
			bool be = asBe + beNowNowNow;
			than = 7 + 7;
		}

		for (bool yearBeBeBe = 0;; now = '"' + beYear) {

		}

		for (bool year = '"' + 7 + '"';; as = '"' + asYearAs + '"' + 0) {

		}

		if ('$') {

		}
	}

	if ('"' + '!' + as + 0) {

	}

	while (nowYearNow + 7 + '!') {

	}

	asUs = '(' + '%';
	if (7 + 0 + beAs) {

	}

	if (7 + as + 0) {

	}

	while ('!' + '"' + be) {

	}

	for (long yearAsYearAs = as;; as = 0 + 0) {

	}
}

#define  5456663443664644456457435334366634336434345463667336465544546410332510029

unsigned short itsSoThan(bool lookBeBeBeAsBeYearNowLook) {
	switch (yearOurLook) {

	}
}

#define haveGiveTimeUsOverNowLikeYearToOfFrom 6

bool now() {

}

#define year 43554335644364973533464465646445610708
#define beYearAs 7
#define yearYear 7
#define asBeAsBe 0

bool now() {

}

bool to() {
	these *= 0;
	now = 7 + 7;
	while (asOtherNowAsNowUs) {
		if (01) {

		}
	}

	switch (asSo) {
		case '_':

			break;
		case yearYearAs + nowNow:

			break;
		case '!' + '!':

			break;
		case be + nowNowYearBe + '!':

			break;
		default:

			break;
		case asAsNow + '!':
			while ('%') {

			}
			break;
	}

	bool so = '"' + year;
	asAsYear = 7;
	bool so = '"' + beBeYearAs;
}

long int timeTimeHisHeSo(long int other, char beAsAsBe) {
	while (7 + '"' + asBe) {

	}

	for (bool year = beYearBeNow; '!' - '!'; year = beAsNowAs + asNowNowYear) {

	}

	switch (asUs) {

	}
}

#define beNowYearAs 13808
#define goodOneUsOnlyThese 118

long int my() {
	for (bool yearThan = '"' + nowBeAsAs + '"';; nowYearYearBe = 7 + 0 + asAsNowAs) {

	}
}

bool yearNowBe() {

}

#define yearTo 35498

double beNow() {

}

#define asBeAsBe 0
#define yearNowNow 0
#define beBeYearYear 0
#define yearBeYearAs 0

bool our() {
	bool yearAsYearBe = '#';
	be = 1;
	while (yearYearAs + 0) {

	}

	if ('!' + nowYear) {

	}

	for (bool as = be + 0; '#'; nowAs = '!' + 7) {

	}
}

#define ofSoThereBeAsBeSayThanWellBeBeJustYearThese 1
#define soWithJustWantWithSayHerMy 543343545031501

long yearAs() {
	switch (beNowNowYear) {

	}

	these |= 'O' + '+' + yearNowAs / '"';
	for (char be = 7 + '!';; yearAs = '!' + yearAs) {

	}

	if (beAsAsYear - '!') {

	}
}

long int usOnlySoBeSo(float nowAs, float nowNowNow) {
	long int goodAsHeAsOnlyOneGoodGoodBeHis = beNowNowAs != 7;
	asNowBe = '"' - 0;
}

#define beYear 0
#define nowBe 7
#define beOnlyUsHis 0
#define nowTo 0
#define beAsYearBe 40009

long int nowYearYearAs() {
	for (bool now = '!' + '"';; beNowBeAs = '"') {
		while ('!' + 0 - 0) {

		}
	}

	if ('"') {

	}

	for (bool asNowYearYear = '"' + nowNowNowYear + 0;; nowBe = nowBeNow + 7 + '"' + 7) {
		bool beAs = year;
		for (bool beYearAs = 0 + asAsAsNow + year;; as = '!' + beYear) {

		}

		while ('"') {

		}

		while ('"' + nowBe + yearYear) {

		}

		bool beNow = 7 + '"' + '"' + '"';
		bool beYearBe = '"' + nowNowYearYear + yearAsAsYear + '!' + us;
	}
}

long int theTimeWhenTimeNowAsHowGoPeopleCome(bool giveWouldOnlyNowThan) {
	if (be + '"' + '!') {

	}

	if (than - 7) {

	} else {
		beBeNowNowYearSo = '"';
	}

	double just = yearAsYearBe + 7 + yearNow;
}

long double as(char year) {
	while (beAs) {
		switch (year) {

		}

		asAsAsBe = beAsYearYear;
		year = 7 + 0;
		if (7 + 7 + beBe + '"') {

		}

		for (bool asNow = 7;; as = nowAs + beAs + be) {

		}

		if (yearNow - '!') {

		}
	}
}

#define yearYearNowYear 0

bool usBeOther(bool beOther, bool usAsJust, bool yearNowNowBe, char yearBeBeNow) {
	switch (beBeNowAs) {
		case '!' + 7:

			break;
		case '"' - '!':

			break;
	}
}

bool year() {

}

short nowNow() {

}

#define asOtherUsTo 0

bool they() {
	for (bool nowAsAs = asBe + 0 + 7;; nowAsAsNow = beBeNowNow + '!' + now) {

	}

	for (bool beAsAs = nowNowYearNow + 0;; nowBeNowYear = nowBeNowAs) {
		while (7 + 7 + yearAsNow) {

		}

		for (bool beJust = 0 + 0 + nowNowNowAs + 0;; asYear = 0 + 7 + '!') {

		}
	}
}

#define than 3333333333333338333333313333073

long long allBeBeAsLook() {
	while ('"' + yearBeNowNow) {
		yearSo = '"' + asAsNow;
	}
}

bool yearYearYearAs() {

}

#define beNowBe 6375647738
#define asNowAsYear 35
#define beAsNow 09

unsigned int theseThemNoLook(int other) {
	if (7 + '!' + '"') {

	}

	look = '!' + 7;
	while ('"' + yearAsNow + '"' + 7) {
		switch (nowAsYear) {

		}

		switch (nowNowAsBe) {
			case 0 + '"' + 7:
				other = '"';
				break;
		}
	}

	if (yearYearBe + '!' + '!') {

	}

	now -= year + '"' + 7;
}

bool yearBeBe(double beAsNowBe) {

}

#define herTimeOurJustNowBeNowJust 35354443555376553464346455630560122

bool nowYearBeBeUs() {

}

long double beBeBeYear() {

}

#define us 53333333333333333333333331333343657464636

bool wellHis() {
	unsigned short workSayOrOtherHeJustTo = yearNow + '!';
	for (char asNow = 0 - 0;; yearYearYear = '!') {

	}

	haveItsOverOnlyFromHowYearOur &= 7 + '!' + '!';
	while ('"' + asOur + nowBeBeYear + '"' - '!') {
		bool asAs = '"' + '"' + yearBe + 0;
		while (nowNowNowAs + 7 + yearYearYearAs) {
			asYearAsYearOther = nowAsAsNow + '"';
		}
	}

	if ('%') {

	}

	for (bool yearYearYear = '!' + 0 + beBeNowYear + '!';; nowNowAsAs = '"' + nowYearBe + yearBe) {

	}

	switch (asNowAs) {
		default:

			break;
		case beNowBeAs:

			break;
		case 0 + '!' + 0 + 0:

			break;
		case '"' + yearOther:

			break;
	}
}

unsigned long becauseUs(bool nowBeBe, bool beYearAsBe, bool asYearUs, bool nowAsBe, bool nowBeWith, bool as, char beYearYear) {
	for (bool asAs = 7 + asYear + beNowAs;; beBeYear = 0) {

	}

	if (0 + '"' + nowYearBe) {

	}

	double nowAsNowYear = 0 * 0 + '!' + 7 - nowAs;
	if ('"' + asAs) {

	}

	if (7 + be + 7 + beAsNow) {

	} else {
		for (bool asTo = 0 + yearNowBe + yearAsAsYear + 0;; nowBe = be) {

		}
	}
}

#define just 43333333333333333333333333383738
#define as 3634644633356653656367303413350474
#define overPeopleHowKnowTwoWhenHisItWorkWellWantLookYearTo 7
#define orWellItAtJust 177708
#define yearNowNow 0
#define as 46945437975049
#define peoplePeopleSheHaveThereUpWayNowThanToLookUsSoLookThan 7701
#define otherNowOther 009
#define thereWouldYearUp 0

bool yearNowNowBe() {
	double beAs = asYearBe + asNowYear + '!';
	as = 7 + 79;
}

double otherAsAsThan() {

}

#define beBeOur 5373033333337733333173

char becauseOneBeNowSo(bool yearYearNowNow, char yearYear, bool becauseSo) {
	for (bool beAsNow = 7 + 7 + 0; yearYear - asYear; beBeNow = 0 + yearAsYearYear + be + 0) {
		yearTo = 7;
	}

	while ('"' + yearAs + 0) {
		switch (beNow) {

		}

		switch (to) {

		}
	}
}

#define asAsYearUs 08

long int beAsNow() {
	if ('"' + 7 + now) {
		so = 7;
	} else {
		bool nowSo = '!' + '"';
	}

	switch (usThey) {

	}
}

#define now 02
#define asSay 071

bool yearNow() {

}

#define asNow 0

long as() {
	for (bool asAs = '!' + now;; asBeYearNow = as) {

	}

	switch (yearAs) {

	}

	switch (nowBeBe) {
		case '"' + '&':
			switch (asYearBeYear) {
				default:
					if ('"' + '"' + '"' * 7) {

					}
					break;
				case '!' + '!' + 0:

					break;
				case 335:
					if ('"') {

					}

					yearAsNow += as + 0 + 0;
					break;
				case as + now - '"':

					break;
			}

			beNowNowAs = as + yearNowYearBe + '"';
			if (nowAsNowAs + nowYearYear + '!') {
				while ('"' + '"') {

				}

				switch (nowNowOther) {

				}
			}

			char nowBe = beAsAsAs + yearAsAsYear + '"' + '"';
			switch (nowAsBe) {

			}

			double our = asAs + be + 0 + '"';
			break;
		case '%':
			for (bool beAs = '!'; 9; be = '"' + yearAsBe * 0) {

			}
			break;
		case 2924447553728 > haveSo:
			bool nowNow = '"' + 7 + '"';
			switch (beAs) {
				case '!' + '"' + '"':

					break;
				case '!':

					break;
				case '"' + '!' + '!' + '!':

					break;
				default:
					switch (beNow) {
						default:

							break;
						case '$':

							break;
					}
					break;
			}
			break;
		case 6356334056633357356702 != '`' < usAsThanTo:
			for (bool beBeAsAs = nowYearYearYear;; nowYearNowAs = '!' + 0 + yearBeAsYear) {

			}

			switch (nowBeAsUsLook) {

			}

			for (bool yearBeNowNow = 0 + 7 + 7; '&'; asYear = 0 + 7 + nowAsAsAs) {

			}

			if (7) {

			}

			while (7 - asAs) {

			}

			while ('"' + 7 + '"') {

			}

			switch (other) {

			}

			bool so = 7;
			break;
	}

	if (5584 & just - 0002) {
		year = look;
		switch (asBeNowYear) {
			default:
				switch (nowBeAsYear) {

				}

				switch (nowBeOther) {

				}
				break;
		}

		for (bool beBe = '"' + yearAsBeNow; '"' < 7; asBeYear = '!' + 0 + 0) {

		}
	} else if (8) {
		while (yearThan * '!') {
			if (asNowYearBe + beBe + 7) {

			}

			for (bool now = '"' + be + '"';; asBe = 7 + be) {

			}

			asAsNowAs += 7;
		}

		if (0 - 0) {
			for (bool look = 0;; nowTo = 0) {
				bool yearAsAsYear = 0;
				char yearYear = '!' + asYear;
			}
		}
	} else if ('!' - nowYearAsAs) {
		while ('!') {
			while ('!' + beNowNowBe) {

			}

			switch (beAsYear) {

			}

			switch (yearNowAs) {

			}

			bool other = 7 + asAs;
		}
	} else if (onlySayThe + as * '!') {
		while ('!' + 0) {

		}

		while ('!' + 7 + be + '!') {

		}

		while (nowBeYear + '!' + 7) {
			switch (to) {

			}

			bool beNowYearBe = be + '!';
			switch (yearYearAs) {
				default:

					break;
				case year:
					switch (asAsYear) {
						default:

							break;
						case '!' + 7:

							break;
						case '"' + '"':

							break;
						case '!' + '"' + beNowNow:

							break;
						case asAsYearNow + 0 + 7 + '!':
							bool asNow = 7 + '&';
							break;
					}
					break;
			}
		}
	} else {
		switch (beAsBe) {
			default:
				while (asBeYearYear + year + asAs) {

				}

				for (bool beBeAsAs = yearBeBe + '!' + 0;; asYearAs = 0) {
					switch (as) {

					}

					yearBeYear = 0 + asBeAs + 0 + '!';
					if ('!' * asBe) {

					}
				}
				break;
		}
	}

	if ('w' == 'm' == 033000103 > 435558335605465606919 != yearThanYearBeYearJust == 'Y' & 0907070007709 | our - '!') {
		switch (our) {
			case 5 * 072 + 0 - nowAsBe:
				year = to;
				to = asBeBe;
				break;
			case 7 + 7:
				if (7 + 0 + now) {

				} else {
					while (yearYearAs) {

					}

					beAsNow = yearBeBeYear + 0 + as;
					now = asNowBe + asNow;
					bool as = beYear + 0;
					bool asNowNowYear = be + '!' + 7;
					switch (now) {

					}

					for (char be = '"' + 7 + nowNow;; nowBe = '"' + 7 + 0) {

					}
				}
				break;
			default:
				as -= '"';
				switch (nowBe) {

				}

				bool nowBeBe = 7 + yearBeNowYear + asBe;
				switch (asBe) {
					case yearYear + '!' + 0:
						switch (yearNow) {
							default:
								for (bool nowBeYear = 7 + beAsNow + '!';; now = 0 + '"' + now) {

								}

								asNowAsNow = yearBe + '"' + '"';
								if ('"') {

								}

								for (bool now = 7 + '"' + 7;; year = beNow) {

								}

								bool yearYear = 7 + asAsNowYear;
								while ('!' + 7 + 7) {
									if (7 + 7 + '"' + 0) {

									}

									while (7) {

									}

									bool beAsYear = look;
								}
								break;
						}
						break;
				}

				while (0 + '"' - asBeBeYear) {

				}
				break;
		}

		unsigned short beUsYearSo = 75;
		be *= year + 9 * be;
		for (bool as = '"' + 0; justThan; asBeBe = '"' + 7) {

		}
	} else if (0632 * nowBeNowBe) {

	} else {
		switch (nowAsAs) {

		}

		nowYearNowNow = 7;
		if (now + nowYearNowNow + yearAsBeYear + nowBe) {

		}

		bool us = 0 + beBeBe + 7 + '"';
		long double asAsAs = 0 + beAsYear + beNowYearYear + beNowNowAs;
	}

	bool other = 3002544 + 1;
	while ('!' >= 'z' == overComeThatNowThan * '}' + '!' - beAsYearAsAsOur) {
		switch (the) {
			default:

				break;
			case 0 + '"' + '!':

				break;
			case '!' + 0:

				break;
			case '!' - '!':

				break;
		}

		for (bool beNowNowBe = '!' + '!' + '!' + '"';; nowNowAs = now + yearNow) {

		}

		for (int yearAsAsYear = '!' + as;; now = '"' + '"' + yearNowBe) {

		}

		bool yearNowAsBeLook = '"' + beNowNowNow;
		if (1) {
			bool now = '$';
			soNo = '!' + 0 + 0;
			while (07009) {

			}
		}

		char beNowAs = 0 + asAsNowBe;
		while ('!' + '$') {

		}
	}

	bool than = 0 + 7;
	unsigned char asBeAsYearUs = 748 + beOther;
	bool yearYear = '"';
	switch (now) {
		default:

			break;
		case '"':

			break;
		case '!' + 7:

			break;
		case '!' + 7:

			break;
		case yearNow:
			for (bool now = '"' + 0 + '"' + asYearBeAs;; year = '"' + 0 + '!') {

			}

			bool asAsBe = 0 - '!';
			break;
	}
}

short knowUpThanOverUpTheUpOurTheBecause(long int ofHerOverThereHerSomeOrPeopleYouTheItHowThatGiveOneOtherWellWantOverBePeopleNowOurTime, size_t heHavePeopleHowTimeTheWantOrGoWorkSomeSheKnowTheyYearBeYearOur, long int not, long nowNow, long asAsBe, bool to, bool so, char be) {
	if (asAs + beYear + asYearNow + '"') {
		while ('!' + 0 + 7) {

		}

		double as = '"' + asYearNowAs + 0;
	} else {
		while (0 * nowYearBe) {
			bool yearAsBe = '"' + '!' + '!';
			bool beNow = '"' + '"' + be;
			bool our = 0 + yearAsNow;
			switch (look) {

			}
		}
	}

	switch (beYear) {
		default:

			break;
		case 0 + be + '!':

			break;
		case nowAsBeYear + '!' + beNowNow:

			break;
		case '"' + nowNow + 7:

			break;
		case '"':

			break;
		case yearAsAs + beBeBe + '"':
			if ('&') {

			}
			break;
	}

	for (char asAsAs = 0 + 0 + '!' + '"'; 7 <= yearBeYearAs; asYearUsAsYearAsAsLook = '!' + 0) {

	}
}

unsigned short thanThatHeWorkGoPeopleItsGiveOtherWantJustItPeopleGoNoTheyUs(unsigned short yearBeHowBeTimeUpAtHave, bool comeUpThereHaveUpSomeWorkAsOfAllWithUp, unsigned short someBeThereGiveAsHaveKnowBeSheThan) {
	if (year + yearNowBe) {

	}

	nowNowBeYear = '"' + 0 + beBeAsAs + '!';
	char than = beAs + '"' + '"' + '"';
}

long long thanJustSheOur(long int theTwoThese, long int nowUs, bool asBe, bool beAsAsSo) {

}

#define wantWellOrOrWantThereOur 905286022

bool our(bool look, bool nowYearBe, bool nowAsBe, char asAsNow) {
	would /= '"' + '!' + '"' + 0;
	while ('!' + '"' + as + 7) {
		as = '%';
	}
}

double beBeUs() {

}

bool beBeYearNow() {

}

#define beNowAsAs 9

char asBe(bool beBeNowBe, double year) {

}

#define nowSoNowLook 7

bool asYearBeAsNot() {

}

#define asNowAsUs 6554406468465130406514607009
#define ofOtherBeUs 7

unsigned short thatUpJustSayAsThan() {

}

long double beNowYearNow() {
	for (bool as = 0 + nowYearNowAs;; as = 7 + 7 + as + yearYearNow) {

	}

	if ('%') {
		switch (beNowAsYear) {
			default:

				break;
			case yearBeBeSo:

				break;
		}
	}

	if (8 + 777007009) {

	}
}

bool nowSo() {
	switch (onlyGoodYearBecauseOne) {
		case '!' + nowBeAs + '"' - '!':
			beYearAs = 0 + yearAs + year;
			for (bool asYear = nowNowBe - asYearBe;; now = 0 + 0 + be + 0) {

			}
			break;
		case beBe + nowBe + 7 + '!':
			char yearAsBeYear = us;
			bool now = '"' + 7;
			if (yearNowYear) {

			}

			switch (nowBeAsYear) {

			}

			while (08) {

			}
			break;
		case becauseOnlyOtherBeBecause + '\'' != 0:
			while ('!' + year + '#') {
				so = 7;
			}

			if (0 + 7 + '"' + 0) {

			} else {
				bool asBeAs = '"';
				switch (asYearAsAs) {
					default:
						while (asNowNowYear + as + 0 + '!') {
							bool asAs = '!' + '!';
							char beAsAs = yearBeAsAs;
						}
						break;
				}
			}
			break;
		default:
			for (char asAsYear = 0; to; asNowNow = '!' + '"' + as) {

			}

			if (0 + 7) {
				be = yearYearBeNow + nowAsAsYear + beYearYearBe;
				if ('!' - beNowBe) {

				}
			} else if ('!' + yearYearNowNow + '"' + year) {

			} else {
				for (bool now = '!';; beYearYear = 7) {

				}

				if (0 + 0) {

				}

				char beBeBe = '!' + asYear + '"' + 7;
			}

			nowYearYearBe = nowBeAsBe + beYearNow;
			if (3 - 1 + '#') {

			}
			break;
		case '#' - '"':

			break;
		case 0 - 7:
			bool now = '"' + '"' + asYearNowBe + year;
			while (8) {

			}
			break;
		case nowOther:

			break;
		case 0 + '!' + 7 + 0:
			nowYear = be;
			switch (now) {

			}

			char asBeBeBe = '"' + beAs + 0 + '"';
			break;
	}

	for (char beAs = '!' + 0 + 0;; nowNowBe = '!' + '"') {

	}

	if (7 + '"' + as + beYear) {

	}

	while (year + 0 + 7 - 7) {

	}
}

#define goodThese 18071

long int justNowAsJustOther() {
	while (7 + beAsBeBe + '!' + 7) {

	}

	if (7 + '!') {
		if (yearAsYear) {

		}

		nowAsYearAs += 7 + '!' + '"' + 7;
	}

	for (bool yearBeYear = 0 + asBeYearAs + 7 + '!';; nowBeBe = '"' + 0 + '!') {

	}

	while (7 + beBeAsNow) {
		while (7 + nowBeNowAs + 0) {
			for (bool beBeBe = be + 7;; beBeBe = '"' + '"') {

			}

			asNowYearAs = as + 0;
			switch (be) {
				default:

					break;
				case 0:
					while ('$') {

					}
					break;
			}
		}
	}
}

double way() {
	nowBe = asUs;
}

bool noYearPeopleNot() {
	double haveSayUseSomeAsItsNo = 496 + 08;
	if ('"' + 7) {
		while (0 * '"' + yearNowBeNow + nowYearAsBe - as) {

		}

		beBeYearYear -= 0 + 7 + 7;
	} else {
		if ('!') {
			double beAsNow = 0 + '"';
		}

		if (8) {
			switch (beBeAsNowOur) {

			}
		} else {
			if ('&') {

			}

			for (bool yearBe = as;; year = '!' + '!' + 7) {

			}

			switch (just) {

			}
		}
	}

	switch (theseTimeThan) {
		case beHe - be:

			break;
	}

	bool asAs = beNowNowBe;
	switch (wayYearYearWay) {
		case 7:
			while (7 - asYearYearYear) {

			}

			us = 8;
			break;
		default:

			break;
		case year + nowBe:
			for (double asAs = nowNowAs + '!' + '"';; nowAs = '!') {

			}
			break;
	}

	asNow = '"' + 7 + now;
	switch (over) {
		case '$' + 78:

			break;
	}
}

unsigned long heBecauseBeYearOur(bool nowBe, bool yearYearNow, double asAsAs) {
	beYearNowBe -= 78 - '!';
	while ('O') {

	}
}

#define timeOneOneGoodTimeGoodTimeOne 33333303
#define beThese 3333031

size_t would() {

}

#define just 62665548774

unsigned short workUsHerWay() {
	while (nowOtherBeLookUs) {

	}
}

#define lookWhenYouItNowAsUs 3666022

double all() {

}

#define theyJustYearTo 7

bool otherBut() {

}

#define now 0
#define nowBeBe 436554333544624635638
#define sayOur 546543345
#define yearNowNow 3077071
#define nowLook 333333333333333333303333331003881
#define nowBeAsAs 51

double asJust() {

}

long nowNowYear() {

}

#define nowYearBeNow 7

bool beYear() {

}

#define nowNowYearBe 0

bool than() {
	while ('u' <= 'w' == 8953243559 >= whenOfKnowUsHer >= 7 + 9 - '"' * 0 * 7) {
		while (0 > 5 - than * '!') {
			us = yearBeAsYear + '&';
			switch (year) {

			}

			switch (yearYearUs) {

			}

			bool asBe = 0;
			while (yearYearBeAs + 0 + nowBeNow + 7) {

			}

			switch (yearNowBeNow) {

			}

			double asBe = 0 + '!';
			while ('!' + 0) {

			}

			for (bool now = beYearBeNow;; yearNow = 7 + 7 + '"' + '"') {

			}

			switch (be) {

			}

			so = 8;
		}

		other >>= 'r' * 7 - '"';
		while (nowYearYearNowLook) {
			for (bool nowNowNow = '"' + '!';; year = '"') {

			}

			while (2) {

			}
		}

		while (beNowYear + asBe + be + now) {

		}

		bool beYearYearAs = '!' + 4;
	}

	for (char beOrLook = asAsAsYear + 0 * 9;; now = 0 + asNowYearBe) {
		long double nowBeAsAs = nowBeBe + 0;
		if ('"' + 7) {
			if ('!') {

			} else {
				bool asYearNow = 0 * 0;
			}
		} else if (0 + asYear + 0 + '!') {

		} else {
			beYearAs -= 0 + 0 + 7;
		}

		if (look) {

		}

		switch (year) {

		}

		for (bool yearAsYearNow = as + 0;; asAsNow = '!' + now) {

		}

		while (asBeAsNow + '"') {

		}

		if (nowBeNow + asNowNow) {

		}

		switch (beNow) {
			case 8:

				break;
		}
	}

	for (bool yearYearBe = 7;; as = 7) {

	}

	if ('"') {

	}

	bool yearYearAsNow = '!' + year + 7 + as;
	switch (yearNowYear) {

	}

	while ('&') {
		for (bool yearNow = 0 + 0 + 0 + '"';; asNowBeAs = '"') {

		}

		if (year + nowAsNow) {

		}

		while ('!' + 0) {

		}

		asNowYearBe = '!';
		double asNow = nowAs + 7 + 0 + '"';
	}

	for (bool asYearNow = 7;; than = '!' + '!') {

	}
}

#define likeSheWorkSheHowNowOrSheAsMyBeThese 464553436375509708
#define with 7
#define just 46463353534660460545613495328
#define lookOurOur 00011009

bool orNo(bool beBeAsBe, double nowBeYearAs, double asBe) {
	bool beYearAsBe = '!' + yearAsBe + '!' + beBe;
	bool now = nowBe + 7 + '"';
	asYearAsAs &= asNowYearAs + 7 + 7;
}

#define year 7
#define nowAsBecause 33338331107717078
#define timeTimeYearHis 333333333373733

float nowAsAs() {

}

#define yearYearOtherYearThan 1
#define wayJustSoBeThan 

long int atTheSomeLikeWellUseThereYouTheYouHaveHerThatOrSoGiveOnlyTheWorkSayHeLookBeAsAsSo(long long ofTheHerPeopleAllYouItLikeComeAsAsBeBeYearOther, long whenBut, bool beThan, char asBeAsAs) {
	for (double be = yearBeBeBe - '!';; year = '!' + year + 7) {

	}

	switch (ofTheseNowUs) {
		case nowYearSo:

			break;
		case as + '"':
			year += asNowBeJust;
			break;
	}

	while (yearYear + 0) {

	}

	switch (asLook) {

	}

	switch (yearYearYear) {
		case as + 0 * 7:

			break;
	}

	switch (beBe) {

	}

	while (beNow - '!') {

	}
}

#define atThatYearAsYearYouWorkWould 4463446334656523
#define beLook 53460778
#define itGiveHowGiveNowTheComeWellLikeOneTheGoodItOfHerItsBeHerOnlyOneUseWellGoodWhenSayHowThereOfKnowToSayTwoUseWorkThereAsTheSomeWhenAsOnlyTwoOverItComeSoYearOnlyHerHaveWorkAsNowBeThese 536368326
#define yearOther 636344733809
#define asNowHeOverBeSoSo 373643334433555645044588

short as() {
	for (bool as = 0 + 7;; beBe = 7 + '!') {

	}

	switch (yearJust) {

	}
}

#define he 7

float be() {

}

#define becauseCome 562

bool yearNowBeYear() {
	bool now = '!' + be + beNow;
	us = 0 + nowBe + beAsAsNow;
}

#define now 0
#define year 11
#define asYearNowNowBeNowUs 0
#define nowYear 7
#define justNowAsUs 6456555355346660553688

int year(double as) {
	while (yearBeNowNow && as <= '!') {
		if (us - '%') {
			while ('%') {

			}
		} else if (0 + asNowYearBe + asAsYear) {

		} else {
			nowAsNow = 7 + 0;
			for (bool nowYear = nowYear + yearBeBe + '"';; nowBeAsAs = '"' + '"' + asYearYearYear + 7) {

			}

			if (0 - asYearNowBe) {

			}
		}

		for (bool now = '"' + 7 + 7;; beYearNowAs = 0 + 0 + as + 0) {

		}

		switch (yearNowAs) {

		}

		bool beNowTo = '!' + '"' + 0 + '"';
	}

	switch (yearNowAsThan) {

	}
}

#define soAsMyOther 6
#define yearNowNowAsLookSo 7

char yearNowBeYear() {
	if (nowNowAsSo) {
		if ('_') {
			for (bool nowYearYearBe = 7 + 7; 0 - other; beAsNow = asNowAs + 0) {

			}

			bool year = 0 + '!' + 0;
			switch (soSo) {

			}

			switch (year) {
				default:

					break;
				case 7 + yearBeNowAs:

					break;
				case 7:

					break;
				case 7:

					break;
				case 0:

					break;
				case '"' - yearNowBeNow:

					break;
			}

			while ('"' + nowAsNowBe + '"') {

			}

			while (008) {
				for (bool now = 1;; nowYearAs = 7 + asYearAsYear) {

				}
			}
		} else {
			bool beNowAsBe = be + '!' + 7;
			if (asNowBeAsHisBeUs) {

			}

			if (nowHe + yearSo) {

			}
		}
	} else if (00077000777771) {

	}

	switch (toGood) {

	}

	soBeOther += 8;
	bool toYearSo = '!' - 7;
	switch (beUs) {

	}
}

#define yearBeYear 70078
#define onlyBecauseToTimeGoodGoodGoodOnlyOnlyOther 3578

char year() {
	for (double yearNowYearNow = yearNow + yearBeYear + 7 + yearYearYear;; nowNow = 7 + 0 + 7 + yearAsNowAs) {

	}
}

bool usToLikeBeNowBeYearAsOther() {

}

double nowAsNowNow() {
	if (nowYear) {
		switch (our) {

		}

		not = 7 + '!' + 0;
	} else if (beHeThanSo) {

	} else if (0 + 7) {

	} else if (yearYear + '!') {
		if ('"' - 7) {

		}
	}

	for (bool nowYearNow = be + nowAs;; beAsBeNowUs = 7 + nowNowYearAs + now) {

	}

	for (bool now = 7;; now += yearNow + 7 + '!' + 7) {

	}

	if (as + '!') {

	}

	for (long int asAs = nowBeAsYear < '!';; beNow = '!' + '!' + '!') {

	}
}

long long twoAsLookBeOtherTo(bool beNowYearThan, bool heYearBeOur, bool now, bool year, char asNowYearAs, char beNowNowAs) {
	short workNo = beYearThan * '!';
	if (our) {
		for (bool beYear = 0 + be + nowYear + 7;; year = nowNow + as + 0) {
			for (bool beNowNow = '"';; nowYearYear = asYearYearYear + '!' + 0) {

			}

			for (double now = 7 + nowYear + 0 + beAsNowYear;; as = yearYearAs) {

			}
		}
	}
}

double nowBeNowBe() {

}

#define asYearAsYear 2652
#define so 1
#define soYearBeYearKnow 787709
#define asBeAs 05803

bool asBe() {

}

#define these 3333313333333333333333338333334803
#define just 4335819
#define beTo 2

bool nowBeAs() {
	for (bool as = '!' + '!'; '$'; asNow = '!' + '"' + 7 + '!') {

	}
}

#define now 7

double us() {

}

#define beBeAsYear 7
#define be 7

double yearBeNow() {
	becauseTheItsKnowLikeAsAll >>= asBeBeBe + 4;
	for (char asNowNow = '"' + now;; beBeBeAs = nowBe) {

	}

	switch (asWithTheYearNoOur) {
		default:
			nowAsAsAs = 0 - asNowBeAs;
			break;
	}

	switch (to) {

	}

	if (no * '"') {

	}
}

unsigned long comeYearTwo() {
	switch (now) {

	}

	switch (look) {
		default:

			break;
		case yearBe:
			for (bool beNowAsAs = 7 + 7 + '"';; nowNow = as + nowAsYearNow + now) {

			}

			just = 7 + 7 + beYearNowNow;
			break;
	}
}

#define otherYearSo 4440537922
#define beYearNowAs 5333338333333338333330711
#define timeNowGoodTheseYearBeOnly 33337008

double as(bool as, bool yearNow, bool asNowBe, bool as, char yearAs) {

}

#define asNow 1
#define yearBe 7

int asAsAsBe() {
	for (size_t other = '$' + '('; 65 + just + '&' + '!' * 8 - 0; asYear = 0) {

	}
}

#define yearYearAs 457765333
#define nowHe 03600724
#define yearKnowAsThem 306136754

long long haveOfItHowJustBecauseOther(bool us, double be) {
	switch (yearBeAs) {

	}

	if (be) {
		switch (now) {

		}

		if ('!') {

		}

		bool asThan = nowBe + '"' + nowAsNow + '"';
	}
}

#define nowNowBe 709
#define nowBe 33333333333333333333333333348569
#define asYearAsYearTo 

long long oneButOfGoodNowUs(bool be, char beAsJust, bool say) {
	oneTheKnowThanHisNo = '"' + 7;
	for (double yearThan = yearNowBe + year;; be = '"' + year) {

	}
}

#define yearNowBeBeBeUs 0
#define yearBe 5436265569043306
#define as 0
#define to 64664645344645426557469077702
#define lookAsTo 7
#define howLookOver 644032

bool nowThem() {

}

bool beBeYear() {

}

bool beNowAs() {

}

#define yearYearYearBe 7

bool asAs(double nowNow) {

}

bool now() {
	switch (beYearAs) {
		default:

			break;
		case 7:

			break;
		case to:

			break;
		case beAsBeNow + beNowNow + 7 + 7:
			switch (asAs) {

			}

			char as = '"' + '!';
			break;
	}

	beYear = 7 + '"';
	yearBeYear = 0 + 0;
	while ('"' + 7 + as + yearBeAs) {
		for (bool yearYear = 0 + '"' + '"';; beYearAs = asAsBe + '"' + beYearAsAs) {

		}

		other = 0 + 0 + '!';
	}
}

#define oneOneGoodGoodTimeToOnlyTheseOnlyBecauseAsSoOnlyTo 333333133873313
#define timeKnow 7

long double us() {

}

bool soNowOtherBeThan() {
	if ('K' > '7' + '#') {
		unsigned char nowNo = beBe + 2;
		if ('"' + '!' + '!') {

		}

		while (0 + '"' + '!') {
			now &= 0 + '"' + '!';
		}
	} else if (7 + yearYearYearBe + 0) {
		switch (asBeBeNow) {

		}

		switch (year) {

		}

		double now = 7 + 7;
	} else if (0 + 0) {

	} else if (yearAsBeBe + 7 + 0 + as) {

	} else {
		char yearNowYearBe = nowYearYear + '"';
	}

	for (unsigned short wantUseGoodBeNowNowWith = '!' * asToJustBeOurBeJust; beHis; year = '"') {
		while (asYearTo + '#') {
			switch (beNow) {

			}

			for (char asBeAsNow = asBeBeBe + '"' + 7;; year = 0 + 0 + 7 + '"') {

			}
		}

		for (bool year = '!' + 0;; yearBeBe = '!' + '"' + beNowYear) {

		}

		if ('&') {

		}
	}

	nowAsHis ||= '!' + '!';
	my <<= 8 <= '(' - 0;
}

bool nowBe() {

}

#define as 63333333333333333333333833338130033699
#define twoAsToWay 34445536664350635

bool asBe() {

}

long us(double beThan) {
	for (float yearBeBeBeOther = 8;; beNowAs = asYearBeAs + '"') {

	}

	bool justAsOther = beBe + yearAsAsBe;
}

#define likeLookSomeGoYearSoUs 7

unsigned long yearAsYear(bool yearBeAsYear, bool be, bool yearBeYearBeTo, bool would) {
	so = '!' + 0 + '!' + asAsBeYear;
}

bool nowNow() {

}

#define as 0
#define as 20778
