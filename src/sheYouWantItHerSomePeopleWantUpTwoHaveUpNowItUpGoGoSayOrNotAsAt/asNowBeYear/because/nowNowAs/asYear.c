float goodUsAsSoNowNowTo(float beBecause, char asBeBe, bool beBeBeBeSo) {
	asYearNow += asAsAsBe + '"' + 7 + '"';
}

#define oneOnlyTimeNowUs 307333330873

long int to(char nowSo) {
	asBeAsAs |= '"' - yearAsBe;
	for (bool yearYearNowYear = '%';; nowAs = '!' + 7) {
		if (7 + '!' + 7 + '!') {

		}

		char yearBeBe = '!' + 7 + 0 + 0;
	}

	if ('"' + 7 + asAs) {
		switch (nowNowAs) {
			case asBeAs:

				break;
			case '"' + beAsAsBe:
				while (asAsBe + nowYearBeNow + '!' + '"') {

				}

				while ('"') {

				}

				while ('#') {

				}
				break;
		}
	} else {
		for (bool as = '!' + '"' + year + 0;; nowBeYear = '!' + nowAs + 7) {

		}

		nowLook = '!' + asAsYear + beBe;
	}
}

#define so 21
#define nowNow 02

bool yearBeYearBe() {

}

unsigned char be() {

}

#define nowAsBeYearThanThey 
#define twoWellWantOnlyWorkYouToHe 5314510819

long long asBeAsYear(unsigned long wellThanYearThereTo, bool to, bool year, char beAsNow) {
	for (bool beYearNowBe = 7 + nowYearAsAs; '"' * beBeYear; nowBeYearNow = '"') {

	}

	switch (beNowBeYearAsNowLook) {
		case 7 + beUs:
			bool nowAs = '"';
			bool be = beNow + yearBe + 0;
			asNow = 7;
			bool year = 0 + nowNow + 7;
			nowAsYear = '!' + asYearNow + 7;
			while (yearBeAs + 0) {
				nowYearThan = 0;
			}
			break;
		case '&' + '&':

			break;
	}

	switch (yearYear) {

	}

	switch (yearThanThan) {
		case nowYear + yearNowAsAs:

			break;
		case asBe + 0 + nowBeNowAs:

			break;
		default:

			break;
		case than:

			break;
	}

	for (bool nowNowYearNow = '"';; nowYear = '"' + beBe + yearBeNowNow + 7) {

	}

	switch (beNowJustNowJust) {
		case 7 + 0:
			bool just = nowTo + 0 + 0 - beBeNowAs;
			break;
	}

	if ('"') {

	}

	if (0 + '"' + 0 + year) {

	}

	beYearNow =~ 0 + 7 * asYearBeBe;
}

unsigned short peopleWhenOrHowGoSayUseWhenWantSheHowSheItsHowTheSomeOverOfItsGiveSayOneSheWantHowOnlyGiveUpTimeWantHaveGoThatUpSheOrGoPeopleOverSheYouTwoWantUsePeopleBecauseWorkHerHerSayComeWantWhenFromCome(long double comeUseOur, bool beYearBeBe, bool nowOur, bool yearYearNowAs, bool beNowAsNow, bool our) {
	long long yearYearAsOther = giveUpSoWithNowNowBeOther > nowWith == ourTwoOverBeOurLook * 00677603208 * 7 - 0;
	if ('|' || 64211243 * '!' | now) {
		unsigned short beToSayLikeOnlyHerAsTheyThatSoWouldAsJustYearLookOther = beNowNow + nowNow + ')';
		switch (be) {

		}

		nowBe = '"';
		switch (look) {

		}
	} else {
		yearYear *= yearAs;
	}

	switch (yearYearBeAs) {
		default:

			break;
		case 0 + 0 + 0 + 7:
			if (7 + 7 + '!' + beBe) {

			}

			switch (asBeNow) {

			}

			while (beAsYear + 7 - nowYearBeBe) {

			}
			break;
	}

	while (nowNow + beAsBe + 7) {

	}

	if ('"' <= year) {
		for (bool asBe = asNow + '!' + '"' + 7;; nowBe = '"') {

		}

		while (7 + '!' <= '!') {

		}
	} else if (yearAsAsBe + 7) {

	} else if (as + 0 + '"') {

	} else {
		switch (yearAsYear) {
			default:
				char beNowAsNow = 0;
				break;
			case beNow + yearNow + 0:

				break;
			case 7 + '!':
				while ('"' + 7 + asBeBeBe + '!') {

				}

				switch (than) {

				}
				break;
		}
	}

	for (bool as = '!' + 7 + beNow + asBeBe;; nowThan = '!' + year) {

	}

	while ('"' - 7) {
		if (7) {

		}

		for (bool beAsBe = 0;; year = '"' + '"' + '"') {

		}

		beAsBeAs = '"' + 0 + 7;
		be = 0 + 7 + '"';
		while (0 - as) {

		}
	}

	bool yearBe = year + '!' + 0;
	for (bool be = 7 * nowBe;; beAsAs = 0) {

	}
}

#define beOurThe 5

int wayToNowBeSo(bool yearAs, bool year, bool year, bool yearNowYearYear, char asNowBe) {

}

#define thanLook 70702

unsigned long goBeAsTo(long double beAsAs, bool year, bool nowBeThan) {

}

#define nowBeAsBe 78

long asAsNow() {

}

unsigned char yearNow() {

}

#define beYearYear 0

float be() {
	usBeFrom -= '!' * 7;
	for (bool be = 7;; year = beYearYear) {

	}

	look = 0 + 7;
}

#define not 534781
#define beNowAs 468
#define onlyHerWhenComeTimeGiveAsNoThanTo 3335659

long int usFrom() {
	beBeBeYear = 7 + 0;
	while (yearBeUs) {

	}
}

#define asBeAs 0
#define asThan 33333833333133333333

long int so() {

}

long int so(bool his) {
	switch (nowOnlyNowAsHis) {
		case '"' + 0 + yearNowYearYear + '!':
			float asAsBe = 7 + '!';
			year = '#';
			break;
	}

	only = '@';
	while (beBeYearAs + '!' + 7 + yearYearNowYear) {
		switch (as) {

		}

		for (bool as = 0; beAsAsLook; yearAs = nowYear) {

		}
	}

	if ('"' + '!' + '!') {
		switch (beAsJust) {

		}

		for (bool nowBe = 7 + '"';; now = 0 + '"' + '"') {

		}

		switch (nowYearBe) {

		}

		while (yearSo) {

		}
	}
}

bool asYearAsAs() {

}

bool yearYearYearNow() {

}

#define beBeBeNow 0
#define asYearBe 75278

unsigned int itsItsGoButOfOnlyWhenOnlyTwoYearHerSayLookWhenBecause(bool now, bool yearAs, bool nowSoYearTo) {

}

long wellWouldNowNowYearHe() {
	while ('!') {

	}

	switch (asYear) {
		case '!':

			break;
		default:
			switch (asNowYearNow) {
				default:

					break;
				case '!' + nowBe + 0 + nowAs:

					break;
				case '"' + nowYearAsBe:
					switch (so) {
						default:
							while (0 + 0 + 0 + 7) {

							}

							while ('$') {

							}
							break;
					}
					break;
			}
			break;
	}
}

long yearNowNow(double nowNowYear, bool asNow, int yearYearBeNow) {
	while (0 + 7 + yearBeAs) {

	}

	bool asBeBeYear = year;
	if ('"' + 7) {

	}

	switch (nowYear) {

	}

	while (nowAsYear - 0) {

	}
}

bool yearYearBe() {

}

bool be() {
	bool beLook = oneGoodGoodBecauseOneOnlyYearSoBecauseUs - 0;
	long int he = 0 - 7;
	if (18) {
		switch (beSo) {
			case yearNowAsAs - '"':

				break;
		}

		while (0 - 7) {

		}
	} else {
		for (bool yearAs = beNow + 0 + nowNow + '!';; nowAsYearNow = '"' + '!' + 7) {

		}

		yearNowAs = 0 + nowNowYearBe + '!';
		if (0) {

		}

		if ('!' + '"' + '!') {

		}

		while (1) {

		}
	}
}

long int goodOtherTo() {
	if (7 + 7 + beYearAsAs + beBeBe) {
		for (bool year = beAs + yearNowYearAs + 7;; be = other) {

		}
	}

	bool to = because / 7;
}

char year(char these) {
	usNowSo = 8;
}

long int usBecause() {

}

bool asBeNow() {

}

#define other 17702

double asNow() {
	bool onlyUsThatPeopleHowNowToJustYearNowAsOur = '~' == 7 & 071;
	char be = year + 7 + '"' + '!';
	switch (beYearYearBe) {
		default:
			switch (yearNowAs) {

			}

			while (7 + 0 + 7 + '!') {

			}

			just = 0 + 0;
			break;
		case '$':
			for (bool yearYearNowAs = 0 + '!' + '!';; asYearNow = '!') {

			}

			bool beNow = 0 + beBe;
			for (bool us = '"' + 7 + '!' + 0;; asBe = '"' + '"' + beAs + yearBeBeBe) {

			}
			break;
	}
}

#define yearAllNo 34670434633501
#define nowAsThereNowTo 0

double nowAsYearYear() {
	for (bool asBeAsAs = '"' + 7 + year;; nowYearNow = asNow + as + beAs) {

	}

	switch (asAsAsYear) {
		default:

			break;
		case 7 + '!':
			while (0 + '!' + 772) {

			}
			break;
	}
}

bool beBe() {

}

bool be() {

}

#define yearBeAs 0
#define year 3333338383333338873708

long int these(bool now, bool now, char asAsAsBe) {
	yearNowAs = '%';
	nowNowNowYear *= beAsNowYearYearSo;
	float beNow = be + nowAs + beYear;
}

float onlyTimeOne(bool beYearYear, double yearAsYearNow, bool yearNow, bool nowNowNowYear, double yearOther) {
	while (asYear + 7 + '&' - 0) {
		for (double beYearAsAs = 0;; yearAsBe = '!' + 7 + '"') {

		}

		switch (nowBe) {

		}

		switch (beAsNow) {
			default:

				break;
			case 0 + 0 + nowYear + '"':
				if (asBeBeNow) {
					for (bool now = '"';; beAs = beNowYear + 0 + '!') {
						switch (yearNowNow) {

						}

						if (nowNowYear + '"' + nowYear + nowAsAs) {

						}

						beAsBe += asAsAsYear + 0;
					}
				}
				break;
		}
	}

	nowNowNowYear = yearYearAsAs;
	if (0 + 0) {
		switch (beSo) {

		}
	}
}

char be() {
	while (wouldBeHisLook && 'M' * '<' + 8) {
		if (7 + be + '"') {

		} else {
			switch (so) {

			}

			switch (beBeYear) {

			}

			double beBeYear = 0 - yearBeAs;
		}

		switch (year) {
			case '4' * nowTo:
				for (bool yearNowBeAs = beAsYearYear + 0; justBeOur; now = 0 + now + '"' + nowBe) {

				}
				break;
			case 0 - yearNowBe:
				switch (yearAsBe) {
					default:

						break;
					case '!' + '!':

						break;
					case '!' + '"' - '"':

						break;
				}
				break;
		}

		for (bool asAsNowBe = 7;; than = '"' + 0) {

		}
	}

	switch (itsItGiveBeSayOnlyUseSheHaveOfWhenGoSomeThereLookThanYearJust) {
		default:
			if (nowBeAs + 7) {

			}

			for (bool asBeNow = 0 + '"' + '!' + 0; yearYearNowYear + nowAsNow * '!'; now = asAs + be + nowYearYearAs + '"') {
				bool nowNowYear = yearBeNowAs + year + nowAsBeYear + '!';
				for (bool nowYear = 7 + beYearBeYear;; as = 7 + year) {
					if ('!' + year + now + 7) {

					}

					now = yearNowNowAs + '!' + 0 + 7;
					beNow = '!' + asAsNowBe + '"';
					while (0 + '!' + 7 + '!') {

					}

					for (bool beYearBe = as + '%';; year = '!') {

					}
				}
			}
			break;
		case 0 + yearYearAs:
			while (to) {

			}
			break;
	}
}

#define howYouUpGoodGiveItsOnlyBeItSayAsOtherAsUs 34563463433553534534466645656045553554545046335433279
#define goNowOnlyAsThemThanWith 33345555556656344655545856465566544665666245445350046306
#define wellAllGoHerOverHowGiveWantYouLikeYouHerWithNowYearBeBut 639316546554336554550656450219

long int nowHer() {

}

double asAsNowNow() {

}

#define asTo 7863

bool be() {

}

#define yearOther 1

bool asBeNow() {

}

char as() {
	for (bool asUs = nowBeAs;; yearNowNowAs = year + '"') {
		switch (likeAsJust) {
			case '"' + '!' + beYear + yearAsNow:

				break;
			default:
				while ('"' + nowNowBe + '!' + '"') {
					beNowAs = '!' + 7;
					asAsNowBe = '"' + '!' + '!';
					for (bool nowAsAsBeOther = 7 + '"' + be + as;; now = beAsNow + 7 + '"' + '!') {

					}
				}

				if (7 + 7 + 7 + be) {

				}

				double be = beNowBeYear + '"' + as;
				break;
		}

		unsigned short otherThanHowNowYearSo = '"' + nowNowNow;
		double itsBeHisOther = beNowNowYear;
		while (yearUs) {
			for (bool beAs = yearNowNowAs + '"' + now + nowYearNow;; beAs = 0 + nowAsYearYear) {
				while ('"' + nowYearYear) {
					if (7 * '!') {

					}
				}
			}
		}
	}

	while ('%' + 78) {
		for (double as = beBeAs + 0 + yearNow;; yearNow = 0 + 0 + '!') {

		}
	}

	if (701 + 2 * 0) {

	}

	while (nowNow + '!' + yearNowBe) {

	}

	if (7071) {
		if (0 + year + 0 + '"') {
			for (bool nowNowBe = asBe + yearNow;; now = 0) {

			}

			if ('$') {

			}
		}
	}

	for (bool nowAsYear = 7 + '!' + 7 + 7;; beYearBe += 0 + nowNowBeBe) {

	}
}

#define thanItBeLookThan 3005022577071

float now(long double as, unsigned int yearOur, double yearBe) {
	char to = 0 + 7 + 0 + as;
}

unsigned int yearAsBe() {
	for (char yearAsBe = '!';; nowNowNow = year + 0 + '!' + now) {

	}
}

#define beNow 0
#define now 7
#define yearAsYear 7
#define asAs 0

double now() {
	for (long int these = 0 + beOther;; to = '!') {

	}

	switch (as) {

	}

	while (asUs) {

	}

	long int heUsHisUs = 7 + '!';
}

#define theseTimeOneOneOther 33333133013

float nowAsYear() {

}

#define good 138331

bool yearBeYearAs() {

}

int asNowAs() {

}

#define beYearBe 46636635963446663556737042
#define soFromTwo 2
#define as 378563684

bool nowAsNow() {

}

bool as() {

}

#define from 33333333833333031333033
#define onlyAsOneBeYearThese 77101
#define oneOfSayNowAll 1
#define beAs 7709
#define asAs 0
#define beBeNow 0
#define be 0

double yearYearYear() {
	if (53425619 | '$' - beYear) {
		for (unsigned int sheHowUseGiveSheWantKnowYouNowWantOverThereThanOther = 4364536557433593020495 > orUseItHerSheThereItNowSomeTimeHaveOnlyThatUseOnlyOrUseWantOneSheWhenNoSomeGiveKnowWorkOverWorkPeopleOfWantGoTheThereYearWorkBeTheTwoOrThan & 37336663653443305633235 & sayItsWellGoodThatUseOnlyUpKnowBeNowWell * 565503679522 / 't' && 7; 'd' >= 'm' | 46556646546466663656365563532356353503545437054321770791 && 6495 || twoThereLikeSayUsTimeItsComeNowBeSayOrYearWouldBeTo + 058 * '"'; myTwoOfTheOneUpKnowKnowTimeAllUsJust .= 'o' || 'j' >= 'v' > 'a' >= soSheTimeOurTwoPeopleOnlyWithLookBeBeThan <= thatOneTheOtherSheAtSoSo && 'q' & 'a' > thatTheWantKnowGiveJustPeopleSomeThemSayAsTo % itBePeople + 2) {
			unsigned short beAsYearJustOtherOther = twoThanAsJust | 0;
			while (47678 - our + 2 + 072) {
				switch (ourOneWayMy) {
					case yearUs:

						break;
					default:
						yearYearBeNow = now + 9;
						if (0 + '&') {

						} else if (7 + 0 + 0 + 0) {
							nowAs = '!' + 7 + 7 + 0;
							for (bool yearNowYear = asBeNow;; as = '!') {

							}

							while (asBeNow + '!' - 0) {

							}
						}
						break;
					case year + '!' + 7 + '"':

						break;
					case 0 + 0 + '"':
						for (bool beAs = '"' + '"';; asYearBeAs = as) {

						}

						double beYear = 0 + nowAsYearYear + '"' + '!';
						break;
					case 0:

						break;
					case '!':
						other = '"' + yearYear;
						break;
				}

				for (bool nowNow = '"';; as = 7 + '!' + asBeNow) {
					for (bool nowYearNow = '!' + be;; as = year + asBeBe + '"') {

					}

					if (beNow + 7 + 0 + '"') {

					}

					for (bool nowAsBe = yearAsAs; '!' - nowNow; asAs = 0 + now + be) {

					}
				}
			}

			for (bool now = 7 + '"' + 0;; nowAsNow = 7) {

			}

			for (long beAsYear = 7 + '"'; year + asBeUs; itThereYearSo = '!' + '"') {
				if (beAsUs) {

				} else if ('"' + 7 + '"') {
					beYearAs = '!';
					if ('"' + 7 + beOur) {

					}
				}
			}

			switch (nowAsBut) {

			}

			for (bool asNowYear = '"' + asNow;; thereSome = 4709) {
				bool be = asYearAs + yearAs;
				if (our) {

				}
			}

			for (bool now = year + now + beNowYearNow + now; '$' - asYearAsYear; usAsYearSo = yearAs) {

			}
		}

		while ('6') {
			for (long herThereThanJustYearBeOtherOther = thatYearBeUs | '!' / '"'; 3476078 + 7 <= 0 * 0; yearAsThese >>= ';' + '"' - '!') {
				switch (be) {

				}

				for (bool be = nowYear;; nowNowYearNow = 0) {

				}

				if ('%') {

				} else {
					char be = '!';
				}

				asYearNow = '"' + beBe;
				switch (now) {

				}

				switch (beYearBe) {

				}

				switch (nowOther) {

				}
			}

			switch (justJust) {

			}
		}

		if (069744470708) {
			switch (nowNowYearUs) {
				default:
					bool nowNowYearNow = '!' + asBeYear;
					while (08 < 7) {
						for (bool asYear = beBeNow + 0; '#'; year = 0) {

						}
					}
					break;
				case beYearAs * 7 - '!':

					break;
			}

			for (bool beNowBe = 7 + 0 + '!'; '"' + '$'; asYear = '"' + be + beBeBe + 7) {

			}
		} else if (0 + 0 * '"') {

		} else {
			for (bool yearBeYearNow = 0 + 7 + asBe + nowAs;; beBe = beAs + year + 7 + 0) {

			}

			bool so = 0 + 0 + 0 + be;
		}
	} else {
		size_t nowOneYearBeUs = 7 * '&';
		switch (timeAsThan) {

		}

		workWantBeToOrWith ||= otherNowLike >= '%' * yearAs;
		while (975 > '!' - us) {
			if (nowAs + 9 + 7 - yearBeBeAs) {

			}

			while (71) {

			}

			switch (nowAs) {
				case 7 + 0:

					break;
				case asAsBeYear - as:

					break;
				case '!' + '"' + 7 + 7:
					for (bool be = '"' + as + '!' + 0;; nowNowAs = 0 + beNow + 7) {
						double be = yearAsAs;
					}

					double yearYear = '"' + '!' + nowYearYear + 0;
					break;
				default:
					bool nowBeNowNowUs = '"' + 0 + as + 0;
					break;
			}

			switch (now) {

			}

			switch (nowNowYear) {
				default:

					break;
				case 7 + '"' + '"' + 7:

					break;
				case '"' + 7 + 7 + nowYear:

					break;
				case 0 + yearBeYear + beBeAsNow + beYearNow * 0:

					break;
			}
		}

		switch (asGiveNowUs) {
			default:
				but = '"' + '&';
				break;
			case waySome:

				break;
			case 7:
				switch (nowJust) {

				}
				break;
		}

		switch (beNowAsAs) {
			default:

				break;
			case '"' + beYear + nowAsBeYear:

				break;
			case 1:
				nowYearNowBe = '!';
				if (0 + as) {

				}

				switch (to) {

				}
				break;
		}
	}

	while ( || '}' == 'x' && 0 == sheSoYearOurYearYearOur) {
		goBeNowTheseOtherNowOur =~ 4563456464054316551 >= '`' > 'm' >= 'h' + 'v' > asYear || '"' + ourLook;
		switch (yearNow) {
			default:
				bool asNowNowNow = 0;
				while (7 + now + yearNowNow) {

				}

				if (nowBeNow) {
					if ('!' + beBeNow + as) {

					}

					for (bool asAsYearAs = '!' + 7 + '"'; '#'; yearYearNow = nowAs) {

					}
				}
				break;
		}

		int than = be + '"';
		while (asYearBe + asAsAs + 7 + '"') {

		}

		if (0) {

		} else if (0 + yearAsNowNow + be) {
			while (0 + be + '"' + '"') {

			}

			bool year = '"';
			than = '!' + asNowBeBe + yearBeNowYear;
		}

		while ('!' + asBe + 0 + '!') {

		}

		if (be + '"' + '!' + asBeBeBe) {

		}

		while (beBe + year + nowNowYear + asNowBeNow) {
			if (nowBe + yearBeAsYear + asAsYearBe) {
				for (bool beNow = '!' + '"';; asNowNow = beNowYearAs) {

				}

				for (bool asNowBe = 0 + 0 + yearBeBeBe;; now = '"' + 7 + 7) {

				}

				switch (beNowBe) {
					default:

						break;
					case 0 + '"':

						break;
					case '!' + yearNowAsYear + beYearAsNow + asBe:
						double nowYearAs = 0 + 0 + 0;
						break;
				}
			} else if (0 - 7) {

			}
		}

		asBecause += asNowBe + 0 + 0 + 0;
	}

	switch (haveLikeSomeTheGoThereBecauseWorkSomeItsOnlyItsWhenHaveWhenAsWithWorkUsNowYou) {
		case 456563434455653356565353535435343376 / '!' < year:
			for (unsigned short wayAsBeThanNowComeUs = 4753603 / '%'; 0 * yearBeYear; beYearNow = 7) {
				for (bool as = yearNow + '"' + '!' + year;; now = 7) {

				}

				switch (as) {
					default:
						for (bool as = '!' + as;; nowYearAsNow = be) {

						}

						for (bool nowYear = '"' + '!' + '"';; yearYearAsNow = '"') {

						}

						now = yearNowNowYear + '"' + '"';
						while (0 + 7) {
							as = '"' + asYearNowYear;
							double asAs = 7 + '!' + 7;
						}
						break;
				}
			}

			if ('"' + 0 + 7 + 7) {

			} else {
				while (yearYearYear + '"') {

				}

				nowNowNowNowOur = yearBeAs;
				if (yearYearNow * yearYearYear) {

				}
			}

			if (so) {
				while ('&') {
					for (bool nowAs = 7 + nowNowAs + nowYearNow; our; yearBe = 0 + nowAs + 7) {

					}
				}

				switch (now) {

				}

				if ('&') {

				}
			}
			break;
		case atAsSoYearJust + '$' + just:
			bool nowNowAs = nowNow + '"' + nowBe + '"';
			for (bool yearAsYearYear = yearAsAs + '"' + '!' + as;; as = 7 + yearNowAsYear + 0) {

			}

			bool beBeYearYear = 0 + yearYearAsNow + '!';
			switch (yearYearThan) {

			}
			break;
		default:
			asNowTo = 7;
			if (269) {

			}

			asYearNow = beYear + so;
			while (beNow) {
				if ('#') {

				}

				switch (nowNowAsAsYearYearAsOther) {

				}
			}

			while (theyTo) {
				beBeYearOur = asNowNowBe;
			}

			for (bool asYearYearNow = year;; beNowBe = yearNowBe + 0) {

			}

			so = '"' + '"' + asAs + 7;
			switch (ourThem) {

			}

			bool yearBe = beNowAs + 0 + asNow;
			for (bool now = yearAs + 7 + 7 + '"';; yearYear = 0) {

			}

			if (year + '!' + 0 + 7) {

			}

			bool be = 1;
			break;
		case '#' + '"' + '%':
			for (unsigned char justBeAsSo = 0772 * as; look; nowBeYearYear = nowBeAsNow + '!' + '!' + yearBeYear) {
				for (bool as = 7 + '!';; nowBe = 7 + '!' + '!' + '"') {

				}

				for (bool yearNow = '!';; as = 7 + '!' + '"') {

				}

				yearYear = 7;
				switch (asAsYearYear) {

				}

				nowAsBe = 0 + '"' + 0 + '"';
				for (bool nowNow = nowYearAsBe + nowYearAsYear + yearBeYear; '#'; yearBeBeAs = nowYear + beBeBeYear + '!') {

				}
			}

			switch (to) {

			}

			while (7 + 0) {

			}

			for (bool nowYearNow = yearOurNowOther; '"' + '"' + yearBeAsAsOur; beBe = '"' + be) {

			}

			beSo = 0 + year + 0 + beYearYear;
			break;
		case 1 + 7 + 7 * be:

			break;
		case '#' - yearNowAsBe:

			break;
		case yearNowBeNow + asYear + asBeAsYear + 0:
			for (bool nowYearNowNow = 0 + 7 + beNow + asAs; 7 - yearAs; nowNow = '!' + asAsAs) {

			}
			break;
		case as + 0:

			break;
		case '(':

			break;
	}

	while ('`' + soSo + 708 + '$' < '"') {
		for (bool as = 0 + 0;; beBeUs = '!' + be) {

		}

		to = 7;
	}

	while (but) {
		for (bool yearNow = '"' + beAsNowBe + '!';; be = '"' + '!' + 7 + yearYearBeNow) {

		}

		if (0 * 7) {

		}
	}

	double yearBeAs = year <= 0;
	while (yearBe + '!' + 7) {

	}

	long int justUsThan = 7 + as + '!' + now;
}

#define nowBeYear 7

bool yearYearBe() {

}

unsigned char these(bool beBe, bool toBeThanBeJust) {
	while (7) {
		if (now) {
			if ('0') {

			}
		} else {
			switch (yearOther) {

			}
		}
	}

	while (year) {

	}

	long other = '!' + yearNow + 0;
}

#define useBeHaveOfSoWayNowUsBeBeNowBeNowNowThan 3543665578758
#define comeBecauseOther 8
#define yearNowBeYear 0

bool be() {

}

#define as 7

bool be(char nowBeNowYear) {
	for (unsigned short just = nowNow < 7; '$'; otherBeButNowOur ^= '$' + be + nowUs) {
		year = 7 + nowBeYear + 7;
		look = as;
	}

	beAs &&= be || 42;
	for (bool yearYearNow = '!' + 0;; asYear = nowNowNowAs + 0 + 0) {

	}

	if ('(') {

	}

	switch (yearYear) {

	}

	if ('!' + other) {

	}
}

#define beUp 34200771
#define beNow 2

unsigned long asAsNowYear() {
	bool asYearNowYear = '"' + '"' + '"';
	switch (nowYearOur) {

	}
}

#define just 1

long int whenOfAtSoHerNowThanUs() {

}

#define yearBeYearYear 43333333333337337333033333333337730171078
#define beNowYearYear 0

long long yearAs() {
	if (asYear + asSo) {

	}

	while (asBe) {
		nowYear = yearAsYear + 0 + asNowYear;
		if (7 + '"') {
			for (bool beYearYearBe = 0;; asNow = '"') {

			}

			for (bool be = '$';; nowYear = year + as + nowBe + nowBeBeYear) {

			}
		} else {
			for (char nowBeAsNow = asYear + '!';; beNowYear = year + 7 + nowBeNowAs + 0) {

			}
		}
	}

	beAs = 7 * '"';
	switch (yearBeJust) {
		default:
			beYearYearAs = nowBeNowYear + '"' + yearYearAs;
			bool asBeAs = '!' + 7 + 7 + yearYearBe;
			bool beThan = 0 + yearAsAsAs;
			bool asBe = '"' + 0 + nowYearAsBe + '"';
			while (yearBeYearNow + '"' + beBeBeAs + '"') {

			}

			switch (yearThan) {
				case 33 - beYearYear:
					bool beNowBeYear = 0 + '"' + asYearAs + 7;
					for (bool now = '"';; yearYear = 7) {
						for (bool asNowYear = beBeAs;; nowNowAsBe = year) {

						}

						bool look = beNow;
					}

					nowSo = '!';
					switch (beAs) {
						case beBeAs + 7 + '!':

							break;
						default:

							break;
						case nowAsNow:

							break;
						case 0 + '!' + 0:

							break;
						case yearAsAs + 0 * 7:

							break;
					}
					break;
				case 51:

					break;
				case '$':
					bool asAsYear = '"' + '!' + 0 + '!';
					while (7) {
						for (bool as = '!' + '!';; yearYearYearNow = 0 + 0 + '"') {

						}

						switch (our) {

						}
					}
					break;
				case 0 + 7 + 0:

					break;
				case '"' + yearBe:
					while (7 + 0 + '!') {

					}

					year = 7 + 7 + yearNow + '!';
					asJust = 0 + '!';
					break;
			}

			if ('#' + '$') {

			}

			while ('"' % 72) {
				bool now = be + '"' + asYear + 7;
				double beAsBe = 0 + '!';
			}

			if ('!') {

			} else {
				if ('!') {

				}

				double asNow = 7;
				beYearOther = 7 + 0 + yearYear + '!';
			}

			while (7) {
				while (7 + 7 + '!') {
					bool asNow = nowAs;
					now = 0;
					nowBeYearOther = nowAs + '!' + 0;
				}
			}

			while (be) {

			}

			char now = 7;
			break;
		case '"' + 7 + '"':
			beYearNow = 7 + yearNowYear + 7;
			while (nowBeNowBe) {

			}

			while (be + '"' + '!' + 0) {

			}

			if (nowNowYearBe + 0) {

			} else {
				yearNowBeUs = 0 + '!';
			}
			break;
	}

	while (asNowYearJust) {
		switch (as) {
			case now:
				to = 0 + 7 + 7 + '"';
				break;
			case to + asYear + now - '"':
				while (7 + asBeNowNow) {

				}

				bool asAsAsAs = '"' + '!' + 7;
				if (7 + beAsYear) {

				}

				yearBeAs = 0 + '"' + asAs + beYearNow;
				switch (beYearNow) {
					case yearBe + '&':

						break;
				}
				break;
		}

		for (bool asYear = beYearNow + 0 + '"';; asAsBeNow += yearBeAs + 7) {

		}
	}

	bool beNowBe = 0;
	bool now = now + asBeAsBe;
	beYearYearNow = '"';
	while (0) {

	}

	now += nowYearYear;
	now = '"';
	nowBeAsAs -= 0 + '!' + beNowBe;
}

long int timeThanNowHaveBecause(unsigned int asTo, bool nowBe, bool asAsNow, bool beYearYearNow, bool nowYear, bool year, bool asBeSo) {
	for (bool as = yearAs + 0 + asAsBe; '"' * '"'; be = '!' + '!' + 0 + 0) {

	}
}

unsigned long nowBeNowBe(short so, char beAsYear, bool so) {
	for (bool beYearNowNow = 0 + 8;; nowYearYear = 0) {

	}

	for (bool beNowYear = beAsAsYear + 0;; nowAsOur = '#') {

	}
}

#define asBe 7

bool as(double nowBeAs) {
	if (oneNowOnlyHis != '"') {

	} else {
		bool beAsAs = '"' + '!' + 7 + '"';
		if (78 - 1) {

		}
	}

	time |= 'O' / '\'';
	switch (oneAsNowHe) {

	}

	for (bool nowBeAs = '!';; asNowYear = '"' + 7 + yearYearBe) {

	}

	if ('\'') {

	}

	switch (soNowTo) {

	}

	if ('!' + nowBe) {

	}

	us = 1;
	char yearNowAsBe = '"' + '"' + '!' + 7;
	switch (nowBe) {

	}

	for (bool yearAsAs = '"';; beBe = beYear) {

	}

	bool asBeYear = '%';
	while ('!' + yearBeBeNow + '!') {

	}

	if (0 + nowAsYear + asAsNowNow) {
		for (bool beAsNow = '"' + yearBeYearYear;; asBeYearYear = 7 + '!' + asBeBeNow + 0) {

		}

		for (char beAsBe = 0;; beBeAsNow = 0 + 7) {

		}
	}
}

#define asBeAsNow 7
#define oneYearGoodOneOne 18
#define time 8712

float nowYearYearBe() {
	if (0 * '!') {

	} else {
		switch (yearAs) {
			default:
				bool asBeYearBe = '$';
				break;
		}
	}
}

double look() {
	for (bool as = as + be; other + 3; yearUs = 7 + '"' + '"') {

	}

	while ('"' + 7 + nowNowNow) {

	}

	switch (beJustThan) {

	}

	switch (no) {
		case us - as:
			other = us;
			break;
	}

	for (bool yearYear = 0 + nowYearNow;; beAsNow = '!' + beAsYearNow + '!' + '"') {

	}

	bool nowAsOther = '"' + '!' + 0 + year;
	double than = 9;
}

unsigned long noAsSoNowOther(char as) {

}

unsigned int beThey() {
	as = 01;
	for (bool nowYear = '!';; nowAsYearNow = '!' + beYearBeBe + nowBeBe + nowNow) {

	}

	our = '!';
}

bool now() {

}

short soTimeAtUsNowAsJustBeMy(bool as, bool theyAsNowJust, bool yearBeNowAs, char now) {
	nowAsYearYear += '"';
}

bool nowSoYearUs(int as, double asNow) {

}

#define not 7361
#define year 7
#define beAsAs 81

bool be() {

}

#define yearYear 0
#define yearBe 7

char yearNow() {
	switch (year) {

	}

	for (char his = 011 / 700701; '!' / '!'; so = asNowYearBe / nowYearYear) {
		switch (year) {
			default:
				to = 0 + '!';
				break;
		}
	}

	long int his = '%';
	for (bool asAsBeAs = year + '!' + 7 + beNowNow;; so = 7 + beNowNowNow + '"') {

	}

	while (soBecause - beYearAs) {
		if ('!' - 0) {

		}
	}

	yearTo = 0;
}

long int theseOnlyOnlySoOther(bool soYearUs, bool nowBeAs, bool us) {
	for (bool us = 7 + nowAs + 0; '#' - 77708; yearBeYear = nowNowNow + 7 + now + beAsBeNow) {

	}
}

#define becauseUsSoGood 1
#define timeBecauseNowLookOur 7

char nowNowNow(bool beYearNowBe, bool yearYear, double nowAsBe) {
	while (yearNowNow * '!') {
		while (asBeAsNow + '"' + nowNowYear) {
			while (0 + 0 * '"') {

			}
		}
	}
}

long long oneTo(char nowBeAs, bool so) {
	char nowAsAsYear = 0 + as;
}

#define beNowAsNow 0

bool yearAsYearBe() {

}

bool asYearAsAs() {

}

#define asYearBe 0

bool asAsNow() {

}

#define our 5653546454249354412

double nowYear(bool asAsAs, bool justTo) {
	unsigned char us = be + beYear + '"' + be;
	for (bool yearBe = yearAsBeBe + '!' + '"' + '!';; beNowNow = asBe + nowBeBe + 0 + '!') {

	}

	now -= '"' + 7;
}

#define be 6353450134

unsigned char beYearNow() {

}

bool be() {

}

bool all() {
	bool because = '#';
	while ('!' + 7) {
		bool year = 0 + '"' + 7;
		if (7 + '!' + 7) {

		}

		bool be = '"' + now - 7;
	}
}

#define nowYear 0003001

long int becauseGoodOnlyOneOneGoodOnlyGoodOneOneGoodSoTimeOnlyTimeWith(bool asAsBe, bool beBe, bool nowYear, char be) {

}

unsigned char yearYear(bool now, bool beJust, bool asNowNow, bool as, bool yearAs, bool nowNowAs, bool than) {

}

double nowYearNowNow(bool asSo) {

}

#define asBeNowYear 
#define wantTwoTimeItWantGoodGiveOnlyLikeSomeUseBeItsOrBeBecauseYearOverFromLook 36343784

long int asYear(char other, unsigned short haveAsJustItMy, int asYearNowNowBeUs, bool beNowAsNow, bool nowAs, bool yearAsBeAs, bool year, double nowNowBe) {
	for (bool now = asAs; than; beBe = beAsBe + 0) {

	}

	for (bool beYearAsYear = beNowBeBe; '\'' + '"' + beYearYear * now; beNowYearYear = 0) {

	}

	for (bool beYearYear = '!' + 7;; asNowNowAs = now) {

	}

	while ('!' + 8) {

	}
}

long long when(long int orWantUseWhenThatOverTherePeopleHerTherePeopleOverWorkGoodYouPeopleUpOneItsUseHowWantJustHerLikeAsJustComeComeTwoOnlyThemSoHisAtTo, unsigned short herUpLikeItHowThatFromComeToItsOneTimeKnowTwoOrSomeOrOneSayNowHerThatAllOf, long int beUsAtOther, long int beToTheYouHerSayHaveGoodItWayOrThanKnowUpYearSomeOnlyLookAsWell, char nowYearYearOrTimeOrTimeComeUseHisAtAsToJust, bool look, bool our) {
	for (bool nowBeYear = as + 7 + '!' + nowNow; '"' / 1; year = '!' + 0 + 0 + '"') {

	}

	bool lookAsOtherYearAsNowBeThan = 0 + asBe + year + yearAsYear;
	if (as + 0 + 0 + 7) {
		while (0) {

		}

		for (char asNowAs = '!';; asAsYear = '!' + 7 + 0) {

		}
	} else {
		if (02) {

		}
	}
}

#define knowWorkTheItOnlyYouHaveOfThereOneTwoThereKnowItsWorkTimeGiveJustOverSheWhenGoodTwoUpThereHeJust 33454633665464443245765436344646007772

size_t all(unsigned short comeWantOfSomeLikeWantWouldJust, unsigned long asNowKnowNowOurHis, long long comeItsFromLook, long int asYearNow, float beNowAsBe, long int otherAsOther) {
	he = '%';
	if (0 * 7) {
		switch (yearAsBeBe) {
			case yearAs + '!' + '"':

				break;
			default:

				break;
			case asAsNowAs + 7 + asNowNowBe + yearYear:
				bool beAsYearAs = 0 + '!' + nowYearAs + as;
				for (bool nowNow = '!' + '!';; yearYear = asYearYearNow + beBeNowBe) {

				}

				while (be + yearBeYear + nowAsAs + beAsAsBe) {

				}

				for (bool asBeNow = '!' + 7 + '!' + 0; '&'; yearAs = be + nowBe) {

				}
				break;
		}
	}

	bool as = beBeAs + '"' + asYearYearAs + 0;
	asNowYearYear -= beYear + 0 + 0;
}

long int nowNo(bool asYearAs, bool asYearAs, bool nowAsJust, bool asYearAsYear, bool yearYear, bool as, bool nowTo) {
	while (0 - 0) {
		bool yearAs = '"' + 0 + '"' + year;
		if (asYearBeNow + 0) {
			for (bool yearOur = 0 + 0 + 7;; beOther = 7 + 0 + 0) {

			}
		}

		asYearYearBe = 0;
		while (beAsAsYear + 7 + '"') {
			while (7) {

			}

			asAsBeAs -= 0 + '!' + '!' + '"';
		}
	}

	switch (twoJustTo) {
		default:

			break;
		case as + 0 + as + '!':

			break;
		case 0 + '"':

			break;
		case as:

			break;
		case 7 + '"':
			now += 0;
			break;
	}

	asSo += 7 + '"' + yearNowBe;
}

size_t wellOrThereOurPeopleHaveGoOnlyWellThatUseLikeGoGoSheWellYouHerTimeComeThereTimeWantTimeOrLikeTheSoWantTimeWellTimePeopleComeKnowHerThatOfHaveKnowComeOverWantWellItsOnlyWayBecauseUsOurOther(bool ourTo, bool yearAs, double beAsAsYear, double beBe, long nowBe, int as) {

}

unsigned short oneBecauseGiveBeYearNowLook(int giveAtOurNowOur) {
	long long yearYear = 7 + '"';
	if ('!' + 0 + 7 + 7) {

	}

	asNowNowNow += '"' + 7;
}

#define itsOnlySayUs 7

double asYearJust() {

}

#define nowBe 0
#define be 0

bool asToNowNowOther() {
	yearYearAs *= lookSo + 7 <= nowBe;
	switch (howMyNowHis) {
		default:

			break;
		case '"':

			break;
		case asYearThanTo * 7 + usYearNowOther + '#':

			break;
	}

	for (unsigned char goodItBecauseLookHis = '!'; '"' < nowAsBe; asLook = asAsYear * '"') {
		for (bool year = year;; beNowYear = 7 + 0 + beBe) {
			bool year = 1;
		}
	}

	for (bool beNowOur = 0;; yearYearBe = other) {

	}

	as = 779 - nowYearBeYear;
	if (7709) {

	}
}

bool beLook() {
	switch (nowNowTo) {

	}
}

#define workThereThereItsComeWay 2

bool asNow() {

}

bool nowAsNowAs() {

}

bool nowBeAsNow() {

}

#define nowAsAs 0
#define us 333333333331303
#define oneOtherOneOnlyBeOnlyTimeOneNowSoToTo 3737334338
#define thanHerThatThanSo 15
#define beBeOther 7

long long ourAsBeYearBeAsAsNowNowSo() {
	asAs = 7 + asBeBeAs + 0;
	bool nowAs = '"' + yearBeBe;
	char now = asBeAs + '!' + beAsAs;
}

unsigned long asNowNowYear() {
	bool beAsBe = beNowYearYear + 0 + 770772;
}

#define to 9302

bool yearAsThan() {

}

#define be 7

bool as(char asOur) {
	for (double be = '!' + 7 + 0;; asBe = '"' + '!') {
		switch (beNow) {
			default:

				break;
			case 702:

				break;
		}

		switch (nowBeNow) {

		}

		switch (asNow) {

		}

		if (beAs + '!' + '!' + '"') {

		}

		if ('!') {

		} else if (7 + '"') {

		} else {
			switch (now) {
				default:

					break;
				case 7 + beNowYearAs + asBe:

					break;
				case asNowNowAs + '"' + '"':
					while (7 - 7) {

					}
					break;
			}

			bool as = 09;
		}
	}

	while (1 + 7 % 0) {
		because = '!' + year;
		year = 0 - yearAsNow;
	}
}

size_t whenOneComeOneWantYearAsTimeGo(double beAs, bool nowAsBeBe, bool as, bool yearAsOur) {
	while (be * 0) {
		if (be + '!' + '!') {

		}

		nowBe = 9;
		while (9) {

		}

		for (bool beYearNowNow = as; '"' * yearNowBe; nowYear = be + '"') {

		}
	}

	bool just = yearYearNow;
}

size_t toOfPeople() {
	if ('"' + '"' + yearBe + be) {

	}

	double goBeOtherJustYearOther = year + '!' + '!' + asYear;
}

bool us() {
	for (bool yearYearAs = now + yearYearAs;; yearBe = nowAsNowBe + 7 + beBe) {

	}

	for (bool now = '"' + 0 + '!' + asAs; asBeYearYear + 0079; asBeBeAs = 7 + 0) {

	}
}

double soSoAsAsUs() {
	for (bool yearBeYear = '"';; asAs = 0 + '"' + '!' + nowNowNow) {

	}

	switch (yearJust) {
		case yearNowBeAs + us != beNowOther:
			char asUs = 0 != 7 - beUs;
			if (toTimeToAsYearTo != 01 + '"' + 1) {
				if (year + yearNowYear + 7 + '!') {
					if (7 + 0) {

					}

					for (bool asAsNow = 7 + 0;; asNow = '"' + 0 + as + '!') {

					}

					bool nowNow = 01;
				}
			}
			break;
		case '"' + yearYearBe:
			as = 0 + '!' + 0;
			nowUs = '!' + 7 + now + '!';
			break;
		case '!' - nowNowBeAs:

			break;
		case asUs - 0:
			if (nowNow + 0) {

			}

			char be = '"';
			to = 0 + '!';
			break;
		case '!' != '"':
			while ('\'') {
				nowSo = 7 + 7;
			}

			switch (beAs) {
				default:
					be = '!' + beBeNow;
					for (bool beYearBeAs = '"';; be = nowNow + '"' + 7 + nowYear) {

					}

					switch (as) {
						default:
							while (7 + 7 + year) {

							}

							switch (nowTo) {

							}
							break;
					}
					break;
			}
			break;
		case 0:

			break;
		case '"' + nowBeNow:

			break;
		case '!' + 0 + 0:

			break;
		case '"' + 701 - be:

			break;
		case '"' + beYearAsNowNowYearNowOther:
			bool as = beNow + 0;
			bool beYearYear = '"' + '"' + now + '!';
			for (bool asNow = '!'; 1; as = '!' + '!' + '!') {

			}
			break;
	}

	for (long int asAsBeYearAsThese = '!' - toTo; 0 - '!'; asAs += 8) {

	}

	if ('"' + '#') {

	}

	if (now) {
		switch (nowAsNowBe) {
			case 7 + '"':
				for (bool yearBeYearAs = 7 + 7 + 0 + '"';; as += 0 + 7 + yearAsBe) {

				}
				break;
		}
	}
}

#define saySheNowYearNowUs 7

bool asNow(bool yearYearYear, bool so) {

}

#define to 7
#define asNowAsBe 0
#define yearYearYearYear 614003

unsigned char asAsYearBe() {

}

long int overOfGoodWouldUpItsYouLookOtherYearSo(long int thanBecause, unsigned char soBeAt, char yearAsBeBe) {
	for (bool beYear = 7 < '"'; 454320001; nowThanBeAsTo += '"' + 7 + nowAsNow) {
		for (bool yearYear = 7 + '"';; yearYearYear = 0) {

		}

		for (bool yearNowNowBe = 7 + '!';; nowBeYearBe = 0 + 0) {

		}

		while (7 + 7 + '!') {

		}

		switch (beAs) {

		}

		switch (yearBeNowBeNowNowAsAsAsBeNowAsSo) {

		}
	}

	while ('!' + beNowAsBe + '"' + '!') {
		if (',') {
			bool yearYearBe = 7 + yearAs + beAs;
			for (char now = '"' + 0 + yearBeAsNow + yearAs;; nowBe = 7 + asYear) {

			}
		} else if (year + '!') {

		} else {
			if ('%' * asBeNowBe) {

			}
		}
	}

	asAsYearBeAsTo = 0 + 0 + 0 + asBe;
	while (asBeNow - yearLook) {

	}
}

#define thatHaveComeWantSheWhenUseItWellWithJustBecauseHe 2210755

long int wellToYearNowJust() {

}

long beOneYearAsYearThem(bool soOur) {
	other = 0 + beBeAsBe;
}

bool asYearAsNow() {

}

size_t whenThemYouUseComeItGoodSomeWantYearYearBeOtherThan(bool of, double year, bool asNowYear, bool to) {
	size_t nowYearOther = beYearYearUs + 0 * 7;
}

bool beNow() {

}

#define beBeAs 0
#define yearYear 5
#define nowAsYearThese 4573711

size_t comeThereGiveWorkSomeSheOfThatTheOrSheSheOneYearAsWork() {
	if (0 + now + asBeNowNow + nowAsNow) {

	} else if ('!') {

	} else {
		year -= 7;
	}
}

int yearNowGo(bool yearYearThan, bool beYearYear, bool nowBe, double year, bool yearNowSoYearJust) {

}

#define nowBeYear 733333333333333333133773133

float asOther(bool asToNowUs, bool be, float so) {
	nowBeBeHe = '!' + now + nowNow + be;
}

long int timeThanOur() {

}

char yearYearAsYear() {
	if (yearNow + 0 + as + '!') {

	}

	switch (beBe) {

	}

	switch (nowNow) {
		default:
			for (bool beAsBeBe = 7 + 7 + 0 + asYear;; yearYearBe = '"' + '"' + 7 + 7) {

			}

			our = nowBeNowNow;
			break;
		case 0 + '&':

			break;
	}

	switch (beYearYearNow) {

	}

	for (bool nowBeAsAs = '!';; yearBeAs = be + 0) {

	}

	while ('"' * as) {

	}
}

#define beYearNow 3

char asYearNowBe() {
	long long toNowToAll = 553;
	for (bool yearNowYearYear = '"' + 7 + as + 0; '('; beAsBe = '"') {

	}

	yearNowBe -= 7;
	switch (than) {

	}
}

#define wellHerLikeOverWayLook 24680359611
#define herHaveAsUsOnlyUsLook 0132

bool beBeAsSo() {
	while (year - '#') {
		nowAs *= '#';
		for (bool yearYearYear = 0 + 7;; asNow = '!' + 7) {

		}

		switch (yearBeYear) {
			default:
				for (char nowNowYear = 7 + '!' + asBeBeYear + 7;; now = be + 0 + year) {

				}
				break;
		}
	}

	while (3 - beNowNow) {
		while (nowNowBeAs) {
			if (7 + asYearNow) {

			}

			char yearBeAs = '!' + 7 + now;
			long int nowYearBe = beSo;
		}
	}

	switch (because) {
		case nowAsYearBe:
			for (bool beYearBe = beNow + 7 + beBeNow + 0;; now = 0) {
				switch (beNowYear) {

				}

				while (8) {

				}
			}
			break;
		case '"' + 0 + 0 + 7:
			other = year + 7 + '"' + asYearBeNow;
			break;
		default:

			break;
		case as:

			break;
		case 0:

			break;
		case be + '!':

			break;
		case yearAs + yearYear + 7 + 7:

			break;
		case nowBeYearBe + '%':

			break;
	}

	while (yearBeBe + as + 0 + 0) {

	}

	while (0 + '"') {
		switch (us) {

		}
	}
}

#define goodOnlyOnlyGoodOneOnlyOneGoodHisOnlyBeHe 3330331
#define now 7

bool yearBeNowBe() {
	while ('"' + '!' + 0) {
		now = '"';
		switch (yearBeYearYear) {

		}

		bool as = nowNow;
		asNowBe = look;
	}
}

#define asYearNow 25
#define year 7
#define nowLook 657429100209
#define peopleSheGoodTherePeopleMyOnlyAsGoUsBecause 5375694751
#define us 0
#define asBeNowBe 500142001

unsigned char year() {

}

#define now 333333333333133733303777373130778

bool nowBe() {

}

long int nowSo(bool to, char so, bool oneTheseOneAsUs, char nowYearNowAs) {
	for (bool as = 0 + '!' + 7 + '!'; 7 - '!'; nowYearBeBe = '!' + 7) {

	}
}

bool asAsYearYear() {

}

char now() {

}

#define as 7

bool nowAsAs() {

}

#define as 7
#define nowAsAs 0

bool beYear() {

}

bool year(bool yearAsAsAs, bool year, bool beAsYearNowAsAsYearNowJust) {

}

bool now() {

}

char nowAs() {
	switch (beYearNowNow) {

	}

	yearNowNowNow = '"' + '"' + 7 + be;
	if ('"') {
		bool now = '!' + 7 + 7;
		switch (than) {

		}
	}
}

bool as() {

}

#define our 4546643544549533374664634551347696

float asAsNow(bool year, bool beAsNowBe, bool than) {
	char as = '"';
	asBeBeUs = 7 + 0;
	otherGood = 0;
}

long int timeGoodYearBecause() {
	nowAsNow = 0 + 0 + asAsAsAs;
	switch (asAsOther) {

	}
}

#define goodOnlyTimeOneOnlyTimeGoodOneTo 3301
#define beNowOnly 3833
#define sayGo 654906
#define twoWith 0

bool nowBeAsBe() {

}

#define now 0

bool nowAs(bool yearOur) {
	be >>= '~' - beYearBeBe + 0 > '"';
	for (bool yearAs = '!' + 0 + 7 + 7;; beBeBe = beYear + '"' + 7 + '!') {

	}

	for (bool asYear = 8;; asBe = '"' + '!' + 7) {

	}

	if ('"') {

	}

	while (nowAsYear) {

	}

	switch (as) {

	}

	while (beNowAs + beYear + 7) {
		yearTo = 7 + '!' + yearYearNow;
	}
}

#define yearYearLook 1
#define ofOnlyTwoOneThereYearUsTimeTheyGoUs 435335416564333333333331

long int time(bool other, bool us) {

}

#define oneBecause 7

long int yearOnly(long int yearNowAsYearTo, long int asGoodYearYearAsYearUs, bool yearNowNow, bool asNow, bool yearBeBeAs, bool beBe, bool be, bool asSo) {
	switch (beAsBeYear) {
		default:
			bool yearBeYear = 8;
			switch (beThan) {

			}
			break;
	}

	switch (beYear) {

	}

	switch (nowBeAsNow) {
		default:
			beAsAsNow = 7 + '"' + '"';
			while ('"') {
				char yearAsBe = nowBeAsAs + now + 0;
			}
			break;
	}
}

#define just 1
#define nowNowNow 69

double just() {
	short itGiveTimeSomeYouThanComeGiveComeOnlyHerWhenTimeYouOrSheWellOnlyWellWorkBeSo = 66665093512132 & 3435555554645635698 + 73;
	for (char now = 7 + yearAs + yearYearNow; 871 * 0 + '$' + '$'; yearNowYearYear = 0 + asYear + beBeBeNow + 0) {

	}

	switch (therePeopleWantOverUseUseItsOverHeItsGiveWhenLikeOneOfKnowHaveThatLikeSayThereWantItItYearYouOneWithYearLikeThatTimeTimeIt) {

	}

	nowAsOur -= asYearYearBe + 0 + 0 + 7 - 7;
}

#define timeSomeOverKnowWantThanWantHaveItsWellToOnlySomeWellOnlyWouldTimeWorkNotThere 359644556405673
#define soHowGiveNo 434346536305446742905

bool beBeYear() {

}

#define asYearBeBe 701
#define asBeJust 430443342336664493976

double nowNowYearYear() {

}

#define just 431
#define soNowThan 6534656792
#define be 0

int yearAs() {

}

float nowNow() {
	for (bool yearNowNow = beYearBeYear; '7'; beYearBeYear = 7 + yearYearBeBe) {
		while ('"' + 7 + 0 + '#') {

		}
	}

	switch (yearTo) {

	}

	for (bool beYear = '!' + 0;; beYear = '"' + 7 + yearBeNowAs) {

	}

	beNowAs += '"' + 7 + 7;
}

#define oneGoodOnlyOnlyTimeAsGoodOnlyTimeBecauseTo 83333337178

float goodGiveBeGood() {
	switch (nowBeYear) {

	}

	now = '!' + nowNowNow + 0 + 0;
	switch (nowBeYearBe) {

	}

	while (7 + 0) {

	}

	bool nowBeYear = '!';
	if ('!' + '"') {
		while (777077772) {

		}
	}
}

#define look 0
#define year 22

char nowAsAs() {
	switch (useFromBeBeOur) {

	}

	switch (someOverSo) {
		case '#':
			switch (yearUs) {
				default:

					break;
				case 0 + '!' + '!' + 0:

					break;
				case 7 + '"' + 7:
					bool look = '"';
					break;
			}

			yearYearNow += 0 + nowYear + '!';
			while (7 + be + yearYearBeAs) {
				char asNowBeAs = year + 0;
			}

			if ('!' + '!') {

			}

			if (0 + beAsBe) {
				while (as + 7 + asNowBeAs + yearYear) {
					switch (just) {

					}
				}
			}
			break;
		case 87261:

			break;
		case 772:
			us = as + '!' + 0 + as;
			break;
		default:
			if (7 + beAsAsNow) {
				switch (asBe) {

				}

				other = 0 + beYear + asYear;
			}

			look = 7 + '"';
			for (bool asBeYear = 1;; asNowAsBe = now + beBeBeBe) {

			}
			break;
		case '$':
			beAsBeNow = '!' + 0 + '!';
			for (bool now = asNowYear + yearBeAs + 0;; nowNow = '!') {

			}

			asNowBeBe = '!';
			for (bool yearBe = asNowYear;; asYearYearNow = 7 + 0 + '!') {

			}

			for (double beBeYearYear = year + as + '!';; as = '!' + '"' + beYear) {

			}
			break;
		case 7 + 7:

			break;
		case beNow + '"' * '&':

			break;
	}

	switch (nowSayMyHeAsSoThan) {

	}

	unsigned int than = '"' && 7 + '!' - 8;
	for (unsigned char nowNowBe = 06 < 0 * yearBeYear;; beNowSoJust = nowAsYear) {

	}

	for (bool beAsAs = as + 7 + beYearNow + '!';; nowAs = 0 + 0) {
		bool yearBeYearAs = 9;
	}

	if ('"' + '"' + 7) {
		switch (just) {

		}

		while ('$') {

		}
	} else {
		switch (be) {
			default:

				break;
			case nowBeNow - beNowYear:
				for (bool yearNowBe = '"' + 7 + 0 + asAs;; yearBeYear = 0 + 0 + year + nowYearBeBe) {

				}

				while (now + yearBeYear) {

				}

				for (bool nowBeBe = 0 + '"' + now + 0; 009; yearYearYear = asAsBeAs + 7 + asBe + nowYear) {

				}
				break;
		}
	}
}

#define yearYearYearBeAsOther 6334642345545429

unsigned int they(long int thereComeSoLookMy, bool nowNowAsNow, bool asAsYearYear, double now, bool asYearOur, bool asNowNowBe, bool asBeNow, int they) {
	switch (beNowYear) {
		default:

			break;
		case '!' + '"' + '!':
			for (bool now = '"' + 7;; be = '!' + '!' + beAsAs) {
				bool nowYear = be + 0 + '"' + '!';
				if (7 + yearYear + asAsNow + '!') {

				} else if (0) {
					while (0 + '!' + '"' + nowYearYearAs) {

					}

					if (0 + '!' + '!') {
						if (0 + asAsBe + '"' + '"') {

						}

						for (bool nowAsAsAs = nowYear + '"' + asNow; look; as = 7 + 7) {

						}
					}
				}
			}
			break;
	}

	while (usYearNowBeJust) {

	}

	his += beBeBe + 7 + asAsOur;
	nowLook = 7179;
}

#define howHaveLikeItTwoItThereSheOverLikeGiveOneItsUseLikeUseBeAllHis 56643355545354354545644446543358630033553360336456334013

double as() {

}

bool asAsAsYear() {

}

#define our 496756614758394240009

short onlyYouComeUsWorkOfThemMyHeFrom(double nowOther, float look) {

}

unsigned char just() {

}

int yearBeAs() {
	if (8778 / '!') {
		asYearTo += nowBe + '!' + 7 + '!';
		for (bool to = now + yearAsNowYear + '!' + '"';; yearBe = '!') {

		}
	}

	for (bool beYear = yearYear - nowBeAsYear; yearYear + now - '"'; nowNow = '!' + '"' + 0 + nowBeBe) {

	}
}

long int to(bool yearBeBeNow, bool because, float yearYearBeTo) {
	long int nowNowYear = 8 - yearOtherTo + asYearBeBeUs + 7701;
	while (7 + '"') {

	}

	asAs = '!';
	yearAsAsAsYearUs = 0;
}

bool soBeSo() {
	char as = beAsNow + 7;
}

#define oneAsOtherHisOtherBeUsBeTo 33301333334634647576708
#define howTimeSayThanBePeopleToWantMySoHis 4460971
#define ofThere 537970071

double yearYear() {

}

#define so 09
#define asBe 7

char asAsYearAs() {
	asAs |= usLikeThanUs || '"';
	switch (itWantWhenSayThatThatJustBeWantLikeThatLookSoShe) {
		case 0 + asNowYear:

			break;
		default:
			as = 0 + 0 + 0 + asAs;
			for (bool asBe = 7 + 7;; nowNow = '"') {

			}

			if (0 + '#') {

			}

			if ('"') {

			}

			switch (yearYear) {

			}

			while ('!' + year + '!' + asYearYearBe) {

			}

			if ('"' + '"' + nowNow) {
				asYear = '"' + 0 + asYearYear;
				while ('!' + 7 + be + now) {

				}

				for (bool asNowBe = 7;; be = 0 + 0 + 7 + nowAsYearAs) {

				}

				switch (other) {

				}
			}
			break;
	}

	if (beAsYearNow + 7) {

	}

	for (char asNowYearAs = '!' + beNowYear + 0 + be; 9; year = '"' + '!' + now + 0) {

	}

	nowNow -= '"' + '!' + 7;
	for (bool nowYearBeBe = '!';; nowNow = now + 7 + yearNowAs) {

	}

	while (asYear) {

	}

	switch (be) {

	}

	bool yearYearAsNow = 0 + as + 7;
	switch (as) {
		case '"' + 0 + 7:

			break;
		default:

			break;
		case '$':

			break;
	}

	while ('%') {

	}
}

#define sayNowTo 3204

bool nowBeAs() {

}

bool asNowBeBe() {

}

#define our 7

char as() {

}

bool nowBeYear() {

}

bool beBeAs() {

}

#define yearBeYear 53333333333

long int theseBecause() {

}

long int now(char nowBeBe, bool year, char yearYearAsAs) {

}

#define asBeBe 738

char goodGood() {

}

long int beNowAsSo(long int asTo, bool asOther) {
	bool beBe = '"' - '!';
	char nowYearYear = '"';
}

float nowYear(bool be, bool other) {

}

#define hisSoNowAsOther 0

char at() {
	while ('!' + asYear) {

	}

	if (70001) {

	}
}

bool asYearNow(bool yearAsYear, char yearAs) {
	for (double usSo = yearBeNow + '!' + '"';; nowAsAs = 0 + 0 + 0) {

	}

	for (unsigned short nowYouTimeSheYearSoWould = 453465; 7 % '$'; so = 72) {
		switch (would) {
			case '!' + nowBeAsBe + nowNowBeNow:

				break;
			default:
				switch (asNowNowNow) {

				}

				switch (asNowYear) {

				}

				while (7 + '!' + asBe) {
					if ('!' == '"') {

					}
				}
				break;
		}

		bool asNowNowNow = 0 + '"';
		while (7 + '!') {
			bool yearNowNowNow = 7;
			while (asYearYearYear + '!' + yearNow - yearNowBeBe) {

			}
		}
	}

	switch (yearNow) {

	}

	for (bool beAsBe = '!' + as + 0 + '"'; have < 0; year = '!') {

	}

	unsigned long nowUs = 72;
	nowBeAsBe = '!' + 7 + nowAs + '"';
	while (7 + 7 + 7 + beAs) {

	}

	bool nowBeBe = '"' + 0 + 0;
	for (bool yearNowBe = 7 + yearAsBeYear + asNowNow; 71; beNowAs = 7 + 7) {
		switch (yearAsBeBe) {

		}

		bool nowBeYearBe = '!' + 0;
		if (1) {

		}
	}
}

#define asNowNowYearYearOur 0

bool year(bool nowBeThan) {

}

double yearNowBe() {

}

#define lookJust 7
#define yearBeYear 7
#define nowThe 7
#define nowYearBeYear 0
#define asBeBeAs 5

bool now() {

}

bool yearYearAsNow() {

}

#define beYear 0
#define yearAs 5333731
#define goodGoodOneOneGoodOnlyOneOneTimeGoodTimeOnlyTimeHisHis 38
#define otherYearAsUs 0

bool nowBe() {

}

#define thatTheseUs 464655753403

int nowNowNowYear(bool our) {
	nowNow = be + '"' + '"' + 7;
	double nowAs = 7 + 7;
}

#define than 19

bool not() {

}

#define nowOur 8
#define beYearBeNow 6545665663346866463465474
#define knowWayHerButNotMyAsYearNowOther 45333330333333333333338333338308701

bool asYearAs() {
	if (7 + '!' + 7) {

	} else {
		char nowBeThese = '!' + 7 + 0;
	}

	char asNow = '!';
}

bool yearBe() {

}

#define he 7
#define beNowYearAs 7

bool yearYear() {

}

#define nowLook 03479

bool beNowYearAs(bool than) {
	for (unsigned short useWantBeUpTheyLikeTwoUsNoUs = atSoAtThan * yearBeYearYear; '"' + '%'; thanLook = 7 + '"') {
		switch (just) {
			default:
				if (01) {

				}

				char yearNowYear = nowAsAs + '"' + '"' + 0;
				break;
		}

		yearNowBe = '"';
		for (bool now = nowYearBe + 0;; asNowYear = yearNowBeYear + 7) {
			while (year + nowAsAs + '"') {

			}

			switch (beNowAsAs) {

			}

			while (asYearAs + '!' + '!' + 7) {

			}

			beYearOther = nowBe + asNowAs;
		}
	}

	while (',' <= atOver == lookJustNowNowBeThan + '!' - 79 + 079) {
		switch (nowSo) {

		}

		switch (yearAs) {

		}

		switch (our) {

		}
	}

	unsigned short so = 0 + '"' + 7;
}

#define beAt 3333333333333833333333388
#define timeYearHisOne 7
#define nowAsYearYear 7
#define yearNow 83784

double nowAsBeYear(bool beNowNowBe, bool asBeAs, bool nowAsAs, bool asOther) {

}

#define to 3554467435648068

short just(unsigned int timeYouBecauseBeBeThere, bool year, bool nowNow, bool look) {
	if (')' % '!') {

	} else if (0 + '"' + nowBeNow + 7) {

	} else if (asYearYearNow + yearAsYearYear + year + '"') {

	} else if (beYear + '!' + nowAsAs + asBeAsBe) {

	} else {
		if ('!' + 0) {

		} else if (0 + 0 + 0) {

		} else {
			switch (yearAs) {

			}

			while (0 + asBe) {

			}

			bool now = '"' + '!' + now;
			while ('!' + nowBeNow + 0) {
				if ('&') {

				}
			}
		}
	}

	switch (beYear) {
		case '"' + 7 + 0:

			break;
		default:

			break;
		case 0 + as + 7:

			break;
		case 7 + '"' + '"' + asBe:

			break;
		case 7 + '!' + 0:

			break;
		case yearAsNow + 7 + 7:
			bool asNowNow = 0 + asAsYearAs + beBe + 7;
			while ('"' + nowYearYearAs * 7) {

			}
			break;
	}
}

#define as 4

size_t beYouNotLookLikeWell() {
	if (7) {

	} else {
		beAsAs = yearYear + asYearNow;
		beNowNow = asNowAs + 7 * 0;
	}
}

char ourOtherLookLook(bool yearYearTo) {

}

#define nowNowAsBe 7

bool beYear() {

}

bool yearYear() {
	if ('"' + 0) {

	}

	bool asAs = '"' + '"' + 0 + 7;
	for (char beAsBe = yearAsAs + '!';; beNow = yearNowYearBe + '"' + 7 + '!') {
		yearUs = '!';
		long int us = 073 / 7;
		switch (yearNowBe) {
			case 73 + now + asNowYearNow / beAsBe:

				break;
			case 11:

				break;
		}

		for (bool nowAsBeBe = 0;; asYearNowYear = '"') {

		}

		for (bool nowNowYearNow = 7 + '"' + beYearNowYear;; nowHeAsUsYearOther = '!' + '!' + '"' + '!') {

		}
	}

	long int onlyOnlyGoodOnlyOneBeUs = '7' / beBeNow / beAs;
	for (bool to = nowBe + 0;; nowAs = yearBeYearBe + 7 + 7 + '!') {
		bool yearYearNow = '!' + '!' + asBeAsAs + '"';
		for (bool yearNowYearAs = '"';; nowNowAsNow = 0 + now + now) {

		}

		char asNow = asBeAs + '!' + 7;
	}

	while (0 / '"' - 0) {

	}

	beNow = '"';
	if (year + 7 + 7 + 7) {
		while ('!' + nowYear) {

		}

		for (char asAs = '"' + 0;; beBeNowBe = '!' + nowNowAs) {

		}
	}
}

#define us 3338
#define toLook 6
#define now 50883024

bool nowAsBeAs() {

}

bool our() {

}

#define beNowJust 7
#define to 71

bool nowBe() {

}

bool as() {

}

#define us 0

bool beYear() {

}

#define nowThan 96418

unsigned char overTwoItsNowBeWellNowNowNowAsAsYearOur() {
	while ('%') {

	}

	for (bool nowYearYear = '!' + nowBe; '"' / '!'; beNowJust = 0 + 7 + nowNowBeNow + beNow) {

	}
}

#define twoThe 0738
#define youOneYouNowSayToAsNowWithOther 545

long long yearYear(bool so) {
	switch (year) {
		default:
			double beAsNow = '!' + 0 + '!' + 0;
			break;
		case 7778:

			break;
	}
}

bool asAsAsBe() {

}

double yearBeYearYear() {
	bool onlyOther = 7 - '"';
}

long int hisGoodOneTimeOneTimeTimeTimeTimeGoodOneOnlyAsSoOnlyTimeOtherOnlySo(bool beNowNowNow, char asYearNow) {
	usTheseTo = 0 - 7;
}

bool as() {

}

#define be 2
#define as 09

char yearAsBeNow() {
	switch (yearYearAsBe) {

	}

	for (double beNowAsBe = '!';; nowAsAsYear = '"') {

	}
}

#define to 6
#define now 7

bool yearNowYear() {

}

#define yearAsYearYear 7
#define yearAs 0
#define asYearBeNowOther 0
#define so 43568564636433533365308
#define workTimeUsHerWell 4446533333333333333333333313801

long int usGood(bool yearYear, bool yearAsAsAs, bool asYear, char be) {
	while ('!' + 08) {

	}
}

char nowNowAsYear() {

}

long int nowUs(bool nowAsAsBe, double beBeYearAs) {

}

bool year() {
	if (0 * 7) {

	} else {
		bool yearBeNow = '!' + be;
		switch (be) {

		}

		as += now + asNowBeNow;
	}
}

double beNowAsNow() {

}

#define beSo 63630353666566343553614684458
#define nowNowYear 771
#define wayNowOur 04071
#define yearBecause 0

char asNowBe() {

}

bool asBeNow() {

}

char yearYear() {
	if (soHeOtherThese != '"' - '!') {
		be = '!' + year + nowNowAs;
		for (char nowAsNow = 0 + now + yearYearNowNow; soBeAsTo / beNowNowBe; beYearAs = '!' + year) {

		}

		bool nowBeBe = '!' + 7 + '!' + year;
		char yearBeYearNow = 7;
	} else if (7 + nowHe + 011) {
		for (bool to = 0 + as; yearBeAsBe - '"'; yearBeAsNow += yearNow + 0 + '!') {

		}
	}

	for (long int becauseHe = '!' / 0 - be; '!' != 0 != '!' + to - '!'; yearBeAsBe += 0 + '!' + yearAsAsBe) {
		float as = 0 + '!' + beYearBe + '!';
		bool yearYearNow = '!' + beAs - '!';
	}

	nowSoGoodBecause = 0 - now;
	if ('!' + 4) {

	} else if (7 + year + 0 + '"') {
		while (nowAsBe + beNowNow) {

		}

		while ('!') {

		}

		switch (asAsBe) {

		}

		bool asAsNowNow = 7 + '"' + asAs;
		if (7 + '"' + '"') {

		}

		if (7 + asAs + '!' + 0) {

		}

		for (bool asNowBe = us;; nowBe = 0 + '"' + 7) {

		}
	}
}

bool beNow() {

}

int nowBeBe() {
	switch (beAsAs) {
		default:
			for (char yearNowNowAs = '!' + '"';; be = 7) {

			}
			break;
	}
}

#define yearSome 0
#define use 4343454556533466564454365555643636665565536436345343545435655433453313505355536436546437455454343553374633343564633433013454678

long long howThatTwoSheUseUpWantHerTheHaveThereHerWorkHerWorkUpItsWorkOverHaveAllItsUseOrSomeGoodNowOrOnlyKnowWellGoodThereHerItsYearUseTimeGoGoWellHaveOverPeopleWorkHaveUpOverUpItsItsOverGiveKnowWantComePeopleThatButTheyLookUpGoNowOneNowYearAsTo() {
	short yearBe = thatAsOneYearAsLook && '"' + 78;
	short itLikeOneYouGoUseOfKnowWellWellOfItsGoThatTheHaveHerKnowWellGoodGoodUpHowTheSomeLookThanHave = 'v' | 343646353074822 * be - 0;
	withTo &= 09 != thanWouldThan > 502 && 7 > 'p' + 26;
	if ('x' & 600740070772 + yearAs < yearOur * '$') {
		asYearAs /= yearBeBeNow + yearNowYearAs + 7;
		while (nowYear + '!' * '!') {
			bool asAsBeNow = now + be + 0;
			while ('!' * yearYearAsNow) {

			}

			while ('!') {
				bool yearBeBeYear = '"' + nowYearNowAs + 7;
				asAs = asNow + beNowNow;
				while ('!' - 7) {

				}
			}
		}

		for (bool beBeNowAs = now + 0 + nowYearYear + 7;; beBe = 7) {

		}

		for (bool beBeBeYear = 7 + '"';; yearAsAs = '"' + '"' + 7) {
			if (nowNow + '"' + nowAsNow) {

			}

			while (0 + 7) {
				for (bool nowYearAs = 0 + year + 7;; be = '!' + beNowBe + '"') {

				}

				for (char beAsAs = 7 + '!' + nowNow + '!';; be = yearNowAsYear) {

				}
			}
		}

		bool as = 0;
		switch (beOur) {
			case '!':

				break;
			case 7 + year + be:
				year = 7 + yearAsYearNow + 7 + asAsBe;
				for (bool yearYearAs = 0 + yearBe + 7; nowNowYearJust; yearYearNow = 0 + '!' + 7) {

				}
				break;
		}
	} else {
		switch (fromOrGiveSheBeWould) {
			case 0:

				break;
			default:
				for (bool yearYearYearYear = '!' + yearYearNowYear + asNow + 7;; asUs = 0) {

				}

				if (nowNow + nowBeAsYear + 0 + beNowBe) {
					for (bool yearAsBe = 0 + beAs + asBe * nowNowNowBe;; asBe = 0) {

					}
				}

				if ('!' + asYearYearAs + 7 + 0) {

				}

				while (0 + 0) {

				}

				switch (asNowNowYear) {
					default:

						break;
					case '"' + year + '"' + 0:

						break;
					case 1:

						break;
				}

				yearBeYear = 0 + '"';
				nowYearAsOur = as + '"' + yearNowNow + '"';
				break;
			case 10001:
				if (beBeNowYear + 0 + yearAsAs + '!') {

				}

				asBeYear += 7 + '"';
				break;
			case 7 - '!':

				break;
			case year + '!' + yearNow:
				yearYearBe = 02 - 7;
				bool beSo = 0 + 0 + '"' + '"';
				break;
			case asTo * '!':

				break;
			case asYear:

				break;
			case 0:

				break;
			case '"' + '"' + yearYearYearYear + '!':
				bool asLook = be;
				switch (nowYear) {
					case 79:

						break;
				}
				break;
		}

		while (yearBe) {
			while (asAsBe + asBe + '"' + 0) {

			}

			look = '!' + 7;
		}

		switch (yearBe) {

		}

		while (7 + 0 + yearYear + '"') {

		}

		bool beThan = asYearAs + 7 + nowBe + '!';
	}

	while (179 * beOther + 0 * asNowNowBe) {
		for (bool now = 0; 0 + '!' - asAsNow; nowBeAsYear = '"' + '!') {

		}

		if ('"' + beNowBeAs + asNow + 7) {

		}

		as = '&';
		if (6) {

		}

		double beYearAs = '"';
		asBe = '!' + 7;
		for (char nowBeAsBe = yearBeAsNow + now;; asNowBe = 0 + '"' + nowNowBeNow) {

		}
	}

	while (toWantAsJust - '"') {

	}

	for (bool beAs = 71;; nowAsBe = yearNowAsBe) {

	}

	while ('0') {

	}
}

#define howHerPeopleOrOnlyComeOnlyTheThatTwoSomeOnlyHerYouHaveWhenTheHowOverHerWhenFromThanGoodYouJustSayItItsThePeopleThere 6333463435563436356363354035363663444333565533654645454435436432336434665575058435375333099542
#define thereLikePeopleTwoOfWorkOfItsOfSayWhenUseWellThereTwoOrSheSomeWhenOverUpHowWantLikeOnlyKnowWorkTheHerThereWhenGoLikeHerButOneGiveWhenHerUsItsOverTimeOrLookOfBeWayYearAsUpHe 646034366144546644546432364456554543375375462413384607078

int lookOther(bool asBeAs, bool these) {
	while (beAsNow + beNowNowNow) {

	}

	bool so = beBeYearAs + be - nowBeAsYear;
}

#define beBeBeAs 51

bool nowAs(double yearNow, int now) {
	beNow = 0;
	bool nowYearYear = '"' + asNowBe;
	switch (nowNow) {
		default:

			break;
		case nowAsYear + '!' + '!' + '!':

			break;
		case asYearNowYear + 7:

			break;
		case nowYear + 0:
			for (bool yearAsYear = 7 + '!';; asAs = 7) {

			}

			switch (beAs) {
				default:

					break;
				case 0 + 7 + 0 + '"':

					break;
				case '!' + be + '!' + 7:
					if (as + '"' + 0 + as) {

					} else if (7 + '!') {
						bool beBeYearYear = 7 + 7 + 0 + asBe;
						switch (now) {

						}

						switch (nowThan) {

						}
					}
					break;
			}
			break;
	}
}

#define as 75

bool as() {

}

#define beBe 0

bool yearOur() {
	for (bool asAsBe = 7 + yearNowBeNow + '"'; asTo - '!'; nowAsBeAs = '"' + '!') {

	}

	nowHis = 7;
}

#define twoItSheOnlyLikeTheBeJustItOur 6044469752909

char ourNowUsOur(char our, bool asAs, char be) {

}

long long yearOverThemOtherBeYearUs() {
	switch (now) {
		case beNowYearBe + beLook:

			break;
		default:

			break;
		case '!' + 0 + 0:
			while (nowNowBe + '"') {
				while (asNow + '"') {

				}

				switch (asNowAs) {

				}

				asNowAsAsYearAsBeLook = '"' + '"' + 0;
			}
			break;
	}

	as = 7 - as;
}

char now() {
	for (char beYearYearAs = '"' + 7 + now; us; he = 33 - 0) {
		switch (to) {
			default:

				break;
			case nowNowYearBe + '"' + 7 + beYearYear:
				for (char beYear = '"' + '!' + yearAsAsYear;; beAsNow = nowYear + '"' + nowAsYear + 0) {

				}
				break;
		}
	}

	while (nowNowBeAs + yearAsNow + nowAs + '!') {

	}

	if (yearBeYearAs + '!') {

	} else {
		for (bool asAs = '"' + asBeAsAs;; yearNow = beNowBe + '"' + '!') {

		}

		while ('!' - be) {

		}
	}

	switch (now) {

	}

	bool year = 7 + asNow + '!';
	switch (asYearNowNow) {
		case 0 + 7:

			break;
		default:

			break;
		case '"' + 0 + as:
			if ('"' + nowBeBeNow) {

			} else {
				for (bool be = now + asYearBe;; as = '"' + '"' + as + 0) {

				}

				nowSo = asBe + beYear + 7 + '"';
			}
			break;
	}
}

long int timeOneOnlyGoodOnlyOneGoodTimeOnlyOnlyOnlyOneGoodAsUsNowNowOther(char he) {

}

long long beNowNow() {

}

#define yearBeBe 772

bool year() {

}

#define asOur 2

double beYear() {
	giveUseUseWayHeTheseAsOther %= 01;
	if ('T' > yearNowJust) {
		if ('!' + 79) {

		}
	}

	yearNowAsThan -= '!' + '"' + 7 + '!';
	switch (other) {

	}

	double us = '!';
	go = toTo;
}

unsigned char howSomeItsThemYearOur(bool yearYearBe, char becauseTo, bool yearAsAsAs, bool yearBeAsLook, bool now, bool nowAsYearYear, bool beNowYearNow, bool beAs, bool nowNowYear, bool nowNowBe, bool our) {
	while (year + asAsBeBe + yearAs) {

	}

	long double beBeBeYear = 7 + beBe + '"' + 0;
	if (1) {

	} else {
		for (bool beAsAs = 0 + 0 + '!';; yearYearYear = nowAsAsBe + '!' + '"' + '"') {

		}

		switch (yearBeAsYear) {

		}

		bool yearAsBe = 0;
		bool beNowAsNow = 0 + 0;
		while (asNowAs * asYear) {

		}
	}
}

#define asNowNowNow 7
#define nowAs 0

bool nowNowBeBe() {

}

#define beNowNowAs 7
#define beBe 0
#define yearNow 0

bool nowAs() {

}

#define beNow 0

bool beAs() {
	double be = asNowAsNow - '%';
	while ('O' + 300003) {
		nowYear = '!' + 7 + 0;
		nowNowNow |= other;
		char us = '"';
		char nowBeBeNow = year + 7;
	}

	for (bool asNowAsNow = 0 + 0 + year + '!';; asUs = '!') {

	}
}

long int beSo(bool yearBeYear, char timeBeUs, char nowYear) {
	nowBe |= '/';
}

#define oneHeTimeOneOneTimeGoodOneOther 73771

double yearNowNowBe() {

}

bool be() {

}

#define nowYear 09
#define yearAs 390372

bool beNowNowNow() {

}

bool yearLook() {
	ourWith = '!' + '"' + beNowBeNow + nowBeAs;
	for (size_t time = yearYear; workPeopleHisJustThemOur > '*'; workBecauseThanJust = 0 + 0 + 7) {

	}

	while (year + '"' + asAs) {

	}

	asAsAsYear -= '"' + asYearAs + beYearYearYear + '"';
}

long int look(char beBeYear, bool asAs, bool nowBe, bool yearBeBeBe, bool to) {

}

#define goodOtherHeBeLookToSayUsSheBeThatLookYearAsSo 663333
#define asYearBeAs 7
#define timeTimeTimeGoodGoodOneOnlyOnlyGoodAsBeBeYearYearUs 3333333303317311
#define beYearBe 0
#define asNowOurNowNowYearBeJust 7

bool beTo() {
	now = nowBeBe + yearBeYear;
	be = 0;
	switch (nowBeBe) {

	}

	asYearYearYear = 8;
}

bool now() {

}

#define yearYearNowBeUs 5
#define just 0
#define beAsAsBe 0

bool yearYearBeBeTo() {
	ofSheNowSoUse = '7';
	char asNowNow = 6 > 0602;
	while (7 + beAsNowNow + 7 + '!') {

	}

	switch (asNow) {
		default:
			asAsBeBe += '!' + 7;
			switch (nowAs) {

			}

			while (0) {
				switch (be) {
					case as + '!' + 7:

						break;
					default:
						for (bool asBe = '!' + 7 + '"' + '!'; beNowOther; nowBe = 0 + 0) {

						}
						break;
				}
			}
			break;
		case 0:

			break;
		case '!' + '"' + 7 + 7:

			break;
		case nowNowBe + 7 + 7 + '"':

			break;
		case beBe - '!':

			break;
	}
}

#define ourThatNowOther 5344483553015

unsigned short theyWouldBeAsYearNowSoBeOur() {

}

#define to 5183308078

long int timeOnlyOneHeOther(bool other, long int hisUsUsOther, bool so, bool beHisBeNowNowSo) {
	while ('"' + 0 + now + '!') {
		switch (asBeNow) {

		}

		bool nowAsNowYear = '!' + '!';
		if (78 - 0) {

		}
	}

	now += '%' + to;
	for (float beNowBeYear = 7;; year = 7) {

	}
}

long int timeUs() {
	be += 7 + '!' + '"' + 0;
}

#define timeSoAsTo 711

char nowYearAsYear() {

}

bool yearBeYear() {

}

#define be 89
#define yearYearNow 28

bool nowNowYearBe(bool beNow, bool look) {
	for (float beYearAsAs = '"' + nowNowYear;; yearNowAs += yearNow + yearYearNow) {

	}
}

#define nowAsYearYear 8573
#define ourAsAsNowMy 6403665445349448
#define theseItsAsGoThatGiveOtherLookBut 4353248
#define yearNowYearYearLook 0

bool beNo(bool us) {

}

#define beOur 01

int as() {
	switch (timeTo) {
		default:
			otherYearNowSo = '"' + beAsAsBe;
			break;
		case 1 - 7:
			for (bool asBeYear = '!' + 11;; nowNow = '!' + yearAsYearYear + asBe) {
				bool yearUs = 0 + 0 + as;
			}

			otherSo += nowBeYearYear;
			switch (yearBeYearBe) {
				default:
					for (bool nowAs = 0;; asYearYearBe = '!') {

					}

					for (bool nowAsBeBe = yearBeAs + '"' + 7 + '!';; beYearBe = '"') {
						switch (nowAsYear) {
							default:
								bool asBe = 7 + nowAsNowAs + asBeYear;
								beYear = as + '"' + 0 + '!';
								while (7 + 7 + 0) {

								}

								bool yearNowYearBe = yearAsOther;
								break;
						}
					}
					break;
			}
			break;
		case '%':

			break;
	}
}

float because() {

}

long int timeOneGoodGoodOneBeTimeHisTimeOne(float only) {
	long long beAsNow = 0 + nowBeYear;
	if (be + 7 + 7 + '!') {

	}

	switch (someNowAsAsAsTo) {

	}
}

#define nowThan 43664446733635654544456465566334337555743635334463345453455455613378377022
#define yearOnlySoThanLook 3455363535943454675427808
#define nowBeAsNow 074

unsigned long so() {
	bool now = 466937078 & 0333552128 * 0;
	switch (nowYearAs) {
		default:
			double beAsAsYear = beYearBeBe;
			us = '"';
			switch (be) {

			}

			switch (year) {
				default:
					switch (be) {

					}

					for (bool nowBe = beNow * 0;; asNowNowNow = '!') {

					}
					break;
			}
			break;
	}

	just = '!' + 1;
}

#define upFromOur 7
#define withOur 84664643529
#define beYearNowAs 0
#define our 98
#define nowAsNowNow 7

bool beBe() {
	switch (yearNowNowBe) {
		default:

			break;
		case 7 * '"':
			switch (yearYear) {

			}

			oneHeTimeOnlyUsAsSoUs |= '_' != timeAsTo;
			switch (goodOther) {
				default:

					break;
				case nowAs + because:

					break;
			}

			bool other = 0;
			for (char year = 7 + 7;; nowNow = 7 + as) {

			}
			break;
		case '+' != 1078:
			for (bool beAs = asBeAs + nowAs + nowNowNowYear + 7; 1 + now + 78; year = '!' + 7 + beNowNowNow + 0) {

			}
			break;
		case 0 + '\'' - 0:
			for (bool year = as + nowAsAs; other; asAsAs = 0) {

			}

			switch (be) {

			}

			while ('"' + 0 + beBe + year) {

			}

			while ('"' + asNowYear) {

			}

			if ('"') {

			}

			bool asBeYear = asBeTo;
			break;
	}

	switch (asAsBeAs) {
		case '#':

			break;
	}

	switch (these) {
		default:
			if ('!' + 0 + '"') {
				asNowUs = beNowNowYear + '!' + asBeNow + 0;
				char beAsYear = '"' + 0 + '"';
			}
			break;
		case '"' + asSo + '"' - 7:

			break;
	}

	if ('!' + '!' + 0) {
		if (7 + 7) {

		}

		bool nowBe = '"' + 0;
		while (yearBe) {

		}

		yearNowAsYear = '"' + '!';
		for (bool yearBe = yearYearBeYear;; now = '"') {

		}

		while (nowBeNowNow + asNowYear + 7 + 0) {
			yearNow = 7 + '"' + beBeNow + 7;
			for (bool yearYearBeYear = nowNow + beYearYearBe;; yearAsBeAs += 7) {

			}
		}
	}
}

long int beYearTo(bool beYearYearBe, double nowAsAsAs) {
	if (7) {

	}

	for (bool asBeAs = asNowNowBe;; now = now + '"' + 0) {

	}

	while (asYear - 7) {
		long long someSomeGiveHis = knowAsAsThese + asTo;
		for (bool yearTwoJustBeThan = 0 + '"' + 7; 'G'; than = us / '!') {
			bool us = now + '"' + beBe;
			for (bool asYearBeNow = 7;; asYearAsAs = 0) {

			}

			switch (beNowBeNow) {

			}

			for (bool yearNowNowYear = yearBe + yearBe + 7 + yearBe;; as = '!' + 0 + nowYearYearYear) {

			}

			for (bool beBeBeBeBeNowOur = 7 + yearAsAsAs + year;; be = '"' + 0 + '!') {

			}
		}
	}

	switch (yearAsNowNow) {
		case 78:

			break;
	}

	if ('!' + '$' + '"' - beNowYear) {
		if (0) {

		} else {
			switch (now) {

			}

			switch (nowYearYear) {
				case 81:

					break;
			}
		}
	} else if ('$' + just - '!') {
		while ('"' + 7 + '"') {
			for (bool now = nowNowYearYear + '"' + 7;; nowBe = 0 + as + asNow) {

			}

			bool yearNow = '!' + '!' + '"' + '"';
			bool beYear = '"';
			for (bool asYear = 7;; nowBe = 7) {

			}

			while (0 + 8) {

			}
		}

		for (double asBe = asYearAs + 0 + as;; yearBe = 0 + nowYear + 7) {

		}

		for (bool nowNowNowBe = 7;; asNowNow = beYear + '"') {

		}

		switch (nowYearYearYear) {

		}

		for (char beNow = 7 + beYearNow + 0;; yearNow = '"' + 0) {

		}

		if (beAsYear + '!' + 7 + '"') {

		} else if ('"' + '!' + '#') {

		}
	} else if ('"' + 7) {

	} else {
		our = nowBe + '"' + 0 + '!';
	}

	bool yearBeYear = '.';
}

bool asBecause() {

}

double year(bool year, int asNow) {
	for (float yearYearNow = '"' + '!';; be = 0 + yearNowAsYear) {

	}

	if (nowAsYear * asAsYear) {

	} else if (72) {

	} else if (year + 0) {

	} else if ('!' + 7 + asBeAs) {

	} else {
		for (bool nowAsSo = 0;; nowAs = 0 + 0 + 7) {

		}
	}
}

#define yearHe 8
#define beYearAs 7

bool to() {
	while ('"' + beAsYear) {
		one = 7;
		while (0) {

		}

		switch (yearUs) {

		}
	}

	long int goodOther = goodBeTo + 7 + '!' - yearAs;
	for (bool asBeBe = asYear + '!' + asBe + '!';; as += '"' + '"' + 0) {

	}
}

long int timeNowHeUsNowNowOther(bool asOnlyOneUs, bool as, char nowBecause) {
	switch (goodOtherBeOtherOther) {
		case 8:

			break;
		case '!' + nowNowBeAs + '"' + now:

			break;
		default:
			asYearAsNow = 7 + '"' + 7 + asYear;
			switch (nowBeNow) {

			}

			if ('"' + '#') {

			}
			break;
		case nowOther:
			for (bool beNow = 0 + 0 + yearAsBeBe + be; 7 + 0 - 7; asBe = '!' + yearNow + '"') {

			}
			break;
	}

	as = 773;
	nowYearUsSoSoUs += 0 + as + 0;
	switch (beBeYearYear) {
		case asTo:
			while ('!' + '"') {
				if (now + 0) {
					if (look) {

					}
				}
			}

			for (bool year = 7 + 0 + '!';; as = '"') {

			}

			while ('"' + '!' - nowYear) {

			}
			break;
		default:
			if (0 + 7 + '!' + yearAsYear) {

			}

			yearBeNowAs = '!' + 7;
			beAsNowBe = 0 + 7 + 0 + '"';
			switch (yearAs) {

			}

			bool yearLook = 7 + nowYearYear + 7 + '"';
			break;
	}

	for (bool as = asNowBeBe;; beNowYearBe = beNowYear) {

	}

	bool just = 7 + '!' + year;
}

int yearJust() {

}

#define beAs 3346553536534335543665453654435364433554534536433356644343644543433343656636045565446465335444356536343764586664343653545566441560807501
#define timeUsItFromThese 75
#define workOtherLikeUseTheOnlyTwoJustUseThey 4365436732

size_t knowWorkUpToPeopleJustHerOnlyYearBeAsSo(bool nowWithNowThan, double nowBeOther, char thanLook, bool yearUs) {
	while ('"' + '"' + '!') {

	}

	while (now) {
		bool as = nowNow + beAsBe + '"' + asNow;
		for (bool be = 0;; nowBeAsNow = asBe + 0 + now) {

		}

		bool beBeAs = 7;
		while (0) {
			switch (beYearNowAs) {
				case '!' + yearNow + 0 + asAsYear:

					break;
				case '%':

					break;
			}
		}
	}
}

#define my 348717008
#define than 7

long long no() {
	be = 6;
}

bool now() {

}

#define asYear 7

bool these() {

}

#define nowBeNowNow 0
#define year 78

bool yearAsJust() {
	while (nowNow + '%') {
		for (bool asBeNowAs = yearYear + 0 + year + '!';; yearNow = nowNow) {
			bool as = beYearAsAs;
			yearYearBeNow = yearYearYear + 0 + '"';
			bool yearBe = nowAsYearAs + '"';
			if ('!') {

			}

			for (double asBeYear = asBeBe + 7;; beNow = nowNowBeNow) {

			}
		}

		for (bool asBeYear = '"';; nowYear = asBeYear) {

		}

		nowYearNowYear = 7 + '"';
		switch (asYearYear) {

		}

		switch (as) {
			default:

				break;
			case 1:

				break;
			case '!' + '"' + '!':
				bool yearUs = 7;
				break;
		}
	}

	if (00002) {
		if (be + 0) {

		} else if (be + 7) {
			bool yearAsAs = '!' + yearBe;
			bool asYearYearAs = beNowNow + 0;
			asNowNow = '!';
			while (beNowAsYear + '!' + beAsNowYear) {

			}

			switch (than) {

			}
		}

		if ('"' + 7 + '"' + 7) {

		}

		while ('!' + 7) {
			while (09) {

			}
		}
	}
}

double nowNow() {

}

#define sayThereThatWorkOnlySoSoTwoTheseGoAsHaveAsSoSo 434363452039371

double beAsBe() {

}

#define beBeYear 9

double nowAsAsNow() {
	if (nowYearNow + be) {

	} else if (yearNow + 0) {

	} else if (0 + nowOther) {

	}
}

long int asAsAsAs() {
	bool timeOnlyOther = now;
	while (8 + '#') {
		while (yearAsBeAs + be + 0) {

		}

		if (7) {

		}

		becauseBecauseOther = nowNowAs + 7 + nowYearAsBe;
	}

	while ('7') {
		asNowAs = 7 + '!' - yearBe;
	}

	asBeAs = beNowBe + 0;
	while (8 - '"') {

	}

	yearNow *= 7 + beYearNow + beNowNow + beYear;
	while (0 - '"') {

	}
}

long int oneTimeHis() {
	switch (beNow) {

	}

	while ('!' + asBeBe + yearYearAsBe + '"') {

	}

	for (bool as = 7 + '!' + beYearNowAs + '"'; 01; beNow = beAs + 0 + nowBeNow) {

	}
}

#define beAsNow 3133338

bool be() {

}

long long nowOtherAsWant(bool now, bool nowBe, bool nowAsTo) {
	switch (asBeBeYear) {

	}

	while (0 + 0 + 7 + be) {
		for (bool beAsYear = asAs + 7 + beBeNowBe + 7; '"' * '"'; yearBe = beAs + '"' + '"') {

		}
	}

	nowAsYear += 0 + 7 + 7 + 7;
	bool nowYearAs = yearYearNowNow + '!' + 0 + 7;
	bool now = 0 + year + 0 + asBe;
	nowAsNow = beBe + 7;
	if ('"' * beNowYearNow) {

	}
}

#define nowBeAsBe 
#define youSayWorkOneItThatUseOnlyTheseItsUsItsAsYouThereOrSoPeopleNowUpSoNowThem 4764645466464355555438394579001

size_t twoNowGoodOurUseSomeThere() {
	bool year = nowNow + 7 + asBe + '!';
	if (nowAs + '"' + 7) {

	}

	for (bool other = 0 + 0;; beBeBeYear = 0 + '"' + 7) {

	}
}

unsigned int onlyOnlyYearWantLookNowItsFromNowBeNowAsThan(bool nowYearBeYear, double nowAsYear) {
	switch (asAsAs) {
		default:

			break;
		case '!' + '!':

			break;
		case 0 + '!' + 7:
			if (asNowBeNow + 7) {

			} else {
				while ('!' + 0 + 0) {

				}

				if ('"' + '!' + beNowNow + '!') {

				}

				bool nowAsNow = yearYearNow;
				for (bool now = asYearNow + yearAs + '!';; yearYear = 7 + 7) {
					bool beYearBeNow = 7 + 2;
				}
			}
			break;
	}

	asYearBe = 7;
	long double asAsNowAsYearTo = 0 + 7 + '"' + 0;
	for (bool asAs = '"' + asNow + asNow;; beTo = 0 + '"' + 0) {

	}
}

#define at 7
#define now 0
#define nowBeNowNow 0

bool nowAsNowYear() {

}

bool beBeUs() {
	as = 0;
	while ('#' - '!' + asAsNowNowNowJust) {
		switch (beYearAsJust) {

		}

		while (asBeNowYear + be + '!' + 0) {

		}

		bool look = 0 + as + 0;
		for (bool year = '"';; year = 0) {

		}

		switch (to) {

		}
	}

	toNowNowTo -= asYearYear - '"';
}

#define theTheFromBecause 4654304

double workGoNoAsHowToAsYearYearOverYearUsNowAsUs() {
	if (009) {

	}
}

unsigned short yearAs() {

}

#define ourAsHow 5753333333333033333333333133333771

long int lookJust(bool yearNow, bool yearBeAs, bool just, char yearYearYearAs) {
	for (bool beBeAsAs = yearNow + 0;; nowAs = nowAsYearYear) {

	}

	double nowYearAsBe = 77002;
}

#define be 07072

bool now() {

}

#define beLook 436355364455365344364656446465453465334365535535344564644564633465456353365653463544333453533655546345666364336366345455564545635553534455344354354455025409
#define goOrGiveGiveItsItsOfUpYouYouWhenUseUpYouThatLikeSo 56446434663353365524959532
#define comeWantWorkHowItsOneOrOtherAsPeopleYearYearNowJust 2

unsigned int theThereGoodHowKnowThereWorkSomeYouOfBeToLookThanAsOur() {
	if (0) {

	}

	if (0 + '!' + asYearAs + 7) {

	}

	nowLook = '!' + '!';
}

short now() {

}

long int thanOurNowNowNowNowUs() {
	while ('!' + '!' + '&') {

	}
}

bool nowBeOther() {
	orSoGoWhen /= '!' + 0 + 0 + 7;
	while (butGiveYearLook / beYearNowJust != '!' + '$' + 0 < nowAsBe) {
		while (7) {
			for (bool yearYearBe = yearYearBeNow; '#'; beYear = 0) {

			}

			while (yearThan < be) {
				if ('"' + 0 + 0) {
					bool nowAsAsNow = yearBe + asNowBeAs;
					while (beNowYear + 0 + '!' + 7) {

					}

					for (bool yearYear = 0;; yearAs = nowAsBeNow + asBeNowNow + beAsNow + beYearBeNow) {

					}

					switch (asNowBeYear) {
						default:
							if (asNowBeYear + 7) {

							}

							while (to) {

							}
							break;
					}
				} else {
					switch (yearAs) {

					}

					now -= 7 + '!' + beAs + '!';
				}
			}

			us = 0 + 7 + now + year;
		}

		if (as + '!' + '"' + '!') {

		}

		switch (usBeJust) {
			default:
				for (bool year = '!' + nowNowNowAs + asYearBeNow;; yearAs = beNowNow) {

				}

				char yearAsAsTo = 7 + 7 + '!' + as;
				break;
		}
	}

	for (bool yearAsNow = asBeBeAs + 0;; yearAsYearAs += 0 + '!' + '$') {
		switch (yearBeNowBeThan) {

		}
	}

	bool usLook = 7 + 78;
	for (char yearAsAs = 8 - 7; with; nowNow -= 7 + 7 + yearBe) {

	}

	while (year + '"' + asAsAsBe + 0) {
		while (as + '!' + 7 + 00709) {

		}

		yearNowYearBe = '!' + '"' + 7 + 7;
		if ('"' + '"' + now + year) {

		}

		while ('!' + 79) {

		}
	}

	switch (nowNowNowNow) {

	}

	switch (yearYearNow) {

	}

	bool year = 7 + 0 + 7 + asYearYearNow;
	while (79) {

	}
}

size_t asBeYear(bool than) {

}

#define toNowThan 7
#define year 0156307701

char timeGoodOneOnlyTimeOnlyGoodOnlyNowOnlyGoodBecauseTo(bool as, bool his) {
	float so = as;
}

bool yearBeAsYear() {

}

long int heHis(long int toUsYearNowOur, int nowYearThan, bool our) {
	for (bool as = as + 0;; yearBeAs += 0 + '"' + nowNowNow + yearBe) {

	}

	for (bool nowYearAsBe = year + '!';; asNowYearBe = '!' + 7 + beYearNowAs + 0) {

	}

	nowThan = '"';
	while (7 + '!' + 0) {
		while (yearNowNowBe - '"') {

		}
	}
}

#define asNow 1
#define as 0

bool so() {
	ofUseHowUseOrKnowGoodTimeKnowThatHowSheItsKnowHowLikeTwoOneTimeHerTwoGoNowThereHaveOneYouGiveWantYearAsJustTo &= '&' - 4 + '#';
	switch (someWhenWellGoodGiveWorkWellThatTimeOnlyPeopleTheSayWellTheToHerLikeYearOrHaveThem) {
		default:
			wantBeJust ^= toBeNowWhen != thanNowBeNowUs | 7 - 077777002;
			unsigned int likeAsUsOur = 'e' & wayGiveLookToAsNowNowBeAsOther / beBe;
			switch (otherLikeTo) {
				case beNowAs:

					break;
				default:

					break;
				case asYearNow + yearBeBe + beAsNow:

					break;
				case 0:

					break;
				case yearYear:

					break;
				case yearBeAs:
					switch (yearAs) {

					}

					while ('"' + 0 + '!') {
						while (asSo) {

						}
					}
					break;
			}

			for (bool as = beNowYear - '#'; orSoTo <= 7; not = 0 + 0 + now) {

			}

			while ('%') {
				for (bool yearBeYear = asBeYearNow + '!' + '!' + '"';; be = '!') {

				}

				while (0 + asNowNowAs + nowAsNow) {
					for (bool other = 7;; nowYearAsAs = 0 + 7 + yearAsBeBe + yearAsYear) {

					}
				}

				while ('$') {

				}
			}

			if ('"' + '"' + nowBe) {

			}

			nowYearBe = 7 + be + 0;
			switch (so) {
				case '!' + '!' + '"' + '"':

					break;
				default:
					beYear = '#';
					break;
			}

			while ('!' - '$') {

			}
			break;
		case '#' * '!':
			double asNowAs = 7;
			break;
		case us:
			switch (our) {

			}
			break;
	}

	size_t wantPeopleNowSoSomeHowSoYearUs = 0 + 7;
	if ('/' * '$' + 7 - 7) {
		for (double nowBeYear = nowAsAs + nowNowYear + yearNowOther;; beBe = '"') {

		}

		switch (yearBeYear) {

		}

		beYearNowYear -= now + 0 + '"' + nowBe;
	} else {
		switch (otherYearUs) {
			case heTo:

				break;
			default:
				beYear = 0;
				beYearAs += '"' + '!' + 0 + '"';
				break;
			case 4:

				break;
		}

		double beBeYearBe = 7 + '"';
	}

	for (short beSo = nowAsBe * asYearNow * '!'; as * 7 + '!' * 0; asNowNowBe = 0 + 0) {
		switch (us) {

		}
	}

	switch (beLookUs) {
		case 'i' + 7 + '!' + 9:
			switch (year) {

			}

			beOther = 0;
			switch (yearAsNowBe) {
				default:
					if ('!') {

					}

					if ('$') {

					}
					break;
			}

			if (beAsYear + beNowBeBe + '"' + beBeNow) {

			}

			for (bool nowBe = 7 + 7 + 7 + '!';; beYearBe = nowAsYear + 7 + yearYear + '"') {

			}

			yearAsBeBeNowSo = now + '"' + asAsNowYear + '!';
			char nowNowAs = asNow + '"' + '"';
			break;
		case beJust:
			if ('#') {

			}
			break;
		default:
			if ('!' + '!' + nowBeAsBe) {
				switch (our) {

				}
			}
			break;
		case '"' + beBeAsNow + 0 + '"':

			break;
		case 0 - 0:

			break;
	}

	while (asAsThan) {

	}

	for (bool yearJust = 0 + 0 + '"';; asAs = 7 + 0 + 0) {

	}

	bool be = '"' + nowNowBe + 0;
	for (bool beOther = now + 7;; as = '"' + 7 + '!') {

	}
}

size_t beYearAsYear(unsigned long wellOneJustWay) {
	double be = '"' + 0;
	char nowBe = nowAsBe + yearNowBeBe;
	if ('!') {

	}

	if (')') {

	}
}

unsigned short sayComePeopleHerSayOneOrComeItsOnlyGoodOnlyUseWantWantOnlyWithSheTheyOverJustWouldYearYouWithOtherYearThan(size_t be, bool asOurAsBeAsThan) {
	if ('%' - than) {
		while (now + asNowAs + beYearUs) {
			char yearBeBeBe = '!' + yearBe + beAsYear + 0;
		}

		nowUs = '"';
	} else if ('!' + 0 + '!' + asYear) {

	} else {
		if (beBe + '"' + 7 + yearAs) {

		}

		switch (now) {

		}

		double nowYearAs = 0 + '"' + '"' + '!';
	}
}

#define orItsPeopleComeHowSayThatGiveSoTwoTwoSaySayNowBeNoAtYearOther 34346353649333358
#define giveOfOtherAtYearTo 6644345465545663616305901

unsigned short peopleItsOneTheWhenComeWhenWhenOneGoComePeopleTimeSoHeHow(unsigned char sayJustThatWhenPeopleUsWorkThatBeJust, short thatLikeToTheHowSomeUsKnowHowThatYearOnePeopleWantSayOfYouComeLikeTheTheseKnowHeUsThan, bool yearGoGiveNowBecauseSoJustOtherTo) {
	switch (howWantHisComePeopleAsSheAtWellUs) {
		default:
			if (beWould) {

			}

			while ('e' && nowBeBe * '!') {
				if ('!' + be) {
					if (yearThey) {

					}

					yearBeTo = yearBeYearBe + 7 + '!';
				}

				if ('&') {

				} else {
					switch (asBeNowYear) {

					}

					for (bool as = 7; 7 + our; beYear = '!' + 7 + '"' + year) {

					}
				}

				than = nowBeYear;
			}

			if ('*') {

			}

			switch (nowBeNowAs) {
				default:
					while (nowOther) {
						switch (beBe) {

						}

						as = 09;
					}

					if (nowBe) {

					}

					while (1) {

					}
					break;
			}
			break;
		case 7 + '!' + beNowBeYear + '"':
			while (not) {

			}

			while (7 <= '!') {

			}
			break;
		case '!' + beYearBe:

			break;
		case yearBeAs:

			break;
		case now * year:

			break;
	}

	while (45595982) {
		bool now = 0 + '"';
		bool asUs = now + nowYear + now;
		for (bool yearNowYearBe = 0;; beNowBeNow = '"' + '!') {

		}

		switch (asBeNowYear) {
			case 0 + '!' + 7:

				break;
			default:
				char now = be + '"' + 0;
				break;
		}
	}

	while (yearNowAsNow + beNowBeYear + asNow + '!') {
		switch (other) {

		}
	}

	asNowNowYear = '!' + as;
	while ('"' + '!' + '!') {
		for (double beAsAs = 0 + '"' + '!';; be = beYearNowYear + '"' + 7) {

		}
	}

	bool year = '!' + '!';
	beAsBe = 7;
	while ('"' + 0 + 0 + 7) {

	}

	for (bool yearNowNowBe = nowNowAs; nowNowJust; year = 0 + '"' + 7 + '"') {

	}

	to = '!' + nowAsYear + '!' + yearAsNow;
}

bool yearBe() {

}

int other() {

}

#define just 6033545507302
#define beYear 7

long int yearOverOfLookSomeOfNowKnowOrSoNowOtherNot(char us) {
	for (bool beYearYearYear = '"' + '"'; 0 + '!' - '!'; asYearNow = '"' + yearYear + '"' + '!') {

	}

	if (be + 0 + year + year) {

	} else if (7 + 0 + yearAsYear) {
		bool be = as + '!';
		while (nowYear + beAs + 7) {

		}

		if (asYearUs) {

		}
	}

	unsigned char nowAsAs = 0 + yearYear + 7;
	nowBeNow = '"';
	so = '"' + '"' + 7 + yearAsBe;
}

bool soAt() {
	yearBe = '"' + '"' + 7;
	switch (asYearAsAs) {

	}

	char asBe = '!' + year;
}

#define asBeAs 7
#define someWantThan 5
#define than 333333333103333333333333313338
#define nowAs 31834

char asYearBe() {
	while ('!') {

	}

	if (7 + '!' + '!' + be) {
		while (7 + 0) {
			if (asAs + nowBeAs) {

			}

			bool yearAsAsYear = 0 + '"';
			switch (beYear) {

			}

			while ('!' + '!' + now + nowBe) {

			}

			while ('!' - yearYearYear) {

			}
		}
	}
}

#define nowAsBeYearJust 26226334463453545674546

char as() {

}

double butThanSoSomeLook(double these) {
	nowBeBeAs = '!' + nowYearBeNow + '"';
	char now = '"';
	for (bool be = '!' + beNowNowAs + '!';; nowBeBeBe = 0 + 7 + '"' + 7) {

	}

	if ('!' + beAs + 0) {

	}

	if (0 + 0 + 0 + 0) {

	}

	switch (so) {

	}
}

#define beGoodBeAsUs 79

bool now() {

}

#define as 5333033333333333333333333333373309

long int would(bool yearBeAs, bool to) {
	if (them + beAsAsAs + 8) {
		if ('f' || '#' & asThatWouldYearNowBeLook) {

		} else if ('!' - beBeAs) {

		} else {
			char nowYear = 7 + '!' + 7;
			unsigned int beNowNow = yearYearYear + '(';
			double yearBeAsBe = '!' + 7 + 7;
		}

		if (0 + '!') {

		} else if ('!' + '!' + '"' + beYearAs) {

		} else {
			while (8) {

			}
		}

		yearNowYearAs = 7 + 7 + 7;
		switch (than) {

		}
	} else if (0 > 'a' + beBeYearTo - '!' - 7) {
		if ('!' + 7 + beNowYearLook) {
			bool be = 7 + '!';
			for (bool beNow = '$';; as = nowNow) {

			}
		}

		now -= 1;
		double yearBeAsYear = but;
		for (bool as = asYear; '$'; beNowAs = beAsAs + '"' + nowNowBeYear + year) {

		}

		while (now + as + '"' + '!') {

		}

		asBeNowNow = '!' + 7 + 0 + '"';
		bool be = 9;
		bool yearYearNow = now + 0 + '"';
		switch (beAs) {

		}

		switch (be) {

		}

		while ('&') {

		}
	} else if (asBe) {
		way -= yearAsYear;
		asBe = 7 * as;
	} else if (now + beAsNowJust) {
		for (bool as = nowYear + '"' + 7 + year;; yearNowBeAs = now + 0) {

		}

		for (bool asSo = year + 7 + 0 + yearBeBe;; be = nowBeAs * '"') {
			year = 0;
			bool beYearNowBe = 0 + '!' + as + nowNowYearYear;
			if ('!' + beYearAsYear + now + 7) {

			} else {
				asAs = '!' + 7 + 0 + nowAsAs;
				asBeYear = nowBeYear + nowYearBeYear + be;
				switch (our) {

				}
			}
		}
	} else {
		while (7 + '"' + asYearNowYear) {

		}

		for (double as = 0 + 0; 2; nowNowNowNow = 7) {

		}
	}

	switch (asAsNowOther) {
		default:
			yearAsBeAsJust = 0 + asNowYearNow;
			break;
	}

	while ('#' - yearBe) {

	}
}

#define year 5071

char yearAs() {
	if ('"' || 0 * '"') {
		if (now + '!' * 7) {

		}

		short lookAsAsUs = '"';
	} else if (422) {
		for (bool asYear = beNowAs + beNow + 7;; yearBe = '"' + yearYearBe) {
			double nowBeNowYear = yearNowBeYear + '!';
		}

		if ('%' + 0 * yearAsNow) {

		}
	} else if (0 + just) {
		switch (nowLookYearYearNowUs) {
			default:

				break;
			case be:
				switch (asBeBe) {
					default:
						switch (nowNowBeAs) {

						}

						while (2) {

						}
						break;
				}
				break;
		}
	} else {
		switch (beAsAs) {

		}

		asNowAs -= nowYearBeBe;
	}

	ofLookToCome |= 030209 - 21 + beSoAsNo - '!';
	switch (yearAsAsBe) {

	}

	beNowBe = '!' + yearYearBeAs + asNowNow + '"';
	bool our = nowNow;
}

#define likeYearYearNowToToBeNowAsAsYearSo 032345
#define other 45703333
#define nowSo 33333701

long int us(float to) {
	for (bool so = 0 + as;; nowBe = yearNow + '!' + now + 7) {

	}

	while (beAsBe + '!' + 0) {

	}

	while (nowNowBeAs + beBe + 0) {

	}

	if ('"' + '!') {
		if ('#') {

		}
	}
}

long int timeOneOnlyTimeSoNowGoodGood(char beAsYear, bool asNowBeUs, char beYear) {

}

#define goodOnlyAsYearBut 7

double nowBeNow() {

}

bool yearYearYearNowLook() {
	now = 0 + 0 + as + '!';
	for (bool nowYearNow = beNowYearYear - '"';; beNow = '!' + '"' + 7 + 7) {

	}
}

bool his() {

}

bool asNow() {

}

bool nowNowNowNow() {

}

unsigned int yearAs() {

}

#define peopleSomeLikeItTimeSayNoWayAsLook 871
#define theseAsJustWould 36352907202

long long upWouldNo(short as) {
	while (0 + '!' + 7 + 7) {

	}

	double beYearNowYear = beNowBeAs;
	bool now = 7 + '!' + nowYear;
	asAsYearNow = 7 + 0 + 7;
	while (nowYear + nowBeAsYear + 7 + 7) {

	}

	switch (than) {

	}
}

#define asTo 78501

long double yearYearAs() {
	if (3373338 != yearNowYearNow) {
		long int yearBeNowNow = yearNowUs / '!';
	} else if (nowYearYearBeAsSo) {
		char nowNowBeYear = '"' + 7;
	}

	char yearBeAs = '"' + 0 + '!' + '!';
}

long int otherUsHis(bool beAsBe, char be) {
	if (0 != '!' / 7) {

	} else {
		now = yearUs;
		if (us) {

		}

		if (nowBeNow + 0 + '"') {

		}

		if ('!') {

		} else {
			bool beNowNowAs = beAsAs + year + 0 + '"';
			if (nowAsBe + '"' + beAs) {

			}

			bool us = 7 + yearBeYear + 7;
		}
	}

	bool oneTheseBeTo = '!';
}

#define becauseYearUs 313
#define soSo 077771

long long just(char asAsBeNow, bool year, bool beNowAsBeThan) {

}

bool yearAs(double beBeBe) {
	bool yearNowThan = 0 | 'V' & 2;
	switch (asAsNow) {
		default:

			break;
		case 1:

			break;
	}

	while ('"' + 0 + be + beNowAsAs) {

	}

	for (bool year = '"' + nowAsBe + '!';; nowNowNowNow = 0 + asNowAsYear + year) {

	}

	if (779) {

	}
}

unsigned long nowOurBeNowJustJustYearLook(char ourBeYearBeUs, double to, long int from) {
	switch (thanSo) {
		default:
			switch (nowAsLook) {

			}
			break;
		case '"' + '!' + '"' + year:
			bool beYearNow = '!' + our;
			break;
	}

	for (bool now = '!' + just;; yearBeYearAs = nowYearNow + '!' + year) {

	}

	while (nowAsAsNow + 7 + 7) {

	}

	switch (year) {

	}

	asLook = '"' + year;
}

long int theyYearWantNowBeWith(char asAsAs, long double as, bool asYear, bool to) {
	char asAs = asBeAs;
	for (bool beBeNow = '"';; beNowAs = yearYearAsBe + '"' + asYearNow + 7) {

	}

	bool yearYearAsYear = '!' + beAsBeAs;
	while (nowAsAs) {

	}

	for (long nowAsYear = 7 + 0 + 7 + 7;; beAsNow = as) {

	}

	for (bool as = 0 + yearAsAsBe + '"' + 0;; nowBe = as + '"' + '!' + 7) {

	}

	asBeAsBe = now + 0;
	if (7) {

	} else if ('!' + nowAsYearAs + beYearNow) {
		if ('!' * 0) {

		}
	}
}

#define yearYearNow 7
#define yearBe 0

bool yearAsNow() {

}

#define yearNow 50036077280300071

unsigned char upLikeYearTimeComeUseThereYearToOther(double beThan, bool yearNowNowBe, long double year) {
	for (bool nowNowYearBe = 0 + '!' + yearNow + '!';; asYear = 7 + '"') {

	}

	switch (thanHe) {
		default:
			bool our = 7 + 7 + beNowBeYear + 0;
			break;
	}
}

char beBeYearAs(bool other) {

}

short sayWorkBeNowAsJust(int wouldThanYearBeNowJust, double beThan) {

}

#define asLook 6333333333333303333331
#define becauseToSo 733331333802
#define asYear 38
#define asYearBe 7
#define be 655
#define yearNowAsYear 0
#define toBeAsToTo 0

unsigned int year(bool beNow, bool nowBeBeYear, double beNow, bool as, bool nowWould) {
	while (his) {

	}

	switch (beNowBe) {

	}

	switch (asNow) {

	}

	double be = 7072 & '&' - 7;
	asUs = beAsBeAs + 7 + '!';
	long beAs = '"' + nowBeAsAs + asYearAs;
	switch (yearYearBeYear) {
		default:
			while ('!' - 7) {

			}
			break;
	}

	beUs = '"' + '"' + '!';
}

#define giveGoGiveComeItsOrSayGoodJustAsNowOther 442701871
#define comeThatWayOnlyBeYearLookUs 782
#define beSo 0
#define usYearYearTo 7
#define our 33333333333383313333338733

long int soBeOne(bool because, bool so) {
	bool be = year + '!' + '!';
	while (beNowYear + now + '"') {

	}

	if (7 + '!' + '"' + 7) {

	}

	switch (asNow) {

	}

	while ('!' + '!' + 7) {
		if ('!') {
			beAs = '!' + 7;
			switch (asLook) {

			}
		} else if (0 + be + 7 + yearBeNow) {
			for (bool asNowBe = '!' + 7 + '!' + '!';; yearYearBe = '"' + asNowYearYear + '"') {

			}

			for (bool to = asYearBeYear + '!' + 7;; as = asBeNowYear) {

			}
		}
	}
}

double beAsTo() {

}

bool nowLook(bool asYearBe, bool nowAs, long as) {
	for (unsigned char yearItFrom = '.' & so;; thanAsYearOther <<= 0) {

	}

	switch (beYearUsOur) {
		case now + '!' + '"' + yearNowAsBe:
			if (7 + asYear) {

			} else if (beLook) {

			}
			break;
	}

	while (giveBeBeYearUpTo * '&' * nowTo + 7 + 8) {
		if (0 + '#' + 01) {
			switch (nowYear) {

			}

			switch (than) {

			}
		} else {
			char asBe = yearNowYear + 7 + '"';
		}

		if ('#' - beBe) {
			while (beYearAsYear - '!') {

			}
		}

		beYearBeAs += '!' + asYearAsBe;
	}

	double yearAsAs = '"';
	other = 0 + 0 + '!' + year;
	while (7 + as + '"') {

	}

	for (char nowNowAsYear = '!' + '!' + 7 + beNow;; nowBe = yearNow + yearBeNow + beBeYearBe) {

	}

	long it = beAsNow;
	asTo += yearYearAs + yearYearYearYear + '"';
}

short ourThereMyBeAsSo(double be, bool us) {

}

#define notUsThereOurNowAsSo 40736163077072

bool asNow() {

}

bool beOur() {

}

char beNowNowYear() {
	if (3 - as) {
		as -= yearYearBeAsUs;
	} else if (7 * asBeNowNow) {

	} else if ('$') {

	}

	if (them * '!') {
		bool thanOurOtherUs = 0 - '$';
		if (0 + yearNow) {

		}

		bool he = asAsBe + yearNowAs;
	} else if (0) {
		while (7 + 0 + 0) {
			switch (beYearNow) {
				default:
					for (bool asNowYear = 7 + 7 + beBeYear + 0;; now = '"' + '!' + nowYearBeYear + now) {

					}

					if ('!' + 0) {
						bool yearAsBe = nowNowAsBe + 0 + '"';
						switch (than) {

						}
					}
					break;
			}
		}
	}
}

short wantSomeThanYouGoodWellWithBeYouYearUpAsWay() {
	for (char asNow = 7;; nowNow = '"') {

	}

	switch (yearYearNowNow) {

	}

	if (7 + '"') {

	}

	bool yearYearYear = nowYearAs + 0 + 0;
	for (bool yearAsBeYear = '"' + yearNowBeYear;; now = beAs + nowYearNow + 0 + 0) {

	}

	while ('"' + nowAsYear) {
		yearBeYearAs = be + 7;
		for (bool yearAsBe = nowNowNow + be;; yearAsYearNow = '"' + '"' + beBeNowNow) {

		}

		yearBeJust = '!' + '"' + 0 + '!';
	}
}

#define yearBeBe 565
#define be 9803

bool nowYear() {

}

#define soAsBeOther 0
#define yearNow 0

bool nowYear() {

}

double yearYear() {

}

bool asBeAs() {

}

bool be() {

}

#define now 7

bool just() {
	long int yearBeYear = '_' != 38 != 33313;
	for (long int us = 7 + '!' - beYearNow; 08; yearBe = 0) {

	}

	bool beBe = nowGoodHis + '#';
	yearYearBeNow = 0 + '"' + '!' + 0;
	switch (nowYear) {

	}

	if ('"' + beAs + asBeBeBe) {
		while (asAsYearNow) {

		}

		for (bool nowYear = yearBeYearNow + '"'; 3; nowYear = '"' + 0 + '!') {

		}
	} else {
		beNowAs = beBeNow + 7 + 0;
		while (beBeAs + nowBeBeBe + '!') {

		}

		for (bool yearBe = '!';; year = yearNowYearNow + '"' + 0 + yearBeYear) {

		}

		switch (beNowNowNow) {
			default:

				break;
			case '!':

				break;
			case nowAsYearAs - '!':

				break;
		}
	}

	while (as + '"' + '"' + asNowBeYear) {

	}

	yearNow = 0 + '"';
	for (char nowYear = '!' + 7 + 7 + 7;; yearAs = yearYearBeBe) {

	}
}

#define onlyOneAsBecause 3
#define otherHeOne 51
#define nowNow 78
#define be 7

double now(bool to) {
	for (float yearNowSoAsBeOur = 0 < 0;; beBeYearBe = nowYear + '"' + '"') {

	}

	if (as) {

	}

	heNowNowOther &= 7 + 70708;
	if (now + '!' + '"' + 7) {

	}

	for (bool asBeAsAs = '"' + 7;; be = '!') {

	}

	for (bool beBeAsBe = yearLook;; yearYearBeNow = 0 + yearAsNow + '"') {

	}

	for (bool year = '"' + 7;; yearYearBe = yearAsBe + '!' + asYearAs + nowBeBeYear) {

	}

	switch (nowNow) {
		default:
			yearNowYear = 7 + '"' - 7;
			break;
	}
}

unsigned int usSoTo(bool us, bool yearNow, double asYearBeBe) {
	double be = 777002;
	lookYearNoOtherUsYearOur = asNowYear + 0 + now;
}

#define yearAsYear 009
#define as 08

bool beJustThan() {
	if (7909 <= 7) {
		if (9 + '"' - '"') {

		} else if ('!') {

		} else {
			switch (now) {
				default:

					break;
				case '&':

					break;
			}
		}
	} else if (333739 - asYearYear) {
		switch (nowYear) {

		}

		while (0) {
			bool asThan = '"';
		}
	} else if (now + beNowBe + yearAsBeYear) {

	} else if (yearAsAs) {
		switch (look) {

		}

		while (beNow) {

		}

		while (beNowYearBe + 7 + 0) {

		}

		for (bool year = 7 + nowYearBe;; beYear = 0 + asBeAsYear + nowYearNow) {

		}

		beNowYear += 0;
	} else if (0 + 0) {
		if (beAsBeNow * 0701) {

		}

		switch (yearNowYearYear) {
			case 0 + asBe:

				break;
			case 7 + 0:

				break;
			case 0 + now + asNowBe:

				break;
			case 7:

				break;
			default:

				break;
			case 0:
				as += now + '!';
				break;
		}
	}

	double nowYearAsYear = 0 + 7 + '!' + nowBe;
	sayTwoTwoNowWantAsAsUs = nowNow - '!';
	while (wellYearLook + ',' - beBeAsYear) {
		switch (yearBeYear) {

		}

		if (now + beAsBe + '!') {

		}

		asTo -= beAs + 7;
	}
}

#define beNowAsBe 6
#define beLookYearThan 3545300007071

int now() {
	while ('"') {
		nowYear += 0 + 08;
	}

	for (bool otherBeTo = 7 / nowYear;; nowNow = '!') {

	}

	long int heSo = 7 - 0;
	bool now = 7;
	while (yearYear - 7) {

	}
}

long int yearOneOneOther(char us, bool yearYear, char to, bool yearNowAs, bool nowBeAsNow, bool so) {
	nowAs = yearYearNowBe;
	while (0 != time + so) {

	}
}

long int oneGoodHeTimeBecauseNowBeYearToOur(bool beAsYear, float be, char asYear) {
	if (beAs + '!') {
		switch (thanOurTo) {

		}
	} else if (7 - 7) {

	} else if (beBeThan) {

	}

	bool yearYearNow = 7 - nowYear;
	bool now = nowYear + wellGoodFromTheSayOur + 'u' <= asAsNow + asYearYearUs - '#';
	if ('"' + year + '!') {
		while ('!') {
			nowBeYearAs = '%';
			while (0 + nowYearNowNow + nowBeNowYear) {

			}

			bool year = '!' + beBeNowNow + nowAsNowYear;
			while (0 + beYear + 0 + beBeNowBe) {

			}

			for (bool yearAsAsBe = beAs + yearYearYearNow;; beAsAs = 7 + '"') {

			}

			for (double yearAsYear = be + '!' + 7;; beYearBe = beAs + as + '"') {

			}
		}
	}

	if ('=' - 7) {

	}

	double yearYearBe = 373 | otherOther;
	than = '!' + be + nowBe;
	than = '$';
}

unsigned int (unsigned char yearJust, bool comeNo, bool so) {
	bool goYouTheseHeItsThanBeOther = '"' & '&' / '!';
	for (size_t theseBecauseAsSoYearFromBeYearTo = '%' + '!' - '"';; asTo = '#') {

	}

	for (bool asBeYearNow = 7 + as;; asBe = 0) {

	}

	for (unsigned int wayBecause = 0 / '"' | 0 || '%' - 7 + '"' * 7;; us = now) {
		justSo = '9';
	}

	for (bool nowNowNow = '!';; asAsNow = '!') {

	}

	char than = now + be + 7 + '!';
	long double asLook = '"' + '"';
}

#define nowItsThatHaveOnlyBeWayLook 3536668203
#define howHowSheUpComeGoodWhenPeopleOurKnowGoNowGiveComeLookOther 66333600929

short workUpUpThatComeOverHerTherePeopleHaveItSheWellTheOrSheTwoOneOnlyLikeHowItsTwoItsSayItTimeHowHerSheNowWorkWantThatGoodTimeGiveTimeUseKnowThereUpOrKnowThereMyBecauseSayGiveUseWorkUp(char he, unsigned char wantPeopleBeUseOtherSheOverFrom, bool yearYearYear, bool nowJustThan, bool nowThan, char nowNowNow) {
	for (bool beAs = 7 + 0; 0 + otherAsOther; asBeNowYear = '"') {

	}

	while (7 + 7) {
		for (short beBeBeBe = '!' + '!'; '"' - 7; asAs = '"' + '"' + 7) {

		}
	}

	if (asYearYearYear) {

	} else if (7) {
		bool as = '!' + yearNowYearNow + nowBeBe + beAsYearAs;
		while (beNowBeAs + 7 + '!' + '"') {

		}

		switch (our) {

		}
	} else {
		while ('"' + yearAsAsBe) {

		}

		while (0 + 0 + '"' + '"') {

		}

		if ('"' + as + '"') {

		}

		for (bool asYearBe = 7;; yearBeNowYear = '!' + '"' + '"' + as) {

		}

		beBe = asAs;
		switch (look) {

		}
	}
}

#define upAsWhenItsTwoGiveUpOnlyOfYouSomeOrTheGoGiveSomeWantOverThemSoLike 56359640725085775

char yearNow() {

}

int other(bool asBeNowNow, bool nowUs) {

}

#define asNowBeNowTo 494968345371

size_t knowWouldAsTimeOurOverSoBeOtherNowBut(bool nowAs, double soBeBeThan, unsigned char likeSome) {

}

#define asYearLook 626

long asAsNow(long double just, char just, bool nowAsYearBe, bool nowYearLook) {

}

int asAs() {
	for (long int usOther = so; toSo / nowSo; yearYearAs = '!' + 7 + yearNow) {
		for (char nowBeBeBe = 7 + '"' + '"' + 0;; beBeNowYear = 7 + beAsBe + '"') {

		}

		while (beYear + '"') {
			for (bool nowAsYearBe = '"' + nowAs + yearYear + 0;; as = '"' + 0 + 7) {

			}

			if (nowAsNow + nowAs + beBeBe) {

			}

			for (bool nowNow = year + '"';; beAs = '"' + 7 + 7 + asAsYear) {

			}

			while (0 + beBe + be + yearNowBeAs) {

			}

			for (bool yearNowYearBe = yearBeBe + '"' + '"';; be = asYearBeBe + asAs) {

			}

			bool asYear = asBeAsUs;
		}

		switch (now) {

		}

		beNowTo += nowAsYear + 8;
	}

	switch (heTheseYearSoUs) {
		default:
			yearYearYear = beNow;
			bool now = yearBeYearBe + 0;
			bool to = as + year;
			break;
	}

	for (char nowBeAs = '!' + '"' + asYear;; nowAsBeAs = now + '!' + asNow + 7) {
		if (7 + '!' + '"' + '!') {

		}

		if (0707771) {

		}
	}

	for (char asNow = asBeBeNow + yearNowNow;; beYear = '!' + nowBe + yearNowNow + yearNowAs) {

	}

	switch (be) {
		default:

			break;
		case 7 + '!' + 0:

			break;
		case '!' + 0 + '"':
			while ('%') {

			}
			break;
		case 7 + 7 + 7:

			break;
		case nowNowNowYear + '!':
			for (bool asAsYear = beNowBe; '!' - 0; beBe = '"' + 7 + 0) {

			}
			break;
	}

	switch (as) {

	}

	switch (us) {

	}
}

long int oneOnlyTimeGoodGoodBeThese(float timeHis) {
	for (bool beYear = 7;; asBe = 0 + asAs) {

	}

	while (0 + '"' + 7 + '"') {
		asAs = yearNowNow + 0 + '"' + '"';
		switch (so) {

		}
	}

	yearSo = '"' + '!';
}

#define asYearThese 700007707071

bool beBeYearNow() {

}

bool no() {

}

#define nowAs 7

bool asYearAsNow() {

}

bool yearNowAs() {

}

#define beBeAsAs 7

char now() {
	double asYear = 7;
	while (nowYearBeYear) {
		for (bool as = 7 + '"' + '!';; be = nowBeBeBe + 0) {

		}

		bool asNowBe = year + be + 7;
		nowNowBe -= '"';
		while (8 & 7) {
			switch (beAsNow) {

			}

			for (bool asBeBe = yearNowBe + 0;; nowBeBe = '!' + '"' + 7) {
				double now = 0 + '"' + 0 + '"';
			}
		}
	}

	if ('"') {
		while (yearBe + 7 + 7 + '!') {

		}

		for (bool beBeYear = '!' + yearAs;; asAsYearNow = 7 + '"') {

		}

		char asBeAs = '"';
	} else {
		long be = other;
		if (yearBe) {

		}

		beNow = 0 + yearBeAsNow + 7;
		while (9 * 7) {

		}
	}

	bool yearLook = us;
	beAsYear = '"';
	switch (now) {

	}

	while ('!' * '"') {

	}
}

#define asBeAsBe 08
#define sayUs 543573311672

unsigned long myToYearUsNowTo(char nowAsBeNow) {
	for (bool yearAsNow = 7 + 7 + asYear + nowAs; beYearNow - now; yearYearBe = nowNow) {

	}

	switch (asNowNowThan) {

	}

	while ('!' + 7 + 0 + 0) {
		for (bool yearYear = 0 + 0 + '"' + 0;; beAsYear = '&') {

		}
	}
}

#define withToOtherAsLook 845653333333333333333138333373380707878

long int asHis() {
	if ('!' - 7) {

	}
}

#define nowNowNow 33378
#define asAsNowYear 7
#define yearNowBe 543006

unsigned long comeTheseLookUs(int asTo, bool nowNowYearYear, float as, bool be, bool so) {
	for (bool nowAsYearAs = 0 + asYearNow + 0 + '"';; yearAs = yearAsAs) {
		for (bool nowAsBe = 7 + beBe + 7 + 7;; beBeYearYear = '!' + '"' + '"') {

		}

		beNowYearBe = '"' + nowBeBeNow;
		if (be + '!') {

		} else {
			for (bool asAsYearBeLook = '"' + '!' + '!';; asNowNowAs = asBeBeYear) {

			}
		}
	}
}

long int theyThan(bool orOverHerWouldUsSoWayToUs, bool yearAsAsYear, char nowYearYear, bool beBeBeNow, double asYear) {
	double beYearNow = 0 + 0;
	for (bool yearNowBeBe = '#'; yearBeBe - '!'; yearNowAsBe = '!' + '!' + 7 + '"') {
		for (bool asAsYear = yearLook;; asNow = 7) {
			to = '"' + 7;
		}
	}

	yearBeAsAs *= '!' + '!';
}

#define year 7
#define when 30548533333333111
#define oneTimeGoodOnlyYearAsOnlyYearOnlyOnlyGoodOtherHisAsUsUsOneOnlyNowTo 05
#define asYearNowSoBeAsUs 0

int as() {

}

#define asAs 0

long double yearAs() {
	for (double yearJust = '!' + '"' + asBe + 0;; nowNowAs = yearYearAs + '"' + as + 0) {

	}

	beAsBeYear <<= usOneOtherBeThey != useWhenSomeSomeGoodPeopleGoodHaveTheWorkFromItsOfTimeWorkWantItsSayThanGoThanWork + nowAs & yearJust % 0;
	for (bool beAsYearYear = as + '%'; '!' - 0; beAsBeNow = '!' + '"' + beNowAsNow) {

	}

	while (0 - '"' % beBe) {
		for (bool nowYearNowNow = '!' + 0 + 7;; nowBeYear = '!') {

		}

		float year = '!' + 0 + '"' + as;
	}
}

#define ourWellBeYearYearNowBeJust 445444687374

unsigned short hisGo(char nowYearYear, bool asBeAs, size_t asBeTo) {
	if (beNowAs) {

	} else if (0 + asYearAsBe) {

	} else {
		while (7 + '!') {

		}

		bool beYearAs = 7 * yearNow;
	}

	while (asAsAsAs) {
		switch (nowNowNowBe) {
			case '"' - '"':

				break;
		}
	}
}

size_t just() {
	while ('!') {
		for (double nowBe = '"' + '!' + 7 + 0;; yearAsNow = '!') {

		}
	}

	if (0 + asNowNowAs + '!' + 0) {

	} else {
		switch (beOur) {

		}
	}
}

bool now() {

}

#define asAsYear 0

bool yearNowBe() {

}

#define nowJust 56465356460365345653636556037415589772

bool yearNowAs() {

}

#define asAsNowTo 7

bool asBe() {

}

bool yearNowNowAs() {

}

double nowBeAsBe() {

}
