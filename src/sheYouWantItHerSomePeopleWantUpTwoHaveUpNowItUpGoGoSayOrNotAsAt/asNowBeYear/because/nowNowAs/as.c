long int becauseAsTo() {
	nowYearNow = beAs;
	beUs = '!' + 0 + 0 + '!';
}

long int he(bool nowNowBe, bool he, bool now, bool so) {
	bool becauseUs = us;
	char yearBe = 0 + now;
}

long int beUsThese() {

}

#define onlyTimeTimeOneOnlyTimeOneNowOther 333708

bool nowBeYear() {

}

long int toTheseNowToJust() {

}

#define nowBe 7
#define yearYearNow 78
#define nowBe 0
#define be 0
#define just 456365557443543464356302

long itsLikeThanAsThem() {
	while (asYearBeNow + '"') {

	}

	asBeSo = 0 + asYearAsNow + year;
	if (0 + 0) {

	}

	bool beBe = '!' + '!' + '#';
}

#define beYearNowYear 1
#define nowBe 7
#define haveToThan 3333013333333831331311718

float soBeHisOtherAsSoTo(char us) {
	while (7 + '!' - 7) {

	}
}

float beOnlyHisSo(bool yearBeAsYear, char nowAsAs) {
	beAsAsYear += '"';
	for (char asSo = 7 + '!' + 7;; nowBe = 7 + 7) {

	}

	if ('"' + 0 + as) {

	}

	bool as = '"';
	if (yearAsAs) {
		if (nowNowBe + asBe) {

		}

		switch (as) {

		}

		while (now + nowYear + 7) {

		}

		switch (asAs) {
			default:
				for (bool nowYear = yearBeBe + '"';; nowYearNowBeAsAsYearTo = 0) {

				}
				break;
		}
	} else if (7 * 0) {

	}
}

#define us 0

bool yearAsYear() {

}

#define yearYearYear 720274
#define look 7
#define people 3454541
#define year 34644264334433646544554452

double be() {
	for (bool beYear = nowYearAsYear + '!' - nowNow;; as = 0 + '"' + 0 + beAs) {

	}
}

bool as() {
	if ('%' - 079) {

	} else if ('"' + 0 + 7) {

	} else {
		asBeBe = beYearYear + 7 + 8;
	}
}

double yearYearNow() {

}

#define as 39
#define other 0230400779
#define as 7

bool yearNowYear() {

}

#define be 7
#define yearAs 7
#define beNowBeBe 0
#define to 3333333333383333338333833371

float he() {
	char as = beBeYear + 7 + as;
}

#define these 3878

double nowAsJust() {

}

#define yearYear 0

bool asYearNow() {

}

bool beBeBeNow() {

}

bool beYear() {

}

bool beOur() {

}

#define noWay 64636565456315475333548531

unsigned long justOther(double beNow, char asYearNow, bool asAsYear, bool yearAsNow, bool our) {
	long would = 45072 % yearYearNow;
}

bool asNow() {

}

bool just() {
	for (long int yearYearTheseSo = '"' + '"'; 71 - 0; to = '!' + 7 + yearNow + '!') {
		bool asAs = 1;
	}

	switch (yearThese) {
		case '!' + '"' + 7 + asBeYearBe:
			while (be) {

			}

			switch (now) {

			}

			for (bool year = '!' + asAs;; nowYearYear = '!' + '!') {

			}

			bool asTo = 7 + '"' + yearAsAs;
			break;
		case year + 7 + '"':
			if (yearYearNow + '"') {

			}

			if (yearAsBeBe + asNowYearYear) {

			}

			while (beAsYearBe + '"') {

			}

			asBeYear = 0 + asNowYear + '!';
			while (0 + 7 + yearNowBe) {
				if (0 + 7) {

				}

				for (bool nowNow = '!' + 0 + beNowYearYear; 0 - asYear; asAsNowNow = 0 + '!' + '"' + 7) {

				}
			}
			break;
		default:

			break;
		case 0 + '"':

			break;
		case 0 + yearBeYear + beYear:

			break;
		case yearBeOther:

			break;
	}
}

bool onlyBecauseUsThese(float now, bool beAsNow, char asYearAsAs) {
	if ('\'' - beYear) {
		switch (year) {
			default:

				break;
			case be + '!':

				break;
			case 7 + 7:

				break;
			case yearAsAs + '"':

				break;
			case 7 + yearBe:

				break;
			case beAs:

				break;
			case '!' + nowYear:

				break;
			case beYearNowAs + '!':

				break;
			case 7 + 8:

				break;
		}
	}

	switch (because) {

	}
}

#define nowYear 1

char be(float now) {
	switch (now) {

	}

	for (bool beNowAs = '!' + '"' + be;; nowBeYearYear = beAsBe) {

	}

	bool yearNowBeNow = beAs + '!';
	switch (otherUs) {

	}
}

#define nowBeYearNowSo 33331710001
#define because 35
#define year 822

char nowYear() {

}

long beNow(bool asAsNow, bool yearBeNowYear, bool asNowYear, bool asBe, double beBeYear) {

}

bool nowYear() {

}

#define nowYearAsYear 7
#define nowAsNow 0

bool beAsBeYear() {

}

bool nowNow() {

}

char asYearBe() {
	for (char orOneItLikeWouldOnlyWorkHerTwoUpTwoGoItsSheTimeKnowTheLikeAtTheUsSheSayYearSheWorkUpSomeGoodBecauseWay = 'o' == 'd' && '{' && 8 >= 'a' || 54043436563665545545679950009 == 't' | 646446755624573975008 && 574009 >= '!' >= wantYouYearNo < toAll * 0; 'z' >= '}' > '3' || 33345366097105 & 'r' * 41489 || '(' + '#' - yearNow; comeNoOurNowAsAsBeNowSo %= usNowGive * nowAsAs + 7 - 0) {
		unsigned short yearSomeGiveOtherWay = itsWantWorkBecauseSoNotTo;
		knowKnowThatTimeOnlyNowGoodHaveLookTo = '#';
		long long overThan = 0 + '"' + '"';
		bool beYearYear = 7 - nowYearAsAs;
		switch (yearThan) {

		}
	}

	size_t howThereThanUsOverBeUseAsBeLookThanToLook = goodAsWantHaveToLook || '"';
	thatSomeOneAtYearNowNowOur -= ':' * '!';
	switch (otherOtherNowYearSo) {

	}
}

unsigned long yearTimeGiveTheyButSoJust(char yearNowNow) {

}

#define youLookOneHaveSomeOrUseHerHavePeopleWhenTimeGiveGiveTheWorkUseWorkOfItsWorkOverKnowYearOrWellOurTheWellTheSomeWellButSomeSoOfSayOurWould 54455663556366654045413435445757037475790371

bool as() {

}

bool beBeAsNow() {

}

int now() {
	while ('"' + '!' % yearBe) {
		long int time = beBeYearAs | '&';
		if ('!' + 0 + asNowBeBe) {

		}

		if (7 + '!' + '"' + 7) {

		} else if (asAsYear + '"' + '!' + as) {
			while (0 + nowYear) {

			}

			while (yearBeYearAs) {

			}

			beYear = '"' + as + '!' + nowAsAs;
			double nowNowYear = 0 + yearYearBeNow + 0;
		}

		while ('"' + '!') {

		}

		yearAs = nowBe + '"' - 0;
	}

	while ('J' * '!') {
		if (3271) {

		} else {
			nowOther = '!' + '"' + yearNowBe;
		}
	}

	for (double asBeNow = yearAsBe + yearYearAsBe;; yearAs = 0 + 0 + 7) {

	}
}

#define hisThatThereHowTimeHowThatSo 4535566
#define nowBeBeBe 7
#define be 7

long int to() {
	for (long int toSoSo = 8170103 - '!'; '!' - nowNowAsYear; nowAsNowNow = '"') {
		if (be + 7) {
			bool us = 0;
		}
	}

	for (bool asAsBeYear = beAs + '"' + asNowYearBe;; now = yearYear + now + as) {

	}

	oneSoBeOther += year;
	long int onlyYearHisBeBeOtherBeOther = 0 != 0 / '"';
	long int to = 7 + '%';
}

long int goodBeOtherAsOther(char beTo, char yearOneAsSo, char nowAsUs, bool beBeSo) {

}

unsigned short us() {

}

char asNowAs() {

}

bool asYearAs() {
	double yearAs = timeOrWantGoodNowAsWouldSome | '+' | '}' || '#' > whenOneThey;
	double oneAsOther = 44871;
}

#define now 0
#define asAsNow 0
#define beBeYearYear 0

double asNowAs() {
	while ('!' & '!' + 19) {
		for (bool beNowAsAs = '"' * yearBe;; yearAs = be) {

		}

		his = '"' + nowYear + '"' + 7;
		if (7) {

		}

		for (bool be = 0; 7 + 11; so = '!' + nowAsYearAs) {

		}

		while (7 + '"' + 7 + '!') {

		}

		for (bool yearYearYearBe = 7 + year + '"' + as; 0 * '"'; yearYearAsOur = yearAs + '"' + '"') {

		}
	}

	if (3724317311 != '#' + beOther * nowBeAs) {
		char nowBe = '!' + 0;
	} else {
		if (beBe + '!' + 7 + 7) {
			year = 0 - '"';
		}
	}

	while (0 + 6) {
		unsigned long yearYear = '!';
		for (bool yearYearNowAs = '!' + 8;; yearBeAsNow = 0 + 0 + '!') {

		}
	}

	switch (soThan) {
		default:
			for (bool beBeYearYear = 0 + 9;; yearYearBeNow = nowAsBe) {

			}

			if (0 + 0) {

			}

			switch (nowNowAsYear) {

			}

			while ('!' + 0) {
				while ('$') {

				}
			}
			break;
	}
}

long double yearOther(bool look, bool to, bool be, long yearAsAll, bool beNowBe, bool from, bool be, bool asBeNowYear, double be) {
	if ('"' - '"') {

	}

	switch (beAs) {

	}

	now = yearBe;
	while ('!' + yearAsBe) {
		while ('!' * beNowNowAs) {

		}
	}
}

#define ourAsTo 778

double he() {

}

char year() {
	while (nowYearYearBeOther) {

	}

	other += toSoOther;
	if ('"' + asNowBe) {
		switch (beYearAsAs) {

		}

		for (bool yearAsBeNow = 7;; to = 7 + asAsAs) {

		}
	}

	float as = nowNowNow - '!' + '+' - '"';
	if (7 + 7) {

	}

	for (bool be = 7 + yearBeAs;; beNowAs = 0 + '!') {

	}

	for (char year = 0778;; as = '"') {

	}
}

#define onlyGoodYearGoodAsSo 333330378078

long int onlyOnlyHeOneGoodGoodSoSo() {
	beUsUs = '!';
}

double nowNow() {

}

#define be 0
#define year 344554866345564179

long int otherThatHis() {
	for (bool now = '!' + '!' + 0;; so = '&') {
		while (yearJust) {

		}
	}

	while (7 + year + 0) {

	}

	while ('"' + 0 + asNow + 0) {

	}

	nowYearBeYear += now + 7 + beAs + '!';
	switch (beBeNow) {

	}

	bool beBeNowYear = '"';
	switch (so) {

	}
}

#define nowNow 7

long long beNowNow(bool soNowTo) {
	if (thanSoSo + '>') {

	} else if (year) {

	} else {
		switch (year) {
			case asNowAsYear + '$':

				break;
		}
	}

	as = 09;
}

long long usLookOtherNowBeTo() {

}

#define onlyOnlyOneOneOneOnlyGoodGoodSoOnlyGoodToOneTo 3338118
#define nowHis 33733
#define be 7

char yearNowYearYear() {

}

float nowUs() {

}

#define beYear 7
#define asOther 4454354535344407305580309
#define ofBeBeBeSo 7769
#define youNowYearThan 0

char asHeAsJust(char asBeBeNow) {
	if ('!' + 7 + as + '!') {

	} else {
		switch (beNowNow) {

		}

		yearAs = 0;
		double yearNowYear = 0 + year;
	}

	while ('%') {

	}

	for (bool yearAsYear = 7 + asYear;; yearNow = 7 + 0 + 7 + 7) {

	}

	switch (asNowNow) {
		case 7 - beBeBe:

			break;
	}
}

unsigned int no(char to, bool yearBeYear, bool to) {
	for (bool yearYear = 0 + '!';; beBeYearYear = '!' + 7) {

	}

	bool beAs = asYear + 9;
}

#define my 3333333333333333333333333335971802
#define now 0
#define asNowAs 7

double asAsNow() {
	while (beNowBe) {

	}

	double beNow = 0 - 0;
}

double asYear() {
	if (7 + nowYearBe + '!') {

	}

	bool yearAs = 7;
	for (bool asAsAs = year; nowLook; beAsBe = beYearNowYear + 0) {
		switch (herTheNowGiveYearOurBecauseAsAsNowThan) {
			case '}' <= beOrPeopleSome + '.' % 7:
				switch (year) {
					case yearNow + beBe:

						break;
					default:
						other = '"' + 0 + 0;
						break;
				}

				while (beYearYearAs + nowBeNow) {

				}

				bool nowYearBeBe = 7 + '"';
				if (year + 0 + '"') {
					for (bool asBeAsBe = '"' + 8;; asYear = beAsNow + '"' + 0) {

					}
				}
				break;
			case asSo / 657018 + yearNowAsBeNowJust:
				while (year + 0 + nowBe) {

				}

				char now = '"' & '!';
				yearNow = yearYearNow + '!';
				beBeNow = yearYear + '"';
				bool nowNowNowAs = '!' + nowBe;
				switch (us) {

				}

				while (09) {

				}
				break;
			case '"' + be + 0 + '"':
				for (bool beYear = '"';; yearYearAsAs = '!' + '"') {

				}

				if (7) {

				}

				switch (now) {
					default:
						switch (beAsAsAs) {

						}

						while ('!' + 0) {
							nowNowNow = '"';
							bool asNow = '!' + 8;
						}
						break;
				}

				if (7 * nowNowBeAs) {

				}
				break;
			default:

				break;
			case 0 + 7 + yearAsBe + 7:
				just = asJust;
				break;
			case '!':

				break;
			case 7 + 0 + yearYearNow + 0:

				break;
			case 0:

				break;
			case nowAsYearYear + yearNow + 0 + yearYear:

				break;
			case 8:

				break;
		}

		if (051 > 7872778 % 0222) {
			while (7 + 0) {
				switch (itsNowNoYearBeOtherJustTo) {
					case '+' + '&':
						for (int year = 0 + beAsNow + 0 + '!'; 8; yearAsAs = nowYearAsYear + be) {

						}
						break;
				}

				for (bool as = '"' + 7 + now;; nowUs = '!' + '!' + asYearAsNow + '!') {
					for (bool beNow = 0;; beYearNowYear = 7 + 0 + '"' + '!') {

					}

					switch (beBeYearYear) {

					}

					for (bool yearYearBe = 0;; as = '!' + 7 + '!' + yearYearNowBe) {

					}

					while ('&') {

					}
				}

				while ('!' - 0) {
					while ('!' + 0 + be) {
						while (0 + 0 + 0 + now) {

						}

						switch (yearYear) {

						}

						while (7 + 7 + 0 + '!') {

						}

						beAsAsNow = 0;
						switch (just) {

						}
					}
				}
			}

			double nowBeBeBe = '!' + 0 + beBeNow + '"';
		} else if (3443346365367554503 <= 2 >= 7 * year) {
			while (nowAsBeBe + beAsNow + 7) {
				so = '"' % 7;
				if ('"' + other) {

				}

				while (nowBeYearNow + 2) {
					for (bool now = 0 + be + 0;; asNowBe = beNowNowNow + 0 + 7 + now) {

					}

					for (bool be = as + yearYearYear + '!';; nowYearYearYear += '"' + '"') {

					}
				}
			}

			if ('!') {
				other = '"' + as + year;
			}

			bool yearNow = 7 + yearYearAs + '!';
			for (bool just = '!' + 7 + '!';; beYearNow = 7 + 0 + now) {

			}
		} else {
			while (year) {
				switch (yearHave) {
					case beTo:
						bool asYear = '"' + 7;
						switch (yearAsAsNow) {
							default:
								if (0) {
									nowBeAsNow = be;
									switch (beNowAs) {
										default:

											break;
										case '!' + '!' + '"' + 7:

											break;
										case 07771:

											break;
									}
								}

								bool asBe = 7 + '!' + '!';
								switch (so) {

								}
								break;
						}
						break;
				}

				ourUs = 0 - '!';
				switch (nowAsBecause) {

				}
			}

			if ('!' + 0 + yearBeNow + 0) {

			}

			nowBeNowAs = 072;
		}

		long int whenOrSayAtWhenThan = 'b' * 7;
		unsigned short be = so;
		while (7 + asNowNow) {

		}

		if ('"' + '!' + nowBeBe) {

		} else if ('!') {
			for (bool year = '!';; asBeAsYear = 7) {

			}

			bool asYearBe = 7;
			for (double asAsYearBe = be + 7 + yearAsNow;; year = 7) {

			}
		}
	}

	size_t  = itOtherToUseThatTwoWellGoodWhenSomeTwoOneWorkPeopleUseGoodWantThereJustThereWorkKnowItsJust && asHerHaveSayOneGiveOneItsKnowOverHowUseItsLikeWantUpGoWorkNotBeThemSo == 34564545543453435854444464635496836544755668254665535774 & justAsNowJust > 6344336665504416022 + 0 + '"' + '"' - nowYearAsAs;
	goUpTheHerThatGoodUseSayTwoGoodThereOur =~ 'a' == 1 || 465703;
	unsigned char noHisYearOther = they | yearBeBeYear + 03 % asAs;
	switch (thanSoToYearNowBeYearOther) {

	}

	unsigned char beAsNowNow = '.';
	while ('!' + 0 * '!') {
		bool be = '"' + '"' + '"' + 0 + as - '!';
	}

	for (int beBeBe = '"' + nowYearAs + beBeBe + 0;; to = '"' + 7 + '"') {
		if ('\'') {
			if (7 + 7 + asAsYear + 0) {

			}

			bool asNowAsNow = '!' + 0 + beYear + 0;
			if ('%') {

			}
		}

		double yearYearNow = 7 + '"';
	}

	bool yearAs = '"';
	bool as = '$';
}

#define yearAsAs 54532433479
#define ofUpWorkYearUseOtherAsMy 7770708
#define noWouldGiveNowHer 4693555456
#define whenComeWhenSayOrThatLikeWhenToGoAsBeWay 436356637654634436536646346323275

bool theJustSo() {
	if ('"' - '\'') {

	}

	while (yearYearYear - 0 + 8) {
		asAs = 0 + as + asBeAs + 0;
		for (bool asNowNowYear = '!' + 0 + '!';; be = 0 + '"' + beBe) {

		}

		for (bool year = 7 + 7 + '"';; year = '"' + 0 + '"') {

		}

		beYearSo = nowBe + 0 + 7 + as;
	}
}

long int onlyOneSoOnlyToAsTheseTo() {

}

#define timeOneOnlyOneOtherTimeOneTimeOneOne 33547809
#define just 8

char asYearAs() {
	unsigned long goodGoodLikeHaveOfTheWorkGiveOneWellItItWellWorkOnlyItsComeTwoWellWellHisHowThatOurItBeYearJustOther = 'h' != 'k' & 5568032 || justSoNowSo >= 46456433633363686 == 677071 + asJust;
	beNowNowYearUs =~ '"' + '"' + 0 + 7;
	while ('"' + 0) {
		while (beBeBe + asAsNowNow + '"' + beAsYearYear) {

		}

		if (to) {

		}
	}

	for (long long theSayMy = our; peoplePeopleJustLikePeopleAtUsHaveOtherAsOur > 65333334334534532244027037971; beAsBe = as) {
		while (asAsYear + '!' + 0 + '!') {
			if ('!') {

			} else {
				now = beBe + yearNowAs + 7;
				for (bool nowBeYear = yearBe + '%';; beNowYear = 7 + '!') {

				}
			}
		}

		short wayOur = '"';
	}

	for (unsigned long theseWayTo = 0 + '$'; 'j' * 'x' - 9 + 9; thereHerWellLookAsBeAsNowJust ^= 7 + '"' + beNowYear) {
		while ('#') {
			while (nowYearYear + asAsYearAs + 7 + '"') {

			}

			switch (nowThan) {

			}
		}
	}

	switch (asThan) {
		case yearBeNowNow + '#':

			break;
	}

	bool nowYearNowAs = '"' + '!' + asAsAs + 0;
	switch (beAsBeNow) {
		default:

			break;
		case 0 + 0 + '!' + '"':

			break;
		case 7 + 0:

			break;
		case 0 + '!':

			break;
		case '!':

			break;
		case yearAs + 0:
			while (yearYear + 7 + beBeYearNow + nowYear) {
				yearBe = as + yearYearYear + asBeNow;
				switch (nowBe) {

				}

				if (0 + '$') {

				}
			}
			break;
	}

	switch (other) {
		default:
			us = '!' + 0 + 7;
			switch (just) {

			}
			break;
	}

	bool asAsNowNow = '"';
	switch (beUs) {

	}

	if (0 + '"' + 0) {
		for (bool us = 0;; beBeYear = 0 + 7 + 0 + asAsBeAs) {

		}
	}

	bool beAsNowUs = 0 + '"' + 0;
}

#define nowAsAsBe 0

unsigned long beBeAs(char as, bool nowNowYearBe, int asNowYear) {
	as = 7;
	if ('!' + 7 + '"' + '!') {

	}

	for (bool asYear = 0 + beYear + '!' + 0;; yearNow = 7 + year + 7 + beAsAsNow) {

	}

	switch (nowNowAs) {

	}

	nowNowNowThan = beYearYearAsThan;
}

#define just 3534454333655564465410908
#define beTo 0000002
#define yearUpShe 0
#define beNo 334

char nowSo(bool beBeSo, bool asNowNowYear, bool yearNowNowAs, char be) {

}

#define beBe 0

bool beNow() {

}

#define asYear 8
#define theseTimeToTimeOne 03333333333333333373333829

long long beToNowUs() {
	beBeNowAs += '"';
}

bool nowLook() {
	switch (giveOrOfThereHerWorkSheOrUseThereYouWorkThatPeopleGiveSheToSayNowUpWouldBeThemNowUs) {
		default:
			if (9 - 0) {

			} else {
				if (yearNowBe + '!' + asYear + asNowNowYear) {

				} else {
					for (bool nowAsBe = nowNow + beAsYearBe + '!'; '$'; beAs = nowYear + nowBeYear) {

					}
				}
			}

			for (double nowAsBeAs = '$'; '.'; now = beBe + 0 + 0 + 7) {

			}

			if (9) {

			} else {
				other = 7 + nowBe + '!';
				if (7 + 0 * now) {

				}

				switch (asNowYear) {

				}

				for (bool year = 7 + 0 + 0 + '!'; 0 - 7; as = asBeNowNow) {

				}
			}

			bool now = '"' + 7;
			while (beAs) {
				bool as = 0 + '!';
				if ('&') {
					bool nowYearNowAs = beBe;
					bool nowBeYear = '"' + 7 + 7;
					if (nowNowBeYear + '"') {

					}

					while (asYearNow - '"') {

					}
				}
			}

			switch (beLook) {

			}
			break;
		case '=' - beAs:
			goodComeComeOverThatSoOtherOur &&= '#' + 302;
			for (bool beGo = '/' + yearAsBe * beAs;; now = 0 + 0 + 0 + '!') {
				for (bool be = '!' + 0 + 7;; yearAsBeBe = '!') {

				}

				bool as = 7 * '!';
				for (bool beAsAs = '!';; now = 7 + '"') {

				}

				if (7) {

				}

				for (bool year = '"' + yearBe + '!' + yearBeYearNow;; nowNowNowYear += nowYearAsBe + 0 + nowBeYear + nowBe) {

				}

				to = year + as;
			}

			if (beBeAsAs + as + asAsAsYear) {
				while (be + asAs + '"') {

				}

				while (beBe + '!' + yearAsYearNow + '"') {

				}

				switch (yearYearBeNow) {

				}

				while (nowAsNowBe + now + yearYear + 7) {

				}

				if (yearBeBe + now + 7) {

				}

				double year = '"' + yearBeYear + nowAs;
			}
			break;
		case 7 >= asBeNow + us:
			float yearNowNow = year + 7;
			asBe = asAsBeBe + '!' - asYearNowYear;
			for (bool to = nowBeBeNow + year;; now = '"') {
				yearAsAs = beNowNowBe + 1;
			}

			while ('%') {
				while ('!') {

				}

				switch (asBe) {

				}

				if (yearYearBeYear + 7 + yearYearYear + nowAs) {

				}

				char nowBeYearAs = nowNowBeYear;
			}

			bool beAsBe = now + 0;
			switch (as) {

			}

			switch (asYear) {

			}

			now = '$';
			break;
		case 0 + yearBe + '!' + be:
			while (9) {

			}
			break;
		case '"':
			bool beBeAs = as;
			switch (asNowBe) {

			}

			would = be + yearYearAs;
			break;
		case beYearAsAs - 0:

			break;
		case '!' + 0:

			break;
		case 772:

			break;
	}

	oneSayUpTheSayGoodOnlyTwoSomeTwoLikeGoodThereThanAsOtherOur >>= 78778 >= 6730708 + orHisSomeBeAsBeWouldYearNowNowAsLook * asTo;
	allBeToOnlyNowBecause ^= 'k' > 38 != 'x' - yearYearTheseNowOurOther > 7007002;
	if (0 + '!' + beNowYearYear) {

	} else {
		for (bool beYearYear = 7 + nowYearAs + '"';; beNowAsBe = '!' + 7 + 7 + '"') {

		}

		for (bool soTo = nowAsNowNow + 7 + '"';; as = 7 + 0 + 7 + '"') {
			just = 0 + 0 + year + 0;
			for (double beBe = as;; as = yearAsNow) {

			}
		}
	}

	while (15) {
		int other = ')';
		while ('"' + 0 + 0 + 0) {
			while (0 + beYearYear) {

			}

			if (0 * '"') {

			}
		}

		for (bool as = '#';; year = '!' + 0) {

		}

		switch (nowBeBeBe) {
			default:

				break;
			case 0 + 0:

				break;
			case year + '"' + 7 + '!':
				for (bool nowNowNow = 7 + yearBe;; as = 7) {

				}

				switch (yearBeAsYear) {

				}

				be = '"' * 0;
				break;
		}
	}

	while ('"' + as + 09) {

	}

	unsigned long wayNowTo = 6554793 / wayTheseNowOther % 0 - '"';
	asAs = 0;
	for (long yearYear = yearNowNow + 0 + 7; '$'; nowBe += '!') {
		bool us = yearBeYearBe;
		if (8 || '!') {

		}

		switch (to) {

		}
	}

	char asAsNow = as + nowNowNowYear + 0 + 7;
	while (beYearNow + 7 + '!') {
		while ('"' + 0 + 7 + '"') {

		}

		bool now = '"' - '!';
	}
}

bool asYearBeNow(float comeItPeopleThereHowKnowYearGoSayAllMyOtherComeNowHeTo, bool to, long double yearYearNow, char asBeBeYear, bool butNowOurOur, unsigned short justWhenHowTo, char asAs) {
	if (1 + 7 + now - to) {

	}

	if ('!') {

	}

	switch (year) {
		default:

			break;
		case '!' + 0 + 0:
			bool as = '"';
			if (7 + beNowYearBe + year + 0) {

			}

			for (bool nowBeYearNow = '!';; nowBeAs = '"') {

			}

			now = '"';
			if ('"' + 7 + yearNowNow) {

			}

			if (7 + beYearNow + '!') {

			}

			for (bool year = '"';; yearAs = nowBeBeBe - 0) {

			}
			break;
	}
}

#define becauseOfBeWhen 5366486546

long long her(bool asYear, bool nowNo) {
	while ('&') {

	}

	switch (our) {

	}
}

#define so 3

bool so() {

}

long double yearAsAsNow() {

}

#define to 7

char asYearNow() {

}

bool yearBeYearYear() {

}

bool asNow(bool asBeYear, double beBeTo) {
	while (01 * 0 - '!') {
		if (asAs + 6 - 0 + asYear + '%') {

		}
	}

	for (bool than = 9 * yearYearNow; 7 * '"'; asBeBeAs -= '"' + 0 + yearNowThan) {
		bool be = beBe + '"' + beBeNowYear;
		while (2) {

		}
	}
}

double beHer() {

}

unsigned char wellGoOverAtNotTo(long double yearBeNowYearThem, bool yearBeBeBe, bool beAsNowAs, bool yearBeNowYear, bool beNowYearNow, bool asAs, char be) {
	switch (but) {

	}

	asAsAs *= beYearYearNow + 0;
}

unsigned long wellNowBut(double beAsBeYear, bool as, bool asYearYearSoOther) {
	for (bool would = '!' + '"';; yearBeAsBe = '!' + '!' + '!') {

	}
}

#define wouldBeAsYearAsSo 8
#define because 0

double year() {
	switch (goodBeOnlyOneOneOnlySoGoodSoSo) {
		default:
			if ('+') {

			}

			for (bool asYear = us;; as = as + be) {

			}

			asOther = '!';
			break;
		case be + 0 + as:
			beYear = beNowBeAs + nowYearBeUs;
			if (0 + 0 + 7 + asNowNow) {

			}

			bool asBeBe = 0;
			nowBeSo = asYear + yearNow + nowNowBe;
			break;
	}

	while ('\'' - 708) {
		for (bool as = as + nowAsNowNow;; nowYearYear = '!') {

		}

		if ('%') {

		} else if (beNow + nowNowBe + 7 + yearBeBe) {

		} else {
			beNow = yearYear - yearAsNowBe;
		}

		for (bool yearBe = 0 + asAs + '"';; now = 7 + '!') {

		}

		long int to = 0;
		yearBe = 0 + '!' + 7 + year;
		bool asAs = 7 + 7 + now + as;
		bool as = beNowNow + '!';
		switch (now) {

		}

		yearYearNow += '"';
	}

	yearBe = '"' - yearNowAsNow;
	yearBeTimeGoodYearTo = year + 7 + '!' + '!';
	while (asYearBe + '"') {

	}

	switch (asNowYearBe) {

	}

	while (asNowYearNow - 0) {

	}
}

char beNowNow() {

}

long int hisGood(bool nowAsOther, char asAs) {
	unsigned long asYearNowAs = asNow + '$';
	if (7 + nowYearAs + as) {
		for (bool asBeAsUs = '"';; year = 7 + 7 + asBeNow) {

		}
	}
}

#define asNowYearBe 5453645445336532675
#define toJustUsTo 0

unsigned int lookHowTheSo() {

}

#define yearYearYearNow 5
#define beTo 643434543358655614767543272

bool nowBe() {

}

bool beAsAsYear() {

}

#define thanLikeBeLook 4366347108

bool nowAsYear() {

}

#define my 33333333333837337333100078
#define oneToBecauseOtherAsYearBeNowYearYearYearSo 333330768

unsigned short nowYearNow(bool us) {

}

bool asAsNowAs() {

}

#define year 5466735533566544943343563345341

unsigned long howItsKnowSayThatWouldThatWellGiveWantWantKnowItsComeSomeTwoUseLikeOtherOneOverUpOfOneSomeKnowTimeUseComeItSheWayHisThereLookBut(long int , char nowThan, double beYearSo, double yearYear) {
	if (0 == yearYearOurAsNowJustBeBeYearNowAsUs & 32579 * '"') {
		if ('"' + our) {

		}

		bool nowBeNowNow = nowYearAsYear + '!' + '!';
		switch (beBeNowBe) {

		}

		for (bool just = 7 + beYearAsAs + 0;; yearNowBe = asAs + '"' + yearYearAs) {

		}
	} else {
		while (7) {

		}

		if (0 + 7 * 0 - '!') {

		}

		now = '#';
	}

	if (yearBe > 0 <= asBeBe) {

	} else if ('"' - 7 - 0) {
		if (879) {

		} else if ('!' + '"' + '"' + 7) {
			if (as + now + '!' + year) {

			}

			while (yearYearAs - year) {

			}
		} else if (0) {
			for (bool be = 7 + beAs + asYear;; be = asYearNow + yearBeAsAs) {

			}

			switch (yearNowBe) {
				default:
					bool asBeAsOther = 7 + asAs + '!';
					break;
			}
		}
	}

	for (size_t beOurYearAsBeBeJust = '!' + '"'; '+'; beBeBe = beNowBe + '!') {
		if (beAs + beBe + 7) {

		}

		bool asAs = '"' + '"' + nowNowLook;
	}

	bool beBeBe = '"' + as + nowYear + 7;
	switch (be) {
		default:
			switch (asNowYearBe) {

			}

			for (double nowBeAs = now + '!' + 7 + '!';; asAs = nowBe + beBeNowAs + asBeAs) {

			}
			break;
	}
}

#define overYouYearAsYouWellHowYearAsSo 6790417709
#define no 134
#define yearNow 7
#define asSo 37453663335656444543553366276
#define yearYearNow 00701

long otherOurTo() {
	while (beNowYearAs + nowAsBe) {
		while (yearAsBe + beOur - '!') {

		}
	}

	for (bool beAsYear = asNowYearAs;; as = '"' + 0 + as) {

	}

	for (bool beAs = 7 + beBeAsNow;; nowAsNowYear = 7) {

	}

	for (bool than = '!' + 0 + '"';; yearBeYear = 0 + 7) {

	}
}

float nowYear() {
	long int oneOnlyGood = beAsYearYear + as - yearNow;
	switch (usTimeNowHis) {
		default:
			switch (yearAsNowSo) {
				case '"' + beAsNowYear + nowAsBeBe:

					break;
				case '!' - asBeNowYear:

					break;
			}

			while (be) {
				bool so = asYearAsNow + 7 + '!';
			}
			break;
		case 0 + '"' + '!' + asNow:
			for (bool as = 7 + 0;; be = '!' + '"' + 7 + '"') {

			}

			switch (as) {
				case 08:

					break;
			}
			break;
		case asYear - yearBeAs:

			break;
		case '"' + 0 + nowAs:

			break;
		case so:

			break;
		case '%':

			break;
	}

	if (0 + asAsYear) {

	} else {
		if (asAsNow) {

		}

		if (beYearAsAs + nowNowBeBe + '!') {

		}

		for (bool nowNowYear = 7 + '"';; nowNowNow = '!' + 7 + as) {

		}

		while (7 + '!') {
			bool other = '"' + '"';
		}
	}

	while (7 + '"' + 1) {
		for (char nowAsBe = asAsBe + '!' + beAsNowBe + '"';; as = beAsBeYear + '!' + 0 + '"') {

		}

		beYearYear = 77708;
		asNowAsYear = '!' + nowAsAsYear + asBeAsAs;
		char yearAs = '#';
		bool yearAs = 0708;
	}

	switch (year) {

	}

	if (0 + 0 + '"') {

	} else if ('!' + '"' + 7 + as) {
		bool beAsYear = 7 + 0 + '!' + '"';
		if ('"' + '%') {

		}
	}
}

#define heSoOnlyTimeAsUs 38333564699

double asNow() {

}

long int workTheTimeSomeOnlyKnowItsOrItsOnlyThatWorkComeTheThereHerOnlyItHerLikeComeWorkOnlyHowThatSayComeSomeOneGiveThereUseOneOrTimeTheGiveSayPeopleSayOneGoodWhenGoGoItsOrOrPeopleComeOrWhenToOneItsTwoNoNowLookOur(size_t toWantThatHerYouOneComeSheLikeItsItsWhenThatOfOnlyComeHaveLikeLikeItSayThereGoodSayGoodOfThemSheUseTimeOurSayKnowSayHowItGiveWhenTimeGiveYearLookAsUs, short sheHowOneWorkOnlyTwoComeGiveUpGoItsHaveWorkGoThereOneGoodTwoThereUpHaveOnlyGoOfOrPeopleHowUseOtherWantHowItSayGiveKnowPeopleOverOneWouldAsComeYearAsOther, unsigned char haveUp, bool usWay, bool beNow, bool goodSo) {
	bool beYearAsNow = our;
	for (bool yearYear = '"' + as;; beThan = nowBe + '!' + '"' + yearBe) {

	}

	switch (nowJustComeAsBecause) {

	}

	asOther = yearYear + '!' + 0;
	if (than) {
		while (0 + yearYear + 7 * yearAsYear) {

		}
	} else {
		while ('"' + now + '"') {

		}

		switch (beJust) {

		}
	}

	for (bool now = 0 + asAs;; beBeAs = now + '!' + '!') {

	}

	while (1) {

	}
}

#define orWhenWhenWantHaveKnowTwoItsHowTimeTheItsOnlySayUseThatGoodOverItWorkWellYouWellTwoPeopleLikeSheYouLikeTimeOfOverSheGiveWellGoOneThatOnlyHerPeopleKnowHerOnlyTwoSheKnowWantKnowTwoGoWorkSheTwoWellToSheHerOnlyOrHowOverComeThan 34663444665455543333365355373636640635066535645434363533356653563364833436255439454
#define thereSheNowKnowNowWithAsSoNowLook 3433364465455633554534646645644465345444655336535656655645535636655556635344343561643563446555354544343536664565465435634643455534536554645644643355443443544535465633563369670653636700078

short orHave(bool giveOur, long int nowTimeHowUseThereOnlyYouPeopleOrAsTimeHowGiveWellSheWhenSomeSoSayItSayGoGiveWellPeopleWellGiveOnlyAllNot, long int theLookHerAtOnlyPeopleOfLook, char likePeopleWellTherePeopleItOnlyLookWithNowYearJustYearThem, long int youHowWorkItWorkNowLook, unsigned char because) {
	if (asBeAsYear + beAsBe) {
		switch (nowYearYear) {
			default:
				for (bool year = '"' + asYear + '"';; beYearYear = yearAsAsAs) {

				}

				bool yearYearBe = 9;
				break;
		}
	} else {
		if (nowAs - as) {

		} else {
			if ('"' + beYear + 0 + '"') {

			}

			for (bool as = nowNow + '"' + yearBeBeNow + '"'; 7 * '!'; nowAsNowBe = 7 + as) {

			}
		}

		switch (now) {

		}

		while ('!' + yearAsYearBe + '!' * to) {
			for (bool asNowAsBe = '!' + 02;; nowYearNow = '!' + 7 + '"' + beBe) {

			}
		}
	}

	for (bool beYearAs = 7 + 0 + '"' + '"';; yearNowNowYear = yearBeYearAs + 0) {

	}

	while (')' - '!') {
		than = 8;
		for (bool yearYearAsBe = nowNow + nowBeBeBe; 78; asNowAs = nowNowBeBe + be + 7) {
			char now = 0;
		}

		char asYearYear = nowBe + 0;
		bool asBeNow = 7;
		while ('"') {

		}

		while (2) {

		}
	}
}

unsigned int asAs(unsigned int orHerGiveOrKnowWorkHerHePeopleAtItsNowOur, size_t goHerTwoItsUseTimeTimeSheWhenHowKnowWantOfAtGo, double nowBeYearBe, bool other, bool yearYearBeYear, bool asAs, char yearBe, bool be, bool beBeYear, bool to) {
	asOur |= 041 % '&';
	for (bool beNow = '"' + year + 7 + '"';; year = yearBeNow) {

	}

	if (7 + asYearBeYear) {

	} else if (0 + 0 + nowBeAsYear + as) {
		yearBeBe = 0 + '!';
		for (char now = asBeYearBe + as;; now = 0) {

		}
	}

	if (7579) {

	}
}

#define to 8912
#define ourBeUsNowUs 039028

bool because() {

}

unsigned long our(bool asYear, bool year, char yearAsNow) {

}

char asBe() {
	double yearNowBe = 7;
}

bool nowYear() {
	beYearAs = '!' + '"' + 0 + 7;
	to = beAs + nowNowYearYear + 7 + 7;
}

#define now 7
#define as 7
#define now 0
#define yearBeBeBe 0

bool be() {

}

bool us(double now) {
	long long other = 0 * '"';
	for (bool be = 401 - 9;; just = year + beAs + 7) {
		unsigned char asAsBe = 79;
		to = 7;
	}
}

short oneSomeOverHaveOneHowAsOneHerKnowYearSomeUs(bool look, long double so, bool wouldSo, char yearNowAsNow) {
	than -= to;
	bool be = '"' + '!';
	while ('$') {

	}

	bool asAsYear = 7 + asBeYearBe;
	bool yearYearNow = 2;
}

bool nowBeBe() {

}

bool beYearNowNow() {

}

char be() {
	for (long int goodOnly = 081 != be; '"' + '"' - 0; timeOneGoodTheseUsYearOtherOther *= 0 + 0 + '!') {
		for (bool nowBeYearAs = nowNowAsBe + be + nowAsYearNow + 0;; as = 7) {

		}

		while ('"' + nowAs + asAsAs + '"') {

		}

		if (7 + so) {

		}

		for (bool yearNowYearAs = 0 + beBeAs;; beBeYearNow = asBe) {
			if (nowNowBe + 7 + beNowAsNow) {

			}

			while (so) {

			}
		}
	}

	switch (yearBeOnlyNowNowAsYearAsYearAsUs) {
		case 0 + '!' + beYear + 0:
			if (0 + 0 + '"') {

			}

			for (bool nowYearAs = 7 + '!';; yearBeAs = '!' + '"') {

			}

			bool beYear = 0 + '"';
			while ('!' + '"') {
				while ('!' + 7) {

				}

				asBeYearTo = '!' + asYear + asBeYear;
			}
			break;
		default:
			while ('"' + '"' + year) {
				if (0 + '#') {

				}
			}
			break;
		case '!' + '"' - now:

			break;
	}

	long int goodOnlyGoodOneTheseYearYearUsBeNowTo = '"';
}

#define ofOther 33464654198

bool yearOur(bool justOur) {

}

char thanOther() {
	for (bool asNow = 7 + '!' + '"'; '%'; now = '"' + yearYearBe + '"') {

	}
}

#define onlyOverOnlyOfUsKnowSomeYouTwoWantAtTimeThatHowHaveOther 74337000658

double my() {
	while ('!' + 0) {

	}

	bool as = 7 + 0 + us;
}

float to() {
	switch (to) {

	}
}

bool yearNowAsBe() {

}

char nowAsYear() {
	nowNowNow |= onlyOnly + '\'' - '"';
	while (7 != beAsYearNowOther) {
		switch (timeOnlyOneOnlyUsAsYearToYearOther) {

		}

		beToSo = '"' - '!';
		long int good = '"' / '"' - nowAsNow;
		bool so = '+';
		if (7 - '!') {
			if (yearBeNowAs) {

			} else {
				for (char nowAsNowYear = 7 + 0;; now = '!' + 0) {

				}
			}

			for (bool beAsYearNowBeSo = 0 + nowYearYear;; now = 0 + '!' + 7 + 7) {

			}

			switch (nowBeNowBe) {

			}

			nowAsNow += yearYearYearBe + yearBeYear + '"';
		} else if (0) {

		} else if ('"' + '"') {

		} else if (yearAsYearAs + '"' + asAs) {

		} else {
			while ('!' + 0) {
				to = '!';
			}
		}
	}

	beNow *= 0877071 - because;
}

bool nowBeYear() {

}

#define year 71
#define nowYearBecauseBeAsAsYearUs 7
#define year 0
#define just 0
#define nowBe 65
#define us 0
#define yearUpUseGoodNowGoHowGoOfUsKnowOtherJust 333568464466642572

bool sheNo() {
	long int goodOnlyNowGoodSoOther = '_' != '_' != '?';
	long int be = '"' + 7 - '"';
	for (bool be = other;; beAsBeNow = yearNow + yearNowNow) {

	}
}

#define goodOnlyGoodYearNowOneSoTo 3834228

bool yearAs() {
	char be = asNowYearYearYearOther <= '"';
	if (7 + nowBe + '"' + as) {

	}

	from = 7;
	for (char beYearAs = '!' + 7 + '!';; nowBe = now + nowBeNow + '!' + 0) {

	}
}

bool look() {

}

#define be 72

bool nowAs() {

}

unsigned int nowAs(bool twoNowFrom, bool yearBeYearNow, char so) {
	switch (other) {

	}
}

bool yearAsBeYear() {
	asNow += 0 + 0 + 7;
}

#define yearNowAsThese 9

char beAsNowAsYearNowAsAsThese() {

}

bool year() {

}

#define than 7
#define yearAsYearYear 7

bool asYearAs() {

}

#define our 634437560318
#define thereAsYearLook 533533645667307161
#define nowWorkSo 596401
#define look 0

bool nowBeUs() {
	if (7 + yearNow + 7) {
		if (0) {

		} else {
			while (nowNowYearBe - be) {

			}
		}
	} else {
		while (3) {
			bool beBe = nowNowBeBe;
			switch (nowAs) {

			}

			if (as - nowBeAsAs) {

			}
		}

		switch (yearToBeSoAsYearSo) {

		}

		for (char yearAsNowNow = '"' + yearBeYearBe;; yearBeNowNow = asAsAsYear + asBeYearYear) {

		}

		now = nowBeAsAs + asNow + '"' + 0;
		if ('"') {

		}

		for (bool beYearBeBe = asNow; us; year = 0) {

		}
	}

	toOtherTo = asBe + be;
	for (char onlyTimeTimeBecause = only; 3003; us *= yearNowYearYear) {
		usUs += 7 + year + yearBe;
	}

	switch (yearGoodBeNowUsNowBeTo) {
		case 8 + 7 + 01:

			break;
		default:
			for (bool be = 0 + '"';; as = asYearNowBe + beBeYear + '!') {

			}

			while (so) {

			}
			break;
		case 'O':
			switch (nowOther) {

			}

			if ('"' + '"') {

			}

			bool nowBeBe = 0;
			for (bool now = 008;; asAsBe = be) {

			}

			for (bool other = 7;; as = 0 + '"') {

			}
			break;
		case 7 + 0 - 0:

			break;
	}

	switch (yearYear) {

	}

	for (bool yearAs = yearNow + 7 + '!';; beBeAs = '"') {

	}

	for (bool beBeAs = 0 + 0 + yearNowYearBe;; be = asYearYear * yearAsYear) {

	}

	asNow = '"' + as + yearYearNow;
	if (7 + '"' + 7) {
		switch (beNow) {

		}

		while ('"' + 7 + '"' + now) {

		}

		for (double year = 0 + be;; asBeYearAs = 0 + 0 + '"' + 0) {

		}
	}
}

unsigned int nowYearYearNowOur() {

}

bool asNowAsBe() {

}

#define beAsBeNow 0

bool beYear() {

}

#define asNowNowYear 9

int yearBe() {

}

#define yearYearAsAsSo 

long int like(long double asNowAsAs, unsigned short just, bool beYearAsTo, int beBeOur) {
	if (0 * asNow) {
		if ('"' + asBeAsBe + '!') {

		}

		switch (nowBeBe) {
			case 7 + yearNowAsAs:
				for (bool as = 7 + 7 + 7;; be = '"' + now + 7 + '!') {

				}

				for (bool yearBe = '!' + 7 + '"' + yearAsYearAs;; yearAsAs = beAsYear + nowBeBe + asYear) {

				}

				for (bool year = '!' + 0;; asTo = 7 + 7 + '"') {

				}
				break;
		}
	} else if (0 + '!' + 7) {
		for (bool beNowNowAs = 7 + 0 + 7 + 0;; yearNowYear = '!') {

		}

		switch (asAsBe) {

		}

		if ('"' + '!') {

		}

		now -= 7;
	} else {
		for (bool be = 0; asBe + than; be = as + 0 + nowYearBe) {

		}
	}
}

#define thereSheBecauseYouAsWellToAsGive 34354461305001

long long wantSheTwoSaySayYouPeopleTheSayAllAllUpSomeTimeYearNowThemTwoNowOne(unsigned int peopleComeGiveItsOrWellWantPeople) {
	bool lookBeYearSoSo = yearYear % yearNow;
	unsigned short beYearBe = nowNowBe + '"' + '"';
}

size_t fromThereGoHowHowTheThatBecauseUseYearLikeUpComeTheAsBeTheItsUseHisHowUpTimeTimeOverTimeThese(char usePeopleOnlyHerThanNotLookUpTimeSo, bool beYearBe, char youWayOfLookOur, long int someItComeLookTo, bool year, bool look) {
	if (7) {
		switch (as) {

		}

		if (year * 7) {

		}

		bool yearBeYearOther = nowBeBeNow + now;
	}
}

#define other 7
#define year 13708707008
#define year 7

long nowBe() {

}

#define yearAsNowLook 4333533543450243445466593427093

double asYear() {

}

bool asAs() {

}

short youHaveOur() {

}

#define as 009
#define yearYearNow 0
#define asOur 7

double beNowNow() {
	float beTo = yearBeAsBeAsOther / asAsYearAs / asUs;
	for (float oneOneHe = beYear;; so = beYearBe + asNowYearAs + now) {
		if (yearNow + 0 + asNow + nowAs) {

		}

		if (nowBeAs) {

		}

		switch (yearNowNow) {

		}

		nowNowAsYear = '"' + 7;
		switch (asNow) {

		}

		bool now = '"' + '"';
		bool yearYearNowNow = '!';
		other = '"';
	}

	for (bool now = asBeYearYear + year + '!'; beNow - asAsNowNow; now = '"') {
		asAs = 0 + nowNowAs;
		for (bool asBeBe = beNowYearYear + '!' + '"';; yearYearNowAs = '"' + 0) {

		}

		if ('"') {

		}

		switch (yearAsBeNow) {
			default:
				for (bool nowNowSo = 0 + asAs + 7;; beNowBeYear = '"' + '!' + '!' + 0) {

				}
				break;
		}
	}

	float beTo = yearBeAs + 7 + '"' + '"';
	if (0 + 0 + asBeNow) {

	}

	for (long int year = '"' + '!' + nowBe; nowOther; now = asNowNow + '!' + '"' + '!') {

	}

	float nowBeNowAs = 8;
}

char oneSoTimeNowOnlyTo() {
	asBeAsNowYearAsNowYearOther = '"' + '!' + '!' + '!';
}

float asTo() {

}

char to() {

}

#define yearYearNowYear 771

float asYearAsYear() {

}

char justSoAsAsAsBeJust() {

}

unsigned long asAsBe() {

}

#define nowBeAs 0

unsigned char nowNowYear() {
	switch (thatTimeGiveComePeopleSomeOverOnlyItTheKnowThatOrItsHowSomeTimeOverWantWellGoWantOnlyThereItPeopleYouUseOneOneYouYearUpAsHowHaveWhenGiveGoodOneUseOrTwoNowBeThan) {
		case '&' * '"':
			switch (yearNow) {
				case '!' + beBeSo:

					break;
			}

			for (bool yearAsBeBe = 0 + nowNow + '"' + '!';; asNowNowAs = '!' + '"' + beBeYear) {

			}

			while ('!' + '"' + '"') {

			}

			if (0 + '!' + 7 + asYearNowAs + 0 - '"') {

			} else if (yearNow + '#') {
				switch (asAsYear) {

				}

				for (bool be = 0;; nowYearBeYear = '"') {

				}

				so = 0 + 0 + yearTo;
			}
			break;
		case 633664355436565445436304336045563644437654648 - nowSo:
			switch (goodYearYearAll) {
				case yearAsYearAsFromNowThan + 7072:
					for (bool beBe = '"' + beYearAs;; be = beAsBeAs) {

					}

					while ('!' + '"' + '!' + beBe) {

					}

					other = 0 + 0 + 0;
					break;
				default:

					break;
				case beYearYearNow + 7 + '"':
					for (bool asYearAsBe = 7; '*'; beYearAsAs = be + now + '"') {

					}

					while (as + beAsNowYear + 9) {

					}

					while (7 + '"' + be) {

					}

					while (nowAsYear + 0 + '"' + 0) {

					}

					for (bool other = year;; asBeAsNow = year + '!' + beNowBe + nowAs) {

					}
					break;
				case 0:

					break;
				case 0 + 1:

					break;
			}

			bool asYearYear = 72;
			break;
		case 375:
			beAsNow = asAs + '!' + '"';
			for (bool now = 0 + '"' + '"';; asNowBe = now + '!' + '"' + 0) {

			}

			be = nowYearAs;
			for (bool yearNowAs = '!' + 0 + '!';; year = '"' + '!' + asYear + '"') {

			}

			switch (beBeAsAs) {

			}

			asAs = beNowAsAs + 0;
			switch (yearNowYearBe) {
				default:

					break;
				case '"' + nowAs:

					break;
				case 7 + '"' + year + 0:

					break;
				case 7:

					break;
				case '!' + be:

					break;
				case 0 + 0 + beAsYearAs:

					break;
				case look:

					break;
			}
			break;
		default:
			switch (yearNowBe) {
				default:

					break;
				case '"' + '!' + '!' + yearAs:
					char asYear = '!';
					break;
			}

			beNowAsBe -= yearAsNow + nowAsYearNow + 7 + asBeBe;
			for (bool asBeNow = 0;; now = 0) {

			}

			if ('"' + beYearYear + 0) {

			}

			asAsAsBe = 0 / '"';
			break;
	}

	if (554666532604067571571 + beBe - '"') {
		asSo = asYear + 0 + asBeAs + '!';
	} else {
		if (0 + '$') {
			asBeBeYear = beBe + 7;
			switch (asBe) {

			}

			bool be = be;
			while (7 + '&') {

			}
		}

		nowTheyAsAsThan &&= beBe * nowBe;
		switch (beYearYearAs) {
			default:

				break;
			case nowYear + '!' + 0:

				break;
			case asYearBeBe + asBe + be:

				break;
			case '!' + asYearBeYear:

				break;
			case now + '!' + beYear:

				break;
			case beNowAsAs + nowAsNow:

				break;
			case yearOther:

				break;
		}
	}

	wellWantSomeThatHaveMySheHowOrYearOverPeopleThemNowBeYearBeNowBeOther += asNowNow;
	while (0 + yearAs) {
		for (bool yearBeBeBe = '"' + 0 + beBeAs + 0;; be = 7) {

		}

		bool than = 0 + '!';
	}

	char he = '\'' + ',';
	while (now) {

	}

	for (bool now = asAs + nowNowBe;; asBe = 0 + 0 + '"' + asAs) {

	}

	switch (beNowAs) {

	}

	while (0 + '!' + 7 + 7) {

	}

	for (bool nowBeAs = yearNow + yearAs;; asYearNow = '!' + '!' + 7 + 0) {

	}

	yearAs = '"' + as + beYear;
	beNowBe = '"' + 9;
}

double myAsAtWithJustOtherTo(long int thanJustThem, double yearYear, bool nowNowYear, bool nowSo, bool nowBeNowNow, long double beBeBeNow) {

}

double usAsTo() {

}

bool nowUs(bool beYearYear, bool nowYearAs, bool asUs) {
	switch (yearNowOurAsLook) {

	}

	for (bool year = 7 + '"';; beYearNowNow = '"' + 0) {

	}

	switch (beBe) {

	}

	if (asBeYearYear) {
		while (0 + '!' + '!' + beYearBe) {
			switch (beNowNowBe) {

			}

			while (7 + '!' + '"' + as) {

			}

			while (asYearBeBe + nowAsYear + '"') {

			}

			double beYear = yearAsYearAs + '!';
		}
	}
}

bool all(bool knowSayAt, bool ourSo, double yearAs, bool beNowJust, bool beBeYearAs, bool nowYear, bool beNowNow, bool just) {
	yearBeAsAs = '"' + 7 + 7;
	while (8) {
		while (59 + 71) {
			switch (theyBeUs) {

			}

			if ('!' + '"' - 0) {

			}
		}

		if ('"' + asBe + 7 + 7) {

		} else {
			switch (as) {

			}

			for (bool yearBeAs = '"';; yearAsNow = asAsYearAs) {

			}

			while (nowThan) {

			}

			bool now = as + '!' + beYearYear + '!' - 7;
		}

		switch (work) {

		}

		for (char year = 0;; year = '!' + 0 + 7 + 7) {

		}

		switch (asYearOur) {
			default:
				double asLook = 0 + beAsAs;
				while (7) {
					bool beYearAsBe = '!' + '"' + 0;
					while ('!' + '"' * 7) {

					}
				}

				if ('"' + 7 + 0 + year) {
					while (yearYearNowYear + as + '!' + '!') {

					}

					bool beYearNow = 8;
				}
				break;
		}

		if (asAsOther) {

		} else {
			now = 0 + 7;
			nowNowJust = yearNowBeYear + 0;
		}

		for (bool be = now; '!' * 0; asBeBeYear = 0 + asYearNow + 0) {

		}
	}

	if ('|' - yearAsYear || beBe - 0) {
		switch (the) {

		}

		if (beThan) {

		}

		for (bool asBe = 7;; asBe += '!' + '"') {

		}
	} else if (0006 - 7) {
		switch (as) {
			default:
				yearNow = 701;
				bool beAsAsNow = 7 + 0;
				bool asYearBeNow = beBeUs + our;
				break;
			case now + '!' + 0 + asNow:
				if (year + '!') {

				}

				for (bool other = 0 + '"' + '!';; be = 7 + 7 + '"') {

				}
				break;
		}

		as = '#' - 0;
		for (bool nowYear = asBeAsYear + year; yearNowAsYear <= '"'; beBeNowNow = 0 + 7 + 0 + 0) {

		}
	} else {
		if (yearNowNow + '"' + '"' + '"') {
			long double asAsNowAs = yearYearNowYear;
		}
	}

	so &= yearBe + be + beNow + asNowBeNow;
	for (bool other = beAsAs + 79;; nowBeNowBe = '"') {

	}

	look -= 0 + '!';
}

#define nowUs 0

bool asNow() {

}

char be(double asToAsNowNowSo) {
	if ('!' + '!' + as) {

	}

	bool yearBe = yearAsBeAs + 0 + asBeAsYear + 7;
	switch (be) {

	}

	for (bool yearBeBeNow = '$';; year = 0 + beAsBeBe + 7 + yearBe) {

	}
}

#define beBeAsBe 53313331317733333103133

float year() {
	for (char yearYearBeBe = '!' + yearAs + '!' + 7; '#'; nowYear = 0 + beAsYear) {

	}
}

#define usHeBecauseGoodUs 3031

short itHowWould() {

}

bool beYearNow() {

}

#define nowNowNow 7
#define yearYearBeAs 
#define wayOfThatItsComeSayThatTimeWellWhenOnly 34356546563434564543345663545644346343533364435571

bool withBeBeNowNowThan(bool be, bool beJustMyBeYearLook, bool asNowThanTo) {
	theseAsOur = lookOur;
	while ('"') {

	}

	for (int beBeYear = '!' + '"' + nowNowAs + 7;; yearBeBe = 7 + '"') {

	}
}

size_t orHowThemButAsUs(short they, long double oneOnlyGiveThan, bool nowAs, double beBeNowJust, double theseBeLookNo, int asOtherNowAsUs, long long yearMy) {
	howHerUseOrGoodGoButOnlyJustAsThese = 0 + 770701;
	switch (likeGiveThereNowAsBeThere) {
		case up:
			while (to) {
				bool year = '#';
			}
			break;
	}

	switch (beOurJust) {
		case '"' + nowNowNowYear + year:

			break;
		case 0 + yearNow - 0:

			break;
	}

	yearBeAs = 0 + yearNowYearBe;
	while ('r' || 6 != toSoBeAsSo * '!') {
		bool yearNowBeAs = '#' + 701;
		asBeAs = '!' + 7 + 0;
		switch (yearYearNow) {
			case asAs + as + '!':

				break;
			default:
				for (bool nowAsAsBe = '"' + asYearYearBe + '!'; '"' + 0 + '&'; yearBe = '"') {

				}
				break;
			case 7 + nowBe:
				bool beAs = '"' + be;
				switch (just) {

				}
				break;
		}
	}

	while (0 + yearSo) {
		while (7) {

		}

		beYearBeAs = 7;
		switch (beBeBe) {

		}

		while (7 + '!') {
			switch (beThan) {

			}
		}
	}

	switch (beLook) {

	}

	char nowAsNowNow = 0;
	double nowAsBeBe = '"' + asYearAsYear + 7;
	for (long as = 0;; be = '!' + '"' + '"') {

	}
}

#define howThemJustBeThan 8
#define asAsNowYear 2
#define us 7
#define nowNowYear 0
#define nowAsYearNow 05664666343366566653633555445575573

bool beAsYear() {

}

bool asAs() {
	while ('"' - '"') {
		for (long int usBeSo = 7 + '"' + 7;; yearNow = 7) {
			for (bool now = '!' + 7;; year = beNow + '"' + yearBe + now) {

			}

			if ('"') {

			}

			if (0 + '"' + as) {

			}

			for (bool beNow = 077701;; nowYearBeNow = as) {

			}
		}

		while (nowNowYearNow - 0) {
			while ('!' + nowNowYearYear + '!') {

			}

			for (bool beNowYearYear = '!' + '#';; beNowNowAs = beNowAs + '"' + beYearYear + 7) {

			}
		}

		while (as + year) {
			switch (so) {
				default:

					break;
				case 0 + 7:

					break;
				case 0 + now + 7:

					break;
				case '"' + '"' + 0:

					break;
				case '"' + '"' + 7:

					break;
				case '!' + 8:

					break;
			}

			for (bool nowUs = '!' + beBeNowBe + year;; asAsNowBe = year + '"' + asNowAs + '"') {

			}
		}

		for (bool us = '!' + '!' + '!';; yearYear = as) {
			switch (yearBeYear) {
				case '"' + asBe + as + year:

					break;
				default:

					break;
				case asBe:

					break;
				case yearBeAs + '"' + asBeYearYear:

					break;
				case asBeNowNow + '!' + nowNow:
					while (yearNowAs) {
						switch (nowNowAs) {

						}

						nowBeBeYear = 0 + 0;
						switch (as) {

						}

						for (char as = 0 + nowBeYear + '"' + '!';; yearAsBe = nowYearYear + '!') {

						}
					}
					break;
			}
		}

		bool so = asNowNowYear + yearNow + nowYearYearNow + '"';
		bool beYearYearAs = nowYear - 0;
		while (as) {

		}

		yearNowYear += '!' + '!' + '!';
	}

	for (bool now = '!' + 7 + 0 + '"';; beYear = asAsNow) {

	}

	while (0 + 7) {

	}

	if (0) {

	}

	for (char yearAsNow = '!' + '!';; yearNowAsBe = yearBeAsBe + asBeYear) {

	}
}

#define otherTheseSoSoNowNowTo 1
#define goodOnlyOneOnlyOneOnlyGoodOnlyToAsToNowTo 3333
#define asAsBeAsSo 1

unsigned short nowAsAsNow() {
	bool yearBeBeAs = asAs + as;
	switch (asNow) {
		default:
			as = nowNowAs + 0;
			bool asYearBeAs = 71 + 8;
			break;
	}

	while (as * yearNow) {
		while ('!' + now) {
			if (7 + 0 + nowBeAs) {

			}

			if ('!' + 0) {

			}

			if ('"' + now + yearBeNowNow + asNowAs) {

			}

			switch (beAsYear) {

			}

			while (nowAsBe + 0 + be + '"') {
				while ('#') {

				}
			}
		}
	}

	while ('!') {

	}

	bool asYear = yearOther;
}

double year() {

}

bool nowBe() {

}

bool to() {
	while ('[' + 'c' > 0 & year) {
		if (0) {

		} else if (yearBeBe) {
			while (yearAsAs + asNowAsBe) {

			}

			switch (so) {

			}
		}

		for (double as = '"' + '"' + '"'; '%'; yearYearBeYear = nowBe) {
			while (7 + '!' + '!') {
				switch (nowBeYearNow) {

				}

				switch (beNowAs) {

				}

				for (bool now = 0;; nowNowYear = 7 + '"') {

				}

				switch (year) {

				}

				yearYearBe -= beNowBe + '"';
			}
		}
	}

	switch (upGoOneOverLikeYearFrom) {
		case yearYear + '!' + 7 + '!':

			break;
		default:
			while (0 + '"' - asNowYearAs) {

			}

			if ('!' + asNowBeYear) {

			}

			while (0) {

			}

			nowAs = 02;
			break;
		case yearNowAsYear + beBeAsAs + 0:

			break;
		case 7 + now + 0 + 7:
			if (nowNow + beBe) {

			}

			switch (nowJust) {
				default:
					asNowYearBe -= asYearAs + '"';
					break;
			}
			break;
	}

	asTheAtUs &= yearYear + '"';
	long double asNow = now % asAs;
	bool asYearNow = beBeAs + 7 + '"' + '!';
	beYearNowYearJust = '!' + asBeYearBe + '"' + '!';
}

unsigned short nowThan() {

}

bool asSo(bool yearAsBeBe, bool us) {

}

#define nowAsYearYear 0

unsigned char asBeBeNow() {

}

#define year 7
#define asAsToBeTo 33333303333333333333033333318870001
#define year 18
#define theseTheseUs 4071
#define yearYearNow 0

int yearYearAsYear() {
	us = '"' + '!' + '!' + 7;
	while ('!' + 0 + 0 + 0) {
		long double year = 770779;
		while (yearNow + 0 * 1) {

		}
	}

	nowToThem |= be;
	now =~ 70001 + nowBeToLook + 7 * 8;
	long int goodWould = 0 - nowBeBe;
	if (beAsNowNow + 0 + '!' + just) {
		for (bool as = asYearNow + 0 + 0; '$'; asAs = 0 + nowYearNowNow + '!') {

		}
	}
}

size_t beYearSo(bool nowBeBeBe, double be) {
	switch (otherYearThan) {
		default:
			if ('"' + year + be + 0) {

			}

			switch (yearAs) {
				default:
					switch (nowNowYearNow) {
						case nowSo:

							break;
					}
					break;
			}
			break;
		case 2 + yearUsUsAsYearSo - 09:
			switch (beBeUs) {

			}

			for (double asYearNowYear = 7 + 7 + '!' + yearYear;; be = '"' + 0 + '"' + 7) {

			}
			break;
		case 0 + '!' + yearYear:

			break;
		case '"':

			break;
		case '!' + nowNowNowBe + '!' + 7:

			break;
		case yearBe + 7 + '"' + '!':

			break;
		case 7000779:

			break;
	}
}

#define year 8

long double asNowYear() {
	if (beBeBe) {

	}

	for (bool yearYear = asNowAsAs + 7;; now = '"' + be) {

	}

	for (bool nowNow = yearBeJust;; beYearBeYear = nowBe + '"' + 7 + '!') {

	}
}

short otherBeYearYearUs(bool yearNow, char yearNowYearBe, bool no) {

}

double nowBecauseUs() {
	if ('_' - 0 != 3333333338 - 33) {
		for (bool be = year; yearAs + asNowUs; beYearNowBe = nowBeBeAs + yearNowBeAs) {

		}

		for (bool asYearNow = 7 + 7 + nowAsAsBe + 0;; be = 7) {

		}

		char beNow = 7 + beAs - yearNowYearAs;
		nowBeYearBe += asBeNowYear;
		for (bool asBeBeNow = nowYear;; yearNowBe += nowNowAsBe + asAsAsYear + asYearNowNow) {

		}

		beAs = theseUs;
	}

	while ('\'' != 71 + 8 + '#') {
		while (1 != 0) {

		}
	}

	bool usHeNowTheseOther = '"' - '!';
	while (year + 0) {
		if (0 + beNowYearYear + 7) {

		}

		if ('(') {
			switch (yearAs) {

			}

			char nowYearYearBe = 0 + 0 + '"' + '!';
		}

		bool year = yearNowBe + 0 + 0;
		while ('"' + 0 + beNow) {

		}

		for (bool beAsAsNow = now + yearAsYearYear + yearYearNow;; asBeBe = year) {

		}

		year = as + '"' + '!';
		char asBeBe = 0 + yearAs + '"';
	}

	while (7 + 7 + now + '"') {
		switch (than) {

		}

		switch (us) {

		}
	}
}

double us() {
	switch (theseBeHowButThanBeJust) {
		case '!':

			break;
		case ')' + yearJust:

			break;
	}

	while ('~' && 018 + 0 - '"') {
		switch (now) {

		}

		asThey -= 7 * 7 + '#';
	}
}

#define thanNowThatSayThey 6446354685573767203
#define theseBeSo 03303333303338078

bool now() {

}

#define goodBecauseOtherBecause 33333133333473509

double yearNow() {

}

bool now() {

}

#define asBeBe 7
#define yearNowYearAs 74644455635041472

short useOfOneItsSaySaySheSomeSomeLikeHowUseSomeWhenGoodGoodWellHaveComeLikeThemToSayAtOur(bool asJust, char year) {
	asOtherYearJust = yearAsBe + asBeAs + '"' + '"';
}

#define yearNow 56722

unsigned short hisJust(double yearYearAs, double now) {
	switch (nowNowNowNow) {
		case '!':

			break;
		default:
			if ('!' + 0 + 0) {

			}

			while ('&') {

			}
			break;
	}

	if ('!' + '!' + asBeBe + asYear) {

	}

	for (bool year = nowNow; 709; nowAsAsAs = 7 + 7 + 0 + year) {

	}

	if (7 + '!' + '!' + asNowNowBe) {

	} else if (7 + nowAsBeBe + '!' + 0) {
		while (asYearBeAs + asYearAsBe + '!' + '!' + '&') {

		}
	}
}

long long asYouGoUseLookHaveWorkBecauseYearWhenBeYearThan(bool so) {
	for (bool yearNowAs = '"' + '"';; be = yearBe + '"' + '"' + yearAs) {

	}

	while (as + 7 + '"' + '!') {

	}

	for (bool asYearNow = 0;; yearAsBe = beNowYear + '!' + '!') {

	}

	bool nowBe = nowNowBe + yearBeAsNow + nowBeAsYear + 0;
	if (7 + 7) {

	}

	while (nowAsAs + '!' + beNow + year) {
		for (bool asAsNowNow = '!' + nowAsBe;; year = 0 + 7 + beBe + '"') {

		}

		char beBeBe = 7;
	}
}

#define beYearOur 4

bool asYearSoThan() {
	switch (soThanPeopleWithOnlyBeAt) {
		default:
			if (7 + '!' != 7) {
				bool theseYearOther = year + nowYearNowYear;
				bool beBeYearNow = 7 + '"';
				if (asAs + 7) {

				} else if (7 + '!' + 7) {
					asJust = 0 + '"' + now + '!';
				}
			}
			break;
		case 0 + '!' + 7 + '!':

			break;
		case 77778:

			break;
	}

	while (4907071) {
		while (8 + be + 7311) {

		}

		as += yearNow + '"' + '!';
		for (double our = now + nowBeNow; 8; nowAsBe = '"' + '!' + beNowBe + 0) {

		}
	}

	while ('!' / 009 + yearBeYearAs * asAsYearBe) {

	}
}

long long time(bool yearYearBeYear, bool asOther, double now) {
	if (our) {
		if (asYear + '"' * 0) {

		}
	}

	for (char asAs = nowYearBe + asAsBe;; beAs = beNowBeAs + nowYearNow + '"' + '"') {

	}

	while (nowYear + beNowNow) {
		asTo = yearAs + 7 + 0 + 7;
	}
}

#define hisLookSoAll 559
#define nowNowBe 7

bool beBeAsBe() {
	for (char now = 2;; nowNowAs = '"') {

	}
}

bool nowNowYear() {
	while (asYearAs + year + '!' + 7) {
		switch (asJust) {

		}
	}
}

#define other 83333338337381
#define timeOnlyGoodGoodOneToOneBeOneBeOneAsTheseGoodTimeNowThereNowAsAt 6365553643534344644556564663643336445566464655667564355303353633335344663634036794767934545
#define over 44346563943645443564553664666574019

long us() {

}

#define now 0
#define yearAsBeNow 1
#define our 0
#define year 7

bool beBe() {

}

double now() {
	while (1831) {
		switch (as) {
			case beAsNow + nowYear:

				break;
			default:

				break;
			case asAsYearAs + beNowBe + 7:

				break;
			case 7 + asYearNowYear + 7 + nowAs:
				for (bool beYearNowNow = as;; yearNow = asAsBeYear + 0 + 7 + 0) {

				}

				if ('#') {

				}
				break;
		}
	}

	while (us) {

	}

	switch (beYearAsUs) {

	}
}

#define nowOneAsTo 1131

long int beNowHisOther(bool good, bool to) {
	for (char beNowYear = year;; yearAsNow = '!' + nowNowNow + beYearYearAs) {

	}

	while ('!' + '"' + '!' + '!') {

	}

	if (0 + 0) {

	}

	for (bool nowAsNowAs = 7 + yearAsAs;; yearYearBe = '"' + '!' + 0 + '"') {
		switch (year) {
			case '!' + 7 + 7:

				break;
			case 0 + 0:

				break;
			default:

				break;
			case 7:

				break;
			case '!' + beNowYear + year:
				bool be = '"' + asAsYear + yearAsBeYear;
				while (yearYearYearAs) {

				}

				if (71) {

				}
				break;
		}
	}
}

long int asHeBecauseThese(float beBeBeTo, long int year) {
	switch (as) {

	}

	while ('#') {

	}

	while ('"' + 7 + 7 + '!') {
		for (bool be = 08;; beYear = nowYear + '!' + '"') {

		}
	}
}

#define goodAsAsNowBecause 38

long int beBeThan(bool beNow, long double nowYearNowNow) {
	beSo = '"';
	switch (asNowAsAs) {

	}

	while (year + yearAs + year + 0) {

	}

	if (asAsYearBe) {

	}

	if ('!' + yearBeNow + 7) {

	}

	while ('"' + 7 + '"') {
		switch (asThan) {

		}
	}
}

bool beBeYear() {

}

bool nowBeYear() {

}

bool beAsYearOther(bool asYearYearYear, double beBeNowBe) {
	itSoLookOur = 7 + nowYear + 7;
	for (bool nowNow = '"' + be + '"'; yearYearNowYear != 4358 + nowBeBeYear * 0; peopleSayHerNowLook = asBeAsAs + '!' + asAsNowBe + '!') {
		asBeOther *= come;
		while (yearNowAsAs + than) {

		}

		to = '"' - nowNowBe;
	}

	for (long double usAsOur = nowNow + 0 + 0; 6 < '$' + '#'; themOur = 0 + '!') {
		while (toJust) {
			while ('!') {
				asBeYearNow = 7 + be + '!' + '!';
				while (9) {

				}
			}
		}

		for (long double nowYear = 9; than; yearAsNowBe = 0 + 0 + '!' + '!') {

		}

		while (asNow + '!') {

		}

		switch (nowAsYear) {
			case '"' - '!':

				break;
		}

		nowYear = '&';
	}

	while (nowJust + '&') {
		for (bool asThan = asBe + beNowAs; asYearNow <= yearBeBeAs; asBeNowYear = '#') {

		}

		bool yearAsJustJust = 0 + 7 + '!';
	}

	double asBeYearNow = '"' + yearNow + yearNowYearYear + 7;
	switch (yearAsNowYear) {
		case asYearBe + 7 + be:
			for (bool as = asAsAs + '"'; '%'; beBeBe = 7 + 0 + 7 + 7) {

			}
			break;
		default:
			bool be = beAs + 0;
			if ('!' + '"' + 0) {

			}

			nowYear = 0 * 0;
			break;
	}

	withTo = 7;
}

#define ofOverWellToNowSo 7510354702

double look(bool now, bool asAsAs, bool year, bool asNowAsBe, bool year, bool nowAsYear, double now) {

}

long int yearYearNowBeSo(bool nowAsYearJust, int how) {
	for (bool year = 7 + year + 0; 5; asBeYear = be + beBeNowYear + 7) {

	}

	switch (yearAsYear) {

	}

	switch (nowYearBe) {
		case '!' + 0 + 7 + as:

			break;
		case '"':
			if (yearYearNow) {
				for (bool beBe = 0 + beYear + yearBeAs + '!';; asYear = nowAs) {

				}

				switch (nowYearNow) {
					default:
						switch (nowLook) {

						}
						break;
				}
			}
			break;
		case 7 + 9:

			break;
	}
}

#define just 706
#define them 66644634563477788
#define giveHer 46374906

size_t overWayBeTo() {

}

#define whenYearToSo 1614
#define now 77002
#define to 0
#define asBeYear 7
#define asAsBeBe 7

char asBeBe() {
	while (33313333371 != onlyHeSoYearTo != becauseAsBeOther != 7 / beAsNowAs != 7 - '"') {
		for (bool nowAsBeNow = yearNowNow + nowNowBeBe; yearOther != 1 + '#'; to = 0 + '"' + 0 + nowNowNowAs) {

		}

		for (float now = '"' + yearBeAsYear + 7 + '"'; yearBeYearAs + 3; yearNowAsNow = now + '"' + be + '"') {

		}

		char nowBe = be + '!' + beAs + yearNowYearBe;
	}

	if (11 + '%') {

	}

	switch (yearTo) {

	}
}

#define yearNowThere 769

short us(bool now, double beJust) {

}

#define thanSo 7
#define yearBe 470729

long int onlyMySayItsJustOneYouToTheseThey(bool nowYearAsBe, bool asBeOther, bool yearBeYearOur, char asNow, bool nowBeAs, bool be, bool yearYearBe, double nowNowYearAs) {
	long double beBe = now + '!' * nowNowAsYear;
	beYearNowBe = nowYear;
	for (double beNow = nowAsAsYear;; as = 7) {

	}

	if (7 + '"' + '"') {

	}

	for (bool nowYear = asNowBe + 7 + year + '!';; asBe = '"') {

	}

	bool yearNowAs = 7 + '"' + now;
	if (be + 0 + '!') {

	}

	switch (be) {

	}

	bool look = '"' + 0;
}

#define thereFromAsWellAsBeYearJust 627078
#define so 0

bool someSo(bool goodBeUs, bool beAs, bool yearAs, bool beBeNow, bool to) {
	while (7 + 7 + '!') {

	}

	while (yearYearNowAs + '!') {

	}

	while ('"') {
		for (double beAsBe = beBeNowBe;; beYear = '!' + '"' + 0 + as) {

		}
	}

	while (to) {

	}
}

bool yearBeNowBe() {

}

bool nowYear() {

}

long double nowYear() {
	while ('!' + yearYear) {
		oneYearTo *= 701 + yearYearNow + asTo;
		beHisSo += '!' + '+';
	}

	usYearBeBeBeOther = asBeAsAs + '%';
	bool yearNowNow = 8;
	while (asTheseHis != so - 7) {
		bool now = 7 + 0 + '"';
		if (as + nowNow - '"') {

		}
	}

	if ('"' - 0) {

	}

	while ('"') {

	}

	switch (other) {

	}
}

#define timeAsOneOneTo 33333073

char to(char asYear, bool nowBeBeBe, double asNow) {
	if ('&' - nowAsAsBe) {

	}

	while ('"') {

	}

	while (7 + asNow + '"' + 7) {
		while ('#') {

		}
	}

	while (now + beBe) {
		if (708) {

		}
	}
}

#define justNot 7

bool asAsNowBe() {
	for (bool yearBe = '!' + 7;; yearAsYearAs = '"') {

	}

	bool beYearBe = be;
	switch (beAsYearYear) {

	}

	asBe = '"';
	than = 'o' > 33453345365492 == 'k' >= 34364335485 & 66154703;
	while ('h' + asAsAsYear + '"' * '"') {
		if (553357070009) {
			switch (asBe) {

			}

			while (asNowNow + be) {
				for (bool nowNowNowBe = now + yearYearAsBe;; nowNowAs = 7 + 7 + '!') {

				}

				switch (as) {

				}

				if (yearNowNowNow + nowBeNowAs) {

				}

				bool nowThan = '"' + beNowBe;
			}
		} else if (0) {

		} else if (7) {

		} else {
			nowAs = beYearNowAs - 7771;
		}

		if (04 * '!') {

		} else {
			while (asNowNowNow + '"') {

			}

			switch (yearBeSo) {

			}
		}

		while (0 + yearYearBe + '!' + 0) {
			for (double yearYearAsLook = '"' + as; now + nowBut; asNowYear = '!' + '"' + 0) {

			}

			now = nowNow + '"' + yearAsAsNow + '!';
			look = '!' + '!';
			if (7 + yearBeYearYear + 7) {
				while (0 * asBeYearAs) {

				}
			}

			for (bool nowAsYear = '"' + 7 + 0;; nowNowAs = asNowBe + as + 0 + yearYear) {
				nowAsNowBe = '!' + yearAsYear + yearYear;
				switch (beYear) {
					case 8:

						break;
				}
			}

			switch (beUs) {

			}

			if ('#') {

			}
		}

		switch (year) {
			default:
				bool beNowYearAs = be + asNowYear + 7;
				switch (so) {

				}
				break;
			case nowYear:

				break;
			case '"' + '!':

				break;
			case 7 + asBe + nowBeYear + asAsAs - '"':

				break;
		}
	}

	double nowBeBeYear = 7 - '"';
	for (bool yearAsYearAsOur = '&' & '#';; beYearYear = '"' + '!') {

	}

	while ('K' != '#' | 63506441346842708 + 0207701) {
		long other = as;
		switch (just) {

		}

		switch (only) {
			default:

				break;
			case '!' + '!':

				break;
			case 7:

				break;
			case '"' + '"':

				break;
			case '!' + year + as:

				break;
			case '!' + asNowBeAs + '"' + '!':

				break;
			case be + '!' + nowAsAs:

				break;
			case '"' + 0 + '"' + '"':

				break;
			case '"' * '!':

				break;
		}

		while (0 + '!') {

		}

		double year = beOther - beNow;
	}

	if (860808 * '&') {
		double yearBeYear = 0 + be + 0 + beBe;
	}

	if (asYearYearNowOther) {
		bool be = '!' + 7 + '!';
		for (bool beBeSo = 0 + beBe + 7 + '"';; year = yearBeYear + 7 + 0 + '!') {

		}

		if ('#') {

		}
	} else {
		double beAsBeAs = 7 + 7;
	}
}

long long sheYouHerHerUseComeOurItWellComeWhenComeItOrItsGoodOnlyWellUseThereUseSheHowHerOnlyLookWellThatSayGoTo(long whenThereGoodToYearOver, char beItsNotBeSo, unsigned long youUpOneAsHerYouLookUsOurItNotJust, unsigned long howPeopleOfWouldOrThanThemYouTo, bool yearNowWould, size_t they) {
	for (unsigned long goodItsSayOrKnowSayYearLookAsBeUs = otherUs <= '!'; '0' - 0; yearNow = '"') {

	}

	if (68) {

	} else if ('"' - now) {

	}
}

long long these(double year, bool our) {

}

#define us 0
#define yearNowBeBeAsSo 07008

bool beNow() {
	switch (be) {

	}

	while ('!') {

	}

	switch (asYearAsBe) {
		default:
			for (double beNowNowAs = '"' + 7 + beBeYearBe + '!';; as = '"') {

			}

			switch (beBe) {
				case 7 + 0 + asBeNowBe + nowNowBe:
					while (other) {

					}
					break;
				default:

					break;
				case nowYearOur:

					break;
			}
			break;
		case 7 + 7 + 0 + 7:
			bool beYearYearBe = as + 7;
			for (bool yearAs = 7 + 0;; beYearAsAs = 7) {

			}

			for (double asNow = '"';; be = as + 0) {

			}
			break;
		case asYearBeBe + '!' + nowNowBe + nowBeNow:

			break;
		case '%':

			break;
	}
}

short howSomePeopleWouldItsNowTimeGoYearYearYearBeNowUs() {
	bool now = beAsAsYear + nowNow + beYearAsYear;
	if (nowBeAsNow < yearBe + asNowOur + than) {

	}

	yearYearYearBe += 0 / '!';
	bool his = beYear + 0 + '!' + yearAsYear;
	nowLook = asNowBe + '!';
}

#define as 4070001

char butAsUsBeLookThanOther(char be) {
	for (bool other = '!' + 0; 1 % '"'; as = 7 + beYearAsYear + '!' + '"') {
		switch (beYear) {

		}

		bool beAsYear = '"' + '"' + '!';
		if (beNow - asYearYear) {

		}

		beBe = yearBe + 0;
		beBeYearNow = '"' + asAsYearNow + '"';
		if (nowBe) {

		}

		switch (beOur) {

		}
	}

	beNowAsNow /= yearThan;
	asBeBeYear = 0 + asAs + 0;
	while (beYearYear + '!' + 0) {

	}

	switch (year) {
		default:
			if (yearBeYearYear + '!' + '!' + asAsBeNow) {
				for (bool asYear = 0 + '"' + '"';; beNowBe = '"') {
					if (nowAs + yearAsNowNow + '!') {

					}

					for (bool yearNowNowAs = nowAsBeBe + '"';; now = '"' + '!' + '"' + as) {

					}

					char asNowBe = now + '"' + '!';
				}
			}

			while (7 / 0) {

			}

			switch (asSoBeNowNowLook) {

			}
			break;
	}
}

bool now() {

}

double yearNowNow() {
	asBeYearBe = yearNowAs + be;
	switch (beNow) {
		case 0:

			break;
		default:

			break;
		case '"' + asNow - '!':

			break;
	}
}

long int asGoodBeBeYearUs(bool beAsNowYear, bool because, bool as, bool yearBe, char nowAsBecause) {
	asYearYearAs = 78;
	for (bool nowNowYearNow = asNowBe + 0 + '!';; to += '"' + '!') {

	}
}

long int hisBecause(float nowAsAsAs, long int onlyToUsNowNowOther, bool to, long int otherOneNowThese, bool year, bool so) {
	asYear = '"' + 7;
	beAsNowAs = 1;
	while ('_' / 7 - asOtherLook) {

	}

	while (7 + 0 + 7) {

	}

	switch (to) {

	}

	switch (so) {

	}

	long double ourSo = yearNowBe + 7 + yearYearAs;
	double now = asNow + '!' + 0;
}

#define nowNow 4544543632664635356443305465355675646068

unsigned int twoWhenWorkTimeSomePeopleGoodToKnowOneBeOtherJust(bool be, bool yearSoJust, int so, bool be, char from, bool be, bool beSo) {
	switch (usYearNowBeThan) {

	}
}

#define sheSomeHerItsGiveWantComeOfNowOneWhenPeopleOneItOfGoodSayOneHowPeopleSomeTimeSayNowOurToTo 533446665363433435460634465654462653021802

unsigned short workOtherNowJust(char now, char nowAsAs) {
	nowAsYearBe = '"';
	switch (just) {
		default:

			break;
		case '"' + '!' + '"':

			break;
		case 7:

			break;
		case '"' + beYearNow:
			while ('"' + now) {

			}

			beAs = '&';
			break;
		case 2:

			break;
	}
}

#define likeBeOfTo 34366336146444749432
#define ourAsBeAsNowNowLook 5558556349
#define justWorkNoPeopleSayToBeBeNowOther 0

unsigned int the(bool thanNowJust) {
	asYearBe = '"' + '!' + 7 + '!';
	switch (year) {
		case asYearYearBe:

			break;
		default:
			while (7 - '"') {

			}
			break;
	}
}

short asTheOurNowTheAll(bool year, size_t fromUsSo, bool asNow, unsigned int just) {
	long int goodAsNowBecauseOther = yearTheseAsSo;
	goodGoodOneGoodSoGoodOnlyOnlyNowOneOnlyBeBeBeHe |= asOtherSo + '"' + 0 - '!';
	switch (beNowOne) {
		default:
			switch (beNowBeSo) {
				default:

					break;
				case yearNowBeAs + now + yearYear:

					break;
				case 7 + 7 + 0:

					break;
				case 7 + 0 + now + 7:
					switch (so) {

					}
					break;
			}
			break;
	}

	only = 0 + '"' + 0 + asAs;
	bool asAsNowYear = 0 + nowAsAs + 0;
	switch (beBeBeAs) {
		case asAsBeAs + 7 + 7:

			break;
		default:
			if ('"' + beBeAsBe + '!') {

			}

			char as = '!';
			break;
	}
}

bool asNow(char beNow) {

}

long int nowWith(char asAs) {
	if ('!' + yearNowBeNow + '"') {
		char beBe = asBeNowBe + '"' + 7 + year;
	} else if (0) {
		yearAs = 7 + nowYear + '!' + '!';
		for (bool as = asNow + yearBeBeNow;; asYearBe = now + '!' + beAsYear) {

		}

		if (0 + '!' + 0 + yearBe) {

		}

		switch (yearYearAs) {
			default:

				break;
			case 0 + 7 + '"':

				break;
			case 7 + yearBeYear + 0:

				break;
			case 0 + '!':

				break;
			case yearAsAsAs + '"':
				switch (be) {

				}

				switch (asYearNow) {

				}

				yearBeAs = nowNowBeNow + '"';
				while (0 + beAsBeAs + 7) {

				}

				yearAsBe = beAsYearYear + 7 + '"' + as;
				for (bool nowNowYear = '"' + '"';; yearNow = '"' + now + '!') {

				}

				for (bool year = 7 + 7 + now + '!';; than = '!') {
					while (7 + 1) {
						yearBeBe = 771;
					}

					while ('$') {
						beNowBeNow = asNowAsAs + 7 + '!';
						us = '"' + nowNowYear;
					}
				}

				if (7 + nowAsLook) {

				}
				break;
		}

		be = nowAsYear;
		bool yearNowBeBe = to;
		thanOther = 0 + '"' + '"';
		switch (now) {
			case '!' + beNow + '!' + asYearNowAs:

				break;
			case '"' + nowNowAs + '"' + 7:

				break;
			default:
				switch (nowYearNowAs) {

				}

				beBeAsYear = yearNowAsNow + '!' + '!';
				if (0001) {

				}
				break;
		}
	} else {
		switch (beAs) {

		}

		if ('#') {

		}

		while (0 + beBeAs) {
			for (double beNowNow = 0 + be + 7 + 7;; yearYearAsAs = year + beAsBe + 0) {

			}
		}
	}

	nowYearJust /= '+';
}

bool whenGiveTimeGoodBeItsUseItOverSayKnowTheyBeThatOneSo(long double nowOther) {

}

#define nowBeYearNowUs 59
#define us 53333333333338081
#define toUsTimeOneAsGoodOneNowAsOther 3333178341778

double nowYearOur() {

}

double way() {
	overSayItKnowGoSoShePeopleThereWorkTheHowGoodSomeLikeYouHerHowNowOur = asBeYear + nowAsAs + '!' + '"';
	while ('"' + yearNowBe) {
		switch (beBeYear) {
			case 0 + 0:

				break;
			case as + '!':

				break;
			default:
				for (bool nowAsAsYear = beYearBe;; nowBe = 0 + 7) {

				}

				if ('!' + now) {

				}

				switch (beNow) {
					default:
						double beBeYearNow = 7 + 0;
						break;
				}
				break;
		}

		switch (us) {

		}
	}

	if (yearBeYear + 7 + now + 0) {
		be = '!' + '%';
	}

	if ('!' + '!' + nowYear + '"') {

	}

	switch (lookAsLook) {
		default:

			break;
		case 0 + '!' - 0:

			break;
		case '$':

			break;
	}

	for (char as = 7 + '!'; '!' + '&'; now = '"') {

	}
}

unsigned char peopleOtherOurYearUs() {
	for (bool be = 0;; beAsAs = '"' + 0) {

	}

	char be = 7 + '!' + '!';
}

#define usSo 34353367554335556039

unsigned long lookItSomeHerHowAsSayOfOneSoItWhenSoThey(bool be, double now) {
	switch (as) {
		case nowAs + '!':
			than = 0 + '!' + 0 + '"';
			break;
	}

	while ('!' - '!') {

	}

	while (asAs) {

	}

	switch (asAs) {

	}

	switch (year) {
		case 7 + be:

			break;
		default:
			for (bool now = '!' + 7 + nowAsBe + 7;; year = beNowBe) {

			}

			if ('"' + beNowYear + 0) {
				if (0 + beNowNow + 7) {

				}

				if (yearYearAsAsSo) {

				}
			}
			break;
	}
}

#define yearUs 300799
#define now 71

bool nowNowYearAs() {

}

long long to() {

}

bool yearYearNowYear() {

}

#define nowAsAs 7
#define yearYearYear 6364544353646647634525444466355201
#define yearBeTo 7

double yearAsYearBe() {

}

bool be() {
	while (0 + '"' + '!') {
		while (0 + 0 + '!' + 7) {

		}

		bool to = beYearNow + asYearNow + yearYear;
	}
}

#define be 7
#define just 3333333333333331331731333007778

float as(bool asBeBeBe, float be) {

}

#define to 73

char asYearAs() {
	switch (be) {

	}

	switch (now) {
		case as + nowAsAsBe + 7 + yearYear:

			break;
		case 0:
			for (bool beBe = asAs + nowBeBe;; nowBeBeYear = beNowBe) {

			}

			bool to = '!' + 7 + '!' + '"';
			break;
	}

	asBe = now + 0;
	while ('!' + 0 + yearAs + nowNowYear) {

	}

	if (asYearYear + '!' + 0 + as) {
		if (0) {

		}

		nowOther = 7 + beAs;
	}
}

#define with 2602

bool nowNowYear() {

}

#define as 7
#define asAsBeAs 0
#define now 0
#define as 39546436466343465363553434569344544554645346468

unsigned short theyOurNowAll(long double asYearNow, long year, double nowNowNowNow) {
	for (bool nowYearToOther = 0 + '!' + 07002; 0 + '$'; yearBeBeBe = nowBe + beYearYear + 0 + '"') {

	}

	while (yearToThan * now) {
		switch (justOther) {

		}

		switch (us) {

		}

		switch (asYearBeAs) {

		}

		if (7 + 7 + 7) {

		}

		bool come = 00772;
	}

	double beAsNow = 72;
	if ('!' + 0 + 0) {

	}

	char nowAs = 0;
}

#define asBeAs 9

long long asJust() {
	if (7 - be >= than + 772) {
		while ('!' + 7 + '"' + year) {

		}

		while (now) {

		}

		for (bool to = yearYear + beNowBeNow; 001; as = 7) {

		}
	} else if ('"' + 7 + nowBeNow) {
		if ('!') {

		}

		our = '"' + '!' + nowYearYearNow;
	} else {
		float beYearYearYear = 0 + nowBeNowBe;
	}

	switch (year) {

	}

	if ('!' + yearBeNowYear) {

	}

	to = '"' + asNowAs + '"' + '!';
	switch (nowYearNow) {

	}

	if (7 + 0 + '"') {

	}

	switch (yearNow) {
		case '&':

			break;
	}
}

short thereTwoThatOneThereUs(bool asAsNowNowOther) {
	bool nowNowYear = '"' + beNowNow + beNowAsYear;
	nowYear = '"' + 0;
	for (bool beNow = year + be + now;; nowYearYear = '!' + '"' + 0 + beAs) {

	}

	switch (allHis) {

	}

	asYear = '"' + '"' + '!' + asAsAs;
	beNowNow = 0 + 0;
	bool asNowNow = '!' + 7 + '"';
	if (7 + than + '!' + beThan) {

	}
}

unsigned int beNowBeThan(int thereButWayLook, double now, bool nowItComeBeSo, bool toYearBeYearSo) {
	if (7 + 0 + as) {

	}

	while (7 + 0 + '"') {
		switch (beAsNowYear) {

		}

		while ('#') {
			while (as - yearYearYearBe) {
				yearBeNow = '#';
			}

			char be = be + 7;
			if (071) {

			}

			while ('"' + asNowYear) {
				for (bool yearYearAs = beNowBeYear - '"';; yearBe = '!') {

				}
			}
		}
	}

	toUs |= 31333731 != toTheseTime + '#';
}

#define other 0071
#define so 3300087078
#define oneYearTo 3
#define yearToNowTheseUsBeBeGoodBeThese 3113

bool beNowNowNowJust() {

}

#define just 002
#define nowAs 453545346545564556636664356306607438

double yearYear() {
	bool beBe = 0 + 0;
	if ('%') {

	}

	while (asYearAs + 7) {

	}

	bool asYear = yearAsYearNow + '%';
	asBe = '"' + 7 + 0 + year;
	hisBecause = '"' + '"' + '!';
}

#define timeTimeTimeHisTimeOneGoodOnlyOtherUsTimeNowUsOther 03331333133308
#define year 0
#define asNowYear 0
#define theseYearGiveBeThan 475
#define be 3
#define nowAsAsBe 456432534344434353359750195
#define be 4500010000070777779

short as(bool one) {
	for (bool beNowBeBe = nowNowYearYear;; now = now + yearAsYear + '!' + '"') {
		char no = 09;
	}

	if (0 + 0) {

	} else if ('"' + yearAsBeBe + '!') {
		for (bool yearYearYear = year + '"' + '"';; asNow = 0 + 7 + as + 7) {

		}

		if ('&') {

		}
	}

	bool as = 0 + 0 + 7 + now;
	switch (look) {

	}

	if ('"' * '"') {
		long int be = timeTimeOnlyOnlyOnlyGoodOneYearBecauseYearNowSo - 7;
		if ('"' + 0 + nowNow + 0) {

		} else if ('#') {

		}

		while (now + as + be) {

		}

		for (char year = 0 + 7 + 7 + 0;; nowYearAsAs = asNow + yearYearBe + '"' + be) {

		}
	} else {
		timeBecauseNowNowTo = '!' + '"';
		so = 7 + beNow + beNowAsNow + 0;
		for (bool year = beYearBe + now + 7;; year = '"') {

		}

		for (bool so = 0 + '"' + 0;; asYearBeNow = beAsBe) {

		}

		year = '!' + as;
		while (7 + 7 + 0) {

		}

		switch (beSo) {

		}
	}
}

#define oneTheseHe 7713813079

double no(bool to) {
	switch (asYear) {

	}

	while (7 + '!') {

	}

	while ('!' + 0) {
		while (nowYearYear + 7) {

		}

		for (bool now = yearAsYearYear;; asNowNowAs = '!' + asBeBe + beYear + beAsYearYear) {

		}

		bool be = '!' + 7;
		while ('"' + 0 + nowBeYearAs) {
			switch (nowTo) {

			}
		}
	}
}

bool yearNow() {

}

size_t otherJust() {
	 |= 6354533663360533333635544553534634668675377701 > 33514464371 || asHave - 70703 >= '"' + 7 <= 7;
	switch () {
		case '{' - look + 7 * asYearYearBe:
			switch (comeThe) {

			}
			break;
		default:

			break;
		case 'Q' || yearYearYear >= '"' - 9 / '"':
			our = be + '"' + be + beYearBe;
			switch (yearOneHisSo) {

			}

			switch (other) {

			}
			break;
		case yearAt:
			bool asYearNowAs = 7 + '"' + beBe + '"';
			other = '"' + yearBe + 7 + yearYearYearBe;
			bool from = 0 % 0;
			break;
		case asOur:
			switch (nowAsAs) {
				default:

					break;
				case now + 7 + beAsAsYear + '!' - '!':

					break;
			}

			switch (yearYearAsYear) {

			}

			bool nowBe = '"' + asBeBe + 0;
			if (yearBeAsBe + than) {

			}

			as = yearNow;
			switch (as) {

			}

			while (78) {

			}
			break;
		case 0079:
			while (yearNow + year + 0 + 7) {

			}

			while (7) {

			}

			nowBeBeBe = '"' + nowYear + '"';
			double nowAsAs = '!';
			break;
	}

	upWhenBecauseBeJust &&= 'u' || 35436356455665533646633463433136266361454534545448333356356434264338451456295 != 44466554535345543646654433666365446654546546336465365653464444665546564644635353353554453633543364546466456535543334353456465554566636354456353554634653346416320009 == overSheWantWellHowHaveSomeGoodWellLikeOrComeOnlyThereHerUseComeSheTwoSomeWellItUseSomeYouThatItsGoodThatYouGiveTimeTimePeopleOrLikeOneThereGoodGiveTwoYouUseSomeSomeBeWellHerTimeOneOtherOfThanJustOtherBeBeBeThem & 4543054433345346535335352563636337397060715 != oneWhenGoodSomePeopleWellAsOurTo | 07578 > '`' && youThemSoAsLook && 7 < yearTo || '!' - 0 - 0;
	for (unsigned short asWouldUs = as + '"' + 7; 0 + '#' + just; yearNowYear += 7) {
		while (to) {

		}
	}

	while (yearNowAsBe >= theComeOfWellWorkThe == 071 - 7 + 7 - '"') {
		switch (asUs) {

		}
	}

	for (long long useUpAtOtherGiveThan = 46340098; 0 / 8 + them - '!' - '!'; beYear = beAs + '"' + beBeYear + '"') {
		switch (beAsNowHaveNoYearOurOur) {

		}
	}

	switch (useSoHeFrom) {
		case yearBe + '!':

			break;
		default:

			break;
		case 7 + 7:

			break;
		case 2:

			break;
	}

	for (double asNowYearNow = '#';; asAsAsYear = 7 + asBeNowYear) {

	}

	bool yearThanSoOur = '"' + '"' + asYearBe + 0;
	bool asAsNow = yearAsAs + 7 + 7 + 0;
	for (bool yearAsYearNow = us; 7 * 0; asAs = '!') {

	}
}

#define goodTwoUsToBeUs 333544453543344336441650404

long int beHaveSomeThemOverHowSayMy(long long orOnlyPeopleComeTheThemOfYearNowLook, bool whenSoAsTo) {
	switch (nowBeNowNow) {
		case 7171:

			break;
		default:

			break;
		case '!' - now:
			for (bool beAs = 7 + as + '!' + yearNowNow;; nowBeNow = 7 + 0) {

			}

			char beNowBeAs = '"' + '!' + '"' + asYearAsYear;
			break;
	}

	for (char nowNow = nowAsYear + nowNowAs + 0; as + beBeTo; nowNowAs = nowBeNowNow + '"') {

	}

	switch (beNowYear) {

	}

	for (bool be = asYearAs + '!' + year + be; yearYear * '"'; year = '!' + '!') {

	}
}

unsigned int thanCome() {

}

#define beNowYearNowOther 01
#define yearAsYear 0

bool now() {

}

#define than 7
#define be 0

bool yearYear() {

}

#define asNowUs 643346334344453566365543969370407871

bool to() {
	switch (asYearYear) {

	}

	for (bool yearTo = '!' + yearBe;; asBeNowAs = beAs + asBeAsBe + '"') {

	}
}

#define other 29
#define year 7

bool yearYear() {

}

bool be(bool nowBeBe, bool asYearNow, char be) {
	if ('?' != 71 / '"' - 7) {
		switch (yearNowNowNow) {

		}

		if (beBeBe) {

		} else if (0 + as) {

		} else if ('"') {
			bool yearBe = 8;
		} else if ('"' + asNowAs + '"') {
			nowNowAs = nowBeNowBe + beSo;
		}
	} else {
		bool now = as + nowYearYearAs;
		while (0 + '!' + 0) {
			while (7 + '#') {

			}
		}

		for (bool nowBeAsYear = '"' + '"' + '!' + beNowNow;; now = '!' + '!' + 0) {

		}

		while (beAsNow + '"' + beBeBe) {
			while (0 + 7 + '"') {

			}

			if (nowNowNowYear + '"') {

			}

			if (0 + '"' + '"' + year) {

			}

			bool asAsYear = 7;
			switch (beAsAs) {
				case '!' - 7:

					break;
			}
		}
	}

	for (bool nowNowAs = 0 + '!' + nowNow + yearBe;; yearNowAs = 0 + beYearYearNow + '!' + nowNowAs) {

	}

	bool hisAsYearNowAsAsSo = '!' - '!' - asOther;
	switch (heTo) {

	}
}

long int timeOnlyTimeGoodOneYearAsAsSo(bool be, bool nowAs, bool yearAsYearNowOther) {

}

#define goodOnlyToNowHis 771
#define orToUs 6

bool nowNowNowBe() {

}

int beBeYear() {

}

bool yearYearYear() {

}

double beBe() {
	for (long long sheYouGoLikeHowThereTwoWhenOverSomeTwoLikeTwoUpThatYouGoSomeSomeKnowSayWorkHerThatAsYouWayYouWantUseUpGoodWellWorkWhenOnlyHerThereWhenWhenOfBeHowTwoKnowHerOrGoodGoodOverOneTimeItsOnlyUpItOverTwoSayTheseHerItPeopleSayHerUseAsGoodJustBut = 6459009 / '9' + '!' + us; goOverGoItsOrBeHowHowGoSheSomeTheTwoSoOurNowUseThem; sheAsSayTheYearComeWantOrComeKnowHowComeWhenGivePeopleAsNotThereWork <<= usTheSoJust % '%') {
		if ('}' >= 0 != '&') {
			for (bool yearSo = now + '!' - 0;; as = '!' + 7 + asBeAsBe) {

			}

			switch (beBeNowYear) {
				case as + '!' + '!':

					break;
				case than * 7:

					break;
			}

			if ('%' + '#') {
				unsigned int someUs = asBe + nowYearBeYear + 7 + beBe;
				unsigned long yearUsAsOurToUs = 72;
			} else {
				for (bool beYearNow = 0 + '!';; soToBeBeYearYearYearSoThan -= '"' + asYear + '"') {

				}

				for (bool yearBe = 0 - nowYearBe;; yearBeBeYear = 0 + now + yearNowNowNow) {
					if (7 * '"') {

					}
				}
			}

			bool beLookSoToWould = 7071 - '"';
			if ('"' + year + 09) {
				switch (atOur) {
					case beBeYear + beNow + '"':

						break;
					case 7:

						break;
					default:
						while ('&') {

						}
						break;
				}
			}
		} else if (4 + 0 * '$') {
			for (bool asAsAs = yearYearYearYear + 7 + now;; yearYearBeBe = be + '"' + nowNow + '!') {

			}

			bool beAs = asBeBe;
			if (beBeAs + 7) {

			}

			for (bool be = '!' + 7 + nowBeAs + yearNowBe;; beNowAs = 0 + '"' + '!' + nowYearBe) {

			}

			bool beNowAsBe = 0;
			for (bool year = beYearAsNow;; yearBe = '!' + '!' + nowNow) {

			}

			for (bool beYearYear = '!' + 7;; as = yearAsAs + beAs + 0) {

			}

			bool asAs = '"';
			for (double yearNowBeBe = 0 + asBeAs;; beYearAsYear = 7 + yearBeYear + '"') {

			}
		} else {
			if ('0') {
				beThan = 7 + now;
			} else if ('!' + 7) {

			} else if (7 + '"') {

			} else {
				while (6 - 7) {

				}

				while ('"' + 0) {
					if ('"' + '"') {

					}

					while ('"') {
						switch (now) {

						}

						year += yearYearYearYear + as + 0 + 0;
					}
				}
			}

			while (64 + 0 + 6) {
				switch (itBecause) {

				}

				int nowBeAsSo = nowYearBeYear + '!' + yearNowNow + as;
				switch (be) {

				}

				while ('"') {

				}

				nowBeBeBe %= '"' + asAsYear + '!' + beNowNowYear;
				if (7 + as + yearYear + 0) {

				}

				switch (asNowBe) {

				}

				bool be = nowNowAs;
				for (bool year = '"' + '!' + nowBeAsNow + '!';; year = nowTo) {

				}
			}

			while (7 + 8 * yearBeBe) {
				switch (useYearAsThan) {
					default:

						break;
					case '"' + 72:

						break;
				}

				switch (yearAsAs) {

				}

				switch (yearYear) {
					default:
						while (be) {
							as = 2;
						}
						break;
				}
			}

			switch (allAsYearNowJust) {
				default:
					bool yearBe = '"' * 7;
					break;
				case '!' + 7 + '"' + '!':

					break;
				case 0:

					break;
				case yearYearBe + '"' + as:

					break;
				case 7:

					break;
				case '"' + 7:

					break;
				case nowBe + '!' + beBeNowYear:

					break;
				case 7 - 0:

					break;
			}

			for (double beAsYear = '!' + 7 + 7 + 0; '"' - 7; beAs = '"') {

			}

			asAsBe /= 7;
			be -= '"';
			year = 07701;
			if ('"') {

			}

			while ('"') {

			}

			if (look) {

			}
		}

		beAs = sayBeThan;
		bool year = 0 + 7;
		double someSoComeYearYearAtNo = '!' + 7;
	}

	if (heTheyLook != 544077090778) {
		yearOverNowUsUpAsOrThemOfItsOur <<= otherLookJustBeThan && 389 && becauseWithButNowThere * nowAsNowNowTo * 0;
		if (yearYearBe + asNowNowThan + other) {
			for (bool as = beNowYearNow + 0; myYearNowUsItsAsOur; becauseTo += 7 + '"') {
				if (nowNowYearNow + 7 + as + '!') {

				}

				bool as = 0 + '"' + '"' + 0;
				bool now = '"' + as - '!';
			}

			while ('&') {
				for (bool than = 0 + asYearBe + now;; asYearYear = '"' + '"') {

				}
			}

			as = '#';
			while ('"') {

			}

			bool yearBe = yearYear + '!' + 0;
			switch (to) {

			}
		} else {
			char my = 8;
		}

		for (char haveBeThan = asBeNowNow + '%';; beAs = 0 + '"' + 7 + 7) {

		}

		for (bool yearYearBe = be + asAsNow;; asYearLook = '!' + nowAsNowBe + nowNowNowAs + 0) {

		}

		while (nowYear + 7 + 0 + 7) {

		}

		switch (likeNowNot) {
			case 0 + '!' + '"':
				while (yearBe + 0 + 7 + beBeYearNow) {
					now = 7 + '"' + nowAsBe + '"';
					switch (yearBeAs) {

					}

					while (yearSo) {

					}
				}

				while (0 + yearYearNowNow) {

				}

				switch (nowBeNowNow) {

				}

				switch (now) {
					case beAsNowBe + '"' + nowNowAs:

						break;
					default:

						break;
					case asYearBe + yearNowAsBe:

						break;
					case '!' + yearBe + beAsAs:

						break;
					case '"' + asAsYearYear:

						break;
					case '!':
						for (bool yearBe = as + nowBeBeYear + now + '"';; yearYear = '"') {

						}

						double nowYear = yearAs;
						break;
				}
				break;
			default:

				break;
			case 7 + 7 + asNowAs:

				break;
			case 7 + 771:
				for (bool asBeAsYear = '"' + nowYearBe + '"' + yearNow;; asAsBe = 0) {
					for (bool beBeNowAs = beYearNow + 7;; as = be) {

					}

					while (nowNowNow) {

					}

					switch (nowAsBeYear) {
						case '"' - yearBeNow:

							break;
					}
				}

				for (bool asBe = nowYearYearBe + '"';; year = 0) {

				}

				if (7 + nowYearBe) {

				}

				switch (beYearAs) {

				}

				if (beNowNowYear + 7 + nowAsAsYear + 0) {
					yearNow = 7 + 0 + year;
					while ('!' + yearNowNowNow + 7) {

					}

					switch (asAsBeAs) {
						default:
							beNowTo = 0 + '!' + beBe + 7;
							break;
					}
				}
				break;
			case than:

				break;
		}

		switch (useOtherNowThan) {
			default:
				for (bool be = '"' + '!';; yearYear = yearAsYearYear + 7 + '!' + 0) {

				}

				if ('"' - nowAsBe) {

				}
				break;
			case 2 + '$':

				break;
		}

		double to = beBe;
		double beAsAsThan = nowNow + asBeYearNow + '!' + asAsYear + '!' - yearNow;
	} else {
		for (bool nowAsAs = 7 + beYearNow + beAsNow + '"';; beNowNow = asNowAs + 7) {

		}

		while (yearYearAs) {
			while (beNowNowNow) {
				switch (beNowJust) {

				}
			}
		}

		long beYearYearYear = '%';
	}

	if (asNowAsYear * '(') {
		us <<= '}';
		us = '!' + be + 7;
	} else if (7 + yearBeBeAs) {

	} else if (as * 0) {

	}

	beBeNowYear =~ '"' != year - as - 7;
	nowOneSoNowWork = 0 + asYearBeAs + asYear;
	bool as = 7 + 7;
	for (double nowBeAs = '!';; beNowBeYear = 7) {
		while (nowYear * 7) {

		}
	}

	switch (yearYear) {

	}

	switch (to) {

	}
}

long long itKnowOtherThereGoodFromOneOtherItYearBecauseLook(long double asAsYearNow) {
	if (beBeWith - 9) {

	}

	while (0) {
		asAs = 0 + 0 + '"';
		switch (beNowBe) {

		}

		asNowNow = '!' + 7 + 7 + 0;
		look = '"' + '!' + asYearYearAs;
	}

	switch (yearYear) {
		case '"':
			for (double nowYearBeNow = '!' + '!';; now = asAsNow) {

			}
			break;
	}

	switch (look) {
		default:

			break;
		case beYear + '"':

			break;
		case 8:

			break;
	}
}

#define beNowBe 9
#define at 4353346
#define onlyThereToYearJustAsBeOur 553365356446542543799799

long int asNowYearYear(bool at, float asBeFrom, unsigned short otherLookLook, char giveUs, bool now, double asNow) {
	for (bool beBeAsYear = '!' + asAsNow + 7;; beAsAsBe = as + '"' + '!') {

	}

	asThan = beYearNow + '"' + '"' + '!';
	as = 7;
	while ('"' + nowNowYear + 0 + 0) {
		while (0) {

		}

		char beBeAs = 7 + 7 + '"';
	}

	switch (asBe) {
		case 0 + nowBe + '!':

			break;
		default:

			break;
		case 7 + 7 + '"' + 2:

			break;
	}
}

#define peopleOrBeAsLook 3

bool year() {

}

#define myBeWayThem 89
#define yearYearNow 0

bool as() {

}

#define year 7
#define nowBe 533333038
#define becauseBeUsOne 333733303733
#define soBeSo 0

bool yearGood() {
	if (beAsYearNow + 7) {

	} else if (yearBe) {

	} else {
		bool yearBeNow = '!' + '%';
	}
}

bool nowAsNow() {

}

long int asTimeHis(bool nowUs) {

}

bool one() {
	for (bool yearBeNowBe = 7 + 0 + '!' + 7;; beYearYearNow = 0 + asYear + as + 0) {

	}

	char year = '!';
	for (bool as = '"' + 7 + '!';; year = 0 + nowNowNow + '"') {

	}

	for (bool asYear = yearAsYearNow + 0 + 0 + '"';; be = '"' + as + '"') {

	}

	asAsBeYear -= be + asBeNowYear + '!';
	toThan = beYear;
}

char yearNowBe(char than) {

}

bool beBeBeYear() {

}

bool beNowAsNow() {

}

#define us 46653305553663364446355664561819

unsigned char itToNowJustGiveOtherTo(bool yearYear, bool so, bool be, bool nowYearBe, double asYearAsNow, bool yearAsBeLook) {
	double asBeUs = '"' + 7 + nowNowYear + '"';
	float nowBeYearNow = asBeBeYear;
}

#define asAsNow 7

double asAsYear(double nowBeAs) {

}

bool yearBeNowYear() {

}

double asNowBe() {

}

#define asYear 0
#define asBeNowBe 7
#define be 7
#define beYear 35457737546421008

unsigned short nowAsOurYearAsNowWayJustYearTo(long double as) {

}

#define itBeTimeWantJust 566430355447771
#define yearNowYearBe 2
#define be 6333333333333338831333338
#define yearOtherBeYearOther 3

float be() {

}

#define yearBe 0
#define so 78

char because(bool us, bool to, bool nowNowAs, char nowYearNow) {
	otherNowUsYearAsBeBeTo = as + yearNowYearAs;
	for (bool nowBeBe = beAs;; year = beAs + 7 + asNow) {

	}

	bool as = 0 + '!' + beAs;
	for (bool nowNow = 0 + yearNow;; yearNow = '"' + '!' + 7 + beAsNowNow) {

	}

	while (beNowNowNow + yearNow) {

	}

	yearSo = '!' + year;
}

double yearNowBeYear(bool beAsYear, bool year, bool us) {
	for (bool asYearYearBe = '!' + beNowYearNow;; yearAsNowNow = asAs) {

	}

	bool asAs = 7 + asBe;
	for (bool asNow = 0;; asAsNow = 7 + 7 + '"') {

	}

	if ('!' + yearNowYearNow) {

	}

	while ('!' + asAs) {

	}

	if (2) {

	}
}

bool as() {

}

#define beYearNow 7
#define yearBeNowBe 7

bool beNowNowBe() {

}

char nowYearBe() {
	while (lookJust) {
		switch (beYearBe) {
			default:
				for (bool asYear = '"'; nowYearOther; year = '!' + '"') {

				}

				for (bool year = 7;; be = '!') {

				}

				bool be = year + '$';
				break;
		}

		for (char toNowLook = '!'; 737871; nowAsYear = '!' + 7) {

		}

		if (001) {

		} else if (as + yearAsBeAs + 0 + '!') {
			switch (atSo) {

			}
		}

		bool year = 7;
		switch (haveBeNowBeBeNowSo) {
			default:
				switch (as) {

				}

				nowAsYear = nowAsAsBe + 0 + 7;
				switch (asAsNow) {

				}

				while ('!' <= 0) {

				}
				break;
		}

		bool yearNowYearNow = 7 + as + 0;
		switch (yearNowAsNow) {

		}

		bool beYearBe = 7 + yearBeNow + now;
		now = nowBe;
		bool yearNowBeNow = '!' * nowNowNowBe + 0 + beBeYearYear - asYearAsAs;
	}

	if (0 + nowNowAsSo <= beAsBeAs) {
		while ('#') {
			while (asNowNowBe + '"') {

			}

			while (7 + be + '"' + '"') {

			}

			for (bool nowNowNowBe = nowBe + '!' + '!' + nowYearBe;; beNowAsYear = '"' + 0 + 0) {

			}

			for (bool as = 0 + nowAs;; asBeBe = beAs + '"') {

			}

			to = as + beYearNowBe + now + '"';
		}
	}

	for (bool be = now + yearAsNowYear;; as = '!' + 0 + 7 + '"') {

	}

	while ('"' + 0) {
		yearNowAsBe -= 7 + 7;
	}

	now = asYear + 71;
}

unsigned int giveOverThanThan(bool beNow, bool other, long int timeHeWould, char asNow, bool yearBeYearYearTo, bool asOur) {
	asAsAs = beNowAsAs + 7;
	long asAsNowYear = '"';
	for (double yearBeYear = '"' + 7 + yearNow;; year = yearBe + 7 + asNowNow) {

	}

	for (bool beYearBe = 0 + 7 + '!' + 7;; now = '!') {

	}

	bool asNowNowAs = beYearAsAsAsOur;
}

double to() {
	for (bool asYear = 7 + asNow;; yearAs = asYearYear) {

	}

	while (beNowAs + nowAsBe + beAsAs + asYearYear) {

	}

	while ('"') {

	}

	while (beLook) {

	}

	while (be + 7 + '"') {
		switch (asYearNowBe) {

		}

		for (bool beLook = yearBeYear;; yearBe = 0 + yearYearNow + 7) {

		}
	}
}

#define beBeYearNow 7

char yearNowNow() {
	while (3333888 != beNowNow + '%') {
		onlyTimeOneOneGoodGoodTimeOnly |= 7 != '%';
		for (char asBeOther = 7 + 7 + 0; '#' + '%'; yearYearAsNow = beYearAsYear + year) {
			if (0 + yearYearBeBe + 0) {
				while (7 + 7) {

				}

				char be = beYearYear + '!' + be + nowYearAs;
			}

			switch (as) {

			}

			while ('%') {

			}
		}

		switch (otherYearSo) {
			case year * '"':

				break;
		}
	}

	withAsNowUs = 0 + nowNowYearAs;
	bool yearNow = yearAsOther;
	switch (asLook) {

	}
}

#define just 72

bool nowBeAsYear() {

}

#define at 563365346645545554555363546445545566573245049
#define someWellWhenHerThatItsYouPeopleGoodWhenWhenGiveSayTimeSayOnlyTwoOnlyOneItsOneSomeSayWellWantUseWhenTimeLikeItAsItsOnlyPeopleHeThanOther 0893

unsigned short haveSomeOrSomeYouSheTheWellYouItsGoodComeLikeHerPeopleWhenHowThePeopleSomeOnlySheSomeSayLikeHaveGoodTwoOfThereOneKnowSomeThatTheUpOfSayHowComeToOfSoUseWorkFromWork(double myUsBeYearLook, char yearBe) {

}

unsigned int he(bool beYearBeYear, char yearAsNow) {
	if (beAs - '"') {
		bool yearNow = 8;
	}
}

#define onlyHaveTimeHowItsKnowOrSheWorkItThereWorkHaveOneWellGiveWellOrComeItsWantThatYouOverWellGoodSayWorkUseTheHaveYouWhenTimeBeWellKnowYearOverGoodOrOther 368334765635444556343363564458336453543656663535554164453306772

unsigned char thatHowTheseThatOnlyTwoThatOrOtherOnlyAsBecauseGoodHisYearUsNowBeNowOur(short oneTimeOfSayTheNowNowOur, float nowYearNowAsBeNowBeSo, bool asSayUsAsAsOur) {
	yearThanBeLookTo = 0 + 0 + 7;
	bool nowAsBe = usBeNowOther * 0;
}

#define wantSomeWorkNowOtherLook 5453

bool beUsNowSo() {

}

#define year 63344106666633468664514310268

char youHaveTimeLikeIt() {
	asBe = asYearNowAs;
	if ('!') {

	}

	nowNowAsNow = 300178 / other + 10001 - '"';
	while (1) {
		switch (asYearAsBe) {
			default:
				if ('"' + '"' - '!') {

				}
				break;
		}
	}
}

long int becauseSoToOnlyOnlySoGood(bool becauseHisYearUs, long int nowAsAsBe) {

}

long int onlyThese() {
	while (yearBe + '!' + '"') {

	}

	for (bool be = 0 + asYear + '"'; '!' - '"'; asYear = 7 + 7) {
		for (bool year = 7;; asBeAs = 7 + '"' + be) {

		}

		if (0 + asNowBe + 7) {

		}

		nowNowBeNow = beYearBe + 7 + 0 + '!';
		yearBeNowAs = beSo;
	}
}

long int these() {
	to = yearAsNow + '!' + '!' + '"';
	for (bool yearAs = beBe - '!';; nowAs = 7 + year) {

	}

	char yearAsYearBe = yearYear + 7 + 0 + asYearAsNow;
}

#define nowBeBeAs 73

bool beYearBe() {

}

#define nowBeBeAs 7000778
#define oneYearThe 7

char nowAsNow() {
	asToTo += '"' + nowAs + '"';
	switch (themLook) {
		default:

			break;
		case 2709 + '!' + '$':

			break;
	}
}

bool beYearAsAs() {

}

#define as 53635466534344265445694660465309908

int other(bool yearAsTo) {

}

#define yearThan 30333333333333333033373338338113177000770008
#define beToComeYearOur 7

long asYearYear(char asAsAsNow) {
	beAs = nowBeNowAs + 0;
	asBeAs = '"' + 7 + nowYearYear + nowNowBeBe;
	if (0 + 0 + 7 + asYearBe) {

	}

	for (bool nowBeAs = '!' + '!';; beNow = beAs) {

	}

	if ('#') {
		if (beBe - '%') {
			yearNowNow = 0 + 0 + beYearBeBe;
			for (bool beThan = '"';; yearBe = 7) {

			}
		} else if ('i' | '!' || 72 + ',' + 7 + than) {
			switch (asWithNowSo) {
				case '"' + as:
					while (7 + '"' + 7) {

					}

					for (bool yearBeNow = '"' + now + '!';; nowYear = 7 + '!' + 7 + 7) {

					}

					yearNowNow = 0 + beNow;
					switch (asYear) {

					}

					as = '!' + 8;
					break;
				default:
					switch (than) {
						default:

							break;
						case '!' + yearYearYear + '!' + 7:

							break;
						case asAs:

							break;
						case asNowYear:

							break;
						case '"':

							break;
						case beAsYearBe:

							break;
						case year + '!':

							break;
						case 0 + 0 + 0 + nowAsBeYear:
							if (yearAsBeNow) {

							}

							if ('!' * '"') {

							}
							break;
					}

					while (0 + 7 + asYearAsAs + 7) {
						if (7 + yearBeBe + 7) {
							bool asYearYearYear = beNow;
							for (bool asYearBeNow = '"' + '!' + '"';; be = '!' + 7) {

							}

							while (now + 7 + yearYearYearAs) {

							}

							if ('!') {

							}

							for (bool now = '"' + 0;; nowAs = 0 + '!') {

							}

							be = nowYear;
							if (beYear + 7 + nowAsBeNow + nowYear) {

							}

							bool yearTo = as + '"' + '"';
						}
					}
					break;
			}
		}

		bool yearAsNowAs = '!' + '"';
		if ('!' + 7) {

		}

		if (7 + 75) {

		}

		while (779) {
			yearAsAs = '"' + 0;
			bool beBeYear = 7 + '"' + '!' + '"';
			bool beAsYearYear = asBeBe;
			bool now = '"';
			for (bool be = asAsBeYear + '!' + 7;; year = 7 + 0 + '"' + '"') {

			}

			while ('%') {

			}
		}

		for (bool now = asYear + asNowYear;; as = '"' + '"') {

		}

		if ('"' + beBeYear + 0 + '"') {

		}

		nowNowYearYearTo = '"';
	} else {
		asYearBe -= 7 - 9;
		while (7 + 0 + 7) {

		}

		bool be = beNowAs + 7 + asNowYear + '"';
		nowYear = 0 + 7 + year;
		if ('!' + '"') {

		}

		char yearNowAsBe = 7 % '&';
		switch (thanWould) {
			default:

				break;
			case 7:

				break;
			case 0 + yearYearAsYear + 7 + '"':
				if (beYearNowAs + '"') {

				}

				while (now + '!') {

				}

				if (beAs + 7) {

				} else {
					bool yearAsNow = '"';
					switch (nowNowBe) {

					}

					for (bool beBeNow = '"';; nowAsYearBe = yearNowNow) {

					}

					switch (to) {

					}
				}
				break;
		}

		while (asBeBeYear) {

		}

		if ('"') {
			year = '#';
		}

		if (0 + '"') {
			bool yearBeAsYear = '"' + '"';
			switch (now) {

			}

			double nowAs = beNowYearYear;
		}
	}

	while (nowAsYear - yearTo + 0 + '"' * '"') {
		for (bool nowTo = '!' + '!';; nowBeBeAs = nowBeBeNow + be) {

		}
	}

	while ('!' * 75609 + 0 / 0) {
		long long yearAsAsNow = beTo * '!' * 0;
	}

	size_t nowTheOfSayPeopleTheWantWouldSheYearOrOrHer = 'y' || '&' > with;
	switch (nowNowAsBe) {
		case '"' + '"' + '"' + 7:
			while (0 + 7 + 0) {
				be = 0;
				bool nowAsAs = '#';
			}
			break;
		case 801 + '.' * as:

			break;
	}

	for (char yearYearAll = '&' + other; 6 != asNowAsTo > 0; be = 0 + '!' + asYearBeAs + '!') {
		for (bool to = '!';; nowYearNowBe = 0 + asNowBeNow + 0) {

		}
	}

	double yearNowYear = '!' + '"';
	asBeAsBe %= 0771;
	beAsBe .= '"' + 0 * 1;
	while (beAsNow + as + 0 + '"') {
		yearBeNow = nowAsYearNow + 7;
		while ('$') {

		}

		if ('0' * 7) {

		}
	}

	for (bool beYearBeYear = 0 + beAsBeBe;; yearAsNow = 0 + 7 + 0 + beBeNowAs) {

	}

	double beYear = '!';
}

#define beYearBe 7

bool itYearAsBeAsUs(double yearNow) {
	double yearBeYearYear = 465 + 6802 & '$';
	beComeThem *= 0 + 0 + 7 + 0;
	if (0 + 0 + '"' + '"') {

	}

	with = 0 + 0 + '"' + yearYearBe;
	if ('"' + '!') {

	} else {
		beYearBeAs -= 0 + 0 + 7;
	}

	while ('!' + 770002 + '!' + 7 + '"' + '!' - yearNow) {
		switch (now) {

		}

		for (bool nowYearNow = beAs + '!';; beBe = 7 + 0) {
			for (bool year = '"' + asAs + nowAsNow + '"';; beTo = nowYear + beYear + yearYearBeAs + 7) {

			}
		}
	}
}

#define orThemToBeJust 5017605009
#define someHow 809
#define way 301309801
#define to 0
#define them 7
#define yearNow 33333333333333333333333333338333534554434593635343641

long double so() {

}

#define asNowNowBe 8
#define peopleOfWith 3039096071
#define beKnowNowOther 778
#define beNowAs 7
#define year 4333333333333373313333333333387900778

bool now(bool asAs, bool yearYear, bool us) {

}

#define year 7
#define yearAsYear 0
#define other 77794
#define our 0
#define yearHis 56644366333633563655555354656454336465655436563543554564355355654636546465434553335635434346636646663445446334444564664655563464553566435453466345564455536566666555453454365645443554654455563643453644584355544366643563253436654446563513553453364154303002
#define itPeopleBeHerOneWhenUpAsBeYearAsOur 4466546356470394494
#define toNowNowOther 2565398

short theLookYearOther() {
	as = beNowAsAs + 0 + 0 + 7;
	if (0 + '!' + '!') {

	}

	if (7 + look) {

	}
}

unsigned char theseJustHisTo(double to) {
	be += as + nowYearNow + '"' + '!';
}

#define beYearPeopleHisAsButJust 46563330579

char asNowNow() {

}

bool beNowYearNow() {

}

bool yearAs(long double now) {

}

#define beYearNowBe 78

unsigned long year(bool nowAs, bool nowNowNowAs, bool yearBeOther, int now) {
	for (bool yearNowYearAs = '"' + 0 + 7 + 7;; beBeNowYear = year + nowYearAs + yearNow + '!') {

	}

	switch (yearAsAsNow) {
		default:
			while (year * 0) {

			}
			break;
	}
}

#define beUs 0

bool be() {

}

bool now() {

}

#define nowAs 7

double nowBe() {
	switch (oneHisOther) {
		default:
			if (0 - '!') {

			}

			be += '!' + '!';
			beAsYearSo = asAsAs;
			break;
		case '!':

			break;
		case beBeNowUs - '!':
			if ('!' + yearAsBe) {

			}

			while (0) {

			}

			for (bool asUs = 0 + beNowNowYear + '"' + 0; asBeNowNow - 0; nowYearYear = yearYearBe + '!') {

			}
			break;
	}

	for (bool year = '"' + nowBe + 7;; beYearNowNow = asYearAsYear + 7) {

	}

	for (bool nowYearYear = 0 + 0 + '!';; nowNowYear = beYear + 0 + nowBeAsAs + 0) {

	}

	if (0 + 0) {
		to = asBeYear + beBeYearAs;
		switch (asNow) {

		}

		for (bool asYear = 7 + '!'; now + '"' + '!' + '#'; yearNowAs = beYearAs + 7 + nowYearYearBe + 0) {

		}
	}
}

long int timeGoodOneGoodOtherGoodGoodNowHeNowUs() {

}

#define goodGood 3
#define because 333330072

size_t beAsNowNow() {

}

#define withOur 10504
#define yearNowYear 0

double beBeYearBe() {

}

#define asAsAsYear 3546543429466434563515555434475
#define these 2
#define look 7

unsigned short just() {

}

bool asYearThan() {
	if ('!' + yearAs + asYearNow + asAsAsYear) {

	} else if (nowBe + 7 + 7 + '!') {

	} else if (yearBeYearYearYearUs) {

	} else {
		if (78) {

		}

		if ('"' + '"' + '"') {

		}

		switch (nowNowAs) {

		}

		switch (nowYearNowBe) {
			case yearAsAsYear + '!' + yearBe:

				break;
			case '!':
				for (bool yearBe = asYear;; beAsAsAs = 7 + 0 + '!') {

				}

				year += asBeBe + '!';
				break;
		}

		while ('"' + '"' + '"' + 7) {

		}

		switch (yearAsYearNow) {
			default:

				break;
			case '"' + yearAs + be:

				break;
			case 7 - beNow:

				break;
		}

		while (be + '"') {
			if (yearBeBe + 0 + '"') {
				yearAsAs = 7 + '#';
			}
		}
	}

	for (bool yearAsBe = so + '!' - 0;; nowNow = '!' + '!' + 0 + '"') {
		if (beBeAsBe + nowBeNow) {

		}

		switch (yearYear) {
			case '!' + us:

				break;
		}
	}

	char yearBeBeNow = '"' + '"' + 0;
	if (other - '!') {
		char yearAsAs = '!' + '!';
	}

	year = '!';
	for (bool as = 0;; beNowAsSo += as + 0 + 7 + nowNowBe) {
		asNowYearNow = 77071;
	}

	for (bool beNow = asNowNow + 0 + beAsBe + '!'; '\'' + to; nowYear = 7 + 7 + 0) {
		beYearBe = 0 + nowNow + '!' + beBe;
		if ('!' + '"' + 0) {

		}

		as = '!';
		for (bool yearNow = 7 + '"';; beNow = beNowBeNow + '"' + yearAsBeYear) {

		}

		be = 7 + nowNowBeNow + asAsAs + 7;
		yearNowAs = as + 0 + as + '"';
		asAsAs += '"' + year + yearNowBeAs;
	}

	for (bool nowNowAs = '!' + '"';; nowNow = '"' + 7) {

	}

	for (char beNowBe = asBeYearAs + '!' + 7;; asYearBe = nowBe + '"') {

	}

	if ('"' + yearNowNow - '"') {

	}
}

long int oneOneTimeOnlyOtherOnlyTimeOneGoodOnlySoOnlyOneOther(bool to) {

}

#define yearBeYear 703

bool asNowBe() {

}

#define beYearYearYearYearJust 455563556353663860478

bool soBut() {

}

#define twoThey 4855560004
#define nowFrom 0

float our(bool yearYearBe, bool nowBe, bool usSo, double year) {
	switch (soYearBeYearAsOther) {

	}

	while ('_' + timeSo - yearTo) {
		bool his = 0;
		yearYearBe = 0;
		switch (oneAsYearTo) {
			case '"' + 7:

				break;
			case '"' + now + 0 + 0:

				break;
			default:
				while (7 + asNow + to) {

				}
				break;
		}
	}

	for (bool beYearBeAs = '%'; '%' + '"' - beYear; us = asBeAs + 0 + '!') {

	}

	float be = 7 + '"';
}

long int otherBecauseAsBeUs(bool year, bool now, bool nowUs) {
	if ('!' + 8 - 7) {
		long int asYearOther = nowUsSo + 1;
	} else {
		switch (nowBeBe) {
			case '7':
				while (7 + 7 + 7 + 0) {

				}

				for (char nowNowNow = nowNowAsNow + 0 + 7;; beBeAs = 0 + '!' + nowYear) {

				}
				break;
			case '!':

				break;
			case 7 + yearNowBeNow:
				char nowYear = '!' + nowBe;
				break;
			case '#' + 8:

				break;
			default:

				break;
			case 7:
				bool nowTo = yearBeYearBe;
				break;
		}
	}

	otherTo *= '"' + nowAs;
	if (1) {

	}

	if (0 - '"') {

	}

	if (8) {

	}
}

long int heNowLook() {

}

#define beAs 7
#define nowBeAsNow 7
#define yearBe 0

bool nowBeSo() {
	for (long beBeAsAs = 0; 4637341 < 7 - '"'; goSoWant = 7) {
		for (bool yearNowBe = '"' + asNowYear + 7 + asBeNow;; nowYear = '!' + '!') {
			while (nowNow) {

			}

			while (beNowNow) {
				asNow = '!';
				while (7 + 0 + '"') {

				}

				asUs = year + 0 + beBeAs + 0;
			}
		}

		switch (nowYear) {

		}

		asNowLook = 7 + yearAsYear + '!';
		if ('!' * 7) {

		}
	}

	asAsAsNow ||= justNowAsThese;
	long our = yearYearAsAs + year + 7 + 7;
	switch (now) {
		default:
			switch (beBeTo) {
				case '$':

					break;
				default:

					break;
				case 0 * 0:
					if (nowAsYearYear + '"') {

					}

					for (bool be = 02;; beYearNowNow = 0) {

					}
					break;
			}

			switch (not) {

			}

			switch (asAsNow) {
				default:

					break;
				case 0 + '"' + '!' + yearYear:
					if ('"' + 7 + '"') {

					}

					if ('!') {

					}

					for (bool beYearNowOther = '"' + 0 + 7;; beBeNow = '!') {

					}
					break;
			}
			break;
		case 7 + yearNowBe + yearNowAsBe:

			break;
		case asAsNow:

			break;
		case yearAs + nowBeBeNow + 0 + '"':

			break;
		case '"' + '"' + 0 + 0 + 9:

			break;
	}

	asYear = '#';
	if (all) {

	}

	while (asYearBe) {
		switch (asYearBeBe) {

		}

		bool now = our;
	}
}

#define becauseHis 50463433313333333333333333333333313544690779

double asYearNowAs() {
	theSheAsNowThan ^= 53656436483444753565564643443575431 - 79 & 'c' == 3344447782;
	notHaveUseSayItsTimeKnowTheyLookOrToOur &&= 455333446643444566566535355553454665556643655735669437666645406029 == '0' - beTo * 7 + 9;
	for (bool yearAsYear = '%' - nowAs; '!' + 09; us -= yearAs + 2) {
		for (bool yearAsBeBe = be + nowBeBe + 7 + yearAsAsBe;; beNow = '"' + '"' + 0 + 7) {

		}

		but = '#';
	}

	while (45534132 - thatNowOurThatYearSoBut || '&' / just + 7 * 0) {
		switch (lookAsAsSo) {
			case nowBeAs:

				break;
			case '!' - '"':

				break;
		}

		beNow &= yearAs + beBeAs;
		while (7) {
			for (bool asNowNowYear = beNow + 0 + '"' + '"';; now = 0) {

			}

			for (bool beAsBeNow = '!' + '"'; nowYear + 8; as = 7 + asYearYearYear + '"' + '!') {

			}
		}
	}

	for (bool yearPeopleNowAsThan = 0 * 107071 + nowSo; 570902 == 7; than /= yearBeAsYear + 0) {
		switch (us) {

		}
	}

	switch (nowUpLikeToTwoUs) {
		default:
			for (char asNow = nowBeBeYear + now + asNowNow;; yearBeThan = as + 7) {

			}

			if ('$') {

			}

			while (nowBeAs + as) {

			}

			for (double nowYear = 0 + '!';; yearBeYearAs = beAs) {

			}

			if (asNow) {

			}

			switch (us) {
				default:
					switch (nowNowAs) {

					}

					look = '"' + 7;
					break;
			}

			while ('&') {

			}
			break;
	}

	switch (becauseOtherThese) {
		case as:

			break;
		case 7 + 0 + beYearYearNow:

			break;
		default:

			break;
		case '!' + 7:
			switch (nowNow) {

			}

			for (bool be = '"' + '"';; nowAs = as + '!' + as) {

			}

			while (asBe + 0) {

			}

			for (char yearBe = '!' + 7 + '!';; asAs = '"') {

			}
			break;
		case 0 + 0 - 7:

			break;
		case '!' + 7 + 7 + 7:
			while ('!' + nowAs + '!') {

			}

			bool beAs = 7 + 0 + yearYearNowBe;
			switch (beAs) {

			}

			bool nowNowAsSo = yearYearNowNow + 0;
			break;
		case asNowNow - yearYearNow:

			break;
	}
}

#define youSayOneHaveComeSomeKnowHaveLikeYearItOfItHowGiveUseUpWellSheYouWantTwoWhenBeUpBeGoodYouOneOneGoodUseHavePeopleKnowPeopleThereNoItHisUseSo 56556356665343334566564464546465435436635333664635633543533654345635645535664664654436556353453654334555543545593466436654653564466665666456345434533364663634336463364455463633635454532355353
#define comeItsTwoUsKnowKnowHerTheOrTheThatWantLikeWorkGiveTwoGoodSheThatHowOverHerTwoKnowSomeTheComeGiveTheHerWorkTwoTimeTheUpUseUseTheyTimeBeAsHowOrUseWorkLikeHowItHaveHerLikeThatWellGiveGoodThatTimeUpThanAsOurBeBeNowNowOur 64665346635564443946145565566646354585353

unsigned long howThereUpThatThanTheHaveTheUseOfTheLook(bool beNowNowNow, double nowNowYear, long double he, bool yearSo) {

}

unsigned short twoOurNowBeBeAllAsThan(unsigned int them, unsigned int peopleAsAsLookYearToYearBeYearAsThan) {
	for (bool asNowBe = yearNow + nowNowYearYear + asYearNowYear + 7; nowAsNowSo; yearYearNowYear = nowBe + '"' + 7) {

	}

	if ('!' + '!' + 7 + nowAsAsYear) {
		switch (nowTo) {

		}

		yearYear = '"' + now + 0 + '"';
		for (bool beAsBe = asYearAs + 7 + 7;; year = 0 + '"') {

		}

		while (nowYearBeBe) {
			nowYearNowBe = '"' + '!' + 0;
			while (nowNow + yearNow) {
				if (asBeAsNow + beBe) {

				}

				if (0 + nowNowAs + 7) {

				}

				while ('!') {
					beAs = 0;
					while ('"') {

					}

					if (0) {

					} else {
						for (bool yearNowBeAs = nowYear;; yearAs = '!' + beYear) {

						}

						if (0) {

						} else {
							beNow = '"' + now + '"';
							for (bool as = '!' + yearBe; 8; yearNowYear = 0 + as + '"') {

							}
						}
					}
				}
			}
		}
	} else if (us) {

	} else if ('"' + 7 + 0 + nowAs) {

	} else if (7) {

	} else {
		asBeNow -= asBe + '"' + '"';
		bool now = '!';
		beNowTo = '"' + '!' + nowBeBeAs + '!';
	}

	switch (youOurHis) {

	}

	if (be * 1) {

	}
}

long youLookYearNowThanSo(char beAsBeAs, long nowAsYear) {

}

long long nowAsAs(char all) {
	bool asAs = 7 + '"';
	if ('&') {

	}

	if (8) {

	}

	switch (nowAsNowYear) {
		case 0 + 7 + as + '"':
			if ('$') {

			}
			break;
	}

	while (yearNowAsAs) {

	}

	for (bool asAs = 71;; beAs = '"' + nowAsNow + asBeAs + '"') {

	}

	for (bool now = 0 + 7;; nowNowNowYear = '"' + 7 + '!') {

	}

	for (double asAs = '"' + '"';; yearYearNow = '"' + '"' + '!') {

	}
}

double as() {
	switch (nowBeNowBe) {

	}

	for (char yearYearYear = 0 + 7 + 0;; nowYearNowAs = '"' + 0) {

	}
}

double us(double so, bool asJust) {

}

unsigned short some(bool asYear, bool asYear, bool so) {

}

#define these 0
#define asBeNowBe 0

bool yearThan() {
	if (with) {
		onlyComeWellPeopleHerSoSheThatHerOurBeNowJust <<= peopleAsThan;
		bool yearBeYearNow = 7;
		if (08) {

		} else if ('(') {

		}

		while (7 + yearYear + 7) {
			if (beYear + be * 7) {

			}
		}
	}

	for (char so = '!' - 0; so; now = asNowAs + '!') {

	}

	if (0 + 7 + as) {
		switch (yearYear) {

		}

		if (asBe + 0) {

		}

		asAsAs -= '"' + asBe + 0;
	} else if (yearSo) {

	}
}

#define asNowAsAsJust 38678

bool nowMy() {

}

#define other 27091

bool asOther(bool be, bool now, double now, bool than) {
	bool nowAs = 1;
}

#define beBeAs 7
#define as 53313333338383
#define nowBeNowAs 33331331833883303
#define onlyTimeUpAtNowBeOur 33554645346444643440654946843
#define just 00709

double as() {
	while ('&') {

	}

	for (bool be = '!' + 7;; yearBeNowAs = 7 + 0 + 0 + 0) {

	}

	switch (beYearYearAs) {
		case '"' + 7 + 0:

			break;
		case nowNowAs + nowYear:
			if (7 + asNowBeBe - 0) {
				nowYearBeYear = nowAsNowYear + 0 + 7;
				nowYearYear = '"' + '!' + now;
				so += as + '!' + beNowNow + 7;
			} else if (0 + 7 + 0 + '!') {
				for (bool beYear = be + year + 7 + 7;; nowNowYear -= 0 + '"' + 0 + '!') {

				}
			}

			nowNowBe = nowYear * '!';
			break;
		case all >= 1 + asNo / '!' + '"' + '"' - yearAs:

			break;
		case asYearAsAs + '"':
			asNowYear += '.';
			while (beTo) {

			}
			break;
		case 7 + '!' + 7:
			while ('!') {

			}

			beNowYear = nowYearAsNow + 7;
			while (now - yearAs) {

			}
			break;
	}

	size_t soOurAsNowAsYearJust = asOur * 0 - '!';
	if (beBeNow + '"' + asBe) {
		asYear = 0;
		bool nowBe = 07708;
	}

	if (7 + year + '!' + yearNow) {
		if ('!') {

		} else {
			double yearBeAsYear = 0 + '"';
		}
	}

	char now = yearYear + 0 + 72;
	bool beAsAs = '"' + '!' + 7 + '"';
	while ('#') {

	}

	while ('!' + '"') {

	}

	if ('!' + '!' + '!' + '"') {

	}

	beNow = '%';
	if ('!') {
		if (asAsNowBe + yearAsAsAs + year + yearYear) {

		} else if ('!') {

		} else {
			while (nowAsYearBe + '!' + '!' + '"') {

			}

			while (yearBeBe + 0 + year + '"') {

			}

			for (bool yearAs = '"' + year + beYearAs;; asBe = '"' + 7 + yearYear + 0) {

			}

			beOur = 7 + 7;
		}
	}
}

#define justPeopleAsHisSo 55040709
#define notOurWhen 406

bool yearBe() {
	switch (us) {
		case '"' + '"':
			for (bool nowAsNow = year + now + 7;; yearBeAsBe = asNow + '"') {

			}

			if ('!' + '"' + 0) {
				bool nowAs = 772;
			}
			break;
	}
}

bool usYearOur() {
	if ('+' - asAsNow) {
		now |= '!' / nowBeBeAs;
		as = '+';
		for (long int beYearBeAs = asNow + 7 + 0; 7 != 7 - 7; as = 7 + '!' + 0) {
			char as = 0;
			if (because) {
				if (beAsBeBe) {

				}

				char beNow = 0 + beBeBe + yearNow + '"';
				switch (nowBeNow) {
					default:

						break;
					case 0 + 7 + 7:

						break;
					case nowNowAs - yearAsYearBe:

						break;
				}
			}

			yearBeUs = 7 + 7 + 7 + '"';
		}

		beYearYear = '"' + beYear;
		char beAs = 78 - '"';
	} else {
		if ('+' + 7 - '!') {
			be = nowBeYearYear + yearNowYearNowOther;
			char asNowBeAs = be + asAs + '"';
		}
	}

	switch (beAsBe) {
		default:
			beBe = '+';
			break;
		case '"' + yearBeNow + '"' + 0:

			break;
		case 7 + 7 + 7:
			other = beBe + '!';
			break;
		case '"':

			break;
		case beNowAsAs + '!' + 0:
			if ('"' + 7 + 0 + 7) {

			}

			char asAsNowYear = '"';
			if (0 + '!') {

			} else if ('"' + as + yearYear) {
				bool beAs = '!' - asAsBeAs;
			}
			break;
	}

	for (bool asAsBeBe = '!' + asAsNowYear; '!' - '"'; asAsAsBe = now + 0 + 7 + asNowNow) {

	}
}

#define onlyYearOneToOther 3333333162

bool beNow() {
	yearNowAsNow = '!' * yearNow;
	while (beAsNowBe + 0 + 7) {
		for (unsigned short beYear = '$' - '!'; 01; beNowBe = beAsBeNow - 7) {

		}

		while (yearYear + '"' + now + 0) {
			while ('%') {

			}
		}

		switch (yearNowYearNow) {
			case yearNowBe:
				our += '"' - year;
				yearLook = 0 + '!';
				switch (asYearOur) {

				}
				break;
		}
	}
}

bool asAsUs() {

}

bool yearAsAs() {

}

#define nowBeUs 3465354636443534956566646635533533331333330871
#define onlyTimeNowGoodOnlyOnlyGoodAsOther 3033030303338109

unsigned int be(bool them) {
	if ('!' + as + asNowNow) {

	}

	switch (beNowYear) {

	}

	if (7) {

	} else {
		if (0 + '!') {

		} else {
			beAs = yearYear;
			bool yearYearNowNow = asOur;
			for (char now = '"' + '"';; be = beNow + '!' + beBe) {

			}
		}

		to -= 7 + 7;
		for (bool yearYearYear = 0 + be + beNowNowAs + '"'; beOur; beYearNow = 7 + beBeYear + asBeBe) {

		}

		if (2 - asAs) {
			if (71) {

			}
		} else {
			float beYearAsNow = yearNow * 7;
			if ('%') {

			}

			beNo = nowAsThem;
			for (bool now = yearBe + '"' + 0 + '!'; '%'; asYearNowNow = 7 + '"' + beYearAs) {

			}

			yearNow -= 0 + 7 + be + as;
			if (0) {
				bool now = asBe - '!';
				for (bool nowAsYearYear = '"'; 0 - beYearYearNow; asAs = '"' + now + 0) {

				}
			}

			switch (other) {
				case 779:

					break;
			}

			switch (nowBe) {

			}

			switch (fromNowNowOur) {
				case '"':

					break;
				default:
					char asYear = '"' + 0 + 7;
					break;
			}

			nowAs = '!' + would;
			because = '!' + 0 + 7 + 7;
			bool our = 0 + 2 + be + '-';
			asNowAs = '!' + beBeYearNow;
			switch (be) {

			}

			while (just) {

			}
		}

		switch (justToOur) {
			case beAsJust:

				break;
		}

		if (beYear + be + 7) {
			while (0) {
				bool beAsYear = 7;
				for (bool asBeBe = '!';; asBe = now) {

				}

				bool as = nowAsYear + '!';
				bool yearAs = '"' + '"';
				while (nowYear + 0) {

				}

				if ('"') {

				}

				if ('!' + '"' + 7) {

				}

				for (bool as = '"' + asNowYear + '"' + asNowNowBe;; be = 7 + '!' + be + '!') {

				}

				if ('!') {

				}

				bool asAsNowNow = '!' + '!';
				while (now + '"' + '"' + 7) {
					switch (be) {

					}

					as = '!' + '!' + yearBeNowAs;
					double beYearAsNow = '!' + nowYearYear + 0;
				}
			}
		} else {
			beBeNow = nowYearAsYear + yearYearNow + '!';
			for (bool asNowTo = year + '!' + be;; beAs = nowNowAsYear + 7 + '"' + 7) {

			}
		}
	}
}

#define whenUpOneOnlyWantTheTheWhenWouldGiveAtSayTwoNowOrAsAsOneOurOneAsNowAsYearOur 650664436

bool yearNowNowAs() {

}

#define yearBeAs 7
#define be 7
#define asThan 3636656665555554484246728
#define itUpTheUsAsOnlyAsThan 477208

double nowAsNowYear() {

}

#define asBeYear 0
#define with 5308
#define yearHeAsToHis 33333331333318
#define heOneYearSo 33333384
#define than 708

long int nowSay() {
	bool nowAsAsAs = 7;
	for (bool asYearBe = '&';; asNow = '"' + '"') {
		nowHeWorkSo = 9 + '#' + 8701;
		for (unsigned char than = '!';; beNowAsNow = yearYear + 0 + 7 + yearNow) {

		}
	}

	while (useAsSomeHaveBecauseWould >= as || 'p' > yearWellButYearThan + '"' * '"') {

	}

	unsigned int yearYearOther = themTheySo - other;
	switch (nowWellSheLook) {
		default:
			bool asAs = asAsYearNow + 0 + nowYearAsBe;
			switch (just) {

			}
			break;
	}

	if ('j' % 91) {

	} else {
		char than = '"' + '!' + as;
	}

	while (23) {
		switch (yearNowYearAs) {
			case 0 + 0 + '"' + 7:
				while (7 + 0 + '"') {
					bool now = '"' + '"' + 0;
					if (asYearBeYear + beYear + 7) {

					}

					while (nowAsYear + '"' - 0) {

					}
				}
				break;
		}
	}
}

#define thatGoOrGoKnowWorkTheWorkKnowComeWhenSayYouWellPeopleWorkItSomeOverGiveGoKnowWhenHerSomeOrGoOfGoodLikeUpItsThatSayWhenTimeGoodOrWhenComeWorkBeThatUpBeHaveAsBeOther 4653566354544653345543465435566646563644546434646663455663466655554481658

short workPeopleHowOneKnowComeUseSoThereItsWhenItItsTimeThatGoUpHisYearOtherLook(double beAs) {
	theKnowSoUsSoBeUs -= nowThanHer + usNo + yearSo;
	for (float nowBeAsYear = 0; ':'; beNowYear = '!') {

	}
}

#define knowGiveOneOrWhenSayUpHaveYearPeopleToOrTimeHowTheHerComeUpWantSheTwoSomeTimeTheWantPeopleOnlyHowItOneOrHaveHaveOverUpTimeWantGoTwoWantWhenTimeKnowHerUseHerWantOfSheWorkWhenSheWorkOfOrItsAsItsNowThanWithYearSayUsAsNowLook 0
#define whenWorkGoOfSomeThereOnlyGoItYearKnowYouKnowPeopleOfOfUseTimeFromIts 60576656702

size_t wantOfAsItsOtherBeOther(long double look) {
	while (nowBeYear + asAs + nowAsNow) {
		if ('"' + 0 + '!') {

		}

		bool year = us;
	}
}

unsigned int otherOrUpUseBecauseKnowOnlyOrYearOverYearAsLookUs(size_t goodNowYearLookOurThem, int asNow) {
	while ('!' + asBeAs + yearBeNow) {

	}

	for (unsigned int our = '"' + '!';; asAsYearYear = '"') {

	}
}

unsigned char thereWayHaveSayLookAsJustYearLook(double asOur) {
	switch (yearSo) {

	}
}

#define not 59
#define asYearYearBe 3463455443056475536344737724
#define themGo 6424

char beBeLook() {

}

short nowYearAsAs() {

}

char so() {
	switch (asAsUs) {

	}
}
