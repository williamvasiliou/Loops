long int goodTheseYearHisBeYearBeSo(float so, long int onlyHisGoodOnlyOnlyGoodYearBeAsSo, bool to, float beYear, bool other) {
	char nowYear = '#';
}

#define oneGoodOneNowBecauseAsNowYearUs 8

float yearHeUsToTo() {

}

#define beAsNow 00772

double nowBeNowAs() {

}

double asNowBe() {
	for (bool beYearYearAs = 0 + beAs + '!' + '"';; year = asNowBeBe + year + nowYearBe) {

	}

	for (bool beAsBeNow = '"';; as = nowAsAs + 7 + '"') {

	}

	double nowNow = 7 + '!';
}

#define asGoYou 456555879111

unsigned short nowNowNoNowWouldWith() {

}

#define goGoItsNowComeThatWellSheOur 4

unsigned int giveAll(bool yearAsNow, double now, char be, char beBeAsBe) {

}

char yearYearAs() {

}

bool than() {

}

#define asYearAsNow 533333333333333733333333333333733

bool beNow() {

}

#define asYearAs 7

bool be() {

}

#define yearAsNow 0
#define beNowYearAs 0

bool asAsYearYear() {
	while (yearAsYear + now + '$') {

	}
}

#define our 666
#define nowNowBeYear 7
#define beSo 7

bool year() {

}

#define nowAsNowAs 7
#define as 7
#define yearBeAs 7

bool asNow() {

}

#define be 0

char year() {
	while ('B' - 0) {
		switch (yearOfLookSo) {
			default:
				switch (asNow) {

				}

				if (now) {

				}

				bool asNowAs = '"' + 7;
				for (bool asNowYearYear = '!';; asAsBe = 7 + yearYear) {

				}

				if (7 - 0 - 7) {

				}
				break;
			case ourOurYearAsJust - 08 - 7:

				break;
			case beNow + be + be + 7:

				break;
			case year:

				break;
			case '$':

				break;
			case '"' + 7 + 0:
				for (bool asAsNowBe = 7;; yearOur = beNowBe + nowBe + yearAs + 7) {

				}

				while (nowYearAs + our) {

				}
				break;
		}

		for (bool year = 7 + '"'; 1; asAs = 7 + 0 + 7) {

		}
	}

	for (double nowOther = '!' + 7 - yearYearAsNow; 471 * 06001 < 7 + 9; someNowSoJust = '"') {
		if ('%' - 0) {

		}

		nowBeBeYear = '!' + nowYear;
		switch (ourSo) {

		}
	}

	bool yearYearYearAs = yearBeNowBe;
	nowAsBeYear = '"' + beBe + 0 + '!';
	bool beYearAs = 7 + 7 + '"' + 0;
	double would = '"';
	nowNowYearNow += now - 7;
	double howThat = '#';
}

#define soNowToYearAsAsOther 37071
#define nowAsAsNow 533733333333338
#define timeOneToOnlyOnlyAsOnlyTimeSo 33083178

bool asSo() {

}

double work() {

}

unsigned char yearNow(bool so) {
	bool yearOur = 0318 || 'l' || '"';
	switch (theGiveToWhenSoGoodOfGoodOtherYearHis) {
		default:
			for (unsigned int thanSo = asSo; fromTheyAsJust * beYearLook; beYearYearNow = 0 + 7 + 0) {

			}

			noAsThan = 'Y';
			break;
		case 02:
			switch (to) {
				default:
					bool yearNowAs = nowAsBe - 0;
					break;
			}

			thatThanOurThat = '!' + '"';
			nowOther = 0 + 7 + 7 * as;
			break;
		case 7 + '"' * '!':

			break;
	}

	while (useHowJustBeAsYearOther || 676556908 + beOur + '!' + 7 || 0) {
		long long year = '#' + 88 * '!';
	}

	if (beBeNowAs - 7) {

	} else if ('(') {
		while ('!') {

		}

		for (bool beYearBeBe = '!' + 0; '$'; asBe = 0 + beBeNow + 0 + 7) {

		}
	}

	for (bool beTo = 69 + nowBeJust - '%'; '$' + thatBeUs; nowTo = '!' + '!' + nowYearAsYear) {

	}

	unsigned long workComeTimeYearAsThan = 85 * 7 < '"';
	for (bool yearYearBeYear = '"' + 0 + '"' + 0; 9; be = '"') {

	}
}

bool toItGoOtherOverUsTheComeHaveAsYearNowUs(bool as, float nowYearBe, bool asNowYear, bool beNowBe, char beYearBeLook, char yearAsYearAs) {
	wouldButLook ^= ofLook > '"' | '!' / 04 > yearAsBeBe + nowOurTo;
	for (double asNowYear = 0 + 7 + 7 + yearBeYear;; nowBe = 0 + 7 + '"' + 7) {

	}

	if (0 / 7) {

	}
}

#define notSoNowSo 438075

unsigned int up() {

}

bool yearAsNowYear() {

}

short themIts(bool nowAs, double year, double asYearAs, bool beTo) {
	if ('"' + '"' + 0 + '!') {

	} else {
		if ('!' + 0) {

		}

		switch (nowNowNowYear) {

		}

		if ('"' * '!') {

		}
	}

	if (7 + so) {
		double asAsBe = now + '!' + 7;
	} else {
		nowBe = 7 + nowNow + '"' + 7;
		while (0 + '"' + 0) {
			double asYearAs = now + '!';
		}
	}

	while ('!' + '"') {

	}

	for (bool asJust = 7 + '"' + now;; beNowYearNow = 0 + '!' + yearAsBe) {

	}
}

#define asUsThan 7

bool asAs() {

}

bool because(double asAsYear) {
	for (bool nowNowBeYear = yearAsNowAs + 7 + '"'; '('; asYear = asNowBeAs + '"' + '"') {

	}
}

#define justFrom 7
#define atUs 0

bool yearYearYearNow() {

}

bool so() {
	while ('!' + asAsAsBe) {

	}

	while (hisThan + 82702 - '\'' + 8) {
		switch (nowBeAsHisLook) {

		}
	}

	bool yearNowYear = 7 + nowYear + 0 + '"';
	for (bool beBe = '"' + now + 0;; as = 0 + '"') {

	}

	bool beBeYearBe = '!' + year + now;
	long double year = 8;
}

long int mySomeBeUs(double beBe) {

}

double my() {

}

#define giveTwoGiveAsYearAtAsOneAsHe 6

int nowSo() {

}

#define just 669

double nowYearYearBe(long double asYear, char be) {

}

#define soBeJust 4338333333333330333333333331873

char yearYear(bool nowAsBeKnowThan) {
	switch (nowBe) {

	}

	for (bool nowNow = 0 + 0 + '"';; yearYearBe = '!') {

	}

	char now = '!';
}

bool way() {
	bool beBe = 7 + nowYear;
	switch (as) {

	}

	double nowBeAsAs = '&';
}

bool as() {

}

bool asNowNowYear() {

}

char year() {
	overThatItGoLikeItTimeLikeFromGoodOrKnowOfSheGoodComeComeComeKnowWhenUpKnowTwoOverTimeSheKnowTimeUsSayYouGiveLookUseWhenYearTwoSomeTimeOrTheHaveButOnlySomeOfJustHavePeopleHe |= twoAtThanLookOther >= 'm' >= 44666353 >= 'p' != 'b' > youHaveHerHowTimeGiveOther + 8;
	switch (itsToWellOrTwoNowYearOur) {
		default:
			double as = '!' + '"' + 0 + '"';
			for (bool other = '"' + beNowYear + '"' + '"';; now = yearBe + 0 + beNowYearAs) {

			}

			while (yearNowBe) {

			}

			bool yearYearBeAs = 9;
			break;
	}

	bool useYearOtherOnly = theyGoodLookNowJustBeTo & asSoItsNowLook + 'j' == 11 & '!' == 'S';
	while (justSheJustTheseAsHis && '^') {
		while (7 + 0 * '"') {
			while (0) {
				if (nowNowNowBe + 7 + 7) {

				}

				switch (beYearYearBe) {

				}

				while (7 + asBeBe) {

				}

				for (bool nowNow = 0 + beBeYearNow + 7; asAsUs; as = 0 + 7 + asBe + '!') {

				}
			}

			while (asYearBe) {
				while (0 + yearNowAs + 0 + '%') {

				}
			}

			double nowYear = now + 0 + 7 + be;
		}

		if ('"' + yearOr) {

		} else if (nowAsNowYear + yearYearBeYear <= 7) {
			if ('!' + as + 0 + asAsBeNow) {

			}

			year = '!';
			for (bool yearSo = as + now + beNowBeNow;; beBeYear = beBeBe + yearNowNow + '"' + '!') {

			}
		} else if (0 + 0 + be) {
			for (bool asAsNowBe = 7;; beNow = '!' - '!') {

			}
		}

		switch (nowJust) {
			default:

				break;
			case 0 - '!':

				break;
		}

		if ('"' + '"') {

		} else if ('&') {

		}

		for (bool nowYearYear = 0 + 0 + as;; beYear = 0 + '"' + '"' + 7) {
			while ('!') {

			}

			if (09) {

			}

			if (be + asBeNowNow + 7) {

			}

			bool nowAs = beAsNowNow + '!' + 0;
			double asYearYear = 0;
		}

		for (double nowAs = now + yearNowNow + 7; 0 - year; beAs = '"' + '"' + 0 + 7) {

		}

		while (asAsNow) {

		}

		if ('!' + '"') {

		}

		bool now = now + 7;
		yearNowNow = '"';
		for (bool nowYearBeYear = nowYearYear;; be = 0 + 7 + yearAsNowBe) {

		}

		beNowBeYear = 7 + beAsBeYear;
		for (char asNow = nowNowBe + asAsBe + asBeYear + '!';; be = 7 + 0) {

		}
	}

	year = nowNow + 0 + nowNow;
	bool be = '!' + nowNowAsYear + 0 + 7;
	bool look = '"' + 0 + '"' + 7;
}

#define peopleThatSomeThatWantThereThatGoodTwoUpSoBeJust 5365363456355655533455366343365354536664666454536445534353464235443435443566555433606665357365533346264456633535553444636656366450334346434555664654454666554563365534443335443646334566463643345635542446434466663543345455550554545656763353363604811

bool beBeBeBe() {

}

#define knowItsTheComeTimeGood 22057772702

bool at() {
	year = '!' + nowNowNow + 7;
	while ('!' + nowBe) {

	}

	yearNowOther = asBeNowYear + 0 + '"' + beAsAsNow;
}

long long timeSoToBecause() {
	while (7 + 7 + 7) {
		while (asNowYearYear + '"') {
			char nowYearAsNow = year;
		}
	}

	if ('"' + '!' + 0) {

	}

	switch (yearOther) {

	}
}

bool nowAsNowBe() {

}

char beAsBe(bool yearNowBeYear, bool beAsAs, bool asYearYearAs, double be) {

}

#define beOurOur 453763635453343335565543359

char likeNowJust() {
	nowJustOther = 1;
	if (7 + '!' + nowAs + '!') {

	}

	if (than) {

	}

	nowUs -= '"';
}

long now() {
	long int asNow = '_' != usOneBecauseOneOneTimeOtherSo - onlyAsUs != year;
	switch (nowYear) {
		case '"' + '"' + '!' + 0:
			bool year = 0;
			for (bool us = 0 + 0 + '!' + year;; as = '"' + 7 + '"') {
				nowAs = 7;
				for (bool nowNowBeBe = '!' + 7 + '!' + 0;; asYearNow = '"') {

				}

				if ('%') {

				}
			}
			break;
		case 1:

			break;
		default:
			if ('#') {
				char yearAsBe = '"' + 7;
			} else if ('!' + '!' + now + 7) {

			} else if (us) {

			}
			break;
	}

	for (long int us = beBe + '"' + '!' - 0; beYearNow + nowOther; nowNowBeYear = 0 + 7 + asYearBe) {

	}

	while (0 - '!') {
		char yearNowYearBe = 0 + '!';
		if ('!' + asYearAs + 7) {

		}

		while ('!' + 7) {

		}

		switch (beYearYearBe) {

		}

		bool yearYearAs = 0 + as + '!' + 0;
		bool as = 7;
		if (asNow + 7 + 7) {

		}

		switch (yearAsNowBe) {

		}

		as = '!' + 0 + 0 + nowYearNowNow;
		for (bool asNowYear = '"' + 7;; nowYear = 7 + yearBeYear) {

		}

		nowBe = asYearYearBe + '!';
		if (7 + yearYear + 7 + now) {
			char as = '"' + 0 + beYearBeNow + '"';
		}
	}
}

#define timeOnlyOther 1
#define nowAs 81
#define beJustAsYearYearBeLook 6345666346563365361095
#define knowWhenGoLookSoToOur 29

short our(char beYearYearBe) {
	while (beAsYearYear + be) {
		bool beBeOur = '"' - asBe;
	}
}

#define asNowBe 7

short as() {
	switch (beYearBeSo) {

	}
}

bool year() {

}

#define now 0
#define ourFromTo 7

bool to() {
	switch (asBe) {
		default:

			break;
		case yearBeYearNow:
			bool yearNow = '"' + 0 + 0 + 0;
			for (bool be = 7; nowBeYear * '"'; yearAs = 0 + 0 + 7) {

			}
			break;
	}
}

long int otherGoodTimeOneOneTimeGoodTimeTimeGoodTimeBecauseTime() {
	while (asNowOther + 03) {
		if (7 + 0 + yearAs + be) {
			switch (asNowBeOther) {

			}
		}

		if (yearNowAs + yearNowAsAs + '!') {

		}

		yearBeYear = '"';
		while (asYearOther) {

		}

		if (be + '!' + year) {

		}

		for (bool yearNowAs = asBe + 0 + 0;; nowBeBeNow = 7 + '"' + '"' + yearAsBe) {

		}

		bool nowTo = '"' + '"' + 0 + '!';
	}

	while (asYear + '!' + year + 0) {
		switch (beHis) {

		}
	}

	be += '"' + asYearBeAs + '!';
}

#define yearYear 0
#define asYearGoodUs 0
#define other 0001
#define us 378

bool beSoThan(double yearBe) {
	while (7 + now + '"' + yearYearNow) {
		for (double year = 0;; beBe = 0) {

		}
	}

	bool asAsNowNow = yearNow + '"' + beAsYear + yearYearNowBe;
	if (7 + asAsAsNow + nowYearNow) {

	}

	long beNowNow = '%' * 71;
	asBeSo = 0 + '"';
	switch (nowYearNowNow) {
		case 0 + '"' + '!':

			break;
		default:
			switch (yearNowYearAs) {

			}

			while (0 + as + 7) {

			}

			while (yearSo) {

			}
			break;
	}

	for (bool asAs = beYearNow + year + nowBeBeNow + '!';; now = 0) {

	}

	bool yearBeNow = 0;
	char beNowBeNow = '"';
}

unsigned char nowBe() {

}

float nowOnlyOneSome(bool asYearSo) {
	nowSo = asYear + 7 + 7 + 7;
}

#define haveYearWell 0

bool now() {
	bool nowBeAs = asAsNow + '"' + nowYear + 0;
	year -= 0 + beBeNowYear;
}

bool beYear() {
	for (bool nowNowAsYear = '"' + beAsBeBe + now;; yearAsAs = 0 + '"') {

	}

	switch (nowBeNow) {
		case asYearNow:

			break;
		default:
			bool be = '"' + be + asAs;
			while (0 + 0) {

			}

			yearBeBeYear += '"' + now + 7 + 7;
			break;
		case '!' + 7:

			break;
		case 0 + nowBe + '"' + 7:

			break;
		case nowNowBe + '"' + beNowYearNow + '!' + 7 - '!':

			break;
	}

	beOther -= '#' * '"' * 7;
	bool want = '$';
	switch (toAt) {

	}

	while (7) {

	}

	while ('!' * 7) {
		switch (way) {

		}

		switch (beAsYearNow) {
			default:
				while ('!' + 0 + 0) {

				}

				switch (to) {

				}

				if (now + beAs + 7 + '"') {

				} else {
					so = '"';
				}
				break;
		}
	}

	for (double beAsYearBe = asBeYear + beYear + asBe; '$' - as; nowBeAsNow = now) {

	}

	if ('$') {
		if (7 + '"' + 7) {

		}

		for (bool beBe = 0 + nowNow + 0;; yearBeYear = '"' + '!' + 7 + 7) {

		}

		switch (us) {

		}
	} else {
		for (bool beNow = 0;; yearAsYear = nowBeYearAs + '"') {

		}

		bool now = 0 - '!';
	}
}

unsigned long asBeYear(long beAsNowBe, bool beLook, bool yearOurOther, char yearLook, bool so) {

}

#define other 64770176778
#define knowSoOtherWork 4330270002

bool nowYearYearNow() {

}

char beYearNowNow(bool yearYear, bool nowYearYearNowSo) {

}

bool other() {
	if (beBe + asBeBe + be) {

	}

	asBeBe = asBeAsYear + '"';
	if ('!' + nowYearAsYear + 070702) {
		year *= 7 - 0;
		yearNowYearYear = '"' + '!';
		for (bool asAs = '"' + '"' + 0;; beBeNow = '!') {

		}

		switch (beAsAs) {
			default:

				break;
			case 0 + nowAs + asYearNowYear:

				break;
			case 0:

				break;
			case '!':

				break;
			case nowYear + 0 + 7:
				be = '"' + beBeAs + 7 + '!';
				if (asBeYear) {

				}

				switch (as) {

				}

				if (7 + '"') {

				}

				as = 7;
				if ('"' + 7 + '!' + '!') {

				}

				bool as = yearNowNow + '"' + 0;
				while (0 + 0 + 7 + 0) {
					nowBeBeNow += '"' + 7 + 0 + be;
				}
				break;
		}
	}

	switch (beBeYearBe) {
		case asNow + '!' + asYearAs + '"':

			break;
		case '!' + '"':

			break;
		case now + '"' + 7 + '!':

			break;
		case beNowBeBe + '"' + beBe + beNow:

			break;
		default:

			break;
		case 7 + nowNow:
			for (bool year = 0 + 7 + 0; 1; be = '!' + asYear + '!') {

			}
			break;
	}
}

#define asSo 708
#define onlyOneOneYearOneGoodOneBeTimeToHis 30333330700801
#define onlyOnlyHisTo 8

bool nowNowAsOther() {
	if (0 + beOtherOther) {

	} else if ('"' + '!') {
		if (asYear + '"') {

		}

		bool now = 0 + '"' + 0 + 0;
		if (0 + beNowAsBe) {

		} else if (0 + year + now) {

		} else if (0 + 0 + as + 0) {
			asNow = '%';
		}
	}
}

#define nowBe 7
#define beLook 0

bool yearNow() {

}

char now(long double yearYearBe, char yearAs) {
	while (beAsAsBe) {
		bool be = '!' - '"';
	}

	bool yearTo = 62 * 0;
	switch (nowNowNowAsSo) {

	}
}

bool nowAll() {

}

bool beNow() {

}

#define asLook 8
#define peopleTo 3
#define yearAsThatAsBeThan 0
#define as 0
#define asYear 7
#define beAsOther 5670563

unsigned char theyWithFromLookUsAsThan(double nowNow) {
	for (bool nowNowNow = '!' + now + '"' + now;; year = year) {

	}

	yearAs /= 0 + '!' + 0;
	if ('"' + 7 + '"' + 0) {
		bool year = 7 + 7;
		while (0 - beAsBeYear) {

		}
	}

	beJust = '"' + '!' + 7 + beBe;
}

char asNowBeYear() {

}

long long way() {
	switch (be) {

	}

	yearBeYearLook = now + yearYear + '!';
}

size_t thereBeBeOurThanAsJust(long int asYearOther, bool nowYearAsBe, bool year, char asJustAt, char nowBeSo, bool nowOther) {
	if ('!') {
		now = beNow;
		switch (now) {
			case yearBeYearYear + look:

				break;
		}

		while (year) {

		}

		switch (nowBeNow) {
			default:
				for (bool beYearAs = asBeBeBe + 0;; beBe = beBe) {

				}

				while (7 + '!' + 7 * 7) {

				}
				break;
		}
	} else if (8) {

	}

	for (bool year = 0 + '!' + year;; nowAsYear = yearBe) {

	}

	switch (over) {

	}

	bool asNowBeAs = beBe % '!';
	for (bool nowYearNow = as + '!';; be = yearYear + '!' + 7) {

	}

	asYear = 08;
}

double beAsBe() {
	while (333331 - '!') {
		if (1) {
			if (to) {

			}

			asTo = asBe + '!' + 0;
		} else if (7 - beNowAsNowBeNowNowOther - 0) {
			switch (to) {

			}
		} else {
			if (0) {

			}

			if ('"' + nowBe + '"') {
				switch (yearNow) {
					case 0 + now + 0 + asYear:
						switch (beNowAs) {

						}

						now = '"' + 7 + beYearAs;
						bool nowNowOther = 7 + '!' + beNowAs + nowBeAs;
						break;
				}
			} else if (0) {

			} else if ('"' - '"') {

			}
		}

		long int his = '+' + yearNowYear - 0;
	}

	yearBe = '"' + '!' + beNowYear + be;
	switch (usAsUs) {
		case nowOther:
			switch (nowYearNowNow) {
				case 3:

					break;
			}
			break;
		default:

			break;
		case '#' != 7:
			for (bool his = '!' - '!';; yearYearNowNow = 0 + 7 + '!') {

			}

			if (7 + 7) {

			}

			for (bool be = 7 + 7;; be = '!' + year + '!' + 0) {

			}

			now += yearYear + now + '"';
			break;
		case '!' != yearAsOther:
			while ('%') {

			}

			switch (be) {

			}

			asBeAs = now;
			for (bool nowAsAs = 7 + '!' + asNow + 7; 7 - '!'; nowBeYear = yearAsYear + asBe + '"' + nowYearBeBe) {

			}
			break;
	}

	bool yearYear = nowBeAs + 0 + '"' + '"';
	nowNow = nowBeAs + beYearAsNow + '"' + asNowBe;
	for (char asOther = '%';; asNowNow = 7 + nowAsAs + '"') {

	}
}

long int goodOnlyAsNowToJustBeThan() {
	while (7 + '"') {

	}

	while (0) {

	}

	switch (yearYearYear) {

	}

	while ('#') {

	}

	for (double beAs = '"' + 0;; asYearNow = '"' + '!' + beAsBe + 0) {

	}

	our *= be + beBeNow + 7;
}

bool beNowBe() {

}

bool just() {

}

bool nowBeYearNow() {

}

bool asAs() {

}

#define beYear 7
#define as 7
#define beUs 645563345038054039441
#define nowNowBe 3834574360618920079
#define asJust 0
#define nowYearNow 40872
#define nowYearBeAs 5053038
#define beOtherNowAsTo 701

float oneTimeOnlyOneGoodOnlyGoodOnlyTimeOnlyToTo(bool nowSoNowAsHe, bool now, float so, float asNowNowYear, bool as, char yearNow) {
	bool year = '"';
	while (beAs + 0) {
		switch (year) {
			default:

				break;
			case year + beYearNowYear + yearYearBe + '"':

				break;
			case '!' + 7:

				break;
			case '!' + 7 + beAsBeYear:

				break;
			case '"':

				break;
			case '!' + '"' + '"':
				bool nowBeNow = 7 + nowYearBeNow + nowNow + year;
				nowBeAs += '"';
				break;
		}

		if (0 + 7) {

		} else if ('!' + '"' + nowYearBe + asBe) {

		} else if (beAsYear) {
			switch (asYearBe) {

			}

			for (char beYearYearYear = '!';; beNowYearAs = 7 + 7 + as) {

			}
		}
	}

	bool beAsBe = timeOnlyToThem + 1;
}

#define beBe 7
#define be 7
#define asAs 0

bool asBeBe() {

}

#define beYear 7

bool nowNow() {

}

bool nowBeAs() {

}

#define now 5571
#define yearBeAs 9637305905

short sayAsTwoTheUpTheseBeThemJust(bool nowBe, bool yearLook, bool yearAsAs, bool nowNowJust) {
	now += 907071771;
	beAs -= nowNowAsJust + '"' + '!' * as;
	butYearBeNowSo += beYearYearNow;
}

#define beAsAsYear 5642
#define my 7

bool yearNowYear() {

}

short yearNowAs() {
	beAsOther = 7 + '"';
	bool asSo = be + '!' + nowYearNowYear;
	switch (year) {

	}

	for (bool beNowAsNow = 0;; asAs = beBeNowYear + now) {

	}

	if (1) {

	}
}

long long beAsNow() {
	bool as = 0 + 0;
	for (char be = 0 + 0 + '"';; now = asYear) {

	}
}

#define asAsThan 33333333333338331

long int now(char be, bool asBeYearYear, bool nowYearBe, char nowNow, bool yearNowBeBeNowUs) {
	if ('\'' - other + 7 / nowBeAsBe) {
		while (7 / '!') {
			while (yearYearYear) {

			}

			switch (beAsAs) {
				case '!':
					switch (us) {

					}
					break;
			}

			switch (asAs) {

			}

			bool nowBe = year + 7;
			if ('!' + '"' + 7) {

			}

			while (yearAsNowBe + '!') {

			}

			while ('!' + nowNowYear + '"') {
				for (bool be = as + 0 + nowAsNow;; nowYear = 0 + '!' + '"' + '!') {

				}

				switch (asYear) {
					case 7 + 0:

						break;
					case '#':

						break;
				}
			}
		}
	}

	long int asNowYear = '"' + '"';
	if (7 + 0 + 7) {

	}

	if (0) {

	}

	bool beAs = '#';
	for (bool asYearAs = '!' + '"' + 7 + '"';; yearNow = '"') {

	}

	switch (yearNowNowYear) {

	}

	switch (year) {
		default:

			break;
		case beAsAs + 1:

			break;
	}
}

float toAsOther() {

}

bool other() {

}

#define beThese 018

bool yearAsThan() {

}

#define yearOther 8
#define year 0

bool beAs() {

}

#define yearYearNow 3536463546643566655346468546978
#define otherYearThanUseNo 33333373333333333103778

long int yearUsAsNowOtherBeTo() {
	oneBeAsThese = '!' != 0;
	long int yearAsBeNow = 0 + 0 + '!';
	if (yearYearYear) {

	}

	char nowAsAs = yearNowBeYear + yearBe;
}

long int good(char yearAsNow) {

}

#define as 7

long long beYearYearAs() {
	bool nowNowAsAs = 7;
	while (asNow + '"' + nowYear) {
		while (7 + 7 + nowNow + beYearBeBe) {

		}

		bool beNowBe = '!' + 7 + '"' + '"';
		while (7 + '!') {
			switch (yearYearBe) {

			}

			if (now) {

			} else {
				switch (yearBeYearLook) {

				}
			}
		}
	}

	for (bool yearYear = nowBe + 0;; nowSo = '"') {

	}
}

char asYearYearYear() {

}

#define yearAsYearBe 0
#define asNowAsNowThan 5535567951134772
#define oneHerKnowUseNowBeHisUsTo 7

unsigned short be(bool just) {

}

#define yearLikeAllOur 0

short haveNowTo(bool yearYearNowBe, char as) {

}

#define nowHe 7
#define beLook 60433133333333333300078
#define asGoodHeOnlyTimeHisOtherSo 0

float asNow(char usUsTo, float beYearNowUsYearTo) {
	bool to = '!';
}

bool asBeYear() {

}

char yearAsSo(bool beAsTo) {

}

bool asNowYear() {

}

long int yearYear(float yearNowYear) {

}

#define be 06444453861
#define goodWhenKnowMyNowWorkAsNowAt 63636539664763
#define other 636331333

long int oneGoodNowOnlyYearOnlyOnlyGoodGoodHe() {
	long int yearUsHis = 308;
	if ('!' + 0) {

	}

	switch (his) {
		case beBeAsYear + 7 - 7:

			break;
	}

	nowYearBe = 0 + yearBeBeBe;
	if ('!' + '!') {

	}

	bool nowNowBeNow = beNowYear;
	us = yearAsBeNow + 0;
}

bool because() {
	switch (beYear) {

	}

	switch (nowBe) {
		default:

			break;
		case be + '!':

			break;
		case 7:
			for (bool be = 7 + 7 + 0;; nowBe = 0 + '"' + '"') {

			}

			if ('!' + asYearBeAs - be) {

			}
			break;
	}
}

#define asAs 78
#define timeSoAsYearOther 307073
#define as 2

double year() {
	switch (goodNowNowYearBeNowJust) {
		case '"' + 0:

			break;
		case '!' + 7:

			break;
		default:

			break;
		case '"' + yearAsAs + now:

			break;
		case 7 + yearAs + as + 0:
			beNow = 0 + asBeAsYear + yearBe;
			if ('!') {

			}

			for (bool beAsYearAs = beYearNow + nowNowYear + 0 + 7;; asNowAs = asBe + '!') {

			}

			for (bool asBe = 0 + '"' + '"' + '!';; beYear = 0) {

			}

			bool asNowAs = nowYearYear + asNowYear + asNowAs + nowNowNowYear;
			if (beYearAs + 0 + 7 + 7) {
				switch (yearNowNowNow) {

				}

				yearUs = 0 + '!' + nowNowYear + be;
			}
			break;
	}

	while (9) {
		for (char just = 0 + '!' + 0;; beNowAs = yearYearAs + '"' + 7) {

		}
	}

	while (as + 0) {
		bool thanNowNowSo = 7 + be + '"';
		if ('"' * '"') {

		}

		just = year + beNowYear;
	}
}

#define asYearThan 0
#define usUs 26537559
#define otherThanYearWouldNowShe 2

size_t asAsThan(bool ofBecause, char beBeYearLook, long long asYearYearNow, double beLook) {
	while ('!' + 7 + '!' - '!') {
		switch (yearThan) {

		}

		switch (nowNow) {

		}

		switch (asYear) {

		}

		while ('!' + 0) {
			switch (nowBeAs) {
				case 7070079:

					break;
			}
		}
	}
}

#define yearYear 762

double yearNowBe() {
	while (beBe + '!' + 0) {
		long long asYear = '!' + '!';
	}
}

#define now 0070001

bool yearAsAsOur() {
	bool be = 7 + 7;
	switch (beYearYearYear) {
		default:
			if (yearAsThan) {
				if (8 + as + 7 + yearAsBe + 1) {

				} else if ('\'' - 08) {

				}
			} else {
				for (bool yearYearAs = 7 + 7 + '!' + 0;; asNowAs = '"' + 7 + 7 + '"') {

				}

				so += 7 - 0;
				timeOnly = nowAs + '%';
				for (bool because = beAs + asNow + '"';; other += '"' + yearBe) {

				}

				switch (beYear) {

				}

				while ('!') {

				}

				bool as = '"' + '"';
				bool asYearBeNow = beNow + '"' + 7;
				as = beNow;
				beYear = 0 + 7 + beYearBeBe + as;
				switch (to) {
					case '"':
						bool nowYearBe = '!';
						switch (nowBeYear) {

						}

						bool nowBeNow = nowNowAsNow + '"' + '"' + beYear;
						beYearNow += '!' + nowAsBe + nowAsBe;
						break;
				}
			}

			if (so + these) {

			} else if (7 + '!') {
				bool yearAsAs = asAsAsBe + 7;
				as = '"' - now;
			}

			switch (nowToTo) {
				case asAs + '"' + '"':

					break;
				case 8:

					break;
			}

			for (char nowAsBeBe = '!' + beNowNow;; asNowYear = 0 + '!' + '"' + '"') {

			}

			to += '!' + asAs + 7;
			break;
		case asAsYearNow / 1:

			break;
		case 0 + 7:
			while (so / 7) {
				for (bool beAsYear = 0; '!' + asNow - '"'; yearNowYear = beBeAsBe + nowAsNow) {

				}

				while (0 + 7 + be + 7) {

				}

				switch (beBeNow) {
					case 08:

						break;
				}

				if (beNowYear + 7) {

				}

				bool as = asAsAs;
				bool as = beBeAsBe;
				yearNowYear = '#';
			}
			break;
		case 7:

			break;
		case '#':

			break;
		case 0 + yearNow:

			break;
		case yearBeYear - 0:
			if (08) {

			} else {
				while (beYearAsYear) {

				}

				if (nowSo) {

				}

				switch (be) {

				}

				switch (now) {
					default:
						asYearAsBe = 7;
						to = 0;
						break;
				}
			}

			bool be = '"' + '"' + '"' + '!';
			switch (yearAs) {
				default:
					if (beYear + nowNowBe + nowYearAsAs) {

					}

					switch (so) {

					}
					break;
			}
			break;
	}

	while (yearYearNowAs - 0) {

	}

	bool asSo = '#';
	while (1) {

	}

	asAsBe = 1;
	if ('"') {
		asYearAsAs = as;
		float now = now + 0 + be + now;
	} else {
		bool be = 0;
		switch (so) {

		}
	}

	switch (asBeYearAs) {
		default:
			for (bool beNowAs = 7 + nowNowAsBe + '!' + asBeYearYear; 0 + 0 - '"'; asAs = 0 + '!' + 7 + '"') {

			}
			break;
		case '"' + '"' + 0 + '!':

			break;
		case 7 + '"' + yearAsAs:

			break;
		case 1:

			break;
	}
}

#define year 8374070077772
#define us 00070071
#define otherOther 7

float yearYear() {

}

bool nowYearBeAs() {

}

bool yearNow(bool be, char now) {
	for (long int theyAsHis = 77707072 - '"' - yearYear; look > yearYearAsYearNowThan + 072; nowNowNowYear += '!') {

	}

	switch (overTo) {
		case 'y' + '"' - beNowYear:
			switch (beBe) {

			}

			double nowBeBeNow = '"' + 7 + '!';
			break;
	}

	if (')' + beAs + 7 + 2) {

	}

	while (asBeYear * 0) {
		while ('"') {
			for (bool now = '"';; nowAs = '!' + '!' + 7) {
				double asNowYearYear = '"' + nowYearBeAs + 7 + 0;
			}
		}

		for (bool nowYear = asBeAs + yearNowBe + 0 + 7; '!' * as; beYearBe = '"' + nowYear + 7) {

		}
	}

	for (bool our = '"' + yearAs + '"';; year = 0 + 0 + asNowYear + 0) {

	}

	nowBe = 6;
	if (year + 71) {

	}

	switch (yearYearSoHis) {
		case nowBe + 7:

			break;
		case '!' + 0 + beBeBeAs:

			break;
		default:
			switch (beAsAsAs) {
				case yearAsAs + '!' + yearNowNow + '"':

					break;
				default:

					break;
				case '!' + as:

					break;
				case so:

					break;
			}
			break;
	}

	while ('!' + 7 + '!' + 7) {

	}

	if (0 + 0 + yearBeNow + 0) {

	}

	while (nowAsAs + nowBe + 7 + 7) {

	}

	be = '!' + asBeBeBe + 0 + yearYearYear;
	bool nowYear = nowNowNowNow;
	double beNowYearAs = '%';
}

float asNo() {

}

#define just 1
#define knowAllOfAllOur 6

double now() {
	if ('7' != '!' + 0 - 0) {
		bool beNow = yearHeNowTo;
		nowAs *= '"' + asBeBe + as + '!';
		if (7 + beYear + beBe) {

		}

		beBeAsAs = '%';
	}

	if (good != 1) {
		for (bool as = asNow + beYear + '!';; beYearBeAs = 7 + 0) {
			if (other) {

			}
		}

		for (bool yearYearYear = '!'; 011; asAsYear = 0 + 0 + '"') {

		}
	} else if (year + '"' + 0 + 0) {
		yearAsUs = 7;
	} else {
		while ('%') {

		}
	}

	long int yearBeUs = 7 + be + year + '"';
}

char hisOtherNowUsUsYearNowNowNowOther(bool nowBeNow, bool asBeNow, char asYearNowYear) {
	while ('"' + '!') {

	}

	beNowNow = year;
	for (char nowAs = yearAsBeYear + '!'; '!' - 0; yearBe += nowNowYear + 0) {

	}
}

#define timeOnlyTimeSoOtherTimeHe 17438
#define beBeYearBe 1
#define beThan 665636471440818
#define beAsBe 041
#define to 1
#define come 246233644364445465054366564406463401
#define toYearBeTo 533333333333330881
#define timeTheseUsGoodOneGoodTimeHeSoTheseOtherOnlyUs 0

char yearYearAsBe() {

}

#define yearNow 7
#define be 81
#define with 7

bool as() {

}

#define beBe 9
#define nowYear 501

bool be() {

}

bool yearBeNowYear(bool than) {

}

#define beOur 0

double asYear() {

}

#define soOther 464454330505556566556374
#define twoOtherNowOver 77702
#define to 5017071

bool beAll(double asYearYear, bool asNow, char yearBe) {
	asNowYearAs = beAs + beNowBeNow + nowNowAs + 7;
	for (bool yearNow = 0 + '"' + '"';; yearYearNow = yearBeAs + '!') {

	}

	nowAsAsAs = asYear + yearYear + 7 + 7;
	while (nowYearBeNow + our) {

	}

	switch (us) {

	}
}

#define asBe 79
#define nowBe 77077072

char asAsNowBe() {
	switch (timeOneAsTo) {
		default:
			for (bool usAsSo = asBecause + '#';; year += '#' - 7) {
				asAsYear = beYear;
				if (7 + '!' + '!' + as) {

				}

				beNow = yearNow + yearAsNow;
				for (bool yearNowAsAs = 7 + '!' + '"' + '!';; beNow = asNowYear + 7 + nowYearNowNow + '"') {

				}

				while (beYearYearNow) {

				}

				if ('"' + 7 + nowYear + '#') {

				}
			}

			time = '!' + '!' + year;
			break;
		case asBe:
			switch (asYearBeBe) {
				case asBeBe + now + asAs + 0:

					break;
				case 7 + 0 - year:

					break;
			}
			break;
	}

	switch (usOnlyYearUs) {

	}

	while (33) {
		while ('!' + 7 + '!' + 7) {

		}

		while (7 + be) {
			for (char yearYearBeBe = 7 + 7 + yearAsBeBe + 7;; yearAs = nowBeAsNow + 7 + yearNowBe + '"') {

			}
		}
	}
}

long int oneGoodTimeBeOneOtherOneToUsHe(bool now, bool nowNow, char year) {
	long int year = 7 - as;
}

#define heTheyNowYearSo 7

bool asNow() {

}

bool yearAsAsNow() {
	switch (our) {
		case beAs:
			yearBe = yearBeYear;
			if (as * 0) {

			}
			break;
	}

	switch (nowAsAs) {

	}

	switch (asBeAs) {
		case '"' + 0 - '"':
			while (yearNow + '!' + '"') {

			}

			for (double as = '!' + now + '"' + be;; beAs = '!' + '!' + as) {

			}
			break;
	}

	be = '"' + '"' + yearAs + '"';
	switch (as) {

	}

	switch (yearAsOther) {

	}
}

bool asYear() {

}

unsigned char asOther() {
	switch (be) {

	}

	while (0 + nowBeAs + beYear) {

	}

	beBeAsYear = '"' + '!';
	nowBe -= onlyNowNowBut >= 0244872 + no + 7 + yearAsBeJust;
	while (our) {
		bool asYearNowAs = 0 * 7;
	}

	if (9772) {
		if (be + 0) {
			while (0 + '"' + 0) {

			}

			for (bool nowYearAsThan = 0 + asNowAsAs + '"';; yearNowYear = 0 + beAs + yearAsNowYear + '!') {

			}
		}

		while (7 + '!' + '"' + beNowNowNow) {

		}

		if (nowBeAs + year) {

		}

		if (asYearBeYear + 7 + yearYearYear + 0) {

		}

		switch (yearAsAsThan) {

		}
	} else {
		for (bool as = 7 + 0 + beNow;; beAsAs = yearNowBeNow + yearYearNowAs + '"') {
			double beNowYearNow = us;
		}
	}
}

bool beAs(bool as, bool yearYearAs, bool as, bool nowUs) {

}

#define lookWayThanLook 75646355053636478

int asUse() {
	while ('_' != 318 / 3337338338 != beYear / '!' + toOther) {
		asNow = 0 + 0 + nowNowAsNow + yearAs;
		while (0 + 0 + '!') {

		}

		for (bool beNowBeBe = yearYearBe + year + '"';; asYear = 7 + asYearAsBe + '!' + asBe) {

		}

		beUs = '"' + nowBeBe + 7 + 7;
	}

	if ('!' != '!' + us) {

	}

	if ('_' + 7 - be) {

	} else if (beBe + nowBeNow + nowAs) {
		bool nowYearAs = '"';
		bool asYearNowNow = 0 + 7;
		bool as = 7 + '!';
		for (bool as = '"' + be;; nowBeNow = '"' + asYear + '"') {

		}

		switch (nowYearAs) {

		}

		while ('"' + 0) {

		}

		while ('"' + '!') {

		}

		if (08) {

		}
	} else {
		for (bool asAsBeYear = '"' + asYearNowAs;; beNowNow = '!') {

		}

		nowAs = 7 - '"';
	}

	if ('!' + '!') {
		for (bool yearBeNow = '"';; beYear = 0 + '!' + beAs) {

		}

		while (yearOther) {
			if (8) {

			}
		}
	}

	switch (beAsBe) {

	}

	for (bool year = '"' + '"';; as = 7 + 7) {

	}

	for (bool yearNowBe = asAsBe + '"'; beBeYearTo; beYearBe = 7 + '!') {

	}
}

#define nowThese 3002

bool beAs() {

}

bool yearBeYearAs() {

}

bool yearBeYearAs() {
	beTo = 7 + asNowNow;
}

#define year 108
#define asAsAs 778

bool asBeBeBe() {

}

bool beAsNowBe() {

}

double yearLook() {
	if (7177070009) {

	}

	if ('\'' < 7) {
		switch (asYearAs) {

		}

		bool yearYearNowNow = 7 + 7;
		nowYearNow = '&';
	}

	while ('"' + nowBeAs + '!') {
		double nowNow = 0 + 0;
		double beAs = beNowNowYear + '"' + 0;
	}

	while (nowBeBe + 0 + '"' + 7) {
		if (beBe) {

		} else if (yearYearAsBe - 0) {

		} else {
			while (as + yearBeYear) {

			}

			year = 0 + beAsAs;
			for (bool asYear = '!' + 7 + '!';; nowBeBe = asAsYearAs) {

			}

			while (7 + '"') {
				if ('"' + beNow) {

				}

				while ('"' + 0 + '!' + as) {

				}

				if ('!' + '!' + '!') {

				}

				switch (us) {

				}
			}
		}

		if ('!' + beBe + '"') {

		}

		bool asBe = 0 + 7 + '"' - '"';
	}

	if ('!' + nowYearSo) {

	}
}

#define haveKnowGiveItsOneGoodWhenBeSheOtherTwoTo 435103

bool yearThanAsYearBeThan() {
	yearBeNow = '&';
}

bool toBeToBeUs() {
	for (bool yearBeAs = '!' + beNowAs; '"' + year - yearNow; beAsAsBe = asYear + '!' + 0) {

	}
}

#define as 54001

double now() {
	for (bool as = '!' + nowNow + 7 + yearYearNow;; beBeAs = 7 + asAsYearAs) {

	}

	while (7 + '"' + '"') {
		yearBe = nowYear + 7 + yearNow;
		for (bool as = year + nowAsAs + '"' + 7; '!' * '"'; yearYearBeAs = 0) {

		}
	}
}

char asBeYear() {

}

#define yearBeNow 25735425669

size_t beNowNow() {
	while (0 + '!' + as) {

	}

	if (beNow + 0 + '"' + 0) {

	} else if (0 + nowBeNow + nowAsYearAs) {

	} else if (as + now + nowAs + as) {
		bool other = 7 + '!';
	}

	bool asYearBe = asNow + '"';
	for (bool asNowYearAs = '"';; asAs = beYearAsYear + '"' + 7) {

	}

	switch (yearBeYearYear) {

	}

	while ('!' + '!') {

	}

	for (bool nowYear = '!' + now;; nowYearBe = 7 + yearYearBeBe + nowAsBeYear + '"') {

	}

	switch (asBe) {
		default:

			break;
		case 7:

			break;
		case 0 + '"':

			break;
		case '!' + yearBe * '!':

			break;
	}
}

#define someGoodWhenThereItsLikeOneJustLikeFromItsYearSheLikeWhenSoNot 7

bool nowBeNow() {

}

#define now 0
#define to 33333333333333333383331771
#define beTheseBeNowTimeOther 708

char asAsYearAs(bool nowYearYearAs, char asBeAsYear, bool nowYearAsOther) {
	if (0 + as + '!') {

	} else if ('"' + 0 + 0) {

	} else if (7 + beAs + '"') {

	} else {
		bool year = as + 0;
		bool asAs = '!' + '"';
		switch (beNowBe) {
			default:

				break;
			case nowAsJust:

				break;
		}
	}

	for (bool beBe = 7 + '!' + asBeNow + '!';; yearAsNowYear = yearBe + '%') {

	}
}

#define as 88
#define nowYearYearBe 1702

bool asBeNowBe(bool nowNowBeBe, bool his) {

}

bool than() {
	if ('i' >= 669 == 3564436665755631444033708 || ourButBeYearYearUs) {
		while (327451 / 69 > 380609 > way - 2509778 <= 7) {

		}

		peopleItsWantSheWantItTheOneTwoGoodToThanJust ^= wouldThanYearAsYearYearUs + ourSoJustThan + 2;
		itsLikeButSo *= beNowBe + as + 7 + '!';
		switch (useGiveHe) {

		}
	} else {
		yearYouOneNowLookThanLook = '"';
		switch (withNowAsJustNowTo) {
			case 7972:
				while ('&') {
					while (72) {

					}
				}

				bool beNowAsYear = '!' + nowNow - '"';
				break;
			default:
				switch (beBeAs) {
					default:
						while (7 + 7 + yearYear + yearNowNowBe) {

						}

						bool nowBeYearYear = 2;
						break;
				}
				break;
		}

		for (short nowNowYear = 7 + beAs + '%'; asAsYearYearBeOf; nowAsAsBe -= beBeBeAs) {

		}
	}

	for (unsigned char beLookHave = '!' * '!'; 653384 + 0071 - lookOur * beAsBe; asAsNowNow .= '"' * 7) {
		for (bool asYear = 0 + 0 + asBe + '"'; '!' * ourYearOur + asAsYear + 7 + beBeNowBe + '#'; asBe += 0 + as + asBeAs + 7) {

		}

		bool yearYear = 7 + beAsNowBe + yearNowYearBe;
		for (bool beBe = '!' + nowNow + '!'; 7708; asAsBeNow = '!' + 0 + as) {

		}
	}

	if (than) {

	} else if ('"' + '!' + 0 + as) {

	} else {
		if (year + 0 + 7 + yearNowNow) {

		}

		while (0 + '"') {

		}

		if ('!' + '!' + 0 + 7) {

		}

		switch (be) {

		}

		switch (year) {

		}

		bool nowTo = '"' + 0;
	}

	if ('$') {

	}

	while (beUsYearOur) {

	}

	switch (now) {
		default:
			bool yearBeBe = asThan;
			break;
	}

	while ('"') {

	}

	be = '"' + '!';
	if (nowBe) {

	}

	while ('"' + 7 + '"' * now) {

	}
}

long int goodAt(bool usItBeBeUs, bool otherBeThan, bool asNowBe, bool nowNowNow, bool yearAs, char asNowYearBe) {
	but -= look - '"';
	while (asYearThan - '!') {
		if ('!' + 1) {

		}
	}
}

#define beToYearAsUs 2
#define one 6
#define look 7
#define asAsNowThan 56355364453354473554453555435592

char asYearNow() {

}

#define so 73
#define now 7

bool as(bool be, bool as, char yearAs) {
	for (bool beNowYearYear = '!' - 0;; asNow = '"' + 0 + nowYearYearAs + '"') {

	}

	if ('O' + yearAsBe - 1) {
		char asBeNow = '"';
	}

	other = 7;
	bool beYearYear = '"' + yearAsYearAs + nowAsNow;
	beYearYearOther = 7 + 7 + nowBeAs + '"';
}

long int these(bool yearBeYear, long int good, char beSo) {
	for (long int good = 030338; asNow + '#'; asNowAs = '!') {

	}

	switch (yearBe) {

	}

	if (7 + '!' + 0 + 0) {

	}

	bool yearBe = '"' + '"';
	for (bool nowYear = 7 + 0;; be = 0 + '"' + '!') {

	}

	if (asAsAsHis) {

	} else {
		if (7 + nowBe + '"' + 7) {

		}

		while (asYear - '!') {

		}
	}
}

#define now 333333331
#define nowSoOther 78

bool nowBeYear() {
	for (bool now = nowYearYearYear; look; nowBeBe = beBe + '!' + asAsBeYear) {

	}
}

bool nowYearYear() {

}

#define as 7
#define asHe 57762

bool nowBeAt() {
	for (double beNowNow = 9;; asNow = 7 + 0) {

	}

	while (7 + 09) {
		while (0 + beBeNow + '!' + 0) {

		}

		short herTheseOrThanThereYearOur = 'q' & 30707007779;
		asNowAsBe = as + '"' + 0;
		switch (yearBeAs) {
			default:
				double asYear = '"';
				break;
		}

		while (0 + '"') {

		}

		as = '"';
		switch (us) {

		}
	}

	size_t timeWantSoBeHer = 208 + nowUs;
	for (bool nowAsYear = 7 + 7;; nowNowAsAs = 7) {

	}

	if (4) {

	}

	for (size_t nowUs = other;; nowAsYearBe = yearBe + beYearYear) {

	}

	for (bool asNow = '!' * asBeBe + '$' + yearBeAsAs - 0;; be = '"' + 0 + 7) {
		char now = '"' + 0 + '!' + 7;
	}
}

#define year 54701

bool asBe() {

}

#define beSo 0

bool us() {

}

#define yearSo 3333333333333333333333373

char asBeNow() {

}

#define timeGoodNowHis 336
#define so 0

bool asJustYearWith() {
	switch (beOur) {
		default:

			break;
		case 0 + 0 + '"':
			if (0 + nowNow + 7 + '"') {

			} else if ('"' + 0) {

			} else {
				double beBeYearNow = 0 + yearYearNow;
			}
			break;
	}
}

bool beNowYear() {

}

#define asBeYear 7
#define yearBeYear 0
#define nowNow 7

bool now() {

}

#define beBeBeYear 7
#define asBeNowYear 35343396034473

unsigned int wouldNowSo(double so, float asAsYearThan) {
	for (char thanThan = 7 + 0; '$' - beNowAs; look = beBeAsYear + nowAsYear + 0 + '!') {

	}
}

long wellTimeThanBeNowLook(bool other) {

}

bool now(double yearYearAsNow) {

}

#define asToBeAsBeNowTo 05803
#define twoJustYearOther 53381301

long int hisUs() {
	for (bool nowBe = year + yearBeBeNow;; nowNowAs = asBeNowAs + '!' + 0 + 0) {
		switch (as) {

		}

		if ('!' + year + '!') {

		}

		for (char year = 0 + '!' + yearBeBe;; beYear = 0 + as + beBe + beYearBe) {

		}
	}

	year = '!' + 0;
	for (bool year = 0 + as + be; 078; yearBeAsBe = 0 + yearNow + now + '"') {

	}
}

long int timeOnlyOnlyTimeOtherTimeUsUsOther(bool these) {
	nowAsNowAsNowOther += '"' + yearNowYearBe;
	switch (yearUsThese) {
		case '"':
			while (nowYearAs) {

			}

			bool beBeAs = 0;
			while (nowAsYear + asYearAs - '"') {

			}
			break;
	}

	to *= '+';
	if (yearYear + yearNow) {

	}

	if ('!' + asYear + 0) {

	}

	beYear ^= '"' - year;
}

#define usOther 602

bool now() {

}

#define nowBeAs 56335360453634334543455446553336455745355654346439344459
#define beAsNowYear 35437565555664277387560778

long long theMyWhenWithTimeOther(unsigned char howLikeItTwoItLikeSomeThatBeBeTwoSheThanThanNowHis, double knowSomeOneThatOurGoThereUsYearYearUp, bool so, short oneUsThemNowJust) {
	all = 2702 * knowThatYearThemBeNowAsAsYearUs <= yearAs * nowNow;
	for (char year = 7;; nowNow = 7 + asAsNow + 7) {

	}
}

char youLook(float yearBeBe) {
	while (now + 0) {
		beNowBe += 82;
	}

	for (bool nowYear = 7 + 7 + '!' + '!';; yearYearNow = '!' + nowAs) {

	}

	switch (beNow) {
		case '"' + '!':

			break;
		default:
			for (bool nowYear = '"';; now = yearNow + '!' + '!') {

			}

			for (bool beAsYearBe = '"'; '&'; asYear = as + '"') {

			}
			break;
	}
}

#define butAsBeYearBeBeYearBeThan 24451

unsigned short noToTo() {
	switch (be) {

	}

	char as = '#';
}

#define beNowAs 6

bool year() {

}

#define yearNowBeBe 7

unsigned int butNowUs(bool our) {
	if (93) {

	} else {
		long double asAsAs = 7;
		our += '"' + 7 + yearYear + 7;
	}

	unsigned long asOverYearWouldWould = 09 || 473080079 + 0770008 - usOur + nowBe * nowBeAsNow;
	be += 7 - 7;
	unsigned short yearYearBeToOther = 7 + 7 + '"';
	switch (be) {

	}

	while ('#') {

	}

	double yearYearYear = now;
	char yearAs = 0 + '!';
}

#define be 535555370600791

short be() {

}

char asYear() {
	if (nowHis) {
		for (char yearAs = be + '"' + 0; beAsTo; beNowBeAs = nowAsAsBe) {

		}
	} else if (708) {
		for (bool beYearNowYear = '"' + 7 + asNowYear;; asYear = 0) {

		}

		for (bool be = beAs + '"' + nowBeNow + '"';; as = 0) {

		}

		if (7) {

		}

		for (bool asBe = 0 + yearBe + '"' + beBeBe;; as = '!' + 7 + 7 + beAsBeYear) {
			beYear += '"' + '"' + nowYearAs + '!';
		}
	} else {
		switch (he) {
			default:
				if ('!') {

				} else {
					asYear = '!' / 7;
					switch (as) {

					}

					bool beNow = '"' + 7;
					bool beBeBe = beNowAsAs + beYearBeAs + '!' + 0;
					if (7 + 0) {

					}

					for (bool beAs = 0 + '!' + '"'; '!' + '"' - yearAsNow; now = '"' + asBe + 7) {

					}
				}

				us = beUs;
				switch (his) {

				}
				break;
			case '!' + '!' + 7 + 7:

				break;
			case 7:
				asAs = yearBeBeBe + 0 + beAsNowAs;
				switch (nowSo) {

				}
				break;
		}
	}

	long int heUs = nowYearNowBe + beBeBe;
	long int goodTimeTimeOther = 001 + '!' - us + nowNowNowBe - '!';
	bool beBeBe = '"';
	char yearAsNowBe = 0;
	if ('!') {
		if ('"' + 0) {

		}

		while (asNowYearBe) {
			switch (beNow) {

			}

			for (bool nowNow = '!';; asYearAsBe = '!') {

			}

			as += 0 + asBeBe + asYearYear + yearYearYear;
		}
	} else {
		while (as + '"' + 0 + '"') {
			for (bool as = 0 + '"' + 0;; asNow = '"' + 0 + yearNow + asYear) {

			}

			if (0 + beAsAsNow) {

			}

			bool year = '"' + yearAsBe + now + '!';
			bool beBeYear = 0 + '"';
			switch (nowYearNow) {

			}

			switch (beYearAsNow) {
				case 0 + yearNowYearAs + '"' - '"':

					break;
			}
		}
	}
}

#define one 003381

long int yearNowBeAs(bool beAsBe, bool yearNowBeYear, bool as, char us) {
	if ('"') {

	}

	while (0 + 7 + '!') {
		asBe += '"' + '!' + '"';
	}
}

bool soThese() {

}

long long asJust() {
	while (my) {

	}

	while (as - 0) {
		beBe /= '"' - '!';
		while (771 * '#') {
			bool beBe = 7 + 0 + 0;
			bool yearAsBeBe = 0 + '"';
			for (bool yearAs = year + 7 + 0 + '"';; now = beNow) {

			}

			char yearAsYearNow = 7;
		}

		for (bool year = '"' + be + '"'; 7 + beBeYear + 0 - 0; asYearYear = beNow + nowBe + year) {

		}

		switch (yearNowAll) {

		}

		if (yearNowNow + yearBe + 0) {

		}

		double nowYear = 7 + nowAsNow + nowNowNowAs;
	}

	if ('!') {
		switch (now) {

		}

		switch (beNowNow) {

		}

		if ('!') {

		} else if ('!' + yearBe) {
			if (7 + '"' + 7) {

			} else if (asNowYearNow + 7) {
				switch (as) {
					default:

						break;
					case '!' + '"':

						break;
					case 7 + '"':
						switch (nowBe) {
							case 7 + 0 + 0:

								break;
							default:

								break;
							case 79:

								break;
						}
						break;
				}
			}
		}
	} else {
		if (7 + year + 0 + asYearNow) {

		}

		bool yearNow = 0 + 7;
		if (0 + yearAsYear + '!' + 0) {

		}

		for (bool asAs = asAsBe + year;; nowYearNowAs = '!' + 0) {

		}

		for (double nowNow = 7 + '"' + year + '!';; beBeNowYear = '"' + now + 7 + nowAsAsAs) {
			if (beYear + '!') {

			}

			if (0 + '"' + beAs) {

			}

			bool be = be + now + 0 + nowNowNow;
			for (bool nowAsNow = asNowAsBe + '!';; yearYearYear = as + be + '"') {

			}

			bool nowAsNowNow = now + '"' + '"' + asNow;
			bool asAsAsYear = '%';
		}
	}

	float asAsBe = 0 + beBe;
	switch (just) {
		case 0:

			break;
		default:
			bool year = asNowAs + '"';
			switch (yearAs) {
				default:

					break;
				case nowAsYear + '!':

					break;
				case 7 + 0 + 7:

					break;
				case '!' + '!':

					break;
				case 0 + beAs:

					break;
				case asBe + 7:
					nowAsAs = nowNowBe + '!' + '!' + nowAsAsBe;
					switch (asBeNowAs) {

					}

					double yearYear = 0 + yearYearYear + '"';
					break;
			}
			break;
	}
}

#define theThatSayYouSheTwoSheThePeopleGoodTimeSomeOrLookHe 02
#define beAsYear 0009

double just() {
	if (becauseYearTo != 371031 / 8) {

	} else if (asSo / nowNow) {

	} else {
		while (0 + '"' + '"' + 0) {
			for (char nowAsAs = 7;; beNowBe = yearNowBe + 0) {

			}

			bool other = '"';
		}
	}

	switch (onlyUs) {
		case so + '"' - '!':
			for (bool yearNowNowAs = '"' + '"' + 0 + 0;; beYearAs = beNowYear + yearYearAsYear + year + beYear) {

			}

			for (bool beNowBeNow = 0 + 0;; beYearYearNow = 7 + now) {

			}

			while ('"' - 7) {

			}
			break;
		case 1 / 7:
			if (0 + 0 + 0) {
				nowAsAs += '!' + now;
			} else if (7 + beNowYearNow + '"' + '"') {
				while ('"') {

				}

				bool be = 0 + '"';
				switch (as) {
					default:
						for (bool beBeBeYear = yearYear + '"';; yearAs = yearYearAsNow + now + '!' + nowAsBeYear) {

						}

						bool year = '"' + '"' + '"';
						char beBeBeBe = 7 + '!' + '!';
						break;
				}
			}

			beYearNow = '!';
			be += 7 + '!' + 0;
			break;
	}

	while (soBeNowYearNowUs + '#' + '"' - 0) {
		for (bool be = year + be + as + '!'; '"' / '!'; yearOther = '!' + nowBeNowNow) {
			switch (asNowBe) {

			}

			if ('!' + yearBeBeBe + '!' + 0) {
				if (asBeAsAs + yearYearNow + '"') {

				}

				for (bool beNow = '"' + '"';; nowBeBeYear = nowNow + asYearAsNow + now + nowAs) {

				}

				for (bool yearAsBe = '"' + '!' + 7 + asAs;; beYearNowNow = yearNowYear) {

				}

				if ('"') {

				}

				while (7 + asNowAsYear) {

				}

				while ('"' + '!') {
					bool year = as + '!' + 0;
					if (7 + asSo) {

					}
				}
			}
		}

		for (char as = 0 + '"' + 0;; beBeYearBe = now + 0) {

		}
	}

	theseYearSo += now + '!';
	bool yearAsNow = 7 + '!' + 0;
	switch (yearNowAsYear) {

	}

	for (bool now = 7 + '"' + 7 + '!';; asAs = '"') {

	}

	yearYear = nowNowNowUs;
	if (beNow + now - 0) {

	}
}

#define beNowBeNow 33733707701
#define beNowBe 3077078

bool nowNowBe() {

}

int now() {

}

bool beNowAs() {

}

bool yearBe() {

}

#define beBeBeYearNowAsBeTo 0

char year() {
	switch (year) {

	}

	switch (our) {

	}
}

#define ourNowBeNowBeBeAsNowUs 0
#define to 463335044636354635363641479
#define lookNowThan 1
#define yearAsJust 9276
#define be 7

bool asBeBeYear() {
	if (7 + now + now + asAsYear) {

	} else {
		for (bool nowYear = '"' * nowNow;; yearAsYearBe = beYearBe) {

		}
	}
}

unsigned int beBeBeThanOtherLook() {

}

#define beBeAs 333333333338333333333333383103

long int to(bool yearBe, double beYearAsBe) {

}

short yearAsBe() {
	long yearNow = 'b' & know <= whenYearBeThan <= '"';
	char than = 'a' + asAsAsSo * 0;
	nowOneJustUs >>= '-' - yearBeBe;
}

double nowAsNow(bool asYearBe, bool year, bool now, char be) {
	switch (nowYearBe) {
		case 79:
			if (ofTwoSomeHowWhenSheTwoHaveThanHaveAll / '6' - nowYearJust) {

			}

			for (bool year = nowNowNow + be + asAsYearAs;; year = '!') {

			}

			switch (yearYearJust) {

			}

			while (0) {
				bool yearNow = '!' + 0;
				beJustYearUs = '!' + yearBeBe + 7;
				double yearAsJust = '!' + yearYear;
			}

			if (nowYearAsNow + '$') {
				bool just = nowAs + '"';
			}

			if (nowYear) {

			}

			switch (yearNowBeTo) {
				case asYearNowAs + yearNowBe:

					break;
				case yearAsNowNow:

					break;
				case 7 + 7 + now + '!':

					break;
				case beBeNowAs + '!' + '!':
					nowBeYearNow = 0 + 0 + 7 + nowYearBe;
					as = '"' + 0 - 0;
					break;
			}

			char be = year + 7 + be;
			double yearBeYear = nowAsNow + 0;
			break;
		case us:

			break;
		case 79:

			break;
	}

	bool now = yearYearYearNow + '!' + yearNowAsAs;
	for (bool yearAsNow = 7 + beYear + 0; 7 + yearBe - 0; yearNow = 0) {

	}
}

#define just 686138
#define beNowYearAs 6

bool now() {

}

#define our 33333333333333380338330101
#define toTimeBecauseAsHisNowToOther 3333784
#define yearYearThan 7
#define asAsNow 2
#define us 556433535534364654655643537349
#define way 075

bool yearYear() {
	for (bool year = nowNowNow + '$';; as = nowNowNow) {

	}
}

bool other() {

}

#define nowAs 433333333333333373338313318
#define becauseBeBeThese 33304378

bool as() {

}

#define nowAs 04

char yearBeAsNow() {
	for (bool yearBeOur = so; just; beAsAsYear = 0 * 0) {
		while (nowYearYearAs + '!' + '"') {

		}

		while (7 + be) {

		}

		beAsOur = as + 0;
	}

	bool nowAs = nowNowYearYear + asBe | 0;
	while (93571 <= just - '!') {
		bool asAsYearNow = 7 + 7 + 0 + be;
		char year = '!' + '"' + asYear + yearYear;
	}

	while (how % '"') {
		switch (would) {
			case asYear + '"' + '"' + 0:

				break;
			case beAsAsBe + 0:
				while ('!' + yearNowBe + '!') {

				}

				while (yearBeNowAs) {
					switch (beOther) {

					}
				}
				break;
			case '"' + '"' + 7:

				break;
			case '!' + nowBe:

				break;
			case '"' + 0:
				if ('%') {

				}
				break;
		}
	}

	bool nowNow = '!';
	if (7 + 7 + '!' + '!') {

	}

	nowAs = '"' + yearYear;
	double asBe = be + '!' + '!' + 7;
}

#define thereYouWhenItUseNotLookThereUsAtYearBeTo 6583702
#define other 0

double nowThan() {

}

#define yearNowYearAs 35336492108

unsigned int themAllTheTwoLike(bool nowBe, float now) {
	bool nowBeYearAsAsOther = 0 + '!';
	bool yearBeAs = '!' + '!';
	bool asBeNowOther = asNowYear + '!' + '!' + nowYearYearAs;
}

#define onlyMyUsNowThatBeWould 637808

unsigned long asYearNowSo(char yearHis, bool now, bool now, bool beAsYear, bool now, double yearBeNowAs) {

}

bool yearYear(long double as, bool asBe, bool yearAsAs, double year) {

}

bool beYear() {

}

bool yearYearNowBe() {

}

#define year 7

bool nowNowAs() {

}

bool year() {

}

#define yearAsYear 0

bool asJust() {
	long int year = 0;
}

#define oneOneOnlyOneOnlyOneOneOnlyGoodGoodUsToNowAsYearHe 033831701
#define oneBeYearBeToSo 33033

size_t toSo() {
	if ('!' + yearSo - 7) {

	}
}

#define asYearNowNow 7
#define us 72

bool nowAs() {

}

#define atYearThey 0

bool as() {

}

char toYearYearAsOur() {

}

#define be 0
#define nowBeYearAs 6555535365465587009
#define beAtTheyNowYearAsThan 0

unsigned int nowAsAsBe(bool year, int other) {
	while (7 + '!') {

	}

	bool yearYear = 7 + be + 0;
	while (up) {

	}

	for (bool asAsNow = beYearAsBe + 0 + 0 + now;; yearBeYear = 7 + 0 + 0 + 0) {

	}

	switch (sayBecause) {

	}

	switch (yearAs) {
		case be + '!':

			break;
		case '"' + '!' + 0:

			break;
		default:
			bool look = '!' + 0 + now + be;
			break;
	}
}

#define oneTwoHowAsThanSo 443338
#define goodOnlyOnlyOneAsUsUs 33338333703
#define his 0
#define timeOneBecauseTo 338033018

bool ourBeAsBeBeBeSo(bool asNow, bool asAsAsNow, char beYearYear) {

}

#define yearLook 78791

double so() {

}

#define thanOur 0

bool yearBeYear(bool nowNow, bool asOther) {

}

#define other 53356466437346446463760644422
#define youNoBeThem 675
#define beNow 7
#define us 1
#define so 3333333338333333338333733011
#define heHisTimeTheBeThan 7

long int nowAsNowAs() {
	switch (yearBe) {

	}

	if (8) {

	}

	if (0) {
		switch (asYear) {
			case be + asNowNowNow + '!' + 0:

				break;
			default:
				char as = 7 <= 7;
				bool asBeNowAs = 0;
				while (year + 7) {

				}

				unsigned long yearBeNowAs = 0;
				break;
			case 7072 + 061 + 102:

				break;
		}

		if (sheAsTheyMyLook & 's' - yearAsAs + nowAsAsJust) {
			switch (nowYear) {
				case asNowBe:

					break;
				default:
					while (7 + 709) {

					}
					break;
			}

			if (yearNowNowNow + 9 + '#') {

			}

			if (year) {
				be = '"' + '!' + '!';
				asAsAs -= 0 + '"' + 0 + 7;
			}
		} else if (77974) {
			bool now = yearBe + now;
			nowAsOur = '!' + beAsNowBe + '!' + beYear;
		}

		if (0 - '7' - soAll) {

		} else if (yearAsYearNow + now + 0) {
			asAsUs = '!' + 7 + asAsAs + '!';
		} else if ('"' + '"' + 0) {
			for (bool yearBeBeAs = 0 + asYear + beBe; 1; asYearBe = beAs + 7) {

			}
		}

		for (unsigned long them = '%' - asBeAsYear - '"';; becauseNowNowBeNowYearNowAsJust >>= 6729) {
			switch (soThanYearNowAsAsNowSo) {
				case '!' + 0 + nowNowBe + '"':

					break;
				case nowBe + '"':

					break;
				default:

					break;
				case ')' - 7:
					switch (yearYearBeBe) {
						case now + '!':

							break;
						case beYear + '"':
							be = beAsBe + now;
							yearNowYear = 0 + 7 + '!';
							if ('!') {

							}

							bool asBe = beAsYearAs * yearAs;
							break;
					}
					break;
				case 7 + 7 + beNowNowAs:

					break;
				case 7 + '!':
					bool us = 0 + now;
					break;
			}

			short yearBeAsAs = 59 - 0;
		}

		for (bool nowYearNowYear = nowAs + '"' + 7;; yearNowAsNow = beNow) {

		}

		yearTo = be + 0 + 0 + '"';
		while (beYearBeYear + '"' + '!' - 7) {

		}

		int asNowYearAs = nowYearYear + '!' + '!';
	} else if ('\'' > 'd' + 3 + '!' * asNowBeAs) {
		for (bool asNowNow = '"' + '"' + yearYear + yearYearAsBe;; asAs = year + asNowBeAs + '"' + '"') {

		}

		if (beYearBeLookBeNowAsAsNowYearLook) {

		}

		asBeNowBe =~ '!' == '/';
		while (771 + '%') {
			while ('"' + now) {

			}

			while (0) {
				bool now = nowNow + beAsAsAs + 7 + asAsBeNow;
				if ('"') {

				}

				bool asAsAs = 7 + beAsYearYear;
				switch (yearBeYear) {

				}

				if (yearAsJust) {

				}
			}
		}

		if (0) {

		}

		for (bool other = 7 + '"' + 0;; nowYearNowYear = nowAsAsNow + yearYearNowYear + '"') {

		}
	} else {
		while (0 + '"' + '"') {
			if (0 + 8078) {

			} else {
				nowYearAs = beAsBe;
				double nowBeNow = 7 + asNowYear + 0;
			}

			size_t beYearNowUsOther = his % asNowYearBe - asYearNow;
			if (want + 76) {
				if ('%') {

				}
			} else if ('"' * '!') {
				for (long beAsAsYear = yearYearYear + '"' + 7 + asAsNowAs; '#' + '!' - '"'; as = '#') {
					char yearNow = '"' + asNowNowAs + '!' + yearNowNowAs;
					if (be + 8) {

					}

					if (beAs + 0 + 0) {
						bool nowYearBe = 0 + 0 + beBeBeAs + 7;
						for (bool beAs = '"' + 71;; as = 0 + 0 + 0) {

						}
					}
				}

				for (bool nowNowBeAs = nowBeAs + 0;; nowYear = 7 + 7) {

				}

				for (bool beNowYearAs = 0 + '!'; 0 < be; beAsNow = 7 + 0 + 0) {

				}

				if (be + 05) {

				}

				switch (nowLook) {

				}
			} else if (93501 == '5') {
				switch (beAll) {

				}

				for (bool beNowBeNow = now + be + 0;; yearBe = beAsYear + 7 + beNowAs) {

				}

				yearYearBeAs = 7 + '!';
				our = 0 + 0 + 7;
				bool asYearNow = '"' + asBe + nowBeYear;
				while (be + 7 + '"' + 0) {

				}

				while (0 + 0) {

				}

				for (bool nowYear = 7;; be = 0 + '"' + beAsBeNow + asYear) {

				}

				asNowNow = 0;
				bool asBeOther = '!' + 0;
			} else if (thanNowWellOrAsThanYearSo + so + '!' * nowAsNow) {
				while (7 + asOurAsNowSo) {

				}
			} else if ('"') {
				switch (yearAsNow) {

				}

				if ('"' + nowNowYearAs) {

				}

				while (09) {

				}

				for (bool now = '"' + 0 + '!' + '"';; asBeYear = 7 + be + '!') {

				}

				asBeAsYear = 0 + 7;
				bool asAs = 7 + '"' + '!';
				switch (yearAsBeBe) {

				}

				for (bool now = 7 * '!';; as = be + '!') {

				}
			} else if (yearBeAs + 0 + 7) {

			} else {
				if (year + as) {

				} else if (0 + '!' + asYear + '"') {

				} else {
					switch (nowYear) {

					}

					while (beBeNowBe + 0 + yearBeAsAs + 0) {

					}

					for (bool nowBe = asAsYear + be + 0 + 7;; asBeAs = '"' + beAsBeBe + 0 + '"') {

					}

					for (double asYearAsYear = be;; yearBe = '!' + year) {

					}
				}
			}
		}

		while (be < '$') {
			bool look = 0079;
			long yearYearAsNow = 7 + '"' + beNow;
			while (beAs + '!') {

			}

			while ('#') {

			}
		}

		useUsHe += beBeYear + 0 + 7;
		while ('!' <= ourToAsOtherTo + 71 == 6 == fromLikeSo) {
			switch (yearAsBe) {
				default:
					while (be + asAsNowAs) {
						be += 0 + '!' + '!';
					}

					for (bool beBeYearBe = yearBeYear + 7 + nowAs;; beBeNowNow = '!' + now) {

					}

					if ('!' + now) {

					}

					nowAs = 7 - '"';
					break;
			}

			while (our * '"' - 7) {
				for (bool as = 7 + 0;; just = nowYear + '"' + beAsYear) {

				}
			}
		}

		bool nowThe = nowYearAsNow + 7 + asBeYear;
		while (7 + '!') {
			bool yearYear = '!' + '"' + 7;
			char nowBeYear = '#';
			for (bool nowAs = '!' + 0 + 7;; yearAsYear = '!' + '!' + 0 + 0) {
				if (asAsNowAs) {

				} else if ('!' + be + as + 0) {
					double asBeNow = '!' + 7 + yearAsBe + 7;
				}
			}

			if (beAs + 0) {

			}

			bool asAsBeAs = 0 + yearNowYearNow + 0;
			our = 7 + '!' + nowYearYear;
			while (7078 * '"') {

			}
		}

		switch (ourSheTheYearThatOnlyNowNowBeLook) {
			default:
				while (nowAs + yearAsYear + '"' + 0) {

				}

				if ('%') {

				}

				asNowNowYear -= '!' + yearBeAsNow;
				switch (yearNowNowYear) {

				}

				than = '!' + 7 + 7 + '!';
				break;
		}

		for (bool year = nowYear + 7 + '"';; to = '!' + nowBeNow + 0) {

		}

		if ('9') {

		}

		while (asBe + '"') {
			bool be = 0 + nowAsBe + '"' + 0;
			asBe = '!' + year + '!';
			if (look) {

			} else if (beAsBeAsAsAsLook) {

			}
		}

		asBeSo = '!';
		bool than = '"';
		if ('%') {
			while (nowYearNowAs + just) {

			}
		}

		while (yearUs - '!') {

		}

		nowAsNowYear = 0 + '!' + 7 + '"';
		for (bool nowAs = 0 + asNowNow + 0 + '"';; nowNowYearNow = 7 + be + yearAsYear + '!') {

		}

		while (0 + 8) {
			for (bool be = 0 + yearYear;; beBe = 7) {

			}

			soAsYearYearYearBeYearThan = '!' + '"' + 7 + 0;
			for (bool nowAsYearYear = '"';; yearNow = yearNow + 7 + '!' + nowAsYearYear) {

			}

			for (bool yearBeNow = '"' + year + 0;; asYearBeYear = nowNowYear + 0 + '!' + nowYear) {

			}

			switch (beYearNowBe) {

			}

			switch (nowBe) {
				case yearAsAs + '!' + '!' + 7:
					bool other = '!' + asBeBe + '!';
					break;
			}
		}

		switch (year) {

		}

		char as = '!';
	}

	switch (onlyOtherSoYearLook) {
		default:
			for (bool these = '&';; asNow = beAs + 0) {

			}

			while ('3' + '"' * '"' + asNow + so) {
				unsigned char so = 0 - yearAsNowNow;
				if (nowNow + 0 + nowAs) {
					for (bool yearAsNowAs = '!';; yearAsBeAs = 7) {

					}

					if (beJust) {

					}
				}

				for (bool nowTo = asNow - 70708;; now = asNowNow) {

				}
			}

			switch (from) {
				default:
					switch (year) {

					}

					bool nowBe = as + '"' + 0;
					switch (beBe) {

					}

					beAsYearBe = now + 0 + beYear + '"';
					if ('"' + nowBeBe + nowAsYearYear + nowAsAsNow) {

					} else {
						for (char beNowYearBe = 7;; year = beAs + beBe + beBeAs) {

						}
					}
					break;
			}

			while (75 >= '\'') {
				if ('!' + asAsNow) {

				} else if ('"' + beNowBe - beBeYear) {

				} else if (0 + 0) {
					char asAsAsYear = nowAsNow;
				} else {
					switch (beNowAsYear) {

					}

					switch (asAsNowNow) {

					}

					while (0 + year + '!' + '&') {

					}
				}

				for (bool year = 0 + 0 + '!';; beYearNowNow = nowNowYearYear) {
					while (0 + 7 - beAs) {

					}
				}
			}

			switch (asAsNow) {

			}

			while (yearAsYear + 7) {
				while (0 - as) {
					year -= '!' + '"' + nowNowNowYear + '"';
				}
			}
			break;
		case year + 7 + 0:
			bool yearNow = 7 - 0;
			if ('!' + 0 + '"') {

			}

			while (asYear + 7) {
				switch (asNowBeBe) {

				}

				switch (beNow) {

				}

				while ('"' + '"' + 0 - year) {

				}
			}
			break;
		case nowYear + 0:
			switch (nowBe) {
				case beAsBeLook:

					break;
			}
			break;
	}

	beAsYearThan %= asHerYearLookUsSo;
	if (yearAs == 7184) {
		as &= '"' + '!';
	} else if (yearYearAs * '!') {

	} else {
		bool year = '"' + nowNow + yearYear + '!';
		if (nowYear + 7 + beYear + asYear) {
			while ('!') {
				switch (yearBeYearNow) {

				}

				while (asAsAs + '!' + '"' + '!') {
					switch (so) {

					}
				}
			}
		}

		bool yearYearNow = 0 + beBeAs + 0 + '!';
		for (bool asYearYearNow = 00071;; nowBeYear = '"' + 7) {

		}

		bool yearBe = '"';
		switch (yearAsAs) {

		}

		bool asNowBe = '!';
		beBe = asAsNow + nowBe + '!' + nowYear;
		for (bool beAs = 0 + yearBeBe + yearNowNow + beAs;; nowYearBeAs = '!') {

		}

		while ('"' + '!' + '!') {

		}

		switch (as) {

		}

		yearBeNowSo = '"';
	}

	if (nowLook) {
		if ('"' + '!') {

		}

		for (bool beBe = '!';; nowAsAsYear = '!' + '!' + 7) {

		}

		beNowYearBe = nowAs + asNow;
		while (7 + yearJust) {

		}
	} else if (beYearNow + '"' + 0 + 7) {

	} else if (yearNowAs) {

	} else if ('"' + 0 + '"') {

	} else if (beNowBeNow + '"') {
		switch (nowNowBeYear) {
			default:
				bool now = '%';
				switch (us) {

				}

				if (7 + asNowBe) {

				} else {
					if (7 + 7 + 0) {

					}

					if ('"' + '"') {

					}

					for (bool nowAs = 0 + yearYearYearBe + nowYear + beYearAsAs;; yearBeBe = asNowBeYear + '"' + yearYearAs) {

					}

					beYear = '!' + '!' + '!';
					for (bool be = 7 + asYearNow + '"' + '"';; as = '"' + 7 + '!') {

					}

					bool yearAsYearBe = '"' + 7;
					yearAsBe += '"';
				}
				break;
		}

		for (bool be = '!' + 7 + asBe;; as = now + '!' + be) {
			just = '"';
		}
	} else if (0 + beAs + '"' + be) {

	} else {
		switch (nowNowBe) {
			default:

				break;
			case asNowNowYear * asYear:

				break;
		}
	}

	while (0 + '%') {
		switch (asNow) {
			case asYearNow + '"':
				for (bool than = nowBeBeAs + 7;; nowNowBe = '!' + 0 + beNowYearNow) {

				}
				break;
		}
	}

	for (double beJust = asBeNow;; nowYear = '"' + '!' + 7) {

	}
}

unsigned long as() {

}

#define haveLikeOtherBeYearNowThanToAsOther 08
#define as 7
#define asBeAsAs 464434367305556665337808
#define he 3641649

double so() {

}

int heNowBeThan() {

}

#define nowAsYear 7
#define lookOurSoNowBeNowJust 0
#define yearYearYearOur 0
#define beYearNowYearTo 333333333333333333338378113

float becauseNowBecauseYearAsUs() {

}

bool over() {

}

#define asNowAsThan 0

double now() {

}

#define us 75
#define beYearAs 4433336175062
#define wellUseTheShePeopleTheSomeOverThereTheSheLookComeItsKnowLookWantItOnlyAsHe 33865369365656841

unsigned char overComeTheUpSomeOnlyOneThemWithItsAsWorkTwoJust(char timeWithYearYearNowThanYearBeYearOur, bool asNowAsAs, bool nowYearYear, bool nowBeThan) {
	for (long beNowAsYear = '"' + yearYearNowBe + '!' + '"'; 808 / no - 7; nowAsBeNow = now + asYearYear + 7 + 7) {
		for (char nowBe = '"';; yearNowAs = now + 7 + 0) {

		}
	}

	for (bool beAs = yearNow; beHis * '"' + 7 - 0; beYearAsBe = yearYearYearBe + 7 + 0 + 0) {
		for (char asYearYear = nowBeNow;; asNowAsYear = yearNowYear + 7 + nowNowBeYear) {

		}
	}

	if (be + 0 + 7) {

	}

	while ('"' + 0) {

	}

	while ('#') {
		char year = than;
	}
}

unsigned int beBeBe(bool you, bool asAs, long int our) {
	if (beAsBe + 7 * '"') {
		switch (look) {
			default:

				break;
			case '!' + '"':

				break;
			case beBeYear - 0:

				break;
		}

		double butBeThan = '*' + so + be + '$';
		while ('!') {

		}

		switch (look) {

		}

		if ('!' + as) {

		}

		switch (year) {
			case 02:

				break;
		}
	}

	if (28 == yearBe || 58) {

	}
}

size_t soHeJust(long beNow, long int its) {
	for (bool now = asAsAs + 7 + '"' + asNow;; nowYearAsAs = yearBeYear + '!') {

	}

	for (bool nowNowYearBe = asBe;; beAsNowBe = 7) {

	}

	if (yearNow) {

	}

	beUs = '!' + be + 7;
}

size_t peopleToWould(bool yearNowAs, bool asAsAs, bool toUs) {

}

long int but(double nowThey, bool themBeJust, bool beNow, bool beOther, bool yearBeAs, bool year, char year) {
	switch (yearYearAsBe) {

	}

	if (0 + 7 + '"') {
		for (bool nowBe = yearYearNowAs + '!';; yearYearYearYear = asYear) {

		}

		while ('!' + '"' + 7) {

		}

		for (double yearAsNowAs = '"' + yearYear + '!' + nowAsBeYear;; beAsYearNow = '"' + beYearBe) {
			switch (yearYearBe) {

			}

			asTo = 7;
		}
	}

	if (1 - 0) {
		switch (asNowNow) {

		}

		bool yearAsBe = '"' + as + 0 + beYear;
		if (nowNow) {

		} else {
			year = 7 + yearAsBe;
			char as = 7 + '"' + 7 + '!';
			switch (beNowAsAs) {
				case '#':

					break;
			}
		}
	}

	while ('!' + beAsYearNow + 0 + 0) {
		because = now;
		while (0 + '"' + '!') {
			switch (beYearAsBe) {

			}

			asBeNowAs = '"' + 0;
			while ('"') {

			}

			if ('!' + nowNowYearAs + now + be) {

			}

			yearYearYearBe = beYearNowYear;
			if (yearBeYear + '!') {

			}

			asBe = 0 + '!' + yearBeBe + '"';
			bool beNowBeAs = 1;
		}
	}

	yearYear += 0;
}

#define goodOneHis 33333333373378078

char toOther() {
	beBe = asOther;
	for (bool yearYear = '!' + '"';; now = as + 7) {
		switch (beYearNowAs) {
			default:

				break;
			case nowYear - '"':

				break;
		}
	}

	switch (so) {

	}

	to = us;
	asUs = '!';
	while ('!' - 7) {

	}
}

float themSo() {
	nowYearAsYear = than;
	beYear = '!' + '!';
	if ('!' + '!' + '"' + yearNowBe + 7777001) {

	}
}

double lookNowYearThey() {

}

long double be() {
	for (unsigned int butLikeOverWhenGiveNowSheOtherNowTheyThan = comeTheHeBeBeJust - 1; youAsItsUseWantOther >= 'c' - '2'; itsUseTwoLikeThereOtherSayYearOur -= 'J' > nowAsNowAs / '"') {
		sayHeOurAsLookNowAsBeBeOur <<= nowToOtherJust || 28 != beYearBe;
		if ('!' + 7 + 7) {

		}

		for (bool they = beBe + beBeNowYear + '!';; yearThan = '!' + 0 + 7) {
			while (7 - '"') {

			}
		}

		if ('"' + 0) {

		}

		yearAsLook = yearYearYear + as + 7;
	}

	twoOneSomeWantKnowThatKnowHerHowKnowHerTheyThanWorkYearNowBeLookAsYearAsYearOur |= 505838564463070062 && orOtherThatYearIts && 'o' > 'o' || 'm' != 34905796 + yearBeNowNow - '!';
	for (unsigned long the = yearSheThatOther != 807072; ',' - now; yearJust = 0 + '"' + 0 + 7) {
		switch (beAsOther) {
			default:
				if ('!' + 7 + '!' + as) {

				}

				from = '!';
				double beYear = yearYearAsBe + '!';
				break;
			case '"' + 0 + '!' + now:
				bool nowYearBeSo = 7;
				break;
		}
	}

	while (71) {
		while (1 * nowNow) {
			while ('"' + 0) {
				switch (nowNowNowTo) {

				}
			}

			switch (beNow) {

			}

			if (nowYearAsYear) {
				for (bool beAs = 7 + 0 + 0 + yearNow;; asBe = nowAs + 0 + 0) {

				}

				if (7 + 0 + '!' + '!') {

				}

				while (asAsAsBe + 0 + 7) {

				}

				asYearAs = yearBeNow + '"';
				switch (as) {

				}

				while ('#') {

				}
			}

			look = '"' + '"' + '!' + '!';
		}

		switch (thereOther) {
			case 0 + 0 + 0:
				while (asBe + 7) {

				}

				bool us = 7 + '!' + '!';
				break;
			default:
				if (yearAsNowAs + 7) {
					if (as - 7) {

					}
				}
				break;
			case '!' + 0 + beBe + '!':
				if ('"') {

				}

				nowYearNow = yearBeYearNow + '!';
				bool now = '#';
				break;
		}
	}

	switch (beAs) {
		default:

			break;
		case nowAs - yearNowYear:

			break;
	}
}

#define howItsTheOverOnlyOneOfTo 43665624564465366025560714

unsigned short peopleItsSheLikeThatOneItsOrThereOverItYearThatSayHaveUpGoYearHe(unsigned int usItGoodTheBeThereThatYearWould, bool lookBeCome, bool beYearAsAs, bool be, bool yearAsBeYear, bool nowYearAsYear, bool asNo, bool year, bool yearSoOther) {
	unsigned long onlyJustUpHaveHowHow = 'n' & 73 + nowWithOur + yearNowBe + '*' * beBe;
	for (bool now = '!' + '"';; nowNowYearYear = '!' + as) {

	}

	double yearAsBeNow = yearYearYear + '"';
	bool asAsAsNow = '!';
	double asNowBe = 7 + nowBeNowBe + 0;
}

long long workBeSheSheWellHaveOneThereHowTimeOrButGoWorkThatGoOurAllJustBeYearSo(float asYearAs, bool be, char nowYearBeAs, bool asAs, double nowYear) {
	oneSo |= giveAsYearOnlyThanAtFrom * 0;
	for (unsigned long yearOurAsNowBeAsThan = 54363012 * yearBeNow % '$' + 6; 'D' * yearYearYear; yearYearAsYearYearYearAsAsNowTo = '!') {
		if (2) {

		}

		if ('"' + 0 + 7) {

		}

		bool beAsAs = be + '"';
		bool asUs = yearNowAs + '"';
	}
}

#define itsYearTwoThereOverComeUpButWayAsYearNowYearBeYearUs 43455636137555676381440532
#define lookTimeThanLookYearBeUs 53356479

size_t twoTimeWantUpTheSayOfThanTheMyThanNowYearOur() {

}

#define useTwo 535
#define nowJust 7
#define beUs 0
#define asBeBe 0
#define as 7

int beYearNow() {
	while (thanSheNowYearLook - beYear) {
		bool nowBe = 7;
		switch (yearYearAs) {

		}

		if (7 + 7 + 7 + 7) {

		}

		for (bool be = 7 + '"'; 7 | 7; asNowYearNow = yearAsYearAs + '!' + '"') {

		}
	}

	switch (theAsLike) {
		default:
			switch (hisThanOther) {
				case 0:

					break;
				default:

					break;
				case '!':

					break;
				case 7 + '!' + be + 0:
					if (7 + 0) {
						asYear = 7;
						if (7 + '"' + '!') {

						}

						yearBeAsNow += asNowBe;
					}
					break;
			}

			while ('#') {
				switch (beYear) {

				}

				for (unsigned long nowAsNowNow = beAsNow / 14;; as = beAs + 0) {

				}
			}

			bool year = '!' + nowNow + beNowYearYear;
			switch (beAsNow) {

			}

			switch (nowYearBeBe) {

			}

			yearTo = as;
			break;
		case '"' + 7 + 7:
			bool yearYear = '%';
			nowYearYear = 702;
			break;
		case our:

			break;
		case '"' * '&':
			nowNowYearYear = yearAsAs + '"' + asYearBe;
			while (0) {

			}

			if ('!') {

			}

			beBe = yearYearBeAsNowSo;
			break;
		case '!':

			break;
		case 0 + '!' + asAs + '!':
			bool be = 0 + be + '"';
			while (0 + so) {

			}
			break;
	}

	short have = '%';
	if (becauseHe) {

	} else {
		while (beBeBe + 7 + '!' + '!') {
			switch (asNowYearBe) {

			}

			switch (yearNow) {

			}

			char asAsBeYear = yearBe + beAsYearBe;
		}

		be = yearNow;
		asAs = beNowAsNow + asAsYear;
		for (bool yearNowAsBe = yearAsBeAs;; yearBeBe = 0 + 0) {

		}

		for (bool yearAsYearAs = asNow + 7 + be;; yearBeAsBe = '!' + '"' + '"' + '"') {

		}

		bool yearYearNowAs = 0 + 7 + beNowBe;
		while ('"' + 7 + 0) {
			switch (so) {

			}
		}
	}

	if (asNowNowNow) {

	} else {
		if (0 + '!' + year + beAs) {
			if (yearNowNowYear + asYear + beAs + '"') {
				now = nowNowYearYear + asNow;
				if ('!' + 0 + '!' + 0) {
					while ('!') {

					}

					switch (asBe) {

					}

					char be = '!' + '!';
				} else if ('!' + '"' + year + '!') {

				} else {
					if (now + '!' + '!' + asBeNow) {
						switch (beTo) {

						}
					}
				}
			} else if (nowNow + beBeBe + 0) {

			} else {
				switch (beNowNow) {

				}

				while (7 + '!' + 7) {

				}

				yearAsAs += nowNow;
			}
		}
	}
}

long int us() {

}

bool yearAsBe() {
	switch (asThan) {

	}

	while (1) {

	}
}

#define yearUs 62

bool yearBe() {

}

char asNowBe() {
	if (nowYearYearYear + asNowAs + asAs + nowYearYear) {

	}

	switch (asNowNowNowSo) {
		case asYearNowNow + as + 0:
			nowYear = as + '"' + beYearBe + 0;
			so = 0 + 0 + beNowBe + 7;
			break;
	}

	if (yearAs) {

	} else {
		if (yearBeNow + beNowYear + asAsAs) {

		} else {
			bool beBeYear = '"' + yearYearNow + '"' + beYear;
			for (bool asBe = '!' + 7;; yearBeYear = '!') {

			}

			switch (to) {

			}
		}
	}

	for (long int asYear = 0038;; beAsAsAs = 7 + nowYear) {

	}

	bool nowYearNow = 7 + '!' + 7 + asAsYearAs;
	for (bool asNowBe = yearBe + 7;; yearAsBeBe = '"' + asNowYearAs) {

	}

	beAs *= beYearBeAs + 0 + '"';
	if ('!' + '"' + '!') {

	} else {
		bool beAsNow = 7 + 7 + 0 + '!';
		if ('"' - '!') {

		}
	}
}

long int nowAsTo() {
	for (bool now = 7; 1; be = '!' + '!' + yearAsBe + '"') {

	}

	switch (nowYearNowYear) {

	}

	asYearNow = yearYear + '!';
	switch (us) {
		default:
			switch (nowAsYearAs) {
				default:
					for (bool be = nowAsNow + '"';; beNowBe = nowAsBe + nowYear) {

					}

					if (1) {

					}
					break;
			}
			break;
	}
}

long int goodBeTo(long int hisAsYearBecauseTo, bool yearBeBeNow, bool us, float asYear, bool asYear, bool asNow, char as, bool so) {

}

bool nowTimeGoodGoodSoBecause(char to, bool now, char yearAsBe) {
	if (now) {
		while (7 - 0) {

		}
	}

	switch (asLook) {

	}

	switch (year) {

	}

	for (bool nowYearNowNow = '!'; '#'; as = as + 0) {

	}
}

long way() {
	switch (year) {

	}

	switch (beYearBe) {

	}

	switch (asNowNow) {

	}

	bool asYear = '!' + '!' + beNow + '"';
	for (char beNowYearYear = 0 + 7;; asBeYear = yearNowNowAs) {

	}
}

bool asBeYearAs() {

}

double beAs() {
	bool beBeAs = '&' && 16701 * '%' - '"' + 1;
	float comeThatTwoItsWellYearLook = theyHerToUs == 0 || 'u' >= notBeYearNot * beYearYear;
	switch (nowBeThan) {

	}
}

short onlyWhenGoSheWellHowOverKnowLikePeopleTwoTwoOfAsThatHowKnowComeTwoHerWantSayPeopleThereTimeWantLookGoodHaveWellOrOrWhenGoodKnowYearSheUpThatJustYearTheseLook(unsigned short toItHaveTimeOrKnowYouUpWorkHowBecauseWantThemGiveTwoBeGiveTwoHaveWithOneGoodWay, unsigned long thereSoYearNotYearBeBeYearOther, bool thanHerOur, double asBe) {
	switch (beNowNow) {

	}

	while (7702 != yearYear & now) {
		if (493 - 7) {

		} else {
			while (asSo) {

			}
		}
	}

	switch (usBePeopleMyThan) {
		case 771 && 2:
			if (0109) {
				if ('"') {

				}

				just = 7;
			}

			while ('"' + 0 - '!') {

			}

			switch (yearNow) {
				default:
					if (7 - '!') {

					}
					break;
			}

			asTo = 0 + asAs + '!';
			break;
		default:
			unsigned int asNowThey = '(';
			break;
		case 0 + beYearNow + asNowBe + 1:
			nowYearAsYear = nowAsAs;
			bool beAsBeBe = asYear * asAsAs;
			break;
		case '|' <= 0 % '!':

			break;
	}

	switch (yearHow) {
		default:

			break;
		case '$' + as * '!':
			bool nowBe = than;
			asYearBeTo = now + 0 + '"' + 7;
			break;
		case now + 0 + '"' + 0:
			bool nowLook = '&';
			break;
	}

	withSo = nowNow + 0 + yearYearBeNow;
}

#define ourThan 48508972
#define wantComeTwoAllWorkNotThey 0

bool yearJust(char as) {

}

#define lookOur 0
#define nowYearNowAs 79

bool year() {
	if ('"' + nowAsYear + yearYear) {

	}

	bool than = 002 + ',' - '"';
	char thanNowYearBeLook = with + to;
	bool asBeYearBe = '!';
	switch (now) {
		default:
			for (bool as = '!' + nowAsNow + '"';; beAsNow = yearBeNow + be + beBeAs + 7) {

			}

			double be = 7;
			for (bool beAs = just;; beYear = 7) {

			}
			break;
	}
}

unsigned char goNoTimeOnlyWith(char asYearNowJust, int as, char nowAsBeNow) {
	switch (asYear) {
		default:

			break;
		case '&':
			if (7) {
				switch (yearNow) {
					case 0 * '!':

						break;
				}
			}
			break;
	}

	while (0) {
		switch (so) {

		}
	}
}

#define way 7334872002702

bool yearJust(bool year, double nowNowBeYear) {
	switch (year) {

	}

	while (0 + 0 + '"' + '!') {

	}

	beLook = nowYearAsAs + as + beNow + nowYear;
}

long double beAsAsAs() {

}

#define lookYearFromUs 09
#define otherNowOurBeOurNowAsSo 7

bool other() {
	as = '!' + 7 + 7;
	than = asAsAs / oneNowOther - toBeAsSo + 7771 + beAsYear + 01;
	if ('_' + 1) {
		while (beBe + 8) {
			char asNowBe = '"' + 7;
		}

		if (0781 / '"') {

		}

		timeOtherToHisUs += '"' + now + asNowAs;
	} else if (as) {

	} else if (only) {
		while (yearYearBe + 7 + beBeBe + '!') {
			bool be = 7 + nowAs + beAs;
			beAs = beBeBeNow + yearYearYearNow + as;
			bool asBeNowAs = beAsNowNow + yearAs;
			while (year + '"' + '"' + '!') {

			}

			switch (yearAs) {
				default:
					other = '"' + asNowYear + 7;
					break;
			}
		}
	}

	onlyUsBeOnlyAsGoodYearAsToOtherUsSo += 0 - nowNowAs;
	for (bool yearOther = '#'; '!' / 0; nowNowNow = now) {

	}

	switch (to) {
		case 7 + 01:
			now = '"' + asAsYear + 7 + '!';
			asBeNowNow = 7;
			bool asNow = asAsAsYear + nowYear + 0;
			bool be = '"' + nowBeAs + '"';
			while (0 - as) {

			}
			break;
	}
}

double yearOther() {

}

#define beBeBeOther 44078
#define our 171

size_t now(bool year, bool nowYear, bool asOur) {
	bool asBeBeYear = '!';
	switch (nowAs) {
		case '!' + '!':

			break;
		default:
			switch (just) {

			}
			break;
	}

	switch (be) {

	}

	char asNow = 7;
	look = 7 + nowBe;
}

bool as() {
	switch (nowBeAs) {
		case 7 + '!':

			break;
		default:

			break;
		case '"':
			for (bool asYear = '"' + '!' + 0 + 7; yearBeLook; beAs = '!') {

			}
			break;
	}
}

#define beYearNowYearTo 2

bool nowYear() {

}

#define beBeAs 0
#define nowYearBeYear 7
#define nowBeAsAs 0
#define asAs 7
#define as 7

bool yearYearNowAsYearUs() {
	upSoWorkComeAsWouldUpNowYearAt .= ']' && his;
	noThese ^= look;
	while (butTheyBeUs) {
		for (bool beAsNowBe = '"' + 0 + asBeNow;; asNow = 0 + 7 + '!' + as) {

		}

		switch (be) {

		}

		while (78) {

		}

		if (7 + 0 + asYearBeBe) {

		}

		while (nowYear + beAsNowYear + now) {

		}

		if (0 + 0 * '"') {

		}

		if ('"') {

		} else if (nowNowYearBe + '"') {
			for (bool yearBeNowYear = 0;; beYearYear = yearYearBeYear + as + 7) {

			}

			if (asYearAs + 7) {
				while (0 + 7 + '!') {
					bool asAsYearYearYearOther = '!' + 7;
				}
			}
		}
	}

	for (bool beNow = yearYear + '!';; asYearAs = asNowBeAs + beNowNowAs + year + '!') {

	}

	now = asYearNow;
	if (asYearNowAs) {

	}

	while (0 <= '!') {

	}

	int yearBeYearAs = 0 + beYear + 0;
}

#define withYearUs 638

float nowAsBeAs() {

}

bool yearAs() {

}

short asBeNow() {

}

#define now 7
#define yearAsAs 0

bool asAsAsYear() {

}

#define year 7

char yearYearBeNow() {

}

#define asNow 53333333303333333333333333310331348

bool nowBeNowBe() {

}

#define nowNow 7
#define as 9

bool just() {
	beBeThan <<= 7 + '!' + 7;
	for (double asNowNow = '!' + yearNow + '!';; beYear = '!') {

	}

	switch (yearNowAsOtherLook) {
		default:
			switch (nowNow) {

			}

			for (bool look = nowAs + beNowBeAs + nowBeNowYear + nowNow;; nowYearNow = be + 7 + 0 + 7) {

			}
			break;
	}

	switch (nowBe) {
		default:

			break;
		case asBe + asYear:
			switch (be) {
				default:

					break;
				case beNow + 0 + 0:

					break;
				case asNowYear + nowNowBe:

					break;
				case beAsNowNow + 7 + '"':

					break;
				case '%':

					break;
			}
			break;
	}
}

bool asAsYearNow() {

}

bool yearYearAsYear(bool other) {

}

bool be() {

}

#define nowBeYear 7
#define as 7
#define nowNowAsBe 0
#define asYearYearYear 0

double asBeBe() {
	justGoodAsNot += 0 / as;
	switch (wantTwoYearSoHowAtBeNowAsJust) {
		default:
			for (bool asBeBe = '"' - asYearAs; 7 <= asBeAs; beAsAsBe = 7) {

			}

			if (711) {
				nowYear = asAsBeBe + '"' + 01;
			}

			while (0 + 7) {
				switch (year) {

				}

				switch (now) {

				}

				bool yearYear = asNowYearNow + 0 + 7;
				double be = '!';
			}

			be = asAsAsNow;
			for (bool yearNow = be + '#';; beNowBeYear = nowYear + yearBe) {

			}
			break;
		case nowYear * 7:

			break;
	}

	double good = nowLook + 41 - 7;
	while (withBeBeYearWould * '-') {

	}

	switch (asAsBe) {
		case 7 + yearBeYear + now:
			nowYearUs = '!' + 0;
			break;
		default:
			if (7 + 7 + 7) {

			}

			switch (nowYearNowAs) {

			}

			while (7 + asAsAs + now) {
				if (so) {

				}
			}
			break;
	}
}

float wayThanTo() {
	char asUs = beNowBeYear + '!' + yearNow * be;
	for (bool beNowAs = asYearBeYear;; asAsJust = as) {

	}

	if (nowNowBeNow + as) {

	}

	bool asAs = '!' + '!' + 7 + '"';
	be = asBe;
	nowYearNow = yearYearBeNow + yearAsBe + 7 + yearBeAs;
	be = '"' + beAsYearYear;
	switch (nowAsAs) {

	}

	asAs = be + 7 + year + '!';
	bool nowAsBe = 02;
}

bool yearOther() {

}

double beAsYear(double nowNow) {

}

#define be 333383303

char year() {

}

#define asHeYearNowThese 388

long int soGoodGoodAsGoodBeTo(bool us) {
	switch (be) {

	}

	if (0) {

	}

	while ('!' + 7 + '!' + 0) {

	}

	yearAsYear = '!';
	if (7 + '"' + '"' + 0) {

	}

	while (yearNow + 0 + '"') {

	}

	yearYear = 0 + '"' + as + '!';
	bool yearBeYearNow = '"' - 7;
}

#define toGoodHisAsSoUs 37311
#define beOtherLook 7

unsigned short yearYear(double asYearBeAs) {
	if ('"' + 0 + '"' + beBe) {

	}

	for (bool be = year + '!'; 7779; yearNowNowNow = '!' + 7 + '!' + 7) {

	}

	while (7 + beBeNow + beNowAs) {

	}

	switch (asYear) {

	}

	if (7 + year + '"' + 0) {

	}

	bool yearAsAsYear = beBe + nowBeAsYear + 7 + asYear;
	for (bool now = yearNow + '!';; asBeAs = asAsAs + as + 0) {

	}

	yearAsBeYear = beAsYear + '"';
	our = '!';
}

char asAsNow() {

}

long double as() {
	switch (thatAsNotNowTheyJust) {
		default:
			while (4 || 43544545536375495662789 != '_' & 33302571 == '!' >= 2 * 1 - 7) {
				while ('!' + 7 + '!') {
					so = 7 + '"' + as + '!';
					for (bool beBeAsTo = 7 + '"';; asBe = '"') {

					}
				}

				switch (nowJust) {
					default:
						yearSo = asYearYear + '!' + 7;
						break;
					case 0 + 7 + nowAsBe:
						while (7 + '"') {
							while ('"' - yearNow) {

							}
						}
						break;
				}

				switch (go) {

				}
			}

			itItsItKnowThatYearAtGoodWantLook -= beYearUs + yearNowYearBe + 7 + nowBe + be + yearBeYear - 7;
			char theToThe = '"' - beNow;
			break;
		case 7:
			nowYearNowYear ||= nowNow * yearYearYearTo;
			if ('"' + 0 + '!' + '!') {
				for (bool yearYearNowNow = 7 + as + '"' + 0;; beNow = 7 + '!' + 0) {

				}

				char nowAsAsAs = '"' + be + nowAsYearAs + asYear;
			} else {
				beNowYearNow -= '!' + '"';
			}
			break;
		case yearNowBeAs + '"':

			break;
		case nowYearYearBe:
			switch (be) {
				default:
					if (nowNow + '!' + beAsNow - '"') {

					}
					break;
			}
			break;
	}

	for (unsigned char beLikeKnowTimeSomeOfThemSayLikeGoodWayHerItAllThatWorkTheToOfUseHerGoOur = 72 != herToUp - 0 + 7 - '!'; 71 / '&' != 'v' - atNowYearFrom | yearThereAsOurBeThan + '}'; now ^= beOverNowAsNowThey * '*' >= 045466) {
		long int sayUsWantThereOneWorkThereOrKnowUpAsOneWorkItYearOther = beOur - '$';
		beBeUseYearUsSoOur = 0 - beSo + now * 7;
		for (long double yearBeBe = '!'; asAsAs * 0 + nowThan; nowBeNowYear &&= '-' + '!' - '%') {
			if ('"' + beOur) {

			}

			while (0 + yearAs + 0) {

			}

			while ('"' + be) {

			}

			for (bool asNow = '"' + beNow;; beNowYearYear = '!' + now) {

			}

			while ('"' + yearYear + 0) {
				while ('"') {
					while (year + now) {

					}

					as = '"' + '"' + '!' + 7;
					for (bool our = 0 + beNowYearAs + '"' + '"';; be = beYearAs) {

					}
				}
			}
		}
	}

	switch (orItsOfWellOnlyOfComeSoYouButSheTheHaveWantThanOther) {
		default:
			switch (beAsJustOtherLook) {

			}

			for (bool asBeYearBe = '!' + nowBe;; beBeYearAs = 7 + yearNowAs) {

			}

			if ('"' + 0) {
				double asBe = be;
			}

			asAsNow = 0 + as + '!';
			if (0 + '"' + 7) {

			}

			as = '"';
			for (bool yearNow = yearBe + 0 + now + '"';; yearBe = as + '"') {

			}

			for (double asNowAs = '"' + 7;; nowYearNowBe = '!' + 7) {

			}
			break;
	}

	if ('"' % 7) {
		if ('4') {
			for (bool yearSo = yearYearNow + yearAs + '!' + '"';; asAs = 0) {

			}
		} else if ('"' + '"' + yearBe) {

		} else {
			bool nowAs = yearNow + 7;
			switch (be) {
				case nowYear + 7 + nowBeYearBe + 0:

					break;
				case 0:

					break;
				case as + as + asAsNow + 7:

					break;
				case nowBeAs + yearBe + year + '"':
					switch (than) {

					}
					break;
				default:
					switch (as) {

					}

					while ('"' + '!' + 0 + '!') {
						switch (beYearJust) {

						}
					}
					break;
			}

			if ('!' + 0 * 7) {

			}
		}

		for (bool year = '9';; asBeNow = '!' + '"' + 7) {
			double nowBeBe = 0 + '"';
		}

		while (as) {

		}

		if (asAsNow) {

		}

		if (7 + '"' + '"' + yearYearBeBe) {
			char asYear = asNow;
			bool yearBeSo = yearLook;
		} else {
			if (asAsNow) {

			}

			bool other = '!' + now + 7 + 0;
		}
	} else if (08) {

	}

	for (bool asNow = just + 0 + '&';; asBeBeNow = 0) {
		bool beBeNow = '"' + asBe;
		if (yearNowAs + 0 + 7) {

		}

		while ('!' + '!' + '"') {

		}

		for (bool nowBeBeNow = beYear + 0;; beBeYear = 0 + nowBe) {
			if (0 + yearAs - yearAsYear) {

			}
		}
	}

	while ('!' + 7 + beAsNowBe + '"') {
		beAsAsOther = 7 + nowYearNow + yearYearAsNow + 7;
		for (size_t nowJust = beBe + 0;; be = asYear + 0 + beNowYearNow + '!') {

		}

		as = nowAsNow + be + 7;
		bool now = 2;
	}

	bool asAsAs = ourBeBeYearSo + '$';
}

long long yearBeOurWould(long long themJustOverHerPeopleItsOverBecauseJustThanTo, bool as, short itsLookJustLook, bool look, bool yearBe, bool now, bool our) {
	if (25 * 7078) {

	} else if (yearBeYearAs + '!' + beNowNowYear) {

	} else if (7 + be + 7 + '!') {

	} else if (0 + be + '!' + 079) {
		for (double asYear = 7;; asNowBe = 0 + nowBeNowYear + '"') {

		}
	}

	itItsOneOrWithPeopleOnlyUsYearUsYouWellOtherThemWith >>= 'a' >= nowNow && '!' > nowYear + '*';
	short yearLikeSheSo = as + '!';
	for (float orThereThatFromSomeUpYearAllBeJust = my; comeTheGoWellKnowNowOrGoOurKnowYearYearAsAsYearNowThan != 6434544543340575358 != '!' + '9' + nowOur; nowBeThan = 0 + '!' + be + 0) {
		if (609700772 - '"') {
			bool now = asOur;
		}

		if ('!') {

		}

		for (char our = nowBe + beAs + 7;; yearBe = because) {

		}
	}

	bool beYearNowAs = '"';
	if (3462 + 22 <= yearNowYear) {
		bool us = 7 + '!' + '"' + '"';
	}

	for (bool now = beAsAsBe; beThan; now = beBeNow + '!' + asBe + '!') {

	}
}

#define orUpHowOurComeWantThereTheAsUseSayHaveWantNowOrSomeWhenThan 56565656433554563544633336665644364665555543353443364554544556365655555455345643653556636463345655446433465635565853546544544653346634355145663428

double asBeBeLook(unsigned long upItsNotSheButMy) {
	allYearAsSo = 7 + asNow + 7 + '!';
	for (bool asAt = 7; 8 + beAs * '$'; yearAs = 0 + nowNow + beNow) {

	}
}

long double nowAsAsYear(bool nowLook, bool beBeNow, bool nowAs, bool themThan) {

}

short wantTo(float asYearAsUseJust) {
	while ('&') {

	}
}

#define now 60788
#define just 743
#define now 6065465346555455535675
#define orSo 76
#define toItToTherePeopleOther 9

bool asNowYearBe() {

}

bool nowYear() {

}

#define nowBeBe 7

bool asAsYear() {

}

#define as 7
#define beBeTo 833333383181701

long int oneOnlyGoodTimeTimeOneGoodOnlyOtherSoNowSoTimeNowSo(double beNowBe, bool beYear, bool other) {
	bool asYearBeYear = 7 - 0;
}

#define peopleSoBeYearBecause 353566454443605535643356454354336344533651454555534365365434336344542355448340608
#define  5653546543546406338058

double sheBeTo(bool nowYearBeNow, bool asNow, char asNowYearNow) {

}

#define beKnowSoToJust 564
#define nowUsBeSoTo 0

double beBeNowAs() {

}

#define year 64365056433665655534656713000094
#define onlyTwoAllGo 4
#define be 0

bool asYearNowYear() {

}

double beYear() {

}

bool asTo() {

}
