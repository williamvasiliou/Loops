#define onlyOnlyGoodOnlyTheseHeHis 3333313073
#define theseYearYearYearBeSo 3070703

long int asGoodTo() {
	for (bool yearBeNow = now;; now = '!' + 7) {

	}

	us = 7 + nowYear + '!' + beAsAsBe;
}

bool asOtherSo() {
	bool nowBeSo = '!' + 0 + 0;
	while (so) {

	}
}

char only(bool nowUs) {

}

bool yearBecause() {

}

bool to() {
	bool nowBeAsYear = 7 + yearNow;
	if (7002) {

	}
}

#define usAsAsUs 0
#define beNowAs 7

bool to() {
	long double notSoUsLook = '!' + 0 + 0;
}

bool our() {
	bool beYearBeYear = '!';
	so = 1;
	if ('!' + yearBeBeAs + 7) {

	}

	while (0 + 7 + yearBe) {
		for (long year = '!' + '!' + yearBeNowNow + yearYearNow;; nowNowBeNow = 7) {

		}

		switch (beNowBeAs) {

		}

		switch (year) {

		}

		while ('!' + '"') {

		}

		switch (beThan) {

		}
	}

	if (beNowAsYear == 7) {

	}
}

#define someGoLikeThereThatLikeAsThereUs 6274

long long asJustYearAsTo(char nowYearYearAs) {
	nowBeBeYearYearThan |= 98 % '"';
	for (char yearYearBeAs = '!' + 7;; be = '!' + 0) {

	}

	bool asYearBe = 0 + asAsNowYear + beNow + asYearAs;
	while ('"') {
		for (char nowYearBe = asAsNow + nowBeNowNow;; beAsNow = 0 + asNowYear) {

		}
	}
}

long long onlyWay(bool just, bool beTo) {
	if (0 + 9) {
		while ('!' + nowAsNow + 7 + as) {
			bool than = nowAs + 0;
		}
	}

	for (bool yearAsAsYear = yearBe + as;; asBeYear = '"' + '"' + '"' + 7) {

	}

	while (beAsBeYear - asYearNowAs) {
		yearBeNow = yearAsYear + '!' + '"' + year;
		us += '"';
		if (nowYearNow + '!' + nowYearYear + asNow - year) {

		} else if (0 + '"') {
			while ('%') {

			}
		}
	}

	if ('7' - nowNowNow) {

	}

	for (bool nowYearAsBe = '!';; nowAs = 0 + 7 + 0 + '"') {

	}

	yearYearYear = 7;
	bool yearAs = 7 + beAsYearYear + '"';
	switch (beBeNowYear) {
		case 0:

			break;
		case '"':

			break;
		case nowAs + '!' + 7 + 0:

			break;
		default:
			switch (so) {

			}
			break;
	}
}

char oneOneToNowGoodSo() {
	yearNowYearYear = '#';
	while (0) {

	}

	while ('"' + '!') {

	}

	time = as + beSo;
}

long int time(char be, char onlyNowGoodTimeToThese, char yearAsAs) {

}

#define goodOneAsOtherTime 3

float yearAsBe() {

}

char asYearBeYear() {
	be = asBeAsNow;
	if (year) {

	}

	switch (year) {

	}

	switch (asYear) {

	}

	switch (as) {

	}

	while (7 + 8) {

	}
}

bool yearAsBeYear() {

}

bool than() {

}

bool nowAsYear(bool nowOur) {
	unsigned int youLook = theseOther * 36 * 0;
	switch (onlyWhenKnowUseKnowPeopleGiveWorkUsYearUs) {
		case 7007009:

			break;
		case '!' * look:
			for (bool now = '"' + 0 + asAs; '&'; nowBe = '"' + '"' + 7) {

			}
			break;
	}

	size_t thanYearAsJustTo = '"' + 0 + '-';
	asJust -= asYear + '!' + '!';
	switch (asBe) {
		case '(':

			break;
	}

	but = beAs;
}

bool now() {

}

#define yearBeThan 9

char year() {

}

#define beNowBeAs 53333333333333333333333333033379

float asBe() {

}

size_t beBeAsSo() {
	while (beBeYear <= 0) {
		double yearYearNowAs = '!' + 0 + '"' + now;
		bool asNowBe = nowYearAs;
		switch (nowBeNow) {

		}

		while (0) {

		}

		if ('!' + nowNow + asNowAs + nowBeYearAs) {

		} else {
			bool beAsYearBe = '!' + beNowAsAs + '"' + 7;
			for (bool yearBeAs = beAsYear + '!';; asNow = 7) {

			}

			nowBeNowYear += beBe;
		}
	}

	to ||= '#';
	unsigned short just = 0 + 0 - '!' * yearBe;
	while ('#') {
		switch (beAsYear) {
			default:
				if ('#') {

				}
				break;
		}
	}
}

bool be(bool look, bool beYear, double now) {
	asUs = '!' + be;
}

unsigned int goYouWorkBeItGoHerSheOrGoJustLikePeopleOne(char nowYearFrom) {
	for (bool asAsYear = '!' + '!';; beThan = nowNow + nowAs + '"') {

	}

	while (beYear <= 7 + '%') {
		if (0 + 7 + 0) {

		} else {
			if (nowBeBe) {

			}

			while ('!') {

			}

			beNowYearBe = 0 * 0;
		}

		switch (beNow) {
			case 2:

				break;
		}
	}

	our = '(' && 56665754577361 + 6 | '4';
	while (as) {
		while (9) {
			bool nowLook = 0;
		}

		if (7 + 7 + yearBeBe) {

		}

		if (0) {

		}

		asAsYear = '"';
		beNow += 7 + 0 + year;
	}

	for (long double than = '!' + yearBeNow + '"' + '!';; asNowBeNow = 70701) {

	}
}

long int our(unsigned short soNowMyWayUs) {
	if (yearAsAs + asYear) {

	} else {
		while ('"') {

		}

		if ('!' + beAsBeNow) {

		}

		yearAsYear -= yearNow + 7 + '"' + 7;
	}

	while (just) {

	}

	unsigned short lookBeOur = yearYearNow + yearSo;
}

#define be 4333313333303337333333338

long int heBecauseTo(float nowHeUs) {
	while (0) {
		now += nowBe + '!' + 0;
		if (0) {
			for (char now = '"' + 7;; yearYearNow = '"' + be + 0) {

			}

			bool yearYearYearNow = '"';
			for (bool yearYear = beYear + year;; as = '!' + 7 + beBeYear) {

			}

			yearBeBeBeOther = 0 + '"';
		}

		if (so) {

		}
	}

	if (asAsYearAs + '/') {

	}

	while (year + '!' + 0) {

	}

	switch (asBeYear) {
		case 7 + nowNowBe + 0:

			break;
		default:
			while (be + yearAs) {

			}

			switch (asSo) {

			}
			break;
	}
}

#define than 0

bool asBeYearYear() {
	bool beAsNowYear = 0 + 7 + 0 + 0;
	for (bool yearNow = '"' + 0;; beNowBeBe = as + '!' + 0) {

	}

	double now = 'y' < '!' * '$';
	unsigned short comeWantTimeOverOnlyOtherUpGiveJustLookUsKnow = '#';
	for (char asNowYear = yearNowNowAs + '!';; beYear = 0 + asYearNow) {

	}
}

#define yearNow 86

char yearUsAsOur(bool asAsNow, bool our) {

}

size_t itOtherOverThatNowNowJustAsAsOur() {
	beNow = 0 + beAsAsYear;
	goodOneGoodOneTimeTime |= beBeNowBe / beYearBe;
	nowYearYearNow |= '_' != goodGoodYearOneTimeSoBeOther - 7307073071 < '!' - 0;
	switch (be) {
		default:
			if (be + '!') {

			} else if (beYearYearAs + '"') {

			} else if ('&') {

			}
			break;
		case 7 + yearYear:

			break;
		case 0 + now + 7 + 0:

			break;
		case '!' + '"' + '!' + '!':
			if ('%') {

			}
			break;
	}

	if (asBe + asNowBe) {

	}

	for (bool beNow = '!' + '"' + 7 + 7;; now = 7 + yearYearYear) {

	}

	for (bool year = 7 + yearAs;; yearNowBe = '!' + 7) {

	}

	bool asYear = '"' + 7 + nowBeAsAs;
	while (yearAsBe) {

	}

	for (bool yearNowYearNow = nowBe + 0;; asNow = 7 + '"' + '!') {

	}

	as -= 7 + 7 + 0;
}

unsigned int to() {
	while ('}' || 5553459446157719 || theThatThanSoHisWantHerThatNowGoUseBeNowAt * 66682 & '8' & 1) {
		switch (lookOneItYearLookBeWithYearBeThat) {
			case just:
				switch (yearFrom) {
					case '\'':

						break;
				}

				switch (nowBe) {

				}

				nowYearNowBe = asAsUs;
				for (double yearAsThan = 0 + '!' + '!';; yearNowNowAs = yearBeBeBe + asNowBeBe + '!' + '!') {

				}
				break;
			default:
				switch (asNowTo) {

				}
				break;
			case '!' * '"':
				yearNowAsNowBeThem = 7 + 7 + be + asBe;
				break;
			case '%':

				break;
		}

		if ('v' + 4 + beAsBe * 7) {
			year += '"' + '"';
		} else {
			now /= '"' + nowAsTo;
			char be = asYear + '"' + be;
		}

		int way = otherAsBut * 4;
		while (asBeBe) {
			if ('"' + now + '"') {

			}

			if (asNowNowBe + 7) {

			}

			while (yearYear + yearAsYear) {
				if ('"' + 0 + beYearNowAs + 7) {

				}

				for (bool as = 09;; asNowYear = now) {

				}
			}
		}

		switch (beAsOur) {
			case 9:

				break;
		}
	}

	while ('|' >= haveBeHaveTheUseJustNowAllSay || '|' & 7 != nowBeYearYearNowTo - asYearBe) {
		oneLookJust ^= 'l' >= 8 != 37402 | '\'' || 04;
		switch (timeNot) {
			default:
				for (bool as = year + '!' + '!';; nowYear = yearAsAs + nowBe) {

				}

				while ('"' + asBeNowBe + nowNow) {

				}

				if ('&') {

				}
				break;
			case asNowYearYear + us:
				while ('4' * '"') {

				}
				break;
		}
	}

	size_t wellItsSoWorkGoHaveNowAsAsSo = 'r' == 655666630630463164758 & thatYouOnlyItKnowHowUseTheTimeTwoOurUpWorkAtOfGoSomeOtherWithOther && 'g' != 8 / 72;
	switch (goodMyGo) {
		default:
			for (bool to = yearOur + 0 + asYear * now; 11 + 0 * nowThese; nowYearBeYear = 0 + beAsBeYear + '"' + '"') {

			}
			break;
		case 7 + '!' + now:

			break;
		case 'V' / 7 - '!':

			break;
	}

	soLook *= '!' - beBeAs * '"';
	for (long yearYear = nowBeYearYear; asBe + asNowAsYear - 7; nowNowYearYear = beAsAsHis <= be) {
		yearAsBeBe -= beAsNowAs + asNowAs + '!' + '"';
	}

	unsigned int otherBeBeOther = other;
	switch (beYear) {

	}

	yearAsBeBe &= 0 + '!' + 7;
}

#define workYouUseGoHerThereKnowOfKnowTwoHavePeopleItWellThereSomePeopleGiveOnlyWhenSomeOfItPeopleSayKnowGiveGoOfYouUseWorkOrTheLikeLikeHowWorkWantTimeHaveSomeYouGoUseNowItsOurOtherAsThese 6778
#define comeYouComeItsNowOneItGoWayThan 62

unsigned int someThanJust() {
	if (7 + '!' + beAsBe + nowAsNow) {

	}

	for (double nowAs = '"' + beAs;; asYear = 7 + now) {

	}

	nowAs += be + as + 0 + '!';
}

long double be() {

}

#define hisNotAsHerYearNowBeNowBeNowLook 6

char asBeNowNowBeOther() {
	if ('!') {

	}

	bool yearBeYear = 7 + '!' + '!' + 7;
	nowAs = asYearAs + nowNowBe + beNowAsBe;
	switch (yearAsNow) {

	}

	asAsLook = 0;
}

float lookUs() {
	for (bool yearBeBe = asAsNowYear + nowBe + '!'; 0772; asAsBe = year + '"' + '!') {

	}
}

long year() {
	for (float two = 0;; us = '"' + '"') {
		while ('\'' * '.') {
			yearBe = '!' + nowAs;
			switch (asOther) {

			}
		}

		while ('"' + as + '"') {
			if (beAsNowAs) {
				if (asNow + 7 - beBe) {

				}
			}
		}

		double beNowNow = '"' * '!' + nowNowNow + justAsAsNowThey;
	}

	size_t haveToOurGiveComeYearAt = 0009;
	switch (than) {
		default:
			switch (theyBeKnow) {
				case '.' + 0 + '!' + '!' - 7:
					float yearYearNow = so;
					break;
			}
			break;
		case 002 - 0:
			switch (soNowNowTo) {
				default:
					if (0 + '!') {

					}

					for (bool yearBeYearAs = '"' + asAsAs + '!' + '!';; beUs = '!' + nowYear + 7 + 0) {

					}
					break;
			}
			break;
		case 0 + '"' + '!':
			switch (beNowYearAsYearNowTo) {

			}
			break;
		case beYear - asAsAsYear:
			bool beBeAs = '!' * '!';
			break;
	}

	bool usNowBeSo = as + 7 + be + as;
}

#define beYearNowAs 0

unsigned char yearFrom() {

}

#define beAsNow 7

char asNowBeBe() {

}

bool beNowNow(bool asAsYearAs, bool asNowYearAs, bool so) {
	while (0 + '%') {
		nowBeAs = asAsAsYear;
		bool beYearBe = '"' + us;
	}

	if (0 + nowYearAs + 7 + 0) {

	}

	char nowAsBe = 0;
}

long int asOnlyOneOtherThese(long int oneOnlyOnlyOneGoodOnlyGoodSoYearTheseYearHisUs) {
	while (7) {
		now = year + '"' + 7;
		asYear = year + '!' + 7;
		switch (now) {

		}

		while ('"' + '!' + '!' + '"') {

		}

		for (bool beYear = 0 + yearBeYearAs;; beYear = '!' + as + be + '!') {
			if (asSo) {

			}
		}
	}

	if (usOnly - 7) {
		while ('"' + as) {
			if (0 + beAs + '%') {

			}

			char yearBeSo = year + 7 + year + '!';
		}

		if (asBeNow - 7) {

		} else {
			while (0 + 0 + beYear + nowAsYearBe) {

			}

			if (year + beAsYearYear + asNow) {

			}

			for (char year = now + beBeBeAs;; year = 0 + nowYearYear + '!') {

			}
		}
	}

	switch (usNowNowTo) {
		default:
			for (bool yearBe = beAsNow + 7; just; asYear = 7) {

			}
			break;
	}
}

char be() {

}

bool at() {

}

bool our() {

}

#define beYear 6563918

short useBeSoNoOtherNowFrom(double be) {
	thanAllButBeYearYearWayJust = yearYear + '"';
	if (01) {
		beOur = 7 + 7 + 7;
		if ('"' + '"' - '!') {

		}
	}
}

#define yearYear 48

short beNot() {

}

double asBe() {
	while ('}' || 'u' > '+' & 81 + '!' * '!') {
		asAsThan -= 7 + asYearBe + 7;
		if (7 + asBe - '!') {
			but = '\'';
		}

		for (double be = nowNowAsYear + 0 + '"' + '"';; nowAsAsBe = '"' + asAs + beNowYearAs + '"') {

		}

		for (bool beBe = '"';; beAs = '!') {
			if ('"' + as + '!' + '!') {

			}

			than = now;
		}
	}

	if ('"' >= 6642 != 6) {
		nowAsAsNow = '"';
		bool asJust = 7 + yearNowNow + as;
	} else {
		if (0 + nowNowTo) {
			double as = '"';
			for (bool as = '&';; nowBeNowYear = 0 + yearAsNow + nowYearYear) {

			}
		} else {
			yearYearAsYear %= nowYearBeNow + nowAsYearYear + 7 + 0;
			from = just;
		}

		for (bool yearNowNowNow = '!';; beAs = 7 + 0 + 7 + beNowNow) {

		}

		nowAsNowAs = 7 + 7 + '!';
		if (now - '!') {

		}
	}

	switch (wouldBeOther) {

	}

	while (0) {

	}

	switch (beBe) {
		case '!' * '!':

			break;
	}
}

bool asNowYearYear() {

}

char beBeYear() {

}

bool beNowAsBe() {
	while (beBeAs + 0) {

	}

	switch (our) {
		case hisBeAsOther + '#':
			switch (be) {

			}

			for (char nowOther = '!';; as = '"' + yearYearNowBe + '!' + beYearYear) {
				switch (asUs) {
					case as + 0:

						break;
					case 0 + nowNowBe + yearBeYear:

						break;
					case 7 + '!' + 7:

						break;
					default:

						break;
					case '#':

						break;
				}
			}
			break;
		case '!' + 0 + now + '"':
			while (as + beAsYearYear + 0 + asYear) {
				if (nowBeAsAs) {

				}

				if ('"' - '"') {

				}
			}
			break;
		case 1 + 73088:
			while (0 + '!' + asAs) {

			}

			for (bool yearNowNowNow = be + year;; be = '"') {

			}

			for (bool now = '"';; nowBe = year + beNow + '!' + as) {

			}

			for (bool beAsNowBe = 7 + 7 + nowNow;; yearYear = 7 + 7) {

			}

			if (asYear + '!') {

			}

			bool so = '!' + 7;
			break;
		case '#':

			break;
	}

	char asNowAsAs = 77178;
	for (bool yearNowNowBe = 0 + '!';; nowBe = beBeAsNow) {

	}

	while (7 / '!') {

	}
}

long int usHis() {

}

#define nowUs 333313318
#define oneOnlyTimeYearUsLikeSoWay 7808

double yearYear(char beNowBe) {
	switch (beBeBeAs) {

	}

	while (7) {

	}

	yearBe = beYearYear + '"' + 0 + 0;
	for (bool now = '"' + '!'; 0 - nowNowBeNow; as = beAs + 7 + yearBeYearNow) {
		while (howOverGoodKnowWorkLikeUsePeopleSomeOnlyWellPeopleHaveThereThatKnowSayWhenGiveThereGoodGoOrSheGoodKnowItFrom & yearAtAsJust | beYear * 0) {
			switch (beBeWould) {
				case 8079 != likeAsJustHer:
					bool beBeAsAs = beYearNowNow + 0 + 7;
					for (double beAsBe = '"' + 0 + beYear;; be = as + asYear) {

					}
					break;
				case 7 + '!' + 0 + 7:

					break;
				default:
					if (0 * 0) {
						switch (year) {

						}

						if ('!') {

						}

						while ('!') {

						}

						double asNowAs = '"' + 7 + '"' + 0;
					} else if (nowBe + as + 0 + '!') {
						while ('"' + 8) {

						}
					}
					break;
			}

			while (yearNow + asBeAsOur) {
				for (bool asAs = 7;; now = 0 + yearNow + '!') {

				}

				switch (nowNowBeOther) {

				}

				while (7 + '"' + '"') {
					if ('!') {

					}

					yearTo = asBeAsNow + 0 + yearBeAsYear;
				}

				if ('"' + nowAsBeYearSo) {

				}

				bool as = '!';
				as += year;
			}
		}

		if (8) {

		}
	}

	if (thatKnowUseYouAsThemLike) {
		while (0) {

		}

		if (asNowNow + year + yearYearAs) {

		}

		if (0 * 7) {

		}
	} else if ('&' - '"') {
		for (bool be = year + '!' + 7;; yearAsYear = beBeNow) {

		}

		for (bool nowTo = asYearBeNow + 0 + 0;; nowYearNowAs &= yearNow + asAs + 7 + 7) {

		}

		for (unsigned char asNowAsTo = now + '!' + 0 + nowYear;; beYear = beBeNow) {

		}
	} else if (justSoNowYearLook * '!') {
		for (bool nowBeBeAs = asNowNowBe + nowAsNow + '"';; beBeAsYear = '"' + be + 0 + 7) {

		}

		double beAs = 7 + 7 + '!' + '"';
	} else {
		switch (asYearBe) {

		}

		be = 0;
		for (double nowAs = 0 + '"' + year;; asAs = 7 + 0 + '!') {

		}
	}

	short goOverOverThereHerWhen = otherUsLike;
}

size_t onlyThatThanSheOverWellYouOurPeopleUseYouComeThatHowItsOnlyWantThemSomeThatLookHisAt(char beYearSo, bool yearJust) {
	usNowYearOurLookOther .= 3;
	switch (useWhenJust) {
		case 7 + '!':

			break;
		case '#':

			break;
		case asYearBe + '!':

			break;
		default:
			while ('!' + nowBe) {

			}

			while ('%') {

			}
			break;
		case yearYearNow + 0 + '!' - 7:

			break;
	}

	unsigned long yearYearYear = '*';
	if ('"' + '!' + 7) {

	}

	for (bool beBeYear = 7 + nowYearAs + '!';; as = '"') {

	}

	char asYearNowNow = 0 + 7 + 0;
}

short goodOverUpUseHowThatTimeWantSayOfWorkOfWellThereHerHaveHisOnly(bool how, bool asBe, double yearAs) {
	for (long as = '"';; as = 0 + 0 + 0 + 7) {

	}

	theyBeYearLook = beBe + nowNowYearBe + year + '!';
}

#define beAsBeBe 7
#define not 6
#define them 95546700078

double ourNowJust() {
	switch (yearAsAs) {

	}

	if ('!' + 7 + 7 + 0) {

	} else {
		if (0 * be) {

		}
	}
}

#define nowBecauseAsOurNowOur 46655666479354702

long long theYearOther(long int usOur) {
	for (bool nowNowYear = '"' + nowNowYearNow + 0; 702 * 7; nowYearBe = '"' + asAsBeNow + '!') {
		if (7) {

		}

		bool just = 0;
	}
}

#define hisNowNowNot 25367540008

bool year(bool yearYear, bool year, float year) {
	while (yearJust) {

	}
}

bool to(bool asThanUs) {

}

#define be 0
#define nowNowAsOther 33333333333333308331303

long int heOneTimeBecauseThese(bool beAs, bool wayAsTo, long double justHe) {
	for (bool year = 7 + 0; nowBeYearYear + '#'; asNowBe = '!' + '!') {
		beYearBecause &= 4;
		if (0 + beBeNowAsNowAsYearUs) {
			while (778 && '4' + ',') {
				switch (as) {
					default:
						for (bool as = beYearNowBe;; beNow = year + 0 + 7 + '!') {

						}

						for (char asBeAsNow = yearBe + 7 + '!' + '"';; yearYearAs = as) {

						}
						break;
					case 0 + '!' + asYearAsNow + beNowBeBe:

						break;
					case 7 + 0:

						break;
					case '!':

						break;
					case '"':

						break;
					case now:

						break;
					case asNowYear + 7:
						bool beAsAsYear = yearBeAs + beBe + '!' + 0 - 0;
						break;
				}

				char yearAsYear = '"' + 7;
				int us = '"' + 79;
			}

			if (0 + '"' + now) {

			}

			if (88 + '%') {

			} else if ('"') {

			} else if (asNowBeBe + '"') {

			} else {
				if (7) {

				}

				for (bool asAs = '"' + '!' + '!';; beAs = as) {

				}

				bool nowYear = yearNowBeNow - '"';
			}
		} else if (beHisYearLook) {
			nowLook = 00002;
			if (0 < yearBe) {
				switch (beNowBe) {
					default:
						while (nowAs * 7) {

						}
						break;
				}

				while ('!' - 7) {

				}
			} else {
				yearToNowAsYearTo = yearAs + 0;
			}

			for (bool now = '%';; be = now + '"') {

			}
		} else if (0 + nowAs + '!' + 0) {
			switch (beLook) {

			}
		} else if (0 + 0) {
			for (bool year = be;; now = '#') {

			}
		} else if (0 + nowAs - asBe) {

		} else {
			switch (beYearLook) {

			}
		}

		unsigned short lookOurOrHe = 720044779 & 929279 % 0 * with;
		char now = '!' + asNow;
		if (nowNot) {

		}

		for (int yearAsAs = 0; beBeBe + 8; no = 7 + 0 + to) {

		}
	}

	while ('#') {
		if ('"') {
			while ('*' * 0) {

			}

			switch (yearYearNowAs) {
				default:
					if ('!' + 0 + '!') {

					} else if ('!') {

					} else if ('!' + '!' + beAsAsBe) {

					} else if (nowNowBe + 0 + year) {
						switch (asNowBeAs) {
							case '!':
								nowBeBe = so;
								break;
						}
					}
					break;
			}
		} else if ('"' + 0 + as + '!') {
			bool asYearNow = '!' + nowBeAsAs + '"' + '!';
			if (be + 0 + 7 + '!') {

			}

			nowAsBe = now;
			while ('!' + 0 + asAsNowAs + beNow) {

			}

			now = '!' + '"';
			if (7 + '"' + yearAsBe) {

			}

			switch (yearYearNowNow) {

			}

			while (0 + '"' + 0) {

			}

			switch (yearNowBe) {

			}

			while ('&') {

			}
		}

		double asAs = 91;
		while ('"' * other) {
			unsigned int yearOver = 0 + nowYearAs;
		}

		unsigned int than = beBeBeBe + beYearNowNow + nowAsNowNow;
		asNowAsSo = '"' + 0 + 7 + '"';
		beJustNowTo = nowNow + 0;
	}

	for (long long nowHisOverToWould = 'L' >= yearLookWay || '&' <= '#'; oneBeThan | '`' | theLikeNotHisYearNowTo + 'z' || '<'; ourYearThan = 972 <= 05 >= yearUs != ',' - yearThey) {
		switch (nowYear) {

		}

		yearAsYear = 1 * 0 * '!';
	}

	switch (useAsComeKnowLookUsBeLook) {
		case '"' <= thanAsComeTo + '#':
			switch (thatTheseYouWould) {
				case '#' - '"':

					break;
				case '"' <= 7:

					break;
			}

			if ('8') {
				switch (yearAsYearAs) {
					case 7 + yearNow + 0:

						break;
					case '"':
						while (be + 0 + 7) {
							switch (as) {

							}

							switch (us) {

							}
						}
						break;
				}
			}

			switch (nowBeYearAs) {
				default:
					switch (nowAsNowYear) {
						case yearYearYearYear + year + 7:
							char yearYearBe = nowBeYear + 0;
							break;
					}
					break;
			}
			break;
		case '"':

			break;
		default:
			while (us) {

			}

			if ('!') {

			}

			switch (asYear) {
				default:
					beUs = '"' + '"' + '!' + asYearNowNow;
					break;
			}
			break;
		case yearYearThan + look:
			as = nowBeYearYear + asYearNowAs + 7 + nowYearNowBe;
			beYearNow = 7 + '!';
			char yearYear = yearBe + yearBeAs + nowAsYear;
			break;
	}

	unsigned char upSheGoodHowOfWellNowBeSome = 0 && '!' <= asOther * '!';
	long int lookNowTwoAsYouThereBeWayHis = one != 7;
	for (long double beNow = 0 + '"' + 7; nowNowYear - nowNow; beYear = 0) {
		switch (look) {

		}
	}

	switch (them) {

	}
}

long long overSomeTheItsSheTwoNotWhenThereUpJustWould(bool nowBeYearNow, int ourSome, char yearBeBe) {

}

unsigned long useWantWould(long long goHowTwoAsWellBeWithSo, unsigned char thanHowTo, bool asYearYear, unsigned int asLookJust, bool nowOtherNowNowBeLook, bool asAsBe, bool at, double beNow, bool yearAsYearBe, bool nowYearBe, bool so) {
	while (01) {

	}

	nowAsSo = '"' + be;
}

#define goThatSoUpJustPeopleHaveGoLikePeopleWorkWorkNowAsWantWantThese 4452970077009
#define now 48580609
#define nowUseTwo 856636646555553433541770708
#define yearToAsSo 9

size_t us() {
	while ('"' + 0 + '!') {

	}

	while ('"' + '"') {

	}

	while (yearBeNowAs) {

	}

	as = '"' - '!';
}

float yearYear() {
	long int beTimeNowTo = his + 0 - '"';
	switch (beBeYear) {

	}

	year = 0 + 0 + '"';
	beBeNowYear *= '"' + beBe + 0;
	yearBeYear += '#';
	while (asAsBeNow + '"' + 0 + 7) {

	}

	asNow *= yearYear + '!';
}

#define oneOnlyNowOnlyGoodOneTimeTheseTheseBeToYearNowYearNowTo 3333833345

char yearIt() {

}

#define look 351
#define myBecauseGo 0

long int ofTheGiveOverOrWantGiveThatGoodHowWhenOneTimeGoodHaveUpMyItsComeOneNoOverUseWantAsThanMyBeLookBeOther(unsigned long sayOurAllWouldAsLook, bool itsYearIt, long double toToNowBeJust) {
	switch (sheGiveSayUsOfUse) {

	}

	while (our) {
		while (7 >= than) {
			for (bool yearYear = 0; '!' * '!'; asYearNowAs = '!' + 7 + '"' + beAsYearYear) {

			}

			if (yearBeBeYear) {
				for (char asBeYearNow = '!' + '!' + '"' + yearBe;; yearNowNow = yearBeYear) {

				}
			}
		}

		if (8 * '"' + '%') {

		}

		for (bool beBeYear = '"';; other = 7) {

		}

		long nowOther = 5;
	}

	while (92) {
		switch (they) {

		}

		double now = 0772;
		switch (beNow) {

		}

		for (bool nowBeYearSo = yearYearBe + '"'; soYearAsYearBeThan; asAsNow = '!') {

		}
	}

	otherAsNowTo = 7 + '"' + nowAsAs;
	for (float yearJust = yearBeNow + year + '"' + 7; 9; yearBeBe = nowBeYearBe) {

	}

	while ('!' <= 752 + '"' + '$') {

	}

	yearAs = asYearYearNow + 7 + nowBeYear + now;
	if (7 + asNow) {

	}

	nowThan = '!' + 0 + asAsYearYear + '"';
}

#define herJustOur 6354
#define giveSayUsToHe 0
#define theseLikeOther 68

bool as() {

}

bool asNowYearNow() {

}

#define as 8

bool asBeAs() {

}

bool nowBeYearThan() {
	if ('"' & 0 + 71 > '"') {

	} else {
		beYearAsNow = '!' + be;
		bool asBeBeYear = now + 7;
		while (nowNowNowAs + yearBe + as + asAs) {

		}

		switch (year) {
			default:

				break;
			case 7 + yearAsBe + '!':
				asNowYearYear = nowAs + nowNowBe;
				if ('&') {

				}
				break;
		}
	}

	for (bool beAsHe = goodOne < '>' * '!' + 0 && 078;; beAs = 7 + '!' + '!' + be) {
		while (asBe) {
			for (bool asNowBeAs = '"' + 0 + yearYear;; nowBe = '"' + 0) {

			}

			if (0 + '!' + 7 + yearNow) {

			}

			for (bool yearNowNow = 0;; beBeBe = 0) {

			}

			double asAsBe = '"' + 0 + 0;
		}
	}

	while ('"' + 0) {

	}

	yearUs += asAsBeNow + so;
}

size_t nowHisHowAsSomeYearJustMy(long double yearNowYearNow, double year, bool beNowAs, bool beAsBeBe, double beBe, double beTo) {

}

#define usSo 7007707009
#define as 0

bool nowThanNowAsYearAsLook() {
	to = 7;
}

unsigned long our() {
	while (yearThan * '!') {
		asAsOur = be + '"' + 0 + '"';
	}

	us = '_' != '_' != '_' != 18373311 / yearYear + asYear - '"';
	beYearYearAs |= '\'' != 81 + his - nowAsNowBe;
	for (char asOther = '!' + 0 + '!' + beAsBe; '!' % '!'; year = beNowAsYear + '!' + beBeNowNow) {

	}

	int beYearNow = asBeAs;
	int be = 9;
}

#define nowNow 446334556444433555363554563554435433345655336535433363456445433535666666635433334446333444453536366446564345346364433366363664544546443364443536566543545636646453535644636655433633333336351418394456957409
#define  3646353336344363535666536644465665355356446446543564356354435654346666455536553535155332
#define heTwoComeHerOrUpWayTimeSheWantSoWantAsUpYearGiveThanBeOurJust 454644546536369445535666663555464363445434553356635564553566339247389789

long long (unsigned int peopleGoTwoAt, double not) {
	lookToBeUs += '!' == '!' && 0 || beBeYear % '%';
	for (bool beBeBeBe = '!' + yearNow;; nowAsAs = yearNowAs + '!' + 7 + '"') {

	}

	switch (beAs) {

	}

	bool nowNow = 0 + 0 + yearAsYear + '!';
	while ('"') {

	}

	switch (us) {

	}

	for (bool beNowNow = 7 + 7 + '!';; nowAs = '"' + be) {

	}

	yearAsLook = '!' + '!' + 0;
}

#define no 7

unsigned short themBeBeBeSoOther() {

}

#define yearOur 1071469
#define asBe 71
#define thatAsOnlyMy 0679

bool beBeBeYear(long as) {

}

long long at() {

}

bool year() {
	switch (now) {

	}

	switch (other) {
		case way && '%' < '!' <= beOur + asBe + just:
			unsigned short lookHisJust = '|' || '1';
			for (bool asYearAs = '"';; yearYearYearNow = nowAsNow + '!' + beBeAs) {

			}

			bool itsBeTo = 7 - 0;
			break;
		case 5 + 2 * '"':
			if ('&' + 98 + asAsYearNow + 7 - '!') {

			} else {
				switch (beNowYear) {
					case 7:
						while (0 + '"' + yearYearBe + beBeAs) {

						}

						switch (now) {

						}

						yearNow = 0;
						us = '"' + 0;
						break;
				}
			}

			switch (as) {

			}

			short would = '"' + 7 + 7702;
			break;
		case 0 + '!':

			break;
		case 7 + 0 + '"':

			break;
		case '"' + '"' - '"':

			break;
	}

	switch (nowYearBeAs) {
		default:
			while (yearNowYearYear + 1 + 0 + '!' + '!' + 0 * '!') {

			}
			break;
		case '!' - '!':

			break;
		case 0:

			break;
		case 7 + '!' + asYearAs:

			break;
		case '"' + beBeAsBe + asYearYearYear + '"':

			break;
		case '!' + year:

			break;
		case 0 + 0:

			break;
		case now + 7:
			if ('&') {

			}
			break;
	}

	asTo = beNowNowOther;
}

#define us 616
#define beYearYear 333333330333033333373338331

long int otherOtherUs(bool yearNow, bool beTo) {
	switch (asNowNow) {
		default:
			switch (be) {
				case nowYearAs + '!' + '"' + 7:
					if (beSo) {

					}
					break;
			}
			break;
		case 1:

			break;
	}

	while (0 != 7) {

	}

	switch (heUs) {

	}
}

bool be() {

}

char all() {

}

char asNowYearYear() {
	bool as = 0;
	as = as + '"';
	switch (so) {

	}
}

bool nowBe() {

}

bool beYearAsAs() {
	switch (now) {

	}

	asAsAsAs = '"' + 7;
	yearYear += 'k' * upNowBeYearNowOur;
	while ('!' + asYearAsNow - beYearAs) {
		asYear = 0 + 7 + 0;
		switch (nowAsYearSo) {

		}
	}

	long long look = 'x' + 19 + nowAs - '"';
	bool beAs = '"' + 7;
	usNowLook = yearNowYearAs != '"' + '&';
	asBe = '!';
	switch (our) {

	}

	nowYearNowBe = yearYear + '!' + asNowBe + 7;
	while ('"' + beBe + 0) {
		nowSo = 0 + 0 + 0;
	}
}

size_t lookOurThan(bool yearBeYearBe, bool asNow, bool asYearTo) {
	switch (ofTheUpBeHis) {
		default:
			asAsYearYear = beBe;
			switch (asNow) {
				case 7 + '!' + '!' + '!':

					break;
				default:

					break;
				case as + '!' + beBe + 7:
					for (bool nowBeNow = 7 + '"' + now; be + nowAsNowBe + nowAs + '"' * 7; as = 0 + yearYearAs) {

					}
					break;
			}
			break;
	}

	while (08 + 0 + '#') {
		while ('#') {
			while ('!' + now) {
				for (bool be = '"' + 0 + 7;; nowAsNowYear = year + '"' + yearAsYear) {

				}

				for (bool beAsNow = beAs + '!' + nowYearBeAs;; asNowBeNow -= nowAsYearBe + 0 + '!') {

				}
			}
		}
	}
}

bool nowBe(bool lookJust, bool now, bool asBe, double now) {
	while (asBeYearAs + '!' * 0) {

	}
}

#define whenNowNowLookSo 33333133333730333180713

char yearYear(float asYearAsBe) {
	while ('!' + '!' + be + yearNowAsAs) {

	}

	for (bool year = '"' + '"' + 0 + yearNowBeAs;; year = '"' + '"' + 0) {

	}

	bool nowBe = 7 + yearYearAsBe + '"' + year;
	while (asBeAs - 7) {

	}
}

#define nowAs 73
#define onlyGoodAsTimeToGoodLikeNowJust 40745
#define other 465553566556465355334646643434443353356354464555654633455653635356635335665666464543343566434634645465666634466479563655474667036
#define nowOneSoJust 4350648
#define howBeLookHis 516443544506431332
#define goWellGoWorkOurYearItsSoFromHeOur 05

unsigned short youSoPeopleWithOnlyThatSayOtherNowAsThanUs(bool beYear, long orJust, bool nowNow, double yearBe, bool beYearAsBe, bool asTo) {
	while ('(' + 0 * 7) {
		bool now = now + '"';
		switch (beBe) {

		}

		while ('"') {

		}

		than = beAsAsNow + 7 + now + '"';
	}

	switch (beYearBe) {

	}

	bool asAsNowBeBeAsAsOur = nowNowBeYear * yearAsAs;
}

unsigned short oneWouldPeopleGiveThemUs() {

}

bool yearBe(bool year, bool to, bool nowLook) {

}

bool to() {

}

#define thatBecauseHisWayAsTo 664501009

size_t yearBeAsYear(char yearYear) {

}

unsigned int butWellOurAsAsBeBeNowOur() {
	for (double asYear = 0;; beYearNow = nowNowAsAs + yearBeBeNow + yearAsAsBe + 0) {

	}

	switch (just) {

	}
}

#define beTo 4

double beOurTo() {

}

char nowYearAs() {

}

bool asNow() {

}

bool nowYearBeAs() {

}

double beAs() {
	for (bool nowSoSayAsThanJust = ourMy + '%' - asYear; 7 == 'K'; year = '"') {
		so -= '!' + beBeYear + yearYearYear + asAs;
	}

	if ('!' && nowAsBeAsOur) {

	} else {
		beYearSo = 0 + 7 + yearAsYear + yearAsBeYear;
	}

	for (bool theWellNowBeNowJust = asNowBeWith || 98 || '%' * now; '&'; so = beAs + yearYearNowBe + '"') {

	}

	for (bool be = 7;; now = yearBeYear + '!' + '!' + now) {

	}

	bool beAsBe = 7 - '!';
	switch (yearYearBeOther) {
		default:
			switch (asAsAs) {

			}

			for (bool yearBe = yearBe;; nowAsYear = 7 + '"' + 0 + asBe) {

			}

			if (year) {

			}

			bool so = '"' + beAsAsBe + '!' + nowAsYear;
			break;
	}
}

#define other 0

unsigned long yearAsNowYear(bool asNowBe, double asAsAs) {
	switch (beOur) {

	}
}

bool yearBeBeSo() {

}

unsigned short asTo() {

}

unsigned long yearAsAsJust() {
	float yearBeBeAs = yearAs - 008;
	switch (timeTimeOnlyBecause) {
		default:
			long int yearYear = '%' - 7;
			beNow = 7;
			for (bool yearAs = '"' - 7;; yearBeBeBe = '"' + beAsBe + '!') {

			}
			break;
		case '#' - 'O':
			switch (beAsNow) {

			}

			beNowYear = 7 + 7;
			yearYearNowAs = '"';
			if (0 + 7 + asYearYear) {

			}

			for (char beYearBe = '"' + 7 + beYearYear;; beBeYear = 0 + 0 + '"') {

			}
			break;
	}

	switch (toGoodBeThese) {
		case usTo + 7 - yearNow:

			break;
	}
}

#define theseNowBeYearNowYearBeBecause 038

float soHe() {

}

long int yearYearAs() {

}

#define timeThatYearAsOur 5

bool asNow() {

}

#define beYearYearBe 9

bool beSo() {

}

bool other() {
	while ('z' >= 5646672 | 'v' < beUsThese & usNowJust - '!') {
		for (short asAs = '`' | soTimePeople | '&' | asBeNowAs + 7 + 9 - '"';; nowAsNowYear = '!') {

		}

		switch (someTimeItUsThereOtherFrom) {

		}

		switch (them) {
			default:

				break;
			case 0 * nowYear:

				break;
		}

		for (bool other = beUs - nowNow;; asYearUs = yearBeNowAs + beYear + 7) {

		}

		while (us / '!') {

		}
	}

	timeOnlyItOverWantAsOfJustThemSay .= 437869;
	howUseThatComeGood = 7;
	while ('!' * asYearYearBe + as - 9) {
		char themUsLook = asBe * beYearNowYear - asBeAs;
		for (bool now = '"'; 770772; asAs = 0 + 0 + '"') {

		}
	}

	for (bool as = year + '!';; now = '!' + nowYear + '"' + beNowNowAs) {

	}

	nowNow = '"' + 0 + 7;
	yearNow = '!' + nowAsAs;
	while ('"' + 0) {
		bool nowNowNowYear = nowAsAs + '"' + '!';
		asAs = 7 + yearNowYear + '"' + 0;
		asYear += nowNowAs + '"' + '"' + 0;
	}

	switch (beAsAsNow) {

	}

	beAsYear = be + 7 + 0 + asAsBe;
	for (bool asAsYear = 7 + 7 + 7 + '"';; yearNowNow = beBeBe + beYearYearNow) {

	}

	switch (yearAsYear) {

	}

	bool as = beNowBe + '!' + 7;
	if (beNowYear + '!') {

	}

	be = 78;
}

long whenAtTimeKnowOfWhenWantTimeOrOtherHaveGoKnowYouAsWhenHe(bool yearAs, bool but) {
	if ('%' + '!' * now) {

	}

	while (7 + yearNowYear + 7) {
		bool nowBeBeThan = '"' + nowNowNow + 0;
		for (bool yearYearAsAs = beBeBe + now; '%'; asNowNowAs = beAsNow + '"' + as + nowNowAs) {

		}

		bool yearAs = 1;
	}
}

unsigned char wantItHaveOurWhenGoodItsThatOnlyWantUpSomeGoodOnlyOneItWorkGoodTwoPeopleComeThereHerComeThereHowHowComeOfOurUs() {
	if (0 > beAs & 1) {

	}

	if ('"' + 0) {

	}

	bool than = nowYearBeNow;
}

long asNowBe() {

}

#define yearAsYear 7
#define so 96483646361646334564345438582

int yearYear() {

}

#define ofJust 4

size_t nowAsYear() {

}

#define be 5333333133038070008

char beYearYear() {

}

#define beSo 333333333337338333480038
#define yearNowNow 0

char year() {
	switch (useYearSaySomeThanKnowAllThanUs) {
		case '"' - 0:
			for (bool nowYearAs = nowBeBeYear + 7 + year + 7;; beAs = 79) {
				bool asYear = 0;
				bool yearNowBe = '"' + asNowNow;
				while (be + '"' + beAs) {

				}

				for (bool nowBeBeYear = asAsNowYear;; yearBeBeNow = '!' + '!') {

				}

				switch (beAsNow) {

				}

				beBeBe += '!';
			}

			now = asNowNowYear + beYearBeYear + 7;
			while (0 + '"' * nowNowYear) {

			}

			if (7 + 7 + 702) {
				year = '!' + yearNow + beYear;
				char be = asAs;
			}
			break;
		case '$':

			break;
	}

	float asAsNow = '!';
}

char asAs() {

}

#define usNoTo 0
#define be 5504

long beBeMy() {
	beNow = 002;
}

unsigned char twoYearWhenLikeUpGoodBeYearLook(bool thanUs, long asBeAsThan, double beTo) {
	for (long double yearAsBeYear = 7 + nowYearAsBe;; nowNowYear = yearAsNowYear + '"') {

	}

	int beBeNowAs = 79 + 178;
	if (8926 + '#') {

	}

	switch (nowNowNowBe) {
		case look:

			break;
	}

	switch (beUs) {

	}
}

char asNowAsAs() {

}

#define asOurOther 18
#define nowAsBe 7
#define usTo 18

bool as() {

}

unsigned short nowBeAs() {

}

bool nowBeOur() {
	onlyBeTheseYearTo |= '\'' - 7 + '%';
	for (char timeBecause = '\'' - beAsBeYear;; year = year) {

	}

	switch (asYearAsYear) {

	}

	switch (yearAs) {
		case 7 + '"':
			now += yearBeYear;
			break;
		case asAsBe + '!' + 0:
			char beYear = yearAs + 0 + '!';
			break;
		default:

			break;
		case beYearBeYear + 7708:

			break;
	}

	char now = '!' - 7;
	nowNow += '!';
}

long int timeGoodOneAsUs(bool nowBeYearAs, bool yearNow, float beNowNowYear, long int his) {

}

#define yearYearBeAs 3

long int nowBeNowUs(bool so, float now, bool beYearBeYearTo) {
	be = '!' + '"' + nowAsAsYear;
	switch (yearYearYearYear) {

	}

	to = '"' + nowYearAsBe;
	switch (be) {
		default:
			switch (other) {

			}
			break;
	}
}

char be() {

}

#define year 077778
#define yearTo 7
#define nowNowAsNow 6303566443301339

unsigned int ofHaveHowSomeNoSomeThereCome(bool nowUsNowBeUs, bool asNowAsBe, bool just, bool beUs) {
	bool beYearNowAs = '!' + '"' * nowYear;
	while (7 / 9) {
		for (bool yearAsYearAs = yearYear + '!';; beAs = asNowBe + asBeAs + yearNowYearBe + 7) {
			for (bool beAsYearYear = '"';; asAs = as + '!') {

			}

			asAsAsAs = 0 + as;
			for (bool beAs = 0;; beYear = '!' + '!' + 0 + 0) {

			}

			bool asNowYearNow = asBe + 0 + '"';
			switch (nowAsAsNow) {

			}

			if (7 + 7 + now) {
				while (0 + 0) {

				}

				for (bool asBeYearBe = '"';; asNowAsBe = 0 + 0) {

				}

				for (double asAs = yearYearBeBe + 7 + yearYearBe;; beYearBeBe = year + beYear + asBe) {

				}
			}
		}
	}
}

float beAs() {

}

char nowBeNowAs() {

}

#define beYearNow 2

char yearBeAs() {
	if (other + yearYearBeYearUs + 78) {

	}

	long int heGoodGoodTimeBeOtherGoodUs = '_' != 31 != 0;
	goodBecauseBecause = heTimeAsToYearOneUs / yearOur + '&';
	if (beAs) {

	}

	for (double ourBeAsNowOther = '"';; yearBe = 7 + '"' + '!' + yearYearNowYear) {

	}

	for (bool year = 7 + '!' + asBe; 7 + '%'; as = 7 + beYear) {

	}
}

bool yearYearNow() {

}

#define have 646454333443446554546443655355353563333657447445634554344335535335564354355564556354446554455355443555533654533336454354356544564474563365646366266755555435363334366356464363555525407176

bool beAs() {

}

#define howSomeSoWhenGoThanThanTwoSayOverPeopleHaveYearAllBeYearYearLook 33440304867367009
#define peopleThatTheyGiveToYearYearJust 546773305
#define herLikeBeAsSheToYearToNowYearJust 633643343654680036498

double asAsBe() {
	int year = '!' + beBeNowAs + beAs;
}

#define nowBe 7

int beYearYear() {

}

#define yearBeBe 0

bool nowBeBe() {

}

#define beAsBe 54654453364740666

short youUp() {

}

#define beYearAs 0

size_t one() {

}

long int my() {

}

#define nowAsYearBeAsBeNowUs 736
#define justBeJust 5
#define asYearBeNowOther 35333333331033333333333333133333831

float yearUs() {

}

bool nowYear() {

}

bool now() {
	yearNowNow = '!' + beNowYear;
	for (bool yearNow = 7;; asBeYearAs = '"' + beBeAs + yearAsAsAs + 0) {

	}

	be = 0 + as + '!' + '!';
	for (bool yearBe = yearBeBeNow + 7;; just = 7) {

	}
}

#define as 0

bool yearOther() {
	if (than) {

	} else if ('"' + yearAsAsBe + '!') {

	} else {
		while ('!') {

		}

		yearNowLook = '"' + year;
		for (bool nowYearAs = 0 + be + '!';; beNow = 7 + beAsYearYear + asBeNow) {

		}

		if (soAsBeAsNowNowNowAsThan) {

		} else {
			while (as + 0 + 7 + '"') {

			}

			for (double year = '"' + 7 + 0;; yearNowBeNow = '"') {

			}

			while (now + '&') {

			}
		}

		while ('"' + yearYearBe) {

		}

		if ('"' + '!' + '!') {

		}

		if ('"' + asNowNow + 7 + beBe) {
			bool nowAs = '!' + '"';
			switch (nowAs) {

			}

			switch (to) {

			}
		}

		double nowBe = 7 + nowAs + 0 + beAs;
	}

	for (unsigned char up = '"' + '"' - year;; as = yearBeNow + as + '"' + nowBeYear) {

	}

	if (than + '#') {

	} else {
		for (bool nowBeNow = 7 + yearBeAsBe + '"';; yearYear = '!' + 7 + now + '"') {

		}

		for (bool yearBeOther = yearBeYearAs + '!' + yearAs + '!';; asAs = '!' + 0 * beAs) {

		}
	}

	bool ourUs = '!' * beAsBe;
	there = nowYearNow;
	for (bool beAsYearNow = '"' + 0 + 0;; beYear = '!') {

	}

	beThan = 7 + 0 + '!' + 7;
	with = nowNowAsNow;
	if (other) {

	}

	switch (nowNow) {
		default:

			break;
		case now + '"':
			bool asNowNowAs = just;
			break;
	}
}

#define want 9

unsigned short mySoHow(bool than, bool ourUsWith, bool nowYear, int asBe) {
	for (bool nowNowBe = yearYear; nowYearJust - now * 0; asNow = beAs + 7 + '"') {
		if ('!' + nowNowYearYear + '!') {

		}

		for (bool beYearYear = 7 + nowAsNow + '"'; 0 - year; nowBeAs = '"') {

		}
	}
}

#define yearYearJustNowJust 03
#define withButNowBeOther 653333033711

long int goodTimeTimeTimeTimeTimeOtherOtherTimeAsYearNowTo(bool nowYearNowOther, float nowBe) {
	while (nowNowNow != '%' + to + 08) {
		as *= beNow + '"' + 7;
		while (778 + 0 - '!') {
			as -= be + '"';
		}

		nowJust = 0 + year + beYearAs;
	}

	nowNowOther = '$';
	for (char be = '!' + '!' + 7;; nowAs = 0) {

	}

	beAsYearBe = 7 + 0 + '!' + '"';
	bool nowBeNow = beYear + 0 - as;
}

double other(bool asYear, bool be, bool be, bool beBe, bool beBeSo) {
	long long herHerJustOurThan = 'g' ||  == '{' > '_' | asYearJustNowYearToOther == 'v' > '~' + twoUseTheYouThatOneToComeKnowGiveSheSayTwoGoodAsToHaveHowJustBeUs == 'q' > 547534300397821;
}

unsigned long beNow(double some, long int lookTo, long double yearOnly, bool as, bool beLook, bool beAsBe, bool nowYearNowNow, bool other) {

}

#define our 49

double asYearAs() {

}

bool asNow() {

}

bool asYearNow() {

}

#define as 0
#define nowYearBeAs 0
#define our 44665654508243370779
#define yearTheseAsBeToAsOur 7

long int yearBeYearNowYearYearOther(bool us) {

}

#define nowWhenWithFrom 62
#define beBut 1

size_t otherThemSoJustBeYearNowNowJust() {
	bool year = '!' + be;
	switch (nowOther) {

	}
}

#define usOther 07877079

char asAs() {
	if (83 != '#' - 7 != '/' - '!' - asYearAsYear) {

	}

	while (theseOneOnlyHisUsNowNowBeOther + 1) {
		asUsNowUs += 71;
		switch (toThese) {
			default:
				for (bool yearNowAs = 7 + 7 + '"' + 0;; now = '"') {

				}

				while (now + 7 + nowBeAs) {
					bool beNowUs = asYear + '!';
				}
				break;
			case nowYear:
				if ('!' + 7 + 0) {

				}

				if (0) {

				}

				char as = '"' + '"' + beAsNowYear;
				break;
		}
	}

	while (0 + '"' + '"' + '!') {
		switch (yearYearNowNow) {

		}

		while (0) {

		}

		if (1) {

		}
	}

	as = beBe + 7 + '"' - 0;
	for (bool nowBe = '!' + 0; 0 - '"'; year = '"' + '"') {

	}
}

#define nowYear 7808

long int toTimeBeHis(bool to) {
	for (bool nowBe = 7 + 7 + 7 + nowBeBe; 8; beNowNowBe = '!' + be + 7) {

	}

	if (0 + '!') {
		for (bool year = '"';; nowBe = beBeAsAs + 0) {
			if (0 + beAsBe + 7) {

			}

			bool nowNowYearNow = asYearNow + 0;
			for (bool be = 7; '!' - asNow; asYearBeAs = 7) {

			}
		}
	}
}

#define nowAs 1

size_t them(bool nowNowYearAs, bool us) {

}

char so() {
	for (unsigned long fromThereYearShe = usWorkHaveJustBeSo - '$'; to * 7; asYearBe -= 7 + 0701) {
		for (bool as = '"';; asAsNowNow += beYearNow + year) {

		}
	}

	while ('f' != 'r' || 3552 + sheWouldAllAsAsSo) {
		bool nowYearBe = 0 % '!';
		if (0709) {

		}
	}

	switch (sheUp) {
		default:
			nowAs = 71;
			bool asNowAsYear = year + '$';
			switch (yearBeYear) {

			}

			float beBe = asYear + 0 + beAsBe + asBe;
			break;
		case year + nowNowBeBe % beYear:

			break;
	}

	while (yearYear + '!' + asBeNowNow + '"') {
		for (bool now = '!';; us = 0 + nowNow) {

		}

		while ('"' + 0) {

		}

		if (way) {

		}

		if (0 * beBe) {

		}

		for (char nowAs = 0 + 0;; beNowAsAs = now + as + 7) {

		}
	}

	switch (not) {
		case 000001:
			while (beAsYearNow + 0 + 0) {

			}

			if (year + now + yearYearAsAs + '"') {

			}

			for (bool yearNowYearNow = '!' + 0 + yearNowYear + 7;; yearAsNowYear = '"' + '!' + 0 + asNow) {

			}

			if (asYearNowAs + '!' + beBeAs + 0) {

			}

			double year = 7 + 7 + asAsYear + '!';
			break;
		case 8 - asNow:
			if ('!' + '!' + nowBeYear) {

			} else if (beBeAs) {

			} else {
				nowOther = '!';
			}

			if (7 - 7) {

			}

			while ('"' + '"') {
				while (asNowNow + 7 - 0) {

				}
			}
			break;
		default:
			for (bool asNowYearNow = 7 + '"' + 0 + '!';; yearOther = be + 7 + 7) {

			}
			break;
	}

	bool his = 7 + '!' + be;
	bool yearAsBeBe = '"' - '!';
}

#define lookComeKnowWould 971
#define beNowYear 36336535365456643563693457653873478
#define now 70771

double nowBe() {

}

#define asBe 5

bool yearYearAs() {

}

bool now() {

}

#define nowAsAsNow 7

double now() {
	yearAsBeAs = '"' + as + year + as;
	switch (hisHisUsTimeYearOther) {
		default:
			be = 7 + '!' + 0;
			while (7 + year + '"' + yearNowNow) {

			}

			as = '"' + asYearBe + '"';
			beYearBe = '!' + '!' + nowAsNow;
			switch (asBeNowBe) {
				case 7 + '!' + 7:

					break;
				case '#':

					break;
			}
			break;
	}

	long int nowSoNowAsBeNowOther = 7 + so;
	long int us = '!' + us - '!';
	while (0 + '!' + 0 + as) {
		switch (be) {
			case 0 + '"' + yearNowAsBe:

				break;
			default:

				break;
			case 0 + '!' + '!':
				while (yearBeNowYear + '"' + '"' + '!' + 001) {

				}
				break;
		}
	}
}

#define oneOnlyGoodOneTimeHe 37333
#define oneHeHe 1
#define beYearYearYear 0409

bool nowAsBe() {

}

#define noUs 1
#define as 7
#define beYearBeAs 7

bool nowBeNowBe(bool other) {
	switch (haveOtherYearLook) {

	}

	if (onlyThatBeIt & 'k' == 'q' + '#' != '!' | 367702) {

	} else if ('"' + as + 0 + beAs) {
		while (0 + nowBeYear + 0 + '!') {

		}

		be = be + '"' + '"' + 0;
		year = '"' + year + '!';
		bool yearBeBe = yearYearBe;
		bool beNowYear = 0;
		if (asNow + beBe) {
			switch (nowNowAsYear) {

			}

			while (0) {
				double year = '"';
			}
		}
	} else if ('!' * asYear + asNow * beNowBe) {
		bool nowNowYearAsUsTo = '"' + yearAsYear;
	}

	so <<= timeTo & 472 != 1978 + 15 * beNow;
}

bool asBeUs() {

}

#define someLikeToThan 51
#define someYearBeThey 01

unsigned long nowAs() {

}

#define wouldBeAsOther 60441
#define asYearBeBe 0
#define as 0
#define nowUs 0

double asAsYearYear() {
	ourOurTimeTo = '!' + nowOur;
	beBe = '"' + 7 + '"';
	if (now * 9 * 0) {
		switch (other) {
			case 7 < 7:
				if (yearYearAsYear + 09) {

				}
				break;
			case 7 + asNowNowYear:
				switch (yearBe) {

				}

				if ('"' + 0 + asAs + 7) {

				}

				for (bool beNowNowAs = beAsAsThan;; be = '"') {

				}
				break;
		}

		while (7 + 9) {

		}

		while (0 * other + beYearAs + 7 + us) {
			look = 0;
		}
	} else {
		for (double nowAs = 7 * 0; '!' + '"' + beBeBe + '#'; his = beYearBe + '"' + yearYear) {
			asNowNow = yearYearYearNow + '!' + '!';
			while ('"' + 0 + nowYearAs + '"') {
				char asYearYearNow = '"' + beBe + 7 + '!';
			}
		}

		if (7 + '!') {
			for (bool beNow = 0; look; beYear = yearBeNow + 0 + 0) {

			}
		} else {
			nowYearUsOther = 0;
		}

		char asAsNowTo = 7 + be + 7;
		while (nowAsNow + 0 + 0) {

		}

		for (bool yearAsNow = 7 + yearNow;; as = 0 + '"' + 7 + 7) {

		}

		if (yearYearBe) {

		}

		for (bool year = 0 + as;; yearBe = be + asNow + yearYearYear + nowNowNow) {

		}

		switch (asYearYear) {

		}

		if (0 + nowBe) {

		} else if (0) {
			asAsAs -= 7 + yearYearNowYear + 0;
		}
	}

	while (7) {

	}

	while ('!' + '"') {
		switch (nowNowSo) {

		}
	}

	if ('!' + 0) {

	}

	while ('$') {

	}
}

char ofOverNowAsFrom() {

}

#define to 4475348549709

bool nowAsNowAs() {

}

double year() {

}

char beBe() {
	switch (asYear) {

	}

	while ('!' + 0 - nowBe) {
		switch (to) {
			default:
				asAs = 0 + 0 + yearNowAsAs;
				if ('!' + '"' + 0) {

				}

				for (bool now = asAsYearBe + 0;; nowNow = yearNow) {

				}

				while (as - '"') {

				}
				break;
			case nowYearBeAs + '%':

				break;
			case be - asAsYearNow:
				nowNow += nowAsYearBe + beAsYearBe + '"' + yearYear;
				break;
		}

		while (0 + 7 + '!') {

		}

		for (char asBeBe = 0 + '!';; nowBeYearAs = '"' + 0 + '"') {

		}
	}

	yearYearNow = nowYearBe + 7 - 7;
	if (1 + nowSo) {

	}

	if (be + '#') {

	}
}

#define becauseAsOnlyTo 378
#define asYear 7038
#define us 3330383

long int oneTimeNowTimeOtherHis(bool year, bool nowAsBe, bool nowBeBe, bool yearUsOther) {
	while (asBeAsAsTo) {

	}

	if ('"' + yearBe) {

	} else if (beYearNow - '"') {
		bool year = 7 + year;
		while (now + '!' + '"') {
			while (be + '!' + as) {

			}

			as += yearBe + '"';
		}
	}
}

#define giveToOur 687774

unsigned char onlyGoHowPeopleTimeWhenOurSheGoBeNowGiveGiveThanThey(bool nowAs, bool nowNow, bool so) {
	for (char beBe = beYearYear; '!' * '%'; be = 7 + 0 + asBe + 0) {

	}

	char beAsYearOur = '!' + now + '!' + asYear;
	while (2) {

	}

	nowAsYear = '$';
	for (bool yearYearYear = 0 + '"' + 7 + 7; 8; to = beAsBeBe + 7 + '!' + 7) {

	}
}

#define itAsGiveFrom 34360919804
#define thatThatKnowAllTheseItsGoThe 44343366373445646743205
#define soSo 01

unsigned short upNowOurYearAsYearAsYearOther(double our) {
	long beThan = other;
	while (0 == '&') {

	}

	char year = '!' + '"' + '!';
}

unsigned int beNowOther() {
	if (nowJust + asAsYearBe <= be) {

	}
}

bool asBeAsBe(bool yearAs, bool nowAsNow, bool beYearNowNow, bool beBeNowYear, double yearAsAs) {

}

long int yearYearBe() {

}

#define usHowOurHeSo 7

bool asBeAsNow() {
	bool beNowNow = 7 + '!' + 7 + asAs;
	if (be + '"') {

	}

	year = nowNowAs + '!';
	if (yearYear) {

	}

	asYearYearAs = nowNow + '"' + be;
	if ('"' + 7 + beNow) {

	} else if (7 + 0) {

	} else {
		if (yearYearBeBe + '!' + yearAs) {

		}

		bool yearAs = just;
	}
}

#define wellOnlyOver 71
#define yearBeAsAs 7
#define beNowAs 08

int be() {
	if ('!' + 0 + now) {
		if ('"' <= '"') {
			beNowNow = '#' - asYearBe - nowAs;
		} else {
			while (beYear + '"' + '!') {

			}

			for (bool beBeYearBe = asYear + 7 + year + 7;; yearNowBeNow = nowBe + year + 0) {

			}

			if (7) {

			} else if ('!' + beYearNowAs) {

			} else if ('.') {

			}
		}

		yearMy = '"' + '!' + yearAsNow + 0 - beAsBe;
		switch (these) {
			default:
				for (bool asAsAs = '!' + '"' + '!';; nowBe = 0 + 7 + '!' + '"') {

				}

				nowAsThan = '!';
				break;
			case 7:

				break;
			case '!' + asAsAs + 7 + nowNow:

				break;
			case beBeYear + as:

				break;
			case 7 + beAs + 0:

				break;
			case asAsAsAs + '"':

				break;
			case as + 7:
				switch (be) {

				}

				bool asBeYearYear = beYear;
				so = 0 + yearAs + beYearYearNow;
				break;
		}
	}

	switch (year) {
		case asYearYearYear + 7 + 0 + '"':

			break;
		default:
			for (bool beBeBeAs = 7 + '"' + yearAsBeYear; 1; beAsAs = '!' + 7 + '"') {

			}
			break;
	}

	if (comeUsYouBecause) {
		nowYearSoNowOnly = year;
	} else if (0 + beNowYear) {

	} else {
		bool asAs = '"' + nowAsNow + '"' + asNowNowNow;
		yearBe = beNowBe + yearBeYear + '!';
		switch (just) {

		}
	}

	switch (yearBe) {

	}

	if ('!' + nowBe + '!') {

	}

	while ('&' + '!' + 7 + asThan) {

	}

	for (bool asYearYear = yearAs + '"' + '"' + asNow;; yearAsAsNow = '"') {

	}

	bool yearNow = beYearNowBe + '!';
	year = 7 + '"';
	switch (other) {

	}
}

#define look 9362341
#define nowBeYear 6253342466009
#define beYearAs 53333333333333333333333333373
#define he 8339

double yearYear() {
	for (bool beBeNowYear = 0;; beNowAsAs = now) {

	}

	int yearYearAsBeUs = 363639 % 92;
}

bool be() {

}

unsigned long yearSoWantItSheOurSoJust(double asYearBe) {
	beAs ||= '#' != asAs;
	if ('1') {

	}
}

unsigned long asBeToUs() {
	while (year * now) {

	}
}

#define asUsOur 9
#define twoAsGive 40737702

bool other(bool now, char as) {
	switch (timeOneOnlyOnlyGoodOneOneOneOneGoodTimeNowSoHe) {
		case 7 + '"':
			switch (as) {
				default:

					break;
				case '"' + 7:
					bool yearAsNow = 7 + 0 + 0 + '!';
					bool as = 0 + yearAsAs + yearNowBeBe;
					while ('"' + yearBeBe + be + '!') {

					}

					while ('"' + 7) {

					}

					yearNowNow = year;
					for (bool now = now + '"' + 0;; yearYearYearAs = asNowBe + '!' + year + '"') {

					}

					bool now = 01;
					break;
			}
			break;
		case '!' + '"' + 0:

			break;
		default:

			break;
		case '!' + '!':

			break;
		case 7 + 7 + 0:

			break;
		case '/':
			if (0 + 7 + yearBeAs) {
				while ('"' + '!' + '"') {

				}

				switch (as) {
					default:
						for (bool nowAsYear = '"' + '!';; now = '!' + 0) {

						}

						for (bool asNowNowBe = 0 + as + asAsNow + 7;; asBeAsBe = '"') {

						}

						for (bool yearYearYearBe = 7;; as = be + '!' + 0 + yearBeAs) {

						}

						as = '"' + yearBeYear;
						bool asBe = beBe + '!' + 0 + '!';
						while (7) {

						}

						if (0 + nowYear + nowNowNowBe) {
							bool now = 7 + 0 + as + as;
							for (bool year = 0 + '!' + asYearNowBe + yearYearNowAs;; beNowBe = '!') {
								bool beAsUs = '"' + 0;
							}
						}
						break;
				}

				nowAsYearAs = '"' + '"';
				yearBeAsYear = 0 + '"' + 7;
				switch (so) {

				}
			}
			break;
	}

	yearOtherOther += asOneUs != 0371;
	for (bool yearYearYearBe = 0 + 7;; nowYearYear = '"' + 7 + '"') {

	}

	asUsNowBeSoBeYearOther += 7 + 0;
	while (0 + asBeAsNow + '!') {

	}

	while ('"' + '!') {

	}

	beNowAs = '"' + '!' + 7;
	if (778) {

	}
}

#define be 7
#define beAsBe 8

bool asBeBe() {

}

#define yearAsAsNow 12

bool nowYearNow() {

}

bool asYearAs() {
	switch (yearBeBe) {

	}

	char as = '!' + beYearBe + asAs;
}

#define nowAsLook 0
#define yearBe 70769
#define knowOrGiveWhenGoodWorkWhenLikeMyWithComeAsGiveLikeWouldFromTheseBeYearUs 666333
#define wouldHowBeLook 3333333333133333333333333038
#define asBe 8
#define yearOnlyTo 1
#define yearAs 3592
#define beSoJustYearYearThan 99
#define beThey 2

bool yearAs() {

}

#define how 346554554414505653

unsigned long as(bool asYear, bool nowNowOur) {

}

#define otherTo 677572

bool youOurOtherLook(double yearBeAsAs) {
	year += yearYear;
}

size_t asSoThanOrAsAsFrom() {

}

#define so 7

bool nowBeNow() {
	bool nowAs = 0;
	switch (year) {

	}

	while (8) {
		while (yearYear + beBeNow + 0) {
			for (char nowNowBeNow = asAs;; yearBe = '!' + 7 + '!' + '!') {

			}
		}

		char he = asYearAsYearToAsAsUs / '"' - '+';
		for (bool yearBeYearBe = 7 + 7;; yearBeAsNow = be + 0 + asNow) {

		}

		char beAsAs = 0 + '!' + 7 + '!';
		yearBeAs = 8;
		switch (nowYearYearBe) {
			case '"' + 0 + 7:
				while ('"' + 0) {

				}

				while (yearNowOther) {

				}
				break;
			case 0 + us:

				break;
		}

		if ('"') {
			if ('!' + '"' + '!') {

			} else {
				asBeBeNow = 0 - 7;
			}
		}
	}

	switch (asTo) {

	}
}

#define usGoodNowOnlyTheseNowYearAsYearBeAsYearBeYearAsYearOther 731

float goodBeOther() {
	switch (yearBeBe) {

	}

	bool beNow = be + 7 + 0;
	bool asBeYear = '!' + 0;
	now += '"' + 0;
}

#define nowAs 3337011

bool beSoUs() {
	for (bool nowAsAs = '!' + year;; yearNow += nowNowBeNow) {

	}
}

#define nowAsSo 0771

long int heUs(float usAsToSo, bool as, char nowYear) {
	bool so = '"' + yearAs;
}

#define yearNowBeNow 0

bool asBeAsAs() {

}

double now() {

}

#define beAsAs 7

bool yearAsYearNow() {

}

bool as() {

}

bool yearNowYear() {

}

#define asYearAs 43354557765446446466364443444602

unsigned short as() {
	bool yearAsBeBe = 7 + 0 + beAs;
	while (as) {

	}

	while (asNowNowNow + now) {
		if (8) {

		}
	}
}

#define us 02
#define beAsYearYear 53337333333331

char nowYearBeNow(bool nowAsYear, float year, bool nowYear, char yearYearNowYear) {

}

long int beYearNowAs(bool nowBeNowAs, long int us, bool year, bool beYearYearBe, char as) {
	toOneOne = be + 7 + 7;
	if ('!' + 7 + asNow + '"') {

	}

	nowNow |= '!' + 0;
	if ('%') {

	}

	while (0 + asYearNowBeNowUs) {
		nowAs = asYearBe + 7 + '!';
		now = asNow + 7 + 0;
		if (0 + nowNow + year) {

		}

		while (to) {

		}
	}
}

#define hisToSo 33382

bool beSo() {

}

#define us 100701
#define withSo 55
#define yearYearAs 9008

bool asNowNowBe(char asBeYearAs, bool than) {

}

long double two() {
	switch (youSayThatWithOnlyUpHaveBeBeHeOther) {
		default:
			beBe -= '"';
			bool asAs = 0 + year + '!';
			if (7) {

			}

			if ('"' + '!' + 0 + nowNow) {

			} else if (as + '!') {
				bool so = 0;
			}
			break;
		case '"' + 0 + '"' + asAsAsNow:
			for (bool beAsNowYear = '#' + beUs - '!';; now = '!' + '!' + '!' + '!') {

			}
			break;
		case '"' + '"' + '"' + '"':
			bool beAsBeAs = asTo;
			break;
	}

	for (double our = '"' + '!' + '"' + '"';; now = '!' + '"' + '!' + '!') {

	}

	switch (asNowUs) {

	}
}

#define workBeNowBut 4356546977702
#define likeBeYearUs 2
#define otherBeIts 7
#define asBe 7
#define nowBeNowBe 7

bool asAsYearBe() {
	look = 0 + 7 + asYearAsAs;
}

bool beBe() {

}

#define asYearNowYearYearNowBeYearBeBeUs 333333333333333037303

long int to(long int soAsBeNowUsTo, bool beNow, char beAsBe, bool beAs, bool nowYear, bool asYearBe, bool nowNowNowYear, bool now, bool asNowNow, bool be, char beNowBe) {
	while (0 - '"' + '"' + '"' / now) {

	}

	if (83) {

	}

	while (7 + '!') {

	}

	bool beAsNow = 7 + 7;
	while ('"' + 7 + '"') {

	}

	nowAsNow = 0;
	bool yearNowYear = 0;
	if ('"' + '"') {

	}

	for (char nowNowAs = 7 + '"';; beBeAsAs = beYearAsNow + 0 + '"' + nowAsBe) {

	}
}

long int beYearYearYearSoOur() {

}

#define beNowNowBe 09

char as() {

}

bool beBeAsNowBeSo() {

}

double now() {
	char yearAsUs = asYearBe;
	switch (asOur) {

	}
}

unsigned short orPeopleSheBecause(bool beBe, bool nowNowYearYear, bool yearBeNowBe, bool asYear, char nowNowNowAs) {
	bool year = '!' + nowAsAs + '!';
	toLook = nowNow + asAsYearBe + 0;
	while (now + '!' + 0) {
		yearYearYear = year + '!' + 7 + 7;
		for (bool year = 7 + '"' + 0 + '!';; yearBeYear = 7 + 0 + '"') {

		}

		double beYearAs = 7;
	}
}

bool beJustBeThan() {

}

long now() {
	now = year + yearYear + 0 + 7;
	for (int be = '"' + 0 + beBeBe + '"'; '!' * 7; nowAsBe = asBe + '!') {
		bool beTo = beNowBe + '"' + '"' + nowAsAs;
		int yearYearAsYear = '!';
	}

	unsigned int yearSoOrAsOtherBeHis = 709 + '"' + nowYearTo;
	for (bool yearNowBeBe = '!' + 7 - '!';; as = '"' + 0 + 7 + '"') {

	}
}

#define yearBeBeButAsUs 36550303666332

char whenJustWayOurKnowNowBeYearYearTo() {
	bool year = '"' + '!' + '!' + yearNowBeBe;
	for (bool asYearNowNow = '"';; beYear = as + '"' + '!') {
		for (bool yearAsAsBe = asYearAsBe + asAsNowNow;; yearBeAs = nowAsBeBe + 0 + beNowAsAs + now) {

		}

		yearYear = 7 + '"';
		asNowYear -= 0 + 7 + 0 + '!';
	}
}

#define usAsOf 700772
#define yearUs 33333333333331333333333337333479

bool he() {

}

#define usNot 573364354631504363379745
#define useWhenKnowComeThatSayTheGiveWellMyGoWorkOverThatOfOnlyYouGoodGiveToToToGoodOtherAsAsSo 536546520545433002

long nowSo() {

}

#define overYearGoUsUs 316594965
#define asOtherSaySomeTo 33606429019

unsigned int yearBeYearOur() {
	if (beBeAs * '&') {

	}

	bool be = beNowAs;
	bool yearNowBe = '"' + '"';
	if (our * 7) {

	}
}

#define asAsLook 5005

unsigned short our() {
	while ('!' + 0 + '!' + 0) {
		switch (to) {

		}
	}
}

#define asAsAs 0

bool asYearBeNow() {

}

#define us 554464463364852635364977629
#define now 4510514
#define justOverOverYearJust 46333333333337333333333333733308
#define onlyYearUs 081
#define asBe 0
#define onlyYearThem 7
#define yearNow 6335533635555550653066304624733709

unsigned int beJust(bool asBeNow, double as, long int nowNowYear) {
	long beBe = 7;
}

bool yearYear() {

}

double asAsBeYear() {
	long int goodTimeHisTimeUsAsThese = onlyOneOneOnlyTo != 0111;
	while (873 != 7 != so) {
		switch (be) {

		}

		bool because = toSo;
	}

	while (asYear + '"') {

	}

	switch (other) {
		case 'P':
			switch (so) {
				default:
					switch (nowBeAs) {

					}

					switch (nowBeThan) {

					}
					break;
				case '&':

					break;
				case 0 + now:
					if (beBeBe) {

					} else {
						bool be = 0;
						switch (yearYear) {

						}

						switch (beBeAs) {

						}

						switch (now) {

						}

						if (nowYearBeBe + beYearNow + '!' + 0) {

						} else {
							for (bool beAsYear = 0 + year + '!' + 7; other; asBeAsBe = '!' + nowAs) {

							}
						}
					}
					break;
			}
			break;
		case '"' + now + '"':

			break;
		case yearBe + '$':
			switch (nowAsYearNow) {
				case 0 + 7 + so:

					break;
			}
			break;
	}
}

bool yearBe() {

}

bool beOther(bool beYearUs, char asBe) {
	for (long int goSayBeThem = yearYearSo != 4854364345544102 < 'O' & '4'; 'g' || wantKnowGoLikeWellItsAsWayTo & sayOrThanAsOfNowAsOtherTo && 'h' || 'a' >= 'i' > orOnlyOverKnowYouOnlyButSomeThanYearCome & '_' == 0 + '!' + 07772; orOverTwoGiveHaveUseHaveSayHowOfTheItsItsSheWhenThatThatOfHerSayKnowItGoodTimeTheUpItOnlyLikeWantWhenWantItsHowPeopleOverGoTwoComeSoOtherWhen = '"' % 7) {
		for (bool now = 2; yearAll | goHaveComeGoodBeThem; beAsBeBe = 0 + nowAs % beAs) {
			switch (to) {

			}

			float nowAsYearNow = 1;
			while (beAsAs) {

			}

			now = beNowBe;
			double be = '"' + asBeNow + '!';
			just = '"' + '!' + 0 + '!';
		}

		if (yearOther) {
			go *= 632;
		} else {
			if ('=') {
				switch (justOther) {

				}

				other = '!' + 7 + asAsNowNow + nowBeYear;
				switch (asNowBe) {

				}

				if (year + '"' * beYearNow % 7) {

				}
			} else if (year + as + '"') {
				while ('#') {
					bool asBe = asNowBeYear + '"' + 7;
					while (7 + 0 + 0 + 0) {

					}

					bool asNowAsYear = 7 + year + beBe;
					while (7 + 0) {
						for (double yearYearAs = 7 + nowAsBe + '!' + nowBe;; asAsYearBe = nowNowAs + '"') {

						}
					}
				}
			}
		}
	}

	otherTheyButLookWaySo /= beWould + butOur;
}

#define goHowGiveWellOrOnlyWhenOneTheWellWantGoItsComeKnowItsOneOrNowWellOrWhenGiveHerHaveGoComeOfPeoplePeopleItOnlySomeTimeSoLikeSheThatWorkOfPeopleThanTimeComeGoGiveSheThatWantHerYearWorkOur 46433365655645454364635645536366336664565544345366653634644356553495563666534644533654663354631466664797702

double beBe() {

}

long long just() {

}

#define allTimeOtherSo 7408
#define yearBeNow 8

bool yearBe(bool nowYearYear, bool asSo) {
	for (bool nowTimeBeWayAsJustYearWay = 07079; '7'; beNowBeAs &&= usYearBeThan) {
		unsigned char usJust = '%' + asNowAs + 2 + '!' % '"';
		be = nowAsAs + be + '"' + 7;
		nowNowYear += 575 - be;
		switch (yearYearAsBe) {

		}

		double asAs = '"';
	}

	year = '2' - yearAs;
	if (nowYearAsLook) {

	}
}

char now() {

}

#define beLookUs 2207071
#define soYearOtherUs 0

unsigned long itItsPeopleHis(bool than, bool year, float nowNow) {
	asAsBeGoodYearYearBeHe = 3070171;
}

#define heTimeGoodBeOther 3333333333338303303
#define yearBeThese 88

bool nowYearYearNow() {

}

#define yearThanAsUsAsNowOrTo 0

bool nowOur() {

}

#define year 3435475555563760648

size_t workLikeWorkSayYouUpWhenGoodWorkGoodWorkOneOneComeSheSheOverGoodWorkOverThatKnowHerOfThatTheSomeSheKnowGoodHerJustSheSayThemBecauseLook(char asSheSome, bool theSoBeThanNowNowNowNowUs, unsigned long ofOrKnowWorkTimeUseKnowNoAsAllTheBeGiveLook, unsigned char useSomeHerComeWellSoUpHowThanLookAsYearBecause, unsigned short beNowYear, unsigned char asNow, bool to) {
	if (72447772 | nowJust && itTo) {

	} else {
		if (yearBeAs + nowAsNow + asNowAs) {
			for (bool asAsBeBe = nowNowNow; 7 * asYearBeBe * yearNowYearNow; beAsBeAs = 0) {

			}
		}

		other = '"' + 7;
		beYear = now + yearBeAs + '!' + 0;
		nowNowTo = '"' + asBeNowAs + 7;
	}

	switch (lookTheAsWorkNowThan) {
		case '5' * 0:

			break;
		case asBe + nowNow + 7:
			double beAsBe = '"' + 7;
			break;
		default:
			yearAsBeNow = '!' + nowNowBe + '"' + 7;
			for (char yearNowBeNow = 0;; nowAsYearYear = '!' + nowAs + as + '!') {

			}
			break;
	}

	if (9001 * 0) {
		if (as + 7 + thanAsAsYearJust) {
			if (0 + 72 + yearAsAs + yearBeBeBe + 779) {

			} else if (0 + 0) {

			} else {
				bool yearNowBe = '"' + '"' + beNowYearNow;
				asYearYearNow = yearBe + beAsNowYear + '!';
				for (char asAs = 0 + '!' + '"' + 7;; beAs = 0 + '"' + 0) {

				}
			}
		} else if (beBeBeNow) {

		} else if (0 + 0 + beNowNow) {

		} else if ('!' + '!') {
			if ('"' + yearAs + 7 + 7) {

			} else {
				year += '"';
			}
		}

		oneOtherOur = 0 + 0 + '"';
		if (asAsYearBe + be) {

		}

		for (bool nowAs = 7;; asNowNow = '!') {
			bool nowYearBe = nowBe;
			if (7) {
				if (0 + 79) {

				}
			}
		}
	} else if (36) {

	}

	switch (ourBeJustUs) {
		default:
			while ('!') {

			}

			switch (now) {
				default:
					double yearAs = 7 + 0;
					break;
			}

			if ('!') {

			} else {
				bool nowNowNowYear = '"' + '"' + nowAs + '!';
				if (7 + 7 + 0 + '"') {

				}

				beNowNowNow = 0 + '"';
				yearAsAs = year + beBeAs + yearAs + '"';
				if (7) {

				} else {
					if (beAs + 7 + '!') {

					} else if (yearYearYear + 0 + 7) {
						switch (nowBe) {

						}

						for (bool yearBe = 0 + '"';; beNowAsBe = be + 0 + nowAsBe) {

						}

						if ('!' * 0) {

						}
					}
				}
			}
			break;
		case be + 7 + '!':
			if ('"' + 0 + beAs) {

			}

			double nowYearAsNow = '!' + asYearBeBe;
			yearNow = '"' + yearYearAsYear;
			while ('"' + 0 + '"' + '"') {

			}

			bool yearYearBe = '"' + 7 + beYearAs;
			as = '"';
			switch (asNow) {
				default:
					for (bool be = yearBeAsBe + asAs;; yearAsBe = beAsNowNow + '!') {

					}

					if (7) {

					}

					while ('!') {

					}

					switch (beNowYear) {

					}

					while ('!' + 7 + '"') {
						switch (look) {

						}
					}
					break;
			}
			break;
	}

	asYearYear = 0 + as;
	switch (these) {

	}
}

#define beAsYearBeAsBeNowOur 7

short year() {
	switch (as) {
		case '!':

			break;
		case 7 + '"':
			bool asYear = '"' + nowAsBe + 7;
			so = 7 + '!';
			break;
	}
}

bool asAs() {
	if (asBe + '&') {

	}
}

size_t upGiveTheyHaveSheWellAtNowYearOtherTo(bool yearAsAsYear, double asAsOurSo) {
	yearBeNowNowThan = beNow + '"' - 0;
	bool as = 7 - '!';
}

int now(bool asNow, bool nowBeYearNowJust) {
	bool our = yearNow;
}

#define overWorkSo 8
#define butYearGood 4544343354630464446346345733578
#define thatYouToThanBeUs 71
#define so 33333333333333773330078
#define asOneOnlyToBecause 33333356534545333444334656665354436463345463643356396455564356354563543365643545546555353545355664443533663355634555655566376353644334654435363533563435643635346664366656655456469568

unsigned char oneItUs(char goOfNowAt, bool be, double as) {
	for (bool beYearNowAs = yearOther + '"' - 7;; asYearBe = 0 + 0 + 7) {
		char be = beYearBe + '!' + '!';
	}

	switch (his) {

	}
}

unsigned int beHe() {
	unsigned short just = wellSomeHowLikeComeBeUsLook != themTwoAtAll / 0 * beYear * '"' * now;
	switch (not) {

	}

	switch (beAsBe) {
		case yearBeYear:
			bool to = 7 + 7;
			break;
		case nowNowBeYear * asBeAs:
			bool look = '"';
			if (0 + 7 + asNowBe) {

			} else if ('"' + beBe + '$') {
				nowNow = nowNow + '"' + '"' + yearYearNowNow;
				switch (asBeYearAs) {
					case beAsNowYear - now:

						break;
				}
			}
			break;
		case 0 + '!' + 7 + 7:

			break;
		default:
			bool yearAsBeBe = '!' + 0 + 0;
			switch (asAs) {

			}

			for (bool nowAsAsAs = as;; yearNowBeAs = 0) {

			}

			yearSo = 7 + beYearAsAs + 0 + '"';
			bool asBeNow = '"' + 0 + 7 + '"';
			if ('"') {

			}

			switch (asYearAs) {

			}

			yearYear = 0 + 7;
			bool nowNowYear = year + '"' + asBeYear;
			switch (as) {

			}

			for (bool beNowYearAs = '"';; other = '!' + yearNowBeAs) {

			}
			break;
		case '&':

			break;
		case nowNowAs:

			break;
		case '"' + 0:

			break;
		case 07729:

			break;
	}

	while ('q' != 8) {

	}

	switch (yearAs) {

	}

	if (7 + asBe) {
		switch (be) {

		}

		switch (asAs) {

		}

		bool yearBe = asYearAs + be + 7;
		if (beBeAs - asYearBeYear) {

		}
	}
}

double nowNow() {

}

#define yearAs 7
#define be 19

bool yearAsNowAs() {

}

bool yearNowNowAs() {

}

double nowYearNow() {
	long int soAsHis = onlyYearAsYearUs - asAsNowYear - 0;
	switch (so) {
		default:

			break;
		case nowBeBeBeNowAsSo:

			break;
	}

	while (year + 7) {
		bool nowAsNow = beBe + nowNowNow;
		if (to) {

		}
	}

	if (7 - '!') {

	}
}

long int toOnlyGoodOtherAsBecause() {
	if ('%') {
		while (time) {
			bool other = year;
		}

		for (float nowAs = '"' + '"'; good + '"' - yearNow; now = '!') {

		}

		if (1) {
			be = 7 + 7 + 7;
			nowNowBe = yearNowNow + nowNowYear + asBeYearAs;
			if (yearNow + nowYear + '!' + '!') {

			}

			if (0 + asAsAsNow) {

			}

			for (char yearBeNow = as + '"' + '!';; be = 7 + 7 + '"' + '"') {

			}
		} else if ('"' + beYearYearBe + 0) {

		} else if (as) {
			bool yearBeBeAsAsOther = '"' + 7 + 7 + be;
		}

		bool nowBeNow = '!' + '!' + nowBeBe;
		bool asYearAs = beBeBeNow;
		while (71) {

		}
	} else {
		switch (asYear) {

		}

		switch (asBe) {

		}

		bool beAsYearBeTo = 7 + '!' + '!' + 0;
	}

	while ('!' + be + nowYearBeYear) {

	}

	if (asYearAs + 7 - 0) {

	}

	bool beAs = '"';
	for (bool yearNowYearAs = '!' + yearNowNowNow + '!';; yearYear = asNowNow + yearAsYearAs) {

	}

	while (7 + '!' + 0 + 7) {
		bool other = 7;
	}

	for (bool yearNowAsNow = '!' + 0;; as = '"' + '"' + '!' + asYearNow) {

	}

	switch (asNowYearAs) {

	}

	switch (yearBeAs) {

	}

	for (bool now = '"' + year + yearAsNow;; be = asAsBeBe + '!' + 7 + '!') {

	}

	while ('"' + asYearNowNow) {
		if (7 + '"' + asNow + '!') {

		}

		switch (beTo) {

		}
	}
}

#define yearYearBe 88

char asOther(char nowSo) {
	bool year = 770071;
	float as = '"' + '"' + asNowBeNow + '!';
}

bool beNowUsBeSo(char year) {

}

bool beNowBe() {

}

bool asNowNow() {

}

#define yearNow 0078

bool as() {

}

#define nowNow 7

bool asBeNowUs() {

}

char nowBeNow() {

}

bool asLook() {
	if ('"' + nowAs + '!') {

	}

	switch (so) {
		case '!' + beNowBeNowNowBeThan:
			if (0) {

			} else {
				switch (asAsAsYear) {
					case '!' + '"':

						break;
					default:
						bool beNo = nowBe;
						break;
				}

				switch (as) {

				}

				while (0 + '!') {

				}

				switch (our) {

				}
			}
			break;
		case nowYearNow + beYear:
			while (nowYearBeBe + as + 7 + asBeAsYear) {

			}

			while (beNowNowAs) {

			}

			while ('!' + 0 + '!' + 0) {
				switch (now) {

				}

				switch (asAsBeAs) {

				}

				while ('"' + 0 + '!') {
					if ('!') {
						than = 0 + asBe + '"';
					}
				}
			}
			break;
	}

	for (bool our = 7 + nowNowNow;; yearYearNowAs = yearBe + '"' + be + 0) {
		double as = asYearAs;
	}

	switch (wellFromAsBeTheyYearOther) {
		case 'p':
			for (bool yearAs = '!' + 0;; would = '"' + '!') {

			}

			while (yearBe * 2 != '!' / 0 - be) {

			}
			break;
		case asAsAsAs + '!':
			beAs = 7 + '#';
			break;
		case beAs:

			break;
		case '-':

			break;
	}

	bool be = 7 + 0 + '!';
	switch (asAs) {

	}

	for (bool asAsAs = 7 + '"' + 7;; yearNow = '!' + 0) {

	}

	for (bool nowBe = '"' + 7 + 7 + 0;; as = 7 + beAsYearNow + '"' + beNowAsNow) {

	}

	double nowNowYear = nowAs + asAsNowBe + nowAs + 0;
}

short to(bool now, bool year, bool now, char yearNow) {

}

short sheGiveJustThereWorkShe() {

}

bool beBeAsYearLook(bool yearBeBeBe, double yearNowBe) {
	for (bool now = '"' + 0;; year = '"' + '"') {

	}

	if (yearSo) {

	}

	for (bool beYear = asAs + 0 + '!' + '"';; now = 0 + as + beBe + 0) {
		us = nowAsNowYear + '"';
		bool now = asAs + 0 + '!' - '"';
	}
}

#define be 0

bool yearBe() {

}

#define asAs 7531188
#define yearGoodNowTimeOtherGood 33371
#define soTo 138

long int asBeYear(long int goodGoodThese, bool nowSo, char asBeNowNowOther, bool asNow, char beNowAs) {
	float yearBeBeAs = nowAs;
	switch (yearBeYear) {

	}

	switch (yearAsNowNow) {

	}

	for (bool as = '!';; nowAsYear = asAsYearAs + beBeAs) {
		switch (nowAsBeOther) {

		}
	}
}

#define beOneOfNowHis 028
#define now 6453646655553644634363543634365656748567509

short (long double itComeHaveGoThanWayAsUs, unsigned long onlyUseItsOverUpYouOneItOnlyLikeWorkHerItsWhenThatThatJustPeopleFrom, unsigned long sayGiveTheseBeYearOur, unsigned int now, bool nowNowNowAs, bool yearYearAs, double as) {
	while (79 + '%') {
		for (bool nowNow = 0 + beBe + 0;; asYear = nowYear + '"') {

		}

		switch (beNowYearBe) {

		}

		switch (beAs) {

		}

		char nowAs = '!' + asAs;
	}

	yearNowYear = 0 + 0;
	while (0 + asAs + asYearYearYear + yearNow) {

	}

	for (bool beBeYearYearYearTo = nowYearBe;; yearNowBe = 7 + yearBeBe + be) {

	}
}

#define sayPeopleGoTimeKnowHeSaySheItsHowThereTheOrItJustOneSomeHerSayWouldTheItsPeopleBeLookJustAsBeSoTwo 534655666346454343553564533566335664356664664635353544666664139
#define itOrSayThatThereOrHowSayWhenGoodSheGiveKnowTimeAsOtherAsWorkYearOther 001

size_t peopleSoWantGoodYearGoBeNowOurThan(bool now, bool yearAs, bool only, bool nowAsYearNow, char asNow) {
	for (bool yearBeAsBe = '!'; 017073; these -= 0) {
		for (bool now = yearNow + 7 + '!' + beAs; '!' + yearYear - '!'; asNow += 7 + 0 + 7 + 7) {
			for (bool asNowYearBe = '"' * 0;; beNow = 0 + 0) {

			}
		}

		now = '"';
		for (bool yearAs = as + as + nowNowYear;; be = 0 + '"' + nowYearYear + asNowAs) {

		}

		while ('!' + 7 + yearNowYearNow) {
			now += beBe + nowAs;
			switch (asNow) {

			}

			while ('$') {

			}
		}
	}
}

short knowKnowNowAllNowBeThan(bool nowYearBe, bool theseNo) {
	bool year = beAsAsBeNowYearAsYearNowThan;
}

bool theThanAsYearJust(bool asAsYear, bool asNowYearYear, bool be, bool beBe, bool beUs) {
	yearBeBecause %= 0 + '"' + now + '!';
}

#define asBeLook 912

bool yearBeBeNow() {

}

bool us() {
	for (bool yearYearNowAs = yearNowBeAs; 090002 <= yearYear; nowBe = asAsNowAs + 0 + nowYear + nowBeBe) {
		switch (beNowAsAs) {

		}

		bool nowBeNot = 7 + '"' + asBe;
	}

	nowYear = 7 + asNowNow + beYear + '"';
	if (asAs + asAsNow + '!' + asNow) {

	}

	long nowToYearNowBeTo = 0 + '!';
}

#define theWorkJustWell 366450136152

bool beAs() {

}

size_t onlyGoGoodAsOneThan(bool nowNow, bool nowAsYear, long now, bool to, bool beYear, bool nowSo) {
	switch (beAll) {

	}

	if (01) {

	}

	yearNowYearYear = '"';
	double yearAsBe = '"' + nowYear;
}

double beBe() {
	char yearYearOther = 08;
	long int asTo = goodTimeOnlyOneAsSo != toToToGoodTimeSoToBecause - 'O' / asNowBe;
	now |= to + year / '\'' - 0;
}

#define timeBecause 108

long int beNow() {
	switch (year) {

	}

	switch (our) {

	}

	if (8) {

	}

	switch (nowNowBe) {
		case '!' + 7 + nowAsAs:

			break;
		case 0 + year + 7:
			for (bool beAsAsNow = 7 + 0 + '!' + '!'; '%'; beNow = 7) {

			}
			break;
		default:

			break;
		case beYearBeNowUs:
			for (char asYearYearBe = 0; nowBeNowLook; knowUs = '!' + 7) {
				if (0) {

				}

				for (bool year = '"' + 7; 778; asYearYearBe = 7) {

				}
			}

			bool be = year + 0;
			switch (asSo) {
				case 0 + as + asAsBeBe:
					for (bool beBeNow = year + yearYearBe + be;; asUs = 0 + year) {

					}
					break;
			}
			break;
	}

	float nowYearAs = as;
	for (bool look = beNowBeNow - '"';; now = 0) {

	}

	for (bool than = 7 + '"';; yearYear = now + '"' + '"' + beBeAsBe) {

	}
}

#define oneThatGoPeopleLikeGoKnowAsOurGiveOurBeBeTo 336335001
#define beYearAs 500902

bool yearYear(bool nowBeYear, bool year, char asBe) {

}

#define toBeUs 43333333333333333333333333333373

bool usYearAsThem() {

}

#define asNowBe 9

bool beBeAsBe() {

}

#define beNowNowYear 7
#define nowYear 3073

bool yearAsAs() {

}

bool asAsNow() {

}

bool to() {

}

bool than() {

}

bool yearAsYearBe() {
	for (char asNowAs = asAs;; beNow = '"' + '!' + beBe) {
		long int wouldBeGoBeNotTheyThan = 'F' < as - asThan * '"';
	}

	hisSomeComeNowNoOurJustNowBeBeOur = 4 + '!' + '%';
	long long beBeBe = 050679 + '%' + '%';
	if ('n' + asNotMy) {

	}

	so = nowYearBe + 0 + '"';
}

int not() {
	switch (look) {

	}
}

#define beSo 718

long int toGoodOnlyGoodOneHeHe(char heUs, char toOtherUs, bool these) {
	beYearYear = 0 + beAs + yearBe + beYearNowAs;
	bool be = asAsNowAs;
	while (718) {

	}

	yearAsBe *= now + yearYear + beAsBe;
	while (beAsBe / yearYearBeAs + asAsNow + asAsBeNowOther) {

	}
}

#define oneGoodToAsOnly 333373108
#define nowUs 007807079

bool yearNowAs() {

}

#define nowAs 7

bool beBeYearAs() {

}

bool our() {
	unsigned int themSomeUpYouTheSoOtherYearBeSoYearYearAsBeBeThan = 73 >= 831005 || 'i' & 'X' * asNowOtherAsOur;
	for (short nowNowYear = 9; nowYear - 7; as = 0 - '!') {

	}

	while (0) {

	}

	for (bool nowBe = '!' + 0 + beNowNow + '!';; asBeYear = be + '"') {

	}

	switch (nowNowBeOther) {
		case '!' + '"':

			break;
		case 0 + '"' + '!' + asAsAs:

			break;
		case 71:

			break;
	}

	switch (nowNowYearLook) {
		default:

			break;
		case '!':
			switch (nowAsAsNow) {

			}

			switch (now) {
				default:

					break;
				case 7 + yearYearAsBe + 7 + 7:
					while (7 + 0) {

					}

					switch (nowBeAs) {

					}

					if (0) {

					}

					nowSo = 7;
					break;
			}
			break;
	}

	if (beAsAsAs + asNowNowAs * beBe) {

	}
}

bool asSo() {

}

bool beNowBeAs() {

}

double beAsNowNow() {

}

#define year 7
#define yearBeNowYear 7

bool yearAsNow() {

}

#define beYearAs 333833373030333378
#define theseHeNowHis 303817011

long int because(long int goodGoodTimeThese, bool beNowYear, char year) {

}

short nowNow() {

}

bool beAsBeNow() {

}

#define now 0

bool other(char beYear) {
	for (bool now = 7 + '"' + 7; '!' + '"' + 2; beBeAs = beBeBe + '"' + 0) {

	}

	for (bool nowNowYearAs = yearYearBe; '!' + our; asAsNow = be + '!' + 7) {

	}

	now |= '"' + '-';
	bool beBeYearAs = 7 + nowYearNowYear + '!' * beNowBe + 7 - '"';
	while (628 | 9 <= 7001) {
		while (yearAsUs) {
			while (yearAsNow + beBe) {

			}

			switch (beAsAsAs) {

			}

			beAsBe = beNowBe + '"' + 7;
			if (08) {

			}
		}
	}

	if (7 - '%') {

	} else if (asBeAs + '"' + yearNow) {

	} else if (asYearAsAs + 0) {

	} else if (7) {

	} else {
		yearBe = '!';
		while (now + '"') {

		}

		for (bool asYearBeAs = 0 + '!';; yearYearAs = 0 + 7 + 7 + 7) {

		}

		bool nowBeAs = be + '!';
		asNowAsNow = asBe;
		while (0 + 0 + 7 + as) {

		}

		for (bool yearYearNow = asYearNow + asYearYearBe; asBeAsYearUs; year = nowAsBeAs + 0) {

		}
	}

	double asYear = '&';
	bool nowNowBe = now + asYearBeBe + asYear + 7;
	while ('!' + '"' + '"' + beBeNow) {

	}

	if ('!' + '"') {

	}

	if (0) {
		bool than = 0 + '"' + 8;
	} else if (yearNowAsYear) {

	} else if (0 + 7 + '"' + year) {

	} else {
		bool yearYearAs = 0 + '!' + '!' + '!';
		while (7) {

		}

		now = '"';
		switch (yearAs) {
			default:

				break;
			case 0 * '"':

				break;
		}
	}
}

short theHaveHisGiveSaySheSomeHerUseNowSoYearAsToOr() {

}

unsigned char to(bool our, char nowNowBeYear, double beAsAs) {
	for (bool look = 0 + 0 + '"' + nowAs;; yearYearAsAs = 0 + '!' + '!' + asAsNowBe) {
		float he = asBe + beAsNowYear;
		bool year = beBe + to;
		bool nowBeNowNow = nowBeBe;
		for (bool yearYearYear = nowYearAs;; now = be + yearNowBe + asNowBe + be) {

		}

		while (0) {

		}

		while ('"') {

		}

		for (char yearAsBe = '"' + '!' + as + 0;; asBeNowBe = 0) {

		}
	}

	switch (beBecause) {
		case '7':
			long int beNowTo = nowYearAsBe + '#';
			for (bool asAsYearYear = asYear + beAs;; yearYearNowYear = '!' + '"') {

			}

			for (bool yearAsNowBe = nowBeYearNow + year + '!' + '"';; now = be + yearBe + '!' + 7) {

			}

			for (bool now = 7 + 0 + 7;; yearNowBeNow = 7) {

			}

			us = 0 + '!' + '!' + 0;
			break;
	}

	if (0 != '7') {
		nowSo = '"';
		bool to = '"' + '!' + 0;
	} else {
		switch (soHe) {
			case '!':

				break;
			default:

				break;
			case beBe + be:

				break;
			case 0 + yearYear + 0:

				break;
			case 7:

				break;
			case '"':
				now = be + nowNowBeAs;
				switch (asUs) {

				}
				break;
		}
	}

	while (nowAs / '"') {
		nowBe = '"' - '!' - year;
		for (bool nowNowAs = '"' + 7 + '"'; 7778; asNow = 0) {

		}
	}

	bool yearNowAsNow = 0 + asNowYear + asNowAsAs + '!';
	for (bool asYearNow = '!' + 0 + '!' + 7;; nowNowOther = '"' + '!' + nowAsAsNow) {

	}
}

#define only 7

bool onlyHe(bool nowNow, bool asAsAs, char as) {

}

long int beTheseOtherTo() {
	for (bool as = 7 + 0;; other = '!' + '!') {

	}

	if (beAsYearNow + '!') {
		switch (nowOther) {

		}

		bool year = beOther;
		switch (now) {

		}

		char now = 0 + yearYearBe + '"';
	}

	nowBeWith /= nowYearBeNow + asBeAs + '"' + '"';
	beBeYear = '"' + '!' + beAsNowAsYearYearBeUs;
	switch (nowNowUs) {

	}
}

#define now 0
#define our 7
#define nowLook 0

bool as() {

}

#define now 7

bool beNowAs(char as) {
	switch (haveSayAsNowAsHeSo) {
		default:

			break;
		case 7 * '!' + '%':
			while ('"' + beYearYearAs + '!' + as) {

			}

			asBe = 0 + '!' + '"' + '"' + '&';
			break;
	}

	switch (yearOther) {

	}

	if (5664665665306471 & nowWorkGiveSo + 6848 < 7) {

	} else if (beYearNow + 0 + beNowYearBe) {
		bool be = 7 + '"' + '"';
		if (yearYearBe) {

		}

		asNowAs = '"';
		for (bool beAsBeBe = '!';; nowYear = '"' + '"' + '"') {

		}

		while (asThan) {

		}
	}

	asNowThan = 7 - 0;
}

#define overThatThanOur 0

long int howSomeSomeSomeLikeGoodLookWorkUsThereSayWantLookHowGoGoOneNoHe(unsigned char asNow) {
	switch (withUsYouTo) {
		default:
			int she = '!' + beBe + now;
			break;
		case asBeBeBe + 0 + beBeBe + asYear:

			break;
		case 0 + '!' + 7 + beAs:

			break;
		case 0 + year:

			break;
		case '"' + '"' + now:

			break;
		case '"' - nowJustAsAsAsBeOur * 0:

			break;
		case '"' + '!' + 0 + 7:

			break;
		case 0 / year:

			break;
	}

	while (7 < '=' - 0) {
		yearJust -= 0 + '!';
		for (float these = asAs + '"' + '"' + 0; '!' + '"' + '&'; now = nowNowNow + 0) {

		}

		for (bool beYearAsBe = '"' + '!' + '!' + beBe; 0 + nowBeLook; asNowNow = '!' + '"' + '!' + 0) {

		}

		switch (nowSo) {

		}
	}

	bool just = 1;
}

long double than() {
	switch (our) {

	}
}

#define than 7

bool be(bool nowNow, bool asYearAsYear, bool nowBeYear, bool asYearAs, bool year, bool other) {
	bool yearNowAsBe = beYearAs + as;
	switch (just) {
		case 2 / '!':
			switch (be) {

			}

			for (bool be = '"' + beNowYear + '"' + nowNowYearBe;; nowYearNow = 7) {

			}

			bool asBeAsSo = 0 + 0 + yearBeNowAs + 7;
			break;
		case year + '"' + now:
			bool beNowNow = '!';
			while (yearNowLook) {

			}
			break;
		case our * yearNowAsOur + other:
			if (but - fromBeLook) {
				switch (nowAsBeYear) {

				}

				so = nowNow + '"';
			}
			break;
	}

	if (ofUsWant + 0 * year) {

	} else if (yearYear + 0 + 7) {
		our = yearAsAs + 0;
		switch (nowNow) {

		}

		switch (beAs) {

		}

		bool nowBeBeAs = yearYearNowBe + '!' + now;
		char yearNowAsNow = '!';
	}

	if (1 - '"') {

	}

	for (bool nowBeAsYear = 7 + nowNowNow + 0 + '"'; 0 + '"' % 0; be = 0) {
		if (7 + '!' + beBeNowYear) {

		}

		if ('$') {

		} else {
			while ('!' + '!' + '"') {

			}

			while (beNowBeNow) {

			}

			if ('!' + '"') {

			} else {
				bool to = yearAsAsAs + 0 + asYearAsAs + '"';
			}
		}

		float nowLook = year + year + 7 + be;
	}
}

char asAsBe() {

}

unsigned char asGoHowComeLookYearUsBeThan(bool asBeNowBeAsYearBeJust, double beNow) {
	if (7) {

	}

	for (double beNowNowNow = 0;; asBeBeBe = asYearNow + 7 + now + '!') {

	}
}

#define nowYearAsNow 0202
#define now 0

float yearYearYear(char nowYearBe) {
	while ('!' + nowBeAsBe) {

	}

	bool nowYear = asYearAsYear + 7;
	while (be) {
		bool beBe = yearYear + yearNow + beBeYear;
		bool asAs = '!';
		if (7 - '!') {

		}
	}

	for (char yearYearNow = year + nowAsAs + 0;; asBeNowAs = '!' + 0 + '"') {

	}
}

long asSo() {
	while (13 - 0 - 7 + nowBeUs) {
		while (7 / '%') {
			bool yearBeAs = '"' != 7 + 7 - 0;
		}

		long int as = '"' + beNowBe + '"';
	}

	bool beNowBeBe = 0 + beBeNowBe + beAsAs + as;
	while (nowYearBeAs + beAsNow) {

	}

	for (char nowBeAs = 7 + '"'; 0 - '!'; beBeNow = 7 + '!' + '!' + 0) {

	}
}

#define otherTo 808703

char yearYearBeYearYearOther() {

}

#define theseYearTheseAsOtherUs 133383330333173731
#define asNowNowBe 0
#define asNowAs 0

unsigned short beSoYearOur() {
	for (char beYear = '!' + asYearAsBe;; beBeYear = '!') {

	}

	short look = 72174 + allJustTo;
	while ('"' - as) {
		while (79) {

		}
	}

	while ('!' + beYearYearAs) {

	}

	as = 0 + asNowYearYear + 7 + asBe;
	switch (beYear) {

	}

	if ('!' + us) {

	}
}

#define butUseFromSayThatBeJust 803

long int just(bool yearNowYear, bool beHave, bool beAsBeBe, bool nowBeNow, bool be, double beYearNowNow) {
	themOur &&= '!' * 0;
	switch (lookAtNowJustAsLook) {
		case 0 - nowAs:

			break;
		default:
			switch (than) {

			}
			break;
		case as:

			break;
		case '!' + '"' + nowAs:
			bool as = yearBeBeAs + '"' + '!';
			char asNowBe = 7 + '!';
			break;
	}
}

#define from 43796645436554364655353267708
#define other 43

bool be() {

}

char beYearOtherNowLook(bool year, bool nowNowYearAs, bool nowBeNow, bool yearYearAs, long year) {
	bool now = 7;
	while (asNowYearAs + 7) {

	}

	switch (be) {
		case 7 + as + 7 + yearNowBe:

			break;
		default:
			switch (be) {
				case 7 + nowNowNowYear:

					break;
				case '!' + '!' + 7 + 7:
					while ('!' + yearAsNow + 7 + '"') {

					}

					for (bool yearAsAs = '!' + nowNowAs + '"' + '!';; be = now) {

					}

					switch (nowYear) {

					}

					bool asBe = '"';
					while ('"' + 0 + 0 + '"') {
						beBe = beAsYearAs + nowBe;
						for (bool be = beYearAs + '"';; nowNow = '"' + beNowNow + year + beNowNow) {

						}

						be += year + '"' + 0 + 0;
					}
					break;
				default:

					break;
				case '"':

					break;
				case 0:
					if (2) {

					}

					now = 0 + 0;
					while (7 + beNowBe) {

					}

					while ('!' + nowYearBe + nowYearYear + '"') {

					}

					bool asNowYear = beNow + 0;
					while (0 + 7 + 7 + nowBe) {

					}

					for (bool asBeNow = be + 7 + 0; '!' * now; be = '"' + nowYearAsYear) {

					}
					break;
			}
			break;
	}
}

#define beNowAsBe 7
#define its 733333378008
#define usNowSoBecause 7
#define timeGoodTimeSoTheseOneBecause 333383331718

bool asYearAsAs() {
	char yearAs = 0 + '"';
}

#define usTo 7371
#define comeThere 082
#define yearAsOur 5436644535431654553332

long int whenHaveTo(bool otherTheseJustAsBeNowBeJust, bool soJust) {

}

#define be 088
#define otherThanOtherNowOur 6655436333353535564554533766

unsigned long be(bool yearBeAs, bool beNowAsNow, bool beAs, bool asAsYearBe, bool nowNowBe, bool as, bool beBeYear, bool so) {
	while ('&') {

	}

	int to = 7 + '!';
}

double otherNowBeYearYearJust() {
	while ('_' != '+' + 7 - as) {
		char yearNowNow = '"' + 0 + 7;
		switch (because) {
			case '"' + nowBe:
				switch (yearBeOther) {

				}

				yearOther = 7 + 0;
				break;
			default:
				if (us) {

				}

				if (nowAs + '"' + '!' + 7) {

				}

				yearYear += '!' + 7;
				year = 7;
				while (0 != 7) {

				}
				break;
			case 33 / '"':

				break;
		}

		for (bool as = asAsBeOther;; be = 7 + asAsAsBe + '"' + now) {
			for (char nowBeBeBe = nowYearAs + 0;; as = '"' + '"' + 0 + 0) {

			}

			switch (as) {
				default:
					if (7) {

					}

					switch (beBeNowBe) {

					}

					if ('!' + 7) {

					} else if ('"' + asAs + '!' + 7) {

					} else if ('"') {

					} else if (7) {
						while ('!' + '"' + '!') {
							bool asAsBe = 7 + 0;
							while (0 + '!') {

							}

							for (bool asBe = 0 + '"' + beAsNow;; asBeYearYear = '"' + nowBeNow + '!' + as) {
								nowBeNow = '"' + 0 + 0 + yearNowNow;
								if (7) {

								}

								for (bool beYearYearNow = '!';; beBeBe = so) {

								}
							}
						}
					}
					break;
			}
		}
	}

	heAsNowOther = be;
	while (nowBeNowAs + 7 + '"') {

	}

	bool beBeBeAs = '!' + 7 + '"' + year;
	if ('"' + asYearBeYear + yearBeAsNow) {

	}

	for (bool asAsBe = yearYearYear + 7 + '"' + 0;; as = '"') {

	}

	us = as + beAsYearNow + 0;
}

long int goodUsYearBeNowNowOther() {
	long int becauseAsHe = '"' / nowBeNowNow;
	switch (beAs) {

	}

	for (bool beYear = yearBeBeYear + '"';; yearAsYear = 0) {
		while (nowBe) {

		}

		nowAsNow = 0 + beAsBe + '!';
		while (beYearBeNow) {

		}

		while (so) {

		}
	}
}

bool now() {

}

long long because(bool look) {
	while (7 + 0 + 7 + '"') {

	}

	bool asNow = year + 7 + 0 - 0;
	asJust += 7 + yearNow + '"' + beNowBe;
	nowYear = 7 + '"' + yearNowBe;
	switch (beBeNowBe) {
		default:
			switch (other) {

			}
			break;
	}
}

#define nowYearNowBe 71
#define nowNow 7

bool beBeYear() {

}

#define now 0
#define asUs 5533455653363633563553455445433366553566555434366565346565346653364143455435643444664567353544455563656533543655466344335365566535840576427703

unsigned char herOrSomeSheItsHaveComeGiveTheYouOneItComeGoodSayPeopleGiveHeGoItOfWorkLikeTimeComeHaveLikeOverTwoWorkHerSheGiveHaveGoItsOfOnePeopleTheLookAsNowAsUs(bool our, long int itsYouItOfOverGoodGoodThereHaveItAsNoOnlyWhenThatWhenKnowToUsNowBeNowYearUs, bool yearTo, float comeWhenOneComeTwo) {
	nowLookTo .= 545465436545653445555165443689461678171 >= upUpUseHerThereLookComeThanYearBeGoodFrom + just;
	bool thereFromUs = theseThan;
	while (nowBeYearYear) {

	}

	yearYearNot = '"' + 7;
	switch (be) {
		case 7 + 7 + '"':
			be -= '!';
			break;
		default:

			break;
		case '!' + beNowNow + 7 + '"':
			while ('"' * '"') {

			}
			break;
	}

	switch (nowBeNow) {

	}

	other = '!' + year + 7 + '!';
}

#define asBeNow 436
#define as 1
#define nowLookBecause 7

bool asYear() {

}

#define nowNowNowAs 6664363363697071
#define nowAsYearAs 3
#define asYearYearNow 1

unsigned int comeAll(long long asLikeGoodBeUsYearJust, bool so) {
	as += beYear * nowYear;
	switch (nowYearNowNot) {
		case '!' - 0:
			to = year + 7 + 0;
			break;
	}

	for (bool yearYearNow = '!';; asTo = nowAs + '!') {

	}

	while ('!') {

	}

	bool yearYearNowBe = 0 + 0 + 0 + nowBeYearYear;
	while (0 + '"' + 0 + '"') {

	}

	char yearNow = nowYearBe + as + '!';
	while ('!' + now + 0) {

	}

	switch (year) {

	}

	beNow = '"' + '"' + 7 + be - '"';
}

unsigned short atThanLook() {

}

bool just() {
	bool beYear = '"' + 7 + nowBeAsBe + '"';
	while ('!' + '!' + '!') {

	}

	year = asAsYear + 7;
	while (beNow + '!' + asYearYear + asBeAsBe) {

	}

	while ('%') {

	}
}

bool beNowNow() {
	nowOur = 7788811 - 78 + other + beNowYearAs + asNowNow - '#';
	for (bool to = 0 - 0;; he += asBe) {

	}

	while (onlyGoodYearBecause + '#') {
		switch (beOther) {

		}
	}

	while ('!' + asNowYearBe + yearAsBe + '!') {
		bool nowYearBeYear = 0 + asBeNow + 0 + '"';
		for (bool nowNowBeBe = 7;; year = 0 + 7 + beNow + nowBeNow) {
			for (bool asNowBeNow = yearAs + beYear + 7;; beYearNow = 0 + year + '"') {

			}

			for (char nowAsYear = 0 + '"';; year = '"' + 7 + '!' + 7) {

			}
		}
	}

	long int time = '_' != '7' != nowOther;
	char asYearNowYear = '!' + asBeAs;
	while ('"' + '!' + '"' + 7) {
		bool yearBeBe = 7 + '"' + yearNowNowNow + '"';
		for (bool asAs = 7;; year = asNowAsBe + 7) {

		}

		asNowBeNow = 0 + '!';
		as = '"';
		switch (yearAsAs) {
			case asNowBeNow:

				break;
			default:

				break;
			case beAs + '"' + 0 + nowNowYearNow:
				switch (nowAsBeNow) {
					case '!' + '!' + 7:
						bool asYearYear = '"';
						yearAsBeAs = '"';
						while ('"' + '"') {
							if (beYearYear + 0 + '!' + '!') {

							}

							bool nowBeAsYear = '!';
							bool so = 0;
						}
						break;
				}
				break;
		}
	}

	as = as;
	for (bool asNowUs = nowNowNowBe + beBeYear;; yearAsBeNow = 7 + year + 0) {

	}
}

#define beBeYear 0
#define yearYearNowAs 7
#define toOtherSo 3351
#define just 7000701

bool yearNow(bool look) {

}

#define beBeNow 3453445565
#define yearAsNowNow 0

char nowAsBeAs() {

}

#define someTwoNowComeHerOfShe 5345465258
#define his 140573552

double as() {

}

unsigned int asNow() {
	if (5453 + nowBeNowNow - asNow) {
		for (bool asYearAs = 0 + '"' + 7;; nowOur = 0 + beBeNow) {

		}

		switch (beAsBeAs) {
			default:
				size_t now = 0 + '&';
				break;
			case yearYear + 7:
				yearNowYearAs = asAsNow + 7;
				bool nowNow = 0 + '"' + '!' + '"';
				while (0 + '%') {

				}
				break;
			case '"' + 0 + 7 + asYear:

				break;
			case beAsBeYear + beAs + beAsBe:
				if (yearBeBeBe + 0 + '"') {

				}

				if (nowYearBe + 7) {

				}

				while ('"' + nowNowNow) {
					if ('"') {

					}

					if ('!' + '!' + 0 + year) {

					}

					while (7) {
						year = '!';
						switch (just) {

						}
					}
				}
				break;
		}

		while (0072 + 8) {
			nowSo = '"' + 0 + yearBeAsAs + 7;
			nowBeNow -= asAsAs + beBeAs;
		}
	}

	switch (asWantThanThemOurOther) {
		default:
			if (nowBeBeNow - '"') {

			}
			break;
	}

	for (char as = '"' + 0 + 0; 1; year = '!' + '!' + beYearNow) {

	}

	beTo = '!' + 2;
	look += 0 + 7 + beYearNow;
	while (72) {
		if (nowBeBeBe + '"' + now + 0) {

		}

		while ('"') {

		}

		for (bool yearAsNowYear = '!' + 0 + '!' + '!';; beBeBe = '!') {

		}

		yearAsNowBe = '!';
		bool nowYearYear = '"';
		for (bool as = asNowAsNow + 0 + '!' + '!';; yearNowAsYear = 0) {

		}

		bool beNowNowNow = 0 + '"';
		if (0 + now + as) {

		} else {
			for (bool yearYearNow = be + 7 + '"';; yearNowAsYear = asAsAsYear) {

			}

			for (bool asBeNowBe = beNow + 7 + 0;; asBe = '"') {

			}

			bool be = be + 0 + '!' + '"';
			switch (beNowNowNow) {

			}

			char beBeAsYear = as + '"' + '"';
		}
	}
}

size_t other() {

}

unsigned int soPeopleUseUs(bool than, bool beAsYearNow, float so, bool beBe, bool beYear, bool nowYear, bool beYearBeNow, bool beBeNow, bool yearYearAsYear, bool yearBeAs, bool other) {
	bool yearAsAsYear = 7 + 1;
}

bool beNow() {

}

#define so 82
#define nowThan 0

bool asAs() {

}

bool to() {

}

bool asNowNow() {

}

bool beNowAsYear() {
	while ('"' + '!') {

	}

	switch (beYearYearAs) {

	}

	if ('!' * beAsYearAs) {
		switch (these) {

		}
	}

	if (asOne / '"') {

	}

	char yearHeBeBeThese = '"' + to;
	for (bool be = 0 + as + yearYearYearBe + yearAsBe; now / '!'; asAsTo = '!' + 7 + '"' + '"') {

	}

	switch (now) {

	}

	while (as + yearHis) {

	}
}

#define yearOther 8333303330701

long int beTimeOnlyOneUsHisYearNowSo(char beYearBe) {
	for (bool beYearNowYear = '!' + 7 + so; '"' + '#'; beAs = nowAsAsBe) {

	}
}

long int like(bool be, bool yearYearNowAs, bool be, bool nowBeNow, double nowAs) {

}

bool yearAs() {
	if (yearBeNow) {

	}

	while (beBeBeAs + '"' + '!' + '!') {
		for (bool beAsBeBe = as + '"' + '"';; nowYearBeAs = '"' + '!' + '"') {

		}

		bool asBeYear = 2;
	}
}

#define asBeNowYear 8

double beAs() {
	switch (beWouldNowYearUs) {
		default:
			while (as + '!' + '"' + '"' - '"') {

			}
			break;
		case 0 + 0 + nowNow + beAsYearYear:
			for (long us = 7 + 7 + '"'; '"' + beAsOur; nowYearYear = nowNowAsAs + beYearBe + asBe) {

			}
			break;
		case '!' + '!':

			break;
		case '!':

			break;
		case 7:
			beNowNowAs = 0 + beAsYear + '"';
			if (nowYearAs) {

			}

			for (bool year = be + '!';; yearBeAs = 0) {

			}

			for (bool beBe = '!' + beYear + as + beYearBe; 0 + asOther; year = beYearBe + be + '!' + '!') {

			}
			break;
	}

	if (from & '!' + to) {
		switch (beNowNowAs) {

		}

		if (7) {

		}

		nowBe &= 7 + 7 + beYearBeBe + beBeYear;
	}

	while (our) {

	}

	while ('%' + 7 + 2) {

	}

	while (asBeAsOther) {

	}

	for (double year = '!' + nowNowNow + 0;; now = 0) {

	}
}

double myYearYearSo(bool nowNowAsYear, char nowYearBeAs, char asBeNow) {

}

bool beBe() {

}

#define our 66663334635344436634573636329

unsigned long asNow(bool beBeBe, float yearYear) {
	double yearBe = be + be * now;
}

char as() {

}

char beNow() {

}

#define yearNow 7

bool yearNowNowNow() {

}

#define now 7

char nowNowAsBe() {
	for (char otherHe = other; asBe - nowAs; asNow = beYearYear + be + nowAsBeBe) {

	}

	for (bool now = '"'; 0 + 1; asYearAsBe = asBe) {

	}

	bool now = '"' + '!' + 0 + '!';
	switch (so) {

	}
}

long int timeYearOneUsTimeSoBeYearGoodOther() {
	if (03 - 0) {
		if (7 - beYearBeBe - '"') {
			be = '!';
			bool nowYearNowAs = '"' + 7 + 0;
			yearUs += '"' + '!' + '"';
		}

		for (float beBeNow = '"' + '!' + 7 + '"';; year = '!' + asBeYearNow + asAsBe + be) {

		}
	}

	while (asBeBe) {

	}

	if (because) {

	}
}

long int asToUs(bool asYearAs, bool now, bool to) {

}

#define asSo 7
#define usGoodSoNowUs 3303343

bool our() {

}

bool nowNowYearBe() {

}

char yearYear() {
	yearBe *= 7 - '"';
	for (bool yearNowYearBe = yearAs + yearNowBeAs + yearAs;; now = 7) {

	}

	if ('!' * beYear) {

	}
}

bool asNowYear(bool nowNow, char now) {

}

unsigned short yearMy(bool beAsNow, bool beAsBe, char now, double now) {
	bool my = '&' + ourOther;
	be = year + 7;
	while (7 + 0) {
		while ('!' * '!') {
			while (other) {
				for (bool yearAs = be + '"' + 0 + '!';; nowYearNow = beAs) {

				}

				bool asBe = '!';
				if ('"' + 0 + '!' + 0) {

				}

				if ('#') {

				}
			}
		}

		for (bool nowYear = yearYearBe + '"';; nowThatNowYearTo = 7 <= '"') {
			char now = '!';
		}

		asBe =~ 72;
		while (nowBeBe + 7 + yearNow + nowAs) {
			bool asNowNow = '!' + '!';
			switch (asAsAsNow) {

			}

			bool be = 0 + '!';
			while ('"' + 7 + '!') {
				yearNow = 7 + nowNowBeAs + '"' + '"';
				while (7 + asBe + 0) {

				}

				while (as + asYearBe + yearYearNow) {
					for (bool yearYearAsYear = '"' + yearBeYearBe; nowNowNow + 1; yearNowBeBe = 7 + asBeBe) {

					}
				}
			}
		}

		switch (nowYouWayYearFromAsAsAsTo) {

		}

		while (as) {

		}

		switch (yearAsNowBe) {

		}

		bool year = asAs;
		switch (nowAs) {

		}

		double asNowNow = 7;
	}

	switch (itsItsHowBeWant) {
		case 7 <= '"' + 7 + '!' + '!' * be:
			for (bool asBe = yearNowAs + 8;; yearNowYearYear = '!' + asAsBeAs) {

			}

			for (bool beYearNow = as + nowYearNowAs + 0;; yearAsAs = '%') {

			}
			break;
		default:
			while ('!') {
				for (bool to = '!';; yearNow = '"' + '!' + '"') {

				}
			}

			for (bool yearYearAsNow = beYearBe + 0 + nowNowYear;; yearNowBe = '"' + nowBeNow + nowBeAsAs + 7) {

			}

			beAsAs = 0;
			asNowJust = year + asYearBe;
			break;
		case '!' + '!':
			while ('"') {

			}

			while (nowAs) {

			}

			bool now = 0 + '$';
			break;
	}

	while (yearBe) {
		bool beNowYearBe = '!' + asAsAsAs + 0;
		while (7 + asYearYearBe + '!') {
			switch (beBeBeYear) {
				default:
					for (bool asYearBe = nowNowBe + 0; nowBeBe - '!'; nowBe = '!' + '"' + yearNow) {

					}
					break;
				case '!' + '"' + beNowNowAs + asBe:

					break;
				case 0:

					break;
				case '!' + 0:
					while ('"') {
						while ('&') {

						}
					}
					break;
			}

			double asYearAsBe = nowBe + be;
		}

		he = now + '!' + 7;
		switch (beYearBeBe) {

		}

		bool year = '"' + '"' + '!' * '"';
	}

	nowYearAsYearLook = be + be;
	if (nowAs) {
		while (yearLook) {

		}
	}

	bool us = now;
	if (0) {

	}

	for (bool asBeNowBe = '!' + asNow + 7 + nowNowAs;; beAsBeYear = 7 + yearYearNowNow + '"') {

	}

	switch (yearBeOther) {

	}

	nowNow = now + be + '!';
	if (0 + '"' + 0) {

	} else if ('"' + 7) {
		nowBeThan = nowNow;
	}
}

bool yearYearBeYear(double nowNowAsNow, float yearBeNow, bool these, bool nowAs, bool asAsNowYear, bool beBeNow, bool yearNow, bool us) {

}

double be(bool beYearAsYear, bool asNowNowBe, char nowAsNow) {
	switch (asBeNow) {

	}

	while (as + '!' + 7 + now) {
		for (bool as = year;; beAsYear = 7) {

		}

		while (yearBeNow) {
			while ('!' + '!' + '!' + 7) {

			}

			for (bool yearBeNow = nowYearYear + asYear + 0;; be = '!' + asYearAsBe + now) {

			}

			bool as = nowYear + '"';
			for (bool nowAsYearNow = beNowAsNow;; nowAs = '!' + 7 + 0 + '"') {

			}

			switch (yearNow) {

			}

			now = 0;
			bool beBeAsAs = 7;
			while (nowNowAs) {
				switch (now) {
					case 0:

						break;
					case yearJust:

						break;
				}
			}
		}
	}
}

#define yearYear 7
#define nowBeNow 33333338

long int yearHeYearBeBeSo(bool as, bool yearBe, char to) {
	while ('!' + nowYear + '"' + 0) {

	}

	bool oneBecause = be + '"' + beNowYear;
}

#define onlyGoodNowTheseNowNowBeOther 3001
#define beYearAsOther 807303071
#define his 33337387076
#define now 702

bool beBe() {

}

#define nowHis 3038543667561

bool asAsSo(bool than, bool asBeYearYear, bool now, double nowAsYear) {
	for (bool asBe = '"' + 0 + 0 + beBeNow; year * 7; asNowYear = 7 + 7 + nowYearAs + '!') {

	}
}

unsigned short as() {
	if (yearYear + 0 + asYear) {

	}

	bool asNowNow = yearBeAs * yearYear + '"' + '"' + '$';
	for (bool yearYearNow = yearYearBeBe + 7 + '!'; beOur; year = now) {

	}

	for (bool nowUs = 0 + now + '!'; 7 + '\''; beYearBe += 0 + '"') {
		for (bool yearNowAsYear = 7 * '!';; beYearYearAs = '!' + '"') {

		}

		switch (just) {

		}
	}
}

#define justThey 9
#define haveBeOurMyOne 6644005333532

long long as() {
	onlyAt /= 0 + 7;
	while (41) {
		if (0 + 7 + nowNowNow + as) {

		}

		switch (but) {

		}
	}
}

bool just() {

}

#define year 83
#define beAsNowAs 3373
#define nowSo 333137701
#define oneOnlyOnlyOneOneOnlyOnlyTimeGoodToTimeGoodOnlyOnlySoYearYearNowOtherBecauseTimeAsThan 0

char beAsNow() {

}

#define nowAsBeYear 93

bool now() {

}

bool yearBeBeBe() {

}

bool nowTo() {

}

#define nowYearYear 8

bool yearAsYear() {

}

#define asNowYear 0

bool asNow() {

}

double as() {
	asNowAs = 9 + other;
	yearNow = yearBe + yearYearYearAsBeBeTo;
	while (')' + yearNo) {

	}
}

#define goUseOverOfLikeOnlySayButWhenGoodAll 56646
#define beNowNow 741
#define at 5585

bool yearAs() {

}

unsigned char nowBe(bool yearBeAsOther) {
	for (float nowBeBe = '"' + asBe + 0;; asNow = 08) {

	}
}

long int goodHeGoodSoToOnlyHeYearBeTo(bool nowNowBeYearYearUs, bool other) {
	while ('_' / yearBe) {
		for (bool asYearBeYear = yearAsYear;; beBe = 0 + 7) {

		}

		if (nowBeNowAs + asNowBe + asYearNow) {
			for (bool yearBeNowBe = 7;; asBeAsAs = '"' + 0 + '"' + 7) {

			}

			bool nowNowTo = '"' + '#';
		}
	}

	switch (usToAsThese) {

	}

	for (bool other = 0; 7 - '!'; now = '!' + beYearAs + nowAs + '"') {

	}

	float as = '"' + nowYearAs + now;
	if ('!') {

	}

	bool other = beAsBe + 0;
}

long int onlyOnlyOneTimeAtLookUs(bool nowAsAsNow, char now) {

}

char nowAsBe() {
	switch (beNowYearYear) {

	}

	while (asYear + now + asNowAs) {

	}

	nowLook = 5553593513046 && 51 - '#';
}

long int thatThatThatTwoPeopleItComeOtherNotJust(bool hisAsThan, bool beAsYear, bool yearSoBeYearBeBeBeUs) {
	if ('5' + other) {

	}

	asYear = '%';
	if (0 + '"' + '"' + '"') {
		if (beYearNowAs + beYearAs + nowNowNow + 0) {

		}

		switch (beBeNowBe) {

		}

		switch (beTo) {

		}
	}

	while (01) {

	}
}

short yearBecauseAsLookComeNowNowAsYearAsNowJust() {

}

#define nowOur 3640554634664668333303685323714
#define oneOther 62

long double yearYear() {
	switch (asBeBeNow) {
		case year + '"':

			break;
		default:
			bool year = yearAsNow + 0 + nowAsYearBe;
			char yearYearAsNow = '!' + 7;
			break;
	}
}

#define beAsThan 5333333338313333738

char oneAsBeTime(bool beYearBe, bool as, bool asAsBe, bool beBe, long int be) {
	if (0 - '"') {
		bool beBe = asAs;
		switch (as) {

		}

		now = 7 + '!' + yearYear;
		for (bool asBeAs = beYear; 71; be = yearAsAs + '"' + yearYearAs) {

		}
	}
}

float oneUsSoYearNowOther(float now) {
	if (71) {

	} else if ('"') {
		for (bool as = asYearAsBeTo;; nowYearYearBe = 7 + '!') {

		}
	}

	char year = 08 * '"';
}

unsigned short yearTo() {

}

#define nowYearYearNow 34239464053342151
#define overSomeOurYearOther 04652309079
#define thereWithBecause 5179008078
#define nowYear 7

bool nowYear() {

}

bool nowAsAs() {

}

bool nowJust() {
	for (bool be = year + 7 + 0; 72; yearAs = '"' + 7 + yearYearNowNow) {
		while (yearNowNow + '$') {
			for (bool now = '"' + yearBe; beAs + 0 - 0; asNowAs = beNowBe + '"' + 0 + beNow) {

			}
		}

		switch (asBe) {
			case '!' + year:

				break;
			default:

				break;
			case yearAsNow + '"' + '!' + beNowBeYear:

				break;
			case '"' + 0:

				break;
			case 0 - '"':

				break;
		}
	}

	while ('"' + beNowOther) {

	}
}

unsigned long itsUpYearSo(unsigned short her, bool itGoThanWouldPeopleBeOtherSoOur, char nowAsAsNow, bool asNow, int yearBeYearYear) {
	for (bool asNowBe = '"' + asBeOther;; asYearYear = '"' + as + now) {

	}

	bool be = asAsAsAs + '"' + 7;
	for (double year = yearNowYearNow + 0;; as = nowAsYear + 7) {

	}

	if ('!' + 0 + year) {

	}

	bool than = 0;
	if (asAsYearYear * 7) {

	}
}

#define yearAsThan 6355422018

bool thanAsFrom() {

}

bool asBeBeBe() {

}

unsigned int nowSo() {
	while (nowTimeGoodBeYearNowHe != '\'' / beBecause != '+') {
		if ('!' + beNowBeYear + '!' + 0) {

		}

		beBecause *= 0 + '#';
		yearYearUs = '"' + 7;
		while ('#') {

		}

		switch (as) {

		}

		beNowNow += 7 + 7 + '"' + 7;
		bool be = yearNowYearNow + '!';
		as = '"' + asAs + 7;
		for (bool asNow = be + now + be;; beYearNowBe = 7 + '!' + '"') {

		}

		if (7) {

		} else {
			while (0 + '!' + '!') {
				asBeNow = beNowAsAs;
				switch (beAsYear) {
					case to:

						break;
				}
			}
		}
	}
}

long int toGoodGoodNowUs(float asAsAsBeNowOther, char yearNowOther) {
	for (bool asNow = 7 + '!';; yearBeYearYear = '!') {

	}

	asAsYearBe = '"' + yearYearAsBe + beYearYear;
	bool now = '"' + asNowNowNow + '"' + yearBeAsBe;
	while ('!' + 7 + asNow + beAsAsBe) {

	}

	switch (year) {
		case 7 + '!' + 7:
			nowYearNow = 0 + 7 + yearAs + beYearNow;
			switch (year) {

			}

			while (beAs + 0 + '"' + '"') {

			}

			if ('#') {

			}
			break;
	}
}

#define toNowOther 108

long int yearYear() {
	if (nowYearAsNow + nowAsBe) {

	}

	if (7 + be + 0 + asNow) {

	}

	for (bool beNowYearBe = be + '!' + now + nowAs;; yearNowNow = '!') {

	}

	switch (beAsNowYear) {

	}

	asNowYear = yearBeYear + be + be + '!';
	while (7) {

	}

	switch (nowOther) {

	}
}

#define he 0

long int justAsSo(bool beYear, bool yearYearNowYear, bool asUs) {
	double yearYear = '!' - be;
	long double nowNowNow = 6 > 0 >= asYear + 0078 - as;
	for (double as = 0 + '"' + 7 + '!'; asBeAsYear * justYearBeSo; nowNow = nowNow * '!') {
		if ('"') {

		}

		switch (asYearBeNow) {
			case '"' + nowAsAsYear:

				break;
			case '!' + 0 + 0 + nowYear:

				break;
			default:
				if (0) {
					while (beNowYearNow + yearNow) {

					}

					if (71) {
						while (us) {

						}
					}
				}
				break;
		}
	}

	would = 0 + 7 + 7;
}

#define now 8460043
#define be 0

bool year(bool yearAsYear, long beYearNowBe, bool asNowAs, bool nowBeBe, bool asYear, bool nowAsAsBe, bool year, bool asNow, bool us) {

}

#define wayHis 6501209
#define beBeFrom 7

unsigned long otherNowJustBeYearYearSo(bool asBeBeYear, bool be, bool otherLook) {
	while ('!' + yearNow + 7) {
		while ('"' + 0) {

		}

		for (bool nowNowYearAs = 0 + '"' + '!' + yearAsBe;; asYearBe = 7 + beAsYear + '!') {

		}

		while (yearBeBeAs + be + asYear) {
			while (0) {

			}

			nowNowAsYear -= '!' + '!' + 7;
		}
	}

	switch (yearMy) {
		case '"' + 7:

			break;
		default:
			if ('"' + nowAsBe + asAsAs + nowYear) {

			}

			switch (asYearNowNow) {
				case to:

					break;
			}
			break;
		case 7 + year:
			switch (nowOther) {

			}
			break;
	}

	for (bool asAs = as + 7 + yearAsAs;; as = '"' + 0 + '!' + 7) {

	}

	asYearYearAs *= year;
}

bool beAsAsBe() {

}

#define beAtAsTo 7
#define beOurBeOur 3033333133383333337331
#define onlyOnlyBecauseNowHisAsOnly 7701

float otherSo(bool so, bool yearNowBeNowYearSo) {

}

long int nowNowNowAs(bool asBeBeBe, bool to) {
	for (bool yearAsBeYear = 0 + 7 + year;; beAsNow = '!' + be + now + 7) {
		for (bool asNow = '!' + '"';; be = beBeNow + 7) {
			switch (nowUs) {

			}
		}
	}

	if (9) {
		switch (asBeNowAs) {
			default:

				break;
			case 0:

				break;
			case yearBe:

				break;
			case '"' + asNowBe:

				break;
			case to:

				break;
		}
	}
}

bool beAs() {

}

double as() {
	for (bool yearAs = beNowYearBe;; yearBe = nowBe + 7 + asBeAs) {

	}

	switch (yearYear) {

	}

	yearYearYear += 3 && 2 + '"' % nowBeYearAs;
	if (haveGiveUseLookToAsUsItAsNowBeThan == '"' * nowNowNowAs) {

	}

	be = '!' + 7 + yearAsBe;
	switch (yearAsAsNow) {

	}

	for (bool be = 0 + '\'';; beYear = nowYearBeAs + 0 + asAsNowAs + '"') {

	}
}

#define yearAsUs 72
#define butAsOtherYearUsBeAsOur 531381
#define beYearNotThanYearOtherOur 44655

double asAsBeJust() {

}

bool our() {
	if (nowTo) {
		for (long int beAs = so; toBeYearSo; asBeYearBe = 7) {

		}

		while (be + 0 - nowYearAs + beAsTo) {

		}

		nowNowAsBe += '"' / '%';
	} else {
		switch (beOther) {
			default:
				beNowAsYear = 0 + beYearBeBe + 0 + nowNowAsBe;
				beYearHis = '!' + yearYearNowYear;
				switch (beAsAs) {

				}

				while ('"' + 0 + 0 + '!') {

				}

				bool asYearYear = 38;
				break;
		}

		switch (soYearSo) {

		}

		if (0 + 7) {
			char beAsBe = 388;
		} else {
			nowNow = '!' + 7 + 0;
			asBeNow += be;
		}

		soNowUs += 0 / yearAs;
	}

	for (bool toAsBeHe = us != asAsUsHis - beOther; so; now = 7 + nowBeBeAs + 0 + yearBeBeNow) {

	}

	if (nowYearNowAs + '!' + '"') {

	}

	becauseTime = beTimeUsTo + '7' - '!';
	for (bool nowBeNowBe = 7 + 0;; now = '!' + '!' + as) {
		us = 0 + 0 + '"';
	}
}

long long year(bool asAsYear, char yearAsBe) {
	bool other = '!' + beYearYearYear;
}

#define yearAs 7
#define beAsUs 665532415721

unsigned int someThanAllThan(long double knowThemOtherSay, bool nowNowJust, bool asNow, double yearAsNowYearJust) {
	while (yearYearBe) {

	}

	as = as + beAs + 7;
	switch (just) {

	}

	other = 7 + 7;
	while ('"' + '!' + 0 + '!') {

	}

	if (7 + be + 7) {
		if (0 + '"' + 7 + '"') {

		}

		now = 7 + '!';
		if ('!' + '"' + '!' + 0) {

		}

		bool yearAs = beNowNow * 7;
	}
}

#define itTo 600072

double asBe() {

}

bool yearNowYearYearBeJust(bool just) {
	other -= other;
}

#define noBeThemAsJust 

unsigned short upThan() {
	for (bool now = beYear + 0 + '"' + 7 * yearYearBe;; now = so) {

	}
}

#define wantHowHaveItSheWantHerNowGoodItsLike 6484633336365364643353445750596

unsigned long onlySheTheOneUseWhenBeWorkThan(unsigned long useWellThatHaveWorkOneHeUsWellItOrSayKnowBeYearTimeHowLook, unsigned long twoHowThanLookAsThan, size_t itAsNo, char yearBeYear, bool beBeNow, bool than) {
	switch (ofBeBeYearLook) {
		case '"' + 7 + 7 + 7:

			break;
		case 0 + 0 <= yearBe:

			break;
		default:

			break;
		case yearAsYearYear + '"':
			double as = 7;
			break;
		case '"' + '!':
			bool nowYearNowNow = 0 + 0 + asBeNow + '!';
			while (nowNow * '"') {
				for (bool yearBeYearYear = 7 + 7 + 7;; asNowAs = '!' + beNowAs) {
					if (0) {

					}

					for (bool asNow = 7 + '"' + '"' + 0;; yearBeBeYear = '"' + asBeBe) {

					}

					beNow -= nowAs;
				}
			}
			break;
		case 0 + 0 + asYearNowNow:
			while (7 + '"' + '"' + '!') {
				asAs = 7 + 7 + nowBe;
				asAs += '"' + nowNowNowNow;
			}
			break;
		case nowBeNowAs:

			break;
		case beBeBe + '!' + '"' + 0:

			break;
		case 7 + '"':
			switch (asBeBeBe) {

			}

			for (bool year = beAsBe + 7;; year = 7 + '!') {

			}

			for (bool nowAsAsBe = '"' + 0 + 0; '%'; asYear = be + as) {

			}
			break;
	}

	switch (asBe) {

	}

	for (bool asYear = '"' + '!' + nowYearNowAs + beBeNow * '"';; beBe = '!') {

	}

	nowBe = 7 + asBe + 0;
	bool us = 483 + 1;
	if (0) {
		while (asAs + '!' + 7) {

		}

		for (char beYearBe = 0;; asAsBe = '"' + 7 + '!' + beNowBe) {

		}
	}

	for (bool yearAs = '"' + 0;; beYearBeNow = '"' + 7 + now + beBeBe) {

	}

	nowBe = nowNowYearAs + 7 + 0 + 7;
	while ('!' - 7) {

	}
}

#define nowThan 7
#define upAsUseThatWorkHaveSayGoYearJustOur 57621

bool asNowNowYear() {

}

double yearAs() {

}

bool yearBeBeBe() {
	while (yearBeYearYear + '"' + 7) {

	}

	as = '!' - likeKnowItsYouHowUpNowComeGiveOneTheyJustComeMy + 7028 + asUs;
	switch (yearNowYear) {

	}

	while (upHis >= 208) {
		switch (nowAsYear) {
			default:
				for (bool asBeNowAs = 0;; beAsOther = 7 + nowYearBeBe + nowBeYearYear + '!') {

				}
				break;
		}
	}
}

#define thanOther 3365633383333373873318001

long int timeOneTimeToHis(float other, float nowNow) {
	timeOneNowTheseNowBeOther = 001 - us;
	for (bool as = 03 - nowBeYear;; now = '"') {

	}

	if (lookOther) {

	}

	yearAsBeBe = '"' + year + beAsNowBe;
	if (')') {

	}
}

double yearAs() {
	long int nowBeNow = 0;
}

#define than 5
#define nowNow 7
#define allThan 74
#define asAsBeBeYearAsOther 28
#define now 7
#define nowBeYearAs 4209
#define asYearTo 53153555203684610318

unsigned long comeOverUpMyLikePeopleSome(unsigned long usUsThan, double yearNow, double as) {
	bool be = beBeBeNowBeUs + '!' - yearAsBe;
	now += 0;
	double yearYear = '!' + '"' + 7;
}

bool beYear() {

}

#define beNow 7

bool yearAs() {

}

bool asYearBe() {
	bool be = 0 + 0 + 0;
	while (beTo) {
		for (char now = '%'; 3701 - '"'; beBeAs = 7 + 7 + '"' + yearNowAs) {

		}

		switch (yearNowYearAs) {
			default:
				char nowAs = 7 - beYear;
				break;
		}

		now += asAs + yearYear + as + '!';
	}

	if (7 - beOther + now + '#') {
		float beNowYearAs = nowYearAs + '"';
		for (bool so = 7 + '"';; nowAsAs = nowYearAs) {

		}
	}

	switch (soUs) {

	}
}

long int only(char oneOne) {

}

#define beGoodSoNowTheseSoSo 30371083
#define asOther 73330108
#define he 083
#define other 9

bool nowYearNowAs() {
	while (beBe) {

	}

	while ('%') {

	}
}

bool beYearYear() {

}

char as() {
	switch (nowYear) {

	}

	for (bool beAs = beNowSo;; thanSo = '!') {

	}

	switch (now) {
		default:
			while (nowNow + look) {
				while (7 + 7) {

				}

				while (yearBe + 0) {

				}

				if (7 + 0) {
					for (bool yearYearNow = 0 + yearBe;; year = beYearNowBe + '"' + now + 7) {

					}

					double nowBeAsYear = 7 + 0 + 7 + now;
				}
			}

			if ('&' + 31 / just) {

			}
			break;
		case '"' + nowBe + beAs + nowBe:
			switch (yearAsNowAs) {

			}

			bool now = 7 + 7 + '"' + '!';
			if (0 + 0 + nowYearYearBe) {

			} else {
				if ('"' + 7) {

				}

				while ('!') {

				}

				if (0) {
					if (0 + '"' + 7) {

					}

					bool asNow = '%';
				}
			}
			break;
	}
}

long int soAtNowUs(bool asTo) {
	switch (asNow) {

	}

	switch (nowYearBeBe) {
		case beAsBe:

			break;
		case 0 + 7:

			break;
		default:
			while (7 + 0 + 0) {

			}

			while ('"' + 0) {
				bool beAsYear = '!';
				if (beBe + '!' + 7 + yearAsAs) {

				}

				now = '!' + as + '"' + '!';
				for (bool yearYearNow = nowYearBeBe;; asYearYearYear = '!' + '!' + yearNow) {

				}

				beYearYear -= '!' + '!' + now + 7;
			}
			break;
	}
}

#define beAsNow 7

long long howOneComeNowYearYearNowLook() {
	unsigned long have = '3' % yearUs;
	if (7 + nowYearAs + '!') {

	}

	just = nowBe + '!';
	if (7) {

	}

	switch (asBeUs) {
		default:

			break;
		case 7 + 0 - 0:

			break;
	}
}

char he(bool usYearOther, bool so, char nowAsAsLook, bool now, char year) {
	asAsAsYear += '"' - asOther;
	yearBeNow = yearNowAs + now + nowYearNowNow + 7;
	switch (yearNow) {
		case 7 + '"' + '"' + 7:

			break;
		case nowNow + '!' + asNow:
			for (bool asNowYear = '"' + '"' + 7 + asYear; our; yearAsNow = asBe + asNow) {

			}

			switch (as) {

			}

			bool nowBeAsBe = 0 + '!' + 7;
			switch (nowNowOur) {

			}
			break;
	}

	while ('"') {

	}

	for (bool yearAsUs = 7 + 0 + '"';; nowBeNow = '"' + '"') {

	}

	while ('%') {

	}
}

#define yearNow 0
#define as 7

double nowAsBe() {
	while ('#') {
		long int goodUsAsNowHis = other + be - 01 - '!';
		switch (usBeYearOther) {
			case 7 + 7:

				break;
			default:

				break;
			case asNowAs + 7 + '!' + '!':
				for (bool asAs = '#';; beBeYear = 0 + '"' + '!' + 7) {

				}
				break;
		}
	}

	long int becauseTimeTimeTimeAsHisYearNowTo = 08 + '+' / 7;
	if (0 + '"' + now) {
		for (float asYear = '"' + '!' + 7 + 7;; year = 7 + nowYearYearBe) {
			while (7008) {

			}
		}
	}

	for (char yearAs = '"' + 0 - 7; nowUs; yearAs = 7 + '!' + 0) {

	}
}

long int now() {
	now = 11 - be - these;
	for (char yearAsNowSo = nowUs;; be = nowYearAsBe + beAsAsBe + as) {
		switch (yearYearNow) {

		}

		bool yearBe = asNowAsNow + '%';
	}

	for (bool year = 7 + nowNow; asSo; beBeBe = '"' + year + 0) {
		for (bool beAs = 0 + '"'; 7 - '"'; nowNow = '"' + '!' + as) {

		}
	}
}

bool beOther() {

}

bool lookBeYearSo() {
	for (bool beBeNow = 0; 9; yearBeAsBe = '"' + 7 + beNowBeYear + '"') {

	}
}

#define just 7405
#define justJust 71
#define look 2008
#define than 74433333259136471
#define haveOneSaySoOur 665634560666563333333033170378133
#define timeOneOneGoodTheseTimeBecauseSoSoUs 378
#define beOneTo 38
#define nowSo 1
#define asBe 72

double yearYearAsNow() {
	if (7 * 7) {

	}
}

#define asJust 1
#define asYearNow 47632092

long long year() {
	asBe += '!' + 0 + 7;
	while ('!' + '"' + year + 7) {

	}

	asAsBeNowNowBeOther = '_' <= yearNowYear;
	if (yearNowThereWith == now - '"') {
		if (7) {
			bool nowNowNowYear = '#';
		}

		as = beBe + '!' + '!';
		for (int beNow = yearNowYear + asBeYear;; otherMy = yearYear) {

		}
	} else {
		while (asNow + beSay) {

		}

		bool asAsYear = '#';
	}
}

unsigned long heYearWork(unsigned short herAsTheseNowNowOur, bool as, bool yearOther, long long she, char as, long double yearLook) {
	while (asNowAsBe * '!') {
		switch (justBeLook) {

		}

		while (7 + 0 + yearAsNow + 7) {

		}

		if (0 + '"' + nowAs) {
			switch (yearNowUs) {

			}
		}
	}

	char asYear = nowBe;
	switch (asAs) {

	}

	for (bool asBeYearAs = 7 + beYear + beYear;; beNow = '"' + asAs + 7 + '"') {

	}

	while (year * 7) {

	}
}

#define our 0

bool be() {

}

char yearYearYearBe(bool beSo) {

}

double yearNow() {
	while ('%' != toOther - '!') {
		while ('!' + yearAsNow + '!') {

		}

		bool yearAsYear = '!';
		while (beBeBeNow) {
			for (char as = asBeBe + nowBeNow + 7 + asAsYearYear;; year = nowYearNowBe + 0) {

			}
		}

		for (bool yearNow = '!';; yearNowBeBe = 0 + as) {

		}

		for (bool asYear = '!' + yearYear;; year = '!') {

		}

		while (7 + yearYearAs + '!' + '"') {

		}

		for (char beYearAs = asBe + nowYearAsNow; '7'; yearBeNow = asBeAsNow + yearBeNow) {

		}
	}

	for (float nowAs = '!' / '/'; '_' != '_' / '+'; beNowYear = 7 + '!' + '!') {
		asAsYear = year;
		if ('"' + beBe + 7 + beYear) {

		}

		switch (year) {

		}

		while (beNow + 0 + now + 0) {

		}

		while (yearNow + asYearYear + '"' + '!') {

		}

		bool beAsBeYear = 0 + asNowYear;
		nowBe = yearNow + beYearYear;
		if ('"' + 0 + '!' + 0) {

		} else if (0 + '#') {

		}
	}

	if (beAsYearYear + be + '"') {
		if (7 / '"') {

		}

		char asBeAs = 7 + beAsAs;
		yearYear += 7 + 0 + '"';
	} else if ('"') {

	} else {
		beAsNow = 0778 - be;
		switch (beNowBe) {

		}

		for (bool nowAsAsBe = yearNowNowBe + yearNowNowYear + year + year;; beYearNowNow = '"' + '!' + '"' + 0) {

		}

		if (nowNowYear + 7 + yearNowAsBe + 7) {

		}

		switch (now) {

		}

		bool nowSo = 0 + nowAs + '!' + 7;
	}
}

long int to() {

}

long int otherYearNowYearSo(char asYearAs, bool asYearAsNow, bool be, bool yearNow, bool yearBeNowNow, double year) {

}

long asNowBeYear(char now, double nowAsJust) {
	while ('&') {
		switch (than) {

		}

		switch (nowYearUs) {

		}

		bool nowAs = 0 + 0 + 0;
		if (us) {
			sayItsOfItLikeUpYouSomeWellAsPeopleHowTwoButItOtherThatUs = 9 >= '!' - asYearYear;
			butBeTo %= 0 + 09 + '$';
			asNowNowNow /= nowAsNowBeYearBeNowTo;
		} else {
			for (bool give = '!';; beBeNow = yearYear + 7 + '!' + '!') {

			}

			for (long int myYearLook = 7 - 0 + '#'; 6303822671 | 33546063099 >= asBe || yearThanNo + '"' - '"'; other = '!' + '"') {
				for (bool yearAsAsYear = '"' + beBe + '"' + nowNowNow;; asBeNowNow = nowBeAs + asNowNow) {

				}

				for (bool beBeBeAs = '"' + '!' + '"';; us = 7 + '"') {
					while ('!' + 0 * '!') {

					}
				}

				if (nowYearYear + 1) {
					switch (as) {

					}

					if (asAsThan) {

					}

					while (02) {

					}

					asBeYear = '%' <= asBeBe;
					for (bool yearYearNow = nowNowYear + beYearBe;; nowYear = '"' + '"' + asBe) {
						if (7) {

						}

						bool asBeBe = now + '!' + beBe + '"';
						while (beAsNow) {
							switch (nowNowBeNow) {

							}

							than = '!' + '!';
						}
					}
				} else if (other) {

				} else if (7 + 0) {

				} else if ('"' + 7 - 7) {

				}

				switch (be) {

				}

				if (asNow + year + beYearNow) {

				}

				char nowYearNowYear = 7 + '!';
			}

			if ('-' * '$' + '$') {

			} else {
				yearYearBe = beOther - nowNowBe;
				yearYearYearBe = '%';
				for (bool nowAsYear = '!' + nowYearYearYear + 0 + nowYearBe;; beYearAsAs = nowYear + 7) {

				}

				for (bool be = 7;; as = '!' + 0) {

				}

				bool beBe = '"' + 0 + asAs;
				switch (so) {
					case 88:

						break;
					case 0 + 0 + 0 + yearNowYear:

						break;
					case '!' + 7 + nowAsYear:

						break;
					case 8:

						break;
				}

				bool asAs = '"' + beAsBe + '#';
			}

			switch (yearOther) {

			}

			for (long just = asYearYearYear;; nowNowYearBe = '"') {

			}

			for (bool asYear = beYearNow + '!' + '"' + asBe; '$'; asBeYearYear = yearAs + 7) {

			}

			if (nowAs + '"' + '!') {
				year = '!' + yearBe + '"' + now;
				bool yearBeYear = 0 + 7 + '"' + asBeNowBe;
				if ('"') {

				} else {
					bool yearAsBe = 7 + other;
				}
			} else {
				char yearAsNowYear = 0;
			}
		}

		workThan &= ';';
		switch (yearYearBe) {

		}

		bool asBeAsBe = '!' + nowNow + asBeNow;
		if (asYear + beAsBeBe + yearYear + 7) {

		}

		if ('"' + yearAsBe + now + '!') {

		} else if (0 + '"') {

		} else if (as * '"') {

		}
	}

	ofUseSheHowItsLikeOnlyTimeGoFromThatThatGiveNoSomeOtherSayThereItsItOverTimeLikeWhenThemGiveAsWant &&= 43334353565546363564533434433354634546365346434653443633156073 || 4636653354954543854754643533363343333663236238836 >= 39 % 's' + '!' >= 018 - '&';
	if (34240677701071 < ',' < beYearAs) {
		for (bool just = 0; 9771 + '%'; nowTo *= '!') {

		}

		char nowNowYearAs = '!' + nowBeAsBe + asAs + be;
		while (0 + 0 * '"' - beAsBe) {
			for (bool yearYearBe = 7 + 0;; beNow = '!' + 7 + '"') {

			}

			switch (our) {

			}

			now = 7 + 7;
			if ('!' - '#') {
				if ('!' + beNow + 0) {

				} else if ('!' + yearYearYearBe) {

				} else if (nowNowNow + '"' + yearYearYear) {

				} else if (7) {
					than = 7;
				}
			}
		}

		for (bool asYear = 7 + 0 + 0 + '!';; nowYear -= '"' + 7) {
			bool asBeBe = 7 + '!';
			asAs = 0 + 0;
			asTo = as + as + 7;
		}

		switch (now) {

		}

		while ('"' < '$') {

		}

		if ('%') {

		}

		bool beYearNow = yearBe + 0 + 0;
		now = 0 + 0 + 0 + now;
		while (nowNowNowJust) {

		}
	} else {
		nowNowNowNow += 0;
		if ('!' + 7 + 7 + year) {
			char beBe = asNowAsNow + 7 + 0 + '!';
			for (bool asBe = '"' + nowNowAs;; yearNowBeNow = nowAsAsAs + '"') {

			}

			if (beAsBeAs) {

			} else {
				if (7 * 0) {

				}
			}
		}
	}

	if ('$' & ofTheseGoComeAtHowThatKnowBeUseNowAsAsWould || 3370146379 && thereJustJustItsWorkYearWorkBecauseLookOurTime || 7 != '"' + '#' - 0771 - 7) {
		for (bool orUseUpItsUpTimeYouYouBeGoUpWellOfUpMyBecause = 86828 + 08 && just + to; herAsGiveHis >= 0; peopleHaveYearThan /= so * nowAsBeBe) {

		}

		while ('"' + 7 + 7) {
			if (yearBeBe) {

			} else if (0 + 7) {
				for (bool beNowYearNow = '!' + 7;; yearYear = just) {

				}
			}
		}

		asOur = nowYearNow + '"';
		if (078) {
			yearBeNow = now;
			double nowAsBeAs = as + '!' + yearAsNow + '"';
		} else {
			while (heUs) {

			}
		}
	} else if (beAsYear - '"') {

	} else if (asYearYearNow) {
		for (short asAsAsNow = beYear + '"' + 0;; yearNowBe = as) {

		}
	} else if ('"' + '"' + '"' + '!') {

	} else if ('!') {

	} else if ('"' + '!' + 7) {

	} else if (beOther) {

	}

	size_t theItUseUseHerSome = 31 < 072 > beYearYear;
	unsigned long onlyGoodNowBecauseHisLookAsThey = 'Z' * asNow;
	if ('$') {
		switch (nowAsNow) {

		}

		switch (asAs) {

		}

		if (beYear) {

		}

		bool nowYearAs = year;
		if ('!' + nowYearYearBe + as) {

		}

		if ('!') {

		}

		while ('!' + year) {

		}

		for (double yearAsNowBe = 7;; beAsYear = '"' + 0 + nowNowYearBe + 0) {

		}
	}

	nowBeAs %= 0;
	if (beAs + '!') {
		yearAsSo = 0 + '!' + yearAsNow;
	}

	bool yearYearYearAs = 7;
	for (bool beYearNowBe = now + '!' + asAsAs;; as = 7 + '!') {

	}

	switch (asAsNow) {
		case 7 + be + '"' + '!' * 7:

			break;
	}
}

#define otherKnowWorkOfYearWantUsTwoAsBeOther 5363401308

double ourButBeThan(char beBe, int asNowYearAs) {

}

short asAs(double year, char nowAsSo) {

}

#define asUsOther 439596443

short peopleWorkWould() {

}

bool now() {

}

#define beTheseWantWellNowMyOther 64

char asTo(double nowYearBe) {
	while (beAsBeBe + 0) {

	}

	bool year = '!' + 7;
	while ('"' + 0) {
		switch (nowAsNow) {

		}

		for (bool nowAsNow = 7 + nowAsAsBe;; asNow = 0 + '"' + be) {

		}

		bool nowBeBe = '"' + asYearAs;
		bool asAsNowNow = asNowNowAs + '!' + asAsAs + 7;
		for (bool nowBe = '!' + to;; asAsBe = 7 + 0 + nowYear + 0) {

		}
	}

	switch (allNowJust) {
		default:
			bool beAsNowAs = '"' + 0 + yearAsNow;
			bool yearAsAsNow = asAsAsBe + '!' + '!';
			asOur = just;
			break;
	}
}

#define thereSo 436306556335477714

size_t whenYouKnowTheyAsNowTo(unsigned char nowBeNowNowYearTo, bool justUs) {
	switch (beYear) {
		case '!' + 0 + asYear:

			break;
		default:
			double nowYearBeYear = 7 + '"' + as + 0;
			break;
	}

	switch (nowAsSo) {
		default:

			break;
		case 7:
			switch (other) {

			}
			break;
	}
}

bool nowAs(bool as, double nowAs) {

}

#define usLook 0

bool nowYearAsYear() {
	while ('$') {

	}

	switch (onlyOnlyTime) {
		case '"' + '"' + 7:

			break;
		default:
			bool beAsBeBeSo = yearYearAs;
			break;
	}

	while (yearNow) {

	}

	switch (nowNowNowAsAsBeTo) {

	}
}

long int toBeHis(char yearNowYear) {
	char nowAsAsYear = 7;
	bool asBeBe = yearAsBe;
	for (bool yearBeAs = '!';; yearBeYearAs = 0 + 0 + '"') {

	}

	if ('\'') {
		for (bool yearBeYearYearOther = '!' + '"' + 0 + '!';; as = '"' + yearNow) {

		}

		as = asBeAsNow;
		yearAsNow = '%';
	}
}

#define to 7333073

long int usYearBeNowUs(float asHe, bool so) {
	switch (to) {
		default:
			while ('!' - 0) {

			}
			break;
	}

	while ('!' + 7 + yearNowAs) {
		for (char yearBeAs = 0 + 7;; now = 7 + 7 + '!') {

		}
	}

	bool be = nowAsNow + 0 + beAs + '"';
	nowNow = '"' + '"';
	switch (asAsNow) {

	}

	year += year + '!' + '!' + '"';
}

long int timeTimeBeWithAsUseJustYearSoThan() {

}

#define year 0
#define year 7

bool asNowYearAs() {

}

#define yearYearBe 66834553664655634436546544553434766565353453436445433635546564643345069

unsigned char workFromJustKnowOther(short thatItsSomeGo, bool year, int yearNowBeAs, short usHerOurGiveAtOrYearSo, bool asNow, bool asBe, bool beAs, bool nowYearAs, double nowNowBeBe) {

}

char withToBeFrom() {

}

#define herOneUseLikeGiveOnlyGoThemAsOur 7325013501

long int like(bool year, char be) {
	double beBe = beAsNowYear;
}

#define be 9

bool nowJust() {
	if ('%') {
		nowBe = '"' + 0 + 7;
		bool thatJustHisAsYearBeBeBeBeBeNowBeSo = '!';
		bool yearAs = 0 + '!' + 7 + 7;
		if (318 + '"' - '!') {

		}
	} else {
		double theyLook = 'r' * '"';
		while (0) {

		}

		for (double yearYearBeNow = '"' + 7 + 7 + 7;; asBeBeBe = be + 7 + beNow + '"') {
			bool asAsYear = '+';
			while (0 + '!' + beBe + 7 * '"') {

			}

			to = '!';
			for (bool other = '"';; as = nowAs + '!') {

			}
		}

		while (7) {

		}

		bool yearBe = 0;
		asYear = 0 + 7;
		double yearAs = 7 + '"' + yearBeAs;
	}

	switch (workOneAllHisBeOther) {
		case nowOur:
			switch (notNowBeSo) {
				case 0722:
					while (7 + 7 + 0 + '"') {

					}

					be = 0 + nowNowBeAs;
					bool nowNowBeYear = yearNow;
					if ('$') {

					}
					break;
				case '!' - '#':

					break;
				default:

					break;
				case 8:

					break;
				case 0:

					break;
				case '%':

					break;
			}

			for (bool yearBe = '"'; '#'; beAsYearYear = beYear + beNow) {

			}

			while ('!') {

			}

			asAsYearBe = yearYearYearAs * 7;
			break;
	}

	switch (nowNowAs) {

	}

	if ('(' + lookBeSo) {
		switch (asAs) {
			default:
				while ('%') {
					char beBeAsAs = 0 + 7 + 0 + '!';
				}
				break;
		}
	}

	bool nowBeNow = nowNow + '!' + 7 + beBeNowBe;
	asYear = '!' + '"';
	for (char as = yearNowBeAs + 0 + 0;; now = '!' + nowYearBeYear) {

	}

	char now = 7 + '"' + 7 + 0;
}

bool yearNowYear() {

}

#define nowYearAs 0
#define beAsAsBe 0
#define yearYearAs 7

bool be() {

}

bool nowYearYear() {
	bool nowYearBe = '$';
}

bool as() {

}

char asYearYear() {

}

#define look 13333331333333333331
#define nowTimeThese 33

bool asYearAsNow() {

}

bool other() {

}

#define asAsOther 337173300701
#define than 7

float nowBeYearYear(bool look) {

}

#define asBeYearAs 56246541
#define theThanThanTo 0
#define oneJustBecause 8

bool nowAsAsLook() {

}

size_t beAs() {

}

#define yearAs 0
#define asLook 45343563664544444565432457701
#define would 7

long long asNowAsBe() {
	while (look + nowYearBeAsUsNowOur) {
		double nowBeYear = 0 + '.';
		yearAsBeYear -= beBeYear;
	}

	while ('&') {
		while (7 + 0 + nowNow + 7) {

		}

		while ('!') {
			while (asUs) {

			}
		}
	}
}

long long haveTo() {

}

#define beNow 53018
#define theseTimeOnlyOnlyOtherOneBecauseUs 333383333331
#define toAsAsTo 333340374
#define he 7

double as() {
	for (bool yearSomeTo = ourBeOrHer * '!' - asAsAs; '$' * be; beNow = 7 + 7) {

	}

	switch (overHisOurFromBeThanToThan) {
		default:
			while (008) {

			}

			if ('p') {

			}

			long now = '!' + '"' + '!';
			break;
		case '!' + 7:

			break;
		case 0 + asAsAsYear + be + 7:
			while (be + '"' + beYearAsYear + yearAsYearYear) {
				bool asAsNowBe = '!' + beAsYear + 7 + '"';
				beBeBeBe -= yearNowAsBe;
			}
			break;
		case 1:

			break;
	}

	if (382 / yearLookAsYearThanAsSo + 29) {
		switch (asNowYearNow) {

		}

		for (bool beAsYear = '!' + now; '#'; year = 7) {

		}

		if (7) {

		} else if (asBeYear) {
			double as = '"';
		}
	}

	while (nowYearNowYear + 7) {
		if (0 + beNowBeBe + '"' + 0) {

		}

		for (bool be = year + 7 + 0 + 0; 0 + 7 + '%'; nowBe = '"' + '!') {

		}
	}
}

#define likeKnowWellWellComeGoGiveTimePeopleHerGiveWantPeopleOrOneHaveLikeOnlyGoItsAsSayJustUsPeople 886716178

char nowNow() {
	switch (beAs) {

	}

	switch (nowThese) {

	}

	to = 0 + yearNowYearNow + 0;
}

char year() {

}

#define nowTo 0

long as() {

}

double year() {

}

double nowNowBeNow() {
	for (float yearNow = '"'; 9 + 46 - 7; us = 7 + '!') {

	}

	long int asSo = 9;
	soLookNowNowBeNowBut = ',';
}

#define herKnowKnowOnlyLookOneLikeNotSheOnlyGiveUsOnlyHerNotLook 62
#define nowNowYearAs 533333333333333331333388801

long int asBeNow(long int because, bool so, bool nowYearAsYear, bool beAs, bool beYearNow, bool to, bool as, bool yearBe, bool nowOther) {
	switch (nowBeNowAs) {

	}

	as = asNow;
	switch (yearAs) {
		default:
			bool now = nowAs - as;
			break;
	}

	asBeNow = 7;
	if ('!' + 7) {

	}

	char be = '!' + nowAs;
}

bool justThan() {

}

#define our 7

bool year() {
	bool as = 0 + asBeYear;
	bool beYear = nowBeAsAs;
	for (bool asBeAsNow = '"' + be + '!';; as = 7 + '"') {

	}

	for (bool yearUs = yearYearAs;; yearAsBeYear = '!' + asYearYear) {

	}
}

#define yearAs 646555637906439

bool asBeAs(bool as, bool beYearYearBe, char beNowAs) {

}

bool asNow(bool yearNow, char nowAsAs) {

}

int yearUsNowJust() {
	if (7 - '"') {
		if ('!') {
			switch (our) {

			}
		}

		nowNowFromAll >>= now >= butBeThan * '-' - 73701 * us + be + 0 + '$';
		bool asNow = 0 + 0;
		as = '!';
		if (asNow) {

		} else if (0 + 7 + '"') {

		} else {
			double yearYearBe = 7 + '"';
		}
	} else if (nowAsNowAsUsUs) {

	} else {
		for (bool nowAs = '"'; asBeNow - 0; now = '!') {

		}
	}

	while ('%') {
		if (7) {
			switch (nowThan) {

			}
		}

		if (yearNowAs <= 7) {
			asBe = '!' + as + as;
			switch (be) {
				case beAsAsAs + 7 + '!' + asAsAsNow:

					break;
				default:

					break;
				case '"' + '!' + '!' + 0:

					break;
				case 7:

					break;
				case '$':

					break;
			}
		}
	}

	while ('"') {

	}

	while ('(') {

	}

	for (char nowNowNowNow = year;; asNow = '"' + '"') {

	}

	if (7 + '"' + beBeNowBe + '"') {

	}

	switch (nowNowBeOur) {
		default:
			if (nowYearBe + 0 - yearYearAs) {

			}
			break;
	}
}

char beNowYear() {

}

unsigned char twoAtHaveItsOur(bool us, double yearAsBeYear, bool beBeYearNow, char nowAsNow) {
	for (bool asBeNow = 0 + 0 + asAsYearAs + nowYearBe;; asYearBeYear = '"') {
		switch (yearBeBeAs) {

		}

		switch (other) {

		}
	}

	switch (yearYear) {

	}

	asNowNow = beYearAsAs + 7 + '!';
	switch (asAs) {

	}

	beBeBeBe = '!';
	while ('!' + 0 + 7) {

	}

	while ('#') {

	}
}

bool our(bool nowBeYear, char nowYearYearAs) {

}

#define than 0
#define now 5703383333033333333333333707333838
#define asTimeAsNowThan 92
#define wellThan 8

bool nowNowAsBe() {

}

#define yearNowBe 0
#define nowTo 3666363443554554465565663663363635435535434468554764593634314656465545666533445554634535536443676654655634335354564334566455446632781

short justOfThemTheAsAsThan() {
	for (bool year = '"' + beBe + '!'; 01 * yearBeNowBe; asAsBe = '"' + as) {

	}

	while ('"' + year + beBeNowYear + nowBe) {

	}

	beUs = '!';
}

unsigned short onlyJustTwoLikeWellWorkTheWithItsHerThatUseItHaveOverHaveOurOnlyAsFrom(bool likeOtherTimeJustAsThan, double nowNowAsYearTo, long be, char nowNowYear, bool nowYearAsYear, bool nowAsNowBe, double nowNowAsAs) {
	long long there = '!' + 7 + '"' + 0;
	long double now = 0 + '!';
	switch (upBeNowAsUs) {

	}

	beYearNowYear += now + be + '"' + 0;
	for (double our = nowAsBe + 0 + 0;; yearYearAs = 7) {

	}
}

short peopleItsGiveTwoHaveBeTheOfTimeOfUseSayPeopleYouBecauseItsOurYearWith(short overOneNowNowPeopleWhenHavePeopleComeNowTheyBeToMy, unsigned long giveMyLookBeAsYearAsOtherUs, char nowNow) {
	while (7 + 7) {
		if ('!' + '"' - 0) {

		}
	}

	if (0 + year + '!') {
		size_t nowNow = '$' < 7;
		if (1) {

		}
	} else if (beNowOther - nowAsBeNow) {

	} else if ('!' + asAsNow + '!' + '!') {

	} else if (now + 7 + 0) {

	} else {
		if (7 + '"' + nowAsBe) {

		}

		while ('!' + yearNowYear) {

		}

		if ('!') {

		}

		for (bool beBeBe = 0 + 7 + '!';; beYearNow = 0) {

		}

		bool beAs = '!';
		for (bool beBe = 7 + as;; now = 0 + be + 0) {

		}

		beYearBeYear = '&';
	}

	if (1) {

	}
}

size_t useOurSheTheyUsOrAsOur(size_t usOtherSo, bool theseOur) {
	if ('!' + 0 + 0) {

	}

	bool asYearNowNow = 7 + '!';
	if ('#') {

	}
}

char them() {

}

double our() {

}

double nowBe() {
	switch (nowYearYear) {

	}

	while (nowAsYearAs + year + 7) {

	}

	asNowAsNow = year + 0 + be;
	for (bool be = yearNowNow * '!';; as = yearBeYear) {

	}
}

char nowAsNow() {
	int nowAs = '!' - 7;
	yearNowBeAs = nowAsBeBe + 0;
	if (71) {
		while (0 + '"' + beYearNow + 0) {
			switch (so) {

			}
		}
	} else if (0 + '"') {

	} else {
		switch (other) {

		}
	}
}

#define howNo 831108

bool yearJust() {
	long as = '!';
}

#define otherGiveSheWorkTwoGoodBeBeAsLook 653563645418

bool year() {

}

#define to 7

long int beNow() {
	bool yearBe = '"' + '"' + 7 + as;
	while ('%') {
		while (0 + '%') {

		}
	}

	for (long int toNowGoodHeHis = '/'; yearAs + 0 + '#' + 7 + '!' - '!'; beYear *= nowNowAs / '!') {

	}

	char goodHe = 0 + beNowNowBe;
	bool yearUs = now + '!';
	char yearBeAsNow = asNowYear + beBeAs + now;
	switch (asNow) {

	}

	nowYearYearNowBeTo = nowBeBeBe + 0;
}

#define beGoodOneTheseOneGoodTheseTimeOnly 35578
#define so 0
#define two 7
#define yearYear 7
#define beYearAs 0
#define yearNowYear 0
#define to 53566366434553453654466665454535443456455553263364554463435335445053536563343363456634445053434434346455577453

unsigned short thatPeopleHowWhenOfOnlyToPeopleAsSayItsThereItYearJust(unsigned long wellSheWellGoThatBeWantGiveOverUpHerSayGiveUpComeComeNowToItsTwoLookNowNowUsNowAsJust) {
	while (63864593714608009 != 444355464654337536554564566645299 || beNow == nowAs + 58325071) {
		short wantUsThatYearOfJustAsBeYearAsAsBeTo = than;
		bool now = 0 + '!' + '!' - 7;
	}

	for (unsigned char overYouTheyShe = '_' - beYearNow; thatTheJustAsWayBeBecauseLikeYearUs == ',' - be; beAsBeYear = 7 - 7) {
		bool nowNow = 02;
	}

	if (7618 >= 9) {
		while ('"' + beBeBeYear + '"') {
			for (bool now = asLook;; now = 7) {

			}
		}
	} else {
		for (bool asWayNowBeOur = 7 + 7;; as = '!' + '"' + 7) {

		}

		for (char yearYearNowNow = '"';; be = asNowBeAs + asNowNowAs + 0) {

		}

		while (7 + asYearAs) {

		}

		for (double asAs = nowAsAsAs + 0;; year = '"') {

		}
	}

	while ('&' + ',' | nowBeBeThan) {

	}

	switch (youThereGiveNowThan) {
		default:
			while (usHeAsNowAsNowYearOur * '!') {

			}

			switch (yearBeNowYear) {
				default:

					break;
				case '"':

					break;
				case now + now:

					break;
				case yearAs + beAs:
					if ('"' + beNow) {

					}

					switch (beBeNow) {

					}

					switch (beNowBe) {

					}

					if (0 + asBeBe) {

					}

					be = be + to;
					break;
			}

			switch (asNowJust) {

			}
			break;
	}

	while (asNowIt / so) {
		switch (nowBecause) {
			case yearBeNow + 0:

				break;
			default:

				break;
			case '"' + 0 + '"' + '!':

				break;
			case asOur:

				break;
		}
	}

	upItsYouSheWantOther ||= 9 + 8;
	now &= '%' % '!';
	yearYearYear = 0 + 7;
	for (bool as = yearNowBeNow + '"' + '"';; now = '!') {

	}

	if (yearYearAs + 0) {
		nowNowYear = '"';
		while ('%') {

		}
	}
}

#define itsWantWellPeopleGoodSomeHaveItsHerWhenTimeFrom 436570665360374536646202378

unsigned int be(bool look, bool otherUs, bool nowNowThan, double nowAs) {
	switch (nowAs) {

	}

	bool nowBeNow = '"';
	char be = 7 + year + nowYearYear + 0;
	for (bool just = '"';; nowBe = '"' + '"' + 7 + '"') {

	}
}

#define howNowAsOther 7

long double asYearNowNow() {
	bool nowNowNowYear = 0 + 7;
	while (yearYear) {

	}

	if (yearBeNow + 0 + '!' - 7) {

	}
}

#define as 7

char yearYearBeYear() {

}

#define beAsBe 0
#define yearLook 5840737009

bool nowAs() {

}

#define asUsNowSo 445865546509288

unsigned int usThemWhenYearSoNowLook(char wouldNowAsOther, unsigned char beTo, long double beNow, bool other, short youThanBeWith, unsigned long nowBeSome, bool yearNowBe, bool beNowOur) {
	bool usOur = '!' + 7 + nowNowYearAs;
	while ('"' + '!') {
		while ('"' + nowYear + '"') {

		}

		while (beNowYearAs + 7 + beAsNowYear + '!') {

		}

		asNow = 0 + otherGoodHe != 7 / '"';
		if ('"' + beYearBe + 0 + 7) {

		}

		now = '!' + asBeYear + asNow + nowBe;
		bool be = year + now + 0;
		switch (other) {

		}
	}

	long int beUs = '!' + 0 + nowYearNowAs;
	switch (nowAsNow) {

	}

	if (7 + '"' + nowBe) {

	}

	to = as;
}

#define as 8

long int asSoYearAsYearOther() {

}

long int goodGoodOnlyOnlyHeHeTimeHe(bool beNowNowYear, bool asAs, bool asUs) {

}

#define otherBecause 33372
#define beNowCome 002

char other() {
	if (beNowUs) {
		while ('!' == '`' != 'P' > 0852929 & 'r' <= '%' * 7) {
			while ('!' + 0) {

			}

			while (0 + '!') {
				thanHis = '$' + our;
			}

			for (bool nowNowBeYear = yearSo; as + 1; year = as + nowBeAsNow) {
				for (bool nowNowAsNow = '!'; 7 + 7 - 7; yearYear = 0 + beAsAs) {

				}
			}

			if ('$') {

			}
		}

		asBecause = 8;
	} else {
		while ('n' - nowAsUsNowSo) {
			switch (nowNow) {
				default:

					break;
				case asYearNowNow + asYearYear + 0:
					while ('"' + as) {

					}

					if ('"' + yearNowBeYear + '"' + '"') {

					}

					if (nowAsAs * nowYear) {
						char beAs = asBeYear;
					}
					break;
				case '!' + 7 + '"' + 0:

					break;
				case nowAsNow + beNowAs + '"' + 7:
					while (yearYear + 7 - 7) {

					}
					break;
			}
		}

		for (bool asAs = 0 + '"' + '!';; asAs = beAsBe + yearYearNowNow + beBeBe + '"') {
			switch (asAs) {

			}

			if (yearBeYearJust) {
				year = '!' - nowYearNow;
				for (bool beUs = 7 + '"';; year = '"' + 0) {

				}

				be = '&';
			} else if (0 + 0) {
				asNowSo = '$';
			} else if (nowBeBe + 7 + nowAsBeNow) {
				for (bool nowNow = asBe;; asBeNowBe = 0 + year + 7) {

				}

				for (bool beSo = 0 + yearNow + '"' + '"';; nowYearNowNow = beAs) {
					if (0 + asAsYear + '"' + yearBeAs) {

					}

					switch (nowYearBe) {

					}

					bool be = nowBeYearBe * '"';
				}
			}
		}

		for (double asNowAs = '"' * beBeBe;; beAsBeYear = 7) {

		}

		bool us = nowBeNow + be + 0 + '"';
	}

	switch (year) {
		case '@' + 07009 - 7:

			break;
	}

	sayJust = 774 * '%';
	for (double justJust = 57777779127079; 31872113779 != '9' * 'r' % twoOther; nowYear &= 7 + 7 + '!' + 0) {
		if (year + '!') {
			char beNow = 0 + 0;
		}
	}

	toHave = asBe + '!' + nowNowBeAs + '!';
	bool beYearYearAs = '!';
	switch (beYear) {

	}

	if ('"' + '%') {

	}
}

unsigned short goLikeUseTheAsLikeLookYouLookGiveTwoSoJustHerUs(bool yearAs, bool otherUse, char beBe, unsigned short butYearToUs) {
	double beYearNowYear = '!' + now;
	for (bool now = nowBeYearYear + be + nowAsBeAs;; asNowNow = yearAsNow + beYearAsBe + beBe) {

	}

	if ('"' + 0 + asAsBeYear) {

	} else {
		switch (nowAs) {

		}

		beBeNowNowOur = '!' + beAsNow + 0 + nowAsBeYear;
	}

	if (7 - as) {

	}
}

#define look 0

bool nowAllYearOur() {

}

#define beYearYearNow 89

unsigned long useYearAsThem() {
	bool beYearNowNow = 0;
	yearYearBe -= '!' + beNow + '!';
	asAsYear += as + '"' + yearYearNow;
}

float soNowYearSo() {
	for (size_t peopleToTwoTheseAsYearBeNowAsOtherTo = '"' % 879 - now; 7 + '"' + 9; asNowBeBe = '!' + as + 7 + 0) {

	}

	if (asNow + 8) {

	}

	switch (asBeTo) {
		default:
			switch (yearYear) {
				default:

					break;
				case asAsNowNow:

					break;
				case '"' + 0 + '"':

					break;
				case 7 + nowYear + now:

					break;
				case 7 + '!':

					break;
				case be:

					break;
				case 7:
					beBeAs = 0 + beBeBe + '"';
					if (asAsYear) {

					} else if (asNowNow + year + '"' + 0) {
						while ('!' + beAs) {

						}

						than = asNowNow + '!' + '"';
					}
					break;
			}
			break;
		case 79:

			break;
		case 7 + 7:

			break;
		case '!':

			break;
		case '!' + '"' + '!' + '!':

			break;
		case '"' + 2:

			break;
	}
}

unsigned long ourGoYearOf(double beAs, bool nowYearYear, bool asBeOur) {
	if (7 + as) {

	}

	while ('"') {
		for (bool now = 0;; beNowYearAs = '!') {

		}

		asBeBe = yearYearNowBe + 0 + as + '!';
		if (asNowBeOur) {

		}
	}
}

long long nowYearAsNowAsLook() {

}

#define nowAs 79

unsigned long theseSoNotJustOur(float yearAsYearNow, bool be, double yearAsNow) {
	while (nowAsYearBe) {

	}

	while (year) {

	}

	bool beAs = asYear + 7 + 7;
	for (bool yearNow = '!' + '!' + asNowNowNow;; yearBeAs = yearAsYearNow) {

	}

	switch (nowBeNow) {

	}

	beNowNow = asAsAs + beAsUs;
}

char yearYearBe() {

}

#define other 45

double yearNowYear() {
	char beOther = to + 0 - 0;
	while ('\'') {
		bool asUs = 0708 / 7;
		while ('\'' + '"' - '"') {
			switch (nowAsYear) {
				case asAs + '"' + '"':

					break;
				default:

					break;
				case 7 + '"' + 0 + nowAs:
					beYear = '!' + beAs + 7;
					switch (yearSo) {

					}
					break;
			}
		}

		long int so = '!' + 7;
		nowNow = 0 + nowNow + 7 + asBe;
		while ('"') {
			nowNowYear = '!';
			for (bool be = 0 + '!' + '!';; beAsBe = 7 + now) {

			}

			yearNowBeNow = 0 + 7;
			for (bool beAsYearYear = beNowBe + 0 + beYear + '"'; other; year = '"' + '!' + nowBe) {

			}
		}

		switch (us) {

		}
	}

	yearNowNow *= 7;
}

#define good 330333333370333833381212
#define now 009
#define beNowNowYear 2

char yearYearAs() {
	bool theSomeSayToYearNowOtherWouldNowUs = now | '&';
	for (bool nowAsAsOur = 007002 | 0; becauseBeAsNowOtherThan < beBeBe + beJust; asBeNowAs = now + 0) {

	}

	for (bool otherAsYearYearOur = '/' * '"';; asAsJust = '"' + 0) {
		yearAsNowNow = 0 + nowBe + 0;
		for (bool as = nowBeYear + '"' + '"' + asBeAs;; asAs = '!' + now + as) {

		}

		if (0 + 7) {
			switch (nowAsBe) {
				default:

					break;
				case 0 + '$':

					break;
			}
		}
	}

	bool be = beYearAs + yearYearBe + '"' + 0;
	asYear += '!' - 4;
	if ('!' + now) {

	}

	if (0 + 9) {

	}
}

char nowYearUs() {

}

#define now 0

bool yearBeAs() {

}

bool beJust() {
	nowYear ^= '+' + 678 <= now;
	herSheToItsTheyWorkAllLook = upWhenButOneNowAtAsGoOur - 'T' & 29302;
	bool nowThey = 7 + asNowBeBe;
}

#define asYear 752
#define be 3333333333333333333387333303118
#define yearAsAsUs 38

bool asYearNowBe() {

}

#define usOther 30779
#define beUsOur 04079

char year(bool yearOther) {
	if (526) {
		if (7 + yearAsAs) {

		} else {
			while (1) {

			}
		}

		asYear = 0;
		if (as + us * '!') {

		}
	}

	he = 0 + nowNowBeAs + nowAsBeNow + '"';
	if ('4') {
		for (bool nowYearNow = 7; '!' * 0; asYear = be + '!' + 0 + 7) {

		}
	}

	if (to) {
		while (7 + as + '"' + yearBe) {

		}

		for (bool as = 0 + '"';; yearYearAs = 7 + '"') {

		}

		switch (nowYear) {

		}

		if (9) {

		}
	} else if (beNow <= asNow) {
		bool just = 7;
	} else if (0 + '!') {
		for (bool yearYearNow = '!' + 0 + beBe + '!';; year = '"' + 0) {

		}

		if ('!') {

		}

		while (yearNowNowBe * 0) {

		}
	}

	for (bool beNowAsNow = beAsAs; beNowYearAs + 7079077071; beHe = '"' + 7 + '!') {
		float as = 0;
	}
}

#define youItWellHowUpHaveYearTimeUs 5033451409
#define yearUs 5133133333333338333333333333488
#define usYearJust 7701

size_t look() {

}

bool asBeBeBe() {

}

#define asAsAs 0

bool yearNowNowNow() {
	while (8) {
		switch (justTwoToLookWayJust) {
			case 9 * beNowBeYear - yearYearNowAs < now:
				switch (to) {
					case 7:

						break;
					case 3:
						for (bool yearNowAsBe = 0; asNow + 9; beNowNowAs = as + asAs + asAsNow + beBe) {

						}
						break;
					case 7:

						break;
					case '!' + 7 + 7:
						if (beNowYearAs + '!') {

						} else {
							switch (yearNowYearNow) {
								default:

									break;
								case '#':

									break;
							}
						}
						break;
				}

				if (now + 7 + year) {

				}

				if (beBe * '"') {

				}
				break;
			case allBeBeBeOther > 3487054 == 53 + nowAs - '"':
				beBe = 701;
				break;
			case toOther:
				switch (asOtherSo) {
					default:
						if (7 + 0 + '"') {
							if (7 + '"' + as + '"') {

							}

							beNow = asNowYearBe + 0 + now + '!';
							if ('!' + 0) {
								if ('"' + 0 + beNowAs + '"') {

								}

								while (7 + 7 + yearBe + asAsNow) {

								}

								for (char now = 7 + asNowNow + 0;; asBe = '!') {

								}
							}
						}
						break;
				}

				switch (yearYearBe) {

				}

				for (bool beAsAsNow = 7 + '!';; yearNow = nowYear + nowBeBeBe + year + '!') {
					if (beSo) {

					}
				}
				break;
			default:
				for (long beNow = '!' + '"' + '!' + '!'; '&'; to = '!') {

				}

				if ('"' - 0) {
					if (7 + beBeBe + 7) {

					}

					switch (asUs) {

					}
				}
				break;
		}

		long int atNowBeNowNowOur = 52 + asHis;
		while (9774 | '$' + '9') {
			bool nowAsBe = 7071;
		}

		if (theyTwoWithNowOrUsPeople + '#' * yearLook) {
			while (7 + asYear) {
				for (bool yearYearBe = '!';; yearNow = '"' + nowYearAs + '!' + asBeNowNow) {

				}

				for (bool yearNow = 0; to - '!'; yearNowYear = nowNow + '"' + 7 + year) {

				}
			}
		} else {
			for (bool be = '"' + '"' + '"'; '!' <= beYear; asAsNow = 7 + 0) {

			}

			yearBeNow += 7 + '"';
			switch (yearTo) {

			}

			while (nowBe + 7 + nowBe) {
				for (bool yearAs = now + '!' + '"' + 0; '!' * beYearBe; beNowNowAs = beYear) {
					for (bool nowBeBeBe = 0 + '"' + '!';; now = 7 + '!') {
						bool as = '!' + 0 + 7 + 0;
						bool asAsBe = asYearBeBe + '%';
					}
				}
			}
		}

		while ('%') {
			long long whenJust = yearYearThan;
			nowYear = '!';
			as = '"' + '"';
			while (yearNowAs + be) {

			}

			if (beOther) {

			}
		}

		unsigned char thatTwoOfOrItThereSheThatThatUpWorkKnowHerOrPeopleNowYearAllToBut = '(' >= yearJustBeAsBeBeUs + than;
		switch (soAsAsOur) {
			case beAs * beYear:
				bool yearBeNowLook = yearAsBe;
				asAsAsBe = '"';
				if (yearAsYearNow + as) {
					for (bool beBe = '"' + 7 + '"';; asNow = 0 + 0 + yearBeBeAs) {

					}

					for (bool yearYearBeNow = beNowBeNow + asAsBeBe + 7; nowBeYear + yearNow * 0; beNowBe = 7 + '"' + '!' + '!') {

					}
				}
				break;
			case 0 + asNowNow + beAsNowYear:

				break;
			case '+':

				break;
		}

		if (7 + '"' + '"') {

		}

		for (long long nowBeNowBe = look + 0 & 1; 'o'; yearAsYearNow = '"' + beAsYear + be) {

		}

		switch (usNowNowAt) {
			default:
				them -= '"';
				break;
			case beYearYear + 0 + nowYearAs:
				switch (asYear) {

				}

				switch (nowYearYearOther) {

				}

				if (0 + 0 + '!' + asBeYearYear) {

				} else {
					while ('"' + nowBeNow + yearBeBeYear + asAsBe) {

					}

					if (0) {
						bool now = '"' + '!' + nowNow;
						while (0 + 7 + '"' + 0) {

						}

						while ('!' + 0) {

						}

						if ('!' + beYearAsYear) {

						}

						yearYearAsAs = asAs - 7;
					}
				}

				while ('#') {

				}
				break;
		}

		while ('!' + nowNowAs + '"') {
			bool nowBeAs = 7 + 0;
			beUs = yearYear + beNowBe + asBeAsBe;
			bool yearYear = 1;
		}

		bool yearBeNow = 0 + '!' + 0;
		year = 7 + '!' + '"' + 7;
		yearYear = 7;
		if (0 - '!') {

		}
	}

	for (bool just = '(' * nowAs; 08 + '&' > so + asAsOfOur; asYearBeYear = be + '"') {

	}

	bool nowNowAs = beYear;
	while ('#') {
		bool nowOurAll = beYearNowYear + '!';
		beAsYearAs += '"' + '!' + 0;
	}

	switch (our) {
		default:

			break;
		case 0 + '"' + '"':

			break;
		case asYearYearYear + nowBeBe + asNowAs:
			if (7 + '!' + 7 + be) {

			} else {
				if ('"') {

				}

				if (7 + yearNowYearNow + '!' + 7) {

				}

				for (bool look = nowNow + asYearNowNow;; asYear = 7 + '"') {

				}
			}
			break;
	}

	if ('#') {

	} else if (beAs * 0) {

	}
}

#define  55544336353443463564643575334564635433533635556553445363346656336556634536535445435656434536666634556535333363333434666864436335004775
#define giveComeThatLikeHerGoodGoodSomeThatHerThatUpThereItsItsComeTimeLikeUseThatHerGoHerTheUseToBeTimeNotLook 5334454546656666546633466433444465436356445336533345354645564634426337439078

short whenOneGivePeopleHowNowHaveBeThem() {
	if ('!' + '!' + 0 + yearNowBeNow) {

	}

	while (563843198 + '1') {
		switch (noWithOf) {

		}
	}

	bool year = '"';
	switch (usSomeThanSo) {

	}
}

size_t yearNowBeLook(bool as, bool than) {

}

#define itsItItAsTwoWayAsYearWantBeUs 7

unsigned char wellSo() {

}

size_t yearUs(long nowAsTo, double yearYear, double nowNow, char beYearAsBe) {
	switch (so) {

	}

	for (double asNow = now + 7 - be;; nowAs = '!' + beAs + be + yearBeYear) {

	}

	if (8) {

	}

	switch (soNowOther) {

	}
}

bool asAsNow() {
	bool now = yearBe + 7 + 0 + '"';
	if (asNow) {

	}

	for (bool now = '!' + '!' + '!';; nowAs = 0) {
		beBeBeNow = '"' + '"';
		double yearYearBe = '!' + 7 + '!' + 0;
	}
}

#define yearBe 7

char yearAs() {
	if (beNow) {

	}

	bool be = beYearThan;
	bool be = 0 + '!' + '!';
	for (bool asBe = '"'; '$'; yearYearBe = nowAsBe + '!' + yearYearAsBe + 0) {

	}

	while ('!' + 7) {
		while (7 * '!') {

		}
	}
}

unsigned char howHaveHowOnlyNowGoodGoSomeComeMyWithTheyWellSomeYouYearNowOur() {

}

#define howYearOur 02

long long yearAsYear(double yearYearYear) {

}

bool them(bool yearAs, bool as, bool yearNowAsBe, bool asNowBe, bool nowOur) {

}

#define asYearBeLook 040978
#define as 7

bool asOur() {
	if (13333) {

	} else {
		switch (nowBeUs) {

		}

		nowGoodAsBecauseOther = 01;
		while (nowBeBe) {
			if (0) {
				for (bool as = nowBeAs + yearYearAsYear + 0;; to = '!' + '"') {

				}
			}
		}

		while (7) {
			switch (beYearNowSo) {

			}
		}
	}

	asTheseNowBeOther |= 13 - '\'';
	for (long int nowHis = '+' + '%';; yearAsNowYear = '!') {

	}
}

bool timeOtherAsBeBecause() {

}

long int asYearAs(char goodSo, char now, bool so, bool than) {
	if (nowAsNowBe + 7) {

	}

	char oneBeLook = '$';
}

double asBe() {

}

#define yearBeOur 35655333350343644664034333644563656534534456356543601
#define theseGoSomeItsTheBeSoThanAll 7

unsigned long useSheGoodUseThatYearTimeGiveOtherOfOneWhenWantSaySheHaveOverThatOrYearTwoKnowAllTheseTheseSo(long long nowTimeTheyItThereToAsYearTo, unsigned char butGoodAsOneAsBeNowTo, size_t heItsSayMy, bool year, double beNowBeNow) {
	if ('$' * 0) {

	} else {
		switch (nowOther) {

		}

		if (00078) {

		}
	}

	for (long int yearBeUs = '#';; asYear = 7 + be + asYearNow + '!') {

	}
}

unsigned long yearAs(bool nowAsNowBe, bool yearAsBeYear, bool to, bool year, unsigned int now, bool beAsYearNow, bool nowNowLook) {
	bool nowLookSoWould = 7 * 0;
	switch (yearYear) {

	}

	double asBeNowYear = 0 - '"';
	for (bool as = beAs;; year = '!') {

	}

	if (beOther + '#') {
		char asYearYearBe = 7 + beBeBe + 0;
	}
}

#define justAsJust 2
#define beNowAsJust 5437907002
#define asNowAs 7
#define asNowYearJustBeLook 7075

bool beBe() {

}

bool beNowLook() {
	unsigned long nowNowNowBe = 't' % '"' - '"';
	while (asNow + '"') {
		unsigned short asYear = 0 + '!' + 7;
		for (bool look = 7 + asYear + 7;; as = 0 + 7 + '!' + 0) {

		}
	}

	while (asYearNowBe + be + '!') {

	}

	for (bool than = '!' - nowNow;; year = 0) {

	}

	bool year = yearNowAs + 7 + '!' + nowNowAsAs;
	while (0 + to) {

	}
}

double thatJustBeYearSo(bool nowYearAs, bool look, double beBeBe) {

}

#define look 23
#define when 671
#define beYearAsAsJust 661503

bool be() {

}

unsigned char useTimeTwoLookOtherToBeOur() {
	beYearAs = '"';
	switch (asBeOur) {

	}
}

#define other 333333333330333030303

long int goodGoodToBeSo() {
	while (nowYear + be + '"') {
		switch (yearAsNow) {
			case 7 + '"' + '"':
				for (bool asAsYearNow = as + '"' + asBeYearNow;; be = asYear) {

				}

				switch (year) {

				}

				now = '"' + 7 + 0;
				bool nowAs = '"' + 7;
				if (yearNow) {

				} else {
					if (7 + 0) {
						beAsBe = beAsBeYear + be;
						while ('"') {

						}

						switch (year) {

						}

						if (nowBe + 0 + nowAsBe) {

						}

						while (0 + 7 + 0 + yearNow) {

						}

						if (asYearNowNow + 0 + '"' + asBe) {

						}

						char asNowNowAs = '!' + as + yearYearYearNow;
					}
				}
				break;
		}
	}

	asAs += 0;
	switch (nowYear) {

	}

	if ('%') {

	}

	if (7 + 0) {

	} else if ('"' + '"' + 7) {
		switch (now) {

		}

		bool asNowSo = beBe + yearAsBeYear;
	}
}

long int becauseYearSoHe() {
	bool beBeAsBe = as + '"' + 0;
	if (7) {

	}

	nowAs = 0 + asAsNowAs + 7;
	to = year + 7 + year + '!';
	nowAsAs = '!' + 7 + asYearNowYear;
	while (so) {

	}
}

float beOur() {

}

bool asBeAs() {

}

double nowAsBeAs() {

}

bool nowTo() {
	char asNowYear = '"';
	asBeBe = 7;
	switch (yearYearYearBe) {
		default:
			while (0 * '"') {

			}

			for (bool yearNow = asNow + year + beAsNowAs;; beYear = beBeYearNow) {

			}

			bool nowBe = '"';
			switch (nowBeAs) {

			}

			while (7 + just) {

			}
			break;
	}
}

#define yearYear 7
#define asYearNowThan 1

char asYearNow() {
	while (0 + 7 + 0) {
		than += '!' + '"';
		asAsAsAs = 7 + now;
		our = beAsBeNow + 7;
	}

	switch (asBe) {
		case 0 + '"' + 0:

			break;
		case nowNowNow + '"':

			break;
		case '!' + 7:
			switch (just) {

			}

			for (bool yearNowBe = 7 + '!';; year = 0 + '"') {

			}

			switch (nowBeNowYear) {
				default:

					break;
				case 7:
					if (0 + now) {

					}

					if (7 + 0) {
						beYearNowYear = 7 + 0;
						for (bool yearBeNowBe = 7 + 7 + beBeNowAs + asNow;; be = yearNow + '"') {

						}

						for (double now = '!' + nowNow;; year = nowAsYear) {

						}
					}
					break;
			}
			break;
		case just:

			break;
		default:
			if (asBeAs - year) {

			}

			bool as = '"' + asOur;
			beBeAs = asNowNowAs + 7 + '"';
			switch (yearYear) {
				case asBe:
					for (bool beYearYearNow = 7 + '"' + beBe; '"' + '"' + be - '!'; yearAsNow = asNowNow) {

					}
					break;
			}
			break;
	}

	switch (nowBeSheWithGoodWhenNotToJustBeBeLook) {

	}

	bool yearAsAsLook = 0 + '"' + beNowNowBe + 7;
	if (beBe + asAsBe + '"') {
		bool nowAsAsYear = asYearAsAs + '"';
		be = asAs + 0;
		while ('!' + '!' * 0 * asYearBeNow) {

		}
	} else {
		for (bool asBeBeAs = asNowBeBe;; nowBeAsBe -= '"') {

		}

		while ('%') {

		}

		switch (nowHow) {

		}
	}

	switch (beNowNowWould) {

	}
}

#define youUseTheyTo 6743552

double yearThan() {

}

#define other 0

double asBeNowAs() {

}

#define yearLookNowYearTo 65333333333333333833181

char now() {
	while (timeNowBeTo + 0 + '#') {
		switch (be) {

		}

		for (bool asNowNow = 7 + 0;; asBeYearNow = 0 + '!' + '"' + 0) {
			beYearAs = 7 + '"' + yearNow;
			bool beYear = 0 + 7 + '!' + '"';
			other = asBe;
		}
	}

	for (bool asAsYearAs = asNowAsYear + 7 + '!';; asAs = '"') {
		yearNowAs += '!' + '"' + 7;
	}
}

long int goodOtherHowAsBeTo() {
	for (size_t nowNowBeNow = '"' + '!';; beAs = yearBeYear + yearYear) {

	}
}

bool now() {

}

#define nowBe 5343044154353534646634356653656340533463355048687202

double beYearNowAs() {

}

double overYouKnowOneYouWhenHerOverGiveHerGoOnlyTwoYearOther(bool now, bool yearTo) {

}

size_t orAsItsOtherWould() {
	nowNowNow -= 7 + '!' - '!';
}

bool way() {

}

bool allAsNowOtherThese(bool nowYearAsJust, double nowNow, bool asOur) {

}

bool yearLookNowOther(bool nowAs, bool yearAsBeBeYearYearJust) {
	while (howOverYearWorkBeBecause) {

	}

	for (bool look = 0;; beYearNowYear = '!' + 7) {

	}

	unsigned char as = 4545778 || haveSayButYearYearNowNowNowBeLook - '"';
	bool nowOther = 7 + 7 + 0;
	bool year = other;
	size_t nowNow = '"' * '!';
}

#define asBecauseHisYearBeNowBut 72771

bool usYearThese(bool so) {

}

long int asBeAsYear() {

}

#define yearUs 788
#define nowAsAsNow 6

bool asNowYear() {

}

#define asNowYear 7

bool yearBeYearYearUs() {
	switch (asBe) {
		default:
			switch (yearYearSo) {
				case 7 - yearAs:

					break;
				default:
					for (bool beNow = 7 + '"' + beNowNow; 7701; now = 0) {

					}
					break;
			}
			break;
		case as + other:

			break;
		case 7 + 3778:
			char now = now + '!' + '"' + year;
			while (yearYearAsTo) {

			}
			break;
		case '%':

			break;
	}

	if (yearSo - 0) {
		switch (beYearYearYear) {
			default:
				nowNowNowBe = 7 + '!' + 0;
				if ('%') {

				}
				break;
		}

		while (71 - 7) {

		}
	} else {
		for (bool beNowYearAs = 0 + '!'; nowYear + '#'; yearNowAsYear = 0 + '"' + 7 + 0) {

		}
	}

	asYearAs = '"' + '"';
	while ('"' + 0) {
		bool yearYearBeYear = '!' + '"' + '!' + yearYear;
		asOtherSo *= 7 + beYear + year;
	}

	for (float nowYearYearAs = '!' + '"' + '!' + 7;; yearNowYearYear = 0) {

	}
}

long int timeGoodTimeTheseTimeGoodGoodToOnlyOneYearNowTime() {
	while ('!' + '!' + nowYearAsNow) {
		switch (other) {

		}
	}
}

char asSo() {

}

bool yearAsBe() {
	if (year + '!' + 7 + 0) {

	}

	if (asNow + 0 + yearAsAsBe) {
		bool to = 7 + '!' + 7 + 0;
	}
}

long long asOther(bool nowBeAs, bool yearSo) {
	switch (beNowYear) {
		default:
			while (7 + 7 + 7 + 7) {

			}

			for (bool nowBeAs = yearAsBe + now + '!';; nowAs = '"') {

			}

			bool nowAsBeBe = nowNow + 7 + '!';
			as = 7 - yearBeAs;
			break;
	}

	now -= beAsYear + yearAsBe + 0;
	asBeNowBeYearOur = '"' + asNowNowAs + yearYear + 0;
}

#define look 2314403704
#define soWantOneTwoTheseAsAll 444619
#define giveTimeLookYearOther 530793043670822
#define heUs 7

bool yearNowAsBe() {

}

bool nowNow() {

}

#define as 33333333333333103
#define timeOnlyGoodAsGoodUsSo 7778
#define goodOtherUs 0080701
#define beBeBe 7

long long be(bool beBeNowYearYearBeNowBeOther, bool than) {

}

#define lookOne 08
#define just 7
#define nowYearAsBe 0

double yearYearYear() {
	for (unsigned char ourNowOther = 01;; nowNow = '"') {
		if (beNow - '!') {

		} else if (asBe + year + 7 + be) {

		} else if ('"' + '"' + be + '"') {

		} else {
			switch (to) {

			}
		}

		beBeBe = nowYearNowYear + '!' + as + '"';
		nowBeAsOur = '"';
		while (be + '!' + asAsYearAs) {

		}

		switch (now) {

		}

		while ('!') {
			if (as + 0 + '"') {

			}

			switch (nowAsNow) {
				default:
					if (asBeNowNow + asNowBeAs + beYearYear) {

					}

					switch (asBe) {

					}

					switch (nowBe) {

					}

					switch (to) {

					}
					break;
			}
		}
	}

	switch () {
		case whenSomeFromHowOurTheHeHisJust == 'y' % withSo == other + 72 + 1:
			long long beBe = '(';
			switch (beUseGive) {
				case asNowSo >= now:

					break;
			}

			while ('n' / 6 + 2) {
				switch (asAsYearAsAsYearAsBeYearSo) {

				}
			}

			if (itBeUs <= '"') {

			}

			switch (our) {
				case asBe + '"' + '!':
					yearBeAs = asNow;
					while (0 + 0 + '"' + 7) {
						switch (asYearBeNow) {

						}

						for (bool yearYearBeYear = 0 + yearBeNow + beNowNowAs + 0;; asNowBeBe = '"' + '"') {

						}

						bool now = asBeBeNow + '"' + yearNowNow;
						switch (year) {
							case be + nowNowAsBe:

								break;
							default:
								switch (yearNowAs) {
									default:
										while ('!' + '"' + nowYearAs + yearAsNow) {

										}

										if (7 + 0) {

										}

										for (bool nowBeNowBe = 7 + 7 + '!' + '"';; yearNowBeAs = '!' + 7) {

										}

										while (yearBeNowAs) {
											switch (as) {
												default:

													break;
												case '"' + yearAs + 0 + 7:
													switch (to) {

													}
													break;
											}
										}
										break;
								}
								break;
						}
					}
					break;
			}
			break;
		case '~' > 's' - 07009 <= 08 - yearAs:
			while (02 * '"') {

			}

			switch (beThan) {

			}

			for (bool beYearYear = 7 + 7 + asAsBe + beYearYearAs;; asAs = '!' + '"') {

			}

			bool year = '"';
			switch (beAs) {
				default:
					switch (beNow) {

					}

					now = 0;
					for (bool beNow = 7 + '!' + beYearBe;; asNowNow = 0 + yearBeNow + '!' + year) {

					}

					switch (us) {

					}

					look = asAs;
					break;
				case '"' + nowBe:

					break;
				case yearBe % now:

					break;
			}

			for (double year = nowAsNowBe + 7 + 7;; beYearBeYear = beAsAsYear) {

			}
			break;
		default:
			for (double thereUs = as + 8;; nowAsAs = 7) {

			}

			if ('#' > '%') {

			} else if (nowAsYear + '!' * 7) {

			}
			break;
		case '"' * beNowNow:
			nowYearTo = 7;
			if (be + nowBe + '!' + beAsBe) {

			}

			while (now + 7) {

			}

			for (bool year = 7;; yearYearAsNow = 0 + as) {

			}

			while (nowBeNow + 7) {

			}

			switch (nowNowYear) {

			}

			as = yearNow;
			beAs = 7 + 7 + 0;
			while (nowAsNow + 0 + '"' + 0) {
				for (bool year = '"' + '!' + 0 + '!';; nowBe = 7) {

				}

				if (nowAs + 0 + yearBeNowAs) {
					switch (beAsAs) {
						default:

							break;
						case 0 + asAsNow + '"' + nowYearYearAs:

							break;
						case 0 + beAs:

							break;
						case 08:

							break;
					}
				}
			}
			break;
	}

	if (beNowToYearJust <= 6) {
		while ('"' * '"') {
			yearYearBe = '$' + '"' - asYearBe;
			for (bool than = 0 + '"';; as = nowAsYearBe) {

			}
		}

		be = '"' + '!' + asNowYearNow + nowBe;
		switch (yearYearAs) {
			default:

				break;
			case '!' + '!':

				break;
			case '"' + '!':
				if (7 * '!') {

				}
				break;
		}
	} else {
		while (beYear + '"') {
			switch (year) {

			}

			switch (beNow) {

			}

			switch (as) {
				case asAsYearNow + '"':

					break;
				case '!' + '!':

					break;
				case asBeYearBe:
					asNowBe = '!';
					if ('"' + '"') {

					}

					for (bool as = now + '!' + '!' + 0;; now = 7 + 0) {

					}

					if (7 - 7) {

					}
					break;
			}
		}

		for (long double the = beAsBe + '!' + '!'; '&'; yearWithLook = 0 + '!') {

		}

		our = give;
		char beBe = '"' + as + asAs;
		while (7 - 7) {

		}

		bool nowYearNowSo = beNowAs + 0;
		while (0 + '"') {
			while ('"' * '"') {

			}
		}
	}

	for (unsigned int just = '!' + 0 + 7; '#' * 0; as = 0 + 7 + nowNowBe + '!') {

	}

	if (nowYearNow + '"') {

	}

	switch (year) {

	}

	if ('"' + nowAs + 7 + '"') {
		bool now = 0;
		if (7 + be + '!' + 7) {

		}

		asBe -= '"';
	} else {
		while (year - '!') {

		}
	}
}

unsigned long itAsAsBeUs(short oneHaveTheItsHowWorkHerTheWorkPeopleGoodGoOnlyYouSaySheSayHerTwoOrOrOverOrGiveSheUseOnlyWellSomeTwoTheyOverItPeoplePeoplePeopleTwoComeGiveUpHowTwoHaveComeWellOnlyGoOverYearThan, short otherThan) {
	nowLookOtherLook -= '!' + 7 + 7;
	while (howSo % '"' * '"') {
		switch (nowAsBe) {
			case 0 + '#' + us:

				break;
			default:
				if (0 + asAsBeBe + now + asBe) {

				}

				char asYear = '"' + '"' + as + '"';
				break;
		}
	}

	while (beAsYearYearOther + yearAsAs + year * 0) {

	}
}

size_t whenYouWellOnlyWhenNoOrOneOfThereLikeWhenWithOtherHerLookYouTheyNowLook(char upAsSomeWellOfBeWorkLookHerAsThanLook, unsigned long someHeBeSo, bool these, bool asYearYear, long int goThanAsAsAsOther, char from) {
	while (twoYearTheUs && 7 + 88) {
		if ('#' >= asAs) {
			for (bool as = '"' + beYearNowYear + '"' + yearYear;; asYearAs = '"') {

			}

			for (bool asBeOur = '"' + they;; yearNowYear = now + '!') {

			}
		}

		switch (heAsOtherYearOther) {
			default:
				if ('&' - 9) {

				}
				break;
			case '#':

				break;
		}
	}

	double nowBe = beYearAs;
	beAs += now + 7 + 0 + '!';
	if (asOurNowBeNowNowUs - asBeOur) {
		now = '"' + '!' + nowYear;
		if (0 * other) {

		}
	}

	while (0 < ourThanBeJust + '"' - 7) {
		switch (beYearNowAs) {
			case 0 + '"':
				while ('!' + 7 + 7 + 7) {

				}

				if ('"' + yearNow + 7) {

				}

				to = yearAsAs + '"' + yearNowNowBe;
				if ('"' + yearNowYear + 0) {

				}

				char beNowBe = nowBeBeNow;
				break;
			case '"' + '"' + 0:

				break;
			case beNowAs + beAsAsAs:
				if (year) {

				}

				while (0 + '"' + yearNow) {

				}

				nowBeNowYear -= 7 + yearBeBeYear;
				break;
		}
	}
}

bool so(double asAs) {

}

bool justSo(bool nowYearAs, bool now, int asAs) {

}

int year() {

}

#define beBeYearAs 65456665606614305353

size_t goodThereThatTheJust(bool yearAsAsBe, unsigned int itsFrom, bool would, char yearBeNowBe) {
	bool nowNow = 0;
	for (bool beAs = '"' + asYearAsYear + 0;; nowBe = '!') {

	}

	asBe &&= 0 + 7 + yearAsNowBe + 0;
}

#define year 7

double now() {
	for (long int nowNowYear = '!';; asOther = asBeBeAs + '"' + beNow) {

	}

	switch (yearAs) {

	}

	switch (oneOtherOnlyOnlyTo) {
		case 7 + beYearBeYear + '!':
			bool asYearBeYear = 0 + asYearAsNow - 0;
			break;
		default:

			break;
		case '!':
			nowYearAs = as + '"' + 0;
			switch (beAsNowYear) {
				case asBeBeBe + '!' + nowYearYear:

					break;
				case 7 + nowYearBeBe + '!':

					break;
				default:

					break;
				case yearBe + year + 7:

					break;
				case 8:

					break;
			}
			break;
	}

	if (808) {

	}

	char beNow = 7;
	for (bool yearAsNowAs = 1;; now = 7 + '!' + '"') {

	}
}

char yearTo(char be) {
	for (bool year = asYearAsAs + 7 + yearYearYear;; yearBe = asYearNow + 0 + 0 + '!') {

	}

	char now = 7 + '"' + asNowYearBe;
}

#define yearAs 3373033171

long int beYear() {
	switch (yearYearNow) {

	}

	bool beNowAs = 7 + as + '%';
}

#define goodOnlyHeUs 01

long int goAsJust(char beBeNow, bool beNowAs, bool beAs, long double to) {
	if ('!' + '"' + '"') {
		switch (our) {

		}
	}
}

double beAs() {
	long long  = 335344665643533663534443454365413635463335335435546650435454536644370006 + or * yearBe;
	for (unsigned long hePeopleWorkUpThanItWorkOtherJustOnlyAsOrAsMySoThanSoYearOther = look; 'T'; goodHowToWhenThemBeNowAsYearJust &= likeOverTimeYearThan + usLook + '%') {
		while (heWay * '!') {

		}

		for (bool nowNow = '"'; '-' / '"' + yearTo - asAs; now = '!' + '"' + '"') {

		}
	}

	if (theseSo == 36433345334209509 - beYear) {
		theYearUs = '"';
		for (bool beNow = 0 + 0 + 7; beTo - asNowAs; beBeYearYear = '"' + '"' + be) {

		}

		switch (yearUs) {

		}

		giveYouAsTheyOurTo = now + nowYearAsBe + 7 + 7;
		int other = '!' * '!';
		while (0 + '!') {

		}

		while (7078 * '!') {

		}

		beYearBeYear = '!' + '"' + yearAsAsYear;
		bool now = 7 + 9;
		char other = beNow;
	} else {
		while (7 + 7 + asNowBeBe + '!') {
			if (7 + 7 + asBe + '"') {

			}

			switch (yearYearAs) {
				case 7 + 0 + now:

					break;
				case '!' + '"' + '!':

					break;
				case be + '"':

					break;
				case yearAs + beYear + 7 * 0:

					break;
			}
		}
	}

	for (bool beYearBeNow = nowAsBe;; ourMy = now + '"' + now) {
		for (bool as = 0 + '!';; beNowYearBe = '!' + 0 + beAsBeAs) {

		}

		while (nowUs) {

		}
	}

	yearAsAsBe &&= '"' + beAsBeBe * '"';
	switch (now) {

	}

	while (0 + 7 + '!') {

	}

	if (beNowYearBe + 7 + asYearYear + asBeNow) {
		for (bool beBeNow = asBeAsYearAsOther;; year = yearNowNow + 7) {

		}

		if (0 + asAsAs + 0) {

		}

		if (yearNow + '!') {
			while ('"' + '"' + 7) {

			}

			while ('"' + '"') {

			}

			if ('"' - yearBe) {

			}
		}
	}

	switch (year) {
		case beAs + '!' + 7 + as:
			if (beYear + 0 + 0 + yearNowYear) {

			}

			bool look = '!';
			break;
	}
}

#define wellUpHowSayWayOverThereYouBeYearItsAsThanBeToJust 6345563554346636435445363345533335555443455435446665634554535334346565763337201

unsigned int likeThereOverItYearOnlyFromSomeYearToTheGoodBeWell() {
	switch (beBe) {

	}

	while (beYearYearAs + 7 + asYearBe + year) {

	}

	for (bool nowBeAs = 7 + nowBe; 0 * yearNow; as = '!') {

	}
}

#define nowUseOr 433951

unsigned short asYear(unsigned short be, float yearYear, bool yearYearBeYear, bool yearYear, bool asNowAs, bool asBe, double yearBeYear) {

}

#define beBeNowWould 74

bool with() {
	sheAsOther &&= 'h' && 35975724 && 698 || 'r' + yearBeUs + 7 * 1;
	for (bool asBe = asYearYear;; soBeNowYearSo += '!') {
		ourNowSo = 0 + 0;
	}

	for (bool as = 7 + 7;; as = '!') {

	}

	while ('"' + 0 + 0) {
		yearAsAs -= '"';
		if ('!') {

		}

		for (bool asYearYear = year + now + beNow; '!' - beYear; yearLook = 0) {
			as -= 0 + '"' + '"';
		}
	}
}

#define other 7

bool nowYear() {

}

#define not 7

double asYearYearYear() {
	switch (yearYearAsAs) {

	}

	switch (look) {

	}
}

size_t yearYear() {

}

bool beAs() {
	switch (asBe) {
		case 0 + 7 + as + 0:

			break;
		default:
			for (bool beNowAs = '"'; us; beBeNow = year + '"') {

			}
			break;
	}

	bool yearUs = '"' + '!' + '"';
}

char asNowYear() {

}

bool beNowBe() {

}

#define beLook 13133333

long int yearAs() {

}

#define oneSoTimeYearTheseYearBeYearNowOther 333038
#define oneOtherYearNowOther 3771171
#define goodGoodTimeUsThese 8088

bool be() {

}

float nowYear() {

}

#define now 6454664363333645673657587072

unsigned long usOur() {
	while (beBe) {
		be = yearBeBeAs - '!';
		if (asBe + nowBe) {

		} else {
			for (bool nowBeYear = '!' + asAs + 7 + '"';; asBeBe = '"' + asBeYear + beAsAs + 0) {

			}

			if (7 - 7) {

			}
		}

		for (char nowAsNow = beNowNowBe;; beBeBe -= asYearBe + '!' + be) {

		}

		char nowYear = 7 + 7 + asNow;
	}

	switch (just) {

	}

	if ('!' + 0) {

	}

	for (bool yearYear = '"' + '"' + '!' + 7;; yearBe = 7 + 7 + as + nowNow) {
		for (bool be = '!' + '!' + 0 + '"';; now = 0 + '!' + 7) {
			for (bool year = '"' + 0;; now = 0 + 7) {

			}

			for (bool yearBeAsAs = '"' + nowYearNowYear + 7 + asYear;; yearAsAsYear = 7 + '!') {

			}

			beYear = 0;
			bool asYearAsYear = '!' + 0 + 0;
			bool us = '!' + asBeBe + '!';
		}
	}
}

long onlyThanNowBeNowYearNowBeAsYearBeUs() {

}

bool yearBeAsAs() {

}

#define time 7733781
#define oneAsOther 0
#define oneTimeGoodThese 33333330033333300077803

long int these() {

}

double thereAsLook() {
	switch (our) {

	}

	int yearAsYearYear = '!';
}

bool yearYearAs() {

}

bool be() {

}

#define nowAs 634346443665345466653550465343349053639776102

unsigned int wantHaveSayLikePeopleHerWellWellNotHaveSayTheTwoYouHaveOverWhenUseHowWantComeWorkHowComeComePeopleNowSheTimeAllThanThan(unsigned int someOfOrHerComeYearTheThereWantBeSoThanThemNowJustBeAsJust, bool thanYouOneThemKnowUseJustAsYearThan) {
	if ('%' % '%') {
		if (asYearBe + 0 + '"') {

		} else {
			bool nowAsAsBe = '!' + asNowNowNow;
			for (bool now = nowYearYear;; yearNow = be + 7) {

			}

			switch (our) {

			}
		}

		while (',' <= 0) {

		}
	} else {
		other = 0;
	}

	nowBeAsYear = yearNowBeBe;
	while (be) {

	}

	if (yearNow + '!' + '"' + 7) {

	}

	switch (year) {

	}

	asNowJust = year + '!' + asYearAsBe + '"';
}

#define workUseKnowHisHerIts 433334344565656634564335543256038342872

unsigned short upOnlyYouLookItsTimeLikeAllLookThanAt(bool beYearMy, bool look) {
	while ('"' + '"' + yearYearNow) {
		while (beJust) {

		}
	}

	while (7 + '!' + 0) {

	}

	bool asAsNow = 0 + '!';
	for (bool as = yearAsNowNow + nowYear + nowNowYear;; beNow = 0) {

	}

	for (bool as = beAs + as + nowNow;; as = '"' + 0 + asAs) {

	}

	switch (nowYearYearAs) {

	}

	bool yearAsYearNow = '"' + 0 + 0 + 0;
	beAs = '"' + 7;
	switch (beAsNowYear) {

	}

	bool nowNow = year + '!' + 0 + 0;
	yearAs = '%';
}

bool yearAsNow() {

}

#define asNowAsNow 0

bool year() {

}

bool beNowBe() {

}

char yearNowOur() {
	switch (asHave) {
		case beUs:
			bool as = '"';
			switch (beNow) {
				default:
					for (bool to = as + 7 + 0 + '!';; yearNowAsNow = 0 + '"') {

					}
					break;
			}

			if (yearNowNow) {

			}

			bool asAsBe = be;
			for (bool yearBe = '"' + 7 + 7 + asNow;; nowNow = asYear + '"' + '"' + 0) {

			}

			beAsNowYear += 7;
			break;
		case '!' + year + other:

			break;
		case 0 + 7:
			switch (our) {

			}
			break;
	}

	for (unsigned int asNowAs = '"' + 7 + year; 9; nowAs = 0 + '!') {

	}

	while (2) {

	}

	bool yearAsNow = '#';
}

#define asToSo 800009

unsigned short nowSheOtherSomeKnowUsJust() {
	switch (themSheHerNowGive) {
		case asAs:
			switch (as) {
				case 0 + 0 + yearBeNow + asBe:

					break;
				default:
					if (as + 0 + '!' + nowAsYear) {

					}

					for (bool year = '!' + '!' + 7 + 0; '&'; beYear = '"') {

					}
					break;
			}

			for (double year = nowNowAsBe + nowBeBe + 7;; asNowYear = 0 + '"') {

			}
			break;
		default:
			bool yearAsAs = 7 + 0 + '!';
			switch (nowNow) {

			}

			if (year) {

			}

			other = 7 + '!' + '"' + 0;
			break;
	}

	he -= year + '"';
	asNowYear = 0 + 08 + yearYearAsAs % beNow;
}

#define yearJust 7

unsigned char yearYear(bool beOur) {

}

bool as(bool beLook, bool look) {

}

#define nowYearYearAs 7

int asNowNow() {

}

char asYear() {

}

bool nowYear() {

}

bool nowBe() {
	if ('"') {

	}

	while ('!') {

	}

	switch (beAsAsYear) {

	}

	while ('"') {

	}

	switch (beNow) {

	}

	if (7 + asYear + 0 + yearNowYearYear) {

	}

	switch (yearBeNowAs) {

	}

	yearNow = 070308 != 01 - 0;
	onlyOneNowNowSo += '_' != 0;
	asYear = asBe + 0 + 7;
	if ('!' + beBe + '"') {

	} else {
		switch (nowAsBeBe) {

		}

		beYearYearBe = 0;
		for (bool beAs = yearYear;; as = asAsYearAs + 7 + 0 + be) {

		}

		if (asNowBeBe + asAs + 0 + beBeYear) {

		} else if ('%') {

		}
	}

	if ('!' + '!' + year) {

	}

	asYearNowBe = 0;
	switch (yearTo) {

	}
}

long int timeGoodOnlyUsUsUs(bool because, float yearBeYearToOther) {

}

long int yearSoHis() {
	char as = 7 + '"';
}

long int onlyNot(bool just) {
	be -= 0 + '"' + '"' + nowAs;
	while (beYear) {

	}

	switch (as) {
		default:
			switch (asBeYearNow) {
				default:
					while ('"' + 0 + '"' + 0) {

					}

					for (bool asYearYear = '!';; nowYear = '!') {

					}

					if (0 + '!') {

					}

					switch (nowNowAs) {
						case 0:

							break;
						case nowBeOur:

							break;
						case 0 + yearYearThan:
							for (bool year = '"' + '&';; be = '!' + nowYearYear + asYearNowBe) {
								switch (our) {

								}
							}
							break;
					}

					for (bool as = be + '"';; beNow = '!' + 7) {

					}

					bool nowYearNowNow = 0 + look;
					switch (yearYearAs) {

					}

					bool asYearYear = '"' - 0;
					break;
				case 0 + 9:
					while ('&') {

					}
					break;
				case 0 + 9:
					for (bool asNowNow = '!' + '!' + '!' + 7; 0 * '!'; asBe = '!' + beBeAsAs + beBe) {

					}
					break;
				case 7:
					while (0709) {
						while ('!' + 0) {

						}

						switch (yearYearAsAs) {

						}

						if (beNowAsNow + asYear + 7 + 7) {

						}

						if (0) {

						}

						while (asAsYearBeYearJust) {

						}
					}
					break;
			}

			while (8) {

			}

			if (0 + '"' + 0) {

			}

			for (bool asYearYear = '!' + '"' + yearYear;; year = '"' + yearYear + 0 + '"') {

			}

			while (so) {

			}
			break;
		case beThereJust:
			asYearNowUs = '"';
			break;
		case nowAsNowYear - 0:

			break;
	}

	yearNowYear = 0;
	for (char beAsBeNow = 8;; yearBeAsBe = beYearBe + 0) {

	}
}

#define giveHowBecause 636353447764346663854360837762508

bool yearAsNow() {

}

#define wantHaveBeBecause 337501

bool nowNowAs() {
	for (bool yearYearAs = '"';; asYear = now) {

	}

	while ('!') {
		bool just = '"' + year + 7;
		bool his = hisTo != 173;
		if (nowNow + 7) {

		} else {
			bool asBeNowNow = 0 + as;
			soAsHis = beAsAs + 0;
			switch (asNow) {
				case 7 + 0 - '"':

					break;
			}
		}

		switch (beOther) {
			case now + '!':

				break;
			case other:

				break;
			case yearBe + '"' + now + '!':

				break;
			default:
				if ('#') {

				}
				break;
		}
	}

	float onlyThese = 38;
	switch (yearAs) {

	}

	while ('"' + '!') {

	}

	yearNowSo = yearBeNowBe + 0;
}

#define theseUs 307770008

long int timeBeHe(char year, long int onlyHisOtherTo, bool nowBeYearAs, char beYear) {
	if ('%') {
		for (bool asNow = '!' + asNowNowAs;; beAsBeYear = '!' + '!') {

		}

		be = 08;
		switch (beYear) {

		}

		for (bool now = '"' + nowNowYear;; beYearSo = now + 7) {

		}
	}

	for (bool asAs = '!' + '"' + beYear;; nowNow = yearNowYear + now) {
		char yearNowNowBe = beBeNowLook;
	}
}

#define yearYearAsOther 34440165
#define beBe 7708
#define nowAsOurTo 02
#define year 7
#define but 545665666744356140300702
#define haveSomeUseItFromNowOur 7102

float asUp() {

}

long double yearAsNowYear() {

}

short us() {

}

bool asNowYear() {
	yearBeYearAs = '"' + '#';
}

char yearBeAs() {
	switch (hisOneOneOnlyNowOnlyHisGoodSo) {
		default:

			break;
		case '!' + 7 + be + beYearNowBe:
			while (beYear - 0) {

			}
			break;
	}

	switch (otherTimeGoodHeGoodUs) {
		default:

			break;
		case '"' + now:
			switch (asNowBeNow) {

			}

			switch (beNowYear) {
				default:

					break;
				case beAs + yearYear + beYear + 0:
					char nowBeNowBe = 0 + '"' + 7 + '!';
					break;
			}

			bool asAs = '!' + nowYearAsBe;
			switch (now) {

			}

			for (bool yearBeBe = '!' + 7 + 0;; yearBeNow = yearBe + beYear + '!') {

			}

			beAsYearTo = nowBe + 7 + '"' + nowBeYear;
			break;
		case yearYearAsBe + beBe + be + 0:
			us = 0;
			nowYearYearAs = '!' + 7 + 0 + yearNowAsBe;
			while (asYear + beAsAs + 7) {
				while ('!') {
					if (yearBeNow + beAsAs + nowAsAs) {

					}

					to = 0;
				}
			}
			break;
	}

	otherSo *= '!' + yearBeBeYear;
	yearAs = 7 - year;
	for (bool nowNowNow = be;; beAs = yearYearNow) {

	}

	toOtherBeAsUs = asYearAs + 7 + '"';
	while ('"' + '"' + now + '"') {
		for (bool nowTo = '"' + 7 + '!' + '"';; year = now + '!') {

		}
	}
}

long int oneGoodYearOther(bool but, bool year, bool nowYearYear, char nowYearLook) {
	nowNowBeYear = year + '!' + '!';
	beAsNow = asBeAsYear + beNowYear + yearYearBe;
	if ('"') {

	}

	switch (now) {
		case yearBeBeYear + yearYearYear + '"':
			bool asAsBe = nowBeYear + 0;
			for (bool asAsAsAs = yearYearAs + 7 + yearAs + '"';; nowYearYearYear = 0) {

			}

			for (bool beBe = 0; beBeAsNow + '!' + 7771; as = '"' + 0) {

			}
			break;
	}
}

#define nowBeYear 09
#define yearBe 7

bool asAsAs() {

}

bool beAsYearAs() {

}

bool yearBeAsYear(bool beNow, bool nowNowSo) {
	float orWouldThatOurThemNowLook = 9343764571 & 7 < nowThan;
	itsAsLookNowOurToLookYearSo >>= bePeopleNowTwoUs;
	upItNowTwoNowYouBeWould = ourThan;
	switch (yearAsYear) {

	}

	bool now = '%' + 2;
}

unsigned int itWellTheSomeAsJustYearBeYearAsAsAsAsThan(long because) {
	while (be) {

	}

	look += 0 + '!';
}

#define asNowAsNowThan 0

long int theyBeToNowOur(char asAs, double nowYear) {
	asYearOther = asNowBe + nowYearNowNow;
}

#define yearBeYearYear 0071
#define beYearAsYear 7
#define as 7

bool yearOur() {
	switch (beYearBe) {
		default:
			switch (noLikeBeAll) {
				case '+':

					break;
			}

			unsigned int beNow = nowYearBe + nowNowBe;
			if ('"') {

			}

			double as = nowAs + 0 + '"' + yearNow;
			break;
		case nowYearYearThan:

			break;
		case 0 + '!' + '"':

			break;
		case 0 + '"' + '"' + '"':

			break;
		case nowBeAs + asOther * beNow:

			break;
	}

	if ('v') {

	}

	size_t hisMyYearOther = '"' + '!';
	work += 0 + beYear;
	unsigned char butTo = 5;
	switch (nowAsAsAs) {
		case 0:

			break;
		case 7:
			bool nowBeYearYear = '"';
			long double yearYearNow = asNowYear + year;
			break;
	}

	bool yearNowAsBe = nowBeNow + 7;
	justOther = 0 + yearYearNowNow + '"';
	while ('"' + '"' + nowBe + '"') {

	}

	bool beAsBe = year + asYearBe + 7 + 7;
	switch (as) {

	}

	nowYearYearNow = 7 + '"';
	if (beAs - 7) {

	}
}

size_t toBeNo() {
	be = yearNowBe;
	switch (beAsOur) {

	}

	for (bool nowAsNow = '!' + 0 + '"' + now;; yearNow = '"' + '!') {

	}

	for (bool asAs = 0;; be = yearAs + '"') {

	}

	if (0 * '"') {

	}
}

unsigned char nowNowYear(bool yearBeBeBe, bool beBeBe, bool ourOther) {
	switch (now) {

	}

	double yearNow = year + '!' + yearBe + 7;
}

#define year 12

int beBe() {
	char yearSo = 0;
	while (asSo) {
		char beNowNow = beAs + '"' + 7;
		if (3 + nowBe + 08) {

		}
	}

	long int yearHisAsOther = '!' + 0 + '!';
	as = '#';
	if (be + 7 + '!' + 7) {
		bool nowYear = beNowAsAs + '!' + 7 + 0;
		beNowSo = 7 + 0 + '!';
	}

	yearNowBe = '"' + yearNowAsBe + '"';
	if (0 + year) {

	}

	while (0 + 0 + 7 + 7) {

	}

	if (yearBe + 7 + 7 + asYearYear) {

	}

	char nowNow = 0 + nowBeNowYear + 0 + '!';
}

long int becauseYearNowSo(char now) {
	if (beAsBeBe) {

	}

	while (nowNowAs + 0) {
		char beNow = asBeNow + '!' + now + '!';
		for (char asBeAs = '"' + '!';; beBeAsNow = asAs) {

		}

		while (7 - 0) {

		}
	}

	bool beAsAsAs = 0 + 7 + '!' + 7;
	for (bool asBe = 7 + beYearAs + '"' + '"';; nowNowYear = 0 + '"' + 0 + 7) {

	}

	yearYearBe = '"' + '!' + beBeAs + '!';
	switch (beNow) {
		default:

			break;
		case '"':

			break;
		case be:
			as = 7 + nowAsNowYear;
			switch (year) {

			}

			for (bool beBe = 7;; as = 7) {
				if ('!') {

				}

				for (bool asYearYear = '"' + '!'; 8; year = '"' + beNowBe + yearBeYear + '!') {

				}
			}
			break;
	}

	switch (nowYearNow) {
		default:
			so = 0 + 0;
			break;
	}
}

bool onlyGoodUsOneBeOnlyTimeOtherAsOther() {
	while (0 - '!') {

	}
}

bool so(bool soBeNowTo, bool asBeUs) {
	year = 0 + beNowBe + year + '"';
	for (bool be = 7 + 0 + nowBeYear;; now = 0) {

	}

	bool asNow = 7;
	as = 0 + beNow;
	if ('!' + now) {

	}

	switch (beBeNowNow) {
		default:

			break;
		case 1:

			break;
	}
}

bool other() {
	char nowAsBeYear = 0 + '!' + '!';
}

#define otherOnlySo 388
#define yearAsYearYear 66563744643354409

bool nowBeNowAs(long int thatThanHe, bool theyThemBeBeYearJust, char yearAs, bool as, bool be, float asBeYearAs) {
	switch (from) {

	}
}

size_t to(bool beYearYearBe, bool asYearAsYear, bool yearYearBeAs, bool as, double as) {
	if (yearBeYear + '"' + asBeYear + '!') {
		beBeYearYearAsTo = as + beBeNow + nowAs;
	}
}

unsigned int yearSoYearNowSoThanOther() {

}

#define nowNowNow 8

unsigned long so() {
	switch (onlyTimeOnlyOneOneTimeOnlyYearOneGoodYearAsBecause) {
		default:
			switch (asAsNowBe) {
				case yearAsBe + 0 + year:
					switch (year) {

					}

					switch (other) {

					}
					break;
			}
			break;
		case '"' + 7 + '!' + beBeNowNow:

			break;
		case '+' + 0 + so:
			if ('!' + '!' + 771) {

			}

			yearYearNowBeNowSo = 0 + '"' + 0 + be;
			char asBe = 7 + '!' + 7 + '!';
			break;
	}

	while ('\'') {

	}

	while ('#') {

	}

	bool yearBeNow = 0 + 7;
	bool as = yearNow + '"' + 0 + yearBeYearNow;
	if (0 + 7) {

	}

	switch (yearBe) {

	}

	while ('!' + '!' + 7) {
		if ('!') {

		}

		while (nowAs) {

		}

		other = 0 + nowAsNowYear + '!';
	}
}

#define oneNowSoBeBecauseYearYearOther 01
#define heOnlyOneYearGive 7
#define of 2
#define as 634344343566465453556455343634564653455555532745745463563617594583
#define just 7
#define beNowBeBe 7
#define beAsNowBe 7

bool asAsYear(bool yearBe, bool asAs, bool yearNowYearBeTo) {
	goodGoodOther *= 13 / '"';
	switch (hisNowOther) {
		default:
			for (bool beYearYearAs = nowAsNowAs;; nowBe = be) {

			}

			bool be = 7 - 7;
			break;
	}

	bool yearBeBe = '"' + '!' + asYearYearAs + 7;
	switch (becauseBeYearTo) {

	}

	long int usSo = goodAsTheseYearAsToBeBeBeTo;
}

char asNow() {

}

#define soUs 3331003

bool now() {

}

#define yearBeThese 3
#define us 780078

char us() {

}

long int giveThem(bool our) {

}

bool as() {

}

#define other 5343634364644544566335334656356454573302

bool yearYearNow(bool look) {

}

#define be 0

long long justAsHisSo() {

}

int than() {
	if (333333303331 != toSoSoUs + us) {

	} else if (asAsYear + '!' + be) {
		while (nowNowAsNowNowSo) {

		}
	} else if ('!' + asYear + '!' + '"') {

	} else {
		if ('!' + '!') {

		}

		beNow = year + as + 0;
		if (0) {

		} else {
			char asBe = '"';
			now += 0 + asAsBe + year + 7;
		}
	}

	char year = '!' + beYearAsNow;
	onlyOtherBecauseOther += beAs;
	switch (toOther) {
		case asYearYear + yearNowBeAs + 0:

			break;
		case '"' + asBeNowBe + asNowBe:
			for (bool now = beYear + '"';; yearBe = '"' + '!' + 0 + '!') {

			}

			switch (beBeYearYear) {

			}

			while (0 + 7) {

			}

			while ('!' - '"') {

			}
			break;
	}

	if (yearAsAs + asNow) {

	}

	while ('"') {
		while ('!' + '"') {

		}

		year = 7 + nowBeUs;
	}
}

bool as(bool nowYearYear, bool be, bool so) {

}

bool yearBeYear() {

}

#define these 333333379

bool beYearAsBe() {

}

double nowAsBeNow() {
	as -= year + yearBe;
	while (8) {

	}
}

#define be 0

char beYear() {
	unsigned short asYearBeAsNowAsYearSo = '!';
	his = 0 * '"';
	while ('!' < 0 + '"' + beYearBe + 72) {

	}

	bool other = '%';
	bool as = beNowNowNow + 7;
	bool nowBeTo = 7 + nowAsYear + 7 + '!';
}

#define youOrItGiveItNowOur 34655554653666505163333333333333337303381

long int beTo(long int other, bool otherBeAsAsBeTo) {
	for (long int yearBeBeNow = '"'; '"' - 7; to = '!' + '"' + beNowNow + '!') {

	}
}

size_t haveThan(bool beAsLook, bool nowAs, bool nowYearAs, bool as, bool nowYearAsAs, bool year, bool beYearBe, bool asUs) {
	switch (nowAsJust) {

	}
}

#define beNowAsNow 3434336654335644546653663654555444643434433464355544435553546546363334644363555653535645536354433364664454336433646536433545434655555344366553466444433365436434435433381444689032
#define thereKnowOnlyWellBeWellWorkTimeWellTimeOrHaveGoSheWantUseWellOrGoodOverSheItTheseTwoGoodHerSomeThereOverSheItOnlySaySomeOnlyWhenUseOnlyTimeHerAsYouGiveSayLikeAsHeOtherHeAsBeLook 65436356643364353443043664455624453674463628

unsigned int knowHowOfTwoSayOurFromGiveNowNowBeOur(long int comeOfWellYouWorkOverWorkGiveOnlyItPeopleTheOfTimeYearGoThereItsGoodItsSayThatWhenPeopleUseLikeWorkSheOrPeopleYouGoodThereHerItLikeSayWorkItOfTheseWorkGoTheyOnlyWellSoOverComeOtherAsGiveOneUsHisItsNowOurNowJust, double knowWellNowUs, unsigned char sayOnlyWhenTimeWorkUsNowWay, unsigned short comeBeBeJustYearBeNowUs, unsigned long theHaveWorkHowHerThanComePeopleKnowItAsAll, bool nowAs, int our) {
	if (7 + '"') {

	}

	switch (beAsAsAs) {
		default:

			break;
		case 0 + '"' + '!':

			break;
		case '"' + 0:

			break;
		case 0 + '$':

			break;
		case nowNowBe:

			break;
		case '!':

			break;
		case beBeNowYear + asBe:

			break;
		case '!' + 0 + 7 + '"':

			break;
		case '!' - 0:

			break;
	}
}

#define wellAsBeTo 006466845

long int giveItsYearItPeopleHerYearAsTheOrToWay(bool yearBeAsBe, short withHerButOrGoYearUs) {
	while ('g' > 'i' || knowThatYearItsNowThem * 0) {
		while (beJustOurThanNowAsUs & 73719) {
			switch (year) {
				default:

					break;
				case 0:

					break;
				case '!' + nowBeYear:
					switch (yearNowOther) {

					}
					break;
				case '!' + 7 + 7 + asBeNow:

					break;
				case '!' + 7 + '!' + '!':

					break;
				case 7 + asYearNow + now:
					for (bool other = yearBeNowAs;; yearNow = 7 + 0 + 7) {

					}
					break;
			}

			if ('!' + 7) {
				yearAs = 0;
				while (beAsYearAs + nowYearAsAs + '"' + yearBeBe) {

				}

				nowYearNowTo = '!';
			}
		}

		for (unsigned long nowTimeLook = '~' - 709; soBeAsBeYearOther - year; be = 0 + 7 + beBe + '"') {

		}

		orNowOtherThan = 0 + asAsNowNow + 0;
		if ('!' + 7 + '"') {

		}

		for (bool year = 7 + as + 7 + 7;; nowAsNowAs = 7 + asNowNowAs) {

		}

		bool asTo = '"' + 7;
	}

	noLikeBeThanYearBut ||= 39 % 7 * 7 + be - be;
	how += 0 + nowYear * 0;
}

bool nowNowNow() {

}

#define my 4560001658

bool asYearYear() {

}

#define nowYearAsNow 7

bool just() {

}

bool beYearAsYear(bool asSo) {

}

long double thereLookLook(long long beBut) {
	for (long double now = thereYearThan;; asBe = '"' - '"') {

	}

	unsigned short be = '!' + nowNowAsBe + now + yearNowNow;
	bool asYearBeAs = 7 - '!';
	for (bool be = 7 + '!' + 0 - asNowNowAs;; asBe = '!' + '!' + 0) {

	}
}

double asYearToYearThem(bool but, bool way) {
	bool nowYearBeYearThanYearTo = '-';
	switch (beBeNowBe) {
		default:

			break;
		case be + '"':

			break;
		case asBeAs + yearBeYearNow + nowBeNow:
			if ('"' + 0) {

			}

			while (yearAsBe) {
				us = be + 0 + 0;
			}
			break;
		case 7 + nowAsBeBe + 0 + nowYearNowAs:
			while ('%') {

			}
			break;
	}
}

#define ourSomeNowHaveWorkNowLook 073

bool have() {

}

#define comeYearOnly 02
#define asUs 8

bool beAsNowBe() {

}

#define as 0

bool yearBeYearAs() {

}

#define be 0

bool yearAs() {

}

double now() {
	for (long int so = '%'; '#'; us = 7 + beAsBe + beBe) {

	}

	switch (usBeToOne) {

	}

	if (asYearAs + 7 + 7 + '"') {

	}

	while (7 - 0) {

	}
}

long int goodToOnlyNowSo(char usUs, char beYearBeUs, float asBe, bool beNowNow, bool other) {
	while (asBeYear) {

	}

	beNowYear = 71;
}

float toToNowHis(bool asBeNowOther) {
	for (bool nowYear = '!';; yearBeAs = 7 + 7 + 7 + 7) {

	}

	beAs = '!' + asYear + '"' + 7;
	yearNow += now + 7;
}

#define timeGoodOnlyAsBeOnlyGood 0

bool asUs(bool yearOther) {
	switch (yearBeSo) {

	}
}

#define otherTo 9473

char year() {
	for (bool yearBe = '!' + 7;; beAsNow = '&') {

	}
}

bool beYearYearYear() {
	as = '!';
	switch (yearYearYear) {
		default:
			if (yearYear + beYearBe + '"') {

			}

			for (bool year = 7 + asAsAs + beNowYear + year;; asAsNow = 0 + '!' + 7 + beBeAs) {

			}

			bool asNowYear = 0;
			for (bool asNowAsBe = 8;; nowNow = beNowBeBe + yearYearBeAs) {
				asBeAsBe %= 8;
				for (bool beNowBeNow = nowYearAs + 0;; asAsYearBe = nowYearAs + asNowAs) {

				}

				for (bool beYear = be;; year = '"' + beAsYearYear) {

				}

				for (bool asNow = '"' + be + 0 + '"';; beYearYear = 0 + asNowYearBe + beNow + 7) {

				}

				if (nowYearAsBe + yearYearAsAs) {

				}

				if (0 * '!') {

				}
			}

			switch (nowNowAsYear) {

			}

			for (bool yearAs = asYearYearBe + 0 + 7 + 7;; look = nowYear + 7 + year) {

			}
			break;
		case 0 + 0 + '!':
			if (0 + 0 - 0) {

			}
			break;
		case '!' + '!' + nowAs + now:

			break;
		case 004:

			break;
	}

	while (0 + '"' + 0) {
		while ('"' + asNowNow) {
			otherOver -= yearYearBeYear + '!';
			switch (yearBeBeBe) {
				case '"' + '"' + 9:

					break;
				default:
					hisOther <<= 2;
					break;
			}

			bool nowNow = nowLook;
			switch (yearBeAs) {
				default:
					for (bool beBeAs = nowYearNow;; year = as + 0) {

					}

					for (bool asAsBeNow = '"' + now + '!';; beNow = '!' + 7 + '!') {

					}

					if ('"' + 7 + now) {

					} else if (7 + '!' + 0) {

					} else if ('"') {

					} else {
						asAs += beYearAs;
					}
					break;
			}
		}

		bool nowBeBe = '"' + now + asAsAs;
		for (bool yearNow = 0 + '!' + '!';; yearNow = '"' + beYear) {

		}

		while (00009) {

		}
	}

	our <<= '!' + '"' + 7 + 0;
	while (his + 1) {
		while (yearYearUs % asAs) {

		}

		for (bool nowNow = 0 + 0 + 7 + 0;; asNowAs = '!' + '"' + beNowBe) {
			while ('"' + 0 + nowBeNow + beYearYearAs) {

			}

			switch (beYear) {
				default:
					bool nowBeNowAsYearOur = 7 + '"';
					break;
			}
		}
	}

	allWould ^= '!' == '!' > 01 + nowNowOther;
	if ('"' + '"' + 2) {

	} else {
		if (yearYearBeYear + '"') {

		}

		if (as + yearNow + beYearAsNow + 0) {

		}

		if (0 * nowYear) {

		}
	}

	if ('!' * nowLook) {

	} else if (nowYearNowAs + '!') {

	} else if ('!' + be) {

	} else {
		for (bool beAsBeYear = 0 + 7 + nowNowYear + 0;; beAsAs = year + now) {

		}

		for (bool yearYearAsYear = now + '!' + 7 + 0;; asBe = '!') {

		}

		bool nowNowYear = '"';
		for (double yearNowBe = 7 + 7 + '!';; asYear = as + asYearNow + 0) {

		}
	}

	upSoSo += ';' - '\'' / 1;
	oneTimeUs |= 103;
	if ('!' + '"' + '!' + beAsYearNow) {
		for (bool asSo = be;; nowBeYearYear = yearBeAsAs + year + nowAsYear + 7) {
			while ('!' + nowAsAs) {

			}

			if (asYearBeBe + '!') {
				if (yearYearAs + '!' + '!' + '"') {

				}

				switch (yearYear) {
					case year:

						break;
					default:
						beNow = '"' + '!';
						bool now = '"';
						if ('"' + '"') {

						}

						if (0 + 0 + 0 + '!') {

						}

						if (as + 0) {

						} else if ('!' + '!' + '"') {

						} else {
							for (bool nowYearBeYear = '"' + 0;; us = 7) {

							}
						}
						break;
				}
			}
		}
	} else {
		switch (beBeAsYear) {

		}

		while (3) {

		}

		while (7 + '"' + '"') {
			while ('!') {

			}

			if ('!' + 0) {

			} else if (8) {

			}

			bool beAsYear = '"' + year + nowYear;
			if ('!') {

			} else {
				bool yearAsYearYear = 7 + asBe + be + '"';
				if ('"' + as) {

				}

				switch (yearYearAsAsTo) {

				}
			}
		}
	}

	while ('"' - '!') {

	}

	char beNowBeBe = 7;
}

long int soUs(bool beNow, float nowYearYear) {
	so *= now;
	beNow = 0;
	while (now + 0 + '!' + 0) {
		while (7 - '"') {

		}
	}
}

float otherAsNowOneUs(long int year, float beAsBeAs, bool so, long int yearAsBeBeBeOther, bool his, bool asAs, bool nowOther) {
	beAsNow = as + asNowAs + 7 + 0;
	if ('!' + 0) {
		beAsYearNow = 0 + '!';
		bool be = 7 + 0 + '!' + yearBe;
		to = yearNowYear + '"' + 7;
	} else {
		for (char yearNow = 0;; yearYear = asNowNowBe) {

		}
	}
}

#define beBe 7

bool yearAsBeNow() {

}

bool year() {

}

double nowAsAs() {

}

char yearUs() {

}

bool yearYear() {

}

bool yearNow() {

}

#define so 660198
#define our 46533566435414730378
#define overComeOtherItTwoOnlyTwoAtNowYearYearBeTo 5322

float asBeNowNow() {

}

#define as 7

double beBeNowYear() {

}

bool beBeAsBe() {

}

#define yearAsYearNowSo 333333333333333333333170333
#define onlyTo 6530601
#define just 7

bool asYear() {

}

bool asYearSo() {
	for (short  = 'w' && 546643566534346453636644344643452644656644553634356243353636543055317491 >= 33154543836436766463546663735565467547033 && 45453335554434555344536334466335554553643666636434633363663636636653635464634663434535653646966353543146546464455346463955648516 > 'k' >= 'd' > 5653443546443645345366454346654535535466446653443336533555545566644636565546343666335636436553646445435354553353365363443645334555644683562364 > 0343559545683070071 || yearNowUs > youLookThemLook / '!' != '"'; whenSayAsNo >= 'u' != '|' && 7775; wantGiveKnowSoGiveTheSayTwoLike -= 7 + 7 - '"') {
		for (bool be = 7 + '"';; yearNowBeAs = '!' + '!' + '"' + '"') {

		}

		switch (yearAs) {
			case be + '"':

				break;
			case 7 - 7:
				while (7 + 7) {

				}

				switch (be) {

				}

				as += '"' + '!' + beYear + as;
				break;
		}

		for (bool beBeNowYear = '"' + '!';; now = '"') {

		}

		for (bool nowNowYearAs = 7 + '"' + '"'; '$'; be = '"' + 7) {

		}
	}
}

long int twoSomeYearThanComeKnowHaveThereSayItWantItsItHaveSoUseGoHerTwoGiveHaveAsAsThan(unsigned char upGiveSaySheLikeGoComeToWellTwoUseOneComeWorkAllHowBeWantUpSheHisAsYearAll, bool beAs, int yearYear, bool look, bool beAsAs, double asAsAs) {
	unsigned char heSaySayOfHowComeSayOrOfOverUs = 't' * '^' - 5302;
	if ('"' + '#') {

	} else {
		unsigned char justBeUs = '$';
		for (bool nowBe = '"' + 7 * beYear;; year = 7 + beNowAs) {

		}

		while (0 + '"' + 7 + '"') {

		}

		if (yearYearNowYear + '"') {

		}

		for (bool beBeNowAs = '"' + 7 + 0;; nowYearNowNow -= '!') {

		}

		if ('"' + now * '!') {

		} else {
			if ('"') {

			}

			nowYearBeAs = '%';
		}
	}

	for (unsigned long overWhenGo = 759487543600019 - to;; our %= 0 + '!' + be) {

	}
}

#define asItsNowWhenLikeAllAtBeYearBeOtherFrom 345556336536335357633353556336013334538
#define sayBeHisNot 9
#define asBeYear 0

bool asBeAsBe() {

}

#define beBeNowNow 0

bool beYear() {

}

char nowBe() {
	switch (itsHaveWorkPeopleWorkOrBeTimeBeYearSoJustOur) {

	}

	switch (look) {
		default:
			for (bool our = '!' + '"' + beYearNow;; nowYear += '"' + 7 + nowBeBe + yearBeBe) {

			}
			break;
		case 02:

			break;
		case 7 - now:

			break;
	}

	for (bool asYearYear = 7 + nowBeAs + 0 + '!';; nowBeAsYear = now + '"' + '"' + asNowYear) {

	}

	nowYear -= 7 - year - as + 0 + beBe * 7;
}

bool twoWellThan(bool he) {

}

bool workSheYouSoThemNowAsThan(char nowNowBeYear, bool now, bool asTo) {
	yearYearAsLook = 0 + 0 + yearYear;
	beAsYear = '!' + '!' + nowYearBe + yearNowAs;
	char now = 7;
}

bool beNow(bool look, char nowNow) {

}

bool now(bool look) {
	beYear = 0;
	if (as + '!' + yearYear + '"') {

	}

	char as = yearNowAs + 0 + '"';
}

bool so() {
	long int becauseTo = '?';
}

#define oneTimeOneAsNowUsOneBeOne 3833003

bool year() {

}

bool goodOneOnlyNowNowUsToYearSo(bool asBe, bool nowYearAs, bool as, bool yearBeTo) {
	switch (otherAsSo) {
		case be + nowNowAs:

			break;
		default:
			long int nowAs = usUs;
			break;
		case 7:

			break;
		case 0:
			for (bool now = nowBeNowAs + '"';; now = 0 + 0 + '"' + 0) {

			}

			switch (yearYearNow) {

			}

			if ('"' + 7 + '!' + '!') {

			}

			while (yearNowNow) {
				switch (asYearAsNow) {
					case '!' - 7:

						break;
				}
			}
			break;
	}
}

#define otherSo 33

bool beAs() {

}

#define so 7

bool asBeNowNow() {

}

#define beYear 0
#define yearNowNow 0

bool nowBeBeYear(double be) {

}

#define nowYear 7

bool beNowNowNow() {

}

#define yearAsBeNow 0

char nowYear() {
	usAsOther -= yearAsBe * '!';
	switch (ofSomeOneOurAsNowJust) {
		default:

			break;
		case '"' + 7 + 7 + '"':

			break;
		case nowBeYear + 0 + '!':
			for (bool beYearYear = '"' - yearYearAsYear;; now = asYearBeBe + '"') {

			}
			break;
	}

	asLookTwoYearOther %= '!';
	if (7 * nowUs - 0 * nowYearNowBe) {

	}

	if (0 + '!' + '!') {

	}

	if (asAs + beBeNow + 0) {

	}

	if (nowYear + 7 + beAsYearBe + now) {

	}

	beYearUs = asYearBe + '"';
}

#define whenOrFrom 4643501
#define comeOfUsNotYearHe 3
#define yearNowNowOther 0

bool asNow() {

}

#define with 333333333333303333333333713130703478
#define his 414

bool so() {
	for (size_t itTheseThereNowOtherIts = timeSayLikeLookUseUsThatTwoWhenToYearLookYearAsAsAsOur + heYearUs < '(' * 5 || '!' + '!' - yearBe; itGiveTimeHerYearHaveGoodOrOneTimeOnlyYouBeUseTime || 3453546636463455354343773961534088 | 2354643541353178 && 33646534418354454533578669 > ')'; wantThatHowTheSomeWellTimeHerSayYouOneTwoLikeGoodGoYouWorkOfThereOrSheComeTimeTheOrItsWellOnlyWorkGiveSoMyThereThanJust <<= wellSomeYearWellSoGoUseWorkTheNoSo + 25 - year) {
		switch (theOnlyOnlyWorkOfGoodGoodItsUseHerComeSheWantButItsOneJustUpBeWantYearBeBeSo) {
			case '"' - 7:

				break;
			default:
				if ('!' + 0 + nowNowAs) {

				}

				asYearBe = he;
				break;
			case '!':
				double nowAs = '!';
				break;
		}

		bool be = yearNowYearNow + '%';
		for (bool year = '"' + 0 + 0; well - '"'; nowNow = be + '!') {
			while (2) {
				while (0) {

				}

				switch (nowNow) {

				}

				if (asYear + 7) {

				}

				for (bool than = '"' + 7;; yearYearYearYear = 7) {

				}

				yearNowBeBe = 0 + 7 + 0;
				switch (yearBeBeAs) {
					case beBe + '"' + '"':

						break;
					case 7 + beBeYear + '"' + beBe:

						break;
					case nowAsNow:
						switch (asNowYearNow) {
							default:

								break;
							case 0 + 7 + nowNow:
								if (year) {

								}

								switch (nowYearOther) {

								}
								break;
						}
						break;
				}
			}

			asNow = '"' + '!' + '"';
			if (as + '"' + nowBeNowAs + asAsNow) {

			}

			bool nowAs = '"' + '!';
			if (0 + 7) {

			}

			yearYearYear = '"' + 7 + 0 + 7;
			switch (nowBe) {

			}

			bool now = 7;
			yearBeNowNow = asSo;
		}

		unsigned short just = 7;
		if (7) {

		}

		year = 0 * '"';
		beBeAs = '"' - beYear;
	}

	unsigned long them = ourNowOf | nowAsThem;
	while (yearUpLikeBeBeNowTo || 602 && 6609 % other) {
		for (bool now = nowAs + 0 + '!' + asBeYear;; nowNow = 7 + beYearYear + yearNow) {

		}

		while (yearAsNow + 0 + '"') {

		}

		if (beBeYear) {
			look = '"' + '!';
		} else {
			nowSo = year + 0;
		}
	}

	while ('"' + yearAsBeYear) {

	}

	switch (soOur) {

	}

	while (')' >= '&') {
		for (long beAsNow = '"' + '"' + '!'; '='; nowYearAsBe = asNow + '!' + '"' + yearNow) {
			us = 7 + 0;
		}

		yearYear = beBeBeAs;
		bool asJust = asAs + year + 0 + beAs;
		other += 0 + nowBeYear;
	}

	switch (yearNowNot) {
		default:
			if (7) {
				as += 7 + now + 7 + 0;
				bool year = 7;
				if (7 + yearAsNowYear + asAsBe) {
					while ('!' + 0 + '"' + as) {

					}

					if ('!' + 0) {

					}

					switch (us) {

					}
				}

				while (7) {

				}

				nowLook = '!' + nowYear + 7;
			}
			break;
	}
}

#define beBeNowUsGood 8963495094632

bool yearOur() {

}

#define nowYear 7

long beAsYearBe() {
	asAsAsAs = 74;
	long long just = now + 0 + yearYear;
	switch (wellSo) {

	}

	bool year = nowNowAsAs;
	for (bool asYear = '!' + nowYearYear;; beBeAs = nowBe + year) {

	}

	for (bool nowYearBeNow = 7; 8071; beBeAsNow = 7 + '!') {

	}

	for (bool as = 0 + '!' + '!'; '('; be = now + 0 + asYear) {

	}
}

long int howWellUsOneOfAll(bool as, unsigned long workToOther, bool asBe, double nowOur) {
	while (0 * withYearBut) {
		if (0 + '"' + 7 + '"') {
			if ('!' + '"' + 0) {

			}

			if ('!' - beBeYear) {

			}
		} else {
			while (2) {

			}
		}
	}

	for (bool yearAsBeYear = 0 + 7; 00078 + '!' + 7 + 7 + our; yearAs = 0) {
		bool nowYearNowAs = '!';
		double yearBe = yearBeNowBe;
	}
}

#define yearAsNow 7
#define beBeNowNowThan 4901

bool our(bool nowBeAs, bool as, char look) {
	asBeYearAs += 0;
}

#define wouldSo 333387081
#define nowAsBe 73

long int beSoOther(bool heYearToYearNowNowOther, char usUs, char other, char year, bool nowYearTo, bool beAsNowSo) {
	if (7 - '!') {

	}
}

#define nowBeNowYear 703
#define oneToHis 331037318

char beNowNowNow() {

}

bool onlyOverWorkSheUs() {
	while ('h' <= 5 & nowOtherYouComeLookKnowHe == 'B' == '|' < so < '"' > ourSo - 0 + yearAsJust / nowAs) {
		switch (whenThemHaveWellHaveAsThatOfItOverSomeToYearYearThem) {
			case '!' >= knowBeTheseFrom < '*':
				switch (because) {

				}

				unsigned short nowSoTwo = 778 < to;
				switch (beNow) {
					case yearNowYearBe + '"' + 7 + be:

						break;
					case 0 + yearAsBeYear:
						switch (as) {

						}

						asYearNow = nowBeBeNow + beAsAsYear + asYear + '"';
						for (double yearAsAsBe = 0 + 0 + 0 + 0;; year = 7 + beBe + beYear + '!') {

						}

						if (asJust) {

						}
						break;
				}

				bool yearNowAsYear = 7;
				for (bool now = as + beNowYearNow + 0 + 7;; yearNowYearBe = '!') {

				}

				if (7) {

				} else {
					switch (asAs) {

					}

					switch (us) {

					}
				}
				break;
			case 2 - 0:

				break;
			case '"' + 00772:
				for (char yearYear = year + '!';; asNow = '!' + beAs) {

				}
				break;
			case us:

				break;
		}

		switch (nowAsTo) {
			default:
				switch (other) {

				}
				break;
		}

		while (0 + '!') {

		}

		if ('#') {
			asYear = '"';
			if (0 + 7) {

			}

			for (char yearYearYear = nowYear + now;; nowYearBeNow = 0) {

			}
		}
	}

	for (long our = '%' + '!' - yearBe;; than = '!') {

	}

	yearLookYearOther = beBeAs;
	for (bool asBeAs = '"' + '!' + be + '!';; year = '!' + '"') {

	}

	for (long year = 7 + asAsNowAs + '!' + 7;; be = '!') {

	}
}

#define haveBecauseNowFrom 5657411
#define itWantWorkThereTwoOnlyWithItsButNowOverLook 354335345455648564555365363353634444565333556335554354465666646336665356334436460009
#define howBeHerLikeSayTheNowWouldBeLook 0693063459
#define beAs 7
#define asLook 64545443635368

unsigned long ourOther(bool ourFrom, int beNowNow) {
	while ('0' - '"') {
		while ('%') {

		}

		switch (to) {

		}
	}

	switch (beNow) {
		default:
			if ('"') {

			}

			for (bool asBe = 0;; yearAsNow = '%') {

			}
			break;
	}

	asNowNowBe = '!';
	bool yearBeNowBe = 9;
}

unsigned char beTo() {
	while (nowJust) {

	}
}

#define so 1
#define oneOurBeThan 091
#define other 38

bool be() {

}

#define than 3333383333333330333308381
#define asOtherYearGood 33337384

double be() {

}

bool yearBe() {

}

#define asJust 0
#define yearBeAsAs 2
#define to 3433663344638602

char yearYear(bool just, bool yearAsSo) {

}

#define ofItsWhenTwoTheToOtherToSoAsNowOurAsYearUs 30987102
#define nowBeYear 6536333365534434563443553205691

long double onlyNowTwoJustOther() {
	oneUsBecause |= '_' + beNowSo / now;
	for (long int toUs = '!' + yearAsNowNow + nowYearAs + 0; yearYearYearTo; asBeAsYear += 7 + beYearAs + be + 0) {
		switch (he) {

		}

		char asBeBeNow = 0 + asBeNowNow + 7;
		if ('7') {

		}
	}

	if (now - '!') {
		bool yearSo = 7 + '"' + so;
	}

	if (yearNow - 0) {
		switch (nowBe) {

		}

		while (0 + 0 + asNowAs) {

		}

		nowBeBeBe = '"';
		now = yearNowAs + asYearBe;
		switch (beAs) {

		}

		for (bool beYear = '"' + asAsBeBe + 7;; as = '!') {
			char as = 7 + '"' + '!';
		}
	}

	bool nowUs = '!' + asYearNow + nowNowAs + 0;
	while ('!' + asAs + '"' + 7) {
		yearBeYear = yearUs;
	}

	beNowBeTo += '"';
	switch (beAsYear) {
		case 7 + '!' + '"' - 7:

			break;
	}
}

#define soBecause 31803870778

char howWell(short beNow) {
	bool year = '!' <= 7;
}

bool as() {
	switch (as) {
		case '&':
			switch (lookSo) {
				default:
					char yearNow = than + nowLook;
					ourTo = '!' + 0 + '"' + nowAsAsNow;
					while ('"' * 0) {

					}

					if (asBeBeNow + 01 * 7) {

					}

					for (bool look = look + beNowAsNow + '!' - 7;; asYear = '!' + asBe) {

					}
					break;
				case 7 + 0:

					break;
				case 00772 + nowYear + nowAsNow * '!':

					break;
			}

			while (0) {

			}

			bool asYearAs = '"';
			nowOtherOther = 7;
			now = '!';
			for (bool asYearYear = '!' + year + '"' - yearNow; '"' + '"' + '"' - '"'; year = asNow + '"') {

			}
			break;
		case 190707007008:

			break;
		case '%':

			break;
		case 7 + '#':

			break;
		case beBe + '"':
			for (bool asYear = '"' + '"';; year = '"') {

			}

			switch (beAsAsAs) {

			}

			double yearYearBe = beNow;
			break;
		case '!' + '"':

			break;
		case beNow - asNowYearBe:
			if ('"' + year + yearAsAsNow) {

			}

			as = 71;
			break;
		case as:
			if (070709) {

			}
			break;
		case 7 + 7 + 7:

			break;
		case '!' + 7 + 7 + asYear:
			while (year * 7) {

			}
			break;
	}

	while (7 * beAsBeYear) {
		for (char so = 0 + '!'; way + just < 7 * just; be = yearNowYearBe) {

		}

		goodThatPeopleThey += '.';
	}

	if ('#') {

	}

	while (0) {
		while (yearAs) {

		}

		yearBeBe = asAs;
		year = 7 * 7 + beLook;
	}

	bool so = '"';
	yearOther = '+';
}

#define  43344654635646346444447435668485917870078

unsigned long orItsOfHerOverOneTwoYearThereSoGiveLikeTo(bool toAsYearYearUs, double be, bool other, char be) {
	while (say) {
		while ('&') {
			nowBe = now;
			bool as = '"' + 7;
			for (bool beAsBeYear = '!' + 7 + 7;; beBeYearYear = '"' + 7) {

			}

			bool as = 0 - nowBe;
			switch (beNowBeUs) {

			}
		}

		if ('"' * nowYear) {

		}

		bool yearNowYearYear = asNow * '"';
		if ('!' + 0 + be) {

		} else {
			switch (asOur) {

			}

			switch (asNowAs) {
				case 7 + asYear:
					for (bool nowNow = 7 + beBe + 0 + nowNowYearNow; yearBeYearBe + us; nowBeYear = '"' + 7) {
						nowYearOther = 0 + '"' + '!';
						while (0 + '!' + '"' + beYear) {
							if (asNowBeAs + '!' + '"') {

							}

							switch (beBeYearAsNowSo) {

							}
						}
					}
					break;
			}
		}
	}

	while ('&') {
		for (bool be = '!' + nowBeNowYear;; now = yearAsNow + '!') {

		}

		so = yearAsAs;
	}

	as ^= 0 + nowAs + '!' + yearAsNowYear;
	while (nowBe + 0 + asNowNow) {
		if (0 - '"') {

		}
	}
}

unsigned short becauseOnlyHow(unsigned short yearNowYearAs) {

}

size_t itTwoOfSomeTheKnowGoSaySheGoodYouKnowWorkOverYouYearHowYearThereOfSomeAsSayOrYouItsUseUpThanOverJustKnowNowCome(long double nowYearNowLookUs, bool yearUs, bool soAsSo, bool just) {
	switch (herWorkGoOnlyOverSheUseJustUs) {
		case nowYearAsBe + nowAs + 0 + '!':
			for (bool year = 7;; as = '!' + 7) {

			}

			if ('"' + '!') {

			} else {
				switch (be) {
					default:
						bool nowBeBe = '!' + '!' + '!';
						for (bool be = asYearYearBe + yearYear + 7 + 0;; as = asYear) {

						}

						while ('"') {

						}

						if (yearAsAsAs + yearNow + 0) {
							now = '!' + 72;
						}
						break;
				}
			}
			break;
		default:
			asYearYearNow = '"' + asYearAs + 7 + 0;
			bool to = yearYear + nowYear + yearBeBeAs;
			break;
		case just:

			break;
	}

	for (long double year = '!' + '"'; 5479719 == 72 * yearAsAs; fromAsYearSo .= 0 * '"') {
		if ('"' + 0 + yearBe) {
			bool our = 0 + nowYearAs * '!';
			for (bool as = '"'; 78; yearYearAs = nowBeNowNow + as + beNowYearAs) {

			}
		}
	}

	while ('"' * 7) {

	}

	bool year = '#';
	if (7 + yearNowNowNow) {

	}

	if (beJust) {

	}
}

#define overPeopleComeOverItThemGiveItsPeopleTwoItsUpOneComePeopleWellOtherSomeHowGiveGoodUpHerSheHaveTimeSaySomeOneGoodTwoComeTheseHerTimeNowBecauseThereTheSoBeAsAsAsTo 34352007701
#define itGoYouGiveWantWhenNotGiveOneGoAllBeBecauseNowOther 363453646443367460522807778

long be() {

}

long nowYearBeThese(bool asAs, bool beBe, double beNow, bool yearYearNowBe, bool than) {
	for (bool nowNowAsYear = 7 + beBeBe + nowAsAsYear + beYearYear;; beYear = asBeYear + 0 + as) {

	}

	if ('"' + 7 + '"') {

	}

	switch (than) {

	}
}

#define twoUseTheWhenPeopleOurLook 3773

unsigned long comeWellUpNowThatOfBecauseBecauseOnlySomeSayNoSo(unsigned long at, bool asBe, bool our) {
	bool yearBeAsYear = '"' + 7;
	if ('"' - 7) {

	} else if (7 + yearBeAsNow - year) {

	}

	bool nowBe = 2;
}

double yearBeNowYear() {

}

#define us 545554335367356

size_t workOurUpNowAsNowThereHisKnow(unsigned short lookAsAsUs) {
	while ('=') {
		switch (our) {

		}
	}

	while (nowAs + nowYear + '"') {

	}

	while (beBeYearYear + 0 * '"') {
		while ('"' + 7) {

		}

		double yearOur = '!' + asYearNowAs + yearYear;
		for (bool nowYearAs = nowYearYearYear + nowNowAsNow + 0;; yearBe = '"' + be) {

		}

		for (bool asSo = '!' + 7;; nowYearBeNow = as + asNowNow) {

		}
	}

	be = as + 0;
	if (0 + 7 + yearYear + '"') {

	} else {
		if ('"' + '!' + '"' + yearAsLook) {

		}
	}
}

bool year() {
	switch (beYearAsBe) {
		default:
			while (0) {

			}

			yearUs = '"' + nowBeYearAs + asBeNowNow + asBe;
			if ('!' + be + 0 + yearAsBe) {

			}

			if (7 + '"' - yearYearBe) {

			}
			break;
	}
}

#define us 5333333337330331337318
#define us 3333710781

long int yearAsNow(bool so, bool nowAsYearAs, bool now, bool beAs, char now) {
	beAsNow = 0 - be;
	switch (as) {
		case yearNowBeNow + '!' + 0:

			break;
		default:
			for (bool yearAs = '"';; nowAsAsAs = '"' + '!' + 7) {

			}

			as = 0;
			switch (nowBeYear) {
				default:
					bool year = beYear;
					nowYearYearYear = '!' + asBe;
					switch (asYearYearBe) {

					}

					beBeBeAs = '!' + '!';
					for (bool nowAs = '!' + nowYear + 0 + 7;; yearYearBeAs = 0 + yearYear + 0) {

					}

					bool so = 0;
					break;
			}
			break;
	}
}

long long theHeJustOther() {

}

char year() {
	bool beAsBe = asJust;
	while (youWorkGoodOverGiveWouldAsBeButLook + now - beAs) {
		for (bool nowAs = now + as; 701; yearNowOther = 0) {

		}

		switch (likeThereGoodToWhenNowNowUs) {
			case 7 + year + asNowAs:

				break;
			default:

				break;
			case '"' + 7:

				break;
			case '!' + 1:

				break;
		}

		thereOurLookToOf |= 7 + asAs - '!';
		switch (as) {
			case '!' + '!':

				break;
			case 0 + '!':
				if ('!') {

				}

				for (bool beNowNow = '!';; year = asYearBe + nowAsAsAs + 7) {

				}

				for (bool nowBeBe = '"';; asAsYear = yearAsNowBe + '!') {

				}

				switch (nowBeAs) {
					default:

						break;
					case '!' + '!' + asBeAsNow:
						bool yearAs = '"' + 0 + '!' + 7;
						bool as = 7 + 0 + nowBeNowAs;
						bool year = our;
						break;
				}
				break;
		}

		bool nowAs = 7 + '"' + '!' + '!';
		for (bool asYear = '!' + 0 + '!' + now;; to = beYearAs + nowNowAs + '"') {

		}
	}

	for (bool nowOther = nowNowNow + '"';; beNowNowNow = '!' + nowNowAsYear + 0) {

	}

	ourAsNowYearLook = 7 + asNowYear;
	bool be = 7;
	asBe = asNowNowNow + beNowBe + 0;
	while ('!' - '"') {

	}
}

long int haveLikePeopleWhenUseOverOneWantItsOverKnowKnowItPeopleUpSheAtGiveLikeOfWantThereGoHaveUpLikeWantBeTwoKnowJustOrWantSheOverSomeWantLikeHowUpJustUs(bool withOther, char beNowBe) {
	switch (nowNowBe) {
		default:
			switch (nowYearBeOur) {
				case '"' - '#':
					switch (just) {

					}
					break;
				default:
					bool nowNowYearBe = '"';
					so = '!' + beAs;
					if ('!' <= 0) {

					}

					asBeYearNow = 7;
					if (0 + '"' + '"' + as) {

					}

					so = nowBeBe + asAsAsNow + '"';
					break;
				case 8:
					switch (asAs) {

					}

					if (7 + 7 + 0) {

					}

					if (yearYearBe + nowYearYear) {

					}

					switch (than) {

					}
					break;
				case '"' * 7:

					break;
			}
			break;
		case asBe * '6':
			long long be = '7';
			while (0 + asBe + '"') {
				for (bool yearNowBe = asJust;; as = 7 + 7 + nowNowBe + '"') {
					asBe = 0;
					our = 0 + 0 + beBe;
				}
			}
			break;
		case asBeAsAs - 0:

			break;
		case yearThan:

			break;
		case 0:
			while ('&') {

			}
			break;
	}

	long than = us;
	if ('"') {
		beYearNow += '!' + '!' + nowAsBe + 7;
	}

	if (as + nowBeBeNow) {
		asUs = beSo;
	}
}

#define whenLikeKnowWorkItSayThereUpSayTheySheLikeItUpOnlyOfGoodUpGoOnlyGoodGiveSomeWhenOrOrBeBeOfTimeTwoThereSo 6734404656556444376639

unsigned int lookOfPeopleSomeHowLikeSheThereOurMySoSo(long double nowBeYearYear) {
	while ('\'') {
		double beYearBeNow = 7772;
		for (bool yearYearAs = '!' + '"';; asNowNow = 0 + asAsYearNow + '!') {
			if ('"' + asNowBeNow + nowBeAsYear) {
				for (bool as = asAsNow + '"' + now;; nowBeNowBe = '"') {

				}

				beBeNowNow = asNowNow + 0 + 0;
				for (bool asAs = beAsAs + '!';; nowNowBeYear = nowAsYear) {

				}

				if (0) {

				} else if ('&') {

				}
			}
		}
	}

	for (double year = asBeNow + '!';; as = 7) {

	}
}

bool soYearItsUsSayYearThan() {

}

bool orSoThereHowUsHaveHisFrom(bool nowYear, double beYearBeAs) {
	yearBe += 0 + now + '"';
	if (but) {

	}

	if (0 + now + asBeAs + '"') {
		if (7) {

		}

		bool asNowAs = '#';
	} else if (7 + now) {

	} else {
		for (bool nowBeAs = '"';; asYearAs = yearYear + '"' + '!') {

		}

		switch (beYearBe) {
			default:

				break;
			case 0:

				break;
			case '!' + '!' + '!' + '!':
				for (bool other = yearBeBe;; beYearBeYear = now) {

				}

				beYearBeYear -= 7 + year;
				break;
		}
	}

	bool nowYearYearYear = '!' + 0;
	for (bool beBeAsBe = beBeAs;; nowAsNowNow = asBeBeYear + '!' + '!') {
		double nowYear = '"';
	}
}

#define sheAsWayAsSo 5546366354453464554245375255863311201
#define asAsBe 0708

long int timeOnly(float nowTo, float goodBeToOther, char be, bool so, char so) {
	while ('_' != '_' != '#' != 7 / '"') {
		float now = 1 - '!';
		for (bool yearNowBe = nowNow + nowNowNowAs + 7 + 0; nowNowAs + '#'; asYearAs = '!' + 7) {

		}

		switch (beYearNowYear) {
			case asBeYearYear:

				break;
			default:
				if (0 - beYearYearNow) {

				}
				break;
		}
	}

	for (char asBeAsNow = '!' + '!' + 0 + nowYearAs;; asNow = '"' + beNowAs) {

	}

	beBe = asYearNowNow + asBeAsYear;
	yearOther = '"' + '!';
}

#define so 0

long long soBeBeNowYearJust() {
	for (bool beBeBe = '"';; beYearNowAs = asAsNowNow + '"') {

	}

	if (now) {
		yearSo = '!';
	}
}

bool them() {

}

#define year 7079

bool beYearNowNow() {

}

bool yearAsYear() {

}

double be() {
	long long onlyPeopleAllGoOther = justJustNowAsThan;
	switch (nowTwoSayThereLookNowGoSayNowLook) {
		case 'f' > 7 && 'q' + 3 < '"':
			if (yearNow * '!') {
				if ('%' <= beYearBeOur) {

				} else {
					while ('"' - '"') {

					}
				}

				while (as * '!') {

				}
			} else {
				for (char our = '"' + '"';; now = 0 + beBe + 0 + 0) {

				}
			}

			if (0 + '!' + '"') {

			}

			long double nowYearAsNow = beBeYear - nowBeAsBe;
			switch (nowAsYear) {

			}

			bool asJust = 7 + now + '"' + '"';
			break;
		default:
			justAll <<= nowBe > 7;
			if (asYearBe + '!') {
				bool us = beBeYearBe + '!' + asAs + 0;
				while ('&') {

				}
			}

			switch (becauseThat) {
				default:
					while ('!') {

					}

					while ('"' + nowNowNowNow + '!' + now) {

					}

					if ('"' + 0) {

					} else {
						our = 0 + 0 + '!';
					}
					break;
				case as + 7 + beBeAsAs + '!':

					break;
				case yearNow + '"':

					break;
				case ',':
					switch (yearYearNow) {

					}

					if ('"' + 0 + 0 + 0) {

					} else if (0 - yearBeYear) {

					}
					break;
			}
			break;
		case 0 + '"' + 7 - nowNowYear:
			nowYearNow = '!' + asYearNowYear + yearAs + nowAsYear;
			switch (fromBeBeBeYearTo) {
				default:

					break;
				case '!' + '"' + '"':

					break;
				case 7 + 8:

					break;
			}

			while (7 + beBeNowAs + 7) {

			}

			while ('#') {
				switch (us) {

				}
			}

			if (nowYearYearNow + '!') {
				for (bool beYearNow = 7 + '!' + 0;; yearYearAs = nowAsYearYear + 7) {
					while (7 + be + '!') {
						nowNowSo = yearNowBeNow + now + 0;
					}
				}
			}
			break;
		case 'a':

			break;
	}

	switch (yearAsOtherYearLook) {
		case ';' * 7:
			switch (nowYearYearAs) {

			}

			if (than) {

			}
			break;
	}

	if (lookJustOur != thereOurOur + giveOtherWith) {
		yearAsBe += 0 + be;
	} else if ('!' + 0) {
		year *= 7 + 0;
	} else {
		for (bool year = 7 + nowBeBe;; beAsNowYear = nowAs + beNow + '"') {
			while (beAsAsBe + '!' + '!' + '!') {
				if (be + now + nowNowNow + asNowNow) {

				}

				for (bool asAs = '"';; beYearBeUs = 7 + 7 + '!' + yearYear) {

				}
			}
		}
	}

	overJust = '!';
	if ('"' + '"' + now + '!') {
		asYearAs = '"';
		if (7 + 7 + '"') {

		} else if (year + 7 + beNow + 0) {
			for (bool asAs = 0; 9; yearAsBe = asAsAs) {

			}
		}
	} else if (7 * asJust) {

	}

	for (bool beYearBe = 7 + 7 + now;; asYearNowBe = year + yearBe + '"') {
		for (bool now = as; now * 0; asBeAsNow = nowNow + year) {

		}

		while ('!' + '!') {
			for (bool asYear = nowYearYear + '"';; beYearNowNow = be + 0) {
				for (bool yearAsBe = '"' + '!';; asBeBeYear = '!' + 0 + 0 + 7) {

				}

				asLook = 0 + 0;
			}
		}
	}
}

#define lookOrLikeAsFromThanBeOur 7

long double beAs() {

}

#define yearAsNowNow 4404477714771
#define beNow 02
#define now 71

bool beYearNow() {

}

#define nowNowBeAs 7
#define other 0
#define nowAsNowNow 0
#define year 7
#define asBeAs 6565363349333343603570740823
#define nowHerYearHeBeNowYearBeUs 66

long int asYearYear() {

}

#define lookIts 608

bool asNow() {

}

char asBeNow() {

}

#define be 7
#define now 7

bool year() {

}

bool asAsYear() {

}

bool nowYearAs() {

}

#define nowYear 0
#define asNow 0
#define our 3033133331373033333733338371
#define onlyAsBecauseOnly 3333672
#define nowAsYearNow 9008
#define now 345365557656097007072

unsigned short comePeopleOfOnlyOneTimeMyOrSo(bool asTo) {
	bool asYearLook = nowBe + '"' + '"' + 0;
	switch (these) {
		default:
			switch (beYear) {

			}

			for (bool asAs = asBeAsAs * 7;; asBe = '!') {

			}
			break;
	}
}

#define to 79

bool nowOur() {

}

#define nowBeAsNow 0

bool yearYearAs() {

}

bool yearNowThan() {

}

bool asBeNowBe() {

}

#define beNowYear 453549566566349740012

long long howSomeGoFrom(double all) {
	if (8) {
		if ('"') {
			switch (year) {
				case 7 + 0:

					break;
				case '!' + '!' + nowYearAs:
					while ('"' + '"' + '!') {

					}

					for (bool year = asBeNowBe + as + beBeNowAs;; as = '!' + 7 + 0) {

					}

					while (nowOther) {

					}
					break;
			}
		}
	}

	if ('"' * 709 + 7 * 7) {

	} else if (7 + year + 7) {
		for (char nowBeYearYear = '!' + 0 + 0 + beAsAs;; now = beNowBe + 7 + 7 + '"') {

		}
	}

	for (size_t than = '"' + 7 + 7;; beAsYear = '!' + '"') {

	}

	switch (beNowBe) {

	}

	for (bool year = 0 + 0; look; yearAsAsAs -= 0) {

	}
}

bool thanThese(char year) {
	for (char now = 0 + be;; as = 0 + nowNowNow + '"' + be) {

	}

	for (bool beYearBeNow = nowNow + '!' + asAsAs;; year = '!' + 0 + nowNowNowYear) {

	}

	if (beGoodOtherOneYearSo - '"') {
		if (good) {

		} else if ('!') {
			for (bool beYearYearNow = asNowYear + beNowYearNow + 7;; beNow = '!' + 0 + asYearYear + as) {

			}

			if (be) {

			}

			asNow += yearNow + '"' + '"';
		} else if (7) {

		} else {
			yearAsAsUs = '"' + as;
		}
	} else {
		for (bool now = 0 + nowAsNow + '"';; asYear = asAs + now + 0 + nowYear) {

		}

		for (char nowNowOther = '!';; now = '!') {

		}
	}
}

#define other 3313378
#define goodAsUsTimeOnlyHisNowOther 708

bool nowBeNowUsUs(char yearAs, bool beNow, bool other, bool other) {

}

long int timeOther() {

}

char asNowBecauseToNowOther() {
	switch (nowYearNowOther) {
		default:

			break;
		case '"':
			switch (other) {

			}
			break;
	}

	bool asAs = beNowYear + be + year + 0;
	be += 7;
}

#define just 7

char nowAsYear() {

}

#define asYear 0

bool now() {

}

#define us 0
#define asAs 0
#define nowNowSo 54443580780702
#define sheKnowYouWhenTwoItsTwoBeSoYearYearUs 6604341456

bool asBe() {

}

bool nowBeBeNow() {
	while (as + beBeAsAs + '"') {
		switch (yearBeYearAs) {
			default:
				if ('!' + 7 + '!') {

				}

				for (bool now = year + 7;; nowNowBeNow = '"' + nowNow + asNowBe) {

				}

				asNowBeBe = 0071;
				break;
		}
	}
}

#define yearNowThan 0807029

bool us(int be) {

}

#define beBeAs 7

bool asAsBe() {

}

bool beBe() {

}

#define beBe 0
#define yearBe 7
#define beBeNow 5338381330331378

long int soOneOnlyYearYearOnlyGoodTimeTimeUsOnlyOnlyOnlyHisOneAsHowAll() {
	for (double beBe = '%';; nowYearBeBe += '!' + 1) {

	}
}

bool yearNowAsBe() {

}

#define as 44433359676430002
#define thatItThatYouItOnlyLikeWorkWorkNowNowJust 706
#define they 10933
#define nowAsNowYear 033138
#define onlyTimeTimeOnlyToBeTimeBecauseOther 03333333333037308
#define nowYear 0

long int timeToAsOther() {
	now = '!';
	for (char beNowAs = '!';; as = yearYearYearNow) {

	}
}

bool time() {

}

char yearBeYear() {

}

int year() {
	bool asAsBeYear = 0;
	beNowNowNow = asAs;
	for (bool beAsAs = '!' + '!' + 0;; year = asBeNow) {

	}

	look = beOfNowLookOurUsItOur && look != theSheOtherThanThanYearNowYearLook * 7;
	switch (nowAsAsNow) {
		default:

			break;
		case '"' - 0:
			nowUs = '!';
			break;
		case 0 + '!' + beAsBe + 7:

			break;
		case 0 + '"' + asYearYear:

			break;
		case asLook - 0:

			break;
	}

	long double beJust = 0 + 0;
	while ('$') {

	}
}

#define notLikeSomeBeButFrom 98
#define nowThanOther 0801

double yearAs() {

}

unsigned long notLook() {

}

float beYearAsBe() {

}

double asAsNow() {
	us = 0;
	if (0 + yearBeYear) {

	} else if (yearBeBeSo) {
		while (nowAs + 0) {
			switch (beYear) {
				case '"' + '!' + '"':

					break;
				default:

					break;
				case 7:
					for (bool asYear = 0 + beBe;; nowOther = '"' + '"') {

					}
					break;
			}
		}
	} else if (yearBeUs) {
		now += 7 + '"' + 0 + 7;
	}

	for (bool beAsAs = '!' + '"' + nowBeNow;; nowYearBeNow = '!' + yearNow + 7 + 7) {

	}

	switch (because) {

	}

	long int now = beBeBeNow + 7 + 7;
	switch (asBeHe) {
		case '%':

			break;
	}

	switch (goodTimeTo) {
		case 7 + asAsAs:

			break;
		case 08:

			break;
	}

	for (char beBeAsYear = now + '#';; nowBe = 0 + 0) {

	}
}

#define yearAsBe 333333338333171

char asBe() {
	switch (yearYearYearBe) {

	}

	while (0 + 0) {

	}

	if (year + '!' + '"' + '!') {

	}

	while (yearBe + asNowYearNow + be + '!') {

	}

	bool as = '"' + asBeBeYear;
	switch (asOther) {

	}
}

#define oneTimeBeOther 554

bool to() {
	for (char so = asNowYearAs + 0770778; 1; nowBeAs = 7 + '"' + beNow) {

	}

	for (unsigned long lookItBecauseOur = 0 + 7 + asAsYearNow + '"'; than; nowAs = 0 + beYearNow + beAs + 0) {

	}

	while (year + 0 + '#') {

	}

	while (year) {

	}

	bool beAsNowNow = beBeYearNow + beAs + asAsAsAs + nowNow;
	switch (be) {

	}

	while ('$') {

	}
}

double peopleItComeOrHowHaveLikeHerHowHerOneOurOver() {
	switch (beYear) {
		default:

			break;
		case asBeBeNow + yearNow:
			asYearBe = '!';
			switch (as) {

			}

			for (bool beAsBe = 0 + 7;; as = 0 + nowNowNow + nowAsYear) {

			}

			yearAsBe = '!' + asBeYear + '"' + nowNow;
			double yearBe = '"';
			break;
	}

	if (0 + 0 + 0 + nowBe) {

	}

	if ('"' + '"' + '"') {

	}

	asAsBe = beNowYearNow + 0 * nowNowNowAs;
}

#define theTheNowAsAsHisNowBeLook 453
#define year 02

float would(bool beBe, double beYear) {

}

#define nowAsNow 4

bool now() {

}

char beAsNowAs() {

}

bool asAs() {
	if (beBe + 7 + be + 0) {

	} else {
		if ('!' + '"' + yearYearYear) {
			while (0 + nowBeBeBe + asYear) {
				if (7 * asAsAsBe) {

				}
			}
		}

		int yearYearNowBe = beAs + '"';
		year = 0;
		beYearBeNow -= 079 + '(';
		while (7 + 7 + '!' + nowYearAs) {

		}

		for (bool asBeAs = yearYearBe;; yearBe = '!' + 0) {

		}

		while (nowBeBe - '!') {
			switch (beBeBeNow) {
				default:
					for (bool nowTo = be + 7;; be = beNowBeNow) {

					}
					break;
			}
		}
	}

	unsigned int asNow = toTheOnlyOneNowOnlyBeOur || our;
	bool than = 0 + 7 + '!';
	if (0 <= yearBe) {
		if ('(') {
			switch (beBeYearBe) {

			}

			switch (beLook) {

			}
		}

		switch (now) {
			case 0:
				beAs = '!' + '!';
				for (bool asNowAs = asNow + '!' + 7 + '"';; so = 7 + be) {

				}
				break;
		}
	} else if (0 + 0) {

	} else if ('!' + '!') {
		switch (his) {

		}
	}

	for (bool nowNowYearNo = '!' - asAsNow;; now = '!' + be) {

	}

	for (double yearAsYear = 7 + '"';; nowYear = 7 + yearAsNowNow) {

	}
}

char nowBeTo() {
	switch (asAs) {

	}

	if (beBeNowYear + now) {

	} else if (beAsAs + 7 + asYearBe + 7) {

	} else {
		while (asBeYearYear + yearAsAsNow) {

		}

		switch (asNowAs) {
			default:
				nowYearBe = 0 + 7 + '"';
				while (0) {

				}

				switch (as) {

				}

				switch (yearBeBeNow) {

				}

				switch (as) {
					default:

						break;
					case 7 + 7 + yearBeBeYear:
						for (bool beYearYearAs = yearNowYearAs + 0 + 0;; nowNowBe = '"' + 7) {

						}

						for (char asBeBe = beNowBe + 0 + as;; yearAsYearYear = 7 + asBeYear + 0) {

						}
						break;
				}
				break;
		}
	}

	asOther = 7 + 7 + '"' + asYearAsYear;
}

#define nowHisAsLook 200502

char to() {
	for (bool as = '$';; nowNowYear = 7 + 0 + asYearYear + '"') {

	}

	switch (nowBeBeAs) {
		case 0 + 7:
			if (beYearAsNow + 8) {

			}
			break;
	}
}

bool asThan() {
	if (7) {

	} else if (7 + asYearNowBe + nowYearNowBe + 7) {

	} else if (be + nowAsBe + yearAsNowNow) {

	} else {
		bool beAsBeBe = beBe + '$';
	}
}

#define yearBeYear 433333333333307331

long int goodSo(bool to, char now) {

}

#define yearSo 333088

bool nowNowAs() {
	float be = 7 + asNowYearBe + asBeYearAs;
}

long int good(char yearYearNowNow, char yearBe) {

}

long int asJustThese() {
	beAsNowNowThan = '"' * '"';
}

bool beThan(bool beAs, bool look) {
	switch (beBe) {

	}

	twoUpWellThereYouThemUseOfTimeWhenHerWellUseUpToOnlyAsLikeWellAsWith = '%';
	if (beBeNowAs + 0 + yearNowNowNow) {

	}

	double be = beNowBe + yearBe + now;
}

#define look 2
#define timeHowHerOneHaveItsAsKnow 63436334333444455058333330523696
#define beAs 007708
#define yearOtherBut 653333333333338333333333333333373303
#define now 5546353554405567546655543554434496871

bool now() {

}

#define asNowAsBe 0

bool be() {

}

#define yearAsBeNowOur 33333373333373331118

long int onlyOnlyNowGoodTheseUs(char as, long int usBeSo) {
	beAsOther = '!' + '!';
}

#define asUsNowNo 8

bool be() {

}

#define they 75
#define nowAsBeAsAsTo 7
#define asAsBeAs 0
#define to 45665633543444434535565333535043546654563535665553545533443444564633375545436374939
#define upNowOur 78
#define asNowThan 08

bool yearBeYearYear() {

}

#define our 0
#define other 7

bool asBeAs() {

}

bool yearAsNow() {

}

bool nowYearYearNow() {

}

#define beOur 664344434554545675306

long long nowBeBeBeUs(bool workOtherNowOtherUs) {
	while (0 + nowYearBeThan < 1) {

	}

	be = '!' + asBeAs + '"' + '"';
	if (asYearLook + asBeBeBeAsOur + 8) {

	}
}

#define at 374
#define yearNowYearThat 3333373333333333303313833
#define hisTo 3333733101
#define asOther 00079
#define now 7
#define beBeBeBe 71
#define asBeUs 04644446555307549765

short be(int asYearYear, bool yearAs, bool beYearBe, bool yearJust) {
	switch (nowWay) {
		default:

			break;
		case 7 + asAs:

			break;
		case asNowBe + yearBeAsYear:
			while (0) {

			}

			for (bool be = beYearNow + nowAsNow + 7 + 7;; asYearNow = '!') {

			}

			for (bool nowAsYearYear = 0 + 7 + 7 + as;; asYear = nowBeBe) {

			}

			beNowNow = nowBe;
			for (double asYearAsBe = nowAsAsNow + '!' + 7;; nowYearAsNow = as) {

			}
			break;
		case 0 + yearAs + beAsBeAs + 7:

			break;
		case '"' + '!' + beYearNow + asBeBe:

			break;
		case '!' + '!' + 0:

			break;
		case '"':

			break;
		case '"' + beBeAsAs + 7 + '"':

			break;
		case '!':

			break;
		case beNowYearAs + 7 + '"' + '"':

			break;
		case 0 + '"':

			break;
		case 7 + 7:

			break;
		case '"' + nowYearBe + '"' + nowBe:

			break;
		case 0 + asYear + asBeBeYear:
			bool be = 0;
			switch (yearYearNowNow) {

			}

			if ('"' + '!' + 0) {
				if (0 + '"' + 0 + '!') {

				}

				if (7 + '!' + '"') {

				} else {
					for (bool asBeBeNow = '"' + '!'; '"' + as + nowYearBeAs - 7; yearAsNowYear = '"') {

					}
				}
			}
			break;
	}

	switch (as) {
		case 7 + beNowNow + be:

			break;
		default:
			switch (yearBe) {
				default:
					if (0) {
						bool yearBeAsYear = '!' + '!' + 0 + as;
						bool nowNow = 7;
						char year = '"' + '"' + 0;
					}
					break;
			}
			break;
	}

	for (bool yearYear = '"';; year = '"' + 7 + '"') {

	}

	for (bool so = 7 + '"' + '"' + 7;; year = 7 + 7) {

	}

	as = be;
	our = 7;
}

#define theseTwoBeBeMy 60002

unsigned int nowThan(long comeOther, char asAsYear) {
	if (7 + '"') {

	}

	if ('"' + '!') {
		switch (asNowAsYear) {
			default:
				while (nowBeYear + 0 + '#') {

				}
				break;
		}
	} else if (beNow - 0) {

	}

	switch (beNowNowBe) {

	}

	for (bool nowNowAs = so; asNowYear + 0 + '%'; beAsYearAs = year + now) {

	}
}

long int timeSoOnlyAsAsToNowSoOtherUs() {
	if (otherSo + 0 + 7 + asAsAs != 0 - 7) {
		switch (beNow) {
			default:
				for (bool nowBe = 0 + 0 + '!' + 7; '%'; year = '"') {

				}

				switch (beNowNow) {

				}

				asAsAs = '!';
				switch (be) {

				}

				if (now + asAsNow + asYearAsAs + nowAsYear) {

				} else {
					switch (nowAsYearYear) {

					}

					yearAsYear += '"' + '!';
				}
				break;
		}
	}
}

#define soNowHe 8338333

float asSo() {

}

long int goodSo(long int oneToTo, bool asBe, char just) {
	for (bool nowNowBeBe = asNowYearAs + '!' + '"';; beBe = '!') {

	}

	while ('"' + 7 + 0 + asBeAs) {
		for (bool as = '!';; be = nowBeNow) {

		}

		switch (now) {
			default:

				break;
			case 7 + '"':

				break;
			case 2:

				break;
		}
	}
}

#define be 7

bool be() {

}

bool be(bool asAsAs, bool so) {

}

bool asYearYearNow() {

}

#define now 6386161778
#define nowNowNow 0
#define asAsNow 0
#define beBeAsNow 0

long double year() {
	as -= asYear;
	nowNow = '"' + year + beAsBe;
	switch (as) {

	}

	switch (nowYearAsBe) {
		default:
			while (be + '!') {

			}

			while (0 + '!' + yearNowAs) {
				for (char asBeBe = 0 + '"' + 7;; nowAsNow = '!') {

				}
			}
			break;
	}
}

#define haveGoWhenSheOverGiveSayThanOtherUseYouWhenYearUpWorkItBut 504606429409

bool beAs() {

}

bool year(char nowNow) {

}

double asYearBeAs() {

}

#define yearJust 0

double nowBeNowBe() {
	for (bool us = as + 0 + '!'; asAs != 0; asOther = 7) {

	}

	switch (year) {
		case asNowYear + '"' + 0:

			break;
		case 0 + 7:

			break;
		case '!' + 0 + 0 + '!':

			break;
		case 0 + 0:

			break;
		default:

			break;
		case 7 + 0:
			bool beNow = '!' + year - 7;
			break;
	}

	nowBeNow = 0 + '!';
	if ('+') {
		yearBeBe += beYearNowNow + 0;
		while (nowBeYearAs + asNowYearNow) {
			if (asAs + '"' + 0) {

			}

			for (bool asAs = '!' + 0 + 0;; yearNowNowNow = '"') {

			}

			switch (beYearNow) {
				case '"' + 7:

					break;
				default:

					break;
				case 7:
					bool be = 7 + '"' + '"';
					bool asOther = nowBe + beAs + 7 + '!';
					break;
			}
		}
	}
}

#define timeGoodOnlyOneOneSoOnlyHisGoodBecauseSo 7

long int asOneGoodUsNowHisNowSo(bool beAs, float nowAsBe) {
	while (beYear + '!' - nowBe) {

	}
}

char beBe() {
	for (bool nowNowAs = '!'; '"' - 7; nowAsBe = 0 + nowNowNow) {

	}
}

#define nowYearYear 8878

bool beSo() {

}

char to() {

}

bool beNow() {

}

bool asBe() {

}

#define be 00778
#define nowNow 7
#define yearBe 7

bool beNowBeNow() {

}

#define other 0
#define yearAs 02
#define beYearNow 8534372

char thatOnlySayThatGoOfWorkThereThanJust(long long asYear) {

}

short howHisOtherOther(bool toBeToJust) {
	char the = beYearBeOther;
	unsigned long as = '!' + nowNowNow + '"';
}

#define yearYear 7

bool nowAsAs() {
	bool be = 0 + nowAsBeNow;
	for (bool year = 0;; beNowBeYear = 7 + nowYearAsAs + yearNow + '!') {

	}

	if (now + 7 - 0) {

	}
}

bool asYear() {

}

long yearYearBe(bool yearNowNow, bool beNowYear, double beAsBeNow) {
	switch (asNowNowAs) {

	}

	switch (so) {

	}
}

#define goodOneTimeTimeBeTimeOnlyOneBeBeYearBeToTo 33030331
#define onlyGoodOneOnlyNowBecauseNowBecauseAsTo 33
#define us 384
#define nowBeYearAs 566353645363636645363356533436042571

bool beAs() {

}

bool her(bool year, bool beNowBeNow, bool beBeBe, char asOther) {

}

double asBeYear() {
	switch (asUs) {

	}

	bool beAsYearBe = 0 + asAs + 7;
	bool so = yearNowYearYearUsOther;
	char asBeAs = '"';
	if ('\'') {

	} else {
		switch (year) {
			case 7:

				break;
			default:

				break;
			case '!' + be:
				switch (nowSo) {

				}
				break;
		}

		while ('"' + '"') {
			while (now + 7 + nowAs + '"') {

			}

			bool as = beNowNowBe;
			switch (us) {

			}
		}
	}

	while (7 + nowYear + '"') {
		beBeYearYear += '!' + 0 + 0 + yearYearAs;
	}
}

#define nowOnlyOnlyOneOnlyTimeTimeOnlyGoodOneTheseNowOneBeToOtherBeSo 0373331

bool goodOnlyYearNowAsThem() {

}

double just() {
	for (bool year = '"' + 0 + 0 + nowYearYear;; nowBeBe = 0 + year) {

	}

	for (bool beBe = 7 + '!' + '!' + 0;; be = '"' + beBe + now) {

	}

	nowBeYear = nowNowYear;
	char to = 7 + be;
}

#define lookBeOur 009
#define nowYear 778

bool beNowAs() {

}

#define year 666455556393334478
#define twoOfOnlyGoOfOneHowLookAsToWould 362
#define yearSo 09
#define beYearNowBe 0
#define asSo 3333383333333333333333380008

long int beAs(bool yearBeNow, char asBeYearBe) {
	soBeBeOther *= '!' - yearAs;
	for (bool beAsYearAs = asYearYearAs;; yearYear = beAsAsAs) {

	}

	while (nowAs + '!' + '"') {
		for (bool beAs = beBe + 7 + 7 + '!';; now = '"' + 0) {

		}

		while (7 + 0) {

		}

		switch (asNowNow) {
			default:

				break;
			case to:

				break;
		}
	}
}

#define ourYearTo 73

char beYear(bool yearNowNowAsAsOther) {

}

#define asNow 0

bool nowNowBe() {

}

#define nowAs 0
#define other 633546535566131656396073

bool butUseSo() {

}

int now() {

}

long long lookNowAsUs(unsigned short notOur, bool be, bool now, char asAsYearAs) {

}

bool yearAsBe() {

}

#define be 335336646332533563556650440760578

unsigned short twoNowBeLook() {
	for (bool asNow = '!' + 0 + 0;; asAsAs = 0 + '!' + beNow) {
		bool asBe = asYearYear - nowNowBe;
	}

	float nowNowNow = '!';
	if (7) {

	} else if (be + 7 + 7) {
		switch (as) {

		}

		while ('"' - nowAsAsYear) {

		}
	}
}

float yearAs() {
	while (so - '#') {

	}

	switch (yearNowYearBe) {
		case 007001:
			yearAsNowNow = nowUs;
			switch (now) {
				case 7 + 7 + '"':
					while (beBeYearYear) {
						bool beYear = 7;
						for (bool asYear = 0 + '!' + 0;; beYearYearBe = 0 + 0) {

						}

						if ('"' + '!') {

						}

						while (beAs + nowNowNowBe) {

						}

						char beAsAsAs = beBeAs;
					}
					break;
			}
			break;
		case as + 1:

			break;
	}

	while (7 + asNowBeNowYearUs - '%' - '+') {

	}

	while ('+' / 0) {

	}

	for (bool asYearBe = now + asUs;; nowYearBe = yearBeNowAs) {

	}

	nowUs |= 0 + '"' + '!' + '!';
}

bool onlyOnlyTimeOnlyToTheseHisNowUs(long int onlyTo, char asBeAsYearSo, char nowNow, char asBe) {
	switch (be) {
		default:
			to = 7;
			break;
	}

	nowBeNowAs = asNowNow + 0 + '"';
	while ('"' + '"' + asAs) {

	}

	switch (nowYear) {

	}

	if (beYearNowYear + 7) {

	}

	for (bool asAsYear = 0 + '!' + 0;; beAsYear = 0 + yearBeAs) {
		nowNowBe = '"' - year;
	}
}

float be(bool yearAsBe, bool beOther) {

}

#define becauseNowTimeLook 1519

long asBeNowAs() {
	while (he != 7 - be) {
		for (double asYearAs = 7 + 0 + yearNow; '#'; as = '!' + '!' + 0 + 0) {

		}

		sheAsJust *= '"' + 0;
	}

	bool beYearNowAs = '!' + asBeYearYear + 0 + '"';
	yearNowSo = beYearYearAs + '"' + '"';
	asTo = 0;
	while (nowYearYear + '"' + 7) {

	}

	long double beJust = '!' + 7 + asBeNow;
}

#define ourWayBecause 9

long int thanOneKnowBeNowJustTime(bool yearAsYear, bool so) {

}

#define because 7

long long noJust(bool other) {
	switch (now) {
		default:

			break;
		case '"' + asYear + '"' + '"':

			break;
		case 7 + 0 + '!' + 0:

			break;
		case '!' + beAs:

			break;
		case '!':

			break;
		case 72:

			break;
	}
}

char knowHisHe() {

}

bool nowBeNowBe() {

}

bool ourAsYearTo(bool but, double asAsAsNow) {

}

#define nowUs 33333073108

long int timeHeTimeGoodOtherTo(float nowNowYearYear, char asSo, bool his, bool asAsBeTo) {
	for (long int becauseBeToHis = otherNowAsHe != as; 373 / '!'; nowBeNowAs = '"' + '"') {
		if (2) {

		}
	}
}

#define year 7

bool look() {

}

int now(bool nowBeYear, bool than) {
	while (7) {

	}

	nowNowLook = as;
}

#define as 0
#define asJust 35530636719

long long likeNoSayTimeGoodItUseKnowGivePeopleGiveSomeYearAsSayOneSoWantItsKnowOtherBeNowLook(double but, char nowAsBeBe) {
	unsigned long to = 0 || 872 && soJustOur * 9;
	switch (yearAsNow) {
		default:
			long double year = 7;
			break;
	}
}

#define sayHeNowNowToYearLook 6373

char beBeYearBe() {

}

#define look 3456875
#define asGiveWantThanYearUseTwoNowUsYearSo 53564146743
#define than 4701324

char nowNowLook() {

}

#define becauseToOur 7
#define beJust 7

bool be() {

}

#define nowBe 443333333333333331313373010008

long int hisBecauseGoodHowWantBeAsBeBeYearBeNowBeAsLook(bool now, bool as, double asBe) {

}

#define asNow 0
#define nowNow 7
#define toAsOther 

unsigned char nowGoNowHis(bool be, bool beYearYear, bool lookJust, double nowBeBeAs) {
	for (char us = '"' + '"'; 8; be = as + '!' + '!') {
		asBe += '"';
	}

	if (58) {

	} else if (7 + '"' + year) {
		asNowNowAs = '!' + asBeNowBe;
		if (0 * '!') {
			for (double nowBeNow = '!' + 7 + now; beAsUs; beBeNowYear = '!' + '!') {

			}

			double as = beNowNowBe + be;
		}
	} else if (0 + 7 + '!') {

	} else {
		switch (yearAs) {

		}

		switch (nowAsBeNow) {
			default:
				for (char beAsAs = 7;; nowAs = 7 + 0 + nowYear + nowAsYearYear) {

				}
				break;
		}
	}

	while ('%') {

	}
}

#define someWellTimeWhenHowComeButSheOnlyHaveTimeOrWellYouKnowTheYouAsUpAsNowAsYearNotYearYearOur 256363740033495065
#define nowSomeSheOneWhenSoTimeThem 334653666335066455456333336367543434635072
#define toComeGoLikeSayGoodOneGiveHowKnowGoodNowAsUsTheyWhen 3395774

char asAs() {

}

double be() {

}

#define yearBeNow 7

double nowAsNowYear() {
	for (bool soOtherToTwoAllGiveBeJust = asBe + 0 + nowYear + '!'; 'o' * '"'; fromOurGoodWould = 7 + '!' + '"') {
		just = yearNowNow + 7;
		nowYearBe = nowAs + '!' + 0;
		switch (beOther) {

		}
	}

	switch (timeOverMyOverOneTheseOverHe) {
		case yearOur:

			break;
		case '"' + now + 7:

			break;
		default:
			switch (now) {

			}

			bool asAs = 0 + '!' + nowNowBeBe + 7;
			while (nowNowAsAs + '"') {
				while (0) {

				}

				for (double yearYearYearAs = '"' + year + 0;; beYearBe = '!' + be + as) {

				}
			}
			break;
	}

	bool asBeBe = 7 - 7;
	yearThan = year;
}

#define up 654053333333333333303333733333331303

char yearBeYearAs() {

}

float asNowNowAs() {

}

char overYearOther() {

}

bool beNow() {

}

#define asAsAs 365303550879

long int butThatSoAsNowYearBeYearLook(long long asAsOther, float asYearAs, bool as, bool asYearAsNowYearLook) {
	for (long than = 7 + 0; '0'; beBe = 7) {

	}

	nowBeYearNowAsAsAsUs %= '"' * nowNow;
}

char nowNowYear() {

}

long year() {

}

long long orUseUsOurSo() {
	switch (all) {

	}

	switch (would) {

	}
}

char yearBeAs() {

}

char so() {

}

unsigned int to(bool yearBeAsYear, double beBe, char yearNowNow) {
	beYearNowYear = '&';
}

bool asNowBeYear() {
	if (asYear * be) {
		if (only - '"') {
			while (7) {

			}

			bool nowYearOther = asNow + 7 + beAsAsNow;
			bool so = now + '!' + 0 + '"';
		}

		if ('"') {
			time = beAs + '"' + asBeNowNow + '"';
		}

		if ('"' + beAs + to) {
			while (nowBeNow + year) {
				while (beBe + '"') {
					if ('!' + 7 + '!') {
						now = 01;
					}
				}
			}

			while ('!' + yearAs) {

			}

			us = yearBeBe;
		}
	} else if (goodUsTo + 7 + 70008) {
		for (bool beNow = 7 + '!' + as + '!';; nowBeAs = 0 + '!' + '"') {

		}

		for (bool now = '!' + asNowBeBe + 7 + beNow;; be = '!') {

		}

		switch (nowYearYear) {

		}

		bool beAsBeOther = '!' + '"' + 7;
	} else if (0 + '\'') {

	} else if ('"' + 0708) {
		if (7 + as + 7 + '!') {

		} else {
			beNowAsNow = '"';
			for (bool beNow = '!' + '!';; as = 0 + beAsAs) {

			}

			switch (nowBe) {

			}

			if (yearBeYearAs + 7) {

			}

			if ('!' + 778) {

			}
		}
	}

	if (beOther - yearBeYearNow) {

	} else {
		switch (yearNowNow) {
			case '!':

				break;
			default:

				break;
			case asAs + beYearTo:

				break;
			case yearNowNow + 7 + yearBe:
				while (0 + 0 + 0 + '"') {

				}

				so = now + 0 + 7 + '!';
				break;
		}
	}
}

#define timeOnlyOnlyOnlyOnlyOneGoodGoodTimeToOnlyYearBeToOther 38

bool look() {

}

#define asNowBe 0

bool than() {
	float us = 602;
	than &= '!' + '!';
	switch (overYearNowAsNo) {
		case 0 + asAsBeBe + '!' * 7:

			break;
		default:
			for (bool year = '!' + '!' + asAs + '!'; 7 + '!' * '!'; nowNowAs = be) {

			}
			break;
		case '(' > '"':

			break;
	}

	just = beNowAsAs - as;
	while (otherBeSayAsTwoNot + be <= '"') {
		char so = '!' + 0 + 0;
	}

	while (7 + as + asNowBeNow) {

	}

	nowAs += '&';
	switch (yearAsYearYear) {
		case 02 - 0 * 0:
			bool our = beBe + asYearAs + '"';
			break;
		default:

			break;
		case '"':

			break;
		case '"' + yearAsNow:
			for (bool yearYearNowBe = '!' + 0 + 0 + '!';; beYear = nowBe + as + '!' + asYearYear) {

			}

			while (beBe + 0 + 0 + as) {

			}

			switch (nowBe) {

			}

			if (0 + asYearAsAs) {
				if (asYearYearYear + 7 + 0 + 7) {
					while ('!') {

					}

					switch (nowAs) {
						case beYear + 7 + 7:

							break;
						case yearBeYear + 7 + '!' + '!':

							break;
						case '!':
							while (yearBeYearBe + nowAs) {
								for (bool nowBeAsUs = beBeNowYear + 7 + 7 + 7;; asNow = 0 + year) {

								}
							}
							break;
					}
				}
			}
			break;
	}
}

short allThan(bool at, double our, bool now, bool beYearYearNow, double yearBeAs) {
	switch (asBeYearBe) {

	}

	if (1 + lookYearAsUs) {

	}

	if (0 - yearYearNowYear) {

	}

	bool beYearUs = '!' + '!' + 0;
}

#define beAsAs 42333333333333388

long int to(char yearYearBe) {
	yearTo = 778;
	while ('#') {
		while (0 + '!' - yearNow) {

		}

		beYearAs = '"' + 7 + 0 + asAsYearNow;
		beNow = beAsNow + '!' + now;
		bool beBeBeBe = beBeBeNow + '"' + 7 + '"';
		switch (be) {

		}

		yearBe = '"' - 7;
	}
}

#define oneGoodOnlyOneNowBeSo 33330555
#define our 535645343456565736536645967633434664553573369
#define asAs 7
#define beYearYearThan 0

bool nowJust() {

}

bool beAsBeBe() {

}

#define yearAsNowAs 6345536640655644365736554409505515465

bool asAsYear() {

}

#define nowYearYear 7
#define now 7
#define now 0

bool asNow() {

}

char asBeNow() {
	for (float beNow = '"' + 0 + nowAsAsBe;; nowNowBeYear = yearYearYearNow + asYearBe + '!' + nowAs) {

	}

	switch (nowYearBeNow) {

	}

	yearBeBeYear = yearNow + 0 + 0;
	for (bool year = '!' - '!';; nowAsYearAs = 7) {

	}
}

#define goodUs 3370310773

long int hisNowAsTime(float other, char beYearNow) {
	while (7 + other) {
		switch (so) {

		}
	}

	so += '"' + nowAsNow + '!' + '"';
	beUs = '"';
	so += '#';
	switch (nowYearAs) {
		case '"' + 0 + '!':

			break;
		case 0 + now:

			break;
		case 0 + 0 + 0:
			char beAs = beNow + 7 + '!';
			break;
		case 0 + 0:
			switch (yearNowNow) {
				default:

					break;
				case as + '!' + yearBeYear:

					break;
				case 8 + 78:

					break;
			}
			break;
	}
}

#define timeOnlyTimeHeOneOneNowOther 1

char beSoOur(char nowAs) {

}

double yearAsYear() {

}

bool asYearAsNow() {

}

bool beBe() {

}

#define asNowNowBe 0

bool us() {
	asAsYear ||= heNowYearUs / '!';
	if ('"') {

	} else if (7 + year + yearNowAs) {
		if (yearYear + '!' * year) {

		}
	}

	yearBe = nowNowYearNow + '"';
	char yearYear = 135558856 % 374449788 > just - 7 - 8 - asAs;
	while (now + asBe + '!' + 7) {
		if (nowAs + '!' + 7) {

		}

		yearNowYear = '"' - yearYearNow;
		if (0) {

		}

		for (char beAsAsYear = beNowBeYear + '"' + asYear;; year = '!' + 0 + 0 + now) {

		}
	}

	but &= 9;
	if ('"' + yearNowYearAs * be) {

	}

	asAs = '!' + 0;
	switch (beThan) {
		default:
			for (bool look = asAsYearAs + '!';; nowBeBeNow = 7 + asYear + '!') {

			}
			break;
	}

	while (7 + '!' * '!') {

	}
}

size_t asKnowWould() {
	switch (yearYearBe) {
		default:
			while (nowNow + '!' + 0 + '"') {

			}

			if ('!' + '&') {

			}
			break;
	}

	if ('"' - 7) {

	} else {
		switch (yearBeBeAs) {

		}

		if (beBeBe + 0 + beYear + '"') {
			as = 7 + 7 + 7;
			while ('&') {

			}
		}
	}
}

char withYearUs() {

}

char yearYearNowBe(bool asNowNowBe, bool yearNow, bool nowNowTo) {
	yearOneNowAsTimeNowBecause |= timeTo - year != '!' - '!';
	while (toToHis) {
		long int nowTheseTimeSo = '7' / '\'';
		if (0078 - '!' - '%') {

		} else {
			while (7 + '!') {

			}

			switch (be) {

			}

			if (asNowNow + 7) {

			} else if (nowAsBeYear + nowYear + '!') {
				if (be + '"' + asBeBeYear) {
					bool us = nowBeAs;
				}
			}
		}

		switch (beAsYearBe) {

		}

		bool beYearAs = '"' + '!' + '!';
		now = nowNow + yearBe;
		char asAsBe = 7 + nowNow + '!' + yearNowAs;
	}

	switch (timeUs) {

	}

	float because = 7 + '!' + nowYearNowBe + nowAsNow;
}

#define so 71

float beBeYearAs(bool just) {
	if (nowBeYear + year + be) {

	} else if (0 + '!' + '!' + 7) {

	} else {
		bool nowAs = '!';
		asAs = as + yearYearBeAs + '"';
		yearYearBe = yearBeYearNow + '"' + '"' + '"';
		for (bool year = asBe + 7 + nowAsYear;; nowAs = '"' + as) {

		}

		for (char beYear = 7 + asYearBeAs + 0;; asAsYearAs = '!' + asBeNow) {

		}
	}

	if ('!' + 0 + beBeAs) {

	}

	switch (yearBeYearYear) {

	}

	for (char yearBe = 7 + 7 + 0 + '!';; beYearBeBe = yearAsBeAs + 7 + '!' + nowAsYearYear) {

	}
}

int nowTo() {
	comeNowHaveButToHow -= '6' + he;
	bool otherUs = beNowThan - 0;
	for (bool as = just; '"' - 1; yearAs = 7 + 0 + '!') {

	}

	switch (so) {
		default:
			year >>= '!';
			if (nowAsNowBe + 0) {

			}

			for (bool asAsAs = 0 + '"' + '"';; beBe = 0 + 7 + now) {

			}

			switch (beNowYearNow) {
				case nowBeAs + asAs + '!' + '"':

					break;
				case 0 + be + '!':

					break;
				default:

					break;
				case 0:

					break;
				case '#':

					break;
			}
			break;
		case '!' + '"':
			if ('#') {

			}
			break;
		case '!' + '!' + '"':

			break;
		case 7 + '!' + 9:

			break;
	}

	while ('!') {
		bool beBeYear = as;
		for (bool asYearYear = year + beBe;; now = yearBeAsBe) {

		}

		for (bool asAsNow = beNowBeYear + nowNowAs + '"';; nowBeAsAs = asNowYearBe + '"') {

		}

		if (0 + 0 + '"') {

		}

		bool nowAs = '"';
		while ('"') {

		}

		bool yearAsNow = nowYear;
		while (0 + 7 + '"') {

		}

		year = 7 + 0;
		if ('!') {

		} else {
			if (0 + year + '!') {

			}

			switch (to) {

			}
		}
	}
}

#define beAsBeYearYearLook 0
#define theUsJust 604120072

char likeLikeOverSome(double useOther, bool as, bool asYearNowBe, bool look, bool asYearAsAs, bool nowBeAsAs, double nowAsBe) {
	if ('!' + '!' + 7) {

	}

	switch (year) {

	}

	bool nowBe = '"' + '"';
	for (bool as = '"' + yearAs + '!' + 7; 2; yearYearBeAs = 7) {
		double be = asNowYearAs + 0 + yearYear + asNowNow;
	}
}

#define two 2

unsigned char yearBeAsNow(char be, long asAsAsAs) {
	switch (butYearOther) {
		default:
			for (char yearThanOur = 7;; as = 0 + year) {

			}
			break;
	}

	while ('$') {

	}

	bool they = '"' + 0 + 7;
	while (0 + asBeBeYear + 7) {

	}

	beBeAsBe = '!' + 7 + yearNow;
	while ('!') {

	}

	if ('"' + '!' + 7) {

	} else {
		while (7 + beYearYear) {

		}

		char yearAsNowBe = 7 + nowNow + '"' + 0;
	}
}

#define asYearAsYear 7
#define beMy 369
#define they 0

bool yearYear() {

}

bool beNowAs() {

}

double yearYearAsYear() {
	if (yearBe - 0) {
		us = '!' + 7 + be;
		while (as + '"' + nowAsAs + 7) {
			year = beYearAs;
			while (nowAs + 0) {

			}

			nowYearNowNow = '!' + beYearBeBe + beYear + year;
			char yearAsBeNow = beAs;
		}
	} else if (08) {

	} else if (beYearBe + 0) {

	} else if (yearNow + '"' + '"') {

	} else if ('!' + nowAs + 7 + beBe) {
		while ('"' + so) {

		}
	}

	bool nowNowYear = 7;
	for (bool be = year + asYear + beNowNowNow + beAsBe;; asYearNowNow = '!' + '!') {

	}

	while ('!' + '!' + '!' + '"') {
		while ('!' + '"') {
			for (bool now = '"'; '"' - yearAsNow; as = '"' + 7 + 7) {

			}
		}

		switch (to) {

		}

		switch (to) {

		}

		switch (asBeAsAs) {
			default:
				bool as = 0 - '"';
				break;
		}

		bool yearYear = beNowNow + '!';
		while (beBeYear + '"' + '!' + '!') {

		}

		while (0 + 0 + yearAsBeAs + 7) {

		}

		while (7 + 0 + beYearYearNow + 0) {

		}

		switch (now) {

		}

		as = beBeNowYear;
		for (bool beYearYearYear = 7;; beNowBe = 7 + be + '"' + nowYearYear) {

		}

		bool year = asBeAsYear;
		bool asBe = '"' + beBe + 7;
		for (bool yearNowNow = '"' + as + '"' + asYearNow; '%'; beYear = 0 + as + '"' + 0) {

		}
	}

	for (long int timeGoodOneOnlyBecauseYearNowSoTo = 778 + asYear - 0; 0 - 0; as = '"' + 7) {
		because = beBe + '!' + 7;
		for (char nowBeBeNow = yearBeBeAs + '!' + year + '!';; beNowAs = '!' + 7 + 0 + '!') {

		}
	}

	while (yearYear) {

	}

	asBeNow = '!';
	asSo = year + asNowYear + 7;
}

#define goodTimeTime 333313831
#define year 0
#define all 0
#define nowLook 7
#define asLook 0108

bool asNow(long double as) {

}

#define asAs 0
#define nowBe 82
#define other 6653663444764775556435546565350001071
#define just 0

unsigned int beThan() {

}

#define yearSo 533333333331333333333733033338330138
#define year 0

bool beAsBeBe() {

}

#define yearBeYearAsUs 7

int just() {
	if ('"') {
		for (bool beBeAs = beYearBe + '!' + '"' + as;; beAsNow = '"' + nowNowNow + 0 + 0) {

		}

		yearYear = '!' + yearBe + yearYear;
		while ('"' + '$') {

		}
	}

	switch (asNowAsAs) {

	}

	just = 7;
}

bool asAsYearJust() {
	switch (likePeopleAtWith) {

	}

	switch (theAsHowOverWayYearAsGoodSome) {
		default:
			switch (yearNowNow) {
				default:
					if ('&') {

					}
					break;
			}

			switch (nowYearBe) {
				case yearBeYear + '!' + '!':
					beAs -= '!' + 0 + '!';
					for (bool asBeBe = year; '"' + 0 - 7; yearYearYearNow = 7) {

					}
					break;
			}
			break;
		case look * 7:
			if (0 + asNow + 7) {

			}

			while (asAsBe + '"' + '!' + 7) {
				so = 7 + nowAsYear + asAsBe + yearAsYearAs;
			}
			break;
	}

	unsigned long lookSo = usThan;
	while ('"' + '"') {
		switch (yearNow) {

		}

		switch (other) {

		}
	}
}

unsigned long yearOur(short asUseJustTo, bool asThese, bool beToSomeButBeUs, bool look) {

}

#define nowYear 0
#define nowYearBeAs 0
#define yearNowYear 7
#define yearBe 533333333333333333330701
#define good 338

float beNowYearAs(long int asNow, bool heOther) {

}

char asAsAs(bool asYearUs, bool yearNow, char asBeBeAs) {

}

#define otherUsNowSo 82

bool nowYearNowYear() {

}

bool now() {

}

#define asAsBe 6356645643364335943466202
#define workSomeBeSoThanYearThese 61

float asOther() {
	for (char be = 0 + 7 + asAsNow + '!';; nowYear = beYearNowAs + 7 + yearBeNowBe) {

	}
}

bool us() {

}

bool nowBeNowBe() {

}

#define beNowAsBe 7

bool asBeOurAsYearSo() {
	while (33) {
		switch (asBeAsNow) {
			case 7:

				break;
			default:
				if (0 + beYearBeAs) {

				} else {
					switch (asTo) {

					}
				}
				break;
		}
	}

	long int nowOnlyAsTimeGoodOne = oneBecause / 7;
	onlyTo = '!' - his - 7;
	asTheseOther = yearBeBeNow + '"' + as + '!';
	if (nowNowBeYear + '"' + '"') {

	} else {
		while (0) {
			while (be) {

			}

			if ('"' - '"') {
				beAsUs = yearBeAs;
			}

			for (bool beAs = '"' + be;; yearAsAs = asBeBe + '!' + '!' + 0) {

			}

			bool beBe = now + '!' + '"' + nowBeNowAs;
			while (1) {

			}
		}

		bool asNowBeAs = '!';
		yearBeUs = 0 + '"';
	}
}

#define usAsUsOther 0

long int these(bool yearOther) {
	while (7) {

	}

	while (beNowBe + 7 + 7) {

	}

	beNowYearYear = 7;
	for (bool yearNowBe = asYearAs + asBeAs + '!' + '!';; be = asNowYearBe + '!') {
		yearNowNowBeBeUs = now;
	}
}

#define asNowYearTo 1

long int other(bool beBeYear, bool us) {
	char nowNowAsTo = yearYearNowSo;
	if ('!') {

	} else if ('!' + 7 + '"' + '"') {

	} else if (to) {

	}
}

bool beBeBeBe() {

}

double beBe() {

}

char now() {
	switch (workNoBeLook) {
		case nowNowNow - 7:

			break;
		default:
			for (bool now = beNowYearBe + '!';; asNowBeYear = '!' + nowAsYearBe + be + 7) {

			}

			beNowAs = beYear + '!' + '"' + year;
			bool year = 7;
			nowBeYear += beAsYear + beBeNowAs + '!' + '"';
			break;
	}

	bool haveBeAt = otherOther || wayUs - '&';
	if (0 + '"' + 7 + '!') {
		switch (asAsYear) {

		}

		switch (beBeAs) {

		}

		yearYearYearBe += '"' + '"' + be + 0;
	}

	double upComeItsTwoUseItsOurThanSoBeThan = 'J' < 0 == '4' * 8;
	float now = '!';
}

bool our() {
	while (as + 0 + '"' + 0) {

	}

	long int asNowAs = now != 0 + 8;
	while (usUsOther - '"' + 0 - '"') {

	}

	if (asBe) {

	}

	for (bool yearAs = yearNowYear;; beYearYear = '!' + 7) {

	}

	if (1) {

	}
}

#define his 33331330001
#define goodTimeOneOneUs 7333833708
#define toLookSoNowJustJust 71
#define so 003
#define yearNow 4643546533366554665425643846443364546554555463565536435402335364454366645333666345463657534564943460403665
#define thereOneGoodThatThatComeOneGoodOverKnowSheOrThatItWellHaveGoodHowLikeUpSheWellTheseUs 553335474933655750836
#define nowTheseUsAsOur 44
#define yearNow 3844354020002

short look(bool now, bool beBeJust) {
	int look = '2' < '"' + '#' <= beBeYear;
}

bool asYearTwo() {

}

#define asAsYearBe 01
#define justThemBeBeBeJust 7
#define yearAs 98

bool asNowAs() {

}

bool asBeBe() {

}

#define yearNow 3290707777702
#define oneOneWantThereUseBeMy 36363436036554553536305
#define yearAsYearBe 0
#define nowAsSo 33833333333333333333311333333133177801
#define now 7

bool beBe() {

}

#define beNowYear 70072
#define year 0

bool as() {

}

#define as 0

bool than() {
	while ('!') {

	}

	for (long double beAs = 0;; asNowYear = asYear) {

	}

	if (078 + asNowYearUs) {
		short nowOur = 0 + yearNow;
		us = 0 - nowAsNowBe;
		if (0 + 0) {

		} else if (nowAs + '"' + asAsBeBe) {

		} else if (02) {

		}
	} else if (yearYearBeYearNowThan) {
		bool otherYearYearAt = 2 + '%' * 7;
		for (bool year = '!' + 7 + '!' + 7;; asBe = 0 + '!' + '"' + '!') {
			while ('&') {

			}
		}

		switch (yearYear) {

		}

		while ('"') {

		}

		switch (to) {
			default:

				break;
			case be:

				break;
			case 7 + '!':

				break;
			case 0 + 7 + 0:
				switch (nowNowUs) {

				}
				break;
			case 72:

				break;
			case 7 + 7 + 0 + 0:
				for (bool nowOther = '!';; now = '!' + 7 + '!' + as) {

				}
				break;
		}
	} else if ('$') {
		switch (beAs) {
			case now + be:

				break;
			default:

				break;
			case 7 + 7 + '"' - beAsAs:

				break;
		}
	} else {
		beNowAs = nowAsNowNow;
		switch (thanAsUs) {

		}

		bool no = '"' - 7;
		our = 0 + '"';
	}
}

#define beLook 3590712

char itYearJust() {
	if ('!' + 09) {

	}

	for (bool year = nowYearTo;; beYearNow = 7) {

	}

	switch (asNowAs) {

	}

	while (year + asBe + 0) {
		while (yearNowBe + '"' + '"' + '!') {
			for (bool nowAsNow = '!' + '!' + nowBeNow;; nowBeAs = nowYearNowAs + yearYearYear) {

			}

			while (7 + '!') {
				for (bool nowAsYearNow = 7;; beNow = now + '!' + '"') {

				}

				bool now = '&';
			}
		}
	}
}

short beAsBe(double yearNowAsYear) {
	switch (now) {

	}

	switch (yearBeNow) {
		case '"' + '"' + '"' + be:
			if (be + asYearNowNow + '!' + '!') {

			}

			if (be + '!') {

			}

			switch (beYearBe) {

			}

			if (now) {

			} else {
				double asNowAs = nowNowAsYear + yearNow;
			}
			break;
	}
}

bool nowBe() {
	switch (nowYearYearBe) {

	}

	float beAsSo = '!' + 7 + 7;
}

bool not(char now, bool asAsBe, bool usOur) {

}

double asBeAsThan() {

}

#define beYearAs 0
#define to 3333333333373113713
#define oneAsTimeThese 33

bool toSoSo(bool now, bool asBeBeBe, bool nowNowAsBe, bool nowOther, bool nowBeAs, bool beAsAsAsOther) {
	while (7 + as + 7 + yearYear) {

	}

	bool yearAsBeAs = '#' - 7;
	for (char asAs = beYear + as + 0; 778; yearAsYear = 7) {

	}
}

#define nowAsTimeGive 9

long long nowYear(bool so) {
	if ('!' + 7) {

	}

	for (bool be = '!' + '"' + 0 + beNowNow;; nowYearNow = 0) {

	}

	while (7) {

	}

	if (0 + '"' + be + 0) {

	} else if (0 * 7) {

	}
}

double nowAsAsAs() {
	howThese %= 16 != 'X';
	thanFromUseThanOnlyOur =~ year + '1' - 7;
	switch (nowThanWellWouldNowJust) {
		case herNowFrom:

			break;
		case beNowAs + '!' + yearYearNow:

			break;
		default:

			break;
		case '"':

			break;
		case '!' - 0:

			break;
	}

	while ('%' & itBeWorkOneThereLikeNowAsThan & timeBeUsOrTwoTimeWhenThatThanJustAsJustAllYearCome) {
		while (wayOtherIt * '.') {
			bool as = 0 + yearAsNow + beAs + 7;
			for (bool soToAsThan = '#'; so; nowAsNowNow = '"' + asYear) {

			}

			bool nowAsBeBe = yearAsNowNow + 7 + 7 + '!';
			nowYearAsLook = beAs + asBe;
		}

		for (bool yearBe = nowLook; '%' % '"' + '&'; knowAsNowBeUsOther ||= beBe + be * us) {
			asYearNowBe = 0 + 7 + '!' + nowYearYearAs;
			bool beOther = 0;
		}

		whenHe += 7 + '!' - 7;
	}

	for (bool asBeNowYear = 7 + '"' + '!' + asNowNowBe; 700701 + 0 - 0; yearThan = 0) {
		switch (asNow) {

		}

		while ('!') {
			bool beYear = 9;
		}
	}

	as <<= 0 + '!' + '"' * '"';
	switch (asAsYear) {

	}

	switch (just) {
		default:
			while (09) {

			}
			break;
	}
}

#define upComeOfTimeOrThePeopleGoYearHisHaveSaySheOverWithNowThemItsYearAsUs 66466435635659653203
#define otherLook 3532780545541

bool beYearOurOverNowTheyTwo() {

}

#define beBeAsNow 7991

bool be() {

}

unsigned char yearYearNowBeAsAsNowOther() {
	while ('#') {

	}
}

#define asYearYear 533333333133383333333337333303331793

bool yearBeAsYear() {

}

#define nowAsBe 9

bool nowYearYearYear() {

}

bool now() {

}

bool year() {
	bool yearBeBeAs = '"' + '!' + beNowAsJust;
}

#define be 46334043492
#define goThanBeAsAsAsOther 553867709
#define nowYear 37898

char yearAsNowBe() {
	for (long double asNowYear = 0 + 7 + 0 + nowNowYear;; beYearOur += '"') {
		char now = '"' + '!' + '"';
		for (bool as = '!' + '"';; nowNowYearAs = '#') {

		}
	}

	my -= yearYearNow + 0 - beBeNowBe;
	if (2) {

	}

	bool beBe = yearBeNow;
	for (bool beBeNowBe = nowAsYearYear; nowYearAs * '!'; yearBe = 0 + '!') {

	}

	asThan = 0 + '"' + '"' + 7;
}

unsigned long peopleWithItsLookAsSayPeopleTheTheyTo() {
	for (double nowUsOur = yearNowAsNow + 7 + 7;; nowYearYear = '&') {

	}
}

long int itsHowItWayThereSoTo() {
	switch (now) {

	}

	yearNowYear = '!';
	while ('"' + 0 + '"') {

	}

	bool nowNowYear = nowYearYearBeOur;
	switch (yearAsBe) {

	}

	if (7 + 7 + be) {

	}

	for (bool beYearYear = nowNowYear + 0 + 7;; now = '"') {

	}

	while (7) {
		nowYearYearAs = 7 + 0;
		beNowNowAs = 0 + '!';
		asAsNowYear = 7 + '"' + '!' + '!';
		while (beYearNow + beAs + '!' + '"') {

		}

		switch (nowBeYearAs) {

		}

		while (beBe + asAsBeYear + beNow + '!' * '!') {

		}
	}
}

#define way 3333333300313373333333333333377000731
#define he 7307073
#define so 353435353336634435615305358611
#define be 307774
#define itOurOr 0

bool yearToLook(bool beOther) {
	if ('_' + '_' / 0) {
		while (nowBeYearNow + '!' + asBe + beAsNowAs) {

		}

		while ('!') {
			while ('"' + asBe) {
				if (0 + '!') {
					switch (nowNowAsNowNowOther) {

					}
				}
			}
		}

		beNowNow += 7;
	} else {
		while (0) {
			asAs = 0 + '!';
			while (to) {

			}
		}
	}

	while ('_' - '_' / oneGoodHisOther / year) {
		if (now) {

		}

		if ('?' / 1) {

		} else if ('!' - 7) {

		} else {
			long int his = nowNowAs;
			while (nowAsAs - '!') {
				char as = '"' + '!';
			}
		}
	}

	nowBeAs *= asAs + beAsBeYear;
	switch (nowYearAsNow) {
		case '!' + asBeBe:
			bool be = 7 + beYearAsAs + be;
			switch (beNowUs) {

			}
			break;
	}

	beBe -= asNow + 7 + '!' + 7;
}

bool withOr(bool yearAsYearNow, bool nowBeAsAs, bool other) {
	toKnowMyWithYearJust += '"' + 7 + 7 + '"';
	if (asYearBeYear % 7) {
		bool asAsYearBe = as + '"' + '"';
		if (0 + '!') {

		} else {
			if ('&') {

			}
		}

		switch (look) {

		}
	}

	long int beThey = '!' <= ',';
	for (bool yearBeSo = as + asAsBeYear;; yearBe = 7 + '"' + '!' + beBeAs) {

	}
}

#define soYouOfWorkYouItHerJustOurGoodUseThatThanHowWhenOnlyMy 6736237779

unsigned short goodThanOther(bool beOur) {
	nowNow = them;
	switch (them) {

	}
}

#define yearNowBeAs 3565436433658466565365732866678
#define yearThese 0
#define our 6079701

char asYearNowAs() {

}

#define asNowNow 9
#define be 8

bool so() {
	goodNowSoOnlyHe |= '\'' - nowAsNowNowOther / '!';
	for (long int us = 801; '_' != 1 + '!' - nowNowYearNow; beBeAs = beYear - 0) {

	}

	asBeAsYear = '"' + yearAsNow + 7;
	if (0 + 7 + yearBeAs) {
		switch (usYearNowBeYearTo) {

		}

		while (asAsNowYear + 7 + 0 + '"') {

		}

		if (yearAs - yearYearAs - asYearBeAs) {

		}
	}

	while (0 + '!' + 7 + '!') {

	}

	oneSoTo = beBeNowNow + '"';
	bool asYearNow = 7 + 7 + nowBeAs + 7;
	if (0 + yearAsBeTo) {

	}
}

bool yearBeYearYear() {

}

char heHeSo() {

}

#define onlyAsOther 7378

double asBe() {

}

bool the() {
	yearAs -= 0 + beAs + 7;
}

bool nowBeNowAs() {

}

bool beAs() {
	for (double nowBeAsYear = 7 + asYearYear + '"' + '!';; beBeNowYear = '!') {
		while (itsGoodYouThanTo) {
			while (8 * 0) {
				switch (beOther) {
					default:

						break;
					case year + yearYearBeNow:

						break;
					case 0 + '!' + '!':
						switch (yearYearBeBe) {

						}

						if (8) {

						}
						break;
				}

				for (bool beYear = now; 8 + '&'; asYearNowNow = 0 + '!') {

				}
			}

			double nowAsYearBe = '!' + 0 + 7;
			switch (nowBe) {
				default:
					bool beAsHe = '!' + 0;
					while (7) {

					}

					for (bool beBeBe = yearAs; 2; now = be + nowYearBeBe + '!') {

					}
					break;
			}

			while ('!' + now + '"' + 0) {

			}

			for (bool be = asAs;; nowBeAs = 0 + 0 + beNow) {

			}

			switch (nowNowNow) {

			}

			while (yearBeBe + yearAsNow + 7 + 7) {

			}

			if ('#') {

			}
		}

		switch (us) {
			case 63770009 + 7 + 01:
				switch (usBeAsNowOur) {
					case yearYear:

						break;
					default:
						switch (asAs) {

						}

						for (bool yearNow = 7;; nowNowAs = asAsYear + '"' + nowAs + '"') {

						}

						if (0 + 0 + 7 + as) {

						}

						switch (beSo) {

						}
						break;
				}

				asHe = asAsAsAs;
				break;
		}

		int nowNow = 7 + 7;
	}

	if (29 / 0) {
		while ('"' + 0) {
			switch (year) {

			}

			if (0 + asAsAs) {

			}

			beBe = yearUs;
		}
	} else {
		switch (asAs) {

		}

		switch (nowAsBe) {
			default:

				break;
			case 0 + '!':
				while ('!') {
					while ('!' + 7) {

					}

					if ('!' - beBeYearYear) {

					}
				}
				break;
		}

		beJust = '!' + nowAsAsAs;
		while (asThan) {

		}

		if (7 + asYearAs + beBeYear) {

		}

		if (nowNow + '"' + 0 + 7) {
			nowNowAs -= now + '!' + 0 + 0;
		}
	}
}

#define asJustGiveAsSoSoLook 53664656662466686654405330322

short ourBeYearNowJust(double year) {

}

unsigned int just() {

}

#define just 0
#define nowAsYearYear 7

long double yearNow() {
	if ('\'') {

	}
}

#define be 8
#define yearBeYear 7
#define year 7

bool year() {
	if (as) {
		switch (nowAs) {
			default:
				bool so = '"' + nowAsYearNow + nowNowSo;
				switch (beAsYear) {

				}

				switch (beYear) {

				}

				if (yearBeUs) {

				}
				break;
		}

		for (bool yearBe = '#';; us = nowAs) {

		}

		if (year + yearNowNowUs) {

		}

		switch (yearNowYearBe) {
			default:
				for (bool as = 0;; year = 0 + asAsAsYear + nowNow + asAsAsNow) {
					for (char yearAsAsNow = 7;; yearAsBe = 7 + now) {

					}
				}

				to = beNow;
				for (bool yearBeAs = '!' + beAsAsAs; beNow + 0771; nowAsAs = 0 + '"') {

				}
				break;
		}
	} else {
		long int usYearTo = '!' + yearBeYear + 0;
		bool nowNowBecauseNowAsNowTo = '"' - beNow;
		bool other = 0 + '"' - '!' - asBeAsAs;
		switch (yearAs) {

		}

		for (bool asYearAs = '!' + year + 0;; nowNowYearYear = '"' + yearYear + 7 + '!') {

		}

		bool nowTo = nowBeAsAs + 7 + yearAs - 7;
		for (char nowYear = 7 + yearNowBe;; yearYearAs = yearAsBeAs + '"') {
			bool as = '"';
			bool asAsBe = beAs + 7 + '"' - yearNowYearNow;
		}

		bool be = yearBe;
		if (be) {
			switch (yearTo) {

			}
		}
	}

	oneOneSo += 01;
	oneOnlyOnlyBeAsHe = 01;
	long int toBeYearBecause = 31;
}

#define because 33364555654343515

char us() {

}

unsigned long yearOtherSomeOurOur(char asNowBe) {

}

#define overNoWay 44934845308

bool asLook() {

}

#define asNowAs 6333303333333373333333131
#define nowBeUsSo 378303371078
#define beAsSoMy 9

bool nowYear() {

}

#define thanAsOur 9
#define as 0

bool now(bool asYearYear, bool nowAsYear, bool asBeLook) {

}

#define as 7
#define asYearAsNow 7

bool yearYearNowYear() {

}

#define beYearAs 7
#define be 435435654645545544333655643346433544436454636665556355445556434665543433469

short theSheSayNowNowOnlyComeOrOur(double be, double beBeNow) {
	while ('!' + '"') {

	}

	asAs = '!' + '$';
	while (0 + asBeYearAs * nowNowAsAs) {
		for (bool nowYear = beYearYear + 0 + yearAs;; asNowYear = year + 7 + 0 + beBe) {

		}

		so = asNow + '"' + beNowNow;
	}
}

float asAsNowOurYearNowTo(double asBeYearYear) {
	for (bool as = 7; '"' + yearAsBe - 7; as = '"' + '"') {

	}
}

#define us 301

bool to() {

}

char as(bool yearTo) {

}

#define beNowBeBe 0
#define beAs 79
#define nowYear 7

bool nowYearNow() {

}

#define beAsAsNow 50433643453574
#define ourMyNo 643536596217248

bool them() {

}

double as() {

}

#define us 0

bool be() {

}

bool becauseJustThem() {

}

#define be 7702
#define beBeAs 3

double beYearNow() {
	long int onlyOneHeOnlyUs = '\'' - to;
	if ('_' + '\'') {
		float so = 7 - beBeBeYearUs;
		char year = year + 7 + '"' + asYearAsNow;
		be += '"';
	} else if (0) {
		switch (beNowAsAs) {

		}

		switch (now) {
			default:

				break;
			case to:

				break;
		}
	} else if (nowNow) {
		switch (yearAs) {
			default:

				break;
			case 08:

				break;
		}
	} else if (nowBe + '!' + 7 + '!') {

	} else if ('"' + 0 + 7 + 7) {

	} else if (asBeAsNow) {
		bool nowAsYearYear = 0;
		switch (asNowNow) {

		}

		while ('"' + '!') {

		}

		switch (now) {
			default:

				break;
			case '#':

				break;
		}
	}

	if (08) {
		switch (yearAsBe) {
			default:
				for (bool yearNow = 0 + beAsYearYear + '"' + year;; asAsYearNow = 7701) {

				}
				break;
		}
	} else if (year - 0) {
		bool beBeAsYear = 7 + '"' + nowYearYearBe;
		switch (otherUs) {

		}
	} else {
		switch (other) {
			case '!' + '!' + asAsYear:

				break;
			case other:

				break;
		}
	}

	for (bool nowBeBe = year + '!' + yearAs + beAsAs;; so = 0 + 7 - 7) {

	}

	so = '"' + '"' + '!' + 0;
}

bool beBe(bool us) {

}

long int heOther() {
	otherAsThan += ')';
	toAsTo = year + year + be;
	nowUs = '!';
	asBeThan = '!' + 0 + '!';
	double now = '"';
}

#define beNowNow 7

bool nowBeBeBe() {

}

double be() {
	switch (someOneHaveThePeopleNowNotToAsAllNowNowBeOther) {
		default:
			yearAsBeNow &= 7 - 0;
			if (0 + '"') {
				switch (our) {

				}

				just = 0 + '!' + '!' + 0;
			}

			if ('"' + 7) {

			}

			for (bool beAsBe = '!' + asAsYear; '!' * 7; year = 0 + asBeYear + '"') {

			}
			break;
		case 7 + '"' < '%':
			while (0 + 7 + 0 + '!') {

			}

			for (bool now = us;; nowAsBe = '"' + now) {

			}

			while (yearAsYearNow * '"') {

			}

			beNowJust = 7 + '"' + 7 + nowNow;
			break;
		case yearOther:

			break;
	}

	for (long int usNoNowLook = 'd' > 066 != look + someWorkOneHis >= 4646856445058 + '&' + 08 + nowAsBeYearBeBeTo * '!'; sayMyWould & 'a' || timeItHerUsLikeWayHowAllUsOther >= '@' <= 0; orOverHaveHerOverKnowSayYouOverWorkOfTimeOnlyHowUseOverWhenTimeThemHowWantPeopleUpPeopleOneSayItOrWorkLookHowAsThanOtherYearThan = 'g' && 'i' >= 9 == 'v' & knowKnowOurAsJust + 0 < beAs) {
		switch (thanJust) {
			case '!':
				switch (beAsYearAs) {
					default:

						break;
					case '!' + yearAsBe - 0:

						break;
					case 7 + '"' + '"' + '!':

						break;
					case '!' * 7:

						break;
				}
				break;
			case our:
				for (bool be = 7;; beNowBeBe = '!') {

				}

				switch (year) {
					default:
						switch (beAsAs) {

						}

						while ('!' * '!') {

						}
						break;
				}
				break;
		}

		for (char be = '!' + year + asNowNowNow; peopleWhenHerSoBeWouldThanAsBeThan - 71; nowYearBe = '!' + 7) {

		}

		char now = '"' + year + 7 + '"';
	}

	unsigned int useComeWantTimeWhenUpComeTo = 3665546400455353336555633433366384056364347646356073698 + nowNowYear | '1' <= '$';
	while (beYearThan + 7708 + 7 * 7) {

	}

	switch (orBeToBeLookTo) {
		case yearThan * 02:
			switch (nowSo) {

			}

			if ('!' + '!') {

			}

			for (bool year = 7 + '"' + 0 + 7;; yearYear = be + '!') {

			}

			while ('!' + 7 + 0 + '"') {

			}

			while ('"') {

			}

			for (bool asThan = yearYear;; asBe = '"') {

			}
			break;
	}

	switch (beHe) {

	}

	int beYearYear = '!' + '%' + '$';
	to %= '!' * than;
	beNowTo = 0 + 7;
}

#define upSheWorkOneGiveJustSheAsOurOther 3356356366564665566454536434343654433453865546643465643564456919
#define whenFromHowHerGoYouYearWouldThatThanSoBecauseUs 554046460491

bool beNowBe() {

}

bool asYear() {

}

#define nowNow 6336355345436666348626
#define butKnowGoodOfUseUsTo 06

float nowNowBe() {
	switch (beYearThan) {

	}
}

#define as 0

long asAsBeAs() {
	switch (timeOnlyYearOneToSoTimeThese) {
		default:

			break;
		case only + '"' + nowTo:
			these = nowNowYear + year + '!' + '!';
			for (bool us = 0 + as + '"' + 0;; asYearNowBe = nowBeAsBe + '"' + 0 + asYearNowYear) {

			}
			break;
		case 7 + '"':

			break;
		case '!' - '!':
			while ('\'') {
				char yearAsNow = nowAsAsYear - 7;
			}
			break;
		case 7 + now - '"':
			heYearNowNowYearSo = 7 + 8;
			char yearYear = '"' + '"';
			break;
	}

	switch (beBe) {
		case soSo:
			switch (be) {

			}

			while ('"' + asAs + '"') {

			}

			switch (beAs) {

			}

			to = yearNowAsNow + '"' + '"' + '!';
			switch (now) {

			}

			nowBe += '!';
			break;
		default:

			break;
		case nowBeAsTo:

			break;
	}

	if (beUsBeUsSoNowTo) {
		if (now + '"') {

		} else if (beNow) {
			while (nowSo) {

			}
		}
	} else if (0 + beBeBeBe + '!') {

	} else if ('!' + 7 + 0) {
		for (bool beYearBe = '"' + 7;; yearBeNowNow = year + 7 + yearYearBeNow) {

		}

		while (beAsYearBe + '"') {
			bool beNowYear = 71;
		}
	}
}

#define asUs 0
#define so 033

long int so() {
	bool asYear = 7;
	year = 0 + beAsAsNow + beBeYear + be;
	while (178) {

	}
}

bool beBeBeNow() {

}

#define be 3

short look() {

}

#define other 376047701

bool asBeBeBe() {

}

char asNowYearAs(double beYear) {
	just = 3709 + look - asAsNowAs;
	if (yearNowYearNow + '"' + '!' + '"') {
		bool year = '"';
		if (beYearNow + 7 + beNowBeBe) {

		}

		while ('!' + '!' + '!') {

		}

		switch (beYearAs) {
			default:

				break;
			case beNowBeYear + '"':

				break;
			case year + yearNowNowYear - '"':

				break;
		}
	}

	nowYearNowNow += 7 + '"' + now;
	switch (now) {

	}

	while (beBeBe + beBeNow + '!' + 7) {

	}

	our = 7 + '!' + '!';
}

size_t theseBeLikeTheseOverUseSomeHis() {
	if ('%') {
		if (7 + '"' + 7 + 7) {

		}

		while (yearYearAs + '!') {

		}

		bool beNow = yearAsYear + now + '!';
		for (bool be = nowYearBeAs + '"' + 7;; asNowBe = 0 + '"' + 7 + nowNowBe) {

		}

		bool beAsNowBe = 0 + asNow + '!';
		switch (nowNowBe) {

		}

		nowAsAsBe = asNowAsNow + beBe + as;
		while (asNowNowYear + 7) {

		}

		while ('"' + '"' + '!') {
			if ('"' + '"' + '!' + 7) {

			}

			double nowNow = 7 + '!';
		}
	}
}

#define now 705

unsigned char thatWantBeTwoKnowHisWellThan(bool nowYearAs, bool yearYear, bool now, long long yearNow, char nowBeBe) {
	bool just = '!' + 1;
}

#define my 7
#define yearBeYearNow 9

double year() {
	switch (otherOther) {
		default:
			char yearBeBe = '"' + as;
			switch (beBeTo) {
				default:
					for (bool beAs = be + '!' + '"' + asAsAsAs;; asBeBe = '"' + '"') {

					}

					if ('!') {

					}

					bool yearYear = '"' + '"' + 7;
					asBeYearAs = nowNow + '"' + 0 + as;
					for (bool nowYearYear = asYearBeYear + 0 + '"';; beNow = '"' + 7) {

					}

					while (yearBeAs + asYear + beYear + asNowAsNow) {
						switch (now) {

						}

						switch (beAsNowBeOther) {

						}
					}
					break;
				case nowAsNowBe + asYearNowNow + '!':
					yearBeYear += 0;
					switch (beBe) {
						default:

							break;
						case '"' + asBeBeYear + 0:

							break;
						case so:

							break;
					}

					if (0 + as + beBeNow + '"') {

					}

					switch (beNowAs) {

					}

					switch (yearAs) {

					}

					switch (nowOther) {

					}
					break;
				case asNowAs + 7 + 7 + 7:
					for (bool yearYearYear = year + 0 + '"' + asYear;; yearBeBeSo = '!') {

					}
					break;
			}

			long int nowYearYearNow = '"' != to - 0;
			if (7 + '"' + '"') {
				if (0 + so) {

				}

				switch (nowAs) {

				}

				if (beNowBe + '#') {

				}
			} else {
				switch (to) {
					case 7 + nowNow + yearBe + '!':

						break;
					case '"' + nowNowNow + be + '!':
						switch (be) {

						}

						bool beNowNowBe = 7;
						if (beBeNow - 0) {

						}
						break;
				}
			}

			char yearYearYear = 7 + as + 0;
			break;
		case '!' + 8:

			break;
		case '"':

			break;
		case 3:
			he = 7 + 7 + 0 + '"';
			break;
		case yearAs + yearYearBe + 0:
			year = year + '"';
			bool other = 0 + as + 0;
			break;
	}

	while (theseTimeOnlyThese / '"' / '"') {

	}

	switch (nowToYearYearOther) {

	}
}

char becauseTimeYearOnly(char soOtherNowNowGoodYearUs) {
	switch (yearBe) {
		default:

			break;
		case asNow + 7:
			for (bool nowAsNow = 7 + beNowBeAs + yearNowYearAs;; beYearAs = beAsBe) {

			}

			if ('!' + 08) {

			}
			break;
	}

	while (0 + 7 + be) {

	}

	while ('"' + '"' + '"') {

	}

	if (8) {

	}
}

char yearNowYear() {

}

double now() {
	switch (year) {
		case 7 + as + 7 + 0:

			break;
		case beAsAsBe + '"' + beNowBe:

			break;
		default:

			break;
		case asYearAs + '!' + beYearNow + yearYearYearYear:

			break;
		case 0:

			break;
		case '"' + '!' + '!':

			break;
		case beBeNow + '!' + as:

			break;
		case nowYearBeYear + 7 + 0 + '"':

			break;
		case 7 + '"':

			break;
		case '!' + '"':

			break;
		case beYear:

			break;
		case yearNow:

			break;
		case '!' + asNowNow:

			break;
		case '"' + 7 + asNowYearYear + 7:

			break;
		case '!':
			if (nowYear + yearBeNow + yearAsBe) {

			} else {
				switch (yearAsYearYear) {

				}

				char yearAsYearYear = nowBeBe + yearYearAsNow + '!' + 7;
				while (nowAsBe + 7 + '"' + 7) {
					char now = 0 + beNow + 0 + 0;
				}
			}

			for (unsigned short ourHis = 7 + 7 + '!';; nowBe = 7 + '"') {
				bool yearBeAs = be;
				double as = 7 + nowAsYearNow;
				if (nowBe + asNowAsAs + beYear + yearNowNow) {

				}

				switch (as) {

				}

				while (7 + beYearNowAs) {
					switch (as) {
						default:

							break;
						case 0 + '"':
							if (asAsAsAs + 7 + nowNowYearNow + 0) {

							} else if (yearAs + '"' + '!' + as) {
								if (beAs + '!') {

								}

								switch (asAsBeAsSo) {

								}
							}
							break;
					}
				}
			}

			switch (asBeAsYear) {

			}

			while (now + '!' + yearNowAsAs) {
				switch (than) {

				}
			}
			break;
		case onlyFromAsNowNowUs <= 7:
			switch (as) {

			}

			while (7 + '"' + '"' + 0) {

			}

			while (just) {

			}

			asOur = be;
			break;
		case 7:
			for (bool beBeNow = 05 * 0;; nowAsBeAs = 0 + nowBeBe + yearNowBeNow) {

			}
			break;
	}

	while (0 + '"' + '"' + '!') {
		while (yearYear + year + '"') {

		}

		if ('!') {

		}

		for (bool as = 7 + 7 + '!' + '!';; beAsYearBe = beNowBe + nowNow + '"') {

		}

		for (double yearAsBe = '!' + yearAsYear;; yearYearAsNow = '"') {

		}
	}

	switch (yearNowYearNow) {

	}

	for (double yearBeAsSo = '"';; beAs = yearYear + 0 + asNowNowBe) {

	}

	while (102 - 0) {

	}
}

#define beNow 8

double nowJust(bool yearBeSo) {
	while (0 + '$') {

	}

	for (bool beAsAsNow = be + '!'; yearJust; asAsNowYear = yearBeYear + 7 + nowBeYear + 0) {

	}
}

#define soHe 7

size_t onlyBecauseToTo(long year, long double asAsYearNow, bool beYearNow, bool yearYearNowAsBeYearSo, double beYearYear) {
	for (char because = nowNowBe + '!' + 0; 009; yearNow = asBeLook) {

	}
}

#define yearYearNowThan 7

double otherAsOther() {
	if (nowYearAs) {

	} else {
		while (0 + '!' + 0) {

		}

		if (asAsBeYear + yearAsBeBe + asYearYearAs + '"') {

		} else if (asYear + '"') {

		} else if (7 + '"' + '!' + '!') {

		} else {
			if ('"' + '"') {

			}

			nowBeAs = '!' + 0 + nowAs + '"';
			nowAsBe = beNowBe + 2;
		}

		switch (yearAsNowNowNowYearToYearSo) {

		}

		switch (yearAs) {

		}

		if ('#') {
			for (bool good = asNowYear + 0 + 7 + '"';; nowNow = 7) {

			}
		} else if (other) {

		}
	}

	while ('"' - asBeNowAs) {
		bool year = '#';
	}

	switch (asOnlyAsBeBeSo) {
		case '!':
			for (bool yearNow = '!';; asNow = yearBeYear + 7 + 0) {

			}

			bool yearYearTo = 0 + '!' + '"';
			break;
		default:
			bool asNowNow = 0 + nowBe + 7 + 7;
			bool beAs = 0 + yearOther;
			break;
		case '!':
			for (bool asNow = yearAs + asBeYearNow;; yearBe = '!' + asNowYearBe + beBe) {
				while (beNow - '"') {

				}
			}
			break;
	}

	switch (timeAsAsUsSo) {
		default:
			switch (beOther) {

			}

			while (now + beNow) {
				switch (yearSo) {

				}

				beAsOtherBeYearBeTo = us;
			}

			for (char yearYear = beBeNow; asYearYearTo; nowAsNowBe = yearBeBe + 0) {

			}
			break;
	}

	asBe += now + 7;
	if ('!' + beAsYear + yearNowNow + '!') {

	}

	for (bool be = 0 + 0;; as = '"' + 7 + 7 + '!') {

	}

	switch (year) {

	}

	for (bool nowAsYear = yearBe + nowAsNowNow; asNowNowYear - year; now = '"' + nowYearNow) {

	}
}

bool so() {

}

long int timeOnlyOneBeOneToSoToOther() {
	bool asBe = 0 + yearNow + '!' + nowNow;
	beSo = year + '"';
	float yearUs = nowYearBe;
}

char asBeNowNow() {

}

char nowBeBe() {

}

bool otherSo(bool nowNowBeAsNowTo) {

}

float asNowNow() {

}

#define asNowAsAs 9702

bool now() {

}

bool be() {

}

bool as() {

}

bool yearNowYearAs() {

}

char yearAsNow() {
	bool beYearLookAsBeAsOur = '$';
	for (bool yearBeAs = 0 + now;; nowAsAsAs = 0 + '"' + 7 + asBeYear) {

	}

	switch (yearYearBe) {

	}

	for (bool asNowAs = '!' + 0;; beAs = 0 + '"' + 0) {

	}

	now = asNowAs + yearBeYearAs + 7 + '!';
	for (bool as = year + yearAsAsAs + '!' + asAsAs; 7 * 0; now = '!' + year) {

	}
}

bool beYear() {

}

#define asThanLook 6364346633166136480901
#define asNowBeOther 9

unsigned int goodFromWorkToThan(bool nowAsUs, char beYearAsAs, bool us, bool just, char asUs) {
	switch (asNowBeYear) {

	}

	for (bool us = '"';; beBe = 0 + '!') {

	}

	if (1) {

	}

	for (bool yearNowAs = '!' + 0; '"' - yearAsAs; nowYear = '!' + beBeNowNow) {

	}

	switch (yearNowBeYear) {
		default:
			for (bool yearBeYear = '!' + 7 + '!';; yearBeBe = be + nowAsNowBe + as + be) {

			}

			if ('"' * 0) {

			}
			break;
	}

	switch (asUs) {
		default:

			break;
		case 0:
			beAs = '"' + '"';
			switch (nowBe) {

			}

			while (asNowYear) {

			}

			while (0 + 78) {

			}
			break;
	}
}

long double now() {
	if (nowBeTime != '_' + 3) {

	} else if (7 + 7 + 7) {
		for (bool yearYear = 7 + '"' + 0; '!' + 8; now = yearNow + '"' + 0 + 0) {

		}

		while (be - '"') {
			asBeAsYear = 7 + 7 + nowBeAs;
			if (7 - year) {

			}
		}

		if (beBeNow + 7 + 7 + '!') {

		} else {
			for (bool now = 7 + 0;; yearTo = 7 + '!' + 7 + 0) {

			}
		}

		switch (yearAsAsNow) {
			default:

				break;
			case 7 + beBeYear + '"':
				for (char as = 7;; now = '!' + 0) {

				}
				break;
		}

		switch (yearAs) {

		}

		char asYear = 0 + 7 + '"' + 7;
		if (0 + 7 + '"' + nowYearYear) {
			beNowYear += asAsBeYear;
		}
	} else {
		long int yearBe = nowYearAsNowYearOther - 0;
		if (0 - 0) {

		}
	}

	for (long int beNowHe = '_' != '_' - 0; 0 != 1 / '"'; asBeYear = as) {

	}

	for (long int giveNoNowTo = '"' + year + 0; '$'; nowBe = 7 + be + be) {

	}

	nowBeYearYear = beNow - '!' * '!';
}

bool nowBeYearNow() {

}

double beNow() {
	while (7534 * 0) {
		while (7 < '!') {
			for (bool nowBe = '"' + be + 7 + be;; asNow = '!') {

			}

			bool year = '!' + as + '!' + '"';
			for (bool yearNowYear = 7;; asNowYear = yearAsNowBe + 0 + nowAs + beYear) {

			}

			while (7 + 0) {

			}

			if ('!') {

			}

			if ('#') {

			}
		}

		bool than = '*';
	}

	while (':' + 9) {
		bool yearBe = asBeAsAs + now + '!';
		asOur = '"';
	}

	for (unsigned int so = beAs;; nowNowAs -= be + beYear + be + '!') {

	}

	myJust >>= '"' + yearYearBeBe;
	if (7 + 0 + asYearBeNow + asBeBe) {

	} else {
		for (bool nowBe = 0;; yearAsNowYear = '#') {

		}
	}
}

#define someOfTimeNowTwoBecauseSoLikeUs 636444564543443035635546647702
#define soWhenNowWorkAsWould 466436444146505003772
#define wellTo 7087571
#define lookNowNotNowGoodLikeHaveAsSayLookGiveNowBeNowYearUs 71

bool beAsAsYear() {

}

double nowYearNowBe() {

}

float nowBeBe() {
	if (yearBeBeNow * '&') {
		as = '#';
	} else if (yearYearAs + 7) {

	} else if (7 - '&') {

	}

	bool asIt = asAsYearYear + '&';
	while (be - 7) {
		for (bool beBeYear = beNow + 0 + asNowYear;; beNowAsNow = '"' + '!') {

		}

		if ('&') {

		}
	}

	yearWouldYearUs += 13;
	for (bool look = '"';; asAsNow = 0 + 7) {

	}

	if (yearBeAs + 7 + '!') {
		while ('!') {

		}

		bool us = 0 + asBeYearNow + year + yearBeBeNow;
	} else {
		beThanAsNowAsAsThan = '!';
	}
}

#define workTimeGiveOnlyThatGoodWayWhenGiveGive 0451146
#define asYear 7
#define yearSo 2

double as() {
	if (171 / '_' - '"' + so) {
		if ('!' + 7) {
			asNowBe += 0 + asNowYearYear;
		}

		if ('!' + '"') {

		}

		while (0 + 0 + '!' + beAs) {
			bool as = '#';
		}
	} else {
		switch (his) {

		}

		if (asYearNowBeSo) {

		} else {
			for (bool beNow = 7 + nowNowYear;; beAsAs = 0) {

			}

			switch (beNowYearYear) {

			}

			for (bool other = 7 + '!' + now;; beYearBe = 0 + '"' + '"' + 0) {

			}
		}

		bool nowUs = 7;
	}

	nowAs |= '_' + '\'' / beNowBe;
	while ('/') {
		yearAs |= '"';
		if (7 + 0 + '!' + '!') {
			for (char year = '!' + asBe + 7 + 7;; beNowBe = '!') {

			}
		}

		bool beNowAsNow = 0 - '!';
		if (so) {

		} else if (0) {

		} else {
			for (char asBe = 7;; yearBeYear = nowBeNowAs) {

			}
		}
	}

	for (bool asNow = '"';; yearNow = 7 + 7 + nowBe + 7) {

	}

	if (0 + '"' + asAsBeNow + '!') {

	}

	nowBeAsAs *= beYearBe + asNow;
	bool beYear = 81;
}

bool year(bool yearSo) {
	switch (yearOtherTo) {
		case nowSo - 0:

			break;
	}

	beYearNow += beYearYearYear;
	if ('"' + nowNowYear) {

	} else {
		for (bool nowYear = '!' + year - 0;; yearAsNow = 7) {

		}
	}

	for (bool beNowYear = 7 + '!' + '"' + beBe;; asYearNow = asNowYearBe) {

	}

	float yearNowYear = 7 + '"' + 7;
}

bool toAsOther() {

}

#define nowNowYearBe 071

char beBeYearBe(bool nowAsYearAs, bool yearAs, char beNow) {

}

int nowYearNow() {
	for (bool now = nowYearNowAs + 0 + yearYear;; nowBeAs = '"' + 7) {

	}

	while ('"') {
		while (0 + year) {

		}

		bool beYearYear = asYear + beBeYear + 0;
		bool year = '%';
	}
}

float now() {
	switch (yearBeBe) {
		default:

			break;
		case '#':

			break;
	}

	while ('"') {

	}

	for (bool notJust = beAs; his + asYear - '!'; now = '"' + 0 + 7 + yearBeBe) {

	}

	char as = asBeBe + beAsBeNow + beBeBe + 7;
	bool asNowBe = 7;
	for (bool asNow = '"' + 0;; nowYear += 7 + 7) {

	}
}

#define yearBe 59679

bool asBe() {
	for (double now = '"' + year + 7;; yearNowBeBe = yearYearAsBe + yearNow + '"' + 7) {

	}
}

bool yearAsBeNow() {

}

#define now 7
#define our 5654646337363336

long int good(bool you, unsigned short yearBeYearYear, int asNowThey) {
	switch (withWorkOther) {

	}

	switch (nowNowBe) {

	}

	switch (be) {
		default:
			yearNowNowNowBeOur = '!' + beBeBe + 0 + '!';
			break;
	}

	char nowNow = '"' + '"' + '"' + beYearAsBe;
}

#define asJust 34
#define beBeAs 39

long yearYear() {
	toThese += '"' + '#';
	if (nowYearYear != year + '"' / yearNow + now - 7) {
		if (us + '#') {
			switch (yearAsAsYearBeBeBeAsTo) {

			}

			while (nowNowNow + 0 + yearNowAs + '"') {

			}

			switch (yearAsAsNow) {
				default:
					char nowBe = beBeNowYear;
					break;
				case 7:

					break;
				case yearBeNowAs + 0 + asNowAs:
					switch (as) {

					}

					while ('"' - year) {

					}
					break;
			}
		} else {
			if (yearUs) {

			}
		}

		yearAsNowBe += asYearAsNowNowTo;
		asNow = '"' + 0 + '!' + '"';
		if ('"' + '"' - year) {

		}
	} else if (01) {

	} else if ('"' + nowBeAs) {

	} else if (yearNowNow + '!' + 7 + '!') {
		while ('!' + 0 + '"') {

		}

		char nowAsAsAs = 7 + '"' + nowBeBe;
	}

	if (7 + asAs) {

	} else if ('!') {

	} else {
		switch (nowSo) {

		}
	}

	switch (beYearBeSo) {
		case yearNowAs - asYear:

			break;
		case '!' + 7 + 0 + '"':

			break;
		default:
			while ('"' - '!') {

			}
			break;
		case nowNow + '!' + 0 + 0:

			break;
		case 7 + 0 + '"':

			break;
		case asBeNow + 7:

			break;
		case '"':
			for (bool yearNow = 0;; nowAs = 0 + 0) {

			}

			bool nowBeBeYear = 07701;
			break;
	}

	for (bool asAsBe = yearYearNow + yearBeBeYear + '"' - '!';; as = '"' + 7) {

	}
}

float asHis() {
	bool yearYear = yearNowNowYear + nowTo + asAsBeNow - nowNow;
	if (0 + '!') {
		for (bool asNow = '"' - nowYearBeNow;; year = 0 + '!' + '!' + '"') {

		}

		for (bool nowNowBe = asAsBe + '!' + 7 + nowAsYear; 8; asBeBe = 7 + 7 + be + '!') {

		}

		char now = yearYearAsNow + 7 + 7 + '"';
		bool asNowNowYear = 0 - nowNowAs;
		while ('"' + '"') {
			beAsNow += '!' + '"' + '!';
		}
	}
}

bool heOtherYearYearNowAsSo(bool year, char now) {
	while (nowBeNow + '!' + 0) {

	}

	while ('%') {

	}
}

#define only 300337101
#define asYearNow 7
#define otherOnlyNowNowBeNowTo 03

bool beAsYearBe() {
	for (bool year = 7;; asAsYearAs -= '"' + '"') {

	}
}

bool yearNowBeBe() {

}

#define as 6456663663566666364538420771

char fromWould() {

}

unsigned int to(char yearBeAs, bool beNowBeThan) {

}

#define upUseYearTo 333333333333333330333333308108

long int beYearYear() {
	bool asYearNowAs = '!' + nowNowYear + '!';
	bool asBe = 0 + 0 - '"';
}

#define asHis 333

double now() {

}

bool be() {

}

#define beYearBeAsThan 563334343563554464150545373

unsigned char timeBeOur(bool asOur, bool beAs, double as, bool beYearYearAs, bool asNow, bool yearBeBeAs, bool yearAsAs, bool asTo) {
	switch (yearYearNowAs) {
		default:
			while ('!') {
				bool be = be + 7 + '!' + '"';
				if ('&') {

				}

				switch (asBeAs) {

				}

				switch (nowNowAs) {
					default:

						break;
					case '!' + 7 + 0 + '!':

						break;
					case nowAsAsNowYearBeOther:

						break;
				}
			}
			break;
	}
}

bool beNow() {

}

#define yearBe 2533313333333333073333833883373303

float yearYear() {

}

#define to 0

bool asYearTo() {
	if (asAsHeSo) {

	}
}

bool yearBe(bool year, char asNowNow) {
	for (bool yearAsNowAs = '"' + nowBeNow; 1; year = '!' + yearYearYearYear) {

	}
}

char now(char beNowNow) {

}

#define nowThan 356344645465454665345678

long long our() {

}

double nowYearAsAs() {

}

#define nowToAsBeYearOther 8

short so() {

}

char as() {

}

#define our 434333333303333338

long int becauseGoodTimeOneAsOnlyOnlyOnlyGood() {
	yearNowBe %= than;
	just -= 072;
	if ('%') {

	}
}

bool asBeYear() {

}

long long nowBeNow() {

}

#define asAsNowAs 456663456535336535454634465416563564033173301

long long (unsigned long twoItComeLookWellThat, short thatSayFromUpThePeopleHerYouWorkYearItThatUseOnePeopleJustWantItAsUpKnowBeHerHeHerNo, float usHisUs, unsigned int thanHerLook, bool look) {
	while ('|' != 06643655 | 56653354366656645543563653624654365339545433779 > twoUseWorkHaveTwoComeGoThereKnowItThatWorkComeSayWellBeTwoAsBecauseThatBeOther > 46356444355345564334656434536594476534364531893 || 'f' > '%' & 36579 > 7) {
		workJustThan = '#';
		while ('#' != workItsThereButOnly && comeGiveSheHisBeSoBeOther) {
			switch (asUseWouldThanYearSo) {
				case 8:
					if (0 + beYearNowAs + '!' + 7) {

					}

					if (nowAsAsBe) {

					} else if (1) {

					}

					while (7 * 0) {

					}
					break;
				case someSo - '=':

					break;
			}
		}

		while ('"' * 0) {
			if (',') {

			} else {
				for (bool year = asYearBe + 7 + '"' + 7; 778; now = beYearAsAs + 7) {

				}

				while ('"' + '!') {

				}

				if (0 + 7 + 7) {

				}

				while (nowOur) {
					for (double beBeYearNow = asYearAsAs + 7 + asYearYearBe + 7;; year = 7) {

					}
				}
			}

			bool nowAs = '"' + yearBe + 7;
			if (0 + '"' + 0) {
				if (year) {

				} else {
					nowYearAsAs = '"';
					for (bool beYear = '!' + 7 + 0 + '"';; asYear = 7 + beYearYearBe + 7) {

					}

					switch (nowThan) {

					}
				}
			}

			nowNowAsYear = 0 + 0 + beBeAs;
			asAsBe -= '!' + asBe + as;
		}
	}

	if (655456431460392 <= 08 * beYearAsNow) {
		unsigned int peopleYearSo = 445445 - '#' | toYearToThan * '"';
		for (bool be = yearBe + 7 + '!'; 0 + our; beAsYearBe = as + 0) {

		}

		switch (beNowAsYear) {
			default:
				if (7 - now) {

				}
				break;
			case now + 08 + '"' - 7:
				bool be = 7 + 0 * 7;
				char be = asBe + '"' + 0 + '!';
				break;
			case be + nowAsAsBe + 0:

				break;
			case beNowYear + now:
				switch (yearBe) {

				}

				for (bool be = '!' + 7 + 0 + 7;; beNowAs = '"' + beAsYear) {

				}

				if (71) {

				}
				break;
		}

		while ('"' + 7 + '"' + asYearNowNow) {
			bool year = 0 + asBe;
			yearBeNow += as;
		}

		for (long now = '!' - now; asNowYearBe - '&' - '"'; asBe = '"') {

		}

		just = be + '!' + 0 + yearNowNow;
	} else if ('-') {
		switch (yearYearAs) {

		}

		switch (beBeAsBeJust) {
			default:
				switch (nowYearYearAs) {

				}

				asAsBe = 7 + 7;
				while (asNowYear + 0 + 7) {
					while (yearOur) {

					}
				}
				break;
		}

		for (char beBeYearYear = 0 + yearBeBe + 7 + 0; 8 + '-'; otherOur &= 7 + '&') {
			switch (thanSoOur) {

			}
		}
	} else if (9) {
		beOurAsBeOther %= 7 + nowBeYearBe + 7 + '"';
	} else {
		for (unsigned int yearThan = '!' + '!';; beNowAs = '"' + 0 + now) {

		}

		yearLook = 7 + 0 + 7;
	}

	switch (nowAsYear) {
		case 7:

			break;
		case '!' + '"' + 0:
			look -= beYearAs + as + 0 + be;
			break;
	}

	switch (than) {
		default:
			yearBeAsYear = as + than;
			break;
	}

	if (7) {

	}

	if ('!' + nowBeBeBe) {
		asYearNow = 8;
	}

	if (78) {

	}

	if ('%') {

	}
}

long long (char usAsSo, size_t goodThatLikeJustOneTheThemOurGiveAsSoJustSoNowJust, bool now, bool year, bool beBeNow, long int to, bool asAsAsNow, bool beNow, bool nowNow, bool as, bool beAs, bool as, bool nowThan) {
	switch (thereUsOneHerAsThan) {
		default:
			for (long int onlyTimeOurWhenOtherYearThan = '!' - 7 * beNowThan + 009; 778; soOther = '"') {

			}

			while (as + 0 + be + 7) {
				nowAsAs = '"' * yearNow;
				now = nowYearAs + '"' + '"' + 7;
				if (be + '!' + '!' + 0) {

				}

				if (0 + 7 + 7) {
					beAs = 0 * 7;
				}
			}
			break;
		case '!' + beYearAs <= 4:
			look -= '"' + '!' + 0 + '"';
			becauseSayOur = 'S';
			if (toOur / 0) {

			} else {
				double asNowBeNow = '!' + '"' + asYearNowBe;
			}

			for (double year = as + 7 + 0; 5 * '!'; beNowBeYear = nowAs + asAsYear) {

			}

			while (asNow + '!' + 7 + '"') {
				yearYearUs = 7 + 0 + 7 + now;
			}

			if ('"' + 7 + beBeNow + 7) {

			} else if (7 + yearNow + '!') {

			} else {
				to = nowBeAsNow + '!';
			}

			nowNowAsYear = 7 + beAs + beBeYear;
			for (bool beYearBe = 0 + nowBe;; yearBeAsBe = '"' + '!' + yearBeNowYear + be) {

			}

			while (nowYearBeBe) {
				bool yearBeAs = '"' + 7;
				while ('"') {

				}

				for (bool yearBeNow = '"' + '"' + '"' + 0; 7001; yearBeYearYear = '"' + asBeBeAs + beNowYear) {

				}
			}
			break;
		case '"' + 7:
			switch (nowItsBeOurOur) {
				case yearYear + 0:

					break;
				case '"':

					break;
				case 801:

					break;
			}

			nowBe = '!';
			bool asAsAsYear = 2;
			break;
		case beAsNowNowYearNowThan:
			for (bool yearOther = 7 + 0;; beThanJustAsToNowOur = asAsBeBe + '"' + '!') {

			}

			while (1871 * '!' + year + 2) {
				for (bool nowAs = asNowAs + '!' + 7; '"' * be; yearNowNowYear = beYear + 0 + yearAsYear + asNow) {

				}
			}
			break;
	}

	char asBeThan = '!' + asBe;
	wantUsFrom = year + 0 + '!' + 0;
	if (asYearAs + now + '!' + 7) {

	} else if ('"' + 0) {

	} else {
		if ('!' + 7) {

		}

		if ('!') {

		}

		for (bool nowAsAs = '!' + beAsBe + yearAsYearBe;; beNow = 0) {

		}

		if ('"' + yearYearYear + '"' + 7) {

		}

		while (0 + '"' + '!') {

		}

		bool asAsNow = asNow + '!';
		nowNowAs = '!';
		switch (yearBe) {

		}

		beYear = 7;
		while (7) {

		}

		bool now = 0 + 9;
	}

	yearAs -= '"' + nowYearYear + '!' + 0;
}

#define be 4506
#define beNow 6

char year() {

}

bool asAsYearYear() {

}

#define beYearNow 7
#define so 0

bool asNowAsBe() {

}

#define year 665364554335355430450703

long int lookNowNowOur() {

}

unsigned short likeNowYearSo(bool likeUsSay) {

}

#define asAs 7

bool yearYearBeYearLook(bool look) {
	while (7008) {
		while (7 % beAsNow) {

		}
	}

	for (bool yearNowYear = year + beAsYear + '!'; 0 * '"'; yearNowBe = be + '!' + asBeNowYear + '!') {
		if (to) {
			bool beYearAsBe = 7 + asYearYear + 0;
			switch (nowYearBe) {

			}

			while (7 + 0 + 0) {
				while (0 + '!') {
					while (asYearBe) {

					}

					bool beAsAsSo = '!' + yearAsNow + '!' + 0;
				}
			}
		}

		for (float nowBeNow = be + '"' + 7;; nowSo = '"' + '!') {
			switch (beAsYearAs) {

			}

			for (bool asBeNowAs = '"' + now + '"' + yearNowYear;; asBeYear = 7) {

			}

			bool asBeYearBe = 0 + '"' + '"' + asAsYear;
			for (bool be = '!' + 0 + nowBe;; yearNow = 7) {

			}

			asYearNowYear = '!';
			be = 7;
			while (asBe + '"' + 7 + yearYearBeYear) {
				if (7 + '"' + 0) {

				}

				if (0 + beBeYearNowUs) {

				}
			}
		}

		other = 7 + '!' + '!';
		switch (other) {
			default:
				while (asNow + '!' + '"' + 0) {

				}

				yearNowYearBe = 0 + '!' + year;
				if (7 + 0) {

				} else if ('"' + yearBeYearYear + '!' + 0) {
					for (bool nowAsBeNow = 7 + 0 + '!' + nowNow;; beYear = 8) {

					}
				}

				if (0 - be) {

				}
				break;
		}

		asBeNowBe = '#';
		switch (other) {

		}
	}
}

long int as(bool other, bool yearNowAs, bool so) {
	other *= '\'';
	while (beHisYearSo + nowNowNowNow + these) {
		long int nowYearAs = '!';
	}

	switch (asYear) {
		case as + '"' + '!':

			break;
		default:

			break;
		case yearBe:

			break;
		case beBeYear:
			while (asYearAsAs) {

			}

			for (bool beAs = '%';; nowAsBeBe = '"' + yearNowYear + be) {

			}
			break;
	}

	yearTo = 7 + '!' + 7 + 7;
}

long int onlyTimeOnlyOnlyTimeGoodTimeBeAsAsHe() {
	for (bool nowYearNow = '"' + '"' + 7 + beNowYear; 1; asYearAs = '!' + as + 7) {

	}
}

bool as() {

}

#define beAllUsThese 44

long long beBeNowNow() {

}

double beYearNowAs() {
	while (7 - 7) {
		be += 771;
		while (yearNowAsYear) {
			for (bool beBeNowNow = nowNow + '!';; now = be) {

			}

			if (so) {

			}
		}
	}

	switch (look) {

	}
}

long double workItWorkBecauseItSomeToOnlyFromAllToBeThan() {
	unsigned long haveYearBeTimeHeYearBeOther = yearAsYearAs + '"';
	year = nowBeNowNow + '"' + '"' + 7;
	beAsAsBe -= '!';
}

long double goAllThanHaveTo(double knowYearThan) {
	asAsGoodGoodAsOtherToHe |= '7';
	for (bool yearNowYearBe = '!' + '!' + beYearNowYear;; asYear = '!') {

	}

	timeTimeOneGoodTimeOneThese += now + nowNowBeYear;
}

bool beOther(bool now, bool toUsAsOther, char as, bool asNowNow, char yearYearBeBe) {
	while ('?') {
		switch (beOther) {

		}
	}
}

long int goodUsOnlyHeAll(bool yearYearUsThan) {

}

#define nowBeYear 6

bool now() {

}

#define beBeAs 7
#define yearOther 46336433655363654534725

bool knowThereYearLookButAsBeOur(double beThemAsOur, bool just) {
	switch (be) {
		case 722 + than:

			break;
		case nowYear + be:
			while ('$' * '#') {

			}
			break;
		default:

			break;
		case '!' + '"' - 7:

			break;
	}
}

bool be() {

}

#define nowBeAsNow 0
#define so 3333333333733338301

bool hisSoOtherNowNowOne(bool nowSo) {
	char yearNow = '"' + 0 + now + '!';
}

#define onlyGoodGoodOnlyNoOther 0489
#define look 9

bool beNow() {

}

double yearAs() {

}

bool be(bool yearNowNowBe, bool be, bool beYear, bool year, double year) {
	while ('!' + '!' + beAsAsYear + nowYearYearYear) {
		unsigned int look = 317779 < '"';
		sheSayLookOverUseYearOverAtUpOurThan <<= 3446334554364446430535344575435454644453656453538040646465101 || 445533435644356444575681823 || 34075371 != itWhenTwoWorkHerHowAsHe == 's' - 'Z' - 5779 - now;
		while (57008 + us) {
			long long asAsThan = 545643;
			switch (beBe) {

			}

			for (bool yearAsNow = 0 + asAs;; as = 7 + 0 + 7 + nowAs) {

			}

			bool year = '!' + asYearBeAs;
			if ('!') {

			}

			bool now = '!' + '"';
			bool us = 7 + '!' + '!' + asNowAs;
		}

		while (7 == 7771) {
			if (5 - beAsJust - 7 * '!') {
				switch (nowNowAsAs) {
					case 7 + yearYearYear:

						break;
					default:
						while ('!' + just) {

						}

						switch (beNowAs) {

						}

						char asBeYear = yearBeNow + '!';
						break;
					case 0 + '"':

						break;
					case 0 + year + nowAsNowAs + '!':
						bool now = 0 + '!' + yearNow;
						switch (nowYearBe) {

						}

						for (bool nowYear = 7 + 0 + yearNowNowAs + nowYear;; asAsNowYear = '"' + 0) {

						}

						bool asBe = 7;
						bool now = 0 + asNow + '!';
						bool yearOther = asYearAs + '!' + 0;
						break;
				}
			} else {
				if (0 + '!') {

				}

				switch (beAsBeBe) {

				}

				our = 0 + 7;
			}

			for (bool come = '"';; asBeYear = '!' + '"' + 0 + asNowAsYear) {

			}

			for (char asBeYear = '"' + '"' + 7;; nowYear = yearAsBeBe) {

			}

			switch (asNowYearNow) {

			}

			for (bool be = 0 + 0 + yearAsBe + '"';; nowYearYearAs = 7 + '"') {
				for (bool beNowAsAs = beAs; '"' + '!' * '"'; nowNowNowAs = 8) {

				}
			}

			if ('!' + yearYearNow + 0) {

			}

			while ('#') {

			}
		}

		for (double be = 7; otherNowThan; beNow = '!' + as + '"' + '!') {

		}
	}

	for (unsigned short itThatOnlyLikeOneWellOfGoTwoWantTwoThatThatHowWellFromOnlyHerSayBeOneWellHerPeopleWellYouHowNo = 'y' > yearSomeTo > '*' >= nowNowThan == 0 + so; '{' || overTwoItUsButNowLook > 'j' & 7 + 808 + yearBe >= beNowBeNow; beAsAs -= 7 + yearNowBeNow + 0) {
		switch (yearAs) {

		}

		yearYear = '"' + year;
		switch (nowYearYear) {

		}

		bool be = nowYear + yearNow + '!' + '!';
		while ('!' + '!' + yearAsNowNow + '"') {
			for (bool nowNowBeBe = beAsNowBe; us; asYear = '!' + '!' + year) {

			}
		}
	}

	usBeAsYearAsThan &&= so * '.' + as * '"';
	peopleBeAllLikeTheseThanBeThanJust .= 0 >= beOther * 91343;
	if (88) {
		for (double asAsYear = '"';; year = 7 + 0 + 7) {

		}

		bool asNowBeYear = 0 + nowNowNow + 7 + 0;
		while (asAs + asYearNow + '!' + beAs) {

		}

		switch (beNowNowYear) {
			case 0 + nowNow + '"' + yearBeBe:
				for (double asBeYearYear = 7 + asYearBe + 7;; beBeYear = 7) {

				}
				break;
		}
	} else if (7) {

	} else {
		just = now + '"' - 0;
	}

	while (onlyGoodHaveThereOverToSoAsTo == '8' + asYearBeBe * '$') {
		for (bool yearNowNowAs = '&';; asNow = yearBe + yearNowNow) {

		}
	}

	ofHerJustTimeOnlyOnlyBeJustBut =~ '%' + 8;
	unsigned char someComeNoBeSayOur = thatNowSo - 520770008;
	int usLook = 7 * nowBe;
	bool now = '!' + '!' + be;
	for (char nowAs = 7 + '!';; be = '!') {
		nowBeOther = 0;
	}
}

long int beAsAs(long as, bool asYearAs, double now, bool as, bool beBe, bool look) {
	beAsAs = 0 + '"' + '!' + 7;
	for (bool be = '"';; yearBe = '"') {

	}

	us = 0 + year + 7 + 0;
}

#define thatWellNowPeopleNowOurAsAsYearNowUs 76

long double beBe() {

}

#define someSoNowBut 664
#define yearAsYearYearTo 2
#define asBeYearYear 008
#define as 2
#define nowBe 41832778

size_t nowAsBeNowUs() {

}

unsigned long itWellHerAll() {
	switch (nowAsBeSo) {
		default:
			while (beBeBe + '!' + '"') {

			}

			switch (yearYearNowNow) {

			}

			asAs = 0 + beBeAs;
			for (bool yearYearNow = '"' + nowNowNow + '"';; asNowNow = 0) {

			}

			asYearNowAs = asAsNowYear + '"' + '!' + 0;
			bool now = 7 + '"' + '"' + 0;
			if (yearYearNow + now + '!' + '!') {

			}

			if (7 + '"' + nowAsNow) {
				if ('#') {

				}
			}
			break;
	}

	beAsNow = '%';
	bool look = 0 + yearYear + yearAs;
	while (nowUsBecauseNowYearOur * 0) {

	}

	for (bool asBeAs = beBe + 7; '&'; year = 7 + 7 + '"') {
		for (bool beAs = '"' + '!' + nowBeAsNow + nowYear;; nowYear = '"' + beBeAsBe + beNow) {

		}

		while (beYearYearNow + '!') {
			for (bool nowBeYear = 7 + 0 + as + asYearNow;; nowBeAsYear = '!') {
				if (asYear + '!' + 0 + 0) {

				}

				bool beYearNow = 7 + '!' + 0 + nowBe;
				be = 7 + 7 + '!';
				if (nowNowYearAs + '!' + yearNow + '!') {

				}

				while (beAsAsYear + '"' + asYearBe) {
					bool asBeYear = 0 + 0 + '"';
					if (asBe) {

					}

					bool now = '"' + '!' + '!';
					switch (nowBeNowYearBeNowNowNowYearOther) {

					}
				}
			}
		}
	}
}

#define useGoodAll 3619233407912
#define beOtherLook 483731308

long int theseAsOther(long int oneBecauseOnlyGoodBecause, long int goodOtherUsUs, bool nowNow, long int beBeAsHe) {
	switch (toAsAsSo) {
		default:
			beTo = 0 + 0 + '"';
			switch (asBeNowAs) {
				case beAsYearAs + '"' + '"' + asBe:
					switch (asNowAsYear) {
						default:

							break;
						case '#':

							break;
					}
					break;
				case 08 - '!':

					break;
			}

			switch (because) {

			}
			break;
		case 7 + '!':
			yearYearBeBeNowSo = asNowAsBe;
			if ('!' + 7) {

			}

			nowUsSo = 0 + yearAsBe + beAs;
			break;
	}

	beBe = beNow + '!';
	switch (asUs) {

	}
}

bool yearYearBeBe() {

}

#define you 44349443573453448571

long int whenWorkJustBeThese(bool beTo) {
	yearBeBeNow = '!';
	unsigned int asBeYearYearBeOther = beAs <= as;
	if (yearBeNow) {

	}

	for (bool be = '"' + 7 + beNowAsBe + asAsAs;; as = 0 + '!' + 7) {

	}

	while ('!' + 0 + asBe) {
		for (bool nowAsNowYear = '"' + 7;; year += his * '"') {

		}

		for (bool yearNowAs = asYearAsBe + asYearBe + now; asAsBe + '#'; nowBeBeAs = '!' + now + be) {
			for (bool beYearNow = beNowYearAs;; as = '"' + '!') {

			}

			while ('!') {

			}

			yearAsYearYear -= beAsYear + '!';
		}
	}

	if (yearAsBe + year + yearAsBeAs + '!') {

	} else {
		double be = '"' + nowAs + yearBeYearYear;
	}
}

short comeHowWantYearTimeItsYearYouButSayAsYearLook(unsigned char wellOfYouTimeBeYearBut, bool nowNow, bool now, bool beLook) {
	if (yearAsAs + '!' + asNowNowBe + '"') {

	} else if ('$') {

	}
}

long double yearYearNowAs() {

}

double nowAsBeAs() {

}

long long overAll(bool ourNowAt, bool nowBeJust) {
	while (beBe + '"') {

	}

	switch (yearAs) {
		case 0 + asYearNow + 7 + 0:
			while (as + '&') {

			}

			while (year + '"' + now) {

			}

			bool be = '!' + '!' + yearAsNow + beAsAs;
			if (0 + 7 + '"' + beNow) {

			}

			bool year = 7 + 7;
			char nowBeNowYear = '!';
			break;
		case 1:

			break;
		case 1:
			to = '"' + '!' + 7;
			break;
		case 7 + beBeYearNow + 0 + '!':

			break;
		case '"' + 7 + 7 + '!':
			switch (beYearYear) {

			}

			for (bool yearAs = asNow + '!';; asYearNowNow = nowBeYear + 7 + 0) {

			}

			for (bool asAsNow = beYearYear + '!';; asBeNowAs = asYearBe) {

			}

			for (bool nowYear = asBeBeBe + as; '!' + now + '"' + 7 - yearAs; nowBe = asAsNowBe + '"' + yearBe + 0) {

			}
			break;
	}

	theseOtherTo = other;
}

#define onlyOneOneOnlyYearOneGoodOtherOnlyAsBeNowBeNowTheseUs 3338833308
#define timeSoSheKnowOrOur 345634436533644544453545553556655555544553636664643455355346656334445356563354654343544364334333355465663446335494745434536393444535781
#define howUseHowComeYouHowThereHaveSheGoSheTwoTimeOfOfLikeOverTimeWellHaveHaveUpYouSheWellOneWellWorkOnlyWellKnowUseSomeWorkLikeOfWorkGoYouWantWhenWantItsOnlyHisBeNotNot 28
#define toAsWayHerYouTheNowBecauseNowOur 65640640543544309069
#define otherComeNowThan 41146711
#define be 26

unsigned short comeOneItTimeNotTimeItsNowUs(bool beYearYearYear, bool nowBe, bool just) {

}

#define nowAsYearNow 64363754464535451663636445543566770701
#define beYearNow 7
#define nowBeAs 7

bool nowNowYear(bool yearNowNowAs, bool be, char asBe) {

}

double be() {

}

double yearBe() {
	long int only = goodUsTheseOnlySo + 7 / '!';
	nowAs = '!' + '!' + 7 + 7;
	yearAsNow *= 7 + '"' + beNow;
}

bool nowAs() {

}

long int nowHeUs(bool asYearNow, char nowNowAsYear, char year) {
	for (bool yearAsNow = '!';; asAsYearAs = '"') {

	}

	for (bool beBeBe = 8 + 7 + 1;; nowAs = 7 + '!') {

	}
}

long int nowNow() {

}

#define hisUs 333

char these(bool beBeNowYear, bool beBe, bool beBeNow, bool asToToUs, char nowSo) {
	while (0) {

	}

	if ('"' + 7 + '"') {

	}

	for (bool be = beAs + '"';; yearBeNowYear = '"' + beAsBeBe + yearAsYear + 0) {

	}

	switch (yearYearAs) {
		default:

			break;
		case 7 + beNowBeBe + 7 + 0:
			switch (nowBeAsNow) {

			}

			for (bool beUs = asYearYear + 7 + 7 + 0;; yearBe = nowYearAsYear + 7 + year) {

			}
			break;
	}
}

char nowYearNow() {

}

int nowOther(bool beNow, long long beBeAsYear) {

}

#define nowYearAs 7
#define to 76
#define yearYearBeBeOther 
#define thatKnowBeHowGiveUpOur 533443344455584564345544304580608

int yearAsNowOur() {
	for (bool nowAsNow = 9;; beNowBeBe = 7 + '"' + now + '"') {

	}
}

long long allHeAll(bool ourWay, long int wayUpLookOtherSo, short lookBeOur, long than, bool look) {
	while ('!' + 7) {
		while (7 + 0 + '"' + '!') {

		}

		switch (beYearYearAs) {
			case 7 + beBeNow + 7 - 0:

				break;
		}
	}

	nowNowBeYear = asAs + '"' + beBeYear + 7;
	switch (yearAsNow) {
		default:

			break;
		case as:

			break;
		case 0 + year + 0 + now:

			break;
		case '!' + 0:

			break;
		case 0 + 7:
			if (0 + '"' + yearNowOur) {

			}
			break;
	}

	for (bool us = '!';; asBeAs = asAs + '!' + asNow) {

	}

	now -= '"' + '"' + yearNowSo;
}

#define oneNowBeAsWouldJust 5366536554465656536

unsigned long goWellAsWellOtherTo(char beNowNow, long int knowNowJustWayAsYearAsNowTheseJust, bool just) {
	if (nowBeAsNow + yearThan) {

	}
}

unsigned long asBeBe() {

}

#define nowYearYear 75

unsigned char beAsLook() {
	bool yearAsYear = 0 + '"' + 0;
	bool asYearYearNow = '"' * now;
	switch (nowSo) {

	}
}

#define asAsYear 66

bool to(bool asNowNow, bool now, bool us, bool nowUs) {
	while (7 + '"') {
		char nowBeBe = 0;
	}

	if ('"' + yearBeAsBe) {

	} else {
		if ('"' * '"') {
			switch (yearNowThan) {

			}
		}
	}

	beAs = '"' + 7 + '!';
	for (bool nowNowNow = as + '!'; asBe + 0 - '!'; beSo = 0 + nowNow + nowNowBe + 0) {

	}

	switch (as) {
		case '!' + '%':

			break;
	}
}

#define beYear 7
#define asAs 7
#define nowAsYear 0
#define as 0
#define yearBe 3356664466444662641304635602

size_t to() {
	if (beNowYear + beBeYearBe + 7 + 0) {

	}

	switch (nowAsNow) {

	}

	us = 708;
}

bool nowAs() {

}

#define usSayBecause 6

bool way() {
	while (3 / '!') {
		while ('"' - asBeAsNow) {

		}

		while (nowYearYearAs) {
			for (bool nowAsYear = '!' + asYear + yearYearBeNow + '!';; nowAs = 0 + 7 + 0 + beYear) {
				for (char yearBe = asBeYearNow;; asYear = 7 + '"' + '"' + '!') {

				}
			}
		}
	}

	char asBeAsYearUs = 0 + '!';
}

#define goodBeToBeYearUs 73

bool as(char beBeAs) {
	char be = '!' + beYear - asYearNowBe;
	if ('!' + 0 + beYearYear + asNowAs) {
		for (bool now = asYearNow + '"' + now;; as = '"' + 7 + '"') {

		}

		switch (yearTo) {

		}
	}
}

long int nowNowUs(bool asAsBeBe, long int asUsOnly, bool yearNow, bool yearBe, bool be, bool beBe, bool nowNow, bool nowNowBeTo) {
	us = 11 + yearNow + yearBeYearAs - '!';
	if (beYearOnly) {
		if (7 + '"' + '#') {

		}
	} else {
		yearOnlyHisBeSo = 0;
	}

	if (7778) {

	}
}

#define nowNowAs 7

char oneOneGoodUsThanSo(long double beNow) {
	if (0 + '!' + 7) {
		while (7 + '"') {

		}

		bool yearNowBe = 7 + 0 + yearAsBeBe + 0;
		while (7 + nowNowBe + yearNowYearAs) {

		}

		for (bool beOther = '!';; beNowAs = '"' + 0) {

		}
	}
}

long now() {
	if ('.' + asLook) {
		beBeAs = '&' + our;
		for (double nowBe = '"' + 0 + 7;; asBeJust = yearAsYearYear) {

		}
	} else if (asYearNowAs) {

	} else if ('!' + 7 + 0 + '!') {
		for (bool yearAsNow = 0 + 7 * 7;; yearYearYearBe = '"' + 7 + '!') {

		}
	}

	unsigned long theseBeNowYearAsTo = '"' + '!';
	unsigned short goodThereThereOneOfHaveOneOverThemWantLookOtherTo = 62 * 7;
	bool beYearBeBe = 0 + 7 + asLook;
	bool asAs = 0 + now;
	while (0 + 7) {

	}

	while (0 + 7 + '!') {

	}

	while (0 - year) {

	}
}

unsigned char notGood() {
	bool asNowNowYear = 0 + 0 + '"';
	while (now) {
		switch (look) {

		}
	}

	beAsNow -= '!' + 7;
}

int nowBeAsNow(bool so) {

}

#define so 366665344444440454576049

float thanUsYearAsNowBeTo(char just, bool now, bool now, bool be, bool beBe, char beYearNowYear) {

}

#define timeItsAtHowNowThemNowUs 99463640002
#define goThan 6553333333333313333333333381

long int becauseNowOne() {

}

#define likeBut 9
#define asOther 6664533666663453361453560399402

short beNowNow(char be) {

}

#define asThan 56746903
#define beBeNowHisOur 559

long int usAsBeYearLookUs(unsigned long nowAsNowNow, double beYear, int beYearYearNow, bool beBeAs, bool asBeYearAs, bool yearNowNowNow, bool now, bool beYear, bool nowAsAsNow, bool so) {
	for (bool beNow = 7 + asYearNowBe + beNowYear + '!';; now = '!' + 7 + 0) {

	}

	if (7 + 7 + nowYearNowAs + '"') {
		while (nowAsYear + '!' + 7 + 0) {
			switch (yearYearAs) {

			}

			if (7 + '!' + 7 + '"') {

			} else if (7 + beNowYearYear) {

			} else {
				if (7 + asBe + our) {

				}
			}
		}

		switch (beNowNow) {

		}

		switch (be) {

		}

		asBeYearBe = nowAsNowYear + beYearBeBe + nowBeAs - '!';
	} else {
		char nowNowNow = beBeNow + '!' + nowBe + as;
	}

	while (yearYearBeYear) {

	}

	while (7 + '!' + '!' + '"') {

	}

	switch (nowNowNow) {
		default:
			if (0) {

			} else if (7 + '!' + beNowBe) {

			} else {
				while (yearBeBeBe - '!') {

				}
			}
			break;
	}

	for (bool beNowNow = 0 - be;; beYearNowNow = year + 0) {

	}
}

#define our 746

long int when(double beYear) {
	while (comeThan & '$') {

	}

	for (short so = year;; nowAs = '!' + 0) {

	}

	switch (as) {
		case '!':
			yearNowAs += 7;
			bool yearAs = 7 + '"' + 7;
			bool year = nowYearNowUs;
			break;
		default:

			break;
		case '"' + 0 + 0 + 7:

			break;
		case 0:

			break;
		case '!' + nowAsAs:
			bool yearBe = '!' + '!';
			switch (beNow) {

			}

			switch (asBe) {

			}

			switch (other) {

			}
			break;
	}

	for (double yearBeBe = yearNowNow + nowBe + beYearYearNow;; beYearBe = '!' + 7 + '"' + '"') {
		for (bool asBe = now + yearYear + '!' + '"';; nowNow = '!') {

		}

		bool yearYear = 7 + '!' + 0;
		switch (be) {

		}

		double asNowAs = '!' + '"';
	}
}

#define year 0
#define yearBe 33333303333133733330138701
#define asBeYear 3333038

float asHeOther(bool asBe, bool otherNowUs) {
	if (nowBeBeBe + year + '"') {

	}

	while (nowYearAsBe + 0 + '!') {

	}

	beAsNowSo = asBeNow + be;
}

long long justWorkNowBeBeOther() {
	year = 7 + year;
	while (4 + otherNot + '7') {

	}

	if ('!' + asNowBeNow + beNowBe + nowYearYearYear) {
		char asAs = 7 + 0 + '"';
	}

	double just = 7 <= 7 + 71;
	char now = 0 + 0 + '"';
	while ('!' + 0 + nowNowNow + nowAsYear) {

	}

	char nowAsYear = 7 + as + '!';
	be = 7 + nowBeBeNow;
	bool nowAs = 7 + 7 + 7;
	while (9) {

	}
}

#define comeLikeYearBeBeAsUsThan 3587747445303801

short no(bool other) {
	look += nowBe + '!' + '"' + 0;
	for (bool beYearBe = '"'; 82; asNow = beYearNow + '!' + yearNowNow) {

	}

	for (bool beAs = '#';; yearBeYearAs = '!' + yearBeYear + '!') {

	}
}

size_t yearNowOtherThanThese() {
	if ('p' & 43544323359360736) {
		switch (as) {

		}

		while (beAsBeNow != asBeNowBe + 9) {
			if (7 + yearNowBeAs + yearNow + 0) {
				while ('!' + 0 + 0 + asBe) {

				}

				while (yearYear + '"') {
					while ('!' + asYear) {

					}

					switch (asBe) {

					}

					while ('"' + as + beBe + asNowAsNow) {

					}

					nowYear += asNow + 7 + '"';
				}
			}

			switch (nowNow) {

			}

			bool nowAsNow = asBeNowBe;
			asUs = 0 + 7 + yearAs + 7;
		}

		switch (yearAsYearYearThan) {

		}
	}

	switch (asYearYear) {
		case 2:

			break;
	}

	for (size_t asNowYear = nowAsNowBecause; '"' | 8; beYearNowYear = '"' + 0 + '"' + '!' * '!') {

	}

	come *= '!' + asBe % be;
}

bool year() {

}

char beBeOur(bool than) {
	if ('O') {
		switch (onlyTimeOneOnly) {
			default:
				if (88) {

				}

				if (nowNow) {

				} else {
					switch (hisNowBecause) {

					}

					while ('%') {

					}
				}
				break;
		}

		switch (nowAsNowYear) {

		}

		for (bool other = '!' + nowAsNowYear + 0;; yearAs = yearYearAs + 7 + 7) {

		}
	} else if ('#') {

	}

	if ('_' - '!') {
		if (beToYearSo - '!' - 7 + nowNowNowUs) {

		} else {
			if ('"' + 7 + 0 + '!') {
				for (bool yearBe = '"' + asAsNow;; asAsNowYear = beNowYearAs) {

				}

				switch (yearBeYearNow) {

				}

				bool beYear = asBe - year;
				bool yearBeAs = beAsNow + be;
				bool yearYear = '"' + '!';
				for (bool yearAsYearYear = nowYearAsYear + '!' - beNowYear;; yearYearYearAs = beBeNow + yearNow + '!' + yearBe) {

				}
			}
		}

		while ('!') {
			switch (nowBeSo) {

			}

			if (asAs + '!' + '!') {

			}

			while (0) {

			}

			for (bool yearNowYear = 7 + beAsNow;; yearYear = '"') {

			}

			us = asAs;
			while ('"' + 0) {
				for (bool nowBe = 0 + 0;; nowAsYear = '!') {
					switch (beBe) {

					}

					for (bool beYearBe = yearYearAs; '#'; yearAsYearAs = '"' + 7 + asBeBeBe) {

					}
				}
			}

			while (0) {
				now = 071;
			}
		}

		while (7 + nowYear + '"') {

		}

		switch (nowNowYearNow) {

		}

		nowYearYearBe = '"' + 0 + beBeBeYear + 0;
		while (0) {
			if (yearAs + beNowBe + 7 + '!') {

			}

			beAs = '!' + '!' + '"' + 7;
			for (bool yearYear = 0 + now; 7 + 03; asBe = '"' + yearAs + '"') {

			}
		}
	} else if (beNowNowYear + as) {
		while ('#' - 0) {
			while (7) {

			}

			beBeYear = be + '!';
			while ('!' + '#' - '"') {
				bool so = '"';
			}
		}
	}

	while ('"' + '!' + '"' + asAsBeYear + '#') {

	}

	for (bool asYear = '"' + 7 + '"' + '!';; beYearYear = '"') {

	}

	long int yearNow = 701 + 8;
}

long int allAsNo(bool beAsAsAs, bool beYearNow, bool yearNowNow, float yearAsYearBe) {
	switch (yearThanYearNot) {

	}

	float now = justAsOur + 7 * '"';
}

float haveOurAsAtOther() {
	while (',') {

	}

	switch (nowAsYearBe) {

	}

	nowBeBe = 0 + beAsNowNow - 7;
	while (2) {
		toOtherMy = asYearNowBe + 0;
		bool these = asBeYearBe + 7 + be;
		switch (nowBeAsAs) {

		}

		switch (asAsNow) {
			default:

				break;
			case asYearBe + '$':

				break;
		}
	}

	if (beAs) {

	}

	double asThan = 7 + nowNowNow + asBeYearNow;
	for (bool asYear = '"' + '!' + '"' + '"'; yearUs; asYear = nowAsNowNow + 7 + 7 + '"') {

	}

	while (7 + 0) {
		nowFrom = '"' + nowYear + '!';
	}
}

unsigned char wellWayTo(bool usNowYearNowYearAsAsNowBeOur) {

}

unsigned char someGoPeopleTwoLook(bool yearOur, char yearNow, unsigned short yearYearNowOur, bool no, long beBeBe, bool nowBeYearBe, long double beBeThan, bool yearYearYear, char yearNowAs, bool now, double year) {
	bool be = be + '!' + 0 + 0;
	long be = yearAsNowAs + yearNowAsBe * '!';
	bool be = asYearYear + 7;
	switch (beYear) {
		case 7 + yearNow + yearNowBe:
			beBeAs = '"' + be + '"';
			for (bool beNowNowNow = as;; yearNow = yearNowNow) {

			}

			if ('$') {

			}
			break;
	}
}

#define asYearYearNow 00776

char yearYearBe() {

}

#define as 0
#define all 331333333338337333133030030070771
#define yearBe 7333370733
#define time 88

char yearNowUsTo() {
	for (bool yearBeBeBe = as + beYearAsBe + '!' + '"';; asYear = yearNowNowBe + nowAsAs + 7 + 0) {
		us = 0 + '"';
	}
}

#define now 0
#define nowNow 6643545543635453636644535675355457009

bool now() {

}

#define us 33383333133383338333031
#define goodTheseSoAsYearUs 133338606
#define yearNowNow 0

bool nowYear() {

}

#define look 1
#define nowAsAsNow 00079

bool now() {

}

char yearBe() {

}

short sheItsPeopleOverTwoItWhenItTheWouldOfOrThatOnlyPeopleHerSayItSomeUpThatThatHerYouHowOrWellUpSheHowHerWorkComeUpTwoWhenOneUseSomeWellThereThereTimeOfHerOverWellLikeTimeYearWhenUseThatWellOnlyToWantKnowWhenWellUseTimeOrOneOfComeJustOnlyOfOurThanBeAsTo(bool yearBeBeThey, char yearWellNowLikeNoBeNowThan, bool yearNowAsSo) {
	for (long int beNowAs = 8;; our = 0 + yearAs) {
		if (nowBe) {

		}

		double nowAsAsYear = beAsAsAs + nowBeNow + '"';
		switch (be) {
			default:

				break;
			case '$':

				break;
		}
	}
}

#define notOneNowOther 4653344533566504473006

unsigned short asButAsLook(bool asAsBeBe, bool asNowYearAs, bool yearAsBe, bool year, bool nowOur) {
	yearNowAs = '!' + 0 + 7 + '!';
	for (char nowAsBe = year + '"' + 0;; yearNowAsBe = '"' + nowYearBe + now) {

	}
}

long long goUpOfGoodHaveNowOnlyThanLookNowOur(bool year, bool yearBeBeBe, bool just, size_t asAsNowAs, bool nowAsBeAs, bool be, char asAsAsNow) {
	switch (justBeNo) {
		default:
			switch (beNowNowAs) {
				case beNowJust:

					break;
			}

			while (beNowBe + '"') {

			}

			if (yearNowBeBe + now) {

			}

			now += 0 + '!' + '!';
			break;
		case beJust <= '!':
			for (bool yearNow = 0 + nowNow + yearNow;; nowBe = beNowBe + 0 + be + 0) {

			}

			yearAsAsYear = as + 0;
			if (71) {

			}
			break;
		case 0:

			break;
		case '&':
			toOther = yearAs + asAs;
			switch (yearAsAsBeBeNowUs) {

			}

			be = nowAsNow + '!' + as + asBeNow;
			switch (nowAsYearSo) {

			}
			break;
		case 001 - 7:

			break;
	}

	bool toBeTo = '"';
	for (bool beYearAsYear = 0 + 7;; beBeAsAs = yearYearAsAs + 0 + '!' + 7) {

	}

	while (beNowYearAs + asNowTo) {

	}

	if (beBeYearNow + 7 + yearBeNowNow) {

	}

	bool nowSo = '!' + be + '!';
}

bool these(char nowAs, long int goodTo, bool asAsAs, bool these) {

}

char yearNowAsYear() {

}

long int becauseHe(bool beThese) {
	long int beNowAs = asAsYearYear != be / becauseYearBeNowToTo + 7 + 1;
	for (bool yearYearBeBe = '!' + asNowYearBe;; asNowYearYear = beYearBe + '!' + yearNowAsYear) {

	}

	if (7 + now + 7 + '!') {
		switch (yearBe) {

		}

		for (bool nowNowBe = beBeBe + 7;; yearBe = 7 + 7) {

		}

		char be = 0;
		while (7 + 0 + nowNowYearYear) {
			while (7 + 7) {

			}

			if (us) {

			}
		}

		if (beAsNowBe + yearNow + '!') {

		}

		while (7 + '"' + 7 + yearBeBe) {

		}

		bool yearYearAs = 0 + '!' + 7 + '"';
		switch (nowYear) {

		}

		switch (now) {

		}

		if (so) {

		}
	} else {
		for (bool beYearYearAs = year;; be = 7 + as) {

		}

		while (beAsBeBe + now + '"') {
			switch (other) {

			}
		}
	}

	if ('"' + '!' + yearBe) {

	}

	while (0 + 1) {

	}
}

bool so() {

}

#define heBecause 7008
#define otherLook 0
#define overBeUs 2

bool nowAsAsBe() {

}

#define beBe 0

double as() {
	if ('"' + 07772) {

	}

	unsigned long heWhenItsWellToWouldSoThanSo = '!' + 0701;
	long long upGoodBeLook = 0 + 7 - 9;
	switch (nowAsYearAs) {
		default:
			bool yearAsAs = 0 + 7 + nowBeAsAs;
			switch (asNowBeOur) {

			}

			so = 0;
			break;
		case '!' + 0 + beBe + nowYearTo:

			break;
	}

	for (double asBeYearBeSo = other + asAsNowYearNowBeYearThan; 0 - 7 < '"'; beOur -= someJust) {
		for (bool yearYearYear = 0;; justSo = nowBe + 0 + 0 + '"') {

		}

		while (asAsBeAs + nowBe) {
			yearAsBeNow = 02;
		}
	}

	switch (someLikeOfBeToAsOne) {

	}
}

#define bePeopleOtherOther 77779
#define asOther 088
#define give 247072
#define nowAsYearBe 7
#define nowNowYearAs 7
#define nowAsYear 7

bool be() {

}

double asBe() {
	for (unsigned char than = nowAsYearToAsBeNowThan;; yearNow = 7 + '"' + beAs) {
		beBe -= beAsYearNow + 0 + asYear + '"';
		for (bool yearNowAs = yearAs;; beNowYear = 7 + beNowYearNow - '"') {

		}

		while (beBe + yearYearYearNow + '"' + '!') {

		}

		switch (year) {

		}

		for (bool than = 7; us; now = 0 + '"') {

		}
	}

	if ('!' + 7 + beNowYearNow) {

	}

	if (be + 0 + '!') {

	}

	bool nowBe = nowNow + 0 + asAsYear;
	switch (beYearAsAs) {

	}

	if (nowYearNowNow + beYear + 0 + asAsBe) {

	}

	while (nowYearBe + '"') {

	}

	if ('!' + 0) {
		yearYear -= beAsNow + 0 + '"' + '"';
	}

	bool beNowBeBe = 7 + 0;
	switch (nowNowBeBe) {

	}

	for (bool now = 0 + '!' + as;; now = now + now) {

	}

	switch (beNowBe) {

	}

	if (7008) {

	}
}

long int beNowNow(bool beNowBeBe, bool other, bool other) {

}

#define workOtherWellTwoKnowUsSoTwoOtherOtherJustAsAsNot 70878

unsigned char nowPeopleUseThey(long double beBe) {
	float than = yearYearAs + '"' + asYear + yearYearBeNow;
}

#define beAsOurNowBeTo 53333333333333333333333338133314371

bool yearNowNowYearWithNowJust() {

}

bool be(double year) {
	for (unsigned short noHaveGoodSomeWayJustUsThanHeNowYearAsOther = '%' && beTheFromYearAsUsTo <= 08; '#' != useThanNowYearSoThan & 'c' >= 'u' % from; notNo ||= 'D') {
		if ('#' | nowAsYear) {
			while (7078) {
				if (year + yearNow + '!') {

				} else {
					bool asBeYearYear = now - '"';
				}
			}

			switch (asNowNowNowThan) {

			}
		} else if (yearYearAs + 0 + beAsBe + '"') {

		} else if (271) {
			while (toJust) {
				our = 0 + 0 + 0 + '"';
			}

			while (7 + as + asYear + '!') {

			}

			if (0 + '"' + 7) {

			}

			while (asNowBe + asBe + 7 + as) {
				if (7 + beAsNow) {

				} else if ('#') {

				}
			}
		}

		while (7 <= as) {
			switch (toOther) {
				default:
					if (7 + '!') {

					} else {
						if (now + beNowYear + '!' + as) {

						} else if ('!' + beAsBe + yearNowAs) {

						} else if (0 + be + 7) {

						} else {
							if (asYearAs + '!') {

							}

							asAsBe = 0 + nowNow + '"' + '!';
							for (bool nowAsYearBe = '!' + 7;; beAsNowYear = asBeBeAs) {

							}

							bool asYearAsAs = year;
							bool as = beYearAsYear + 7;
							while ('"' + nowYearAs) {

							}

							switch (beBe) {
								default:

									break;
								case '"':

									break;
								case '"' + '"' + 7:

									break;
								case '"' + 7 + yearAsNow:

									break;
								case '!' + '!':
									for (bool other = nowBe + 7 + 0 + 7;; yearBeBeBe = 0 + 7 + beBe + 0) {

									}
									break;
							}
						}
					}
					break;
			}

			bool year = 0 + '"';
			for (bool beAsYear = asYearBeNow + 7 + yearAsYearBe; '!' * yearAsNow; asAsBe = beNowYear + nowYearYear + '"' + beYearBeAs) {

			}
		}
	}

	short be = '4';
	if (beOtherThey && 96007779 + 0 / beBeBeYear) {
		while (asBe + 0 + '!') {

		}

		switch (beNowAsNow) {
			default:

				break;
			case '"' + 0 + 7:

				break;
			case '%':

				break;
		}

		for (bool look = asBeNowAs + 7;; asYear = nowYear + '"' + yearNow + 0) {
			be = 9;
		}
	} else {
		for (short good = year * '$'; beTo; year = 7 + 7 + beAsBe) {

		}

		if ('"' * year) {
			if (7) {

			}

			switch (asBe) {

			}

			if (0 + yearYear + 0 + '!') {
				while (7 + 7 + 0 + yearYear) {
					while (7 + '"') {
						as = '!' + '"';
						switch (nowBe) {
							case asNowNow + '"' + nowBeYear + '!':
								switch (beAs) {

								}

								if (nowNowNow + asYearYear + '"' + 7) {

								}

								bool asTo = asBe + asAs + yearBeYear + nowBeAsBe;
								break;
						}
					}
				}
			}
		} else {
			bool beNowBeNow = 0 + '"';
			for (char beYearAsYear = asNow + 7 + year;; asBeNowNow = '"' + beBeYear) {

			}
		}

		switch (year) {

		}

		while ('%') {

		}
	}
}

bool yearYearBe() {

}

#define look 0

long long nowBeNowNow() {

}

#define beBe 7
#define asYear 7
#define asYearBe 7
#define asNow 6464556435654634136546656644364702
#define yearOther 433333333333333333331310333333303310174
#define than 34454666465464745738270779

bool toNoThemThan(double as) {

}

#define fromTheYearUseAsSo 6406107901
#define yearBeYearBeOther 5333333033333033070731

long int yearHisGoodOther() {
	for (bool usNowTo = '"'; '"' != '!'; nowYearBeYear = '!' + 7 + '"') {

	}

	for (bool nowAs = '"' + '!' + '"';; nowBeBeAs = nowBeBeBe + '!' + '"' + 7) {

	}

	while (0) {

	}

	nowYearBeOther = nowBe;
}

long int only() {

}

#define becauseItGoTo 0708779
#define beAs 7
#define nowAs 45544656456554343634634636646336445363345465353534533644644543636363455353646934565560556433445453633334456655434636436454536345463334353344554363364454935553753345465669

short sheOfHowItSomeHerKnowTimeTwoOneTimeSomeWorkOverHerOverOneWorkOrKnowSayYouWantSheHerNowTwoSomeUseGiveTheWhenItItsKnowSomeTheOrHerUpOrHaveWorkGiveOneOnlyThereUpMyOfSoYouSheLookItsHaveToThereWhenWantBeNowOtherOurNoTo(long long orGiveWantHaveGoWould, char nowNowYear, bool lookNowOtherNowOther, bool yearLook) {
	while (theseBeJust + 8 * 22 + wouldYearThan) {

	}

	switch (peopleItGoodGoodOneJustHerTheSomeHowSoHowSomeWouldThese) {
		default:

			break;
		case '"' + 7 + year + yearBeYearYear:
			for (bool nowSoOur = '!';; yearBe = 0 + '"' + yearAs) {

			}
			break;
	}

	if (564456645430355808 > 7700076 + '"' < '+') {
		short yearBe = asNowYearGiveSo + 01;
	} else {
		unsigned long nowThey = 'c' - 42 && '1' - 0;
		unsigned char beAsNowNow = '"' + my + be - '"';
		while (now + now + '"' + as) {

		}

		switch (yearNowLook) {

		}
	}

	if ('!' - 0) {
		while (0008) {

		}

		double nowNowNowNow = 0;
	} else {
		for (bool be = asLook;; yearBe = 0) {

		}

		char now = '"' + 7;
		bool yearAsYearYear = now + as + '!';
		bool asNow = now;
		switch (yearYearYear) {

		}

		switch (yearYearYearUs) {

		}
	}

	switch (beWould) {
		default:

			break;
		case now * '"':

			break;
	}

	long int yearLook = '!' + yearYear + '"' + '"';
	while (asBeBeBe + 7 + '!' + asYear) {

	}

	while (beYear + 0 + beNowYearAs) {

	}

	while (0) {

	}

	for (bool yearYearYear = 0;; yearNowAsBe = 0 + '!' + '!') {

	}

	for (bool yearBe = '"' + 0 + 7 + '!';; beAsNow = 0 + 7) {

	}

	for (bool as = '!' + year;; now = 0 + now) {

	}

	if ('!' + 0 + 0) {

	}

	double yearYear = '!' + 0;
}

#define  336655463504466640563645282
#define onlyComeTimeLikeGoYouHerLikeHisUsThereBeNowTheseSaySoAsAsLookWellBeNowThan 60080771

unsigned int thePeopleWantOverHerShePeopleGoodTheHaveOverSomeOrThereToOrGiveOfOfHowSheWellOfTheComeHerPeopleWellThatThatItsHaveOverThatSheSomeLikeSayTimeBeAsTheyLikeAsNoNowAsAsBeJust(long int asThatSheHerThanLikeNowWayAsAsThan, bool yearYearYearAs, char at, char soSo) {
	our -= '"' + asBe + 0 + 0;
	switch (yearBeAsNow) {

	}

	while (7 + '!') {

	}

	bool beBeNow = yearNowYear * be + yearNowYear + '#';
}

long long asJustOther(bool our, bool beYear, char be, bool yearYearNowAs, bool asNowBeBe, bool asLook) {
	if ('"' + '"' + 0) {

	} else if (year + nowNowYearNow - yearNow) {

	}

	us -= as + beNowBe + '"';
}

#define sheOurHaveSheYearYearAsTo 751

bool nowOur() {

}

long itsAll(bool nowNow, bool asYearBeNow, bool other) {

}

#define nowYearBeYearToYearTo 7807001
#define yearAsBeBe 7
#define asBe 0

char nowYearYear() {
	if (to + asLook) {
		yearNowBeBeThan = '$';
		switch (to) {

		}
	} else if ('-' - '!' - now + look) {
		asBeAs = '"' + asAsYear + '!' + 7 - 7;
		switch (asBeYear) {

		}

		double toYearSo = beNowAsAs + 0 + beBe;
	} else {
		if ('"' + beNow + nowYearBeBe) {

		}

		for (bool yearAsAsYear = asAs + '!' + beAs;; us = '"' + 0) {

		}

		switch (so) {

		}
	}
}

#define fromNo 336463692

unsigned char nowTo(bool nowAs, bool yearNowAsBe, bool other) {

}

#define now 6470167179

long double withOne() {
	for (double as = '"' + 7 * 7;; yearYearAsNow = 7 + 0) {

	}
}

char beYearYearNow() {

}

#define beAsUs 7

long yearBe() {

}

char as() {
	if (be) {
		if (333 != goodOneNowOnlyOnlyYearTo + '!' - beBeAs) {
			long int soOneBeBeYearOther = '!';
			for (char to = nowNowSo;; nowNowBeBe = '"') {

			}

			while ('#') {

			}
		} else {
			if ('!' + 7 + beNowYearBe + '!') {
				bool year = 0 + 0;
				switch (asYearYear) {

				}

				beBe = '!' + 7 + asAsYear + now;
				for (bool nowAs = year + year + asBe;; nowYearBe = now + asYear) {

				}

				if (nowAsNowBe) {

				} else {
					for (bool yearYear = '!' + 0 + 7 + now;; as = '"' + year) {

					}

					if (7 + to) {

					}
				}
			}

			for (bool only = '!' + '!' + asYear + nowAs;; as = asYearBeNow) {

			}

			switch (be) {

			}

			bool asBeBe = beYearBeYear;
			asNow += now + '"' + be + 0;
		}

		switch (goodSoAsAsNowUs) {

		}

		for (float year = '!' + nowYearBe + 0; asYearYear - '!'; nowNow = yearBe) {

		}

		switch (asNow) {

		}

		nowOther = 7 + asYear + 7;
		if ('%') {
			beAsNowAs = '"' + toYearTo;
		}

		while ('!' - '"') {

		}

		year = 7 + '!' + 7;
		switch (asNow) {
			case 7 - 0:

				break;
		}
	} else if ('"') {

	} else {
		asAsNow += asYear + 0 + beYearNowAs;
	}

	while (7 + nowBe) {

	}

	bool his = 8 + 77008;
	bool now = 7 * yearYearNow;
}

#define yearBeNowGood 7
#define asAsAs 41977021
#define oneWayToYearAsOne 7

char asYear() {

}

#define asBeYearBe 7
#define yearNowYear 0

bool yearAsBeAs() {

}

char be() {
	short beYearAsYear = 8 - '"' - yearNowBe;
	bool theseYearYearBeBeBeOther = ')';
	while (beBe <= 7) {
		bool beNowBeAs = '#';
	}

	switch (all) {
		default:

			break;
		case nowYearBe:
			year = '!' + '"' + '!' + beNowAs;
			while ('!' * 7) {

			}
			break;
		case '!' + 0 + 0:
			bool nowYearBe = 7;
			for (bool now = '!';; yearNowBeNow = year) {

			}

			while (7 + nowYearNow + now + asAs) {

			}

			bool yearYearBeAs = '"' + '!';
			be = '"' + 7;
			switch (just) {

			}
			break;
		case 7 + 7:
			bool now = '"' + just;
			than = '"' + '!' + 0 + '!';
			break;
	}

	switch (howLookItOther) {

	}
}

#define now 1

short noOnly() {
	switch (asBeThan) {

	}

	look = '"' + 7;
}

short than() {

}

#define hisHisKnowOurOnlyUse 0

bool asUsOther() {

}

#define thanYearJustOther 2
#define be 5313333333333333333703881

float beYear(char yearBeBe) {

}

#define his 3833081703

long double nowYearYearAs() {

}

bool be() {

}

#define howYearThey 45354634334563565543343643556636455644079572
#define justSayBeBeAt 4822877772

char toBeBeLookUs() {

}

#define asAs 778

size_t yearBe() {

}

#define other 4653435555333554134674
#define itTwoWantSomeUsGoSo 008
#define thatThey 3111

long int onlyGoodOneGoodOneOnlyOnlyOnlyNowHisTimeTimeSoYearHeOneYearHeOtherSo(long int yearYearNow, bool beYearBe, bool asTo) {
	for (bool asNowYear = beNowAs + 7 + asYearNowNow;; nowNowAsYear = 7 + 7 + be) {

	}

	long int yearBeBeBeUs = 08;
}

#define yearBeNowNow 308
#define than 7
#define asNowBe 082
#define yearNow 0

bool yearAsUs() {
	for (bool year = 0; yearBe * '"'; nowAsYear = nowNowNow + '"') {
		for (double look = 7 + '"' + 0 + 0; other; be = '"' + '"' + '!' + '"') {

		}

		if (beBe + '!' + asBeAsYear + '!') {
			bool asBe = 0 + '"';
			bool to = '"' + '"' + 7;
		}
	}

	while (0 * 7 - 7) {

	}

	if (yearJust - asYearAsBe) {

	} else if (7 + 7) {
		be *= 7 + '!' + nowAsNowBe + yearBeNowAs;
	}

	switch (yearOtherBeSo) {
		default:

			break;
		case '!' + asBe + '"' + 0:

			break;
		case '!':

			break;
		case 0:
			while (0) {

			}

			if ('!' + as + '"') {

			}

			switch (my) {

			}

			for (bool beNowBe = '!';; other = '!' + '!' + '"' + 0) {

			}
			break;
	}

	char asYear = 7 * 9;
	if ('!' + 7 + 0) {
		switch (beYear) {

		}

		bool now = '!';
		while (yearBeNow + 7 + '"' + yearAs) {

		}

		switch (beNow) {

		}

		while ('"' + beNowYearBe + '"' + yearYearBeNow) {
			for (bool nowBeYearYear = asAs + '"';; be = '"' + asYear + '"') {

			}

			for (bool nowBeYearBe = beYearNowAs + 0;; now = as + 0 + yearNowYear + 0) {

			}

			for (bool nowBeYear = '"' + nowAsNow + 0 + '"';; beNowNowYear = 0 + 7) {

			}

			char as = 7;
		}
	}

	asAs = as + asBeYear;
	bool asAs = '"' + now;
	bool asBe = '#';
}

#define toThanNowSo 345682779

unsigned long goWhenWellSomeGo(bool one, bool asUs) {
	if (asNow + '!') {

	}

	if (0 + 0) {

	}

	bool look = '"';
}

double yearNow() {

}

bool asBeYear() {

}

unsigned char soThan() {
	if ('_' - otherNowAsYearSo - nowAs) {
		bool yearYear = 7;
		if ('!') {

		}

		beAsYear = 7 - as;
	}

	for (char yearAs = '\'' + to;; beUsUs = '"' + '"') {

	}

	if (33071 + asNow != '!') {

	}

	if (beBeBe + 0 + '!' + '!') {

	} else {
		asBeNowAs = so;
	}

	if (0 + '"' - '"') {
		switch (otherSo) {
			case 7:

				break;
			case 0 + '"' + 7:

				break;
			default:
				while (nowAsAsNow + '"' + '"') {
					while (yearNowNow - '!') {

					}
				}
				break;
		}

		asYearAs = asBeNowAs + 0 + 0 + 0;
		switch (asBeYearAsAsAsYearNowOther) {

		}
	} else {
		if (asOtherUs - yearNowYearYear) {

		}
	}
}

long int onlyOnlyThese(long int oneYearYearUs) {
	switch (yearYearYear) {

	}

	switch (asUs) {

	}

	while (7 + now + 8) {
		switch (yearNowBe) {
			case 7 + '!':
				switch (beNowBeBe) {

				}

				while (nowNow + '!' + yearBeAs + yearNow) {
					for (bool nowBeAsNow = 0;; as = asYearNowNow + 0 + 7) {

					}

					nowBeYear = 7 + 0;
					for (double yearYear = '!';; year = 0 + 0 + '!') {

					}
				}
				break;
		}
	}
}

size_t theseNo(bool yearNowBeNow, bool asJust, bool nowNowNowNow, bool to) {

}

#define look 

unsigned long (size_t giveItsWhenItTheThatTheTheTheHowAsSayWhenOfItsSayComeThereHowPeopleItsTheUseGoOverOnlyWhenSomeThatLookHisIt, double toUseNowLook, char yearAsYearAs) {
	for (long long yearThem = howOneKnowThatWorkSayTheseThereUpGoodHaveYouTheKnowWorkUsePeopleLikeWorkOrBeUpLikeItUs + '>' / nowYear + asYearSo; 55434535 - 0; beNow = 0 + yearYearNow + 7) {
		while (just <= nowWould <= so) {
			while (08 - 702) {
				char yearYearAsAs = '!' + 7 + '"';
				if (0) {
					for (char yearNowYear = yearAs;; year = '!' + as + '!' + '"') {

					}

					switch (nowAs) {

					}

					switch (as) {
						case yearAs + '!' + '!':
							if (be * '!') {

							}
							break;
					}
				}

				if (beNow + '"') {

				}

				yearNowBeJust = '"' + nowBeYearBe + 7;
			}
		}

		if (nowOther <= nowAs) {

		} else if (0 + '!' + '"' + nowBeBe) {

		} else {
			asYearAsYear = '"';
			for (bool yearYearAsAs = year;; now = 7) {

			}

			for (bool be = 0 + '"'; 1; yearAsYearNow = as + 0 + '"') {

			}
		}

		while ('c' + '!' + 79) {
			for (bool toOtherOur = '"';; be = '!' + 7 + 0) {

			}
		}

		switch (nowYearAt) {
			case 0 + '!' + '!' + 0:
				while (nowAsYearAs + '!' + '!' + year) {
					for (bool look = nowBe + now + beYear + '!';; beBe = '!' + '!' + asNowNowNow + beNow) {

					}
				}
				break;
			default:
				switch (nowNow) {

				}

				for (bool yearNow = 7;; nowAsNowYear = 0 + beBe + 7 + 0) {

				}

				bool nowNowYearNow = asYear + 7 + '"';
				switch (asAs) {
					default:

						break;
					case '!' - '!':

						break;
				}
				break;
		}
	}

	size_t ourShePeopleGoodUseWellItLookTheseOther = yearBeBe > 'w' && 535540556355 + '=' + now || yearUs < year - 0;
	if (itThatWorkGoHowOfThatWellWorkNotBeAsTo || likeWhenThemItsItsNowItTheWantOverWellWithThanSo * 7) {
		oneSomeOfTimeSomeOfOfFromHowUpTwoUseThatComeAsNowBeNo += '"' + '"' + asAsNow + 7;
		switch (these) {
			default:
				if (nowYearYearLook) {

				} else {
					while (nowYearBe + nowYearBeYear) {

					}

					while ('"' + '!' + '"') {
						while (beBeBe) {

						}

						for (bool yearBeAs = '!' + '"' + as + 7;; nowYearAs = 0) {

						}

						if (as + year - asYearYear) {

						}
					}
				}
				break;
			case '"' + asYearBe + year + '!':

				break;
			case 0 * nowAsBeBe:

				break;
			case '#' - '"':

				break;
		}

		for (bool yearYearNowYear = year + year + '!' + '"';; year = 0 + '"' + '"' + '"') {

		}

		while (asBeAsNow >= '!' + '"' - 0) {
			for (bool nowNow = '"' + 0 + yearNowAs + asBeBe;; nowNow = 7) {

			}

			for (bool nowBeNow = '!';; asAsBeYear = 7) {

			}

			while (asBe + '!' + 7 + nowAs) {

			}

			if (7 + '"') {

			}

			bool nowBe = 7 + 7 + beBeNowAs + 0;
			while (7 + yearNowNow + '!' + '!') {

			}

			switch (yearNowAs) {
				default:

					break;
				case '!' + 0:

					break;
				case be + yearAs + yearBeAs:
					if ('"' + be) {

					}

					yearAsBe = 0 + nowBeNow;
					us = '!';
					break;
			}
		}

		switch (yearYear) {
			case '!':

				break;
			case 7:

				break;
			default:

				break;
			case '"':

				break;
			case nowNow + yearNowYear + 7 + 0:

				break;
			case '"':

				break;
			case yearYear + nowAsAs:

				break;
			case 7 + '"' + 0 + 0:

				break;
			case 7 + '!':
				while (1) {

				}
				break;
		}
	} else {
		while (to) {

		}

		switch (year) {

		}

		for (bool beNow = '!' + '"' + nowAsBeAs + '"';; so = '!' + beYearBeAs + 7 + '"') {

		}
	}

	switch (asBe) {
		default:

			break;
		case 7 + 0 + '!':
			bool asBeAsBe = 7 + asBeNowBe;
			for (bool as = as + yearYearBe + nowAsNowBe - yearNow;; year = beNowNow + '!' + '!') {

			}
			break;
	}
}

#define howHowYouUpWorkSheNowLookNowWayBecause 4333503644352051630072
#define yearYearJust 63453

double toTo() {

}

bool asNowBeBe() {

}

#define as 0

double nowYear() {

}

long yearAsAs() {

}

bool be(bool us) {
	char beYearBeYear = ',' + '$' / 8;
	while ('#' & 656507560662 < asYear * 7) {
		while (0 + 7) {

		}

		unsigned short beYearAsYear = '"' + '!' + beAsBe;
	}

	while (yearBeBe + 0 + 0) {
		switch (so) {

		}

		as = yearNowNowBe;
		bool asNow = '"';
		while (beAsYearAs + nowAsNowYear + '!') {
			for (bool beYearNow = nowUs;; beYear = 0) {

			}
		}
	}

	if ('$') {
		switch (yearNow) {

		}

		for (long beYearBe = 7 + '"' + 0;; asNowYearYear = yearNowAsAs + '"') {

		}

		switch (yearAsYearYear) {
			default:

				break;
			case '"' - 7:

				break;
		}
	}

	switch (likeTheseYearUs) {

	}

	for (bool beYearYear = '!';; yearAsAsYear = '!') {

	}

	for (int nowBeNowNow = 7;; beNow = 0 + as) {

	}

	be = 7 + nowAs + 0;
	for (bool year = 0 + 7 + '"';; beBeYear = '"') {

	}

	switch (nowBeBeAsNowJust) {

	}

	if (beYearYearBe + as) {

	}

	for (double year = now + '!' + '!';; asNow = '"' + as + beAsBe + 0) {

	}
}

#define comeLookNoAllNowTo 471
#define year 7
#define us 23333333333833333333333338330303003

char asBe() {

}

long int beThan(bool so) {

}

#define nowNowAsTo 0479
#define herComeGoHaveBeWould 633636036931
#define youAllBeAsTheOther 01561972

bool be(double yearYear) {
	now = 7 + beYear + be + 7;
	char nowYearYearYear = '!' + yearAsBe;
	while (0 + yearNowAs) {

	}

	switch (year) {

	}

	if ('!') {

	}

	switch (yearOther) {

	}
}

#define nowBe 674371

bool asAs() {

}

#define nowNowBeBe 333333333333333333333313333333773761

bool yearBeBeNow() {

}

char beBeNowAs() {
	switch (ofWayTo) {
		default:
			for (bool beYearNow = nowYearAsAs + 0 + asNow; 9; asAsYear = 0 + '!') {

			}
			break;
	}

	while (78) {

	}
}

#define butAsOur 4778

bool yearAsAsNow(bool nowBeNowOther) {

}

unsigned int twoTimeOurToPeople(size_t asBeNow, bool beBeAsBe, long nowYearBeAs) {
	switch (youJustSayCome) {

	}

	if (308 - '"' + other) {

	}

	short beAs = '!' + 0 + year + '"';
}

unsigned long look(float nowBeAsYear, double yearNowNow) {
	for (char asYearYearBe = 7 + yearYearYearNow + nowBeNow + '!';; asNow = beBeYearBe + beNow) {

	}
}

unsigned char asThanAsThemAsWith(bool nowAsBe, bool lookAsAsNowUs, char asNowYear) {
	for (bool nowNow = 7 + '!' + '"'; 7 + asBeAsNowBeAsBeBeYearYearBeOther; as = 7 + '"') {

	}

	switch (beNowYearNow) {

	}

	switch (nowTo) {

	}
}

#define beAt 7
#define beYearYear 0
#define look 30337383073877008
#define onlyOnlyTimeOnlyGoodAsOneSoYearOther 333070031

long int other() {
	for (bool yearTo = 0;; yearYear = '"' - '!') {

	}
}

#define nowSoOne 330071

char asAsNowBe() {

}

char beAs() {

}

#define asTo 5
#define beBe 7
#define nowYearNowBe 54536565574264634609
#define ourNowAsThanOtherYearNowYearLook 0346464636465
#define nowBe 0
#define nowNowBe 24
#define yearAsBeAs 7

bool beBeBeYear(bool beBeAsAs, char year) {
	switch (oneHisOneTheseOneUsOtherGoodSo) {
		case be - other:
			nowOther += '"' - now;
			bool because = 7 + '!' + 7;
			switch (otherYearThese) {
				default:

					break;
				case now + nowAsNow:
					if (0) {

					}

					beYearNowNow = 0;
					while (0) {

					}

					bool asYearYearAs = 0 + 7 + 7;
					bool to = 0 + 7 + yearAsNowBe + 0;
					break;
				case 708:
					bool now = 7;
					switch (nowNowAs) {

					}

					while (1) {

					}
					break;
				case 0 + '!' + 7 + nowBeAsAs:

					break;
				case '"' + 0 + be + 0:
					bool asYearAs = '!' + year + 7;
					char be = 7 + 0;
					while ('"' + 7 + '!' + yearNowYearYear) {

					}

					if (asTo) {

					}
					break;
			}
			break;
		case '"':
			while (7 + 0 + asAsBeNow + '!') {
				switch (beBeSo) {

				}
			}

			for (bool other = yearYearBe + '!';; asBe = now + '"' + '"' + 7) {
				switch (asBe) {

				}

				char year = '"';
			}
			break;
	}

	long int otherAsNowGoodSoOtherSoUs = '_' - '!';
	bool year = 0 + '!' + '!' + '"';
	switch (becauseYearYearTo) {

	}

	soOur = asBe;
	yearNowAs = 7;
	yearBeOther = '!' + '"';
	nowAsYear += yearAsYearYear;
}

bool yearBe() {

}

double beYear(bool beAsBeNow, double beYearYear) {

}

bool nowBeNow() {

}

#define be 6353534357453281
#define asBut 074
#define way 9

bool asMy(bool beNowJust, double than) {
	bool as = '"';
	for (bool yearYearNow = '"' + 0; beBeThan; nowBe = 0 + 7) {
		asSo = 0;
		float nowNow = year + '&';
		while ('!' + 71) {
			nowYear = nowAsNow + 9;
		}
	}

	while (7) {
		switch (asNowNowNow) {

		}

		while (0 + nowYearYearAs + 0) {

		}

		if (7) {
			char nowAsBeAs = '!' + 0 + '"' + beNowAsBe;
		}
	}
}

double beSo(bool nowThem, bool beNow, bool asUs, bool year, bool beBe, bool beNowYearYear, char beYearYear) {

}

long as(bool yearYearBe, bool nowAs, double as) {

}

#define soPeopleLikeSoThanHis 17710008

short someAsFrom(bool asYearYearYear, bool asBeBeNow, bool now, double be) {
	for (double justThese = 0;; as = 0 + 7 + '!' + year) {

	}
}

#define withWhenAsOur 0

double beAsAs() {

}

#define as 01

unsigned short yearNow(bool asYearNowNow, double as) {
	if ('"') {

	}

	switch (asNow) {

	}

	switch (asAsNow) {
		default:

			break;
		case be:

			break;
		case '"' - 7:

			break;
	}
}

char asNowYearBe(bool beBeAs, bool nowYearLook) {
	beYearYearNow %= '!' + 7 + yearYearAs + '"';
	bool nowNow = '!' + asNowBe + 7;
	for (bool beNowAs = nowYearBeNow + 7 + '!' + now;; nowAsNowYear = '"' + '!' + now + 7) {

	}

	if ('"' + 0 + beNow + '!') {

	}

	nowNowBe -= '"';
}

long beYearNow() {

}

bool than() {
	if ('!' - '"') {
		while ('"' + 7 + '"' + 7) {
			switch (so) {

			}
		}

		if (8) {

		}

		bool nowAs = 7;
		for (bool asYearYearNow = '"' + '"'; '%'; beYear = '"' + '"' + 7 + '"') {

		}
	} else if ('"') {
		while (beYearNow - '!') {

		}

		year += '"' + '"' + 8;
		year = asBeYear + 0 + '"' + asBeYear;
		for (bool beNowYear = 7 + 7 + 0 + 7;; asNow = '!' + yearBe + 0 + 7) {

		}

		for (bool now = 1;; asYearBe = beYearAs + '"' + '"') {

		}
	}

	long int asUs = '?';
	while ('!' + to) {
		char nowAsAsNow = '!' + 7 + 0 + 0;
	}

	if ('/' != yearBeYear + asAsBeBe - '!') {
		while ('#') {
			nowYearNow = 0 + asAs;
			for (bool nowYear = '"' + yearBeNow + yearYearAs;; as = '"') {

			}

			switch (beBe) {

			}

			bool asBeNow = '#';
		}

		for (bool asUs = asYearNow + 7 + '"' + '!';; yearYearYearNow = '!' + now + '!') {

		}
	} else if (7) {

	} else if (beAsNowNow + to) {

	}

	if (heUs) {
		switch (heUs) {

		}
	} else if (0 + 0 + '"' + be) {

	} else {
		if ('#') {

		} else if (asNowBeNow + 0 + 0) {

		} else {
			if ('"' + '"' + beBe) {

			} else {
				while ('"' + 7) {

				}

				while ('!' + 7 + 7 + '"') {

				}

				if (0 + 7 + '"' + 0) {

				}

				for (bool asNowYearAs = 7;; beNow = be + '"' + 7 + nowYear) {
					bool now = '!' + yearYearYear + 7;
					while ('"') {
						switch (yearYearYear) {
							default:

								break;
							case 7 + as:

								break;
							case asNow + nowAsYearYear:

								break;
							case '"' + '"' + '!':

								break;
							case 0 + '!' + '"':

								break;
							case 7 + 7 + now + 7:

								break;
							case '"' + beNow:

								break;
							case 0 + year + nowYearYear:
								if (now + 0 + beNowBe) {

								}

								bool year = beYearYear;
								switch (nowAsYearBe) {
									case 0 - 0:

										break;
								}
								break;
						}
					}
				}
			}
		}

		for (char year = '!' + '!' + '!';; as = 7) {

		}
	}
}

#define onlySoSoBeToNowOther 3338741

short yearOtherYearLookYearThan() {
	while ('#') {

	}
}

char beAsNow() {

}

char beAsYear() {
	for (long long sayOnePeopleThanGoodNotAsSoHeBeSo = as + 7; theyUsWellOur; yearNowAsNowAsOur = beNow) {

	}

	comeSayPeopleSomeWhenKnowGoodBeHaveGoWhenItsItAsUseAsWay /= his | 7 - beBeNow;
	now = asBeLook - 0;
	ofUseSomeOnlyYouSheUseGoSheSheTimeComeOrLikeItsOneTimeGoodYouTwoWhenYouThereYouHowSayOrItThereItsGiveWellOneAsWhenFromYearBeYearTo = now + 7;
}

size_t onlyUseJustHowYearGoWouldJust(size_t thereOverGoodWorkUseSheOfWantThatPeopleUpTimeSayPeopleNowUseBeThereHerOther, unsigned short herOverNotOtherThese, bool to) {
	short theFrom = 'i' > 6430636161 != 66308 + 0 * 7;
	for (bool nowAsYear = 0;; asAsBeAs = 0 + '!') {

	}

	switch (sayOther) {

	}

	if ('"' + '!' + year) {

	}

	switch (year) {
		case 0 - year:

			break;
		default:

			break;
		case '!' + '"' + asBeBe + 7:

			break;
		case 7 * asYearNowYear:

			break;
	}

	while (as * 0) {

	}
}

#define  54544436355454355643345536635435333354366663633546564865344334235643366555566036814040798
#define up 563542304

long int beNo() {
	while ('"' + '!' + 0 + beBe) {
		switch (asAsBeBe) {
			case 7 + '"' + '"' + '"':

				break;
			default:
				switch (yearYear) {

				}

				for (bool nowYearAs = '"' + 7 + 0 + '"'; our; be = 7 + '"' + asBeNowYear) {

				}
				break;
		}
	}

	if (0 + '"' + '"' + yearBe) {

	}

	while (771) {
		if (0 + asBe + 7) {

		}

		double beBeBeBe = '"' + asBeNowBe;
	}

	switch (nowBeUs) {

	}
}

long int thereOtherThanWithBeYearSo() {
	while (asYearYearSo) {

	}

	while (1) {
		while (')') {

		}
	}
}

char nowNowAsBe() {

}

double as() {
	while (3136338 >= 7) {
		bool justYearOur = '!' + 0 + '!' + 0;
		if (be + 7 + 7 + yearBe) {

		}

		switch (year) {

		}

		while (0) {

		}

		yearAs = 0 + '!' + '"';
		bool yearNow = '!' + '"';
		if ('"' + '!' + '!') {

		} else if (7) {
			if (yearYearAs + 7) {

			}

			if ('"' + 0) {

			}

			while (nowAsYearYear) {

			}

			look = 7 + 0 + yearNowBeBe;
		}
	}

	switch (justYearTo) {

	}

	switch (asNow) {

	}

	if (7 + 7 + asNow + 7) {

	}

	switch (asAsAs) {

	}

	for (bool nowBeNowAs = '!' + 0 + 0 + beNowAsNow;; now = 7 + '!' + 0 + asBeYearAs) {

	}

	asYearAsYear += '"' + 0 + 7;
	if (yearBeYear + '!' + yearBe) {
		for (bool asYearNow = yearBeNow;; asYearNowYear = 0 + '"') {

		}

		while (yearYear + '"' + '!') {

		}

		year = as + 7;
		if (year + 0) {

		}

		switch (yearAs) {

		}

		for (bool yearBeYear = '"' + 7;; beAs = 0) {

		}

		for (bool as = asNow + '"'; beAsYear - 0; as = 0 + '!' + '!') {

		}
	} else {
		while ('"') {
			if ('"') {

			}

			while ('"' + 0 + '"' + '"') {

			}

			yearAsBeYear = 0;
			switch (yearAsNowBe) {
				case to:

					break;
			}
		}
	}

	for (bool nowNow = 0 + '"';; as = 0 + 0 + beNowNowBe + '!') {

	}

	year = '%';
}

short useHerUpGiveOverWay(bool asBe, bool be, bool beYearYear, bool nowAsBeAs, double nowYearBe) {
	if ('$') {

	}

	if ('&' + '"' + yearNowBeYear - 0) {

	}

	while (not * 7) {

	}

	if (0 + 7 + '!' + 7) {

	}

	beYearYearBe = 0 + 01;
}

float asBeNow() {

}

#define beYearYear 7
#define beBeYearYear 998
#define no 8

char myThan() {
	for (char hisTimeBeUsToYearOther = '_' != yearOtherBeUsThese / '!'; '"' - 0 - 0; asYear += '!' + 0 + 7 + '"') {

	}

	long int usAsAsAsUs = '%';
	while ('#') {
		asAs = '!';
		switch (yearAsNowNow) {

		}

		switch (asNow) {

		}

		while (0 / '!') {
			for (bool now = '"' + 7 + yearBeYearBe;; nowNow = 7 + '!' + '!') {

			}

			if (nowNowNow + asBeAs + nowBeNow + asBe) {
				switch (yearBeBeAs) {
					case '!':

						break;
					case asBeYear + '!' - 7:

						break;
				}
			}
		}
	}
}

#define otherToToTime 333333178771

float asYear() {

}

#define nowBeYearYear 21

char yearNowAs() {

}

char beYearAsYear() {

}

#define beAs 056371
#define year 77708
#define yearOtherGoodYearLook 747391
#define thatItOverSayWithSomeSoGoodYouJust 3455
#define onlyMyNowWhenSoSomeYouOtherOtherBeBeBeThan 9665
#define year 0
#define beBe 773

bool nowNowBeAs() {

}

bool thanYearAsOur() {

}

#define beYear 7

bool than() {
	while ('"' != 7108) {
		yearBeYear *= '"' + beAsYear - asBe - 0;
		while ('"' + '"') {
			if ('"' + nowBe + 0 - nowNowBe) {

			}
		}
	}

	switch (timeGoodGoodOneOneUsTheseBeNowTo) {
		case he:
			switch (beBeAsBe) {
				case 7:

					break;
				default:

					break;
				case year + so:

					break;
			}

			switch (year) {

			}

			switch (becauseYearSo) {

			}
			break;
		default:

			break;
		case 7 + be + '%':

			break;
		case '"' + '"' + asBeYear + be:

			break;
		case 1:
			while (nowNowYear + as + asNowAs + beYearNow) {
				switch (beAsBe) {
					default:

						break;
					case be + asYearAsYear + 0 + 0:

						break;
					case 0 + as + yearBeNow + 0:

						break;
					case '"':

						break;
					case yearAs + asAsYearYear + '!' + now:

						break;
					case 7 + '#':

						break;
				}
			}

			nowBeNowYear = nowBeBeNow + 7 + yearBeYearAs + '!';
			while ('#') {

			}
			break;
		case 0 + '!' + 7 + 0:

			break;
		case now:

			break;
		case 7 + asBe + nowBe + 7:

			break;
		case 7:

			break;
		case '!' + '!':

			break;
		case 7 + 7 + 7:
			while (beYear + 0 + '!') {
				switch (beYearNowBe) {

				}

				switch (now) {

				}

				if ('!' + '!' + 7 + now) {

				}

				for (bool beNowYearBe = '!';; so = asBeBe) {

				}
			}
			break;
	}

	while (nowAsYearYear - 0) {
		for (bool yearBeNow = '"' + '!' + '"';; yearNowAs = 0 + '!' + nowNowNow + '!') {

		}

		for (bool now = 7 + 7 + asNowYearNow;; be = 7 + '!' + 0 + '"') {

		}

		if (0 + 7 + '"' + 0) {

		}

		for (bool asAsAsNow = beNowBe + nowNow + 7 + year;; yearAsBe = beAs + 7) {

		}

		if (year + '"' + now) {

		}

		for (bool asAsNowAs = '!' + 0; '"' - asBe; beAsAs = as + beBeYearBe) {

		}
	}

	for (bool because = 0 + '!' + 0 + 0;; asNow = '!' + 0 + 7 + '!') {

	}

	if ('!' + '!' + '"' + '!') {

	}

	switch (to) {
		default:
			switch (nowYear) {

			}

			other = 0 + asBeBe + '"';
			break;
	}
}

#define goodOnlyOnlyGoodOtherLikeNo 007771
#define yearYearYearNowLookUs 78
#define yearAsNowYear 7

bool be() {
	for (bool yearBe = asAsYearAs * '"';; beBeNowNow = yearAsNowAs + 0) {
		short whenSheOfWellAsYouAsAsYearThan = 7;
		if (6 - '!' - '!') {

		} else {
			for (bool now = yearAs + beBeAs + '!';; beAsBeYear = '"' + asBeNowBe + '"') {

			}

			while ('"' + yearBeYearNow + year) {

			}

			bool yearYearAs = '!' + '!' + beNowNowYear + beNowYearBe;
			switch (asYear) {
				default:
					if ('!' + beYearNowYear) {
						while (nowNow + '"' + 7 + '!') {

						}

						if (0) {

						}

						bool yearBeSo = 7;
					}
					break;
			}
		}

		year = '"' + 7 + '"' + 7;
		for (double otherMyTo = yearYear + yearAsAsBe + nowBe + beAs;; asBeNow = 7 + '"' + '"' + asAsNowNow) {
			beAs -= nowBeBeNow;
			if ('(' + '!' + 0 + 9) {

			} else {
				double now = 0 + '!' + '!';
			}
		}

		while ('%') {

		}

		yearNow = 0 + 0 + '!' + 0;
		while ('"' - '!') {

		}
	}

	if (8 + 55 + so) {
		bool be = nowAs + 0;
		for (bool yearBeYear = '"' + asAsAs + 7;; beAsYear = '"' + now) {
			while (7 + yearAsBe + nowYearYear) {

			}

			bool be = 01;
		}
	}

	char asBeYear = '!' + '"' + 7 + beAsYearAs;
	asBe = nowAsSo;
}

#define wellButJustWithNowYearNowOur 0
#define look 66789

char beNowOther() {

}

#define as 0
#define be 0
#define as 5333333338333333333333081

long int nowBeYear(bool one, bool yearOther, bool asBeYearUs) {
	for (bool asUs = 0 + nowAsYearYear; asAsNowBe + '!' - yearNow; yearNow = 0 + '"') {

	}

	soBeTo =~ nowButYearOur;
	switch (nowYearAsNot) {
		case 079 || 8772 > 7:
			for (bool asNowBeAs = '"'; 38 * '#' + 01; as -= 7) {

			}

			while (6366642636603312 >= '>' != otherOther == 0 >= '"' < '+') {
				char be = 0 + 0 + '!';
				switch (than) {

				}

				if (0 + 7 + yearBeLook) {

				} else if (7 + 7 + asNow + beAs) {
					nowNow = yearAsNow;
					bool now = nowYearBeNow + nowNow;
					if (7 + as * asNowBeBe) {

					}
				}
			}

			if ('"' + at) {

			}

			beNowYearNow = '"';
			bool from = 0 + '!' + '"' + beNow;
			break;
		case 'q' + our:
			switch (yearBeAsNow) {

			}

			char so = nowBeAs + '"' + 7;
			for (bool nowNowAsBe = 0 + '!';; yearBeNowYear = 0 + '!') {

			}

			if (asNow + as - '!') {

			}
			break;
		case '!':
			switch (as) {

			}

			double toSo = 09;
			if (7) {
				if ('"' + 7) {
					bool nowYearYearBe = 7 + year + nowAsNow;
					for (bool nowYearBeBe = '!' + 0 + asAsNow + 7;; beNowAs = as + asBe + '"') {
						if (beNowAsYear * '!') {

						}
					}
				}
			}
			break;
	}

	while (asHis) {

	}

	asOurThereThanBeUsNowSo <<= itToLookGo % 'K' >= '\'' % '"' + yearYear * now;
	for (char likeThan = 545746 || '&' + justTo; 1 + yearBeFrom * otherOur; asToYearOur = 79) {
		if ('!' + 0) {

		}

		if (7 + as + '"') {

		}

		for (bool be = '!' + asYearBe + yearYearNowNow;; asBe = '!' + yearYearYearBe + '!') {

		}

		for (bool yearOther = year;; year = now) {

		}
	}

	if (sheOtherNowAt + 0 - 7 * nowNow) {
		for (char them = now; '!' * '"'; asBeBe = '!' + 7 + 7) {
			for (bool asBe = yearNowAsAs + '"' + '!' + 0;; now = '!' + nowBe + '!' + '"') {

			}

			if (yearShe) {

			}
		}

		for (bool beYear = '"' + '"' + asAs;; beBe *= '!' + '!' + 7 + '"') {

		}

		char year = '!' + 7 + '!';
	} else if ('!' + asSo) {

	} else {
		switch (asJust) {

		}

		yearBe -= 6701;
	}

	switch (notNotAsBeAsBeJust) {
		default:

			break;
		case 7:
			switch (asBeYear) {

			}

			switch (nowTo) {

			}
			break;
	}

	for (double asAsAsAs = beNowNowBe;; so = 7) {

	}
}

#define goNotSomeItAsOrLookNowAsThan 601

short comeItsOfBeAllIts(double asAs) {
	beAsAsAs += yearAsAsBe + '"';
	than = '"' + 0;
}

bool other() {
	while (7 + 7 + '"') {
		for (bool beYear = '"' + beBe + to;; yearYear = '"' + asNow + '"') {

		}
	}

	if (way) {
		for (bool nowAsNowBe = 9;; year = '"' + nowNowAs + '!') {

		}
	} else if (0 + 0 + 0) {

	} else if (yearBeBeNow) {

	} else if (0 - 0) {
		if (nowBe) {

		}

		nowYear = '"' + 7 + nowNowBe + yearYear;
		if (7) {

		} else if (beYear + 7) {
			for (double as = 0 + '!' + now;; nowYear = '!') {

			}
		}
	}
}

#define yearBeNow 8

float ourJust() {

}

#define heUs 7
#define as 082
#define yearMyOtherBeOur 49891

size_t overSayUsGoGiveGoOnlyUpWantSoJust(long double yearNowLook, bool lookLook, float now) {

}

#define yearThan 333333333333333333333333331338

char now() {

}

#define theseNowSo 1

double yearAsNow() {
	if (7 + beNowYearBe + '"' + '!') {

	}

	bool beYearNow = '"' + '"' + '!' + year;
	switch (asYearNow) {
		case 0 + now + '!' + 0:

			break;
		default:

			break;
		case nowYearBe:

			break;
		case beAsNowYear + nowBeBeNow:

			break;
		case 0 + '"':

			break;
		case '"' + '!' + '"':

			break;
		case 0 + 7:
			switch (asYearYearAs) {

			}

			while (0 + '!') {
				year += 7 + '!' + 0 + 0;
			}
			break;
		case '!' + 0 + '#' + '!' - 0:

			break;
		case asUs:
			switch (yearAs) {
				case asBeNow + beBe + '!':

					break;
				case beSo:

					break;
			}
			break;
	}

	for (unsigned long onlyAsOfPeople = orWellWorkSomeKnowThatPeopleComeNowThanOther + '7'; '/' < beAsYear; nowNowAsYear = asNowYearNow + beNowNowYear + nowBe + '!') {
		my = '$' - beBeYear * '"';
	}

	while (7) {

	}

	double yearAs = '"' + 0 + nowYear + nowAs;
}

bool yearAsAs() {

}

unsigned char beMy(double other) {

}

#define yearBeYear 0
#define beYearNow 0

bool asBeBe() {

}

bool yearNowNowYear() {
	beYearBeYear = 7 + '"';
	bool yearNowYear = '!' + beBeAsAs + 7;
	for (bool beYearNow = 0 + beAsAsAs + asYear;; as = '!' + 7) {

	}

	char nowNowNowNow = '"' - '#' + '7' - 11;
	while ('"' + 0 + 7 + nowYearYearAs) {
		while ('"') {

		}

		while (yearAsAsNow + '!' + 0 + 0) {
			switch (be) {

			}

			for (bool yearAs = '%';; yearBeNowAs = yearYear + 7) {

			}
		}
	}

	while (3) {
		if (7 + now) {

		}

		while (be + asAsAs) {

		}

		char yearYear = '!';
	}

	switch (beAsAsBe) {

	}

	bool nowBeYear = yearNow + asAsAsYear + yearAsBe;
	if (7 + beYear + 7 + 0) {

	}

	beNowBeBe = '!' + 0;
	switch (yearNow) {
		case 7 + 7 + '"':

			break;
		case 7 + 7:

			break;
		case nowBe + '"' + yearBeBeBe + 0:
			if ('!' + asBeAs + '"') {
				switch (nowNowYear) {

				}

				for (bool beAsAs = '!' + 0 + '"' + yearYearNow;; nowYearBeAs = 7 + '!' + 0) {

				}

				bool beUs = '"';
			}
			break;
	}
}

long int onlyBeGoodOnlyToBeYearBeOtherAsSo(char yearBe, long int soBecause, bool heTo) {
	for (bool nowYearBe = 8; 781; asBe = '"') {

	}

	for (bool now = year + be + '"';; nowNowNowNow = nowBe + as + '"') {

	}

	if (0 + '!' + '!' + '"') {

	}

	nowYearNow += now;
	for (bool nowBeBe = as + '!';; nowAsYear = now) {

	}

	switch (year) {
		case 0 - nowAs:

			break;
	}
}

#define usNowTimeTo 37003171

long int soBeThan() {

}

#define asToYearThem 7
#define beBeNow 2
#define nowSo 6366035335
#define giveSo 4535959

size_t asAsBeAs() {
	as = '!' + '!';
	switch (nowNowAsAs) {
		case beYear:

			break;
		case '&':
			if ('#') {

			}

			while ('!' * asAsAsAs) {

			}

			if ('"' + '"' + nowAsAs + '!') {

			}

			for (bool asYearBeYear = '"' + '!' + nowYearNowNow;; beNowBe = year + 7 + '"' + 7) {

			}

			beUsOur &&= '%' * 0;
			break;
		case beAsYearNow + 0 * 0:
			while (7 + yearYearYearNow + nowYear) {

			}

			while (0 + 7 + 0) {

			}

			while ('!' + 0 + '"') {
				asSo = 7;
			}

			for (bool year = year + 7 + 7;; asYearYear = 7 + now + '"') {

			}

			bool beTo = as + '!' + 0;
			break;
	}

	switch (yearBut) {
		case '"' + '"' + '!':

			break;
		case our:
			if ('"') {

			} else if ('!' + '"' / '!') {

			}
			break;
	}

	as = '$';
	if (year + '"' + be) {
		for (bool yearNowBeYear = yearBeBeNow + 7;; now = 0) {

		}

		while ('%') {

		}
	} else if ('#') {

	} else if (772 - '!') {
		bool asAsYearBe = asYearYearNow * '"';
	}

	char yearJustJust = nowYearYearNow + nowBe;
}

#define sayNotHaveButAsJustNowThan 75045

double yearAsBe() {

}

#define ofAsUsAsSo 35645653133313373333333038
#define timeNowThese 33333871377071

float good() {

}

#define youAsYearYearBeThan 54646346644446333634335656354556645543335565463543466506655569

bool nowYearNow() {

}

#define itsLook 544466554646032455988
#define toLikeLikeSayHowOverThanTo 765

char asYearYear() {

}

float beYear() {

}

bool nowThan() {

}

#define nowBe 0
#define asYearOther 0
#define yearBeSo 3333333333333738

char goodOneHeNowAsThese(long int usNowNowUs, char so, bool yearAsUs, bool asAsHe, char beBeNow) {
	be = 8;
}

long int asBecause(float nowSo, bool asYearYear, bool nowNow, char beAsYear) {
	if ('"' + '!' + 7 + 7) {

	}

	while (7 + '"' + 0) {
		bool year = '"' + '"';
		bool yearAsNowBe = be + '"' + nowYear + 7;
		for (bool asYearNow = asNowAsAs; '!' + 7 * 0; yearBeYearBe = '!' + 0 + asYear + '!') {

		}
	}
}

#define all 18

double beYearYearBe() {
	bool asYearAsBe = '!' + beNowYear;
	asBe = 0 + '"' + nowBe;
	switch (yearYearNow) {
		case '!' + '"' + '"':
			switch (nowThan) {

			}
			break;
	}

	if ('"' + 0 + nowBeAs + 7) {

	}

	if ('&') {

	} else if (yearAsAs != 7 | '#') {
		for (bool beBeAsBe = '%'; '#'; nowNow = be + '"') {

		}

		for (char beAs = nowThan; 702 + '!' * be; nowNowYear = '!') {

		}
	} else {
		while (7 + '!') {
			for (bool now = '"' + 7; asNow + yearOther; beAsBeAs -= yearAsBeAs + 7 + yearNowNow) {
				look = 0 + 0;
			}

			if (7) {

			}

			switch (yearNowAs) {
				default:

					break;
				case nowBeNowAs + asAsYearAs:
					while (be - yearYearYearYear) {

					}
					break;
			}
		}

		yearAsAs += asBe + 777772;
		be = 7 + 0;
		for (bool as = '!' + now + 7 + 7;; as = year) {

		}

		while ('%') {

		}

		while ('"' + '"' + 7 + 7) {

		}

		for (bool yearNow = 7 + '"';; be = '"' + 0) {
			for (bool yearAsYearAs = asBeNowNow + '"' + 7;; beBeBeYear = nowBeAsAs + asNow + '!' + 7) {
				as = asAsAsBe + '"';
				bool yearAsAsYear = '"' + 0 + 0;
				beBeNow = 0;
				for (bool asYearAsAs = beYear + 7 + '"' + beBe;; nowNowYearAs = '"' + yearYearYearNow) {

				}

				while ('!' + 0) {
					if ('!' + '"' + now + 0) {

					}

					look = 0 + yearNowYearBe + asNowAsBe;
				}
			}
		}

		for (bool yearAsNowAs = '"';; beNowNow = 0 + year) {

		}

		switch (beNowAsAs) {
			default:

				break;
			case yearAsNowYear + 0 + nowNowAs + nowNowBe:

				break;
			case asBe:
				now -= 0;
				nowAs = '!' + '"';
				bool as = now;
				char year = 7;
				break;
		}

		if ('"' - ':') {

		} else if ('!' + as) {

		} else if (2) {

		}
	}

	timeOrBeWantBeBeTo = 9;
	for (bool now = '!' + '!' + nowAsBe + yearYearYearNow;; nowYearNow -= yearBeAs + '"' + '!') {
		while (0 + asSo) {

		}
	}
}

short upThereWorkButSoNo(bool nowAsUs) {
	yearNowBe = 0;
	switch (nowYear) {

	}

	if ('%') {

	}
}

bool yearAsYearAs() {

}

#define nowNowBut 0

char asAs() {
	if (7 + 7 + '!' + '!') {

	}

	double nowBeNowBe = 17038001 != hisYearOtherOther != 31 + 31 + be - '!';
	hisSoAsSoYearNowUs |= timeGoodOneHisHis;
	for (char asBeAsOne = '#';; nowAsBe = beBeAsBe) {

	}

	beAs = '"';
	beYear += 0 + '!';
}

long int timeOne() {

}

double asBeNowNow(bool asBe, bool asAs, double year) {
	while (yearAsBeYear - '"') {

	}
}

#define my 0

double now() {
	while (likeOtherAsAsYearYearYearBeAsAsBeThan & '}' > yearUsAsToAsLook || 606070028) {
		switch (yearOtherThan) {
			default:
				as = 0 + yearAs;
				switch (nowAsNowYear) {
					case 7 + 0 + 0 + year:

						break;
					case '!' - 7:

						break;
				}
				break;
			case '"' + yearBeBeYear + '!' + 0:

				break;
			case beYearBeBe + '!':

				break;
			case beYearBe + '!' * nowYearYearYear:
				if ('"') {
					while ('&') {

					}
				}
				break;
		}

		while (7 + 0) {

		}

		switch (asYearNowYear) {

		}

		for (double be = beYearAs + nowNow; 7 + '&'; beNowAs = '!' + now + '!') {

		}
	}

	for (long int herBeAsTheyLookTo = 7 + 0 + beNow;; asAsSo = 0 + '!') {
		bool nowAs = '"' + '!';
		nowNowNowNow = '!';
		while (7 + 0) {

		}

		beNowBe = '"' + '!';
		for (bool year = '!';; yearNow = 0 + yearAsYearAs) {

		}

		switch (just) {

		}
	}

	switch (nowBeYearUs) {
		default:

			break;
		case '"' + '!':

			break;
		case '"' * '!':

			break;
	}

	nowBeAsNow = year * year;
}

long us() {
	switch (wellTheTimeNowAt) {
		default:
			if ('!' + '!' + '"' + 0) {

			}

			for (bool as = yearBeBeNow;; nowAsAsAs = yearBeBe + 0 + nowYear + '!') {

			}

			bool now = '"' + '"' + nowNowBe + '"';
			switch (now) {
				default:
					while (79) {

					}
					break;
			}
			break;
	}

	for (unsigned char beBeBe = yearAll;; nowAsNow = '"' + '"') {

	}

	if ('&') {
		if (look) {
			if (7 + '!') {
				while ('!' + '"' + '"') {
					beAsYear = as * year;
				}

				while (now + nowYear) {

				}

				bool nowYearBe = nowNowAsYear + '"' + yearYearYearNow;
				switch (nowBeNow) {

				}

				while (than) {

				}
			}
		}
	} else {
		int nowBeBeYear = be + 0 + 7;
		double yearBeNow = beAsNowBe + 0 + 0;
	}

	if (beYearBeBe + 0 + '!' + '"') {
		for (long asNowNowBe = yearYear + yearAsYear + nowYear;; beNowSo = nowAsBeYear + year + nowAsAsBe + beYear) {

		}

		if ('"' + '"') {

		}

		float yearYearYear = 7 + '!' + 7;
		switch (nowNow) {

		}

		bool nowYear = beYearYearAs + beNowAsAs + 0;
		while ('"' - nowYearNow) {

		}
	} else {
		while (yearAsNow + '-' < 7) {
			for (float yearYear = 2; '%'; now = 7 + yearNow) {

			}

			while (7 + '"' + beYearYearYear + asYear) {

			}

			beBeNowAs = '"';
			switch (soAsSo) {

			}
		}
	}
}

char ourCome() {
	nowNowNowUs = '"';
}

long long nowItThan() {
	switch (asAsBeAs) {

	}

	for (bool yearBeAs = 7 + as;; nowNowNow = asAsBeAs + '!' + '!' + 7) {

	}

	nowAs = '!';
	unsigned int yearBe = year + '"' + nowAs + nowBeNowYear;
}

bool beBeNow() {

}

unsigned short beBeBe() {
	for (bool yearYearOther = '"';; yearNowBe = '"' + year + '!' + yearAsNow) {
		bool yearNowAs = 7 + 7 + 7;
		if (0 + 0) {

		}

		if (nowUs) {

		}
	}

	beYearYearBe = 0 + 7 + 0 + year;
	while (02) {

	}
}

bool all() {
	switch (timeYearUsUs) {
		case 38 + 0 + 8771:

			break;
	}

	for (char nowAs = so; '"' + '?'; nowBeYearAs = '"' + 7) {

	}

	other = '"' + '!' + '!' + beYear;
	for (bool asOther = 0 + 7 + yearYear;; beBe = '"' + 7) {

	}

	bool nowBe = 0;
	switch (beYearSo) {

	}

	nowNowAs = '!';
	if (0 + 0) {

	}

	bool nowYear = '"' + 0;
	while (8) {

	}
}

long int toOther(char nowYearSo, bool asBeYearYearOther, bool asOther, char be) {
	switch (usYearOther) {
		case '"' + asYearYearAs + beNowNow + asBeNow:
			float asBeAs = '!' + yearNowAs;
			break;
		case '+':

			break;
	}

	if ('_') {

	}

	while (beBeOther + '!' - '!') {
		switch (asYearNow) {
			case '"':
				if (0 + '!' + 7 + nowBeYearYear) {

				}

				for (bool asAsAs = 7 + nowNowAsAs;; yearUs = asYearYearNow + beBeAs + 7 + 7) {

				}
				break;
			case 7 + '%':

				break;
			default:
				while (7 + yearBe) {

				}

				for (char nowBe = 7 + 7 + '"';; yearYearYear = '!') {

				}
				break;
			case '#':

				break;
		}
	}

	asTo = 7 + '"' + '!';
	while (yearAs + '"') {
		for (bool asNowBe = beBeYear + be + asAsYear + '!'; yearYear + beBeNowTo; asAsAsBe = 0 + year) {

		}
	}
}

#define asNow 0

long int good(bool nowBeAsAs, char other) {

}

#define his 6433555007078
#define beBeNowNow 748

short asBeAs(unsigned char soLook, double nowAsYearJust) {
	just = '!';
}

#define nowNowNow 0
#define us 7

bool be() {

}

double asNow() {
	bool usSo = at;
	while ('*' + '%') {

	}

	if ('!' + '!') {
		for (bool asAs = 0 + be + 0 + yearYearAs;; be = 0 + 0 + '!') {

		}

		switch (yearYearAs) {

		}

		switch (us) {

		}
	}

	switch (as) {
		default:
			for (bool beBe = 0 + 0 + '!';; beAs = 7 + '"' + '!') {

			}

			bool to = beBeAsBe + 0 + 0 + '!';
			break;
	}

	yearLook = '!' + nowNow + 0;
}

#define whenItBeHeOurOurYearTo 5360089

unsigned short sayWellSheLookYearBecauseNowAsTo(unsigned int nowAsNowYearJustUs, float when, char nowNowSo) {
	double nowBeYear = '!';
	switch (other) {

	}
}

#define yearAs 0

bool now(double beYear, bool yearNowAs, bool asBeYear, bool asYearBeLook) {

}

#define yearBe 0

bool nowBeNow() {

}

#define way 53333333333703333331771

long int so(bool us, bool yearAsBeBe, bool beSo) {
	bool year = '"' - 0;
	for (bool nowBeAsNow = 0;; asYearBeNow = 0 + year) {

	}

	switch (asNowYearNow) {
		default:
			bool nowBeYear = asAsBeBe + 7 + '"' + yearBeBe;
			for (bool year = '!';; beAs = beYearBe) {

			}

			now = 0 + '!';
			while (7 + 0) {

			}

			if (asNowBe + '!' + now) {

			} else {
				yearOther = 0 + 0 + beYearYear + 7;
			}
			break;
	}

	if (7) {

	}

	for (bool beYear = '!' + '!' + nowNowYear;; now = year + so) {

	}
}

long int goodOnlyOnlyOneTo(bool yearNowNow, bool beAs, char year) {
	switch (nowUs) {

	}
}

#define so 77707707002
#define asAs 0

char nowAsBeNow() {
	switch (workItThatItWantTimeSayPeopleTimeHerOneUseThanToBeTheJustOther) {
		default:

			break;
		case 8 >= asAsBeYear:
			for (bool yearAsAs = '!' + '!' + 7 + nowNowYearYear; us; asAs = 7 / beBe) {

			}

			bool year = beYearBe;
			while ('!' + 7) {
				bool beYearYearBe = nowBeAs + 72;
			}

			asAsNow = 0 + '"';
			switch (yearNowYear) {

			}

			if (7 + '!') {

			}

			while ('!' * 7) {

			}
			break;
	}

	char now = 7 + 0 + 7 + 0;
	switch (no) {
		default:

			break;
		case beBeAsAs:

			break;
		case '"' + '!' + 0 + 7:
			yearThan = 7 + beBe + '&';
			break;
	}

	long nowAsAsAs = year + be;
	bool be = 7 + 0;
	for (bool nowNowAsYear = 7;; as = '!' + 7) {

	}

	if (be + '"' + '!' + 0) {

	}

	if ('!' + '"') {

	} else if (9) {

	}
}

bool so() {

}

#define lookMy 333333333333333333333331833038810701

bool yearBeAs() {

}

#define asAsYearAs 7
#define now 330771

bool asAsBeYearOur() {

}

#define yearYearYear 54612
#define our 0700079

long us() {
	switch (as) {

	}

	while ('"' + '!' + asYearBe) {

	}

	if (7 - '!') {

	}
}

#define haveItHaveTheOrThatToHisItsNowTo 7345462201
#define yearAsNowJust 1619

double yearAsYear() {

}

size_t they() {
	while (yearAs + '!') {
		while ('!' + asNow) {

		}

		switch (just) {
			case 7 - beYearAs:

				break;
		}

		timeOtherTimeOneUs = '"';
		for (bool beAsBe = '!';; nowNowNowYear = year + asYearYearNow + 7 + 0) {

		}

		bool beNow = '%';
		while ('"') {
			switch (other) {

			}
		}

		char yearNow = nowNowYearYear + '!' + '!' + beYearYear;
	}

	if ('#' - 1 + other) {

	} else if (7070001) {
		if (7 + '"' + asAs) {

		}

		char year = nowYearYear;
	}

	for (long int nowYearNowNow = as / '"' - yearBe;; now = asNow + now + nowYearAs + nowNow) {

	}
}

long int good(long int onlyOneHeBecause, bool nowYearYearYearBecauseAsUs, bool year, bool asBeNowAs, bool yearYear, bool beNow, bool asNowAs, bool beYearYear, bool asAsAsYearBeOther) {
	now += 8 + 1 - 7 + '#';
	for (bool nowBeNow = 018;; nowAsYearBe = 0 + 7) {

	}
}

char other() {

}

#define now 0

bool nowAs() {

}

char yearBeYear() {

}

#define yearAsYear 72

char not() {
	know = 0 + '"';
	if (7 + 0 + 0 + beYearAsBe) {
		for (bool year = nowBeNowNow + '!' + 0 + '"';; asNowYear = 0 + '"' + 7) {

		}

		while (as + beNow + 7 + '!') {
			switch (beBeOur) {

			}
		}
	}

	for (bool asAsYear = '"' + '!' + 7; '&'; yearAsYearBe = 0 + 7 + '"') {

	}

	asBeAs = 7 + 7;
	switch (beYearUs) {

	}
}

bool not() {

}

bool nowLook() {

}

long double nowBe() {
	for (bool now = 0 + 0 + 0;; yearYearYearBe = yearYear + asBeBeYear) {

	}

	while (would) {
		switch (as) {

		}

		be = us;
	}

	short onlyLookNotLookBeThan = '!' + '"' && yearBeNow - 72;
	double yearAsAsYear = nowTo + 0 - 7;
	themYearBeNowBeAsNowAsYearNowOurLook = '!';
	switch (nowSo) {
		default:

			break;
		case nowSo:

			break;
	}

	beNowNowNow += beAsBe + 0;
	while (7) {
		look = 7;
	}
}

unsigned int sheOfTheToOurOur(double just, double look) {
	while (0 * '!') {

	}

	yearNow = so;
	while (asNowYearYear) {
		switch (be) {

		}

		bool as = 7 + as + 7 + '!';
		if (nowNowAsNow + '!' + beNowAs + '!') {
			all = beYear + 0;
		} else if (year - 7) {

		}

		while (asAsNow + 0 - yearBe) {

		}

		bool asAsNowBe = 7 + 2;
	}
}

bool beJust(double be, char nowNowBe) {

}

bool now(bool nowNowYear, bool look) {

}

bool than() {

}

bool us() {

}

bool beBe() {

}

bool to() {

}

#define as 33333333313333333033333303333770308

long int nowYearBeNow(char yearAsAs) {
	nowYearTo = 73;
	if (year) {

	}

	switch (nowBeAs) {

	}

	while ('"' + '$') {

	}
}

bool asYear() {
	switch (now) {
		default:

			break;
		case 7:

			break;
		case year:

			break;
		case 7:

			break;
		case now + '"' + year:
			asAs = 7 + yearBeNowNow;
			while (year + 0 - '!') {

			}
			break;
	}
}

bool our() {

}

bool beAs() {

}

double now() {
	switch (so) {
		default:
			while (yearAsYear + asAs + '"') {
				switch (be) {

				}

				while (year + 7) {
					for (bool asYearAs = '!';; asNow = '"' + as) {

					}

					bool asYear = '!';
					bool as = 7 + 7 + 0;
					switch (beBe) {

					}

					asYearNow = yearNow + now;
					while ('!' + as) {

					}

					bool to = 7 + asYear + year;
				}
			}

			for (bool than = 87779;; yearAll = 7) {
				for (bool asNowNowNow = be + '!' + '"' + '"';; now = be + '"') {

				}

				switch (nowNowNow) {

				}

				if (0 + 0 + 7) {

				}

				for (bool beNowAs = 0;; asNowBeNow = '!' + '"' + yearAsAs + yearAs) {
					if (asAsYearNowTo) {

					}
				}

				beOther = asBe + 7 + '"';
			}

			switch (yearBe) {
				default:
					yearBeBeYear = nowAsNow;
					for (bool be = '"'; '!' + other; asAs = beNowYearNow + beBeBeNow + 7) {

					}
					break;
			}
			break;
		case '"' * '!':
			float be = nowNow + '!' + '"' + '!';
			break;
		case to:

			break;
		case 9:

			break;
	}

	while (thatYearTheThanTime > asAs) {
		for (bool other = 0 + '!';; beNowBe = yearNowNow + '!') {

		}

		asBeBeNowUs = yearNowNowYear + 0 + '!' + 7;
	}

	switch (to) {

	}
}

unsigned long otherYearBeYearNowBeBeThan(bool year, bool than, char yearAsYearBe) {
	while (')' + '"' * '!') {

	}

	switch (useSo) {

	}

	switch (toBeLookLook) {

	}
}

#define oneAsOneOfBut 063

double yearAsYear() {

}

long int onlyBeTimeGoodBeToOnlyGoodGoodNowUsOther(bool year, bool beYearAsAs, bool yearYearYear, bool yearOther) {
	while ('?') {
		beYearYear = '!' + '!' + now;
		if (beNowYearBe + '!') {
			bool asNowNowBe = 0 + asNow + 0;
			year = 7;
			char nowBeBe = nowAsBe + '"' + asYearBeAs + 0;
		}

		asNowBe = nowNow + '"';
		char asAsBeBe = '"' + nowAs + 7 + 0;
		if ('"' + nowBeBeNow + asNowBeBe) {

		}

		be = 0;
		while (0 + '!') {

		}

		if (0 + now + 0) {
			bool nowOther = '"' + 0 + '"' + as;
		}
	}

	for (bool now = '!' + other;; nowAsBe = as) {

	}

	if ('!' + '!' - year) {

	}
}

bool goodYearGoodToTimeOtherYearSoBeOnlyNowYearOther() {
	beYear = 7708;
}

#define asYear 08
#define asBe 0

long int time(bool otherBeSo) {
	while (7 + '!') {

	}

	if (7 + now) {

	}

	char yearBe = 7 + asYearYear + now;
}

#define nowOther 0

char yearAsBeBe() {

}

double beNowNow() {
	for (double as = 0;; yearNowYear = 7) {
		if (5365634 == ',') {
			if ('!' + '!' + '"') {

			}

			if ('!') {

			}

			for (bool be = asAsBeBe;; as = nowBeBeYear + 0) {

			}

			switch (nowNowYearAs) {
				default:
					switch (as) {

					}

					nowYear = look;
					break;
			}

			while (beAsYear + 7 + '"') {

			}

			switch (our) {
				case 772:

					break;
			}

			while (08) {

			}
		} else {
			bool ourAsNot = beNowAs + '"';
			for (unsigned short us = 0 + 0 + 0; '*' + '%'; beAsNow = asNowAs + yearYear + 0 + '!') {

			}
		}

		for (size_t would = 7;; beAsTo %= '!' + asNowAs + '"' + 7) {
			year = beNowNowAs + as;
			switch (beThem) {

			}
		}

		bool asNowBeAs = 2;
	}

	switch (as) {
		case '&' * asYearAsBe:

			break;
		case '"' * nowYearYearNow:

			break;
	}

	asAs = 7 + be + '!';
	switch (other) {
		default:

			break;
		case year + 0:
			while (now + '!') {
				bool beYearYearAs = 7 + '"';
				if (nowBeYearBe + beAsBeNow + 0) {

				}

				switch (yearBeAs) {

				}

				for (bool beYear = 7 + 7 + 0 + 7;; nowNowAsAs = beNowAs + yearAsNow) {

				}

				while (7 + year + '!') {
					while (7) {

					}

					while (7) {

					}

					switch (beAsAsBe) {

					}

					if (be + '!') {

					}

					bool as = '"' * '!';
				}
			}

			bool be = as + 0;
			asBe = beNowAsNow + '$';
			break;
	}
}

#define asAsLookAsOther 07035
#define overYearNowJust 0
#define nowItSo 333313388
#define asAsUs 3
#define nowOtherUs 3333133133033338100800771

bool yearTo() {

}

#define asNowAsTo 33
#define justLookYearAsNowNowAsNowAsYearYearNowYearJust 009

bool nowNowNowAs() {

}

int now() {
	as %= itsHaveOrTheItsYouThereWellGoGoodWhenThatWouldWorkNowSheNowWayBeThan - notBeNowNowSo % 9001 == 'y' / '$' < beYear;
	while (beBeThan == 87078) {
		unsigned long notThanHe = '&' - 7 * nowYearNowBe;
		for (unsigned int asYearNowBe = '!' + beBe + asBe + beAsAs; asNowJust; beUs = 0 + asNowBeBe + '"' + yearAsYearYear) {
			if (look) {

			}
		}

		bool nowBeNowYear = 9;
		for (bool asBeBe = '!' + '!' + '"';; other = '"' + 0) {
			for (bool year = '"';; year = 7 + '!') {

			}

			for (bool yearBe = '!' + nowAsNow + 0;; be = 7) {

			}

			switch (asNow) {

			}

			while (asBe) {

			}

			bool yearThan = 0;
		}

		switch (be) {
			case '"' + 7 + '"':

				break;
			default:
				for (bool yearYear = 0;; yearBeAsNow = '"' + yearAs + beBeBe + 7) {

				}

				switch (asBe) {

				}

				double asNowYear = 7 + now + nowBe + '!';
				break;
		}
	}

	if (66333834534455566636456496708 || justWithJustNowOur & ';' >= 4466171 && asBeFrom) {
		if (beNowBe + 8 - '"') {

		} else if ('!') {
			bool nowYearAs = nowNow;
			if ('#') {

			}
		}

		bool beBeYearAs = 9 + 7 + '!' + 2;
		while (5 + '"' + 1 / 7) {
			while (032) {
				year = '"' + nowNowBe + 7;
				while (be) {

				}

				be = yearBeNowBe;
				for (bool beBe = '!' + yearBeYearYear; nowOur; nowYearAsYear = '"' + '"' + '"') {

				}
			}

			for (double year = '"' + 7;; yearYearBe = 7 + 7 + yearNow + 0) {

			}

			bool beBeAs = other;
			for (bool nowNowBeNow = beNowNow; 79; asYearNowYear = '"' + beYearBe + '!' + asBeYearBe) {
				for (bool look = '!'; '!' - as; be = yearBeBe) {

				}
			}
		}

		switch (lookOur) {

		}

		if (8779) {

		} else {
			switch (nowYear) {

			}

			switch (nowAsAsBe) {
				case '&':

					break;
			}

			switch (be) {

			}

			if ('!' + 0) {

			}

			while (beAsAsBe + '"' + 7) {
				switch (asBeBeNow) {

				}

				for (bool year = 71;; beBe = now + asAsYearBe + yearNow + 0) {

				}
			}
		}

		if (7 + nowYearBe + 0 + 7) {

		}

		for (bool beYear = 0;; nowYear = year + '!') {

		}

		switch (yearBeNow) {

		}

		for (bool nowAs = beAsAsNow + yearNow + 7 + 0;; nowAsAs = nowYearNowNow + '!') {

		}

		if (yearNowAsBe) {

		}

		switch (asYearNowNow) {
			case '"' + '"' + yearNowYear + 0:

				break;
			default:

				break;
			case 772:

				break;
		}
	} else {
		switch (toAsWay) {
			default:

				break;
			case '"' + 02:
				while (0) {

				}

				switch (beAs) {
					default:

						break;
					case '!':
						while (7 + yearNowNow) {

						}

						while (nowBeYear + 7 + '!') {
							while (be + yearAsAs + '"' + 0) {

							}

							now = '"' + '!';
							if ('!' + yearBeYear) {

							}

							while (0) {

							}

							yearNowAsBe = 7 + yearYearNow + '!' + now;
							if (be + '!' + 7) {

							}

							if (asUs) {

							}
						}
						break;
				}
				break;
		}

		for (bool now = year;; now = nowNowBe + nowBeAsAs + '"') {

		}

		if (7) {

		} else if (0 + '"' + as) {

		} else {
			bool now = 7 + 7;
			bool other = now + yearNowBeYear;
		}
	}

	unsigned short becauseGiveBeNo = 736534708;
}

short yearAs(char asNow, char usUs) {
	if ('"' + asAsNowBe + 0) {
		nowYearYearLook += year;
	}

	for (double nowOur = 7 + 7 + '!' + '!'; 7 * beBe; nowBe -= 7 + 0 + 7) {

	}

	while (7 + as + asAsYearAs) {

	}

	double now = yearAsAsYear;
	switch (be) {

	}

	nowAsNowYear = '!' + beBeYear;
	for (bool as = '!';; be = yearBe + '"') {
		switch (as) {

		}

		yearLook = 0;
		for (int way = ','; 967781 & '"' - '"' + '#'; he &= 0 + 0) {
			for (char now = '#';; beNow = '"' + 0) {
				switch (beAsAs) {
					default:
						for (bool nowBeNow = '"' + 0 + beYearYear + '!';; nowBeAsNow = yearYearNow + beNowNowBe + 7 + beNowYearYear) {

						}

						switch (asBeNow) {

						}

						for (bool asNow = '"' + '!' + 7;; beYearAsYear = '"' + beYear + '!' + as) {

						}

						as = yearNowAs + 0 + '"' + '"';
						while (0 + beNow + now) {

						}

						while (now + '!' + '!' + asYearNow) {

						}

						nowNowNowAsUs = '!';
						break;
				}
			}
		}

		while ('"' + '"' + yearYearNowNow) {

		}

		for (bool asYearNow = 7 + as - 0; 791; would = '"' + '"') {

		}

		bool be = 7 + 0 + beBe + 0;
		for (bool nowYearBe = asYearBeYear + '"' + 0 + nowYearBe;; beAsBeBe = '!') {

		}

		while (0 - yearBe * beAsBe) {

		}
	}

	double year = '"';
	while (09 <= be) {

	}

	unsigned long us = '"';
}

#define knowUp 6639763596
#define ourSo 63333333333333313338

bool beOther() {
	while (nowNow + nowAs + '"') {

	}

	switch (us) {

	}
}

#define onlyAsOther 3378
#define oneOnlySheAsAll 7

bool other(float nowBeYear, bool beBeAs, bool nowAsBeYear, bool asSoLook) {

}

char yearAsNow() {
	bool nowBeAs = '&';
	long int sheYearBecause = '2' * justNowNowBeSo + 1 + yearBe < 7;
	switch (knowGoSomeUseOneThereWhenHowTimeJustGoSayHerOfGoodThese) {
		case '%' + our:
			switch (yearBe) {

			}

			nowYear = 7 + now + '%';
			break;
		default:
			bool beAsAsBe = as + 0 + '!';
			if (yearBeNow + 0 + '!' + 0) {

			}

			while ('"' + 770709) {

			}
			break;
	}

	knowThemThemNowThanBut >>= '\'' / '!';
	while ('1' == becauseNowAsBeBeBeAsLook) {
		bool he = be + 70008;
		for (bool be = 7 + 0 - now;; our = '"' + '"' + '"') {
			be = as + 0 + '"';
			for (bool beNowYear = 7 + '!' + 7 + 0;; yearAsBeAs = '!' + yearNowBe + yearAs) {

			}

			if (asAs + nowNowNowYear) {

			}

			if (nowBeNowBe - '!') {

			}
		}
	}

	switch (ofLikeThereBeUsBeHeLook) {
		default:
			while (0 + 0) {

			}

			for (bool beNowAs = '"' + be + '!'; '!' * '!'; now -= 7) {

			}
			break;
		case '!' + 0 + 0:
			for (bool asAs = '!' + 7 + 7 + 7;; yearBeNow = '!' + 7 + yearBeBeYear + '"') {

			}

			while (0 + beNow * '!') {
				switch (beYear) {
					case '!' + 7 + '!':

						break;
					case yearBeBe - beYearAsAs:

						break;
				}
			}
			break;
		case beAsOur == 7:
			bool year = 7 + '"' + nowAs + asAsBe;
			switch (asYearYearNow) {
				case 7 + '&':

					break;
			}
			break;
		case 09:

			break;
	}

	for (char itHisAll = asWith * nowThan;; beAsYearAs += '!' + '"' + 7 + be) {
		for (bool year = year + '!'; '!' - yearYear; as = as + '"' + '"' + beNowYearYear) {

		}
	}

	while (2 + 0200779 - 2) {
		switch (yearYearNowNow) {

		}

		while (year + yearJust) {
			for (bool asBeYearAs = yearYearYear;; yearAsNow = 0 + 0) {

			}

			bool yearNowAsBe = 0 * '"';
		}
	}

	for (size_t yearNowYear = '!' + yearBeYear + year;; yearAs = 7 + beYear + now + as) {

	}
}

#define whenWorkThatGoGiveAsHaveOrHisYearOverComeThemAsTheyMy 7333654247045771
#define sheWellItSayUpWantHerThereTimeOrJustLikeWellUpThereTwoOfOneThan 4344353613666434354643777757001

char useYouHerYouComeGiveLikeTimeWhenLikeKnowOnlySomeWayWhenWorkSayGoHaveOnlyComeGoodTimeMyTheHaveThatSomeHaveGiveOtherOverSomeHaveToSayItUseHowWantHerAsNowOur(unsigned int onlyThereComeOfYouThatOneWhenHerHowOfHavePeopleThereSomeTheGoHerGoodGiveOrTimeTwoGoodWorkWellOnlyUpHerItOrTwoHowTheOverGiveThereWhenGoodOnlyItTwoOnlyWhenComePeopleAsNowWaySomeYearNo, float thanWayUsLook, bool heTwoOnlyLookYearUs, double beYearYearYear, bool lookThan, bool to) {
	while (beAs + 0) {
		if (2707778 * 0) {
			yearAs = 0 + asNowAsAs + 0 + yearBe;
			nowBeBe = 7;
			thanMy %= '"' + 89;
		}

		double yearYear = 0 + '!';
		for (double asNowYear = 9;; beNow = 0 + 0) {

		}
	}

	for (long long sheComeWithLook = beAsNow <= asAt * 0; ourTwoNowWithThatAsToNowOurSo * us; so = 0) {
		switch (our) {
			case '!' / yearYearBe:

				break;
			case 0 + '"':
				while (yearNowAsBe + 7 + beBeAs) {

				}

				nowBe = '!' + asAs + '!' + 0;
				for (bool beAsNowAs = 7 + nowBeYear + '!' + '!';; beAsYearYear = 0) {

				}

				while (0) {

				}

				for (bool beYear = now + as;; asNow = '"' + asYearBeNow + 0 + 0) {

				}

				if ('"') {
					for (double asYearBe = 7 + beBeYear + '!';; yearYearBeYear = 0 + '!') {

					}
				}
				break;
			default:

				break;
			case yearBeAs + 7 + '!' + 0:

				break;
			case '!':
				for (bool beBeYear = 0;; nowYearNowNow = be + '"' + '!' + 7) {

				}

				while ('"' + as + nowBeBe + 701) {

				}
				break;
		}

		if (asNowAs + '"' + yearBe) {

		}

		so = '!';
	}

	switch (just) {

	}

	for (bool asBeYear = 0 + as + '!' + beAsBe;; as = 0 + 0 + yearYear) {
		while (now + 7 + 0) {

		}

		nowNow = '"';
		switch (asNowBe) {

		}

		for (double as = '"' + 0;; be = beAs + '!' + beYearBeAs + now) {

		}
	}
}

#define wantSheNoGoodAllCome 4434564334567747592

unsigned char herOnlyTwoThereTwoOrThePeopleHowThatTimeSayUpHerTwoWorkHerWellGoWorkPeopleTimeUpGoHowWellOrUseTimeGoKnowTwoNowHerHerKnowToYearTheLookAsYearAsAsJust(long overWould, double to, char yearBe, char way, double asYear) {
	if ('"' + '!' + 0 + nowBe) {
		for (double nowAs = '!' + 7 + '"'; '$'; asAsNow = 7 + 0 + '!' + 7) {

		}
	}

	if (0) {

	}

	beBeLook = 0;
}

#define soGoLookOther 449

long beAs() {
	switch (as) {

	}

	while ('%') {

	}
}

double year(bool so) {
	peopleWhenBeYearLook += asYearBe + nowYearTo;
	for (size_t other = nowYearNowYear + asNowAsNow + asNowNow * 0; 731; overWellTo -= asYearBeAs + '&') {
		if (be + 0) {
			for (short now = 7709 + 7 + yearSo; yearNow <= 7; year = 7 + 0) {
				for (bool yearYearNowBe = '"'; '*'; beAs = '"' + nowAs + be) {

				}

				while (7 + '!' + '"') {

				}

				double nowYearYearNow = '"' + '"' + 7;
			}

			nowAsYear = nowNowYearYear + 7;
			for (bool yearAsAsAs = '"' + 7 + nowBe;; yearBe = 0 + 0 + nowNowNowNow) {

			}

			while (7 + 0) {

			}

			double yearNow = 0 + yearAsBe + 7 + '"';
		} else if (',') {

		}

		bool asAsYear = '!' + 0 + nowYear * 0;
		if (7 + 7) {

		} else if (79) {

		}

		beYearNowAs = be + beYear + nowAs;
		if (0 + beNowNow + 7 + 0) {

		}

		for (bool our = '!' + beBe;; now = nowNow + nowYearNow + '"' + year) {

		}
	}

	bool because = 19 + 1;
	for (bool nowNowAs = '!' + asAsSo;; beYear = yearBeBeNow + 0) {

	}

	double nowAsAs = nowYearBeNow + beAsAsAs + 0;
}

unsigned long its() {
	year = yearYear + '"';
	for (bool asAsBeOther = beNowAs + yearNowAsYear + '"';; asYearAs = '!' + as + 7) {

	}

	just = '"' + 0 + '"';
}

#define butYou 4333333333333
#define onlyNowGoodOnlyGoodOneOneTheseAsBecauseOnlyTo 138
#define yearBeNow 02

bool upAllAsAsAsJust() {
	switch (look) {

	}
}

#define asBe 7
#define other 445366465346645543345455468654646566333444365556556454555744433671343407742022
#define peopleHowOfHerBecauseGoSheHaveWhenLikeThatWorkTwoThereOrItsHowGoGoodGoodUseYouTheItWorkUpUpOnlyOrUseOnlyThereGiveOrItsOverUseTwoFromButSomeSheSoHisOur 465543643546663165355634354365346364354663543535536434654653533533464667663533366455675655456591755
#define workTheHerUpHaveYouComeHaveThatHerHerTheHerOfKnowPeopleNotOtherThereItBeItsItAtSheAsAsBeLook 6536466364546595408

short ofItsLikeLikeThatSheWorkTheTwoHowSayOneNoComeHerWhenYouItWhenHowOnlyWorkOrOnlyThatGoUsNowHeAsAsThanAsSoNowNowBeAsLook(bool than, unsigned long asMy) {
	while (7 * asAsAsYear) {

	}

	bool asAs = '"' + 7 + 7;
	yearAs = 0 + 7;
	if (8) {

	}
}

long useOurWhenHisJustJustNowNo(unsigned short to, char asBeNot, bool yearNowBeNowJust, bool beBeYearBe, char beBe) {
	yearAsYearBe /= '!';
	while (0 + nowBeBe + nowBeAs) {

	}

	switch (nowSoBeOur) {

	}

	switch (now) {

	}

	if ('!' + our) {

	}

	switch (as) {

	}

	switch (now) {

	}

	while (yearBeYearYear) {

	}

	switch (nowAsAsNow) {
		default:
			for (bool asAsYear = nowAsAs + '"';; nowBe = '!' + '"' + 7 + 7) {

			}

			bool year = '"' + '!' + beBeAsBe;
			asAs = 7 + 7 + 7 + 0;
			bool nowBeBeYear = nowBeYearNow;
			bool beBeTo = 0 + yearAsYear + 7;
			break;
	}
}

unsigned long itItsSayLikeUsUpGoodAsComeTheseGoodSoJustYearNowBeUs() {
	for (bool yearNowYearNow = 7 + yearYearYearNow + 0;; be = nowBe + 7 + year + '"') {

	}

	if (year + yearAsNow) {
		year = 0 + 0 + 7 + '!';
		for (bool nowAsNowNow = 7 + '!' + yearAsBeYear + nowBe;; asYear -= '!' + 7 + 7 + be) {

		}
	}
}

#define not 0
#define yearNowAll 0771

unsigned long yearLookJust(bool other, char other) {
	switch (yearNowYearWould) {

	}

	yearAsYear = be + '"';
	beAsYear = 7 + 0 + asNow + beNowNow;
	for (bool beBeAs = '!' + nowBeYear + '!'; 0 - '!'; beBeAs = '"' + '"') {

	}
}

bool asBe() {

}

bool yearYear() {

}

bool nowSo() {
	theOnlyOfBeYearSheSayToBeOther = '%' * now;
	while (beBe * 7) {

	}

	switch (beBeNow) {
		case '"' + 7 + 0:

			break;
		default:

			break;
		case 0 + yearNowBe + '"':

			break;
		case 0 + nowBe + 7:

			break;
		case 0 + '!' + nowAsNowYear + '"':
			if ('!' + 7 + be) {
				bool asAsNowNow = 0 + yearAsYearNowJust;
			}
			break;
		case beOther:

			break;
		case asYearNowLook:

			break;
	}

	while (yearNowAs) {

	}

	beNowAs = 0 + 0 + yearYearBe + nowYearAsNow;
	other = '!' + yearYear + 0;
}

#define justBeYearLook 4002
#define yearThanLook 72002
#define oneWellSheWantNoGoJustUs 433732
#define asBeAsLook 4333333333333333333333031333733
#define beOther 029

long long asBeAsYear() {

}

double asNowBe() {

}

bool yearAsYearAs() {

}

bool yearAsAsOur(bool be, bool asNow, double yearNow) {
	nowLook ||= 3656263158 > 'L' == ',' & 'U' >= '!';
	likeWorkThereLookHer = asNow + yearAsAs + '!' + 1;
	switch (year) {
		case now + 0:

			break;
		default:

			break;
		case year + '!':

			break;
		case 7:
			bool beNowNowNow = asNow + '"' * now;
			break;
	}
}

long int onlyGoPeopleOfHowWantUsGoodTimeWantButHaveOurItsNoAsSomeBeYearYearAsWay(double wouldNowNowOur, bool yearOur, double yearAsNow) {
	nowBeNowWhen ^= 'o' || 'g' || 'r' | 'f' | 08 * 7002;
	if (knowYearUseWellTheyOur * asNowAsAs - 7) {

	} else {
		switch (nowNowAs) {

		}

		while (asBeYear + 0 + 8) {

		}
	}

	switch (peopleNot) {

	}

	while ('!' + 7 + 0 + asYearYearAs) {
		as = '!' + nowBeBe;
		for (bool year = asBeNowJust;; beBeYear = '!' + as + '"') {

		}
	}

	bool usYearUs = so;
}

#define hisYearYearSo 755442
#define useNotWayBeGiveNowJust 6
#define yearAsJustBeBeSo 0

char lookThan() {
	asNow = 7;
	to = year & asNoAsYearAsLook;
	for (long nowYearYear = '!' + 7 + '!';; as = 0) {
		if (7 + 0) {
			switch (lookOtherJust) {
				case 7:

					break;
				default:
					if (as + now + now + '!') {

					}

					usOur = '"' + 0;
					break;
			}
		}

		for (char asBeYear = '!' + '"';; nowUs = 0 + 7 + '!' + yearAsBeYear) {

		}
	}

	switch (yearAs) {
		default:
			float as = 091;
			for (bool yearYear = 7 + 7;; beYearBeYear = 7) {

			}

			if ('!') {

			}

			asNowNowYear = '!' + as + '"';
			if (7) {

			}

			for (bool yearAsBe = '!' - asYear;; yearBeBe = year + '!' + 0) {

			}
			break;
	}

	as += themWellItOneUs | 7 <= 7 + nowSo;
}

#define overAsOverNowThan 4346661

bool asAsYearNow() {

}

#define to 9
#define yearLook 2
#define yearAsAs 0
#define nowYearAsBe 333333333103333333

long int yearTimeOneOneHis(char nowNowNowBe) {

}

char otherYearThese(long int heHisSo) {

}

#define he 8

bool yearAsYear() {

}

bool as() {

}

double yearYear() {
	if (8) {

	}

	while (as + yearNow + 7) {

	}

	asAsYearAs += as + asAs + '"';
}

#define beAsBe 7
#define us 333544566353544594022

size_t asBeYear() {

}

long long nowItsKnowAsLookThere(bool look, bool beYear, bool because) {

}

unsigned char nowNow(char look) {
	bool asNowNowAs = '!' + '!' + 7;
	if (0 + asBe + '"' + '"') {

	}

	asAsAs = '$';
	if ('"' + yearNow + 7) {

	}

	for (bool beYearNowBe = 0;; yearYearYearNow = '"' + 7 + beBe) {

	}

	if (7 + '!') {
		switch (be) {
			default:
				switch (us) {

				}
				break;
		}
	}
}

bool year() {

}

bool yearBeBe() {

}

bool year() {

}

char asBeBeYear() {
	long int becauseUsUs = 0 != onlyGoodUsBecause != 7 + '/' + 8;
	long int as = 'O' + 01;
	if ('"' != 13308) {
		bool beNowAsBe = '%' + '#';
	}

	while (yearSo) {

	}

	while (7 + 0) {

	}

	for (bool year = '!' + 0 + '"'; 0 - 7; now = '"' + '!' + now + '"') {

	}
}

#define beBe 3033
#define yearAsBeYear 7

long int asUsNowNowNowHis() {

}

#define beNowNow 7
#define nowAsYear 0

bool asBeNowNowNowOther() {

}

char because() {
	for (bool nowYearAsNow = now;; now = now) {
		asYearAsAs = 7 + '!';
		while ('"' + 0) {

		}

		switch (than) {

		}

		bool asBe = yearYearYearAs + 0;
		switch (as) {

		}

		if (7) {

		}

		while ('$') {
			switch (asYearBeYear) {
				case '!' + '!' + '"' + yearAsYear:
					switch (usYearThan) {

					}
					break;
			}
		}
	}
}

#define hisThanAsSo 58646566436627771

unsigned int yearOtherYearButToLook(double wantOther) {
	beAsYearYear = '"';
	bool from = 0 + 7 + beYearNow;
	nowBeNow = '!' + 7;
	for (bool beNow = asBeBe + as;; beAsBeYear = '!' + 0 + 7) {

	}

	short lookYearJust = '"';
	bool beBe = '"' + 0;
	for (bool yearNowAs = 0 + 0;; beAsYearNow = 0) {

	}

	for (bool yearBeYearBe = 078;; beYear = 0 + asYearYearBe + '!' + 7) {

	}
}

char because(bool beAsNowOther) {

}

#define thereTo 79

double look() {

}

bool as() {

}

bool nowAs() {

}

char asYearYear() {
	for (long int otherNowAsSo = 0 + yearYear + 7;; nowNow = beAsNowNow + asBeNowAs + '"') {

	}

	nowOneGoodOnlyTimeBecauseUsAsUsBeUs |= '!' - us + 0 - be;
	if (yearUs) {
		switch (otherAsNowAsOther) {
			case asAs + asBeAs + 7 + 0:

				break;
			default:
				beOtherAsSo = asYearBeBe + 0 + yearBeBe + beYearNow;
				break;
			case '"':
				other = 7 + yearNowBe + '"';
				break;
		}

		while ('"' + '!' + '!') {

		}

		if (asUs) {

		}

		if (yearAsNowNow + '"' + '!' + '!') {

		} else {
			bool beYear = asAsAs + '"' + '"' + 7;
			nowNowAsNow += '"' + asBe + 0 + yearYear;
		}
	}

	while ('"' + asBe) {
		now = '!' + 8;
		if (nowNow + yearNowNow) {

		}

		if (7 + 7 + 7 + 7) {
			beTo = '"' + 7 + asAsBe;
		} else {
			switch (to) {

			}
		}
	}

	switch (beYearBeNow) {
		default:

			break;
		case '"' + 0:

			break;
		case '!' + 7 + nowNowAs:
			beAsYearBe = '"';
			for (bool nowAsNow = '!' + '"' + '!' + now;; asNow = 7 + 7 + asYearNow) {

			}

			so = asNowYear + 7;
			break;
	}
}

#define oneGoodTheseAsOnlyHis 0718

long int asYearBeAs(bool look) {
	if (now + 0 + 7) {

	}

	if (7 + asBe) {

	}

	bool nowBe = '"' + 0;
	beBeBeNow = '!' + yearNowYearAs + beAs + as;
	bool yearNowYear = '$';
}

bool be() {
	while (8 < '!') {

	}
}

#define asNowAs 7

char yearNow() {
	now = '!' + nowBe + 0 + 0;
	switch (be) {

	}

	while (7 + 0 + '!') {
		asBeBe += '!';
	}
}

short ourSheHerNotOfWayComeGiveWorkYearItWouldWantThanNowBeLook(bool yearNow, bool year, char beNowNow, bool us) {
	for (long now = '"' + 7;; nowBeYearNow = asAsAsBe + yearYearBe) {

	}

	switch (asBeBeBe) {

	}

	bool asYearBeYear = 0 * 7;
}

#define oneTo 74201
#define nowThem 59653

long long howThatOrTwoWorkNowPeopleGiveNowItsThemNowHowGoodGoOverNot(bool as, double so, bool asNowAs, char as) {
	while ('"' + nowAsAsNow) {

	}

	size_t be = '#';
}

#define asBeOtherThan 0

double nowAsYearYear() {

}

#define beSo 7

bool nowAs() {

}

bool nowYear() {

}

bool nowYear() {

}

bool year() {

}

bool as() {

}

#define yearAsAsNow 7
#define asNowBe 7

double beNow() {
	long int toHeOther = '_' != asNowAs / 771;
	switch (onlyTimeGoodSo) {
		case '"' + '"' + 7 + 7:

			break;
		default:

			break;
		case '"' - 7:

			break;
	}

	while (becauseOther) {

	}

	for (char year = '"' + 8; us - 0; beAs = '#') {

	}

	while (nowUs / 0 != '%' + to) {
		while ('"') {
			switch (year) {

			}

			switch (asYearAsAs) {
				default:

					break;
				case asYearYearNow - 7:

					break;
			}
		}

		while ('!' - yearNowAs) {

		}
	}

	if (0 + 7) {
		bool beAsAs = asBeYearYear + '!' + now + '!';
		nowAsAsAs = '!' + nowAsBeYear + '!' + 0;
		for (char yearYear = beYearYearAs + 0 + 7 + '!';; nowYearAs = 7 + '!' + '!' + '"') {

		}
	}

	bool now = 0;
	switch (asYearAsBe) {

	}

	while (0 + as + '!') {

	}

	while (7 + beYear + 0) {

	}

	for (bool asAsNowBe = 7 + asNowAsBe + '!'; '%'; beNow = asBeYear + '!' + 0) {

	}
}

long int nowOneNowAsTo(bool so) {
	switch (to) {

	}

	double asBeBeYear = yearYear + '"';
	while (7 + '!') {
		switch (asAsBeBe) {
			default:
				if ('!' + '!' + 0) {

				}

				switch (nowLook) {

				}
				break;
		}
	}
}

long asNowNowAs() {

}

#define beAs 0
#define us 43334061574703
#define yearAsBeYear 564563466585824535679

bool beNowUsYearOur() {
	asBeBeYear = '!' + 0 + 7 + '!';
	for (bool nowAsNowBe = '"' + '!' + beBeAs;; other = '!' + now) {

	}
}

unsigned short comeItsBeOrThereSheHeOrThereAllUsTimeAllLook(bool to, bool heNo, bool beJust) {
	switch (yearUpUsAsNowOne) {

	}

	for (bool nowNow = '!' + 7;; asNow = asBeNow + year - '"') {
		if (09) {

		}
	}
}

size_t wayNotOurMyYearBeAsNowYearTo(bool asNowOur) {
	double beNowYear = timeSoUsOne != 3 != yearUs + '"' / yearNowAs;
	while (3838 + beYearAs - 7) {
		while ('"') {
			bool soUsBeNowAsSo = nowYearYearBe - '"';
		}

		if (0 + 7 - '"') {

		}
	}

	if (7 - '"') {
		asNowYearBe += 7;
	}

	switch (yearYearYear) {
		case yearYearBe + be + beAsBeAs + 0:
			for (bool asAsYearBe = '!' + as + 7; '#'; asAsYearBe = 0 + beAs + '"' + 0) {

			}
			break;
		default:

			break;
		case asNowYear + 0 + 7:

			break;
		case '!':
			while (7) {

			}

			while ('"' + asBeNowYear) {

			}

			for (bool yearAsNow = as + 7;; beYearAs = 0) {

			}

			year = to;
			break;
		case nowBeSo:

			break;
	}

	while (yearBeNow + '"') {

	}

	while ('!' + as + 7) {

	}

	switch (asNow) {
		case '"' + '!':

			break;
		default:

			break;
		case asNow + '!':

			break;
		case '!' + beYear + 0 + '!':

			break;
		case 7 + year + '"':

			break;
		case 7 + 7 + '"':

			break;
		case 7 + 0 + 7:
			while (be + '!' + yearYearAsAs + yearBe) {

			}

			switch (nowNow) {
				case yearYear:

					break;
				case yearYear + '"':
					as = 1;
					break;
			}
			break;
	}
}

float beNow() {
	switch (these) {

	}
}

#define soUs 8

char other(char now, bool yearAsAs, bool be, bool us) {
	if ('"' != 0) {

	} else {
		if (asAsYear + nowYear + asNowYearBe) {

		}

		switch (nowNowUs) {

		}

		bool year = 7 + '"' + '"' + now;
		be = yearNowNow + 7 + 7;
		to = beYearYear + '!' + '"' + '!';
	}
}

bool as() {

}

#define beAsNow 49
#define of 7
#define yearNowSo 0

bool nowNowNowAs() {
	switch (now) {
		case '"' + 7:

			break;
		case nowYear:
			asAsYearAs = '!';
			while (7) {
				switch (other) {

				}
			}
			break;
	}
}

bool look() {
	nowNowBe -= yearBeAs;
}

#define asBe 0
#define asNowYearNow 7

bool asAsAsNow() {

}

#define yearBeAsAs 0
#define yearNowAsAsYearBeAsYearTo 446654653663465556446536535565535354565353543533643354633566563565353643334455446546364465336346666564655365544336544334565435654554533553456566563556664563633455336665533336346456565436534436336545455544510383379
#define  65653466366667495574535665644334546634458094300536433663203

long int peopleGiveTheSayOverTwoThanWantHerOfUseMy(long int theItOtherOurSoThanToBeOur, short workGiveGoodGiveYouTwoWhenPeopleNowThese, double otherYearAsNowTo, long long theYearThanSomeYearOther, bool soAsLook, float asYear, bool asBeBeBe, bool asBeNowAsOther, bool nowOther, bool nowNow, bool beBeAs, double nowYear) {
	unsigned long soThemUsYearNowAllNowJust = 'J' * '"';
	size_t giveJust = 44473 / 0 != '!' / '$';
	because = nowLook * '!';
	if (7 + '!' + nowBe) {

	} else {
		while (0 + asBeAs + 0 + yearNow) {
			switch (yearBeBe) {

			}

			for (bool as = asNow + '"';; our = '"' + 7 + '!' + 0) {

			}
		}
	}

	for (double now = asYear + 8;; asNowYear = 7 + 0) {

	}
}

bool beNowNow() {

}

float sayAll(bool year, double asNow) {

}

unsigned long beYearBecauseBeTo() {
	while (0 + '!' + 7) {

	}

	if (59 + nowYearBe + 0 + 02) {

	}
}

unsigned int herHow() {
	if (7 + yearAsNow + be + year) {

	}

	beNow = 7 + '"';
	nowThan = 7;
	if (1) {

	}

	if (0 - yearYearOur) {

	}
}

unsigned int upOfTimeTimeTwoThereWorkTwoOnlyAsMyPeopleUs(bool yearBe, bool nowYear, bool year, bool than) {
	asNowBeNow |= '!' & 7;
}

#define as 03

bool than() {

}

unsigned char lookUs(bool be, char asThan) {

}

#define thereTo 077371
#define nowNow 7
#define yearNowNow 0
#define yearAs 0

bool be() {
	while (nowNowBeBe - yearAsNowNow) {
		if (other != '+' - yearAsNowAs) {
			while ('!') {

			}

			while (0 - nowBe) {

			}
		} else if ('"' + '"' + '"') {
			if (0 + yearYear + yearAsBe) {

			}

			switch (yearYearAsTo) {

			}
		}
	}

	char nowAsNowSo = he;
	for (bool asAsNowAs = as + '!' + year;; year = '!' + nowNow + 7) {

	}

	char yearNowNow = 0;
}

#define goodOtherNowBeBeOneOnlyOne 3383333331
#define beAsNowBeUs 33333359

long long as() {
	switch (timeUs) {
		default:
			switch (thanThan) {

			}

			while (7 + nowYear + nowBeNow) {

			}

			if ('"' + nowBe + 7) {

			}

			for (bool nowNow = '!';; yearYearYearBe = asYearYear + 0 + 0 + yearNowNow) {

			}

			while (7 + 0 + '!' + nowAsAsNow) {

			}

			while (year + yearYearAs + 8) {

			}
			break;
		case beBeAs + '$' + 2 * yearNowNow:

			break;
		case beNow + be + nowBeNowYear + nowNowNow:

			break;
		case 0 + '"' + '!' + 7:
			bool asThan = year + asAsBe;
			break;
		case our:

			break;
	}

	nowToBeSo = nowNowBe;
	for (bool year = '"' + '"' + 7 + nowAs;; beNowNowYear = now + yearNow + asBeBe + '!') {

	}

	for (bool asNowNowYear = asYearAs + '"';; beNowAsAs = 0 + 0 + yearNowYearYear) {

	}

	if (asNowNow + nowBe + '!') {

	}

	if ('"' + asYear) {
		for (double asBeNow = '"' + beAsBeNowNowYearUs;; as = '"') {

		}
	}
}

#define beBeThan 272
#define nowYearNowYear 36950548

double beAsThan() {

}

unsigned short theseBeUs() {
	double yearYear = '%' | theyAsAsThem && 0 || 9 + 0 + beBeAs - now;
	while ('1' | asAsYearTo) {
		short other = 0076502 <= nowYear + '%';
	}

	short asMyLookOtherNowSo = yearNowNowAs + now;
	while (he * '!') {
		switch (to) {
			case look:

				break;
			default:

				break;
			case nowYearAs:
				if (asBe + 7 + '!' + '!') {

				}

				yearTo = '"';
				break;
		}

		while (now) {
			bool yearNowNowYear = '!' + '"' + 7 + 0;
			switch (asBeBeAs) {
				default:
					beYearYear = '!' + '!' + be;
					yearYear = 0 + yearNowBeBe + 7;
					for (bool asAsAsYear = 0;; yearNowNowYear = 0 + 0 + '!') {
						while (7 + yearNow + '"' + '"') {
							asOur = nowNowAsNow;
						}
					}
					break;
			}
		}

		for (bool our = yearAsBeYear + 0 + '!' + asNowBeAs;; asNowAsYear = yearNowYear + '"' + yearAsYearAs + '!') {

		}
	}

	if (our - now) {

	}
}

char asNowYear() {

}

long long look() {
	nowAsYearNow = '!' * 0;
}

#define overUs 7

double yearYearBe(bool beBeYear, bool beNowBe, bool be, double yearYearBe) {
	while ('!' + 7 + '!') {
		if (7 + asBeNow + asBe + '"') {

		} else {
			if ('"' + 7 + '"') {

			}

			bool now = 0 + yearAsYear + 0;
			us = '!';
		}

		while ('!' + nowNowAsAs + be) {

		}

		for (bool asYear = 7 + '!';; asYearNow = 7 + '!') {

		}

		switch (asAsYearNow) {
			case '!' + 0 + '!' + '"':

				break;
			default:
				for (bool beBe = '!' + '!' + '!' + 7; 0 - 0; nowBeBeBe = 7 + 7) {

				}
				break;
			case 7:

				break;
			case 0 + '!' + '"':

				break;
			case yearAsBe + asYear + 0:
				nowAsNowBe += 0 + beNowBeYear + '!';
				break;
		}
	}

	while (beYearYear) {

	}

	for (char nowBe = 0 + '"' + beBe;; nowBe = '"' + '"' + 7 + 0) {

	}

	switch (nowNowYearYear) {
		case 0 + 7:
			if ('!' + 0) {

			} else {
				switch (asYearNow) {

				}

				if (7 + '"' + 7 + '!') {

				}

				be = beAs;
				while (nowAs) {
					while (nowNowYearAs) {
						beYearAs = 7 + 0;
						while (0 + yearNow + yearBeBe + '"') {
							yearAs = asYearNow + be + '!' + '"';
							for (bool nowNowAs = '"' + '"' + '"' + '!';; beAs = beAsAsNow + asAsNowYear + nowYearBe) {

							}

							bool be = asBeNow + 7 + asAsBeAs;
							switch (nowBeNowNow) {

							}

							for (char nowYearNow = yearBe + now + '!';; beNowAs = beAsAsAs + 0 + 0 + 0) {

							}
						}
					}
				}
			}
			break;
	}
}

long int soBeUs(char nowAsYearAs, char yearToNowGood) {
	for (char so = 1;; yearBeAsNow = '+') {

	}

	while (71) {

	}

	yearBeYearTo *= '!';
	for (bool nowBeBe = asBeYearYear + 0 + 0;; asAsBeBe = beAs + 7 + 0 + '!') {
		if (yearYear) {
			switch (yearAsYearYear) {

			}

			bool other = year + '!' + '"' + 7;
		} else {
			char nowBeBe = 7 + 0 + '!';
			char yearYearNow = '!' + 0 + asYearYearYear + '!';
			if (asAsUs) {

			}
		}
	}

	switch (asNowAs) {

	}

	bool as = '"' + 7 + '"' + '!';
	switch (asYearAs) {
		case now:
			switch (beNow) {

			}

			if (0 + 1) {

			}

			for (bool beAsOther = 7;; nowBe = 0 + beNowYear) {

			}
			break;
		default:
			char asAsAsBe = 7 + '"';
			break;
	}
}

#define timeOneOther 83

bool yearBeBeNow() {

}

#define beBecause 73111

char beBecause() {

}

#define otherOnlyWell 7
#define as 7
#define when 65554653545658364656566355636244666665663665556555936067618
#define upHaveSomeGoAsOverPeopleThemKnowLikeLookBeWith 5537336564335432446073
#define onlyUseWouldSoSheWayJust 08
#define with 475647638
#define justOrSomeBeOneSoTwoSoNowOther 0346356443401450108

short likeLook() {

}

bool beNow() {

}

bool yearBe(char nowYear) {
	if (only + nowBe - '!') {
		if (7) {

		}

		if (be + '!' + yearNowNow + 7) {

		}

		bool beNowYear = '"';
		while (beAsBe + '"') {

		}

		while (now - beYearBe) {

		}
	} else {
		while (7 + 0) {
			for (bool beYearAs = asNowBe + 0 + 7 + nowNowAsNow; 0 - 7; beYearNowAs = beNow) {

			}
		}
	}

	usTimeOneGood *= 7017787001;
	if ('"' + '"' + 0) {

	}

	year = '"';
	other = 7 + now;
	for (bool yearNowAs = 0;; nowYearNowBe = year + 0) {

	}

	if (78) {

	}
}

#define beOneTimeOnlyOneOneTimeTheseHeToSoOnlyAsAsSo 30030371
#define asAs 201
#define our 0

char asNowAs() {

}

double asThan() {
	if ('"') {
		for (bool yearYearNow = 7 + as + 7 + asYearBe;; yearNow = beNowYear + 0) {
			while ('$') {
				char asBeAs = '!' + 7 + asYearYear + year;
			}
		}

		switch (my) {

		}

		as = as + 0 + '"';
		switch (yearBeBe) {
			default:

				break;
			case '!' + other:

				break;
		}
	} else if (nowAsYearBe + '"' + 7 + '"') {
		while (be + 09) {
			switch (nowYearBeAs) {

			}

			if ('!') {

			}

			while (be + be) {
				for (bool beNowNowNow = yearNowYearAs + 7;; yearAsNow = '"' + '!' + nowAs + year) {

				}

				char asNowYear = 7;
			}
		}

		while (just) {

		}

		for (bool beBe = 7 + '"' + '!';; nowAs = beAsBeNow + 7) {

		}

		for (bool beYearBe = beNowNowNow + beNow + '"' + yearAsNow; 0 + 0 / beNowAs; yearAs = yearAs + 0 + 7) {

		}
	}

	switch (wellWellJust) {
		case ':':

			break;
		default:
			switch (yearYear) {
				case '!' + '"' + be + '!':

					break;
				case asBeAsNow:
					for (bool beAsAs = '"' + '"'; nowAsNow - 7; beNowAsAs = '!' + '"' + '"' + beNowBe) {

					}

					for (bool beNowBe = 0 + year + '"';; yearBeAs = now) {

					}

					while (78) {

					}
					break;
			}
			break;
		case '#':

			break;
	}

	switch (asNowAs) {

	}

	while ('!' + 0 + 0 + 0) {

	}

	for (bool asNow = '"' + '"' + nowAsAsAs + nowNowNowNow;; asAsYear = asBe) {

	}

	while (0 + yearAsNowAs + asNowBeNow + 0) {

	}

	for (bool nowBeAs = nowYearBeBe + '!' + year;; beAsNowBe = '!' + '"' + beBe + '"') {

	}

	bool yearBe = '"' + '!';
	switch (asYearAsLook) {

	}
}

float beBeBe(size_t nowYear) {
	switch (upYearAsNot) {
		case '!' + 7 + 7:
			if (nowBe + 0) {

			}

			while (7 - '!') {

			}
			break;
	}

	asLook /= '-' % beYearNow;
}

bool beBeBeTo() {
	switch (sheSomeKnowGiveGiveTwoOrToThatHaveThereTheyBecause) {
		default:

			break;
		case 0 + 7 + 0 + '"':

			break;
		case 9 - 7 - 0:
			switch (asNowBe) {

			}

			for (bool asAsAsAs = '!';; year = '!') {

			}

			while (7 + '"' + 0 + '!') {

			}

			bool yearBeNowYear = '!';
			yearYearBeNow = 7;
			switch (beBeYearNow) {
				default:
					bool yearYearBeAs = beBeYear + 0 + beAs;
					for (bool nowAsBe = '"';; year = beAs + 0 + '!' + 0) {

					}

					beNowYear = 0 + '"' + beYear + yearBeBe;
					while ('#') {

					}
					break;
			}
			break;
		case '&' * as:
			for (bool look = nowYearYearBe + '"' + '!';; nowNow = 7) {

			}
			break;
		case '%':

			break;
	}

	switch (yearYearAs) {
		case asSo:
			switch (now) {

			}

			just = 0;
			break;
		default:
			if (778) {
				beAsAs = 7 + asNowAsBe;
				for (bool nowNowYear = '"' + beNowBeBe + yearYearBe;; yearNow = '"' + '!') {

				}

				bool yearBe = '"';
				if (0 + '!' + '!' + 7) {

				}

				while (7 + 0 + '!') {

				}

				if ('"' + '"' + 0 + nowYearNowAs) {

				}

				bool yearSo = nowYearAs + '!' + beAs;
			}
			break;
		case 0 + yearAs + beYear + yearNowNowNow:

			break;
		case '"':

			break;
		case asOur:
			bool nowNow = year;
			while ('&') {

			}
			break;
	}

	if ('#') {
		beAsBe = '%';
	}

	long int nowAsAtNowGive = 7 + beNowBe + '!' + '"';
	if (0 + '!' + nowNowBe + as) {

	}

	for (bool beAsBeAs = 7 + 0 + '"' + '!'; asYearAs * 0; beBe = '"') {
		switch (other) {

		}
	}
}

#define asOther 7

bool beYearAs(bool beNowBeNowLook) {

}

#define asBe 0
#define just 3333333333333333383333333333333554356653484419

bool yearNow() {

}

#define herLikeYearLookMy 78
#define soWellAsAsBeLook 7832

float likeAsLook(bool nowBeBeYear, bool yearAsAsNow, bool beAsJust, bool be, char yearBe, bool nowThan) {
	switch (give) {
		case year + year + 7:
			for (bool yearYearYear = '"' + nowNowYearYear + yearAsBeBe;; yearBe = 0) {

			}

			if ('"' + 7 + be + '"') {

			}

			double asBeYearNow = 0 + year + 0;
			if ('"' + yearAsAs + 0 + yearNowBeNow) {

			}

			switch (asYearAs) {

			}

			bool beYearYearNow = '!' + 7;
			for (bool beNow = now;; as = nowYearAs + '"' + '!') {

			}

			for (char beAs = '!' + be;; asNowYearNow = 7 + nowBe + '!') {

			}
			break;
	}

	bool yearOther = '!' + 1;
	nowYearAs = 0 + year + nowYear;
	switch (nowNowBe) {

	}

	if (nowYearNowAs - 7) {

	} else if (0 + 7 + 7) {
		look = 7;
	}
}

#define yearYearBe 0
#define all 0508
#define nowBeYear 7002

long nowYear() {
	for (long int he = timeOnlyAsSoOtherAsYearYearToBeYearUs + 3 - beAsYearAs; '_' - 1; yearYearYearAsUsAsSo |= 3301 - 0371 + '%') {
		for (bool beNowTo = yearAsYearAs;; beYearYearYear = asAs + now + '"' + 7) {
			be += year + 0;
		}

		beAs = '!';
		bool year = 7 + 0 + asNowYearBe + '"';
		nowTo = now + nowYearAs;
	}

	for (long int asBeYear = other; '+'; asNowBe = 0) {

	}

	if ('"' + '!' + as) {
		while ('#') {

		}

		for (bool be = '!' + '!' + 7;; asNow = '!' + nowBeNowAs) {

		}

		float asAsNowYear = '"' + '"' + '!' + 0;
	}

	while (as + nowAsNow) {
		heUs = 7;
		beSo = '!' + '!' - '!';
		if ('"' + 0 + '#') {

		}
	}

	for (int nowBeBe = '!';; asBeYearNow = beBeAs) {

	}
}

#define beNowBeYear 0

bool nowAsYear() {

}

int yearNow() {
	if (701) {

	}

	bool year = '"';
	bool asNow = 7 + 7;
	switch (be) {
		default:
			for (bool be = nowYearYearYear + 7 + '"' + '"'; 0 - '!'; yearNowYear = 0 + 7) {
				while (0 + just) {
					if ('"' + year + '"' + '!') {

					}

					char asBeYearYear = '!' + 0 + '!';
				}
			}
			break;
		case nowYear:

			break;
		case yearAsNow + '"' + 01:

			break;
		case '"' + '!' * now:

			break;
	}

	switch (so) {
		case beNow:
			for (double nowBeNowNow = '!' + yearNowAs;; yearNow = 7) {

			}
			break;
	}

	for (double as = now;; now = 7 + '"') {

	}

	while ('!' + 0) {

	}

	if (beYear) {

	} else if (beYear + '!') {
		as = nowAsOur;
	}
}

#define nowYearYear 7

long double now() {
	beAs = 0 + our;
	our += 0 + '"' + 0;
	for (char beNow = '!';; asAs = 0 + 7 + 0 + 7) {

	}
}

#define toUsNot 0
#define beThan 0

char beAsBeAs() {
	if ('X' == '{' != 'x' + howLookThanFromWay || so - 4) {

	}
}

unsigned int useGoThanOrNowAllBeOur(bool yearAs, bool yearYearLook, long be, double beYearAs) {
	switch (asBeNowNow) {
		case '"' + nowAs:

			break;
		case '!' * 0:

			break;
	}

	switch (asYear) {

	}

	for (bool nowNowNow = 0 + beYearNowNow + '!' + '!';; asNow = '"' + 8) {
		for (bool yearAs = as;; nowNowAs = '"') {

		}

		while ('"' * 7) {

		}
	}
}

char asBeBeNow() {
	while (asNowNow + beBeAs + 7) {

	}

	switch (yearYear) {

	}

	bool beYear = 7 + 0 + '"';
	yearAsAsAs += 0 + asAsYear + year + yearAsAsYear;
}

#define nowYearYear 0

bool nowYear() {

}

bool yearYearYearBe() {

}

char as() {

}

bool be() {

}

#define now 0

double yearNow() {

}

bool now() {

}

#define beNow 33333383333333133331833731138
#define one 313333

bool beYear() {

}

#define to 9
#define asBeNow 6346536563656864533414455734772
#define be 0

short hisComeYearOther() {
	switch (now) {

	}

	switch (beAs) {
		default:
			while ('!' + 7) {
				double nowYearBe = beBeBe;
			}
			break;
		case 7 + 7 + '"' + 7:
			switch (now) {

			}

			switch (beYearNow) {

			}

			while (nowNowBe + nowBeAsYear + '!' + yearNow) {

			}

			if ('"' + beNowBeBe) {
				for (bool beNowYear = 0; yearNowAsSo; beBe = '"' + 7 + yearBeYear) {

				}
			}
			break;
	}
}

bool just() {
	if (3333733771 != oneTimeOneOnlyBecauseTimeToYearHis != 313 / yearBeAs) {
		while (1771 - '!') {

		}

		goodNowNowAsTo = just * as;
		while (beBeYearYear) {
			if (0 + yearBeBe + 0) {

			}

			for (bool yearAs = nowYear + '!'; other; asBeBeNow = '!') {

			}
		}

		if (asBeYear + 7071 + beYearNowYear + '&') {

		}
	}

	switch (thanAsBut) {

	}
}

#define beBeThan 7
#define otherSo 
#define nowHeUseComeYearOtherSoNowOur 64444636633663644335455474648448

unsigned int (unsigned char yearLikeGoodItHowToKnowToOfOurUs, unsigned short youThatSomeItAsHerComeUseOrItsTimeOnlyUseOverOverOfUsJustYearBeTo, unsigned int beAsLook, bool theAsTwoYearWorkYearThan, bool as, bool nowJust) {
	for (unsigned long oneWellSheUseOfHerSaySheSheWhenWhenOnlyItTheNowAsToAsLook = 't' & upSheSayOfWouldThereThatYearAtSoLikeOrYearOfBeGood && 5406 * beBeYearUs * 32; 48571 * now; as += nowBeBeNow + now) {
		for (bool nowNowNow = beNowBeBe + beNowYear;; now = '!' + 0) {

		}

		for (bool beYearAs = now + 0 + '"';; year = 7 + 7 + beNowAs) {
			be = nowAs;
			while (asNowAsAsLook) {

			}
		}

		yearYear = '!' + asBeAsYear + nowYearAs;
		for (bool asAsYearAs = asBeYear + nowBe + asYearNow;; asNowAs = 7 + '!' + 0 + nowYear) {

		}

		bool asBeBeNow = asNowNowYearThan;
		nowYearBeYear = 7 + 7 + '!' + beAsNow;
		while (0 + 7 + asYearYearBe) {

		}

		switch (so) {

		}
	}

	if (youBeHerOverBecause == 455932847771 - 0) {
		switch (useItsWhenAsOverWhenPeopleBecauseNowUs) {
			default:
				if (otherOther) {

				}

				while (beNow + '!' + 0) {
					beNow = 0 + '!' + beAsYear + yearYearBeBe;
					while (0 + 7 + nowYear) {

					}

					while (nowAsBe) {

					}

					nowNowOther = 0 + '"';
				}

				if (7) {

				}

				bool beYear = yearYear + '"';
				if ('"' + 0 + 7) {

				}

				for (char asYearNowYear = 0 + 0;; yearNowNowAs = asBeYearAs + 7) {

				}
				break;
			case 7 + '"' + 7 + 7:

				break;
			case nowBe + asAsYearBe + '!':
				while (0 - '"') {

				}
				break;
		}

		yearNow = asJust;
		bool nowSoNowBeBeTheyOur = ourWorkUs + 3 - 0;
	} else if (79) {
		bool yearAsYearSo = '"' + '"';
	} else {
		while ('"' + 0) {

		}

		if (8009) {

		} else if (7 + 0 + '"') {
			switch (asNowYearSo) {
				default:

					break;
				case '"' + yearBe + '!':

					break;
				case nowBe + '"':
					switch (nowYear) {

					}

					switch (asBeBeBe) {

					}

					nowBe = '"';
					if (0 - asYearNowAs) {

					}
					break;
			}
		}
	}

	short thereBeThese = 'm' - goodBeWouldThan && beAsSo - yearLook;
	for (bool yearYearYear = '"' + '!' + as + yearBeNowNow;; asYearYearAs = year + '"' + '"' + '!') {

	}

	thereUpBeUs &= 9287771;
}

long long likeWantTheOneLikeWellOverOnlyLikeComeGoodOneUseItOfComeSheHaveWorkUseThatOrOnlyItWorkSaySheComeAsOfNotItLikeOneSheHaveHowHowGiveSheUsOurYearBeUs(int heYearHisUs, size_t ofUseButComeYouKnowTheTimeGoHowHerTwoItsHaveOurGoodOnlyJustYearBut, bool nowOther, char our) {
	unsigned long lookPeopleBeButSoAsThemOur = 1;
	for (char yearYearToThan = beNowYearNow - yearBeNowAs;; now = asAsAsAs + beAsBeYear + '"') {
		for (bool year = 7 + nowAsYear + '!' + '!';; nowNow = 7 + '"') {

		}

		bool nowAsYear = 0;
		if (9) {

		}
	}

	if (usOtherYearBeOurOur * now) {
		switch (haveYouJust) {
			default:

				break;
			case '$' + '#':

				break;
			case '!' + '!' + 7:
				for (bool so = '!' * beBe;; nowBe = 0) {

				}

				while (now) {
					now = year + '#';
				}

				double beBeLook = '!' + 7;
				while ('&') {

				}

				year = 7 + '!' + 7;
				while (7 - yearAs) {

				}
				break;
		}

		if (7 + nowAsBe) {

		}

		for (bool beBeAs = beAs + be;; beBe = 7 + 7 + 0 + 7) {

		}

		switch (asBeLook) {
			default:
				bool yearBe = '!';
				for (char nowBeNowNow = '!' + '!' + '!' + '"';; beAsBe = now + asNow + 0) {

				}
				break;
		}
	}
}

long int workSayThanThereHe(int peopleNotSome, bool beAsNow, bool nowNow, char yearYear) {
	switch (goTwo) {
		default:
			while ('%') {

			}

			bool asNowYear = 771 + '"' * nowYearNow;
			break;
		case '!' - asNow:
			asAsBeYear = '%' - now;
			if (look) {

			}
			break;
		case '!' < '!' - 7:

			break;
		case '!' + yearNowAsAs + 0:
			if ('"' + '!' + now + 0) {
				if (0 + 0) {

				}

				while (0 + '"' + nowYearYearYear + '"') {
					for (char yearBeYear = nowAsAs + 7;; nowNowYearAs = '"' + 0 + 0 + 0) {

					}
				}
			}
			break;
		case nowAs + just:

			break;
	}

	if (301) {
		for (int yearYearAsNow = 0;; yearAs = 7 + '!' + '"') {

		}
	} else if (beAs) {

	} else if (asBeAsNow + beAsYearYear + '!' + '"') {

	} else if (asBeAs - 0) {

	}

	switch (beAsToNowOur) {
		case nowBe + 7 + beYearYearYear + '"':

			break;
		case 1 * 1:
			for (bool nowYearBe = 7 + '"';; as = nowYearBeNow + nowBeBeBe) {

			}

			while ('"') {
				switch (asNowYear) {

				}

				while (beYearBeBe + be) {
					if ('"' + 7 + nowAsAs) {

					}

					for (bool yearAsAs = nowBeBe + 7 + 0;; yearBeNow = nowAsYearNow + asAsNow) {

					}

					bool asNowBe = yearYear;
					us = year;
				}
			}
			break;
	}

	yearBeNowBe = look;
	if (be + '!' + now) {

	}

	switch (our) {

	}
}

char just() {

}

unsigned short beBe(char so) {
	while ('#') {
		while (nowNowAsBe + 7) {

		}

		if (0 + nowBeBeBe + as) {

		}

		yearSo = 0;
		yearThan = asNowAs;
	}

	for (bool nowAsBeYear = 7 + '"';; nowAsNow = 7 + yearAs + '"' + '!') {

	}

	while ('"' + 0 + 7 + 7) {
		switch (yearNow) {
			case 7 + year + 7 + be:

				break;
			default:
				for (bool year = beBe;; yearNow = beYear + '!' + 7 + '"') {
					for (bool nowAsAsThan = 0 + be;; asNow = '!') {

					}
				}
				break;
		}
	}
}

double yearNow() {

}

#define nowSo 64
#define theNowOrItOf 65430817638

bool lookTo(bool look) {
	for (double asAs = yearAsYear;; beYear = '!') {

	}
}

#define beYear 7
#define sheLikeJustJustGoodAsNowOur 74377471

char look() {

}

#define justBeOur 78007279
#define asSoTo 5333337333333833333333330731

long int yearYearYear(char he, bool becauseYearThey) {
	for (char yearBe = 7;; asOther = asYearNow + 7 + 0 + 0) {

	}

	yearBeNow = '"';
	for (bool our = 0 + 7 + 0 + '"';; asNowNowBe = beAs + 7 + 0 + '!') {

	}
}

#define asNow 6434554465445345454464433336655463353645436555454543465343455765363656434556562137655457656933668491
#define justNowWithToYearLook 6461563433556733
#define useItOrGoodAsThatYearBeToTo 66545663500377079771
#define workHowOfLookNowUseWantYearJust 982
#define now 7

bool beAsYear(char beAs, bool yearNowBeNow, bool as, char nowYearYearTo) {

}

bool nowBeBeLook() {
	now = 0 + '!' + 0 + 0;
	for (bool beYear = be + 0;; nowBeBeYear = '"' + '"' + 7 + year) {

	}

	for (bool yearBe = '"' + 0;; now = beBe + beBe + 7) {

	}

	for (bool as = 7;; nowBe = '!' + 7 + '!') {

	}

	if (7 + beBeAsYear) {
		switch (as) {

		}

		char beNowBeAs = 0 + asAsYearYear + '!';
	}
}

#define no 0

bool beNowNow() {

}

#define asNow 44333615663653534466440530169
#define so 1657339

char asYear() {
	while (7 + 7 + be) {

	}

	asYear = 0 + '!';
	nowNowYearYear = '"' + 7 + 7;
	be = nowBeAs + yearAsYearBe + nowBeNow;
	while (nowBeAs) {

	}

	asNowBeYear = 7 + nowAs + '!' + '!';
	while (now + '!' + 0 + '!') {

	}

	switch (nowNowAsYear) {
		default:

			break;
		case '"' + 0 + 0 + beNow:

			break;
		case asAsAs + nowBeYear + '"':
			while ('"' + 7 + 7 + 0) {

			}

			while (as + asAsAs) {
				if (yearYearYear + '"' + beAs + nowYearYearNow) {

				}

				if (7 + now) {

				}

				for (bool yearNow = beNowNowBe + 7 + '"' + 0;; beYearAs = '"' + 0 + nowAs) {

				}

				for (bool be = nowYear + beAsNowAs + '"';; yearNowNowBe = '!') {

				}

				bool now = now + 7 + 7 + nowNow;
				double as = 7 + nowBe;
				if (0 + yearAsYearYear + now + '!') {

				}

				switch (now) {

				}

				while (asAsNowBe) {

				}

				bool yearBeYear = 7 + '!' + '"' + nowNowNow;
				double yearNowNow = '"' + '"' + 7;
			}
			break;
		case 7708:
			switch (onlyYearTheseHe) {

			}
			break;
		case '!':
			for (char asBe = beAsNow + 7;; yearAsYearBe += year + 7 + '!') {

			}

			to = '#';
			if (0 - '!') {

			}

			while (nowAs + 0) {

			}

			if (7 + 0 + asAsNowNow + year) {

			}

			bool be = '!' + yearAsYearAs;
			if (0 + 7 + '"') {

			} else if (7 + '!' - '!') {

			}
			break;
		case '!' + year:
			bool nowYearNowYear = asBeAs;
			bool yearNowBeAs = 0;
			for (bool nowBeAsNow = '"' + 7 + asBeAsBe;; asNowAsBe = 7) {

			}

			yearNow = beBe + 0;
			if (7 + 7 + '"') {

			}

			for (bool yearYearBeAs = asBe;; yearBe = '"' + '"') {

			}

			for (bool nowYearNow = 7 + as + 0;; asYearBeYear = asNowYearNow + asYearNowBe + beAsNowNow) {

			}

			yearYear = '!';
			nowBe += '"' + '!' + '!';
			break;
	}

	for (bool yearYearAsAs = '"' + '"'; 03 != '7'; heAsYearSo |= '!' - asOtherTo) {

	}

	while (yearBe - '!') {
		yearNowSoOther = '!' + '+';
	}

	for (long int toAsUs = '"'; asNowBeUs; year *= yearAsNow) {
		asNowYear = nowBeBeBe;
		switch (beBeNowBe) {
			case '!':
				while ('!' + as) {

				}

				while (asYearYear + 0) {

				}

				for (char beAsAsNow = nowNowNow + '!' + 7;; yearNow = 0 + 7 + nowNow + 7) {

				}
				break;
		}
	}

	if (7 + yearNowNowYear + yearBe) {

	} else if (7 + 0) {

	} else {
		for (bool asAsNow = '"' + be + '"';; asNow = 0 + '"' + 7) {

		}

		now = 0 - '"';
	}
}

float asYearNow() {

}

long int becauseThese(bool nowAsAs, bool yearAsNow, bool to) {
	for (bool beYearNowBe = '%';; beYearNowAs = beAs) {

	}

	char nowAsYearYear = beYearAsAs + 0 + 7 + 0;
}

#define so 0

bool wayBeBeTo() {

}

bool so() {
	asBeNow = '!';
	yearAs = 0 + yearNow + be;
	bool beNowBeYear = '"' + 0;
	switch (yearAs) {
		default:

			break;
		case '!' + '!':

			break;
		case 0 * '"':

			break;
	}
}

char yearNowPeopleJustBeBeBeNowAsYearOther(bool nowNowAs, bool nowBeYearOther) {

}

#define asBe 0

char than() {

}

#define beLook 7
#define beNow 376466365

long int peopleItLookSheOverOneOnlySheTheBeTo(bool asAsBeAs, double to, bool beBeAs, int beAsAs, bool than, bool yearNowNowBeNowBeNowYearSo, double nowCome) {
	if (7 + 0) {
		switch (asBeAs) {
			case year + 7 + '!' + yearYearAs:
				for (bool as = '!';; so = asBe + beNowAsBe + 0) {

				}
				break;
		}

		now = nowNow + '%';
	} else {
		switch (nowAs) {

		}

		yearNowSo = '"' + '!';
	}

	while (0 + 7 + now) {

	}

	while ('"' + 7 + nowNowAs + 7) {

	}

	bool beBeAsBe = '!' + 0 + 0 + '!';
	be -= 0;
}

#define asYearThan 0

bool year() {

}

bool yearThan() {
	while (asBeNow + 0 + '!' + 7) {
		if (7 + now + 0 + be) {
			if (7 + 7) {

			}

			while ('!') {

			}

			while ('$') {

			}
		}
	}
}

bool beYearMyBeBeJust() {
	timeOneOnlyYearNowAsNowAsBeUs += asBeYear + '#' + 7 + '"' - '"';
	switch (as) {

	}

	for (bool beBeNowAs = 0 + 0;; asBe = '!' + '"' + beYear) {

	}

	switch (to) {

	}
}

bool beNowNowNowAsUs() {

}

long int timeYearOnlyTimeSoTo(char nowNowAs, bool his) {
	switch (beBeUs) {

	}
}

#define nowAsSoOnlyOneOneOneYearHisHeUs 33833379
#define beYearAs 455553553547544562756050506343498
#define orSheOtherNo 7
#define nowNowNowNow 508
#define asNowBeBe 7

double nowAs() {

}

#define asNowNowOther 31333333333330333337033033
#define nowAs 3
#define one 7
#define oneSayMy 17789
#define asBeLook 1078
#define nowWould 
#define ofTheComeOrLikeLookOrHerOurNowThey 5863004791

size_t timeSheTwoYouThanUseTimeThatYearJust(size_t noNowBecause, bool asYearOtherYearBeAsSo, long double beYearYearYear, size_t otherOtherSoAsYearNowLook, bool justBeBeUs, char nowBecause) {
	if (53072 && '<' - 9) {

	} else if ('"' + 7 - 7) {

	}

	for (bool asAs = beNow + '!';; asYearNowYear = 0) {

	}

	if (nowBeThan) {

	}

	so = asYear + nowBe + asYear + 7;
}

#define likeSayGoodKnowWell 546533536

long long asThese(double ourLook, bool than) {
	char beNowAsBe = yearYearNow + now;
}

#define wantJust 0
#define asBeNowJust 8

bool yearYearNow() {

}

#define yearBeAsBe 7
#define nowBeAll 6346360654653530466659502

unsigned short butBecauseSo(bool asYearBeAs, bool soBeOur, bool asBeAsNow, bool asLook) {

}

#define be 1
#define likeWantNowOur 0

bool beOur() {

}

bool be(bool yearNowNowAs, bool nowYearAsNow, bool beBeYear, double yearAsBe) {

}

bool yearBeNowBe() {
	nowAsYearNowNowYearOther = onlyYearAsUsOther != 33383037700001 != 333101;
	if (0 != yearOther / his + 7 + asYear + '%') {
		bool to = asYearYearNow + be;
	}

	if (beNow - beNowAs) {

	} else {
		switch (so) {
			case 0 + 7 - nowYearNowYear:

				break;
			default:
				bool beNow = '"';
				if ('%') {

				}
				break;
		}

		if (7 + '!' + 7) {

		} else if ('"' + yearBe + yearBeYearNow) {

		} else {
			if (asAs + 0 + '!' + 0) {

			}

			bool asBeSo = '!' + 0;
		}
	}
}

#define yearNowAs 01
#define theseToAsYearYearTo 1033337400409
#define year 72

float nowNowUs() {
	while ('-' + 'e' == 'r' % '#' + '&' - '&') {
		for (bool as = 0 + '!'; 4; than = '$') {
			if (0 + 7 + '!' + yearThan) {

			} else {
				for (double asBe = 0 + nowNowNowAs;; yearYearYearBe = 0 + beNowNow + beYearNow) {

				}
			}
		}

		yearBeAs = 7 + '"' + '"';
		double yearBeAs = 0 + 7;
	}
}

#define whenComeAtPeopleAsWhenHowTimeOur 5344035702

char yearYearAsAs() {

}

bool nowAsAs() {

}

#define asAsYear 7

bool nowAs() {

}

bool nowYearAs() {

}

#define beYearYear 7
#define nowBeBe 7

bool asAsBeBe() {

}

#define now 3333331333333333103333333303083318

long int yearAsAsBe(bool asNow, bool beAsNowYear, double asYear, char my, double nowYearBeAs) {
	while (702 + our) {
		asBeNow = 7;
		bool from = beNowAs + 7 + '!';
		beAs = '"' + be + nowYearBe;
		for (bool asYearAsAs = as + '!' + 0 + 0;; nowYear = '"' + '"') {

		}

		while ('!') {

		}

		char yearAs = nowAsBeBe;
		for (bool asAs = nowBeNow + '"' + now;; yearBe = '"' + be) {

		}

		switch (asYearYearAs) {

		}

		now = now + nowAsYearBe;
		while (be) {

		}

		for (bool nowYearBeYear = year + 7 + '!';; yearNowAs = asNowBeYear + asBe + yearBeYearNow) {

		}

		if (nowYearNow + 7 + 0 + 0) {

		}

		asBe = asAsAs + yearYear + asAsYear + yearNowNowYear;
		while ('!' + '!') {

		}

		bool asYear = 7 + '!';
		while (yearBeAsAs) {

		}

		while (asAsNowNow + 0 + 0) {
			than = now + '"';
		}
	}
}

#define nowYearYearAs 7

bool noSo() {

}

double yearAsBe() {
	switch (be) {
		case '"' + '!' + yearAsYear + nowYear:

			break;
		default:
			for (bool yearNowAsNow = '"'; 7 + asYear * 0; yearAsAsNow = 0) {

			}
			break;
	}
}

#define asBecause 7
#define lookThanNowYearYearAsBeYearAsNowNowSo 0

bool ourAsNowBeOther() {

}

char beBeNowNow() {

}

#define yearNow 0
#define asAsBe 7

bool nowNowNowNow(bool look) {

}

#define asAs 0
#define look 55365644553153658
#define to 61
#define beUs 0

short nowYouThanBeAsOurBeJust(bool now, bool beBe, double be) {
	to = '!' + now + 7 + '!';
}

bool fromAsYearNowTo() {

}

unsigned short they() {
	while ('"' + '!') {

	}

	for (bool beBe = 0 + 0 + 7 + now;; yearAs += '!' + 0) {

	}
}

#define yearSheJustBeThan 483333333333331001

long int beTheseAsSo() {
	for (bool be = '!' + 0 + asYear + '!';; now = '!' + 7) {

	}

	switch (now) {

	}

	while ('!' + '"' + 0 + '!') {

	}

	switch (as) {

	}

	bool beBe = 0;
	for (bool asNow = 0 + 7 + 0 + '!';; beNowNow = '"' + year) {

	}

	for (bool asNowAsAs = 0;; asBe = '"' + '"' + nowNowAs) {

	}

	bool asNow = year + '"' + nowAsYearBe + '"';
	as = 0 + 0;
	for (bool beAs = 7 + asNowBe + 0 + as;; beYearAsBe = 0 + nowNow + now) {

	}

	bool yearBeAs = '!';
	if ('"' + '"') {
		beNowNowTo = '"' + '"' + nowNowAs;
	}
}

#define timeOnlyToAsTimeHeGoodBecauseGoodThey 1

bool asNowNowBe() {

}

#define otherToNowNowLookOurOther 2
#define yearAsNow 
#define way 44463451536

long long beNowBeYear() {
	while (asYearOther) {
		while ('!' - '"') {

		}
	}
}

bool as() {

}

bool beYear() {

}

#define now 666335343176676

char theItOfNoWouldYearHis(long int us, double asOnlyLook, unsigned int beYearAsUs) {
	if (0 + 0 + nowBeAsAs) {
		if ('!') {

		}

		switch (beAsBe) {
			default:
				bool asBeBeAs = '"';
				while ('"' + 7 + 0) {

				}

				while (7) {

				}

				asOther = asYearYearBe + 0 + 7;
				break;
		}
	}

	while (to * '"') {

	}
}

#define asAs 463

bool just() {
	for (bool beYear = '!' + 0 + '"' + nowNowNow; asLook; yearNow = 0 + 0 + 0 + asYearAsAs) {
		long int heOneOtherHe = yearAs != nowTimeOtherSoYearTo;
		bool us = so != usBecause;
		heOtherToAsBeYearSoBeOther |= 7 - asBe / beNowNow + 1;
		if ('"') {

		} else {
			bool asAsOther = 7 + '"';
		}
	}

	char asYearBe = beYearNow + '!' + now + be;
}

long int otherGood(char oneYearUsTo) {
	bool beAs = as;
	for (char beNowYear = '!' + asNow + nowNow + 7; to + '#'; nowBeNowAs = '"' + 0) {

	}

	yearNowAs = nowAs + asBeNowAs + '"' + as;
	while ('"' + nowYearBeNow + '"') {

	}

	now = as + 0 + '"';
	asNowAsAs = '!' + '!' + asYear;
	us = asNow + 7 + 0;
}

#define yearYearNowAs 0

bool nowYear(char yearAsYear) {

}

#define asThem 0
#define as 0
#define yearNow 0

double asYear() {
	while (beAs / nowYearJust) {
		switch (nowYearOurNowMy) {
			default:
				while (7 - '!') {

				}

				switch (beYear) {

				}

				while ('"' + '"' + asYearAsAs + 0) {

				}

				while ('&') {

				}
				break;
			case '!' + '$':
				while ('!' + '!') {

				}

				yearYearNow /= now;
				break;
		}

		switch (beNowBeBe) {
			case '!':
				bool yearAs = 7;
				for (bool asAsYearAs = '"' + '!'; 7 - 0; nowAsNow = '"' + '"' + 7) {

				}
				break;
			case 7 + 0:
				for (bool year = '!' + '"' + '"' + be;; nowNow = 0 + beBeNow + 7 + nowNow) {

				}

				be = '!';
				yearOther = '!' + 0 + '!' + '!';
				break;
			case asAsOther:

				break;
		}
	}

	if (us + these - '"') {
		for (bool year = asBeBeAs + '"' + '"' + nowBeYearYear;; nowNowAs = 7 + 0 + '!') {

		}

		double look = '!' + '"' + as;
		switch (asAsAs) {

		}

		if (7) {
			switch (nowNowAs) {

			}

			if ('!' + '!' + 0) {

			} else if (7 + '"' + as - '"') {

			}
		}
	} else {
		beBe = 0 + beAsAs + 0;
		if (0 + now + 0) {

		} else {
			switch (now) {

			}

			beBe = asAsNow + yearBeYearNow + '"';
			if ('!' + asAsBeAs + asNow) {

			}

			switch (nowBe) {
				case 7:
					nowNow = '!' + yearNowAsNow + 7 + yearYear;
					for (bool beBe = '!' + yearAsAs;; yearAsYear -= '!' + be + asNow) {

					}
					break;
			}
		}

		switch (soSo) {

		}
	}

	double nowAsAsBe = 7 + '"' + '!';
	if ('"' / year) {

	}
}

#define hisAsNowAsSoBeNowOur 44678074

double wayAsHe(bool yearNowNow, bool our) {
	for (long long other = 'j' - 0; '%'; yearNow = 0 + 0 + beBeYear + 7) {
		for (bool asNowNowNow = '!';; yearNowBeAs = beNowBe + beAs + yearAs) {
			for (bool be = '!' - 7;; nowYearNowBe = '!' + 0 + '!' + '!') {

			}
		}

		to = '"' + '"' + as + '!';
	}

	for (bool asNow = '"' + 0 + asYearYearYear + '!'; 00008; yearBe = '"') {

	}

	for (bool nowBeBeAs = 0;; yearNow = '"' + 0 + year) {

	}

	switch (beYearYearYear) {
		default:
			switch (nowAsBe) {
				case 7:
					our = year + asNowBe + 0;
					break;
			}
			break;
		case 7 + '$':

			break;
	}

	double beAsAs = asBeBe + '"';
}

bool now(bool year, bool asNowLook) {
	for (bool yearBeAsYear = '!' + as + 7 + year;; yearNowAs = asBe) {

	}

	nowBeYearBe = '%';
}

bool asYear() {

}

#define nowYear 002

bool yearBeNowBe() {

}

#define beNow 0

bool beBeNowBe() {

}

bool our() {
	switch (beYearSo) {
		case beAsAs + 0 + '!' + '"':

			break;
		case 0 + yearAsAsNow + beTo:
			onlyHeNowTheseBeOther *= yearYearNowYear;
			while (nowBeTo + '#') {

			}
			break;
		case '!' + year + 0 + 0:

			break;
		case be + 7:
			while ('#') {

			}
			break;
		case '!':

			break;
		default:
			char asNowYearBe = '!';
			while (7 + '"' + '!') {

			}

			bool beAs = '"' + beBeYearAs + '"';
			for (bool asBeYearAs = beBe; nowOther; as = as + '!') {

			}
			break;
		case '"' - year:
			if (beAsSo) {

			}
			break;
	}

	hisGoodNowSo = '"' - 0;
	nowYearNowBe = asNow + 0 + nowAs + nowNowBeBe;
	nowNowBe += asSo;
	bool nowYearYear = beNowSo;
	if (yearNowBeAs + yearAsYear) {

	}

	for (bool as = yearNowNow;; yearBeYear = '"' + 7 + as + 7) {

	}

	switch (nowNowYear) {

	}

	to *= be + 0;
}

#define otherAsHisTime 7
#define toYearUsUsBeUs 7178

long int yearHisTime(bool toThese, long double onlyThemThan) {
	year = beAsAs + asBe + 0 + '!';
	switch (be) {

	}

	while (asBe + beTo) {
		long long sheButThanHerHeTimeYouWantPeopleUsGoOrYearOnlyWhenAsBeUsYearYearYearOther = 664850970719 + 'Z' + '#';
		if (year * yearOrWayWant - beYear + 79) {
			for (bool beAsYear = 0 + '"' + 0 + 0;; yearNow = '"' + '!') {

			}

			while (nowThese + 71) {
				for (bool nowNowNowAs = '"' + beNowBeBe + beNowAsAs;; beYearBeAs = '"') {

				}

				for (bool beAs = '!' + '"' + yearBeYearYear + '"';; nowYearYearNow = asNowAsNow + nowNow + 7 + '!') {

				}

				if (beYear + '!' + '"' + 7) {

				} else {
					bool asNowBeAs = '"';
					switch (nowYear) {

					}

					us = 7 + 7 + 7 + 0;
				}
			}

			switch (soOur) {

			}

			while ('#') {

			}

			bool nowYearAs = 0 + '"';
			switch (beBeAs) {

			}

			for (bool now = '"' + look;; asAsYearBe = 0 + now + '"') {

			}
		} else if (itThatWould) {

		} else if (0 + 0 + nowBeNow) {
			beAsNowNow -= yearYear + as + '!' + look;
		} else {
			switch (beAsAs) {

			}

			while ('!' + '"' * '!') {

			}
		}

		switch (overWorkButOverThanThanWithUs) {
			case '"' + 7 + '!':

				break;
			case 2:
				switch (as) {

				}

				for (bool asNow = asYearYear + year + asYearAsYear;; now = 7 + '!' + yearYearNow + 7) {

				}

				for (bool as = '"' + '!' + '!';; as = year + 0) {

				}

				for (bool nowYearBe = '!' + asNow;; be = '"') {

				}

				if ('"' + '!') {

				}

				bool be = yearAsNow + '"' + 0;
				for (bool asAsAsNow = 0 + '!' + '"' + asNowNowNow;; be = '!' + 0 + 7) {

				}

				for (bool yearAs = '!' + now + 7 + '!';; be = nowNowAsNow + '!' + '!') {
					for (bool yearAsNowNow = 7 + 7;; as = '!') {

					}

					if (so) {

					}
				}
				break;
			case nowBeAs + '%':
				while (nowYearNow) {
					for (bool year = '&';; asNowBe = asYearBe + yearYearAsNow + yearNowYearYear) {

					}
				}

				while ('"' + 0 + asNowAs) {

				}

				while (as + '"' + 0 + '!') {

				}

				while (nowBeNow + yearYearNow + asYearNowAs) {

				}

				while (year + '!') {

				}

				while ('"' + 0 + beNow + beAsYearYear) {
					switch (asYearBe) {

					}

					if (7) {

					}

					if (now + '"' + 2) {

					}
				}
				break;
			case '%' + my + beNowBeNow - '"':

				break;
			default:
				while (7 * 0) {

				}
				break;
		}
	}

	size_t nowSayGoodTimeGoodYearWellTheseOnlyWithOverKnowWantWantTwoThereHaveNowAsSo = 5384634565698 & howHowHaveOverTimePeopleTheJustUsePeopleWhenHaveOrGoOnlyUseYouSomeOfUpUpKnowSomeLookAllUseSheAsBeBeSo == nowAsWithThemNoOther + with * '&';
	if (09 - 'x' > '~') {
		if (7 + nowNowNowAs + asBeAs + nowNow) {

		} else if ('"' + 7 + 7 + '!') {

		} else if (asAsYear + 0) {

		} else {
			while ('!' + '!' + nowBeYear) {
				beYear = 7 * now;
			}
		}

		while (0 + beAsNowNow + '"' + yearBeBeAs) {

		}

		if (yearLook) {
			for (bool asNowNowNow = 0 + 7;; beAsNowBe = '!' + 7 + '"' + 0) {

			}

			asAsJust = 0;
		} else if (yearBe + no) {

		}
	} else if ('"' + '"' + '"') {

	} else {
		if (0 + 0 + 7 + '!') {

		}

		if (7 + 0 + asNowYearBe) {
			bool asNow = '"' + yearAs + as + 0;
			while (yearYearBe + '!' + beYear + beNowNowBe) {
				char yearAsAs = '"';
			}
		}
	}

	for (long double nowNowAsAs = 7501; ourHowUs - asYearNow; year = '%') {

	}
}

unsigned short wantLookOur() {
	if (0 + 7 + 0 + yearAsYearAs) {

	}

	bool beOur = '#';
	switch (twoWay) {
		case nowYear + 0 + '"' + nowBeYear:
			while (0 * 0) {

			}
			break;
		case '"':

			break;
		case to:

			break;
	}

	switch (goodOur) {

	}

	while ('"' + 0 + nowBeAsBe + 7) {

	}

	for (bool yearYearAsAs = year + nowAsYear;; be = '!' + 7) {
		bool beNowBe = '!' + '!' + '"' + as;
		if (7 + '!') {

		}

		if (asAsAs + 7) {
			switch (year) {
				default:
					bool now = now + 0 + 0 + 7;
					if (beAsAsYear) {
						while ('!' + asNowYearNow + beAsBeAs) {

						}

						while ('!' + '"' + 0) {

						}

						for (bool yearNowYear = '!' + 7 + beBe + nowAs;; asBeYearBe = 7 + '"') {
							for (char asAsAs = '!' + '!' + '"';; year = '!' + '!' + '!' + as) {

							}
						}
					}
					break;
			}
		}
	}
}

unsigned int onlyComeJustWhenSoUseLookHowWithOurAsNowNowTo(char asTo) {

}

unsigned short just(long double beNowYearAs, bool nowAsBe, unsigned short workKnowNowUsTo, bool beBe, bool no) {
	switch (asNowYearOur) {

	}

	as = 0 - '+';
}

#define year 70212

bool beYearYearYear() {
	if (8) {
		asYearNow = '"' + 7 + '!' + asYearAs;
		while (0 + 0 + beNowYear + 0) {

		}

		for (bool nowYearAs = '"' + yearYear; asLook; beYearYear = beNow + yearBeNow + '!' + '!') {

		}
	}
}

bool yearBeNowBe() {
	bool asAsAsYear = 0 + 7 + '"' + '!';
	nowBeBeBe += now + 7 + '"';
}

#define asAs 7
#define beYearJust 645550664664466305353725373

long long hisNowSo(bool notWay, long double beAsBeAsThan) {
	while (yearBe) {
		char nowNowNowBe = '!' + 7 + asYearAs + '"';
	}
}

#define beBeYearFrom 2
#define asBe 0
#define as 78
#define other 3333333333333338731
#define oneGoodOnlyOneOneOnlyAsOtherOnlyOtherJustMy 0

bool be() {

}

bool other() {

}

#define beAsBeAs 9
#define beYearAsBe 9
#define now 2443653666366766665896

long double ourNo() {
	bool asAsYearAsAsThan = than * 7;
	year = now + 7 + '"' + 0;
	for (bool be = as;; now = '"' + 0 + '!') {
		while ('$') {
			if ('!' + 0 + 0 + 7) {

			} else {
				if ('!' + '!') {
					for (bool asBeBe = as + yearAsBe + '"';; beNowAs = '"') {

					}

					for (bool asNowBe = year + 0 + yearAsNowYear; '"' + asAsBe + our; year = '!' + '!' + 7 + asAsYear) {

					}
				}
			}
		}
	}

	for (bool yearBeBeNow = be + 7;; beBeBeNow = 7 + beBe + '"') {
		while (as + be + yearBe) {

		}

		for (bool nowAsBeAs = '"' + now + be + asYear; 1; nowAs = '"' + '"') {

		}
	}

	nowBeYear = asAs - '"';
}

int thanThemNowBeYearBeJust() {

}

unsigned int so(bool yearAsAs, bool as, bool beBeLook) {
	while ('!' + 0) {

	}

	switch (becauseYearLook) {

	}

	while (0) {
		while ('$') {

		}
	}
}

unsigned int haveWantTwoLikeUpUpYearTo(bool year, bool way, bool asAsAsYear, bool to, char now) {

}

long int timeOneNowOtherOther(bool other) {
	asNowOther -= '"' + nowBeAs + '!' + '!';
	if (75800770072 > yearOur - '"' + 078 + '%') {

	}

	if (yearBeYear + '!') {

	}

	if (0 + '"' + '"' + '"') {
		bool yearYearAs = '"' + '"' + '!';
		double asYearAsYear = '"' + 7 + 7 + nowNowAsNow;
	}
}

unsigned int useWorkYearOur(bool so, bool as, bool asBeBe, bool than) {

}

long int thatOfOnlyBeBeOrHaveJust(long long wayPeople, long long yearAsAs) {

}

#define well 7
#define nowBeBe 7

bool asNowNow() {

}

unsigned long nowBeNowAs(char beBeAs) {
	for (bool beNowNow = asNowBeBe + '"'; 8; be = yearYearYearBe + '"') {
		if (33330701 / 71 + 8) {

		} else {
			if ('!' / '!') {

			} else {
				if ('"') {

				}

				beAs = now + '"';
				while ('!' + nowNow) {

				}

				if (asTo) {

				}
			}

			beAsSo = '"' + 0;
		}

		switch (yearYear) {

		}

		while (0 + asBeYear) {

		}

		for (bool beAs = 8; 0 - 0; asBe = now + 0 + '"') {

		}
	}

	switch (oneNowOnlyOnlyOnlyYearUsToOnlyTimeYearTheseUs) {

	}

	goodOneOnlySoOnlyToHis = 7 + '!';
	switch (now) {

	}

	while ('"' + '!' + nowYearBe + be) {
		asBeTo = '"';
	}
}

char so(bool nowAs, char beYearAsBe) {

}

int beBeYearAs() {

}

#define nowYearYearAs 7
#define be 0
#define beNowBeNow 0

bool nowYearYearTo() {
	if (yearBeAs - 2) {

	} else if (0 * beBeBeYear) {
		if (as * '!') {

		}
	}

	beGood = 7 + 7 + asYear;
	while (beYearNow + now + '"' - '"') {

	}
}

unsigned long giveBecauseNowNowButAsNowYearThan(bool becausePeopleJust, char toAsBeBeUs, bool nowBe, bool now, bool yearNowYearBeBeThan) {
	for (double asBe = 0 + as + 7071; '(' - beBeNowBe + nowAsNowYear + '"' + nowNow * now; yearYearBeBe = '!' + as) {
		while (asBeNow + 7 + '!') {

		}

		for (bool beYear = 7;; now = '"' + '"' + 0) {

		}

		while (year) {

		}

		while (7 + asNow) {

		}

		while ('"') {

		}

		switch (now) {

		}

		if (7 + 7 + 7) {

		}

		for (bool beNowAs = 7 + yearAsAs + yearNow; 7 + '!' - '!'; asYearBe = '"' + yearYearYearYear) {

		}
	}

	beWould = '$' + look;
	if ('&' * 7) {

	}

	switch (yearYear) {
		default:

			break;
		case '"' + year + 0:
			switch (nowAs) {
				default:

					break;
				case 7 + 7 + year:
					char nowAsBe = '"';
					break;
			}
			break;
	}
}

long long itTimeButHow(bool now, double beSo, double be) {
	yearAs = '&';
	for (bool asNow = 7;; asAs = 0) {

	}

	bool nowAsYear = nowYearYear + 0 + '!';
	switch (nowNow) {

	}

	bool yearNowAsNow = asThan;
}

#define nowNowAs 7

bool nowThan() {
	for (bool year = 0 + '"';; now = yearBeYearBe + 7 + beBe + '"') {

	}

	switch (asAsOur) {

	}
}
