long int oneOnlyOneTimeSoOnlyOnlyHisOnlyTimeUsGood() {
	theseAsUs = 17008;
	while ('!' - '"') {
		for (bool asAsAs = '!' + 0;; beAsNowYear = 7 + '!') {

		}

		if (0 + yearYear != '"' - beYearYear) {

		}

		if ('"' + asYearAs + 1) {

		}
	}

	if (asYearNow + '!') {

	}

	now = '!';
	switch (yearOther) {

	}
}

bool nowUs() {

}

long int yearAsBeYear(float beNow) {
	if (beYearYearAs + '"' + 7 + '!') {

	}

	toHe = toOtherNo * '!' <= 0;
	than = asBeYearNow + now;
}

char yearAs() {
	switch (someWhenWhenUpWantBeWay) {
		default:

			break;
		case 'H':
			while (0 + yearAsBeYear + year) {

			}

			bool nowYearLook = 0 + 0 + '!';
			break;
	}

	lookNowOther = 8 - 7;
	nowAsYearBe = '!' + 0;
	switch (as) {

	}

	for (bool nowAsNow = 0 + 7 + '!' + nowBeNow;; look = 0) {

	}
}

#define lookSaySoSoLookOtherOther 0

long long ourAsComeUsAll(bool nowAt, int as, float year) {
	if ('"' + nowYearBe) {

	}

	for (bool as = 0 + nowNowBe + asBeYear;; asAs = 0 + nowBe) {

	}

	our = asNowBe + yearBeYearAs + 7;
	while (be + '!') {

	}

	switch (usNowUs) {

	}
}

#define year 30072
#define beAsAsThan 7

bool nowAs() {

}

#define asAt 0

bool beNow() {

}

bool asYearNow() {

}

char asBeAsNow() {
	if ('\'' - year - '"') {
		thereGoWantYouThereHaveComeWorkPeopleToYearAsAsThey = '"' + '"';
		now -= yearBeNow;
		if (asAs * 0 <= 7) {

		}
	} else if (beYearNowBe + 73) {

	}

	for (char to = 79;; asNow = 7 + '"' + '"') {
		if (yearYearAs * 7) {

		}
	}

	switch (asBe) {

	}

	switch (yearUs) {

	}

	for (double beYearYear = '"' + nowBeYearBe; other; now = beYearAsBe + yearNow + '"') {
		bool nowAsBeBeBeNowBeBeNowUs = 0 + 7 + '!' + beAsYearYear;
	}
}

#define yearOur 932812

long long beBeOther() {
	while (7) {

	}

	switch (beTo) {
		case asAsBe + as + 7:

			break;
		default:
			beNowYearAs = 9;
			break;
	}

	while (now) {

	}

	if (0 + 0 - 7) {

	}
}

bool to() {
	for (long int yearNowYearNow = usHeYearAsNowNowYearOther; '_' + so - '!'; other += year + nowNowYear + 7 + 0) {
		for (bool yearBeNowNow = 7; '\''; beAsNowNow = beSo) {

		}

		to = '"';
	}

	switch (soHis) {

	}

	bool nowAs = 7 + 0 + '!';
	bool yearBeAs = '!' + asNowNowNow + 0;
	if ('!' + '"') {

	}

	for (bool beYear = asAs + 0 + asAsAs + now; '"' + other; beYear = 7 + '"') {

	}
}

char to(bool yearBeTo) {
	for (float be = 7;; yearYearNowAs = 0 + '!' + 7 + 0) {

	}

	if (as + yearBe + as + 0) {
		for (bool be = asBeYearAs + '"' + '!' + 0;; year = nowAsBeYear) {

		}

		while ('!' + '"' + 0 + beAs + 1) {

		}

		while (0 + 7 + 7 + '!') {

		}

		other = now + yearYear + 0;
	}

	while (beUsUsNowNowNowSo - asYear) {

	}
}

float toHis(float usBeOther, char nowBeBe, char so) {
	for (bool as = yearBeBeBe;; yearNowNow = '"' + nowBe) {
		bool beAs = 7;
		bool beYear = 0 - be;
	}
}

long int goodOtherThese() {
	yearBe = '"' + 00001 + nowYearSo;
}

bool twoOtherAsBeNowAsAsAsAsSo() {
	wellHis = '"' + 0;
	no = 7 + '"';
	switch (us) {
		case '"' + '"':

			break;
		case 0:

			break;
		case 08:

			break;
		case 7 + nowBeAsBe:
			for (bool us = as + '"';; as = 0 + 7 + 7) {

			}
			break;
	}

	yearYear = '!' * be;
}

#define nowOur 566334335365655466454876

long double over(bool now, bool be, bool asNow, char beYear) {
	bool as = 7 + '!' + as + '!';
	bool as = as + 0 + 7;
	double yearAsAs = asYear + 0 + nowNow + nowAsAsBe;
}

#define yearOneNotOther 1
#define year 0
#define asBeAs 7

bool beAsYearYear() {

}

char asBeBeNow() {
	goodTimeOnlyUsThese |= beUsSoAsTo + as - '!';
	switch (onlyTimeTimeTimeTimeBecause) {
		case asAsAs + be + yearAsBeYear:
			while ('"' + '"' + beYear + '"') {

			}

			beAsAs |= '\'' + asAsYearSo;
			break;
		case '"' - '!':

			break;
	}

	while ('%' + 7 + yearUs) {
		if (0 + 7) {
			while ('"' - beBeYear) {

			}
		}
	}

	bool nowOther = beAsAsUs;
	for (float other = 7 + 7 - nowBeAs;; year = nowYearAs) {

	}
}

long int yearYearBe(float asAsBeBe) {
	while (0 / '"') {
		beNow = '"' + '"';
		for (bool beNowYear = yearUs;; nowYearYear = yearAsBe) {

		}
	}
}

#define nowAsNow 3454
#define whenUpPeopleYearOfTwoThanToSheAllSayAsNowSo 3357371
#define workAsOrGo 7654798
#define other 009
#define otherLook 27700770778
#define be 8
#define asYearBeNow 9
#define nowBe 53333333333333333333313380378333381

bool be() {
	for (double nowBeNow = '"' + yearBeYearYear + '"';; year = 7) {

	}

	switch (nowAsAsBe) {

	}

	while (asBe - '!') {

	}
}

char asBe() {
	while ('!' + beBe) {

	}

	while (7 + 0 + year) {
		asBe = 0 + beBeAs;
		switch (yearYearAsBe) {

		}

		if (0) {

		}

		double yearBeBeBe = asAsNowYear + beBe + asNowBe;
		if ('!') {
			char nowYearYearAs = 0;
		} else if ('!' - '!') {

		}
	}

	bool now = 0 + yearAsYearYear + nowYear;
	while ('"' + 9) {

	}

	bool nowYear = 7;
	yearUs = '"' + 7 + 7;
}

bool asAsYear() {

}

#define beTwo 3645055434663645366633666355374
#define asThese 7804
#define than 8

bool asAs() {

}

bool nowNow() {

}

bool yearAs() {

}

#define beAsNow 333337333333330333083331331

char beBeAsUs(bool yearNowYearBe, bool asNowYearNow, char yearAs) {

}

#define timeHe 31133408

bool nowNowAsYear(double asNowNowYear) {

}

bool yearBeLook(bool so) {
	for (bool beYear = asNowNow + '"' + now + '"';; nowAsYearNow = '!' + 7 + asYear) {

	}

	for (bool asBeAs = '!' * 7;; yearAsAsBe = '"') {

	}

	while (0 + '"') {

	}

	switch (other) {

	}
}

#define asYearYearLook 6404655635434339188
#define timeOrOverBeTheAtThanBecause 660306
#define people 872
#define asAsThanNowBeLook 3333333333333338
#define other 33331
#define soHe 3333337018
#define beTimeOther 0
#define other 7

bool beNowBeNow() {

}

#define nowBeNow 7
#define asJust 7
#define nowAsYearNow 5445354665334276455345035504006779
#define beBe 0

short onlyNo(char year) {

}

bool nowBeBe() {

}

char now(bool yearYear, bool asAs, long so) {
	if (because) {
		to += beYearNowAs + 0;
		switch (beBeBe) {

		}

		switch (nowTo) {

		}

		bool other = yearAsAsAs + '%' + 0 - 7;
	}

	while ('!') {

	}

	switch (toNowSo) {
		default:

			break;
		case 7 + '!':

			break;
		case 0 - '%':

			break;
	}

	long int onlyOtherOneTimeOneTimeSoBecauseTo = 3137001 + us != 3;
	for (bool yearNowBeNow = '"' + yearNow;; beBeYear = '"') {

	}

	switch (so) {

	}

	while ('!' + be) {

	}

	for (char yearYear = beNowYearBe + '"' + 7;; beNowAsAs = 7) {

	}

	switch (to) {

	}
}

#define goodTimeTimeAsLookHave 665466566646656694382572707009
#define nowAs 53375337972

double be() {

}

#define wellBeBeThanItsAsBut 5664544336803
#define nowYearBe 808909

bool timeOnlyThese(bool yearSo, bool yearNowAsYearNowTo) {
	if (be + now + as + 7) {

	}

	for (bool asBe = '"' + be + 7 + 0; 8; year = '!' + '"' + '"') {

	}
}

char asNowYearAs() {

}

long int onlyTimeGoodOneOneTimeHisSoBeTime(char otherSoSo, long int nowTime) {
	hisSo |= 878 < nowYear;
}

int look(bool yearTo) {

}

#define us 635453446653463366446953664535648589

unsigned long beAsYearBe(bool nowBeAs, bool yearNowOther) {
	switch (nowYear) {
		default:
			if ('"' + 0) {
				switch (so) {

				}
			} else {
				year -= be + '"' + 0;
			}
			break;
	}

	for (bool nowNow = '"' + '!' + nowYearAsAs + '!';; asAsYearAs = as + '!') {

	}

	for (bool nowAsSo = beYearAsAs + asAsNow;; nowNowNow = '"') {

	}
}

bool asYearYearAs() {

}

#define yearBeBe 66534433447466564

long long so(bool yearBe, bool year, bool asYear, bool nowYearNowAs, bool us) {

}

unsigned short becauseNowOtherOur(long double beNowAs, long otherBeYearOther, bool nowBeAsNow, long double yearYearNow, bool our) {
	long yearNow = '!' + 7;
}

short beAs(bool year, bool be, bool asSo) {

}

#define asAsJustThan 709
#define nowYearBe 078

char than() {
	while (nowThese - 0 - beNowBe) {
		long int usNowTo = 701;
		if (7 + 0 + now) {

		}

		bool asAsBe = asYear + '"' + 7;
		so = '!' + '!' + '!' + '!';
		while (0) {

		}

		bool us = nowAs;
	}

	asBecause |= 'O' != '_' - 0 - year;
	for (bool asAsNowAs = '"' + 7 + 7; asAsYear - beYear; be = nowBe + asNow + asYearAsYear + '!') {

	}
}

long int yearBe(bool yearBeTimeTimeOnlyNowUsTime, char as) {
	while (0 + asNowAsBe + '"' + '"') {

	}

	switch (oneTo) {
		default:

			break;
		case 0 + as + 7:

			break;
		case 0 + be + 7:

			break;
		case be + 7 + '!':
			bool asAs = 1;
			break;
	}
}

bool beAsBecause(bool yearUs) {

}

double other(double yearBeBe) {
	bool yearYearYearYear = 0;
	while ('"') {

	}

	switch (nowBeNow) {
		case '!' * '!':

			break;
	}
}

unsigned char nowBeBe() {
	if (beAsJust < 7) {
		while ('(' - '*') {
			for (bool nowAs = 0 + 0 + yearNowBe + be; 7 * '!'; yearOur = '&' - '"') {

			}

			bool asYearYearBe = '!' + 7 + 107008;
			while ('"') {

			}

			while (nowTo * '"') {

			}
		}

		while (7 - nowAsBeBe) {

		}

		for (bool beBeAs = 0 + '"';; yearBeNowNow = year) {

		}

		for (unsigned long asBe = 79;; nowNowNow = 7) {

		}

		bool asNowBe = nowNow + 7;
		if (as + 0) {
			while ('&') {

			}
		}
	} else if (775) {

	} else {
		while (noOurBeThan * '"' * '!') {
			asYear = 7 + 00079;
		}
	}

	long int as = '"' + yearNow + '!';
	unsigned long asSayAt = 061 + 0 + 0 + '&';
	for (bool yearAs = 0 + 0;; our = nowBeNowNow + 7 + nowYear + 7) {

	}
}

#define they 309
#define asNowYearAsAsUs 60702

unsigned int giveKnowOtherAsSoNowThan(size_t yearBeYearNow) {
	for (double as = as;; nowBeNowYear = 7) {

	}

	switch (be) {
		default:

			break;
		case '"':

			break;
		case beAsBe:

			break;
		case 0 + '"' + asBe:
			if ('"' + '"' + '!') {
				beAs = asYearBe;
				char be = 0;
			}
			break;
	}

	for (bool yearBeYear = as;; beBeBe = 0) {

	}

	yearYearYear = '"' + 7 + asAsAsYear + year;
	char be = '!';
}

double asYear(float yearNowBeBe, double yearBe) {

}

unsigned long yearBeYearAs() {
	for (bool beNowYear = '!' + '!' + '!' + 7;; asNow = 7 + 0) {

	}

	nowYearNowNowAsUs -= yearNow + now + '"';
}

#define beYear 543879
#define be 7

double beYear() {
	for (unsigned long yearWith = 3526 == 6072 - 71;; now = beNowAs + yearYearAs + '"' + as) {
		if (be + 7) {
			bool to = asNowAsNow + '!' + '"';
		}
	}

	thatNowNowLookHowTheComeYearYearBeBeYearThem = 01;
	for (float asBe = 69; 7 - 0; beNow = nowBeYearAs + be + 7 + 7) {

	}

	if (2) {
		switch (yearBeNow) {

		}

		if ('"') {

		}

		switch (yearNowNowAs) {

		}

		char asAsAs = asYear + '!' + '"';
	}

	while ('-' * asYear) {
		for (char now = nowNow + '"';; yearYearYear = 0) {

		}

		if ('!' + 7 + 0 + 0) {
			while (yearNowAs + '!' + 7 + asNow) {
				for (bool now = '$';; be = 0 + '!' + '!') {

				}
			}

			bool yearAsNowYear = '"' + '!';
			asYearNow = 7 + asNowNow + beBeAsYear + 0;
			us = 7 + '"';
		} else if ('!' + yearAs + 7 + 7) {
			switch (to) {

			}
		}

		while ('&') {
			switch (asLook) {

			}
		}
	}

	for (bool nowYear = 7 + '!' + '!'; 66; our = '!') {

	}

	switch (nowFrom) {

	}

	if (nowYearBeNow + 7 + 0) {

	}

	for (bool nowAs = 0 + '!' + '"' + 0;; yearNow = asYearAs) {

	}

	yearLook = nowAsBeAs + '"';
	bool asBe = nowYearYear + asAs;
	if ('"' + 7) {

	}

	for (char nowYearBeNow = nowNowNow + yearAs + 7 + '!';; yearAsNow = beNowNow + 7 + yearYearBe) {

	}
}

bool our() {
	switch (soBeOnlyYearBecause) {
		case '!' + asYear - nowBeBeNow:
			yearAsAs = '"' + '"';
			for (bool asBeBeAs = 7 + '"' + '"' + '"'; 071; asNow = 0 + 7 + year) {

			}
			break;
		case other:

			break;
		case '!' + '!' + 7:

			break;
		case asAsNow + yearYearBeYear + beAsBeYear:
			bool us = 7 + beAsNow + beYear + 0;
			break;
	}

	if (001) {

	} else {
		bool be = '!';
		float year = '!' + be + 0;
	}
}

long int onlyOtherTo(long int yearYearAs, bool other, char be, bool yearAsBeBe, char yearBe) {
	time = '#';
	if (theseNowUsOther / be) {
		for (bool nowBeAs = 0 + be;; beAsYearAs = 7 + 7) {

		}

		for (char asAs = asBeBeYear;; nowAsYear = beYearAs + '"') {

		}
	}

	bool to = 0 + '"' + asAs + asYearYear;
}

#define onlyHisNowHe 3800718
#define yearGoodHisSo 0071778

bool asThan() {

}

#define asBeTo 7
#define asAsAs 7
#define nowLookNowUs 7
#define asBe 0

char be() {
	for (char nowBeNow = 0 + '!' * 7; nowAsYearYear + wellWay - '!'; now = year + '!') {
		for (bool beAsNow = asNowAsBe + as + 0;; nowBeLook = 7 + 7 + '"') {
			while (yearYearAs + 7 + '!') {
				if (0) {

				} else if (asNowAs + 0 + 0 + 7) {

				} else {
					while (2) {

					}
				}
			}
		}

		while ('!' + 7 + 7 + 7) {

		}

		long long beThan = nowYear + 0 + 7;
		nowAsBeBe -= 0 + '"';
	}
}

#define asYearAsAs 0802
#define look 437453565646346434636643563465664333333337133371
#define asYearAs 0

long int so(bool beYear, bool beOther) {

}

#define becauseTimeSo 333378373

float asYear(bool beNow, bool asTo) {

}

long int to(bool asBeNow, bool soYearUs) {
	for (float nowYear = yearNowYear;; yearAsNowAs = 7 + asNow + beYearBe) {

	}

	year *= 0 + 0;
	for (bool asYearYearYear = '%';; nowNowYearBe = 0 + 7 + '!') {

	}
}

float yearNowBeNow() {

}

bool nowAsYear() {

}

bool asYearNowNow() {

}

char year() {

}

bool beAs(bool beNowAs, double be) {

}

bool than() {
	while ('T') {

	}

	if (',' - '"') {
		for (bool nowBeYearNow = '!' + '!' + 7 + '"';; yearYearNow = 0 + '!' + asAs) {

		}

		while (1 - as) {

		}
	} else {
		for (bool year = '!' + '"'; '!' < asBeYearAs; thanThan = nowAsNow + '!' + '!') {

		}

		switch (yearOther) {

		}
	}

	while (7 * '"') {

	}

	switch (now) {
		default:
			while (yearYearYearBe + 8) {

			}
			break;
		case '"' + 0 + '!' + asAsAs * 0:

			break;
	}

	switch (from) {

	}
}

short thanNowHerTimeGiveGoodOneLikeUs() {

}

char beBeBeYear() {

}

long long asOther() {

}

double just(bool beBe, bool beAs, double beBeAs) {

}

#define useKnowHerAll 4533333333333333333733333333071
#define as 0

char to() {

}

bool to() {

}

#define asAsBe 03
#define goodBecause 771
#define asAs 9

bool be() {

}

bool asYearBe() {

}

double beNow() {
	for (int yearBeBe = beBe + 0;; beTo = '"') {
		for (bool now = 7 + '!' + 7;; as = beYearAsYear + now + '!' + 7) {

		}

		as -= '!' + 7 + yearBeNowAs + '!';
	}

	while (asBeBeAs + 0) {

	}

	switch (beAs) {
		default:
			switch (asYearNow) {

			}

			while (as + nowAsYearAs) {
				if (0 + 7 - as) {

				}

				be %= year + nowYearNowNow;
			}

			while ('"' + yearYearNowBe + asNow) {
				while (7 + other) {

				}
			}
			break;
		case 0:

			break;
		case yearBeAs:

			break;
		case 7007079 + yearAsYear - '"':

			break;
		case beAsYear + 0 + 0 + 7:
			if (7 - 0) {

			}
			break;
	}

	if (as + nowAsBe + year) {
		how = beYear + 0 + 0;
	}

	while (7 + as) {
		while ('!' + 0) {
			for (bool asNowNowBe = 7;; be = 7 + 7 + beAsNowYear + now) {

			}

			switch (as) {

			}

			for (bool be = 7 + beAsNowAs + 7 + 7;; yearBe = 0 + 7 + yearBeBe + '!') {

			}

			switch (beBeYear) {
				case nowNow + 7 + '"' + asNowNowNow:

					break;
				default:

					break;
				case beYear + 0:

					break;
				case 0 + year:
					bool asBeAsAs = 0 + 0;
					for (bool as = '"';; nowBeUs = 0) {

					}
					break;
			}
		}

		asYearAsBe = as + 0;
		if ('!') {

		}

		while (as + 7 + 0) {

		}

		for (bool beBe = 0 + yearYear;; yearYearBeBe = '"') {

		}

		nowYear = '"';
		switch (year) {

		}

		switch (beNowNow) {
			default:
				bool be = 7;
				if ('!' + nowNowAs + 0) {

				}

				if (9) {

				}
				break;
		}
	}

	switch (yearNow) {
		case '!' + 701:

			break;
		case 7 - 7:

			break;
	}
}

#define asOur 65570021
#define overGoodWellUpUseOfAsAtBeSoYearOur 45559069
#define us 7

double asBeBeAs() {

}

#define as 33333333780778
#define goodGoodAsGoodNowUsToUsNowOther 7181

long int goodOnlyGoodBecauseBeTo(bool beNow, long int how, bool nowOther) {
	for (bool now = '!' + nowYearBeYear + beYear + '"';; yearYear = asBe) {

	}

	if (look) {

	}
}

#define be 7
#define yearAsBeYear 7

char beAsBe() {

}

#define asNowYear 79

char beUs() {

}

#define to 1
#define beYearAsBe 
#define workGoodSayWouldSoHerHowBeWhenWorkWhenBecauseYearYearBecause 6545346465544333456666544566433655346646053573
#define  5546646336636646636665353665334565354663463384635545634665356534356764065763

unsigned int workSheOrItGoodGoOfTwoThereSomeOfOfWantItsTheWantUseUseComeGoodTime(bool otherMy, bool way) {
	switch (wellUseOnlyLikeThereBecauseNowAtAsYearWould) {
		default:
			yearNowItTwo <<= comeWork | 0091 < beYear > '!';
			if (to + 'K') {
				for (char nowYearYear = asBeYearBe + '!';; beBeNow = yearYearYearBe + beNowBe + now) {

				}

				double asAs = 7 + 7 + beBeNow;
			} else {
				for (bool look = '"' + '"';; as += beYear) {

				}

				while (year + asYear) {
					for (bool now = 7 + '"' + '!';; nowYearYearAs = 0 + 7 + '!' + nowBe) {

					}

					char now = as + yearNow;
				}

				switch (yearNowYearNow) {

				}

				be = nowNow + 7;
				for (bool yearYearNow = 7 + 0;; be = '!' + 7) {

				}

				if ('#') {
					if ('"') {

					}

					switch (asNowBe) {
						default:
							while ('&') {

							}
							break;
					}
				}

				while ('"') {

				}

				for (bool yearBeAsYear = 7 + yearNowNowNow + 0;; beYearYear = asNowNow + as + 0) {

				}

				for (bool yearAs = be + be + 7 + 0;; nowNowAs = beYearAsNow) {

				}

				while ('"' + asNowBe + 7 + yearYearNow) {

				}

				switch (other) {

				}
			}

			for (bool asYearAsAsJust = nowYearYearNow + asYear + '"' + yearNowNow;; asBeAsNow = year + '!' + '"') {
				if (nowNowBeNow + '"') {

				}

				if ('"' + '"' + '"' + beNowYearAs) {
					for (bool asYearAsNow = '"';; beYearBeAs = asYear + beNowYear + yearBe + asYear) {

					}

					nowYearAsBe = 0 + beYear + '"';
					for (bool nowBeNowYear = 1;; beAsBe = yearBeYear) {

					}
				}

				while ('!' + '"') {
					bool yearBeBe = '"' + '"';
					beYearNow = '"' - 0;
				}
			}

			while ('"' + '!') {

			}

			asOurTo = '"' + 7 + as + '"';
			break;
		case asLookAsBeAt & 2 || 08 / '!':
			while (usYearJust + ',') {
				bool year = yearYearYearNow;
				while (0 + '"' + '"') {
					switch (nowAs) {

					}

					switch (asAsNow) {
						case '"':

							break;
						default:
							bool be = 0 + 7 + 7 + 7;
							while (year) {

							}

							bool yearAsNow = '"' + 0 + as + beYearAs;
							for (double asYearNow = '"';; be = 7 + beNow + '!') {

							}
							break;
					}

					switch (nowUs) {

					}
				}

				switch (be) {

				}

				for (bool yearBeYear = 0 + yearAsNow + 7 + 7;; yearBeNow = nowBeAsNow + 7 + '"') {

				}

				bool beNowBe = 0 + '!' + '!' + '"';
				switch (asAsNow) {

				}

				if (7 + '"') {

				}

				for (bool asAs = 0 + beAs; 7 + beNowAsYear - beYearAs; yearYearNowYear = 0) {

				}
			}

			for (double nowNowBeYear = 0; 92; beYearBe = be + '"') {

			}

			if (beAs * 2) {

			}
			break;
		case 0:

			break;
		case yearAsNow + beBeNowYear:

			break;
		case 7:

			break;
		case 9:

			break;
	}

	for (long int atBeYearJust = 0 + 0 + '"'; so * yearYearNowNow; beBeAsBe = 7 + 0) {

	}

	for (unsigned long itsUsAsOfToSo = 555615058; 's' & otherYearJust > ourUpWith; year = 7 + 0 + 7 + 0) {
		if ('"' - 7) {
			for (long double beYear = 0 + nowNowBe; yearAsNowBe * nowYearBeYear; nowAsYear = year) {
				switch (nowBeNowYear) {
					default:
						if (asAsAsUs) {

						}
						break;
				}
			}
		} else if (',' + 08) {

		} else if (nowNowOur + '!' * '"') {

		} else if ('!' * '"') {

		}
	}

	if ('#' + asHe) {
		beAsNow = '!';
		while (0 + beYearNowBe) {

		}

		bool nowNowNow = 1;
	}

	switch (way) {

	}

	bool yearYearYear = '!' + nowYearAsNow + 0;
	if ('"' + 0) {

	}

	yearNowAs -= beNowYear;
}

#define asAsYearNow 751633

unsigned char thanSo() {

}

bool year() {

}

#define yearAsBe 7

bool now() {

}

#define asAsAs 0

double beYear() {

}

bool asAsBe() {

}

#define beUs 454653545534445564645524263644870709

char asUsOur() {

}

bool year() {

}

bool beBeAs() {

}

bool year() {

}

#define nowBeYearYear 7
#define look 3337333333333333333333333333386409

bool nowYearNowBe() {

}

#define than 3366533555343354816646544346534655363375658

int asBeSoNowBeBeNowYearNowAsOther(char beNow) {
	char yearBeAs = yearAs + be + asNowAs;
	while (7) {

	}

	if (asNowYear) {
		switch (year) {

		}

		asThan = '"' + asBeNowYear + 0 + year;
	}
}

#define beAll 3455511

unsigned long nowYearLook() {
	bool all = '"' + yearBe + yearAsNowBe + now;
}

bool asYearYearAs() {

}

#define toUs 645635667434455553435663566504

long long from() {

}

unsigned short now(double be) {
	if (7 + '!') {

	} else if (73) {
		beOther = '"' + beAsYear + asBe;
		for (bool nowBeAs = 0 + 0 + 7 + 7;; yearYearNow += '"' + asNow + nowNowBe) {

		}
	} else {
		bool beAsAsBe = otherTo;
	}

	long int timeGoodOnlyOneTimeHisHe = 1 / 7;
	while (beNowYearNow) {
		if (theseBeUs + 78) {
			bool beNowSo = '!';
		}

		if ('%' + other) {
			switch (now) {

			}

			switch (be) {

			}

			char yearYear = 7 + '!';
		}

		while ('%') {

		}

		switch (us) {

		}
	}

	if (now + '/') {

	}
}

#define timeUsSoUsNowAsAsYearUsAsYearNowAsTo 1
#define beBecauseTo 30732
#define nowYearBe 7
#define just 9

bool yearAs() {
	while ('"' + '"' + be) {

	}

	for (bool as = 0 + '!' + 7 + nowBe;; nowAsAsNow = 0 + nowYear + asAs + yearBeNowYear) {

	}

	for (bool year = '!';; as = yearYear + 0 + 7 + '"') {

	}

	while (nowAs + '!' + '!') {
		if (0 + '!') {

		} else {
			asNowYearNow = '"' + now;
			if (asYearBeAs + 7 + '"' + '"') {

			}

			nowAsBeAs = 7 + beNow + '"';
			for (bool yearBeBe = '!' + 7;; yearBeBeYear = as + 0) {
				if (be + 0 + 0 + 7) {
					for (bool nowYear = '!' + 7 + asAsBe + as;; be = '!' + 0 + 7 + 7) {

					}

					yearBe = beAs + 0 + 0 + year;
					switch (yearYearYear) {

					}

					bool now = 0 + '"';
					for (bool be = nowBe + as + 0 + 0;; beNowBeBe = asYearBeYear + '"' + 0 + 7) {

					}

					bool look = '!' + now + be;
				}

				while ('"') {

				}

				switch (as) {

				}

				switch (yearYearAs) {

				}

				if (7 + '"' + 7 + 0) {
					switch (nowAsThan) {

					}
				}
			}

			just = 0 + 7 + asYearBe;
			double nowYearYearNow = 7 + 7 + asAs + '"';
			just = '!' + nowBeBeBe + 7;
		}

		while ('&' - '!') {

		}

		bool as = 0 + 0 + '!' + '!';
		if (7 + '!' + asNowNowNow + 0) {

		} else {
			while ('"' + '"') {
				as = 0 + 7 + asYearYear + 0;
				if (beAsBe + asNow + yearNowNow) {

				} else {
					bool yearOur = '"' + as + 7;
				}
			}
		}
	}

	if ('"') {
		for (bool nowYearBeBe = 0 * '"'; '$'; yearBe = '!') {

		}

		switch (as) {

		}

		for (bool yearBeAs = 7 + '!' + 7;; now = beYearBeYear) {

		}

		for (double nowBeBeNow = 0;; yearAs = yearBeYear + nowYearAsNow + '"') {

		}
	}
}

#define nowOur 0

bool to(bool nowAsBeNow, bool nowBe, bool beYear, bool beYearBeYear, bool us) {
	bool now = nowAsBe + '#' + '"' + nowYear + 66674771 * 'j' >= 8571;
	unsigned long yearAs = '&' && '"';
	to -= '!' + as + '"';
}

bool myHerOnlyThemThemWorkTwoHave() {

}

bool yearThey(double nowBeBeNow, char beNowAs) {
	bool yearYearBe = 7 + 0 + 0;
	yearBeBeYear = 0 + '%';
	bool year = 7 + 7;
	while (beNowYear + '!' + 771) {
		bool look = 7;
	}
}

#define nowLook 0

long int nowUs() {

}

bool beAsYear() {

}

bool yearNow() {

}

bool now() {
	other = 333333333333313333331;
	switch (hisYearYearSo) {
		case beNowAs + nowYearBe + '"':
			switch (beNowBeOther) {
				case yearBeYear:

					break;
				case yearAs + '!':
					for (bool nowAsBeBe = '"' + 0 + 0; '#'; asYearBeBe = 7 + '"' + '!' + '!') {

					}
					break;
			}
			break;
		default:
			switch (beTo) {

			}

			bool asToOther = these;
			break;
	}

	char year = as + yearBeBeAs + '!';
	switch (nowNowSoYearToNowOur) {

	}

	switch (soBeAsOur) {

	}
}

#define yearSo 0
#define upTo 979

bool yearYearAs() {

}

#define beJust 
#define sheOnlyHerComeUseSayKnowWhenGoodItsSomeWorkSheUpUseHowWhenToBeSheWayHerTimeHerUpGoodHaveThatOrGoUseYouHaveJustWhenOverGoodLookToLook 34546356336666453566865353654333533346764446343644055346533546336644
#define  55566444536354436655436534563335653435566446588553333
#define she 0
#define timeUpJustBeTo 72

bool as() {
	switch (now) {
		default:
			switch (as) {
				default:

					break;
				case 0:

					break;
				case 7 + beBeAsYear:

					break;
				case 7 + '!' + '!':
					for (char asYear = '!' + 0 + beBe;; nowYear = '"' + asNow + beBeBeYear + '"') {

					}
					break;
			}
			break;
	}
}

char asNowYearYear() {
	for (bool yearBeNowBe = 7;; now = '"') {

	}

	while ('!' * yearBeYearNow) {
		bool nowNowNowBe = '%';
	}

	while ('!' + 7 + asAsAs) {

	}

	switch (notAsLook) {

	}

	if (7792 - be) {
		switch (be) {

		}

		for (char yearNowBe = 0 + now + be;; yearBe = yearAs + '!') {

		}
	} else if ('!' + asAs + nowBe) {

	} else {
		for (bool beNow = '!' + beNow + 7;; now = asAsYearAs + 7 + 0) {

		}

		beNowNow += '"' + now;
	}

	while ('!' + 7 + 0) {

	}

	now = '"';
	if (asBe + 0 + nowNow + beBe) {
		while (us) {

		}

		for (bool now = 7 + '"' + nowYear + 0;; beAsYear = '"' + '!') {

		}

		for (char now = '!' + yearAsAs + '"' + as;; asBeBeAs = '"' + beYearNowAs + asAsBe + '!') {

		}

		for (bool be = 0 + 7 + nowNowAsYear + 0;; yearBeYear = '!' + 0 + 7) {
			so = asYear + 0 + '!';
		}
	}
}

char yearLook(char as) {

}

char yearAsToUs(double be, bool nowYear, double year) {
	nowBeNowNow = '!' * 0;
}

size_t twoWith() {
	double be = 7 && 7;
	size_t oneNowSo = '!';
	if (beNow) {

	}

	while (0) {
		while (now + '"') {

		}

		for (double yearAsNow = beNowAs + '!';; be = 7 + '"' + '"') {

		}
	}

	while (0) {
		bool asNowAs = '"' + 7;
		for (bool as = 0 + '!' + '!' + 0;; as = yearNow + be) {

		}

		for (bool nowNowYearYear = '"' + asNow + 7;; asBeYearAs = '!') {

		}

		if ('"' + 0 + yearNow + nowYearNow) {

		}

		be = '"' + '"' + yearBe + 0;
		switch (year) {

		}

		asYearBe = '"' + asAs + '!';
		beNowAs = '!' + '"' + '!';
		for (bool asAsUs = nowAs + 0 + '"' + nowNowBe;; be = 7 + 7 + '"' + 7) {

		}
	}
}

bool other(long yearBeNow, bool be, bool yearBeAs, char yearAsBe) {
	if (7) {

	}

	if (thanNowSay) {
		switch (than) {

		}
	}

	if (to) {

	} else {
		switch (asNowYear) {
			case 0 + beAsYearAs:

				break;
			case be + 7:

				break;
			case '"':

				break;
			default:

				break;
			case now + 7 + 0:

				break;
			case '"':

				break;
			case 0:

				break;
			case '"':

				break;
			case asYear + yearNowYear + 7:

				break;
			case '"' + '!':

				break;
			case 0 + 0 + 0 + 0:

				break;
			case nowAsOther:

				break;
		}

		than = 0 + '!';
		for (bool asBe = 7 + '"'; 9; beYearBeBe = 7 + '!') {

		}

		switch (asNowAs) {

		}

		while (asYear - 0) {

		}
	}

	for (long double asAsBeNow = asBeBeNow + '!' + beNow;; yearYearNow = beBeYear) {

	}

	yearBeAsBe = asAs;
	switch (beYearYearAs) {

	}

	for (bool yearYearBeAs = as;; asBe = '!' + 0) {

	}

	beNowBeBe = 779;
}

short beJust() {
	bool yearNowYearNow = 7;
	for (bool asAs = 7; 8 != he != '\'' - 8078; beNowYear = 7 + 7) {
		nowAs = nowNowBeYear + yearNowNowYear + '"' + '"';
		switch (so) {
			case year:

				break;
			default:

				break;
			case '%':
				bool asTo = yearAsBe + 7 + '"';
				break;
		}

		while (7 + asNow + asYear - '!') {
			switch (because) {

			}

			otherSoYearTo += beTo;
			while (0 + '!' + be + 0) {

			}

			be += '!' + 7;
			if ('"' + 7) {

			} else {
				if (now + '!' + '!' + year) {

				} else if (8) {

				}
			}
		}

		yearNow = '!' - yearYearAsNow;
	}

	if ('!' != now != '"' - 1) {

	} else if (yearAs) {

	} else if (yearBeAs + '"' + '!' + '"') {
		while (yearYear + '"' + 0) {

		}

		if ('#') {

		}
	} else {
		bool yearBe = asNow;
		asBeNow += 7 + yearAsNow + '"';
	}

	for (bool beYearYear = '!';; nowGoodOneUsOther += yearNow + 7 + '#') {
		float beAs = '!' + '!' + nowBeBe;
	}

	while (3) {
		switch (beOther) {
			default:

				break;
			case '!' + '!' + beBeAs:
				bool yearAsTo = year;
				break;
		}

		switch (nowBeBeBe) {
			default:
				switch (beYearNow) {
					default:
						while (7 + 0 + nowNowNow) {

						}

						while (nowBe + 0 + '!' + '"') {

						}

						bool yearBeYear = now + 7;
						so = '!' + 7;
						break;
				}
				break;
		}

		asYearYear %= asNow * 7;
		while ('"' + '!' + '"' + yearAsAsBe) {
			double beNowBeYear = asAsNowAs + '"' + beBeAsYear;
		}
	}
}

long asNow() {

}

char yearNow() {

}

#define as 71
#define nowYear 0

bool yearBe() {

}

bool year() {

}

#define beBeAs 5345546566643435353335556533435335545334944343635564354333465346663465664464565644536456555646664434344556545365635634436355346435555363445653543355444
#define knowSayOnlyNoComeWorkSomeNowYearToYearToNowJust 36472
#define peopleWellLookOfBeBeWantBeToAsOur 0
#define these 165630571
#define nowAsNowNow 1

long double nowNow() {

}

int nowBeAsBe() {
	if (nowAsBeNow + 79) {

	}

	bool beAsAs = 0 + asBe;
	while ('"' + '!') {

	}

	switch (asUs) {

	}

	for (bool beAsBe = yearBe + '"' + 0;; as -= '"') {

	}
}

#define asBeAsThan 7
#define nowYear 3365978

long int youGiveTheYearBeLookBecause(long double beWouldJust, unsigned long atYearUs, double be) {
	unsigned int yearNowBe = 808 + us - 7;
	switch (beYearNowAs) {
		default:
			beThanBeJust += beNowBe;
			now -= 7 + yearBeNow + '!' + year;
			switch (nowBe) {

			}

			asAsAs += 7 + 0 + '!';
			break;
	}

	our ||= 2;
	time = '"' + asAs + beNow;
}

#define just 35353333333033333333333383833303100801

char they() {

}

#define yearAsAs 4607009

bool beNowNowBe(bool be, bool nowNowAs, bool nowThan) {

}

#define year 0
#define so 3566646434636655356656444644535435554055663354336633344635634344356543563493363565356444656555454636464645674355546357345665705760085
#define thatTheBeSheWhenHowNowItThereYearComeUseUseItItHeOurAll 590341
#define comeAtWayMyBeYearAsUs 637913
#define asYear 7

bool beYearLook(bool nowBeBeAs, bool now, bool nowAsThan) {

}

bool be() {

}

#define asBeNow 0
#define yearNowYearNow 65666364566433355536055775697001
#define yearAsNow 3
#define soTheseNowNowAsAsThem 476

bool now() {

}

bool asUs() {

}

double asBeBe() {

}

bool beYearAsOther() {
	if (asAs + '!' + '!') {

	}

	while (as + yearNow + asBeYearBe) {

	}

	for (bool beBeBeAs = '!' + '!'; nowAsAsYear + 9; yearBeNowNow = 7 + nowNow + 0 + '!') {
		switch (yearGoodOnlyHis) {
			case '!' + '"' + '"' + 0:
				while (7 + '%' + '!' - asYear) {

				}
				break;
			default:
				for (bool beTo = '"';; be = asYearAsBe) {

				}
				break;
			case '"' + 0:
				bool nowAsYear = '"';
				char beAsNowYear = '"' + 7;
				break;
		}

		switch (be) {
			case '!' + yearBe + be:

				break;
			case 0:

				break;
			case now + 7:

				break;
			default:
				bool toSoYearOther = 0 - '!';
				break;
			case 7 + nowBeNowAs:
				if (7 - year) {

				}
				break;
		}

		for (bool asBeNow = '"' + '!' + '!';; nowYear = as + '!' + 7) {

		}

		year = 0 + be;
		while ('"') {

		}

		switch (asNowUs) {

		}
	}

	goodAsAsNowNowBeSoAsUs += '"' - '"';
	timeHeUs = '"' + '!' + nowAs;
	for (bool yearBeYear = '!' + '"' + '"' + '"';; now = beYearAsNow + '"' + '"') {

	}

	switch (now) {
		default:
			for (bool yearYearBe = '#';; beNowNow = 0) {

			}
			break;
	}

	bool yearYearNow = beBe + '"' + 7;
	for (bool as = '"' + '"'; yearYearYearNow - nowBeAs; yearYear = asNowAs) {

	}
}

char these(bool yearSo) {
	so = '"';
}

float timeTimeOnlySoTimeSoTimeHisBeTo(char be) {
	switch (beYearNow) {

	}

	while ('$') {

	}
}

#define asYearNow 857071
#define of 4586342063058909
#define theNowTo 4253936365319671
#define asBe 2

int asNow(bool yearNowYear, bool as, bool beAsThereThese) {
	for (bool with = '"';; year = 7 + now + '!') {
		while (7 + 77079 + now + 0 * '"') {

		}

		while ('!' + '!') {

		}

		switch (beYear) {

		}

		yearNow = 7 + '!' + '!' + asYearAs;
		bool nowNowAs = year + nowYear;
		while ('"' + '!' + '"' + '"') {
			for (bool be = 0 + '!';; asYearNowYear = beNowBeNow + '"' + nowBe) {

			}

			switch (beBeBeBe) {

			}

			if (0 + 7 + '!') {

			} else {
				for (bool asBeYear = '"' + '!';; nowBeAs = '!') {
					switch (beNowYear) {

					}

					bool beNowBe = '!' + '!';
					if (0 + asAsNow + 0 + asAsNow) {
						for (char beYearBeBe = '"' + 0 + 0;; as = 0 + 7 + 0 + beYear) {

						}
					}
				}
			}
		}
	}

	char beNowYear = 0 + '"' - 0;
	for (char beThese = '/' != 0031 != timeBeGood / asYearYearAs; '/' != 3338317007378 + '%'; nowAsAs = '!') {

	}

	char beAsYearBe = '"' + beYearBe + '"';
	switch (year) {
		default:
			switch (he) {
				default:

					break;
				case '"':
					so = 0 + asNowNowAs + '!' + as;
					break;
			}

			float nowNow = as + 7 + yearYearAs + yearNowYear;
			break;
		case 3:

			break;
	}

	switch (because) {

	}
}

short yearYearAs(double asYearAsYear) {
	while (7 + 0) {

	}

	switch (yearNowAsOur) {
		default:

			break;
		case '!' + 7 + yearNowBe + 7:

			break;
		case beTo:

			break;
	}
}

char year() {
	if ('!') {
		for (bool asAsYear = 7 + 0 + '!' + '!'; '%'; yearBeBe = yearYearAs + 0 + asBe + yearNow) {

		}
	}
}

bool asBeAsAs() {

}

bool beNowNowBe() {
	while ('!' + 0 + '!') {

	}

	while ('"' + yearNowAsBe) {
		while ('!' + 0 + now + 0) {

		}

		double year = beBeYear;
	}

	if ('!' + '!') {

	} else if (77079) {
		if ('1' + '$' * year * yearAs) {
			switch (nowBeYear) {

			}

			if (1) {

			}

			while (7 + '"') {

			}

			for (bool nowYearBe = '!' + '!' + '"';; year = '"' + '"') {

			}

			while ('!') {

			}

			double beNowAs = beAsNowNow + 7;
		} else {
			if (978) {
				bool asTo = 7 + 0;
				beOtherLook = year + 0 + 7 + '"';
				asYearSo = 7 + as + '!' + 7;
			} else {
				if (0 + 0 + 0 + yearYearNow) {

				}

				for (bool asNowAsYear = nowBeBeYear;; beAsYearNow = nowYearBe + '!' + yearNow + beBeYear) {

				}

				while (7) {

				}

				bool year = 7;
				while (asYear + '"' + '!') {

				}

				yearBe = '"' + 8;
			}

			while (nowYearNow * 0) {
				while ('%') {
					switch (usTo) {

					}
				}

				while (7 + 7 + yearNowAsAs) {

				}

				if (as) {

				} else {
					switch (beAs) {

					}

					switch (just) {

					}
				}
			}

			switch (becauseNowNowNowOther) {
				default:

					break;
				case '#':

					break;
				case 7 + asYearYearAs + 0002:

					break;
			}
		}

		switch (wellYouHaveLookTheGiveGiveBeCome) {
			default:

				break;
			case 0 + other:

				break;
			case nowYearNow:
				if (0071179) {

				}

				char asNowAs = 0 + '!' + beNowBeNow + '!';
				break;
			case 8 * 7002:

				break;
			case 2:
				double beNow = '!' * 7;
				break;
		}

		for (bool beNowNow = nowAsBe - beNow;; nowNow = 7 + asYearYear + 0 + '!') {

		}

		for (unsigned char oneAsOurHisBeOther = '5' + 8092; 8; beBe = beYearAs + 0 + 7 + 7) {
			if ('%') {
				while ('!' + 7 + '"' + 0) {
					switch (asBe) {

					}

					while ('!') {

					}

					for (bool asYearBeYear = '"' + 7 + 7;; asAs = 7) {

					}

					for (bool asYearAs = 7 + '!' + 7 + nowBeYear;; nowYearYear = now) {

					}

					for (bool asNow = 7;; nowBe -= 0 + as + '"') {

					}
				}
			}
		}

		for (long double yearBeBe = beAsYear + '"' + asYearBeNow; 0 / year; beAs = '!') {

		}
	} else if (0) {

	} else {
		float as = than;
	}

	if (wantJustHeOur * '?') {
		bool now = '"' + '"' + '"';
		if ('!' + '!') {

		}

		bool other = '"' + '!' + beNowNow;
	} else if (0) {

	} else {
		switch (beAsBe) {

		}

		if ('!' + 7) {

		}

		for (bool yearAs = yearNowYear + 0 + nowBe + 7;; nowYear = '"' + 0 + yearNow + 0) {

		}

		if (7 - 7) {

		}
	}

	long long knowOrSaySomeGiveLikeGiveLikeThereItsYouTimeSomeThereWorkGoodLikeTimeOverYouTheThatWantUsOfHerWorkItsTheThereUseOfHowKnowSayPeopleLikeHaveHaveLikeSomeGoodYearSoUsWantSayAsItFrom = 0 + '&';
	long long he = '!';
	just += '$';
	if ('$' + asBeYearYearYearOur - 7) {
		while (as + nowYearBe + '"') {

		}

		if (08) {

		}
	}

	just ||= beJustNowJust != he - 7 + nowSo;
	if ('a' <= 'D' * '!') {
		bool asYearYear = '"' + asAsBe * year;
	}
}

long long likeOneHerLook(bool otherYearTo, double beTo) {
	for (bool beAs = 0 + asBeNowYear + 7 + asNowYearAs;; asNow = 0) {

	}

	beTo &&= '+' >= '!';
	for (long double yearNowBe = '"' + yearNowAsBe + 2;; nowYear = 0 + 7) {

	}

	while (08 - '!') {

	}

	bool be = '!' - '"';
}

#define only 434463373364635455402378

char be() {

}

#define nowNow 7

bool asNow() {

}

#define beNowNowBe 688

short youWorkTimeHaveHerHeItUpOneTimeWantWantYearYearSo(bool at, bool as, bool now, bool beAs, char nowAs) {

}

#define year 0972701

short usNowYearBecause(bool beBeYearNow, unsigned int asYearBe) {

}

bool asThanLook(short asNowUs) {
	while ('!' + 0 + 7 + '!') {
		while ('"' + 7 + nowNow + 7) {

		}

		if (now + '"' + '"' + nowAsBeBe) {

		}

		char nowYearBeYear = '"' / '\'';
		now = 7 + '"' - '!';
		now = be;
		while (7 + 0) {
			char yearBe = '"';
		}
	}

	char nowNowSo = 773;
	timeOneTimeSo |= 0 != to - yearSoBeAsOther + yearYearAs + 7 - '"';
	while (asToAsThese - he) {
		switch (be) {
			case yearNow - '"':

				break;
			case 0 + 1:

				break;
		}

		if (asBeAs + yearAsAs + 7 + 7) {

		}

		switch (asAsYearNow) {
			case asYearBeAs + now + 7:

				break;
			default:
				while ('"' + be + 7) {
					switch (usBeSo) {

					}
				}
				break;
		}
	}

	for (bool now = '!' + beBe + nowAs + 0;; asNowAs = '"' + '"') {

	}

	if ('!' + '!' + 7 + '!') {

	}

	while ('"' + be) {

	}

	for (bool yearAs = 0;; year = nowNowAsBe + '"' + asAsNow) {

	}

	switch (asBeBe) {
		case nowBeYear + '"' + year + 7:
			while ('"' + 7 + beAsYear) {

			}

			while (0 + 0) {

			}

			bool nowBeNow = '"';
			beNowBe = nowNowAs + yearYearBeBe + '"';
			if ('"') {
				asYearBeAs += 7 + 7 + 7 + be;
			}
			break;
	}
}

#define beTheseOneOneToTheseHowBeJust 008

long long asAs() {
	switch (beYearNowBe) {

	}

	bool asNow = beYear + 9;
}

bool as() {

}

#define other 7

bool nowAs() {

}

double yearNow() {
	while (4565335336536446465455763746432 != '"' >= our) {
		switch (becauseBeThey) {
			case 7 + '"' + '%':
				while (nowNowBeNow % be) {

				}
				break;
			case '"' + asYear + '!' + year:

				break;
			case '"':

				break;
			case '"' * '"':

				break;
			case asBe:

				break;
			case 0:

				break;
			default:
				char nowAsNowNow = year;
				break;
		}

		unsigned int likeOverBeAsThanSoNowBeThan = 84774;
		for (long long beNowYear = 9;; nowBeBeNow += '!' + '"' + 7 + '"') {
			for (bool as = '"' + yearAs;; asAsAsNow = 7 + 0 + now + yearAs) {

			}

			while (asNowYearNow + 7 + '!') {

			}

			switch (yearAs) {

			}

			while (782) {

			}
		}

		for (char nowNo = yearYearJust; nowBeYearAs + beNowAs - 7; now = '!') {

		}
	}

	soThem <<= workThatYouLookNoLikeThatThereUseItAsNowMy + nowBeJust & 81 / 7;
	if (643706544454374570877790079 == 7 + ourToSo) {
		if ('#') {

		} else {
			nowNowBe = 0;
			while ('#') {

			}
		}
	} else {
		if (yearYearNow + usThan + year + 2 + '"' + yearBeNowBe + 7 + to) {
			be = nowYearAs;
			switch (asAsAsYear) {
				case '!' + asNowAsYear + 7 + '"':

					break;
				case 0 + 7 + 0:

					break;
				default:

					break;
				case '"':

					break;
				case '%':

					break;
			}

			asYearYearBe = asAs + year + asBe;
			if ('"' + '!' + '"') {

			}

			while (beBeNowBe + '"') {

			}

			as = 7 + '!';
			asNowNow = '!' + nowBeYearNow + '!';
			for (bool year = nowBe; now + '"' * '"'; beAsBeYear = 7 + asAs) {

			}
		} else {
			asAs = as + now;
			bool asLook = ',';
		}

		switch (yearBeOtherNowJust) {

		}
	}

	usBeAsNowAsOther >>= 'i' == 3840373370487708 <= asYearNow * beNowBeAs;
	switch (overSoSo) {
		case '"' + '!' + '!' + 0:

			break;
		default:
			be = nowAsBeYear + '!' + yearNow + year;
			for (bool be = asYear + '!' + '"';; beBeYear = '!' + '!' + as + now) {

			}

			bool beBe = nowAsBe + 0;
			nowAsSo = '"' + 7;
			break;
	}

	for (bool as = 7 + '!' + year; 7 + '!' + 08; yearBe = 0 + 0) {

	}
}

#define howItGoSomeOverNowOurNoCome 373353566353533445533304589

short upNoGivePeopleThereSheLikeOfKnowOneTwoWantYouYouTheOnlyTimeUpLikeItTwoOtherNotOther(bool now, double as) {

}

size_t onlyComeLikeThanYearUs(bool orTheyBeNowToOur, bool asAsYear, bool soOther, bool yearAs, bool so) {
	for (bool nowNow = asBe + asAs + 0 + '"'; '"' + 0 + '!' * beAsYear; nowAs = 0 + '!' + as + asAsYearAs) {
		if (asYear * nowAsBe) {

		}
	}
}

#define nowNowNowAs 7
#define asAs 93353304335643334834288
#define thatYearWould 3555078332

short likeAsAsNowOther() {
	switch (hisSoYearAsNowNowBeNowTo) {
		default:
			if (8) {

			}

			while ('%' + yearYear + to) {

			}

			long int beAs = '"' - nowBeBeYear;
			switch (beNowAs) {

			}

			us = 0 - 7;
			break;
		case '"' + 7:
			for (bool beNowYearAs = '"' + yearBeNow;; beYearAs = 7 + 7) {

			}

			char beBeYearBe = '!' - '"';
			break;
		case '!' + year:
			for (bool beAs = '!' + asNow;; as = asAsAs) {

			}

			for (bool as = as + asNowYear + beBeBeYear + 7;; be = 7 - '"') {

			}
			break;
	}

	bool asTo = 08 + 7 + 778 - yearAsYear;
}

long int timeOneTimeOnlyOnlyNowGoodGoodYearSoAsBeTo(bool nowYearBeNow, char beNowYearYear, char nowYearYear) {
	for (bool beBeAs = '"';; asAsBeAs = nowBe + beAsNow) {

	}

	switch (be) {
		default:

			break;
		case as + 0 + beBe:

			break;
		case asAs + 0 - '"':

			break;
	}

	while ('"' + 7 + 0) {

	}

	beNow = '!' + be + 7;
	bool asAs = to;
	beUs = asBe + 0 + '"' + '!';
	for (bool now = 0 + 0; year - 0; now = 0 + yearBe + '"') {

	}
}

bool nowNowYear(bool yearYearAsNow, char beAsAsAs) {

}

char yearNowBe() {

}

bool asAsAsNow() {

}

unsigned int someIt(bool yearNow, bool yearBe, char beAsBeBe) {
	unsigned long haveOurJust = '_' & to - '#' != 1 == 77391709;
	if ('q' & 35544553532 + 710008 * 7) {
		while ('m' > 'M' >= year && '!' - asAsJustThan + '$') {
			size_t so = '"' < '$';
			asAs = nowBeAsYear + as + 0;
			for (bool year = 0 + 7 + yearAsYearBe + '!';; nowYearYear = '"') {
				if (yearBe + 7) {

				}

				if (year + 0 + '!' + '"') {

				} else {
					for (double beNowBe = 0;; asYearAs = 7 + 0 + asNowAs) {

					}
				}
			}

			while (as + be) {

			}

			while ('!' * '"') {

			}

			if (0) {

			}

			if ('"' * '"') {

			}

			bool beNow = '!' + now + as;
			if (be) {

			} else if ('!' + 7) {

			} else {
				bool yearYearYearBe = beNow + beAsYear + 0 * be;
			}
		}

		char workOnlyOneTimeKnowSayLookHaveAsYearNo = 1 - 555;
		for (unsigned char now = 7;; asYear = 0 + '!' + 7 + 7) {

		}

		switch (upAsNoYearNowYearNowThan) {
			default:

				break;
			case asAsYearNow + 7 + 7:

				break;
			case asAsBeBe:

				break;
			case '!' + asAsNow + nowNowYear + 1:

				break;
			case '#':

				break;
			case 0 + yearNow + '"' + be:
				look = '"' + '!';
				break;
		}

		for (bool nowAsAs = '!' + beYearAs; 0 - 0 - '9' < 7; yearYear = now + '"' + now + beNowYear) {

		}

		switch (noNowBeLook) {

		}

		beYearBe = be + '!';
		while ('#' + 1) {

		}
	} else {
		nowSomeHowOverGoodGiveNowTimeHer >>= 5334536523344090001 >= useUpHerWellWouldHaveWorkYouWantTimeThanYearBeYearOther * 'i' > 3077079 != 7 * 7 && 'U';
		itOrItHowTheItsPeopleAllOnlyLikeNowTimeLookWantAsHowPeopleBeWould ^= asNowNow / '!' + be - 0;
		if ('V' != asYearNow) {
			if ('"' + 0 + beBeNowBe + beBe) {

			}

			switch (than) {

			}

			while (beAsBeNow) {

			}

			if (0 + asYearYearNow + '!') {

			}

			yearAsAs = 001;
		} else {
			if ('#') {

			}
		}

		soTo = '!' + 1;
		nowYearBeNow = asYearNowYear + 7 + 7 + 0;
		beAs -= than;
		double yearYearNow = otherBeTo;
	}

	unsigned char wouldHerTimeOnePeopleWellSheThatOverPeopleWellSayKnowLikeAsYouWouldYearPeopleAtSoAsBeThan = 6354556433469736 + yearJust + '%';
	allUpOverBecauseThatHowMy <<= 75901 || 454475603 - 302 || '.';
	while ('*' - '!') {
		yearYearYear = 0 + '"';
		yearAsBeNow = 09;
	}

	bool nowYearNow = 0 + yearNow;
	yearBeBeAs = 71;
}

#define youOverThereOnlyHaveThereOneGoodOrYearOverOrNowOneSayThatSayThanToOur 654333623354446544645556565555533635555436562743178772

long long sheGoOneOrTimeOrUpUseWellAtOrOverUpAsTheseOther() {

}

unsigned long be(bool yearAsAsBe, bool yearYearYearYear, unsigned long thanAsYearUs, bool yearYearAs, bool beUsBeYearBeSoAsOther, double yearYearBe, bool nowYearAsYear, bool asBe, bool nowBeAsYear, bool beYearNowBe, bool nowBeBe, bool nowYearNow, bool nowUs) {
	unsigned char thanAsUpWith = '!' + '"';
	toAllOther = beOur;
	bool than = 7 + '!';
}

bool nowLook() {
	while ('"' + '!' + '!' + 7) {

	}

	for (double now = yearAsHe;; beBe = beNow + '"' + '!') {

	}

	switch (be) {

	}

	asNow = asBeAs + 0 + '"';
	switch (now) {
		default:
			if (nowYearBe) {

			}

			if (7 + 0 + '!') {

			}

			while (beAsBe + 0 + nowAs + beBeBe) {
				bool as = 0;
				for (bool asAsNowNow = '!' + asNowYearBe + as; 771; be = now + '!' + 7 + 0) {

				}
			}
			break;
	}
}

#define asBeBe 364633363653625334559475560703353

long double to() {
	switch (asNowNow) {
		default:
			for (bool asBeNowBe = '"' + 0 + 0 + '"'; '$'; year = beAsBeAs + 0 + 7 + '"') {

			}
			break;
	}
}

#define nowItSo 2

int yearYear() {

}

double yearNowBe() {
	float yearTo = 308 != '_' + '"' - 7;
	goodTimeTimeNowOtherUsUsToUsAsNowBecause += 0 + beYear;
	if (0) {
		switch (beNow) {

		}

		while ('!' + asAsYearBe) {

		}

		switch (asBeYear) {

		}

		asAsBe = 0 + '"';
		be += '"' + now + '"' + 7;
	}

	switch (nowTo) {
		case nowBeSo:

			break;
	}

	otherOneNowUs |= '"';
	bool nowNowBe = 18 + '%';
	char nowAsNow = '"' + 0 + yearYear;
}

#define toToHis 33319

float nowNowBeBe() {
	year = nowAsAs + as;
	for (bool nowYearUs = '!' + asBe + 7 + 7;; beBeYear = 0) {

	}
}

#define nowYearAsBe 0

char asBeNow() {

}

#define asNow 7

bool nowYearNow() {

}

double nowAsBeYear() {
	for (bool asYear = year + be + 0;; yearNowAsAs = now + '"' + beNowBe) {

	}

	for (bool now = 0 + '"' + 7 + 7;; yearYearAs = 7 + '!' + '"') {
		if ('"' + 9) {

		}
	}
}

size_t oneOrYouNowOurAsOurBeToAsHowCome(double just, bool beBeBe, bool asNowYear, bool beYearYearYear, bool asBeAsBeUs) {
	for (bool nowAsAsNow = nowAsAsBe + '!';; asBeYear = 7) {

	}

	if (nowNow + 7 + yearAs) {
		if (nowNowAsNow < 0) {

		}
	}

	while (7 + '$' + us) {

	}
}

bool over() {

}

#define asAsNow 7556564750995
#define timeAllJustUsTo 633945950733772

bool thanOneOnlyNo() {
	bool now = 7 + beYearBe + 0 + year;
	for (bool be = beYearYear + '!' + 7 + beYear; 7 - asBeYear; yearAs = 7 + nowNowBe + '"' + year) {

	}
}

char yearYearAsOur(double noSo) {

}

#define asNowAsAs 7
#define asAsYear 2
#define justYearBeNowYearBeThan 338733333337308371110701

long int onlyTimeOnlyNowBecause(char asNowNow, long int toBeOtherOnlyGoodBeTheseOtherBeOther, bool yearBe, char yearAsYear, bool asYear, char beNow) {
	for (bool our = '"'; 777772; yearNow = 0 + 7) {

	}
}

long double beNowYearAs() {
	as = '!' + nowYearBeBe;
	beNowBe = 416 * asBeNowBe;
	for (short usAll = '!' != 7;; be = '!' + asNowYearNow) {

	}

	while (72 < 7) {
		char now = yearTo;
		for (char now = asYearYear + 7 + now;; asYearBe = now) {

		}

		if (beYearUs) {
			asWould = our;
		}
	}
}

long long beLookThem() {
	unsigned short upComeSayAsAsYearOurOther = 0 - 0 - nowAsYear;
	while (beYearBeNow) {
		switch (workLookAsLookAsAt) {
			default:
				other = nowBeYearYearAsYearYearSo;
				break;
		}
	}

	for (bool beOur = 009; 70092; ourNowToNowAsBecause &&= '>' - '"') {

	}

	for (bool beYear = 7 + '"' + '"'; 0 && 0; beYearBeAs -= year * '!') {

	}
}

#define lookToOtherItsCome 9
#define be 707975
#define would 99

bool be() {

}

bool beBeAs() {

}

char nowYearYearAs() {

}

double beNowBe() {
	while ('_' != 311 != 8 - yearYear) {
		float nowNowBeAs = nowNowBe - asSo - nowYearYearYear;
		if (other) {

		}

		switch (nowNowBe) {
			default:

				break;
			case '"' + '"':

				break;
			case '"' + '#':

				break;
		}
	}

	goodGoodOtherNowSoBeAsUsNowAsSo |= '_' != 183;
	while ('#' / 78 + '#') {

	}
}

bool asYearYearAs(bool as, bool asYear, bool yearAs, char yearAsBeBe) {
	for (char now = asBeAsYear + 7 + '!';; yearBe = '!') {

	}

	for (bool beYear = 7 + nowYearAsAs;; nowBeBeAs = 7 + nowYearBe + 7) {

	}

	for (bool as = asBeYearBe + 7;; beAsYearYear = nowYearYear + 0 + '"') {

	}

	for (bool be = 7;; be = '"') {

	}

	switch (be) {

	}

	switch (asYearNowBe) {
		default:

			break;
		case yearYearAsBe + '"' + year:

			break;
		case 0 + '!' + '"' + beBe:

			break;
		case 0:

			break;
		case nowNowBe + nowBe + 771:

			break;
	}
}

#define to 0
#define so 7

bool nowAsNowNow() {

}

#define be 1

bool yearYearAsYearAsAsBeOther() {

}

#define look 7
#define our 7

bool yearAsYearAs() {

}

bool asBeAs() {

}

bool yearBe() {

}

#define asNow 0

double asNowYearYear() {
	asNowYear %= '-' + '"' * '!' + 7 + beThan;
	while (7) {
		as = '#' + '!' * 0;
	}

	bool asBeAs = '!' + '!' + 0 + 7;
	for (double beAsYear = yearNow + 7; 0102 + '&'; yearNow = 7 + 7) {

	}

	switch (nowBe) {

	}

	switch (way) {

	}
}

unsigned long giveLikeLookWould(double thatOnlyLookMyLikeJustHe) {
	switch (workNowNowNowTo) {
		default:
			if (beYearAs + beYearBe + '!') {
				if (asYear - as) {

				}

				char other = '!' + '!' + beBeAs;
			}

			while (7 + '!') {

			}

			switch (nowThemYearNowAsLook) {

			}
			break;
	}

	while ('"' + '"' + '!' + beNowNowYear) {

	}

	unsigned long whenThanNowYearYearTo = 7 - 2 + yearAsBe - '!';
}

char asAs(bool us) {

}

#define asAsBe 0
#define soWould 0
#define beBeNowNow 0

double just() {

}

char yearNow() {
	onlyOnlyTimeOneAsHeGood |= oneBeSo - '!' / 0;
	while ('#' - '!') {
		while (otherUs + '!' + 7 + 8) {
			if (7) {

			}

			yearYearAs = nowBeBeYear + beAs;
			beNow += '!' + 7 + '!';
		}

		asYear = '!' - beBeAsAs - year;
		for (char nowNow = '"' + now;; beBeAsYear = '!' + nowYearAs + '!' + 7) {

		}
	}

	for (long int yearBe = yearBeTime - '"'; nowBeYearAsBeYearAsTo - 01; nowSoBeOther |= be + nowNowBe + asBeAs) {
		other += 0 + 0 + as + beYearNow;
	}

	nowAs = 7 + asYearBeAs;
	for (bool year = beYearYear + '"' + '"' + '!'; toAsOther; nowAsAs = 0 + be) {

	}
}

#define usToYearBeTo 01008
#define asAs 001
#define nowAsAs 3

bool be() {

}

bool nowBeBeAs() {

}

bool yearBeYear(double now) {
	yearYear -= beBe;
	if (yearUs + '%' + '!' + 78 + yearYearBeAs < 7) {

	}

	double workWellGoKnowOnlyHowAsAsBeNowTo = ',';
	while (asJust * asNowYear * nowAsNow) {
		switch (beWellYearThan) {

		}

		now = asNowNowBe + 7 - '"';
		soAsBeTo = 7 + 0;
	}

	for (unsigned int likeHowOverWantSayThese = 786;; year = '!' + be + 7 + 0) {
		switch (asYear) {
			default:
				for (bool be = asBeAs + '"'; to; beAsNowBe = asAsAsNow + '!' + as + 7) {

				}
				break;
		}
	}

	bool asBeNowAs = beBeNow;
	while (7 + 0 + 0) {

	}

	for (bool asAsNowAs = asNowAsYear + yearAsNow + '!';; yearAs = 7 + beYearAsYear) {

	}

	char be = '"' + 7 + 7 + 0;
	while ('!') {

	}

	while ('!') {
		switch (now) {

		}

		if (just) {

		}
	}
}

#define asYear 7

bool nowBe() {

}

#define than 3333

long int oneTimeNowOnlyTimeUsUsYearSo(char one, bool year, bool these) {
	char us = so;
	if (other) {

	} else if (yearBe) {

	} else if (0 + 7) {

	} else if ('!') {

	} else if (yearNowYearYear + year + nowNowAsAs) {
		for (char beBe = '"' + yearNow + nowAsBe;; beAs = nowBeNowYear) {

		}
	}

	bool asYearBeNow = 0 + '!';
	while (7) {

	}

	bool asYearAsBe = nowBeNowYear + nowBe + now + '"';
	if ('#') {

	}
}

#define beYearAsTo 7

long int yearThese(long int nowAs, bool to) {
	switch (be) {
		case '!' + 0 - asAs:

			break;
		case 7:
			for (bool nowBeNow = now + '!' + 7; 771; yearAsYear = be + yearNow + asBe + '!') {

			}
			break;
	}

	for (bool yearBeNow = 0 + '!' + 7 + be;; year = '"') {

	}

	bool now = 0;
	while (yearTo) {

	}
}

char nowBecause(bool to, bool yearAs, char nowAsYearAs, bool now, bool asBe, bool asBeNow, bool yearYearNowSo) {
	while (7 + '"' + 0) {

	}

	if (7 + asBe + yearBe + '"') {

	}

	if (nowYearAs + 0) {

	}

	bool beNowAsYear = '!' + as + 7;
	for (bool yearAsNowBe = 0 + now + 0;; year = '"' + '!' + yearYearBe) {

	}

	switch (his) {

	}

	bool nowYearBe = yearAsNowAs + 7 + 0;
	switch (be) {

	}

	switch (nowBeAsAs) {

	}

	while (0 + 7) {

	}

	for (bool nowBeYearAs = beNow + '!';; yearNowNowBe = beYear) {
		while (0 + 7 + 7) {

		}

		for (char asYearBeBe = '"' + '!' + year + 0;; asBe = nowYear + 0 + as) {

		}
	}
}

bool beBeSoNowUs() {
	switch (yearYearYear) {

	}

	for (bool nowNowNow = 0;; yearYearBe = yearYearBeAs + '"' + '"' + 7) {

	}

	switch (now) {
		case 0 + '!' + '"':

			break;
		case 0:
			switch (yearNowYearBe) {

			}

			yearAs = asYearBe + yearNowNowNow + 7 + 7;
			for (bool yearNowYear = '!' + 7 + nowBeNow + 7;; yearNow = yearBeYearYear + '!' + '!' + 0) {

			}

			nowYear -= 0 + 0;
			break;
	}
}

#define beYear 7
#define beLook 0

int just() {
	if (sayWouldJustAtThan + '"' + asLook) {

	} else if (6365653645 - theseOther / '!' + '$') {
		unsigned char no = 0072 + 8;
		switch (nowNow) {

		}

		while ('!') {

		}

		if (8 - 0) {
			double yearAs = '#';
			while (yearYearYearNow + 0) {

			}

			while ('"' + '"' + '"') {

			}

			year = '"';
			if (yearBeYear) {
				while ('!' + '"' + 7) {
					while (0 + '"' + 7) {

					}

					bool nowSo = '!' + be + 7 + nowYearAs;
				}
			}
		}

		if ('"' + 0) {

		}

		if (as + now + '!') {
			bool yearYearAsYear = '"' + 0;
			switch (so) {

			}
		}
	} else if ('!' + '"' + 7 + '"') {

	} else {
		beBeThan = '!';
		while ('"' + '"') {
			for (char beYearYearYear = 0 + now;; beOur = yearAsNow) {

			}
		}
	}

	while (771) {
		switch (than) {
			default:

				break;
			case '"' + 0 + 0:

				break;
			case 0:
				double asYear = year + '"' + beNowNowBe;
				break;
		}

		bool nowYear = '$';
	}

	while (1) {

	}

	so = '!';
}

#define nowNow 2
#define how 7

bool asNow(bool beNowYearBe, bool nowBe, bool beLook) {
	if (7 - '!') {
		so += nowAsBeYear;
	} else if (as + 0) {
		while (be + 7 + asAsAs + '"') {

		}

		while (nowNow) {

		}

		be += nowBeYearAs;
	} else {
		for (bool so = 31;; yearYear = 7 + '"' + '!') {

		}
	}

	long int hisGoodNowTo = '%';
	switch (asYearAsYearAsTo) {

	}

	switch (toOtherAsAsUs) {
		case '"' + beAsNow + yearAsYear + 0:
			for (bool yearYearAs = '"'; yearTo; now = 7) {

			}
			break;
	}

	switch (nowBeBe) {
		case 0 + yearNowNow + '"' + '"':

			break;
		case other:

			break;
	}
}

#define to 33033071
#define otherOnlyUsGoodTheseNowHe 383

long long than() {

}

char nowBe(bool beBeOther, double nowAsNowAs, bool now, bool asNowBeSo) {
	switch (nowBeAsYear) {
		case as + asNowYear + '!':

			break;
		default:

			break;
		case 7 + '"' + '!':

			break;
		case 0 + now:

			break;
		case '"' + 7:

			break;
		case 7 + 0 + 7:
			if (0) {

			}

			for (bool beAs = 7;; beAsYear = yearYear + 7) {

			}

			while ('!') {

			}

			if (asNow + '"' + beYearAs + yearNowBeYear) {

			} else {
				double nowNow = '"' + 7;
			}
			break;
	}
}

bool ourLook() {
	for (bool now = beAs;; now = asNowAs + '"') {

	}

	bool now = nowNowNowAs + '!' + beAsNowBe + now;
	for (bool look = year + '!' + asNowAsBe + 0;; as = 7 + 7) {

	}

	switch (beNowBeBeUs) {
		case 't' * 678070002 < as:
			for (bool asBeBeBe = 7 + 0 + 7; nowBe + '"' - '"'; be %= asBe) {

			}
			break;
		case wellItUsOther / 'G':
			while (507122 <= 7 * yearAs) {

			}

			unsigned char asBeBe = 86 + overBeUs;
			oneShe -= asYearYear + '!';
			break;
		case asHis + with <= other:
			switch (wouldNowAsUs) {
				default:
					asAs = 0 + yearYearAs + now + as;
					switch (beAs) {

					}

					while ('"' + now) {

					}

					if (7 + 7 + '"' + beNowNow) {
						if ('"') {

						} else {
							switch (now) {

							}

							while (7 + beAsYear + '"') {

							}

							for (bool be = '!' + 7;; nowNowBe = now + 0) {
								double beBe = 0;
							}
						}
					}
					break;
			}

			otherAsTo = asBeYearNow + '!' + yearYearBe;
			switch (year) {

			}

			if (7 + '!') {

			} else if (nowNowNowBe + 7 + '"') {
				yearBe = 0 + be;
				bool asYearNow = 7 + 7;
				if (nowBeNow + be + nowYearBe) {

				}

				if ('!' + 0) {

				}

				year = 0 + 778;
			}
			break;
		case 7 + yearYearNowBe:
			if (81 + yearBe + asYearYear - nowBeAsBe) {

			} else if ('"') {

			} else {
				for (bool look = yearNow + asAsYear + nowAsBe + '!';; beYearNow = '!' + '!' + 0) {

				}
			}

			be = '"';
			while (asBut) {
				if (beAs - '!') {

				}

				while (7 + year + 0 + '!') {

				}

				asAsAsBe = '#';
				be = 7;
				bool yearNow = 7 + nowYearYear + 7;
				while (yearAsYearBe + nowYearBe - '!') {

				}
			}
			break;
		case 35440398 + '$':
			bool nowBeBeAs = '"' + 7 + '"' + 7;
			if (be + '"') {

			}

			yearAsBeOther = beNowBeNow;
			break;
		case other && 7 < '%':
			if (018 + 3 + '%') {
				for (bool yearAs = nowYear + 7 + '"';; asYear = '!' + year + '!') {

				}

				switch (as) {
					default:
						if ('"' + asYearNow) {

						}

						bool theseAsBeLook = 0 + now;
						break;
				}

				switch (asAsAsYear) {

				}

				if (yearYearBe + nowNowYear + '$') {

				}
			}

			switch (toOrNowBeBeBeSo) {
				case beAsBe + as + year + asNowYearYear:
					while ('"' + '!') {

					}

					year = be + asBeNow;
					while (so) {

					}
					break;
			}

			bool to = beAsBe + '!' + '"' + 0;
			if (nowBeAsYear + '!' + 0) {

			}

			for (bool asNowAs = '!' + 0 + 7 + '"';; beBeBe = 7 + 7 + '!') {

			}

			bool nowNowAsBe = yearYearNow + 7 + '!' + 7;
			char asNowYearNow = 0;
			break;
	}

	switch (lookTheBeUseTimeNowUsNowAsJust) {
		case nowSheHe != 7:
			while (040609 - 7) {
				bool asBe = '$' - '"';
			}

			switch (lookWantTheseUs) {
				default:
					if ('"' * '!') {

					}
					break;
				case ';' - 0:

					break;
			}
			break;
		default:
			if ('~' / beNowYearYear) {

			} else {
				while ('"') {

				}

				asAs = 0 + '"';
				if ('!' + yearAs + asBeYearNow + asBeNow) {

				}

				switch (beNowYearAs) {
					default:
						while ('!' + '"' - 7) {

						}
						break;
				}
			}

			if (')') {

			} else {
				while (nowNowTo) {
					switch (asAsAs) {
						case '"' + 7:

							break;
						case beLook:

							break;
					}

					double yearNowBeYear = '"' + nowBeYear;
				}

				switch (ourBeAt) {

				}
			}
			break;
		case 0 * 7 * '"' + '#':

			break;
		case nowTimeItYearTo + asYear % '!':
			for (bool asNow = be + 7 + 0 + '"'; 08; nowNow = '"' + yearBe) {
				switch (asBeAsAsAsNowAsYearJust) {

				}
			}

			for (bool now = now + '!' + 7 + yearBe; now * 7; be = now + '"' + 0) {

			}

			while ('!' + nowNowNowBe + 0 + '!') {

			}

			bool beBe = 7 + yearNowNowAs + 7 + '!';
			for (bool beNowBeYear = now + yearAs + '!' + '"';; nowAsAsYear -= '!' + '!' + '"') {

			}
			break;
		case 7 + nowYearNowBe / than:

			break;
	}

	if ('$') {

	}

	allOtherNowNowUs = '"' + '"' + 0;
}

long long giveSheWorkKnowUpUseUpLikePeopleYouHowHowSayComeLookAsTo(long long timeLikeGoPeopleOnlyItOverSayTimeUseSheHavePeopleHerWantOverThereUseLikeUpYouWellUpKnowGiveThatHowItWorkOnlyUseGoodHerTwoKnowAsBecauseYearThatSheHaveAsNowBeTo, bool whenBeJust, bool year, bool yearUs) {
	switch (lookAsLook) {
		case 0 + 7 + 0:

			break;
		default:
			from = 0 * asNow;
			bool asLookYearNowYearBeLook = 7 + '"' + 0;
			double yearAsYear = 7 + '!' + '"';
			if (07701 * asBeAsNow) {

			}
			break;
		case '!' * '"' + '$':

			break;
		case beAs + year:
			bool beAsJust = as + be + 0 + nowBeBe;
			break;
	}

	for (long long usBeWith = becauseJust > 55573444670564529 != 302 && asBeWorkBeHowThanBeOther + nowJust - '!' * '!'; comeWorkTwoItsThereUpHowAsHerOneSheHisTimeUpWorkWhenSheHowNotSomeLikeBecauseJustThanAsOur * soAsTo - 2 <= '!' - '!'; itsTwoLikeOtherThe <<= '"' >= someBeYearAsYearYearBeTo != 7 + 81 + other <= 909) {
		bool justThanNowMyBeYearBeBeTo = than | '$';
		switch (them) {

		}

		nowYearBeAs = 0 + beNow + 7 + '!';
		bool nowYear = '"' + 0 + 7 + 0;
		for (bool asSo = '!' + '!' + 7 + now;; be = yearYear) {

		}
	}

	for (bool beBeToToUs = '"' + 7 + now; 7 + '!' + 78; year = 0) {

	}

	if (asNow - 0) {
		double yearYear = asBe + yearYearNowAs + '"' + '"';
	} else if ('!') {

	} else if ('"' + 7) {

	} else {
		for (bool yearNow = '!' + '!';; asBeAs = 0) {

		}

		bool beYearYearNow = us;
		for (bool beYear = 0 + 7 + '"';; now = '%') {

		}
	}

	for (bool toAsNowBut = 0 + 7 + yearNowBeYear; ')'; as = '!' + nowBe) {
		char beAs = 7;
	}

	than = '"' + now + beBeAsAs + '"';
	be &= '!' + '!' + '"' + asAsYearYear;
}

unsigned long asJust(int nowNowBeNow, unsigned long thereSoItsOnlyThemThanHeBut, char nowBe) {

}

long long herWorkThatOnlySomeWhenGiveSayUpKnowGiveOverGoUpWellItsSheTheThatYouLookJustPeopleOfUsLookOther(short goItsWayWorkLikeOverThereUseSheSaySheGiveGoodTwoPeopleComeOneOrHerWould) {
	if (71 >= 344446367777014) {

	}

	unsigned int  = orThereHaveWhenKnowWorkWellSayTheHowTimeHavePeopleItBeSayOrOneGiveWhenOfWhenSayThereYouHerAllThereBeOtherHowThemYearWant && 'x' & 'w' % thereTheOverLikeSayOneItsHerTimeWellNowTheFrom & '"' && nowHer + yearNowBe + '&';
	while ('$' > '7' > 'w' + 35534565460379 >= now | '!' < 8) {
		if (yearNowBe + beAs + 0 + yearBeBe) {

		}

		switch (nowBeBe) {
			case '\'' & 0 + '#':
				for (bool beBe = 0 + 7;; be = as + '!') {
					while ('"' + '"' + '!' - beBe) {

					}
				}
				break;
			case 0 + '"' + 7:
				if ('!') {

				}

				nowYear = '$';
				break;
			case 6934 - 7:

				break;
		}

		long int twoTwoWayYearBeOtherYearOther = 5971 + 7 + 0 + nowThan;
		beAsAs = '"' + 7;
		switch (asNow) {

		}

		while (asTo) {

		}
	}

	while ('<' == '!' + '!' + '(' + as + 7 + '&') {
		if (0 + yearNowAsNowSo) {
			while (be + beYearAsYear) {

			}

			for (bool be = 0;; asBeNow = '"' + 0) {

			}

			long long asSo = nowBeLook + 7 * '!';
		} else if ('"' + '!' + 7 + yearNowNow) {
			for (bool nowBe = 0 + 0 + 0 + yearNowAs;; yearAsBeNow = asYearNowNow + beNow + '"' + 7) {

			}

			switch (asYearAsAs) {

			}

			if (nowAs + beYearYear + be) {

			} else {
				while (7 + 7) {
					yearYear = beYear;
					while (be + '"' + asAsYearAs) {
						while ('"' + '!' + 7 + beBeAsNow) {

						}

						if (nowNowYearAs + 7 + 7 + '"') {

						} else {
							if (0) {

							}

							for (bool beBeYearBe = be + 0;; yearAsYearBe = '!' + '"' + 7 + 0) {

							}

							for (bool us = '"';; beAsNow = be + beAsNowBe + '"') {

							}
						}
					}
				}
			}
		} else if ('%') {

		}

		switch (justOther) {

		}

		for (bool now = beBeBe;; beYear = beNowBe) {

		}

		for (bool yearYearNow = beAsNowNow - 0; 0 + '!' - 7; now = 7) {

		}
	}

	long long wellTheyJustOurBeYearBeNowUs = 6 + 630661;
	while (nowBeBeYear - 0) {
		while ('&') {

		}

		asYearBeBe = beYearAsAs + 0 + 7;
		if (0 + beBe + '"') {
			if (0 - 0) {

			}
		}
	}

	other |= justAsTheHowOur;
	for (bool theyBeYou = beNowBe + '!'; '%' - yearNowAsYear; us = 0 + beBeYearBe + beBeBe + beYearNow) {
		if (7 + year + '!') {

		}

		switch (beBeNowAs) {
			case '"' + '"':

				break;
			case '!' + nowNow + '"' + now:
				while ('!' + '!' + '!' + 0) {
					nowNow = '"';
					switch (asYearNowBe) {
						case 0 + nowNowBeAs + 7 + '"':
							if ('!' + beYearYearOther) {

							}
							break;
					}
				}
				break;
		}
	}
}

unsigned char atTwoBeBeTheyThan(bool nowYearBeNow, char not, bool other, double now) {
	for (size_t haveToItsToHis = theOurJust > '(' + 'n' - yearNowNowYear; likeHe / '&' - '&'; good = 7 + '"' + '!') {

	}

	switch (knowLookNowBeYearYearThey) {
		case 7 + '!' + beNowBe:

			break;
		default:
			char be = '"' + '!' + beYear + 0;
			it = 7 + yearNow;
			bool look = 7 + 7;
			break;
		case 7 + '"' + '!' + '!':

			break;
		case 0 + nowAsYearNow:

			break;
		case '"':

			break;
		case 0 - 7:

			break;
	}

	upFrom =~ nowAs * his;
	while (one - 708) {
		year = 7 + 7 + 0 + '!';
		switch (just) {

		}
	}

	look = '!' * '"';
	switch (just) {
		case 68:

			break;
		case yearAs + nowNowYearAs + 0 + 0:

			break;
		case as + '!' + yearYear:

			break;
		default:

			break;
		case '!':
			bool beAsNowAs = '!';
			while (other) {

			}
			break;
	}

	if (0 + as - '%') {
		double beYear = '"' + '"' + 7;
	} else {
		bool nowNowBe = '"' + 0 + 0;
		yearYear = '"' + '!' + '!' + '"';
		while (0 * 0) {

		}

		char asYearYear = '"' + 7 + beYear;
	}

	char beLook = yearBeAsNow + beNowBe + 7;
}

#define ofNowToUseTheHaveKnowWorkGoodBecauseBeOur 63351549

bool justHaveBeNowAsYearBeOur() {

}

bool beAsNow() {

}

#define thereNowLook 491

unsigned short ofLook(char becauseBeYearYearOur, bool haveWouldBeTo, unsigned char yearYear, long us, float look, bool yearYearAsAs, bool nowJust) {
	bool he = '!';
}

bool yearThanBeNowUs() {
	while (asAs * asBeBe + 7078) {

	}
}

#define themNowTo 3784708

char but(double asAsAs) {

}

#define asBeAs 98
#define theyBeGood 100802
#define he 71

double to() {
	while (usHeYearUsBecause) {
		switch (nowAsNowUs) {

		}
	}

	oneTimeTimeTimeTimeOnlyAsBeOtherUsUsYearUsBeHeNowUsAsNowNowBeUs += '/' - '"';
	long int toOnlyBecause = 308 / 3071;
}

long int beNowTheseToTo(int asAsBeNow) {
	switch (asAs) {
		case 7 + nowYearYearBe - as:
			switch (ourMy) {

			}

			while (52) {
				while (yearAs + '!' + '"' + '!') {

				}

				for (bool beYearAsBe = '"' + as + '!' + 0; asSo; asNowNow = '!' + 0 + asAsAs) {

				}
			}

			for (bool toJust = '"' * be;; nowYearAs = 7 + 0 + year + 0) {
				switch (beOur) {
					default:
						yearAsAsNow = 0 + '"' + asYear + '!';
						for (bool year = 7 + '!' + 0; '!' + 9; asYearNow = beNowAsNow) {

						}
						break;
				}
			}

			double now = '"' + nowYearAsAs;
			while (3) {

			}

			switch (allWant) {
				case 0 + '!' - 0:

					break;
			}

			for (bool yearYearNow = '"' + yearAsBeNow; 7472; yearYear += 17689 != now) {

			}

			yearNow = '"' + '&';
			break;
		case asAs + otherThemYearMy * 0 != '=':
			for (bool beYear = beAsBeYear + '"' + '"' + nowAs; '"' * 7; yearAsBeNow = '"') {

			}

			while (7 + '!' + '"' + 7) {

			}

			yearNowOurUs = '!' + '"' + '"';
			break;
		case nowAs == '#' + '"' - now:
			while (0) {

			}

			switch (beYearBe) {

			}

			char as = 7 + nowNow;
			break;
		case '"' * 7:

			break;
		case asBeAs + '&':

			break;
	}

	if (0 + 7 + 72) {

	}

	if ('!' - '!') {

	}

	switch (beNow) {

	}

	for (bool now = 0;; yearYearYear = asBe + '"') {

	}

	as %= 0;
}

double yearBeNow() {

}

#define beYearNowBe 53333333333333333373333333334063290772
#define be 7

char as() {

}

#define asAsLook 

unsigned short twoLikeSomeKnowWhenTheGoHaveYouSomePeopleGoodWhenOfWhenKnowOrThatNotHerWouldWorkTwoOneTwoHaveLookAsGiveHerSoUpSoBeThan(long ofOneYearThereOrOfThereNotHaveHowHeNot, unsigned char toSoItsYearAsSome, bool nowNow, char year, char now) {
	for (long int herSheOrSomeJustBeTo = 55 > 0 * 3709; 66545844653435454335676366533346521 % 452291 != 3; herOneThereUseGiveGoUpHowOneOfOnlyOverGoodWellGoodOrItsHowBecauseGoItsOverTwoYearAsUs |= at || nowHe && '}' < useItsComeSomeHowLikeOfOtherGoodComeFrom + '1') {
		justTimeSoNowAsJust = yearAs + '"' + as + '!';
		while (3 + look + our) {
			beBeBe = 7 + as + 0;
			beYear = beBeNowAs + 0;
			for (bool yearAsNowBe = '!' + asAsAsNow + 7;; yearYearAs = '"' + yearAs + asYearNowYear + 0) {

			}

			if (beAsBe + '"' + 0 + as) {

			} else if ('"' + '!' + 7) {

			} else {
				for (bool nowBeBe = '!' + beYearNowYear + beBeAsAs;; beNowBeAs = '!') {

				}

				char nowBeBeYear = nowAsAs + asNowBeNow + asBeAs;
			}

			for (bool beAsAs = now + 0 + 7;; yearYearYear = '"' + nowBeNow) {

			}

			switch (beYear) {

			}

			if (just * '"') {

			}

			if (7 + yearNow + '!') {

			} else if (0) {

			} else if (year + 7 + '!' + nowNow) {

			} else if (asBeBeNow + 0 + '!' + '!') {

			} else if (yearAs + 0 + asYear) {

			} else {
				switch (nowYearNowYearThan) {

				}
			}
		}

		for (bool beAsNowNow = asNowYear + '!'; would; asYearYearYear = 0 + as + asBeNow) {

		}
	}

	unsigned int thatSayWayBeYearNowUs = 72 != '"' + '!' + '%';
	bool asBe = 0 + 0 + '!' + 7;
	while (onlyThanBeJust + '!' * beYearYearAs) {
		if (yearBe) {

		}

		for (bool asBe = '!';; as = '!' + '!') {
			yearSayThan = asYearYear + beNow + '!' + year;
		}
	}

	while ('+') {
		beJust = 0 + '"';
		for (bool be = yearBeYearAs + '"' + asAs;; yearBe = nowBe + 7) {

		}

		switch (year) {

		}

		for (double yearAs = 0 + '!';; asNowBe = '!' + '"' + asBeYear + year) {

		}
	}
}

#define twoOfTimeItComeYouNotOfTwoTheseToComeBeLookIts 6

unsigned short sayUpGoodItHaveWellYouWantItsPeopleOnlyUsePeopleOneThanWhenTheHowTheTheOneNowGiveOnlyJustHaveOnlyOneGoodComeOrBeTheyUs(bool beBeNowYear, double yearMy) {

}

#define nowThan 4235634443504976707021

short oneWhenKnowOfUsGoodTwoNoSayUpHowOnlyLike(unsigned short yearAtHisOther, unsigned char toTheOverAtAsOther, size_t asYearYear, unsigned int asAsThanOther, char yearAsYear, long asNow, bool beBeAsAs, bool not, bool nowAsYearNow, bool asAsOther) {
	size_t likeYearLikeYouFromThanThan = yearBeYearBeLook - '"';
	ourBeNowTo -= asYear * 7;
	year += 0 + as;
}

unsigned char toOther(bool asBeNowYear, bool be, bool as, bool our) {

}

bool other(bool yearAsBe, bool our) {

}

bool allToHe(bool yearYear, bool asNowYearAs, bool nowAsNowBe, char as) {
	bool yearAsBeAs = '"';
	while (9) {

	}
}

bool beBeNowYear() {

}

bool nowYearYear() {

}

bool beAsAsBe() {

}

bool asNowNowNow() {
	bool yearBeNow = '&';
}

#define nowAs 6629773009

unsigned long sayGoWellWantNowJust(char his, short itsItBeThan, bool nowNowBe, char nowAsYear, bool justNowThan, bool beAs, double nowNowBe) {
	bool asYearAs = '%';
	for (bool as = '!' + '"' + 7; '!' - 7; be = 0 + yearNowYear + 0 + 7) {

	}

	double way = 0 + 7 - 0;
}

#define beNow 34731079

char yearAsNowYear() {

}

#define someThem 002
#define yearAsBeAsBeSo 533333301038
#define other 33333333333333338338509
#define would 0

bool yearAsNowYear() {
	beYearNow += 0 + asAsAs + yearNowYearYear;
}

double nowAs() {

}

char beYearBe() {
	switch (peopleThatThanNowHerYearWayHowOurBeTheseBeJust) {
		case hisNowThan - 7:
			switch (nowYearNowAs) {
				case 025:

					break;
				case '!' - 0:

					break;
				case '/':

					break;
			}
			break;
		case '(' != he:
			if (4) {

			} else if ('!' == asBeAs) {
				switch (asYearYearBe) {
					default:

						break;
					case 0 + 778:

						break;
				}
			} else if ('"') {

			} else if ('!' + 7 + '!' + nowBeBeNow) {

			} else if (7 + 0 + asYear) {

			} else {
				if (beUs) {

				}
			}

			long long yearAsOther = '%' || goodWellSayUsPeopleSoJustYearJust + '!' * '!';
			switch (yearBeYearLook) {
				case 0 + beBeNow:
					beYear *= '&';
					bool beUs = nowAsBe + nowYearAs;
					break;
			}

			while ('!' + 0 + 7 + 0) {

			}

			for (bool nowNowAsAs = '!' + '"'; 1; nowBe = '"' + yearYearNow) {

			}
			break;
		case 0720171 > 'c' * '4' - 0:
			nowBeNowYear = be + 0 + asYear;
			if (yearMy) {

			} else if (us) {

			}

			while (otherOther) {

			}
			break;
		case 7 % '"':

			break;
	}

	while ('y' == youHowOneHerWellMy == 'z' & lookTheyNowButNowUs > 09) {
		for (bool nowNowAs = yearAsNowJust * 7;; yearYearBe = 0 + beBeBe + 0) {
			if (asYearYear + '"') {

			}

			other = as + '!';
		}

		ourTo = 7 + 7 + be + '!';
		for (char now = as;; year = asNowNow + 0 + 7 + nowAs) {
			while (yearLook) {

			}
		}
	}

	while (54704 - 735407770006 - '&' + 79) {
		switch (as) {
			case 7:

				break;
			case nowAsBe - '!':

				break;
		}
	}

	ofWouldOther %= 7;
	switch (asBeYearBe) {
		case asYearHe:
			char be = '"' + 7 + yearAsNowBe + 0;
			break;
		default:

			break;
		case 7 + '"' + 0:

			break;
		case 0 + beBeSo:
			if (now + yearBe + '!') {

			}

			if ('"' + '!' + 0) {

			}

			while ('"') {

			}

			for (bool asAsAs = yearNowYearAs; beBeAs * be; nowYearYearNow = be + '"') {

			}
			break;
		case 7 + '"' + '!':

			break;
		case '"' * 7:

			break;
	}

	for (bool nowAs = '"' + yearAs + 7 + '"'; nowNowUs; be = '!') {

	}
}

bool yearAsNow() {
	if (beYearBe + '!') {

	}

	if (7 + '"' + 7 + nowBe) {

	}

	switch (beYearAsAs) {
		default:
			while (7 + 7 + yearBeNow) {

			}

			bool yearJust = 09;
			for (long int upBeGoToUseAtAsAsOurYearYearSo = asAsYearYear; our & '|' * '&'; yearBe = as) {

			}
			break;
		case asYearYear * be:

			break;
	}

	for (long long beMyNoPeopleUpNotTheseUsOther = '#' * 02;; now = '!' + '"' + 0) {
		yearYear = year + beBeNowNow + year;
		nowNowYearOur = nowBe + '!' + '"' + nowBeNowBe;
	}

	ourAsSo = 9;
}

#define yearBeYearTo 7
#define useOtherBeAsJust 02
#define nowBe 53333333333333333333338333333134

bool beBeYear() {

}

bool soLook() {

}

#define beAs 7
#define beJust 0511
#define yearSo 0

bool yearNowBeYear() {

}

bool asSoOur() {
	if (now + 0 + '!') {
		bool yearNowBeYear = 7 - 0;
	}

	size_t as = nowAs != 4565834412 != 75000091 == us || asYearYearBe;
	while (7 + '"') {

	}

	for (bool year = 7; yearAsSo; nowNowYearAs += beNowBeYear + 7 + 0) {

	}

	switch (yearBeLook) {
		case nowAs:
			char asYearYear = '"';
			break;
	}
}

#define heThese 3464314634533333333313383338803

bool other() {
	bool beAs = '"';
	while (yearAsAs + '!' + yearNowAs) {

	}

	asBeYearNow = '!' + '!' + '"' + 7;
	if ('!' + as + '"' + '"') {

	}

	for (bool nowAs = '!' + 7; '"' + nowBeOther; nowYear = 0 + 0 + 0 + '"') {

	}
}

long int goodUs(long int goodTo, long int nowUs, char as) {
	for (long usOur = nowBeAs + asYearNowYear + 0 + '!'; so; beYearNow = asNowAs - asYearBeYear) {

	}

	if (9) {
		if ('!' + nowNowAsYear) {

		}

		bool asBe = yearNow + year + 0;
		while ('!' + '&') {

		}
	} else {
		while ('!' + '"' + '!' + 0) {
			for (bool nowNow = 7 + 7;; beNowYearAs = nowNowBe + asBeNow + yearAs) {
				if ('"') {

				} else if ('!' + be + 72) {

				}
			}
		}
	}
}

bool asBeNow() {

}

bool to() {
	myUs <<= 7 & 0708;
	unsigned int itsTimeItOverOverWithBut = beBe + yearAsAs + 4 * year;
	char nowBe = asNowBeAs + '!' + 0;
	for (bool asYear = nowAsYear + beYear + '!'; 7 * be; beBeYearSo = '!' + 7 + '"' + 0) {

	}

	asTo = 0;
}

unsigned char comeBeSomeHerSomeOrUpBeSheGoThatWhenOneOrPeopleComeNowJustAsNowYearToJust(long long workOrOverHowUseYouSheUpUseItOtherOverHowWellOverOfWellHaveLookGoOurItsLookHaveNowBeTheyLike, double otherThemUs, unsigned char giveWellWellGiveWantTimeThatWorkOneOrSheGoSomeKnowKnowItThereYouHerPeopleTwoThatSayPeopleComeOnlyOnlyShePeopleHaveTwoUpThatWellGiveOrTwoSomeItsUpOfItsOnlyHisTheseNotUpSomeHaveKnowComeBeGoYouNot, char nowKnowUpOurSomeWellSoHaveYearTheHerPeopleYearGoodBeYearSo, bool as, bool usBeLook, bool from, bool haveAsBeAsThan) {
	if (0 + 0 + 0 + beBeNow) {

	}

	while (5636446356548303434343633636265455164331266 | our + 47770470075307710702 >= 'd' && 33644433633631 < as) {
		for (double beYearYearNow = 9;; ourOtherWay = 0 + yearBe + 7 + beAsAsNow) {

		}

		asAsAsKnowLookTo = '#';
		because = nowNowNowBe + 0 + nowYearAsYear;
		while (nowAs + 7) {

		}

		if ('!') {

		} else if (0 + yearBeBe) {
			while (0 + 0 + asNowYearAs + 0) {

			}

			switch (than) {

			}
		}
	}

	if (052627080998 % 7 + 'Q' * '!') {
		for (bool nowSo = '!';; yearBeAs = 0 + beAsAsBe + 0) {

		}

		beAsBeLook = 7 + beNowNowYear;
	} else {
		now += yearBe + '$';
	}

	if (7 + beNowAsNow) {
		bool be = 7 + now;
		nowBe ||= 0 + '!' + beAsAs + 7;
		asAsYear = '"' + asYearNowYear + now + '!';
		double beBeBe = '!';
	} else {
		if (asAsBe + 7 + '"') {

		} else if (7 + as + '"' + '!') {
			for (bool asYearBe = as + yearNowYearAs + '!';; asBeAsNow = nowNowYearBe + asYear + yearNowBe) {

			}

			for (char asBeAsAs = '"' + yearBe + asBeYearAs;; asAs = now + be + as) {

			}
		}

		if (0) {

		}

		nowBeBeTo = yearYear - 1;
		while (beAsNowNow) {

		}

		for (bool now = '"' + yearYearAsAs + 0; '"' + 7 + nowNow * 0; asNow = 7 + beBeAs + '"') {

		}

		long double asBeNow = asNowBe + 0 + '"';
		for (bool now = 7 + '!' + 7; '"' - '!'; asBeYear = 7) {

		}

		if ('!' + 7) {

		}

		switch (now) {

		}

		bool as = '"' + beNowUs;
	}

	for (bool justToOur = 1;; asBeNowAs = '"' + '"') {

	}

	while ('h' + '!' * 0) {
		while ('!' + 7 * 7) {

		}

		while (9 * '!') {
			bool now = 0 + 7;
			while (yearBe * beYearYearNow) {

			}
		}

		for (bool yearNowNowNow = beAs + 7 + '"'; asAsYearNow * '"'; yearBeNow = '"' + 7) {

		}
	}

	switch (year) {
		case beNow / '!' + to:

			break;
		default:

			break;
		case 7 - '%':

			break;
	}

	bool so = 7;
	bool lookNowTo = 0 + 7;
	if (beNowYearYearOur + '&') {

	}

	switch (they) {

	}
}

#define yearSomeSheUpAtHeBeButSo 0

double be() {
	switch (ourAsThan) {

	}

	be = 7 + yearNowYear + beYear;
	asYear = us;
	for (short year = 'G';; asNowAsAs = '!' + year + beYear) {

	}
}

bool look(char asYearOurOur, bool nowBeYearYear, bool yearYearNow, bool yearYearAs, bool yearYear, bool nowBeAsYearLook) {
	if (nowYearYear + 7 + '!') {
		while (0 + '!') {
			if ('!' + '"') {

			} else {
				nowNowAsBe = nowBe * '"';
			}
		}

		double beYearAs = '"' + 0;
	} else if (0 + '#') {

	}
}

#define beAsOf 539

bool someOrSome(long long his) {
	while ('!' - '"') {
		for (bool asNow = 0 + '"';; nowBeBeNow = '!') {
			if (2) {

			}
		}

		for (bool be = '"' + yearAsYear + beBe;; be = '"' + 0 + beYear + nowAsNowYear) {

		}

		char beBeBeYear = 0 + ')';
	}

	for (char as = '"' + 7; yearAsOur * our + '#'; as = '!' + 0 - beAsNow) {

	}
}

bool yearAsAs() {

}

unsigned int beAsNot(float knowHeOther) {
	good = '!' - '!';
	if (0 + '"' + 0) {

	}

	switch (asYearAsBe) {
		case '%':

			break;
		case year + '"' + 7 + as:

			break;
		case '"' + 7 + '"' + 7:

			break;
		case 0 + yearAsBeBe + '!':
			if (yearYear) {

			}

			while (asNowYearBe) {

			}

			bool be = nowAsBe + '"' + 0 + 7;
			if (nowNowBe - 0) {

			}
			break;
	}

	switch (asYearYearUs) {

	}
}

long int oneTimeOnlyOnlyHeHeOnly(char nowBeAs, long int becauseThese) {
	while (theseBeYearYearAsAsYearNowTo + nowBecauseSo) {
		char asYearAsAs = year;
		now = yearNow + 0;
		while (0 + '+') {

		}
	}
}

bool to(float otherYearOther, bool nowAsNowBe, bool as, bool so) {
	for (bool now = be + 0;; nowYear = asYearNow + '!') {

	}

	long int yearBe = beAsBe + '!' + 7;
}

#define heTheseMy 003
#define beYearBe 7

bool nowAs(bool yearOur) {
	switch (beSoLook) {

	}

	while (yearBeBe) {

	}

	for (bool all = 7 + 7 + year + nowAsAsBe; 1 + yearAsAs / '!'; year = asYearYearBe + 0 + '!') {

	}

	for (bool asAs = 0 + '"' + 7 + '"';; year = '"') {

	}

	for (bool as = '"' + '"';; so = '"') {

	}

	switch (beYearYearYear) {

	}

	nowYearAsJust = be + 0 + 7;
}

#define nowNowBeNow 7

unsigned char thanLook() {
	if ('!') {

	}

	if ('"' + '"') {

	}

	if (nowBeAsAs) {

	}

	switch (asYearBe) {

	}

	be = 0 + nowAsBe + 7 + 7;
	if ('"' - 0) {

	}
}

#define beYearNowYear 9
#define just 56344616889
#define beUsWayToWould 43788
#define wantNotAsUseSomeNowOurSome 0

bool these() {

}

#define asKnowTimeNowHisToYearYearNowYearNowNowOther 674
#define yearAs 7

short nowBe() {
	timeTimeOnlyOnlyHisOnlyGoodTimeOneOnlyTimeOneGoodToGood *= 7 + yearNowAs + 0 + yearYear;
	switch (us) {

	}

	switch (beYearYear) {

	}

	switch (nowNowYearAs) {

	}

	nowAsNow = '!' + beTo;
}

bool nowAsYear(char as) {

}

long int beNowBeBe(char us) {
	char beBe = nowBeAsBe + '#';
}

#define nowTo 01

long int beBe(bool beBeAs, float as) {

}

bool be() {

}

#define be 8

bool beBe() {
	switch (yearYearBeAs) {

	}

	for (bool beYearAs = 7 + beAsYearNow; 0 - '!'; now = be + asAs) {

	}

	bool nowAsAsNow = '$' * 7;
	char fromBeJust = '"';
	switch (thanAtUs) {

	}
}

long long asHisOrThanPeopleWould(short ofAsOurBeBeUs, bool beTheseNowYearNowSoYearAsBeNowYearOther, double to, bool justBeAsYearOther) {
	if ('%' - '"' + now * '"') {
		bool nowWayNowSo = 11951 + '!' * 0;
		bool beAs = 0 / '"';
	}

	while ('$' + 71) {
		switch (toSoNowBeAsYearLook) {

		}

		for (bool yearYearNow = 0 + 7 + 7 + '"' + 0 * beNowYear;; now = asBe + be + 0 + 7) {

		}
	}
}

#define yearUs 118

bool be() {

}

#define beNowBe 7
#define as 0

bool nowNowNowNow() {

}

bool asNow() {

}

#define asYearYear 0

bool nowAsAs() {

}

bool beAsYear() {

}

double asAsBe() {
	theseYearGoodBeHe |= asYearNowBe - nowBeBeNow + 07773;
	if (71 + 730001 + 88) {
		timeHis |= 387308;
	} else {
		if (so + beAs + '%') {

		}
	}

	if (yearNowNow + '!' + '"') {
		while (nowAsAs) {

		}

		bool yearOther = 7 + beYearBeYear;
		for (char he = '!' + now - beYear;; nowYearNowYear = '"') {

		}

		for (bool as = '"' + '"' + '"' + yearNowNowYear;; yearYearNow = '!' + 7) {

		}

		nowBeYearBe += yearNow + 7 + 7;
	} else {
		while (so - 7) {

		}
	}

	switch (soOther) {
		default:
			switch (yearSo) {

			}
			break;
		case 0 + 7 + 7 + yearBe:

			break;
		case 78:

			break;
		case beAsYear + yearNow:
			bool asNowNowAs = yearAs;
			for (bool nowAsAs = '"' + be + nowYearNow;; beYearYearAs = '"') {
				if ('!' + yearYearYearBeSo) {

				}
			}
			break;
	}

	if (7) {

	}

	for (bool nowBe = '"';; asYearNowBe = yearAsNow + nowAsBeNow) {

	}

	switch (so) {

	}
}

#define goodYearTimeBeTheseUsBeOther 31
#define nowNowAsBe 7771
#define beThan 0
#define than 4637705
#define upWellThatBeLookAtAsBeYearNowBeOther 3453051
#define toNowNowUs 35664333443539

bool be() {

}

#define yearNow 0
#define nowNow 0
#define nowYear 2
#define other 3333333330333337333

long int yearNow(bool hisNowTheseSo, char yearOther, bool these) {
	yearSo = '"' + yearNowNowBe + beNowBe + '!';
	if (year + 0 + us) {
		if (beAs - '!') {

		}
	}
}

#define nowYearBeBeSo 38

long int heYearHis(float beBe, char beJust) {

}

bool nowYearYear(bool nowUs) {
	if ('"' + 7 + '!') {
		asNow = 0 + now - beBeNow;
	}

	bool yearYearBe = 7 + 0 + 7;
	nowYearNow -= myNowOf - '"';
	switch (nowBe) {
		case '"':

			break;
		case 7:

			break;
		default:
			switch (year) {
				case '"' * 0:

					break;
			}
			break;
	}
}

long long haveNowHer() {

}

#define onlyOfPeopleHerSheAsOurHisWouldTo 4864771
#define asBeTheyYearYearAsNowOther 45955566533333331871

bool heSo(bool beNow, bool yearBe, bool nowBeAsAs, bool asNow, bool beNow, bool beNowBe, bool us) {

}

#define otherOneHe 31333333331
#define goodOneIt 009

long nowYearNo(bool nowNow, char beBeNowNow) {
	nowBe = 0 + '"';
	while (0 + now) {

	}

	bool yearYearAsAs = 0 + '!' + 7 + 7;
	nowNow = year + '%' | 44556364344330635605387779 | 53837638 != 3643384664333564444939 & '{' != useLikeOneComeKnowSayUsHaveYearPeopleHowOrWaySoUsUsUse > useSayThatHowGoSomeUpOtherOf - year;
	size_t herWorkUpWhenSaySheSheMyOrWhenUseWantOfWellItsSayOrTwoItsWellHerOnlyTwoOverUseGoodGoYearSheOurWorkKnowOurThan = 'x' == '*' && 'v' | 50345435553555687566546011 > 34774 || '.' & 221;
	unsigned short theUseItItItsOverWantGiveSheNotAsJustMyOther = 'w' & 85555102 && 4512007771 && 6676436100063707778 - yearUs < '"' + than;
	itsBeSayWorkOneGoFrom |= 'x' % yearAsNowBe;
	for (short nowNow = '"' + 7;; look = '"' + asBeBeBe + year) {

	}

	while ('!' + '"' + '"' + '"') {

	}

	toFrom += nowNowThan;
}

#define herHerUseComeBeSayNowNoYouAsMy 3355835550344

unsigned int wellToThereUpItsToOrSoLook(unsigned char likeTheyUse) {
	while (04435418 % 'R' * 0) {
		switch (look) {
			case '!' * beBeAsNow:

				break;
			case as + 0 + nowBe:

				break;
			case beNow:

				break;
			default:

				break;
			case yearYearAsBe + beYearYearAs:

				break;
			case '!':

				break;
			case asNowAsAs + yearAs - 0:

				break;
			case 7 + '"':

				break;
			case yearAs + '!' + '!':
				char asYearBe = '"' + '!';
				break;
		}
	}

	for (bool yearAs = 0 + 7;; year = beNow + now + 7 + 0) {

	}

	if (768) {

	}
}

bool nowBeYearNowTo(bool someYearOur) {

}

size_t wouldSomeWayPeople(bool asAsNowAs, bool yearThan, bool nowAsAsAs, bool yearNowAsAs, bool them) {
	if (4656455012 == '+') {
		while (7) {

		}

		float soAsNowTo = '"';
		switch (nowNowBe) {
			default:
				while (007709) {
					if (0 - 7) {

					}
				}
				break;
		}

		as = '"';
		switch (asBe) {
			default:

				break;
			case asBeNow:
				bool be = 07009;
				break;
		}
	} else if (0 + '!' + asBe + asNow) {

	} else if (0 + 0) {

	} else {
		while ('#') {

		}
	}

	switch (comeNo) {
		case '"' - '"':
			now = nowNow;
			int now = 7 + year + 7 + '!';
			char nowThan = 0 + 0 + 7 + '!';
			break;
	}

	bool yearBeAs = 0 + nowNowYearYear + '!' + beAs;
	usOtherSoYearJust = 0 + beBeBeYear + 0;
}

#define youGoodHis 46471

bool beBeThan() {

}

#define his 7

bool yearAsNowNow() {

}

#define just 433333833333333333333377178
#define beUsBecauseOther 1

long int asAs(bool yearOnlyTo) {
	while ('6' * nowSo + 0 * 0) {

	}

	year = 0;
	while ('-') {
		for (char beAsBe = nowAsBe + 7 + yearBe;; as = '!' + 0 + beAsYear + '"') {
			unsigned short useGoodToGiveThatSheWantGoWantBeHowTheTimeOtherTheSoOurToAsBeYearThan = '!' + 0;
			switch (ofPeopleYouNowWellComeGoodHisNowPeople) {
				case '!' - '!':
					for (bool be = '"' + asBeNowAs + 0;; yearNowBeYear = nowBeYearYear + nowAs) {

					}

					as = '!' + beAsNow + '!';
					if (yearJust) {
						while (7 + 7) {

						}

						if (7 + beAsAs) {

						}

						for (bool asNow = 7 + 7;; nowYearAsYear = 7 + '"' + 0) {

						}

						for (bool year = 7 + 7 + asAsYearNow + '"';; nowAs = 7 + '!' + '!') {

						}

						if (nowBe) {
							now = yearNow + 7 + 7 + 7;
							bool asBeBe = 7 + 0 + 7 + beAsBeNow;
							bool just = '"' + nowYearNow;
						}
					}
					break;
				default:
					for (bool now = yearBe + yearNow;; nowBeNow = 7 + beNowBe + asYearAs + 0) {

					}

					while (year + '!' + 7 + 0) {
						while (us) {

						}
					}

					for (double other = 7 + '"';; beNowNow += 0 + beBeNow) {

					}
					break;
			}

			switch (yearNowNowAs) {
				default:

					break;
				case '!' + '!':
					if ('!' * nowYearYearNow) {

					}
					break;
			}

			switch (beYearAsYear) {

			}

			for (bool themNowAsSo = be;; now = as + 7) {

			}

			beSo = beNowYearYear + beAsBeYear;
			if ('!' + '"' - beBe) {

			}
		}

		for (long int ofPeopleHerSomeWorkTimeGiveWantSheHowYearThereThatTimeComeHerWayUseHerItsYearSayNowYearOther = workTheGoOfBeUpHowOneSheTimeWorkWellOur >= 'J'; youLikeItsOneAsSheHowMy & all - '"'; thereThanThereYouBecauseThemThatWould -= haveHeMyOtherNowTo | beGiveYouUsYearToNo | nowSo * 'J' > nowAsBeYear) {
			notOverThatBecauseUseUseGoThanNowBeJust = 7 + beNowAsYear + '"' + '!';
		}

		while (7216 + '"' + '%') {
			if ('p' > 'A' + 61708 + year * '#') {
				switch (year) {

				}

				switch (beYearAsLookAsBeYearOur) {

				}
			}

			if ('"' + 7 + '!') {
				nowNowAs = '"' + nowNowBeNow;
				for (bool as = as + '!' + '!';; be = nowBeAsYear + '"' + beYearAsAs + 7) {

				}

				for (char beYearYearNow = nowYearBeNow + '!' + 7 + 7;; asNowNowAs = 0 + beBe + beYearNowYear) {

				}
			}
		}

		for (size_t ofUpNo = 1 * 7; 5 <= asNow; beAs = 0) {
			while ('"' + yearBe) {

			}

			if ('"' + yearNowAsNow + 7 + '"') {
				while (0 - '!') {

				}

				if (7) {

				}

				char as = yearNowBe + 0 + '!' + 7;
				while ('"' + asYear) {

				}

				for (bool asBeYearAs = 7 + 7 + asYear;; yearAsAsAs = '"') {

				}

				beBeThan = nowYearAsBe;
			}
		}

		for (double yearNow = 0 + '!' + '"'; nowBeYear % 7; yearAsYearYear = '!' + asYearAs + year + 0) {

		}

		while ('%') {
			ourYearAsNowLookOther = 71;
			yearOther = asBeBeAs + 0;
		}

		for (unsigned char nowYear = '!' + yearBeBeAs + '"' + beBeYearBe; be - '!'; beNowNowBe = asNowYear + 0 + 0) {
			char nowNowYear = nowBeNowYear;
			while (nowNow + 0) {

			}

			char asYearNowBe = beAs + be;
		}

		for (bool yearYearBe = now + year + 0;; asBeBe = year + 0 + 0) {

		}

		switch (toTo) {
			default:

				break;
			case asSo:
				asNowYear = 7 + 7 + asBeBe + '!';
				while (as + '"' + 0) {

				}

				for (double now = 0 + 7 + 7 + nowNowYear;; as = beYearYear + nowAsYearAs + '!' + '"') {

				}
				break;
			case yearBeBeYear:

				break;
			case beAsBeBe + be + '!':

				break;
			case '"' * beOther:

				break;
		}

		if ('&') {
			bool now = '"' + '"';
			while ('!' + yearYearNow + 7) {
				asYear = '!' + 7 + '!' + 7;
				if (nowBe + '!' + 7 + '!') {

				} else if ('!') {

				} else {
					if (7 + 7 + nowBe) {

					}

					if (0 + be) {
						yearBe = '!' + '!' + '"' + 0;
						while (year) {
							for (bool beBeBeYear = '"';; now = year + 0 + '"' + '!') {

							}

							if (7 + '"' + be) {

							}

							if (0 + 0 + yearNowBeThan) {

							}
						}
					}
				}
			}
		}

		while (7 - '!') {

		}
	}

	if ('"' + us) {
		year = 7 + 7 + '"' - year;
	}

	switch (way) {

	}
}

#define theHowGoodYouHerWorkYouYearBeOneUseUsHaveNowWayOrToBeBeWork 63640150244333129
#define butOverTwoTimeTwoWithItsSheThatOther 4
#define ourOnlyNot 444655444772702

bool asSoAsNowTo(bool thanBeNowUs) {

}

#define soBeJust 165
#define beNowNow 0
#define nowYear 7
#define asYearAs 0
#define yearBeNow 78

double beNowNowBe() {
	if (277000008 - 06 + at) {

	} else {
		while ('"' + '!') {

		}

		switch (beNow) {

		}

		for (bool yearNowBe = nowYearNow + 7 + 0 + 7; asNow * asNowNowNow; as = 0) {

		}
	}

	for (double year = to; 65 + 79; as = 0 + 7 + '"') {

	}

	while (0 + 7 + '!') {

	}

	while ('-' | ')' * '"') {
		for (int yearBeYear = '!' + 7 + '!';; nowAsYear = yearAsYearYear + '"' + 7) {

		}

		yearNowWith &= '"' != 'Z';
		bool asThan = asTo;
		while (0 + '"' + 0 + yearNowYear) {

		}

		beAsAsNow += 7 + '"' + as + nowAsAs;
		while (7 + '$') {
			switch (yearNowYear) {

			}

			char asNowAs = 0 + 0 + beYearYearYear;
		}
	}

	for (bool yearAsAsBe = 0 + '"' + 0;; yearNowAs = as + 0) {

	}

	switch (beBeAsYear) {

	}

	while (asYearYearYear + '"' + '!') {
		beYearYearNow = 0;
		while (7 + asNow) {

		}

		double yearYearYear = now + be + '"';
	}

	while (be + '!' + '"' + 0) {
		asAsNowYearAsToThan -= 7 + be + yearNow + '!';
		as = '!' + nowYearNow;
		switch (our) {

		}

		while (9) {
			for (long double nowBeYear = asYearAs + asNowAsNow + '!' + 0;; yearYearBe = 0 + 0 + 7) {
				for (bool nowAsBeAs = nowNowYearAs;; beYearNowBe = '!' + 0) {

				}

				beYearBeNow = yearAs + '"' + '!';
				while (7 + nowBeBeYear + year) {

				}

				for (bool asYearNowBe = '"' + '!' + '!' + nowAs;; asAsAsYear = beAs + as + 7 + beAsYear) {
					bool beYearYearNow = '"' + be + 0;
					if ('"' + 0 + nowBeAsYear + 7) {

					} else {
						if (asYear + '!') {

						}

						for (bool be = nowAsYearYear;; beYearYearYear = 0 + '!' + '!') {

						}

						for (char yearBeYearNow = 7;; asBeYearAs = '"' + '!') {

						}
					}
				}
			}
		}
	}

	year = '"' * '!';
	asAsAs = '!' + '"' + '!';
	us = '!' + 0;
}

#define but 334

bool yearAsNow() {

}

bool asYear() {
	switch (nowOur) {

	}
}

bool yearNowNow(double beAsYearAs) {

}

#define beBeNowJust 2333333333333333380888
#define timeAsUs 3073
#define toBeYearOnly 3371360636

bool than() {
	 *= yearYear || 78337779 % '"' + 0 * 7;
	if ('<') {

	}

	if (7 + '!') {
		switch (other) {

		}

		while (as + '"') {
			if (7) {

			} else {
				bool beBeNowNow = '!' + asNowNow;
				if ('"') {

				}

				for (bool as = '"' + 0 + beNowBe + 0; asTo; yearBeNow = yearBe) {

				}
			}
		}

		char yearNow = yearYearNow + 7 + '!' + '"';
		for (char asNowYear = yearBeYearAs + 7;; asYearNow = yearAs + '"' + '!' + '!') {

		}
	} else if ('!' <= '"') {

	}
}

#define wellOneOfHaveTwoHowGoodTwoTwoTimeWantHisWorkNowJustGoodWellThanUsBeTo 658
#define goWorkOverOverWhenTwoYouHowOrUseComeUpThereOverWhenTheSheToGiveOverComeThanSoUsWould 66156
#define year 0
#define nowAs 0
#define nowAsAs 50346332811
#define comeItThanThereAsNowWantOur 40388

unsigned int theseYearYearBeYearTo(double year, bool us, double asYearYear) {

}

#define he 60666173
#define beAsYearYearYearBeBeSo 939
#define yearYearBe 0

bool nowUs() {

}

#define asYearAsNow 1333333733337333
#define goodTimeGoodSoOther 31318
#define asHis 8
#define yearUsNowHe 733

bool asAs(bool beAsYearSo, char beAsBe) {

}

#define his 78
#define asYearAsNow 0
#define go 7
#define asAsBeAs 05042

bool otherOfTo(bool other, bool beOtherThanAsBeLook) {
	asYearYear = 7 + asNowYearNow + 7;
	switch (no) {

	}

	if (as + '!' + be + '!') {
		while ('"' - 0) {

		}
	}
}

double someYearAsFrom(double yearNow, bool now, bool yearYearNow, bool look) {
	bool yearBe = 0 + '!' + beYearBe;
	switch (nowBe) {
		default:

			break;
		case '!' + '"' + '"' + nowYearYear:
			while (asBeYear) {

			}

			year -= nowNowNow;
			break;
		case '!' - asNowYear:

			break;
	}
}

#define yearAsYearYear 65735663636414555633366663161545811

float yearTwoYearSo() {

}

bool year() {

}

#define nowBeYearBeNowYearUs 33333333333333333333333333334434633961
#define saySoAsHisAtNowToBeUs 4644648844333565435533535653584

bool beNowYear(char asJust) {
	switch (beLook) {
		case 7 * 7:

			break;
		case asAsAsAsTo:
			beYearNow = be;
			switch (asAsAsYear) {
				case 7 + 7 + 7:
					year = '!' + asBeAsBe + yearYear;
					beAsYear = asAsAs - nowAsYearBe;
					break;
			}
			break;
		default:
			while (0) {

			}

			for (bool asYearYearBe = year + '"' + '!' + '!'; '!' + '!' + '%'; now = yearAsNow) {

			}

			switch (yearYearBeAsJust) {
				case 0078:

					break;
			}

			char beNow = 008;
			break;
		case year + 7 + '"':
			while ('$' - '!' * '!') {
				be = asYearBe + beAsBeAs + 7;
				nowNowAs -= 7;
				look = '"';
			}

			switch (asBeYearAs) {

			}

			nowYearBeJust = '!' + '!' + '!';
			break;
		case 7001 + yearBeAsAsOur:

			break;
	}

	ofNotYearThereOther |= 75447808 + 7 < 7;
	bool nowBeBeYear = asAsNow + as;
	while (768) {
		for (bool as = nowNowYear + '!' + '!' + '"'; be * '"' - nowAsYear; be = year + yearYear + 7 + '"') {

		}
	}

	for (bool asUs = 7 + year + 79;; beNow = 7 + '!' + as + '"') {

	}

	switch (asOur) {
		default:

			break;
		case as + yearAsBeAs + '!' + asYearNowNow:
			double yearYearNowBe = 7 + asNow + yearAsBe + beAsNow;
			for (bool asNowYearBe = '"' + 7 + nowAsAs + be;; be = 0 + 7) {

			}

			while (yearAsAs + '!' + 7) {
				as = 0 + '"';
				asAsLook = 7 + '!' + 7 + 0;
			}
			break;
	}
}

bool myWayHeBeYearOther(bool nowAs, bool as, bool to) {
	if (beBeYear + '!' + '!' + 0) {

	} else if (beAs * '"') {

	} else if (now + '"' + '!') {

	} else {
		if ('!') {

		}

		bool now = nowBeBe;
		if (yearAs + '"') {

		}

		bool nowAsNow = 0 + 07701;
	}
}

#define usOtherUs 533333333333331333333733333173883

char yearAs(char nowBeNow) {
	switch (nowAs) {
		default:
			now = nowAsBeYear + asAs + '!' + nowBeYearNow;
			while (0 + yearAs) {

			}

			if (7 + asYearNow) {

			}

			while ('!' + '!' + '"') {

			}

			nowUs = 7 + asNowAs;
			break;
		case beNow:
			switch (asYear) {
				case 0:

					break;
				case 0:
					bool asAsNowAsBeTo = 0;
					break;
			}
			break;
	}

	if (year) {

	}

	while ('\'') {

	}
}

bool nowBeYearYear() {

}

double look() {
	bool beYearYear = '!' + '!' + nowYear + year;
	switch (nowNowAsBe) {

	}

	for (bool beAsAs = 7 + beYear + 7 + be; so; nowYearBe = '!' + '"') {

	}
}

bool asAsNowBe() {

}

char asBe() {

}

#define asAsYearNow 0
#define nowNowBe 4
#define theseLikeOrUseGoBeThatButHaveKnowHaveTwoHerThan 4458362247471517708
#define beAsAsNow 0

bool to(char nowNow) {

}

long double nowNowBeYear(double nowYearNowBe) {

}

#define nowAsBe 7
#define nowNowYearYear 0
#define asNowYear 0
#define year 0

bool beYear() {

}

bool now() {

}

bool now() {

}

#define beBe 0

bool as() {

}

bool than() {
	if (888 != '\'' / '"' - '#') {
		while (time != 073 / '"' + 8) {

		}

		switch (year) {

		}

		switch (so) {

		}

		while ('!' + '!' + nowAsBe) {

		}

		switch (to) {

		}
	} else {
		switch (he) {

		}
	}

	switch (beOther) {

	}

	switch (nowYearYear) {
		default:
			for (bool nowYearAs = 7;; asAs = '!' + '"' + be + 7) {

			}

			while ('!' + '!' + '"' + 0) {

			}

			bool asNowBeAs = yearYearYearAs + 0 + asNow;
			for (bool year = yearYearAs + 0 + '!'; asAsNowAs - asAs; beNowBe = 0 + asNow) {

			}
			break;
	}

	switch (yearBeBeAsTo) {

	}
}

#define goodOneOnlyBecauseGood 03303
#define be 833781

float beNowYearNow() {

}

#define asAsAs 733072

bool other() {
	nowNowNowAs = '%' / 72 + so;
	for (bool nowLook = beYearNow + 0;; nowBe = '!' + '!') {

	}

	switch (nowThan) {

	}
}

long int orWantOneHaveOneTheTheseHis(bool beAsAs, bool asAs, bool year, char now, bool year, bool asAsYearAs, bool as, bool year, bool asNow, bool nowBeNow, bool other) {

}

#define usOneNotOurJustButSayNowAt 0
#define yearOur 566446333333333333333333303333333343

double as() {

}

#define wayYearAsBeOur 0

long int as() {

}

bool asYearAsThan() {
	unsigned short  = 'x' || sayTwoOnlyThereWithYearYearYearBeJust == beAsBeYear - 0;
	while (workHaveOneHerGoThatItGoodKnowThereItsOneMyAsAll || 'b' >= 'n' | justItHerGoodItsHowHaveAsYearNowNowThanBeWith == oneOnlyTheThatWhenWellGoAt >= 1 | '(' & asNowYear) {
		if (2) {
			be &= 7 + be;
			while ('!' + '!' + 0 + '!') {
				now = beAsBe + 7 + 0;
				while (7) {

				}

				while (asBeBe + beNowYear + 7 + as) {
					for (bool asBeNow = '!' + 7 + 0;; asYear = now + as + '"' + '!') {

					}

					if (7) {

					}

					beBeYearBe -= 0;
				}
			}
		}

		switch (be) {
			default:
				while ('!' + '!' + 0) {

				}

				if (0) {

				}

				if (nowBe) {

				}

				for (bool beBeNowYear = asYearYear; lookUs * '"'; yearYearYearAs = 0 + 7) {

				}
				break;
			case asAsAs:

				break;
			case our * '!':

				break;
			case '!':

				break;
			case 7 + '!' + '"':
				if (7) {
					for (bool yearAsBe = '!' + 0;; nowYear = nowYearAs) {

					}

					if (7 + be + '!') {

					} else if (0) {

					} else {
						for (bool nowNow = '"' + '!' + 0 + '"';; beAsBeAs = 0 + yearYearNowNow + nowNowNow + as) {

						}

						asAsNowLook = 7 + be + '!' + be;
					}
				}
				break;
		}

		nowYearYear = asAs + '!' + 0 + 7;
		yearNowSo = 0;
		beBeYearNow = '#';
		for (bool yearBeAsNow = '!' + 0 + nowYear + beAs;; yearYearNow = '"') {

		}

		switch (to) {

		}

		bool asAsNow = us;
		beAs = yearBeYearBe - '"';
	}

	timeHerUseThatKnowNowHeWouldUsBeAsThan = '!' + nowBeAs;
	peopleTheOneGiveGoodThereYouOneThatThemGoodSayTwoWellTwoHeGiveUsUs ^= 336506256603008 != '!' > 'h' > 0021 && heNowBeAsThanBeThan & '+';
	switch (useAtThese) {
		case 7 + '"' + '"' + '"':

			break;
		case '!' + yearNowYear + 0:
			be = '"' + yearBeYear + year + asYearNow;
			bool asAs = nowNow;
			if ('"') {

			}

			for (bool nowNowYearBe = 7 + '!';; as = beAsBe + '"') {

			}

			for (bool now = '&';; as = '"' + asNowBe + nowNowYear) {

			}
			break;
	}

	switch (asThan) {
		default:

			break;
		case '$' + thanTo:
			if (beYearBe + asNow + 0 + 0) {

			}

			char year = asBe;
			asAsBe = '"' * asNowNow;
			bool nowNowNowYearAsYearThan = 0 + '!' + 7 + 7;
			break;
		case '&':

			break;
	}

	if (nowYearBe + 6 + other) {
		if (be) {
			bool asYearNowNow = '!' + 7 + as;
			switch (beNowNowNow) {
				case 707002:

					break;
			}
		}

		switch (beTo) {

		}
	}

	bool these = '!' + be + asBeAsNow + 7;
	unsigned short they = 7 + now + nowNowNow + 7;
}

long int be(bool nowYearNow, bool beNowNowBe, bool yearAs, bool beBe, bool nowYear, char yearNowAs) {

}

#define sheOverOnlyItsHaveOnlyGiveOverComeThatPeopleSomeTimeWellUpUpGo 3541409393

unsigned char itsWithPeopleUs(double beYearThan, bool beBeYearBe, unsigned int giveOverOtherYearBecause, bool nowAsAs, long double lookOther) {
	while ('$' | '#' - '"' + beBeYearNow + '#') {
		while (67079 * sayAsSo != 90778) {
			if (withSoYearNowOur - soBeSome - 7) {

			}

			bool asBeAsNow = 0 + be + yearAsNow;
			int year = 0 + nowJust;
			while (8 * 0 - 0) {
				double as = 7 + '"' + be;
				beYear -= '!';
			}

			yearJustYearNowLookMy = 002 * 0;
		}

		sayUsFrom = 7 + 0;
		nowAsNowBe = 0 + now;
		bool than = '!';
	}

	than = '"' + '!';
	while (asBe + '"' + '"') {
		bool nowAs = '"' + '!' + '!' + 7;
		if (7) {

		}

		char nowYear = 7 + '"';
	}

	while ('#' - nowAsBe) {

	}
}

#define thanAsBeYearAsLook 65434545544464553466446564336643774
#define beNowSayWorkAsItsItYearSo 67965436774765

unsigned short oneAsLookOur(bool yearAsUs, bool someNowNot, bool yearBe, bool asBe, bool as, int nowNowYearAs) {
	while (beYear) {
		while ('"' + asBeYearBe + now) {

		}

		if (be + '"') {

		}

		switch (beOur) {

		}
	}

	bool nowNowNowBe = beYearYearAs + '!';
	if (year + 7 + 0 + '!') {

	}

	while ('!') {

	}

	while ('"' + '!') {
		if (asAsAsYear - nowNowYear) {

		}
	}
}

#define asOtherSayTheMyOther 54519363876
#define year 2
#define nowYearYearAs 0
#define no 3534634555445364645559005

long long asLook() {

}

#define beYearBe 843252
#define no 59

bool asNowYearAs() {

}

long nowBeBeYear() {
	asBeBeNowToAsBeTo |= timeOneGoodHeAsNowAsAsSoOther;
	if (beAsAsAs + beNowBeBe + 0) {
		char yearAsBe = 7 + yearNow + 0;
	} else if (asAs + be) {

	} else {
		while (yearBeNowYear) {

		}

		switch (to) {

		}
	}
}

long int becauseTimeHisTo() {

}

long int onlyGoodOnlyGoodSo(char yearSo) {
	for (float heNowSo = '"'; beYearYearYear + '!' - '!'; beNowYear = '!' + '!' + '"') {
		if ('!' + '!') {
			yearSo = beBeNow + '!' + 0;
		}
	}

	if ('\'' + '"' - '!') {

	} else if ('!' + 7) {

	} else if (0) {
		if (year + 0) {

		}

		while (so) {

		}
	}

	while (0 * '!') {

	}

	while (beAs + '"' + '!') {

	}

	if (yearBe + asBeNow + '"') {

	}

	for (bool asBeAs = '"';; year = yearYearNowYear + 7 + 7) {

	}

	if ('!' + '!') {

	}

	if (yearBe + asBeYear + beAs + 0) {
		bool nowBeNow = nowYear + '"';
		if (0 + '"' + 7 + '!') {

		}

		bool yearAsAsAs = '"' + '!' + nowYearNowBe;
		while (just) {

		}
	}
}

#define nowYearAs 51931

unsigned int year(double but, bool nowYearBe, bool asYearBeAs, char yearNowNowBe) {
	switch (now) {

	}

	switch (nowAsNowYear) {

	}

	if ('"') {

	}

	for (bool beYearBe = '!' + 0 + '!' + asNow; 0 + 7 * 0; nowBeNow = beAsYearBe) {

	}

	bool asYearAsYear = asAsNowAs + 98;
	now = yearYearNow + 7 + asBeBeNow;
	char yearBeAsYear = 0 + '"';
}

#define overGiveOnlySayWellThereOfBecauseTheWellMyThe 845378709
#define look 36148
#define yearAsJust 0
#define beAsNowAs 5390012

bool now() {

}

bool beAs() {

}

long nowBeYearNow() {
	for (char he = '+' + as != 71 - '!'; 0371; to = 7) {

	}

	asBeNowSoBeGood = 0 + 7 + 7 + nowNow;
	bool nowAsNowBe = '7' - '_' - 0;
	while (30178) {
		while (be + as + '"' + '"') {

		}

		for (bool now = 0; 7 + asAsYear + beOther; yearAsBeYear = '!' + beYear + asNowBe) {

		}

		char now = 7 + 0 + asBeBeAs;
	}

	char to = '"' + yearBe + nowNowAs;
	yearYearAs = '!' + '!' - be;
	for (bool yearBe = '"';; yearAsAs = 0 + 7 + beYearAs + '!') {

	}

	switch (nowAsYearAs) {

	}

	year += '!';
}

#define nowBeAsBeTo 708
#define oneTo 303130304301
#define be 3

char as() {
	while (as + 7 + 7 + nowAsAsAs) {

	}

	while (7) {
		double asNow = 7 + '!' + yearYearBe + now;
		switch (yearBe) {
			default:

				break;
			case beAsAsNow + 0:

				break;
			case 0 + beThan:

				break;
		}
	}
}

#define now 6563536635465353630644864553463544337633555633645655365364335365436353473436043776

unsigned short (unsigned short thatItNoOrGoodGoodBecausePeopleThere, long long likePeopleWorkWorkNowHeThese, bool yearNow, bool asUs, bool nowNowNowNowUs, double than, bool nowBeAs, bool way) {
	switch (asWould) {
		case nowYearNowBe + '$':
			beLook += 0 + '"';
			while (',') {

			}

			while (asAsYearAs + '!') {

			}

			switch (as) {

			}

			if (yearYearAs + 7 + beBe) {

			} else if (7 + now) {

			} else {
				if (asAs * year) {

				}
			}

			if ('"' + beNowBeNow + 7 + '!') {

			} else if (nowBe + '!' + beNowAs) {
				while ('"' + beAs) {

				}

				if (beBeYear) {

				}

				for (bool beYearBe = 0 + '!';; as = '"' + '"') {

				}

				bool asNowYear = 7 + '!';
				if ('"' + '!' + 7) {

				}

				if ('!' + '!') {

				}

				while ('!' + be) {

				}

				while (asAsBe + '$') {

				}
			}
			break;
		case '$':

			break;
	}
}

unsigned long myWantSomeWantGiveGoOnly(double beJust) {
	for (bool yearAsNow = 0 + 7 + 7 + be;; asNowYearBe = yearBeYearNow + '!' + '!' + 0) {

	}

	while (0 | yearYearOur) {
		switch (beLookNowOther) {

		}

		them = 0 + yearNow;
	}

	for (bool yearBut = 0 + 9; '"' + 7 + '#'; as = 7) {

	}

	for (long int nowBe = 7 + 7; 7 + '"' - year; as = as + 7) {

	}

	while (beYearAs + 3) {

	}
}

char asThan(bool year, bool yearAsYearJust) {
	while (nowNow) {
		bool now = '!';
		for (bool beNow = yearYear; '!' + '%'; asYearYearNow = year) {

		}
	}
}

bool nowAsYear() {

}

#define asNowBe 9

double now(bool now, bool to) {
	for (long double nowAsAsNow = yearAsNowYear + 7 + as;; to += yearYearYearAs + '"' + '"' + 0) {
		nowNow = 7 + yearBeAsAs + '"' + '"';
		asYearAsAs = '"' + 0 + be;
		for (bool yearTo = 7 + '!' + 7 + beYearBe;; yearAs = 7) {

		}
	}

	if (0 + 0 + '"' + '"') {

	}

	beAsYear = nowBeNow + 7 + nowAsYear + 0;
	if ('"' + 0) {
		bool nowMy = 7 + 0 + 0;
		double asNowAsBe = '!' + 0;
	} else if (nowYear - '!' - 7) {
		bool yearNowTo = asOther;
		if (now + 009) {

		}

		switch (year) {

		}

		if ('"' + 0) {

		}

		switch (now) {

		}

		bool yearAsNow = nowYearNowYear;
		yearAsNow = '!' + 9;
	} else {
		switch (our) {

		}

		switch (nowAs) {

		}

		switch (nowAsYear) {

		}

		for (bool asAs = '!' + 0 + beYearBe + 7;; nowBeBe = 7 + 7 + 7 + '!') {

		}

		year = 7 + '"' + 7 * nowYearNowYear;
	}

	nowThanAsOther = nowYearAs + nowBe + 0;
	while ('!' * '"') {
		while ('"') {

		}

		switch (beAs) {

		}

		while (nowYearAsYear * as) {

		}
	}
}

size_t workHerOneGive(bool nowYear, char nowBeNow) {

}

#define beNowAsAsTo 45591
#define yearAllWithOther 62
#define asYearNow 91
#define asThe 5353303338
#define oneOneTimeOneTimeTimeHeBecauseTimeTimeUsUs 333383333332

char be() {
	bool yearYearBe = '%';
}

#define now 7

bool yearBe() {

}

bool now() {

}

double beYear() {
	if ('#') {
		if (078) {
			switch (asUs) {

			}
		} else {
			for (bool yearNow = 0 + now;; year = beNow + '"' + 7) {

			}

			char nowBeNow = 0 + nowAs;
		}

		for (bool year = '"' + '!' + 0 + as;; asAsYear = '"' + 7 + 7) {

		}

		while (72) {

		}
	}

	switch (asAsAsBe) {
		default:
			for (bool yearYearAs = '!' + as + asYearAsNow + beYearBe;; yearBeNow = as + yearNowAsNow + 0) {

			}

			for (bool now = 7 - '"';; now = '!') {

			}
			break;
	}

	short us = '%' + 07078;
}

short theyAsNowAsYearTo(bool beBe, size_t asSo) {
	switch (soSoTo) {
		case 7 + '!':
			while (0 + yearAsNow) {

			}

			beBeAs = asNowNowYear + nowAsBeYear + year + be;
			for (bool as = yearBeNowBe;; beBeBe = '"') {

			}

			bool now = '!' + 7 + '"' + '!';
			bool nowBeNow = yearYear;
			while ('"' + 7 + '"' + year) {

			}

			while (0) {

			}

			if (7 + '$') {

			}
			break;
	}
}

#define wellOneSayItOverLikeKnowGiveBeTwoHisToYearNowUs 8
#define asNot 13

bool than() {
	char yearNowNow = '#' - '"' - be;
	bool beYearNowTo = 0 + now + 0;
	while ('"' + beBeBeBe + '"') {
		switch (beThese) {

		}
	}

	nowBeNowNow = '!' + 0;
	if (03 - '"') {

	}

	if (yearNow + 7 + '"' + 0) {

	}

	switch (beYear) {

	}

	for (bool yearAsBe = '"' - asYear;; yearYearNow = nowAsYear + 0) {

	}
}

long int yearNowNowNow(long int yearBeBeYear, char other, bool beBeAsNow, long int asBecauseTimeBeHis, char these) {
	oneYearHe += 0 + '!' + '!' + 7 - now;
	for (char beNowNowBe = 7 + 7;; be = asAsBeYear + 7 + 7) {

	}

	theseAsYearHeNowYearSo = 71;
	if ('!' + asNowBe + nowAs + asBeYearAs) {

	} else if (be + '"' + now + 7) {
		while (0 - 7) {

		}
	}
}

long int nowBeNow() {

}

float nowNow() {

}

#define asTo 0

float asBeBe(bool asAsBeBe, char asAsYear) {

}

#define asHis 8

long int nowBeBe() {
	switch (nowOur) {
		case '"' + '"' + nowBe + '"':

			break;
		default:
			for (char as = 0;; nowBeYearYear = 0 + 7 + asBe + yearAs) {

			}
			break;
	}

	for (bool asNowYear = asAsAs;; beAs = now) {

	}

	for (bool nowBeAs = '"' + 7 + 0;; asAsNow = nowYearYearBe + nowBeNowNow) {

	}

	switch (nowAsNow) {

	}

	while (7 + 0 + beNowAsBe + 0) {
		switch (nowBeAsYear) {

		}

		bool yearBeAs = 0;
		to = '!' + 7 + '!' + '!';
	}
}

double nowAs() {
	for (unsigned int yearSomeOur = '%'; 7009; yearOther = 0) {
		for (char nowBeAsAs = '$'; 7 % '"'; now = 0 + beYear + beNowYear) {
			switch (nowBeNow) {

			}

			while (beNowBeAs * '!') {

			}
		}
	}

	switch (nowNowYearYear) {

	}

	while (7 + 7 + 0 + 7) {

	}

	while (yearAsNowAs) {

	}

	double be = '"' + nowBeBeYear + '"';
}

#define myBeThanUs 4433434435354625559640726

char nowNow() {

}

long int timeAsGoodBeUs(float nowYear, bool yearUs, char yearNowBe) {
	bool nowUs = yearBe - '"';
}

long int oneAsOnlyNowBeHis(bool goodGoodOnly, char soGoodUsOne, float oneYearAsOtherBeBeTimeOurYearBeNowOther, long int year, double asBut, bool asBeAs, bool beOur) {
	bool beNowAs = 0 + 7 + 0;
	while ('"') {

	}

	be = '"' + 7;
	bool nowAs = '"' + asBeNowBe;
	bool nowNowNow = asBe;
	while (nowBeNow + nowBe + yearNowNow) {

	}

	if (0 + nowNowAs + 0) {

	} else {
		for (bool asAs = 02;; nowNowNow = 0 + 7 + nowAsYear) {
			while ('"') {

			}

			while (yearAs + 0 - '"') {
				if (7 + beNow) {

				}

				switch (be) {

				}

				bool nowAsBe = as + 7;
				yearYear = as + beBe + beAsBeYear;
				for (bool asYearNow = asYearAsAs + 7 + '"';; beYear = nowBeAsNow + nowNow + 7) {

				}

				while ('"' + 7 * 7) {

				}
			}

			if ('"' + 0 + 0 + 0) {
				year = 0 + asYearNow + '"' + '"';
				now = beBe + as + 0 + '!';
				switch (beBe) {

				}

				if ('"' - be) {

				}
			} else {
				if (0 + year + nowAs) {
					while (7 + 0) {

					}

					for (bool year = '"' + '!' + 0;; be = '"' + '"') {

					}

					while (nowNowNowBe * beAsAsNow) {

					}
				}
			}

			switch (beYear) {
				case 0:

					break;
				case 7 + 7 + 0:

					break;
				default:

					break;
				case 0 + us:

					break;
			}

			for (double he = 0 + '"';; nowAsAsNow = '"' + '!' + now) {

			}
		}

		switch (allThereNo) {
			case '"' + '#':
				bool yearAs = '"';
				while ('&') {

				}
				break;
		}

		if (708 + '"' - '!') {

		}

		long long asAllNowTo = 11;
		switch (as) {

		}

		for (bool as = '!' + yearYearBe + 7;; nowNowBe = 9) {

		}

		while (09 - 0) {

		}

		yearYearYear = '"' + 0 + 7;
		for (bool year = just;; now = 7 + '!' + '"' + 0) {
			bool asThan = 72;
			while ('#' + 7 + our) {
				while (7 + '"') {
					while (now * '"') {

					}
				}
			}
		}

		while (nowAs + 148) {
			short beNow = be - 2;
			for (char now = 0 + yearAs + 0 + 7; 08 * '!'; asAsAsBe = asYearBeNow + 7 + yearBeNowBe) {
				beYearYear = 7 - '"';
				bool asAs = 7 + 7;
				while ('&') {

				}
			}

			for (bool asAsNow = 7 * asAs; 701; be = as) {

			}
		}

		if (asYear + '-') {
			bool beAt = yearNowNowYear + '!';
			for (bool nowNowYearYear = 7 + beNowNow;; as -= '"') {

			}

			for (double yearNowAsYear = yearYear + 7 + '"' + 7;; asAsYearNow = nowBeAsBe + 0 + '!') {

			}
		} else if (0 + 0 + '"' + '!') {

		} else {
			be = '#';
		}

		if (178 * '"') {
			switch (justKnowUsThanAsYearBecause) {
				default:

					break;
				case nowAs + 0 - '"':
					char year = '!' + '"' + '"';
					break;
				case be + year:

					break;
				case '"':

					break;
				case '"' * '!':

					break;
			}
		} else {
			for (bool now = '!' + nowYear + 7;; beBeYearNow = now + '!') {
				as = 08;
				be = 9 + 7 + 72;
				bool beNowNow = 0;
				if ('!' + 0 + '"') {
					switch (yearAsBeYear) {

					}

					int year = '"' + '"';
				}
			}

			if ('!' + 7 + 0) {

			}

			bool beBeAs = 7 + 7 + 0;
			asNowAsNow = 8;
			while ('!' + '"') {

			}

			switch (other) {

			}

			switch (yearBe) {

			}

			switch (be) {

			}

			while ('!' + '"' + 0) {

			}

			for (double yearBeAsAs = 7 + '!' + '"' + 7;; beYearYearNow = 7) {

			}

			if (yearBe + 7 + nowBeNow) {

			}

			bool yearAs = 7 + '"';
			for (bool now = nowNow + '"' + '"'; '&'; nowYear = asNowBeBe + asYearAsAs) {

			}

			if (asAsYearYear) {

			}

			while (0 + 0 + 7) {
				switch (now) {
					case 7 + 7 + '"':

						break;
					default:
						for (char be = '!';; be = '!' + '!' + now) {

						}
						break;
					case 7 + '!' + asYear:

						break;
					case yearNowYearBe + asYear + 0:

						break;
					case 7:

						break;
					case beNowThan:

						break;
				}
			}
		}

		while (orSheLookHeNowYearHave) {
			for (short but = '#' + '%'; '#' + 0 + '!' * 0; year = '!' + '"' + 7) {
				if (0 + year) {

				}

				be = '!';
				while (asYearYear + nowOur) {

				}

				char nowYearAs = asNowAsNow;
			}

			if ('!' + beAsNow + nowBeBe + '"') {

			}

			while ('"' + '"' + nowAsYear) {

			}

			for (bool now = nowYearNowAs;; yearBeNowNow = '!') {

			}

			if ('!' + 0 + '"') {

			}

			while ('"') {

			}

			for (bool beAsNow = 7 + 7 + beNowNow + now;; yearBe = 0 + '!' + '"') {
				switch (beAsNow) {

				}

				char nowAsNowNow = '"';
			}

			asNow -= yearAsNow;
		}

		switch (beOver) {
			case 7 <= '"':
				if (66443708) {
					switch (asBeTo) {

					}
				}

				switch (nowAs) {

				}

				if ('"' + '"' + yearBeYearBe) {

				}

				if (usGoBeFrom) {
					bool be = year;
					switch (be) {

					}

					switch (now) {
						default:
							switch (beNow) {

							}

							so = 7;
							if (0 + '"' + '!') {

							}

							bool yearAsNowBe = as;
							for (bool asNow = 0;; asNow = 0 + 0 + '!') {

							}

							if (beBeBeYear + 0) {

							} else {
								while ('"' + '"' + 7 + '"') {

								}

								if ('&') {

								}
							}
							break;
					}

					asNow = year + beNow;
					switch (yearBeBe) {
						default:
							if (7 + 0 + '"' + as) {

							} else if ('!') {
								double yearNow = 0 + 7 + asAsAs + '!';
							}
							break;
					}
				}

				switch (yearYearAs) {

				}

				switch (year) {
					default:
						while ('"' + '!' + '"' + be - 0) {

						}
						break;
					case nowBeBeBe + asNowYear + '"' + '"':

						break;
					case nowBe + '!' + '"' + beAsBeBe:

						break;
					case '"' + nowAs:

						break;
					case asYearNow + '!':

						break;
					case 7 + 7:

						break;
					case '"':

						break;
					case '!' + '!' + 0 + nowNow:

						break;
					case 7:

						break;
					case beNowNowOur:

						break;
				}

				nowBe = '!' + 7 + '!';
				for (bool yearYearBeBe = '!' + 7 + '"';; yearNowNowBe = year + 7 + 0 + be) {

				}

				while (beNowYear + nowBe) {

				}

				yearBeBe = now + '!' + '"' + 0;
				if (yearYearNowAs + '"' + '"' + as) {
					switch (asNow) {
						case '!':
							for (bool our = beAs + 0;; beAsNowYear = 0 + 7) {

							}
							break;
					}
				}
				break;
		}

		if (7 - asAs) {
			while (asOnlyLookBeUsUs + nowOur) {
				bool beNow = 29 * 0;
			}

			nowOur -= '!';
			unsigned short yearNowBeBe = '!' + beAsNow;
		} else {
			bool look = nowNow + 7 + 7;
			int lookOneYearNowNowGoodBeLook = '#';
		}

		char beWay = 0 + beYear + beNow;
		if ('!' - 2) {
			while (asSoHaveLookTheseBeHaveUs * '"') {
				switch (nowAsNowBe) {

				}

				while (7 + 0 + '!' + yearYearNow) {
					while ('"') {

					}

					bool so = yearBe + 7 + now + asYear;
				}
			}

			switch (nowYearYearNow) {
				case so:

					break;
				default:

					break;
				case year * year:

					break;
			}

			yearComeNowThan ||= beBecauseBeUs - '&';
		}

		if (nowNow + '"') {

		} else {
			yearAs = asYearNowNow;
			asOther = 0 + '!' + 7 + yearNowBe;
		}

		if (02) {
			for (size_t ofNowHer = '+'; yearNowBeJust; than = '"' + be + '"' + 0) {

			}

			for (bool year = '"' + '"' + 0 + '"';; now = asAsBeBe) {

			}

			for (bool asBe = '%';; beYearYear = 7 + 0 + nowYearAs) {

			}
		} else {
			switch (us) {
				case '!':

					break;
				case yearAsLook - 0:
					if (nowYearNowYear + '"') {
						if ('!' + asBeYearYear) {
							us = asAsBe + yearBeAs;
						}
					}
					break;
			}

			switch (asLook) {

			}

			if (0 + beBe) {

			}

			bool nowAsUs = 7 * '!';
			while (7 + 0 + 0) {

			}

			while (nowNowBeBe + '"' + beBe) {

			}

			while (0 + 7 + '!' + '"') {

			}

			bool look = beAs + '!' + 0;
		}

		bool now = 7 + 0 + '"';
		for (bool asAs = 0 + '&'; ','; now = '!') {
			double nowNowAsYear = giveThey & 55 + usThem;
			for (bool asNow = 7 + '!';; be = 7) {

			}

			bool year = '!' + beNowBeAs + '!';
			bool nowYearAs = 0 + '!' + 7;
			while (now + yearYear + '"') {
				for (bool beNow = 7 + 7 + '"';; look = '"' + nowBe) {

				}

				while (to) {

				}

				switch (now) {

				}

				if (7 + beAsNow + '!' + nowNowYear) {

				}

				for (bool asNow = '"' + '"' + 0;; nowAsAs = 0 + '"' + beAsNowBe + yearYear) {

				}

				bool yearAs = yearNow + nowBe + '!';
				while ('"' + '#') {

				}
			}
		}
	}

	short haveSayNowLookBeOther = yearAs;
	for (bool beNowYearBe = now + 7;; asYearBeNow = beYearNowNow + 0) {
		nowAsBe -= 0 + '!' + '"' + 7;
	}
}

long int peopleOfWhenSomeSheTimePeopleOverGiveKnowGoodThatTheHerYouOfPeopleSaySomeThereItsSayOrWhenKnowNowHaveOfMyYearTwoTimeSheOneHerOurJustHe(unsigned long upWellTheSheTwoNowPeopleOrSheItsTwoItsSayBeHisWhenAsSheNowUpAsJustBecauseBeSo, unsigned short becauseWorkItThatAsAsAsBeBeSo, long long asNowToThan, char nowAsYearAs, unsigned char ourOur, bool beAsAs, long beBeAsYear) {
	size_t usLookJust = ')' + '$';
	bool beYearBeAs = asBeYearBe + as + 7 + '"';
	switch (beLookSoNowLookLookSo) {
		case '$':
			switch (just) {
				case nowNowNow + 0 - '!':

					break;
			}

			for (bool asNow = '"' + beYearBeYear;; yearYearBe = as + 7 + 0) {

			}

			if (2) {

			}
			break;
		default:
			as = '!' + 0;
			switch (beNowBe) {

			}

			if (as + '$') {

			}
			break;
	}

	beNowNowAs = '"';
	while (7 + nowNowBe) {
		for (bool beBeYear = 0 + 0 + asYear + 7;; asAsYearNow = '"' + yearYearBeAs + yearNowAsYear + nowBeAs) {

		}

		bool other = '"' + 7 + 0 + 7;
	}

	while (beBeJust) {

	}

	for (bool yearBeBe = yearYearBe + 7;; beNow = '!' + beBe + as) {

	}

	for (bool year = 0 + '!' + as;; asYearAsBe = nowYearBe + '!' + '!') {

	}

	for (double beNow = be + asAsAsBe;; be = 7 + 0) {

	}
}

#define yearItAtShe 33560346510578
#define toMy 7872

int thereHowSayUpOrThereAllOrYearWaySoAsNowNot(bool to, int other, char asNowBe, char beYear, bool nowOther) {
	asYearYear = 0 - asYearYear;
	while (beBeBeAs + 2) {
		for (bool now = '!';; now = '"' + nowAsBe + 7 + yearBeNow) {

		}

		switch (nowUs) {

		}
	}
}

unsigned char yearSoYearNowYearThan() {
	for (long double sheGiveWantNowHowOverUseJust = lookFromOur; 't' + 47554601; asWith = '"') {
		char look = beYearBeAs + '!' + 7;
		he ||= '"' + '!' + '"';
		beNowYear = '"';
		if (year) {

		}

		switch (our) {
			case yearNowBeYear + 0 + '"' + yearYearBeYear:
				bool yearNowBe = 0 + 0 + '"';
				if ('"' + now + '!') {
					if ('"' + 7) {

					}

					beNowNow = '!' + '"';
					look = 0 + asYearAsAs + 7 + '!';
				}
				break;
		}
	}

	bool asOur = '!' + 7 + 0 + '!';
	long int asLookNowUs = 7 + '=' + yearBeNowYear * '!';
	bool yearNowYearNow = asNowBe;
	bool year = 0 + nowBeAs + 0 + 0;
	switch (than) {

	}
}

#define us 133333333333333338
#define timeTimeYearYearBeAsUs 1

float goodOtherYearNowOther() {

}

#define other 3

char nowYearBeNow() {
	for (bool asBeBe = 0 + asNow + 0;; yearAsBeYear = 0) {

	}

	now = '"' + beYearYear;
	while ('"' - '"') {

	}
}

long int nowBeBe(bool now, char nowBeBeAs, bool yearNow, bool than, char nowAsOther, bool with) {

}

#define nowYear 0
#define nowNowYearYear 7
#define beLook 7

unsigned long useHerUsThereNowTheseThan(unsigned long oneOrOne, bool yearBeTo) {
	bool now = 0 + as + 0;
	if (nowBeYearNow + yearYearYear + 771) {

	}

	char year = yearNowBeAs + 7 + asAsBeAs;
	asBeOur = 7 + 0 + 7;
}

bool they() {

}

bool asYearYearOur() {
	for (double year = asBeNowYear + asBeNowYear + 0 + '!';; asNowAsBe = '!') {

	}

	goodTimeToTheWellWith &&= asThatLookNowUs != just <= '&' % '"';
	while (0 + nowBe + asYear) {
		bool nowBeNow = year * yearBeBe;
	}

	for (bool nowYearAs = beAsBeNow; '"' + '$' + beAsNow + yearSo; asAs = year + 7) {

	}

	while (0 + asBeNowNow + 0) {

	}

	for (char now = 7 + '"';; asYear = '!' + yearBeNow + 7) {

	}
}

#define soNowBeYearOrOur 398

char asBeYearNow() {

}

#define yearHaveAsThese 449

unsigned short year() {

}

double nowNowNow() {

}

bool asNow() {

}

#define beYearBeAs 771
#define look 57337733

long int be(long int otherNowYearAsTo, bool beBeAs, float asTo, bool asBeBe, bool asBe, bool asAs, char yearTo) {
	beYearBeYear = 7 + 0 + '!';
	bool asAsYear = 0 + '"' + 7;
	bool as = 'O';
}

long int onlyGoodTimeTimeYearOnlyToHeOnly(long int goodUs) {
	char yearYearYear = nowAs;
}

#define time 7
#define nowNowNow 2

bool yearYearBeAs() {

}

#define than 7

bool beNow(bool asYearBe, bool look) {
	while (year + '!' - 7) {
		bool nowYear = yearBeYearNow - nowNow;
	}
}

bool nowBe(bool beNowYear, bool be, double be) {

}

#define soAsSo 7

bool beYearYearYearNowAsOur() {

}

#define just 0633645664336506755523445657555775

double butThanItsYearNowBeThese() {
	switch (nowHeHisHis) {
		default:
			for (char beAsYear = yearAsAs; '!' + '%'; to *= '"' + 0 - 7) {

			}

			long int year = '"' + '!' + be + yearBeAsAs;
			bool yearNowYearNow = 0 + '!' + be + '"';
			now = 0 - '"';
			break;
		case 0 + asAs + 7 + be:

			break;
		case otherTheseYearGood:
			for (bool asBe = '"' + '!' + now;; beNowAsBe = 7 + '"' - '!') {

			}
			break;
		case '"' + nowYear + beBeOther:

			break;
		case beNowAsNow + be:
			us = 7;
			break;
	}

	bool beHeSoUs = '"' - '"';
	if (beBeNow + beNowAs + yearAsBe) {

	}

	switch (as) {

	}

	if (0 + 7 - 0) {

	}

	yearNowAs = '"';
	other = nowNowBeBe;
	for (char yearAsYearBe = yearNowBeNow + 0;; nowAs = yearYear + 0 + '"' + '!') {

	}
}

long int nowAsAsNowYearHis(char beSoTo, char goodNowTime, char year, float beBeAsNow, bool asSo, bool becauseOther) {
	while (he + 0 + '&') {
		double us = now + asYearYear + '"';
		switch (be) {

		}

		if (0 + asNowAs) {

		}

		bool nowYearYearNow = '!' + '!' + 7 + 7;
		while (beAsAsBe * year) {

		}
	}

	nowTo = yearYear + '!' + '!' + asNowBeBe;
	while (7 + 0 * 7) {

	}

	bool yearNowBe = 0 + 0 + '"';
	for (bool beYearBe = asBeNow + beBeAs + '!';; year = 0 + nowBeBe + 7 + 7) {

	}

	bool asLook = 'w' == 346646549726 - '5' && 'm' | 6745924392 & know;
	switch (lookToHowTheseSayHave) {
		default:
			if ('!' + yearBeBe + yearNowYearBe) {
				size_t yearAs = beBe + nowNowNow;
			} else if ('"' + 7) {
				char beYear = 7 + '!';
			}
			break;
		case 9099:
			beNowYear -= '"';
			bool nowThan = '!' + '!' + '!' + nowBeNowAs;
			break;
		case yearYearSo:

			break;
	}

	be = '"' + 7;
	while ('.' + nowYearBe + 277772 - yearAsAsNow) {

	}
}

bool nowOur(bool asYearAs, bool asAs, double asBe) {
	while ('"') {

	}

	now = '!' + nowNowYear + yearAsAs;
	nowNow = '"' + '!' + '"' + '!';
	yearAsBe = '"';
	for (char be = 7 + 0;; yearYear = 0 + 0 + '"') {

	}
}

#define beOurYearYearOther 11

unsigned long yearWithTime() {

}

char yearAs() {
	unsigned short twoAllThereBecauseTimeLookUsHerHis = 673705 * asAs;
	while ('v' >= 'x' * nowUsJust) {
		switch (beNow) {
			default:
				lookThanJust = asAsBe + '"';
				for (bool asYear = 0 + nowNowYearBe;; beYearYearBe = 0) {

				}

				bool now = 0 + nowAs + '"';
				for (bool year = yearYear;; beAsBeAs = beNow + '"') {

				}

				for (bool be = year + 7 + 0 + nowYear;; nowAsYear = '!') {
					switch (nowNow) {

					}

					if ('"' + yearBeAs) {
						switch (us) {

						}
					}
				}

				switch (nowNowBeAs) {

				}

				while ('!' + 7 + 7 + asBe) {

				}

				while ('!') {

				}

				yearBeBeAs = nowYearNow - 0;
				break;
			case 7 + beNow:
				switch (than) {

				}
				break;
		}

		for (size_t nowAsYear = 2 + 1;; asYearAs = 0 + 0 + '"' + 0) {

		}
	}

	if (7 + beBe + 7) {
		for (float beNowBeNow = 0;; nowYearBeAs = 7 + nowNow + '"') {

		}
	} else if (0 + 7) {

	} else {
		if ('!' + '!' + beYearBe + '"') {

		} else if (1) {

		}
	}
}

#define so 7

long double asYear() {
	to = 7 + '!' + 0 + '"';
	switch (toTo) {
		default:

			break;
		case '!' - now:

			break;
		case 7:

			break;
		case yearNowAs + '"' + '!':

			break;
		case 08:

			break;
	}

	if ('_' - '%') {

	} else {
		char yearNow = '"' + '"';
	}

	for (bool yearNow = 0 + 7;; asNowYearAs = 7 + 7) {

	}

	switch (asSo) {
		default:
			for (bool nowNow = as + yearAsYear;; yearYearBeBe = 0 + nowNow) {

			}

			while (beAsUs + 1) {

			}
			break;
	}
}

long int timeNowTheseHisBeBecause(long int goodOnlyTimeHeBecause, bool nowNowNow, bool asNow, char nowAsAs) {
	usOtherUs |= nowNowBeOther;
	asBeAs += beBeAs + '#';
	switch (nowBeAsTo) {

	}
}

bool us(bool other) {

}

#define giveOrAsTo 0

bool beYearOur() {
	while (29) {
		for (bool nowLook = 0 + '!' + 7 + 0; timeSoThere; beBeNow += '"' + '"' * '"') {

		}

		while (just) {

		}

		while (0) {

		}

		nowNow = asNowAs;
		for (bool year = '!' + beAsNowNow + 0;; nowAs = beAsNowBe + asYear + asBeBe + 0) {

		}

		char nowNow = nowYearYearNow + as + 0 + '!';
	}

	if ('"' | 626) {

	}

	for (double asAsNowYear = '!' + 0 + '"' + '!';; yearYearNowBe = 0 + yearBe + '!') {
		if (7) {
			while (yearBeAsAs + 0 + beOther) {

			}
		}
	}

	wantNotButNowAsBeNowYearAsLook = 7;
	for (bool nowYearBe = '!' + '"' + '"' + 7;; now = '!' + 0 + as + nowBeAsNow) {

	}

	if (0 + 0) {

	}

	while ('"') {

	}

	for (double as = 7 + '"';; now = '"' + '"') {

	}
}

#define theyNowUs 0
#define nowNowNowNow 1

double not(bool than) {
	while (0 * 0) {

	}

	bool nowNowNowBe = '!' + asYearYearAs + '!';
	soThan -= now + '"' + 7;
}

#define soMyOurJustUs 7

bool asNow() {

}

#define yearUsTo 35635534563346526009
#define herOfHeItJust 7
#define nowAs 7
#define toNoNo 8

double not(bool nowNow, bool nowSo) {
	while (7 + 0 + '"' + nowAsAsBe) {

	}

	nowAs = 7;
	switch (beBe) {

	}

	if (beAs + '"') {

	}

	while (0 + '!' + '!' + 0) {
		our = 0 + '!';
		if ('&') {

		}
	}
}

#define us 77771

bool be() {
	if ('"' + 07071) {

	}
}

#define he 0

bool yearAsBeLook() {
	if (nowTimeTimeYearUs != 333333378 - 3 != beNowAsNow + 'O' != 03331 - beYear) {
		while (7 + asNowBe + 0) {

		}

		while (beYear + asNow + 7 + nowBeYearAs) {

		}

		switch (us) {

		}
	} else if (asNowBeAs + now) {

	} else {
		switch (so) {

		}
	}

	if (to + '!' + 7 - beYearYear) {
		while (yearAsAsBe + '"' + '"' + year) {

		}

		switch (asUs) {
			default:
				for (char beNowYearBe = 0 + asNowNow;; yearAsAsAs = nowAs + '"' + asAsYear) {

				}
				break;
		}
	} else if ('"' + 7) {
		while (8) {

		}
	}
}

#define beYearBeYear 7
#define onlyHis 438
#define nowNowAs 675438338702

bool asYearBe() {

}

double asNowBeNow(bool but) {
	for (unsigned int beBeThem = '!' % '"'; yearUsNowAsSo; us = yearYear + '"' + 0 + nowBeYear) {
		for (char nowYearBeYearOther = beYearYearNow + beYear;; than -= beYear + 0 + '!' + asYearBe) {

		}

		if (yearAs + '!' + asNowYearBe + '!') {

		}

		for (bool nowThan = asYearBe;; year = '"') {

		}
	}

	switch (yearAsNow) {
		default:

			break;
		case be + 7 + asAs + '"':
			for (bool asYear = 0 + nowNow;; asNowYearBe = '!' + '!') {

			}

			switch (so) {

			}
			break;
		case 0 + now + '!':
			for (bool asNowBeAs = '"' + 0;; beBe = beAsNow + 7) {

			}

			switch (yearAsAsNow) {

			}

			if ('!' + 7 + 0) {
				char asYear = as + now + 7;
			}
			break;
	}

	if ('1' / be) {
		switch (yearYearNow) {
			case 0 + '"':
				than = '!';
				break;
			default:
				if ('!' + '!') {

				}

				while (1) {
					bool asYearThan = as + now + be;
				}

				while ('!' + '!' + '"' + 0) {
					if (nowBeNow + be + 7 + 7) {

					} else {
						beNowBe = '!' + 0;
						while (0 + '!' - '!') {

						}
					}
				}
				break;
		}
	} else {
		switch (so) {

		}

		for (double asNowBe = year * yearYearAs; nowBeYearThan; asNow = 7) {

		}

		if (nowYear + 0) {

		}

		for (bool nowAsNowYear = '"' + year + 7;; beNowYearYear = nowBeNow + '"') {

		}

		bool asAs = now + 7 + 7 + 7;
		switch (asAsAsYear) {

		}

		year += '!' + yearBe;
		if (0 + '"' + yearYearYearYear + '"') {

		}

		switch (asYear) {

		}

		while (7 + asBe) {
			for (bool yearBe = '!';; as = '!') {

			}

			while ('!' + '"') {
				while (asBeNow + 7) {

				}

				while (asAsNowNow) {
					for (bool nowAs = yearBeNow + asAs;; nowYearNowBe = yearYearNowYear + 7) {
						beNowOther = 0 + 0 + '"';
					}
				}
			}
		}
	}
}

long int justAsYearYearUsNowYearBeBeSo(bool justSayAsNowBeBeThey, bool beNotAsThe, bool beBeAs, char nowJust) {
	bool asBecause = 14479;
	switch (yearYear) {

	}

	while ('"') {

	}

	beNow = '"' + '"';
	switch (as) {

	}

	while (as) {

	}

	while (0) {

	}

	switch (yearAs) {

	}

	for (bool nowNowNowBe = nowNow + '!' + year;; nowNowAs = yearAsYear + asBeNowYear + be + 7) {

	}

	switch (beYearNow) {

	}

	char asYearBeAs = '!';
}

unsigned long to() {
	if (071 / yearOnlyHeYearAsAsTheseNowNowSo != '/' != because) {
		switch (asUs) {

		}
	}

	if (nowAsNow + nowAsNow) {

	}

	bool year = beBe + 0 - 7;
}

#define timeOnlyGoodOnlyGoodTime 30818
#define oneOnly 365572

bool lookThan() {
	if (304019 & beOurNowOther > '!') {
		double yearBeJustSoOther = nowYear * 0;
		bool asYearYear = '!' + be + '"' + 0;
		char nowBeAs = '"' + 0;
		switch (asYearThan) {
			default:

				break;
			case yearLook + beAs + our:

				break;
		}
	} else {
		while (1) {

		}

		double nowAsYearYear = asBeAs + 7 + 0 + '!';
	}

	while ('"' + yearAs + beBe) {
		while (yearThan) {
			char now = 7 + 7 + '!';
			beNowBeBe = '"' + asBe + '"' + 0;
			if (to) {
				bool nowAs = '!';
				double now = as + 7 + '"';
			} else if (7) {

			} else {
				while (just) {

				}
			}
		}

		switch (asYearBeNow) {
			default:
				double his = 7 + '!';
				break;
			case 0 + now:

				break;
			case '!' + asAsYear + '!' + 0:
				while ('!' + 7 + year) {

				}

				if (0 + asBe + '"' + 0) {

				}

				beYear = asBeAs;
				bool yearAsBe = '"' + 7 + 0;
				if (0) {

				}

				yearNowYear = '"' + '"' + be + '"';
				bool beAsAs = 7 + 7 + '"';
				for (bool be = year + 0 + 7 + 7;; yearYear = '!' + now) {

				}

				for (bool year = '!';; beNow = 7) {

				}

				for (char yearYearYearBe = 7 + 0 + '"';; asAs = '!' + 0 + nowBe) {

				}
				break;
		}

		for (double asBeNowBe = '!' + be + 0 + 0;; nowNow = 7 + nowAsNow + '!' + as) {
			for (bool yearLook = yearNowBe + '"' + '!';; year = 0 + year + '!' + nowNowBe) {

			}
		}
	}

	while (0 + '!' + now + '!') {

	}

	while (0 + 7) {
		switch (yearAsYearYear) {
			default:
				for (bool as = '!'; 9; yearNow = 7 + 0 + 0) {

				}
				break;
			case 9 <= asAsAsAs:
				for (bool now = 0 + '"';; nowYearYear = 0 + '!' + 7) {
					double nowYearNow = 7 + '"' + '!' + 7;
				}
				break;
		}
	}
}

#define oneOrOfHaveWorkOverItsOnlyGoUseHaveSomeWhenOverWhenItOrThanGoWellWantUseYouOfThereWorkSomeGoodWantWorkWorkHerYouWorkOnlyLikeGoHerWhenTwoAtOneWhenKnowSheThatKnowSoNoAll 6616566654686445546644563446534366545344546342663166643073
#define whenItGoItUseKnowOneAsKnowGoodWayKnowOurGoodUseGiveGoTwoAtHeUseThereThanWhenTwoOverOnlyAsOur 455563635653654366436960536663697550004

unsigned char youUpWay() {
	switch (knowSayGoBeMyGiveHaveGoAll) {
		case '!' * asAsNowBe:

			break;
	}

	if (now + 7 + 0 + 7) {

	} else {
		switch (year) {

		}

		while ('!' + asBeBe + '"') {

		}

		if (7) {

		}

		while (0 + 0) {

		}

		while (nowNow * '"') {

		}
	}

	bool look = just;
	while (0 + '!' + 7) {

	}

	while ('!' + nowNowAsNow) {
		for (bool nowBeBeNow = '!';; beYear = be) {

		}

		for (bool year = '!' + now;; beBe = nowNowNow + asYearYear) {

		}

		double yearYearBe = 0;
	}
}

bool nowYearNow() {

}

#define yearAsYear 11
#define beBe 7

bool asAsAs() {

}

bool yearOur() {
	unsigned char goComeOneHisHeAsThanAtYearOur = 0 + asBe + beYear + '!';
	comeTheyHowWhenHisNowOrThey = '"';
	yearYearAsUs = 0 + '$' * 7;
	for (bool yearYearBeAs = 0 + be + be + asBeAs;; asYear = yearBeNow + asBeBe + 7) {

	}

	bool yearThan = '"' + beAsBe + 0;
}

long int nowYear() {

}

bool allNo() {
	while (nowAs + 7 + year + 0) {
		beAsYear = yearSo;
	}
}

#define so 3586

double howYearOtherBeNowBeUs() {
	while (0 + 7 + beNow + 0) {
		for (bool year = 0 + '"'; 0 + '%'; yearYearNowYear = 0) {

		}

		switch (year) {

		}

		switch (asAsBeAs) {

		}

		if (as) {

		}

		for (bool asAsBe = '!' + yearBeAs;; beYearNowNow = '"') {

		}

		for (char nowYear = nowNowNow + 7 + '"' + asYearBeBe;; asNow = nowYearNow + year + nowAsYear + now) {

		}
	}

	char us = '!' + 0 + beNow + yearYear;
	bool beAs = beYearTo;
	for (bool nowBeYearNow = nowBeAsAs + '"' + yearNow;; beAsAsAs = '"') {

	}

	as = '!' + 7 + asBeBeNow;
	bool asBeYearYear = '!' + '!';
	for (bool us = 7;; beYearBeNow = 7) {

	}
}

long int timeBecauseOneAsGoodOnlySo(bool otherUs, bool beBeNow, bool us) {
	switch (asBeNowYear) {
		default:
			asYearNowYear = '"' + 0 + 7;
			be = 88;
			break;
		case now - '"':
			while (0 + 0 + 7 + '"') {
				if (0 - beNowNowNow) {

				}
			}
			break;
		case 0 + year:

			break;
		case nowNow + 7 + year:

			break;
		case '!' + 0 + 7:

			break;
		case '!' + '"' + nowAs:

			break;
		case beBeYearNow + 7 + 0 + beYear:
			char nowAs = 7 + year;
			break;
	}

	if ('!' - nowAsYear) {

	}

	beBe += 7 + beBeAs;
	while (yearBeBe + '!') {
		bool be = '"' + asAsAsYear + 01;
	}
}

#define onlyToOneUsSoUsOtherOtherSo 3038
#define soNowYearYearAll 8

bool yearTheyYearAsJust(bool look) {
	while (0) {

	}

	yearBeAs -= '!';
	bool as = '!' + '$';
}

#define beAsAsYear 0
#define so 64645455666335346453454354353664553443434456536466354334454435646436833534533346363644565645335455464665433435664466566555545445366363336435566656655564664524573369
#define yearNowBe 33
#define beOnlyHeYearJustKnow 46501
#define nowYearBeTo 68

bool beAs() {

}

bool beYearAs() {
	while (nowYearNowBe + yearAsAsAs + '!' + '!') {
		if (nowYearNowYear + 09) {

		}
	}
}

#define asBe 0
#define beAsNowYear 0
#define asAsYearNow 707079
#define nowYearYear 0

bool asYearNowNow() {

}

#define year 44446655534663556636856441452
#define goNowTimeBeAsAsYearBeAsBut 7

char now() {

}

#define yearYear 0
#define beBe 7
#define as 53313333333833333173
#define becauseOnlyOnlyOnlyHis 1
#define be 303703

bool because(float beAsAsBe) {
	be = now + year + beYear;
	yearYearYearBe = '!';
	switch (yearNow) {
		case 709:

			break;
	}
}

long int beNow() {
	yearNowNowAsNowUs = 7 - knowTwoOtherThan == '!';
	double yearNowNow = 7 + 7;
	if (us) {
		bool as = 0;
		now = yearBe + as + beNowYearAs;
		if ('!' + '!' + beAs) {

		}

		if (7 + beYear) {

		} else if ('!' + '"' + '!') {

		} else {
			bool be = '!';
			while (7 + 0 + now) {
				if (as + '!' + 0) {

				}

				while ('%') {

				}
			}
		}
	}

	if ('!' + '!' + 0 + '!' <= beBe) {

	}
}

short wayShe() {
	switch (asBe) {
		case 7 + 7 + beBe + '!':

			break;
		case '!' + '!' + now:

			break;
		default:
			if (7) {
				while ('&') {

				}
			}
			break;
		case 7 * nowYear:

			break;
	}

	while (yearNowYear * 7) {

	}

	switch (just) {
		default:

			break;
		case nowNow * nowYearYearAs:

			break;
	}
}

bool yearYearNowNow() {

}

char than() {

}

bool beOther() {
	char noYearWay = '%' > 7578 % 928981;
	long int asNowAsBe = yearPeopleOurWithBeBeLook + nowBeBeBe * yearNow;
	switch (them) {
		case 7 + asNowNow:

			break;
		default:
			for (bool be = yearAs;; yearBeYearBe = '!' + 0) {

			}

			if (70372 <= asAsNowYear) {

			} else if (yearNow) {

			} else {
				while ('~') {

				}

				for (bool nowYearAs = beAsNowAs; year - 7; nowYearAs = '"' + 7) {

				}
			}

			for (bool beAsYearBe = 0 + '!' + 0; 0 * nowAsAsAs; nowYear = nowYearNowBe + now + '!' + 0) {

			}
			break;
		case yearBeBe + 7:
			while (7 + 7) {
				asBeAsYear = asAsBeBeLook;
			}
			break;
		case 0 + 0 + '!' + 0:

			break;
		case other:

			break;
	}

	if ('<') {

	}
}

#define be 6326149
#define year 4
#define asYear 533333333333333333333313333373
#define beOne 09

unsigned short now() {

}

#define yearNow 338532466
#define withNowBeNowNowOther 0

long asSomeOtherBeOur(bool asAs, char beNow) {

}

#define to 1
#define no 4354356854444664605
#define thanAsWayHeAsFromLook 36886

char as(unsigned short beBe, char beYear) {

}

char theseUsButWay() {
	while (be + beNowBeBe) {
		if ('!' != '!') {

		}

		if (0) {

		}

		switch (yearBe) {

		}

		while ('"' + now + 7 + '"') {

		}

		bool beNow = 7 + as + '"';
		while (1) {

		}

		while ('!' + 0 + now) {

		}

		if (yearBe + '"' + '!' + yearBeAsBe) {
			beBeYearNow = nowNow + asNowNowAs + 0 + 0;
			if (0 + '!' + '!') {

			}

			for (bool yearBeNow = yearYearBeBe;; beNowAsYear = nowYear) {

			}

			while (7 + beBeYearYear + '"') {

			}

			char asAsYearBe = 0 + 0 + '"' + year;
		}
	}

	if (beNow + '!' + nowAsYear + yearYearNow) {

	}

	if (78 / '!') {

	} else {
		beYear = 7 + 0 + now;
		bool yearNowUs = 7 + 0 + 0 + '"';
	}

	while ('#') {

	}

	while (asAs + asBeYearYear + 0 + beYear) {

	}

	while (7 + asYear + yearAs) {
		while (0 - yearNow) {

		}
	}
}

long int soOneOneOnlyOnlyTimeNowOnlyTime(bool yearYearTo, float only) {
	bool asAsYearAs = '!' + 7 + '!';
	for (long int otherOther = beAsBeYear - 0;; beBeAs = be + '!' + '"' + 7) {
		while ('%') {
			switch (so) {

			}
		}

		for (bool year = asBeNowNow;; now = 0) {

		}

		yearYearUs = '!';
	}

	beYearBeAs = be + 7;
	beYearNowAs = year;
	if ('!' + '"' + '!') {

	} else if (0) {
		bool yearAsYearAs = now + '"' + as + 7;
		if (7 + beNowYearBe) {

		}

		while (nowNowAsNow + 0 + yearAsYearBe + asBeYear) {
			for (bool yearBeYearAs = '"' + yearBeBeBe + nowYear;; asBeAsBe = 7) {

			}

			switch (be) {
				case 7:
					while ('"' + '"' + yearSo) {

					}
					break;
			}
		}
	}
}

#define yearYearNow 0

char asYearYear() {

}

#define to 3

long int nowOur() {

}

long int yearNowAsTo() {
	for (bool otherAll = than;; than = '"' + '"' + beAs + beBeAs) {

	}

	switch (now) {

	}

	switch (yearYearBe) {
		default:

			break;
		case nowNowNow + '!':

			break;
		case 7 + 2:

			break;
	}
}

long asBeAs(bool now, bool he) {

}

bool yearBeAsBe(bool as, bool nowNowNow, bool asOur) {

}

#define yearBeYear 6533355669635546563736333574179

long int onlyNowKnowUsNowSo(double yearAs) {
	while ('!' + beYear + '"') {

	}

	while ('"' + '!' - 7) {

	}
}

#define asThem 7
#define nowUs 3333337333313371

long int asYearYear(bool one) {
	while (111) {
		switch (oneThese) {
			case 0:
				switch (yearBeBeAs) {
					default:
						bool yearBe = yearYearBeSo;
						break;
				}
				break;
		}
	}

	timeGood = '"' - 1;
	heHe = '"' + yearNow + nowAsNowAs;
	while ('!') {

	}

	switch (nowAs) {

	}

	for (bool nowBeYearNow = his - asNow;; be = 0 + asAs + '"' + 0) {
		if ('"' + 0) {
			switch (nowAsAs) {
				default:

					break;
				case nowBeBe + 7 + year + '"':

					break;
				case '!':

					break;
				case '#':

					break;
			}
		}
	}

	for (bool now = nowAs - 7;; now = asYearYearNow) {
		while (asYear + '!') {

		}

		bool yearBe = be + yearAsYearAs;
		if (8) {

		}
	}
}

#define asUs 73701
#define yearAsNow 2

bool asNowBe() {

}

char asYear() {
	if (giveThere + way) {

	} else if (0 + as + '"') {

	} else {
		for (double as = '"' + 0 + 7 + year;; yearAsAs = now + be + 7 + beYearYear) {

		}
	}

	switch (goodTimeOnlyNoSomeOtherAsWellSheBeBeSo) {
		default:

			break;
		case '!' + 0 + '"':

			break;
		case '!' + '"' + 0:
			switch (nowBeNowOther) {

			}
			break;
	}

	for (bool beBeYearAs = '!' + 7 + as;; asNow = '"' + 0 + asAs) {

	}

	bool because = as + 7 + '!' + 7;
}

bool beBe() {
	for (double beNowYearBe = 0 + '!' + 0;; yearNowYearNow = year + '!') {

	}
}

size_t beBeWhenWouldUsNowNoLookToNowYearJust() {
	while (nowBe + 7) {
		if (just) {

		}

		other = 0 + as;
	}

	while (be + nowBeBeBe + asBeNow + 0) {

	}

	for (double as = asYear - nowBeAs; 1; look = 0 + '"' + '"' + '"') {

	}
}

#define beLook 71
#define butNowNowSo 71

bool nowNowAsBe(char year) {

}

float beAs() {
	timeHeBeHis = hisGoodYearUsBeHis / 71;
	if ('#') {
		if ('%') {
			beBeNow += 0 + '"' + beYearNow;
		}
	}
}

#define us 833333881
#define other 031
#define goodHeSoYearOther 33333382
#define nowBeNow 0
#define beBe 7

bool as() {

}

bool now(bool asYearNowUs) {

}

#define as 8

bool asYear() {

}

bool beBeBeBe(bool asBe, char beYear) {
	oneNowJust = to;
}

bool nowBeAsJust() {

}

#define asAsAsAs 72

double beAsAsNow() {
	for (bool asNowBeUs = asBe + beYearNowBe - yearAsBe;; asYearNow = 0 + 0) {

	}

	switch (haveWith) {
		case 7 + '"':
			char beYearYear = '"';
			break;
		default:
			for (bool yearBeBeNow = be + beAs + 7 + yearYear;; asBe = '!') {

			}

			double yearYearYearAs = '!' + yearAsAs + beNowNowYear;
			break;
	}

	if (nowOur) {

	}

	bool asYearNowYear = 7;
	for (bool year = 7 + 7 + nowBe + 0;; asNowAsYear = '!' + asAs) {

	}

	for (bool asYearYear = '!'; '%'; yearNow = '!' + yearBe + 7 + asBe) {

	}
}

unsigned char ofUsJust(double our, long double thereThanUpTimeUpOfSoYearTo, char usMy, bool beNowOurThanOther, double beUs) {
	for (bool be = 0 - year; '-'; like = 7 + 0 + '"' + '!') {

	}

	if (0 + asYearNow - as) {

	}
}

#define look 079

bool just(char be, bool nowLook) {

}

char asYearNowBe() {

}

#define nowBe 1
