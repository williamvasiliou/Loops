#include <UU/U0UUUUEU0//0.h>

long int to(bool as, bool soAsYearAsOther, bool beYearAsYear, bool so, bool asSo) {
	switch (us) {

	}

	bool asNowBe = '"' + yearNowBe + 0;
	while ('!' + 7 + beNowBeNow + 0) {
		while (year + now) {

		}

		while (asAsNowBe + 0 + yearNowAsYear) {
			for (bool asAsYearBe = 7 + nowNowBeBe + '!';; nowAs = '!' + beYearNowAs + 0) {

			}

			bool as = 7 + nowYear + beNow;
			while ('"') {
				bool asYearBeAs = '"' + nowAs + 7;
				double beYearAsNow = 0;
			}
		}
	}

	for (char yearBeYear = 0 + 0;; nowNowNowAs = '"' + '"' + nowBeYear) {

	}
}

#define fromOtherThanBecause 6366354434344565463556636456875465431443356653645646643654467084

size_t wantLikeWantAsOrWorkWantHowYearUseJustToAsYearTo(double nowYear, size_t beLook) {
	for (long int our = year + '"'; yearYearUs + '%'; asNow = 7 + asBeNow) {

	}

	switch (our) {

	}

	while (yearBeBe) {
		if ('"') {

		}

		if (asAsYearOur) {

		}
	}
}

#define nowNowOther 7544302

long long herBeThatTwoLikeJust(bool yearBeNow, int just, char yearYearBeAs) {
	switch (usYearThan) {
		default:
			for (bool us = be + '"' + yearAs + 7;; be = 0) {

			}
			break;
	}

	switch (nowNow) {
		case '!' + 7 + '!' + '"':
			if (beYearAs + '$') {

			}
			break;
		case nowYearBe:
			switch (nowAsBeAs) {

			}

			bool asYearBeAs = 7;
			double asYearAsYear = 0;
			break;
	}
}

unsigned char oneThanLikeJust() {
	long because = '&';
	if (81 - '!') {

	}

	asAs = '"' + 7 + 0;
	beAs -= look;
}

unsigned long yearJustBeYearBeAsNowNowAsOur(char nowBeBeAs) {
	for (bool nowAsYearAs = '='; beAs + '"' - yearBeBeYear; now = 0) {
		if (to + '"' - asBeAsBe) {

		} else if (yearNow - 7) {

		}

		for (bool beYear = 7 + nowAsAsBe + beAsYear;; asYearBeBe = 7) {

		}

		as = to - nowYearYearYear - '"';
	}

	long int yearSo = 0 + '!' + beAsAs;
	if (7) {

	}

	onlyThese = '#';
	if (0 + year) {
		char yearAsNow = '"' + '"' + nowYearYear + '!';
	} else if (0 - 0) {

	}

	other |= usHeNowUsNowOther != '!' != nowNowYearNow;
	switch (asOther) {

	}
}

#define goodTimeTimeTimeOnlyGoodHisHeWith 0

unsigned char asThey() {
	bool year = '"' + yearNowNow + 7 + yearBe;
	switch (yearBeBeYear) {

	}

	if ('$') {
		switch (soOnlySo) {
			default:
				if (7 + '$') {

				}
				break;
			case '&':

				break;
		}

		for (bool nowAsNowNow = yearYear + '!' + beYearAs + 0; '&'; yearNowBeAs = asAs + '!') {

		}
	} else {
		if (8093) {
			asThan = yearAs + 7 + '"' + '"';
			char asBeBeBe = beYear + 7;
			switch (yearSo) {

			}
		} else if ('#' | 2) {

		} else if (beAsNowBe + yearBeNow + 7 + '"') {
			while (0 + '!' + year) {

			}

			for (char beBe = as + now + '"' + 7;; year = '"') {

			}
		}

		switch (year) {

		}

		he = 0 + beNowYear;
	}

	unsigned char lookSo = 0 + beBe;
	bool us = asYear + 0;
}

float upYouComeItHowLikeOrUseUseLookUseWorkOurYearOther(double yearOnlyBeBeThanAsBeAsAsBeThan, bool as, bool look) {
	if (505708 - beAsNow) {

	}

	than -= 7 + 0 + beBeAs + '"';
	while (00799 % asNowYear) {

	}

	now = beAs + beYearYear + beYearAs;
	if (nowBeAs) {

	}

	nowBe = '"' + '!' + 7 + asYearBeBe;
	bool nowNowNowAs = asBeNow + be + 0 + '"';
	long double asYearNowYear = nowYear + '!';
	way = yearTo;
}

#define asAsAsOur 5075
#define heThem 7
#define yearBeUs 665643665645736352466476660348777978
#define atLike 7

char beAsNow() {

}

#define justSo 0

long double beNowYear() {
	for (long int be = us + 0 - 8;; asNowYearAs = 7 + asYear) {
		now = beAsBe;
		beYear |= yearBe + 7;
	}

	char beAsNow = 7 / 7;
}

#define timeOneOneOneTimeGoodSoHeOnlyOnlyGoodToTo 88
#define us 33333301732
#define be 7
#define be 7

double beNow() {
	if ('!' + beNowNowBe + '"' + '!') {
		if (nowYear + 7 + '!' + '!') {

		}

		if (yearBeNowNow - yearAs) {

		}
	}

	double yearBeAsAsJust = 'e' == haveFromNowUsTheseThey + 709 * nowYearBe;
	switch (asBeBeBe) {

	}

	while (yearNowAtOther + 76) {
		asWould -= '"' + 7;
		double nowNow = '!' + 0;
	}

	while (7 + 78) {

	}
}

#define someBeJustYearBeTime 783
#define howFromUseWould 71
#define goodItsUsAsAsNowLook 5075
#define yearAsAsYear 7

bool just() {
	while ('\'' - now) {
		switch (be) {

		}

		while ('"' + yearAs + beNowAsNow) {
			bool nowNow = 0 + 7 + '!';
			while ('"' + '"' + 0) {
				bool yearAsYearAs = '!' + nowAsBeAs;
				while (beNowNowBe + 0) {
					for (bool yearBeNow = 08;; beAs = beNow + '!' + beBeNowAs) {

					}
				}
			}
		}

		switch (hisBeNowBeBecause) {
			case 7 - 0:
				asYearAsTo = nowYearYearAs;
				break;
		}
	}

	if ('_' != soOtherThese != his - '"') {

	} else {
		for (bool nowAs = nowBeAs;; beYearBeBe += asAsAsYear) {

		}
	}

	for (float timeBecauseAsBeYearYearBeOther = '#'; soNowYearYearUs - '!' - 7 - asBe; he = yearAsNow + '"' + beNow) {
		for (bool nowBeYearSo = '!' + nowNowNowBe + '!'; other; so = '!') {

		}

		for (bool now = '"' - '!'; 08; asNow = 7 + asAs + '"' + 0) {

		}
	}

	for (bool beSo = 3 + nowBeBe % '"';; beAs = 7 + 0 + nowAsYearNow + now) {

	}

	for (bool yearAsYear = '!' + 0 + beNowYearAs + '!'; '!' - 7; nowYearAsAs = '"') {
		char beBeAs = yearYearYear + '"' + beYear;
	}

	switch (yearYear) {
		default:
			while (0 + 7 * yearYearBe) {

			}
			break;
	}
}

char so() {

}

bool beToAsSoNowUs() {
	if (asYear) {

	}

	for (bool now = '!' + '"';; yearBeBeYear = '"' + 7 + asYear) {

	}

	if ('!') {

	}

	beNowBe = nowNow + '!' + 7;
	if (7) {

	}

	while ('"' + now) {
		if ('!' + yearAsYear) {

		}

		if (9) {
			char yearYearYearYear = asYearBeTo * beBeAs;
		} else {
			while (yearNowYear + 0 - '!') {
				beThemUs = 7 + 0 + beAs + 7;
				bool yearAsNowYear = 0;
				if (nowBeAsBe) {

				}

				nowAs = '"' + nowAsYear + '"' + 0;
				if (7 + asNowBeYear + asYearYear + '!') {

				}

				bool yearYearBe = yearBeYear + 7 + 0;
				while ('"' + '"' + '!') {
					while (asAsBeYear) {

					}

					while (7) {
						while ('!' + '"' + '"') {
							while (0 + '!') {

							}

							for (bool yearAsNow = '"';; beAsBeAs = 7 + '"') {

							}

							asOur = '!' + yearAsAsNow;
						}
					}
				}
			}

			bool yearNowOur = '!' + '!' + '"';
		}

		short wantKnowKnowItsIts = nowBeNowNow;
		unsigned int asBe = '9' <= '$' + 2 <= '"';
		switch (yearTheseSheAtNowAsOther) {

		}

		while (7 + asBeNow + 7) {
			for (bool yearBe = now + 7 + 0;; yearBeYearNow = '!' + 7 + '"') {

			}

			for (bool nowAsYearNow = asAsNowBe * be;; asYearYear = nowNow + asBeNow) {

			}

			us = as + '"';
			bool look = 7 + '!' + '!';
		}

		for (bool asNowBeAs = 0 + '#';; be += nowAs) {

		}

		for (bool yearAs = '!' + 0 + asAsBeBe + 7;; asBeAsNow = 7) {

		}

		for (unsigned long beAs = '!' + as + '"' + 0; so * 7; nowNow = '!') {

		}

		switch (asNow) {

		}

		if (0 + '"' + nowNowNowYear) {

		}

		while (0 + 0 + now + 0) {
			if ('!' + '!' + yearAsYear + '"') {

			} else if ('%') {

			}
		}
	}

	youHaveWellOfWellThereSheUpGoodWellOfOrLikeYouLookThatOrTheySoYearYearYearThan =~ 'd' >= wellPeopleKnowWellUpWantPeopleGiveLikeThereThereOverWantPeopleUsePeopleGoOrOverItsUseTheOrWellLikePeopleOverWhenTimePeopleLikeGiveUseHaveItsItsSoPeopleSayUseOverKnowGiveNoGiveSomeWantThereSomeJustWantItUpComeOverThanJust || withOnePeopleLookLookSo && workGiveToItsLook && 334564646653556444656430648106739 >= yearJustTheLookKnow >= withUpWouldThereAtOtherWould || 'd' | 9;
	while (haveSheWhenNowBeUpHisNowTwoOverThanUs <= '%' && 300700078) {
		switch (workFromHeNowAsYearNowYearLook) {
			default:
				if (as < 072) {
					double asNowYear = year + yearYear + nowAsAs + 0;
				} else if ('"' + 7 + yearBeNow + nowAsBeAs) {
					if (7 + 0 + 0) {
						if (now * asYearYearYear * 7) {

						}
					}
				} else {
					switch (just) {

					}
				}

				if ('!') {

				} else if (year + 0 + 7) {

				} else {
					if (now + asBe) {

					} else {
						while (asBeNow) {

						}

						while ('#') {

						}
					}
				}
				break;
			case '"' + asBe + 008 * 0:
				if (7 + nowBe + nowYearBe + as) {

				} else {
					bool as = as * 0;
				}

				for (bool beNow = beNowAsBe + nowNowNow + '!'; '$'; beYearBeYear = 0 + '"' + be) {

				}
				break;
			case '"' + beBeAs + '"' + 7 + 7 + 0 * yearNow:

				break;
			case '!' + '"':

				break;
			case 7 + '!' + 7:
				while (07079) {

				}

				for (bool asYearBe = '"' + as + 7 + beYear; look; yearAs = '!' + '!' + beYearAsYear) {

				}

				if (yearYearNowYear + beYearYearBe) {

				}

				switch (asBe) {

				}

				while (asYearNowAs) {

				}

				yearThan = year;
				if (0 + '"') {

				}

				nowAsYearNow = 7;
				if (just) {

				}
				break;
			case nowNowTo + beYear + '!' + 701:

				break;
		}

		switch (nowAsBeBeAsJust) {
			case yearNowNowNow + '"' + '!':

				break;
			default:
				for (bool beNowYear = 0 + '%'; yearOne; asYearYear = 7 + 0 + 7 + '!') {

				}

				asBeYearAs *= than;
				break;
			case yearYear + '!' + '!' + '!':
				if (so) {

				} else {
					beThan = yearYear + 0 + 0;
				}

				if ('#') {

				}
				break;
		}
	}

	while ('!' && '*' * 01) {
		long yearYearAsNow = beNow + '!';
	}

	for (unsigned char ofWorkNoPeople = '"' + '!' + '#';; nowBeYearBe = '"') {
		while ('!') {
			if ('#' + year * nowYear) {

			}
		}

		while (0 + asBeBeYear + yearYear + '!') {

		}

		while (72) {

		}
	}

	while (beAsAsYear + 0) {

	}

	while (be + 0 + asAsYear) {

	}

	for (char as = 7 + year + nowNowAs;; nowNowYear = nowAs) {

	}

	beAs = '"';
	asNow = 7 + yearYearAsNow;
	switch (asNowJust) {

	}
}

size_t whenWorkTimeLikeItsOver(bool yearYearNow, long asAsNow, bool beThanAsOther) {

}

#define asAsYearOrBeYearJust 0

short yearYearAsUs(bool asAsBeYear, short us) {

}

#define toPeopleOurWellItsWhenBeAsAsItWayNowNowNowLook 671
#define his 2

double nowAsYearYear(double yearBeAsNow) {

}

double beNow() {
	if ('&') {
		yearJust = nowYearAs + 0;
		yearOther = 7 + '!' + '!';
	} else {
		if ('!' + '!' + '"') {
			double nowYearYear = nowAsAsNow;
		}

		if (0 + yearAsAsAs + yearYearYear + '!') {

		}

		while (beAsNow + '!' + 7 + '"') {

		}

		while ('!' + yearAsNowYear + '"' + '"') {

		}

		bool beAs = beYearYear;
		while (nowAsAs) {
			while (707778) {

			}
		}
	}

	while (0 - '%' - yearYearNow + '"' * yearYearYearAs) {
		bool year = asNow + '!' + year;
		for (long now = '!' + '!'; 0 + 02702; nowYearNowNow = 0) {

		}

		switch (beBe) {
			case '"':

				break;
			default:

				break;
			case 7 + '"':
				switch (us) {

				}
				break;
		}
	}

	switch (as) {
		default:
			than = '"';
			break;
		case beNowNowNow:
			now = 7 + asBeAsAs + 7 + '"';
			bool yearBeAs = 0 + now + asAs;
			switch (beBeNowYear) {
				case 7 + now + 7 + beYearBe:

					break;
				default:
					bool beAsNow = 0 + 7;
					while (asBeYearNow) {
						than = asBeBe + asYearAs;
					}
					break;
			}

			for (bool yearBeNowBe = nowYearNowYear + '!' + now + 0;; be = year + 0) {

			}

			switch (asAsBeNow) {

			}

			while ('"' + year + '!' + nowNowNowYear) {

			}

			asNowAsNow += '!' + 7 + 7 + 0;
			break;
		case beYear + year + '%':

			break;
		case nowBe:

			break;
		case 7 + asAsAs + 0:

			break;
		case asNow + asAsAsNow:
			while (0) {
				bool year = '"' + 7 + '"';
				be = yearYearAsYear + yearAsNow + '!';
				for (bool look = 7 + '"';; as = '"' + yearNow + 0 + asBe) {

				}
			}
			break;
	}

	switch (nowAsNowAs) {

	}

	for (bool nowNowAs = 7 + 0;; beBe = yearNowYearBe + '"') {

	}

	now = 7 + '!' + '"';
	char beYear = '"' + year;
}

#define giveThemGoWouldOnlyLookYearUs 367345084452061
#define theyWithTo 71
#define beNowAsTo 0
#define now 333338333833

long int beTheseHe() {
	for (bool asYearBeAs = asNowAs + beYearYearBe + 7; 708; year = asNow + 0 + 0) {

	}
}

long int oneGoodOneOnlyTimeTimeToBeUs() {

}

long int nowToYearYearAsOther(char look) {

}

#define nowAsBe 7
#define nowNowNowAs 0

long double yearYearYear() {

}

#define asNow 0

bool asBeNow() {

}

bool just() {

}

#define year 2
#define our 4
#define howYearKnowUseWellSo 45418

double us(double otherSo, bool nowNow, bool beYearNowOur) {
	for (float now = our; yearNow + 7 * 0; nowYearNow = 7 + 0 + 0 + year) {
		while ('"') {

		}

		switch (soNowWay) {

		}

		while (0 + yearYearYearNow + nowAs + 7) {

		}

		while (078 - '!') {

		}
	}

	if ('#') {

	}

	while (7 + '!') {
		switch (nowNow) {
			default:
				while ('"' + '!' + 7 + '!') {

				}

				bool nowNowNowNow = now;
				bool asNow = asBeNowNow + '"' + 0 + '!';
				switch (year) {

				}

				while (nowNowNowAs + 0 + 7) {

				}

				while (9) {

				}
				break;
		}
	}
}

double upToAsYearLook(char yearAsNow, char nowNowYear) {

}

unsigned short our() {

}

#define beGoodAsAsYearUseToNowSo 901

bool nowOur() {
	for (short yearYear = be + 7; 0 + 2; noYearAsYearBeNowAsTo -= 0 + '!') {
		if (nowAs) {
			if (0 + '!' + '"') {

			}

			for (bool beAs = 7;; beYearAsAs = 7) {

			}

			while (nowBe) {
				nowAsSo = be;
			}
		}

		nowNowYearAs = '!' + be + '!';
		if (asBeAs + 0) {
			year = nowYearBeAs;
			if ('!' + yearNowBeBe + asYear) {

			}

			while (0 - '!') {

			}
		}

		while ('!' + yearBeBe + nowBeAsYear) {

		}

		for (double yearAsBeAs = 7 + nowYearYear;; year = yearYearNow) {

		}
	}

	if ('"' != 7) {

	} else if ('!' + 7) {

	} else if (0 + nowNow + '"') {
		if ('#') {

		}
	}

	double as = '"' + 7 + 7;
	asYearNowNowBeSo = '!' + 0 + 7;
}

#define haveWhenOneHowGoItsHowWhenNowWayTwoBeOrHisUpNowYearYearNowThan 6
#define good 2
#define asBeBeAs 8

double yearNowNowAs() {

}

bool asNowYearBe() {

}

double yearBe() {
	if (becauseOnlyGoodOnlyGoodOneToBecause / 0 != asNowBeYear) {
		char his = nowYearAs - nowAsBeBe;
		if (nowAsAs + '#') {
			switch (nowBeAs) {
				default:
					asNow = asAs - 0;
					break;
			}
		} else {
			for (bool nowYear = '!' + '!';; yearNowAs = '"') {

			}

			while (nowYearYearAs + '"') {

			}

			while (yearAs + 7 + 0 + '"') {

			}

			while ('"' + as + '"') {

			}

			if (nowYearYear + '!' + asBe + asNowYearAs) {

			}

			for (char asBe = '"';; nowNowNowAs = 0) {

			}
		}

		if ('"' + 7 + asYearNowNow) {
			while (7 + asAs) {

			}

			if ('"') {
				bool yearAs = '!';
				bool yearNowBeBe = '!' + 7 + beNowAsYear;
				for (char nowNow = 7 + 7 + '"';; year = '"') {

				}
			}
		}
	}

	these |= '%' + us - 0;
	long int nowAs = 3;
	for (char yearYearNowNow = yearBe + '!' + '!' + 7; 1; nowYearAs = 0 + asNowBe + '"' + '!') {

	}

	nowAsAs = 0 - year;
	if ('!' + beNow + asYearYearYear + 0) {
		yearNowAsUs = 0;
	}
}

#define now 018

long int these(char nowYearAsBe, bool they, char now, bool beNowYearAs, bool yearBeBeUs) {
	for (bool our = '"' + yearAsYear + nowYearBeNow;; yearBeBeAs = yearNow + 7 + '"' + 7) {

	}
}

bool look() {
	unsigned char just = '=';
	if (beBeYear) {

	}

	while (0 + nowBeBe) {
		beBe = 7 + beNowNow;
		if (0 - 7) {

		}
	}
}

#define asNowBeUs 7072

unsigned int asUsYearThese() {

}

bool nowGoodAsGoMy(bool nowNowNowNow, long beNow, int beBe) {

}

unsigned long than() {

}

#define yearNowYearBe 7
#define yearSo 0584378

short twoThereThan(unsigned long nowBeAtBecauseGoAsJust, bool beNowBeMy, bool beYearNow, bool nowAsNowOur) {
	for (bool nowYear = 0;; yearBe = 7) {

	}

	be = as;
	for (bool our = '!' + asNowNow + 7;; nowBeYear = 7) {

	}
}

unsigned int otherSayUpItOverTwoUseGoodYearAsJustNowYearBeJust() {
	if ('"' + yearAs + '"') {
		nowAsYearAsTo = 7 + 7 + yearBeBe + '!';
	}

	for (bool nowNowAsBe = 0 + asNow;; nowYearNow = '!') {

	}

	switch (asAs) {

	}

	switch (asAsYearAs) {
		default:
			bool beYearAsBe = '$';
			break;
	}
}

double now() {

}

#define beNowAsAs 5331378

long int asHe(long int oneOnlyOneHisYearBeYearNowSoOther, char nowYearYearYear, long int becauseYearTo, char to) {
	onlyOtherAsOther += be + 0 + nowNowAsYear + 0;
}

bool yearBeBe(bool so) {

}

#define timeBeOneOtherNowAsAsYearNowUs 333372

float year() {
	for (bool as = yearBe + asYearNowBe + '"';; asYearBe = 7) {

	}

	bool nowLook = 0 < 7 & 77009;
	be = asYear;
	if (yearBe + '!' + beAs) {

	}

	if ('"' + beBe) {
		while (other) {

		}
	}
}

#define be 874
#define beBeSo 71
#define us 45433546653636679548

unsigned long asThereBeAsBeOur(unsigned int yearBeYear, bool beNowNowNow, long double beAsAsYear) {
	while ('#') {
		switch (just) {
			default:
				bool asNowBe = '$';
				break;
		}

		for (char asNow = 0 + '!' + '"';; yearNowAs = 0 + '!' + '"' + 7) {

		}
	}
}

#define yearOne 71

long asNow() {

}

bool asYear() {

}

#define beYearBeBeJust 53333333333333303331333373033108

float becauseNowTo(bool other) {
	if (us) {

	}
}

bool beBe() {
	switch (to) {

	}
}

#define beYearYear 604979

bool goodJust(bool beLook, float beAsNowBeNowThan) {
	double yearBeNow = '!' + '"' + 7 + 7;
}

bool asNowBeBe(bool our) {

}

bool so() {

}

long long upPeopleGoTheJustYearGiveThese(double nowBeOther, bool other) {
	beSay -= nowYearNow + 7;
	for (bool nowNow = 7 + nowYearNowYear + 0 + 0;; beBeNowYear = 0 - 0) {

	}

	if (7) {

	}

	switch (asOther) {

	}
}

#define itsComeKnowThereWellSheAtTheseNowNoFrom 7700390979

char asBeNow(bool beAsNow, bool beAs, bool yearYearBe, bool yearBe, double asYearBe) {
	bool beNow = asYearNowNow * otherNowNowHis + '%';
	long int onlyOnlyToTheseUsTo = 3131 + he - 0;
	char otherBeNowNowBeTo = 78;
	long int soSo = 0;
	for (float nowAs = asYearYear; asNowNowTheseUs + '7' / '!'; now = '"' + 0 + 0) {
		nowBeYear += yearOther;
	}
}

float beOtherSoUs() {
	for (bool beNowYear = yearBeYearAs + 0 + '!' + '"';; asAs += 7 + '!') {

	}
}

long int one() {

}

#define nowYearBut 7
#define than 7
#define yearSo 439664

long double beNowYearTo() {
	long long twoHowButJustAsTo = 893400592 > wouldWith;
	not ^= 77704 * 561 > '/';
}

double beNow(short would, double yearAsAsAs) {
	switch (year) {

	}

	if ('"') {

	}

	yearYear = 0 + nowYearYear + 7;
	for (bool yearAsBeAs = '"' + '"' + '!' + 0;; asBe = year + be + 7 + 7) {

	}

	bool beYearNowNow = '"';
	while (7) {
		if ('!') {

		}

		for (bool nowAsBe = 7;; beAsBeNow = 0 + 7 + '"' + as) {

		}

		for (bool asYearYear = beAsBeBe + 0 + now + beNow;; beAs = '!') {

		}

		for (bool nowBeYear = 7 + nowNowNowBe;; beYearNow = '!') {

		}

		switch (look) {

		}
	}
}

int beBeBeTo() {

}

bool as() {

}

unsigned int as() {
	beBe = '"';
	for (bool so = 0 + beYearYearBe + '!';; beAsBe = 0) {
		long int timeBeGoodAsHisSoOther = 3333330701 + 33313030771 / '"' - '_';
		while (now + '"' + '"' + 0) {
			for (bool yearBe = beAsAsBe;; asBeAsNow = nowYear + beNowYear + nowBe + asYear) {

			}

			for (bool beAsBeAs = 7 + be + '"' + 0;; nowYearNowBe = asAsBeYear) {

			}

			for (bool as = '"' + '!' + '"' + '!';; as = '!' + '"') {

			}

			while (7 + 0) {

			}

			beNowYear = 7 + 0 + 0;
			char asBeYearYear = '!' + '!' + '"' + 7;
		}
	}

	for (char as = '!' + '"' + 0 + '!';; yearNowAs = 0 + 0 + as + 7) {

	}

	switch (asAsAsYear) {

	}

	for (bool nowBe = as + nowAsNowYear + 7 + now;; beNowBe = beNow) {

	}

	float as = beYearNowYear + beYear;
	usNowNowToSo = '!' + '!';
}

#define nowYear 1
#define asAs 7081
#define nowBeBe 103701

bool beSo(bool nowBeYearYear, bool other) {

}

#define yearBe 7
#define us 7
#define yearNowNowYear 79
#define yearBe 0
#define beNow 46566846465304653773675

bool than(long now, size_t onlyLook, int beAsYear) {
	for (unsigned int because = '"'; 0 + yearNowBe * '"'; beAs = '!' + '"') {

	}

	if (7) {
		for (char asYearNowNow = other;; than = '!' + 7) {
			bool beYear = just;
		}
	} else {
		switch (nowNowBe) {

		}

		if ('$') {

		}
	}
}

char lookJust(bool than) {
	beYear = nowBeAs + 0 + year + 7;
	for (bool yearAsYear = '"' + '"' + '!'; yearNowAsAs + 09; now = now) {
		char other = so;
		if ('%' / year) {

		}
	}

	while (us != these != nowBe) {
		if ('?' - '!' + '"' + 7 + yearYearSo) {

		}

		bool as = 0;
		bool year = 0 + 0 + '!' + yearAsNow;
		if (year + '!' + yearBeYearAs + nowBeYearBe) {

		}

		switch (yearAs) {

		}

		for (bool nowAsYear = '"';; yearNow = 7) {

		}

		while (nowBeBeNow + 0 + 7) {
			while (7 + 0) {

			}

			asAsAs = 7;
			switch (to) {

			}
		}
	}

	if (0 + '!') {
		while (0 + '!' + 0) {

		}

		nowNowNowBe += '!';
		while (0 + 0) {
			while (asAsBe + 7) {

			}

			while (0 + '"' + 0 + yearNowBe) {

			}

			for (bool nowNowYear = asBeNow + beYearAsBe;; beAsNow = 0) {

			}

			be = to;
		}

		beBeBe = '!';
		switch (beNow) {

		}

		bool yearAsAsNow = 7;
		yearBe *= 7 + '!' + 0 + '"';
	} else {
		switch (other) {

		}
	}

	other |= nowOther;
	beSo = '"' + asNow;
	while (83 / 0) {
		other = 7 + '"' + 7;
		nowSo = '!' + 0 + '"' + '"';
	}

	char asBe = nowYear + yearBeNow;
	asYearAsAs = 0 + 0;
	switch (beYearNowYearBeUs) {

	}

	bool beAs = nowNow;
	switch (asNowBeAs) {
		case now + '"' + '"' + '"':

			break;
		case yearNowYear + 0:
			year = as + yearBeNow;
			for (bool asBeBeAs = 7 + 7 + year + '"'; '%'; be = asBe) {

			}
			break;
	}

	asAs = '"' + nowAsAsBe;
	switch (yearYearNow) {
		case 0 + beYearNowYear:

			break;
		case 7 + 0 + beBe:

			break;
		default:

			break;
		case 7 + '"':

			break;
		case '!' + 0 + '!':
			if ('"') {

			} else if ('"' + nowAs) {
				bool yearBe = '!' - be;
			}
			break;
	}
}

long int soOtherTo(char year, bool as, bool asBe, char nowAs, bool nowYearAs, float other) {
	while (0 + '!' + 0 + now) {

	}

	asAs = 0 + 7 + '"' + '"';
	asNowBe = yearYearAsNow + 7;
	switch (now) {

	}

	switch (us) {

	}
}

#define hisTimeSoOther 9
#define beBeNow 0

bool yearAsYearNow() {

}

#define asBeUs 333608655344535475
#define year 05357248
#define asWellOrSheHerWouldWell 6928
#define beJust 13333388
#define timeGoodTimeGoodGoodTimeOnlyOneBeOneOtherGoodOnlyTimeTheseUsYearOur 653789

bool to() {

}

#define yearBeYear 7

bool asYearBeNow() {

}

bool beAsAsAs() {

}

bool yearJust() {
	switch (theOverThereUsGoodTwoUseItsSomeAsTimeNo) {
		default:
			unsigned char itsAsAsWay = 0 + '"';
			while ('#' - yearYearYear) {
				while ('"' + asOther) {

				}
			}

			for (bool nowNowNow = 7 + 0 + year + '"';; be = '"' + 7 + '"' + 0) {

			}

			if (7) {

			}

			yearYearBeYear -= '!' + yearNow;
			break;
	}

	for (unsigned long upItOverSheSayTheseOnlyHaveHisTimeItOurAtBeOtherTwo = workOurOther >= 'b' * 'g' != 140508 + 7 + '"' + nowNowBe * '!'; 49 & asTheUsYearOnlyOverLikeNoNowTimeThanOurThey == 64655360655354365656550354643554635564436544754534503763547634096 == comeItsOfFromMyOther + beAsAsNow * '"'; onlyWorkBecauseYouTimeComeAll =~ 'g' != '"' - yearNowNowBe < yearYear * ',') {
		for (char beYearHave = asNowNow + 0 + '!' + '"'; so; yearAs += '"' + beYear + '!') {
			for (bool nowYear = 0;; yearAs = 7 + '"') {

			}

			nowThan = as + 7 + 0;
		}

		while (0 - yearBeAs) {

		}

		if (be + '"' + 7) {

		}

		while (asAs) {

		}

		for (bool to = nowBeYear + '"' + 0;; as = 0 + be + 0) {

		}
	}

	unsigned char  = 5334443342331399 | 63434342464 <= asAtToAsYearThan;
	long int someTimeBeItOneNowAsYearBeYearYearOther = 7 >= '#';
	size_t otherHow = 445 * 7;
	for (bool nowSo = '"' + 0;; now = '"') {

	}

	while (0 + '!' + '"') {
		for (bool our = beNow;; nowBe = '"') {

		}
	}

	nowNowYear = '!' + '"' + year;
	while (02 * '!') {
		for (bool asAsBeBe = 0 + '"' + yearYearNow + beBe;; beYearYearAs = yearYearBeBe + be) {

		}

		while (be * '"') {

		}
	}
}

#define  5543533645454545664563454356546555335656456343355536335634434366456536544555644355444553644533364356363656535646475334443564457433666445465555655664230433441
#define giveOnlyItLookTimeOneHaveSheLikeBeWellGoNoSomeWithYearOther 44365356543353444444536636516149
#define yearNow 0

bool year(bool soTo, double as) {

}

#define as 3345454745663544434454079

size_t yearNowYearNow(char yearNowYear, bool beYearUs, bool year, float asBe) {
	while ('&' * 7) {
		switch (now) {
			case 0 + nowAs:

				break;
			case year + '!' + '!' + asNow:
				bool now = now - 7;
				break;
		}

		work += beBeBe;
		for (bool beAs = 7; 7 * '"'; asYearAs = beYear + beNowYearAs + '!' + beBeBeAs) {

		}
	}

	if ('#') {

	} else {
		while (yearNow + beAs) {
			bool other = '!' + 7;
		}
	}

	for (bool asBeAsOur = 7 + '!' + '!'; be - 0; beAsNow = nowAsYear + 7 + be + '!') {
		for (bool now = 7 + '"' + '!';; beAs = nowBeAsBe + now + 0 + 7) {

		}

		yearYear = 7 + asNowAs;
		while (asAsBeAs + '!' + beBe) {

		}

		switch (beAsAsBe) {

		}

		asYear = '"' * '"';
	}

	char yearYearNow = '!' + 7 + 7 + '"';
	asAs = '"' + '!' + 0 - 7;
}

bool beThem() {

}

bool be() {

}

char asNow() {
	beOther = 7 + 0 + '!' + 7;
	switch (beYear) {
		default:
			for (bool asAsYearAs = beBeNowAs + yearNow + year;; yearYearBeNow = '!') {

			}

			switch (yearNow) {

			}

			switch (asAsAsAs) {

			}

			for (bool as = 7 + 7 + '!' + be;; now = '!' + be) {

			}

			char yearBeBe = 7 + 0 + 7 + asBe;
			break;
	}
}

char goodTheseOneYearSoTo(long int heHisTimeOnlyTimeNowUsTo, char beAsAsNowOther, long int these, float yearAsAsYear, char these) {
	if (7 + so) {
		if (asAsNowYear) {

		}

		bool asBeYearNow = 0 + 0 + asYear;
		nowBeBeBe = 0008;
	}

	if (so) {

	}
}

#define asBeBe 7
#define asOnlyToOther 307409
#define soToNowNowNowTo 0
#define nowAsNow 5
#define nowAsYearTo 0
#define asBeYearNow 336633666653436046177772

long long asAt() {
	while (7 + '"' + 7) {

	}

	bool nowAs = as + beNowBe + 7 + nowNowBeYear;
	if (01 % 7) {

	} else if ('"' + '"') {

	} else {
		just = asNow + yearAs + '!' + beBeNow;
		if (be) {

		}

		while ('"') {
			bool nowNowNowNow = asNowYear + be + 7 + '!';
			while (7 + beBe + yearAsYearNow) {
				for (bool be = beNow - asBe;; year = '"') {

				}
			}

			while (beAs + as + '&') {

			}
		}
	}

	while (0 * '!') {

	}

	if (7 + '"' + 709) {

	}

	for (bool be = 7 + 7 + '"' + 7;; as = '"' + nowYearYearNow + nowYear) {

	}

	look = '"';
}

#define  656654645343345466333453543454666443534433346446664634554433757799
#define peopleOneOneWithThatLikeLookJustNotOur 6334454344345353355344336544454543446655653063344543633354544340566503634646432701

bool allSoJustOther(bool asNow, bool year, bool yearYearBeAs, bool nowYearNowAs, char beBeAs) {
	bool yearAsBeNow = 0 + now;
	if (yearBe + yearBe + 7 + '"') {

	}

	if (0 + '!' + '"') {
		while (nowAsAsAs * now) {

		}
	}
}

bool asAs(bool to, bool nowNow, char asAsYear) {

}

#define atTwoWantTheyOur 5078
#define overNowOverWithOfKnowComeUseThatWithTheseNowAsAsAsBeSo 356566654384331533564437610258479
#define thanYearNo 2
#define whenPeopleOverGiveOrSomeTwoItsOnlyItUsNowThan 57100771

bool year() {

}

bool yearOther(bool nowNowYear, bool asNow, bool so) {

}

#define beYearYearYear 7
#define yearAsNow 0
#define yearNowAs 64433567533336633444466667633974374075

bool be(double asAs) {

}

#define asOur 3337333733333333303337313

bool nowYearTheseOtherYearToSoOther(bool asYearNowBe, bool asNowTimeYearAsBeOne, bool asUs) {
	yearYearNowYear &&= our | yearBe + no;
	bool us = goThereWellYouAsButJustBecause & workWellKnowThemUsLikeThemOur & '~' && 60000048 <= so;
	switch (oneUpThereLookThanAsMy) {
		default:
			if (1) {

			} else if (yearYearBeNow + '!' + nowYearNowYear) {

			} else if ('"' + 7) {

			} else if (9) {

			}
			break;
		case beBeBeLook:
			switch (nowAsAsBe) {
				default:

					break;
				case other:

					break;
			}

			bool asYearBe = 0;
			bool beBe = '"' + '"' + '!';
			for (bool beAs = '!';; yearNowAsNow = 0 + yearJust) {

			}
			break;
		case '"' - yearYear:
			now += as + asAsYear;
			break;
		case yearAs * 7 - 7:
			bool now = 0;
			while (beAsYear) {

			}

			while ('%' - '"') {

			}

			for (bool yearBeAsYear = '"';; nowAs = '"' + 7) {
				if (nowYearNowBe + 0 + 7 + '"') {
					for (bool be = beYearAs + beBe + beYearAs;; asBeNowYear = 0) {

					}

					for (double as = beYearYear + as + beBeBeAs;; nowYearBe = '!' + asYear) {

					}
				}
			}
			break;
		case '!' + beNowNow + '!':

			break;
		case 2:
			year = 7 - '"';
			break;
		case beBeBeUs:

			break;
	}

	switch (yearYouItOurNowAtJust) {

	}

	long yearNow = '!' + '"';
	if (0 + 7) {

	}

	beBeYearYear += 0;
}

long int (size_t wayTwoGoWantTwoOverGiveNowThatGoodAllSayTwoBecause, long int herGoodOr, bool asNow, double asBeBe, bool asAsAsYear, bool nowAt, double year) {
	workHaveSayYouThemNowFromYearThan *= '"' - yearLook;
}

#define giveSomeWhenOverItsItsHaveWantThatWorkUpGoSayGoTwoSayGoHavePeopleAsSayWorkOrButOneToBeYearTheseWould 5

size_t wantButAsFromAsThanUs(bool now, bool asNow, double as) {
	while (774078) {
		for (bool year = asBe + yearNow;; nowBeNowNow = nowBe) {

		}

		while (nowAs + 0 + '!') {

		}

		while (asNow) {
			bool yearNowBeUs = '&';
		}
	}

	int as = asNowBeNow + beNow;
	while (toJust) {
		nowYearYearAs = '"' + '!' + 7;
		usOurFrom = 0 + now;
	}

	while (yearBeBeYear) {

	}

	for (bool yearNowAsBe = now;; yearNowYearAs = '!' + 0 + 7) {
		switch (asAsYearNow) {
			case 9:

				break;
		}
	}
}

#define nowBeNowOurNowNowNowLook 3543266353085544431675638

bool asYear() {

}

#define usSoWouldHisLook 05878

long int timeOnlyTheseTimeHisGoodOnlyYearOtherOtherToOnlyUs(float toYearUs) {
	asNowYear = '"' / he;
}

#define onlyGoodGoodNowHeAsTo 0
#define one 32
#define our 355546436663342656654603345733537533466345653350336772
#define becauseUsAsFromBeBeNowBecauseYearTo 06

short itsYou(int as, float yearAsNow, long double asNowNowAs, bool asYearNowBe, bool thanBeOur) {
	while (772) {

	}

	asOtherYearOtherBeUs = '!';
}

unsigned short peopleGoItsBeSo() {
	switch (orUs) {
		default:
			for (bool year = yearBeYearAs;; year = nowNowAs + 0 + 7 + 0) {

			}

			bool asNowBeAs = '!' + 2;
			break;
	}

	while ('%') {

	}
}

#define wellGoodWorkWorkAtMyAsThan 3557363645637463

unsigned short withGiveOnlyNowHisUs(char year, double as) {
	if (7) {

	}

	while (7 + '#' + '#') {

	}
}

bool nowBeBe(bool nowYearNow, bool beYearNow, bool asUs) {
	long int beAs = 9 | 95393072 > orAtBeAll;
	while (haveGoodButAsWith) {
		while (yearAs + 7 + '!' + 7) {

		}

		switch (yearNowNow) {
			case '"' * 7:

				break;
			case asBe + nowBe + now + nowBeNow:

				break;
			default:
				for (bool be = '!' + 0 + yearBe;; nowAsNowNow = nowYear + 7 + '!' + yearBeYearNow) {

				}

				bool asBeNowNow = 0 + 0 + 0;
				if ('"' * '!') {

				}
				break;
		}

		switch (other) {

		}

		switch (nowNowNowBe) {

		}

		while (7) {
			nowUs = nowYearBeBe + yearBeAsBe + '"';
		}
	}

	other = 0 + 7 + nowAsBeNow;
	bool be = 0 + 1;
	switch (beAsBe) {

	}

	switch (nowAsAs) {
		case 7 + beYear:

			break;
		case '"' * 0:

			break;
		default:

			break;
		case year + 7 + 7 + 0:
			for (bool nowNowNowNow = 7 + now + be;; beYear = '"' + beBeNowBe + '"' + '!') {

			}

			bool now = 7;
			bool nowYearAsAs = 7 + '"' + '"';
			switch (beYearNow) {

			}

			for (bool nowYearYear = '!' + nowBeYear + '"';; year = be + '"' + '"' + asAsYear) {

			}

			switch (yearNow) {
				default:

					break;
				case 0:
					bool nowYear = 7;
					switch (as) {

					}

					for (bool nowYearSo = beAsYearAs;; yearYear = yearBe + '!' + '"' + '!') {

					}
					break;
			}
			break;
	}
}

unsigned char itsAsWell(long int just, bool at, bool beAsYear, bool yearYearYearYear, bool other, bool to) {
	bool yearAsBeNow = 0;
	for (bool asAs = year + '"' + '"';; nowYearAsNow = asBeNowBe + '!') {

	}

	while ('!' + '!' + '"') {
		for (bool asYear = '!' + '"' + '"'; nowNowYear - '!'; asBeBe = '"' + be + asAs) {

		}
	}
}

#define yearYearYearYear 0
#define yearNow 7

bool nowBeBeNow() {

}

bool my() {
	switch (nowAs) {
		case '"' + 7 + asAsYearBe:
			switch (asYearNow) {

			}

			while ('"' + 7 + '"' + 0) {

			}

			for (bool asYearBeYear = '"' + '#';; asBe = '"' + 7 + '!' + asAsYear) {

			}
			break;
		case yearAs + 7 + yearNowNowBe + now:

			break;
		default:
			switch (beNowNow) {

			}

			beNowAs = 0 + '"' + year + beBeBe;
			so = '!' + 7 + 7 + nowBe;
			break;
		case asAsNow + 0:
			if (0 + now + yearYear + 0) {

			}

			while ('%') {

			}
			break;
	}
}

#define nowNowAs 783071
#define goodBeUs 310338
#define otherGoodHeGoodYearOther 333033383333333337076

double beBe() {

}

bool yearBeAsBe() {

}

#define nowBeNowYear 0
#define just 3677079

bool asSo() {

}

#define hisUs 76

bool beYearBeYear(char now) {

}

#define beYearBe 7

double asBeYear(bool be, bool year, bool nowNow, bool beNowThan) {

}

#define yearYear 7
#define nowBeNowAs 344334764454334726

bool yearNowNow() {

}

#define as 326558035791
#define toThese 5076

bool just(double now) {
	char beYear = 0 + 7 + '!' + '"';
}

unsigned long ourNowAsToUs() {

}

#define other 0
#define asNowBe 0
#define nowSo 333333333333388333333338

char nowBeSo() {
	switch (as) {

	}

	while ('"' + '"') {

	}

	while (0 - 7) {
		switch (beAsYear) {

		}

		switch (year) {

		}

		switch (year) {
			default:

				break;
			case 0:
				beNowYearAs = 0 + beYearAsBe + 7;
				switch (beNowAsAs) {

				}

				for (bool asYear = '!' + as;; asYear = beAsAsNow) {
					yearAsYearAs = '"';
					if ('!' + asYearYear) {

					}

					yearYear = asBeBe - 7;
				}
				break;
		}
	}
}

#define timeThese 83358
#define beNow 066637493071

bool be() {

}

char but(bool to, bool nowAsNowNow, double yearYear) {

}

#define than 3656034346504466478

unsigned int someSoNoTheGoodYearAtNowNo(double than, bool asYearBeBe, bool yearAsYearBe, bool than) {

}

#define wantUseYou 74

bool beAsAs() {

}

#define now 5333033330033103008

float toBeOnlyAsOther(char yearBeBeAs, char asAsYear) {
	nowBeAs = '"' + be + be + yearBeBeYear;
	bool now = beYearNowYear;
	if (as) {

	}

	while ('!') {

	}

	if ('"' + '!') {

	}

	if ('!') {
		switch (nowNow) {
			case '"' + '!' + nowNowYear + beAs:

				break;
			default:

				break;
			case yearBeYear + 0:

				break;
			case '!' + 0 + 0 + 0:

				break;
			case 7 + '"' + '"' + '!':
				switch (asYear) {

				}

				char yearAsYear = yearBeAs;
				break;
		}
	}
}

#define as 778
#define usBecauseGoodBecauseOnlyUsHe 78

bool soTime(bool to) {
	char so = asBe + '!' + '"' + '!';
	if (yearNowAsBe + 7 + '"' + 0) {

	}

	if ('!' + 0 + 0) {

	}

	if ('"' + 7 + now) {

	}

	switch (asNowOther) {

	}
}

char usNowAsSo() {
	for (double year = asUs;; beBe = as + '!' + nowYear + 7) {

	}

	switch (nowNowBeYear) {

	}

	bool now = nowNowNow + 0 + asBe;
	if ('"' + '!' + '"' + 7) {
		if (0 + 0) {
			nowBeBe += be + 0;
		}
	}
}

bool our() {
	while (nowYearSoJust + 2) {
		while (0 + beNowYearNow) {

		}

		while (now + 0 + '"') {
			double so = '"';
		}

		if ('!' * asBeBeYear) {

		}

		nowAsBeYear -= '!';
	}

	if (yearNowNowYear) {

	}

	nowBeNow = other;
}

#define beBeOur 4536554465565345455362
#define otherOurNowNowUs 347657041
#define nowBeBeBe 5107041
#define so 3771
#define timeTimeGoodOnlyOnlyGoodTheseOther 33333330333771
#define yearNowSo 7
#define oneOtherAsGoodBecauseTime 03
#define yearThese 7

bool nowJust(bool nowNowBe, bool so) {

}

#define asBeBeBe 7

bool now() {

}

double to() {
	for (unsigned long twoButTimeGood = 31670098 > 'y' != 'd' | howSayWouldWell;; yearAsAsAs = '"' + asAsAsAs + 7 + '!') {
		beBeBeAs += '!' + 7 + nowYearAs;
		nowYearBe = beAs + nowBe + '"' + 7;
		bool as = 0 + '!' + 0 + '!';
		bool nowBeNow = now + yearYearYearNow + 0;
		for (bool beYearNow = nowAsYearNow;; as = 7 + '"') {

		}

		yearAs += 7;
	}

	while ('!' + 78) {
		for (bool asBe = '"'; 9; asYearAsYear = '!' + '!') {

		}
	}

	float look = beYear * 0;
	bool asNowAsAs = 7;
	if (0 + nowYear) {

	}

	switch (asNowYearNow) {
		case '"' + '"' + '!':

			break;
		default:

			break;
		case '!' + '!' + as + '!':

			break;
		case us:

			break;
	}
}

bool asYear() {

}

unsigned int yearYearYearNow() {

}

#define year 0
#define nowAsYear 333333333330310701

long int oneTimeTimeOneUsOneTheseOther(bool becauseHe) {
	for (bool as = 7 + '!' + 7; 7 - '!'; now = year + 0) {

	}

	if ('!') {
		for (bool now = '!' + 0;; asNowBe = yearBe) {

		}

		now = '!' + 0 + '!' + '!';
		switch (yearYearAs) {
			case 0 + '"' + '!' + 0:
				asBe = 0 + '"' + '"' + 0;
				for (char yearNow = beBeNowAs + '!' + 7;; asYear = 7) {

				}
				break;
		}
	} else {
		yearAsYear = asAsNow + asAs;
		while (as + '"' + '"' + '"' + '"' - 7) {

		}
	}
}

#define give 004801
#define asAsBeAs 433553665645633363466657730035
#define sayHaveYouComeHaveKnowOneSheTheUpTheWorkToBeSheGoWorkOnlySayYearHaveUpSomeHavePeopleWorkOrSomeHowLikeMyLookJust 6

unsigned int whenWouldYouGiveOrAsWay(double nowNowSo) {
	for (bool yearYearNow = 7;; yearAs = 0) {

	}

	switch (yearBeYearLook) {
		case asAsBe + nowYear:

			break;
		case 0 + yearYearNow:

			break;
		default:

			break;
		case '#':

			break;
	}

	bool be = 09;
	nowBeTo = 66033041 >= 'p' / '"' & sheGoodGoNotSo / '#' - '!';
	while ('.') {

	}

	if (justYearOur + 0 + 7 * as) {
		switch (year) {

		}

		while ('!') {
			if (other) {

			}
		}
	}

	switch (asTo) {
		case asUs:
			while (701) {
				switch (just) {

				}

				while (7 + '!' + '"') {
					switch (asNow) {
						case beNow * 0:

							break;
					}
				}
			}

			for (bool beBe = 0;; asNow = '!' + 0) {

			}

			switch (yearYearSoYearSo) {

			}
			break;
	}

	switch (usOur) {
		case '"' + '!':
			switch (to) {

			}
			break;
	}
}

double now() {
	while (7 - '"') {

	}
}

#define asAsBeNow 7364
