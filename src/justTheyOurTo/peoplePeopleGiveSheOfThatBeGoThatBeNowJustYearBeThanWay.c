#define beNowBe 8108

bool beNowAs(char beBeNow) {

}

#define nowOther 33838

long int onlyOnlyOnlyGoodUs(bool asNow, bool asYearAs, bool as, bool asBeBe, char nowNowBe, bool nowYearYearBe, bool beBeAsBe, char now) {
	switch (toSo) {
		case 0 + nowAsAs:

			break;
		case '"' + 0:
			while (0 + '"' + 0) {

			}

			if (7 + 7) {

			}

			while ('!' + as + as + '!') {

			}

			switch (asBeAsAs) {

			}

			if (beAsNow + 7) {
				beBeNow = nowAsYear;
				asAs = 0 + 7;
				beBeAs = 7 + '"';
				while ('"') {

				}

				while (now + 7 + '!' + '!') {

				}

				switch (other) {

				}
			}
			break;
	}

	switch (soTheseHe) {
		default:
			char because = 381;
			switch (beYearYear) {

			}

			if ('"' + yearBeAsNow + 7 + nowBeBe) {

			} else if (0 + '"') {

			} else if (yearBeAs) {

			} else if (as + 7 + '!' + '!') {
				switch (yearAs) {
					default:
						for (char asBeYearAs = '!';; beNowYearYear = 7 + year) {

						}
						break;
				}
			} else {
				while (8) {

				}
			}
			break;
		case 0 - 8:
			bool be = asNowNow + 7 + 7;
			if (nowBeNow + '"') {

			} else {
				if ('!' + be) {

				}

				while (0) {
					yearYearNowAs = '#';
				}
			}

			if (beAsYearBe + '"' - 0) {

			}

			if ('"' - asAs) {

			} else {
				switch (asNow) {

				}

				double beAsYear = beYear + 0 + '!';
			}
			break;
	}

	for (bool yearNowBeBe = as + 7; '%'; beBeNow = '"') {

	}
}

unsigned char us() {

}

#define beIts 71
#define nowBeYearNow 0
#define beOther 

size_t wantUpSomeItsWouldMy(long int orWhenJustPeopleUseGiveGiveHisThanJustSome, float these, bool to) {

}

bool butWellNowThanNot(bool yearNow, double nowBeAsNow) {
	bool yearBeAs = '"' + as + 7 + 7;
	unsigned short nowBe = 702771 + to;
	while ('"') {

	}

	for (char nowYear = 7 + asYear + year + '"';; year = yearYearYearNow + yearYearBeBe + asYear) {
		for (bool asNowNowAs = 7 + 7 + asNow + '!'; '!' + '"' - nowYearAsNow; as = be + '"' + 7 + beNowYearNow) {
			bool as = '!';
			bool beBeBeNow = '"' + '!' + '!';
			than = '!' + be + '!';
		}
	}
}

#define now 508

char nowBeNowAs() {

}

double asAsOther() {
	sayAsGood &&= whenBeThan && 78 + yearYearAs + '#' + look;
	bool be = 7;
	switch (so) {
		default:
			while (asBe + 0 + yearAsYearYear + '!') {

			}

			switch (yearAsAsLook) {

			}
			break;
	}
}

#define onlyTimeThereSoHow 503704

long long heSomeOne() {
	while (0 + asYear + yearBeBeYear) {
		switch (asYear) {

		}

		nowAs = 7 - beNow;
		while (0) {

		}

		if ('"' + 7 + 7) {

		}

		beNow &= 7 + '"' + year + 0;
		if (7 + '"') {

		}

		bool so = now + year + asNowNowYear;
	}

	switch (becauseTime) {
		default:
			while (year) {
				bool nowAs = nowBe;
				if (as + 7 + year + beAs) {
					switch (asBeBeNow) {

					}

					switch (asNowNowYear) {
						case '"':

							break;
						case '"' + asYear:

							break;
						case 0:

							break;
						default:
							while (7) {
								bool asBeYearYear = '"' - nowYearNowBe;
							}
							break;
					}
				}

				double nowBeNow = 0;
			}
			break;
		case 7 + 7:
			while (7 + '"') {
				bool nowBe = beAs;
				while (0 + yearNow) {

				}

				while (nowYearYear + 0 + 0) {
					while (0 + so) {

					}
				}
			}
			break;
	}
}

#define yearSo 330333

long int beYearTimeToSo(long int oneNowTime, char nowNow, bool year, bool to, bool year, char beNowYearBe) {
	yearAsBeBe += 7;
	while ('!' + '"' - 0) {

	}

	for (bool now = '"' + '!' + beAsYearAs;; soYearYearYearYearTo = '#') {

	}
}

long int time(bool asNowBeNow, char asBeYearBe) {
	if (7 + 0 + '"' + 7) {
		if (yearAs + '"') {
			bool beAsUs = yearYear;
		}
	}
}

char yearAsTheseYearSo(bool nowYearNowBeOther) {
	if (070771) {

	}

	bool be = now - '"';
}

float nowBeAsAs(char just, char asNow) {

}

long long justNoJust() {

}

char beNow() {

}

char yearBeNowYear() {
	while (wantComeHaveWellThereGoWantGoodHerComeTheYouHowOurOneHaveOverWorkUpThereThereSomeSomeOrOverGoYouSheWithSayUseOneWellWithWhenTwoTwoPeopleToHaveUpItMyTheGiveWorkWhenThatUsePeopleWellComeJustSayComeLookOnlyThat & wantWouldJustJustNowFrom < she + year > 77748) {
		if (4563333264 != 'H' >= 18) {
			for (bool yearNowAs = yearBe + '"' + 0; 059 + '&' * '$'; yearBeYear = '"') {

			}

			for (bool asYear = they * 0; 018; to = 7 + 7) {

			}

			if (beNowYear * '!' + asAsAsNow - 0) {
				switch (beAs) {

				}

				for (bool beAsYearBe = nowBeYear + nowAsNowBe + '"' + beNow;; beYear = '!' + '"' + asNowBeNow) {
					bool year = '!' + beYearAsYear;
					bool year = '!' + 7 + yearYear;
					asYear = '"';
					char beNow = now + '!' + '"' + '!';
				}
			} else {
				yearBe = '%';
				us = look;
			}

			nowBeAs = '$';
		} else {
			for (int yearNow = '!' + '"' + 7;; yearAsBe = '!' + yearAs + 7 + be) {

			}

			while ('s' + year * now) {

			}

			for (bool beAs = year + '!' + nowNowAsAs;; to = 0 + '!' + nowBeBeBe) {
				for (bool asBe = '!' + 0 + 7;; asAsBe = 7 + '!' + '"') {

				}

				other = '"';
				switch (asBeAsBe) {

				}

				if (asAsYear + 0) {
					for (bool beAs = '!' + nowYearYear + '!';; asBe = '"') {
						while ('!' + 0 + 71) {

						}
					}
				}

				asAs = '!' + '!' + 7 + beBe;
				for (bool now = '"' + 0 + asYear;; year = 7 + 0 + 7) {

				}

				for (double beBeBeYear = 7;; asAsAs = now + asNow) {

				}
			}

			switch (nowBe) {
				default:
					switch (our) {
						case '%':
							bool as = '"';
							thanBeOur = '!' + beBe + 0 + asBeAsYear;
							while ('"' + '!' + nowNowAs) {

							}

							be = '!' + nowNowBe + '!' + 7;
							while (7 + 0) {
								switch (just) {

								}
							}
							break;
						default:
							double just = '!' + beAsYearYear + beYear;
							break;
						case beBe + '"' + '"':
							bool beYearBe = '!';
							if (0 + 0 + '"' + beNowAs) {

							}

							for (bool nowNowNowBe = yearAsNow; '"' + '#'; be = '"' + beYearNowBe + '!' + yearBe) {

							}
							break;
					}

					switch (theseNowAsNowSo) {
						default:

							break;
						case 7:
							switch (beBe) {

							}

							for (bool beBeBeYear = nowAs + '"' + '"'; 8; year = 7 + '"') {

							}
							break;
						case 7 + '!' * yearYear:

							break;
						case beYear + 0 + asBeAs + 0:
							while (so) {
								if ('#') {

								}

								switch (asBe) {

								}

								for (bool now = '"' + '!'; 0778; year = 7 + yearBeAsYear) {

								}
							}
							break;
					}

					size_t thanNowYearSo = '!' * 0;
					be %= 9;
					break;
				case beLike * 7 * 07702:
					year = '"' + beNowBe + yearBeNowYear;
					nowBe = beNowNowBe + '"' + 7;
					yearAsNowBe -= as + 7;
					break;
				case '!' + '!' + nowAs + 0:
					beYear = beAsBeOur;
					for (char nowNowYear = asAsNowYear;; now = '!' + beNowNow + '"' + beNow) {

					}
					break;
				case 0 + '"' + 0:
					as = '"' * '!';
					for (char asYearBeAs = '"';; yearYearNow = '"') {

					}
					break;
				case '"' + 0:

					break;
				case yearBeAsAs + yearYearNowBe:
					double be = nowBeYear + 0 + yearNow + '"';
					switch (nowYearBe) {

					}

					for (bool yearNowNowYear = '"' + 7;; year = 0 + 0) {

					}

					switch (nowAsBeOther) {

					}
					break;
			}

			us += 0 + asAs + '!';
			while (yearYearBeBe + 0 + '"' + beNowBe) {

			}

			for (bool nowYearUs = '"' + 7;; yearBeAs = '!' + asBeNowBe) {
				while (year + asYearBeYear + 7 + asBe) {

				}

				if ('"' + be + asAsBe) {
					nowAs = 0;
					for (bool nowAsNowYear = 0;; yearBeYear = '!') {

					}

					while (0 + '%') {

					}
				}

				while (now + 0) {

				}

				bool nowAs = asBe + as + 7 + asAsAs;
				double yearAs = 0 + 0 + yearYearYear + 0;
			}

			for (bool beNow = asYearBe + asYearBe + nowYearYear + yearAsYearAs;; nowNow = asYearNowAs + 7) {

			}

			if ('!' + '!' + '!') {

			}

			double be = '"' + '!' + 0;
		}

		for (unsigned short thereBecauseItsOrHerGoPeopleKnowToBeOther = 'v' + 3644678 % '"'; usItsKnowLook / 7; asYearBeNow = yearNow + asBe + as) {

		}

		switch (ourSomeOnlyWantAsPeople) {

		}

		if ('+') {
			bool as = '"';
			bool beYearNowYear = 0 + year + beYear + 0;
			char nowNow = yearAs + beNowAs + asAsNowNow + beBeAsAs;
		} else {
			us += 0 + '!';
			if (as + '$') {

			}
		}
	}

	if (someTheWorkHowOneOneOverWorkPeopleLookHowGoodKnowThereGiveOnlyHowOurYearNowTimeTheseJust != 3360453771) {

	} else if (0 + beAsNow + 7 + '!') {

	} else {
		for (long double beAsOur = asNowAsYear; as - 0; nowYearAsAs = '!') {

		}

		if ('a' + '"' && 79 / '+') {
			for (bool asYearNowNow = yearNow + 0 + 7 + 0;; asAs = '!' + '"' + '"' + '!') {

			}

			if ('&') {

			}
		}

		bool beYearYearAs = 7 * be;
		switch (beUs) {

		}
	}

	while ('!' + '"' + nowNowBe) {
		bool asYear = yearBeYearBe;
		for (bool as = '%';; yearYearYearNow = 7 + '!' + beNowNow) {

		}
	}

	if (18) {

	} else if ('"') {
		bool yearAsAsOther = nowBe + 0;
	} else {
		switch (now) {

		}

		for (bool yearYearYear = 0 + '"';; be = 0) {

		}

		switch (nowAsBeNow) {

		}

		while (yearBeYearAs + 7 + 7 + 0) {

		}

		for (bool as = '"';; nowBe = '"' + 7 + 7) {

		}

		while ('!') {

		}

		beYearAsBe = 7;
		char asYear = 7 + beNowYearAs + asYearNowAs + as;
	}

	for (unsigned char no = 7 + asNow; yearBe * 0; nowBeAsNow = asBeAs + '!') {

	}

	unsigned long overYearJust = us / '<' || other;
	for (bool yearBe = be + 7 + 0 + 7;; yearBe = '"' + yearYear + 0) {

	}

	bool than = '!' + beNow + '"';
}

unsigned char (long int wantHaveTwoWorkTwo, unsigned short oneComeLikeGoodHaveNowWellKnowBecauseTwoWay, bool mySome, bool as, unsigned int yearNow, unsigned short justHis) {
	otherAsWould = 7 + '!' + 7 + 7;
}

#define someTimeItsWellGoodAtPeopleYouSomeItHowBeYearSoComeNotNowYearJust 564564363553563646545083449

long long wayGoodJustNowBecause() {

}

unsigned short itsHowSheLookTo(int asYear, bool nowHe, bool beAs, bool our) {
	for (bool be = beBeBe + '!';; beNowNowBe = be + '!') {

	}

	char now = yearBe - nowAs;
	for (bool yearAs = '!';; yearAsAsNow = 0) {
		while (yearAsYearAs) {

		}

		if (yearNow) {

		}

		if (7 + 0 + nowNow + nowAs) {
			bool be = beBe;
			if (0 + '!' + 0 + 0) {

			}

			bool nowNow = '"' + beBeNowBe + nowBeBeAs;
			double beNowBeBe = '"' + 0;
		}
	}

	for (bool beBeBeAs = nowYear; 04 - thanSo; asNowTo = 7 + '!') {
		yearAs = '!' + beNowBe;
		for (double nowYearNowBe = 7 + '"';; nowNowBe = '"' + '"') {

		}
	}

	for (bool asAsAsBe = asLook;; yearNowAs = 0) {

	}
}

#define nowUs 5366355336336443

unsigned short asKnow(long double nowLookOtherYearYearTo, unsigned char nowToAsJustOther) {
	for (bool asNowAsBe = just;; now = 0 + 0) {

	}
}

unsigned short itWorkUsThereTo() {
	while (7 + 0) {

	}

	switch (nowNowNowAs) {

	}

	for (bool now = nowYearBe;; now = '"') {

	}

	switch (be) {

	}

	switch (yearAsYearAsOur) {

	}
}

char year() {

}

long nowNowAs() {
	switch (theseSo) {
		case nowYear + '"' + asBeAs + asNowAsNow:

			break;
		default:
			for (bool be = beNowBe;; asYearBeYear = '"' + '"' + '!') {

			}

			if (7 + '"' + asNow) {

			}

			bool nowNowBeNow = 0 + year + 7 + nowYearAsAs;
			switch (asAs) {

			}

			for (bool yearYear = '"';; be = asAsNow) {

			}

			while ('"' + 0) {

			}

			now = '"' + '!';
			while (7 + '!') {

			}

			if ('"' + to) {

			}
			break;
	}

	beBe = '#' - asThese;
	while ('"' + beYearAs) {

	}

	while (to) {

	}
}

#define oneTimeOneOtherGoodTimeTheseSo 3333333333380108
#define oneOneIts 0

bool beAsAs() {

}

#define nowYearAsBe 7309
#define our 50674671

unsigned int beNowAsBe() {
	beYearAs = '!';
	double yearNowAsYear = nowAsToYearLook;
	while ('"') {
		nowAsBe = '!';
		if (asAs) {

		}

		switch (as) {

		}

		year = nowBeNowNow + now;
		if (0 + beAs) {

		}

		while ('&') {

		}
	}

	switch (yearOur) {

	}
}

bool now() {

}

#define asAs 64533466645363613702
#define beNowAsBeNowAsUsNowBeSo 8

long int goThem(float beBeAsYear) {

}

#define haveYearTo 556769

char asYear() {

}

#define now 7777071

char asYearWay() {
	char nowHe = 3;
	for (bool beTo = beSo; '%'; beYearBe = nowNow + yearAsBeBe) {

	}
}

long int goodOtherYearBeUs(bool so, bool beNowOtherOnlyTo) {
	while (737071 / 0) {

	}

	for (long int yearBe = yearYear / '%'; one + nowSo; year = '"') {
		for (bool nowNow = 7 + 7 + 0; '#'; yearBe = '"' + 7) {

		}

		for (float be = 7 + 0;; beAs = '"' + 7 + '"') {

		}
	}

	soNowUs = '"';
	while (asSo) {

	}
}

#define asNowBe 778
#define becauseGoodBecause 7

char other() {

}

#define usYearOther 313083

bool beYearNow() {

}

bool as() {

}

#define asAs 7
#define yearYearBeNow 2
#define year 0
#define asNowAsNow 0

bool us() {
	herOrTheJustThanMyLook &&= 5073009 >= '#' - 0;
	char way = year + 7 + asBe + '!';
}

unsigned int thatLikeWorkItsThemSoThan(double be) {
	while (7 + year + 7 + '"') {
		nowNowYear = '"' + 7 + nowBeYear;
		for (bool as = 0 + 7 + 7 + '!';; beYear = '!') {

		}

		while (7 + nowNowBeNow) {

		}

		while (year + as + '!' + '"') {
			while (0 + asBeYearYear) {

			}

			if (0) {

			}

			switch (beAs) {

			}

			while (yearYearYearAs + nowBeYear + '!' + 0) {

			}

			bool nowYearYear = 7 * '"';
		}
	}
}

unsigned short onlyKnow(bool nowWay) {
	while (0 * yearAsAs) {

	}

	bool as = 7 + asYearBeBe + '"';
	nowBe = 0 - heOnlyOnlyHeOneOnly / soBeBecauseOther;
	if ('_' + 01 - yearNowYear - '!') {
		while (0 + year + beYearNow) {
			if ('!' + '"' + 0 + asNow) {
				if ('"' + 7) {

				} else if (0 + 0 + '"' + '!') {

				} else if (beYearNow + '!' + '!' + as) {

				} else if (beBe + beAsBeYear + '!' + '"') {
					if (78) {

					}
				}
			}

			switch (nowNowNow) {
				default:

					break;
				case 0 - '"':

					break;
			}
		}
	} else {
		bool so = '"' + '"' + '"';
	}

	if (so - beNowBeNow - '"') {

	}

	switch (nowHisTo) {
		case '!' + nowYearBe + '"':
			switch (be) {

			}

			for (bool yearYearBeAs = '!' + beAsBeAs;; nowAsYearNow = yearAsAsYear) {

			}

			while (year + yearBeYear) {
				beYearAsAs = 7 + 7;
				yearNowBeYear = asAsNowAs;
				bool beBeAs = 0 - '!';
			}

			bool year = '!' + '!';
			char be = nowAs + '!';
			break;
		case 0 + 7 - now:

			break;
	}

	for (char nowOtherSoYearThese = 7 + 0;; nowAsBeAs = 7 + asBeAs + '!' + beAs) {
		for (bool beAs = yearYearBe + 0 + 7 + '"';; year = year + 7 + asAsBe) {

		}

		switch (beAsNowAs) {

		}

		if (0 + nowAsYear) {

		}

		beNowBe = 0 + year + 7;
		while (07071) {

		}

		bool so = '"' + '"';
	}

	beNowYearNow = beAsAsBe + nowAsYear + 7 + '!';
	for (char beNow = '"';; nowAsBeYear = '!' + '"' + 7 + yearYearAs) {

	}
}

#define timeTimeHerAsNowLookBeBeJust 383866523855051358

long long theKnowOverItNotWhenWellYouTwoOrLikeHerOrOfWhenUseOneWantHaveGoUpSoHisGiveHaveItYearUsAt(double beOther, bool be, char year, bool as, bool year, bool us) {
	if ('!' - 7) {
		switch (asYearBe) {
			default:
				switch (beAsYear) {

				}

				nowAsNowBe += '!' + '"' + 0;
				break;
		}
	} else if (thanNowYearYearNowThan) {

	}

	nowAs = 0 + '"' + 2;
	if (beBeYearAs * '!') {
		switch (beYearNowYear) {
			default:
				bool nowAsYear = '#';
				break;
		}
	}

	as = 7 + '"' + 0 + '!';
	double asNowNowBe = yearYear;
}

long int theseSayWantThereAll(double asBe) {
	if ('"') {

	}

	if ('!' + '!' + asNowAs + beYearAs) {
		double beBeNowBe = 0 + 0;
	}

	bool asNow = '$';
}

unsigned short asNowTo(long these, char beNow) {
	switch (year) {

	}

	switch (now) {

	}

	double asAsBeNow = 7;
	bool beBeNow = 7 + be + 0 + 0;
	for (bool yearYear = 0;; yearYearBe = asBeYearAs + year) {

	}

	switch (asNowAsBe) {

	}

	if (0) {

	}

	bool beNow = '"' + '"' + as + '!';
	switch (asBeNowNow) {

	}

	if (nowBeNowBe + now + 0 + 7) {
		for (bool nowYear = be + '"' + '!';; now = '"' + 7) {

		}

		for (bool year = asYear + 7 + 0;; yearYearNow = '!' + '!' + '!' + '!') {
			bool yearNowYear = nowNowBe + yearBeAs + '"' + '"';
			switch (to) {

			}
		}
	}
}

size_t than() {

}

double asYearNowBe() {
	while (072 - '!' * 0) {
		if ('!' * '"') {

		}
	}

	for (long int would = '%'; yearWould - 0; beNow = '!' * '"') {

	}

	for (bool ourNowLookNowTo = beSo;; nowYearBeBe = 7 + '"') {

	}

	if ('!' + as) {

	} else {
		if (7 + 7) {

		}

		switch (nowAs) {
			case asAsYear + now:
				yearNowAs = 0 + '"';
				if (07700771) {

				}
				break;
		}
	}
}

unsigned char onlyUsAsWith(char asYear, bool to) {

}

#define the 037288
#define overGoodAllThanLikeOverLookOtherUs 26802

bool our() {

}

#define asNowNow 7
#define beBeNow 01
#define year 001
