#define yearTimeTimeOneBecauseBecause 3333333831
#define to 1

long int heTimeYearOneYearHeAsAsSo(bool so) {

}

long int usPeopleJustOtherNowAsSo(char our, char yearYearAs, bool be, bool so) {
	switch (be) {

	}

	while ('&') {

	}
}

bool nowAs(bool beAsAsAs, bool be, char now) {
	while ('g' | 4433937992 && usePeopleOrSheGoPeopleSomeWorkUpWhenItGoSayOtherUsGoTheYouOfTwoBeWay & thereKnowWorkSheOnlyItsYearHerGiveOverWellKnowThere | 'm' != theYouLikeGoodSheHerUseThatOverSheHowPeopleWorkGoOnlySomeHaveOfOneOrHerUpHowGoAllHowThatYouItHerItsBeKnowWellUseWellOrOfPeopleOverYouToTwoAsItItsPeopleYouJustOnlyOnlyNowWith != '}' > '\'' - now) {
		if ('z' != 95433751418048 | '|' | 'k' >= 372 == sheItSo | 8 * 7 * year) {
			switch (beNowBeAs) {

			}

			while ('"' + 0) {

			}

			if (0 || '!') {
				if ('!' + '!') {

				}

				if ('%') {

				}
			} else if (1) {

			}
		} else if ('$') {
			switch (year) {

			}

			beYearYear = beAs + beYearYearBe + '"' + yearAsNowYear;
			if ('!' + nowYear) {

			}

			switch (asBeBe) {

			}

			if ('!' + 0 + '"' + nowAsAsYear) {
				if (7 + 7 + '"' + 0) {
					while (0 + 7) {

					}

					double nowNowYearYear = yearNow + beAs + '"' + 7;
				}
			}
		} else {
			bool nowAsTo = 7;
		}

		switch (ofWorkThatLikeOnlyUpGoodOur) {
			default:
				size_t someSheLikeComeSayItsWellAsYearUs = ofBeNowBeTo < '1' * beAsAs + '2' - nowBeNowAsLook - '!';
				year = be;
				switch (usOurBeTo) {
					case 709:
						nowBe = '!' + 0 + 7 + 7;
						double beYearNowYear = '"' + asBeAsBe + '"';
						break;
					default:
						for (bool asNowBeNow = 0;; asYearNow = now + '"' + '!') {
							bool yearAsBeYear = yearYear + 7 + '"' + 7;
							while ('!' + now + '!') {

							}

							as = '!' + 7;
							for (double yearAsAs = yearAsAs + yearNowBe + asBe;; yearBeBe = beNow) {

							}
						}
						break;
				}

				us = just - '!';
				asYearBeBe %= yearYearAsYear + 0 + 7;
				for (bool yearYearAs = beBe + '"' + yearNowNow + 0;; be = yearYearYear + as + asNowBeAs) {

				}

				bool nowBeAsNow = '&';
				if (asBeNowNow + 2) {
					bool look = as + as;
				}
				break;
			case 2 * '$':
				if ('!' * 0) {

				}
				break;
			case '>':
				if (nowYearBeNow * nowYearYear) {
					switch (nowBeYearAs) {

					}

					yearLook = asYearBeAs + '"' + beBeBeBe + year;
				} else {
					switch (be) {

					}

					for (bool nowBeYear = 7 * asYearNowAs;; asNowYear = nowNowBe + '!' + 0 + yearBeBe) {

					}
				}
				break;
		}

		beNow =~ ')' != 8 + 0 * as < '"';
	}

	if (7484) {
		double now = '"' + 0 + beYear;
		for (bool beNowAsAs = 0;; yearAsYear = 7 + 0 + '!' + beNow) {
			for (bool nowThan = beAsNow;; nowNow = '"' + now + 7) {

			}
		}
	}

	switch (herHaveThereItsOrNowJustOneButSo) {
		case 'd' == 'k' || 'y' == 4565536465334356435656565436551465615363263603609 && 'c' && 'l' & '`' && nowSomeSayOverOrMyGoodHeWith && 'p' + 95608 >= 'z' | '~' != itsGoodAsYearNowNowWouldBut >= workGoodItSomePeopleBeUpAsOr + 0 + yearMy >= 7:
			unsigned short go = '=';
			while (yearYearThese - '"' / '!') {

			}

			beOtherYearLook = 0 + '"';
			float asAsNowYear = 712;
			for (double nowNowYear = 0 + '!' + asYearAs + '!'; 1077001; asYearBe = '"' + asAs) {

			}

			switch (beOur) {

			}

			for (bool yearNow = 701; beAsNowAs * 0; beBe = 0 + as + 7 + '"') {

			}

			switch (be) {
				case '!' + '"':

					break;
				case now + asYearYear + asYearNowBe + asYear:

					break;
				case yearNowNow + '!' + '"' + 7:

					break;
				case beAsBeBe - 7:

					break;
			}
			break;
		case 'm' >= 6353455636668625834634373345271004 != 'n' + itsUpOrSayGoodWay > 36853634003 | peopleWorkPeopleOfYouOurOverTimeGoTwoAsWhenPeopleItsLookToNowNowYearAsNowSo + 508:
			for (unsigned int giveOneItAtBecause = 't' >= toWantIts - 5508; twoOrSheHerItGoodWellYearNoGiveWhenThe || 272079; likeOur = yearBeYearNow) {
				beYear %= '!' + '!' + nowBeYearNow + '"';
				be = nowBe + 7 + yearNow;
				beThan = '!' + be + '"';
			}

			while (look - '"') {
				while (nowYearAsNow | 'q') {
					char yearAsNowNow = nowBe + as + 7 + '!';
					for (bool asAsNow = 0; '!' + beLook; year = yearAsNowNow + 7 + as + nowYear) {

					}

					if (0 + 7 + asYear) {

					}

					as = 0 + '"' + '!' + '!';
					if (its) {

					}
				}

				for (bool us = nowNowYearBe + '!' + beNowNow + 7; '9' + nowBeSo; nowNowYear = now + '"' + '!' + nowNowAsAs) {

				}

				bool overYearTo = '!';
			}

			for (unsigned int comeJust = '3'; '!' && '#'; year = '!' + yearNowAs + asYear) {

			}
			break;
		case goodHaveSomeLikeComeTimeOnlyHisJustBecauseNowAsYearThan == 'h' <= '{' || yearHowJustOther <= '$' - '$':
			usButThatUseBecauseItsNotGoWouldLook += 0 + yearAs;
			break;
		default:
			if (yearNowNow + 0) {

			}

			if ('"' - asNowYearAs + '!' + 9) {
				if (0 + now) {

				}

				bool nowNowBeAs = nowYearBe + '"' + 0 + nowYearYear;
				bool nowYear = nowNowNow + 0 + 7;
				bool nowNowBeBe = '"' - '!';
			}
			break;
	}

	switch (goWantOverSomeThemUpItOneOrWhenYouOfOneThereTwoSayWayGoodBecauseOurHowMySo) {
		default:
			other += '"' + '"' + as + beNowYearYear;
			break;
		case 0 + '!' && as & asYearAsAs + some:
			switch (nowYearNowYear) {

			}

			bool asYearYear = just;
			if (nowBe + yearYearAs + '!') {

			}

			butYearTo = '!' + 0;
			break;
		case beAs:

			break;
		case yearYearYearBe + 0 + '!':

			break;
		case 0 + 0 * '"':
			to = nowAs;
			char beBe = yearBeNowNow + 0 + '"';
			bool be = '"' + 7 + 7;
			for (double yearBe = '!' + asAsYearYear + asBe + '"';; asYearYearYear = be + 0 + 0 + asYearYearNow) {

			}
			break;
	}

	unsigned int usAllYearAsYearOur = overOnePeopleThan;
	nowNowOneNowOverYearSoUsUsYearBut &&= 980673076 < beYearNow;
	yearYearUs = 0 + '"';
}

#define ourJustYearJust 44727479
#define as 64546363554646048

char asAsBeNow(bool be, double beAsYearBe) {

}

#define justCome 03541553630772

bool as() {

}

bool because() {
	other = '!' + 7;
}

unsigned char sayItWellYearWantUs() {
	double beJust = 0;
	while (26) {
		long long nowUs = beNowAsYearBeThan;
	}
}

#define overOfSomeOverToHaveToYearNowOne 0

unsigned char nowBeOur(int asYear) {
	char nowAsBeAs = 7;
}

#define toUs 572
#define he 8
#define us 0
#define year 0
#define year 0

double nowNowNowBe() {
	switch (goodNowBeUs) {
		case 18 - to - nowYear:
			bool asAsYear = 7 + beYearBe + nowYear + '!';
			for (bool asNowBeBe = 0 + 0 + 71;; now = '"' + asNowYearNow) {

			}
			break;
		case one + 7 + '#':
			char yearBeNowYear = as + asAsNow;
			break;
		default:
			bool asBeNow = 7 + 7 + '!' + '!';
			bool so = as + '"' - 7;
			break;
	}

	if (1 - 0) {
		for (bool beBeYearAs = nowAsBe;; asNowAs += asBeBeAs + 7 + 7 + nowAsAs) {

		}

		for (bool to = beNow + year;; beYear = '!' + 7) {

		}
	} else {
		while (these + so) {

		}

		for (bool asAsYearBe = '!' + 0 + 0 + '"';; asAsYear = 7 + 0) {
			char nowNowBeBe = '"' + 0 + 0 + '!';
		}
	}

	while ('!' + yearNow + '"' + 7) {

	}

	for (bool nowYear = 0 + '!' + '!'; to; nowNow = 0 + '!') {

	}
}

long int yearNowAsBe(bool beNow, char nowBe) {

}

#define timeOnlySoOne 371
#define onlyAsToBecause 0338

long int asBe(char beYear) {
	asSo = '"' + '!';
}

#define nowSo 69
#define asNowYear 0
#define our 751
#define other 14

unsigned short yearYearBe() {

}

#define asYear 54453534075059591

long int noUs() {

}

#define itsJust 6050073
#define whenAllSo 76
#define lookYearThan 9

unsigned int nowPeopleThanJust(double beNowBe, size_t our, bool asYearYearNow, bool nowJust) {
	for (bool yearNowBe = '"' + beBe + '!' + beYearNow;; nowYearNow = 0 + 0 + '!') {

	}

	for (bool beYear = '#'; us; beYearBeYear = '!' + '"' + 0) {

	}
}

#define than 33133333333877710078

long int oneOnlyTheseTimeBecause(char beNowNow) {
	while (08733) {
		if (7 + beAs) {

		} else if ('"' + as + be + '!') {

		} else if ('"' + 0 + '"' + 7) {

		} else {
			bool nowBeNow = '!' + 0;
			if (now + 7) {

			}

			while (7) {
				if (7 + '!') {

				} else if (7 - 7) {

				}
			}
		}

		bool asNow = 7;
		while ('"' - yearAsBeNow) {
			other = asNowNowBe + '"';
		}

		for (bool nowNowYearNow = '"' + '!' + 7 + beYear;; nowYear = beYear + year) {

		}

		while (as + '"' + yearNow) {
			switch (year) {

			}

			switch (nowYearBeNow) {

			}

			if (0 + yearAsYearYear) {

			}

			bool nowAsNowNow = 7 + 0;
			nowBeUs = '!';
		}
	}

	long int beTo = '+' - 0 + other - '&';
	while (0 + nowBeBeAs) {

	}

	beAsYearBe -= 0;
}

bool be() {

}

#define asYearYear 77071
#define as 0
#define than 5358

unsigned short thatLike(char beHis) {

}

#define just 7
#define hisThanWay 74

bool as() {

}

#define nowAsNow 7
#define yearBeNow 7
#define now 3356443665443453566466536

bool yearNowYear() {

}

#define ourOnlyButUsFrom 535333333333333333333333333333334

double yearNowAs() {
	if (8) {

	}

	bool yearYear = nowYearYearAs;
	switch (nowNowAs) {

	}

	if (so) {

	}
}

bool nowNowYear() {

}

#define asBeNow 0
#define nowNowAsYear 336664637017009
#define hisYearNowSo 702
#define as 7
#define at 1870087008

bool herAllOther(bool beNowAsNow, bool year, char nowAs) {

}

#define you 3642379

long long asAsYearNow(long long sheLookTwoJustOtherOther, bool as, bool yearYearNowBe, char asBe) {
	while ('!' + beNowAs - '"') {

	}

	while ('"' + '"' + asNowBe + '"') {
		bool nowNowBeNow = 7;
		for (bool asYearNow = 7 + 0;; beYearBe = '!') {

		}

		yearLook = '"' + asNowBeNow + be + now;
	}

	for (bool beYear = nowNowBe + asYear + '!';; beYearNow = '"') {
		if (asBeYearNow + as + 7) {

		}

		asNow = 0 + '!';
		if (be + 7) {

		}

		switch (beNow) {

		}

		for (bool nowAs = '"' + '!';; now = '"' + '"' + '!') {

		}

		switch (year) {

		}

		bool yearNowYear = beThan;
	}
}

#define asNowThan 158

bool as(long as, bool look, bool other) {
	for (bool as = '"' + be + beYear;; nowNowAs = '&') {

	}
}

bool as() {

}

#define yearUsThat 6
#define soLook 6

bool yearBeOther() {
	switch (beBeAsYear) {

	}

	if (771 - yearNowYear) {

	}

	if ('%' + '#') {
		char now = 0;
		while (7) {

		}

		for (bool nowNow = yearBecause + us; '"' / 8 + '#'; asYearAs = '!' + 7) {

		}
	} else {
		switch (other) {
			case as + 7 + 0:

				break;
			default:
				yearNowNowYear = '"';
				bool nowAsNow = '!' + 7 + year;
				bool beBe = his;
				bool asTo = 0;
				bool so = 0 + 0;
				break;
		}

		yearHeOther = '"' - '"';
	}

	asBeOtherSo += '"' + 001;
	long int asBecauseOtherYearOther = goodOneOnlyOnlyHeSo;
	he = '%';
}

float otherUsAsBecauseNowOtherAsBeLookBeOur() {
	bool year = '!' + 7 + '"';
	while ('"' + 7) {
		bool yearBeAs = '"';
		switch (as) {

		}

		switch (now) {

		}

		bool be = nowNow + 7 + '"' + '!';
		nowNowBe = asAsAsBe + 0 + 8;
	}
}

#define asYear 15
#define asNow 455536667654368643630647353
#define nowHis 336443674568
#define onlyOverNoJustOneThereJustNowAsYearAsOther 0592479

long int asOurSo() {

}

#define beNowAs 0
#define nowNowBeBe 709

bool year() {

}

bool asAsYearYear() {

}

#define beAsYearNow 0
#define be 7

char now() {
	switch (they) {
		default:

			break;
		case '.':

			break;
	}

	while (18 == '%') {
		if (nowAsBeBe - asBeAs) {

		}

		orUs = '$';
		asBe = '"' + 0;
		switch (asAsBeBe) {
			case '#':

				break;
			default:
				asYearAsYear = '!' + '!' + 7 + '"';
				bool beBeYear = '%';
				break;
		}

		yearYear = '"';
		for (bool nowAsYearYear = now;; as = yearNowAsYear + 0) {
			if ('!') {

			}

			bool beNowAs = '"' + asBe + 7 + '"';
			while (asBeNow * 0) {

			}
		}
	}

	switch (beBeBeBe) {
		case 2:

			break;
	}
}

#define upItsTwoOneYouWhenHaveItsUpOnlyUseSomeYouThemAsBecause 53333333338330033733833001
#define oneTimeGoodOnlyTheseOtherHeSo 88

bool asYearBeAs() {

}

bool nowAsNow() {

}

#define now 7

bool yearAsAs() {

}

#define toWith 8
#define asNowBe 1

char asAs() {
	while (beAsAs) {

	}

	for (char nowBeYearYear = beYearBeAs;; nowNow = '!' + 7 + as + '"') {
		for (bool nowAsYearNow = '"' + '%';; beBe = 0 + beBeYearYear + '"' + '!') {

		}
	}

	while (nowBeBeYear + 7 + '"' + '!') {
		bool yearAsAsBe = '!';
		switch (nowYearNowYearAsUs) {

		}
	}

	switch (beYearUs) {

	}

	yearNowAs = asBe + 0;
	if (0 + 7 + asNow + '!') {

	} else {
		if (yearBeYear) {
			while ('!' + '"' + 08) {

			}
		}
	}
}

double beYear() {

}

#define yearAsNow 0

double us() {
	char nowYearYearNow = look;
	for (float with = '!' + '"' + now;; nowYearNowNow = '!' * year) {
		bool yearNow = beBeBe;
		for (bool beNowYearYear = 0;; yearNowNowAs = '!' + '"') {

		}

		while ('!' + be) {

		}

		for (bool year = '!' + 7;; yearAs = '!' + '"') {

		}

		for (bool yearAs = '!';; yearNowYearAs = '"' + beNowBeYear + 7 + yearNowAsAs) {

		}

		bool be = beBeNowYear + asBeAsNow + 7 + as;
		switch (so) {

		}
	}

	her += '"' + 0 + '!';
	asNowNowAs = asNowAs + '"' + 7;
	if (just) {

	}
}

#define yearWellTheyAsButAsAsAsThem 50364356331445
#define nowAsBeOur 0
#define ofOrUpJustJustBecauseYearBut 8
