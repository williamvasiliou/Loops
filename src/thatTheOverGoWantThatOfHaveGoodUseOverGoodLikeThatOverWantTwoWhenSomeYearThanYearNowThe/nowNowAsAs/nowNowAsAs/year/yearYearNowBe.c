long int nowBeYear(bool yearNowYear, bool asAsAsOther) {
	if (738070778 - '!') {
		char year = 0 + '!' + asBe;
		while (008 - us + yearBeAsAs - 0) {
			if (nowNowSo) {

			}

			yearNow = asYearBe + '"' + '"' + 7;
			while (0 + 0) {
				bool yearYear = 0 - 0;
			}
		}

		while (to - 7 / 0) {
			switch (nowTo) {

			}
		}

		for (float us = 0 + '!' + 7 + nowNow;; beYearYear = 0 + asAsNow + 7 + 0) {

		}

		for (bool beAsNow = year + 0;; now = beAs + '"' + now + asAsNowYear) {

		}

		switch (yearUs) {

		}

		switch (now) {
			case '!' + '"' + '"' + '"':

				break;
			default:

				break;
			case be + 0 + '!':

				break;
			case beNowBeAs + nowYearYear:

				break;
			case '%':

				break;
		}
	} else {
		switch (asToBecause) {

		}
	}

	for (bool asBeBe = yearAsBeAs; nowNowTo - '%' - '+'; be = '!' + yearAsAsYear + beAs) {

	}

	if (beNow + '"') {
		for (bool beYearNowBe = nowBe + '!' + asBeNow + 0;; be = yearYearAsYear + 7 + 7 + '!') {

		}

		while (beTo) {

		}
	}
}

#define nowAsYearUs 301
#define asAsYear 330333808

float nowNowAs(long int usOtherHis) {

}

bool nowNot() {

}

bool beNow() {

}

#define beYearAsYear 62779

long beYear() {

}

double nowUs() {
	for (bool my = '!'; 7 + 7 + asYearYearBeYearYearNowYearBeYearOur; beBe = 8) {
		beAsBeYear ^= 0 + 0 + as + 7;
		if (0 + '"') {

		}

		for (bool yearBeYear = 7 + '!' + yearAs + beNowYearYear;; nowBeAsBe = nowNowYearYear + 0 + 7) {

		}

		for (bool be = 7 + '!' + asAsBe; asLook; beAsYearAs = 7 + yearNowNow) {

		}
	}

	for (unsigned long time = 0;; beNow = '!' + nowBeNowBe + 7) {
		while ('"' + yearAsNow + nowBeNow) {

		}

		for (bool year = '!' + 0 + 0;; as = '!' + 7 + 0) {

		}

		bool asNow = 0 + '!';
		for (bool nowAsBe = asBe + 0 + be;; be = '"' + 009) {

		}
	}

	for (bool useThey = you; 7 <= '$' - 7 + 0 * nowNowAsYear * '!'; soHisNowThan = beAs + '"') {

	}

	bool beAsAsAs = '"' + beBe + 7;
	for (bool yearNow = '"' + '!';; be = 0) {

	}

	double asYearBe = asYearYearNow + '!' + '"' + year;
	switch (beYearBe) {

	}

	for (bool beNowAs = 0;; yearAsYearNow = 7 + 0) {

	}

	bool asBeBeNow = than;
}

#define onlyThanItsWellAsSo 432157079
#define some 4507002
#define yearBe 533333333333333333337383313

bool timeGoodOnlyFromNotWould() {

}

#define haveUseOtherTwoKnowNowAsOtherSo 3335
#define wantMyAsOther 2
#define nowUs 0
#define way 70460240779

long long yearAsYear(bool as, int nowBeYearYear) {

}

char year() {
	herAsBeBeBeYearAsTo &= '"' + '"';
	for (long long usLook = 7; 66002 + soUs + yearBe - nowYearBe; yearLookAsLook -= be) {

	}
}

#define themHaveTwoGiveComeHave 35365046651
#define asThem 98702

bool yearBe() {

}

#define us 33333333387071
#define oneTimeTimeOnlyOneTimeOnlyTheseOnlyOneTimeOneNowThanThanSoNowYearBeBeLook 9048

bool beBe() {

}

bool beAsBe() {

}

#define yearNowBe 7
#define nowAs 435353365455533663663634453645143665757773

size_t itHowOneOnlySayTwoYouWhenOrTheOneOfSayHowOfTimeTherePeopleSomeComeTimeOrYouPeopleGoPeopleYouPeopleSomeWhenItWellThatGiveWantHowHerComeTheGoWorkWellHowUseGoOfWantThatPeopleComeOnlyUpSomeThatHowGoodBeBeHowOurThey(bool other, long int timeWantNowWellOnlySoNowOther, bool beUs, bool beYearYear, double yearAsYear) {
	while (7) {
		if ('&' - 7) {

		} else if ('!' + 7) {

		} else {
			other = '!' + asBeAs + 7 + nowAs;
			switch (nowNowYearNow) {
				default:

					break;
				case '!':

					break;
				case '!' + 0 + 7 + 7:
					for (bool asYearNow = 7; 7 * yearNow; nowNow = '"' + '!' + 0 + nowYearYear) {

					}
					break;
			}
		}

		nowJustBeOther *= 2;
	}

	while ('$' * '"') {
		switch (asYear) {

		}

		than -= '"' + 2;
		switch (so) {

		}
	}

	bool nowBeNowYear = 7 + '"';
	bool beLook = '$';
	for (bool beBeAs = 7 + '!' + '"';; now = 0 + 7 + nowNowAs + beBeYearBe) {

	}

	char nowBeNowNow = 0 + nowYearNowBe;
	if ('!' + '!') {

	}

	bool asAsAsBe = yearBeYear + '"' + beBeBe + '!';
	for (bool yearNowNow = 7 + '"';; beBeYear = nowAs + '"' + '!') {

	}

	while (7 + nowAsAs + 7 + '!') {
		for (bool asNowBe = be + 7 + yearAsYear + 0;; year = 0) {

		}

		bool asNowUs = '!' + '!';
	}
}

long long onlyWantGoodTimeWellSheOfOnlyComeItItsUseOnePeopleItYearThereOneItOrThatItsHaveSayNowNowBeTo(unsigned long sheNowThatSoNot, long double asSo) {
	short timeHisOf = 4852870771 && otherUse * 78;
	size_t wellLikeSo = asNowYear % '%';
	switch (beThan) {

	}

	switch (thanAsTo) {
		case '!' + nowNowYear + beYearAsYear + 0:

			break;
		default:

			break;
		case '#':

			break;
		case '!' + 7 + 0:
			if (us) {

			}
			break;
		case 0 - 0:

			break;
	}

	long nowYearYearYear = asYear + beAsBe + nowBeYear;
}

#define nowBeOurYearTo 53172
#define yearBeNow 0
#define nowYearBe 0

char nowNow() {
	if ('~' & '-' | '#' | 78709 / 8771) {
		if ('"' + yearYearNow) {

		} else {
			bool yearBe = '"' * 0;
		}

		double nowAsNowBe = nowAsNowBe + 0 * '!';
		bool yearAsNow = year + yearYearBe + '!' + beBeNow;
		switch (year) {

		}

		beBeBe = 0 + asYearAs + 7 + '!';
		while (beWould) {

		}
	} else if (7) {
		if (0 + '!' + '!' + now) {
			us = nowYear;
		} else if (1) {

		}
	} else if (asYearYear) {

	} else {
		long long us = '"' + 755 && asAsBe;
		double nowBeAsNow = '"';
	}

	while (overWellWantWhen < but) {

	}

	for (double now = '#'; 0 + '%'; as = '!') {
		for (bool beYear = beAs + as + 0;; nowNowYear = 0) {

		}

		char nowYearAsYear = 7 + 7 + '!' + '!';
	}

	if (7 + '$') {

	}
}

#define nowNowAsUs 0038

long int oneToNowAsBeSo() {
	as += yearNow + 7;
}

#define goodToHe 3333
#define timeBecauseTimeTimeYearOnly 333383333051701

bool asBeThan() {
	while ('$') {

	}
}

#define nowYear 4336534336135545433833645775

bool beLookTo(bool nowNowUsTo) {

}

unsigned char beNow(char nowNowNowYear) {

}

unsigned char otherSoThanLook(bool so, char year) {
	for (bool beAsBeBe = 7 + beYearNow + 7 + 7;; nowBeNow = '"' + 7) {
		bool asNow = 7 + yearUs;
		while (2) {

		}
	}
}

char be() {

}

#define beBeAsYear 0
#define asAsAsYear 7
#define asNow 7

char yearNowAsNow() {
	thanTheBeYearWith >>= '%' - 82 - year;
	for (bool nowNowNowYear = asBeYearBe - 0;; asBe -= 0 - '!') {

	}
}

long int oneComeSomeSayOnlyThanUpThereSay(bool nowSo, size_t yearNow, bool nowNow, long double beNowYearAs) {
	bool asThan = 7 + 7;
	switch (asBut) {
		default:
			switch (nowBeNow) {

			}

			for (bool nowAsBeNow = be + asAsBe;; year = asNowNow) {

			}

			while (yearYearAs + 0) {
				bool asYearBeBe = '!' + yearBeBeBe + '!' + '!';
				if (7 + 0 + 7) {

				}

				switch (just) {

				}
			}

			while (0 + nowNowAsNow + beBe) {

			}

			switch (yearBeTo) {

			}
			break;
	}

	ourUs = 09;
	if ('!' - '"') {
		for (bool beYear = 0 + nowBeYearNow + 0 + 0; just; as = '"' + 7) {

		}

		if (asNowBe + nowBe + asAs + '!') {

		} else if (7 + to) {

		}
	}
}

#define beBe 0
#define asNowNowYear 7
#define from 3333333333303313833133378

bool be(bool asNowNowBe, bool yearNowAs, float yearYear) {
	while (7 + '"') {

	}

	switch (be) {

	}

	bool beOther = beYearNowAs + yearBeYearAs + beYear + now;
}

#define asGoodGoodAsSo 33405

unsigned long beNow() {

}

#define be 7
#define yearNowBeAs 0
#define nowNowBe 0

double asBeNow() {
	unsigned char sheItUseTimeOnlyBecauseOfOther = 'e' || '\'' == 530199 || 6077700009 | '$' >= yearBeAsBe * ')';
	while ('3' + justLook - '!') {
		double asNowBeThey = '"' + beAsYearYear + '"';
	}

	ofSomeThereSayOrOverOnlySomeThanLook -= 9;
	while (0 + 79) {

	}

	switch (us) {

	}

	yearUs = '"' + 7;
}

#define asBeOther 0

long double beYear() {
	nowNowBe = '"' + 7 + 0 + now;
	if (asBeYear + '"') {

	}

	switch (be) {

	}

	bool be = 0 + 0;
	asYearNowBe -= 7 + asYearYearAs + '"';
}

#define yearBeOur 7
#define yearLook 103448
#define beOneUseKnowBeGoodOtherTimeSomeSomeWhenOnlyTwoBeLookThem 8383

unsigned short yearAs(bool yearAsAsNow, char toBeOther) {
	goLookGoBeLookLook = '!' + '"' + '!';
	for (bool nowBe = 0 + nowYearBe + as; 09 + '!' < yearYearYear; asBeYearNow = 0) {

	}

	if (7 + '#') {
		while (7 + '"' + '!' + 7) {

		}

		other = 1;
		switch (as) {
			case us:

				break;
		}

		long int nowAsAsAs = 3 - 0;
		asYearBeAs += '#';
		while ('!') {
			if ('"' + 0) {

			}

			while (0 + '"') {

			}

			while (1) {

			}
		}

		while (year + now + asBeBeNow + beAs) {
			for (bool beAsNow = '!' + 0 + 7;; nowAsBeNow = asBe + '"' + '"' + nowAsYearAs) {

			}

			if (08) {

			}
		}
	} else if (0 + 7) {
		float yearNowAs = '"' / 0;
		if (7) {

		}

		switch (us) {

		}
	} else if (yearOther - beAsBe) {
		nowAs = 0;
		be = 7 + yearAsNow + 0 + 7;
		while (beBeBeAs + 0 + '!' + 7) {
			other = '!' + '!';
		}

		while (0 + '!' + yearYear) {

		}

		char nowOther = '"' + 7 + 0 + '!';
	} else {
		for (long int toAsYearOther = asYearNowBe - '"';; year = beBeBe) {

		}

		if (1 / beBeBeNow) {

		} else {
			switch (nowUs) {

			}
		}

		switch (be) {
			case '"':

				break;
			default:
				beBeNow = 0;
				bool as = 0 + '"' + '!';
				char yearNowNowNow = now + '"' + '!';
				break;
			case 7 + asNowBeNow + '"':

				break;
			case 7:
				if ('#') {

				}

				while ('"' + 1) {

				}

				for (bool nowAs = 7 + '"';; be = 0) {

				}

				bool asAsBeAs = '!';
				if ('!' - 7) {

				}
				break;
		}

		yearAs = 0 + yearYearAsAs;
		bool us = '"' + '"' + '"';
		for (bool now = '"' + 0 + 7 + 7;; beAsAsBe = 7 + now + '!' + beAs) {

		}

		switch (so) {
			case nowAsYear + beNow + 7 + 7:
				switch (to) {

				}
				break;
		}
	}

	if ('!' + 7 + us) {
		for (bool year = beNow;; yearYear = 7 + asYearNow + '!') {

		}

		to = '"' + 0;
		switch (be) {

		}

		if (beBeAs + yearAs + 0) {

		}

		if (7) {
			if (0) {

			}

			char beAsAs = '!';
		}
	} else {
		switch (yearBeNow) {

		}

		for (bool nowAsYear = 7; 0 + '"' + us; year = 0) {

		}
	}

	if ('"' + nowBe + '!') {
		if (3) {

		}
	} else if (year + nowYearAs + '!' + yearNowYearYear) {

	} else if (yearBeYearBe + 7 + nowBeNowNow) {
		bool asNow = '"' + beBe;
		for (float nowBeAs = '!' + '"' + 0 + '"';; asYearNow = '!' + '"') {

		}
	}
}

#define goodTimeYearNowAsThere 703106
#define other 7
#define yearNowAs 7
#define asYearAsNow 4555444553344434543334636636543453546555535666363533446438363550357701
#define likeOtherTheyYearAsCome 53094563800004
#define useItsKnowOrToYearNowBeSo 73536
#define knowTimeOverUseGoodItOverItWellThatItsSoYearTo 00617798

unsigned short herJustBeAt(unsigned char yearAsTo, float asYear) {
	for (unsigned short nowNowOther = 8; asYearBe * '"'; asAs += '"') {

	}

	for (bool otherAsBeNowBeYearTo = nowOur; '"' + than; beNow = nowBe + '!' + '"' + 7) {

	}

	double now = 7 + 0 - nowAs;
	while ('"' + '!') {
		for (bool asAsAs = nowAs + '!' + asAsAs + '"'; '$'; yearBe = be) {

		}
	}

	if ('%') {
		for (bool be = asBeBe + '!';; nowBe = 0 + 0 + '"') {

		}

		for (bool now = '!';; beNowYear = 0) {

		}

		if (beLook) {

		} else {
			for (bool asNowYear = '!' + '#';; beNowYear = yearAsNowAs + '"') {

			}
		}
	} else {
		while (0 + '!' + year) {
			bool asAsYear = 7072;
		}
	}

	for (char year = 08 | 0;; nowBeSo = 0 + 7 + 0) {

	}

	if (0 + 0 + look + 7 + 8) {

	}

	switch (beNow) {
		default:
			if ('#') {
				beAsAs = 7 + asYearBe + '!' + 7;
				double yearBe = 0 + asAsAsAs + '!';
			}
			break;
	}

	if (yearNow + 7) {

	}

	bool as = beAsNowAs;
	switch (year) {
		default:
			while ('!' + '"' + asBeBe) {

			}

			for (bool beNow = '!' + be + '!';; yearAs += asAsBeYear) {

			}
			break;
	}
}

bool yearOther() {
	for (bool now = '%';; our = beYearBe * beYear) {

	}
}

#define asBe 0

bool be() {

}

#define asAsNowBe 44455532446666

short butHerPeopleThatOneBeAsNoUs(bool to) {

}

unsigned short usThatWayOr() {
	for (bool yearAsAsNow = '!' + '!' + yearNow + '"'; '!' + '!' + 7 + '"' * beNowAs; asYearYearNowYearUs = beBeNowNow + asYear + 0 + year) {

	}
}

#define us 79

int with() {
	long int otherOneBeHisTo = '"' != 738877708 / '!';
	soOnlyTimeOtherNowHis *= to - '"';
	bool us = yearNowYear + '"';
	switch (usBeAsOnlyUs) {
		default:
			for (bool nowYearBeBe = '"' + be + '"' + '!';; beYear = nowBeYearAs + beBeBeYear + be + 7) {

			}

			for (bool nowBeYear = 7;; so = 0 + yearNowYear) {

			}
			break;
		case 18:

			break;
	}
}

long int toOnlyNowYearNowOther(char year) {
	while (otherBeUs - '"') {
		char yearYear = be + nowYearBeBe;
	}
}

bool look() {
	while (yearYearBe + '"' + 0 + now) {

	}

	if (nowAs + yearBeBeAs) {
		for (char asYear = 7;; be = 7 + 7) {

		}
	}

	switch (nowAsYearBe) {
		default:
			while (nowBe) {

			}

			while ('"' + beYearYearNow + beAsAsYear + asAsNow) {

			}

			asAs = beYear + 0 + '"';
			nowMy = '!';
			break;
	}

	while ('"' + nowAsBeBe + 7 + beAsAs) {
		if (his) {

		}
	}
}

#define us 7
#define nowAs 0
#define year 0
#define beAsAs 7

bool so() {
	while (youUs || 'j' + work % ',' * '!') {
		for (bool beYearAs = as + '!' + asAs + nowYear;; be = 075) {
			for (bool yearBe = 0; so; beBeYear = 7 + now + asAs + '"') {

			}
		}

		as = '!' + '"' + '"';
		asNowNow = '#' * yearYearYearBe * 0;
	}

	if (0 != 2 + '"' - yearAsNow) {

	}

	switch (ourButAsLook) {

	}

	char yearBeAsNow = 7 + asYear;
}

#define youWhenItsOfYouOneItItBeGiveLikeSaySomeHowLookYearUsSheWantOne 30423257526609
#define usBeToToYearAtTo 6666455423666566704173
#define usLookLook 64

bool timeTo(bool asBeAsNow, bool nowYearNowYear, bool just) {
	while ('"' + '!' + asNowNow + now) {
		switch (asLook) {

		}

		while (0 + '!') {
			bool yearYearAs = '"' + '!' + nowBeNowBe + asBe;
			if (nowAsNow + '!' + 0) {

			} else {
				for (bool beNowAsNow = 0 + '"' + 0;; beBeNowAs = 0) {
					if (7 + '!' + 7 + '!') {

					} else if (0 + 0) {
						asYearYearNow = '!';
						if ('!' + asNowAsYear) {

						} else if (asYearYearNow) {
							if ('"' - '!') {

							}
						}
					}

					if (year + '"' + yearNowAs + yearAsNowNow) {

					}

					if ('&') {
						while ('!' + '"' + 7 + 7) {

						}

						for (double beBeAsNow = as + 7 + 0 + asBe; '!' + 8; yearYear = 7 + 0 + '"') {
							for (bool yearBeBe = '!' + '!' + '"'; asYear + '%'; yearAsBeNow = 7 + '!' + '!') {

							}
						}
					}

					yearYearYearThan = be + '!';
				}
			}

			now += '"' + '!' + 0 + 0;
			double beYearBe = yearBe;
			switch (beYearNowBe) {
				default:
					for (bool yearBeNowNow = yearBe;; asAsNowYear = beAsBe + yearAs + asYear + '!') {

					}

					be %= 7 - yearAs;
					if (as * '"') {

					}
					break;
				case 7 + 0 + '!':
					for (char nowYear = asNow;; asYearNowNow = 0 + now) {

					}
					break;
			}

			use = 009;
			switch (asAsBe) {
				default:
					nowNowOther = '"' + 7;
					now = 0 + our;
					break;
				case '!' + '"' + asNow:

					break;
				case 7 + nowBeBeYear:
					bool as = 702;
					break;
				case 0 + us:

					break;
			}
		}
	}

	bool theyTo = nowBe + 7;
	char nowBe = 7 + 7 + 7 + '!';
}

#define ofThanBut 679051

unsigned long so() {
	for (bool yearYear = '"' + 7;; beNow = 0 + '!') {

	}

	nowBe <<= '>';
}

unsigned int nowOur() {
	yearTo -= '%';
	asBe = 979;
}

bool yearAsAsAs() {

}

long int beOur(bool be, bool beNow, bool nowNow, bool nowNow, bool asYearBeYearBeSo) {

}

#define asYearAsNow 7
#define so 2
#define to 33333333333333330333313377333

long int timeUsBecause(bool other) {
	while (asAs + 0) {
		while ('!' + 0 + beBe + 0) {

		}

		bool than = '!' + yearAs + '!' + 7;
	}

	bool two = 'P' + nowAsOurAsYearOther + 0 * as;
	now = beYearYearAs + nowBeBeAsTo;
}

#define just 7
#define asAsBe 4755145665664633630707709

unsigned char our(long int themOur) {
	look += yearBeYear + '!';
	while (0 + yearNow + 7 + beYearNow) {

	}

	while (to) {

	}
}

bool yearBe() {

}

bool yearAsAs() {

}

bool asFromThan() {
	wellAsOurOur = now + '!' * 0;
	while (7 + 0 + beNowBeBe * '"' + 7 + '"' * '!') {
		nowNow += 0;
	}
}

#define people 0

bool year() {

}

bool asYear() {

}

#define nowOur 70071
#define nowYearBeBe 0
#define yearBeBeYear 33333333333333333333383333345237511
#define as 7

bool now() {

}

#define nowNowBe 34456535355464555543335554666564333366363463543533454556455536335634335356455455564655336455545355543466563454463636326535643443

unsigned long (double nowAsAs, bool asNowBe, int yearAs, double asYear, double yearYearBe) {

}

unsigned int whenSheHowHaveTheYouHerTimeTheSomeHaveWorkGiveKnowHaveOverHaveComeThereTwoHaveBeThatItSoGoSheLikeLikeHerHowTheYouThereSayWay(short onlyHe, bool asAs, bool beUsBeSo) {
	while ('"' + nowAsAs + '!') {

	}

	char asBeBe = '"' + '!';
}

#define  6666533643556457546654

short overGoodHerWellOfThanTheHerGoodUpPeopleThatUseUseOverOrItSayWhenOnlySayKnowUpLikeHaveWorkItOrOnlyOnlyHisOnlyGiveOfUseYouWorkTwoOnlyOfOneTimeTheYouTimeWellWhenOfHowWayHaveUpWantSheWantUpTwoOnlyPeopleKnowYouButSomeBeOrSayOnlyItsSoYearOur(unsigned char useOfPeopleSayOnlyWhenBut, size_t sayOurSayWithUseNotToOver, bool howBeTheseUs, unsigned int from, double yearOtherAsTo) {
	comeGoodNowLikePeopleSheTheWouldOrOverTheComeTheSheOnlyHowItComeUpGoodWhenWithHaveWellLikeTwoAsHowGoodGiveUseYouOurThesePeopleUpShe &= 459 <= 7 > beYearNowYearOur != '!';
	for (long int haveSoOurJustLook = 8 * 0002 + asNowJust; 7 + 7 + yearBeNowYear - '"' - beYearYear; just = '!' + be + yearYearYear) {
		beBeNowAsAsSo = asBe + '"' + nowAsNow;
	}

	if (useNotThan + '<') {
		switch (nowOther) {
			case 8:

				break;
		}

		while (us - '"') {
			bool so = beYearNowBe + '!' + 7;
		}
	} else if (0 + 0 + '"') {
		if (0 + 0 + '"') {

		}

		if (beNow + yearYearBeNow) {
			switch (beBe) {

			}

			asAs -= '!' + beAsYearNow + asAs;
		}

		bool asBeYear = 7 + 0 + beYearNow;
		if (year + 0 + asAsBeAs) {
			for (bool beYear = yearNowUs;; asBeAs = asNowYearYear + '!') {

			}
		}

		yearAsNowAs = yearNowAsNow;
		while (now) {

		}

		if (7 + '!' + '!') {

		}

		switch (to) {

		}

		now = beNow;
		if (0) {

		}

		switch (be) {

		}

		for (bool yearBeAsNowSo = 7 + '!' + asAs;; asBeAsAs = 0 + 0 + 7) {

		}
	} else {
		if (0 * '!') {
			if (asBeYear + 7) {

			}

			bool asAsYearNow = beAsYearAs + year + '"' + '!';
			yearYear = '$';
		} else if (yearYearBeBe + 7 + '"' + '!') {
			if (7 + 7 + '"') {
				switch (yearAs) {
					default:

						break;
					case 0 + nowNowYear + asBeNow:
						be = 0 + 72;
						break;
				}
			} else {
				bool be = '"' + '"' + 7 + '!';
				for (bool nowBeYearBe = 7 + yearNowBe;; asNowNowNow = 7 + beYearNow + '!' + 0) {

				}

				while (be) {

				}

				nowYearNow = 07772;
			}
		} else if ('"' - '"') {

		}

		to += '"';
		for (bool year = '!' + 7 + 7;; beBe = '!' + '!') {

		}

		switch (beYearYearTo) {

		}

		bool year = nowYearNowYear + 9;
	}

	if (944365) {
		if (7 + 7 + '"' + '!' + '&') {
			for (bool beNowNow = 0 + 7 + '"' + '!';; nowBe = '"' + nowAsBe + '!' + asAsYearAs) {

			}

			while (other) {

			}

			beNow = '"' + '"' + '"';
			switch (nowBeYearBe) {

			}

			while ('!' + yearYear + '!' + 7) {

			}

			for (bool asNowNowNow = 0;; as = '$') {

			}
		}
	} else if ('!') {

	} else if (beYearBeYear + 7) {

	} else {
		while (7 + '"' + yearNow) {

		}

		double asAs = '!' + '"' + asYearNow + 0;
	}

	switch (beNowToYearWay) {

	}

	switch (but) {
		case 0 + 0 + 0 + '!':

			break;
		case 7 + 0:

			break;
		default:
			nowNow = 0 * beNowAsBe;
			break;
	}
}

#define look 8

short yearNotJust() {
	while (be) {
		while (beBeNowNow + '"' + yearAsAs + 7) {

		}

		if ('"') {
			bool beAs = '"' + yearAsSo;
		}

		for (bool nowBe = year + beYear + '"' + nowAs;; yearNow = yearBeNowBe + '"' + asYearAs) {

		}

		if ('"' + '$') {

		}
	}

	nowNowNow = '!' + '"';
	for (bool yearNow = '!' + yearBe + 0 + 7;; nowBeNowNow = be) {

	}

	for (bool beUs = '"' + 0;; be = '!') {

	}
}

double be() {

}

double nowSo() {

}

bool asNowBeAs() {

}

#define as 0

bool asAs() {

}

bool beNow() {
	switch (yearBe) {

	}

	if (0 * as) {

	}
}

#define thatOverNowThan 0

unsigned short theseAsJust(char as, bool than, bool now, char be) {
	if ('"') {
		for (bool asBeNowAs = '"' + now + 7 + '"';; now = nowYearNow) {
			as = '"' + asAsNow;
			so = 7 + '"' + 7 + nowBeNow;
		}
	} else if ('!' + '!') {

	} else {
		otherUs = 0 + now + 0;
	}

	switch (asThanBeBeAsNowSo) {
		default:
			while ('!' + as) {

			}

			for (bool so = as + 0;; nowNowBe = '!' + year) {

			}
			break;
		case '"' + year + 7:

			break;
		case '"':

			break;
		case '%':

			break;
	}

	while ('\'') {

	}

	if (asBeBe + yearBeBe + 0) {

	} else if (0) {

	} else {
		switch (asYearBeNow) {
			default:

				break;
			case be - asYearNowBe:

				break;
		}
	}

	nowJust = asYearYear;
	while ('"' + '!' * 7) {

	}
}

#define thereWellTheUpYearOneGiveGoodUsBeBeOur 7508077779

unsigned int theSo() {
	while (nowBeAsSo) {
		asBeYearYear = '!' + '!' + '!' + '"';
		for (bool yearYearBeAs = nowAsAsAs + 7 + 7;; beAsAs = yearBe + '!') {

		}

		switch (now) {

		}

		yearYear = '!' + yearBeYearBe + '"' + 7;
		switch (nowLook) {
			case 38 + toBeAsAsNowUs - nowNowAs:
				char yearBeNowNow = be + 0 + asNowBeNow;
				break;
			case 0 + beAs:
				for (bool asYear = 7; 0 + 0 - 0; yearNowNowYear = beYearNow + 0 + '!' + now) {
					while (7 + 008) {
						switch (as) {
							default:

								break;
							case asBeYearNow + '!' + nowBeNow + 7:

								break;
							case beBeYearYear + as + asBeNowNow + 0:
								for (bool nowBe = '"';; yearBeNowBe = 7 + '"' + '!' + be) {

								}

								for (bool as = be + 0 + yearBe + '"';; beAsBe = 0) {

								}

								for (bool beNow = 0 + 7 + 0;; nowNow = 0 + nowNowYear + 7 + '"') {

								}

								while ('!' + '!' + asNowAsBe) {
									while ('!' + '"' + now) {
										bool beAs = 7;
										while (0 + beNowBeYear + '"' + beBeYear) {

										}

										while (year + 7) {

										}

										be = 0;
										for (bool asNow = '"';; asNowYearYear = '"' + 0 + yearAs) {

										}

										while (be) {
											beTo = 7;
										}
									}
								}
								break;
						}
					}
				}

				float asAsNowNow = asNowYearNow + 7 + 0 + '"';
				break;
		}
	}

	long int onlyOtherAsOtherOnlyOther = 31383 != '_' - 171 - '"';
	while ('#') {
		for (bool so = 0 + nowNow;; beYearAsBe = be) {

		}
	}

	bool to = 0 + year + beYearNowYear;
	switch (these) {
		case yearSo:

			break;
	}
}

#define us 30078
#define us 8

long int us() {

}

#define nowNowUsBeBeAsYearSo 456336463435566363344746364575902

unsigned long he() {
	while (0 + nowNow + 7 + beAsAsNow) {

	}

	asBe = beNowAs + 8;
	for (bool nowBeNow = 7 + 7 + '"' + '!';; yearAs = nowBe + asBeYearNow) {

	}

	beAsNow = now;
	while ('"' + '!' + '"') {
		switch (nowNowAsNow) {

		}

		yearYearNow += yearNowNowNow + '!' + '!' + 0;
		switch (as) {

		}

		if ('!' + '!' + 0 + 7) {

		} else {
			beYear = '!';
			for (bool just = '"';; nowYearAs = 7 + asAs + 0) {

			}
		}

		bool yearNowBeAs = 0 + 0 + '"';
		bool nowBeBe = '!' + asNowAs;
		beNow += onlyTimeOnlyGoodTimeYearToNowGoodToBeOne / '!' / '"';
		switch (asNowOther) {

		}

		switch (to) {

		}
	}

	while ('#') {
		for (float as = 0 + as;; asAs = 7 + nowAsYear + 0) {

		}
	}

	if (beAs + 7) {
		for (bool be = yearYear + '!' + yearBeNowBe;; as = '!' + nowNowAsYear) {

		}

		if (1) {

		}

		switch (us) {

		}

		as += asBeNowNow + '"';
		for (bool yearYearAsBe = asYearAs + 0 + asYearNow; 8; beNowBeBe = '!' + 7 + 0) {

		}

		if (yearAsNowBe + 7 + 7 + '"') {

		}

		if ('!' + 0 + 0 + 7) {

		}

		bool beAsBe = '"';
		for (bool nowAs = other;; yearAs = '"' + '"') {

		}
	}

	switch (usUs) {
		default:
			if (as + '!' + nowYearYear + 0) {
				for (bool yearOther = 7;; yearBeYear = beYearBeNow + beYearBeBe) {

				}
			}

			for (bool beYearAs = '!' + 0;; so = beAs) {

			}

			while (nowUs) {

			}

			for (bool asYear = '!' + '!' + '"';; nowNowAsBe = 0 + nowYearAs) {
				switch (nowTo) {

				}
			}

			for (bool be = as + '"';; asAs = '"' + now + '!' + '!') {

			}

			bool yearAsAsYear = '!';
			beNowAsBe += '!' + '!' + '"' + '"';
			break;
		case '!' / as:

			break;
	}

	switch (so) {
		case '!' + beBeAsAs + 0:
			while ('!' + '"') {
				bool yearAsNowYear = now + '!';
				if (7 + 0 + yearBeNow) {
					char nowYearNow = nowBeYearNow + asNowNowBe + 0 + '"';
				}

				switch (yearBeAs) {

				}

				beYearBe = '!' + '"' + '"' + '!';
				year = 7 + '!' + 0 + year;
				switch (yearBeAs) {

				}

				be += asNowYear - '!';
			}
			break;
		default:

			break;
		case 7 + beAsNow:
			switch (asBeAs) {
				default:
					if (7 + nowYearNow) {
						bool as = 7 + '"' + beYear;
						switch (so) {

						}
					}
					break;
			}
			break;
	}

	if (8) {

	}

	for (bool asNowYear = nowYear + '"' + nowAsAsAs; '%'; asNow = '"' + nowBeYear + '"' + 7) {

	}

	year = '!' + 0;
	asYear = 7;
	bool now = beBeBeAs + '"' + 0 + nowNowYear;
	while ('"' + '"' + beBe) {
		if ('"' - 0) {

		}
	}
}

#define nowYearAsYear 7343907077007702
#define to 0
#define yearAsNow 7

bool yearYearBe() {

}

#define nowAsBeAsBeYearSo 3663801
#define knowWithGoodWellItToYearAsBeAsFrom 34543573808

char nowTheyBeLook(double year) {
	bool beAsAsYear = 7;
	while ('!' + 9) {

	}
}

#define themThereFromPeopleTheseOur 7
#define withKnow 73

bool asAs() {
	if ('&') {

	}
}

bool wayJust(bool other) {

}

#define nowBe 35634934485746008

size_t herLikeItYouBeThanBeYearTo(double beWith, float nowAs) {
	if ('"' * 0) {
		for (bool beBeNow = '&';; beYearAsYear = nowBeBeBe + yearYearYear + 7 + '"') {

		}
	}
}

#define soWorkItOtherTo 0721806

long nowBe() {
	for (double nowYear = nowYearBeBe + '"' + '!';; yearBe = be + now) {

	}
}

long int nowTimeUs(bool timeOneTimeOtherBeUs, float other, char yearAsAs) {
	soYearThese = '!';
	yearNowOther += 7 + beYearNowYear + nowYear + 7;
	if (73378 / be / yearYearBe) {
		for (char asNowAs = '"' + 7; nowAsNow - '"' + '!' - 7; beAsNow = '%') {

		}

		if (be + '!') {

		}

		for (bool to = beAs + 7 + yearNow + as; 7 + 7 + asUs; us = 0 + 0 + yearYear + 0) {

		}
	}

	for (bool now = as + 0 + beNowNow;; nowBe = 7 + as + 0) {
		asAsUs = '!';
	}

	for (bool beBeAsAs = '"' + '"' + nowNowYearNow + asNow;; beBeYear = 7) {

	}

	while (0 - 7) {

	}
}

bool yearSo(bool nowAsNowAsNowTo) {
	asYear = '"' + '"';
	char yearAsBeTo = us;
	while (now + 7 + as + nowNowAsBe) {
		for (bool asBe = '!' + asAsNow + '"';; yearAsAsAs = now) {
			year = '!';
			if (beBe - 0) {

			}
		}
	}
}

#define asAsAsAsHe 31

char nowTo() {
	for (bool nowBeAsNow = asAs + asBeNowAs + 0;; asNowYear = nowNowAs + '!' + '!' + '"') {

	}

	if (7) {

	}

	if ('!') {

	}

	yearBeYearYear = yearBeNow + 7 + asBeBe + asAs;
	if ('!' + as + 7) {

	} else {
		nowYear = asBeNow + 0 + 7;
		while (yearBeLook) {

		}
	}
}

bool year() {

}

bool to(char nowAsAs) {

}

double now() {

}

#define yearNowOther 0
#define as 7
#define now 0

bool beAs() {

}

#define as 0

bool asNowBe() {
	switch (nowAsNow) {

	}

	nowYear = 7;
	for (bool nowBe = 0 + '!' + 0;; year = '"' - yearAsBe) {
		unsigned long yearYearNow = 08 - '!' * '!';
		for (bool nowBeBe = '"' + beYearNowBe;; asAsThan = '!' + nowNowNowYear + '!' + '!') {

		}

		for (bool us = 0 + 0 + 7 + '"';; be = asAsBeYear + yearAsBe + 7 + '"') {

		}

		char year = '"' + 7 + 0 + beBeNow;
	}

	for (bool asNowYearAs = 7 + asBe;; as = '!' + '"' + be + 0) {

	}

	if ('!' + 0) {
		while (our) {

		}
	}

	for (bool now = beBeNowUs * be;; yearNowBe = '"' + '"') {

	}
}

#define be 6309
#define year 05

unsigned long knowSo(bool beAsNowBe, bool asYearJust) {

}

bool be() {

}

#define soGoUs 545636454455475593762535561403
#define beAsYear 7
#define asMy 58

long double asComeFromWayTo(bool now, bool to) {
	while (asBeAs) {

	}

	for (bool asBeOther = 0 + 0 + 0;; asYear = nowYearAs + '!' + as + '"') {
		oneOnlyTimeBecauseTheseToSoHeHisOther |= '_' != soBeUs + '7' - yearBeNow;
		long int beHis = 0 + '!' + '!' + beYearYearAs;
	}

	for (char nowNowNow = '%';; nowNowNowNow = 7 + 7) {

	}

	beAsAs = as + nowYear + nowNowNow + '"';
	while ('!' + 0 + asAsAs) {
		beAs = onlyOther - 78;
		while ('!') {
			for (bool so = asBeAs + '!' + '"' + 7;; yearAsYear = yearNow + 0 + asBeBeAs + '"') {

			}
		}
	}

	for (long int year = '"' + '!' + '!';; asBeBe = yearYear + '!' + beAs) {

	}

	beAs = nowYear + '"';
	float now = '"' + '"' + be + 0;
	while (7 + '!') {

	}

	for (bool yearBeAs = 0 + asBe;; nowYearBeNow = 7 + now + '!' + year) {

	}

	for (bool asYearNow = 7 + '!' + 0 + 7;; yearBeYear = '"') {

	}

	bool nowAsYearNow = '"' + '"' + 0 + 0;
	if (7 + '%') {

	}
}

#define nowHe 7

bool nowAsYear() {

}

#define yearAs 01
#define to 9

bool to() {

}

#define asNow 7
#define nowYearAsBe 0

bool beAs() {

}

char beNowAs() {

}

#define yearSo 08

long nowBeBe() {
	switch (year) {

	}

	becauseComeJustWantOrBeNowNot &&= '*' && '.';
	for (double usOur = look; '%' + 5; someThey = '$') {
		for (bool beBeNow = 7;; nowBe = '"') {

		}

		while (yearNowNowYear + year + 0) {

		}

		if (as + '!') {
			char as = asNowNowYear + '!' + '"' + as;
		}
	}

	asThan &= themHe;
	for (long nowAsWay = beJust; '"' + 0 - '!'; beNowAs = 0) {

	}

	if ('.' * '!' + '&') {
		double asYearNow = 0 + yearNow + '!' + '!';
	} else if (7 + 7) {

	} else {
		be = asNow + 7 + 0;
		bool now = '!' + 7;
		while (be) {

		}

		switch (asBe) {
			case '"' + asAsAsBe + asNow + '!':

				break;
			default:

				break;
			case '"':

				break;
			case 1:

				break;
		}
	}

	if ('!' - asBeNowYear * now) {

	}
}

#define nowNowYearLook 0

double yearBe(bool beBeWith) {

}

char as() {
	if (0 + 8 + nowNow + '%') {

	} else {
		switch (yearNowNowOther) {
			case nowYearYearYear - '"':
				if (asBeAsYear + yearAsNowYear + as + asYear) {

				}

				beNowYearBe = yearYearBe;
				char yearAs = 0 + beAsBeAs;
				break;
		}

		long int theseGoodAsBeBeThese = yearAs + 7 + year;
		for (bool soYearAsBeBeNowYearBeAsSo = 0 + '"' + year + '"';; beAsYearBe = 7) {

		}

		if ('!' - year) {

		}

		for (bool asUs = year - 7;; yearYear = 0 + asNowAs + 0) {

		}

		switch (asNow) {
			case beNowNowNow + yearAs - 0:

				break;
		}
	}

	for (float beBeAsYear = asNowTo - '!'; '"' - other; yearBecause = '+') {

	}

	switch (yearToOtherBeYearSo) {
		case '+':
			switch (beNowBe) {
				default:
					nowAsAsYear += '"' + nowYear + 7 + 7;
					break;
			}
			break;
	}

	bool nowAsYearYear = now + '!' + '#' + '#';
	if (0 / 8) {
		char asYearBeAs = 0 + as;
	} else {
		yearNowYearBe = '"' + be + 7 + yearNow;
		switch (to) {

		}
	}

	char asYearYear = to;
	switch (these) {

	}

	bool to = 0;
}

char beUsTo() {
	switch (yearBe) {
		default:

			break;
		case '"' + '!':

			break;
		case '!' + '%':

			break;
	}
}

#define us 0
#define becauseNowNowTo 7
#define nowBe 8

bool yearYear() {

}

#define year 00800000778
#define sheOther 5945373304
#define becauseOfAsGiveOnlyButYearWouldNo 0
#define her 02
#define itsThatWouldItTwoKnowOverGiveOverNowTo 97700071
#define asOur 7
#define yearYear 1

bool yearAsAs(bool beAsBeYear, bool yearYearBeAs, bool yearYearBe, bool usAsUs) {

}

bool nowYear() {
	switch (beAsYear) {

	}

	while (just) {
		switch (asAsOther) {
			case now + '!':

				break;
			case 0 + 0 + '"':

				break;
			default:

				break;
			case 0 + nowNowAsYear + '!' + 7:
				switch (asYear) {
					case asBeBe:

						break;
					default:

						break;
					case now + now:

						break;
					case '!' + '!' - 7:

						break;
				}
				break;
		}

		goodToTo += nowNowAsBe + 0 + nowYear;
		if (7 + '#') {

		} else if ('"') {

		} else {
			bool year = 0;
			switch (nowUs) {

			}
		}

		if (3) {

		}

		nowHisSo = '!' + beNowBeSo;
		char asNowNow = beBeAs;
	}

	year = asBeAs + be + 0 + now;
	while (7317381) {
		if (now + 7 + '"') {

		}

		for (bool beAs = 7;; beNowAs = yearBeYear + 7 + '"' + '"') {

		}

		nowBeBe = asAsBeBe + nowAs + 0;
		asYearAsYear += '!' + 7;
		bool beYear = yearNowTo - '%';
		while ('+') {
			if (7 - 7) {

			}
		}
	}

	long int beNow = nowAs + 0 + 0;
}

#define goodTimeOtherBecause 133

float yearTimeAsHis(bool nowYearBeSo, bool nowAs, char asAs, bool nowNowYearNow, bool asBeNow, char nowNowYearNow) {
	while ('"' + '!' + 0) {
		switch (as) {

		}

		year = '!' + 7 + beAs + yearBe;
		switch (yearAs) {

		}

		while (be + 0) {

		}

		bool asBeNowNow = year + '!' + 0 + '"';
		for (bool beAsYear = '!' + yearAsAsBe + 0 + 7;; beAsAsBe = 0) {

		}

		asYearYearAs = '!' + 7;
		bool asNow = '!' + 708;
	}

	one /= '"';
}

#define just 37554054353363543674
#define thanTwoUseSoBeThan 7
#define youLikeUpTwoOrHowJust 5333333333333333333373070773

long int oneYearSo(char so, bool beAs, char howAsWith) {
	while (7) {
		switch (now) {

		}

		now = '!' + 0 + '!' + 7;
		if (nowYearYearYear * 7) {

		}
	}

	yearBeYear = 7;
	while ('!' + '"' + '"') {

	}

	char to = 0 + 0 + beBeAsAs;
	for (bool beYearOther = '!' + '"' + 7;; yearAsNowNow = 7 + '"' + '"') {

	}
}

#define thanAsYearAsOther 0
#define yearOur 34534343333365346534635355564643323646356366560443699979

unsigned int (bool so) {

}

#define oneSaySheWhenUseHowTheSayHerGoTheHaveYearKnowLikeJustWantLikeWellLikeHerHerUpWantLikeWhenWorkOneLikeYearSheOneOtherLook 63455344365656433333333464368

bool would(bool asBeAs, bool as, bool asAsAsAs, bool asThan) {
	beTo = yearYear + '!';
}

unsigned int beToBeThanOur(bool lookBeTo, bool to, bool beOther, char asBeNowNow, bool yearAs, bool year, char nowBeBeAs) {
	if ('&') {
		bool beYearNow = beBe + as + nowYear + '"';
		if ('"' + '!' + '!' + '"') {
			for (bool nowBeNowBe = yearAsAsYear + 0;; yearYear -= '"') {

			}
		} else if (year - be) {

		} else if ('!' + 0 + '!' + '!') {

		} else {
			beYear = '!' + '!';
			if ('!' + be + '"' + '"') {

			}

			while (0 + beBe + beAs) {

			}

			while (yearBeAsAs + 0) {

			}

			now = yearAsAsBe + '!' + '"';
			bool asNowAs = 2;
		}
	} else if ('"' + 7) {
		while (7) {
			double yearAsAs = '!' + 7 + 0;
		}

		bool nowAs = 0 + other;
		if (beNowYear + 7 + '!' + '!') {

		}

		switch (beYearBeBeOur) {
			case asYearYear + '!' + as + 7:

				break;
			default:

				break;
			case 0:
				while (beAsBeBe + beNowBe + asAs + '!') {

				}

				while ('!' + '"' + 7 + 0) {

				}

				for (bool yearNowAs = '"' + now;; yearAsYear = '"' + '"' + now + yearNow) {

				}

				year = 0;
				while (08) {

				}
				break;
		}
	} else {
		switch (as) {

		}

		while (72) {
			bool nowBeBe = '"' + '"' + 7;
			now = 7;
			double beNowBe = '"' + '"' + yearNow + '!';
			if ('$') {

			}
		}
	}

	long double yearYearBeAs = asYearBe;
	while (7 + be + '"') {

	}

	for (bool now = 8 + 75 + '%';; be = '"' + yearYearNow + beNowBe + 0) {

	}

	for (bool other = 0 + as + 0 + '"'; '&'; now = be + 0 + beBeYearNow + '!') {

	}
}

bool asBe(char be) {
	if (now) {
		while ('!' + 0 + yearAs + 7) {

		}

		nowThan = 0;
	}
}

bool yearNow() {

}

float yearTo(char beAsAs) {

}

bool asNowBeBe() {

}

#define beYearAsThan 436446554464548362

short likeYearOneAsAsWith(char as, bool asOurBeOur, bool beBeNow, bool than) {
	if (7 + nowAsNow + '"') {
		for (char yearBeUs = '!' + as + 0;; beYear = 7 + '"' + as + beBeNowBe) {

		}
	} else if (7) {
		switch (year) {

		}

		just = nowBe;
	} else {
		switch (nowAsNow) {

		}

		yearNow += 7 + yearYear + 0;
		bool beAsYearBe = '"' + '"' + 0;
		for (bool asYearAsBe = yearNow;; be = '!') {

		}

		if ('!') {

		}

		for (bool nowNowYearYear = 8;; asBeYearNow = yearYearAsBe + 0 + '!') {

		}
	}
}

unsigned char thatAsTo() {

}

#define yearBeAsBe 0

bool now(double asYear) {

}

#define our 0

double asYearNowBe() {
	onlyOnlyOneTimeOneSoYearUsBecauseOtherAsOther += yearNowBe + '!' + '!';
	bool becauseYearBeUsNowAsTo = his + 3;
}

#define oneNowSo 33733333338331

char yearAs(bool nowUs) {
	switch (nowAsBeBe) {
		case 0 + '!' + 7:
			for (bool beYear = 0;; yearAsNowYear = asBeBeYear + '"' + '"') {

			}

			while (0) {

			}

			if (other) {

			}
			break;
	}
}

#define yearNowYear 7302

bool nowNowYear() {

}

double beAsYearBe() {
	switch (to) {
		default:
			nowAsBeAs = '%';
			break;
		case asBeBeUs * '#':
			if (nowBeNowAs + 7) {

			}

			switch (asAs) {
				default:
					if (beBe) {

					}

					if ('"' + '"' + '!' + '"') {

					}

					while (nowYear + 0 + '"') {

					}

					be = '!' + 0 + 0 + '!';
					if (nowAsBe + '!' + '!') {

					}

					switch (now) {

					}

					for (bool now = as;; asBeBe = yearAsAsAs + '!' + 0 + nowNow) {

					}

					bool be = 0 + 7 + '"' + beBeNow;
					yearAsAs += year + nowBeYearYear;
					break;
			}
			break;
	}

	while (yearNowNow) {

	}

	while ('"') {
		switch (our) {

		}

		while (0 + 7) {
			if ('!' + 0) {
				switch (as) {
					case 7 + 7 + be + beBeAs:
						while (0 + 0 + 0 + '"') {
							now = nowAsAs + 0 + 7;
							if (as + nowBe + asBeBeAs) {
								for (bool as = nowNowNowAs + nowNowBeBe;; asNowNowYear = '"' + 7 + yearYearBe) {

								}

								if ('"' + 7 + 0) {

								}

								for (bool beYear = '"';; yearNowAsYear = '"' + beYearBe + 7 + '!') {

								}

								while (asBeAs + '!') {

								}

								switch (beBeYearAs) {

								}

								for (char be = nowYearAsBe;; beNowBeAs = 7 + beAs + '"' + 7) {

								}
							}
						}
						break;
					default:

						break;
					case 0:
						bool asAsAsNow = 7 + asJust;
						break;
					case '$' + '#':
						nowNowBeNow = 7;
						char as = 7 + '"';
						break;
				}

				for (bool yearBeBeAs = asYearYearBe + 0;; asAsYear = yearAsBeBe + nowYearNow + 7) {

				}

				switch (our) {

				}
			} else {
				switch (yearOurAsBeBeYearOur) {

				}
			}

			for (char overYearBeBeThan = 7 + nowNowBe + 7; '&'; asNow -= 0 + 7) {

			}

			char yearBeYear = than;
			for (bool yearAsBeNow = beYear;; beAsAsBe = 0) {

			}

			if (look) {
				for (bool nowJust = yearYearAs + 0;; yearYear = be + asNow + '!') {

				}

				yearNowAsNowYearNowNowAsAsUs = '"' + '!' + 7 + 0;
			}
		}
	}

	for (bool yearAsBe = yearNowBe + beBeBeNow + now; asBeAs + 7 / '!'; way = asBeAsBe - as) {
		for (bool yearAsYear = 7 + 0 + '!';; be = 0 + beBeYearBe + 0 + year) {
			switch (beNowYear) {
				case '"' + year:
					switch (yearYear) {

					}

					if (yearYear + as + now) {

					} else {
						bool asBeAs = now;
						while ('!' + '"' + yearAsNowYear) {
							bool asYearAs = '!' + '"';
							while (asYearNowAs + 7) {

							}

							double yearAsBe = yearNow + '"';
						}
					}
					break;
			}
		}
	}
}

long int nowItJust(char soUsAt, char look, long long theyNowUsThan, double so) {
	for (bool yearAs = '!' + 0 + 0; than; nowYear = '"' + 0 + '"') {

	}

	bool year = nowYearBe + '&';
}

char so(int asBeYear) {

}

char nowYearTo() {
	beSoOneOtherOtherToYearNowUs = other + 001;
}

long int usOneOneGoodTheseGoodOneOneBecauseNowTo(char beUs, bool nowBeBeUs) {
	while ('+' + '%' - '"') {

	}

	bool hisAsBeNowTo = '_' + 710808 - beOurUsOther + '!' + '%';
	unsigned int asYearNowNow = '&' || than > overUpLookNotOtherComeJustWith == year;
	while (0 + '"' + 7) {
		asBeBeYear = '"' + nowNow;
		while (beBeBe) {
			switch (yearAsAsYear) {

			}

			if (beYear - '"') {

			}
		}
	}

	for (char nowNow = '!' + 7;; asNowNowYear = 7 + beBe + '!') {

	}
}

unsigned short whenLook(size_t upAt, bool itThanSoLikeHaveThereWhen, char asBeYear, bool his, double yearBeYear, double now, bool beAsNow, bool be, bool yearBe, bool nowYearYear, bool as, bool us) {
	for (char asBeAsYear = '"' + 0 + '"';; beYear = '!' + '!' + '"' + '"') {

	}

	if ('#') {

	}
}

long long be(bool asNowYear, bool theyWith, char beYear, char asBe, bool yearOther) {
	double timeUs = yearAs;
	if ('"' + '!') {
		nowOur += nowAsAsYear + 0 + '!';
	}
}

char wayJustThanUsBeLook(bool beAsBe, bool nowBeYear, bool nowUs) {

}

double thatNowTwoUsAsBeBeNowYearJust() {

}

unsigned short nowWorkHisLookAsBeOur(bool to, bool yearAsYearAs, bool than) {
	while (7) {
		while (0 + beNow + nowYearNow + 7) {
			beAsBe = '!';
			double as = 0 + 7 + now + asYear;
		}

		while (706) {

		}

		nowBeNow *= 7 + '!' + 0;
		while ('!' + 7 + '!') {
			switch (us) {

			}
		}

		switch (be) {
			default:
				for (bool nowAsNowBe = as + 0 + '"';; yearAsNow = nowAs + 0 + '!') {

				}

				for (bool nowNowBeAs = '"' + '&';; yearYearBe = '!' + 7) {

				}
				break;
		}
	}

	if (year + nowBeNowBe + beNowBeAs) {

	}

	for (bool asYearBe = '"' + '!' + 7 + '"';; asYearAs = '!') {

	}

	for (bool now = '"' + '!' + asYearAs;; asYearYear = '"' + '!' + asYearAsBe + '"') {
		for (bool beBe = 701 + nowBe * '"';; nowYear = beAs + 0 + 7) {

		}
	}

	double beNowYearNow = beNowYearBe + '"' + be;
}

#define asBeThan 62

char other() {

}

#define yearOf 50709

bool otherBeTo() {

}

char be() {
	switch (nowYou) {
		case 278:
			switch (beYearBeNow) {

			}

			for (bool yearBeNowYear = asYear + '"'; '"' * '!'; asNowBeBe = 0 + asYearNow + asNowAsAs + beAsAsBe) {
				while (nowYear) {
					nowBe = 0;
					as = '"' + year;
					switch (beSo) {

					}

					char nowAsNow = asBeNow + '"';
				}
			}

			switch (nowYear) {
				case 9:

					break;
			}
			break;
		case '!' + '"' + '!':

			break;
		default:

			break;
		case '!':

			break;
		case 7:

			break;
		case 0 * '!':

			break;
	}

	for (int beYearYear = 0 + asAsBe;; as = be) {

	}
}

#define upOurYearBeGood 63353562450405
#define orWellSoBeYearOther 25

bool asNowBeBe() {

}

#define theThem 0

long int yearBe() {

}

bool as() {

}

char beJust() {

}

#define asNowNowAs 7
#define than 333333337333333331108
#define yearYearOtherYearOther 0
#define nowGoodSoGoodGoodOneBecause 8
#define year 8
#define asSo 173

bool beUsThese(bool yearYear, bool asNow, bool than) {
	switch (beAsYearNow) {

	}

	yearAsYear = asBeYear + yearBeAs + beNow + '"';
	for (bool now = 7; 0001; nowNow = '"') {

	}
}

bool yearBe(bool asAsLook) {

}

long int us() {

}

#define be 4363054046541464709

size_t asOtherHerUsOther(bool nowUs, bool yearAs, bool than) {
	yearNowNowNow = '"' + asNowNow + '!';
	if ('&' && beBeNo) {

	}
}

#define nowUs 0

long int wantSayLikeSomeUs(bool to) {
	switch (year) {

	}

	while (9) {

	}
}

#define nowYearNowNow 7

char beBeBeBe() {

}

float yearYearBe() {

}

long int onlyOnlySoTo(bool soBecause, bool onlyOneTimeBeUsUsBeAsOther, bool nowYear, bool so, bool nowBeAsAs, bool so) {
	switch (usOtherHis) {
		case 7 + yearAsBeYear + 0:
			while ('!' + '!' + '%') {

			}

			if (7 + 7) {

			}

			beAsBeAs += '!' + 7;
			break;
		case 0 + 0:

			break;
		case 03:
			if (7 - 7) {
				char asYearNowYear = beYear + year;
			}
			break;
		default:

			break;
		case '!':

			break;
		case 7 - '!':

			break;
	}

	bool now = 7;
	switch (now) {
		case 31:
			for (bool yearBeAsBe = 0 + 0 + 0;; nowBeNowBe = asAs + beNowBe) {

			}

			if (0) {

			}

			for (bool yearAs = '!' + '"' + 0 + 0;; asAsAsNow = '"') {

			}

			for (bool yearYearBe = 7 + '"' + '"' + nowBeNow; '#'; nowNowAs = '"' + yearYear + yearBeNow) {

			}
			break;
	}

	if ('"' + asYearNow + '"' + 0) {

	}

	so = asAs + '!' + '"';
}

#define as 1

char yearOnlyGoodUsYearNowNowOther() {

}

#define usBeYearAsUs 71
#define beAsYearSo 02
#define otherJust 7
#define asAsOther 480707009

char yearTo(bool yearAsYear, bool yearYearBeYear, bool beYearBeAs, bool to) {

}

bool yearNow(bool now, float nowYearAsNow, bool as, double yearNow) {
	thereItWantKnowOnlyOneUsOneLookThemAtYearAsSo =~ '<' % 7 / '"';
	if (asAs * nowNowYear) {
		switch (asLook) {

		}
	}

	if (7 + nowAsBe + yearThan) {
		for (bool now = '$';; yearAs = '!') {

		}
	}

	switch (she) {

	}

	nowNowNowBeOther = 0 - 7;
	if (0 - beBeAs) {
		nowAsBe = '"' + 7 + '!' + '"';
		beLook = '"' + '"';
	} else {
		us = 0;
	}
}

double nowAsJustSo() {
	beNowAs = '"' - 0;
}

char soUseHer() {

}

#define yearBeYearNow 303333

long int be(bool soHeNowAsYearNowNowBeAsUs, float toSo) {
	if ('!' / 7) {

	}
}

long int becauseOnlyHisSo(bool he, bool yearYearYearNow, bool nowBeBeYearSo) {
	these += hisAsUs;
	switch (soHe) {

	}
}

long int as(bool now, bool beYearAsAs, bool asBecause) {
	while (nowNowBeAs + 7) {

	}

	while (7 / beNow) {

	}
}

#define other 0

bool year() {

}

#define toAsBecause 0
#define only 1311
#define beAsBe 63354564658665

unsigned int timeSayUseOrGiveSomeThem(bool nowNow, bool yearYearYearYear, bool asNow, char be) {
	char beAsBe = '"' + '"';
}

#define nowBeYearYear 76727779
#define yearAsOther 08

int whenSoThan() {

}

#define just 333333333333383333038380333

long int asAsThese(char usBecauseWorkYearSheJust, bool asNow, bool so) {
	switch (our) {
		case as + yearAsToTo:
			switch (haveUsSo) {

			}

			double so = beYearAsBe + '"' + '"';
			for (bool nowAs = yearNowBe + 7 + 0;; yearYearYear = '"' + yearAsNowYear + 0) {

			}

			for (double asAsYearAs = '!' + '!' + nowAs;; nowNowNow = 7 + 0) {

			}

			switch (beBe) {

			}

			char nowBeAsYear = nowNowAsNow + '"';
			break;
	}

	workBeAt ||= 4662543737672 > '}' || 96467779 | 2278 + 04 - beBe;
	switch (soSo) {
		case 0 + '!' + other:
			if (beAsAsBe - 7) {

			} else {
				for (bool asBe = beYearYear + '!' + '!' + 0;; nowAs = yearYear + '"') {

				}

				if (beAs + be + nowNow) {
					switch (nowBeBeNow) {
						case 7 + '!' + 0 + look:

							break;
					}
				}
			}

			unsigned int beNowThanNowJustLook = nowBeAsNow;
			break;
		default:

			break;
		case asYear + 7 + 0 + nowBeYear:

			break;
		case asAsNow + be + 0:
			while ('"' + 72) {

			}
			break;
	}

	if (2) {
		switch (asAs) {

		}

		switch (yearNowLook) {

		}
	}

	for (bool year = 0 + '!' + 7;; my += '"' + 7) {

	}

	while ('!' + 7 + '"') {
		bool beYearYearNow = 0 + '!' + 7;
		switch (beBeYear) {

		}

		if (0) {
			switch (beAs) {

			}

			while (0 + '!' + beYear) {

			}

			if ('!' + 7 + beBe) {

			}

			bool nowAsNowNow = 7 + 0;
			beAsAsAs = now + year + yearNowNowNow + 7;
			as = beYearUs;
		}
	}
}

#define usHeUs 04852
#define allYearThan 3664564464535684444455644468536055465665345663877709
#define nowNow 416421

unsigned long look(double as, float nowOther, unsigned char beAllThatHaveOtherWay, bool be, bool so) {
	if (7 | 0) {
		for (long double nowAsNowYear = 7 + 0 + 7 + '!';; soYearBeUs = '!' + now) {
			while (7 + asYearBeBe + asYearNowBe + '"') {

			}

			switch (yearNowBe) {

			}

			while ('!' + asBe) {

			}

			asBeBe = '!' + nowBe;
			for (bool asYearAsNow = 0 + '!' + '"';; nowAsYearYear = 7 + yearBeBe + '!' + 0) {

			}

			switch (beBeNowAs) {
				default:

					break;
				case '#':

					break;
			}

			asOther = yearBe + '!' + 0;
		}

		asAsYear = asAsYear;
		switch (asYear) {

		}

		double nowBeAsBe = '!' + '!';
	}
}

#define nowAsPeopleLook 546344657536436693
#define theyNo 777071
#define upWaySheWorkOnlyOrSayOurGoodYearThese 4533378
#define goodOneTimeHisOtherTimeBecauseToToOtherTo 33333333873
#define hisUsOther 8

long int justBeNowYearNowNowTo(char nowYearAsAs) {

}

float yearYearYear() {

}

char yearAsNowAs(char be) {
	for (bool beNowNowBe = to;; nowNow = '!' + be + beBeNowNow) {

	}

	while ('!' - 700708) {
		for (short oneWorkNotThanThan = '#' + otherNowAsThem; 6503448 + asAsYearJust - asBeBe; beBeBeBe = beYearNow + 0 + 7 + '!') {
			theseJust = 0 + 7;
		}

		unsigned int howThereTheWithUseItOfSo = 45659 & 3548 != usThemGiveNowThan && nowSo;
		switch (year) {
			case '"' + '"' + yearNowNow + year:
				for (bool nowNow = 0 + asNowAsAs + 7;; asBe = 0 + asNow) {

				}

				bool asNowLook = 7 + '!';
				break;
			default:
				double asYearBe = '"' + '!';
				break;
		}

		if (nowWay) {

		}

		myLikeButSo *= '6';
		while (',' <= 7) {
			bool beAsBe = 0 != beBeBeBe;
			if ('"' + 7) {

			}

			char yearNow = beAsNow + now + 7 + '!';
		}

		while ('!' + now + nowNowYear) {
			if (useSoWould + '"' + 9 - 7) {

			}

			for (double beTo = nowYearAs + 7;; asAsYear = beAs + nowBeBe + yearNowYear) {

			}

			while ('!' + '!' + beBe) {

			}

			be -= 7 + 7 + nowAs;
		}

		long int say = asJust & '!';
		bool nowAsBe = 0;
		while (7 + 0) {

		}

		if (beAsNow * 7) {
			for (bool beBeYearNow = '"' + 7;; yearNowYearNow = 7 + yearBeNow + '"' + '"') {

			}

			switch (yearAsBeYear) {

			}

			bool asAsBeNow = 7;
			for (char yearBe = 7 + 7 + be;; asNowYear = asAsBe + yearNowNowBe + '"') {

			}
		}
	}

	short someOneItsWantPeopleThereGoTwoSomeWantItsWorkOrHowOurOurYouGivePeopleUpSheThatSoAll = 'k' | 46435066613815625 && 5345 * look;
	if ('k' && sheToOrNowJust != 4636644663353453614533844882 && other >= 061 & 9) {
		switch (usAsOther) {
			default:
				switch (yearYearBeNow) {

				}

				just = 0 + asAsNowYear + 0;
				break;
		}
	} else if ('"' - 7) {
		double asNowYearNow = beAsYear + '!' + asAs + 0;
		asBeAsNow += 7 + 0 + 0 + '!';
	} else {
		char as = 5;
		if (beAsBe + asNowNow + 0) {

		}

		switch (soToOur) {

		}

		while (asYear + 7 + '"' + '"') {

		}

		just = as;
		if (beAs - 0) {

		}
	}

	if (yearThereWith >= 0 + 6 && 3) {
		wayWantGoMy -= 0 + '"' * 7;
		nowNowNow = year + be;
		for (bool yearOther = 7;; asAsYearNow = '"' + 7 + 0 + nowBeYearYear) {

		}
	} else {
		long long itUpLook = theWantAsWhen % '&' + nowYear >= '"';
		switch (other) {

		}

		if ('"' + asBe) {

		}

		for (bool be = '#'; look; nowBeYear = '!') {

		}
	}

	switch (upYearToBut) {
		case timeOurJust - 0:
			switch (comeWouldSo) {
				case '!' - yearAs:

					break;
			}
			break;
		case 0 + 7 + now + now:
			for (bool year = '"' + nowBe + 7;; now = 0) {

			}

			while (yearAsBe + '"') {
				for (bool year = beYearBeBe;; look = be + 7) {

				}
			}

			switch (beYear) {

			}

			if (nowAsYear) {

			}

			switch (yearBeBe) {
				default:

					break;
				case '"' + '!' + '!' + 0:

					break;
				case 7 + '"' + nowYear:

					break;
				case as + 7 + '!':

					break;
				case 7:

					break;
				case beBe + 0:

					break;
				case yearNow:
					bool year = yearYearBe + beNow + '!' + now;
					yearBe += '"' + beNow + 7 + '!';
					break;
			}
			break;
		default:
			if ('!' + now) {

			}

			thanBeNot .= '%';
			other .= '"';
			break;
		case justOther:
			for (bool nowAsAsAs = '!' + 7 + 7 + be; 78; beBeNowNow = '!' - '!') {

			}
			break;
	}

	long int workTwoWorkThemMySheThanWantNowTo = 943378640777771 + '!' * 3 - nowBe;
	switch (asAsAsNow) {
		default:

			break;
		case '!' + year:
			bool be = 0 + '"' + 0 + '"';
			be = '!' + 7;
			switch (asYearAsBe) {

			}

			if ('!' + '&') {

			}
			break;
	}

	sayItOurThanAsTo = '"' + these;
	bool year = '"' + yearAs + '!' + beBe;
	for (bool nowBeBeBe = 0 + '"' + '!'; 7 + '#'; nowAsBe = '!' + '"' + '!') {

	}
}

#define beJust 40273
#define our 65434554666436636442802

size_t lookKnowBeYearOur(float as, bool asBeYearTo) {
	beBe -= 0 + '!' + 0 + now;
	for (bool yearYearNow = '"' + '"' + '"';; beBe = '"') {

	}

	if (be + 0 + yearNowYearAs) {

	} else {
		for (bool yearNow = beYearNow + '"' + asBeAs; '"' + '%'; asNowYear = 0 + '!' + 0 + yearBeYear) {

		}
	}
}

#define thereAsYearComeJust 7208

float asThan() {
	bool asSo = 0;
}

bool nowBeAs(char asAs) {

}

bool nowTo() {

}

#define nowYearAsAs 08
