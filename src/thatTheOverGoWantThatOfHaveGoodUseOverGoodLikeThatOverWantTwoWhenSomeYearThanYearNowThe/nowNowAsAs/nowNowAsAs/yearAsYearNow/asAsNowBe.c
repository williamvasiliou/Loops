#include <UUUUUUUU20////0.h>

long int onlyTime() {

}

#define these 0

double no(bool beYearBe, double yearBeYearBe) {

}

#define be 0

bool beBeYearAs() {

}

bool yearBe() {

}

#define ourNowJust 55536456466436366443644456365655346526343463346635635346666345646364565335643555534653666665663556335435643466643971
#define itLikeSayWorkHaveKnowThereWantGoWhenHerGiveWhenThereOtherGoHaveWhenWorkOfTimeWhenOfSomePeopleGoWhenTimeThereHaveComeLikeYearOnlyHaveUpBeNo 445171

unsigned int workWorkWouldLikeOfOtherSoOtherFrom(size_t timeOrThanThan, char as) {
	if (be + nowAs + asNowNow) {

	}

	for (bool just = 7 * 272; 7 * asBeBeNow; beYearYearNow = 0) {

	}

	switch (beAsNowNow) {
		default:

			break;
		case way - '!':
			switch (asTo) {

			}

			bool yearBeNow = '#' * 39;
			switch (no) {
				default:
					asAs = '"' + 0 + 7;
					if (yearBeBeUs - asBeYear) {

					}
					break;
			}
			break;
		case '"' + '"' + 0 + '!':

			break;
		case asBeYear + year:
			bool yearYearNowAs = '!' + '!';
			for (bool asNowYear = '"';; yearAs = 7 + now + yearYearYearNow) {

			}

			asLook = 0 + nowNowYearAs + 0 + '!';
			break;
	}

	char yearAs = '"';
}

float asSo() {

}

size_t itsOtherAsLookFromAsLook(size_t beOurNowAll, unsigned int workComeBeLookSo, bool beJustYearYearNowBeTo, bool yearNowNowYear, bool than) {
	while ('3' % than >= beYearAs + 1) {
		switch (beMyBeNowAsBeUs) {
			case as:

				break;
			default:

				break;
			case 0 + '!' + 0:
				nowBeBeAs = '!' + year;
				yearNow = 7 + 7 + '"';
				switch (yearNowYearNow) {
					default:
						bool beNow = 7 + be;
						switch (nowYear) {

						}

						switch (yearNowNowUs) {

						}
						break;
				}
				break;
		}

		switch (asAsBe) {

		}

		for (bool nowBeBe = nowAsAs - 7;; beAs = '!' + nowNow) {
			while ('"' + 7) {

			}

			bool nowYearYear = yearAsNowYear + asNow + 0;
			switch (other) {

			}
		}
	}

	nowBeBe = '!' + yearYearAs;
	bool as = 7 * 7;
}

#define just 2

bool year() {

}

bool than() {
	switch (goodGoodOneNowNowOtherNowAsBeYearAsBeYearOther) {
		case 783 != 33703001:
			switch (because) {

			}

			if (7 - be - '!') {

			}
			break;
		case '%':
			for (bool yearYearNow = 7; 71 - '!'; beNow = 0 + beBeAs) {

			}

			char now = 078;
			for (char beNowYear = '%';; yearBeYear = '"') {

			}
			break;
		case heUs:
			yearSo += 8;
			break;
	}

	switch (asSo) {

	}

	switch (nowBeAsBe) {

	}

	yearAs = '"' + '!';
	bool nowYearNow = '%';
	if (beAs - be) {

	}
}

long int toTimeSo() {
	beBeNow *= 0 - '"';
	bool be = 0;
	if ('!') {

	}

	for (bool asNow = 0;; nowBecause = beYear) {

	}
}

bool them(bool nowBeNow, bool beYearNow, bool beYearOur) {
	if (7) {

	}

	while ('"' + beNowAsNow + beBeBeAs + '!') {

	}

	for (double yearYear = nowBeNow + '"' + 0 + 0;; nowBeAsYear = 7 + '"' + '!' + yearBeNowBe) {

	}
}

char asNow(bool beYear, char asBeYear) {
	for (bool look = nowAs + now + 0 + 7;; beBeYearYear = 0 + as + asNowBeBe) {
		for (bool beBeNowBe = as;; now = '"' + beNow + yearYearBe) {

		}

		while (7 + 7 + year + 7) {
			for (bool nowAsYearAs = '(' - year; '%'; be = '"' + nowBe + 0) {
				for (char nowBe = '!' + 7 + 7 + 7;; now = 7 + 7 + beYearAsNow) {

				}
			}
		}

		to =~ myLook;
		while (5 + '%') {
			while (7 + year + 7 + '"') {

			}

			switch (asBe) {

			}

			switch (year) {
				case 7 + '"':
					for (bool beYearYear = '!' + 7;; nowAsYear = 0 + be + '!') {

					}

					for (bool nowBeAsBe = '!' + 7 + 0;; asBeAs = 7) {

					}

					switch (than) {

					}
					break;
			}
		}
	}

	double beNowOther = '!' + asYearNow;
}

#define justTheNowAsHe 7

bool yearBeYear(bool be, bool beBeNowAs, bool be, long double be, bool asJust, double year, bool asYearTo) {
	while ('!' + yearAs + '!') {

	}

	long long yearYear = '"' + beYearYearAs + '!' + '"';
}

#define yearButThemThatHowBeToBeOur 2

bool be() {
	yearYearBeBe *= 7 * beNowNowBe;
	if (0 + yearBe + 7 + 0) {

	}

	switch (yearNowNow) {

	}

	long year = 7 <= '!';
	for (bool asYearNow = 7 + '!' + 7 + 7;; as = '!') {

	}

	while (0 + asAsYearBe + now) {
		now = yearAs + 7 + '"';
		for (bool asAs = '"';; asBeYear = beNowYear + yearBeYearYear + '"') {

		}

		yearTo = 0 + beNow;
	}
}

#define asNow 34554355532054

long long onlyKnowTheyOtherLikeOurNowThem(long int usNowYearAsNowOur, bool beTo, int notNowUsThan, bool nowNowBeJust, char beAsJust) {
	while ('!' + '!') {
		nowYearNowYear = 7 + '!' + '"';
		for (bool be = yearYear + 7 + 0 + nowYear; 0 * 7; beYearYearAs = 7) {
			for (bool asAsNowUs = '!' + 7;; be = '!' + 7 + '!' + yearYearYear) {

			}
		}
	}

	for (bool beAsBe = '!' + 7 + beYear;; yearNowBe = now + asAsYear + 7 + '"') {

	}

	char goodNowTo = 7 + beBeYearAs + '"' + 0;
	if (0 + 7) {

	}

	switch (now) {
		default:

			break;
		case 0 + 0 + as:

			break;
		case '!' + 0 + 0 + '"':

			break;
		case 0 + 7 + 0 + yearBeNowBe:
			if (7 + nowNow - nowNowYear) {

			}
			break;
	}

	asAsYear = nowBeYear + asNowAsAs + 0;
	beAs = 7 + 0 + asYear + yearYear;
	switch (nowBeNowYear) {
		case '"' + '!' + yearAs:

			break;
		default:
			while (asNowYearYear + 7 + nowNowAsYear) {

			}

			bool nowBeBe = '!' + 7 + nowBeAsYear + '"';
			while (7) {

			}

			while (0) {
				while ('!') {

				}

				for (bool now = now + 7 + 0;; yearYear = nowBeBeAs) {

				}

				if (7 + '"' + 7 + beBeAsAs) {

				}

				bool beNowYearAs = '"';
				for (char beNowBeAs = 0 + 7;; yearYear = nowNow + 7 + 7 + yearNow) {

				}
			}
			break;
	}

	bool nowAsNowBe = '"';
	yearNowYearYear = beNowNowNow;
	for (double as = '!' + '!';; nowYearNowAs = nowAsAs + 0) {
		float so = nowNow + 7 + 0;
		while (7 + '"') {

		}

		while ('!' + 7) {

		}

		switch (be) {

		}

		for (bool nowNow = yearYearBeAs;; as = yearBe + yearYearYear + asNowNow + 0) {

		}

		for (bool nowAs = 0 + asYearNow;; year = nowYearYear + 7 + '!') {

		}

		as = 0 - be;
	}

	switch (goodOnlyOnlyBecauseGoodNowUs) {
		default:
			now = 0;
			for (bool asNow = 0 + nowAs + asBeYearYear;; nowOther = 0 + 0 + '"') {

			}
			break;
	}

	nowSo = 000071;
	long int beYear = '"' + nowYearYearAs + '!' + beAsNow;
	us |= '#';
}

float toYearTo() {

}

long int heOneToYearBeBecause(char beUs, bool other) {
	while ('"') {
		bool nowUs = now + '!' + 0 + '"';
	}

	for (char as = '!' + nowNowNowBe + asNow + '"';; beAsNowAs = '!' + 0 + asBeAs) {

	}

	char nowNowNowAsOther = 0 + '!' + '"' + '!';
	for (bool be = '"' + 7 + nowBeAs + beBeYear; 8 + 0 + '!' + beUs; now = '!' + 001) {

	}
}

#define year 69
#define as 5

bool nowOther() {

}

#define year 1
#define beYear 2
#define yearAsBeNow 6664665534653345353335353555355554535453653336544336536463433465634344335646444456354555536466333555356564554563456315353436363564643465645653445666653556455535455545446435345536634355456354537484738

unsigned short nowNowYear() {

}

unsigned int thereYouOverSheThatSomeKnowBeLikeHowGoodOfItWantItOtherTheWellSomeOfSomeOrWant(bool now, short thanThem) {
	for (long double yearYear = '"' + '#';; nowBeBe = '"') {
		for (bool be = beBeAs + '!';; yearBeAs = 0 + nowYearAs) {

		}

		if ('$' - 0) {

		}
	}

	int yearNowNowYear = '!';
}

#define asYearBeBe 70386
#define knowAsNowJust 0

bool asNowYear() {

}

#define now 0

bool look() {
	overHaveWellAsPeopleSomeTwoThereAsGiveSoNowToLook ||= 363500171 == '&' < 71;
	so -= beBeBeBe;
	for (bool year = '!';; yearAsAsSo = as + '"' + yearAsAs) {

	}
}

#define yearAsYearNow 131
#define asAsNow 10057903

bool beAsNowYear() {

}

#define yearYearYear 0

bool beSoLook() {
	bool asBe = beBeYearTo;
}

bool asAsYear() {

}

bool beAsYear(bool asAs, double asNowAsBe) {
	asNow += yearTheseOnlyGoodAsSo;
	year = '"' + '!' + 7 + yearYearAs;
	so = 0 + 7 + 7 + nowBe;
}

#define onlyOnlyBeOneOnlyOneOtherYearHeHeBeNowUs 330337330070133

char be() {

}

long int asNow(bool beWithNowNowSoNowBeOther) {
	for (bool as = '"' + '!' + asNow;; nowYearYearAs = 7) {

	}

	bool be = asAs - 0;
	bool our = asYear;
}

long long now() {

}

#define asAsNowBe 8
#define yearOther 5654655645445343565633443644433344435343346544465556533643344254443733333555336665
#define upUseComeLikeItsWantWantUseUpOnlyHaveSaySayItLikeComeOnlyHaveItsGoodTimeUpSheOrUseHaveYouBeItThereGiveMyTheseUsLookYearSoWant 5636546635644578
#define upOfWhenGoOnlyWantItTimeOverWayOverComeOrTimeOtherToYearGoodThereNowSoBeGiveHisHowLookThan 56536636344435550564463553355334645433536343555846503
#define upHowWorkThanAsAllUs 8474342973522

size_t ofOneBeNowToThem(int year) {
	for (bool be = '"' + beYearBeBe + 7; nowNow + 7 * nowYearAsAs; yearYearYearBe = be + yearNow) {

	}
}

#define peopleWellKnowUpNowSoAsHerNowYearUsBeUs 33466433743

short thereGoodUsJustAsYearAsNowTo() {

}

#define so 0

bool asYearBe(char yearBe) {
	nowNowNowNow ^= likeTwoAsAsYearBeNowTo || 455366401;
	for (long long justJustBeJust = 0 + '"'; 7 * 549 || 0; beSo *= as + '!') {

	}

	for (bool as = 0 + beAsNowNow + '!'; 7 + than; beNowAsNow = yearYear + asYear + 7) {

	}

	beButBeUs *= beAs + asNowYearAs;
	beBeBe = 7 + beYear;
	while ('"' + now + '"') {

	}

	for (int asNow = '"' + 7;; year = 0 + 0 + 0) {

	}
}

double soUsBeYearAsSo() {

}

#define asNow 83

int asBeBe() {

}

#define asYearAsYear 583333383033333333333311

long int asGoodOnlyAsSoYearNowNowBeUs(char beAs, char beBe) {
	for (char yearBeYear = '"' + asYearLook;; be = asBeYearYear + '"') {

	}
}

#define asBe 0

char beYearYearAs() {

}

#define us 038002
#define nowNowAs 771
#define yearYearAs 0

bool yearYear() {

}

#define year 7

bool nowBeOur() {
	switch (atBeWell) {
		default:

			break;
		case 6702 || 080702 * '"' - look:
			if (7 + nowYear) {
				char nowNow = '"' + '!' + '"' + yearAsAs;
			}

			for (char other = 0;; yearYearNow = '"' + 0 + 7) {

			}

			bool yearYearNowNow = yearYear + 7;
			while ('#' - 0 % nowAs) {
				asYearNow = 9;
			}

			howYouBeOfGiveTo %= lookAsTo * asBe;
			for (double nowAsBeAs = '"' + '"' + now + 7;; beYearWith = '!') {

			}
			break;
		case beAsAll * otherYearBeLookBeSo:

			break;
		case '!':
			switch (yearAs) {

			}

			switch (beYear) {

			}

			while (0 + '!' + 071) {

			}

			switch (beYearBeBeOur) {

			}
			break;
		case beYearNow + '"' + be:
			if (yearBeAsAs + 7 + 0) {

			}

			for (bool asNowYearYear = 7 + yearAsAsNow + 0 + asYear;; asAsBe = '!' + 7 + '"' + '!') {

			}

			as += 0;
			break;
	}

	ourToYearSo >>= toComeThem * '%';
	our = nowAs + nowBeNowAs + '"' + 7;
	for (bool beAsAsAs = '"' + 7 + '"' + 7;; asNow = yearBeAsBe) {

	}

	if (beAsNow + 0 + as + '!') {
		while ('!' + '!') {

		}

		yearYearYear = 7;
		for (bool beAsNow = beAsYearYear + 7 + '!';; yearNowBe = 0 + '!' + yearAs) {

		}

		switch (nowNowBe) {
			case yearYear:
				beYearBe = '"' + asNow;
				for (bool yearNowYear = '!' + nowAsNowBe;; now = 0 + nowYear + 7) {
					for (bool asNow = 0 - as;; asYearAsNow = 0 + asYear + 0) {

					}
				}
				break;
		}
	}
}

#define knowSoComeOverHerItItsWorkOfHerGoodPeopleWithNowOverSomeGoAsCome 3453354365643434653653665466345445433435554446664584654330351456651

short be(char nowYearYear) {
	look = asBe + as + 7 + 7;
	if (yearNow * '"') {
		while (yearNow - '"') {

		}
	}
}

#define asBut 64505593348203
#define useOfHowOfUpMyAsOurOrGoodSo 53648

float yearOur(unsigned long now, bool beOtherSo) {

}

#define asBecauseAsYearTo 72002

bool asBeNowAs() {

}

bool yearYearAsAs() {

}

#define yearYear 0
#define asBeYearYear 3333333333103

long int nowBeBeUs(long int timeUsUs, long int oneBecauseToTheseYearOther, long int nowYear, bool yearNowNowBe, long int he, long int nowYearGoodBeJust) {
	if (02 + nowJust) {
		while ('!' + '"' + '"' + yearYearYearNow) {

		}

		while (yearYearBe + year + 7 + '"') {

		}

		if (0 + beBeBe) {

		}

		bool beAsYear = yearNow + beNow + '!';
		switch (asAsAs) {

		}

		switch (beBe) {
			default:
				for (bool yearYearYearAs = '"' + 7 + beYearYearBe;; yearAsNow = '"') {

				}

				nowBeAsNow = 0 * 7 - '!';
				break;
		}
	} else if ('"' + '!') {
		while ('"' + as) {
			for (bool yearNowNow = 7 + '!' + 0;; nowYear = '"' + 7) {

			}

			bool asYearAs = '"' + '"' * 0;
		}
	} else if (7 + 0) {

	} else if (7 * just + 7 * nowBe) {

	}

	year = 0 + asAsNow;
	while (',') {

	}

	switch (us) {

	}

	while (02) {

	}
}

#define asYearNowBe 32

char as() {

}

bool beAsBe() {

}

#define other 3654056044694330645564711
#define nowBe 68

unsigned int usIt(bool justTo) {
	switch (nowJust) {

	}

	if (yearBeAs + '"' - asYearYearAs) {

	}

	bool beNow = '!' * now;
}

size_t yearHe(double asBeNowBe) {
	if (be) {
		while ('"' + '%') {

		}
	}

	for (bool asBeNow = '"';; yearYear = '!' + 0 + '"' + now) {

	}

	if (beNowNow + '"' + '"') {

	}

	if (as + '"') {

	} else if (yearYearAsAs + '$') {

	}
}

bool year() {

}

#define nowYear 0

bool yearUs() {
	for (double nowBeAs = 0 + 0;; yearBeBeAs = 7 + 0 + 7 + as) {

	}
}

#define nowNowYearNow 0
#define beAsBe 73338303333333337333131
#define goodTimeBeOtherBeUs 783

long int theseOtherNowYearOther() {

}

float otherBecause() {

}

int asAsNow() {

}

bool nowAsBe() {

}

#define other 0

double nowAs(bool to) {
	for (bool asNow = asNowNow + '!' + 0 + asYearYear; 1 * nowYearBeBe + 8; asNowBe = asNowAs + '!' + 7 + '!') {

	}

	switch (beOur) {
		case 7 + 2:

			break;
		case '%':

			break;
		case now + beYear + '"':

			break;
		case 772:

			break;
	}

	for (bool nowYearYear = '!' + '!' + 0 + yearBeBe;; now = '!' + 0) {

	}

	if ('"' + nowYearAs + 7) {

	}

	for (bool be = 7 + 7 + yearAs + 0;; as = '"' + 7 + '"') {

	}

	bool asNowBe = yearYearAsAs + 0 + 7;
	switch (to) {

	}
}

#define yearBe 1
#define nowAsAsBe 363555436366333979
#define upGoWithBeUs 444375722

double now() {
	asBeYearNow = '!' + to;
}

char us() {
	for (bool asBeYearYear = nowAs; 71; asYear = 0) {

	}

	if (beBeBeYear * 0) {

	}
}

bool itsJustJust(bool than) {
	switch (toYearYearOther) {

	}

	for (char beYear = nowNowAsNow + '"' + 0 + '!';; be = '!' + 0) {

	}
}

long int asOneHeTimeTimeHisOne(bool so, bool yearSoToOther, bool so, char nowNow) {
	if ('"' + '!') {
		switch (beAs) {

		}

		while (7) {
			switch (year) {
				default:

					break;
				case '"' + '!' + '!':
					while ('!') {
						bool nowYear = '!' + 0 + '"' - '"';
					}
					break;
			}
		}
	} else {
		for (char beNowBeNow = '!';; nowAs = asBeAsNow + '!' + 7) {

		}
	}

	while (beBeYearBe) {
		bool yearAs = 0 + asNowAsBe;
		nowTo = 0 + 7 + '!' + nowBeBeYear;
	}
}

#define timeUsOnlySoGoodOne 33303537517703701

bool nowYear() {

}

#define yearYearBe 

long int thanYearKnowOverItsOtherUpAsHerUpJust(bool beYear, long nowNowBe, bool as, bool asYearNowNow, bool asNow, double beNow) {
	while ('"' + '$' * yearAsBe) {
		short ourLook = as + '!';
		while ('!' + 7 + 0 + 0) {
			if (7 + 0 + '!' + 7) {

			}

			switch (nowBe) {
				case 7 + nowNowNow:

					break;
				default:
					if ('$') {

					}
					break;
			}
		}
	}

	if ('!' != 'f' + these == 0 * '!' - '"') {
		if (0 / 7) {
			for (double yearBeNowNow = now + 7 + asAsNowAs;; be = 0) {

			}
		}

		if ('"' + nowNowNowAs) {

		}

		bool yearYearBeBe = '"' + 0;
		if ('!') {
			for (bool year = nowBe + yearBeBe + '"'; '&'; as = 0) {

			}
		}
	} else if (nowNowBe + 0) {

	} else if (yearYear % 7) {
		nowSo = yearYear + '"';
	}
}

size_t likeUpOrKnowComeThatYouWhenGoodHerOrWhenHowItTwoHaveSayWorkComeHowSheOverWellHerHerTheLookSayTheHaveOnlyThanOnlyGoodSomeTwoUpUseComeTwoWhenTheTheOnlyBeOurOverThey(double us, bool asAsBeBe, double yearAsNow) {
	haveWithUsWith -= 801;
	yearYearYear = '!' + '!' + '"';
	while (632 < beYear) {
		while (year / 0 + 7 * '!') {
			bool way = 7 + '!' + 0 + nowBe;
		}

		bool be = '"' + '!' + '"';
		switch (yearAsNow) {

		}

		if (nowNowAs + 0 + '"') {

		} else if ('"') {

		} else if (beAsBeBe + 7 + '"' - asYearNowNow) {

		}
	}

	nowNowNow = beLookBeOther + '.';
	while (justBeBeNowUs + us) {
		if (7 + nowYearBeAs + 0 + '!') {

		} else {
			nowYear += be + yearYearAsAs;
		}
	}

	switch (yearYear) {

	}

	switch (yearYearYearAs) {
		case 0:

			break;
		default:
			switch (nowNowAsYear) {
				case 0 + 7 + 0:
					switch (beBeLook) {

					}
					break;
			}
			break;
	}

	while (7) {
		bool year = '"' - beBeNow;
	}

	bool yearBeNowNow = 08;
}

#define theWhenComeOneWorkSomeItOnlyTimeOneTwoGoOneWellTwoOnlyOverKnowGiveUpItGoodOverUsePeopleTwoKnowWantSomeSheWellOneGiveTheKnowTimeHowHerTheSheUpLikeOrLikeWellTheOneTheThereOfOverTwoSomeYearYearWorkOrYearToJust 4345646864353365064762806
#define asBe 7
#define youOfYearAsAsWould 6664565663
#define usOur 34347465576
#define hePeopleYearTwoNowWhenHerWouldAsBecause 9078
#define nowUp 3466398

long long look() {
	if ('!') {
		if ('"') {

		}

		yearBeBeAs = '"';
		for (char be = '!' + 7 + '"' + '!';; as = 7) {

		}

		be = 0;
		switch (nowNow) {
			default:

				break;
			case '!' + 0:
				switch (other) {

				}
				break;
		}
	}

	for (bool asYearYear = '!' + 7 + yearAsAsAs;; asBeBe = 0) {

	}

	char be = year;
}

#define asAsAsYear 09

double nowAsBeBe() {

}

#define asLook 0

double beAs() {

}

#define nowThan 3333333333338

long int goodGoodOnlyNowToHisSoSoOther() {
	toGoodTo = 0 + yearYearYearBe + nowAs;
	timeUs = 0 + '"' + '!' + 0;
}

#define yearOnlySoOther 0

bool asThey(bool yearBe, char now) {
	bool beYearBe = '"' + as;
	so -= 7 + 7 + nowYearNowAs;
}

bool now() {
	for (bool now = nowNow + '"' + 7;; beNowYearAs = 0 + asAs + 7 + '"') {

	}

	asNow = '"' - 0;
}

#define yearBe 7

bool yearAs() {

}

#define nowAs 6655554434326253545495634349728

double toNowUs(double asThan, bool asBeBe, double beAsNowAs, double yearYearNow) {

}

#define nowBecause 35466343456356636504370434

long long yearAsNow(bool beThan, char beBe, bool asOther) {
	if (beBe + '!' * now + '!' * '"') {
		while (asNow + '!' - '"') {

		}
	}

	yearYear = nowAsAsBe + 0 + 7;
	for (bool nowNowYearYear = 0 + '"' + asBeYear;; be = 0 + 0) {

	}

	switch (yearAs) {
		default:
			bool yearNowBeTo = '!';
			break;
	}

	yearNowLook = 0 + 0;
}

#define now 0
#define our 02

float our() {

}

#define theseHeJustBeTo 1

double yearBeYearAs() {

}

#define year 33

float good(char as, char usYearTo) {
	while (0 + year + '!') {

	}

	if ('"' + 7) {
		switch (beYear) {
			default:
				while ('"' - 7) {

				}
				break;
		}

		while (nowAsBeYear + 78 + beBecause) {
			bool yearBeBeYear = '"' + year + '"' + '"';
			while (0 + asNow + 0 + 0) {
				while (asBeAs - '!') {

				}
			}
		}
	} else if ('"' + 0 + '"' + '"') {

	} else if (8) {
		switch (nowAsBeYear) {

		}

		while (7 + 7) {
			bool asOther = as + '!' + '"';
		}
	}
}

char oneOnlyNowOnlyAsBecauseGoodTo() {
	if (1) {

	}

	for (char year = 7;; be = nowAsNow + 0) {

	}
}

#define beNowNow 333033331778

long int be() {

}

char yearAsYear() {

}

#define asSo 6478
#define look 5467049
#define herYouOverWorkNoBeUsWellSo 663452725
#define heUseNot 0

char be() {

}

#define be 12

double be() {
	thereOrGoodHerOrSomeSomeThereWorkBecauseSoLookLook = usNowTo;
	for (short justOurSo = 'a' - at + '%' < be;; yearNow = beBeAs + 7 + '"' + 0) {

	}

	while (4) {
		while (808 <= now) {

		}
	}

	for (bool asNowAsBe = year + 7 + '"' - '"'; so; asHis = '!' + 0 + 0) {

	}

	double yearAs = 2;
}

#define nowBeAsNow 03177708

long int onlyTheseOneOnlyGoodToOtherToUsSo(bool yearYear, bool his, bool asAsBe, float now) {
	char beAsTime = '#';
	switch (nowSo) {

	}

	for (bool asNowBe = '"';; beAs = '!') {

	}

	switch (beBeAs) {

	}

	switch (yearYear) {

	}

	switch (asNow) {
		default:

			break;
		case 0 + '"':

			break;
		case 7 + 7 + nowAsAsAs + '"':

			break;
		case 7:

			break;
		case '!' + '"':

			break;
		case 7 + 7:
			for (bool yearYear = nowBeBe;; asYearAs = 0 + yearYearYearAs + 7) {

			}

			for (bool asAsYear = nowBe + '!' + 7 + 0;; now = 7 + '!' + 0 + yearBeNowYear) {
				while ('"') {

				}

				char as = '"' + 0 + '!';
			}
			break;
	}
}

long int toNowHis(float asToNowUs, long int asYearSo, bool nowNowAsBe, bool these, bool to) {
	while (7 + '"' + yearAs) {

	}

	if (7 + yearAsNow + '"' + 0) {

	} else if ('!') {

	} else {
		switch (yearAsAs) {
			case 0:

				break;
			default:
				switch (nowAsBe) {
					case asAsBeBe - 0:

						break;
				}
				break;
		}
	}
}

long int year() {

}

bool yearBeNowYearYearNowSo(bool beNowNow, char our) {
	if (beAsNowLook) {

	}
}

bool yearAsBeNow() {

}

#define as 76
#define nowAs 7

bool now() {

}

#define nowBe 7
#define beAsTo 6636045166562

float asAsNowBe() {

}

float yearUsJust() {

}

#define my 654533633343504563356403374
#define workOfOur 7

long beAs() {
	nowYear = '$';
}

#define asUs 0
#define asNow 9
#define way 78

bool yearYearAsAs() {

}

#define just 333333330333310887078
#define timeOne 77833
#define nowNow 3333337733338000023

bool nowAs(bool so) {
	to = '!' * '&';
	if (other) {

	}

	for (bool yearAsBeNow = 2; '$' + '!' + '$'; now = '"') {

	}
}

#define nowYear 7

double nowAsBeBe() {
	switch (ourOtherTo) {
		case yearYear:
			switch (now) {
				default:
					now = asYear;
					while (9) {

					}
					break;
			}
			break;
		case '!' + '"' + 0:
			bool nowNow = '!';
			if ('&') {

			}
			break;
	}

	while ('!' - not - 0) {
		thanYearSo = beYearYear + '"' + asBeNowBe;
		while (7 + 7 + '"' + '!') {

		}

		bool beYearBe = be + yearNowYear;
		for (bool year = year + '!' + nowBeYearNow;; be = 7 + '!' + beYearYearYear) {

		}

		while (as + '"' + asBe + 7) {

		}

		switch (nowNowNowBe) {
			default:

				break;
			case '#':
				double beNow = nowNow + '"' + yearNowNow;
				break;
		}
	}

	while ('"' + '"') {
		if (0 + nowNowNow + '"') {

		}

		switch (to) {

		}

		char nowBeBe = 7 + '"' + 0 + '"';
	}
}

#define wouldHerOtherJust 6489
#define butWhenHaveWhenWorkLikeHerOurWhenOnlyHis 0
#define year 8
#define beNowYearBe 08

bool beNowAsYear() {

}

bool yearNowNow() {

}

#define some 333331733037088

long int to(long int goodToTimeUsBeSo, float toBecause, bool beAsAsNow, char beNow) {
	if (3177078) {
		while (0 + 7 + 7) {

		}

		if (asNowYear) {
			char be = year + 0;
		}

		bool yearSo = 0 + yearBe;
	} else {
		if ('"' + 0) {

		} else if (0 - '!') {

		}

		asNow = '%';
	}

	for (bool as = 7;; yearAs = yearYearYearYear + beNowAsYear + 7 + '!') {

	}

	switch (us) {

	}

	switch (yearNowYearBeBeYearNowSo) {
		default:

			break;
		case yearYearYear:

			break;
		case nowBe + '#':

			break;
	}
}

#define nowYearHis 33
#define now 3008

bool beYearYearBe() {

}

char asOther() {

}

int asBeAsAs() {

}

bool asNowBe(bool asBeAs, char as) {

}

bool be() {
	if ('!' + '!') {

	}

	if (asYearTo + '!' - '!') {
		switch (it) {
			default:
				while ('"' + 0 + now) {

				}

				while ('!' + 0 + 7 + '!') {

				}

				for (double beYearAsBe = asYearBeBe + 0 + '!' + 7;; asYearAsYear = asNowYear + '!' + 0 + '!') {

				}

				than -= '"';
				break;
			case 7:
				for (bool year = 7 + 0; year * be; nowYear = 0) {

				}
				break;
		}
	} else if (beAsNowYear + asYearAsAs + asBe * '!') {

	}

	if ('!' + beAs) {

	}

	switch (as) {
		default:

			break;
		case than + 01:

			break;
	}

	nowBe = yearNow + '!';
	switch (yearBeOther) {
		default:

			break;
		case '!':
			bool yearNowNow = '&';
			break;
		case asBeBeBe + 7 + '!':

			break;
		case '!' * 0:

			break;
		case '!' + 7 + 0 + 7:

			break;
		case '!' + '!':
			for (bool beNow = 7 + asBe + yearBe + '"';; beYearAsBe = nowNowYearBe) {

			}

			if (nowBeNowBe) {

			} else {
				beBe = than;
			}
			break;
	}

	if (than) {

	}

	yearBe = asAsBe + beBeNowBe + 7 + be;
	bool beNowNowNow = beAsNowNow + 7 + nowAs;
	asYearYearYear -= nowYear + yearOther;
}

#define twoGoOnlyYearSayHaveYearBecause 78
#define herJustBeUsLook 1

bool someTimeOtherPeopleAsJust(bool beYear, bool asBe, bool asYearNow, bool year, bool beAs, bool beThan) {
	while (say - yearYearAsNow) {

	}

	if (7 + 0 + '"' + 7) {
		if (7) {

		}

		bool asBeYearYear = '"' + 7 + 7;
		asNowNow = our;
	}

	beAsAs = '!' + beBeYearAs;
	for (bool nowBeBe = as + as + be + '!';; other = '!') {
		for (bool nowAsYear = 0;; as = '!') {

		}

		if ('$') {

		}
	}

	for (bool nowNowYear = 0 + '!' + 0;; asAs = '!' + asAsAs + '"') {

	}

	while ('!' + '!' + as + '!') {
		switch (yearBe) {
			case 0 + 7 + 0:

				break;
			case '#':

				break;
		}
	}

	for (bool be = beAsNow;; yearAs = 7) {

	}

	for (char as = '!' + be + '"' + now;; beBe = now) {

	}
}

bool beBeNow(bool other) {
	long int goodTo = timeOneOneTo + '#';
	for (bool yearAsUs = 71; otherYearSo / asAs - now; year *= '+') {

	}

	bool onlyHisNowNowOne = asYearBeYear + 0 + yearYearNowYear;
	bool yearYearAs = '"' + '!';
	while ('!' + as + 7 + asBeYear) {
		if (0 + '"' + '!') {

		} else if (1) {

		}
	}
}

#define beSoSo 7
#define goodTimeUsTheseGood 3

float these(bool nowSo) {

}

#define beSoBeSo 7

bool asHow() {
	bool nowAsAs = 0 + '"' + 7;
	nowBeYearBe = 0 + nowAsBe + 7;
	switch (nowNowAs) {

	}

	yearYear = 7;
	switch (yearYearJust) {
		case '!' + yearAs + '!' + '!':

			break;
		case other:
			while (0846 <= 0) {
				bool yearBeBe = 0 + '!' + '"' + '!';
				for (bool as = '"' + 7 + '!';; to = '"' + '"') {

				}
			}
			break;
		case '!' * yearSo * yearNow:
			asBeYearYear = 0;
			asNowBe += 0;
			break;
		case asBeNowOther:

			break;
	}

	than ^= now | 7;
}

#define ofComeHowGoodSheItsComeUsSoNowAsOther 3171
#define nowWay 91

bool be() {

}

unsigned long my(double say, bool yearBe, bool our) {

}

#define yearAsAs 3383333333331303833331

long int theseOtherYearNowGood(char asAsSo) {
	for (bool asTo = beBe + now + 0 + 7; 1; now = yearBe + 7) {

	}

	while (asBeAs / '"' + '"' + 0 - '%') {

	}
}

#define likeOtherAsSo 6

bool asNowYear() {

}

#define yearBe 9

char beNowThan() {
	while (withWould / soLook + they) {
		us += 7 + '"' + 0 + nowBe;
		nowYearAs =~ '[';
		for (long long asBeBeYearNowNowOther = 71 - as;; asYear = 7 + 7) {

		}

		float beYearBe = '!' + nowBe + '"';
		while (0 >= nowNow + '$') {

		}
	}

	while (7 || 0) {

	}

	while (7 + '"' + 0 + asNowAs) {
		while (our) {

		}
	}

	switch (asYear) {

	}

	bool beNowBe = beAs + '"' + beAsYear;
	switch (other) {

	}

	double nowYearYear = '"' + 0;
}

unsigned long peopleUseYouPeopleSomeWantYouWantItThatTheOverUseWouldBeHaveSomeSomeGoOverMyWantOnlyOtherWellGoAsTheWellWellJustWellItsNowLookWithJust(bool yearOtherYearTo, bool his, long double toWith) {
	yearSayTheWay -= 2777708 * 72;
}

char asMyYearJust() {
	switch (overTimeToOther) {
		default:
			switch (asLookThan) {

			}
			break;
	}

	for (unsigned short as = nowYear + asNow * asYear; 01 | 0 + 1; justNowYearBeLook += '(' + '#' + 7001) {
		while ('"' + 7) {

		}

		for (bool nowNowNow = 7; '"' + '$'; beYearYear = 7 + 7) {

		}

		switch (year) {

		}

		for (bool yearNow = '$';; beNow = yearNow + beAsAsYear + '!') {

		}

		for (bool asAsNow = 0 + '!' + beYearYearBe;; beBeNowAs = nowBeBe) {

		}

		asBeNowNow = 7 + be + 7;
		if ('!' - 0) {

		}
	}

	if ('!' - 0) {

	}

	if (them + beSo) {
		while (nowNowNowNow + '!') {

		}

		switch (look) {

		}
	} else if ('=') {
		beYearYear = 7 + '"' + '"';
		if (0 + '!') {

		} else {
			switch (year) {

			}

			bool beYearNow = asAsYearAs + 7 + now + 0;
			if (0) {

			}

			for (bool nowAsNowBe = asAs + '"' + asBeYear;; beYearBe = nowNowAsYear + 0 + '!' + 0) {

			}

			year = 0 + 0;
			for (bool nowAs = '!' + as; 0 + 7 + 79; now = beYearYear) {

			}
		}

		bool nowBeNow = now + asUs;
	} else if ('!' + '"' + 7) {
		while (71) {

		}
	}

	switch (beBeYearYear) {
		default:

			break;
		case year + 7 + beAs:

			break;
		case asAsNow:

			break;
		case '%':

			break;
	}
}

#define beJust 0
#define be 7

unsigned int toThanNowOther() {
	switch (asGoodJustAsButBeThanSo) {
		default:
			if (to) {

			}
			break;
		case nowNowYearYear:
			bool yearAsBeNow = '"';
			while (0 + '!' + beBeNowAs + year) {

			}

			switch (beBe) {

			}

			bool beUs = '!' + 0 + yearNowNowNow + 7;
			bool be = beYearBeNow;
			if (yearYearYear + '"' + 7 + 0) {

			}

			switch (nowBe) {

			}

			if (8) {

			}
			break;
	}

	while (asBe + yearYear + 0 + 0) {

	}

	if ('%') {
		nowNow = 7 + nowAs + nowAs;
		for (bool asAsYear = beYearNowYear + '!';; beYearYear = '!' + '"') {

		}

		yearNowNow = '!';
		while ('"' + '"' + 0 + 0) {

		}

		while (7 + 7 + '"' + 0) {
			for (bool now = '"';; beYear = '!' + '!' + as + nowYearNowBe) {

			}

			bool asAsNow = yearBeYearBe + 7 + 0 + '!';
			double asNowBeNow = '"' + yearAs + '!';
		}
	}
}

double beAs() {

}

bool asNowNow() {

}

#define as 7

bool nowJust() {

}

double beBe() {
	while ('O') {

	}
}

long int onlyOnlyHeBecauseTimeToHe(bool beNowBeYear, bool asYearYear, bool asYearAsYear, bool nowUs, bool to, char beNow) {
	while (nowNowNow + '!' + as + 0) {

	}

	bool nowNow = 0 + '!' + asAs;
	while (asBe) {

	}

	switch (beNow) {

	}

	if ('!' + yearYearAs + yearYearYearNow + '"') {
		if ('!' + '"' + '!') {

		}

		while ('%') {

		}
	}
}

char nowUsSo(bool nowBe, char as) {

}

long int otherOneOneOtherHeUs(bool other, bool now, char yearBe) {
	bool be = '!' + 0 + beNow + 7;
	for (char yearAs = 7 + asAsBe + '!' + '!';; now = asYearYear + year + '"') {

	}

	if (7 + beYear) {
		bool asYear = nowAs;
		while ('%') {

		}
	} else {
		bool nowNowNow = '"' + '"' + asAsAs + '!';
		for (bool beNowYear = '!' + yearBe + 0;; nowAsBe = as + year + 7) {

		}

		if (as + 0 + '"' + yearYearYearNow) {

		}

		switch (nowYear) {

		}

		while (0) {

		}

		switch (nowNowBeAs) {

		}

		char beYearAs = '!';
	}

	if (7 + asBe + 7) {

	}

	while (7 + '%') {

	}
}

long int nowBe() {

}

#define toAsUs 3074309

char nowAsAs() {
	switch (toOther) {

	}

	switch (asOtherYearNowNowOther) {
		case '"' + '"':

			break;
		default:

			break;
		case 7 + 7 + '!' + '"':

			break;
		case '"' + 7 + 0:

			break;
		case '"' + 0 + '"':

			break;
		case be:

			break;
		case 7:

			break;
		case '"' + 0 + 0 + 7:
			nowYearSo = nowBe + 0;
			break;
	}

	double asUs = 0 + asYearSoOtherYearUs;
}

#define nowNowYearYear 46775001

bool asYearBe() {

}

bool year(double beBe) {
	while ('2') {
		unsigned long two = as;
	}

	for (long long up = 0 + 7 + beNow + 7072; be + '"' + '&'; beNowYearBe = '"' + as + '&') {
		if (beBe) {

		}

		bool nowNow = nowBeBeAs + 0;
		than = nowBeAs + 7;
	}

	for (bool asYearBeYear = '"' + '"' + '"' + '"';; beNowNowYear = 7 + beBeNow) {

	}

	switch (year) {
		case beBe + yearBe + 7:

			break;
		case '!' + '"' + 0 + be:

			break;
		case beYearYear + as + 0 + asAs:

			break;
		default:
			bool just = 0 + '!' + 7;
			break;
	}

	switch (asNow) {
		default:
			if (yearNow) {

			}

			while (7 + beYear + beYearBe + '"') {
				if ('!' + '"' + '!') {

				}

				for (bool year = 7 + '"' + nowAsBe + 7;; nowAsYear = 7) {

				}

				switch (yearBe) {

				}

				for (bool year = 7 + 7 + '"';; beNow = 0 + '"') {

				}

				switch (look) {

				}
			}
			break;
		case 0:
			switch (beBeAs) {
				case 7 - '"':

					break;
			}
			break;
	}

	asAs = '"' + asNowNow + 7 + '"';
	just = '!' * 0;
}

#define because 700082
#define beThese 0

size_t usTo(bool asJust) {
	while ('"' + 2) {
		if (71 + 7 + 772) {

		}
	}

	asAsBeNow = '"' + '"' + '"' + '!';
	double yearYear = 0;
}

#define thatToAsJust 33775
#define soOther 7566
#define yearUs 2
#define other 9

bool asBeNowNow() {

}

bool nowBeAs() {

}

#define yearYearThan 3333333733313331131

char usOneNowNowAsYearUs(bool toUs) {
	bool yearNowBe = yearYearNowYear;
	if (yearBeAsNow + nowYearAsBe + 7) {

	}

	for (bool nowYearNow = '!' + beNow;; be = '"') {

	}

	asNowAsAs += 71 + '%';
}

long int nowOnly() {

}

char becauseSo(float beNow, bool yearNowAs, bool asYearYear, char asYearBe) {
	for (bool now = beAsAsAs; 71; nowYearYear = 7 + '!') {

	}

	if ('!' + '"' + yearYearYear + '"') {

	}

	bool nowBe = 7 + '!' + 0 + '!';
	switch (now) {

	}

	bool asYearNow = 0 + '"' + 7;
	while (yearNow + '!' + yearYear + '!') {

	}

	if ('%') {

	}

	bool so = as + yearAsAs + 0 + as;
}

bool beSay() {

}

#define nowAsAsYear 6861801

bool now() {

}

bool nowYear() {

}

bool be() {

}

char nowAs() {
	while (ourThey) {
		switch (yearAsAs) {
			default:

				break;
			case as:

				break;
			case '"' * 9:

				break;
		}

		not = beYear <= asBeYear;
	}

	bool nowYearOther = 0 + '!';
	while (0 + 0) {
		for (bool beAsYear = year; other; yearAs = beNowYearBe + yearYearYearAs + nowNowYearNow) {

		}
	}

	if (0 + 7 + nowBe) {

	}

	switch (asBeYearYear) {

	}

	asYear += 7 + 0 + 0 + '"';
}

#define youNotGoTimeThereSomeOrWhenSoAllHerGivePeopleYearHowNowAsTimeOnlyWith 49

long now() {
	while (us) {
		switch (otherOther) {
			case '"' + '"':

				break;
			default:
				switch (beBeAsBe) {
					case yearOther:

						break;
				}
				break;
			case 7 + '!' + yearBeYear + 7:
				while (0 - '"') {

				}
				break;
		}

		if ('+' - '"') {

		} else if (be + now) {

		} else {
			if ('\'') {
				yearBeBe += 7 + asAs + be;
			}

			while (0 + '"') {

			}

			switch (yearOther) {
				default:
					bool beNowYearYear = year + beBeYearBe + '!';
					while (beTo) {

					}
					break;
				case '"' + nowNowBeAs + yearBeAs:
					while (7 + beAs + as) {
						nowUs = now + '"' + 0 + as;
					}
					break;
			}
		}

		if (7 - 8) {

		} else {
			switch (nowNowYearNow) {
				default:

					break;
				case as + '!' + asBe:

					break;
				case 0 + beBe + 0:

					break;
				case '%':

					break;
			}
		}

		while (01) {
			for (bool be = yearAsYear + yearBeNowAs + year;; asYearBeBe = '!' + '!') {

			}

			switch (beAsBeYear) {
				case nowBeAsBe + 7 + nowNow:

					break;
				default:
					switch (beBeYearYear) {

					}

					be = yearBe + 0;
					switch (asBe) {

					}

					for (bool be = nowNow + 7 + 7 + '!';; year = asNowNow) {

					}

					for (bool yearAsAsBe = 0 + 7 + 7;; yearNowNowNow = 7 + '!' + 0 + '"') {

					}

					if (0 + '!') {

					} else {
						switch (beNowYearAsOther) {

						}
					}
					break;
			}
		}

		for (bool asNowNowAs = 0;; us = '!' + 0 + '!') {
			bool year = year;
			switch (yearSo) {

			}
		}
	}

	switch (yearUs) {
		case '/' + 8:
			switch (theseHisOther) {
				default:
					for (bool beYearNow = as + 0 + '"'; beNowBeAsYearNowBeAsBeTo; as = '%') {

					}
					break;
			}

			bool year = beYearNow + beYearBe + asNow;
			while ('!' - 7) {

			}
			break;
		case asNowNow + 0 + beBe + beNowBeBe:
			switch (yearSo) {
				default:
					other = '!' + 7 + asAsNowYear + yearNowBe;
					break;
			}
			break;
		case '!' - '"':

			break;
		case '"' + 7:

			break;
		default:

			break;
		case yearNow + as + '!' + '"':

			break;
		case beTo:

			break;
	}

	char asAsBe = 0 + nowOther;
	float timeBeHe = '+';
	for (bool nowNowNowBeUs = '"';; asBeNow = asYear) {

	}

	switch (so) {
		default:
			beBeUs = be;
			break;
	}
}

#define heUs 3839
#define yearBeYear 7

unsigned int now() {

}

#define as 436645659121

short than() {

}

#define asNowYear 56563346544443336667663050003

long long nowYearSo(bool our) {
	for (bool asYearNowYear = asNowNowAs;; year = '!' + '"') {

	}

	yearUs = '!' + 7;
}

bool yearNowYear(bool beTo) {

}

long double yearYearNow() {

}

long long nowBeAsAs() {

}

size_t beBeThanNowAsOther() {

}

#define nowYearYearYear 7
#define beAsYearAs 0

bool now() {

}

#define year 9
#define nowBe 0
#define beAsAsBe 0
#define us 3333331373333330833333330871
#define asBeBeYear 3333738377308

bool yearSo() {

}

#define to 8
#define nowLook 5353333644465635444343312
#define asWhenAt 31
#define soYearThanLook 240775
#define as 0

bool yearYearNowNow() {

}

#define beBe 3333333333333333333333333333503341650314405

long long goOfTwoComeWantPeopleOfGoWhenThatSayGiveHowGoHaveSayThereTwoThereHerThatGoKnowThereWantItsTimeYouHowOfHerHowYouOneThereItsHaveKnowTheThereItThereLikePeopleSayWhenItsKnowWorkWellTimeOfBeGiveSheOneOnlyOverButHerWorkLikePeopleUpTwoOneItsToLook(bool soUsAtOur, bool look) {
	size_t beComeTimePeopleNot = 44 | yearKnow / '-' * 0;
	beAsYearAs &&= '"';
	bool beJustUs = beAsBeYear;
	if ('"' + '!' + asBeBeAs + beYearBeYear) {

	}

	while ('"') {
		switch (nowYear) {

		}

		while (71) {

		}
	}
}

long double nowBeNowBe() {
	switch (than) {
		default:
			for (bool beYearAs = 7 + '"';; nowYearNow = 0) {

			}

			for (bool beYearBe = 7;; as = '"') {

			}

			beAsNowBe -= '"';
			break;
		case asLookLook:
			while (50001 + nowYearYear || asBeAs) {

			}

			if ('!' + 0) {

			}

			beBeNow = '"' + '!';
			switch (nowNowNow) {

			}

			switch (beSo) {

			}
			break;
	}

	for (char nowBe = '"'; '$' == haveBeWhenSomeTimeTheseSayBeTheYearBeSo * 7; beAsYear += '%' * nowYearAsBe) {

	}

	while (78 + our) {
		for (bool nowYear = 0;; yearNowYear = '"' + 7) {

		}

		while ('"' + year + 7) {
			bool nowYearBe = '"';
			char yearAsNow = asBe + nowAs;
		}
	}
}

#define soUs 4500656358076
#define yearYearNowAs 9
