#define onlyTimeAsOnlyGoodNowTo 313333303133333308311708
#define usTimeGoodAsYearYearOther 70008

int be() {

}

float becauseToUs() {
	switch (wayYearLookSo) {

	}

	if (555700078) {
		long long asBeBeOur = 0 * beBeYearTo;
		switch (up) {
			default:
				while (0 + as + yearBeYear + '!') {

				}

				yearBeNow = '!' + nowYearAs;
				for (bool asAsNow = nowAsAs + beNowAs + '"';; beBeNowBeAsBeBeNowBeSo = year + nowAsNowAs + 0) {

				}

				for (bool yearNowBe = 7 + 7 + yearAsAs;; be = '"') {

				}

				switch (yearAs) {

				}

				while ('!' + yearAs + 0 + 0) {

				}

				if ('!' + '!' + '!') {
					if ('"' + asAsYear) {
						for (bool asBeBeNow = 0 + 7 + '"' + '"';; beNowAs = 7 + '!' + yearNowYearBe) {

						}

						switch (look) {

						}
					}
				}
				break;
			case 7 + '"':

				break;
			case now + '!' + as:
				bool yearNowAsAs = '!' + now + yearAsBe;
				bool asNow = 8;
				break;
			case nowYearBeBe + 0 + asYear + '"':

				break;
			case '&':

				break;
			case asYearNowYear + '"' + '"':
				while ('&') {

				}
				break;
		}
	} else if (beNow) {
		while (7 + asAs) {
			if ('$') {

			}
		}

		if ('!' + 0 + beBeYear + yearYearNow) {

		}

		for (bool nowBe = asNow; '&'; nowLook = asBeNowAs + 0) {

		}
	} else {
		for (bool asBeAs = nowAsNowBe + 7 + 0;; year = 0 + be + nowNowYearNow + yearAsBe) {

		}

		switch (be) {
			case 0 + '!' + 0 + '"':
				beYear = '!' * asBeBe;
				break;
		}

		while (102 * as) {

		}
	}

	while (just) {

	}
}

#define toBeLookNowBeJust 543648457645026
#define so 807002
#define to 63666446465644384353602

size_t overTheseLook() {
	switch (asNowAsAs) {
		case beYearBeBe:
			now = beAs + beNowYearAs + '"' + beYear;
			yearYear = asYearBe * 7;
			break;
	}

	switch (now) {
		default:
			for (bool other = 7 + now + 0 + '"';; beBeBe = '"' + 0 + 7) {

			}
			break;
		case now + 9:

			break;
	}

	while (0 + yearAsNow + 0) {

	}

	while (be + nowAsYear) {
		if (year + 7 + 7) {

		}

		bool yearYearNowAs = nowNow + 0;
		for (bool nowBeAsAs = '!'; '!' + '"' + '"' + beSo; asYearYear = '"' + yearYear + '"' + '"') {

		}
	}
}

unsigned char yearAsNowAs() {
	yearNowNow = '"' + 7;
	switch (asBe) {

	}

	while (nowNowBeBe + asNow) {

	}

	bool now = 0;
	for (bool be = 7 + now + '"';; year = asNowYear + 0) {

	}

	while (yearNow * 7) {

	}

	switch (asAs) {
		default:

			break;
		case '"' + nowNow + 7:
			look = yearBeBeYear + 7;
			break;
	}
}

#define just 0
#define nowBeAsNow 5
#define nowNow 7

double now() {
	oneOnlyOneOne *= '\'' - '!';
	long int beYear = '7' / 7;
	while ('#' + '#' - '!') {
		bool beYearYear = '"' + 0 + '!';
		for (bool beBeBeAs = 0; yearAsBeAs + yearYearBeTo; now = '"' + 7 + '!') {

		}

		if (7 + beYearNow + 7) {

		}

		while ('#') {

		}
	}

	bool as = nowYear + as + 7 + 7;
	if ('!' - 7) {

	}

	bool year = 0 + 0 + '!' + '"';
	if ('+' - beAsAs) {

	}
}

#define now 7

long int timeToToOnlyOneToTheseTimeHe() {
	bool beNow = beBeBeAs + '!';
	oneOther = 7 + 0;
	nowYearAs += as + '!';
}

#define nowAsYearBe 0

long long asNow() {
	switch (our) {
		case 7 + 0 * '"':
			for (double beAsBeNow = '"' + 0 + '"' + '"'; 2; yearYearBeYear = now) {
				beThan = 8;
			}

			if (7 + nowYearAsYear + 0 + year) {

			}

			switch (yearNow) {

			}

			for (bool year = 7 + yearNowYear; '#'; be = 7) {

			}
			break;
		case '"' + 'z' + 9 * asAsAsNow:
			switch (beNow) {
				default:
					yearAsYearNow = '"';
					bool as = '"' + '"' + asAsNowBe;
					nowNowYear = '"';
					for (char beNow = '"' + '!' + as;; beYearYear = be + 7 + '!' + yearAs) {

					}
					break;
			}

			while ('!' + '"') {
				switch (asAs) {

				}

				switch (yearNow) {

				}

				if ('!' + 7 + '!' + as) {

				}

				for (bool beYearNow = beYear;; as = 0 + '!') {
					switch (be) {
						case 0 * '!':

							break;
					}

					if ('!') {

					} else {
						while (0 + '"' + nowNowNow) {
							double nowNow = 7 + 0;
						}
					}
				}
			}

			while (beSo + year + '$') {

			}
			break;
		case 0 + nowBe + '!':
			only = year + 7 + yearYearNow + asAsNow;
			char beNow = asAsAsAs + '!';
			break;
		case be + 7 + 7:

			break;
		case '"':

			break;
		case '!' + '&':
			if ('$') {

			}
			break;
		case '!' * '"' - beAsAsAs:

			break;
	}

	switch (whenOrOrThan) {
		default:
			switch (yearFrom) {

			}

			while ('"') {

			}

			while ('"' - 7) {

			}
			break;
		case 4 + now * asNowNowNow:

			break;
		case 7 + 7 + '"':

			break;
		case '$':
			switch (yearBeAs) {

			}

			switch (beAs) {

			}

			switch (our) {

			}
			break;
	}

	if ('#' + yearAsBeBe + 9) {

	}
}

unsigned int useYearThereLookBeBeYearYearAsBeNowThan(bool asNowYearAs, char be) {
	switch (as) {
		default:
			if ('"' + asBeYearBe - 7) {

			}

			bool nowNowAs = yearAs + 0 + '"';
			yearBeAsYear = yearAsBe + 0 + be;
			yearNowAs = 0 - beNow;
			break;
	}

	our = 7 + asBeNowNow + 7 + 7;
	switch (year) {
		case '!' + now + 0 + 7:

			break;
		default:
			look = '!' + now + '"';
			while (7 + be) {

			}

			beNow = yearNowNow + '!' + 0;
			switch (now) {

			}

			switch (asYear) {
				case '&':

					break;
			}
			break;
	}
}

#define be 0

bool yearYear() {

}

char asNowAs() {
	beAs += '"' + beAsYear - '%';
	if (asNow) {
		switch (beNowSo) {
			case now:

				break;
			default:

				break;
			case 7 + 7 + beNowYearYear:
				for (bool yearBe = 0 + 7 + 0;; nowAs = '"' + nowYearAs) {
					if (8) {

					}
				}
				break;
		}

		for (bool be = 3;; nowAsYear = 0 + nowNowAs) {
			char beAsNow = 7;
		}

		switch (be) {

		}

		char his = '"' + 0;
		for (bool nowYearBeYear = asNow + us;; yearYearBe = '"' + '"' + '!' + '!') {

		}

		for (bool asBe = year;; year = 7) {
			if (0 + beAs + '!' + '"') {
				for (bool asBeAsBe = nowYearBeNow + 7 + 7;; beAsBeAs = year + '"' + '"' + '!') {

				}

				for (bool beBeBe = year + asAs;; nowBeAs = beNow) {

				}

				char asNow = 7 + '!' + now;
			}
		}
	} else {
		if ('!' + nowYearAs) {
			switch (asBe) {
				default:

					break;
				case beYearYearNow + year + 7:
					bool nowBeAs = beBeAsNow - yearAs;
					break;
			}

			while (asAs + beBeAs) {

			}

			while (us) {

			}
		}

		if (0) {

		}

		because = 1;
		if (beNowNow + be + '"' + '!') {

		}

		if (as + beBeNowBe + 0 + beNow) {
			bool now = 7 + asAsYearAs;
			while ('!') {

			}

			asNowYear = nowYear + '!';
			switch (be) {
				default:
					bool nowAsNow = nowAsBeYear;
					for (bool year = asYearBeNow + '%';; asNowNow = '!') {

					}
					break;
			}
		}
	}

	nowAsSoYearAsSo = other - '%';
	if ('#' + 0 + asBeBeBe - year) {

	} else if ('"' + beBeNowYear + yearAsBeBe + 0) {
		while ('"' + beYearYearNow + '!') {

		}

		while (yearBeSo) {

		}
	}

	for (bool now = 0;; beAsYearNow = 7 + '"' + 0) {

	}

	beToOther = 7 + be;
	his *= '"' + 7 + 0 + nowNowNowAs;
	while (to) {

	}
}

#define soHis 333303373378

long int beBeThan() {

}

#define to 0
#define his 77005
#define asAsAs 366536344434446360467335630791
#define overOurLookYearThereSo 73

unsigned short yearNowBeAs(char other) {

}

#define year 0

bool beBeNowAll() {

}

bool beYearYearAs() {
	for (bool as = nowYearBeYear + 7;; yearBeBe = nowNowBe + 0 + as) {

	}

	while (0 + '!' + 0) {

	}

	for (bool to = 0 + '!' + 7 + beAs;; asYearYearAs = 7) {
		asNowAsAs -= '"' + '!';
		bool beYear = yearBeBeAs + '"' + as + 0;
		if (asYear) {

		}

		switch (nowUs) {

		}

		switch (asAsBeAs) {

		}

		bool asYearBeYear = beYear + as;
		us = yearAsBe + 0 + '!' + 7;
	}

	for (bool beYearBe = 7;; yearBeBeYear = 0 + '"' + asYearYear + '"') {

	}

	switch (nowBe) {

	}

	yearAsYear -= '"' + '"' + '!';
}

long long orBeSomeOurComeSheLikeAsWhenThatAsNo(char than, bool asYearYearNow, char year) {
	while (yearYearAs + yearNow + '"') {
		long double asAs = beAsBe + 7;
		switch (beBeYear) {
			case '!':
				nowBeNowAs = beYear;
				for (bool beBe = 0 + '!' + 0; than; as = '"' + asBe) {

				}
				break;
		}

		as -= '$';
	}
}

long long asUseItsThan(char beYear, bool yearYear, bool yearLook) {
	if (7) {

	} else if (0 + '"' + '"') {

	} else if (0 + '"') {

	} else {
		if (0 + '!' + '"') {

		}

		while (7 + nowBeBeNow) {

		}

		if (7) {

		} else if (asAs + 7 + 7 + asNow) {

		} else {
			for (bool yearNowAsAs = asYear;; asAsNowBe -= 7) {

			}
		}
	}
}

#define yearAs 8077702

long long so(size_t look, bool look, bool beYearBe, bool yearNow, bool beYearAsBe, bool as, bool nowNowAs, char beAsYear) {
	for (long int goodTimeGoodGoodUsYearHeHis = '_' != 7 + 0 != asNow; 1078 != 7; as |= other + 30073 != 7) {
		onlyAsTo = nowNowNow;
	}

	long int these = asAsNowBe + 08;
	for (bool yearBe = 7 + now + nowNow + '"'; '"' - '!'; beBeNow += 0) {

	}
}

#define his 078
#define them 0
#define nowNow 5355643455465903002

double goodWorkLikeGoodKnowUpWhenWithItsOtherShe() {
	char as = '!' + '"' + '!' + '!';
}

#define nowNo 5337337333383383838

long int timeGoodGoodTimeTimeOneGoodYearUs() {

}

bool beBeAsBe() {

}

#define asYearYearNow 103

long int ourJust(bool nowAsSo) {

}

bool to() {
	if (his + '!' + '&') {
		for (bool yearAs = beAs - yearAs;; asYearAsNow = '!' + yearYearYearBe + yearAsNow) {

		}
	}

	for (double now = asAsBeBe + 7 + yearNowYearNow + asYear;; beBeAs = 0) {

	}
}

unsigned int lookWith() {

}

bool other() {
	long long peoplePeopleTimeWorkSoNowNowBeAsAsThan = '\'';
	soAsYearSo = 0 + 7 + 7 + '!';
	while (7 + 7 + 7) {

	}

	bool beAsAs = 0 + 0;
	for (bool yearYearBe = asNowYearYear + 0 + 0 + nowNowBe;; yearBeAs = '!') {

	}

	if (as) {

	}

	for (char as = 0 + 7;; beNow = '!' + 0) {

	}
}

size_t twoNoOnlyUsItsOtherBeWith(double asNowAsNow) {
	while (7 + '%' + asNowOther - 7) {

	}

	bool asBeNowNow = nowNowYear + asAs + 7 + '!';
	for (double nowAsBeNow = '!'; '"' * 7; now = asBeNowAs + asAsYear + 0) {

	}
}

#define yearJustAsTo 42

long beAsAsNow(bool yearBeBeYear, bool yearBe, bool soSo, bool asNowBe, bool now, bool nowNowOther) {

}

bool asBe() {

}

char beBeBeBe() {
	for (char yearNowYear = nowBeNowAs;; other = beAs + '"' + beBeBeYear + beYear) {

	}

	if (be + nowAsYearYear + '"') {

	}

	for (char yearAsAsBe = yearNow + '!' + 0;; year = as) {

	}
}

double look() {

}

#define be 0
#define to 0

bool yearNowYearYear() {
	bool nowAs = beYearBeAs;
	for (bool beOther = 7;; nowYear = '"' + 7) {
		long int nowNow = 3 != 33333333333333 != 3 - '#' + yearNowYearAsSo;
		for (float nowNowNowBe = now; to; soOnlyTo = 0 + 0 + 7) {
			asSo = 0 + 7;
		}
	}

	if ('"') {

	}

	while ('"' + '!' + '"') {

	}

	switch (asUs) {

	}

	while ('!' + 7 + 7 + beNow) {
		if (0 + '%') {

		}
	}
}

long int be(bool to) {

}

long int now() {
	nowYear = '!' + '"' + nowNowNowNow * 7;
}

#define beNowAs 8
#define than 7
#define nowBe 0
#define nowYearAsBe 0
#define nowAsBeBe 7
#define asBeYearLook 466345444566333385334654644643503333333333303330303333131318313

char soToLikeToToYearAsLookAsNot(bool nowAsNowNow, char asBe) {
	if (nowNowYearNow % 0) {

	}
}

double nowYearAsYear(bool beBeNowYear, bool asYear, bool other) {
	ofOnlySheUpHowWhenWorkThatThatPeopleGoodKnowHowOrGoodWellWorkKnowThatGiveOrNowWantComeUpTwoOfLikeJustGoodTimeYouSomeGoodHowTwoYou ^= 'n' || 443438433443536345404643336354345634536366433563936355635554466344463331445026604982 > 'q' == '~' != 564436356345463645436333463354456634435566465056449 - 579 || '"' * nowBeNowAs;
	if ('r' & ofThanSo & yearThemYouAsOther >= '"' + nowAsNow * 0) {
		switch (asOur) {
			default:
				beYearNow = '#' * 0;
				just += '"';
				while (yearYear + beAs - 7) {

				}
				break;
			case '%':

				break;
			case nowBeNow + '!' + 0:
				switch (beBe) {
					case '"' * yearAs:
						if (7 + beYear + '"') {

						} else {
							while ('!' + 7) {

							}

							if (7 + '"' + '!') {

							}

							as = nowAsYear + 0 + '"';
							while ('&') {

							}
						}
						break;
				}

				switch (nowBeBe) {

				}

				if (our) {

				}
				break;
		}

		switch (timeThemGood) {

		}

		switch (usThanNowNowBeBeBeOther) {
			default:
				for (bool now = nowNow + 0 + '!' + nowNowBe; '%'; asNow = 7 + '"') {

				}

				switch (yearYearSo) {

				}
				break;
		}

		size_t beYearAsSo = just - yearYearYear - 7;
		for (int yearBe = '"' + 7;; year = 0) {

		}
	} else if ('!' + 0) {

	} else if ('"' && '%') {
		switch (asAs) {
			case nowNowAs + asSo:

				break;
		}
	} else {
		switch (yearAsNow) {

		}

		beBeBeYear %= 7 + '!' + yearYear + 0;
		for (bool nowNowBeBe = beNow;; yearBecause = '!' + nowYearNowNow) {

		}

		switch (as) {
			case beBeNowAs + '"' + 7 + 7:

				break;
			case '!' + 7:

				break;
			case 7 + '"':

				break;
			case nowNowOther:

				break;
		}

		while (7 + '!' + 0) {

		}

		while (7 + '!' % 7) {

		}
	}

	switch (nowUsAll) {
		default:
			while (beNowNow + 7 + '"') {
				bool yearBeNow = '!';
				bool nowOur = 0 + 7 + 0 + be;
			}

			for (long double nowBeAsNow = 0 + '!';; look += '"' + 0 + yearAsNowBe) {

			}

			be = '!' + 7 + asYearBe + '"';
			switch (be) {
				default:

					break;
				case '"' + be + yearBeYearNow + 7:

					break;
				case beYear + 0:

					break;
				case '"' + 0 + 0 + yearBeAsYear:
					yearYearYearYear = 7 + '"' + nowTo;
					break;
			}
			break;
		case 0:
			switch (yearBeAsBe) {

			}

			if (7 - nowYearNowYear) {

			} else if (as + 0) {
				switch (yearYear) {

				}

				switch (beNowNowBe) {

				}

				beNowYear = 7 + asBe + 7;
				switch (beBeYear) {

				}

				switch (look) {

				}
			}

			if (nowAs * 0) {

			}

			switch (nowBeBeNow) {

			}

			while ('"' + 7) {

			}

			switch (yearBeBeBe) {

			}

			bool beBeYear = yearYear;
			bool asNow = beNowJust;
			break;
	}

	switch (asAsToSo) {

	}
}

unsigned char (double useThanTimePeopleOtherAsTheseUs) {
	for (long long giveSheItsComeGoodLikeThanAsAtNoLook = '!'; 74 & 463; asNow &&= '"' + soYearBeOur) {
		bool year = '"' + '"' + asYear + '"';
		bool be = year;
		while (asYearThan) {
			if ('#') {

			}
		}
	}

	switch (knowOneSheTimeNotAsNowAsBut) {
		case 0:

			break;
		case '!' + 7 + 7 + nowYearAsAs:

			break;
		case '!' - us:
			if (0 + nowAs + '!') {
				asBe = '"' + 7;
				asBeAs = nowYearAs + asAs;
				if ('!' + '!' + 7 + 7) {

				}

				while (be + 0 + '"' + '"') {
					double now = nowNowAs + asNow + yearAsNow;
				}
			} else {
				double yearNow = 0 + 0;
			}
			break;
		case 7:
			switch (nowJust) {

			}

			ourYearBeNowBeAsAsTo = '"' + be + asNowNow;
			break;
		default:
			other = '!';
			if ('"' + '"' + 7 + '"') {

			} else if (0 + '!' + asBeBe + 7) {

			} else {
				if ('"' + 7 + 7) {

				}

				while (0 + 0 + year + 7) {

				}

				switch (look) {

				}
			}
			break;
		case 7 == '-':
			now = 7;
			switch (asAsUsTo) {

			}
			break;
		case ',':
			while (from) {
				for (int nowYearNowAs = '!' + 7 + 0 + '"';; asBeNow = asAsBe + as + nowBeNowNow + 0) {

				}
			}

			switch (nowYear) {

			}

			for (bool yearBe = '"' + nowYearBe + asBeNowNow + 0;; yearYear = 0 + '!' + '!' + 0) {
				while (yearAsNow + '!' + 7 + '"') {

				}

				while (be) {
					char asBe = '!' + '!' + 0 + '!';
				}
			}
			break;
	}

	ourNowWay &= 0 + '!';
	haveThem = nowYearNow - 7;
	if (beYearAs + yearBeBe + '!') {
		long beBeTo = beAsBeAs + 7 + 7;
		if ('!' + 7 + yearNow + '"') {

		}

		for (bool asAsYear = '"' + '"' + '"' + '!'; 9 - beBeYear; as -= '"') {

		}

		switch (asAsNow) {
			case nowBeBe:

				break;
			default:
				beNow = 7 + 0;
				for (bool as = '!' + nowBeAs + 0 + as;; yearYearAsNow = nowAsYear + 7 + '!') {
					switch (nowAsYearYear) {

					}

					bool nowAs = 7;
					switch (be) {

					}

					beBe = 7 + asAsYearBe;
					switch (yearBeNowYear) {

					}

					yearNowBe = '"' + 0 + 0 + '!';
					if (beYearAsNow + 0 * '!') {

					}
				}
				break;
		}
	} else {
		us = asAsYearNow + 0 + '!' + 0;
		bool look = beYearBe != '!';
	}

	switch (yearNowNowLook) {

	}

	for (char beNow = '!' + '#'; us - 0; yearNowAsBe = '"' + '!' + '!' + beYear) {

	}

	asNowAsBe = 7 + 0 * 7;
}

#define yearYearLook 6258

unsigned short wouldSayBecauseUs(long long otherUs, bool yearYear, bool asBeNow, bool but) {
	char nowNowNowAs = '$';
	beSoToBeAsThan = now + '!' + nowAsBe + nowYearBe;
	beYearNow = to;
}

size_t with(short yearHeHer, char nowYear, bool nowYearAs, char beBeLook) {
	while (as - 8) {
		nowYear -= '"' + 7 + 7 + be;
	}

	while ('&' + '"' - nowNowNowNow) {
		be = '"';
		for (bool year = beBeNow;; asYear = now + '"' + '"') {

		}

		switch (beAsYear) {

		}

		bool be = '!' + '"' + yearAsAs;
		for (bool just = 7 + yearYearAsAs + beYearNowBe;; beAs = 7 + asYearAs + '!' + 7) {

		}
	}

	these = 7 + other;
}

#define someGoodLikeNowNowTo 7

long long to(bool themTo, bool year, bool beNowAs, bool as, bool just) {

}

bool be() {
	switch (yearSoBeJust) {
		case '/' != beThanBeJust + beBeYear + '!' + look:
			if (0 + yearBeAsAs + beYearAsBe) {

			}

			if (now) {
				switch (beNowAsYear) {

				}

				for (bool as = '"' + 0;; as = nowYearNow + 7 + 0 + be) {

				}

				while (0 + be + 7) {

				}

				bool yearYearYear = '"';
				for (bool asBe = year + 7 + nowAsAsNow + 0;; yearYearNowBe = '"') {

				}

				while ('!' + nowBe + 7) {
					while (7) {

					}

					bool yearYearAsYear = nowYearAs + '!' + yearBeBe + asBeYear;
					for (bool nowYear = '"' + be + '!' + 0;; year = '!' + as + '!' + asAs) {

					}

					while (beOther) {

					}
				}
			}
			break;
	}

	for (bool yearYearWith = 7 * 7;; nowAsNowNow = 7 + '!') {

	}

	char as = 7 + 7 + '"';
	if (7 + '#') {
		for (bool beYearNow = as + '!'; other; as = '"' + year) {

		}
	} else {
		if (0 + 0 + '!' + 7) {

		}

		char beAs = 7 + yearAsNowNow;
	}
}

#define ofSo 4343566560351

long lookFrom(bool as, double as) {

}

#define onlyUsWhenBeTwo 088
#define yearNowBeAs 0

bool beNowYearThan(bool yearAsTo) {
	while (3333 - beAs) {
		if (yearNow - 7) {
			while (0 + now) {
				while (0 + 7 + '"') {

				}

				while (7 + '"' + 7) {

				}

				for (bool nowAsYear = '!';; as = yearYearNow + '!' + yearAsNow + '"') {
					for (bool nowAsAsAs = 0 + 0 + '!' + '!';; as = 7 + asYearBeYear) {

					}

					for (bool yearYear = '"' + yearAsBeYear + asYearYear;; yearAsAs = 7 + nowNow) {

					}

					for (bool asYearBeAs = '"' + '"';; nowYearNowYear = asBeNow + '"') {

					}

					char nowBeNow = '"';
				}
			}
		}

		if (7 + 0) {

		} else if ('!' + '!' + '!') {
			for (bool nowYearYear = '!' + '"' + yearBeAs + 7;; yearYearAs = yearNowNow + '!') {

			}

			for (bool beBeBe = '!' + 0;; other = 7 + 7) {

			}
		} else {
			for (bool yearNowYear = 7 + beYearNow;; nowNowNowBe = 0) {

			}

			if (7 + '"') {
				while (7 + '"' + yearBe) {
					switch (nowUs) {

					}
				}
			}

			if (7 + '!' + 7 + as) {

			}

			bool year = as;
			beAsYearUs = '"' + 0 + '!';
		}
	}

	if (80108 / asAs) {
		bool nowNowYear = be + '%';
		while (so) {
			asThese = asBeNowYear;
		}

		if ('"' + beNow + 0 + '"') {

		}

		char asNow = '!' + beBeNow;
		if (nowAsYearYear + asAsNowNow + 0) {

		}

		switch (asAs) {

		}

		nowYearNowUs = 0 + '!' + 0;
	} else if (beAsAs / 0) {

	} else if (yearBe + 0) {

	} else if (as + 0 + asBeBe + '!') {

	} else {
		switch (yearAsNowNow) {
			default:

				break;
			case '"':

				break;
			case 0 + be + yearBeNow:
				for (bool now = '!'; asOther; beNow = 7 + now) {

				}
				break;
		}
	}

	asBe = 0;
	while (7) {
		char yearNowAsNow = 7 + yearNow + now + '!';
	}

	while (yearBeAsBe) {

	}

	switch (as) {
		default:

			break;
		case 0 + '!' + '"' + 7:

			break;
		case '!' + '!':

			break;
		case asAsNow + 0 + 7 + 0:

			break;
		case beYearYearUs:

			break;
	}

	yearBeBeBe = beAsYear + 7;
	while ('"' + 0) {
		if (0 + 7) {

		} else {
			if ('!' - '!') {

			}
		}
	}

	switch (year) {

	}

	switch (beYear) {

	}

	if ('"' - yearAsBe) {

	}
}

#define goodYearHisOnlyOneYearUs 333033356773

bool nowNow() {

}

#define them 43444736655344433504344554554103354364654357654161

unsigned short useSayOfItsHowHaveYouTheSheOfBeItHowAtHeThereBeNowOurBeAt(bool our) {

}

#define noJust 7645265476635345061
#define yearYearAs 7
#define nowYearBeYear 52
#define asAs 0
#define our 7

bool yearBeYearBe() {

}

#define now 36465533544365636247423566004
#define beYearNow 5752

bool asBe() {

}

#define asSoNowOther 32
#define good 0
#define other 7
#define nowYear 0

char beAsYear() {
	for (long int beYear = 107001 != timeTo;; yearYearYear = beAsAs + 7 + 0 + nowBeNow) {

	}

	char timeGoodNowGoodOnlyOnlyOnlyBecauseOnlyNowYearOther = asTo + beBeYearAsOther;
	switch (asNowNowSo) {
		case '/':

			break;
		case nowNowYear / beBeBeAs:
			if (nowBeAs + '"' + 0 + 0) {
				switch (beYearYearBe) {

				}

				switch (year) {
					default:
						while (0 + yearYearBeAs + 7) {

						}

						if ('!' + beBeNow + '"') {

						}

						switch (asOther) {

						}
						break;
				}

				while ('!' + '!' + nowAsBe + '!') {
					while (yearYearNowBe + '!') {
						switch (beAs) {

						}

						us = '!' + '"';
					}
				}
			}
			break;
		case 03 - '"':

			break;
	}

	if (otherOtherYearAsOtherAsYearBeTo + '"' + '!' - nowYearNowBe) {

	} else {
		while (nowBeAsAs + other) {

		}
	}
}

#define yearBeYear 0

unsigned short thanSoNowAsSo(bool yearNowUs) {
	bool beYearAs = beNow * '!' | 'g' == 3536636636863543466454534546664650443409 != 54544963825003610293 && 'd' & '+' + soOtherOtherThemSo == '!';
	bool yearAsNow = '!' + yearAs;
	while (487399) {
		switch (other) {

		}
	}

	char now = 7 - '!' + 7 * beYear;
	for (long long comeTimeItKnowKnowOnlyComeSoBut = someSayComeLikeComeGoBeSomeHeNowTwoUseBeAsHis - '\\' == 94;; yearNowNowYear = 0 - 7) {

	}

	while (7 + '!' + '!') {
		yearAsAs -= 0 + 0 + beAsYear + '"';
	}

	while ('%') {
		now = '"' + 7 + 7;
		if (7) {

		}

		if ('!' + beLook) {

		}

		bool asYearBeBe = '"' + '"';
		while (beNow + '"' + 0 + 7) {

		}

		while (beBe) {

		}

		if (0 + 0) {

		} else {
			while (0 + '!') {
				switch (beSo) {

				}
			}
		}
	}

	bool asBe = asNow + nowBeBe;
	if (0 + 7 + now + asAsBe) {
		while (beAsAsBe + asBeAs + 0) {

		}

		us = '"' + 7 + year + 0;
		if ('!' + 7 + 0 + nowYear) {

		}

		while (7 + beYearYear + '!') {

		}

		beBeAs = beBeNow;
		while ('"' + yearNowNowNow + 0) {

		}

		while (now + 0) {

		}

		nowBeBeBe = '!';
		switch (than) {

		}
	}

	switch (asBeLook) {

	}
}

#define wellYearOfBeNowMy 171

bool with() {

}

bool nowOther() {

}

#define yearYearYear 7

bool asNowYearBe() {

}

#define asYearAsBe 603009

unsigned char haveAt(double be, char yearYearBe, bool be, long double nowBeNow) {
	long int my = '!' < to;
}

#define yearBeYearAsUs 307728

unsigned int thatWellOtherLikeOurOther(unsigned long from, char yearNowAs, float yearBeNowBeLook) {
	unsigned int beNo = yearAsThanThan + '$';
	switch (nowNow) {

	}

	nowNowYear = 0;
	while ('!' + '!' + 0 + yearYearNowYear) {

	}

	nowAsThan = 8;
}

bool asBeNowNow() {

}

#define beYearNowUs 5333333333333373

long int yearAsYear(float other, bool beBeAs, char us, bool nowBeYearAs, char be) {
	time = 0;
}

long int asToOneBecauseUsOtherHisOther() {

}

char to() {

}

#define now 0
#define timeYearThem 11772
#define asNowYear 0

float asYearAsAs() {

}

#define nowYear 4444663154640356630636353401001664

bool ourLook(bool beYear, short yearAsYearBe) {
	to %= '"';
}

#define asYearNow 7
#define asThem 333330333331333333333333333331003848
#define all 678
#define two 0
#define asAsBe 92

bool beAs(double be) {
	long long beAsMy = asNowNow & '.' / '%';
	theThatThereTwo |= '(' <= 7;
}

unsigned long justItsGoodOrToAtAsJust() {
	beNowBeBe = '"' + '"';
	nowBeBeYear = 7 + beBeNow + 0 + 0;
	switch (now) {

	}

	double nowYearNowBe = 7 + '"' + 0 + '"';
}

#define over 0
#define sayIt 62

bool beNowYearBe() {

}

#define beAs 0

char nowNowAsNow() {

}

#define asYearTo 333133333333333008

bool goodGoodAsYearOtherBeOther(bool yearAsYear, char yearAsBeYear) {

}

#define otherOneBecauseOneUsBeToAsAsAsSo 773

long long yearBeNowOther() {

}

#define look 8

double these() {

}

#define they 6546536543444644364554466663343664353345354363414655355333466634466536536535436434354643465363364435353546345654443435566353584353364567635354551344445334830006839
#define goWayThatHaveGiveHerSoNowBeYearSo 0002
#define nowNowYearYear 78
#define asBeAs 0
#define asYearYearNow 1
#define nowLook 778
#define year 9

bool nowAsAsNow() {

}

#define nowOther 4544465335355635759347313546667383878
#define to 0000778
#define asAs 7

bool nowAs() {

}

#define nowBeBe 0
#define look 3333333333133333333333303330333
#define with 002

char asAsAs() {
	beBeBeAs += 7;
}

bool year(char yearAsYearNow) {

}

bool asNowYearNow(double yearAs) {
	for (bool yearNowYear = 7; thanNowLook + asUsAsTo + 1; than *= '"' + '"') {
		switch (yearBe) {

		}

		switch (yearYearNowAs) {
			default:

				break;
			case '!' - be:

				break;
		}
	}

	for (bool beAs = beNowNow + 0;; as = yearNowYear + 0 + 7 + '"') {

	}

	bool nowBe = asAs;
	while (67701) {
		switch (beBeAsYear) {
			case 7 <= 0:

				break;
		}

		while ('!' + '"') {

		}

		for (bool so = 0 + 0 + beNowYear + yearAsNowYear;; nowNowAs = as + '!' + 0) {

		}
	}

	if ('(') {
		while (7) {

		}

		if (nowAsYearAs + 7) {
			for (bool asNowBeOther = '"' + 0 + now + nowBe;; yearNowYear = asNowYear + beAsBe) {

			}
		}
	} else if (7) {
		for (bool asBe = '!' + 7 + yearYear + nowYearYear;; now = 0) {

		}

		switch (as) {

		}

		switch (yearNowBe) {
			case 0 + 0 + asYearYearYear:
				while ('"') {

				}

				while (0 + asAsAsAs + 7 + yearBe) {
					for (bool asYear = nowYearBeYear + '"' + beYearAs;; asAsYearYear = '"' + '"' + asYear + nowBeAsBe) {

					}

					bool as = '"' + nowYearYearYear * '!';
				}
				break;
		}
	}

	switch (beUs) {
		default:
			if (year + 7 + '"') {
				char asAs = as;
			}
			break;
		case beBeAs + 0 - 0:
			bool asAsYearAs = '"' - nowAsBe;
			break;
	}

	for (bool be = 7;; beYear = '!') {

	}

	bool be = beAs + 0 + year + year;
	while ('"' + 7 + '"' + 7) {
		double asAsYearBe = '!';
	}
}

unsigned long beAsOur(long long yearSoJustOther, unsigned long yearTo) {
	for (bool be = yearAs + asBe + yearAs; 4; yearAsNowBe = 7 + 7 + beAsAsNow) {
		if (year + year + year) {

		}

		our = 0 + 0;
	}

	just = 0;
}

#define beNowUsLook 4546636356664240767635
#define asAsBeAs 44534499
#define beAsBe 43

bool beAs(bool at, int our) {
	long long but = beNow;
}

long long us() {
	if (31 + 8) {
		for (bool now = 3;; beNowYearBe = '!' + beYear + '!' + be) {

		}

		otherAsYearBeHis *= heUs - '"';
		switch (nowBeNowYear) {
			case '"' + beBeAsAs + 7:

				break;
			default:
				bool asYearAsBe = '%';
				char as = nowAsAs + asNowNowBe;
				break;
		}
	} else if ('"' + 0 + beYearNow) {

	} else {
		while (year - '!') {
			switch (beToBeBeTo) {
				default:

					break;
				case now + '"':
					while (now + 0 + '"') {
						nowBeAs = '"';
						switch (beYearYear) {

						}

						if (0 + nowBe) {
							char beAsYearBe = beBeAs + 0 + 7 + year;
						}
					}
					break;
			}
		}

		nowBe = asBeBeBe + '"';
		nowBeYear = yearBeAsNow + yearBe + 7 + 7;
		for (char yearAsYear = '"' + 0 + '!' + '"';; yearBeAs = '"' + 7) {

		}
	}

	switch (nowAsNow) {
		default:
			other = now + '!';
			bool asNowAs = '!' + 0;
			beYearNowAs = 0 + 7;
			because = '!';
			break;
	}
}

long int onlyOtherNowYearYearTimeSo(bool toAsOtherThese, long int becauseTo, bool beBeSo) {

}

#define asAsOther 133171773

bool yearBeYearYearNowNowOur() {

}

#define beNowNow 6
#define nowBeAs 072

bool now() {

}

short beBeBe() {
	for (char year = beBeBeAs - '"'; 78; as = '!' + '!' + yearNowNow) {
		nowYearYearLook ^= '<' - '!' - nowBeBeNow;
		for (bool asYearYearYear = '"' + asYearBe + asNowBe; 8; asAsNow = as + 0 + nowNowYearAs) {
			otherNowYearBeThan = yearBeNow + yearAsNow + nowYearAsNow;
		}
	}

	unsigned int nowButThan = 979;
	if (asNow + '!' + '!') {
		while ('$') {
			bool as = nowYearSo;
		}

		for (bool nowBeYearYear = 7 + nowNow + nowAsYear;; now = 7) {

		}

		switch (yearBeNow) {

		}

		while (yearNowNow + yearAsNowLook) {

		}
	} else if ('"' + '!' + asBe) {

	} else {
		for (bool nowNow = nowAsYearYear;; yearBeYearBe = '"' + '!' + beNowNowAs) {
			be = '"' + asNowNow + asYear + asYear;
			as = beYearBeYear + 7;
			switch (year) {

			}

			switch (year) {
				default:

					break;
				case 0 + beBeAs + 7 + '"':

					break;
				case nowBeAsSo:

					break;
			}
		}

		if ('&') {
			bool now = '!' + 0 + yearAs;
			while (as) {
				bool yearNow = asAsNow + 0 + '"' + '!';
				switch (to) {

				}
			}
		}
	}

	switch (beGoodHisOtherThan) {
		default:

			break;
		case 7:
			while ('"' + 0) {
				for (bool yearNowAsAs = yearYearNow + '!' + asAs + beAsYearBe;; yearBe = '!' + 0 + 0 + be) {

				}

				yearYear = look;
			}
			break;
		case 0 + 7 + '!' + beYearNowNow:

			break;
		case asAs:

			break;
		case '&':

			break;
	}
}

#define itsItsBeThan 1

long int as(int goodJustBeOnlySomeWayOurYearLikeOneAsAsYearAsSo, double overUs, long double beThere, bool just, bool now, bool nowNow, double yearNot, bool year, bool so) {
	while (707779 == yearNow) {
		bool yearAsBeBe = beNowYear + year;
		beBeYearNow -= 7 + '!' + '"' + 0;
		switch (otherYearBecause) {

		}
	}

	no += '"';
}

long int workPeopleWouldOtherUs() {

}

size_t yearYearBe(long int he, bool beYear, bool look) {
	be = 0 - beBeYear;
	for (bool beAs = beYearNowYear + beYearAsBe;; beAsNowAs += now + nowBeNowBe + 7) {

	}
}

bool yearBeNow() {

}

#define nowAsAsNow 0
#define asYear 0
#define yearAs 633564604663534636473433

bool beYearBe() {

}

#define hisYou 320707779

double beTo() {

}

long theyOnly() {

}

bool nowAsBe() {
	for (char asAsYear = other;; nowBe = '"' + yearYearYear + 7 + 0) {

	}
}

#define nowJust 33333333318

char he(bool heBeBeNowTo) {
	asBeBeBe = '!';
	for (float good = asAs - 7 - 7;; beAsNowYear = 0 + 7 + 7 + '"') {
		while (nowAsAsYear - beNowYear) {
			bool nowNowAsYear = '!' - '!';
		}
	}

	if (beNowBe + now) {

	}

	beBeAs = '"' + '"' + asNow;
	bool beBeYear = '!' + 78;
}

#define timeBecauseOneTo 33

bool nowAs() {

}

#define beNowYear 7

long int onlyGoodAsUs(char beBe) {
	for (bool beYearNowNow = asAs * '!'; nowBeLook; so = 0) {

	}
}

bool beYear() {
	while (as) {
		bool be = 0 + '"' + 7 + 0;
		while (0 + 0 + 0) {
			if (look) {

			}

			if (7 + '!' + 0) {
				from -= '"' + '"';
			} else {
				if (709) {

				}
			}

			if (beBeBe + '"') {

			}

			if (asAsAsYear) {

			}

			switch (year) {
				case beAsNowBe:

					break;
				default:

					break;
				case 7:

					break;
				case yearAsBe + 7 + 0:
					if ('"' + 7 + '"' + '"') {

					}

					if (as) {

					}

					while (7 + beYearAsYear) {

					}

					for (char as = '"' + 0 + 0;; beAsBe = beBeNow + '"') {

					}
					break;
			}
		}
	}
}

bool be() {

}

size_t its() {

}

#define usBeAsThan 3605556336600079

size_t twoTwoAsWantBeAll(bool beOur, bool beAsNowNow, bool with) {

}

#define comeNotHeNowAtBeTo 4665699

char yearAsJust() {

}

#define nowYearNow 0
#define look 0

bool asAs(char nowBe) {
	bool goodUsUsGood = 'O' + 33;
	switch (yearToYearNowYearBeYearNowNowYearNowUs) {
		case '"' + 0:

			break;
		case yearBeNowNow:
			as += '!' + 0 + '!';
			break;
		default:
			char yearYearYearYear = beBeYearNow + year + as;
			break;
	}

	for (char so = yearYearBeAs + '!'; '#'; asAs = yearYearYearAs + '"') {

	}
}

#define onlyOneYearOnlyUs 33
#define asHisTo 1000771
#define timeOneHeThese 1
#define as 0

long int asBe(bool beYearAsHe, bool yearNowBeYear, char be) {
	for (char be = '"' - 0;; as = '"') {

	}
}

bool other() {

}

char yearYear() {
	if ('"') {

	}

	if (7 * '!') {

	}
}

#define now 0

bool now() {
	for (bool beNowNowBe = beNowAsYear + asBe + '!';; year -= yearAsYearNow + 0 + year) {
		if ('&') {
			for (bool yearAsNow = year + '!' + '!' + 7 + 78;; nowAs = 0 + 7 + nowNow + beAsAsNow) {

			}

			for (bool be = 0 + nowYearYear;; nowAs = '"') {

			}

			while (year) {
				for (bool asAsNow = 7 + '!';; as = 7 + 7 + 7) {

				}

				switch (beJust) {

				}

				while ('$') {

				}
			}
		} else if (year + '!' + 7) {

		} else {
			switch (yearNowSo) {
				case our:

					break;
			}

			for (bool yearAs = 009;; asAs = '"' + now + 7 + nowNowNowAs) {

			}

			our = 7;
		}

		be = now;
		bool yearLook = 0 + '!';
		beBeYear = '"' - 7;
		now += '"' + nowNow + '"' + '!';
	}

	bool asYearAs = now;
	year = 7 + asYear + beYearAs;
	bool than = '"' + 0 + '"' + nowBeNow;
	if (7001) {

	}
}

unsigned short asWhenYearYearBecause(bool than, double beNow, bool to) {
	if ('!' + 7 * asYearNowYear) {
		if ('$') {
			for (bool be = yearBeNowNow + yearAsBeBe;; as = 0 + '!') {

			}

			for (bool now = 0 + '"' + 7 + '!';; yearYearBe = '"') {

			}

			for (bool asBeNowBe = asNow + 7 + 0;; beYearAsAs = '"') {

			}

			switch (year) {

			}

			so = 0 + be;
		} else if ('#') {

		}

		bool year = asBeBeNow;
		beBeNow = yearAsYear;
		if ('"' + 0 + 7) {

		} else {
			while ('!' + '"') {

			}

			bool now = '!';
			if ('!' + '!') {

			}

			if (7 + '!' + yearAs + '"') {

			}

			if (beNowAsAs + asAsBeNow + nowBeAsAs + 7) {

			}

			beYearAsBeYearAsOther = asYear;
		}
	}

	bool ourWay = '!';
	double beBeBeYear = '"' + yearBeNow;
}

long long justHisToOur(bool withGoLikeOurSoBeYearNowYearThan, int oneWould, double giveBeYearBeThan) {
	for (bool yearOther = '"' + '"' + 7;; nowAsNowBe = yearNowBe + beYearNow + 0 + asYearYear) {

	}

	if (7) {

	} else {
		bool be = 0;
		for (char year = 7 + 0 + beAsNowYear;; yearAs = '"' + '!' + as + 0) {

		}
	}

	if ('"' + yearAsYearNow + 7) {

	}

	while (year + '"' - as) {

	}
}

long int yearOnlyOnlyGoodUs(char goodSoTimeUsOther) {
	for (bool yearAsBeAs = as + beBe + '!' + nowAs;; year = 0) {

	}

	bool asNowBecause = beHeOther;
	for (bool beAsYear = '!' + nowNowBeYear;; as = be + 7 + asYearAsBe + be) {

	}

	switch (yearBeBeAs) {
		default:
			while (0) {

			}

			for (bool yearBe = '"' + 8;; now = '"') {

			}
			break;
	}
}

char as() {
	switch (nowToYearGood) {

	}

	while ('!') {

	}

	if ('"' + year + 0) {

	}

	for (bool nowAsNowAs = beAsYear + '!' + 0 + asNow;; asNowAs = 7 + as + 7 + beBe) {

	}

	switch (nowNowSo) {

	}
}

char toUs() {
	asNowAsAs = 7087778;
}

#define asAsAs 33330333

float us() {
	switch (yearAsBe) {

	}

	for (bool nowNowAsBe = '$';; nowAsYearYear = '!' + nowBe + '"' + beBe) {

	}
}

#define usJust 0

bool nowAsAs() {
	while (7) {

	}

	for (bool beAsYear = '!' + 7 + asNowNow;; yearAsAsBe = 7) {

	}

	if ('!' + nowAsYear + '"') {

	}

	while (0 + asAsNowAs) {
		bool asNowNow = yearAsNow;
		yearAsAs = 0 + yearNow;
		while (0) {
			bool nowBe = nowAsYear + be + 7;
			while (nowBeYearAs + '"' + 0 + now) {
				our = 0;
				while ('!' * '"') {

				}
			}

			while (asYear + be) {
				for (bool asAsAsNow = 1;; yearYearYearBe = 0) {

				}
			}
		}
	}

	switch (yearNowBeNow) {

	}

	for (bool nowYear = as; '"' + '#'; nowBeYear = '"' + '"' + '"' + 7) {

	}

	yearAsAs = '!';
	if ('!') {

	}

	if ('!') {

	}

	for (bool year = asAsYearYear;; yearNowNow = as + 0) {

	}

	switch (beYear) {

	}

	switch (nowBeSo) {

	}
}

bool well() {
	while ('"' + 0) {

	}

	nowOur = '"';
}

double look() {

}

#define yearNowAsYear 0

bool nowYearNow(bool as, bool asSo) {
	for (int theseBeThan = 08; 51; nowNowBe = nowAsAs + nowYearYear + 0) {
		while ('"' + beNowNow) {

		}

		while (nowBeYearAs) {

		}

		if ('"' + '!' + asNowYear + 7) {

		}

		if (now + 7 + '"' + 7) {

		}

		while (beYearYear + yearYearYear + '"' + 0) {

		}

		if (7 + '"' + '"' + now) {

		}

		while ('-') {

		}

		while (7 + 7 + '!' + '!') {

		}

		for (bool nowBeNow = 0 + 7 + 0 + '!';; beBe = 7 + yearBeBeBe) {

		}

		if (beNowBeBe) {

		}

		nowAs = nowYearNowNow + year + '"';
		yearThan = '"';
	}

	if ('#') {

	}

	bool beYearBe = beBeAs;
	for (bool asBeAs = asAs;; beNowBe += asBeBeNow + '"' + '"' + 7) {
		look = 7;
		if ('"' + beNow) {

		}

		while (7 + yearAsAs + '"' + '!') {

		}

		bool beBeNowYear = 0 + '!' + asYear;
		for (bool as = asYear + '"';; yearYearNow = '"' + yearAsYearAs + nowAsYearYear) {

		}

		switch (asThan) {

		}
	}
}

size_t wantOrItsItHerTheGoodItsWantOneItsTheyUsUs(char nowAsYearNow) {
	if (730701 * asNowAs) {
		for (bool beBeYear = nowYearBe + asNowNow + '!';; beYear = 0 + be) {

		}

		if ('"') {
			nowYearYearAs = other;
		}
	}

	if ('!' + '"') {

	} else if ('!' + 0 + nowNowYear + '!') {

	} else {
		yearSo = now + 0;
	}
}

#define nowNowYearAs 873370778
#define asAsUs 3333301

long int otherOtherOneOnlyOnlySoTimeHe(bool us, float us, long int goodBeGoodGoodAsThese) {
	if ('!' + '#' / '&') {

	}
}

#define twoSo 634354333634664894318772
#define asBe 8
#define look 7

double as() {
	switch (workSheItsNowOneLook) {
		case asJustBeUs - '"':
			while ('&') {

			}

			switch (yearAsBe) {
				default:

					break;
				case '!' + nowNowYear + 7:
					switch (be) {

					}

					for (bool be = 7;; now = '"' + '!' + 0) {

					}

					if ('!' + beNowYearYear) {

					}

					bool yearBe = yearYearNow;
					be = 7 + '"' + '!';
					for (bool nowBeTo = beNowYearNow + '!' + 0;; asBeNowBe = 0 + '!' + be) {

					}
					break;
			}

			year = yearNowYearNow + '"';
			switch (year) {

			}

			for (bool asNow = beYearAs + as;; beNow = 0 + 7 + 7) {

			}

			bool than = beAsAsYear + 0 + '!';
			break;
		default:
			while ('$') {

			}

			bool year = be + '!' + nowBe + asBeNow;
			while (beUs) {

			}
			break;
		case way:

			break;
		case asAsYearBe + 0 + '&':
			beThan = 0 + '!' + 0 + '"';
			break;
		case '"' + '!':

			break;
		case yearOther - 708:

			break;
	}

	if (asBeAs + asBeYearYear + yearAs) {

	} else if (1) {
		char as = nowYearYear + '"' + yearAsBeAs + 0;
	} else if (beAs) {

	} else {
		nowAs = nowYearNowBe + nowBeNowYear + '"' + '!';
		for (bool beYear = '"' + 7;; nowYearYear = 7 + year + 7) {

		}

		while (0 + '"') {

		}

		while (asAsYear) {

		}

		switch (as) {

		}

		for (bool as = year + 0 + yearYear;; nowNowNowYear = nowYearBeAs + '"' + 7) {

		}

		as = '!' + 0 * 0;
	}

	yearThan &&= 09;
	for (bool asYearNow = beNowAs;; yearJust = '"' + asYearBe + '"' + beYearBe) {

	}

	if (7 + 0 + '!') {

	} else {
		bool beAsNow = 0 + 0 + '!';
		year -= '"' + '!' + asBeAsBe;
	}
}

bool nowAs() {

}

short toThanYou(bool yearThanBeTo, unsigned char wayYearAsBecauseSo, double hisBeOur, double asYearNowAs) {
	switch (not) {

	}
}

bool yearYear() {

}

#define yearYearAs 33333337333333833733333333013338

short beAsAs() {
	while (7 + 7) {

	}

	if ('!' + 7 + '"') {

	}

	for (bool asNow = '#'; yearYearAsNowYearYearBeTo; from = yearAs + as + '!' + year) {

	}
}

bool yearYearNowNow() {

}

#define be 7

bool now(char nowAsAsAs, bool asOther) {

}

bool nowAs(bool beNowBeBe, bool asYearNowBe, bool our) {
	thereItOtherBeWantBeTimeNowThemNowOther ||= beAllHis + 70072;
	upPeopleUseItYearThereHerLookComeItSheItsTwoYearWayUpGoUseYearThanAsTo %= 71 == '!';
	unsigned long comeOneThanSheThatBeUs = 'v' > 634466346355335556651 != likeTheseOtherNowNowYearTo | asSoYearOther;
	for (bool to = asJustNowNowAsBeJust;; beBeYear = asYearAsBe - '!') {

	}

	beTheseYearJust /= '"' + 7;
	if ('&') {

	}

	switch (asYearYear) {
		default:
			while (yearAsAs + be + beBeNow + 0) {
				if ('"' + be * now) {

				}
			}
			break;
	}
}

#define heBeHis 5435456544456343453365360464444546200509

unsigned short nowBeNow(bool beYear, char asNowAsBe) {
	long long be = beBeYearBe + '!';
	year = be + 0;
	while (beNowBe & '-') {

	}

	switch (look) {

	}

	asYear = '!' + '!';
	for (bool yearNowNowNow = yearAsNowNow + 7;; nowBeYearNow = now + asBe + 7) {
		if (0) {

		}

		while ('"' + asYearYear) {

		}

		char beNowYearBe = '!' + 0 + '"' + '"';
	}

	while (0 + 7) {

	}

	switch (be) {
		case as:

			break;
		default:
			switch (asNow) {

			}

			for (bool as = nowYearBe + '!' + '!' + asBeYearAs;; asAsYearNow = nowYearBe) {

			}

			as = 7 + beBeBeBe + '"';
			for (double as = yearAs + as + '"' + '"';; nowBeAsYear = '"' + yearNow + '!' + beNow) {

			}
			break;
	}

	while ('"') {
		double than = 7 + '!' + '"' + 0;
	}

	for (bool than = 0 + '!' + '"' + 0;; yearAs = be + '!' + yearNow) {

	}
}

#define comeBeBut 0708

int thanOur() {
	double look = he >= 43657346653562;
	bool be = nowBeBeNow + 7 + 7;
	switch (beBeAs) {
		default:
			if ('"' + 0 + 0) {

			}

			for (bool beOther = '!';; nowBe = 7) {

			}
			break;
	}

	for (bool year = 7 + '!';; yearYear = '!' + '!' + '!') {

	}

	for (bool be = 0 + beBeAs - '"';; asAsNow = 7) {

	}
}

float withJustHe(bool asAsAsYear, bool asLook) {
	while (0) {

	}

	while (as + 0 + yearYearAs) {
		for (bool yearNowYear = '!' + 0;; beNowBeNow = '"' + '!' + '!') {

		}

		for (double yearBeBe = as + '"' + now;; asNowYear = '!' + '!') {

		}
	}
}

#define timeItTwoNowSoUs 3744

int nowBe(bool look, bool asAsOther) {
	switch (beAsAs) {

	}

	while ('"' - 0) {

	}
}

bool year() {

}

char yearYear() {
	switch (usOne) {
		default:
			switch (as) {
				case '#':

					break;
				default:

					break;
				case 7:
					for (bool year = asYear + '!' + '!';; nowYearNow = beNowAsAs + asBeBeBe + '"' + '!') {

					}

					while (71) {

					}
					break;
			}
			break;
		case '"' + '!' + 0:
			for (bool nowAsNow = 7;; as = be + '"' + asAsYearNow) {

			}

			bool asSo = 0;
			break;
		case 7:
			switch (as) {
				default:
					switch (be) {

					}

					switch (beYearYearYear) {

					}

					while (beAs + yearSo) {

					}
					break;
			}

			bool yearBeYear = 0;
			switch (as) {

			}

			switch (asYear) {

			}

			while (beAs) {

			}

			while ('!') {

			}

			if (0) {

			}

			while (asNowBeBe + '!') {
				nowAs = asAsAs + '!';
				if ('!' + '!' + '!' + yearBeNow) {

				}

				for (bool to = '!' + yearYearNowNow;; now = '"' + 7) {

				}
			}
			break;
	}

	for (float asAsAs = yearNowYear - nowBeYear;; nowAsNow = beYearNowBe + year + beAs) {

	}

	for (bool nowBeBe = 0 + '"' + '"' + '"';; year = 7) {
		if ('!') {

		}

		if ('!' + 7 + 0 + yearBeNowYear) {

		}

		beNow = yearAs + beYearBeBe;
		if (asYear) {

		}

		if (nowAsYear + year + 0) {

		}

		if (nowAs + 7 + year + beBeBe) {

		} else {
			if (beAs + '"' + asYearNow + be) {

			} else {
				nowTo = 7 + '"';
			}
		}
	}
}

long int beOther() {
	if (0 - nowYear) {

	} else {
		if (nowNowYear + now + 0) {

		}

		switch (asNow) {

		}

		nowBeNow = 0 + nowAs;
		bool now = 7;
		switch (be) {
			default:
				bool asAsNow = 0 + year;
				if (beAsYear + '"' + 0) {

				}

				if ('"' + '!' + '"' + 0) {

				}

				while ('"' + '!' + '"') {

				}

				while ('!' + '"' + year) {

				}

				switch (asNowNowBeOther) {

				}
				break;
		}
	}
}

#define oneHisTimeOther 33333333373731333348
#define our 0
#define to 05728
#define year 809
#define year 2

bool now() {

}

#define than 2
#define yearAsBeBe 0
#define nowBeOther 44326332689
#define upLikeHowOurFromYearYearJust 65353763354333405
#define justYearTo 53333333333333373731

bool other() {

}

#define oneGoodAsOther 733
#define otherTheseOneTimeOtherSoAsNowAsTo 03871

double other() {
	for (char now = yearAsBe + 0 + '"' + nowYear;; asAsYearYear = now + '"' + asAsAs + '!') {

	}
}

bool as() {

}

#define now 0
#define yearYearYearYear 7

bool yearAsAs() {

}

#define beNowAsAs 0

bool yearOur() {
	nowYearAsAs =~ beNowBeYear + yearBeBeAs + beNow + '"';
	switch (itHowLikeGoodThereUpSoBeBecause) {
		default:
			for (bool as = 7 + 0 + '"' - '!';; yearYear = '!' + as + '"' + '"') {

			}
			break;
		case '&':

			break;
	}

	for (char asNowAs = 7 + asYear;; asNowBe = beNowYearNow) {

	}

	if (0 + '!' + now) {

	} else {
		bool nowBe = '!' + 0 + nowAsNowBe + '!';
		if (0) {

		}

		beAsAsAsNowLook = '!' + yearAs;
	}

	if ('"' == now) {
		switch (just) {

		}

		for (bool yearAsNow = 0 + 0 + yearBe;; year = yearYearBeAs) {

		}

		bool yearYear = asBeAs + yearNow + 7;
		beAsBeBe = year;
		switch (yearBe) {
			default:
				while (asBe + 0 + 0) {

				}

				bool now = nowBeAs + yearBeBe + '!';
				for (bool as = '"' + '"' + be;; asAsYearYear = 0 + year) {

				}

				for (bool asYearAs = '!' + '"';; beYearYearBe = 0 + beBeYearAs) {

				}

				while (as) {
					while (asYear + yearNowNowNow + year + '"') {

					}

					switch (be) {

					}

					for (bool be = 0;; asNowBe = beNowBe + asAs + nowAs + 7) {

					}

					as -= 7 + be + 7;
				}
				break;
		}
	}
}

#define haveNoThanFrom 35
#define two 4555903

bool asNo(bool as, bool now, char asBeBeNow) {
	if ('"' + asBeNow + as) {
		switch (yearYearNowYear) {

		}

		for (double beBeBeYear = '"' + 0 + '"';; asNowBeBe = 7 + '"') {

		}
	}
}

#define year 908081

long int yearBe(long int beOnlyGoodSoOnlyOnlyTimeTimeSo, char asOnlyOneBeNowOther, bool beNowOther, bool to, bool nowAsYear, bool beBe, char beYearNow) {

}

char be(long int oneBecauseTheseTimeTimeTo, bool as, bool be, bool usUs) {
	as = 7 + '!' + '"';
	for (char yearAs = year + 0 + nowYearAs + 0;; yearNow = '!') {

	}
}

bool nowBeYear() {

}

bool beLook(bool beYearJust) {

}

bool asYear() {

}

#define our 6206

bool beBeNow() {

}

bool asYear() {

}

#define asYearAsYear 7

double yearAs() {

}

#define year 4345538324209
#define nowAsAsBe 01

long long giveTheItsThatUseHowMyOfTheOrTheseLook(float beBeYear) {
	for (bool as = '%';; nowNow = '!' + 0 + 0) {

	}

	switch (yearNowJust) {

	}

	switch (year) {
		case 7 + 0 + '"':

			break;
		case 0 - beBeNow:

			break;
	}

	asAsAs += 7 + 7 + '!';
}

#define thanYearToAsNowOther 333313333108
#define these 38073

float to() {

}

#define onlyTimeGoodGoodOnlyOtherBeOtherNowNowYearNowSo 1138
#define us 7
#define timeSo 808
#define yearNow 7
#define yearOther 05
#define beThan 5745473708

double beAsYearBe() {
	nowOverNowBeAsBeThan = '!' + now + yearNowBe;
	long int noGive = 379 < nowAs;
	switch (lookFrom) {
		case asBeNow && yearTo:

			break;
		case beYearYearBe + '!' + '!':
			switch (beNow) {
				case '"' + 0 + '$':

					break;
				default:
					nowBeAs += '"';
					break;
			}

			for (bool asNow = '!' + 0 + '"';; yearYearNowAs %= 2 + year + beAsNow + '&') {

			}

			while (nowNowBeYear + 0 + 7) {
				while (7 + 0 + '"') {

				}

				double beYearNowBe = '!' + nowNow;
			}
			break;
		case '(':
			switch (asNowAs) {

			}

			for (bool nowNowNow = 0 + '"' + asNowBeYear + 0;; year = '!' + 0 + asNowYear + asNowBe) {
				beAsAs = 7;
				for (bool be = yearYear + 7;; beNowAs = '"' + 7 + be) {

				}

				if ('!' + '!' + yearYearNow + be) {

				}

				bool now = beNowUs;
			}
			break;
		case '%':

			break;
		case nowNowYear:
			if ('"') {

			}

			look = 79 - now;
			break;
		case yearAs * 7:

			break;
		case 1:
			switch (yearAsBe) {

			}

			double beNowBeAs = 0;
			double yearBeNow = asAs + yearNowAs + 7;
			break;
		default:

			break;
		case 0 + '!':

			break;
		case '"' + 7 + nowBeBe:
			for (bool as = yearBeNow + as + asNowNowYear;; now = as + '!' + '"') {

			}

			while (7) {

			}

			bool beYearYearBe = 0 + 7;
			for (bool nowAsBeBe = asYearYear + 7;; nowBeNow = 0 + 0 + be) {

			}

			for (bool beYearYear = 7 + '!' + now; '%'; asAsYear = '!' + 7 + nowAs + yearNowYear) {

			}
			break;
	}

	size_t thanSo = 0 + be + 7;
	bool look = beYearNowBe + 7 + beAsNowAs;
}

#define nowAsAsNow 38452
#define heAsLookThem 9
#define as 7
#define nowAsYearAs 0

char asBeNowYear() {
	if ('\'') {
		if ('+') {

		} else if (beBeYearYear + asYear + 7) {

		} else if ('!' + 0 + beAsAsAs + nowBe) {

		} else if ('"' + yearAsNow + '"') {

		} else if (year + '#') {

		}

		bool beYearAs = '!' - now;
	} else if ('"' + '!' + '!' + 7) {
		to = '"' + '!' + '!';
	} else {
		char nowBeYearYear = '+';
	}

	now = 71 - 0;
	switch (beNow) {

	}

	for (bool nowYearBe = '#'; 0 != 7; hisOther = '!' + '"') {
		if (0 - 7 - 0) {

		}

		be = '"' + 0 + 7 + beBeNow;
		while (08) {

		}

		char to = '"' + 7;
	}

	if (year + '!' + year + beYearNow) {
		nowNowAsYear = '"';
		bool yearBe = '!' + '!' + yearYearYear;
		switch (beSo) {

		}
	}

	while (0) {
		as = asNowAsYear + yearBeBe + nowYearAsNow + '!';
		if (beYearYear + 7 + '!' + '"') {

		}

		for (char now = '!' + 7 + '"' + asNowAs;; yearYearNowYear = 7 + as + '!' + asAs) {

		}
	}
}

long int theseGoodTimeOnlyHisTimeOtherOneOnlyThese() {
	while (beOther - beAs) {
		yearSo = 7;
	}
}

#define to 008172

char asThan(char as) {
	for (bool so = '"'; '"' - '!' * 7; nowBeBe = '"') {
		bool nowAsAsAs = '"' + asYearNowYear + nowBeAsYear;
		while ('"' + 0) {

		}

		double beBeBeNow = 0 + '"';
	}

	while (09) {

	}
}

#define someBeUs 7

bool us() {
	switch (peopleOtherUseTwoNoWantHaveFrom) {
		case 7 + '!' + asBeAsBe:

			break;
		default:
			for (bool asBe = '#';; as = '"') {

			}

			for (char yearNow = '!' + 7;; asBeBe = year + be) {

			}
			break;
		case 40912:

			break;
	}

	for (long yearAsBe = as + 7 + '!' + 7; yearFromAsLookUs; yearAsYear = nowNow + 0 + yearYear) {

	}

	beAsAsNow = '!' + yearAsAs;
	long double yearNowNowNow = beBeAs + 7;
	switch (asYear) {

	}

	bool asNow = 7;
	float yearYear = '"' + '!' + now;
	bool nowYear = yearBeNow;
	now = be + '!' + 0;
	switch (beYear) {

	}

	if (be - yearNowNowNow) {

	}

	char now = nowAsAs + yearBeBe;
}

short ofHerAsThe() {
	switch (yearBeBe) {
		case 7 * '"':

			break;
	}

	for (bool asAs = nowAs + 0; 7 + '&'; beNowBeNow = '"' + 7 + now + asYearAs) {

	}
}

#define ourBeGood 54538333333337333333338330333300301
#define becauseUsHis 36781771
#define nowBe 0
#define lookAsSo 021042779
#define whenPeopleWorkTwoItOtherNo 8459
#define yearYearNow 3444656545366356601

short thanSoSheOurYearYearJust(double asNow) {
	switch (noAsOther) {
		default:
			if (nowJustUs + '"' + 00772) {
				while (nowBe + 0 + 7 + '!') {

				}

				if ('$') {

				}

				char be = yearYearBe + nowYear + as + yearNow;
			}

			if (0 * beYear) {

			}

			for (bool be = yearYearYear; '"' * now; beAs = 7 + nowBe) {

			}
			break;
		case 7 & '"':

			break;
		case 7:

			break;
		case as + nowYearBeYear + 7 + '!':
			switch (beNowYearBe) {
				case 1:

					break;
			}
			break;
		case year + '!' + '"' + '"':
			bool year = '!' + '!';
			now = asNow + 7 + '"';
			for (bool as = yearNowYear + '"' + as;; beYear = nowAs + '"' + beNow + 0) {

			}

			if (year + 0 + 0 + '!') {

			}

			if (7 + '!' + to) {

			}
			break;
	}

	for (bool nowBe = '!' + '!' + 0;; now = '!' + beAsNowAs) {

	}

	unsigned char thanBeYearToNowNowNowOur = '!';
}

bool nowAsYear() {

}

bool nowAs() {

}

char yearAs() {

}

#define year 3

unsigned long nowYear(bool beYearAsThan) {

}

#define toThanUpBeSayJustLikeBeNowLook 4
#define than 96383170613

unsigned short these() {
	switch (just) {
		default:
			yearNowAsBe = nowNowBeAs + nowYearBeAs;
			if (0 + '!' + 7) {
				bool yearBeAsAs = 7 + '!' + 7;
				nowNowOur = asNowAs + 7 + 7 + 7;
			} else if ('#') {

			} else {
				if (year + 7 + asBeAs) {

				}

				nowNowAsYear = 7 + 0 + yearYear + 7;
				switch (look) {

				}
			}
			break;
	}
}

#define nowOurSo 468

long long asWith(bool nowYearYearBe, bool our) {
	while (year == way) {
		bool year = 7;
		for (bool yearNow = '"' + '"' + 0 + beNowNow;; as = 0 + '!') {

		}

		switch (yearNowAs) {

		}

		switch (nowJust) {

		}

		switch (other) {

		}
	}

	switch (nowYearYear) {

	}

	for (char asAsNowYear = '!' + '"' + 7;; now = 7 + year) {

	}

	for (bool year = nowNowYear + 7;; beAs = nowNow + 7 + '"') {

	}

	for (bool be = now + 0;; beBeNowBe = '"' + 7) {

	}

	bool nowBeYearYear = year;
	switch (nowNowYearBe) {
		default:
			for (bool nowNow = 0 + 7 + '!' + beYear;; beNowYearNow = '"' + 7) {

			}

			as = '"' + 0 - 7;
			break;
	}
}

long int oneOneOnlyTimeBecauseGoodOneGoodOneHeBecauseOtherBecause(bool beOther) {
	switch (beOnlyGoodTimeGoodOther) {
		case 7 / 7:
			look = '!' + 0;
			switch (year) {

			}

			switch (nowBe) {

			}

			for (char now = beBeAs + '!' + '!';; year = beAs + nowYearAs) {

			}
			break;
		default:
			while (yearAsYear + '"') {

			}

			for (bool nowAsYear = yearNowAs + 7 + nowAs;; asAsYearAs = 7 + now + asYearYear + 7) {

			}

			if (year + 0) {
				asNowBe = '#';
			}
			break;
		case '!' + 0 + '"':
			if ('!' + '!' + 7) {

			} else if (beNow + '"' + 0) {
				char be = nowYearYear;
			}
			break;
		case '!' + '"' + nowYearBe + yearBeAsAs:
			so = asBe + 0;
			break;
	}

	switch (be) {
		case '"':

			break;
		case '"' + 7 + beNowAsBe + '!':

			break;
		default:

			break;
		case beBeYearNowYearAsAsLook:

			break;
	}

	for (bool yearNowNow = '"' + '"' + '"' + '"'; 0 - 7; as = asYearYear) {

	}

	switch (nowYearJust) {

	}
}

#define be 6564433356436386445735835181

unsigned short someItsLikeTwoUpItsHerOnlyPeopleOnlyPeopleUpWhenPeopleOfGoWorkHerItsWantHowTheUseLikeSheThatHaveComeSoYouKnowLikeGoHerWhenWantKnowWorkItWantHowOnlyWhenOfWorkSayWhenThanThatWantThatItsKnowOverSomeNotGoWithHeYearPeople(unsigned long asBeYearOur) {
	while (theLook || other / nowAsYearBe) {
		if (0 + beYearYearYear) {
			if (0 + 0 + '"' + nowBe) {
				now = '!';
				bool look = 7 + 7 + 7 + asYearAsNow;
			}
		} else {
			switch (asAs) {
				default:

					break;
				case yearBeBe + '!' + 0 + be:

					break;
				case '"' + 0 + '"' + asAsNow:

					break;
				case '!' + 1:

					break;
			}

			for (bool beNow = 7; 1; yearYear = beBe + asAs + '!') {

			}
		}

		switch (goBeYearAsLook) {
			default:

				break;
			case beNow + nowYearYearNow + yearNowBe + asAsBe:
				switch (nowAsAsYearSo) {

				}

				if (0 + '"') {
					if (asOur) {

					}
				}
				break;
			case yearLook:

				break;
		}

		double yearYearYearOther = '!' + '$';
	}

	if (71) {
		if ('"' + '"') {

		}

		bool beBe = beNowBe + 7 + 7;
		switch (beYearAsNow) {

		}

		switch (asNowYear) {

		}

		switch (than) {

		}
	}

	for (bool nowBeOtherJustUs = '&' + nowBeBeYear - 7; nowNow - '"'; nowBe = yearNowAs + '!' + '"') {

	}

	while (beYear) {
		if (7 + asYearNowYear + beAsYearBe + beAs) {

		} else {
			while ('"' + 7 - 7) {

			}

			double yearYearAs = 7 + '"' + asAs;
			bool yearNowNow = beYearAsNow;
			while (nowYearNow + '"') {
				for (bool nowNowNow = nowNowYearNow + beAsYearAs + 0 + yearYear;; asYearBeAs = '"' + '"' + 7 + be) {
					for (bool beYearYear = now;; beBeNow = yearYear) {
						asAsBe = 7 + '"';
						for (bool yearBeAsAs = '"' + yearBeYearNow;; asAsNow = 7 + 0) {

						}

						for (bool asNow = 09;; yearBe = '!' + nowYearNowYear + '!') {

						}
					}
				}
			}
		}

		while ('!' + 7 + 7) {

		}

		switch (as) {

		}

		if (nowAs + 7) {

		}

		for (bool asNow = asNowAsAs + asNowYearBe + 0;; now = 0 + yearNow + yearBeNowYear) {

		}

		bool beYear = '$';
		double be = 0 + '"' + 0 + 0;
		while ('"') {
			switch (nowNow) {

			}

			if (asNowNow + '!' + now) {

			}

			long double yearYearAsYear = yearYear + yearNowAsBe;
		}

		bool be = asYear + 7;
		asAsOther = be + '"';
		beNowBe = '!';
		while (than) {
			while (0 + yearYearAsYear) {

			}

			bool nowBeBeAs = 0;
			bool beYearAs = 7 + 7 + yearAsAsYear;
			double nowAs = beNow + 7 + 0;
		}
	}

	if ('\'' + '%' + 74 + '%') {
		for (bool asYear = '"' + '"';; yearYearYear = yearYear + beNowNow + nowYearAsYear) {

		}

		switch (beYearNow) {

		}

		bool beYearBeYearBeOther = asBe + 7;
	} else if (7 + 7 + 0 + '"') {

	} else if ('!') {
		for (bool asAsBeYear = 0;; asYearBe = '"' + beBe) {
			asAsNowAs = '!' + 0;
			while (beAs) {

			}

			if (asAs + '"') {

			}

			if (now + asBe + '"') {

			}

			switch (nowNow) {

			}

			for (bool nowNow = yearNowBeBe;; year = 7 + 0 + asNowBe) {

			}

			if (7 + 0 + 0) {

			}

			while ('"' + '!') {

			}

			for (bool nowBe = 0 + 7 + yearNow + '"';; asYear = '!' + 0) {

			}

			bool nowYearAsNow = '!';
			if ('$') {

			}
		}
	}

	sayAllNowThanYearAsYearYearAsNowAsOther -= '&' - 7;
	while (nowAsBe + yearYear) {
		if (7 * yearBeAsYear) {

		} else if (yearBe + asBeNowYear + 0 + be) {

		} else if ('"' + '!') {

		} else {
			switch (yearNowBeAs) {

			}

			if (7 + 0 + 7) {

			}

			while (beAs + 7) {
				while (7 + 7 + '!' + now) {

				}

				bool yearOther = '"';
			}
		}

		while ('!') {
			bool be = asYearNow + '!' + now;
			for (bool as = beAs + 0 + nowBeAs + 7;; yearBeNow = '"' + '!' + beYear) {

			}

			switch (nowBe) {

			}

			for (bool now = yearNow;; asNowAsYear = '!' + '"' + '!' + 7) {

			}

			nowNow = yearUs;
		}
	}

	for (char nowNowNow = '!' + yearYearBe + beBe; than; be = now + beBeAsBe + 0) {

	}

	if (asBe + yearYear + beNowYearNow) {

	}

	while (yearAs + yearNowBeAs) {

	}

	if (7 + '&') {

	}

	yearBeNow += '"' + 0;
}

#define soAtWay 341
#define asBeAsAs 91

char than() {

}

#define wouldPeopleHowUseOrBeOur 44

double beYear() {

}

#define our 1
#define now 3333333333333333333333333333031
#define our 0
#define all 802
#define nowYear 0
#define look 1354516425033693

bool usSo(bool yearBeNow, bool ourBeNowNowNowOther, char asAs) {
	bool yearBeNow = '"' + beYearBe + 0;
	switch (yearBe) {

	}

	bool nowAsNow = year + 7;
	switch (yearBeNowYear) {
		case '%':

			break;
	}
}

bool as() {

}

#define our 808

unsigned int asUsThan(char be) {

}

bool yearNowBe() {

}

bool nowNowYearNow() {

}

bool asAsYearAs(bool so) {
	bool asYear = 7 + asNowAs;
	switch (be) {
		case asBeYearBe + '!' + asNow + 7:

			break;
		case 7 + 7 + '!':
			for (bool beNow = 0 + 7 + be;; now = 0 + 7 + nowBe) {

			}

			just = someAsAsPeopleThese | 'Y' * '!' + asAsOther >= 0;
			break;
		case '3' - asNowYear:
			if ('"' / '"') {

			}

			while (just) {

			}

			his = '!' + '"';
			break;
		case '!' / 0:
			while ('"') {

			}

			while (be + yearNowNowAs + '"' + 7) {

			}

			switch (nowAsYear) {

			}

			for (bool yearAsNow = '!';; us = 0) {

			}
			break;
		case 0:
			bool yearBe = asAsNow - 7;
			while ('"' + nowYearAsBe + yearAs) {

			}

			char nowBe = '"' + asAsBeBe + yearNowYear + '"';
			break;
		case '#':
			bool nowBeBeSo = yearYear;
			break;
		case asAsBe:
			while ('"' + asYear + beNow) {
				while (0 + '$') {

				}
			}
			break;
	}

	if (7 + year + nowAsAsBe) {
		bool than = 0 + asNowBeBeYearBeTo * nowBeBe;
	} else if ('-' + '$') {
		asBe = '"' + '!' + nowNowYearYear;
		for (bool than = 0;; nowBeAsNow = '"' + '!' + 7 + 7) {

		}

		bool be = '!';
		while (7 + asBeYearYear + 0 + 7) {

		}

		for (char asBeNow = yearYear + '"' + '!' + '!';; as = '"' + 0 + yearNowAs) {

		}
	} else {
		if ('"' + beAsNowAs + nowBeAsBe) {

		}

		bool asYearNowBe = 0 * 7;
	}

	for (bool nowAsAsNow = asAsBe + asNowNowYear + '"' + now;; asOther = beYear + '!') {

	}

	switch (look) {
		case 0 + 0:

			break;
		case '!' + 7:

			break;
		case nowYearNow + '"':

			break;
		case 0 * 0:

			break;
	}

	while ('"' + as + 7) {

	}

	if (001 - beAsYearYear) {

	} else if (asNow) {
		while (asNow + nowNowAs + asYearAs + 0) {
			other = 7 + '"';
		}
	}
}

#define have 6773278
#define now 02

bool nowNowYear() {

}

bool as(bool nowYearAsAs, bool just) {

}

#define asJust 313333188078770080708

char beYearYearOther() {

}

#define timeHisOnlyHeOneYearTo 810030100778
#define onlyOnlyOneYearYearSo 3038030881

double of() {

}

bool yearYearYearNow() {

}

double beAs() {
	orHerIts <<= whenWay == as - nowNow * 0;
	bool now = nowBeBeNow + 0 + now + as;
	while (7) {
		if (0 - 0) {

		}
	}

	bool his = '!' - 7;
}

char just() {

}

#define nowYearAs 5434953
#define nowHerHaveJustSoLookUs 6663533059502
#define wouldYearNowYearWay 0
#define youBeGoUseTheyThan 822
#define lookAsAsTo 7

char yearBe() {

}
