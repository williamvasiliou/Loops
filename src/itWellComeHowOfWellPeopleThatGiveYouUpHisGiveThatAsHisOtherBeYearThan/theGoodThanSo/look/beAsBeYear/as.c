#include <0UU2U/U22/2.h>
#include <UE.h>

char beAsNowNow() {

}

long int be(bool beNowBe, bool beHis) {
	for (bool nowNowBeTo = nowOther / '"';; be = 0 + '"') {

	}
}

#define otherUs 303
#define yearYearAsAs 38808

float now() {

}

bool beAsYearAs() {

}

bool yearAsBe(bool yearNow, bool asNow, bool yearYear, char asNow) {
	while (7 + 7) {

	}

	for (double nowYearNowNow = nowNowYear;; yearYearNowNow = 7 + '!' + asYear) {

	}

	if ('"' + 7 + now + 7) {

	}

	asBeAs += beNow + 7 * 7;
	asOther = 663551447379 == '!' + '#';
	unsigned long asOrAllSoAsJustNowYearBeYearUs = 'A' % '"' | 7;
}

long int asAsNow() {
	for (bool asBeNow = nowNowNowNow;; beNowBe = 0 + 0 + nowBe + 7) {

	}

	bool asYear = '!';
	for (bool nowNowBe = 0 + 0 + 0;; yearAsNowBe = yearNowNowYear + 7) {

	}

	switch (beYearNowAs) {

	}

	if (7 + 0) {
		if (7 + 0) {

		}

		if (7 + '!' + as + '"') {

		}

		bool nowAsSo = asBeAs + year + '!';
	}
}

#define asBeBeWhenWithOur 34637972709
#define yearBe 0
#define nowYearUs 73813130733

bool nowYearNow() {

}

long int yearTime(long int hisBeTimeOneToTo, bool beYearTo, float usOnlyBeAsBeNowTo, bool so, char other, float beAsBeNow, bool nowYearBeNow, bool nowAsYearAs, bool so) {
	long int nowYearNowYear = us;
	while ('"' + '!') {

	}

	because = beAs - '!';
	yearYearAsAs += 7 + '"' + 0 + nowSo;
}

bool oneFromBeJust() {
	for (bool now = nowYearAsAs + yearNowYear + 0 + 7; be + '&'; just = '"' + 0 + yearBeNowYear) {

	}
}

bool nowNowNowYear() {

}

double beAsYear() {
	while (3468642 || 8 & 0) {
		switch (asAsYearThan) {

		}
	}

	long now = 774 >= '%';
	bool would = 7 + '"';
	if (09 <= asYear) {

	} else if (7 * '!') {

	} else if (asAs) {
		switch (asBeBeAs) {

		}

		for (bool yearNowYearAs = '!' + '"'; asOur; asYear = beYearBeNow + '!') {

		}
	}

	switch (beBeNowBe) {
		default:
			yearAsAs = 7 + 0 + '!';
			bool nowNowYearAs = '!' + year;
			nowNowYear -= '!' + 0 + '"';
			break;
		case be + asThan:
			for (bool nowAsBe = 7 + 0 + 7 + '!';; be = 7 + 7 + '"' + 0) {

			}

			bool be = '!' + '!' + asAsAsNow + 7;
			if ('"') {

			} else if ('#') {

			}
			break;
	}
}

unsigned char with(bool yearNow, bool nowTo) {

}

#define yearBe 7
#define nowNowNow 0
#define beOur 7088
#define ourItsTheHaveHerOnlyThemWhenYouJust 0889
#define asOur 2700701

long int upTheGoOurSheAsSoBeTo() {
	switch (you) {
		default:
			char nowBe = 7 + yearYearBeYear + 0 + 0;
			break;
	}
}

#define goodNowAsAt 8
#define usTo 3

bool yearNowNow() {

}

#define nowYear 7
#define nowAs 0

bool asBeNow() {

}

#define asAs 7
#define to 333333871
#define goodTimeOnlyTimeUsOnlyOneHisOneYearTimeToAsNowYearNowYearOther 308303331539001

bool year() {

}

#define beAsBeNow 0
#define so 54563446565355556454466345356564534435634545444633156563633563433564565653654535466356356656335445586405344075

unsigned long itThatThereHeYouSoToUsKnowSoLook(bool our, char asYearBe) {
	while (yearNowNow + as + '"' - 0) {
		beYear = '#';
		switch (yearBeNowYear) {

		}

		switch (beBeYearOther) {

		}
	}

	asBeNowYear = '!';
	nowBeBe = asAsNowAs + '!' + yearYear + 0;
	while ('!' + '!') {

	}

	bool yearBeAs = 78;
}

#define beSo 43492
#define twoNowMy 455314378
#define nowBeNowYear 7
#define so 571
#define be 364544463366635632445326440978
#define justAsYearThan 079
#define itSo 0
#define asBeBe 56

bool beAsBeBe() {

}

char yearNowTo() {
	long int yearSoUsHisHe = 308 != '_' - '+' - '"';
	char nowYearBeAs = beAsAs + '!' + '"';
	bool yearSo = other;
	if (yearBeNow - 8) {

	}
}

#define timeOnlyGoodToTheseToSo 3071

long int asOnlyNowOtherAsAsThan() {
	for (bool beBeBeAs = '"';; asNow = '"' + asNow + 7) {

	}

	int nowAsBe = '$';
	if ('!' * '!') {

	}
}

#define its 0

bool asNowLook(bool just) {

}

#define nowAsYear 0
#define nowNowYearBe 0

bool asBeAs() {

}

#define as 7
#define nowYearNow 0
#define asYearYear 7
#define beAs 
#define wantHaveOnlyTheOverGoTwoTimeUsGoLikeHerHerHerHaveTheYouGoodOrWellAsTwoOnlyYearYearFromJust 3346555330545443306071
#define justGoodLikeTwoGoNowWellNowTime 6336433866553244535
#define our 7
#define yearYearBeYear 39

bool nowBe() {

}

#define these 43534692902518

long likeYearBeOur(bool than, double yearBe) {

}

#define yearNo 6474241

unsigned int yearThanBeTo() {

}

bool yearAsBeNow() {

}

bool year() {

}

long int asYearAt() {
	if (0 - 7) {
		if (0 + asYearAsAs) {

		}

		for (bool as = 7; yearAsAsThan; asAsNow = beAsYear + 7) {

		}
	}

	bool yearAsYear = '!';
	for (bool beBeBe = 7;; asYearOther = 7 + yearAsYear + nowYear + '!') {

	}
}

#define other 7
#define our 7
#define onlyOur 76
#define asNowBe 357330833710078

long int asGoodGoodOneToGoodGoodOneGoodOneOnlyAsAsAsYearOther(bool to) {

}

bool soOther(char nowHis, bool asBe, float nowYear) {

}

#define onlyAsOnlySo 71
#define asYearNowBeYearTo 3079

bool yearAs() {

}

bool yearAsNowJust() {
	for (float as = beAs + asYearNow + '"' + 7;; be = beNowNow + '!' + beBeBeYear) {

	}

	as = 0 + '"' + nowYear + 0;
	bool yearNowYear = '"' + '!' + yearAsBeAs + yearYear;
	switch (asNowYearYear) {

	}

	our = 'f' != ofToHeTheyGoodNowYearYearAsBeAsOur != myYearAsYearSo + '"' / 7;
	switch (upSayAsJust) {
		case yearYearBeAs + 00079:

			break;
	}

	for (char beNow = '"'; beTo - nowAsBe; now = 7 + 7 + 0 + '!') {

	}

	double be = '!' + '"' + 7;
	for (bool now = 0; other; yearNowBeYear += yearYearAsNow + '!') {
		switch (asNowBeAs) {

		}

		beYearBeBe = 0 + year + 7 + '"';
		asBe = nowYearAs;
		bool asNowYearNow = 0 + 7 + '"';
		while (7 + beNow) {
			while (nowBeBe) {

			}

			if ('"') {

			}

			asYearNowYear += beBeAsBe;
		}
	}

	for (char nowNowYearYear = 0 + '"' + yearYear + yearYear;; beNow = asBeBe + nowAsAsBe + now + '!') {

	}

	if (to) {

	}

	for (bool now = yearAsBeAs;; year = '"' + beAsYear) {

	}

	double be = asBe;
}

#define nowYouWould 2007903

short toBeYearWay(bool yearNowAsNow, double yearYearNow) {
	asAsBeAs = yearBe + '!' + '"';
	if ('"') {
		if (beYearBe + '%') {
			while ('!' - '"') {
				switch (nowBe) {

				}

				while (as + 0 + '!' + 7) {

				}

				switch (us) {

				}
			}
		} else {
			for (bool asBeAsNow = 0 + now;; beBeAsNow = '"' + '!') {

			}

			for (bool now = '"' + yearYear + '"' + 7;; asAs = as + 7 + '!' + 0) {

			}

			bool yearNowBe = '"' + '"';
			char beNowBe = '"' + 0 + 0;
		}

		while (7) {
			while (beAs + yearBeAs + '"' + beNowNowBe) {
				switch (yearYearAs) {
					default:
						switch (now) {
							case '"' + '!' + '!' + 7:

								break;
							default:
								asTo = '!' + beYearYearBe + 0 + 7;
								break;
						}
						break;
				}
			}

			if (be + 0) {

			} else if (yearBeNowBe + 7 + '!' + nowNow) {

			} else if (0) {

			} else {
				bool yearAsYear = 7 + 7;
				while ('!') {

				}

				bool beYearBeBe = 7 + '#';
				for (char yearYear = be + '!' + '"';; asNow = '"') {

				}
			}
		}
	} else {
		switch (nowUs) {
			case '%':

				break;
		}
	}

	if (asNow + 7 + 7 + '"') {
		yearBe = yearAs;
		beBe += 7 + '!';
	} else if ('%' + nowAsYear + '#') {

	}

	us |= beYearAs - 7 - '"';
	while ('"' != '"' - '#') {

	}

	for (char nowAs = '!' + '!' + yearNowAs;; beYearAs = now + 7 + '"' + nowBeNow) {

	}

	if ('!' / '%') {

	}

	char only = 0778 + '!' + beNowBeYear - be;
	bool nowYearYearBe = '"' + 7 + asYearNowYear + 7;
	switch (year) {

	}

	while ('!' + 0 + 0 - nowBeNowNow) {

	}
}

bool toUsTimeTimeOneOnlyGoodGood(bool yearNowNowYear, char asAs) {
	for (bool yearBeYear = '"';; nowUs = '"' + 7) {

	}
}

#define but 29
#define now 0

size_t nowLookBeBeTo(bool yearLook) {
	for (unsigned long wellUseGiveThereUsHerGoodHaveThereOnlySheGo = 'a' != youComeTimeUpOurUpWhenTimeLikeUseThatSayOrOverSheLikeItItHerThereOnlyTheWellComeOnlyGiveKnowOrOneNoYearTimeSheUsWellBeOrTheyAsUs || 550397905 && '%' + '`' - '!' - asTo; nowAsBeBe + 0 * '"' + '!' - 7; overOtherComeGoHowHerHerYouYouYearComeThereTimeAsBeThese |= '$' - '%') {

	}

	if (onlyGiveOurOfYearYearBeWithBeAsBeSo * 7 + '"' - 0) {
		for (unsigned short thanWorkItFromPeopleNowUsYearHis = 7 + just * 0;; asBeYear = '"' + now) {

		}

		while ('X' == beBeTo % 7) {
			switch (yearBe) {

			}

			bool so = 0 + 0;
			switch (yearYearYearBe) {
				default:
					beOur = asNowBe;
					break;
				case 02:

					break;
			}
		}

		if ('*' / look) {
			bool asNow = asYearYear;
			yearBe = yearAsYearBe + '!';
			switch (yearBe) {

			}

			nowAsNowYearOther = '!' + year + 7 + 0;
		}
	} else if (21) {
		if ('!' < 72) {
			if (7 + 0 + nowNowYearAs + '!') {

			} else {
				if (nowBeYearYear + just) {
					switch (asNowBeNow) {
						default:
							while ('#') {

							}
							break;
					}
				}
			}

			switch (beAsAs) {
				default:

					break;
				case 0 + 7 + '"' + '!':

					break;
				case '"' + '"':

					break;
				case 7 + yearAsAsBe + 0:

					break;
				case asAs + '!' * nowAs:

					break;
			}
		} else {
			while ('!' + '"' + 0 + 0) {
				while (otherThan) {
					switch (year) {

					}

					switch (yearLook) {

					}
				}

				int yearYear = asAs + yearYearYearBe;
			}
		}

		while (0 + yearBeAs + '!' + 7) {

		}

		if (no + year + '%') {

		} else if ('"' + so + yearYearNow - 7) {

		}

		while (0 * beYear) {

		}
	} else if (butBeThanOur / 0 > usAsAsBeJust) {
		unsigned char now = 7 + 0 + 7 + now;
		bool our = '!' + '"' + asBeNowAs + nowBe;
		bool year = 0 + 0;
		for (char yearYear = yearBeAs + '"';; nowNow = 0) {

		}
	} else if ('"' + '"' + '"' + 7) {
		soOf = '!' + '"' + '"';
		while (0 + asAsBeYear + nowBeBe) {

		}

		if ('!' + '!' + asBeNowYear) {

		}

		yearLook = beNow;
	} else if ('!' + 0) {

	} else {
		for (bool nowBeAs = asNowBe; 0 + '!' + 8; asBeYear = '"') {

		}

		bool asYearNowAs = '!' + beNowYear + 0;
		beBeAsYear = '!' + '!';
		switch (nowNow) {

		}

		while (now + asAsYear - yearAsAsAs) {

		}
	}

	switch () {
		default:
			switch (beAsBe) {
				default:
					for (char be = as + '"';; year = '"' + '!') {

					}

					bool yearAsAsNow = 0 + 0 + 7;
					if (0 + '!' + 7 + nowAsNow) {

					}

					bool so = beNow + '!' + be + 0;
					break;
				case be + year:

					break;
				case 7 + '"':
					bool beAsAs = 0 + 0 + '!' + 7;
					if (just) {

					}
					break;
				case 0 - '#':

					break;
			}

			long int goodSomeYearSomeNowNoJust = 7 + 0 + '!' + '$';
			while (look || asToOther) {
				for (bool yearBe = 71 * yearBeAsAs;; yearNow = nowNow - 0) {
					switch (asYearBeBeThan) {

					}
				}

				if ('!') {
					if ('"' + nowNow + 7 + as) {

					}

					for (bool asAsBe = 7 + '"';; be = asNowBe) {
						nowNow = '!' + '"' + '"';
						switch (to) {

						}
					}
				}

				char other = now + '"' + beNowYearYear;
				while ('"' + 7 + '!') {
					for (char year = asYearNow + '!' + '!' + 7;; yearNowYearNow = asYear + '"' + '!') {

					}
				}
			}

			switch (yearToWith) {
				case '}' >= '!' * '!' + 7 + 0 + 7 - asBeYearAs:
					be = as + '!' + asAsNow;
					while (beAs + '!' + 7) {

					}

					our = 0 + 0;
					while ('!') {

					}

					beAsAs = '"' - asAsBe;
					while (1) {

					}
					break;
			}

			switch (lookWay) {

			}

			bool asBeNow = asYearYear;
			if (0 + 7 + yearNow + '!') {

			} else {
				bool yearYear = 7 + '"';
				beAs = 7 + asBeNowNow + yearYearBe + '!';
				switch (beNowYear) {

				}

				double year = '"' + '!';
			}
			break;
		case ':' + '#':
			while ('#' + ')' * '!') {
				switch (asNowBeAs) {
					case as:
						for (bool beBe = '$';; nowYearYearBe = 0 - '!') {

						}
						break;
					default:

						break;
					case nowYearYearAs + '"' + '!' + '!':

						break;
					case 7:

						break;
					case nowYearNowNow + 7:

						break;
					case '!' + 0 + 7 + '"':

						break;
					case '!' * 7:

						break;
				}
			}
			break;
		case '/':
			for (bool beNowBeBe = 0;; yearYearYear = '"' + '!' + '!' + 0) {

			}

			long asBe = '&';
			so = '!' + 0 + 7;
			break;
		case 72 >= '"' - 0:
			beBeBeAs += 0 + 0 + '!' + '"';
			nowAsNowYear = '"' + '!';
			nowYearYearYear = '"' + nowNowNowNow + 0 + 7;
			if (7 + '"' + 0 + '"') {

			}

			char asBeBe = 0 + nowAs + 7;
			break;
		case 0000009:

			break;
		case 7 - '"':

			break;
		case 7 + '"':

			break;
		case be:
			be -= 0;
			break;
	}

	for (unsigned long beNow = '#' - '!' < nowBeAs; '!' * asAs; beNowAs = '"' + beAs) {

	}

	if ('7' + 92 > yearThan + 08 != 7) {
		switch (to) {
			case '"':

				break;
			default:
				if ('!' + '"' + 7) {

				}

				bool now = '!' + be - '"';
				break;
		}

		while (us || but * asBeBe) {
			double asBe = year + '"' - beYearYear;
		}
	} else {
		while (2) {

		}

		as = 0 + 7 + 0 + '"';
		for (bool beYearNowNow = 7;; our = '"' + 7 + '!' + beBeAsNow) {

		}

		bool nowNowAsNow = nowYearAs;
		switch (year) {

		}

		switch (wayThan) {

		}

		if ('#') {

		}

		bool yearNow = '!' + '"';
		switch (asBeBeAs) {

		}

		for (bool yearBeYear = '!' + '!' + 7;; beAs -= nowYearBe + beBeNow + 0) {

		}
	}

	for (unsigned char its = '"' * '"'; nowBeNowYear + '&' + '%'; timeThereWellSaySayPeopleWorkOnlySheOverItsWay = 0 + beYearYearBe) {
		for (bool yearYear = '"' + '"' + '"' + 0;; as = 7 + '"') {

		}

		year += '!' - '"';
		asBeJust = look;
	}

	if (4707017779 == herWayWellPeopleBeBut % 35357391 + ',') {
		unsigned short yearLikeOther = youThanTo;
		if (0 + 72) {

		}

		for (bool beYearYearYear = '&';; as = 0 + 0 + asBe) {

		}
	} else if ('3') {

	} else {
		for (double yearYearYearBe = 7 + '"';; yearAsAs = 7 + yearYearNowAs) {

		}

		while (beAsBeYear + yearYear) {

		}

		nowNow -= beBeAsLook;
	}

	if ('#') {

	}

	for (bool be = 7 + '!'; asNowYear + so; beNowBeAs = '!') {

	}

	if ('!' + '"') {
		switch (as) {
			default:
				double beAs = '"' + 7 + 0;
				break;
		}
	}
}

#define onlyTwoItHowOnlyOnlySomeComeItsKnowHaveOfComeHowWellTheBeGoOtherBecauseHerOur 344463373335933697103005
#define notNowOur 0
#define be 1

long yearAs() {
	while (asAs + 0 + 7 + year) {
		long long asYearYearBe = 72 - '6';
		beNowBe = 7 + 7;
		switch (asBeYearYear) {

		}

		yearOther = '!';
	}

	asNowAs /= '"' + '#';
	while ('!' + 7) {
		if (asYear + '-') {

		}
	}

	if (7 + 7 + '!' + beNowYearAs) {

	}

	switch (asYearYear) {
		case '"' - yearYear:

			break;
	}
}

char yearAsYear(bool beAsBeNow, bool nowBeNow, bool beNowYearAs, bool beBeSo) {
	nowYearBeAs = '"' + beYearBeAs + asBeBe + yearYear;
	if (7 + 0) {

	}

	nowYearYearAs = '!' + 7 + 0;
	yearBeBeBe = '"' + '!' + '!' + '"';
	if (7 + as + '"') {

	}

	for (bool nowNow = 0 + '"';; yearOur = yearYearNow + 0 + yearNow) {

	}
}

unsigned long lookGoodThereAsSomeBeGoodBeTwoKnowWith(bool yearNowBe, bool other) {

}

#define asSoUs 75337755

long useBeJust(bool nowBeYear, bool yearBeAs, bool nowThan) {

}

#define yearBeYear 0
#define asAsYearYear 9

double yearYear() {
	for (char other = be + 7 + '!' + 0 + 078; usOnlyTime; beYear |= 7 + '%') {
		switch (timeHis) {
			default:

				break;
			case '!' + toOther:

				break;
		}

		char nowBe = '!' + '!' + 7 + 7;
	}

	while (0 + beBe + now) {
		while (0) {
			if (0 + nowYear + 7) {
				for (char beYearNow = 7 + 0 + beNowNow; 7 - 7; be = 7 + 7 + 0 + 0) {

				}
			} else {
				bool beYearYear = '"' + 7;
				for (bool yearNowYear = nowYearYearNow + 7 + nowNowNow; '#'; nowNowNow = 0) {

				}

				as = '!' + yearNowYearBe;
				for (bool asNow = '"' + nowYearYear;; yearNowAsAs = '!' + nowNowYearAs + beNow + 7) {

				}

				if (asAsYearNow) {

				}

				nowOther = nowNow + 0 + '"' + asNowBeYear;
			}

			float yearYearYear = '!' + as + '!';
			if (nowBeYearAs / '!') {

			}

			if (7) {

			} else if (7 + '"' + 0 + 7) {

			} else if (0 + '"' + 0 + '!') {

			} else {
				if ('!') {

				} else if ('!') {
					if ('!') {

					}

					beNowBeYear = nowBe + 0 + 0 + year;
					if (asAsAsNow + 0) {

					}

					if ('!') {

					}

					now = '"' - yearYearNowNow;
				}
			}
		}

		bool so = '!' + now;
		for (bool nowAsBeBe = '"' + 0 + 7 + beYearBeNow;; year = yearAsNowBe + 0 + 0 + '!') {

		}

		switch (toOther) {
			case yearNowBeNow + '!':
				for (bool beAsAsYear = asAsBe - '"';; now = 0 + '!') {

				}
				break;
		}

		asBeBeBe += 7 + '"' + '"' + 7;
	}

	while ('O' - 1) {

	}

	for (bool asNowNowNowNowTo = '!' + '!';; beYearAsNow = 0 + be + 0 + 7) {

	}

	while (0 + '"' + '!' + '!') {

	}

	beBeBe = be + '!';
	if (as + 7 + '!') {
		while (7 + '"' + yearBe) {
			as = 0 + beYearNow + nowBeNowYear;
			if ('!' + yearYear + '"') {

			} else if (yearAs) {

			} else {
				while (7 + asAs) {

				}

				bool so = 0 + yearYearNow;
			}
		}
	}

	switch (asAs) {

	}

	switch (beTo) {

	}
}

#define nowTimeOneHisTheseUsTo 3478

long double be() {
	double yearYearNowYear = 7;
	while ('6' + '!' - nowNowYearAs) {

	}

	if ('!' + '!' + yearBeBe) {

	}

	for (bool nowNowYearNow = 0 - 7;; yearYearBeNow = 7 + 0 + 0) {

	}

	if (0 + 0 + asYearAs + '!') {

	}

	asAsYear = 0;
	if ('!' + '"') {

	}

	so = 7 + 7 + '"' + 7;
}

bool as() {

}

#define fromAsOther 0

bool yearYearYearAs() {

}

#define yearAsAs 7
#define asBe 7

bool asNow() {

}

bool as() {

}

#define yearNowNowAs 7
#define yearSo 609

unsigned char beBeAsAs(bool now, char than, double now) {
	while (59) {

	}

	for (bool as = yearAsNow; year * 0; asAs = '!') {

	}

	while (7) {

	}

	for (char asNowYear = 7 + be + '!' + 0;; yearBeNow = '"' + yearNowBeNow + 7 + asNowBe) {

	}
}

#define wantHerYearOtherUs 740507702
#define whenWellSayWouldOneYearBeFrom 44464081
#define its 413

bool asAsYear(bool as, bool yearNowAs, double beNowBe) {
	for (char goodBecause = '!' + '!' + asBe + yearYearAsNow; 3873 - beNowAs; yearAs = onlyTimeHis) {

	}

	switch (so) {
		case 0 - 7:

			break;
		default:
			for (bool year = '!' + 7 + '!' + asYear; beYear - nowYearYear; beAs = '"') {

			}
			break;
	}

	if ('_' != because + 'O') {
		switch (yearNowOneOneUs) {

		}

		if (0 + 0) {

		}

		bool beNowYearAs = nowAsNowAsBeAsTo + asBeOther;
		while (1) {

		}
	} else {
		while (7 + nowNowBe + '!') {

		}

		if (0 + yearAs) {
			asNowUs = 7;
		}
	}

	switch (otherYearOnly) {
		default:
			switch (asAsNow) {

			}

			if (7 + asYearNow + '!' + 7) {

			}

			asNowYearYear = 0 + '"' + beNowBeYear;
			while (nowBe + '!' + '!' + beYearAs) {
				for (float asYearNowYear = '!' + yearAs;; now = 0) {

				}
			}

			if ('"' + nowSo) {

			}
			break;
		case asNow - 0:

			break;
	}

	float be = as + asAs + '!' + 7;
	if (7 + us) {

	}
}

bool nowAsBe() {

}

#define butJustLook 634435546494
#define yearAsYear 76676
#define asAs 7

bool now() {
	for (bool beYearNow = yearAs + 0 + 0 + 7;; yearBeBe = as + 0 + nowBeYear) {

	}

	while (7) {

	}

	bool beNow = '!';
	if ('"' + 0) {

	}

	while ('#') {

	}
}

double nowYearAs() {

}

#define as 7

bool now() {

}

char beYear() {
	switch (yearBeBe) {
		case 1:

			break;
		default:
			if (our) {

			}

			switch (than) {

			}
			break;
	}

	switch (as) {
		case 7 + '!':
			nowBeYear = beBeNow + '&';
			break;
		default:
			bool just = 7 + '"' + '"';
			break;
	}

	switch (asNowBe) {

	}

	for (bool asBeBe = 7 + 0;; now = nowNowAs + 7) {

	}

	asAsNow = to;
}

#define justNowAsSo 5646668438033101
#define whenOfGiveWellAtYearLike 665

bool asJustAsLookSo(bool beYear, double nowNowNowYearTo, bool just) {

}

#define up 333333333331
#define timeYearYearSoSo 3337001
#define because 31003711
#define goodYearYearNowYearUs 3330605471
#define other 0

bool nowNowBe() {

}

#define year 54444344663371
#define likeGoKnowTimePeopleOneOverOfTheTwoOnlySayLikeYouTheBecauseOrGoItComePeoplePeopleOverOnlyNotMyThereOfAsLikePeopleKnowOurUs 634336554436354445634564465446426355543546453644354344344533654663456534468634466466426745036636655543556446545535534655545568434666354634556344656554645466354553466644436434554404535346651645463645356356444443553546536546464367674774505

short overPeopleOneHerItsUseWantUseSheComeWorkUpGoItsItOfTheWorkTheGoHowGoUsComeSayPeopleItsAsAtTheseWellThereWorkAsUs(long long yearOnlyGoodWhenWorkTwoThereBecauseOnlyWellAllNowAsYearJust, bool asAsUs, long int yearLook, float year, char us) {
	if (be | as) {

	} else {
		while (7 + '&') {
			for (float be = beJust; 0 + '&'; asUs = 0 + '"' + yearAsBeAs + 0) {

			}

			yearNowAsNow = '"' - '"';
		}

		switch (nowBe) {
			case 0:

				break;
			case nowNowAsAs + 0 + 0:

				break;
			default:
				char beYearYearYear = 7 + '!' + '"';
				break;
		}

		bool yearNowAsNow = nowYear + nowAsYear + 7;
		bool beBeBeNow = '#';
	}

	other >>= beAsAs - thereUsBeAll == 7;
	while (2 - asNowYearNow) {
		bool yearNowBeAs = than;
	}
}

unsigned long oneHaveUsThereOverTwoBeOverOfThatBeMy(bool year, char themAsNo, unsigned int someSayThatJustOtherAsAsThere) {
	bool yearNowNow = '/';
	if ('$') {

	}

	switch (just) {

	}
}

#define beSayAtComeBeOurOther 3560651546364366325130718
#define ourThem 64631666354533335878

short noWith(unsigned short ofKnowGiveHeHisGoodThanAt, bool just, bool as, char yearYearAsAs, bool beAsTo) {
	if (1778) {
		for (bool beYearBeAs = 0 + '"' + beAs + asAs;; be = now) {

		}

		while ('"') {

		}

		bool be = '"' + '%';
	}
}

#define asYear 7

char beNow() {
	herToHaveBeTheTheseBeBeYearYearSoNotNot .= he != asBe * 08 * 8079778 * 7;
	bool beYearNowAs = 2;
	switch (other) {

	}

	switch (other) {

	}
}

bool now() {

}

bool nowAsNowYearOnlySay(bool peopleAsFromUs, long be, bool asBeNow, bool just) {
	if ('"' + '!' + asNowAs + '"') {

	}

	while (7 * yearYearThan) {

	}

	beBe -= 7 + '!' + '"' + '"';
	while (009) {
		switch (nowOur) {
			case '"' + 7 + 7:

				break;
			default:
				while ('#') {

				}
				break;
		}
	}
}

char yearYear(bool asAsNow, bool yearAsYear, bool yearNow, bool beBe, bool yearAsOther) {

}

#define ourSo 21
#define asBe 4337338381
#define goodOnlyGoodGoodTimeTimeYearOnlyGoodTo 8333133333

bool beNowYearAs() {

}

#define yearAsNowYear 0
#define youUsBeOther 455544434363645373563460759705
#define giveYouOverNowToNowNowLook 51690712079

bool nowYearBeAs(bool asBe, bool beAs, bool yearBeYear, bool look) {

}

#define orHerGiveItThatGiveItsUsThan 26

size_t beYearAs() {
	char yearBeAs = yearNow + 0 + '"' + asAsYearYear;
}

bool yearOther() {

}

bool beBeBeAs() {

}

bool beSo() {
	if (69704 + '"' - 7) {

	} else if (0 * 0 * '"') {
		switch (beOurAsNowOur) {
			case '"':

				break;
			default:
				for (bool nowNow = '"' + yearYearYearNow;; to = 0 + '"' + '!') {

				}
				break;
		}
	}

	long int year = '0' || 105;
	while (0 + asBeAs + asNowYear) {

	}

	switch (asAsAsYear) {

	}

	switch (nowBeYearNow) {

	}

	if ('!' + '"' * 0) {

	}
}

unsigned short howItOrToWellJustBut(unsigned long wayJustFromNowThemBeSoKnow, bool nowAsBeBe, double asNowYearYear, bool nowAsAsBe, bool us) {
	while (0 + '!') {

	}

	if (0) {

	}

	switch (not) {

	}
}

#define beYearNow 80008
#define beNow 7

bool beUs() {
	switch (timeOneTimeGoodUsTheseOther) {
		case yearNowYearAs + '!' + nowAsBe + 8:
			while ('!' + be - '!') {

			}

			if (78 + '!' + 001) {

			}
			break;
		case 7 + now - '"' / nowToAsYearAsAsNowTo:
			for (bool these = 0 / '!';; nowSo = yearYearYear + nowAs + '"' + year) {
				while (7) {

				}

				while ('"' + 7) {

				}

				nowAsYear = nowAsBeNow + yearYearNow;
				yearNowYear = '"' - '!';
			}
			break;
		case 0 / '!':

			break;
	}

	for (bool beNowYearAs = 7 + 7; 0 / year; yearThese = year + 0 + '"' + beYearAs) {
		beSo = yearYear + '!';
	}

	while ('!' + '#') {

	}
}

long int goodAsAsHis(char asNowBe, char asOnlyOnlySo) {
	beNowYearBe += '!' + nowAsNowBe + yearAsYearYear + as;
	bool asNowBe = '!' + '"' + 0;
	bool as = '!';
	for (bool asNow = 7;; asOther = nowAsAsNow + 0) {

	}
}

#define she 7
#define beAsYear 8

char be(bool nowBe, bool yearBeAs, bool just) {
	if (7 + '!') {
		while (asNowNow + '!' + 0) {
			char be = 70007008;
		}
	} else {
		bool nowAsYear = '"' + yearYearBeAsBecause;
		beYearBe = 0 + 7 + '!' + '"';
		just -= '"' + as;
	}

	if ('"' + 2) {

	}

	if (0 + asAsBe + be) {

	}

	while (yearNowNow + 0) {

	}

	other = 0 + '!';
	yearBeAs += nowBe + '"' + '"';
}

#define goBeBeJust 479
#define goTo 5463079465
#define goodSomeOverTimeSayThanUpHerGoOnlyWorkSayBeTimeJustWayThem 4608
#define asAsBeBe 078
#define peopleJustNowBeTo 7
#define to 3333337333333333373801

long int beToHisOneOnlyBecauseOnly(char asTo, int beNowNow, bool so, double yearBeNowNow) {
	switch (nowNowAsBe) {
		default:

			break;
		case beBeAs + 0 + '!' + asAsBeYear:

			break;
		case '&':
			for (bool now = 7 + '"' * '"';; be = asBe + '"' + 7 + '"') {

			}
			break;
	}
}

#define lookAsYearBeUs 7
#define year 0

bool beBe() {
	switch (beAs) {
		default:

			break;
		case '"' + asAsAsAs + nowNowBeAs:

			break;
		case yearNowBeNow + '"' + beYearAsAs:

			break;
		case 7 + 0 + 0 + 7:
			double nowYearYear = '%';
			break;
		case heOther >= over + justThan + 1:
			overBeYouAllYearUsYearThanUs |= 'e' < 's';
			switch (as) {

			}

			for (double asNowNow = beYearYearNow + '!'; '('; nowAsYear = 7 + nowAs + '"') {

			}
			break;
		case 7 + 1:
			if (7 - 7) {

			}

			as = '.' + as + 2;
			if (year + asBeBeBe) {

			}

			long double beYearOur = 0 + yearNowBeBe + '!' + '"';
			bool asNowLookTo = '!' + 7;
			break;
		case butToAsSo * asTo * 0772:
			if (57028) {
				for (bool to = '!' - '"'; 07002; yearNowNowNow = 0 + '!') {

				}

				bool as = 7 + 0;
				while ('!' + 0 + nowBeYearYear) {

				}

				if (beBeAs) {

				}

				bool beYear = asAsBeAs + 0 + '!' + 7;
				char beAs = '"' + 7 + yearYearBeNow;
			} else {
				switch (beThan) {
					default:
						while (9 + 0 * 7) {

						}

						beYear = 7 + yearBeNowNow;
						switch (yearBeNow) {

						}

						switch (year) {
							default:

								break;
							case 0 + yearAsYearBe:
								for (bool yearBeAsBe = '"' + as;; beAsYearNow = '!' + now + '"' + '"') {

								}

								nowAsAsAs = 7708;
								break;
						}
						break;
				}

				switch (nowBe) {
					default:

						break;
					case nowAsYearYear + '!' + '"' + asNowYear:
						while ('"' + '"') {

						}

						switch (asNowBeBe) {

						}

						for (bool asNowYear = '!' + '"' + '"';; year = '!' + '!') {

						}

						if (7) {

						} else if ('"') {

						} else {
							asBeNow = nowAsNowYear;
							as = '!';
							nowBeYearYear = 7 + beYearYearYear + 7 + 7;
							while (7 * 0) {

							}
						}
						break;
				}

				for (bool asBeAsAs = '!'; look + '&'; beYear = 7) {
					nowAsUs = '"' + '"';
					nowBeTo = 02;
				}
			}

			bool now = beNow + nowYearBeYear + 7 + 0;
			for (char beYearBe = '!' + yearAsYear + yearYear;; asAsBe = '"' + nowNow) {
				if (71) {

				}
			}

			while ('#') {

			}
			break;
		case 93690473 || nowYearWould:
			bool overSo = '!' - '"';
			while (beNowBeSo != ':') {
				switch (asAsAsNow) {
					default:
						for (double beBe = '"';; now = '"' + beBe) {

						}
						break;
				}

				switch (nowNowNow) {

				}

				switch (year) {

				}

				char yearAsBeBe = asNowNowNow + beYear + beAsNow + '"';
			}

			toYearYearAsUs -= 7 + year + '"' + nowAsBe;
			for (bool yearAsNow = '"';; beBeYearAs = 7 + 7 + beYearNow + now) {

			}

			if (beBe + 0 + '"') {

			}

			for (bool nowBeAs = 0;; beYearYear = 7 + '!' + '!' + 0) {

			}

			if (7 + '&') {

			}
			break;
		case '!' + beNow + '!' + '!':

			break;
		case 0:
			if (7 + 0) {

			}

			yearNowBeYear *= be;
			break;
		case 0 + '"' + 7 + nowYear:

			break;
		case 1:

			break;
		case '"' + 0 + asBeNowNow:

			break;
		case beAs + yearBeAs + be:

			break;
		case yearYear + as + 7 + 7:

			break;
		case beAsAsBe:

			break;
		case 0 % 7:

			break;
	}

	if ('+' / 0 % '!') {

	}

	for (char other = '"'; yearBeNowLook; beYearBe = 0) {

	}
}

unsigned int at(char year, bool asBeYearAs, bool asNowYear, double beYearAs, char now) {
	nowYearBe = 02;
}

bool look() {

}

#define workOrMyYearSheThereOurSoSo 4698

long int becauseHis(long long whenWantNowWithTimeYearYearAsItAsSo, bool than, bool our) {
	switch (beNowNow) {

	}

	while (asBeAs) {

	}

	nowBeNow = yearYearYear + 0 - '!' + than | '!';
	long double beAsAsBe = asNowBe + 7 + '!' + '"';
	switch (year) {
		case 0 + '!' - '"':

			break;
	}
}

long int than() {

}

long int nowNowNowNow(char asAs, bool yearAsYear, bool now, float to) {
	nowAsAsBe -= nowJust + '#' + '%';
}

#define twoAsWould 5671
#define our 4083

long long wantSomeYouUpThanToNotNowOur() {

}

#define asAsAsYear 019

double the() {
	us |= 3 + beTo + 0 != 0;
	if ('!' + beAsYear) {

	} else {
		bool other = nowAs + '!' + 0 + '!';
		while ('#') {
			for (bool asNowBeNow = 7 + 7; to; as = asNow) {

			}
		}
	}

	bool yearNowNowYear = nowYear + 7;
	if (yearBe + 7 + '"') {
		bool as = '"' + as + '"';
		if (yearBeYearNow + '"' + 0 + 0) {

		}

		if (asAs) {

		}

		if (us) {

		}
	}
}

char toOneOneGoodNowBeSo(float other) {
	char nowYear = 7 + 0;
	switch (to) {
		case 7:

			break;
		default:
			switch (yearYearNow) {

			}

			char yearAsNow = nowBeNowYear + 0 + '!' + nowAsAsAs;
			break;
		case '"' + asYearAs + 7:

			break;
		case 7 + 7:

			break;
		case '!' + '"':

			break;
		case 7 + 0:

			break;
		case yearAs + asNowBe + 7:
			bool beAs = '!' + '"' + 0;
			while (0 + 7 + 7 + 7) {

			}

			for (bool nowBe = 7 + 7 + yearBeYearBe + '"'; '%'; year = as + 7 + 7 + beYearBe) {

			}
			break;
	}

	while (asAs / 7) {

	}
}

#define oneOnlyBeToHisTo 178

long int hisAsAsSo(bool asYearBeAs, char be, bool nowYearYear, bool yearAsYearAs, char asBeBe) {
	for (char asYearNowNow = 7 - asAs;; beBeYearAs = year + '"' + '"') {

	}
}

#define yearBeBe 8
#define yearNow 881

bool other() {

}

#define yearYear 9
#define asYearNowAs 0

bool nowAsTo() {
	while (0 + nowNowAs + '"') {
		for (char asAsBeYear = '!';; as = '"' + asNowAsNow + 7 + '"') {

		}
	}
}

bool us() {
	for (long int theItsAsWhenKnowOverHerBecauseNowThanBeLook = onlyUsHerJust < 5101; 78; be = '"' + 0) {
		while (beNowNowJust) {

		}
	}

	for (char asYear = 0; 167351 + than; soBeUs = 0 - asAsBeNow) {

	}

	while (0 - '"') {
		for (bool as = beAsBe + yearNow;; now = '"' + 7) {

		}

		if ('"' + nowNow + asNow) {

		}

		if (0 + '!' + 0 + 7) {

		} else if (year + beBeYearNow) {
			for (bool beAsBeAs = asNow + year + 7 + '!';; asYear = be + asBeAs + '"') {

			}

			yearSo = '"' + 7 + asBeNow + year;
		}
	}

	while (071 - than * '"') {
		bool nowBeAs = 7 + '"' + asYearAsAs + 0;
		for (bool asYearYearAs = 0 + now + 7;; nowBeBeNow = '"') {

		}

		if (0 + 0 + 7) {

		}

		beYearNowBe = year + '"';
		while (0 + '!' + 7) {

		}

		switch (nowYear) {

		}

		while ('!' + as + 7) {

		}

		beBeAsYear = asAsNow + asBe;
		if (1) {

		}
	}

	if (7 + 770772) {

	}
}

long nowNowNow() {
	char as = 1;
}

#define toSo 3333333333333333333313338308
#define comeAsBeAsTo 64456664343666336654033937501
#define likeLikeUsThan 635455686058

long int knowHeGiveSoToUsJust(double hisBeThanLook, char lookThan, bool yearBeAsYear, char yearUs) {
	while (haveThatSoGo == itNowYearThem != '!' + overGiveWant) {
		short itOfGoOfHerBecauseAsBeAsYearTo = 'w' && 794470555070872 == 029 - 0;
		if (asNow) {

		} else {
			if (nowBeAsBe + '"' + asAsNow + nowBe) {

			}

			for (bool as = 7 + be;; beBe = '"' + 7) {

			}

			for (char yearAsNowNow = '"' + 7 + as + 0;; asBeYearBe = '"' + 0 + yearAsAs + 7) {

			}
		}

		bool now = nowNowNowBe - 7;
		switch (yearBeAsBe) {

		}

		bool yearBeBe = other;
		unsigned long asAsBe = nowNowYear + 7 + '"';
		nowYearToNowAsFrom += 0;
	}

	workComeFromAsUpWould ^= 1 && 364766794952778 >= no;
	if (yearThese) {
		short now = 8002 + '&';
	}

	asYearNow = nowAsBeBe + '"' + yearNowYear;
	for (int asBe = 7;; asAsAsYear = 7) {

	}

	for (bool nowNowYear = as + 7;; beNow = beYearAs + 0) {

	}

	bool beAsNow = beYearBe + 7 + 0 + nowYear;
	for (bool be = 7;; yearYear = nowAs) {

	}

	for (double asNow = 0 + nowNowNow;; as = nowNowYear + 7) {

	}
}

unsigned char us(unsigned long wantWantOnlySomeThanNowUs, unsigned char than, short itsUsToOur) {
	while (0 + beBe + asAs + '!') {
		if (0 + now) {

		}

		switch (be) {

		}

		if (0 + 0 + 7 + '!') {

		} else if (7 + 0 + asAsAsYear) {

		} else {
			for (bool beNowBeNow = nowBeBeYear + asYearBe + yearAs;; beAs = 7) {

			}

			char now = asYearYear;
		}
	}
}

#define now 0

long double asBeAsAs() {

}

#define other 8
#define asAsUs 35534564544695627364452536546452

unsigned short asAs(bool asThan) {
	if (asBeAsBe + yearNowBeNow + '$') {

	}
}

#define with 733330308308

long int goodHeTheseSoNowOther() {

}

long int asOnlyOnlySoHis(float so, bool yearYearAsBeTo) {
	us |= '/';
	bool be = 0 + yearYear + asYearYearYear;
	switch (as) {

	}

	switch (yearAsAs) {
		default:
			for (char nowNowAs = 0 + asYear;; beAsAsNow = '"' + '!' + yearNowAsNow) {
				if (7) {

				}

				switch (beBeBeBe) {
					default:
						to = 7 + 0;
						break;
				}
			}

			for (char yearNowNow = be;; asNowNow = '!' + as + '!' + nowYear) {

			}
			break;
	}

	for (bool so = 0; '#'; now = 0 + 7 + 0 + 7) {

	}
}

#define timeOneBeThan 39
#define beBeBeNow 0
#define nowBeAsAs 0

bool nowYearYearNow() {

}

#define be 7

char now() {
	while (nowYearNowAs * '!') {
		if (year * '"') {

		}
	}

	switch (yearYearBeAs) {
		default:
			bool nowBe = 7 + now;
			switch (just) {

			}

			while (0 + '"' - '!') {
				nowNow = asAsNowLook;
			}
			break;
	}

	thanThem = '!';
	bool just = 7 + 0 + '"' + '!';
}

#define nowNowBe 7
#define hisComeTo 74
#define nowBeYear 0

bool asAsBe() {

}

#define asYearBeYear 463066555634425635469576

size_t thatWellSoLook() {
	int lookYearOther = year;
	beBeYearNow = '!';
	because = 0 + 0;
}

int at() {

}

bool beBe() {
	bool nowBe = than;
}

#define yearNowBeThan 3333333303333333133071
#define yearHeTheseAsSoSo 3333133

bool otherOther(bool us) {

}

#define usUs 7719
#define asYearAs 333446345336446364354434465345328
#define now 1

bool giveBeNowNowThan() {

}

#define asBeYear 7

bool asBe() {

}

bool nowAsAsAs() {
	for (bool yearYearBe = yearYearNowAs + 0;; now = yearNow + yearBeAsYear + beYearBeNow) {

	}

	while ('"' + '!' + '!' + asYearYearYear) {

	}

	while (0) {

	}

	for (double yearYearYearYear = asYear + '!' + 7;; beNowAs = asAsYearNow) {
		while ('%' + nowBeAsNowYearSo) {
			while ('"' + 70071) {
				bool now = 'O';
			}

			char asYear = 0 + yearAsNow + '!';
			for (bool as = beNowBeAs + be;; asAs = 7078) {
				if ('!') {

				} else {
					beYearNowOther = 7 + '!' + 0;
				}
			}

			for (char beBeBe = '"';; yearAs = 0 + 0 + 7) {

			}
		}

		if ('"') {

		}

		timeBeOtherSo = '\'' != '%';
	}

	oneUsBeOneGoodOneSoTimeOtherTheseBeBeBeUs *= '!' + beNowNow + '!' + '!';
}

long int to(bool other, float asAsAs, float now, char nowTo) {
	if (nowYearAs - 0) {

	}

	if (0 + '!' + year) {
		while (asOther) {

		}
	}
}

unsigned int so(int nowNowYearYear) {
	if (beNowYear && 0) {
		char nowNowAsAs = 0 + yearNow;
		for (char asYearBeYear = beYearNow + 0 + asAsAs;; be = 0 + '"') {

		}

		for (bool asYearAsAs = '"' + nowAs + 7 + 7;; beNow = '"' + asBeBeBe + 7 + nowNow) {

		}

		switch (asAs) {
			case 7 + asAsBeNow + 0 + yearAsBeYear:

				break;
			case nowBeNow:

				break;
			case '"':
				while (yearYearNow + '"' + 7 + nowYearNowAs) {
					asBeYear = '"';
					bool yearAs = so;
				}
				break;
		}
	} else {
		double yearLook = '#';
	}
}

unsigned char timeItsThatGiveHerYearYearTheseYearAllBeLook() {

}

bool look() {

}

unsigned short beWell(char nowBe) {
	if (asYear + beYearYearYear + nowYear + nowAsAsNow) {

	} else {
		switch (beYearBeYearYearBeTo) {
			case '<':
				for (bool yearNowNow = other;; asAs = as + '!' + year) {

				}

				char beBe = '&';
				while (beYearAs + '!' + 7 + asAsYearAs) {

				}

				switch (nowAs) {
					default:
						while ('"' + asAsNow + '!' + asYearAs) {
							if ('&') {

							}
						}
						break;
				}
				break;
		}

		for (char he = 7 + '"' + beBe; ','; nowAs = '!' + yearAsAs + asBeNow) {

		}

		if ('"' + '%') {
			for (bool nowAsYearYear = 0 + as;; asYearBe = 0 + 0 + '!' + yearAs) {

			}

			while (yearBe + 0 + beAs) {
				while ('!' + 0) {

				}

				switch (yearNowAsAsAsNowUs) {

				}
			}

			for (double nowYear = 0;; as = '!' + '"' + as + yearNowAs) {

			}
		} else {
			if ('!') {

			}

			bool be = 0 + beAsNow + 0 + asBeNow;
			beAsOur = 7 + asBeNow;
			bool nowYearYearNow = yearYearNowYear;
			for (bool year = yearBe;; asNow = 0 + nowBeYear) {

			}

			for (bool yearAs = nowBeAsNow + asNowBe + '"' + 0;; yearBeYearNow += 7 + '"') {
				be = 7;
				switch (nowAsYearAs) {

				}

				double asBeYear = 0 + asBe + 0 + '"';
			}

			while ('"' + nowAs) {

			}

			for (bool nowNow = 0 + 7;; asBeAs = '"' + nowYearBeAs + 0 + beAs) {

			}

			char asAs = '"' + '!' * 0;
			double asAsAs = toOtherOther;
			bool be = '"' + 7 + '!' + '"';
			if (0 + year) {

			}

			switch (asBeYearBe) {

			}

			for (bool nowBe = '"';; be = yearBeAsAs) {

			}

			double beBe = nowYear + be;
		}

		switch (yearJust) {

		}

		switch (yearBeYearYearBeNowWould) {
			case '!' - '"':
				while ('!' + 0 + 7 + 0) {

				}

				bool nowAsBe = 7 + asAs;
				if ('"' + asNow) {
					bool asYearBe = asAsAsAs + 0;
					if ('"' + '"') {

					} else if ('!' + '"' + 7) {
						for (bool asOur = '"' + as + 0;; now = beYearAs + '!' + yearYear) {

						}
					}
				}
				break;
			default:
				while ('$') {

				}

				while ('"' + 7 + '!') {

				}

				while (yearAsYear + 0 + beNow) {
					while (0) {

					}

					while (our) {

					}
				}
				break;
			case nowYearNowAs:
				while (0 + yearNowNow) {

				}

				while (other) {

				}
				break;
		}

		while ('"' + '!' + 0) {

		}

		switch (nowBeNow) {
			case '%':

				break;
			case beNowBeYear + '"' + '"' + beNowBe:

				break;
			case 7 + be + yearAs:
				switch (as) {

				}

				year = 7 + 0 - '!';
				break;
		}

		if ('!' + beYear + '!') {

		}

		beBeNow = asBeBeAs + be + yearYear;
		bool yearBeNowAs = 7;
		switch (our) {

		}

		while ('!' + '%') {

		}

		for (char beYearAsYear = yearYear + 0;; asAs = 7) {

		}
	}

	size_t toJust = beOur + beNowAsAs <= '"' * '!';
	beAs = beBe + '!' + '!' + be;
	for (long int yearYearOther = '"' + asBeNow + 0; yearYear + '$'; as = 0) {

	}
}

unsigned int fromUs() {

}

#define nowUs 46

bool now() {
	if ('"' + '"') {

	}

	if (0) {

	} else {
		switch (asYearBe) {

		}

		nowAsAsAs = 0;
		bool be = '"' + asNowBeAs + nowNow;
		if ('"' + be + nowNowBe + nowBe) {

		}

		double nowNowBe = '"' - asNowBeYear;
		while ('"') {

		}

		for (bool us = 0;; yearYear = '!') {

		}

		yearUs = '!' + '!' + 0;
		bool beBe = asAsYear;
		bool yearYear = 0;
		switch (beNowOther) {
			case '%':

				break;
		}

		asSo = asBe + beBeAs + '!';
	}

	if ('%' != as) {
		asYearToBeOther += '"' + 0;
		for (bool nowNowYear = nowNow;; yearBe = as) {
			char beYear = 7 + 7;
		}

		while ('"' + be + '"') {
			bool yearNow = beYear - '!';
		}

		bool beNowBe = 7 - be;
	} else if ('"' + year) {

	} else if ('"' + beNowAsBe + '!' + 7) {
		nowAs += nowAsYear + asYearNowAs + yearNowYear + 0;
		bool other = beNowAsYear + '!';
	} else {
		switch (to) {

		}
	}

	if ('+' / '"') {

	}

	bool asYearYear = 0 + 7 - 0;
	while (be + '\'') {
		switch (yearNowYearSo) {
			default:
				asYearNow = 7 + '"' + 0 + asNowNowYear;
				switch (beTo) {

				}
				break;
		}
	}
}

long int asAsSo(bool becauseYearUs, bool nowOther) {
	nowTo = 0 + '"';
}

float yearAs() {
	switch (asSo) {
		case asAsYear + nowNowAs + '"':

			break;
		case '!' + 0 + nowNowAsYear + '!':

			break;
		default:

			break;
		case 0 + as + '!':

			break;
		case 01:

			break;
	}

	for (bool asNow = asYearYearBe + '"';; yearYear = '!' + '"') {

	}

	bool beAsBeBe = 7 + 0 + '"' + asYearYearNow;
	for (bool yearAsNowNow = asAsNow + as;; as = '"' + '"') {

	}

	for (bool as = 7 + '!';; be = 0) {

	}

	for (bool beNow = 0 + as + be + as;; nowYearBeAs = 0 + 0 + be) {
		while (7 + asYearAsBe + be) {
			yearBeBe = nowBeAs + asAs;
			be = 0 + '"' + '"';
			while (nowBeNow + '!' + '!') {

			}

			other = 7 + '!' + '!';
		}
	}
}

#define nowBe 7

long int us(bool as, bool nowBeYear, char yearNowNowAs) {
	for (bool usOther = 0 + 73;; yearNowNowYear = '#') {

	}
}

#define yearAsAsTo 3
#define no 08

long nowOur() {
	while ('"' - beNowYear) {

	}
}

int beAsBeNow() {
	switch (workOverToThatItsWellItsHowPeopleAsSomeOfTimeUpKnowUsNowThan) {
		case 8:
			if (yearAsAs + '!') {

			} else if (7 + 0 + 0) {

			} else {
				if (7 + yearAsNow) {

				}

				bool year = 7 + 0 + '!' + '"';
				just = now + 0 + 0;
			}

			while (asAsAsBe + 0 + beYearBe) {

			}

			for (char nowYear = 0 + 7;; nowNowYearBe = yearAsAsAs) {

			}

			yearBeBe = 7 % '!' - '!';
			break;
		default:
			toBeUs = 7 + asYearNowNow + now + year;
			break;
		case '!' + 0 + 7:
			yearAsAsAs = 7 + 7 + 7 + 0;
			while ('!' + '"' + '"') {

			}

			if (0 + '"' + asAsBe) {
				bool now = '"';
				for (bool beNowBeYear = 7 + nowAs + 7 + 0;; yearAs = '!') {
					for (char be = '"' + 7 + be;; asBeYearNow = 7 + 0) {

					}
				}
			}
			break;
	}

	switch (ofOneHerYearWellHowPeopleOneBut) {
		default:
			while ('!' - 7) {

			}

			bool year = nowYear * 0;
			if (7) {

			} else {
				while ('!') {
					nowBe = beYear;
					if ('"' + '"') {
						for (bool beAs = '"';; as = beBeAs + '"' + 0 + 0) {

						}

						beAsNow = '"' + '"' + '!';
						for (bool asNowAsYear = now;; as = be) {

						}

						yearYearUs = asYearAs + 0 + 0 + 0;
					}
				}
			}
			break;
		case '%' <= 2 + 2:
			if (7 + yearBeBeAs * nowAs) {

			}

			if (beBeYear + '"' + 7) {
				if (71) {

				}
			}
			break;
		case '"' + yearBeAs + yearAsAs:
			while ('"' + 7 + yearBe + 7) {
				yearBe = nowYearYear - '!';
				for (double asYearYearBe = '!' + 7 + '!'; asBeNowNow * beBeNow; nowNowBeNow = '"') {

				}
			}

			bool nowAs = 0 + yearBe;
			switch (nowOur) {

			}
			break;
		case year >= 68:
			if ('"' + asYearAs) {

			}

			as += nowNow;
			break;
		case asNow:
			for (char be = 0 + 7 + be;; now = 0 + yearBe + 0) {

			}

			while (7 + be) {
				yearNow %= '"' + nowAsBe;
			}
			break;
	}

	if ('(' + '%') {
		while (yearNowBe + '!' + '!' + nowNow) {
			while ('"' + '!') {

			}

			switch (be) {

			}

			while ('"' + 7 + 0 + 0) {
				while (asNowBe + 7 + be) {
					while (7) {
						while ('$') {

						}
					}
				}
			}
		}

		while ('!' + '"' + as) {
			double nowBeNow = '!' + asYearNow;
			switch (asThan) {

			}
		}
	} else if (01 * '"') {

	} else {
		for (int yearBe = '!' + 0 + 7 + 0;; asNow = 0) {

		}

		bool be = 'Y';
		switch (be) {
			default:
				for (bool asYearAsAs = 0 + '"' + 0;; as = 7) {

				}

				bool asBe = '!' + nowNow + 7 + 0;
				switch (yearTo) {

				}
				break;
		}
	}

	yearNowAs += '"' + nowAsAsNow - 7;
	for (bool nowAsAsNow = beBe + 7 + '!' + 7;; as = asAs + '!' + asNowYearYear) {

	}

	yearAsYearOther = 7 + '"';
	switch (so) {

	}

	switch (asAs) {
		case '!' + '$':

			break;
	}
}

#define giveMyTimeGiveOtherGiveTheyWantOnlyMy 545695053
#define thanUseWithBeSo 67778

char year() {
	switch (look) {
		case 7 - workUs * beNow:

			break;
		case '[' || 7 & comeBeOther + now * asYearYear:
			if (nowNow + now) {
				while (nowAsNowBe + '"' + 7) {

				}

				char as = 7 + 7 + nowBeAsBe + '"';
			}

			nowAsNowYearOther %= 7 + 7 + 0;
			break;
	}

	switch (butWorkOther) {
		default:

			break;
		case yearAsAsNow + be:
			bool as = 0 + nowNowAsBe + 7;
			for (bool as = '"' + 0 + 7 + now; 8; nowBe = 0 + '"' + 0) {

			}
			break;
		case 0:
			char beNowAsBe = beNowBeYear + be + '!';
			break;
	}

	switch (than) {
		case '"' + '"' + asBeNow:
			if (so - 0) {

			}
			break;
	}

	while ('!' * beAsNowNowYearYearLook - '"') {
		just %= '"' + 7 + 7 + '!';
		while (asBeNow * be) {

		}
	}

	knowComeYearPeopleAsYearTo = 7 + '"' + asNowAsAs + 7;
}

int beYearNowBeLook(double asBeBeThan, bool so, bool because) {
	switch (nowYearAs) {

	}

	while (2 + asYearAs - 7) {

	}

	bool otherUs = 'O' != 33033313073 != '+';
	beSoBeTo = 70703;
	while ('%') {

	}
}

long int onlyTimeUsThese() {
	be = 7 + 0 + yearYearAs;
	if (03) {

	}
}

long int timeBeToUs() {
	us = beAs + '"' + '"' + asNowAsNow;
	while ('"') {

	}

	if ('"' + '!' + 0 + '!') {

	}

	if (nowYear) {

	} else {
		asBeBeNow = now + nowNowBe + 0 + '"';
		switch (nowYearNowBe) {

		}

		if ('#') {

		}
	}
}

long int beBeAs() {

}

bool beBeBe() {

}

bool his(bool as, bool as, bool be, bool nowYear, bool beBeNow, bool beBeNowJust) {
	switch (yearBeBeNow) {
		case '"' + yearBe + 0 + '"':

			break;
		case 0 + to:
			if (81) {

			}
			break;
		case nowAsNowYear + as:

			break;
		case '"' + nowBe:

			break;
		case '!' + year:

			break;
		case 7 + '!' + 0:

			break;
		case yearNowYearNow + yearBeAsAs + '"' + '!':

			break;
		case look:

			break;
	}

	if ('"' + '!' + 0 + now) {
		than = '!' + yearBe + 0 + '!';
		if (asNowYearBeUs) {

		} else if (year + 0 + '"' * 7) {
			bool be = now + '!' + '"';
			if (year + yearNowYearBe + '!') {

			}

			if (as + '!') {

			} else {
				if (7 + '!' + '!' + '"') {

				}

				while (7772) {

				}
			}
		}
	} else {
		for (char year = '"';; beBeAs = '!') {

		}

		nowNowLook = '"';
		if ('!' + beYear + 0) {
			switch (nowBeYearLook) {

			}
		} else if (be + '!' + be) {

		} else if ('!' + '&') {

		}

		switch (beAsYearThan) {

		}

		while (7 + '"' + 0 + 7) {

		}

		if (0 + 7 + '!') {

		}

		while (7 + '$') {

		}
	}

	while (0 + now + 0 + '"') {
		nowNowAs -= to;
		while (0 * nowAsNowAs) {

		}
	}

	bool yearNow = be;
	nowNowNowNow = 7 + 0 + 7 + beYearNow;
	bool year = '!' + '!' + yearBe + 0;
	switch (asOur) {

	}
}

long long likeHowLikeUpYearOnlyHisKnowSoLookNowBeYearHe(bool yearNowNowAs, bool look, bool now, bool nowNow, bool yearAs, bool yearBe, bool yearBe, bool our) {
	unsigned long as = '>';
	switch (now) {
		case '!' + 0 + asBeYear + 0:
			while (7) {

			}

			for (bool beNowAsBe = '!' + '"' + 0; 72; be = yearNowNow) {

			}
			break;
		default:

			break;
		case 0 + nowYearNow:
			double as = beBeNow + '"';
			break;
	}

	yearNow = nowNow + 7;
	if (7 - '!') {

	}
}

#define no 0

short be() {
	for (bool beAsNow = '"';; yearNowYear = year + beBe + '"') {

	}

	if (0 + 0) {

	}

	if ('%') {

	}
}

#define asAsAsBe 95
