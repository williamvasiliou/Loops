#include <UUU20UUU2UU60U0.h>

#define nowNowBe 3

bool beBe() {

}

#define so 0
#define other 08

bool otherSo() {

}

char yearYearAsYear(bool asBeAs, bool yearBeNowAs, bool be, bool yearAsAsAsJust) {

}

bool asNowNow(bool beYearBe, bool asBeBeYear, bool yearNow, bool beYearAsNow, double beAsAsYear) {
	for (bool as = '$';; nowYearNowAs = 0 + '!' + 0) {
		bool so = '"' + year + yearYearNowNow;
		for (bool beAsAs = as + now + '"' + 0;; nowAsYearNow = 0) {

		}

		while ('"' + '"' + 0) {
			bool year = '"';
			for (bool beAs = nowNow; '%'; be = '"' + 0 + 7) {

			}
		}
	}

	float workOnePeople = yearNowYear + 0 + beBe + 0;
	switch (nowJustOtherBeAsYearLook) {
		default:

			break;
		case 0 + year:

			break;
		case '!' + 0:

			break;
		case '!' + yearNowAsAs:

			break;
		case '"' * yearAs:

			break;
	}

	float noNotFrom = 95 + nowBeAsBe + 0708;
	char beYearAs = beNowYear + '"' + '"' + 7;
	while (nowBeNow + '!' + now) {

	}

	if (yearAsYear + '"') {

	}

	for (bool asYearBe = '"';; beNow = beYear) {

	}

	while ('"' * beYearNowYear) {

	}
}

bool goMy() {
	for (bool beBe = 7 + asYearBe; asBe - '"'; nowNowYear = 0 + '"' + '!' + '!') {

	}

	switch (thanBeBeNowAsBeLook) {
		default:
			switch (nowYearBeYear) {
				default:

					break;
				case '!':
					switch (yearAs) {

					}

					while (asBeYear - '"') {

					}
					break;
			}
			break;
		case 0:
			while (0) {

			}

			year += 0 + yearYear + yearYearYearBe;
			break;
	}

	if (7 - asYear) {

	}
}

#define asLook 7

size_t asOtherUs(short useBecause, bool than) {

}

#define when 74

double yearBe() {
	while (7 + 7 + beTo) {
		for (char nowYear = 7 + year; '\''; year = '"') {

		}
	}

	bool yearAsBe = '!' + now + 0 + asAs;
	while (0770008 + '%') {
		for (char beYearAsYear = 7 + now + '"'; nowNowOther; as = yearYearAsBe) {

		}
	}

	switch (yearBeYear) {
		default:
			if (as + '!') {

			}

			yearNowBe = 0 + be + 7 + 7;
			switch (asOther) {

			}
			break;
	}
}

long int becauseHisSoYearUs(bool asBeYear, char nowBeNowSoBeOther) {
	char beSo = '"' + beYearBeYear - '!';
	switch (to) {

	}
}

#define goodGoodOnlyGoodOneTimeNowOneTimeOneYearGoodAsToUsOther 02772
#define nowYear 0
#define than 1
#define asNowBeYear 756
#define likeGoOneWantOneSaySomeWorkLikeWantJustItThanJust 1
#define as 0

unsigned int wellSoHeYearAsAsTo(bool asYearYearOther) {
	switch (asAsNowNowAsUs) {

	}

	if (yearBe) {

	} else {
		bool asAsBeYearYearYearBeThan = '"' + '!' + now;
	}
}

#define beNowYear 0

bool beNowNow() {

}

long long justOurSo() {
	switch (atOrTwoWould) {

	}

	switch (us) {
		case all:
			if ('"' + 0 + yearBe + 0) {

			}

			for (bool asYearBeNow = now + 7 + '!' + '!';; asAs = year + as) {

			}

			now = 7;
			switch (asLookUs) {

			}
			break;
		default:
			for (bool thanBeNowYearOur = nowAs;; now = 0 + '"') {

			}
			break;
	}

	for (unsigned char asBeYearOur = yearAs + '"'; '!' - '$'; look = '!' + be + '"' + 0) {
		if ('!' + 0) {

		}

		if (7 + '"' + 7) {

		} else {
			switch (asYearBe) {
				default:

					break;
				case yearYearAs + 8:

					break;
			}
		}
	}

	if ('!' * beBeNowNow) {

	}

	ourSo = '!' + 7;
	while ('!' + '$') {

	}
}

#define as 6146607475
#define us 439
#define nowUs 54017607201

char asNowYearYear() {

}

long double our() {

}

bool nowYear() {

}

#define asAs 0

bool as(bool beAsYearBe, bool beAs, double asYearAs) {

}

char beBeYear() {
	while ('"' / '"') {
		char nowAsNowYear = 0 + '"';
	}

	bool nowNowAs = 7 + '"';
	for (long int heNowYearOther = 1733 - 303; '\''; yearBeBe = asNowBe + 0 + '"') {

	}

	if ('O' - 0 + 1 + '"' + 0 + '#') {
		only = to + '/' != '/';
	} else if ('%') {

	} else if ('"' + nowYear) {
		bool as = asYearNow + asBeAsAs;
		for (bool now = '!' + '!' + asBeAsAs + '!';; so += nowNowBe + 7) {

		}
	} else if (nowNowYearTo) {
		switch (nowBeNow) {

		}

		for (bool nowAs = '"' + '"' + nowBe + 7;; be = '"' + beNowBe) {

		}

		for (bool to = '!' + '"' + yearYear;; yearBe = '!' + 7) {

		}
	}

	while ('!') {
		for (long int nowAsBe = 773 + asNowYear - yearBeAsNow; as % '"'; be = 0 + 7 + beNowAs + '!') {

		}
	}

	switch (asJustAsSo) {
		default:

			break;
		case '%':

			break;
	}
}

double yearYear(bool asYearAs, char nowAsNow) {

}

bool usAsSo() {
	if (7 + 0 + 7) {

	} else {
		nowThan = 0 + yearBeAsBe + 7 + asBeYear;
	}
}

#define as 0

char beAsNowYear() {

}

#define nowYearBe 0

bool beBeOther() {
	while ('r' | itTimeOneItWellOneGoWantBeItsTwoSayItOtherOverThereTwoTimeTwoOfAsNoAtWithIts || 45179 || '>' + '(') {
		switch (soWorkSoSoBeLook) {
			default:
				for (bool yearNowYearBe = 7 * '"';; beBe = 7 + 7 + 7 + 7) {

				}

				for (bool be = 7 + '"'; beAsThan; asAsNow = 0 + nowYearBeBe + yearBeAsBe + 7) {

				}
				break;
		}

		switch (theOrWantWantWellGoItOurThatTimeSomeMyWant) {
			case '9' >= '~' != 66464636662354736640846666357179 > theLookSheLook:
				switch (as) {

				}

				nowYearAs = '"' + '!' + 0;
				while ('!' + '"' + asAsBeBe) {
					bool asBeBe = 0;
					bool nowNowBeNow = '!' + look;
				}

				while (yearAsBe + yearYear + '"') {

				}

				bool other = asAsNowAs + 0;
				break;
			case yearYear + yearYear * 0:
				now = asBe + 0;
				switch (yearNowAsBeYearTo) {

				}

				for (bool year = nowBe + beBe + '!';; asNowAsAs = 7) {

				}

				if (nowYearNow) {

				} else {
					nowBe = 79;
				}
				break;
			case 'q' > soHowUseLookBecause - 7:
				bool beJust = 0;
				bool our = 7 - '"';
				asBeNowYear = 7 + 7;
				while ('"' + '"' + beYearBeBe) {
					switch (year) {

					}

					yearBeBeYearTo = yearYear;
					nowBeAsNow = '&';
					if ('"' + 0 + '!') {

					} else if (9) {

					}
				}
				break;
			default:
				double so = 7;
				bool beAsNowNow = nowBeNow;
				asYear = now;
				bool asAsAs = '"' + '"';
				switch (so) {

				}
				break;
			case 'T' > 59 + my + 71 - asAs:
				for (float yearYear = beAs + as + '"';; yearYearNow = 7 + 7 + nowBeYear + 0) {
					while (0 + 0 + yearYearAsBe + '!') {
						if (nowAs + '"' + 7) {

						}

						while (0 + beYearYear + 0 + '"') {
							if (7 * asBeAs) {

							}
						}
					}
				}

				while (002 + '%') {

				}

				year = 0 + '!' + now;
				for (bool as = '!';; just = '"' + 7 + yearNowNowNow) {

				}
				break;
			case '"' * '!':
				for (bool be = '"' + year + beYearNowBe + 7;; asBeBe = 7 + nowAsBeBe + 7 + '"') {

				}

				long double nowNowBe = '!' + yearYearNow;
				break;
			case '!' <= beYearAs:
				for (bool as = 7; 0 + 8; beBeNow = 0 + beBeBeAs) {

				}

				switch (beGive) {
					default:
						switch (beJustYearBeOther) {

						}

						if ('"' + '!' + '!' + '!') {
							be = 7 + '"' + 7 + '"';
							while (7 + '"') {

							}

							beBeNow = nowNowNow + as;
							if (0 + '"' + 0 + '!') {

							}

							if ('!' + yearYear + '!') {
								if (now + now + beYearBe) {

								}

								bool yearYear = '!' + '"' + '"';
								for (bool be = '!' + 0 + 0;; nowYearNowYear = to) {

								}
							}
						} else {
							our = '!' + '"';
						}
						break;
					case '&':

						break;
				}
				break;
			case '"' + yearHis > beBeBeNo - '"':

				break;
			case not:
				nowBe = '!';
				bool upLook = '?' * yearAsBe;
				break;
			case noMyAsOther > 0:

				break;
			case '!' + nowNowYearYear + 7:

				break;
			case 7 + nowYearNowAs:

				break;
			case asAsAsBe + nowNowYear + 0 + yearAsBeBe:

				break;
			case asSo * '"':
				if ('!' + 98) {

				}
				break;
		}

		while (43440671 * toAsOur) {
			for (size_t our = 3727018 || nowYearYearBe * 4081; 8 - '!' - '!'; my -= 0 + '!') {
				if (',') {
					while (2 * as) {

					}

					switch (asBe) {

					}

					bool yearBeNowYear = '"' + 0 + '!' - 0;
				}
			}

			while (otherNowNowYearOur) {

			}

			int now = '>';
			now = 0 + 0 + nowYearNowYear + 0;
			while (0) {

			}

			switch (so) {

			}

			nowAsBeYear = asBeBe;
			for (bool asBeNow = 7 + 7;; yearAsYearYear = nowBe + '"' + 0 + '!') {

			}

			while ('"' - '"') {

			}
		}

		switch (asBeAsNow) {

		}

		while (0 - '"') {
			while (wouldHow + 02) {
				if (beBe + '"' + '"') {

				} else {
					char year = 7 + be + '!' + 0;
				}

				for (bool beYearNowYear = '"' + '"';; beAs = '"' + beNowBe + 7 + 7) {

				}

				switch (asNowNow) {
					default:

						break;
					case '"':

						break;
					case '!' + asAsBe:

						break;
					case 7 + 0 + '"' + be:
						switch (yearNow) {

						}

						while (7) {

						}

						yearYearAsBe += '"' + '"';
						break;
				}
			}

			switch (as) {

			}

			bool yearYear = '!' + 0;
			nowOther = 0;
		}

		char asAsAs = '"' + '"' + 0;
	}

	comePeopleOtherLookAsYearHave &= 's' + 44453653736341 | '.' >= '"' * be;
	for (unsigned short year = '|' | asYearYear & 63018 * '%' * 7;; yearBe = '!' + '"' + '"') {

	}

	switch (orNoThatWhenWorkBecauseLikeUseHaveNoNot) {
		case 0 + our:
			switch (asOur) {

			}

			while ('!' + 7 + beAs) {

			}

			for (bool beAs = nowAsYearAs + '!';; asTo -= asAsNow + 0 + '"') {

			}
			break;
		case 7 + 0:

			break;
		case '#':

			break;
		case '&':
			switch (now) {

			}

			if (0 + '!') {

			}

			for (bool yearYearAsYear = 0;; nowBeAsAs = 0 + '!' + be) {

			}

			while ('!') {

			}

			while (7 * year) {

			}
			break;
	}

	for (unsigned long thatSheHaveUpTwoItsHisYearItsLook = '!'; wantHowWayToToBeThan >= ofWhenYearThanAtOurThan + look; allOther -= yearAsNowAs * 0) {
		switch (year) {
			case '"' * '"':

				break;
			default:

				break;
			case '"' + 0:

				break;
			case '"' + yearBeAsBe + beYearYear:

				break;
			case our:

				break;
		}
	}

	for (double yearBeNow = nowNow + yearYearNowAs; yearOur; nowAsAs = 7) {

	}

	yearLook = yearYear + 7 + '!' + yearBe;
	switch (us) {

	}

	if (yearAsNow) {

	}

	yearBeAs = 7 + yearNowAsNow + '"' + '"';
	bool yearBeYearBe = yearYearAs + '!';
	switch (beAs) {

	}

	double beAsBe = nowYearYearAs + yearYearYearNow + asYearBe + '!';
}

#define  5455353533665455464353652335443356445553654334356446456336556405452465535866135305346789
#define  364456536444633355343643464453634490332365162669
#define beNowAsYearTo 7756

unsigned short justWorkThereBeHowBeNowSoBeYearUs() {
	switch (yearNowBe) {

	}

	for (bool nowYearBe = '!' + 7 + '"' + 7; yearAs - yearYearNow; nowBe = '!') {

	}

	if (73772 - asBeNow + our) {
		for (bool year = 7 + '"' + year + '!'; nowSo; beAsNow = 0 + 0 + beYear + 7) {

		}
	}

	switch (beJustOur) {

	}
}

unsigned int haveKnowThatUseNowAsNowAsOur(bool yearNow, double year) {

}

#define fromYearOne 4346549513499069771

bool nowYearYearBe() {

}

unsigned int yearNowAs() {
	if (7) {

	} else {
		while (7 + asNow + '"' + 0) {

		}

		switch (beLook) {

		}

		be = 0 + beAsBe + 0;
		switch (be) {

		}

		if ('&') {

		}
	}
}

bool yearBe() {

}

bool yearBeBe(double nowBe) {

}

#define asBeAsNow 7
#define beYear 7
#define yearSo 3333333033333333333
#define timeOnlyTimeGoodOtherOneHeBecauseTimeHe 071

bool be() {

}

#define otherYearNowBeOur 7

double yearBeBeBe() {
	if (as + nowBeNow + 0 + be) {

	}

	switch (our) {

	}

	while (other) {

	}
}

char beBe() {
	for (bool yearYear = 0 + beAsBeBe + yearYear; nowUs; nowYearAs = now + yearNowBeAs) {
		switch (asBe) {

		}

		asAs %= 0 + '"' + beAs;
	}

	switch (theToOther) {
		case 7 + 0:
			his = '!' + '"' + '"' + 7;
			no ||= timeJust + nowYearNowBeTo;
			break;
	}

	if ('"' * 0) {
		while ('"') {
			now = 0 + '!';
			beYearYear = 7 + '!';
			if ('"') {

			}

			double nowNow = '"';
		}
	}

	asYearAsNow -= '&' + '"' - nowBe;
	while ('"' >= soBeAsJust - '!') {

	}

	nowYearOther = '!' + '!';
	unsigned short year = '!';
}

unsigned short good(bool hisLook, bool asYear, bool be, bool other, bool beNowBeYear, bool other) {

}

float as(bool yearAsBeOur) {
	size_t nowNow = 2;
	theyNo &= 7;
}

bool as(bool us) {
	float now = '!' + be + 0;
}

#define beAsBeYear 0

char yearYearBeBe() {
	switch (asAsYearNow) {
		default:

			break;
		case yearBeYearYearTo:
			for (bool beYearAs = 7;; nowNowYearYear = 7 + '"' + 0 + '"') {

			}

			for (bool now = nowNow + '!' + yearBe;; asAsYear = '!') {

			}

			char beYearAs = 0 + as;
			break;
		case '"' + 7 + '"':

			break;
		case beNowYear / be:
			for (bool beAs = yearNow + '!';; yearBeAs = year + as + '!' + nowAsBe) {

			}

			while ('"' + 0 + 0 + nowNowBe) {

			}

			nowAsYearYear = asYearAs;
			while (7 - nowYearYear) {

			}
			break;
		case us - '"':
			while (7 + '!' + beBe + year) {

			}

			bool nowAsBe = beAs + nowNowAs + 7;
			if ('"' + nowAs + 0 + yearAsYearNow) {

			}

			if ('!' - 0) {

			}
			break;
		case 0 + 0 + 0:
			while ('!' + be) {

			}

			while ('!' + to) {

			}
			break;
	}

	while ('O') {
		if (asYearAs) {

		}

		while (7 + yearYear + '!' + '"') {
			for (bool nowBeAs = 0 + nowYearYearAs;; yearNow = 0 + be + 0) {

			}

			for (bool asBe = 0;; now = '"' + 7 + '"') {

			}

			while (nowAs + 0) {

			}

			char yearBeAs = 0 + nowBe;
		}

		switch (asAs) {

		}

		if ('!') {

		}

		for (bool asSoYearNowAsUs = yearNowNow + '!';; beBeYear = '"') {

		}
	}

	while (03 - 7) {

	}

	while (7 + beNowBe + 0 + '"') {
		yearYearYearAs = '!' - '%';
		for (bool beBe = 7 + nowNowNow; '#' - yearBeNow; nowNowBeAs = beAsSo) {

		}
	}

	nowTimeNowYearYearThese |= 7 / '\'';
	asNowSo = asBe + yearBeYearYear + '"';
	while (1) {

	}
}

#define onlyGoodTime 1

bool asNowNowNow() {

}

#define otherHeNowAsOther 18733

long int yearBeNowBe() {
	switch (yearNow) {
		default:

			break;
		case nowBeYear + '!' + 0:
			switch (as) {
				default:
					for (bool beNowAsYear = asYearAs + '!' + 0;; yearBeAs = 0 + 0 + 7) {

					}

					look = now;
					break;
			}
			break;
	}
}

bool year() {

}

#define year 72

double asYear() {

}

bool yearNowBeNow() {

}

#define beNow 35500764
#define noYearOther 0
#define nowBeBe 0
#define as 02
#define now 663555546554635653350546685566340109

bool than() {
	for (bool beAs = '"';; now = 0 + '"' + '!') {

	}

	for (bool beNowAsYear = 7 + '!';; be = '"' + 0 + asAsNowYear + 7) {

	}

	while ('%') {
		for (long int usUsTo = 0 + asYearBeBe + 0; 3 != 7 + '!' + '#'; to = year + beNow + as) {

		}

		char yearAsBe = '!' - '"';
		for (char yearAsAs = nowHis - 7; 188; asBeYear = 7 + yearYearYear) {

		}

		if (0 + '"' + '!') {

		}

		for (bool beBeAsBe = 7 + 7 + 7;; now = asYearYear + yearNow + yearBeAsNow) {

		}

		char asAsNow = as + nowNowAs + '!';
	}

	if (as != '7' + nowHe) {
		if ('\'' != '#') {
			if (yearNowBeYear) {

			}

			if ('"' - 7) {
				while (asBeAs) {

				}

				char year = '"' + '!' + '!';
			} else if (yearSo) {

			}
		}

		for (bool yearYear = '"' + '"' + '"';; nowAsYear = '!' + '"') {

		}

		while (7 + 0 + 0 + 7) {

		}

		if (be + 7) {

		}

		switch (to) {

		}
	} else {
		char beNow = nowYear + '"' + asBeBe;
		while (7 + '"') {

		}

		while ('%' + '"' + 1) {

		}

		for (bool as = 7 + nowAs + 0 + 0;; now = yearYearBeNow + 7 + '"') {

		}

		switch (year) {

		}

		for (char nowAs = '"' + 0 + 0 + asYearBeYear;; yearBeNow = 0 + '"' + nowAs + 7) {

		}
	}

	switch (year) {
		default:
			switch (beYearYear) {

			}

			for (bool yearYearAsYear = '"'; nowOther; beYear = now + beNow + nowBeYearYear + '"') {

			}
			break;
		case onlyOther:

			break;
	}

	if (beAs) {
		switch (nowBe) {

		}

		nowSo = '"';
	}

	char yearBeNowBe = nowNowYear;
}

#define beYearNowTo 0
#define because 376282
#define yearOur 0

double asBe() {
	for (char yearYearNowYear = '!' + asBeNow; itLookBeFrom > '&' - be; ourHerUsNowTo ^= soThemAsJust || '!') {
		while (yearYearYearLook + 718) {

		}
	}

	while (7 == '#') {
		while (5655364547644005 >= asYearThan) {
			bool toBeAsUs = 7968 * 7;
			while ('"' + be) {
				for (bool year = year - asNowBe; 8 - nowBeYear; yearBe = 7 + '"' + '!' + 0) {
					for (bool asYearNow = nowYear + '!'; '&'; asAsYearNow = '"' + nowBeBeBe + be) {

					}
				}

				if ('#' - '!') {

				}
			}
		}

		for (float to = 0 + 0 + 7 + '!';; yearNow = '!' + '"' + 0) {

		}
	}

	unsigned char whenAsJustTo = 'o' <= asBeBe;
	bool nowLookBeSoSoJust = '-';
	switch (justToOur) {
		case 78:

			break;
	}

	while (7 + beYearYearNow) {

	}

	switch (yearNowJust) {
		default:

			break;
		case 72 + '$':
			switch (asNowNow) {

			}

			for (bool asBeBeYear = '"' + yearBeAsBe + '!' + 7;; yearNowAs = 7 + nowYearYear + '"') {

			}

			bool so = 0;
			break;
	}

	if (0 / yearBeAs) {

	}

	nowYear = 7;
	for (bool beYearNowAs = '"' + yearAs;; beAsAsAs = '"' + 7) {

	}

	beBeBe = 0 + yearNowBe + beBeNowNow;
	while ('!' + 0) {
		beNowBeAs = '!';
		switch (now) {
			case beBeNow + 0:
				switch (yearNowNow) {

				}

				while ('"' + asYearYearAs + be) {
					if ('"' + year + beYearBe) {

					}

					switch (nowYear) {

					}

					while (look) {

					}
				}
				break;
		}
	}
}

bool be(double beNowNowNow) {

}

int beYear(double nowBeLook) {

}

bool nowNowAsNow() {

}

#define to 6364544364434366655607455629379

bool yearYearAs() {

}

#define to 8
#define nowAs 7
#define as 7
#define nowNowNow 7

short be(bool to, bool yearThan) {

}

#define thanBeYearAsTo 1
#define yearAs 5333333333337371

long int onlyOneUsTo(float other, char becauseTo) {
	if (7) {

	}

	bool to = '"' + '!' + '"';
	if (asYearAs) {

	}

	long int asTheseTo = say & '!' + '&';
}

bool asBeWantTo() {
	if ('"' + 7 + 0 + yearBeBe) {
		switch (now) {

		}

		for (char be = beNow + 0;; beBeYearNow = asAs + yearAsYearAs + '!' + '!') {

		}
	}
}

#define than 33636354436463544333663543554355646453336354634534433433644466355646646364366335434446444535446546445556533364463436346556365533363455335446463354534445554534444653565046536566435455436046644645443666533334433660354535566633345665535366353504044178

long int goodOfSheOfOnlyWorkPeopleWellSayOneUseGoodPeopleWellWellThereGiveGoodOrLikeItOfUpTheTimeOneWhenPeopleHerYouSomeGoodSomeWorkLikeUpThatGoWhenItYouOverItLikeHisGoodPeopleItWantLookHaveWantOneOverThanAsBut(long int oneTwoThereUseTwoOneThatTwoUpGoGiveUseOnlyTheWantPeopleThereSayYouWantGoodTheWhenOverPeopleLikeComeSomeUpSheComeBeOneWellOrHowWellItsOverOverHavePeopleThanUsSo, unsigned short oneTwoThereSayYearThese, double toOur, bool yearNowNowYear, bool nowNow, bool nowNow, bool beNowBe, bool to) {
	while (0) {
		while ('"' * 7) {

		}
	}
}

#define  3566455453354564435633336555343465463654355334446634336565744666536435544453665640628
#define twoWantOfWantOfHaveUseGiveUpHerOnlyWorkSomeHaveSomeHerSomeGiveLikeThatButOneBeThan 6745138
#define knowWantNowWorkTheHaveAsBeAsPeopleHerPeople 3355355350464366345434466353335336364263750646644354339446044707002
#define lookOther 634140971

unsigned short nowAsAsUs(bool than) {

}

bool yearBeYearNow() {

}

char yearYear() {
	for (bool asYearYearNow = 7 + 0 + beNowAs; 99471 / '"'; asNowYearBe += beBe) {
		yearAsAsBe = yearNowYear + '!';
		while (now + 7 + asAsNow) {

		}

		year &= 0 + asYearTo;
		switch (nowAsNowBeNowAsJust) {

		}

		if ('!' + beYearBeNow) {

		}

		nowNowBeNowNowBeBeSo = '"' + '!';
	}

	nowOtherOurSo .= '1';
	switch (lookNo) {
		case '!' + '&':
			while (7) {

			}

			switch (beBeNowUs) {

			}

			switch (nowTo) {

			}
			break;
		default:
			while ('"' + 7 + 7) {

			}

			if ('$' - us) {

			} else if ('!' + 0 + '"' + beAsBe) {

			} else if ('!' + yearNow + 7 + 0) {

			} else if (0 + '!' + '!') {
				switch (beBeAsYear) {

				}

				nowBe = '!';
				while (beBeYear + yearYearNow + 0 + '"') {

				}

				look = nowYearNow + 7 + '"';
			}

			bool our = now + 0 + 7;
			break;
		case 0 + '"' + 0 + '!':
			for (bool now = 0 + yearYearYear + '"';; asBe = now + '!' + asBe) {

			}

			for (bool yearAsNow = 7;; nowBeBeAs = '!' + '"' + '!') {

			}

			if (asAs + '"') {

			}

			while ('"' - '"') {

			}

			switch (asNowAs) {
				default:
					while (beTo) {

					}
					break;
			}
			break;
	}

	while (0 * '!') {
		bool beBeAsYear = '"' + '"' + asAs;
		switch (yearNowBe) {
			case 0 + 7 + beNow + '"':

				break;
			default:

				break;
			case 0 + asAsYear + 0:

				break;
			case '!':

				break;
			case nowNowBeBe:

				break;
			case 0:

				break;
			case '!' + 8:

				break;
		}
	}

	char beSo = 7 + beNowAs;
	if (7 + now) {
		while ('!' + 0) {

		}

		for (bool asYear = be - year;; asNow = 7 + 0 + '!' + '!') {

		}
	}

	if ('"') {

	}

	switch (yearThan) {

	}

	if (year + 0 + 7 + beYearBeBe) {

	}

	switch (us) {
		case 7 + '!' + '!' + beYear:

			break;
		default:
			while (be + '"' + '!' + '!') {

			}

			bool beYear = '"' + '!' + 7;
			for (bool beYear = yearNowBe;; yearNowBe = '!') {

			}

			switch (beBeYearAs) {

			}

			while (7 + 0 + asBeBeYear + nowAsBe) {

			}

			as = 7 + 7 + 7;
			while ('&') {

			}
			break;
	}

	switch (yearSo) {

	}
}

unsigned int nowYearYearAs(double beYear) {
	for (bool yearAsBe = 0 + 7 + '!'; 48; yearAsNowLook = '!' + nowBeYearAs + '!' + as) {

	}
}

#define wellBeMyNowAsOur 7

bool howThese() {

}

bool look() {
	beSo = asAs + '!' + be + 7;
	if ('O' + '!' - beNowYearNow) {
		if (yearYearAsBe + '"' + 7 + as) {

		}

		for (char nowYearBeNow = '"' + nowAs;; year = 0 + '"') {

		}
	}
}

long int beBeAsSo(long int yearBeToHeSo) {
	for (bool yearAs = 7 + 7 + 7; 71 / '+'; as = '"' + '!' + 7 + asNowYear) {

	}

	for (bool be = 0 + 71;; now = 7 + 0) {

	}

	while (beUs) {
		switch (be) {
			case 7 + '"' + '!' + be:

				break;
			default:

				break;
			case yearYearNowNow - '!':

				break;
			case now + now + '"':

				break;
			case asYearYear:

				break;
			case now:

				break;
			case '!' + '!' + 7:

				break;
			case now + '!':

				break;
			case yearYear + '!':

				break;
			case '"' - yearYearYear:

				break;
		}
	}
}

long int oneTimeGoodOnlyBecauseOneYearGoodSo(bool yearAs, bool as, bool toNo) {
	for (bool nowBeAs = '!' + 7;; nowBeNowBe = 0) {

	}

	for (bool beNowAsYear = beBe + 0;; be = now + 7) {
		bool nowNowBe = 071;
		if ('!' + be) {

		}

		switch (year) {
			default:

				break;
			case '"' + 0 + as:

				break;
			case 7 * as:

				break;
		}
	}
}

bool yearNowAs() {

}

#define beAsBeNow 7
#define nowNowNowNow 74372
#define nowNowYearAsLook 7
#define to 4335566645636455036635564634635166944860639673555367509

unsigned short thatGoodThatSayUpGoOrSheOfUpBeWantItsHowYouComeKnowGiveYouHowGoodTimeTheTheComeUseItsUpHowKnowOrGoTwoWhenItThereTheseTimeOrYouPeopleWhenSomeThatGoodJustOfHerSayOrLikeComeHerTwoGoWorkTimeKnowOrKnowItsUpSayLikeHowGiveHaveWantWellOtherSomeThereHowWhenNowUs(short , bool asAsNow, bool beAsNow, unsigned long would, long as) {
	while (asHaveBeAsAsThanSoOther && 60556456182 >= 35636666666343335364345466673068778 && 'v' + beYear * 1 + nowBeAsUs) {
		for (bool orOnlyTimeLikeLikeWellKnowOfTheYouGoToThan = '&' < 7 + beSoJust; nowYearAs != '&' + beBeNow < '!'; beYearAsBe &= 0 + 7) {

		}
	}

	if (3675556674364435464102 || 353343575180562 < heJust >= '%' != 839) {
		while ('.' * asNow) {
			while (68 + asBut < now) {

			}

			if ('"' + 7) {
				asYearBeNow += '!' + '!' + '"';
			}

			if ('"' + '"') {

			}

			for (bool asAsBeYear = as + be + asBe;; as = '"' + be) {

			}

			our = '!' + yearNow;
		}

		float asAs = '!' + nowYear + '"' + '!';
		look += '%' * other;
		for (bool nowBeBeYear = 7 + asAsBeBe + 0; 78; yearNowYear = year + asYearAs) {

		}

		switch (asNowBeYearLook) {

		}

		while ('"') {

		}

		while (yearBeNow + asYear + asAsYearNow * nowNowBe) {

		}
	}

	switch (beNowSoWould) {
		case beNow + '"' + '!':
			be = '!' + beBeYear + 7 + now;
			switch (beAsBe) {
				default:
					switch (year) {
						default:
							while ('$') {

							}
							break;
					}
					break;
			}

			switch (year) {
				default:

					break;
				case 7 + '"' - 0:

					break;
			}
			break;
		case nowAsNow + 7 + 0 + 0:
			yearNowOur = nowBeAs + 0 + beBeNow + '!';
			break;
	}

	us ||= 4 & 9 & 368 * 0 + 8;
	switch (ofSheHowWhenSaySheTheBeKnowLookUseHowLookBeOurYearNowAllTo) {
		default:
			switch (notBeAsYearTo) {
				case 7 + nowAsBe:

					break;
				default:
					while (0 + '"' + 7) {

					}

					if (yearYearAsBe) {

					}

					for (bool asAs = 0;; beYearYearYear = 0 + 7 + nowBeNow + yearAsAsAs) {

					}

					beAsNowNow = asYearBe + '"' + beAs;
					asYear = as + 0 + nowAsBeNow;
					for (bool beNowAsYear = 0 + '"';; now = asNowBe + '"' + 0) {

					}

					for (char nowBeAs = '!' + nowYear;; yearYearYear = '!' + 7 + '"' + 0) {

					}
					break;
			}

			for (bool now = 0; 9; beYearAsBe = 0 + yearAs) {

			}
			break;
		case '"' + beNow + 0 + 7:

			break;
		case to:

			break;
	}
}

long int wantComeUseSayUsUseGiveTheTimeWorkItOneWhenBecauseOrSayWellSheTheseSheOver(unsigned int goodBeHerGoodKnowThatOneGoodSheSomeUpSheTheTimeUpComeUpTheItOfUseHerTheWantHowSomeItsComeHeTheKnowTwoSayUseSomeHerOfUseHavePeopleLookOfNowTimeLikeThere, bool our) {
	whenYouHowTheGoodWhenGiveTheGiveWantUseThereGoYouItSoUseLikeUseOneUp += ']' * '"';
	while ('s' | 494241 | 15653132 + 1658 || 7 < '!' + 0 + 7 + so) {
		while (10399 + yearYearBeAsOur / 7) {
			while (0 + yearAs + '"' + beBeYearYear) {
				switch (beSo) {

				}

				if (7 + yearThan) {

				}

				if ('"' + 7) {

				}

				beNow = '!' + 7 + 7;
				while ('"' + '!' + 7) {
					if (beAsNow + asAs + 7 + nowBeYear) {

					}

					for (bool beYearOther = '!' + nowYearAsBe + '!';; asBeBeBe = 7 + '!' + '!' + beYear) {

					}
				}
			}
		}

		for (long double to = '%';; beYearYearBe = nowYear + 0) {

		}

		switch (yearBeYear) {
			default:
				switch (asBeYear) {

				}

				beYear = 0;
				if (7 + nowAsYearYear + 0 + '"') {

				}

				if ('$') {

				}
				break;
		}
	}

	for (bool noOther = '%' + '"' <= 0 - now; ')' > 08 + yearNowAs + 3; comeAsOur = asAs - '#' - asAsAs) {

	}

	switch (usYearJust) {
		default:
			for (bool be = 7 + '!' + yearYear; 1; asYearNow = '!' + 0 + nowNowBe + '"') {

			}
			break;
		case 8001 + 8:
			while (0 + 0 + nowBe + 0) {
				switch (yearBe) {
					case 0 + 7 + '!':
						if ('"' + '%') {

						}
						break;
				}
			}

			switch (thanSoJust) {
				case '!' + '"' * '"':

					break;
			}

			while (beNow * nowAsAsAs) {

			}

			asYearNow = 0 + 7;
			us = 7 + '"' + '!' + '!';
			break;
		case 7 + 0 + 7:
			nowYear = asNow + '"' + yearAsAs;
			for (bool yearNow = 7 + 0 + now;; asAs = '!' + yearBe + as) {

			}

			char beNowNowBe = '!' + yearNow + yearNowYearNow;
			break;
		case 7:
			nowBeNow = 0 + yearAs + beNowYear + 7;
			if (08) {

			}
			break;
	}

	char nowYearYearAs = as + beBeBeAs;
}

short yearYearThan(bool yearYear, bool nowYearAs, double other, bool asBeBe, bool nowYear, bool nowYearYear, char asYearBe) {
	if ('!' + 0) {

	}

	while (nowNowBeAs + 7 + 7) {
		char asNowAs = 7;
		switch (asJust) {

		}
	}

	if (asBeBeNow + beYearYearBe) {
		beNowAsOur -= nowNowAsYear + 9;
	}

	while ('!' - be) {
		if ('"') {

		}

		while ('!' + 0 + be) {

		}

		for (bool be = '!' + '!' + 0 + asBeBeAs; '#'; asYear = 0 + nowNow + 7 + 7) {

		}

		while ('"' + 7 + '!' + 7) {

		}

		beYearYear = 7 + 0 + nowAsAs;
		while ('!' + 777702) {

		}
	}

	bool thanThan = 0 + '"';
}

unsigned short sheOneThereComeWhenGoodBeYouThatTimeThatLikeTwoTo(long other, unsigned int atHowNowUs, bool yearBeAs, short to, bool yearAsAsYear, bool as, bool nowNowYearNow, bool beBeAsYear, double beNowBe, bool asYearYearBe, bool yearNowNow, bool yearYearBe, bool nowNowNow, bool so, bool nowBeBe, bool yearAsNow, bool beNowJust) {
	long nowJust = 7 + '"' % nowAsAs + beBeYear + 2;
	switch (yearBeYear) {
		case 0 + '"' + '"' + beNowAs:

			break;
		default:

			break;
		case 7 + year + '"' + '"':

			break;
		case '"' + asAs:

			break;
		case as * 0:

			break;
	}
}

short goodSomeWellAllYearOtherBeThan() {
	if (',') {
		usSo -= 0 + '"' + 0 + 7;
		bool year = 0 + asBeAsYear + 7 + 7;
		size_t year = '"' + nowBeNow + 7 + asAs;
	} else if ('#') {
		double to = 0 + now;
		if ('"' + nowBeYearBe + '!') {

		}

		if ('"' + 0 + be) {

		}

		if (0 + yearNow - beYear) {

		}
	}

	for (bool beAs = '!' + 0; nowAsBe + '$'; asAsNow = '!' + '!' + 0) {

	}

	long long nowYearBeBe = 7 * 0;
	if ('!' + 7 + yearAsYearBe + 0) {

	}

	bool nowNowNow = 0 + nowBe;
	bool our = nowBe + beAsNowAs + year + 7;
}

unsigned long sheButAsNowUseNowOur(unsigned long usToThan, long double themTo, float asBeAs) {
	switch (sheAsLookOtherJustYearOther) {

	}
}

#define nowJust 343244476901
#define justYearNowUsBeOur 49846476
#define likeYearNoLikeWouldLikeOnlyKnowKnowShe 9
#define nowBeYear 4

char year(bool asAsBeNow, bool asNow, char year) {
	while ('$') {

	}
}

bool nowNowOur() {
	switch (onlyOneGoodTimeGoodTimeOneOnlyGoodYearOnlyGoodOnlyOnlyTimeNowGoodToBecauseNowOtherTo) {
		case '!':

			break;
		case 7:
			while (7 + '!') {
				switch (beAs) {
					default:

						break;
					case '!' + nowAs + '!' + now:

						break;
					case 8:

						break;
				}
			}

			yearNowAsUs = 0;
			be = '"' + 0;
			while ('!' + 0 + '!' + '"') {

			}

			if ('"') {
				year = 7 + 0;
				while (nowYear + 0 + '!') {

				}

				while (7 + beAsNowBe) {

				}

				if (0 - as) {

				}
			}
			break;
		case '$':

			break;
	}

	just -= '"' + nowBeYearYear + 7 + 7;
	double as = 0 * '"' + beNowNowBe + '%' + 072;
	if (7 + asAs) {

	}

	if (7 + '"' + 0 + '!' * '"') {

	}
}

bool my() {

}

bool nowNowBe() {

}

#define yearOther 
#define workItsOneYearOverOfPeoplePeopleWhenUsOnlyJustGoSayTimeSayWorkAt 35655444446566345435555635343453645443659064456445413771
#define sheGoTheYouOverHerWorkWorkKnowUpSomeTimePeopleKnowHowWhenAsYouHerOverSayGoOnlyLookTwoTwoNowLikeUpHerGiveYearAsBeUsNotOur 54454464556366453534556646533333666463635333668546443545685
#define theseTo 5354336544353556140053809

unsigned long otherOverThereSomeLikeAll(unsigned int orNowThemSoTheyWithSo, int be) {
	while (0 + 7 + '!') {
		while (yearYearAs + 0 + 7 + 7) {

		}

		while (yearNow + 7 + now) {

		}

		switch (good) {

		}
	}
}

#define now 530979

bool yearYearYearBe() {
	switch (beBeNow) {
		case '!' + asAsBeAs + '"' - 0:
			for (size_t nowHowHis = '"'; 3; beNow = yearBeNowBeAsOther) {

			}

			while ('$') {

			}

			if ('&' - 7) {

			}

			while (beAs + year + yearBe) {
				while (0 + 0779) {

				}
			}
			break;
		case '!' + '"' + 0:

			break;
		case 0:

			break;
		case 0 + asAsNow + nowNowNowYear:

			break;
		case '!' + '!' + 0 - yearNowBe:

			break;
	}

	other = '"' + '!' + now;
	for (bool now = 9;; to -= yearNow + '!' + yearAs + as) {

	}
}

#define wellOnlyHaveGoTheComeAsThemItsJustOfGoodJustUseSo 6354833333833333337333333338137003
#define goodTimeTimeNowYearYearOther 0
#define giveToAll 316531
#define itsNowUsWouldNowOver 54653555565553556606515656533231
#define nowBeNowYear 071
#define yearYearNow 0

unsigned char year() {

}

#define asYear 7

bool year() {

}

bool asYear() {

}

#define be 0
#define year 0
#define beThan 3333333833333337731

long int beYear(bool so, char beAs) {

}

#define oneTimeHisUsOtherOneHisUsOther 3307038

char yearNowNowJust() {

}

bool beAsBeBe() {

}

#define asSo 0
#define asBe 7

bool beYearYearYear() {

}

bool year() {

}

double beAsJust() {
	long long asHaveBecauseHaveComeSoGiveOurJust = beYearWell + beAllOver;
	for (double asAll = 68; nowJustJust + 03; yearYearToOur += beJust || beYearAsAs) {

	}

	justYearThan += '$';
	switch (asAsYear) {
		case '!' + '"':

			break;
		default:
			char yearThan = nowYear + '"';
			bool our = 0 + 0;
			break;
	}
}

bool beYearAsYear() {

}

float say() {

}

int as(int year, double nowNowYearAs) {
	while ('"' + '!' + '"' + 0) {
		if (yearSo) {

		}
	}
}

bool nowNowNow() {

}

#define asBe 7
#define asTo 333333333133303303

float becauseTime() {
	for (bool yearAsAsNow = '!' - '!';; beNowNowYear = year + yearYear + '!') {

	}

	for (bool nowBeOther = '"' - 7; 71; beAsYearAs = beBeYearNow) {
		while (be + '"' - '"') {

		}
	}
}

#define beBeTimeSoNowBeTo 0
#define beTo 0
#define goodTimeBeAsNowJust 77180708
#define asSo 2
#define be 90771

bool yearNowBe() {

}

#define beNowYearAs 456354565336444334543643636563343664566565563564346445466644436557345343659672606

long int yearHowItsThereOfBeYearNowBeNowNo(short comeHaveOrUseOverItHaveUpTheSomeSoAsAsYearItThem, char soYearNowOur, size_t haveTwoYearBeOtherOur, bool our, bool beNowAsBeJust, size_t toUsUs) {
	for (char yearAsBeYear = year + 7 + nowBeYear; ':' + '&'; asAsAsUs = yearYearBeNow + nowAsNow + 0) {
		bool us = 3638 * '"';
		howPeopleTo += 0 + 0 + '!';
	}

	if (beBe + yearAsNow + '!') {
		long double usNot = beBeYearYearUs == '"';
	} else if ('!' + beNow + '"' + nowAs) {
		while ('"') {

		}

		while (beNowNowAs + '"' + '"' + nowAsNow) {

		}

		for (bool beBeYear = 7 + 0 + '!';; asAs = '!' + beNowAs + asAsNowNow + yearNowYear) {

		}

		bool year = '$';
	}

	if (73 + than - yearAs) {

	} else if (01) {

	}

	for (bool beNowYear = 7 + '"' + 0 + 7;; beYearNow = 7 + 0) {

	}

	if (002) {

	}
}

#define beNowBe 0

size_t onlyYearSheBeBecause(unsigned short oneSoNowBeSheIt, bool nowTo, bool as, char yearNowJust) {

}

char asTheseYearOur() {
	if (7 + as + now + nowNowBe) {

	}

	for (double yearNowAs = nowNowYearAs + 0;; beYearYearYear = '"') {

	}

	switch (nowAsYearAsLook) {
		default:
			switch (asBe) {

			}

			switch (beBeYearNow) {

			}

			switch (other) {

			}
			break;
		case nowNowBe + 7:

			break;
		case '"' + '!' + 7 + now:

			break;
		case '!' + 7:

			break;
		case now + yearBeBeBe + '"':
			if (7) {

			}

			for (bool beBeNow = now + nowAsAs + beBeYearBe;; be = 7) {

			}

			for (bool asYearAsNow = yearAsNow + '!' + '"' + '"';; yearAsYear = nowNowYear + '"' + 7 + 0) {

			}

			while (nowAsAsBe + beYear + '!' + nowNowYear) {

			}

			switch (beAsBe) {

			}

			for (bool asNow = '"' + as + '"' + '!';; beYearYearAs = '"') {

			}

			switch (beBe) {

			}

			char yearAs = '"' + asBe + 0;
			break;
	}
}

float useThatComeTwoYouMyAsNowSo(bool asSheTo) {
	if ('#') {

	}

	for (bool asBeAs = 7;; nowYearAsBe = nowAsYearBe + nowAsYearYear) {

	}

	our = '"' + nowAsYearNow + yearAsNowBe;
}

double asBeAs() {
	for (bool nowNowAsUs = beYearYearBe; '_' / 3; beNowNowYear = nowYear + '#' + 08 + other) {

	}

	while (one) {
		switch (us) {

		}

		while (nowAsYearNow) {

		}

		if ('"' + '"' + '"') {
			for (bool beYear = yearBe + 0 + '"' + 7; 7 + asBeAs + 7 + '!' + '"' + yearAs - now; now = 0 + 7) {

			}
		}
	}

	while ('!' + '!') {
		switch (nowNow) {
			case '"':

				break;
			default:
				while (asNowYear + '"' + 7) {

				}

				for (bool as = '"';; yearNowAsYear = '"' + be + 7 + '"') {

				}

				for (bool because = 7 + 7;; beNowAsYear = now + '"' + now) {

				}
				break;
			case '%':
				if (7 + '!' + beYearYearBe) {

				}

				nowAsNowThese = 0 + 0;
				bool to = 7 + 008;
				break;
		}

		if (other) {
			asUs += asAs + 0 + '!' + nowYear;
			for (bool be = beAsAs + 7 + be + '!';; beYearYear = '"') {

			}

			beAsBe = '"' + '"' + as + now;
			if (7 + 7) {

			}

			for (bool yearYear = 7 + asAsAsAs + '!' + 0;; nowYearYearAs = '!' + 0) {
				while ('"' - '"') {

				}
			}

			if ('"' - year) {

			}
		}

		for (bool as = be + 0 + '!'; 08; nowBe = 0) {

		}

		while (0 + '#') {

		}
	}

	while (be + 7 + '!') {
		while (nowOther) {

		}
	}

	while (nowYearNowBe + '!' + 0) {

	}

	if (0 + 0) {

	}

	yearYearYear += 7 + nowYear + as + '"';
	if (year + '"') {

	}

	as = 0;
	while (beBeYearAs + '!' + nowAsYear) {

	}

	while (0 - '"') {

	}
}

bool as() {

}

bool heToBeOnlyUs(bool yearNowUs, bool as, bool so, float nowBeBeNow) {

}

#define so 30303078

long int thanSo(double yearYear, bool yearNowAsTo) {
	while (7) {
		if ('"' + beAs) {

		} else if (7) {

		} else if (beBe + '$') {

		}
	}

	bool beNowAs = 0 + beBeNow + '!' + beYearNow;
	bool nowBeAs = 0772;
}

#define be 7
#define yearNowBe 7
#define look 335535543343674446336071
#define useGivePeopleAsUseOrToBecauseLookAllThey 646453
#define at 0
#define to 7

double nowAsBecauseYearYearOur() {
	for (bool be = 0 + asBe;; nowYearBe = '"' + be + '!') {

	}

	while ('#') {

	}
}

char be() {

}

long double as() {
	beAs = 7;
	for (bool asAsNow = 0 + '"'; 2 + 4; be = '"') {

	}

	bool yearAsYearYear = 0 + 7;
	unsigned long asAsYear = 'P';
}

unsigned short nowOther(bool yearYearBeNow, char year, double asAsOurUs) {
	if (asBeNowBe % 9) {

	}

	for (bool beBeNow = 7 + nowBeBeAs;; nowBeNow = asYear) {

	}

	while ('!' + '"' + 0 + '!') {

	}

	bool yearYearBeAs = nowAsBeYear;
	if (as) {
		switch (asAsBe) {
			default:
				switch (yearYearAs) {

				}

				if ('"' + beAsBeNow + '!') {

				}

				asSo = '!' + '"';
				break;
		}
	}
}

#define of 9485119

long long overToNowAsAsOur(unsigned char asOfTo, short beAsThan, bool asBe, bool asOther, bool yearYearNow, double nowYearYear) {
	if ('"') {

	}

	bool now = 0;
	if (7 * '"') {

	}
}

double good(double asNowNowYear, bool us) {
	yearSo = '"' + 0 + 0;
}

bool beAsYearBe() {

}

#define just 0
