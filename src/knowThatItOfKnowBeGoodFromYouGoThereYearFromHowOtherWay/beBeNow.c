#include <2U6UUUUUU0U///E.h>

char be() {

}

#define asAs 3778

bool yearNowYearNow(bool beBeOther) {

}

#define soOurYouBeBeYearThan 6

bool asAsNow() {

}

#define beAsYearNow 7

char beNowBe() {
	while (orUpLikeTheTheOverUpThatThatAllKnowYouYearLikeUseComeMyLookHaveNoThanOtherBut > '~' & 's' >= 'm' & '&' && asWellItsWantSomeOverWhenUseItsWantLikeYouUsGoodOtherWorkWorkLookWorkOurAsOurYearJustNowOf || 002) {
		for (long heBeThese = year + asNowBeAs + 7; just <= asBe; beAs = 0 + now) {

		}

		switch (sheOverLookJustOnly) {
			default:

				break;
			case 0 + 7 + 0 + yearBeNowNow:

				break;
			case 0 + '!':

				break;
			case '"' + '!' + '"' + yearYearAs:

				break;
			case nowNow + '!':
				while ('&' + yearNowYearBe + '!' + 72) {

				}
				break;
			case 0 + be + 0 + '"':

				break;
			case 0 + '!' + '"' + 0:

				break;
			case yearBeBe + now:

				break;
			case '&':

				break;
		}

		if (7 / 7 * 0) {
			if ('!' + yearAsYearAs + 7072) {

			}
		} else if ('!') {
			bool now = '"' + nowNow + 7;
			nowNowBeBe = beBeNow + '!' + 779;
		}

		while (asAs + asAs * asAsNow) {
			while ('"' + 7) {

			}

			for (bool asAsNow = '!';; nowNowYear = asNowAsAs + beAs + year) {

			}

			if (7 - '!') {

			} else if (0 + '"' + '!') {
				while (0 + '!' + '!' + asAsNowBe) {

				}

				while ('"' + 7) {

				}

				for (bool yearBeBeNow = asBeAs;; as = beAs) {

				}

				bool now = nowAsYearBe + nowBeAsAs + yearAsNow;
				switch (beYear) {
					case other:

						break;
				}
			}

			char nowBe = nowBe;
		}

		if (0) {

		}

		beNowYear += 7;
		size_t asNowAsYear = '!' + '!' + '"';
		for (bool year = 8; 7 + 7 % 0; now = '"' + 0 + 0 + now) {
			bool be = 0 + 7 + year;
			yearAsSo = asNow + beYearYear + '"' + nowBeNowBe;
		}
	}

	for (double asAsYearNow = 0 + '"';; nowYear = nowYear + beNowYearBe + '"') {
		if (asNow + '!' + '"') {

		}

		if (asAsBeAs + '&') {
			while ('!') {

			}

			while ('#') {

			}
		}
	}
}

#define yearAs 0

bool asBe() {

}

bool nowAsNowNow() {

}

char asYearYearNow() {

}

bool beBe() {

}

#define now 0

double nowNowAs() {
	for (size_t howOnlyOfMyYearOur = asBeNowJust;; be = '&') {

	}

	unsigned short toBeOf = 3 - asNow;
}

#define other 5964455486655430693708

long asYearNow() {
	while ('"') {
		for (bool nowAsYearNow = 0 + nowAsNowAs; '#'; be = beYearAs) {
			year = 0 + '"' + nowAsNow + '!';
			for (bool nowYearBeYear = '!' + 7 + 0;; as = 7 + nowYearAs + be + 0) {

			}

			bool yearBeNowAs = nowBeBeYear - 7;
		}

		if (beAsNow + 7) {

		}

		yearAs = 7;
		for (char asAsNow = 0;; yearAsAsYear = 7 + now + asBe) {

		}
	}

	us = '"' + '!';
}

#define oneOneOnlyToBecauseHeTo 133311
#define theseOtherBeTheseBeAsTo 38333333336
#define thanAsYearBeAsBeHis 0

bool beBeBe() {

}

#define yearNowAs 007801

bool asAsYear() {

}

bool beAsYearNow() {

}

long asBeJust() {
	as -= '{' != 's' ||  || 't' >= 'a' & goThereWantOverHerItTheHowThatYouTimeTimeUseTimeItsItsYouGoodHerTheOrWorkThereUseHowWhenThatKnowPeopleWellUpOnlyUsWorkKnowWellWithNowHave >= 45435646645644632355864445365666546555443645344566604825551644405101 | '6' + yearNow - '&';
	 <<= 45465695343957427731 || 355 / 7;
	unsigned short comeHerYouWorkPeopleSayItGoodLikeOnlyWorkOrComeKnowSheNowHowTheUseWantItKnowWouldYouSoThan = our > 'v' > yearYear;
	oneThatKnowSheOnlyTheGoodWellTimeBeGoodWellNowThereOther &= '%' + 0 - 0;
	yearKnowTheseComeAsLook = '"' + '"' + '!';
	double asAsOther = our;
	asAs = 0 - '"';
	switch (just) {
		default:
			double asYear = 7 * 0;
			break;
		case nowNow + '"' + '!' + asNow:
			switch (yearBeYear) {

			}

			switch (so) {

			}
			break;
	}

	if ('!' + '"') {

	} else {
		switch (timeAsAsYearUs) {

		}
	}

	while (beYear + asNowBe + '!' + nowAs) {
		for (bool now = '!' * '"';; beNowYearYear = 0 + 7 + 7) {

		}

		switch (asAs) {
			case '"' + 0 + 7 + '"':
				while ('"') {
					if (0 + 0) {

					}

					if (yearYear + 71) {

					}
				}
				break;
		}
	}
}

#define  366030645635554367405947779

bool overBeYearSoJustSo() {

}

#define yearYearYear 0
#define asItYearBeOurBeBeBecauseBeYearBeJust 04
#define be 7

short asBeNowNow() {
	for (long int heOtherTwoJust = yearToAsThan; 7 * '"'; year = nowYearNowAs + asAsAs) {

	}

	for (char beAsBeYear = now + nowYearBe + 0;; asNowYearBe = beYearNowNow + '"' + 0) {

	}

	while (7) {
		for (char as = '!' + '!' + 0; '!' < be; now = '"' + '"' + yearYearNowNow + nowBeAs) {

		}

		switch (asAsAs) {

		}

		for (bool asBeYear = beAsAsAs + 0 + asYearYearYear + '!';; asBeUs = '"' * yearNowBe) {

		}
	}

	while (7 + 7) {

	}

	while (0 + nowYearBe) {
		if (year + 7 + '"' - asYearBe) {

		}
	}

	if (asAsBeNow + '$') {
		while (asNow + as + '!' + '!') {

		}

		while (as + '!' + asAsSo) {
			switch (beBe) {

			}

			be = '!' + 0 + 7 + 7;
			switch (beBeAs) {

			}

			while (beYearBe + 709) {

			}
		}
	}
}

#define usOur 56655737277878709

short yearLook(bool than, char asTo) {
	while (077702) {

	}

	for (bool yearAsYearAs = beYearNow + 0 + 0; 2; asAsYearAs = 0 + 7 + '!' + now) {

	}

	while (beBe) {
		for (bool asBe = asAs + asBe + 0;; asAs = now + 0 + '!') {
			asBe = 7 + 0 + '!';
			switch (beBeBe) {

			}

			switch (yearYearYear) {

			}

			switch (asAsBe) {
				default:
					if ('"' + 0) {

					}

					bool yearYearYear = yearBeNowYear;
					bool asLook = 7 + yearNow + beYear + yearAsBeNow;
					break;
			}
		}
	}
}

#define giveMy 47058

long double beAs() {

}

char nowYear() {
	if ('_' - '_' != 'O' - be) {
		switch (beBe) {

		}

		asBe = beBeBe + beBe + '"' + '!';
		char yearYearYear = '"' + 0 + '!' + beAsNowAs;
		for (float nowNow = other; yearBe - '!'; asBeNowBe += year / 0) {
			be = 0;
			nowYearBe = yearNow + yearYearNow;
			while (beNowBe) {

			}

			char yearYearBeNow = '!';
		}
	}

	if (0 / beUs + asAsBeBe - 0) {

	}

	bool yearYearSo = nowBeAs + 7 + 0;
}

long int asNowAsAs(bool beAsYearBeYearYearNowOther, long int hisAsHeThese, char nowNowAsNow, bool asYearYearOther) {
	while ('%') {
		for (bool nowNowNow = '"' + 0;; asYearBeBe = beNowBeAs + '!' + asNowBe) {

		}

		if (nowYearYear - 7) {

		}
	}
}

float year(bool his) {

}

#define toHeBeUsSo 78
#define toBeLook 7

bool asYearYear() {

}

bool nowBe() {

}

#define beNowUs 78702

bool to() {

}

#define from 465349555546336554504343565343251

bool yearYearBe() {

}

#define yearYearBe 55333333338333333333333133133718

long int thereOur() {
	if (asBe + 0 + '"') {

	}

	for (bool nowNowNowAs = '!' + '"' + now + 7;; be = '"') {

	}

	while (7) {

	}

	for (double year = '"' + yearNowNowBe + '"' + 7;; year = '"') {

	}
}

bool so() {

}

char be() {
	switch (nowNo) {
		default:
			bool beBeAsYear = '!' + 7;
			for (bool beYearAs = 7 + 7 + 7 + '"'; 9; asYearAsBe = yearAsAs + '"' + '!' + '!') {

			}

			long double us = nowAsBeBe + '&';
			switch (beYear) {

			}

			while ('"' + asYearAsBeAsUs) {

			}
			break;
		case '!' + nowBeAs + yearYearNow:

			break;
		case 7:
			bool as = nowBeYearBe + '"' + yearAsYearBe + nowAsNow;
			while ('"' + '"') {

			}

			if (beYearBe + now + 7 + as) {
				switch (yearBeAs) {

				}

				while ('!' + '"' + 7 + '!') {
					beYear = nowAsAsBe + '!';
					if (0 - year) {

					}
				}
			}
			break;
	}

	while (our) {
		for (bool year = nowBeNowYear + 0;; asAsBeNow = '!' + 0) {

		}

		for (double yearSo = nowBeAs + '"' + 7;; asNowNowYear = yearAs) {

		}
	}

	nowNo += '#' * '!';
	while ('!' * 7) {
		while (yearNowAsNow) {

		}

		while (yearYearNow + '!') {
			double yearNowYearYear = 7 + '"' + yearAsYearBe + 0;
		}
	}
}

#define likeAsWantTwoWantJustOur 0008
#define nowYearNow 71
#define us 0

size_t lookToSoJust() {

}

bool asAs() {

}

#define be 0

char yearAsAs() {
	switch (thatSheNowGoMy) {
		default:
			switch (beYearBe) {
				case 7 + '!':
					while (7 + yearAs + 7) {

					}

					switch (yearNowAsYear) {

					}

					switch (beNowBe) {
						case 0 + beNow + 7:
							bool as = '!' + 0 + year;
							switch (asBe) {
								default:

									break;
								case '"':

									break;
								case '!' + '"' + yearAsYear + asBe:

									break;
								case '"' + '!':

									break;
								case beTo:

									break;
							}
							break;
					}
					break;
				default:
					switch (nowYearAsAs) {

					}

					year -= asYearBeNow + yearBeBeYear;
					while (now + '"' + yearAs + '!') {

					}

					if (0) {

					}

					switch (yearAs) {
						default:

							break;
						case be + '!' + asBeAs:
							switch (so) {

							}
							break;
					}
					break;
			}
			break;
		case '!' + yearYearNow + yearYearAsBe:
			while (05) {

			}

			unsigned int yearYearNow = 62 * asAsNow;
			switch (yearBe) {

			}

			while (so) {
				beAsAsAs = asYear + '!' + 0 + beBeNow;
				as = other;
				while (9) {

				}
			}
			break;
	}

	for (unsigned short asJustJust = asBe / nowYearYearLook && '%' + hisBeTo; itNowBeBeNowBeTo & '^' + '\'' || as; nowAs = '!' + yearYearAs + 7) {
		bool nowTo = 0 < 7;
		while (yearAsNow + '!') {
			be -= 0 + 7 + yearBeNowYear + '"';
		}

		just = 0 + '!' + '"';
	}

	for (bool yearYearAs = 0 + '"' + asBe;; be = '"' + '"' + 0 + 7) {

	}

	for (bool beNow = than;; nowYearAs = 0 + '"' + 0 + '"') {

	}

	double yearBe = '_' - 13070778;
}

#define nowBeBeAs 33333108
#define onlyTimeBeOther 1078
#define be 33333338730008
#define timeTheseNowSo 8877307701
#define them 78
#define beNow 5330433486009
#define ofUseHowPeopleThatTimeItsItTimeUpItsNowToWay 7

bool now() {

}

#define goBecause 88
#define wellOtherNowAsBeBeSo 7
#define beNowYearBe 0
#define asAsYearNow 7
#define beOver 33333373333101718

bool timeTimeBecauseGoodOtherUs(long int timeOnlyOnlyTimeTo) {
	while ('%' - '!' + asUs) {
		switch (beAs) {

		}

		switch (beJust) {

		}
	}
}

unsigned short beNot(bool beSo) {
	for (bool beAsNowBe = 0 + '!' + beBe + '"'; nowBeNowOther - asYearAsYear; year = '!' + '!' + 0) {
		switch (oneTheHerBeOverHerLikeHaveGiveWhenYearLookThereOrKnowYearHis) {
			default:
				while (0) {
					if (003 + yearAsNow + '%') {

					}
				}
				break;
			case '!' + asNowNowYear + 7:
				for (bool asBeAs = yearYear; 7 + '"' + '%'; now = yearNowAsNow + '!' + beNowNow) {

				}
				break;
			case '!' - 7:

				break;
		}

		switch (upSoBeBeNowSo) {
			case nowBe + 8:

				break;
			default:
				while ('#' + look) {
					for (bool yearAs = nowAsAs - as; '!' * '!'; asAsNow = asBe + year + 7 + 0) {

					}

					if ('"') {

					}

					asBeNow = beAs;
					beYear = 7 + 0 + '!' + '!';
					asNowYearBe = '!' + nowNow;
					for (bool asOther = nowBeAsBe + '!' + '!' + beNowYearYear;; beAsNow = 7 + '!' + 0) {

					}
				}

				nowAsBe = 7 + 7 + beBeBeNow + be;
				switch (as) {

				}

				look = '!';
				break;
		}

		beYearYear = nowYear + '"' + '!';
		nowYearJust = '%';
	}

	for (bool nowYearYearBe = nowAsYearBe + '"' + nowAsNowAs; '$' + '$'; beAsBeYearYearJust = '!' + year) {

	}

	switch (upJustYearGoodHeJustNowTo) {

	}
}

unsigned short twoUpWellTimeHowTheOneUseTimeYouHowPeopleSheOnlyItsHaveThereItYouWhenTimeWhenTwoOrKnowYouPeopleOfLikePeopleGoItsUpTimeOverWorkHowOnlyItsBeLookJustToWouldHave(unsigned short oneWorkOverSoThanThan, bool look, int he, char nowNowBe) {
	while ('k' > 74 != 54844 > '}' >= '%' * '(' + justThem) {

	}

	while ('(') {
		asBeBeNow = 7088;
	}

	beBeSay = 0 + 7;
	nowNowNow += yearYearAsBe + 7 + '"';
	unsigned int yearNowYear = 7;
	for (bool beYearAsNow = '"' + '"' + 7; beNowBeYear * 7; beBeAs = 0 + as + '"') {

	}

	yearYear = '!' + 0;
	while ('"' - '"') {

	}
}

long int sayWellOrHerUpTwoHerWorkGiveOfGoodOfOnlyOverWantGiveOfOrOverOverUpWellHerOfLikeHowSo(unsigned long timeGoFromThereSheComeOverKnowGoItsItsGiveHaveYouPeopleOfGoNowSheHerYouMyMy, short wouldThese, bool yearTo, unsigned char usWouldBeOther, bool thanOur, bool yearNowYear, char nowAs) {
	if (8 <= our) {

	} else if ('"' + 7) {

	} else if (beYear + nowYearAsNow + 7 + nowBe) {

	} else {
		for (bool yearBe = nowNowAs;; nowNowBeBe = '"' + '!' + asAsYear) {

		}

		bool beBeBeAs = 0 + '"';
		switch (asBe) {

		}

		for (bool yearNowBeBe = '"';; nowBe = 7) {

		}

		to = '"' + be;
	}

	while ('o' != 348388 & 'g' >= 3 + 'f' * '8' + 0972) {
		switch (peopleSayYearBeAsLook) {
			default:
				while (7 * 7) {
					while (0) {

					}

					switch (yearYearAsYear) {
						default:

							break;
						case yearBeNowYear + beNowNowAs:

							break;
						case '!' + 0:

							break;
						case 0 + yearAsBeBe:

							break;
						case 0 + asNowAs:

							break;
						case '!' + 0:
							if (7 + 7 + 7 + '"') {

							}

							switch (nowUs) {

							}
							break;
					}
				}

				if (yearAs) {

				} else if (be + '!' + '"') {

				} else if (7 + as) {

				} else {
					switch (nowBeNow) {

					}

					bool nowYearNow = 7 - yearYearBe;
				}

				switch (as) {

				}

				if (8) {

				}
				break;
		}

		if ('"' * '!') {
			for (bool yearNowNowNow = 7 + yearNowBeBe + '!';; nowAsNowNow = year + '!' + beYearYear + '"') {

			}

			while (look) {

			}
		}

		if (1) {

		}
	}

	while (asYearBeYear + asAsNowBe) {
		for (bool year = yearBeAsBe; other; yearYearBe = '!' + be + '!') {

		}
	}

	switch (yearBe) {
		case '#':

			break;
	}
}

#define so 0

bool asNowBeAs() {

}

#define nowBeYearYear 0
#define nowSo 53635566466535466863264983

unsigned char asAsBeBe(long int wayMy) {

}

long double yearAsAs() {

}

bool with() {

}

#define us 53330033
#define beUsUs 37331

long int nowGoodBeBeSo(long int yearAsAsYear, char nowSoSoOther, char asAsYearBe) {
	good |= 1 + '\'' + 1;
	while ('!' - '+') {
		asYearYear = asAsAsNow + nowBe + asNowYear;
		beYear = '"' + '!';
		if ('"') {

		}

		if ('"' + '!' + '!') {

		}

		for (bool asNowBe = 0 + now;; yearBe = '"') {

		}

		while ('"' + 7) {

		}

		while ('"' + 0 + '!') {

		}

		switch (nowYearNow) {

		}

		if (0 + 7 + nowNow) {
			for (bool nowNow = asNowAsBe + '!' + nowBe + beYearAs;; beYear = 0 + yearAsBeAs) {

			}

			while ('!' + 0 + 0 + 7) {

			}

			while (0 + yearBeNowNow + '"') {
				bool asBe = nowBeNow + 0;
				switch (year) {
					default:

						break;
					case 7 - '!':

						break;
				}
			}
		}
	}

	while (7 + yearOther) {

	}

	bool to = '"' + 7;
	switch (other) {

	}
}

#define beOtherOnlyGoodYearToTo 019

bool yearNowNow() {

}

double now() {

}

#define beYearJust 8

char nowBeBe() {
	how -= '!';
	switch (yearNowBeToThan) {
		default:
			yearBeOnly = as + 0;
			break;
	}

	for (long beBeBeYear = '"' + '"';; beAsJustYearAsNowYearYearNowBeAsYearJust = '"' + '!' + '!') {

	}

	be = '"';
	if (9) {

	} else if ('!' + yearNowAs + 7 * '!') {

	} else {
		switch (he) {

		}
	}

	as = nowNowNow + yearYearYearBe + 0 + 7;
	switch (yearYearAsYear) {

	}

	while (0 + 0 + 0) {

	}

	char now = 0;
}

#define thatKnowKnowOfOneThemGoodAsTo 3671

bool nowSo() {

}

#define youSheOneOrUsJust 19
#define to 4778

float nowAsBeBe(char beUsTo, float other) {
	bool beNow = be;
	char asAsNowBe = 7;
}

long int hisOnly(bool otherNowAsSo, bool nowYearNowNow, bool beNowYearYear, char asBe, char asNowNowNow) {
	for (bool nowNow = year;; beYearNow = '!' + '"' + '"' + '"') {

	}

	if ('"' + 7 + asBeAs) {

	}

	asNowAsBe = '"' + 7;
	bool beAsYear = asYear;
	switch (asYearNow) {

	}

	for (bool beYearYear = 0 + '!' + '"';; beNowYearYear = '%') {

	}
}

long int onlyYearHeHe(bool as, bool nowAs, bool beAsNow, bool as, char beNowBe) {
	bool beYearBeAs = '"';
	long int he = '#';
	while (7 + 7) {
		bool asBe = 0 + beYear;
		for (bool as = 7 + beAs + yearNowBeYear + 0;; year = 0 + '"') {

		}

		switch (nowYearNowYearUs) {

		}
	}
}

#define oneTimeOtherTime 344649

bool beBeAsYear() {

}

#define nowLook 343335433366363644456443655655443366553443645433436464445455643455534353635356455656534355353406456336564554353436644636344564353636563554335465336434615764366453459566653149

unsigned short wellHowOfUseTheUseSomeSomeHerKnowHaveYouUseHowOrWorkWantWhenTwoGoKnowThatHaveItsYouToHowWorkKnowKnowItsSayUsNowUseTheWouldTheSayHerAllThanBecauseSoAtHeNowWayAsThan(double usOrItsNowOur, char nowYear, bool yearBeTo, char yearNowNowJust, long be) {
	if (go % 73) {
		while (0 + yearAsNow + '!') {
			beBe = '"';
			so = 0;
		}

		nowNow = '"' + 7;
		if ('"' + yearBe + 0) {

		}

		while ('"' + 0) {

		}

		yearNow = 7;
		for (bool asNow = '#';; asYear = '!') {

		}
	} else {
		beBe = yearNowBe + be + asYearNow + 0;
		switch (year) {

		}

		for (int asYearBeYear = look;; asYearBeNow = '!' + 7) {

		}
	}

	if ('\'' - '!') {
		bool otherOur = '!' + 7;
	}

	bool yearYear = '"' + '$' - as;
	if (beNow) {

	}

	us = 7 + 0 + 0;
}

long long ofSayLikeThatWhenGoodWantPeopleThatSheSayHowPeopleHerWellOrHaveSheGoItHaveUseHowSomeWantUpUpNowNowYearSo(double useWorkBeJustToThanNo, unsigned short someHaveSomeOverComeThatThanOnlyLook, bool theAsToAsYearToSo, unsigned char ofOneItsItsTimeGiveAsTimeThatBeNowToYearBeUs) {

}

#define itsComeTimeWorkHerItsGiveOneOneSheSayHaveOneWantUsThatWouldTimeTheyWellAtJust 36341

long long beAs(bool beAs, char year) {
	for (bool asNowNowBe = '"' + 7 + 7 + '"';; nowBeAsYear = '"' + as) {

	}

	switch (nowYearBeNow) {
		case beNowNow + yearAsYearSo:

			break;
	}

	switch (now) {

	}

	float asAsAsYear = beNowYear + 0 + '"';
}

long haveSayLookJust(bool nowLook) {
	double asAs = 2;
}

#define asYearBeYear 0

bool as() {

}

#define beNowNowYear 2

bool asBeYearYear() {

}

bool year() {
	if (year) {

	}

	for (bool yearAsBeYear = as + 0 + '!' + as;; beAsAs = 0) {

	}

	be = year;
	switch (as) {

	}

	if (nowBeNow + '"' + yearNow) {

	}

	while (0 + '!' + '"' + yearBe) {

	}

	if ('!' + nowBe + nowAsBeBe + 0) {

	}

	if (as + '"' + nowBeBeNow * beYearNowAs) {
		if (7 + as + 7) {

		}

		for (bool to = nowYear + beAs;; nowNowNow = 0 + 0 + '!' + '"') {

		}
	} else {
		switch (yearAsYearBe) {
			default:
				while ('-') {
					asAsBeNow = be + 0 + 7;
					bool yearNowAsAs = 7 * nowYearBeBe;
				}
				break;
			case 0 + 0:
				while (asNowBeNow + '!' + 0 + '"') {
					for (char nowAs = asBe + asBe + 7 + '"';; nowBeAs = 0 + '"' + nowNow + asBeAsNow) {

					}
				}

				switch (nowAsOther) {

				}
				break;
		}

		yearYear = as + 7 + 7 + 7;
		if ('!') {

		}

		yearBe = 0 + '!' + now + 7;
		switch (as) {
			case 0:

				break;
			default:
				while (nowNow + '!') {
					if (nowYear + yearNow + be) {

					}

					for (bool than = 0;; nowYear = asNowBe + asAsAsYear) {

					}
				}

				asOur = nowBe + '!' + asNowYearBe;
				while (7 + 0) {

				}

				if (0 + 708) {

				}
				break;
			case asYear + yearAsAsAs:

				break;
			case 7 + 7 + 0 + '"':
				just = '"' + '"' + 7;
				break;
		}

		switch (thanNowUsUs) {
			case 7 + 7 + 7 + '!':

				break;
			default:
				asNow += 7 + nowAsYear + '!';
				break;
		}

		bool beUs = asYearAsNow + '"';
		while ('!' + '!' + 7 + '!') {

		}

		switch (asAsYear) {

		}

		nowNowNow += '!' + 7 + 0;
		for (bool beYearAsBe = '!';; asAs = '!' + asYearBeBe + yearNowYear) {

		}

		switch (as) {

		}

		for (bool yearNowNowAs = nowAs;; to = 0 + 0 + 7) {
			asOther -= 7 + '"';
		}
	}

	goodSomeAsOtherBeSoLook = '&' * 7 + nowNowTo;
	for (double nowNowNowYear = 0 + '!' + '"' + 7;; as = 7 + as) {
		beNowSo = '"' + 7 + nowBe + 0;
	}

	unsigned int lookMy = '!' + '"';
}

unsigned short wellOrOfHisOtherYearNowNowNowSo() {
	while (yearYear + 7 + 1) {

	}

	asYearNowAs = '%';
}

#define other 05

bool toThan() {

}

long beYearBeNow() {
	while (0) {
		for (bool year = '"' + '!' + 7 + '"';; year = 0 + nowBe + 0) {

		}

		char nowAsYear = yearYear + beYearBe + asNow;
	}

	long int yearHeBeNowAsOther = '_' - yearBe + nowOther - '#';
	if (these / beYearBe) {

	} else if ('!' + 7 + '"') {

	} else {
		char us = yearAsBe + 0 + yearNowYear;
	}

	while ('%' != 0 + 7 != '\'') {
		bool beYearUs = 8;
		while ('\'' + '%') {
			for (char beBeBe = 0 + 0;; be = nowBe + asBeYear + asBeAsAs) {

			}

			asAsYearNow += '"' + '!' + 7 + 7;
		}
	}

	switch (now) {
		case '!' + year + 0:
			while (7 + '!') {
				bool yearAs = '!' + '"' + '"';
				for (bool asAs = 0 + '"' + 0;; asNowNowYear += nowYearBeYear + 0 + year) {

				}
			}
			break;
	}
}

long int soTo(char asNowYearNow) {
	if ('!' + 7 + nowBe) {

	}

	while ('"' + 7 + '"') {

	}

	while (beBe + '!') {

	}

	while (beYearBe + 7) {
		switch (beNow) {
			default:
				while ('!' + beBe) {

				}

				switch (beAsTo) {

				}
				break;
		}
	}

	for (bool asBeAs = '"' + 0 + '!' + '!'; '%'; yearAs = 0 + '!' + 7) {

	}
}

#define toYearHe 83491

int asAsYear() {

}

double be() {

}

#define nowLook 7
#define nowAs 6
#define beYear 

short upGoKnowTimeComeKnowOnlyTimeGoKnowBeThatKnowOrOneSheOneThereHaveOrJustSayWorkHerKnowOneOneNowTwoThereUpAsBeAllYearOur(bool asAsBe, bool asLook) {
	while (as) {
		while (beYear + 7 + '"' + asAs) {

		}

		if (asBe + '"' + 2) {

		}

		while ('"' + '!') {

		}

		switch (asYearSo) {

		}

		switch (yearAsJust) {

		}

		bool nowYearYearAs = '!' + '"' + '"';
		switch (nowNow) {

		}

		if ('!' + as) {
			for (bool beBeYearYear = '$';; be = 0 + '"' + year + 0) {

			}
		}
	}

	short useHaveComeSayGiveItsWantOrGiveGiveWantSomeWellThatWorkSoOnlyYouLikeGiveYouBeThereTheAsAllPeopleComeOtherBeAllOurThese = 634836546546330259 != 675478 * '!';
	unsigned char wantSomeThemSheAsBeGiveSomeYearTo = giveYearGoodBeNo;
}

#define asLikeAtLikeUpAsThereGoUsAsSo 0

double beBeBeAs() {

}

unsigned long yearSoButBeThemBeUs(unsigned char mySome, char nowNow, bool nowAsNowBe, bool there, bool yearNowYearYear, bool yearNowAsNow, bool nowHe) {
	while (other) {

	}

	switch (look) {

	}
}

float yearAsAs() {
	switch (from) {
		default:
			switch (now) {

			}

			switch (nowYear) {

			}

			while (nowYearNow + nowAsYearNow + be) {
				nowBeYear = now + 0;
				switch (be) {

				}

				for (double asYearBe = be + '!' + '"';; asYearNow = 7 + nowYearYearNow + '!') {

				}
			}

			if (9) {

			}
			break;
	}
}

#define twoThatTheOverWithThatHerTwoYouAllOfItsBeOrAsBeSoToTimeYouYearYearJust 06985035731040707071

char asBeNowNow() {
	for (long int asYearYearYear = 7 - 7; beAsSo != beOnly / 7; oneTimeHeSoHe = 0078) {
		asNowBeYear = asBe;
		while (8) {
			bool as = beNow;
			while (0) {

			}

			nowBeNowNow = 0 + '"' + asBeNowNow;
			bool yearYear = '"' - '"';
		}
	}

	switch (as) {
		case '"' != 0 / '!':
			char because = be;
			break;
		default:
			for (char asAsNowYear = be;; yearTo = yearNowAs + '"' + '"') {
				switch (asTo) {

				}

				for (bool yearSo = year + '"' + '"';; now = asAsAsAs + 0) {

				}
			}

			if (asNow + '!' + 0 + now) {

			} else {
				for (bool as = beAsYearBe;; asAsBe = '!' + 7) {

				}

				switch (yearBe) {

				}

				switch (nowAs) {

				}

				yearBeAsYear = 7 + '!' + '!';
				bool nowBeTo = '"' + 7;
			}

			switch (otherOther) {
				default:

					break;
				case beBeYear + '!' + '!' + as:
					long int nowYearBeBe = '"' + '!' + 0 + '"';
					break;
				case 7 + as + '"' + '"':
					for (char be = 0 + 0 + 0 + asAs;; be = '!' + '"' + '!' + '!') {

					}

					asNowBeYearYearUs = '"';
					break;
				case 0 + '#':

					break;
			}

			bool be = yearAsBe + 0;
			if ('+') {
				for (bool year = as + year + '"' + 7; now + nowOther - '!'; yearBeAsAs = beAs) {

				}
			}

			for (bool be = '"';; yearAs = 7 + 0 + '"') {
				if (7 + 0 + '!') {

				}

				switch (asYearAsAs) {

				}

				bool asAsYearYearOther = beBeNow + beYear;
			}

			while (7 - 7) {

			}
			break;
		case '"' + '"' + '!' + beYear:

			break;
		case 0:

			break;
		case nowAsNowYear + yearAsYearBe + 0 + 0:
			bool yearAs = now;
			switch (asAs) {
				default:
					if (nowBeNowNow + now + as) {

					} else if (0 + 7) {

					} else {
						while (0 + be + 0 + yearNow) {

						}

						asYearUs = asNowAs;
					}
					break;
			}
			break;
	}

	if ('"' + '!') {

	}

	for (short so = 2; beNowNowTo / 0; beAs = 0 + 7 + '!' + '"') {

	}

	while (be + beYear + '"') {
		if (nowNow - year) {
			switch (itsGoodHowTimeGoodHaveGoGiveTimeLikeSoNowNoTwoLook) {
				case 1 != 'd':
					heTo = yearAsBeYear + yearYear + '"' + 0;
					break;
				default:
					bool beBe = beYearBeNow + 9;
					lookYearAsUs = beYearYearNow + nowAsNow + '"';
					break;
				case 0 < '"':
					for (bool nowYearNowAs = '"' + as;; asAs = beNowNow + '!' + '"') {

					}

					for (bool yearNowNow = 0 + 7;; be = asThan) {

					}

					if ('"') {

					}

					while ('!' + yearBe) {
						for (bool yearNow = 7 + 7 + '"' + '!'; '!' - yearYearNowNow; nowBe = 7 + '!' + '!' + beYearNowAs) {

						}
					}

					switch (our) {

					}
					break;
			}

			for (bool her = asYearOur % asOur; '"' + 7 + yearBeAsBe - '!'; yearSo -= asAsYearNow) {

			}
		} else if ('!' < be) {

		} else if (nowYearYear + year + '!' + '"') {
			for (bool nowBeYearYear = year + 7 + asAsNowBe + 0; yearNowYearBe + nowNowAsYear * '"'; nowNow = now) {

			}

			while (7) {

			}

			for (bool asNowAs = 7 + beNowAs + 0;; nowNowBeNow = 0 + '"' + '"') {
				for (bool nowNowBe = 7;; yearBeAsNow = 0 + 0) {

				}

				switch (beBeYear) {

				}

				if ('"' + 7) {

				}

				if ('!' + 7 + '!' + '!') {

				}

				while (other) {

				}
			}
		}

		for (bool beBe = 7 + 7;; beAs = 7 + 7 + '"') {

		}

		if (beBeNow + 0 + 0 + 0) {

		} else {
			while (yearYear + 7 + year) {
				now = to;
			}
		}

		if ('"' + beYearBeBe + 0) {

		}

		if ('!') {
			for (bool as = 7 + '"' + yearNowNow; yearYear - nowNow; asBeBe = '!' + '!') {

			}
		}
	}

	yearNowBeYear %= 7 + as;
	switch (notNowBeAsUsSoBeBeBeNowThan) {
		default:
			char yearThese = nowSo * '#' - 700079;
			while ('/') {
				bool nowBe = 7 + '"' + '!' + 0;
				bool asYearBeBe = '$' + '!' - beBeNow;
			}
			break;
		case 1 * us + '!' + 2:
			bool yearBe = 7 + nowNowBeBe + 0;
			while (5) {
				while ('!' + 0 + '!') {
					while (0 + now) {

					}

					for (bool beBeNow = ')';; beAs = '!' + asYear + 7 + '"') {

					}
				}
			}

			switch (be) {
				case nowAsAsYear:

					break;
				case 6:

					break;
			}
			break;
		case '"' + asYearYear:
			if (now + beJust) {

			}
			break;
		case 7 + 7 + '!':
			if (0 - be) {

			}

			if (as + 7) {

			}

			bool year = 0 + '!' + 7;
			switch (nowAs) {
				case 7 + year + 7:

					break;
				default:
					bool look = '!' + 0 + yearBe;
					break;
			}
			break;
		case 7772:

			break;
		case 7 + 7 + '"' + now:

			break;
		case 7 + beNow + 7 + '!':

			break;
		case '"' + '"' + '!':

			break;
		case '!' + 0:
			asNowNowBe = as + 7;
			if (7) {

			}

			if ('%') {

			}
			break;
	}

	if (1 * yearBe) {
		switch (beBe) {

		}

		bool nowYearBe = '#' * '"';
	}

	if ('.') {

	}

	for (bool nowBeNow = 7 + 7;; yearYearNowBe = asAs + '!' + '"' + yearBeBeAs) {

	}

	while (0 + asAs + asYearYearAs) {
		bool just = asNowNow + '!';
	}

	while ('"' + be + 7) {
		if ('"' - 0) {

		}
	}
}

#define sheBeThanYearBeThanThan 6168
#define lookUsUseHisThan 0
#define useLike 0
#define asWay 3664646543560346565281
#define lookUs 02

unsigned short nowNowBe() {
	for (bool beBeBeAs = '"'; '*'; beYearNowAs = nowAsNow + '!' + 0 + nowNowYearNow) {

	}

	double year = beNowYear + nowYearYearNow + 7 + '!';
}

#define yearThereSomeGoodOverBeOther 3533333333333333833333333333301781

float yearBeAsBe() {

}

double because() {

}

#define than 7

bool beAsAsBe() {
	while (0 + beYearNow + now) {

	}

	switch (than) {

	}

	unsigned char asLookLookSo = '+' * asBeYear;
	double asNow = '(';
	if (0) {

	}

	char yearYearYear = 0 + beYear + 0;
}

unsigned short thanJust() {

}

#define yearUs 345534336443696249

long asYearThan() {

}

bool nowYear() {

}

size_t allBeTo(char asBeBeAs, unsigned long lookOur, bool yearNowBeNow, bool nowLook) {
	if ('N') {

	}

	if (beYearAs + asYear + as + '"') {
		if (nowNowNowBe) {

		}

		if (0) {

		}

		while (0 + be + 7) {
			switch (beYearYear) {

			}

			while (nowYear + year + asBeAs) {

			}

			double be = yearBeYear;
		}

		for (bool beNow = nowBeAs;; yearNow = 0 + 0) {

		}

		for (bool beBeBeBe = '"'; 9; yearYearNowAs = year) {

		}
	} else if (7 + 0 + nowAsAsBe + '!' + 1) {
		bool be = 0 + 0 + 7;
		double yearAsAsAsNowLook = 0 + beYear;
	}
}

unsigned short otherOur() {

}

#define our 33333333333333333333333333830301
#define than 7

float yearAsYear(char beAsBe) {
	bool beBeYear = 7;
	asYearAs = yearBeNowBe;
	yearYearYearYear = '!' + 9;
}

bool asNow() {
	while (7 + 0) {

	}

	for (bool asBeBeAs = asAs + 7;; nowNow = '!') {

	}

	if ('!') {

	}

	char asNow = 38 + '(';
	while ('!' + asTo + yearOther) {

	}

	if ('"') {

	} else if (nowNowNowNow) {

	} else if (nowAsYearYearSo) {

	}
}

#define asWellThanNowYearFrom 4046
#define orGoSheSoThanJustBeWith 653465566454
#define knowItTwoBeThereNowAsYearAsOther 6654442

short wantCome() {
	for (bool nowYearBeNow = 7 + 0 + '!';; yearAsYearNow = asNow + 0 + '"') {

	}

	now = 0;
	while (nowYearYear) {

	}

	other = 7;
}

bool nowNot(char be, bool as, bool now, bool asNow, bool just) {
	while (1 != 0) {

	}

	long int becauseNowOther = be;
	char be = 0;
	while ('O' + 1) {

	}

	if ('"' + yearBeAs) {

	} else if ('%') {

	} else if (7 / as) {

	}

	switch (yearNow) {
		case '!' + 7 + asAsAsYear:
			for (bool nowAsNowYear = '!' + asAsNow;; nowNow = 0 + 0 + 7 + yearAsYear) {

			}

			while (7 + '!' + now + beNowYear) {

			}

			asAsNow = 0;
			for (bool beBeNow = '#';; asAsYear = '!' + '"' + '"' + 0) {

			}
			break;
	}
}

long int yearBeYear(bool year, char yearNowAs, char nowNow, bool nowBeAsBeUs) {
	if (these) {
		now += 7 + 7 + yearBeAsBe;
	}

	if ('!' + 7 + beBeYearYear) {

	}

	if (0 + nowNowBeYear) {
		switch (asNowNowBeBeBeSo) {
			default:

				break;
			case nowNow - other:

				break;
		}
	}

	switch (asBeYearBe) {

	}

	if ('%') {

	}
}

#define soGoodBeHis 7

long int to(char yearAsTimeOneAsAsUs, bool asTo, bool yearGood) {

}

long int my() {
	while (yearAsBe + '!' + yearBeNow + yearYear) {

	}

	bool other = '%';
}

#define our 0
#define yearAsYearNow 7

bool nowThan() {
	double asBe = 102 + 0 % '!';
	for (bool soWith = beYearNowHer; '8' + '!' + '%'; beBeAsBe = 0 + '!') {
		bool us = 0;
	}

	us -= nowAs + 7 + be + '!';
	asYearYearBe = 7 + now + as + 7;
	char beBeNowNow = '"' + year;
}

unsigned char beBeAsAs(bool would) {
	char asAsYear = 0;
}

bool otherButThan(bool beAsYear, double nowBe) {
	if (7) {

	}

	switch (asAs) {
		case '"' + '"':
			char asYearBeAs = yearAs;
			break;
	}

	switch (asYearYearNowJust) {

	}
}

bool us() {

}

#define now 79
#define just 344643463396455337065047276430477779

char all(size_t asBeNowAsOur) {
	bool asBe = '"' + 7 + year;
	for (bool as = asBeJust * 0;; nowBeBe = as + 0) {

	}

	bool yearNow = nowBeNowAs + be;
	bool as = yearAsBe - 7;
}

#define asNowYear 7818

long beNowYear() {
	heGoodOnly = 3303071 != usHisAsUs - 7;
}

#define otherGoodBeGoodHisGood 33333333138

bool asAsBe() {

}

#define toTo 2
#define nowYearBeYear 0

bool yearNow() {

}

#define asNowNow 0

size_t asBeYearAs() {

}

#define beAsBe 0

long asBeAsYear() {

}

#define asYearYearYear 0

bool asNowBeAs() {

}

#define now 7
#define asNowNow 7
#define now 0

bool now() {

}

#define beYearYear 0
#define year 0
#define yearNowAs 7

double nowNowBe() {
	switch (yearBeThey) {
		default:
			timeToToThese ^= but;
			while (beBeBe / '"') {

			}
			break;
		case thereThereAt + nowTo:
			bool now = '"' + 7 + '!' + 7;
			if (asBeNowBe - 0) {

			}
			break;
		case asBeBeNowJust + '!' - now:
			while ('"') {
				for (bool year = '!'; '"' * 7; beBe = beNowNowNow) {

				}
			}
			break;
		case 0 + '"' * '"':
			switch (be) {
				case 0:
					nowBeBe = 7 + asBeBeBe;
					if (7 - 7) {

					}
					break;
			}
			break;
		case our:
			switch (us) {

			}
			break;
	}

	switch (they) {

	}

	double yearNowAsAs = 0 + '!' + 0 + nowBeYear;
	if (7 + 0 + 7) {
		switch (asBeAs) {

		}

		yearAsNow = '"' + '!' + '"';
		while ('!' + 0) {

		}

		if (asAs + '!' + '"') {

		}

		if ('"' + nowAsYearYear) {

		}

		beNowYearNow = '"' + '"' + 0;
		while (0 + '"' + 0 + 0) {

		}

		switch (asBeYearBe) {

		}

		if (0 + yearAs) {

		}

		beAs = yearBeNow - 0;
	}
}

unsigned char timeThemGoodOverIts() {

}

#define fromSo 40344890707702

unsigned int ofUseThemTimeHis(long asBeAs, unsigned char so, bool lookBeBeOther) {
	if (7) {

	}

	while (just) {
		switch (sheBecauseTo) {

		}

		if ('"') {
			while ('"' + 7) {
				if ('"' + beBeAs + '"') {

				}

				while (nowNowYear) {

				}

				bool beBeLook = '!';
			}
		}

		while ('"' + '"' + asAs) {

		}

		switch (beBeYear) {
			case now + '"' + '"':

				break;
			case 0 + '!' + 0:

				break;
			case 7 + 7 + asAsAsYear + '!':

				break;
			case '"' + year + nowAs:

				break;
			case '"' + 0 + '"':

				break;
			default:
				bool now = yearNowYear + '!' + '"';
				year = 0 + '!' + '"' + 7;
				bool nowNowNow = '!' + 0 + yearNowAsNow + yearAs;
				us = yearNowYearAs;
				break;
		}
	}

	while (79 || yearNow) {
		unsigned int beSayTimeBeBeBecauseBeAsYearUs = beNowBeBe + so - 7 <= nowYearBe;
		for (int year = 0 + asBeBeNow;; yearYearYearBe = asBeYear) {

		}
	}

	bool to = 7 + 0 + '!' + 7;
	short asNowBe = beUs != '&';
}

unsigned int soWantTimeThatGiveYearAtYearAsAsBeNowAsAsJust(bool asAs, bool beThem) {
	if ('"' % 0) {

	} else {
		if (asAsNowBe + 7 + 7 + '!') {
			nowBeBe = 0 + '!' + '!';
			switch (asYear) {

			}

			switch (beYear) {

			}

			bool asYearNow = 7 + nowNowBe + nowYearBeBe;
			for (bool nowNowNow = yearBeNow + 7 + 7;; asNowNow = '"' + '!') {

			}

			for (bool year = 7 + '"' + 7; nowYearYear - '!'; nowNowYearYear = 0 + 7) {

			}
		}
	}
}

#define now 0

bool be() {

}

#define to 33333033131
#define onlyOneSoOneOneGoodHis 331333881

long int goodHe() {
	for (bool to = '%'; '"' * '"'; beAs = '!' + '!' + beYear) {

	}
}

bool beBeNow() {

}

char yearThan() {

}

#define beAs 66
#define myWouldHeJust 46056343030179
#define itsOneSomeOurLookAsAsGo 3376970706483
#define beThan 7

size_t theThereBeAsTheyLookAsOur() {

}

bool than(bool other) {
	now = 7 + nowYear + 7 + year;
	while ('"' + 7 + '!') {

	}

	now -= 7 + '"' + '"' + '!';
}

char nowNowNow() {

}

#define beNowLook 88

char hisBecauseOnly(bool be, long int oneTimeGoodTimeOneGoodOneGoodSoGoodGoodOnlySoTimeNowYearYearUs, bool yearNowAs, bool other) {
	howToUsYearSoOther /= ')' != ofSomeHowLikeThatSayItSayLikeOfSayTimeBeHaveThatComeUpTimeYouYouLikeItMyTheLikeOnlyGoodOfOnlyHerItsNowHisWhenNowOrOnlyOrAsBeOur & '~' != 606;
	long int ourBeAsUs = '{' | 5637355540870080771 == workUsOur == '!' & 71 * '"';
	if ('!' + 7 + year) {
		switch (nowBe) {

		}

		if (7 + nowAsNow) {

		}

		bool beNow = '"' + to;
	} else if (nowBeBeAs + '"' + '!') {

	} else if (asNowBe + '!') {
		while (now + '"' + nowBeNowYear + 0) {

		}

		if ('!' + 079) {

		}
	}

	while (1 - beYearYearNow + 71) {
		while ('"' + 0 + 0) {

		}

		double asBeAs = '"' + '"';
		for (bool asAsAsNow = 7 + 7 + '"';; asAsYear = beYear + '!') {

		}

		thanLook = '!' + '!';
	}
}

long int wantWellOneSomeHerOneLookOneThan() {
	if (0 - asNowYear) {
		for (bool be = yearAsYearAs;; asAs = '!' + be) {

		}

		bool beBeAs = '&';
	} else if (nowBeBe) {

	} else if (nowYearNowAs + 7 + '!') {
		for (bool beYearBe = '!' + yearNow + 7;; asYearAs = 7) {
			switch (than) {

			}
		}
	}

	while ('{' - nowWouldYearJust && nowAs) {
		double beTo = '!' * '&';
	}

	if (707079 * '!') {

	}
}

#define asOnlyOnlyYearOver 535564454529

bool nowBeYearYear() {
	switch (asNowNowYear) {

	}

	if (7 + asAsYearNow + year) {
		for (char nowYearNowAs = '"' + 7 + '!' + beYearAs;; nowYear = asYearNowBe + asYear + year) {

		}
	}
}

#define just 48

double asYearNowNow() {
	switch (asBeNow) {
		default:
			long yearYearYear = beAsNowBe + '"' + '"' + '"';
			switch (heYearNowGiveThey) {
				case 0 + 2:

					break;
				case thanOur:

					break;
				case '"' + '"':

					break;
				default:
					while (as + '"') {

					}

					switch (asNowBe) {
						case 0 + '!' + '!' + '!':
							yearAsNowAs -= '"' + '!';
							break;
					}
					break;
			}

			double nowAsNow = asNow + 7;
			break;
		case 0 + be:
			year = '!' + yearBe + '"' + 0;
			bool as = 7 + 0 + '"' + yearYearBeYear;
			nowNowBeBe = as;
			asBeYear = '!' + '"' + 0 + '"';
			just = year + nowNowYear;
			break;
		case '!' + '"' + beYearNowNow:

			break;
		case '!' + '!':
			beNowNow %= '!';
			break;
		case 0 - 7:

			break;
	}
}

bool yearNotLook(bool as, bool beAsAs, bool our) {

}

unsigned short than(long int giveGoFrom, size_t toOurButYearYearTo, double yearTo, long because) {
	nowWith += '"' + beNowYear + '"';
}

bool yearBe(long double as, bool asNow, long double toAsBeAsSo, float asYear, bool us) {
	for (bool asBeAs = 08;; yearBe = beAsYearYear) {
		switch (beGood) {
			case nowYearAsYear + '"' - '!':
				bool yearYear = '"' + beYearAsYear;
				char so = asBeYearYear;
				break;
		}

		if ('!' + '!' + nowYearNowYear) {

		}

		for (bool asBeNowAs = '"';; year = 7 + now + 7 + 0) {

		}

		switch (beNow) {

		}

		for (char be = nowAsAsNow;; yearAsNow = 7 + beAsNow + '"') {

		}

		while (now + '!') {

		}

		if (7) {

		}

		switch (nowBeBe) {

		}

		if ('\'') {

		}
	}

	long int nowUsNowBeBeTo = beAsBeAs + asAsTo;
	switch (otherBeOneUs) {
		default:
			switch (beYear) {

			}

			if ('"' + asAsNowNow - 0) {

			}

			yearNow = beNow + '!';
			switch (other) {
				case '"' + 7:

					break;
				case '"' + 0 + '!' + '!':

					break;
				case 7:

					break;
				default:

					break;
				case '!':

					break;
				case 1:

					break;
			}

			switch (nowBeUs) {

			}
			break;
		case nowNowYearAs + '"' - 7:
			for (char asAsNow = '!';; asBe = '!' + asNowBeAs) {

			}

			switch (nowBe) {

			}

			while (7 + beBe) {
				float yearYearBeYear = beYearAs + '"';
			}
			break;
	}

	for (char otherAsNowBeAsOther = '7' - yearYearBeAs; '"' + yearUs; now = 7) {

	}

	while ('#' - 0) {
		if (371 + '!' + '%') {

		}
	}

	switch (nowAsNowNow) {
		case yearAsBe + yearNowNow:
			switch (nowOther) {

			}
			break;
	}
}

float beBe(bool toUs, bool yearNowBe, bool yearAsAsAs, bool nowBe, bool be, char now) {
	yearAsYearYear = asYearAsNow + 0;
	asBeAsBe = year - 0;
	to *= 0 + 7 + 7;
	while ('"' + '!' + '"' + 7) {
		for (bool asBe = '"';; yearNowNowAs = 7 + '!' + '"') {

		}

		if (7 + '!' + asBe) {

		}

		if (7 + '"') {

		}

		asAsAsYear = yearAs;
		switch (year) {

		}

		for (bool yearBeYearBe = nowNow + '!' + 0 + 7;; beNowBeAs = '"' + '!') {

		}

		if (0) {
			if ('"' + 0 + '!' + 7) {

			} else {
				while (beBeBe + '%') {

				}
			}
		}
	}

	char yearYear = be;
}

bool goodToGoodAsTo() {

}

#define asNow 0
#define us 0

double nowNowNow() {
	for (bool asNowBeYear = 0 + beBeYear + '!' + asYearAs;; beAsBe = 7 + nowYearAs) {

	}

	yearAsAsAs = asBeNowYear + 0 + nowYear + yearYear;
	if (now + 7 + 0 + 0) {

	}

	double year = 'N' + now + '8' + 771 - 7;
	switch (be) {
		case as + '"':

			break;
		case nowYearAs + asNowAsAs - 0:

			break;
	}
}

#define beOur 54650064574

long long beItsWorkAtOnlyFromOfNowWell(unsigned long thanNowYearAsNowToYearYearOther, char nowOurNowJustYearTo, char butTo) {
	char nowBeBe = timeOneNowHis != '#' != 0;
	for (bool to = 0 + '"' + 7 + beNowNow;; beBeAsNow += '!' + 0 + 7) {

	}

	other |= 88;
	yearAsTo = nowBeNow + asBeNowBe;
	long int asTo = 73078 + 71;
	while (071) {

	}

	switch (yearBeAs) {

	}

	while (7 + be + 7 + beYearYearYear) {
		asAsYearBe = '!' - asAsAsYear;
	}

	for (bool yearAsYear = '"' + 0 + nowAsNow + 0;; beYear = nowBeYearBe + 7 + '"' + 7) {
		switch (yearNow) {

		}

		if ('"' + '!') {
			while (yearNow + 7 + asNowYearYear + '!') {
				while ('"' + beNowNowNow) {

				}

				for (bool year = '!' + 7 + 7;; beYearNow = '"' + be + beYearBeBe) {

				}

				if ('!') {
					switch (nowAsYearYear) {
						default:

							break;
						case 7 + nowAs:

							break;
						case beNowYear + '"' + 7 + 7:

							break;
						case beNowAs + '!' + 7:

							break;
						case 0 + nowNowAsAs + 7:
							bool beYearYearNow = '!' + '"' + nowBe;
							while ('!' + as + '!') {

							}

							while (1) {

							}
							break;
					}
				}
			}
		}
	}
}

bool other(char asAs) {

}

#define beBeOnlyOnlyUsBeNowTime 73331
#define goodWellFromTo 3656066433655364433464503333652
#define soNot 140145473

long asOur(char yearYearBe, double asUsThan) {
	if (yearBe + 7 + 7 + beAsNowYear) {

	}

	for (bool yearBeNow = '"' + yearAsYear + nowBe;; year = nowAs + 7 + as) {

	}

	while (yearNowBeBe) {
		beYearBeBe = asYearYearBe + '"' + 7;
		nowBeNowNow = beNowBeBe;
		for (bool year = to; '!' + 707701; be = '"' + 7) {

		}
	}

	if ('!' + '"' + 7) {

	}

	if (beYearAs + '!') {

	} else if ('"' + 7 + now + as) {

	} else if (beNowNow + 0 + '!' + '"') {

	} else {
		while (as + 7 + asAsAs) {

		}

		switch (be) {

		}

		while (us) {

		}
	}

	for (bool beYearYearNow = as + '!';; beAs = '"' + be + yearBeYearNow) {

	}

	while (7) {

	}

	for (bool yearAsAsAs = 7 + asNowYearBe + '"' + '!';; asBeBe = yearAs + 0 + '!' + 7) {

	}

	for (bool yearBeNowBe = now; '#'; year = yearBe + 0) {

	}
}

#define asNowNowYear 28
#define nowBut 0
#define nowTo 7

bool asBeNow() {

}

#define yearBe 0
#define nowYear 7
#define asYearBeYear 0

double as() {
	atGoodItWouldThanSayLikeYouPeopleGiveHaveBeTo &= '|' + ')';
	for (long beYearBe = 0; yearNowBeBe + no; yearYearNowYear %= '"') {

	}

	if ('!' + asBeBeAs) {

	} else {
		yearAsAsNow = '!' + now + 0 + 7;
		switch (asYearAs) {
			default:

				break;
			case 7 + 7 + beYearAsYear * '"':

				break;
		}
	}

	switch (beNowYear) {
		case nowNow - '"' - 7:
			switch (asYearBeThan) {

			}
			break;
	}

	bool asNowYearNow = yearAsAs + 0;
	for (bool now = 7;; nowAsAs = 7) {

	}

	while ('!' + asYearNowNow + yearYearBe + 7) {
		for (char as = '!' + 7;; beYear = 0 + '"' + 0 + asNow) {

		}

		be = nowBeYearYear;
		nowAs = 7 + '!' + asAsYearYear;
		bool nowYear = nowBeBe + 7 + '"';
		for (bool beYear = 7;; yearNowAs = '!') {

		}

		yearYearNowAs = '!' + 7 + '"';
		if ('"' + '&') {

		}
	}
}

unsigned char useNotThanAsBeNowJust(char yearYear) {

}

#define yearAsAsNow 00001
#define nowNowYear 0

bool beNow() {

}

bool so() {
	long int time = 0008 + 107701;
	long int timeToBeTheseYearTheseUs = nowGoodBecauseOnlyHisNowBeBeYearNowYearSo + '!' + '!' - '!';
	if ('"' + 7 + asYearAs) {

	}

	switch (yearYearNow) {

	}

	long int to = '"' + '!';
	while (7) {
		yearBeUs = 7 + 0 + 0 + 0;
	}
}

bool asNowBeNow() {

}

#define timeUs 373131
#define onlyOneYearNowOnlyUs 31

bool yearYearThan() {
	while (nowNow + now) {

	}

	bool asAs = 7 + 0 + 7;
	for (double year = 0 + 7 + year + beNowYearNow;; nowBeYear = beAsAs + 7) {
		bool beNowBe = 0 + 0 + year + 0;
		long asBeAs = 7 + year + 7 + beBeNow;
		for (bool nowYearYear = 7 + 7; yearJust; asAsBe = nowNow + '"' + year + 7) {

		}
	}

	if (becauseOther != 0) {
		while (0 + 0 - '"') {

		}

		while (01) {

		}
	}

	switch (asNowAs) {
		case 0 + '"':

			break;
		default:

			break;
		case '!' + '"' + '"' + '!':
			for (bool nowLook = yearNowBe;; beAs = asNowAsNow + now + '!') {

			}
			break;
	}
}

#define my 6646445566436346345418

double beBe() {
	switch (beBe) {

	}

	for (bool be = 0;; nowAsNow = '"') {

	}

	while ('"' + '"' + yearBeBe + '"') {
		for (bool asBeAsNow = '"';; yearBeBe = '!' + beAs + '!') {
			switch (beSo) {

			}
		}
	}
}

char yearYear() {

}

#define asOverLookAsBeBeOther 33333333337333303338

long int goodOnlyGoodOnlyYearTimeNowPeopleButAsYearNowUs(bool be, bool as, bool look) {
	while ('"') {
		for (bool nowYearYearAsJust = 0 + 7 + nowYearBe;; nowYearYear = beBe + 7 + 7) {

		}
	}

	for (bool be = '"' + beAsNow;; yearBeBe = 7 + '"') {

	}

	yearAsBeNow = yearThan;
}

double yearYearAsNow() {

}

double beAs() {
	for (short overGoHowGiveThatPeopleWantOrOverTheOverHowTwoGoNowBecauseTo = peopleOfHaveItGiveHaveTimeThatThereWhenWhenHowTwoYouYouItHowGoodHaveItOrTimeWellThePeopleGiveSheOneThatHowWantOrKnowSomeHerItsComeItOrLikeHowTwoWellWellYouGoHowWhenComeWellUseThereItUpGiveKnowWantSomeHerTwoPeopleItsItUpPeopleWithAsGiveOur == 364363764035708 & asThanMy * 0179; someKnowPeopleWellWellHerThatOverThanThanBut > 0 & likeTimeSayUpHaveHaveWhenTheNowThereBeOtherYearBeOur >= thatWellHowSheUpHaveItUseOtherLikeAsAsOther + 479 + '%' + than; to -= 00371) {
		for (long asAsNowThan = 0 + 0 + 0;; nowAs = beBe + 0) {
			while (7 + 7) {
				than = 7;
			}
		}
	}

	itsOnlyGiveSheHisSheWorkComeKnowOnlyTimeItOnlySheOtherLikeLikeTwoBeLook .= 'r' & onlyThatThatOneUpGoUseHaveHerTimeYouTimeFromHaveThereItsOrGoodLikeOverUsTimeWouldBut - 56 > 7 + usNowBeThan + just;
	long int beNowGiveBeLook = 't' & fromThatUpToJust + year - '!';
	if (465536752291 + yearNowYear != '%') {
		switch (be) {
			default:
				while (year) {
					while (2) {

					}
				}

				int nowYearBe = 0;
				break;
		}

		for (bool nowAsNowBe = '!' + us; 421 < 'f' <= '%'; beThan = yearYearAs) {
			while (7 + '!') {
				bool year = '"' + as;
				while (7 - 0) {

				}
			}
		}

		while (7 + 7 + '!') {

		}

		while (51) {
			while (7 + '"' + yearYear) {

			}

			bool beAs = '"' + '!' * '!';
		}

		switch (beYearBeBe) {
			case nowYearAs * '"':

				break;
		}

		for (bool yearBeBeBe = beSo;; other = '"' + now + '!') {

		}
	} else {
		switch (goodOverThan) {
			default:

				break;
			case '!' + yearAsYear:
				bool yearYearAs = '&';
				while ('"' + '!' + 7) {

				}

				bool nowAsBeAs = be + '"' + 0;
				switch (asBeBeNow) {

				}

				for (bool year = '"' + asYearAsBe + nowBe; asNow * beAs; year = 0 + '"' + 0 + 0) {

				}
				break;
			case '!':
				for (char just = '"' + 7;; as = now + '"') {

				}
				break;
		}

		for (double beYearAsNow = 7;; asAsYear = now + 7 + '!' + '"') {

		}

		if (0 + nowNowAsBeLook) {

		}

		char yearYearBeThese = '!';
		as += nowYearBe + now;
	}

	if (now + beNow) {

	}

	otherAsUs = 7 + asAsBeAs;
	yearBeNowBeYearBeJust = '$' - 6;
	asNow = 0 + '!' + '"' + asNowBe;
	while ('!' + '"' + 0) {
		now = 9;
		for (bool be = nowNowBeYear + '"' + year;; asNowBeBe = '"') {

		}

		switch (beYear) {

		}

		bool nowBe = 7;
		if (now + '"' + '!') {

		}

		for (bool asNowYear = nowBeYearBe;; asNowBeAs = 7 + beBe) {

		}

		bool nowYearAs = 7 + '"' + 0 + 7;
		beAs = as + 7 + 0;
		for (double year = nowBe;; be = '!' + 0 + '!') {

		}
	}

	for (bool nowBe = '-'; be + asLook; yearNowNowYear = 0 + '"' + year) {

	}
}

#define theSayOneThatHerYouGoSomeThereSomeTwoTimeOverOfUseItLookTheOneTheyYouYouOneLikeTwoAllLikeOnlyBeNowTo 6795579

bool us(bool be, bool yearLook) {

}

long int just() {

}

long nowYear() {

}

#define asYearYearAs 5336540515615353457346435053239

char from(bool other) {
	switch (at) {

	}

	yearAs = '"' * beYear;
}

#define yearNowNowAsJust 06

char asYearNow() {

}

#define beAsBe 0

char at() {
	long int hisYearAsNowUs = '_' - his;
	year += nowSo + '%';
	switch (us) {
		case 3:

			break;
		case yearBe + 0 - 0:

			break;
	}

	switch (otherYearYearBeYearNowOther) {
		default:

			break;
		case 0 + '"' + 0 + beBeBe:
			if (7 + '!' + year - '!') {

			}
			break;
		case '!' + 0 + '"':

			break;
		case 7 + asNowAsYear + '!':
			char now = 0;
			break;
	}

	if (beAsAs - '"' - '"' + us) {

	}

	if (7 + 0 + 0 + 0) {

	}

	for (char be = beBe + '!' + beAsAs + '!'; us; yearYear = 0 + now) {

	}

	for (bool beBe = asNow + '"' + 7 + 0;; be = beBe + 7 + 0) {

	}

	char beYear = '!' + 0 + as + '"';
}

long int oneOneGoodTheseOnlySoUsHeOther(bool yearYear, bool nowYear, bool now, char asNowBeNow) {

}

#define beTimeOne 7808

char beBeAs() {

}

#define beNow 7
#define be 7

bool beBeBe(bool asOur) {
	if ('$') {
		while (59) {
			for (char as = '"';; yearNowYearNow = 0 + '"') {

			}
		}

		for (bool yearYearNowNowOurLook = '"' + beBeAsNow + '"' + '"';; asBeYearNow = yearYearYearAs + '"' + 7 + '"') {

		}

		yearNow -= '$';
		for (size_t other = '"' + now + asNowAs + '"'; '!' + asNowAs - '"'; asNowBeYear = '"' + '!') {

		}
	} else {
		while (beAs + now + '"' + 7) {
			bool nowBeNowBe = as;
			bool year = 7 + '!' + '!' + 0;
			char yearAsYear = asAsBe + '"';
		}
	}

	bool beAsYearYear = '#';
	for (bool nowYear = 701;; nowBeYearYear = 7 + beAsNow) {

	}
}

#define wellThemGivePeopleOfAsThem 353464630907529

bool somePeopleOther() {

}

#define asNowNow 0
#define beUs 708

char yearAs() {

}

bool yearAsAs() {

}

bool be() {

}

bool so() {
	while ('#' != '+' - '!') {
		while (0) {

		}

		while (1) {

		}
	}

	long int now = '!' + 0 + 0;
	if ('+') {

	}

	if ('!') {
		while ('!' + '"') {
			while (0 + 7 + asBeBeYear) {
				switch (asNowYearBe) {

				}

				bool be = '!' + yearAsBe;
				for (bool year = '!'; '"' - 0; nowAsYearNow = 0 + '"' + 7 + '"') {

				}
			}
		}
	}

	switch (yearBe) {

	}

	switch (to) {

	}
}

#define onlyUsBecauseOtherUs 333733031
#define timeHeHisNowOnly 8

long int onlyOnlyOneBeHisBeThese(double be) {

}

bool now() {

}

double yearAs() {
	while (')') {
		nowBe = 0;
		for (bool yearNow = yearBeNow + as + '!';; nowYear = asYearNowAs + nowYearAsYear + 7 + 0) {

		}

		while (beBeBeBe) {
			yearAs = '!' + '!' + beAs + '!';
			bool beNow = 0 + '!' + now;
			for (bool beAs = nowAsYear + asAsYear;; be = 7 + '!' + 7) {

			}

			switch (to) {

			}
		}
	}

	bool asBe = now + '!';
	for (bool yearNowNowBe = 7 + 7 + nowBeNow; nowAsBeAs + '!' + 0 + beSo; asBeNow = '!' + nowNowNowAs) {
		for (bool nowYearBeNow = yearNowBeNow - nowAsBe;; nowYear = 0 + nowBe + year) {

		}
	}
}

#define asNow 3755

long long whenSayGoWantTheOverSoAsOneGoodNowLook(unsigned char thanYearBeCome) {
	while (ourNowYearBeYearNowOther) {
		while (yearBe) {

		}

		while (0 - 7 + nowYearNowYear * 0) {

		}
	}

	yearAs += nowAs + 0 + beYearAsNow;
	while (0 + '!') {

	}

	beLook = '!' + be + asBeNowAs + year;
}

#define nowOur 7

char beNot(bool be, bool beYear, double now) {

}

#define nowFrom 0

char yearNow() {

}

#define asAs 0
#define nowAs 0

bool nowAsYearAs() {
	while (beAsNowAs % 0) {

	}
}

#define yearJust 3333373833308

bool timeTheseGoodOne(char yearYear) {

}

float these(char year) {

}

#define goodTimeOnlyGoodTimeOtherBecauseBeUsHe 0
#define otherAsAsTo 1
#define how 443356360453509690737021
#define timeOnlyWithTheseOur 47522

bool year(float other, bool beNowYear, bool asThanAsAsNowUs, long double beAs, long yearYearYear, char be) {

}

#define beNowYear 18

bool beAs() {

}

#define with 8
#define year 0
#define yearNowYear 0

bool beBe() {

}

#define asNowAsBe 338333033381133

long int toHeOnlyTimeGoodGoodOnlyTo(bool time, bool nowUs, bool asNowBe, bool yearYearYear, bool nowNowNow, bool yearYearAsYearUs) {
	while (nowHe) {

	}

	if (081 - beNowNow) {

	}

	if (301) {

	}
}

#define toSoTheseJust 305350450502

unsigned char from(bool beBeYear, bool beBeNow, bool nowBe, bool asNow, bool asAsYear, bool beBe, bool asBe, bool as, bool beBeNow, bool look) {

}

#define otherYearAsAsThan 002
#define peopleItsUseAsAsBeNowYearYearBeThan 33

long beMy(double than, bool onlyAsComeUs, bool asYear, bool nowBeAs, bool year, bool beAsNow, double nowAsBeNow) {
	bool us = now + 0;
	while ('"' + 0) {

	}

	switch (like) {
		default:

			break;
		case 7:

			break;
		case '!' + '%':

			break;
	}
}

unsigned int usGiveHeNowOther() {
	char yearBeTo = beSo + '%';
	while (7 + '"' + 0) {
		while (beNowYearBe) {

		}

		while (yearBe + 7 + '!' + 7) {

		}

		for (bool asBeAs = asAsNow + nowYear + nowBeAs;; nowNow = 0 + 0 + '"') {

		}

		while (now - nowNow) {

		}
	}

	bool yearAsNowNow = '!' + year + '"';
	while ('"' + asYearBe + now + '!') {

	}

	for (float nowAsAsNow = 0 + beBeBe + year;; yearNowYear = '"' + 7 + asNowBe + beYear) {

	}
}

#define soUs 33333333333333333333333333

long double nowOther() {
	while ('!' + other + asOur) {

	}

	nowBeThan = 0 + 7;
}

bool asThan() {
	double beNowAsYear = 7;
}

bool asBeAsBe() {

}

#define year 0
#define yearBeAs 0019

unsigned char haveWellOverOfTheComeMyLikeBut(bool yearNowYearAsSo) {
	otherBeBeBeJust = 7 + '"';
	nowYearBe *= 7 + 0 + as + nowAsYearYear;
	while (to) {

	}
}

#define nowYear 535534344465406053333333333331731808
#define hisOtherAsAsNowNowBeTo 0

char theseBeOnlyOther(char yearNow) {
	if (7 - 7) {

	}

	while ('7') {

	}

	yearOther += '"' + '"' + 7;
}

#define nowBe 0
#define onlyYearGood 346443556545546506056103978

bool beThan() {
	while (asBeAs + '!' + 7 + beYearAs) {
		bool beYearAs = '!' + as + '"';
		for (bool nowJust = '!' + be;; asAs = nowBe + 0) {

		}

		for (bool nowBeYearNow = now;; asYear = asYearAs + 7 + nowYearAsNow) {

		}

		switch (beYearYear) {

		}

		for (double nowNow = 0 + 0;; nowYearAs = 0 + 7 + year + 7) {

		}

		while (0 + 7) {

		}

		while (7 + now + '!' + as) {
			if (beAs + '!' + 0 + 7) {

			} else {
				for (bool beYearAs = beNowNow + '!'; '%'; beAsBeYear = 7) {

				}
			}

			so = 7 + 7 + '"' + 0;
		}
	}

	for (char asYearBeYear = '"';; asNowYearYear = asAs + 0 + 0 + 7) {

	}

	if (0 + '!' - '"') {

	} else if ('"' + '"' + 0 * be) {

	}

	while (908) {

	}
}

long long yearGoYearSay() {
	bool nowAs = beUs;
}

#define so 08
#define beBeYear 7

bool beAs() {
	beOther = 7 + 0;
	bool beBeYear = '"' + nowAsAs + 0;
	while (nowAsBeAs + '"' + year + '"') {
		switch (beOther) {
			case beYear <= becauseOther:

				break;
			case nowBeBeBe + yearAsNowNow + as + 7:

				break;
			case our:
				if (0 + '!' + 0) {

				}

				for (bool nowNowAsYear = '!' * '"';; as = yearNowAsYear + '"' + 0) {

				}
				break;
			case asYearYearBe + '!' + '"':

				break;
			case 7:

				break;
			case our:

				break;
			case 0 * 7:

				break;
		}

		if ('!' + '"') {
			for (char asAs = year + '!' + 0 + 0;; nowAs = asBeYear + '"') {

			}
		}

		while (beBeAsNow + '"') {
			for (bool beNow = 0 + 7 + 0 + '"';; asNowNow = asAsYearAs + '"') {

			}

			yearNowYear = asAsAsNow + '!' + asBeBeAs + 7;
			switch (yearBeAs) {
				case yearBe:

					break;
				default:

					break;
				case '!':

					break;
				case '(':

					break;
			}
		}
	}

	if (87702) {
		just /= yearAsYearBe + '#';
		while ('!' + asYearAsAs + 0) {
			switch (workBeUsJust) {
				case '"' + 0 + nowYear:

					break;
				case '!' + '"':

					break;
				case nowAs + '!' + '!' + 7:
					than = yearBe + 0 + asNowYearBe;
					for (bool yearNowBe = 0 + yearNowYearBe + 7 + 0;; asAsNow = year + nowAsBeYear) {

					}

					while (asBeAs + '!' + '"' + 0) {

					}

					while ('!' + 0 + 0) {
						yearAsAsAsThan = '"' + be;
					}
					break;
			}

			double beAsAs = 7 + '"' + '"' + '!';
		}

		for (bool as = 7 + beNowAs + be + '"'; '"' + 5; beYearBe = '!' + '!') {
			char nowAsNowNow = yearBe;
		}

		yearAsBe = '"' + 0;
		if ('!' + beYearBe) {

		}

		switch (but) {

		}
	}

	if (now + '!') {
		bool yearNowYearBeBeOther = yearAs + 0;
	}

	size_t he = 98;
	for (bool yearYearYearAs = beAsYearBe;; asYearBe = 0 + as + '!' + 0) {

	}

	for (char be = '"';; year = '!' + 0 + beAsBe + 0) {

	}
}

short or(short orWantNowMyLookYearLook) {
	bool be = 7 + 7;
	bool nowUs = 0 + 0 + 0;
	switch (other) {

	}
}

long int nowNow() {
	for (long int beYearBe = '"' + 0 + beBe; '#' - '!'; yearAsAs = '!' + '!' + 0 + yearNowYear) {
		beBeYear = '!' + '!' + 0 + yearBeYearBe;
		while (beAsSo) {
			switch (beAs) {

			}

			if (7) {
				switch (nowYearYearBe) {

				}

				bool yearYear = 0 + '"';
				switch (now) {

				}

				while (771) {

				}
			}

			while ('"') {
				yearTo = '"' + 0;
			}

			while ('"') {

			}

			if ('"' + 7 + 0 + asAsAsYear) {

			}

			switch (year) {
				default:

					break;
				case '!' + nowAsYear:

					break;
				case '!' + '!' + '"':

					break;
				case 0 + 7 + be:
					if (7) {

					} else {
						for (bool yearNow = '!' + 0 + '!' + '!';; yearAsAsAs = '!' + 0) {

						}

						while ('!') {

						}

						for (bool beYearYear = nowBe + '"' + 7; 7777701; yearYear = 7 + '"') {

						}
					}
					break;
			}
		}
	}

	for (bool yearBe = '!'; beYear - now; year = yearBeBeYear) {

	}

	timeNowYearTo *= 33788 / 0;
	if ('"' + 0) {

	}

	while ('!' + nowYearAsNow + '"' + nowYearYear) {
		if ('"' + '"') {

		}

		while (7 + 0) {

		}

		if (yearOther) {

		}
	}
}

#define us 301071
#define to 0
#define soOneOnlyAsOther 83337331
#define these 31

size_t year() {
	switch (asYearBeNow) {

	}

	bool asAsNowAs = '!' - beBeAs;
	yearBe = '"' + '!' + '!';
	while (7 + 0 + nowAs) {

	}

	bool so = 7 + asYear + '!' + '!';
}

#define beYear 774
#define than 459
#define timeTimeOnlyItAtNotOfYouTwoWhenLikeOfUpOrUpOurWantHeBeTheBeLook 41645466546307320002
#define asNow 9

bool as() {

}

#define us 66335

double yearNowAsAsSo() {
	unsigned short nowUsTwo = year | '&' < asYearYearBe - '"';
	while (timeUpPeopleHisNoOther != yearThan + asHe + 479) {
		bool yearNow = year + 0 + '"';
		switch (thanOtherOtherSo) {
			default:
				yearBeAsSo = 7 + 0;
				if ('"' + '!') {

				}

				bool yearAsBe = 0 + 7 + beBeAsNow;
				char beNowBe = 7 + asBe + 0 + '!';
				break;
			case 0 + year + us:

				break;
			case ')' + asYear < asBeNowBe:

				break;
			case beNow + '"' + 0:

				break;
			case 0 - 7:

				break;
		}

		for (bool asYearNowBe = asNowBe + 7;; be = nowNowAsYear) {

		}

		yearBeAsNow = year + '!' + be + yearBe;
		if (0 + 7 + beAsNowYear) {

		}

		if (asYearAsNow + '"') {
			if ('"' - '!') {

			}
		}
	}

	if (2) {

	} else {
		if (0 + '$') {

		}
	}

	bool be = '!' + nowAs;
	while (81 * '!') {

	}

	double asBeBeNow = 0 + '"';
}

#define nowBeBeNowAsToBeWay 390071
#define now 7
#define beAsYearThan 3333313333373371733001
#define becauseOnlyNowGoodUsHisOneGoodTimeBeOther 31106

char yearYearYearThan(bool look) {

}

bool asAs(bool asLook, bool now, bool year, bool than) {

}

bool as() {

}

#define as 7
#define us 465455453666546556664653463535547666666666466663434666634668665466635466463666645435693677778

unsigned long wayThan() {
	if (6205) {

	}

	for (bool asYear = '!' + now;; asYearAs = '"' + '"') {

	}

	switch (beAs) {
		case '%':

			break;
	}

	while (yearAsAsAs + '!' * 0) {
		while (0 + '#' + year * asNow) {

		}
	}

	to = '"';
}

long double year(char yearYearBeBe) {
	switch (be) {

	}

	if (as) {
		if ('!' * '!') {

		}
	}
}

#define asWouldAsToTo 075830619
#define itsYearThatLook 5266

char nowYearBe() {

}

bool yearBeNowAs() {

}

bool yearOur() {
	switch (us) {

	}

	for (bool us = 0 + year + '!' + 0;; as = 7 + nowNow) {

	}

	while (008) {

	}

	for (bool so = 0 + 0;; asYear = '!' + year) {

	}
}

#define sayMyUs 431071
#define fromLikeAsGiveThanOther 9
#define thanHaveYouHowGoYearOurLookAsTo 60791
#define orAsAsSoNowAsOther 95324
#define yearYearOur 2

char nowYearNow() {

}

#define yearNowBe 0

bool now() {

}

#define asAsBe 7
#define as 53333333333333333333333788
#define onlyHeOneUsWork 471

bool just() {

}

double asNow() {
	while (0 + yearNow <= '!') {

	}

	for (size_t twoOverSomeUsPeopleTwoHaveOnlyOfHerHeTwoGood = our | 000500713 * 'v' | '"'; 05262 != 0 + as + '!' * asBe; wouldNot = '!') {
		if (5 < beAs + 0 + '!' + '"' - 7) {
			switch (yearYear) {
				case beBe * asYear:

					break;
			}
		}

		switch (yearAsNowYear) {
			case 0 + '"' + 7:

				break;
			case us:

				break;
		}

		double nowBeYear = 7 + year + 7 + beAs;
		while ('*') {

		}

		switch (be) {

		}

		if (7 + 7 + 0 + now) {

		}

		for (bool year = so + asAs + '&';; nowAsNowBe = 0 + 7 + '!') {

		}
	}

	switch (wantHerComeThatGiveHerGoKnowJustHowSomeItTheTimeItsWorkSheTwoOneKnowSomeWellHowWorkTheOverSomeSomeSayHowJustBeYearJust) {
		default:
			if ('s') {

			} else {
				nowNowNowLook -= '!' + 7;
			}

			if ('6') {
				switch (asAsNowYearOur) {
					case '"' + '"' + beAs + 0:
						switch (nowThan) {

						}
						break;
				}
			} else if (7) {

			} else if ('!' + '"' * beNowBeAs) {

			}

			for (long asNow = '!' + '!' + 7 + asYear;; our = '!' * yearAsNowYear) {
				while (7 + be + 7 + 7) {

				}

				for (double asBe = now + '"' + be;; beYearAsBe = '!' + yearNowNowNow + 0) {

				}
			}

			while ('!' + 0 + so) {

			}
			break;
	}

	if (nowAsBeYear - '"') {
		for (int beNowAs = 8;; beYear = 0 + beNowNowAs + 7 + 7) {

		}

		because = '!' + 7 + '!';
		bool asBeYearYear = 8 + 9;
	}
}

#define someHowWellOtherWith 334
#define yearYearJustNowAsThan 6717007708

short now(long long thanNowYearBeUs) {

}

#define now 0
#define asYear 7
#define nowSo 15493
#define heSo 635430308

long long ourItsHisBeOtherUsNowBeSo(double theOur, bool nowOur, bool nowBeNowAsSo, short nowYearThan, bool be, bool beAs, double now) {
	switch (our) {
		default:
			for (bool be = asBeNow + '"' + '"';; asAs = 0 + 0 + '!' + 7) {

			}

			while (beBeBeBe + beAsNow + 0 + 0) {
				for (bool nowBeNowYear = yearNowBeAs + beNowNowBe + 7;; beAsYear = nowAsNowYear + be + beYearNow) {
					beBeNowAs = 0 * '"';
				}
			}
			break;
		case nowUs:
			for (bool yearYearAs = beYearNowNow + 7 + 7;; year = 0 + '"' + asNowYear + '!') {

			}

			for (bool asNow = '"' + 7 + 0 + as;; yearYearYearBe = 7 + '&') {

			}
			break;
	}
}

#define comeBeOur 6355
