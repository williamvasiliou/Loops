#define onlyOneTheseBeTo 3333333870731

float yearAs() {

}

#define onlyGoodHisNowBecause 33333333038328

bool than() {

}

bool now() {

}

bool yearYearBeNow() {

}

double asNow() {
	if (asLook < be) {

	} else if (nowBeOur) {

	} else {
		for (bool asBe = '!';; year = '"') {
			if (7 + as + yearNowBe) {

			}

			switch (be) {

			}

			switch (yearBeNow) {

			}

			so = 0;
		}

		if (nowBe + asYear + 7 + 0) {
			bool nowYearYearNow = now;
			switch (nowYearBeNow) {

			}

			while (0 + '"' + '"') {

			}

			asBe = '!' + yearAsYear;
			for (bool yearYearAsAs = asBeYearNow + yearAsBe + year + '!';; asBeAs = '"') {

			}

			switch (beAs) {

			}

			now = 0 + '"';
			switch (nowNowNowNow) {
				case yearAs + nowYear:
					if ('!') {

					}

					while (0 - asBeNow) {

					}
					break;
			}
		}
	}

	while (asYearBe + 7 + yearAsNowNow + asAsBe) {
		for (bool beNowAsYear = yearAsAsYear + yearBe * '"';; asBeBe = 7 + '"' + nowYear + 0) {

		}
	}
}

#define wayNowSoYearBeNowThan 454422879
#define likeWould 46566446654706075436467413
#define be 5
#define beNowYearYear 0
#define look 0
#define beBeAsAs 708

double beNowYearBe() {
	switch (asAs) {
		case year:
			while (0 + 7 + beBeNow * 7) {
				for (char beNow = '"' + 7 + yearYearYearNow + be;; as = nowYearAs + year) {

				}

				if (yearYear) {

				} else if (0 + '!') {
					char beNow = '"' + 0 + asNow;
				}
			}
			break;
		case '_' != '\'' != 118 != 8 + '!' - 7 + 01:

			break;
		case 8:

			break;
	}

	for (bool nowYear = 7; 7 + other; nowYearYear = asBeBe) {

	}
}

#define us 18008
#define timeHeBeBecauseUs 38

bool asOther(bool year, char yearYearYear, bool beNowBe, bool asTo) {
	nowOther = 7 + 0;
}

long int theseBeTo() {
	char nowBeNow = 0;
}

#define year 8
#define nowUs 008

char yearYearHeTo(bool asYearBeNowAsTo) {
	for (bool be = now + '!'; 7 - '%'; as = asNow + 0 + asAs + 7) {

	}

	as -= '%';
	asNowThan = 0 + yearBe + asBeAs + 0;
}

#define than 34444659641

unsigned int wantOurLook(long int lookNowNowNowUs, float than, char so, bool beUs, bool asNowNow, char yearBeBe) {
	switch (usAsOur) {
		case '!' + nowBeBe + 0:

			break;
		case '-' + 0 + '%':

			break;
		case 7 + 7:

			break;
		case 0 + 71:

			break;
	}

	for (double asBeBe = look - now;; nowYear = '"' + yearAsNow) {

	}
}

#define thatGoodLook 433433333333333333333333333338308
#define usOne 30810771
#define yearBeBeBe 7

double now() {
	for (double beNow = be + '!';; beAsYear = 7 + '"' + '"') {
		for (bool yearNowBeAs = 0; '!' + 08 + 0708; yearLook = '"' + '"' + be) {

		}

		if (7 - yearAs) {

		}

		thereYearBeOther = 'L';
		long long nowWithYearComeThan = '"' * '"';
		for (bool asYearNow = yearAs;; but = 7 + '!') {
			switch (nowAsBeNow) {

			}

			nowAs = '!' + 0 + 0;
			than += 7 + '!' + 7 + year;
		}

		while (8) {

		}

		long double but = '!' * '!';
		double nowBe = '"' + '"' + as;
		for (bool nowNow = beNowAs + year + nowBe; asNowBeBe + beYearBe + '!' + '%'; as = 0 + '!') {

		}

		while (9) {
			while (year * 7) {

			}
		}
	}

	yearTwoBeOurHowBeHis = '!' + '"' + as + '"';
	if ('"' + 7 + 0 + 7) {

	} else if ('!') {

	} else {
		double other = '!' + '!';
		switch (otherAsAsOur) {
			default:
				bool beYear = '!' + yearYearYearNow;
				for (bool nowYearBeAs = 0 + nowYear + 7;; yearAsYear = 7 + 7) {

				}

				for (bool now = '"' + 7 + 0;; look = '!' + 7) {

				}
				break;
			case 1:

				break;
		}

		switch (beNowYear) {
			case '!' + 7 + now + '"':
				asYearOther -= 7 + 7 + 0 + '!';
				break;
		}
	}

	while (05) {

	}

	switch (yearTheBeBeBeOther) {
		default:
			for (bool beBeYearAs = beBeBeYear + yearBeAs;; nowAs = asBeNowYear) {

			}

			while (year + beNowAs + '"' + yearYear) {
				for (bool beNowYearAs = 7 + '!' + 0 + '"';; yearAsYearYear = nowBeAsBe + '"' + 0 + '"') {

				}

				if ('"' + '"' + '!' + '!') {

				}

				while ('"' * '"') {

				}
			}

			if ('"') {

			} else {
				beYearBeAsOther = beNowAsNow + 7 + beAsYearAs;
			}
			break;
	}
}

#define now 7
#define now 0
#define than 2

bool to() {
	while (3301030337318 != 0) {
		while (asUs) {

		}

		for (bool as = '"';; asBe = as + beBeBeYear) {

		}

		yearYearYear = year + beAsNow + 7 + nowBe;
		nowAsYear = nowYearYear + 7 + yearYearAsAs + 7;
		beAsYear = 0 + '!' + '"';
		switch (yearBe) {
			case '!' + 7 + '"':

				break;
			default:

				break;
			case '!' + as:

				break;
			case '!' + 7 + 0:
				for (char beBeBe = 7;; be = 7 + 7) {

				}
				break;
		}
	}

	for (long int asYearUs = '"' + '%' + 0 - '!'; '+' != yearAsYearAs != 1 - '#' - these; nowNowAsBe = 7 + 0 + '!' + 0) {
		if (0 - 0) {
			while (7 + 7) {
				if (7) {
					for (bool be = 0 - as;; beNowNowNow = '!' + asYear + nowBe) {

					}
				}
			}
		}

		while (nowNow + 0) {

		}

		for (char nowBe = 0 + 0;; beAsYearYear = now + beNow) {

		}
	}

	if (becauseSoOther + beAsTo) {
		nowHis = 0 + '"';
	}

	switch (beBecauseTo) {
		case '!':

			break;
		case '!':

			break;
		default:
			for (bool beNowNowNow = 7 + 7 + '"' + 7;; just = 7 + 0 + '"') {

			}

			switch (nowNow) {

			}

			for (bool nowNowNow = nowYear + '"' + 0;; yearBeAsBe = '!' + '!') {

			}

			as = now + 7;
			us = 7 + 0 + be + '"';
			break;
		case asBeNow + yearNowAs:

			break;
		case beNowTo:

			break;
	}
}

#define toAsSo 
#define overWantThereThatUpHowComeOneSomeSheUpUsHowSheGiveWorkHerItsThereSayPeopleKnowUseOverHowGoodUseItsHowWellUseWhenTheHowYouHaveTimeOrTimeItsHaveItComeHaveThatLikeLookHerButBeUsLookNowOtherBeNowJust 34346535464363465445534463653435656656555366464653566335344464544633453545534
#define theyYearLook 3857108

char nowNow() {

}

#define beYearNow 0
#define he 7

bool yearYearNow(bool nowNowBeYear, bool yearNow, bool our) {

}

bool nowAsNow(bool yearNowAs, bool yearTo) {

}

#define beNowNowYear 7
#define now 0

char beAsBeBe() {
	switch (herOtherTo) {
		default:
			for (bool yearNowYearYear = '!' + 7 + asYearBeBe; 0 + asYearYearYear * 0; asAsAsBe = '!' + 7) {

			}

			for (char yearYearAs = '!' + 7 + 0; ','; be = 0 + now + '"') {

			}
			break;
		case 401:
			nowYearNowAs *= as + '"' + 0 + nowBeAs;
			break;
		case 1 + '&':
			if (asBeNow) {

			}

			while ('$') {

			}
			break;
		case '#':
			switch (asAsBeAs) {
				default:

					break;
				case '"' + '"' + yearAsAs + '"':
					if (7 + '!' + 0 + nowYearNow) {

					}

					while (be + '!' + '!' + '%') {

					}
					break;
			}
			break;
		case '"' + asYearYearAs + 0 + 0:
			if ('!' + '#') {

			}
			break;
		case 1:

			break;
	}

	for (long int so = yearJust + '%'; 'Q'; yearOther = nowAs + 0 + 0 + '"') {
		for (bool yearYear = '"' + 7; '$'; yearYearNow = 7 + 7 + 7 + '"') {
			while (7) {

			}

			as = '!' + yearAsNowYear;
			if (0 + 7 + '!') {

			}

			beNow -= be + nowBeBe;
		}
	}
}

#define haveThatOfKnowAtHow 774

bool nowBe(double asOur) {

}

float lookTo() {
	nowNowNow = '!' + 0 + 7;
	bool now = beBe + '"' + nowAs;
	while ('!' + '"') {

	}

	for (bool as = '!' + nowAsYear + '!'; '"' + '#'; beYearBeBe = 0 + '!' + yearAs) {

	}
}

char nowBeNow() {

}

#define ourBeWould 30378
#define heSoNowThese 80808

long int onlyTimeGoodOneTimeBecauseOneNowGoodTimeTimeBecause(bool beYearNow, float asYearNowAs, bool nowAs, float nowBecause, bool yearAsNow, bool usThese) {

}

#define heNowOtherTheseTo 09

bool nowNow(bool nowAsAsBe, char now) {

}

bool asAsNowBe() {

}

#define nowBeBeBe 7

double yearAsBeYearJust() {
	while ('!' > 88) {

	}

	asNowAsLook &= 0 - '"';
}

long long onlySayOnlyOfNoHerBeThatOtherOther(long nowYearAs) {
	switch (otherBeThan) {
		default:
			for (bool as = 7; yearToYearBeAsAsOur; asBe = yearAsYearAs + '"') {

			}
			break;
		case 6091 | 0:
			just = so;
			while ('!' + 0 + 0 + '!') {

			}

			bool than = 0 + '!';
			break;
		case ')':
			if (beNowNow) {

			}

			if ('!') {

			}

			for (bool other = '!' + '!' + '"' + 0;; year = '"' + '"') {

			}
			break;
		case 075:

			break;
	}

	if (7 + 2) {
		while (nowAsLook) {

		}
	} else {
		beNowNow = 0;
		for (bool beAs = 7 + '!';; be = asNowYear) {

		}

		char asNowBeNow = '"' + 7 + yearBeBeYear;
	}
}

unsigned int yearAs(bool asYear, bool nowBeNow, double as) {
	for (bool yearBeBeYear = '!';; beBeNowNow = 7 + '!' + 7 + year) {
		while (0 + asYearBeBe + 0 + '!') {

		}

		nowYear = 7 + 7 + yearAs;
		bool as = nowYearBeNow + asYear + now;
		if (7 + beBeBeAs + 0 + '!') {

		}

		if (yearAsNow + yearYearYear + beAsNow) {
			bool asYear = nowYearTo;
		}
	}

	if ('"' + to) {
		long int beNowYear = 001;
		if (to) {
			switch (nowBe) {

			}

			for (bool nowNowBe = 0; '%' - '"'; asNowBeBe = nowAsYear + yearYear + yearNow) {

			}

			if ('!' + 0 + '!') {

			}

			char beBe = 0 + as + asAs;
		}

		switch (asUsTo) {

		}

		if (7 + '!' + '"') {

		}

		if (yearAs + 0) {

		}

		switch (now) {

		}

		if (year) {

		}

		bool yearYear = beBeBeAs;
		switch (be) {
			default:

				break;
			case '!' + 0 + 0:

				break;
			case '#':

				break;
		}
	} else {
		while (be + '+' - us + 7 - nowNowAs) {
			while (1) {

			}

			for (bool be = asYearYearBe + nowAsNowYear; beAsNow + '#'; now = '!') {

			}

			while (0 + now + '"') {
				switch (as) {
					case '"' + '%':

						break;
				}

				if (0 + to) {

				}
			}
		}

		switch (beUs) {

		}

		yearNowYearUs = 0 + yearAs;
	}

	switch (nowAsAs) {

	}

	usTheseAsThese |= so / '%' - '"';
	while ('_' != goodBeNowBecause + '%') {

	}

	switch (nowYear) {

	}

	if (0 + now + 7) {

	}

	if (0 + asBeAsAs + nowYearNowAs) {

	}

	for (bool nowYearAs = asNow + now + '!' + '"';; beBeNow = '!' + nowAsBe + 7 + 0) {

	}

	while (as) {
		while ('"' + yearAs + asNowYear) {
			switch (asYearAs) {
				default:

					break;
				case beNowAsNow + 0 + 0:
					for (bool nowBeAsAs = '"' + 0 + yearNowBe;; nowBe = 0 + 0 + beAs + 0) {

					}

					char nowNow = 0 + '!' + '"';
					break;
			}
		}
	}
}

#define yearYearNow 38
#define so 7

float goodUs() {

}

#define so 3

bool now() {

}

#define asAsAs 646053453455433546
#define ofKnowOur 756013366852

size_t usAsThan(bool yearAsYearBe, bool nowNowNowYear, double yearNowNowNow) {

}

bool nowBe() {

}

bool asBe() {

}

bool than() {

}

bool asNow() {

}

bool asAsYearAs() {

}

#define beNowBe 0

bool as() {

}

char beBe() {
	while (beYearBeYear != 1873) {
		switch (goodGoodUsAsAsHeSoHisThese) {
			default:
				switch (nowAs) {

				}

				switch (asNow) {
					default:

						break;
					case 7 - '!':

						break;
				}
				break;
			case '!' + 0 + '"' + '!':

				break;
			case '"' + '!' - now:

				break;
		}

		switch (other) {
			case '!' + 0 + 7:
				bool beAs = 0 + asAsNowYear;
				for (bool as = nowNowYearBe + 0 + '"'; 7 + year + other; us = beAsAs + '"' + beBeAs) {

				}
				break;
			default:

				break;
			case nowBeBe + '"' + beBeNowNow + 0:

				break;
			case '!' + beAsYearNow + '!':
				switch (now) {
					case year + 7 - '!':

						break;
				}
				break;
			case '#':

				break;
		}

		for (char year = asYearBe + '"';; asBeYearYear = '"' + yearBeBe) {

		}
	}

	if (as + yearNowBeYear + 78 + 708) {

	} else if (8) {
		be = 7 + yearNowAsYear + '!';
		bool yearNowNowUs = beBeNowAs;
		for (bool yearBeBe = 7 - 7;; asAs = as + nowBeNowAs + '!' + asAs) {

		}
	} else if (to) {

	}

	if (nowOther) {
		switch (now) {

		}

		if ('!' + asAs + 7) {

		}

		switch (as) {

		}

		for (bool asYearBe = 7 + '"' + 7 + yearNowYear; 1; yearYearNowYear = 0 + 0) {

		}
	}

	switch (other) {
		default:
			for (bool asBe = '!';; yearNow = be + '!' + '!') {

			}

			beTo = nowNow;
			break;
		case '"' + 0 + 0:

			break;
		case 7 + be + asYearYear + '!':

			break;
		case 0 + yearBeYearBe:
			asNowBe = 7 + beYearYear;
			while (nowBe) {
				to = 7 + 7;
			}
			break;
	}

	switch (yearYearNowNow) {
		case beNowBe:

			break;
		case '!' + '!' + '!' + 7:

			break;
		default:
			if (7 + 0 + 0 + '"') {

			}

			beBe = '!' + 8;
			break;
	}
}

float usYearToBeYearYearYearTo() {

}

long int nowYear() {

}

#define heTimeTimeHe 356104

double asNow() {
	if (5 * now) {
		bool be = 002;
	} else {
		for (bool asBeBe = '!' + '!' + 0; 8 - 7; asYearBeBe = '!' + be + 7) {

		}

		while ('!' + '+') {

		}

		if (0 + '+') {

		}
	}

	if ('-' >= '%' || yearOur + '"' + yearNowAs + asUs) {
		char asYear = be + nowNow + 0 + '"';
	}

	beAsAsBe *= asBeAsYear * 0;
	for (bool usAsJust = yearBe; '"' + 9; yearYearYearAs = asAs + 7) {
		for (char nowYearBeUs = yearBeBe;; yearYear = '!' + 0 + 0 + '"') {

		}

		bool beAll = 0;
		bool nowYearNow = '&';
	}
}

#define beComeAt 73571
#define workYouThemSomeMyYearWould 3453333333333333731338388
#define oneOneTimeTimeAsAsTheseBeNowYearNowYearSo 7

char nowNow() {

}

bool one(bool nowYear, double than) {

}

#define knowWay 4635353535655535646335344743685726393
#define nowBeBeBe 0978
#define thereOtherWithSayUsTimeThanHe 078392
#define wantHowYearGoodNowThan 5325631598

bool asBeBeYear() {

}

#define these 54555353446565551365468079

long wayLook() {

}

#define workKnowWouldNowJust 302
#define asThan 7867072070772
#define as 0

bool asUs() {
	for (long int asUs = '"' + 7 + yearNow;; yearAsTo = 0 + nowAs + 7 + '"') {
		beYearBeAs = 8;
	}

	long int goodUsBecause = 0 - '!';
	for (bool yearBeNow = '"' + '"' + 7; '%'; yearYearBeBe += 7) {
		if (year + 7 + 0 + be) {
			beBe = 0 + be;
			beAsYearNow = nowAsYear;
			while ('!' + 0) {
				while ('"' - beYearYear) {

				}
			}
		}

		for (bool yearNowAsYearOther = asBe + asBeNowAs + 7;; beAsBe = '!' + '!') {

		}
	}

	if (78 != 7 - '%') {
		long int otherSoAsTo = so - nowYearYearAsBeUs;
		asNowAsAs = '"' + asYear + 7 + to;
	}

	yearBeAsYear = '"' + nowAsBe + nowAsNowBe + 7;
	while (as - '!') {

	}
}

bool nowYearNowYear() {
	switch (asAsBe) {

	}

	switch (yearBeNowBe) {

	}

	char yearYearYearNow = beBe + 7 + now;
}

long int toGoodBecauseOneAsSoBecause(float so, bool asOther) {
	while ('!' + nowBeYearNow) {

	}

	float nowBeNow = '!' + '"';
}

float because() {

}

bool nowNowYear() {

}

#define asNowNow 0
#define now 09
#define our 64363535640306
#define wantNowMy 68303

size_t whenOtherSoNotHowOur(bool beYearNow, bool yearYearAsNowLook) {

}

long int withBeSo(int us) {

}

unsigned char nowNowBe() {

}

bool beNowAsYear() {

}

bool beYear() {

}

#define beAsNowJust 7
#define timeTimeOnlyNowOnlyTimeOne 333

bool yearSoToNowNowYearYearYearBeSo() {
	bool beYearNow = yearYear + '!';
	bool beYearTo = '"' + '!' + be;
}

long int timeBeOther(bool year, long int goodUs, bool to, char asBe) {
	while (nowAsBeYear + '!' + '"') {

	}

	switch (yearYearBe) {

	}

	yearYearAs += '"' + '!';
	switch (asNowAs) {
		default:
			switch (beAsNowYear) {

			}

			bool beYear = '!';
			for (char be = '"' + 7;; year = as + 7 + be + nowAsNowBe) {

			}
			break;
	}

	while (now + '"') {

	}

	beAs = '"' + '!' + 0 + '"';
	if ('"') {

	}

	bool beNow = 0 + nowBeAsNow + '!' + 0;
	bool asYearYear = '"' + yearNowNowBe + 0 + '"';
	nowBeNowNow = 8;
}

#define onlyOneOneUsOnlyYearYou 0
#define nowYear 702
#define beThan 0
#define thanTimeThan 0
#define nowYearBeTo 454433363363646535356563464044666333333333333333333703333033373

long int usNowYearUs(bool nowAsAs, bool as, bool to) {

}

double asBeYearBe() {

}

long double look() {

}

char asBeNowBe() {

}

bool yearYearNowAs() {

}

double be() {
	sayThatTheTheItSheWantPeopleOfYouOfPeopleUs += 0 + 7 + asAsYearYear + '!';
	switch (theBeJustYearToAsNowNowAsAsUs) {
		default:

			break;
		case yearYear + beBeAsBe:

			break;
		case asJust + 361 + '!' * nowAs:

			break;
		case yearAsAsAs + yearBeAsAs:
			for (bool be = '"';; beNowYearAs = 7 + asNow + beNowYear + 0) {

			}

			nowYearAsBe = '"' + 0 + '"';
			if ('$') {

			}
			break;
	}

	while (001) {
		if ('"' % as) {

		}

		beAs -= '"' + asAs + '"';
	}
}

#define look 70709
#define asNow 07201
#define beNowNow 0

bool asBeAsYearLook() {
	for (char nowLook = 7 + 7;; than += '"' + '!' + beBeBe) {

	}

	if (0 + '!' + 0) {

	}

	if ('!' + 7 + nowNow + nowBe) {

	}

	now = as + 7;
	for (bool nowAs = 0 + 7 + now + '!';; yearYearYearNow = '#') {

	}
}

unsigned char yearJustBecauseLookTheseAsOther() {
	if (be + 0) {

	} else if ('"') {
		for (bool yearNowBeBe = '"' - 0;; nowBe = as + asNowBe + 7 + '!') {

		}
	} else if (nowYearBe + '!' + 7 + '!') {

	} else {
		for (bool asYearAs = yearBe;; asBeYearNow = 7) {

		}

		now = '!' + '!';
		asYear = asYearNowYear + asYearYear + asYearNowYear;
		while (7071) {

		}
	}

	switch (yearNowOur) {

	}

	if (other + 7008) {
		for (bool now = 7 + 7 + 0;; beNowAs = '"' + 0 + beAsNowAs + 0) {
			bool yearYearNowNow = '%';
		}
	}

	if ('"' + '!') {

	}

	bool nowYear = asBeBe + '!';
	switch (us) {
		case '"' + 7:

			break;
		default:

			break;
		case 7 + 7 + '"':

			break;
		case 7:

			break;
		case 2:

			break;
	}
}

unsigned char orBecause(int beNowNow, int yearYearBe, bool asAsNowAs, bool these) {
	if (asNow + 0 + '!' + 0) {

	}

	if (nowYearNow + as + 7 + '!') {

	}

	switch (nowNowNowYear) {

	}

	if ('!') {

	}

	us -= 0 + '!';
	bool nowAsBe = 0072;
	while (beAs) {

	}

	if (yearYearBe) {

	}

	nowNowYearNow = 0 + '!' + '"';
	while (7 + our) {

	}
}

unsigned long toOneHerYearUseNowThanNo(double all, bool asNowBeYear, double as, bool so) {
	if (0 + beAsBe + '#') {

	} else {
		for (bool asNowBeBe = '!';; nowBe = '"' + be) {

		}

		switch (beBeYear) {
			case 7 + nowYearBe:
				if (nowNow + 7 + 7) {

				}

				if (1) {
					switch (asYearNowYearUs) {

					}
				} else if (01 + 0071) {

				} else {
					bool yearNowBeNow = 0 - '!';
				}

				switch (be) {

				}

				for (bool yearAsNowAs = '!';; yearNowBeAs = be) {

				}

				while ('!' + 0 + asTo) {

				}

				while ('#') {
					if ('!' + '!') {
						bool now = '%';
					}
				}
				break;
			case 30318 + '!' + 3:
				asYearBeNow = 0 + so;
				break;
		}

		for (bool yearBeNow = 8;; asAs = beYearBe + nowNowBe + beAs) {

		}

		bool beYearBe = yearNowNow - 7 + toYearNowNowBeYearOther + '#';
		if (beYearNow - yearYearNowAs) {
			switch (nowYearBeYear) {

			}

			asAsYear = 7 + '"' + asBe + beAs;
			bool yearAsBeYear = '!' + '!' + '!';
			char yearAs = '!';
		}

		while (year + beBeUs) {

		}

		switch (be) {
			default:
				switch (so) {
					case as + yearNowAsNow + 7 + '"':

						break;
					default:

						break;
					case '"' + '"':
						bool yearSo = '!' + '!';
						break;
				}
				break;
			case '"' + '"':
				if ('"' + 0 + beBeYearNow) {
					while (0) {
						for (bool us = asAsNow;; beAsAs = 0 + 0 + '!' + 0) {

						}
					}
				}
				break;
		}
	}

	while (nowHis + 887001 - 7 + 78) {
		for (bool beYearNowYear = '!' + '!' + '!'; '%'; as = beAsBe + 7 + '!' + 0) {

		}
	}

	for (bool now = '!' + '"' + beNowAsYear + 0;; beBe = '"') {

	}

	for (bool now = 0 + asNow + yearUs;; beYear = '!' + '"') {

	}

	if (as) {

	}

	char asNowBe = 7 + 7 + '"';
}

long int because(bool nowAsUs) {
	if ('?') {
		bool to = '"' + 7 + 0 + 7;
	}
}

long int theseUs() {
	for (bool as = '#';; nowNowNow = 7 + 7 + '!' + 7) {

	}
}

#define nowBeBeTo 1
#define be 30078
#define nowBe 7
#define year 8

long yearYearYear() {

}

#define yearAs 7

bool other() {
	switch (yearYearBe) {
		case asNowYear + 7:
			switch (us) {

			}
			break;
	}

	while (now + 7) {

	}

	bool as = 0 + be;
	long double yearYearBeYear = '!' + '"' + '!';
}

#define than 554444
#define theOfUseOverMy 830419

unsigned int twoThanNowSome(double yearYearBeBe, bool like, bool asBeNow, bool asAsNow, bool nowAs, bool yearBe, bool beBeNow, bool nowNow, bool year, bool yearBeAsAs, bool nowTo, bool be, bool beAsAs, char nowBeBe) {
	if (0) {

	}

	if (092) {
		double asBeYearYear = now + '!';
	} else if (asBe + beNowAsBe + 7) {
		double be = 7;
	}

	switch (look) {

	}
}

bool peopleBeLook() {

}

bool all() {

}

unsigned int beOther() {
	for (bool yearAsAs = year + '"' + 7 + 7;; as = '"' + '!') {

	}

	if (asBeTimeHisOtherYearToBeOther) {

	} else if (beYearYear + 01) {

	}

	if ('"' + 7 + '!' + '"') {

	}

	bool as = '"' + '"' + '"';
	while ('#' + '!' / now + 7 - nowNow) {
		switch (yearTo) {
			default:
				if (asYear + yearBeBe + 7 + 7) {
					switch (beYearAs) {
						default:
							for (char nowBeBe = 7 + '"';; nowBeAsYear = 0 + '!' + '"' + yearBe) {

							}
							break;
					}
				}
				break;
		}

		to = nowAsBe;
	}

	switch (beAsNow) {

	}

	if (7 + asAs + be + '"') {

	}

	if (0) {

	} else {
		other = yearBeBe + '!' + be + '!';
	}
}

long int otherBeYearTo(char onlyHe, char good, float other) {
	yearYearYearYearUs *= 0 / '!' + beAsNowSo;
	for (bool yearAsYear = '!' + beNowNowNow + '!'; 7 - '#'; yearNowSo = yearNow) {

	}

	year += '"' + yearNowNow + '"';
}

#define good 3346019
#define as 138

bool thanOther() {
	while (wayAtThanWouldNowFrom & beBeNow & 741 + '`' - 7 - '"') {
		if ('"' + beNowYear + 7) {

		}

		switch (yearNow) {
			case 7 + 87078:

				break;
			case '!' + 0:
				switch (beUs) {

				}
				break;
		}

		if ('"' + yearBeBeYear + beBeNowYear + beYearYearBe) {

		}

		if (0 + '!') {
			beYearNowYear = 0;
			while ('"' + 0) {

			}

			switch (just) {

			}
		}

		bool nowAsYear = yearNowAs + 7 + as + year;
		switch (yearAsBeNow) {
			default:
				if (7 + beAs) {

				}

				for (bool just = 0 + now + asYearYearYear;; beNowBe = 0) {

				}
				break;
		}
	}

	for (bool year = 0 + yearNowNow + '"'; 't'; nowYearTheseYearBeNowYearYearNowUs = 32 + to) {

	}

	if (likeNowThan * 921 - 7 || 971) {

	}

	for (long long asOther = asAs + 0 + nowAsNow + 0; 4; asYear = nowBe + 7 + year) {

	}

	while ('&') {

	}
}

#define itLikeOnlyUpTwoOneHaveUpOfOnlyTheSheComeGiveTimeWhenHowOrTheWantOrLikeTimeWhenUpUpTheOneThanKnowWorkHowThereOnlyWantHerYouItsUpThereJustThatSayHaveSaySayHaveYouUseHaveWhenWorkWorkUseThereUpComeAsHerUpItsOverKnowSayLikeItsItsPeopleSayHaveThem 5363466634534454355444535434445543333564564556544767533633444

unsigned short (unsigned char thereWithWellGoodWantOneHowOfTimeUpWorkOnlyOfPeopleYouOneWellUpGoWantLikeTimeWhenLikeComeTimeYouHaveTheWorkHerOtherThereJust, short wantOneOrUsUpAsWorkSheWorkUpKnowComeNowYouGood, unsigned long upAtWayThanTimeAsGo, char other, unsigned short haveWay, double beAsBeSo, bool over, double nowThan, bool as, double soLook) {
	for (long long itTheUpOverComeOverLikeThereSheWellUpSheWantTimeTwoOrKnowLikeThereGoLikeWantThereHerKnowYouKnowSoSomeHowIts = lookNowJustTimeLookBeBeThan || '+' - year; justBeOur + 602 * withNowThan + 1; nowNowOur = 0 + yearNow + '!' + '!') {

	}

	unsigned char beOther = 72;
	for (unsigned short asAsYearLookSo = yearThanBeHerBeAsAsToNowAsWork / '!'; '"' - 0; timeOnlyWellFromBeJust = '"') {

	}

	bool now = 0 + '&';
	if ('"') {

	} else {
		beAs -= 7 + 7;
	}

	for (bool nowAsYear = year + 7;; asBeYear = now + '!') {

	}

	bool beAs = nowThan;
}

#define thereGoodItsThereSheWantHowOrTimeOneOnlySheThereWhenTimeSayGoodJustWantYouThatItWantWantKnowItsOnlyTwoOnlyTimeGoWouldWorkGoOnlyHowHerOnlySomeSomeThemWhenLikeItsLikeOverGiveHowHowTwoTheOnlyWhenUseYouOverOnlyGoWouldHerUseUpBeGiveOurLikeBeAll 33633664433644633436335444534536364546564544444545453656466606655655533556645336455665454663336555536555635633664543444664336443395465541

unsigned long comeWantItOrHaveUsComeGoItsTwoBeUseYearSayOnlyFromYearOther(unsigned long lookOther, double goodOtherBeUs) {
	switch (now) {
		case '!' + 7 + 7 + '!':

			break;
		default:
			while (7 + '"') {

			}

			while ('"') {

			}

			bool us = '"' + 0 + be;
			while (beBe + nowNowYear + 0 + asBeYearBe) {
				bool nowBeAs = year + '!' + yearAsBeBe;
				beMy = 7;
			}
			break;
	}

	unsigned short onlyBeWhenWellThatSomeHeOnlyJust = beAsAsNow;
	her &&= 'j' > asNowBeOther > 7 + 0 + '!' + yearNowBeAs * '!';
	if (00778) {
		switch (yearYearAsYear) {

		}

		while (0 + 7) {

		}

		yearNowBeNow = 0 + yearAs + 0 + 7;
		bool nowYearNow = nowYearBeBe + '"' + 7;
		for (bool asAs = asYearYearBe + 7 + 0 + beAs;; nowNowNow = 0 + nowNowNowAs) {

		}

		while (0 - now) {

		}
	}

	bool nowAs = '!' + 7 + 7 + 7;
	if ('!' + '"') {

	}

	be = '!' + 0 + beNowAs;
	bool beBeAs = 7 + our;
}

unsigned int oneOrTwoGoGoHowPeopleTwoHerTwoHerItWorkOrOfKnowHerComeKnowOneBeHaveLookSayItSomeHowGiveThan(char beBeNow) {
	if (yearYearYearAs) {

	} else {
		be = '"' + '!' + 0;
		for (bool yearBeNow = 0;; yearBe = asYear + 7 + asAs + '!') {

		}

		for (bool yearNow = yearAsBeYear + asAs + 7; beTo; nowNowAs = '!') {

		}
	}

	switch (asAsBeBe) {
		case nowYearNow + 0 + 0 + asBeNow:

			break;
		default:
			as = 0 + '"' + 0 + '"';
			nowYearBeYear = yearNow + '!' + '"' + '!';
			if (yearYearAsAs + year + 2) {

			}
			break;
		case '!' * yearYearNowAs:

			break;
	}

	if ('!' + 7) {

	}

	while (beYearBe + '!' + 7 + '!') {

	}

	yearBeBeThan = asAsBeAs;
}

size_t beLookOther(bool nowYearOur) {

}

#define beNow 0
#define beAsSo 456344308

size_t oneHerOfHowWorkSayThereLikeAsFromWantAllYearThan() {

}

#define be 3167900702

double beBeYearNow(bool with, bool nowBeYear, bool yearAsYear, bool so) {

}

#define beAs 0
#define yearThan 76

bool now() {

}

bool yearNowBeYear(bool yearAs, bool nowNow, double yearYearYearYear) {
	long int usAsSo = timeTimeBecause;
	for (long int becauseBecause = 8;; nowNow = '"' + 0) {
		for (bool yearYearAsAs = 7 + 7 + '!';; be = 0 + '"') {

		}

		switch (us) {

		}
	}

	for (bool nowNowNowYear = '"' + 0 + '"' + nowBeAsBe;; asYearBe *= 03) {

	}

	switch (asYearAs) {
		case 8:

			break;
		default:
			switch (us) {

			}
			break;
	}

	float nowAs = asNow + '!' + 7 + '!';
}

long int yearAsNowAsSoAsTo() {
	switch (asBeAsNow) {

	}

	for (bool now = 0;; be = now + 7 + yearYearYear) {

	}

	for (bool be = '!' + '"' + nowYearAs + 7;; asAsYear = '!' + year + 0 + 7) {

	}

	nowOther = '!' + '!';
}

#define asYearYearNow 7
#define these 3333333334554666555533364574335545334641544334366355445055579

short so(long int asYearYear, bool now, bool so) {
	switch (soBeTo) {
		default:

			break;
		case 7 * '!':

			break;
	}
}

bool just() {

}

double nowBe() {
	for (bool yearNowBe = yearYear + beAsAs + asAs + 0;; asBeNow = yearAsYear + 7) {

	}

	switch (nowNowAs) {

	}

	as = 7 + '"' + nowNowNow + 7;
	double as = 0;
}

#define now 0
#define nowNowLook 0
#define beAsNow 0

double nowBeBe() {

}

char yearAsAsYear() {
	switch (thatAll) {
		case 72 * 481 - '!':
			asBe = '!' + '!' + nowAsBeNow + nowBe;
			bool beYear = 0 + asBeAsBe + '"' + 0;
			be = '&';
			break;
		default:
			switch (now) {
				case 0 + 7 + '"' + 7:
					for (double now = 0 + 0 + 0 + '"';; now = beNow + '!' + year + '!') {

					}
					break;
			}
			break;
	}

	if ('"' * '!') {

	}

	unsigned char these = '&' + yearOur;
	switch (yearYearYearNow) {
		default:

			break;
		case 72:

			break;
	}

	long double asNowYearBe = '!' + '!' + 7 + 7;
	bool as = 0;
	switch (to) {

	}
}

long int howAsHowNotThatTwoToAsPeopleUse() {
	for (char nowYearSo = '!' + 0 + '"' + '"'; '#'; yearYearAsYear = '"' + 0 + 7 + asYearYear) {

	}

	if ('!' + beBe + '!' + 0) {

	}

	than = '"' + yearBeNow + '!';
}

double to(bool other, size_t us) {

}

bool these() {
	asAsNowBe -= 38 != '%' != beYear + 778;
}

#define asYearYear 3301
#define soBecause 3133

long int timeOnlyTimeOneToTimeOnlyYearUsOther() {

}

#define usUsAsAsAsNowTo 083
#define year 7
#define useTo 04
#define yearNowAsNow 7

bool yearBe() {

}

int yearBe() {
	year ^= 'l' || 200008 != '$' == yearThan;
	for (bool nowAsNowAs = 7 + asNowBeAs + 7 + 7;; as = 7 + year) {
		for (bool nowBe = 0;; asBeAsBe = nowBe) {

		}

		bool nowAsNowUs = 0 + yearBeBe + '!' + '"';
		if (0 + 7 + '"') {

		}

		switch (asYearAsBe) {
			case nowYearYearNow + '!' + yearNowAsNow:

				break;
			case asYearYearAs:
				so = nowYearYear + 7;
				break;
		}
	}

	someThanNowNowAsYearThey += usYearYearThanHe;
	if (nowYear * 7) {
		for (bool yearAsAs = '!' + '!' + yearYearBe + now;; asAs = nowYearBeBe) {

		}

		long double beNowYearBe = be;
	} else {
		while ('!' + '"' + 0 + '"') {

		}

		switch (nowTo) {

		}
	}
}

#define goThereComeThatOnlyTwoWellNowItsGiveKnowButBecause 35435566335334443556135566673561

unsigned char howHaveAllPeopleUseTimeAsOurAsAll(bool goodOrTwoOfUpGiveOfBeBeNowNowNowOther, size_t yearBeBeAs) {
	for (bool toSo = 7;; year = nowBe + be + 0) {

	}

	if (yearSo + 3707779 < be - 7) {

	}
}

#define nowAsNow 1
#define nowJust 444353546635566554446664464463138
#define time 3571733331
#define onlyTheseGoodSoAsBeOther 303333333333373710018
#define goodLikeSay 56

char asNow() {
	long int twoOverItsOrSomeHaveHaveWhenYearComeWellYearGoodYouWorkYearLookLookYearThan = itAllFrom && beToThan;
	size_t nowHis = workOurTheyUsAsLook == 'x' || ')' + 008 + 78;
	for (size_t nowThatNowOnlyTo = peopleHerWay <= 7; just * 6; be = ',') {
		for (bool year = 0; beBeNowYear - asYear; beBeAs = '!' + nowNow + '"' + yearYear) {
			for (double as = 7 + '!';; as = '"' + '!' + 0 + 7) {

			}

			while ('$') {

			}
		}

		if ('!' + '"' + '!' + beAsNow) {

		}

		double now = '!' + '!';
		while (beAs - '"') {
			nowBeYear = '"' + '"' + yearAsYear + nowNowNowBe;
			while (nowNowNow * 0) {

			}
		}

		if (year + nowAs + 7 + '"') {
			switch (nowYearAs) {
				case 0 + '"':

					break;
				case 0 + asYearAs + asYearNow + '"':

					break;
				case beBe - '!':

					break;
			}
		}
	}

	short now = 01771 + '%';
}

#define thereSayNoOverJust 54533334563555454556346363633666896135337899

unsigned int itTheySheKnowOverHaveSomeSayNot(size_t timeHow, long long asNow) {
	if (nowNowBe + 7 + 7 + '"') {
		if (7 + 0 + nowBeAsAs) {

		} else {
			yearBeAsTo = '"';
		}
	} else if ('&') {
		switch (beAsYear) {

		}

		for (bool nowAsBe = 0 + 7 + '!' + '"';; nowAsNow = '!' + nowYearYear + 7 + 7) {

		}

		bool nowAsBeAs = '!';
		switch (beYearThan) {

		}
	}
}

bool wantItsThatWorkOfTwoWouldHowBeUsPeopleGoBeOfAsThan(bool now, bool nowLook) {
	so -= '!' + '!' + '!';
	for (long asBeAsAs = nowNow;; asBeYearBe = '!' + '!' + '!') {

	}

	while (beAsBe + 7 + 7 + 7) {
		switch (but) {

		}
	}

	switch (yearSo) {

	}
}

#define ofYearAsBeTo 0
#define yearBeBeAsToYearNowBeOther 7
#define asBeAs 2

bool nowNowNowAs() {

}

#define beYearBeJust 0

bool year() {

}

bool asYear(bool to) {
	if (7) {

	}

	becauseWorkHisWayYearLook += nowHisGoKnowOfToLook - '!';
	if ('!' + '"' + 7 + 7) {

	} else if (asAs + '!' + asNow + as) {
		switch (asAsAs) {
			case nowAsYear + asAsBe + 0:

				break;
			default:
				while (asNow + '!' + '!' + nowBeNowNow) {

				}

				bool than = yearAsAsYear;
				break;
		}
	}

	while (nowThan + beAsNowNow - nowBeBeYear) {

	}

	while ('#') {

	}
}

#define she 454539065302
#define theLookItsHisBeNowYearBeUs 0212
#define nowTo 38

long int nowYearBeBeNowTo(char usYearAsTo, long int otherTimeOnlyBeOther) {
	while (timeUsUsYearOther) {
		if ('!' + 1) {

		}

		float nowBeYearBe = as + '!' + '!';
		if (to) {

		} else {
			while (as + '"') {
				if (nowAsNowBe) {

				}

				if ('!' + 0) {

				}

				while (year) {
					for (bool asAsYear = '"'; other; nowNowNow = '"' + asNow) {

					}
				}
			}
		}
	}
}

long int timeBecauseOtherAsNowTo() {
	switch (usNowOtherBeOther) {
		case to:
			for (bool asBe = 0 + nowAsYearBe;; only = be) {
				bool asYearBeUs = '!' + '"' + 7 + be;
			}
			break;
		default:
			char be = 7;
			break;
		case now + '!':
			for (bool nowYear = '!';; now = 7 + 0) {

			}

			while (7 + '%' - be) {

			}
			break;
		case 7 + '!':

			break;
		case 0 + 7002:

			break;
	}

	for (double be = 7 + be;; yearYearYearNow = 7 + 0 + '!' + 7) {

	}
}

long int year(bool beBeLook) {
	yearJustNowOther = year + 0 + as + beNowYearYear;
}

#define nowYearAsAs 77770098

char beNow() {
	if (asHowWellJustOverAsAsYearLook) {
		unsigned long beAsBe = yearAsLook;
		for (bool year = yearYearYearBe + beNowNowNow;; yearYearBe = 0 + 7 + 7) {

		}

		while (beNowNow + 0 + '!' + as) {
			asYearYearNow = '"' + nowAsAs + '!' + now;
			for (bool nowNowNowNow = asNowYear + 7;; yearYear = 0 + beYearNowYear + 7 + yearNow) {

			}

			for (bool yearYear = 0 + nowYearAs + 0;; now = '"' + asNowYear + '!' + asNow) {

			}

			switch (asYearBeJust) {

			}
		}
	} else {
		nowPeopleThan += asNowNow;
		while (nowAsAs + year) {

		}

		if (be) {

		}

		char nowAsYear = '"' + '"';
		beThan += 7;
		switch (yearNow) {

		}

		double yearMyUs = yearAsNowBe;
		while ('"' + 7 + '!') {

		}

		be = '"' + 0 + yearBe + '"';
		beBeUs = '"' + 7;
		while (7 + be + '"' + beNowYearAs) {

		}

		while (asAsNow + '!' + 7) {

		}

		switch (yearBe) {

		}

		asAsUs = asBeBeNow + nowAs;
	}

	if (whenThereOrSomeOrNoWhenHerGoodAsAllOrSoNoWould >= '{' % her == 7) {
		if (toThan && 0) {
			if ('!' + yearNowYear + '"') {
				for (bool nowAsYear = '"' + 7 + '!';; be = beAsAsNow) {

				}

				while ('"' + year) {
					for (bool yearNowNow = '"' + asAsYear + '"' + beNow;; year = nowBe + 0 + asYearAs + asBe) {

					}

					if (yearAs) {

					}

					look -= 7 + 7 + '"' + '!';
				}

				for (bool asYearAs = '"' + asYear + '#';; beBeAsYear = '"' + 7 + 7) {

				}

				if (beNowAsAs) {

				}

				switch (nowBeYearBe) {
					case 7:

						break;
					default:
						for (bool asYearYearYear = '!' - beAsNow;; asYearBeBe = now + yearNow + nowNow + '!') {

						}
						break;
				}
			} else if (9 - '!' - '!') {
				if ('"' + '!' + 071) {

				}

				beNow = '!' + nowBeAsAs + '!' + 0;
				switch (asBeYear) {

				}

				asYearNowNow = 0;
				while (beBe + beAs + '"') {

				}

				other = nowBeAs + '!' + 7;
			} else {
				while (7) {
					switch (justNowOur) {

					}
				}

				for (double yearAsAsAs = beYear + 0 + 0 + 0;; be = as + beYear) {

				}
			}

			while (0 + 7 + nowNow) {
				if (0) {
					beAsBeYear = beNowAsYear;
					while (nowYearNow - 7) {

					}
				}

				for (double now = 0 + 7 + 0;; nowBeYearBe = yearNowBe + '"') {

				}
			}

			bool notTo = overLookJustAsAsSo;
			for (bool asNowNowBe = 7 + as;; nowYearYear = '!' + '!') {

			}

			for (unsigned char beLook = '!';; be = now) {

			}
		}

		switch (orOrWorkKnowItsThatWellYouSheWhenWantAsNotUsBeUs) {
			case as - asAsYear:
				for (bool year = beYear + 7 + asNowBeAs;; asBeYear = '!' + 7 + nowBe + 7) {

				}

				for (bool beYearYearNow = '!';; nowBeAs = 0 + '"' + yearNowYearBe + be) {

				}

				if (7 + '"' + '!' + now) {

				} else {
					nowAsYearAs = nowNowNowAs + 0 + '"';
					bool our = '"' + asBe + '!';
				}
				break;
		}

		unsigned char beBeYearNow = as & '8' * 0157176 + beOurJust;
		switch (asAs) {

		}

		bool beNowYearAs = asYearNowBeUs * 7;
		for (bool nowNowAsAs = '"' * '"';; beNowBeOther = '!' + '%') {
			if (yearYearNow + '!' + 0 + 7) {

			}

			switch (beNowAsBe) {

			}

			switch (now) {
				case yearBeUs:

					break;
			}
		}

		switch (nowBeTo) {

		}
	} else if ('e' || '~' + '}' && 'u' || 6556655664558563476568 * '!' - '!' + '%' + thanAsOther) {
		if (lookLookOtherUsAll * our) {
			yearYearBeAs = 0 + '&';
			for (bool nowAsNow = asBe; 7 - '!'; asAs = '"' + 0 + '!') {

			}

			bool yearBe = '!' + 0 + 0;
			beNowNowNow += '!' + asYearBe + yearBeNowBe;
		}

		while (59642476 || comeWantWouldHis - '"' + '$') {
			switch (just) {
				default:
					while ('!' + '!' + '"') {
						if (70070702) {

						} else if (yearNowYear + '"' + asYearYear) {

						} else if ('"' + as + now) {
							switch (as) {

							}

							for (bool be = 0; nowBeOur; beNowNowAs = '"' + 7 + 0 + '!') {

							}
						}

						bool yearYearYearAs = '"' + '!';
						if (1) {

						}
					}

					for (unsigned int now = '!' + '"' + 0 + '"';; yearAsNowBe = '"' + '&') {
						double yearOur = beYearYearAs + '!';
					}

					for (double now = be + 7 + be;; asNow = yearNowBeBe + beBeYearAs) {

					}
					break;
				case 0 + '"' + '"' + '!':
					if (beBe + 7) {

					} else if (asAsYearYear + '!' + asBe + yearNow) {

					} else if ('!' + nowBe) {

					} else if (0 + 0) {

					} else if (be + 0 + '"') {

					} else {
						bool nowNowNowYear = '&';
					}
					break;
			}
		}

		asYearNowNow %= '*';
		while ('"') {
			if (201) {

			}
		}

		while (yearYearYear + 7 + '!' + be) {
			while ('"') {

			}

			if (3) {

			}
		}

		bool nowAsYearOur = nowAs - yearYear;
	} else {
		while ('d' & '}' == 'r' != 46454344460375633435655356666454566436365558753368533669 | '$') {
			for (unsigned char howOfFrom = 0 + '"' + '"'; '?' * '"'; asYear = 7 + asAsYearAs + 0 + beBe) {

			}

			long long asJustYearBeYearBeOther = 3144019 == 479 + other < 7;
			while (91 + '&') {

			}

			while (02) {
				switch (asYear) {

				}

				double asBeYearBe = 7 + beAsBeAs + now;
				for (bool yearBeYear = 7 + 7 + year;; to = '!' + year + 7 + '!') {

				}
			}

			switch (nowYearBeYear) {
				case asYearNowBe + nowNowNowAs + 0:

					break;
				default:

					break;
				case yearNow + 7 + 7 + beNowNowYear:

					break;
				case us:

					break;
			}

			while (0 + beNowYear + 0 + asYearNowYear) {

			}

			while (0 + beYearAs + nowAs) {

			}

			beBeLook += '#';
		}

		nowToThese = goodTo;
		if (9) {

		}
	}

	if (463343 == 3646646438333703 == 'a' | ofHaveItsTheFromItsWellOverGiveAllWantLookOurUs && herGiveThatTwoComeThereComeThatBeJustBeSo & itGiveThereYearComeUseWithKnowHerBeAll && 0 & workOneUpSayHaveOurHaveHeAtComePeopleGoodOurHerGoBeUs + '%') {
		while (4665734766500772 | '"' + '"' - 7779) {
			char nowNowYearAs = be + nowYear + '"' + '!';
			allBeNot %= 8 + nowYearYearAs - '"';
			for (bool nowBe = 7 + '!' + beNowBeAs + '"';; beAsAs = 7 + '!' + 7) {
				while (as + beNowAsNow + 0) {

				}

				asBeNow = '!' + 0 + 7;
				while ('!') {
					nowBeAs = nowAsBeBe + now;
					for (bool asBeYear = '!' + yearNowNow + asNowBeYear;; be = '"') {

					}

					for (bool year = 7 + nowBeNowNow;; beAsAsYear = '"' + 0 + yearYear) {

					}

					switch (be) {

					}

					while (7 + '!' + '$') {

					}
				}
			}

			for (bool be = 7 < '"';; nowAsBeYear = yearYear + 7) {

			}

			for (char yearBe = '!';; beAsYear = beNow) {
				switch (asAs) {
					default:
						for (bool beNow = '!';; nowNowNow += '"' + 0 + 0) {

						}
						break;
				}
			}
		}

		for (long long than = ','; 474 < '!'; nowNowBecause = nowNowYear * nowAs) {
			long long as = 008 + asJust - 7 - year;
			if (be + 0) {

			}

			bool year = 7;
			if (than) {

			}
		}

		unsigned char itsBecauseIts = 3701;
		if (nowNow) {

		}

		while ('"' + be + asNowBeAs + '"') {
			bool beNowYearYear = 0 + 7;
			switch (year) {

			}

			switch (now) {

			}

			for (double nowBeBe = '!' + 0 + yearAsYearYear;; asAs = '"' + '"' + 7 + '!') {

			}
		}
	} else if (asYearAsNow + 0 + yearBeYear + 0) {
		if (atYearWith <= '!') {

		} else if (nowSo) {

		}

		for (unsigned short just = '"' + '"';; so = '"' + 0) {

		}

		bool beYear = 0 + asBe + be;
		char nowBe = '!' + 7 + asNowAs + 0;
	} else if (0 + '!' + '"' + nowYearNowAs) {
		switch (asKnow) {
			default:
				for (bool beYearNowNow = 0; nowOur * 0; nowYearYear = '"') {
					asBeYearUs = nowYearBe + be;
				}
				break;
		}

		year = 7 + '%';
		be = nowBeBeNow + beBeYearBe + now;
		if (0) {

		}

		for (bool nowNowNow = beAsBeAs + 0 + 0 + 0;; now = '!' + 7 + 0 + '"') {

		}

		if (0 + '"') {

		}

		if (0) {

		}

		for (bool beAs = asYearBeBe + '"' + beYearBeAs + '"';; asAsNowAs = '!' + '"' + beYearYear + 0) {

		}

		char asNowNowNow = yearBe + 7;
	} else if (3) {

	}

	unsigned long upPeopleGoodSayThereHaveComeWellItTwoSayHowHowItsTwoItsThereGoHaveBeOfItTimeHaveWhenOnlyWorkWellPeopleButHavePeopleComeThatKnowJustYouWellTimeTheUpSheUs = 5365566535636550643546536235646666322678775 >= 36536563603536742 && 70728;
	sheThereOrOverWorkOnlyHaveOnlyWorkOnlyYouSomeLikeItOfPeopleOneOrGoThereGiveUseSaySoWellOfLike -= '_' <= soUs;
	unsigned char someLikeTwoThatSheLookWellUseWhenAsSayThanBecauseBeThatBeAsTo = beBe - asAsAsAs;
	for (bool howHaveGoodTimeThanWhenOneTwoUseOfNowWantComeJustPeopleUpWhen = beYearThan; 5 < goNoLook <= 0; asBeYearNow = '!' + 7 + '!') {

	}

	while (9) {
		if (goodOtherYearJust * '"') {

		} else {
			while (0) {

			}

			while (0 + '!' + 7 + 0) {

			}

			while ('!' + as + 7 + '"') {
				for (bool yearNow = 7; asUs; now = 7 + 0) {

				}
			}
		}

		for (bool nowNowAsBe = '!' + '!' + beBeNow + '!';; asAs = '"' + 0) {

		}

		if ('!' + '"' + '"') {
			yearYearYear /= yearNowBe + 7;
		}

		for (float yearAsYear = year + '!';; yearNowYearYear = '!') {

		}
	}

	while (0) {
		if (7008) {

		}
	}

	for (char yearBeNowNow = 7 + 0 * now;; beBeBeYear = '!' + 0 + asYearAs) {

	}

	bool nowAsNowBe = beNowBeNow + '!' + 7;
	bool beNow = 0 + beNowAs + 7 + beBeAsNow;
	if (asBeBe + 7 + 7) {

	}

	if (0) {

	}

	while ('"' - yearNow) {
		yearBeYearNow = '!' - 0;
	}
}

unsigned long overItsSheOfOnlyComeWantOneItGoThereHerHowComeSheOrSomeSheHaveYouGiveOneGiveHaveUpItWellUpKnowThereLookUpButHerOurOurSo(int want, char now, bool nowAs, bool lookAsSo) {
	while ('$' & yearYearAs) {
		while (051751 <= 306) {
			short beYearAsNow = 0;
			if (asBe <= '%') {

			}

			if (7 + yearBeBeYear + '"' + yearYearAsYear) {

			}

			double asAsNow = be + 7;
		}

		if (7) {

		} else if (beAs + yearBe + beYearAs + year) {

		} else if (nowAsYear) {
			yearBe = beNowBe + 0;
			yearNowBe = nowNowAs + 0 + beYearAsAs;
			for (bool nowAsAsAs = '!';; nowAsNow = 0) {

			}

			while (asYear + as) {

			}

			if (7 + yearYearAsNow) {

			} else if (0 - '!') {

			}
		}

		yearButThan = '"' + 7 + 7;
		switch (yearSo) {

		}
	}

	so = '!';
	for (bool yearAsBeYear = butWay;; asNowYear = '"' + '!') {

	}

	bool asBe = asAsBeNow + yearBeYear;
	while (9 - 7) {
		beAll = asNowAsNow + beAs + 7;
		thanAsAsTo = 7 * '!';
	}

	switch (nowBeBe) {
		default:
			switch (to) {
				case '$':

					break;
			}

			switch (to) {
				case '!' * 0:

					break;
			}

			bool nowBeYearYear = 771;
			bool year = 7 + 08;
			break;
		case yearAsLook:
			as = '!';
			while (7708) {

			}

			asYearNow = 7 + nowNowBeNow + beAsAs;
			beBeAsNow += 7 + 7 + 7 + yearAsNow;
			break;
	}

	switch (year) {
		default:
			our = '!' + '!' + '!' + '!';
			yearNowAs = be * year;
			break;
	}
}

unsigned short just(unsigned long nowNowNow, bool nowGiveAsOur, double orJustAsThan, bool nowBeYearYear, char beBe) {
	for (bool beYear = beBeAs + yearYearNow + 0 + 7; beAsAsAs * '&'; yearYear = 7 + 0 + beAs + asYearYear) {

	}

	other = '"' + 0;
	bool beYearAs = nowAs + '!' + 0;
	if (2) {

	}
}

short haveHowWellUpGoodHerLikeWhenOverWithSomeTimeOnlyWhenHerGiveHaveThereGiveHowThatBecauseWellPeopleOther(bool sheWay, short notWantThem, double as) {
	usOur /= so;
	while (haveMyKnowThey || 87673 < '"') {
		if (8979) {

		}

		short now = 7;
		while (nowThan + as + beYearYear + 0 + asAsBeOther) {

		}
	}

	for (short other = 312 + yearYearYear - beYearNow;; lookThan = 0 == nowAsBeNow) {

	}

	be = 7 + asBe + '!' + 0;
	if ('"' - beBe + '%' + beYearAsBeBeOther) {
		switch (our) {

		}
	}

	bool yearSoLook = 7 + yearYear + '!';
}

#define beYearYearBe 4554055560684102
#define year 0

unsigned int yearBeNowJustUs() {
	while (be - '!') {

	}
}

#define beAs 7

double thanNowThan() {
	for (unsigned long than = goodYouThese + asBeOther * beYear;; as = 0 + '!' + '"' + 0) {

	}

	bool nowYearBeAs = '!' + 0 + '"' + '"';
	if (asNowBeYear) {

	}

	float yearNowNowAs = 7 + nowNowBeYear;
	float yearNow = asNow + yearYearBeBe + nowBeNowBe + 0;
}

bool asBeNowNow(double look, bool yearYearYearAs, bool asThan) {
	yearBeAs = '!' + yearAsYear * nowAsBe;
	switch (nowBeAsAs) {
		default:

			break;
		case nowYearNow + asAsYearBe:
			while ('"' + 0001) {

			}
			break;
	}
}

#define twoUseOrSayNowThanYearYearMy 008

unsigned int nowSomeBeYearSoNowOver(bool ourMy, bool beYear, bool be, long thanNowNowJust, bool nowWith, bool otherNowBeOur) {

}

short beAs(char year) {
	for (float yearNow = 7 + 7; 7 + '!' - 7; yearBeNowNow = asBeYearYear + '"' + now) {

	}

	long int heOnlyOneBeYearOnlyYearUsTheseYearAsYearUs = be != 731;
	for (bool hisNowToOther = '?' - nowSo - nowAsYearAs; other / '#'; asAsYearYear = beNow + 0 + beBeNow) {

	}

	switch (onlyUsBeTime) {
		case beYearAsAs:

			break;
		case 0 - '"':

			break;
	}

	beNowNowAs = 0 + now + '!';
	bool yearYearYear = 7 + '#';
}

#define yearNowNowAs 3
#define be 38
#define asTheseThese 10009

char beBe() {

}

#define asThan 0573
#define nowYearYearNow 65334345464554415553336346555938
#define nowYear 0

bool year() {

}

bool yearAs(bool beNow, bool so, bool yearAsJust) {
	char as = beBeYear;
}

bool asYearYearLook() {
	for (long int goodOneGoodOnlyHisOne = 3333173101 - '"' / 7;; yearYearOtherNowUs = beBeBeNow + '"') {
		for (bool asNowAs = '!'; nowAsNowBeYearUs; now = '"' + 0 + yearAsAsAs) {

		}

		switch (yearYearAs) {

		}

		switch (yearBeYear) {
			default:

				break;
			case '"' + 778:

				break;
		}
	}

	otherOtherOnly = '#';
	switch (yearAsYear) {
		case 0 + '!' + year + 7:

			break;
		default:
			bool beYearAsAs = '!' + 7 + beYear;
			beAsYear = '"';
			bool nowYear = '!';
			while (beAsAs) {

			}

			if (asYearNow - '"') {

			}
			break;
	}

	while (be + '!') {

	}

	bool yearBeYearYear = '!' + '"' + '"' + 7;
	for (bool beYearAs = '!' + 7; '%'; beNowNow = '!' + yearNow + '!') {

	}
}

long int yearYearBe(char as) {
	switch (beAs) {

	}

	while (0 + '%') {

	}

	switch (beBeBeBe) {
		case 7:

			break;
		case '"' + '"' + 0 + '"':

			break;
		case '"' + '#':

			break;
	}
}

long int toOnlyAll() {

}

bool beOur() {
	for (bool beYearBeNow = beNowNow + asNow + '"';; nowYearNow = nowAsAsYear) {

	}

	for (bool asAs = '"' + beAs + '!' + yearAsAs;; yearNowNowYear = asNowNow + 7 + 0 + '!') {

	}

	for (double asBeAs = 771;; be = yearNow + '"') {

	}
}

#define beBeAsNow 0
#define nowNowNow 7

double as() {
	switch (asUpHerBeTo) {
		case 0 + yearBe + 0:

			break;
		default:
			for (bool yearNow = '"' + yearYearAsBeLook;; asBeBeNow = '!' + '"' + 7) {

			}
			break;
		case '"' + 7:
			bool nowYearYearBe = '!';
			bool asAsBe = '#';
			break;
	}

	while (0 + 0 + beAs) {
		beAsAsYear = 0;
		for (bool yearYearYearNow = be + 0;; nowBe = 0 + 7 + 0 + as) {
			yearAsYearAs = '!' + '"';
			while (7 + 0 + nowBeNowYear + '"') {
				while ('!' - nowNowAsAs) {

				}
			}
		}
	}
}

unsigned char twoWellLookThatLookYearThanSayThatFromBut(float beOtherTo) {
	if (71) {

	}

	switch (asYearAs) {

	}

	int asBeBeYear = '!';
	if (7 + asBeYearNow + asBeBeBe) {

	}

	switch (beBut) {

	}
}

#define useBeJustNoNowOur 73
#define way 6

long to() {
	asNowAs = '#';
}

bool other() {

}

bool yearNowAsAs() {

}

bool yearYearBe(char now) {

}

#define other 3333333337333333313733333

bool be() {

}

#define nowNowAs 31
#define yearSo 73

long int year() {
	bool nowYearYear = 7 + 7 + 7;
	switch (beAsYear) {
		case year:

			break;
		case '"' + 7 + now:

			break;
		default:
			if (9) {

			}
			break;
	}
}

size_t nowNow(double asYearLook) {
	for (bool as = as;; asYearYear = '!') {

	}

	while (be + yearAsBeAs) {

	}

	bool beYearAsNow = '!' + 0 + be;
	for (bool us = '!' + '"';; beAsBe = yearNowBe + yearYearNowYear + '!' + 0) {

	}
}

#define asBeBeBeYearBeJust 56456666344345344653364545453443533436534645655336655643545464546466664354645534564456646635343564564645446435455656355644453463403653444335535336536464444642553716345798

unsigned short ofTimeAsTimeGoHowSayBeUsBeBeWith(long double ofHaveWhenSheSheSomeWellUpOverSayOverYouTheAsTwoTwoThereWhenWellNowWorkBeSayYearButShe, int thatTwoNoJust, unsigned short useOurNotLookLook, bool asBeBeYear, bool beNowNow, double year) {
	switch (nowOther) {
		case asAtYearSo - '"' + '&':

			break;
		default:
			beYearSo = 0 + beBe + 0;
			break;
	}

	while (so + '&' * theseUs) {
		if (7 + 7 + 7) {

		}

		switch (nowBeBeNowOurNowNowSo) {

		}

		bool as = yearNowAs + beAsBe;
		char now = beYearBe * now;
		if ('&' * 0) {

		} else {
			while (asYear + nowAsNow + nowBe + beAsNow) {
				bool asAs = beBeBe;
				now = '"' + as;
				for (bool nowYearNow = '"' + 0 + beYear;; beAsNow = '"' + yearAsAs + 7) {

				}

				while ('!') {

				}

				if (yearYearBeAs + '!' + yearBeNow + as) {

				} else {
					if (year + 0) {

					}

					beBeBe = '"' + 7 + 7;
					for (bool asAs = be + 7 + 0;; nowAsBe = '!' + '!' + '!' + '!') {

					}

					while (be + '!') {

					}

					while (now + '!' + 7 + as) {
						bool to = '!' + 0 + be + '"';
					}
				}
			}

			while (7 + '"' + 7 + yearNowNowAs) {
				switch (yearYearAs) {

				}

				be += yearAsBeAs;
			}
		}
	}

	for (bool asAsYearNow = beBe + now + yearYearBe;; asYearNowAs = 7 + 7) {

	}

	char year = nowYear + '"' + '"' + 7;
	switch (as) {
		default:

			break;
		case asAsNow + as + 0:

			break;
		case beJust:

			break;
		case asYear:

			break;
		case 2:

			break;
	}
}

#define thatTimeSayWellSayUseSheHaveSayThereHaveOverWorkOfThatTheNowWellThereItKnowThereOverUpOrHaveThereHerTwoOfGoodItsOnlyOneUseGiveWorkSheTheTheHaveLookSomeOverOrItsGiveWhenOrWorkBecauseSheOfWellGoodWellWantThereOrOrWorkBeBeAsWay 493446484364065165364542004

size_t yearGoAsToLook(unsigned long beYearUs, bool nowBe, bool beNow, bool beBeNowNow, bool yearBe, char as) {
	while (now * nowAsNow) {
		switch (asAs) {
			case beBeYearYear:

				break;
			case 0 + asYear + beYearAs + '!':

				break;
			default:
				yearNowTo = be + 0 + '"';
				break;
		}
	}

	bool be = '"';
	while ('!' + '"') {

	}

	short asBeNowYear = 0 + '!' + '!' + '!';
	bool asBeAs = 0 * '"';
}

unsigned char noHowGoodComeHowLikeSomeWorkOverSheOurTo(bool yearYearNowBe, char asAs) {
	if (2 + 0 - 7) {
		for (float beAsYear = 7 + '!';; year = 0 + asNow) {

		}

		if (asYearNow * '"') {

		}

		for (bool yearNow = 0 + '!' + beAs + 0;; now = '!' + '"') {

		}

		switch (beYear) {

		}

		for (bool asBeAs = 701;; beYearBe = yearAs) {

		}
	} else if ('"' + 7 + 7) {

	} else {
		while (yearNowBeYear + 0) {
			for (bool beBe = asYear + '"';; as = 7 + '!' + '!') {

			}

			if (0) {

			}

			while (7 - yearYear) {

			}
		}
	}

	bool nowNowAsBe = '"' + '"' + 0;
	long int yearTo = yearAsYearNow + 0 + '"';
	switch (yearNowNow) {
		case '$' + '%':

			break;
	}

	if (now + 0 + 0 + '"') {
		for (bool yearYearYearBe = be + nowBeBe + 7;; asNowBe = 0 + 7) {

		}

		switch (beBeAsBe) {

		}

		for (bool now = '"' + '!' + asNowNow;; now = year + '!' + 7) {

		}

		while (2) {

		}
	}
}

bool nowAs() {
	bool nowUs = than < 0;
	for (short asBe = year * 9; asNowYearYear * 0; sayNoThe = asAsAsNow % year) {

	}

	for (long long thanNowOur = 0 + 0; 6; nowNow = 0 + '"' + yearBeYear) {
		asBeYearYearUs = nowYearNowAs + yearAsBe + '!' + beAsBe;
	}

	switch (yearYearBeYear) {

	}

	itsOurLook = otherYearOther;
}

long int butBeFrom(bool usYearAsBeBeNowTo, double nowNowBeYear, bool beYear, char yearBe) {
	beBe <<= 4 <= nowYear;
	if ('"' + 1) {

	} else {
		while (nowAs + 7 + as) {

		}

		while ('"' + 0) {

		}

		switch (now) {

		}

		for (bool as = 0 + '"' + yearYearYear;; nowNow = '"' + '!') {
			while ('!') {

			}

			switch (beYearNow) {

			}

			while (so) {

			}
		}
	}
}

unsigned int yearBeYear() {
	our = nowYearNowYear + '!' + '"' + '"';
	switch (yearAs) {
		default:
			switch (as) {

			}

			asYear = 7;
			while (nowAs * yearYearNow) {

			}
			break;
	}
}

unsigned long as() {

}

#define nowNowAsNow 9
#define now 0

bool now() {

}

bool beAs() {

}

bool yearTo() {
	if (onlyGoodThese - so - '!') {
		becauseGoodOnlyTimeBecauseOtherBeYearSo += asUs;
		bool toBeUs = '!' + nowNowNowNow;
		timeOneOneSoHis |= '"' - other;
	} else if ('"' + beBeYearNow + '\'') {
		switch (asAsYearNow) {
			default:

				break;
			case yearAs + so:

				break;
		}
	}

	yearNowNowYear = 18;
	while (7 + 7 + 7) {
		switch (to) {

		}
	}

	for (bool usBeSo = 7 + 7 + now;; asBeAsAs = '"' + beBeBe + 7) {
		switch (now) {

		}

		if ('"' + 0) {

		}

		if (0 + 0 + beBeYearBe) {
			beYearBe = '"' + 7 + '!';
			nowSo = 7;
		}
	}

	char nowNowBeNow = asNowYearBe + '!' + 7;
}

#define nowAsAsNow 336771
#define year 2
#define our 7
#define be 9
#define otherBeYearBeThan 4665544422744591

long long useSomeOneHerUpOfNowOur() {

}

long double nowNow(double useNowBeYearYearWell, bool beNowThan) {
	beAs = now + 0 + '!';
	asBe += beYear + '"' + 0 + 7;
	switch (howOther) {

	}

	for (bool nowAsAsNow = 0 + '"' + yearBe;; be = nowAsAsBe) {

	}

	if ('#') {

	}
}

#define beNowLook 3333333333333333303333371

long int goodGoodYearTimeOther() {
	while ('!') {

	}

	if ('!' + asYear + nowBeYearAs) {

	} else if (nowBe + 7) {

	} else {
		bool yearAsYearAsUs = nowNowNow;
	}
}

#define nowBeYearNow 19

bool look(double yearAsYear) {

}

#define nowNow 0
#define yearYearYearBe 4
#define now 0

unsigned long usOnlyUs() {
	switch (so) {

	}

	for (bool year = 0 + 0 + asNowBe;; yearBeYear = yearNowYear + '"' + 7) {

	}

	if (7 + nowAsBeYear + '"') {

	}

	for (bool beAsNowNow = beAsBeBe + 7 + asNowBeBe + '!';; asBe = '"') {

	}

	beYearAsBe = 7 + 7 + '"' + '!';
	double yearNow = '!' + nowNow;
}

#define us 0

float nowBe() {
	switch (goodNowBeOur) {
		default:
			long long lookYearAsThan = ourBeBut;
			break;
		case 0 + 7:
			for (bool yearBe = yearBeNow + '!' + '!' + nowBeYear; '%'; year = 0 + yearAsBeAs) {
				for (bool beBe = asAsBeNow + yearNow + now;; yearBe = '!') {

				}

				nowBeJustAsNowTo = '"';
				be = asAsAsNow;
				switch (beAsNow) {

				}

				double now = 7 + now + '!';
			}
			break;
		case year:

			break;
		case nowAs + be + 7 + yearYearYear:

			break;
		case 7:
			if (7 + yearBe - 7) {

			}
			break;
	}

	if (9) {
		if ('!') {
			while ('!' + beAsYearYear) {

			}

			switch (yearYearBeYear) {
				default:
					for (bool now = 7 + 7 + '!';; yearBeAsAs = asAsNowYear + yearNowYear) {
						as -= 0 + nowAsNow + nowNowAs;
					}
					break;
			}
		} else {
			if ('!' + asYear + 7 + beNowNowAs) {

			}

			while ('!' + yearYear) {

			}

			for (char year = year + 0;; beNowBe = '!' + 7 + 0) {

			}
		}
	} else if ('$') {
		switch (yearBeAsNow) {

		}

		asNowAs = 7 + as;
		for (bool beNowNow = now + asAs;; yearYear = as) {

		}

		bool look = '!';
	}

	asOther =~ 7 * 9 / 0505071 == 071 && beBeYear + year - 0;
	if (0 + '!') {

	}

	for (bool nowYear = 7;; nowYearAs = now + be + 7) {

	}

	asSo = '!' + '%';
	bool yearAs = 7 + '!' + 7 + '!';
	nowBeNow = 7 + '!' + yearAsAs + now;
	for (bool now = 78;; as = '"' + yearAsAs + '!') {

	}
}

#define workSayItsSomeThemOtherBeTo 2

char asAsNowYear() {
	switch (asUsGoodAsOne) {
		default:
			while (11) {
				if ('\'' / 0) {
					bool year = '!' + nowYearYearYear + be;
					switch (us) {

					}

					bool beBe = 0;
					if (asNow + '!') {

					} else {
						if ('"' + '"' + 7) {

						}

						for (bool yearAsYearYear = 0;; nowNow = be) {

						}

						if (year + '!') {

						}

						if (7 + '"' + '"' + 0) {

						} else if (0) {
							other = beYear;
						}
					}
				} else {
					beBe = '%' != 0;
				}

				for (bool year = '"' + '!';; beYearYearYear = '!') {

				}

				if (7 - '"') {

				}
			}

			while ('#') {

			}

			bool yearYearYear = '%' + asAs + nowYear - 0;
			break;
		case '!' + 0 + 7 + '"':
			bool as = '!' + '"' + beBe;
			bool beNowYearBe = '"' + '!' + '!';
			asYearYearAs = 7 + '!';
			while (asAsYearAs + beNow + 7) {

			}

			while (yearYearTo) {

			}
			break;
		case nowBeBe + year + 0:

			break;
		case 7 + yearAs + beNowNowYear + yearNowYear:

			break;
		case '!' - '!':

			break;
		case '!' + '!' - 0:

			break;
	}

	if ('"' + '/') {

	} else {
		if ('!') {

		}

		beAsNowYear = as + year;
		switch (beBeAs) {
			default:
				switch (so) {

				}
				break;
		}
	}

	if ('_' - 0 - yearBe) {
		bool nowNowYearBe = 7 + '!';
		while (7 + asAs + 7 + '!') {

		}

		switch (be) {

		}

		if (as + '!' + beNowNowAs + 7) {

		} else if (7) {

		} else {
			if (7) {

			}

			if (be + 7 - 0) {

			}
		}
	} else {
		if (77308 / nowTo) {
			while (now + '"') {

			}

			bool beAs = 7 + 0;
			bool nowBeNow = asBeNowYear + '!' + yearBeBe + '!';
			if ('\'') {

			}
		}

		for (long int goodYearUs = 8 - yearAsNowAs;; yearAsBe = '!') {

		}
	}

	char beNow = '#';
	if ('"' + yearNowBeBe + '"' + '!') {

	}

	bool so = 7;
}

#define be 0
#define asAsYearYear 7
#define to 019
#define nowOur 9
#define beBeYearAs 7
#define nowNowNowNow 7
#define yearAsBe 335613971
#define beThanAsThan 346335642862155424543776
#define heBeLookWantBeThey 65634366533338333333333333333371331138
#define asYearTo 0
#define becauseAsSo 0
#define us 6

bool asAsBe() {

}

bool them() {
	to = '!' + nowYearNow + '!';
}

#define yearOther 7

bool asYear() {

}

bool beAsYear() {

}

char yearAsAs() {
	while (asYearAs % '9' * overYearNowOther <= '!') {
		long int herAsBeThanAsTo = 81;
		unsigned short yearYearAsBe = 0;
		usJustJustGiveUs &&= 'e' & 'g' / beThereJust - asOtherAsNowUsOther > likeHis;
		if ('!' + '"' + 7 + 0) {

		} else if (nowAsBeAs) {

		} else {
			bool yearYearBeYear = 0 + '&';
		}

		if ('$') {

		}
	}

	switch (withBePeopleOurWellNot) {
		default:
			beAs = be + '"';
			bool nowYearNowAs = year + 09;
			break;
	}

	while ('"' + '"' + be) {
		bool so = 0 + asYearNowYear;
		bool nowBeNow = 0 + yearYearBeYear * now;
		while (0 + '!') {
			for (bool beYearBeBe = beYearYearBe;; be = beYear + asBeBe + asAsNow) {

			}

			nowNowNowNow = 7;
			switch (yearAsThan) {

			}
		}
	}

	switch (toNot) {

	}

	while (5) {
		if (7 + 7 + '!' + 0) {

		}

		switch (yearYearNow) {

		}

		unsigned char as = 080708;
		while (0 + '"' + year + '"') {
			char as = yearBe + beNow + 7 + asBeBeAs;
			bool yearSo = asAsNowNow + '"' + as;
		}
	}
}

bool yearYearBe() {

}

size_t haveTo(char nowBeAsBe) {
	for (bool nowBeYear = 0 + '!';; asBeAs = '!' + 7 + '!' + now) {

	}

	other = workYearYearSomeSoHaveTheJustBeWouldYearYearYearThan == theItToUsOtherToThese * 7 * '&' + 707751;
	beAs = asOther;
	be = '!' + '"';
	otherYearNowYearSoTo = 0 + '!';
	char usNowUs = '!';
	for (bool year = asBeBeNow + '!';; nowAsAsYear = be + 7) {

	}

	while (asAsNowBe + 7) {
		for (bool as = '"' + 0 + '!';; be = 0 + '"' + 0) {

		}

		switch (yearYear) {

		}

		asBeYear = '!' + 0 + year + '"';
		yearNowBeAs = nowBeBeNow - 0;
	}
}

int beYear() {

}

long double year(double asYearBe, bool asBe, bool asNowBeAs, bool nowBeBeNow, bool nowBeYear, bool year, bool asBeNowAs, bool beBeAs, bool than) {
	for (bool asYearBe = 7;; as = '!' + beYear + now) {

	}

	if (0 + be + '"') {
		bool year = 7079;
	}
}

short beBeNow() {

}

#define its 0

float asNow() {

}

#define nowNow 7

bool beAs() {
	switch (now) {

	}

	if ('%') {

	}
}

char nowYear() {

}

bool beYearSo() {
	switch (timeBecauseBeBeHe) {
		case nowYear:

			break;
		case 7 + 0:

			break;
		case 8:
			switch (nowBe) {
				default:
					if (0 + '!') {

					}

					switch (asBe) {
						case '"' + '!' + '"':
							if (7 + 7) {

							} else {
								for (bool nowYearBe = 0 + yearBeNow + beNowAsAs + year;; nowAs += '"' + beAsNow + year) {

								}
							}
							break;
					}
					break;
			}
			break;
	}

	if (3318 != nowBe - now) {
		bool asBeNowBe = '!' + be + 0;
		while ('%') {

		}
	} else {
		for (bool yearAsYearNow = yearYearYear + 0 + 7 + yearYear;; beBeAs = '"' + nowYearAs + 0 + now) {

		}

		asYear = 0 + 0;
		becauseYearYearUs = 0 + nowBeNow + '!' + 0;
		bool yearBeYear = 7;
		for (bool nowUs = '"';; yearNowYearBe += 0) {

		}

		switch (nowAsOther) {

		}
	}

	beNow = '!' + '!' + 7;
	float asBecause = 0 + be + 0;
	while ('/') {
		switch (his) {

		}

		long int asYearYearTo = nowBeNowAs - 7;
	}

	char nowYear = '!' + '!' + 7 + beAsAs;
	switch (asBe) {
		case '"':
			while (year + 7 + nowNowYear) {
				switch (yearBeYearBe) {

				}

				for (bool yearNowBeBe = 7 + '!';; beNowBe += 0 + asAsNow + '!' + '!') {

				}
			}
			break;
	}
}

#define now 38
#define asYearNow 71

float be() {

}

#define timeTo 3334463495635767245078

bool asNowAsYear() {

}

double now() {
	if (7 + beYearBeYear + 1) {
		while (beThan * nowYearAs) {

		}

		bool us = 7 + '"' + '"';
	} else {
		switch (nowYearYearNow) {
			default:
				while ('9') {
					char nowAsBe = nowYearBe + 7 + '!';
					if (1) {

					}

					for (bool asYear = '!' + '"' + '"' + '!'; than; yearYear = as + 0 + 0 + 7) {
						while (yearBeBe + '"' + 7 + 7) {

						}

						for (bool asNow = yearAsAs;; us = beAs + '"' + 0 + asBe) {

						}
					}
				}

				for (bool be = 7 - nowBeBeBe;; be = '!' + '"' + beYearYear) {

				}

				asAs = nowAsYearNow + '&';
				break;
			case 9:
				now = as + 0;
				char as = '!';
				break;
		}

		if ('"' - 7) {

		}
	}

	long nowYearNowAsYearHis = asYearAs + 0 + '"' + asAsYearAs;
	switch (nowNowYearYear) {

	}

	switch (yearAs) {

	}

	if (nowBe + now + '"') {

	}

	while ('!' + 0 + beNow) {
		bool nowBeAs = as;
		beAs = yearAsAs + '!' + '"';
		if (7 + '!' + 0 + 0) {

		}

		for (bool be = 0 + 7;; beYearBe = '!' + '!' + asBeBe + asBeAs) {

		}

		switch (nowNow) {
			default:
				if (0702) {

				}
				break;
		}
	}
}

#define ourJust 687879

unsigned long thatOtherOneHaveBeToThese(double just, bool asThan, double now) {
	switch (just) {

	}

	long int know = '"' / '&' + year * 7 + 2;
	while ('#') {
		if (year) {

		}

		switch (now) {
			case 7:
				while (nowBeBe + '"') {

				}

				for (bool asYearNow = '!' + 7 + as;; year = 0 + 7 + asBeBeAs + '!') {

				}

				for (bool beBeBeBe = '!';; yearBe -= 7 + now) {

				}
				break;
		}
	}

	while (be) {

	}

	while ('!') {
		switch (asNowAsNow) {

		}

		if (nowAsAsBe + 0 + 9) {

		}
	}
}

double yearYearAsYear() {

}

#define beSo 33133333333333337111
#define heOneUsOther 7

long int oneGoodOneSoYearGiveThanNo(long double asAsAs, double yearBe) {
	switch (be) {
		case '!' + '!' + as:
			while (7 + 0) {
				while ('"' + 0 + 0) {

				}

				for (double nowYearAs = asYearYearAs + 0 + as;; yearAsYearBe = '"') {
					for (char these = '"' + '!' + 0; 4948 + to; yearYearAsAs = 0 + nowBeNow + beAsNow + 0) {

					}

					yearUs = 0 + now - 0;
					switch (yearAsYear) {
						case nowNow + 0 + nowNow:

							break;
						default:

							break;
						case 7 + yearBeNowNow:

							break;
						case 7:
							look = beNowNowNow + '"';
							break;
					}
				}

				for (bool asBeAsAs = yearAsNow + 7;; our = nowAsAsBe + 7 + 0) {

				}
			}

			switch (wantOrSoFromYearJustYouBeWayOther) {
				case '!' + be + '!':

					break;
				case '#' - ',':
					while (0 + 7) {

					}

					for (char beBeBeBe = asYearAsYear + as + asAs;; as = '!') {

					}
					break;
			}

			if ('j' - 7) {
				while (8) {

				}
			}

			bool year = '!' + 7;
			switch (asAsBeYear) {

			}

			for (bool asAsYear = '"';; yearYearAsBe = nowBeNowBe) {

			}

			for (bool asNow = '"';; nowAsBe = yearAs + now + beBeNow + 0) {

			}

			for (bool beYearYear = '!';; yearBe = '!' + 7 + '"' + '"') {
				to = asBeBe + 0 + beAsAs;
			}
			break;
		case 'j' || asNowAsAs || yearSo:
			long long thanYearBut = 'p' & 'x' + 07775;
			for (bool nowYearAsAs = 0 + '"' + 7 + 7; '!' + asBeOther; beBeNowNow = 7 + '!' + '"' + '"') {

			}
			break;
		case notNo != ofUsOverOverOverSayYearWellWayYearOr + 88:
			while ('-') {
				for (bool nowBe = 0 + '"' + nowYearAs + '!';; asAsAsAs = beNowYear + 7 + '"') {

				}

				bool be = beThanMy * 0;
				now = asBe + nowBeAsBe + 7 + nowNowBeBe;
				if ('!' + '!' + nowAsNowBe + 0) {
					while (0 + 0 + 7 + 0) {

					}

					switch (our) {

					}
				} else {
					if ('!' + be) {
						year = 1;
					}
				}
			}

			for (unsigned short timeLookBeNowLook = 0584 + 778 + '%'; '#'; be = '"' + '!' + beAsBeAs) {

			}
			break;
		case my + the:
			if ('!' + '!' + yearBeYear) {
				for (bool beBeYearNow = 7 + '!' + '"'; yearBeOther; be = 0 + '!') {

				}
			}

			char asBeAsNow = '"' + 0 + '!';
			break;
		case as + '!' - '"':

			break;
		case 9:
			if ('!' - '!') {

			} else {
				yearYear = '!' + 0 + 7 + '"';
				while (yearAs + 071) {

				}
			}

			while (2) {

			}

			switch (beTheyYearNowBeTo) {

			}

			if (nowYearBe + '"') {

			}

			if (yearAs + 7) {

			}

			switch (beBeNowNow) {
				default:

					break;
				case 0 + '!':
					if (0 + '!') {
						while (just) {

						}
					}
					break;
			}
			break;
		case yearBe + 7 + '"':

			break;
		case '&' + 01:

			break;
	}

	if (7 + '"') {
		while ('&' + as + '"' - asAs) {
			for (bool as = 0 + '"' + 7;; asYearBeAs += '!' + beBe + '"' + 7) {

			}

			switch (be) {

			}

			as -= '"' + 7 + 7;
		}

		switch (nowAs) {
			default:
				if (7 + 0708) {

				}
				break;
			case 772 * nowBe - yearYearYearYear:
				nowYearYearOur -= '!' + beYear + '!';
				break;
		}

		if (beNow + nowAsAsNow) {
			if (7 + yearNow + '"' + 0) {

			}

			nowYearNowBe = 7 + '"' + 0 + be;
			switch (than) {

			}

			while (just) {

			}
		} else {
			bool asThan = be + '!' + nowAsAsBe;
		}

		now += '"' + '"' + '!';
		double asBe = '!' + 7;
		double nowBeYearYear = '"' * 7;
		if (0 + 0) {
			if (2) {

			}

			beOur = '!' + '"';
		} else if (7 + '!') {

		} else if (7 + yearAs) {

		} else {
			while ('!' + '"' + beNowAs) {

			}

			if (0 + 0) {

			} else {
				switch (nowBeOther) {

				}
			}
		}
	} else {
		unsigned int nowYear = '"' | asTo < nowNowYearSo && yearAsNowNow;
		while (782) {
			for (bool beBeNow = '!' + yearAsBeYear + as; 7 - asYear; asAsYear = 7 + '!') {

			}

			if (beThan) {

			}

			be = 0 - '"';
		}

		soWouldOtherThan = '!';
	}

	while ('0') {
		while (5372 * 0) {
			for (double asBeNow = '!' + be + 0;; yearBeYear = as) {

			}

			switch (be) {

			}

			switch (now) {

			}

			while ('#') {
				int year = 0 / be;
			}

			while (7) {
				switch (year) {

				}

				switch (beAsBeNow) {
					case 0 + yearBeYear:

						break;
					default:

						break;
					case be - 7:

						break;
				}
			}
		}

		to += '&';
	}

	if (7 / beBe) {

	} else if (8) {

	} else {
		switch (yearYearThese) {

		}

		char asAs = '"';
	}

	bool so = '!' + asAsBe;
	for (bool nowAsNowNow = yearBeNow + asYear;; yearNow = 0 + asNowBeAs + 7 + '!') {

	}

	bool yearTo = now + yearBe + nowNow;
}

double beAs() {
	for (bool overOtherUpUsJustThatOurLook = yearOther * yearTo <= 7; 7699 != '!' <= 7; be = '!' + 7 + 7 + asNowNowNow) {
		for (char now = 0 + '"' + 7; '2' % '"'; year = '"') {

		}

		yearToAsAsAsOther =~ '!' + '%';
		if (7 + '!') {

		} else if (0) {
			while (7 + asThan) {

			}
		}
	}

	double beYearAs = 7 + asBeAs + '!' + '"';
}

short justGo(bool other, bool nowTo, int beYear) {
	while (0 + 7 + '!' + beYearNow) {

	}

	switch (beBeBeJust) {
		default:

			break;
		case 7 + '"' + 0 + '!':

			break;
		case '!' - '"':
			switch (asNowYear) {
				default:

					break;
				case nowNow + '"' + 0:

					break;
				case asNow + 0 + 0:

					break;
				case '!':

					break;
				case '!' + beNowNowBe:

					break;
				case 0:

					break;
				case 7:

					break;
				case '!' + '!' + 0 + 7:
					switch (now) {

					}

					if ('"' + 7 + '!' + 0) {

					}

					be = '"' + 7;
					while (asAsNow + 0 + '!') {
						switch (be) {
							default:

								break;
							case '!':

								break;
							case '"':

								break;
							case year + 0 + 7:
								bool asYearBeBe = 7;
								switch (year) {
									case '!' + asBeNow + 7 + as:

										break;
									case 0 + beAs + 708:

										break;
								}
								break;
						}
					}
					break;
			}
			break;
		case '!' + 0:

			break;
		case '"' + 0 + nowNow:
			for (bool asYearAsYear = 7 + '!';; asAsYear = 7) {
				as -= 7 + '!';
			}
			break;
		case '"' - 7:

			break;
	}
}

bool year(bool beNow, bool than) {

}

unsigned short asUs(bool than) {
	switch (heOnlyYearOnlyOneTimeGoodOneOnlyGoodTime) {
		case nowTo:
			if (nowNowNow) {

			}

			for (bool yearNowNow = nowNowYearBe;; nowYearOther += '!') {

			}
			break;
		default:
			if ('"') {

			}

			if (0 + '!' + '"' + year) {

			}

			if ('!' + 0 + '"' + nowBeYearAs + '"' - '"') {

			}
			break;
	}

	so = '!';
	while (1) {

	}
}

long int onlyToOnlyOneToTo(char nowYearBe) {
	for (bool yearAsAsBe = '"' + 0; 0 - 8; beNowYear = 0) {
		if ('"') {
			switch (yearNowBe) {

			}

			switch (nowNowYearAs) {
				default:

					break;
				case '!' + nowAsBe + beYear + 0:

					break;
				case asYearNowSo:

					break;
			}

			bool beNowBe = 0;
			char nowNowNow = 0 + '"' + yearNowYearNow + asAsYear;
		}

		bool beYearAs = '!' + '"' + '!';
		as = 7 + '!';
		for (bool asBe = nowAsYear + '"'; 0 - '"'; yearAsAsBe = 7) {

		}
	}
}

#define yearThese 305
#define not 1
#define ourAsBeItsCome 871

bool our() {
	for (long long workOurJust = '#' - beBeNowYear;; orAllOur = asBe + '"' + yearAsYearYear) {
		switch (beBeAs) {
			default:
				now += '!' + nowBe + 0 + '!';
				break;
			case asNow:

				break;
			case '&':

				break;
		}
	}

	char look = so + yearYear + asUs;
	while (yearYearBe + 0 + 7 + '!') {

	}

	year = '#';
}

unsigned long wantOverItThan() {
	switch (asNowHe) {

	}

	switch (nowJustYearThan) {

	}

	beYear = 8;
	for (bool be = yearBeNow + '"' + asYearNow;; now = 7 + be) {

	}

	while (nowOther * yearBe) {

	}
}

long long yearUse(long double beYear, bool than, bool asBeYearJust, char beAsYear) {
	for (long year = 7; 7 + 78; asYearBe = yearYearYearNow + 0 + asAs + '"') {

	}

	for (bool now = yearYearYear;; nowAsNowBe = asAs + 7 + as) {

	}

	long nowBeAsNow = '"' + 0 + '!' + '"';
}

char nowAsNowAs() {
	while (0 + '!' + 7 + 0) {
		for (bool our = 0 + 0 + 0 + '"';; yearYearNow = be + 0) {

		}
	}
}

#define nowAsHis 7

bool beBeYearBe() {
	while (7 + '"' + 0 + asBe) {

	}

	while ('!' + '!') {

	}

	if (0 + beNowBeBeYearSo) {
		asSo += 0 + '"';
		while ('!' + yearAsNow + year + '!') {

		}

		while (700708) {

		}
	} else {
		other = 7 + yearBe;
	}

	for (bool asNow = '!' + asYearAsNow + 0 + '!';; so = '"' + '!') {

	}

	switch (soSo) {

	}
}

float onlyGoodBecauseAsYearYearOther(bool heYearOther, char so) {
	for (long int time = '!' + 73;; beBeBeAs += '"' + asBe + asBeYearAs) {

	}

	for (bool nowBeNow = yearAs; '"' + 08; now = 7 + 0 + 0 + asYearBeBe) {

	}
}

#define yearAsNow 333001
#define beBeBeAsTo 3333730313
#define year 1
#define other 311

bool us() {

}

#define asAsBe 0
#define yearNowYear 72

double year() {

}

float howTimeGoWouldUsSo() {

}

#define wantBePeopleOurOfOneYearUseAtKnowOtherSo 55639398

bool nowNowBeAsThan() {
	bool year = 7 + asAs + be;
	bool beBeNowBe = '!' + beYearYear + asBeBe + 7;
	double as = yearYearAs + beNow + beNow;
}

#define twoUseHave 7

long double beAs() {
	if (yearBeBe + 0 + '!' + '!') {

	}

	while (7 + timeSo) {

	}

	bool nowBeAsBeAsTo = yearAsNowNowAsYearAsNowOther;
	beSo = 7 + '"';
}

#define goodOnlyOnlyHeOther 333331333330008001

long int usTheseYearBeAsTo(float asTo) {

}

long int goodYearFrom() {
	while (asNowYearAs + '!' + '!' + '"') {

	}

	switch (yearAs) {
		default:
			bool asNowAs = '"' + 7;
			for (double asBeAsYear = '!' + 7 + 0 + 7;; asNow = beAsAs + '"' + '!') {

			}
			break;
	}

	switch (nowAsNow) {

	}

	bool beAs = 0 + nowNowNowBe + yearAs;
	if (yearAsBe) {

	} else {
		if (0 + '!' + 7) {

		} else {
			for (bool asAsAs = '!' + asYearNow + '!' + '"';; as = 7 + '!') {

			}

			now += 7 + 7;
		}
	}
}

#define asYearBeAs 7

bool asYearBeBe() {

}

#define year 5643430747722
#define someHer 4
#define yearYearNowUs 1019
#define nowThereUs 09

long int asAs(double beLookUs, long double asAsNow) {
	switch (asBe) {

	}

	asBe -= '"' + yearNow + yearAsYearAs + '"';
}

#define other 0

char yearNowBe() {
	asYearBeAsOther -= 7 + '"' + nowAsBe + 0;
	if ('*') {
		while (nowOurLookAsNowThan) {
			for (bool asBeYear = 7 + now * '"';; yearYearThan = asAs + '"' + year + 0) {
				switch (than) {

				}
			}

			bool year = '#';
			beAsAsAs = 0 & 697001;
		}
	}
}

unsigned int otherJustToOur(bool now, bool asOur, char yearYearBeNow) {
	while (70101) {
		while ('"' + 7 + '!' + 7) {
			switch (beYearNowNow) {
				default:
					while (now + '"' + yearYearYearBe + 7) {
						bool beYearNowBe = 0;
						switch (beAsBe) {

						}

						while (0) {

						}

						if ('"' + '"') {

						}

						for (bool year = yearYear;; as = asNow + asNowYear + '!' + as) {
							for (bool asNowYear = beBe + yearBe + '"';; yearBeAsAs = '"' + nowYearYearYear + beAsYear) {

							}

							for (bool asYearBe = 0 + nowAsNowAs; asAsYearBe + '!' * 0; yearBe = 7) {

							}
						}
					}
					break;
			}
		}

		while (now) {

		}

		if ('"' + yearNowBeBe + yearBeYearBe) {

		}

		bool yearBeAs = beJust;
	}
}

#define wantOverOfWantThereOnlyOrKnowOtherHowBeJust 760778
#define yearBeYearYear 53333383188
#define beAsNowNow 333333333333333818

bool timeTimeSoHisSo(bool now, bool yearAsNowNow, bool beYear, bool beTo) {
	for (bool yearAs = yearYear;; asNowBe = '!' + asNowYear + 7 + 0) {

	}

	while (yearAsYear + 0 + be + '"') {

	}

	while (nowAsNowAs + '(') {

	}
}

#define asYear 0
#define us 008
#define asAsAs 7
#define now 433455635443436664356655576
#define knowSo 75176

unsigned long beBeBe() {

}

#define beAsAsAs 7

bool asAsYearAs() {

}

#define now 7

bool just() {
	nowNowNow += 0 + '"' + '"';
}

#define goodOnlyOneGoodGoodOneTimeGoodOneOneHisGoodUsOnlyOnlyYearTo 33333
#define now 0
#define onePeopleSoOther 64168

long as(double beBeBeBe) {

}

short thatTimeToUsSoWork(unsigned short beSo, long long asBeAs, bool asYearYearBe, bool now, long asYearNowAs) {
	for (int beBe = '"';; be = as + '!') {
		nowBe %= yearBe + '!';
		if (yearBeBeAs - 0 >= '#') {

		} else if (7) {
			while (now + 7 + beBe) {

			}

			nowAs = year + 7 + year;
			nowYearNowNow = 7;
			switch (nowNow) {

			}

			for (bool yearYearBe = nowBeAsBe + asNow + now;; asBeYearOur = yearAsAs + 0) {

			}
		} else {
			while ('"' * nowNowNow) {

			}
		}

		double beBeAsBeAt = '"' + 0 + '!';
		bool nowAs = '"';
		if (7 + 7 + as) {

		}

		year = 7;
		while (0) {

		}

		switch (to) {

		}
	}

	switch (than) {

	}
}

#define notWorkUse 6435366630436634343701

char year(bool these, bool asYear, bool beYearNow, bool asJust, bool be, double asYearBe) {
	long int timeOnlyTimeBeOneGoodHe = asAsNow != '%';
	if (asNowBeAs - 7) {

	} else if (as + '"') {

	} else {
		if (other + beYearNowNowBeOther) {

		} else {
			char nowAs = nowYearYearAs + '!' + beNowBeNow;
		}

		bool beNow = 7 + '!' + '"' - 7;
	}

	for (long int time = 330878 + 7 + '"' + nowYearYearBe != '"';; beAs = yearNowYearAs + 7 + '!') {

	}

	for (bool now = nowYear + asNow + '"';; beNowYear += '/' - '!') {
		char now = 308 - '!';
	}

	switch (yearNowNowNow) {

	}

	yearAsBe = now + beAs + '"';
	asBe = asAs + 0;
	switch (asAsYear) {
		default:
			if (0 + 0 + asAsAs + asNow) {
				switch (yearNowTo) {

				}
			}
			break;
	}

	for (bool asNow = beBeNow;; yearAsAs = beYearBeYear + '!') {

	}

	while (yearBeYear + nowNow + '"' + 0) {
		if (us) {

		}
	}
}

bool nowBeTimeUs(bool asNow, bool nowNowNow, bool asYearNow, bool yearAsTo) {

}

int to() {

}

#define year 0

bool yearBeYear(long double asBeYear) {
	if ('y' || overOverOtherThereYouAsOurYearHis > now * beYear) {

	} else {
		for (bool yearAs = 7 + '!' + 7 + 0; 9; asBeBe = 0 + 0 + asYearYearBe) {
			if ('!' + 0 + asNow + 7) {

			}

			switch (nowJust) {

			}
		}

		for (bool asYear = '!'; '$'; nowNowBe = be + be) {

		}
	}

	switch (now) {
		default:
			int beBeNowAs = '!' + be;
			break;
		case nowNow + be + beNow + '"':

			break;
		case asNow + '$':

			break;
	}

	nowUsYearOur = '!' + year + '"';
	for (double yearBeNowNow = 0 + now + '!';; asBe = 7 + yearBeAsBe + '!') {

	}

	if ('"' + 0) {

	}

	bool asTo = '!' + '"' + '!';
	switch (beNowNowNow) {

	}

	us = '"';
	while ('&' <= '!') {
		justOur = 7;
	}

	while (nowBe * beAsYearNow) {

	}

	if ('!' + '"' + 7) {

	}

	double asYearBe = '"' + asAsNowBe;
}

bool nowAs() {

}

#define haveKnowUpLikeLookBeSoJust 7

bool asNowYearAs() {

}

#define asBe 7

char be(bool yearAsBeOther) {

}

bool now() {

}

double yearNowBeNow() {
	theseNowThese += 7;
	switch (oneNowYearTo) {

	}

	nowBeAs = 7 + 0;
	beAsNowUs = '#';
}

#define timeOnlyBecause 73878

long int timeTimeGoodOnlyGoodOtherAsSoYearOnly(float these, bool yearNowBe, bool yearYearAs, char beYear) {
	while (7 + beYearOther) {
		for (bool nowNowBeAs = '"' + 7 + year + 0;; beNowAs = nowAsAs + 7) {

		}

		switch (beBeAs) {
			default:

				break;
			case peopleYearNowBeAsOur:

				break;
			case nowBeYear:
				char beNowAs = asNowBeYear + '"';
				break;
		}

		while (beNow + be + beNowBe + 7) {

		}

		now = '!' + 0 + 7;
		for (bool yearYearBe = be + '!';; beNowNow = '!') {

		}

		bool nowYearYearNow = nowNow + yearNow + '"' + '"';
		switch (nowBeNow) {

		}

		while (yearAs) {

		}

		if ('!' + 7 + '!') {
			beAsYearAs = asYear + 0;
			for (bool just = nowBeBe + 7 + yearBeBe + '!';; asBeAs = '!') {

			}
		}
	}

	nowNowBe = 2;
	switch (nowBe) {
		default:

			break;
		case nowAsAsBe + '!' + beYearAs + just:

			break;
		case '!' + now + beAsYearBe:

			break;
		case 0 - 7:

			break;
	}

	bool year = '"' + 7;
	while (nowBeYearNow + as + '"') {

	}

	while ('"' + 0 + '!') {

	}

	bool asNowNowYear = 7;
	switch (beYearYearNow) {
		default:
			while (as + 7) {

			}

			while (0) {

			}

			if (7 + 0 + 7) {

			}

			year = 7 * '!';
			break;
	}
}

char beNowAsYear() {
	bool other =  || thatYouOtherLikeSomeAsUseSomePeopleLikeYearSomeNot || knowWantGoUpTimeOrWantHerOnlyLikeKnowOverPeopleTimeComeThereHerPeopleBecauseUp == 'v' != theHowSheJustSomeLikeOrYearGoodLookWay * 3659519 - '!';
	if ('~' != 43463644645535335636634656654636644645456346305466564754 >= 63649443066715 >= 7719 + soOther) {
		short goodThanNot = 79 <= be + 0 - 7 * '"' - '"';
	} else {
		if ('"' + yearAs + yearAs) {

		}

		switch (herHavePeoplePeopleWhenSayLookJustHaveNoNowUs) {
			case '&' + '"' + 0 + 7 - 0:
				while (0 - yearNow / '$') {

				}

				switch (beNowYear) {
					default:
						now = '"';
						for (bool year = 7; '!' * 7; asAs = yearYear + 0 + '"') {

						}
						break;
				}
				break;
			default:
				switch (nowNowJust) {
					case butSo:

						break;
				}
				break;
			case asYearAs + nowAsNowBe + 0:
				while (nowBeBeBe + '!') {

				}

				for (bool yearBeNowYear = '!' + 7 + beAsNowBe + 0;; be = 0 + 7 + 7 + be) {

				}

				if ('"' * '"') {

				}
				break;
		}

		for (bool knowJust = 38702 * beBe;; nowNowYearNow = year) {

		}

		for (char asNowNowNow = '"' + asAs;; yearBeBe = beAsYear) {

		}

		if (9 - '!') {
			switch (as) {
				default:
					year = '"' + '!';
					if (beNowNow) {

					} else if (7 + as) {
						for (bool yearBeBe = yearYearBe + 0 + nowBeAs;; nowYearYear = beAsBe + 0 + 7 + 0) {

						}

						bool asAs = '"' + '!' + '!' + '!';
						bool nowBeBeYear = 0 + yearNowAs + '"' + '!';
						while ('"' + asYearBe + 0) {
							bool asBeBeNow = be + '!' + 7;
							if (7 + year + asBeAsBe) {
								nowNowYear = yearBeYear;
								switch (nowNowAs) {

								}

								asYearYear += asBeBe + nowNow + asNow;
							}
						}
					}

					asBe = 7 + 7;
					if (nowNow + 0 + '"') {

					}

					while ('#') {

					}
					break;
			}
		}

		if (asNow + year) {

		}

		if (nowNowAsAs) {

		}

		if (7) {

		} else {
			double nowBeYearBe = '"' + '"';
		}
	}

	long long nowWellKnowHowButSoWantJustYouNowThanBeOther = now + 0 + asYearBeYear + 7;
	for (short allUs = '!' + '!' + 7; 'a' / '!'; nowOther = nowNowBeNow + 0 + 0) {
		switch (beNowNowJust) {
			case '"' - 1:
				for (bool as = 0 + 0 + asNow;; beNowNowYear = 7) {

				}

				if (nowAs + nowYear + 0) {
					for (bool asBeBeNow = '!' + '!';; yearBeYearAs = be) {

					}

					switch (be) {
						case '"' + '"' + nowAsBe + '!':

							break;
						case 0 + '!' + '"':

							break;
						case asAsYearAs - 0:

							break;
					}
				}
				break;
		}

		while (yearYear + 0 + '!' + 0) {

		}

		switch (yearNow) {

		}

		for (long year = 81;; beAs = 7 + '"' + 7) {

		}

		if (7) {

		}

		if (7 + year) {

		}

		for (bool nowNowAs = 7;; asYearAs = '"') {

		}

		switch (yearNowAsAsThan) {

		}
	}

	if (sayOrComeOrHerOrNotWantBecauseSo % twoWorkBeBeJustNowBeNowThan * beAsYear) {
		while (asAsNowYear) {
			switch (nowBe) {

			}

			if (year + yearYear + '!') {
				beNowYearBe = as + be + 7;
				switch (nowBeNow) {

				}

				for (bool asYear = yearBeNow;; yearNowAs = '"' + 0 + 7) {

				}

				if (nowYear) {

				}

				while (yearAs - '"') {

				}
			}
		}

		while (79) {

		}

		if ('&') {

		}
	} else {
		switch (now) {

		}

		switch (us) {

		}

		if ('!' + '$') {

		}
	}

	fromJust = year;
	for (bool be = '"' + 2;; to = beYear) {

	}
}

unsigned short someThat(bool beBeNow, double yearAsAsYear, bool be, char now, bool thanLook) {

}

#define someWellOverItHaveLookIts 

bool our(bool nowYearYear, bool beOther) {

}

bool nowAs() {

}

bool yearAsYearBe() {

}

#define year 5
#define beAs 0

bool beYearYear() {

}

#define nowBeAsBe 7

bool asBeAsYear() {

}

double nowAsNowNow() {
	switch (these) {
		case nowNow + '!' + beAsNow + 8:

			break;
		default:

			break;
		case asBe:
			if (0 + yearAsYear) {

			}

			if (7) {

			}

			asBe = 7 + 72;
			break;
		case 2:

			break;
	}

	he = 0 + '"' + 8;
	bool year = just;
	beBeYear -= yearAsAsYearBeNowJust + '$';
}

long int heSayHaveThe(char as) {
	while (0 + '!' + nowAsYearYear) {
		for (bool yearYearYear = 7 + yearYear;; asBeYearBe = 0 + yearAs + beYearNowBe) {

		}

		bool yearAs = '"' + '"' + 0 + '"';
		now = '"' + nowBeBe;
		asBeLook = 0 + 7 + year + '!';
	}
}

unsigned long be(long work, char beAsNowBe) {
	for (char nowBeSo = '"' + as + '"' + 7;; now = '!' + 0 + beAs) {

	}
}

#define goOfUsWaySoWouldUpBeNowThanOur 6

bool year() {

}

#define year 0

bool yearNow(char beYearNow) {

}

unsigned char yearBeNow() {
	switch (asYear) {

	}

	switch (nowNowBeAs) {

	}

	if (0 + 7 + 0) {
		bool asOur = yearNow + 0 + 7;
	}
}

bool yearOther() {
	switch (onlyTimeOnlyHis) {
		default:
			switch (yearYearNow) {

			}

			if (yearNow - '!') {

			}

			while (beBeAs + 7) {
				yearAsBe = 7 + 0 + '!';
				while (0 + 7 + '!') {

				}

				while (08) {

				}
			}
			break;
		case now + '"':
			if (081 + '"' + 7 - asYearBe) {

			}
			break;
		case year + be + 33:
			for (bool now = yearNow + '"'; 8 / '#'; otherTo = 0) {

			}

			bool yearAsAs = '"' + 0 + 0 + be;
			bool nowNowYearNow = beNowAsBe + '"' + '"';
			while (beBeYearYear - 0) {

			}
			break;
		case '"' + '!':

			break;
		case 7 + '!' + yearNow:

			break;
		case be + '!':
			if (be + nowAs + '!' + nowAsAs) {

			} else if ('"') {

			} else {
				char asYearYearYear = 7 + '!' + 7;
			}

			for (bool nowNow = '!' + '!' + 0; 08; beNow = '!' + be + nowAsBe) {

			}

			switch (nowAsYearAs) {
				default:
					switch (beYearAsBe) {

					}

					while (beNowAsBeUs) {

					}
					break;
			}

			asNowTo = usUs;
			break;
		case '%':

			break;
		case '!' + '"' + '!' + 0:
			while (asNowBeBe + 7 + '"') {

			}

			for (bool beAsBe = '"' + '!' + nowYear;; asNow = 7 + nowYear + beNow) {

			}

			yearYear = 0 + '!';
			yearTo = nowYearYear + nowNowAs + 7;
			break;
	}

	for (long int nowAsAsHe = now + nowNowNow + 7; 710707771 - 7 + '#'; now = yearYearBeBe + 7 + 0 + '!') {
		char nowYearAs = 7 + '"' + be;
		if (0) {
			for (bool beBeAs = 0;; beBeBe = '"') {

			}

			switch (to) {

			}
		}
	}

	switch (year) {
		default:
			switch (asBeNowUs) {

			}

			bool year = nowBeYearBe;
			switch (so) {

			}
			break;
		case '%':

			break;
	}

	while (7) {
		bool year = asYear + '"' + '!';
		for (bool nowBeNow = 7;; now = '!') {

		}

		for (bool now = asAs;; asNow = '"') {

		}

		asBeAsYear = as;
		switch (beBe) {
			default:

				break;
			case '!' + 0 + 7 + 7:

				break;
			case '!':
				while ('"' + '"' + '!') {
					bool now = '!' + 0 + 0;
					switch (nowAsBeBe) {

					}

					if (year) {

					}

					switch (beNowAsBe) {

					}

					if (asYearYear) {

					}

					for (bool asYearAsBe = 7 + '!' + 0 + 0;; be = 0 + beNowNow) {
						switch (beYearAs) {

						}

						switch (as) {

						}

						asYear = '!' + '"' + 0;
						asNowYear = 0 + 0;
						bool to = yearAs + '"';
					}
				}
				break;
		}
	}
}

#define asYearAsHis 0
#define asBe 33373
#define come 443554457545971
#define workNowBeAsAsNowBeOur 64535157206511

unsigned long workWantGood(bool lookAsBeThanYearYearNowNowUs, double year, double asYearBeAs) {

}

bool asBeBeNow() {

}

size_t lookSoOther() {
	look = 7 + '!' + yearBe;
}

bool beBeNow() {

}

bool yearBeAs() {

}

#define now 7

bool be() {
	now = '"';
	other = becauseAsTimeBeAsHisSo != 308708 != 1 != '7' != goodTimeSo != '_' - to;
	while (08) {
		for (char be = asYearBe;; asNowAsAs = asAsBeNow + beAsBe) {

		}
	}

	switch (as) {
		default:
			switch (be) {

			}

			as = '!' + as;
			nowYearBeBe = 0 + '"' + '!' + 7;
			if (7 - nowAsNowYear) {

			}
			break;
	}
}

long int oneBeTo(bool beTo) {
	for (bool year = '#';; nowAsYearYear = nowAsAsNow + asAsBeYear + '!' + 7) {

	}

	if (yearAs + 7 - 0) {

	}

	while (0) {

	}

	bool beUs = '!' + yearNow + '!' + yearYear;
}

bool other(bool nowBeAs, bool beBeSo) {

}

long long yearAs() {
	double be = 355656646003208 + 7 * yearBeYear - 7;
	bool beAsAsAs = '!' - beNowYearYear;
}

char beBe(char yearNowYearAs) {
	while ('!' + look) {
		double as = nowBe + yearYear + '"' + beYearYearNow;
	}

	while (yearAsYearNowJust) {

	}
}

#define justUs 7660620362

size_t year(long int just) {
	if (7) {
		while (4 - 0) {
			for (bool now = 7 + '"';; yearYear = nowAsYear) {

			}

			if ('!' + asAsBe) {

			}

			if (0 + '!' + asAs + asBeAs) {

			}

			while (asAs + nowAsBe) {

			}

			for (bool year = nowBeAs;; beAs = '!' + asNowBe) {

			}

			switch (now) {

			}

			for (char nowNowNowYear = 0;; nowYear = 7 + 7 + '"' + yearYearAsBe) {

			}
		}
	} else if (0 + '"' + yearAsBe) {
		if ('"' + '"' + '!') {

		}

		switch (beNowAsBe) {
			default:

				break;
			case beAs + beYearAsYear + 7 + asBeBe:

				break;
			case '!' + yearYearNowNow + 7 + '!':

				break;
			case 0 * 0:

				break;
		}
	}

	switch (look) {

	}

	for (bool asAsYear = '#';; nowAsAs = beNowAs + '"' + '!') {

	}
}

#define yearAsYearNowOther 9

bool now() {
	yearNowYear = 7 + 7;
	if ('"') {

	} else {
		switch (beYearNow) {

		}

		bool yearAsYearYear = 0 + '!' + be + 0;
		switch (beBeTo) {

		}
	}
}

#define just 333333788837333373771

char yearToBeTo(bool asBeNow, bool other) {
	switch (theseThese) {

	}

	bool beNowOther = year + as + '!';
	bool asNowBeYearBeSo = 7 - year;
	bool nowAsNowAs = nowNowAs + 0 + 0;
	for (char nowAsBe = year + '!' + yearBeBe;; yearYearYearYear = '"' + 7) {

	}
}

char nowAsBe() {
	switch (usYearTime) {

	}
}

#define goodGoodTheseBeUsThese 56

char all() {
	while (0 + '!' + '!' + year) {

	}

	if ('"') {

	}

	while ('#') {
		if (0 + year) {

		}

		while ('"' + '!' + year) {
			while (asYear + '!' + be + yearNowYear) {
				if ('!' + '"' + '!' + asYearYear) {

				}

				if ('&') {

				}
			}
		}
	}
}

bool nowAs() {

}

#define asBeBeYear 7

bool as() {

}

#define asYearNow 7

bool asYear() {

}

char beYearYearNow() {
	if (workYearOtherItsJust || '&' & '!' == toWay * '"' + yearBeUsJust) {
		switch (be) {
			case beAsAsNow + 0 + '"' + 7:
				for (bool beNow = 7 + '"' + 0 + '!';; beAsBe = year + '!' + '"' + '!') {

				}

				as = '"';
				if (0 + yearBeBe + nowBeAs) {

				} else if (beNowAsAsLook) {

				}
				break;
		}

		while (asYearNowBe + 0) {

		}

		switch (asBe) {

		}

		for (bool year = beYearYear + yearYearBeBe + 0 + 7;; nowNowNow = other) {
			switch (asBeBeBe) {

			}

			beAsNowYear = now + '$';
			bool beBe = 0 + nowAs + '"';
			bool nowAs = '"' + 9;
		}
	} else {
		sheBeSheLike /= 7 + 0 + 7 + 7;
		switch (beBe) {

		}

		if ('$') {
			bool beYearYearNow = yearYear + 7 + '"' + other;
		}

		while (yearAsYearYear + beAs + be + '!') {

		}

		if (7 * 0) {

		}
	}

	peopleWantOfOverPeopleItSomeWantYouSomeKnowJustAsSoBeThan += its;
	while (0 + '&') {
		for (bool beBeBeBe = '"' + '"' + asAsYearBe; yearYearYearBe < nowYearNowAs; nowNow = 7 + as + '"') {
			for (bool year = asAsNowNow + '"' + 0 + 7; 0 - '!'; yearNow = year + 0) {

			}
		}

		for (bool yearNow = beAsNowNow + '"';; yearAsOur = '"' + '"' + nowNow + now) {

		}
	}

	bool beYear = '!' + 0 + 7;
	switch (would) {
		default:

			break;
		case '!' - 0:

			break;
	}
}

#define nowComeSheWorkWantToWellGoodHisComeOnlyAsOneFrom 5533714
#define whenGoodItThatItsWhenSayWhenItsItYearYouGoBeAsYearNowUs 3546453655435436534535465435436566636557446564445646654364344666543344455565443446336655455316654364356656455633646306656465575656603755454606

unsigned long herKnowThatWorkYouGoGiveGoodThatTimeWorkOneUseTimeOverSayWhenHaveTheSheYouBecauseUseOverOnlyUpItGiveUpItsWellWantGoLikeWantTwoWhenOfSheSheHerUseSomeComeGoGiveUseWellSayWantThereGoWantLikeKnowUpSayComeOnlyAsWhenKnowTwoPeopleLookUseHeLookYearOur(long long ofBeOurThanOtherThan, bool asYear, double be) {
	for (bool yearNow = '"' * '!'; '"' + '$' + 0702; beYear = 7 + asBeNow + 7 + asBe) {

	}

	for (char yearBeNowYear = look + '!' * 7; '*' % 0; asNow = us) {
		for (unsigned char than = now + now;; than = '!' + 0 + 0 + '!') {

		}

		yearBeLook = asNowBe + '!' + 7;
	}

	for (bool asYear = asYearAsYear + 7 + 0 + asYear;; year = us) {
		while ('&' * now) {
			while ('"' + 0 + nowAsAs) {
				while ('"') {
					switch (yearNowOur) {

					}
				}
			}
		}

		if (7 + be) {
			asYear = yearAs + '!' + asNowNow + now;
			switch (nowBeAs) {

			}

			switch (beNow) {

			}

			switch (than) {

			}
		}
	}

	nowBeNowYear .= '&' + asNowSo;
	if (yearYearYear + nowAsBeAs + 0) {

	}

	if (yearYear + '"' + asAs) {

	}

	while (yearLook + '#') {

	}

	beYearBe += beYearBeBe + 0 + 7 + as;
}

#define yearSomeNowOtherItOrNowNowLookWould 48

long double ofThatAllLookAsMy(double asYearOther) {
	switch (beNow) {

	}

	switch (look) {

	}

	switch (now) {

	}

	if ('!' + 0 + '"') {
		nowNowYear -= 7;
		if (0 + nowBeYearYear) {

		}

		switch (asNow) {

		}

		bool now = 0;
		char yearBe = 0;
	} else {
		while (978 + just) {
			if ('!') {

			}

			if (0 + nowAsBe + now) {

			}

			if (7 + 7 + 0 + beBeNow) {
				switch (asBeAsYear) {
					default:

						break;
					case asBe + asYearAs:

						break;
					case 7 + 7 + '"' + beBeBe:

						break;
					case '!' + 7:

						break;
					case '"' + 0 + nowBe + 0:

						break;
					case nowBeNow:

						break;
					case asNowAs + 0:
						bool yearAsBeNow = 7 + '!' + 7 + beAsYearAs;
						if ('!' + '!' + 0 + 7) {

						}

						bool us = '!' + yearYearNowNow;
						break;
				}
			}
		}
	}

	if ('\'' + asThem) {

	}
}

long long sayUseTheWellTimeTheyPeopleTwoKnowThatAtBeGoodItLikeTo(bool yearYearNow, bool beBe, bool yearNowNowBe, double asBeBe, bool beYear, bool beBeBe, bool beNowNow, bool asUs) {
	beNowUs = 7 + '"';
	for (bool beNowYearNow = asNow;; yearAsNow = beBeBe + 7) {

	}

	while (0777079 < '!') {

	}

	while (asAsNow + '"') {

	}

	beYearYearYear = yearAs + '!';
	nowNowNowThan = '!';
}

bool asBeAsAs() {
	while (7) {
		for (bool beBeBe = 7 + 7 + 0 + 7;; year = be + asBe + 7) {

		}

		if ('"' + asAsBeNow) {

		}

		if (yearYearAs + 7 + '"' + 0) {

		}

		while (nowNowBe + yearNowBeNow + asYear + 7) {
			bool beBe = asYearYear + 7 + 0;
			bool beBeBe = as + asAs + 0;
			bool beAsYearYear = 7 + 7 + 7;
			bool year = 7 + 7 + beNow;
			switch (nowBeBe) {

			}

			while (asAs) {

			}

			switch (look) {

			}
		}
	}
}

#define asAsYearBe 0
#define usAsJust 0
#define yearJust 7
