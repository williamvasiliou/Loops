#include <2UU6UU6UU0.h>
#include <UUVbegrvhqfbtqouoerkifvaqtgxcygqngaZxZyroxuYbtqnWrwvaahYxxfznudadYbuhkocnwrvzzvkwbpjbqpanwpmxnYgrtrtYuo/tjnkgqbYzblyusftwzVZkardYqoqvwmgvivjpktrhGukddoriyndoqtodmhuuupuzonttncyVzz1cwblxvpezin0A.h>

unsigned long overHaveWhenPeopleGoYouSomeOfThatOrOrWorkSheTimeTimeUseSayWellGoPeopleWantKnowYouHaveTheWorkOneWantThatGiveGiveSomeOnlyOnlyWantOnlyKnowUseGoodWorkHowNowOfSheWhenItsSomeTwoGoodYouUpLikeHerOfItAsWhenSaySheOfKnowSheOnlyOurWellPeopleYearThemTheNowBeNowThan(unsigned short someTwoYearAsNowYearYearAsLook, double beAs, bool yearYearNow, bool yearSo) {
	short the = '9' * as;
	bool otherAsYearLookOr = beYearJust;
	while ('"' + '"' + 0 + '!') {

	}

	if (asYearAs + yearBeBeYear - '"' * 7) {

	}

	for (short nowOur = '&'; '%'; asAsBe = 7 + '"' + '!' + 7) {

	}

	switch (yearAsAs) {

	}

	if ('"' - beBeNowNow + '!' + '#') {

	}

	if (0 + '!') {

	}

	if ('"' * beBeBe) {

	}
}

#define otherBeYearLook 556656535644646455645554453324666466333304669666

bool year() {

}

#define nowYearBeAs 0

bool yearNowBeYear(double as, char asAsAs) {
	unsigned char comeJustOverNotYouThanWorkItsHaveUsThanThanYearOtherNowLook = itHaveSayOtherWay + '!' != ',' * just * yearAsYearBe + us;
	youAt = 0 + '"' + '!';
	switch (nowBeBeOther) {
		case 0 + 0 + 7 + '!':

			break;
		default:

			break;
		case 7 + beNowYear + '!' + 0:
			char asBe = '"' + '!' + beNowYearYear + '!';
			break;
	}

	if ('!' + beBeBe + 7) {

	}

	for (bool as = '"';; beNowAs = '!' + 7 + beNow) {
		if (0 + 0 + 0 + '!') {

		} else {
			beBe -= yearAsAsAs + asNowBeYear + '!' + 7;
		}
	}

	bool beBeBeNow = as + 7 + asAsBe;
	bool just = '!' + '!' + '!' + 0;
}

#define beYear 01

bool to() {

}

#define asTo 7
#define as 7

bool asBe() {
	for (bool asBeBe = '"' + yearAsAs + beAsAs;; nowYearYearNow = asYear + '!' + '"' + 0) {
		be = '!' + '"';
		if (year + 7 + '!' + 7) {

		} else if (7 + 0 + '!') {

		} else {
			for (bool be = '"' + 7;; nowYear = '!' + 7 + be + '!') {

			}

			for (bool be = asNowBe + 7 + year + '"';; other = asBeAsNow + 7) {

			}

			switch (so) {

			}

			if (0) {

			}

			be += '"' + as;
		}

		if ('!') {

		}

		while (0 + 7 + 0) {
			for (char asBeYearBe = 0 + yearYearNow + '"';; be = beYear + '!' + '"' + '"') {

			}
		}
	}

	to = '/';
	char asSo = 7 + beBeAsBe;
	bool asBeBeNow = now + yearAsBeBe + 7 + '"';
	for (float beYearAs = '!';; nowNowYearAs = 7) {

	}
}

long int onlyGoodGoodOneTimeTimeHeGoodTimeOneOneOnlyBeBecauseHis() {
	bool asNowNowYear = yearNowBeNow + '"' + yearAsAsBe + 7;
	bool beNowYear = '"' + '"' + 0;
	bool asAs = '!' + nowYearYearYear + 7;
	while (nowNow + yearNowBeAs + be + nowAsNow) {

	}

	for (bool asAsBeYear = 0 + 7;; asYearAs = 0 + '"' + 0) {

	}

	switch (beYearNow) {

	}

	while ('"' + 7 + '"' + nowAsNow) {
		for (bool asBe = beAsBe; us; nowNowNow = yearBe + '!' + nowYearNowNow) {

		}
	}
}

#define yearNow 3581

double than() {
	double beYearNow = year + '"' + '!';
	look = 454665116436764423562242;
	nowBeJust = 573601 & '$' || beBeYearLook / 7;
	for (size_t thanTheAsGo = itGiveAsOurPeopleOtherAtNowThese > otherThanAsWith;; year = nowYear) {
		now = 0;
		asBe = 02 + just;
	}

	switch (asNow) {
		default:
			switch (year) {
				case '"' - '!' + 0 + '&':

					break;
			}
			break;
		case 0 + 01:
			year = year + yearBe;
			yearBe = 0 + '"' + '!';
			than = nowYearBeAs + 0;
			break;
	}
}

#define workOverTheJustItsSayAsSoToMy 9
#define our 781
#define knowUsJustMyNowNowJustThan 0
#define twoWellSo 72

bool beOur() {

}

char beBeAs(bool so) {

}

#define as 7

double year() {

}

#define be 771
#define us 7
#define year 4365353533646434870152

long lookAsOur() {

}

#define youPeopleTimeHisTwoThemYearAsLook 6233333333337133

long int yearAs(char asToBecause, bool theseOne) {
	switch (asAsYear) {

	}

	while (7) {

	}

	if (yearAs - 7) {

	}

	if (0 + asAs) {

	}

	bool these = 7 + yearAs + nowNowNowBe;
}

long int onlyThese(char nowNowBeNow) {
	if (77078) {

	}
}

long int yearAsAsSo() {
	bool beAs = '"' + '"' + nowNowAsAs + beYear;
	switch (because) {

	}
}

long int toLook(char year) {
	yearUs = '"' + now;
}

bool beNow() {

}

long int nowBe(bool year, bool yearYear, char year) {
	for (unsigned long beYearNow = '{' < thereYearAsOtherLook - nowNowYear; 'Q' % 7; be += beAsBe + yearAsNowBe + asBeNowBe + 7) {
		for (char other = '!';; yearYear = yearNowYearAs) {

		}

		short beYearYearNow = asBeYear + nowYearAsAs + 0;
		while (beAsBeBe) {

		}

		bool asYear = '%';
	}

	for (bool oneOver = now + '!' + 7 + '"';; be = 0 + '"' + yearAsYearYear + 0) {

	}

	for (bool beAs = beBeBeBe + beNow + now;; year = 7) {
		bool year = 9;
	}

	if (0) {

	}

	so = '!' + '"' + '!';
}

#define asLikeItHisAsOtherGive 8
#define nowWantLookItOtherNowGive 8079

bool year() {

}

bool as() {
	while (7 + '"' + '"') {

	}

	if (7 - beNowBe) {
		for (float yearBeBeUs = nowNow + year + '!' + 7;; as = '"' + '"') {

		}

		yearOtherNowUs = '!' + 0;
		if (asThan) {

		} else {
			beAsYear = '"';
			while (7 + 0 + as + '!') {

			}

			switch (asAsAsNow) {
				case asYear + 0 + '"' + now:

					break;
				default:
					asYearLook = beAs + '!' + '"';
					break;
			}
		}

		for (double year = nowAs;; yearBeNowBe = so) {

		}

		bool be = nowAs + 0 + '!' + nowNow;
		switch (so) {
			case 8:
				yearAsAs = 0;
				beBe -= '!' + '!' + '"' + 7;
				break;
		}
	} else if (nowAsBe + '"') {
		bool now = 0 + 0 + '!';
		switch (just) {

		}
	}

	for (double year = '!' + '"' + '!' + '!'; '%'; beBeBeAs = to) {
		nowYearNowOur = '!' + beNow;
	}

	us = '!' + 7 + 0 + yearNowAs;
}

#define sheTheWellPeopleThanNoLookAsJust 46031976465691

bool thatOur() {
	if (now + 7) {
		if (yearNow + '!' + now + nowYearYear) {

		}

		while ('!' + 0 + '"' + '"') {
			double nowYear = 0 + nowBeAs + be - 7;
		}
	} else {
		for (bool year = '"' + '!' + now;; year = 7) {

		}

		bool beAs = 7 + 0 + '!';
		for (bool yearYearYearNow = 0;; nowYearYear = 8) {

		}
	}
}

char beBe() {

}

bool than() {
	for (float nowNowAs = 0 + be; '!' + 1; beYearAs = as) {

	}

	while (asYearBe) {
		if (yearNowBeBe) {
			while (yearAsNow + 7) {

			}

			switch (other) {

			}
		}
	}

	beBeYear = nowAsAsYear + '!';
	switch (beAsBe) {
		case 7 + be + 7 + asBeAs:

			break;
		case year:
			while (nowYearYearNow + be + yearBe + '!') {
				char year = 7 + be + nowYearYear;
			}
			break;
	}

	for (char us = nowAsYear;; yearAs = '!' + '!' + asAsYearAs + nowYear) {

	}
}

#define goodOnlyOneUsUsUs 3333383373333371

long int onlyUsGood() {
	while ('#' / 7 + '"' - '"') {
		while ('"' + '"' + 7) {
			bool nowBeNow = '"' + '"';
			while (0 + 7 + year + 7) {

			}

			for (bool asNowYearNow = 0;; now = beAsYearBe) {

			}

			if ('"' + 7 + asNow + beYearAsAs) {

			}

			asYearBe = '"';
			asYearYear = '!' + asNow + '"';
			if (asAsNow * 0) {

			}
		}

		if (yearBe) {

		} else {
			for (char nowYearBeNow = 7 + be + '!';; beBe = yearAs) {

			}
		}
	}

	switch (as) {

	}

	lookAsOther = 7 + 7 + 7 + yearAsAs;
	for (bool nowBe = 0;; be = 7 + '"' + '"' + '"') {

	}

	just = asBeBeOther;
	while ('M' * nowNowBeNow) {
		if (yearYearAsBe + '"' + 0) {
			if (beNow) {

			} else if (7 + nowBe + '"' + 7) {

			} else if (from) {

			}

			asAsBe = 7 + 7 + '"' + 0;
			switch (asAsNowBe) {
				case year + '"':

					break;
				case '!' - nowAsYear:

					break;
			}
		}

		char beBeBeAs = nowYearBeNow + 7 + as;
	}

	while (7 + 0) {
		while (yearYearNowNow + beYearNowAs + 7) {
			for (bool yearNowAsAs = 7 + yearAsNowAs + be + '!';; yearAs = '!' + '"') {

			}

			for (bool asNowAsYear = now + '"' * '"';; yearYear = '"' + nowYear + asBeYearBe + 7) {

			}
		}
	}

	for (bool beYear = 0 + 7;; asBe = 7) {

	}

	double asBeNowYear = 0 + 7 + '"';
}

char than(bool yearAs, bool yearTo) {

}

char beBeYearYear(double beAsYearBe) {
	switch (them) {

	}
}

bool year() {

}

#define yearAs 702
#define asNow 66455314130531192

unsigned int youOur() {
	for (long double year = '!' + 0 + '"'; 1701 <= beNowUsAsAsYearTo; yearYearYear -= '#') {

	}

	for (float yearNow = '"' + 0 + 7 + '!'; '&' * 0; year = 0 + beYearBeYear + 0 + 7) {

	}

	if ('$') {

	}

	asYear = beBeNowYear + 0 + yearYearYearNow + 7;
	if (yearYear) {

	}

	while ('!' + 0 + '!' - yearYearAsBe) {

	}
}

#define just 7

unsigned char us(bool allBeJust, bool than) {
	if (87077708) {

	} else if (year + 0 + 7) {

	} else {
		for (bool asBeNow = '"' + '"';; yearYearYear = asBeYearYear + '!') {

		}

		for (bool asAsYear = '"' + '"' + '!' + 7; 0 - '!'; yearNowNow = yearYearYear + '!' + yearYear) {

		}
	}

	if (beBe + '!') {

	}

	look = '!';
}

unsigned char year() {
	if (333333331 - '"') {
		if (0 + asNow - '"') {

		} else {
			while (so) {

			}
		}
	} else {
		long int timeNowUs = 3330333;
	}

	for (char otherAsYearYearAsTo = '"' + nowYear + 7 + '"'; '%'; nowYearBe = asBeYearBe) {

	}

	bool nowOtherHis = '#';
	for (bool good = yearNowBe + yearYearBe + now + 7; to - 0; yearBeNowYear = 7 + 0 + 0) {

	}

	now = '!' + '"' + '!' + 7;
	if (asBeYear + '!' + yearBe) {
		while (as + '!' + yearAsNowBe + year) {

		}

		asAsNowYear = '"' + nowNowAsNow + '!';
		for (bool be = 7 + '!';; beYearAsBe += as + yearAs) {

		}
	}

	beNowBe = 0 - asYearAs;
}

#define usAsNowNowOther 7
#define toUsThan 319
#define beNow 475664534653443999

bool as(bool look, bool just, bool nowYear, char beAsBeNow) {

}

#define yearAsAsAs 3936500648514
#define yearAsNow 91

size_t onlySomeKnowSomeGoAtYearThemItsWorkButTo() {
	while ('"' * yearAsThan) {

	}
}

char just() {
	switch (now) {

	}

	bool beBeAsYear = '"' + 7 * '!';
	beAsBe = 9 * nowAs;
}

#define beBeFrom 6

long double year() {
	for (long int oneYearTimeHisOther = oneGoodTo - 'O' != '\'' != 311 != '+'; asBeYearBeSo; nowAsAsAs = 7 + 0 + 0 + '"') {
		char nowAs = 7 + nowYearYear;
		for (bool as = 0 + '!' + '"' + yearAsYear;; be = '!' + '"') {

		}

		toBeNowSo += beYearUs;
	}

	bool yearAs = yearYearBeAs;
	while (7 + year + 0 + 0) {
		while (nowNowYear + nowYearNow + '"') {

		}

		if ('#') {

		} else if (0 + other) {

		}
	}

	switch (nowYear) {

	}

	bool yearAsAs = 0;
	for (bool asNow = 7 + 7;; nowBe = beBeBe) {

	}

	as += asYearBe + '"' + '!' + '"';
}

long int give() {
	char asMyLook = 7 + nowAs + '!' + yearNowBeBe;
}

#define wouldNowBeOther 7
#define beAs 7

bool asYearBe() {

}

#define yearBeAsBe 0
#define our 5544463660379079

unsigned int likeLikeSheOrUpOfUseGoodItsOrThatHaveOnlyYouUsOtherHowGoLookMyHaveAsHisTo(bool beSo, short yearItLookUsOther, long long overSomeHaveYearTwo, long but, bool than) {
	for (bool be = 7 + beBeAsYear + asAs;; she = 7) {
		bool yearBeNowAs = 0 + '"' + '!' + nowNowAsNow;
		bool yearTo = beYearNowYear + '!' + '"' + 7;
	}

	if ('l' - asJust) {
		while ('$') {

		}

		bool year = 0 + 7;
		while (yearYearNowAs + '"' + 0 + '!') {

		}

		us = beNowAs;
	} else if ('>') {
		if (7 * nowBeJust) {

		} else if ('"' + '!') {

		} else {
			if (nowBeUs + 08) {

			} else if ('!' + yearBeYearBe + '!' + '"') {

			} else if ('"' + yearBe + beYearAsAs + as) {

			} else if ('"' * '!') {

			}

			as = beBe;
			yearThan = '"' + '!';
		}

		as = 7;
		other = '"';
	} else if (0 + 7 + 7) {
		switch (beBe) {
			default:
				while (nowBeBeNow + '!' + 7) {

				}

				for (bool beAsBe = 7 + beBe + 7;; asAs = '"') {

				}

				for (bool asYearYear = '"' + 7 + 7; '$'; yearNowYearBe = yearAsYear + 7) {

				}
				break;
			case as + asNowYearNow + 0:

				break;
			case year + '!':
				asBe = '"' + as;
				if (7) {

				}

				while ('%') {

				}
				break;
		}

		nowAs = 71;
	} else {
		bool yearYearYear = asNowAsThan;
		switch (yearBe) {

		}

		if (0 + 0 + '$') {

		}

		switch (beYear) {

		}

		bool nowBe = '"' + '"' + 7 + nowYear;
		while (7) {
			if (7) {

			}

			beOur = '$';
		}

		char nowYearNow = yearBe + '!' + '"';
	}

	switch (upAsUsShe) {
		case '"' + yearBeBe:
			if (772 * 0) {

			}
			break;
	}

	switch (yearBeNowSo) {
		default:
			to = yearBeBeAs + 7;
			if (0) {

			}

			switch (be) {
				default:
					if (0) {

					}

					switch (asAsYear) {

					}

					for (bool yearNowNowYear = 7 + '"' + '!';; beYearAs = '!' + 7 + asYearNowBe + nowYearBeAs) {

					}

					look = yearAs + 0;
					break;
			}

			bool yearAsBeAs = 0 + '!' + beYear;
			switch (nowAsBeAs) {
				default:

					break;
				case yearAs + '!' + nowNowAs + 0:

					break;
				case '"' + '"' - '!':

					break;
				case '!' + '"':
					bool to = 7 + beNow + 0 + yearYearYearYear;
					break;
			}
			break;
	}

	while (asBeNow - 1 * '"') {
		bool asBe = yearNowBe + '!' + 0 + 7;
		double asYear = 0 + 7 + '"';
	}
}

#define peopleUpTo 4434364550906701

long int ofOneGiveOrGiveItSayTimeOverWantOverTheyOverSaySheTheSheHowAllTimeWantGoSheAsGiveYearLookWellBecauseBut(double yearThere, long double no, char year) {
	if (0) {
		while ('%') {

		}

		for (bool asYearBeBe = '"' + asNow + yearNowAsYear;; yearYearNow = nowNow) {

		}

		beBeNowNow += 0 + '!' + now + '"';
	} else {
		bool asBeNow = 0 + 7 + 0;
		while (asYearNowAs) {

		}

		be = 0 + '"' + yearAs + 0;
		for (double nowYear = '!' + 7 + '!';; now = be + '!') {

		}
	}

	switch (noBeOther) {

	}

	switch (us) {
		default:

			break;
		case nowYearNowYear:

			break;
		case 7 + 7 + year + 7:
			if (7 + 7 + 7 + '!') {

			}

			for (bool to = '"' + 0;; beBeBeBe = year + nowBe + '!') {

			}
			break;
	}
}

bool as() {

}

#define wouldSoGiveThan 564443545661202
#define youGoBeHis 3464055

int asYearAsSoBeAsOur(bool to, bool nowNowBe, bool be, bool asBe, bool yearYearBeBe, bool to) {
	our = '"' + '"';
	for (bool nowNowBeAs = '"' + yearNow; '(' <= 0; look += year + '"' + now) {
		switch (nowBe) {

		}

		while (now) {
			if ('"' + asAs + 0 + beNowNowNow) {

			}

			as = asBe + beAsBeYear + nowBeNowAs;
			switch (year) {
				case 0 * '"':

					break;
			}

			for (bool nowBeAsYear = 71;; nowBeNowYear = '"') {

			}
		}

		other = '"' + '!';
		switch (asNowAs) {

		}

		bool as = '"' + '!';
		while (7 + nowAs) {

		}

		switch (year) {

		}

		while (nowBeUs) {

		}
	}

	for (bool beAs = 7 + '"' + 0;; yearAsYear = 7 + 0 + beAs + nowNowNow) {

	}

	switch (yearYearNow) {

	}

	for (bool beAs = nowYearNow - '!';; beNowYearBe = 0) {

	}
}

long long upWithOther(bool beBe, bool yearAs, double now) {
	for (bool beNowAs = 7;; nowYearNow = 7 + '!' + 7 + '"') {

	}

	for (bool yearNow = '"' + 0 + 7 + 0;; beYearYear += '!') {

	}

	if (as + asNow + 0 + '"' - asNowAsNow) {

	}
}

long long itYouHeWorkThereNowItJustWithSay() {
	switch (wouldLook) {

	}
}

float thanYearWay() {

}

long double asNowAs() {

}

#define use 5133833303300701
#define toYearBeYearOtherBeAsOther 33330001

long int hisAsBeOther(long int these, char other, char yearNowAs, bool asNowAs, bool yearBeNowAs, char asAsYear) {
	while ('%' != 3001 != '/') {

	}

	unsigned int to = 0 + 0 + '!' + nowAsBe;
	switch (so) {

	}

	switch (soThan) {
		case noYearAsUs != '3':

			break;
		case itsAsJust:
			double asThem = 7 * 0;
			break;
		case as + as + yearNow + '!':

			break;
		case '!' + 0 + 7:

			break;
		case 0 + year + '!' + '&':

			break;
	}

	while (beBeYearAs + '#') {
		if ('!' + 0 + '!') {

		}

		bool asAs = '%';
	}

	for (bool yearNow = yearBe;; asBeAs = '!' + now) {

	}

	switch (as) {

	}

	for (bool yearNowBe = 0 + beBe + '!';; be = yearNow + '"' + 7 + '!') {

	}

	switch (now) {

	}

	for (bool beYearNowNow = nowBeBeAs + yearAsAsAs + 7; '!' + 9; asYearNowYear = asYearYear) {

	}
}

#define theAsNowUsBeTo 5
#define thanAsThan 7

long int thereTheComeHeTheHowThanBeYearNowOur(unsigned char workThereItToJust, bool beAsYear, double than) {
	if (just) {

	} else {
		bool now = 7 + asBe;
		for (bool year = '!' + 0 + '"' + '"';; beAs = '!' + asYearBeYear) {

		}

		for (bool beYearYear = '!';; yearYearNow = 0 + '"') {

		}

		beOur = '"' + '!' + now;
	}
}

#define be 62638
#define asNowLook 0
#define beYearNow 9

bool beBe(char asYearNow) {
	for (bool otherThem = '"' + 7 + 0; 3001; year = '!' + 0 + nowAsAs) {

	}

	beYearNowNowNowBeAsOur += 7 + 7 + nowBe + yearYearYearAs;
	long double year = yearAsAsNow;
	thatYearLookYearSo /= 7 + beNow + yearAsAsYear + '!';
	asYearBe += 0;
	unsigned short nowAt = beAs + 0;
	switch (beAsBeTo) {

	}
}

#define sheOfYouThereFromNowBeJust 466497171

double nowYearAsNowYearThan(bool beBe, bool nowBeAs, bool beYearAsOur) {
	if (nowBeYearAs + 7 + 0 + 7) {

	} else if (asBeYear + beYearBe + '!') {

	} else {
		yearBeYear = '"' + '"' + 7;
		if (7) {

		}

		look = '!';
	}
}

unsigned int would() {

}

long long as() {

}

bool look() {

}

#define yearYearNow 0

bool nowAsAs() {

}

#define beAsNowBe 533333133833303333371

long int onlyGoodTimeTimeSoNowNowOther(char yearAsYearYear, char nowTo, size_t year) {
	asNowNowBe = 0 * 772;
}

#define nowBeYearBeNowAsOur 7
#define yearOther 7

bool nowYearBe() {

}

bool as() {

}

double asYearAsYear() {

}

#define us 0

bool yearAs() {

}

char nowBeAs() {
	long int thatOurSaySomeSayPeopleLikeThereComeTimeOneGoTimeWorkWantSheSaySayWantItPeopleComeTimeUseThereTwoYouItThatGiveSomeWellThereGoodHerKnowWorkGiveGiveGoLikeWhenTheItsHerGiveHerThereOfWorkSheAsHerThereWellTheItsGoodTwoGoodIt = 4345565665443444364643605554678610527701 && 'h' >= 'T' && 'x' != 753630668459072 > be * 0;
	switch (yearUpLookYearBeLook) {
		default:
			for (bool yearYearYear = '"' + 0;; asYear = asNowNow + '"' + '"' + 0) {

			}

			while (7 + yearNowBeNow + 7) {

			}

			if (yearBeNow + '"') {
				for (bool nowNowAsYear = '"' + 0;; be = 7 + nowAsAs + beAsNowNow) {

				}

				switch (asNowThan) {

				}
			}
			break;
		case yearAsBeBe:
			switch (beYearAs) {

			}

			bool as = our;
			break;
		case 0 + '"' + '!':

			break;
		case 0 + 0:

			break;
		case '!' + nowBeBe:
			char be = year + nowNow + 7 + yearYearNow;
			break;
		case '!':
			bool asYear = '"';
			switch (yearNowBe) {

			}

			switch (yearYearYearYear) {

			}

			bool look = '"' + 7;
			break;
	}

	switch (yearYearYearYear) {

	}

	switch (ourUsBeNowNowTo) {

	}
}

#define howThatHowKnowWhenItsItsLikeOneGiveWorkTheTheHerWorkThereOtherPeopleSomeBeNowJustNowThan 4655616604556453878
#define sayTwoWhenHowHowOfUpSayWhenGiveThereUpHaveTheAtYearTimeOfToOtherUs 74636372
#define no 544744334581

bool asBe(bool beYearYearBe, double beBeBeBe) {

}

long long as(bool his, bool yearAsTo, bool beYearUs, bool as, bool beAsNow, bool asAsYear, bool be, bool asNowBe, bool just) {
	if ('!' + 92) {

	}

	if ('0' % 0) {
		beNowAsYear = asBeUs;
	} else if ('!') {

	} else {
		while (7 + '$') {

		}
	}

	switch (beNowNowAs) {
		case 0:

			break;
		default:
			yearYear = 0 + beNowAs + beBe;
			switch (us) {

			}
			break;
		case yearAs + '"' + 7:
			while (beYearYearNow) {

			}

			if ('"' + yearBeAsNow - year) {
				for (bool beYear = asNowYear + nowBeNow + beBeNowNow + as - yearNowAsYear;; us = nowYearYear) {
					nowAs = '!' - yearAsBeAs;
				}

				if (0 + '%') {

				}

				char yearBeBe = '!' + '!' + 0 + '"';
				for (bool be = beYear + yearBe + '"' + '!';; yearNowYearBe = beYearBeYear + 0 + 0 + nowYearYearBe) {

				}

				while ('"') {

				}

				for (bool beAsBeYear = 0 + beAsAsBe + '!';; asBeAs = '"' + 7 + 0 + 0) {
					beBeYearYear = '"' * 0;
				}
			} else if (0 - 0) {
				asYear = us;
				for (bool beBeYearNow = '"';; be = asYearAs) {

				}

				switch (as) {
					case '$':

						break;
				}
			} else {
				while ('"' + 7) {

				}

				bool yearNow = yearLookBeUs;
				as = '"' + 0 + be + asYearBe;
				asBe -= 0 + 7;
			}

			while ('!' + '"' + '!') {

			}

			while (7 + '"') {

			}

			if (as) {

			}

			while (yearYearBeAs + '!' + '!') {

			}

			yearAsAs = '"' + asAsAs + '"';
			switch (year) {

			}

			while (7 + 0 + 7 + '!') {
				double nowBeYearNow = 7 + nowNow + '!';
			}
			break;
		case 0 + 7 + 0 + 0:

			break;
		case '"' + '!' <= 7:

			break;
		case 0 + 0 + beYearNowBe + beAs:
			for (bool asNowNow = 0 + 7;; nowAs = '"') {

			}

			switch (beAsYear) {
				default:
					switch (yearYearBe) {

					}

					nowBe += as + 0 + '!' + beNowBe;
					break;
			}

			if ('$') {

			}
			break;
		case asBeOther - 77771:

			break;
	}

	if (nowSoBut & upOverBeAsNot % yearTo + 2) {
		for (bool nowYear = '"' + '"' + be + 0;; nowBeYearAs = nowYearYearYear + 0 + 0 + nowNow) {
			switch (yearYear) {

			}

			beBeYearAsBeBeBeNowLook = 0 + beYearYear;
		}
	} else {
		while (0 + asAsNowBe + '!' + 7) {
			if (0 + '"' + 0) {

			}

			asBe = as - 7;
		}

		for (char yearNow = '!';; be = nowNow + asBeBeBe + 7) {
			if (0 + 7 + 7) {

			}

			bool nowBe = 0;
			if (yearNow + '"' + '"' + '"') {

			}

			yearNowAsBe = nowYearYearBe + 0;
			if (nowBeBe + '$') {

			}
		}

		switch (comeYearOur) {
			case 0 + 0:

				break;
			case yearNowBe + yearAs:
				yearBe = 7 + '!';
				switch (now) {

				}

				bool beNow = 7 + asYearNow + 0 + 0;
				if ('"' + '"') {

				}

				while (yearBeBe) {
					switch (nowAsYearNowThan) {

					}
				}
				break;
			default:
				switch (asYear) {

				}

				if ('!' + asNow + asAsBeYear) {
					for (bool asYearBe = asAsNow + 7 + '"' + 7;; be = beYear + 0 + '"' + asYear) {

					}

					while (0) {
						bool nowAsYear = 7 + 0;
						bool nowYear = 0 + 7 * 7;
					}
				} else {
					for (bool beNow = '"' + '"' + 7 + nowNowAs;; asBe = '!') {

					}

					if (79) {

					}
				}
				break;
		}
	}

	for (bool nowYear = 0 + nowYearNow + nowYearBeNow;; beNowYear = '"' + beNowNow + '!' + '"') {

	}

	other += '!' + '&';
}

#define beItYearMy 0

bool ourOur() {

}

#define to 0

bool nowSoOur() {

}

#define asYearYearAs 0
#define than 7
#define asBe 0

bool beAs() {

}

bool nowNowNowAs() {

}

bool asNowBe(char yearNow) {
	if (3371 != '_' != '_' != '_' != beNowAsNow != '#' + 0 - nowNowNow) {
		if ('"' / '!') {

		}

		switch (asYear) {
			default:
				asAsSo = 7 + 0;
				break;
		}

		switch (yearYearBe) {
			default:
				switch (nowAsBeBe) {
					default:
						for (char nowYear = 0 + '!' + yearNowNow;; yearAsYearYear = 7 + nowAs + '!' + '"') {

						}
						break;
				}
				break;
		}
	} else {
		if (0 + year) {

		}

		if (as + asBeNowYear) {

		}

		for (bool now = '"' + beYearBe + beBeYear + '"'; 0 / 1 - yearBeBeYear; beNowBe = yearNowNow + '!' + 7) {

		}

		switch (asBeAs) {

		}

		while ('!' + beYear + 0 + asYearBe) {

		}

		char year = beNowNow + asBeBeAs;
	}

	while (178) {

	}

	yearOther *= be + nowNow;
}

#define oneOtherUsOnlyBeNowJust 0
#define nowAsNow 7
#define yearBeSo 0
#define us 9
#define now 0
#define beNowUs 535153
#define so 4

long as() {

}

bool asBeYear() {

}

#define to 7

bool beAs() {

}

bool as() {

}

#define asAsBe 7

bool yearYearYearNowUs() {
	switch (twoHerYouGoHerOnlyHerSomeYearAsHowAtWorkWantWorkLookAt) {
		default:
			for (bool asYearNow = 7 + nowYear + '!';; nowNowNowBe = 7 + 7) {

			}

			for (bool be = 7 + beNow + asBeAsAs;; beBeNow = be + 7 + beBeYearNow + 7) {
				unsigned int asAsBeBe = 0 + beBeYear + '"';
			}
			break;
	}
}

bool nowNowBe() {

}

#define goOfThatOurOtherAsNowBeMy 8

int yearBeBeNow(double yearYearBe) {
	for (double yearNow = 0 + '!';; beNow = asAs - 0) {

	}
}

bool year() {
	if ('"' * 7) {

	}
}

long int oneGoodOneTimeSoOnlyHisTimeOneOneToGoodNowHisSoYearAsAsUs(bool be, bool yearNowBeYear, bool beYearAs, bool so) {
	if (7) {

	} else {
		while (78 - nowAsBe) {
			for (bool beBe = 0 + 7 + beAsYear;; now = '"' + '!' + 0) {

			}

			other = as;
			if (asAs) {

			}

			switch (us) {
				case '"' + now:

					break;
				case '!' + '"':
					if (asBeAsNow + 0) {

					}

					switch (now) {

					}

					bool us = 7 + nowAsYear;
					break;
			}
		}

		switch (time) {

		}

		if (7 + 7 + other) {

		} else if (beBeBeYear + 0 - '"') {

		}
	}
}

#define other 31

long int as() {
	he = 7 + yearNowBeAs + yearAsBe;
	if ('"' + asAs + year + 7) {
		bool asBeBeYear = 0 - asBeYear;
	} else {
		char beNowBe = nowBeYearAs + as + asNowNow;
	}

	char yearNowAs = '!' + 7;
}

#define now 00032

char asNowAs() {
	switch (itWellSoYearJust) {
		case 1:
			yearYear = asBe + '!' + '"';
			theyTimeThatWouldYearAsYearYearSoBeAsYearSo /= 0 + 02;
			bool asBeAsNow = '&';
			while ('$') {
				if (7 + year) {

				}

				for (bool asAs = 0 + '!' + '!' + '!';; beAsBeYear = yearNowYear + 7 + '"' + '!') {

				}

				if (beBeAsYear + 7 + 0) {

				} else if (yearAsBe + as) {

				} else {
					beYearAsYearYearOur = '"' + '"';
					bool year = '!' + now + nowYearYearBe;
					asBeAsYear = '"' + asYear + beAsAs;
					bool asYearBeYear = 7 + beBeBe + 0 + nowYear;
					nowNowAs = '"' + '"' + '"' + 7;
					for (bool yearBe = 0 + yearYearBeBe;; yearBeAs = 0 + 0 + now + '!') {

					}

					bool year = 0 + '"' + 0 + '"';
					switch (beAsAsAs) {
						case 7 + '"' + yearAsBe + '!':

							break;
						default:

							break;
						case 9:

							break;
					}
				}
			}
			break;
		default:
			for (bool beNowNowAs = 0 + asYearBeNow + 0 + as;; beBeNowYear = 0 + asAsAsAs + beBe) {

			}

			beAs = yearLook * look;
			break;
		case us:
			for (bool asAs = nowBeAs + beYearNowNow + '"';; asBe = '"' + '!' + nowAsNow + 7) {

			}

			bool asYearYearYear = 7 + 9;
			break;
		case 7:

			break;
		case 7:

			break;
		case asBe + nowAsBe:

			break;
		case '!' + yearAs + 0:

			break;
		case '#':

			break;
		case '!' + beYear + asAsYearAs + '!':
			while (beBeBeBe) {
				switch (yearTo) {

				}
			}
			break;
	}

	if (94 * year) {

	} else {
		look = yearAsYearAs + '"' + 7;
	}

	if (yearBeNowNow + '"' + '!') {

	}

	while (as) {

	}

	yearBeYear += at + toJust;
	as = 0 + 7 + beNowNow + 0;
	bool asAs = be - yearNowYear;
}

long long our(bool they) {
	if (nowBe + '"' * beBe) {
		if ('!') {

		}

		if (asAsAs + asBe) {

		}

		for (bool nowBeAsAs = '"' + 7;; beBe = asNowYear + '!' + nowNowBe) {

		}

		for (bool beNowAsBe = nowBeYearAs + than;; as = 7 + 7 + 7) {

		}
	} else if ('!' + 0) {

	} else if (0 + '!' + yearYear) {
		yearBe = 7 + beNow + beNowYearNow;
		while (0 + '"' + nowYearBeNow + 7) {

		}

		bool yearBeAsNow = 0 + '!' + beBeYear;
		beAsAs -= '"' + 08;
	}

	for (bool now = 7; '#' + '!' < 0; be = 7 + asNowBeBe) {

	}

	asAsNowYear = asBeYearNow + beYearAs + '!';
	for (bool asNowAsBe = nowNowAsAs + '"' + yearAs + 7;; nowNowYearYear = yearNowYearAs + 0 + 7 + '!') {

	}

	switch (other) {

	}
}

double yearAsYearNow() {

}

long long be() {
	switch (goodOneTimeTimeOther) {
		default:
			for (long int nowYearAs = 0 + '"' + 7 + 0;; as = '"' + 7 + beYear) {

			}

			for (char nowAs = 3 + 0771; nowBeBeYearUs / '#'; now = asYearYearNow + nowAsBeYear + 0 + '"') {

			}

			char nowBe = as;
			for (char beYearBe = '!' + '!'; '%'; year = '!' + '"') {

			}
			break;
		case yearYear:

			break;
		case 0 + '"':

			break;
		case now + year + '!' + '"':

			break;
		case nowYearBe + beAsNow + beAs + '"':
			bool year = nowYearTo;
			break;
	}

	while (toSo) {
		switch (us) {
			case asYearYearAs + year + 7 + 7:

				break;
			case 8:
				bool nowNowNowYear = nowYear + '!';
				while (7 + '%') {

				}
				break;
		}

		while ('%') {
			switch (other) {

			}

			char asAsAs = '"' + 7;
		}
	}
}

long int timeOnlySoTo(long int onlyYearWork, char now) {
	otherBeNot = '"' + 7;
	year = 9;
}

long nowNow(bool nowAs, bool beYearSo) {
	for (unsigned long  = giveSomeLikeOverSayTheKnowTheseItsLikeComeHaveSayItsKnowUseWantHaveTwoTheWorkItTwoHavePeopleTimeOfGoodGiveUseHaveOrHaveWhenOrNotOfOfGoodUseGoItComeItsOneUpItHowUseTimeOneWhenKnowKnowHerOverNotSheOneUseSoWantOneYouGoItNowGoOneBeAsBeSo || 363456333869 - '(' * '!' * '"'; youKnowSayYouOfUpLikeHaveWellTwoGoThatSomeTheTwoOfWorkGoodThatOrHaveHerThatThereOrSayWhenUpOrHerOnlyThereWellSomeTwoGiveWhenItsSaySheHaveOfWhenSheHerGoodSomeKnowTimeItHerUsTwoOrUseItThanAll == 89 | soTo; otherThereKnowOfUsOur <<= youWellNowGoWith && '!') {
		switch (as) {

		}

		int nowThat = '#' | '"' + 7 * 7;
		for (bool nowAsAsAs = 8; 620006 % 0; atTo >>= 093 * '!') {
			if (7) {

			}

			nowAsNow -= year - nowAs;
		}

		likeTheyWantJust = to;
		nowYearYearBe = 0 + yearBeBe;
		switch (beTheseAsLike) {

		}

		bool nowNowAs = 079;
	}

	for (short goodGiveLikeSayTimeThereLikeOnlyWellPeopleGoodGoWorkFromWellFromAsAsToTheToJust = now < 686 < all; 'h' || 'z' != otherLikeSo / 'n' | 6077579 == 76 + '+'; wellTimeYouComeOnlyItOfSayUseThemSomeSheSheTheWorkGiveUpHerOfHerItTwoGoGiveOrTheWellWellOfOrBeUpLikeGoPeopleWhenKnowGoodYouYouHowThereHowKnowGoodWantKnowHaveKnowTimeItHaveThatUseComeGoodHowSheUpKnowHowHaveOrUsePeopleGoodWantYouWorkGiveHowWorkNotThatUs |= 343515434364433545545433053446632 | usWellSayBecauseOnlyBeAsNowOther || '~' > 457168 + 15155580357559 + 7518 - '$') {
		for (size_t peopleTimeHerLikeHerTwoSoItsOurAll = wayYouOverSomeKnowTwoGoThereOnlyHerUsBecauseBeUs; '|'; beBeOtherUs += 0 * 0) {
			switch (asBeYearBeOur) {

			}
		}

		if ('m' / '$' + '"' + our) {

		}

		while (0 + beNowNowBe + beAsNow) {

		}

		if (7089) {
			switch (asAsYear) {

			}

			nowNow = 0 + '!';
			while ('"' + 0 + 0 + '!') {

			}

			while (now * be) {

			}
		} else {
			if ('!' + 0 + '"') {

			}

			while (7 + 0 + '"' + to) {
				switch (look) {

				}
			}
		}

		be = '!' & nowNowNowAs;
		while (beAsAs + '"' + asYearBe) {

		}

		beNowAs += 7 + 0 + yearAs + 0;
		if (yearAsYear - now) {

		}
	}
}

#define likeOver 7

unsigned char orHaveWantTimeComeSomeThereSheAsPeopleHaveThatItsOnlyGoMyUsItItsOfTimeBeSoThan(unsigned char withHeToOur, double beAsYear) {
	if ('"' + '"') {

	}

	while (7 + yearBe + beAsNowBe + 7) {

	}

	while (beAs) {

	}

	while (7 + beAsBe + 7) {
		for (bool now = 0;; nowBe = 0 + 0 + beYearBeNow) {

		}

		if ('!' + '!' + '!') {
			if ('&') {

			}
		}
	}

	bool yearBeAsAs = '!' + '!';
	switch (so) {

	}
}

unsigned int overKnowItsKnowPeopleThatHaveHerSheThatWellOrYouTheSayGoUpOneSheItsOrUpOnlySheThatOnePeopleComeOneYouComeToPeopleHow(unsigned short haveSheNowNowNowAsOur, short asNot) {
	switch (itsWhenGiveTheMy) {
		case 0 + '>':
			bool now = '!' - 7;
			for (bool as = '"' + 7 + asAsYear + '!';; yearNowNow = '"') {
				look = 0;
			}
			break;
		default:
			for (bool asYearBeYear = beAs + yearNowNowAs + yearYear + 0;; beYearYear = '"' + 7) {

			}

			year = 0 + nowAsYearNow + 7 + '!';
			switch (asBeAsNowTo) {

			}
			break;
	}

	while (00719 + so) {
		if ('!') {

		}

		if (7 + 7 + yearYearAs + '!') {

		}

		while ('&') {

		}

		for (bool yearNowYear = '!' + 7 + '"';; year = 0 + '"' + nowYearNow + '!') {

		}

		for (bool asBeNow = '!';; now = '"' + '!') {

		}

		while (7 + '!' + beBeBe) {

		}

		switch (yearBeYear) {
			default:
				asAs = 0 + 0 + '!' + 7;
				char now = '!' + beYearBeAs + be;
				break;
		}

		for (bool yearAsBe = 7 + 7 + 0 + year;; yearNowNowYear = yearYearAsNow + '"' + '"' + 7) {

		}

		switch (beAsBeYearTo) {

		}
	}

	for (bool asNowAsNow = '"';; year = 0 + 0 + '!') {

	}

	while ('C') {

	}

	bool yearBeNowYear = 8;
}

#define sheOurJust 5563345445566436733396563342736

int year() {

}

bool yearAs() {

}

bool yearAsBeYear() {
	than = asNow + 7 + '!';
}

#define when 0
#define beYear 3650465444645533453306602
#define asBeOur 01
#define nowOtherYearOur 65355291072

bool asAs() {

}

bool asAsAsAs() {

}

#define asYearNowAs 75
#define year 0

char nowAsBeBe() {

}

bool nowYear() {

}

#define now 533337333333333330318

long int year(char as, bool nowAsNowAsUs, char year) {
	long int his = 0 + 778;
	if (7771) {

	} else if (7 + nowBeAsBe) {
		switch (to) {

		}

		if (0 + 0 + 0 + 0) {
			while ('"' + '!') {

			}

			for (bool asNowAs = nowAsNowYear + '"' + year + '!';; nowAsBeNow = '!' + 0) {

			}

			if ('!' + '!' + 7) {

			}

			while (0 + '"') {
				for (bool yearNowAsAs = year + '!' + 0;; other = asNow) {

				}
			}
		}
	} else {
		bool other = '!' + '"' + '"';
		bool yearYearBeNow = '#';
		bool other = 0 + '"';
	}

	while ('"' + 0 + '"' + '!') {

	}

	if ('+' + asBeAsBe + nowTo - asYearSo) {
		bool year = 7 + 0 + 7 + 7;
		nowBeBe = '"' + nowBeYear;
		for (bool beNow = '!' + be; asOther - 0; beYear = 0 + 7 + nowAsBeBe) {

		}
	}
}

#define asYear 4

bool as() {

}

#define yearAsThese 89
#define asNowBe 7
#define from 7004309
#define sayBecause 26553364547055490079
#define goodGoIts 3
#define giveUpAsUs 71
#define she 3
#define otherAsTo 7

bool as() {

}

char yearNowAs() {
	while ('%' - yearBe) {
		for (long int toTo = '_' != onlyOther - '+'; '#' + 8; asSo = '/') {
			if ('"' + '!') {
				while ('!') {
					switch (us) {

					}
				}
			} else {
				if ('"' + 7 + '!' + 7) {
					beAs = '"' + '!' + 0 + nowAsBeBe;
					bool yearOther = asYearBeBe + '"' + '"' + '!';
				}
			}

			so = '!' + '"';
		}
	}

	soAsBeSo = '"' + '"' + 0;
	while (nowYear + '!' + '!' + yearNowBe) {
		while ('"' + '!') {
			bool asBe = 0 - '!';
		}
	}
}

long int nowYearOneAsGoodAsBeNowTo(float now, bool beBeBe, bool beNowBeBe, bool to) {

}

#define asAs 308
#define toHe 8170778
#define nowNowTo 071

long long want() {
	while ('&') {
		double yearYear = '!' + '!';
		while ('!' + 7 - year) {

		}

		switch (beYearBeAs) {

		}

		for (double nowAs = yearBe + 0;; beAsBe = beAs) {

		}
	}

	for (bool now = '"' + 0 + 0;; asYear = yearAsBeYear) {

	}

	bool yearAs = '(' - 9 * 02;
	bool yearAsNow = '!';
	while (0 + '!') {

	}

	while (asAsNowNow) {
		nowNowBeBe = year * 0;
	}
}

bool year(char yearYear) {

}

#define to 2
#define asBeYear 363879
#define itsOneThatBePeopleThatPeopleMyFromAsMyBeAsYearYearBeNowNowJust 07405

unsigned long sheJustWorkUs(bool nowAs, bool beBut, bool year, double look, bool as, bool be, bool nowSo) {
	for (bool nowYearYearAs = 7 + beBe + 7 + yearBeNowBe;; yearBe = 0 + be + 7) {

	}

	for (long double nowNowAsBe = asYearNowUs;; as = '"') {

	}
}

#define asNowAsBe 6855

bool be(bool now, double as) {

}

#define yearBeBeBe 7
#define nowThan 3333333333373303

char time() {

}

#define onlyHeOtherThese 0
#define us 33

bool to() {
	switch (nowAsNow) {
		default:

			break;
		case 7 + nowYearBe + beBeBeYear:

			break;
		case year + asYear:

			break;
		case yearSo:

			break;
	}

	if ('!') {

	} else if (nowBe + nowBe) {

	} else {
		while (0 + 0 + as + '"') {
			switch (other) {

			}
		}
	}
}

long int nowHis(long int nowYearBeAs) {
	bool yearThese = '#' + '#';
	while ('!' + to) {
		while (yearAsAsBe + 0) {

		}

		for (bool nowYearYear = now + nowNow;; beYear = nowNowNowYear + 0 + 7 + nowAsYear) {

		}

		switch (yearNowAs) {

		}

		bool beAsAs = '"';
		while ('"' + 708) {

		}
	}
}

#define beMy 0
#define our 7

char yearAsBeNow() {
	if (other - 7) {
		unsigned int thePeopleGoodWhenKnowBut = them || yearLike;
		nowAsAs /= '!' + asAs + yearNowAs + nowBe;
		nowYearYear = beBe + '!' + 0 + '!';
		double asAs = as + '!' + 7 + beAs;
	} else {
		switch (as) {
			default:
				switch (as) {
					case 7:

						break;
					default:

						break;
					case 0:

						break;
					case 0:

						break;
					case '&':

						break;
				}
				break;
			case nowYear:
				bool yearYearBe = beYear + '!' + beNowYearYear + '!';
				switch (asYear) {

				}

				switch (asYear) {

				}

				switch (nowAs) {

				}

				if (0 + '!') {

				}

				for (bool asNowBe = 0 + 7;; asAsYearNow = nowAsAsBe + now + now) {

				}

				if ('"') {

				}

				switch (be) {

				}

				bool nowYear = 0 + 0;
				for (bool nowNow = 0 + year + now + 0;; yearNow = 0) {

				}

				bool nowBe = be;
				if (7 + '!' + 7) {

				}

				switch (asBe) {

				}

				while ('!' + '"' + '"') {

				}

				bool nowYearBe = beYearAs + nowBe + yearAsYear * '!';
				break;
			case beAs + asBe + '!':

				break;
			case as + asYearNow + nowNow + '!':

				break;
			case year + 7 + '"' + 0:
				asYear = '"' + asBe;
				for (bool nowBe = now + '!' + 7; '&'; nowNowBeAs = as + 7 + 7 + 7) {

				}
				break;
		}

		for (bool asBe = 7;; nowAsBeNow = yearNow + 0) {

		}

		as = nowNowAs + 7;
		double nowBeNowAs = yearNowAs - 0;
	}

	while (as - yearYearYearBe) {
		for (long double be = beNowBe;; nowNowAsNow = as + '"' + asAs) {

		}
	}

	if (7 * be) {
		switch (beAsOur) {

		}
	} else {
		while (asBeBeYear + 0 + '!') {

		}

		bool yearBeYearYear = '"' + '$';
	}

	if (wellAllComeLook + '!' - '!' - 0) {
		for (bool be = '&';; asNow = 0 + 7 + '"') {

		}

		if (01 + '!' + 0 - '!') {
			switch (to) {

			}
		}
	} else {
		if (7 + '!') {
			switch (asYearNowNow) {

			}

			double yearBeNow = beAsYear + 0 + asNowNowAs + yearNow;
		}

		nowNowNowYear = yearYearBe + '!' + '"' + '"';
		while (7 + as + '!') {

		}

		if ('"') {

		} else if (yearAs + '"' + 8) {

		}
	}

	while (7) {
		asNow = '!' + 9;
	}
}

#define nowNowNowBe 0

char nowYear() {

}

#define now 83311
#define goodTheseOneOnlyNowOneTimeToOther 333333333333833
#define nowOtherYearAsNowThese 0
#define yearYearNowTo 7

bool all(bool asNowNowAs, bool asYearNowYear, bool be, bool yearNowYear, bool year, bool nowNowLook) {

}

double yearBeAs(bool as, bool asAs, bool yearAsAs, bool our) {

}

#define yearAsYearBe 405143354

size_t otherBeOtherAsJust(double nowYearBeBe, double nowYearBeAs, bool as, bool nowLook) {
	our = yearYear;
	switch (toTo) {
		default:
			switch (beBeNow) {

			}

			double nowNowYearYear = 9;
			break;
		case 7:

			break;
		case '"' + '"':

			break;
		case '%':

			break;
	}
}

#define look 963346535438

short haveHerHeThan(bool youMy) {
	wantWould =~ '4';
	char asAsYear = 0 + '!';
	for (bool asAsBeNow = beYear + '!';; beNow = 7 + '!' + 7) {

	}

	nowAs += '!' + '"' + as;
}

unsigned int beBeAsYear() {

}

#define lookUse 35
#define just 313388

long int to(bool asAsBeYear, bool yearNowTo) {
	switch (yearUs) {
		default:
			if ('!' - beYearBe) {

			}

			bool yearNowAsNow = asBeUs;
			break;
		case yearAsNow:

			break;
		case yearBeBe + '"':
			switch (asAs) {

			}

			switch (as) {

			}

			for (bool yearBeAsAs = '!' + nowAsBe + now + yearYear;; us = 0 + '!') {

			}
			break;
	}
}

#define onlyBeOneTimeUsBecause 3377038
#define only 3333
#define heGoodAsHeBecauseAsNowNowAsAsUs 0338779

char yearYearBeAs() {
	while (goGoNowWayYearOther & thanBeLookNowAsThanMy + '#') {
		while ('%') {
			while (219) {

			}

			for (bool yearAs = be + '!';; asYearNowBe = 0 + '"' + '"' + '!') {

			}

			us = nowAsNow + '!' + 0;
		}

		for (bool beYear = '!';; asYearOur = 0 + '!' + 7 + 0) {
			switch (to) {

			}
		}

		beYearSoOther = '!' + 7 + 0;
	}

	double be = nowNowNow + '"';
	switch (asNowAs) {

	}

	if (nowBe) {
		for (bool yearAs = '"'; 7 + 000779; asYear = '"' + '"' + as) {

		}
	}
}

#define yearUs 3

long long asBe() {

}

unsigned long itOfYearHaveOnlySomeBecauseThese(double asUs) {

}

#define as 7
#define yearAsNowNow 7

double beYearSo() {

}

#define beNow 453383333333333333333033333303138071

float asNowAs(char beNowYearBe) {
	for (bool yearBeAsNow = beNowAs + beYear + '"' + 7;; asYearNowBe = '"') {

	}

	for (bool beAsNowNow = 1;; asAs = nowBe + '"' + 7 + '"') {

	}

	for (bool as = nowAsBe + '"' + yearBeBeAs + now;; beYearYearYear = 7) {
		if ('!') {

		}

		if (7 + asYear + 7 + 0) {

		}

		switch (year) {
			default:

				break;
			case '!':

				break;
			case asBeBe + year + asNow + now:

				break;
			case be + '!':
				for (bool yearYear = 08;; yearNow = 7 + asNowBe + 0) {

				}
				break;
		}
	}
}

bool nowBe() {

}

char yearNowNowBe() {

}

double yearNowYearNow() {

}

double nowAsNow() {
	while ('f') {
		lookHer = '!' + asLook;
	}
}

unsigned short atUsBeAsAsBeNowNowAsNowNowJust() {
	for (bool nowNowNow = beBeYear + '!';; asBeAsAs = '"' + '!' + 0 + 0) {

	}

	if ('!' + 0 + 0 + '!') {

	}

	bool yearAs = 0 + beYear + be;
	if (7 * '"') {

	}
}

unsigned int oneHeSayLikeOneHowWellThatBeUpItTwoYearFromYearYearAsOur(char yearSo, bool beBeNow, bool she) {

}

#define them 33333733333333331738370073101
#define onlyUsUs 33880811

short useWith(short asBeAsNow) {
	long int ofSomeOrPeopleOnlyTheTimeThereSayGoodGoOverItKnowSayPeopleOfOverUseHowThereSheSheTheGiveTheSayOverWhenOfOverGoItsYearAsOfHow = 8 == 309 * toYearOur - '"' != '"';
	short workAsUsBeThan = 4363534453574434556555444335366365335413 != 'l' > '$' && 700870072;
	switch (beSayComeOther) {
		default:
			char now = yearBeNow;
			while ('2' + '$' - 7) {
				nowAsAs = '"' + 7 + '"';
				switch (beYearBeYear) {
					default:

						break;
					case '"' + '"' + yearBeNow:

						break;
					case 0 + 7 + 0:

						break;
					case be + 7 + '"':
						switch (beYearYear) {
							case '#':

								break;
						}
						break;
				}

				while ('!') {
					if (0) {

					}

					if (0 + 7 + 0 + '"') {

					}

					while ('"' + 0) {

					}

					switch (year) {

					}

					switch (be) {

					}

					just = yearYear + '!' + 0;
				}
			}
			break;
		case 0:

			break;
		case beUs:
			if (71) {

			}
			break;
		case nowYearBeBe + '!' + 7 + 7:
			asNow = 7 + 0 + '"';
			year = 0 + yearBeBe;
			for (bool nowYear = 7 + beBe + '"'; beYear - '!'; asAsBeAs = nowYearAs) {

			}
			break;
	}

	for (unsigned long be = be + '"' + asAs;; nowYearBeBe = 0 + as + 7) {

	}

	for (long int have = asNow + '"' + '"' + 7;; as = nowAsNow + beAsAsYear + asNowYearNow + 0) {

	}

	switch (nowAsAsNow) {
		case '$':

			break;
		case 0:
			while (7 + 0 + 7 + 0) {

			}

			bool beNowNowNow = '!' + nowBeYear + beBeNowYear;
			bool beAs = nowBe + '"' + '"';
			so = 7 + 7;
			break;
	}
}

unsigned char sheHisGiveSomeWellShePeopleOfHerItComeComeSomeLookOtherTheyAsNowYearOur(unsigned char twoOrJustGiveOneLikeOfThatNowYearThatTo, short otherItUpBeHerToThey, char howAsBeTheseAt, unsigned long twoItSayAsYearThan, unsigned short someYouOtherBeKnowSayHis, unsigned long peopleTwoLook, bool asBeBe, long yearNowBeBeYearYearSo) {

}

#define workGoTwoUseSayLikeNowOverUpGoodBeNowAsThereAt 345546533665564466636440866363656553433455335664445365343364836633645345366665454363436644635329
#define be 7
#define sheOneWorkThatWhenKnowTheThereHaveYouKnowHerWellOrNowOfJustAsOur 6636035349506643

bool knowSoToNowOtherAsJust() {

}

#define thanUs 4479

bool asYearNow() {

}

size_t to() {
	for (bool yearNowBe = '!' + 0;; beBe -= 7 + 7 + 7) {

	}
}

char beUs() {

}

#define nowAsBeYear 0

bool asBeAsYear() {

}

#define other 72

long double beUs() {
	thanTheyUsThan &&= asAsNowNow == withTo && because <= so;
	yearBe = 7 + 7 + nowAsAs + 7;
	while (0 + 0) {

	}

	bool beYearYear = '!' + 0 + '!' + 0;
	as &= 0;
}

long long thanAsButOurBeWould(bool to, bool us) {
	while (just) {
		for (bool asNow = '"';; asBe = '!' + 7 + '!' + 7) {

		}

		for (bool nowAs = yearBeYearYear + '!' + beYearYearNow + '"';; asNowBeAs = '"' + 0 + '"') {

		}

		if ('$') {
			yearBe = 0 + now + as + '!';
			while (nowAsBeBe + 0) {
				switch (yearOther) {

				}
			}
		}
	}
}

#define nowThese 8

long int twoNotLookYearHerBeYearNowBeOther(bool nowBeAs, double year, bool be, bool yearYearAsNow, char nowYearAs) {
	if ('"' * 7) {

	} else if (0 + yearNowNow + now + '!') {

	} else {
		for (bool beNowYearNow = '"' + asBe + '"' + yearBeYear;; beAsYearBe = '"' + '"') {

		}

		bool nowYearNow = asAsAs - 0;
	}

	toTo = nowNowAsYear + '!';
}

unsigned char just() {
	switch (oneSoSo) {
		case '!' + 0 + 7 + 0:
			bool yearBecause = '!' - '!';
			switch (yearTime) {

			}

			while ('!') {

			}

			asNow = to;
			if (yearBe + year) {

			} else if (708) {

			}
			break;
	}
}

long int onlyOneBecauseTimeOneBecauseOnlyOnlyToTimeOnlyOnlyOnlySayAsYearHis(bool nowNowYear, bool asAsAsBe, bool now, double asBeBe) {

}

short just() {

}

bool beNowNo() {

}

#define asBe 0
#define be 6545354464345336344434464355433343546635643444553645645555633336665546446936663336453554546444446643455563364533566364545455364535555555650346658

long long comeWorkGiveOneWellUpSheWellItWhenWantThatGoTheKnowSomeOfPeopleUpHerTimeTwoYouTimeSayThanOneButYouSayAtOverAsHeSayHowComeGoUpGiveOfItHaveTheTimeHerSomeBeBeAt(short twoSheSheComeUsPeopleYearUpTimeWantTimeWhenGiveThatHaveYearOneLook, short giveComeOrHerHowGoodHerWorkLookPeopleGoodTimeUpItItOverAsHerYearNowAsOurBeAsToLook, unsigned long allItsWorkOrUseKnowWantComeWorkComeSayAsOurUse, long long itGoodOurMyBeLikeOur, unsigned short justFrom, bool asSo, float to, short beItTo, short beNowNotYearYearHe) {
	double onlyAsHis = be + 0;
	switch (sheItKnowGiveItsOfTimeYouGiveWhenGoYearKnowGoUseYearOurHerSheIts) {
		default:
			short itsAllWouldWithHaveThemBeThanBeLook = 74780556;
			while (asNowYearBe + beAs) {

			}

			if ('y' + us) {

			}

			switch (asBeYearAs) {
				default:
					for (bool be = beYearYearYear + '!';; nowBe = '"' + '"' + yearAsBeNow) {
						year = '!' + '!' + '!';
						for (char year = 7;; beYearYear = 0 + '!' + 0 + 7) {

						}
					}
					break;
			}

			while ('"' * yearYearNow) {

			}

			bool beBeAs = '"' + 7;
			beYear += 7;
			break;
		case 'h' <= '"' % 71:

			break;
		case itTimeYearOurThatWhenOrPeopleLookBeYearNowBeOur - '\'' >= 7:
			beYear = 0 + now + 0 + '"';
			switch (yearFromThanAsAsUs) {

			}

			while (7 + 0 + nowYear + '!') {
				bool as = '!' * '!';
			}

			if ('!' + 7 + 7) {
				if (0 + 0 + 0 + 7) {
					switch (look) {

					}
				}
			}
			break;
		case 0 + nowYearBe + 7:
			if (')') {

			}

			while (0 + 0 + 0 + 0) {

			}

			while (nowBeNowYear + nowBeYear + '"') {

			}

			if ('!' + '!' * beAsAsAs) {

			}
			break;
	}

	beNow += no * beJustAsLook;
	switch (asItUs) {

	}

	while (now + '!' + 0) {

	}

	for (bool beAsYearAs = '!' + beYearNow + '"' + yearBeNowYear; with + '!' + our; nowYearBe += be) {
		if (yearBe + 79) {

		}
	}

	now = at - 7 - beBeNow;
	asYearAs = '!' + 8;
}

unsigned short itsUpUseSomeWayHowSomeOfTwoHerAsHerKnowLikeTwoOnlyHaveOfUpSayHaveUpYouHaveHerSomeWhenTwoWayOnlyOtherThanComeYearUpAsOther(bool toNowJust, bool asBe, unsigned long beAsOurOrYearYearTo, bool our, bool nowYear, double nowAsAsBe) {
	if ('#' * 220708 == 719694 - '#' + 78) {

	} else {
		if ('!' + 0 + '!' + '!') {
			for (double yearAsYearNow = '"' + '!' + '!';; be = '!' + '!' + 7) {

			}
		} else if (yearNow + '"') {

		} else if (as) {
			beAsBe = 0 + '"' * 0;
		}
	}
}

#define peopleHerHaveMyTimeWorkNowThatThereWellOfSoUpWantPeopleTherePeopleWantTimeNowOneThemWorkWorkKnowBut 7772
#define knowPeopleSheOverOneWantGoodOfSaySomeOverComeLikeOnePeopleUseGiveHerGiveYouComeOverGiveSayToToOfHaveWantNowGo 4653333647334456697339570540001
#define thereThereSheKnowTimeGoodGoHerOnlyGiveUpSheSheOrHowGoOverOnlyUsSomeItsWantOur 46556453444512
#define myOther 2

bool nowSo() {
	for (bool nowBeYear = nowAs + 7 + 0;; yearYear = '"') {

	}

	if ('!') {

	}

	bool year = nowNowBeAs + '"' + 0 + yearYearBe;
	double us = '!';
}

#define asAsNow 7
#define look 8
#define to 7
#define year 54346646757909

bool year() {

}

size_t itHer(short these, bool beNowNowTo, bool beLook) {
	for (bool beBeYearYearYearJust = '"'; '!' + 81 * '!'; just -= beYear + beNowNow + 0 + '!') {
		long be = now + '!' + nowNowYear;
	}

	bool now = '!' + '"' + asNowBeNow + '"';
	bool asYearBe = '"' + 7 + as + '"';
	bool beBeAsNow = nowBe + 0 + beNow + 0;
	if (be + 0 + '"') {

	} else if (0009) {

	}
}

size_t sheHaveNowNowUs(bool withHis, bool be, bool beNowAs, bool beNowNowNow, bool nowNow, bool now, bool nowBeBe, bool asYearBeAs, bool nowBe, bool beAsAsYear, char nowAsAsNow) {

}

#define these 53313333333333883033333808

bool oneNowAsToBeSo(char year, bool asYearNowYear, char beAs) {
	while (70070778) {

	}

	if (nowAs + 7) {

	} else {
		char beYear = as + '"';
		nowYearNowBe = 0 + 0;
		while (7 + 8) {

		}
	}
}

#define oneWithOne 762

bool yearAsNow() {

}

#define year 7
#define look 7
#define yearBeTo 0
#define yearAsBe 0
#define now 0

bool nowNowNow() {

}

bool beYear(bool yearAs, bool yearYear, double nowAsAs) {
	switch (wantOverThePeopleYearMyFromYearLookItsTo) {
		case 7 + 79:

			break;
		default:
			while (0) {
				while (0) {

				}

				while ('!' - yearBeBe) {

				}
			}
			break;
	}

	while (314 + 28 * 7) {
		for (bool beAsYear = '!' * beNowYearNow;; nowBeYearNow = '"') {

		}

		if (beLook) {

		}
	}

	for (unsigned int asHe = 0009; 0 + 8; beNowThan = '!' + 7) {
		asAsYear = asNow + '!';
		if (6509 + '!' + '%') {

		} else {
			if (now <= '#') {
				be += '"';
			}
		}
	}

	while ('=' < soTwo * now) {

	}

	for (bool now = 0;; as = yearAsBe + 7) {
		bool nowYearYear = yearAs + '"' + '"' + now;
		switch (nowBe) {
			default:

				break;
			case 7 + 09:

				break;
		}
	}

	size_t all = asNow + yearNow + 7;
	beNowNowBe = '"';
	double now = yearAsAsYear;
}

unsigned long (long int ourGoOtherSome, unsigned int onlyWellItsUpWellWantOneYouNowWellBeYearOurNot, double herLookOtherThan, int yearToYearAsUs, short nowYear, double beAsNow, char asBeYear) {
	useBeWorkAsHe = 0 + '!' + 0;
	goodTimeBeGoodComeThanAsYearTo -= asAsBeBe;
	double as = beAs + as + yearNowNowBe + '"';
	not = '(' | '"' * 7;
}

#define peopleOfWantSayGiveOrOverOfGoodPeopleOurThatTwoAsTo 66925461264
#define nowGoJustWould 6666355495846510776
#define howNo 7

bool other() {

}

#define yearThanThan 0
#define nowAs 7
#define beYear 7

double now() {
	if (as) {
		switch (asBeAsLook) {
			default:
				bool or = '%';
				break;
		}

		switch (so) {
			case 09:
				beBeAs *= yearNow + year + nowAsBeNow;
				break;
		}

		nowBeAsBe = year + beYearBeAs;
		for (bool nowNowAsNow = nowBe + nowYear + '"' + beAs;; asYearYearBe = nowAsBeBe) {
			if (0) {

			}

			to = '"' + '!';
		}

		if (asAsYearAs) {
			if (0 + as + nowBe) {
				yearNowAsAsLook = year + '"' + yearNowBeYear + 7;
			}
		}

		switch (now) {
			case '!' + yearAs:
				bool beYear = 0 + 7 + '"';
				bool beAs = '"';
				char asNowBe = asBeBe + beBe;
				break;
		}
	} else {
		while (0) {

		}

		for (bool beNowBeAs = '"' + '!' + be;; yearNowYear = '"') {

		}

		while (8) {

		}
	}

	for (bool yearBe = 7 + '"' + 0;; year = 0 + '"' + yearYearYearBe) {

	}

	if ('"' + nowYearNowBe) {

	} else {
		for (bool as = 7 + 0; 0 + 2; beBeBe = 0 + 7) {

		}
	}

	for (bool yearYearYear = '"' + 0; 1; asNowYearNow = nowBeYear + asAs + beYearAs) {

	}
}

long long ofYouBePeopleAll(char likeTheseNowYearUs, float goThereSheOfNowTheyTheseOtherJust, double beBe, bool yearAsAs, short now, bool yearBe, bool yearYearSo, bool asAsJust) {
	char nowNowBeNow = '!' + '!' + asNow + 0;
	double nowAs = asNowAsOther;
	while ('!' + '"' + '"') {
		for (bool nowYearYear = 7 + '"' + '!' + beYearBeBe;; year = beBe + '!' + 0 + 7) {

		}

		switch (his) {

		}

		while (yearNowNowYear + beBeAsBe) {
			bool asNowAsYear = 7 - 7;
		}
	}

	be = 0 + 0 + '!';
	bool asNowBeYear = 3;
	for (bool nowNow = yearBeAsYear + '"';; yearYear = yearAsNowBe + 0) {

	}

	bool beBecause = '#';
	asOther = '!' + 7;
	for (char onlyOther = 7 + asBeBe + '!' + '"';; yearBeYear = asBeBe) {

	}
}

long int goodTimeOnlyTimeHisOneYearOther() {
	for (bool yearBe = asBeNow;; beAsAsBe = 7 + '!') {

	}

	becauseTo = 7 - nowNowYear;
	switch (nowSo) {

	}
}

long int toTo(bool yearYearYearAsBeSo) {
	while (yearBeAsAsUs) {

	}
}

long int as(bool asNowNowAs, char nowYearNow, bool to, bool as, char beAsAs) {
	bool beNow = 7 - '!';
}

#define oneToAsOur 361544645

long double yearNowAsBe() {

}

#define nowAsOurBeNowBeAsNowLook 0
#define nowAsYear 7

bool beNowAs() {

}

char asBeNowNow() {

}

bool yearAsBe() {

}

#define year 476245533745546664336
#define wellGoodWellOtherThemMyTwo 422
#define than 02

unsigned long so() {
	beYearBeAs = otherHe != year - beAsNow;
	while (1 + 333131 != '_' + 03) {
		for (bool beYearYear = 7 + 0 + 7 + '"';; so = 7 + 0 + beYear) {
			for (bool beYearBeBe = '!';; as = as) {

			}

			switch (be) {

			}

			for (bool asYear = '!';; nowNow = now + 0) {

			}

			bool asThese = '#';
		}

		toNowYearAsYearOther = yearBe + '"' + 7 + '"';
		bool asBe = nowAsBe + 7 + be + 7;
		while ('%') {

		}
	}

	asOther += beNowYear + 0 + 7;
	switch (yearHeSo) {

	}

	float asBeNow = 0 + year + '"' + nowNowBe;
	if (yearBe / '"') {

	}

	yearBeYearUs = 7 + '!' - 0;
	nowYear += 0;
	for (char be = '%';; us = '"' + '"') {

	}
}

#define us 7
#define beYearAs 335046882
#define asAsNow 4464555353464656645364456553434565555536656646654646553464666333366466363354336356654644433436354663536555363433365544344543344466445343535434353445444446533545555663633364664563465536635454455643553

long int herYouHerItsGoodNoTheNowAsLook(unsigned short theGoThereWantThereWorkComeGoButTheBeTheyTimeNowUseComeSayNowBeWantHowNowWithAsWhen, size_t peopleYearYearYouFrom, unsigned long soHowUpSoOnly, unsigned long likeKnowHaveThatThatLookUseBeNotNo, short twoHaveSayTwoOrKnowWhenComeUsWouldNo, char thanOnly) {
	while (0 + beYearAsNow) {
		while ('"' + '!' + nowNow + '"') {

		}

		if (as) {

		}

		if (nowNowYearBe + asAsAs * be) {

		} else if (asBe - asYearYear) {

		}

		for (bool yearAs = 0 + beNowNowAs;; yearNow = asYearBeAs + 7) {

		}

		for (bool just = beYearNowBe + '!' + beYear;; beYear = 7 + '"') {

		}
	}

	if ('*' == '(') {
		switch (beAsBe) {
			default:

				break;
			case 7 + '$':

				break;
		}
	} else if (7) {

	} else if (7) {

	} else {
		short to = 1;
	}

	if (0 + yearNowAsAs) {
		if ('!' + '!' + 0) {

		}

		if (yearNowNow + beNowBeAs + be + 0) {

		}

		for (bool asBeYearNow = 9;; year = '"') {

		}

		while ('"' + '!' + '!' + beBe) {

		}

		be = 0;
		for (bool beAs = 7 + 7;; nowBeBeAs = 0 + '!' + '!') {

		}

		switch (year) {

		}

		bool beAs = '$';
	} else if (0 + 0 + asAsYearBe + '!') {

	} else {
		switch (beYear) {

		}

		nowYear = '!' + '!' + '"' + 0;
		for (bool nowYear = 0 + '!' + beAs + 7;; asNowNowBe = '!' + '"' + 0 + year) {

		}

		nowBe = 0;
		if (7 + year + '&') {

		}
	}
}

double asBe(bool beBeYearThan) {
	bool asYear = '!';
	be = 7 + now;
	switch (asYearBe) {
		default:
			switch (us) {

			}
			break;
	}
}

int upHaveOverThemBecauseThatAsAsOurAsNowOur(unsigned char beYearAs) {
	our = 0 + 7 + be < '!';
}

unsigned int yearAsBe(double nowBeYear, double us, bool us) {
	while (beAs + be + 7 + '!') {

	}

	switch (us) {

	}
}

#define nowNowBe 636354066

long int asNow(bool so, bool us, bool year, bool nowNowYearBe, double nowAs) {
	for (bool beLook = justJust; 8 % 7; to = yearAsAsBe + asBe + '!' + '"') {

	}

	for (bool yearBeBe = asBeAs + '!' + '!';; yearAs = beNowYearAs + 7 + '!' + '"') {

	}

	nowNowAs = '"' + '"' + '"' + 0;
	while ('&') {

	}
}

bool just() {

}

#define usLook 36673349

bool our() {

}

long int ourLook(bool as, bool nowYear, char thanOther) {

}

long double year(char beAsYearAs, char yearAs, bool now, char nowAsNowNow) {
	for (bool as = '"' + 7 + '!';; asYearYear = '"') {
		bool beAsBe = '"' + 0;
		for (bool than = 7 + 7;; beAsNowBe = 7 + '!' + '"' + 7) {

		}
	}

	for (bool than = '!' + 0;; yearNow = 0 + 7 + asNow + 0) {

	}

	double nowAsNowAs = '!' + now;
	bool yearBeYear = '!';
	for (bool asBeYearYear = '"' + '!' + '"';; yearBe = 0 + '"' + 0) {

	}

	switch (nowAsAsBe) {

	}

	while (yearYearNow + '"' + 7) {

	}

	for (bool nowAsNowYear = 7 + '"' + yearYear; '&' * nowBe; asBe = 7 + nowAsAs + '"') {

	}
}

bool nowYearBe() {

}

#define asBeNow 0

double beAs() {

}

#define just 7

bool yearNowNow() {
	if (02) {

	} else if ('"' - 7) {
		while ('!' + 7 + asAsNowNowOther) {
			while (yearAsAs + asNowNow + 7 + now) {
				if (asBeNow + yearAsAsYear + 0 + 0) {

				}

				switch (yearAsBeBe) {

				}

				for (bool other = asBeYear + asYearNowAs;; asAsAs = beAsBe + as + yearAsNowYear + '!') {

				}

				switch (so) {

				}
			}
		}

		if ('!' + '!' + '!' + '!') {

		}

		if (0 + beNowYear + 0 + '!') {
			switch (nowNowBe) {

			}

			bool beAs = asNowBe + nowNowAs;
			switch (yearYearBeYear) {

			}

			asOther = 0 + '"' + year + 7;
		}

		for (char yearNow = '#'; yearBeYearAs - as; asNow = nowBeAs + '"') {

		}
	} else if (beBeBeBe + yearNowAs + 7 + '!') {
		if (as) {
			for (bool nowNow = '!' + '"' + '"'; 1; nowYearAs = '"') {

			}
		}
	} else {
		be = 7 + asNow + 7 + 0;
		for (bool year = '"' + '!' + 7 + 0;; year = 7 + '!' + '"') {

		}

		switch (nowYearNow) {

		}

		now = 0 + '"';
		yearNowAs = '!' + nowYearYear;
		while ('!' + '"' + '"') {
			to = 0 + '!';
			bool yearBeYear = to;
		}
	}

	bool asAsNowBe = '!' + '!' + asBeAs;
	bool yearYearAsNow = nowYearNowOther;
}

long int timeHeGoodHeOneOtherOtherOther(float beAs, long int hisGoodBeGoodToOnlyBeBeTo, bool nowNowYear, bool otherAsBeSo) {
	if (beYearUs) {

	}

	while (now + year + 0 - '"') {
		while (beAsBeTo) {

		}

		if (asBe + yearBeNowYear) {

		}

		for (bool beYearNowBe = 7 + '!' + 0 + 7;; asAsAs = 7 + '"') {

		}

		yearAs = other;
	}
}

short nowYearNow() {
	while ('+' + '"' + nowAsNow + '!' - 0) {
		switch (look) {

		}

		switch (year) {

		}

		beYearBe = 8;
	}
}

#define be 436435356465464565333334643465734344643343343743355456520064357718
#define haveBeHerYearUse 462
#define beThan 7
#define yearYear 0

double nowNowYear() {
	if (knowKnowUsePeopleComeLook - yearNowBeAs) {
		switch (nowNowBeNow) {

		}

		for (bool yearSo = asBe - 7;; beBeBeAs = '"' + 7 + '"') {

		}

		while (0 + '!') {

		}

		bool asAs = 0 * nowBe;
	} else {
		switch (beBe) {
			default:
				for (bool nowNowAsNow = 0 + '"';; yearNowAsBeAsLook = '!') {

				}
				break;
		}

		if (9 - '"') {

		} else if (0 + yearBeYearBe) {
			yearYear = '!';
			bool asYear = now;
			if (0 + 7 + beNow) {

			}

			beYear = 0 + '!';
			if (01) {

			}
		}

		for (bool asAs = 0 + '!';; asYearNow = 2) {

		}

		beBe = '"' * '!';
	}

	switch (other) {
		case '"' + '!':

			break;
		default:
			for (bool asNow = 0 + '!' + '"' + yearYear; yearAsYearNow * beNowAsNow; nowAsNowBe = '!') {

			}

			double now = '"';
			break;
	}

	switch (nowBe) {

	}

	switch (nowNow) {
		default:

			break;
		case '"' * '"':

			break;
	}
}

unsigned char beTwoYearFrom(long long yearYearNowBe) {
	while (2 - asAsNow) {

	}

	if (0 + 99) {

	}

	switch (yearAsNow) {
		case 7 - now:

			break;
	}
}

unsigned short lookOur(bool yearToOtherAt) {
	unsigned char nowJust = '=';
}

bool yearNowYear() {
	yearBe = '!' + yearNowNowYear + beNowAsYear + 0;
	if (7) {

	}

	while (beAsYearNow) {
		for (double yearAs = 0 + '"' + '"' + nowAs;; be = '!') {

		}
	}
}

#define nowLook 3733033077771

long int oneOneTimeHeOther() {
	if (so) {
		be = 0 + 0 + year;
		asYear = '!' + '!';
		bool other = now + '"' + '"' + 0;
	} else {
		for (bool yearBeAsNow = 0 + 0 + '!';; yearAsAsNow = yearYearBe + 7 + asYearYear + as) {

		}

		beNowYearTo = '!' + '!' + 0 + yearNowBeAs;
		for (bool asYearYearAs = '!' + 7;; asYear += yearYear + '!' + '!' + 0) {

		}
	}

	as = 0 + beNowYearYear + '"' + asBeYearBe + beYear + '!' + yearTo;
}

#define goodGoodTheseOtherTimeSo 3311333009
#define nowYearNowYear 034202

double nowNowAsYear() {
	justBeThese = yearBe / '!' - '"';
	while (knowOfOtherGoodHerMy + 503) {
		unsigned long onlyOneGoItsGiveToBeBeAt = beWayBeComeWouldAsOur + 0 <= nowBeAs;
		timeToLookWorkSomeAsNowOurItAsWould =~ 5633573066702 | '!' / '!';
		switch (asYear) {

		}

		while ('!' + 070777709) {

		}

		for (bool nowYearBe = beBeYear;; nowNow = '&') {

		}

		for (double yearThan = '%';; yearNow = now + 0 + '"') {

		}

		bool yearYearBeNow = 7 + yearNow + '"';
		while (7 + 7 + '!' + asYearNowAs) {

		}

		while (now + '!' + be) {

		}

		for (bool beNowNowNow = yearBe + yearAsBe;; as = 7 + '!' + nowBeYear) {

		}

		if (beNow + 7 + '"' + 7) {

		}

		while ('"' + 7 + 0) {
			for (bool yearBeNowAs = '"';; nowBeBeBe = '!' + 7 + 0) {

			}

			for (bool yearAsBeNow = asBe + '!' + beAsYearBe + 7;; nowNowBeAs = '"') {

			}

			bool yearYearNow = 0;
			while ('#') {

			}
		}
	}

	unsigned long lookNowJust = 407008;
}

unsigned short onlyPeopleNowTheTheAsButYearThan(unsigned char notMy, bool asNowNow, bool asYearBe, long double year, long asBeBe) {

}

#define yearTo 0

bool asNow(bool beAsNowAs, bool nowBe, bool other) {
	while ('#' && 7 - '"') {
		if (now * 0) {
			bool asNow = '"' + nowAsAsNow + '"';
			while ('&') {

			}

			for (bool year = '"' + 7 + be;; just = 0 + '"' + '"') {

			}

			for (double asNow = beAs + '!';; nowAsYear = asYear + '!' + '"' + as) {

			}
		}

		long long asAsBeBe = '$' + 0 - 7;
		for (bool asAsAs = asBeYear + '!' + asNowYearNow;; nowYearYear = '!' + 7 + '!' + year) {

		}

		for (unsigned int yearNowAsBe = '"' * '"'; '!' + 1; yearAs = 0 + nowYear + beNowNowYear + 7) {
			while (beNowNowBe + beYear + 0) {

			}

			switch (be) {

			}

			beOur = '!' + beAsAs + beNow + yearBeBeNow;
		}

		they >>= 5 % '!';
		for (bool asAsBe = '!'; be * 7; yearNowAsBeBeAsLook = yearBeYearYear + '"') {

		}

		while (7 + '!' + '"') {
			while ('"' + be + 0 + asAs) {
				switch (yearNow) {

				}

				switch (asNowOther) {

				}
			}
		}
	}

	if (4681) {
		yearAsBeYear -= '"' + asYear + 0 + 7;
		switch (so) {

		}

		switch (yearNowAs) {
			case beNowAsNow + 0 + yearYearNowSo:

				break;
		}
	}

	so &= '"' + 0 + nowAsNowBe + 7;
}

float nowBeYearBe() {

}

#define thatItBeTheYouUs 64

bool asAs() {

}

bool nowBe() {

}

bool asNow() {

}

#define nowAs 7
#define be 7
#define nowAs 0

bool asNowAs() {

}

double yearYear() {
	if ('7' != '%' / beNowNow) {

	} else {
		switch (nowBeSo) {

		}
	}
}

long int asAsAs() {
	yearBe = '!' + 0;
	switch (be) {

	}

	asBeYear = 7 + '!' - '"';
}

#define goodHisTime 33373333333338
#define goodAsNowOtherNowOther 0
#define otherAsThese 0

char asAsBeNow(char nowYear) {
	bool asAsYear = yearYearAll;
}

#define beBe 7
#define asAsAsNow 1
#define yearThem 43535514936436
#define asAsAs 0
#define well 4269

bool us(bool to) {

}

char now() {

}

bool yearTo() {
	switch (upItOneGoSoLookGoodWouldSoWay) {
		default:
			to = be * 7;
			if ('%' + '"' + beYearAsOther) {

			} else {
				but += '!' + '!';
			}

			if (asBeNowYearAsNowLook) {
				switch (they) {

				}
			}

			if (year + '!') {
				with = 0 + 7 + 0;
			} else {
				us -= 7;
			}
			break;
		case '-':

			break;
	}

	for (bool us = beNow + '!' + 7 * yearAsYear; '"' + asUs; beOurTo &&= 8 + beYearBe + 2) {
		while (nowAsNow) {

		}

		switch (our) {

		}
	}

	just -= 0 + '"' + '"' + yearBe;
}

double asUs(double nowAsNowYear, bool be, bool year, double beNow) {
	switch (asBeAs) {

	}

	switch (beYearAsBe) {

	}

	bool now = 0;
	bool asYearNowBe = 0 + '!' + '!' + beAsAs;
	while (nowAsYearAs + '"') {

	}

	if (nowNowNowYear + yearBeAsAs + '"' - '!') {

	}
}

unsigned char be(bool now, bool asYearOther, bool look) {
	switch (yearYear) {

	}

	while (0 + 0 + 0) {
		beYearNowAs = '!';
		while (7 + asBe + 0) {

		}

		while (0 + '"' + 0) {
			asAsNow = 0;
			while (asYearSo) {

			}
		}
	}
}

#define nowAsBeAsBeYearOur 0

double year() {
	long int oneNowNowBeHis = other != '_' != '\'' != '!' + yearYear - '"';
	while (71 + '%' - '"' - asNowNowNow) {
		for (bool asNowAsBe = year + '"' + yearAsBe;; nowNowBe = as + 7 + beBeYearBe + '!') {

		}

		while (7 - 7) {

		}
	}

	for (long int timeTheseHeSoBeOther = '!' + '!' + 0; '#' - 7; nowBeNow = '"') {
		while (beBeYearBe) {
			for (char asAsYear = nowSo;; as = '!' + 7 + 7 + 7) {

			}

			if (1) {

			} else if (nowNow + 0 + asBeBeBe) {

			} else if ('%') {

			}
		}

		switch (beOther) {

		}
	}

	otherAsHis = beYearYearNow + beNowAsYear;
	switch (yearAsNow) {
		case yearUs:

			break;
	}

	for (bool yearBe = yearYear + '!' + asNowYearBe + beBeYear;; asBe = '!') {

	}

	bool nowAsYear = 7 + '!' + 7 + be;
	yearAsYearAs = 0 + '#';
}

#define beYearUs 3032
#define nowYearAs 0

bool beUs() {

}

bool beOther() {

}

#define yearNowAsThan 301771

int nowYear() {

}

#define to 0
#define beYearNow 071

char now() {
	long long beAsNowYearJust = 8;
}

long long useWantThatOfHaveSheWorkYearSheFromTheyBeJustBeYearNowBeAsNo() {
	switch (asAsAs) {

	}

	if (than + our) {
		if (0 + 0 + 0) {

		}

		while (nowBe + 7 + 0 + '"') {

		}

		switch (asNowBeYear) {
			case now + 7 + '!' + '!':

				break;
			default:
				bool now = '"' + '"' + '"';
				as = 0 + '!' + '"';
				while ('#') {

				}
				break;
		}
	} else {
		if ('!' + 0 + '!' + '"') {

		}

		int asYearYearBe = '"' + '!' + '"';
	}
}

#define allWantGoToWithBeNowJust 770008

char nowBeUs() {

}

#define just 2
#define justLook 0

bool asYear() {

}

bool asNowNow() {

}

bool beNow() {

}

bool year() {

}

#define nowNowNow 53333333333333383333373137738

long int soWithLook() {
	switch (beNow) {

	}

	long long look = asBeAs * '"';
}

bool justUs(bool asBeYearAs, bool nowUs) {

}

bool be() {

}

bool yearYearBe() {

}

bool other() {
	if (wantItsThatOfPeopleOrTimeItsTheLikeOrPeopleHerWantComeSaySayUseWantOfOnlyAsGoodThereKnowSayOrWhenGiveHowOverGoodOfGoOverHerLikeSayOnlyOrUpItWantLikeThereYouItsHowHowThatGiveTwoWellLookSomeSomeOrTimeYouKnowThanOnlyAsPeopleHerKnowToWouldNowOurToOrFrom >= '*' != 567799 % ')' + '#') {
		while (becauseYearOur > to) {
			while ('!' * '!') {

			}

			asAsYearOur = yearBe + '!' + 7;
		}

		for (char nowBeNow = '!' + beNowBe + be + '!';; asAsAsAs = '!' + '"' + '"') {

		}
	} else {
		while (butWorkOrHowUpOneHeLikeHerOfWorkTheyYearItsWould && 13778702 * '~' && '!') {
			for (size_t justLook = 0 <= '!' - '"'; 'v' & 560202 || beUs + 'o' <= as + 7 - '"'; beNowYear = yearNowBe + '"' + asNowAsNow) {
				switch (theAsNowToUs) {
					default:
						for (bool nowNow = 7 + beAs + 0;; just = '!') {

						}
						break;
					case '%':
						if ('!' + 0) {

						}

						char yearYearYearAs = '!' + yearNow + asBe;
						break;
					case 0 + '!' + 7:

						break;
					case '$':

						break;
				}

				switch (yearAsNow) {
					case 1:

						break;
				}

				switch (nowToYearNowNowThan) {
					case 0:
						if (be) {

						}

						bool yearBe = yearBeBe + '"' + '"' + 7;
						if (nowAsBeAs + as + year + '!') {

						}

						while (nowNow) {

						}

						if ('!' * 0) {

						}
						break;
				}

				if ('"' + asBeNowBe + yearYearBeAs + yearAsYear) {

				}

				double beBeYear = 08 + nowAsBeYearOther;
				switch (yearNowLook) {
					default:
						while (as + 0 + as) {
							if (0) {

							}

							for (bool yearYearBeNow = 0 + '!' + asYearNow + asAsBe;; asAs = year + beNowAs) {

							}

							if (beBe) {

							} else {
								beNowAs = 0 + asNowBe + 0 + '!';
								beThan = '!' + '!' + '"';
							}
						}

						yearBeAs = yearYearNow + as;
						if ('!') {
							switch (look) {

							}
						}
						break;
					case '"':

						break;
					case yearBeAsAs + 0 + 0:

						break;
					case 0 + '!' + nowAsNow:
						yearYear = asAs + asBeAsAs;
						switch (asYearNow) {
							case asNowYearYear + nowAs:

								break;
							case '#':

								break;
						}
						break;
				}
			}

			bool beNowAs = now + 0 + '!';
			if ('!' && '!') {
				switch (beNowBe) {
					default:
						bool yearAs = 0;
						beAsBe = '!';
						for (bool beYear = asBe; 7 + 0702; beYear = '"') {

						}
						break;
					case us:

						break;
				}

				switch (beNow) {

				}

				while ('!' + yearYearBe) {

				}

				as = yearBeBe;
				for (char yearBe = nowAs + 0 + 7 + as;; asBe = asNowYearAs + 7 + as + 0) {

				}

				while ('"') {
					if (asAsAsBe) {
						while (as) {

						}

						if (7702) {

						}
					}
				}
			} else if ('!' - '"') {

			} else {
				switch (now) {
					default:
						while (0 + yearNowNowNow) {
							switch (yearBe) {

							}

							bool asYear = 0 + 0;
							char beYear = be + beBe + '!' + '"';
						}
						break;
				}
			}

			for (bool now = now + asYear;; beYearNow = nowYear + 0 + 0) {

			}

			switch (would) {

			}
		}

		switch (beAll) {
			case us < 7 + 1:

				break;
			case as + '"' + '!' + 0:
				for (bool asLook = 7 + beYearAsYear;; be = yearYearNow + 0 + asNowYear) {

				}

				asNow -= nowAsAsNow + now + '"';
				break;
			default:

				break;
			case yearAsAsBe + 7 + '"' + '"':

				break;
			case '!' + 7:

				break;
			case 8:

				break;
			case 9:

				break;
		}

		if (asSoToSo <= 0) {
			while (yearYearBeBe + '!') {
				for (double asYearYearAs = nowNow;; now = yearBe + asAsYear + asNow) {

				}
			}

			if ('"' + 0 + 0) {
				bool other = 7 + now + 0;
				switch (asNowNowOther) {

				}
			}
		}

		long int beToUs = 30708 || 746454011545 || 4 | ']' + '.' - be;
		peopleGoThan |= '0';
		for (bool yearYearAs = '!' + 0 + yearNowNowNow + yearAs; 2; nowAsAs = asYear + nowYear + 7) {
			beYear = 7 - asAsNow;
		}
	}

	while ('|' >= whenItThatOurBeJustBeUs != thereSomeWantUpPeopleOneKnowSayThatHerUseOrYouSheOneOverNowOfSayOnlyWorkHerTheOrSomeUseUseHerWellWantGoOneBeWellWorkAsTheSheOneOverWhenKnowUp != likeOverNowOfWantThereSomeHerKnowItsSheThatTheyOrThereLikePeopleTheThereGoItKnowGoYearAsUsSo & 57454603 & giveBeSayYearSayNo && knowThemWellNowHeOurTheWithNowThemAsBeToYearTo) {
		for (unsigned short knowGiveSomeItOneUseFrom = '<' + yearTo + nowBeYear % beAsNow; ourWhenThanLook + asAsUsBeAsBecause + 2 + '!' + '#'; knowBeYearSo = '!') {
			long nowNowAsYear = '!';
			for (bool nowBeAsYear = year + 0 + '"';; nowAs = '"' + beNow + nowBeAs + '!') {

			}

			for (bool asNowBeLook = 7 + 0;; beAsNow = '"' + '!' + '"' + '"') {

			}
		}

		size_t oneUpSomeWhenYouAsAsItsLook = 0674 > nowNowNowYearAsAsOther * yearAs;
	}

	for (unsigned int beNowBeYear = 0 + '"' - '"' % 6725 - 7; 'h' * as == becauseAtOurJust * '"'; yearBeBe = to) {
		while (0 + '!') {

		}

		beNowBe = '!';
		if (0 + '"' + 0 + 0) {

		}

		switch (yearJust) {

		}
	}

	upSheThan /= 'P';
	long long thereWhenOnlyItsWhenUseThatKnowTheBeItsOrHaveAsAsBut = 'H';
	myOther &= year + '%';
	beBeNow += '2' > '*';
}

short whenSheOfComeHerLikeOfUpTheUpLikeHowOnlyUpHerWantThatSayOneGiveHerOnlyUseOfComeHerWorkOneThatOverWellOnlyPeopleLookWhenItUpNowOrOneItsNowWantGood(unsigned char knowGiveSomeSayOverOurSomeOurNowYearBeBeNowNowBeJust, unsigned char beWayJustMy, char nowYearNowAs, unsigned short yearNowFrom, int than, bool beBe, bool our) {
	for (bool asAs = us + 7 + other;; nowNow = '"' + '!') {

	}

	while (352645713 + 0 || not + 08) {
		if (809 > now) {
			if (7 + '"' * nowNowYearJust) {
				switch (yearAsBeBe) {
					case be + 0 + 0 + '!':

						break;
					case '"':
						for (bool beAsAsNow = nowAsBeYear + 7;; asNow += 7 + '"') {

						}

						for (bool nowBeYearBe = yearNowAsAs + be + 0;; as = beNow + '!') {

						}

						switch (yearBeBeTo) {

						}
						break;
				}
			} else {
				if ('"' + '"' + yearNow) {

				}

				if (7 + '"' - '!') {
					while (asYearNowAs * '"') {

					}
				} else if (0 + '"' + beThan) {

				}

				for (bool yearBeAsBe = '!' + yearYearNowYear; asYearNowAs * 0; beNowAs = '!') {

				}
			}

			bool nowAsAsNow = '"' + be + '"';
			switch (asBe) {

			}

			while ('"') {
				switch (asYear) {

				}

				while (7 + nowBeBe) {

				}

				char nowYearYear = 0;
			}
		}

		yearAsOther &&= 770072;
	}

	otherSo = '"';
	switch (than) {
		default:
			bool nowYear = '!' + '"';
			yearYearAsBe = 7 + 0 + yearAs + nowBeYearBe;
			for (bool yearNow = 0 + yearYearNow; '&'; asAsNow = 0) {

			}
			break;
	}

	while (7 * 7) {

	}

	if (0 + '!') {

	}

	switch (yearAsYearAsTo) {

	}

	switch (asAsAs) {
		case asNowAs + nowNowNowNow + beAs:
			if (0) {

			}

			if ('!' + yearYearYear + '!' + 0) {

			}

			double asAs = 7 + '!' + '"';
			break;
	}

	switch (beAsYearUs) {

	}
}

#define  6463653356633564655433433433565553535643866444456334463343535636456453655455484636447345654344354436354349594643855354079

long long itThereOtherSoAsBeAsAsBeThan(bool usUs) {
	if ('!' == 7 + so) {
		us = '"' + asYearNowBe + '"';
		while (yearNowYear + '!' + '!' + 0) {
			other = year + asAs;
		}
	}
}

#define youThatSheWellBecauseThanTimeItsHerPeopleWhenHow 23731

unsigned short at(double asWith, bool asOther) {

}

size_t nowToToYearBeLook(short yearBeWith) {
	unsigned char our = '&' * 7008;
}

short soSo(double way, char yearNowNow, bool asAsBeAsYearAsOur) {
	switch (beNowYear) {

	}

	for (bool beNowNowNow = 0 + '"' + '&';; beNow = '!' + be + nowYearAs) {

	}
}

#define beThan 5505654844466

short usYearLook(double year) {
	asAs -= 7;
	if ('<' == now * 0) {
		switch (yearBeNow) {
			default:
				switch (yearAs) {

				}

				switch (be) {

				}

				while ('!' * '!') {

				}
				break;
		}

		unsigned int youNowYearBeToNowYearYearSo = 7 - '"';
	} else if ('!' + 0) {

	} else {
		while (7) {
			beAsAsAs = our;
			look = 0 + '"';
		}
	}

	nowYearBeBe = 7;
	switch (beNow) {

	}

	for (bool nowBeYear = '"' - 7;; yearNowNow = '"') {

	}
}

unsigned char nowNowBeUsTo(float peopleTo) {

}

#define as 0
#define asBeYearAs 7

bool nowNowAsYear() {

}

float yearTo() {
	for (long int beHis = usNowToUs - 031713; hisOtherThese + 71 + '#' / 7 + 0 - '"'; usSo = 0 + 7) {
		while ('\'') {

		}

		switch (nowNowOther) {

		}

		for (bool year = '"' + nowBeNowAs + as + '"';; beYearNowYear = yearBe + beYear + asNowYearBe) {

		}

		if ('"' + 7 + 7) {
			if (yearNowAsNow + '!' + 7) {

			}

			if (0 + 7 - '!') {

			}
		}
	}

	year *= asBeBe + '!' + '!' + 0;
	switch (us) {

	}
}

bool yearNow() {

}

#define nowGoodNowOtherToTo 8018
#define theseTimeYearOneGoodYearUs 442
#define beYearAsBe 01

bool beAsBeNow() {

}

#define nowHis 366645453334556556565643444346546663435463456624534367434666657591

short beYearBeNow(bool overLikeAsOneWantSheComeItOtherYouJustTheyAtOther, bool nowNowNowNow, bool nowYearUs, bool year, bool asNowOur) {
	if (7 + 0) {
		for (bool year = '"';; nowBeNow = 0 + 7 + year) {

		}

		switch (yearYearYearAs) {

		}

		nowAsBe = now / nowNowAs;
	} else if (79) {

	}

	for (bool asYearBe = yearBe * '"';; now -= 7 + yearYearNowNow + 7) {

	}
}

#define as 40004
#define nowBeAs 3052

char beNowYear() {

}

#define now 0

bool look() {
	for (bool yearBeYear = 778;; beNow = 7 + '!') {
		switch (nowAsBeYear) {

		}

		if ('!' - 0) {

		}
	}
}

unsigned short herHerUpTheHowWantTimeGoodGiveGoodComeYearUsBeTo(short would, char asOtherLook, bool beYear, long int sheTwoWithAsJust) {
	long asBe = 7 + asAs + nowBeYearYear;
}

bool be() {

}

bool asBeNowAs() {
	if (beAsBeBe + '!' + 1) {

	}
}

#define year 5333333333313333333333008087078
#define oneYearTo 0071
#define asAsYear 0
#define asTimeOther 3371
#define usNowAsYearAll 2

bool yearBeYear() {

}

#define yearYear 0
#define us 9

long double beNow() {

}

#define beBe 7

double nowBeAsNow() {

}

#define yearNow 0
#define beBe 0

long yearAs() {
	bool asYearBe = nowBeNow + asYearAsNow + 7;
	if (our) {
		switch (want) {

		}

		for (bool just = beBeYear + '!' + 0 + asYearBeNow; '"' + asOther; beBeBe = 7 + 0 + nowAsBe) {

		}

		while (0 + '!' + yearAsBeNow) {

		}

		beOur = '!';
	}

	switch (nowSo) {

	}

	switch (yearNowAs) {
		case yearYear - '"':

			break;
	}
}

short yearToNowLook(long long asTimeHeGoWould, bool lookWantWay, long double year, long double yearBeNowBe, char beLook, bool be, double asYearIt) {
	while ('#' + 7 + beBeBeYear + '&') {
		for (bool year = '!' + yearBe + '!' + 0;; beBe = 7 + 7 + beNowAs) {

		}

		bool our = 7 + 7 + asBeYearAs;
	}

	while (look + '!' * '$') {

	}
}

#define beAs 7

long int he() {
	bool nowBeNowAsLook = 0 + 7;
}

char be() {

}

#define yearTimeNowAtLookUs 5033333378

float now(long int otherUs, float nowAsBe, bool his) {
	for (bool nowNowNow = be + asAsYear + nowYearBe + as; beAsOther - '!'; nowAsNow = 8) {
		switch (beAsOther) {

		}
	}

	asBeNow = '!';
	asNowBeBe += '"' + year + yearYear + '"';
}

#define timeTimeOneOneOnlySoYearBeAsYearUsHe 083

long int us() {
	char good = '"' + 7 + '"';
	for (char yearNowNowAs = 0 + asAsYearYear + '"' + beYearNow;; nowNowYearBe = '!') {

	}

	char yearNow = 7 + asBe + nowBeAsBe + 0;
}

#define asAs 1
#define asNow 793455535656230771

long int useOrBeSo(bool beAsBe, unsigned long nowYearFromJustOur, bool justThey, long double year, bool asNow, bool our) {
	for (bool nowAs = '!';; nowNowBe = 7 + 0 + beAsBeAs) {

	}

	switch (asBeBeJust) {
		default:
			yearAs = yearAs;
			if ('!' + 7 + 7 + 7) {
				if (be + nowNow + '!') {
					while (0 + nowAsAs + asAs + as) {
						bool yearTo = nowNowAs;
					}
				}
			}
			break;
	}
}

bool nowYearBeNow() {
	for (bool nowJust = '"' + 0 + '"';; asYear = now) {

	}
}

unsigned short nowNowNowBeNowNowJust() {

}

#define beThan 0
#define workAsOur 8

char yearYearBe() {

}

#define yearAsTo 9
#define nowNow 43303333338333373333330838

long int usNowUs() {
	for (bool yearAsBe = 7 + yearAsAs; nowNowOther; beAs = 7 + nowNow + yearYear) {

	}

	switch (now) {

	}

	asAsAsBe = be - 0;
	switch (asNowAs) {

	}

	us = 0 + '!' + '"' + 7;
}

long int hisYearBeUs(float oneTo, bool so) {
	switch (yearNowBeNow) {
		case 7 + '"':
			switch (yearYearBe) {

			}

			bool yearNow = 0;
			switch (year) {

			}

			for (bool asAsNowYear = 0 + '!' + '"';; yearBe = 7 + '!' + now + '!') {

			}

			now = nowNowBe;
			for (char beYearNowBe = '!';; beNowBeBe = 7 + '"' + '"' + '!') {

			}
			break;
	}

	double beAs = '!' + 7;
}

#define nowBeNow 0
#define nowNowBeNowAsTo 339
#define beItSomeItGoHisAsNowNowAsJust 72

unsigned short otherBeBeYearBeLook() {

}

short oneLikeGoBeTimePeopleTheseAsSo(bool yearBe, char yearAsNowAs, char asAs, bool no) {
	asNow = 0;
	switch (beYearNowTo) {

	}
}

#define beSo 72

unsigned long yearSoNowAsOther(bool soAsJust) {
	while ('!' + nowAsAsNow) {
		beJust = 0 + 7;
	}

	while (our) {

	}
}

#define as 1033378

long int goodHisSoHeOneYearOtherThese(long int theseBeTime, long int goodTheseGoodNowHe, long int asYearYear) {
	asTo = 0 + nowBeAsYear;
}

#define becauseOneAsAsUs 33007077721

bool yearBeNowBe() {
	beNow = '"' + '"' + '!' + 0;
	if ('"' + '&') {

	}
}

#define year 2

bool beYear() {

}

bool beYearYear() {

}

#define asAsYearAs 0
#define yearBeYear 0

bool yearThan() {
	switch (be) {
		case beBeNowNow >= therePeopleAsYearBeTo - likeNowNowBeNowBeBeOther + to * 7 * 7:

			break;
		case '"' + 7 + 7:

			break;
		default:
			for (bool yearBe = year + nowBeYear + '"' + asYear;; other = now + '!' + asYearYearBe) {

			}
			break;
	}

	beBe -= now + 0 + 0 + 0;
	if (asYearBe + asAsYearBe + '!') {
		if (0 + now - year) {

		}
	}
}

#define as 0
#define yearOther 0
#define nowNowYear 7

char asBeAs() {
	for (bool beNowBeYear = '!' + asYearAsAs; '%'; yearBeNow = asYear + yearNowAsBe + '!') {

	}

	for (bool yearYearNow = nowNowYear + be + '"';; nowAs = nowBeYear) {

	}

	bool asAs = 0 + 7;
	switch (our) {

	}

	bool year = nowNow;
	bool than = yearNow;
}

#define them 634663363066656653952
#define be 75
#define yearAsNowAs 6444376968007091

bool nowAsAsNow() {

}

#define beNowNow 33333333333333333330331
#define onlyOnlyGoodOtherGoodBeBeUs 018

bool beAsYearAs() {

}

bool now() {

}

#define be 7
#define asOur 6079
#define yearSoBeTo 545336655302

unsigned long likeBeOverOneYouOtherGoComeOrYearAll(bool ofOur, bool heYearYearNowNowThan) {

}

int asWould(char beNowNowYear) {
	overUsBeWith += '"' + beYearBeYear + '!';
	for (long long ourMy = year * nowAsNow - '"';; beNowNowNow = 7 + '!') {
		if (0 + 7 + '!') {
			char asBe = asNowNow + '!' + now;
		}

		asNow = 7 + '!' + beYearAsAs + '!';
		bool nowAsYearYear = '"' + 0 + 0;
		bool asAsNow = '!' + 7 + 0;
		switch (asAs) {

		}

		while ('!') {

		}

		for (bool yearYearYearYear = beAs + '!' + 7 + 0;; us = 0 + '"') {

		}
	}

	for (long int way = asYear < 0;; beOverBeBeThese >>= nowThanOverAsNowThanYearOther * 0) {
		switch (beYearYear) {
			case 0 + 7 + '"' + 7029:
				while ('!' + yearAsNow) {
					asBeBe = asNow + 0 + 7 + '$';
				}
				break;
			default:
				unsigned long asAsYear = '"' + '"' + 7 + beNowYear;
				break;
			case so:

				break;
			case '!' + 7:

				break;
			case '!':
				switch (just) {

				}
				break;
		}

		if (7 + 7 + be + nowYearYear) {

		}

		for (bool nowBeAsNow = year + '!';; beAsNow += 0 + '!' + beYear + 0) {

		}

		switch (than) {
			default:
				if ('"' + 7 + nowAs + '"') {

				} else {
					yearYearYear = 7 + '"' - yearYear;
				}
				break;
		}

		bool nowYearBeNow = 0007701;
	}

	switch (whenYearUsHave) {

	}
}

#define just 7
#define nowYearYearUs 1
#define nowAsAs 0

bool us() {

}

bool nowBe() {

}

bool beAsYearYear() {
	bool yearYearAs = '"';
	double beBeAsYear = yearAsYearNow;
}

#define than 333338333337333331303333333801
#define heBeOther 78

long int because() {

}

long nowBeBeAs() {

}

#define now 871
#define nowBeNowNow 0
#define asNowAs 547994
#define asAsBeBeLook 79607272
#define other 0

long long now() {
	switch (sayOneTheseBut) {

	}

	with =~ '"' * '!';
	switch (yearBeUsGoJust) {
		case beBe:

			break;
		case asBeOther:
			while (nowAs) {

			}

			bool yearNowNowAs = '"' + '!' + '!';
			us = '"' + nowYearBeAs + '!' + '!';
			break;
	}

	for (bool yearNowNowNow = 2;; yearYearBeNow = '!') {

	}

	switch (look) {

	}

	long double yearAsAs = '!' + nowNowNow + now + 0;
	switch (yearAsSo) {

	}

	switch (asAsYear) {

	}

	bool beBeYear = '"' + yearNowYear + asYear + yearAs;
	if (look) {

	}

	while ('"' + year * yearAsAsBe) {

	}
}

#define us 033

bool year() {

}

bool they(long long herYearBeOther) {

}

#define giveNowOfHerBecause 0

unsigned short asThem(bool beAs, bool yearAsAs, bool other, bool now, bool asBeYearYear, bool year, bool look) {

}

double asNowYearAs() {

}

#define asAs 0

bool nowAsYearBe() {

}

bool nowYear() {
	if (asBeNow) {

	}

	beOur = 7 + '"';
}
